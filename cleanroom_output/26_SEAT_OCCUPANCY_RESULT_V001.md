# 26 — Cycle 6 Result: MARKER_OCCUPIES; the Pre-Stage-10 Chain Is Complete

Sealing order: spec 25 (c8dd3615…) → blind adjudication lane (consequence
rule unread by construction; verdict reached with no downstream
consideration; small numeric verifications only) → this result.
`alpha_computed = false`. No value compared to anything (G3).

## Verdict: MARKER_OCCUPIES — g_N·T_R = πħ

Typing forced by sealed text at three independent points, plus one decisive
discovery made while attacking:

1. The seat is sealed as a projector coefficient (N_i = |1⟩⟨1|_i; the
   elimination consumes it as an energy: block [[g_N r₁, −h_s],[−h_s,
   g_N r₂]]).
2. The marker is sealed as exactly that type: (projector coefficient) ×
   (interval) = πħ (closure-gate algebra; bridge gate
   `relative_action_marker_interval = pi*hbar`).
3. The budget is sealed as a different type by its own gate, with an
   explicit prohibition: "No geometric uncertainty budget may be
   substituted for physical action without a theorem" — and the one sealed
   theorem bridging the types (the bridge gate's factor-2 statement)
   assigns the coefficient slot πħ = 2·J_FS.
4. **Convergence lemma (found in adversarial mode):** the budget's own
   normalization condition, applied correctly to the sealed seat
   (ΔH = g_N·ΔN = g_N/2 at the forced balanced calibration), gives
   ∫ΔH dτ = πħ/2 ⟹ **g_N·T_R = πħ**. Every type-consistent sealed route
   converges on the marker value; πħ/2 is reachable for the seat product
   only via a sealed-against representation error. The value πħ/2 is not
   wrong — it correctly normalizes the half-gap coordinate g_N/2.

All constructed attacks failed except the conversion attack, which
**partially succeeded as a conditionality finding**: the realization
τ_orth = T_R is sealed **by adoption** (the Level-1 onset rule + the BID
cell-realization pattern), not derived — three sealed disclaimers quoted on
the record. This conditionality is candidate-symmetric (the budget's
defining integral runs over the same cell), so it cannot alter the typing;
it is carried as an inherited condition on the chain, not repaired.

## Consequence (mechanical, per sealed spec 25)

g_N = πħ/T_R. Note the sealed coincidence now explicit: g_N = m\* = E\*
(all equal π/T_R in ħ = c = 1) — the coupling rate, the mediator gap, and
the edge shell energy are one relational scale.

**The surviving family reduces 4 → 2:**

ρ = (3/16)·g_N²/(m\*·E_ref) with the one remaining fork E_ref ∈ {E\* ≡ m\*, μ}:

- E_ref = E\* (≡ m\*): **ρ = 3/16** (= 0.187500)
- E_ref = μ (= m\*/√2): **ρ = 3√2/16** (≈ 0.265165)

Per standing guardrails, neither value is compared to anything, and neither
may be preferred for any numerical property.

## Status: the pre-Stage-10 chain is complete

Every fork decidable ahead of the response construction has now been
decided by derivation or computation: seat located (c1), scopes reconciled
(c2), form computed (c3), scale-independence proven (c4), mediator scale
decided (c5), seat normalization decided (c6). The single remaining fork —
E_ref — is, by its nature, the **matching question**: which reference rate
normalizes the public transverse response. That is Stage 10 of the locked
plan (Gates 1–5, κ_record, Ward identities, Thomson map), gated behind
Stages 6–9, and it cannot be legitimately decided by any interface
argument. The chain's full conditionality stack (adopted Level-1 onset
rule; A24 conditioning; QFC and DCC as adopted scoped principles; leading-
order status of 3/16 with its O(g) correction) travels with these two
numbers wherever they go. `alpha_computed = false` — and it stays false
until the locked plan's own stages authorize otherwise.
