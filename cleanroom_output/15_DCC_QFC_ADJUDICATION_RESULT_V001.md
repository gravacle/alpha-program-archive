# 15 — Adjudication Result: DCC vs Quasi-Free Completeness

Executed per sealed protocol 14 (29bc10a9…). Three mutually-blind lanes plus
a judge restricted to the sealed resolution rule. No downstream consequence
was consulted by any lane (each verdict's reasoning records this).

## Verdicts

- **Q1: DC1_ADOPTED.** The content-superselection clause is NOT derivable
  from the primitives. Decisive gaps: (a) exact all-time invariance exists
  in the primitive basis only as a **Grant** (the binary gate's assumed
  endpoint-invariance premise — mathematically equivalent to [H,P]=0, so
  the single-cell instance of DC1 is an assumption there, and DC1 is its
  many-cell lift); (b) the primitives' actual durability obligations
  (finite persistence interval, finite-fidelity recoverability) support at
  most a **near-diagonality schema**: a quantified ε-family of
  content-transfer terms survives any finite (T, δ) demand
  (ε ≤ arcsin(√δ)ħ/T degenerate; ε ≤ (Δ/2)√(δ/(1−δ)) gapped — verified
  numerically); exact commutation is the unmandated double limit T→∞, δ→0.
  The exactness of DC1 is its entire cutting power — and it is adopted.
- **Q2: QFC_ADOPTED.** By its own file: "an additional microscopic Gravacle
  premise… does not follow from CAR, locality, or disjoint monoidality
  alone"; the quartic competitor is "rejected_only_by_adopted_quasi_free_
  completeness."
- **Q3: SCOPES_DISJOINT.** The apparent conflict **dissolves under operator
  inspection**: QFC's rejected quartic is λ·n_i n_j ⊗ **I_R on SOURCE
  modes** — it commutes with every record observable N_i (DC1-invisible);
  DCC's admitted diagonal record cross-talk is a *record-only* kernel that
  QFC forbids **only as primitive and expressly permits as effective**
  ("Effective quartic or record-only terms remain permitted when derived by
  eliminating fields or coarse-graining"); QFC's required off-diagonal terms
  are source-orbital hops, outside DC1's post-registration scope. QFC binds
  the primitive pre/at-registration joint action; DCC binds effective
  post-registration record dynamics. No shared scope, no cross-prohibition.

## Resolution (forced by the sealed rule, disjoint-scopes clause)

Both principles **stand, as ADOPTED content, with scopes explicit**: QFC on
the primitive connected source-record action (finite stationary
one-complex branch; durability expressly disclaimed by its own text); DCC on
effective connected generators over completed-registration sectors (exact
clause carried as adopted strengthening; magnitude λ_c not fixed). No
re-scoping, no retirement, **no new fork**.

## The named interface object (the consequential output)

**The effective record-only connected generator on the registration complex
R(K)** — the effective-descendant kernel QFC licenses after eliminating/
coarse-graining the source fields from the one complete action, **seeded by
the induced h_K² two-step propagation across shared source support**. This
one object is QFC's *output* (permitted, magnitude-underived) and DCC's
*input* (the candidate B that DC1–DC4 filter). Consequence, stated
plainly: **λ_c is not a free parameter awaiting a new axiom — it is a
computable quantity**: the induced record-record cross-talk obtained by
integrating out the source sector of the QFC-scoped primitive action. The
"closure equation" for the coupling seat is a *calculation from already-
adopted structure*, not a new principle. That calculation (second-order
source elimination → effective record kernel → DC1–DC4 filter → λ_c form)
is the next sealed object.

## Errata (append-only; sealed files remain unmodified)

- **E1 (corrects OUTPUT/13):** the cycle-1 "unpredicted finding" of a
  *direct conflict* between DCC and QFC is **WITHDRAWN**. It rested on
  misreading which tensor factor QFC's quartic exclusion acts on
  (source-sector n_i n_j ⊗ I_R, not record-sector). The correct statement:
  DCC's surviving seat is the *effective record-only* class QFC explicitly
  permits. The B_λ test's mathematical content (P1–P4) is unaffected.
- **E2 (corrects OUTPUT/11, primitive basis item 3):** the paraphrase
  "non-diagonal dynamics on a comparison produces recurrence" misstates the
  binary gate v002, whose return theorem is derived *under* the granted
  diagonal case and afflicts the phase dynamics DC1 permits; it cannot
  discriminate diagonal from off-diagonal terms. DC1's status is
  correspondingly ADOPTED (per Q1), not motivated-by-theorem.
- **E3 (status metadata):** DC1's exact clause is adopted; any future
  asymptotic replacement must pre-freeze its (T, δ) quantifiers to remain
  fail-closed, and would reopen an ε-neighborhood in every family DCC
  collapsed (including |λ_c| ≤ tol in place of λ_c = 0 on disjoint cells).

## Status

`alpha_computed = false`. No magnitude selected. Next sealed object: the
induced-kernel calculation at the named interface.
