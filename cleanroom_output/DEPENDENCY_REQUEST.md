# DEPENDENCY_REQUEST — Missing items, roles, and reasons

Per DEPENDENCY_POLICY.md. No parent/sibling directory was inspected; no
missing item was replaced by a convenient assumption. A curator may add
hash-verified, target-independent copies and reissue the manifest.

## Needed for conceptual derivation

1. **V156 single-handle face result with hypotheses H1–H6** — conditional
   base of the bridge gate (ALLOWED_INPUTS #2). Authority: sealed pre-alpha
   result. Without it the bridge gate's conditional base is inherited, not
   verified.
2. **COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md** — referenced
   by the transport-only principle as the countermodel family its adoption
   excludes; needed to judge the size of that exclusion.
3. **SOURCE_RECORD_MASS_TRANSFER_GATE_V001.md** — referenced by the
   odd-component identification principle.
4. **BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md** — the BID spec calls its
   disjoint theorem "proved" and frozen there; absent, so least-favorable
   status was applied (`monoidal_disjoint_record_composition_proved=false`).
5. **The onset-saturation defining gate** in ALLOWED_INPUTS form (it exists
   in CURRENT_WORK/provenance as a sealed gate; its classification as
   allowed input vs audited work should be made explicit by the curator).

## Needed for independent verification

6. **The 21 absent subordinate audit scripts** frozen in
   BID_SOURCE_PARENT_SUBORDINATE_OUTPUT_CONTRACT_V001.json (incl. CPT
   intertwiner, axial CP reduction, unique controlled coupling, one-normal
   zero-form enumeration, commutant classification, Hilbertization,
   quasi-free completeness, CAR composition, decorated first-opening
   classification, and 12 more). Without them, 21 of 26 frozen PASS labels
   are unverifiable in-package — this is load-bearing for SP09.
7. **audit_primitive_source_record_chiral_operator_v002.py** — claimed by
   ALLOWED_INPUTS #6 as its executable verification; absent from disk and
   manifest.
8. **The per-gate V013 computation scripts** (source_record_generator
   structure, durable-pointer selector, onset-saturation, flux-conditioned
   write audits + tests) — the V013 narrow results are otherwise verifiable
   only against expected-value dictionaries.
9. **The V010 parent authority quad** (ledger, result, audit, test) — the
   V013 parent chain is otherwise pinned constants only.

## Needed for runtime reproduction (see NEEDS_EXTERNAL_EXECUTION.md)

10. v011-generation launcher/validator sources; v011 execution manifests and
    receipts; failed-run stderr transcript; v012 runtime gate specification
    document (only v010's exists in-package).

## Provenance only

11. An updated MANIFEST.sha256 or curator note resolving the mid-audit
    external rename `ALLOWED_INPUTS/` → `AUDIT_ONLY_ADOPTED_INPUTS/`
    (8 path-level manifest failures; all contents byte-identical; see
    OUTPUT/00 addendum).
