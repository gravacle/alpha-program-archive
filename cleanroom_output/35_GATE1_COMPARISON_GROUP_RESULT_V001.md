# 35 — Cycle 9 Result: The Comparison Group Is U(1), Target-Independently

Sealing order: spec 33 (cdf65998…) → script 34 (0e107dfd…) → this result.
`alpha_computed = false`. No comparison to anything (G3).

## The classification theorem (P1–P3 confirmed)

Given the sealed setting (per-axis phase group = a **closed subgroup of
U(1)**, forced by the imported compact period and the closedness of a
comparison group's character range), the competitors die as predicted:

- **Z₂×Z₂ (sign frame): DEAD by K1.** The sealed V013 quarter-turn
  holonomy operator contains (−iY) with (−iY)² = −I — exact order 4.
  Exponent-2 characters (phases {0, π}) cannot host it, and **rephasing
  cannot reduce its order** (the F1 check passed: order is
  rephasing-invariant).
- **Every finite Z_n (including Z₄): DEAD by K2.** The sealed cycle-7
  chain spectra force cross-cell readout phases like e^{iπ√3}
  (irrationality of √3 and φ² verified by exact residue descent; membership
  in Z_n excluded exactly for all n — spot-verified to n = 2000, orbit
  max-gap 0.002 rad at 5000 multiples). The generated subgroup is dense;
  finite groups are closed; contradiction.
- **Unique survivor per axis: U(1).** Over the three adopted comparison
  axes: the torus — **the action-character model**, exactly the third
  historical candidate.

## What this closes

The oldest structural fork in the program — the three-model
comparison-group fork whose only historical selector was the quarantined,
target-motivated sentence "the action-character route is the cleaner
default because compact U(1) electromagnetism is already established" —
is now closed by **sealed, target-independent structure**: an order-4
holonomy and incommensurate record spectra. **The quarantined selector is
unnecessary.** The U(1) carrier route is forced, not preferred. One of the
original run's deepest target-awareness flags dissolves.

Combined with cycle 8 (differential class = unit-modulus transport with
holonomy the sole freedom) the two gates now interlock: Gate 4 forced the
*form* (compact gauge transport); Gate 1 forces the *group* (U(1) per
axis). The record surface's electromagnetic-shaped kinematics — compact
U(1) gauge structure — is emerging from record principles plus sealed
spectra, not from knowing what electromagnetism is.

## Conditionality (P4, carried not hidden)

Inherited adopted layers: the compact period 2πħ with imported winding
k = +1 (Stage-1 ledger item b); the three-axis/d = 4 carrier (MPCP,
adopted); V013 holonomy scope (order-4 shown rephasing-invariant); the
cycle-7 chain-model scope (A24-conditioned incidence); and the declared
cross-cell readout premise (a comparison group hosts phases read at record
events elsewhere on the complex). Full Gate-1 authority additionally
requires the spec's review/seal process. Within those declared bounds, the
classification is a theorem.
