# FIELD_SIGNATURE_PHYS V003 -- ROOTED FRAME FAMILY, PATH-VISIBLE QUOTIENT, AND RESTORED ENDPOINT INTERTWINER

**PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION (DoR-015 RESERVED)**

**Status:** `V003_COMPLETE_FOR_FINAL_ADVERSARIAL_REVIEW`

**Register head at start:** Q-292, register SHA-256
`5cbee0d840f1636b6bce4ea7b66c343c8828c464587042476f9ead9cc2b6662e`.

**Scope:** bounded repair of V002 after the Q-292 adversarial determination.
This artifact replaces V002 as a proposal. It does not ratify any authored
field, select any member of a proposal family, or execute a physical response.

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

---

## 1. Authority, scope, and declared imports

### 1.1 Frozen authorities

| Authority | SHA-256 | Load-bearing use |
|---|---|---|
| `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V002.md` | `deaa86ee58edb9f841ae3f7bae8ccf9b1cf659328b99fb60cd290a348641e1ad` | six-field merge, derived subtraction, seven seams, doors, and residue reconciliation |
| `STAGE8_FIELD_SIGNATURE_PHYS_V002_ADVERSARIAL_REVIEW_DETERMINATION_V001.md` | `d11c0ffd6b24876ce8da7821fd733c326b41095e8b4a7d3a1e831a19a906641b` | Q-292 S3/S5 refutations, R6 omission, and bounded repair order |
| `STAGE8_TASK4A_RECORD_SURFACE_TO_PHYSICAL_FIELD_SIGNATURE_DERIVATION_AND_BETA_GAP_ATTACK_V001.md` | `65e4dd6a6e2926c9c100edb162f57352311e73438f23dd19509dda0403e2e4f6` | rooted signed incidence and exact R1-R10 residue (`:185-260,588-618`) |
| `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | root/successor roles and primitive tuple (`:587-619`) |
| `STAGE8_TASK4A_P5_MAXIMAL_TRANSPORT_CHAIN_AND_DEPENDENCE_ACCOUNTING_PACKAGE_V002.md` | `31a738ec17696ea01e1cb6a6ee7a37a29e6c0ca24d6fb8cc050d06aed32a583f` | mandatory class-formation door fields (`:572-629`) |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | completed physical quotient, endpoint intertwiners, and raw-G signature (`:641-688`) |
| `STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md` | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4` | `T_cyl` finite cylinder completion and retractions (`:213-243`) |

### 1.2 Roots entered and exclusions

```text
ROOTS_ENTERED = (
  current cleanroom root,
  local Q-292 review mirror,
  alpha_supervision register through Q-292
)

EXCLUSIONS = (
  a32_holdout/custodian_private,
  every response evaluation,
  every value/root/rank-member/background-member selection,
  every measure/contour/boundary/domain construction,
  every register/plan/tracker/git/commit/push act
)
```

### 1.3 Premises beyond the current stack

Three ordinary mathematical facts are imported, with their application scope
declared.

1. A principal U(1) fiber is a U(1)-torsor and has no natural distinguished
   point. This applies because A1 proposes a principal U(1) bundle.
2. A chosen point in a principal fiber defines an intertwiner from the model
   one-dimensional character representation to the associated line fiber.
   This applies only inside the complete frame family introduced below.
3. Pullback along a continuous surjection is an injective isometry on the
   corresponding continuous-function C-star algebra. This applies only to the
   finite framed holonomy maps whose surjectivity is an explicit A2 predicate.

These are mathematical consequences of proposed carriers. They do not derive
the carriers, select a frame, or supply physical dynamics.

```text
UNDECLARED_PREMISE_USED = false | TYPE-S |
  roots: Sections 1-14 and every displayed implication |
  exclusions: no downstream response or evaluation text |
  fences: no value or member selection |
  query: every claim not traced to a frozen authority or one of imports 1-3
```

---

## 2. Exact repair verdict before construction

Q-292 proposed testing whether the derived roots already supply endpoint
frames. They do not.

The sealed root has type

```text
plus_root = |r> in K_cell,
```

with successor `|p>` and ready tensor root `|r>^(x N)`
(`...GEN_OMEGA...V003.md:587-619`). Q-290 types it as a local finite carrier
role (`...BETA_GAP_ATTACK...V001.md:189-209`). A physical endpoint frame would
instead be a point

```text
p_v in (P_M)_(iota_N(v)).
```

No sealed map sends `|r>` to such a point.

### 2.1 No-natural-frame proof

Assume a natural rule selected `p_v` from the principal fiber over the rooted
vertex. For any `h in U(1)`, right translation `R_h` is an automorphism of that
fiber preserving its basepoint and torsor structure. Naturality would require

```text
R_h(p_v)=p_v
```

for every `h`, while the free torsor action gives `R_h(p_v)=p_v h`, equal to
`p_v` only for `h=1`. Therefore no natural selected frame follows from the
rooted basepoint data.

```text
ROOTED_INCIDENCE_SELECTS_PHYSICAL_FIBER_FRAME = false | TYPE-R |
  test: right-translation automorphisms fix the root/basepoint data but move
        every point of the physical U(1) fiber

ROOT_TO_PHYSICAL_FIBER_POINT_MAP_DERIVED = false | TYPE-U |
  would-build: a ratified source-record-to-associated-line realization that
               sends the finite root vector to a physical endpoint-fiber point
```

This negative does not kill the repair. It determines its lawful form: retain
the complete frame family, select no member, and make every construction
equivariant under frame change.

---

## 3. Derived foundation retained exactly

V003 inherits without modification:

```text
D1 = rooted signed local write | TYPE-P | premises: DoR-013;
D2 = finite locality, E_post, faithful character covariance, and N<=M
     zero-extension | TYPE-P | premises: DoR-008/009/013;
D3 = finite CTP branch, metric, ordering, and reality grammar
     | TYPE-P | premises: DoR-008;
D4 = separated T_cyl norm C-star/module completion with natural finite
     retractions and zero common-restriction tail | TYPE-P | DoR-008;
Gate4 = unit-weight covariant incidence modulo vertex rephasing
     | DERIVED_OVER_ENUMERATED_FAMILY.
```

Nothing below upgrades the pre-DoR standing of these objects or reauthors
their content.

```text
DERIVED_FOUNDATION_CHANGED_BY_V003 = false | TYPE-R |
  test: compare D1-D4 and Gate4 declarations against V002 Sections 3.1-3.5
```

---

## 4. Exact V003 proposed tuple and authorship count

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

FIELD_SIGNATURE_PHYS_V003_prop := (
  derived_foundation=(D1,D2,D3,D4,Gate4_invariant),
  authored_remainder=(A1,A2_v003,A3_v003,A4_v003,A5_v003,A6),
  generated_consequences=(FrameFam,EndInt,C_Emb_v003,S1-S7),
  governance=Gov_V003
).
```

The independent authored structure count remains six. The count is not held
fixed by decree:

* the full endpoint-frame family is canonically constructed from the A1
  principal fibers over the A2 realized vertices; it is not an independent
  choice and no member is selected;
* the path-visible quotient is the defining physical-identification clause of
  A3;
* once those premises are present, the associated-line endpoint intertwiner
  and `C_Emb` are mathematical consequences, not independent choices.

The A1 bundle, A2 realization, and A3 physical-identification rule remain
authored proposal content. `FrameFam` and `EndInt` are conditional mathematical
consequences of A1/A2, so they add no seventh authored structure. Calling the
consequences generated does not erase the price of their antecedents.

```text
V003_INDEPENDENT_AUTHORED_STRUCTURE_COUNT = 6 | PROPOSAL-LEVEL COUNT
V003_HIDDEN_AUTHORED_CLAUSE_COUNT = 0 | MECHANICAL_AUDIT
EVERY_AUTHORED_POSITIVE_STANDING = PROPOSED_NOT_ADOPTED
AUTHORED_POSITIVE_MARKED_TYPE_P = false | TYPE-S |
  roots: A1-A6, FrameFam antecedents, visibility relation, seams, and doors |
  exclusions: D1-D4 and Gate4 inherited statements |
  fences: no principal ratification performed |
  query: every positive claim whose existence depends on A1-A6
```

---

## 5. A1-A6, repaired and frozen

### 5.1 A1 -- external physical-background family

A1 is unchanged:

```text
B=(M,g,o,t;P_M),
```

where `M` is smooth and paracompact, `g` is globally hyperbolic with the live
four-dimensional Lorentz signature, `o,t` are orientations, and `P_M` is a
principal U(1) bundle. The whole family remains; no member and no global
trivialization is chosen.

```text
A1_LOCINC4_EXT_PROPOSED = true [PROPOSAL FIELD]
A1_GLOBAL_BUNDLE_TRIVIALIZATION_ASSUMED = false | TYPE-R |
  test: the admitted A1 family still includes the nonzero-Chern-class member
        used by Q-292
A1_MEMBER_SELECTED = false | TYPE-S |
  roots: complete A1 family |
  exclusions: no background evaluation |
  fences: no member selection |
  query: any frozen (M,g,o,t;P_M) member
```

### 5.2 A2_v003 -- rooted realization plus the complete endpoint-frame family

For the existing finite write systems, write
`E_N={e_1,...,e_N}` for the first `N` oriented write edges and retain every
injection

```text
iota_N:K_write,N -> M
```

satisfying V002 R1-R6. Replace R7/R8 and append the frame interface as follows:

```text
R1 vertices map to events;
R2 each oriented write edge maps to a future-directed causal path gamma_e;
R3 each record cell maps to a causally convex support region D_e;
R4 boundary_phys iota_N = iota_N boundary_write;
R5 physical support adjacency pulls back exactly to record adjacency;
R6 iota_M compose j_NM = iota_N under N<=M zero-extension;

R7 Fr_N(P,iota)=product_(v in V(K_write,N)) (P_M)_(iota_N(v));
   every frame tuple p=(p_v)_v remains; none is selected;
R8 frame restriction Fr_M -> Fr_N forgets only vertices beyond N and is
   natural under R6;
R9 the framed scalar transport map H_N defined in Section 6 is continuous and
   onto U(1)^(E_N) at every finite stage;
R10 its differential reaches every admitted finite edge-coordinate direction
    modulo the displayed vertex-frame action;
R11 every member satisfying R1-R10 remains; no realization, filtration,
    frame tuple, or chart is selected.
```

The finite root determines which realized vertex is the root endpoint. It does
not determine a point of that endpoint's physical fiber. Thus R7 is the full
torsor family indexed by derived roots and vertices, not a selected lift.

```text
A2_REAL_EXT_V003_PROPOSED = true [PROPOSAL FIELD]
A2_FRAME_FAMILY_CONSTRUCTED_CONDITIONALLY = true |
  premises: A1 principal fibers and A2 realized finite vertices
A2_FRAME_MEMBER_SELECTED = false | TYPE-S |
  roots: Fr_N for every admitted A1/A2 member |
  exclusions: no global section and no selected endpoint lift |
  fences: family discipline |
  query: a distinguished p_v or coherent p=(p_v)_v
A2_COMPLETE_FAMILY_NONEMPTY_CERTIFICATE_PRESENT = false | TYPE-S |
  roots: R1-R11, choice table, and void conditions |
  exclusions: no post-proposal construction |
  fences: no target-aware supplementation |
  query: one instantiated family member or a theorem proving R1-R10 jointly
```

### 5.3 A3_v003 -- framed transport presentation and path-visible quotient

For a fixed A1/A2 member, let

```text
Conn(P_M) = smooth U(1) connections on P_M,
Gauge_c(P_M) = compactly supported bundle gauge transformations,
X_N = (Conn(P_M) x Fr_N(P,iota))/Gauge_c(P_M),
```

where `Gauge_c` acts diagonally on the connection and the endpoint frames. For
an oriented realized edge `e:s->t`, define `h_e(A,p) in U(1)` uniquely by

```text
PT_A(gamma_e)(p_s)=p_t h_e(A,p).
```

This scalar is invariant under the diagonal physical bundle-gauge action, so
it is well defined on `X_N`. It does not require a global section.

Changing only endpoint frames by the convention

```text
p_v -> p_v g_v^(-1)
```

gives

```text
h_e -> g_t h_e g_s^(-1),
```

exactly the Gate-4 vertex-rephasing law. This residual frame action is kept as
covariance data; it is not silently declared absent.

Define

```text
H_N:X_N -> U(1)^(E_N),
H_N([A,p])=(h_e(A,p))_(e in E_N).
```

Let `X_inf` be the compatible family of all finite framed presentations. The
path-visibility relation is

```text
x ~vis y  iff  H_N(x)=H_N(y) for every finite N,
Conn_fr,vis := X_inf/~vis.
```

Equip `Conn_fr,vis` with the initial/projective topology generated by all
finite `H_N`. Equivalently, it is identified with the image of the total
coordinate map `H=(H_N)_N`, without adjoining its closure.

This is the visibility quotient requested by Q-292. It deletes exactly the
common kernel of every realized finite-path coordinate. If `H_N(x)` and
`H_N(y)` differ for even one `N`, then `x` and `y` remain distinct. Hence no
record-visible content is removed.

```text
A3_CONN_FR_VIS_PROPOSED = true [PROPOSAL FIELD]
A3_VISIBILITY_EQUIVALENCE_PROPOSED = true [PROPOSAL CLAUSE INSIDE A3]
VISIBILITY_QUOTIENT_DELETES_RECORD_VISIBLE_CONTENT = false | TYPE-R |
  test: a single differing finite H_N coordinate prevents equivalence by definition
A3_GLOBAL_GAUGE_REPRESENTATIVE_SELECTED = false | TYPE-S |
  roots: diagonal Gauge_c quotient and full frame family |
  exclusions: no gauge slice chosen |
  fences: no post-output selection |
  query: a distinguished connection or frame representative
A3_COMPLETE_WARD_QUOTIENT_AND_OPERATOR_BUILT = false | TYPE-U |
  would-build: the physical Ward operator, its endpoint-compatible domain,
               residual-frame descent, and the completed raw-G quotient square
```

### 5.4 A4_v003 -- visible source-kernel rigging

The V002 point-arrow is removed. For a tangent perturbation `a` of a
connection chart, define the edge-current functional

```text
u_e(a)=integral_(gamma_e) a.
```

For finite coefficients `c`, the source-kernel map is

```text
iota_src(c)=sum_e c_e u_e,
J_a^mu(x)=sum_e c_(a,e) u_e^mu(x).
```

The second line is kernel notation in the declared test-current pairing; it is
not a map sending one edge label to one point `(mu,x)`.

At a chart point `x`, define the visible tangent carrier by

```text
T_vis,x = T_x X_inf / intersection_N ker(dH_N|x).
```

R10 ensures that finite coordinate directions are represented. The family
`{u_e}` separates `T_vis,x` by construction: if every `u_e([a])=0`, then
`[a]=0` in `T_vis,x`. Freeze the same frame-norm alternatives as V002 and,
for the proposed orthonormal member, set

```text
H_1(B)=Hilbert completion of span{u_e} in the declared frame norm,
U_B:ell^2 -> H_1(B), e_e -> u_e,
H_CTP=H_1,+ direct-sum H_1,-,
J_phys=U_B(ell^1_+ direct-sum ell^1_-),
K_J(c)=iota_src(c),
K_R(R)=U_B R U_B^*.
```

Finite coefficient recovery is

```text
rho_J,N(iota_src(c))=(c_e)_(e in E_N),
```

on the frozen frame family. It is a source-kernel map, not a point-index
bijection.

```text
A4_RIG_VIS_PROPOSED = true [PROPOSAL FIELD]
SOURCE_KERNEL_MAP_WELL_TYPED = true [PROPOSAL-CONDITIONAL CONSTRUCTION]
A4_A5_COORDINATES_SEPARATE_VISIBLE_TANGENT = true |
  PROPOSAL-CONDITIONAL THEOREM
A4_COORDINATES_SEPARATE_UNQUOTIENTED_CONN = false | TYPE-R |
  test: Q-292 outside-support perturbation lies in the visibility kernel
A4_FRAME_NORM_FORCED_BY_INCIDENCE = false | TYPE-U |
  would-build: an independent source-normalization theorem over the complete
               proposed realization family
```

### 5.5 A5_v003 -- visible holonomy cylinders and local affine calculus

V002's global affine display is replaced by a chartwise, quotient-compatible
one. The bounded algebra is generated globally by finite holonomy cylinders

```text
f(H_N([A,p])),  f in C(U(1)^(E_N)),
```

with the Gate-4/frame action represented equivariantly. Around any frozen
reference chart in the nonzero `Log_0` neighborhood, use local phase
coordinates

```text
phi_e([A,p];[A_0,p_0])
  = -i Log_0(h_e(A,p) h_e(A_0,p_0)^(-1)).
```

Their differentials are the A4 currents `u_e`. The local polynomial affine
algebra is represented by multiplication on the common algebraic cylinder
domain. All charts are retained; no reference chart is selected as physical.
Chart transitions are generated by finite U(1) coordinate changes and the D3
branch-exchange involution.

A5 supplies neither a state, measure, scalar physical functional, dynamics,
nor analytic closure.

```text
A5_VISIBLE_ALGEBRA_PROPOSED = true [PROPOSAL FIELD]
A5_GLOBAL_TRIVIALIZATION_USED = false | TYPE-R |
  test: all coordinates are framed holonomies or local relative phases
A5_SCALAR_PHYSICAL_FUNCTIONAL_BUILT = false | TYPE-U |
  would-build: scalarization, measure/contour package, connected subtraction,
               and the physical restriction certificate
```

### 5.6 A6 -- bilocal class

A6 is unchanged except that its source arguments are A4_v003 visible kernels:

```text
E_J,phys=J_phys,
E_R,phys=S_1,sym(H_CTP),
Bil_phys=Bil_cont(J_phys x J_phys;C),
RawGClass=Bil_phys intersect D3-real symmetric forms.
```

The abstract bounded-bilinear class is separated on its finite core. The
physical raw-G image, completed quotient, and restriction square remain
unbuilt.

```text
A6_BILOC_CLASS_PHYS_PROPOSED = true [PROPOSAL FIELD]
A6_RAW_G_PHYSICAL_IMAGE_EXISTS = false | TYPE-U |
  would-build: scalar functional, physical differentiation, connected
               subtraction, completed quotient, and Door-D image certificate
```

---

## 6. R-A construction -- scalar open transport without a global section

For every frame-family member `p`, Section 5.3 gives a scalar `h_e(A,p)` for
each open path. Parallel-transport composition gives

```text
h_(e2 compose e1)(A,p)=h_e2(A,p) h_e1(A,p)
```

whenever the intermediate endpoint frame agrees. Reversing an edge gives the
inverse. The construction covers every admitted A1 bundle because it uses
only points in the finitely many endpoint fibers, not a global section.

The no-choice object is the action groupoid

```text
G_N actson X_N,  G_N=U(1)^(V_N),
```

with scalar coordinate map `H_N` equivariant for the edge coboundary action.
The selected-frame reading is refuted; the full-family/groupoid reading is the
repaired interface.

```text
R_A_SELECTED_ROOT_FRAME_REPAIR_HOLDS = false | TYPE-R |
  test: Section 2 no-natural-frame proof
R_A_FULL_FRAME_FAMILY_REPAIR_HOLDS = true |
  PROPOSAL-CONDITIONAL MATHEMATICAL CONSTRUCTION
R_A_GLOBAL_TRIVIALIZATION_PRODUCED = false | TYPE-R |
  test: nontrivial A1 bundles remain admitted and only finite endpoint fibers are used
```

### 6.1 `C_Emb_v003`

By A2-R9, each finite `H_N` is continuous and onto. Therefore

```text
Emb_N:C(U(1)^(E_N))->C_b(X_N),
Emb_N(f)=f compose H_N
```

is an injective isometry. It is equivariant under the frame/vertex action.
A2-R6/R8 give naturality, and D4 gives the unique norm-continuous map

```text
Emb_cyl:T_cyl -> C_b(Conn_fr,vis,+ x Conn_fr,vis,-)
```

on the framed visibility presentation. Gauge-invariant consumers must later
apply the residual-frame/Ward descent; V003 does not pretend that operation is
already built.

```text
C_EMB_V003_CONDITIONAL_THEOREM_PROVED = true |
  premises: A2-R6/R8/R9, A3_v003, D4
C_EMB_V003_IS_INDEPENDENT_AUTHORED_CHOICE = false | TYPE-R |
  test: pullback and D4 universality fix it once the proposed premises are fixed
C_EMB_V003_IS_COMPLETED_WARD_DESCENT = false | TYPE-R |
  test: residual-frame/Ward quotient remains explicitly TYPE-U
```

---

## 7. R-B construction -- visibility separation and consumer audit

### 7.1 Exact quotient theorem

Let `q_vis:X_inf->Conn_fr,vis` be the quotient map. By its defining relation,
the total finite-coordinate family factors as

```text
H=Hbar compose q_vis
```

and `Hbar` is injective. Hence:

```text
Hbar([x])=Hbar([y]) implies [x]=[y].
```

The same statement at tangent level is

```text
[a] in T_vis,x and u_e([a])=0 for every e imply [a]=0.
```

This proves A4/A5 separation on the quotient. It does not prove separation on
the unquotiented connection space and does not need to.

### 7.2 What the quotient changes

| Consumer | V003 compatibility with `~vis` |
|---|---|
| A1 background | unchanged; the quotient acts on connection/frame presentations, not `(M,g)` |
| A2 paths/supports | unchanged; `iota_N`, roots, endpoints, and supports remain explicit |
| A3 transport | defining quotient; all finite framed holonomies factor |
| A4 source kernels | factor through the tangent quotient by construction |
| A5 field coordinates | finite holonomy cylinders factor; local phase differentials factor |
| A6 bilocal class | consumes only A4 visible sources, so it factors |
| `C_Emb` / `T_cyl` | factors through `Hbar`; retains residual frame covariance |
| raw-G signature | permits a completed physical quotient (`RAW...SPEC:657-688`); Ward/domain descent still required |
| measure/contour/boundary/domain | must be quotient-invariant when built; none is built here |
| stationary/Schur and background | must consume quotient classes; `TYPE-U | would-build: stationary lift fiber, 2PI/Schur blocks, and quotient-compatible evaluation map` |

No sealed consumer was found that affirmatively requires two presentations
with identical every-stage framed path data to remain physically distinct.
This is a scoped search result, not a claim about future physics.

```text
SEALED_CONSUMER_REQUIRING_SUBVISIBILITY_CONNECTION_DATA_FOUND = false | TYPE-S |
  roots: V002 consumers, Q-288 six accounts, raw-G map spec:641-688,
         Q-290 R1-R10, T_cyl theorem:213-299 |
  exclusions: objects not yet authored and a future principal ruling |
  fences: no response construction or evaluation |
  query: consume or distinguish connection data outside all realized paths;
         off-path; sub-quotient; full Conn modulo gauge; local source completeness

R_B_VISIBILITY_QUOTIENT_SEPARATES_COORDINATES = true |
  PROPOSAL-CONDITIONAL THEOREM
R_B_CONSUMER_REQUIRES_INVISIBLE_DATA = NO_VERDICT |
  prerequisite: a future consumer may explicitly author such a requirement,
                but no current sealed consumer does
```

---

## 8. R-C construction -- restored physical endpoint intertwiner

For the unit character `chi` used by the finite transport, form the associated
line at each realized vertex

```text
L_v=(P_M)_(iota_N(v)) x_ch C.
```

For every member `p_v` of the complete endpoint-frame family, define

```text
I_(v,p):C_chi -> L_v,
I_(v,p)(z)=[p_v,z].
```

This is an isomorphism. Under `p_v->p_v g_v^(-1)`, the intertwiner transforms
by the corresponding character action, so the family is equivariant and no
member is preferred. Parallel transport obeys the commuting square

```text
PT_A(gamma_e) compose I_(s,p)
  = I_(t,p) compose multiplication_by(h_e(A,p)).
```

This is the physical endpoint intertwiner omitted from V002 R6. Its
provenance is split exactly:

```text
endpoint vertex/root labels = D1/D2 | TYPE-P;
physical fiber and realization = A1/A2 | PROPOSED;
full frame family = A2_v003 | PROPOSED;
associated-line intertwiner family = mathematical consequence of those premises.
```

```text
R6_ENDPOINT_INTERTWINER_RESTORED = true |
  PROPOSAL-CONDITIONAL MATHEMATICAL CONSTRUCTION
R6_ENDPOINT_INTERTWINER_DERIVED_FROM_ROOT_ALONE = false | TYPE-R |
  test: root data contain no physical principal-fiber point or associated line
R6_WARD_OPERATOR_AND_DOMAIN_BUILT = false | TYPE-U |
  would-build: residual-frame descent, Ward generator, common operator domain,
               and completed quotient/restriction theorem
```

---

## 9. Choice table and true price

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
```

| Field | V003 proposed choice | Live alternatives considered | Minimality / price | Void condition |
|---|---|---|---|---|
| A1 | full globally hyperbolic oriented Lorentzian U(1)-bundle family | nonsmooth causal locale; fixed member; Euclidean family | supplies the external point/support and causal grammar openly; no member or trivialization | family empty or raw-G signature mismatch |
| A2 | complete causal/support realization; its **full** natural finite endpoint-frame family is retained as a generated interface | selected global section; selected endpoint frames; endpoint-transport groupoid with no scalar presentation | full family is the least scalarization repair that chooses no frame and works on nontrivial bundles; only its A1/A2 antecedents are authored | R1-R10 conflict or family empty |
| A3 | diagonal-gauge framed transport presentation, then exact path-visible quotient | unquotiented full connection; ordinary gauge quotient only; loop-only quotient; curvature-only carrier | removes exactly every-stage invisible freedom and no visible coordinate | one finite coordinate is deleted or a sealed consumer requires invisible data |
| A4 | source-kernel map and separated visible tangent rigging in one declared frame norm | Parseval/Riesz frame; arbitrary local test-current space; no Hilbert rigging | repairs the point-index type error and gives failure-capable finite reach | kernel map discontinuous, R10 fails, or norm is output-dependent |
| A5 | global bounded framed-holonomy cylinders plus chartwise relative-phase multiplication calculus | global affine trivialization; Weyl/CCR; bounded cylinders only | avoids a global bundle trivialization and preforms no state/dynamics | chart transitions violate D3 or require hidden measure/state |
| A6 | trace-class symmetric source and continuous bilinear raw class on A4_v003 | Hilbert-Schmidt; distributional dual; unrestricted algebraic bilinear | same minimal separated abstract raw class as V002 | finite core fails separation or hidden completion invoked |

The rejected selected-frame alternative would add a physical datum. The
chosen full-family interface adds no selected value, but ratification would
still adopt the proposition that endpoint frames belong to the presentation
used to compare record transport with the field layer.

```text
CHOICE_TABLE_COMPLETE_FOR_Q292_REPAIRS = true | MECHANICAL_AUDIT
TARGET_AWARE_AUTHORED_CHOICE_FOUND = false | TYPE-S |
  roots: complete A1-A6 definitions and choice table |
  exclusions: downstream consequence prose |
  fences: no value/evaluation act |
  query: p_ch; alpha; stiffness; response value; residual value; measured target
```

---

## 10. Seven-seam rerun on V003

### S1 -- orientation to causal path

A2-R2 still requires every derived oriented write edge to map to a
future-directed physical path with the same endpoints.

```text
S1_ORIENTATION_TO_CAUSAL_PATH = PASS_AS_PROPOSAL |
  premise: A2-R2 and E_post
```

### S2 -- incidence/locality to support

A2-R4/R5 still require the physical boundary and adjacency pullbacks to equal
the inherited write boundary and adjacency.

```text
S2_INCIDENCE_TO_SUPPORT = PASS_AS_PROPOSAL |
  premise: an A2 member satisfying R3-R5
```

### S3 -- Gate-4 transport to framed connection transport

Define `T_iota,p(U_e)=h_e(A,p)`. Section 5.3 proves

```text
T_iota,p(g_t U_e g_s^(-1))
  = g_t h_e(A,p) g_s^(-1).
```

The endpoint-intertwiner square in Section 8 proves equality of the finite
edge map and physical parallel transport on associated lines. No global
trivialization or selected frame is used.

```text
S3_CONNECTION_INCIDENCE_SEAM = PASS_AS_PROPOSAL |
  premise: A1, A2-R7/R8, A3_v003, and Section 8 intertwiners
S3_SCALAR_MAP_CANONICAL_WITHOUT_FRAME_FAMILY = false | TYPE-R |
  test: Q-292 nontrivial-bundle counterexample remains valid
```

### S4 -- D3 reality to A5

Branch exchange plus conjugation acts on framed holonomy cylinders and local
relative phases exactly as on V002's algebraic cylinder domain.

```text
S4_CTP_REALITY_REPRESENTATION = PASS_AS_PROPOSAL
```

### S5 -- finite labels to physical source kernels

The corrected map is

```text
(a,c) -> J_a^mu(x)=sum_e c_(a,e) u_e^mu(x),
```

with finite recovery by `rho_J,N`. It preserves branch order and separates
the visible tangent quotient. It makes no point-index bijection claim.

```text
S5_SOURCE_KERNEL_AND_VISIBLE_SEPARATION = PASS_AS_PROPOSAL |
  premise: A2-R10, A3 visibility quotient, and A4 frame norm
S5_EDGE_LABEL_IS_ONE_SPACETIME_POINT = false | TYPE-R |
  test: an edge current has extended support
```

### S6 -- zero-extension to source rigging

The frame restrictions and A2-R6/R8 give

```text
U_B compose j_NM = inclusion_NM^vis compose U_B
```

on the finite core, and likewise for finite matrix corners.

```text
S6_ZERO_EXTENSION_TO_SOURCE_RIGGING = PASS_AS_PROPOSAL
```

### S7 -- T_cyl to framed physical holonomy cylinders

A2-R9 supplies the scalar continuous surjection missing in V002. Section 6.1
therefore applies the pullback-isometry theorem and D4 universality.

```text
S7_TCYL_TO_FRAMED_VISIBILITY_CYLINDERS = PASS_AS_PROPOSAL |
  premise: A2-R6/R8/R9, A3_v003, and D4
S7_CREATED_TAIL_IMAGE = zero | PROPOSAL-CONDITIONAL THEOREM
S7_COMPLETED_WARD_INVARIANT_DESCENT = false | TYPE-U |
  would-build: residual-frame quotient, Ward-domain theorem, and physical
               restriction square
```

### Seam aggregate

```text
SEVEN_SEAMS_V003 = (
  S1=conditional-pass,
  S2=conditional-pass,
  S3=conditional-pass-on-full-frame-family,
  S4=conditional-pass,
  S5=conditional-pass-on-visibility-quotient,
  S6=conditional-pass,
  S7=conditional-pass-on-framed-cylinders
)

SEAMS_PROVE_A1_A6_EXIST = false | TYPE-R |
  test: every seam retains at least one proposed existence antecedent
```

---

## 11. R1-R10 reconciliation, with R5 and R6 corrected

| Q-290 residue | V003 interface | Standing |
|---|---|---|
| R1 public geometric/support realization | A1+A2 | proposed family |
| R2 physical field/source intertwiner | A3+A4+A5, S3/S5/S6 | proposed; conditional maps constructed |
| R3 external support and `(mu,x)` realization | A1+A2+A4 source-kernel map | proposed kernel realization, no point-bijection claim |
| R4 causal-support structure | A1+A2, S1 | `TYPE-U | would-build: interacting retarded boundary-value prescription on the proposed causal carrier` |
| R5 common scalar physical CTP representation | A5 supplies only an algebraic multiplication representation; scalar functional remains absent | `TYPE-U | would-build: scalarization functional with state/measure provenance` |
| R6 physical quotient, Ward operator, endpoint maps | A3 path-visible presentation quotient + Section 8 endpoint intertwiners; completed Ward quotient/domain absent | endpoint maps restored; `TYPE-U | would-build: Ward generator, common domain, and residual-frame descent` |
| R7 pairing/measure/contact/boundary | A1/A2/A4 carriers only | `TYPE-U | would-build: invariant pairing, measure, contact, boundary, and gluing package` |
| R8 completed bilocal class and lift | A4+A6 | class proposed; `TYPE-U | would-build: physical raw-G lift, image theorem, and restriction square` |
| R9 stationary background/Schur | A1/A5/A6 interface only | `TYPE-U | would-build: stationary lift fiber, 2PI blocks, and Schur map` |
| R10 finite-to-physical restriction theorem | D4+A2-A4+`C_Emb_v003` | framed cylinders conditional; `TYPE-U | would-build: full physical restriction square and Q-279 reproduction` |

```text
R6_PHYSICAL_ENDPOINT_MAP_RECONCILED = true |
  PROPOSAL-CONDITIONAL MATHEMATICAL CONSTRUCTION
R5_SCALAR_FUNCTIONAL_SILENTLY_CLAIMED = false | TYPE-R |
  test: A5 explicitly stops at algebraic multiplication and R5 remains TYPE-U
RESIDUE_ITEM_SILENTLY_DROPPED = false | TYPE-S |
  roots: R1-R10 table, A1-A6, Sections 6-8, and consumer statements |
  exclusions: no later analytic construction |
  fences: no post-output supplementation |
  query: each Q-290 residue against one typed V003 interface
```

---

## 12. Class-formation doors, including the visibility door

### 12.1 Door D0 -- inherited norm cylinder completion

Unchanged from Q-288/Q-292:

```text
DOOR_D0 = CLOSED_SEPARATED_ZERO_CREATED_TAIL
```

### 12.2 Door V -- framed carrier to path-visible quotient

```text
CLASS_FORMATION_DOOR_V := (
  input_class=compatible framed connection presentations X_inf,
  input_topology=initial/projective topology generated by finite H_N,
  input_restrictions=frame forgetting plus finite path restrictions,

  formation_or_completion_operation=exact quotient by intersection_N ker(H_N),
  output_class=Conn_fr,vis identified with image(H),
  output_topology=subspace projective finite-coordinate topology,
  output_restrictions=finite H_N coordinates,

  topology_changed=true,
  every_limit_named=true,
  limit_topology=NOT_APPLICABLE -- no closure is adjoined,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,

  Tail_output={0} relative to all finite H_N by construction,
  created_tail_image=zero -- quotient removes rather than creates common-kernel content,
  class_separation_proved=true,
  quotient_exactness_proved=true,
  closure_uniqueness_proved=NOT_APPLICABLE,

  restriction_square=PASS_AS_PROPOSAL via A2-R6/R8,
  Q279_full_tuple_reproduced=TYPE-U |
    would-build: raw-G image and complete physical restriction tuple,
  common_origin_provenance=TYPE-U |
    would-build: ratified A1/A2/A3 descent and common-origin witness,
  target_independence=PASS,

  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and common-origin provenance
).
```

### 12.3 Door A -- T_cyl to framed visibility cylinders

```text
CLASS_FORMATION_DOOR_A := (
  input_class=T_cyl separated norm C-star,
  input_topology=C-star norm,
  input_restrictions=canonical finite-coordinate retractions,
  formation_or_completion_operation=Emb_cyl framed holonomy pullback,
  output_class=bounded equivariant holonomy cylinders on Conn_fr,vis,+ x Conn_fr,vis,-,
  output_topology=sup norm,
  output_restrictions=finite H_N coordinates,
  topology_changed=false,
  every_limit_named=true,
  limit_topology=C-star norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on A2-R9,
  created_tail_image=zero by injective isometry,
  class_separation_proved=true conditional on A2-R9,
  quotient_exactness_proved=true via Door V,
  closure_uniqueness_proved=true by D4 universality,
  restriction_square=PASS_AS_PROPOSAL,
  Q279_full_tuple_reproduced=TYPE-U |
    would-build: raw-G image and complete physical restriction tuple,
  common_origin_provenance=TYPE-U |
    would-build: ratified source-record-field descent witness,
  target_independence=PASS,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015, Ward descent, and provenance
).
```

### 12.4 Doors B and C

Doors B and C retain the V002/Q-292 conditional proofs, now over A4_v003:

```text
DOOR_B = CONDITIONAL_PASS |
  input/output topology: finite norm -> Hilbert norm |
  Tail_output={0} by dense finite span |
  weak_star=false; bidual=false; distributional_completion=false

DOOR_C = CONDITIONAL_PASS |
  input/output topology: finite trace norm -> trace norm |
  Tail_output={0} by finite-rank density |
  weak_star=false; bidual=false; distributional_completion=false
```

Their physical provenance and Q-279 full-tuple rows remain:

```text
DOORS_B_C_PHYSICAL_PROVENANCE_BUILT = false | TYPE-U |
  would-build: ratified A4 descent and common-origin witness
DOORS_B_C_Q279_FULL_TUPLE_REPRODUCED = false | TYPE-U |
  would-build: physical raw-G image and complete restriction run
```

### 12.5 Door D

The abstract continuous-bilinear class remains separated on A4_v003. The
physical raw-G image remains open.

```text
DOOR_D_ABSTRACT_CLASS_SEPARATED = true |
  PROPOSAL-CONDITIONAL MATHEMATICS
DOOR_D_PHYSICAL_IMAGE = NO_VERDICT |
  prerequisite: scalar physical functional, completed quotient, raw-G lift,
                restrictions, and common-origin certificate
```

### 12.6 Door audit

```text
WEAK_STAR_OR_BIDUAL_STEP_PROPOSED = false | TYPE-S |
  roots: D0, Door V, Doors A-D, and all A1-A6 formation acts |
  exclusions: unbuilt future analytic operations |
  fences: no response construction |
  query: weak-star; bidual; nonseparating extension; class completion

UNFLAGGED_CLASS_FORMATION_STEP_FOUND = false | TYPE-S |
  roots: every quotient, completion, closure, dual, and pullback in V003 |
  exclusions: no operation merely named as TYPE-U |
  fences: Q-288 mandatory schema |
  query: every topology-changing or class-forming arrow
```

---

## 13. Consumer readiness and preserved boundaries

The repaired proposal supplies, conditionally on ratification:

1. a scalar open-path presentation without a global section;
2. endpoint intertwiners natural over the full frame family;
3. an exact path-visible quotient and separated source coordinates;
4. a corrected source-kernel map into the physical compound-index pairing;
5. an applied framed `T_cyl` embedding and finite restrictions.

It does not supply:

```text
PHYSICAL_SCALAR_FUNCTIONAL_BUILT = false | TYPE-U |
  would-build: scalarization on the proposed algebra with provenance
MEASURE_OPERATION_BUILT = false | TYPE-U |
  would-build: invariant descended physical measure and convolution pairing
CONTOUR_OPERATION_BUILT = false | TYPE-U |
  would-build: interacting prescription and named boundary-value topology
BOUNDARY_CONTACT_OPERATION_BUILT = false | TYPE-U |
  would-build: physical contact terms, boundary form, and gluing theorem
DOMAIN_CLOSURE_OPERATION_BUILT = false | TYPE-U |
  would-build: common unbounded domains, closure, and endpoint compatibility
WARD_DESCENT_BUILT = false | TYPE-U |
  would-build: residual-frame Ward generator and completed quotient theorem
RAW_G_PHYSICAL_IMAGE_BUILT = false | TYPE-U |
  would-build: physical functional, derivative, connected subtraction,
               lift, restriction square, and Door-D image theorem
STATIONARY_BACKGROUND_BUILT = false | TYPE-U |
  would-build: STAT_BG_LIFT_FIBER, 2PI blocks, and stationary selector
```

The Q-290 unit-modulus no-go is neither reopened nor repaired. V003 changes
the target carrier and its typing; it does not claim that phases alone provide
the missing amplitude/weight structure.

---

## 14. Kill-passes

### 14.1 Root/frame identity trap

The finite plus root and a physical principal-fiber point remain different
types. Section 2 refutes their direct identification.

### 14.2 No visible deletion

The visibility quotient identifies two presentations only when every finite
`H_N` agrees. A mutation changing one path coordinate survives the quotient.

### 14.3 No hidden global section

Every scalar transport is relative to a finite endpoint-frame tuple. The full
tuple family is retained, and nontrivial bundles remain admitted.

### 14.4 No distributional completion

A4's current notation records the kernel of a finite algebraic source map.
The only completion used there is the declared frame Hilbert norm; no weak-star,
bidual, or distributional completion is taken.

### 14.5 No answer-defined membership

No A1-A6 predicate mentions `p_ch`, alpha, a response value, a target residual,
or a measured constant. R9/R10 are finite reach and covariance conditions,
not output filters.

### 14.6 No scope creep

The four analytic operations, complete Ward descent, raw-G image, physical
background, and response chain all remain outside this proposal.

```text
ROOT_FRAME_TYPE_COLLISION_REMAINS = false | TYPE-R |
  test: Section 2 and Section 8 domain/codomain table
VISIBLE_CONTENT_DELETED_BY_QUOTIENT = false | TYPE-R |
  test: one-coordinate mutation survives
GLOBAL_SECTION_SMUGGLED = false | TYPE-S |
  roots: A1-A3, R-A, S3, C_Emb, and endpoint intertwiner construction |
  exclusions: selected-frame alternative in choice table |
  fences: no trivialization choice |
  query: global section; trivial bundle; selected frame; canonical fiber point
TARGET_TUNING_FOUND = false | TYPE-S |
  roots: every membership predicate and choice rationale |
  exclusions: downstream status statements |
  fences: no evaluation |
  query: p_ch; alpha; measured; response value; root value; stiffness value
```

---

## 15. Updated provenance line and final gate

```text
OBS17_PROVENANCE_LINE_V003 =
  derived record shadow: D1-D4 plus Gate4 invariant;
  authored external amplitude/weight layer: six independent structures A1-A6,
    including the path-visible physical-identification rule inside A3;
  generated proposal-conditional consequences: the complete endpoint-frame
    family from A1/A2, endpoint intertwiners, source-kernel separation, C_Emb,
    and seven seam maps;
  common-origin physical descent, complete Ward quotient, four analytic
    operations, raw-G image, and stationary background remain TYPE-U.

R_A_REPAIR = COMPLETE_AS_FULL_FAMILY_NOT_SELECTED_FRAME
R_B_REPAIR = COMPLETE_AS_PATH_VISIBLE_QUOTIENT
R_C_REPAIR = COMPLETE_AS_ASSOCIATED_LINE_INTERTWINER_FAMILY

SEVEN_SEAMS_RERUN = CONDITIONAL_PASS_ON_V003
DOOR_A_REOPENED = CONDITIONAL_PASS_ON_V003
DOOR_D0 = CLOSED_SEPARATED_ZERO_CREATED_TAIL
DOORS_B_C = CONDITIONAL_PASS
DOOR_D_PHYSICAL_IMAGE = NO_VERDICT |
  prerequisite: scalar physical functional, completed quotient, raw-G lift,
                restrictions, and common-origin certificate

OVERALL_PROPOSAL_STATUS = V003_COMPLETE_FOR_FINAL_ADVERSARIAL_REVIEW
DOR_015_PACKAGE_ISSUED = false | TYPE-C |
  constraint: final independent review and principal ratification have not occurred |
  release: V003 survives final adversarial review and principal issues DoR-015

REGISTER_HEAD_AT_START = Q-292
REGISTER_HEAD_AT_SEND_TIME = Q-292
REGISTER_SHA256_AT_FINAL_CHECK =
  5cbee0d840f1636b6bce4ea7b66c343c8828c464587042476f9ead9cc2b6662e
LATER_BEARING_RULING_CONSUMED = false | TYPE-S |
  roots: alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md through Q-292 |
  exclusions: no unregistered draft or lane artifact |
  fences: no cross-lane coordination |
  query: Q-293; FIELD_SIGNATURE_PHYS V003; DoR-015

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

Custody: seal this proposal, verify its sidecar, mirror artifact and sidecar to
`alpha-program-archive/cleanroom_output/` and
`alpha-program-archive/workspace/`, report hashes and paths, and stop. No
register, plan, tracker, git, commit, push, gate, or deploy action is performed
by this lane.
