# 21 — Cycle 4 Result: The Dimensionless Ratio Is Structural Up To a Finite Fork

Sealing order: spec 19 (816abd2b…) → citation-only enumeration lane (no
arithmetic) → assembly script 20 (922db545…) → this result. All guardrails
G1–G5 held: complete Cartesian product computed, nothing selected or
highlighted by value, no comparison to any measured constant.
`alpha_computed = false`.

## Headline result (P1 confirmed)

**T_R cancels in every combination.** Every sealed candidate for every slot
is relational in the single free interval, so the dimensionless ratio
ρ = (1/4)·g_N²/(h_s·E_ref) is **independent of the continuous scale
freedom**: Families A and C — the two continuous families that returned
BLOCKED in the original run — **do not infect the dimensionless coupling**.
The continuous underdetermination of the package has been converted, at the
interface, into a **finite discrete identification fork**.

## Verdict (per G4): DISCRETE SURVIVING FAMILY — BLOCKED at identification

Three sealed forks survive with no in-package selector (enumeration lane,
citations on record):

1. **g_N fork (factor 2):** action marker πħ vs FS budget πħ/2 as the
   phase-coupling normalization. The bridge gate exhibits the distinction
   ("projective speed vs generator gap") without resolving seat occupancy.
   The cycle-3 phase-type-only selection rule cuts the Y-type candidates
   but not this pair.
2. **h_s fork (factor √2):** hop coefficient μ vs spectral gap m_* = √2·μ
   as the mediator scale. The sealed Schur relation ties them; nothing
   seals which occupies the elimination denominator in the full model.
3. **E_ref fork (factor √2, plus one G1-flagged option):** E_*/m_* vs μ
   (≡ ħτ_R/T_R symbolically) as the reference rate; bare ħ/T_R carried as
   flagged (beyond sealed pairing forms).

Surviving family of pure numbers (unflagged): **{1/16, √2/16, 1/8, 1/4,
√2/4, 1/2}** — six exact values spanning a factor of 8. Flagged additions:
{π/16, √2·π/16, π/4, √2·π/4}. The full 12-row table with per-row provenance
is in the sealed script output.

## What decides each fork (declared targets, not begun)

- **h_s fork — mechanically decidable now:** repeat the cycle-3 elimination
  in the *full sealed kernel* K_L (record eigenvalues {−√2, 0, +√2}) rather
  than the minimal two-site model; the calculation itself determines
  whether the induced-kernel denominator is the hop coefficient or the gap.
  This is a computation, not a choice. (Natural cycle 5.)
- **g_N fork:** derive seat occupancy — whether the QFC phase-coupling term
  enters the elimination with the marker or the budget normalization — from
  the full action's coupling term, not from preference.
- **E_ref fork:** this is Stage-10 territory (the Thomson/matching rule
  fixes which reference rate normalizes the public response); it cannot be
  legitimately decided before the response object exists.

## Honest significance statement

Positive content: the coupling ratio is **structural with respect to every
continuous freedom the program has** — a genuinely nontrivial closure
property (it could have failed: one T_R-dependent combination would have
sunk it). What remains is finite, discrete, enumerated, and each fork has a
named, principled decider — two of them computational, one staged.

Boundary: a six-element discrete family is **not** a derived coupling, and
under G3 no member of the family has been compared to anything. The
angles-not-amplitudes finding stands in refined form: the continuous
amplitudes canceled out of the ratio; what survives of the coupling
question is — once again — a finite set of *pure numbers* built from π, 2,
and √2, awaiting selection by derivation, not by taste.
