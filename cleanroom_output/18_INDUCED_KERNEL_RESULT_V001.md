# 18 — Induced-Kernel Result (Cycle 3)

Sealing order: spec 16 sealed (49bd8fbf…) → script 17 written, executed,
sealed (823e2f94…) → this result. `python -O` clean; no measured constant.

## Result

**λ_c = (1/4) · g_N²/h_s** at leading order — the α-seat magnitude is now a
**computed quantity**, not an adopted one, in the minimal faithful H_K
instance: the durable record–record cross-talk induced by eliminating the
one-particle source mediator.

All four sealed predictions confirmed; both failure conditions avoided:

- **P1** ✓ nonzero connected induced kernel (two-step, mediator-suppressed).
- **P2** ✓ **the selection asymmetry** — the writing-type coupling (g_X)
  induces only DC1-inadmissible terms (X₁X₂-type; filtered to exactly
  zero, alone and in mixture); **all durable cross-talk is sourced by the
  phase-type coupling g_N alone**. The registration-writing interaction
  builds records; only phase-modulation couples them durably.
- **P3** ✓ the structural coefficient **c = 1/4 exactly** (SW), independent
  of g_N; hand-derivation confirmed independently by the script.
- **P4** ✓ scale-relative: only λ_c·h_s/g_N² is parameter-free; the
  absolute magnitude traces to the model scales (→ Families A/C).
- **F2** ✓ scheme-independent: exact diagonalization (closed-form 2×2 block
  ground energies; block structure [H,N_i]=0 verified) agrees with SW,
  deviation 6.2e-4 → 3.9e-5 → 1.6e-6 as g_N = 0.2 → 0.05 → 0.01, inside
  the declared O(g²/h²) envelope at every point.

## What this establishes

The coupling seat's *form* is now derived end-to-end from adopted structure:

```
λ_c  =  c · (phase-coupling)² / (mediator scale),   c = 1/4 (structural)
```

— the shape of a second-order mediated interaction: coupling-squared over
mediator energy, exactly the structural silhouette of an electromagnetic
coupling. The chain from cycle 1–3 now reads: durable cross-talk exists
only in the diagonal seat (cycle 1); that seat is QFC's licensed effective
output and DCC's filtered input (cycle 2); its magnitude is computable by
source elimination with a pure structural prefactor and a phase-only
selection rule (cycle 3).

## What remains free — stated plainly

g_N and h_s are free inputs *of the minimal model*. The absolute λ_c
therefore still traces to the package's open scale families (A: closure
magnitude; C: absolute interval). Angles-not-amplitudes is upheld, not
evaded: what the calculation forces is a **pure number (1/4)** and a
**selection rule** (phase-type only), not an absolute magnitude.

## The cycle-4 question (declared, not begun)

In the *actual* QFC primitive action — not the minimal model — the analogs
of g_N and h_s are not obviously independent: the unit character fixes the
charge normalization ("no additional charge-normalization factor may be
inserted later"), and the source kinetic normalization fixes the hop scale.
Whether the **dimensionless ratio** g_N²/(h_s · E_ref) is itself structural
in the full H_K — i.e., whether the same rigidity that forced c = 1/4
extends to the coupling ratio — is the precise question separating "form
computed" from "coupling computed." That is the next sealed object, and it
must be approached with the standing caution: if the full-model ratio comes
out unforced, that is Families A/C reappearing at the interface, and it
gets reported as such. `alpha_computed = false`.
