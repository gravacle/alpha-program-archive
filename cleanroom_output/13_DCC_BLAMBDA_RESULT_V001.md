# 13 — DCC Applied to the B_λ Counterfamily: Result

Sealing order (verifiable from seal files): principle 11 sealed
2026-07-24T02:41:43Z (ec559cb7…) → test script 12 written, executed, sealed
(c92ccf4f…) → this result. All arithmetic exact (Fraction); `python -O`
clean; no measured constant anywhere.

## Outcome vs sealed predictions

| Prediction | Result |
|---|---|
| P1: swap terms violate DC1 | **CONFIRMED** — ‖[B_λ, N_i]‖² = 2 for every λ tested; violation entirely from the swap, λ-independent |
| P2: admissible subfamily is diagonal | **CONFIRMED** — all 6 off-diagonal Hermitian pair-generators fail DC1; all 4 diagonal generators pass; admissible algebra = {Σ_P E(P)\|P⟩⟨P\|} |
| P3: disjoint cells collapse to a point | **CONFIRMED** — λ_c = 0 forced; unique survivor λ = 0 |
| P4: incident cells retain exactly one coefficient | **CONFIRMED** — one-parameter family λ_c survives DC1–DC4; form fixed, magnitude not |

**Family E is reduced from an operator family to a single scalar per
incident pair.** The exhibited counterfamily (swap + λ\|11⟩⟨11\|) is dead:
its swap part violates content superselection for every λ, and its diagonal
part is fixed to λ = 0 for disjoint cells. What survives on incident cells
is exactly one connected coefficient λ_c with a physical identity: the
mutual action-phase rate shift of jointly-registered incident cells —
phase cross-talk.

## Unpredicted finding: DCC contradicts the adopted quasi-free completeness premise

The surviving seat is **exactly the n_i·n_j (quartic-type) diagonal
cross-talk term** (verified: the λ_c seat equals N₁N₂ on two cells) — the
very class of term the package's ADOPTED quasi-free completeness premise
excludes. If DCC is right, the adopted premise excludes the physical
coupling seat; if the premise is right, no durable cross-talk exists at all.
They cannot both stand. This is a genuine fork between an adopted premise
and a proposed principle, to be adjudicated target-independently — recorded
here, not resolved here.

## Honest scope

- The magnitude λ_c was **not** selected, exactly as sealed prediction P4
  anticipated (DC1–DC4 are kinematic/compositional; fixing a magnitude
  requires a dynamical closure). `alpha_computed = false`.
- The two-cell computation is the minimal faithful test, not the L→∞
  construction the spec's durable-interval pass conditions require.
- DCC itself is a proposed Level-1 principle: target-value-free,
  structurally motivated, and now non-vacuous (it kills real competitors)
  and non-trivial (it leaves real structure standing) — passing both of its
  own pre-declared failure conditions — but it is adopted-grade until its
  content-superselection clause is derived from the record primitive rather
  than motivated by it.

## Over-determination check (the tell)

DCC decided **two** independent open items in one application: (a) it
collapsed Family E's operator freedom to one scalar per incident pair, and
(b) it exposed and forces an adjudication of the adopted quasi-free
completeness premise (SP-level structure). It also constrains the Fork-1
closure dynamics' form (content-superselecting, incidence-local). Per the
sealed tell, this qualifies as load-bearing structure, not a single-knob
patch.

## Next objects (in order)

1. Adjudicate DCC vs quasi-free completeness by a target-independent
   argument from the record primitive (does phase-only dynamics on facts
   follow from exp(iS/ħ) + persistence, or is it independent content?).
2. The λ_c closure equation: the self-consistency condition tying λ_c to
   the registration dynamics that produce records (the magnitude-fixing
   equation this program has never had a candidate location for — it now
   has one seat and one variable).
3. The L→∞ durable-interval construction (Fork 2) within the DC1-stable
   sectors, per the spec's own pass conditions.
