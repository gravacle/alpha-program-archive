# 39 — Cycle 11 Specification: Gate-2 First-Opening Accounting v001

Frozen before execution. Executes the mathematical core of BID Gate 2. Two
jobs: (a) the **accounting** — verify every sealed count and spectrum of
the first-opening complex; (b) the **derivation** — the spec declares the
rooted star K₁,₃ as a "target-aware first-opening premise"; test whether
r = 3 is instead a THEOREM given the adopted three-axis comparison layer
(MPCP, disclosed), which would relocate the target-awareness flag onto the
already-disclosed adoption one layer up. **Scope**: theorem over the
enumerated family; full Gate-2 authority requires the spec's reviews.

## Setting and declared premises

- Adopted (inherited, disclosed): three independent commuting comparison
  axes (the MPCP layer; d = 4 carrier).
- Declared reading of "first opening" (sealed premise language "minimal
  first-opening 1-complex"): first = minimal — fewest edges, then fewest
  vertices.
- Declared root-incidence premise (from the sealed root-incidence /
  first-opening documents): comparisons open at the root readout point —
  the three comparison directions are root-incident edge-boundary
  directions.

## Sealed predictions

- **P1 (uniqueness theorem):** among ALL connected simple rooted
  1-complexes hosting three independent root-incident comparison
  directions, the minimal one is **K₁,₃, uniquely up to isomorphism**
  (exhaustive enumeration over all connected graphs within a declared
  bound |V| ≤ 6, |E| ≤ 6, all root choices; independence ⟺ three distinct
  root-incident edges; minimizers collected and checked for unique
  iso-class). Loops, paths, triangles, and r > 3 stars all fail
  independence or minimality.
- **P2 (accounting):** on K₁,₃: dim C₀ = 4, dim C₁ = 3, total 7;
  ∂ has rank 3, ker ∂|C₁ = 0 (tree), H₀ = 1 (connected), Euler
  characteristic 1; full-star singular values {2, 1, 1} (∂ᵀ∂ = I + J,
  eigenvalues {4, 1, 1}); 7-dim B spectrum {±2, ±1, ±1, 0};
  handle-conditioned operator characteristic polynomial λ(λ² − 2) with
  spectrum {−√2, 0, +√2} — every value matching its sealed counterpart.
- **P3 (flag relocation):** with P1, r = 3 is derived GIVEN the three-axis
  layer; the first-opening premise's target-awareness collapses onto the
  disclosed MPCP adoption. The sealed honesty flag
  `seven_dimensional_carrier_counts_as_new_evidence = false` is
  REAFFIRMED, not overturned: the 7-count is a consequence of the adopted
  axis count, evidence for nothing.
- **P4 (conditionality):** the three-axis layer stays adopted; the
  first=minimal reading and root-incidence are declared premises of this
  cycle; no promotion of any count to evidence.

## Failure conditions

- F1: if the enumeration finds a second minimal iso-class, P1 fails —
  report the competitor; no repair.
- F2: any sealed count/spectrum mismatch in P2 is reported as found.
- F3: no comparison to any measured constant; `alpha_computed = false`.
