# 32 — Cycle 8 Result: Gate-4 Differential Uniqueness — Theorem Established Over the Enumerated Family

Sealing order: spec 30 (2f9acdfe…) → script 31 (0510e4b1…; one instrument
repair before sealing — the random test graph initially left the perturbed
edge without a successor, so the violation check exercised nothing; fixed
by selecting an edge that provably participates in a composition; no claim
changed) → this result. `alpha_computed = false`. No comparison (G3).

## Theorem (established, P1–P4 all confirmed)

Over the hostile family enumerated by BID v011 itself — per-edge complex
coefficients (a_e, b_e), arbitrary positive-definite Hilbert forms M₀/M₁,
the D_x continuum, residual phases, the sealed orientation involution —
the sealed constraints force **exactly one normalized differential
equivalence class**:

1. **Interior closure ⟺ a = b, constant on connected complexes.** The
   composite-path boundary carries interior residue (a₁ − b₂)·U₁ψ; closure
   kills it pairwise, and connectivity propagates the constant.
   **The D_x continuum dies for every x ≠ 1** (residue ∝ |√(2−x) − √x|);
   the sole survivor of the spec's named stop-rule counterfamily is x = 1.
2. **Normalization forces |a| = 1**; vertex rephasing removes tree phases;
   **loop holonomy is gauge-invariant and survives**. The unique class is
   **unit-weight covariant incidence modulo gauge — a compact gauge field
   — with holonomy as the sole physical freedom.**
3. **Forbidding lemma at the incidence level (P3):** no independent
   edge/handle magnitude survives C1–C3. What the spec had as an axiom
   ("independent primitive F²… axiomatically disallowed") is, at the
   differential level and within the enumerated family, now **derived**.
4. **Forms cannot reopen the family (P4):** the normalization ratio is
   congruence-invariant.

## Significance, stated within scope

The theorem lands exactly where the program's structure said it should:
the unique differential class is *transport with unit magnitudes and free
holonomy* — i.e., **the transport-only principle re-derived at the
incidence level**, and the gauge-field character of the record surface
*emerging* from closure + normalization rather than being adopted. The
magnitudes (the amplitude freedom the angles-not-amplitudes finding tracks)
are exactly what the theorem kills; the phases (the angular content) are
exactly what it preserves. The spec's own sketch ("naturality, closure
a=b, |a|=1, phase removal") is now verified step by step, as Gate 4
demanded.

## Scope and what remains for Gate-4 authority

This cycle discharges the **differential-uniqueness theorem over the
enumerated family**. Full Gate-4 authority per the spec still requires:
Gates 1–3 (comparison-group, first-opening accounting, Hilbert-functor
uniqueness — the M₀/M₁ *selection*, beyond the congruence-invariance shown
here), the public-collapse covector-ray half, and the three independent
sealed spec reviews. Any competitor OUTSIDE the enumerated family (e.g.,
non-local or higher-degree differentials) is beyond this theorem's reach
and remains governed by the spec's completeness obligations.
