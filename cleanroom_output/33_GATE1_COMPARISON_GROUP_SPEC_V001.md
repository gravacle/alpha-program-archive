# 33 — Cycle 9 Specification: Gate-1 Comparison-Group Classification v001

Frozen before execution. Executes the mathematical core of BID Gate 1: does
a target-independent classification select among the three surviving
comparison-group models of the historical fork (Z₂×Z₂ sign frame; Z₄
cyclic quadrature; continuous action-character torus) — rendering the
quarantined "because electromagnetism is established" sentence
unnecessary? **Scope**: classification over closed subgroups of the
compact per-axis phase group, given the adopted three-axis/d=4 carrier
layer (MPCP — inherited, not derived here); full Gate-1 authority still
requires the spec's review process.

## Setting (sealed inputs only)

Per comparison axis, the phase group G is a **closed subgroup of U(1)**:
compactness is sealed by the unit character with period 2πħ (imported
winding k=+1 — Stage-1 ledger item (b), inherited conditionality);
abelianness by the sealed commuting-generator requirement; closedness
because a comparison group must be topologically closed (a compact group's
character range is closed). The closed subgroups of U(1) are exactly: the
finite cyclic groups Z_n, and U(1) itself. So the classification reduces
to: **do the sealed structures fit in any finite Z_n?**

## The two sealed killers (both target-independent)

- **K1 (quarter-turn holonomy).** The sealed V013 narrow result stores the
  unit-flux record-changing holonomy in the form (π/2)·Q_Σ⊗Y with the
  quarter-turn phase e^{−iπ/2} = −i explicitly present in the sealed
  operator U_χ. A comparison group of exponent 2 (Z₂×Z₂: all characters
  valued ±1, phase set {0, π}) cannot host a quarter turn.
- **K2 (incommensurate sealed spectra).** The cycle-7 sealed chain spectra
  contain the pairs {√3, 1} (P₄) and {φ, 1/φ} (P₅). At the sealed
  record-completion event of the ω = 1 mode (t = π), the ω = √3 mode's
  comparison phase is e^{iπ√3}. Under the cross-cell readout premise
  (declared here: a comparison group must host phases read at record
  events elsewhere on the complex — that is what "public comparison"
  means), G must contain e^{iπ√3}, an element of **infinite order**
  (√3 irrational — exact integer proof), whose orbit is dense in U(1).
  A closed subgroup containing a dense set is U(1). **Every finite Z_n
  dies**, including Z₄.
- **K2′ (stronger, continuous-reading layer).** The onset gate defines
  J_FS as a continuous cell integral ∫dτ ΔH(τ): under the continuous
  reading, G contains a phase **arc**, and a closed subgroup of U(1) with
  an interior point is U(1) outright. K2 is the fallback that holds even
  under an endpoint-only reading.

## Sealed predictions

- **P1:** Z₂×Z₂ dies by K1 (π/2 ∉ {0, π} mod 2π — exact).
- **P2:** every Z_n (all n) dies by K2: √3 and φ² are irrational (exact
  integer arguments verifiable over residues), so the generated phase
  subgroup is dense; finite groups are closed and cannot contain it.
- **P3:** the unique survivor per axis is **U(1)**; over the three adopted
  axes, the torus — i.e., the **action-character model**, selected without
  any reference to electromagnetism. The historically quarantined
  target-motivated selector sentence is rendered unnecessary.
- **P4 (conditionality, declared):** compactness (imported winding) and
  the three-axis carrier remain inherited adopted layers; K1 inherits
  V013's "up to rephasing" scope (rephasing shifts phases but cannot make
  a quarter turn order-2 — to be verified); K2 inherits the cycle-7 chain
  model's scope and the cross-cell readout premise declared above.

## Failure conditions

- F1: if rephasing freedom CAN reduce the sealed quarter turn to exponent
  2, K1 fails — report. If some finite Z_n hosts all sealed phases, P2
  fails — report the n.
- F2: no comparison to any measured constant; `alpha_computed = false`.
