# Stage 8 Task 4a Orientation Sealed-Fact, Reality-Invariance, and p-Verdict Determination V001

Date: 2026-08-02

Lane: CODEX LANE 1

Task: PASTE 394 V002 / Task 4a

Register head tested: Q-311

## 0. Lead finding: new class - covariant but uninstantiated descent

`NEW_CLASS = COVARIANT_BUT_UNINSTANTIATED_DESCENT`

The two proposed routes do not exhaust the mathematical possibilities. The
sealed square fixes an oriented **representative**, including the ordered
`c_square` traversal, and the ratified reality law
relates the two traversal orientations **covariantly**. Those facts remove the
need to select an orientation member for the binary question "does symbolic
`p` occur?" They do not construct the absent relative-history-to-cycle map or
the absent cycle-to-2PI map.

That is a distinct class:

```text
COVARIANT_BUT_UNINSTANTIATED_DESCENT :=
  a family for which the requested verdict predicate is invariant under the
  family symmetry, while the dynamics-bearing map on which the predicate must
  be evaluated does not yet exist.
```

It resists both existing bins:

1. It is not a missing orientation **member**. Reversal cannot change zero
   support or create/remove symbolic `p` dependence.
2. It is not an executable family-wide descent. Equivariance constrains a map
   if the map exists; it does not prove existence, factorization, domains, or
   the stationary 2PI blocks.

The two route verdicts are therefore:

```text
ROUTE_A_SEALED_FACT_ORIENTATION_SLOT = CLOSED_BY_CITATION | TYPE-P
  citation: V011's frozen edge arrows, basis, and ordered holonomy encode
            c_square=(1,-1,1,-1)

ROUTE_A_CLOSES_FULL_DESCENT = false | TYPE-R
  test: the cited target convention supplies no source map, fiber
        factorization, cycle-to-2PI map, or stationary block

ROUTE_B_STRONG_ORIENTATION_BLINDNESS = false | TYPE-R
  test: Phi_(-c)=-Phi_c and u_(-c)=-u_c are ratified, reality-compliant
        counterexamples to "reality-compliant implies orientation-blind"

ROUTE_B_P_SUPPORT_PREDICATE_INVARIANCE = true | TYPE-P |
  premises: V005 cycle reversal plus the Q-306/Q-307 U1 reality package

TWO_PI_DESCENT_EXECUTED = false | TYPE-C |
  constraints: T_N, rho_cycle,2PI, stationary blocks, background, and common
               completed domains remain uninstantiated

P_SURVIVES_IN_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT
P_CANCELS_FROM_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT
```

The exact correction to Q-311 is important: **the orientation slot is sealed,
but the residue is not only that slot**. Orientation selection is also
unnecessary for the verdict predicate, while the equivariant dynamics-bearing
descent remains unbuilt. An authored alternating sign would add nothing and
would not discharge that object.

No descent or physical `p` verdict is installed in this artifact.

## 1. Gates, custody, and scope

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The terminal flags above are process declarations and are Q-54-exempt.

Custody follows
`/Users/bgm/MB Work/alpha_supervision/LOCKED_PROCESS.md`: this lane writes in
the cleanroom, seals and verifies the artifact, mirrors artifact and sidecar to
both archive destinations, reports, and stops. No register, tracker, plan-file,
git, commit, push, gate, or deployment action is performed.

### 1.1 Roots entered

1. `/Users/bgm/MB Work/alpha_supervision/LOCKED_PROCESS.md`
2. `/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md`
3. `STAGE8_TASK4A_COMMON_ORIGIN_ORIENTATION_INTERTWINER_2PI_DESCENT_AND_P_VERDICT_DETERMINATION_V001.md`
4. `STAGE8_TASK4A_CYCLE_KERNEL_RESPONSE_AND_2PI_DESCENT_BRIDGE_DETERMINATION_V001.md`
5. `STAGE8_V011_COMPOSITION_LOOP_MATRIX_EXPERIMENT_RESULT_V001.md`
6. `STAGE8_COMPOSITION_LOOP_STRUCTURAL_PREDICTION_BUILD_V001.md`
7. `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V005.md`
8. `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V007.md`
9. `STAGE8_TASK4A_N_UNIQUENESS_GERM_V007_AND_FULL_DOOR_D_CROSS_VERIFICATION_V001.md`
10. `STAGE8_TASK4A_RATIFIED_BILOCAL_INVOLUTION_REALITY_LAW_DETERMINATION_V001.md`
11. `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/field_access_allow_require_unification_v001.md`
12. `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TREE_LOOP_VERSUS_ALLOW_REQUIRE_ADJUDICATION_EINSTEIN_V001.md`

All unqualified Stage-8 paths are rooted at:

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`

### 1.2 Exclusions

```text
a32_holdout/custodian_private/ entered                         false | TYPE-S
  roots: every path entered above
  exclusions: the fenced directory itself
  fences: never list, enter, or inspect
  query: custody path audit

orientation, cycle basis, anchor, rank, or background selected false | TYPE-S
  roots: all constructions and displays in this artifact
  exclusions: quoted frozen representatives are not installed
  fences: no-selection discipline
  query: any installed family member

physical response, inverse, root, coupling, scale, spectrum,
interval, or measured constant evaluated                        false | TYPE-S
  roots: all operations in this artifact
  exclusions: previously sealed matrix bytes quoted as authority
  fences: construction and proof only
  query: any new barred evaluation
```

The finite exact check performed here enumerates basis signs and verifies
change-of-basis identities. It computes no spectrum, root, coupling, scale, or
physical interval.

## 2. Currency and authorities

At freeze:

```text
REGISTER_HEAD = Q-311
REGISTER_SHA256 = 88e80069d813651eae3547925a12b13893880e7716ddec7837269de41233a0b1
LOCKED_PROCESS_SHA256 = f3bd9a62e172ba58667840e7717eb7ab8cd74be9fbe8d190802794ee5c3377ba
```

| Authority | SHA-256 | Use |
|---|---|---|
| Q-311 common-origin orientation determination | `b556fb2496e2edf32c91824dcbcd3eb842397af25a26d5d62950e2bee19cbfd7` | Missing factorization, orientation, and 2PI fields |
| Q-310 cycle-kernel bridge determination | `a4a4f4e76c22a41caea00c434055099b6b9401626ac6ef523ab3741017b3e03e` | Exact closure split and completed source zero |
| V011 matrix result | `29fcbb76748cec8ecbaa8f9debb1e60736c85dbcf54823cb8076c3f17d6e3ffb` | Frozen oriented representative and matrix signs |
| Composition-loop structural build | `5810377123b823e680891a3337855750ac8f362951dca048dad3a78a34c79d79` | Ordinary incidence and unique cycle line |
| Field signature V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | Cycle family, `d Phi_c=u_c`, reversal seam |
| Source germ V007 | `bd33e54c27ddb8ed5224637d7888a071026db33a1a17c7127a27ffa647b69896` | Live source/output package and exact `q(1-q)` form |
| V007 independent cross-verification | `6e36bf05d653b0874579d796e4e8c3c0a9a9a70ad2cd87fb65a0814fe736d0b5` | Live U1 law and honest physical stop |
| Allow/require origin text | `3316531d77fe8fe1883bc6cf62d6d03280d442279375475641ddfcf8c2f63605` | Exact allow/require wording |
| Tree/loop versus allow/require adjudication | `58fd2e60632633786b49053697170224bcac99b9ea28002752f81f5d26bee4bb` | Existing refutation of topology/require transport |

```text
LATER_BEARING_RULING_FOUND = false | TYPE-S |
  roots: QUESTIONS_SETTLED_REGISTER_V001.md after Q-311
  exclusions: unregistered drafts and relays
  fences: send-time currency
  query: later orientation, cycle descent, U1 reversal, 2PI, or p-verdict ruling
```

## 3. Route (a): what the sealed square actually fixes

### 3.1 A definite representative is sealed

`STAGE8_V011_COMPOSITION_LOOP_MATRIX_EXPERIMENT_RESULT_V001.md:57-79`
declares the four arrows and the ordered holonomy:

```text
e_a0: v_00 -> v_10
e_0b: v_00 -> v_01
e_ab: v_10 -> v_11
e_ba: v_01 -> v_11

u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi).
```

The same source freezes the ordered bases at `:94-99` and prints the exact
twisted incidence and block matrix at `:101-123`. Therefore:

```text
SEALED_K_SQUARE_ORIENTED_REPRESENTATIVE_EXISTS = true | TYPE-P
SEALED_EDGE_ORDER_EXISTS = true | TYPE-P
SEALED_MATRIX_SIGNS_EXIST = true | TYPE-P
SEALED_TRAVERSAL_REPRESENTATIVE = c_square | TYPE-P
```

In the frozen edge order `(e_a0,e_0b,e_ab,e_ba)`, the ordered holonomy

```text
u_ab u_a0 (u_ba u_0b)^(-1)
```

has exponent vector `(1,-1,1,-1)`: U(1) commutativity permits writing its
factors in edge-basis order. This is exactly the structural build's
`c_square`. Route (a) therefore recovers the requested alternating traversal
as a citation-level fact about the matrix experiment's presentation.

### 3.2 The twisted bytes do not isolate geometric orientation by themselves

The structural build distinguishes the ordinary oriented boundary from the
twisted connection-weighted boundary. At
`STAGE8_COMPOSITION_LOOP_STRUCTURAL_PREDICTION_BUILD_V001.md:188-225`:

```text
partial_1 =
[[-1, -1,  0,  0],
 [ 1,  0, -1,  0],
 [ 0,  1,  0, -1],
 [ 0,  0,  1,  1]],

c_square=(1,-1,1,-1),
partial_1 c_square=0.
```

The V011 twisted matrix has last column `(0,0,-1,-1)^T`, whereas the ordinary
boundary has `(0,0,-1,1)^T`. The extra sign is the frozen transport value on
`e_ba`, not a second geometric arrow. Thus:

```text
TWISTED_MATRIX_BYTES_ALONE_SEPARATE_INCIDENCE_SIGN_FROM_TRANSPORT_SIGN
  = false | TYPE-R |
  test: ordinary and twisted last columns above
```

The full sealed source fixes the representative because it separately states
the arrows, gauge, and bases. The matrix bytes alone do not reconstruct that
typing without those declarations.

### 3.3 Exact orientation-family enumeration

Let `s=(s_1,...,s_4)` with each `s_e` in `{+1,-1}` and
`S_s=diag(s_1,...,s_4)`. Reorienting edge basis vectors gives

```text
partial_s = partial_1 S_s,
c_s       = S_s c_square,
partial_s c_s = partial_1 S_s S_s c_square = 0.
```

For the twisted incidence `D`, let

```text
U_s=diag(I_C0,S_s),
B(D)=[[0,D],[D^T,0]].
```

Then exactly:

```text
D_s=D S_s,
B(D_s)=U_s B(D) U_s^(-1).
```

The independent integer check enumerated all edge-basis presentations:

```text
edge_basis_representatives=16
all_cycle_boundaries_zero=true
all_twisted_blocks_related_by_exact_conjugacy=true
traversal_orientations_per_rank_one_cycle=2
```

No matrix invariant under exact conjugacy can select one of the 16 edge-basis
presentations. The rank-one graph's traversal family is the two-element set

```text
O_square={+c_square,-c_square}.
```

This distinguishes 16 presentation conventions from the two physical
traversal orientations. No member of either family is installed here.

```text
SEALED_CHARPOLY_OR_TRACE_DATA_SELECT_EDGE_BASIS_ORIENTATION
  = false | TYPE-R |
  test: exact conjugacy B(D_s)=U_s B(D) U_s^(-1)

SEALED_SPECTRAL_INVARIANTS_SELECT_TRAVERSAL_ORIENTATION
  = false | TYPE-R |
  test: reversal produces the same conjugacy class and inverse holonomy
```

No characteristic polynomial, trace, spectrum, or eigenvalue was recomputed.
The conclusion follows from exact change of basis.

### 3.4 Why copying the sealed representative does not build the descent

Q-311 states at
`STAGE8_TASK4A_COMMON_ORIGIN_ORIENTATION_INTERTWINER_2PI_DESCENT_AND_P_VERDICT_DETERMINATION_V001.md:19-59`
that DoR-013 supplies a ready ray and finite law but no global cycle output,
and that common input induces an inter-image map only after a fiber-
factorization condition. At `:433-440`, the missing data include cell-to-edge,
branch-to-path, full-law transport, state/effect traces, and 2PI tangent data.

Citing V011's target representative fills the orientation convention. It cannot
supply those source-side maps or prove the factorization condition. Defining
the source map by reading the desired V011 target backward is the anti-
relabeling failure already identified at Q-311 `:55-59`.

```text
SEALED_REPRESENTATIVE_IS_A_DYNAMICS_BEARING_INTERTWINER
  = false | TYPE-R |
  test: codomain convention does not supply source map or factorization

ROUTE_A_CLOSES_THE_DESCENT = false | TYPE-R |
  test: representative recovered; every dynamics-bearing arrow remains absent
```

**Route (a) verdict:** the orientation slot closes by citation, with no new
authorship. The spectral outputs do not force that representative, but the
full sealed source explicitly freezes it. The descent is not constructed by
citing the target convention.

## 4. Route (b): reality covariance versus orientation blindness

### 4.1 Reversal is the ratified anti-linear action on cycles

V005 defines, without a cycle-basis choice, at
`STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V005.md:90-124`:

```text
u_c(a)=<c,L_N a>,
c in ker(B_N^T).
```

At `:338-363` it defines the cycle phase and its differential:

```text
Phi_c=-i Log_0 product_e(h_e h_e,0^(-1))^(c_e),
d Phi_c=u_c.
```

Replacing `c` by `-c` gives, on the same local chart,

```text
Hol_(-c)=Hol_c^(-1)=conjugate(Hol_c),
Phi_(-c)=-Phi_c,
u_(-c)=-u_c.
```

The last equality is also immediate from linearity. V005's ratified seam at
`:527-534` states that branch exchange plus conjugation preserves the
invariant cylinder algebra and sends a cycle to its orientation-reversed
cycle. Therefore:

```text
ORIENTATION_REVERSAL_EQUALS_THETA_ON_CYCLE_HOLONOMY = true | TYPE-P |
  premises: V005 cycle calculus and S4 CTP-reality seam

ORIENTATION_FAMILY = {+c_square,-c_square} | TYPE-P |
  premises: rank-one cycle result
```

This proves route (b)(i) as a covariance statement.

### 4.2 Reality compliance does not imply pointwise invariance

The proposed implication

```text
reality-law compliant => orientation-blind
```

is refuted by the cycle apparatus itself. `Phi_c` and `u_c` are admitted,
reality-compatible objects, yet they change sign under reversal. The current
source/output package supplies a second control. The Q-307 cross-verification
at
`STAGE8_TASK4A_N_UNIQUENESS_GERM_V007_AND_FULL_DOOR_D_CROSS_VERIFICATION_V001.md:401-424`
gives

```text
G_(-n)^007(Theta f,Theta h)=conjugate(G_n^007(f,h)),

(i/hbar)G_(-n)^007(Theta f,Theta h)
  =-conjugate((i/hbar)G_n^007(f,h)).
```

Both even and odd transformation laws are reality compliant in their typed
roles. Reality is a covariance law, not a universal fixed-point law.

```text
EVERY_REALITY_COMPLIANT_QUANTITY_IS_ORIENTATION_BLIND
  = false | TYPE-R |
  witnesses: Phi_c, u_c, and the odd condition-6 output

REQUIRE_SIDE_FACT_MUST_BE_POINTWISE_FIXED_BY_EVERY_ALLOW_SIDE_TRANSFORMATION
  = false | TYPE-R |
  test: no such implication appears in the typed reality laws; covariant
        required currents are explicit counterexamples
```

### 4.3 The allow/require slogan cannot supply the missing implication

The origin text says at
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/field_access_allow_require_unification_v001.md:60-68`
that compact U(1) holonomy/flux support is **allowed**, and at `:395-404` says:

```text
allowed U(1) holonomy, required charge/flux access.
```

That sentence distinguishes support from access. It does not state that every
required quantity is invariant rather than covariant under reversal.

The earlier sealed adjudication is directly on point. At
`STAGE8_TREE_LOOP_VERSUS_ALLOW_REQUIRE_ADJUDICATION_EINSTEIN_V001.md:31-67`
it finds that the allow/require filter is trace/spectral rather than graph-
topological. At `:94-120` it separates at least three objects called
allow/require and records an empty qualifying corpus link to cycle rank. At
`:209-216` it names identifying a carrier-existence boundary with a
requirement boundary as the structural Q-69 error.

```text
CORPUS_CONNECTS_ALLOW_REQUIRE_TO_CYCLE_ORIENTATION = false | TYPE-S |
  roots: allow/require origin and sealed adjudication
  exclusions: the PASTE-394 hypothesis itself
  fences: no unproved identity transport
  query: allow/require as a cycle-orientation invariance theorem

ALLOW_REQUIRE_GRAMMAR_PROVES_ORIENTATION_BLINDNESS
  = false | TYPE-R |
  test: the sealed adjudication refutes the required topology transport
```

### 4.4 The weaker family-invariance theorem does prove

Let a typed family obey

```text
F_(-c)(p)=epsilon C(F_c(p)),
```

where `epsilon` is a fixed sign and `C` is a bijective conjugation independent
of the real symbolic parameter `p`. Then:

1. `F_c(p)=0` identically iff `F_(-c)(p)=0` identically.
2. `F_c` is independent of `p` iff `F_(-c)` is independent of `p`.
3. A nonzero symbolic `p`-carrying component exists on one orientation iff its
   conjugate/sign partner exists on the other.

The proof is immediate from bijectivity of `epsilon C`; no division or
normalization is used. Applied to the completed source mixing, Q-311's
`beta_N(k,r)=0` remains zero under `k -> -k`, and its P2-norm extension remains
zero on the complete orientation family.

```text
ZERO_SUPPORT_IS_ORIENTATION_INVARIANT = true | TYPE-P
SYMBOLIC_P_DEPENDENCE_IS_ORIENTATION_INVARIANT = true | TYPE-P
COMPLETED_SOURCE_BETA_ZERO_IS_ORIENTATION_FAMILY_WIDE = true | TYPE-P
```

This is the part of route (b) that lands. It proves that an authored orientation
member cannot decide whether `p` is present. It does not prove what the absent
physical 2PI block is.

### 4.5 Symbol correction

PASTE 394 names the modulus as `q(1-p)`. The live V007 formula is, verbatim at
`STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V007.md:211-244`,

```text
G_n^007=-hbar^2 q(1-q)L_n tensor L_n.
```

The independent verification repeats it at
`STAGE8_TASK4A_N_UNIQUENESS_GERM_V007_AND_FULL_DOOR_D_CROSS_VERIFICATION_V001.md:624-627`.

```text
LIVE_FORM_IS_q_TIMES_1_MINUS_p = false | TYPE-R |
  test: V007 and Q-307 both state q(1-q)
LIVE_FORM_IS_q_TIMES_1_MINUS_q = true | TYPE-P
```

No value of `q` or `p` is selected or evaluated.

## 5. Why the 2PI descent still cannot execute

Q-311's four-carrier table at `:457-490` keeps separate:

1. the origin carrier;
2. the relative-history carrier;
3. the incidence-cycle carrier;
4. the stationary physical 2PI carrier.

Its exact absent fields at `:539-560` are:

```text
T_N,
rho_cycle,2PI,
Gamma_AA, Gamma_AG, Gamma_GG, Gamma_GA,
G_* and the source-free physical background,
common completed domain,
restriction and Tail_R squares.
```

Neither route constructs any item in that list. Route (a) supplies only a
target presentation. Route (b) supplies a covariance theorem that a future map
must satisfy.

The lawful conditional is:

```text
For any instantiated continuous Theta-equivariant descent T_N and
rho_cycle,2PI whose physical mixing block is solely the pushforward of the
completed source beta, the physical cycle mixing is zero, p-free, and
orientation-family-wide.
```

The antecedent is not currently discharged. A zero has a unique pushforward
through a map, but that statement neither creates the map nor excludes
additional physical 2PI terms outside the source pullback.

```text
THETA_EQUIVARIANCE_CREATES_THE_MISSING_MAP = false | TYPE-R |
  test: equivariance is a property of an arrow, not an existence theorem

PHYSICAL_2PI_BLOCK_IS_SOLELY_THE_PUSHFORWARD_OF_SOURCE_BETA
  = false | TYPE-U |
  would-build: stationary block descent and exclusion of additional
               kernel-sector terms

LIFTED_SOURCE_ZERO_IS_PHYSICAL_CYCLE_ZERO = NO_VERDICT
```

The exact remaining object is therefore:

```text
THETA_EQUIVARIANT_RELATIVE_HISTORY_TO_CYCLE_TO_2PI_DESCENT_PACKAGE := (
  f_rel,N and its exact doubled trace/quotient;
  g_cyc,K and global finite-complex assembly;
  a family-wide T_N with no orientation-member selection;
  the fiber-factorization certificate g_cyc=T_N o f_rel;
  branch/path and cell/edge covariance under Theta;
  rho_cycle,2PI;
  the four stationary 2PI blocks;
  physical background and common completed domains;
  restriction, adjoint, Tail_R, and door squares;
  proof that every alpha-facing verdict factors through the family-invariant
  p-support predicate
).
```

This package replaces Q-311's proposed authored-orientation decision. The
orientation field may cite the sealed `c_square` representative, but the map
must be built family-wide; no sign member is needed.

```text
AUTHORED_ORIENTATION_REQUIRED_FOR_P_SUPPORT_PREDICATE
  = false | TYPE-R |
  test: family-invariance theorem

NEW_AUTHORED_ORIENTATION_REQUIRED_FOR_TARGET_CONVENTION
  = false | TYPE-R |
  test: V011's ordered holonomy already freezes c_square

AUTHORED_ORIENTATION_REQUIRED_FOR_FULL_DESCENT = NO_VERDICT

Q311_RESIDUE_IS_ONLY_ONE_ORIENTATION_DATUM
  = false | TYPE-R |
  test: Q-311 itself lists T_N, rho_cycle,2PI, stationary blocks, background,
        domains, and restriction/Tail_R squares; neither route constructs them
```

## 6. Restriction checks and six-account rows

### 6.1 Executed structural checks

| Check | Result | Standing |
|---|---|---|
| frozen V011 edge arrows and basis | representative recovered | **TYPE-P** |
| all 16 edge-basis reversals | ordinary cycles remain closed | **TYPE-P** |
| all 16 twisted block presentations | exact conjugacy | **TYPE-P** |
| two traversal orientations | related by inverse/conjugation | **TYPE-P** |
| `d Phi_c=u_c` under reversal | both sides change sign | **TYPE-P** |
| completed source beta under reversal | zero remains zero, p-free | **TYPE-P** |
| physical cycle 2PI restriction | unexecutable | **TYPE-C**: physical map absent |

No standing finite falsifier fires.

```text
STANDING_FALSIFIER_FIRED = false | TYPE-S |
  roots: the executable checks above
  exclusions: physical 2PI checks are unexecutable, not passing
  fences: no claim beyond finite and completed-source domains
  query: disagreement with a sealed finite restriction
```

### 6.2 Corrected six-account table

| Account | What this artifact proves | What remains absent | Tail/p consequence | Verdict |
|---|---|---|---|---|
| measure | no measure operation used | physical measure in 2PI descent | none created | physical measure **TYPE-U** |
| contour | reversal is the CTP anti-linear partner action | branch/path-to-edge dynamics map | p-support predicate invariant only | map **TYPE-U** |
| boundary/contact | all basis reversals preserve ordinary closure; target representative sealed | source-to-global-cycle boundary certificate | completed source zero unchanged | physical boundary descent **TYPE-U** |
| domain closure | P2-norm completed source zero remains family-wide | common physical 2PI domain | p absent on built source mixing | physical domain **TYPE-U** |
| stationary Schur | no new block or reduction | `rho_cycle,2PI`, four blocks, background | `NO_VERDICT` | **TYPE-U / TYPE-C check** |
| class formation | finite sign family and conjugacy class enumerated; no weak-* step | inter-class equivariant descent | no tail created here | descent class **TYPE-U** |

### 6.3 Door flags

```text
DOOR_D_PHYSICAL_IMAGE_EXECUTED_HERE = false | TYPE-C |
  constraint: no physical cycle 2PI image exists

DOOR_F_PROJECTIVE_LIMIT_INVOKED = false | TYPE-S |
  roots: finite basis changes and existing P2 norm result
  exclusions: unbuilt physical class
  query: projective limit or new completion

WEAK_STAR_OR_BIDUAL_CREATOR_INVOKED = false | TYPE-S |
  roots: every operation executed here
  exclusions: unbuilt physical response class
  query: weak-star, bidual, or distributional class formation

UNFLAGGED_CLASS_FORMATION_FOUND = false | TYPE-S |
  roots: all family, conjugacy, and conditional-descent displays
  exclusions: none
  query: any class formation without a declared topology or status
```

## 7. Final typed ledger

```text
NEW_CLASS = COVARIANT_BUT_UNINSTANTIATED_DESCENT

SEALED_K_SQUARE_ORIENTED_REPRESENTATIVE_EXISTS = true | TYPE-P
SEALED_EDGE_ORDER_EXISTS = true | TYPE-P
SEALED_MATRIX_SIGNS_EXIST = true | TYPE-P
SEALED_TRAVERSAL_REPRESENTATIVE = c_square | TYPE-P

TWISTED_BYTES_ALONE_SEPARATE_INCIDENCE_FROM_TRANSPORT_SIGN
  = false | TYPE-R
SEALED_MATRIX_INVARIANTS_SELECT_EDGE_BASIS_ORIENTATION
  = false | TYPE-R
SEALED_SPECTRAL_INVARIANTS_SELECT_TRAVERSAL_ORIENTATION
  = false | TYPE-R
SEALED_REPRESENTATIVE_IS_A_DYNAMICS_BEARING_INTERTWINER
  = false | TYPE-R
ROUTE_A_SEALED_FACT_ORIENTATION_SLOT = CLOSED_BY_CITATION | TYPE-P
ROUTE_A_CLOSES_THE_DESCENT = false | TYPE-R

ORIENTATION_REVERSAL_EQUALS_THETA_ON_CYCLE_HOLONOMY = true | TYPE-P
ORIENTATION_FAMILY = {+c_square,-c_square} | TYPE-P
EVERY_REALITY_COMPLIANT_QUANTITY_IS_ORIENTATION_BLIND
  = false | TYPE-R
REQUIRE_SIDE_FACT_MUST_BE_POINTWISE_FIXED_BY_ALLOW_SIDE_TRANSFORMATION
  = false | TYPE-R
CORPUS_CONNECTS_ALLOW_REQUIRE_TO_CYCLE_ORIENTATION
  = false | TYPE-S
  roots: allow/require origin plus sealed adjudication
  exclusions: relay hypothesis
  fences: no identity transport
  query: cycle-orientation invariance theorem
ALLOW_REQUIRE_GRAMMAR_PROVES_ORIENTATION_BLINDNESS
  = false | TYPE-R

ZERO_SUPPORT_IS_ORIENTATION_INVARIANT = true | TYPE-P
SYMBOLIC_P_DEPENDENCE_IS_ORIENTATION_INVARIANT = true | TYPE-P
COMPLETED_SOURCE_BETA_ZERO_IS_ORIENTATION_FAMILY_WIDE = true | TYPE-P

LIVE_FORM_IS_q_TIMES_1_MINUS_p = false | TYPE-R
LIVE_FORM_IS_q_TIMES_1_MINUS_q = true | TYPE-P

THETA_EQUIVARIANCE_CREATES_THE_MISSING_MAP = false | TYPE-R
PHYSICAL_2PI_BLOCK_IS_SOLELY_SOURCE_BETA_PUSHFORWARD = false | TYPE-U
  would-build: the complete family-wide descent package in Section 5

AUTHORED_ORIENTATION_REQUIRED_FOR_P_SUPPORT_PREDICATE
  = false | TYPE-R
NEW_AUTHORED_ORIENTATION_REQUIRED_FOR_TARGET_CONVENTION
  = false | TYPE-R
AUTHORED_ORIENTATION_REQUIRED_FOR_FULL_DESCENT = NO_VERDICT
Q311_RESIDUE_IS_ONLY_ONE_ORIENTATION_DATUM = false | TYPE-R

THETA_EQUIVARIANT_RELATIVE_HISTORY_TO_CYCLE_TO_2PI_DESCENT_PACKAGE_BUILT
  = false | TYPE-U
TWO_PI_DESCENT_EXECUTED = false | TYPE-C
PHYSICAL_2PI_MIXING_ZERO_PROVED = false | TYPE-U
PHYSICAL_2PI_MIXING_NONZERO_PROVED = false | TYPE-S
  roots: no instantiated physical mixing block
  exclusions: completed source beta
  fences: no relabeling of source zero as physical dynamics
  query: a physical nonzero cycle-kernel mixing term

P_SURVIVES_IN_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT
P_CANCELS_FROM_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT

SELECTED_ORIENTATION_OR_FAMILY_MEMBER_FOUND = false | TYPE-S
  roots: all constructions in this artifact
  exclusions: frozen representatives quoted but not installed
  fences: no-selection
  query: selected edge orientation, traversal, cycle basis, or realization
```

## 8. Final determination

Route (a) closes the orientation slot by citing V011's frozen `c_square`
traversal; it does not create an intertwiner. Route (b) proves reversal
covariance and the orientation invariance of zero support and symbolic `p`
dependence, but refutes the
stronger claim that every reality-compliant quantity is orientation-blind.
The allow/require language supplies no missing topology theorem.

Therefore an authored orientation is not the next act. The next object is the
family-wide, `Theta`-equivariant relative-history-to-cycle-to-2PI descent
package. Until it exists, the completed source zero cannot be identified with
the physical cycle RetHess, and the physical `p` verdict remains
`NO_VERDICT`.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The terminal flags are Q-54-exempt. The holdout was untouched.
