# 45 — Stage-10 Handoff Brief: The Geometric Frame (for the Continuing Agent)

Audience: the agent (Codex lineage or successor) executing Stages 6–10 of
the locked plan. Self-contained; all paths relative to this OUTPUT/
directory unless noted. This brief INFORMS Stage 10; it decides nothing
Stage 10 must decide. `alpha_computed = false`.

## 1. What Stage 10 inherits from the 12-cycle corpus (files 00–44)

- **The surviving family**: ρ = (3/16)·g_N²/(m_*·E_ref) ∈ {3/16, 3√2/16},
  with exactly one open fork: **E_ref ∈ {E_* ≡ m_*, μ = m_*/√2}** — the
  matching question. Sealed decisions behind it: mediator scale = gap m_*
  (cycle 5, by computation); g_N·T_R = πħ, marker-typed (cycle 6, blind
  adjudication); T_R-independence of ρ (cycle 4 — continuous Families A/C
  cancel). Per result 26: **the E_ref fork may not be decided by interface
  argument — only by the derived response/matching of Stage 10.**
- **Canonical kinematics** (gate cores, cycles 8–12): comparison group
  U(1) per axis; first-opening complex K₁,₃ (r = 3 derived given the
  three-axis layer); Hilbert forms = identity fibers (the sealed
  calibration is a form selector); differential class = unit-weight
  covariant transport, holonomy sole freedom; public-collapse ray =
  registration counting. Conventions are DERIVED — do not re-choose them.
- **Thresholded durability** (cycle 7): exact many-cell orthogonality
  fails for incident complexes (incommensurate spectra); all L→∞
  constructions in Stage 10 must carry pre-frozen (T, δ) quantifiers.
  The spec's durable-interval pass-condition wording requires this repair.
- **The conditionality stack** (travels with every number): adopted
  Level-1 onset rule (τ_orth = T_R by adoption); A24 conditioning; QFC
  and DCC as adopted scoped principles (scopes disjoint, interface named
  in result 15); imported winding k = +1; MPCP three-axis/d = 4 layer;
  vectorlike inventory (undeclared minimality — Stage-1 ledger item a);
  3/16 is leading-order with an O(g) correction (cycle-5 refutation).

## 2. THE GEOMETRIC INSIGHT — two idle structures, flagged as Stage-10 targets

**(A) The causal diamond is present but unconsulted.** Sealed since cycle
3 and re-verified in cycle 7: max radius T_R/2, spatial volume πT_R³/6,
four-volume πT_R⁴/24. The coupling chain (cycles 3–6) used ONLY the
star's internal spectral geometry and phase budgets — the cell's
Lorentzian geometry never entered. The one existing cross-geometry lock
is sealed: **m_*·T_R = π** (mass-geometry ↔ phase-geometry). Stage 10's
continuum response lives ON this diamond; its geometry is expected to
supply the normalization structure the matching rule needs.

**(B) The skeleton-to-cell embedding — where the 4π lives.** The K₁,₃
star was treated combinatorially throughout. But its three leaves are the
three comparison axes (MPCP layer) — the discrete skeleton of the spatial
cell. The Stage-12 formula α(0) = 1/(4π·κ_Thomson) contains a
**solid-angle factor no graph-level computation can produce**. The
embedding of the skeleton into the continuum cell (flux partition of a
source's unit flux — sealed holonomy π/2 per unit flux — across incident
cells; solid angles; the πT³/6 sphere geometry) is exactly where 4π-class
factors enter the Thomson map. This step is Stage 10's, and it has never
been attempted.

**Reframing for Stage 10**: the E_ref fork is geometrically the question
*which cell geometry normalizes the public response* — the ratio of
internal (Fubini–Study/phase) geometry to external (Lorentzian/diamond)
geometry of one record cell. This is the first point in the program where
the **gravity-facing geometry becomes load-bearing for the coupling** —
the co-registration surface doing the work it was named for.

## 3. Discipline requirements (non-negotiable, inherited)

1. Sealed-spec pattern: predictions frozen (hash + UTC) BEFORE any
   computation; failed predictions reported refuted, never repaired.
2. **No selection by value, ever**: embedding/geometric ratios must be
   enumerated blind from sealed structure with ALL combinations computed
   (the cycle-4 pattern, guardrails G1–G5). The diamond is a rich object;
   "search until a ratio lands well" is the numerology cliff.
3. **G3**: no output may be compared to any measured constant before
   Stage 12 authorizes the single comparison. `alpha_computed = false`
   until the locked plan's own stages flip it.
4. Stage gating: Stage 10 requires Stages 6–9 (authority ledger, three
   independent spec reviews, sealed core, complete Q_spec). Prerequisites
   elsewhere: SP14 external run (RESTORE the pinned runtime from
   ~/.Trash/dot-cache-cleanup/codex-runtimes to ~/.cache BEFORE emptying
   the Trash — see NEEDS_EXTERNAL_EXECUTION.md); SP09's 21 missing
   scripts (DEPENDENCY_REQUEST.md).
5. Gate F risk to retire honestly: the finite c·F² deformation
   (`finite_c_F2_deformation_excluded = false`) — the K-ambiguity's last
   stand. Stage 10 must derive its exclusion (K_bare = 0 compositeness +
   cellulation independence) or report the surviving family.
6. Least-favorable status; summaries never stronger than detail; every
   instrument bug disclosed on the seal (three precedents in this corpus).

## 4. Concrete Stage-10 work items under the geometric frame

- W1: Construct the continuum response by embedding the canonical skeleton
  in the causal diamond; enumerate the forced embedding invariants
  (solid-angle fractions, volume ratios, flux partitions) blind — sealed
  prediction list first.
- W2: Derive the Thomson map; the matching rule must FALL OUT of the
  derived response normalization, deciding E_ref ∈ {m_*, μ} — and thereby
  ρ — by derivation.
- W3: Ward identities + cellulation/refinement independence (contact-term
  regulator risk is a known hazard from the parent program's CTP work).
- W4: The c·F² exclusion or its surviving family (Gate F).
- W5: L→∞ statements in thresholded (T, δ) form per cycle 7.
- Exit per the locked plan: one absolute coefficient, no deformation
  family — then Stage 11 (genuine holdout) before any comparison.

## 5. Corpus pointers

PRECOMPARISON_MANIFEST.sha256 (84 entries, all verifying) hash-chains
everything. Key files: 05/NEEDS_THEORY_DECISION (original seven forks);
15 (DCC–QFC interface); 18/24 (λ_c chain); 21/26 (family + fork gating);
29 (thresholded durability); 32/35/38/41/44 (gate cores). Errata and
refutations: 15 (E1–E3), 24 (P3 refuted), instrument repairs disclosed in
24, 38, 32-adjacent. Read 00 and 01 first for provenance and the premise
ledger.
