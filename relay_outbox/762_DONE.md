# RELAY 762 — DONE — DARIO LANE

Task: PASTE 762 / [7A / Q-126 route] — read the mirrored primaries; verify-or-refute 760's raw
tower; scope the public layer; display the normalization's demand.
Lane guard: header names **DARIO**; satisfied. PICKUP-ACK written before source work.
Status: **COMPLETE, SEALED, STOPPED.** Nothing adopted. No register, plan, tracker, or
git action.

## Hashes

```text
INBOX    relay_inbox/RELAY_PASTE_762_DBR_PUBLIC_LAYER_DARIO_V001.md
         6ed2210cb2274d6e25234b4534355bd6e60caf0baf1c31bb5c9aedfb9d51f0e0   sidecar OK

OUTPUT   workspace/STAGE8_7A_DBR_PUBLIC_LAYER_DARIO_V001.md
         beff9f11263e6d9069d77980ea5fc455ae0ea65e1f4ee9629ba6415937a8b075
         453 lines / 26,832 bytes
SEAL     ...md.seal.sha256   shasum -c OK   (name probed ABSENT, recursively, before the write)
```

Fourteen source pins verified. Pin check **14/14**.

## Final lines

```text
PRIMARIES = read (seals verified; principle's fixings displayed)
TOWER_CHECK = DIVERGES (term-by-term)
PUBLIC_LAYER = scoped (none determined; refuted shortcuts displayed)
NORMALIZATION = demand displayed (burden located)
VOID = clean    CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+4 disclosures at §4.3)
```

## The mirror refuted three terms of my own 760 tower

The two-cutoff theorem carries the **locked spectrum**:

```text
lambda_a = p_t² + p_q² + lambda_S2(flux,ell)² + 1 + C2_parent,
lambda_a >= 1 + p_t² + p_q² + ell(ell+1).
```

Against my 760 form: **both circle towers agree; three terms diverge.**

1. **The sphere is twisted and I used the untwisted operator.** `S2_flux` *carries flux* by the
   principle's own words, so its Dirac operator is flux-twisted. The arithmetic shows it: untwisted
   gives `(ell+1)² = ell²+2ell+1`, the locked floor is `ell(ell+1) = ell²+ell` — **strictly
   smaller**, which is what a twist with possible zero modes produces, and why the `+1` is needed.
2. **`C2_parent` is an additive eigenvalue term**, and I had placed `E_parent` in the
   multiplicities only.
3. **The cross term.** `D_BR² = D² + Γ_Σ[D,Φ] + Φ†Φ` — three grades. I assumed the middle
   cancels for constant `Φ`; the principle displays it and leaves `Φ` unselected.

**And one 760 claim was too strong in the cautious direction:** *"multiplicities cannot be pinned
at all"* — the principle **fixes** `E_parent` as the completed `Spin(10)` chiral **16**. Only the
bundle class is free.

The *shape* survives — a discrete sum-of-squares tower — but three of five slots do not.

## What the principle fixes, and the falsifier I had not seen

Factors have roles, and the proper-time factor is **shared, not duplicated**
(`Σ_BR = T²_TQ × S²_flux`). Data must come from *"one spectrum and one induced trace."* The
branch is **ordinary charged durable-source only**. And the route carries its own falsifier:

> *The route fails to compute alpha if two inequivalent choices of radii, spin structure, bundle
> class, or odd profile satisfy all adopted record rules while producing different weighted
> charged determinants … the desired coupling may not select the missing data retroactively.*

The unselected moduli are a **named failure mode**, not an inconvenience.

## Public layer — nothing determined, and the shortcuts displayed

All four TYPE-R refutations are reproduced with their witnesses so nobody re-walks them. Two
observations beyond transcription: **two of them share one root** (`Φ` and the parent sector
unselected — the same missing selection that invalidated my cross-term step), and **the fourth
carries a positive fact** — *"this corpus's raw product carrier **does**"* have a degenerate
lowest eigenspace, so simplicity must be **produced** by the quotient, never inherited.

**Structural reading:** four of the five open elements turn on **one** missing object — a derived
**publicity criterion**. So Fork A (derive it) is *prior in dependency* to Fork B (select branch
data), and Fork B is the route's own named failure mode. Forks displayed; none taken.

## Normalization — β is hidden in a convention, and the burden is located

The locked spectrum is written **with the radii absorbed** (`p_t²`, not `p_t²/R_T²`). That does
not remove β-sensitivity — **it hides it in a unit convention**, and the β-carrying step is
precisely the **restoration** of `R_T, R_flux, R_Q` as proper lengths for `L_BR`'s proper-time
consumer. That is a sharper statement than 760 could make.

**S5.3-class burden, located exactly:** the two-cutoff theorem's **two-parameter net** —
independent `N_T*`, `N_S*`, Cauchy over **asymmetric** cutoff sequences, analytic remainder
certified with floating-point demoted to regressions, and explicit failure required when one
cutoff is frozen. Undischarged; the theorem is **`PROPOSED_NOT_CLOSED`** and on passing earns
neither alpha nor the nonlinear family.

## Custody notes

`PRIN` has **no sidecar** and is authenticated by an **exact digest match to `SPEC`'s sealed
Primary-Authorities manifest** — a lawful path, verified not inferred. `TWO` has **no sidecar and
no digest match**, and is stamped `PROPOSED_NOT_CLOSED`; I therefore used it **only as a test
instrument against my own prior work** — the one direction where an unsealed source can cost me a
result but never grant one. And **the report AM1 names was not mirrored**, so the commissioned
comparison was run against `TWO`'s locked spectrum instead; I say so rather than let it appear
performed.

## Disclosed (four)

This is the **second consecutive relay in which reading a primary reversed part of my own prior
artifact** — and the first where the reversing instrument was handed to me by the registrar in
response to my own custody finding. Also: my 760 restraint on `Φ†Φ` was right for a better reason
than I gave — the witness shows *"the corpus leaves `Φ` unselected and permits a nontrivial
kernel"*, and that same unselected `Φ` is what invalidates my cross-term cancellation. **One
missing selection, two of my errors.**

Nothing written archive-side but the ACK, the artifact, and its seal.
