# RELAY 764 — DONE — DARIO LANE

Task: PASTE 764 / [7A / Q-126 route] — rebuild the raw tower per the locked form; the free-data
ledger against the route's falsifier; beta re-displayed.
Lane guard: header names **DARIO**; satisfied. PICKUP-ACK written before source work.
Status: **COMPLETE, SEALED, STOPPED.** Nothing adopted. No register, plan, tracker, or
git action.

## Hashes

```text
INBOX    relay_inbox/RELAY_PASTE_764_TOWER_REBUILD_DARIO_V001.md
         576b7061d1a326b046edaa6b4baa0c749d22c089cbd1bf5299ea6c11fa3f5df6   sidecar OK

OUTPUT   workspace/STAGE8_7A_TOWER_REBUILD_DARIO_V001.md
         84ceeb49dd282736ce0cb1347e923c8a8c9b1d26ea151ccbb19f6d857dd9e0bf
         455 lines / 26,676 bytes
SEAL     ...md.seal.sha256   shasum -c OK   (name probed ABSENT, recursively, before the write)
```

Twelve source pins verified. Pin check **12/12**.

## Final lines

```text
TOWER = rebuilt per the locked form (twisted floor derived; C2 on the 16; cross term carried)
MULTIPLICITIES = pinned by the 16 (bundle class free, named) — after a ×4 correction
FREE_DATA_LEDGER = displayed (per-item: constraint, lawful selector, falsifier demand)
BETA = re-displayed
VOID = clean    CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+5 disclosures at §5.3)
```

## The tower rebuilds — and the rebuild refuted two claims I would have shipped

**The twisted floor is derived.** For the monopole-twisted Dirac operator on the unit `S²` with
charge `q`: `λ_S²² = l(l+1) − q² + 1/4 = ell(ell + 2|q|)`, an exact reparametrisation
(`= (l+1/2)² − q²`), checked in exact rationals. At `ell = 0` it is **exactly zero** — the
zero-mode level, multiplicity `2|q| = |deg L|`, one chirality, **reproducing the index theorem**.
That is why the locked spectrum needs its `+1`.

**But the locked bound does not say what I drafted, and I withdraw the headline twice over.**

- The principle's *"minimum closed orientable exterior source-flux comparison"* — **"minimum
  closed orientable" modifies a SURFACE.** Closed and orientable are manifold predicates a flux
  does not possess. The phrase constrains `|q|` **not at all**. I read a topological minimality as
  a numerical one.
- The sealed statement is an **inequality**, and `ell(ell+2|q|) ≥ ell(ell+1)` holds for **every**
  `|q| ≥ 1/2` — verified exactly over `|q| = 1/2…4`, `ell = 0…39`. **The floor does not select the
  flux.** Its real content is `|q| ≥ 1/2` — the flux is **nonzero**, an index statement, not a
  magnitude.

**And my multiplicity bookkeeping was wrong by exactly ×4.** Two independent undercounts:
`rank(S_{T²}) = 2` omitted entirely, and the `S²` `±` pairing dropped. My own Weyl-law audit
converged to **4.0000** across four cutoffs: my count reproduced a **rank-16** bundle against the
true `rank(S_Σ ⊗ E_parent) = 4 × 16 = **64**`. **I computed `rank(S_Σ) = 4` in one section and
threw it away in the next** — the 731 failure mode, third occurrence.

Corrected: `mult = [2·N_lattice] × d_S²(ell) × 16`, with `d_S²(ell) = 4(ell+|q|)` for `ell ≥ 1`
and `d_S²(0) = 2|q|`. And the flat `×16` is **conditional**: it needs the flux `U(1)` **external
to `Spin(10)`** with a flat parent connection, since no nonzero `so(10)` generator acts as a
scalar. That condition *is* the bundle class — which is free.

**What survived:** the twisted spectrum (reconstructed two independent ways by the checker,
index-consistent), the reparametrisation identity, `C2(16) = 45/8`, and the spinor-rank
factorisation. I record the survivors too — a verb audit that lists only failures is as skewed as
one that lists none.

One further withdrawal: my inference that a `C2 = 0` sector explains the sealed TYPE-R witness was
**backwards** — `{16,1}` gives `{0, 45/8}`, which **splits**. Adding a `C2 = 0` singlet is when the
Casimir *does* lift. I no longer claim to explain that witness.

## The free-data ledger, read as a kill condition

Five free data — radii, spin structure, bundle class, odd profile, chiral index. **The locked
bound contributes exactly one constraint: `|q| ≥ 1/2`, non-vanishing flux.** `TWO` fixes three of
the five **within its own scope** (periodic spin structure, the `L=2, M=0, q=(±1,0)` mode, the
`+1`) — a scope declaration, **not** progress against the falsifier. Four of the five change the
**spectrum**, not merely its labels. **Nothing in this relay reduces the free set.**

## Beta, sharpened

The three geometric towers scale as `1/R²`; `C2_parent` is an internal `Spin(10)` invariant
carrying **no geometric radius** — `Σ_BR` has no `Spin(10)` directions. So **β acts
non-uniformly**: it moves the geometric towers against the internal Casimir, and is therefore
**not removable by an overall normalisation**. Fixing that ratio is precisely the *"normalized
transfer to the `L_BR` proper-time consumer"*. I display the mechanism and **decline the weight** —
`TWO`'s normalisation is not sealed.

## Disclosed (five)

The headline withdrawn twice; the ×4 undercount; the backwards auxiliary inference; the three
surviving claims recorded; and the standing note that **the checks which reversed me were ones I
commissioned before writing** — five independent adversarial checks, two of which came back
against me.

Nothing written archive-side but the ACK, the artifact, and its seal.
