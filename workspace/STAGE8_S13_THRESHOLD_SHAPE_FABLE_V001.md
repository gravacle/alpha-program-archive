# STAGE8_S13_THRESHOLD_SHAPE_FABLE_V001

LANE: FABLE-S13-1111 (blind record-native constructor). RELAY ROLE: single-fork attack — the
PROVENANCE SHAPE of S13's threshold scale (the first interacting charged pole / infraparticle
threshold), the fork F6a localized by the prior S16 attack. STRUCTURAL SHAPE-TYPING ONLY.
**ALL RESULTS CLAIMED** until the adversarial check.

GATES DECLARED AND HELD: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false; JOINT_ANCHOR_DERIVED = false. **NOTHING WAS COMPUTED — no value,
bound, or estimate of any coupling, kappa, K_*, beta, threshold, eigenvalue, spectrum point,
scale, or alpha; every numeric string below is a sealed-text quotation or a symbol; every scale
symbolic. No measured-constant comparison.** No register, tracker, plan, road, continuation, or
ledger file read. No git action. Anchor sections I–III taken as GIVEN; the prior S16 attack's
F1–F5 typings and localization taken as GIVEN (not re-established).

## SOURCES (all verified at path before use)

```text
ANCHOR alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md
       20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df   MATCHES-TASKED-SEAL
S16    workspace/STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md
       45fa8f6e340f886ce677af917a62377b14f398eb1d682f23f53cd15971d5cb18   MATCHES-TASKED-SEAL
CENSUS workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md
       04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d   MATCHES-TASKED-SEAL
V002   workspace/COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md
       52664ddbd02e04fa2b9f9856f75ee5c1ab2368eb9959495d2c8cab5d568619f2   matches S16's table
UCC    workspace/BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md
       b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f   matches S16's table
CHA    workspace/BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md
       d127d2edec53920ed6cade3635a4cacba369509ad3cca7f41aac76a90718ee1f   matches S16's table
                                                                          (NO SIDECAR, flag carried)
CSP    workspace/BID_CHIRAL_SOURCE_RECORD_INCIDENCE_PARENT_V001.md
       a5105755d2339266a416b632f6da0f2e4f151933518df24a1521768b401927ba
R34    workspace/R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md
       14b807227f5594789b2cab042fe3633640f51921aacf94c72536a17137e72b45
DBR760 workspace/STAGE8_7A_DBR_SPECTRUM_DARIO_V001.md
       a27453107bc97fce05e377b696055c8c24d71d4888e9097fae146a5518cf9e20
DBR789 workspace/STAGE8_7A_DBR_BETA_SECTORS_V3_DARIO_V001.md
       dcb6617452a14c961fac13fee38bc71d3246809d87ffc97c7c93e67b00b352c8
```

CUSTODY FLAGS CARRIED, NOT UPGRADED: CHA has no sidecar (S16 flag (iii), carried at the same
level; nothing below rests solely on it — UCC, sealed, corroborates every CHA clause used).
DBR760 discloses that the D_BR object's DEFINING principle (ae1d0492…) is ABSENT archive-side and
that its structure is derived from sealed quotation; I consume DBR760's disclosed structure at
that custody level and nothing from the absent primaries. DBR760/DBR789 are DARIO-lane relay
artifacts, not BID seals; every clause I take from them is a status/structure clause a later
derivation would have to make good on, and each is marked where used.

---

## 0. LEAD

**S13_SHAPE = LOCK-SHAPED, as a provenance-typing of the DERIVED S13 — the only S13 a derived
S16 can consume.** The record's own structure cannot furnish a fiber-shaped (eigenvalue-type,
R-carrying) threshold at all: its primitive inventory contains no fiber magnitude (a radius is a
metric datum, not a surface datum — ANCHOR I.4), and the weight/amplitude structure a
dimensionful eigenvalue-scale would ride on is refuted at TYPE-R exactness (Q-290, ANCHOR II.6).
What the record's charged-spectrum machinery actually does, in every built exemplar, is fix
threshold-type data against the RECORD INTERVAL as derived pure numbers: `tau_first(B_ch) =
pi/sqrt(2) = tau_R` (UCC, unit-compression class, no numerical multiplier possible);
`spec(c_partial) = {-sqrt(2), 0, +sqrt(2)}` with the incidence-weight family exhausted to unit
magnitudes (CSP); the built interacting band structure whose threshold condition consumes only a
source-mass datum times that derived pure number, with no occurrence of the fiber radius R
anywhere (R34); and the one derived cross-geometry mass lock `m_* T_R = pi` (ANCHOR II.9). The
fiber-shaped alternative exists in the corpus — the D_BR charged-circle tower `(n_Q+a_Q)^2/R_Q^2`
is its concrete face — but ALL of its R-content is adopted/unselected of record (three radii
free, DBR760/DBR789), its normalizing step IS the rail conversion (ANCHOR III.11), and its
threshold map is underived (`quantum_threshold_map_derived = false`, V002 :112). A fiber-shaped
S13 is therefore constructible only ON THE RAIL, and rail beta-sensitivity types the rail, not
the surface. **Consequence: beta cannot enter a derived S16 through the threshold foot ⇒ with
the prior attack's F1–F5 (given), a derived-and-passing S16 is beta-INVARIANT ⇒ Finish B side.**

---

## 1. THE QUESTION AND ITS DISCIPLINE

The prior attack (S16, given) proved every BUILT foot of a derived S16 beta-blind and localized
the only surface-native beta channel to F6a: the provenance of S13's threshold scale. The
executable discipline is the census's occurrence criterion (CENSUS §0, executed twice, four
verifiers): under V002's beta action `R = beta c Delta tau`, `R -> beta R` at fixed `Delta tau`
(T_R and the branch data unchanged), **an object whose derived statement contains no occurrence
of the fiber radius R is beta-blind; one that contains R moves.** Lock-shaped = scale fixed
against the record interval, no R occurrence, threshold-to-cell ratio a derived pure number.
Fiber-shaped = an eigenvalue-type datum of the normalized D_BR spectrum, R-carrying. The typing
below is of PROVENANCE — which named object gives the threshold its scale — never of value.

## 2. THE SURFACE'S PRIMITIVE INVENTORY CONTAINS NO FIBER MAGNITUDE [PROVABLE ground + YOURS corollary]

[PROVABLE] The ground, all booked at the ANCHOR: the record's dynamics is indexed by
connection/holonomy histories alone, with no native metric argument anywhere in the ratified
finite object (I.1); the holonomy characters are unit modulus, transition functions
`g_ij = exp(i theta_ij) in U(1)` (I.3); "Connections on a fixed line bundle form an affine
space; bundle geometry does not choose one. No fiber metric; a radius is a metric datum, not a
surface datum" (I.4, Register :224 as consolidated); the internal/external metric conversion is
a RAIL import, not a surface object (III.11); and "the surface forces the entire DIMENSIONLESS
structure and leaves the absolute scale free" (II.7, II.10 — the free orbit is lambda on T_R,
GATE 8f9bdd93 as consolidated, which the beta action is NOT).

[YOURS — the corollary, marked] A derivation whose every consumed primitive is a surface object
(holonomy characters, incidence operators, record intervals, the discrete charge datum) produces
statements in which the fiber radius R does not occur, because **R is not in the inventory to be
consumed.** Hence: *no surface-derived statement can contain R; a threshold statement containing
R has, of record, rail provenance* (its R entered through an adopted metric datum — exactly
ANCHOR III.11's typing). This corollary rests on the primitive inventory being what the ANCHOR
consolidates; if a future booked surface result added a metric primitive, it would need
re-typing. It is the load-bearing step of the NO in §5 and is flagged as assembly.

## 3. THE AMPLITUDE CHANNEL IS REFUTED AT TYPE-R EXACTNESS [PROVABLE ground + YOURS classification]

[PROVABLE] ANCHOR II.6, booked TYPE-R (refuted, exact mechanism, Q-290 / relay 371 as
consolidated): the write structure carries unit-modulus (phase) weights, not
amplitude/magnitude; "Whatever converts internal to external is NOT a magnitude on record"
(:6093 as consolidated); the write-signature route to beta fails because unit-modulus structure
cannot produce the bounded trace-class -> T_cyl lift — "the physical signature needs
weight/amplitude structure the records provably do not carry."

[YOURS — the classification, marked] A fiber-shaped threshold is a DIMENSIONFUL eigenvalue-scale:
per DBR760 §2.1/AL3 every D_BR eigenvalue is an inverse proper length (`~ 1/R^2` on the square),
i.e. a magnitude datum, and its "normalized" form is reached only through the transfer to
L_BR's PROPER-TIME consumer — the external side of beta's conversion. Such a datum is
weight/amplitude-type in exactly Q-290's sense: it is a modulus, not a phase, and it cannot be
carried by unit-modulus write structure. The record admits eigenvalue data as such —
`spec(c_partial) = {-sqrt(2), 0, +sqrt(2)}` (R34, verified by its executable) IS eigenvalue
data — but only PURE-NUMBER spectra: dimensionless, R-free, lock-material. The record's
phase-rich / amplitude-poor character thus does not merely fail to supply a fiber-shaped
threshold; it is booked, with an exact mechanism, as unable to.

## 4. EVERY BUILT EXEMPLAR OF THE CHARGED-THRESHOLD MACHINERY IS LOCK-SHAPED [PROVABLE]

Four independent built objects, each verified at path, each typed by the occurrence criterion:

**E1 — the primitive charged activation (UCC, sealed).** The controlled charged generator is the
orthogonal compression `B_ch = P_ch tensor B_Q`, coefficients `(a_0, a_+, a_-) = (0, 1, 1)`
forced by the projection-module axioms — "No numerical multiplier is present in either
construction"; the affine solution space is zero-dimensional. Exact evolution gives
`tau_first(B_ch) = pi/sqrt(2) = tau_R`, the record's own least-positive interval, supplied as a
CHECK, not a normalization premise. **Unit-compression class; scale fixed against the record
interval; no R occurrence. LOCK-SHAPED.** (Corroborating CHA, flag carried: `spec(Q_Sigma) =
{-1, 0, +1}` discrete, orientation-only — "The charge sign enters the compact
connection/holonomy orientation, not the primitive incidence magnitude." No continuous fiber
magnitude exists in the charge foot at all.)

**E2 — the incidence-weight exhaustion (CSP).** Before selecting the incidence column the
complete weight family `D_(a,b,C)` is admitted with `a, b in C`; one-record normalization plus
exact completed first transfer FORCE `|a| = |b| = 1` — "the complete magnitude family reduces to
the unit-incidence column without comparing a response value." The joint operator's spectrum is
`{-sqrt(2), -sqrt(2), 0, 0, +sqrt(2), +sqrt(2)}`; the first opening is at `tau_R = pi/sqrt(2)`;
"No independent relative source/record normalization exists in this finite dilation."
**The record actively kills continuous magnitude freedom at the source-record vertex; what
survives is a derived pure-number spectrum against the record interval. LOCK-SHAPED.**

**E3 — the built interacting band structure (R34): the germ of S13's own object.** The one
derived interacting charged-spectrum fragment of record is the translation-invariant Lorentzian
source-record Hamiltonian `H(p) = alpha_D . p + mu S tensor c_partial`, with
`spec(c_partial) = {-sqrt(2), 0, +sqrt(2)}` and band form `H_lambda(p)^2 = |p|^2 + mu^2
lambda^2` (sealed forms, quoted). Its threshold structure is exactly the S13 type-material: the
band edges sit where `p_lambda(E) = sqrt(E^2 - mu^2 lambda^2)` vanishes — a condition consuming
ONLY the source-mass datum `mu` and the derived record pure number `lambda` — and the spectrum
is absolutely continuous with **integrable threshold singularities, not atoms** (the
infraparticle-compatible disjunct of S13, derived, with return amplitude tending to zero).
**No occurrence of the fiber radius R anywhere in H, its bands, or its threshold condition.
The record's entire contribution to where the threshold sits is a derived pure number.
LOCK-SHAPED in provenance.** (R34's own scope discipline carried: this Hamiltonian is not yet
proven to be the complete physical outgoing sector; `complete_parameter_free_Q_spec_frozen =
false`. It is consumed here as the built germ whose provenance structure S13's completion
inherits, not as S13 itself.)

**E4 — the mass-datum lock class (ANCHOR II.9).** The one derived cross-geometry mass junction of
record is `m_* T_R = pi` — "contains no R, trivially beta-invariant" (CENSUS §1.3). The
NON-identification of `m_*` with the charged threshold mass is carried, not violated (that
non-identification is F6a's content, per S16 §8): E4 is consumed only as the exemplar of the
CLASS — on this surface, a derived mass-type datum is fixed as a pure number against the record
interval, because (II.10) the surface forces dimensionless structure only. [YOURS, marked:] any
DERIVED `mu` in E3 therefore lands in this class — `mu` × (record interval) = derived pure
number — which is the lock shape of the threshold-to-cell ratio; its derivation is S13's own
construction and is NOT performed here (§6, fence).

**Inventory result: four built exemplars, all lock-shaped; zero built fiber-shaped exemplars.**

## 5. CAN THE RECORD FURNISH A FIBER-SHAPED THRESHOLD AT ALL? — NO [PROVABLE ground + YOURS assembly]

The fiber shape has a concrete face, and it is not a strawman: DBR760 §2.1 derives (from sealed
quoted geometry, custody flag carried) the raw tower `spec(D_(Sigma,A)^2) = { (n_T+a_T)^2/R_T^2 +
(k+1)^2/r_F^2 + (n_Q+a_Q)^2/R_Q^2 }` on `Sigma_BR = S1_T x S2_flux x S1_Q` — the charged circle
`S1_Q` contributes an explicitly R-carrying tower `(n_Q+a_Q)^2/R_Q^2`. A charged threshold read
off that tower would be fiber-shaped and beta-sensitive (`D_BR^2 ~ beta^(-2)`, DBR760 AL3). Test
whether the RECORD can furnish it — four independent closures, each from a named object:

1. **The R-content is not the record's.** Every R-carrying item is unselected/adopted of record:
   the three radii, both spin structures, the bundle class, the odd profile, the index —
   "unselected" (DBR760 §1.1 quoting GAP's sealed quotation of the defining principle); as of
   DBR789 the radii are "THREE independent free data," `R_T, R_Q, r_flux` CARRIED-AS-PARAMETER,
   beta CARRIED-AS-PARAMETER, no modulus selected. The carrier itself descends from the ADOPTED
   skeleton (the census's "untwisted adopted skeleton" characterization, carried at DBR760). The
   defining principle is ABSENT archive-side (DBR760 §0.2). `theorem_proof_reachable_now = false
   | TYPE-U` (sealed, quoted at DBR760 §1.4).

2. **The normalizing step IS the rail conversion.** "Normalized" in "complete normalized D_BR
   spectrum" is of record the transfer from `D_BR^2` to L_BR's PROPER-TIME consumer
   (VALUE/CONSUMER-CRITICAL, unbuilt — DBR760 §1.3/AL3): proper time is the external side of
   beta's conversion, and the internal/external conversion is booked a RAIL import, not a
   surface object (ANCHOR III.11). A fiber-shaped threshold is thus not merely unbuilt on the
   surface; its beta-carrying step is definitionally off-surface as currently booked.

3. **The fiber-to-threshold map is underived of record.** `quantum_threshold_map_derived = false`
   (V002 :112, verified at path this relay), and "threshold matching to the Thomson limit" is
   reopen item 5 of the rail's OWN list (V002 :97) — the rail itself books its threshold
   matching as unfinished. There is no derived map, anywhere of record, taking a D_BR eigenvalue
   to a physical threshold.

4. **Deriving the R-content is the beta-fixer, and no sealed sentence performs it.** For the
   D_BR route's R-content to become surface-derived, the census's C8 jointness would have to be
   discharged — "the complete parent action class, radion stabilization, spectrum, and matching
   rule must be derived together" (V002 :83-84 as carried at CENSUS §4) — and the census's N5
   proximity sweep found ZERO sealed sentences joining the stationarity structure to the fiber
   radius, the radion, or beta, with the sealed corroboration that even deriving T_R absolutely
   does NOT reopen the fiber question (Q-08 :209-210 as carried, a non-implication of record).
   Plus §3 above: the amplitude structure such a datum would ride on is Q-290 TYPE-R refuted.

[YOURS — the assembly, marked] (1)–(4) together with §2's corollary close it: **the record's own
structure cannot give a threshold its scale as a normalized-D_BR-class eigenvalue. RECORD_CAN_
FURNISH_FIBER_SHAPED = NO.** A fiber-shaped S13 can still be WRITTEN — adopt the skeleton, adopt
the radii, adopt the transfer — but every R-occurrence in it enters through an adopted metric
datum, which is rail provenance by ANCHOR III.11, and a rail beta-sensitivity "types the rail,
not the surface" (S16 §5, given). It is the same class as `K_KK ~ beta^2`: visible, real on the
rail, and not a surface fact.

## 6. THE VERDICT, ITS EXACT CONDITIONAL, AND THE ROBUSTNESS DISJUNCTION

**S13_SHAPE = LOCK-SHAPED**, precisely conditioned: this is the provenance-typing of a DERIVED
S13 — the only S13 a DERIVED S16 can consume (a derived S16 with a rail-conditioned threshold
foot would not be the derived S16 of ANCHOR IV; its beta-sensitivity would be rail-typed and
Finish-A's lever requires DERIVED-and-scale-sensitive). The typing is carried by elimination
(§2, §3, §5: the fiber shape is not record-furnishable) AND by positive exemplar (§4: every
built piece of the charged-threshold machinery, including the built germ of the interacting
spectrum itself, fixes its threshold data lock-fashion — derived pure numbers against the record
interval, unit-compression class, no R occurrence).

[YOURS — robustness, marked] The one residual against LOCK is that S13's completion (R34's open
list: complete CTP state, physical source two-point function, durability, connected-cell
stitching, pole/residue extraction) might prove impossible from surface objects alone — i.e.,
the record might not furnish ANY complete S13 without rail imports. That disjunct does not
rescue the fiber shape: it would make S16 underivable-as-specified rather than derived-and-
beta-sensitive, and the Finish-A lever (DERIVED and scale-sensitive) would be dead in that
branch too. **Both disjuncts kill the surface-native beta channel through the threshold foot.**
The verdict is therefore stable against the unbuilt remainder: LOCK-SHAPED if the record
completes S13; no surface beta channel either way.

**CONSEQUENCE FOR S16.** With F1–F5 beta-blind (S16, given), F6a now typed LOCK-SHAPED, and the
commissioned localization (S13's provenance as the ONLY surface-native beta channel into a
derived S16) taken as given: **a derived-and-passing S16 is beta-INVARIANT — Finish B side.**
Per the prior attack's own protocol the subordinate F6b (gluing foot) typing becomes the live
residual after a lock-shaped F6a; noted without trespass: its beta-carrying disjunct is the
skeleton-to-cell embedding, one of the three junctions booked off the surface (ANCHOR III.12,
GAP, "has never been attempted"), so it is not a surface-native beta channel under the same
discipline that governed this relay — but F6b is NOT retyped here; it stays the prior artifact's
named secondary.

## 7. THE VALUE-STEP BOUNDARY [FENCE]

No step of this typing required computing any threshold, eigenvalue, mass, coupling, kappa,
K_*, beta, or alpha, and none was taken. The nearest value-shaped steps, named and NOT taken:
deriving the actual interval-lock of E3's mass datum `mu` (that derivation IS S13's own
construction — the lock SHAPE is typed here, its pure-number VALUE is not touched); evaluating
any threshold-to-cell ratio; evaluating any D_BR tower entry or selecting any radius, spin
structure, bundle class, `w_Phi`, or lambda-orbit member; extracting any pole or residue. Every
displayed formula is a sealed-text quotation consumed for its occurrence structure only.

## 8. OVERCLAIM AUDIT

- PROVABLE (named surface objects, verified at path): the primitive inventory and rail typing
  (ANCHOR I.1–I.4, III.11–III.12); Q-290 TYPE-R and the phase-rich/amplitude-poor booking
  (ANCHOR II.6); the free orbit and dimensionless-only forcing (ANCHOR II.7, II.10); the
  occurrence criterion and beta action (CENSUS §0, V002 :57); E1 (UCC, sealed), E2 (CSP), E3
  (R34), E4 (ANCHOR II.9 / CENSUS §1.3); the D_BR raw-tower form and its R-carrying charged
  circle, moduli unselected, primaries absent (DBR760, relay-artifact custody); radii three free
  data, all moduli carried-as-parameter (DBR789, relay-artifact custody);
  `quantum_threshold_map_derived = false` and reopen item 5 (V002 :112, :97, first-handed this
  relay); C8 jointness and N5 zero-hits (CENSUS §4, §5).
- YOURS (assembly, marked inline where made): the §2 corollary (no surface-derived statement
  can contain R — rests on the ANCHOR's inventory being the complete primitive inventory); the
  §3 classification of a dimensionful eigenvalue-scale as weight/amplitude-type in Q-290's
  sense; the §4/E4 class-extension to any derived `mu`; the §5 assembly of (1)–(4) into the NO;
  the §6 robustness disjunction; consuming the commission's localization (F6a the only
  surface-native channel) as given rather than re-proving it.
- NOT claimed: S13 built or discharged (it remains UNBUILT); `mu = m_*` or any identification of
  the charged threshold mass with any named mass (the non-identification is carried); any value
  of any lock's pure number beyond sealed quotations; any retyping of F6b, of the three booked
  beta-sensitive junctions, or of the D_BR public layer; that the record CAN complete S13 from
  surface objects alone (left open, shown irrelevant to the beta channel either way); anything
  about which finish is TRUE.

---

## FINAL LINES

```text
THRESHOLD_PROVENANCE_NAMED = the record gives the first charged threshold its scale against the
    RECORD INTERVAL, by named objects: the derived record incidence spectrum spec(c_partial) =
    {-sqrt(2), 0, +sqrt(2)} entering the built interacting band structure H(p) = alpha_D . p +
    mu S tensor c_partial (R34), whose threshold condition p_lambda(E) = 0 consumes ONLY the
    source-mass datum mu times that derived pure number — with mu's record-side determination
    class the interval lock (exemplar m_* T_R = pi, non-identification carried; unit-compression
    class per UCC's tau_first(B_ch) = pi/sqrt(2) = tau_R, coefficients (0,1,1), no numerical
    multiplier).  No occurrence of the fiber radius R anywhere on that chain.

RECORD_CAN_FURNISH_FIBER_SHAPED = NO (the phase-rich/amplitude-poor record admits only
    PURE-NUMBER spectra: a fiber-shaped threshold is a dimensionful eigenvalue-scale — an
    inverse proper length — which is weight/amplitude-type structure the records are booked,
    TYPE-R with exact mechanism (Q-290), NOT to carry; R is not a surface primitive at all
    (radius = metric datum, Register :224 as consolidated); every R-carrying item of the D_BR
    route is unselected/adopted of record with its normalizing step the rail conversion itself
    (ANCHOR III.11), its threshold map underived (quantum_threshold_map_derived = false,
    V002 :112), and its derivation gated behind the unperformed C8 joint system (N5 zero hits)).

S13_SHAPE = LOCK-SHAPED (beta-blind -> Finish B) — as the provenance-typing of a DERIVED S13,
    the only S13 a derived S16 can consume; carried by elimination (the fiber shape is not
    record-furnishable) and by positive exemplar (all four built pieces of the charged-threshold
    machinery, including the built interacting germ R34, are lock-shaped; zero built
    fiber-shaped exemplars).  A fiber-shaped S13 remains writable ON THE RAIL ONLY (adopted
    moduli), and rail beta-sensitivity types the rail, not the surface — the K_KK class.
    Robust against the unbuilt remainder: if the record cannot complete S13 without the rail,
    S16 is not derivable-as-specified in that branch either, so the surface-native beta channel
    through the threshold foot is dead in BOTH disjuncts.

CONSEQUENCE_FOR_S16 = beta-INVARIANT (derived-and-passing S16, on the commissioned localization:
    F1-F5 beta-blind of record, F6a herewith LOCK-SHAPED; the prior attack's subordinate F6b
    residual noted, not retyped — its beta-carrying disjunct is a junction booked off the
    surface (GAP, ANCHOR III.12)).  Finish B side; the Finish-A lever through S13 is NOT LIVE.

FENCE_RESPECTED = CERTIFIED (no value computed; no threshold, eigenvalue, mass, coupling, kappa,
    K_*, beta, or alpha evaluated, bounded, or estimated; every numeric string a sealed-text
    quotation; no measured-value comparison; value-step boundary named at section 7 and not
    crossed).
IMPORTED_NOTHING = CERTIFIED (no metric or continuum machinery imported; no continuum theorem
    used as ground; no register/tracker/plan/road/continuation/ledger file read; every typing
    traverses named record objects, YOURS-assembly marked inline; custody flags carried, not
    upgraded).
SURFACE_ANCHOR = present (20ee87c0…, verified at path; sections I-III taken as GIVEN; section IV
    is the fork decided).

ALL RESULTS CLAIMED. alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; JOINT_ANCHOR_DERIVED = false
```
