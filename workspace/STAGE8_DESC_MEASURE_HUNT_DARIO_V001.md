CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00005522   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 17 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing (Q-913 standard).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001. Every constraint below cites a member and a locus.
  No flag name is an anchor. Member numbers are THIS artifact's.
LANE = DARIO   ROLE_THIS_RELAY = THE MEASURE HUNT (relay 1075)
SELF_CITATION_BAR = DARIO's own outputs are NOT record witnesses. Members 02 and 03 are SUBJECTS the
  assignment names — the source of the Z_N/q_N finding and of the two-object arithmetic — never
  grounds. EVERY constraint ruling below is anchored to a sealed non-DARIO member at a locus, or is
  recomputed here from such a member's own displayed structure.
NO AUTHORING = this relay hunts constraints; it neither selects nor authors omega_phys, and it does
  not take the Dirac-at-one-history point, which remains DECLINED as a selection rather than a
  constraint.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1075_MEASURE_HUNT_DARIO_V001.md` | `e572425f7f5c433fd94953e2fecf284f29424513297b4b903282f7e4d7747975` | assignment |
| 02 | `workspace/STAGE8_DESC_THETA_HIST_DARIO_V001.md` | `47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba` | the delivery — the `Z_N`/`q_N` finding (SUBJECT) |
| 03 | `workspace/STAGE8_DESC_B03_CLOSURE_DARIO_V001.md` | `6ad9bfe7f2ea5035fe838abfaa833fc941011f2ec025d92e3a3fd08d92ae3c28` | the B03 closure — the two-object arithmetic (SUBJECT) |
| 04 | `workspace/ATTACH_ENTRY_ADDENDUM_V001.md` | `420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73` | the entry of record |
| 05 | `workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md` | `ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc` | the entered candidate |
| 06 | `workspace/STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | the S1 receiver typing |
| 07 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **`Lambda`, `A_F`, `B`, `X`, `Y`, the generators, the U1 map, THE SURVIVOR TABLE** |
| 08 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U3_DOR008_DOR009_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `b0118d89e0b4b321fbf9caab5bfb811a0b5fd572e808d02615ee8503db2bc1ac` | **`q_N`, `Q_N = U(1)^N`, and the common-gauge invariance** |
| 09 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md` | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` | **`Theta_F` at its own source — the U1 reality map** |
| 10 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | `Z_N`'s exact form, equation (3); the equal-history identity |
| 11 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `res_B`; the non-uniqueness theorem |
| 12 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | `p_ch`-neutrality. READ ONLY WITH 13 AND 14. |
| 13 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 12 |
| 14 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the neutrality certificate; the no-member clause |
| 15 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 16 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 17 | `workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | the mandate — the road this relay reports on |

```text
COMPUTED IN THIS RELAY (exact structure; no physical quantity evaluated):
  the Gelfand transform of  prod_j [ e_plus(U_(e_j)) . e_minus(U_(e_j))^* ]  equals  Z_N,
    factor for factor, and equals prod_j q_N(...)_j                       -> IDENTICAL, verified
  Theta_F fixes each factor b_j, hence fixes Z_N                          -> omega(Z_N) is REAL
  omega(Z_N) attains +1, ~0 and -1 on U1-compatible states                -> NO VALUE IS FORCED

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEMBER BOUND.
NO FIXED-POINT EXECUTION. NO END TEST. NO MEASURED-CONSTANT COMPARISON. NO FREEZE.
omega_phys NEITHER SELECTED NOR AUTHORED. PE-1..17 POINTER-ONLY. CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESC: THE MEASURE HUNT
## RELAY 1075 — `[PLAN:DESC-36]` — DARIO LANE — [CLAIMED]

Date: 2026-08-12

**THE ANSWER: `STILL-CONTINUUM`.** And the reason is structural rather than a failure to find
something: **`Z_N` was already an element of `B` before the entry.** It is the Gelfand transform of a
specific product of DoR-008's own ratified generators. The entry did not enlarge the observable
algebra; it gave an element that was already there a physical reading. **A state space defined over
an algebra cannot be narrowed by learning what one of that algebra's elements means.**

I attacked outcome (3) first and hardest, as instructed, because a derivation would dissolve the
authoring question entirely. It does not survive. What survives is one genuine consequence — `ω(Z_N)`
is real — which is a property of the admissible set, not a cut in it.

---

## 1. THE DECISIVE STRUCTURAL FACT

### 1.1 `Z_N` is a product of ratified generators

Member 07 `:141-168` fixes the algebra at bytes: `Lambda = direct-sum_(j>=1) Z e_j`,
`A_F := C*(Lambda)`, `B := A_F,+ tensor_min (A_F,-)^op`, `X := Hom(Lambda,U(1))`, `Y := X_+ x X_-`,
`A_F` the continuous cylinder-function algebra on `X`, and `B isomorphic to C(Y)`. Member 07 `:246-252`
uses `b_lambda := e_plus(U_lambda)` as a generator.

Under `B iso C(Y)` a point of `Y` is a pair of characters `(chi_+, chi_-)`, and the generators are
evaluations: `e_plus(U_lambda) -> chi_+(lambda)`, `e_minus(U_lambda) -> chi_-(lambda)`. Therefore

```text
e_plus(U_(e_j)) . e_minus(U_(e_j))^*     ->     conjugate(chi_-(e_j)) . chi_+(e_j)
```

which is **exactly** member 08's common-gauge coordinate `q_N(z_+,z_-)_j = conjugate(z_(-,j)) z_(+,j)`.
Taking the product over `j`:

```text
prod_(j=1)^N [ e_plus(U_(e_j)) . e_minus(U_(e_j))^* ]     ->     Z_N
```

**Recomputed here at random points and identical to twelve places.** So `Z_N` is not new to `B`. It is
a finite product of the generators DoR-008 ratified, and it has been an element of `B` since that
ratification.

### 1.2 What follows, and it is the whole answer

`State(B)` is defined as the normalized positive functionals on `B` (member 07 `:174-207`), and the
survivor table (member 07 `:480-487`) is a census over that set. **`Z_N` is in the algebra the census
already ranges over.** An observable cannot constrain the states of its own algebra: every state
evaluates every element, and no state is excluded by an element's existence.

**The entry supplies an interpretation — that this particular product of generators is the physical
connection's relative holonomy — and an interpretation is not a constraint.** That is why the answer
is `STILL-CONTINUUM`, and why it is a structural result rather than a search that came up empty.

---

## 2. THE FIVE CONSTRAINTS THE ASSIGNMENT NAMES, EACH RULED

### 2.1 The entered evolution — **DOES NOT NARROW**

The entered `U_N[A,J,g]` (member 04 `:31-34`) acts on `A_SR`, and `A` enters it as an *argument*, not
as an object the dynamics acts upon. A dynamics constrains a state only through an invariance demand
(stationarity, KMS, or the like), and **no sealed member imposes one on `omega_phys`**: `B` is the
history algebra, and the evolution is a function of the history rather than an action on it.

```text
RULING: DOES-NOT-NARROW.  Span: member 04 :31-34 against member 11 :126-128 (the factorisation that
        keeps the evolution on A_SR and omega_phys on B).
```

### 2.2 Charge / `p_ch`-neutrality — **DOES NOT NARROW**

Member 14 `:9-13` states the neutrality certificate: every member of the ratified family forces the
same normalized-identity invariant state and the same symbolic `p_ch` form. **That is a statement
about the source state on `A_src^A`, not about a measure on `Y`.** It constrains `d_state`'s output
and says nothing about `omega_phys`.

```text
RULING: DOES-NOT-NARROW.  Wrong sector — the certificate binds the source factor, not the field one.
```

### 2.3 The quotient's own invariances — **DOES NOT NARROW THE SET; IT QUOTIENTS IT**

Member 08 gives `q_N` and proves its invariance: *"two pairs lie in the same common-gauge orbit
exactly when their `q_N` values agree."* So functions descending through `q_N` cannot distinguish
measures that share a `q_N`-pushforward.

**This is the one place a narrowing might have hidden, and it does not.** The effect is a quotient, not
a subset: two admissible measures with the same pushforward are indistinguishable *to `q_N`-descended
observables*, but both remain admissible states of `B`. And `Prob(Q_N) = Prob(U(1)^N)` is itself a
continuum, so even the distinguishable content is not narrowed to a finite-parameter family.

```text
RULING: DOES-NOT-NARROW (quotients rather than cuts).  Span: member 08's q_N invariance display.
        Structural note: the physically distinguishable object is the pushforward to Q_N; the
        admissible SET is unchanged, and Prob(Q_N) is still a continuum.
```

### 2.4 Positivity / normalization forced on the two-branch diagonal — **DOES NOT NARROW**

`|Z_N| = 1` everywhere (member 10 equation 3), and `Z_N = 1` on the diagonal `A_+ = A_-` (member 10
`:286-298`, the equal-history identity). One might hope a normalization demand of the form
`omega(Z_N) = 1` would force the measure onto the diagonal — which *would* be a genuine cut.

**No sealed member imposes it.** The zero-source condition sets `A = 0`, where `theta_j = 0` and
`Z_N = 1` identically for *every* history — so the condition is satisfied vacuously and constrains
nothing. Any demand on `omega(Z_N)` at nonzero connection would be a statement about a response, which
is downstream and is not consulted here.

```text
RULING: DOES-NOT-NARROW.  The equal-history identity constrains the OPERATOR, not the state; the
        zero-source condition is vacuous on the measure.  Span: member 10 :286-298.
```

### 2.5 The survivor table re-read WITH the entry — **UNCHANGED, ROW BY ROW**

Member 07 `:480-487`. Re-read with the entry rather than without it:

```text
raw positivity                     finite measures on Y      — Z_N in the algebra changes nothing
inclusive normalization            Prob(Y)                   — unchanged
U1 reality                         invariant Prob(Y)         — see §3; yields a PROPERTY, not a cut
one-cell C0-scope restriction      same class                — consumes omega only through omega(1)=1
weak rho_pre type-only existence   no further cut            — unchanged
concrete rho_pre + common origin   NO_VERDICT                — unchanged; still the unbuilt test
```

**Not one row moves.** The entry adds no row and removes none.

---

## 3. THE ONE GENUINE CONSEQUENCE — AND WHY IT IS NOT A NARROWING

Member 09 (and member 07 `:259-276`) give the U1 reality map:

```text
Theta_F(e_plus(U_lambda)) = e_minus(U_(-lambda)),   Theta_F(e_minus(U_lambda)) = e_plus(U_(-lambda))
omega is U1-compatible  <=>  omega(Theta_F(b)) = conjugate(omega(b))  for every b in B
```

Write `b_j := e_plus(U_(e_j)) . e_minus(U_(-e_j))`, the `j`-th factor of `Z_N` (using
`e_minus(U_lambda)^* = e_minus(U_(-lambda))`). Then

```text
Theta_F(b_j) = e_minus(U_(-e_j)) . e_plus(U_(e_j)) = b_j        — each factor is Theta_F-FIXED
=>  Theta_F(Z_N) = Z_N
=>  omega(Z_N) = conjugate(omega(Z_N))                          — omega(Z_N) is REAL
```

**So every admissible state assigns `Z_N` a real expectation, in `[-1,1]` rather than the closed unit
disc.** That is a genuine, checkable consequence of the entry read against U1 reality, and it is worth
recording.

**It is not a narrowing, for two reasons, and I state both rather than the flattering one.** First,
U1 reality was *already* the third row of the survivor table, so the admissible set was already the
invariant measures; realness is a property they all have, not a filter that removes any. Second — and
decisively — **no value is forced**: recomputed here, `omega(Z_N)` attains `+1` on a diagonal-supported
invariant measure, `≈ 0` on the symmetrized Haar product, and `-1` on an invariant measure at a point
where `Z_N = -1`. The range is the full interval.

```text
CONSEQUENCE (not a constraint on the set): omega(Z_N) in [-1,1], real, with every value attainable.
```

---

## 4. OUTCOME (3), ATTACKED HARDEST — AND REFUTED

A `PINNED` verdict would be a **derivation**, dissolving the authoring question and voiding the
mandate's necessity. It therefore deserved the hardest attack and got it first.

```text
ATTACK                                                    RESULT
Does Z_N's existence exclude any state?                   NO — §1.2. Z_N was already in B.
Does the diagonal identity force support on the diagonal? NO — §2.4. It constrains the operator; the
                                                          zero-source condition is vacuous.
Does U1 reality plus Z_N force a value?                    NO — §3. omega(Z_N) ranges over all of
                                                          [-1,1] on U1-compatible states.
Does the q_N quotient force a representative?             NO — §2.3. It identifies measures; it does
                                                          not select among them.
Does the entered dynamics impose an invariance?           NO — §2.1. No sealed member demands one.
Does neutrality bear?                                     NO — §2.2. Wrong sector.
```

**Nothing pins the measure. The Dirac-at-one-history point remains available and remains DECLINED** —
it is a selection dressed as a constraint, and member 07 `:464-468` bars converting missing
construction into a physical claim.

---

## 5. THE ROAD

```text
ADMISSIBLE_SET = STILL-CONTINUUM
ROAD           = MANDATE-FULL
```

The measure is undetermined **at full strength**, and the entry did not reduce the object a mandate
would have to supply. The admissible set is exactly what member 07's table said it was before Attach
existed: the U1-invariant probability measures on `Y`, a continuum.

**And the reason is worth stating precisely, because it is not "we looked and found nothing."** The
record's sealed content constrains the *algebra* and the *dynamics*; `omega_phys` is a state, and the
only sealed conditions on states are positivity, normalization, U1 reality and the one-cell scope —
all of which the table already ran. **The entry was an arrow into the dynamics. Arrows do not
constrain states.** That is why no constraint could have been found by any amount of further hunting,
and why saying so is more useful than another sweep.

`FINISHING_PROPERTY = N/A` — there is no narrowed set to finish. Had the outcome been `NARROWED`, the
finishing property would have been whatever cut the residual family to a point; there is no residual
family distinct from the original.

---

## 6. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN (unchanged)   JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  members 02/03's findings as subjects; member 04's entry and three carried conditions; member 05's
  entered map; members 06-11's spans in their declared roles; members 12-14's neutrality certificate
  under its read rule; member 17's mandate; all charter fences.

DERIVED HERE:
  that Z_N is the Gelfand transform of a product of DoR-008's own ratified generators, hence an
    element of B prior to the entry — and the consequence that an interpretation cannot cut a state
    space defined over that algebra;
  that Theta_F fixes each factor of Z_N, hence omega(Z_N) is real on every admissible state;
  that omega(Z_N) attains +1, ~0 and -1 on U1-compatible states, so no value is forced;
  the row-by-row re-reading of the survivor table with the entry, and the five constraint rulings.

SELECTED HERE:
  NOTHING.  No measure, no representative, no Dirac state, no member of the origin family.
  omega_phys was NEITHER SELECTED NOR AUTHORED.  NOTHING ADOPTED, ENTERED OR FROZEN.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

## 7. FLATTENING CHECK — S01–S37 walked

- An **interpretation** was not flattened into a **constraint** (§1.2) — the artifact's whole content.
- A **quotient** was not flattened into a **narrowing** (§2.3): identifying measures is not excluding
  them, and I say so where a narrowing would have been the flattering reading.
- A **property of the admissible set** was not flattened into a **cut in it** (§3): `omega(Z_N)` real
  follows from a row the table already ran.
- An **operator identity** was not flattened into a **state condition** (§2.4).
- A **source-sector certificate** was not flattened into a **field-sector constraint** (§2.2).
- A **vacuous condition** was not flattened into a **satisfied** one (§2.4): the zero-source demand
  holds for every history and therefore selects nothing.
- A **selection** was not flattened into a **constraint**: the Dirac point is named and declined (§4).
- A **structural impossibility** was not flattened into a **failed search** (§5): no further hunting
  would find a constraint, and that is a stronger statement than "none was found."

## 8. AUDITS AND CUSTODY

```text
PROSE_DIGESTS     = 17/17, STRICT==STABLE (0 live members)
RECOMPUTATIONS    = the generator-product/Z_N/q_N identity; Theta_F fixing Z_N; the attainability of
                    +1, ~0 and -1 — all reproduced by the sealed generator
RESIDUE           = 0 hits (27-token scan over authored prose)
CLOSURE           = declared-first, byte 0, fixed point
OUTPUT_INSPECTION = NONE-CERTIFIED — every constraint tested comes from Z_N and the sealed record;
                    kappa_record, alpha and the battery are pointers only and were not opened, and no
                    test was chosen for what a downstream quantity would want
VERB_AUDIT_SELF   = CLEAN
```

- **BUILDER NEVER VERIFIES OWN WORK.** CLAIMED until the Codex 2 check.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- PE-1..17 pointer-only; no member bound; no freeze; nothing authored.

---

## 9. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00005522 computed as a fixed point on bytes; 17 members;
     0 live; residue scan 0 hits from its own run)

CONSTRAINTS = 5-BEARING, and every one DOES-NOT-NARROW:
  entered evolution        DOES-NOT-NARROW   member 04 :31-34 vs member 11 :126-128 — A is an
                                             argument, not an object acted on; no invariance demanded
  charge/p_ch-neutrality   DOES-NOT-NARROW   member 14 :9-13 — binds the SOURCE state, wrong sector
  quotient invariances     DOES-NOT-NARROW   member 08's q_N — QUOTIENTS the set rather than cutting
                                             it; Prob(Q_N) is itself a continuum
  two-branch diagonal      DOES-NOT-NARROW   member 10 :286-298 — an OPERATOR identity; the
                                             zero-source condition is vacuous on the measure
  survivor table + entry   DOES-NOT-NARROW   member 07 :480-487 — not one row moves

  THE STRUCTURAL REASON, which subsumes all five: Z_N is the Gelfand transform of
  prod_j [ e_plus(U_(e_j)) . e_minus(U_(e_j))^* ] — a product of DoR-008's OWN RATIFIED GENERATORS,
  verified identical to q_N's coordinate factor for factor.  Z_N WAS ALREADY AN ELEMENT OF B.
  The entry supplied an INTERPRETATION of an existing element, and an interpretation cannot narrow a
  state space defined over the algebra that contains it.

ONE GENUINE CONSEQUENCE, recorded and correctly typed: Theta_F FIXES Z_N (each factor is fixed), so
  omega(Z_N) is REAL — in [-1,1], not the unit disc — for every admissible state.  NOT a narrowing:
  U1 reality was already the table's third row, and omega(Z_N) ATTAINS +1, ~0 AND -1 on U1-compatible
  states, so no value is forced.

ADMISSIBLE_SET = STILL-CONTINUUM.  Exactly what the table said before Attach existed: the U1-invariant
  probability measures on Y.  Outcome (3) PINNED was attacked first and hardest and is REFUTED on six
  independent lines (§4).  The Dirac-at-one-history point remains DECLINED as a selection.

FINISHING_PROPERTY = N/A — there is no narrowed set to finish.

ROAD = MANDATE-FULL.  The measure is undetermined at full strength and the entry did not shrink the
  object a mandate must supply.  And the reason is structural, not a failed search: the record's
  sealed content constrains the ALGEBRA and the DYNAMICS; omega_phys is a STATE, and the only sealed
  conditions on states are the four the table already ran.  THE ENTRY WAS AN ARROW INTO THE DYNAMICS,
  AND ARROWS DO NOT CONSTRAIN STATES.  No further hunt finds a constraint.

OUTPUT_INSPECTION = NONE-CERTIFIED
PROSE_DIGESTS = 17/17, STRICT==STABLE
CHAIN_INVOKED = true (chained pickup proceeds to RELAY_PASTE_1074_DIAGB_CHECK_V002_DARIO_V001.md,
     an earlier DARIO paste still unprocessed; 1072 and 1073 are CODEX 2 assignments)
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

All findings **CLAIMED** until the opposite-lane check.

**What this settles, and what it does not.** It settles that the measure will not be arrived at by
finding a cleverer constraint in what is sealed — the obstruction is not that the constraints are hard
to find but that the sealed content is of the wrong kind to bear on a state. **That makes the mandate
road the only lawful one, at full strength, and names the object it must supply exactly**: a
U1-invariant probability measure on `Y`, together with its descent map and provenance certificate.

**And it leaves the second object untouched.** The B03 disposition's arithmetic had two: this measure,
and the `d_U2` descent witness whose domain names `C0`. This relay bears only on the first. Nothing
here narrows, pins, or otherwise moves the second, and I note that so the two are not conflated by a
reader who takes `STILL-CONTINUUM` as the section's whole remaining story.
