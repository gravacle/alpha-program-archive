# STAGE 8 — ADVERSARIAL AUDIT OF THE r-2 DETERMINATION (kappa_n FOR THE FAMILY OF RECORD)

## INDEPENDENT AUDITOR — CODENAME R2-AUDIT — COMMISSION S9AD — [SEALED]

Date: 2026-08-14
Role: independent adversarial auditor of
`STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md` (digest 3b5e95b6, sidecar
MATCH, verified at path before reading). Default posture REFUTE. The
independence protocol was followed: sub-tasks (1) and (2) were worked and
FIXED in working notes (scratchpad, pre-build timestamps this session)
BEFORE the build was opened; the build was then read ONCE, in full, and
attacked at its cited bytes.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ONLY — two CAS batteries this session (the
build's §6 battery re-run VERBATIM in a fresh venv `r2auditvenv`, sympy
1.14.0, 6/6 PASS reproduced; my own adversarial battery, output verbatim
in §6 below); every constant symbolic or an exact rational; nothing
numeric evaluated as authority; no measured constant; no value frozen (no
numeric kappa, M, delta, D); M(t)/1_{D_t} consumed SHARP; the stricken
display (E1 :773-778) consumed nowhere as ground; no register/road_/
ledger/lens/plan/tracker/THE_HANDOFF file opened; "Q-..." tokens inside
sealed artifacts treated EXPECTED-UNLOCATABLE, noted, not chased; no git
action; no existing file edited; ONE file written (this artifact) plus its
seal sidecar; output path probed before write: ABSENT. Every enumeration/
uniqueness sentence below claims only its own displayed sweep.

---

## 0. VERDICT LINE

**CONFIRMED-WITH-CORRECTIONS — the branch verdict (B1-DERIVED): kappa_n ->
+infinity at the full-family quantifier for the actual carrier compression
family of record SURVIVES independent re-derivation: my own derivation,
fixed in working notes BEFORE the build was opened, reached the SAME
conclusion from the SAME three sealed inputs (C6 frozen commutator
non-HS-ness; C1 exact dilation-invariant symbol; a79939ad strong
convergence) by an independently fixed proof form (WOT-lower-semicontinuity
with finite-block exhaustion; the build used the SOT special case — both
exact); every seal the build claims re-verified 13/13 at path; every
load-bearing identity re-derived exactly (by hand and by CAS in a fresh
venv; the build's battery reproduced 6/6); the family-relativity trap is
ESCAPED, not tripped — the two load-bearing inputs are actual-family
operator data lying outside the abstract eigenvalue constraint class, and
the V/NV models are cited only as known-of-record context; the corpus
sweep found NO missed sealed object that changes the answer and TWO
uncited corroborations (the blind C_n -> C verdict of 2026-07-28, which
independently proves the strong convergence AND the zero-mode-convention
immateriality; the R1-audit's c-1, which independently confirms the
build's G-13 guard-digest supersession note). THREE corrections, ALL
COSMETIC, NONE VERDICT-CHAIN: k-1 the step-R conjugation display has a
dagger-side slip (U_r^dag 1_{B_1} U_r = 1_{B_{1/r}}, not 1_{B_r}, under
the build's own convention — conclusion unaffected: r -> 1/r is an exact
involution of (0, infinity), and E1's sealed S3-grounds display the
radius-independence independently; exact witness in §6); k-2 X-3's
trace-grade consistency check invokes step L for the TRACE without
displaying the positivity ground trace-LSC needs (holds: C_nPC_n =
(PC_n)^dag(PC_n) >= 0, displayed in §6); k-3 §2.3 resolves the clause's
{C_n} without quoting N.1's own descriptive phrase "compressions onto the
sealed carrier subspaces", whose plain reading is the Q_n the build's K-1
rightly excludes — the resolution is correct and the residue honestly
carried at CH-A, but the phrase should have been confronted at its bytes.
The B2-hypothesis refutation, the B3-does-not-arise scoping, the F-b/F-c
impossibility at a = 0, the F-d/r-3 untouched scoping, and the
R-L4b-not-discharged boundary are all exactly as derived. Adoption of the
(B1) input is the registrar's; this audit fires nothing.**

---

## 1. INDEPENDENCE PROTOCOL, EXECUTED

Order of operations this session, on the record: (1) clause 72d5343e
verified at path vs sidecar and tasked digest, read in full; (2) candidate
e1da7446 verified and read in full; (3) the defining stock chased at path
— E1 46846730 (:311-390 frozen inputs C1/C2/C4/C6; :640-800 R.0-R.2 and
the clauses), CERT a903716c (§2.3-2.4 in full), ZF 26f9314b (§3-4.4), PA
789338ad (in relevant part: A1-A2, pinned-digest table), pure-convergence
a79939ad (in full), mixed-covariance 235246ab (in full through scope) —
all seals verified BEFORE reliance; (4) sub-tasks (1) and (2) worked and
FIXED to
`scratchpad/R2_AUDIT_WORKING_NOTES_PREBUILD.md`
including a pre-registered 10-point attack list (a)-(j); (5) ONLY THEN the
build read once in full; (6) the attack run.

---

## 2. MY INDEPENDENT PASSES (AS FIXED PRE-BUILD)

### 2.1 Independent constructibility answer (fixed before the build was opened)

```text
SPECTRUM: the actual eigenvalues s_i(n) of C_n P C_n are displayed on NO
  sealed artifact (sweep displayed in §5; the Hermite JSONs are
  numeric-grade and fenced as authority). At fixed n they are in-principle
  exact-derivable (finite exact linear algebra on exact matrix elements)
  but are NOT needed.
AGGREGATE: the ONE aggregate the sealed dichotomy consumes IS derivable
  from sealed stock without any spectrum:
    (i)   C6 frozen input: ||[C, 1_B]||_2 = +infinity (E1 :353-358),
          radius-independent via C1's exact dilation-invariant symbol
          C(p) = (I - alpha.p-hat)/2 (E1 :320-329; the same ground E1's
          own S3-deletion displays at :713-715);
    (ii)  sealed strong convergence C_n^(pure) = 1_(-inf,0)(Q_n h_0 Q_n)
          -> P_- = C (a79939ad, verdict DERIVED,
          pure_state_strong_convergence_derived = true);
    (iii) one exact step: lower semicontinuity of the HS norm.
IDENTIFICATION (my own, pre-build): the dichotomy's {C_n} must be a
  PROJECTION family (the of-record identity kappa_n = 2 sum s_i(1-s_i)
  fails for non-idempotent C — exact witness §6 A5 — and the R-L1
  block-triangular reduction is projection algebra). Sealed carrier-
  indexed candidates swept (displayed sweep, §5): Q_n, M_n(t),
  C_n^(mix) = Q_n P_- Q_n, C_n^(pure). C_n^(mix) excluded (contraction,
  not idempotent — sealed at 235246ab's own bytes); M_n(t) excluded
  (same); Q_n excluded (det_n(0) must regularize E1's
  D = det_{ran C}(1 - 2CPC) with C the SEA (C1; R.2 reads tr(CPC) as the
  sea's mode count); a Q_n baseline carries no sea and its commutator
  limit [I, 1_B] = 0 regularizes nothing sea-shaped). Hence {C_n} =
  {C_n^(pure)}: unique at the displayed sweep.
ANSWER FIXED PRE-BUILD: spectrum not constructible-as-sealed; the
  B1-deciding aggregate IS constructible; predicted branch outcome
  B1-DERIVED conditional on the identification.
```

### 2.2 Independent derivation (fixed pre-build; my own proof form)

```text
CLAIM: kappa_n = ||[C_n, P]||_2^2 -> +infinity, full-family, P = 1_B
  (spinor-diagonal; any radius r > 0).
STEP 1 (WOT of the commutators — WEAKER hypothesis than the build's):
  C_n -> C strongly => for all x, y:
  <[C_n,P]x, y> = <C_n(Px), y> - <C_n x, (Py)> -> <[C,P]x, y>.
  (Only WOT of C_n is used; strong convergence implies it.)
STEP 2 (finite-block exhaustion): ||[C,P]||_2^2 = +infinity (C6) means:
  for every R there exist FINITE orthonormal families {u_k}, {v_l} with
  Sigma_{k,l} |<[C,P]u_k, v_l>|^2 > R (else the sup over finite blocks —
  which equals the HS mass — would be finite).
STEP 3 (transfer): the finitely many block entries converge (Step 1), so
  eventually Sigma_{k,l} |<[C_n,P]u_k, v_l>|^2 > R; and every such block
  sum is <= kappa_n (Bessel; exact orthonormal-family form verified §6
  A3). Hence for every R: kappa_n > R cofinitely. FULL-FAMILY divergence;
  every subsequence diverges. QED.
WHAT THIS DOES NOT GIVE (fixed pre-build): R-L4b is NOT discharged — the
  clause N.3 requires the (B1) input PLUS the r-3 joint polydisc rate;
  only the r-2 slot is addressed.
```

AGREEMENT: my pre-fixed conclusion, quantifier, and input list coincide
with the build's. My proof form (WOT + finite doubly-indexed blocks) is
strictly more general than the build's step L (SOT + single-basis
columns); both are exact; the build's suffices for its inputs.

---

## 3. SEAL RE-VERIFICATION TABLE (every seal the build claims, recomputed at path THIS session)

```text
72d5343e...c839aa7  R_L4A_N_CLAUSE_V002_ADOPTED_2026-08-14.md   MATCH (sidecar + tasked)
e1da7446...b690ed6  STAGE8_R1_NAMING_CANDIDATE_V001.md          MATCH (tasked)
46846730...4cccb5   STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md MATCH
a903716c...132e0    STAGE8_RL4_RL0_CERTIFICATION_V001.md        MATCH
685afac8...74af52   STAGE8_RL4_RL0_CHECK_V001.md                MATCH
26f9314b...48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md         MATCH
52f2490b...dba47ed6 STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001  MATCH
789338ad...7bed5bc3 STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_...   MATCH (own sidecar)
a79939ad...4dc8e370 ...PURE_HERMITE_VACUUM_STRONG_CONVERGENCE   MATCH (path = PA pin)
235246ab...c18d6f8685b  ...EXACT_HERMITE_MIXED_COVARIANCE       MATCH (path = PA pin)
5cbcd28e...f38fbd0d7  STAGE8_ROUTE2_COMPLETE_QSPEC_STATE_BINDING MATCH (path = PA pin)
a1258dcf...29bec510  ...CONTINUUM_GALERKIN_PROVENANCE_CORRECTION MATCH (path = PA pin)
2baa4c31...8c4fd5   ACTUAL_SURFACE_GUARD_V001.md (alpha_supervision) MATCH (own sidecar)
3b5e95b6...17ab9f   STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md MATCH (build vs sidecar)
```

13/13 of the build's table MATCH, plus the build's own seal. The G-13
supersession note (R1-era guard prefix 43db1d77 -> current 2baa4c31) is
independently CONFIRMED by the R1-audit's c-1 at its own bytes (guard
appended mid-session 2026-08-14, append-only by the guard's own
REOPEN/UPDATE clause, which I verified at the guard's tail).

---

## 4. THE ATTACK, RESULT BY RESULT

### 4.1 Quoted bytes re-checked at their lines

```text
b-1 C6 display: "||[C, 1_B]||_2 = +infinity" — E1 :355 VERBATIM; frozen
    input (:311-313); reviewer re-verified. The build consumes the
    COMMUTATOR form, exactly as displayed — NOT the ||CPC||_2 form (the
    two are inequivalent in general: C = P makes CPC trace-divergent with
    [C,P] = 0; my pre-registered attack (d) — the build does not trip it).
b-2 C1 symbol: "C(p) = (I - alpha.p-hat)/2" — E1 :321 VERBATIM; the mixed
    result's P_-(p) display (235246ab) is the same symbol byte-for-byte:
    the P_- = C identification holds at bytes.
b-3 R.2/C4: P the sharp causal-ball projector, promotion display
    "sum_lambda w_lambda u_lambda = ... = P" — E1 :350-351 VERBATIM;
    V(0) - 1 = -2P on opposite-phase pairs, same P for all four — :746-748.
    Spinor-diagonality of record (phase acts as scalar times I on the
    ball, S^2 = I — :346-348).
b-4 R-L1 block-triangular identity — E1 :657-659 VERBATIM.
b-5 CERT §2.3(c)/(d): the fixed-n display, s_i in [0,1], the dichotomy,
    "NEITHER BRANCH IS SEALED", tr(C_nPC_n) -> infinity sealed — :228-273
    VERBATIM. The build's claim that CERT's both-branches-open statement
    is a CONSTRAINT-CLASS statement (escapable by consuming MORE sealed
    data) is correct at CERT's own scope: CERT §2.3(d) grounds it on
    trace-mass placement, i.e. eigenvalue-data grade — it did not consume
    a79939ad.
b-6 G-9 verdict and scope: "C_n^(pure) -> P_- strongly", DERIVED;
    "Strong convergence of one-particle covariances does not by itself
    imply convergence of growing-dimensional quasifree determinants" —
    VERBATIM; the build consumes ONLY covariance convergence (step S) and
    NEVER a determinant limit: scope caveat respected.
b-7 G-12 formula typing: "For the mixed state the finite quasifree
    formula is the general Klich determinant, not a Slater-compression
    determinant. For the pure state the Slater formula applies" — :73-75
    VERBATIM. "Q_n -> I strongly" — :47. "Two state restrictions ...
    without selecting between" — :55. "when the finite operator has no
    zero mode" — :71. All as the build quotes them.
b-8 G-11: "the stationary quasifree incoming source state of the frozen
    free Hamiltonian h_0" — :25-26 VERBATIM; no post-hoc selection.
```

### 4.2 Load-bearing identities re-derived

```text
i-1 Step S (commutator strong convergence): re-derived by hand; exact;
    needs only C_n -> C on the two vectors x, Px and boundedness of P.
i-2 Step L (HS lower semicontinuity, SOT): re-derived by hand; the
    finite-subset Bessel bound verified in the general orthonormal-family
    form (§6 A3, rotation-parameterized — strictly wider than the build's
    D4 column instance); the sup-over-finite-subsets identity for the HS
    mass is monotone convergence of a positive series; basis-independence
    classical. EXACT. Full-family quantifier: for every R a finite block
    is eventually exceeded — every subsequence diverges. No gap.
i-3 Step R (radius-independence): substance CONFIRMED; display slip
    FOUND — correction k-1 (§4.5). Under the build's own convention
    (U_r f)(x) = r^{-3/2} f(x/r), conjugation gives U_r^dag M_phi U_r =
    M_{phi(r .)}, hence U_r^dag 1_{B_1} U_r = 1_{B_{1/r}} (exact witness
    §6 A1a'/A1a''); the build wrote 1_{B_r}. IMMATERIAL to the
    conclusion: {1/r : r > 0} = {r : r > 0} (exact involution), so
    ||[C, 1_{B_r}]||_2 = ||[C, 1_{B_1}]||_2 = +infinity for every r > 0
    holds regardless of the dagger side; and E1 :367-369 displays the
    dilation-covariance radius-independence as sealed ground
    independently. COSMETIC.
i-4 kappa identity kappa_n = 2 sum s_i(1-s_i): re-derived by hand from
    C^2 = C, P^2 = P and cyclicity; CAS confirmed (build's D3 re-run;
    my A5 exhibits its FAILURE for non-idempotent C — so the identity
    genuinely selects the projection realization: K-2 is substantive,
    not decorative).
i-5 X-1 chain |det_n(0)|^2 = prod(1-4u_i) <= exp(-2 kappa_n): re-derived;
    exact; of record (CERT §2.3(c)).
i-6 Step 4 (scheme-robustness): QPQ - P = QP(Q-I) + (Q-I)P re-derived in
    my own frame (§6 A6); the strong-convergence transfer for the mixed
    family is exact (||Q_n P_-|| <= 1; (Q_n - I) -> 0 strongly on each
    vector). The build correctly scopes the eigenvalue-product DISPLAY to
    the pure realization only.
i-7 The build's §6 battery: re-run VERBATIM in a fresh venv (sympy
    1.14.0): 6/6 PASS reproduced.
```

### 4.3 The five hazard hunts

```text
h-i  UNPROVEN CONVENIENCE DOING LOAD-BEARING WORK: NOT FOUND. The one
     unforced identification (CH-A) is named as the ledger's PREMISE,
     grounded at three sealed constraints (K-1/K-2/K-3, all re-verified
     at bytes), AND the branch conclusion is proven robust across both
     sealed covariance schemes (step 4) — so nothing unproven carries the
     divergence. The per-member zero-mode convention is confined to
     single members (tail statement unaffected) — and the sweep-found
     blind verdict (§4.4) PROVES all cut conventions share the same
     strong limit, retiring even that residue.
h-ii FAMILY-RELATIVITY TRAP (model quoted as actual): NOT TRIPPED. V/NV
     appear only as known-of-record context (§3 scope note, X-4); the
     load-bearing inputs are the actual family's operator data. X-4's
     escape statement is exactly the LSC contrapositive: a realization
     with bounded kappa_n cannot converge strongly to a pair with non-HS
     commutator — correct as stated.
h-iii VERDICT STRONGER THAN DERIVATION: NOT FOUND. Full-family
     quantifier derived (cofinite exceedance of every R); every-radius
     derived (step R, both dagger sides); both-ell consumed at the
     sealed verdict's own unqualified family quantifier; four-pair
     uniformity is R.2's pair-independence of A_n(0); B2 refuted at
     HYPOTHESIS level only (no avoidance claim made); B3 scoped as
     not-arising; F-b/F-c impossibility claimed at a = 0 ONLY; F-d and
     r-3 explicitly untouched; R-L4b explicitly not discharged; no value
     frozen; the conclusion is a limit statement.
h-iv MISSED SEALED OBJECT: NONE FOUND THAT CHANGES ANY ANSWER (sweep
     displayed §5). Finds: (1) BOHM_BLIND_CN_TO_C_VERDICT_2026-07-28
     (supervision) — corroborates G-9 and the zero-mode immateriality;
     (2) R1-audit c-1 — corroborates G-13; (3) G-12 :49's additional
     sealed strong convergence Q_n M_c(t) Q_n -> M_c(t) — unused,
     consistent; (4) TASK2D's "kappa_n" is a NAME COLLISION (connected
     cumulants, a different object) — the build correctly never touched
     it; noted so the registrar does not cross-wire the two.
h-v  FENCE/LEDGER VIOLATIONS: NONE FOUND. Exact-symbolic throughout; the
     numeric strings inside sealed inputs (0.166..., 1e-15) are cited by
     NEITHER the build NOR this audit as grounds; sealed verdicts are
     consumed at their stated DERIVED grade; gates unchanged; one file +
     sidecar written; commission-distinct S9AD path; no witness/flag
     action; consumption left to the registrar.
```

### 4.4 Sub-task 4 (blocker test) — N/A AS PRIMARY, RUN ANYWAY

The build did not answer NOT-DERIVABLE-TODAY, so the blocker audit is
moot as tasked; its one conditional fallback (CH-A: "if the clause's C_n
were the bare Q_n ... NOT-DERIVABLE-TODAY") was nevertheless tested: the
Q_n reading is excluded at K-1's sealed grounds (E1 R.0/C1 place the
STATE covariance in the C slot; G-11 binds the state; R.2 reads tr(CPC)
as the sea's mode count), and my sweep (§5) found no sealed sentence
supporting a Q_n reading of the clause. The exclusion stands.

### 4.5 Corrections (each tagged)

```text
k-1 COSMETIC — step R display: "U_r^dag 1_(B_1) U_r = 1_(B_r)" should be
    1_(B_(1/r)) under the displayed convention (or conjugate by U_(1/r)).
    Exact witness §6 A1a'/A1a''/A1b; conclusion unaffected (A1c
    involution; plus E1 :367-369 sealed alternative ground). NOT
    VERDICT-CHAIN.
k-2 COSMETIC — X-3 (consistency check only): trace-LSC under strong
    convergence needs positivity of the operators; the ground
    C_nPC_n = (PC_n)^dag(PC_n) >= 0 holds and is displayed at §6 A4, but
    the build's X-3 does not display it. NOT VERDICT-CHAIN.
k-3 COSMETIC — §2.3: the clause's own descriptive phrase "compressions
    onto the sealed carrier subspaces" (N.1), whose plain English reads
    as Q_n, is not quoted or confronted; the build's K-1/K-2/K-3
    resolution is CORRECT and the residue honestly carried (CH-A), but
    an identification argument should meet the strongest contrary byte
    at its own text. NOT VERDICT-CHAIN.
```

---

## 5. THE SWEEP (displayed; exhaustive at this sweep only)

Corpus roots swept recursively minus fenced filename classes
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF), patterns:
`kappa_n`, `[C_n, P]`, `[C, 1_B]`, `Shale`, `avoidance`, `C_n P C_n`;
plus the PA pinned-digest table chased row by row. Every hit examined:
clause/candidate/CERT/CHK/R1/R1-audit/build (the known chain);
E1 V001/V002, BL2STAR, ASSEMBLY_QUANTIFIER(+CHECK), G3_PANEL (C6
consumers, consistent); PUSHBACK_RESOLUTION_SWEEP (RL4 rows restate the
dichotomy, both branches open at their date); CHAIN_CHOICE_AUDIT_DRAFT
(r-2 named as residue); TASK2D (name collision, different kappa_n);
supervision-side BOHM notes (the blind C_n -> C verdict, corroborating;
route memos consistent). NO sealed artifact computes the actual s_i
spectrum, bounds kappa_n from above, or asserts bounded commutator mass
for the family of record. NO regularized family on the discharged basis
other than the carrier compression was found (V-N2 not triggered at this
sweep).

---

## 6. MY ADVERSARIAL CAS BATTERY (fresh venv `r2auditvenv`, sympy 1.14.0; output verbatim)

Script at scratchpad `r2_audit_battery.py`; the A1a Piecewise probe was
re-formalized as exact solution sets (a harness form, not a mathematical
change — the Piecewise-subtraction form returned an unevaluated object).
Final outputs, verbatim:

```text
A1b the build's displayed direction 1_(B_r) DIFFERS from the conjugation's 1_(B_(1/r)) at the exact witness (r, y) = (2, 1) — dagger-side slip, display-level only: PASS
A1c the conclusion is direction-independent: 1/r ranges over ALL positive reals as r does (exact involution r -> 1/r; (1/(1/r)) - r = 0): PASS
A2 U_r isometry exhibit: int r^{-3} e^{-(t/r)^2} t^2 dt = int e^{-u^2} u^2 du EXACT (= sqrt(pi)/4): PASS
A3 orthonormal-family Bessel: ||T||_F^2 - ||T u1||^2 - ||T u2||^2 = ||T u3||^2 >= 0 for ALL theta and ALL T (deficit exactly the omitted square): PASS
A4 CPC = (PC)^T (PC) >= 0 ALL theta (positivity ground the trace-route consistency check X-3 needs; exact factorization): PASS
A5 non-idempotent witness: at C' = (1/2)C, theta = pi/4 the identity ||[C',P]||_F^2 = 2tr(C'PC') - 2tr((C'PC')^2) FAILS exactly (difference = a nonzero exact rational) — the of-record kappa_n identity requires the PROJECTION realization (K-2 substantive): PASS
A1a' {y: ry<=1} = {y: y<=1/r} EXACT (the conjugation lands on 1_(B_(1/r))): PASS
A1a'' {y: ry<=1} != {y: y<=r} as sets for generic r (differ unless r=1): PASS
A6 QPQ - P = QP(Q-I) + (Q-I)P for ALL Q,P (re-derived in my own frame): PASS
```

Plus the build's own §6 battery re-run VERBATIM: 6/6 PASS (D1-D6,
R2-BATTERY-DONE), fresh venv, this session.

---

## 7. CHOICE LEDGER (this audit's own unforced choices, classified)

```text
AC-1 THE {C_n} IDENTIFICATION (pure Hermite vacuum projection family):
     PREMISE(named), same premise as the build's CH-A, reached
     INDEPENDENTLY pre-build from the same sealed constraints; my A5
     witness shows the of-record identity FORCES idempotence, and the
     D-mirror (E1 :770 with C1's C = sea) forces the sea reading —
     forced-among-sealed-candidates at the displayed sweep; residue
     identical to the build's (no sealed sentence writes the
     identification literally). The branch conclusion does not rest on
     it (scheme-robustness re-verified).
AC-2 PROOF FORM (WOT finite-block vs the build's SOT single-basis):
     IMMATERIAL(derived) — both exact; mine consumes a weaker hypothesis
     from the same sealed input.
AC-3 SWEEP PATTERN SET (§5's six patterns + pinned-table chase): OPEN —
     any grep sweep is bounded by its patterns; claimed only at its own
     displayed sweep. Mitigation: the patterns cover every symbol the
     dichotomy and the build use for the object.
AC-4 RE-RUNNING THE BUILD'S CAS IN A FRESH VENV RATHER THAN AUDITING ITS
     TRANSCRIPT: IMMATERIAL(derived) — reproduction is strictly
     stronger evidence than transcript inspection.
AC-5 CONSULTING SUPERVISION-SIDE NOTES (the blind C_n -> C verdict) AS
     CORROBORATION ONLY: PREMISE(named) — they are not defining stock
     and NOTHING in this audit's verdict rests on them; they are cited
     as independent confirmation found in sweep, never as ground.
```

## 8. TOY_SEPARATION (this audit's self-assessment)

```text
CLAIMED CLEAN at the stated quantifiers. Every CAS exhibit above is
either an all-quantified identity checked symbolically (A3/A4/A6 in free
symbols; A1a'/A1c as exact set identities) or an explicitly labeled
WITNESS instance used only to REFUTE a would-be universal (A1b the
dagger direction; A5 the non-idempotent failure) — no instance is quoted
wider than its derivation. The V/NV models were consumed only as
known-of-record context per the commission. The 1-D radial isometry
exhibit (A2) instantiates a change-of-variables identity whose general
form is classical; nothing downstream consumes more than the instance
shows. The RULING on the build's own TOY_SEPARATION: CLEAN — its CAS
instances are identity-grade with universal quantifiers displayed, and
no model family does load-bearing work in it.
```

---

## 9. FLAG BLOCK

```text
R2_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the build's branch
  verdict (B1-DERIVED): kappa_n -> +infinity at the FULL-family
  quantifier for the actual carrier compression family of record
  SURVIVES independent adversarial re-derivation; my derivation was
  fixed in working notes BEFORE the build was opened and reached the
  same conclusion from the same three sealed inputs by an independently
  fixed exact route (WOT lower semicontinuity, finite-block exhaustion);
  where our routes differ mine consumes the weaker hypothesis; no
  divergence between my result and the build's. )
CORRECTIONS = THREE-ALL-COSMETIC( k-1 step-R dagger-side slip
  (1_(B_(1/r)) not 1_(B_r); exact witness; conclusion survives by the
  r -> 1/r involution and E1's sealed dilation ground); k-2 X-3's
  undisplayed positivity ground for trace-LSC (holds, displayed here);
  k-3 N.1's "compressions onto the sealed carrier subspaces" phrase not
  confronted at its bytes in the identification argument (resolution
  nevertheless correct). NONE VERDICT-CHAIN. )
SEALS = 14/14-REVERIFIED( the build's 13 (incl. the G-13 supersession,
  independently confirmed via the R1-audit's c-1 and the guard's own
  append-only clause) + the build's own seal 3b5e95b6 vs sidecar. )
INDEPENDENT_PASSES = FIXED-PRE-BUILD( constructibility: spectrum
  sealed-nowhere, decisive aggregate derivable; derivation: B1 by
  C6 + a79939ad + LSC; both answers fixed to the scratchpad working
  notes before the build was read; post-read comparison: agreement. )
HAZARDS = NONE-TRIPPED( no unproven convenience load-bearing (the one
  premise named, scheme-robust); family-relativity ESCAPED by
  actual-family operator data with V/NV context-only; no verdict
  stronger than derivation (quantifiers audited one by one); no missed
  sealed object (sweep displayed; two corroborations found, one name
  collision flagged for the registrar: TASK2D's kappa_n is connected
  cumulants, not the commutator mass); no fence/ledger violation found. )
SCOPE_GUARD = INTACT( r-2 slot only; R-L4b NOT discharged; r-3
  untouched; B2 refuted at hypothesis level only; B3 does not arise for
  this family; F-b/F-c impossible at a = 0 only; F-d polydisc
  territory untouched; consumption of the (B1) input is the
  REGISTRAR'S after seal + check; this audit fires nothing, flips no
  flag, retires no witness; all three R-L4 witnesses STAND. )
FORBIDDEN_IMPORTS = none( exact symbolic only; two batteries (build's
  re-run 6/6 PASS + audit battery) in a fresh venv; no float consumed
  as ground; no measured constant; no value frozen (no numeric kappa,
  M, delta, D); sharp localizers; stricken display consumed nowhere;
  no fenced-class file opened; register tokens EXPECTED-UNLOCATABLE
  noted not chased; no git; ONE file written plus sidecar at the
  commission-distinct S9AD audit path, probed ABSENT before write. )
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked (the registrar's check governs).
R2_KAPPA_N_DETERMINATION_AUDIT_RESULT = SEALED.
```
