# STAGE 8 / 7A / Q-675 PATH-A — THE MAGNETIC-SECTOR HUNT: ONE SURVIVOR, CONSTRAINING NOTHING

Lane: DARIO (Builder B, independent verifier). Relay 773.
Governing: Q-675 — the flux correspondence stopped at the electric/magnetic category distinction.
**This relay is the hunt, not the ruling.** Nothing adopted, derived, or transported.

## Lead determination

**The record displays a magnetic-sector object exactly once, and it constrains nothing.**

Eighteen probes over **2,952 archive-side files** produced raw hits in seventeen. Reading every
one, **exactly one survives the meaning probe**: a sealed adversarial determination that
*constructs* a `U(1)` bundle over `S^2` **with nonzero first Chern class** —

```text
STAGE8_FIELD_SIGNATURE_PHYS_V002_ADVERSARIAL_REVIEW_DETERMINATION_V001.md
file sha  d11c0ffd6b24876ce8da7821fd733c326b41095e8b4a7d3a1e831a19a906641b   SEALED-OK
span      [10143,11262)   span sha  b0d024c2e0ad1f93ad6417265034224e950819f0f6367cedb466afebd3b62030
```

**It is a genuine display and not a word**: the object is built, its first Chern class is stated
nonzero, and a consequence is drawn from it. **It constrains the `D_BR` bundle class not at all**,
for three reasons given in §4: it is a **counterexample**, not a law; its base is
`M = R x S^2 x S^1` — the **A1 background family**, not `Sigma_BR = S^1_T x S^2_flux x S^1_Q`, so
using it would be the unproved transport the record bars; and what it establishes is that
nonzero-`c_1` members are **admitted**, which neither forces nor excludes any value.

**Everything else fails the meaning probe, and five false-positive classes are worth naming**
because each looks like a hit and is not — including the largest raw count in the whole sweep
(`c_1`, 553 occurrences, almost entirely **symbol-glossary entries**), an angular `integral_(S^2)
dOmega` in a coarea formula that is **not a curvature flux**, and `winding number` at 131
occurrences that is **numerical zero-free diagnostics**.

**And the record repeatedly states the absence directly** — *"the sealed 'integer character
lattice' is a **character** lattice, **not a flux** (**ABSENT**)"*; *"parent-arm monopole
correspondence = **still not supplied**"*. Those are not survivors; they are the record saying, in
its own voice, that the thing being hunted is not there.

```text
SELECTOR_CONSTRAINT = NONE.  The selector question passes whole to the principal's
(b)/(c) fork, as AS5 contemplates.
```

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively: **absent**, artifact and sidecar both.

### 0.2 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities.  No comparison to measured constants.  No common cell formed.
No junction map evaluated.  No smooth data derived.  NOTHING adopted, derived beyond the
display, or transported.  No register, plan, tracker, or git action.
```

---

## 1. AS1 — THE SEARCHED SPACE

```text
GLOB (stated):   ./workspace/**/*.{md,json}   +   ./supervision/**/*.{md,json}
                 recursive, run from the archive root, .git excluded
FILE COUNT:      2,952 files enumerated at run time
   of which      mirrored_sources/        220
                 corpus_members/          217   (all members, per AS1)
memory-bank:     NEVER SEARCHED (not present under these roots)
```

[YOURS] **`_DARIO_` files were deliberately NOT excluded**, contrary to my standing
writer-exclusion practice, and the reason is directional: **this relay's finding is a negative
existential, and a wider space makes a negative stronger, not weaker.** Every survivor is
provenance-tagged, and §3.6 records that the one probe dominated by my own artifacts (`deg L`)
contributes nothing independent.

**CONTROLS, case-sensitive** (the refinement installed at 766, after I inflated a control 20× by
matching case-insensitively):

```text
"S2_flux"       16 files        "deg L"          7 files
"E_parent"      17 files        "Fubini-Study"  81 files
```

All non-zero, so an empty probe below is a real absence.

---

## 2. AS2 — THE OBJECT-NAMES PROBE

Exact and normalized forms, hyphenation-sensitive (`[\s_-]` between tokens), backticked symbolic
names included.

| Probe | files | occurrences |
|---|---:|---:|
| magnetic charge | 2 | 2 |
| magnetic flux | 9 | 15 |
| magnetic (any) | 56 | 132 |
| 2-form flux | 3 | 4 |
| flux quantization | 6 | 9 |
| bundle class | 28 | 72 |
| line-bundle degree | **0** | **0** |
| `deg L` | 7 | 56 |
| first Chern | 7 | 9 |
| `c_1` (symbolic) | 145 | 553 |
| Chern number | 2 | 7 |
| Chern (any) | 11 | 20 |
| monopole | 10 | 34 |
| topological charge | 13 | 27 |
| topological sector | 6 | 8 |
| winding number | 5 | 131 |
| `integral_(S^2)` / curvature integral | 5 | 6 |
| Dirac quantization | 3 | 5 |

```text
HITS_RAW = 17 of 18 probes returned at least one file; "line-bundle degree" returned zero.
```

---

## 3. AS3 — THE MEANING PROBE, HIT BY HIT

**The rule applied: substring presence is not a display.** A display builds or states a
magnetic-sector object or law — a 2-form flux over a closed surface, a bundle class as record
content, a quantization of that flux. A glossary line, a rejection, an absence statement, or a
word in another object's sentence is not.

### 3.1 Rejections and absence statements — NOT displays, and they cut the other way

| Text | File | What it is |
|---|---|---|
| *"flux quantization — the sealed `"integer character lattice"` is a **character** lattice, not a flux (**ABSENT**)"* | `STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001` | an **ABSENT** verdict |
| *"parent-arm monopole correspondence = **still not supplied**"* | `STAGE8_7A_GATE_PROVENANCE_CODEX2_V001` | a **not-supplied** verdict |
| *"Do not re-propose flux quantization as a stiffness selector — quantizing flux is not fixing the parity-even coefficient"* | route graveyard, `BLOCK_ABSOLUTE` | a **standing bar** |
| *"It would **author** flux quantization — new physics well beyond an interface … Declined on minimality"* | `…XI_N_CANDIDATE_DARIO_V001` **[SELF]** | a **declined** option |
| *"Every member of this class — Atiyah–Singer, … Chern numbers — … a category error, cleanly"* | `BOHM_SLOT9_THEORY_ATTACK` | a **rejected mechanism class** |

[YOURS] **These strengthen the finding rather than qualify it.** The record does not merely fail
to display a magnetic sector — in four independent places it **states that it does not have one**.

### 3.2 Requirements and would-builds — NOT displays

`STAGE8_TASK5_EQ6_WHERE_CLAUSES_LANE2_V005` and `…FINAL_CHECK_LANE1`: the field law *"requires the
corresponding **first-Chern-class equation**"* before connection transport follows. **That is a
demand on an unbuilt object**, and the same lane's witness hunt lists *"graph cycle current **!=**
first Chern class"* among its symbol distinctions — i.e. it is separating the two, not supplying
one.

### 3.3 Symbol glossaries — NOT displays, and this is the sweep's largest false positive

`c_1` returns **145 files / 553 occurrences**, the biggest raw count in the probe. Reading them:
they are overwhelmingly **glossary lines** of the form
`c_1(P_bundle) = first Chern class` in EQ6 where-clause symbol tables
(`…ALLRANK_W1W4_MEMBERS…`, `…LOCAL_ORTHOGONAL_EXCISION_CERT…`), plus substring matches.
**A glossary entry defining a symbol is not a display of the object.**

### 3.4 Notation collisions — NOT displays

- **`integral_(S^2)`**: two of five hits are `integral_(S^2) … dOmega` in the **coarea formula**
  of `R3_4_LORENTZIAN_THRESHOLD_RETURN_*` — an **angular integral over the momentum sphere**, not
  a curvature 2-form flux. Same symbols, different object.
- **`winding number`**: 5 files but **131 occurrences**, nearly all
  `"winding_number": 0` inside QSPEC **numerical zero-free diagnostics** (`winding_by_volume`).
  Unrelated to bundle topology.

### 3.5 A different object under a shared name — NOT a display

`topological charge` (13 files) is almost entirely the **Ξ_N source-to-loop threading** — the
claim killed at OBS-21 (*"integrality is the authored codomain condition"*). It is a
record-surface threading assignment, **not** a 2-form flux. This is the name-match trap the sealed
census warns of, and it recurs here.

### 3.6 Self-sourced — excluded from survivor status

`deg L` (7 files): **five are `_DARIO_` artifacts of mine** (764, 765, 768, 770 and its relay),
one is Codex 2's cross-check *of my work*, one is the register *recording* it. **The probe's
entire yield is this campaign talking about itself.** At 765 the same probe returned **zero
files** — the corpus had no `deg L` before I introduced the notation. Contributes nothing
independent.

---

## 4. AS4 — THE ONE SURVIVOR, AND WHAT IT CONSTRAINS

### 4.1 The display

[PROVABLE] `STAGE8_FIELD_SIGNATURE_PHYS_V002_ADVERSARIAL_REVIEW_DETERMINATION_V001.md`,
`d11c0ffd…`, **SEALED-OK**, span `[10143,11262)`, span sha `b0d024c2…`:

```text
Take
   M = R x S^2 x S^1
   g = dt^2 - g_(S^2) - dtheta^2.
This is a globally hyperbolic four-manifold. Let `P_M` be the pullback of a
principal U(1) bundle over `S^2` with nonzero first Chern class. It is an A1
member as written. There is no global section. …

OPEN_PATH_SCALAR_HOLONOMY_CANONICAL_ON_FULL_A1_FAMILY = false | TYPE-R |
  test: nontrivial U(1) bundle counterexample above
```

**One sentence on what is displayed:** a `U(1)` bundle over `S^2` with **nonzero first Chern
class** is constructed, declared an admitted member of the A1 family, and used to prove that
scalar open-path holonomy is not canonical on that family.

### 4.2 Why it constrains nothing for `D_BR`

1. **It is a counterexample, not a law.** Its verdict is `TYPE-R` on a *holonomy* claim. It
   establishes that a nonzero-`c_1` member **exists in A1**; it states no law about which class
   any object carries.
2. **It is a different base.** `M = R x S^2 x S^1` — the A1 background family. `Sigma_BR =
   S^1_T x S^2_flux x S^1_Q` has a **circle** where this has `R`. Carrying the class across would
   be exactly the **unproved transport** the sealed separation discipline bars, and the difference
   is not cosmetic: `R` and `S^1` differ in precisely the compactness that makes the `T^2_TQ`
   momentum lattice discrete in the tower.
3. **Its content is admission, not selection.** *"Nonzero `c_1` members are admitted"* is
   consistent with every value in the surviving set and excludes none. It is, if anything, the
   opposite of a selector: it confirms the family is **not** restricted to `c_1 = 0`.

```text
SELECTOR_CONSTRAINT = NONE.
```

### 4.3 What I did not do

[YOURS] I did **not** transport the counterexample's bundle onto `Sigma_BR`; did **not** read
*"admitted"* as *"selected"*; did **not** treat the `first-Chern-class equation` requirement
(§3.2) as a supplied object. Each of those would have manufactured a constraint out of a
requirement, an admission, or a different family — the three shapes this thread has already been
caught by.

---

## 5. A PREREGISTERED EXPECTATION MET DURING THE HUNT — RECORDED, NOT CONSULTED

[PROVABLE] `PRINCIPAL_EXPECTATIONS_LEDGER_V001.md`, `a25af72e…`, **SEALED-OK**, carries **PE-4**,
headed *"SEALED 2026-08-09 ~03:20, **BEFORE relay 773 returns**"*, stating the principal's
pre-result expectation that the electric face is record-native while *"the MAGNETIC face (2-form
flux through a closed surface — a global continuum datum) is **NOT separately displayed record
content**"*, with *"**Zero verdict weight** throughout; **no 7A criterion may consult this
expectation**."*

[YOURS] **I encountered it inside the hunt and I record it rather than use it.** My probe list was
fixed by AS2 before I met it, every classification in §3 rests on the span read, and §4's verdict
rests on the survivor's own bytes. **No step above consults PE-4**, per its own clause. The
comparison between a preregistered expectation and an independently produced result is the
principal's to make, and I deliberately do not make it here — a hunt that reached for a sealed
expectation mid-flight would forfeit exactly the independence preregistration exists to buy.

---

## 6. GROUNDING, JURISDICTION, VERB AUDIT

### 6.1 Grounding

| # | Claim | Basis | Tag |
|---|---|---|---|
| 1 | Glob and count: 2,952 files; 220 mirrored, 217 members | §1, enumerated at run time | PROVABLE |
| 2 | Controls non-zero, case-sensitive | §1 | PROVABLE |
| 3 | Raw probe table | §2 | PROVABLE |
| 4 | Four independent absence/rejection statements | §3.1 | PROVABLE |
| 5 | `c_1`'s 553 occurrences are glossary lines | §3.3 | PROVABLE |
| 6 | `integral_(S^2)` collides with a coarea angular integral | §3.4 | PROVABLE |
| 7 | `winding number` is numerical diagnostics | §3.4 | PROVABLE |
| 8 | `deg L` is entirely self-sourced; zero files at 765 | §3.6 | PROVABLE |
| 9 | The survivor's text, span, and span digest | `d11c0ffd…[10143,11262)` | PROVABLE |
| 10 | The survivor's base differs from `Sigma_BR` | §4.2 | YOURS |
| 11 | PE-4 exists, is preregistered pre-result, zero weight | `a25af72e…` | PROVABLE |

**Pin check: 11/11.**

### 6.2 Jurisdiction check

**On reporting a near-empty result.** *What was the rule written to protect?* AS3's
substring-is-not-a-display clause protects against a sweep that counts words and reports a
finding. The risk was live: this sweep's largest count (553) is a glossary artifact, and its
second largest (131) is unrelated numerics. *Does the outcome space distinguish false from
cannot-see?* Yes — §2 reports the raw hits, §3 classifies every one, §4 displays the single
survivor with its span digest. *Would the evidence appear if the theory is right?* Yes: a sealed
magnetic-sector law would have surfaced in probes that found the word in seventeen of eighteen
cases.

**On the VOID CONDITION.** Nothing chosen from a desired outcome. The survivor is reported as a
display and refused as a constraint, in the same section.

**On PE-4.** §5 records it and declines to consult it, per its own zero-weight clause.

### 6.3 Self verb audit — **CLEAN, with three disclosures**

1. **I widened the space against my own standing practice**, including `_DARIO_` files, because a
   negative existential is strengthened by a wider search. Every survivor is provenance-tagged and
   §3.6 shows the one self-dominated probe yields nothing independent. **Stated so the deviation is
   visible rather than silent.**
2. **The sweep's biggest numbers are its emptiest.** `c_1` at 553 and `winding number` at 131 are
   both false-positive classes. Had I reported counts instead of reading spans, this artifact
   would have claimed a magnetic sector the record does not have. **That is the exact failure this
   lane has committed before**, and naming the two classes is the useful part of the result.
3. **`deg L` returned zero files at 765 and seven now** — all of them this campaign. A probe can
   be contaminated by the investigation itself, and I flag it rather than count it.

---

```text
SEARCHED = ./workspace/**/*.{md,json} + ./supervision/**/*.{md,json}  (2,952 files;
   mirrored_sources/ 220, corpus_members/ 217, all members).  memory-bank NEVER searched.
   _DARIO_ deliberately NOT excluded — a wider space strengthens a negative existential;
   survivors provenance-tagged.  Controls, case-sensitive: S2_flux 16, E_parent 17,
   deg L 7, Fubini-Study 81 — all non-zero.
HITS_RAW = 17 of 18 probes returned files ("line-bundle degree" returned zero); largest
   raw counts c_1 = 553 occurrences and winding number = 131, BOTH false-positive classes.
SURVIVORS = ONE.
   STAGE8_FIELD_SIGNATURE_PHYS_V002_ADVERSARIAL_REVIEW_DETERMINATION_V001.md
   d11c0ffd6b24876ce8da7821fd733c326b41095e8b4a7d3a1e831a19a906641b  SEALED-OK
   span [10143,11262)  span sha b0d024c2e0ad1f93ad6417265034224e950819f0f6367cedb466afebd3b62030
   DISPLAYS: a principal U(1) bundle over S^2 with NONZERO FIRST CHERN CLASS, constructed
   and declared an admitted A1 member, used to prove OPEN_PATH_SCALAR_HOLONOMY_CANONICAL_
   ON_FULL_A1_FAMILY = false | TYPE-R.
SELECTOR_CONSTRAINT = NONE.  The survivor constrains the D_BR bundle class not at all:
   (i) it is a COUNTEREXAMPLE, not a law; (ii) its base is M = R x S^2 x S^1 — the A1
   background family, NOT Sigma_BR = S^1_T x S^2_flux x S^1_Q, and carrying the class
   across is the unproved transport the record bars, the R-vs-S^1 difference being exactly
   the compactness that discretises the T^2_TQ lattice; (iii) its content is ADMISSION,
   not selection — "nonzero c_1 members are admitted" excludes no value and confirms the
   family is not restricted to c_1 = 0.
   Everything else fails the meaning probe: four independent ABSENCE/REJECTION statements
   ("a CHARACTER lattice, not a flux — ABSENT"; "parent-arm monopole correspondence still
   not supplied"; a BLOCK_ABSOLUTE graveyard bar; a declined authoring option), a
   first-Chern-class REQUIREMENT on an unbuilt object, 553 glossary occurrences of c_1,
   an integral_(S^2) dOmega coarea collision, 131 numerical winding_number diagnostics,
   the KILLED Xi_N topological-charge object under a shared name, and a deg L probe that
   is entirely this campaign citing itself (zero files at 765).
   THE SELECTOR QUESTION PASSES WHOLE TO THE PRINCIPAL'S (b)/(c) FORK, per AS5.
   PE-4, a preregistered zero-weight expectation sealed BEFORE this relay returns, was
   encountered mid-hunt and is RECORDED, NOT CONSULTED, per its own clause.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+3 disclosures at section 6.3, including that the sweep's two
   largest counts are its two emptiest, and that a probe was contaminated by this
   investigation's own notation)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
