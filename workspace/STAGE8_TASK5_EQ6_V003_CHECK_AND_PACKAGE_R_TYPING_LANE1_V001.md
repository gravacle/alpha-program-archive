# STAGE 8 TASK 5 / EQ6 - EXHIBIT V003 CHECK AND PACKAGE-R TYPING - LANE 1 V001

Date: 2026-08-04
Lane: Codex Lane 1
Task: 5 / EQ6
Custody: hostile review of Lane 2 V003; Package-R forward typing

## Lead result

```text
EXHIBIT_V003 = KILLED(Y1+Y2+Y3+Y5)

KILL_ANATOMY =
  V003 correctly rejects the Q-430 old-to-new Riesz sector mixer, but its
  LS-5 certificate then requires the stronger condition
    g_K(S K_G(O), K_G'(V)) = 0
  for every pair of disjoint physical sectors and claims this on Ref_0.
  DoR-019 does not imply that condition.  A rank-preserving two-cycle
  surface can have an exchange-covariant positive off-diagonal A4 metric,
  while P=id remains support-local and every Q-418 square commutes.
  Thus X1-8 does not imply X1-10, and the advertised LS-1-through-LS-8
  theorem on Ref_0 is false as stated.

MAXIMAL_GEOMETRIC_SCOPE_RETAINED =
  actual finite objects + rank-preserving Ref_path maps, with their actual
  path/current/test/kernel squares; the overstrong Riesz clause is removed.

SECTOR_MIXING_REGRESSION = PASS |
  the Q-430 old-to-new mixer is rejected on Ext_cycle.

PACKAGE_R_FIRST_UNDERIVABLE_OBJECT =
  a covariant nonempty family of actual local surface/path refinement
  relations on cycle-creating primitive arrows.

PACKAGE_R =
  R_path_current  : DECLARABLE_ONLY;
  R_test_transport: CONSTRUCTIBLE_WITH_ROUTE(after R_path_current);
  R_Riesz_locality: CONSTRUCTIBLE_WITH_ROUTE as a test of the fixed
                    DoR-019 metric, after correcting the condition to the
                    transported local-analysis image;
  R_local_fields  : DECLARABLE_ONLY for the new field member, with its
                    pullback/curvature maps DERIVABLE after that member.

C1_RESUMPTION = N-A
B_Q408_REFINEMENT_FULL_ROOT = OPEN / TYPE-U
JOINT_EQ6 = OPEN
```

The kill is bounded.  V003's most important correction survives: a formal
signed-chain/Riesz diagram is not a physical cycle-creating refinement.
The defect is in the opposite direction.  V003 turns a sufficient
old-image/new-cycle no-leakage condition into global orthogonality of all
disjoint current sectors, then attributes that stronger condition to the
already built rank-preserving scope.  The finite witness below refutes that
at the exact carrier where the claim is made.

---

## 0. Preflight, custody, and standards

### 0.1 Three-line preflight

```text
DOES THE_OBJECT_EXIST?  yes: V003 exists and Package R is explicitly named.
IS_THE_VERSION_CURRENT? yes: send-time register head Q-431.
ARE_ITS_INPUTS_PRESENT? yes: both prior kills, frontier row 1A, V005,
                        Q-408, DoR-019, Q-418, C1, and DoR-020.
PREFLIGHT = PASS
```

The artifact under review was hash-verified before reading:

```text
STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V003.md
SHA-256 = 74d88d6d450e15a8aed2c0f3db6d2d8c8b80f440b8cb0eab81845dfebcb6069d
SEAL = PASS
```

Verified standards and authorities:

| Object | SHA-256 | Role |
|---|---|---|
| locked process | `d537e294c03b3fc50fa49844f5b166bdcee1d64fe1513b9201047f602cd1518f` | geometry/rails rule and custody |
| questions-settled register at Q-431 | `4c6ee0fcd6b861baafb8ea24f1efd75b5067283ff926a77609722c703a35ba56` | send-time scope |
| V001 hostile kill | `505bd329b29a45bf57feec84b89c2fa53481a8117808df81872e15194b16d825` | new-cycle kernel and skeleton failures |
| V002 hostile kill | `4bfe044d62247950089142715e1534695922ac17c34e239cc63097df090347a1` | support-separated Riesz mixer |
| frontier row 1A | `b8294e917531dd4f1731aedd54fd1c52a320840335061bbb4ab872830d001028` | B_Q408 contract |
| field signature V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | actual paths, quotient currents, local field family |
| Q-408 current/kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | physical current analysis and finite kernels |
| carrier metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | fixed finite Riesz form and W3 scope |
| Q-418 Ref_path theorem | `fa73114214d7653d9a5d181a684b3bd65f0d6e9f91fd2256fb159ca97e56c4bf` | rank-preserving path/current/kernel square |
| C1 stopping artifact | `5203347c9bba7f99c2f033467815defa7ac047f74ea73b313972f44975d250a5` | maximal current physical core |

### 0.2 Register sweep

The sweep checked Q-297, Q-355, Q-384, Q-408, Q-418, Q-423 through Q-425,
and Q-427 through Q-431.  The bearing facts are:

```text
Q-297  physical scalar currents are the complete ker(B^T) family;
Q-355  no general cycle-creating upward quotient is available;
Q-384  the finite/completed metric exists, with W3 rank-preserving scope;
Q-408  finite path currents and faithful bilocal kernels exist;
Q-418  Ref_path parameter subdivisions carry an exact physical square;
Q-425  Ref_0 is the maximal built physical refinement category;
Q-430  Riesz-induced old-to-new support mixing kills V002;
Q-431  V003 claims locality on Ref_0 and names Package R.
```

No entry adds a locality theorem for the A4 inner product on disjoint
physical supports.  DoR-019 itself says that W3 does not prove generic
batching or cycle-creation isometry, and its authored automorphism clause
requires covariance/isometry rather than support orthogonality.

### 0.3 Verdict table

| Item | Verdict | One-line reason |
|---|---|---|
| Y1 proven scope | **KILL** | actual Ref_path maps are local, and the old-to-new mixer is rejected, but V003's LS-5/X1-10 orthogonality is neither required nor proved on Ref_0 |
| Y2 stopping point | **KILL WITH BOUNDARY CONFIRMED** | the first physical stop is the cycle-creating path relation; Package R is not one irreducible four-datum block, and its Riesz row is overstated |
| Y3 Package-R typing | **KILL AND RETYPE** | two rows are member data; two are conditional constructions/tests; global disjoint-sector Riesz orthogonality is not the lawful condition |
| Y4 whole-exhibit audit | **KILL WITH LARGE CORE SURVIVING** | anchors, composition, refinement, scoping, and anti-smuggling pass on Ref_path; the advertised LS-1-through-LS-8 board does not |
| Y5 fresh attacks | **KILL** | the rank-preserving off-diagonal metric witness refutes X1-10; the connection/curvature coherence attack exposes a mandatory subcertificate of local field transport |

---

## 1. Y1 - proven scope recomputed at geometry

### 1.1 Actual rank-preserving Ref_path calculation

For a q-fold parameter subdivision of an already realized oriented path,

```text
gamma_(e,j)(t)=gamma_e((j-1+t)/q),
gamma_e=gamma_(e,1) star ... star gamma_(e,q).    (Y1-1)
```

For every compactly supported physical test one-form a,

```text
sum_j integral_(gamma_(e,j)) a=integral_(gamma_e) a.  (Y1-2)
```

Let S copy a parent current coefficient to its oriented children and take
P=id on the same physical test-form space.  Then

```text
A_G=S^* A_G' P,
J_G'(S c)[P a]=J_G(c)[a].                         (Y1-3)
```

The support of the child-current sum is the support of the same physical
distribution, and the union of child path images is the parent image.  The
map is therefore local at the surface level, including self-intersections
and shared endpoints: no assertion that distinct child supports are
disjoint is needed.

On rank-preserving Ref_path, S is an isomorphism of the complete conserved
carrier.  W3 supplies

```text
S^* R_G' S=R_G.                                   (Y1-4)
```

Equations (Y1-3) and (Y1-4) give

```text
Phi_G' P=S Phi_G,
Kern_G'(j^H H)[P a,P b]=Kern_G(H)[a,b].          (Y1-5)
```

Composition is parameter-restriction associativity; orientation reversal
reverses child order and signs together; zero-extension contributes no
current; Gate-4 removes only coboundary presentation.  Identity, admitted
surface isomorphisms, finite restrictions, zero-extension, Ref_path, and
Gate-4 descent therefore retain their actual geometric maps.

```text
REF_PATH_PATH_CURRENT_TEST_KERNEL_SQUARE = PASS / TYPE-P
REF_PATH_SUPPORT_LOCALITY_OF_P_ID = PASS / TYPE-P
```

### 1.2 Q-430 sector mixer rerun

Take one old cycle in U, one new target cycle in disjoint V, and

```text
K_G=R,
K_G'=R e_U direct-sum R e_V,
S x=(x,0),
R_G=1,
R_G'=[[1,epsilon],[epsilon,1]],
0<|epsilon|<1.                                   (Y1-6)
```

With the finite analysis maps represented by the identity, the unique
arbitrary-profile pullback is

```text
I^T(1)=R_G' S R_G^(-1)(1)=(1,epsilon).           (Y1-7)
```

The algebraic kernel square commutes because R_G'^(-1)I^T=S, but (Y1-7)
has a component on the disjoint new sector.  It fails support locality and
is not a V003 member.

```text
Q430_SECTOR_MIXER_REJECTED = PASS
```

### 1.3 Fresh rank-preserving counterexample to X1-10

Now use two old record-visible cycles with disjoint actual supports U and V
on one admitted finite surface.  Work in the localized current/test basis:

```text
K_G=R e_U direct-sum R e_V,
Abar_G=id,
R_G=[[1,epsilon],[epsilon,1]],
0<|epsilon|<1.                                   (Y1-8)
```

This metric is positive, real, and invariant under the exchange U<->V.  It
therefore satisfies every finite positivity/reality/automorphism condition
DoR-019 states.  DoR-019 has no support-locality axiom that sets epsilon to
zero.

Apply any rank-preserving Ref_path subdivision.  In identified coordinates,

```text
S=id,
P=id,
R_G'=R_G,
I^T=R_G' S R_G^(-1)=id.                          (Y1-9)
```

Thus an old test supported in U remains supported in U; (Y1-3)--(Y1-5)
all pass.  Nevertheless V003's X1-10 demands

```text
g_K,G'(S e_U,e_V)=0,                             (Y1-10)
```

while the left side is epsilon.  Hence:

```text
X1_8_IMPLIES_X1_10 = false / TYPE-R
GLOBAL_DISJOINT_SUPPORT_RIESZ_ORTHOGONALITY_ON_Ref_0 = false / TYPE-R
```

The reason is exact.  Support locality concerns the transported test image,
not every current vector whose distribution happens to be localized.  In
(Y1-8), R_G^(-1) sends the local analysis covector through both cycle
coordinates, and multiplication by the same R_G cancels that mixing before
the physical test class is recovered.  The metric may be nonlocal while the
rank-preserving physical test transport remains local.

### 1.4 Correct relative locality condition

For a candidate cycle-creating relation, the necessary pairing condition is

```text
g_K,G'(S Phi_G(Tbar_G(O)), K_G'(V))=0
whenever F_R(O) intersect V=empty.               (Y1-11)
```

It is the transported local-analysis image S Phi_G(Tbar_G(O)), not all of
S K_G(O), that the connection/current pairing tests.  To obtain an actual
support-local test class, one must additionally prove the local-range
condition

```text
Abar_G'^(-1) R_G' S Phi_G(Tbar_G(O))
  subset Tbar_G'(F_R(O)).                        (Y1-12)
```

For the one-dimensional Q-430 witness, (Y1-11) is exactly the vanishing of
the old-image/new-cycle off-diagonal block.  In higher rank it is weaker
than global orthogonality between every pair of disjoint old current
sectors.  Conditions (Y1-11)--(Y1-12) are failure-capable and basis-free.

---

## 2. Y2 - exact stopping point and Package-R minimality

### 2.1 What is exhausted before the stop

The following content is already derived before any Package-R member is
supplied:

```text
objectwise actual paths and finite current supports;
the signed incidence/cycle map on the rail category;
the finite analysis and faithful Q-408 kernel maps;
the fixed DoR-019 Riesz maps;
the unique algebraic candidate
  P_alg=Phi_G'^(-1) S_R Phi_G;
target new-cycle currents and kernels at an actual target object;
Ref_path physical transport and every rank-preserving restriction square.
```

The first unavailable physical object is not all of Package R at once.  It
is an actual relation between source and target surfaces on a primitive
cycle-creating arrow:

```text
f_R:M_G->M_G',
f_R gamma_(G,e)=ordered target-child path chain,
J_G'(S_R c)=f_(R*)J_G(c).                        (Y2-1)
```

No ratified clause selects or constructs (Y2-1) for a new interior PL edge.
The formal signed map does not imply it.  Therefore V003 is correct that the
full B_Q408 root remains open, but its indivisible Package-R stopping label
is too coarse.

### 2.2 What is padded and what is not missing

V003's four headings cover the right territories, subject to two repairs:

1. The Riesz territory must use (Y1-11)--(Y1-12), not global X1-10.
   X1-10 is a sufficient special case and is not a necessary physical law.
2. Local field transport must explicitly include curvature formation and
   coframe/density duality commuting with transport.  These are proof
   obligations inside the fourth component, not additional roots.

New-cycle support/kernel retention is not a fifth member datum.  It follows
from injectivity of S_R, the finite target's faithful Q-408 kernel, and the
rule that the physical pullback is asserted only on the old image.  Likewise
covariance, composition, and actual common-refinement equality are family
certificates on the four components, not additional physical fields.

```text
PACKAGE_R_TERRITORY_COUNT = 4
PACKAGE_R_AS_STATED_EXACT = false / overstrong_Riesz_row
PACKAGE_R_AFTER_RETYPE_EXACT = true | subject_to_family_certificates
```

---

## 3. Y3 - Package-R typing table

| Component | Geometry currently present | Rails currently present | Typing | Exact route or clause candidate |
|---|---|---|---|---|
| R1 path/current support correspondence | objectwise V005 paths and Q-408 currents only | signed incidence/cycle map and formal composition | **DECLARABLE-ONLY / TYPE-U** | Declare a covariant nonempty family of proper local primitive refinements f_R carrying every old path to an oriented target-child chain, inducing injective S_R, current pushforward, support inclusion, composition, and actual common-refinement equality. Void on any unrelated-path witness or deleted visible cycle. |
| R2 support-local test transport | Ref_path has P=id; no Ext_cycle map | unique algebraic P_alg exists | **CONSTRUCTIBLE-WITH-ROUTE after R1/R3** | Compute P_alg=Phi_G'^(-1)S_RPhi_G, then prove (Y1-12), current pairing, boundedness, reality, units, and kernel restriction. Uniqueness forbids authoring a different P. Failure means that R1 candidate is not an admissible generator. |
| R3 Riesz locality | fixed DoR-019 metric at each endpoint | W3 rank-preserving isometry only | **CONSTRUCTIBLE-WITH-ROUTE as a certificate; V003 global form REFUTED** | For each R1 candidate, compute (Y1-11) on actual local sectors and prove it family-wide. The Riesz map is fixed, so there is no free R3 member to select. If the check fails, exclude the candidate; changing the metric would require a separate DoR-019 amendment. |
| R4 local field transport | objectwise A1/A2 coframe, density, connection, curvature data | typed P4 slots Cof_R, Dens_R, eta_conn,R, eta_curv,R | **DECLARABLE-ONLY for new field member; DERIVABLE transport after member** | Declare a covariant family of positive, unit-correct local field extensions on new cells compatible with f_R. Then use geometric pullback to derive the maps and prove Dens/Cof duality plus eta_curv(F_A)=F_(eta_conn A), support, E_post, reality, composition, and diamonds. |

### 3.1 Why R2 is not independent authorship

V003's own arbitrary-profile theorem proves that a physical test map, if it
exists, is fixed (componentwise orientation signs being fixed by the actual
path orientation):

```text
P_R=Phi_G'^(-1)S_RPhi_G.                         (Y3-1)
```

An authored alternative P would either fail the Q-408 arbitrary-profile
square or change the ratified Riesz/analysis data.  The lawful work is to
construct (Y3-1) and test its local range, not to add a P coordinate by
choice.

### 3.2 Why R3 cannot be declared silently

DoR-019 fixes g_K from the retained A4 form.  A Package-R witness may search
for actual path relations whose induced S_R passes (Y1-11), but it may not
zero a cross term by redefining g_K.  Such a redefinition changes a ratified
finite metric and fires DoR-019's standing falsifier.

### 3.3 Geometry/rails split per component

```text
R1 geometry = actual local path/current relation; rails = signed chain map.
R2 geometry = local test support and physical pairing; rails = P_alg square.
R3 geometry = no leakage into physically disjoint new support on the
              transported local-analysis image; rails = positivity/W3.
R4 geometry = local field extension and genuine tensor/connection pullback;
              rails = typed Cof/Dens/eta slots and functor equations.
```

None of R1 or the member part of R4 is a rail artifact.  R2 and R3 are not
new roots: they are constructions/certificates once candidate physical data
exist.  This reduces the declaration burden inside B_Q408 without removing
the root.

---

## 4. Y4 - whole-exhibit audit

### 4.1 Anchors

| Claim | Result |
|---|---|
| finite stage objects G,B,K,Q,Tbar,Abar,R,Kern | **PASS** - anchored in V005/Q-408/DoR-019 |
| objectwise local sectors from actual current support | **PASS** |
| identity/relabeling/reality/finite restriction/zero-extension | **PASS on stated scope** |
| Ref_path relation | **PASS** - actual parameter subdivisions and P=id |
| Ext_cycle relation | **PASS AS OPEN/TYPE-U** |
| global X1-10 on built scope | **FAIL** - witness (Y1-8)--(Y1-10) |

### 4.2 Composition and refinement

Ref_path composition follows directly from nested interval restriction.
Actual Ref_path diamonds use common parameter subdivisions of the same
realized path and therefore commute at path, current, test, support, and
kernel levels.  V003 correctly refuses to promote formal Ext_cycle diamonds.

```text
COMPOSITION_Ref_path = PASS
COMMON_REFINEMENT_Ref_path = PASS
COMPOSITION_Ext_cycle = OPEN / TYPE-U
```

### 4.3 Seven regressions

| Regression | Independent result |
|---|---|
| pure new-cycle kernel | **PASS** - retained at target; no equality on all fine tests |
| covariance orbit/moduli | **PASS** - no orbit representative chosen |
| all-stage skeleton | **PASS** - explicitly stagewise only |
| rank-preserving Ref_path | **PASS for physical maps; FAIL for added global X1-10** |
| cycle-creating upward quotient | **PASS** - none installed |
| arbitrary-profile restriction | **PASS WITH SCOPE** - physical on Ref_path, rail-only on Ext_cycle |
| Q-430 Riesz sector mixer | **PASS** - rejected before membership |

The five permanent regressions and joint-equalizer check also pass.  V003
does not substitute an abstract kernel, define geometry from a reader,
claim full nonemptiness, infer Hodge from isometry, select a minimum, or
promote its partial exhibit to EQ6 inhabitance.

### 4.4 Smuggling and the too-easy check

V003 does not make the seam look easy: it withdraws the full exhibit and
keeps C1 stopped.  No response, threshold, fixed point, or measured datum is
used.  The defect is an internal mathematical overclaim, not target tuning.

```text
SMUGGLED_DOWNSTREAM_PROPERTY = false
TARGET_TUNING_USED = false
TOO_EASY_SUCCESS_CLAIMED = false
```

---

## 5. Y5 - two fresh attacks

### 5.1 Fresh attack A: nonlocal metric with local identity transport

The rank-preserving witness (Y1-8)--(Y1-10) is fresh relative to V003's
battery.  It keeps both disjoint cycles in the old carrier, so the same
off-diagonal Riesz form appears at both ends and cancels in P_alg.  It proves
that V003 has confused:

```text
no NEW support leakage under refinement
```

with the stronger and unratified statement

```text
the A4 metric is local on EVERY pair of disjoint currents.
```

The first is required; the second is not.

### 5.2 Fresh attack B: connection/curvature mismatch

Suppose a candidate R4 member gives support-local, real, unit-correct maps
eta_conn and eta_curv but omits curvature-formation compatibility.  On a
nonzero local curvature take, schematically,

```text
eta_conn=id,
eta_curv=2 id.                                   (Y5-1)
```

Both maps preserve support and can commute with relabeling, but

```text
eta_curv(F_A)=2F_A != F_(eta_conn A)=F_A.        (Y5-2)
```

Thus separate local field maps do not constitute physical field transport.
The corrected R4 clause must include

```text
eta_curv compose Curv = Curv compose eta_conn,   (Y5-3)
```

and the analogous coframe/density duality square.  This is a proof obligation
inside R4, not a fifth Package-R component.

---

## 6. Falsifiers and final disposition

### 6.1 Finite and restriction checks

| Check | Result |
|---|---|
| Q-408 bottom object | **PASS** - no finite current/kernel value changed |
| DoR-008 Ref_path restriction | **PASS** - equations (Y1-1)--(Y1-5) are exact |
| one-edge/tree | **PASS** - K=0; no artificial cycle |
| pendant quotient | **PASS** - Gate-4 precedes current realization |
| S8-A rank-two exchange | **PASS for covariance; exposes the allowed exchange-invariant epsilon metric** |
| reality/orientation | **PASS** - epsilon real; reversal acts covariantly |
| batching | **PASS WITH ORIGINAL SCOPE** - no batching isometry inferred |
| pure-new target kernel | **PASS** - remains outside old pullback |
| unrelated paths | **PASS AS REJECTION** - no R1 membership |
| Riesz old-to-new mixer | **PASS AS REJECTION** |

The counterexample is itself finite, so it cannot be hidden by completion.
It reproduces every sealed rank-preserving current/kernel restriction while
refuting only V003's extra global orthogonality statement.

### 6.2 Surface geometry versus rails

| Territory | Surface geometry proved | Rails proved | Honest status |
|---|---|---|---|
| finite and Ref_path | actual parameter paths, current support, P=id, finite kernels | restriction/isometry diagrams | **BUILT after deleting X1-10 overclaim** |
| cycle-creating path relation | none beyond objectwise endpoints | signed chain map | **R1 DECLARABLE-ONLY / TYPE-U** |
| cycle-creating test map | locality not proved | unique P_alg | **R2 CONSTRUCTIBLE-WITH-ROUTE** |
| Riesz locality | relative no-leakage not checked | positivity, covariance, W3 | **R3 CONSTRUCTIBLE-WITH-ROUTE; global V003 form refuted** |
| local fields | objectwise fields only | typed P4 slots | **R4 member DECLARABLE-ONLY; pullback then derived** |

### 6.3 Final lines

```text
EXHIBIT_V003 = KILLED(Y1+Y2+Y3+Y5)

PACKAGE_R =
  path/current support correspondence = DECLARABLE_ONLY;
  support-local test transport = CONSTRUCTIBLE_WITH_ROUTE(after path +
    corrected local-duality checks);
  Riesz locality = CONSTRUCTIBLE_WITH_ROUTE on the fixed metric, using
    Y1-11/Y1-12, not global X1-10;
  local field transport = DECLARABLE_ONLY for new member data, with
    pullback/curvature transport DERIVABLE after the member.

GEOMETRY_VS_RAILS =
  actual finite + Ref_path geometry survives;
  Ext_cycle signed maps and P_alg remain rails until R1-R4 pass;
  the R1 and R4 member data are physical, while R2/R3 are conditional
  construction/certificate obligations rather than new declaration roots.

C1_RESUMPTION = N-A
B_Q408_REFINEMENT_FULL_ROOT = OPEN / TYPE-U
JOINT_EQ6 = OPEN

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
measured_constant_compared = false
MACHINERY_APPEAL = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

No register, plan, tracker, git, commit, or push action belongs to this lane.
