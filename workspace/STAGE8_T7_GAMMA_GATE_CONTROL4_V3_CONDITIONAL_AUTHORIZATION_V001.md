# Stage-8 T7 Control-4 v3 — Conditional Principal Authorization V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_SUPERSEDING_PRINCIPAL_DECISION
```

This SUPERSEDES item 1 only of STAGE8_T7_GAMMA_GATE_FINALITY_DECISION_
RECORD_V001 (the refusal to author control-4 v3). That record is NOT
altered. Everything else in it STANDS unchanged: the beta disposition,
the preservation of the computation as computation, L4 as a sealed
structural discovery with the three defeated designs as its evidence
trail, and the calibration endorsement.

## The reversal and its recorded reason (the principal's)

```text
CONTROL-4 v3 IS AUTHORIZED, CONDITIONALLY.
```

Reason of record, plainly: the prior refusal declined v3 because a
falsifier chosen after three defeats is selected with foreknowledge of
its effect. That reasoning optimized for a CLEAN RECORD rather than for
FINDING OUT. Declining leaves a permanent gap in what is known — whether
the apparatus can detect a-parity breaking AT ALL — and buys only
procedural tidiness. The foreknowledge objection is real but DISSOLVABLE
BY COMMITMENT, not a reason to abandon the question.

## CONDITION 1 (gating; argument before any code)

Before v3 is authored, a typed operator-level argument must answer:

```text
Does a perturbation that LEAVES the antiunitary reality class still
exercise THE SAME APPARATUS that control-4 is chartered to certify —
the a-parity detection path for Lemma 2 on the completed-record object
— or does it exercise a DIFFERENT detection path?
```

The argument must state exactly what control-4 certifies, then show at
the operator level whether the class-leaving perturbation drives that
same path, naming the shared machinery explicitly (which operators,
which contraction, whose derivative is read).

```text
OUTCOME A (SAME APPARATUS): v3 is a legitimate control -> Condition 2.
OUTCOME B (DIFFERENT APPARATUS): the control is IMPOSSIBLE IN PRINCIPLE
  for this gate. That is a STRONGER structural finding than "three
  designs defeated" — it says WHY no design can work. Seal it as such;
  the gate's BLOCKED verdict then stands on a proven basis rather than
  an exhausted search, and the prior decision's disposition is correct
  after all (and this lane must say so).
```

The lane is instructed NOT to assume Outcome A. The honest answer may be
Outcome B.

## CONDITION 2 (if and only if Outcome A): freeze the prediction first

A spec sealed BEFORE any execution must contain: (a) the predicted
|Z'(0)| magnitude under the class-leaving falsifier WITH its window and
the reasoning deriving it from the witnessed |dD| ~ 1e-4 positive
control — not merely "above the 1e-9 floor"; (b) the predicted unbroken
behaviour with its window; (c) what outcome would REFUTE the claim that
this control has teeth; (d) an explicit statement that the falsifier was
selected after three defeats, that the principal authorized it knowing
this, and that the frozen prediction is what restores its evidential
value — selection history and compensation visible together.

CALIBRATION REQUIREMENT (standing, per the ledger): this lane's
magnitude predictions have MISSED TWICE CONSECUTIVELY while its
sign/outcome-class predictions have been reliable. The magnitude window
must therefore be stated EXPLICITLY WIDENED, with that reason given in
the spec. A third narrow-window miss would be uninformative; a hit
inside an honestly widened window is worth something.

## CONDITION 3: what the outcome does and does not change

```text
v3 BITES  -> control-4 satisfied; the gate's control obligation is
  discharged; the gate may be re-run to a verdict under its existing
  sealed rules. GAMMA_REFUTED_NONZERO would then fire from B1 — but the
  reviewer's standing position on the blind-commitment chain still
  governs whether it may be CITED as established refutation evidence.
v3 DOES NOT BITE -> recorded as a FOURTH defeated falsifier; materially
  strengthens the case that the protection is structural rather than a
  design deficiency; the gate stays BLOCKED, finally; L4 grows in
  significance.
EITHER WAY -> gamma's refutation continues to rest on the STRUCTURAL
  argument (exact counterexample; kappa_A = 0.707 vs kappa_B = 1.802 at
  leading order in ||J||^2). No artifact may cite the gate as
  established refutation evidence absent the reviewer's acceptance.
BETA IS NOT CONTINGENT ON ANY OF THIS and proceeds now, as adopted.
```

## Protected status

```text
control4_v3_conditionally_authorized = true
control4_v3_condition1_satisfied = false
gamma_gate_verdict_final = false   (superseded: reopened under this authorization)
gamma_refutation_computed = false
ER_A_disclosed_premise_adopted = true
alpha_computed = false
proof_authorized = false
```
