# STAGE 8 — AXN — THE RECORD-CARRIER LABELLING DISCREPANCY: FACTS AT BYTES
## DARIO LANE — RELAY 930 — `[PLAN:AXN-BUILD-D29]`

## 0. Preflight

Relay 930 verified before reading at
`436a4a8b34956b568b9d6741c7d3540a04a0690a9b5f6f6666146e616c6344d8`. Lane guard read DARIO; the
pickup ACK was written before content access. `PROGRAM_STATE_BRIEF_V005.md` verified and read before
task work at `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`. The output name was
clear.

---

## 1. Law-9b closure — declared first

```text
C_930 = {
 1  RELAY_PASTE_930_RECORD_LABELLING_DARIO_V001.md
      436a4a8b34956b568b9d6741c7d3540a04a0690a9b5f6f6666146e616c6344d8
 2  supervision/PROGRAM_STATE_BRIEF_V005.md
      e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
 3  STAGE8_AXN_CARRIER_CONSTRUCTIONS_DARIO_V001.md              [my 928 — BR-1, §2.2]
      3c59ba330e84d2062600aa3021dd61e1cca1a31b3967263b7ca4c211880bf0b0
 4  R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md    [SITE A]
      40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9
 5  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md        [SITE B primary — UNSEALED, §3.2]
      b5a60c354d05e84e91d4f1fbeb45c3bce83776c59df7fc307e65ef2a529d5287
 6  STAGE8_GAMMA_K_COMPLETED_ALGEBRA_EXTENSION_ATTEMPT_V001.md  [the sealed quotation of member 5]
      2198524f9f4b4f048953526e4084ae7282e85edce1767e410aef50b013ed0ab1
 7  supervision/QUESTIONS_SETTLED_REGISTER_V001.md              [swept FIRST]
      3e8db2e25c7e4f7114bb3fbe9668b7ba0a4d0bf3032283aa41de1be8d5c4e7a6
 8  STAGE8_7A_PB_INSTANCE_RECEIVER_CODEX2_V001.md               [third lineage — FENCED, §4.3]
      3f321101430d4397eb8ff1051a3d387432f1f79c08a7fdc0df052924ed65e05e
}
```

| key | bytes | span SHA-256 | content |
|---|---|---|---|
| `SITE-A` | member 4 `[4220,4680)` | `e9f1e91a4a0d1a5b78ecb7214927da990daeb2bb005317b6418befc1f17572f0` | `R_c=span{|r_c>,|p_c>,|e_c>}` with its carrier context |
| `A-CHG` | member 4 `[4712,4905)` | `b652833551e490f2e10a4618f57b55d923ac1c8ac5536f9a8c61b0bd1f7c445b` | where charge enters in lineage A |
| `SITE-B-Q` | member 6 `[1643,2103)` | `fb0ccce276d0be1c96760c00dd56b58f507faef2cb52c18732d449d9c2a5014d` | the sealed quotation of member 5's `R_c` |
| `Q-661` | member 7 `[1343185,1344356)` | `ed7575a2c79e2bc47fc56281fe427b26c04e74abd525cd88ab8704fef814b094` | the Q-69 name census and its keep-separate ruling |

**Name probe.** `STAGE8_AXN_RECORD_LABELLING_DARIO_V001.md` and its sidecar: clear at pickup.

---

## 2. Gates and standing

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

All items **CLAIMED**. **This report establishes facts and adjudicates nothing.** No preference, no
resolution, no recommendation. **BLIND held:** the subscript is treated strictly as a *label*
question — where charge is written — never as a rank, ratio, or fiber comparison.

**BR-1.** I surfaced this discrepancy at 928, so member 3 carries **zero** confirming weight. Both
declaration sites were re-opened at their own bytes in this relay rather than cited from my report of
them. The re-reading confirms both quotations verbatim; §4.3 records where the re-reading turned up
something that **cuts against** an inference of mine.

**The asymmetry I declared at pickup, applied.** "Notational" requires **positive evidence** — a
sealed identification shown at bytes. Absence of a stated difference is not evidence of sameness.

---

## 3. The two sites, and their provenance chains

### 3.1 Site A — the parent-spec lineage

`SITE-A`, member 4, dated **2026-07-24**, verified against **its own sidecar**:

```text
On a Cauchy surface `Sigma`, let
  K_Sigma = L2(Sigma, S tensor L^q);
  R(K) = tensor_(c in K) R_c;
  R_c = span{ |r_c>, |p_c>, |e_c> }.
There is one global source CAR algebra CAR(K_Sigma). Record factors are
even and distinguishable; no source carrier is copied per record.
```

Provenance: member 4's own pinned-authority table descends from FBRA V002, PSC V001 and CISP V001
(the three adopted Level-1 premises), plus the BID superconnection/CPT chain. **No pointer to member
5 appears anywhere in it** — see §3.3.

### 3.2 Site B — the composition-derivation lineage

Member 5, dated **2026-07-23**, under its own heading **"## Labeled record factors"**:

```text
For each causal record cell `c`, retain the distinguishable record carrier
  R_c = span_C{ |r_c>, |p_Q,c>, |e_Q,c> }.
For a finite labeled cell set `C`, the public record algebra is
  R(C) = tensor_(c in C) B(R_c).
The total source-record algebra is
  A_SR(C) = CAR(K_Sigma,q) graded-tensor R(C).
Give every record operator trivial fermion grading.
```

**Custody fact, carried from relay 911 and re-verified here: member 5 has no sidecar of any spelling.**
It is reachable as a sealed object only through member 6's verbatim quotation at `SITE-B-Q`, whose own
seal verifies. So the two sites differ in custody kind: **A is directly sealed; B's primary is
unsealed and reached through a sealed quotation.**

### 3.3 Seniority

| test | result |
|---|---|
| does A cite B? | **no** — `grep` for the member-5 filename in member 4 returns `0` |
| does B cite A? | **no** — `grep` for the member-4 filename in member 5 returns `0` |
| is either downstream of a correction to the other? | **none of record** — with no citation either way, no supersession or restatement chain exists between them |
| dates | B **2026-07-23**, A **2026-07-24** — one day apart |
| seal kind | A: own sidecar. B: no sidecar; sealed only as a quotation inside member 6 |
| register adjudication | **none** — see §4.1 |

```text
SENIORITY = NONE-OF-RECORD
  Neither lineage supersedes, restates, or cites the other. The date and seal-kind
  asymmetries are displayed as facts and are NOT offered as a ranking.
```

---

## 4. The substance test

### 4.1 The register carries no adjudication of the labelling

Swept first, per the relay. `charge subscript` returns **0**. `p_Q` and `e_Q` return **1 each**, and
that single occurrence is **my own 928 entry**. The eight `R_c` matches are mostly `R_comp`, a
different object; the genuine `R_c` hits concern a cellular orientation unitary and the name census
at `Q-661`.

```text
No settled record on the record-carrier labelling predates 928.
```

### 4.2 `Q` is never defined in its own source

**Member 5 uses the subscript once and never says what it denotes.** A scan of member 5 for `Q` in a
defining context returns only the `R_c` line itself and an unrelated `complete_Q_spec_sealed = false`
status. The heading calls them *"Labeled record factors"* and the sentence calls the carrier
*"distinguishable"*, but **no sealed sentence in that lineage states what the label ranges over.**

### 4.3 A third lineage uses the same three names — and the register fences it

Member 8 (own seal verified) types `r`, `p_Q`, `e_Q` as a three-channel rooted star:

```text
r --M--> p_M ,   r --Q--> p_Q ,   r --G--> p_G
edges: e_M, e_Q, e_G, each an abstract unitary  L_r -> L_p*
```

There `Q` is unambiguously **one of three channel labels**, and `e_Q` is a **unitary**, not a basis
vector. That is suggestive for the substance question — and **the register forbids using it.**
`Q-661` carries the Q-69 name census:

> *"SEVEN DISTINCT OBJECTS SHARING 'complex' OR 'cell', KEPT SEPARATE — `Omega_c` a Lorentz-covariant
> continuum diamond, not a CW object; **`R_c` a three-dimensional Hilbert span, not a cell of any
> complex**; 'merging any two on the word cell would be the name-match trap the charter warned
> about'."*

Member 8's carrier is `C_0(K_open_PB;L)` — a complex's chain group with Hermitian lines. **Importing
its channel reading into `R_c` is exactly the name-match trap the census fenced.** I found the third
lineage, and I record that the record bars its use rather than leaning on it.

### 4.4 Where charge sits in lineage A

`A-CHG`, member 4:

> *"The charged-control projector is the spectral projector of the disclosed source charge, and `c_c`
> is extended by the identity on all other record factors. No new cell coefficient is admitted."*

So in lineage A charge enters through a **spectral projector of the source charge**, with the cell
operator extended by the identity elsewhere. Lineage A therefore writes no charge into the record
basis labels. **This is a fact about lineage A's own construction, not a statement about lineage B.**

### 4.5 One agreement worth recording, and its limit

Both lineages state the record factors are **even** — A: *"Record factors are even and
distinguishable"*; B: *"Give every record operator trivial fermion grading."* **Evenness is about
FERMION grading, not about charge.** It makes the graded tensor product collapse to the ordinary one
in both lineages, and it decides nothing about whether a charge label sits on the pointer and edge
states.

### 4.6 Verdict on substance

```text
SUBSTANCE = UNDECIDABLE-OF-RECORD

  NOT provably NOTATIONAL : no sealed identification of |p_c> with |p_Q,c> exists anywhere
                            in the corpus. "Notational" has no positive evidence, and I
                            declined to let it win by default.
  NOT provably SUBSTANTIVE: within the R_c lineage no sealed sentence states what Q ranges
                            over, so no distinguishing content is exhibited either.
  AND the one lineage where Q has a sealed meaning is EXPRESSLY FENCED from R_c by Q-661.
```

---

## 5. Relating act

```text
RELATING_ACT = ABSENT

AUTHORITY TABLE (law 9b), each entry a pinned sealed object rather than a failed grep:
  member 7  register: `charge subscript` 0 hits; `p_Q`/`e_Q` 1 hit each, both my own 928 entry;
            no Q-entry adjudicates the record-carrier labelling.
  member 4  contains no reference to member 5 (citation grep = 0).
  member 5  contains no reference to member 4 (citation grep = 0).
  member 5  introduces the subscript once and defines it nowhere.
  member 7  Q-661 supplies an adjacent KEEP-SEPARATE ruling about a DIFFERENT pair
            (R_c against cells of a complex). It is not an identification of the two
            labellings and is not offered as one.
```

No map, identification, or correction relates the two sites.

---

## 6. Consequence map — both branches, neither adopted

### 6.1 If NOTATIONAL

The subscript would carry no state content, and my 928 **freedom 2** — that two sealed sources label
the carrier differently — would dissolve.

**My 928 freedom 1 would survive untouched:** writing `E_joint = E_ch (x) id_R_inf (x) id_B` still
asserts that joint superselection acts trivially off the source factor, and that is a claim about
where charge lives which the tensor presentation does not supply. `E_ch` remains declared on the
source sector only.

```text
=> the superselection field STILL STOPS, on one freedom instead of two.
```

### 6.2 If SUBSTANTIVE — and the relay's sharp question answered

If the subscript marks charge content on the pointer and edge states, then the record factor carries
a nontrivial charge structure of its own. A joint superselection conditional expectation must fix
exactly the charge-diagonal part of the **joint** algebra. Acting as the identity on a factor that
carries nontrivial charge structure would leave charge-off-diagonal record elements fixed, so the map
would fail to be the joint superselection projection.

```text
=> under this branch, E_joint = E_ch (x) id_R_inf (x) id_B is not merely UNFORCED: it is FALSE,
   because id_R_inf cannot be the charge-diagonal projection of a factor carrying charge.
```

**That is strictly stronger than the stop I recorded at 928**, where I found the form unforced. It is
stated **conditionally on a branch this report does not adjudicate**, and it is conditional on the
charge acting nontrivially on the record factor — which is what "substantive" would have to mean.

### 6.3 What both branches share

Under either branch the superselection field remains unfilled by derivation, and under both the
question of *where charge lives* is the operative one. **The branches differ in how much they cost:**
notational leaves a field unforced; substantive falsifies the natural form outright and forces a
different construction.

---

## 7. FREEDOMS-CONSUMED (law 2a)

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN ; JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  both declaration sites at their own bytes; both provenance chains; the register's
  Q-661 name census as an adjacent keep-separate ruling; member 5's unsealed custody
  status; member 8's typing, carried as fenced.

CONDITIONED-ON:
  section 6.2 on "substantive" meaning that charge acts nontrivially on the record factor,
  which is the only reading under which the branch has content; the consequence is stated
  as a conditional and its branch is not adopted.

SUBSTITUTED:
  NOTHING. Neither labelling was preferred, adopted, or ranked. No identification was
  constructed, no seniority asserted beyond displayed facts, no branch chosen, and the
  superselection field was not filled. My own 928 finding was re-established at bytes
  rather than cited, and the one inference of mine the record bars was reported as barred.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 8. Flattening check, verb audit, byte audit

**FLATTENING CHECK — S01–S37 walked, clean.** Two labellings of one carrier name were not merged. A
date and a seal-kind asymmetry were not converted into seniority. Evenness was not identified with
charge-neutrality — §4.5 separates fermion grading from charge. A third lineage's identical names
were not identified with `R_c`; the register's own census forbids it and §4.3 says so. Absence of a
stated difference was not identified with sameness. A conditional consequence was not identified with
a resolution: §6.2 states a branch's cost without adopting the branch. My 928 was not cited as
evidence for itself.

**BUILDER-B INDEPENDENCE:** both sites re-opened at bytes; every claim traces to a pinned sealed
object. Member 3 is mine and carries no weight.

**SELF VERB AUDIT.** "Pinned" is used for facts recomputed here. "Undecidable-of-record" is used once,
with both halves of the test displayed. "Fenced" is used for member 8 and attributed to the register's
census, not to my judgement. "Would" carries every consequence in §6, and neither branch is adopted.
No preference, resolution, or recommendation appears. `VERB_AUDIT_SELF = CLEAN`.

**BYTE-POSITION SELF-AUDIT.** §1's heading begins at byte **499**, its closure fence opens at byte
**548**, and the exact member list ends at byte **1761**. A scan of bytes `[0,548)` over `no `,
`not `, `none`, `never`, `absent`, `missing`, `without`, `lack`, `fail`, `gap`, ` open`,
`unresolved`, `underived`, `false`, `cannot`, `zero`, `stop`, `block`, `wrong`, `refus`, `unswept`,
`unsealed` returns **zero hits**.

---

## 9. Final lines

```text
CLOSURE = declared-first (sec-1 heading 499, closure fence 548, members end 1761; pre-closure scan over 22 forms = 0 hits)
SITES = pinned at bytes, both. SITE A: parent-spec lineage, member 4 [4220,4680) sha e9f1e91a…, dated 2026-07-24, verified against ITS OWN SIDECAR — R_c = span{|r_c>,|p_c>,|e_c>}, in a context stating "Record factors are even and distinguishable". SITE B: composition-derivation lineage, member 5 dated 2026-07-23 under its own heading "Labeled record factors" — R_c = span_C{|r_c>,|p_Q,c>,|e_Q,c>} — with the custody fact that MEMBER 5 HAS NO SIDECAR OF ANY SPELLING and is reachable as a sealed object only through member 6's verbatim quotation at [1643,2103) sha fb0ccce2…, whose own seal verifies.
SENIORITY = NONE-OF-RECORD. Neither lineage cites the other (both citation greps return 0), so no supersession, restatement, or correction chain exists between them. The two asymmetries are displayed as FACTS and offered as no ranking: the dates are one day apart (B 07-23, A 07-24), and the seal kinds differ (A directly sealed; B's primary unsealed, sealed only as a quotation). The register adjudicates nothing — see below.
SUBSTANCE = UNDECIDABLE-OF-RECORD. NOT provably NOTATIONAL: no sealed identification of |p_c> with |p_Q,c> exists anywhere in the corpus, and I declined to let "notational" win by default, since absence of a stated difference is not evidence of sameness. NOT provably SUBSTANTIVE: within the R_c lineage no sealed sentence states what Q ranges over — MEMBER 5 USES THE SUBSCRIPT ONCE AND DEFINES IT NOWHERE. One agreement recorded with its limit: both lineages call the record factors even / trivially fermion-graded, but EVENNESS IS ABOUT FERMION GRADING, NOT CHARGE, and decides nothing here. And in lineage A charge enters elsewhere entirely — through "the spectral projector of the disclosed source charge", with c_c extended by the identity on other record factors.
RELATING_ACT = ABSENT, with the law-9b authority table: register `charge subscript` 0 hits and `p_Q`/`e_Q` 1 hit each, BOTH MY OWN 928 ENTRY, so no settled record predates it; no citation either way; member 5 defines Q nowhere. THE ONE ADJACENT RULING CUTS AGAINST AN INFERENCE OF MINE AND I REPORT IT THAT WAY: a third sealed lineage (member 8, own seal verified) types r, p_Q, e_Q as a THREE-CHANNEL rooted star r--M-->p_M, r--Q-->p_Q, r--G-->p_G with e_Q a UNITARY rather than a basis vector — suggestive for substance — but the register's Q-661 name census rules "R_c a three-dimensional Hilbert span, NOT A CELL OF ANY COMPLEX" and warns that "merging any two on the word cell would be the name-match trap the charter warned about". Member 8's carrier IS a complex's chain group, so importing its channel reading into R_c is exactly the fenced trap. I found the lineage and record that the record bars its use.
CONSEQUENCES = mapped both ways, neither branch adopted. IF NOTATIONAL: my 928 freedom 2 dissolves and freedom 1 SURVIVES — E_ch is declared on the source sector only, so E_joint = E_ch (x) id (x) id still asserts trivial action off the source factor, a claim the tensor presentation does not supply; the superselection field STILL STOPS, on one freedom instead of two. IF SUBSTANTIVE, answering the relay's sharp question: the record factor would carry nontrivial charge structure, and a joint superselection conditional expectation must fix exactly the charge-diagonal part of the JOINT algebra; acting as the identity on a factor carrying charge would leave charge-off-diagonal record elements fixed, so E_joint = E_ch (x) id (x) id would be NOT MERELY UNFORCED BUT FALSE. That is strictly stronger than the stop I recorded at 928, and it is stated as a conditional on a branch this report does not adjudicate.
BLIND = HELD. The subscript was treated strictly as a LABEL question — where charge is written — with no rank read, no ratio computed or stated, and no fiber compared.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
