# STAGE8 — THE THREE ABSENCES AT THE MAGNITUDE SITE — O41SR — V001 (2026-08-16)

Lane: BUILD (read-and-classify at bytes). Commission: map what stands between the record's
point-cutting requires and the magnitude. This artifact **describes gaps and never fills
them**: it authors no object and no clause, adopts nothing, advocates nothing, re-reads no
catalogued negative, and does not cross — or attempt to cross — the distance it maps.

**FENCES, live throughout:** `alpha_computed = false` · `proof_authorized = false` ·
`kappa_record_computed = false`. No numeric value of any coupling, scale, root, eigenvalue,
norm, or constant is computed anywhere below. No magnitude is attempted, reached, or
evaluated. Every number appearing is a *quoted* dimension, count, hit-count, or the corpus's
own displayed symbol.

**STATUS:** CLAIMED / PROPOSED-NOT-ADOPTED. Nothing here is record.

---

## §0 — THE HEADLINE, RECONCILED AGAINST THE SWEEP BLOCK (§10)

```text
THE THREE ABSENCES, LOCATED AT BYTES AND GRADED

  A-1  A CLAUSE NOT DISPLAYED  — "no clause linking any response magnitude to any
       phase datum"
       Q1 = NEGATIVE-RESULT, scoped to the displayed equation set of the sealed
            ground (a finite sealed text, enumerated); ABSENCE at any wider
            quantifier.
       Q2 = NOT-HELD-NOT-EXCLUDED.

  A-2  AN OBJECT NOT HELD  — "both sealed candidates unavailable"
       Q1 = ABSENCE, unambiguously.
       Q2 = NOT-HELD-NOT-EXCLUDED. No impossibility anywhere.

  A-3  A CARRIER SHORT OF A THRESHOLD  — "fibers of dimension 1 ... below the
       forcing threshold"
       Q1 = ABSENCE as to the VALUE (a higher fiber dimension is not held and is
            nowhere shown unavailable); the negative result travelling with it is
            NARROWER THAN ITS HEADLINE by the corpus's own sealed audit.
       Q2 = NOT-HELD-NOT-EXCLUDED, with the impossibility's quantifier reported
            in §4.

THE SHARPEST RESULT — Q3 PROVENANCE OF THE FIBER DIMENSION:  **ENTERED.**
  The dimension descends from an imported carrier premise whose uniqueness the
  corpus itself flags NOT DERIVED: `unique_primitive_carrier_derived = false`,
  `pure_C2_comparison_representation_imported = true`. No DOR_* ruling touches it
  (0 hits across 14 sealed rulings). **This is a choice, not a wall.**

THE SHARPEST FINDING IN Q4 — ONE OFF-GROUND CARRIER holds a phase datum and a
  magnitude-valued factor together in a single displayed expression. Reported as
  ADJACENCY, with the ground-scoping that keeps it from being a contradiction.
  NO LINK IS CONSTRUCTED HERE.
```

---

## §1 — SEALS

`shasum -a 256 -c` run from each artifact's own directory.

**WORKSPACE ROOT — 19 / 20 consumed sources sidecar-verified OK.**

| # | Consumed source | Seal |
|---|---|---|
| 1 | `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md` **[AJ]** | OK |
| 2 | `STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md` **[AJ-A]** | OK |
| 3 | `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md` **[JCT]** | OK |
| 4 | `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md` **[JCT-A]** | OK |
| 5 | `STAGE8_REQUIRE_TYPOLOGY_O39SR_V001.md` **[TYP]** | OK |
| 6 | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` **[RL]** (= digest `5e49d209`, confirmed by hash) | OK |
| 7 | `STAGE8_CROSS_BLOCK_READ_V001.md` **[XBR]** | OK |
| 8 | `STAGE8_5D_REREAD_T13SR_V001.md` **[5d]** | OK |
| 9 | `STAGE8_THRESHOLD_FORM_O19SR_AUDIT_V001.md` **[THF-A]** | OK |
| 10 | `BARE_SURFACE_I2_DETERMINATION_V001.md` **[I2]** | OK |
| 11 | `BARE_SURFACE_I2_DETERMINATION_AUDIT_V001.md` **[I2-A]** | OK |
| 12 | `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` **[LPRB]** | OK |
| 13 | `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` **[BRG]** | **NO SIDECAR** — see note |
| 14 | `STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` **[EIN]** | OK |
| 15 | `STAGE8_DELTAPHI_PI_PROTECTION_DETERMINATION_V001.md` **[DPHI]** | OK |
| 16 | `STAGE8_GLUING_CANDIDATE_O23SR_V001.md` **[GLUE]** | OK |
| 17 | `STAGE8_COUNTERFACTUAL_PROFILE_O35SR_V001.md` **[CF]** | OK |
| 18 | `STAGE8_S13_THRESHOLD_SHAPE_FABLE_V001.md` **[S13]** | OK |
| 19 | `STAGE8_COMPLETION_AND_FORCING_O37SR_V001.md` **[CAF]** | OK |
| 20 | `STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md` **[T2]** (consumed at §6.1) | OK |

**SUPERVISION (DOR_* only, as expressly permitted) — 14 / 14 OK.** DOR_016, DOR_017,
DOR_018, DOR_019, DOR_020_A1, DOR_020_A2, DOR_020_A3, DOR_020_A4, DOR_020_A5, DOR_020_A6,
DOR_020_A7, DOR_020_A8, DOR_020_A9, DOR_020_CONTINUUM. No other file in that directory was
opened, listed-and-opened, or read.

**NOTE ON [BRG] (the one unsealed source), declared not buried.** It carries no `.sha256` and
no `.seal.sha256` in either corpus root. Substituted corroboration, run: the two corpus roots'
copies are **byte-identical** at `fc3e44f0ce78955c3c3ecbce57a901ca5f2770728b051052cc3ea638bcf3acdf`.
Additionally, the one flag pair consumed from it (`unique_primitive_carrier_derived = false`;
`pure_C2_comparison_representation_imported = true`) is **independently carried, verbatim, inside
sealed [EIN]** — so the Q3 provenance grade does not rest on the unsealed file. Grade of any
[BRG]-only content: SECOND-HAND-AT-CROSS-ROOT-IDENTITY, used nowhere load-bearing.

**TOTAL SEAL TALLY: 33 / 34 sidecar-verified OK; 1 unsealed and corroborated as above.**

**REACH LIMIT, declared.** `SURFACE_DEFINITION_OF_RECORD_V00{1,2,3}.md` sits in the barred
supervision directory and was **NOT opened**. Every §I / §II clause quoted below is quoted
*as the workspace artifacts carry it*, each with its carrier named. Where the surface's own
bytes would decide something the carriers do not, this artifact says so.

---

## §2 — Q1: THE THREE ABSENCES ESTABLISHED

### 2.0 The corpus's own ABSENCE / NEGATIVE-RESULT vocabulary

The distinction the commission asks for is one the corpus itself draws, in its own words. The
sharpest instance, quoted whole from [XBR] §5's upgrade map:

> "U-F1. A derived no-go sealing a BAR on the both-blocks O11/O12/O-D1
>        composition. HOLDER: a lane derivation (none exists; 92dba8d3 records
>        absence-of-construction, not prohibition)."

And again in [XBR]'s own flag block: `"no sealed composition bar (absence of construction, not
prohibition)"`.

**ABSENCE-side vocabulary observed:** "absence of construction", "unavailable", "unbuilt",
"NOT SUPPLIED", "SPEC-GAPPED", "NOT-FORCED", "vacancy", "NOT-HELD", "UNAUTHORIZED until",
"none exists".
**NEGATIVE-RESULT-side vocabulary observed:** "prohibition", "no-go", "killed by proof",
"refutation", "DEAD", "UNSATISFIABLE", "provably do not carry", "¬∃J", "empty of junctions".

The vocabularies are kept apart deliberately. `NOT-FORCED` is never written as `REFUTED`
anywhere in the six carriers checked.

---

### 2.1 ABSENCE A-1 — THE CLAUSE NOT DISPLAYED

**Located at:** [AJ] §0 verdict (:29–35) and [AJ] §6.2 "Ground fact" (:237–243). Cited
onward at [AJ] §8.1 (:515–520), [JCT] J-6 (:221), [TYP] §4.3 (:1291–1293).

**Quoted whole, with the surrounding clause that fixes its scope — [AJ] §0, the verdict
sentence's Class II arm:**

> "and (II) routes adjoining non-displayed structure (superposition, inner product, measure,
> norm/metric, spectral calculus), killed by the two-model conservativity theorem — the
> ground's displayed equation set contains no clause linking any response magnitude to any
> phase datum (CAS M8), so two models of every displayed clause exist that differ on the
> junction (CAS M1–M9), hence no candidate profile is forced and every Class II construction
> necessarily carries an AUTHORED ingredient, which voids it as *derived*"

**And the load-bearing statement of the same fact at its own site — [AJ] §6.2, whole:**

> "**Ground fact (CAS M8, M9).** The ground's full displayed equation set — the three lock
> equations, the character map, the transition/cocycle law, the conjugate-pair closure, the
> orbit action — contains **no clause in which a response magnitude and a phase-sector
> variable co-occur**. This is the audit-corrected I-2 Chain A inventory (cross-sector items
> exactly {L1 character map, transition law, L2 indexing declaration}; the first two
> phase-to-phase with integer slots; L2 asserting a dependence EXISTS while displaying no
> form), re-verified here by variable-set inspection."

**WHAT IS ABSENT: a CLAUSE.** Not an object, not a proof, not a value, not a certification.
Specifically: a displayed equation of the sealed ground in which a response-magnitude symbol
and a phase-sector symbol both appear.

**GRADE: NEGATIVE-RESULT — scoped to the displayed equation set; ABSENCE at any wider
quantifier.**

**The deciding words, quoted.** What makes it a negative result rather than a mere non-holding
is the enumeration warrant, quoted whole from [AJ] §3:

> "the ground is a finite sealed text, so its displayed objects and operations are enumerable
> by inspection"

— restated at [AJ] §8.2 as `**(Finite-text warrant)** [DERIVED, same warrant as the audited I-2
pair]`. Over a finite enumerated set, "contains no clause" is *shown*, not merely *not held*.
It is called a "**Ground fact**", it carries CAS check tags (M8, M9), and the sealed audit
[AJ-A] completed the one half that had rested on prose:

> "**C-3 (CAS M8 coverage).** M8's mechanical content is `lock_vars ∩ phase_sector_syms = ∅` —
> the lock half of the co-occurrence claim; the phase-law half (character map, transition/
> cocycle law, conjugate closure contain no response symbol) rested on prose inspection.
> Completed mechanically here (X5): TRUE. Effect: wording-level; the conservativity fact holds
> at bytes on both halves."

**The wider quantifier, where it reverts to ABSENCE — carried because it cuts against the
negative-result reading.** [AJ] §8.2 marks the arm extending non-forcing beyond the *displayed*
set as CONDITIONAL, and says so in the same breath:

> "Theorem II's conservativity core does NOT depend on C12 — it rests only on the displayed
> equation set — so even under a weakened §II.6, what would remain is: no junction is FORCED by
> the displayed ground; only the impossibility-of-supply-from-withheld-content would reopen."

So: **shown not to exist among the displayed clauses; merely not held anywhere else.**

---

### 2.2 ABSENCE A-2 — THE OBJECT NOT HELD

**Located at:** [RL] §5.2 (:393–402) and [RL] §5.3 / flag block (:406–416, :497–510). [RL] is
the artifact the corpus cites as digest `5e49d209`; the identification was confirmed here by
hashing every workspace `.md` and matching the first eight hex digits — exactly one match.

**Quoted whole, its own section heading carried because it fixes the scope — [RL] §5.2:**

> "### 5.2 What forcing would require, and where it is sealed shut today [PROVABLE]
>
> Forcing `|n|` through this object requires an EQUATION between the n^2-scaled channel and an
> n-blind comparand — a second determination that does not scale as n^2 (the
> quadratic-against-linear crossing named in T1 §6.1). The derived form exhibits the n^2 side
> exactly; the record's two candidate comparands are both unavailable of record: the energy
> form `c_L <.,.>_(2,ell)` is gated by the undecided materiality fork with both settlement
> paths blocked on SPEC GAPS (T2 §3.3–3.4), and the conversion of `kappa_record` to a physical
> action coefficient is sealed UNAUTHORIZED until `Z_Q` is constructed from complete `Q_spec`
> (B9). So the forcing, if it exists, lives in an unbuilt external crossing, not in the
> cross-sector term."

**And the flag-block form the downstream artifacts actually quote — [RL] `WINDING_FORCED`,
final clause, whole:**

> "Forcing |n| requires equating the n^2 channel to an n-blind external comparand, and both
> sealed candidates are unavailable: the energy-form comparand is gated by the undecided
> materiality fork with both settlement paths SPEC-GAPPED, and the kappa_record-to-physical
> conversion is sealed UNAUTHORIZED until Z_Q from complete Q_spec."

**WHAT IS ABSENT: an OBJECT** — an external n-blind comparand — of which the corpus names two
candidates and holds neither. Corroborated at the docket level in [JCT] A-3:
`"this docket supplies NO external n-blind comparand" (5d §5.1)`.

**GRADE: ABSENCE. Unambiguous.**

**The deciding words, quoted.** Every operative phrase is absence-vocabulary and not one is
prohibition-vocabulary:

- `"both unavailable of record"` — un-held, not excluded;
- `"blocked on SPEC GAPS"` — a gap is a missing specification;
- `"UNAUTHORIZED **until** `Z_Q` is constructed from complete `Q_spec`"` — a bar with a stated
  lifting condition is by construction not a proof of non-existence;
- `"the forcing, **if it exists**, lives in an **unbuilt** external crossing"` — the source's
  own conditional; a negative result would not be written with an "if it exists";
- the whole site is headed `WINDING_FORCED = NOT-FORCED`, never `REFUTED`.

**Carried because it cuts against nothing but must be displayed:** [RL] §5.3 does supply a
*local* negative result at the same site —

> "a FORCED verdict here would have required exhibiting an n-blind channel INSIDE R_record,L,
> and D2 proves there is none on the ratified content."

That is a proof of absence **inside `R_record,L` on the ratified content** — the interior. It
is not a proof about the exterior comparand, which is exactly what A-2 is about. Quantifier
reported in §3.2.

---

### 2.3 ABSENCE A-3 — THE CARRIER SHORT OF A THRESHOLD

**Located at:** [AJ] §6.3 kill 2 (:282–285). Its ONLY origin in the corpus: the phrase "fibers
of dimension 1" occurs in 5 workspace files and 0 cleanroom files, and 4 of the 5 are citations
of [AJ] (§10 sweep block). Cited onward at [JCT] J-6 (:221) with the corpus's own ellipsis, at
[JCT-A] :85–87 (splice verified faithful), at [TYP] §4.3, and flagged as a missed threshold by
[THF-A] :416–420.

**Quoted whole, with its numbered position inside the three-way kill that fixes its scope —
[AJ] §6.3, item 2:**

> "2. **Dimension, on the ground's own words.** §I.4: the record's bundle is a **fixed line
>    bundle** — fibers of dimension 1. Frame-function forcing fails in dimension ≤ 2 even when
>    full inner-product structure is granted; on dim-1 fibers there is no nontrivial frame
>    consistency to force anything. The ground's own carrier is below the forcing threshold."

**The clause's own governing header, quoted so the scope is not lost:**

> "### 6.3 The strongest objection, hunted and killed: forcing-by-consistency (Gleason-style)
>
> Objection: "consistency conditions on the withheld orthogonality/endpoint-transfer structure
> could FORCE a unique profile, the way frame-function consistency forces the quadratic profile
> on a Hilbert space — so the profile freedom of §6.2 might be an artifact of ignoring joint
> constraints." Killed three independent ways:"

**WHAT IS ABSENT: a VALUE** — a fiber dimension above the cited threshold. The carrier holds
dimension 1; what it does not hold is a higher one. Not an object, not a clause, not a proof,
not a certification.

**GRADE: ABSENCE as to the value — and the negative result travelling with it is NARROWER THAN
ITS HEADLINE.**

**The deciding words for ABSENCE, quoted from the sealed carrier that holds them ([EIN] §1.1,
whole sentence including its citation):**

> "One **primitive projective record degree** per admissible causal record cell: *"Its physical
> state is a ray in the primitive complex two-dimensional carrier"*, normalized lift
> `z_i^dagger z_i = 1` (LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md `:17-24`) — carrier uniqueness
> NOT derived (`unique_primitive_carrier_derived = false`;
> `pure_C2_comparison_representation_imported = true`)."

`carrier uniqueness NOT derived` decides it: nothing in the corpus shows a different fiber
dimension unavailable. The value is **not held**, not **shown not to exist**.

**The deciding words for the narrowing, quoted whole from the sealed adversarial audit
[AJ-A] §C-2:**

> "**C-2 (§6.3 kill 2, dimension argument mis-scoped).** "On the ground's own dim-1 line
> bundle" applies §I.4's fiber dimension to the frame-function substrate. A Gleason-style
> forcing would run on the span of superposed record data — a space the AUTHORED Hilbert
> structure would set, not the fiber — so kill 2 reaches only the minimal fiber-local
> substrate. Kills 1 (the substrate is itself adjoined — decisive and fully general) and 3
> (the displayed joint constraints do not separate the models — CAS-verified) carry the
> objection's defeat alone. Effect: "killed three independent ways" over-counts by scope; the
> objection stays dead; verdict untouched."

The headline sentence reads as a general statement about "the ground's own carrier". The
corpus's own sealed audit rules that it reaches **only the minimal fiber-local substrate**.
That is a real quantifier, and it is narrower than the headline.

**Not AMBIGUOUS-AT-BYTES.** Build and audit disagree, but the corpus decides: [AJ-A] is the
commissioned adversarial audit of [AJ], its verdict is `CONFIRMED-WITH-CORRECTIONS`, and C-2 is
one of the three named corrections. The narrowed reading is the corpus's settled one.

**Also displayed, because it bears on A-3 and is adverse to reading kill 2 as load-bearing:**
[AJ-A] Choice Ledger row 6 —

> "| 6 | Treating Gleason facts as generic mathematics for adjudicating §6.3 |
> IMMATERIAL(proven) | used only to SCOPE the build's kill 2 (C-2); the objection's defeat
> rests on kills 1 and 3, both ground-native |"

The threshold sentence is therefore not load-bearing for the vacancy. It is load-bearing for
this commission only because [JCT], [TYP], and [THF-A] all carry it forward as one of the three
separators — which is precisely the thing being audited here.

---

## §3 — Q2: FILLABILITY

For each absence: does the corpus anywhere show the absent thing **impossible**?

### 3.1 A-1 (the clause) — **NOT-HELD-NOT-EXCLUDED**

**No impossibility of the clause is shown anywhere.** What is shown is non-*forcing* on this
ground, which is a different object. The corpus states the supply route in the open:

> "### 8.3 What would change the verdict (the supply route, sharpened)
>
> The closure PROVES what the I-2 pair named: the junction cannot be derived on this ground —
> supply requires **a change of ground**, i.e., genuinely new booked surface structure.
> Sharpened by Theorem II: the new structure must do more than provide an exit operation
> (interference, inner product, measure) — it must provide the **forcing equation**: a booked
> clause in which a response magnitude and phase-sector data co-occur with enough rigidity to
> select the profile."

A clause whose supply route is named cannot be a clause shown impossible.

**THE IMPOSSIBILITY PRESENT AT THIS SITE, WITH ITS QUANTIFIER REPORTED EXPLICITLY.**

Impossibility: **Theorem II (two-model conservativity)** — [AJ] §6.2:

> "**Theorem II.** For every Class II candidate — any adjoined exit operation, any profile —
> the ground does not force it: there exist two models of every displayed ground clause that
> agree on all ground content and differ on the junction. Hence no Class II candidate satisfies
> J-der."

> **QUANTIFIER, as the corpus states it ([AJ] §8.1):** "Let G be the sealed ground (§2.2) and
> let J range over functionals satisfying the junction specification J-mag ∧ J-cross ∧ J-β ∧
> J-der (§2.3). Then **¬∃J**"
>
> — i.e. quantified over **candidate junctions on one named ground G**, where G is exhaustively
> "1. Surface of record §I–§II + displayed locks... 2. The I-2 determination... 3. Nothing
> else." It is *not* quantified over clauses, over grounds, or over the future.

**THE SENTENCE AFTER THE IMPOSSIBILITY — checked, as the commission requires, and it names what
works.** [AJ] §8.3's closing sentence, immediately following the supply-route statement, quoted
whole:

> "Whether such structure may legitimately EMERGE downstream rather than sit at the origin is
> outside this artifact's quantifier and is not foreclosed by it (the un-hold names this as a
> live honest reading; this run neither uses nor evaluates it)."

The same disclosure is carried in [AJ]'s own flag block: `"downstream emergence neither used nor
foreclosed"`.

**A SECOND, NARROWER IMPOSSIBILITY AT THE SAME SITE — reported with its quantifier and its
conditional mark.** [AJ] §6.4:

> "No disclosure of withheld content can contain the missing forcing equation: a
> magnitude-valued internal→external junction among the thirteen would contradict "whatever
> converts internal to external is NOT a magnitude on record"; refinement stages preserve unit
> modulus (Theorem I); identities and bounds are slot-free forms. The lack is structural, not
> informational"

> **QUANTIFIER:** the four named content-withheld items only ("the refinement law's exact form;
> balanced-geodesic identities; onset bound's form; the thirteen interface junction
> identities"), and **CONDITIONAL** — [AJ] §8.4 marks it `K10 ... CONDITIONAL (C12 — inherited
> Chain E typing, §6.4)`, and §8.2 names the condition: "If a future booked result weakened that
> wording, those components lose categorical scope and this closure must be re-derived."

### 3.2 A-2 (the object) — **NOT-HELD-NOT-EXCLUDED**

**No impossibility of the comparand is shown anywhere.** The corpus's own words rule it out
being one: `"the forcing, if it exists, lives in an unbuilt external crossing"`; `"UNAUTHORIZED
until"`; `"blocked on SPEC GAPS"`. Both blockers carry lifting conditions (§6).

**THE ONE IMPOSSIBILITY AT THIS SITE, WITH ITS QUANTIFIER REPORTED EXPLICITLY.**

Impossibility: [RL] §5.3 — `"D2 proves there is none on the ratified content."`

> **QUANTIFIER: an n-blind channel *INSIDE* `R_record,L`, on the *ratified* content.** The
> quoted sentence names both bounds itself: "a FORCED verdict here would have required
> exhibiting an n-blind channel **INSIDE R_record,L**, and D2 proves there is none **on the
> ratified content**."

**THE SENTENCE AFTER IT — checked. The source's own next move names the exterior as the place
that is open:** [RL] §5.2's closing sentence (which sits immediately before §5.3 in the file)
is `"So the forcing, if it exists, lives in an unbuilt external crossing, not in the
cross-sector term."` The impossibility is *interior*; the absent object is *exterior*. The
source says so in its own voice.

**Corroborating non-exclusion at the docket level** — [XBR] §4 V5 and [5d] :69 both carry
`WINDING_FORCED = NOT-FORCED`, never a refutation; and [XBR]'s upgrade map lists holders and
acts for every conditional, i.e. it treats these as suppliable.

### 3.3 A-3 (the value) — **NOT-HELD-NOT-EXCLUDED**

**Nothing shows a fiber dimension other than 1 impossible.** The decisive bytes are the two
status flags quoted in §2.3: `unique_primitive_carrier_derived = false` and
`pure_C2_comparison_representation_imported = true`, plus [EIN]'s prose gloss `carrier
uniqueness NOT derived`. A carrier whose uniqueness is expressly not derived cannot carry an
impossibility about its own dimension.

**THE IMPOSSIBILITY AT THIS SITE, WITH ITS QUANTIFIER REPORTED EXPLICITLY.**

Impossibility, quoted from [AJ] §6.3 kill 2:
`"Frame-function forcing fails in dimension ≤ 2 even when full inner-product structure is
granted"` and `"on dim-1 fibers there is no nontrivial frame consistency to force anything"`.

> **QUANTIFIER AS WRITTEN:** dimension ≤ 2 — and, per the concessive clause carried whole,
> *"even when full inner-product structure is granted"*, i.e. the failure is asserted at
> dimension ≤ 2 under the most generous hypothesis.
>
> **QUANTIFIER AS THE CORPUS'S OWN AUDIT NARROWS IT:** `"kill 2 reaches only the minimal
> fiber-local substrate"` — the impossibility governs the fiber, not the space the forcing
> would run on.

**THE SENTENCE AFTER THE IMPOSSIBILITY — checked, and it is the textbook instance of the
pattern the commission named. [AJ-A] C-2's very next sentence names what works:**

> "Kills 1 (the substrate is itself adjoined — decisive and fully general) and 3 (the displayed
> joint constraints do not separate the models — CAS-verified) carry the objection's defeat
> alone."

The narrow impossibility is real and the source's own next sentence names the two general
arguments that do the work instead. **This is a real divergence between a headline and its
quantifier, and the corpus caught it before this commission did** — the credit is the audit's,
not this lane's.

---

## §4 — Q3: THE FORCING THRESHOLD (the sharpest of the three)

### 4.1 What the forcing threshold IS there, stated symbolically

The definition is not given as a named constant; it is given as a **failure region in a
dimension**, in the sentence that names it. Quoted whole from [AJ] §6.3 kill 2:

> "Frame-function forcing fails in dimension ≤ 2 even when full inner-product structure is
> granted; on dim-1 fibers there is no nontrivial frame consistency to force anything. The
> ground's own carrier is below the forcing threshold."

**SYMBOLICALLY, AS DISPLAYED:**

```text
QUANTITY  :  dim  — the dimension of the carrier the frame-function forcing runs on
FAILURE   :  dim <= 2      ("Frame-function forcing fails in dimension ≤ 2")
SITE      :  dim  = 1      ("fibers of dimension 1"; "on dim-1 fibers")
VERDICT   :  "below the forcing threshold"
```

The corpus supplies no other symbolisation and no separately displayed threshold constant. The
threshold is therefore **the boundary of the region `dim ≤ 2`**, and its definition is the
quoted sentence itself.

### 4.2 What dimension would cross it

**dim = 3.** This is not an inference of this lane's; it is read off the corpus, which
adjudicated exactly this sentence as a threshold and stated the crossing in its own words.
[THF-A] §MISSED-1, quoted whole:

> "A THRESHOLD IN A DIMENSION, with a quantity (fiber dimension), a crossing (≤ 2 fails / 3 and
> above forces), and the record's own site displayed BELOW it. AND IT IS A **FORCING**
> THRESHOLD — it governs whether content is FORCED, i.e. it sits directly on the allow/require
> axis this commission was sent to survey."

`3 and above forces` is the corpus's own statement of the crossing dimension. The gap between
the site and the crossing is **two integer steps**, and the corpus displays no fractional or
intermediate carrier.

### 4.3 PROVENANCE OF THE FIBER DIMENSION OF 1 — **ENTERED**

This is the commission's most valuable question, so the whole chain is displayed, step by step,
each step at its own byte-grade.

**STEP 1 — What [AJ] actually cites.** [AJ] attributes it to `§I.4`. But `§I.4`'s own words, as
the workspace carries them, do **not** contain the phrase "fibers of dimension 1". [S13] §1
quotes §I.4 whole (with its own citation of the consolidated register line):

> "\"Connections on a fixed line bundle form an affine space; bundle geometry does not choose
> one. No fiber metric; a radius is a metric datum, not a surface datum\" (I.4, Register :224 as
> consolidated)"

So `"fibers of dimension 1"` is **[AJ]'s own unpacking of the word "line"** in the ground's
phrase "fixed line bundle". [JCT-A] verified the splice as faithful — `MATCH (build splices
with ellipsis, faithful)` — and this lane concurs that the unpacking is sound as an unpacking.
But an unpacking is not a derivation, and it inherits whatever grade the underlying phrase has.

**STEP 2 — Where "fixed line bundle" comes from, and how it is derived.** [LPRB] displays a
derivation, and it is a real one. Its premise, quoted whole:

> "The new principle places one primitive record degree on every admissible causal record cell.
> Its physical state is a ray in the primitive complex two-dimensional carrier."

Its step, quoted whole:

> "Two lifts represent the same local ray exactly when, on an overlap,
>
> ```
> z_j = g_ij z_i,
> g_ij = exp(i theta_ij) in U(1).
> ```
>
> On triple overlaps, equality of the represented ray requires
>
> ```
> g_ij g_jk g_ki = 1.
> ```
>
> The local lifts and transition functions therefore define a complex line bundle, equivalently
> a principal `U(1)` comparison bundle."

Its own status flag: `local_projective_record_bundle_derived = true`. [GLUE] §6.2 independently
re-reads the same bytes and reports the same: `the source itself says they "define a complex
line bundle, equivalently a principal U(1) comparison bundle" (bytes :38-41), and a bundle is a
glued object.`

**So the step ray-in-C² + U(1) overlap redundancy ⇒ line bundle IS displayed and IS derived.
The question is the grade of what it is derived FROM.**

**STEP 3 — The grade of the premise.** [LPRB]'s own Authority block, quoted whole:

> "This derivation uses:
>
> ```
> the sealed Fundamental Boundary Record Action Principle v001;
> the current primitive projective record carrier;
> the current ordered-endpoint projective stabilizer.
> ```
>
> It is therefore a Level-1 result under the newly adopted microscopic principle. It is not
> retroactively attributed to the five pre-alpha sources."

And the flags that settle it, carried verbatim inside sealed [EIN] (§2.3 above) and originating
at [BRG]'s Status block:

```text
unique_primitive_carrier_derived            = false
pure_C2_comparison_representation_imported  = true
bloch_ball_derived                          = false
M2C_algebra_derived                         = false
```

**STEP 4 — The ADOPTED test, run.** The commission directs that ADOPTED be checked against the
`DOR_*` rulings. **RUN, all 14 sealed rulings, per-pattern:**

```text
DOR_* sweep (14 files, all seals OK)      hits
  "line bundle"                              0
  "fiber dimension"                          0
  "dimension 1"                              0
  "forcing threshold"                        0
  "ray"                                      0
  "C^2"                                      0
  "two-dimensional"                          0
  "rank"                                     8  -> ALL are cycle-rank / rank-two MEMBER
                                                  classes (graph and member objects), NOT
                                                  bundle-fiber rank. Inspected, not counted.
```

**No authored ruling of record touches the fiber dimension. ADOPTED is excluded at bytes.**

**STEP 5 — THE GRADE.**

```text
PROVENANCE OF THE FIBER DIMENSION OF 1  =  **ENTERED**

  NOT DERIVED  — the bundle-rank step is derived, but from a carrier premise the corpus
                 itself flags as not derived and as imported:
                 "carrier uniqueness NOT derived (`unique_primitive_carrier_derived =
                 false`; `pure_C2_comparison_representation_imported = true`)".
  NOT ADOPTED  — 0 hits across all 14 sealed DOR_* rulings (sweep above).
  ENTERED      — carried in as a premise of "the newly adopted microscopic principle":
                 "Its physical state is a ray in the primitive complex two-dimensional
                 carrier."

  ONE STEP OF DISPLAYED DERIVATION SITS ABOVE THE ENTRY, and is disclosed as such:
  ray + U(1) redundancy ⇒ line bundle is derived and flagged
  `local_projective_record_bundle_derived = true`. The ENTRY is the ray-in-C² carrier
  underneath it, not the bundle rank on top of it.
```

**IN THE COMMISSION'S OWN TERMS: this is a choice, not a wall.**

**Adverse span, carried whole because it cuts against the ENTERED grade.** [LPRB]'s status
block does mark the bundle result derived, and [AJ] does cite §I.4 as "the ground's own words" —
so at [AJ]'s own ground-scope the dimension arrives as *given ground*, which is exactly what
ENTERED means, but a reader restricted to [AJ] alone would reasonably call it DERIVED-ON-GROUND.
The reach that changes the grade is one file deeper, at [LPRB]/[BRG]/[EIN]. This lane's grade is
stated at the deeper reach and the shallower reading is displayed here rather than suppressed.

**Reach limit restated:** `SURFACE_DEFINITION_OF_RECORD` was not opened (barred directory). If
its §I.4 bytes contain a derivation of the line-bundle rank that no workspace carrier quotes,
this grade would need re-running. On the carriers available: **ENTERED**.

### 4.4 What bears on whether the fiber dimension could be other than 1

**THREE THINGS DO. All three are byte-facts of the corpus, reported and not constructed.**

**(i) A STATEMENT — the carrier's uniqueness is expressly not derived.** Quoted at §2.3:
`carrier uniqueness NOT derived (`unique_primitive_carrier_derived = false`;
`pure_C2_comparison_representation_imported = true`)`. The corpus states in its own voice that
nothing forces this carrier rather than another. Alongside it, `bloch_ball_derived = false` and
`M2C_algebra_derived = false` say the same of the two structures the carrier would otherwise
bring with it.

**(ii) A PARAMETER IT DEPENDS ON — two of them, both displayed.** [LPRB]'s derivation makes the
rank a joint consequence of exactly two entered choices, each quoted above:
- the **projectivisation** — "Its physical state is a **ray**"; and
- the **redundancy group** — "`g_ij = exp(i theta_ij) in U(1)`", scalar-valued.
The corpus displays no derivation of either. It also displays, on the very same object, an
**ambient carrier of complex dimension two** — `z_i : U_i -> C^2` — so a second dimension is
present at the site in the corpus's own display, sitting one construction away from the fiber.
**No claim is made here about what that ambient dimension would do to the threshold.** Reporting
that two dimensions are displayed at one site is not a construction; relating either of them to
`dim ≤ 2` would be, and is not done.

**(iii) A RULING ON WHICH DIMENSION IS EVEN THE RIGHT ONE — [AJ-A] C-2.** The audit's finding is
not merely that kill 2 is narrow; it is that the fiber's dimension **is not the dimension the
threshold governs**:

> "A Gleason-style forcing would run on the span of superposed record data — a space the
> AUTHORED Hilbert structure would set, not the fiber"

So the corpus holds that the quantity to compare against `dim ≤ 2` would be set by an authored
object, and the fiber's own dimension is the wrong input. That is a stronger statement than "the
fiber could be other than 1"; it is "the fiber's dimension is not the one at issue".

**NOTHING FOUND ON THE OTHER SIDE.** No counterfactual, no statement, and no parameter anywhere
in either corpus root asserts the fiber dimension is forced, unique, or underived-but-necessary.
Sweeps in §10.

**INDETERMINATE-AT-BYTES:** whether the ambient `C²` and the fiber's `1` are ever *both* in play
at the Gleason site. [AJ] names only the fiber; [AJ-A] names only "the span the AUTHORED Hilbert
structure would set"; neither names `C²`. The bytes do not decide it and this lane does not.

---

## §5 — Q4: THE PHASE DATA

"No clause linking any response magnitude to any phase datum" presupposes phase data. This
section enumerates what phase-type objects the corpus holds, names each one's carrier, and then
reports **adjacency, not reach**.

### 5.1 Enumeration — phase-type objects, ON THE GROUND WHERE THE ABSENCE LIVES

The ground in question is exhaustively named by [AJ] §2.2 as the surface of record §I–§II +
displayed locks, plus the I-2 pair, plus "Nothing else". The phase-sector inventory of that
ground is quoted whole from [I2] §2.2(a):

> "**(a) The phase/holonomy sector:** connection/holonomy histories as sole index of the
> dynamics, no native metric anywhere, no logarithm taken (§I.1); continuous fibers over a
> discrete base (§I.2); holonomy characters z_j^(n) = χ_n(h_j[a_j]), unit modulus, n ∈ {+1,−1};
> U(1) transitions g_ij = exp(iθ_ij) (§I.3); affine connection space, no fiber metric, no
> distinguished connection (§I.4); the exact refinement/coarsening law of the cell (§I.5 —
> named as exact and derived; its explicit form is not displayed on the sealed ground)."

Broken out, each with **the carrier it lives on**:

| # | Phase-type object | Type | **Carrier it lives on** | Site |
|---|---|---|---|---|
| P-1 | Holonomy characters `z_j^(n) = χ_n(h_j[a_j])`, unit modulus, `n ∈ {+1,−1}` | character | the write weight slot of the record cell — the map's OUTPUT is a sector-(b) write character, its INPUT a sector-(a) holonomy | §I.3 / [I2] L1 |
| P-2 | U(1) transition functions `g_ij = exp(iθ_ij)` | transition function | **patch overlaps** `U_i ∩ U_j` of the fixed line bundle | §I.3 / [LPRB] |
| P-3 | Triple-overlap cocycle `g_ij g_jk g_ki = 1` | cocycle condition | **triple overlaps** `U_i ∩ U_j ∩ U_k` | [LPRB]; [GLUE] §6.2 |
| P-4 | Holonomies `h_j[a_j]` of connections round loops | holonomy | **the fibers over the discrete base** — [AJ] §5: "holonomy lives in the fibers; counts are base data" (§I.2 base/fiber separation) | §I.4 |
| P-5 | Relative holonomy `w = z[a]·conj(z[a′])`, transition-invariant | holonomy ratio | **one loop and one sector n** — [AJ] §6.1: "for two connections on the same loop and the same sector n" | [AJ] §6.1 |
| P-6 | Transition law `z_j = g_ij·z_i` | transition action | **write objects** — [I2-A] C-C: "a phase-sector variable acting on a write object" | [I2-A] C-C |
| P-7 | Winding integers `n`, `\|n\|` (Chern-type cocycle windings) | winding label | **closed cycles of the phase sector**; [CAF] R-1: "single-valuedness on a closed cycle (holonomy/monodromy)" | [AJ] §5; [CAF] |
| P-8 | Write weights, unit-modulus (phase) weights | phase weight | **the write structure** | §II.6 / [I2] §2.2(b) |
| P-9 | Transition phases `θ_ij`; holonomy phases | angular variable | chart/history — [I2] Chain D: "chart/history variables ... not invariants (vary over connection space and gauge)" | [I2] §3.4 |
| P-10 | Relative character phase `δ = φ₊ − φ₋` | angular variable | the two-character pair — **RECLASSIFIED** by [I2-A] C-A: "under the ground's own U(1) transition action δ → δ + 2θ, a chart/history variable" | [I2] §3.4 / [I2-A] C-A |

**Not on this list, and declared:** the toy profile argument `Δ` of [AJ] §6.2 is a TOY (see
§8). It is a phase-type symbol but not corpus phase data.

### 5.2 Enumeration — phase-type objects ELSEWHERE IN THE CORPUS

These are outside [AJ]'s ground. They are enumerated because Q4 asks what the **corpus** holds,
and each is flagged as off-ground so nothing is smuggled across the scope line.

| # | Phase-type object | Type | **Carrier it lives on** | Site |
|---|---|---|---|---|
| P-11 | `Delta Phi` — "a dimensionless accumulated action-phase difference in the primitive two-character U(1) record map" | angular / action-phase | **the primitive two-character U(1) record map**; concretely the record overlap `z_R` | [DPHI] §1 |
| P-12 | `Gamma_Q[A] = -i Log(Z_Q[A]/Z_Q[0])`, `c = 1` | phase object | **the charged amplitude `Z_Q[A]`** | [RL] B9 |
| P-13 | `U(1)_rel` — "a one-dimensional relative Lie algebra, and a character lattice `Z`" | U(1) / character lattice | **the compact relative-phase stabilizer** | [DPHI]; PRIMITIVE_RELATIVE_PHASE_STABILIZER_V002 as [DPHI] cites it |
| P-14 | Same-phase monodromy `U = u_+^† u_-` | monodromy | **the cross-branch pair `(+√2, −√2)`** in the boundary-closure operator setting | STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001 WB3 (name-level only; not consumed as a premise) |
| P-15 | `U_R(Phi) = diag(1, exp(i Phi))` acting on `\|R_0> = (\|0> + \|1>)/sqrt(2)` | U(1) values on two characters | **the two-character record map** | [DPHI] §1 |

**Explicitly NOT held, per the corpus's own denial** — carried because it bounds the
enumeration. [DPHI], whole:

> "It is not sealed as a Berry phase, spectral flow, index, or already physical electromagnetic
> holonomy."

### 5.3 ADJACENCY — every site where a phase datum and a response magnitude sit on ONE carrier

**First, what counts as a response magnitude here.** [I2] §2.2(b), quoted whole:

> "**(b) The write/response sector:** the write structure with unit-modulus (phase) weights and
> no amplitude/magnitude structure (§II.6); the opening/response kinematics — first-opening
> probabilities, orthogonality, endpoint transfer, projective lengths, T_R, H_R (§II.7); the
> write action \|ΔS_record\| (§II.9); onset content m_* and the onset bound (§II.9); the
> balanced-geodesic identities (§II.9 — named; form not displayed)."

**THE ADJACENCY LIST. Four sites. Nothing is joined; two things sit on one carrier.**

```text
ADJ-1  THE WRITE/RESPONSE SECTOR ITSELF  — the strongest on-ground adjacency.
  ON ONE CARRIER (sector (b), quoted whole above):
    · a PHASE datum   — "the write structure with unit-modulus (phase) weights" (§II.6)
    · RESPONSE MAGNITUDES — "first-opening probabilities, orthogonality, endpoint
      transfer, projective lengths" (§II.7)
  NO CLAUSE JOINS THEM: that is exactly what A-1 says, and [I2] §3.1 says it in the
  complementary direction, quoted whole:
    "No other displayed law contains variables of both sectors: each displayed lock's
     law contains no phase-sector variable (CAS B3.3b — variable-set inspection of the
     lock system), and no response quantity co-occurs with n, θ_ij, or any holonomy
     variable anywhere on the sealed pages."
  SO: the phase weights and the response magnitudes are BOOKED IN THE SAME SECTOR and
  no equation of the ground puts them in one clause. Two things on one carrier.

ADJ-2  L1, THE CHARACTER MAP  z_j^(n) = χ_n(h_j[a_j])
  ONE CLAUSE holds a sector-(a) input and a sector-(b) output. But BOTH ENDS ARE PHASE
  — [I2] L1, whole: "converts a holonomy (sector a) into a write character (a sector-b
  weight). Both ends are unit-modulus (CAS B2.5): L1 is phase-to-phase. Its only slot is
  the in-phase index n."
  ADJACENCY OF SECTORS ON ONE CLAUSE — NOT of a phase datum with a magnitude.

ADJ-3  THE TRANSITION LAW  z_j = g_ij·z_i   ([I2-A] correction C-C)
  "The displayed transition law z_j = g_ij·z_i is cross-sector on the build's own sector
   split (a phase-sector variable acting on a write object) and is omitted from the
   'ONLY items' enumeration."
  Again a phase-sector variable ON a write carrier — and again both ends unit-modulus.
  ADJACENCY OF SECTORS — NOT of a phase datum with a magnitude.

ADJ-4  L2, THE INDEXING DECLARATION — the site the corpus's own audit names.
  [I2] L2, whole: "**L2 — the indexing declaration** (§I.1): 'the record's dynamics is
   indexed by connection/holonomy histories alone.' This asserts that a cross-sector
   dependence EXISTS, with NO displayed functional form — hence no displayed slot."
  AND [AJ-A]'s correction C-1 names EXACTLY the pairing this commission asks after,
  quoted whole: "the strongest natural candidate family — the booked response magnitudes
   (first-opening probabilities, projective lengths) with L2's indexing assertion as the
   crossing — is never NAMED in the build's route tables".
  SO: a response magnitude and a phase datum are DECLARED dependent at L2, and the
  declaration displays no form. THE DEPENDENCE IS ASSERTED; THE CLAUSE IS ABSENT.
  This is the precise shape of A-1, seen from the other side.
```

### 5.4 THE OFF-GROUND ADJACENCY — one carrier holds both, in one displayed expression

**Reported as adjacency. No link is constructed. The scope line is drawn first, before the
finding, so the finding cannot be mistaken for a contradiction.**

**SCOPE LINE.** [AJ]'s M8 is quantified to *its* ground and no further — [AJ] §2.2 row 3 reads
`"3. Nothing else."`, and §8.1's `¬∃J` is "Let G be the sealed ground (§2.2)". The object below
is **not on that ground**: it belongs to `primitive_complete_boundary_transition_functional_
principle_v002` / `results/primitive_boundary_ctp_record_map_v001.json`, as [DPHI] cites them.
Nothing below contradicts A-1, and nothing below is offered as filling it.

**THE SITE.** [DPHI] §1, quoted whole:

> "For two histories separated by dimensionless accumulated action `Delta Phi`, the record
> overlap is
>
> ```
> z_R(Delta Phi)
>   = <R_0|U_R(Delta Phi)|R_0>
>   = exp(i Delta Phi/2) cos(Delta Phi/2).
> ```
>
> The same source says the first orthogonal public record occurs at
>
> ```
> Delta Phi_* = pi.
> ```"

**WHAT SITS ON ONE CARRIER.** The carrier is the record overlap `z_R` in the primitive
two-character U(1) record map. Displayed on it, in one expression, are:
- a **pure-phase factor** `exp(i Delta Phi/2)` — a phase datum; and
- a **magnitude-valued factor** `cos(Delta Phi/2)` — real-valued, not a phase, not an integer
  label;
- both **functions of the single phase datum** `Delta Phi`, which [DPHI] types as "a
  dimensionless accumulated action-phase difference in the primitive two-character U(1) record
  map".

And the response-side event is displayed on the same carrier: `"the first orthogonal public
record occurs at Delta Phi_* = pi"` — first-opening language, on a phase argument.

**THIS IS ADJACENCY, NOT REACH — and the corpus's own flags say why it is not more.** [DPHI]
carries, verbatim:

```text
closure_residual            = "C_record(K)=DeltaPhi[K;X_K]-pi"
closure_residual_derived    = false
complete_operator_constructed = false
primitive_phase_record_map_derived = true
```

and the proviso [DPHI] quotes whole: `"but only under the proviso that complete dynamics proves
the crossing and no earlier independent record channel opens"`. Also
`DeltaPhi_map_protected_against_action_form_mutations = false | TYPE-U` and
`higher_derivative_tower_moves_DeltaPhi = NO_VERDICT | blocker: DeltaPhi[K;X_K] is unbuilt`.

**NO CORPUS ARTIFACT JOINS THIS SITE TO THE GROUND WHERE A-1 LIVES.** Sweep run: the phrase
"first orthogonal public record" occurs in **exactly one file in each corpus root** — [DPHI]
itself (§10). No artifact relates `Delta Phi_* = pi` to the ground's displayed write-action lock
`\|ΔS_record\|/ℏ = π`. **This lane relates them nowhere either.** That both are the corpus's
displayed `π` is recorded here as a byte-fact and nothing is built on it; building on it would
be authoring the very clause A-1 names as absent, which is barred.

**INDETERMINATE-AT-BYTES:** whether the `Delta Phi` carrier and the surface-of-record carrier
are the same object under two names. [DPHI] names its own sources; [AJ] names its own ground;
neither names the other. The bytes do not decide it.

### 5.5 The Q4 answer in one block

```text
PHASE DATA HELD: 10 objects on the ground where the absence lives (P-1..P-10),
  5 more elsewhere in the corpus (P-11..P-15). Carriers named for all 15.
  ONE object explicitly denied of record ("not sealed as a Berry phase, spectral
  flow, index, or already physical electromagnetic holonomy").

ADJACENCY ON THE GROUND: FOUR sites (ADJ-1..ADJ-4). Not one of them puts a phase
  datum and a response MAGNITUDE in a single clause — ADJ-2 and ADJ-3 are
  phase-to-phase, ADJ-4 is an assertion with no displayed form, and ADJ-1 is a
  shared SECTOR with no clause across it. Consistent with A-1 at bytes.

ADJACENCY OFF THE GROUND: ONE site, [DPHI]'s record overlap, where a phase datum and
  a magnitude-valued factor stand in one displayed expression on one carrier.
  Off-ground; unbuilt at three of its own flags; unlinked by any artifact; unlinked
  here.
```

---

## §6 — Q5: THE TWO UNAVAILABLE CANDIDATES

Both are named at [RL] §5.2 and again in [RL]'s `WINDING_FORCED` flag (§2.2 above). The
corpus's one-line typing of the pair, quoted whole from [JCT] §5's A-3 row (:368):

> "D-ENTER: "an external n-blind comparand" entering sealed (both prior candidates
> unavailable — one fork-gated, one UNAUTHORIZED); arrival would be SHAPE-2 (comparand + the
> derived n^2 prefactor jointly determining \|n\|)"

`one fork-gated, one UNAUTHORIZED` is the corpus's own two-word summary, and it is the
distinction Q5 asks for.

### 6.1 CANDIDATE 1 — **the energy form `c_L <·,·>_(2,ell)`**

**Named at:** [RL] §5.2 — `"the energy form \`c_L <.,.>_(2,ell)\` is gated by the undecided
materiality fork with both settlement paths blocked on SPEC GAPS (T2 §3.3–3.4)"`.

**Source of the blockage, at its own seal.** [RL]'s consumed-source table names T2 =
`STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md`, role `"the materiality criterion and its two SPEC
GAPS"`. That file's seal was verified from its own directory this session: **OK**. Its §3.3,
quoted whole, both paths:

> "**Path A — a factorization derivation.** Derive that `Q_flux` is a conformal isometry in the
> sense above. Requires a **sealed structural property of `R_record,L`'s form** on connection
> tangents. [PROVABLE, negative, searched space as §1.7.2] `V011` supplies `R_record,L` as a
> *definition* — a second derivative of a normalized record functional after a limit — and I
> found no sealed statement of its **form** anywhere in the 1,958 files searched. **That is a
> SPEC GAP**, and by the Builder-B law it is a gap to report, not a detail to go looking for in
> a producer's code."

> "**Path B — a sealed domain restriction.** Seal the set `D` of pairs the criteria actually
> evaluate and prove agreement on `D` only (reading (F-c)). Requires an **enumeration of the
> pairs the sealed criteria consume**. [PROVABLE, negative, same searched space] No such
> enumeration is sealed. **Second SPEC GAP** — and it is the one that makes outcome 3 live
> rather than hypothetical, because without `D` the question cannot even be asked in its
> material form."

**GRADE: UNBUILT — an object that does not exist in sealed form. Not excluded by any proof.**

**The words that decide the grade, quoted whole — the source states the distinction itself, in
the sentence the commission's discipline would have this lane hunt for:**

> "**Neither path is available today.** Both are blocked on an object that does not exist in
> sealed form, not on an unperformed computation."

That sentence rules out both alternative gradings at once: not "excluded by a proof" (nothing is
proved impossible) and not "merely uncomputed" (the object itself is missing). The remaining
grade is UNBUILT.

**Not superseded, not barred by a rule.** The materiality question is typed OPEN with three live
outcomes: [T2]'s flag block reads `MATERIALITY = typed (settlement path + three outcomes
stated)` and lists `FACTORS` / `DOES NOT FACTOR` / `UNDETERMINED`, with
`UNDETERMINED = the S5.3-style burden carries, which is the status today.` An open three-outcome
fork is neither a supersession nor a bar.

**Carried because it cuts against reading this candidate as near-at-hand:** [RL] §4.3 records
that the fork is *logically independent* of the block question its own derivation decides —
`"the sealed materiality fork (T2 §3.4) is logically independent of this block question (it
concerns \`Phi_f^T C Phi_f\` against \`c_L G_(2,ell)\` on the flux block alone, not the cross
block)"` — and [RL] §6 states plainly `"IT DOES NOT decide the materiality fork"`. So nothing in
the derivation that exhibits the `n^2` side moves this candidate at all.

### 6.2 CANDIDATE 2 — **the conversion of `kappa_record` to a physical action coefficient**

**Named at:** [RL] §5.2 — `"the conversion of \`kappa_record\` to a physical action coefficient
is sealed UNAUTHORIZED until \`Z_Q\` is constructed from complete \`Q_spec\` (B9)"`.

**The bar at its own byte-site.** [RL] B9, quoted whole:

> "**B9 — the phase object and its bar (V011).** `Gamma_Q[A] = -i Log(Z_Q[A]/Z_Q[0])` with
> `c = 1` fixed by the complete charged amplitude; "Until `Z_Q` is constructed from complete
> `Q_spec`, the conversion from `kappa_record` to a physical action coefficient remains
> unauthorized.""

**GRADE: BARRED BY A RULE — explicitly, and this lane says so explicitly as the commission
directs. It is not barred by mathematics.**

**The words that decide the grade, quoted:** `"remains **unauthorized**"` — an authorization
predicate, not a mathematical one; and `"**Until** \`Z_Q\` is constructed from complete
\`Q_spec\`"` — a bar with a stated lifting condition written into its own sentence. A bar that
names the act that lifts it cannot be a proof of impossibility. [JCT] types it in one word:
`UNAUTHORIZED`.

**What the lifting act is, as the corpus states it and not as this lane supplies it:** the
construction of `Z_Q` from a *complete* `Q_spec`. The corpus holds `Q_spec` incomplete —
[RL] carries `Gamma_Q` as the phase object with `c = 1` "fixed by the complete charged
amplitude", i.e. by an amplitude the record does not hold complete. **This lane names no route to
completing it and proposes none.**

### 6.3 The Q5 answer in one block

```text
CANDIDATE 1  the energy form  c_L <·,·>_(2,ell)
  WHY UNAVAILABLE : UNBUILT — two SPEC GAPS, both settlement paths blocked
  DECIDING WORDS  : "Both are blocked on an object that does not exist in sealed
                     form, not on an unperformed computation."
  BARRED BY A RULE? NO. Gated by an undecided fork whose three outcomes are all live.
  EXCLUDED BY PROOF? NO.

CANDIDATE 2  the conversion of kappa_record to a physical action coefficient
  WHY UNAVAILABLE : **BARRED BY A RULE, NOT BY MATHEMATICS** — stated here
                     explicitly as the commission requires.
  DECIDING WORDS  : "Until `Z_Q` is constructed from complete `Q_spec`, the
                     conversion from `kappa_record` to a physical action
                     coefficient remains unauthorized."
  EXCLUDED BY PROOF? NO. The bar names the act that lifts it.

NEITHER CANDIDATE IS SUPERSEDED, AND NEITHER IS EXCLUDED BY ANY PROOF ANYWHERE IN
EITHER CORPUS ROOT. One is missing; one is forbidden-for-now.
```

---

## §7 — CHOICE LEDGER (every unforced choice this lane made)

| # | Choice | Class | Disposition |
|---|---|---|---|
| 1 | Treat [AJ] §6.3 kill 2 as the site of A-3 rather than [JCT]/[TYP]'s citations of it | FORCED | the commission names the fragment; the sweep (§10) shows [AJ] :283 is its sole origin, 4 of 5 hits being citations |
| 2 | Identify digest `5e49d209` as [RL] by hashing every workspace `.md` | FORCED | exactly one match; no ambiguity to resolve |
| 3 | Grade A-1 NEGATIVE-RESULT *at the displayed-set quantifier* rather than flatly | AUTHORED-READING (declared) | the corpus supports both a shown-not-to-exist reading (finite-text enumeration) and a not-held reading (wider scope); this lane displays both rather than picking one silently. A reader preferring flat ABSENCE loses nothing else in this artifact |
| 4 | Grade A-3's *value* ABSENCE and report the attached negative result separately | AUTHORED-READING (declared) | the commission's menu offers one grade per item; the site carries two different things (a value not held, and a scoped negative result). Splitting them is this lane's choice and is visible |
| 5 | Read Q3 provenance at the [LPRB]/[BRG]/[EIN] reach rather than at [AJ]'s ground-scope | AUTHORED-READING (declared) | at [AJ]'s own scope the dimension is given ground; the deeper reach changes the grade from "given" to "entered-and-imported". Both readings displayed at §4.3, adverse span carried |
| 6 | Enumerate off-ground phase objects (P-11..P-15) at all | IMMATERIAL | Q4 asks what the *corpus* holds; each is flagged off-ground and none is consumed as a premise for any grade |
| 7 | Report the [DPHI] record-overlap site as an adjacency | FORCED | Q4 asks for every site where a phase datum and a response magnitude sit on one carrier; suppressing the one clear instance would be the unforced act |
| 8 | Decline to relate `Delta Phi_* = pi` to the ground's `\|ΔS_record\|/ℏ = π` | FORCED | relating them would author the clause A-1 names absent — barred |
| 9 | Scope the phase enumeration to objects the corpus *displays* rather than sweeping all 869 "character" hits | PREMISE (named) | the corpus is ~6,800 files; the enumeration is exhaustive over the ground where the absence lives and representative elsewhere. Declared, not concealed. No grade rests on the off-ground list |
| 10 | Consume [BRG] despite no sidecar | PREMISE (named) | cross-root byte-identity substituted; the flags it carries are independently inside sealed [EIN]; nothing load-bearing rests on it alone |

**OPEN choices: none. AUTHORED-READINGS: 3, all displayed at their sites with the competing
reading carried.**

---

## §8 — TOY_SEPARATION

**TOY_SEPARATION = clean.**

This artifact constructs **no** object of any kind — no profile, no clause, no comparand, no
carrier, no bound, no model. There is nothing here that could be a toy, because nothing here is
built.

Toys encountered in the sources and handled as follows:

- **[AJ]'s two model profiles `f₁ = (2 − w − conj w)/4`, `f₂ = f₁²`** — declared TOY by [AJ] §12
  itself. Referenced here **only** by the fact of their existence (that two models of every
  displayed clause differ on the junction), never by their form, never evaluated, never carried
  into any grade. Their argument symbol `Δ` is explicitly excluded from the Q4 enumeration at
  §5.1 on exactly this ground.
- **No toy is introduced by this lane.** No illustrative construction, no worked example, no
  "suppose the dimension were 3", no candidate comparand sketched "for concreteness". The Q3
  question "could the fiber dimension be other than 1" is answered **only** by quoting corpus
  statements about it (§4.4) — this lane exhibits no alternative carrier, and the ambient `C²`
  is reported as displayed at the site, with an explicit refusal to relate it to `dim ≤ 2`.
- **No toy quantity leaks into any verdict line, grade, or flag.**

---

## §9 — IMPORT AUDIT (every non-corpus notion, declared)

| # | Notion | Where it enters | Status |
|---|---|---|---|
| I-1 | **Gleason / frame-function mathematics** ("Frame-function forcing fails in dimension ≤ 2 even when full inner-product structure is granted") | inside the *quoted* [AJ] §6.3 kill 2, and inside the *quoted* [AJ-A] C-2 | **IMPORTED BY THE SOURCES, NOT BY THIS LANE.** [AJ-A]'s own Choice Ledger row 6 declares it: "Treating Gleason facts as generic mathematics for adjudicating §6.3 \| IMMATERIAL(proven) \| used only to SCOPE the build's kill 2". This lane quotes it, reports its quantifier, and adds nothing to it. **No independent Gleason fact is asserted here.** |
| I-2 | **"line bundle ⇒ fibers of dimension 1"** | [AJ]'s unpacking, quoted | IMPORTED BY [AJ]. This lane reports the unpacking and its byte-status (§4.3 Step 1); it neither endorses nor disputes the mathematics |
| I-3 | **Reading `exp(iΔΦ/2)` as a pure-phase factor and `cos(ΔΦ/2)` as magnitude-valued** | §5.4 | **MINIMAL READING OF A DISPLAYED FORM.** Nothing is evaluated, no value computed, no identity applied. The corpus itself types the whole object as "the record overlap" and its argument as a phase datum. Declared because it is the one place this artifact names a type the source does not spell out in that exact word |
| I-4 | Ordinary English discourse vocabulary ("absence", "negative result", "quantifier", "provenance") | throughout | **COMMISSION VOCABULARY, not record vocabulary.** Every *grade* is anchored to quoted corpus words (§2.0 displays the corpus's own two vocabularies for exactly this reason) |

**FORBIDDEN IMPORTS: NONE USED.** No `Λ^even(C⁵)` carrier or carrier machinery; no `K_KK`; no
fiber proper radius `R`; no metric or fiber metric; no `ℓ_P`; no length or scale object; no β
conversion; no S16/Thomson matching; no `κ_Thomson`; no "α rides a scale"; no KK action; no
Finish-A/Finish-B framing; no E1/Carleman/S2′ objects; no GR, sphere, or 4π reading; no
continuum-diamond import; no empirical constant; no measured value. **The lens/ether reading
motivates nothing here and appears in no chain.** TYPE-R is not re-walked and its machinery is
not touched.

---

## §10 — SWEEP CUTOFFS (pattern · hit count · leak counter)

**METHOD.** Every sweep ran through one script holding exclusions as a **bash ARRAY**, never as
a string variable:

```bash
EXC=( --exclude='*REGISTER*' --exclude='*TRACKER*' --exclude='THE_PLAN*' \
      --exclude='ROAD_REMAINING*' --exclude='THE_HANDOFF*' \
      --exclude='OBSERVATIONS_REGISTER*' --exclude='*DECISION_SHEET*' \
      --exclude='STAGE8_THREE_ABSENCES_O41SR_V001.md' )
```

**SELF-EXCLUSION:** this artifact's own basename is in the array of every sweep. **23 of the 24
corpus sweeps ran before this file existed on disk** (its path was probed ABSENT before any
write), so they could not have swept it even had the exclusion failed. The one sweep that ran
after the file existed is **S-14 (`materiality fork`)**; it carried the exclusion array and
returned **LEAK 0**.

**LEAK COUNTER, per pattern:** every result list was re-scanned for any of
`REGISTER|TRACKER|THE_PLAN|ROAD_REMAINING|THE_HANDOFF|DECISION_SHEET|STAGE8_THREE_ABSENCES_O41SR`
and the count reported. **Counts are file-hit counts, run, not listed.**

| # | Pattern | Root 1 (workspace) | Root 2 (cleanroom) | LEAK |
|---|---|---|---|---|
| S-01 | `no clause linking` | 3 | 0 | 0 |
| S-02 | `both sealed candidates unavailable` | 5 | 0 | 0 |
| S-03 | `forcing threshold` | 5 | 0 | 0 |
| S-04 | `fibers of dimension` | 5 | 0 | 0 |
| S-05 | `n-blind comparand` | 9 | 1 | 0 |
| S-06 | `WINDING_FORCED` | 9 | 1 | 0 |
| S-07 | `line bundle` | 58 | 61 | 0 |
| S-08 | `fixed line bundle` | 9 | 6 | 0 |
| S-09 | `principal U(1) comparison bundle` | 3 | 0 | 0 |
| S-10 | `two-dimensional carrier` | 4 | 2 | 0 |
| S-11 | `unique_primitive_carrier_derived` | 5 | 5 | 0 |
| S-12 | `pure_C2_comparison_representation_imported` | 4 | 4 | 0 |
| S-13 | `absence of construction, not prohibition` | 1 | 0 | 0 |
| S-14 | `materiality fork` | 9 | 1 | 0 |
| S-15 | `first orthogonal public record` | 1 | 1 | 0 |
| S-16 | `first-opening` | 200 | 258 | 0 |
| S-17 | `monodromy` | 12 | 3 | 0 |
| S-18 | `holonom` | 566 | 565 | 0 |
| S-19 | `Wilson loop` | 17 | 20 | 0 |
| S-20 | `winding` | 158 | 98 | 0 |
| S-21 | `transition function` | 25 | 14 | 0 |
| S-22 | `character` | 869 | 854 | 0 |
| S-23 | `Berry phase` | 2 | 1 | 0 |
| S-24 | `Aharonov` | 2 | 2 | 0 |

**DOR_* SWEEPS (supervision, permitted files only, 14 files):** `line bundle` 0 · `fiber
dimension` 0 · `dimension 1` 0 · `forcing threshold` 0 · `ray` 0 · `C^2` 0 · `two-dimensional`
0 · `rank` 8 (all inspected: cycle-rank and rank-two *member* classes, no bundle-fiber rank).
**LEAK: 0.**

**TOTAL: 24 corpus patterns + 8 DOR patterns = 32 patterns run. TOTAL LEAK COUNT ACROSS ALL
PATTERNS AND BOTH ROOTS: 0.**

**RECONCILIATION OF THE §0 HEADLINE AGAINST THIS BLOCK — run, not asserted:**

- §0 claims A-3's origin is a single site. S-04 returns **5 / 0**; the 5 are [AJ] (origin),
  [JCT], [JCT-A], [TYP], [THF-A] — four citations of one origin. **CONSISTENT.**
- §0 claims "0 hits across 14 sealed rulings" for the fiber dimension. DOR sweeps return
  `line bundle` 0, `fiber dimension` 0, `dimension 1` 0, `ray` 0, `C^2` 0, `two-dimensional` 0.
  **CONSISTENT.**
- §0 claims one off-ground carrier holds both. S-15 returns **1 / 1** — a single file in each
  root, the same file. **CONSISTENT.**
- §0 calls A-2's absence unambiguous. S-02 returns 5 workspace files, all carrying the same
  quoted phrase back to [RL] (`5e49d209`), and S-05/S-06 corroborate at 9/1 each. **CONSISTENT.**
- §5.1's on-ground enumeration is not a sweep result and is not claimed as one: it is the finite
  displayed inventory of [I2] §2.2(a) plus [I2-A]'s corrections, quoted. §5.2's off-ground list
  is explicitly representative, per CHOICE LEDGER row 9. **NO SWEEP-BACKED EXHAUSTIVENESS IS
  CLAIMED FOR §5.2** — the large counts at S-16/S-18/S-20/S-22 are reported precisely so that
  claim cannot be read into it.

**SWEEP_CUTOFF, declared:** the two named corpus roots at their files as of this session, plus
the 14 `DOR_*` rulings. No file matching the barred classes was opened. No sweep reaches beyond
the two roots. Sweeps bound *this lane's search*; they bound no quantifier reported in §3 —
every quantifier there is quoted from its own source.

---

## §11 — FLAG BLOCK

```text
FENCES = INTACT — alpha_computed = false · proof_authorized = false ·
  kappa_record_computed = false, live throughout. NO numeric value of any coupling,
  scale, root, eigenvalue, norm, or constant computed anywhere. No magnitude
  attempted, reached, or evaluated. The only numerals in this artifact are quoted
  dimensions (1, 2, 3), quoted corpus symbols, seal digests, file/hit counts, and
  section numbers. NO CAS was run; nothing was evaluated; no expression was
  simplified.

SEAL_TALLY = 33 / 34 sidecar-verified OK, each `shasum -a 256 -c` run from the
  artifact's own directory (20 workspace consumed sources: 19 OK + 1 no-sidecar;
  14 DOR_* rulings: 14/14 OK).
  THE ONE UNSEALED SOURCE (PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md)
  IS DECLARED, NOT BURIED: no sidecar in either root; corroborated by cross-root
  byte-identity (fc3e44f0...) and by its consumed flags being carried verbatim
  inside sealed [EIN]. Nothing load-bearing rests on it alone.

BAR_INCIDENTS = ZERO. No file whose name contains REGISTER or TRACKER was opened;
  no THE_PLAN*, ROAD_REMAINING*, THE_HANDOFF*, OBSERVATIONS_REGISTER*, or
  *DECISION_SHEET* was opened. In the supervision directory ONLY the 14 DOR_*
  files (and their sidecars) were read; SURFACE_DEFINITION_OF_RECORD_V001/2/3 were
  listed by filename during the seal survey and NOT opened — declared as the reach
  limit at S1, and the one place it changes a grade is flagged at S4.3.
  LEAK COUNTER: 0 across all 32 patterns and both roots.
  SELF-SCAN OF THIS ARTIFACT for barred basenames: 9 hits, all inspected and all
  benign — 5 are the exclusion array and leak-counter pattern displayed at S10,
  2 are this flag's own bar declaration, and 2 are the string "Register :224"
  occurring INSIDE the [S13] quotation of SI.4 (the corpus's own consolidated-line
  citation, carried because the quotation is carried whole). NO BARRED FILE IS
  NAMED AS A SOURCE ANYWHERE, AND NONE WAS OPENED.

SELF_EXCLUSION = HELD. Own basename in the exclusion ARRAY of every sweep, and the
  file was absent from disk for 23 of the 24 corpus sweeps (path probed ABSENT
  before writing). The single sweep run after creation (S-14) carried the exclusion
  and returned LEAK 0. No count in S10 includes this artifact.

QUOTATION_INTEGRITY = 27 load-bearing spans checked as whitespace-normalized exact
  substrings of their sources by script BEFORE writing — 27/27 WHOLE-OK, 0 FAIL
  (three batches: 13 + 10 + 4).
  One span initially failed and was traced to a `<=` vs `≤` transcription in the
  CHECKER, not in the source; re-checked against exact source bytes: WHOLE-OK.
  MID-SENTENCE LINE WRAPS: every checked span was normalized across source line
  breaks precisely so a wrap could not truncate it; the [AJ] §6.3 kill-2 span, the
  [RL] §5.2 span, the [AJ-A] C-2 span, and the [I2] §2.2(a)/(b) spans each cross
  2-5 source lines and are carried whole. ELISIONS BY THIS LANE: NONE. Three
  ellipses appear inside quoted material and ALL THREE ARE THE CORPUS'S OWN (the
  "fibers of dimension 1 ... below the forcing threshold" splice at [JCT] :221,
  reproduced only where [JCT]/[TYP] are being quoted; the whole [AJ] sentence is
  displayed unelided at S2.3 and S4.1).

ADVERSE_SPANS_CARRIED = 6, each marked at its site: the C12 conditional that
  reverts A-1 to ABSENT at wide scope (S2.1); [RL] §5.3's interior negative result
  that cuts toward A-2 being stronger than graded (S2.2); [AJ-A] Ledger row 6
  making kill 2 non-load-bearing (S2.3); the shallower DERIVED-ON-GROUND reading of
  the fiber dimension (S4.3); [RL] §4.3/§6's statement that its own derivation does
  NOT move candidate 1 (S6.1); and the [DPHI] flags showing the off-ground
  adjacency is unbuilt at three of its own gates (S5.4).

BARS_HELD = nothing authored (no object, no clause, no comparand, no carrier, no
  bound, no model); nothing adopted; nothing advocated; no catalogued negative
  re-read; no gap filled or repaired; no defect this artifact reports is fixed
  here. Where bytes do not decide, INDETERMINATE-AT-BYTES is said — TWICE (S4.4:
  whether C² and the fiber's 1 are both in play at the Gleason site; S5.4: whether
  the DeltaPhi carrier and the surface-of-record carrier are one object).

OWN_DRAFT_DEFECTS, disclosed:
  D-1  The Q1 menu (ABSENCE / NEGATIVE-RESULT / AMBIGUOUS) fits A-3 imperfectly,
       because that site carries TWO things — a value not held and a scoped
       negative result. This lane split them rather than force one grade. A reader
       who wants one word for A-3 should read: ABSENCE, with a narrowed negative
       result attached. Declared as CHOICE LEDGER row 4.
  D-2  The Q3 provenance grade depends on identifying §I.4's "fixed line bundle"
       with [LPRB]'s line bundle. The identification rests on the sentence
       "Connections on a fixed line bundle form an affine space" appearing in BOTH
       [LPRB] :96 and the [S13]-carried quotation of §I.4 — strong, but it is an
       identification across a barred file this lane could not open. If they are
       two bundles, the grade is CANNOT-DETERMINE, not ENTERED. Flagged at S4.3.
  D-3  S5.2's off-ground phase list is representative, not exhaustive (CHOICE
       LEDGER row 9). A full enumeration would require walking 869 "character" and
       566 "holonom" file-hits. No grade rests on it.
  D-4  P-14 (the boundary-closure monodromy) is named from a file whose seal this
       lane did NOT verify, because it is used only as a name in an enumeration and
       nowhere as a premise. Marked NAME-LEVEL-ONLY at its row.

MAGNITUDE_APPROACHED = NO. This artifact maps three absences and does not cross
  them. It supplies no comparand, no clause, no carrier of higher dimension, and no
  route to any. The one place where the corpus's own bytes sit closest to a
  crossing (S5.4) is reported as adjacency with an explicit refusal to relate its
  two displayed pi's, because relating them would be authoring the absent clause.

NET = The three separators are NOT of one kind, though the corpus's own summary
  calls them all "an absence". At bytes they grade differently and fill
  differently. A-1 is a NEGATIVE RESULT over a finite enumerated clause set and an
  ABSENCE outside it; its impossibility (Theorem II) is quantified to one named
  ground and its own next sentence says downstream emergence is "not foreclosed".
  A-2 is a plain ABSENCE of an object, with two named candidates: one UNBUILT
  ("blocked on an object that does not exist in sealed form, not on an unperformed
  computation") and one BARRED BY A RULE that names the act lifting it
  ("UNAUTHORIZED until Z_Q from complete Q_spec") — neither excluded by any proof
  anywhere. A-3 is the softest of the three, not the hardest: the threshold is
  dim <= 2 with the crossing at 3; the fiber dimension of 1 is **ENTERED**, riding
  a carrier the corpus itself flags `unique_primitive_carrier_derived = false` and
  `pure_C2_comparison_representation_imported = true`; no authored ruling touches
  it (0/14 DOR_*); and the corpus's own sealed audit already ruled that the fiber's
  dimension is not even the dimension the threshold governs. On the commission's
  own test — a derived dimension is a wall, an entered one is a choice — this one
  is a CHOICE. And Q4 shows the phase data and the response magnitudes already sit
  in ONE booked sector (SII.6 phase weights beside SII.7 first-opening
  probabilities and projective lengths) with no clause across them, while one
  off-ground carrier displays a phase datum and a magnitude factor in a single
  expression. Two things on one carrier, in four places on the ground and one off
  it — and not one clause joining any of them. NOTHING HERE REACHES, OR ATTEMPTS TO
  REACH, THE MAGNITUDE.
```
