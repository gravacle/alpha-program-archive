# CODEX 2 — Is the Kaluza–Klein framing derived or imported?

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`

## Determination

The KK framing is **imported/adopted, not derived**. The corpus derives a compact `U(1)` comparison group
and a projective/Fubini–Study record geometry, but it does not derive that this internal state space is a
spatial fifth dimension with a physical length radius.

## 2.1 Spatial-fiber identification

The geometric route separately states the internal/projective record-cell geometry and the external
Lorentzian/diamond geometry; it explicitly says the Fubini–Study metric fixes dimensionless distances but
not their dimensional conversion (`STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md:48-58,76-91`).
The held-fixed audit states that a relative `U(1)` is “not automatically that canonical principal fiber” and
that the dimensional conversion remains an open target (`STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md:193-198`).
No sealed derivation identifies the projective direction with a spacetime dimension. The five-dimensional
Einstein-Hilbert parent action is therefore an imported KK ansatz, not an emergent-gravity derivation.

`projective_state_space_is_spatial_KK_fiber = false | TYPE-S | scope: corpus roots and cleanroom_output;
query: dimensional reduction, spatial fiber, projective-to-spacetime identification.`

## 2.2 Internal space

Sealed text supports a primitive two-character record map and relative `U(1)` comparison group
(`STAGE8_DELTAPHI_PI_PROTECTION_DETERMINATION_V001.md:128-182,211-228`). It does not establish that the
state space itself is `S^1`; a group action and its projective state space are distinct claims. The relevant
internal object is the projective record degree with Fubini–Study metric, not a derived circle length.

## 2.3 Squashing contradiction

The saddle-system artifact records the one-dimensional-fiber objection: a one-dimensional fiber has one
modulus and no independent squashing referent (`STAGE8_JOINT_SADDLE_SYSTEM_POSING_EINSTEIN_V001.md:119-126`).
The reopening requirement nevertheless demands breathing and squashing modes. A single-circle ansatz cannot
supply an independent squashing mode; this is a **TYPE-R refutation of the granted ansatz as a complete
saddle**, not merely missing physics.

## 2.4 Parent action

The parent construction introduced `S5=(1/2κ5²)∫√−G R5` as a standard KK choice, explicitly marked
imported/choice (`STAGE8_PARENT_KK_ACTION_ELL_P_DEFINITION_AND_REDUCTION_V001.md`). No corpus derivation of
5D Einstein–Hilbert gravity from record structure was found. Thus the geometric route assumes gravitational
dynamics at its parent level while claiming gravity emergence downstream.

`five_dimensional_EH_derived_from_record_structure = false | TYPE-S | scope: corpus-wide search; no derivation
of parent normalization/action.`

No repair or fork choice was made. No git, commit, push, gate, or deploy action was performed.
