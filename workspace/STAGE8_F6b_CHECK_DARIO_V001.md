# STAGE8_F6b_CHECK_DARIO_V001

LANE: DARIO (external adversarial anchor, relay 1113). ROLE: [CHECK] — adversarially refute
the F6b verdict: Finish-B via *surface-native and beta-carrying are mutually exclusive for
the skeleton-to-cell embedding*. Default to skepticism. Re-derived at the bytes from named
members; **zero testimonial weight given to the subject's own assertions.**
**ALL RESULTS CLAIMED** until the Codex 2 check. Builder never verifies own work.

GATES DECLARED AND HELD: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false; JOINT_ANCHOR_DERIVED = false. Nothing was computed — no value,
bound, or estimate of any coupling, kappa, K_*, beta, threshold, solid angle, fraction,
ratio, or alpha; the 4 pi is a SUBJECT throughout and was never evaluated; every numeric
string is a sealed-text quotation or a symbol. No measured-constant comparison.
NO_REGISTER_READ = CERTIFIED. No git action of any kind. Run solo: the lane's no-delegation
bar overrides the session's standing ultracode/Workflow directive, and no subagent was used.

**NO_SELF_CITATION = CERTIFIED.** The subject books this lane's own 1111 output (KCHK) as
*"[PROVABLE, checked ground]"* and rests part of its load-bearing section on it. KCHK is a
PARTY to this question, not a witness to it, and KCHK itself marked that clause as its own
assembly rather than as PROVABLE. Every ground used below is re-derived from sealed members
other than KCHK. Where the subject's argument depends on KCHK, that dependence is reported
as a custody defect and the claim is re-established from scratch or left unwitnessed.

## SOURCES (all digested at path this relay)

```text
01  454f81e955cb7cdb72343df7da3f16ea2fb546f64112e25be0324b76be18a86a  workspace/STAGE8_F6b_EMBEDDING_TYPING_FABLE_V001.md
02  20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df  supervision/SURFACE_DEFINITION_OF_RECORD_V001.md
03  04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d  workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md
04  b654a29ca86bbb147c37b2480a1c66b5e45316b1a014b1e98e80015a41ed5b61  cleanroom_output/45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md
05  5c679e3741abe782688b8a75ffa1928c308775248e41af192d03976f01cb4dbf  workspace/BID_SOURCE_PARENT_CLOSURE_GATE_V003.md
06  949181d78aca143ebef06eacc4ab1018d43cdf79962ee8c78c350f654a7555dd  workspace/BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md
07  3ef35b34cfdeb7f7b8381dce65a37d026769f29f4e2bbc6184fb581b5a394024  workspace/STAGE8_TASK4A_RAW_G_SOURCE_TO_PHYSICAL_FIELD_LIFT_CONSTRUCTION_AND_TCYL_VERDICT_V001.md
08  382052c4caf7d8d4187c3fdbe98796845060e66519e11ed3b8eb3c454b68d504  workspace/STAGE8_TASK6_A35_EVALUATOR_SPEC_LANE2_V012.md
09  9c9e120e10759f55d9e2f2fd5a09ea5ac61d88f2f3a63de57559b211ba8dd6b5  workspace/STAGE8_S16_BETA_CHECK_OPUS5_V001.md
10  becb0017ef919bf2b48b1e8ddc04edc569ef276c37e16766838c0ccf6dfd7462  workspace/STAGE8_S13_SHAPE_CHECK_OPUS5_V001.md
11  dcb6617452a14c961fac13fee38bc71d3246809d87ffc97c7c93e67b00b352c8  workspace/STAGE8_7A_DBR_BETA_SECTORS_V3_DARIO_V001.md
12  f78c115aea4e78bd18f7213304d51749e5aed57b7715be015d1072be992c0c9c  workspace/STAGE8_7A_BETA_V3_CROSSCHECK_CODEX2_V001.md
13  52664ddbd02e04fa2b9f9856f75ee5c1ab2368eb9959495d2c8cab5d568619f2  workspace/COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md
```

SUBJECT PIN: member 01 matches the paste's pinned digest at path **and carries a sidecar** —
the custody gap this lane flagged at 1089, 1109 and 1111 is closed for this subject. Members
04, 05, 06, 09, 10 carry no sidecar of their own and are flagged; each was digested at path
here, and members 05/06 additionally match the pins recorded inside the sealed member 08.

## 0. LEAD

**The typing is sound. The verdict drawn from it is not — and the error is one level up from
the one I caught at 1111.**

Every attack axis returned for the subject: no non-R surface-native beta route exists (K1),
the two-dimensionful-data ground holds when re-derived without leaning on my own lane (K2),
the embedding requirement is correct (K5), and the fence is clean (K6). F6b **is** now typed,
and the lever — *a junction simultaneously DERIVED and SCALE-SENSITIVE* — **is** dead through
it. On that, the subject is right and I confirm it.

**But `VERDICT = FINISH-B` does not follow from that, and member 04 is the witness against
it.** The subject's own Branch A says: if the embedding enumeration closes, the embedding is
surface-derived, beta-blind, and *"S16 obtains its `4 pi`-class factors as derived pure
numbers"*. It then reads that as **"No lever"** and folds it into Finish B. The inference
fails, because the absence of a scale-sensitive junction is not the absence of a forced
constant. Branch A is not the branch where alpha stays free — **Branch A is the corpus's own
planned route to a forced alpha**, and member 04 books it in those words:

- the surviving family is a **pure-number family with one open fork** — `rho = (3/16) g_N^2 /
  (m_* E_ref)` in `{3/16, 3 sqrt(2)/16}`, `E_ref` in `{m_*, mu}` (member 04 section 1);
- that family is booked **`T_R`-independent** (*"T_R-independence of rho (cycle 4 —
  continuous Families A/C cancel)"*) — so it does **not** ride the free lambda scale;
- the fork is to be closed **by derivation**, not by measurement: W2 requires that *"the
  matching rule must FALL OUT of the derived response normalization, deciding E_ref ... and
  thereby rho — by derivation"*, and section 1 adds that the fork *"may not be decided by
  interface argument — only by the derived response/matching of Stage 10"*;
- and the locked plan's exit condition is **"one absolute coefficient, no deformation
  family"**, held out before any comparison.

A derived pure number, `T_R`-independent, with its last fork closed by derivation and an exit
of one absolute coefficient, is **Finish A** — reached with no scale-sensitive junction
anywhere, because nothing in that chain ever consumes the free scale. The Finish-A lever of
member 02's section IV is a *sufficient* condition for Finish A, not a necessary one; the
subject (and member 02's own `iff` at V.14) treat it as necessary. That treatment is right on
the rail, where alpha's route runs through `K_KK` and therefore through beta — and it is
exactly what Branch A denies.

**So: lever dead — CONFIRMED. Finish B — NOT ESTABLISHED.** The subject has closed the door
member 02 named and left standing the door member 04 describes.

## 1. K1 — THE NON-R BETA-CARRIER, AND A MISATTRIBUTED PREMISE

The task premises K1 on a flag it attributes to the 1110 Codex/Opus5 check: that R is *not*
the sole beta-carrier, a named EMBEDDING CARRIER and separately-weighted Phi SECTORS carrying
it too. **I could not find that flag, and the artifacts on disk say the opposite.**

- Member 09 — the 1110 check itself — contains no occurrence of *carrier*, *Phi*, *w_Phi*, or
  any 'not the sole' clause. Its only *sole*-token is an unrelated custody sentence.
- Member 10, the sibling check of the S13 subject, states the contrary in its verdict lines:
  *"beta is DEFINED as the common fiber-radius rescaling (DBR789 :98), so the fiber radius R
  is the sole beta-carrier"*.
- The Phi sectors do carry different powers of beta (member 11's symbolic table: geometric
  `beta^-2`, cross `beta^(w_Phi - 1)`, `C2_parent` `beta^0`, `Phi^dagger Phi`
  `beta^(2 w_Phi)`), and member 11's non-uniformity result is real — *"beta cannot be
  absorbed by any overall normalisation"*. **But that is not a non-R carriage.** Member 12,
  the sealed cross-check of that very table, closes it: every fixed exponent is *"either (i)
  the sealed derivative/radius weight, (ii) the sealed radius-free weight, or (iii) the
  definitional common-radius weight"*. Every beta exponent in the operative table is a
  RADIUS weight or radius-free. The Phi sectors carry beta **through the radius**; the
  witnessing pair for non-uniformity (`beta^-2` geometric against `beta^0` `C2_parent`)
  *"contains no `Phi` at all"*. Member 11 also records an independent probe finding **no
  sealed scalar-`Phi` carrier**.

[MINE, marked] So the Phi route supplies a genuine result — beta is non-uniform across
sectors and unabsorbable — but it is a RAIL-side result about the parent action, and its
carriage is R's. It gives no surface-native, non-R route by which a derived embedding could
carry beta. `K1_NONR_SURFACE_BETA_ROUTE = NO.`

**Process finding, reported not repaired:** the highest-value axis of this relay was pointed
at a premise the record does not carry. That is worth a registrar note — a check tasked to
find a lever via a named source cannot find one there if the source says the reverse.

## 2. K2 — THE TWO DIMENSIONFUL DATA, RE-DERIVED WITHOUT MY OWN LANE

The subject rests this on KCHK. Under the self-citation bar I re-establish it from members
02, 03, 04 and 13 alone.

**The two:** beta is a ratio of two lengths. Member 13 fixes it at the bytes —
`R = beta c Delta tau` — numerator the fiber proper radius, denominator a record-side length.
**The one:** the record's dimensionful content is a single interval. Member 02 II.7 books the
scale orbit `(T_R -> lambda T_R, H_R -> H_R/lambda)` as free with
`absolute_SI_record_duration_derived = false`; II.10 nets it — the surface forces the entire
dimensionless structure and leaves the absolute scale free.

[MINE, marked] The other derived locks do not add a second length. `|Delta S_record| = pi
hbar` is an ACTION and `m_* T_R = pi` a mass-interval product (member 02 II.9); with one
interval and a conversion they generate no length independent of `c T_R`. The one further
length in play, `ell_P`, is not surface-supplied: member 03 section 1.2 shows it entering
through the compactness selector `C_R = 1`, **adopted** at Level-1. And member 03's lead
states the consequence directly — completing the program's named F-equation *"WOULD FIX THE
DIAMOND'S SCALE AND LEAVE `beta` FREE"*, with a condition whose only output is `T_R` unable
to be the fixer. One length, free; the second is always rail.

`K2_TWO_DATA = CONFIRMED` — on sealed ground, independent of this lane's prior output.

## 3. K3 — IS THE AMPLITUDE BAR TOTAL? NO — AND IT DOES NOT NEED TO BE

Member 02 II.6 books Q-290 TYPE-R with an exact mechanism, but its scope is stated: the write
structure carries unit-modulus weights, and *the ratified write-signature route to closing
beta* fails because unit-modulus structure cannot produce the bounded trace-class lift. That
is a bar on an **amplitude channel in the write structure**, and on **one named route**. It
is not a general theorem that no surface partition of the embedding can carry beta. The
subject uses it as though it were (*"Q-290 TYPE-R bars the amplitude/weight structure a
beta-carrying partition needs"*). `K3_AMPLITUDE_BAR_TOTAL = REFUTED` as a total bar.

[MINE, marked] **The conclusion survives, on stronger ground.** The embedding's invariants are
solid-angle FRACTIONS, volume RATIOS and PARTITIONS of a UNIT flux (member 04 W1). A fraction
of a unit is dimensionless by construction, and beta is a ratio of two lengths. So no
partition can carry beta — **weighted or not**, and whether or not amplitude structure
existed. The bar that does the work here is dimensional, not amplitude-theoretic. This is the
second relay running in which the verdict is right and the cited ground is the weaker one.

## 4. K4 — DO THE CARRIED RESIDUALS HIDE A LEVER?

1. **The one-level-shallow Q_spec residual.** Genuinely open: if the complete Q_spec's
   construction consumes an object outside its sealed contents list, a third channel could
   exist. Member 09 could exhibit no concrete third channel and neither can I. Open, not
   hiding a lever I can name.
2. **The `sqrt(2)`-fork seam.** Member 03's C7 books beta's defining `Delta tau` as
   `sqrt(2)`-fork-conditional; member 04 shows the SAME `sqrt(2)` in the open coupling fork
   `E_ref` in `{m_*, mu = m_*/sqrt(2)}`. [MINE:] this is a real seam and the subject carries
   it correctly, but it is not a beta channel — it makes the invariance statements
   fork-conditional. Its actual weight is on section 0: the fork member 04 requires to be
   closed **by derivation** is the same one conditioning beta's denominator, which sharpens
   the Branch-A reading rather than reviving the lever.
3. **The MPCP three-axis conditionality.** A conditionality on the skeleton (`r = 3` derived
   *given* the three-axis layer, member 04 section 1). Carried correctly; no scale content.

`K4_RESIDUAL_HIDES_LEVER = NO` — with residual 1 recorded as genuinely open, not discharged.

## 5. K5 — DOES A DERIVED S16 REQUIRE THE EMBEDDING?

Re-derived at the bytes, and it holds:

- **The sealed gluing really does stop short of the continuum.** Member 05's SP07 row, read
  at path: global CAR composition, associative cell pushouts, relabeling and orientation
  covariance, shared-support structure — then *"Continuum/time-dependent ordering and
  preparation remain downstream."* Member 06 adds *"fixes the primitive finite stationary
  connected action"* and *"does not select a continuum vacuum, CTP state, durability sector,
  clustering state, physical pole, or residue"*. Both clauses verified whitespace-normalised
  (a plain line-oriented grep misses them — they wrap).
- **The landing is booked at the embedding.** Member 04 section 2(B), exact: the Stage-12
  formula *"contains a solid-angle factor no graph-level computation can produce"*, and the
  embedding of the skeleton into the continuum cell *"is exactly where 4 pi-class factors
  enter the Thomson map. This step is Stage 10's, and it has never been attempted."*
- **The cap theorem holds at the cited span.** Member 07 `[37181,37901)`, first-handed here:
  if the program requires the physical field layer to be derived from geometric record
  structure, the lift is in `T_ref/T_phys` and O-D3 territory and `T_cyl` is insufficient by
  theorem — with the alternative named in the same passage as *an independently authored
  physical CTP field realization* that *"would need its own premise gate"*. The subject's
  typing of that route as premise-gated, hence unable to yield the DERIVED S16, is supported
  by the sealed sentence itself rather than only by assembly.

`K5_REQUIRES_EMBEDDING = CONFIRMED.` I also confirm the subject's positive separation of the
A28 gluing item from the embedding: the gluing content is carrier/incidence/orientation
algebra with no metric, radius, angle or embedding datum in it, so it is beta-blind, and the
requirement sits at the landing foot. The 1110 no-identity bar is properly discharged.

## 6. K6 — FENCE, IMPORT, AND TWO CUSTODY DEFECTS

**Fence clean.** No value computed; the `4 pi` and `pi/2` appear only as sealed-text
subjects; no continuum theorem is used as ground; barred routes are named only to exclude
them. `K6_CLEAN = CONFIRMED.` Defects found, neither load-bearing:

1. **A span citation that does not resolve at the file it names.** The subject writes that
   member 08 pins the gluing content *"at its blocker bytes `[20261,20408)`"*. Member 08's
   own bytes at that range are an unrelated manifest-digest recipe. The quoted sentence is
   real and correctly transcribed — member 08 carries it under `C-B-V011-SP1-07`, where the
   byte range indexes the **V011 blocker packet**, not member 08. So the span is second-hand
   and not verifiable at path; a checker verifying `[20261,20408)` against member 08 finds
   foreign text and could mistake a sound quotation for a fabricated one. **I nearly did.**
   Not load-bearing: member 08 points onward to member 05 line 33, which I verified at path,
   so the substance is first-handed at members 05 and 06 as the subject claims.
2. **A custody upgrade of this lane's own output.** The subject books KCHK as *"[PROVABLE,
   checked ground]"*. KCHK is a CHECK, not a checked object — it has had no Codex 2 pass —
   and the clause borrowed from it was marked there as **MINE (assembly)**, not PROVABLE. The
   direction of the slip is the one this program polices: an unchecked assembly clause
   entering a later artifact as provable ground. Not load-bearing here only because the claim
   is independently re-derivable, which section 2 does.

## 7. WHAT I DID NOT DO

I did not run the W1 enumeration or any part of it; did not evaluate the `4 pi`, the `pi/2`,
any solid angle, fraction, ratio, partition, threshold or coupling; did not decide the
`sqrt(2)` fork or select a lambda-orbit member, radius or spin structure; did not read the
register, tracker, plan, road or ledger; did not enter `a32_holdout/custodian_private/`; ran
no subagent and delegated nothing. I did not retype F1-F5 or F6a.

## 8. OVERCLAIM AUDIT

- **PROVABLE (re-derived at the bytes this relay):** all thirteen member digests; member 04's
  section 1 family, `T_R`-independence, fork-by-derivation clause, section 2(B) booking, W1,
  W2 and the exit condition; member 02's I.4, II.6, II.7, II.9, II.10, III.11, IV.13, V.14;
  member 03's lead, section 1.2 `C_R = 1` adoption, section 1.3 inventory and C2/C7;
  member 05's SP07 row; member 06's two scope clauses; member 07's cap-theorem span;
  member 09's absence of any carrier/Phi clause; member 10's sole-beta-carrier verdict line;
  members 11 and 12 on the Phi-sector weights; member 13's beta relation.
- **MINE (assembly, marked inline):** the reading that Branch A is a Finish-A-shaped route
  and that the lever is sufficient but not necessary for Finish A; the dimensional repair of
  the amplitude bar in section 3; the argument that the action and mass-interval locks add no
  second length; the weighting of the `sqrt(2)` seam toward Branch A.
- **NOT claimed:** that a non-R surface beta route exists (none found); that the embedding is
  derivable or underivable (its provenance stays UNDETERMINED); that Branch A **will** close
  — its antecedent is the unperformed W1 enumeration and it may fail; that member 04's family
  is alpha (it is the coupling family headed there, carried with member 04's own long
  conditionality stack and its `O(g)` correction, and member 04 *"decides nothing Stage 10
  must decide"*); that Finish A is TRUE; that Finish B is FALSE; anything about which finish
  is TRUE. I type the verdict's licence, not the world.

---

## FINAL LINES

```text
K1_NONR_SURFACE_BETA_ROUTE = NO (no surface-native non-R carrier exists; and the premise the
    task cites for it is not in the record — member 09, the 1110 check, contains no carrier /
    Phi / w_Phi / 'not the sole' clause at all, while member 10 states the contrary outright:
    'the fiber radius R is the sole beta-carrier'.  The Phi sectors DO carry distinct powers
    of beta and the non-uniformity result is real and unabsorbable, but member 12 closes the
    carriage question: every fixed exponent in the operative table is a sealed derivative/
    RADIUS weight, a sealed RADIUS-FREE weight, or the definitional common-RADIUS weight, and
    the witnessing pair contains no Phi.  Rail-side result, R-carried.  MISATTRIBUTED PREMISE
    REPORTED.)

K2_TWO_DATA = CONFIRMED (the two: numerator the fiber proper radius, denominator a
    record-side length, fixed at the bytes by member 13's R = beta c Delta tau.  The one: the
    record's single interval, free on the lambda orbit (member 02 II.7, II.10).  The action
    lock and the mass-interval lock add no independent second length; ell_P enters only
    through the ADOPTED C_R = 1 selector (member 03 1.2), and member 03's own lead books that
    fixing the diamond's scale LEAVES BETA FREE.  Re-derived without this lane's prior output,
    per the self-citation bar.)

K3_AMPLITUDE_BAR_TOTAL = REFUTED (Q-290 is scoped in its own sealed text to the write
    structure's unit-modulus weights and to ONE named route — the write-signature route to
    closing beta.  It is not a general theorem excluding every beta-carrying surface
    partition, and the subject uses it as one.  THE CONCLUSION SURVIVES ON STRONGER GROUND:
    the embedding's invariants are fractions of a unit flux, volume ratios and solid-angle
    fractions — dimensionless by construction — while beta is a ratio of two lengths, so no
    partition can carry beta whether or not amplitude structure existed.  The load-bearing
    bar is dimensional, not amplitude-theoretic.)

K4_RESIDUAL_HIDES_LEVER = NO (the one-level-shallow Q_spec residual stays GENUINELY OPEN —
    no concrete third channel exhibited by any lane, including this one; the sqrt(2)-fork seam
    is real and correctly carried but is not a beta channel — its weight falls on the
    Branch-A reading below, since the fork member 04 requires closed BY DERIVATION is the same
    sqrt(2) conditioning beta's denominator; the MPCP three-axis layer carries no scale
    content.)

K5_REQUIRES_EMBEDDING = CONFIRMED (member 05's SP07 stops at 'Continuum/time-dependent
    ordering and preparation remain downstream'; member 06 'does not select a continuum
    vacuum, CTP state, durability sector, clustering state, physical pole, or residue'; member
    04 books the 4 pi-class factors as entering EXACTLY at the embedding, 'a solid-angle
    factor no graph-level computation can produce', 'never been attempted'; member 07's cap
    theorem at [37181,37901) lands the derived-geometric route in O-D3/T_phys and names the
    only alternative as an authored realization that 'would need its own premise gate'.  The
    A28 gluing item is positively SEPARATE and beta-blind; 1110's no-identity bar properly
    discharged.)

K6_CLEAN = CONFIRMED (no value computed; the 4 pi and pi/2 are subjects; no metric or
    continuum machinery used as ground.  TWO CUSTODY DEFECTS, neither load-bearing: (a) the
    blocker span [20261,20408) does NOT resolve in member 08 — those are member 08's bytes
    for an unrelated manifest recipe; the range indexes the V011 packet that member 08 quotes
    under C-B-V011-SP1-07, so the citation is second-hand and unverifiable at the named file,
    though the quotation itself is genuine and the substance is first-handed at members 05/06;
    (b) this lane's own 1111 output is booked as '[PROVABLE, checked ground]' when it is an
    UNCHECKED CHECK whose borrowed clause was marked there as assembly, not provable.)

VERDICT = UNDETERMINED - LEVER DEAD THROUGH F6b CONFIRMED, FINISH-B NOT ESTABLISHED.
    The F6b typing survives every attack I could mount, and the Finish-A LEVER — a junction
    simultaneously DERIVED and SCALE-SENSITIVE — is dead through this foot; I confirm that
    against my own 1111 relocation.  What does not follow is FINISH-B.  The subject's Branch A
    concedes that a surface-derived embedding yields the 4 pi-class factors as DERIVED PURE
    NUMBERS, then reads that as 'No lever' and folds it into Finish B.  But member 04 books
    that same branch as the locked plan's route to a FORCED constant: the surviving family is
    a pure-number family, booked T_R-INDEPENDENT so it does not ride the free lambda scale;
    its one open fork is required to be closed BY DERIVATION, not by measurement; and the exit
    condition is 'one absolute coefficient, no deformation family' held out before any
    comparison.  A derived pure number that never consumes the free scale is Finish A reached
    WITHOUT any scale-sensitive junction.  The lever is a SUFFICIENT condition for Finish A,
    not a necessary one; member 02's V.14 'iff' and the subject both treat it as necessary,
    which is true on the rail — where alpha's route runs through K_KK and hence through beta —
    and is exactly what Branch A denies.  Branch A is therefore not the no-lever branch; it is
    the Finish-A branch, and it is the one the corpus's own W1/W2 work items aim at.
    NEXT DECIDER, NAMED: the W1 blind enumeration with forcing certificates — the subject
    calls it a flavor-sharpener the verdict does not wait on; on this reading it is the
    finish-selector itself, and it decides Finish A versus Finish B rather than which Finish B.

SUBJECT_TYPING = SOUND (sections 2, 3, 5 hold at the bytes; the no-identity bar is discharged
    positively; both provenance branches are genuinely typed — this is a materially better
    artifact than the one this lane checked at 1111, and its body discloses its conditionals.)
SUBJECT_VERDICT_LINE = OVER-READ (FINISH-B outruns what the two branches license; the defect
    is the consequence drawn from Branch A, not the typing.)

EVALUATED_NOTHING = CERTIFIED
IMPORTED_NOTHING = CERTIFIED
OUTPUT_INSPECTION = NONE-CERTIFIED
NO_REGISTER_READ = CERTIFIED
NO_SELF_CITATION = CERTIFIED
NO_SUBAGENT_DELEGATION = CERTIFIED
CHAIN_INVOKED = false

ALL RESULTS CLAIMED. alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; JOINT_ANCHOR_DERIVED = false
```

---

## CLOSURE (declared-first)

```text
CLOSURE_END_BYTE = 24842
CLOSURE_MEMBERS = 13 (content-addressed, each digested at path in this relay)
  member 01  454f81e955cb7cdb72343df7da3f16ea2fb546f64112e25be0324b76be18a86a
  member 02  20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df
  member 03  04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d
  member 04  b654a29ca86bbb147c37b2480a1c66b5e45316b1a014b1e98e80015a41ed5b61
  member 05  5c679e3741abe782688b8a75ffa1928c308775248e41af192d03976f01cb4dbf
  member 06  949181d78aca143ebef06eacc4ab1018d43cdf79962ee8c78c350f654a7555dd
  member 07  3ef35b34cfdeb7f7b8381dce65a37d026769f29f4e2bbc6184fb581b5a394024
  member 08  382052c4caf7d8d4187c3fdbe98796845060e66519e11ed3b8eb3c454b68d504
  member 09  9c9e120e10759f55d9e2f2fd5a09ea5ac61d88f2f3a63de57559b211ba8dd6b5
  member 10  becb0017ef919bf2b48b1e8ddc04edc569ef276c37e16766838c0ccf6dfd7462
  member 11  dcb6617452a14c961fac13fee38bc71d3246809d87ffc97c7c93e67b00b352c8
  member 12  f78c115aea4e78bd18f7213304d51749e5aed57b7715be015d1072be992c0c9c
  member 13  52664ddbd02e04fa2b9f9856f75ee5c1ab2368eb9959495d2c8cab5d568619f2
SPAN DIGESTS (bounds checked against file length):
  01a  [22866,22916)  486afed8e7e91dd2b6a77a141b50e2d90c6cf94051f7aa551cf0965232d25373
  02a  [4870,4895)  97ef18c96fab398aaaa84da952868f9095d1c18553c529b97e3791d8d586d0e0
  03a  [1804,1861)  79bcb20fbfd147031d3e9704ad311374ebc63815a194821ae8b2397bae79e52c
  04a  [3030,3087)  4a2dac27e427ac49a6ed2067fd2c38f023c2cc9bc5991eaf259c5140fa33a12d
  04b  [6090,6130)  4e36e90ece55297bee48c3297b656b8160ded3cc84b3f18642e24dd2979edd9b
  04c  [746,765)  266971f215db33796eca4cb296a05f1354a77d75960200cd1db88b10ee17cbff
  04d  [5647,5685)  5fc3f465982b13efdb1d40decec49c8098664223c678fcb019e8441039e6844a
  05a  [3920,3987)  73bff9d79a85f7dc51290e0c1742fb91c7e51d09b72d671d377abae8aae43c92
  07a  [37519,37529)  34a1e5bde212b65f37b901f728cfc680aa9be8302643a27fc389c270bb1c558d
  08a  [88228,88262)  f967efa6e800a642b2ccb9914fcedd71e76174b9934b9250009336eca5a52fb1
  10a  [12678,12702)  60179f67b930d3f045fe7a387efa8acbe1101d501d312f556dd74ceb4273d114
  12a  [3614,3643)  5ef27d6613ead2138fde80c21202a2b0f8505c64c2c80a25609c5187f214be16
  13a  [1559,1579)  a9dd6dd5a3eef7161754f24c1aa89aa4f20c3c672f4d83b1a03e1fb772241488
```
