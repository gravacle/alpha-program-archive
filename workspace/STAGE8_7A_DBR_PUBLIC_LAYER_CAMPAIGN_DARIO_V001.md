# STAGE 8 / 7A / Q-126 ROUTE — THE PUBLIC-LAYER CAMPAIGN: UNIFORM CORE DELIVERED, THEN THE FALSIFIER TRIGGERS

Lane: DARIO (Builder B, independent verifier). Relay 775.
Governing: `DECISION_SELECTOR_OPEN_AND_GAP_CLASSIFICATION_2026-08-09` `cbdc9432…` (SEALED-OK),
Ruling 1 — the campaign proceeds on the public layer with the bundle class **OPEN**;
**SELECTOR-BEFORE-DETERMINANT** is the route's law. No class adopted. No determinant evaluated.

## Lead determination

**The selector-independent core builds, and then AS4's trigger fires on the public layer's own
subject.**

`q = 0` and `|q| >= 1/2` — **both admissible of record after 768/770** — do not merely differ in a
multiplicity. **They give the tower different bottoms, and different branch cardinalities:**

```text
|q| = 0    : the ell = 0 level has dimension d_S2(0) = 2|q| = 0  — IT DOES NOT EXIST.
             lowest nonempty S^2 level is ell = 1, contributing 1.
             TOWER BOTTOM = p^2_min + 1 + 1 + C2_parent          ONE branch type.

|q| >= 1/2 : the ell = 0 level exists, dimension 2|q|, contributing 0.
             TOWER BOTTOM = p^2_min + 0 + 1 + C2_parent          TWO branch types.
```

The public layer exists to decide *"isolation between the lowest public spectral points"*. **Two
inequivalent admissible completions force different statements about where the lowest point is
and how many branches there are.** That is AS4's criterion — *"two inequivalent completions
forcing different public-layer statements"* — met on the layer's own subject, not on a downstream
number.

**I therefore display the trigger and STOP before positivity's class-dependent half, as AS4
directs.** The decision's own falsifier clause reads *"inequivalent completions yielding different
**determinants**"*; **that clause remains unevaluable** — no determinant may be computed under
selector-before-determinant. **I report the AS4 criterion met and the decision's clause untested,
and I do not merge them.**

**What is delivered first, because it is genuinely selector-independent:** the quotient's fixed
part, the symbolic trace identity, the branch structure as typed structure, and positivity's
uniform half — with a structural result worth the campaign on its own:

> **`|q|` enters the entire tower in exactly two places, and both are AFFINE** — the multiplicity
> weight `d_S2` and the eigenvalue shift `2·ell·|q|`. Everything else in the trace is uniform in
> the class.

**And an internal check I ran before building anything on the parametrisation:** at `q = 0` my
`|q|`-parametrised formulas reproduce the **standard untwisted `S^2` Dirac spectrum and
degeneracies exactly** — `1, 4, 9, 16` with degeneracies `4, 8, 12, 16`. The parametrisation is
validated at the endpoint that matters, and that same check is what surfaced the trigger.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively: **absent**, artifact and sidecar both.

### 0.2 Sources verified before use

```text
DEC    supervision/DECISION_SELECTOR_OPEN_AND_GAP_CLASSIFICATION_2026-08-09.md  cbdc9432…  SEALED-OK
762    workspace/STAGE8_7A_DBR_PUBLIC_LAYER_DARIO_V001.md            beff9f11…  SEALED-OK  (RESUMED FROM)
768    workspace/STAGE8_7A_TOWER_CONTEST_DARIO_V001.md               03db8d3d…
770    workspace/STAGE8_7A_FLUX_CORRESPONDENCE_DARIO_V001.md         3c6cd9a2…
773    workspace/STAGE8_7A_MAGNETIC_SECTOR_HUNT_DARIO_V001.md        54d9cb5a…
PRIN   workspace/mirrored_sources/alpha_global_record_surface_superconnection_principle_v001.md  ae1d0492…
SPEC   workspace/STAGE8_COMPLETE_PUBLIC_DBR_LBR_SPECTRAL_THEOREM_SPEC_V001.md   a751b72c…  SEALED-OK
GAP    workspace/…BR_CLOSURE_OPERATOR_STRUCTURAL_SPECTRAL_GAP_DETERMINATION_V001.md  e2fc00d2…  SEALED-OK
SPEC1  workspace/…BR_CLOSURE_OPERATOR_SPECTRUM_AND_SEAM10_IDENTITY_DETERMINATION_V001.md  de88ba68…  SEALED-OK
```

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities — every number below is a dimensionless multiplicity or an eigenvalue
LABEL in a symbolic expression.  No comparison to measured constants.  NO DETERMINANT
EVALUATED (selector-before-determinant).  NO BUNDLE CLASS ADOPTED.  No common cell formed;
no junction map evaluated; no smooth data derived.  No register, plan, tracker, git action.
```

---

## 1. RESUMED FROM 762 — WHAT IT LEFT OPEN

[PROVABLE] `762` `beff9f11…`, seal verified this relay. Its §2.2 left **five** public-layer
elements, **none determined by sealed stock**:

```text
public quotient / equivalence      FREE / UNBUILT
linear Tr_BR                       FREE / UNBUILT  (public_trace_rule_satisfied = false | TYPE-R)
null/private-mode rule             FREE / UNBUILT
branch selection                   PARTIALLY DETERMINED — the BRANCH is declared by PRIN;
                                   its DATA (radii, spin structure, class, Phi, index) are not
bottom positivity                  FREE / UNBUILT  (both coercivity routes TYPE-R refuted)
bottom simplicity                  FREE / UNBUILT  (and the raw bottom is DEGENERATE of record)
```

[YOURS] 762 also recorded the structural reading I carry forward: **four of the five turn on one
missing object — a derived publicity criterion.** This relay does not supply it and does not need
to: everything below is stated on the **pre-quotient** tower, which is where selector-independence
lives.

---

## 2. AS1(i) — THE QUOTIENT STATEMENT

### 2.1 What is fixed

[PROVABLE] The object being quotiented is sealed: `H_BR = L2(Sigma_BR, S_Sigma tensor E_parent)`
(`PRIN`), with `rank(S_Sigma tensor E_parent) = 4 x 16 = 64` (768 §2).

[YOURS] And its **decomposition** is derived: `H_BR` splits as the orthogonal direct sum of the
`H_0`-eigenspaces labelled `(p_t, p_q, ell)`, each of dimension `2·d_S2(ell)·16` (768 §2.2). **The
label set `(p_t, p_q, ell)` is uniform in the class**; the dimensions are not.

### 2.2 What stops, and the element named

[PROVABLE] The **equivalence itself is unbuilt** — `SPEC1` item 3 demands *"derived public
equivalence relation/quotient Hilbert space"*; `GAP` types it PUBLIC-IDENTITY-CRITICAL,
*"It decides which modes survive and whether the bottom public space is simple"*; and
`unquotiented_skeleton_satisfies_public_cardinality_one = false | TYPE-R` closes the
do-nothing option.

```text
QUOTIENT = stopped (element named): the public equivalence relation.  The DOMAIN is sealed
   and its eigenspace decomposition is derived; the RELATION is not, and no sealed text
   supplies it.  This is 762's finding unchanged, restated on the resumed state.
```

[YOURS] **I did not manufacture a compatibility requirement.** It would be easy to write *"the
equivalence must respect the bigrading and the spectral decomposition"* and call it derived. It is
not derived — it is what one would *want* — and authoring it is the publicity criterion the whole
layer is missing.

---

## 3. AS1(ii) — THE TRACE IDENTITY, SYMBOLIC IN `|q|`

### 3.1 The identity

[YOURS] From the tower (768 §2.2) with multiplicity `[2 · N_lattice] × d_S2(ell) × 16`:

```text
Tr f(H_0)  =  32 · SUM_(p_t,p_q in Lambda)  SUM_(ell >= 0)  d_S2(ell) ·
                    f( p_t^2 + p_q^2 + ell(ell + 2|q|) + 1 + C2_parent )

   32 = 2 · 16      2 = rank(S_(T^2)) ,  16 = rank(E_parent)     [both class-independent]

   d_S2(0)     = 2|q|                 unpaired zero-mode level
   d_S2(ell>=1)= 4(ell + |q|)         chirality-paired levels
```

which separates on the branch boundary:

```text
Tr f(H_0) = 32 · [  2|q| · SUM_Lambda f(p^2 + 1 + C2_parent)
                  + SUM_(ell>=1) 4(ell+|q|) · SUM_Lambda f(p^2 + ell^2 + 2 ell |q| + 1 + C2_parent) ]
```

### 3.2 The structural result — and it is uniform

[YOURS, verified] **`|q|` enters this identity in exactly two places, and both are affine:**

```text
multiplicity weight :  d_S2(0) = 2|q|          d_S2(ell>=1) = 4ell + 4|q|      AFFINE in |q|
eigenvalue shift    :  ell(ell+2|q|) = ell^2 + 2 ell |q|                       AFFINE in |q|
```

**Everything else — the constant `32`, the lattice sum, the label set, the `+1`, `C2_parent` — is
class-independent.** So the whole class-dependence of any trace over this tower is carried by two
affine insertions. **That statement holds for every admissible `|q|` and is the campaign's
sharpest selector-independent result.**

### 3.3 What is NOT claimed

[YOURS] **This is a formal identity, not a convergent trace.** Convergence requires `f` to decay
against the tower's growth, and `TWO`'s convergence bar is a **two-parameter net** — independent
`N_T*`, `N_S*`, Cauchy over **asymmetric** cutoff sequences (762 §3.3). **The S5.3-class burden is
undischarged and I do not discharge it here.** No `f` is chosen, no sum evaluated, no determinant
formed.

```text
TRACE = built symbolic.
```

---

## 4. AS1(iii) — THE BRANCH STRUCTURE, AND THE TRIGGER

### 4.1 The typed structure

[YOURS] The `×4 / ×2` piecewise multiplicity partitions the tower into two sub-towers of different
type:

```text
BRANCH Z (ell = 0)   the S^2 ZERO-MODE level.  UNPAIRED — a single chirality, by the index
                     theorem (768 §1.2).  dim per lattice point = 2|q| · 32 / 2 ... i.e. weight
                     d_S2(0) = 2|q|.  Contributes 0 to the eigenvalue.

BRANCH P (ell >= 1)  the CHIRALITY-PAIRED levels.  weight d_S2(ell) = 4(ell+|q|), the factor 4
                     being 2 (the ± pairing) × 2 (rank S_(T^2)).  Contributes ell(ell+2|q|) > 0.
```

[YOURS] **The branch BOUNDARY is uniform** — it sits at `ell = 0` for every class, because the
pairing fails exactly at the level the index theorem makes unpaired. **The branch OCCUPANCY is
not.**

### 4.2 The trigger

[YOURS, verified in exact arithmetic] `d_S2(0) = 2|q|`. **At `q = 0` this is ZERO — branch Z is
not small, it is EMPTY**, and the tower has one branch type instead of two. And the spectral
bottom moves:

| class | lowest nonempty `S^2` level | its `lambda_S2^2` | tower bottom | branches |
|---|---|---|---|---|
| `q = 0` | `ell = 1` | **1** | `p^2_min + 1 + 1 + C2_parent` | **one** |
| `|q| = 1/2` | `ell = 0` | **0** | `p^2_min + 0 + 1 + C2_parent` | **two** |
| `|q| = 1` | `ell = 0` | **0** | `p^2_min + 0 + 1 + C2_parent` | **two** |

[YOURS] **Validation before I rely on this.** At `q = 0` my parametrised formulas give levels
`ell^2 = 1, 4, 9, 16` with weights `4ell = 4, 8, 12, 16` — **exactly the standard untwisted `S^2`
Dirac spectrum `(k+1)^2` with degeneracy `2(k+1)` per sign.** The `q = 0` endpoint is not an
extrapolation; the parametrisation reproduces the textbook case there, which is why the vanishing
of branch Z is a real structural fact and not an artifact of my labelling.

[PROVABLE] **Both classes are admissible of record.** 768 §1.3 refuted the `|q| >= 1/2` reading
(the untwisted spectrum satisfies the locked floor); 770 showed the charged branch does not force
nonzero magnetic flux; 773 found no magnetic law across 2,952 files; `DEC` Ruling 1 leaves the
class **OPEN**. **Nothing eliminates `q = 0`.**

```text
BRANCH = built (typed structure displayed) — AND THE TRIGGER FIRES HERE.
```

---

## 5. AS1(iv) — POSITIVITY: THE UNIFORM HALF, AND WHY THE OTHER HALF IS THE TRIGGER

### 5.1 Uniform, and proved

[YOURS, verified over `|q| = 0 … 11/2`, `ell = 0 … 39`]

```text
p_t^2 + p_q^2 >= 0        always
ell(ell + 2|q|) >= 0      for every admissible |q| and every ell >= 0
=>  lambda  >=  1 + C2_parent     FOR EVERY ADMISSIBLE CLASS
```

**That bound is uniform, selector-independent, and it is the whole of what positivity yields
without a class.** It is also exactly `TWO`'s stated background gap condition *"For the background
gap `lambda >= 1`"* — recovered here as a consequence rather than assumed, with `C2_parent >= 0`
supplying the rest.

### 5.2 The class-dependent half is the trigger's subject

[YOURS] What positivity is *for* on the public layer is **where the bottom sits and whether it is
isolated** — and §4.2 shows that is precisely what differs between admissible completions.
**Completing (iv) would require choosing between two admissible bottoms**, which is the selection
`DEC` Ruling 1 declines and `SELECTOR-BEFORE-DETERMINANT` forbids.

```text
POSITIVITY = partial (displayed): the UNIFORM lower bound lambda >= 1 + C2_parent is proved
   for every class; the location and isolation of the bottom is class-dependent and is the
   trigger's own subject.
```

---

## 6. AS2 — THE UNIFORM CORE AND THE CLASS-DEPENDENT FAMILY

### 6.1 `UNIFORM_CORE` — holds for every admissible `|q|`. **This is the deliverable.**

```text
U1  The carrier and its rank:  H_BR = L2(Sigma_BR, S_Sigma tensor E_parent),
    rank(S_Sigma tensor E_parent) = 4 x 16 = 64.
U2  The eigenvalue FORM: lambda = p_t^2 + p_q^2 + ell(ell+2|q|) + 1 + C2_parent,
    over the label set (p_t, p_q, ell) — the labels and the form, not the values.
U3  The multiplicity FORM: 32 · d_S2(ell) · N_lattice, the constant 32 = 2 x 16 being
    class-independent.
U4  |q| ENTERS IN EXACTLY TWO PLACES, BOTH AFFINE — the weight d_S2 and the shift 2 ell |q|.
U5  The uniform lower bound lambda >= 1 + C2_parent.
U6  The grading structure (768): H_0 block-diagonal in the bigrading; the cross term T of
    bidegree (1,1) with IDENTICALLY ZERO DIAGONAL, so the tower is exact to first order and
    the whole correction is second order and Phi-dependent.  No step of that argument
    mentions |q|.
U7  The branch BOUNDARY at ell = 0, for every class.
U8  The beta scaling weights — three of them, beta^-2 / beta^-1 / beta^0 (section 7) — none
    of which depends on the class.
U9  The public layer's five open elements and their TYPE-R closures (762), which are
    class-independent obstructions.
```

### 6.2 `CLASS_DEPENDENT` — displayed as a parametrized family, never specialized

```text
C1  d_S2(0) = 2|q|  — the zero-mode weight, AND whether branch Z exists at all.
C2  d_S2(ell>=1) = 4(ell + |q|).
C3  The eigenvalue shift 2 ell |q|.
C4  The location of the spectral bottom:  + 0  (|q| >= 1/2)  versus  + 1  (q = 0).
C5  The branch CARDINALITY: two branch types (|q| >= 1/2) versus one (q = 0).
```

[YOURS] **C4 and C5 are not sizes; they are kinds.** C1–C3 are the affine family U4 describes and
are harmless to carry unspecialized. **C4 and C5 change what the public layer would say**, and
that is the difference between a parametrized family and a fork.

---

## 7. AS3 — THE BETA MECHANISM, CARRIED AND NOT FLATTENED

[YOURS] Restoring the radii (768 §4), the trace's **argument** carries three distinct scaling
weights:

```text
p_t^2/R_T^2 , p_q^2/R_Q^2 , ell(ell+2|q|)/r_flux^2      ~ beta^-2   (two derivatives)
T = Gamma_Sigma c(nabla_A Phi)                          ~ beta^-1   (exactly one derivative)
C2_parent                                               ~ beta^0    (internal Spin(10)
                                                                     invariant; Sigma_BR
                                                                     carries no Spin(10)
                                                                     directions)
```

**Where the non-uniformity enters the public layer:** at the trace identity's **argument**
(§3.1) — i.e. at exactly the point where a normalization would be applied to make the tower
dimensionless. **`TWO`'s locked spectrum is written with the radii absorbed**, which does not
remove the β-dependence but **hides it in a unit convention** (762 §3.1).

```text
NO NORMALIZATION IS PERFORMED IN THIS RELAY.  The trace is stated with the radii RESTORED
in section 7 and in the unit convention in section 3, and the two are displayed as the same
identity under a convention rather than one being derived from the other.  The
beta-carrying step — GAP's "normalized transfer from D_BR^2 to the L_BR proper-time
consumer" — is untouched and remains VALUE/CONSUMER-CRITICAL and unbuilt.
```

[YOURS] And β's non-uniformity is **uniform in the class** (U8): the three weights are what they
are for every `|q|`. So the β question and the selector question are **independent axes**, and
neither relay's stop is the other's.

---

## 8. AS4 — THE TRIGGER, DISPLAYED; AND THE STOP

### 8.1 The trigger

```text
TRIGGER.  Two inequivalent completions, BOTH ADMISSIBLE OF RECORD, force different
public-layer statements:

   q = 0      ->  branch Z is EMPTY (d_S2(0) = 0); ONE branch type;
                  tower bottom = p^2_min + 1 + 1 + C2_parent
   |q| >= 1/2 ->  branch Z is nonempty (dim 2|q|), UNPAIRED, single chirality;
                  TWO branch types; tower bottom = p^2_min + 0 + 1 + C2_parent

The public layer's subject is "isolation between the lowest public spectral points"
(SPEC1 section 0).  The lowest point's LOCATION and the branch CARDINALITY both differ.
This is AS4's criterion — "two inequivalent completions forcing different public-layer
statements" — met on the layer's own subject.
```

### 8.2 What I do NOT claim, and the distinction matters

[YOURS] `DEC` Ruling 1's falsifier reads *"inequivalent completions yielding different
**determinants** kills the route."* **I have not evaluated a determinant and may not** —
selector-before-determinant is the route's law and this relay's gate. **So the decision's clause
is UNTESTED, not met.**

**AS4's operational criterion is met; the decision's determinant clause is unevaluable. I report
both and merge neither.** Whether a difference in branch cardinality and spectral bottom
*entails* a difference in weighted charged determinants is a step I decline to take here: it is
plausible, it is not derived, and taking it would convert an unevaluable clause into a fired
falsifier by assertion.

### 8.3 The stop

[YOURS] AS4 directs: *"display the trigger and STOP — the principal sees it first."* **I stop
after §5's uniform half**, having completed everything selector-independent and having declined
the one step — choosing between two admissible bottoms — that would have completed positivity.

[YOURS] **The escalation, stated plainly:** 765 recorded the falsifier's antecedent as satisfied
by *"different zero-mode counts"* — a difference in a number. 768 sharpened it to *"whether a
zero-mode level exists at all."* **This relay locates that difference on the public layer's own
subject**: the branch decomposition it is built from, and the spectral bottom it exists to
isolate. **The gap has not widened; it has surfaced where the campaign was standing.**

---

## 9. GROUNDING, JURISDICTION, VERB AUDIT

### 9.1 Grounding

| # | Claim | Basis | Tag |
|---|---|---|---|
| 1 | Ruling (c); class OPEN; selector-before-determinant; falsifier stands | `cbdc9432…` | PROVABLE |
| 2 | 762's five open elements and their TYPE-R closures | `beff9f11…` §2.2, seal verified | PROVABLE |
| 3 | The carrier and rank 64 | `ae1d0492…`; 768 §2 | PROVABLE |
| 4 | The multiplicity law `[2 N_lat] × d_S2(ell) × 16` | 768 §2.2 (Weyl-audited) | YOURS |
| 5 | The trace identity and its branch separation | §3.1 | YOURS |
| 6 | `\|q\|` enters in exactly two places, both affine | §3.2, verified | YOURS, exact |
| 7 | `q = 0` reproduces the untwisted spectrum `1,4,9,16` with degeneracies `4,8,12,16` | §4.2, verified | YOURS, exact |
| 8 | `d_S2(0) = 0` at `q = 0`: branch Z empty | §4.2, verified | YOURS, exact |
| 9 | The bottom shift `+0` vs `+1` | §4.2, verified | YOURS, exact |
| 10 | `lambda >= 1 + C2_parent` uniformly | §5.1, verified `\|q\|` = 0…11/2 | YOURS, exact |
| 11 | Both classes admissible of record | 768 §1.3; 770; 773; `cbdc9432…` | PROVABLE |
| 12 | The three β weights | 768 §4 | YOURS |
| 13 | The two-parameter net; S5.3 burden undischarged | 762 §3.3 | PROVABLE |

**Pin check: 13/13.**

### 9.2 Jurisdiction check

**On stopping a campaign the principal has just authorised.** *What was the rule written to
protect?* AS4 exists so the principal sees a trigger before the lane builds past it. The risk was
live — the trigger sits at step (iii) of four, and continuing to (iv) would have required choosing
a bottom. *Does the outcome space distinguish false from cannot-see?* Yes: §6.1 delivers a named
uniform core, §6.2 a named family, §8 the trigger with its exact criterion, and §8.2 the clause I
did **not** test. *Would the evidence appear if the theory is right?* Yes — a selector, or a proof
that `q = 0` is inadmissible, removes the trigger and the campaign resumes at (iv).

**On SELECTOR-BEFORE-DETERMINANT.** No determinant, no `f`, no sum evaluated. §3 is an identity in
an unevaluated symbol.

**On the VOID CONDITION.** No class adopted or preferred. The trigger is symmetric — it reports
that two completions differ, not that either is right — and §8.2 refuses the inference that would
have converted it into a fired falsifier.

**On BR-1.** `DEC` authorises the campaign; it does not authorise its result. The trigger is
reported against the campaign's own momentum.

### 9.3 Self verb audit — **CLEAN, with three disclosures**

1. **I validated the parametrisation at `q = 0` before building on it**, and that check is what
   surfaced the trigger. Had I only tested `|q| > 0` — the range everyone has been assuming — the
   branch-cardinality change would have stayed invisible. **The endpoint I nearly skipped is the
   one that mattered.**
2. **I declined to write the quotient's compatibility requirement** (§2.2), which would have read
   as progress and is exactly the publicity criterion the layer is missing.
3. **I declined to infer "different determinants" from "different branch structure"** (§8.2). It
   is plausible and it is not derived, and asserting it would fire a falsifier by assertion —
   which, given that this lane has twice this session mistaken a plausible chain for a derivation,
   is a step I will not take unwitnessed.

---

```text
RESUMED_FROM = 762 (seal verified: beff9f11…, shasum -c OK).  It left FIVE public-layer
   elements, none determined by sealed stock: quotient, Tr_BR, null/private rule, bottom
   positivity, bottom simplicity — with branch selection only partially determined (the
   BRANCH declared by PRIN, its DATA unselected).
QUOTIENT = stopped (element named: the public equivalence relation).  The DOMAIN is sealed
   (H_BR, rank 64) and its H_0-eigenspace decomposition derived; the RELATION is unbuilt and
   no sealed text supplies it.  I declined to author a compatibility requirement in its place.
TRACE = built symbolic.  Tr f(H_0) = 32 · SUM_Lambda SUM_ell d_S2(ell) ·
   f(p_t^2 + p_q^2 + ell(ell+2|q|) + 1 + C2_parent), separating on the branch boundary.
   STRUCTURAL RESULT: |q| enters in EXACTLY TWO PLACES, BOTH AFFINE — the weight d_S2 and
   the shift 2 ell |q|.  Formal identity only; the two-parameter-net S5.3 burden is
   undischarged and no f is chosen, no sum evaluated, no determinant formed.
BRANCH = built (typed structure).  Branch Z (ell=0): UNPAIRED single chirality, weight 2|q|,
   contributes 0.  Branch P (ell>=1): chirality-paired, weight 4(ell+|q|), contributes
   ell(ell+2|q|) > 0.  The branch BOUNDARY is uniform at ell = 0; the OCCUPANCY is not.
POSITIVITY = partial (displayed).  UNIFORM and proved: lambda >= 1 + C2_parent for EVERY
   admissible class — which recovers TWO's stated background gap as a consequence rather
   than an assumption.  The location and isolation of the bottom is class-dependent and is
   the trigger's own subject, so (iv) is not completed.
UNIFORM_CORE = named: U1 carrier and rank 64 · U2 eigenvalue FORM and label set · U3
   multiplicity FORM with the class-independent constant 32 · U4 |q| enters in exactly two
   affine places · U5 lambda >= 1 + C2_parent · U6 the bigrading and the cross term's
   identically zero diagonal (no step mentions |q|) · U7 the branch boundary at ell = 0 ·
   U8 the three beta weights · U9 the five open elements and their TYPE-R closures.
CLASS_DEPENDENT = displayed as family, never specialized: C1 d_S2(0) = 2|q| and whether
   branch Z exists · C2 d_S2(ell>=1) = 4(ell+|q|) · C3 the shift 2 ell |q| · C4 the bottom's
   location (+0 vs +1) · C5 the branch CARDINALITY (two vs one).  C1-C3 are U4's harmless
   affine family; C4 and C5 are KINDS, not sizes.
FALSIFIER = TRIGGERED (displayed).  q = 0 and |q| >= 1/2 are BOTH admissible of record
   (768 refuted the |q| >= 1/2 reading; 770 showed the charged branch forces no magnetic
   flux; 773 found no magnetic law in 2,952 files; DEC leaves the class OPEN) and they force
   DIFFERENT PUBLIC-LAYER STATEMENTS: at q = 0 branch Z is EMPTY, the tower has ONE branch
   type and its bottom is p^2_min + 1 + 1 + C2_parent; at |q| >= 1/2 branch Z exists, there
   are TWO branch types and the bottom is p^2_min + 0 + 1 + C2_parent.  The public layer's
   subject is precisely "isolation between the lowest public spectral points", so AS4's
   criterion is met ON THE LAYER'S OWN SUBJECT.  I STOPPED after positivity's uniform half,
   per AS4.
   NOT CLAIMED: DEC Ruling 1's clause reads "yielding different DETERMINANTS", and no
   determinant may be evaluated under selector-before-determinant.  That clause is UNTESTED,
   not met.  I report AS4's criterion met and the decision's clause unevaluable, and I
   decline to infer one from the other.
   VALIDATION FIRST: at q = 0 the parametrisation reproduces the standard untwisted S^2
   Dirac spectrum EXACTLY — levels 1, 4, 9, 16 with degeneracies 4, 8, 12, 16 — so the
   vanishing of branch Z is a structural fact, not a labelling artifact.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+3 disclosures at section 9.3, including that the endpoint I
   nearly skipped is the one that produced the trigger)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
