# K*-TYPING DETERMINATION — AUDIT V001 (2026-08-14, second issue)

Auditor: KST-AUDIT (independent adversarial; default posture REFUTATION — every verdict
component attacked at the cited bytes before concession). Subject:
`KSTAR_TYPING_DETERMINATION_V001.md`, recomputed at path =
`2b12dc4066794d49a012e81aca54ca5055d8dbfdfcf83c3eff5aead4c2a88d1c` = tasked digest = sidecar.
Instrument: `ACTUAL_SURFACE_GUARD_V001.md`, recomputed `2baa4c31…` = sidecar = tasked prefix;
its RELEVANCE BURDEN, CALIBRATION, PURPOSE, and PUSHBACK FORK clauses govern this audit's
framing (hole-finding, never prohibition).

Fences live throughout: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false. K*'s TYPE only: no value of K*, kappa_record, kappa_Thomson,
beta, or alpha computed, bounded, estimated, or compared, anywhere in this audit. Every
numeral below is a sealed-text quotation, a digest, an occurrence count from this audit's own
scans, or a line number — declared as such. No register, road_, ledger, lens, plan, tracker,
or THE_HANDOFF file opened (grep FILENAME hits noted only). Register rows cited as "Q-…"
inside sealed artifacts are unreachable BY DESIGN and are treated EXPECTED-UNLOCATABLE
throughout (Q-34, Q-62, Q-140 N11, Q-290, Q-09, Q-10, Q-691: all so treated; none counted
against the build). Probe scope: the two corpus roots recursively (*.md, fenced classes
excluded) plus wider negative diligence under "/Users/bgm/MB Work" only. No git. One output
file + its seal; no other file edited.

---

## 0. VERDICT (one line)

CONFIRMED-WITH-CORRECTIONS — all three components of the build's SPLIT verdict (NOT-TIED;
TYPED-MAGNITUDE with the I2 vacancy ENGAGED and APPARATUS-ONLY-as-STATUS; IDENTITY-at-§IV
plus HOMONYM-at-§II.9) survive independent refutation attempts at bytes and independent
re-derivation of every occurrence claim; five corrections c-1..c-5, of which ONE (c-1, the
§2.2 J_FS-carrier enumeration: four files stated, seventeen found) touches the verdict
chain's stated evidence and is outcome-preserving — the corrected full scan strengthens
NOT-TIED — and four are cosmetic, two of them favorable to the build (the onset gate V003 is
in fact SEALED; a sealed re-attestation of its bar exists that the build did not need).

---

## 1. PROVENANCE, AND THE PRIOR ARTIFACT AT THIS PATH (decision of record)

### 1.1 The supersession record

At audit start this path already held a sealed artifact: a complete prior K*-typing audit,
digest `3618394825a2adbc3a1fb28de6676799eb9927cdc1adb0f0e451061d1671710b` (= its sidecar;
mtime 2026-08-14 14:17), verdict CONFIRMED-WITH-CORRECTIONS, read here in full before
replacement. Its own ledger discloses two custody acts BARRED under this audit's brief: (its
A-2) it read `THE_HANDOFF_2026-08-14_V002.md` as tasking instrument — a fenced filename class
here; (its A-1) it stepped outside the corpus boundary into the Documents origin tree to
locate `primitive_record_cell_selection_principle_v002.md` (its correction C3, claimed digest
`a3c7349bf7b1dbc4f87bd449effa7d82040ab8967e3c5c79f5dd89705fd6a839`, manifest-pinned). This
re-issue runs under the stricter brief. Disposition, stated plainly:

- Findings of the prior audit REPRODUCED INDEPENDENTLY here, before it was read: its C1
  (= c-2 below, the V003 sidecar) and its C2 as far as it went (= c-1 below — which this
  audit EXTENDS: the prior audit's corrected count of twelve carriers itself missed the five
  `cleanroom_output/` carriers found here).
- Findings NOT verifiable under this brief: its C3 (v002 located at the origin tree). Carried
  below strictly as PRIOR-AUDIT-CLAIM at flagged custody; nothing in this audit's verdict
  rests on it.
- Nothing is silently lost: the prior artifact's digest is pinned above; the registrar can
  retrieve that version by digest if its wider-license findings are wanted of record.

### 1.2 Seal-verification table — every claimed seal re-verified at path (`shasum -a 256`)

| Document | Recomputed digest (prefix) | Sidecar | Build's claim | Audit |
|---|---|---|---|---|
| KSTAR_TYPING_DETERMINATION_V001.md (subject) | `2b12dc40…a88d1c` (full above) | MATCH | — | = tasked digest |
| alpha_supervision/ALPHA_STIFFNESS_IS_K_STAR_PRINCIPAL_DECISION_2026-07-30.md | `75450619` | MATCH | MATCH | CONFIRMED |
| alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md | `20ee87c0` | MATCH | MATCH | CONFIRMED |
| alpha_supervision/ACTUAL_SURFACE_GUARD_V001.md | `2baa4c31` | MATCH | MATCH | CONFIRMED |
| BARE_SURFACE_I2_DETERMINATION_V001.md | `95235195` | MATCH | MATCH | CONFIRMED |
| BARE_SURFACE_I2_DETERMINATION_AUDIT_V001.md | `97ae605d` | MATCH | MATCH | CONFIRMED |
| STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md | `04c10c0d` | MATCH | MATCH | CONFIRMED |
| alpha_supervision/RESULT_TWO_STIFFNESS_OBJECTS_2026-07-30.md | `f3eb0c84` | MATCH | MATCH; = digest the decision quotes at :7 | CONFIRMED (both) |
| STAGE8_F0_KSTAR_DEFINING_CHAIN_CODEX2_V001.md | `95c003ca` | MATCH | MATCH | CONFIRMED |
| STAGE8_F0_COMPUTAND_CHAIN_V001.md | `05eeab53` | MATCH | MATCH | CONFIRMED |
| STAGE8_KAPPA_RECORD_KSTAR_PAIR_TEST_UNDER_Q61_V001.md | `5d86c9c1` | MATCH | MATCH; cross-attested at CODEX2 D10 | CONFIRMED (D10 row :104 pins the identical full digest) |
| STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md | `45fa8f6e` | MATCH | MATCH; quoted MATCH in OPUS5 :15 | CONFIRMED (both) |
| STAGE8_S16_BETA_CHECK_OPUS5_V001.md | `9c9e120e` | **NONE found** (name-variant search + archive-wide grep for the digest: no sidecar exists) | NO SIDECAR | CONFIRMED |
| alpha_supervision/DERIVED_RESULTS_INVENTORY_2026-08-01_V001.md | `f12a95b4` | MATCH | MATCH | CONFIRMED |
| CHAIN_RELEVANCE_MAP_V001.md | `eae6cf88` | MATCH | MATCH | CONFIRMED |
| STAGE8_SLOT16_REPOSED_SCOPE_AND_FK1_AUDIT_V001.md | `df0d1c74` | MATCH | MATCH | CONFIRMED |
| BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md | `cc113a16` | **MATCH — sidecar EXISTS** as `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.seal.sha256` (naming without `.md`; mtime 2026-07-23) | NO SIDECAR | **REFUTED — correction c-2 (favorable)** |

Additional cross-pin re-verified byte-exact: CODEX2 row D07's span
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md[17597,20485)` re-hashed here =
`775e7191fd4e83ee5116dd4d0cb937f83679b902c983a313d6fd99a2bf7c19ae` = the pinned span SHA
(carrier file digest `2d63dfad` = CODEX2's pin). The C_record definition span is byte-real.

### 1.3 Line-cited quotations — every one opened and checked at its bytes

Decision :11 ("the onset root of the one-cell closure condition"), :12-13 (the displayed
defining condition + the v002 `:95-97,:104` citation), :15-16 (`K_* -> kappa_Thomson`),
:23-25 (impedances, `Z_F = 1/kappa_F`), :30-32 ("only through the complete microscopic
action…"), :46-47 (transport banner), :54-55 ("relocates a hard problem"), :67-69 (F-GK3,
"none complete") — ALL CONFIRMED in context. Two-stiffness :17-18 (defining condition,
double attestation with decision :12-13 CONFIRMED), :52, :55-58, :60-64, :67 ("0 of 11
SUPPLIED") — ALL CONFIRMED. Surface: §II.8-9 = :42-47; "the onset bound" at :47; K_* occurs
in the file ONLY at :63 and :68 (audit grep, whole file) — CONFIRMED. Hunt: "Onset budget
`J_FS,rel >= pi hbar/2` derived; 'Saturation is adopted, not derived.'" at :84-85 (build
cites :83-85, span-inclusive — no miss); "the onset inequality" as fourth derived beta-blind
junction at :110-114; §METHOD "ALL FOUR VERIFICATIONS RETURNED CONFIRMED" :18 — ALL
CONFIRMED. CODEX2: 1,787 files :58; sixteen spans D01-D16 §2; "MERE MENTION" ruling :112;
D07 row :101; AS3 :190; AS4 :214-219; link 9 ABSENT (§3 table); F0_VERDICT = PARTIAL :296 —
ALL CONFIRMED. F0 chain :278-279 (TYPE-U flags), :303-308 (`K e_HL^2 = 1`;
"generic…DERIVED; …`K_*` physical = CARRIED/ADOPTED, TYPE-U"), :493-497 ("required-not-built
… F-RP1 FIRED") — ALL CONFIRMED. Pair test :125-129 ("not as write strength"), :131-134
("surrogate coordinate for an induced connection response…") — CONFIRMED. FABLE :124-125 (F1
verbatim), :283 ("K_* (derived lock, no R)"), header :5 "ALL RESULTS CLAIMED", pin table
:17-18 (ANCHOR = the surface 20ee87c0; CENSUS = the hunt 04c10c0d — the build's reading of
F1's citation targets is exact) — CONFIRMED. OPUS5 :93 ("onset inequality / `K_*` (SURFACE
§II.9) — β-blind = subject F1"), :10 + :21-23 (five tasked files; un-tasked sources NOT
opened) — CONFIRMED. Inventory :174-175 (LOWER BOUND DERIVED / SATURATION ADOPTED), :192
(the formula "IS ITSELF ADOPTED") — CONFIRMED; the file's ONLY K_* token is :192, in an
entry unrelated to its J_FS entry. Relevance map item 28 :409-424 (three named unknowns:
definition of record / tie / typing — the build's "posed exactly the three questions" is a
fair mapping), item 27 (kappa_record A-O by the I2 constraint), U-11 :471-473 ("plausible
eventual consumer") — CONFIRMED. Slot-16 audit :20-22 — CONFIRMED verbatim. Onset gate
:88-90, :163, :172-173 — CONFIRMED verbatim (and now at SEALED custody, c-2). I2
determination §0/:9 (missing referent named), :76 ("A strength has nothing on-record to act
on"), :116 ("structural, not informational"), :284/:345; I2 audit :9
(CONFIRMED-WITH-CORRECTIONS, "none touching the verdict chain") — ALL CONFIRMED.
ZERO misquotes, zero out-of-context reads, zero line-number misses found.

---

## 2. INDEPENDENT OCCURRENCE SCANS (task C) — exact search forms, run fresh

Token classes used throughout: K-side regex `K_\*|K_star|K\^\*|Kstar` (also ` K \* ` probed:
nil); residual `C_record|C_\{record\}`; `(?i)stiffness`; onset-side narrow
`J_FS|J_\{FS\}|onset[ -]bound|onset inequality|onset budget`; onset-side broad
`(?i)saturation|J_?FS|pi hbar ?/ ?2|πℏ/2|Fubini`; typing hunt
`(?i)phase-typed|action-typed|surface-native|surface-derived|phase/action|record-native`.

**2.1 The lock's lineage (build §2.2 bullet 1).** All four files the build names scan ZERO
for every K-side form, ZERO `C_record`, ZERO `stiffness` — CONFIRMED. But the enumeration
"every J_FS-carrying non-fenced file in both roots" is INCOMPLETE — correction c-1. The
audit's full enumeration of J_FS carriers (both roots, *.md, fenced classes excluded,
subject and this audit excluded): SEVENTEEN distinct files across twenty-one paths —
workspace (8): the four the build lists plus `STAGE8_AXN_GRAMMAR_CROSSCHECK_DARIO_V001.md`,
`STAGE8_BETA_SURFACE_DERIVATION_CHECK_OPUS5_V001.md`,
`STAGE8_BOUNDARY_FORCING_CHARACTERIZATION_FABLE_V001.md`,
`STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md`, and the hunt itself;
supervision-class (4, present byte-identically in BOTH `alpha_supervision/` and
`alpha-program-archive/supervision/` — all four mirror pairs digest-verified identical):
`AXIOM_SET_MAP_2026-07-29.md`, `BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md`,
`DERIVED_RESULTS_INVENTORY_2026-08-01_V001.md`,
`RELAY_PASTE_255_DISCHARGE_STEP3_ONSET_SATURATION_V001.md`; cleanroom_output (5):
`03_SYMBOLIC_DERIVATION.md`, `06_DIMENSION_SYMMETRY_AND_LIMIT_AUDIT.md`,
`25_SEAT_OCCUPANCY_SPEC_V001.md`, `26_SEAT_OCCUPANCY_RESULT_V001.md`,
`33_GATE1_COMPARISON_GROUP_SPEC_V001.md`. **Corrected result: every carrier scans ZERO for
all K-side forms and ZERO `C_record`**, with one typed exception — the inventory's single
K_* token at :192 (the ADOPTED-formula booking, an entry unrelated to its :174 J_FS entry;
evidence FOR the build's §3, not a tie). NOT-TIED's first side survives its own correction,
strengthened.

**2.2 K*'s lineage (build §2.2 bullet 2).** Hunt: 0 occurrences, every K-side form
(re-derived; and 0 `C_record`, 0 `stiffness`). CODEX2's D01-D16 read row by row: all sixteen
are register/Gamma_K-spec/currency-audit/pair-test/V011/Thomson/F-RP1 objects; ZERO
onset-bound or J_FS spans ("onset" occurs in D01/D10 only inside "onset root"/"onset/root
surrogate" — K*'s own naming, the homonym's other half); the whole CODEX2 file: 0 J_FS
tokens. K*'s own defining stock scans 0 J_FS everywhere: decision 0, two-stiffness 0, pair
test 0, F0 chain 0, CODEX2 0, FABLE 0, OPUS5 0. Two-sided absence: RE-DERIVED IN FULL.

**2.3 Beyond the build — corpus-wide co-occurrence sweep.** Narrow sweep (K-side form AND
narrow onset-side form anywhere in one file): 8 hits = the surface, the inventory (+ their
two byte-identical mirrors), the relevance map, FABLE, OPUS5, and the subject itself — every
one already treated by the build; no untreated hit. Broad sweep (K-side AND
saturation/pi-hbar-2/Fubini forms): 21 paths; every hit not already treated was opened and
adjudicated at context:
- `BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md` (same-line hit inside a one-line embedded
  JSON report): its "K_*" is an ORPHAN — "K_R(mu) and K_* (orphan running/fixed-point
  stiffness)", `K_* = b_ch / (4 pi f_grav)`, quoted from a file it describes as "NO seal
  sidecar, in NO authority ledger"; its onset-side hits are gate-retirement bookkeeping.
  NOT A TIE (and a third homograph — see §5, hole h-2).
- `BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md` :154: "Everything derived here is
  one-sided: `C_R <= 1`, `J_FS,rel >= pi hbar/2`, `|Z| <= 1`, `K_R > 0`, `g >= 0`. In each
  case the magnitude arrives only by adopting saturation." — the corpus's closest
  same-sentence approach of a K-object to the onset bound is an explicit ANTI-tie statement
  (and its K-object is the superseded K_R, not K*). AFFIRMATIVE CORROBORATION of NOT-TIED.
- `AXIOM_SET_MAP_2026-07-29.md`: J_FS context = the orthogonalization budget; stiffness
  contexts = the K_bare = 0 adoption lineage. Disjoint topics. NOT A TIE.
- `STAGE8_AXN_GRAMMAR_CROSSCHECK_DARIO_V001.md` :121-125: re-seals the onset gate's own bar
  — quotes "`J_FS,rel = pi hbar/2`" and lists "identification of J_FS,rel with microscopic
  dynamical action" under Not Established. A SEALED re-attestation of the gate line the
  build consumed (moot after c-2, but see hole h-5). NOT A TIE — the opposite.
- `STAGE8_RUNS_TARGET_POSED_V001.md` / `STAGE8_RUNS_TARGET_POSE_CHECK_V001.md`: "saturation"
  there is the l1-tight weight-sum clause (C4 lineage) — a different "saturation". The
  POSED artifact is a blind poser ("DECLARES nothing…STANDING_STATUS = LEAN-UNFORCED"). NOT
  A TIE.
- `STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md`, `STAGE8_LANE_STATUS.md`,
  `STAGE8_GAMMA_K_QUASIFREE_ROOT_ROUTE_ATTEMPT_V001.md`,
  `DECISION_OF_RECORD_003…`, `815_DONE.md`, `STAGE8_GC2_MATERIALITY_OPENING_DARIO_V001.md`,
  `STAGE8_F0_COMPUTAND_CHAIN_V001.md`, `BOHM_RATIO_ROUTE_ADJUDICATION…`,
  `RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE…`: saturation/Fubini contexts all unrelated or
  barred-route statements ("Fubini-Study explicitly NOT taken as a multiplier"; "BARRED
  ROUTE: Fubini-Study, closed by V011 itself"). NOT TIES.
**No booked derivation tying a K*-token to an onset-bound/J_FS-token exists anywhere in the
swept corpus. NOT-TIED: REFUTATION FAILED.**

**2.4 Negative probes.** `find "/Users/bgm/MB Work" -iname "*primitive_record_cell*"`: 0
hits — the build's §1.2 absence claim re-derived at its licensed scope (see c-5 for the
prior-audit claim beyond it). OPUS5 sidecar: none under any naming variant; archive-wide
grep for `9c9e120e` finds only citations, no sidecar. Both NO-SIDECAR/absence claims that
survive c-2 are true.

---

## 3. THE CONTRARY TEXT (task D) — re-adjudicated at bytes

The build's four §2.4 points, each attacked:
1. "The citation does not contain the claim" — CONFIRMED. F1 cites "ANCHOR II.8-9; CENSUS
   §1.3"; FABLE's own pin table (:17-18) fixes ANCHOR = the surface and CENSUS = the hunt.
   Surface §II.8-9 (:42-47): 0 K-side tokens (the file's only two sit at :63/:68, §IV). The
   hunt: 0 K-side tokens in the entire file, a fortiori in §1.3. Neither cited span can
   perform the identification.
2. "Contradicts the definition of record" — CONFIRMED. The double-attested defining
   condition makes K* the root of `C_record` in the K-indexed family; the §II.9 lock is an
   inequality in `J_FS,rel` in which no K occurs (its lineage: §2.1 above). Grammar check,
   run as the audit's own refutation attempt on the build: could "the onset root of the
   onset inequality" be RIGHT — is K* the root OF the inequality? The displayed bound
   `J_FS,rel >= pi hbar/2` has no root structure and no K-dependence at any sealed carrier;
   "onset root" grammar attaches at bytes only to `C_record(K_*) = 0` with
   `dC_record/dK != 0` (decision :12-13; two-stiffness :17-18; CODEX2 D01/D07). And K* is
   not "one of the four derived locks": sealed status flags say
   `physical_K_star_selected_or_derived = false | TYPE-U` (F0 chain :278-279), the residual
   is "required-not-built … F-RP1 FIRED" (:493-497), and the assembly formula "IS ITSELF
   ADOPTED" (inventory :192). REFUTATION OF THE BUILD FAILED; F1 remains an unsupported
   homonym-binding.
3. "The confirmation could not have caught it" — CONFIRMED and COMPLETED. OPUS5 :10/:21-23:
   only the five tasked files opened; un-tasked sources not opened. Audit addition: the two
   tasked sources the build did not individually clear were scanned — GATE
   (`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md`) 0 K-side tokens, DARIO
   (`STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md`) 0 K-side tokens. NO file in
   OPUS5's scope carries K*'s definition. The blind-spot mechanism is exactly as the build
   states; OPUS5 :93 itself performs the same unsupported slash-binding ("onset inequality /
   `K_*` (SURFACE §II.9)") against a cited span with no K_* token.
4. The consequence-statement (F1's foot loses its ground; the FABLE headline
   `S16_BETA_SENSITIVE = UNDETERMINED` NOT overturned; K* not a built foot) — CONFIRMED at
   the FABLE/CODEX2 bytes; the slot-16 audit precedent (:20-22) is quoted accurately.

---

## 4. TYPING (task E), REFERENT (task F), VACANCY + FORK (task G)

**4.1 TYPED-MAGNITUDE, attacked.** Corpus-wide typing hunt (forms in §2 header) for any
sealed span typing K* phase/action-typed or surface-native/surface-derived, or displaying a
C_record ingredient that is a surface clause or displayed lock: 7 candidate files; each
adjudicated at bytes; ALL FAIL to refute —
`RELEASE_CONDITION_F2_DRAFT_V001.md` G1.3 "RECORD-NATIVE AND BLIND" is a DRAFT gate
REQUIREMENT on a future computation (its own falsifier: "no sealed chain defines K_* →
'compute' is undefined") — not a typing; the RUNS_TARGET pair poses and declares nothing;
`STAGE8_SLOT18_Q34_NATIVENESS…` ranks slot-18 OBSERVABLE candidates and merely quotes the
Q-34 ruling — not a typing of K*; FABLE's "record-native germ" is Z_N, not K*; the map and
the subject are the instruments themselves. Meanwhile every positive typing byte found runs
the build's way: "Stiffness is a magnitude" (two-stiffness :52), impedance form (decision
:23-25), `K e_HL^2 = 1` (F0 chain :303-308), "not as write strength … induced connection
response" (pair test :125-134; CODEX2 AS3 :190), admitted-data classes "geometry, clock,
measure, regulator, action partition, state/domain, spectrum, duration" (CODEX2 AS4
:214-218) — none a §I-§II clause or displayed lock. TYPED-MAGNITUDE: REFUTATION FAILED. The
build's scoping of "magnitude-valued" (response-strength-typed, unit-dimensionless — the I2
sense) is byte-faithful.
**Honesty quantifier on beta-consumption:** correctly OPEN — decidability requires an
appointed executable `C_record(K)` in which to count occurrences, and CODEX2 link 9 is
ABSENT ("D09 expressly finds no current appointment"). Not decidable at bytes today; the
build refuses exactly the shortcut F1 took. CONFIRMED.

**4.2 REFERENT, attacked.** §IV identity: all five load-bearing phrases of surface §IV.13
byte-matched to the decision ("(principal act", `K_* -> kappa_Thomson` :15-16, "transport a
cell quantity to a continuum coupling" :46-47, "relocated hard problem" :54-55, "the onset
root" :11); the surface's charter (:3-8) authors no new physics. Alternative reading attempt
(§IV's K_* = F1's "onset root of the onset inequality"): no §IV byte supports it; §IV's own
anchor is the principal-act citation. IDENTITY: REFUTATION FAILED. §II.9 homonym: the two
lineages share zero tokens both directions (§2.1-2.2); C-4's identification chain
re-derived independently — surface §II.8 cites the hunt as source (:44-45); the hunt's §1.3
fourth derived junction is its §1.1 displayed budget, the only inequality in the
internal-side display; inventory :174-175 and I2 Chain D (:99 "onset bound | named bound,
form undisplayed") carry the same single object. HOMONYM: CONFIRMED.

**4.3 VACANCY + CALIBRATION + FORK.** The I2 determination (sealed, audited
CONFIRMED-WITH-CORRECTIONS with no verdict-chain correction — audit :9, verified) names the
missing referent: "a derived, β-invariant, magnitude-valued internal→external response
junction on the write structure" (§0, :284, :345), with the lack "structural, not
informational" (:116). K* per §4.1 is an apparatus-side object of precisely that type;
engagement for any magnitude-side landing FOLLOWS, and is the same inference the sealed
relevance map already runs for kappa_record at item 27 (verified). The build presents
APPARATUS-ONLY strictly as STATUS with all three CALIBRATION exits, exit (1) primary and
identified with the §2.5 theorem, exit (2) expressly NOT indicated, exit (3) named
principal-gated (5fa46838 — the guard's own cited handle, :63) — the harsher two-exit
framing the guard bars is NOT committed. PUSHBACK FORK (§3.4): both horns stated with
standing and named deciders; neither collapsed; the fork left open. GUARD-COMPLIANT ON ALL
FOUR CLAUSES. CONFIRMED.

---

## 5. COMPLETENESS (task J) — holes found, named as findings (never prohibitions)

- h-1 the §2.2 carrier enumeration (= correction c-1; closed by this audit's full scan).
- h-2 a THIRD K_* homograph exists in-boundary and is nowhere named: the orphan
  running/fixed-point `K_*` (`K_* = b_ch / (4 pi f_grav)`) quoted inside
  `BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md` from a no-seal, no-authority file. It
  fails all three §IV byte-anchors and predates the decision — harmless to the referent
  verdict — but the homonym-trap inventory (build §4.2) is incomplete without it.
- h-3 the build asserted OPUS5's scope carried no definitional authority without clearing
  GATE and DARIO individually; audit cleared both (0 K-side tokens each). Closed, favorable.
- h-4 the byte-identical supervision mirrors are unmentioned; all four pairs verified
  identical here (no divergent-copy hazard is live today).
- h-5 a SEALED re-attestation of the onset gate's NOT-ESTABLISHED bar exists at
  `STAGE8_AXN_GRAMMAR_CROSSCHECK_DARIO_V001.md` :121-125; the build's §2.3 custody caveat
  was dischargeable in-corpus even before c-2. Favorable texture only.
- h-6 affirmative corroboration unnamed: BOHM_CONSISTENCY :154 (one-sided inequalities
  yield magnitudes "only by adopting saturation") independently supports both NOT-TIED and
  the two-stiffness magnitude ruling the typing rests on.
None of these holes, closed, moves any verdict; h-1 is the only one touching stated
evidence, and its closure strengthens the build's conclusion.

---

## 6. CORRECTIONS

- **c-1 — VERDICT-CHAIN (outcome-preserving, conclusion strengthened).** Build §2.2 bullet
  1: "Occurrence scan of every J_FS-carrying non-fenced file in both roots" enumerates FOUR
  files. True carrier set: SEVENTEEN distinct files / twenty-one paths (§2.1) — quantifier
  slippage in the guard's failure-mode-1 sense, and internally inconsistent with the
  build's own §2.1, which quotes the inventory's J_FS lines (a fifth carrier). Corrected
  full scan: zero K-side/C_record tokens in every carrier (single typed exception: the
  inventory's unrelated :192 ADOPTED-formula line). NOT-TIED survives and is strengthened.
  (Extends the prior artifact's twelve-carrier count, which missed the five
  cleanroom_output carriers.)
- **c-2 — COSMETIC (favorable).** `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` is
  NOT sidecar-less: `…V003.seal.sha256` (naming convention without `.md`) exists, matches
  `cc113a16`, mtime 2026-07-23 — predating the build. §2.3's corroboration and the C-6
  custody premise upgrade from flagged to sealed custody; the build's NOT-TIED never rested
  on §2.3 (its own statement, verified), so no verdict motion. OPUS5's missing sidecar
  remains true.
- **c-3 — COSMETIC.** Build §3.1's "0 of 11 complete" is attributed to "decision :67-69",
  whose bytes read "11 of 11 Section-1 pieces partially supplied, none complete"; the
  numeral form "0 of 11" is sealed at two-stiffness :67 ("0 of 11 SUPPLIED"). Attribution
  nit; the numeral remains a sealed-text quotation; content equivalent.
- **c-4 — COSMETIC.** Strict-reading tension: the build's charter line "every numeric string
  below is a sealed-text quotation" is not literally true of its own process counts ("grep
  count 0", §2.2). No fenced quantity is involved; the value fence itself is clean (§7).
- **c-5 — COSMETIC.** Build §1.2's "absent from the reachable corpus"/"ZERO hits anywhere"
  for `primitive_record_cell_selection_principle_v002.md` is accurate at its licensed probe
  scope (all of "/Users/bgm/MB Work" — re-derived here: 0 hits) but should carry that scope
  qualifier: the prior artifact at this path reports locating the file OUTSIDE that scope
  (Documents origin tree, claimed digest `a3c7349b…`, defining bytes claimed verbatim) —
  carried here strictly as PRIOR-AUDIT-CLAIM at flagged custody, unverifiable under this
  brief. If accepted of record it VERIFIES the build's C-3 premise at source and changes no
  verdict; if rejected, the build's C-3 stands exactly as the named premise it already is.

---

## 7. FENCE COMPLIANCE OF THE BUILD (task I)

Full-text scan: no numeric evaluation, bound, estimate, closed form, or measurement
comparison of K*, kappa_record, kappa_Thomson, beta, alpha, or alpha_micro anywhere in the
subject. Every physical-content numeral checked to a sealed carrier: `J_FS,rel >= pi
hbar/2` (hunt :84-85; inventory :174), `alpha_micro = 1/(4 pi K_*)` (decision :13;
two-stiffness :18; inventory :192), `K e_HL^2 = 1` (F0 chain :305-306), `K_KK ~ beta^2`
(FABLE §5/V002 as carried; hunt §1.3), "0 of 11"/"11 of 11" (two-stiffness :67; decision
:68), "sixteen"/"D01-D16" (CODEX2 §2), "1,787" (CODEX2 :58), "thirteen sealed interface
quantities" (surface :43; hunt :106), "four derived locks" (surface :46-47). Remaining
numerals are line numbers, digest prefixes, and its own occurrence counts (c-4). No fence
crossed; no fenced flag disturbed. CLEAN.

---

## 8. CHOICE-LEDGER AUDIT (task H) AND INDEPENDENT TOY_SEPARATION

Build ledger, entry by entry: **C-1** boundary — honestly named; its second-hand mitigation
(CODEX2's 1,787-file sweep "register included, its D01/D02", at CLAIMED custody) is real at
the CODEX2 bytes. PREMISE(named): SOUND. **C-2** fenced-class-by-filename — the evasion
margin is real and the build names it and narrows; the audit's own sweeps share the same
margin (a-6). OPEN, honestly held: SOUND. **C-3** definition from two display carriers —
double attestation verified byte-identical here (decision :12-13 = two-stiffness :17-18);
the premise is named, not smuggled; the prior-audit claim (c-5) would verify it at source.
PREMISE(named): SOUND. **C-4** — re-derived independently at §4.2: the identification is
forced by the citation chain and display structure. IMMATERIAL(derived): CONFIRMED. **C-5**
no-CAS — confirmed by the subject's content: every load-bearing step is occurrence- or
type-reading; no symbolic identity requiring checking appears. IMMATERIAL: SOUND. **C-6**
custody grading — half-REFUTED favorably (c-2): the V003 flag was wrong, the OPUS5 flag
right; the entry's own claim that nothing verdict-bearing rests solely on either is
verified (§2.2 of the build stands on sealed files alone). Missed unforced choices hunted:
the §2.2 enumeration shortfall (c-1) is a missed narrowing the ledger should have carried;
no other unforced choice found that the ledger omits.

**TOY_SEPARATION (independently re-derived) = clean — after correction c-1.** No
load-bearing step rests on an unproven convenience: the verdict chain runs on sealed-byte
occurrence facts (re-derived here in full), named premises (C-1/C-2/C-3/C-6, each honest),
and one derived identification (C-4, re-derived). The single convenience that had leaked
into a load-bearing claim's WORDING — the four-file enumeration standing in for "every
J_FS-carrying file" — is exactly a guard failure-mode-1 instance, now corrected with the
conclusion preserved and strengthened; it never functioned as a hidden generality premise
because the census cross-check (CODEX2) and the map's item 28 independently bounded the
same absence.

---

## 9. AUDIT CHOICE LEDGER (this audit's own unforced choices)

- a-1 corpus boundary = the two tasked roots recursively, *.md, fenced filename classes
  excluded; wider probes under "/Users/bgm/MB Work" only; Documents/origin trees NOT
  entered (differs from the prior artifact's license) — PREMISE(named).
- a-2 prior-artifact handling: the sealed prior audit at this path was read in full,
  digest-pinned (§1.1), adjudicated, and superseded at the tasked output path per the
  tasking; its out-of-boundary finding is carried flagged (c-5), never as ground —
  PREMISE(named).
- a-3 search-form set (§2 header) — OPEN at the margin: a nonstandard token spelling could
  evade all forms; mitigated by running four independent sweep families and by CODEX2's own
  multi-form census as a second-hand cross-check at its CLAIMED custody.
- a-4 mirror identity: all four supervision mirror pairs digest-verified byte-identical —
  IMMATERIAL(derived).
- a-5 embedded-JSON handling: the two BOHM supervision files carry single-line embedded
  reports; adjudication used ±150-200-character context windows around EVERY K-side and
  onset-side hit (all windows individually read) rather than full-file line reads —
  PREMISE(named), margin: a tie phrased with >200 characters separating its tokens inside
  one blob would evade the window; the proximity sweep (±3 lines corpus-wide) and the
  narrow sweep bound the residual risk.
- a-6 fenced-class exclusion by FILENAME — OPEN at the margin (same as build C-2): a
  register-class file under a nonstandard name would evade; all quantifiers here range over
  the filtered set.
- a-7 the C-4/§4.2 identification consumed the surface's citation of the hunt plus display
  structure — IMMATERIAL(derived in-artifact).

TOY_SEPARATION (this audit's own chain): clean — every step is byte-reading at recomputed
digests; the named margins (a-3, a-5, a-6) are disclosed with their mitigations and none is
load-bearing alone.

---

## 10. FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS — all three components of the build's SPLIT
    stand under adversarial re-derivation: NOT-TIED (re-derived two-sidedly: 0 K-side/
    C_record tokens in all seventeen J_FS carriers; 0 J_FS tokens in K*'s entire defining
    stock; 0 onset-bound spans among CODEX2's sixteen; corpus-wide co-occurrence sweeps
    find NO untreated tie, and the closest same-sentence co-occurrence, BOHM consistency
    :154, is an explicit anti-tie); TYPED-MAGNITUDE (typing hunt found NO sealed span
    typing K* phase/action-typed or surface-native; every positive typing byte is
    magnitude/impedance/induced-response; beta-consumption OPEN is the correct quantifier
    — link 9 ABSENT makes occurrence-typing impossible today); IDENTITY at SIV + HOMONYM
    at SII.9 (five-phrase byte-chain verified; alternative readings refuted; F1/OPUS5
    re-confirmed as an unsupported homonym-binding citing spans with zero K_* tokens).
CORRECTIONS = c-1 VERDICT-CHAIN (outcome-preserving): "every J_FS-carrying non-fenced file"
    was FOUR of SEVENTEEN files (twenty-one paths, incl. five cleanroom_output carriers the
    prior audit also missed); corrected scan zero K-side tokens everywhere — NOT-TIED
    strengthened. c-2 COSMETIC favorable: onset gate V003 sidecar EXISTS (naming without
    .md, cc113a16 MATCH, mtime 2026-07-23) — S2.3/C-6 upgrade to sealed custody. c-3
    COSMETIC: "0 of 11 complete" attribution (numeral sealed at two-stiffness :67, not at
    decision :67-69). c-4 COSMETIC: charter-line strict reading vs the build's own process
    counts. c-5 COSMETIC: the v002-absence claim is scope-true (0 hits under MB Work,
    re-derived) and should say so; the PRIOR-AUDIT-CLAIM of its location at the origin
    tree (digest a3c7349b..., unverifiable under this brief) is carried flagged and, if
    accepted, verifies build premise C-3 at source.
PRIOR_ARTIFACT = superseded at this path per tasking, digest pinned 36183948... (sealed
    2026-08-14 14:17; its brief permitted THE_HANDOFF and an origin-tree step-out, both
    barred here); its C1/C2 reproduced independently here before reading; its C2 extended
    (twelve -> seventeen carriers); its C3 carried as flagged claim only.
HOLES (PURPOSE clause: findings, not prohibitions) = h-1 closed by c-1; h-2 an unnamed
    third K_* homograph (the orphan running fixed-point K_* = b_ch/(4 pi f_grav) inside
    BOHM_TWO_STEPS, no-seal no-authority source; fails all three SIV anchors; harmless to
    the referent verdict but missing from the homonym inventory); h-3 closed favorably
    (GATE and DARIO cleared: 0 K-side tokens); h-4 mirrors verified identical; h-5 sealed
    re-attestation of the onset gate's bar at AXN grammar :121-125; h-6 BOHM consistency
    :154 as unnamed affirmative corroboration.
LEDGER_AUDIT = build C-1..C-6 all honestly named; C-4 re-derived IMMATERIAL; C-6
    half-refuted favorably (c-2); one missed narrowing (c-1) — the ledger's only real gap.
TOY_SEPARATION(build, re-derived) = clean — after correction c-1; no load-bearing step
    rests on an unproven convenience; the corrected quantifier preserves and strengthens
    the conclusion.
TOY_SEPARATION(audit) = clean — byte-reading at recomputed digests throughout; margins
    a-3/a-5/a-6 named with mitigations.
VALUE_FENCE = CLEAN both sides — build scanned line-by-line: no evaluation, bound,
    estimate, or comparison of any fenced quantity, every physical numeral traced to a
    sealed carrier (two nits, c-3 attribution and c-4 strict reading, neither a value);
    this audit wrote only sealed-text quotations, digests, occurrence counts, and line
    numbers.
PUSHBACK_FORK = both horns stated by the build (S3.4) with standing and named deciders,
    neither collapsed; verified guard-compliant; this audit also collapses neither.
EXPECTED_UNLOCATABLE = all "Q-..." register rows cited inside sealed artifacts (Q-34, Q-62,
    Q-140 N11, Q-290, Q-09, Q-10, Q-691) — unreachable by design, not findings.
FENCES_AT_CLOSE = alpha_computed = false; proof_authorized = false;
    kappa_record_computed = false — live at open and close; no register/road/ledger/lens/
    plan/tracker/THE_HANDOFF content opened; no git; no existing file edited except the
    tasked output path, whose prior sealed occupant is digest-pinned above.
```

---

Fences at close: alpha_computed = false; proof_authorized = false; kappa_record_computed =
false. No value of K*, kappa_record, kappa_Thomson, beta, J_FS, or alpha was computed,
bounded, estimated, or compared; every numeral above is a sealed-text quotation, a digest,
an occurrence count, or a line number. One output file; seal alongside. KST-AUDIT ends.
