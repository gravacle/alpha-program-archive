CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00003352
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 9 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2   ROLE_THIS_RELAY = OPPOSITE-LANE CROSS-CHECKER
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_964_FAMILIES_V002_CROSSCHECK_CODEX2_V001.md` | `289a758c96c817000a090d05ec5da577015b78f66eb943817219ca83f68679ba` | assignment |
| 02 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 03 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process and claim-status law |
| 04 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01-S37 flattening guard |
| 05 | `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V002.md`; correction `[2841,4228)`; retyping `[4229,6387)`; payload/root `[6388,15426)` | whole `bda1dcf4e3c395a4aac1eb7709a2a118c615bf93ae720314b34e5564a2fc7e6e`; spans `47476453061d10231850d5927ee0e788fc4ab76ef0ea440ca788fe296fd46904`, `d328e7ea93d3b8697721f706f8ab8d35398709101490b3e1c2b401449c8cef17`, `094636593a47b60eec07fd0dfd221b6205045b75eef0b1c989a018d83d9f5590` | subject |
| 06 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V003.md`; JAC-14 law `[19618,21630)`; closed suite `[21684,24116)`; variant-invariant matrix boundary `[24121,24326)`; bounded escape law `[26035,27186)`; `G_joint`/no-coordinate `[13731,14585)`; tag templates `[14620,15899)` | whole `79f0c35161204f846666badb38f28398ae317d56a64d8fe70f7f29e2ee01072e`; spans `9198511b2bd9c0d854b4acaf4ff5160016494e5af29c8df5c39b7da537a94e2e`, `2dfc97a91656b6f7d41c3498cf8cf15f09e2c12479774cb372f5555950afe407`, `26f48d2e782e8dde2261ade64af3bcfaed7414fc909e8d1942a0fb6039d891d3`, `4a68dcdf88e666192a1bf0ee5d0878bebce66cee6964d5217e7965eb3734a688`, `65f5fd4a9275cc2eed242b0f8f74baa401e2d60c3c270bcfb60413c320bcc07b`, `482972b3bdddf597d3776cc8feecfcecf1c1775ddc2217192db530cd995185c6` | governing types, named receivers, grammar, and tag content |
| 07 | `STAGE8_AXN_FAMILIES_CROSSCHECK_CODEX2_V001.md`, receiver finding `[12370,17546)` | whole `c2bd93eb80fb719f91601eedb2d62d980dcefc473e286a902cfb74875bd2e481`; span `fa8fb454959341d804ecbac28206e13c5c8e4a025f250df29ab74241ef49e8dc` | prior opposite-lane finding and selector disposition |
| 08 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V002.md`; `E_joint` certificates `[9457,14353)`; pairing/faithfulness `[15786,18227)` | whole `fd2625a079c77fbc0a102a54a0dd8ba1d97dcfb393035c2b691b0475de254444`; spans `ba65f87c9347ede0c261cf30560b92d3c6f1535512f633e94d648b35c094f917`, `cd0705a9f6e5d039b2523793047a978459fee0b9c9add50e410e12be495775d7` | exact receivers of the booked comparison objects |
| 09 | `STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V002.md` | `f450b0a356b249509fb59b897c4f6a14e6996ba7da5ea513e69112a89907eed9` | bounded stage class and receipt ground |

```text
ALL NINE WHOLE-FILE SEALS WERE VERIFIED BEFORE SEMANTIC USE.
NO PHYSICAL QUANTITY WAS NUMERICALLY EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN `for_class` FAMILIES V002 CROSS-CHECK — CODEX 2 LANE — V001

Date: 2026-08-10  
Relay: 964 / `[PLAN:AXN-BUILD-C63]`  
Status: **THE RE-TYPING IS WRONG AT THE GOVERNING BYTES. `BOUNDED_CLASS` CHANGES HOW THE STAGE INVENTORY IS COVERED; IT DOES NOT ERASE JAC-14'S FINITE-STAGE BASIS, EXACT RESTRICTION-MATRIX, OR EXACT FACTORIZATION/INVERSE RECEIVERS. THE THREE OPERATOR-ONLY WITNESSES DO NOT FILL THEM, SO THE SELECTOR RETURNS. INDEPENDENTLY, ALL EIGHT PAYLOADS DROP THE `N>=1` QUANTIFIER, NONE BINDS A CANDIDATE INSTANCE OR A UNIVERSAL PROOF CARRIER, FC-08 DOES NOT BIND THE ENTERED BI CONTENT, AND THE CLAIM THAT BOOKED `E_joint` ENTERS ONLY FC-06 IS BYTE-FALSE. PAYLOAD BYTES AND THE ORDERED ROOT DO REPLAY. VERDICT: FAILURES NAMED; RUN 4 DOES NOT GO.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

## 1. Re-typing ruling — first and load-bearing

### 1.1 The governing bytes do not permit receiver substitution

The subject quotes only the value types inside the `BOUNDED_CLASS.for_class` object. Those eight
values do use the common phrase `exact total replay object on the bounded class` (with the closed BI /
DB / SYM refinement on FC-08). But that phrase is nested under eight **named keys**. The enclosing
JAC-14 law, which governs both inventory variants, says:

```text
finite-stage basis of Delta_0^joint,
exact restriction matrices,
exact factorization/inverse,
...
reject any promise of exact/replayable that lacks ... exact matrices ...
```

The closed schema then names `delta0_basis_family`, `phi_restriction_matrix_family`, and
`factorization_and_inverse_family`. Immediately after the variant closes, V003 states that the
finite matrix is stage-wise. Its bounded-escape explanation says every named `for_class` family
remains mandatory and every family must be a total exact replay object on **every `N>=1`**.

The only lawful reading of the common value type is therefore a total stage-indexed replay object
that produces or replays the object named by its key. The common phrase does not authorize changing
the key's semantic receiver. Otherwise any exact replay object could fill any of the eight keys,
contradicting the closed-field names, JAC-14's outer definition, and its express exact-matrix
rejection.

```text
RETYPE_RULING = WRONG
SELECTOR = RETURNS
```

The bounded-class repair replaces an impossible exhaustive finite **stage list** with a universal
stage rule and reopening trigger. It does not replace bases with subspaces or matrices with
operators.

### 1.2 Three proposed witnesses

| Family | Proposed witness | Governing receiver | Cross-check |
|---|---|---|---|
| FC-01 | membership evaluation for the subspace cut out by `Tr_joint(Delta)=0` and `E_joint(Delta)=Delta` | `delta0_basis_family`; JAC-14 finite-stage basis | **FAIL.** A membership predicate decides a subspace; it neither exhibits nor replays an ordered basis of that subspace. |
| FC-02 | restriction operator `Phi_joint|Delta_0^joint` | `phi_restriction_matrix_family`; JAC-14 exact restriction matrices | **FAIL.** An abstract operator is not its exact matrix. The payload supplies no candidate operand, basis, entries, matrix bytes, or matrix digest. |
| FC-03 | prose saying factorization and inverse obligations are operator identities, with injectivity from `ker=0` | `factorization_and_inverse_family`; JAC-14 exact factorization/inverse | **FAIL.** No factorization, factors, inverse operator, or identities are displayed. Kernel zero proves injectivity only; it does not by itself prove surjectivity or a two-sided inverse on this carrier. |

Thus `WITNESSES = 0/3-LAWFUL`. FC-01 and FC-02 require the selector content already enumerated in
member 07. FC-03 additionally requires actual factorization/inverse data and proof. Merely spelling
the obligations in a procedure field is not replay.

## 2. Candidate and stage totality

### 2.1 Quantifier census

All eight payloads contain the exact same quantifier syntax:

```text
quantifier=all-Phi_joint-in-G_joint@79f0...:[13731,14294)#105f...
```

That is a syntactic improvement over V001's stage-only direction. It is not totality over the
governing receiver, for two independent reasons.

First, the bounded-class type requires replay on every `N>=1`. V002 removes `N` from all eight
quantifier fields. Some procedures or accept strings say “stage-stable,” but a prose accept string
does not restore a missing quantifier. The correct direction has both dimensions:

```text
for every candidate Phi_joint in G_joint, for every N>=1 in the sealed bounded class
```

V001 quantified only over `N`; V002 flips to only `Phi_joint`. It does not bind both.

Second, the cited `G_joint` bytes define a **class**. They are not bytes of an arbitrary member and
do not content-address a candidate operand. A fixed-string check of all eight input fields finds no
`Phi_joint@<digest>` (or equivalent candidate-instance carrier) at all. A generic universal theorem
could avoid enumerating members, but then its proof object and replay rule must be content-addressed.
These payloads instead say “replay the grammar admission predicate.” Assuming a predicate from the
definition of membership is not an independent certificate that the predicate holds at an entered
candidate, and it does not instantiate the exact `Cert_IF_joint(Phi_joint)` or
`Cert_A_joint(Phi_joint)` required by the same grammar.

Accordingly all eight have candidate-looking quantifier text, but none is an exact total replay
object across candidate and stage dimensions.

### 2.2 Nine admission predicates, one-for-one press

| # | `G_joint` admission predicate | Claimed family location | Binding result |
|---:|---|---|---|
| 1 | normal and CPTP | FC-04 | **UNBOUND.** Grammar restatement plus stage digest; no candidate or universal proof carrier. |
| 2 | joint-charge covariance | FC-05 | **UNBOUND / WRONG GROUND.** The booked commutation certificate proves identities for `E_joint` and embeddings, not covariance of a candidate `Phi_joint`. |
| 3 | `Phi_joint E_joint=E_joint Phi_joint` | FC-06 | both symbolic map names appear, but candidate remains unbound; this is the one correct location for the booked `E_joint` comparison object |
| 4 | one normalized mixing invariant state | FC-07 | **UNBOUND / WRONG GROUND.** The fixed-space certificate is for `E_joint`; no candidate mixing proof is supplied. |
| 5 | `ker(Phi_joint|Delta_0^joint)={0}` | FC-03 and FC-08 | grammar predicate restated; no candidate restriction object or exact replay certificate |
| 6 | exact replayable `Cert_IF_joint(Phi_joint)` | none | **MISSING.** No payload consumes or constructs it. |
| 7 | one frozen tag `A` | FC-08 accept text | **UNBOUND.** The grammar span contains a tag slot, not the entered `{BI}` content bytes. |
| 8 | exact replayable `Cert_A_joint(Phi_joint)` | none | **MISSING.** FC-08 states BI equations but supplies no candidate tag certificate. |
| 9 | every finite/cylindrical restriction and limit square commutes | FC-04/05/06 procedure text | **INCOMPLETE.** Formal `N>=1` quantification is absent from all eight; only FC-06 carries the seven-receipt root. |

This is not nine-for-nine binding. Predicates 6 and 8 have no receiver at all; predicates 1, 2, 4,
5, and 9 are restated or grounded at the wrong object rather than replayed for a bound candidate.

```text
TOTALITY = FAIL (FC-01..FC-08 omit N>=1; candidate instance/universal proof carrier absent)
```

## 3. `E_joint` scope

The subject's statement that booked `E_joint` enters only FC-06 is false at the payload bytes:

| Family | `E_joint` carrier |
|---|---|
| FC-01 | direct `EJOINT@67e4...` input and `FIXED-SPACE-CERT@7019...` |
| FC-02/FC-03 | `DELTA0-DEF`, whose second defining condition is `E_joint(Delta)=Delta` |
| FC-05 | `COMMUTATION-CERT@b6bc...`, whose exact booked receiver is `E_joint` and the three embeddings |
| FC-06 | direct `EJOINT@67e4...` plus `EJOINT-BUNDLE@d7ce...` — the lawful comparison-object use |
| FC-07 | `FIXED-SPACE-CERT@7019...`, whose exact conclusion is `Fix(E_joint)=...` |
| FC-08 | `DELTA0-DEF` and FC-01/FC-02 references carry the `E_joint`-defined subspace transitively |

Even the narrow direct-token census returns FC-01 **and** FC-06. The semantic booked-object census
also returns FC-05 and FC-07. FC-06 is the only family whose *criterion* names `E_joint` as the
comparison object, but the payload claim was that the booked object enters only there; it does not.

```text
EJOINT_SCOPE = FC-06-ONLY NOT CONFIRMED
```

## 4. FC-08 operand and BI replay

FC-08 fails three independent binding checks:

1. The cited `G_joint` span is a set grammar, not a content-addressed candidate member. The input
   field has no candidate digest. Writing `CONTENT-ADDRESSED-candidate-operand` in the procedure does
   not create one.
2. The `G_joint` span says only that one tag `A` is frozen in `ANCHOR_TAG_CLASS_ENTRY`; it does not
   contain the entered `{BI}` value or the BI equation. V003's BI template occurs outside the cited
   span, and no completed tag-content or `Cert_A_joint(Phi_joint)` digest appears in FC-08.
3. FC-01's witness decides membership in `Delta_0^joint`. It can receive the kernel/input-
   faithfulness condition, but `Phi_joint(I_C0)=I_C0` is an identity-carrier equation. No sealed byte
   says `I_C0` belongs to `Delta_0^joint`; “replay both on the FC-01 witness” therefore sends the
   unitality equation to the wrong receiver.

The pairing and faithfulness digests are booked, but they cannot supply the missing candidate or tag
certificate. `FC08 = OPERAND-NOT-CONFIRMED`.

## 5. Payload and ordered-root replay

Terminal linefeeds were excluded. Independent replay of every single-line tuple gives exactly seven
pipe-delimited fields and reproduces the subject's byte counts and hashes:

| Family | Bytes | SHA-256 | Tuple form |
|---|---:|---|---|
| FC-01 | 984 | `7db9d060d4b26f17a71dd41325e6d89c54bf310a69b184cc919c0894d58f78b7` | 7/7 fields |
| FC-02 | 815 | `9dc078a4e034794c1b857ab11f7e590652b067de063c9507aebbbe4c29a2590b` | 7/7 fields |
| FC-03 | 785 | `c7de56ccc2e01ba3ea41ab1b3fb56ea433afd3cbf7efcdc4dc7579b39966b794` | 7/7 fields |
| FC-04 | 733 | `452bce537de2ce4cc89799bebc2801e48e528a3fccf6a9a1441ddb074af23234` | 7/7 fields |
| FC-05 | 823 | `a084fb82f2c2f3bb6d64399310a84fd48e34a413c737423cc53d1047089b5b21` | 7/7 fields |
| FC-06 | 970 | `5ce2a1e77eefad97cfedc439f9b1f28bec0d7260f88d4e070cb2b418d1915cfd` | 7/7 fields |
| FC-07 | 917 | `39af59e7e88ea0975598ece02afe9b68ca07a741bd116812ff67b8e69d011d79` | 7/7 fields |
| FC-08 | 1087 | `16cc4fcbde5a2a156a9fbf70ceafe3d5f564846ce3c98b7e70cb318bd1e306ee` | 7/7 fields |

The ordered carrier is 641 bytes and rehashes exactly:

```text
FOR_CLASS_LIST_V002_SHA256 = d77f3be2c2cf5ad0e942f16baec20be84cc4df2f4bea1c40eb25c2a47095f6dc
```

Thus `PAYLOADS = 8/8-REPLAYED + ROOT` is a byte-integrity result only. The carrier's
`quantifier=candidate-total-over-G_joint` label cannot cure the missing stage quantifier or semantic
binding failures.

## 6. Basis-independence and no-coordinate press

At the level of literal payload data, all eight avoid positive basis vectors, orders, matrix entries,
matrix inverses, and an `Omega_Phi` coordinate. FC-07 honors the no-coordinate sentence syntactically.
That yields a narrow `8/8-SYNTACTIC` finding.

It does **not** yield a lawful 8/8 suite. For FC-01 and FC-02, the absence of basis/matrix data is the
reason the named JAC-14 receivers are unfilled. FC-03 also supplies no inverse object. For FC-07, no
coordinate is needed, but a proof of candidate existence/uniqueness/mixing still is; the booked
`E_joint` fixed-space certificate does not provide it.

```text
BASIS_INDEPENDENCE = 8/8-SYNTACTIC; NOT RECEIVER-COMPLETE
```

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the governing V003 JAC-14 law and both inventory variants;
  the sealed G_joint and Delta_0^joint definitions;
  the eight subject payload lines, ordered root, booked E_joint objects, pairing, stage ground,
  and the prior selector finding.

AUTHORED OR SELECTED BY THIS LANE:
  nothing. The selector is returned to its existing route; no basis, order, matrix, inverse,
  candidate, tag, state, proof, or coordinate is supplied here.

SUBSTITUTED:
  nothing. A common value phrase was not substituted for named-field semantics; a subspace was not
  substituted for a basis; an operator was not substituted for its matrix; injectivity was not
  substituted for an inverse; a class grammar was not substituted for a member or theorem proof;
  and an accept label was not substituted for totality.

STOPPED AND ROUTED:
  FC-01..FC-03 return to the selector/data stop; FC-04..FC-08 remain unbooked for the binding and
  totality failures named above. Run 4 does not open.

SCALING WEIGHTS = NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED
```

## 8. Batteries and custody

- **FLATTENING CHECK:** a shared value-type phrase was not flattened over eight differently named
  receivers; inventory repair was not flattened into semantic retyping; membership was not flattened
  into a basis; an operator was not flattened into a matrix; kernel zero was not flattened into an
  inverse; a class definition was not flattened into a content-addressed member; candidate-only
  syntax was not flattened into candidate-and-stage totality; and byte-valid payloads were not
  flattened into booked proofs.
- **F_PLDEC:** only exact bytes, hashes, schema directions, symbolic maps, and proof receivers were
  examined. No physical quantity or measured constant was numerically evaluated.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string, and byte-span/semantic-receiver
  checks covered all three retyping witnesses, both inventory variants, all eight quantifiers and
  inputs, nine grammar predicates, `E_joint` direct and transitive carriers, FC-08's candidate/tag/
  identity receivers, all payload digests, and the ordered root.
- **BLIND:** held. No rank, dimension, ratio, multiplicity, fiber value, candidate output,
  `Omega_Phi` coordinate, or downstream comparison was read or computed.
- **PE-1..PE-14:** pointer-only, zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** byte integrity is confirmed independently; semantic booking is refused
  at the governing receivers.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet run, state solve,
  physical numerical evaluation, or measured-constant comparison was invoked.

## 9. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 3352; scan 0 hits)
RETYPE_RULING = WRONG (JAC-14 outer law and named keys retain basis/matrix receivers; selector returns)
WITNESSES = 0/3-LAWFUL (FC-01 subspace!=basis; FC-02 operator!=matrix; FC-03 no factorization/inverse and ker=0 only injective)
TOTALITY = FAIL (FC-01..FC-08 omit N>=1 and bind neither a candidate instance nor a universal proof carrier)
EJOINT_SCOPE = FC-06-ONLY NOT CONFIRMED (direct FC-01/FC-06; booked derivatives FC-05/FC-07; Delta0 transitive elsewhere)
FC08 = OPERAND-NOT-CONFIRMED (class grammar!=candidate; BI content/certificate absent; identity equation misbound to FC-01)
PAYLOADS = 8/8-REPLAYED + ROOT d77f3be2c2cf5ad0e942f16baec20be84cc4df2f4bea1c40eb25c2a47095f6dc
BASIS_INDEPENDENCE = 8/8-SYNTACTIC; NOT RECEIVER-COMPLETE
VERDICT = FAILURES-NAMED; SELECTOR-RETURNS; RUN-4-DOES-NOT-GO
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
