# Stage-8 T7(iii) Connected Linked-Cluster Majorant Derivation Spec V001

Date: 2026-07-25 (late night; queue D6)

## Status

```text
SPECIFICATION_SEALED_BEFORE_EXECUTION
```

Drafting-lane return only. Nothing in this file is sealed authority. The
construction lane seals it (verifying every hash below at seal time); a
hostile fresh-context lane reviews it pre-execution; execution is
fresh-context from the sealed text. This spec implements the majorant
route memo (Route H) under the principal's standing answers recorded in
the memo's scope-flags section and the gamma/beta disposition. It is the
owner of the named blocker `T7III_MULTICELL_COMPOSITION_AND_LIFT`
(L1 assessment): that blocker is discharged here or not at all.

One theorem, stated once, on the COMPLETED chain per the sealed L2 typing
freeze, uniform over the relayed causal exhaustion, the declared
cellulation families and their common refinements, the complexified
per-cell history polydisc, and per-cell independent source assignments —
with every cell at FULL record time `tau_R = pi/sqrt(2)`. The expansion
parameter is the connection difference, never the record coupling.

## Pinned authorities

All hashes verified by the executor before any computation; drift blocks.

| Role | Path | SHA-256 |
|---|---|---|
| Route memo implemented (Route H; controls; scope flags) | `/Users/bgm/MB Work/alpha_supervision/ROUTE_MEMO_2026-07-25_linked_cluster_majorant.md` | `3e37eaa4d9183d171eb9e4b0dd0ad799a3503cd364c022858ac402396d04834e` |
| Principal disposition (adoption-watch answers; beta; L1/L2 orders) | `/Users/bgm/MB Work/alpha_supervision/DISPOSITION_2026-07-25_gamma_killtest_beta.md` | `143d4b494b5ce5c24e105ac8ddbaa3f7655803ce53b4803d72c1bb4c076ec638` |
| L2 typing freeze (completed chain carries the theorem; fences 1-3) | `STAGE8_T7_L2_INTENSIVE_HESSIAN_TYPING_FREEZE_V001.md` | `cdbacdaf5efcd3a70a6ffa456bc790753e0ff36609c40c67b9bb606b79624746` |
| L1 assessment (Lemma 0 constitutive; named blocker; owner = this spec) | `STAGE8_T7_L1_MULTICELL_LEMMA_ASSESSMENT_V001.md` | `03fadd3634cf8dbc729795c4009012933d3ad836e663c9c509e4e43b7b4883bc` |
| Architecture amendment (A4 obligation relocation — CAR-lane citation) | `STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md` | `8a7f52ffa2500d20ad834b11e3762ed114ee1a201f2fec18bcb119e3c7ead860` |
| CAR-state bridge scope correction (determinant fence, gate item 9) | `STAGE8_ROUTE2_CAR_STATE_BRIDGE_SCOPE_CORRECTION_V001.md` | `4e1282bc800c47441d255e9d9d576958608d955dce15f02969261cd6e601e268` |
| Phase-A spec (A3/A4 pointer construction; `w_lambda`; Gaussian-sum form) | `STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md` | `789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3` |
| Phase-A execution binding (production gating; no relaxation) | `STAGE8_T7_ACTUAL_PARENT_CAR_PHASE_A_EXECUTION_BINDING_V001.md` | `6fa8845a2d30635d0b307c362051d97d80fb084042f695128d1ea469d5403c5a` |
| Sealed closure result (GHZ witness; t^-3 insufficiency; 5-item repair) | `STAGE8_T7_CONNECTED_ANALYTIC_CLOSURE_RESULT_V001.md` | `f891d3afc58e695529d8d569b5d2ba4b853e30c9cfa4296817cb17b60f38b054` |
| Relay necessity result (relay isometries; ready-root supply) | `STAGE8_T7_OPEN_EXHAUSTION_ATTACHMENT_AND_RELAY_NECESSITY_RESULT_V001.md` | `0df721a170f4f4a1ec630775a3ae47b0d793c82e100b326e681030389eaf0694` |
| Relayed-family resolution (causal order; disjoint-cell commutation) | `STAGE8_T7_RELAYED_FAMILY_RESOLUTION_RESULT_V001.md` | `52401eefc3ff84e22f04bbc329a619b830058661db7f969d2d43d167d97d893d` |
| Certified periodic transfer machinery (Route-T parent; NC4 target) | `COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_RESULT_V001.md` | `b54f20ea4782ee9212afd66c941d3902e1a4dfff4da0882ea2432009e0a54a8e` |
| Certified periodic promotion (`|z| <= 1/500`, all volumes; NC4 target) | `COMPLETE_QSPEC_PERIODIC_ZERO_FREE_PROMOTION_RESULT_V001.md` | `506131686e97a27e90fb29d614ed1f74a33ea5ceec941a0805aa6dd7468ae178` |
| T7(i) amplitude authority (`Z_h(A) = a_h(A)/a_h(0)`) | `BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md` | `6e01e5bac60b0f9efdb6f964e2b2ea7727ca46e4cfd66153e8ef70baf240a6eb` |
| `tau_R = pi/sqrt(2)` derivation | `BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md` | `b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f` |
| Monoidality on disjoint cells (T7(iii) first half authority) | `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md` | `451550c3825288d699db35c7289e408e8314ad042450253b58f32722c4ead46b` |
| Battery authority (T7/T13/T15; Frozen Numerics; cellulation pinning) | `STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_AUTHORITY_FABLE_V002.md` | `ddb36cbfe4edfc2a0520e9ae58063295214c64afed5cad5a64e9f311826358f5` |
| Envelope-profile class (`v_A`, `v_B` as sealed) | `STAGE8_T7_ENVELOPE_REALIZATION_COMPARISON_SPEC_V001.md` | `4059eff522426d06d41d2a0542ddf6be309867bd077491d84c6998b2098ede31` |
| ER-fork status (beta: ER-A disclosed premise; fork unresolved) | `STAGE8_T7_ER_FORK_KAPPA_INSENSITIVITY_RESULT_V001.md` | `a087e90a078e571c01b7de360cccfb831eb948f0a77fd618d0dd74e47b49271d` |
| Critical-path scope correction | `STAGE8_T7_CRITICAL_PATH_SCOPE_CORRECTION_V001.md` | `9410ee80ff89beed4e133f75fcdb952d059f3386df12069793b5d60895d15486` |

All cleanroom paths are relative to
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`.
Adjacent `.seal.sha256` files, where they exist, were verified to match the
hashes above at drafting time; the construction lane re-verifies at seal.

## Spec-header scoping (ADOPTED per the memo's recommendation and the principal's flag (c))

1. CONSTANTS ARE CARRIER-INDEX-BLIND. Every constant, radius, decay rate,
   and tolerance in this spec — including `eta` and `epsilon_star` — is an
   explicit functional of the tuple

   ```text
   (||b_D||, tau_R, sea-kernel decay data, p_lambda)
   ```

   ONLY. No constant may depend on the Hermite carrier index `n`, on `ell`,
   on a truncation level, or on any cellulation-family index. A constant
   whose derivation smuggles in a carrier index is a spec violation and
   BLOCKS.
2. ENVELOPE-PROFILE CLASS. The theorem is stated over the sealed
   envelope-profile CLASS containing both sealed branches
   (`v_A(t) = tau_R*32*r(t)^3`; `v_B = 24*tau_R/pi`, per the pinned
   comparison spec), so the result transports across ER-A/ER-B without
   re-execution. Under the adopted beta posture, ER-A is carried as a
   DISCLOSED PREMISE on any downstream consumer; this spec neither selects
   nor closes the ER fork.

## Frozen definitions

### D1 - The object (completed chain; L2 freeze binding)

Per the sealed L2 typing freeze, the theorem is TYPED ON THE COMPLETED
CHAIN. Per cell `c` with record spectral data `(P_lambda, p_lambda,
w_lambda)` of the Phase-A A3 construction
(`p_lambda = <ready|P_lambda|ready>`, `w_lambda = <pointer|P_lambda|ready>`),
the per-cell pointer Kraus operator and pointer kernel are the sealed
Phase-A A4 objects:

```text
K_pointer^(c)(a)      = sum_lambda w_lambda Gamma(u_lambda^(c)(a));

R_pointer^(c)(a_+,a_-) = sum_(mu,lambda) w_mu^* w_lambda
                          Gamma(u_mu^(c)(a_-)^dagger u_lambda^(c)(a_+)).
```

RESTATEMENT OBLIGATION (binding; part of the frozen definitions, not an
executor choice, per the L1 assessment's typing note): every display the
route memo wrote for the exhaustive `Z_K` is restated here with the
POINTER WEIGHTS `w_lambda` in place of the trace weights `p_lambda`.
Consequences that must be stated and respected:

```text
m0 := sum_lambda w_lambda = <pointer|ready> = 0.
```

The completed object has NO UNITALITY ANCHOR: `R_pointer(a,a) != I`, and
the memo's note that "`R_all(a,a) = I` makes the diagonal exact,
discharging the baseline problem by type" holds ONLY for the exhaustive
COMPANION. On the completed chain the normalization anchor is supplied by
the T7(i) ratio

```text
Z_hat_comp(a) := Z_comp(a) / Z_comp(0),
Z_comp(a)     := omega_in( K_pointer^(K)(a_-)^dagger K_pointer^(K)(a_+) )
                 (state evaluation per the A2 relocation),
```

and the baseline nonvanishing

```text
H-B (named hypothesis): Z_comp(0) != 0 on every admitted finite complex
```

is a NAMED HYPOTHESIS of this theorem — it is T7(ii) where it always
lived (L2 freeze fence 3). This spec does not discharge H-B, may not cite
any exhaustive-chain zero-free result toward it (fence 1), and every
logarithm below is the principal branch anchored at the T7(i)-normalized
baseline under H-B. The exhaustive closure
`Z_K = omega_in(W_-^dagger W_+)` is RETAINED as the companion object with
its own sealed roles and appears in this spec only inside negative
controls; substitution in either direction is fenced (F-6).

### D2 - Relayed causal exhaustion

The admitted exhaustions are the relayed causal exhaustions of the sealed
relay-necessity result: cell closure `U_c(tau_R): L_(r_c) -> L_(p_c)`
followed by the typed relay isometry

```text
R_c |p_(c,h)> = |e_(c,h)> tensor |r_(c+1)>
```

(record preserved, ready root supplied), with causal order fixed per the
relayed-family resolution (disjoint cells commute exactly; linear
extensions equivalent). Every cell runs at FULL `tau_R = pi/sqrt(2)`. No
small-record-coupling hypothesis exists anywhere in this spec.

### D3 - Cellulation families, pinned skeleton, common refinements

Per the battery authority's T13 pinning:

```text
skeleton:  the primary hypercubic fixture (pinned skeleton; a regression
           fixture, not proof of universality);
family A:  the cubical bisection sequence;
family B:  the oriented simplicial/barycentric subdivision sequence;
plus:      all common refinements of members of A and B.
```

The theorem's quantifier ranges over the skeleton, A, B, and common
refinements. Pinning the quantifier to any finite list is NOT available
to any lane under any outcome (F-2).

### D4 - Histories, polydisc, sources

Per-cell complexified history parameters `a_c` on the closed polydisc

```text
|a_c| <= epsilon_star   for every cell c,
```

with `epsilon_star` frozen by rule E1 below. Per-cell INDEPENDENT source
assignments (tensor-form ready-root supply; no cross-cell record-color
correlation — this independence is hypothesis H-IND, discharged
structurally by Lemma 0, and is the GHZ-discriminating hypothesis of
NC1). The expansion parameter is the connection difference; the record
coupling is never expanded in.

### D5 - Action-density activity form (REQUIREMENT, not a convenience)

The refinement-stability trap is binding: activities MUST take
action-density form. For an anchored connected cluster `gamma` of cells,
the activity bound must be stated per unit cell 4-volume:

```text
sum_(gamma ni C, |gamma| = n) |Phi_gamma(a)| <= |C|_4 * eta^n
```

with `|C|_4` the cell 4-volume and `eta` a per-unit-4-volume functional
of `(||b_D||, tau_R, sea-kernel decay data, p_lambda)` only. Any activity
normalization that is per-CELL rather than per-4-volume breaks uniformity
under common refinement and is a spec violation (its executable failure
is NC6). The re-aggregation identity — that refining a cell re-partitions
`|C|_4` exactly and the majorant re-aggregates without loss — must be
proved, not assumed.

### E1 - Frozen derivation rule for epsilon_star (outcome-blind)

`epsilon_star` is DERIVED, before any verdict-bearing computation, by the
following frozen rule, and sealed in a pre-execution constants file:

1. Derive the certified per-unit-4-volume activity majorant
   `eta(epsilon)` as an explicit monotone functional
   `F(epsilon; ||b_D||, tau_R, sea-kernel decay data, p_lambda)` with
   certified outward enclosures (Frozen Numerics: exact rational or exact
   symbolic arithmetic; interval arithmetic over exact rationals with
   outward rounding for every transcendental bound; implementation named
   and hashed at sealing).
2. On the frozen dyadic grid `epsilon in {2^-k : k = 1, ..., 64}`, take

   ```text
   epsilon_star := max { 2^-k : eta(2^-k) <= 1/2 certified }.
   ```

   The threshold `1/2` is frozen here, outcome-blind; it is the
   Kotecky-Preiss-type convergence margin used by O3 and O5.
3. If no grid point qualifies, the verdict is
   `T7III_BLOCKED (witness: EPSILON_STAR_VACUOUS)`.
4. `epsilon_star` may NEVER be tuned by any output — not by the witness
   W1, not by any control, not by any downstream consumer (memo
   dependency-verdict clause). Re-derivation requires an append-only
   successor spec citing this rule.

No measured constant appears anywhere in this spec, in its constants
file, or in any admissible execution artifact.

## Target theorem (frozen statement)

Under hypotheses H-B (named, not discharged here) and H-IND (discharged
by Lemma 0), with `epsilon_star` and `eta = eta(epsilon_star)` frozen per
E1:

```text
THEOREM (T7(iii) connected linked-cluster majorant, completed chain).
For every relayed causal exhaustion K (D2), every cellulation X drawn
from the pinned skeleton, families A and B, and their common refinements
(D3), and every per-cell independent source assignment (D4), with every
cell at full tau_R = pi/sqrt(2):

(1) [Composition]  the completed-chain functional composes cell-locally
    per Lemma 0, with per-cell independent record-color pairs and no
    cross-cell record-color correlation;

(2) [Cluster expansion + majorant]  on |a_c| <= epsilon_star,

      -Log Z_hat_comp^(K,X)(a) = sum_(gamma anchored connected
                                      clusters of X) Phi_gamma(a),

    absolutely convergent, activities in action-density form (D5), with

      sum_(gamma ni C, |gamma| = n) |Phi_gamma(a)| <= |C|_4 * eta^n,
      eta <= 1/2 certified,

    uniformly in K, X (including common refinements), and the source
    assignment; all constants functionals of (||b_D||, tau_R, sea-kernel
    decay data, p_lambda) only, carrier-index-blind, stated over the
    envelope-profile class (spec-header scoping);

(3) [Differentiated series]  the series of first and second a-derivatives
    of the activities converges absolutely and uniformly on the same
    polydisc with the same form of bound (O5) — convergence of the
    undifferentiated series does not imply this and may not be cited
    for it;

(4) [Corollaries, inside this same theorem]  (i) the intensive limit
    lim |X|_4^-1 * (-Log Z_hat_comp^(K,X)) exists and is cellulation-
    independent across the quantifier of (2); (ii) boundary corrections
    are subextensive with an explicit certified rate; (iii) the first two
    a-derivatives converge to the derivatives of the limit (the
    interchange used by T7(iv) — stated here, consumed there).
```

The theorem quantifies over the ENVELOPE-PROFILE CLASS of D5/spec-header;
it therefore transports across ER-A/ER-B without re-execution. It claims
nothing about kappa_record, T7(ii)/H-B, T7(iv), or any coupling.

## Obligations (numbered; Lemma 0 first, per the memo's mandate)

**O1 — LEMMA 0 (completed-chain relayed multi-cell composition
identity).** The first numbered lemma. Prove exactly, for the relayed
exhaustion (D2) and the completed-chain pointer objects (D1):

```text
K_pointer^(K)(a) = prod_c^(relay-ordered) K_pointer^(c)(a_c)
```

and hence

```text
R_comp^(K)(a_+,a_-)
  = sum_((mu_c),(lambda_c)) prod_c w_(mu_c)^* w_(lambda_c)
      Gamma( u_(mu_c)^(c)(a_-)^dagger u_(lambda_c)^(c)(a_+) ),
```

with (i) the record-side relay isometries cancelling exactly between the
CTP branches (`R_c^dagger R_c = I` on the record chain), (ii) the
tensor-form ready-root supply yielding per-cell INDEPENDENT color pairs
`(mu_c, lambda_c)` — no cross-cell record-color correlation (this IS
H-IND's discharge), and (iii) the `m0 = 0` restatement of D1 respected
throughout (no step may invoke a unitality anchor). This obligation is
the discharge site of the named blocker
`T7III_MULTICELL_COMPOSITION_AND_LIFT`; its failure is a program-level
block on T7(iii), T7(iv), and kappa_record, reported as such.

**O2 — Action-density activity construction.** Construct the per-cell
activities `Phi_C` and cluster activities `Phi_gamma` in the D5
action-density form, and prove the re-aggregation identity under one step
of common refinement (exactly; the executable control is NC6).

**O3 — Route T stage (certified transfer-operator gap on the pinned
skeleton).** Generalize the sealed periodic machinery (the corpus's own
named successor step; NC4 is its regression) to the anchored
transfer operator of the relayed completed chain on the PINNED HYPERCUBIC
SKELETON only:

```text
TT1: construct the anchored transfer operator (exact representation,
     content-addressed);
TT2: certify spectral isolation of the leading mode (named certificate:
     outward-enclosed gap bound; the periodic machinery's invariant-graph
     method is the template);
TT3: convert certified isolation into certified exponential clustering
     on the skeleton, hence the majorant of theorem clause (2) in a
     certified disk |a| <= epsilon_star on the skeleton.
```

Every TT-failure is a NAMED failed certificate (graceful-block ranking:
T degrades best); no TT certificate quantifies beyond the skeleton.

**O4 — Route Q stage (determinant-locality transport of the
quantifier).** Transport the skeleton majorant across families A, B, and
common refinements via the sea-compressed determinant representation with
CELLULATION-BLIND constants:

```text
M1: per record-color pair, write each term of the O1 sum as a
    determinant over ran C — each term IS a single second quantization
    Gamma(u_mu^dagger u_lambda) per the sealed Phase-A A4 Gaussian-sum
    representation. COMPLIANCE CITATION (i): this satisfies the
    determinant fence TERMWISE — STAGE8_ROUTE2_CAR_STATE_BRIDGE_SCOPE_
    CORRECTION_V001.md gate item 9 (4e1282bc...) forbids a determinant
    shortcut unless the operator is first proved to be the corresponding
    second-quantized Gaussian operator; each single term is exactly
    that, and no single postselected determinant replaces the sum.
    COMPLIANCE CITATION (ii): the CAR-lane fence does not bar the
    relocated complete-Q_spec obligation — the scalar obligations sit on
    state-evaluated scalars per the A4 obligation relocation of
    STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_
    AMENDMENT_V001.md (8a7f52ff...). Both citations are mandatory in the
    result artifact.
M2: per-cell activity bounds in D5 action-density form, constants
    functionals of (||b_D||, tau_R, sea-kernel decay data, p_lambda)
    only (carrier-index-blind);
M3: colored Kotecky-Preiss-type convergence at eta(epsilon_star) <= 1/2
    (threshold frozen in E1), uniform over the D3 quantifier;
M4: see O5 (stated separately because it is separately falsifiable).
```

Known failure mode to be confronted head-on, not absorbed: IR marginality
of the massless sea kernel (`|x|^-3` class decay) under refinement — the
same infrared structure already named as T7's true bottom. If M2/M3 fail
there, the verdict is BLOCKED with that named witness.

**O5 — Uniform absolute convergence of the DIFFERENTIATED cluster
series.** Its own numbered obligation (memo B-L3/M4; L1 assessment):
prove theorem clause (3) directly. No artifact may infer it from
clause (2).

**O6 — Item-4 corollaries inside the same theorem.** Prove theorem
clause (4)(i)-(iii) as corollaries of O1-O5 within this spec — not as a
separate later theorem. Monoidality on disjoint cells enters via the
pinned monoidal-extensivity authority; the subextensive boundary rate
must be exhibited with its certified form.

**O7 — Refinement-intertwiner lemma (THE CLIFF; predeclared honest
outcome).** The analytic lemma that lets the O3 certificates plus the O4
constants quantify over the infinite refinement families: an intertwiner
transporting the certified skeleton gap across common refinements.
Numeric certificates cannot quantify over infinitely many refinements;
this lemma may not exist. Predeclared handling: if O7 cannot be derived,
the spec's OWN honest outcome is the SCOPE-RESTRICTION verdict below,
escalated to Brian framed as an Axis-3-style scope decision (it changes
what T7(iii)/T13 can ever claim) — NEVER a silent pinning of the
cellulation family, never a lemma-shaped restriction slipped into a
result artifact (principal's flag (b)).

**O8 — The seven negative controls (all mandatory; any misbehavior
blocks).**

```text
NC1 (GHZ refusal with the named discriminating hypothesis). The
    GHZ/coherent witness of the sealed closure result — preparation
    (|0...0> + |1...1>)/sqrt(2), Z_N(A) = cos(N tau_R A), first zero
    pi/(2 N tau_R) — is fed to the pipeline. PASS CONDITION: the
    pipeline REFUSES by naming the violated hypothesis H-IND (per-cell
    independent record colors; GHZ is the perfectly-correlated-color
    limit). A numeric failure, a silent acceptance, or a refusal citing
    any other ground is a control failure.
NC2 (correlated-color variant). Replace the per-cell independent color
    sums in Lemma 0 by a single shared color across cells. The
    composition identity must DETECTABLY fail (nonzero cross-cell
    record-color correlation witness, certified outward enclosure
    excluding zero). Silence is a control failure.
NC3 (t^-3 insufficiency). An activity bound built ONLY from the sealed
    one-root temporal-return t^-3 results must fail the E1/M3 criterion
    (named witness), reproducing the sealed closure result's finding
    that those results cannot discharge the linked-cluster obligation.
NC4 (periodic positive regression). Specialized to the sealed
    period-two regulator, the machinery must REPRODUCE, within certified
    outward enclosures and without weakening, the certified periodic
    results: zero-freeness and uniform thermodynamic density on
    |z| <= 1/500 (b54f20ea..., 50613168...). Any degradation blocks.
NC5 (detuned relay: record-erasing advance). Replace the D2 relay by
    the record-erasing advance (ready root supplied, completed record
    erased). Lemma 0's completed-chain identity must DETECTABLY break
    (predeclared witness class: the erased variant restores an
    exhaustive-type anchor / collapses the pointer-weight structure
    toward the p_lambda form). The pipeline must detect and REFUSE.
NC6 (refinement-stability executable control). A deliberately
    NON-action-density activity assignment (per-cell constant activity)
    must FAIL uniformity under one common refinement of family-A and
    family-B members — the executable form of the D5 trap, with the
    failure exhibited by certified enclosure.
NC7 (no-target fence). No kappa_record value, no alpha, no function of
    either, and no target-adjacent numeric appears in any artifact of
    this spec's execution. The evaluator runs the battery's T15-style
    transform-grep over every artifact and FAILS the gate on any hit.
    The lane's numerical outputs are exactly: eta(epsilon_star),
    epsilon_star, the certified constants, the certificates, the
    control witnesses, and W1.
```

**O9 — Preregistered sharpening witness W1 (computed either way).** The
two-cell connected cumulant at full `tau_R` on the relayed completed
chain:

```text
W1 := -Log Z_hat_comp^(12)(a) + Log Z_hat_comp^(1)(a)
      + Log Z_hat_comp^(2)(a),
      evaluated with certified outward enclosures at the frozen history
      a = (7/100, -11/100) per-cell pair convention of the Phase-A
      binding, |a| <= epsilon_star permitting; else at the largest
      frozen dyadic history inside the polydisc.
```

W1 is computed under EVERY verdict arm (DERIVED, SCOPE_RESTRICTED, and
BLOCKED wherever O1 survives), so that a Route-Q failure FALSIFIES rather
than merely returning inconclusive: W1 outside the `n = 2` majorant bound
`|C|_4 * eta^2` refutes the constants; W1 inside it sharpens them. W1 is
not a target quantity and is exempt from no numeric fence only as listed
in NC7.

## Predeclared verdicts (frozen; not revisable)

```text
T7III_MAJORANT_DERIVED           iff O1-O6 discharge with certified
                                 constants at the frozen epsilon_star,
                                 O7 derived, all seven controls behave,
                                 W1 computed and inside its bound
                                 => theorem sealed at epsilon_star with
                                    certified (eta, constants), under
                                    named hypothesis H-B;
T7III_SCOPE_RESTRICTED_ESCALATE  iff O1-O6 certify on the pinned
                                 skeleton (TT1-TT3, M1-M3, O5) but O7
                                 fails or cannot be derived
                                 => BLOCKING SCOPE RESTRICTION escalated
                                    to Brian (Axis-3 style) as a scope
                                    DECISION about what T7(iii)/T13 can
                                    claim; no lane pins the family; no
                                    result artifact narrows the
                                    quantifier;
T7III_BLOCKED                    on any other obligation failure, any
                                 control misbehavior, any authority
                                 drift, EPSILON_STAR_VACUOUS, the IR
                                 sea-kernel witness, or any reach toward
                                 a clustering adoption — always with a
                                 NAMED witness; failures preserved,
                                 never repaired.
```

## Frozen predictions (calibration record; err toward blocks)

Recorded per the standing calibration note that this lane's family of
predictions has run one level optimistic twice; these predictions are
deliberately conservative and are not revisable after seal.

```text
P1: the Route-T stage certifies on the pinned hypercubic skeleton —
    TT1-TT3 return named certificates and a nonvacuous epsilon_star
    (E1 grid point with eta <= 1/2).
P2: the refinement-intertwiner (O7) is the likely honest block: the
    predicted overall outcome is T7III_SCOPE_RESTRICTED_ESCALATE, not
    T7III_MAJORANT_DERIVED.
P3: W1 is nonzero and inside the n = 2 majorant bound.
P4: NC1 refuses by H-IND; NC2 and NC5 detectably fail; NC4 reproduces
    the certified periodic results; NC6 exhibits the trap.
```

## Fences

```text
F-1 CLUSTERING ADOPTION FENCE (principal's flag (a), standing REFUSAL):
    if any route, lemma, or repair reaches for
    BUFFERED_EXHAUSTION_CLUSTERING or ANY clustering axiom or principle,
    the gate BLOCKS immediately and escalates to Brian. Adoption is
    Brian-only; no lane may adopt to rescue Route Q or anything else.
F-2 O7 failure is handled ONLY by T7III_SCOPE_RESTRICTED_ESCALATE —
    never a silent pinning, never a lemma-shaped restriction (flag (b)).
F-3 L2 freeze fences 1-3 bind verbatim: exhaustive zero-free results
    never cited against completed-chain obligations; completed Hessian
    never checked against the exhaustive three-way agreement; no
    unitality-anchored wording inherited — the D1 restatement (m0 = 0,
    T7(i) anchor, named H-B) governs.
F-4 epsilon_star per E1 only; never tuned by any output; no measured
    constants anywhere; Frozen Numerics culture binds (exact rational /
    exact symbolic arithmetic; certified outward interval enclosures;
    implementations named and hashed at sealing).
F-5 No-target fence NC7 with evaluator transform-grep; verdict authority
    rests with the hashed evaluator alone — no in-execution PASS string
    has authority.
F-6 The exhaustive closure Z_K is companion only; substitution between
    completed and exhaustive objects, in either direction, blocks.
F-7 Interface caveat (memo dependency verdict): NO hard Phase-A/B
    production dependency — this theorem is about sealed definitions and
    may start now; a representation-level Phase-A BLOCK demotes it to a
    statement about the abstract sealed form, recorded as a demotion,
    not silently.
F-8 Fresh-context execution from the sealed text; hostile pre-execution
    review; an independent fresh lane re-derives the O3 certificates and
    the W1 enclosure from this spec without the primary's worksheets
    before any result seals; failures preserved, never repaired.
```

## Protected status

```text
T7III_multicell_composition_and_lift_discharged = false
lemma0_completed_chain_composition_derived = false
action_density_activity_form_derived = false
routeT_skeleton_gap_certified = false
routeQ_determinant_locality_transport_derived = false
differentiated_series_uniform_convergence_proved = false
refinement_intertwiner_derived = false
epsilon_star_frozen = false
connected_linked_cluster_density_proved = false
volume_uniform_zero_free_neighborhood_proved = false
Duhamel_intensive_Hessian_equality_proved = false
ER_fork_closed = false
kappa_record_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
