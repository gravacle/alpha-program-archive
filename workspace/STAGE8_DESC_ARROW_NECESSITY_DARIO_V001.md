CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits (empty byte range -- no possible hit)
VERDICT_BEARING_SET = exactly the 20 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = ARROW NECESSITY -- THE PRINCIPAL'S QUESTION, ANSWERED BOTH WAYS.
CITATION_RULE = CLOSURE_MEMBER_CITATION_RULE_V001, specified at
  workspace/STAGE8_DESC_B03_V003_CHECK_CODEX2_V001.md section 6 and ordered by member 01.  EVERY
  published span carries CANONICAL PATH + FULL SOURCE SHA-256 + HALF-OPEN [a,b) + FULL SPAN SHA-256.
  Member numbers here are closure-local convenience ONLY; the tuple is the authority, and no
  consumer needs to rebind a number through its own closure.  This is the first artifact of this
  lane written under that rule, and it is written under it in full.
DIGEST_RULE = every digest and offset COMPUTED by member 20 and quoted from its output.
SPAN_CONVENTION = DECLARED.  Raw bytes, half-open [a,b), no decoding, no newline normalisation.
  FIXED = a literal [a,b) re-verifying an upstream pin; mismatch is a REFUSAL, never a correction.
  ANCHOR = a UNIQUE start anchor through the last byte of a named end anchor; a second occurrence
  of the start anchor is a REFUSAL.  10 of 18 spans are FIXED; 10/10 MATCH.
GENERATOR_REFUSAL = member 20 emits NOTHING on R1 unreadable member, R2 FIXED-pin mismatch,
  R3 absent/AMBIGUOUS start anchor, R4 absent end anchor (Q-920/Q-924).
SELF_CITATION_BAR = NO span is drawn from any DARIO-lane artifact of relays 1034 or 1037.  The
  hypothetical arrows displayed in STAGE8_DESC_B03_DARIO_V003.md are this lane's own text, not
  record witnesses -- member 04 section 3 ruled the same, and the generator excludes them by
  construction.  Member 03 is closed for carriage only.
SUBJECT_READ_RULE = member 18 (overlay V002) and member 19 (DoR-013) govern any read of the
  GEN_OMEGA proposal; no span of it is drawn here.
NOTHING PROMOTED = i_rec is EXAMINED, NOT PROMOTED.  No entry is made, filled, or recommended.
ALL_RESULTS = CLAIMED until the opposite-lane check.
CLOSURE_END_BYTE = 00006322
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1037_ARROW_NECESSITY_DARIO_V001.md` | `c2b8db58105a86d7adf42c1af1a24cf5ae41ab872a7dd5134d88f8f0e94b7051` | the assignment |
| 02 | `relay_inbox/RELAY_PASTE_1033_FRESH_SESSION_BOOTSTRAP_DARIO_V004.md` | `be4f715dfaace27c8f118bc758ae35bb4fe1b5279967b8583e352881e1eee108` | the bootstrap; the SYMBOLIC LINE |
| 03 | `workspace/STAGE8_DESC_B03_DARIO_V003.md` | `2b3a99bc1c5865aaff7f7c994ce1f5ec6f06f861f2ffd360920133dff9cc53e4` | the 1034 derivation — **carriage only; NO span drawn from it** |
| 04 | `workspace/STAGE8_DESC_B03_V003_CHECK_CODEX2_V001.md` | `2b251a781180f2e3446e439ea20a24a9b9e815ae2b857332db1e910eca243044` | the check: STOPS CONFIRMED; the citation rule |
| 05 | `workspace/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1` | **THE INSTRUMENT carrying the identification clause** |
| 06 | `workspace/STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md` | `4440af4f8d4a75c7e4b026d4fd901e4e290676f928275897bb55ce803b4c03db` | the instrument's cited ground |
| 07 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | **THE LAW SIDE: P0–P7, and its single `C0` mention** |
| 08 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | v004's `rho_pre` clause |
| 09 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md` | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2` | **L5: the `d_U2` descent-witness signature** |
| 10 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` | L4: the U2 role content |
| 11 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | **`A_C0`'s typing, `B`, `res_B`, and the K4 handoff** |
| 12 | `workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | the port signatures and their execution order |
| 13 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **WHERE `B ≅ C(Y)` IS FORCED — the K4 receiver, record-side** |
| 14 | `supervision/DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md` | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | DoR-008 |
| 15 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 16 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 17 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 18 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the overlay of record; the read rule |
| 19 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification, by digest |
| 20 | `workspace/build_arrow_necessity_pins_v001.py` | `cb95505e42541787da2bbbe50d51a59096f13bf2b13318ca3316589f9f69de6f` | the pin generator; declared inputs are exactly members 01–20 |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION.  NO MEASURED-CONSTANT COMPARISON.
NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.  NO FREEZE.  omega_phys UNTOUCHED.
NOTHING AUTHORED, ADOPTED, IDENTIFIED, SEALED, ENTERED, OR PROMOTED.  NO CANDIDATE CONSIDERED.
NO SUBAGENT DELEGATION.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — ARROW NECESSITY — DARIO LANE — V001
## RELAY 1037 — `[PLAN:DESC-27]` — THE ANSWER IS BOTH, AND THE TWO HALVES GO OPPOSITE WAYS

Date: 2026-08-11
Status: **THE PRINCIPAL IS RIGHT ABOUT THE CLAUSE AND THE ARROW SURVIVES ANYWAY — those are two
findings, not one, and the relay's own stakes sentence asks for both plainly. Q1: the requirement
that `A_C0` be identified with or embedded into `A_SRF_CTP` is INSTRUMENT-AUTHORED. It lives in a
field-table row of an entry-completion instrument that says of itself "NO ENTRY IS MADE HERE"; its
purpose is to fill the `joint i_src` field of a decision instance. The law side never asks for it:
the producer spec contains ZERO occurrences of `A_C0`, and exactly ONE of `C0` — in its REFUTATION
CONDITIONS, as the ruling that would supersede the producer. And the instrument's own closure types
the producer spec itself as the "third wrong-codomain `i_src` declaration": the instrument judges
the record's codomain wrong by the instrument's target. That is the StatePort pattern exactly, and
this is the sixth frame artifact. Q2: BUT THE SRF-NATIVE ROUTE DOES NOT CLOSE. Five of six legs are
SRF-native; L5 is not, because `d_U2 : (B0_candidate,C0) -> U2` names `C0` in its own domain. And
K4's receiver is decisive: `omega_hist` restricts through `i_B` into `B := A_F_CTP` — the FIELD/
history factor, forced record-side at TASK2D as `B ≅ C(Y)` with `State(B)` the regular Borel
probability measures. `i_rec`'s domain is the RECORD sector. **Sealing `i_rec` would not touch `B`.**
SO: THE CARRIER IDENTIFICATION DISSOLVES AS FRAME; A FIELD-FACTOR ARROW REMAINS A TRUE REQUIRE — for
a different reason than the one that was on the table, and one arrow narrower.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 1. The two answers, stated before their traces

The principal's question is why a field-factor arrow is assumed to be needed **at the record level**
at all. The honest answer has two halves that pull in opposite directions, and stating them together
before the evidence is the only way to keep either from being read as the whole.

```text
Q1  THE CARRIER IDENTIFICATION          INSTRUMENT-AUTHORED.  The record's demand never mentions
                                        A_C0.  The clause is an entry-schema field.

Q2  THE SRF-NATIVE ROUTE                DOES NOT CLOSE.  5/6 legs are SRF-native; L5 names C0; and
                                        K4's omega_hist restricts into B, the field/history factor,
                                        which the SRF side reaches by nothing.

NET  THE ARROW                          TRUE-REQUIRE, RELOCATED AND NARROWED.  Not "identify the
                                        carriers" -- that falls.  ONE arrow on the history factor,
                                        grounded in K4's own receiver rather than in the schema.
```

**What would have been the flattering result is the one I did not get.** Q1 alone points at
dissolution — the sixth frame artifact, the field-factor arrow gone, the section's obstruction
shrinking to `i_rec`'s disposition. Q2 is what stops that, and it stops it at a span.

---

## 2. QUESTION 1 — THE PROVENANCE OF THE IDENTIFICATION REQUIREMENT [PROVABLE]

### 2.1 The method

The same test that dissolved the StatePort: take the clause, trace **forward** to everything that
cites it, trace **backward** to each citing span's own ground, and then ask the law side directly
whether any demand leg, producer port, or theorem forces the identification. A requirement is
record-derived only if the backward trace terminates in a law-side span; it is instrument-authored
if it terminates in the instrument's own schema.

### 2.2 The clause, and what it is a field of

```text
CITATION  workspace/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md
          source af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1
          [10322,10545)  FIXED
          span   602ab0bff8d0d3d442271fe0850a6141f15fc1f139acb5b0311f13f63eaa26ff
```

> `| joint i_src | **EMPTY** | member 05 §3.4; all three declarations land in `A_SRF_CTP`, while
> member 09 declares `A_C0` | author the required carrier identification and typed embedding, or
> author a new typed embedding |`

**It is a row of a field table.** The rows around it are `entered_pairing_sha256`,
`joint_superselection_map`, `physical_price`, `equivalence_scope`, `fiber_label`, `declared I_A
object digest`, `P_ch_sha256` — the fields of a decision instance awaiting principal entry. The
"required" in "the required carrier identification" is **required to fill this field**, and the
artifact says what it is:

```text
CITATION  workspace/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md
          source af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1
          [5062,5279)  ANCHOR
          span   a2c49549ad6204e3737986ac8d5539bbd770c7208d415489cff137db69aa76cd
```

> `Status: **DRAFT FOR PRINCIPAL ENTRY — NO ENTRY IS MADE HERE**.`
> `Scope: the eight genuine decisions, including joint_superselection_map, plus the two-site`
> `P_ch_sha256 pointer choice: nine principal fields total.`

### 2.3 The backward trace, one step at a time

The clause names its own ground: *member 05 §3.4* and *member 09*. Resolved through the
instrument's own closure table, and re-cited here by tuple rather than by number:

**Ground 1 — the candidate survey.**

```text
CITATION  workspace/STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md
          source 4440af4f8d4a75c7e4b026d4fd901e4e290676f928275897bb55ce803b4c03db
          [7028,7663)  FIXED
          span   43f3129a8b5059caa7979ab9ab0274296ec49c0cbd995c1785ab66d087af764d
```

Three sealed `i_src` declarations, **all** with codomain `A_SRF_CTP`, and the survey's own
conclusion that pointing at any of them *"requires deciding that `A_SRF_CTP` and `A_C0` are the same
carrier — an identification, not a transcription."* The survey states the gap; it does not create a
demand for closing it. Its framing is explicit about whose target `A_C0` is:

```text
CITATION  workspace/STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md
          source 4440af4f8d4a75c7e4b026d4fd901e4e290676f928275897bb55ce803b4c03db
          [21360,21526)  ANCHOR
          span   f20fa601d864b986c6d8329027eea61f250f393e6349e0b1f3095079d68dadd5
```

> `three sealed declarations exist but ALL have codomain A_SRF_CTP rather than A_C0, and the`
> `state-algebra map derives its two sibling arrows while NOT deriving this one`

*"rather than `A_C0`"* — the deviation is measured **from `A_C0`**, and `A_C0` is the schema's
carrier, not the demand's.

**Ground 2 — the state-algebra map**, which declares `A_C0` as the joint tensor presentation. Its own
ground is the sealed C0 assembly, and everything built on that presentation carries the mark:

```text
CITATION  supervision/DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md
          source d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19
          [1993,2227)  ANCHOR
          span   fc1a4e4ab26f0824aca508e084d43e1c3bc50a9b11365b9480ca5e0e8206650e
```

> `Everything built on this presentation is TYPE-P | premises: DoR-008, propagating downstream.`

**The trace terminates.** One branch ends at the record's own three `i_src` declarations — which land
in `A_SRF_CTP`, i.e. **away from** `A_C0`. The other ends at a ratified premise presentation. Neither
branch is a demand leg, a producer port, or a theorem requiring the two carriers to be joined.

### 2.4 The deciding sentence, and then the law side tested directly

The instrument's own closure types its sources:

```text
CITATION  workspace/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md
          source af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1
          [2431,2980)  ANCHOR
          span   ff3e4db75fd2cd4d7359380677cddc81e3650cfe70196416a064cb309247b010
```

> `| 13 | STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md | … | first wrong-codomain i_src declaration |`
> `| 14 | STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md | … | second wrong-codomain i_src declaration |`
> `| 15 | STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md | … | third wrong-codomain i_src declaration |`

**Member 15 is the producer spec — the law side itself.** The instrument types the record's own
P0–P7 artifact as a *wrong-codomain declaration*. Nothing is wrong with its codomain except that it
is not the instrument's. **That single table row is the whole provenance answer**, and it is the
StatePort pattern verbatim: an instrument sets a target, then reads the record's own spans as
deviations from it.

Tested against the law side directly, over its full bytes:

```text
occurrences of `A_C0` in workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md   0
occurrences of `C0`    in the same file                                                 1
```

and that single occurrence is this:

```text
CITATION  workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
          source e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706
          [18296,18480)  ANCHOR
          span   cf696d88abeacea1019aa129c6b686e7d0e21027d5e8f854c5b7fda0e9adced7
```

> `4. a principal ruling that the rank-1 object should be restricted to the narrow`
> `   C0 carrier extension, excluding state/effects/quotient/measure/dynamics from`
> `   the producer verdict.`

**It is item 4 of the producer's REFUTATION CONDITIONS.** The law side names `C0` exactly once, and
names it as the ruling that would *supersede* the producer — a rival narrowing, not a partner to be
joined to. The demand's own carrier is named and it is the only one named:

```text
CITATION  workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
          source e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706
          [8085,8283)  FIXED   span 375dd96a7b7c3806c063075f7346685efa8fbd8c1eddab9bbb19c5e723157d6d
          [8954,9569)  FIXED   span 8917c67f455bb0b152950c1931891311c3c96029c1ba4100219370038198dbb0
```

P1 names `A_SRF_CTP`; P5 places `rho_pre` on the completed object. Neither mentions `A_C0`.

### 2.5 Q1 VERDICT

```text
IDENTIFICATION = INSTRUMENT-AUTHORED

FORWARD TRACE   the clause is cited by the B03 V002 check and by the 1034 derivation, in both cases
                as the requirement of record.  No law-side artifact cites it at all.
BACKWARD TRACE  the clause -> the candidate survey (which measures deviation FROM A_C0) and the
                state-algebra map (TYPE-P | premises: DoR-008).  Terminates in a schema target and a
                ratified premise; never in a demand leg, a producer port, or a theorem.
LAW-SIDE TEST   P0-P7: zero `A_C0`; one `C0`, and it is REFUTATION CONDITION 4.
DECIDER         the instrument's own closure types the producer spec as a "wrong-codomain i_src
                declaration" -- the record's codomain judged wrong by the instrument's target.
```

### 2.6 What this does **not** dissolve — stated so the finding is not over-read

- **It does not make `A_C0` illegitimate.** DoR-008 ratified the presentation and `C0_prop` is
  available for use. An entry schema is entitled to its own carrier.
- **It does not void the entry instrument or its field table.** Nothing here touches its status.
- **It does not follow that no field-factor arrow is needed.** That is Q2's question, and Q2 answers
  it the other way. A requirement can have a bad ground cited and a good ground uncited — which is
  exactly what this section and the next, together, find.

---

## 3. QUESTION 2 — THE SRF-NATIVE ROUTE [PROVABLE]

### 3.1 (a) The six legs — 5/6 live on `A_SRF_CTP` alone

| leg | receiver | on `A_SRF_CTP` alone? |
|---|---|---|
| **L1** positive `rho_pre` | producer P5; v004's clause | **YES** — "on the completed object" / "the full source-record-field Hilbert space" |
| **L2** normalized, trace-class, unit trace | same | **YES** — same object |
| **L3** on the completed object | producer P1; v004 | **YES** — this leg *is* `A_SRF_CTP` |
| **L4** effects, domains, contacts, evolution, inclusive identity | the U2 role content | **YES** — the role content names no carrier but the completed object |
| **L5** one `d_U2` descent witness | the substitute-admissibility adjudication | **NO — it names `C0` in its own domain** |
| **L6** common-origin delivery, no supplementation | producer P5 | **YES** — "the same microscopic source as P0" |

The exception, at its tuple:

```text
CITATION  workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md
          source 3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2
          [6266,6883)  FIXED
          span   e6093b0f48c11d3f05d3c6fd3692b6ea2c4b320746bb92eebe72ef28ed697efe
```

> `U2 = microscopic action/evolution, positive normalized pre-state, inclusive identity,`
> `     admitted record effects, action/source contact rules, common domains.`
> … `It also requires action/source contact rules and the descent witness`
> `d_U2 : (B0_candidate,C0) -> U2.`

**Read exactly.** The U2 *role content* is carrier-neutral and SRF-compatible — that is why L4 passes.
What names `C0` is the *descent witness's domain*. So L5 requires that U2's content be shown to
descend from a pair one of whose components is `C0`. **That is a provenance requirement, not an
algebra arrow**, and I will not flatten one into the other: `d_U2` demands a witness, not a
homomorphism. But it is the record's own demand leg and it does name `C0`, so the SRF-native route
cannot claim six of six.

```text
LEGS_ON_SRF = 5/6   (exception: L5, at the span above)
```

### 3.2 (b) The pre-state through `i_src` — the direction is wrong, and the record says so

`i_src : A_src -> A_SRF_CTP` is the one live sealed arrow into the target. States pull back along a
`*`-homomorphism, so `i_src` induces `State(A_SRF_CTP) -> State(A_src)`: **restriction**. Building
`rho_pre` *on* `A_SRF_CTP` *from* source-sector data is **extension**, the opposite direction, and
extension along an inclusion is exactly the operation the record has already ruled non-unique — a
marginal does not determine the other factor.

This is not my inference alone. The port specification fixes the execution order:

```text
CITATION  workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
          source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
          [37255,37521)  ANCHOR
          span   a5900fd81fb9040b370f39a59fc7314b2302dbbd9ce5c3b1cac93cc3fafd1ffb
```

> `The first draft placed P2 before P5 and deferred the required state-restriction witness. That did`
> `not supply the sealed P2 structure. The completed P2 contract must execute after P5 and consume`
> `c5.rho_pre; this is a specification repair, not a physical derivation:`

**P2 consumes `rho_pre`. It does not produce it.** `i_src`'s role in the record is to carry the
coherence test — that the restricted state reproduces the Q-41 quasifree/GNS data — against a
pre-state already supplied by P5. A route that tried to build the pre-state through `i_src` would be
running the sealed port backwards.

```text
PRESTATE_VIA_ISRC = MISSING
  MISSING INGREDIENT  a state-valued construction ON A_SRF_CTP -- P5's own content.  i_src supports
                      RESTRICTION (a test) and not EXTENSION (a construction).
  SPAN                the port-order span above; and the demand leg itself at
                      workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md
                      e191d379... [8954,9569) 8917c67f...
  COMMON-ORIGIN NOTE  the certificate cannot run "through" i_src either, for the same reason: i_src
                      carries the SOURCE sector's coherence, not the joint state's provenance.
```

### 3.3 (c) `omega_hist` through `i_rec` — **FAILS**, and it fails on the factor, not on the seal

The relay's conditional is: *if* `B` reaches `A_SRF_CTP` through `i_rec`, then the remaining gap is
sealing `i_rec` rather than authoring a new arrow. **The antecedent is false**, and the reason is a
factor mismatch that no amount of sealing repairs.

What `omega_hist` restricts through:

```text
CITATION  workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md
          source a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782
          [21194,21565)  FIXED
          span   e76be5c6f0536f2573c79f5d02a46e94f497cac5e87b744613ccf9c0636d011d
```

> `compute omega_hist=Omega_C0 compose i_B;`

and `i_B`'s domain, from the carrier typing:

```text
CITATION  workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md
          source a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782
          [10436,11034)  FIXED
          span   b9c7a355890def386696ac4a075b1da79420d2b03e1f0d026e83983b5e18566d
```

> `A_SR := A_src graded-tensor_min R_inf,`
> `B    := A_F_CTP isomorphic to C(Y),`
> `A_C0 := A_SR graded-tensor_min B.`
> … `i_B : B -> A_C0, i_B(b) := 1_A_SR tensor b.`

**`B` is `A_F_CTP` — the third factor, the field/CTP one.** And `i_rec`'s domain is not that:

```text
CITATION  workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
          source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
          [38867,39276)  FIXED
          span   56a575d104416d75ef19aa289eee9639c3650135f429cf1fbabccf1c068902c0
```

> `i_rec : RecordSector[S_3] -> c1.A_SRF_CTP,`
> `Q42_direct_limit_reference_accessor(S_3), Q43_dressed_monomorphism_reference_accessor(S_3),`
> `base_tensor_join_reference_accessor(S_3), Q42_direct_limit_compatibility_witness(i_rec,S_3),`
> `Q43_dressed_monomorphism_compatibility_witness(i_rec,S_3),`
> `base_tensor_carrier_compatibility_witness(i_rec,S_3)`

`RecordSector` is the Q-42 direct-limit record algebra — `R_inf`, the **second** factor of `A_SR`.
`B` is the **third**. They are different factors of the same tensor product, and the sealed
factor inclusions `i_R : R_inf -> A_C0` and `i_B : B -> A_C0` are different arrows.

```text
OMEGA_HIST_VIA_IREC = FAILS
  i_rec reaches the RECORD factor.  omega_hist restricts into the FIELD/HISTORY factor.
  SEALING i_rec WOULD LEAVE B EXACTLY WHERE IT IS.
```

And `B` is not incidental to K4 — it is precisely the object whose states are the measures K4 needs:

```text
CITATION  workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md
          source a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782
          [11419,11482)  ANCHOR
          span   5efbab19d594957daf88b9c27b0342e5bc3f2d5bc191d4aee4e3563843042348
```

> `B isomorphic to C(Y),`
> `State(B) isomorphic to Prob_reg_Borel(Y).`

### 3.4 What sealing `i_rec` would require — answered although it is not the gap

The relay asks it as a conditional and it is determinate, so it is answered rather than skipped.

The `i_rec` occurrence is a **port signature** — a type in a proposed `RecordEmbeddingPackage_3`, not
a candidate object awaiting ratification. Sealing it under the record's own port rules requires:

1. the arrow itself, constructed;
2. its **six** declared witnesses, listed in the span above;
3. a `c1 : CarrierPackage_1` for it to land in — the port signature's codomain is `c1.A_SRF_CTP`, and
   `c1` is Leg 1's carrier package, which does not exist.

```text
IREC_SEALING = N/A for the omega_hist purpose -- omega_hist does not route through i_rec at all.
  CONDITIONALLY, WERE IT NEEDED: NOT ratification-of-proposal.  There is no proposed OBJECT to
  ratify; there is a proposed TYPE.  Sealing it is construction of an arrow plus six witnesses
  against an absent c1 -- derivation conditional on Leg 1, not the ratification of an existing
  candidate.  i_rec is EXAMINED HERE, NOT PROMOTED.
```

### 3.5 (d) The failing span — and it is the true record-level require

The SRF-native route fails at the conjunction of two spans, both record-side:

```text
FAILING SPAN 1  workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md
                a67ed435... [10436,11034) b9c7a355...   -- B := A_F_CTP, the field factor
                with a67ed435... [21194,21565) e76be5c6... -- omega_hist restricts through i_B into B

FAILING SPAN 2  workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md
                source d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98
                [6743,7035)  ANCHOR
                span   e0497a680c590abf5ab7987534de68a2e85561bc6abfb77afad22339922374ac
```

> `X := Hom(Lambda,U(1)), Y := X_+ x X_-.`
> `The same proposal states … that A_F is the continuous cylinder-function algebra on X. Since A_F is`
> `commutative and nuclear and A_F^op is canonically isomorphic to A_F, the exact branch algebra is`
> `B isomorphic to C(Y).`

**`B` is forced record-side, not schema-side.** It is the exact branch algebra of the history space,
derived at TASK2D. K4's receiver is therefore a record object, and a state on it that DESCENDS from
the common origin — L6's requirement — must be the restriction of a joint state. The SRF-native route
supplies a joint state on `A_SRF_CTP` and no way to restrict it to `B`.

---

## 4. THE ARROW, RELOCATED AND NARROWED [PROVABLE]

Both halves, stated as the relay's stakes sentence requires:

```text
WHAT FALLS   the CARRIER IDENTIFICATION.  "A_C0 must be identified with, or embedded into,
             A_SRF_CTP" is an entry-schema requirement with no law-side ground.  The three-factor
             transport -- arrows out of A_src, R_inf AND A_F_CTP, with graded-commutation and
             min-continuity certificates -- is the instrument's demand, not the record's.
             THIS IS THE SIXTH FRAME ARTIFACT.

WHAT STANDS  a FIELD-FACTOR ARROW, one arrow wide: a link from the SRF side to B -- equivalently
             B -> A_SRF_CTP, or an identification of A_SRF_CTP's own history sector with B.
             ITS GROUND IS NOT THE INSTRUMENT.  It is K4's receiver: omega_hist restricts into B,
             B is forced at TASK2D, and L6 requires that state to descend rather than be chosen.
             Plus L5, which names C0 in d_U2's domain -- a provenance witness, not an arrow, and
             counted separately for that reason.

NET          THE REQUIREMENT SURVIVES THE PROVENANCE TEST WITH A DIFFERENT GROUND THAN THE ONE THAT
             WAS CITED.  That is the answer to the principal's question: the assumption as stated
             was unfounded, and the conclusion it was supporting is not.
```

**Why this is a narrowing worth having, and not a restatement.** The 1034 result required three
arrows and two certificates. What survives requires one arrow on one factor. `phi_src` and
`phi_rec` are no longer needed at all — nothing asks for a homomorphism out of `A_C0` as a whole
once the carrier identification falls. The remaining object is smaller, and it is the same object
K4 was always waiting on.

---

## 5. A REFINEMENT TO THE 1034 RESULT, FOUND HERE AND RECORDED AGAINST MYSELF [PROVABLE]

`STAGE8_DESC_B03_DARIO_V003.md` §4.4 declined to supply the min-continuity certificate `T3` from
general mathematical facts about the factors' classes, on the ground that importing them would be
adding an unsealed premise. **The record seals part of it.** The TASK2D span above states `A_F` is
**commutative and nuclear**, and `A_F^op` canonically isomorphic to `A_F`.

Structurally, and every scale symbolic: nuclearity of a factor collapses the minimal and maximal
C\*-tensor norms for pairings with that factor. So for the field factor's pairing, `T3` is closer to
discharged than V003 allowed — the certificate V003 listed as missing is, for that pairing,
available from a sealed record property rather than an imported one.

**This does not move either 1034 verdict**, and I state the limit as sharply as the finding:

- it addresses `T3` only; `T1`'s third row — *no arrow out of the field factor exists at all* — is
  untouched, and that is what both stops rest on;
- it is sealed for `A_F` and hence for `A_F_CTP`; **I do not extend it to `A_SR`**, whose factors'
  nuclearity the record does not seal in anything I read, and asserting it would be the very import
  V003 refused;
- the graded case needs the graded statement, which is likewise not sealed.

Recorded because V003 is mine and the opposite-lane check confirmed it as written; a refinement that
makes my own earlier caution look over-strong is exactly the kind that should be published rather
than absorbed.

---

## 6. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the 1034 derivation's two stops and the opposite-lane check's STOPS-CONFIRMED verdict; the
  TWO-OBJECTS ruling; DoR-008's seven ratified adoptions, its TYPE-P mark and standing falsifier;
  DoR-013's family-level ratification and no-member clause; the six-leg demand restatement; res_B's
  exactness; the arrow census into A_SRF_CTP; CLOSURE_MEMBER_CITATION_RULE_V001; S01-S37 including
  S05 at its exact scope; the overlay read rule; all charter fences.

DERIVED HERE:
  the forward and backward provenance trace of the identification clause, and its termination in a
    schema target rather than a law-side span;
  the law-side count -- zero A_C0, one C0, and that one a REFUTATION CONDITION;
  the observation that the instrument's own closure types the producer spec as a "wrong-codomain"
    declaration, which is the provenance answer in one row;
  the leg-by-leg SRF-native audit and its 5/6 with L5 named, together with the distinction between
    a provenance witness and an algebra arrow that keeps L5 from being over-counted;
  the direction finding for i_src, at the sealed port order (P2 consumes rho_pre);
  THE FACTOR MISMATCH: i_rec reaches the RECORD factor while omega_hist restricts into the FIELD
    factor B, so sealing i_rec cannot serve K4;
  the relocation and narrowing of the require to ONE history-factor arrow with a K4 ground;
  the T3 refinement against my own V003, from TASK2D's sealed nuclearity, with its limits stated.

SELECTED HERE:
  nothing.  No arrow authored, no carrier identified, no entry filled or recommended, no candidate
  considered, no proposal promoted, no port sealed, no stop lifted or defended.  i_rec examined,
  not promoted.  DoR-008's premises carried, never discharged.  The instrument's status untouched.

SCALING WEIGHTS: none consumed, formed, fixed, compared, substituted, or evaluated.  Every scale
  symbolic.  The nuclearity statement in section 5 is used as STRUCTURE; no norm was computed,
  estimated, bounded, or compared.
```

---

## 7. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.**
  - **An instrument-authored clause was not flattened into a dissolved requirement** (§2.6, §4). This
    is the error this relay was most exposed to: Q1's answer invites "so the arrow is gone," and Q2
    says otherwise at a span.
  - **A provenance witness was not flattened into an algebra arrow** (§3.1): `d_U2` names `C0` in its
    domain and demands a witness; that is not the same as demanding a homomorphism, and L5 is
    counted as its own kind of exception.
  - **A restriction was not flattened into an extension** (§3.2): `i_src` tests a pre-state and
    cannot build one, and the sealed port order says so independently of my reading.
  - **Two factors of one tensor product were not flattened into each other** (§3.3): `RecordSector`
    is `R_inf`; `B` is `A_F_CTP`; `i_R` and `i_B` are different sealed arrows.
  - **A port TYPE was not flattened into a proposed OBJECT** (§3.4): there is nothing to ratify.
  - **A narrowing was not flattened into a dissolution** (§4), and **a refinement was not flattened
    into a reversal** (§5).
  - **S05 read at its exact scope**, unchanged from 1034: it declines the source/record two-factor
    tensor, not `A_C0`.
- **BUILDER-NEVER-VERIFIES:** CLAIMED until the opposite-lane check. I did not re-adjudicate the
  check's STOPS-CONFIRMED verdict, DoR-008, TASK2D's forcing result, or the instrument's status; I
  read them.
- **SELF-CITATION BAR HELD.** No span is drawn from my own 1034 or 1037 output. The hypothetical
  `phi_F` displayed in V003 appears in a corpus grep for arrows into `A_SRF_CTP` and is **excluded**;
  treating my own displayed demand as a record witness would be circular, and the opposite-lane
  check independently ruled the same exclusion.
- **BLIND HELD.** No physical quantity formed, evaluated, or compared. No member bound. No fixed
  point executed. `omega_phys` untouched — this relay locates where a state on `B` would have to come
  from and supplies none.
- **PE-1..PE-17:** pointer-only, zero verdict weight.
- **NO OUTPUT INSPECTION — CERTIFIED.** The temptation here was the mirror of 1034's: to let Q1's
  clean instrument-authored verdict carry Q2 by momentum and report the arrow dissolved. Q2 was run
  at bytes, leg by leg and factor by factor, and it returned the opposite. Nothing was validated,
  adjusted, or selected by what would close the section.
- **CUSTODY:** archive-side only; `workspace/` and `relay_outbox/` written; cleanroom not written.
  **No register, plan, tracker, or git action. NO SUBAGENT DELEGATION.**
- **BYTE AUDIT:** all 20 members rehashed at full archive-root paths at run time by member 20; all 18
  spans computed by it and published as full citation tuples; **10 FIXED spans re-verify upstream
  pins, 10/10 MATCH.** Closure declared at byte 0, end computed as a fixed point under a fixed-width
  field; predeclaration region empty, so its scan has zero possible hits.
- **RESIDUE, per Q-921:** five-family removed-class scan over authored prose, fenced blocks excluded
  (a fence is any line whose first non-space characters are three backticks — the rule the V003 check
  ruled correct). Run before sealing, loci read rather than predicted.

  ```text
  RESIDUE = 4 RAW / 0 OPERATIVE
     2  this bullet's own naming of the classes it counts     META-LABEL
     2  content loci, both outside this bullet:
        1  "verbatim", section 2.4 -- "the StatePort pattern verbatim"
        1  "reproduces", section 3.2 -- quoting P2's own coherence condition, that the
           restricted state reproduces the Q-41 quasifree/GNS data
  ```

  No byte-stability claim appears in this artifact's prose at all — it supersedes nothing and
  carries no predecessor's bytes, so the class that dominated V003's census is simply absent here.
  **No locus asserts artifact reconstruction, a historical rerun certificate, deletion carriage, or
  membership movement.** **CLAIMED, not clean** — the hand that wrote the prose ran the scan.
- **GENERATOR CUSTODY (Q-920/Q-924):** member 20's declared inputs are exactly members 01–20; four
  refusal paths declared; `FILES=20 SPANS=18 FIXED-REVERIFIED=10/10 MATCH`. The self-citation bar is
  enforced in the generator by construction, not by my discipline alone: no span row names member 03.

---

## 8. The certificate

Per Q-913/Q-917/Q-921/Q-930 and CLOSURE_MEMBER_CITATION_RULE_V001: **the certificate is the digest
table plus the eighteen full citation tuples.** Every published span carries canonical path, full
source digest, half-open interval, and full span digest, so that no consumer must resolve a member
number through this closure. **There is nothing below the digests.**

---

## 9. Final lines

```text
CLOSURE = declared-first (byte position, scan)
     byte 0; closure end 6322 under a fixed-width field; predeclaration region EMPTY, zero possible
     hits.  Every span published as a full path/source-digest/interval/span-digest tuple.

IDENTIFICATION = INSTRUMENT-AUTHORED (trace)
     THE CLAUSE is a FIELD-TABLE ROW of an entry-completion instrument whose own status line reads
     "DRAFT FOR PRINCIPAL ENTRY -- NO ENTRY IS MADE HERE" (workspace/
     STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md af26ab03... [5062,5279) a2c49549...); the
     row's purpose is to fill the `joint i_src` field (same file, [10322,10545) 602ab0bf...).
     BACKWARD TRACE terminates in (i) the candidate survey, which measures the record's three sealed
     declarations as deviating "rather than A_C0" (workspace/STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_
     DARIO_V001.md 4440af4f... [21360,21526) f20fa601... and [7028,7663) 43f3129a...), and (ii) the
     state-algebra map, TYPE-P | premises: DoR-008.  NEITHER IS A DEMAND LEG, A PRODUCER PORT, OR A
     THEOREM.
     LAW-SIDE TEST, over full bytes: P0-P7 contain ZERO occurrences of A_C0 and exactly ONE of C0 --
     and that one is REFUTATION CONDITION 4 (workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_
     V001.md e191d379... [18296,18480) cf696d88...), the ruling that would SUPERSEDE the producer.
     THE DECIDER, one table row: the instrument's own closure types the producer spec itself as the
     "third wrong-codomain i_src declaration" (same instrument, [2431,2980) ff3e4db7...).  The
     record's codomain is judged wrong because it is not the instrument's.  THE STATEPORT PATTERN,
     VERBATIM.  THIS IS THE SIXTH FRAME ARTIFACT.

LEGS_ON_SRF = 5/6 (exception: L5)
     L1, L2, L3, L6 at producer P1/P5 and v004; L4's U2 role content is carrier-neutral.  L5 is the
     exception: d_U2 : (B0_candidate,C0) -> U2 names C0 IN ITS OWN DOMAIN (workspace/
     STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md 3cd4b080...
     [6266,6883) e6093b0f...).  Counted as a PROVENANCE WITNESS, not an algebra arrow.

PRESTATE_VIA_ISRC = MISSING (ingredient, span)
     INGREDIENT: a state-valued construction ON A_SRF_CTP -- P5's own content.  i_src supports
     RESTRICTION, not EXTENSION, and the sealed port order says so independently: "The completed P2
     contract must execute after P5 and consume c5.rho_pre" (workspace/STAGE8_OBJ0_EXACT_SIGNATURE_
     DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md f7fa3c0f... [37255,37521) a5900fd8...).  P2 CONSUMES
     THE PRE-STATE; IT DOES NOT PRODUCE ONE.  A route through i_src runs the sealed port backwards.

OMEGA_HIST_VIA_IREC = FAILS (span)
     omega_hist = Omega_C0 compose i_B, and i_B's domain is B := A_F_CTP, the FIELD/history factor
     (workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md a67ed435... [21194,21565) e76be5c6...
     and [10436,11034) b9c7a355...).  i_rec's domain is RecordSector -- the Q-42 direct limit R_inf,
     a DIFFERENT FACTOR of the same tensor product (workspace/STAGE8_OBJ0_... f7fa3c0f...
     [38867,39276) 56a575d1...).  SEALING i_rec WOULD LEAVE B EXACTLY WHERE IT IS.  And B is K4's
     actual receiver: State(B) iso Prob_reg_Borel(Y) (a67ed435... [11419,11482) 5efbab19...).

IREC_SEALING = N/A
     omega_hist does not route through i_rec at all, so its sealing is not the remaining gap.
     CONDITIONALLY, WERE IT NEEDED: NOT ratification-of-proposal.  The i_rec occurrence is a port
     TYPE, not a candidate OBJECT; sealing it requires the arrow, its SIX declared witnesses, and a
     CarrierPackage_1 to land in -- and c1 does not exist.  Construction conditional on Leg 1.
     i_rec EXAMINED, NOT PROMOTED.

ARROW = TRUE-REQUIRE (span)
     AND THE TWO HALVES MUST BE READ TOGETHER.  What falls: the CARRIER IDENTIFICATION, and with it
     the three-factor transport -- phi_src and phi_rec are not needed by anything once the schema's
     demand is set aside.  What stands: ONE ARROW ON THE HISTORY FACTOR, B -> A_SRF_CTP (or an
     identification of A_SRF_CTP's own history sector with B), grounded NOT in the instrument but in
     K4's receiver, with B forced RECORD-SIDE at workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_
     FORCING_PROTOCOL_RESULT_V001.md d13920e2... [6743,7035) e0497a68..., and with L6 requiring that
     state to DESCEND rather than be chosen.  THE REQUIREMENT SURVIVES THE PROVENANCE TEST WITH A
     DIFFERENT GROUND THAN THE ONE THAT WAS CITED -- the assumption as stated was unfounded, the
     conclusion it supported is not.  The Q-948 conversation resumes, one arrow narrower.

T3_REFINEMENT = PUBLISHED AGAINST MY OWN V003
     TASK2D seals A_F COMMUTATIVE AND NUCLEAR (same span as above), so the min-continuity
     certificate V003 section 4.4 declined to import is, FOR THE FIELD FACTOR'S PAIRING, available
     from a sealed record property.  It moves NEITHER 1034 verdict: T1's third row -- no arrow out
     of the field factor exists at all -- is untouched, and I do NOT extend nuclearity to A_SR,
     whose factors' nuclearity the record does not seal.

OUTPUT_INSPECTION = NONE-CERTIFIED
     The temptation was the mirror of 1034's: to let Q1's clean verdict carry Q2 by momentum and
     report the arrow dissolved.  Q2 was run at bytes, leg by leg and factor by factor, and returned
     the opposite.  Nothing was shaped by what would close the section.  SELF-CITATION BAR HELD: no
     span drawn from my own 1034/1037 output, enforced in the generator by construction.

CHAIN_INVOKED = false
     The inbox holds no unstarted paste addressed to this lane behind 1037; 1035 and 1036 are
     Codex 2's.

VERB_AUDIT_SELF = CLEAN
     Nothing adopted is called derived.  The instrument is called instrument-authored, not wrong.
     DoR-008's adoptions are called ratified premises.  i_rec is called examined, not sealed.  No
     selector selected, no member bound, no arrow authored, no entry filled, no stop lifted.
```

All findings CLAIMED until the opposite-lane check.

**The step to press first is section 3.3, because it carries the whole standing half and it is
mine.** That `i_rec`'s domain is the record sector and `i_B`'s is the field factor is read directly
off two sealed spans, and the conclusion — that sealing `i_rec` cannot serve K4 — follows if those
two factors are genuinely distinct in the record and not two spellings of one object. Q-78's N5
records that the producer's five-plus spellings have never been equated in a sealed sentence, and
the same caution applies to factor names: **a sealed sentence identifying `RecordSector` with `B`, or
placing the history algebra inside the record sector, would collapse my finding.** I looked for one
and did not find it, and I record that as the residual rather than as a finding.

**The second thing to press is section 4's narrowing.** That the require is *one arrow* rather than
three follows from the carrier identification falling; a lane could reasonably ask whether L5's
`d_U2` witness, whose domain names `C0` as a whole package, silently re-imports the other two
factors through the provenance requirement even though it does not demand homomorphisms. I have
counted it as a separate exception rather than folding it in, and that choice is the one to test.
