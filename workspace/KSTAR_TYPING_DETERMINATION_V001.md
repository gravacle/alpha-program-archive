# K*-TYPING DETERMINATION — V001 (2026-08-14)

Analyst: KST-BUILD (blind; sealed artifacts on disk only). Instrument: the ACTUAL-SURFACE
GUARD V001 (RELEVANCE BURDEN, CALIBRATION, PURPOSE, PUSHBACK FORK clauses govern the framing —
this is a hole-finding DETERMINATION under a governance instrument, never a prohibition and
never a construction). Task of record: the K*-typing determination named at the program's
relevance audit (CHAIN_RELEVANCE_MAP_V001.md item 28, which classed K* TRACE-INCOMPLETE(named)
from its own sweep and posed exactly the three questions answered here).

Fences live throughout: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false. K*'s TYPE only — no numeric evaluation, no closed form of K*,
no comparison to measurement, no evaluation of alpha_micro; every numeric string below is a
sealed-text quotation. No register, road, ledger, lens, plan, or tracker file opened (see
Choice Ledger, C-1/C-2: THE_HANDOFF excluded as plan-adjacent). No CAS was needed: every step
is occurrence- and type-reading of sealed bytes; no identity required symbolic checking. No git;
no edits to any existing file; one output file + seal.

---

## 0. VERDICT (one line)

SPLIT — (1) NOT-TIED: no booked derivation ties K* (the onset root of C_record) to the surface's §II.9 onset-bound lock; the sole contrary text (FABLE F1 :124-125, echoed OPUS5 :93) is an unsupported homonym-binding whose own cited spans contain zero K* occurrences; (2) TYPED-MAGNITUDE: K*'s defining condition types it at bytes as a magnitude-valued induced-response/impedance quantity ("stiffness is a magnitude"; "not as write strength"; K = 1/e_HL^2 form) defined wholly on Gamma_K/BR apparatus data (geometry, clock, measure, regulator, spectrum — none surface-native), so the I2 vacancy constraint engages for any magnitude-side landing and K* is APPARATUS-ONLY as of today — a STATUS, not a verdict, whose primary lawful exit (a booked phase/action-structure landing, most plausibly on the §II.9 onset bound) is exactly the absent derivation of (1); (3) IDENTITY-plus-HOMONYM: the surface §IV's "K_* the onset root" is the decision's K* — same referent, by explicit principal-act citation — while §II.9's "onset bound" (J_FS,rel >= pi hbar/2) is a DIFFERENT object sharing only the token "onset", related to K* by no booked derivation.

---

## 1. SEALS AND LOCATED AUTHORITIES

### 1.1 Tasked seals — all verified at path with `shasum -a 256`

| Document | Computed SHA-256 | Sidecar | Expected prefix |
|---|---|---|---|
| `/Users/bgm/MB Work/alpha_supervision/ALPHA_STIFFNESS_IS_K_STAR_PRINCIPAL_DECISION_2026-07-30.md` | `754506191e1c9129101ada17255e07db55e48619b291c1190d3af011ec1e8126` | MATCH | `75450619` MATCH |
| `/Users/bgm/MB Work/alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` | `20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df` | MATCH | `20ee87c0` MATCH |
| `/Users/bgm/MB Work/alpha_supervision/ACTUAL_SURFACE_GUARD_V001.md` | `2baa4c31fbe566516c3f61dcac351889e914368bd33b8c96ff40a161788c4fd5` | MATCH | (none tasked) |
| `/Users/bgm/MB Work/alpha-program-archive/workspace/BARE_SURFACE_I2_DETERMINATION_V001.md` | `9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c` | MATCH | `95235195` MATCH |
| `/Users/bgm/MB Work/alpha-program-archive/workspace/BARE_SURFACE_I2_DETERMINATION_AUDIT_V001.md` | `97ae605d065b7b614ed2927602403cd72550507b26bf71fb0f7299be4c545d73` | MATCH | `97ae605d` MATCH |

The I2 audit's verdict is CONFIRMED-WITH-CORRECTIONS with no verdict-chain correction (audit :9),
so the I2 vacancy result is consumed here at audited grade.

### 1.2 Cited authorities — located and verified

**The beta-closure hunt (digest prefix 04c10c0d): LOCATED.**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md`
= `04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d`, sidecar MATCH. Four
adversarial verifiers CONFIRMED (its §"METHOD"). This is the source of record for the four
derived locks including the onset bound (surface §II.8-9 cites it).

**`primitive_record_cell_selection_principle_v002.md`: TRACE-INCOMPLETE(named) as a file.**
Searched both tasked roots recursively, plus a wider diligence probe (`find "/Users/bgm/MB Work"
-iname "*primitive_record_cell*"`): ZERO hits anywhere. The file the K* decision cites at
`:95-97,:104` is absent from the reachable corpus. Its operative content is NOT guessed at: the
exact defining condition is DISPLAYED VERBATIM at two sealed carriers, mutually consistent —
the decision itself (:12-13) and the decision's own cited basis,
`/Users/bgm/MB Work/alpha_supervision/RESULT_TWO_STIFFNESS_OBJECTS_2026-07-30.md` (:17-18),
verified `f3eb0c8431588601a94bf80817256b66bf8b0d6097ae8dc73bd63948e48d82bb` = sidecar = the
digest the decision quotes at :7 (double attestation). Both display:
`C_record(K_*) = 0`, `dC_record/dK != 0` at `K_*`, `K_* > 0`, `alpha_micro = 1/(4 pi K_*)`.
This determination proceeds on those displayed bytes (Choice Ledger C-3); the v002 file itself
remains TRACE-INCOMPLETE(named).

### 1.3 Additional sealed authorities consumed (all sidecar-verified unless flagged)

| File (workspace unless pathed) | Digest (prefix) | Role here |
|---|---|---|
| `STAGE8_F0_KSTAR_DEFINING_CHAIN_CODEX2_V001.md` | `95c003ca` MATCH | K*'s complete defining-chain census: 16 fixing spans D01-D16; 1787-file sweep; status CLAIMED |
| `STAGE8_F0_COMPUTAND_CHAIN_V001.md` | `05eeab53` MATCH | lineage typing; KNI flags; assembly-identity bytes |
| `STAGE8_KAPPA_RECORD_KSTAR_PAIR_TEST_UNDER_Q61_V001.md` | `5d86c9c1` MATCH (also cross-attested: CODEX2 row D10 pins exactly this digest) | the three-stiffness typing |
| `STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md` | `45fa8f6e` MATCH (also quoted MATCH in OPUS5 :15) | the sole assertion of a K*-to-onset-bound binding; header "ALL RESULTS CLAIMED" |
| `STAGE8_S16_BETA_CHECK_OPUS5_V001.md` | `9c9e120e` — **NO SIDECAR anywhere in the archive** | the FABLE artifact's adversarial checker; consumed at flagged custody |
| `alpha_supervision/DERIVED_RESULTS_INVENTORY_2026-08-01_V001.md` | `f12a95b4` MATCH | onset-bound derivation status; `alpha_micro = 1/(4 pi K_*)` ADOPTED (Q-140 N11) |
| `CHAIN_RELEVANCE_MAP_V001.md` | `eae6cf88` MATCH | the naming instrument; item 28; converse inventory U-11 |
| `STAGE8_SLOT16_REPOSED_SCOPE_AND_FK1_AUDIT_V001.md` | `df0d1c74` MATCH | slot-16 name adjudication ("not conditioned on the onset root") |
| `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` | `cc113a16` — **NO SIDECAR** | the onset bound's own gate; used only as corroboration where its lines are re-sealed elsewhere, and at flagged custody for its V003-only bars (see §2.3) |

---

## 2. QUESTION (1) — THE TRACE: does a BOOKED derivation tie K* to the §II.9 onset-bound lock?

**ANSWER: NOT-TIED.** No booked derivation exists in the reachable corpus. The absence is
two-sided at bytes, the corpus's own census instruments corroborate it, and the single text
that asserts a tie is an unsupported homonym-binding — a hole, reported per the PURPOSE clause.

### 2.1 The two objects, each at its own bytes

**The onset-bound lock (surface §II.9 :47, "the onset bound").** Its content of record, at the
lock's source (the hunt, 04c10c0d §1.1 :83-85): "Onset budget `J_FS,rel >= pi hbar/2` derived;
*'Saturation is adopted, not derived.'*" — listed in §1.3 :110-114 as the fourth derived,
beta-blind junction ("the onset inequality"). Sealed corroboration of its derivation status:
DERIVED_RESULTS_INVENTORY :174-175 — "`J_FS,rel = pi hbar/2` — THE LOWER BOUND IS DERIVED from
the imported theorem. SATURATION IS ADOPTED, NOT DERIVED." It is a Fubini-Study relative-action
budget on the record cell — internal phase/action geometry. (Identification of §II.9's "onset
bound" with the hunt's "onset inequality"/"onset budget" is derived at bytes in Choice Ledger
C-4.)

**K* (the decision :11-13; basis :16-21).** "`K_*` — the onset root of the one-cell closure
condition": `C_record(K_*) = 0`, `dC_record/dK != 0`, `K_* > 0`, on "the complete
Boundary-Resolved cell" — with C_record fixed by the Gamma_K construction spec as "the complete
on-shell scalar closure residual" of the Gamma_K/BR stationary one-cell problem (CODEX2 D07 row,
:101), and with the residual "required-not-built" of record (F0 computand chain :493-497:
"C_record(K) itself — the residual whose onset root DEFINES K_* is required-not-built …
falsifier F-RP1 FIRED, the mutation audit unexecutable"; CODEX2 link 9: appointed executable
formula ABSENT).

### 2.2 The absence, established two-sidedly

- **The lock's lineage never mentions K*.** Occurrence scan of every J_FS-carrying non-fenced
  file in both roots: `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md`,
  `STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md`,
  `alpha_supervision/RELAY_PASTE_255_DISCHARGE_STEP3_ONSET_SATURATION_V001.md`,
  `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` — each contains ZERO occurrences of
  `K_*`, `K_star`, `C_record`, and even of the token "stiffness".
- **K*'s lineage never mentions the lock.** The hunt (the lock's source of record) contains
  ZERO `K_*`/`K_star` occurrences (grep count 0 over 04c10c0d). The CODEX2 defining-chain
  census — a 1,787-file sealed-stock sweep with multi-form search (its §1.2-1.3) — recovered
  SIXTEEN independent fixing spans for K* (D01-D16, its §2) and NONE is an onset-bound/J_FS
  span; all are register/Gamma_K/Thomson objects. Its ruling: "All other searched occurrences
  are MERE MENTION … They add no independent defining predicate."
- **The naming instrument already recorded the gap at its own quantifier.** CHAIN_RELEVANCE_MAP
  item 28 (:409-424): "zero occurrences of K*, K_star, or any Thomson/stiffness token in any of
  the 14 chain artifacts"; whether the "onset root" naming "ties it by booked derivation to the
  SURF §II.9 onset-bound lock … could not be verified" from its sweep. This determination,
  with the definitional carriers in scope, resolves that named verification debt: the tie does
  not exist in the reachable corpus.

### 2.3 Affirmative corroboration beyond absence (flagged custody)

The onset lock's own gate, `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (NO sidecar —
consumed at flagged custody; its "Saturation is adopted, not derived" line is re-sealed verbatim
inside the hunt and the inventory), types the bound as "an energy-uncertainty/path-length
budget. It is not automatically either branch's dynamical action, the action difference, or a
coefficient in the microscopic Lagrangian" (:88-90); lists among NOT ESTABLISHED:
"identification of J_FS,rel with microscopic dynamical action" (:163); and closes: "No
geometric uncertainty budget may be substituted for physical action without a theorem"
(:172-173). K*'s side lives exactly on the microscopic-action side (the stiffness departure is
fixed "only through the complete microscopic action, its stationary saddle, and the generated
unitary record dynamics" — decision :30-32). So the needed tie is not merely unwritten — on
these (unsealed-gate) bytes it is affirmatively gated behind a named theorem. NOT-TIED stands
on §2.2 alone; this paragraph upgrades its texture, at its custody.

### 2.4 The one contrary text — adjudicated as a hole (PURPOSE clause: reported, not outlawed)

`STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md` :124-125 (sealed, header "ALL RESULTS
CLAIMED"): "**F1 — the cell foot, K_*.** [PROVABLE] The onset root of the onset inequality —
one of the four derived locks, booked beta-invariant (ANCHOR II.8-9; CENSUS §1.3). No R
occurrence. BETA-BLIND." — and FINAL LINES :283: "K_* (derived lock, no R)". Echoed by its
checker `STAGE8_S16_BETA_CHECK_OPUS5_V001.md` :93 (NO sidecar): "onset inequality / `K_*`
(SURFACE §II.9) — β-blind = subject F1."

Adjudication at the cited bytes:

1. **The citation does not contain the claim.** ANCHOR (surface) §II.8-9 contains no `K_*`
   token (K_* occurs in the surface document ONLY at :63 and :68, section IV); CENSUS §1.3
   contains no `K_*` token (zero in the whole file). Neither cited span performs — or could
   perform — the identification "K_* = the onset root of the onset inequality."
2. **The claim contradicts the definition of record.** K* is the onset root of `C_record` (the
   Gamma_K/BR closure residual), not of the inequality `J_FS,rel >= pi hbar/2`; and K* is not a
   "derived lock" — its residual is required-not-built (F-RP1 FIRED), its physical selection
   carries sealed flags `physical_K_star_selected_or_derived = false | TYPE-U` and
   `alpha_micro_from_physical_K_star_derived = false | TYPE-U` (F0 chain :278-279), and
   `alpha_micro = 1/(4 pi K_*)` "IS ITSELF ADOPTED (Q-140 N11)" (inventory :192).
3. **The confirmation could not have caught it.** OPUS5 opened only its five tasked files
   (its :21-23); none carries K*'s definition (the census has zero K* tokens; the surface names
   K_* only in §IV, definition elsewhere). The checker had no definitional authority in scope
   against which to test F1's binding — a blind-spot confirmation, at unsealed custody.
4. **Consequence for that artifact's own chain, reported not repaired:** F1's "beta-blind"
   typing of the cell foot loses its cited ground. The correct definitional route cannot be
   occurrence-typed today at all — there is no appointed executable `C_record(K)` formula in
   which to count fiber-radius occurrences (CODEX2 link 9 ABSENT). The FABLE/OPUS5 headline
   (`S16_BETA_SENSITIVE = UNDETERMINED`, decider = S13's shape) is NOT overturned by this —
   it rests on the other feet — but "every built foot beta-blind" has a hole at F1: K* is not
   a BUILT foot; it is PARTIALLY DEFINED apparatus output (CODEX2 §0). This is a MISSED
   unforced identification in the guard's sense, and precedent exists for exactly this
   name-level failure mode: the slot-16 audit refuted the analogous naming hypothesis —
   "'threshold-conditioned' means conditioned on the interacting pole-versus-infraparticle
   outcome, not conditioned on the onset root" (:20-22).

### 2.5 What derivation would create the tie (the gap, stated exactly)

A booked theorem deriving that the onset content of `C_record` — record-closure onset over the
K-indexed family on the complete on-shell Gamma_K/BR problem — coincides with (or lands on) the
FS onset budget `J_FS,rel >= pi hbar/2` of the §II.9 lock. Its typed obligations, from the
sealed stock: (i) it must first have an appointed executable `C_record(K)` (CODEX2 link 9,
ABSENT); (ii) it is exactly the "identification of J_FS,rel with microscopic dynamical action"
the onset gate holds NOT ESTABLISHED, so it must be a theorem, not a substitution (gate
:172-173, flagged custody); (iii) if it proceeds via saturation it must DERIVE saturation,
which is booked ADOPTED (hunt §1.1; inventory :174-175). If created, it would land K*'s onset
content on derived, beta-invariant, phase/action-typed surface structure — the "partial surface
landing" the relevance map's item 28 names, and the primary lawful exit of §3's status.

---

## 3. QUESTION (2) — THE TYPING: what does K*'s defining condition type K* AS?

**ANSWER: TYPED-MAGNITUDE, apparatus-resident; the I2 vacancy constraint ENGAGES for any
magnitude-side surface landing; K* is APPARATUS-ONLY as of today — a STATUS per the
CALIBRATION clause, not a condemnation, with the primary exit (path 1: earn the trace) named
and gated on exactly §2.5.**

### 3.1 The defining condition's own ingredients, at their bytes

- **The family index K is a stiffness, and stiffness is a magnitude.** The decision's basis:
  "**Stiffness is a magnitude, so a threshold alone cannot produce one**" (two-stiffness :52);
  the onset mechanism produces one anyway ONLY by running the threshold over a K-INDEXED
  family — "Applying a *when* to a one-parameter family converts it into a *which*" (:55-58) —
  and ONLY IF "everything else is fixed independently — state preparation, species, measure,
  curvature weight" (:60-64), i.e. the Gamma_K Section-1 data (0 of 11 complete; construction
  stopped at F-GK3 — decision :67-69).
- **The type of the coupling K selects is impedance — magnitude-valued.** "Couplings are
  IMPEDANCES OF ACCESS CONDITIONS, `Z_F = 1/kappa_F`" (decision :23-25); the assembly identity
  is restated of record as `K e_HL^2 = 1`, `alpha_micro(K) = 1/(4 pi K)` (F0 chain :303-308) —
  K has the `1/e^2` normalization form: dimensionless in unit terms, magnitude-typed (a
  response/impedance size, not a phase). And its physical occupancy is not derived:
  "`alpha_micro(K)` as generic normalization identity = DERIVED; `alpha_micro = 1/(4 pi K_*)`
  with `K_*` physical = CARRIED/ADOPTED, TYPE-U" (F0 chain :306-308).
- **K is typed of record NOT as write strength but as induced-response magnitude.** The OBS-05
  test "types `K` as a local surrogate or projected coefficient of exact induced connection
  response in the retarded action-Hessian problem, and not as write strength" (pair test
  :125-129); net typing: "`K_*` is a cell-level onset/root value of the Gamma_K/BR stationary
  closure problem … a surrogate coordinate for an induced connection response, conditional on
  the induced-only branch and on the unbuilt Gamma_K machinery" (pair test :131-134; same
  typing in CODEX2 AS3 :190).
- **C_record's admitted data are apparatus data.** "Its admitted data include geometry, clock,
  measure, regulator, action partition, state/domain, spectrum, duration, and the matching
  floor"; the defining form is "continuous-data, not a ratio of counts" (CODEX2 AS4 :214-219).
  None of these classes is a surface object: the surface has NO native metric argument (§I.1),
  NO fiber metric (§I.4), and no measure producer (the F-GK3 root — decision :67-69); the
  hunt's three beta-sensitive junction classes (fiber radius ADOPTED, D_BR spectrum UNBUILT,
  skeleton embedding GAP) are "each off the surface" (hunt §1.3/surface §III.12).

### 3.2 The typing, derived

The question's two horns: (a) a dimensionless invariant of derived (beta-invariant) surface
structure — the type of the four §II.9 locks; (b) a magnitude-valued quantity whose meaning
consumes apparatus/conversion structure. At the bytes above, K* is horn (b) in every tested
respect: it is bytes-typed a magnitude (stiffness; impedance; induced-response coefficient;
`1/e_HL^2` form — where "magnitude-valued" means modulus/response-strength-typed, exactly the
I2 result's sense, NOT "carries units": K* is unit-dimensionless and still magnitude-typed);
it is defined on NO surface structure (every admitted datum of `C_record` is a Gamma_K/BR
apparatus object); and it is not derived (residual unappointed; F-RP1 FIRED; physical selection
TYPE-U; the assembly formula ADOPTED). It is NOT horn (a): no defining ingredient is a §I-§II
clause or a displayed lock, and its derived status is PARTIAL of record (CODEX2 F0_VERDICT).

**On the beta-consumption sub-question specifically, the honest quantifier:** whether K*'s
meaning consumes the internal-to-external conversion CANNOT be occurrence-decided today,
because the executable `C_record(K)` formula in which occurrences would be counted does not
exist (CODEX2 link 9 ABSENT). What is decidable at bytes: the defining problem's admitted data
INCLUDE geometry/clock/regulator/spectrum classes, and its lineage's only visible
coupling-carrier with a conversion occurrence is the rail (`K_KK ~ beta^2`, imported-not-derived
— FABLE §5, OPUS5 Claim 1, both consistent with the hunt). OPEN, and named as such — not
resolved by the FABLE F1 shortcut (§2.4).

### 3.3 Vacancy engagement

The I2 determination (audited, confirmed) establishes: on the bare surface "the in-amplitude
slot is booked absent with exact mechanism … A strength has nothing on-record to act on" (I2
§3.2), the lack is "structural, not informational" (§3.5), and the missing referent is exactly
"a derived, beta-invariant, magnitude-valued internal->external response junction on the write
structure" (§0, §3.7). K* per §3.1 is precisely an apparatus-side object OF THAT TYPE — a
magnitude-valued induced-RESPONSE coefficient. Therefore: **any surface trace for K* that lands
magnitude-typed content on the write structure is structurally impossible on today's booked
surface — the vacancy constraint ENGAGES — and K* is APPARATUS-ONLY as of today** (the same
constraint the relevance map applies to kappa_record at its item 27, engaged here for the same
type-reason).

Per the CALIBRATION clause, that is a status with three lawful exits, the FIRST primary:
(1) EARN the trace on structure the surface DOES carry — the surface is phase-rich by bedrock,
and the §II.9 onset bound is phase/action-typed, derived, beta-invariant: the corpus's own
converse inventory names it "the plausible eventual consumer" of K*'s onset naming (relevance
map U-11 :471-473). That exit is exactly the absent theorem of §2.5 — questions (1) and (2)
close or stay open together; (2) discard as scaffolding — NOT indicated: K* is the principal
decision's alpha-stiffness of record, and this determination adjudicates standing, not worth;
(3) new booked surface amplitude structure (the held amplitude-junction construction route,
5fa46838 — principal-gated, per the guard's RELEVANCE BURDEN clause and the I2 supply route).

### 3.4 THE PUSHBACK FORK, applied (both horns stated; neither decided)

The pushback: the program's named alpha-stiffness refuses to trace to the program's own surface.

- **Horn (a) — MACHINERY-FIT:** K* carries structure the actual surface does not carry: it is
  magnitude/response-typed on a Gamma_K/BR carrier with metric, measure, and spectrum data,
  while the actual surface is phase-rich/amplitude-poor with the magnitude slot booked absent
  by exact mechanism (TYPE-R) and audit-confirmed. Standing: strong and structural (I2 +
  four-verifier hunt). On this horn, K* as currently defined is machinery through and through,
  and its alpha role rides on the unbuilt S16 transport of an apparatus object.
- **Horn (b) — SURFACE-DESCRIPTION-FIT:** the surface of record is a consolidation of BOOKED
  results and "may under-describe the actual surface" (guard, PUSHBACK FORK). The onset bound
  is displayed in the surface document as a NAME (form undisplayed); its saturation is adopted,
  not derived; and the actual surface's onset structure could be rich enough to host
  C_record's onset content — the corpus itself flags the onset bound as K*'s plausible eventual
  consumer. A booked absence records what has been derived, not necessarily what the actual
  surface carries.
- **What would decide between them:** the appointed executable `C_record(K)` (link 9) plus the
  §2.5 theorem. If the theorem lands C_record's onset on the FS budget, horn (b) was right and
  K* acquires a partial surface trace (typing upgrades toward SURFACE-DERIVED at that
  landing). If the completed residual's onset content provably consumes only apparatus objects
  with no phase/action landing, horn (a) was right and K*'s alpha role stands or falls with
  S16's transport of an apparatus quantity. Neither is decided here; the fork stays open.

---

## 4. QUESTION (3) — THE REFERENT: what does surface §IV's "K_*" name?

**ANSWER: THE SAME OBJECT as the decision's K* — an identity of referent, established at
bytes, not by name; and the §II.9 "onset bound" is NOT that object — the "onset" token is a
homonym across the two clauses.**

### 4.1 The identity, at bytes

Surface §IV.13 :63-70: "S16 — threshold-conditioned Thomson matching, the bridge to alpha
(K_* -> kappa_Thomson) … Slot 16 must transport a cell quantity to a continuum coupling
(principal act, K_* the onset root; the relocated hard problem)." Every load-bearing phrase is
the principal decision's own: "(principal act …)" is an explicit citation; `K_* -> kappa_Thomson`
is the decision's re-posed slot 16 verbatim (:15-16); "transport a cell quantity to a continuum
coupling" is the decision's new-obligation banner (:46-47); "the relocated hard problem" is the
decision's "The ruling relocates a hard problem" (:54-55); "the onset root" is the decision's
ruling 1 (:11). The surface document authors no new physics by its own charter (:3-8) — §IV
consolidates the principal act's naming. Referent chain: surface §IV K_* -> the principal
decision's K_* -> the displayed defining condition `C_record(K_*) = 0` (v002 :95-97,:104 as
carried at the two sealed carriers, §1.2). **IDENTITY.** Consistently, §IV books S16 as
"underived … TYPE-U" — the surface names the decision's object as the open edge's cell foot;
it does not claim to have derived or absorbed it.

### 4.2 The homonym, at bytes

Two different objects wear "onset" in this corpus: the **onset ROOT** (K*: root of `C_record`
in the K-indexed stiffness family — microscopic-action side, magnitude-typed, underived) and
the **onset BOUND** (`J_FS,rel >= pi hbar/2`: FS relative-action budget — internal
phase/action geometry, derived lower bound, adopted saturation, beta-invariant lock). Their
lineages never co-occur (§2.2). They are related by NO booked derivation (§2). The sole text
binding them into one object is the FABLE F1 line, adjudicated unsupported at §2.4 — the
homonym trap, instantiated once, caught by this determination rather than by the artifact's
own checker. Decision at bytes, per the tasking: **§IV's K_* = the decision's K* (identity);
§II.9's onset bound = a different object (homonym relative to K*'s "onset root" naming).**

---

## 5. FLAG BLOCK

```text
TRACE_STATUS = NOT-TIED — no booked derivation ties K* (the onset root of C_record, per the
    displayed definition of record) to the surface's SII.9 onset-bound lock (J_FS,rel >= pi
    hbar/2, the hunt's "onset inequality"); the absence is two-sided at bytes (the lock's
    lineage carries zero K_*/C_record/stiffness tokens; K*'s 16-span defining census carries
    no onset-bound span; the census 04c10c0d carries zero K_* tokens); the ONE contrary text
    (FABLE F1 :124-125 "onset root of the onset inequality — one of the four derived locks",
    echoed OPUS5 :93, unsealed) cites spans containing zero K_* occurrences, contradicts the
    definition of record and the sealed TYPE-U/ADOPTED status stock, and is reported as a
    hole (a missed unforced identification), not repaired; the tie-creating derivation is
    named exactly (S2.5): an appointed executable C_record(K) plus a booked theorem
    identifying its onset content with the FS onset budget — the exact theorem the onset
    gate's own bar demands, with saturation currently ADOPTED not derived.
TYPING = TYPED-MAGNITUDE — K*'s defining condition types it at bytes as a magnitude-valued
    (modulus/response-strength-typed, though unit-dimensionless) induced-response/impedance
    quantity: "stiffness is a magnitude" (two-stiffness :52); impedances-of-access-conditions
    Z_F = 1/kappa_F (decision :23-25); K e_HL^2 = 1 form (F0 chain :303-308); "local surrogate
    or projected coefficient of exact induced connection response … not as write strength"
    (pair test :125-134); defined WHOLLY on Gamma_K/BR apparatus data ("geometry, clock,
    measure, regulator, action partition, state/domain, spectrum, duration" — CODEX2 AS4),
    none surface-native; NOT a dimensionless invariant of derived beta-invariant surface
    structure (no defining ingredient is a SI-SII clause or displayed lock; residual
    unappointed, F-RP1 FIRED, physical selection TYPE-U, assembly formula ADOPTED Q-140 N11);
    beta-consumption as-defined OPEN of record (no appointed formula to occurrence-type —
    the FABLE F1 beta-blind shortcut is unsupported).
REFERENT_IDENTITY = IDENTITY at SURF SIV (SIV.13's "K_* the onset root" cites the principal
    act; K_* -> kappa_Thomson, "transport a cell quantity", "relocated hard problem" are the
    decision's own bytes — same referent as the decision's K*, hence the C_record onset root)
    + HOMONYM at SII.9 (the "onset bound" J_FS,rel >= pi hbar/2 is a DIFFERENT object from
    K*'s "onset root": distinct lineages, zero co-occurrence, no booked relation; the one
    text merging them is the S2.4 hole).
VACANCY_ENGAGEMENT = ENGAGED for any magnitude-side landing — K* is exactly an apparatus-side
    object of the I2 result's missing-referent TYPE (a magnitude-valued internal->external
    response coefficient), and the bare surface's magnitude slot is booked structurally
    absent (I2, audited CONFIRMED); therefore K* = APPARATUS-ONLY as of today — a STATUS per
    the guard's CALIBRATION, not a verdict: exit (1), primary, is a booked phase/action-
    structure landing (the SII.9 onset bound is the corpus's own named plausible consumer,
    relevance map U-11), which is identically the S2.5 theorem; exit (3) is the held,
    principal-gated amplitude-junction construction (5fa46838). PUSHBACK FORK held open:
    machinery-fit horn (strong, structural: I2 + four-verifier census) vs
    surface-description-fit horn (live, unbooked: the surface displays the onset bound
    name-only and its saturation is adopted); decided by neither here.
CHOICE_LEDGER =
    C-1 corpus-search boundary — PREMISE(named): the two tasked roots recursively, *.md,
        with one wider negative diligence probe (find over all of "/Users/bgm/MB Work" for
        the absent v002 principle file); all quantifiers ("no booked derivation", "zero
        occurrences") range over this boundary; spans in fenced files (register/road/ledger/
        lens/plan/tracker), in Documents roots, or in a32_holdout are not swept by me —
        partially mitigated at second hand by the CODEX2 census's own 1,787-file cleanroom
        sweep (register included, its D01/D02), cited at its CLAIMED custody.
    C-2 fenced-class exclusion by FILENAME (register|road_|ledger|lens|plan|tracker, plus
        THE_HANDOFF excluded as plan-adjacent; one grep hit line in it, used only to locate
        my tasking, no content consumed) — OPEN at the margin: a register-class file under a
        nonstandard name would evade the filter; named, quantifier narrowed accordingly.
    C-3 taking K*'s definition of record from its two sealed display carriers (decision
        :12-13; two-stiffness :17-18, digest double-attested) while the cited
        primitive_record_cell_selection_principle_v002.md file is absent everywhere under
        "/Users/bgm/MB Work" — PREMISE(named): TRACE-INCOMPLETE(named) for the file; the
        displayed bytes are mutually consistent and are what every downstream sealed
        authority itself quotes.
    C-4 identifying SURF SII.9 "the onset bound" = hunt S1.3 "the onset inequality" = hunt
        S1.1 "Onset budget J_FS,rel >= pi hbar/2" — IMMATERIAL(derived in-artifact): the
        surface's SII.8-9 cites the hunt as its source; the hunt's S1.3 fourth derived
        junction ("the onset inequality") is its S1.1's displayed budget, the only
        inequality in its internal-side display; the I2 determination's Chain D row and the
        inventory :174-175 carry the same single object.
    C-5 no CAS opened — IMMATERIAL(proven by inspection): every load-bearing step is a
        byte-reading (occurrence counts, displayed-type quotations); no symbolic identity
        was asserted that required checking; fence-safe by construction.
    C-6 custody grading — PREMISE(named): OPUS5 check (9c9e120e) and onset gate V003
        (cc113a16) carry NO sidecar; each is consumed only at flagged custody, and nothing
        in the NOT-TIED verdict rests solely on either (S2.2 stands without them).
    TOY_SEPARATION (self-assessment; the independent audit re-derives): clean — no
        load-bearing step rests on an unproven convenience; the two custody-flagged sources
        are corroborative only.
NET = SPLIT, at three quantifiers: the alpha-formula's stiffness does NOT land on any surface
    lock today (NOT-TIED — and the single sealed sentence suggesting otherwise is a
    caught homonym-hole, FABLE F1 :124-125/OPUS5 :93); K* is bytes-typed a magnitude-valued
    induced-response apparatus quantity, so the I2 vacancy engages and K* is APPARATUS-ONLY
    as of today, with its primary lawful exit (a booked landing of C_record's onset content
    on the phase/action-typed SII.9 onset bound) being exactly the same absent theorem —
    questions (1) and (2) open and close together; and the surface's SIV "K_*" is the
    decision's K* by identity of referent while SII.9's "onset bound" is a homonymous
    different object. Nothing here prohibits K*: per the PURPOSE clause this is the
    inventory of named gaps — one absent authority file (v002), one absent executable
    residual (link 9), one absent tying theorem (S2.5), one unsupported identification to
    strike or re-derive (F1), and one principal-gated construction alternative (5fa46838).
```

---

Fences at close: alpha_computed = false; proof_authorized = false; kappa_record_computed =
false. No value of K*, kappa_record, kappa_Thomson, beta, or alpha was computed, bounded,
estimated, or compared; every numeral above is a sealed-text quotation. One output file; seal
alongside. KST-BUILD ends.
