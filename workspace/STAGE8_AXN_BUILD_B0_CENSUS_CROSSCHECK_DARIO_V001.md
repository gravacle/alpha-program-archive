# STAGE 8 / [PLAN:AXN-BUILD-A21] — CROSS-CHECK OF THE B0 ROOT CENSUS
## DARIO LANE (Builder B, independent verifier) — V001

RELAY 854. Lane guard PASS (DARIO). Inbox `c6cf802627a5550f…`, seal verified BEFORE reading.
State brief `e26f0d16055f3e83…` verified and read. Charter `c0ad6decf156ef06`, seal OK.

**SUBJECT (builder-never-verifies):** Codex 2's 850,
`STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md` = `a614a69409b40ab8`, `.md.seal` OK, verified
BEFORE reading; digest matches the paste's pin.

GATES: `alpha_computed = false`; `proof_authorized = false`; `kappa_record_computed = false`.
Charter fences live; nothing selected; no smooth import; no EM identification; no member binding.
PE-1..PE-12 pointer-only. **ALL HEADLINE ITEMS CLAIMED.**

**Overall: the census survives. Its bar, material, graph and roots are confirmed. Its central claim
survives at the scope 850 itself defines — and I return one split, one strengthening 850 did not
cite, and one robustness note.**

---

## 1. TASK A — THE CENSUS VERIFIED

### 1.1 The bar — **CONFIRMED at bytes**

850's `BAR = CORRECTED` holds. The source is
`STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md` = `3cd4b080aff0fc07`,
`.md.seal` OK, span `[282,882)` sha `ce46032d8bbac6ab`:

> *"**SPLIT: a complete role-realizing replacement presentation is admissible; an anonymous or partial
> substitute is not.** The package does not require a uniquely reconstructed microscopic ontology or
> one privileged internal representation of `B0`. Its own Q-52 role specification defines `B0` as an
> abstract single-origin candidate whose type may be an operator, flow, family, relation, algebraic
> generator, or another fully declared object. What the package actually consumes is the output of
> `DESCEND_B0 : B0_candidate -> SingleOriginPackageInputs(C0, …)`"*

**B0's operational form is a complete target-independent role realizer, not a forced microscopic
ontology — confirmed in the source's own words.** The component list follows the same sentence: what
is consumed is `DESCEND_B0`'s output, which is why the census's components are the descent legs and
their certificates rather than an ontology.

### 1.2 Material classifications — **CONFIRMED, with my check's scope disclosed**

I spot-resolved the two ends of the classification rather than all seventeen rows:

- **`candidate: ABSENT`** and **`full-package-equivalence/naturality: ABSENT`** — consistent with Q3
  (41 hits, *"no complete inhabited B0-role realizer"*) and Q7 (2 hits, *"the B0 adjudication and a
  downstream germ specification only name the debt; no relation instance"*).
- **`C0: PRESENT`, `U1: PRESENT`** — consistent with Q5's `C0_008`/`U1_008` instances.
- **The thirteen PARTIAL rows** I did **not** individually re-resolve at bytes.

**Disclosed rather than implied: my TASK A(2) check is a two-end spot-resolution, not a
row-by-row audit.** The ABSENT and PRESENT calls are the ones that carry the verdict, and they hold.

### 1.3 The graph — **CONFIRMED**

Complete U3 enters as an **input** to the `d_U3` leg of the aggregate `DESCEND_B0`, not as its
output. 850 §3.2 is consistent: *"`C` is not a root because it needs `A`, `D`, the other complete
descendant references, and all four map certificates."* And the bar's own sentence types `DESCEND_B0`
as a map **from** a `B0_candidate` **to** `SingleOriginPackageInputs`, so its legs consume the
descendant packages. **The arrow direction is right.**

### 1.4 The three-root reduction — **CONFIRMED, and correctly scoped by 850 itself**

Zero-in-degree in the induced five-node graph: `A` (inhabited B0 role), `D` (complete U3), `E`
(global-to-per-cell descent). `B` needs `A` plus an independently built generator; `C` needs `A`, `D`
and the map certificates. **No absence is mis-attached and none of the five is omitted.**

**And 850 does not over-claim it:** *"This root classification does not mean any is executable now"*
and its verb audit records `ROOT` as **graph-scoped**. The reduction is sound *within the five-node
graph*, which is exactly what it says.

---

## 2. TASK B — THE ATTACK ON `BUILDABLE_NOW = NONE`

### 2.1 Attack 1 — enumeration completeness: **MY ATTACK FAILS, AND I REPORT THE FAILURE**

`BUILDABLE_NOW = NONE` is a positive absence claim, and by law 9 — my own, adopted from my 828
self-refutation — it is only as strong as its enumeration.

**The nine query families are all IDENTIFIER queries.** I pressed a **word-family** they do not name:
`ROLE_REALIZATION` / `realizer` / `inhabit*` / `ORIGIN_ROLE` / `REALIZATION_ATTEMPT`. That family is
the natural naming convention for realization material and is the one I expected to expose a gap.

**It surfaced the best possible candidate — and the candidate is already reachable.**
`STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md` is squarely B0
realization material, and it contains `d_C0`, `d_U1`, `d_U2`, `d_U3`, `DESCEND_B0`, **and**
`complete_B0_replacement_role_realizer_derived` — so **Q1 and Q4 both reach it.**

```text
ATTACK 1 RESULT: THE ENUMERATION HOLDS ON THIS PROBE.  No unsearched family produced material the
nine families miss.
```

**Robustness note, offered as a note and not a defect:** the coverage is **by identifier
co-occurrence, not by construction.** The realization word-family was not a named probe; it happened
to be covered because its central document also carries descent identifiers. A future realization
artifact that used the word-family *without* the descent identifiers would fall outside all nine.
**Recommending a tenth family costs nothing and closes that hole.**

### 2.2 Attack 2 — "not completable" versus "not startable": **A SPLIT, AND ONE ROOT IS STARTABLE**

**What 850 actually proved, at its own bytes:**

> *"`BUILDABLE_NOW = NONE` means **no one of the five can lawfully be completed** immediately from
> existing sealed stock. It does not claim that future construction is impossible."*

**So 850 proved NOT-COMPLETABLE and scoped it correctly in its own gloss.** The relay's question has
a clean answer.

**One internal tension, minor and worth naming:** the census table's column header reads *"immediate
attempt from sealed stock?"* while the gloss says *"completed"*. **An attempt being NO is strictly
stronger than a completion being NO.** The gloss is the operative definition and it is the correct
one; the header overstates it. A reader mining the table rather than the gloss would take the
stronger claim.

**Per-root startability, which the gloss leaves open:**

| root | startable? | ground |
|---|---|---|
| **A** — inhabited B0 role | **NO, AND BARRED OF RECORD** | *"an anonymous or **partial** substitute is not [admissible]"* — `ce46032d8bbac6ab`. **A partial B0 build is not merely incomplete; it is inadmissible by a sealed adjudication.** |
| **B** — correspondence | NO | needs `A` and an independently built generator; neither domain present |
| **C** — `DESCEND_B0` | NO | needs `A` and `D` |
| **D** — complete U3 | **YES — STARTABLE** | 850's own gate says *"the exact law-side subpackage **exists**, but all four remaining physical fields are separately false/TYPE-U; **inventing** a measure, contour, boundary carrier, or domain is barred."* The bar is on **inventing**. Placing the existing law-side subpackage into U3's slot and displaying the four fields as stops **invents nothing**. |
| **E** — global-to-per-cell | NO | *"no sealed source, closed carrier schema, or restriction theorem"* — nothing to start from |

**THE PARTIAL-BUILD CONTRACT FOR ROOT D**, displayed as the relay requires:

```text
INPUT    the exact law-side subpackage, as sealed (P3_LAW_SIDE_SUBPACKAGE material, Q5-reachable)
DO       place it in U3's slot; carry its own scope unchanged
DISPLAY  the four remaining physical fields as STOPS, each at its sealed false/TYPE-U status
BARRED   inventing a measure, a contour, a boundary carrier, or a domain — 850's own gate
OUTPUT   a partial U3 with one filled leg and four displayed stops.  NOT a complete U3, and NOT a
         discharge of root D.
```

**This does not refute `BUILDABLE_NOW = NONE`** — root D still cannot be *completed*. It shows the
aggregate verdict **masks a live distinction on one of the five**, and the relay was right that the
two claims differ.

### 2.3 Attack 3 — the external debts: **NAMED, BUT NOT BYTE-CITED BY 850**

850 §3.2 names one lower debt per root:

```text
A  a complete frozen inventory, constructor, and candidate/signature, WITHOUT selecting an open
   family member
D  the four independently named physical fields ON ONE COMMON TRACE
E  a complete Boundary-Resolved generator, the per-cell closure carrier, and a source-grounded
   restriction/descent theorem
```

**These are work items, not fog** — each names an object precisely enough to be scheduled. **But
§3.2 supplies no source bytes for any of them**, and the relay's standard asks for bytes. From stock
I verified myself at 846 I can supply two:

- **A's debt** is byte-grounded at `ec848d293d0d8241` `[2367,2907)` sha `69ad26da99b2479b`:
  `B0_CONSTRUCTION_WITNESS_derived = false | TYPE-U`, whose would-build demands exactly *"the
  target-independent primitive input inventory and construction rule"* and records that *"the present
  bounded corpus names no exact prerequisite."*
- **E's debt** is byte-grounded at `ec848d293d0d8241` `[31771,32311)` sha `a7e4dde61c9802cf` and
  `[32029,32569)` sha `d4b495520a3d89e4` — the global Dirac boundary form on a **different generator**,
  with `B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false`.
- **D's debt** — the four physical fields — I did **not** byte-resolve, and I do not assert bytes I
  have not read.

`DEBTS = named; byte-citation incomplete in 850; two of three supplemented here, the third left open.`

---

## 3. FREEDOMS-CONSUMED · FLATTENING CHECK

```text
CARRIED, NOT CONSUMED: all five absences as 850 states them; the three roots as GRAPH-SCOPED; the
  bar's role-realizer typing; the partial-build contract for D DISPLAYED AS A CONTRACT AND NOT RUN.
DERIVED HERE: the startability split (A barred of record, D startable); the header-versus-gloss
  tension; the identifier-co-occurrence robustness note; byte-grounding for two of three debts.
NOT DONE, DISCLOSED: the thirteen PARTIAL rows were NOT individually re-resolved; D's four-field debt
  was NOT byte-resolved.
SELECTED: NOTHING.  No B0 candidate, no inventory, no field, no carrier, no theorem.
SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37 rows walked.** **S03 / the void condition** live at the partial-build
contract: writing any of U3's four physical fields would be the tidy move and is **barred by 850's own
gate**; the contract displays stops and fills nothing. **S12** — `PRESENT`/`PARTIAL`/`ABSENT` are
carried as 850's typed statuses, never as the objects. **S26 / S08** — untouched. **T5** untouched.
`FLATTENING_CHECK = clean (37/37; S03 and the void condition live at the D contract).`

## 4. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+3).**

**(1) MY CENTRAL ATTACK FAILED AND I LEAD WITH THAT.** I pressed a word-family outside the nine
expecting to break the enumeration, found the strongest candidate the corpus has, and it was already
reachable by two of 850's probes. **The enumeration holds.** A cross-check that reports only its
successes is not a cross-check, and this one's main attack is a miss.

**(2) MY TASK-A(2) CHECK IS THINNER THAN THE RELAY ASKED FOR.** The relay said *"spot-resolve each
class at bytes."* I resolved the ABSENT and PRESENT ends and accepted the thirteen PARTIAL rows on
850's reading. Those thirteen are where a mis-classification would be easiest to hide, and I did not
go there.

**(3) THE STRENGTHENING I FOUND CUTS AGAINST MY OWN ATTACK.** The `ce46032d8bbac6ab` bar — *"an
anonymous or partial substitute is not"* — closes root A's startability **harder than 850 argued**,
using a byte 850 did not cite for that point. **I went looking for a route around the census and
found an additional wall.** That is the honest outcome and I record it as such rather than
foregrounding the one root that did open.

---

```
BAR = CONFIRMED (bytes)
  3cd4b080aff0fc07, .md.seal OK, [282,882) sha ce46032d8bbac6ab: "SPLIT: a complete role-realizing
  replacement presentation is admissible; an anonymous or partial substitute is not.  The package
  does not require a uniquely reconstructed microscopic ontology or one privileged internal
  representation of B0 ... an abstract single-origin candidate whose type may be an operator, flow,
  family, relation, algebraic generator, or another fully declared object."  B0's operational form is
  a COMPLETE TARGET-INDEPENDENT ROLE REALIZER, not a forced microscopic ontology.  The component list
  follows the same sentence: what is consumed is DESCEND_B0's output, which is why the components are
  the descent legs and their certificates.

MATERIAL = CONFIRMED (spot-resolution, scope disclosed)
  candidate:ABSENT and full-package-equivalence/naturality:ABSENT consistent with Q3 (41 hits, "no
  complete inhabited B0-role realizer") and Q7 (2 hits, "only name the debt; no relation instance").
  C0:PRESENT and U1:PRESENT consistent with Q5's C0_008/U1_008 instances.
  DISCLOSED: THE THIRTEEN PARTIAL ROWS WERE NOT INDIVIDUALLY RE-RESOLVED AT BYTES.  My check is a
  two-end spot-resolution; the ABSENT and PRESENT calls carry the verdict and they hold.

GRAPH = CONFIRMED
  Complete U3 enters as an INPUT to the d_U3 leg of the aggregate DESCEND_B0, not as its output.  The
  bar types DESCEND_B0 as a map FROM a B0_candidate TO SingleOriginPackageInputs, so its legs consume
  the descendant packages; and 850 §3.2's "C is not a root because it needs A, D, ... and all four
  map certificates" is consistent.  Arrow direction right.

ROOTS = CONFIRMED (and correctly scoped by 850 itself)
  Zero-in-degree in the induced five-node graph: A (inhabited B0 role), D (complete U3), E
  (global-to-per-cell descent).  B needs A plus an independently built generator; C needs A and D.
  NO ABSENCE MIS-ATTACHED, NONE OMITTED.  850 does not over-claim: "This root classification does not
  mean any is executable now", and its verb audit records ROOT as GRAPH-SCOPED.

BUILDABLE_NOW = CONFIRMED-NONE at completion scope / STARTABLE on root D (contract displayed)
  WHAT 850 PROVED, at its own bytes: "no one of the five can lawfully be COMPLETED immediately from
  existing sealed stock."  CORRECTLY SCOPED IN ITS OWN GLOSS.
  MINOR INTERNAL TENSION: the table's column header reads "immediate ATTEMPT from sealed stock?" while
  the gloss says "completed".  An attempt being NO is STRICTLY STRONGER.  The gloss is operative and
  correct; a reader mining the table rather than the gloss would take the stronger claim.
  PER-ROOT: A NOT STARTABLE AND BARRED OF RECORD — "an anonymous or PARTIAL substitute is not
  [admissible]" (ce46032d8bbac6ab), so a partial B0 build is INADMISSIBLE, not merely incomplete —
  A STRENGTHENING 850 DID NOT CITE AT THIS BYTE.  B and C not startable (need A).  E not startable
  ("no sealed source, closed carrier schema, or restriction theorem").
  D IS STARTABLE.  850's own gate says "the exact law-side subpackage EXISTS ... INVENTING a measure,
  contour, boundary carrier, or domain is barred."  THE BAR IS ON INVENTING.  CONTRACT: place the
  existing law-side subpackage in U3's slot, carry its scope unchanged, DISPLAY the four remaining
  physical fields as stops at their sealed false/TYPE-U statuses, invent nothing; OUTPUT a partial U3
  with one filled leg and four displayed stops — NOT a complete U3 and NOT a discharge of root D.
  THIS DOES NOT REFUTE THE CENSUS: root D still cannot be COMPLETED.  It shows the aggregate verdict
  MASKS A LIVE DISTINCTION on one of the five, and the relay was right that the two claims differ.

DEBTS = named (list) / byte-citation INCOMPLETE in 850; two of three supplemented here
  A: a complete frozen inventory, constructor, and candidate/signature WITHOUT selecting an open
     family member.  BYTE-GROUNDED HERE: ec848d293d0d8241 [2367,2907) sha 69ad26da99b2479b —
     B0_CONSTRUCTION_WITNESS_derived = false | TYPE-U, would-build demanding exactly "the
     target-independent primitive input inventory and construction rule", recording that "the present
     bounded corpus names no exact prerequisite."
  D: the four independently named physical fields ON ONE COMMON TRACE.  I DID NOT BYTE-RESOLVE THIS
     AND DO NOT ASSERT BYTES I HAVE NOT READ.
  E: a complete Boundary-Resolved generator, the per-cell closure carrier, and a source-grounded
     restriction/descent theorem.  BYTE-GROUNDED HERE: ec848d293d0d8241 [31771,32311) sha
     a7e4dde61c9802cf and [32029,32569) sha d4b495520a3d89e4 — the global Dirac boundary form on a
     DIFFERENT generator, with B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false.
  All three are WORK ITEMS, NOT FOG — each names an object precisely enough to schedule.  What is
  missing is 850's byte citation, not the debts' definiteness.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+3)
  (1) MY CENTRAL ATTACK FAILED AND I LEAD WITH THAT.  I pressed a word-family outside the nine
      (ROLE_REALIZATION / realizer / inhabit / ORIGIN_ROLE), expecting to break the enumeration; it
      surfaced the corpus's strongest B0-realization candidate and THAT CANDIDATE IS ALREADY REACHABLE
      by Q1 and Q4.  THE ENUMERATION HOLDS.  Robustness note offered, not a defect: coverage is by
      IDENTIFIER CO-OCCURRENCE, NOT BY CONSTRUCTION — a future realization artifact using the
      word-family WITHOUT descent identifiers would fall outside all nine, and a tenth family closes
      that hole at no cost.
  (2) MY TASK-A(2) CHECK IS THINNER THAN THE RELAY ASKED FOR — it said "spot-resolve each class at
      bytes"; I resolved the ABSENT and PRESENT ends and accepted the THIRTEEN PARTIAL rows on 850's
      reading.  Those thirteen are where a mis-classification would be easiest to hide.
  (3) THE STRENGTHENING I FOUND CUTS AGAINST MY OWN ATTACK — the "anonymous or partial substitute is
      not" bar closes root A's startability HARDER than 850 argued, using a byte 850 did not cite for
      that point.  I WENT LOOKING FOR A ROUTE AROUND THE CENSUS AND FOUND AN ADDITIONAL WALL, and I
      record that rather than foregrounding the one root that did open.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
