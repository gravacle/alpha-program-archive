# Stage-8 T7 Arm-2 Sharpening and Projection-Tail Binding V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_BINDING SUPPLEMENT — BINDS THE E1 SUCCESSOR SPEC v002
(applied AT SEAL by the construction lane; the drafting lane was
launched before this relay — disclosed, per the standing pattern)
```

Consumes the independent-system lane's Assignment-C return on A-L0 arm 2.

## The honest split, exactly as pre-predicted

Both factors of the arm-2 target now carry as CONTINUUM BOUND-CLASS
statements — and NEITHER YET as an object theorem uniformly over D3.

```text
(i) HUYGENS COLLAR (the R^3 -> R^2 shell reduction). The unprojected
    massless Dirac bridge is light-cone supported; Dyson insertions
    localized INSIDE the endpoint cells create no macroscopic interior
    propagation — they thicken the null relation by endpoint cell
    diameters / time supports. CTP subtraction does not enlarge support.
    COST, STATED: this is NOT sharp Huygens for arbitrary dressed
    operators. A nonlocal Galerkin/projector implementation can destroy
    exact support AT FINITE CUTOFF, and a genuinely background-dressed
    bridge with intermediate MACROSCOPIC insertions would fill the
    timelike cone.
(ii) >= R^-1 AFTER TIME INTEGRATION. The free Dirac propagator is a first
    derivative of the 3+1 scalar light-cone kernel
    (~ gamma.partial [delta(t-r)/(4 pi r)]); the delta' terms integrate
    by parts onto the smooth cell profiles, and the boundary terms VANISH
    because the sealed smooth causal-diamond cutoffs vanish to all
    orders — leaving leading O(R^-1) with lower-order O(R^-2).
```

## THE REPAIR SURFACE — now three concrete named items

```text
NAMED LEMMA:
  E1_FREE_LINE_HUYGENS_COLLAR_AND_TIME_INTEGRATION_R_MINUS_ONE
UNCERTIFIED COMPONENTS, each to be authored as its own obligation with
its own witness:
  U1 uniform control of the time-profile W^{1,1} / BV norms;
  U2 LIGHT-CONE TRANSVERSALITY over ALL common A/B refinements in D3
     (note: under the RATIFIED standard D3 reading, this quantifier is
     the unrestricted one — U2 is therefore the harder statement, not the
     overlay-only one);
  U3 the finite-Q PROJECTION-TAIL issue, if that cutoff is retained.
```

OBJECT-VS-BOUND MAINTAINED, per the principal's pre-registered tripwire:
the negative here is against the CURRENT BOUND-CLASS CERTIFICATION, NOT a
proof that the object lacks the decay. Any successor artifact reporting
otherwise must make the (a)/(b)/(c) declaration or BLOCK.

## THE PROJECTION-TAIL DEPENDENCY — bound explicitly, not discovered at execution

```text
IF the certified route RETAINS a finite-Q (Galerkin/finite-carrier)
cutoff, THEN factor (i) requires a PROJECTION-TAIL / LIMIT LEMMA ON TOP:
the nonlocal projector can destroy exact light-cone support at finite
cutoff, so collar support must be re-established either uniformly in Q or
in the Q -> infinity limit with certified tails.
THIS DEPENDENCY IS BOUND HERE so no lane discovers it mid-execution. The
E1 v002 spec must carry it as an explicit conditional obligation attached
to U3, and its verdict language must state which of the two it relies on
(uniform-in-Q, or limit-with-certified-tails).
```

## Calibration — Rule 6 applies and is applied

```text
The independent lane's pre-frozen C(ii) prediction — "morally present
from the free kernel after smooth time integration, but object-level
uniform certification over D3 remains open without a sealed
profile/transversality/projection-tail lemma" — LANDED VERBATIM. That is
the THIRD CONSECUTIVE CYCLE for the independent-system family.
RULE 6 (as amended) APPLIED HERE: this lane holds no competing
route-survival prediction on arm 2 that disagrees with the above; where
the E1 v002 spec's frozen predictions touch arm 2, they must state the
independent family's three-cycle record and WEIGHT IT, and record that
they did. Recovery of this lane's own family remains the PRINCIPAL'S
judgment on the reviewer lane's reading — not this lane's.
```

## Protected status

```text
arm2_factor_i_bound_class = true    (not an object theorem over D3)
arm2_factor_ii_bound_class = true   (not an object theorem over D3)
U1_time_profile_norms_uncertified = true
U2_lightcone_transversality_over_full_D3_uncertified = true
U3_projection_tail_uncertified = true
E1_one_line_cross_term_uncertified = true
connected_linked_cluster_density_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
