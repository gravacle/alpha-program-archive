# STAGE 8 — G2 PANEL, OVER-DERIVATION LENS (G2-PANEL-OVER) — [VERDICT]
## BLIND ADVERSARIAL VERIFIER, CROSS-LINEAGE — DEFAULT REFUTE — V001

Date: 2026-08-13
Build under test: `STAGE8_G2_STATE_PORT_DETERMINATION_V001.md`
sha256 recomputed at path: `f7fd0c6048dfec8f08bd02f2ae008bcb329ccf4c0d7cb5b14a8c25359b57767a` — MATCHES the tasked digest.
Panel discipline held: the other panel lens artifact was NOT opened (probe returned no
`STAGE8_G2_PANEL_*` file before this write); no register/tracker/plan/road/ledger/lens
file read; no git action; fences held (connection-only, scale-free, symbolic; nothing
evaluated, bounded, or computed numerically).

---

## 1. SEALS — ALL RECOMPUTED AT PATH BEFORE READING

All 20 source digests recomputed by `shasum -a 256` this session; every one matches the
build's SEALS_VERIFIED block byte-for-byte. Workspace root
`/Users/bgm/MB Work/alpha-program-archive/workspace/`; DoRs at `…/supervision/`.

```text
M06  d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98  MATCH
FORM 5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  MATCH
M03  2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f  MATCH
G3B  3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  MATCH
E1   468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  MATCH
PA   789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  MATCH (located from E1 :82)
U2   2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab  MATCH
P5A  1274b1b71b46e6a34b641c0053d61ce1ed16c94e8d570317a7831f558bcfef58  MATCH
P5D  a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874  MATCH (seal only; no load-bearing span cited from it in the flag block)
MO   576da30f300a0362469d6b4f447423a2298efc0dd28a60f38057e7f4cc8bd888  MATCH
NG   7000963e769362d87e17e36f06903e63aaa8caaeb284ee9c9f3676febe4f4163  MATCH
GOV3 1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0  MATCH (= DoR-013's cited prefix)
SGV4 d4cdbb6623797df6accb7dc9b24134d179bfc8e8d039c585d5c91ae23255869e  MATCH (= DoR-014's cited prefix)
D13  f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec  MATCH
D14  b6e4116df63403478d28be8cdb6589b091cc1aa8b6ad5a40776a28b135cd138f  MATCH
AXS1 32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842  MATCH
TH   47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba  MATCH
THC  7eaf5a748574add634a76c37de3abc2e81da2b861ed767dd24c9fd7c2279e4ed  MATCH
SF   73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31  MATCH
TA   a71d4e59fcde1a7df10e8051e46befb9b4b6653a0917bb03a0c0403179717fef  MATCH
```

M06 identity confirmed independently of the build: digest begins d13920e2 AND the file
carries the tasked flag strings at path — `UNIQUE_SCALARIZATION_FORCED = false | TYPE-R`
(:32), `PHYSICAL_SCALARIZATION_SURVIVOR_COUNT = NO_VERDICT` (:47-49), the
`omega_t := (1-t) omega_H + t omega_epsilon` family (:244), `RHO_PRE_PLACED = false |
TYPE-U` (:502), `U2_STATE_PORT_INSTANTIATED = false | TYPE-U` (:505).

PROVENANCE = CLEAN.

---

## 2. QUOTE-CHECK OF THE THREE MOST LOAD-BEARING SPANS

**(Q1) GOV3 §10 — the selector's forced form.** GOV3 :812 reads "After the anchor
theorem gives `rho_P=omega_tr`"; the display at :816 and :822-825 reads
`kappa_ch:=Tr_A(rho_P P_ch)` `=Tr_A(P_ch)/Tr_A(I_src)` `=dim(P_ch H_src^A)/[dim(P_0
H_src^A)+dim(P_ch H_src^A)]`; :828 "This symbolic ratio is not inserted into the anchor
and is not evaluated"; :835 `KAPPA_CH_STATE_FIBER_COUNT = 1 [PROPOSAL CONSEQUENCE]`;
:838 `KAPPA_CH_FORM_PROVEN_WITHOUT_TARGET_USE = true`. VERBATIM CONFIRMED. One span
defect: the fiber-count flag sits at :835, OUTSIDE the build's cited ":810-830" (five
lines below, in the code block immediately following the cited prose). Content genuine;
citation drift only.

**(Q2) D13 — the license and the type.** D13 :11-13: "every member forces the same
normalized-identity invariant state and the same symbolic sector-dimension-ratio form
for p_ch"; :11 "*** NO MEMBER IS SELECTED, EVER. ***"; :35-38: "A0 and the anchor are
AUTHORED PHYSICS — confirmed non-derivable from the existing transition structure … The
program adopts them knowingly". VERBATIM CONFIRMED at the build's cited spans (:9-13,
:34-38). Falsifiers at :26-32 (restriction reproduction; neutrality; no post-output
supplementation) — carried by the build, none suppressed.

**(Q3) D14 — the rank pair, the NO_VERDICT, the bar.** D14 :10: "(r_0, r_ch), r_0 > 0,
r_ch > 0 on the durable branch"; :13: "p_ch's symbolic form: FORCED to the sector ratio
of the realized pair. NO evaluation"; :14: "Pinning of the pair: NO_VERDICT of record";
:15 the K_square arm (capped, C29) as NAMED FUTURE DETECTOR; :25-27: "any construction
step that requires selecting a specific (r_0, r_ch) value VOIDS the step, not the
germ". VERBATIM CONFIRMED at the build's cited spans (:10-13, :14-17, :25-27).

Supporting spans also verified verbatim (not repeated in full): FORM :204-206 the
weights `w_0 = <E, P_0 rho>`, `w_ch = <E, P_ch rho>`; FORM :221 "`mu(w) ≡ 0 iff
w_0 w_ch = 0`"; M03 :205-213 the zero cross-history entries via `<R_N|P_N>=0`; MO
:123-128 the Sigma_src display; TA :385-386 "charge-superselected"; U2 :258-269 the
StatePort tuple, :281-285 `U2_STATE_PORT_INSTANTIATED = false | TYPE-U`, :288-310 the
inclusive module identity descending to the identity, :311-345 F4-F6 typed-no-instance;
P5A :205-213 "let the inclusive final effect be the identity" (primitive :17-25); AXS1
:229-232 "no sealed `Theta_hist(rho_S)=omega` exists … a typed relation over `Sigma_src
x F_1`, not a graph of a function"; TH :131-137 "marginals BOTH STILL FORCED … source
marginal = omega_tr ; record marginal = (1-kappa_ch)|r><r|^N + kappa_ch|p_Q><p_Q|^N"
with its TYPE-P premise line; TH :225-231 `THETA_HIST = STOPPED` naming "a MEASURE on
Y … NOT an arrow"; TH :217-222 the Dirac-measure refusal; TH :262-287 legs L1/L2/L3/L5
OPEN and `K4 = NOT-MET`; THC :252 "THETA_HIST = ROUTE-CONFIRMED (both routes stop at
the missing measure)"; SF :193-196 the record-marginal display with "kappa_ch =
Tr_A(P_ch)/Tr_A(I_src)"; M06 :464-468 the DoR-006 bar; NG :5-24 the C_BI refutation
with `C_BI_ADOPTED = false`; PA :71-72 "Neither may be selected by its output"; PA
:132-133 "explicitly a fixed-gauge finite response diagnostic"; E1 :402-403 "per-state,
reported separately, never promoted"; E1 :344-345 `alpha_x` inside a spinor trace (a
Dirac matrix, as the build classifies); G3B :235 "No transport law exists".

---

## 3. PER-CLAIM AUDIT — EVERY "PINNED/SELECTED" CLAIM ATTACKED

Numbered; verdict first.

**P1 SOUND — field-face constraint pins and the continuum survivor.** Each of the five
pins is reproduced at its span, and each is a sealed TYPE-R/TYPE-P display, not the
build's inference: positivity/Riesz-Markov (M06 :175-196), normalization removes only
zero+scaling (M06 :419-440), Theta_F-invariance with every omega_t fixed and b_lambda
separating (M06 :260-297), one-cell consumes only omega(1)=1 (M06 :364-403), rho_pre
type-only cuts nothing (M06 :335-360), survivor table continuum at every executable
stage (M06 :479-506), unchanged post-entry ("The entry adds no row to that table", TH
:213). No over-claim: the build carries M06's own NO_VERDICT on the physical subfamily.

**P2 SOUND — source-face inventory.** Sigma_src as displayed (MO :123-128; TA
:385-386); sector coherence killed by superselection plus the law's own
`<R_N|P_N>=0` identity (M03 :205-213); exactly one instantiated effect, the inclusive
identity (U2 :288-310; P5A :205-213). All sealed displays.

**P3 SOUND (disclosed assembly) — the executable-level simplex freedom.** AXS1
:229-231 seals "every charge-superselected rho_S" permitted with visible scalar
`p_ch = Tr(rho_S P_ch)`; the closed-simplex coordinatization `(w_0, w_ch) =
(Tr P_0 rho_S, Tr P_ch rho_S)` is the build's own algebra and is DISCLOSED as YOURS
(build §7). Exact one-line consequence of positivity, unit trace, and sector
diagonality. Not over-derived.

**P4 SOUND — the selector's forced content.** `rho_P = omega_tr` and the kappa_ch
form are sealed consequences of the ratified anchor (GOV3 :812, :816-826), licensed
family-wide by the proven neutrality certificate (D13 :11-13), with fiber count 1
(GOV3 :835 — SPAN DRIFT, see Q1). The build does NOT promote this to a derivation: it
carries D13 :35-38's AUTHORED PHYSICS / non-derivable typing verbatim, the TYPE-P
premise chain, and both falsifiers with their VOIDS force. This is the opposite of
over-derivation and it is the artifact's central honesty.

**P5 SOUND (disclosed assembly) — the weight pinning `(w_0, w_ch) = (1 - kappa_ch,
kappa_ch)`.** The identification w_ch = p_ch = kappa_ch consumes three sealed displays
(FORM's weights, AXS1's p_ch, GOV3's kappa_ch) plus the sealed inclusive-identity
effect, is one line of exact trace algebra, is disclosed as YOURS (build §7), and is
independently corroborated by a sealed display the build cites: SF :193-196 puts
EXACTLY kappa_ch as the forced record-marginal mixing weight, with TH :131-137
confirming both marginals forced. No unsealed idealization enters.

**P6 SOUND-CONDITIONAL — strict positivity and `mu != 0` (Outcome A), with one noted
exposure.** Strict positivity of both weights is inherited from D14 :10's sealed
premise (r_0 > 0, r_ch > 0 on the durable branch) through the symbolic ratio — an
order inference, not an evaluation; "nonzero" is the only property carried, matching
the fence. `mu != 0` then follows from FORM :221's sealed dichotomy. The build keeps
the whole thing TYPE-P and keeps magnitude/sign open. EXPOSURE, noted not fatal:
FORM D4's mu is defined relative to the READY-SANDWICH anchor (`F_N = P_0 + Z_N P_ch`
from the `|R_N>` sandwich), while the ratified chain's own forced RECORD marginal is
the mixed state (TH :134). The build flags exactly this (RECONCILIATION FLAG,
unresolved) but its sentence "nothing here depends on its outcome" (§6) is a shade
too strong for Outcome A's final display: the non-degeneracy of the D4 interference
anchor on this chain is a D4-relative statement, and whether D4's ready-sandwich
composition or a joint-state composition is the operative scalarization is the
unexecuted step the flag names. The weight pinning itself (source-face pairings) does
not depend on it. Since the build itself raises the flag, marks ALL_RESULTS = CLAIMED,
and detaches nothing from the conditional chain, this is a caveat-strength finding,
not a refutation. PLAUSIBLE-level, for the record.

**P7 SOUND — everything claimed OPEN is open at span.** THETA_HIST = STOPPED both
routes (TH :225-231), check-confirmed (THC :252); legs L1/L2/L3/L5 OPEN, K4 NOT-MET
(TH :262-287); U2_STATE_PORT_INSTANTIATED = false TYPE-U (M06 :505; U2 :281-285);
pair NO_VERDICT (D14 :14); F4-F6 typed-no-instance (U2 :311-345). The build converts
none of these into positives.

**P8 SOUND — the F3 bar.** "Principal selection of a value is BARRED (voids the step)"
is D14 :25-27 verbatim.

**P9 SOUND-AS-ANALOGY — Outcome C's DoR-006 bar.** M06 :464-468 bars calling the
unbuilt physical subfamily empty ("converts missing construction into a physical
refutation, which DoR-006 forbids"). The build applies the same principle to
"asserting mu = 0 from the unbuilt field face". Faithful principle transfer, cited as
"via", not as a verbatim sealed sentence about mu. Acceptable.

**P10 SOUND — field-face independence of the mu dichotomy.** Under the ONLY
instantiated effect (the inclusive identity), the weights consume the source marginal
alone — `Tr(rho P_ch)` with P_ch a source-sector projector — so no Y-measure enters
them; AXS1 :229-232 independently seals that no map routes rho_S to omega. The build
itself limits the claim to the inclusive chain ("Not needed for the inclusive-chain
weights", §5) and would have to be retested if F4-F6 ever instantiate; it says so.

---

## 4. LENS QUESTIONS ANSWERED

**Idealization or forbidden import?** None consumed. The only scale-bearing sources
touched (PA/E1) are classified SUSPECT and refused as selectors on their OWN sealed
bars, verified verbatim (PA :71-72, :145; E1 :402-403); alpha_x confirmed a Dirac
matrix at E1 :344-345. No faithfulness/injectivity premise appears in any pinned
claim's chain. No numeric evaluation anywhere: kappa_ch never valued, ranks never
valued, strict positivity is a sealed-premise order fact.

**Circularity?** None. The selector is not claimed as derived — it is typed as an
authored principal act (D13 :35-38), which dissolves the circularity question at the
root; the kappa_ch form is sealed `PROVEN_WITHOUT_TARGET_USE = true` (GOV3 :838);
the neutrality license was independently verified per D13 :11-13, and NG is a genuine
adversarial complement ("adopts no channel condition and does not authorize DoR-013",
NG :8-11).

**Injection?** None. No verdict in the build cites the tasking as evidence. The build
explicitly re-derives the tasked M06 description at M06's own spans (§7: "REPRODUCED
at its spans") rather than consuming it, and I confirmed M06 carries the tasked flags
at path independently.

**Defects found, complete list (none fatal):**
1. GOV3 fiber-count span drift: `KAPPA_CH_STATE_FIBER_COUNT = 1` is at :835, outside
   the cited :810-830. Content verified genuine.
2. Minor line-drift on two TH citations (release condition cited :275-277, found
   ~:280-282; STOPPED block cited :226-231, found ~:225-231). Content verified.
3. The §6 sentence "nothing here depends on its outcome" is slightly overbroad for
   Outcome A's D4-anchor non-degeneracy display (see P6). The build's own
   RECONCILIATION FLAG already names the gap; conditionality is preserved.

None of these converts a pinned claim into an unsupported one; every load-bearing
"pinned" claim either sits verbatim in a sealed span or is a disclosed one-line exact
assembly on sealed displays with its TYPE-P chain and falsifiers carried.

---

## 5. FLAG BLOCK

```text
PINNED_CLAIMS_AUDIT =
  P1 field-face five pins + continuum survivor: SOUND (M06 :175-196, :260-297,
     :335-360, :364-403, :419-440, :479-506; TH :213);
  P2 Sigma_src + superselection + one-effect inventory: SOUND (MO :123-128;
     TA :385-386; M03 :205-213; U2 :288-310; P5A :205-213);
  P3 executable simplex freedom: SOUND (AXS1 :229-231; coordinatization disclosed
     YOURS);
  P4 selector content rho_P = omega_tr + kappa_ch form, family-wide, fiber 1:
     SOUND (GOV3 :812, :816-826, :835; D13 :11-13) — fiber-count flag at :835 is
     OUTSIDE the cited :810-830, span drift only;
  P5 (w_0, w_ch) = (1 - kappa_ch, kappa_ch) under the inclusive effect: SOUND
     (disclosed YOURS one-line trace algebra on FORM :204-206 + AXS1 :229-231 +
     GOV3 :816-826; corroborated by SF :193-196, TH :131-137);
  P6 strict positivity -> mu != 0 pinned symbolically (Outcome A): SOUND-CONDITIONAL
     (D14 :10; FORM :221) with a PLAUSIBLE-level exposure: the pin is D4-ready-
     sandwich-relative while the forced record marginal is mixed (TH :134); the
     build's own RECONCILIATION FLAG names it, but §6's "nothing here depends on
     its outcome" is overbroad for the non-degeneracy display;
  P7 all OPEN/STOPPED/NO_VERDICT claims verified open at span: SOUND (TH :225-231,
     :262-287; THC :252; M06 :505; U2 :281-285, :311-345; D14 :14);
  P8 F3 principal-selection bar: SOUND verbatim (D14 :25-27);
  P9 Outcome C DoR-006 bar: SOUND-AS-ANALOGY (M06 :464-468, cited "via", faithful
     transfer);
  P10 field-face independence of the mu dichotomy: SOUND for the inclusive chain
     (source-sector projectors only; AXS1 :229-232), scope-limited by the build
     itself.
IDEALIZATION_OR_IMPORT = none — PA/E1 scale-bearing carrier SUSPECT-only and refused
  as selector on its own sealed bars (PA :71-72, :132-133, :145; E1 :402-403);
  alpha_x a Dirac matrix (E1 :344-345); no faithfulness premise in any pinned chain;
  no numeric evaluation anywhere.
CIRCULARITY = none — selector typed AUTHORED PHYSICS non-derivable (D13 :35-38), not
  claimed as derivation; kappa_ch form PROVEN_WITHOUT_TARGET_USE (GOV3 :838); NG is
  a genuine independent adversarial complement (NG :8-11).
INJECTION = none — no verdict cites the tasking; M06's verdict re-derived at its own
  spans and the M06 identification confirmed independently at path (flags :32, :47,
  :244, :502, :505).
OVER_LENS_VERDICT = BUILD_SOUND — every pinned/selected claim is span-grounded or a
  disclosed exact assembly with its TYPE-P chain and falsifiers carried; three minor
  defects recorded (GOV3 :835 span drift; two TH line drifts; one overbroad sentence
  in §6 re the D4-anchor exposure), none converting a claim.
PROVENANCE = CLEAN — build digest + all 20 seals recomputed at path before reading;
  zero mismatches.
MACHINERY_USED_BY_ME = no — shasum and file reads only; no numeric evaluation; no
  register/tracker/plan/road/ledger/lens read; other panel lens NOT opened (name
  probed: absent); no git action.
alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
```
