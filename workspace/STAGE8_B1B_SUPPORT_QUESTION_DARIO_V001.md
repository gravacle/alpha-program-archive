# STAGE 8 / 7A / [PLAN:B1b] — THE SUPPORT QUESTION: THE BLOCK IS REAL BUT IT IS NOT THE ONE THE AUDIT NAMED

Lane: DARIO (Builder B, independent verifier). Relay 792.
State brief pinned by the relay: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…` — digest
matches the pin, seal OK, read before task work. Drift check: V005 and `DECLINE_REGISTER_V002`
current.
Governing: the principal's challenge — B1b was scored *"blocked: no unique complex is fixed"* (M02)
in one line and never received the demand-extraction + hunt treatment that re-scoped B1a
(Q-692/Q-694). **RULING-LAST in force: this is the three-pass check before "blocked" may stand.**
All headline items **CLAIMED**.

## Lead determination — CLAIMED

**No sealed sentence demands a unique COMPLEX. The audit's one line reads a uniqueness demand
about a continuum object as a uniqueness demand about a CW object — the exact NAME-MATCH TRAP the
Q-69 census exists to prevent.**

```text
WHAT IS SEALED (three items, span-cited in section 1):
  (i)   support(L_c) is contained in Omega_c                     [Causal Incidence Support]
  (ii)  "Failure to derive a UNIQUE CAUSAL CELL ... blocks downstream promotion"   [same, falsifiers]
  (iii) "two cofinal physical exhaustions give different stabilized local record states
         or dressed maps" is a FALSIFIER                          [Causal Direct-Limit Record]

WHAT Omega_c IS, per the sealed Q-69 census authored by a lane that is not mine:
  "(5) Omega_c the causal record cell — A LORENTZ-COVARIANT CONTINUUM DIAMOND, NOT A CW OBJECT
   AT ALL, with its own uniqueness open"

=> (ii)'s "unique causal cell" is Omega_c.  IT IS NOT A COMPLEX.
```

**And the reframe the relay asked me to test is not merely available — the record has already
sealed it, at a different index than the audit was looking.** Item (iii) is a *cofinality-invariance*
condition on a **direct limit over the causal exhaustion**: uniqueness is demanded not of any single
object but of the *stabilized limit*, across cofinal exhaustions. That is precisely "uniquely
derived up to the directed system," already of record.

**But the reframe does not reach B1b's transport field, and the reason is the real block.** My own
sealed 755 established it: the support transport law is *"indexed by the exhaustion, not by
refinement: 'Once the future boundary has crossed the closure face of `Omega_c`…'. **Growing volume,
not subdivision.**"* `Ref_a` is the **refinement** system. The sealed directed structure for support
is the **exhaustion** system. **Nothing sealed bridges the two indices.**

```text
THE BLOCK IS REAL AND IT IS MISNAMED.
   audit (M02): "no unique complex is fixed"        -> NOT the sealed obstacle
   actual      : the EXHAUSTION-vs-REFINEMENT INDEX GAP — support lives on a sealed direct limit
                 over growing volume; Ref_a is a directed system over subdivision; no sealed
                 object carries one to the other.
```

**VERDICT = PARTIAL**, not BLOCKED-CONFIRMED. What would unblock it is now a different and much
sharper object than "select a complex": an index bridge, or a proof that the support correspondence
is cofinal-invariant *and* refinement-natural simultaneously.

---

## 0. Preflight and custody

```text
OUTPUT NAME   probed recursively before any write: artifact and sidecar ABSENT.
STATE BRIEF   PROGRAM_STATE_BRIEF_V005.md  e26f0d16055f3e83…  seal OK, digest matches the pin.
751 frontier  STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md        4d072e7671113357…  SEALED-OK
755 support   STAGE8_7A_SUPPORT_AND_FORCING_DARIO_V001.md    9685af44cc48f01f…  SEALED-OK
757 census    STAGE8_7A_IDENTIFICATION_FAMILY_DARIO_V001.md  487cc63f5dfc1c8d…  SEALED-OK
CIS           CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md     b0c636f3b2b00f06…  DOUBLY SEALED
CDL           CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md   packet member, manifest OK
DECLINES      DECLINE_REGISTER_V002.md  957476c8c605a370…  SEALED-OK

CIS SEAL PROVENANCE, STATED PRECISELY (and see the verb audit): its sidecar is named
   CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.seal.sha256  — DROPPING THE .md.
   shasum -c from its own directory: OK.  It is ALSO a member of STAGE7_PACKET_MANIFEST_V001.sha256,
   which verifies OK.  Doubly sealed.

GATES: alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
No member binding; no fixed-point execution; no end test; no numeric evaluation of physical
quantities; no comparison to measured constants.  NO COMPLEX SELECTED OR ADOPTED.  NO SMOOTH
IMPORT (S26).  No common cell formed; no junction map evaluated.
PE-1..PE-7: pointer-known, ZERO WEIGHT, not opened, not consulted.
```

---

## 1. AS1 — THE DEMAND, EXTRACTED FROM THE SOURCES' OWN WORDS

### 1.1 What the frontier actually says (not the audit's summary)

751 §2.3, verbatim:

> *"The structural statement is one line: a refinement partitions each cell, so the support of a
> pulled-back cochain is the union of the children of the cells in the original support; it is
> monotone and composes. **Nothing in the searched space seals it** … Not obstructed, not
> supplied."*

**That paragraph demands no unique complex.** It states monotonicity and composition — a
*naturality* shape. **And 755 withdrew it**: what 751 described is set-theoretic cochain support;
the sealed object is something else.

### 1.2 The sealed demand — three items

**(i) The support containment.** CIS, verbatim:

> *"For every primitive record-forming incidence `c`, the complete microscopic parent assigns one
> Lorentz-covariant causal cell `Omega_c` and one interaction density `L_c` such that
> `support(L_c) is contained in Omega_c`."*

**(ii) The uniqueness demand — and what it is about.** CIS falsifier section, verbatim:

> *"Failure to derive a **unique causal cell**, a complete parent, or a durable outgoing sector
> blocks downstream promotion. It is not repaired by assuming durability."*

The cell named is `Omega_c`. **`Omega_c` is not a complex** — see §3.2.

**(iii) The directed-system uniqueness.** CDL falsifier section, verbatim:

> *"**two cofinal physical exhaustions give different stabilized local record states or dressed
> maps**"*

plus, in the same list, *"the outgoing record states fail inductive compatibility."*

### 1.3 The extraction, typed

```text
DEMAND ITEM 1  support(L_c) subset Omega_c                       CONTAINMENT, per incidence
DEMAND ITEM 2  Omega_c unique                                    UNIQUENESS, on a CONTINUUM DIAMOND
DEMAND ITEM 3  cofinality-invariance of the stabilized limit     UNIQUENESS-UP-TO-COFINALITY
DEMAND ITEM 4  inductive compatibility of outgoing record states NATURALITY over the exhaustion
DEMAND ITEM 5  one-use support: completed record factors exactly INVARIANCE under later cells
               invariant

UNIQUE-COMPLEX DEMANDED:  ***NO.***  No sealed sentence in CIS, CDL, 751, 755 or the Q-69 census
   demands that a single CW complex be fixed.  Items 2-4 are uniqueness/naturality conditions on
   a CONTINUUM object and on a DIRECT LIMIT, not on a cellulation.
```

---

## 2. AS2 — THE DIRECTED-SYSTEM TEST

### 2.1 The reframe is licensed — and already sealed, at the exhaustion index

Demand items 3 and 4 *are* the directed-system statement. The record does not ask for a fixed
object; it asks that the **limit** be independent of which cofinal exhaustion computes it. That is
exactly "uniquely derived up to the directed system," and it is sealed in CDL rather than needing
authorship.

```text
LICENSED TYPED STATEMENT (displayed, not adopted):
   The support datum is a functor on the causal-exhaustion direct system whose colimit is
   required to be invariant under passage to a cofinal subsystem, with the transition maps
   leaving completed record factors exactly invariant (one-use support).
   No single Omega_c and no single complex is fixed by this statement.
```

### 2.2 The obstacle — and it is not the one the audit named

**But `Ref_a` is not that directed system.** 755, verbatim:

> *"its transport law is **indexed by the exhaustion**, not by refinement … **Growing volume, not
> subdivision.** So support is not 'immediate and unsealed'; it is indexed to a different index —
> the same exhaustion-vs-refinement gap carried since `IDX` §3.3."*

```text
EXACT SEALED OBSTACLE:
   Ref_a            = a directed system of SUBDIVISIONS   (cells split; volume constant)
   causal exhaustion = a directed system of GROWING VOLUME (region grows; cells not split)
   These are different index categories.  The B1b transport field is demanded over the FIRST;
   the sealed support law lives on the SECOND; and NO SEALED OBJECT CARRIES ONE TO THE OTHER.
   The gap is carried of record since IDX section 3.3 and is not created here.
```

**So the reframe succeeds at discharging the *uniqueness* reading and fails at the *index*.** The
audit's block is real in the sense that B1b cannot proceed — but its stated cause ("no unique
complex is fixed") is not the sealed cause, and fixing a complex would not discharge the actual
obstacle.

---

## 3. AS3 — THE SUPPLY HUNT

### 3.1 (a) What 755 supplies against the §1.3 interface, item by item

| demand item | 755's supply | status |
|---|---|---|
| 1 containment | identifies CIS as the sealed definition and quotes the containment | **SUPPLIED (as located, not built)** |
| 2 `Omega_c` uniqueness | records it as open; does not derive it | **NOT SUPPLIED** |
| 3 cofinality-invariance | not addressed at 755 | **NOT SUPPLIED** (located here, §3.3) |
| 4 inductive compatibility | not addressed | **NOT SUPPLIED** |
| 5 one-use invariance | quotes the closure-face clause | **SUPPLIED (as located)** |
| the index question | **states it exactly** — exhaustion, not refinement | **SUPPLIED — and it is 755's real contribution** |

**755 supplies the diagnosis, not the object.** Its load-bearing item is the index finding.

### 3.2 (b) The 757 census — used, not re-litigated

The sealed Q-69 census enumerates **seven distinct objects sharing "complex" or "cell"**. The two
the support demand could be confused with:

```text
(5) Omega_c  the causal record cell — "a Lorentz-covariant continuum diamond, NOT A CW OBJECT
             AT ALL, with its own uniqueness open"
(6) R_c      the record cell — "a three-dimensional Hilbert span, NOT A CELL OF ANY COMPLEX"

"MERGING ANY TWO OF THESE ON THE WORD 'CELL' WOULD BE THE NAME-MATCH TRAP THE CHARTER WARNED
 ABOUT, AND ONE OF MY OWN DRAFT AGENTS DID EXACTLY THAT WITH C_ref."
```

```text
CENSUS_BINDING: the support demand binds to (5) Omega_c ONLY.
   It binds to NO CW complex, and to no cellulation of the record complex.
   The census already records Omega_c's uniqueness as OPEN — so demand item 2 was on the books
   as open, about Omega_c, before the audit restated it as a complex question.
```

### 3.3 (c) Wider hunt — correspondence-shaped objects under other names

**One decisive hit, found through the packet manifest rather than by name-probing:** an entire
sealed `CAUSAL_DIRECT_LIMIT_*` family — `RECORD_PRINCIPLE`, `RECORD_HYPOTHESIS`,
`ARCHITECTURE_ADJUDICATION`, `COVECTOR_RAY_LIFT`, `REDUNDANT_RECORD`, `FORK_8_..._PROMOTION`, with
scripts, tests and results, all manifest-verified OK.

**Meaning probe on the load-bearing one (CDL).** It DISPLAYS, not merely names, a directed
architecture for the exhaustion:

> *"a durable public record is the compatible outgoing sector of a **future-directed
> Lorentz-covariant causal exhaustion** governed by the same finite source-record parent"* — with
> *"one-use causal support leaves completed record factors exactly invariant, producing a
> compatible quasi-local output state, its GNS representation, and recoverable central record
> sequences."*

**Scope, stated so this is not read as more than it is:** CDL's own flags carry
`global_source_inclusive_state_limit_derived = false`,
`global_infinite_future_source_Moller_unitary_derived = false`,
`absolute_charged_response_normalization_derived = false`. **It is a principle with undischarged
scope, not a supplied correspondence.**

Other probes run — `tau`-density carrier/reference, U1 cross-sector unit, the conservation law's
four-volume additivity carrier: each returns objects of the *right shape* but the *wrong index*
(all refinement-side). **None bridges exhaustion to refinement.** That negative is the finding.

---

## 4. AS4 — CLASSIFICATION AND VERDICT

| interface item | status |
|---|---|
| 1 containment `support(L_c) ⊆ Omega_c` | **SATISFIED** (sealed, CIS) |
| 2 `Omega_c` unique | **UNDECIDABLE_FROM_STOCK** — open of record in the census itself |
| 3 cofinality-invariance | **FAILED-AS-UNDISCHARGED** — sealed as a *falsifier*, never verified |
| 4 inductive compatibility | **FAILED-AS-UNDISCHARGED** — same list, same status |
| 5 one-use invariance | **SATISFIED** (sealed, CIS) |
| the index bridge (exhaustion → refinement) | **UNDECIDABLE_FROM_STOCK** — no sealed object |

```text
VERDICT = PARTIAL.
   NOT "BLOCKED-CONFIRMED": the unique-complex demand the audit cited IS NOT SEALED, so the
   stated ground of the block does not stand.
   NOT "IDENTIFIED": two demand items are undischarged falsifiers and one is open of record.
   Gaps typed:
      UNDECIDABLE_FROM_STOCK : Omega_c's uniqueness ; the exhaustion->refinement index bridge
      FREE_MULTIPLE          : none found — nothing here is a choice among sketches
      FORCED_UNIQUE          : none — no sealed constraint pins either gap
WHAT WOULD UNBLOCK IT (and it is not "select a complex"):
   either (a) a sealed object carrying the exhaustion index to the refinement index, or
          (b) a proof that the support correspondence is simultaneously cofinal-invariant and
              Ref_a-natural, which would let the two directed systems be treated as one.
   Selecting a complex discharges NEITHER.
```

### 4.1 What C1 inherits, either way

```text
C1_INHERITS:
   C1 (the common cell) inherits an OPEN Omega_c and an UNBRIDGED INDEX — not a missing
   cellulation.  Concretely: C1 may not form the common cell by choosing a complex, because the
   support field it must carry is not indexed by complexes at all.  The Q-69 census bars the
   merge that would make it look available (Omega_c is not a CW object), and B1b's actual
   prerequisite is the index bridge.
   POSITIVE INHERITANCE: C1 also inherits a SEALED directed architecture (CDL) it can build
   against, which the one-line block had made invisible.  The uniqueness C1 needs is
   cofinality-invariance, not object-selection — a weaker and better-posed demand.
```

---

## 5. AS5 — FREEDOMS CONSUMED, FLATTENING CHECK

### 5.1 `FREEDOMS_CONSUMED` — item by item, against the text actually written (law 2a in force)

| datum | tag | where |
|---|---|---|
| the record complex / cellulation | **NOT CONSUMED — none selected or adopted** | the whole point of §1.3 |
| `Omega_c` | **CARRIED-AS-OPEN** | §1.2(ii), §4; its uniqueness reported open, not supplied |
| the causal exhaustion (choice of cofinal system) | **CARRIED-AS-PARAMETER** | §2.1; no exhaustion selected; invariance is stated as the demand, not assumed |
| `L_c`, the interaction density | **CARRIED-AS-PARAMETER** | §1.2(i); never given a form |
| the smooth `C_ref` constituent | **NOT CONSUMED; BARRED (S26)** | not invoked anywhere |
| scaling weights (law 2a) | **NONE CONSUMED** | no `beta`-graded or scaling statement appears in this artifact |
| `Ref_a` directedness | **CARRIED AS ADOPTED OF RECORD** | §2.2; used only to say which index it is |
| CDL's undischarged scope flags | **CARRIED AS FALSE, NOT ASSUMED TRUE** | §3.3 |

**SUBSTITUTED: none.** Checked line-by-line against §1–§4, which is the enforcement standard the
784/789 blocks failed.

### 5.2 `FLATTENING_CHECK` — against `DECLINE_REGISTER_V002` (S01–S37)

```text
S26  C_ref barred as a source                                      CLEAN — not invoked at all.
S08  no EM / smooth-public-field / Maxwell / response identification  CLEAN — the support field is
     treated as a containment law on an interaction density; no physical field identification.
S28  the five free D_BR data unselected                            NOT TOUCHED (different arc).
S01, S27 and the remaining rows: not touched.
FLATTENING_CHECK = clean (37 rows walked; 2 live, both discharged).
```

---

## 6. JURISDICTION AND VERB AUDIT

**Jurisdiction.** *What was the rule written to protect?* RULING-LAST forbids a "blocked" standing
on one line without the three passes. The risk was live: B1b is on the forced spine, and a
misnamed block sends the next relay to select a complex — which §4 shows discharges nothing.
*Does the outcome space distinguish a real correction from lane preference?* Yes — §4 could have
returned BLOCKED-CONFIRMED, and two of six interface items are recorded FAILED and one UNDECIDABLE
rather than argued away. *Would evidence look different if the audit were right?* Yes: some sealed
sentence would demand a complex. §1.3 reports that none does, from the sources' own words.

**VOID CONDITION.** No complex selected, adopted, or preferred. The CDL find is reported with its
`= false` scope flags attached precisely so it is not read as a supply.

**BR-1.** The relay authorises the re-opening; it does not authorise its result. The verdict
PARTIAL is weaker than the "not blocked" the challenge might have hoped for, and I state the two
undischarged falsifiers plainly.

**Builder independence.** No `evaluator_build_A/` or `checks/` file read. `~/.codex` untouched;
`memory-bank` never searched.

### 6.1 Self verb audit — **NOT CLEAN: three disclosures**

1. **I generated a false custody finding mid-relay and caught it before it entered the artifact.**
   My preflight probe tested only `<name>.md.seal.sha256` and reported CIS as having **NO
   SIDECAR** — I was one step from recording that the support demand was unsealed. Its sidecar
   drops the `.md`. **This is the third occurrence of that exact trap** (753, 770, here), and at
   770 I wrote that I had "failed to generalise" it. **I still have not: my probe tests one
   variant.** The rule I should have been carrying: *probe both sidecar spellings, always.*
2. **The decisive AS3(c) hit came from a packet manifest, not from my probe list.** I found the
   `CAUSAL_DIRECT_LIMIT_*` family while verifying a seal, not while hunting for
   correspondence-shaped objects. My named probes (`tau`-density, U1 unit, four-volume carrier)
   would have missed the one object that changed the verdict. **The instrument found it by
   accident; the meaning-probe list was too narrow.**
3. **751 §2.3 is mine and it was wrong twice over** — wrong about what support is (755 withdrew
   it), and its "monotone and composes" line describes the refinement-side notion, which is
   precisely the conflation this relay had to undo. The audit's one-line block may well have
   inherited its framing from my own §2.3.

*Direction check:* all three run against me or my instruments; none creates a result. The finding
that could have flattered the challenge — "the block is wrong" — is returned as **PARTIAL** with
two undischarged falsifiers, not as a clearance.

---

```text
DEMAND = extracted (5 items; UNIQUE-COMPLEX demanded: NO).
   (1) support(L_c) contained in Omega_c [CIS, verbatim]; (2) "Failure to derive a UNIQUE CAUSAL
   CELL ... blocks downstream promotion" [CIS falsifiers] — and the cell is Omega_c, which the
   sealed Q-69 census types as "a Lorentz-covariant continuum diamond, NOT A CW OBJECT AT ALL,
   with its own uniqueness open"; (3) cofinality-invariance — "two cofinal physical exhaustions
   give different stabilized local record states or dressed maps" is a FALSIFIER [CDL]; (4)
   inductive compatibility of outgoing record states [CDL]; (5) one-use support leaves completed
   record factors exactly invariant [CIS].  NO SEALED SENTENCE in CIS, CDL, 751, 755 or the census
   demands that a single CW complex be fixed.  The audit's M02 line reads a uniqueness demand about
   a CONTINUUM object as one about a CW object — the NAME-MATCH TRAP the census exists to prevent.
DIRECTED_REFRAME = licensed, AND ALREADY SEALED — but at a DIFFERENT INDEX.  Demand items 3 and 4
   ARE the directed-system statement: the record asks not that an object be fixed but that the
   COLIMIT be invariant under passage to a cofinal subsystem, with transition maps leaving
   completed record factors invariant.  That is "uniquely derived up to the directed system",
   sealed in CDL rather than needing authorship.
   OBSTACLE DISPLAYED: Ref_a is a directed system of SUBDIVISIONS; the sealed support law is
   indexed by the causal EXHAUSTION — 755, verbatim: "indexed by the exhaustion, not by
   refinement ... Growing volume, not subdivision."  Two different index categories, and NO SEALED
   OBJECT CARRIES ONE TO THE OTHER.  The gap is carried of record since IDX section 3.3; it is not
   created here.  So the reframe discharges the UNIQUENESS reading and fails at the INDEX.
SUPPLY_755 = 3 of 6 items supplied AS LOCATED (containment; one-use invariance; and — its real
   contribution — the exact index diagnosis).  NOT supplied: Omega_c's uniqueness,
   cofinality-invariance, inductive compatibility.  755 supplies the diagnosis, not the object.
CENSUS_BINDING = Omega_c ONLY (census item 5).  The support demand binds to NO CW complex and to
   no cellulation of the record complex.  The census already recorded Omega_c's uniqueness as OPEN
   — so demand item 2 was on the books as open, about Omega_c, before the audit restated it as a
   complex question.  R_c (item 6, "a three-dimensional Hilbert span, not a cell of any complex")
   is the adjacent trap and is not bound either.
VERDICT = PARTIAL (gaps typed).  NOT BLOCKED-CONFIRMED: the unique-complex demand the audit cited
   IS NOT SEALED, so the stated ground of the block does not stand.  NOT IDENTIFIED: two demand
   items are undischarged falsifiers and one is open of record.  Interface: 2 SATISFIED
   (containment, one-use invariance), 2 FAILED-AS-UNDISCHARGED (cofinality-invariance, inductive
   compatibility), 2 UNDECIDABLE_FROM_STOCK (Omega_c's uniqueness; the exhaustion->refinement index
   bridge).  FORCED_UNIQUE: none.  FREE_MULTIPLE: none — nothing here is a choice among sketches.
   UNBLOCK CONDITION, STATED: either (a) a sealed object carrying the exhaustion index to the
   refinement index, or (b) a proof that the support correspondence is simultaneously
   cofinal-invariant and Ref_a-natural.  SELECTING A COMPLEX DISCHARGES NEITHER.
   NEW OF RECORD (AS3c): an entire sealed CAUSAL_DIRECT_LIMIT_* family (RECORD_PRINCIPLE,
   HYPOTHESIS, ARCHITECTURE_ADJUDICATION, COVECTOR_RAY_LIFT, REDUNDANT_RECORD, FORK_8 promotion,
   with scripts/tests/results), packet-manifest verified OK, DISPLAYING a future-directed
   Lorentz-covariant causal exhaustion architecture.  Reported with its own scope flags attached
   (global_source_inclusive_state_limit_derived = false; global_infinite_future_source_Moller_
   unitary_derived = false; absolute_charged_response_normalization_derived = false) so it is NOT
   read as a supplied correspondence.
C1_INHERITS = an OPEN Omega_c and an UNBRIDGED INDEX — not a missing cellulation.  C1 may not form
   the common cell by choosing a complex, because the support field it must carry is not indexed by
   complexes at all, and the census bars the merge that would make it look available.  POSITIVE
   INHERITANCE: C1 also inherits a SEALED directed architecture (CDL) to build against, which the
   one-line block had made invisible; the uniqueness C1 needs is COFINALITY-INVARIANCE, not
   object-selection — a weaker and better-posed demand.
FREEDOMS_CONSUMED = the record complex / cellulation NOT CONSUMED (none selected or adopted) ;
   Omega_c CARRIED-AS-OPEN ; the causal exhaustion CARRIED-AS-PARAMETER (no cofinal system chosen;
   invariance stated as the demand, not assumed) ; L_c CARRIED-AS-PARAMETER ; the smooth C_ref
   constituent NOT CONSUMED and BARRED (S26) ; scaling weights NONE CONSUMED (law 2a: no
   beta-graded statement appears) ; Ref_a directedness CARRIED AS ADOPTED OF RECORD ; CDL's
   undischarged scope flags CARRIED AS FALSE, not assumed true.  SUBSTITUTED: NONE — audited
   line-by-line against sections 1-4.
FLATTENING_CHECK = clean (37 rows walked; S26 and S08 live and both discharged; S28, S01, S27 not
   touched).
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3): (1) I GENERATED A FALSE CUSTODY FINDING MID-RELAY and caught it
   before it entered the artifact — my probe tested only <name>.md.seal.sha256 and reported CIS as
   UNSEALED, one step from recording the support demand as ungraded; its sidecar drops the .md,
   and this is the THIRD occurrence of that trap (753, 770, here) after I wrote at 770 that I had
   "failed to generalise" it.  I still had not: probe BOTH spellings, always; (2) the decisive
   AS3(c) hit — the CAUSAL_DIRECT_LIMIT_* family that changed the verdict — was found while
   verifying a seal, NOT by my meaning-probe list, which was too narrow and would have missed it;
   (3) 751 section 2.3 is mine and was wrong twice over — wrong about what support is (755
   withdrew it) and framed on the refinement side, which is precisely the conflation this relay
   had to undo; the audit's one-line block may have inherited its framing from my own paragraph.
   All three run against me or my instruments; the finding that could have flattered the
   challenge is returned as PARTIAL with two undischarged falsifiers, not as a clearance.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
