# STAGE 8 — AXN — THE THREE SMALL BOOKINGS: TWO SUPPLEMENTS AND THE DENOMINATORS
## DARIO LANE — RELAY 920 — `[PLAN:AXN-BUILD-C18]`

## 0. Preflight

Relay 920 verified before reading at
`b0c19e000c8567fb366a61a42f7c204771ae2d2bc45318afd45969477c69b53d`. Lane guard read DARIO; the
pickup ACK was written before content access. `PROGRAM_STATE_BRIEF_V005.md` verified and read before
task work at `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`. The requested output
name and its sidecar were clear at pickup.

---

## 1. Law-9b closure — declared first, exact members, full digests

This is the first substantive content in this artifact. Every determination below is taken **at a
named receiver inside this closure**. All three items are deferrals of my own coming back — items 1
and 2 are the residuals I typed for the ledger at 916 rather than performed, item 3 is the scope
limit I declared at 918 — so §2.2 states how that is handled.

```text
C_920 = {
 1  RELAY_PASTE_920_SUPPLEMENTS_DARIO_V001.md
      b0c19e000c8567fb366a61a42f7c204771ae2d2bc45318afd45969477c69b53d
 2  supervision/PROGRAM_STATE_BRIEF_V005.md
      e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
 3  R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md            [the named-but-undisplayed relation]
      40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9
 4  STAGE7_QSPEC_REVIEW_CANDIDATE_V002.md                               [restates the same three formulas]
      5026afb89b52ead9f309168cb2fa4f06b81039f8dd01b8940f40e64aad679a7e
 5  BID_FULL_DIRAC_CAR_SOURCE_TYPING_DERIVATION_V001.md                 [names H_D; see §3.1]
      0fe3905aa14ed744bda883dd68aa799dc9bb90f4f5647b477be3f6de65330f57
 6  STAGE8_MODULAR_CONJUGATE_ENERGY_LIMIT1_APPLICABILITY_RESULT_V001.md [quotes the STAGE7 passage]
      73a5a36c9de72a476fcb5f8dacd1615383c05a6dc4d9407c8e7406265a9fcd94
 7  supervision/QUESTIONS_SETTLED_REGISTER_V001.md                      [live-append; item 3]
      9aeb427e9e499fcc0593ae55826d05a48148b0c10d1d91f7a8735f7f4a5dad30
 8  STAGE8_AXN_SPEC_ACTS_DARIO_V001.md                    [my 916 — the two deferrals; §2.2]
      241ccf880266a895f5955173c8c87e7d180d8c6ba6dffefd10f257267454eca1
 9  STAGE8_AXN_INSTRUMENT_CROSSCHECK_DARIO_V001.md        [my 918 — the routed scope limit; §2.2]
      77257e5fe335d6ffedf2a0e8999dcf05865896c71ea1391987fa2d157dcd9887
}
```

**Exact span index** — each recomputed here from the sealed bytes:

| key | sealed bytes | span SHA-256 | content |
|---|---|---|---|
| `PARENT` | member 3 `[5711,6867)` | `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3` | `S_n`, `h_0[g,a]` and its "obtained from" phrase, `h_K(t)`, the lift |
| `DESCEND` | member 3 `[6867,7879)` | `827cf361f052d36c62e7fc6ea57e61c04cf8c18fc552dcde18bd7ee5e5ef8e3a` | `D_K`, `C_K(x)`, `D_K^2` |
| `Q817-NUM` | member 7 `[1669330,1669462)` | `713a3e812d9b6d4096fdf35c9fda8a8cf2c0509a811ad6eb05f6a6e9f05e44a5` | *"if the morphism is found, S1 closes and S2/S3/S4 unblock in the same stroke."* |

Members 4 and 5 carry no own sidecar. Member 4's digest reproduces the `stage7_candidate` pin carried
in the T7 parent-to-primitive authority-custody block; member 5 is used only for the negative in
§3.1, at a single quoted line, and carries no positive weight.

**Name probe.** `STAGE8_AXN_SUPPLEMENTS_DARIO_V001.md` and its sidecar: the workspace listing at
pickup returned a clear name for both. This artifact creates the first instance.

---

## 2. Gates, claim status, and how I handle discharging my own deferrals

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

Every headline determination here is **CLAIMED**. This report installs no identity, chain, schema,
convention, or ledger entry; the schema in §4 is **drafted for the registrar's routing and adopted
nowhere**. No member was bound; no fixed point was executed; no end test was run; no physical
quantity was numerically evaluated; no measured constant was consulted. PE-1 through PE-13 remained
pointer-only. Builder-A code paths were not accessed.

### 2.1 The bar I hold for item 1

The relay offers a three-step escalation — display the identity at bytes, else derive it from the
named connection data, else stop named — and the middle step is the temptation. The 3+1 slicing
identity is standard; I could write it from memory and it would read like a derivation. **It would
not be one.** I may display what the record displays, and I may derive from the record's own named
connection data where that data is sealed and sufficient. A step needing a convention, a signature
choice, or a decomposition the record does not supply is authorship, and it stops there named. §3
reports which of the three outcomes was reached and why, without blending them.

### 2.2 BR-1, in the direction it has not needed before

All three items are mine returning. **Discharging one's own deferral is not verifying one's own
finding** — it is a different act, and a lawful one. But it would become self-verification if a
discharge leaned on my earlier reasoning being *correct* rather than merely *stated*. So members 8
and 9 are present **only as the addressees of the deferrals**, and every substantive step below is
re-derived from members 3–7. In particular §3 re-runs the display sweep from scratch rather than
citing 916's conclusion that the identity is undisplayed, and §5 re-locates the ledger's numerators
in the register rather than citing 918's statement that it could not.

---

## 3. Item 1 — SUPPLEMENT-916-2, the display act

### 3.1 The identity is displayed nowhere sealed

The relation of record is `PARENT`'s phrase: `h_0[g,a]` is *"the Dirac Hamiltonian **obtained from**
the same spin-plus-U(1) connection used in the covariant superconnection."* The sweep for a
displayed identity was run in four shapes and returns nothing:

| probe | result |
|---|---|
| any time-derivative split (`partial_t`, `nabla_0`, `nabla_t`, `d/dt`) co-occurring with `gamma`, `Dirac`, `h_0` or `H_D` | **zero hits** |
| `H_D` defined by a formula (`H_D =`, `H_D :=`) | **zero hits** — member 5 introduces `H_D(q,A)` as *"the self-adjoint stationary Dirac Hamiltonian"* and then uses only its spectral projectors |
| explicit slicing forms (`gamma^0 gamma^j`, `alpha^j`, `slash(n) gamma`) | only `S_n = -i slash(n) gamma^5`, which is the write-term factor, not a slicing of the differential term |
| every occurrence of `h_0[g,a]` | four sites; member 4 and member 6 restate `PARENT`'s three formulas verbatim, member 6 by quoting member 4. **The phrase is restated, never unpacked.** |

**Step 1 is exhausted: the identity is not displayed.**

### 3.2 The named data is insufficient — and I can say exactly what is missing

The declared branch supplies: a *"globally hyperbolic spin spacetime"*, one vectorlike charged Dirac
source, the adopted compact relative-phase U(1) connection, finite causal diamonds, a source-free
flat asymptotic region — together with the Cauchy normal `n`, the metric `g`, the connection `a`, and
`S_n = -i slash(n) gamma^5`.

Carrying `i gamma^mu nabla_mu` to `h_0[g,a]` needs a **slicing**: a time function whose flow relates
to `n` by a lapse and a shift, or a convention fixing them. A sweep for `lapse`, `shift vector`,
`time function` and `ADM` across the workspace returns **no such datum of record**.

**What the supplied data does fix, and what it does not:**

```text
FIXED by n, g, a and the connection:
  the SHAPE of the decomposition -- a normal term carrying slash(n) plus a tangential
  operator on the slice;
  the converting factor -slash(n), already derived at 916 from the two displayed write
  terms and FORCED BY HERMITICITY (i gamma^5 anti-Hermitian, S_n Hermitian, S_n^2 = I).

NOT FIXED, and needed:
  the lapse-and-shift pair that turns nabla_n into i partial_t;
  the extrinsic-curvature coefficient that the choice of slice determines.
```

Writing either would be choosing a convention the record declines to state. **Step 2 closes, and the
outcome is step 3.**

### 3.3 The stop, and the supplement it re-prices

```text
IDENTITY = STOPPED (named).
  The 3+1 slicing identity is displayed in no sealed source, and deriving it from the
  named connection data requires ONE datum the declared branch omits: a slicing --
  a time function with its lapse and shift, or a convention fixing them relative to n.
```

**This is an advance on 916's typing rather than a repetition of it.** At 916 the supplement was
"display a standard identity", which is an open-ended authorship request. It is now localised: the
missing object is **the slicing datum**, one clause, after which the identity follows by the standard
decomposition and needs no further authorship. `SUPPLEMENT-916-2` is re-priced accordingly, and it
remains a producing-lane supplement rather than something this lane performs.

---

## 4. Item 2 — SUPPLEMENT-916-1, the naming schema, drafted

**Drafted for the registrar's routing. Adopted nowhere. It names no chain.**

```text
GALERKIN-CHAIN NAMING SCHEMA  (draft; supplement to the parent spec)

GC-1  DECLARE a sequence of finite-dimensional subspaces
        K_1 subset K_2 subset ... subset K_Sigma = L2(Sigma, S tensor L^q),
      with orthogonal projections P_n : K_Sigma -> K_n.

GC-2  DENSITY.  The union of the K_n is dense in K_Sigma.

GC-3  COMPRESSION.  Finite-stage one-particle operators are P_n T P_n for T the
      parent's own named operators.  No operator is replaced by another.

GC-4  TARGET-SELECTION BAR, carried verbatim from the parent spec: the chain may
      not be "a target-selected projector".  No member, dimension, or basis may be
      chosen by comparison with any downstream value, response, coupling, endpoint,
      or measured datum.

GC-5  ORDERING.  Any Jordan-Wigner presentation uses a mode ordering compatible
      with the chain (the modes of K_n precede those of K_m \ K_n for n < m), under
      which the stage embedding is A |-> A tensor I.

GC-6  MUST-PROVE -- CHAIN-INDEPENDENCE.  Any statement about the n -> infinity limit
      must carry a proof that it is independent of the admissible chain chosen --
      for instance that the union is a core for the operator in question.
      DISCLOSURE CARRIED IN: the strong-resolvent convergence of record is for the
      LATTICE family h_a -> h_0, a DIFFERENT regulator; no Galerkin-chain convergence
      is sealed.  Until GC-6 is discharged, no limit statement may lean on a
      particular chain.

GC-7  SCOPE.  This schema names no chain.  Naming one is a separate later act; the
      schema fixes only what any admissible naming must satisfy.
```

### 4.1 The content-free check, run clause by clause

"Content-free" is exactly the kind of claim that passes unexamined, so it is checked rather than
asserted. A clause is content-free if it selects no chain, dimension, basis, member, or physical
value, and is uniform over admissible chains.

| clause | selects anything? | uniform over admissible chains? |
|---|---|---|
| GC-1 | quantifies over sequences; names none | yes |
| GC-2 | a property of the sequence | yes |
| GC-3 | compresses the parent's **own** operators; introduces none | yes |
| GC-4 | a **prohibition**, carried verbatim from the sealed spec | yes |
| GC-5 | a presentation convention; 916 verified the embedding's residuals at `0.000e+00` under it, and the algebra is presentation-independent | yes |
| GC-6 | an **obligation**, not a claim | yes |
| GC-7 | explicitly **withholds** the naming | yes |

**No clause selects a chain, a dimension, a basis, a member, or any physical value.**

```text
SCHEMA = DRAFTED (content-free confirmed, clause by clause)
```

GC-6 is the clause that carries my 916 forward-disclosure into the schema itself, so the
lattice-versus-Galerkin gap cannot be lost between relays: it becomes the schema's own must-prove
rather than a note in an artifact.

---

## 5. Item 3 — the denominators

### 5.1 What the register verifies: the numerators

The register states the ledger's movement in one sentence, at `Q817-NUM`:

> *"if the morphism is found, **S1 closes and S2/S3/S4 unblock** in the same stroke."*

That fixes both numerators, and it is the only place in the register where the moved lines are
**named** rather than counted:

```text
DIRECTLY CLOSED, named  : S1.                       -> the "2" of 2/5 is S1 plus one unnamed line
UNBLOCKED, named        : S2, S3, S4.               -> the "3" of 3/4, complete and named
```

Q-818 separately says a supply act *"unblocks four lines"*, which is the same movement counted loosely
— one closed plus three unblocked — and is consistent with the instrument's finer `2/5` and `3/4`
once the closed line is separated from the unblocked ones.

### 5.2 What the register does not contain: the denominators

**No five-item core-line list and no four-item dependent-line list appears anywhere in the register.**
The two nearest enumerations are neither:

| enumeration | where | size | why it is not the denominator |
|---|---|---|---|
| the closed frontier | Q-779/874 entry, register byte `1595961` | **15** root items | a frontier list, not a core/dependent split |
| the atomic basis | Q-805 entry, register byte `1645664` | **11** elements (the S1/S2/S3 realizations; S4's construction half; B0's inhabitant half; the Boundary-Resolved generator; the correspondence; the descent; the two-system intertwiner; the finite carrier/membership binding; I2's prescription half) | eleven, and not partitioned 5 + 4 |

A five-subset of the basis-11 and a four-subset containing S2/S3/S4 can be **reconstructed**
plausibly. I decline to report a reconstruction as a verification: the trap in this item is accepting
a plausible five-item list because it has five items, and a denominator asserted from inference is
exactly what 918 declined to accept from the instrument.

### 5.3 Where the tally originates, and why it is out of reach here

917 states it is carrying *"the assignment's program-level tally"* — that is, the figures arrive from
relay 917's own assignment paste, which 917 pins as closure member 05 at
`141439c321452245f68fcd7dd6d5af1a114af94855c30341f5c3467faf98e2ca`. **That paste is not archive-side:**
a filesystem search returns no `RELAY_PASTE_917*` anywhere, and its digest appears in exactly one
file — 917 itself. It is the producing lane's assignment, not mine.

```text
DENOMINATORS = PARTIALLY VERIFIED
  numerators   VERIFIED at Q817-NUM: S1 closes; S2, S3, S4 unblock.
  denominators NOT LOCATED: no 5-item core list and no 4-item dependent list of record;
               the tally's source is an assignment paste outside this lane's reach.
```

**For V002's ledger, two lawful options, and the choice is the registrar's.** Either enumerate the
five and the four in the register so the denominators become citable, or have V002 cite the
**verified numerators** — *S1 closes; S2, S3, S4 unblock* — and drop the ratios. The second needs no
new work and says exactly what the record supports.

---

## 6. Typed controls (Q-797 discipline)

| control | type | source index |
|---|---|---|
| the four-shape display sweep, §3.1 | **ELIMINATES** — removes "the identity is displayed somewhere" as an available answer | members 3, 4, 5, 6 |
| the lapse/shift/ADM/time-function sweep, §3.2 | **ELIMINATES** — removes "the named data suffices" | workspace-wide |
| the fixed-versus-unfixed split, §3.2 | **EXPLAINS** — localises the missing object from "the identity" to "the slicing datum" | `PARENT`, `DESCEND` |
| the clause-by-clause content-free table, §4.1 | **EXPLAINS** — converts an assertion about the schema into a checkable property of each clause | the draft itself |
| `Q817-NUM`, §5.1 | **TRANSCRIBES** — supplies the numerators from the register's own sentence | member 7 |
| the frontier-15 / basis-11 comparison, §5.2 | **ELIMINATES** — removes the two candidate lists that a reconstruction would have used | member 7 |

---

## 7. FREEDOMS-CONSUMED (law 2a)

```text
CARRIED-AS-PARAMETER:
  the parent spec's declared branch, carrier, and the "obtained from" phrase;
  the converting factor -slash(n) and its Hermiticity forcing, at 916's scope;
  the sealed lattice strong-resolvent convergence, at ITS regulator's scope;
  the register's Q-817 movement sentence;
  the frontier-15 and basis-11 enumerations, as the nearest lists and not as denominators;
  my 916 and 918 as the addressees of the deferrals only.

CONDITIONED-ON:
  section 3's stop on the workspace sweeps displayed, which are mine and could be too
    narrow -- a second reader should widen them before the stop is treated as final;
  section 4's content-free verdict on the seven clauses as drafted, and on nothing else;
  section 5's numerators on Q817-NUM being the register's operative statement of the
    movement, which is the only place it is named rather than counted.

SUBSTITUTED:
  NOTHING. No slicing identity, lapse, shift, time function, extrinsic-curvature
  coefficient, chain, dimension, basis, mode ordering, denominator list, or ledger entry
  was authored or selected. The schema is DRAFTED and adopted nowhere; the identity is
  STOPPED rather than written; the denominators are reported NOT LOCATED rather than
  reconstructed.

SCALING WEIGHTS:
  NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 8. Flattening check, verb audit, byte audit

**FLATTENING CHECK — S01–S37 walked, clean.** A named relation was not identified with a displayed
one — that distinction is the whole of §3. A standard identity was not identified with a derivable
one: standard-and-unstated is authorship, and the escalation's middle step was declined explicitly
rather than quietly taken. The shape of a decomposition was not identified with the decomposition: §3.2
separates what `n` fixes from what the slicing fixes. A prohibition was not identified with a
selection — GC-4 carries the spec's own bar and adds nothing. An obligation was not identified with a
claim — GC-6 is a must-prove. A drafted schema was not identified with an adopted one. A plausible
reconstruction was not identified with a verification: §5.2 declines the five-subset it could have
asserted. A loose count ("unblocks four lines") was not identified with the finer ledger, and the two
were reconciled rather than merged. Discharging my own deferral was not identified with verifying my
own finding — §2.2 states the difference and §3 and §5 re-run from the sources.

**BUILDER-B INDEPENDENCE:** all three items derived from sealed sources. A's code was not accessed.
Members 8 and 9 are mine and carry no weight beyond naming what is being discharged.

**SELF VERB AUDIT.** "Stopped" is used for item 1 with the missing datum named. "Drafted" is used for
item 2 and is paired with the explicit statement that nothing is adopted. "Verified" is used in item 3
only for the numerators, which are quoted from a pinned span; "not located" is used for the
denominators, and no reconstruction is offered as a substitute. "Re-priced" is used once, for the
supplement whose scope narrowed. No identity, chain, convention, or ledger entry is claimed.
`VERB_AUDIT_SELF = CLEAN`.

**BYTE-POSITION SELF-AUDIT.** Measured on the sealed bytes of this file: §1's heading begins at byte
**542**, its closure fence opens at byte **983**, and the exact member list ends at byte
**2426**. A token scan of bytes `[0,983)` for absence-shaped forms — `no `, `not `, `none`,
`never`, `absent`, `missing`, `without`, `lack`, `fail`, `gap`, ` open`, `unresolved`, `underived`,
`false`, `cannot`, `zero`, `stop`, `block`, `wrong`, `refus`, `unswept`, `unsealed` — returns **zero
hits**.

---

## 9. Final lines

```text
CLOSURE = declared-first (byte position: sec-1 heading 542, closure fence 983, members end 2426; pre-closure absence-token scan over 22 forms = 0 hits)
IDENTITY = STOPPED (named). Displayed in NO sealed source — swept in four shapes: no time-derivative split co-occurring with gamma/Dirac/h_0/H_D; H_D never defined by a formula; the explicit slicing forms yield only S_n, which is the WRITE-term factor; and all four occurrences of h_0[g,a] restate the parent spec's phrase without unpacking it, one of them by quoting another. Deriving it from the named connection data needs ONE datum the declared branch omits — a slicing: a time function with its lapse and shift, or a convention fixing them relative to n; a workspace sweep for lapse/shift/time-function/ADM returns no such datum. WHAT IS SUPPLIED FIXES THE SHAPE (a normal term carrying slash(n) plus a tangential operator) AND THE CONVERTING FACTOR -slash(n), forced by Hermiticity at 916; what is unsupplied is the lapse-shift pair turning nabla_n into i partial_t, and the extrinsic-curvature coefficient the slice determines. ADVANCE ON 916: the supplement is RE-PRICED from "display a standard identity" to "name the slicing datum" — one clause, after which the identity follows and needs no further authorship. I declined the escalation's middle step rather than write a textbook identity that would have read like a derivation.
SCHEMA = DRAFTED (content-free confirmed). Seven clauses GC-1..GC-7: chain declaration, density, compression of the parent's OWN operators, the target-selection bar carried VERBATIM from the spec, chain-compatible mode ordering, the chain-independence MUST-PROVE, and an explicit scope clause withholding the naming. Content-freedom CHECKED CLAUSE BY CLAUSE rather than asserted: no clause selects a chain, dimension, basis, member, or physical value, and every clause is uniform over admissible chains. GC-6 carries my 916 forward-disclosure INTO the schema — the sealed strong-resolvent convergence is for the LATTICE family, a different regulator, so no limit statement may lean on a particular chain until chain-independence is proved. DRAFTED FOR THE REGISTRAR'S ROUTING; ADOPTED NOWHERE; IT NAMES NO CHAIN.
DENOMINATORS = PARTIALLY VERIFIED. NUMERATORS VERIFIED at a pinned register span (Q817-NUM, [1669330,1669462), sha 713a3e81…): "S1 closes and S2/S3/S4 unblock in the same stroke" — so the three unblocked dependent lines are S2, S3, S4, named and complete, and S1 is a directly-closed line. DENOMINATORS NOT LOCATED: no five-item core list and no four-item dependent list exists anywhere in the register; the two nearest enumerations are the FIFTEEN-item frontier (byte 1595961) and the ELEVEN-element atomic basis (byte 1645664), neither of which is a 5+4 split. The tally's origin is "the assignment's program-level tally" — relay 917's own assignment paste, which is NOT archive-side: a filesystem search returns no RELAY_PASTE_917* and its digest appears in exactly one file, 917 itself. I DECLINE TO REPORT A RECONSTRUCTION AS A VERIFICATION — a five-subset of the basis-11 is plausible and is not evidence, and a denominator asserted from inference is precisely what 918 declined to accept. FOR V002, two lawful options, the registrar's to choose: enumerate the five and the four in the register so the denominators become citable, or have V002 cite the VERIFIED numerators — S1 closes; S2, S3, S4 unblock — and drop the ratios, which needs no new work and says exactly what the record supports.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
