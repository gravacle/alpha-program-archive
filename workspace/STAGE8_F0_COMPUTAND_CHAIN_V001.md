# STAGE 8 — F0: THE COMPUTAND DEFINED BY CITATION — THE kappa_record / K_* DEFINITIONAL CHAIN
## BLIND DEFINITIONAL ASSEMBLY — F0-PIN — [CLAIMED]

Date: 2026-08-13
Role: CITATION/TYPING ONLY. The complete definitional chain record action ->
energy response `R_record,L` -> intensive limit object (Gate-5 kernel `C`) ->
onset-stiffness / `kappa_record` / `K_*` -> alpha assembly convention is
assembled BY CITATION from sealed sources. Nothing is computed, bounded,
estimated, evaluated, or approximated. No value of `kappa_record`, `K_*`,
`alpha`, `n`, `mu`, `C`, or any quantity appears. No measured value appears
anywhere. Every numeric string below is a sealed-text quotation or an exact
structural integer inside a quotation.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`

Fences held: no scale/imported-GR/faithfulness as authority; the three
scale-bearing sources (ECO, RFA, R4O) were consumed SUSPECT-ONLY, solely to
cite their sealed spec-gap flags, never as definitional authority. No
register, tracker, plan, road, ledger, or lens file was read; where a sealed
workspace artifact QUOTES such a file (the SLOT18 result quoting the
questions-settled register; the K-normalization verification quoting the v004
dimension-convention ledger), the quotation is cited from the sealed artifact
and the underlying file was NOT opened. Every seal verified at path before
reliance; digests recorded in Section 1. Output name probed ABSENT before
write.

---

## 0. VERDICT IN ONE LINE

**The chain is BROKEN-AT three absent definitions. Links 1 and 2 (record
action; `R_record,L`) are sealed definitions of record. Link 3 (the Gate-5
kernel `C` / the intensive limit) is sealed only as an OBLIGATION — no sealed
construction exists, and the only written existence route is scale-bearing,
refuted-as-written, with no sealed replacement. Link 4 is TWO distinct sealed
stiffness objects — `kappa_record` (an intensive Hessian on the V011 lineage)
and `K_*` (the onset root of `C_record(K_*) = 0` on the Gamma_K lineage) —
connected by NO sealed defining map: the conversion of `kappa_record` to a
physical action coefficient is sealed UNAUTHORIZED, `kappa_record =
kappa_Thomson` is sealed NOT-ASSUMED, and `C_record(K)` itself is a
required-not-built spec-slot whose enabling declarations have a FIRED
falsifier of record. Link 5 (the assembly) is CITED and single-form: every
sealed assembly statement is the same functional form `alpha = 1/(4 pi x
stiffness)` — `alpha(0) = 1/(4 pi kappa_Thomson)` in V011 and `alpha_micro =
1/(4 pi K_*)` on the live route — with a sealed verification that no two
incompatible alpha formulas exist; what differs is the ARGUMENT, and the
argument identification is exactly the broken links. On rationality: sealed
text mandates exact-rational/exact-symbolic arithmetic for every finite-stage
object and exact-rational enclosure strings, and is SILENT on whether
`kappa_record` or `K_*` is rational by construction from the defining form.
On imports: one finding — the sealed defining route for `K_*` consumes the
record floor `k_R` (a spectral scale) as a declared premise inside the
equation that fixes `K_*`, sealed as conditional-not-absolute.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
below recomputed by `shasum -a 256` at path this relay before reading.
CONN = connection-only of record; SCALE = scale-bearing, SUSPECT-ONLY.

| Tag | Source | Digest (first 16) | Seal attachment | Type |
|---|---|---|---|---|
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b` | MATCHES-TASKED | CONN |
| G1M | `STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md` | `b28c08130887b361` | MATCHES-TASKED | CONN |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d4904706276` | matches FORM/G1M recorded digest (packet member, NOT the same-named top-level copy) | CONN |
| GKS | `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` | `2d63dfadbb741c46` | sidecar `.md.seal.sha256` MATCHES | CONN (declares its own floor premise; see §5) |
| GKA | `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_AMENDMENT_001.md` | `5bbf0152c511956e` | sidecar MATCHES | verified, not load-bearing here |
| KNI | `STAGE8_K_NORMALIZATION_IDENTITY_AND_SURROGATE_SLOT_ADVERSARIAL_VERIFICATION_V001.md` | `efe006c000a420dd` | sidecar MATCHES | CONN |
| S18 | `STAGE8_SLOT18_Q34_NATIVENESS_AND_HOLONOMY_BRIDGE_RESULT_V001.md` | `8abb40899bda75d9` | sidecar MATCHES | CONN |
| CVP | `STAGE8_GAMMA_K_Q22_Q38_RULING_COVERAGE_PASS_V001.md` | `dfc0d06ba7d9cbe1` | sidecar MATCHES | CONN |
| DOR | `STAGE8_TASK6_EVALUATION_DOR_DARIO_V003.md` | `da5b03e151bc1b39` | sidecar MATCHES | CONN (self-typed DRAFT: "ADOPTS NOTHING, RULES NOTHING" :4-6) |
| TB1 | `STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_V001.md` | `85d24996987278b2` | sidecar `_V001.seal.sha256` (no-.md variant) MATCHES | superseded by TB2, retained as record |
| TB2 | `STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_AUTHORITY_FABLE_V002.md` | `ddb36cbfe4edfc2a` | matched in THREE attachments: `stage8_execution/spec_seal.sha256:1`, `stage8_execution/execution_authority.seal.sha256:1`, `STAGE8_REQUIRED_RETURN_COMPLETION_V001.seal.sha256:3` | CONN |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03` | matches FORM/G1M recorded digest | CONN |
| M06 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac5` | matches FORM/G1M recorded digest | CONN |
| ECO | `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md` | `0f3082cab910f2eb` | matches G1M recorded digest | SCALE, SUSPECT-ONLY |
| RFA | `STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md` | `2ede02aea415157a` | matches G1M recorded digest | SCALE, SUSPECT-ONLY |
| R4O | `STAGE8_RL2B_REFUTING_BRANCH_FOUR_OBLIGATION_ANALYSIS_PROPOSED_V001.md` | `f6fe40724ff4dfeb` | matches G1M recorded digest; content consumed only as quoted in G1M | SCALE, SUSPECT-ONLY |
| POSE | `STAGE8_REQUIRE_TARGET_POSED_V001.md` | `e3c482b5facfb741` | matches G1M recorded digest | CONN |
| CHK | `STAGE8_REQUIRE_TARGET_POSE_CHECK_V001.md` | `86068343c6e0ac1e` | matches G1M recorded digest | CONN |

Full digests of the two tasked anchors, recomputed at path this relay
(verbatim `shasum -a 256` output, spliced by command, never hand-typed;
each matches its tasked digest character-for-character):

```text
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md
b28c08130887b361ecbe4cdc75f1caae0f4c8dab8fbf06750400cf538184705a  STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md
```

---

## 2. THE CHAIN, LINK BY LINK

### LINK 1 — THE RECORD ACTION. SEALED. Typing: RATIFIED (disjoint) + SPEC-GAP (connected).

**Source:** V011 :1249-1281 (packet member `aa7c6d49...`).

Defining statement, quoted:

```text
V011 :1253-1257:
  H_(disjoint c)=tensor_c H_c,  r_(disjoint c)=tensor_c r_c,
  U_(disjoint c)=tensor_c U_c,  A_(disjoint c)=product_c A_c,
  Gamma_(disjoint c)=-log|A_(disjoint c)|=sum_c Gamma_c.
V011 :1260-1261: "Thus the Hessian of Gamma is additive without inserting an
  extensive factor."
V011 :1261-1277: "For a connected cellulation the complete amplitude must
  additionally admit a linked-cluster expansion Gamma_K=sum_(connected
  clusters C subset K) gamma_C, with a target-free locality bound that makes
  gamma_density=lim_(K exhausts R^4) Gamma_K/number_of_4_cells(K) exist and
  makes boundary corrections subextensive."
V011 :1279-1281: "The exact disjoint proof and the connected-cluster closure
  conditions are frozen in BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md. The
  disjoint theorem is proved; the connected thermodynamic limit remains a
  Gate-5 obligation."
```

Typing: the modulus action `Gamma = -log|A|` and its monoidal disjoint
additivity are RATIFIED of record; the connected linked-cluster form of
`Gamma_K` is a sealed REQUIREMENT whose enabling limit is a named obligation
(carried into Link 3).

### LINK 2 — THE ENERGY RESPONSE `R_record,L`. SEALED DEFINITION.

**Source:** V011 :1283-1302.

Defining statement, quoted:

```text
V011 :1285-1290:
  V_L(a)=d/ds B_L(sa)|_(s=0),
  G_L(a;tau_R)=integral_0^(tau_R) exp(iB_L(0)t)V_L(a)exp(-iB_L(0)t) dt.
V011 :1292-1297: "The primitive record susceptibility is the intensive
  Hessian
  R_record,L(a,b)=d^2/ds dt [Gamma_K(sa+tb)/N_4(K)]|_(s=t=0),"
V011 :1299-1302: "evaluated after the thermodynamic/linked-cluster limit. The
  Duhamel covariance computed from G_L must reproduce this Hessian.
  Fubini-Study is a consistency check on each normalized local output ray,
  not an independently selectable action multiplier."
```

Typing: SEALED DEFINITION, conditional in reach — the definition quantifies
over a limit ("evaluated after the thermodynamic/linked-cluster limit") whose
existence is Link 3's unbuilt object. Derived closure of the FORM (CLAIMED,
not ratified): `R_record,L(a,b) = n^2 sum_(j,k) C_jk <ell_j,a><ell_k,b>` on
difference-branch connection tangents (FORM §7 :466-484; carried into G1M
§2 :92-94). Supporting ratified inputs: the connection-only finite-N law
(M03, quoted at FORM B5 :96-105) and the one-character-power write (FORM B6).

### LINK 3 — THE INTENSIVE LIMIT OBJECT / THE GATE-5 KERNEL `C`. BROKEN-AT: NO SEALED CONSTRUCTION EXISTS.

**Sources:** V011 :1261-1281, :1728-1757, :2185; FORM D5-D6 :234-282; G1M §3
:119-220; ECO/RFA/R4O flags as quoted in G1M (SUSPECT-ONLY).

What IS sealed: the intensive limit object is NAMED and its required
properties are sealed —

```text
V011 :1270-1272: gamma_density=lim Gamma_K/number_of_4_cells(K)   [required to exist]
V011 :1740-1741 (Gate-5 pass condition): "the many-record amplitude to be
  monoidal on disjoint cells and to possess a local linked-cluster
  thermodynamic density on connected cellulations;"
V011 :1751-1752 (Gate-5 pass condition): "the full-neighborhood analytic
  locality bound and cellulation-independent thermodynamic limit to pass;"
V011 :2185 (sealed flag): connected_linked_cluster_density_proved = false
```

The kernel `C` is the FORM's derived name for exactly this object one level
down (FORM D6/G1 :270-274: "the Gate-5 connected linked-cluster limit of the
doubled record functional (sealed as an obligation, unsealed as an object)"),
typed of record: symmetric, PSD, intensive per `N_4`, additive over disjoint
cells, cluster-summable with subextensive boundary corrections, n-independent
(FORM D5 :246-252; G1M §2 :99-103).

BROKEN-AT, cited: `C` has no sealed construction and no connection-only
existence certification of record (G1M §3.1 :127-139: "No connection-only
certification exists of record"). The ONLY written existence route is
scale-bearing and is itself incomplete:

```text
ECO §2 :92-100 (as quoted in G1M :151-158): "IF R-L2b CLOSES, THE EXTENSIVITY
  QUESTION CLOSES WITH IT."
ECO §4 :132-134 (read at path; also G1M :157-158): "IF connected extensivity
  does fail, NO SEALED REPLACEMENT CONSTRUCTION EXISTS — both lanes confirm
  this independently."
R4O :139 (as quoted in G1M §3.3 :192-194): refuted as written;
  connected_extensivity = UNRESOLVED_BY_RL2B_REFUTING_BRANCH.
G1M flag :341-357: C_CONNECTION_ONLY_EXISTENCE = GAP(...the "short one
  power"...). NOT "NO": impossibility of a scale-free certification is not
  proven.
```

### LINK 4 — THE ONSET-STIFFNESS / `kappa_record` / `K_*`. TWO SEALED OBJECTS, NO SEALED BRIDGE. BROKEN-AT THE BRIDGE, AND `C_record` IS A SPEC-SLOT.

**4a. `kappa_record` — the V011-lineage stiffness. SEALED DEFINITION (conditional).**

```text
V011 :1304: "At this stage the output is named only kappa_record."
V011 :1529-1535: pass requires "an analytic limit or certified convergent
  enclosures M_record=lim_(odd L->infinity) M_L" (the reconstructed local
  tensor).
V011 :1567-1569: "Rotation and parity/CPT tests must first reduce M_record to
  H_trial=(kappa_record,E/2)|E|^2+(kappa_record,B/2)|B|^2"
V011 :1574-1579: "Only the predeclared exact test [M_record,J_star]=0 may
  yield kappa_record,E=kappa_record,B=kappa_record>=0."
V011 :1583-1585: "it does not yet identify kappa_record with the physical
  electromagnetic action coefficient."
V011 :1589-1590: "The primitive gate ends with kappa_record. It does not
  output kappa_Thomson, e, or alpha."
V011 :1792: "Its strongest numerical output is kappa_record."  [CORE-RESULT-SEAL]
```

WORDING NOTE (flagged, not adjudicated): the defining display :1292 says
"INTENSIVE Hessian" while the Scope section :2102 and TB2 :21 say "the
EXTENSIVE dimensionless record susceptibility / primitive record
susceptibility `kappa_record`". Both descriptors are sealed. The defining
display is the intensive Hessian; the descriptor conflict is recorded here as
a wording-level tension inside one source, not as two inequivalent
definitions of the object.

**4b. `K_*` — the Gamma_K-lineage stiffness. SEALED SELECTOR SHAPE; ITS RESIDUAL IS REQUIRED-NOT-BUILT.**

```text
GKS §4 :439-447 (quoting primitive_record_cell_selection_principle_v002
  :91-109): "Let C_record(K) be the scalar closure residual derived from the
  complete on-shell problem. The strict alpha route is authorized only if
  C_record(K_*) = 0, d C_record / dK at K_* != 0, K_* > 0, and an exhaustive
  admitted-family audit finds no other inequivalent positive root."
GKS :400-412: "The ordinary primitive charged branch opens at K_* only when
  the complete selector establishes all of the following: [six conditions] …
  The phrases 'first record' and 'allow/require' are not numerical equations
  by themselves."
GKS :497-499: "this spec requires C_record(K) to be an output of the
  completed on-shell Gamma_K/BR stationary problem, not a chosen function."
GKS :415-416: "the BR closure spectrum is not supplied by the word 'unique'".
DOR :270-271 (draft, reflecting the Q-34 ruling): "K_* — the onset root of
  C_record(K_*) = 0 with dC_record/dK != 0 and K_* > 0 — is alpha's
  stiffness".
```

Spec-gap on this lineage, cited: `Gamma_K` and `C_record(K)` are the
program's construction TARGET, not constructions (GKS :125-132:
"Gamma_K + C_record(K) IS THE PROGRAM'S SOLE CONSTRUCTION TARGET"); and the
coverage pass records a FIRED falsifier on the enabling declarations —

```text
CVP :111 (Q-38 row): "F-RP1 fired: the declarations lack definite
  constructive content for states, inner product, domain, rho_pre,
  quotient/measure formula, record-effect family, and executable
  mutation-channel members. The root premise lapsed, and the mutation audit
  is unexecutable."
```

**4c. The bridge between 4a and 4b: ABSENT OF RECORD. This is the sharpest break in the chain.**

```text
V011 :1311-1315: "Until Z_Q is constructed from complete Q_spec, the
  conversion from kappa_record to a physical action coefficient remains
  unauthorized."
V011 :1644-1651: "the primitive kappa_record may constrain or appear inside
  this construction, but equality kappa_record=kappa_Thomson is not assumed.
  It must follow from the complete amplitude or fail."
V011 :2077 (failure rule): "the equality kappa_record=kappa_Thomson is
  assumed rather than derived" [is a listed failure].
S18 :95-121 (quoting the Q-34 register row and principal ruling; register
  itself NOT opened by this relay): "Q-34. 'Which stiffness is alpha's — K_*
  or kappa_record?' … RULING: K_*, THE ONSET ROOT. … kappa_record is an
  INTENSIVE HESSIAN, a density on the completed chain … K_* is an ONSET ROOT
  on ONE cell … an impedance of an access condition is threshold-shaped, not
  a volume density."
CVP :108 (Q-34 row): "Q-34 makes K_*, the onset root, alpha's stiffness;
  kappa_record is not the value path. … [C_record] is now the value-path
  stiffness source, conditional and untransported."
KNI :44-48 (sealed flags): physical_K_star_selected_or_derived = false |
  TYPE-U ; alpha_micro_from_physical_K_star_derived = false | TYPE-U.
KNI :344-347: "no live result yet makes the surrogate coefficient the exact
  induced response or produces a physical K_*."
```

Net for Link 4: the tasked chain runs Links 1-3 down the V011 lineage to
`kappa_record`, but the assembly's argument (Link 5) is `K_*` on the Gamma_K
lineage; the two lineages are sealed as DIFFERENT TYPES ("volume density" vs
"threshold-shaped onset root", S18 quotation), joined only by a role ruling,
and NO sealed definition transports either object into the other. BROKEN-AT:
the `kappa_record`-to-physical conversion (sealed UNAUTHORIZED pending
`Z_Q`/complete `Q_spec`, itself a sealed content-list spec-slot, V011
:1592-1606) and the untransported `C_record`/`K_*` construction (4b).

### LINK 5 — THE ASSEMBLY CONVENTION. CITED. ONE FUNCTIONAL FORM, SEALED IN TWO PLACES; THE ARGUMENT IS THE OPEN PART.

```text
V011 :1657-1664: "Only after kappa_Thomson is derived may the unit-character
  convention be applied:
  S_EM=-(kappa_Thomson/4) integral d^4x F_(mu nu)F^(mu nu),
  kappa_Thomson=1/e^2,
  alpha(0)=e^2/(4 pi)=1/(4 pi kappa_Thomson)."
  [kappa_Thomson itself defined at :1634-1636: kappa_Thomson=lim_(q^2->0)
  kappa_Q(q^2), from Gamma_Q built on Z_Q/Q_spec.]
KNI :25 (quoting the live v004 dimension-convention ledger; ledger NOT
  opened by this relay): "alpha_micro(K) = 1/(4 pi K)." — with the sealed
  derivation restated at KNI :156-168 (K e_HL^2 = 1; e_G=e_HL/sqrt(4 pi);
  alpha_micro(K)=e_G^2=e_HL^2/(4 pi)=1/(4 pi K)), and the typing at KNI
  :241-242: "alpha_micro(K) as generic normalization identity = DERIVED;
  alpha_micro = 1/(4 pi K_*) with K_* physical = CARRIED/ADOPTED, TYPE-U".
KNI :249-251, :261-262: the v001 form "K_R,* = 4 Delta Phi_EM,* / I_F,*,
  alpha_* = 1/(4 pi K_R,*)" is "the same functional formula";
  "two_incompatible_alpha_formulas_found = false | TYPE-R".
DOR :271 (draft): "alpha_micro = 1/(4 pi K_*)"; DOR :305 (gate row 7):
  "Symbolic assembly 1/(4 pi K_*) … K_* established as the onset root, nu
  still symbolic"; numeric alpha barred behind row 8 and a separate licensed
  act.
```

Typing: ASSEMBLY_CONVENTION = CITED. The sealed convention is exactly the
`1/(4 pi x stiffness)` form; a sealed adversarial verification (KNI) states
no second incompatible formula exists. NOT AMBIGUOUS in form. The two sealed
statements differ in ARGUMENT — `kappa_Thomson` (V011, reachable only through
unbuilt `Q_spec`/`Z_Q`) versus physical `K_*` (live route, TYPE-U,
unproduced) — and that difference is not a competing definition of the
assembly; it is the Link-4 break restated.

---

## 3. LINKS ALREADY KNOWN-UNBUILT FROM THE SEALED RECORD (cited, not re-derived)

```text
U1  The connected linked-cluster limit / kernel C:
    V011 :1281 "remains a Gate-5 obligation"; V011 :2185
    connected_linked_cluster_density_proved = false; FORM D6/G1 :270-274;
    G1M C_CONNECTION_ONLY_EXISTENCE = GAP :341-357 and NET = GAP :379-385;
    ECO :132-134 "NO SEALED REPLACEMENT CONSTRUCTION EXISTS"; R4O
    refuted-as-written (as quoted in G1M :192-194).
U2  The scalarization/state-port pair (consumer of C):
    M06 flags as quoted at FORM B8 :120-123 and G1M :264-270:
    CANONICAL_SCALAR_F_N_FROM_RECORD_SANDWICH_ALONE = false | TYPE-R;
    RHO_PRE_PLACED = false | TYPE-U; U2_STATE_PORT_INSTANTIATED = false |
    TYPE-U.
U3  The write chains' support/current-density realization (G3):
    FORM B7 :113-118 and D6/G3 :278-282: RA27-3 fields NOT SUPPLIED.
U4  Q_spec / Z_Q (the kappa_record conversion authority):
    V011 :1311-1315 conversion UNAUTHORIZED until Z_Q from complete Q_spec;
    V011 :1592-1606 Q_spec sealed as a required content list (spec-slot).
U5  Gamma_K and C_record(K) (the K_* lineage):
    GKS :125-132 sole construction TARGET; GKS :497-499 required-not-built;
    GKS :415-416 BR closure spectrum not supplied; CVP :111 falsifier F-RP1
    FIRED, mutation audit unexecutable.
U6  The physical K_* and its assembly application:
    KNI :44-48 physical_K_star_selected_or_derived = false | TYPE-U;
    alpha_micro_from_physical_K_star_derived = false | TYPE-U.
U7  The Gate-5 execution ceiling (context for 4a):
    TB2 :27-40: SPEC-SEAL "currently UNATTAINABLE"; strongest producible
    claim GATE5_CORE_EXECUTED_SEAL_PENDING; BID_core_result_sealed false.
```

---

## 4. RATIONALITY-BY-CONSTRUCTION — POSED, NOT ANSWERED. WHAT SEALED TEXT SAYS.

The question, from the defining FORM alone: is the assembly argument
(`K_*`, or `kappa_record` on its lineage) rational BY CONSTRUCTION?

What sealed text says, exhaustively of the sources read:

```text
DOR :29 (lead): "ARITHMETIC = EXACT RATIONAL OR EXACT SYMBOLIC ONLY
  [PROVABLE — Gate-5 spec]".
DOR :262-266 (V1-9, 'The rationality check's place'): "All finite-L objects
  in exact rational or exact symbolic arithmetic, with the implementation
  named and hashed, and any enclosure reported as exact rational strings.
  A float-path result is not a value of this declaration at all."
TB2 :138: "All finite-L objects in exact rational (or exact symbolic)
  arithmetic."
TB2 :149-150: "kappa_record is reported as a closed form if analytic, else
  as its certified enclosure. Never as a bare truncated decimal."
TB2 :396 (schema): "kappa_record_enclosure": {"lower": "exact rational
  string", "upper": "exact rational string"}.
V011 :1520-1523: finite design rows retained by "exact rational rank";
V011 :1529-1535: the limit M_record admitted as "an analytic limit or
  certified convergent enclosures".
```

Verdict of citation: sealed text mandates that every FINITE-STAGE object in
the `kappa_record` lineage is exact-rational (or exact-symbolic) BY
CONSTRUCTION, and that any reported enclosure has exact-rational endpoints.
Sealed text is SILENT on whether the LIMIT object `kappa_record` — and
therefore on whether any stiffness fed to the assembly — is rational by
construction: the sealed disjunction "closed form if analytic, else certified
enclosure" (TB2 :149-150) poses the question and does not answer it. For
`K_*` sealed text carries NO rationality statement of any kind: `C_record(K)`
is required-not-built (U5), so no defining form exists yet from which the
question could even be checked. The exact-rational discipline's own demand —
that this be checked from the FORM before any value appears — is therefore
CHECKABLE-AT: the constructed `C_record(K)` (for `K_*`) or the constructed
Gate-5 limit (for `kappa_record`), neither of which exists of record. POSED,
OPEN, and correctly located.

---

## 5. FORBIDDEN-IMPORT AUDIT OF THE DEFINITIONS THEMSELVES

```text
MEASURED VALUE: none anywhere in any defining statement. V011 expressly
  fences it: Q_spec "with no measured alpha" (:1592-1594); "measured masses
  or thresholds enter a first-principles alpha result" is a listed FAILURE
  (:2076 region). No measured value appears in this artifact.
FAITHFULNESS: consumed by no defining statement in the chain. (FORM
  :516-517 certifies the R-form lineage derives without it.)
IMPORTED GR: consumed by no defining statement in the chain. The
  scale/GR-bearing content (sea covariance, Lorentzian causal diamonds,
  |D|_3/|D|_4, ell_P/G_4/beta) enters only the EXISTENCE-CERTIFICATION route
  for C (R-L2b) and the DISTINCT energy-comparand node (XSM), both already
  sealed as the GAP's content and held OUT (G1M :319-333, :387-392). A
  certification route is not the definition; this is the sealed Q-1013 seam,
  not a definitional import.

FINDING (definitional scale consumption, sealed as conditional):
  The defining route for K_* consumes the record floor k_R — a spectral
  scale — inside the equation that fixes K_*. Quoted:
    GKS :36-43 (the induced-only axiom): "Gamma_BR,k = -(1/2)
      integral_(1/k_R^2)^(1/k^2) ds/s STr'_BR exp(-s L_BR). … The lower
      proper-time boundary is the first durable record scale".
    GKS :46-48: "this spec may consume the lower proper-time boundary at k_R
      only as a declared condition. It does not derive K_bare = 0, does not
      derive the floor".
    GKS :64-67: "a completed Gamma_K with a unique simple positive C_record
      root would determine K_* conditionally on the induced-only axiom, not
      absolutely. … k_R — the floor — appears in the equation that fixes
      K_*."
  Typing: this is a DECLARED PREMISE of the sealed spec (GKS §0.3 :94-96),
  sealed as making any K_* CONDITIONAL, not absolute. It is a scale-shaped
  object inside the K_* definition itself — named here as the tasked
  finding, with its sealed conditionality quoted.

NOTED (not in the four forbidden classes): kappa_record's Maxwell-form
  reduction freezes the Lorentzian signature/orientation inputs
  "eta=diag(+1,-1,-1,-1), epsilon_0123=+1" (V011 :1553-1557) as ordinary-
  branch inputs of the sealed test battery; and the assembly identity
  consumes the declared Gaussian/Heaviside-Lorentz conversion
  e_G=e_HL/sqrt(4 pi) (KNI :164-165) — a unit convention, sealed as such.
```

---

## 6. WHAT THIS ARTIFACT DOES NOT SAY

```text
IT DOES NOT compute, bound, estimate, or evaluate kappa_record, K_*, C, mu,
  n, alpha, or any quantity; every numeric string above is inside a sealed
  quotation.
IT DOES NOT adjudicate the intensive/extensive wording tension (4a NOTE);
  both descriptors are cited and left standing.
IT DOES NOT claim the two assembly statements are two conventions: a sealed
  verification (KNI :261-262) states the functional form is one; the
  difference of argument is Link 4's break, and it is reported there.
IT DOES NOT treat the Q-34 ruling as a definition: it is a role ruling
  (quoted via S18); the missing DEFINITIONS it points at (C_record, the
  transport) are reported as the break.
IT DOES NOT read the questions-settled register, any tracker/plan/road/
  ledger/lens file, or the v004 dimension-convention ledger; sealed
  workspace artifacts QUOTING those files are cited with that provenance
  stated inline.
IT DOES NOT rely on the SCALE-typed sources (ECO/RFA/R4O) for any
  definition; they are cited solely for their sealed spec-gap flags,
  SUSPECT-ONLY.
The two full anchor digests in Section 1 are spliced verbatim from the
  shasum recomputation by command, never hand-typed.
```

---

## 7. FLAG BLOCK

```text
CHAIN = BROKEN-AT(three absent definitions:
  [1] the Gate-5 connected linked-cluster kernel C — the intensive limit is
      sealed only as an OBLIGATION (V011 :1279-1281, :1740-1741, :1751-1752;
      flag :2185 connected_linked_cluster_density_proved = false); no sealed
      construction exists; the only written existence route is scale-bearing,
      refuted-as-written, with no sealed replacement (G1M :341-357; ECO
      :132-134; R4O as quoted in G1M :192-194);
  [2] the stiffness bridge — NO sealed definition transports the Link-1..3
      lineage's kappa_record into the assembly's argument K_*: the
      kappa_record-to-physical conversion is sealed UNAUTHORIZED pending Z_Q
      from complete Q_spec (V011 :1311-1315), Q_spec is a sealed content-list
      spec-slot (V011 :1592-1606), kappa_record=kappa_Thomson is sealed
      NOT-ASSUMED and a listed failure if assumed (V011 :1644-1651, :2077),
      and the Q-34 ruling types the two stiffnesses as different shapes
      (S18 :95-121; CVP :108 "kappa_record is not the value path");
  [3] C_record(K) itself — the residual whose onset root DEFINES K_* is
      required-not-built (GKS :439-447 selector shape; :497-499 must be an
      output of the completed on-shell Gamma_K/BR problem; :415-416 BR
      closure spectrum not supplied; CVP :111 falsifier F-RP1 FIRED, the
      mutation audit unexecutable).
  INTACT LINKS, spans: record action (V011 :1253-1281, disjoint RATIFIED);
  R_record,L (V011 :1285-1302, SEALED DEFINITION conditional on [1]);
  kappa_record's defining battery (V011 :1304, :1529-1535, :1567-1585,
  :1589-1590, SEALED, conditional on [1] and the Gate-5 ceiling TB2 :27-40);
  K_*'s selector shape (GKS :439-447, SEALED SHAPE over the unbuilt [3]);
  the assembly form (Link 5, CITED). One wording-level tension flagged inside
  V011, not adjudicated: "intensive Hessian" :1292 vs "extensive
  dimensionless record susceptibility" :2102 (and TB2 :21, :473).)

ASSEMBLY_CONVENTION = CITED(one sealed functional form, two sealed
  statements: alpha(0)=e^2/(4 pi)=1/(4 pi kappa_Thomson), applied only after
  kappa_Thomson=lim_(q^2->0) kappa_Q(q^2) from Z_Q/Q_spec — V011 :1657-1664
  with :1634-1636; and alpha_micro(K)=1/(4 pi K) with the live-route argument
  K_*, sealed via KNI :25, :156-168, typed at KNI :241-242 as "generic
  normalization identity = DERIVED; alpha_micro = 1/(4 pi K_*) with K_*
  physical = CARRIED/ADOPTED, TYPE-U"; DOR :270-271, :305 displays the same
  symbolic assembly behind unlifted gates. Sealed verification that no two
  incompatible alpha formulas exist: KNI :261-262. NOT AMBIGUOUS in form;
  the argument identification is CHAIN break [2].)

RATIONALITY_POSABLE = POSED-NOT-ANSWERED(sealed text mandates exact-rational/
  exact-symbolic arithmetic for every finite-stage object and exact-rational
  enclosure endpoint strings — DOR :29, :262-266; TB2 :138, :149-150, :396;
  V011 :1520-1523 — and is SILENT on whether the limit kappa_record is
  rational by construction: the sealed disjunction is "closed form if
  analytic, else certified enclosure" (TB2 :149-150). For K_* sealed text
  carries NO rationality statement at all; the defining form C_record(K)
  does not yet exist (CHAIN break [3]), so the form-level check the
  exact-rational discipline requires has no object to run on. CHECKABLE-AT:
  the constructed C_record(K) / the constructed Gate-5 limit.)

DEFINITION_IMPORTS = one-finding(the K_* defining route consumes the record
  floor k_R — a spectral scale — inside the equation that fixes K_*, as a
  DECLARED induced-only premise sealed as conditional-not-absolute: GKS
  :36-48, :64-67, :94-96. No measured value, no faithfulness, no imported GR
  in any defining statement; the scale/GR content of C's existence
  certification (R-L2b) and of the energy-comparand node (XSM) is
  certification/comparand content, already sealed as the GAP and held OUT
  (G1M :319-333, :387-392). Noted, outside the four forbidden classes: the
  frozen Lorentzian signature inputs eta/epsilon in kappa_record's
  Maxwell-form test (V011 :1553-1557) and the Gaussian/Heaviside-Lorentz
  unit conversion inside the assembly identity (KNI :164-165).)

SEALS_VERIFIED = FORM 5e49d209 (full digest matches tasking) ; G1M b28c0813
  (full digest matches tasking, spliced verbatim in §1) ; V011 packet member aa7c6d49 ;
  GKS 2d63dfad sidecar ; GKA 5bbf0152 sidecar ; KNI efe006c0 sidecar ;
  S18 8abb4089 sidecar ; CVP dfc0d06b sidecar ; DOR da5b03e1 sidecar ;
  TB1 85d24996 sidecar(no-.md variant) ; TB2 ddb36cbf via three attachments
  (spec_seal.sha256, execution_authority.seal.sha256,
  STAGE8_REQUIRED_RETURN_COMPLETION_V001.seal.sha256) ; M03 2cd1ffce ;
  M06 d13920e2 ; ECO 0f3082ca ; RFA 2ede02ae ; R4O f6fe4072 ;
  POSE e3c482b5 ; CHK 86068343. All recomputed at path this relay.

MACHINERY_INVOKED = no. Citation and typing only; nothing computed, bounded,
  estimated, evaluated, or compared; every numeric string is a sealed-text
  quotation or an exact structural integer inside one; no register/tracker/
  plan/road/ledger/lens file opened; no git action; output name probed
  ABSENT before write.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
```
