# Stage-6 Parent-Action and Q_spec Ledger v002

Date: 2026-07-24

## Verdict

```text
STAGE7_CONDITIONS_LANDED_STAGE8_CROSS_EXECUTION_READY
```

This is the append-only successor to the v001 Stage-6 ledger after three
independent Stage-7 reviews returned `PASS_WITH_CONDITIONS`. It authorizes
handoff to the separately authored Stage-8 theorem battery. It does not
state that Stage 8 has passed or that a complete `Q_spec` exists.

## Independent review

```text
Mathematical/operator:         PASS_WITH_CONDITIONS
Physical/QED:                 PASS_WITH_CONDITIONS
Provenance/anti-circularity:  PASS_WITH_CONDITIONS
failures:                     0
blocking findings:            0
```

The reviewers reproduced the sealed algebra and verified the packet
provenance. C1-C6 are implemented below; C7 is frozen as an ordering rule.

## Adopted premise classes

```text
Fundamental Boundary Record Action;
Parent-State Covariance;
Causal Incidence Support;
Boundary Superconnection / Single-Operator Completeness;
Global Boundary Descent / Quasi-Free Completeness;
and zero independent bare F^2 compositeness.
```

These are target-value-free but historically target-aware.

## Disclosed branch and standard inputs

```text
ordinary 3+1 Lorentzian globally hyperbolic spin spacetime;
one massless-bare vectorlike Dirac pair;
smooth compact relative-U(1) connection;
flat source-free asymptotics;
distinct even M3 record factors;
stationary quasifree in-state of h_0;
and ER-A, the time-marginal-as-amplitude envelope realization.
```

Standard or kinematic inputs are the Dirac/CAR framework, standard CPT and
spin-statistics structure, ordinary functional analysis, and the ready,
pointer, completed, and recoverable record axioms.

## Corrected derived scope

The current derived stack contains:

```text
the finite causal durability parent and generated descendants;
the common finite-parent domain and finite propagator;
the smooth-to-sharp limit under the stated L1 operator-norm hypothesis;
the free CTP contour derived from the disclosed stationary quasifree state;
the free source tail;
per-state thresholded nonreturn for the admitted E_fin class;
the causally sequential outgoing record state and GNS;
the coherent source-dressed record net;
the recoverable central record sequence;
and the causally sequential Causal Direct-Limit Record Principle.
```

The free in-state itself is disclosed, not derived.

Exact record compatibility is not claimed for spacetime-overlapping
concurrent cells. The review-supplied counterexample is retained as a scope
boundary. The old hard-coded spacelike commutation and swap-independence
flags are not derived.

## Envelope and excitation scope

The intrinsic four-volume measure fixes:

```text
w(s)=32 min(s,1-s)^3.
```

The current parent adopts:

```text
v_c(t)=(tau_R/T_R) w((t-t_c)/T_R)
```

as disclosed realization `ER-A`. Its checks are separated:

```text
shape:     w is the normalized time marginal;
amplitude: integral v_c(t) dt = tau_R.
```

For every fixed `psi` in the admitted normalized finite-energy class and
every `delta>0`, there is `T(psi,delta)` after which the return probability
stays below `delta`. No state-uniform `T(delta)` is claimed.

## Cell-complex and representation scope

The proven cell class is a locally finite, future-directed, causally
sequential exhaustion by primitive diamonds. Each new cell opens after all
prior closure faces. Packing-dependent response normalization is forbidden.

`H_K(t)=dGamma_R(h_K(t))` and the finite `W_N` are
finite-regularization/algebraic Bogoliubov-automorphism objects. No unitary
implementability of intermediate-time dynamics in the asymptotic quasifree
representation is claimed. Only the completed maps in the scoped outgoing
construction are claimed to be implementable.

## Open `Q_spec` slots

```text
absolute physical T_R;
full gravitational action and gravitational quantum measure;
dynamical U(1) action;
gauge fixing, ghosts, and gauge edge modes;
normalized interacting CTP amplitude;
parent-derived functional regulator and finite renormalization;
induced-polarization transversality and photon-mass exclusion;
Lorentz- and packing-independent renormalized response;
finite c F^2 deformation exclusion;
source-inclusive state projective limit;
infinite-future source Moller limit;
continuum-regulator independence of the source-inclusive limit;
interacting charged pole or infraparticle threshold;
complete charged-species and threshold map;
enlarged-branch exhaustion;
threshold-conditioned Thomson matching;
CISP descendant test in the interacting outgoing sector;
and one unused structure-sensitive prediction.
```

## Frozen electromagnetic order

```text
dynamical U(1)
-> gauge/ghost/edge completion
-> normalized interacting CTP amplitude
-> regulator and finite renormalization
-> transversality and photon-mass exclusion
-> Lorentz/packing-independent response
-> charged pole or infraparticle threshold
-> threshold-conditioned Thomson matching
-> interacting CISP descendant test.
```

No later item may be used to select an earlier one.

## Fixed authorization state

```text
stage7_independent_reviews_completed = true
stage7_review_conditions_landed = true
stage8_cross_execution_ready = true
stage8_theorem_battery_authored = false
stage8_cross_execution_completed = false

complete_source_inclusive_parent_limit_derived = false
complete_parameter_free_Q_spec_frozen = false
physical_Thomson_stiffness_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
