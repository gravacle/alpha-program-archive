# Primitive Vectorlike Charged Source Branch v002

Date: 2026-07-23

## Correction and logical status

Version 001 referred to a "projective U(1)" character. Version 002 removes
that stale dependency. The source transforms under the active relative-phase
group `U(1)_rel` on the Level-1 adopted principal bundle.

The existence of one primitive charged spinor source remains a disclosed
ordinary-branch input. It is not derived from the record carrier or from a
coupling target.

## Frozen branch inputs

```text
3+1 Lorentzian spacetime;
local Lorentz covariance and spin-statistics;
CPT compatibility;
one primitive charged spinor source psi;
the active U(1)_rel unit character;
the Level-1 adopted principal U(1)_rel bundle and auxiliary connection.
```

## Charge and anomaly completion

The active relative character lattice is `Z`. The primitive faithful
nonzero character has

```text
|q| = 1.
```

In `3+1` dimensions, a chiral `U(1)` spectrum must obey

```text
sum_i q_i^3 = 0,
sum_i q_i = 0.
```

One unit-charge Weyl field fails these conditions. The smallest vectorlike
completion is the left-handed charge pair

```text
(+1, -1),
```

equivalent to one four-component Dirac field with unit vector charge.

## Branch scope

One vectorlike pair is frozen as the explicit ordinary primitive branch:

```text
primitive_fundamental_vectorlike_pairs = 1.
```

Additional vectorlike pairs are consistent and are not proved impossible.
They may not be added after response evaluation. Composite durable
excitations are not counted as independent ultraviolet flavors unless the
sealed parent dynamics derives them.

The primitive source is frozen on the scale-critical branch

```text
m_bare = 0.
```

A public mass and threshold must be generated by the later record-closure
solution.

## What is frozen

```text
source_bundle = adopted_principal_U1_rel_bundle
source_character = primitive_active_U1_rel_unit_character
primitive_charge_magnitude = 1
primitive_vectorlike_pair_count = 1
primitive_source_representation = four_component_Dirac
primitive_source_statistics = fermionic
primitive_bare_mass = 0
```

## What remains open

```text
spinor_source_derived_from_pre_alpha_record_principles = false
source_is_identical_to_primitive_record_degree = false
Dirac_operator_and_domain_derived = false
fermion_measure_and_regulator_derived = false
record_generated_mass_derived = false
universal_matter_spectrum_derived = false
complete_Q_spec_frozen = false
charged_determinant_evaluation_authorized = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```

## No-retuning rule

The representation, pair count, charge, statistics, and zero bare mass are
frozen before response evaluation. If the branch fails, these choices may
not be changed within version 002.

