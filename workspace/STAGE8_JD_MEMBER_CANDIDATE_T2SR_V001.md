# STAGE 8 — JOINT-D DISCHARGE CANDIDATE — MEMBER-BUILD T2SR — V001

## PROPOSED PHYSICS — PROPOSED_NOT_ADOPTED — READY-FOR-PRINCIPAL

Commission: T2SR (MEMBER-BUILD), 2026-08-15. The principal COMMISSIONED (did
not adopt) D-1 of the gravity-close act drafts (`add4c806…` §2, menu option
(a)): author the Joint-D discharge candidate as PROPOSED PHYSICS in the §7.5
sector-reduction shape, under the ratified mandate pattern (`ad9fc14e…`).
This artifact is the authoring step only. It arrives PROPOSED_NOT_ADOPTED,
behind an opposite-lane check that has not yet run; ratification is a SECOND,
SEPARATE principal act that this artifact does not presuppose, request, or
schedule. Until that entry addendum exists, per the mandate's own words:
*"no construction may consume it as premise. The registrar may not enter; the
lanes may not enter; conversational summaries may not substitute for the
addendum."*

**SK-7 BANNER, ON THE FACE: THIS CANDIDATE DOES NOT LAND THE MEMBER.** Joint A
(`SOURCE_EXTENSION_INHABITANCE`) remains STOPPED and blocks both routes, and
Joints B, C, HJ7, E, and F remain STOPPED/open of record (fdf20bd4 line-1723
determination; ba9430c1 K5). This candidate targets Joint D only, and even
that discharge is proposed, not performed. Any presentation of this artifact
as the member landing is false and self-killing.

Fences held: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. No numeric evaluation, no float, no
measured-constant comparison; symbolic scales only. MANDATE CLAUSE 3 LIVE
FROM THE FIRST AUTHORED BYTE: no downstream target was inspected, cited,
computed, or tested against anywhere below; no output of any authored map was
evaluated on any record object; no number appears as a value of anything.
No git. Output path and sidecar probed ABSENT at session start before any
other act; ONE file written plus its seal sidecar.

**SWEEP CUTOFF: 2026-08-15T07:51:47Z.** Exhaustiveness below is
sweep-displayed only; nothing absolute is claimed.

---

## 0. STATUS IN ONE LINE

AUTHORED — the (JD-3)/(JD-3a) candidate is constructed in the commissioned
sector-reduction composite shape `A_G o rho_N o sigma_field,N`; obligations
P-1 (orientation covariance), P-2 (presentation independence), P-3
(continuity), P-4 (contact-kernel) are PROVED on the composite; (JD-3c)
E_C-confinement is DISPLAYED as the authored law CONF-1 with falsifier F-5
armed; the cross-sector unit `lambda_JD` is declared explicitly, symbolically,
unassigned, with SK-8-clean provenance; no falsifier fired; nothing was
evaluated; nothing is adopted.

---

## 1. GROUND

### 1.1 Seals verified at path (`shasum -a 256 -c`), THIS session, before reliance

```text
fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md          OK
ba9430c17c61adbfce41df21dc5a14a34707c210cb69fc9c7455dd0530c19236  STAGE8_TASK5_JII_REVIEW_DARIO_V001.md               OK
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md    OK
b53d9e931efe1ebde333ef49fb4243e41917bb5d467f5a86e7052102ae5a0310  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md        OK
5f4979d50c905c009c1fa18cec65cde6d9812b7f6c8e7c6870e4e6bea6cf78d5  STAGE8_7A_R9JII_JOINT_LANDING_TEST_V001.md          OK
ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c  STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md  OK
add4c806002b6e0ce4e068b91d47071ba842029fb837b15e682dc0e6f44c1462  STAGE8_GRAVITY_ACT_DRAFTS_S9AD_V001.md              OK
```

7/7 sidecar MATCH; zero mismatches. Scoped reads only: D-1 (`add4c806` §2)
read in full per commission; fdf20bd4 §1.2–1.3, §1.5–1.6, §2 piece-ledger
rows 21–24, §3.5 in full, §4 reciprocal-stage row, line 1723; ba9430c1 read
in full; ad9fc14e read in full; b53d9e93 (S20)/(S20a)/(S20b) block; 5f4979d5
read in full; KER §2.1–2.2. No register/tracker/plan/continuation file
opened. "Q-…" tokens are EXPECTED-UNLOCATABLE and are cited below only as
they appear inside the seal-verified artifacts (the Q-126 census as quoted at
5f4979d5 §4.2). DoR-019's rule is consumed AS CARRIED at ba9430c1 K6.1 and
add4c806 §2.2 row 5; DoR-019 itself was not opened.

### 1.2 Sweeps run (cutoff 2026-08-15T07:51:47Z)

```text
SW-1  output collision probe: STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md and its
      .seal.sha256 sidecar ABSENT at workspace root before any authored byte.
SW-2  filename sweep "JD_MEMBER|MEMBER_CANDIDATE", workspace + cleanroom
      roots -> zero hits; no competing candidate exists at any
      commission-adjacent name.
SW-3  content sweep "rho_N" (workspace, fence-excluded) -> 10 files, every
      hit a different object's restriction/route map or the D-1 proposal
      text itself; NO sealed arrow FieldSym_N -> T_B exists anywhere swept —
      independently consistent with ba9430c1 K1.3 ("No sealed arrow
      Sym_N^loc -> T_B exists").
SW-4  content sweep "FieldSym" (workspace, fence-excluded) -> only the five
      artifacts already in the sealed-input set plus the S9AD check
      instrument (not opened; not a commissioned input).
```

### 1.3 The forced target, quoted at its own bytes (fdf20bd4 §3.5)

```text
R_C,N:C_N^k -> (C_N^k)^*.                         (JD-1)

Phitilde_N:Sigma_N^(2),Ward -> (C_N^k)^*,
Ctc_N^sym subset ker(Phitilde_N),
im(Phitilde_N) subset R_C,N(ker(E_C,N)),

Phi_N:Sym_N^loc -> (C_N^k)^*,
Phi_N([s]_(Ctc_N^sym)):=Phitilde_N(s),

iotatilde_N^H:=R_C,N^(-1) compose Phitilde_N,
iota_N^H:=R_C,N^(-1) compose Phi_N,

E_C,N R_C,N^(-1) Phitilde_N=0,
E_C,N R_C,N^(-1) Phi_N=0.                          (JD-2)

Phitilde_N(s)(delta_e)
 :=incidence/integration/evaluation of s on the named k-cell e,   (JD-3)

Phitilde_N(s)(sum_e a_e delta_e)
 :=sum_e a_e Phitilde_N(s)(delta_e).                    (JD-3a)
```

with the obligation sentence of record: *"and must prove orientation
covariance, presentation independence, continuity, and `Ctc_N^sym subset
ker(Phitilde_N)`; the latter two clauses make `Phi_N` and `iota_N^H`
presentation-independent quotient maps"*, and the confinement display
`(JD-3c)`. The target form is FORCED; nothing in it is authored here.

### 1.4 The sealed machine, consumed and not rebuilt (KER §2.1)

```text
T_B:=Omega_c^1(M;iR)                             (B2-1)

<J_c,a> := sum_e c_e integral_(gamma_e) a = u_c(a),   a in T_B.   (B2-2)

A_G:T_B -> (K_G^fin)^*,
(A_G a)(c):=<J_c,a>.                             (B2-4)
```

`(B2-4)` is SEALED, built, and proved ONTO the finite-dimensional dual; every
`J_c` is a continuous distribution on the nuclear test space (KER §2.2). The
per-cell integrals `integral_(gamma_e) a` consumed below are `(B2-2)`'s own
displayed summands; the KER artifact's own words: *"This is not newly
authored: A2 gives the causal paths and A3/A4 give exactly the current
pairing (B2-2)."* The review of record's sizing is adopted as the design
premise: *"the machine exists, is built, and is proved onto — it eats the
wrong sector"* (ba9430c1 N1). This candidate supplies the sector and only
the sector.

### 1.5 Codomain reconciliation, displayed (an interpretation display, ledgered)

The commissioned composite `A_G o rho_N o sigma_field,N` lands in
`(K_G^fin)^*`; the forced target `(JD-2)` lands in `(C_N^k)^*`. The
commission's own clause "with (JD-3a) linear extension" is read as the
reconciliation, and it is the only reading that meets `(JD-2)`'s codomain:
the candidate's `Phitilde_N` is the per-cell rule of §3 (T2-8), whose values
on the cycle sector ARE the sealed composite's values (proved identity
CYC-1, §3.9), and whose off-cycle values are the `(JD-3a)` linear extension
through the cell basis. The sealed machine is consumed as the cycle-sector
content; the off-cycle extension is authored. CL-9.

---

## 2. TYPE SLOTS OF RECORD FOR THE TWO AUTHORED ARROWS

```text
sigma_field,N : Sym_N^loc -> FieldSym_N     TYPE of record at the S20a site
  (b53d9e93: "Build a field-symbol map in a named topology and prove the
  limit exists"; a REQUIRED build obligation, NOT a sealed construction —
  ba9430c1: "REQUIRED, not sealed").  Sym_N^loc := Sigma_N^(2),Ward /
  Ctc_N^sym of record (SPEC-5).  Only the TYPE is of record; the
  construction below is AUTHORED.

rho_N : FieldSym_N -> T_B                   NO SEALED ARROW OF THIS TYPE
  EXISTS (ba9430c1 K1.3; SW-3).  This is the sector crossing itself — the
  type mismatch both independent routes died on.  Everything about it below
  is AUTHORED.
```

What this candidate does NOT claim from the S20a site: the `LW_2,N` limit,
its topology, the `(S20b)` Maxwell/contact separation, and the
restriction/reality/Ward squares remain construction obligations of the S20a
program, NOT discharged and NOT consumed here. Only the type slot is used.

---

## 3. THE AUTHORED CONSTRUCTION

Every item T2-1 … T2-8 and CONF-1 is AUTHORED PHYSICS unless its row in §6
says otherwise. Presenting any of them as forced voids this candidate (SK-3).

### 3.1 (T2-1) Graded-symbol extraction `gr_N` — AUTHORED

Of record, elements of `Sigma_N^(2),Ward` are order-two Ward symbols carrying
"a cotangent variable and quantization data" (ba9430c1 K1.3 typing sentence),
over the A1-typed base with the explicit DoR-019 metric `g`. AUTHORED:

```text
FS_N^(0) := the graded-symbol carrier — fiberwise-polynomial functions of
            degree <= 2 in the cotangent variable q on the A1-typed
            cotangent carrier at stage N, in a named topology tau_FS
            (declared: the topology of coefficientwise convergence in the
            stage-N section topology of record);

gr_N : Sigma_N^(2),Ward -> FS_N^(0),  continuous, linear, surjective —
       "forget quantization data, keep the graded (x,q) symbol function".
                                                              (T2-1)
```

The EXISTENCE of the graded (x,q) structure is of record (the K1.3 typing
sentence); the REALIZATION `FS_N^(0)` and the map `gr_N` are authored data of
this candidate. `Qdata_N := ker(gr_N)` is the quantization-data null space —
derived from the authored datum, not postulated about any sealed object.

### 3.2 (T2-2) `FieldSym_N` realization and `sigma_field,N` — AUTHORED

```text
FieldSym_N := FS_N^(0) / closure(gr_N(Ctc_N^sym)),   quotient topology;

sigma_field,N : Sym_N^loc -> FieldSym_N,
sigma_field,N([s]_(Ctc_N^sym)) := [gr_N(s)]_(closure(gr_N(Ctc_N^sym))).
                                                              (T2-2)
```

Well-defined WITHOUT any postulate about `Ctc_N^sym`'s content: if
`s - s' in Ctc_N^sym` then `gr_N(s) - gr_N(s') in gr_N(Ctc_N^sym)`, so the
classes agree. Linear, continuous (quotient-induced), surjective. The TYPE
matches the S20a slot exactly. `FieldSym_N` so realized is an `(x,q)`
cotangent carrier quotient — consistent with the review's typing of the
target slot. The `(S25c)`-shaped naturality `sigma_field,M j_f^Sym = j_f^Field
sigma_field,N` is carried as a DISPLAYED OBLIGATION on any future stage pair:
`j_f^Field` is the functorially induced quotient map WHEN `j_f^Sym` and the
stage maps preserve the null spaces; this square is untested here and is
armed under F-3.

### 3.3 (T2-3) Authored section `sect_N` — AUTHORED

```text
sect_N : FieldSym_N -> FS_N^(0),  a declared continuous linear section of
the quotient (a complement choice for closure(gr_N(Ctc_N^sym)) in FS_N^(0)),
REQUIRED by declaration to take values with compact x-support in the
stage-N carrier.                                              (T2-3)
```

The section is an authored gauge of the construction: distinct admissible
sections are DISTINCT CANDIDATES; no canonical section is claimed; this
candidate is the one determined by the declared `sect_N`. Its two declared
properties (section identity; compact-support values) are displayable
conditions on actual stock and are guarded at §7 (F-1/F-4 rows).

### 3.4 (T2-4) Reader-minimal fiber reduction `M_1`, `flat_g`, `Pi_iR` — AUTHORED

```text
(M_1 f)(x) := integral over the unit g-cosphere fiber S*_x of
              qhat . f(x, qhat) dvol_g(qhat)     in T_x M tensor C,

flat_g     := the DoR-019 metric's index-lowering isomorphism
              T_x M tensor C -> T*_x M tensor C   (explicit metric use),

Pi_iR(w)   := (w - conj(w))/2                     (iR-part projection),
                                                              (T2-4)
```

each fiberwise, composed to `Pi_iR o flat_g o M_1 : FS_N^(0) ->` compactly
supported iR-valued 1-forms `= T_B` (compact support from T2-3's clause;
iR-valuedness from `Pi_iR`; smoothness in the declared section topology).
PARITY (CAS-1, exact symbolic): `M_1` annihilates fiber degrees 0 and 2 and
reads degree 1 linearly with a nonzero structural factor. The reduction is
READER-MINIMAL by design: it consumes the explicit DoR-019 metric and the
fiber structure ONLY — no reference covector field, no external reader — the
K3.2 reader-dependence killer for external-field families is avoided as a
named design choice (CL-4). R-LINEARITY of the composed reduction: CAS-4.
C-linearity is NOT claimed and cannot be: `T_B` is a real (iR-valued) space;
`Pi_iR` is the candidate's reality interface (REAL-1), authored, with its
compatibility square against the record's reality transports `R_N^Sym`
displayed as an untested obligation under F-3.

### 3.5 (T2-5) THE CROSS-SECTOR UNIT — EXPLICIT DECLARATION

```text
UNIT DATUM: lambda_JD — one R-scalar datum, carried SYMBOLICALLY.

DIMENSIONAL TYPE (fixed by the two sector carriers of record):
  [lambda_JD] = [connection-variation of record at (B2-1)]
                per [graded-symbol first-moment of the authored FS carrier].

VALUE: NOT ASSIGNED. No number appears; "silently fixed to 1" is excluded on
this artifact's face by the symbol's explicit carriage everywhere it acts.

PROVENANCE (SK-8-checked): authored free datum of commission T2SR under the
ratified mandate pattern (ad9fc14e), introduced solely to discharge the
explicitness obligation on any lawful supplier of (JD-3) — DoR-019
NO_IMPLICIT_CROSS_SECTOR_UNIT as carried at ba9430c1 K6.1 — with dimensional
type fixed by (B2-1) and the S20a slot. The provenance names NO downstream
target, NO measured constant, and NO lens/narrative content.

ANTI-TUNING (SK-10, the K6.1 repair row adopted into this candidate's own
ledger as a standing row): any assignment, rescaling, or normalization of
lambda_JD by any consumer is a tuning surface of record. On any future
common formed cell, R9-JII J1/J3 police the declared unit against beta as a
FALSIFIER, never a constructor (5f4979d5 §2); no common cell exists or is
formed here.
```

### 3.6 (T2-6) Cell-realization interface `cell_N` — AUTHORED INTERFACE

```text
cell_N : {oriented k-cells e of C_N^k} -> {A2 causal paths of record},
e |-> gamma_e,  with degree matched to the current degree of the sealed
(B2-2) stock at the matched stage/grouping, and REVERSAL COMPATIBILITY
REQUIRED BY DECLARATION:  cell_N(e^-) = -cell_N(e)             (T2-6)
```

honoring the record's own reversal pairing (fdf20bd4 §4: "primitive
`gamma_RL`, reverse `-gamma_RL`") and the SPEC-15 sign law `mu(a^-) =
-mu(a)`. CAUTION, displayed: this authored interface is NOT the record-cell
identification. The commissioned determination of record returned
OVERLAPPING, not IDENTICAL, for `(JD-3)`'s named oriented k-cell against the
admissibility spec's record cell; nothing here forms a common cell,
identifies the two cell descriptions, or advances R9-JII past PENDING.
`COMMON_CELL_FORMED = false` throughout.

### 3.7 (T2-7) The sector crossing `rho_N` — AUTHORED

```text
rho_N := lambda_JD . ( Pi_iR o flat_g o M_1 o sect_N )
       : FieldSym_N -> T_B,                                   (T2-7)
```

corestricted by CONF-1 (§3.10) to the confinement subspace
`T_B^conf,N := { a in T_B : E_C,N R_C,N^(-1) W_N(a) = 0 }` (with `W_N` the
per-cell landing of T2-8). Linear and continuous as a composite of the
declared continuous linear factors, with the explicit unit as a symbolic
scalar. This arrow is the candidate's physics.

### 3.8 (T2-8) The candidate rule — `(JD-3)` instantiated, `(JD-3a)` extended

On representatives `s in Sigma_N^(2),Ward`, with
`a_s := rho_N(sigma_field,N([s]_(Ctc_N^sym))) in T_B`:

```text
Phitilde_N(s)(delta_e) := integral_(gamma_(cell_N(e))) a_s,      (T2-8/JD-3)

Phitilde_N(s)(sum_e a_e delta_e) := sum_e a_e Phitilde_N(s)(delta_e),
                                                           (T2-8/JD-3a)

W_N : T_B -> (C_N^k)^*,  (W_N a)(delta_e) := integral_(gamma_(cell_N(e))) a,
so that Phitilde_N(s) = W_N(a_s);

Phi_N([s]_(Ctc_N^sym)) := Phitilde_N(s)            (well-defined by P-2/P-4),
iotatilde_N^H := R_C,N^(-1) o Phitilde_N,
iota_N^H      := R_C,N^(-1) o Phi_N                       ((JD-2) display).
```

The per-cell integral is `(B2-2)`'s own displayed summand, consumed. The
finite stage has finitely many cells; the extension `(JD-3a)` is the linear
span rule on the cell basis.

### 3.9 (CYC-1) Consumption identity — the sealed machine is the cycle sector

For every cycle `c = sum_e c_e e in K_G^fin` at the matched stage, with
`chi_N(c) := sum_e c_e delta_(cell-basis)`:

```text
Phitilde_N(s)(chi_N(c))
  = sum_e c_e integral_(gamma_e) a_s          (T2-8, (JD-3a))
  = <J_c, a_s>                                ((B2-2), sealed)
  = (A_G a_s)(c)                              ((B2-4), sealed)
  = (A_G o rho_N o sigma_field,N)([s])(c).                     (CYC-1)
```

PROVED (index display verified generically at CAS-3). This is the
commissioned composite, exactly; `A_G` is consumed through its own sealed
displays and is not rebuilt. `(B2-4)`'s onto-ness gives the machine-side
reach display: on the cycle sector the machine loses nothing — the
candidate's cycle-sector image is `A_G(im(rho_N o sigma_field,N))`, and its
size is determined by the authored crossing alone. Onto-ness OF THE
COMPOSITE is not claimed. Off the cycle sector, the values are the authored
`(JD-3a)` extension (§1.5).

### 3.10 (CONF-1) E_C confinement — AUTHORED LAW, displayed, armed

```text
CONF-1 (PROPOSED PHYSICS): im( lambda_JD . Pi_iR o flat_g o M_1 o sect_N )
       subset T_B^conf,N — equivalently, im(Phitilde_N) subset
       R_C,N(ker(E_C,N)).
```

STATUS, displayed honestly: `E_C,N` enters this candidate only through its
sealed TYPE and reducing law (`E_C,N : C_N^k -> C_N^k`, `[E_C,N, Delta_N] =
0`, piece-ledger row 22) and the SPEC-9 display; no byte-level action of
`E_C,N` was consumed; CONF-1 is therefore NOT provable from the consumed
sealed stock at authoring time and is carried as the candidate's authored
confinement law — a genuine physical claim about the crossing's
compatibility with the sealed reducing structure. Its exact falsifier is
F-5. Per-A7-branch display (both branches carried of record, none selected
here): under a branch where `E_C,N` acts as identity on a sector of
`C_N^k`, CONF-1 forces the crossing's raw landing component in that sector
to vanish; under the zero-acting branch it is unconstraining there. CONF-1
as stated quantifies over the sealed `E_C,N` whatever the branch resolution
of record turns out to be; no selection is made or needed.

---

## 4. THE FOUR (JD-3) OBLIGATIONS, PROVED ON THE COMPOSITE

### P-1. Orientation covariance — PROVED

Claim: `Phitilde_N(s)(delta_(e^-)) = - Phitilde_N(s)(delta_e)` for every
oriented k-cell `e`, and the value family is equivariant under the addressed
orientation orbit's sign law.

Proof. By T2-8, `Phitilde_N(s)(delta_(e^-)) = integral_(cell_N(e^-)) a_s
= integral_(-cell_N(e)) a_s` (T2-6 reversal compatibility, declared)
`= - integral_(cell_N(e)) a_s` (exact change of variables on the line
integral of a 1-form under path reversal — CAS-2, exact symbolic)
`= - Phitilde_N(s)(delta_e)`. Orbit equivariance: the orbit acts by cell
relabelings and reversals; relabelings permute the `(JD-3a)` summands
(CAS-3); reversals flip signs as above (CAS-3b) — matching `mu(a^-) =
-mu(a)` (SPEC-15) and the sealed reversal pairing `c, -c` of the §4 row.
QED. Consumed sealed: SPEC-15, the A2/reversal pairing, `(B2-2)` summand
form. Authored input: T2-6's declared compatibility (displayed; its failure
on actual stock displays under F-1/F-4).

### P-2. Presentation independence — PROVED (structural)

Claim: `s - s' in Ctc_N^sym  =>  Phitilde_N(s) = Phitilde_N(s')`.

Proof. By T2-8 the rule factors: `Phitilde_N(s) = W_N(rho_N(sigma_field,N(
[s]_(Ctc_N^sym))))` — it depends on `s` only through the class
`[s]_(Ctc_N^sym)`, and every factor is (R-)linear (T2-2, T2-3, T2-4 with
CAS-4, T2-7, T2-8). Equal classes give equal values. QED.

HONESTY DISPLAY: this obligation discharges STRUCTURALLY because the
sector-reduction shape consumes the quotient before the crossing — a design
feature of the commissioned composite, not a computation. The substantive
burden moves into the DECLARED properties of the authored data (gr_N
class-consistency; sect_N the section identity); a displayed failure of
either on actual stock re-opens this obligation and fires F-1. J2/F'-5
facing display: no `ell`, no truncation level, no cellulation-family index,
and no cellulation geometric datum appears in any authored formula (sweep of
T2-1…T2-8 displayed; the cell enters only through `cell_N(e)`'s sealed A2
path).

### P-3. Continuity — PROVED (typed, named topologies)

Claim: `Phitilde_N : Sigma_N^(2),Ward -> (C_N^k)^*` is continuous and
R-linear in the named topologies, and so are the induced `Phi_N`,
`iotatilde_N^H`, `iota_N^H`.

Proof. Composite of continuous linear maps, factor by factor: the quotient
maps `[.]_(Ctc)` and `[.]_(closure(gr_N Ctc))` (quotient topologies,
continuous by construction); `gr_N` (T2-1, continuous by declaration);
`sect_N` (T2-3, continuous by declaration); `M_1` (lemma L-M1: the fiber is
the compact unit g-cosphere and integrands are fiberwise-polynomial of
degree <= 2, so the moment integral is continuous `tau_FS ->` the declared
section topology — displayed symbolically; parity/linearity at CAS-1/CAS-4);
`flat_g`, `Pi_iR` (fiberwise bounded); the symbolic scalar `lambda_JD`;
`W_N` (each `a |-> integral_(gamma_e) a` is continuous on `T_B` — the
sealed display "every `J_c` is a continuous distribution" on the nuclear
test space covers the per-cell summands, KER §2.2 — and the stage has
finitely many cells, so the joint map into `(C_N^k)^*` with its
finite-dimensional dual topology is continuous); `R_C,N^(-1)` (sealed
finite-stage isomorphism, JD-1). QED.

Posture display: continuity is the ONE obligation the review found lawful to
weaken and inert (ba9430c1 N2). This candidate PROVES it rather than
weakening it, per the D-1 ingredient-8 posture; the lawful weakened variant
remains on the record's own table and is not needed.

### P-4. Contact-kernel — PROVED (structural)

Claim: `Ctc_N^sym subset ker(Phitilde_N)`.

Proof. `s in Ctc_N^sym => [s]_(Ctc_N^sym) = 0` in `Sym_N^loc` (SPEC-5
quotient of record) `=> sigma_field,N(0) = 0 => a_s = rho_N(0) = 0 =>` every
per-cell value is `integral 0 = 0` `=> Phitilde_N(s) = 0`. QED. Hence, with
P-2, `Phi_N([s]) := Phitilde_N(s)` is a well-defined presentation-independent
quotient map, and `iotatilde_N^H`, `iota_N^H` are the `(JD-2)` Riesz
composites — exactly the two clauses the obligation sentence of record
demands of them. The same honesty display as P-2 applies; the guard is F-2's
row at §7.

---

## 5. (JD-3c) DISPLAYED

Given CONF-1 (authored law, §3.10):

```text
E_C,N R_C,N^(-1) Phitilde_N = 0,
E_C,N R_C,N^(-1) Phi_N = 0,                       ((JD-2) confinement)

E_C,N iota_N^H([s])
 = E_C,N R_C,N^(-1) Phi_N([s])
 = E_C,N R_C,N^(-1) Phitilde_N(s)
 = 0,                                              (JD-3c)

iota_N^H([s]) = (1 - E_C,N) iota_N^H([s])          (SPEC-9's displayed form).
```

The chain is exact substitution on the definitions (T2-8) once CONF-1 holds;
CONF-1 itself is authored and armed at F-5. A candidate arriving with this
conditionality displayed is lawful; hiding it would be void (SK-1). No
E_C branch is selected anywhere in this artifact (A7 discipline of record).

---

## 6. FORCED vs AUTHORED, INGREDIENT BY INGREDIENT

Per the mandate's honesty clause (ad9fc14e): *"An authored ingredient
presented as forced voids the candidate."* Only TYPE slots are of record for
the authored arrows; never a construction (SK-3).

| # | Ingredient | Class | Ground |
|---|---|---|---|
| 1 | Target form `(JD-1)/(JD-2)/(JD-3)/(JD-3a)/(JD-3c)` + the four proof obligations | **FORCED** (sealed demand) | fdf20bd4 §3.5 |
| 2 | `A_G : T_B -> (K_G^fin)^*` with `(B2-2)` pairing and onto-ness; per-cell summands | **FORCED/SEALED — CONSUMED, not rebuilt** | KER §2.1; CYC-1 |
| 3 | `R_C,N` (JD-1); `C_N^k` stock; `E_C,N` reducing law; SPEC-15 sign law; A2 reversal pairing; `T_B` (B2-1) and its test topology | **FORCED/SEALED — consumed** | fdf20bd4 rows 21–22, §1.6, §4; KER §2.1–2.2 |
| 4 | `sigma_field,N` TYPE `Sym_N^loc -> FieldSym_N`; `rho_N`'s absence-of-record | **FORCED TYPE SLOT / SEALED ABSENCE** | b53d9e93 (S20a); ba9430c1 K1.3 |
| 5 | `gr_N`, `FS_N^(0)` realization (T2-1) | **AUTHORED** | this artifact |
| 6 | `FieldSym_N` realization + `sigma_field,N` construction (T2-2) | **AUTHORED** | this artifact |
| 7 | `sect_N` (T2-3) | **AUTHORED** (gauge of the construction; per-candidate) | this artifact |
| 8 | `M_1`, `flat_g`, `Pi_iR` (T2-4); REAL-1 reality interface | **AUTHORED** (reader-minimal; explicit metric) | this artifact |
| 9 | `lambda_JD` unit datum (T2-5) | **AUTHORED DATUM, OBLIGATORY** (explicitness forced by DoR-019's rule as carried; the datum itself free) | ba9430c1 K6.1; 5f4979d5 §2 |
| 10 | `cell_N` interface (T2-6) | **AUTHORED INTERFACE** | this artifact |
| 11 | `rho_N` (T2-7) — the sector crossing | **AUTHORED** | this artifact |
| 12 | `Phitilde_N` per-cell rule + `(JD-3a)` extension + induced `Phi_N`, `iotatilde_N^H`, `iota_N^H` (T2-8) | **AUTHORED definition on FORCED form** | this artifact; fdf20bd4 (JD-2) |
| 13 | CONF-1 confinement law (§3.10) | **AUTHORED LAW** (proposed physics; F-5-armed) | this artifact |
| 14 | P-1 … P-4 and the (JD-3c) chain | **PROOF OBLIGATIONS** — P-1..P-4 PROVED on the composite; (JD-3c) displayed conditional on CONF-1 | §4, §5 |

---

## 7. FALSIFIER SET F-1..F-6, INSTANTIATED (per D-1 §2.4)

```text
F-1  presentation dependence: one actual stage on which two presentations of
     the same [s] yield different Phitilde_N values.
     THIS CANDIDATE: structurally closed at P-2 ON THE DEFINITION; stays
     armed against the declared properties (gr_N class-consistency, sect_N
     section identity); a displayed failure of either re-opens and fires.
     STATUS: NOT FIRED; not evaluable here (clause 3).
F-2  contact escape: one s in Ctc_N^sym with Phitilde_N(s) != 0.
     THIS CANDIDATE: structurally closed at P-4; same guard as F-1.
     STATUS: NOT FIRED; not evaluable here.
F-3  transport failure: any S23/S24/S25 square failing on the actual
     reciprocal u or the q=2 refinement r_nm.
     THIS CANDIDATE: the j_f^Field functorial square (T2-2) and REAL-1
     reality square (T2-4) are the displayed square obligations; Joint E
     bases are NOT supplied here.  STATUS: ARMED, UNTESTED.
F-4  domain failure: one actual Q-408 reciprocal/refinement kernel outside
     the claimed properly-supported order-<=2 domain.
     THIS CANDIDATE: includes failure of sect_N's compact-support clause or
     cell_N's degree/stage matching on actual stock.  STATUS: ARMED,
     UNTESTED (JB-2 not discharged; actual-stock domain empty until it is).
F-5  confinement failure: E_C,N R_C,N^(-1) Phitilde_N != 0 displayed on any
     actual stage.
     THIS CANDIDATE: the exact falsifier of CONF-1.  STATUS: ARMED,
     UNTESTED — the candidate's most exposed clause, said plainly.
F-6  unit implicitness: the composite fixes the cross-sector unit silently
     (including silently to 1).
     THIS CANDIDATE: NOT FIRED on the face — lambda_JD is explicit,
     symbolic, unassigned, and carried at every site it acts (T2-5, T2-7);
     re-arms against any consumer that drops or silently normalizes the
     symbol.
```

DISPLAY OF RECORD: **no falsifier fired during construction.** None was
evaluable — mandate clause 3 barred every evaluation, so no authored map was
applied to any record object at any point; there is nothing hidden (SK-1).
Even the non-vacuity of the composite on actual stock is left undisplayed BY
LAW here: exhibiting a nonzero value would be an output evaluation. Where
triviality or failure would surface is named: the opposite-lane check
(structural), JB-2's landing (domain), and the blind battery (SK-4).

## 8. SELF-KILL CLAUSES SK-1..SK-10, CARRIED IN SUBSTANCE (per D-1 §2.5)

```text
SK-1  any falsifier F-1..F-6 fires at bytes on a sealed display -> VOID.
      A candidate arriving with failures displayed is lawful; hiding one is
      void.  None is hidden here.
SK-2  FAMILY EMPTINESS: a sealed determination discharging OB-2 as
      family(Pkg_N^H) = emptyset voids this candidate whole (its target
      family is empty).  OB-2 is presently NOT proved either way, of record.
SK-3  an authored ingredient presented as forced -> VOID.  The §6 table is
      the compliance display; rows 5-13 are authored; only TYPE slots are of
      record.
SK-4  the blind battery downstream scores a fed output failed -> VOID with
      Q-212 blast radius (mandate clause 1a as carried at ad9fc14e).
SK-5  a sealed forced result later contradicts this candidate's content at
      bytes -> VOID without awaiting any cascade (mandate clause 1b).  Most
      exposed rows: CONF-1; T2-1's realization against any future sealed
      byte-display of Sigma_N^(2),Ward's internal structure.
SK-6  a future target-independent derivation selects the arrow -> this
      candidate dissolves into the derivation and the authored core vanishes
      from the premise set (mandate clause 2, the open forcing slot:
      future_derivation_can_select = NO_VERDICT).
SK-7  presenting this candidate or its adoption as LANDING THE MEMBER while
      Joint A (or B, C, HJ7, E) remains stopped -> VOID.  The banner and §9
      are part of the candidate; contradicting them voids it.
SK-8  the unit provenance naming any downstream target, measured constant,
      or lens/narrative content -> VOID.  T2-5's provenance block is the
      compliance display.
SK-9  R9-JII, if sealed and a common cell is ever formed, fires J1 on the
      declared unit vs beta — disagreement kills (5f4979d5 §2).  Carried;
      no common cell exists or is formed here.
SK-10 anti-tuning: any tuning of lambda_JD or of any normalization in
      T2-1..T2-8 is a standing kill row (the K6.1 repair row adopted into
      this candidate's own ledger).
```

## 9. WHAT THIS CANDIDATE DOES AND DOES NOT DO

```text
DOES: instantiate (JD-3)/(JD-3a) in the commissioned sector-reduction shape;
  prove P-1..P-4 on the composite; display (JD-3c) via CONF-1 with F-5
  armed; declare the cross-sector unit explicitly with SK-8-clean
  provenance; carry the FORCED/AUTHORED split, the falsifier set, and the
  self-kills on its face; arrive READY-FOR-PRINCIPAL behind the
  opposite-lane check.

DOES NOT: land the member (SK-7 banner).  Joint A SOURCE_EXTENSION_
  INHABITANCE remains STOPPED and blocks both routes; JB-2 (Joint B) is not
  discharged — THE COMPOSITE'S DOMAIN IS EMPTY ON ACTUAL STOCK UNTIL JB-2
  LANDS; Joint C's physical Loc/contact member is presupposed at its slot,
  not supplied; HJ7, Joint E's S23/S24/S25 bases, and Joint F's A9 gates
  remain open of record.  Does not discharge the S20a program (limit, S20b
  split, squares).  Does not form or presuppose a common cell; R9-JII stays
  PENDING, run_state NOT_RUN, "states the test without making it runnable."
  Does not select an E_C branch, a route, or an address.  Does not evaluate
  anything, anywhere, on any record object.  Does not enter the record: no
  construction may consume it as premise until the ratification addendum
  exists (ad9fc14e entry mechanics, quoted in the banner).
```

## 10. CAS VERIFICATION APPENDIX (exact symbolic; fresh venv)

```text
Environment: fresh python venv; sympy 1.14.0.  Script:
t2sr_cas_checks.py, sha256
3aa35b9119b6da524bd466de23ec77766d106127b0ba3a97a94e4dac83f22c52.
Generic-placeholder models verify ALGEBRAIC IDENTITIES of the P-block only;
no record object is instantiated; no number is produced as a value of any
program quantity; results are identities (vanishing/equality), displayed:

CAS-1a/b/c (dim-2 fiber model) and CAS-1d/e (dim-3): the cosphere first
  moment annihilates fiber degrees 0 and 2 for generic coefficients and
  reads degree 1 linearly with a nonzero structural factor  -> PASS (parity
  lemma of T2-4).
CAS-2: line integral of a 1-form under exact change of variables for path
  reversal: I(-gamma) + I(gamma) = 0 for generic f, gamma  -> PASS (sign
  engine of P-1).
CAS-3/3b: the (JD-3a) extension applied to chi_N(c) equals the (B2-2)
  displayed sum for generic coefficients (m=5), and single-cell reversal
  flips the value  -> PASS (CYC-1 index display; P-1 orbit display).
CAS-4: R-linearity of Pi_iR o M_1 on generic complex degree-1 fiber data
  with real scalars  -> PASS (P-2/P-3 linearity input).

ALL_IDENTITIES_HOLD = True.  9/9.
```

---

## CHOICE LEDGER (terminal; commission T2SR)

```text
CL-1  CORPUS BOUNDARY: exactly the seven registrar-resolved sealed inputs,
      verified 7/7 at path this session (§1.1), at the scoped spans named in
      §1.1; sweeps SW-1..SW-4 displayed; CUTOFF 2026-08-15T07:51:47Z.  A
      wider sweep could change this candidate only by exhibiting a sealed
      object at one of the authored slots.  PREMISE(named).
CL-2  QUOTIENT ROUTE (the named unforced design choice): Phitilde_N is
      defined through [s]_(Ctc) (factoring), NOT directly on representative
      data.  Reason: the commissioned composite's own typing (sigma_field,N
      eats Sym_N^loc) and provability — P-2/P-4 discharge structurally.
      The representative-route alternative (define on s directly, prove
      descent substantively) is displayed as the road not taken; under it
      obligations 2 and 4 are unprovable without Ctc_N^sym byte content.
      PREMISE(named).
CL-3  sect_N: an authored complement/section choice; distinct sections are
      distinct candidates; no canonical section claimed.  PREMISE(named).
CL-4  M_1 as the fiber reduction: chosen READER-MINIMAL (metric + fiber
      structure only) to avoid the K3.2 reader-dependence killer for
      external-field families; the reference-covector alternative is
      displayed as rejected for exactly that hazard.  PREMISE(named).
CL-5  cell_N: authored interface to the sealed A2 paths with declared
      reversal compatibility; NOT the record-cell identification
      (OVERLAPPING-not-IDENTICAL of record honored).  PREMISE(named).
CL-6  CONF-1 as authored LAW rather than as a projection step: a
      post-composed (1-E_C) landing was considered and rejected because
      E_C,N's equivariance under the orientation action is not displayed in
      the consumed stock — the projection could buy (JD-3c) at the price of
      P-1.  Displayed.  PREMISE(named).
CL-7  lambda_JD: the unit symbol's name and its R-scalar arity are authored
      notation; its explicitness is obligatory (K6.1); its value is nobody's
      here.  PREMISE(named).
CL-8  CONSUMPTION GRADES: D-1 consumed as the commissioned proposal
      (add4c806 §2 in full); the review consumed as the review of record
      (its N1 sizing adopted as design premise; its K3.1/K3.2/K6.1/N2 rows
      carried); KER consumed at its own bytes for (B2-1)..(B2-4);
      fdf20bd4/b53d9e93/5f4979d5 at the spans in §1.1.  PREMISE(named).
CL-9  CODOMAIN RECONCILIATION READING (§1.5): "with (JD-3a) linear
      extension" read as the per-cell rule whose cycle sector is the sealed
      composite (CYC-1) — the only reading meeting (JD-2)'s codomain; the
      alternative (a per-cell cycle-selection family) is displayed as
      rejected for requiring an over-complete non-canonical cycle basis.
      PREMISE(named).
CL-10 CAS scope: four identity families only, generic placeholders only,
      fresh venv, script hash displayed; no record object touched.
      PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The candidate's carriers are the ACTUAL sealed carriers of record:
  Sigma_N^(2),Ward and Sym_N^loc (SPEC-5), the S20a type slot, T_B (B2-1),
  C_N^k and R_C,N and E_C,N (rows 21-22), the A2 paths and reversal pairing,
  A_G (B2-2/B2-4).  Every authored object (T2-1..T2-8, CONF-1, lambda_JD)
  is typed against those actual carriers — no toy model substitutes for the
  actual surface at any load-bearing site.  The CAS models of §10 are
  generic-placeholder verification scaffolds for four algebraic identities,
  cleanly separated, never carriers of the candidate, and promoted to
  nothing.
```

## FLAG BLOCK

```text
CANDIDATE = STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001 — the Joint-D discharge
  candidate in the commissioned §7.5 sector-reduction shape:
  Phitilde_N := per-cell (JD-3) rule of A_G o rho_N o sigma_field,N with
  (JD-3a) extension (CYC-1 the consumption identity, proved)
PROVED = P-1 orientation covariance; P-2 presentation independence; P-3
  continuity; P-4 contact-kernel — all four on the composite, each with its
  consumed-vs-authored inputs displayed
DISPLAYED = (JD-3c) E_C confinement via the authored law CONF-1, conditional
  and said so; per-A7-branch display, no branch selected
AUTHORED_CORE = gr_N/FS_N^(0); FieldSym_N realization + sigma_field,N
  construction; sect_N; M_1/flat_g/Pi_iR; lambda_JD; cell_N; rho_N; the
  per-cell definition; CONF-1  (14-row FORCED/AUTHORED table at §6; SK-3
  honored)
UNIT = lambda_JD declared EXPLICIT, SYMBOLIC, UNASSIGNED; provenance
  SK-8-clean (no downstream target, no measured constant, no lens content);
  anti-tuning standing row adopted (SK-10); R9-JII J1/J3 police any future
  common-cell comparison as falsifier only
FALSIFIERS = F-1..F-6 instantiated at §7; NONE FIRED; F-5 (confinement) is
  the candidate's most exposed clause and says so; F-6 not fired on the face
SELF_KILL = SK-1..SK-10 carried in substance at §8; SK-7 banner on the face
NOT_LANDED = the member (Joint A stopped, blocks both routes); JB-2 (domain
  empty on actual stock until it lands); Joint C member; HJ7; Joint E bases;
  Joint F gates; the S20a program (limit/S20b split/squares)
MANDATE = pattern of record honored: authored PROPOSED PHYSICS ->
  PROPOSED_NOT_ADOPTED -> opposite-lane check (not yet run) -> separate
  ratification addendum (not presupposed); clause 3 held from first byte —
  no output inspection, no downstream shaping, no number
SWEEPS = SW-1..SW-4 displayed; CUTOFF 2026-08-15T07:51:47Z
CAS = exact symbolic only; fresh venv; sympy 1.14.0; script sha256
  3aa35b9119b6da524bd466de23ec77766d106127b0ba3a97a94e4dac83f22c52;
  9/9 identities hold; no float anywhere; no value frozen
PROPOSED_NOT_ADOPTED = true ; READY_FOR_PRINCIPAL = true (behind
  opposite-lane check) ; RATIFICATION_PRESUPPOSED = false
MEMBER_BOUND = false ; CHAIN_INVOKED = false ; COMMON_CELL_FORMED = false ;
  JUNCTION_MAP_EVALUATED = false ; NUMERIC_EVALUATION = false ;
  MEASURED_CONSTANT_COMPARISON = none ; MACHINERY_APPEAL = none beyond the
  consumed sealed board
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
```
