# STAGE 8 / TASK 6 / STEP 2 — THE EVALUATION DoR: THE SCOPE DECLARATION — V002

Lane: Dario (Claude Opus 5), drafting lane
Task: PASTE 607 / Task 6, Step 2
Authority to draft: DoR-020-A8 + the ratified A32 instrument. **THIS ARTIFACT IS A
DRAFT. IT ADOPTS NOTHING, RULES NOTHING, LIFTS NO GATE, AND COMPUTES NOTHING.**
Supersedes: my V001 (`9704f273…`), reviewed DEFECTIVE (+4 bounded) at `20be8030…`.
Custody: bounded redraft applying the four supplied renderings; Codex 3 confirms
bounded; the ruling itself waits where the gate map requires.

## Lead — the declaration's type, stated first

```text
THE PRODUCT OF THIS DECLARATION IS TYPED, BEFORE ANY VALUE EXISTS.

The four fields below are carried VERBATIM from the ratified SUBJECT:

  COMPUTATION_SCOPE = LOCAL
  PRODUCT_TYPE      = LOCAL-SHADOW
  TRIAL             = ONE-SIDED_ON_CURRENT_FINITE_CHI_LATTICE
  PERIOD_NATIVE     = false                                        (W1-1)

It may not be renamed a global period, holonomy charge, period-native Maxwell
quantity, kappa_Thomson, physical alpha, or a two-sided trial.

THE FOLLOWING TWO FIELDS ARE NOT PART OF THE RATIFIED FOUR-FIELD SUBJECT:

  A7_BRANCH  = VACUOUS_ON_LOCAL_ROUTE   [PROVABLE — resolved at (V1-6), per W2]
  ARITHMETIC = EXACT RATIONAL OR EXACT SYMBOLIC ONLY   [PROVABLE — Gate-5 spec]

The further exclusion "NOT K_*" carried in V001 is [YOURS], not ratified
provenance, and is retained on that tag only.                       (W1-3)

DECLARATION = DRAFTED
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
MEMBER_BOUND = false ; FIXED_POINT_EXECUTED = false ; END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
```

[PROVABLE] The propagation law is unchanged and is the decisive anti-promotion
clause:

```text
LOCAL-SHADOW --[K |-> 1/(4 pi K)]--> LOCAL-SHADOW,
1/(4 pi .) is not a promotion operator.                             (W1-2)
```

## 0. Preflight, standing, and the delta board

| Check | Result |
|---|---|
| Register head Q-542 (live-append tolerant) | verified — advanced only by entries recording this relay's own inputs; verify-and-proceed |
| Review `STAGE8_TASK6_DOR_REVIEW_LANE3_V001.md` = `20be8030aa69792e9ea963445f0a5844fcd27b593d2975c1d208f3bba6bdc93b` | **verified before reading** |
| My V001 = `9704f27355ec97f447c23e180d0e52f1177b7bd713773c347061d8fe1b1616d8` | verified |
| Output name collision | none — clear to write |

### 0.1 The delta board — exact

| # | Defect | Rendering applied | Where it lands |
|---|---|---|---|
| K1 | lead block called "verbatim" while carrying six fields | `(W1-3)` **verbatim as supplied** | Lead |
| K2 | gate row 2 omitted the chain's cumulative Steps 0–7 | `(W1-7)` **verbatim as supplied** | (V2-2) row 2 |
| K3 | `(V1-6)` carried a two-disposition fork typed TYPE-U | `(W2-5)`/`(W2-6)` **verbatim as supplied**, with the scope paragraph | (V1-6) |
| K4 | six/eight count treated as unresolved at five surfaces | `(W3-1)`/`(W3-2)` + the five-surface landing table **as supplied** | (V1-3), row 4, (V4-1), (V5-5), handoff |

```text
DELTA = bounded
CHANGED  = the lead's provenance sentence; gate-map row 2; (V1-6) in whole;
           five C1-dependent surfaces.
UNCHANGED= every other clause of V001, carried verbatim.
NO CELL MATHEMATICS CHANGES. NO GATE IS OPENED. NO VALUE IS PRODUCED.
```

[YOURS] **I record what the review corrected in me, without softening it.** K2 is
the one that mattered: I bound Step 8 to "gate 1" — A32 readiness — as though
that carried the chain's prerequisites. It does not. The review's countermodel is
exact and I adopt it as the reason, not merely the rendering:

```text
A32_PRE_EVALUATION_READY  -/->  SUCCESSFUL_CHAIN_STEPS_0_THROUGH_7   (W4-2)
```

A32 readiness is **evaluation custody**; chain entrance is **mathematics**. They
are independent obligation systems and my row 2 conflated them. A prose
description of `(V1-1)` elsewhere in the draft does not cure an omitted
prerequisite inside a load-bearing "may not begin until" table.

**Tag legend.** `[PROVABLE]` = carried from a sealed source at its sealed verb.
`[PART-PROVABLE]` = holds on a named subscope. `[YOURS]` = drafted here, not law.
`[TYPE-U]` = a lawful object with no member produced; emptiness not proved.

[PROVABLE] **The ratification's limit on this document is unchanged:** the
evaluation DoR *"may DRAFT in parallel against the ratified gate text but may not
be ruled before the subgate's own gate conditions are met where it requires
them."* `(M5a-V002)` remains `false_of_record`.

---

## V1. WHAT IS COMPUTED, EXACTLY

### (V1-1) The subject and its domain — [PROVABLE] — carried verbatim from V001

```text
(D_w, d_w) complete;  D_w subset K_amb;  D_w closed in complete K_amb;
B_w := ell_w o Pi_w o Schur o S_w ;   B_w : D_w -> D_w.        (V1-1a)
```

Two obligations are **not** discharged by ambient completeness and are carried as
named gates: `D_w` nonemptiness is *"carried only by explicit `C_ret` clause"*,
and *"Completeness of the ambient scalar carrier does not imply completeness of
`D_w`; `domain_complete_cert_w` is an independent item."*

### (V1-2) Step 8–9: the modulus — [PROVABLE] — carried verbatim from V001

```text
RetExtract[dot Schur] = a_loop Rhat_K                            (step 8)
A_loop := sup_(D_w) |a_loop|
q_loop  = sup_(D_w) |dot B_w| = |chi_K| A_loop   under C_RET_SCOPE_w   (step 9)
```

`MODULUS_COMPATIBILITY_CERT[w]` is a separate falsifiable gate at the Step 8/9
seam, discharged by exactly one of `DIFF_TO_METRIC` or `DIRECT_MODULUS`. The
failure mode it blocks is the alternate-complete-metric attack.

[YOURS] **The witness is named and sealed before `A_loop` is evaluated, not
after.** A modulus computed first and reconciled afterwards is the attack, not a
check against it.

### (V1-3) Step 10: the case-lattice cell determination — **K4 APPLIED**

[PROVABLE] The cell identifier is the Cartesian pair, and the count is fixed of
record by corrigendum `C1`:

```text
A_loop regime in {0, 0<A_loop<infinity, infinity}
  times
finite chi_K regime in {0, nonzero}                              (W3-1)

3 x 2 = 6.        CELLS = six_of_record.                         (W3-2)
```

| Cell | `A_loop` | finite `chi_K` | Status |
|---:|---|---|---|
| 1 | `0` | `0` | `q_loop = 0`, strict |
| 2 | `0` | nonzero | `q_loop = 0`, strict |
| 3 | finite positive | `0` | `q_loop = 0`, strict |
| 4 | finite positive | nonzero | strict iff `|chi_K| < A_loop^{-1}` |
| 5 | `infinity` | `0` | `q_loop = 0` by separate pointwise rule, strict |
| 6 | `infinity` | nonzero | `q_loop = infinity`, excluded / not contractive |

[PROVABLE] `C1` corrects the one `P1` prose numeral; this displayed lattice
governs Step 10 and every disposition; `CHAIN-X` independently enumerates the same
six. **There is no seventh or eighth cell.** V001's "inherited discrepancy, not
harmonized" is superseded and withdrawn.

### (V1-4) Step 11: the Banach consequence — [PROVABLE] — carried verbatim

Conditional existence/uniqueness *"under Step 10 for the same `w` and the same
complete `D_w`."* [YOURS] The sameness conditions are **operative**: a fixed point
obtained on a different `w`, a different domain, or a domain whose completeness
certificate is not the one used at Step 9 is not this chain's fixed point and may
not be reported as it.

### (V1-5) The one-sided-trial rider, attached to `chi_K`'s reading — carried verbatim

```text
seed nonzero  -> chi_K finite; the computation remains INSIDE the licensed
                 lattice; a confirming outcome MAY CONFIRM.
seed zero     -> chi_K is a POLE / OUT-OF-LATTICE condition.
                 IT IS NOT A NEGATIVE VERDICT.                    (V1-5a)
```

[PROVABLE] The mechanism of record: `chi_K^Mx` carries `q_T,RL` in a denominator,
and **the licensed case lattice has no infinite-`chi_K` cell** — so a vanishing
seed exits the licensed lattice rather than refuting anything. On seed zero the
payload records `OUT_OF_LATTICE` or noncomputable, **and no value is invented.**

### (V1-6) Contact on the local route — **K3 APPLIED; the V001 fork is withdrawn**

[PROVABLE at the declared local-composite contract] The factor walk proves the
following signature statement; it manufactures no branch-restricted maps:

```text
LOCAL_CHAIN_FACTORS
  = {S_w, Schur_w, Pi_w, ell_w};

E_C,N in Hom(C_N^k,C_N^k);

C_N^k is not a declared interface of LOCAL_CHAIN_FACTORS;
E_C,N is not a declared field or argument of any local factor;

LOCAL_CHAIN_C_N^k_INTERFACE   = none_of_record;
LOCAL_CHAIN_E_C_CONSUMER_SEAM = none_of_record;

therefore, for this declared LOCAL-SHADOW composite contract,
A7_BRANCH = VACUOUS_ON_LOCAL_ROUTE.                              (W2-5)
```

```text
CONTACT_LOCAL_ROUTE = VACUOUS_ON_LOCAL_ROUTE
CONTACT             = VACUOUS_PROVEN.                            (W2-6)
```

**Scope of `VACUOUS_PROVEN`, carried exactly:** the declared local-chain contract
consumes no sealed `E_C` seam. No equality of stationary families across A7
branches is asserted; no `S_(w,epsilon)` family is formed; no branch is computed,
chosen, or bound. **`A7` continues to carry ZERO and IDENTITY through any later
formed period machinery.**

[YOURS] **The field is DISPLAYED, never omitted.** V001's requirement survives the
determination and is the reason the field appears in the lead: a payload silently
lacking the branch index cannot be distinguished later from one whose branch was
dropped. What changes is only that the value is now **proven** rather than forked:
`A7_BRANCH = VACUOUS_ON_LOCAL_ROUTE`.

[PROVABLE] The residual rule is unchanged: *"If the prediction-map schema cannot
carry both branches, that is an unresolved gate, not permission to choose one."*
It governs the later period machinery, where A7 is **not** vacuous.

### (V1-7) `nu` symbolic throughout — [YOURS] — carried verbatim

`nu` is carried as a **symbol** at every step. No numeric `nu` is substituted,
defaulted, fitted, or set to a convenient value at any point of steps 8–11 or the
assembly. Any step that cannot proceed with symbolic `nu` is a **stop**, reported
as such, not a licence to instantiate one.

### (V1-8) The sensitivity ladder's place — carried verbatim

[PROVABLE] Chain step 12 is *"Sensitivity-system and witness-to-number ladder
preparation."* [YOURS] Its place is **after** the Step-11 conditional result, and
it is theory-side only: *"`(M5)` is not repaired by sensitivity alone."*

[PART-PROVABLE] Under `(W2-6)` there is no A7 branch index on this route, so the
V001 phrase "per A7 branch" is withdrawn **for the local route only**. Sensitivity
is computed once here; branch-indexed sensitivity returns with the period
machinery.

### (V1-9) The rationality check's place — [PROVABLE] — carried verbatim

All finite-`L` objects in **exact rational or exact symbolic arithmetic**, with
the implementation **named and hashed**, and any enclosure reported as exact
rational strings. [YOURS] It is a **precondition on every numeric step**: a
float-path result is **not a value of this declaration at all**.

### (V1-10) The assembly, symbolic-first — carried verbatim

[PROVABLE] `K_*` — the onset root of `C_record(K_*) = 0` with `dC_record/dK ≠ 0`
and `K_* > 0` — **is alpha's stiffness**, and `alpha_micro = 1/(4 π K_*)`.

```text
1. Derive the assembly SYMBOLICALLY first, nu symbolic, arithmetic exact.
2. The symbolic form is the deliverable of this declaration.
3. NO numeric alpha is produced by this declaration under any outcome.
4. Any numeric evaluation is a SEPARATE licensed act behind (G6), and it
   remains subject to every type in the lead.                    (V1-10a)
```

[YOURS] `alpha_micro` computed from a `LOCAL-SHADOW` input is a
`LOCAL-SHADOW`-typed expression, per `(W1-2)`.

---

## V2. THE ORDER OF OPERATIONS AND THE GATE MAP

### (V2-1) The authoritative ancestry — [PROVABLE] — carried verbatim

The ruled 11-node graph governs; `stage_dependencies` is authoritative for node
set, edges, and fail-closed descendants; certification is **transitive**. The
ruling's binding phrase travels: *"This act cannot loosen any gate."*

### (V2-2) The gate map — **K2 APPLIED at row 2**

| # | Operation | May not begin until | Certification point |
|---|---|---|---|
| 0 | **This declaration is ruled** | `(M5a-V002)` supplies the conjuncts this DoR requires of it | principal's Step-2 act |
| 1 | Any **numerical** execution whatsoever | `A32_PRE_EVALUATION_READY = true`, i.e. the full rail `SPEC-SEAL → {HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL} → PREDICTION-MAP-SEAL` with every non-seal conjunct at each node | evaluator-certified per `(G3)` |
| **2** | **Step 8 (`RetExtract[dot Schur]` and `a_loop` extraction)** | **`A32_PRE_EVALUATION_READY` + successful chain Steps 0–7 on the same `w` and `D_w` + `C_RET_SCOPE_w` + differentiability on that branch** `(W1-7)` | evaluator |
| 3 | Step 9 (`A_loop`, `q_loop`) | gate 2; **plus `MODULUS_COMPATIBILITY_CERT[w]` witness named and sealed** | evaluator |
| 4 | Step 10 (lattice cell determination) | gate 3; the governing six-cell identity `(W3-1)`/`(W3-2)` | evaluator |
| 5 | Step 11 (Banach consequence) | gate 4; plus `domain_complete_cert_w` and `D_w` nonemptiness by explicit `C_ret` clause | evaluator |
| 6 | Step 12 (sensitivity) | gate 5 | evaluator |
| 7 | Symbolic assembly `1/(4π·K_*)` | gate 6; `K_*` established as the onset root, `nu` still symbolic | evaluator |
| 8 | **Any numeric alpha value** | `ALPHA-RESULT-SEAL`, which by `(G6)` requires `THOMSON-RESULT-SEAL` **and** `PARENT-COMPARISON` **and** `HOLDOUT-UNIVERSE-SEAL` **and** `PREDICTION-MAP-SEAL`, each transitively certified | evaluator + a separate licensed act |
| 9 | **Any comparison to a measured value** | after the governed result seals; beacon, unmask and contamination only at their ruled positions | Step 9 of the escrow order |

[PROVABLE] Row 4 no longer waits on a count: `C1` fixed it, and only gate 3 plus
the six-cell identity govern Step 10.

[PROVABLE] **Cumulative inheritance is now carried at the chain's entrance.**
Because row 2 requires successful Steps 0–7, every later row inheriting row 2
inherits them transitively — the chain's rule *"Every step inherits all
successful preceding gates"* is satisfied through the corrected entrance rather
than restated per row.

[PROVABLE] Step 8 of the board seals the assembled number; **it does not
retroactively create the prerequisite prediction-map seal.**

### (V2-3) Where `(M5a-V002)`'s conjuncts gate which step — carried verbatim

```text
instrument_ratified                                   -> gate 0   (SATISFIED)
schema_and_canonical_ID_tensions_resolved             -> gate 1
executable_eligibility_and_comparator_interfaces_frozen -> gate 1, and gate 9
Q25_current_scalar_commitment_disposition_carried     -> gates 1 and 9 (headline)
SPEC-SEAL + its non-seal conjuncts                    -> gate 1 (root)
HOLDOUT-UNIVERSE-SEAL + conjuncts                     -> gate 1, again at gate 8
QSPEC-SPEC-SEAL + conjuncts                           -> gate 1
PREDICTION-MAP-SEAL                                   -> gate 1, again at gate 8
every_type_in_Task6TypedPayload_preserved             -> EVERY gate, 1 through 9
```

[YOURS] Type preservation sits on **every** gate: type erasure can occur at any
handoff, and the failure ladder arms it without limiting it to a step.

[PART-PROVABLE] `SPEC-SEAL` is `false_of_record` and is the graph root, so under
the fail-closed rule **no gate above 0 is presently open** — the ladder is
unformed at its root, not partway up.

---

## V3. WHAT IS NOT COMPUTED, AND WHY — carried verbatim from V001

| # | Not computed | Why, with its citation |
|---|---|---|
| 1 | The period-native functional | `Γ` unformed on **both** routes; `PERIOD_NATIVE = false` is ratified |
| 2 | Anything requiring `ExtSrc` | `TYPE-U` at derivation level; the named reopening object |
| 3 | Anything on the new-cycle factors | A6 carries the projected old-image scope only; no upward new-cycle lift, **by law** |
| 4 | Any comparison before the seal | gate 9; `proof_authorized` ≡ `FINAL-CLAIM-SEAL` only |
| 5 | `kappa_record` | `kappa_record_computed = false`; `K_*`, not `kappa_record`, is on alpha's path |
| 6 | The seed's value | `END_TEST_STRUCTURAL`, unexecuted by design |
| 7 | `A_RP^+` inhabitance | `EMPTY_OF_RECORD` |
| 8 | R9's identification | `PENDING` its first common physical cell |
| 9 | Any numeric `nu` | (V1-7): symbolic throughout |

---

## V4. THE FALSIFIER AND DISPOSITION PLAN — pre-registered before any value exists

### (V4-1) Lattice-cell dispositions — **K4 APPLIED**

| Outcome | Disposition — pre-registered |
|---|---|
| Cell 1, 2, 3, or 5 | `q_loop = 0`, strict contraction. Step 11 proceeds on the same `w` and domain. Record the cell **and** which of the four it was. |
| Cell 4, `\|chi_K\| < A_loop^{-1}` | strict contraction. Step 11 proceeds. Record the inequality's two sides as exact rationals/symbols, not the verdict alone. |
| Cell 4, `\|chi_K\| ≥ A_loop^{-1}` | **not** strict by this threshold. Step 11 does **not** proceed. Record `NOT_CONTRACTIVE_BY_THRESHOLD`; **no search for an alternative metric to rescue it** — that is the alternate-complete-metric attack. |
| Cell 6 | **excluded of record**; `q_loop = ∞`; record `EXCLUDED_CELL`. No rescue. |

[PROVABLE] V001's fifth row — a stop before Step 10 pending the count — is
**deleted**. `CELLS = six_of_record`; no count ambiguity remains.

### (V4-2) The `chi_K` one-sided readings — carried verbatim

```text
chi_K = 0        -> cells 1/3/5; q_loop = 0; strict contraction.
                    This is a LATTICE CELL, not a seed verdict.
seed q_T,RL = 0  -> chi_K polar; OUT_OF_LATTICE.
                    NOT a negative verdict; no value invented.      (V4-2a)
```

[YOURS] These must not be conflated in any report. A report rendering both as
"chi is zero" destroys the one-sided rider.

### (V4-3) Branch dispositions — **updated by K3**

| Outcome | Disposition |
|---|---|
| **This route** | `A7_BRANCH = VACUOUS_ON_LOCAL_ROUTE`, **displayed** in every payload, never omitted. No branch is computed, chosen, averaged, merged, or bound. |
| Later period machinery | A7 carries **ZERO and IDENTITY**; both reported per branch; no aggregate verdict |
| Schema cannot carry both branches (period machinery) | **unresolved gate**, not permission to choose one |

[YOURS] V001's rows for "one branch exits the lattice" and "both branches
computable" are withdrawn **for the local route**, where no branch index exists,
and are retained for the period machinery, where it does.

### (V4-4) OBS-ledger dispositions and the armed falsifiers — carried verbatim

[PROVABLE] Armed and unchanged: the axiom's `F0–F7`; the four A9-era falsifiers;
the A4/A5 voids; the counterexample regressions; `F-U1`/`F-U2`/`F-C3`. Each of
the following fails the appropriate gate: prior outcome access; an empty eligible
set; registry/universe drift; failed commitment; beacon substitution; duplicate
ambiguity; identical comparator prediction; post-unmask map editing; **type
erasure**; **undeclared cross-sector conversion**; branch/route/member selection;
and any attempted numerical execution before its gate.

```text
EMPTY ELIGIBLE SET  -> FAILS CLOSED. An empty screen is not permission to relax
                       a rule, select a favorable branch, or strip the shadow type.
A WRONG RESULT ON
ONE BOUND MEMBER    -> refutes THAT INSTANCE. It does not refute the whole family
                       without a separate rigidity theorem.          (V4-4a)
```

---

## V5. BATTERY

### (V5-1) `F_PLDEC` and the false anchor — carried verbatim

[YOURS] The only dependency direction used is **downward from sealed structure to
the shadow value**. No step consumes a desired reader value, the false anchor, a
seed outcome, an A7 branch choice, an R9 equality, a HOL return, or any measured
constant. `pi_Mx ∘ Loc ∘ Kernbar ∘ Q = 1` is a **forbidden** input here.

### (V5-2) Anti-tuning ledger — carried verbatim, +1 row from K2

| Attack | Check in this declaration | Result |
|---|---|---|
| **use A32 readiness as chain entrance** | **row 2 requires successful Steps 0–7 on the same `w` and `D_w`; `(W4-2)` records that the implication is absent from every cited rule** | **clean** |
| choose an `E_C` branch to open a route | vacuity **proven** on this route and displayed; both branches carried in the period machinery | clean |
| tune the metric to rescue cell 4 | `MODULUS_COMPATIBILITY_CERT` sealed **before** `A_loop`; no post-hoc metric | clean |
| instantiate `nu` to make a step close | `nu` symbolic throughout; inability is a stop | clean |
| float arithmetic then round to a rational | exact rational/symbolic only, implementation named and hashed | clean |
| relax a rule because the screen is empty | empty fails closed, explicitly | clean |
| set a cross-sector unit silently to one | every factorization displayed in the propagation trace; undeclared conversion is an armed falsifier | clean |
| promote `LOCAL-SHADOW` through A32 | forbidden; type propagates through `1/(4π·)` per `(W1-2)` | clean |
| seal the map after the value | Step 8 does not retroactively create the map seal | clean |
| invent a seventh or eighth cell | `CELLS = six_of_record`; `C1` governs | clean |

### (V5-3) Surface anchor

**Named actual objects:** `D_w`, `d_w`, `K_amb`, `B_w`, `S_w`, `Schur_w`, `Pi_w`,
`ell_w`, `a_loop`, `A_loop`, `q_loop`, `chi_K`, `Rhat_K`, `C_RET_SCOPE_w`,
`MODULUS_COMPATIBILITY_CERT[w]`, `domain_complete_cert_w`, `K_*`, `C_record`.

**Rails awaiting members:** the period-native functional; `ExtSrc`; `Γ` on either
route; `A_RP^+`; the R9 common cell; the P3 comparator; the P4 domain bridge.

[YOURS] The left column is what this declaration computes with; the right column
is what it must not pretend to have. Note that `E_C,N` and `C_N^k` appear in
**neither** column for this route — that is the content of `(W2-6)`.

### (V5-4) Dependency re-audit — everything the four renderings touch

| Rendering | Direct consumers re-walked | Result |
|---|---|---|
| K1 `(W1-3)` | the lead block; every later reference to "the ratified type block" | four fields now attributed to the ratification; `A7_BRANCH`, `ARITHMETIC`, `NOT K_*` re-tagged to their true sources |
| K2 `(W1-7)` | gate row 2; rows 3–7 which inherit it; (V5-2)'s new row | cumulative Steps 0–7 now enter at row 2 and propagate transitively; no other row required editing |
| K3 `(W2-5)`/`(W2-6)` | (V1-6) in whole; the lead's `A7_BRANCH` field; (V1-8)'s "per branch"; (V4-3); (V5-3) | fork withdrawn; field value proven; branch-indexed sensitivity withdrawn **for this route only**; A7 preserved for the period machinery |
| K4 `(W3-1)`/`(W3-2)` | (V1-3); gate row 4; (V4-1); (V5-5); the V001 handoff | all five re-keyed; the count-stop deleted; the handoff removed |

[PROVABLE] No re-walk changed any cell's mathematics, any gate's ancestry, or any
protected flag.

### (V5-5) Self verb audit

| My verb | Check |
|---|---|
| "carried **verbatim** from the ratified SUBJECT" | Now scoped to the **four** fields `(W1-1)` only. K1's exact defect: the claim previously covered six. |
| `A7_BRANCH = VACUOUS_ON_LOCAL_ROUTE` | **PROVABLE** at the declared local-composite contract, with the review's scope paragraph carried. Not a claim that A7 is discharged — it is not, and the period machinery still carries both branches. |
| `CELLS = six_of_record` | **PROVABLE** via `C1`. V001's "inherited discrepancy, not harmonized" is withdrawn: `C1` consumed, six-cell re-key complete. |
| gate row 2 | Now names the chain's cumulative prerequisites. I record that my V001 conflated custody readiness with chain entrance, and that `(W4-2)` is the reason, not merely the correction. |
| `DRAFTED` | A draft for later ruling. `READY_FOR_RULING_PENDING_SUBGATE` is **not** asserted here — `(M5a-V002)` remains `false_of_record`. |
| "is computed" throughout V1 | Means **would be computed when licensed**. `NUMERIC_EVALUATION = false`. |
| `DELTA = bounded` | Four renderings applied plus their dependency re-walk. No clause outside the delta board was altered. |
| Sources | Every clause cites a sealed source or is tagged `[YOURS]`. No physics law, candidate, comparator, number, branch selector, threshold, or seal is authored. |

---

```text
DOR_V002 = DRAFTED (+4/4 renderings applied:
  K1 (W1-3) verbatim — the four ratified fields scoped, A7_BRANCH resolved by W2,
     ARITHMETIC and the NOT-K_* exclusion re-tagged to their true provenance;
  K2 (W1-7) verbatim — Step 8 may not begin until A32_PRE_EVALUATION_READY
     + successful chain Steps 0-7 on the same w and D_w + C_RET_SCOPE_w
     + differentiability, with (W4-2) recorded as the reason;
  K3 (W2-5)/(W2-6) verbatim with its scope paragraph — CONTACT = VACUOUS_PROVEN,
     A7_BRANCH = VACUOUS_ON_LOCAL_ROUTE displayed not omitted, A7 preserved for
     the later period machinery;
  K4 (W3-1)/(W3-2) verbatim + the five-surface landing — CELLS = six_of_record,
     the count-stop deleted, the V001 handoff removed)
DELTA = bounded (changed: the lead's provenance sentence; gate row 2; (V1-6) in
  whole; five C1-dependent surfaces. Unchanged: every other V001 clause, carried
  verbatim. No cell mathematics changed; no gate opened; no value produced)
VERB_AUDIT_SELF = CLEAN
```

All four renderings are applied as supplied, with adjustments confined to
placement and to the dependency re-walk each one forced. The two items I handed
the reviewer in V001 — whether contact enters the local route, and the six/eight
count — both came back **resolved against sealed text** rather than as forks, and
V002 carries the resolutions rather than the questions.
