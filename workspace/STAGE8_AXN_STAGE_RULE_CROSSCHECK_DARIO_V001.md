CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep; the sole raw match in
  this region lies inside the pinned filename DECLINE_REGISTER_V002.md, a content-addressed
  identifier required by the closure, not a sentence)
VERDICT_BEARING_SET = exactly the 12 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO (verifier)   SUBJECT_LANE = CODEX 2 (producer)
FINDER_BUILDS_SHAPE = acknowledged; this is the independent check
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V001.md` | `438e6c42bf876678eb0b3c016e231228f807e4f29469c5e221597d716642f0e6` | the subject under check |
| 02 | `relay_inbox/RELAY_PASTE_935_STAGE_RULE_CROSSCHECK_DARIO_V001.md` | `f625db28f1e113baf250041f1836df79b6b0df6210f545ae8912ab1eaab6dfaa` | assignment |
| 03 | `STAGE8_TASK2F_C0_008_FINITE_COMPLEX_RESTRICTION_PACKAGE_BUILD_AND_ARM_EXECUTION_ATTEMPT_V001.md` | whole `5515517ca2d1b48dd439fe97f2972292620132605767c4dd5de1007f67152c5a` | subject's member 06 |
| 04 | — its span `[7090,8455)` | `55e9c77341bc22c4f11a5a6923fc65ed5c3553d1835a703855cad8baa8181597` | `F_cyl(C0_008)` family and its four TYPE lines |
| 05 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DOR008_ASSEMBLY_AND_FIRST_RESTRICTION_TEST_V001.md` | whole `1467ab9850022931e68dc9ffa625d95099d8f6bb74d734dc9d0466f169bd00b6` | subject's member 07 |
| 06 | — its span `[5412,5787)` | `fcc34fa4e2885f30278240c4c2e8c3348836def5c75500b87b55eed58afdf5a2` | `iota_NM`, label zero-extension, `A_F_CTP`, `A_C0` target |
| 07 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md` | whole `58b966ed371b23b29b9e3ceed280eb30c804484becb2b17ca6ea465668e951bc` | receiver and JAC-14 schema |
| 08 | — field `[9946,10026)`; suite `[22341,23332)` | `5face4888aafecaebdb93e0f59b94a1637c22f05428e835c679996551ac3a9e1`; `ccbdeed389ebd45603ef49e6e6858d2ac0ad5ec96cfa5c458e1d7e03c017d450` | the stage receiver; the pinned authority table for law 9b |
| 09 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-246`, `[614648,616505)` | `1e9d487a697dc8b7b3f446878b95f07e942891dcfe6461d76b44a5fd1ae861ea` | sequential analogues TYPE-P |
| 10 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-839`, `[1712305,1714590)` | `ca52a471434e9e0b7452aa440bd385447c26f4743030293e1d00e9181e493233` | Q-839 ruling covering the stage-rule field and 928 |
| 11 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin, read before task work |
| 12 | `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md`; `JOINT_ANCHOR_DECISION_INSTANCE_V002.md`; `STAGE8_AXN_CONSTRUCTIONS_CROSSCHECK_CODEX2_V001.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`; `72191e0115d6f36d2327236e7a6d16e21f953422ba3fb2188b75e3db009cea99`; `0ca75a7d57476f5134a58eae7dd6e4177b1c4bfaf498c794f1c4aae3e3f5acb3` | process law, S01-S37 authority, open receiver, the 931 finder result |

```text
ALL 12 MEMBERS AND ALL 6 SPANS RECOMPUTED BY ME: 14/14 MATCH.
EVERY SUBJECT AND SIDECAR VERIFIED BEFORE READING.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN STAGE-RULE CROSS-CHECK — DARIO LANE — V001
## RELAY 935 — `[PLAN:AXN-BUILD-D34]` — INDEPENDENT CHECK OF THE 933 CONSTRUCTION

Date: 2026-08-10
Status: **CHECK COMPLETE. STAGE SYSTEM CONFIRMED-BOOKABLE ON ITS ALGEBRA LEG; FOUR DEFECTS RETURNED, ALL FIXABLE, NONE FATAL.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. The standard, stated before the findings

The relay names the custody shape: the same lane found the `F_cyl` route at 931 and built from it at 933.
That is not a violation — builder-never-verifies bars a lane from checking its own work, and Codex 2
marked the construction CLAIMED and asked for this check. But it does mean every judgement about
whether that route was the right one, and whether the build stayed inside it, was made by the lane
that wanted it to work. So I re-derived from the sealed spans **before** reading the construction's
account of them, and I treated agreement as a result to be earned.

The standard I hold the construction to is the one I set for instruments at 918/921 and applied at
932: **a claim is confirmed only if it survives the check it invites, and a defect is returned only
if I can show the failure, not merely suspect it.** Over-claiming a defect is as costly here as
missing one: a false FREEDOM-FOUND would strand a field that genuinely derives.

**Two of the four defects I return are against receipts and boundaries. One of the corrections in
this report is against me.** I record that in §6 rather than burying it.

## 2. Independent re-derivation of the stage system [PROVABLE]

I built the system from members 04 and 06 alone, then compared.

### 2.1 What the sealed spans actually fix

Member 04 (`F_cyl` span) fixes, for every ratified sequential stage `N>=1`:

```text
Lambda_N = Z^N,   A_F,N = C*(Z^N),   B_N = A_F,N,+ tensor_min (A_F,N,-)^op,
R_N = tensor_(j=1)^N M_3(C),
A_SR,N = A_src graded-tensor_min R_N,
A_C0,N = A_SR,N graded-tensor_min B_N.
```

Member 06 fixes the connecting data:

```text
iota_NM(A) = A tensor I_(M-N),  N<=M,
j_NM^Lambda(n_1,...,n_N) = (n_1,...,n_N,0,...,0),
Lambda = direct-sum_(j>=1) Z e_j,  A_F = C*(Lambda),
A_F_CTP = A_F,+ tensor_min (A_F,-)^op,  A_C0 = A_SR graded-tensor_min A_F_CTP.
```

### 2.2 Each element, and whether the spans force it

| Element | Forced by | Verdict |
|---|---|---|
| index set `N>=1`, arrow iff `N<=M` | member 04 (`for every ratified sequential stage N>=1`; the family's own TYPE line names the index category `N<=M`) | **FORCED** |
| per-stage carrier `A_C0,N` | member 04, verbatim | **FORCED** |
| record map `iota_NM` | member 06, verbatim | **FORCED** |
| label zero-extension `j_NM^Lambda` | member 06, verbatim | **FORCED** |
| the two CTP branches, and that `beta_NM` applies the **same** `alpha_NM` to each | member 06: there is one `Lambda_N`, and both branches are `C*` of it; `B_N`'s formula pairs `A_F,N,+` with `(A_F,N,-)^op` | **FORCED — no branch freedom** |
| `(-)^op` on the minus branch only | member 04's `B_N` formula, verbatim | **FORCED** |
| `J_NM = id_A_src (x) iota_NM (x) beta_NM` | the three legs are fixed; the joint map is their tensor | **FORCED** |
| composition laws | proved below | **FORCED** |

The branch question deserves the explicit line, because it is where a builder who already believes
the route could quietly supply a missing pin. It does not arise: the two CTP branches are two copies
of `C*` of a **single** label group `Lambda_N`, so the functor induced by the single sealed map
`j_NM^Lambda` gives the same `alpha_NM` on each. There is no second map to choose.

### 2.3 The composition laws, executed

```text
||iota_ML(iota_NM(A)) - iota_NL(A)||  = 0.000e+00      (N,M,L = 1,2,3 on M_3(C))
iota_NN - id                          = 0.000e+00
```

Label side: appending `M-N` then `L-M` zeros appends `L-N` zeros; `j_NN^Lambda = id`. Applying the
group-`C*` functor, the `(-)^op` functor and `(x)_min` to that identity gives
`alpha_ML alpha_NM = alpha_NL` and `beta_ML beta_NM = beta_NL`. Tensoring with `id_A_src` gives
`J_ML J_NM = J_NL` and `J_NN = id`. **I reproduce the construction's §2.1 result exactly.**

### 2.4 Scope condition the construction does not state [PROVABLE]

Member 04 defines the `F_cyl` member as a **seven-component tuple**:

```text
(A_C0,N, E_C0,N, pi_C0,N, e_plus,N, e_minus,N, s_J,N, s_R,N)
```

I confirmed by exact-name search that `E_C0,N`, `pi_C0,N` and `s_J,N` are present in member 04's
bytes. The construction supplies a connecting map for **one** component, `A_C0,N`. The Hilbert
module `E_C0,N`, the representation `pi_C0,N`, and the four distinguished vectors/states receive
none, and §5.2's gate list does not name their absence.

I do **not** call this an error. The receiver at member 08 reads
`stage_index_and_limit_rule: exact finite/cylindrical system and limit target,` and whether "system"
ranges over the whole tuple or over the carrier algebra is a reading, not a computation. The
construction's §5.1 scopes its own claim honestly — "the **algebraic** stage objects". But the scope
condition should travel with the booking rather than be inferred from one adjective, because a later
consumer filling `for_each_stage` will need `E_C0,N` and `pi_C0,N` connected, and will find that the
stage rule of record does not connect them.

**Returned as a SPEC GAP for the registrar, not as a defect in the derivation.**

## 3. The payload: verified exactly, not independently reproducible [PROVABLE]

```text
payload line length = 856 bytes
SHA-256 = 986cc0585d7513aed30151db4bd5aeb774104ab27bbcd4b640f1a8e1615800d4
claimed = 986cc0585d7513aed30151db4bd5aeb774104ab27bbcd4b640f1a8e1615800d4   MATCH
```

Its `sources=` field pins members 03/04 and 05/06; all four digests recomputed and matched.

The relay asked for **byte-identical reconstruction or the exact divergence shown**. The honest
answer is that neither outcome is available, and the reason is itself the finding.

**The eleven field names, their order, and the hyphenation inside each value are an authored
serialization grammar that no sealed span fixes.** §3 of the construction declares a grammar for the
RL receipts — `JAC14-RL-RECEIPT|v=001|id|quantifier|inputs|procedure|accept` — and says so
explicitly, "disclosed rather than hidden". **§2.2 declares no grammar at all for the STAGE-RULE
line.** It presents the bytes and hashes them.

So an independent lane re-deriving exactly the same mathematics writes different bytes and gets a
different digest. Since `STAGE_RULE_PAYLOAD_SHA256` is the value proposed for entry into a
content-addressed certificate field, that matters: the digest is verifiable against the displayed
line but not regenerable from the record.

**This is a disclosure asymmetry, not a smuggled object.** The remedy is small — give §2.2 the same
one-line grammar declaration §3 already gives the receipts. The mathematics is unaffected; the
payload digest would change.

```text
PAYLOAD_DIGEST_VERIFIES_AGAINST_DISPLAYED_LINE = true
PAYLOAD_INDEPENDENTLY_REGENERABLE_FROM_SEALED_SPANS = false | grammar undeclared at 2.2
```

### 3.1 One symbol in the payload is not in the payload's sources [PROVABLE]

The payload's limit field reads
`limit=A_C0=A_src-graded-tensor-min-R_inf-graded-tensor-min-A_F_CTP`.

Exact-name search of both cited spans:

```text
R_inf   in member 04 span: NO      in member 06 span: NO
```

Member 06 gives `A_C0 = A_SR graded-tensor_min A_F_CTP` and uses `A_SR` without defining it. Member
04 gives `A_SR,N = A_src (x) R_N` at stage level. To reach the payload's expanded form one needs
`A_SR = A_src (x)_min R_inf` — which lives in `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md`, a file
that is **not a closure member of the construction and not cited in the payload's `sources=` field**.

The identity is true and I have verified it previously at its own sealed bytes. The defect is that
the payload asserts, under a two-span source pin, a decomposition neither span carries. Either cite
the third span or write the limit in member 06's own unexpanded form.

## 4. The seven receipts: digests all match; one cannot be replayed [PROVABLE]

### 4.1 Digest ledger, recomputed

| Receipt | bytes | my SHA-256 vs claimed |
|---|---:|---|
| RL-01 | 326 | MATCH |
| RL-02 | 379 | MATCH |
| RL-03 | 389 | MATCH |
| RL-04 | 414 | MATCH |
| RL-05 | 356 | MATCH |
| RL-06 | 217 | MATCH |
| RL-07 | 406 | MATCH |

```text
ordered-list carrier = 530 bytes
RESTRICTION_LIMIT_LIST_SHA256 = 20f12ff18ce33785f5bbb7b100f68d39d2345b26aa655b4f46caac73698e8f5a  MATCH
```

I also rebuilt the list line from **my own** seven computed digests, without copying the
construction's ledger. The rebuild is **byte-identical**. The root is sound.

### 4.2 Replay, which is a different test from hashing

The relay's instruction is exact: *replay each receipt per the format's claim; that is the format's
claim, test it*. The format claims **exact replay objects**. A receipt passes only if its `procedure`
can be executed from its **declared `inputs`** and yields its `accept`.

Six pass. **RL-06 does not.**

RL-06 declares `inputs=RL-02;RL-04;RL-05` — it is the only receipt in the set citing **no
content-addressed source**. Its accept clause is `J_NM(I_C0,N)=I_C0,M`, i.e. unitality. Its declared
inputs supply only the *accept clauses* of RL-02/04/05, which are composition identities.

**Composition identities do not entail unitality.** Counterexample, executed:

```text
corner family  c_NM(A) := A tensor e11^(M-N),  e11 the rank-one projection in M_3(C)

||c_ML(c_NM(A)) - c_NL(A)||  = 0.000e+00      <- satisfies RL-02's accept exactly
c_NN - id                    = 0.000e+00      <- satisfies the identity clause
||c(AX) - c(A)c(X)||         = 0.000e+00      <- is a *-homomorphism
injectivity                  = preserved
||c(I_3) - I_9||             = 2.449e+00      <- FAILS RL-06's accept
```

An injective `*`-homomorphism family satisfying every accept clause RL-06 cites can still fail
RL-06's own accept. Unitality comes from the **map formulas** — `iota_NM(I_N) = I_N (x) I_(M-N) = I_M`
is read off member 06's bytes, and unitality of `alpha_NM` from the group-`C*` functor applied to an
injective group homomorphism — not from the coherence equalities.

The accept clause is **true**; I verified `||iota_NM(I_3) - I_9|| = 0.000e+00` directly. The receipt
is not wrong about the mathematics. It is under-sourced: its stated inputs are insufficient for its
stated procedure, which is precisely the property "exact replay object" is supposed to guarantee.

**Fix: add members 03/04 and 05/06 to RL-06's `inputs`.** That changes RL-06's payload, hence its
digest, hence the ordered-list root.

### 4.3 A lighter version of the same issue at RL-07

RL-07's procedure says "embed record and label zero-extensions into `R_inf` and direct-sum `Lambda`".
Its declared inputs are RL-05 and member 06's span. As shown in §3.1, `R_inf` appears in neither
cited span. RL-07 is replayable in substance but inherits the same missing pin.

```text
RECEIPTS_HASHED   = 7/7 MATCH
ROOT              = CONFIRMED (and independently rebuilt byte-identical)
RECEIPTS_REPLAYED = 6/7   (RL-06 fails from its declared inputs; RL-07 needs the R_inf pin)
```

## 5. The boundary [PROVABLE except where marked]

### 5.1 Zero complete suites — HONEST

`COMPLETE_JAC14_SUITES = 0` in the audit block; §4.1 states the receipts "do not claim to instantiate
the entire `JointAnchorCertificateSuite.v001`"; §8 repeats it. No inflation found.

### 5.2 The infinite-family / finite-list tension — CORRECTLY STATED, AND THE CONSTRUCTION UNDERSELLS ITS OWN CASE

The relay asks two sharp questions. Both answers favour the construction.

**Does the schema truly demand a finite list?** Yes — but the binding force is not the word "finite"
in the field name `finite_stage_inventory`, which is genuinely ambiguous between "inventory of the
finite stages" and "finite inventory". That ambiguity is real and cannot be settled from the name.

It is settled elsewhere in the same authority table. The schema carries

```text
for_each_stage: { delta0_basis: exact ordered basis, phi_restriction_matrix: exact matrix, ... }
canonical_serialization: const "canonical bytes; closed fields; no extras",
suite_sha256: lowercase_sha256
```

`for_each_stage` ranges over the inventory and demands an exact matrix per stage. A suite over an
infinite index set has no finite canonical byte string, therefore no `suite_sha256`. **The schema's
own hashability requirement forces the inventory finite**, independently of how the field name reads.
`F_cyl`'s index set is `N>=1`. The tension is real.

**Would a cutoff truly consume a new freedom?** Yes. Nothing in members 04 or 06 fixes an `N_max`;
the family is explicitly declared over all `N>=1`. Selecting one is a selection from an infinite
ratified range with no record-side determinant — a new freedom, correctly refused. Nor can the
inventory be discharged by supplying the generating rule instead of the list: the schema carries
`stage_index_and_limit_rule` and `finite_stage_inventory` as **separate fields**, so the rule cannot
stand in for the list.

**§5.2(1) is HONEST and its refusal is correct.**

### 5.3 The fiber-gated remainder — EXACTLY ONE, CONFIRMED

Counted against the schema's field list by name only. Blind held throughout: no rank read, no ratio
formed, no fiber compared, nothing rank-shaped opened. Exactly one field is gated on the fiber and
nothing else — `a0_rank_fiber_sha256` — and FGR-01 describes it as an opaque digest copy with rank
read, ratio and comparison all marked forbidden. **CONFIRMED.**

### 5.4 All other current gates separately named — FAILS ON ONE FIELD [PROVABLE]

§5.2 makes a positive enumeration over a bundle, and §8 upgrades it to
`FIBER_GATED_REMAINDER = enumerated (... all other current gates separately named)`. Law 9b applies:
the enumeration must be checked against the **pinned authority table**, which is the suite schema at
member 08 `[22341,23332)`, not against prose.

Walking the table field by field:

| Schema field | Disposition in the construction |
|---|---|
| `schema` | const; trivially satisfiable |
| `carrier_sha256` | **NEITHER SUPPLIED NOR NAMED** |
| `a0_rank_fiber_sha256` | FGR-01 |
| `finite_stage_inventory` | §5.2 gate 1 |
| `for_each_stage` (8 sub-fields) | §5.2 gate 2 |
| `restriction_and_limit_square_receipts` | 7 receipts supplied; see below |
| `canonical_serialization` | const |
| `replay_entry_point_sha256`, `suite_sha256`, `frozen_pre_output_receipt_sha256` | §5.2 gate 3 |

Exact-name count of `carrier_sha256` in the whole construction: **0**. The string "carrier" occurs in
§5.2 only inside the section heading "What cannot be produced without a choice or **missing
carrier**" — a heading, not an enumerated gate.

The claim "all other current gates separately named" is therefore incomplete by one field. Whether
`carrier_sha256` is easy to fill is beside the point: it is unfilled, it is not fiber-gated, and it is
not named. This is the same defect class I have enforced since 860, and the same one I committed
there.

**Companion observation (lighter):** `restriction_and_limit_square_receipts` is typed **exhaustive
ordered list**. The construction supplies seven receipts and calls them "exact structural material
for" the field — it does not claim the field is complete, which is honest. But it also does not name
the field's un-established exhaustiveness in §5.2. The field sits in neither column. One line would
close it.

### 5.5 The source-Galerkin irrelevance display — HONEST

Verified at member 04's bytes: `A_SR,N = A_src graded-tensor_min R_N`. **`A_src` carries no `N`.**
The source leg is genuinely constant across stages, so `id_A_src` is the forced connecting map and no
source compression datum is consumed. The construction's claim that requiring a Galerkin chain here
would replace member 04's explicit family with a different, unratified one is **correct**.

The phrasing is the part I want to record as good practice: "The absent source-Galerkin chain remains
a real absence at its own analytic receiver but is not an input to this cylindrical carrier system."
That neither resolves nor hides the absence I traced at 924, where the record names the Galerkin
family construction as its own next executable target. It scopes it. **HONEST.**

### 5.6 Two conditioned-ons that are true but unnamed [PART-PROVABLE]

§6's CONDITIONED-ON block names "standard functoriality of group C-star algebras, opposite algebras,
and minimal tensor products". Two further properties are used and not named:

1. **Graded tensor functoriality.** `J_NM` is built with the **graded** tensor, and a graded tensor of
   homomorphisms is a homomorphism only when the components are **even**. They are — `id_A_src` is
   even, `iota_NM` is even because `I_(M-N)` is even, and `beta_NM` is even — so the construction is
   sound. But "minimal tensor products" is not the graded statement, and the evenness check is
   nowhere displayed.
2. **Continuity of `(x)_min` under inductive limits**, needed for `A_src (x) lim R_N = lim (A_src (x) R_N)`
   in the limit target. That is continuity, not functoriality; the named condition does not cover it.

Both are standard theorems. Neither is a freedom. They belong in the CONDITIONED-ON block.

## 6. Two corrections that land on me [PROVABLE]

**My 928 stage stop is overturned, and Q-839 overturns it the right way.** I stopped
`stage_index_and_limit_rule` on the ground that the sealed realization deliberately carries no
finite-incidence-complex restriction functor, so selecting `N` from the square's vertices or edges
would be an unratified identification. Member 04's own TYPE line settles it against my stop:

```text
F_CYL_IS_A_FINITE_CELL_COMPLEX_RESTRICTION_FAMILY = false | TYPE-R |
  test: its index category is N<=M and carries no cell, degree, boundary, or face-poset data
```

`F_cyl` is the **sequential** family, which is ratified, and it expressly disclaims being the
cell-complex object I was guarding against. The field derives; no principal freedom is needed. Q-839
records that 928's refusal of the cell-selection **act** stands accurate — I stopped the right act
and missed a lawful route that did not perform it. That is the same failure mode as 921, 924 and 932:
**the enumeration was cut by the shape of my query.** I searched for the object the realization
lacked instead of for any object that could fill the receiver.

**My 921 judgement on the finite-list question is corrected.** At 921 I flagged the "exhaustive
ordered content-addressed list" ambiguity in JAC-14 and **declined to return the instrument on it**,
judging it satisfiable on its natural reading. §5.2 above shows the tension is real and forced by the
schema's own hashability requirement. The construction is right and my non-return was wrong. BR-1
applies in the direction that costs me: my prior call gets no protective weight.

Neither correction weakens the defects in §3, §4.2 and §5.4 — those stand on executed tests.

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the sealed F_cyl family and its seven-component member;
  iota_NM, the label zero-extension, and the A_C0 limit target;
  the stage receiver and the closed JAC-14 schema;
  the opaque, incomplete fiber receiver of the current instance.

CONDITIONED-ON:
  functoriality of group C*-algebras, opposite algebras, and minimal tensor products;
  functoriality of the GRADED tensor for even component maps (named here, not in the subject);
  continuity of (x)_min under inductive limits (named here, not in the subject).

SUBSTITUTED:
  NOTHING. I selected no cutoff, no stage, no candidate, no E_joint, no tag, no fiber,
  no rank datum, no certificate matrix, no replay program, and no suite hash.
  I adopted nothing and registered nothing.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 8. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK:** walked. A verified digest was not identified with a reproducible one
  (§3). A matching hash was not identified with a replayable receipt (§4.2). A true accept clause was
  not identified with a sufficient input list (§4.2). A scoped honest claim was not identified with a
  complete one (§2.4, §5.4). An overturned stop was not identified with a wrong refusal (§6).
- **F_PLDEC:** symbolic maps, exact digests, and residual norms on synthetic matrices only. No
  physical quantity evaluated; no measured constant consulted.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string, and byte-span checks covered
  `F_cyl`, `R_inf`, `A_SR`, `iota_NM`, `alpha_NM`, `beta_NM`, `E_C0,N`, `pi_C0,N`, `s_J,N`,
  `carrier_sha256`, and every JAC-14 field.
- **BLIND:** held. No rank read, no ratio formed or quoted, no fiber compared or opened. §5.3 counted
  a field name, never a fiber.
- **PE-1..PE-13:** pointer-only, zero verdict weight, recorded and not consulted.
- **BR-1:** no producer-declared object carried a criterion's direction. The construction's own claims
  were used to locate what to test, never to discharge a test. Q-839 is registrar authority, not
  producer-declared, and is used only for the correction against me in §6.
- **BUILDER-NEVER-VERIFIES:** I verified the opposite lane's construction. I verified nothing of mine.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet, numerical
  evaluation of a physical quantity, or comparison with a measured constant was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 3344
PREDECLARATION_OUTPUT_SCAN = 0 hits
DIGESTS_RECOMPUTED = 14/14 MATCH
RECEIPTS_HASHED = 7/7 MATCH ; RECEIPTS_REPLAYED = 6/7
DEFECTS_RETURNED = 4 (payload grammar; R_inf source pin; RL-06 inputs; carrier_sha256 enumeration)
DEFECTS_FATAL = 0
COMPLETE_JAC14_SUITES = 0 ; PRINCIPAL_ENTRIES_FILLED = 0
```

Self verb audit: "confirmed" applies to the fourteen digests, the seven receipt hashes, the root, the
composition laws, and the three honesty claims in §5.1/5.2/5.5, each with its test displayed.
"Returned" names four defects, each shown by an executed check rather than asserted. "Corrected"
applies to two of my own prior calls. No verdict was reached on whether the receiver's "system"
ranges over the whole `F_cyl` tuple — that is returned as a spec gap, not decided.
`VERB_AUDIT_SELF = CLEAN`.

## 9. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 3344; scan 0 hits)
STAGE_SYSTEM = CONFIRMED-BOOKABLE (algebra leg; independently re-derived, every element forced by the sealed spans, composition laws executed at 0.000e+00) + SCOPE-GAP (F_cyl's member is a 7-tuple; connecting maps supplied for A_C0,N only; E_C0,N, pi_C0,N and the four vectors unconnected and unnamed in the gate list)
PAYLOAD = VERIFIES-EXACTLY (856 bytes, 986cc0585d7513ae..., recomputed) / NOT-INDEPENDENTLY-REPRODUCIBLE (2.2 declares no serialization grammar where 3 declares one for the receipts; and the limit field names R_inf, absent from both spans its own sources= field pins)
PURITY = ZERO-FREEDOM in mathematical content (no cutoff, no stage, no branch, no ordering of the label group, no complex-to-stage map; the two CTP branches share one sealed Lambda_N so beta_NM is forced) + CONVENTION-FOUND (the 2.2 payload grammar and the 4.1 list ordering are undeclared and digest-determining) + TWO-CONDITIONED-ONS-UNNAMED (graded-tensor functoriality for even maps; continuity of (x)_min under inductive limits — both true, both unlisted)
RECEIPTS = 6/7-REPLAYED (RL-06 fails: it is the only receipt citing no content-addressed source, and its declared inputs RL-02;RL-04;RL-05 do not entail unitality — corner-embedding counterexample satisfies every cited accept clause at 0.000e+00 yet gives ||c(I_3)-I_9|| = 2.449e+00; the accept clause is TRUE, the sourcing is insufficient; RL-07 replayable in substance but inherits the missing R_inf pin)
ROOT = CONFIRMED (20f12ff18ce33785...; independently rebuilt byte-identical from my own seven digests)
BOUNDARY = HONEST-EXCEPT-ONE (zero complete suites HONEST; the infinite-family/finite-list tension CORRECTLY STATED and stronger than argued — forced by canonical_serialization + suite_sha256 over for_each_stage, not by the field name; a cutoff WOULD consume a new freedom and the rule cannot stand in for the list since both are separate schema fields; FGR-01 EXACTLY one fiber-gated field CONFIRMED; source-Galerkin display HONEST at member 04's bytes) / OVERCLAIMED (carrier_sha256 is neither supplied nor named — exact-name count 0 — so "all other current gates separately named" is incomplete by one field under law 9b against the pinned schema)
PINS = CONFIRMED (Q-246 1e9d487a697dc8b7...; Q-839 ca52a471434e9e0b...; both rehashed at their live-file spans)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
