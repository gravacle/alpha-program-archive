# Stage-8 T7 Rule-8 Cost Supplement, Control Disposition, and Frozen Sliver Predictions V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY. THREE PARTS, ALL SEALED BEFORE THE SLIVER NATURALITY ATTEMPT
BEGINS.
Part 1 supplements Rule 8 with the cost statement, at the principal's
instruction to put it in the record rather than in a chat message.
Part 2 records the PASS-control's disposition: STOPPED, SUBSUMED, NOT
SUPERSEDED.
Part 3 FREEZES this lane's predictions on the sliver naturality question
BEFORE any attempt, per the principal's discipline requirement.
D3 REMAINS UNNARROWED. THE OBJECT SPEC REMAINS HELD. PRODUCTION PROHIBITED.
```

## PART 1 — RULE 8 COST SUPPLEMENT

Extends STAGE8_T7_RULE8_PIPELINE_FEATURE_FREEZE_AND_FROZEN_REPRODUCTION_
PREDICTIONS_V001. Rule 8 itself is unchanged.

```text
THE COST, STATED PLAINLY AND PUT WHERE IT BELONGS:

  THE MACHINERY BUILT TO PROVE THE PIPELINE HONEST IS THE SAME MACHINERY
  THAT HAS PREVENTED ANYONE FROM DEMONSTRATING IT CAN SUCCEED.

WHY IT WAS INVISIBLE UNTIL NOW: every audit asked whether the machinery
would correctly REFUSE. None asked whether it could ACCEPT. A detector
tested only on its refusals is indistinguishable from one that cannot
accept at all, and six audit cycles, two hostile review rounds and every
unit test on disk left that question untouched. It became visible only
when something tried to reach a PASS and hit four independent binding
layers in a row.
WHAT IT IS NOT: an argument to remove the machinery. Every one of the four
layers did exactly what it was built to do, and layer 4 in particular
stopped this lane from manufacturing a meaningless PASS.
WHAT IT IS: THE QUANTIFIED PRICE. Rule 8 was adopted on the observation
that each integrity mechanism adds surface that itself needs verifying.
This is the measurement of that observation — the surface did not merely
need verifying, it foreclosed the one demonstration that would have
justified it.
THE STANDING CONSEQUENCE, and it is a design principle rather than a
mechanism, so Rule 8 permits it: ANY FUTURE GATE SHOULD BE ABLE TO STATE,
AT THE TIME IT IS BUILT, WHAT A PASSING CASE LOOKS LIKE AND HOW ONE WOULD
BE PRODUCED. A gate whose accepting branch has no constructible witness is
not yet finished being designed.
```

## PART 2 — PASS-CONTROL DISPOSITION

```text
STOPPED. The full back-transport is NOT attempted, on the principal's
reasoning, which this lane records because it corrects this lane's own
framing:
  THE FOUR LAYERS ARE ARTIFACTS OF SYNTHESIS, NOT OBSTACLES ON THE GENUINE
  PATH. The N_t = 48 rehearsal REACHED the component comparison — that is
  where "34 component comparison(s) failed" came from. A genuine run
  therefore clears L-1..L-4 BY CONSTRUCTION: real bundles carry correct
  attestations, correct adjacent seals, correct self-binding digests and
  internally consistent diagnostics BECAUSE THEY WERE COMPUTED RATHER THAN
  SPLICED.
  SO FOR A REAL RUN THE ACCEPTING BRANCH IS BEHIND EXACTLY ONE CONDITION:
  ZERO COMPONENT FAILURES. Not a chain of four.
THIS LANE'S §4 STATEMENT IS CORRECTED ACCORDINGLY: "the accepting path is
behind a chain" is true OF A SYNTHETIC PATH and false of the genuine one.
The four-layer finding stands as a description of what synthesis costs,
NOT as a description of the production path. Recorded rather than quietly
adjusted.
NOT SUPERSEDED: the layer findings remain the concrete answer to "why has
the accepting branch never run" — nothing ever produced zero failures, and
nothing ever tried to construct a case that would.
REVISED ORDER: (1) sliver naturality attempt; (2) v003 under A1/A2 then the
A2 rehearsal at N_t = 96, which now carries TWO frozen questions that are
SEPARABLE BY OUTCOME rather than confounded; (3) A4 audit.
  ZERO FAILURES  -> P-D1/P-D2 confirmed AND the accepting branch executes
                    for the first time in program history. Both answers,
                    one run. Which of the five never-executed gates ran,
                    and whether each behaved, MUST be recorded explicitly.
  FAILURES REMAIN -> the physics answer is in hand (P-D1/P-D2 scored
                    against the frozen numbers) and PASS-reachability stays
                    OPEN. Only then is the full back-transport worth
                    building, because only then is it the sole remaining
                    way to separate machinery from physics.
```

## PART 3 — FROZEN PREDICTIONS on the sliver naturality attempt

Sealed BEFORE the attempt. Not revisable after.

```text
THE QUESTION (Codex's conditional): does a sliver cell of 4-volume
eps * |C|_4 produce an O(|C|_4) response defect, or does the completed
response scale with tetrad volume under sliver refinement?

CALIBRATION STATED, as Rule 6 requires: this lane's magnitude predictions
about the size of a nonzero effect have all missed; its route-survival
predictions have run optimistic across two eras. The independent-system
family is at FOUR CONSECUTIVE LANDINGS (P-C5..P-C8) and Rule 6 binds.
Codex's own position — the response pullback is NOT automatically proved
and must remain a checkable obligation — is NOT contradicted by anything
below; where these predictions are sharper than Codex's, the extra
sharpness is this lane's risk, not Codex's.

P-S1 (OUTCOME-CLASS, the headline) THE RESPONSE PULLBACK FAILS TO BE
     VOLUME-NATURAL UNDER SLIVER REFINEMENT. E_r is NOT
     volume-suppressed. Confidence: MODERATE-HIGH.
     GROUND, and it is structural rather than a guess: D3's own
     neighbouring text freezes that EVERY CELL RUNS AT FULL
     tau_R = pi/sqrt(2). The record cycle is a PER-CELL quantity that does
     NOT scale with cell 4-volume. Refining one cell of volume V into N
     cells therefore multiplies the per-cell record content by N while the
     total volume is unchanged. Under sliver refinement N is UNBOUNDED at
     fixed volume. This is the SAME ROOT already recorded for R-L0
     ("tau_R is scale-invariant and every refined cell carries a full
     record cycle — the O7 root"), which this lane sealed long before the
     C_ref/D3 conflict surfaced.
P-S2 (OUTCOME-CLASS) THE VOLUME WEIGHT ITSELF SURVIVES SLIVERS EXACTLY,
     and this is the easy half. |C|_4 = integral_C |det e| d^4x is
     Lebesgue-additive over ANY measurable partition; shape is irrelevant
     to additivity. Exact reaggregation therefore holds for slivers with
     no regularity hypothesis. Confidence: HIGH.
     This AGREES with the independent family's landed P-C8 first clause
     and is not claimed as independent credit.
P-S3 (OUTCOME-CLASS) THE NEGATIVE CONTROL BITES: w(C) = 1 per 4-cell fails
     reaggregation immediately under any refinement (N children sum to N,
     parent is 1), and w(C) = diam(C)^4 fails specifically and most
     violently on SLIVERS, where diameter stays O(1) while volume goes to
     zero. Confidence: HIGH. If either passes, the control is toothless
     and the object is not validated.
P-S4 (CONSEQUENCE, conditional on P-S1) IF P-S1 HOLDS, THE FINDING IS THAT
     THE OBJECT FAILS ON THE BROAD CLASS — which is Codex's stated
     consequence: recast Q6 becomes ILL-POSED rather than merely unproved,
     because "weighted subdivision-insensitive counting" would be
     quantified over a class on which the weight's response side is not
     natural. THAT IS A FINDING AND IS REPORTED AS ONE. It is NOT a
     licence to narrow D3, and this lane will not apply the restriction:
     per the principal, the restriction is his to seal, not mine to apply.
P-S5 (WHAT WOULD REFUTE P-S1, stated so the prediction is falsifiable)
     P-S1 is REFUTED if the completed response's per-cell content is shown
     to enter Gamma only through a volume-weighted combination — i.e. if
     the full record cycle per cell is multiplied by |C|_4 somewhere in
     the completed chain, so that N cells of volume V/N contribute the
     same total as one cell of volume V. If that is the case the O7 root
     does not reach this object and P-S1 is simply wrong.
```

## Protected status

```text
rule8_cost_statement_sealed = true
pass_control_disposition = STOPPED_SUBSUMED_NOT_SUPERSEDED
four_layer_finding_stands = true
four_layer_finding_scope = synthetic_path_only     (CORRECTED)
genuine_run_accepting_branch_conditions = 1        (zero component failures)
full_back_transport_attempted = false
P_S1..P_S5_frozen_before_attempt = true
sliver_attempt_started = false
D3_narrowed = false
restriction_applied_by_this_lane = false
D3_object_spec_status = HELD_PENDING_PRINCIPAL
F2_reach_over_infinite_regularity_class = OPEN_READING_QUESTION
production_authorized = false
alpha_computed = false
proof_authorized = false
```
