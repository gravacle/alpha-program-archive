# STAGE 8 / 7A / Q-126 ROUTE — THE BUNDLE-CLASS SELECTOR HUNT: UNDERDETERMINED, AND THE FALSIFIER'S ANTECEDENT IS ALREADY SATISFIED

Lane: DARIO (Builder B, independent verifier). Relay 765.
Governing: the rebuilt tower 764; the route's own sealed falsifier; the VOID condition.
Nothing adopted.

## Lead determination

**UNDERDETERMINED — and not for want of a candidate. The corpus contains exactly the statement
that would force `|q|`, and it also contains a sealed require forbidding the step that would let
it.**

The candidate is real and I found it: `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V001` carries

```text
Q_Sigma = integral_Sigma j^mu dSigma_mu,   spec(Q_Sigma) = {0,1}
```

on *"the declared **zero-or-one** primitive source sector"* — a **unit-character flux with spectrum
{0,1}**. On the charged branch that is one unit, and one unit through the flux surface would give
`deg L = 1`, `|q| = 1/2`. **Three sealed facts stop it:**

1. **The gate carries NO SIDECAR** (`33a76510…`). The statement that would force is **unsealed**.
2. **A sealed adjudication forbids the identification it needs.** `8eba3542…` (SEALED-OK):
   *"Parent-arm **monopole**, Wilson-loop, graph-cycle, plaquette, and composition-loop hits were
   **not transported onto the source-flux record-write object**. The current adjudication itself
   **requires that separation**"* — citing *"`HOLONOMY` NAMES AT LEAST FOUR DIFFERENT OBJECTS."*
   **The monopole charge on `S2_flux` and the record-write flux `Q_Sigma` are named as objects
   that may not be transported onto one another.**
3. **The derivation headline was already KILLED.** OBS-21 — *"the source-to-loop threading
   assignment is a topological charge — integer-valued by structure … so **charge quantization is
   derived, not imposed**"* — is **KILLED**: *"integrality is the authored codomain condition and
   A9 inhabits nothing."* And that killed preregistration's source was **my own lane's artifact**.

**And the route's own principle pre-emptively bars the two next-most-tempting selectors.** `PRIN`:
the free data are outputs *"only if boundary closure plus the induced action selects them **before
any comparison with alpha, particle masses, family multiplicity, or the source-free endpoint**"* —
so **fixing the index by demanding a generation count is barred** — and *"**A value may not be
called primitive merely because it is an integer** or a compact radius."*

**The sharpest thing I can report is not the verdict but the status.** `PRIN`'s hard-failure rule
fires when *two inequivalent choices satisfy all adopted record rules while producing different
weighted charged determinants*. Today `|q| = 1/2` and `|q| = 1` **both satisfy every sealed rule I
can find**, and they give **different zero-mode counts** — `2|q| = 1` versus `2` — which is a
difference in the determinant's kernel dimension. **The falsifier's antecedent is structurally
satisfied. Only the fact that no determinant is computable yet keeps its consequent from being
evaluated.** The route is already in the condition the rule was written to catch.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively: **absent**, artifact and sidecar both.

### 0.2 Sources verified before use

```text
PRIN   workspace/mirrored_sources/alpha_global_record_surface_superconnection_principle_v001.md
                                                                  ae1d0492…  digest-matched to SPEC's sealed manifest
GATE   workspace/SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V001.md   33a76510…  **NO SIDECAR**
HOLO   workspace/STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md
                                                                  8eba3542…  SEALED-OK
CLOSE  workspace/STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md       (OBS-21 row)
STIFF  workspace/ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md  (named current authority)
CENSUS workspace/STAGE8_INCIDENCE_STRUCTURE_DETERMINATION_TEST_EINSTEIN_V001.md  9cdc25e2…  SEALED-OK
764    workspace/STAGE8_7A_TOWER_REBUILD_DARIO_V001.md             84ceeb49…
TWO    workspace/mirrored_sources/alpha_br_full98_c2_two_cutoff_continuum_theorem_v001.md
                                                                  7a3c6bc6…  UNSEALED, PROPOSED_NOT_CLOSED
```

**Searched space:** recursive glob `./workspace/**/*.md` + `./supervision/**/*.md`, run **from the
archive root**, **all `_DARIO_` files excluded** (199 artifacts; the corrected filter from 758) —
**1,977 files** — with `E_parent` (217) and `S2_flux` (8) as printed known-positive controls.

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities.  No comparison to measured constants.  No common cell formed.
No junction map evaluated.  No smooth data derived.  NOTHING ADOPTED — §3's brief is a
brief.  No register, plan, tracker, or git action.
```

---

## 1. AO1 — THE REQUIRES, ENUMERATED AND MODALITY-TAGGED

| # | Require | Source | Modality (verbatim-tagged) |
|---|---|---|---|
| **R1** | *"Adopting the topology does not select … the parent bundle and its characteristic class … or the chiral index."* | `PRIN` | **DECLARATION of non-selection** |
| **R2** | *"Those are outputs only if **boundary closure plus the induced action** selects them **before any comparison with alpha, particle masses, family multiplicity, or the source-free endpoint**."* | `PRIN` | **REQUIRE on the selector** |
| **R3** | *"**A value may not be called primitive merely because it is an integer** or a compact radius."* | `PRIN` | **REQUIRE (bar)** |
| **R4** | *"The route fails to compute alpha if two inequivalent choices of radii, spin structure, bundle class, or odd profile satisfy all adopted record rules while producing different weighted charged determinants."* | `PRIN` | **REQUIRE (falsifier / hard failure rule)** |
| **R5** | *"On the declared zero-or-one primitive source sector, the conserved unit-character flux … `spec(Q_Sigma)={0,1}`."* | `GATE` `33a76510…` | **UNSEALED** (no sidecar) |
| **R6** | *"Parent-arm **monopole**, Wilson-loop, graph-cycle, plaquette, and composition-loop hits were **not transported onto the source-flux record-write object**. The current adjudication itself **requires that separation**."* | `HOLO` `8eba3542…` SEALED-OK | **REQUIRE (separation)** |
| **R7** | OBS-21: *"charge quantization is derived, not imposed"* → **KILLED**; *"integrality is the authored codomain condition and A9 inhabits nothing."* | `CLOSE` | **KILLED PREREGISTRATION** |
| **R8** | *"Symmetry, compactness, charge quantization … fix the active pointwise `U(1)_rel` character lattice and unit charge … `status = KINEMATICS_ONLY`"*; V001: *"CLOSED AS KINEMATICS; **RETIRED AS A STANDALONE K_R SELECTOR**."* | `STIFF` | **REQUIRE (retirement of a selector)** |
| **R9** | The locked floor gives `|q| >= 1/2` — non-vanishing flux only (764 §1.3) | `TWO` | **UNSEALED, CONSTRAINS WEAKLY** |
| **R10** | Zero-mode count `= 2\|q\| = \|deg L\|` (index theorem on `S^2`) | 764 §1.2 | **DERIVED (mine)** — ties `q` to a count, forces nothing |
| **R11** | *"Gauge, source/proper-time, finite-mass, and index data must therefore be computed from **one spectrum and one induced trace**."* | `PRIN` | **REQUIRE on method**, not on the class |
| **R12** | The Q-69 name census: seven objects sharing "cell"/"complex" **kept separate**; *"MERGING ANY TWO OF THESE ON THE WORD 'CELL' WOULD BE THE NAME-MATCH TRAP."* | `CENSUS` `9cdc25e2…` | **REQUIRE (process law)** — reinforces R6 |

```text
REQUIRES = 12 (modality-tagged)
```

---

## 2. AO2 — THE MATRIX

Candidate classes: `|q| ∈ {1/2, 1, 3/2, 2, …}` (`2q` a nonzero integer, by Dirac quantisation).

| Require | `\|q\|=1/2` | `\|q\|=1` | `\|q\|≥3/2` | Does it FORCE? |
|---|---|---|---|---|
| R1 non-selection | permitted | permitted | permitted | **No** — it *states* the class is unselected |
| R2 selector route | permitted | permitted | permitted | **No** — it restricts *how*, not *which*. **Excludes the family-multiplicity route** |
| R3 integrality bar | permitted | permitted | permitted | **No** — **excludes** "it's an integer, so it's primitive" |
| R4 falsifier | permitted | permitted | permitted | **No** — it is the *kill condition*, §3.2 |
| R5 `spec(Q_Σ)={0,1}` | would give `\|q\|=1/2` | excluded | excluded | **WOULD FORCE — but UNSEALED, and blocked by R6** |
| R6 separation | permitted | permitted | permitted | **No — and it BLOCKS R5's route** |
| R7 OBS-21 killed | permitted | permitted | permitted | **No — it CLOSES the derivation of integrality** |
| R8 kinematics-only | permitted | permitted | permitted | **No — it RETIRES charge quantization as a selector** |
| R9 `\|q\| ≥ 1/2` | permitted | permitted | permitted | **No** — excludes only `q = 0` |
| R10 index link | `2\|q\|=1` | `=2` | `=2\|q\|` | **No** — a *consequence* of `q`, not a constraint on it |
| R11 one-spectrum | permitted | permitted | permitted | **No** |
| R12 name census | permitted | permitted | permitted | **No — reinforces R6** |

### 2.1 The one chain that could have forced it, and where it breaks

```text
S2_flux is "the exterior source-flux comparison" surface            [PRIN, sealed-by-manifest]
   +  conserved unit-character flux, spec(Q_Sigma) = {0,1}          [GATE — **UNSEALED**]
   +  the branch is the ordinary CHARGED durable-source branch      [PRIN]
   =>  one unit of flux through the comparison surface
   =>  deg L = 1  =>  |q| = 1/2
```

**Break 1 — the middle premise is unsealed.** `GATE` has no sidecar.

**Break 2 — the inference needs an identification `Sigma ≅ S2_flux` that a SEALED require
forbids.** `Q_Sigma`'s `Sigma` is the **causal-cell boundary**; `S2_flux` is a factor of the
compact comparison surface `Sigma_BR`. `HOLO` requires the separation of exactly these object
classes and names **monopole** among the hits *not transported onto* the source-flux record-write
object. `CENSUS` independently makes merging-on-a-shared-word the named trap — and the shared
words here are **"flux"** and **"Sigma"**.

**Break 3 — the conclusion's own route is killed.** R7 refutes *"charge quantization is derived,
not imposed"*; integrality is **authored**, not derived.

[YOURS] **I record that this chain is attractive and that its answer is the tidy one.** `|q| = 1/2`
is also the value that makes 764's locked floor an equality — and 764 already caught me reading
that floor as a selection. **Deriving `|q| = 1/2` here would be the same error with a longer
chain**, and the void condition names it: no selector may be chosen from a desired coefficient.

---

## 3. AO3 — VERDICT

```text
VERDICT = UNDERDETERMINED.
   Surviving set:  |q| ∈ {1/2, 1, 3/2, 2, …}  —  every 2q a nonzero integer.
   Eliminated:     q = 0 only (R9, and by the branch being CHARGED).
```

### 3.1 What a lawful selector must fix — the principal's brief

[YOURS] `PRIN` names the **only** lawful route, and it is narrow:

```text
"boundary closure plus the induced action selects them BEFORE any comparison
 with alpha, particle masses, family multiplicity, or the source-free endpoint"
```

A lawful selector must therefore:

1. **Come from boundary closure + the induced action** — not from any downstream comparison.
2. **Not use family multiplicity** (R2 bars it explicitly). The generation-count route — the most
   natural way to fix a chiral index — is **closed**.
3. **Not appeal to integrality as primitiveness** (R3).
4. **Not use charge quantization as a standalone selector** (R8: retired), and not re-run OBS-21's
   derivation (R7: killed).
5. **If the `Q_Sigma` route is wanted**: seal `GATE`, **and** discharge R6/R12 by *deriving* the
   identification of the causal-cell boundary with `S2_flux` — which 757 established is a
   cross-type correspondence of exactly the kind the corpus keeps separate, and which sits below
   an unfixed-complex root.

### 3.2 FALSIFIER STATUS — restated against the verdict

[PROVABLE] `PRIN`'s hard failure rule: *"The route fails to compute alpha if two inequivalent
choices of radii, spin structure, bundle class, or odd profile satisfy all adopted record rules
while producing different weighted charged determinants."*

[YOURS] **Its antecedent is structurally satisfied today.** `|q| = 1/2` and `|q| = 1` are
inequivalent, both satisfy every sealed rule enumerated in §1, and they differ in an observable of
the determinant: the **zero-mode count** `2|q|` — `1` versus `2` — i.e. the dimension of the
kernel, which enters any weighted charged determinant directly.

[YOURS] **Its consequent is not yet evaluable**, because no determinant is computable: 762
established the public layer is unbuilt and `theorem_proof_reachable_now = false`. **So the rule
has not fired; it cannot yet be evaluated.** But the route is in exactly the condition the rule was
written to catch, and the honest statement of record is:

```text
FALSIFIER_STATUS = restated.  Antecedent STRUCTURALLY SATISFIED (two inequivalent
   admissible classes, differing in zero-mode count 2|q|).  Consequent NOT YET
   EVALUABLE (no determinant is computable; the public layer is unbuilt).  The rule
   has NOT fired and is NOT discharged.  What the route needs BEFORE any determinant
   is computed is a selector meeting section 3.1 — because computing a determinant
   first, and then selecting, is precisely the retroactive selection PRIN bars:
   "the desired coupling may not select the missing data retroactively."
```

[YOURS] That last clause is the practical consequence and I state it plainly: **the selector must
land before the determinant, not after.** A route that computes determinants for several `|q|` and
then picks is the failure mode, not the fix.

---

## 4. THE OUTSTANDING HUNT — EXPOSURE STATED SYMMETRICALLY

[YOURS] I commissioned four modality-distinct hunters (charge/holonomy/quantisation; index, zero
modes and generations; the bundle class directly; the branch and the one-spectrum rule), each with
adversarial testing of any claimed forcing require, denied my own lane's artifacts. **None had
returned at seal time.**

**The exposure, stated in the direction that can hurt me:** my verdict is **UNDERDETERMINED**, and
the outstanding work could overturn it in exactly one way — by finding a **sealed require that
forces `|q|`** which I missed. That is the same class of failure as 733, and I say so rather than
imply the hunt was decorative.

**What limits the exposure, stated without inflating it:** this verdict does **not** rest on
absence alone. It rests on four **positive** sealed findings — the candidate gate is unsealed (R5),
a sealed adjudication forbids the identification it needs (R6), the derivation headline is killed
(R7), and charge quantization is retired as a selector (R8). **A missed require would have to
outrank those, not merely exist.** If one does, this artifact is wrong and `VERDICT` becomes
`FORCED`.

---

## 5. GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | The unselected list; the selector clause; the integrality bar; the falsifier | `ae1d0492…` | PROVABLE |
| 2 | `spec(Q_Sigma) = {0,1}` on the zero-or-one source sector | `33a76510…[508,859)` | PROVABLE **(unsealed source)** |
| 3 | The separation require; monopole not transported; "FOUR DIFFERENT OBJECTS" | `8eba3542…[4767,5142)` SEALED-OK | PROVABLE |
| 4 | OBS-21 KILLED; integrality authored; A9 inhabits nothing | `CLOSE`, OBS-21 row | PROVABLE |
| 5 | Charge quantization KINEMATICS_ONLY / retired as standalone selector | `STIFF` V001/V003 | PROVABLE |
| 6 | The name-census process law | `9cdc25e2…` | PROVABLE |
| 7 | `|q| >= 1/2` only; `2|q| = |deg L|` | 764 `84ceeb49…` | YOURS, exact |
| 8 | `enclosed charge` = 0 corpus-wide; no Gauss linkage stated | §2.1 census, 1,977 files, controls printed | PROVABLE |
| 9 | The falsifier's antecedent is structurally satisfied | §3.2 | YOURS |

**Pin check: 9/9.**

### 5.2 Jurisdiction check

**On returning UNDERDETERMINED when a forcing chain was available.** *What was the rule written to
protect?* R4 protects against a class chosen to make a determinant come out. The risk is live and
the chain in §2.1 is exactly its shape. *Does the outcome space distinguish false from cannot-see?*
Yes — §2 displays a full matrix and names the one candidate that would force, with the three sealed
reasons it does not. *Would the evidence appear if the theory is right?* Yes: sealing `GATE` and
deriving the identification would move the verdict to FORCED, and §3.1 says so.

**On the VOID CONDITION.** The chain in §2.1 lands on the value that would make 764's floor tight.
**That is the coefficient-shaped attraction, and I name it rather than follow it.**

**On R6 and R12.** Both are separation requires, and they are the reason this relay does not
produce a selector. I apply them against my own most useful finding.

**On R9's provenance.** `|q| >= 1/2` comes from `TWO`, which is **unsealed**. It is the only
constraint eliminating anything, and it eliminates only `q = 0` — which the charged branch already
does. So **no sealed text eliminates any candidate in the surviving set.**

### 5.3 Self verb audit — **CLEAN, with four disclosures**

1. **The killed preregistration R7 was authored by my own lane** (`…XI_N_CANDIDATE_DARIO_V003`).
   The route I would have re-walked is one this lane already walked and had killed. That is the
   sharpest argument for the writer-exclusion rule I mis-implemented until 758.
2. **The tidy answer and the desired answer coincide here**, and §2.1 records that as a warning
   rather than a corroboration.
3. **I sealed with four hunters outstanding** (§4), and stated the exposure in the direction that
   can hurt the verdict. Unlike 746 I also state what limits it — the verdict rests on positive
   sealed findings, not on absence alone.
4. **`GATE`'s unsealed status is load-bearing and easy to miss:** it is the only text in the corpus
   that would force `|q|`, and it carries no sidecar. Had I not checked, §2.1 would have read as a
   derivation.

---

```text
REQUIRES = 12 (modality-tagged) — 4 from PRIN (non-selection declaration; the selector
   clause; the integrality bar; the hard-failure rule), 1 UNSEALED candidate (GATE's
   spec(Q_Sigma) = {0,1}), 1 SEALED separation require (HOLO), 1 KILLED preregistration
   (OBS-21), 1 retirement (charge quantization = KINEMATICS_ONLY), 1 weak unsealed
   bound (|q| >= 1/2), 1 derived consequence (2|q| = |deg L|), 1 method require
   (one spectrum, one induced trace), 1 process law (the Q-69 name census).
MATRIX = displayed.  NO SEALED REQUIRE FORCES OR EXCLUDES ANY CANDIDATE IN THE
   SURVIVING SET.  Exactly one statement in the corpus would force |q| = 1/2 —
   GATE's unit-character flux with spec {0,1} — and it fails three ways: the gate is
   UNSEALED (no sidecar); the inference needs an identification of the causal-cell
   boundary with S2_flux that a SEALED adjudication forbids (HOLO 8eba3542...
   [4767,5142): monopole "not transported onto the source-flux record-write object",
   "HOLONOMY NAMES AT LEAST FOUR DIFFERENT OBJECTS"), reinforced by the Q-69 name
   census; and its conclusion's route is KILLED (OBS-21: integrality is the AUTHORED
   codomain condition, "A9 inhabits nothing").  PRIN independently bars the two next
   selectors: family multiplicity (the natural way to fix a chiral index) and
   integrality-as-primitiveness.  "enclosed charge" returns ZERO across 1,977 files —
   no Gauss linkage from source charge to the flux through S2_flux is stated anywhere.
VERDICT = UNDERDETERMINED.  Surviving set: every |q| with 2q a nonzero integer.  Only
   q = 0 is eliminated, and the charged branch already did that.  The principal's brief
   at section 3.1: a lawful selector must come from BOUNDARY CLOSURE PLUS THE INDUCED
   ACTION, before any comparison, and may use neither family multiplicity, nor
   integrality-as-primitiveness, nor charge quantization as a standalone selector, nor
   OBS-21's killed derivation — or else must seal GATE and DERIVE the forbidden
   identification.
FALSIFIER_STATUS = restated.  ANTECEDENT STRUCTURALLY SATISFIED: |q| = 1/2 and |q| = 1
   are inequivalent, both satisfy every sealed rule enumerated, and they differ in the
   zero-mode count 2|q| (1 vs 2) — the kernel dimension entering any weighted charged
   determinant.  CONSEQUENT NOT YET EVALUABLE: no determinant is computable (762: the
   public layer is unbuilt, theorem_proof_reachable_now = false).  The rule has NOT
   fired and is NOT discharged.  The route is in the condition the rule was written to
   catch, and the selector must land BEFORE any determinant — computing determinants
   for several |q| and then picking is precisely the retroactive selection PRIN bars.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+4 disclosures at section 5.3, including that the KILLED route
   R7 was authored by this lane, and that the tidy answer coincides with the
   coefficient-shaped one — named as a warning, not followed)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
