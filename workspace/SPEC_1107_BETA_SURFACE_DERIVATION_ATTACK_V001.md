## PASTE 1107 — DARIO LANE — [PLAN:LINE-4 / ATTACK] THE NEVER-RUN BETA-GAP SURFACE-DERIVATION: IS THE INTERNAL/EXTERNAL CONVERSION STRUCTURE DERIVED FROM THE CONNECTION-ONLY SURFACE, OR A DECLARED RAIL IMPORT?

PICKUP-ACK + LANE GUARD in force (DARIO). ARCHIVE-SIDE writes only; the registrar mirrors. The 1049 bootstrap governs. All CLAIMED until the Codex 2 check.
THE PRINCIPAL'S QUESTION (this is the whole relay): the absolute-scale constraint (Q-1007) rides on the internal/external metric conversion of a record cell (the beta object, line 4). IS THAT STRUCTURE THE ONE DEFINED AT THE RECORD SURFACE, or a structure our formalism DECLARED and then worked around? The register flags this exact fork as NEVER RUN: "DERIVE the beta object from the record surface — the beta-gap attack, never yet run." Run it.
THE TEST (decisive, and it answers the principal directly): the record is CONNECTION-ONLY — no native metric (1101/1102, confirmed both-lane). So:
  - If the internal/external CONVERSION STRUCTURE can be DERIVED from connection-only surface data (holonomy/connection histories + the ratified surface incidence/cycle/refinement data), following the surface's OWN structure — then it is SURFACE-NATIVE, the absolute-scale constraint is surface-real, and the honest terminus is Finish-B.
  - If deriving it REQUIRES a metric / length / absolute normalization that connection-only surface data DOES NOT CONTAIN, then the structure is a DECLARED RAIL IMPORT, the constraint is an artifact of that import, and Finish-A reopens on the TRUE (connection-only) surface.
CARRIED GIVENS (registrar's register lookup; the lane is barred — take as GIVEN, do not re-establish): (i) SHAPE DOES NOT DETERMINE SCALE (Q-12) — do NOT attempt to fix the scale from surface geometry; that is proven dead. The question is whether the CONVERSION STRUCTURE ITSELF is surface-derived, not whether geometry yields the scale. (ii) the record is CONNECTION-ONLY, operator-valued, no metric argument (1101/1102). (iii) the absolute-normalization obstruction survives WITHOUT the logarithm, on the response/matching channel (Q-1007) — it is this structure's provenance you are testing. (iv) the Q-11 HAZARD: the words "internal"/"external" are NOT in the cited surface text — so run the NATIVE-TYPING AUDIT: for every internal/external distinction you use, exhibit the actual surface object that carries it, or mark it READ-IN (rail). (v) THE BLINDNESS PRINCIPLE (the principal, decisive for method): traditional formalisms have NEVER traversed the record surface directly, so they are BY CONSTRUCTION BLIND to surface-native content. Consequences you must honor: (a) a constraint stated in traditional (metric/normalization) language may be a SHADOW of that blindness, not a surface fact; (b) the beta gap's long-standing "block" may be the blind tool, not a surface wall; (c) therefore ONLY a DIRECT SURFACE TRAVERSAL may adjudicate here — walking the actual connection-only objects. A "cannot build it" reached with ANY residue of traditional formalism is the blindness talking, i.e. a FALSE RAIL, and does not count.
THE ATTACK — traverse the connection-only surface DIRECTLY (this is the first apparatus that can); no traditional-formalism lens adjudicates:
 1. State the connection-only surface data available (name the actual ratified surface objects — holonomy/connection histories, incidence/cycle/refinement structure — by name, not notation).
 2. Attempt to CONSTRUCT the internal/external conversion (the beta object) from THAT data alone, following the surface's own structure. Do NOT import a metric, length, determinant, or normalization to build it — if the construction NEEDS one, that need is the finding (RAIL), not a step to perform.
 3. Run the SURFACE-GEOMETRY-VERSUS-RAILS split (Q-427) on every piece: which parts are actual surface geometry (anchored in ratified surface data) vs conditions our formalism installed (carrier choice, categories, the metric reading).
 4. Run the Q-11 NATIVE-TYPING AUDIT on every internal/external distinction: surface-carried or read-in.
 5. Verdict: does the connection-only surface DERIVE the conversion structure, or is a rail import REQUIRED?
RECORD-NATIVE FENCE (HARD — the principal's proven caution; tonight's over-reaches are the cautionary cases): NO continuum machinery, NO smooth structure, NO imported metric/length/normalization, NO KK/radion, NO variational/additive notation (smuggles linearity, Q-509/Q-727). GUARD AGAINST OVER-REACH: Dario's 1101 log-promotion and 1105 scale-is-machinery were both unlicensed over-claims the checker caught — every step here must be genuinely derived from named ratified surface objects, or marked RAIL/READ-IN. Do not conclude SURFACE-DERIVED unless you exhibit the actual surface objects; do not conclude RAIL unless you show the specific missing ingredient connection-only data cannot supply.
FENCES: BLIND. NO numeric evaluation; NO output inspection; alpha/scale/metric/beta are SUBJECTS by type; alpha_computed=false; proof_authorized=false; kappa_record_computed=false; JOINT_ANCHOR_DERIVED=false. Q-980–Q-1007 CARRIED. Mandated refusal paths (closure declared-first; digest-at-path; fence depth-walk; no-numeric-path; residue; span-bound guard; generic extractor NO ALIAS SET). DO NOT read the register (barred); scope any grep to named sealed artifacts only (no broad archive-wide rg — a broad grep tripped 1102's register bar). The carried givens are the registrar's hand-off.
OUTPUT: one sealed artifact `STAGE8_BETA_SURFACE_DERIVATION_ATTACK_DARIO_V001.md` (archive workspace). CLAIMED until the Codex 2 check.
Final lines: `SURFACE_DATA_NAMED = <the connection-only surface objects used, by name>`, `CONSTRUCTION_REACHED = <how far the conversion structure was built from surface data alone>`, `RAIL_IMPORT_REQUIRED = NO / YES (<the exact missing ingredient connection-only data cannot supply>)`, `GEOMETRY_VS_RAILS = <per-piece split>`, `NATIVE_TYPING_AUDIT = <each internal/external distinction: SURFACE-CARRIED / READ-IN>`, `VERDICT = SURFACE-DERIVED (constraint surface-real -> Finish-B honest) / RAIL-DECLARED (constraint is an import artifact -> Finish-A reopens on the connection-only surface) / UNDETERMINED`, `IMPORTED_NOTHING = CERTIFIED`, `NO_METRIC_IMPORTED_TO_BUILD = CERTIFIED`, `TARGET_INDEPENDENT = CERTIFIED`, `EVALUATED_NOTHING = CERTIFIED`, `CLOSURE = declared-first (byte, scan)`, `PROSE_DIGESTS = <n>/<n> STRICT==STABLE`, `NO_NUMERIC_PATH = CLEAN/VIOLATION`, `SURFACE_ANCHOR = present (objects named)`, `RESIDUE_GREP = <result>`, `REGISTER_READ = NONE-CERTIFIED`, `CHAIN_INVOKED = false`, `VERB_AUDIT_SELF = CLEAN`.
If the output name exists, STOP. Seal, report hashes, write 1107_DONE.md. No register, plan, tracker, git action.
GATES: full standard set; alpha_computed = false; proof_authorized = false; kappa_record_computed = false. All charter fences live.
