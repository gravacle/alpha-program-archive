# STAGE 8 — G1 KERNEL CERTIFICATE, ADVERSARIALLY CHECKED: SOUND WITH THREE NARROWING CORRECTIONS

## BLIND ADVERSARIAL VERIFIER — CODENAME G1-CERT-CHECK — CROSS-LINEAGE — [SEALED]

Date: 2026-08-14
Under test: STAGE8_G1_KERNEL_CERTIFICATE_V001.md, sha256
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034 —
recomputed at path this session, MATCH. DEFAULT = REFUTE; every attack run.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: citation/assembly checking + exact bookkeeping only — no
physical quantity evaluated, bounded, or computed; no scale, imported GR, or
faithfulness authority; no register/tracker/plan/road/ledger/lens file read.
Every seal verified at path before reliance (§1). No git action.

---

## 0. VERDICT IN ONE LINE

**CERT_VERDICT = SOUND. The certificate's PARTIAL verdict, the B-L2*
discharge (AQ gate item (1)), the entire OPEN map as listed, and both
consumer gatings all survive every attack. Three narrowing corrections,
none overturning: (c1) the V011 pass-item attachment ":1751-1752 first
half" must be struck from the discharged leg — the phrase "full-neighborhood
analytic locality bound" is coined at V011 :1537-1545 for the RECONSTRUCTED
SYMBOL K_hat, a different object the chain does not touch; (c2) LINK 3's
flag-block typing over-grades one component — the absolutely convergent
stratum sum (:352-383) rests on the declared estimate-grade steps, not
identity-grade (recoverable: LINK 1 carries the same spans at
estimate-grade); (c3) one sealed demand is absent from BOTH lists — the
volume-uniform zero-free neighborhood for the derived amplitude (V011
:1232-1233; flag :2184 false), which "C exists" needs before Gamma =
-log|A| is defined. The corrected licensed sentence is drafted verbatim
in §4.**

---

## 1. SEALS AND SPANS, VERIFIED AT PATH THIS SESSION

All 12 tasked seals recomputed from bytes at path (shasum -a 256): 12/12
MATCH against sidecars — 2e4e5163 REM; 89641f76 REM-CHK; a7969f0c R2;
a4f2e1b7 R2-CHK; 0a10c030 AQ; 331035d3 AQ-CHK; 5e49d209 FORM; 22a2a478
TRA; 52f2490b RL2B-REF; 05eeab53 F0; e4243c0d F0-CHK; and the V011 packet
member at `evaluator_build_A/inputs/evidence/aa7c6d49...` recomputes to
aa7c6d49 exactly. The certificate's drift finding is CONFIRMED REAL: the
working-tree `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` recomputes to
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48, and both
cited manifests (STAGE7_PACKET_MANIFEST_V001.sha256,
SOURCE_REFERENCE_MANIFEST_V001.sha256) carry aa7c6d49 for V011 — the
certificate's quarantine (evidence member relied on, working tree not) is
the correct handling. SPAN AUDIT: all 25 cited line/byte spans with 16-hex
span digests independently recomputed (python byte-offset extraction):
25/25 MATCH — V011 x10 (:1261-1281, :1740-1741, :1279-1281, :2179-2180,
:1292-1300, :2185, :1751-1752, :1397-1412, :2188-2189, :2051-2052), REM x9,
REM-CHK x2, R2 x1, R2-CHK x1, AQ x3, AQ-CHK x1, FORM x3, TRA x2,
RL2B-REF x2, F0 x1. TRA's sub-span citation aa7c6d49[47025,47247) lands
exactly on OBL-D's two clauses. Nothing unverifiable.

---

## 2. ATTACK 1 — THE DISCHARGED LINKS

```text
LINK 1 (carrier-uniform HS bound -> OBL-C = AQ item (1)):
  CORE DISCHARGE CONFIRMED. REM :104-134 delivers carrier-uniformity as
  HS-finiteness of the uncompressed X via ||Pi X Pi||_2 <= ||X||_2 over
  the D3/Hermite-Galerkin family; :266-311 + :352-383 the estimate; NET
  :864-877 "B-L2* DISCHARGED at the summed level"; REM-CHK :28-47 CONFIRMED
  at exactly the licensed strength, grade audit :309-349 (5a-5e); AQ §5
  :339-382 names item (1) verbatim as the first of exactly three blockers.
  Grade typing CONFIRMED: e-i/e-ii declared with overturn conditions
  (REM §5 :445-519); 31/31 CAS re-run IDENTICAL + independent 48/48
  (REM-CHK). The AQ-item-(1) mapping is exact.
  DAYLIGHT (c1) — the V011 attachment ":1751-1752 first half": V011 coins
  "full-neighborhood" at :1537-1545 — "The reconstructed symbol must
  satisfy, uniformly for all directions in a full neighborhood 0<|p|<p_0,
  K_hat(p)=Q_(M_record)(p)+Rem(p), ||Rem(p)|| <= C |p|^(2+delta)" — and
  the matching fail item :2063-2064 reads "locality is inferred only from
  finitely many rays rather than a uniform full-neighborhood analytic
  bound". The pass item :1751 first half therefore attaches to the
  RECONSTRUCTED-SYMBOL locality bound (object: K_hat of M_record), not to
  :1268's target-free bound (object: the linked-cluster gamma_density
  instrument). The chain bounds the assembled response X; it does not
  touch K_hat. The :1261-1281 attachment (target-free bound, with the
  certificate's own "summed-level leg" qualifier and §4.5(i) non-carriage
  of the density) SURVIVES through the sealed AQ §5 identification; the
  :1751-1752-first-half attachment does not. (The sealed F0 chain groups
  :1751-1752 into the same obligation cluster, but as an obligation-only
  citation for break [1] — never as a discharge attachment; precedent does
  not rescue the attachment.) Consequence: §5's "before the Gate-5 pass
  item :1751-1752 can pass in full" understates what remains — even with
  OBL-D done, :1751 first half awaits the K_hat bound, not this chain.
LINK 2 (double degree-0 kill -> degree -2): CONFIRMED, identity-grade.
  R2 :35-57 (SURVIVOR_FATE = CANCELS; [U(0)]_0 = 0 both ways) and R2 §5(c)
  / check (d2) ([F]_0 = 0) verified present at path; R2-CHK :36-53
  CONFIRMED (38/38, two extra checks); REM :239-264 the assembled product
  grading [X]_{-1,intra} = 0; REM-CHK negative control (nonzero [F]_0
  reproduces the refutation shape, which does NOT obtain). No daylight.
LINK 3 (parity floors -> OBL-B summability leg at the chain's level):
  FLOORS CONFIRMED, identity-grade: REM :165-202 exact enumeration,
  2o + c >= 4 assembled, NO (c=1,o=0) class; REM-CHK independent
  re-enumeration n <= 12 (vs build's n <= 8) reproduces floors exactly,
  and C5c shows the excluded class would be log-divergent — load-bearing.
  DAYLIGHT (c2), grade nit: the flag block's LINK 3 places "absolutely
  convergent stratum sum (:352-383)" under TYPE = identity-grade, but the
  per-stratum HS bounds feeding that convergence consume (e-i) (§3.3a
  rate) and (e-ii) (§3.5 steps (ii)-(iii): Cauchy-Schwarz in L^2(chi_hat)
  + mixed-regime trade) — estimate-grade by REM §5's own ledger. The
  identity-grade label is true of the ENUMERATION only. Recoverable at
  certificate level: LINK 1 carries :352-383 at
  estimate-grade-with-declared-conditions; a consumer reading LINK 3
  alone would over-read.
LINK 4 (reversal exclusion -> FAIL-E guard): CONFIRMED. REM :405-441:
  norm exclusion (absolute convergence immune to rearrangement; an HS
  operator cannot carry degree-0 intraband content) + independent limit
  exclusion (|sigma| <= C(x) min(1, omega^{-2}); degree-0 IS the
  large-omega limit: zero); REM-CHK attack 3 hunted a missed
  coincidence-strata resummation and found none. Typing honest: norm
  route consumes LINK 1; limit route independent, licensed by the
  inherited domination. FAIL-E's second horn (V011 :2051-2052) correctly
  quoted. No daylight.
```

LINKS_AUDIT = DAYLIGHT at two points: (c1) LINK 1's ":1751-1752 first
half" V011 attachment (wrong referent — the reconstructed-symbol bound
:1537-1545, untouched); (c2) LINK 3's identity-grade typing of the
stratum-sum convergence component (estimate-grade per REM §5). All four
core discharges CONFIRMED at their claimed AQ/REM-level strength.

---

## 3. ATTACK 2 — THE OPEN MAP, AND THE REVERSE HUNT

```text
OBL-D: CONFIRMED OPEN, and the wrong-quantifier analysis is exact — REM's
  family is CARRIER compression (D3/Hermite-Galerkin, :104-134); V011's
  OBL-D is the C_ref CELLULATION class (:1397-1412); nothing in REM/REM-CHK
  ranges over C_ref. TRA verified: O9/RA27-6 UNBUILT unconditional,
  "conditional theorem exists; premises absent", premises O5-O8
  (:264-268); O7/RA27-4 UNBUILT with the exact-square demand, citing
  aa7c6d49[47025,47247) verbatim (:244-253); f_g, F_g, eta_resp the three
  record-voiced undecidables (:239-240, :301-302). Flag :2189 false stands.
PER-PAIR: CONFIRMED — 52f2490b §0 :16-27 verbatim ("NO SUCH M EXISTS");
  typing :1-12 verbatim (lane result, no Part 2 row, no F'-5 ruling,
  program not declared blocked); REM NET :877 "Per-pair nothing moves";
  REM-CHK 5c re-verified. Route A closure per AQ §4.2/§5. No conflict with
  the summed discharge: different sealed quantifiers (AQ Q-1..Q-11,
  :119-142, verified).
(r-b)/(r-c): CONFIRMED as declared residues — REM :514-516, NET :874-875;
  REM-CHK 5a ("correctly graded, nothing silently absorbed") and 5c.
F'-14: CONFIRMED OPEN — AQ §4 :282-335 verbatim (valid per colour pair
  only; "the log of a sum is not a sum of logs"; witness
  SCAD_COLOR_SUM_LOG_MISUSE; Route B "does not exist yet"); DEBT 2 at
  REM :508, carried in NET.
SUMMED-S2': CONFIRMED OPEN — AQ §2.3(iii) :178-185 verbatim ("exists
  NOWHERE in the sealed text ... an E1-successor spec amendment — an act
  of the spec's author, not of any lane"); DEBT 3 at REM :509.
K-EXHAUSTION DENSITY: CONFIRMED not carried — V011 :1263-1270 is a limit
  over spatial exhaustions with connected clusters of cells; REM performs
  no multi-cell expansion and no K-limit; FORM D5 verified ("the finite-N
  all-pairs kernel is NOT cluster-summable ... the thermodynamic/
  linked-cluster limit must replace the all-pairs weight").
OBL-A: CONFIRMED untouched; the internal tension is real and verbatim
  (:1279-1281 "The disjoint theorem is proved" vs flag :2180
  monoidal_disjoint_record_composition_proved = false), cited not
  adjudicated — correct posture under GOV-F (:1763-1765 verified:
  "computed by a separate evaluator").
FLAGS :2185/:2189: both verbatim false at path; reservation to the Gate-6
  evaluator correct. (r-d): carried at REM :517. CONFIRMED.

REVERSE HUNT — MISSED (c3): V011 :1232-1233 — "The remaining connected
  theory must prove a volume-uniform zero-free neighborhood for the
  derived amplitude" — with its own status flag :2184
  volume_uniform_zero_free_neighborhood_proved = false (the flag
  immediately preceding :2185, quoted by the certificate, which skips
  :2184). This sealed connected-theory demand is absent from BOTH lists.
  It is load-bearing for the existence statement the certificate correctly
  declines: Gamma = -log|A| is undefined where the amplitude vanishes, so
  "C exists as an intensive object" needs it in exactly the way it needs
  the F'-14 passage (AQ's own per-pair display is valid "only on the
  surviving sector, where det(1+A(0)) != 0"). It is DISTINCT from F'-14
  (a validity bar on log-of-sum assembly, not a zero-freeness proof) and
  from AQ's sealed three-item gate (which the certificate correctly
  reproduces); but the certificate itself extends beyond AQ's three when
  it adds OBL-D and the K-density to the gating list — by the same sealed
  letter, :1232-1233/:2184 belongs in CLAUSES_OPEN's "ALSO NOT CARRIED"
  set. Grade: completeness miss; does not change PARTIAL; adds one named
  open demand.
BOOKKEEPING NIT (non-load-bearing): §2 OBL-A's quote head "V011 instead
  requires..." sits at :1249, outside the cited span :1261-1281 (the span
  digest itself matches; the quote attribution is wider than the span).
  OBL-A is not a discharged link; nothing consumes the attribution.
NO OTHER MISS: the full Gate-5 pass list (:1729-1758) re-read at path;
  every other pass item attaches to objects outside this certificate's
  declared scope (F_1 curvature, Clifford square, tau family, preparation
  domain, V010 control, c>0 family, Fubini-Study, flux lift, tetrad
  measure, Fourier reconstruction, Maxwell-form tests, core output). The
  Duhamel-reproduction constraint (:1300-1301) is carried in the consumer
  map through FORM D5/B3 typing — not missing.
```

OPEN_AUDIT = MISSED(volume-uniform zero-free neighborhood, V011
:1232-1233, flag :2184 false — belongs in CLAUSES_OPEN); every item the
certificate lists as OPEN is CONFIRMED genuinely open at path.

---

## 4. ATTACK 3 — THE LICENSED SENTENCE, WORD BY WORD

Verified at exactly the licensed strength EXCEPT one clause: "zero
degree-0 intraband content" (R2/R2-CHK, verbatim-supported); "remainder
begins at degree -2" (REM §3.2); "Hilbert-Schmidt CARRIER-UNIFORMLY over
the D3/Hermite-Galerkin refinement family — sup_n ||X_n||_2 <= M in the
sealed trilemma form, M symbolic and carrier-free" (REM §5(1), REM-CHK 5a
against the sealed require-form); "estimate-grade with two declared
conditions ... named overturn conditions" (REM §5); "no resummation ...
can resurrect degree-0 content" (REM §4); "discharges B-L2*, the first of
the three sealed blockers" (AQ §5); the NOT-stated tail (existence gated,
per-pair unmoved, residues declared) — all EXACT. THE ONE OVERREACH: "and
with it the summed-level analytic-locality leg (the target-free/
full-neighborhood locality bound) ... (aa7c6d49 :1261-1281, :1751-1752)"
claims the :1751-first-half leg, whose disciplined referent is the
reconstructed-symbol bound (§2 c1). The gating tail also omits the
K-density (named elsewhere in the certificate) and the :2184 zero-free
demand (named nowhere).

**THE CORRECTED LICENSED WORDING — the §5 sentence with three minimal
edits, nothing else changed:**

```text
(edit 1) "...and with it the summed-level analytic-locality leg (the
  target-free locality bound instrument of the connected expansion) of
  V011's Gate-5 connected linked-cluster obligation (aa7c6d49
  :1261-1281)."   [strike "/full-neighborhood" and ", :1751-1752"]
(edit 2) tail, after "(V011 :1397-1412; RA27-6 unbuilt)": insert ", the
  K-exhaustion multi-cell density itself (V011 :1263-1270), and the
  volume-uniform zero-free neighborhood for the derived amplitude (V011
  :1232-1233; flag :2184 false)".
(edit 3) wherever the certificate says the discharged leg advances pass
  item :1751-1752 "in full" modulo OBL-D: note instead that :1751 first
  half (the full-neighborhood analytic locality bound on the
  reconstructed symbol, V011 :1537-1545, fail item :2063-2064) is NOT
  advanced by this chain and remains with the reconstruction package.
```

SENTENCE = CORRECTED as above. Everything else in the sentence is at
exactly the licensed strength — no word weaker than the chain supports
was found either.

---

## 5. ATTACK 4 — THE CONSUMER MAP

```text
CONSUMER 1 (FORM fork arbitration): CONFIRMED. FORM :345-358 verified —
  the dichotomy needs "at post-limit strength, the Gate-5 kernel C on the
  cross pairing" together with the write-chain realization; D5 :233-267
  verified (typing symmetric/PSD/intensive/additive/cluster-summable/
  n-independent; finite-N all-pairs kernel NOT cluster-summable — the
  limit burden is the gap); D6 :268-283 verified (G1/G2/G3; G3 "the
  deciding object of section 4"). "Unlocked the first sealed analytic leg
  at the summed quantifier consistent with C's typing" is fair against
  F0 break [1]'s "no sealed replacement" (prior sealed per-stratum
  certifications never included the uniform bound — AQ item (1) said
  UNBUILT). Gating list (F'-14 + summed-S2' + OBL-D + G2/G3; deciding
  datum stays G3) CONFIRMED.
CONSUMER 2 (F0 break [1]): CONFIRMED. F0 :478-484 verified verbatim;
  F0-CHK break [1] CONFIRMED (:139-149, :293). "Narrowed, not closed — a
  bound is not a construction" is exactly right; the construction gating
  (F'-14 + summed-S2' + OBL-D + K-density) CONFIRMED; breaks [2] (Z_Q
  stiffness bridge) and [3] (C_record(K)) verified independent at F0
  :485-499 — nothing in REM touches either. Per §3(c3), the zero-free
  demand (:2184) also stands on the construction path; adding it
  strengthens, does not alter, "narrowed, not closed".
SHARED: compute pre-gate (REM NET :873) and GOV-F flag reservation
  CONFIRMED. No missed consumer found: the kappa_record battery is
  conditional on break [1] and is reached only through it; the AQ
  FIXED/RUNNING determination is this certificate's own subject, not a
  downstream consumer.
```

CONSUMERS = CONFIRMED (with the c3 addendum folding into both gating
lists).

---

## 6. PROVENANCE + INJECTION

PROVENANCE = CLEAN: 12/12 seals recomputed at path; 25/25 cited spans +
digests independently recomputed, all MATCH; the V011 drift is real,
correctly detected, and correctly quarantined (the sealed lineage cites
the aa7c6d49 member — F0's :1279-1281/:2185 citations land verbatim there,
re-verified); both packet manifests carry aa7c6d49; no cited source
unverifiable; the certificate's date and no-git posture consistent.
INJECTION = none: no instruction-shaped content found in any artifact
read; every quoted clause is technical text landing at its cited bytes;
nothing in any source attempts to steer this verifier or relax a fence.

---

## 7. FLAG BLOCK

```text
LINKS_AUDIT = DAYLIGHT(two points, core discharges all CONFIRMED:
  (c1) LINK 1's V011 attachment ":1751-1752 first half" — wrong referent:
  "full-neighborhood analytic locality bound" is coined at V011 :1537-1545
  for the reconstructed symbol K_hat (fail item :2063-2064), an object the
  chain does not touch; the B-L2* = AQ-item-(1) discharge and the
  :1261-1281 target-free-leg attachment SURVIVE; (c2) LINK 3 types the
  absolutely convergent stratum sum (:352-383) identity-grade, but its
  per-stratum bounds consume (e-i)/(e-ii) = estimate-grade per REM §5's
  own ledger — the identity-grade label is true of the enumeration only;
  recoverable via LINK 1's estimate-grade carriage of the same spans.
  LINK 2 CONFIRMED identity-grade; LINK 4 CONFIRMED as typed.)
OPEN_AUDIT = MISSED(the volume-uniform zero-free neighborhood for the
  derived amplitude, V011 :1232-1233, flag :2184
  volume_uniform_zero_free_neighborhood_proved = false — a sealed
  connected-theory demand standing on the existence path (Gamma = -log|A|
  needs it), absent from BOTH lists, distinct from F'-14; belongs in
  CLAUSES_OPEN "ALSO NOT CARRIED". Every item the certificate lists OPEN
  is CONFIRMED genuinely open at path: OBL-D (TRA O7/O9 UNBUILT verified),
  per-pair 52f2490b verbatim, (r-b)/(r-c)/(r-d), F'-14 (AQ :282-335),
  summed-S2' (AQ §2.3(iii)), K-density (V011 :1263-1270), OBL-A tension
  verbatim, flags :2185/:2189 false. Completeness nit: §2 OBL-A's quote
  head lands at :1249, outside its cited span :1261-1281 —
  non-load-bearing.)
SENTENCE = CORRECTED(three minimal edits, all else at exactly the licensed
  strength: (edit 1) "...and with it the summed-level analytic-locality
  leg (the target-free locality bound instrument of the connected
  expansion) of V011's Gate-5 connected linked-cluster obligation
  (aa7c6d49 :1261-1281)." — strike "/full-neighborhood" and
  ", :1751-1752"; (edit 2) gating tail additionally names the
  K-exhaustion density (V011 :1263-1270) and the volume-uniform zero-free
  neighborhood (V011 :1232-1233; flag :2184 false); (edit 3) pass item
  :1751 first half is NOT advanced by this chain — its referent is the
  reconstructed-symbol bound (V011 :1537-1545), which stays with the
  reconstruction package. No word weaker than the chain supports found.)
CONSUMERS = CONFIRMED(FORM fork arbitration and F0 break [1] both
  verified at path at the certificate's stated gating; breaks [2]/[3]
  independent, verified; compute pre-gate + GOV-F reservation correct;
  the c3 zero-free demand folds into both gating lists, strengthening
  "narrowed, not closed"; no missed consumer.)
CERT_VERDICT = SOUND(the PARTIAL verdict, the B-L2* discharge at the
  summed quantifier, the OPEN map as listed, and both consumer gatings
  all stand; three narrowing corrections applied — c1 strike the
  :1751-first-half attachment, c2 re-grade the LINK 3 convergence
  component, c3 add the :2184 zero-free demand to CLAUSES_OPEN — none
  overturning the verdict, the discharge, or any gating; the certificate
  remains PARTIAL with a slightly LONGER open list and a slightly
  NARROWER discharged V011 attachment.)
PROVENANCE = CLEAN(12/12 seals recomputed at path, MATCH; 25/25 cited
  spans + 16-hex digests independently recomputed, MATCH; V011
  working-tree drift to 20a3a17d confirmed and correctly quarantined —
  aa7c6d49 evidence member is the sealed lineage's citation target, both
  manifests carry it; nothing unverifiable.)
INJECTION = none.
MACHINERY_USED_BY_ME = no(shasum -a 256, file reads, greps, python
  byte-offset/span-digest extraction only; nothing symbolic executed,
  nothing numeric evaluated.)
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
G1_KERNEL_CERTIFICATE_CHECK_RESULT = SEALED.
```
