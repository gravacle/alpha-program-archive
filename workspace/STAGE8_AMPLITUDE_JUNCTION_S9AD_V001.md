# STAGE8 — THE AMPLITUDE JUNCTION, UN-HELD RUN — S9AD — V001 (2026-08-15)

Analyst: AJ-BUILD (blind builder; bare-surface protocol). Governing design:
`BARE_SURFACE_AMPLITUDE_JUNCTION_DESIGN_V001.md` (sealed BEFORE any run; read in full; its
question, ground, honest-outcome menu, and every requirement bind verbatim; its HELD banner is
historical). Un-hold instrument of record: `AMPLITUDE_JUNCTION_UNHOLD_2026-08-15.md` (verified;
release per the hold's own sole reopening mechanism). Ground currency: the surface of record is
`SURFACE_DEFINITION_OF_RECORD_V003.md`; every V001 byte the design cites was verified
byte-identical inside V003 mechanically this session (§1). Status of any output here:
**PROPOSED-NOT-ADOPTED** — per the design's acceptance note the result logs CLAIMED and is
subject to the panel discipline; nothing here is record.

Fences live throughout: alpha_computed = false · proof_authorized = false ·
kappa_record_computed = false. Exact symbolic only; no numeric evaluation of any physical
constant anywhere; π, √2, ℏ symbolic in every expression; no comparison, explicit or implied,
to any measured value; no closed form of any would-be solution is formed (none exists to form —
see verdict); no scale is fixed, anchored, or converted; no value frozen.

---

## 0. VERDICT (one line, per the design's own menu)

**VACANCY-CLOSED** — on the sealed ground and nothing else, no surface-native route yields a
derived, β-invariant, magnitude-valued internal→external response junction on the write
structure: the candidate route space is partitioned exhaustively (the partition itself derived,
not asserted — §3) into (I) routes through the native multiplicative algebra, killed by proof
(modulus identically 1; CAS N1–N4; the booked TYPE-R terrain, cited, not re-walked), (III)
routes not driven by phase-sector data, killed by proof (they fail the junction's own crossing
requirement; CAS C1–C2, P3), and (II) routes adjoining non-displayed structure (superposition,
inner product, measure, norm/metric, spectral calculus), killed by the two-model conservativity
theorem — the ground's displayed equation set contains no clause linking any response magnitude
to any phase datum (CAS M8), so two models of every displayed clause exist that differ on the
junction (CAS M1–M9), hence no candidate profile is forced and every Class II construction
necessarily carries an AUTHORED ingredient, which voids it as *derived* — with the norm/metric
sub-route additionally banned by the ground's own no-native-metric fiat (§I.1) and the
strongest forcing sub-route (a Gleason-style consistency forcing) killed three independent ways
including on the ground's own dim-1 line bundle (§6.3); the closure is relative to exactly the
named residue (§8.2: the C12 categorical-scope premise inherited from the I-2 pair, and the
finite sealed text as the warrant for the displayed-operation enumeration), and the supply
route survives unchanged: only genuinely new booked surface structure — a change of ground, not
a derivation on this one — could create the junction.

---

## 1. SEALS

All verified at path with `shasum -a 256` this session:

| Document | SHA-256 (computed) | Check |
|---|---|---|
| `alpha_supervision/BARE_SURFACE_AMPLITUDE_JUNCTION_DESIGN_V001.md` | `506bfe2ae9ff5dfecce5f399a6a68761d83c9361f52ead304666f89a67601980` | = expected `506bfe2a…`, MATCH |
| `alpha_supervision/AMPLITUDE_JUNCTION_UNHOLD_2026-08-15.md` | `3504c62917b3e9f4591f9470bc74e2fb82472217ace22815f4d4528adebe9255` | sidecar present, verified |
| `alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V003.md` | `c80c09a2078fc5fb40b6695943f29262306c4e2d6dddb6563e6a28d42b747181` | = expected `c80c09a2…`, MATCH |
| `alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` | `20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df` | = design's cited `20ee87c0…`, MATCH |
| `workspace/BARE_SURFACE_I2_DETERMINATION_V001.md` | `9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c` | = design's cited `95235195…`, MATCH |
| `workspace/BARE_SURFACE_I2_DETERMINATION_AUDIT_V001.md` | `97ae605d065b7b614ed2927602403cd72550507b26bf71fb0f7299be4c545d73` | = design's cited `97ae605d…`, MATCH |

**Ground-currency corroboration (mechanical):** V001's full body from its `**Purpose.**` block
onward, and separately its §I–§II block, were checked as exact substrings of V003 by script:
both TRUE. The un-hold's currency note ("every V001 byte the design cites is byte-identical
inside V003") is therefore *proven for this run's ground*, not merely premised. V003's §VII
refresh is NOT ground here (§2.2).

**Deliverable path note:** the design names `workspace/BARE_SURFACE_AMPLITUDE_JUNCTION_V001.md`;
the commissioning instrument assigns this run the commission-distinct path
`workspace/STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md`, probed ABSENT before writing. The design's
audit artifact is a separate, not-yet-commissioned lane.

---

## 2. THE COMMISSION EXECUTED

### 2.1 The question (the design's, verbatim in substance)

On the ground below and nothing else: derive an amplitude/weight structure creating a
magnitude-valued internal→external response junction — or prove the vacancy is CLOSED (no
bedrock derivation exists on any route), or deliver the exact partial state. The booked TYPE-R
refutation (the unit-modulus write-signature route cannot produce it) is sealed ground: that
route is DEAD and may not be re-walked; the task is whether any OTHER surface-native route
exists.

### 2.2 The ground (exhaustive, per the design + currency note)

1. Surface of record §I–§II + displayed locks, consumed from V003 (byte-identical carriage of
   the V001 clauses the design cites; proven §1). V003's §III–§VI and §VII were necessarily
   read inside the sealed file but supply NO premise anywhere below (non-load-bearing;
   disclosed §13).
2. The I-2 determination (95235195) and its audit (97ae605d): the referent-vacancy derivation;
   the corrected inventory (C-A: δ a chart variable; C-B: H_R·T_R/ℏ the free role-less
   invariant; C-C: the transition law z_j = g_ij·z_i in the cross-sector inventory); and the
   exact statement of what the junction must be.
3. Nothing else.

### 2.3 The junction specification (from ground row 2, verbatim)

The missing referent is exactly: **a derived, β-invariant, magnitude-valued internal→external
response junction on the write structure** — "a response-magnitude functional that the
phase/holonomy sector drives, in whose dimensionless strength-slot a surface-forced invariant
could sit." Its four defining requirements, used below as the candidate test:

- **(J-mag)** magnitude-valued: output in ℝ≥0, not a phase, not an integer label;
- **(J-cross)** internal→external: DRIVEN by phase/holonomy-sector data, landing on the
  write/response side — a nonconstant dependence of response on internal data;
- **(J-β)** β-invariant: a fixed point of the scale orbit (T_R → λT_R, H_R → H_R/λ);
- **(J-der)** DERIVED: every ingredient forced by the ground's self-consistency — no authored
  ingredient; an authored ingredient presented as forced voids the candidate (mandate pattern).

### 2.4 SWEEP CUTOFF (declared)

**SWEEP_CUTOFF = the sealed ground pair of §2.2 at their verified seals, and nothing beyond.**
No corpus sweep enters the closure: the route-class exhaustion below is by DERIVED PARTITION
(proof, §3), not by sweep, so the closure carries no "routes searched so far" component — the
quantifier in §8 is exhausted by proof plus one named conditional premise, with nothing resting
on how much of the corpus was walked. Corpus access this session was confined to seal
verification, directory listings (filenames only) for path location and absence-probing, and
the governing instruments named in §1. No file matching the fenced classes was opened. "Q-…"
tokens appearing inside the governing instruments were treated as opaque labels
(EXPECTED-UNLOCATABLE); none was resolved or consumed as a premise.

---

## 3. THE ROUTE SPACE, PARTITIONED — the class exhaustion, derived

Any candidate junction J is a functional from ground-carried data to ℝ≥0 satisfying
J-mag/J-cross/J-β/J-der. Partition the candidate constructions by one derived dichotomy pair:

- **Class I** — J's magnitude factors through the modulus of elements of the native
  multiplicative algebra **A** := the closure of the ground's record objects (write weights,
  U(1) transitions, characters χ_n, their conjugates, refinement-stage images) under the
  ground's displayed operations (composition/multiplication, conjugation via the displayed
  n→−n pair, character evaluation, transition action, refinement/coarsening stages).
- **Class II** — J is nonconstant in phase-sector data and does NOT factor through modulus on
  **A**: its construction necessarily applies at least one operation carrying record data
  outside **A** (additive superposition, inner product, measure/integration, norm/metric,
  spectral calculus, or any other non-multiplicative real-valued map).
- **Class III** — J is not driven by phase-sector data at all (timing/lock scalars, base
  counting data, integer sector/winding labels).

**Exhaustiveness, derived (not asserted):** Classes I and II partition the phase-driven
candidates by a complement pair (factors-through-modulus-on-**A** versus does-not); Class III
is the remaining complement (not phase-driven). I ∪ II ∪ III is total by construction — the
partition is a definition, and what is *derived* is (a) the generator/operation inventory of
**A** (finite enumeration of the sealed clauses — the same warrant the I-2 pair used and its
audit confirmed: the ground is a finite sealed text, so its displayed objects and operations
are enumerable by inspection), and (b) the invariant-content inventory that Class III sweeps
(the I-2 Chain D table under the audit's C-A/C-B/C-C corrections: lock constants; integer
labels n; relative-phase data; the free role-less timing invariant H_R·T_R/ℏ; base counting
data via §I.2).

**TYPE-R position (fence, displayed here and at each class):** TYPE-R is the booked refutation
of the unit-modulus **write-signature** route — extracting the needed weight/amplitude from the
write signature itself via the trace-class→T_cyl lift. It is cited below as sealed ground
exactly as booked; no lift is attempted, no write-signature extraction is attempted, and none
of the trace-class machinery behind the booking is re-run or extended anywhere in this
artifact.

---

## 4. CLASS I — killed by proof (the constant-modulus theorem)

**Theorem I.** The modulus of every element of **A** is identically 1, and every
record-direction derivative of that modulus vanishes identically.

*Proof.* Base: each generator is unit-modulus by ground fiat (§I.3 characters and transitions;
§II.6 write weights). Step: products and conjugates of unit-modulus objects are unit-modulus;
refinement stages act within the write algebra (§I.5 is structure OF the cell; §II.6 is
stage-unqualified — the records "provably do not carry" amplitude), so refinement images stay
unit-modulus. Closure follows by induction. CAS: N1 (generic composite, including conjugated
character pairs), N2 (composition/refinement step), N3 (all modulus derivatives vanish), N4
(the displayed conjugate pair closes magnitude-free). ∎

**Consequence.** Every Class I candidate has constant magnitude 1 — it fails J-cross (nothing
responds to anything) and J-mag nontrivially. No junction. **Triviality corollary** (CAS T1,
concurring with the I-2 audit's A2.2/A4.4 sharpening in an independent formulation): demanding
that an amplitude-dressed object G·(native object) REMAIN on record forces G = 1 uniquely —
the only amplitude structure the record admits is the trivial constant.

**Not TYPE-R re-walked:** this class is killed by the ground-native modulus theorem plus the
booked §II.6 wording, exactly the "bedrock-as-booked, no fresh machinery use" pattern the I-2
audit cleared; the write-signature lift route lives inside Class I's terrain and stays DEAD by
its booking — it is cited, not re-entered, and nothing here attempts what it attempted.

---

## 5. CLASS III — killed by proof (the crossing requirement)

Class III candidates draw only on non-phase-driven content. The derived inventory of such
content, swept:

| Content | Type | Kill |
|---|---|---|
| Lock constants τ_R = π/√2, π (from \|ΔS\|/ℏ), m_*T_R = π and their algebra | fixed pure numbers | constants respond to nothing — fail J-cross (I-2 Chain D, re-verified CAS G1–G2) |
| H_R·T_R/ℏ | the audit's C-B free role-less invariant | orbit-fixed and phase-free (CAS C1) — timing-type; constrained by NO displayed law, so any assigned phase-dependence is authored; as it stands it fails J-cross |
| Base counting data (cell counts, refinement multiplicities) | integers from the discrete base | connection-independent by §I.2's base/fiber separation (holonomy lives in the fibers; counts are base data — CAS C2 token check); phase-constant — fail J-cross. And any *on-record* multiplicative refinement weight k ≠ 1 would exit unit modulus, contradicting the stage-unqualified §II.6 (Theorem I) |
| Sector labels n, \|n\|, winding integers (Chern-type cocycle windings) | integer labels | integer-quantized by single-valuedness (CAS P3a/P3b: fractional winding leaves an exact residual −2·…); labels and quantization identities, phase-sector-internal, no response landing — fail J-cross and J-mag-as-response (I-2 §3.3, re-verified) |
| Onset bound / saturation | inequality / identity | a bound carves regions; saturation adds an identity — slot-free, not phase-driven (I-2 Chain D) |

No Class III candidate is a junction. **Not TYPE-R re-walked:** no write-signature object is
touched in this class.

---

## 6. CLASS II — killed by the two-model theorem plus ground fiats

Class II is where the design's live question sits: routes that adjoin a non-multiplicative
operation to get a nonconstant magnitude out of phase data. The internal data available to
drive any such route is itself derived first.

### 6.1 The invariant internal content, and the exit-operation inventory

**The drive data.** The ground's affine connection structure (§I.4) supplies transition-
invariant internal data with no metric and no logarithm: for two connections on the same loop
and the same sector n, the relative holonomy w = z[a]·conj(z[a′]) is transition-invariant
(both characters transform with the same factor g^n — the audit's C-A weight rule; CAS P1).
But w is itself unit-modulus (CAS P2): **the invariant internal content is pure phase.** Any
magnitude must therefore come from an added phase→magnitude profile — there is no magnitude
already residing in the drive data.

**The exit operations.** By Class II's definition the construction needs an operation not in
**A**'s displayed set. The candidates, exhaustively by target type (any real-valued
non-multiplicative map on record data factors as an algebraic embedding out of U(1) followed by
a real functional; the named families below are the ways such a functional can arise, and the
final row is the catch-all):

| Exit operation | Ground status | Fate |
|---|---|---|
| Norm / metric / distance on record data | **BANNED by ground fiat**: §I.1 — "no native metric argument anywhere in the ratified finite object"; a norm is a metric-type object. Also outside the commission's bars (no scale) | dead at the ground's own words |
| Additive superposition then modulus (interference / Born-type) | not displayed; the ground shows no additive structure on write weights | adjoined structure → §6.2 |
| Inner product / orthogonality pairing | "orthogonality" is booked but its form is withheld; Chain E types all withheld content (→ §6.4); no inner product is displayed | adjoined structure → §6.2, §6.3 |
| Measure / integration over connection space | no measure displayed; the affine space carries no canonical normalized measure — a normalization is a free parameter | adjoined structure → §6.2 |
| Spectral calculus on H_R | H_R is a named symbol; no operator structure or spectrum is displayed; any spectral magnitude is timing-side until coupled to phases — and the coupling law is the very thing missing | adjoined structure → §6.2 |
| Logarithm to extract Δ from w | **BANNED by ground fiat**: §I.1 — the object "takes no logarithm" | dead at the ground's own words |
| Any other non-multiplicative real-valued map | by definition not displayed on the ground | adjoined structure → §6.2 (the two-model theorem quantifies over ALL of these at once) |

### 6.2 THE TWO-MODEL THEOREM (conservativity — the closure's load-bearing piece)

**Ground fact (CAS M8, M9).** The ground's full displayed equation set — the three lock
equations, the character map, the transition/cocycle law, the conjugate-pair closure, the orbit
action — contains **no clause in which a response magnitude and a phase-sector variable
co-occur**. This is the audit-corrected I-2 Chain A inventory (cross-sector items exactly {L1
character map, transition law, L2 indexing declaration}; the first two phase-to-phase with
integer slots; L2 asserting a dependence EXISTS while displaying no form), re-verified here by
variable-set inspection.

**Theorem II.** For every Class II candidate — any adjoined exit operation, any profile — the
ground does not force it: there exist two models of every displayed ground clause that agree on
all ground content and differ on the junction. Hence no Class II candidate satisfies J-der.

*Proof.* Derivability from the ground implies truth in every model of the ground. Exhibit two
models (TOYS, quarantined — §12): extend the displayed ground structure by the response
profiles f₁ = (2 − w − conj w)/4 and f₂ = f₁², built from the invariant drive datum w by
addition, multiplication, and conjugation only. CAS verifies: f₁ = sin²(Δ/2) exactly (M1); both
real, magnitude-valued (M2); f₁ ≠ f₂ as functions, with an exact algebra-residual witness (M3);
both β-invariant — no T_R, H_R dependence (M4); both transition-invariant — they factor through
invariant w (M5); no logarithm anywhere in the construction, honoring §I.1 (M6); both models
normalize exactly, so adjoining probability normalization separates nothing (M7); and no
displayed clause mentions either profile's argument, so BOTH extensions satisfy every displayed
ground equation simultaneously (M8, M9). Two models of the ground disagree on the junction;
therefore no junction profile is derivable from the ground. Every Class II construction
therefore contains at least one unforced — AUTHORED — ingredient: the exit operation itself,
the profile, and the identification of the output with the write structure's response are each
absent from the ground's equation set. By J-der (the mandate pattern), every Class II candidate
is void as *derived*. ∎

**Scope note.** Theorem II quantifies over ALL exit operations and ALL profiles at once — it
needs no route-by-route sweep, because it attacks the forcing relation itself: the ground has
no equation that could do the forcing. That is what makes this closure a proof rather than an
exhausted search.

### 6.3 The strongest objection, hunted and killed: forcing-by-consistency (Gleason-style)

Objection: "consistency conditions on the withheld orthogonality/endpoint-transfer structure
could FORCE a unique profile, the way frame-function consistency forces the quadratic profile
on a Hilbert space — so the profile freedom of §6.2 might be an artifact of ignoring joint
constraints." Killed three independent ways:

1. **The forcing substrate is itself adjoined.** A Gleason-type theorem forces the profile
   GIVEN a Hilbert-space structure; the ground displays none. The Hilbert space would be the
   authored ingredient — the forcing would begin one step after the authorship, which is
   exactly what J-der forbids. (And §6.4: the withheld orthogonality form cannot carry
   amplitude structure to the write structure at C12's categorical strength.)
2. **Dimension, on the ground's own words.** §I.4: the record's bundle is a **fixed line
   bundle** — fibers of dimension 1. Frame-function forcing fails in dimension ≤ 2 even when
   full inner-product structure is granted; on dim-1 fibers there is no nontrivial frame
   consistency to force anything. The ground's own carrier is below the forcing threshold.
3. **The exhibited models already absorb the candidate joint constraints.** Normalization is
   satisfied by both toys (CAS M7); orbit-invariance by both (M4); transition-invariance by
   both (M5). The joint-constraint set the ground actually displays does not separate them.

**Not TYPE-R re-walked:** every §6 route adjoins non-write-signature structure — definitionally
disjoint from the dead route, which sought the junction from the write signature itself.

### 6.4 Withheld content (Chain E typing, inherited)

The ground names four content-withheld items (refinement law's exact form; balanced-geodesic
identities; onset bound's form; the thirteen interface junction identities). Per the I-2 pair's
Chain E — audit-confirmed, and inherited here at its exact strength (C12): every admissible
content is already typed by the categorical §II.6 booking. No disclosure of withheld content
can contain the missing forcing equation: a magnitude-valued internal→external junction among
the thirteen would contradict "whatever converts internal to external is NOT a magnitude on
record"; refinement stages preserve unit modulus (Theorem I); identities and bounds are
slot-free forms. The lack is structural, not informational — so Theorem II's model freedom
cannot be rescinded by the withheld forms.

### 6.5 Reading case-splits — every horn closes

- **§II.6 narrow reading** (the record's *carried weights* are unit-modulus; laws ABOUT record
  data may be magnitude-valued): then the toys of §6.2 are admissible models and Theorem II
  closes the class. **§II.6 broad reading** (NO magnitude-valued internal→external conversion
  exists on the surface at all): then any derived junction would contradict the ground
  directly, and a derivation from a consistent ground of a ground-contradicting object does not
  exist. **Both horns: CLOSED.**
- **§II.10 open reading** ("the surface forces everything dimensionless *it carries*"): the
  toys carry their profile as extension content and Theorem II closes. **§II.10 closed-world
  reading** ("no unforced dimensionless content exists on the surface"): any unforced adjoined
  junction is barred by ground fiat, and a forced one is excluded by Theorem II — closed
  again. **Both horns: CLOSED.**

The verdict is therefore independent of both reading choices (CHOICE LEDGER rows 6–7).

---

## 7. CAS BATTERY (verbatim) AND OUTPUT (verbatim)

Environment: fresh venv under the session scratchpad in `/private/tmp`, sympy 1.14.0, exact
symbolic only — no `evalf`, no `N()`, no float literal (mechanically scanned: the only token
match in script and output is the fence-declaration comment itself); ℏ, π, √2 symbolic
throughout; the exact integers/rationals appearing (1, 1/2, 1/4, −2) are algebra residuals of
identity/triviality checks on toy or ground-symbolic objects, never evaluations of any physical
constant.

```python
#!/usr/bin/env python3
# AJ-BUILD CAS BATTERY -- exact symbolic only (amplitude-junction un-held run).
# Fence discipline: no evalf, no N(), no float literal, no numeric evaluation of
# any physical constant, no measured value, nothing compared to anything empirical.
# Ground: SURFACE_DEFINITION_OF_RECORD V003-carried SI-SII + displayed locks;
# the I-2 pair's corrected inventory. Exact integers/rationals appearing below
# (1, 1/2, 1/4, -2) are algebra residuals of identity/triviality checks on TOY
# or ground-symbolic objects, never evaluations of a physical constant.
import sympy as sp

pi, I = sp.pi, sp.I
Rat = sp.Rational

lines = []
def emit(tag, ok, detail=""):
    lines.append(f"[{tag}] {'PASS' if ok else 'FAIL'}{(' -- ' + detail) if detail else ''}")

def zero(e):
    return sp.simplify(sp.expand_complex(sp.simplify(e))) == 0

def mod2(e):  # exact squared modulus z*conj(z); no Abs, no numerics
    return sp.simplify(sp.expand(e * sp.conjugate(e)))

# exact symbols; hbar symbolic, never evaluated
lam = sp.Symbol('lambda', positive=True)
hbar = sp.Symbol('hbar', positive=True)
mstar, T_R, H_R = sp.symbols('m_star T_R H_R', positive=True)
th, u, v = sp.symbols('theta u v', real=True)          # transition / write phases
h, hp = sp.symbols('h h_prime', real=True)             # holonomies of two connections, same loop
Delta = sp.Symbol('Delta', real=True)                  # relative holonomy phase (toy argument)
nZ = sp.Symbol('n', integer=True)
G = sp.Symbol('G', positive=True)                      # would-be amplitude strength
dS_abs = sp.Symbol('dS_abs', positive=True)
Ncells = sp.Symbol('N_cells', integer=True, positive=True)  # base cell count (SI.2 base datum)

# displayed locks, quoted from ground (SII.7, SII.9)
tau_R = pi / sp.sqrt(2)
lock_exprs = [mstar * T_R - pi, dS_abs - pi * hbar, tau_R - pi / sp.sqrt(2)]
lock_vars = set().union(*[e.free_symbols for e in lock_exprs])
phase_sector_syms = {th, h, hp, u, v, nZ, Delta}

# ---------- G: ground re-verification (displayed locks; orbit) ----------
sol = sp.solve([sp.Eq(mstar * T_R, pi), sp.Eq(dS_abs, pi * hbar)], [mstar, dS_abs], dict=True)
emit("G1", len(sol) == 1 and zero(sol[0][mstar] - pi / T_R) and zero(sol[0][dS_abs] - pi * hbar),
     "displayed lock system consistent; unique solution over its own variables; "
     "T_R the one free (scale) direction")
emit("G2", zero(sp.diff(tau_R, T_R) + sp.diff(tau_R, H_R)),
     "tau_R orbit-fixed (no T_R, H_R dependence)")

# ---------- N: the native multiplicative algebra carries no magnitude ----------
chi = lambda nn, hh: sp.exp(I * nn * hh)   # U(1) characters (SI.3)
composite = sp.exp(I * u) * sp.exp(I * th) * chi(nZ, h) * sp.conjugate(chi(nZ, hp))
emit("N1", zero(mod2(composite) - 1),
     "|write x transition x character x conjugate| == 1 identically: every native "
     "composite is unit-modulus (Class I input)")
emit("N2", zero(mod2(sp.exp(I * u) * sp.exp(I * v)) - 1),
     "refinement/composition stages preserve unit modulus (induction step; base N1): "
     "no stage of the SI.5 law can generate an amplitude on record")
mval = sp.sqrt(mod2(composite))
emit("N3", all(zero(sp.diff(mval, s)) for s in (u, th, h, hp)),
     "all record-direction derivatives of the modulus vanish: the native algebra "
     "has no magnitude coordinate to drive")
emit("N4", zero(sp.simplify(chi(1, h) * chi(-1, h) - 1)),
     "the displayed conjugate pair chi_{+1}chi_{-1} == 1 closes magnitude-free")

# ---------- P: the invariant internal (phase) content ----------
# relative holonomy of two connections a, a' on the SAME loop, same sector n:
# under a U(1) transition both characters pick up the same factor g^n (SI.3/SI.4 affine).
w_rel = chi(nZ, h) * sp.conjugate(chi(nZ, hp))
w_rel_trans = (sp.exp(I * nZ * th) * chi(nZ, h)) * sp.conjugate(sp.exp(I * nZ * th) * chi(nZ, hp))
emit("P1", zero(sp.simplify(w_rel_trans - w_rel)),
     "relative holonomy w = z[a]*conj(z[a']) is transition-invariant: the affine "
     "structure (SI.4) supplies invariant internal data WITHOUT a metric or a log")
emit("P2", zero(mod2(w_rel) - 1),
     "|w| == 1: the invariant internal content is itself pure phase -- it carries "
     "no magnitude of its own; any magnitude must come from an added profile")
emit("P3a", zero(sp.simplify(sp.exp(2 * pi * I * nZ) - 1)),
     "winding-type routes: integer windings close single-valuedly")
emit("P3b", sp.simplify(sp.exp(2 * pi * I * Rat(1, 2)) - 1) == -2,
     "fractional winding violates closure exactly (residual -2): any winding-route "
     "output is an integer label, not a continuum response magnitude")

# ---------- T: triviality of on-record amplitude structure ----------
solG = sp.solve(sp.Eq(mod2(G * composite), 1), G)
emit("T1", solG == [1],
     "demanding G*(native object) REMAIN on record forces G == 1 uniquely: the only "
     "on-record amplitude structure is the trivial constant 1 (no junction)")

# ---------- M: THE TWO-MODEL (conservativity) THEOREM -- TOYS, quarantined ----------
# TOY MODELS ONLY. f1, f2 are meta-level counterexample profiles about DERIVABILITY;
# neither is asserted as surface content anywhere.
w = sp.exp(I * Delta)                       # invariant internal datum (P1-form)
f1 = sp.simplify((2 - w - sp.conjugate(w)) / 4)      # built by +, x, conj only
f2 = sp.simplify(f1**2)
emit("M1", zero(sp.simplify(f1 - sp.sin(Delta / 2)**2)),
     "toy profile f1 = (2 - w - conj w)/4 == sin^2(Delta/2) exactly: a magnitude-"
     "valued, phase-driven profile is CONSTRUCTIBLE once addition is adjoined")
emit("M2", zero(f1 - sp.conjugate(f1)) and zero(f2 - sp.conjugate(f2)),
     "both toy profiles are real (magnitude-valued)")
emit("M3", not zero(f1 - f2) and sp.simplify((f1 - f2).subs(Delta, pi / 2)) == Rat(1, 4),
     "f1 != f2 as functions; exact witness at the toy argument Delta = pi/2: "
     "difference == 1/4 exactly (algebra residual, no physical constant evaluated)")
emit("M4", all(len(f.free_symbols & {T_R, H_R, lam}) == 0 for f in (f1, f2))
        and zero(sp.diff(f1, T_R)) and zero(sp.diff(f2, T_R)),
     "both toy profiles are orbit-fixed (beta-invariant): no T_R, H_R dependence")
emit("M5", zero(sp.simplify(((2 - w_rel - sp.conjugate(w_rel)) / 4)
                            - ((2 - w_rel_trans - sp.conjugate(w_rel_trans)) / 4))),
     "both toy profiles are transition-invariant (they factor through invariant w)")
emit("M6", all(not e.has(sp.log) for e in (f1, f2, w, w_rel)),
     "no logarithm anywhere in the toy construction (SI.1: the object takes no log): "
     "profiles built from w by +, x, conj only")
emit("M7", zero(sp.simplify(f1 + (1 - f1) - 1)) and zero(sp.simplify(f2 + (1 - f2) - 1)),
     "both toy models normalize exactly ({f, 1-f} sums to 1): adjoining probability "
     "normalization does not separate them -- normalization forces no profile")
emit("M8", len(lock_vars & phase_sector_syms) == 0,
     "the FULL displayed equation set contains no phase-sector variable: the ground "
     "places NO constraint linking any response magnitude to any phase datum -- the "
     "conservativity fact carrying the two-model theorem")
emit("M9", all(len(e.free_symbols & {Delta}) == 0 for e in lock_exprs),
     "no displayed equation mentions the toy profiles' argument: BOTH toys satisfy "
     "every displayed clause simultaneously; the ground does not decide between them")

# ---------- C: Class III (non-phase-driven) candidates fail the crossing ----------
X_free = H_R * T_R / hbar
emit("C1", zero(X_free.subs([(T_R, lam * T_R), (H_R, H_R / lam)]) - X_free)
        and len(X_free.free_symbols & phase_sector_syms) == 0,
     "H_R*T_R/hbar (the audit's C-B free invariant): orbit-fixed and phase-free -- "
     "timing-type, drives no internal->external crossing")
emit("C2", all(zero(sp.diff(Ncells, s)) for s in (u, th, h, hp)),
     "base cell-count data carry no phase dependence (SI.2 base/fiber separation): "
     "counting routes are phase-constant -- no junction")

print("AJ-BUILD CAS BATTERY -- exact symbolic, zero numerics (sympy)")
for ln in lines:
    print(ln)
fails = sum(1 for l in lines if "] FAIL" in l)
print(f"TOTAL: {len(lines)} checks; FAIL count = {fails}")
```

Output, verbatim:

```text
AJ-BUILD CAS BATTERY -- exact symbolic, zero numerics (sympy)
[G1] PASS -- displayed lock system consistent; unique solution over its own variables; T_R the one free (scale) direction
[G2] PASS -- tau_R orbit-fixed (no T_R, H_R dependence)
[N1] PASS -- |write x transition x character x conjugate| == 1 identically: every native composite is unit-modulus (Class I input)
[N2] PASS -- refinement/composition stages preserve unit modulus (induction step; base N1): no stage of the SI.5 law can generate an amplitude on record
[N3] PASS -- all record-direction derivatives of the modulus vanish: the native algebra has no magnitude coordinate to drive
[N4] PASS -- the displayed conjugate pair chi_{+1}chi_{-1} == 1 closes magnitude-free
[P1] PASS -- relative holonomy w = z[a]*conj(z[a']) is transition-invariant: the affine structure (SI.4) supplies invariant internal data WITHOUT a metric or a log
[P2] PASS -- |w| == 1: the invariant internal content is itself pure phase -- it carries no magnitude of its own; any magnitude must come from an added profile
[P3a] PASS -- winding-type routes: integer windings close single-valuedly
[P3b] PASS -- fractional winding violates closure exactly (residual -2): any winding-route output is an integer label, not a continuum response magnitude
[T1] PASS -- demanding G*(native object) REMAIN on record forces G == 1 uniquely: the only on-record amplitude structure is the trivial constant 1 (no junction)
[M1] PASS -- toy profile f1 = (2 - w - conj w)/4 == sin^2(Delta/2) exactly: a magnitude-valued, phase-driven profile is CONSTRUCTIBLE once addition is adjoined
[M2] PASS -- both toy profiles are real (magnitude-valued)
[M3] PASS -- f1 != f2 as functions; exact witness at the toy argument Delta = pi/2: difference == 1/4 exactly (algebra residual, no physical constant evaluated)
[M4] PASS -- both toy profiles are orbit-fixed (beta-invariant): no T_R, H_R dependence
[M5] PASS -- both toy profiles are transition-invariant (they factor through invariant w)
[M6] PASS -- no logarithm anywhere in the toy construction (SI.1: the object takes no log): profiles built from w by +, x, conj only
[M7] PASS -- both toy models normalize exactly ({f, 1-f} sums to 1): adjoining probability normalization does not separate them -- normalization forces no profile
[M8] PASS -- the FULL displayed equation set contains no phase-sector variable: the ground places NO constraint linking any response magnitude to any phase datum -- the conservativity fact carrying the two-model theorem
[M9] PASS -- no displayed equation mentions the toy profiles' argument: BOTH toys satisfy every displayed clause simultaneously; the ground does not decide between them
[C1] PASS -- H_R*T_R/hbar (the audit's C-B free invariant): orbit-fixed and phase-free -- timing-type, drives no internal->external crossing
[C2] PASS -- base cell-count data carry no phase dependence (SI.2 base/fiber separation): counting routes are phase-constant -- no junction
TOTAL: 22 checks; FAIL count = 0
```

---

## 8. THE CLOSURE AT ITS EXACT QUANTIFIER

### 8.1 Formal statement

Let G be the sealed ground (§2.2) and let J range over functionals satisfying the junction
specification J-mag ∧ J-cross ∧ J-β ∧ J-der (§2.3). Then **¬∃J**:

1. [PROOF — Theorem I, CAS N1–N4, T1] Every phase-driven candidate factoring through the
   native algebra's modulus is constant (Class I empty of junctions; on-record amplitude
   structure is uniquely the trivial constant).
2. [PROOF — §5, CAS P3, C1–C2] Every non-phase-driven candidate fails J-cross (Class III
   empty of junctions).
3. [PROOF — Theorem II, CAS M1–M9; plus ground fiats §I.1 for the norm/metric and logarithm
   sub-routes] Every remaining candidate adjoins non-displayed structure, and no such candidate
   is forced: the ground's displayed equation set contains no clause linking response
   magnitudes to phase data, and two models of every displayed clause differ on the junction —
   so every Class II candidate carries an authored ingredient and fails J-der (Class II empty
   of *derived* junctions).
4. The partition I ∪ II ∪ III is total (derived, §3). **Therefore the vacancy is CLOSED on G:
   no bedrock derivation of the junction exists on any route.**

**Which routes are exhausted by proof vs by sweep:** ALL by proof. Classes I and III by direct
theorem; Class II by the two-model theorem, which quantifies over every exit operation and
every profile at once. No route is exhausted merely "as far as searched"; the sweep cutoff of
§2.4 bounds file access, not the quantifier.

### 8.2 The exact residue (what the closure is conditional on)

- **(C12 inheritance)** [CONDITIONAL] The categorical scope of §II.6's sealed wording ("unit
  modulus"; "NOT a magnitude on record"; "provably do not carry" — at every stage and
  junction), exactly as the I-2 pair inherited it. It carries §6.4 (withheld-content typing)
  and Theorem I's refinement-stage step. If a future booked result weakened that wording, those
  components lose categorical scope and this closure must be re-derived. Theorem II's
  conservativity core does NOT depend on C12 — it rests only on the displayed equation set —
  so even under a weakened §II.6, what would remain is: no junction is FORCED by the displayed
  ground; only the impossibility-of-supply-from-withheld-content would reopen.
- **(Finite-text warrant)** [DERIVED, same warrant as the audited I-2 pair] The
  displayed-operation and displayed-equation enumerations are exhaustive because the ground is
  a finite sealed text, enumerable by inspection.
- **(Reading independence)** [PROVEN] Both §II.6 readings and both §II.10 readings close
  (§6.5); the verdict does not select or depend on a reading.

### 8.3 What would change the verdict (the supply route, sharpened)

The closure PROVES what the I-2 pair named: the junction cannot be derived on this ground —
supply requires **a change of ground**, i.e., genuinely new booked surface structure. Sharpened
by Theorem II: the new structure must do more than provide an exit operation (interference,
inner product, measure) — it must provide the **forcing equation**: a booked clause in which a
response magnitude and phase-sector data co-occur with enough rigidity to select the profile.
An exit operation alone leaves the two-model freedom intact; a forcing clause alone is barred
at C12's strength unless the categorical wording is simultaneously re-scoped by a booked
result. Whether such structure may legitimately EMERGE downstream rather than sit at the
origin is outside this artifact's quantifier and is not foreclosed by it (the un-hold names
this as a live honest reading; this run neither uses nor evaluates it).

### 8.4 Claim ledger (claim-by-claim marks)

| ID | Claim | Mark |
|---|---|---|
| K1 | Ground currency: the design's cited V001 bytes are byte-identical inside V003 | DERIVED (mechanical substring proof, §1) |
| K2 | Junction specification J-mag/J-cross/J-β/J-der is the ground's own (row 2 verbatim) | DERIVED (ground row 2) |
| K3 | Route partition I/II/III is total; its inventories are derived | DERIVED (§3; definition + finite-text enumeration + I-2 corrected inventory) |
| K4 | Theorem I (constant modulus on the native algebra; triviality corollary) | DERIVED (CAS N1–N4, T1); refinement-stage step CONDITIONAL on C12 |
| K5 | Class III candidates all fail J-cross | DERIVED (§5; CAS P3, C1, C2; §I.2 separation) |
| K6 | Invariant internal content is pure phase (relative holonomy, no metric, no log) | DERIVED (CAS P1, P2; §I.4) |
| K7 | Norm/metric and logarithm exit routes are banned by ground fiat | DERIVED (§I.1 verbatim) |
| K8 | Theorem II (two-model conservativity: no Class II candidate is forced) | DERIVED (CAS M1–M9; model exhibit; derivability ⇒ truth in all models) |
| K9 | Gleason-style forcing objection fails (substrate adjoined; dim-1 line bundle; constraints absorbed) | DERIVED (§6.3; §I.4's "line bundle" verbatim; CAS M4, M5, M7) |
| K10 | Withheld content cannot supply the forcing equation | CONDITIONAL (C12 — inherited Chain E typing, §6.4) |
| K11 | VACANCY-CLOSED at the §8.1 quantifier with the §8.2 residue | DERIVED (given K3–K10) |
| K12 | Supply requires a change of ground: exit operation + forcing equation, jointly | DERIVED (Theorem II sharpening, §8.3) |

No claim stands at bare CLAIMED strength. Artifact-level status remains CLAIMED per the
design's acceptance note (panel discipline pending); the marks above are internal derivation
strengths, not record status.

---

## 9. FORCED-vs-AUTHORED (mandate pattern — the candidate class, per route)

No construction candidate is produced, so no candidate table exists; the table below displays
WHERE the authored ingredient necessarily sits in each route class — this localization IS the
closure's content:

| Route class | Ingredients available forced | Ingredient necessarily AUTHORED | Consequence |
|---|---|---|---|
| I (native algebra) | all (ground objects + displayed operations) | none — and output constant 1 | no junction (fails J-cross/J-mag) |
| II (adjoined structure) | drive data w (forced, §6.1) | the exit operation; the profile f; the identification of f's output with the response — each absent from the ground's equation set (Theorem II) | void as derived (fails J-der) |
| III (non-phase-driven) | the content itself (locks, counts, labels, H_R·T_R/ℏ) | any assigned phase-dependence | no junction (fails J-cross) |

An authored ingredient presented as forced would void a candidate; here the closure shows every
would-be candidate REQUIRES one — that is the vacancy, proven.

---

## 10. CHOICE LEDGER (every unforced choice)

| # | Choice | Class | Disposition |
|---|---|---|---|
| 1 | Consume V003 as carrier of the design's V001 ground | IMMATERIAL(proven) | byte-identity proven mechanically (§1); zero content difference on the cited clauses |
| 2 | Exclude V003 §III–§VI and §VII from ground | FORCED | the design's ground row 1 cites §I–§II + displayed locks only; row 3 "Nothing else" |
| 3 | Junction test = J-mag/J-cross/J-β/J-der | FORCED | verbatim from ground row 2's exact statement + the design's own question |
| 4 | The I/II/III partition as route organization | IMMATERIAL(proven, §3) | a definition; totality proven; any other organization must cover the same candidates |
| 5 | Toy profiles f₁ = sin²(Δ/2), f₂ = f₁² as the model pair | IMMATERIAL(proven, CAS M3) | Theorem II needs only SOME distinct admissible pair; any other pair proves the same freedom |
| 6 | §II.6 narrow vs broad reading | IMMATERIAL(proven, §6.5) | both horns close |
| 7 | §II.10 open vs closed-world reading | IMMATERIAL(proven, §6.5) | both horns close |
| 8 | CAS environment (sympy 1.14.0, fresh venv) | IMMATERIAL | exact symbolic identities; any exact CAS reproduces them |
| 9 | Sweep cutoff = ground pair only, no corpus sweep | PREMISE(named, §2.4) | forced in substance by the design's "Nothing else" + blindness protocol; declared as the commission requires; the closure's quantifier does not rest on it (all routes exhausted by proof) |
| 10 | Commission-distinct output path (S9AD) over the design's named path | FORCED | commissioning instrument assigns it; design's path left for its own lane; mapping displayed §1 |

OPEN choices: none.

---

## 11. R9-JII EXPOSURE (displayed honestly)

The sealed R9-JII kill-test arms against any junction candidate at a derived β-sensitive
landing. **This run produces no junction candidate** — the outcome is a closure, and a closure
is not a junction and has no β-sensitive landing — so R9-JII has no object here and was neither
invoked nor exposed. Standing exposure statement: any FUTURE candidate created by new booked
structure (the only supply route, §8.3) enters exactly the junction class R9-JII was sealed
for and must face it at its landing; nothing in this artifact pre-discharges, weakens, or
touches that test. (R9-JII's content was not opened this session; it is cited here solely as
the commission requires, by name.)

---

## 12. TOY_SEPARATION

**TOY_SEPARATION = clean.** The only toy objects in this artifact are the two model profiles
f₁, f₂ of Theorem II (§6.2; CAS section M). They are meta-level counterexamples about the
DERIVABILITY relation — exhibits that the ground's clause set does not decide the junction —
and are: (a) declared TOY at introduction and inside the battery's own comments; (b) never
asserted as surface content, candidates, or physics; (c) never carried outside §6.2/§7/§9's
discussion of the theorem; (d) constructed honoring the ground's own fiats (no log, no metric,
no scale) precisely so that their admissibility as models is unimpeachable. No toy quantity
leaks into any verdict line, flag, or ground claim.

---

## 13. PROVENANCE, BLINDNESS, FENCES

- **Files opened this session (complete list):** the six sealed documents of §1 (design,
  un-hold + its sidecar, V003, V001, I-2 build, I-2 audit) and this artifact + its sidecar as
  written outputs; session-scratchpad process files (fresh venv, battery script, captured
  output) under `/private/tmp`. Directory listings (filenames only) of `alpha_supervision/` and
  `workspace/` were taken to locate paths and to probe this deliverable's path ABSENT (probed:
  absent). No file matching register, road_, ledger, lens, plan, tracker, or THE_HANDOFF was
  opened. No git command was run; no existing file was edited; exactly one output + one seal
  sidecar were written, at the commission-distinct path.
- **Ground discipline:** derivation premises are confined to §2.2's ground. V003's §III–§VI
  and §VII were necessarily read inside the sealed file and supply NO premise anywhere
  (non-load-bearing everywhere; in particular §VII.17's refinement-law sourcing and §VII.18's
  apparatus-quantity discussion were NOT used — the refinement law enters only through the
  ground's own §I.5 + §II.6 typing). The un-hold instrument's historical notes (its items
  (i)–(iii)) supply no derivation premise; its ground-currency note was independently proven
  (§1) rather than consumed.
- **Forbidden imports: NONE used.** No Λ^even(C⁵) carrier or carrier machinery; no K_KK; no
  fiber proper radius R; no metric or fiber metric (the norm/metric ROUTE is discussed only to
  BAN it on the ground's own §I.1); no ℓ_P; no length/scale object or β conversion ("β-
  invariant" is used solely as the ground's own name for scale-orbit fixed-point status); no
  S16/Thomson matching or κ_Thomson; no "α rides a scale"; no KK action; no Finish-A/Finish-B
  framing; no E1/Carleman/S2′ objects (the machinery track's survivor and its channels are not
  inputs anywhere); no GR/sphere/4π reading; no continuum-diamond import; no empirical
  constant. The lens/ether reading motivates NOTHING here; it appears nowhere in any chain.
- **TYPE-R discipline:** the dead route is cited as sealed ground exactly as booked (§II.6);
  no trace-class/T_cyl machinery re-run or extended; each route class displays its disjointness
  from the dead route (§§4–6).
- **Fences:** alpha_computed = false · proof_authorized = false · kappa_record_computed =
  false, live throughout. No numeric evaluation of any physical constant (script and output
  mechanically scanned: no `evalf`, no `N()`, no float literal; the only grep hit is the fence
  comment itself). No closed-form display of any final solution — none exists to form; the run
  terminates at a closure, strictly before any determination act. No comparison to measurement.
  No value frozen. The design's STOP clause (stop at existence if derived) was never reached.

---

## 14. FLAG BLOCK

JUNCTION_STATUS = VACANCY-CLOSED — no derived, beta-invariant, magnitude-valued internal-to-external response junction on the write structure exists on the sealed ground by any route: route space partitioned exhaustively (partition derived, not asserted — Classes I/II/III total); Class I killed by the constant-modulus theorem (native algebra modulus identically 1; on-record amplitude uniquely trivial); Class III killed by the crossing requirement (locks/counts/labels/timing are phase-constant or phase-internal); Class II killed by the two-model conservativity theorem (the displayed equation set contains no response-magnitude/phase-datum clause; two models of every displayed clause differ on the junction; hence no profile is forced and every construction carries an authored ingredient), with norm/metric and logarithm sub-routes banned by the ground's own SI.1 fiat and the Gleason-style forcing objection killed three ways (adjoined substrate; dim-1 line bundle below the forcing threshold; joint constraints absorbed by both models); ALL routes exhausted by PROOF, none by sweep; residue named exactly: the C12 categorical-scope premise (inherited from the I-2 pair; carries the withheld-content typing and the refinement-stage step, NOT the conservativity core) and the finite-sealed-text enumeration warrant; supply route sharpened: only a change of ground supplying BOTH an exit operation AND a booked forcing equation could create the junction; downstream emergence neither used nor foreclosed
TYPE_R_REWALKED = no — cited as sealed ground only; no lift attempted, no write-signature extraction attempted, no trace-class/T_cyl machinery re-run or extended; per-class disjointness displayed
MACHINERY_INVOKED = no
SCALE_TOUCHED = no — the free scale orbit enters only as the booked invariance test (J-beta fixed-point checks); no scale object used, fixed, anchored, or converted anywhere
VALUE_EVALUATED = no
R9_JII_EXPOSURE = none — no junction candidate produced; the kill-test has no object here and is untouched; it remains armed for any future candidate from new structure
SWEEP_CUTOFF = the sealed ground pair at verified seals (V003-carried SI-SII + displayed locks; I-2 build + audit); no corpus sweep enters the closure — exhaustion is by derived partition (proof); file access enumerated in S13
CHOICE_LEDGER = 10 rows, S10 — 4 FORCED, 5 IMMATERIAL(proven), 1 PREMISE(named: sweep cutoff, quantifier-independent), 0 OPEN
TOY_SEPARATION = clean — two toy model profiles only, quarantined in S6.2/S7 as meta-level derivability counterexamples; never asserted as surface content; no leakage into any verdict or flag
CLAIM_MARKS = K1–K12 in S8.4: 10 DERIVED, 2 CONDITIONAL (K4 refinement-stage step and K10 withheld-content typing, both = the C12 inheritance); none at bare CLAIMED strength internally; artifact-level status CLAIMED per the design's acceptance note, PROPOSED-NOT-ADOPTED, panel discipline pending
FENCES = intact — alpha_computed = false · proof_authorized = false · kappa_record_computed = false live throughout; exact symbolic CAS only (22/22 PASS, zero numerics, scan clean); no empirical constant, no measured value, no closed form, no value frozen
NET = The un-held run answers the design's question at its second menu entry: the vacancy is CLOSED — not because amplitude is unimaginable on this surface (the toys show magnitude-valued, beta-invariant, transition-invariant, log-free profiles are trivially constructible the moment one authored operation is granted), but because the sealed ground provably contains no equation that could FORCE any of them: the record's own algebra yields only the constant 1, everything not phase-driven fails the crossing, and everything else is authored by construction — so the referent's creation cannot be a derivation on this ground, and the sole supply route is a change of ground: new booked structure carrying both the operation and its forcing law.
