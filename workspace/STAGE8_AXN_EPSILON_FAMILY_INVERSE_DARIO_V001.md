# STAGE 8 — THE EPSILON-FAMILY INVERSE ATTACK: S2'S CONSTRAINT SYSTEM
## DARIO LANE (Builder B) — RELAY 888 — [PLAN:AXN-BUILD-B16]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

All charter fences live. No smooth import; no numeric evaluation of any physical quantity; no member
binding; no fixed-point execution; no end test; no comparison to measured constants; no EM
identification; no common cell formed; no junction map evaluated. PE-1..PE-13 pointer-only, none
opened or consulted. Builder-A code not opened. `~/.codex` untouched; memory-bank never searched. No
register, plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.** Reached by **chained pickup** from 887.

Subjects seal-verified BEFORE reading: four-field census `d3668b908777ea76` (mine); its check
`c9e5b5d5…` = `STAGE8_AXN_BUILD_FOUR_FIELD_CROSSCHECK_CODEX2_V001.md`; the S2 stop requirement at
`f97ee43e…` `[1907,2702)`, **span recomputed `8368dcbbc5d4af2f`**; the `EpostEndpointOrientation`
receipt and the `I_EPSILON` `NO_VERDICT` at their source.

**DISCLOSED.** S2 is the field **I** typed at 864 as *"the weakest of the four — an inverted
structure, a receiver with nothing to receive"* and flagged as *"the one field where a reader could
defensibly argue ABSENT."* The method is the S1 inverse derivation **I certified one relay ago at
887**. **Both ends are mine.** The specific hazard: at S1 the receiver carried the object's shape, so
the system had something to constrain; **at S2 the sealed receipt carries no `epsilon` at all**, and
deriving conditions on an `ε`-family from it risks authoring them and attributing them to the record.
**Every condition below traces to a sealed demand at bytes; anything traceable only to what an
i-epsilon prescription "would obviously need" is not written.**

---

## 0. LEAD

**`CONSTRAINTS = derived (5, at bytes)`. `VERDICT = CONSTRAINT-GAP`. `CONTROL = COROLLARY-CONFIRMED`.**

**The system inverts — but it inverts onto a gap of a different kind from S1's, and that difference is
this relay's finding:**

```text
S1 :  the object's SHAPE was known (amplitudes are the integrands a base family must reproduce).
      Missing: four INTERFACES AROUND the object.   The quotient R_A was DISPLAYABLE, uninhabited.

S2 :  the missing input INCLUDES THE OBJECT ITSELF (the epsilon-indexed family).
      The quotient is NOT EVEN DISPLAYABLE, because the map whose fibers it would take does not exist.
```

**So S2 is strictly weaker than S1 — and that is now DERIVED from the dependency order, not asserted.**
My 864 called S2 the weakest by observation; here it follows structurally.

**And the consistency control lands exactly as the relay's S1 analogue predicts.** The recorded
`I_EPSILON_LIMIT_IN_TOPOLOGY_SRC = NO_VERDICT | prerequisite: no epsilon-indexed family exists` is
**reproduced as a corollary of the system's dependency order**: E5 (limit topology) is conditional on
E1 (the family), and E1 is absent. **The "receiver with nothing to receive" inversion I typed at 864 is
the statement that `topology_src` EXISTS while its argument does not** — an observation there, a
consequence of `E5 ← E1` here.

---

## 1. THE RECEIVER'S OWN SEALED CONTENT

**The receipt is genuine and it is structured** — four components, not a bare flag:

```text
ContourOrient_law := ( branch order +/- ,
                       U_-^dagger U_+ ,
                       E_post endpoint-charge arrow ,
                       U1 reality/branch involution )

LAW_SIDE_CONTOUR_ORIENTATION_RECEIPT_BUILT = true | TYPE-P | premises: DoR-008, DoR-009
```

with `E_post` ratified as *"time-oriented endpoint-charge assignment — charge follows the write's
direction"*, attaching the character **once** to the written output, with incoming ready and outgoing
pointer carrying the **same faithful** character.

**The demand it must serve**, at the S2 stop requirement (`8368dcbbc5d4af2f`, recomputed):

```text
P3_INTERACTING_CONTOUR_BUILT = false | TYPE-U |
  would-build: interacting contour/analytic/i-epsilon prescription
```

and the receiver's own itemisation of what such a prescription needs:

```text
K_epsilon or U_epsilon ;  epsilon-domain and sign/insertion rule ;
analytic half-domain and boundary-value map ;  common invariant operator domain ;
limit epsilon -> 0 in a named topology.
```

**Four sealed components constraining five demanded parts. That is enough to invert.**

---

## 2. THE CONSTRAINT SYSTEM — five necessary conditions

**Law 9b, closure declared BEFORE any absence line:**
`C_EPS :=` {the S2 stop requirement span `8368dcbbc5d4af2f`; the P3 second attack's §4 (receipt and
its §4.2 refusal); DoR-009's `E_post` ratification and its underlying proposal; my 864 four-field
census and its Codex check; the `I_EPSILON_LIMIT_IN_TOPOLOGY_SRC` source}. **"Absent" below means
absent from `C_EPS`.**

### E1 — the ε-indexed family must exist as an operator-valued map

`epsilon |-> K_epsilon` (or `U_epsilon`). **Sealed demand**, first item of the receiver's list.
**Status: ABSENT** — *"No such epsilon-indexed object appears in DoR-009, `E_post`, the finite law,
PathCert, or P2."* **E1 is not a condition ON a family; it is the family's existence, and it is the
one the others depend on.**

### E2 — the sign/insertion rule must reproduce the sealed orientation

**Derived from two of the receipt's four components** — the **branch order ±** and the **`E_post`
endpoint-charge arrow**. Since `E_post` fixes that *charge follows the write's direction*, and the CTP
order `U_-^† U_+` is ratified, **an insertion rule's sign is not free**: it must reproduce the ratified
branch order and the charge arrow's direction on the written output.

**This is a genuine necessary condition and it is the strongest one the receipt yields** — the receipt
does not supply an `ε`, but it **does** constrain the sign any future `ε`-insertion may carry.

### E3 — the boundary-value map must intertwine the reality/branch involution

**Derived from the receipt's fourth component.** The `U1` reality/branch involution relates the two
CTP branches; an analytic half-domain with a boundary-value map must therefore be **compatible with
that involution** — the map must intertwine it rather than be chosen independently of it.

**Scope held:** this constrains the boundary-value map's **equivariance**, not its existence, its
half-domain, or its analyticity class. **Those are not derivable from the receipt and are not
written.**

### E4 — a common invariant operator domain, and it is NOT S4's by default

The receiver demands a common invariant operator domain for the `ε`-family. **S4's domain conjunct is
the same object family with a distinct receiver** — Q-784/793's
`SAME-FAMILY-DISTINCT-RECEIVERS`, which **I conceded at 884 after over-merging it at 875.**

**I do not re-merge it here.** E4's condition is: *the `ε`-family must preserve a common dense
invariant domain.* **Whether that domain is S4's, and whether either implies the other, is a separate
question with no sealed map** — and stating otherwise would repeat exactly the error I withdrew four
relays ago.

### E5 — the limit topology must be an operator topology on the family's codomain

`epsilon -> 0` must converge **in a named topology**, and the object converging is an **operator**
(the `K_ε`). **Therefore the topology must be one on the family's operator codomain.**

**And here the sealed material is present but of the wrong type:** `topology_src` **exists** and is *a
topology on **source increments***. **A topology on source increments is not a topology on an
operator-valued family.** So E5 is unmet not for want of any topology, but because **the available one
receives a different kind of object.**

---

## 3. THE CONDITIONAL RESIDUAL — not displayable, and that is the result

At S1 the residual quotient `R_A ≅ image(D_A) ∩ Proj ∩ Cov ∩ Norm ∩ Prov` was **displayable**, with an
affine fiber `λ_0 + ker(D_A)`, because the map `D_A(λ)_N := A_N λ_N` was **defined** — its inputs were
typed placeholders, but the map itself existed.

**At S2 there is no analogous map.** The quotient would be taken over families satisfying E2–E5,
modulo those inducing the same prescription — **but "inducing the same prescription" presupposes E1,
the family-to-prescription assignment, and E1 is precisely what is absent.**

```text
S1:  the quotient is DISPLAYABLE and UNINHABITED.
S2:  the quotient is NOT DISPLAYABLE — the map whose fibers it would take does not exist.
```

**That asymmetry is the exact sense in which S2 is weaker than S1**, and it is derived here rather
than observed. **No quotient is written**, because writing one would require supplying E1.

---

## 4. THE CONSISTENCY CONTROL — **COROLLARY-CONFIRMED**

The relay asks whether the system reproduces the recorded `NO_VERDICT`, as S1's system reproduced the
Haar refutation. **It does, and by the same kind of structural mechanism.**

The dependency order is forced by the conditions' own content:

```text
E5  (limit in a named topology)   REQUIRES an argument to take the limit OF
                                   -> E5 is conditional on E1
E1  ABSENT
=>  the limit statement has NO ARGUMENT, so it is neither true nor false
=>  I_EPSILON_LIMIT_IN_TOPOLOGY_SRC = NO_VERDICT | prerequisite: no epsilon-indexed family exists
```

**Exactly the sealed value, recovered as a corollary of the dependency order rather than imported.**

**And it sharpens my own 864 characterisation.** There I wrote that S2 has *"an inverted structure — a
receiver with nothing to receive."* **The constraint system says precisely what that means:**
`topology_src` **exists** (the receiver), E1 **does not** (the thing to receive), and E5's
conditionality on E1 is why the inversion produces a `NO_VERDICT` rather than a failure. **An
observation at 864; a consequence of `E5 ← E1` here.**

**Comparison to the S1 control, stated because the analogy is imperfect and the difference matters.**
S1's Haar corollary **refuted a named candidate** — a substantive negative about an object that
exists. **S2's control reproduces a `NO_VERDICT`** — it explains why no verdict is reachable. **The
first eliminates; the second explains a non-elimination.** Both are consistency checks; **only the
first would have caught a wrong candidate**, and S2 has no candidate to catch.

---

## 5. VERDICT — `CONSTRAINT-GAP`

| terminal value | available? |
|---|---|
| `FAMILY-FORCED` | **NO** — nothing forces an `ε`-family; the receipt constrains a sign (E2) and an equivariance (E3), which is far short of forcing an object |
| `FAMILY-REFUTED` | **NO** — no inconsistency is exhibited. **E2–E5 are mutually satisfiable in principle**; nothing in `C_EPS` shows they cannot be met together |
| `RESIDUAL-FREEDOM` | **NO** — and here **more strongly unavailable than at S1**: the quotient is not merely uninhabited but **not displayable** (§3) |
| **`CONSTRAINT-GAP`** | **YES** — missing inputs named below |

```text
CONSTRAINT-GAP, missing inputs:
  E1  an operator-valued epsilon-indexed family  epsilon |-> K_epsilon   [THE OBJECT ITSELF]
  E3+ the analytic half-domain and the boundary-value map's existence and analyticity class
      (the receipt constrains the map's EQUIVARIANCE only)
  E4  a common dense invariant domain for that family, NOT inherited from S4
  E5  an operator topology on the family's codomain — topology_src is on SOURCE INCREMENTS
      and receives a different kind of object
```

**`S2 = MATERIAL-PARTIAL, and its gap is now typed rather than described.`** Unchanged in class from
my 864. **What changes:** the gap was *"an ε-indexed analytic operator family, absent"*; it is now
**five conditions, of which two (E2, E3) are DERIVED CONSTRAINTS the record does supply on any future
family, and three (E1, E4, E5) are missing inputs — with E1 being the object itself.**

**That the record supplies two real constraints is worth stating**: S2 is not empty. The sealed
orientation **does** bind a future family's insertion sign and its involution-equivariance. **My 864's
"a receiver with nothing to receive" was right about E1 and slightly unfair to E2/E3** — the receipt
constrains more than it receives.

---

## 6. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the EpostEndpointOrientation receipt as GENUINE COMPATIBILITY MATERIAL and nothing
  more; "no epsilon-indexed object appears in DoR-009, E_post, the finite law, PathCert, or P2";
  I_EPSILON_LIMIT_IN_TOPOLOGY_SRC = NO_VERDICT with PREREQUISITE ABSENT; topology_src as a topology on
  SOURCE INCREMENTS; S4's domain conjunct under SAME-FAMILY-DISTINCT-RECEIVERS, NOT re-merged; S2's
  class MATERIAL-PARTIAL.

DERIVED HERE: (a) E2, the insertion sign must reproduce the ratified branch order and the E_post charge
  arrow; (b) E3, the boundary-value map must intertwine the U1 reality/branch involution; (c) E5's type
  mismatch — the demanded topology is on an OPERATOR codomain while topology_src is on SOURCE
  INCREMENTS; (d) the dependency order E5 <- E1 and the recovery of the sealed NO_VERDICT as its
  corollary; (e) the S1/S2 asymmetry — S1's quotient DISPLAYABLE-UNINHABITED versus S2's NOT
  DISPLAYABLE — which derives S2's weakness rather than asserting it.

SELECTED HERE: NOTHING.  No epsilon-family, epsilon-domain, insertion rule, half-domain,
  boundary-value map, invariant domain, or topology is constructed, chosen, or installed.  NO QUOTIENT
  IS WRITTEN, because writing one would require supplying E1.  NO FLAG MOVES.

NOT DONE AND DISCLOSED: E2 and E3 are DERIVED CONSTRAINTS, not verified against an instance — there is
  no instance.  I did NOT derive the half-domain's analyticity class, the epsilon-domain's shape, or
  the insertion rule's explicit form: NONE traces to a sealed demand, and each would be MY authorship
  dressed as a necessary condition.  THAT IS THE HAZARD I NAMED AT PICKUP AND IT IS WHY THE SYSTEM HAS
  FIVE CONDITIONS RATHER THAN THE FULLER LIST AN i-EPSILON PRESCRIPTION "OBVIOUSLY" NEEDS.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at §2, and this is the relay where they cost the most.** The
system could have been made to look complete by adding the conditions any i-epsilon prescription
plainly requires — an analyticity class, a half-plane, a pole prescription. **None of those traces to
a sealed demand in `C_EPS`, and none is written.** A five-condition system that stops where the record
stops is the honest product; a ten-condition system would have been authorship with citations.
**S12** — `NO_VERDICT` carried as a refusal and **recovered** as one, never converted into a gap.
**S26 / S08 / S19 / S24** untouched — no topology, domain, or analyticity class imported.
**T1 / T5** untouched.
**BR-1 HELD.** My own 864 typing and its Codex check are producer-declared objects: they got no
confirming weight. **The result partly corrects 864** — "nothing to receive" understates E2/E3.

---

## 7. FINAL LINES

```text
CONSTRAINTS = derived (5, at bytes).  E1 the epsilon-indexed operator-valued family itself — SEALED
  DEMAND, ABSENT from DoR-009, E_post, the finite law, PathCert and P2.  E2 THE INSERTION SIGN MUST
  REPRODUCE THE SEALED ORIENTATION — derived from two of the receipt's four components, the branch
  order +/- and the E_post endpoint-charge arrow, since "charge follows the write's direction" and the
  CTP order U_-^dagger U_+ is ratified; AN INSERTION RULE'S SIGN IS NOT FREE.  E3 THE BOUNDARY-VALUE
  MAP MUST INTERTWINE the U1 reality/branch involution — derived from the receipt's fourth component;
  scope held to EQUIVARIANCE, not existence, half-domain, or analyticity class.  E4 a common dense
  invariant operator domain, AND NOT S4'S BY DEFAULT — Q-784/793's SAME-FAMILY-DISTINCT-RECEIVERS,
  which I CONCEDED AT 884 AFTER OVER-MERGING IT AT 875 AND DO NOT RE-MERGE HERE.  E5 THE LIMIT TOPOLOGY
  MUST BE AN OPERATOR TOPOLOGY ON THE FAMILY'S CODOMAIN — and the sealed material is PRESENT BUT OF THE
  WRONG TYPE: topology_src exists and is a topology on SOURCE INCREMENTS, which RECEIVES A DIFFERENT
  KIND OF OBJECT.  LAW 9b: C_EPS declared BEFORE any absence line.
VERDICT = CONSTRAINT-GAP.  FAMILY-FORCED unavailable — the receipt constrains a SIGN and an
  EQUIVARIANCE, far short of forcing an object.  FAMILY-REFUTED unavailable — no inconsistency is
  exhibited and E2-E5 are mutually satisfiable in principle.  RESIDUAL-FREEDOM unavailable, AND MORE
  STRONGLY THAN AT S1: at S1 the quotient R_A was DISPLAYABLE and uninhabited because the map D_A(lambda)
  = A_N lambda_N EXISTED; AT S2 THERE IS NO ANALOGOUS MAP, since "inducing the same prescription"
  PRESUPPOSES E1 — SO THE QUOTIENT IS NOT EVEN DISPLAYABLE, AND NONE IS WRITTEN.  MISSING INPUTS
  NAMED: E1 the object itself; the half-domain's existence and analyticity class; E4's domain, not
  inherited from S4; E5's operator topology.
CONTROL = COROLLARY-CONFIRMED.  The system reproduces the sealed I_EPSILON_LIMIT_IN_TOPOLOGY_SRC =
  NO_VERDICT | prerequisite: no epsilon-indexed family exists — AS A COROLLARY OF ITS OWN DEPENDENCY
  ORDER: E5 requires an argument to take the limit OF, so E5 is conditional on E1; E1 is absent; the
  limit statement therefore has NO ARGUMENT and is neither true nor false.  RECOVERED, NOT IMPORTED.
  AND IT SHARPENS MY OWN 864: I wrote there that S2 has "an INVERTED structure — a receiver with
  nothing to receive"; THE SYSTEM SAYS PRECISELY WHAT THAT MEANS — topology_src EXISTS (the receiver),
  E1 DOES NOT (the thing to receive), and E5's conditionality on E1 is why the inversion yields a
  NO_VERDICT rather than a failure.  AN OBSERVATION AT 864, A CONSEQUENCE OF E5 <- E1 HERE.
  THE ANALOGY TO S1 IS IMPERFECT AND THE DIFFERENCE MATTERS: S1's Haar corollary REFUTED A NAMED
  CANDIDATE — a substantive negative about an object that exists; S2's control EXPLAINS WHY NO VERDICT
  IS REACHABLE.  THE FIRST ELIMINATES, THE SECOND EXPLAINS A NON-ELIMINATION, and only the first would
  have caught a wrong candidate — S2 HAS NO CANDIDATE TO CATCH.
S2 = MATERIAL-PARTIAL (class unchanged from my 864), GAP NOW TYPED RATHER THAN DESCRIBED.  It was "an
  epsilon-indexed analytic operator family, absent"; it is now FIVE CONDITIONS, of which TWO (E2, E3)
  ARE DERIVED CONSTRAINTS THE RECORD DOES SUPPLY ON ANY FUTURE FAMILY and THREE (E1, E4, E5) ARE
  MISSING INPUTS, WITH E1 BEING THE OBJECT ITSELF.  AND THIS PARTLY CORRECTS MY OWN 864: "a receiver
  with nothing to receive" was RIGHT ABOUT E1 AND SLIGHTLY UNFAIR TO E2/E3 — THE RECEIPT CONSTRAINS
  MORE THAN IT RECEIVES.  S2 IS NOT DISCHARGED AND NO FLAG MOVES.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) BOTH ENDS OF THIS RELAY ARE MINE — the target typing (864's "weakest field") and the method's
      certification (887's confirmation of the S1 inverse).  The tidy outcome was a matching system and
      a pleasing symmetry.  WHAT I GOT INSTEAD IS AN ASYMMETRY: S2's quotient is NOT DISPLAYABLE where
      S1's was, and the control EXPLAINS a non-elimination where S1's ELIMINATED a candidate.  I
      report the asymmetry rather than the symmetry BECAUSE THE ASYMMETRY IS WHAT THE BYTES GIVE.
  (2) THE AUTHORSHIP HAZARD WAS THE WHOLE DIFFICULTY AND IT COST CONDITIONS.  Deriving constraints on
      an epsilon-family from a receipt that never mentions epsilon invites writing what such a
      prescription "obviously" needs.  I DID NOT DERIVE the half-domain's analyticity class, the
      epsilon-domain's shape, or the insertion rule's explicit form — NONE traces to a sealed demand,
      and EACH WOULD HAVE MADE THE SYSTEM LOOK MORE COMPLETE.  FIVE CONDITIONS IS WHERE THE RECORD
      STOPS.
  (3) I CORRECT MY OWN 864 IN THE MILD DIRECTION AND SHOULD SAY WHY THAT IS STILL A CORRECTION.  "A
      receiver with nothing to receive" understates the receipt: it constrains an insertion SIGN and an
      involution EQUIVARIANCE for any future family.  That does not rescue S2 — E1 is still the object
      itself — BUT A READER OF 864 ALONE WOULD HAVE THOUGHT S2 SUPPLIED NOTHING, AND IT SUPPLIES TWO
      CONDITIONS.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** All charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-13
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
