# Stage 8 Task 4a Transport Infrastructure: Common Domain and Physical Squares Build Attempt V001

**Relay:** PASTE 397 — CODEX LANE 2 (HIGH EFFORT) — [TASK 4a]

**Status:** SEALED BUILD ATTEMPT AND CARRIER-TYPING DETERMINATION. The
bounded source/raw-`G` transport substructure constructs. The requested common
physical `A/G` operator domain and completed RetHess restriction/Schur squares do
not construct from the stated inputs.

**Premise standing:** positive claims using the ratified stack are
`TYPE-P | premises: DoR-008, DoR-009, DoR-013, DoR-014 as amended, DoR-015`.

**Gates:** `alpha_computed=false`; `proof_authorized=false`;
`kappa_record_computed=false`.

---

## 0. Lead determination

The proposed common-domain build fails at carrier typing before any closure
question arises.

The tempting construction is

```text
H_1^005 direct-sum S_1,sym(H_CTP^005).
```

It is indeed a complete space with a dense finite core. But its first summand is
the conserved-current/source-test carrier and its second summand is the
symmetric bilocal-**source** carrier `R`. The stationary Schur system acts on
physical mean-field and connected-bilocal tangents

```text
(delta Abar, delta G),
```

not on `(J,R)`. Q-309 and Q-314 explicitly forbid identifying the finite/source
two-sector block with the stationary 2PI block system because their variables,
derivatives, carriers, and stationarity differ. The raw bilinear class also does
not repair this: a raw connected correlator is an output bilinear form, not by
that fact the tangent domain on which `Gamma_GG` acts.

Accordingly, the requested full object remains unbuilt:

```text
COMMON_COMPLETED_PHYSICAL_AG_DOMAIN_EXISTS = false | TYPE-U |
  would-build: physical Abar- and G-tangent carriers, their finite restriction
               maps, a common invariant core, and closure/boundary certificates

PHYSICAL_RETHESS_RESTRICTION_MAP_rho_H_N_EXISTS = false | TYPE-U |
  would-build: RetHess_phys class/representation and restrictions intertwined
               with the physical A/G carrier

STATIONARY_SCHUR_RESTRICTION_SQUARE_EXISTS = false | TYPE-U |
  would-build: Gamma_AA/AG/GG/GA, the GG inverse, common domain, and C5(d)
```

This is not a total non-result. The maximal construction supported by the
inputs is exact:

1. the completed source/test domain and its restriction/retraction squares;
2. the bounded raw-bilinear restriction maps;
3. zero common restriction tail on that bounded class;
4. Map 1 and `G_phys^007` restriction squares, with Q-243/Q-279 reproduced;
5. a precise carrier/domain/restriction interface for builds (ii) and (iii).

```text
BOUNDED_SOURCE_RAWG_TRANSPORT_SUBPACKAGE_EXISTS = true | TYPE-P
BOUNDED_RAWG_CLASS_TAIL_ZERO = true | TYPE-P
```

The reviewer premise “buildable now” is therefore only half right. The maps are
buildable on already typed bounded source/raw-`G` classes; the common physical
stationary domain is not, because its two tangent carriers have never been
instantiated. Declaring their direct sum would be an unproved-identity transport.

---

## 1. Preflight, currency, roots, and authorities

### 1.1 Preflight

```text
DOES THE OBJECT EXIST?
  NO at start; Q-314's synonym count is zero.

IS THE VERSION CURRENT?
  YES at construction start through Q-314.

ARE ITS INPUTS PRESENT?
  PARTLY. All bounded source/raw-G inputs are present. The physical Abar/G
  tangent carriers and stationary operators are not.
```

The input claim was tested rather than inherited. That test produces the lead
carrier mismatch.

### 1.2 Roots entered

The public roots entered were:

1. `/Users/bgm/MB Work/alpha_supervision/`
2. `/Users/bgm/MB Work/alpha-program-archive/workspace/`
3. `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`

The excluded root `a32_holdout/custodian_private/` was never entered.

### 1.3 Frozen authorities

| Authority | SHA-256 | Use |
|---|---|---|
| `LOCKED_PROCESS.md` | read in full | fences and custody |
| `QUESTIONS_SETTLED_REGISTER_V001.md` | `ffa728c0d0e78b6da2960931ac18bb67d0ef5a2bcd6042cef9c1a538606ffa46` at construction start | Q-313/Q-314 standing |
| Q-314 stationarity/synonym audit | `671a94a7f55a5649cc8280bb0cb460a3a08b758b629279ab2ec06d982d0965a8` | T1/T2/S1/S2 split and carrier distinctions |
| Q-257 P7 certificate | `07205bf5e1888bd39a97d4e86543852d5e9b88b103e5c0b429c76bd77290d6be` | conditional class-transport and T5 theorems |
| P2 V002 | `40b2af34443e051fffdc7bf2ec7025c811a98c501a82d19164596ee6f37f00c1` | Banach source completion and calculus |
| P3 law-side verification | `aaff995613e60fdf792473dcb8d3ffefcc2390428f4e6aa21ea9fef12ec97e27` | bounded-domain/GNS scope ceiling |
| Q-288 accounting V002 | `31a738ec17696ea01e1cb6a6ee7a37a29e6c0ca24d6fb8cc050d06aed32a583f` | domain, square, tail, and door schemas |
| FIELD_SIGNATURE_PHYS V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical quotient, conserved currents, bilocal-source/raw classes |
| Map-1/Q-313 build | `ea8f4e9a79a9e4aca4a0b9e9aab470d78cd4d9c78794c20d9e979c0db16fbeab` | scalar cycle factorization and naturality |
| Q-309 kernel/two-sector build | `a4c916a7cfa82c2130c82d8947c869f118e224959d7824bba45695711b4919c3` | completed source kernel and 2PI distinction |
| V007/Door-D execution | `00414b0d5617e44b12e9f7faef4484803c858992be6ed1951df38c22719b2696` | bounded physical raw bilocal and exact restrictions |
| raw-correlator/RetHess specification | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | physical domain/codomain and Schur requirements |

### 1.4 Acts not performed

No gate flag was changed. No register, plan, tracker, or prior artifact was
edited. No git command, commit, or push was performed. No barred quantity was
computed or bounded.

---

## 2. Carrier census: why no common physical domain follows

### 2.1 The completed carriers that actually exist

The corpus supplies the following distinct completed objects:

| Carrier | Meaning | Topology/domain | Standing |
|---|---|---|---|
| `E_src=ell^1_+ direct-sum ell^1_- direct-sum S_1,sym(H_CTP)` | source variables `(J_+,J_-,R)` | P2 Banach sum norm | built `TYPE-P` |
| `K=ker L subset E_src` | completed source-kernel sector | inherited P2 norm | built `TYPE-P` |
| `H_1^005` / `J_phys^005` | conserved physical source/test-current completion | Hilbert/source norm | built `TYPE-P` under DoR-015 |
| `S_1,sym(H_CTP^005)` | symmetric bilocal-source carrier | trace norm | built `TYPE-P` under DoR-015 |
| `Bil_cont(J_phys^005 x J_phys^005;C)` | bounded bilinear output class | bounded-bilinear norm | built class `TYPE-P` |
| `RawGClass^005` | D3-real symmetric sub-class of the preceding class | bounded-bilinear norm | built class; physical image built by Door D |
| `X_phys^005` | quotient field/configuration carrier | projective quotient topology | built `TYPE-P` under DoR-015 |
| `H_out` | outgoing record-only GNS carrier | Hilbert norm | built `TYPE-P`, record-only |

None is a common stationary `(Abar,G)` tangent domain.

### 2.2 Four prohibited identifications

#### A. Source current is not mean field

`J_phys^005` is the current/source-test carrier paired with connection
variations. `Abar` is the mean physical field. Separation by currents supplies
a dual pairing; it does not identify the current space with the field tangent.
No Riesz identification is sealed, and the physical field carrier has a
projective quotient topology rather than the current Hilbert norm.

```text
SEALED_J_PHYS_TO_TANGENT_ABAR_IDENTIFICATION_EXISTS = false | TYPE-U |
  would-build: a quotient-compatible source-field duality/intertwiner with
               topology and finite-restriction certificates

J_PHYS_MAY_BE_IDENTIFIED_WITH_TANGENT_ABAR_BY_NOTATION = false | TYPE-R |
  test: current/source and mean-field roles are distinct in the live signatures
```

#### B. Bilocal source is not connected bilocal

`R` is the symmetric bilocal source. `G` is its conjugate connected-bilocal
response variable. The 2PI identities distinguish them explicitly. Trace-class
completion of `R` does not instantiate the tangent carrier of `G`.

```text
R_SOURCE_CARRIER_EQUALS_TANGENT_G = false | TYPE-R |
  test: source variable and Legendre-conjugate connected response have distinct roles
```

#### C. Raw correlator is not automatically its own tangent domain

`G_phys^007` is a bounded bilinear output. Its membership in `RawGClass^005`
does not prove that every stationary `G_*`, every variation `delta G`, or
`Gamma_GG` uses that same Banach class. The raw-map specification requires this
class/domain information separately.

```text
RAWG_OUTPUT_CLASS_FIXES_STATIONARY_G_TANGENT_CLASS = false | TYPE-U |
  would-build: physical Legendre map and stationary G-carrier/domain certificate
```

#### D. Record GNS is not the physical response domain

P3 verifies that `H_out` is record-only and that every existing record
operator is bounded. It contains no physical endpoint, inverse, 2PI, or
RetHess operator.

```text
H_OUT_EQUALS_PHYSICAL_RESPONSE_DOMAIN = false | TYPE-R |
  test: distinct carrier, representation, scope, and operator content
```

### 2.3 The failed direct-sum candidate

The family-first candidate was

```text
D_bad := H_1^005 direct-sum_1 S_1,sym(H_CTP^005).
```

This carrier is mathematically complete and finite-core dense, but it is typed
as `(source current, bilocal source)`, not `(mean field, connected bilocal)`.
It therefore fails the task's common-domain contract before any operator acts.

```text
D_BAD_IS_COMPLETE = true
D_BAD_IS_COMMON_PHYSICAL_AG_DOMAIN = false | TYPE-R |
  test: both summands have the wrong physical role for the stationary block system
```

This counterexample kills the only choice-free direct-sum construction present
in the ratified data.

### 2.4 Exact missing carrier package

The common-domain floor is:

```text
PHYSICAL_AG_TANGENT_AND_RESTRICTION_PACKAGE := (
  T_Abar X_phys with a declared complete locally convex/Banach/Hilbert class,
  T_G ConnBil_phys with a declared complete class,
  source-field and bilocal-source/response duality maps,
  finite physical tangent spaces and restrictions rho_A,N, rho_G,N,
  isometric/continuous inclusions and W3 adjoints where applicable,
  quotient and U1/CTP reality covariance,
  common dense finite core,
  boundary/contact form and permitted closure topology
).
```

This is not a fifth final object. It is the first required subobject of
Q-314's T1 `COMMON_COMPLETED_PHYSICAL_DOMAIN`.

```text
PHYSICAL_AG_TANGENT_AND_RESTRICTION_PACKAGE_EXISTS = false | TYPE-U
COMMON_COMPLETED_PHYSICAL_AG_DOMAIN_EXISTS = false | TYPE-U
```

---

## 3. The maximal bounded transport subpackage that does construct

### 3.1 Completed source/test domain

Retain P2's exact domain:

```text
E_src := ell^1_+ direct-sum ell^1_- direct-sum S_1,sym(H_CTP),
||s||_src:=||J_+||_1+||J_-||_1+||R||_trace.
```

For finite coordinate projections `P_N`, define

```text
rho_src,N(J_+,J_-,R)
 :=(P_NJ_+,P_NJ_-,P_NRP_N),

iota_src,N := zero extension.
```

Then

```text
rho_src,N iota_src,N=id,
rho_src,N=rho_(N<-M) rho_src,M,
iota_src,N=iota_src,M iota_(N->M),
||iota_src,N rho_src,N(s)-s||_src -> 0.
```

Thus

```text
E_src^fin:=union_N iota_src,N(E_src,N)
```

is dense in `E_src`, and

```text
intersection_N ker(rho_src,N)={0}.
```

These are consumed from P2, not reproved as new P7 content.

### 3.2 Completed physical current test carrier

Door B supplies the Hilbert completion `H_1^005` of the finite conserved
current span with continuous finite restrictions and W3-adjoint inclusions.
Let

```text
J_fin^005:=union_N iota_J,N(J_N^005).
```

It is dense in `H_1^005`, and

```text
intersection_N ker(rho_J,N)={0}.
```

This is the correct bounded physical **test** carrier for the cycle currents.
It is not renamed as the mean-field domain.

### 3.3 Bounded raw-bilinear class and restrictions

Define the already-ratified bounded class

```text
B_raw:=Bil_cont(J_phys^005 x J_phys^005;C).
```

For `B in B_raw`, define

```text
rho_raw,N(B)(x_N,y_N)
 :=B(iota_J,N x_N,iota_J,N y_N).
```

This is a linear contraction into the finite bounded-bilinear class and obeys

```text
rho_raw,N=rho_(raw,N<-M) rho_raw,M.
```

Reality acts covariantly:

```text
(Theta_raw B)(x,y)
 :=conjugate(B(Theta_J x,Theta_J y)),

rho_raw,N Theta_raw=Theta_raw,N rho_raw,N.
```

### 3.4 Bounded raw-class tail theorem

Suppose `rho_raw,N(B)=0` for every `N`. For arbitrary `x,y` choose finite-core
sequences `x_m->x`, `y_m->y`. At a stage containing both finite vectors,
`B(x_m,y_m)=0`. Bounded bilinear continuity gives `B(x,y)=0`; hence

```text
Tail_raw:=intersection_N ker(rho_raw,N)={0}.
```

This covers `RawGClass^005` as a closed sub-class. It does not cover a future
unbounded, bidual, weak-star, or distributional RetHess class.

```text
BOUNDED_RAW_RESTRICTIONS_EXIST = true | TYPE-P
BOUNDED_RAW_RESTRICTION_NATURALITY = PASS | TYPE-P
BOUNDED_RAW_REALITY_COVARIANCE = PASS | TYPE-P
BOUNDED_RAWG_CLASS_TAIL_ZERO = true | TYPE-P
```

### 3.5 Map 1 and raw `G^007` squares

Map 1 already proves

```text
T_M^char j_NM^Q=T_N^char,
T_N^char=Hol_(c_square)^(-1) compose Z_N.
```

Its quotient and reality covariance remain exact. It is a scalar-character
square, not a full operator-domain square.

Door D/V007 proves

```text
rho_raw,N(G_phys^007)=G_N^007.
```

The exact Q-243 and Q-279 finite shadows are reproduced: the
difference/difference block retains its symbolic dependence, while the
finite ordered retarded shadow remains zero and dependence-free. No value is
evaluated.

```text
MAP1_SCALAR_RESTRICTION_SQUARE = PASS | TYPE-P
RAW_G007_RESTRICTION_SQUARE = PASS | TYPE-P
Q243_SHADOW_REPRODUCED = PASS | TYPE-P
Q279_SHADOW_REPRODUCED = PASS | TYPE-P
RAW_G007_TAIL_ACTION = zero | TYPE-P
```

### 3.6 What P7 can and cannot consume

P7's proved content is used exactly at its stated boundary:

```text
C1 SOURCE-CORE:
  instantiated by E_src^fin dense in E_src.

FINITE-CYLINDER AND LEFT-MULTIPLIER SEPARATION:
  already proved by Q-257 and not rebuilt.

BOUNDED RAW CLASS SEPARATION:
  directly proved above on the DoR-015 current-test class.
```

P7's physical antecedents remain uninstantiated:

```text
C2 RESPONSE-CLASS:
  RetHess_phys and its injective representation do not exist.

C3 PHYSICAL-RESTRICTIONS:
  rho_H,N does not exist; rho_raw,N is not renamed rho_H,N.

C4 PACKAGE-PRESERVATION:
  stationary contour/measure/contact/boundary/domain data absent.

C5 EXTRACTION-CHAIN NATURALITY:
  inverse, contour Hessian, retarded extraction, and Schur maps absent.

C6 FINITE-BOTTOM-LEG:
  finite shadows exist, but no matched completed physical upper object exists.
```

Therefore P7 cannot convert the constructed raw squares into the physical
RetHess/T5 square.

```text
P7_SOURCE_CORE_CONSUMED = true | TYPE-P
P7_OBJECT_SIDE_PHYSICAL_CERTIFICATE_EXISTS = false | TYPE-U
P7_FULL_T5_CERTIFICATE_EXISTS = false | TYPE-U
P7_RETHESS_TAIL_THEOREM_INSTANTIATED = false | TYPE-U
```

---

## 4. Physical squares: built shadows versus absent completed squares

### 4.1 Built squares

The following diagrams genuinely commute:

1. P2 source restriction/retraction and zero extension;
2. conserved-current finite restriction and W3-adjoint inclusion;
3. bounded raw-bilinear pullback under finite current inclusions;
4. Map 1 under sequential zero extension;
5. `G_phys^007` to Q-243/Q-279 finite raw shadows.

Their kernels, images, sector transfers, and tails are recorded in Section 5.

### 4.2 Absent `rho_G,N` on the stationary connected-bilocal carrier

The symbol `rho_raw,N` restricts a bounded bilinear output to finite test
directions. The stationary `rho_G,N` must restrict physical connected-bilocal
configurations/tangents and preserve the source-free stationary surface. No
such carrier or map exists.

```text
RAW_BILINEAR_RESTRICTION_EQUALS_STATIONARY_G_RESTRICTION = false | TYPE-R |
  test: output-form pullback versus configuration/tangent restriction

PHYSICAL_STATIONARY_rho_G_N_EXISTS = false | TYPE-U |
  would-build: T_G carrier and finite physical restriction maps
```

### 4.3 Absent `rho_H,N` and physical Tail_R

P7 requires

```text
rho_H,N:RetHess_phys -> RetHess_N
```

intertwined with the ratified finite left multipliers. Neither
`RetHess_phys` nor this map is built. The bounded raw tail theorem cannot be
transported across this missing class-formation map.

```text
PHYSICAL_RETHESS_RESTRICTION_MAP_rho_H_N_EXISTS = false | TYPE-U
PHYSICAL_TAIL_R_ZERO = NO_VERDICT
```

### 4.4 Absent stationary Schur square

The required failure-capable identity is

```text
rho_H,N(
  Gamma_AA-Gamma_AG Gamma_GG^(-1) Gamma_GA
)
=
Gamma_AA,N-Gamma_AG,N Gamma_GG,N^(-1) Gamma_GA,N.
```

No term on either side is instantiated. Even after the blocks exist,
finite restriction need not commute with inversion. Build (iii) must prove a
reducing-subspace or exact inverse-intertwining theorem; P7 C5(d) does not
assume it.

```text
STATIONARY_SCHUR_RESTRICTION_SQUARE_EXISTS = false | TYPE-U
PHYSICAL_SCHUR_SQUARE_EXECUTION = BLOCKED | TYPE-C |
  constraints: block system, inverse, common domain, rho_G,N, rho_H,N absent

FINITE_RESTRICTION_COMMUTES_WITH_INVERSION_BY_DEFAULT = false | TYPE-R |
  test: compression of an invertible operator need not invert its compression
```

### 4.5 Essential self-adjointness

No named symmetric unbounded physical endpoint or stationary operator exists.
The bounded raw bilinear acts on its full test domain and needs no
self-adjoint extension theorem. Reality covariance is not self-adjointness.

```text
ESSENTIAL_SELF_ADJOINTNESS_CHECK_EXECUTABLE = false | TYPE-C |
  constraints: no named symmetric operator/common core pair

REALITY_COVARIANCE_IMPLIES_SELF_ADJOINTNESS = false | TYPE-R |
  test: covariance under an involution does not determine deficiency indices
```

---

## 5. Six-account rows and door flags

### 5.1 Operation table

| Operation | Kernel/applicability | Image | Sector transfer | Restriction square | Tail action | Verdict |
|---|---|---|---|---|---|---|
| P2 source completion | norm-zero equivalence | complete `E_src` | none | source retraction **PASS** | `Tail_src=0` | **PASS / TYPE-P** |
| form completed source kernel `K=ker L` | kernel of continuous `L` | closed source subspace | none | finite kernels commute | no created tail | **PASS / TYPE-P** |
| complete conserved-current test span | Hilbert norm-zero equivalence | `H_1^005` | no field identification | W3 square **PASS** | common finite tail zero | **PASS / TYPE-P** |
| form `B_raw` and `rho_raw,N` | bounded bilinear forms | finite bounded forms | no CTP/sector transfer created | pullback square **PASS** | `Tail_raw=0` | **PASS / TYPE-P** |
| Map-1 scalar factorization | scalar `Z_N` content only | primitive cycle quotient character | relative-history scalar to cycle character | Q-313 **PASS** | zero | **PASS / TYPE-P** |
| restrict `G_phys^007` | bounded raw class | exact finite raw blocks | DD/noise block retained; no retarded transfer | Q-243/Q-279 **PASS** | zero | **PASS / TYPE-P** |
| construct physical A/G domain | tangent carriers/intertwiners absent | none | source-to-field and R-to-G transfers unbuilt | cannot execute | `NO_VERDICT` | **TYPE-U** |
| construct `rho_H,N`/RetHess square | response class absent | none | `NO_VERDICT` | cannot execute | `Tail_R NO_VERDICT` | **TYPE-U / TYPE-C check** |
| stationary inverse/Schur square | blocks/domain absent | none | AG/GA transfer `NO_VERDICT` | cannot execute | `NO_VERDICT` | **TYPE-U / TYPE-C check** |

### 5.2 Door S — existing bounded source completion

```text
CLASS_FORMATION_DOOR_S := (
  input_class = finite P2 sources,
  formation = sum/trace norm completion,
  output_class = E_src,
  output_restrictions = rho_src,N,
  weak_star = false,
  bidual = false,
  distributional = false,
  Tail_output = {0},
  created_tail_image = zero by finite-core density,
  restriction_square = PASS,
  door_verdict = CLOSED_SEPARATED_ZERO_CREATED_TAIL
).
```

### 5.3 Door R — bounded raw bilinear class

```text
CLASS_FORMATION_DOOR_R := (
  input_class = continuous bilinear forms on finite conserved-current tests,
  input_topology = bounded-bilinear norm,
  input_restrictions = pullback by W3 inclusions,
  formation = bounded bilinear class on completed current-test carrier,
  output_class = B_raw,
  output_topology = bounded-bilinear norm,
  output_restrictions = rho_raw,N,
  weak_star = false,
  bidual = false,
  distributional = false,
  nonseparating_extension = false,
  Tail_output = {0},
  created_tail_image = zero by continuity/density,
  class_separation_proved = true,
  restriction_square = PASS,
  Q279_full_tuple_reproduced = PASS on built raw tuple,
  common_origin_provenance = TYPE-P on G_phys^007,
  target_independence = PASS,
  door_verdict = CLOSED_SEPARATED_ZERO_CREATED_TAIL
).
```

### 5.4 Door AG — unbuilt physical tangent/domain formation

```text
CLASS_FORMATION_DOOR_AG := (
  input_class = TYPE-U: physical Abar/G tangent carriers absent,
  input_topology = TYPE-U,
  input_restrictions = TYPE-U,
  formation = TYPE-U,
  output_class = COMMON_COMPLETED_PHYSICAL_AG_DOMAIN,
  output_topology = TYPE-U,
  Tail_output = NO_VERDICT,
  created_tail_image = NO_VERDICT,
  restriction_square = TYPE-U,
  common_origin_provenance = TYPE-U,
  door_verdict = UNDECIDED_TYPE_U
).
```

### 5.5 Door H — unbuilt RetHess/inverse/Schur formation

```text
CLASS_FORMATION_DOOR_H := (
  input_class = TYPE-U: stationary blocks and common domain absent,
  formation = inverse/Schur/retarded extraction with named topology,
  output_class = RetHess_phys,
  output_restrictions = rho_H,N,
  Tail_output = NO_VERDICT,
  created_tail_image = NO_VERDICT,
  restriction_square = TYPE-U,
  door_verdict = UNDECIDED_TYPE_U
).
```

No unflagged completion occurs.

---

## 6. Interfaces for builds (ii) and (iii)

### 6.1 Build (ii): stationary background realization

Build (ii) receives the exact bounded/source material:

```text
BackgroundInputInterface := (
  X_phys^005 quotient field/configuration family,
  E_src and dense finite source core,
  H_1^005 conserved-current test carrier,
  Map-1 cycle character and naturality,
  G_phys^007 bounded raw bilinear and finite restrictions,
  U1/CTP reality covariance,
  Q-281 seed and common-origin provenance
).
```

It must return:

```text
(Abar_*,G_*(Abar_*)),
T_Abar and T_G carrier/class declarations,
source-field and source-response intertwiners,
rho_A,N and rho_G,N,
stationary-surface and finite-restriction certificates.
```

The tangent/restriction part is not optional bookkeeping: without it the
background pair cannot be placed on the common physical domain required by
build (iii).

### 6.2 Build (iii): stationary blocks and Schur reduction

Build (iii) consumes build (ii)'s tangent carriers and must supply:

1. `Gamma_AA`, `Gamma_AG`, `Gamma_GG`, and `Gamma_GA` on one common domain;
2. a common dense invariant graph core and closure topology if any block is
   unbounded;
3. the `GG` inverse/prescription without deleting cycle-visible content;
4. `rho_H,N` and all intermediate restriction maps;
5. arrow-by-arrow P7 C4/C5, especially stationary/inverse/Schur C5(d);
6. the RetHess class representation and its tail theorem.

The bounded raw class and its tail theorem are available as a target if all
new operators remain bounded there. If any output leaves that class, build
(iii) must open Door H and prove the new tail action.

### 6.3 Consumer readiness

```text
BUILD_II_BOUNDED_INPUT_INTERFACE_READY = true | TYPE-P
BUILD_II_PHYSICAL_AG_TANGENT_INTERFACE_READY = false | TYPE-U |
  would-build: build (ii)'s field/connected-bilocal realization and restrictions

BUILD_III_SOURCE_RAWG_SHADOW_INTERFACE_READY = true | TYPE-P
BUILD_III_COMMON_PHYSICAL_DOMAIN_READY = false | TYPE-U
BUILD_III_PHYSICAL_SQUARES_READY = false | TYPE-U

P_VERDICT_COMPUTABLE_AFTER_THIS_ARTIFACT = false | TYPE-R |
  test: physical A/G domain, background, stationary blocks, and RetHess square absent
```

---

## 7. Kill passes

### 7.1 Carrier-role kill

The initial direct-sum candidate was attacked against Q-309/Q-314. It failed
because it joined source currents to bilocal sources and called them physical
mean-field/connected-bilocal tangents. The candidate was removed rather than
relabelled.

### 7.2 Raw output versus response tangent

No claim identifies a bilinear output with the domain of `Gamma_GG`. That
would require the missing physical Legendre/stationary map.

### 7.3 P7 antecedent versus conclusion

No `rho_raw,N` map is renamed `rho_H,N`. P7's class-transport theorem remains
proved but uninstantiated at the physical response class.

### 7.4 Inversion naturality

No claim assumes finite restriction commutes with inversion. The Schur square
remains a failure-capable build-(iii) certificate.

### 7.5 Topology and tail

Only ratified norm topologies are used. No weak-star, bidual,
distributional, graph, strong, or resolvent completion is invoked. The raw
zero-tail theorem is not transported beyond its bounded class.

### 7.6 No selection or evaluation

No background, stationary member, rank, frame, cycle basis, contour,
inverse prescription, domain extension, or physical fork is selected. No
spectral or numerical output is evaluated.

---

## 8. Final typed ledger

```text
BOUNDED_SOURCE_RAWG_TRANSPORT_SUBPACKAGE_EXISTS = true | TYPE-P
P2_SOURCE_RESTRICTION_SQUARE = PASS | TYPE-P
CURRENT_TEST_RESTRICTION_SQUARE = PASS | TYPE-P
BOUNDED_RAW_RESTRICTIONS_EXIST = true | TYPE-P
BOUNDED_RAWG_CLASS_TAIL_ZERO = true | TYPE-P
MAP1_SCALAR_RESTRICTION_SQUARE = PASS | TYPE-P
RAW_G007_RESTRICTION_SQUARE = PASS | TYPE-P
Q243_SHADOW_REPRODUCED = PASS | TYPE-P
Q279_SHADOW_REPRODUCED = PASS | TYPE-P

P7_SOURCE_CORE_CONSUMED = true | TYPE-P
P7_OBJECT_SIDE_PHYSICAL_CERTIFICATE_EXISTS = false | TYPE-U
P7_FULL_T5_CERTIFICATE_EXISTS = false | TYPE-U

PHYSICAL_AG_TANGENT_AND_RESTRICTION_PACKAGE_EXISTS = false | TYPE-U
COMMON_COMPLETED_PHYSICAL_AG_DOMAIN_EXISTS = false | TYPE-U
PHYSICAL_STATIONARY_rho_G_N_EXISTS = false | TYPE-U
PHYSICAL_RETHESS_RESTRICTION_MAP_rho_H_N_EXISTS = false | TYPE-U
STATIONARY_SCHUR_RESTRICTION_SQUARE_EXISTS = false | TYPE-U

PHYSICAL_TAIL_R_ZERO = NO_VERDICT
ESSENTIAL_SELF_ADJOINTNESS_CHECK_EXECUTABLE = false | TYPE-C |
  constraints: no named symmetric physical operator/common core pair

FENCE_BLOCKED_STRUCTURAL_RESULT = false
SELECTED_MEMBER_FOUND = false | TYPE-S |
  roots: all constructed source/current/raw families |
  exclusions: proofs quantify over arbitrary finite stages and forms |
  query: selected background, tangent identification, extension, inverse, rank

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

The bounded transport shadows are now fully explicit and certified. The full
transport infrastructure does not yet exist: its missing datum is not a
closure trick but the physical `Abar/G` tangent-and-restriction package. Builds
(ii) and (iii) now have that interface named without identifying sources with
their conjugate physical variables.
