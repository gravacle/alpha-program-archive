# Stage 8 CTP Physical Input Package — Item 2 Physical `Log_0` Germ Specification V001

Date: 2026-07-30  
Lane: CODEX 2  
Authority: relay 168; Q-52/Q-54; register head Q-64

## 0. Declaration, ranking, and stop point

Sealed Item 0 at
`STAGE8_CTP_PHYS_INPUT_PACKAGE_TRIAGE_AND_LOAD_BEARING_FUNCTIONAL_SPEC_V001.md:964-982`
ranks item 5b/D1 immediately after the pointwise normalized item-1 functional:

```text
item 1 -> D1/item 5b -> D2/raw G -> D3/D4 -> D5/item 6.
```

The next load-bearing object is therefore:

```text
PHYSICAL_NONZERO_DIFFERENTIABLE_LOG0_GERM
```

This artifact specifies that object under Q-52. It does not instantiate or
derive it.

Q-52's controlling words are quoted in sealed Item 0 at `:56-69`:

```text
declared, marked derived = false, and never reported as derived.
```

```text
PHYSICAL_NONZERO_DIFFERENTIABLE_LOG0_GERM_derived = false | TYPE-U | would-build: instantiate the pointwise item-1 functional and U1/U3 physical source package, prove an open nonzero neighborhood, construct the basepoint-continuous Log_0 branch, and prove the frozen regularity profile
specification_status = Q52_CONDITIONAL_SPECIFICATION
D1_property_tests_executed = false | TYPE-U | would-execute: run each applicable L-T0 through L-T7 property leg after its independent candidate, fixture, domain, and reference exist
physical_verdict = NO_VERDICT
```

The applicability stop is the unbuilt L0-L3 package, with B0/C0 upstream.
Sealed Item 0 at `:921-923` says the item-1 output:

```text
is not an analytic, differentiable, or nonzero germ.
```

```text
ITEM1_POINTWISE_NORMALIZED_CTP_FUNCTIONAL_derived = false | TYPE-U | would-build: construct B0, co-derive C0/U1-U3, and instantiate the sealed item-1 functional-evaluation contract
```

No upstream object is constructed or respecified here.

### 0.1 Process-review correction

A provisional draft treated sealed Item 0's adjacent lines `:612-613` as a
contradiction. Hostile review rejected that reading: "`under the
already-derived U3/item-5a prescription`" is a future sequencing condition in
a `would-build` clause, while `physical_contour_prescription_package_derived =
false | TYPE-U` is its present status. They are compatible.

```text
provisional_item0_contradiction_hypothesis_status = REJECTED [PROCESS_REVIEW_RESULT; NON_Q54_RESULT]
sealed_item0_U3_sequence_reading = FUTURE_APPLICABILITY_PREREQUISITE [PROCESS_CLARIFICATION; NON_Q54_RESULT]
```

No sealed artifact is repaired, retyped, or blocked by this clarification.

## 1. Scope and premises declared at the outset

### 1.1 Exact bounded scope

This item uses only the following roots:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha_supervision
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
```

and only these exact files:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v004.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_complete_dimension_convention_ledger_v004.md
/Users/bgm/MB Work/alpha-program-archive/workspace/COMPLETE_QSPEC_CANONICAL_FULL_ZERO_FREE_PROMOTION_RESULT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md
/Users/bgm/MB Work/alpha_supervision/NEGATIVE_RESULT_TYPING_PROTOCOL_V001.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_CTP_PHYS_INPUT_PACKAGE_TRIAGE_AND_LOAD_BEARING_FUNCTIONAL_SPEC_V001.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_CTP_PHYS_INPUT_PACKAGE_ITEM1_WORD_BOUNDARY_TRIAGE_REVERIFICATION_V001.md
```

Excluded:

```text
a32_holdout/custodian_private/ [not listed, entered, or searched]
the six Q-59 bridge objects
C_R and the sqrt(2) fork
forcing-boundary and registry questions
scripts, JSON payloads, binaries, attachments, superseded versions,
duplicate mirrors, and every file not listed above
```

No recursive root search, path-list-to-`xargs` pipeline, or alias merge was
used.

Q-54's adopted definitions are stated by
`NEGATIVE_RESULT_TYPING_PROTOCOL_V001.md:35-53`: TYPE-R is an executed
refutation and the only physical type; TYPE-U is unbuilt; TYPE-S is a
scope-empty bounded search with its packet; and TYPE-C is a
constraint-blocked check with a release condition. Its applicability rule at
`:82-98` sends a failure resting on an unbuilt premise to `NO_VERDICT`.

The controlling source-typing probe was this case-sensitive,
multi-pattern, fixed-string command form:

```text
LC_ALL=C grep -nH -F
  -e topology -e Topology -e topological -e Topological
  -e normed -e Normed -e seminorm -e Seminorm -e Banach
  -e "locally convex" -e "Locally convex"
  -e Fréchet -e Frechet -e Gâteaux -e Gateaux -e Bastiani
  -e "directional derivative" -e "Directional derivative"
  -e "derivative calculus" -e "Derivative calculus"
  -e "differentiability class" -e "Differentiability class"
  -e "source topology" -e "source-space topology" -e topology_src
  <the six source-evidence files in Section 1.1; the Q-54 protocol excluded>
```

It returned exit status 1 and zero occurrences. Every file was a discrete
quoted argument. Therefore the bounded result is deliberately narrow:

```text
named_source_topology_or_derivative_calculus_terms_found = false | TYPE-S | roots: the pre-cleanroom, archive-workspace, and cleanroom roots; exact scope is the six source-evidence files in Section 1.1 and excludes the Q-54 protocol | excl: the exclusions in Section 1.1 | fences: relay-168 fences and Section 1.1 | query: the exact case-sensitive LC_ALL=C grep -nH -F pattern family printed above
```

This scoped absence does not prove that no such calculus could exist or that
an unqueried synonym is absent. The positive text at
`primitive_record_cell_selection_principle_v004.md:49-51,63-69` supplies
formal source derivatives and the phrase "differentiable source
neighborhood"; it does not choose one of the queried topology/calculus
profiles. This specification therefore refuses to choose a profile and
declares L1 TYPE-U.

One earlier exploratory loose-substring probe for `i epsilon` matched the
unrelated token `pi epsilon_0` at
`alpha_complete_dimension_convention_ledger_v004.md:231`, inside the exact
six-file evidence scope. The probe was a case-sensitive loose fixed-string
search for `i epsilon`; it was discarded before classification and no content
from that line was used:

```text
loose_substring_probe_status = FALSE_POSITIVE_DISCARDED [PROCESS_ATTESTATION; NON_Q54_RESULT]
loose_substring_false_positive_event_count = 1 [PROCESS_COUNT; NON_Q54_RESULT]
```

### 1.2 Premises

The specification has four independently required inputs. All remain TYPE-U.

```text
L0_ITEM1_POINTWISE_FUNCTIONAL_derived = false | TYPE-U | would-build: instantiate the sealed pointwise Z_inc contract from B0 and C0/U1-U3

L1_PHYSICAL_SOURCE_TOPOLOGY_AND_CALCULUS_derived = false | TYPE-U | would-build: derive an explicitly typed physical source-space topology, its zero-source basepoint, admitted J/R domains, a fixed derivative calculus, and the D1 regularity profile before any D1 candidate is formed

L2_U3_CONTOUR_PRESCRIPTION_derived = false | TYPE-U | would-build: derive and freeze the physical contour/i-epsilon prescription before inspecting zeros, derivatives, or response output

L3_U1_REALITY_AND_SOURCE_INVOLUTION_derived = false | TYPE-U | would-build: derive the physical branch/source involution, compound-index order, and source symmetry on the completed quotient
```

The relay-158 premises P1-P8 remain TYPE-U. In particular P3 at
`STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md:42-49` is:

```text
Nonzero differentiable Log_0 neighborhood with fixed prescription
```

with status:

```text
derived = false | TYPE-U | would-build: prove a source neighborhood on which
the normalized inclusive functional is nonzero and differentiable
```

Nothing in this specification discharges P3 or any other P-row.

## 2. Recovered definition and ceiling

`primitive_record_cell_selection_principle_v004.md:57-69` states:

```text
G is the raw connected contour correlator, not an action Hessian. Log_0 is
the branch continuous from W_inc[0,0]=0.
```

It limits normalization:

```text
Unitarity fixes normalization only on the physical trace-preserving source
surface, in particular at J=R=0 with equal branch backgrounds; it does not
imply Z=1 for an arbitrary cross-branch bilocal source.
```

and gives the package ceiling:

```text
A nonzero differentiable source neighborhood, i epsilon prescription, and
invertible physical quotient remain Step 5 obligations.
```

`alpha_complete_dimension_convention_ledger_v004.md:238-258` defines:

```text
Z_IF[A,g;A,g]=1,
Gamma_ind=-i hbar Log_0 Z_IF,
Gamma_ind[A,g;A,g]=0.
```

and states:

```text
Existence of a nonzero differentiable Log_0 neighborhood and its i epsilon
regulator remains a Step 5 result.
```

A narrower positive subgate exists.
`COMPLETE_QSPEC_CANONICAL_FULL_ZERO_FREE_PROMOTION_RESULT_V001.md:88-129`
proves nonvanishing and a continuous logarithm branch for its frozen finite
periodic family, but expressly withholds:

```text
the local-source polydisc
```

and the physical continuum/cellulation promotion. That result is a finite
control, not the physical D1 object specified here.

## 3. Conditional domain

An input candidate is:

```text
(Z_inc, D_src, 0_src, topology_src, Diff_src, Reg_D1, U1, U3)
```

where:

1. `Z_inc` is an instantiated item-1 normalized inclusive pointwise
   functional on the physical source set `D_src`;
2. `0_src` is the zero-source/equal-background point and is admitted by
   `D_src`;
3. `topology_src` is independently derived and fixes the meanings of open
   neighborhood and continuity;
4. `Diff_src` is independently derived and fixes the derivative calculus,
   admissible directions, convergence/remainder criterion, and comparison
   topology before a candidate germ is inspected;
5. `Reg_D1` is frozen before candidate formation and requires exactly the
   first admitted `J` derivative and first admitted symmetric-`R` derivative
   used by the sealed formal identities at
   `primitive_record_cell_selection_principle_v004.md:49-54`;
6. U1 fixes the source symmetry and CTP reality involution;
7. U3 fixes the physical quotient, measure, boundary domains, and contour
   prescription before any zero or derivative is inspected.

The following are not assumed inputs:

```text
an open neighborhood N;
nonvanishing of Z_inc on N;
a Log_0 branch;
proof that the candidate satisfies the frozen Reg_D1 profile;
D2/raw G;
D3 contacts;
D4 Ward identities;
D5 inverse data.
```

Those are candidate outputs or successors. Missing L0-L3 makes a physical D1
test inapplicable and yields `NO_VERDICT`; it is not evidence against the D1
specification.

## 4. Conditional codomain

The codomain is a tuple:

```text
PhysicalLogGerm =
  [(N, Z_inc|N, Log_0, W_inc, Reg_req, provenance)]_0_src
```

with:

1. representatives equivalent when they agree on a smaller admitted
   neighborhood of `0_src`;
2. `N` open in `topology_src` and `0_src` an interior point;
3. `N` contained in the admitted physical quotient/source domain and stable
   under every U1/U3 map claimed for the candidate;
4. `Z_inc(s) != 0` for every `s` in `N`;
5. `Log_0` a single-valued branch continuous from the zero-source basepoint;
6. `Log_0 Z_inc(0_src)=0`;
7. `W_inc=-i hbar Log_0 Z_inc` on `N`;
8. `Reg_req=Reg_D1` is expressed only in the independently frozen `Diff_src`
   calculus and records:
   - the first admitted `J`-direction derivative needed later to define
     `Abar`;
   - the first admitted symmetric-`R` derivative needed later to define
     D2/raw `G`;
9. U1 CTP reality and source symmetry inherited on the same domain;
10. U3 prescription, quotient, measure, and boundary-domain provenance carried
   as part of the germ's identity.

This codomain does not include D2/raw `G`, an inverse, contacts, Ward
identities, or a retarded Hessian. It also does not select a Banach,
locally-convex, Fréchet, Gâteaux, Bastiani, or other calculus: that choice must
arrive through L1 before the candidate is formed. A second- or mixed-derivative
claim is a separately identified extension candidate with its own frozen
profile; it may not alter this D1 candidate after inspection.

## 5. Required relation

Conditionally on L0-L3, a valid candidate must satisfy:

```text
exp(Log_0 Z_inc(s)) = Z_inc(s)               for every s in N,
Log_0 Z_inc(0_src) = 0,
W_inc(s) = -i hbar Log_0 Z_inc(s),
W_inc(0_src) = 0.
```

The branch is selected only by continuous continuation from the normalized
basepoint under the already-frozen U3 prescription. It may not be selected
after inspecting a response output.

No assertion is made that `Z_inc=1` away from the physical
trace-preserving/equal-background source surface.

The candidate record must expose the frozen description of `N`, the
nonvanishing witness, the anchored logarithm-lift witness, and the regularity
witness in `Diff_src`. These are testable candidate claims, not admission
criteria that are silently checked before a candidate can be seen. A failed
candidate neighborhood may not be shrunk or replaced after the failure without
creating a new, separately identified candidate.

## 6. Failure-capable, property-specific tests

No test below is executed here. Missing applicability data gives
`NO_VERDICT` for that property only.

### L-T0. Upstream basepoint compatibility

Applicability requires instantiated L0-L3.

Check that `0_src` belongs to the admitted domain and that the separately
tested item-1 normalization is the declared basepoint value.

Failure routes upstream:

```text
L-T0_ITEM1_BASEPOINT_COMPATIBILITY_VERDICT
  FAIL_TYPE_R only against item-1 normalization/domain compatibility;
  implication = does not refute the abstract D1 specification.
```

### L-T1. Open-neighborhood topology test

Applicability requires a candidate `N` and independently derived
`topology_src`.

Failure condition:

```text
N is not open in topology_src, 0_src is not interior, or N leaves the admitted
physical source domain.
```

Verdict target:

```text
L-T1_D1_DOMAIN_VERDICT = FAIL_TYPE_R only against the candidate D1
open-neighborhood property.
```

### L-T2. Nonvanishing test

Applicability requires candidate `N`, instantiated `Z_inc|N`, and an
independently specified zero-detection oracle or certified zero witness.

Failure condition:

```text
there exists an admitted s in N for which Z_inc(s)=0.
```

Verdict target:

```text
L-T2_D1_NONVANISHING_VERDICT = FAIL_TYPE_R only against the claimed
nonvanishing of that candidate neighborhood.
```

No oracle/witness gives `NO_VERDICT`; sampling that finds no zero is not a
proof of nonvanishing. A certified zero sequence tending to `0_src` refutes
existence of the claimed germ on that topology; a certified isolated zero
inside the frozen `N` refutes that representative's nonvanishing claim.

### L-T3. Branch existence and monodromy test

Applicability requires a proved nonzero candidate domain and an independently
derived family of paths/loops sufficient for the claimed topology.

Failure conditions:

```text
continuation from 0_src is path-dependent;
a closed admitted loop returns a different logarithm value;
or the candidate branch violates Log_0 Z_inc(0_src)=0.
```

Verdict target:

```text
L-T3_D1_LOG_BRANCH_VERDICT = FAIL_TYPE_R only against the claimed
single-valued basepoint-continuous branch.
```

### L-T4. Regularity-profile test

Applicability requires a candidate `W_inc`, the frozen `Diff_src` and
`Reg_req`, and independently certified same-domain directions, sequences, and
remainder or convergence references. No D2-D5 candidate output may serve as
the oracle for D1.

Test each claimed derivative order separately in the predeclared calculus. A
certified failure of its derivative/remainder/convergence criterion is TYPE-R
only against that named regularity property.

```text
L-T4_J_FIRST_DERIVATIVE_VERDICT
L-T4_R_FIRST_DERIVATIVE_VERDICT
```

Neither leg asserts or refutes a higher regularity class.

### L-T5. Prescription freeze and equivalence test

Leg (a) checks process provenance. A prescription selected after zero/response
output was inspected violates the predeclaration constraint; that is a TYPE-C
usability block, not physical content:

```text
L-T5a_PRESCRIPTION_PREDECLARATION_VERDICT_RULE =
  if late selection is observed, report
  BLOCKED | TYPE-C |
  constraint: prescription, topology, derivative calculus, regularity profile,
              branch rule, and candidate-neighborhood identity must be frozen
              before zero or downstream response output is inspected |
  release: supply a new separately identified candidate with those fields
           frozen before inspection

L-T5a_current_verdict = NO_VERDICT
```

Leg (b) is applicable only when two prescriptions have an independently
proved equivalence map on the same physical domain. Failure occurs if the two
germs disagree after that map.

Targets:

```text
L-T5b_PRESCRIPTION_EQUIVALENCE_NATURALITY_VERDICT = FAIL_TYPE_R only against
the claimed physical equivalence/naturality property.
```

Missing a frozen U3 prescription gives `NO_VERDICT`; it is not a physical
failure of D1.

### L-T6. CTP reality test

Applicability requires U1, a candidate germ on a source domain closed under
the U1 involution, and an independently passed item-1 pointwise
`Z_inc`-reality test. If the item-1 reality property fails or remains untested,
the verdict routes to item 1 and L-T6 returns `NO_VERDICT`.

Failure condition:

```text
Log_0 Z_inc(U1(s)) != [Log_0 Z_inc(s)]^*
or
W_inc(U1(s)) != -[W_inc(s)]^*
```

Verdict target:

```text
L-T6_D1_CTP_REALITY_VERDICT = FAIL_TYPE_R only against the candidate D1
reality property.
```

### L-T7. Finite-control restriction test

Applicability requires an independently derived restriction intertwiner from
the physical source domain to the finite periodic control and a common
prescription/branch map. The corresponding item-1 pointwise `Z_inc`
restriction square must have independently passed first. A pointwise mismatch
routes upstream to item 1 and leaves L-T7 at `NO_VERDICT`.

Failure condition:

```text
the physical candidate germ and the finite proved germ fail the claimed
restriction square.
```

Verdict target:

```text
L-T7_FINITE_RESTRICTION_COMPATIBILITY_VERDICT = FAIL_TYPE_R only against the
claimed restriction compatibility.
```

It does not refute D1 existence when no restriction intertwiner is claimed.

## 7. Verdict router

```text
D1_DOMAIN_CONFORMANCE
  consumes L-T1 only.

D1_NONVANISHING_CONFORMANCE
  consumes L-T2 only.

D1_BRANCH_CONFORMANCE
  consumes L-T3 only.

D1_REGULARITY_CONFORMANCE
  reports each L-T4 leg separately.

D1_PRESCRIPTION_PROCESS_USABILITY
  consumes L-T5a only and reports TYPE-C, never TYPE-R.

D1_PRESCRIPTION_EQUIVALENCE_CONFORMANCE
  consumes L-T5b only.

D1_REALITY_CONFORMANCE
  consumes L-T6 only.

D1_FINITE_RESTRICTION_COMPATIBILITY
  consumes L-T7 only.

ITEM1_BASEPOINT_COMPATIBILITY
  consumes L-T0 and remains upstream.
```

No aggregate failure is propagated across targets. A property survives only
if at least one applicable test for that property executes, every executed
applicable test passes, and every inapplicable test is named. A pass sets no
`derived` flag.

## 8. Reachability and result

The finite zero-free result supplies a genuine control. Its own scope at
`COMPLETE_QSPEC_CANONICAL_FULL_ZERO_FREE_PROMOTION_RESULT_V001.md:113-131`
stops before the local-source polydisc and physical continuum promotion. The
remaining promotion is typed below rather than read as a physical negative.

```text
D1_reachable_now = NO_VERDICT
ITEM1_POINTWISE_NORMALIZED_CTP_FUNCTIONAL_derived = false | TYPE-U | would-build: instantiate sealed Item 0 Section 5 from B0 and C0/U1-U3
PHYSICAL_SOURCE_TOPOLOGY_AND_CALCULUS_derived = false | TYPE-U | would-build: derive and freeze topology_src and Diff_src before proposing a D1 neighborhood or derivative claim
U3_CONTOUR_PRESCRIPTION_derived = false | TYPE-U | would-build: derive and freeze item 5a before zero or response inspection
FINITE_CONTROL_TO_PHYSICAL_D1_PROMOTION_derived = false | TYPE-U | would-build: derive the physical source/refinement intertwiner and prove the nonvanishing, branch, topology, and regularity square on the physical source domain
PHYSICAL_NONZERO_DIFFERENTIABLE_LOG0_GERM_derived = false | TYPE-U | would-build: instantiate L0-L3 and satisfy the property-specific tests above
```

Relay 158's P3 specification is retained and narrowed by explicit topology,
regularity, provenance, and verdict routing. Its formulas are left unchanged.

```text
relay158_P3_status = RETAINED_AND_NARROWED
D1_Log0_germ_contract_specified = true [PROCESS_ATTESTATION; NON_Q54_RESULT]
construction_status = SPECIFICATION_ONLY [PROCESS_ATTESTATION; NON_Q54_RESULT]
new_physical_premise_status = NO_NEW_PREMISE_ADOPTED [PROCESS_ATTESTATION; NON_Q54_RESULT]

terminal_fence_declarations_below = Q54_EXEMPT
alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_Thomson_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
coupling_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
radius_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
scale_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
root_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
eigenvalue_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
beta_function_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
measured_constant_comparison_status = NOT_PERFORMED [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
protected_holdout_access_status = PROHIBITED_AND_NOT_PERFORMED [PROCESS_FENCE_ATTESTATION; NON_Q54_RESULT]
```
