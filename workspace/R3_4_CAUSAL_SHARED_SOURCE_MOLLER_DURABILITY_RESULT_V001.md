# R3.4 Causal Shared-Source Moller/Durability Result v001

## Verdict

```text
PRIMITIVE_CAUSAL_MOLLER_AND_PUBLIC_DURABILITY_DERIVED
```

Causal Incidence Support upgrades the earlier moving-front calculation from
a conditional construction to a derived primitive-sector consequence. The
ordinary local parent has a finite-support Moller map, and completed public
record observables are not rewritten by later primitive events, even when
those events share and noncommutatively update the same source.

This is primitive public durability. It is not yet complete physical
durability because effective descendants of the complete action have not
been derived and exhausted.

## Exact outgoing statement

For a finite causal history ending at `T_N`,

```text
H_N(t)=H_S tensor I_R + sum_j v_j(t)B_j
```

in the primitive sector, and every `v_j` has compact causal support. Hence

```text
Omega_N=exp(+i H_S T_N)U_N(T_N,0)
```

is an exact finite-support relative Moller operator.

For a completed pointer observable `Z_j`,

```text
[H_S tensor I_R,Z_j]=0,
[B_k,Z_j]=0  for k>j.
```

Its Heisenberg image is therefore constant after its closure face. The
argument is independent of overlap among the source-supported pieces of
`B_j` and `B_k`.

## Intrinsic-cell regulator

The finite check uses the time marginal forced by the uniform measure on a
unit `3+1` causal diamond:

```text
w(s)=32(1/2-|s-1/2|)^3,
integral_0^1 w(s)ds=1.
```

Two cells use overlapping source projectors with
`Tr(P_0P_1)=1/4`. The calculated first-pointer probabilities are:

```text
after first closure:       0.8066740227533682
after second closure:      0.8066740227533951
after later free evolution 0.8066740227533942
```

The stability error is `2.69e-14`. No target value was assigned to the first
probability. In particular, this result does not attempt to restore the
rest-normal exact endpoint after the Lorentzian gate showed that a generic
finite packet does not have one.

The relative Moller unitarity error is `2.57e-13`. Reversing the two
overlapping-source cells changes the finite parent by `4.806`, confirming
that causal order remains physical. Midpoint evolution converges at the
expected second-order ratio `4.00007`.

An independent verifier evolves the state with fourth-order Runge-Kutta
rather than exponentiating midpoint Hamiltonians. It reproduces both
pointer probabilities and exact persistence.

## What is now closed

```text
primitive_finite_support_Moller_derived = true
primitive_public_pointer_persistence_derived = true
primitive_public_outgoing_endomorphism_derived = true
```

The successful object is the ordinary local sum. The conditional comoving
transport rule is not used.

## Remaining obligation

Causal Incidence Support explicitly permits descendants generated by the
complete source, gauge, gravitational, record, and environmental action.
Those descendants must be derived and checked rather than omitted because
they might disturb the public pointer algebra. Until that is done, the
primitive theorem cannot be promoted to complete physical durability.

The source-return spectral problem is also separate. Stable record
probability does not by itself derive a finite-energy root, an absolutely
continuous complete spectral measure, or thresholded source-return decay.

```text
generated_descendant_action_derived = false
complete_physical_durability_derived = false
physical_in_state_selected = false
finite_energy_physical_root_derived = false
complete_root_spectral_measure_derived = false
thresholded_source_return_decay_derived = false
complete_parameter_free_Q_spec_frozen = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
