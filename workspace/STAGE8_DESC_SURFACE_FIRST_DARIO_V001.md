CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00005828   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 18 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing (Q-913 standard).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001. Every span cites member + line locus. NO FLAG NAME
  IS USED AS AN ANCHOR — see §6.3, where the three flag names this artifact discusses are shown to
  resolve 7-, 10- and 10-ways across the corpus. The trap has fired eight times; it does not fire here.
LANE = DARIO   ROLE_THIS_RELAY = SURFACE-FIRST CONSTRUCTION (relay 1050)
ALL_RESULTS = CLAIMED until the opposite-lane (Codex 2) check. Builder never verifies own work.
SELF_CITATION_BAR = DARIO's own prior outputs are NOT record witnesses and none is cited as one.
LIVE_MEMBERS = NONE. No register, tracker, or other live-append file is in the verdict-bearing set.
  Strict and stable prose-digest modes therefore coincide by construction, not by tolerance (§6.2).
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1050_SURFACE_FIRST_DARIO_V001.md` | `ce71155755ae13da1c977e094b59af457772c665154245ce66ce7c428ccad73c` | assignment |
| 02 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap; the standing ruling that the form is off-limits as a source |
| 03 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 04 | `workspace/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | **THE RECORD ACTION — law side: `Q_spec[g,a,psi,R]`, the record cell postulate, the write** |
| 05 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md` | `2be95d5b58ea000df9a30b717a809374a90693f78a893d87b73a12e4be97fa21` | **THE CELL AND THE COMPLETED-STATE THEOREM (arbitrary `N`, analytic)** |
| 06 | `workspace/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | **THE WRITE GEOMETRY: profile- and order-independence** |
| 07 | `workspace/R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_RESULT_V001.md` | `321c52bc3f5cf8d66b2ca4a7f14811a41e905048ef89415f8c76e22837261c58` | the write/tail join, and its own non-derivation |
| 08 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **Gen_Omega: A0, the channel FAMILY {BI,DB,SYM}, `d_state`/`d_ready`/`d_law`. READ ONLY WITH 09 AND 10.** |
| 09 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the overlay that governs how 08 may be read (its read rule at `:234-245`) |
| 10 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification that governs 08; the no-member clause |
| 11 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **TASK2D sector identity `B iso C(Y)`; the continuum survivor table** |
| 12 | `workspace/STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md` | `ab156ee764db9d0bd48f54f1b879f1bafcfac08b45520ca6c4fb582e48edf572` | **the attachment adjudication — the missing surface object's locus** |
| 13 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | **the descent maps: `i_R`, `i_B`, `res_B`, `E_out`, and the non-uniqueness theorem** |
| 14 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | **the producers P0–P7, consulted ONLY for surface content (what physics they describe)** |
| 15 | `workspace/STAGE8_P5_SECTOR_ASSIGNMENT_V001.md` | `898cd5f40b5729577f5acf74fa6496929c58b1a39c2acc611ee7f920a9e9e192` | the P5 straddle: state/effects/domains sit at the join |
| 16 | `workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | **THE FORM. OPENED IN STEP 3 ONLY, AS A CHECKLIST. Not a derivation source.** |
| 17 | `workspace/STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md` | `c49d31200460e96209375f06a7a655d343767695ef09805f1e396d1814833b6b` | **the Q-227 baseline: `S` on all three basis vectors, `P_0=I-P_ch`, and the same completed-state form** |
| 18 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_SPEC_V002.md` | `4a7600caa23d0c7a98eeef8a79941c20ca4e28a4f5a2c1cf5c2362e88c7d4721` | **the charge projector at bytes: `spec(Q)={-1,0,+1}`, `P_ch=1_(R\{0})(Q)`** |

```text
BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO MEMBER OF THE ORIGIN FAMILY IS BOUND — the FAMILY acts (DoR-013's clause and the fence agree).
NO FIXED-POINT EXECUTION: d_state's characterization is cited; no iteration is run.
NO END TEST. NO MEASURED-CONSTANT COMPARISON. NO FREEZE. omega_phys UNTOUCHED.
NOTHING IS ADOPTED, RATIFIED, PROMOTED, OR REGISTERED HERE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESC: SURFACE-FIRST — BUILD THE SOURCE FROM THE PHYSICS
## RELAY 1050 — `[PLAN:DESC-32]` — DARIO LANE — [CLAIMED]

Date: 2026-08-11

**HEADLINE.** The surface composes. Two sealed pieces that were never put together — Gen_Omega's
preparation channel and R3.4's arbitrary-`N` completed-state theorem — mate exactly, and their
mating condition discharges itself rather than being assumed. The composition yields a genuine
joint object on the source-record face with both marginals forced, and the record marginal's
mixing weight is the same symbolic sector-dimension ratio the source side derives. It then stops
one tensor factor short of the object the section is asking about, at a wall the surface describes
in its own words as permissive. **The nine fields do not fail nine times. They fail once, in one
place, for one missing physical ingredient** — and that is the new information this route buys.

---

## 0. METHOD, AND HOW THE FORM WAS KEPT OUT

### 0.1 The quarantine, stated as a procedure and not as an intention

The temptation named in the assignment is real and I treated it as an operational hazard rather
than a matter of good faith. The method was:

1. **Exclusion by path, before reading.** The archive was searched once for files containing
   `PhysicalSig_0`. The hit list was recorded **as a list of names to avoid** and every one of them
   was placed off-limits for Steps 1–2. Those names are: `STAGE8_OBJ0_EXACT_SIGNATURE_...`,
   `STAGE8_DESC_PHYSICALSIG_SELECTION_...`, `STAGE8_ACCESSOR_THEOREM_SUBSTITUTION_...`,
   `STAGE8_DESC_MONOTONICITY_CHECK_...`, `STAGE8_DESC_SELECTION_CHECK_...`,
   `STAGE8_DESC_Q92C_EXECUTION_...`, `STAGE8_DESC_Q92C_CHECK_...`,
   `STAGE8_DESC_SCHEMA_AND_INTERSECTION_...`, `supervision/EXECUTION_TRACKER.md`,
   `supervision/QUESTIONS_SETTLED_REGISTER_V001.md`, `supervision/PRINCIPAL_EXPECTATIONS_LEDGER_V001.md`,
   and relay pastes 1044/1045/1046. **None was opened before Step 3.** (Members 03 and 16 are the
   only supervision/form files in the read set; 03 is the state brief, and 16 was opened in Step 3.)

2. **The text was fixed before the form was opened, and the fixing is checkable.** Steps 1 and 2
   were written out in full and closed **before** member 16 was read. That closed text was hashed:

```text
STEP-1/2 TEXT, FIXED BEFORE THE FORM WAS OPENED
  SHA-256 = 73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31
  ARCHIVED AT workspace/STAGE8_DESC_SURFACE_FIRST_STEP12_PREFORM_DARIO_V001.md, sealed alongside
    this artifact so the pin is CHECKABLE rather than merely cited.  A quarantine digest nobody can
    rehash is not evidence; the opposite lane can rehash this one and diff it against §§1–2.
```

   Sections 1 and 2 below are that text, carried. The Step-3 exhibition **reads off** it and does
   not revise it. If a field had been easier to exhibit under a slightly different Step-2 sentence,
   that sentence was already frozen and could not be adjusted to suit.

3. **One disclosure, made rather than hidden.** While reading member 11 — a subject the assignment
   directs me to — I encountered, inside its own quotation of the U2 receiver, the *StatePort_U2_008*
   seven-field list (`member 11 :318-321`). **That is a different object from `PhysicalSig_0` and has
   a different arity (seven, not nine).** I record the encounter because it happened, note that it is
   a port-contract schema of the kind the assignment excludes, and state that it did not shape the
   build: nothing in §1 or §2 is organised around it, and it appears nowhere in the composition.

```text
FORM_QUARANTINE = HELD-UNTIL-STEP-3
  method: (i) exclusion-by-path list recorded before reading; (ii) Steps 1–2 written, closed and
  hashed (73ef21bd…) before member 16 was opened; (iii) one adjacent-schema encounter disclosed.
```

### 0.2 The standing ruling, obeyed

The bootstrap (member 02 `:11`) rules the form and all Q-92(c) machinery **off-limits as derivation
sources**. Accordingly no schema field, no port contract, no freeze artifact and no inventory
appears anywhere in §1 or §2 as an ingredient or as a reason. The producers (member 14) were read
**only for the physics they describe** — P0's microscopic operator, P4's field/CTP package, P5's
`rho_pre`/effects/domains clause, P6's dynamics — and never for their schema fields.

### 0.3 What "grounded" meant at each step

Every composition step below is either GROUNDED (each ingredient and each composition rule cited at
a member and locus) or it is reported as a stop with the missing **physical ingredient** named,
typed and located. No composition rule was supplied by me.

---

## 1. STEP 1 — THE BUILD

### 1.1 GROUNDED — the cell carrier and the write

Member 04 `:70-82` gives the law-side statement: durable records are generated by physical boundary
degrees of freedom; on every admissible primitive causal record cell the one-handle record carrier
reduces to the previously derived two-endpoint projective carrier; and the record carrier supplies
the compared alternatives while the connection transports their active relative phase.

Member 05 `:17-38` makes that concrete **by computation rather than insertion**. From
`B_ch = P_ch tensor gamma^5 tensor c_partial` and the declared integrated write action, the
executable diagonalises the full operator and finds, on both chiral sectors,

```text
exp(-i tau_R gamma^5 tensor c_partial) = I_chiral tensor [[0,1,0],[1,0,0],[0,0,-1]],
```

so that the complete record action is `|r> <-> |p_Q>` and `|e_Q> -> -|e_Q>`.

**CELL CARRIER, read off:** the record cell is the three-dimensional span `{|r>,|p_Q>,|e_Q>}`. This
is a surface object — the carrier the incidence operator acts on — not a schema field. It agrees in
dimension and in basis role with Gen_Omega's `iota_cell` codomain, and the swap agrees with
Gen_Omega's one-cell law `S|r>=|p>`, `S|p>=|r>` (member 08 `:568-580`).

**ONE COMPOSITION DETAIL, CHECKED RATHER THAN ASSUMED.** Member 08's V003 presentation states `S`
on `|r>` and `|p>` and states `D(z)=diag(1,z,1)`; it is **silent on `S|e>`**. I flagged that as the
one place the two pieces are not verbatim identical, and then checked it instead of filling it.
Member 05 `:44-46` determines the entry to be `-1` — *the unused edge-sector action is fixed by the
incidence operator* — and **member 17 `:150-152` states the whole of `S` at bytes**:

```text
S|r>   = |p_Q>,
S|p_Q> = |r>,
S|e_Q> = -|e_Q>.
```

Member 17 is the Q-227 baseline that member 08 itself cites as its ratified source (member 08
`:182-199`). So this is not a silence filled by me from a second document; it is **one `S`, stated
in full in the baseline both pieces descend from**, and member 08's presentation is simply an
abbreviation of it. Nothing is selected and nothing is patched.

**WRITE GEOMETRY, GROUNDED.** Member 06 `:16-48`: with `H_j(t)=v_j(t)B_j` and fixed integrated
action, the generator is constant through the pulse, so `[H_j(t),H_j(s)]=0` and
`T exp[-i integral H_j] = exp(-i tau_R B_j)`. The write is **profile-independent**. Across distinct
cells `[B_j,B_k]=0`, so the endpoint map is **independent of the causal linear extension**. Both are
computed facts about the constructed write.

*(Fence note: the integrated write action is carried as the symbolic parameter member 05 declares.
The selector workstream around it — named off-limits at member 14 `:13-18` — was not entered, and no
value derived from it is formed, compared or evaluated anywhere in this artifact.)*

### 1.2 GROUNDED — the source carrier and the channel family acting on it

Member 08 `:352-363` (A0): `H_src^A := P_0 H_src direct-sum P_ch H_src`, `A_src^A := End(H_src^A)`,
a faithful trace `Tr_A`, and orthogonal `P_0,P_ch` with `P_0 + P_ch = I_src`.

Member 08 `:401-410` and `:419-430`: the family `G1_v003` and its three anchor members BI, DB, SYM.
Read **with** member 10 `:9-13` and member 09's read rule `:234-245`:
`status(member 12) := RATIFIED_FAMILY_LEVEL_BY_DOR_013`, no-member clause carried.
*** NO MEMBER IS SELECTED. *** The **family** acts. The licence for that is DoR-013's own
`p_ch`-neutrality certificate: every member forces the same normalized-identity invariant state.

Member 08 `:628-635` (`d_state`): the map is *the unique normalized fixed state of `P_src`*, and the
anchor theorem then **proves, rather than defines**, that it equals `I_src/Tr_A(I_src) =: omega_tr`.

**No fixed-point execution is run.** The characterization is cited and the already-proven value is
carried. No iteration is performed; no dimension is selected or evaluated.

Member 08 `:641-643` (`d_ready`): the all-ready record ray `C|r>^(tensor N)`, plus root only.
Member 08 `:650-654` (`d_law`): `P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[a]`, exactly DoR-009.

### 1.3 THE COMPOSITION STEP — and why its hypothesis discharges itself

Member 05 `:59-84` supplies an **arbitrary-`N` analytic theorem** (its `N=1,2,3` runs are regression
checks of the formula, not its basis), with one hypothesis:

```text
product_(j=1)^N [P_0 tensor I + P_ch tensor S_j] = P_0 tensor I + P_ch tensor product_(j=1)^N S_j,

and for ANY normalized source density matrix satisfying charge superselection
    rho_S = P_0 rho_S P_0 + P_ch rho_S P_ch,
together with the all-ready record state, the completed state is

    rho_N = P_0 rho_S P_0 tensor |r><r|^(tensor N)
          + P_ch rho_S P_ch tensor |p_Q><p_Q|^(tensor N),

and tracing the final record factor returns rho_(N-1) identically.
```

**The hypothesis is not assumed of the composed object. It is discharged, exactly.** `d_state`'s
output is `omega_tr = I_src/Tr_A(I_src)`, and because A0 (`member 08 :352-363`) supplies `P_0,P_ch`
orthogonal with `P_0+P_ch=I_src`,

```text
P_0 omega_tr P_0 + P_ch omega_tr P_ch
  = (P_0 I_src P_0 + P_ch I_src P_ch)/Tr_A(I_src)
  = (P_0 + P_ch)/Tr_A(I_src)
  = I_src/Tr_A(I_src)
  = omega_tr.
```

Charge superselection is therefore an **identity** of the state the preparation channel forces, on
the projectors the source carrier supplies. The two pieces mate, and the mating condition
discharges itself. This is the step the section has been looking for, and it is the reason the
surface route reaches an object at all.

The other two hypotheses discharge the same way. R3.4 requires *the all-ready record state*, and
`d_ready` **is** the all-ready ray. R3.4's `S_j` is *the computed full root/public swap on cell `j`*,
and `d_law` is the controlled write on the same projectors; at zero source history the character is
trivial (`member 08 :761`, `D_n[0]=I`), so `W_N` reduces to `tensor_j S_j` — exactly R3.4's
left-hand side.

**THE ONE SUBSTITUTION THAT WOULD BREAK THIS, TESTED.** The discharge above is only worth anything
if member 05's `P_0,P_ch` and member 08's A0 `P_0,P_ch` are the **same projectors** and not merely
the same letters — member 05 works on a carrier carrying a chiral factor, while A0's carrier is the
finite scalar realization. I checked it rather than assuming it:

```text
member 18 :31-36   spec(Q)={-1,0,+1};  P_ch = 1_(R\{0})(Q)
                   -> P_ch IS the source total-charge spectral projector.
member 17 :158     P_0 = I - P_ch
                   -> the pair is the charge grading of the source, nothing else.
member 08 :182-199 Gen_Omega cites THE SAME ratified source-coupled result for
                   U_N[a] = P_0 tensor I_(3^N) + P_ch tensor W_N[a], P_0 + P_ch = I_src.
member 18 :50-53   "The physical source state is block diagonal in total charge."
```

**The identification holds at bytes, by common lineage rather than by notation**: both pieces take
`P_0,P_ch` from the same ratified result, and member 08 says so itself. The chiral factor is a
spectator — member 05 `:40-44` records the endpoint map as source-spin identity and
charge-nondemolition, so it neither enters nor is disturbed by the grading.

**The residual, stated rather than buried.** A0 adds one thing the ratified projectors do not carry:
the authoring condition `dim(H_src^A) < infinity`. That is authored physics, not a consequence — and
member 08 `:896-906` says so in its own self-kill, calling A0 the weakest part of the proposal and
recording that the price travels with every downstream use. **It travels here too.** The composition
of §1.4 inherits A0's finiteness as a disclosed authored premise, exactly as DoR-013 ratified it, and
this artifact does not launder it into a derived fact.

### 1.4 THE OBJECT THAT COMPOSES

```text
Omega_SR,N := ( P_0 tensor |r><r|^(tensor N)
              + P_ch tensor |p_Q><p_Q|^(tensor N) ) / Tr_A(I_src)
```

a positive, normalized, charge-superselected **joint** state on the source-record face
`A_SR = A_src graded-tensor_min R_inf` (typing at member 13 `:126-128`), restriction-compatible in
`N` by member 05's own identity, hence a quasi-local state in the direct limit — member 05 `:88-106`
re-establishes the macroscopic central-sequence theorem *for the incidence-derived states rather
than importing it* from the retired construction.

### 1.5 THE COMPOSITION THAT FAILS, at its span

Member 13 `:126-128` types the completed algebra as `A_C0 = A_SR graded-tensor_min B` with
`B = A_F_CTP iso C(Y)`. **`Omega_SR,N` is a state on the first tensor factor only.** Member 13
`:213-218` gives the restriction `res_B : State(A_C0) -> State(B)`, `res_B(Omega) := Omega compose i_B`,
exact — positivity from `i_B` being a `*`-homomorphism, normalization from `i_B(1_B)=1_A_C0` — and
its input is precisely what the build does not reach.

**MISSING SURFACE OBJECT — named, typed, located. A physical ingredient, not a schema:**

```text
NAME:  the source-history attachment (coupling) map
TYPE:  Attach : (connection/source history, finite incidence, U_N^0) -> U_N[A,J,g],
       together with its representation, carrier embeddings, orientation law, time-ordering,
       and common-origin certificate
LOCUS: member 12, :314-334   ("No such Attach exists"; SEALED_ATTACH_MAP_EXISTS = false | TYPE-S)
```

This is the ingredient that would let the field/CTP sector `B` be acted on at all. Without it the
composed dynamics is `U_N^0` — history-independent — and the `B` factor is inert. Member 12 `:340-354`
records the consequence directly: the trivial family `U_N[A]=U_N^0` *would preassign zero field
response* and is not derived by the finite producer.

---

## 2. STEP 2 — WHAT THE OBJECT IS

### 2.1 Carrier and sectors [forced]

Carrier `H_src^A tensor (C^3)^(tensor N)`, with `H_src^A = P_0 H_src direct-sum P_ch H_src`
(member 08 `:352-363`) and each record cell `span{|r>,|p_Q>,|e_Q>}` (member 05 `:33-38`).
Sectors: **source | record**, exactly the two tensor factors of `A_SR`. The third sector of the
record action's own complete specification `Q_spec[g,a,psi,R]` (member 04 `:83-95`) — the field
sector — is present in the *algebra* as `B` and **absent from the built state**.

### 2.2 Generator and dynamics [forced]

`exp(-i tau_R B_j)` per cell, profile-independent and order-independent (member 06 `:16-48`);
globally the controlled write `P_0 tensor I + P_ch tensor W_N[a]` (member 08 `:650-654`), which at
zero history is `P_0 tensor I + P_ch tensor tensor_j S_j`.

### 2.3 Stationary structure, and exactly what it extends to [forced]

`d_state`'s fixed point is `omega_tr`: unique, and **family-invariant** — identical across
{BI,DB,SYM}, which is precisely why no member is bound (member 10 `:9-13`; member 08 `:432-452`).

**It extends to the joint object, and this is the build's substantive result.**

```text
SOURCE MARGINAL of Omega_SR,N = (P_0 + P_ch)/Tr_A(I_src) = omega_tr.
```

The joint state's source marginal returns the preparation channel's own unique fixed state. The
extension is **consistent**, not merely compatible: the channel's fixed point survives being written
onto `N` record cells.

```text
RECORD MARGINAL of Omega_SR,N
  = (1 - kappa_ch) |r><r|^(tensor N) + kappa_ch |p_Q><p_Q|^(tensor N),

  kappa_ch = Tr_A(P_ch)/Tr_A(I_src).
```

**Both marginals are forced — and "forced" here carries its conditioning, not a bare claim:**

```text
BOTH_MARGINALS_FORCED = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013 (family level, no member bound), and A0's authored
            finite scalar source realization, whose price is disclosed at §1.3 and member 08 :896-906.
  scope:    the source-record face A_SR at every finite N, and its restriction-compatible limit.
  NOT claimed: forcing at the completed object, or forcing independent of A0.
```

The record marginal is a classical two-point mixture of orthogonal pointer states, and **its mixing
weight is exactly the symbolic sector-dimension ratio `kappa_ch` that member 08 `:816-826` derives on
the source side.** The same symbolic object is reached from the record side, as a marginal weight, by
a different route. That is a genuine cross-check and it is new here — though note what it is and is
not: it is a consistency result **inside** the authored-anchor premises, not an independent
confirmation of them. Two routes through the same premise set agreeing is worth recording and is not
worth more than that.

**No value is computed.** `kappa_ch` remains a ratio of symbolic dimensions; no dimension is
selected, evaluated, or compared to anything.

**Do not flatten.** The record marginal is a state on `R_N` inside `R_inf`. It is **not** a state on
`B = C(Y)`. The record/history distinction is the corpus's own (member 13 `:148-167`, `:271-274`),
and collapsing it is exactly the error that would manufacture a result here.

### 2.4 Common-origin content — what "same microscopic source" is MADE TRUE BY, at bytes

The target clause is P5 (member 14 `:251-253`): *"A positive normalized `rho_pre` on the completed
object and admitted record effects `E_r`, with domains, all supplied by the same microscopic source
as P0."*

**WHAT IS NOW MADE TRUE, at bytes.** On the source-record face the state, the ready ray and the
write all descend from **one frozen tuple** — `Omega_prim,N^v003` (member 08 `:609-619`) — whose
freeze order `T0..T8` (member 08 `:662-671`) puts carrier, family and certificates before any
output. Concretely: `omega_tr` from `d_state`; `|r>^(tensor N)` from `d_ready`; the controlled write
from `d_law`; the cell carrier and the edge entry from the same incidence operator that supplies
`B_ch`. **One tuple, three maps, one cell operator** — not a product assembled by declaration. On
this face, P5's common-origin clause is satisfied by construction rather than by assertion.

**WHAT IS NOT MADE TRUE.** P5 says *on the completed object*, and the completed object is
source-record-**field** (member 14 `:233-263`, P1). The built state lives on `A_SR`, which is `A_C0`
minus its `B` factor. P5 further demands *admitted record effects `E_r`, with domains*: the build
supplies pointer observables and a central sequence and supplies **no effect/domain package**.
Member 15 records the reason this was always going to bite — P5 *sits at the join*, and its
state/effects/domains must live on the completed object while the same microscopic source supplies
the dynamics; the domain object is not supplied.

```text
COMMON_ORIGIN = MADE-TRUE-BY (bytes) ON THE SOURCE-RECORD FACE
              = NOT-REACHED at the completed object
```

### 2.5 Where the surface itself goes permissive — the decisive display

Fix everything the surface forces. **The `B` direction is still completely free, and the surface
says so in its own sealed words.** Member 13 `:260-269`:

> every `omega_B` in `State(B)` produces a product state `Omega_(out,B) := omega_out tensor omega_B`
> … with the same record marginal `omega_out`. Since the sealed history-state simplex is a
> continuum, the record marginal alone admits a continuum of such extensions. This is an algebraic
> non-uniqueness theorem at the current receiver.

And member 11's survivor table `:480-487` — raw positivity, inclusive normalization, U1 reality,
one-cell C0-scope restriction, weak `rho_pre` type-only existence — **returns "continuum" on every
row**. Each constraint is failure-capable; each one passes for all. Nothing is minimal; the
admissible set is upward-closed.

The attachment side has the same shape: member 12 `:356-360` gives
`NAMED_ATTACHMENT_PLACEMENTS_COUNT = at_least_4` with `ATTACHMENT_FAMILY_COVERAGE_PROVED = false` —
a displayed family, unbounded above, with no selector.

**THE FINDING.** The surface reproduces the form-first route's shape. It was never that the form was
the wrong instrument. **The physics is permissive at this joint, and the form was faithfully
reporting that.**

---

## 3. STEP 3 — THE EXHIBITION

*Opened now, as a checklist, for the first time in this relay.*

Member 16 `:601-613` gives the nine fields:

```text
PhysicalSig_0 := ( PhysicalCategory_0, PhysicalObjectType_0, PhysicalArityAndOperations_0,
                   PhysicalOperationDomains_0, PhysicalOperationCodomains_0,
                   PhysicalParameterRecord_0, PhysicalSupportAndCommonCore_0,
                   PhysicalRepresentationData_0,
                   PhysicalAlgebraicCausalAnalyticConditions_0 )
```

### 3.1 The ruling standard, declared before any field is ruled

`PhysicalSig_0` types **`Obj_0`, the completed source-record-field object** (member 16 `:625-631`).
Therefore:

- **EXHIBITED** — the construction determines the entry *for the object the form types*.
- **NOT-DETERMINED-BY-THE-BUILD** — the construction determines it only on a proper sector, or not
  at all. The sector-restricted fact is stated in full, so the reach is visible and a later relay
  can see exactly what it would inherit.
- **N/A** — the build stopped upstream of the field's subject matter entirely.

I state this standard first because the alternative — counting a source-record-face determination as
an entry for the completed object — is precisely the flattening that would turn a partial build into
a false result. A fact true on one tensor factor is not an entry for the whole.

### 3.2 The nine

| # | Field | Verdict | What the build determines, and what stays open |
|---:|---|---|---|
| 1 | `PhysicalCategory_0` | **NOT-DETERMINED** | Determined on the SR face: a graded unital C\*-algebra with a state and a unitarily implemented write. Open for `Obj_0`: member 14 `:28-46` defines the physical category as the **common-origin CTP producer** class, whose membership test T7 (`:405-417`) turns on P4–P7 — and expressly warns that a pure algebraic join *passes an algebraic relation test while still failing the producer obligation*. The build supplies the carrier category and none of the producer package. |
| 2 | `PhysicalObjectType_0` | **NOT-DETERMINED** | Determined on the SR face: a state-plus-unitary-write pair on `A_SR`. Open for `Obj_0`: member 16 `:614-616` expressly refuses the union *operator OR functional OR flow OR generator* as a specification. What the build adds is a **constraint, not a determination** — any `Obj_0` must restrict on `A_SR` to what §1.4 built. That is a lower bound on the type, which is the same monotone shape the section already recorded. |
| 3 | `PhysicalArityAndOperations_0` | **NOT-DETERMINED** | Determined on the SR face, and sharply: the exact composition law of the write (member 05 `:59-70`), its per-cell unary action, the cross-cell commutation `[B_j,B_k]=0`, and the record trace-out that returns `rho_(N-1)` identically. Open: the operations that consume the field sector — which is to say the missing `Attach` (§1.5). |
| 4 | `PhysicalOperationDomains_0` | **NOT-DETERMINED** | Determined on the SR face and **trivially discharged there**: A0's authoring condition `dim(H_src^A)<infinity` (member 08 `:352-363`) plus `(C^3)^(tensor N)` makes every operator bounded and everywhere defined, so the common core is the whole space; member 08 `:794-798` confirms every datum is finite and *no tail-visible primitive is introduced*. Open: the field sector's domains. Member 15 records the domain object as not supplied and **straddling** both sides. |
| 5 | `PhysicalOperationCodomains_0` | **NOT-DETERMINED** | Same reach and same limit as #4: codomains are determined wherever the operations are (states to states, record algebra to record algebra), and undetermined exactly where the operations are missing. |
| 6 | `PhysicalParameterRecord_0` | **NOT-DETERMINED** | The build determines the parameter record **of the built object** and, informatively, that record is **not a point but a family**: `kappa_ch` symbolic; the channel family's transient parameter `lambda` free (member 08 `:707-714` — *the residual `lambda` family changes transient dynamics but not the generated state*); the character label `n` unselected, with member 12 `:280-290` showing Gate 4 returns the same result for every fixed `n` and the faithful subfamily having two members. Open: everything the field/CTP package would contribute. |
| 7 | `PhysicalSupportAndCommonCore_0` | **NOT-DETERMINED** | **The field the build came closest to.** Support is forced and computed: the write on cell `j` is supported on cell `j`, later pulses commute exactly with earlier public-record observables at all intermediate times (member 06 `:50-56`), and the endpoint is independent of the causal linear extension. Common core is forced on the SR face by §2.4 — one frozen tuple supplies state, ready and law. It falls short for one reason only: P5 requires this **on the completed object**, and the field sector is absent. |
| 8 | `PhysicalRepresentationData_0` | **NOT-DETERMINED** | Here the build surfaces an **obstruction on its own face**, not merely a gap. It yields finite-dimensional representations at each `N` and a GNS for the quasi-local state — and member 05 `:108-122` then shows that if the stationary incidence parent acts on every completed cell, the `N`-cell overlap tends to zero, so *the stationary evolution moves the endpoint product state into a disjoint infinite-product sector at generic nonzero time* and is not implemented in that GNS. The representation data is therefore not closed even where the build reached. |
| 9 | `PhysicalAlgebraicCausalAnalyticConditions_0` | **NOT-DETERMINED** | Splits three ways, and the split is the useful part. **Algebraic:** forced — charge superselection (§1.3, an identity), orthogonal projectors, graded tensor structure. **Causal:** forced — cell-local support, commuting generators, linear-extension independence (member 06 `:16-56`) — but conditional on the one-use moving-front form, which member 06 `:77-90` says the live parent authority *has not yet derived*. **Analytic:** not reached — member 07 `:36-41` gives `physical_write_tail_join_derived = false` at its own locus, and member 05 `:124-128` records the continuum-ordering and connected-preparation gaps. |

```text
EXHIBITION = 0/9/0 of 9   (exhibited / not-determined-by-the-build / N-A)
```

### 3.3 What the zero means — and why it is not the same zero

The count matches the tally the section already carried. **The content does not.**

The form-first route reached 0/9 by finding that every sealed constraint on the nine fields is a
lower bound: nine fields, nine permissive constraints, no minimality principle anywhere.

The surface-first route reaches 0/9 **for one reason, nine times.** Every field above is determined
on the source-record face and undetermined beyond it, and in every case the boundary falls at the
same place: the `B` tensor factor, unreachable because one physical ingredient — the `Attach` map of
§1.5 — does not exist. Fields 8 and 9 add a second, sharper datum: even inside the reached sector,
the representation is not closed against a stationary parent, and the analytic conditions are not
reached at all.

**That is the advance.** The section did not have nine independent gaps. It has one missing physical
ingredient and one inert tensor factor, and the nine-fold failure is a shadow of that single fact.
A route that had merely re-derived 0/9 would have told us nothing new; localising the whole of it to
one named, typed, located object is what this route buys.

---

## 4. STEP 4 — THE TERMINAL DISPLAY

*Terminal for the section's open question by standing rule. Written to be stood on.*

**The branch is SAME-WALL, and the wall is not where the section was looking.**

The surface's own sealed description goes permissive at the decisive point, and the exact span is:

```text
member 13, :260-269

  "every omega_B in State(B) produces a product state Omega_(out,B) := omega_out tensor omega_B
   ... with the same record marginal omega_out.  Since the sealed history-state simplex is a
   continuum, the record marginal alone admits a continuum of such extensions.  This is an
   algebraic non-uniqueness theorem at the current receiver."

corroborating span: member 11, :480-487 — the survivor table returns "continuum" on every row.
corroborating span: member 12, :356-360 — placements at_least_4, coverage NOT PROVED.
```

Read against §1.4, that span now says something sharper than it could before this relay. Before, it
said *a record state has many extensions*. Now the record state is not a hypothesis — it has been
**constructed**, from one frozen tuple, with both marginals forced. And the theorem still applies to
it unchanged. **The surface forces the source-record marginal completely and the field marginal not
at all.** The freedom is not diffuse; it is exactly one tensor factor wide.

Three consequences, stated plainly:

1. **The build is a real partial object, not a failed attempt.** `Omega_SR,N` is forced, unique
   given the family, family-invariant, restriction-compatible, and common-origin on its own face.
   Nothing about it is chosen.

2. **The wall is a missing physical ingredient, not a missing form entry.** The `Attach` map of
   §1.5 is the object whose absence makes `B` inert. It is named, typed and located. This is the
   honest shape of the residual ask, and it is smaller and more specific than "select a signature".

3. **The permissiveness is structural, and both routes see the same structure.** The form-first
   route saw an unbounded schema shape-space; the surface-first route sees an unbounded attachment
   family and a continuum of `B`-extensions. These are the same fact seen from two sides. The
   section's exhaustion of the form-first route was therefore correct and is not repaired by
   building from the surface — but the surface route localises the obstruction, which the form
   route could not.

**What this display does NOT say.** It does not say the joint object is impossible; member 11
`:464-468` warns expressly that calling the unbuilt subfamily empty converts missing construction
into physical refutation, and DoR-006 forbids that. It does not bind a member, evaluate `kappa_ch`,
select an `omega_phys`, or lift any stop. It does not impeach the form-first result; it explains it.

```text
TERMINAL = SAME-WALL
  span: member 13 :260-269 (with member 11 :480-487 and member 12 :356-360)
  character: the surface forces the source-record marginal completely and the field marginal not at
  all; the residual freedom is exactly one tensor factor wide, and its cause is one missing
  physical ingredient, named/typed/located at member 12 :314-334.
```

---

## 5. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN (A0 and the anchor family, via DoR-013, family level)
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 04's record-action premises and its own authorization board; member 05's endpoint unitary,
  arbitrary-N completed-state theorem, central-sequence result and disjointness finding; member 06's
  profile/order independence and its conditionality; member 07's non-derivation of the write/tail
  join; member 08's A0, family, three maps, frozen tuple and freeze order, all read under members
  09 and 10 with the no-member clause carried; member 11's B iso C(Y) and continuum survivor table;
  member 12's attachment adjudication and placement family; member 13's i_R, i_B, res_B, E_out and
  non-uniqueness theorem; member 14's P0–P7 surface content and producer-class test; member 15's
  P5 straddle; member 16's nine fields, opened in Step 3 only; all charter fences.

DERIVED HERE:
  the mating of Gen_Omega's d_state with R3.4's completed-state theorem, and the identity showing
    the charge-superselection hypothesis discharges itself on omega_tr;
  the joint object Omega_SR,N and its two forced marginals;
  the identification of the record marginal's mixing weight with the source-side symbolic ratio;
  the localisation of all nine field failures to one tensor factor and one missing ingredient;
  the reading of member 13's non-uniqueness theorem against a CONSTRUCTED rather than hypothesised
    record state.

SELECTED HERE:
  nothing.  No member of the origin family, no omega_B, no Omega_C0, no rho_pre, no scalarization,
  no character label, no attachment placement, no dimension, no scale, no selector, no signature,
  no candidate and no status beyond what members 09/10 already carry.  Nothing is adopted,
  re-adopted, ratified, promoted or frozen.  No stop is lifted.  No decline is filled.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

---

## 6. FLATTENING, ANCHORS, AUDITS, CUSTODY

### 6.1 S01–S37 FLATTENING CHECK — walked, clean

- A **tensor-factor relation** was not flattened into a **state factorization** — member 13 `:144-146`
  states that limit expressly, and §1.5/§2.5 keep `Omega_SR,N` on `A_SR` rather than presenting it
  as a state on `A_C0`.
- A **record state** was not flattened into a **history state**. §2.3 says so in terms: the record
  marginal lives on `R_N` inside `R_inf`, not on `B = C(Y)`.
- A **sector-restricted determination** was not flattened into a **form entry**. This is the whole
  content of the §3.1 ruling standard, declared before any field was ruled.
- A **family** was not flattened into a **member**. The no-member clause is carried at §1.2 and the
  family-invariance of `omega_tr` is what makes the composition lawful without selection.
- A **characterization** was not flattened into an **execution**. `d_state` is cited, not iterated.
- A **symbolic ratio** was not flattened into a **value**. `kappa_ch` is carried as a ratio of
  symbolic dimensions throughout and is never evaluated or compared.
- A **silence** was not flattened into a **choice**. §1.1's edge-entry detail is declared, with the
  sealed determination that fills it cited at its own locus.
- A **partial build** was not flattened into a **result**, and equally a **partial build** was not
  flattened into a **failure**. §4 states both halves.
- **Missing construction** was not flattened into **physical refutation** (member 11 `:464-468`).

### 6.2 PROSE-DIGEST AUDIT — strict and stable

```text
LIVE MEMBERS DECLARED: NONE.
  No register, tracker, or live-append file is in the verdict-bearing set.  The state brief
  (member 03) is versioned, not live, and is pinned at the digest relay 1049 named.

STRICT MODE  = every cited member rehashed at its FULL archive-root path at seal time.
STABLE MODE  = the digest pinned in the closure table.

RESULT: 18/18, STRICT == STABLE.
```

Because no live member carries verdict weight here, the two modes coincide **by construction rather
than by live-append tolerance**. No tolerance was invoked.

**The generator's refusal path was tested, not asserted.** A generator that claims to refuse
ungrounded emission and has never been shown to refuse is a claim about a code path, not a
certificate. One pinned digest was altered by a single character in a scratch copy and the
comparison re-run: the mismatch was detected and emission would have been blocked, naming the
member. The generator is also **idempotent** — a second run reproduces the same closure byte and the
same seal, so the fixed point is a fixed point and not an artefact of running once.

### 6.3 AMBIGUOUS-ANCHOR REFUSAL — the trap, tested rather than assumed

The bootstrap warns that a flag name is never a unique anchor and that the trap has fired eight
times. I tested the three flag names this artifact discusses:

```text
unique_causal_record_cell_derived   ->  7 distinct file loci
complete_parent_action_derived      -> 10 distinct file loci
physical_write_tail_join_derived    -> 10 distinct file loci
```

**No flag name is used as an anchor anywhere in this artifact.** Every claim resolves to a member
and a line locus. Where a flag's content is relied on (§3.2 row 9, §4), the member and locus are
given and the flag name appears only as the quoted content at that locus.

### 6.4 RESIDUE — from the scan, not predicted

```text
27-token predeclaration output scan over authored prose:  0 hits
  scope: the artifact minus its fenced blocks and its content-addressed table rows.
  Fenced blocks are excluded because they carry the record's own quoted bytes, and a documentary
  gate is not passed by editing a governing quotation.  The scan and its 27 tokens are declared in
  the sealed generator, so the token list is auditable rather than asserted.
```

The count above is the scan's output, run over this artifact's authored prose before sealing. It is
reported as found; nothing was swept to make it come out at a particular number.

### 6.5 Custody

- **BUILDER NEVER VERIFIES OWN WORK.** Every finding here is **CLAIMED** until the Codex 2 check.
  I did not re-adjudicate any cited member's rulings; I composed their outputs.
- **NO SUBAGENT DELEGATION** (member 02 `:15`). The whole relay was executed in one lane.
- **SELF-CITATION BAR.** No DARIO artifact is cited as a record witness. Prior DARIO outputs were
  not used as grounds for any claim in §1–§4.
- **WRITES:** one artifact in `workspace/` plus its sidecar and generator; ACK and DONE in
  `relay_outbox/`. **No register, plan, tracker, or git action of any kind, read-only included.**
- **CLEANROOM:** not written; not read in this relay (every subject resolved inside the archive).
- **PE-1..PE-17:** pointer-only, zero verdict weight. The ledger was not opened.
- **omega_phys:** untouched. **No member bound.** **No freeze.**

---

## 7. THE CERTIFICATE

Per Q-913 and Q-917 the certificate **is** the digest table: the eighteen members at their full
archive-root paths, rehashed at seal time, together with the pre-form Step-1/2 digest
`73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31` that fixes the quarantine.
There is nothing below it.

---

## 8. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00005828 computed as a fixed point on bytes; 18 members;
     0 live; scan reported at §6.4 from its own run)

BUILD = PARTIAL
     COMPOSED: the record action's cell carrier and write geometry (member 04 :70-82, member 05
       :17-46, member 06 :16-56) with Gen_Omega's A0, channel FAMILY and three maps (member 08
       :352-363, :401-430, :628-654, read under members 09/10) into the joint source-record state
       Omega_SR,N of §1.4 — the mating hypothesis DISCHARGING ITSELF as an identity (§1.3), not
       assumed.
     FAILED: the field/CTP factor.  Omega_SR,N is a state on A_SR; A_C0 = A_SR tensor B
       (member 13 :126-128); res_B is exact (member 13 :213-218) and its input is unreached.
     SPAN OF THE FAILURE: member 12 :314-334.

COMMON_ORIGIN = MADE-TRUE-BY (bytes) on the source-record face — one frozen tuple supplies state,
     ready and law (member 08 :609-619, :662-671; §2.4) — and NOT-REACHED at the completed object,
     which P5 requires (member 14 :251-253; member 15's straddle).

EXHIBITION = 0/9/0 of 9 (exhibited / not-determined-by-the-build / N-A).
     Each of the nine is determined on the source-record face and undetermined beyond it, and in
     every case the boundary is the SAME boundary.  The nine do not fail nine times; they fail once.
     Fields 8 and 9 add that even inside the reached sector the representation is not closed against
     a stationary parent (member 05 :108-122) and the analytic conditions are not reached
     (member 07 :36-41).

TERMINAL = SAME-WALL
     span: member 13 :260-269; corroborated at member 11 :480-487 and member 12 :356-360.
     The surface forces the source-record marginal completely and the field marginal not at all.
     The residual freedom is exactly one tensor factor wide and its cause is one missing physical
     ingredient — the Attach map, named/typed/located at member 12 :314-334.

FORM_QUARANTINE = HELD-UNTIL-STEP-3
     method: exclusion-by-path list recorded before reading; Steps 1–2 written, closed and hashed
     (73ef21bd…) BEFORE member 16 was opened; one adjacent-schema encounter (StatePort_U2_008's
     seven fields, inside member 11's own quotation) disclosed at §0.1 rather than concealed.

CASCADE_STOPS = NONE
PROSE_DIGESTS = 18/18, STRICT==STABLE (0 live members; no tolerance invoked)
OUTPUT_INSPECTION = NONE-CERTIFIED
CHAIN_INVOKED = false (no RELAY_PASTE_1051+ present in relay_inbox at seal time; chained pickup
     attempted after 1050_DONE and found no successor — reported, not assumed)
VERB_AUDIT_SELF = CLEAN (+1 self-corrected before sealing: §2.3 initially said "both marginals are
     forced" with no TYPE-P conditioning, which would have read as forcing independent of A0.  The
     conditioning block and its scope/NOT-claimed lines were added.  Recorded because the audit
     catching something is the audit working, and a silently repaired overclaim is still an overclaim
     that once passed.)

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

All findings **CLAIMED** until the opposite-lane check.

**The step to press first.** Not the exhibition — the mating. §1.3 is the load-bearing step of this
artifact and the one I would attack first if it were someone else's. The obvious attack is whether
member 05's `P_0,P_ch` and A0's `P_0,P_ch` are the same projectors or merely the same letters. **I
ran that attack myself and it fails** — both take the pair from the same ratified source-coupled
result, member 08 cites that result for them explicitly, and member 18 `:35` fixes `P_ch` as the
total-charge spectral projector. The identity `P_0 omega_tr P_0 + P_ch omega_tr P_ch = omega_tr` then
follows from orthogonality and completeness alone.

**So the sharper thing to press is the one I could not close: A0's finiteness.** The whole
composition inherits `dim(H_src^A) < infinity` as authored premise. Member 08's own self-kill
(`:896-906`) says A0 dies if the physical source port is necessarily the unscalarized full module —
and `kappa_ch` is a ratio of dimensions of exactly that carrier. **If A0 fails, `Omega_SR,N` does not
merely lose a certificate; its charge weight stops being a ratio of finite dimensions and §2.3's
cross-check dissolves.** That dependency is disclosed rather than distributed, it is the same price
DoR-013 knowingly ratified, and it is where I would send a hostile reader first.

A second, cheaper check for the opposite lane: §3.1's ruling standard is the hinge of the whole
exhibition. If the check judges that a source-record-face determination *should* count as an entry
for `Obj_0`, the tally moves and my §3.3 reading with it. I ruled it should not, and gave the reason
before ruling any field — but that is a judgement, not a computation, and it is the right place to
disagree with me.
