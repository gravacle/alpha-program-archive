# STAGE 8 — THE REQUIRE-TARGET, POSED: THE ONE OBJECT THAT CLOSES R_record,L's FACTORIZATION FORK
## BLIND SCOPING / POSING ONLY — REQUIRE-POSE — [CLAIMED]

Date: 2026-08-13
Role: TYPING / POSING ONLY. This artifact does NOT answer the forced/free question.
It pins, OF RECORD, the single unbuilt object whose construction settles it, states
the minimal obligation that decides it, tests whether that object coincides with the
program's other named require-targets, types the built-vs-gap map, and guards the
posing against the three forbidden imports. No value of R_record,L, n, kappa, alpha,
any scale, length, coefficient, spectrum, or constant is computed, bounded, estimated,
or compared. Every symbol is symbolic; every numeric string is a sealed-text quotation
or an exact structural integer. Factorization is NOT decided; the winding is NOT
decided; R_record,L is NOT computed or bounded.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

Forbidden imports, held and audited throughout: NO SCALE (no ell_P, fiber radius,
metric length, K_KK, c^2); NO FAITHFULNESS AS A PREMISE (must be DERIVED if it
appears; the |n|=1-via-faithfulness route is BARRED); NO IMPORTED GR (no metric g, KK
ansatz, Einstein-Hilbert, quasilocal energy — GR's boundary structure must be DERIVED
from the record surface). No register / tracker / plan / road / ledger / lens read.

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Full digests
recomputed by `shasum -a 256` at path this relay before reading; first 16 hex shown.

| Tag | Source | Seal (first 16) | Role in this posing |
|---|---|---|---|
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b` MATCHES-TASKED | the derivation that closes R_record,L to `n^2 Phi^T C Phi` and names the Gate-5 kernel; §0, D5, D6, §4, flag |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d4904706276` SEALED-OK | the bedrock: `Gamma_K = sum_(connected C) gamma_C`; "the connected thermodynamic limit remains a Gate-5 obligation"; the Duhamel-reproduction constraint; the tangent complex |
| T1 | `STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md` | `e6fae1428cecffb3` MATCHES-FORM | the boundary/overdetermination artifact; the void overdetermination (holds for EVERY winding); U1–U6 unbuilt list separating the conversion (U2) from the R_record,L form (U4) |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03` SEALED-OK | the RATIFIED finite-N law `F_N = P_0 + Z_N P_ch`; connection/holonomy-only; source operator |
| M06 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac5` SEALED-OK | scalarization unbuilt: canonical-scalar-from-sandwich = false TYPE-R; rho / state-port TYPE-U |
| ECO | `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md` | `0f3082cab910f2eb` SEALED-OK | R-L2b as ONE estimate carrying FOUR obligations; obligation 4 = CONNECTED EXTENSIVITY = convergence of the connected cumulant expansion uniformly as cells shrink |
| RFA | `STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md` | `2ede02aea415157a` SEALED-OK | R-L2b's object `X_n = C_n(V_n(a)-V_n(0))C_n`; the Schatten-2 exponent target; cluster-summable aggregation iff the exponent condition holds |
| R4O | `STAGE8_RL2B_REFUTING_BRANCH_FOUR_OBLIGATION_ANALYSIS_PROPOSED_V001.md` | `f6fe40724ff4dfeb` SEALED-OK | the four-obligation binding; `connected_extensivity = UNRESOLVED_BY_RL2B_REFUTING_BRANCH` |
| XSM | `STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md` | `3c008ecccc2b01ac` SEALED-OK | the internal/external metric conversion of a record cell: Fubini-Study(internal) vs Lorentzian-diamond(external), `ell_P`, `G_4`, factor `beta` |

No register/tracker/plan/road/ledger/lens read. No git action. Output name probed
before write: ABSENT.

---

## 2. DELIVERABLE 1 — THE ONE OBJECT

**Pinned, of record: the Gate-5 connected linked-cluster kernel `C`** — the intensive
post-thermodynamic-limit Duhamel/cluster covariance kernel of the transported per-cell
write generators in the base record state, the single unbuilt object to which the
FORM's full closure and the post-limit arbitration of the cross block both reduce.

The artifact's own naming, quoted at the bytes:

```text
FORM §0 (:24-31): "R_record,L = n^2 · Phi^T C Phi ... with ... C the intensive
  post-limit Duhamel/cluster kernel — closing everything except three named objects:
  the Gate-5 connected linked-cluster kernel C ..."
FORM D5 (:263-265): "The FORM therefore closes to n^2 Phi^T C Phi with every factor
  typed, and does not close further; the exact missing pieces are named in D6."
FORM D6/G1 (:270-272): "the kernel C — the Gate-5 connected linked-cluster limit of
  the doubled record functional (sealed as an obligation, unsealed as an object)."
FORM flag IMPLICATION (:530-531): "... the form's full closure to ONE unbuilt object
  — the Gate-5 cluster kernel (G1) with the scalarization port (G2) as its consumer."
V011 (quoted at FORM :81-82): "The disjoint theorem is proved; the connected
  thermodynamic limit remains a Gate-5 obligation."
```

**What it is, precisely.** `C = (C_jk)` is the symmetric, positive-semidefinite,
intensive (per `N_4`), n-independent bilinear kernel obtained as the connected
linked-cluster thermodynamic limit of the doubled (CTP) record functional's Duhamel
covariance of the per-cell write generators, required to be cluster-summable with
subextensive boundary corrections (FORM D5 :248-252, on V011's B2 locality clause).
It is the post-limit replacement for the coherent finite-N all-pairs weight
`C_jk^(finite) = mu` for all `j,k`, which is NOT cluster-summable and therefore cannot
itself be the post-limit form (FORM D5 :254-262). Its construction is what makes
`R_record,L = n^2 Phi^T C Phi` a closed object rather than a typed template.

**Two distinct "ones", not to be flattened (FORM IMPLICATION :528-531).** Of record
the FORM separates:
- the FORM's FULL CLOSURE reduces to ONE unbuilt OBJECT = the Gate-5 kernel `C` (G1),
  with the scalarization port (G2) as its consumer;
- the FACTORIZATION FORK's finite-N anchor reduces to ONE unsealed DATUM = the write
  chains' block decomposition (G3), i.e. whether the write direction has nonzero
  components in BOTH sector blocks;
- at POST-LIMIT strength the cross-block verdict ALSO consumes `C` on the cross
  pairing `Phi_f^T C Phi_H` (FORM §4.4 :354-356; flag FACTORIZES :486-489).

So the ONE OBJECT that closes the fork AT POST-LIMIT STRENGTH — the arbiter of
"cross-block present-or-removable" — is the Gate-5 kernel `C`; the finite-N anchor of
the same fork additionally needs the write-chain datum G3. Both are pinned below; the
posing does not conflate object (C) with datum (G3).

---

## 3. DELIVERABLE 2 — THE MINIMAL FORCED/FREE OBLIGATION

**The smallest well-posed proposition whose discharge settles the fork** (stated as a
TARGET, NOT answered), on the derived block display
`R_record,L = n^2 [[Phi_f^T C Phi_f, Phi_f^T C Phi_H],[Phi_H^T C Phi_f, Phi_H^T C Phi_H]]`
over `[flux = im(d_1^dagger) | H = dim-32 flux-free physical block]`
(FORM §4.2 :310-313):

```text
PROP(cross):  Phi_f^T C Phi_H  !=  0.
  (equivalently, at the finite-N anchor, the rank-one cross term
   n^2 mu · phi_f ⊗ phi_H is nonzero — FORM §4.2 :315, :330.)
```

Because the finite-N anchor is rank one, the FORM has already decided (PROVABLE, no
value used) that there is NO THIRD CASE (FORM §4.2 :316-331): a rank-one form is never
a direct sum of two nonzero blocks, and a nonzero cross block survives every
sector-respecting (block-diagonal congruence) basis change. Hence the target is a
clean binary.

```text
FORCED_CRITERION (NON-factorization; gravity/entangled branch, per the lens — NOT
  used as input):  PROP(cross) HOLDS. Concretely three symbolic conjuncts:
    (F-i)   phi_f != 0                 — write chains reach the flux block im(d_1^dagger)
    (F-ii)  phi_H != 0                 — write chains reach the dim-32 flux-free block H
    (F-iii) Phi_f^T C Phi_H != 0       — the Gate-5 kernel C does NOT annihilate the
                                          cross pairing (post-limit strength)
  ⇒ irreducible rank-one cross term n^2 mu phi_f ⊗ phi_H that no sector-respecting
    basis removes; R_record,L DOES-NOT-FACTORIZE in the tasked sense.

FREE_CRITERION (factorization; decoupled/free branch):  PROP(cross) FAILS, i.e. ANY of
    (R-i)   phi_H = 0    (total write direction lies entirely in the flux sector), OR
    (R-ii)  phi_f = 0    (entirely in H), OR
    (R-iii) Phi_f^T C Phi_H = 0    (C annihilates the cross pairing at post-limit)
  ⇒ the form factorizes only DEGENERATELY: R = R_sector ⊕ 0, the other sector's
    response identically zero (FORM §4.2 :326-329; flag FACTORIZES :490-495).
```

Minimal deciding inputs, of record: (F-i)/(F-ii) [= (R-i)/(R-ii)] are the write-chain
block-decomposition datum **G3** (RA27-3 support/current-density, NOT SUPPLIED — FORM
D6 :278-282, §4.3 :334-343); (F-iii) [= (R-iii)] is a property of the **Gate-5 kernel
C** (G1) on the cross pairing. No sealed object forces either way today (FORM §4.3
:340-342). POSING ONLY: PROP(cross) is neither asserted nor denied here.

---

## 4. DELIVERABLE 3 — THE UNIFICATION, AT THE BYTES

Two named require-targets are tested against the Gate-5 kernel `C`.

### 4.1 R-L2b — SAME GATE (co-referential obligation), distinct ROLE

R-L2b's object is the uniform Schatten-2 bound on
`X_n = C_n(V_n(a) - V_n(0)) C_n` (RFA §6 :119-121, :161), i.e. "does the difference
kernel bound on a diamond" with a DERIVED scaling exponent. The campaign binds it to
four obligations, the fourth verbatim:

```text
ECO §2 (:86-95): "FOUR OBLIGATIONS ARE ONE ESTIMATE: 1. R-L2b itself ... 4. ***
  CONNECTED EXTENSIVITY *** — via §1.2: convergence of
  sum_n tau_R^n <B_K(A)^n>_connected / n! uniformly as cells shrink, with tau_R
  FIXED, depends entirely on the shrink rate of the connected cumulants, which IS
  R-L2b's exponent. ... IF R-L2b CLOSES, THE EXTENSIVITY QUESTION CLOSES WITH IT."
```

The Gate-5 kernel `C` exists iff exactly that connected linked-cluster limit clusters:

```text
FORM D5 (:254-262): the coherent finite-N kernel is "an ALL-PAIRS,
  separation-independent, rank-one kernel ... NOT cluster-summable ... the
  thermodynamic/linked-cluster limit must replace the all-pairs weight by a
  cluster-decaying C_jk — and V011 seals exactly this as the open obligation ('the
  connected thermodynamic limit remains a Gate-5 obligation')."
FORM D5 (:248-252): C typed "cluster-summable with subextensive boundary corrections
  (B2's sealed locality requirement on the connected expansion)."
```

**Verdict: SAME_OBJECT at the load-bearing gate, with a role caveat.** The defining,
load-bearing property of `C` (cluster-summability / intensivity of the connected
linked-cluster limit) IS connected extensivity IS the very proposition R-L2b's
exponent decides. They are the same obligation reached from two sides:
- OBJECT side (FORM): `C` is the constructed intensive covariance; it exists ⟺ the
  connected limit clusters;
- ESTIMATE side (ECO/RFA): R-L2b's Schatten-2 exponent on `X_n` IS the shrink rate of
  the connected cumulants that decides whether that limit clusters.

Caveat, kept explicit so the unification is not forced beyond record: `C` (a full
kernel) is broader than the single exponent bound R-L2b; R-L2b is the load-bearing
SUB-ESTIMATE that certifies `C`'s existence, not the entire kernel. And R-L2b is
currently OPEN and refuted-as-written on the H1/F'-5 chain, with
`connected_extensivity = UNRESOLVED_BY_RL2B_REFUTING_BRANCH` (R4O :139, :206-208) —
i.e. the shared gate is presently unmet by the only written campaign, and no sealed
replacement exists (ECO §4 :133-134). The identification is of the OBLIGATION, not of
a discharged result.

### 4.2 The internal/external metric conversion of a record cell — DISTINCT

XSM defines this object as the cross-sector metric rule on one record cell,
determining the dimensionless conversion factor `beta` between:

```text
XSM (:88-92): "internal side: Fubini-Study / phase / primitive record-degree geometry
              external side: Lorentzian causal-diamond / spacetime metric geometry"
XSM (:48-54): the FS metric fixes internal lengths "but ... does not fix that internal
  length relative to the public spacetime metric and G_4 ... does not fix their
  dimensional conversion relative to the spacetime metric or ell_P."
```

**Verdict: DISTINCT, at the bytes, on two independent grounds.**

1. **T1 lists them as SEPARATE unbuilt nodes.** T1's exact unbuilt census puts the
   conversion at U2 and the R_record,L form at U4:
   ```text
   T1 (:146-147): "U2  the internal/external conversion — booked a rail import; every
     derived junction is scale-blind and every scale-capable junction is
     adopted/unbuilt/gap"
   T1 (:152-153): "U4  the FORM of R_record,L on connection tangents — SPEC GAP ...
     the response has a definition and no form"
   ```
   The Gate-5 kernel `C` is the closure object of U4's SPEC GAP (FORM D6/G1
   :270-272). U2 and U4 are different rows of the same sealed census.

2. **Type mismatch — the conversion is SCALE/GR-metric-bearing; `C` is
   connection-only and scale-free.** XSM's conversion consumes `ell_P`, `G_4`, the
   Lorentzian spacetime metric, and yields the length-normalization factor `beta`
   (XSM :48-54, :80, :88-92) — precisely forbidden import #1 (SCALE) and the GR-metric
   side (forbidden import #3). The Gate-5 kernel `C` is defined on the RATIFIED
   connection/holonomy-only object with "no metric argument anywhere" (FORM B5, on
   M03/T3), is intensive/scale-free, and n-independent (FORM D5 :248-252). They cannot
   be the same object; identifying them would inject two forbidden imports into the
   target.

**Net:** the ONE OBJECT = Gate-5 kernel `C` is the SAME require-target as **R-L2b at
the gate level** (connected extensivity / cluster-summability), and is **DISTINCT**
from the internal/external metric conversion, which is a separate (U2), scale-and-GR
bearing require-target that must be fenced OUT of this posing (see §6).

---

## 5. DELIVERABLE 4 — BUILT vs GAP, TYPED

Typing key: TYPE-P ratified / TYPE-R unratified / TYPE-U undetermined-unbuilt.

```text
BUILT NODES (toward the Gate-5 kernel C and the fork):
 P1  Ratified finite-N influence functional F_N = P_0 + Z_N P_ch; connection/holonomy
     -only; record sandwich leaves a source OPERATOR.                       TYPE-P
     (M03 2cd1ffce; FORM B5 :96-105)
 P2  Disjoint-cell monoidal additivity of the modulus action (Hessian additive with
     no extensive factor) — "The disjoint theorem is proved."               TYPE-P
     (V011 B2, quoted FORM :74-82)
 P3  Tangent complex d_1 d_0 = 0 and the derived three-block split
     A^1 = im(d_0) ⊕ H ⊕ im(d_1^dagger), im(Q_flux) = im(d_1^dagger)
     (one line from the sealed complex).                                    TYPE-P
     (V011 B4, quoted FORM :90-94; FORM D1 :134-157)
 P4  Phi = (<ell_j,·>)_j as the ONLY connection dependence (exactly one character
     power per cell), and the uniform factor-out of the winding: V_L ∝ n, base objects
     n-blind since chi_n(identity)=1 — so C and Phi carry no n.             TYPE-P
     (FORM D2 :159-178; W character structure B6)
 P5  Two-branch necessity: single-branch charged amplitude identically zero, branch
     diagonal flat; the channel carried by a_Delta.                         TYPE-P
     (FORM D3 :180-199, on M03 B5)
 P6  The FORM's typing R_record,L = n^2 Phi^T C Phi with C typed symmetric, PSD,
     intensive, additive, n-independent (the CLOSURE TEMPLATE — the frame, not the
     kernel value).                                                         TYPE-P (typing)
     (FORM D5 :234-252; flag :467-479)
 P7  Void-overdetermination background: on FACTORS the boundary agreement holds for
     EVERY winding; on DOES-NOT-FACTOR no forcing exists today. (Confirms the interior
     "allows"; the fork is the live question.)                              TYPE-P
     (T1 :234-257)

GAP NODES (the genuine unbuilt targets):
 G1  The Gate-5 connected linked-cluster kernel C — THE ONE OBJECT. Sealed as an
     obligation, unsealed as an object; its cluster-summability = connected
     extensivity = R-L2b's exponent (§4.1), presently OPEN / refuted-as-written, no
     sealed replacement.                                                    TYPE-U
     (FORM D6/G1 :270-272, D5 :254-262; V011 :81-82; ECO §2 :86-95; R4O :139)
 G2  The scalarization / state-port pair (w_0, w_ch) — C's CONSUMER. Canonical scalar
     from the record sandwich alone = false; rho / U2-state-port not instantiated.
       CANONICAL_SCALAR_FROM_SANDWICH_ALONE = false                         TYPE-R
       RHO_PRE_PLACED / U2_STATE_PORT_INSTANTIATED = false                  TYPE-U
     (M06 d13920e2; FORM B8 :120-123, D6/G2 :274-276)
 G3  The write chains ell_j support/current-density realization — the FORK's finite-N
     deciding DATUM (whether phi_f != 0 AND phi_H != 0). RA27-3 support and current
     density NOT SUPPLIED.                                                   TYPE-U
     (FORM D6/G3 :278-282, §4.3 :334-343, on FR/CV B7)
```

Load-bearing ordering, of record (posing, not scheduling): the fork's finite-N anchor
is decided by **G3** alone; the fork's post-limit cross-block verdict and the FORM's
full closure are decided by **G1** (with **G2** as the scalarization consumer that
makes the finite-N anchor a scalar at all). G1 is the single object; G3 is the single
datum; G2 is the consumer.

---

## 6. DELIVERABLE 5 — CIRCULARITY GUARD

The posing above is built only from: the ratified connection/holonomy-only functional
(P1), the sealed disjoint additivity (P2), the sealed tangent complex (P3), the sealed
character law (P4), the sealed CTP branch identities (P5), and the sealed
Duhamel-reproduction/locality typing of `C` (P6). Audit against the three forbidden
imports:

```text
SCALE:        none enters the target. C is intensive (per N_4), connection-only, and
              n-independent; the fork criteria PROP(cross), phi_f, phi_H, Phi_f^T C
              Phi_H are all scale-free. tau_R appears only inside quoted sealed
              definitions (ECO/V011), never consumed as a length or magnitude here.
FAITHFULNESS: not used. The |n|=1-via-faithfulness route is BARRED and not consumed;
              the fork's forced/free criteria contain no injectivity premise. (If
              faithfulness ever appears it must be a DERIVED consequence — T1 :250-257
              records exactly this discipline.)
IMPORTED GR:  none. No metric g, KK ansatz, Einstein-Hilbert, or quasilocal energy in
              the target. The exterior/response sector im(d_1^dagger) is DERIVED from
              the sealed complex d_1 d_0 = 0 (P3), not imported. T3's one-argument
              import-block at g is left standing.

FLAGGED — a sealed source that DOES carry forbidden imports, correctly fenced OUT of
the target: XSM (the internal/external metric conversion, §4.2) consumes ell_P, G_4,
and the Lorentzian spacetime metric to fix the factor beta (XSM :48-54, :88-92). This
is a SCALE + GR-metric object. It is NOT part of the posing; it is the require-target
this artifact REFUTES unification with, precisely because pulling it into the target
would smuggle imports #1 and #3. The distinctness finding (§4.2) is therefore also the
circularity guard: the Gate-5 kernel C is posed entirely on connection-only, scale-
free content, and the scale/GR-bearing conversion is held at arm's length as a
DISTINCT node (T1's U2, not U4).
```

No source used to POSE the target smuggles a forbidden import into it. POSING ONLY:
nothing is decided, computed, or bounded.

---

## 7. FLAG BLOCK

```text
THE_ONE_OBJECT = the Gate-5 connected linked-cluster kernel C — the intensive,
  post-thermodynamic-limit, connection-only Duhamel/cluster covariance kernel of the
  transported per-cell write generators, to which the FORM's full closure and the
  post-limit cross-block arbitration both reduce. [FORM §0 :24-31; D6/G1 :270-272;
  IMPLICATION :530-531; V011 quoted FORM :81-82]

FORCED_CRITERION = PROP(cross) HOLDS: phi_f != 0 AND phi_H != 0 AND Phi_f^T C Phi_H
  != 0 — an irreducible rank-one cross term n^2 mu · phi_f ⊗ phi_H that no
  sector-respecting (block-diagonal congruence) basis removes; R_record,L
  DOES-NOT-FACTORIZE. [FORM §4.2 :315-331; flag FACTORIZES :486-495]

FREE_CRITERION = PROP(cross) FAILS: phi_H = 0 OR phi_f = 0 OR Phi_f^T C Phi_H = 0 —
  the form factorizes only degenerately, R = R_sector ⊕ 0 with the other sector's
  response identically zero. [FORM §4.2 :326-329; §4.4 :347-357]

UNIFICATION =
  SAME_OBJECT( R-L2b, at the gate: C's cluster-summability/intensivity IS connected
    extensivity IS R-L2b's Schatten-2 exponent on X_n = C_n(V_n(a)-V_n(0))C_n —
    FORM D5 :254-262 & V011 :81-82  <->  ECO §2 :86-95 & RFA :119-121,:161; ROLE
    CAVEAT: C is the constructed kernel, R-L2b its load-bearing existence sub-estimate,
    presently OPEN/refuted-as-written, connected_extensivity = UNRESOLVED_BY_RL2B_
    REFUTING_BRANCH, R4O :139 )
  DISTINCT( internal/external metric conversion of a record cell: separate sealed node
    — T1 U2 :146-147 vs U4 :152-153 — and type-incompatible, being SCALE + GR-metric
    bearing (ell_P, G_4, Lorentzian diamond metric, factor beta; XSM :48-54,:88-92),
    whereas C is connection-only and scale-free (FORM D5 :248-252, B5) )

BUILT_NODES = P1 ratified finite-N functional (TYPE-P); P2 disjoint monoidal
  additivity (TYPE-P); P3 tangent complex + three-block split, im(Q_flux)=im(d_1^dag)
  (TYPE-P); P4 Phi as sole connection dependence + winding factor-out (TYPE-P); P5
  two-branch necessity (TYPE-P); P6 FORM typing n^2 Phi^T C Phi with C typed (TYPE-P,
  typing only); P7 void-overdetermination background (TYPE-P)

GAP_NODES = G1 Gate-5 kernel C (TYPE-U) — THE ONE OBJECT; G2 scalarization/state-port
  pair (TYPE-R canonical-scalar / TYPE-U state-port) — C's consumer; G3 write-chain
  support/current-density realization (TYPE-U) — the fork's finite-N deciding datum

FORBIDDEN_IMPORTS_IN_TARGET = none. (Flagged distinct, fenced OUT: XSM's
  internal/external conversion carries SCALE + GR-metric — it is the DISTINCT node,
  not part of the posing.)

POSING_ONLY = CERTIFIED. Nothing decided/computed/bounded: factorization NOT decided
  (PROP(cross) neither asserted nor denied); winding NOT decided; R_record,L,
  R_record,L-value, n, kappa, alpha, mu(w), C, beta, and every scale/spectrum left
  symbolic. Seals verified at path before reading (V011 packet member aa7c6d49). No
  register/tracker/plan/road/ledger/lens read. No git action. Output name probed
  ABSENT before write.

alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until adversarial panel check.
```
