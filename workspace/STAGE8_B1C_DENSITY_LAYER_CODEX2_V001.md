# STAGE 8 / 7A / [PLAN:B1c-4 / Gc2-ADJACENT] — THE DENSITY LAYER CAMPAIGN

Lane: CODEX 2. Relay 810. All headline determinations are **CLAIMED** pending registrar cross-check.

## Lead determination

The seven demanded components do not form one undifferentiated absence. The sealed stock separates them into three kinds:

```text
DERIVABLE-FORCED: s_g, S_g
UNDECIDABLE:      f_g, F_g, eta_resp
OPACITY-BOUND:    delta_K', d_g
```

No component in the seven is already displayed at the complete demanded `Ref_a` signature, and no component is presently a demonstrated free family. The current stock is nevertheless load-bearing: `K_G^fin`, `J_c`, and the older `S_R` law supply the current domains, realizations, and admissibility equations. Once an actual physical `f_g` is supplied, the signed child-path chain is forced generatorwise and `S_g` is its forced restriction to the conserved-current kernel. The support stock does not supply `F_g`: it concerns interaction-density support under causal exhaustion, not support under subdivision. The density pair is not an honest negative finding; Codex 2's own relay 793 deliberately made `R4Dens` opaque, so the present stop is **OPACITY-BOUND**.

`eta_resp` is a third kind of gap. It is the transport map on `ResponseData`, not the response Hessian and not the evaluated-pair set `D`. Those two materiality gaps are different objects: the Hessian's form is content consumed by `Eval`, while `D` is a domain restriction on the pairs tested. They nevertheless block verification of a candidate `eta_resp` square.

No member is bound and the gluing diagnostic is not run.

## 0. Preflight, custody, and pin check

The pickup acknowledgement `relay_outbox/810_ACK.md` was written before the relay body was read. The inbox digest is `fce42f4a2cb4e7bf1879fe7996433523f89d698443191377a6d5327e9fb55acf`; its adjacent sidecar content matches. The output artifact and sidecar were absent before this write.

The state brief was verified and read first:

```text
PROGRAM_STATE_BRIEF_V005.md
e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
adjacent seal: OK
```

The following decisive sources were hash-verified before use. Adjacent seals verified unless a packet-membership mode is stated.

| key | sealed source | SHA-256 | decisive content |
|---|---|---|---|
| D012 | `STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md` | `74bbb7aa971554f83d5ce2eb38710b6aae38d340055ab31eca1c23379bc685da` | `[30747,33958)` addressed cospan and response signature; `[44665,46055)` dependency rows |
| JREF | `STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md` | `8dd59b35bb9f63f8c8107d438c757c0cb9a110ee1078c173213c6da657bdfb24` | `[13461,14260)` response square and absent carriers |
| 791 | `STAGE8_B1C_CURRENT_DENSITY_HUNT_CODEX2_V001.md` | `f7b218cd6c7a23bb19d87413576147fcdf64b1d0e7705bf2b1e14ab5727ccd37` | `[11963,14211)` survivor/interface classification; `[14211,16991)` exact remainder |
| 793 | `STAGE8_B1C_DENSITY_RECEIVER_BUILD_CODEX2_V001.md` | `0c53549766730513635bd35f89cebcc03210ddb6de2531b5eaded4d4b0e23a00` | ground/receiver block through `[6965)`; bridge `[7035,7848)`; generator dispositions `[7848,9478)` |
| 806 | `STAGE8_B1C_RECEIVER_INHABIT_DARIO_V001.md` | `3151fd1a38ab30a5c442339b879c450fd2c00d7013331c08b2c5634effd50277` | `[727,2831)` 4/10 supplied and 6/10 absent; `[2836,4821)` candidate conversion and opacity |
| 808 | `STAGE8_B2_GLUING_SCOPE_AND_RUN_CODEX2_V001.md` | `3280674c2427bd9ff8e9f91d9f8944fa5b97f99baed00373e2ab06f329fa0349` | `[3071,5133)` exact cospan/response extraction; `[5133,6076)` run refusal |
| WHERE | `STAGE8_TASK5_EQ6_WHERE_CLAUSES_LANE2_V005.md` | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | `[4505,5661)` P4/X4 field population; `[6510,9332)` path/current/support law |
| R4 | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | `[7633,8436)` finite current seam; `[12210,13564)` realization |
| Q408 | `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V004.md` | `c5254f663a1e3a6d1079027184c6dea96a35ab7255a856aa39e584b3d7c32dc2` | `[12570,15287)` path/current clause; `[19302,20226)` finite exhibit |
| CIS | `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` | `b0c636f3b2b00f0694ad001cb32a3a84c5d4fc09c25c57fd4fdb8885e8206b30` | `[527,755)` support type; `[818,1091)` exhaustion law; packet-member seal |
| SUP | `STAGE8_7A_SUPPORT_AND_FORCING_DARIO_V001.md` | `9685af44cc48f01fb04e57329cedf4f9a871eb393c6d41396179776957287e9b` | `[10095,11777)` support/exhaustion distinction; `[13621,14918)` stopped refinement residue |
| CDL | `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md` | `625b4ed9c91b28dd15a2884498f980dcbb792c8b9cf9b13a743b2e8ec2bb8953` | `[344,1200)` exhaustion and one-use causal support; packet-member seal |
| TAU-C | `STAGE8_7A_TAU_DENSITY_CARRIER_DARIO_V001.md` | `0743eb526132d4cc29102121b4a6e79b9b35dd72e1562ffd395e5c63c26ff26f` | intensive-log carrier and exhaustion/refinement mismatch |
| TAU-R | `STAGE8_7A_TAU_DENSITY_REFERENCE_DARIO_V001.md` | `6441b787eaa28c817edd501369f3ba5550a6831312a5b54f3b5c3f2cb9e508dd` | `[10817,11534)` token is silent as a carrier typing |
| E4B | `STAGE8_C1_E4B_PROOF_AND_MOLLER_HUNT_DARIO_V001.md` | `162c6d7ddcd280f567645eb863828d86c3d9a8cd06f49c0641a4361ff8a1a0f5` | `[7897,11800)` ERR/GNS precursor and boundary |
| ERR | `R3_4_SHARED_SOURCE_OUTGOING_RANGE_ERRATUM_V001.md` | `5f8e60629f2b0fc703b5b6de02ca2de96ffb9d5e8aa48f961a043c40f8c93f41` | stable dressed outgoing-record monomorphism; packet-member seal |
| GNS | `R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md` | `10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995` | `[653,3983)` finite compatibility and dressed net; `[3983,5020)` boundary; packet-member seal |
| MAT | `STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md` | `660e0c1464aee4f3a0bccd60e79050d5f1573378f5b11cfe84ec1ba3a975df1f` | `[23555,31747)` materiality typing and two spec gaps |
| INC | `STAGE8_B1A_CARRIER_CROSSCHECK_NATURALITY_CODEX2_V001.md` | `614e20c8bfd1978a4273c831b76bd6145483876c975ea87f80ef31a589b8bdc7` | `[3953,7622)` independently rebuilt generator incidence carrier |

For the packet-only sources, the exact workspace bytes match the entries in `review_packets/STAGE7_QSPEC_CANDIDATE_V001/STAGE7_PACKET_MANIFEST_V001.sha256`. Law 8 is therefore satisfied without pretending an absent adjacent sidecar exists. PE-1 through PE-9 were pointer-known only, unopened, unsearched, and carry zero verdict weight.

## 1. AS1 — the seven exact demanded types and the stock hunt

### 1.1 Signature envelope

D012 `[30747,33958)` requires a common-refinement cospan carrying

```text
incidence, degree, connection, coframe, volume,
current-density normalization, and support.
```

The same span types the response side:

```text
r_F^*       : F_phys(K) -> F_phys(R)
eta_resp(r) : ResponseData(K) -> ResponseData(R)
Eval_K      : ResponseData(K) -> Quad(F_phys(K))
Eval_R      : ResponseData(R) -> Quad(F_phys(R))
(r_F^*)^*   : Quad(F_phys(R)) -> Quad(F_phys(K))

N_r(d_K)
 := [(r_F^*)^* Eval_R(eta_resp(r)d_K)]_resp
    - [Eval_K(d_K)]_resp
 in Q_resp(K).
```

JREF `[13461,14260)` independently records that the top response arrow lacks a completed response assignment on geometric refinements, a density map, and the common-region relation. Later work supplies the index grammar and coframe layer; it does not silently supply the seven components below.

### 1.2 Closed per-component type table

The component spellings come from 793 `[7035,7848)` and its receiver block before byte 6965; WHERE `[6510,9332)` supplies the expanded domains and equations.

| component | exact demanded type | required equations / role |
|---|---|---|
| `f_g` | `f_g:M_K -> M_K'`, a proper orientation-compatible local map over `g:K->K'` | maps each old actual path to the ordered target-child path; proper/full-rank on the old physical image; identity/composition, orientation/reality |
| `F_g` | `F_g:Loc_K -> Loc_K'`, the induced support-poset map | preserves inclusions, finite unions, and disjointness; `Supp_K'(S_gc) subseteq F_g(Supp_K(c))`; common-refinement coherence |
| `s_g` | `s_g:C_1(K) -> C_1(K')`, a signed path-chain map | `partial_K' s_g=s_g^0 partial_K`; sends each old path basis element to its ordered signed target-child chain; identity/composition |
| `S_g` | `S_g:=s_g|Curr(K):Curr(K)->Curr(K')`, with `Curr(K)=K_K^fin=ker(B_K^T)` | injective, current-preserving; `J_K'(S_gc)=f_(g*)J_K(c)`; incidence and common-refinement intertwining |
| `delta_K'` | `delta_K' in R4Dens(K')` | `Pos(delta_K')`; `Dual(delta_K',Cof(K'))`; `VolNorm(delta_K',Vol_4|K')`; `CurrentDomain(delta_K')=Curr(K')` |
| `d_g` | `d_g:R4Dens(K)->R4Dens(K')` | identity/composition; duality square with `Cof_g`; exact `Vol_4` quadratic; orientation/reality; current/support/common-topology coherence |
| `eta_resp` | `eta_resp(g):ResponseData(K)->ResponseData(K')` | makes the `Eval`/`r_F^*` response square meaningful in `Q_resp(K)` and obeys refinement identity/composition on the common completed domain |

### 1.3 Meaning-probed stock, component by component

#### `f_g`

The record displays two nearby but non-identical things: the target-independent `Ref_a` cellular generator grammar, and the older `PathRel_adm` **law** for a physical local map. It does not display the conversion between them. WHERE `[6510,9332)` expressly allows the admissible physical-path set to be empty and records the law as proposed/not adopted. INC `[3953,7622)` supplies coordinate incidence/aggregation data for A0/A1/A2, but a coordinate cellular refinement is not by itself a map of the physical path carriers `M_K`. Treating the barred same-smooth-coframe constituent as that map would violate S26. The missing sealed object is:

```text
one nonempty, content-addressed PathRel_adm(g) realization for every licensed
Ref_a generator, or a sealed functor converting the Ref_a cellular arrow to
the older actual-path arrow.
```

#### `F_g`

CIS `[527,755)` supplies `support(L_c) subseteq Omega_c`, where the supported object is an interaction density in a Lorentz-covariant causal cell. CIS `[818,1091)` and CDL `[344,1200)` transport that support by a future-directed **exhaustion**: after the boundary crosses a closure face, the completed incidence is not reapplied. SUP proves that this is not a subdivision law and identifies the closest refinement-support certificate as STOPPED. No sealed text identifies `Omega_c` with a `Ref_a` cell or tells how `support(L_c)` moves when that cell is subdivided. The missing sealed object is therefore the `Loc_K -> Loc_K'` refinement-support functor plus that identification; set-theoretic cochain support is the wrong type.

#### `s_g`

The current stock supplies the named inputs needed for a forced construction, not an already emitted `Ref_a` component. R4/Q408 provide the path basis, `Curr(K)=ker(B_K^T)`, and its distributional realization. Given a lawful `f_g` and the licensed generator's ordered child decomposition, define on every oriented edge basis element

```text
s_g[e] := the signed ordered target-child chain representing f_g(gamma_(K,e)),
```

and extend linearly. The image of every vertex endpoint is fixed by `f_g`; hence the generatorwise incidence identity is exactly `partial_K' s_g=s_g^0 partial_K`. Identities and composites are then forced by the corresponding path compositions. INC's large cochain-section residuals do not create a competing `s_g`: those residuals concern field/cochain lifts, while this map is the actual child-path chain fixed after `f_g` is named. No map is instantiated here; this is the sealed pinning recipe.

#### `S_g`

Once `s_g` exists, the demanded map has no independent datum:

```text
S_g = s_g|ker(B_K^T).
```

The `K_G^fin/J_c/S_R` lineage supplies the domain, codomain realization, old-current injectivity requirement, and the equation `J_K'(S_gc)=f_(g*)J_K(c)`. Thus the restriction and its conservation test are forced. This is the precise sense in which the conserved-current half **exists**: it supplies the carrier and the check, while the missing physical `f_g` prevents an already-displayed complete `Ref_a` bridge.

#### `delta_K'` and `d_g`

The stock displays `Dens_R` as a positive current/volume-density operator dual to `Cof_R`, and 793 packages the exact predicates above. It deliberately declines to say what an element of `R4Dens(K)` is. TAU-C's intensive holomorphic-log carrier is indexed by causal exhaustion and TAU-R finds that the token `tau_density` is never typed on the receiver; neither is a conversion into `R4Dens`. Because candidate comparison is prevented by that declared wall, a negative census would be misleading. Both components are **OPACITY-BOUND**, not `UNDECIDABLE` on corpus evidence.

This status does not assert nonemptiness, uniqueness, or a member. It says only that the present inability to decide those questions is caused by DS2's self-imposed type opacity. Once that wall is lawfully replaced, the candidates must be classified again.

#### `eta_resp`

D012 supplies only the signature and naturality target. JREF records the completed response assignment as missing. ERR and GNS constrain any future map: the finite completed-record states obey exact inductive compatibility and the dressed incoming net is coherent, but the result is a monomorphism/finite-completion precursor, not a global source-inclusive state limit or infinite-future unitary. It therefore cannot be converted into `eta_resp:ResponseData(K)->ResponseData(K')`.

MAT `[23555,31747)` gives the two nearest missing response objects: the sealed definition of `R_record,L` has no sealed **form**, and the pair set `D subseteq F_phys x F_phys` consumed by the material question is not enumerated. No sealed `ResponseData` instance, response topology/quotient, or natural transformation closes those gaps. The missing sealed object is a completed `ResponseData` carrier with `Eval`, `Q_resp`, and an arrowwise `eta_resp` compatible with both refinement and the finite ERR/GNS record embeddings.

## 2. AS2 — classification

| component | classification | decisive reason |
|---|---|---|
| `f_g` | **UNDECIDABLE** | the exact physical-path realization or `Ref_a -> PathRel_adm` conversion is unsealed; a cellular map cannot be relabeled as the physical map under S26 |
| `F_g` | **UNDECIDABLE** | CIS/CDL support is exhaustion-indexed interaction-density support; the subdivision/support functor and carrier identification are absent |
| `s_g` | **DERIVABLE-FORCED** | once `f_g` and the named child decomposition exist, the image of every chain basis element and hence the linear boundary-intertwining map are fixed |
| `S_g` | **DERIVABLE-FORCED** | it is definitionally `s_g|Curr(K)`; the existing current lineage fixes the carrier and conservation/injectivity checks |
| `delta_K'` | **OPACITY-BOUND** | 793 deliberately leaves `R4Dens(K')` opaque despite the sealed positivity/duality/normalization predicates |
| `d_g` | **OPACITY-BOUND** | its domain, codomain, and action cannot be candidate-checked through the same declared `R4Dens` wall |
| `eta_resp` | **UNDECIDABLE** | the completed `ResponseData`/`Eval`/`Q_resp` realization is absent; ERR/GNS is a strict precursor, not a conversion |

```text
SUPPLIED          = 0
DERIVABLE-FORCED = 2  (s_g, S_g)
FREE              = 0
UNDECIDABLE       = 3  (f_g, F_g, eta_resp)
OPACITY-BOUND     = 2  (delta_K', d_g)
```

There is no principal-facing free component at this layer **yet**. The 1887-dimensional B1a family remains carried whole and unselected, but it is connection/coframe assignment freedom, not one of these seven density-layer components. A future `R4Dens` retyping may expose free density members; this relay may not prejudge that census.

## 3. AS3 — exact assembly path to a formable `d_K`

Seven items stand between the present record and a formable response datum `d_K`:

1. **Supply the physical bridge input `f_g`.** Seal a nonempty physical-path realization for every A0/A1/A2 generator, or a content-addressed conversion from each `Ref_a` generator to the older actual-path relation. The conversion must be target-independent and may not import the barred smooth constituent.
2. **Execute the two forced current derivations.** From item 1, emit and verify the generatorwise `s_g` path-chain map, then restrict it to `S_g`; replay boundary intertwining, injectivity, current pushforward, identity, composition, and common-refinement equality. This is lane work, not a principal choice.
3. **Supply the support refinement law.** Type the CIS interaction-density support carrier against `Ref_a`, and seal `F_g` with support containment and common-refinement coherence. Exhaustion reuse alone is insufficient.
4. **Retype the density receiver lawfully.** Replace 793's opaque `R4Dens(K)` by a sealed, content-addressed carrier specifying: the R4 unit torsor; the positive cone; the pairing dual to `Cof(K)`; intrinsic-`Vol_4` normalization; `CurrentDomain=Curr(K)`; and the exact equality notion. This is a receiver-scope question because Codex 2 authored the opacity.
5. **Reclassify and inhabit the density arrows.** After item 4, test the stock for `delta_K'` and `d_g`; prove or refute nonemptiness and then verify identity/composition, duality, `Vol_4`, orientation/reality, support, and common-topology squares. Opacity removal is not itself an inhabitant.
6. **Complete the response carrier.** Seal `ResponseData`, `Eval`, `Q_resp`, and the response topology; close or explicitly carry the materiality form/domain gaps; construct `eta_resp` and check it against D012, JREF, and the finite ERR/GNS compatibility. This remains undecidable work, not a zero fill.
7. **Only then form the diagnostic input.** Exhibit `d_K in ResponseData(K)` with all consumed density/current/support bindings. At that point `eta_resp(g)d_K`, both `Eval` terms, and hence the formal expression `N_g(d_K)` become formable. No evaluation or gluing run occurs in this relay.

### 3.1 `eta_resp` versus the materiality gaps

The relationship is **DIFFERENT OBJECTS, ADJACENT DEPENDENCIES**:

| object | type/role | relation to `eta_resp` |
|---|---|---|
| `eta_resp(g)` | arrow `ResponseData(K)->ResponseData(K')` | transports response data |
| form of `R_record,L` | bilinear/Hessian content on connection tangents, entering the pulled-back response form | part of what `Eval` must evaluate; not a transport arrow |
| evaluated-pair set `D` | subset of `F_phys x F_phys` | domain on which material agreement is tested; not response data or an arrow |

Thus `eta_resp` is **not the same object** as either gap. Its candidate naturality cannot be certified without knowing what response form is transported and on which evaluated domain the criterion is asserted. The three gaps compose; they do not collapse.

## 4. AS4 — freedoms consumed and flattening check

### 4.1 `FREEDOMS_CONSUMED`

| datum | treatment |
|---|---|
| `Ref_a` A0/A1/A2 generator family | **CARRIED WHOLE**; no arrow member selected |
| B1a 1887-dimensional connection/coframe family | **CARRIED WHOLE**; no point bound and no density inference drawn from it |
| physical `f_g` / support `F_g` | **CARRIED AS ABSENT SEALED OBJECTS**; neither a smooth map nor a zero support map substituted |
| `s_g`, `S_g` | **DERIVATION RECIPES ONLY**; no map emitted, no member bound |
| density normalization/scale | **NONE SELECTED**; `VolNorm` remains a predicate |
| `delta_K'`, `d_g` | **CARRIED BEHIND DECLARED OPACITY**; no negative verdict disguised as a search result |
| `tau_density` / `tau_ker` | **NOT IDENTIFIED OR SELECTED**; the intensive-log topology is not retyped as `R4Dens` |
| `ResponseData`, `Q_resp`, response topology | **CARRIED AS ABSENT**; no quotient or zero object chosen |
| ERR/GNS monomorphism and net | **CONSUMED ONLY AS SEALED CONSTRAINTS**; not upgraded to a source-inclusive unitary or response transport |
| materiality Hessian form and pair set `D` | **CARRIED AS TWO SPEC GAPS**; neither assumed |
| output, coefficient, reader, measured datum | **NOT CONSULTED** |

`SUBSTITUTED = none.`

### 4.2 `FLATTENING_CHECK`

`DECLINE_REGISTER_V002.md` SHA-256 `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` and adjacent seal verified. All 37 rows were walked. Live rows:

- **S08 clean:** no discrete incidence, density, current, support, or response carrier is identified with electromagnetism, a Maxwell connection, gravity, or a smooth public field.
- **S26 clean:** `C_ref`'s smooth/coframe constituent is not used as a source for `f_g`; it remains an audit/target interface.
- **S28 clean:** no member of the free B1a family or any future density family is selected by consequence.
- **DS2 surfaced:** the declared `R4Dens` opacity is reported as the active receiver wall, not converted into an absence-of-record verdict.

All other decline rows are untouched. `FLATTENING_CHECK = clean`.

### 4.3 Gates and self verb audit

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
component authored = none
member binding = none
fixed-point execution = none
end test = none
diagnostic run = none
numeric evaluation of a physical quantity = none
comparison to measured constants = none
smooth import = none
EM identification = none
common junction cell formed = false
junction map evaluated = false
```

Self verb audit: **CLEAN.** “DERIVABLE-FORCED” is explicitly conditional on the named missing `f_g` input and does not claim the maps were constructed. “OPACITY-BOUND” reports the author-declared scope wall and does not claim density inhabitance. All headline determinations remain CLAIMED.

COMPONENTS = 7 typed (signature spans cited)
CLASSIFICATION = SUPPLIED 0 / DERIVABLE-FORCED 2 (`s_g`, `S_g`) / FREE 0 / UNDECIDABLE 3 (`f_g`, `F_g`, `eta_resp`) / OPACITY-BOUND 2 (`delta_K'`, `d_g`)
D_K_PATH = enumerated (7 items to a formable `d_K`)
ETA_RESP = different object from both materiality gaps; adjacent dependency on the Hessian form and evaluated-pair set `D` stated
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
