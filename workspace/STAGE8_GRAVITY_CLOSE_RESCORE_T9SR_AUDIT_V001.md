# STAGE 8 — GRAVITY CLOSE RE-SCORE — INDEPENDENT AUDIT — COMMISSION T9SR — V001

## RESCORE-AUDIT, 2026-08-15 — DEFAULT-REFUTE EXECUTED

Role: independent re-walk of every row R1-R14 at the frozen spec's own bytes
(R11/R13 under the retyped reading of record, G-1), against the same ground
the build used, with my own per-row verdicts fixed BEFORE consuming the
build's rows in detail; then adjudication of every difference at the bytes.
Builder testimony carried ZERO weight: every seal re-verified at path this
session; every load-bearing claim re-read at its source bytes.

**OVERALL VERDICT: CONFIRMED-WITH-CORRECTIONS.**

**ADJUDICATED TALLY ACROSS R1-R14 (the frozen spec's own verdict
definitions; R11/R13 at the retyped reading):**

```text
  CONFIRMED                   2   (R2, R4)
  INERT                       0
  NOT_RECOGNIZABLE_AS_STATED  12  (R1, R3, R5, R6, R7, R8, R9, R10, R11,
                                   R12, R13, R14)
  U1, U2                      REGISTRAR-DISPOSED (G-3) — not scored,
                                   not tallied, per commission
```

This is the build's tally EXACTLY — every per-row verdict, every carrier,
and every movement cause re-derived independently and adjudicated CONFIRMED.
One correction (a self-contradicting count inside the build's §10 tally
annotation, verdict-neutral) and two notes are recorded at §6; none is
verdict-changing, none voids.

**THIS ARTIFACT DECLARES NOTHING AND LANDS NOTHING.** SK-7 carried on the
face: the member is NOT landed; Joint A (the ExtSrc fiber) and Joints B, C,
HJ7, E, F remain STOPPED/open of record; no row herein consumes any of them
as discharged. The adopted arrow is consumed as AUTHORED PHYSICS, never as
derivation.

Fences held: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. Scoring/determination audit only — no
construction, no authored physics, no evaluation of any junction map, no
member bound, no numeric evaluation of anything, no measured-constant
comparison. The number-free rule is held at the program's own displayed
grade (the baseline's and the build's): no number appears as a value of any
program quantity anywhere; bookkeeping counts and timestamps are not
program values. No git. No register/tracker/plan/continuation file opened.
"Q-..." references EXPECTED-UNLOCATABLE, consumed only as quoted inside
seal-verified artifacts. Output path and sidecar probed ABSENT before any
other act; one file written plus its seal sidecar.

**SWEEP CUTOFF: 2026-08-15T10:09:00Z.** Exhaustiveness below is
boundary-relative (ACL-1); nothing absolute is claimed.

---

## 1. STEP 0 AND SEALS — VERIFIED AT PATH (`shasum -a 256 -c`) BEFORE CONSUMPTION

Step 0: the claimed artifact probed PRESENT with its sidecar; `shasum -a 256
-c` OK. My own output path probed ABSENT before any write. All grounds then
verified, zero mismatches:

```text
b72d7bfd047dd901ee9178635e450d6ba9d9f177382901f11defb37903e18f98  STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_V001.md            OK (THE CLAIMED ARTIFACT)
9f0d12b4556427eb965bcd9c869f645b984009fdbcc0d1d19a2c7216d31c51f1  STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md    OK (THE FROZEN SPEC; byte-unchanged, matching G-1's delta-of-record statement; read in full)
ee91d85c0798d73b7003b9eee12c15d27a949dbc3b81adc84299686f1472932e  STAGE8_GRAVITY_CLOSE_SCORING_V001.md                 OK (THE FIRST PASS / BASELINE; read in full; its tally CONFIRMED 0 / INERT 0 / NOT_RECOGNIZABLE_AS_STATED 15 re-read at its own §0/§5 bytes)
7638c807774823ce0c865f7f7932ebf8cce258ee27e01614c2d7925901e2a364  STAGE8_GRAVITY_CLOSE_SCORING_AUDIT_V001.md           OK (baseline's audit; seal verified; not otherwise consumed — the baseline enters as the ground of record per commission)
d422fc453a8863ca5bcf001b6201d4342a53b6af7d9966ed41ef64930992d567  STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md              OK (matches G-2's named digest EXACTLY; read in full)
e719044c8963adbbe0fdfece95d9d1f6429b2235ffd28bf0dcaa871c9bba37df  STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001.md        OK (matches G-2's named digest EXACTLY; read in full; CHECK-PASS-WITH-CORRECTIONS at its own bytes)
```

The three carried givens (G-1 retype; G-2 ratified premise; G-3 U1/U2
registrar-disposed) are consumed AS STATED, of record; their instruments
were not hunted, per commission.

INDEPENDENCE DISPLAY, for honesty: locating the build's input declarations
required one grep of the build file, whose output unavoidably exposed
fragments of ten of the build's rows before my re-walk was fixed. My
verdicts below were nonetheless derived from the ground bytes (frozen spec,
baseline, premise pair), each row argued from its own test and evidence
form; where a glimpsed fragment and my derivation could have interacted, the
row's reasoning is displayed in full at §3 so the derivation, not the
glimpse, carries the verdict. The four rows never glimpsed (R1, R3, R9, R13
table rows) landed identical to the build by derivation alone — a displayed
consistency check on the method.

---

## 2. THE GROUND FACTS MY RE-WALK RESTS ON, FIXED AT BYTES

```text
FROM THE FROZEN SPEC (9f0d12b4, its own bytes):
  §1.2 R1-R9 tests/evidence forms; §1.3 R10-R14 (R11/R13 superseded for
  scoring by G-1's retyped text); §1.1 the PASS bar ("a rail is not a
  member"; "Where an actual manifest is absent, totality is not
  inferred"); §3 the three closed verdicts with the schema's conditional
  clauses; §2 guards G-1..G-5; §5A kill set {K-1, K-3}, K-2 NAMED-OPEN.
  Wording classes at bytes: R1 and R5 are MEMBER-worded ("A member
  supplies…", "the member provides… in topologies the member names");
  R2/R3/R4 are RULE-worded ("the rule's behaviour…", "the rule's
  value…", the inclusion). Evidence-form classes at bytes: R1 demands
  rule-as-bytes PLUS one displayed evaluation on a NAMED oriented
  k-cell; R3 demands proof object PLUS a displayed pair with equal
  value; R2 and R4 demand proof objects with no instance clause.
FROM THE BASELINE (ee91d85c): fifteen third verdicts of record; the §2.4
  PUSHBACK FORK carried OPEN with its named deciders; the §3.2
  divergence records; the corpus-sweep findings consumed as the baseline
  of record (not re-swept — same boundary the build declared at CL-1).
FROM THE RATIFIED PREMISE PAIR (d422fc45 + e719044c, per G-2 always as
  AUTHORED PHYSICS, never as derivation):
  EXISTS on lawful stock: the (JD-3)/(JD-3a) rule as bytes (T2-8,
  candidate §3.8, verbatim on the forced target form — check §2 Row 1
  PASS); P-1 orientation covariance PROVED (check: CONFIRMED, re-derived
  MC-2/MC-3a/3b, "No gap promoted"); P-2 presentation independence
  PROVED structural (check: CONFIRMED, MC-6); P-3 continuity PROVED on
  declared data in supplier-named topologies covering Phitilde_N, Phi_N,
  iotatilde_N^H, iota_N^H (check: CONFIRMED with grounding note N-1);
  P-4 contact-kernel PROVED (check: CONFIRMED, MC-6b, verified against
  the obligation sentence); the sealed reducing law [E_C,N,Delta_N]=0
  consumed as sealed stock (candidate §3.10/§6 row 3; check Row 3 PASS).
  DOES NOT EXIST on lawful stock: any member (SK-7 part of the premise;
  Joints A/B/C/HJ7/E/F stopped); any evaluation of any authored map on
  any record object (mandate clause 3 from first byte; candidate §7:
  "none was evaluable"; §9: the composite's actual-stock domain is
  EMPTY until JB-2 lands); any displayed pair of presentations with
  values (check F-1 sweep: none); Loc_N^phys or Loc_N^C (Joint C
  presupposed at its slot, not supplied — candidate §9); any Sch_N
  object; any proof of CONF-1 (authored law, F-5 armed; no actual-stage
  confinement display in sealed stock — check §5); the closure induction
  (only the (S25c)-shaped square, DISPLAYED OBLIGATION, untested, F-3
  armed — candidate §3.2); any normalization object on any provenance
  (G-2: the Rule-2 door is shut; lambda_JD symbolic, unassigned — a
  unit datum, not a relation, not an output).
```

---

## 3. THE INDEPENDENT RE-WALK — MY VERDICT PER ROW, WITH THE ARGUMENT

Verdict rule, byte-faithful to spec §3 and inherited §1.1: absent evidence
never scores CONFIRMED and never scores INERT; untested is not failed.

```text
R1  NOT_RECOGNIZABLE_AS_STATED. Test is member-worded; no member (SK-7).
    Evidence form is two-part: the rule as bytes NOW EXISTS (authored
    premise, lawful); the one displayed evaluation on a named oriented
    k-cell DOES NOT and lawfully could not (clause 3; JB-2-empty actual
    domain). Carrier = member status of the supplier + the displayed
    evaluation; rule-as-bytes half supplied — carrier NARROWS.
R2  CONFIRMED. Rule-worded test; the (JD-3) rule exists on lawful stock;
    the demanded evidence is a PROOF OBJECT over the named cell and its
    reverse, no instance clause. P-1 is that object: proved for EVERY
    oriented k-cell and its reverse (universal covers any named cell),
    check-confirmed with the declared input T2-6 displayed inline and
    guarded (F-1/F-4). "Proved, not asserted" — satisfied by executed
    proof, not by provability. Provenance authored, displayed. NULL
    carrier per schema.
R3  NOT_RECOGNIZABLE_AS_STATED. Evidence form read CONJUNCTIVELY (proof
    object PLUS displayed pair) — the baseline's own reading ("plus"),
    and the spec marks alternatives with an explicit "or" where it means
    one (R6). Proof half exists (P-2); the displayed pair is an
    evaluation-grade instance, barred at authoring by clause 3 and
    absent from lawful stock (check F-1 sweep). Carrier = the displayed
    pair; proof half supplied — carrier NARROWS.
R4  CONFIRMED. Evidence form is "proof object", full stop. P-4 exists,
    check-confirmed, and with P-2 (P-3 typing) delivers exactly the
    obligation sentence's two-clause demand — Phi_N and iota_N^H
    well-defined presentation-independent quotient maps in (JD-2)'s
    displayed form. Provenance authored, displayed. NULL carrier.
R5  NOT_RECOGNIZABLE_AS_STATED. Member-worded test; no member. Even at
    candidate provenance only TWO of the four named arrows carry
    supplier-named topologies with continuity proofs (iotatilde_N^H,
    iota_N^H via P-3); Loc_N^phys and the composite Loc_N^C have no
    construction and no proof (Joint C presupposed, not supplied).
    G-2 honored: topologies named by the rule's own supplier, pre-fixed
    by nobody. Carrier = a member providing all four; concretely the
    two Loc-side arrows — carrier NARROWS.
R6  NOT_RECOGNIZABLE_AS_STATED. Premise does not touch Sch_N; neither
    discharge object exists on lawful stock. Unchanged from baseline.
R7  NOT_RECOGNIZABLE_AS_STATED. Clause 1 sealed of record (reducing
    law); clause 2 unconditional corollary of P-4 through the sealed
    R_C,N isomorphism (kernel inclusion survives composition with an
    isomorphism); clause 3 — the confinement identity universal over
    [s] — NOT PROVED: exact-conditional display on CONF-1, an authored
    law, F-5 armed, no actual-stage confinement display anywhere in
    sealed stock. Untested is not failed: no INERT. Carrier = the
    confinement-identity proof — carrier NARROWS to one clause.
R8  NOT_RECOGNIZABLE_AS_STATED. No inclusion attempt, no formula
    discharge, nothing for G-3 to fence; PART-PROVABLE tag carried
    unchanged. Unchanged from baseline.
R9  NOT_RECOGNIZABLE_AS_STATED. The premise adds an armed, untested
    (S25c)-shaped square OBLIGATION — adjacent display, not the closure
    induction over the generating schemas. Verdict and carrier
    unchanged in substance.
R10 NOT_RECOGNIZABLE_AS_STATED. Retyped §1.3 closing clause binds the
    object whatever its lawful provenance — and NO candidate
    normalization object exists on either provenance (none derived,
    baseline sweep carried; none authored, Rule-2 door shut per G-2;
    lambda_JD is a unit symbol, not a normalization object). Surface
    finding stands; PUSHBACK FORK remains OPEN (none of its three named
    deciders occurred; the retype exercised the §5 route on provenance
    clauses only, not the referent class). Carrier re-expressed to
    both admissible provenances.
R11 NOT_RECOGNIZABLE_AS_STATED at the RETYPED evidence form: neither
    the derivation nor the mandate-lane authored construction record of
    a normalization candidate exists — the widened class is
    uninhabited. The premise's own clause-3/SK-8 target-blindness is
    Object A's discipline — different object, correctly NOT entered.
R12 NOT_RECOGNIZABLE_AS_STATED. No candidate, hence no requirements
    pair and no intersection argument. CORR-0003 stays adjacent at
    weight zero. Unchanged from baseline.
R13 NOT_RECOGNIZABLE_AS_STATED at the RETYPED test and evidence form:
    no relation displayed same-cell on ANY provenance; no dimensional
    match exists either (nothing was obtained at all); the retyped test
    can now SEE an authored relation, and has nothing to look at.
    Carrier = the same-cell relation with its provenance certificate
    (either provenance) plus the explicit no-dimensional-match
    statement.
R14 NOT_RECOGNIZABLE_AS_STATED. No selection event exists for Object B.
    The one adoption event on today's stock (the T2SR ratification) is
    Object A's and carries F5-consonant discipline — adjacent,
    displayed, correctly NOT entered. Unchanged from baseline.
U1, U2  REGISTRAR-DISPOSED per G-3, consumed as stated — not scored.

MY TALLY: CONFIRMED 2 (R2, R4) / INERT 0 / NOT_RECOGNIZABLE_AS_STATED 12.
```

---

## 4. ADJUDICATION AGAINST THE BUILD, ROW BY ROW

The build's rows were then consumed in detail and compared:

```text
ROW  MINE                        BUILD                       ADJUDICATION
R1   NRAS, two-absence carrier   NRAS, same two absences     AGREE at bytes
R2   CONFIRMED on P-1            CONFIRMED on P-1            AGREE at bytes
R3   NRAS, instance half         NRAS, instance half (CL-3)  AGREE at bytes
R4   CONFIRMED on P-4 (+P-2)     CONFIRMED on P-4 (+P-2)     AGREE at bytes
R5   NRAS, two Loc arrows +      NRAS, same carrier          AGREE at bytes
     memberhood
R6   NRAS unchanged              NRAS unchanged              AGREE at bytes
R7   NRAS, clause-3 proof        NRAS, same carrier          AGREE at bytes
R8   NRAS unchanged              NRAS unchanged              AGREE at bytes
R9   NRAS unchanged in           NRAS unchanged in           AGREE at bytes
     substance                   substance
R10  NRAS, both provenances;     NRAS, same; fork OPEN §8    AGREE at bytes
     fork OPEN
R11  NRAS, retyped form          NRAS, retyped form          AGREE at bytes
     uninhabited                 uninhabited
R12  NRAS unchanged              NRAS unchanged              AGREE at bytes
R13  NRAS, retyped whole,        NRAS, same                  AGREE at bytes
     uninhabited
R14  NRAS unchanged; Object-A    NRAS, same, adjacent not    AGREE at bytes
     adoption adjacent           entered
U1   REGISTRAR-DISPOSED          REGISTRAR-DISPOSED          AGREE per G-3
U2   REGISTRAR-DISPOSED          REGISTRAR-DISPOSED          AGREE per G-3

DISAGREEMENTS REQUIRING ADJUDICATION: ZERO on verdicts, carriers, and
movement causes. The build's schema rows (§6) were checked against the
frozen schema's conditional clauses: both CONFIRMED rows carry
missing_carrier null; all twelve third-verdict rows carry nonempty
carriers; objects respect the enum. CONFORMANT.
```

### 4.1 The commissioned hunt, item by item

```text
HUNT-1  a CONFIRMED row whose evidence form is not discharged at bytes:
        HUNTED AND NOT FOUND. R2's form ("proof object over the named
        cell and its reverse") carries no instance clause — P-1's
        universal proof covers any named cell (the build ledgers the
        reading at CL-5; I re-derived it independently); R4's form is
        "proof object" alone — P-4 exists, check-confirmed against the
        obligation sentence. The two rows that DO carry instance
        clauses at bytes (R1's displayed evaluation; R3's displayed
        pair) were BOTH scored third by the build with the instance
        absence as carrier — the exact trap, not fallen into.
HUNT-2  the authored premise consumed as DERIVED anywhere: NOT FOUND.
        Both CONFIRMED rows carry the authored-as-premise provenance
        display in their own evidence arrays; §3's rails state no
        derived-demanding text is satisfied by the arrow; R11/R13's
        widened forms admit authored records and the build enters
        nothing into them.
HUNT-3  "provable" silently promoted to "proved": NOT FOUND. Movement
        occurs only on P-1/P-4 (executed, check-confirmed); R7's two
        discharged clauses are sealed law + an unconditional P-4
        corollary; R8's PART-PROVABLE tag travels unchanged; no
        lane-PROVABLE tag moves any verdict.
HUNT-4  a Joint A/B/C/HJ7/E/F consumption (SK-7 breach): NOT FOUND.
        R1/R5/R7 name stopped joints as ABSENCES (naming an absence is
        not consuming a discharge); SK-7 banner on the build's face;
        the two CONFIRMED rows are criterion-level facts about the
        rule, and Joint D is not in the SK-7 stopped list — even its
        discharge is nowhere claimed performed.
HUNT-5  a missing-carrier name drifted from the frozen text: NOT FOUND.
        All twelve carriers walked against the frozen bytes (R11/R13
        against G-1's retyped text): R1 names the member and the named
        oriented k-cell evaluation; R3 the displayed pair; R5 all four
        arrows by their frozen names; R6 SPEC-5/6's two discharges; R7
        the universal confinement proof; R9 the closure induction over
        the generating schemas; R10 the two typed inputs on one and
        the same record cell; R11/R13 the retyped two-provenance forms
        (R13 with the explicit no-dimensional-match statement); R12
        both requirements and the intersection argument; R14 the
        selection record and the anti-tuning ledger. Faithful, with
        the boundary rename ("scored corpus" -> "lawful stock")
        applied uniformly and correctly.
HUNT-6  movement attributed to the wrong cause: NOT FOUND at any row
        (verdict moves R2/R4 -> the premise's executed proofs; carrier
        narrowing R1/R3/R5/R7 -> the premise; R10/R11/R13 re-expression
        -> the retype, route-only, uninhabited; R6/R8/R9/R12/R14 ->
        neither). One COUNT of the narrowed set inside §10 contradicts
        the build's own movement table — that is Correction A-1, a
        count defect, not a cause misattribution.
HUNT-7  any number as a value of a program quantity: NOT FOUND. Fence
        battery on the build: no Thomson; alpha/kappa only inside the
        held-false fence flags; no float outside digests, timestamps,
        and section numbering; no measured constant; no coupling value;
        no evaluation output anywhere.
```

---

## 5. THE STANDING BLOCKS, RE-CHECKED

```text
PUSHBACK FORK: the build carries it OPEN (§8) with the correct display
  that the executed retype exercised the spec's §5 revision route on
  PROVENANCE clauses only and did not resolve the referent-class
  tension; the baseline's three named deciders re-read at its §2.4
  bytes — none has occurred. CORRECT.
§5A STANDING: kill set {K-1, K-3} NOT ENGAGED — the premise advances no
  coupling reading of any divergence (its fence-scan at the check's
  bytes carries no coupling value; CONF-1 is a confinement law, not a
  divergence reading); K-2 remains the frozen NAMED-OPEN SLOT,
  unfilled; the baseline's two divergence records left exactly as
  recorded. CORRECT.
FACE ITEMS OF THE BUILD: CHOICE LEDGER terminal, commission named,
  seven rows, zero OPEN; TOY_SEPARATION clean and accurate (only the
  check's confirmed grades consumed, never the CAS scaffolds); FLAG
  BLOCK complete; sweep cutoff declared; seals 4/4 verified before
  consumption; output path probed absent. ALL PRESENT AND HELD.
```

---

## 6. CORRECTIONS AND NOTES (none verdict-changing; none voids)

```text
A-1  CORRECTION — SELF-CONTRADICTING COUNT IN §10: the build's tally
     annotation says "five of them NARROWED by the adopted premise"
     and, in the movement recap, "five with narrowed carriers". Its
     own §7 movement table marks exactly FOUR rows "carrier NARROWED"
     (R1, R3, R5, R7; R9 is "none in substance", cause NEITHER), and
     its own FLAG BLOCK says both "carrier narrowing at R1/R3/R5/R7"
     and "narrowed four carriers". Four is correct at the rows; the
     word "five" (twice, §10) is the defect. VERDICT-NEUTRAL: the
     tally 2/0/12, every verdict, every carrier, and every cause are
     unaffected. Fix the two words.
A-2  NOTE — CL-2's TAIL SENTENCE IS OVERBROAD: "the differential is
     displayed, not load-bearing on the tally" is true only for the
     alternative CL-2 names (reading "member" loosely as "an
     instance", which indeed moves nothing). The OPPOSITE tightening
     (reading R2-R4's "the rule" member-anaphorically, so that with no
     member the rows cannot see their object) WOULD move R2/R4 — so
     the wording-class differential is load-bearing in that direction.
     The adopted reading is nonetheless CORRECT at bytes, and I adopt
     it independently: R2-R4 are rule-worded; the rows' shared ground
     span is the obligation sentence, which binds whatever lawfully
     supplies (JD-3) ("and must prove orientation covariance,
     presentation independence, continuity, and Ctc_N^sym subset
     ker(Phitilde_N)"); and G-2's "LAWFULLY CONSUMABLE AS PREMISE" is
     the registrar's disposition that these criteria may consume the
     authored rule as their object. The reading should be ledgered as
     load-bearing WITH that ground, not waved off as neutral.
A-3  NOTE — R9's CAUSE COLUMN, hairline: §7 marks R9 "none in
     substance … NEITHER" while its own parenthetical records an
     armed square display ADDED by the premise. The column's referent
     (verdict/carrier movement) is genuinely unmoved and the addition
     is correctly sourced to the candidate at §4.1/§6, so no
     wrong-cause defect exists; recorded only to close HUNT-6 on the
     one row where the surface could mislead.
```

---

## 7. VERDICT

```text
OVERALL: CONFIRMED-WITH-CORRECTIONS.

ADJUDICATED TALLY (identical to the build's):
  CONFIRMED 2 (R2, R4) / INERT 0 / NOT_RECOGNIZABLE_AS_STATED 12
  (R1, R3, R5, R6, R7, R8, R9, R10, R11, R12, R13, R14);
  U1, U2 REGISTRAR-DISPOSED (G-3), not scored, not tallied.
  MOVEMENT vs baseline: CONFIRMED 0 -> 2 ; INERT 0 -> 0 ;
  NOT_RECOGNIZABLE 15 -> 12 — verdict movement caused only by the
  adopted premise's executed proofs P-1/P-4; carrier narrowing at
  R1/R3/R5/R7 (FOUR rows — A-1); retype re-expression at R10/R11/R13
  with the widened provenance class uninhabited.

WHY CONFIRMED-WITH-CORRECTIONS AND NOT CONFIRMED: A-1 is a real
byte-level self-contradiction inside the build (its §10 against its
own §7 and FLAG BLOCK), of exactly the count-drift class this program
corrects rather than waves through; it changes no verdict, carrier,
cause, or tally bucket, so it corrects rather than refutes.

WHY NOT REFUTED: zero disagreements survived adjudication on any
verdict, carrier, movement cause, schema row, guard application, fork
status, or §5A standing; the commissioned hunt returned empty at every
item; the build's honesty rails (authored-never-derived, PROVABLE-not-
PROVED, SK-7, §1.1 inheritance) are held at its own bytes and at the
grounds'.
```

---

## CHOICE LEDGER (terminal; commission T9SR RESCORE-AUDIT)

```text
ACL-1  CORPUS BOUNDARY: exactly the claimed artifact plus the same four
       grounds the build declared (frozen spec 9f0d12b4; baseline
       ee91d85c; premise pair d422fc45/e719044c), every seal verified
       at path this session before consumption, plus the baseline's
       audit seal verified for chain completeness without further
       consumption; the three carried givens consumed AS STATED, their
       instruments not hunted, per commission. The baseline's corpus-
       sweep findings consumed as the baseline of record and not
       re-swept — matching the build's own CL-1 boundary, which is the
       question this audit adjudicates. A wider sweep is a different
       audit. PREMISE(named).
ACL-2  INDEPENDENCE PROTOCOL: my per-row verdicts fixed after reading
       the grounds and before consuming the build's rows in detail;
       the one input-locating grep's partial exposure displayed at §1
       rather than hidden; the four never-glimpsed rows landing
       identical by derivation displayed as the method's consistency
       check. PREMISE(named).
ACL-3  READINGS ADOPTED AND OWNED INDEPENDENTLY (each also the
       build's): R3's semicolon conjunctive (the baseline's "plus";
       the spec's contrastive "or" at R6); R2's universal-covers-named
       proof form; the rule-worded/member-worded differential WITH the
       obligation-sentence ground stated at A-2. PREMISE(named).
ACL-4  CONSUMPTION GRADES: the check's verdict board consumed at its
       sealed grades (P-1..P-4 CONFIRMED; C-1..C-3/N-1/N-2 carried);
       the criteria's ground files (fdf20bd4 etc.) entered only as
       quoted inside the seal-verified frozen spec and premise pair —
       citation grade, the same grade the build and baseline used;
       not re-verified at source. PREMISE(named).
ACL-5  GRADING STANDARD for A-1: an internal count contradiction at
       bytes earns CORRECTIONS (the program's own precedent: the
       frozen spec's §D self-caught miscount; the check's C-1..C-3),
       not REFUTED, when zero verdict content moves. PREMISE(named).
ACL-6  NUMBER-FREE read at the program's own displayed grade: fences
       and no-program-value, with bookkeeping counts permitted — the
       grade the baseline and build both used on their faces.
       PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  Every load-bearing step of this audit rests on sealed bytes of the
  five seal-verified artifacts (claimed + four grounds) and the carried
  givens of record. No toy model, no stand-in object, no convenience
  substitute appears at any load-bearing site; the premise pair's CAS
  scaffolds were consumed only through the check's confirmed grades,
  never directly; nothing herein narrows any quantifier silently —
  every narrowing is a named ledger row.
```

## FLAG BLOCK

```text
COMMISSION = T9SR (RESCORE-AUDIT, 2026-08-15) — DEFAULT-REFUTE audit of
  STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_V001.md (b72d7bfd…)
VERDICT = CONFIRMED-WITH-CORRECTIONS (one correction A-1, two notes
  A-2/A-3; none verdict-changing; none voids)
ADJUDICATED_TALLY = CONFIRMED 2 (R2, R4) / INERT 0 /
  NOT_RECOGNIZABLE_AS_STATED 12 / U1, U2 REGISTRAR-DISPOSED — identical
  to the build's at every row, carrier, and cause
HUNT = all seven commissioned items hunted; every one returned NOT
  FOUND at bytes (the §10 count defect is A-1, a count, not a cause or
  verdict defect)
SK7 = carried; member NOT landed; Joints A/B/C/HJ7/E/F stopped/open of
  record; nothing herein consumes any as discharged; authored premise
  consumed as AUTHORED PHYSICS only
PUSHBACK_FORK = OPEN of record, correctly carried by the build
KILL_SET = {K-1, K-3} NOT ENGAGED ; K-2 NAMED-OPEN, untouched — build's
  §9 standing display re-checked correct
SEALS = claimed artifact 1/1 + grounds 5/5 verified at path this
  session before consumption (incl. the baseline-audit sidecar); the
  premise-pair digests match G-2's named digests exactly; the frozen
  spec byte-unchanged at its sealed digest; zero mismatches
SWEEP_CUTOFF = 2026-08-15T10:09:00Z (boundary per ACL-1)
FENCE_SCAN = build and this artifact: no Thomson, alpha/kappa only in
  held-false fence flags, no float outside digests/timestamps/section
  numbers, no measured constant, no coupling value — NO HIT
CHAIN_INVOKED = false ; MEMBER_BOUND = false ; COMMON_CELL_FORMED =
  false ; JUNCTION_MAP_EVALUATED = false ; NUMERIC_EVALUATION = false ;
  MEASURED_CONSTANT_COMPARISON = none ; MACHINERY_APPEAL = none
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
DECLARATION = not-made (this audit declares nothing; the declaration
  gate is the principal's, separately)
NET = re-walked blind, the frozen spec's fourteen rows land exactly
  where the build landed them: two rule-worded criteria CONFIRMED on
  executed, check-confirmed proofs of the ratified authored rule, zero
  real negatives, twelve exactly-named absences, the fork open, the
  kill set unengaged — and the build's one defect is a two-word count
  that its own movement table already refutes.
```
