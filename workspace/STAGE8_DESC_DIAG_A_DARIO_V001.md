CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep) -- CLAIMED, not clean (Q-921)
VERDICT_BEARING_SET = exactly the 21 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path at seal time (Q-913).
LANE = DARIO   ROLE_THIS_RELAY = PE-17 DIAGNOSIS ARM A.  TWO STRUCTURAL QUESTIONS ONLY.
  THIS RELAY DOES NOT DEFEND, RE-RUN, OR ACT ON THE B03 STOP (Q-937).  Member 02 is read FOR ITS
  PINS.  Both answers below are constructed to hold under EVERY outcome of the 1024 attack.
DIGEST_RULE = every file digest and every byte offset below is COMPUTED by member 21 and quoted from
  its output.  No digest and no offset in this artifact is typed by hand.
SPAN_CONVENTION = DECLARED (Q-927/931/935).  Two shapes, each span tagged:
    ROW    -- leading pipe THROUGH terminating newline (the B01/B02/B03 convention, inherited).
    FIXED  -- a literal [a,b) used ONLY to re-verify a span an upstream sealed artifact already
              pinned; a mismatch is a refusal.  16 of the 41 spans are FIXED; 16/16 MATCH.
  A third shape, ANCHOR (unique start anchor to/through a named end anchor), is used for the four
  spans that are not rows and not upstream-pinned; the generator refuses on a non-unique anchor.
SUBJECT_READ_RULE = member 07 is read ONLY WITH member 06 (overlay V002) and member 08 (DoR-013),
  per Q-931/Q-935.  status(member 07) := RATIFIED_FAMILY_LEVEL_BY_DOR_013, no-member clause carried.
  **SECTION 5.2 QUOTES A SPAN OF MEMBER 07 THAT CONTAINS A STALE STATUS SURFACE** -- overlay pin #7,
  Q03 below, lies inside Q02 -- and the quotation is fenced and flagged in place rather than
  silently trimmed, because trimming a ratified object to make a quotation tidy is the edit the
  overlay exists to bar.
REGISTER_SNAPSHOT = member 20 is live-append and has advanced since members 03/05/12 pinned it; its
  digest is its value at my seal time.  Only Q-900's heading carries weight here, at its own span.
NOT_OPENED, DECLARED = relay_inbox/RELAY_PASTE_1024_B03_CHECK_CODEX2_V001.md and any 1024 output.
  It is the opposite lane's attack on my own build, it is not a named subject, and reading it would
  let the check's framing shape a diagnosis required to hold under every outcome.
GENERATOR_REFUSAL = member 21 emits NOTHING unless every declared input opens, every anchor is
  present AND unique, and every FIXED span reproduces its upstream digest (Q-920/Q-924).
ALL_RESULTS = CLAIMED until the opposite-lane check.
CLOSURE_END_BYTE = 00007104
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1025_DIAGNOSIS_ARM_A_DARIO_V001.md` | `00f57b2bb60c3ca07fefa7b2b233939ca3a0a7a02790f2af3171c444ba34656b` | **the assignment** |
| 02 | `workspace/STAGE8_DESC_B03_DARIO_V001.md` | `581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506` | B03 — **read for its pins, not re-argued** |
| 03 | `workspace/STAGE8_DESC_AUDIT_CODEX2_V002.md` | `078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d` | **the supply map; basis rows B04–B15** |
| 04 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | **the demand; the conditions naming the joint state** |
| 05 | `workspace/STAGE8_DESC_B02_DARIO_V001.md` | `80fc23ed9687b4ccde1a51e4455e0c1878837c535cd4c5594e839c43f0743d1a` | the B02 trace B10 aggregates on |
| 06 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | **the overlay of record; the read rule; pin #7** |
| 07 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **THE DEFINITION OF `d_state` — question 2's hinge.** READ ONLY, with 06 and 08 |
| 08 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | **the ratification that makes the definition of record** |
| 09 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` | **THE ROLE CONTENTS — the ledger's structural spine** |
| 10 | `workspace/STAGE8_TASK4A_P3_COMPLETE_U3_PACKAGE_CONSTRUCTION_AND_FOUR_FIELD_STOP_V001.md` | `f97ee43e052969f0c999b7e57d13310f818222a102c21c249845b8beef1301ff` | the four-field would-build signatures |
| 11 | `workspace/STAGE8_AXN_BASE_FAMILY_INVERSE_CODEX2_V001.md` | `e857302bd35c01dd7eb30062a48f2ec1937fc02f87a802aa7efd447e222465b2` | **K7's base-family requirement** |
| 12 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | **the invariance theorem; the square; the K4 handoff** |
| 13 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | **the port and its DoR-008 premise typing** |
| 14 | `workspace/STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | **the artifact that authored the WRONG CODOMAIN typing** |
| 15 | `workspace/STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md` | `35f581dee0683add232367f953ec7807e89bb5eac47e34f4ad5d781a87c95be0` | **`P_src`'s carrier; purification/extension** |
| 16 | `workspace/STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab` | G4 — the instrument whose pinning Q-900 typed |
| 17 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 18 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 19 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 20 | `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `3f522a8ccc296d28bd392b55ed6fb5424c98eeaa29a8d97961771bf4b428d8b5` | live-append; **Q-900 at its span** |
| 21 | `workspace/build_diag_a_pins_v001.py` | `c500cbc7229632417218ffb22ffea5d5668f48f06a3932360d01254c1247cea3` | the pin generator; **declared inputs are exactly members 01–21** |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF ANY PHYSICAL QUANTITY.
NO COMPARISON TO A MEASURED CONSTANT.  NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.
NO STATE SELECTED, RANKED, NAMED, PREFERRED, OR APPROACHED.  NO FREEZE PROPOSED.
THE CHOOSE BRANCH IS BRIAN'S AND IS NOT ENTERED.  omega_phys UNTOUCHED.
STRUCTURE READ; NO DOWNSTREAM VALUE EVALUATED.  NOTHING ADOPTED.  NOTHING REGISTERED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — DIAGNOSIS ARM A — DARIO LANE — V001
## RELAY 1025 — `[PLAN:DESC-24]` — IS THE FREEDOM GAUGE, AND WHO AUTHORED THE BAR?

Date: 2026-08-11
Status: **TWO ANSWERS, BOTH OUTCOME-INDEPENDENT. (1) THE FREEDOM IS NOT GAUGE. Nineteen consumers
dispositioned; eleven CONSUME-STATE at cited spans, four consume objects composed from state-variant
data, and only two are marginal-only. The record marginal's blindness to the choice does not
propagate: the program's own descendants carry the state in their declared role contents — U2's
"positive normalized pre-state" and U3's "descended contour/spacetime measure" — and K7 and K4
consume it in terms. (2) THE CODOMAIN BAR IS RECORD-FORCED, and the C18/C19 near-miss CLOSES. It is
not a typing decision at all: DoR-013 does not stipulate `d_state`'s codomain, it DEFINES `d_state`
as the unique normalized fixed state of `P_src`, and `P_src` is a channel on the source carrier — so
the codomain is ENTAILED by the ratified definition. This is not Q-900's class, and I say why at
§5.5. A methodological finding comes with the first answer and limits it: this instrument can refute
gauge but could never have confirmed it.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 1. What is being measured, and against what [PROVABLE]

### 1.1 The invariance, stated exactly — because the whole ledger is measured against it

Member 12 at `[15554,15965)` (FIXED), span SHA-256
`e9dafa5963128305d212f83b8c6617bfa47293f70bf91f5d7c0729fdb6828532`, is the theorem this relay's
question is built on: for every `omega_B in State(B)`, the product state `omega_out tensor omega_B`
on `R_inf tensor_min B` has **the same record marginal**, so the sealed history-state simplex being a
continuum, the record marginal admits a continuum of extensions.

**What is proved invariant is the RECORD MARGINAL — a state on `R_inf`, the record factor. Nothing
else is proved invariant by that theorem, and the ledger below turns entirely on not widening it.**
A reading that took "the record cannot see the choice" to mean "nothing downstream can see it" would
be assuming the answer to question 1 rather than computing it.

### 1.2 The disposition vocabulary, declared

The assignment fixes three dispositions. Applying them exposed one distinction the assignment's own
stakes sentence already carries — it says *"MARGINAL-ONLY or COMPOSED-**from-invariants**"* — so I
declare the split rather than let it work silently:

```text
CONSUMES-STATE           the object's own declared content or would-build names the state, the
                         density, or a carrier that varies with omega_phys.  A cited span is
                         REQUIRED for this call; the burden is on the call, not against it.
CONSUMES-MARGINAL-ONLY   the object touches only choice-invariant data.  Two ways to qualify:
                         it touches the record marginal only, or its declared role content
                         carries no state term at all (invariant a fortiori).
CONSUMES-COMPOSED        the object needs a derived object rather than omega_phys itself.  SPLIT,
                         because the two halves point opposite ways:
                           -from-INVARIANTS  the derived object is built from choice-invariant data
                           -from-VARIANTS    the derived object is built from state-consuming data,
                                             e.g. Omega_C0 compose i_B (which IS omega_phys under
                                             SM-4) or the descended measure dmu_C.  THIS IS NOT
                                             GAUGE; it is load-bearing one step removed.
```

Two further dispositions were forced by the bytes and are declared rather than folded:

```text
STATE-BEARING-NOT-A-CONSUMER  the row's content includes a state, but the row is an INDEPENDENT
                              reference rather than a consumer of omega_phys.  Folding it into
                              CONSUMES-STATE would inflate the count; folding it into
                              MARGINAL-ONLY would hide a state.  Exactly one row (B05).
UNDETERMINED-AT-THE-SPAN      the would-build names no state datum and no carrier that would import
                              one, and no other span settles it.  NOT typed CONSUMES-STATE, because
                              that call requires a citation and there is none.  Exactly one (B07).
```

---

## 2. The structural spine, at bytes [PROVABLE — quoted]

Everything in the ledger rests on one span. Member 09 at `[37523,38115)` (FIXED), span SHA-256
`e16f8b9438c518546fa7eda93c724316f4470f9a8298a50ac8d275eb09d293ad`, declares what each descendant
role actually carries:

```text
C0 = joint carrier/algebra, representation, common domain, branch embeddings,
     and physical source maps only

U1 = physical branch/source orientation, metric, involution, compound-index
     order, source symmetry, and embeddings on C0

U2 = microscopic action/evolution, positive normalized pre-state, inclusive
     identity, admitted effects, action/source contact rules, and common domains

U3 = physical orbit/constraint map, quotient, descended contour/spacetime
     measure, boundary/edge/gluing and endpoint operator domains, and the
     predeclared contour prescription
```

**Read for state content, this span answers most of question 1 by itself:**

```text
C0  NO STATE TERM.  The word "only" is in the record's own text and does real work.
U1  NO STATE TERM.
U2  "positive normalized pre-state"          <- THE STATE IS ROLE CONTENT HERE
U3  "descended contour/spacetime measure"    <- THE MEASURE, which K7 requires to DESCEND from
                                                the state (section 3.4)
```

The arrows those roles sit on are member 09 at `[38121,38437)` (FIXED), span SHA-256
`cb264602a0537a07948c31d2763094395c5670a5001ea8378d9e7f83b67ed72c`: `d_C0 : B0_candidate -> C0` and
`d_Ui : (B0_candidate,C0) -> Ui`, with the rule that no required physical datum in `C0` or any `Ui`
may be supplied by an undeclared external supplement after any descendant output is inspected.

**One distinction is load-bearing and I fix it here rather than let it float.** C0's "representation"
is the **module** representation `pi_C0 : A_C0 -> L_B(E_C0)`, which is state-free. The **scalar**
representation `pi_omega : A_C0 -> B(H_omega)` is a StatePort field and varies with `omega`. Member
14 at `[8902,10649)` (FIXED), span SHA-256
`20b0b65035f4dbf75ae72f2c62983f881a71cb8bca69fe568e9d9eaa83c7e77f`, keeps them apart in terms:
*"`pi_C0:A_C0->L_B(E_C0)` exists on the Hilbert module — **UNINSTANTIATED**: module representation is
not a scalar Hilbert representation."* Collapsing the two would move C0 from marginal-only to
state-consuming on a confusion.

---

## 3. THE DEPENDENCY LEDGER [PROVABLE]

Nineteen consumers: the twelve basis rows B04–B15, the five demand conditions that name the joint
state, K7's base-family requirement, and K4 — the receiver K7's own row routes through. B10 is a
basis row and is counted once, at its row.

### 3.1 The twelve basis rows

Each row pinned at its own ROW span in member 03.

| Row | span / digest | Disposition | Ground for the disposition |
|---|---|---|---|
| `B04` | `[32848,33032)` `1cf1b31c77cac5e0a0e53d962a937fadad7bed3b17a1e6c5d88a1546071dbbea` | **CONSUMES-STATE** | The port itself. Member 13 `[10824,11934)` (FIXED) `4313fb92…`: `omega_phys` is field one, and `H_omega := completion of E_C0 / N_omega` where `N_omega` is *"the null space of the scalarized module form"* — **the Hilbert space is built from `omega`, so it varies with the choice.** The row also carries the commuting placement square, member 12 `[16822,17244)` (FIXED) `7b1ee519…` |
| `B05` | `[33032,33246)` `9032501bbbad73da96d28e504bb6e399394ff03123d51f8b7af58f4fa766937d` | **STATE-BEARING-NOT-A-CONSUMER** | Its content is the complete U2 reference, whose role content names the *positive normalized pre-state* — but the row's own release column types it *"comparison oracle for `d_U2` and P5 receiver"*, an **independently completed** reference. It does not consume `omega_phys`; it supplies the state that B13's byte-bound comparison meets |
| `B06` | `[33246,33501)` `ba57d0053ad0ed2facae9ef7469dca0cf23a1e2f169be35ca1541b7c043c7996` | **CONSUMES-STATE** | Member 10 `[8708,9198)` (FIXED) `7b0f4b36…`: *"a branch-joint measure generated from the common-origin action/**state**/history-domain package"*. The word is in the would-build |
| `B07` | `[33501,33608)` `02e7d5c0977d37a581eba29fff8da8f386e27dd1bfa284dd0b622984d06b1e71` | **UNDETERMINED-AT-THE-SPAN** | Member 10 `[10691,11236)` (FIXED) `270b8925…` names an analytic domain, a boundary-value direction, a regulator rule and a convergence theorem in a named operator topology — **no state datum, and no carrier that would import one.** I do not type it CONSUMES-STATE on a guess about which space the operators act on |
| `B08` | `[33608,33715)` `d287ee5510eb227d29fa268cbffacd843773a736cbb67bd84bcd33fce6a9eee8` | **CONSUMES-COMPOSED-FROM-VARIANTS** | Member 10 `[12613,13281)` (FIXED) `037d0b1c…`: *"compatibility with `dmu_C` and the interacting contour"*. `dmu_C` is the descended measure B06 derives from the state |
| `B09` | `[33715,33857)` `2022dc6d01ecf9046e2e35ae737b8c70631334648fb04cb89334878a4074c975` | **CONSUMES-STATE** | Member 10 `[14191,14909)` (FIXED) `3c0a264b…`: *"named unbounded physical operators on a **scalar physical representation**"* — the scalar representation is the `omega`-dependent one §2 distinguishes from the module representation |
| `B10` | `[33857,34008)` `b270158d7b63475386308739e6fd7973f0880ccb81127a5606edb2f02891f991` | **CONSUMES-COMPOSED-FROM-VARIANTS** | It aggregates B06–B09 on the B02 trace; two of those four consume the state and one consumes the measure. U3's own role content names the *descended* measure |
| `B11` | `[34008,34338)` `33de9f9b80aac3b58630402a61723c982d999d479b86cc5da13076a2aef6ebd0` | **CONSUMES-MARGINAL-ONLY** | C0's role content carries **no state term** — *"carrier/algebra, representation, common domain, branch embeddings, and physical source maps only"* |
| `B12` | `[34338,34423)` `346ee0fc615570ce79f24533d47155338bf3a6e8787a1a3d4e5ce256c2b9d735` | **CONSUMES-MARGINAL-ONLY** | U1's role content carries no state term: orientation, metric, involution, index order, source symmetry, embeddings |
| `B13` | `[34423,34545)` `817064bf584e692c49eec1c34206620a94c7ad94a8e0382b0d110def56d5e56d` | **CONSUMES-STATE** | `d_U2` must deliver the complete U2 role, whose content includes the pre-state; member 04's condition 13 `[24330,24561)` `67b55bd8…` types the U2 role signature a **delivery constraint** on `d_U2` |
| `B14` | `[34545,34658)` `0a4c70c666734ab895ad4e4f08c6206452a35516e17c81d6e0f784af418193e7` | **CONSUMES-COMPOSED-FROM-VARIANTS** | `d_U3` outputs the complete U3 package and is byte-bound to B10 |
| `B15` | `[34658,35153)` `6474744117402cfe408c6f753c1e8a819ab94f0e193fde7910f081c3ebf96b6a` | **CONSUMES-COMPOSED-FROM-VARIANTS** | The certificate package must establish the state's own properties — member 04's condition 7 |

### 3.2 The five demand conditions that name the joint state

| # | span / digest | Disposition | Ground |
|---|---|---|---|
| 1 | `[22947,23046)` `b7cdab9972cc4391579b4c100957f26e04e281279e0cb0dc3f43ce71bed40e91` | **CONSUMES-STATE** | The four typed descent maps, of which `d_state(Omega) = rho_pre` is one |
| 7 | `[23605,23719)` `fde0455196a69d779f381eb60f1f9c6388234ec2b188840531a579d3656b5751` | **CONSUMES-STATE** | *"State properties: positive, trace-class where applicable, normalized"* — properties **of the state** |
| 13 | `[24330,24561)` `67b55bd880de6839c9622d4ba2bb36f2805b8dab424f3aeaa7165c85e438890d` | **CONSUMES-STATE** | The U2 role signature as a delivery constraint, including the inclusive identity |
| 18 | `[25313,25492)` `7bbd968e8bc73caaa6c9872caabbf0cfdbaf4e3426b9dd06e8b9042dcd39c8b7` | **CONSUMES-STATE** | K7: the base and weighted family must descend from the frozen joint common-origin trace |
| 35 | `[27780,27998)` `00d7b4c8a84fea121d9d19745f1ff3ee44e327fd5a09c17c66da145ef96130cc` | **CONSUMES-STATE** | It names `omega_phys` itself and binds it to the representation and density |

### 3.3 K7 and K4

Member 11 at `[7502,8047)` (FIXED), span SHA-256
`829581cdd1f29ea25316f6b127cf44daa4ba2178c771df70e087a91f41a09987`, quoted because it is the single
most decisive entry in the ledger:

> *"The branch-joint base must arise from the frozen common-origin action/**state**/history-domain
> package, then descend through the physical orbit map with variation/tightness control and contour,
> spacetime, boundary, representation, and common-domain compatibility. Intrinsic quotient measure
> cannot replace this direction-bearing descent."*

**K7 = CONSUMES-STATE**, and its last sentence forecloses the obvious escape: an intrinsic quotient
measure — one built from the history quotient without descending from the state — *cannot replace*
the direction-bearing descent. That is the record refusing, in advance, the construction that would
have made the measure choice-independent.

Member 12 at `[21194,21565)` (ANCHOR), span SHA-256
`e76be5c6f0536f2573c79f5d02a46e94f497cac5e87b744613ccf9c0636d011d`: *"`K4` requires an actual
`omega_hist`, hence an actual projective finite base family."* **K4 = CONSUMES-STATE**, and it is the
tightest link in the chain: `omega_hist` **is** `omega_phys` under SM-4, so K4 consumes the selector
directly and the base family is built from it.

### 3.4 The tally

```text
LEDGER = 19 CONSUMERS, EACH DISPOSITIONED

  CONSUMES-STATE                    11   B04 B06 B09 B13 | conditions 1, 7, 13, 18, 35 | K7, K4
                                         -- that is 12 names for 11 slots?  NO: K7 and condition 18
                                         are the SAME requirement at two receivers, and I count the
                                         REQUIREMENT once, at K7's own span, with condition 18 listed
                                         as the demand map's carriage of it.  The eleven are
                                         B04, B06, B09, B13, cond 1, cond 7, cond 13, cond 35, K7,
                                         K4, and condition 18 counted as the demand-side carriage of
                                         K7 -- which makes eleven only if K7 and cond 18 are counted
                                         separately.  THEY ARE, because the assignment enumerates
                                         "every demand condition that names the joint state" AND
                                         "K7's base-family requirements" as separate populations.
                                         COUNTED AS THE ASSIGNMENT ENUMERATES: 11.
  CONSUMES-COMPOSED-FROM-VARIANTS    4   B08 B10 B14 B15
  CONSUMES-MARGINAL-ONLY             2   B11 B12
  STATE-BEARING-NOT-A-CONSUMER       1   B05
  UNDETERMINED-AT-THE-SPAN           1   B07
  CONSUMES-COMPOSED-FROM-INVARIANTS  0
                                    ---
                                     19

  CONSUMES_STATE = 11.  MARGINAL-ONLY = 2 of 19.
```

**The bracketed note above is left in the open on purpose.** I drafted the CONSUMES-STATE row, could
not immediately reconcile twelve names with eleven slots, and worked the double-count out in place
rather than quietly re-typing a number. K7 and condition 18 are one requirement carried at two
receivers; the assignment enumerates the two populations separately, so they are counted separately,
and the count is eleven. A reader who prefers the requirement-count gets ten and the verdict does not
move.

### 3.5 The one-line conclusion

```text
THE FREEDOM IS NOT GAUGE.  The record marginal's blindness to omega_B does not propagate: eleven
consumers declare that they consume the state, four more consume objects composed from state-variant
data, and only two of nineteen are marginal-only.  The choice becomes load-bearing at the U2 pre-
state and at every object built on the U3 descended measure.
```

### 3.6 The methodological finding this ledger forces, and the limit it puts on the answer

**This instrument is asymmetric, and the asymmetry is worth more than the verdict.**

```text
IT CAN REFUTE GAUGE.  Exhibiting one consumer whose own declared content names the state is enough,
  and eleven were found.  That is a structural fact, readable without evaluating anything.

IT COULD NEVER HAVE CONFIRMED GAUGE.  "Every consumer is invariant" is not a structural claim.  A
  consumer can DECLARE that it consumes the state and still be independent of WHICH state, if the
  dependence cancels.  Establishing that it does would require evaluating the consumer -- computing
  what it returns for two different members -- and THAT IS THE LINE THE ASSIGNMENT SAYS THIS RELAY
  MUST NEVER CROSS.
```

So the honest form of the verdict is: **load-bearing at the level of declared consumption.** If a
lane later wants the stronger claim — that some particular consumer's dependence is not merely
declared but ineliminable — that is a different question, it needs a different instrument, and it is
not answered here. **The weaker claim is enough for the stakes the assignment set**: it names the
spans where a choice becomes load-bearing, and there are eleven of them.

---

## 4. GAUGE VERDICT

```text
GAUGE_VERDICT = LOAD-BEARING-AT

  member 13 [10824,11934)  4313fb92…   the StatePort itself: omega_phys is field 1 and H_omega is
                                       built from it (B04)
  member 10 [8708,9198)    7b0f4b36…   the measure would-build: "from the common-origin
                                       action/STATE/history-domain package" (B06)
  member 10 [14191,14909)  3c0a264b…   the domain would-build: "on a scalar physical
                                       representation" (B09)
  member 09 [37523,38115)  e16f8b94…   U2's role content: "positive normalized pre-state" (B13,
                                       condition 13)
  member 11 [7502,8047)    829581cd…   K7: the base must arise from the common-origin action/STATE/
                                       history-domain package, and intrinsic quotient measure
                                       CANNOT REPLACE the direction-bearing descent
  member 12 [21194,21565)  e76be5c6…   K4: requires an ACTUAL omega_hist
  member 12 [16822,17244)  7b1ee519…   the placement square (B04, condition 35)
  member 04 [22947,23046)  b7cdab99…   condition 1
  member 04 [23605,23719)  fde04551…   condition 7
  member 04 [25313,25492)  7bbd968e…   condition 18
  member 04 [27780,27998)  00d7b4c8…   condition 35

NOT MIXED, and the distinction matters: MIXED would suggest the section splits into a
choice-sensitive part and a choice-free part that could proceed independently.  It does not.  The
two marginal-only rows are B11 and B12 -- d_C0 and d_U1 -- and B13/B14 sit downstream of them in the
same basis, so the choice-free region is a PREFIX of the construction, not a parallel track.
```

---

## 5. QUESTION 2 — THE PROVENANCE OF THE BAR [PROVABLE]

### 5.1 What the bar says

Member 14 at `[8902,10649)` (FIXED) carries three rows that together are the bar:

```text
| rho_pre | DoR-013 generates rho_S,[A]=I_A/Tr_A(I_A) on the finite source sector | **WRONG
  CODOMAIN**: no trace-class density on H_omega or the joint source-record-field carrier |
| positivity and unit trace | independently proved for the source density at fixed A0 | **PASS ONLY
  AT SOURCE-SECTOR SCOPE**; not a joint-state certificate |
| state_descent_map | d_state:Omega_prim,N->Dens_1^+(H_A) is ratified | **WRONG CODOMAIN**: it is
  not d_state_Omega:Omega->StatePort_U2_008 or d_joint:Omega->State(A_C0) |
```

The question is who authored that typing: is it derived from the transition structure, or is it a
decision an instrument took?

### 5.2 The definition that fixes the codomain

**The bar is not a stipulation about `d_state`'s codomain. `d_state` has no stipulated codomain — it
has a DEFINITION, and the codomain follows from it.** Member 07 at `[22842,24541)` (FIXED), span
SHA-256 `84a5b7050188448e2a0cc526de0131b297cb734bc96b292968e1e37080b61cf0`, §7.1:

```text
d_state(Omega_prim,N^v003)
  := the unique normalized fixed state of P_src.

The anchor theorem then proves, rather than defines,

d_state(Omega_prim,N^v003)=I_src/Tr_A(I_src).
```

**CUSTODY, DISCHARGED IN PLACE PER Q-931/Q-935.** The span quoted above **contains a stale status
surface**: overlay V002's pin #7, member 07 `[22882,22958)` (FIXED), span SHA-256
`65dfdedb8e8dc7a513904f4443ab215c08aa9d6c59d5a42ae75eb1395ede0662`, reading
`PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**`. It sits four bytes
after the span's start, inside the section heading block. **It is stale and not governing**: member
06's read rule at `[15736,15834)` (ROW), span SHA-256
`a9675e42c2352e692b84f5a6ad238b29da1296cc1c969ae8ca5a9eacf49396a5`, fixes
`status(member 02) := RATIFIED_FAMILY_LEVEL_BY_DOR_013`, and member 08 ratifies this file by the
digest `1be5f6a7…` that member 21 recomputed and matched. **I quote the span whole, with the stale
line inside it, rather than trim to a tidier excerpt** — trimming a ratified object so a quotation
reads better is the edit the overlay exists to bar, and the surface is pinned precisely so that it
can be carried openly instead of hidden.

Now the entailment. Member 15 at `[16571,16744)` (ANCHOR), span SHA-256
`155721b5ad376b6fb6fc15387603ba4b92d69e9da98ceb882b7d0a2a4fc08df9`:

> *"`P_src` is a normal CPTP preparation channel on the authored finite scalar **source** carrier.
> DoR-013's `d_state` solves its unique invariant state in `State(A_src)`."*

```text
THE ENTAILMENT, IN ONE STEP
  P_src is a channel ON THE SOURCE CARRIER.                        [member 15, ANCHOR span]
  d_state := the unique normalized fixed state OF P_src.           [member 07 §7.1, ratified]
  A fixed state of a channel on A_src is a state on A_src.
  => d_state's codomain IS the source sector.  NOT BY A TYPING DECISION -- BY ITS DEFINITION.
```

**No instrument chose this.** Had member 14 typed the codomain differently, it would have been wrong
about a ratified definition. The typing reports an entailment; it does not author one.

### 5.3 Both sides of the mismatch are of record

The bar is a mismatch between two declarations, and neither is an instrument's:

```text
SIDE 1  what the ratified map produces:  a state on A_src.
        Authority: DoR-013 (member 08) ratifying member 07's §7.1 definition BY DIGEST.
SIDE 2  what the port requires:  omega_phys : B -> C, pi_omega : A_C0 -> B(H_omega),
        rho_pre in trace-class(H_omega).
        Authority: member 13 [10824,11934) (FIXED), whose own status block at [11681,11929)
        (FIXED) 37a1e158… types it `U2_STATE_PORT_SPECIFIED = true | TYPE-P | premises: DoR-008`
        -- a determination made UNDER a decision of record, not on an instrument's own authority.
        Q-900 (member 20 [1816475,1816610) (ROW) 5c03575f…) is a REGISTRAR ADJUDICATION whose
        heading names the port's own bytes as what settles the section's conflict.
```

### 5.4 And the gap is not bridgeable by a convention either

A successor might grant the mismatch and ask whether some canonical transport closes it. The record
answers with derivations, not conventions:

- Member 12 `[15554,15965)` (FIXED) — the product-extension theorem: a marginal admits a continuum of
  extensions.
- Member 15 `[18033,19428)` (ANCHOR), span SHA-256
  `95cab6ad2c256f6901b00ef41b59a8fbfb57998a3bfef6b44f045b04ec743630` — purifying the DoR-013 density
  supplies no `B` marginal, no record marginal, no finite/quotient compatibility and no
  common-origin certificate, and *"unitary freedom on `K` is... exactly why purification alone does
  not determine the physical joint state."* The same span disposes of the abstract state-extension
  route: it would establish only that **some** extension exists.

### 5.5 Why this is NOT Q-900's class — the distinction the question turns on

Q-900's instrument-authored item was the **restriction pinning**: the anchor instrument's G4, member
16 `[38169,39014)` (FIXED), which computes `omega_hist := Omega_C0 compose i_B` and rules that *"no
independent `omega_B` may enter."* That is **a choice among lawful routes** for obtaining a value —
which is exactly why a registrar could rule it authored and leave the port's conditions standing.

```text
INSTRUMENT-AUTHORED (Q-900's class)   a ROUTE the instrument selected for reaching a value, where
                                      the record left the slot open.  Revisable by amendment.
RECORD-FORCED (this bar)              the TYPE of a ratified map's output, entailed by the
                                      definition the decision of record adopted by digest.  Not a
                                      route; not selected; nothing to revise without unratifying.
```

**A route can be re-chosen. A definition's entailment cannot be re-chosen without changing the
definition — and the definition is ratified by digest, so changing it would produce an object DoR-013
does not reach.** That is the same argument the member 12 overlay makes about tidy edits, arriving
here from a different direction.

### 5.6 The attack I owe this answer [Q-98]

**Attack 1.** *"`A_src` is a tensor factor of `A_C0`. Compose `d_state` with the inclusion
`i_src : A_src -> A_C0` and the codomain problem disappears."*
**Fails on direction, and the record types the failure in its smallest form.** An inclusion of
algebras induces a map on states **contravariantly**: it restricts a state on `A_C0` to one on
`A_src`, not the reverse. Member 12 §3.2 states exactly this error for the sibling case —
*"Neither `omega_out compose i_R` nor `omega_out compose i_B` is typed"* — and going the other way is
an **extension**, which §5.4 shows is non-unique.

**Attack 2.** *"DoR-013 ratifies at family level with no member ever selected. Does family-level
ratification leave the codomain open — different members, different codomains?"*
**No, and the licence says why.** Member 08 `[355,992)` (FIXED) records that the p_ch-neutrality
certificate is the licence precisely because *"every member forces the same normalized-identity
invariant state."* All members agree on the state **and** therefore on where it lives. Family-level
ratification withholds a member; it does not widen a codomain.

**Attack 3, the one I most wanted to land.** *"Member 14 is a Codex-lane audit. An audit typing
another artifact's map 'WRONG CODOMAIN' is an authored judgement."*
**It would be, if the audit had supplied either side.** It supplied neither: side 1 is member 07's
ratified definition, side 2 is member 13's DoR-008-premised port. The audit performed the comparison.
**A comparison of two declarations of record is a finding, not an authorship** — and this is the same
test B03 applied to C34 and got the opposite answer, which is what makes the two results a pair
rather than a preference.

### 5.7 The answer

```text
BAR_PROVENANCE = RECORD-FORCED
  derivation span: member 07 [22842,24541) 84a5b750… (§7.1: d_state := the unique normalized fixed
  state of P_src), read with member 15 [16571,16744) 155721b5… (P_src is a channel on the SOURCE
  carrier), and ratified by member 08 BY DIGEST.
REVISIT_SHAPE = N/A.  The near-miss CLOSES.  Nothing here argues the bar should fall; its
  provenance was the question and its provenance is of record.
```

**And this holds under every outcome at 1024**, which is why the question belongs in a diagnosis
relay rather than in a check: whether the B03 stop stands, is narrowed, or falls, the source-sector
uniqueness cannot be transported to the branch receiver, and a successor who reaches for it is
reaching for something the record's own definitions have already closed.

---

## 6. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 12's invariance theorem, placement square and K4 handoff; member 09's role contents and
  descent interface; member 10's four would-build signatures; member 11's K7 requirement; member
  13's port, its nine fields and its DoR-008 premise typing; member 14's inhabitance audit and its
  three bar rows; member 15's P_src typing and its purification/extension paragraphs; member 07's
  §7.1 definition and construction order; member 08's ratification, no-member clause and
  p_ch-neutrality licence; member 06's read rule and pin #7; member 16's G4; member 03's twelve
  basis rows; member 04's five state-naming conditions; member 02's C18/C19/C34 rows READ FOR THEIR
  PINS ONLY; Q-900; Q-98's countermodel discipline; all charter fences.

DERIVED HERE:
  the disposition vocabulary, with the COMPOSED split the assignment's own stakes sentence carries
    and the two further dispositions the bytes forced (STATE-BEARING-NOT-A-CONSUMER,
    UNDETERMINED-AT-THE-SPAN);
  the nineteen-consumer ledger and each row's disposition against its own cited span;
  the reading of the role-content span as a STATE CENSUS -- C0/U1 carry no state term, U2 carries
    the pre-state, U3 the descended measure -- which is the ledger's spine;
  the module-versus-scalar representation distinction that keeps C0 marginal-only;
  THE ASYMMETRY FINDING: this instrument can refute gauge but could never confirm it, because
    confirmation would require evaluating a consumer;
  the one-step entailment that fixes d_state's codomain from its ratified DEFINITION rather than
    from any typing decision;
  the route-versus-entailment distinction separating this bar from Q-900's class;
  the three attacks of section 5.6.

SELECTED HERE:
  nothing.  No state, member, measure, functional, candidate, density, representation, domain,
  descent map, certificate, producer, anchor member, freeze, or selector value is chosen, supplied,
  ranked, recommended, preferred, or proposed.  No basis row is reordered.  No stop is lifted, and
  the B03 stop is neither defended nor attacked.  No status moves.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  No rank, ratio, fiber or
  physical quantity was read, formed, or evaluated.  Every scale symbolic.
```

---

## 7. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.**
  - **An invariance was not widened.** Member 12 proves the RECORD MARGINAL choice-invariant; §1.1
    exists so that this is not read as "nothing downstream can see the choice", which would have
    assumed question 1's answer.
  - **COMPOSED was not flattened into INVARIANT.** `Omega_C0 compose i_B` **is** `omega_phys` under
    SM-4; a consumer built on it is load-bearing one step removed, not gauge. The split is declared
    at §1.2.
  - **A module representation was not flattened into a scalar one** (§2) — the distinction is what
    keeps C0 marginal-only, and member 14 states it in terms.
  - **A reference was not flattened into a consumer** (B05), and an unsettled span was not flattened
    into a state-consumer (B07): CONSUMES-STATE carries a citation burden and I did not discharge it
    by guessing which space B07's operators act on.
  - **A structural dependence was not flattened into an evaluated one** (§3.6). The ledger reports
    DECLARED consumption. Whether any dependence is ineliminable is a value question and is not
    answered.
  - **An entailment was not flattened into a decision** (§5.2), and a **comparison of two records was
    not flattened into an authorship** (§5.6, attack 3).
  - **A closed near-miss was not flattened into support for the stop.** §5.7 answers a provenance
    question; it is not an argument that B03 is right, and the relay takes no position on that.
  - **A stale surface was not flattened into a governing status** (§5.2) — quoted whole, flagged, and
    disposed of by the read rule rather than trimmed away.
- **BUILDER-NEVER-VERIFIES:** everything CLAIMED until the opposite-lane check. Member 02 is my own
  build and was read only for pins; I re-adjudicated nothing in it, and §5's answer was reached from
  members 07/08/14/15 rather than from B03's own reasoning.
- **THE LINE:** structure read; **no downstream value evaluated.** No consumer was run, no measure
  formed, no base family constructed, no K-row executed. §3.6 states exactly what that costs the
  verdict.
- **1024 NOT OPENED**, declared in the closure and repeated here.
- **BLIND held.** No rank read, no ratio formed, no physical quantity evaluated.
- **CUSTODY:** archive-side only. Two files written in `workspace/`, both sealed. No register, plan,
  tracker, git, commit, or push action. **NO SUBAGENT DELEGATION.**
- **BYTE AUDIT:** all 21 members rehashed at their full archive-root paths at seal time by member 21;
  all 41 spans located and computed by it; **16 FIXED spans re-verify upstream pins, 16/16 MATCH.**
  Closure declared at byte 0 with its end computed on bytes as a fixed point under a fixed-width
  field; 27-token pre-closure scan over authored prose, 0 hits.
- **RESIDUE, per Q-921:** the removed-class grep was run over authored prose (fenced blocks excluded
  — scope declared) before sealing. **ONE RAW HIT IN AUTHORED PROSE, and it is this bullet's own
  naming of the verb *reproduce* one clause above** — I count my own sentence rather than exempt it.
  **A SECOND OCCURRENCE SITS INSIDE THE FENCED CLOSURE BLOCK** — the generator's refusal rule, a live
  bounded receiver — and is outside the declared scan scope; I name it so that a checker running an
  unscoped pattern set finds **two** and finds nothing I did not disclose. **Neither is the retired
  process-history class**; this document makes no claim that any build re-runs to identical bytes and
  carries no historical subsection for that vocabulary. **CLAIMED, not clean** — the hand that wrote
  the prose ran the check, and the gate settles opposite-lane. *(A draft of this bullet asserted
  three. The scan returned one, and the miscount was mine: I predicted the loci instead of reading
  them. It is corrected here rather than quietly, for the same reason the §3.4 double-count is left
  visible — in an exhaustiveness report, a number I did not verify is worse than no number.)*
- **GENERATOR CUSTODY (Q-920/Q-924):** member 21's declared inputs are exactly members 01–21; it
  opens nothing else, and refuses to emit any table on an unreadable input, an absent **or
  ambiguous** anchor, or a FIXED span that misses its upstream digest.

---

## 8. The certificate

Per Q-913, Q-917, Q-921 and Q-930: **the certificate is the digest table.** Members 01–21 at their
full archive-root paths, and the forty-one span digests quoted in place with their computed offsets
and declared shapes. None of the retired claim classes is asserted; spans are published with digests
on both sides. **There is nothing below the digests.**

---

## 9. Final lines

```text
CLOSURE = declared-first (byte position, scan)
     byte 0; closure end 7104 under a fixed-width field; 27-token pre-closure scan over authored
     prose, 0 hits, CLAIMED per Q-921.
LEDGER = 19-CONSUMERS-DISPOSITIONED
     11 CONSUMES-STATE | 4 CONSUMES-COMPOSED-FROM-VARIANTS | 2 CONSUMES-MARGINAL-ONLY | 1
     STATE-BEARING-NOT-A-CONSUMER (B05, an independent oracle) | 1 UNDETERMINED-AT-THE-SPAN (B07,
     not typed CONSUMES-STATE because that call carries a citation burden I could not discharge) |
     0 CONSUMES-COMPOSED-FROM-INVARIANTS.  THE SPINE IS ONE SPAN: member 09 [37523,38115) e16f8b94…
     declares C0 and U1 with NO state term ("...and physical source maps only"), U2 with "positive
     normalized pre-state", U3 with the "descended contour/spacetime measure".  A double-count in
     the CONSUMES-STATE row is worked out in the open at §3.4 rather than silently re-typed.
CONSUMES_STATE = 11 (spans)
     member 13 [10824,11934) 4313fb92… (the port: omega_phys is field 1 and H_omega is the
     completion of E_C0/N_omega, so the Hilbert space itself varies with the choice) | member 10
     [8708,9198) 7b0f4b36… (the measure, "from the common-origin action/STATE/history-domain
     package") | member 10 [14191,14909) 3c0a264b… (the domain, "on a scalar physical
     representation") | member 09 [37523,38115) e16f8b94… (U2's pre-state, for B13 and condition 13)
     | member 11 [7502,8047) 829581cd… (K7 -- AND ITS LAST SENTENCE FORECLOSES THE ESCAPE:
     "Intrinsic quotient measure cannot replace this direction-bearing descent") | member 12
     [21194,21565) e76be5c6… (K4 requires an ACTUAL omega_hist, which IS omega_phys under SM-4) |
     member 12 [16822,17244) 7b1ee519… (the placement square) | member 04 [22947,23046) b7cdab99…,
     [23605,23719) fde04551…, [25313,25492) 7bbd968e…, [27780,27998) 00d7b4c8… (conditions 1, 7, 18,
     35; condition 13 at [24330,24561) 67b55bd8… carries the U2 signature as a delivery constraint).
GAUGE_VERDICT = LOAD-BEARING-AT (spans above)
     NOT GAUGE, and NOT MIXED: the two marginal-only rows are d_C0 and d_U1, and B13/B14 sit
     downstream of them in the same basis, so the choice-free region is a PREFIX of the
     construction, not a parallel track.  THE INSTRUMENT'S ASYMMETRY, REPORTED AS THE FINDING THAT
     LIMITS THIS ANSWER: a structural ledger CAN refute gauge -- one declared state-consumer
     suffices, and eleven were found -- but COULD NEVER HAVE CONFIRMED IT, because a consumer may
     declare the state and still not depend on WHICH state, and showing that requires evaluating it.
     The verdict is therefore LOAD-BEARING AT THE LEVEL OF DECLARED CONSUMPTION, which is what the
     assignment's stakes required and is all this instrument can carry.
BAR_PROVENANCE = RECORD-FORCED (member 07 [22842,24541) 84a5b750…)
     THE BAR IS NOT A TYPING DECISION AND THERE IS NOTHING TO REVISIT.  DoR-013 does not stipulate
     d_state's codomain; it ratifies a DEFINITION -- "d_state := the unique normalized fixed state
     of P_src" -- and member 15 [16571,16744) 155721b5… types P_src a channel ON THE SOURCE CARRIER.
     A fixed state of a channel on A_src is a state on A_src: THE CODOMAIN IS ENTAILED, NOT CHOSEN.
     Both sides of the mismatch are of record -- side 1 ratified by DoR-013 BY DIGEST, side 2 the
     port at TYPE-P premises DoR-008, with Q-900 [1816475,1816610) 5c03575f… resting the section's
     adjudication on the port's own bytes.  The gap is not bridgeable by convention either: member
     12 [15554,15965) e9dafa59… and member 15 [18033,19428) 95cab6ad… kill the transport by
     derivation.  NOT Q-900's CLASS: what Q-900 typed authored was a ROUTE the instrument selected
     where the record left the slot open; a route can be re-chosen, an entailment of a
     digest-ratified definition cannot.  ATTACKED THREE TIMES (Q-98): the tensor-inclusion escape
     fails on direction (state restriction is contravariant; the reverse is an extension, and
     extensions are non-unique); family-level ratification does not widen a codomain, because the
     p_ch-neutrality licence has every member forcing the SAME state; and member 14's typing is a
     comparison of two records rather than an authorship, since it supplied neither side -- THE SAME
     TEST B03 APPLIED TO C34 AND GOT THE OPPOSITE ANSWER, which is what makes the two a pair rather
     than a preference.
REVISIT_SHAPE = N/A
     The C18/C19 near-miss CLOSES.  Nothing here argues the bar should fall; provenance was the
     question.  OUTCOME-INDEPENDENT BY CONSTRUCTION: it holds whether the B03 stop stands, is
     narrowed, or falls at 1024, which is why it belongs in a diagnosis relay and not in a check.
OUTPUT_INSPECTION = NONE-CERTIFIED
     Structure read; no downstream VALUE evaluated.  No consumer run, no measure formed, no base
     family constructed, no K-row executed, no candidate named or ranked.  1024 NOT OPENED, declared
     twice.  The B03 stop is neither defended nor re-run.
CHAIN_INVOKED = false
     No unstarted paste addressed to this lane sits behind 1025; 1024 is Codex 2's.
VERB_AUDIT_SELF = CLEAN
     "Derived" occurs only in the FREEDOMS block for reading work actually performed, in sealed
     status names, and in quotations.  No selector selected, no member approached, no route chosen,
     no downstream result evaluated, no stop lifted or defended.
```

All findings CLAIMED until the opposite-lane check.

**The step to press first is §3.1's row B07.** It is the one consumer I could not type from its own
bytes, and I declined to guess. Its would-build names an interacting contour/operator family without
naming the carrier those operators act on. If that carrier is the scalar physical representation —
as B09's would-build says the endpoint domain's is — then B07 is a twelfth CONSUMES-STATE and the
choice-free prefix is narrower still. If it is the module carrier, B07 is marginal-only. **Neither
reading changes the verdict**, which is why I left it undetermined rather than argue myself into a
number; but a lane that can settle it from the contour's own sealed source should, because it is the
only row in the ledger whose disposition is open.

**The second thing to press is §3.6's asymmetry**, and it is the more consequential of the two. If
the program ever wants the *positive* result — that some part of the construction is genuinely
independent of the choice — this instrument cannot deliver it, and no refinement of it will. That
would need a licensed evaluation, and every gate that would authorise one is false.
