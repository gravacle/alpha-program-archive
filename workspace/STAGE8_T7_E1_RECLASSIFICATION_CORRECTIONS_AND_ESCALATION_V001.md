# Stage-8 T7 E1 Reclassification — Corrections of Record and Escalation V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_CORRECTIONS_OF_RECORD + PRINCIPAL ESCALATION
```

Issued on the IR/sea-kernel attack lane's return (memo sealed at
/Users/bgm/MB Work/alpha_supervision/ROUTE_MEMO_2026-07-26_ir_sea_
kernel_attack.md, 51f655a0…). No sealed artifact is altered; the
corrections below govern how the named artifacts are READ. The
construction lane independently re-verified the load-bearing algebra
(record moments, the second-difference identity, the tau_R phase
saturation, the zeta constants, the kernel modulus) before sealing.

## Corrections of record

```text
C1. THE SEA COVARIANCE IS PINNED EXACTLY, NOT BY CLASS.
    MAJORANT_LEMMA0_PROOF_DRAFT_V001 §3.3 ("the sealed corpus pins only
    the decay CLASS of the sea kernel") is over-pessimistic. The sealed
    state-regulator result pins C = 1_(-inf,0)(h_0), C(p) = (I -
    alpha·p-hat)/2, whose position kernel is closed form:
      C(r) = (1/2) delta^3(r) I - i alpha·r / (2 pi^2 |r|^4),
    off-diagonal modulus exactly 1/(2 pi^2 |r|^3), homogeneous degree
    -3, odd (Calderon-Zygmund). E1 is therefore reclassified from
    "input absent" to "LEMMA UNPROVEN ABOUT A FULLY KNOWN OBJECT".

C2. K_sea AS DEFINED IS DIVERGENT, NOT UNCERTIFIED.
    Under the spec-header's own carrier-blindness requirement the
    Phase-1 functional's trace-norm of (sea) x (sharp diamond-slice
    localizer) diverges as a power (~ |D_t| Lambda^3); it is finite
    only on a fixed carrier, which is forbidden. The Phase-1 eta(eps)
    architecture must be REPLACED, not certified.

C3. NC3 DOES NOT CORROBORATE E1 FROM BELOW.
    STAGE8_T7_MAJORANT_PHASE2 status/draft states that NC3's divergence
    "corroborates the E1 named block from below". CORRECTED: NC3 is a
    ONE-LINE control and its exact 24·H_K divergence is a one-line
    counting artifact. The lowest connected two-cell cumulant is a
    TWO-LINE object (fermion-loop structure of the quasifree
    determinant: the first-order term has no cross term). NC3 remains a
    correct PASS of its own charter; it says nothing about E1.

C4. THE POINTER WEIGHTS: TWO MOMENTS VANISH, AND THE CANCELLATION IS
    EXACTLY SATURATED AT FULL tau_R.
    m0 = m1 = 0 exactly; the completed Kraus operator IS the symmetric
    second difference -(1/4)[f(+sqrt2) - 2 f(0) + f(-sqrt2)]; all Dyson
    terms with fewer than two record insertions per CTP branch are
    annihilated, and the bare a-linear tadpole vanishes identically
    (also killed independently by the odd spinor trace -2 p-hat_x).
    BUT at full tau_R the record phases are exactly (+1,-1,-1)
    (lambda·tau_R in {0, +-pi}), so sum_lambda w_lambda·phase_lambda = 1
    = sum_lambda |w_lambda|: Phase-1's l1-mass step is TIGHT, not lossy,
    and the zero-sum weights buy NO decay improvement. Worse, they
    promote the SHARP causal-ball projector to the leading in-cell
    object.

C5. THE LONG-DISTANCE DIRECTION HAS MARGIN.
    Two-line pair activities give R^-6; anchored sums converge with
    exact constants 24 zeta(4) + 2 zeta(6) (3-D) and 64 zeta(3) +
    16 zeta(5) (4-D). Under refinement the action-density form absorbs
    the cell scale with an L-INDEPENDENT eta^2 = eps^2/2. Clustering
    was never the missing ingredient, and no clustering axiom is
    approached by any candidate route (F-1 untouched).

C6. THE BLOCK IS CONFINED TO n = 1.
    Specifically to the pairing of the marginal degree-(-3) sea with the
    SHARP localizers M(t) = Q 1_{|x|<=r(t)} Q of the sealed Phase-A A1
    construction (equal-time localization of the 3-D massless Dirac sea
    fails Shale-Stinespring; only two-time / scattering-type objects,
    where the cell time integration supplies decay, can work).
```

Net effect: the obligation named "the infrared structure of the massless
charged sector" is replaced by a smaller, better-named target — the
single-cell coincidence limit against a sharp record localizer.

## Escalation to the principal (two questions; no lane may answer them)

```text
Q1 (may retire an escalation already on your desk).
    Does the T7III_MAJORANT_DERIVED arm GENUINELY require O7?
    The executed M3 schema derives its uniformity over the full D3
    quantifier from R1-R4 re-aggregation plus cellulation-blind
    constants, WITHOUT consuming any Route-T output. If that holds,
    Route Q carries the whole quantifier alone: O7 refutes only Route
    T's transport MECHANISM, and the sealed O7 escalation
    (pin-the-family vs carry-underived) may be unnecessary. E1 and O7
    are two encodings of one requirement — refinement uniformity — and
    refuting the Route-T encoding does not refute the Route-Q encoding.
    This must NOT be settled by amending a spec.

Q2 (definition-level, Axis-3-adjacent).
    Routes IR-B and IR-C can terminate in the finding that the sealed
    SHARP record localizer M(t) is the obstruction. Softening it is a
    change to a sealed Phase-A A1 DEFINITION — it changes what the
    record cell IS. If a route reaches that point, does the principal
    wish to (i) treat the sharp localizer as inviolable and accept a
    certified-divergent E1 as a refutation of the current activity
    architecture, or (ii) consider a smoothed-localizer successor as a
    scoped definition decision? No lane may choose.
```

## Next step (held pending Q1/Q2)

A successor spec carrying the corrected baseline (C1-C6 as its derived
starting point), the three candidate routes with their named
graceful-block witnesses, and — per the lane's own recommendation and
this lane's calibration record — IR-C ranked first for information
yield and IR-A first for certification yield. Not authored yet.

## Protected status

```text
E1_reclassified = true
connected_linked_cluster_density_proved = false
volume_uniform_zero_free_neighborhood_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
