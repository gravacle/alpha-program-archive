CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = CLAIMED
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_OUTPUT_SCAN = 0 hits for this artifact and its sidecar
READ_SET = exactly the 18 content-addressed members below
UNDECLARED_SOURCE_VERDICT_WEIGHT = forbidden
ADOPTION_EXECUTED = false
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 02 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 03 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 flattening guard |
| 04 | `relay_inbox/RELAY_PASTE_919_INSTRUMENT_V002_CODEX2_V001.md` | `4995c9484c4134b5fab1b75c42faa2e89137603843560192f7dce40ccc64c0f9` | assignment |
| 05 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V001.md` | `c792ff4bb02d8406c6e02864218cbad0b14d3badddae01762d5d8d2214c12060` | finite-delta base |
| 06 | `STAGE8_AXN_INSTRUMENT_CROSSCHECK_DARIO_V001.md` | `77257e5fe335d6ffedf2a0e8999dcf05865896c71ea1391987fa2d157dcd9887` | opposite-lane four-item revision order |
| 07 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, `[18275,20831)` | `60939033df33a5dac50958182d92ad0019ffa3993950555306421971fd0c4821` | A0–A3 table and exact finite certificate format |
| 08 | `STAGE8_TASK4A_ORIGIN_FED_REFINEMENT_TOWER_GENOMEGA_PORT_TYPECHECK_DETERMINATION_CODEX_LANE2_V001.md`, `[738,989)` | `9bcee6a4472597fecaebd445dbadce9d8265274ce44f60abe256d5803ab7cf38` | `rho_S,[A]`, rank ratio kept symbolic |
| 09 | `STAGE8_TASK4A_ORIGIN_FED_REFINEMENT_TOWER_GENOMEGA_PORT_TYPECHECK_DETERMINATION_CODEX_LANE2_V001.md`, `[8215,9375)` | `da6a9e94797d8e126e89ba0e7bf86c91a11147432af31f11a1fb5a1ea3029a7c` | fixed-A0-fiber neutrality and singleton state image |
| 10 | `STAGE8_AXN_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md`, `[15056,16394)` | `d8374b1454d8cd22d76f98434a3fd0e76a92d64e985551c5b99fc6099fbfc1dd` | closed equivalence/naturality record pattern; builder's prior pattern has no self-verifying weight |
| 11 | `STAGE8_AXN_B0_ACCEPTANCE_CROSSCHECK_DARIO_V001.md`, `[5839,6853)` | `bb919ede5d7afc4d9f95b7442386c27e2d80d8a1f4914353ae5a89eb2e6cdaeb` | opposite-lane confirmation that the pattern is satisfiable and instance-free |
| 12 | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md`, `[12114,13268)` | `fcf8d90eecb442ce114689d2c73bb546b32fe769a14de28c5e32dc8365ad8ce6` | identity-Dirac and product-Haar K4 controls |
| 13 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md`, `[6431,9107)` | `5e79ba2d662e871493babd1631859a196aee2dcdd554c53df186a63de67ab30f` | `B≅C(Y)`, countable circle product, Haar/trivial-character seeds |
| 14 | `STAGE8_AXN_FORCING_AUDIT_CROSSCHECK_DARIO_V001.md`, `[9104,9996)` | `494ec4c206def66d58cb4dece87c15cc2e6a285c734df45f4ae1875b20324fd3` | prospective freeze, record-native supersession, stop rule |
| 15 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-814`, `[1662214,1663709)` | `b06011f470eb36a751fe56b30ce004b2aae0d9cf1e342a0cd1839ab7a98ab20d` | living-file entry pin: producer and S1 receiver |
| 16 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-817`, `[1668068,1669463)` | `54a425a36f00fcc9b4f18a5f0608e0cf6b70919f997c7e9fa8adbaaaf6b6170f` | living-file entry pin: S1 closes; S2/S3/S4 unblock |
| 17 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-818`, `[1669464,1670876)` | `ca9a0eedf549921a69fc2164cea889191c462b7948b36356ce2ddee7f91795af` | living-file entry pin: complete acceptance frame and four-line consequence |
| 18 | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-825`, `[1684580,1686933)` | `bea14ab4130b9936bc1e1a5cb18da748a6370f1141191c993069f2cee92bedfe` | living-file entry pin: four revisions and lawful carry-forward findings |

CLOSURE_DECLARATION_END

# STAGE 8 — AXN JOINT-ANCHOR ADOPTION INSTRUMENT — V002
## CODEX 2 LANE — RELAY 919 — `[PLAN:AXN-BUILD-C17]` — [CLAIMED]

Date: 2026-08-10  
Status: **DRAFT FOR PRINCIPAL DECISION — NOTHING ADOPTED HERE**.  
Scope: a complete two-horn instrument for either approving or declining one prospective,
certificate-quantified joint-state anchor act. The document specifies what the principal would have
to enter and what the acceptance run would execute. It chooses no carrier, A0 rank fiber, tag class,
channel, state, marginal, rank, measure, control class, or outcome. Every substantive headline is
**CLAIMED** pending opposite-lane or registrar disposition.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Load-bearing byte receivers [CLAIMED]

Each span digest was recomputed from the sealed cleanroom bytes. The register is living: only the
four exact entry spans in the closure carry weight; no whole-file register digest is asserted.

| Receiver | Source, half-open span, span SHA-256 | Instrument use |
|---|---|---|
| exact A3 format | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, `[18275,20831)`, `60939033df33a5dac50958182d92ad0019ffa3993950555306421971fd0c4821` | finite-visible, pre-output suite: basis, exact matrix, factorization/inverse, channel/tag certificates |
| A0 state/fiber | `STAGE8_TASK4A_ORIGIN_FED_REFINEMENT_TOWER_GENOMEGA_PORT_TYPECHECK_DETERMINATION_CODEX_LANE2_V001.md`, `[738,989)`, `9bcee6a4472597fecaebd445dbadce9d8265274ce44f60abe256d5803ab7cf38`; and `[8215,9375)`, `da6a9e94797d8e126e89ba0e7bf86c91a11147432af31f11a1fb5a1ea3029a7c` | `rho_S,[A]=I_A/Tr_A(I_A)` is one state per fixed A0 rank fiber; ratio remains symbolic |
| equivalence/naturality pattern | `STAGE8_AXN_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md`, `[15056,16394)`, `d8374b1454d8cd22d76f98434a3fd0e76a92d64e985551c5b99fc6099fbfc1dd`; opposite-lane check `[5839,6853)`, `bb919ede5d7afc4d9f95b7442386c27e2d80d8a1f4914353ae5a89eb2e6cdaeb` | full-object relation, induced maps, independent classifier, commuting/naturality replay, no instance selected |
| K4 controls | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md`, `[12114,13268)`, `fcf8d90eecb442ce114689d2c73bb546b32fe769a14de28c5e32dc8365ad8ce6` | identity-supported Dirac passes algebraically; product Haar fails on the charged fiber; neither gains provenance |
| history carrier | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md`, `[6431,9107)`, `5e79ba2d662e871493babd1631859a196aee2dcdd554c53df186a63de67ab30f` | `B≅C(Y)`; `Y` is built from a countable product of circles; Haar and trivial-character states displayed |
| prospective/superseding pattern | `STAGE8_AXN_FORCING_AUDIT_CROSSCHECK_DARIO_V001.md`, `[9104,9996)`, `494ec4c206def66d58cb4dece87c15cc2e6a285c734df45f4ae1875b20324fd3` | freeze before consumers; record-native supersession only prospectively; negative runs never substitute for supply |
| denominator producer | `QUESTIONS_SETTLED_REGISTER_V001.md#Q-814`, `[1662214,1663709)`, `b06011f470eb36a751fe56b30ce004b2aae0d9cf1e342a0cd1839ab7a98ab20d` | joint-state producer is the missing first gear and hands S1 its member through `res_B` |
| denominator cascade | same living file `#Q-817`, `[1668068,1669463)`, `54a425a36f00fcc9b4f18a5f0608e0cf6b70919f997c7e9fa8adbaaaf6b6170f`; `#Q-818`, `[1669464,1670876)`, `ca9a0eedf549921a69fc2164cea889191c462b7948b36356ce2ddee7f91795af` | exact consequence: S1 closes and S2/S3/S4 unblock after the complete producer/acceptance run |
| revision authority | same living file `#Q-825`, `[1684580,1686933)`, `bea14ab4130b9936bc1e1a5cb18da748a6370f1141191c993069f2cee92bedfe` | exactly four revisions; DB/SYM strengthenings and limit-square addition retained |

## 2. Decision envelope — no decision embedded [CLAIMED]

The principal receives one closed choice with two lawful dispositions. The instrument is invalid if
an `APPROVE` entry leaves any required field open.

```text
JointAnchorDecision.v002 := {
  schema: const "rd22.axn-joint-anchor-decision.v002",
  disposition: enum { APPROVE, DECLINE },                    [PRINCIPAL ENTRY]
  decision_artifact_sha256: lowercase_sha256,                [PRINCIPAL ENTRY]
  decision_time: declared timestamp,                         [PRINCIPAL ENTRY]
  governing_instrument_sha256: sha256(this sealed instrument),
  scope: const "joint-state producer into State(A_C0)",

  if disposition == DECLINE:
    decline_scope: const "this adoption instrument only",
    derivation_route_status: const "remains open",
    downstream_movement: const "none",

  if disposition == APPROVE:
    joint_carrier_entry: JointCarrierEntry.v002,              [PRINCIPAL ENTRY]
    anchor_tag_class_entry: nonempty subset {BI,DB,SYM},      [PRINCIPAL ENTRY]
    anchor_content_entry: exact tag equations below,          [PRINCIPAL ENTRY]
    certificate_manifest_sha256: lowercase_sha256,            [PRINCIPAL ENTRY]
    prospective_freeze_receipt_sha256: lowercase_sha256,      [PRINCIPAL ENTRY]
    supersession_rule: const "DERIVATION_SUPERSEDES_PROSPECTIVELY",
    post_freeze_mutation: const "FORBIDDEN",
    member_selection: const "FORBIDDEN"
}
```

No default is supplied for `disposition`, carrier, A0 rank fiber, tag class, tag content, trace,
control classification, or supersession relation. `APPROVE` is a whole act: all fields,
certificates, and exact hashes must seal together. `DECLINE` neither proves the anchor false nor
narrows the derivation route.

## 3. Joint carrier entry and the disclosed price [CLAIMED]

V003's theorem works only after its authored scalar carrier and trace are fixed. The joint version
must disclose the corresponding physical price rather than smuggling a history state into a
“reference trace.” The principal's `JointCarrierEntry.v002` must contain:

```text
JointCarrierEntry.v002 := {
  carrier_schema: const "rd22.axn-joint-carrier.v002",
  declared_joint_algebra: content-addressed realization of sealed A_C0,
  a0_rank_fiber: content-addressed A0RankFiber.v001,           [PRINCIPAL ENTRY; BLIND]
  stage_index_and_limit_rule: exact finite/cylindrical system and limit target,
  joint_identity: I_C0 (or exact stage family I_N),
  scalar_pairing_or_trace: faithful declared pairing with normalization convention,
  history_pairing_control_certificate: HistoryPairingControl.v001,
  source_factor_embedding: i_src,
  record_factor_embedding: i_R,
  history_factor_embedding: i_B,
  joint_superselection_map: E_joint,
  restriction_and_limit_certificates: exact content-addressed list,
  equivalence_scope: exact representation-change invariance statement,
  physical_price: explicit statement of every authored carrier/pairing/fiber datum
}

A0RankFiber.v001 := {
  schema: const "rd22.axn-a0-rank-fiber.v001",
  fiber_label: [A],
  source_carrier_sha256: lowercase_sha256,
  I_A_sha256: lowercase_sha256,
  P_0_sha256: lowercase_sha256,
  P_ch_sha256: lowercase_sha256,
  structural_rank_record_sha256: lowercase_sha256,
  entry_freeze_receipt_sha256: lowercase_sha256
}

HistoryPairingControl.v001 := {
  schema: const "rd22.axn-history-pairing-control.v001",
  entered_pairing_sha256: lowercase_sha256,
  induced_history_functional_sha256: lowercase_sha256,
  control_relation: enum {
    IDENTITY_SUPPORTED_DIRAC_EQUIVALENT,
    PRODUCT_HAAR_EQUIVALENT,
    NEITHER_EQUIVALENCE_PROVED
  },
  exact_equivalence_or_nonclassification_certificate_sha256: lowercase_sha256,
  pre_output_timestamp: declared timestamp
}
```

### 3.1 A0-fiber resolution [CLAIMED]

At the entered fiber only, the sealed source state is

```text
rho_S,[A_entered] := I_A / Tr_A(I_A).
```

This resolves both readings returned by 918. The source-marginal clause no longer asks for an
unspecified one-fiber reproduction, and it does not demand the generally incompatible condition of
reproducing every admitted fiber simultaneously. It asks for exactly the content-addressed fiber the
principal entered before output. The builder selects no fiber and evaluates no rank ratio.

The entry point is load-bearing because
`p_[A]=Tr_A(P_ch)/Tr_A(I_A)=r_ch/(r_0+r_ch)` is a symbolic descendant of the same fiber. A target-aware
fiber choice could therefore import a rank ratio without writing the ratio as a field. JAC-03 binds
the fiber record, its choice receipt, and every candidate/admission predicate to a pre-output scan for
`p_[A]`, `r_0`, `r_ch`, “rank ratio,” “branch weight,” response/coupling/residual/target vocabulary,
and the two named K4 control classes. Structural ranks may occur only inside the declared fiber
record; no ratio, preference, or downstream predicate may select the fiber.

### 3.2 Pairing/trace and the named G6 controls [CLAIMED]

If the pairing/trace on the commutative history factor already encodes a preferred measure, that
fact must be stated as authored input. It may not be called derived merely because it is written as
a trace. A reference-density balance rule is inadmissible unless its reference density is itself
independently derived or expressly included in the principal's adoption price. This is the joint
version of V003's reverse-A2 self-kill.

The required control certificate connects that authored price to G6 before the act is entered:

- `PRODUCT_HAAR_EQUIVALENT` discloses that the induced history functional is the product-Haar control
  on the countable circle-product carrier. The sealed control removes a nontrivial charged character
  under full-fiber integration and therefore fails K4 at that receiver.
- `IDENTITY_SUPPORTED_DIRAC_EQUIVALENT` discloses algebraic K4 compatibility only. It supplies no
  common-origin provenance and cannot by itself pass K7.
- `NEITHER_EQUIVALENCE_PROVED` makes no control prediction; the actual generated member must undergo
  G6 without a shortcut.

This comparison is a disclosure, not a selector and not advocacy. JAC-03 forbids using either
control class to choose a carrier, trace, fiber, candidate, or tag. The two names are lawful only in
the designated `HistoryPairingControl.v001` disclosure and the preregistered G6 control block.

## 4. Anchor statement the principal would freeze [CLAIMED]

### 4.1 Common joint-producer family [CLAIMED]

For the exact joint carrier and A0 rank fiber entered by the principal, define the declared
traceless, superselection-fixed tangent space

```text
Delta_0^joint := {
  Delta : Tr_joint(Delta)=0 and E_joint(Delta)=Delta
}.
```

The adoption would freeze **the whole family**, never one convenient member:

```text
G_joint := {
  Phi_joint : joint normal-state/density carrier -> same carrier :
    Phi_joint is normal and CPTP;
    Phi_joint is joint-charge covariant;
    Phi_joint E_joint = E_joint Phi_joint;
    Phi_joint has one normalized mixing invariant joint state Omega_Phi;
    ker(Phi_joint restricted to Delta_0^joint) = {0};
    Cert_IF_joint(Phi_joint) is an exact replayable certificate;
    one tag A is frozen in ANCHOR_TAG_CLASS_ENTRY;
    Cert_A_joint(Phi_joint) is exact and replayable;
    every finite/cylindrical restriction and limit square commutes
}.
```

The family definition contains no `Omega_Phi` coordinate, history measure, `K4` result, response,
coupling, physical residual, measured target, rank ratio, or downstream verdict. Candidate
enumeration, carrier, A0 fiber, tag class, tag certificate, and all admission predicates freeze
before the invariant-state solve.

### 4.2 Exact tag conditions — principal entry, not builder selection [CLAIMED]

The principal may enter any nonempty tag class allowed below, but the entered class is then frozen
whole. Every admitted tag must prove the same joint state at the fixed carrier and entered fiber.

| Tag | Exact template condition | Required implication |
|---|---|---|
| `BI` — leading case | `Phi_joint(I_C0)=I_C0` together with the common trace-preserving and input-faithful conditions | the normalized joint identity/tracial functional is invariant; mixing uniqueness makes it the only joint state |
| `DB` — alternate | `Phi_joint=Phi_joint^dagger` for the **already entered** joint pairing/trace; no free reference density | trace preservation implies the identity is fixed, reducing to the BI theorem |
| `SYM` — alternate | a finite/content-addressed unitary action `G_joint` is frozen; `Comm(G_joint)=C I_C0`; `Phi_joint` is `G_joint`-covariant | fixed-state uniqueness makes the state invariant under the action; scalar commutant plus normalization gives the same state |

```text
ANCHOR_TAG_CLASS_ENTRY = [PRINCIPAL ENTRY: nonempty subset of {BI,DB,SYM}]
ANCHOR_CONTENT_ENTRY   = [PRINCIPAL ENTRY: exact carrier/fiber-bound equations/certificates]
BUILDER_SELECTED_TAG   = false
BUILDER_SELECTED_STATE = false
BUILDER_SELECTED_FIBER = false
```

DB is void if its pairing imports an undisclosed reference state. SYM is void if its action is
incomplete, its commutant is nonscalar, or covariance fails. BI is void if identity and trace are
not typed on the actual joint carrier. The class is void if any tag produces a different invariant
state or if its advertised nonempty member class is empty.

### 4.3 State follows only after freeze [CLAIMED]

The frozen construction order is:

```text
J0 seal the principal decision, joint carrier, A0 fiber, tag class, candidate grammar, and certificates;
J1 verify carrier/fiber/restriction/limit typing;
J2 enumerate candidates and verify Cert_IF_joint plus the tag certificate;
J3 close the candidate set; expose no downstream result;
J4 solve each admitted candidate's unique invariant-state problem;
J5 replay the tag-specific anchor theorem;
J6 prove the family-wide singleton image and finite/limit compatibility;
J7 construct the three marginals and common-origin trace;
J8 only then expose Omega_C0 and res_B(Omega_C0) to the acceptance gauntlet.
```

No fixed-point solve occurs in this instrument. The display is a future proof order, not execution.

## 5. Required certificate manifest — 14 certificates [CLAIMED]

An approval is incomplete unless one content-addressed manifest carries all fourteen certificates.

| ID | Certificate | Failure-capable receiving test |
|---|---|---|
| `JAC-01` | **carrier and type** — exact `A_C0` realization, entered A0 fiber, identity, pairing/trace, factor embeddings, `E_joint`, domains, restriction and limit maps | reject a source-only, record-only, co-located, wrong-fiber, or wrong-representation object |
| `JAC-02` | **output-free primitive tuple** — no joint state, history measure, response, K4 value, rank ratio, or target occurs as primitive data | reject an answer carried as an input field |
| `JAC-03` | **target independence** — fiber entry, candidate grammar, and every admission predicate fixed without downstream vocabulary; scan names `p_[A]`, `r_0`, `r_ch`, rank ratio, branch weight, identity-supported Dirac, product Haar, K4–K7, response, coupling, residual, and measured-target forms | fixed-string, normalized-name, hyphen/spacing, and semantic-receiver scan over the complete frozen manifest; control names permitted only in the designated disclosure/G6 fields; reject any selection predicate that receives them |
| `JAC-04` | **failure-capable anchor** — at least one admitted candidate passes and at least one structurally comparable candidate fails; exact witnesses supplied | reject tautological, impossible, or cannot-fail membership |
| `JAC-05` | **no hidden state coordinate** — tag coefficients, DB pairing, SYM action, carrier trace, fiber entry, and control classification contain no undisclosed freely chosen density/measure or target-aware selector | reverse-A2 inspection; any adopted measure-like datum must be priced openly; control relation must be proved from the entered trace |
| `JAC-06` | **joint positivity and normalization** — every generated `Omega_C0` is a state on the declared carrier | kernel/positivity and unit tests at finite stages and limit |
| `JAC-07` | **entered-fiber three-marginal certified square** — source, record, and history restrictions bind at `i_src`, `i_R`, `i_B`; the source restriction equals `rho_S,[A_entered]=I_A/Tr_A(I_A)` on the content-addressed entered A0 fiber; record reproduces its sealed input; history is the generated output | compare all three restrictions at the entered fiber; reject an unspecified-fiber claim, an all-fibers demand, a wrong-fiber state, or an independently attached marginal |
| `JAC-08` | **finite/cylindrical/quotient compatibility** — restrictions commute with embeddings, `q_N`, sequential extension, branch conventions, and the limit | one failed square rejects the candidate |
| `JAC-09` | **common provenance** — carrier, fiber, state, ready data, law, record state, history state, domains, and required effects descend from one frozen trace | reject product assembly by declaration |
| `JAC-10` | **no supplementation** — no field is added after J3 and no product factor is independently chosen after seeing an output | hash-diff J3 against final trace; any new datum fails |
| `JAC-11` | **coverage/reopening** — enumerate the admitted family with completeness certificate, or state a bounded class and explicit reopening trigger | reject one successful member reported as forced |
| `JAC-12` | **prospective freeze** — decision and all fields seal before K4–K7 or any downstream consumer executes; stop-rule item 7 bars retrospective fitting | timestamps, input-root hashes, and unread-output receipt |
| `JAC-13` | **supersession by derivation** — a later record-native derivation at the same receiver automatically supersedes the authored act only after `SupersessionEquivalenceNaturality.v001` passes against the adopted act; provenance and earlier uses remain logged | reject absent/partial equivalence, silent replacement, backdating, result-driven mutation, or a relation not replayed against the adopted act |
| `JAC-14` | **exact certificate format** — every `Cert_IF_joint` and `Cert_A_joint` instantiates `JointAnchorCertificateSuite.v001`: finite-stage basis of `Delta_0^joint`, exact restriction matrices, exact factorization/inverse, CPTP/charge/superselection certificates, fixed-space/mixing certificate, tag certificate, and restriction/limit-square receipts | reject any promise of “exact/replayable” that lacks the closed suite, canonical bytes, exact matrices, or a replay entry point; suite must be finite-visible and frozen pre-output |

### 5.1 JAC-14 closed suite format [CLAIMED]

```text
JointAnchorCertificateSuite.v001 := {
  schema: const "rd22.axn-joint-anchor-certificate-suite.v001",
  carrier_sha256: lowercase_sha256,
  a0_rank_fiber_sha256: lowercase_sha256,
  finite_stage_inventory: exhaustive ordered content-addressed list,
  for_each_stage: {
    delta0_basis: exact ordered basis,
    phi_restriction_matrix: exact matrix in that basis,
    factorization_and_inverse: exact replay object,
    cptp_certificate: exact replay object,
    charge_covariance_certificate: exact replay object,
    superselection_commutation_certificate: exact replay object,
    fixed_space_and_mixing_certificate: exact replay object,
    anchor_certificate: exactly one closed BI / DB / SYM variant
  },
  restriction_and_limit_square_receipts: exhaustive ordered list,
  canonical_serialization: const "canonical bytes; closed fields; no extras",
  replay_entry_point_sha256: lowercase_sha256,
  suite_sha256: lowercase_sha256,
  frozen_pre_output_receipt_sha256: lowercase_sha256
}
```

The finite matrix is stage-wise; the limit conclusion is received only by the separately displayed
restriction/limit squares. No finite certificate is promoted into an unproved completed-carrier
identity.

### 5.2 JAC-13 supersession equivalence to the adopted act [CLAIMED]

```text
SupersessionEquivalenceNaturality.v001 := {
  schema: const "rd22.axn-supersession-equivalence-naturality.v001",
  adopted_act_sha256: sha256(principal decision + carrier/fiber/anchor manifest),
  derived_artifact_sha256: lowercase_sha256,
  receiver_identity: exact same joint-state-producer receiver,
  Eq_anchor: declared equivalence relation on complete adopted/derived presentations,
  relation_laws: reflexive / symmetric / transitive replay objects,
  carrier_maps: exact forward/backward maps with inverse/factorization certificates,
  preserved_structure: {
    A_C0, [A], identities, pairing/trace, i_src, i_R, i_B, E_joint,
    stage/restriction/quotient/limit maps, tag equations, certificate semantics
  },
  producer_intertwining:
    F Phi_adopted = Phi_derived F and G Phi_derived = Phi_adopted G,
  state_and_marginal_naturality:
    F_*(Omega_adopted)=Omega_derived and all three marginal squares commute,
  gauntlet_input_invariance: exact equality/equivalence for every G0-G8 receiver,
  classifier: independently fixed representation-equivalence oracle,
  certificates: equivalence-law + typing + commuting/naturality replay objects,
  downstream_consumer_inventory: exhaustive content-addressed list,
  supersession_time: after derivation seal and all checks, before future use,
  historical_uses_retained: const true
}
```

Automatic supersession fires only when every field is displayed and every replay passes **to the
adopted act itself**. A non-equivalent later proposal requires a new principal act. A failed run
cannot manufacture equivalence, and no result may be deleted or backdated.

## 6. Proof obligations after an approval [CLAIMED]

The approval supplies an authored input class; it does not supply the theorem. The producing lane
must discharge all obligations below without changing the frozen act.

| ID | Proof obligation | Required conclusion |
|---|---|---|
| `JPO-01` | carrier/fiber realization | every symbol and map in `JointCarrierEntry` is total and typed on the actual joint lineage and entered A0 fiber |
| `JPO-02` | tag-by-tag anchor theorem | BI, every admitted DB presentation, and every admitted SYM presentation each make the same declared normalized joint identity/tracial functional invariant |
| `JPO-03` | mixing uniqueness | the invariant state in `G_joint` is unique for every admitted candidate |
| `JPO-04` | input-faithfulness | the exact restriction of `Phi_joint` to `Delta_0^joint` has zero kernel; every replacement channel fails |
| `JPO-05` | nonempty/failure-capable family | exact passing and failing witnesses exist independently of downstream results |
| `JPO-06` | singleton-image theorem | `{inv(Phi_joint): Phi_joint in G_joint}` is exactly one state at the fixed carrier/fiber, across all admitted tags and transient fibers |
| `JPO-07` | stage/limit compatibility | the finite/cylindrical invariant states form one compatible family and define `Omega_C0 in State(A_C0)` |
| `JPO-08` | entered-fiber three-marginal theorem | `Omega_C0 compose i_src`, `Omega_C0 compose i_R`, and `Omega_C0 compose i_B` exist; source equals sealed `rho_S,[A_entered]`, record equals its sealed input, and history is generated |
| `JPO-09` | common-origin/no-supplement theorem | all outputs are functions of the J3-frozen trace; no desired marginal is appended |
| `JPO-10` | representation/equivalence honesty | allowed carrier or tag presentations preserve the state verdict and every gauntlet input |
| `JPO-11` | coverage theorem | the declared family coverage or bounded-class/reopening statement is true at the sealed receiver |

The anchor theorem's core statement is preregistered as:

```text
FOR EVERY Phi_joint IN G_joint:
  inv(Phi_joint) = Omega_anchor,C0;

StateImage(G_joint) = {Omega_anchor,C0};

Omega_anchor,C0 is proved at J4-J6 from the J0-J3 frozen carrier, fiber, and anchor,
not supplied as a coordinate and not selected after any acceptance output.
```

## 7. Full ordered acceptance gauntlet [CLAIMED]

The run is fail-closed and ordered. A failure records the first failed receiver and stops; it does
not reopen the frozen candidate class.

### G0 — decision and pin completeness [CLAIMED]

1. Validate the decision schema and exact disposition.
2. On `DECLINE`, record no adoption and stop with no movement.
3. On `APPROVE`, require every principal entry, all 14 certificate-manifest rows, source pin,
   carrier/fiber inventory, control-comparison certificate, and prospective-freeze receipt.
4. Refuse an approval containing an unfilled field, non-content-addressed object, or post-freeze
   mutation.

### G1 — seven-item no-go benchmark [CLAIMED]

Run, in order:

1. complete structural predicate fixed before the state solve;
2. every pure replacement channel excluded;
3. affine state-valued freedom excluded, not merely supplemented by transient action;
4. nonempty passing family exhibited;
5. singleton invariant-state image, or an exact physical-equivalence theorem for residual members;
6. joint charge covariance, `E_joint` commutation, law fidelity, and finite/cylindrical authority;
7. every new physical price stated openly.

Any failure stops. A 7/7 source-side precedent is not a 7/7 joint result; the joint carrier and joint
certificates must replay independently.

### G2 — anchor theorem and certificate replay [CLAIMED]

Replay `JPO-01` through `JPO-11` and `JAC-01` through `JAC-14`. Run every admitted tag class, not
only BI. Compare their state outputs and require a singleton image. Perturb identity/unitality,
input-faithfulness, the DB adjoint equation, the SYM commutant/covariance equation, the A0 fiber,
restriction maps, and freeze order; each advertised control must bite.

### G3 — SM-1 through SM-8, unchanged [CLAIMED]

| Order | Row | Required receiving verdict |
|---:|---|---|
| 1 | `SM-1` typed source/codomain | the frozen common-origin input lands in `State(A_C0)` or a complete `StatePort_U2_008`; totality proved |
| 2 | `SM-2` joint positivity/normalization | the actual output, not a source subcheck, is a positive normalized joint state |
| 3 | `SM-3` record marginal | restriction along `i_R` reproduces the admitted outgoing-record state |
| 4 | `SM-4` history binding | `omega_phys=Omega_C0 compose i_B` is a construction output, not an independently chosen attachment |
| 5 | `SM-5` finite/quotient compatibility | all finite history cylinders, `q_N`, embeddings, branch conventions, and limit squares commute |
| 6 | `SM-6` common primitive provenance | carrier, law, record state, history state, domains, and required effects descend from the frozen trace |
| 7 | `SM-7` target independence/no supplementation | no K4–K7 or downstream result entered candidate generation or added a field |
| 8 | `SM-8` coverage | complete family enumeration/certificate, or bounded scope plus reopening trigger, is displayed |

No source-only subcheck counts as an SM row. Any row failure stops the core acceptance run.

### G4 — `res_B` construction [CLAIMED]

Only after eight SM passes, compute structurally

```text
omega_hist := res_B(Omega_C0) := Omega_C0 compose i_B.
```

Verify positivity, normalization, declared representation, and equality to the `JAC-07` history
marginal. The slice `E_out` is not a substitute and no independent `omega_B` may enter.

### G5 — S1 K1 through K3 preconditions [CLAIMED]

| Row | Test on the actual `omega_hist` descendant |
|---|---|
| `K1` | use the already derived regular-Borel carrier, sigma-algebra, and bounded-total-variation extension class; do not treat `A_N` as their source |
| `K2` | construct `d nu_N=A_N d lambda_N` from the state restrictions and prove countable additivity/reproduction at that receiver |
| `K3` | replay `j_NM^{Q,*} A_M=A_N` as zero-section naturality; do not call it projectivity |

### G6 — K4 fiber test on the generated member [CLAIMED]

Before the full-fiber computation, replay `HistoryPairingControl.v001` against the exact entered
pairing and the generated `omega_hist`. The classification cannot replace the actual test.

Let `lambda_N` be the finite history bases obtained from `omega_hist`, and let
`nu_N=A_N lambda_N`. For every `N<=M`, require

```text
(pi_MN)_*(A_M lambda_M) = A_N lambda_N.
```

The test integrates over the full fiber. K3 evaluates only on the zero-section image and cannot
stand in for K4. The identity-supported Dirac and product-Haar families remain controls:

```text
Identity-supported Dirac: algebraic K4 PASS, but no independent promotion or provenance credit.
Product Haar: K4 FAIL on a nontrivial charged character by full-fiber integration.
```

If the entered pairing's induced history functional is certified product-Haar-equivalent, the
principal has that structural consequence before the one-shot act; the run still records the
receiver and cannot swap the pairing. If the generated history marginal is in the failing control
class, the run records `K4 FAIL` and stops. If it is Dirac-equivalent, K7 provenance still must pass.
If neither equivalence is proved, no control verdict is assigned to it. In every case the anchor is
not revised, narrowed, or swapped after seeing the result.

### G7 — S1 K5 through K7 [CLAIMED]

| Row | Required test |
|---|---|
| `K5` | the generated bases and weighted measures carry the matching common-gauge, boundary, contour, and domain transformations; amplitude invariance alone is insufficient |
| `K6` | measure normalization is checked at its own type and is K4-compatible; `A_N(1)=1` and state normalization do not substitute |
| `K7` | the base and weighted family descend from the frozen joint common-origin trace; record-only Q7 or a provenance status flag cannot substitute |

The reproduction/characteristic theorem and every receiver-bound interface must close on the same
generated family. Only a G0–G7 all-pass run may issue `JOINT_ANCHOR_ACCEPTED_AT_CORE = true`.

### G8 — stop-rule and mutation guard [CLAIMED]

The complete run records all inputs, the first failed clause, and all pass/fail receipts. It may not:

- rerun with a different tag, trace, action, carrier, fiber, or marginal because a downstream result
  was inconvenient;
- discard a failed adopted anchor as though it had never been adopted;
- count repeated negative runs as a derivation;
- move dependent lines before the full gauntlet passes.

## 8. Honest decision ledger and verified denominators [CLAIMED]

### 8.1 Denominator census at register bytes [CLAIMED]

The denominator is no longer carried as an unexplained assignment fraction. Q-814 identifies the
joint-state producer as the missing first gear that supplies S1 through `res_B`. Q-817 states the
exact consequence: if the producer is found, **S1 closes and S2/S3/S4 unblock**. Q-818 confirms the
complete producer acceptance frame and the four-line consequence. Therefore:

```text
CORE_FIVE = {
  JOINT_STATE_PRODUCER,
  S1_HISTORY_MEASURE_MEMBER_AND_K1_K7,
  S2_INTERACTING_CONTOUR_OPERATOR_FAMILY,
  S3_BOUNDARY_EDGE_PACKAGE,
  S4_PHYSICAL_WRITE_LAW_AND_ENDPOINT_DOMAINS
}

DIRECTLY_CLOSED_ON_G0_G7_ALL_PASS = {
  JOINT_STATE_PRODUCER,
  S1_HISTORY_MEASURE_MEMBER_AND_K1_K7
} = 2/5

DEPENDENT_FOUR = { S1, S2, S3, S4 }
UNBLOCKED_NOT_PASSED_BY_CORE = { S2, S3, S4 } = 3/4
```

S1 is the fourth dependent line, but it is not counted among the merely unblocked three because G4
through G7 run its member through K1–K7 as part of the all-pass core gauntlet. S2–S4 receive a
generated common-origin input and may start; this instrument does not run or pass their own
receivers.

### 8.2 Before the principal decides [CLAIMED]

```text
JOINT_ANCHOR = NOT ADOPTED
JOINT_STATE = NOT PRODUCED
CORE = unchanged
LINES_DIRECTLY_CLOSED = 0/5
DEPENDENT_LINES_UNBLOCKED = 0/4
```

This instrument itself moves nothing.

### 8.3 If the principal declines [CLAIMED]

```text
JOINT_ANCHOR = DECLINED AT THIS INSTRUMENT
ROAD_1_NAMED_VOCABULARY = exhausted at the registered scope
DERIVATION_ROUTE = remains open
CORE = open at the joint producer
DOWNSTREAM = unchanged
```

Decline is not a no-go theorem and does not authorize a different silent supply.

### 8.4 If the principal approves, before the gauntlet [CLAIMED]

Every downstream `FREEDOMS-CONSUMED` block must carry exactly:

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false
JOINT_ANCHOR_SOURCE = <principal decision artifact + instrument hash>
A0_RANK_FIBER = <principal entry; blind; target-independence certificate passed>
ANCHOR_TAG_CLASS = <principal entry>
STATE_OUTPUT = not yet exposed
SUPERSESSION = future same-receiver record-native derivation, prospectively logged,
               only after SupersessionEquivalenceNaturality.v001 passes
```

Approval supplies the input class and price. It does not assert the anchor theorem, SM passes, K4,
or core closure.

### 8.5 If the entire gauntlet passes [CLAIMED]

The register-grounded program-level tally is carried without inflation:

```text
CORE = closed at the joint-state producer
CORE_LINES_DIRECTLY_CLOSED = 2/5
DEPENDENT_LINES_UNBLOCKED = 3/4
DEPENDENT_LINES_PASSED = 0/4 by this instrument
```

“Unblocked” means the dependent construction/test may start on a generated common-origin input. It
does not mean its own receiver passes. `res_B` supplies the S1 member only after the gauntlet reaches
that step; K4–K7 can still reject it, in which case the all-pass branch is not reached.

### 8.6 If the gauntlet fails [CLAIMED]

```text
JOINT_ANCHOR_INPUT = remains ADOPTED-AND-FROZEN
CORE_ACCEPTANCE = FAIL at the first named clause
CORE_LINES_DIRECTLY_CLOSED = 0/5
DEPENDENT_LINES_UNBLOCKED = 0/4
RETROSPECTIVE_REPAIR = forbidden
```

A later non-equivalent anchor requires a new principal decision. A same-receiver record-native
derivation may supersede prospectively under `JAC-13`; the complete equivalence object and downstream
consumer inventory must be displayed and replayed first.

## 9. Balanced decision display — zero advocacy [CLAIMED]

| Disposition | What it knowingly does | What it does not claim |
|---|---|---|
| `APPROVE` | openly authors and freezes the exact joint carrier/fiber/anchor class before output; commits to carrying `ADOPTED-AND-FROZEN`; authorizes the preregistered proof/acceptance run | does not derive the anchor, guarantee the theorem, guarantee K4, or guarantee downstream success |
| `DECLINE` | leaves the joint producer and core open; keeps derivation-only work lawful; records that the completed instrument was declined | does not prove impossibility, revive a closed named route, or authorize a hidden substitute |

The instrument recommends neither disposition. The choice and every principal entry belong to the
principal.

## 10. FREEDOMS-CONSUMED [CLAIMED]

```text
CARRIED-AS-PARAMETER:
  the registered Road-1 exhaustion at its bounded scopes;
  V003's authored carrier/anchor template and DoR-013 freeze discipline;
  the two lawful strengthenings: DB has no free reference density; SYM's action is content-addressed;
  the joint-carrier limit-square commutation addition;
  A_C0, StatePort_U2_008, res_B, SM-1..SM-8;
  the derived S1 carrier/class and exact K1..K7 conditions;
  the stop-rule prospective/supersession pattern;
  the register-derived five-line core and four-line dependent census.

CONDITIONED-ON:
  any future approval on every principal field being filled and content-addressed;
  source-marginal reproduction on exactly the entered A0 rank fiber;
  any control-class consequence on an exact equivalence certificate from the entered pairing;
  any automatic supersession on the complete same-receiver equivalence/naturality replay.

SUBSTITUTED:
  nothing. No disposition, carrier, trace/pairing, A0 fiber, rank, ratio, E_joint, tag class, tag,
  channel, state, marginal, measure, history member, control classification, K4 outcome, line
  movement, equivalence relation, or principal entry was chosen or filled.

SCALING WEIGHTS:
  none consumed, fixed, or substituted. The symbolic A0 rank ratio is not evaluated.

DERIVED HERE:
  a closed two-disposition decision schema;
  the joint-carrier, A0-fiber, and control-disclosure schemas;
  the 14-certificate manifest and exact suite format;
  the supersession-equivalence schema;
  the 11 proof obligations;
  the fail-closed G0..G8 acceptance order;
  the approve/decline/pass/fail/supersession ledger semantics;
  the register-grounded denominator partition.

FREEDOMS NOT CONSUMED:
  a principal-entry slot was not flattened into an entered value;
  adoption was not flattened into derivation;
  one A0 fiber was not flattened into every fiber or into a rank ratio;
  a control equivalence was not flattened into a physical member choice;
  an anchor theorem was not flattened into its premise;
  a source-state precedent was not flattened into a joint-state proof;
  a trace/pairing was not flattened into a derived state;
  K3 point evaluation was not flattened into K4 fiber integration;
  unblocked was not flattened into passed;
  supersession was not flattened into result-driven mutation;
  no member binding, fixed-point execution, end test, physical numeric evaluation,
  or comparison with measured constants occurred.
```

## 11. Surface anchor, flattening, batteries, and custody [CLAIMED]

- **SURFACE ANCHOR:** physical raw material is the exact `A_C0` lineage, entered A0 fiber, factor
  embeddings `i_src/i_R/i_B`, finite history cylinders, quotient maps, and `pi_MN` fibers. The
  decision/certificate schemas, J0–J8 order, and G0–G8 gauntlet are rails. No PASS is issued on rails
  notation alone, and no clause quantifies over an unentered fiber or unprovided equivalence.
- **JURISDICTION CHECK:** JAC-03 protects target-independent admission, distinguishes a forbidden
  selection use from a lawful designated disclosure, and permits a true carrier/fiber/control fact
  to appear in its typed field. G6 protects weighted fiber projectivity, distinguishes FAIL from
  `NEITHER_EQUIVALENCE_PROVED`, and permits a non-control physical member to reach the actual test.
- **S01–S37 FLATTENING CHECK:** walked. Load-bearing protections include S03 (principal entries remain
  visibly unfilled), S05 (co-location is not a joint extension), and S12 (status, certificate, trace,
  state, control classification, and verdict remain distinct). No declined identification is reversed.
- **F_PLDEC:** structural schemas and symbolic equations only; no physical quantity was numerically
  evaluated and no measured constant was consulted.
- **ANTI-TUNING:** fiber, candidate class, certificate set, proof obligations, gauntlet, controls,
  and failure semantics all precede any output. The whole candidate family is the unit of adoption.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string, and byte-span/semantic-receiver
  checks were run. Hyphen/spacing/underscore and line-wrap variants of `common-origin`,
  `no-supplementation`, `input-faithful`, A0 rank fiber, identity-supported Dirac, product Haar,
  `ADOPTED-AND-FROZEN`, and supersession-by-derivation were included.
- **PE-1..PE-13:** pointer-only and zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** this is a CLAIMED draft. The builder's prior B0 equivalence schema is
  used only as a typed pattern and receives no self-confirming weight; the opposite-lane crosscheck
  supplies its independent satisfiability/no-instance check.
- **Search-surface disclosure:** exploratory recursive `*.md` searches in the cleanroom root touched
  names/snippets beyond the 18-member evidence closure. Those bytes carry zero premise, citation,
  inference, or verdict weight; every load-bearing statement was rerun on the pinned spans above.
- **PIN CHECK:** all sealed whole-file members matched their sidecars before writing; all living-file
  entry spans and their byte digests were recomputed. The register spans are checked again at seal.
- **CHAIN:** no anchor, fixed-point solve, acceptance run, evaluator, K4 test, or downstream chain was
  invoked.

## 12. Complete finite delta and byte-position audit [CLAIMED]

Declared diff convention: `git diff --no-index --no-color -U0` over sealed V001 and final V002;
line counts use literal added/deleted lines including blank lines. Closure/version metadata,
process-audit additions, and denominator citations are certificate/support changes; the only
operative revisions are R1–R4 below.

| Delta row | Authorized changed content | V002 receiver |
|---|---|---|
| `R1` | add JAC-14 exact finite-visible, pre-output certificate-suite format | §§5, 5.1, G0, G2 |
| `R2` | add blind A0 rank-fiber entry; bind JAC-07 to it; guard the rank-ratio entry point in JAC-03 | §§2–5, G0/G2, ledger/freedoms |
| `R3` | specify the complete same-receiver supersession equivalence/naturality certificate to the adopted act | §§5, 5.2, ledger/freedoms |
| `R4` | connect the entered history pairing/trace to identity-Dirac/product-Haar controls and name them in JAC-03's scan | §§3.2, 5, G6, freedoms |

The final zero-context diff has 74 hunks. The following finite assignment covers every hunk once;
`C0`, `D0`, and `P0` are non-operative closure/version, denominator-audit, and process-certificate
support respectively.

| Assignment | Diff hunks | Authorization / line decomposition |
|---|---|---|
| `C0` | H01–H12 | declared-first closure, source pins, title/version/schema identifiers |
| `R2` | H13, H18–H23, H28–H29, H32–H35, H38, H43, H49, H56 | A0-fiber carrier, entered-fiber reproduction, guarded rank-ratio path, JAC-07 and dependent records |
| `R4` | H14, H17, H27, H39–H42 | pairing-control carrier, named control classes, and G6 replay |
| `R1` | H24–H25, H37 | fourteenth certificate count and its G2 execution |
| `R3` | H31, H50, H55 | supersession equivalence/naturality schema and ledger/display consequence |
| `R2/R4` | H15 | new lines 126–139 are `R2` (`physical_price` plus `A0RankFiber`); new lines 140–151 are `R4` (`HistoryPairingControl`) |
| `R2/R4` | H16 | new lines 154–174 are `R2`; new line 176 is the `R4` subsection receiver; blank line 175 separates them |
| `R2/R4` | H26 | new lines 280–281 are `R2`; line 282 is the indivisible JAC-03 scan jointly required by `R2` (rank/fiber selectors) and `R4` (named control classes) |
| `R1/R3` | H30 | new line 292 is `R3`; new lines 293–323 are `R1`; new lines 325–351 are `R3` |
| `R1/R2/R4` | H36 | new line 397 is `R1`; new line 398 is the jointly frozen fiber/control inventory required by `R2/R4` |
| `D0` | H44–H48, H51–H54 | exact register-span denominator reproduction, section numbering, and five-core/four-dependent accounting |
| `P0` | H57–H74 | carriage, freedoms, audits, complete-delta certificate, counts, and final verdict lines |

Shared contextual hunks are decomposed above at line level where they contain two operative items;
no unassigned or multiply assigned hunk remains.

Carriage controls, checked as exact V001 substrings after writing:

```text
DB_STRENGTHENING = carried unchanged: "already entered" pairing/trace; no free reference density
SYM_STRENGTHENING = carried unchanged: finite/content-addressed action
LIMIT_SQUARE_ADDITION = carried unchanged: every finite/cylindrical restriction and limit square commutes
OVER_CONSTRAINT_FINDING = carried: none found; {BI} remains a lawful one-tag entry
```

Mechanical delta counts and hunk attribution are filled from the final re-diff in the seal-time
audit below.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 3846
PREDECLARATION_OUTPUT_SCAN = 0 hits
PIN_FAILURES = 0
PRINCIPAL_ENTRIES_FILLED = 0
DELTA_HUNKS = 74
DELTA_INSERTIONS = 331
DELTA_DELETIONS = 147
```

Verb audit: “adopt” appears only in historical/template scope or conditional decision branches;
this artifact adopts nothing. “Prove” appears only as a future obligation or as a cited sealed
theorem. “Close” and “unblock” are conditional on the all-pass gauntlet and retain distinct ledger
meanings. “Supersede” is prospective, equivalence-bound, receiver-bound, and never a post-result
rewrite. `VERB_AUDIT_SELF = CLEAN`.

## 13. Final lines [CLAIMED]

```text
CLOSURE = declared-first (byte position 0; scan 0 hits)
REVISIONS = 4/4 implemented (per-item bytes)
CERTIFICATES = 14
DENOMINATORS = verified (spans cited)
ENTRIES_FILLED = 0
ADVOCACY = zero
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
