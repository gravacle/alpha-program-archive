CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 16 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = DEMAND READING V002 (no construction, no candidate, no supply call)
DIGEST_RULE = every pinned digest computed from its file at seal time
SUPERSESSION = this artifact supersedes STAGE8_DESC_DEMAND_DARIO_V002.md (and through it V001) as
  the demand reading of record.  Both remain sealed and on the books.  CARRIAGE IS SEMANTIC, NOT
  LITERAL, and section 6 states exactly which kind applies where -- V002's "CARRIED = VERBATIM" was
  too strong and is corrected here.
CLOSURE_END_BYTE = 6784
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_993_DEMAND_V003_DARIO_V001.md` | `9dc354c6b56b7faf75ddbee1263f06f6bf0da91e89ba50989245449c0266f6a7` | assignment |
| 02 | `STAGE8_DESC_DEMAND_DARIO_V001.md`; `STAGE8_DESC_DEMAND_DARIO_V002.md` | `da32dc9dfff38a32668b673e0c1b9e05fee27d02cd49b2f7ed99a78b71c51da9`; `c883d3732af61800f6ac2219e87406ccbbae60f7928172a80281e2e2cae951e3` | my V001 and V002, both superseded here |
| 03 | `STAGE8_DESC_DEMAND_CHECK_CODEX2_V001.md`; `STAGE8_DESC_DEMAND_V002_CHECK_CODEX2_V001.md` | `f3704df1bc4d7b2f45833fb12a40352117751e8f1b036ba0ab7de9fd4cfa1414`; `8f42e7dc3590bd0ba746f55c6ec9e055357c9c91b973306b334e37692fdaf91c` | the V001 check this folded, and **the V002 check this repairs against** |
| 04 | `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md`; demand `[2044,6922)`; unpacking `[3001,5172)`; window `[6923,8531)`; Finding `[8533,9079)` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874`; `58ddd239b70e190b00813aa88c3bb4139cd480fb5dd6b93aef869e05ea3faf15`; `394b14cd7dc72775136ea2aeaceb1feeaddea5935c2538a1b48af8add3855801`; `f3d413e2804c19f10c0a155e0a3011a10973420e9c9e85ccd3d1a6365e7dece8`; `412e608e4378b6c92a44d434d9c6a0876081b87b915edc5d4d4f04fd26cb5281` | the P5 requirement |
| 05 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md`; port `[10824,11928)`; C6 `[23900,24472)`; B0 citation `[3916,4671)`; `rho_pre` `[7290,7829)` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab`; `63f743bfcc1df70bccb8cf1c0e3041c7a9202009c944c33adbfe8ae75da3535e`; `19d3ca3fe6573d2714e08e3d2bec05ef6ec2a52547cccb7d45dc835dfd8c1431`; `9b57498c802ac2fe6af3a63114501f650fc4d03035e57cebea81e0525e29d1c1`; `f9fb7a84ce4e3b954e5444baedc6703d9cc8ec52fe04f41e1b3b6d915b3d5372` | the port's two conditions |
| 06 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md`; G7 `[40368,41551)`; **G6 `[39637,39732)`**; SM rows `[36914,38169)` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab`; `864b46ca8562526e13972bd186e56d201a0b5898de015178063ebe9acd854f2f`; `7803f1a8cf15293faf673e4c9daedbb1cbe13f3b22c734a1fea3bee2941048e8`; `35030c25f9cd3343bb848cefdd8336e68745a393f5d300833ac89a51c001467a` | K7; **the corrected Q20 home**; SM-4 |
| 07 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md`; H-DIRAC price `[20579,20661)` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1`; `5895a4ab33eccfb7fb533d8c5ba2ce98f16119df0aea7d21c0cc949f293252dc` | **the corrected Q21 source** |
| 08 | `STAGE8_TASK4A_P3_COMPLETE_U3_PACKAGE_CONSTRUCTION_AND_FOUR_FIELD_STOP_V001.md`; ledger `[19846,21324)` | `f97ee43e052969f0c999b7e57d13310f818222a102c21c249845b8beef1301ff`; `5a4cef8ab2a29bba52bbc8bd292a228a15123a4425d72b4f3e10a0fae213ef18` | the four-field stack, kept separate |
| 09 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md`; roles `[37523,38115)`; interface `[38121,38437)` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c`; `e16f8b9438c518546fa7eda93c724316f4470f9a8298a50ac8d275eb09d293ad`; `cb264602a0537a07948c31d2763094395c5670a5001ea8378d9e7f83b67ed72c` | the B0 role bar |
| 10 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md`; U2 row `[6266,6883)` | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2`; `e6093b0f48c11d3f05d3c6fd3692b6ea2c4b320746bb92eebe72ef28ed697efe` | operational requirements; the `d_U2` witness |
| 11 | `STAGE8_AXN_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md`; A01–A05 `[7262,10112)`; A06–A09 `[10112,12021)`; A10–A13 `[12021,13587)`; A14–A17 `[13587,19040)` | `ae93720f5f5534a8d0d9915ab84368639f3fd96927b0d51a206a35c6b40b6019`; `843c25b5f88255919b20587fe25fecc8b5d908974af50b5aa4af8b0094088b70`; `5d3a233b21eda18b06975b786ff5e2d11cc289247ac1b258f90be7baf9d7efe3`; `1944cfd0553316c6401cb008e68894f1a26672ec5bfb0e6fc1eed6e160038e8b`; `c48efc3118bdcd0a0221ec0ec1c91a7bd3b9b68dea2357b669262f522d57f04e` | **U7, U8, U9, U10 — the B0 acceptance bar whole** |
| 12 | `STAGE8_B0_MD3_DESCENT_NON_DEGENERACY_ACCEPTANCE_TEST_V001.md`; MD-3 `[8794,11563)` | `23f5427159b2505a29629b28a3985f33efd090f5e852f43a337e58f4b0d5270f`; `d37bddd34151f7986c51719b9537487427855a97c9bdaac96c49bb5137a83378` | **U11 — descent faithfulness** |
| 13 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md`; **full repair span `[16298,17631)`**, containing `MISSING_COMPOSITION` `[17244,17540)` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782`; `2fd79b2e25c7afc381df9b65f7195b8aa0954b729d4b84c37292490a7653e012`; `d3d545b052817637a6ba33098cd177eb157ffeaf32b00c03c905c2e9cb8788e5` | **U12 — the placement binding, at its complete heading-to-heading span** |
| 14 | `STAGE8_TASK2D_MULTIAXIAL_STATE_TRANSITION_ENVELOPE_SPECIFICATION_V001.md`; `PathCert(Omega)` `[19909,21513)` | `bda00e99e964bd75d60429549e5f6c70762bf1607270b9a6294dc4c97cf2c635`; `02e56051cb65a2428f86111064205f8aa137117889c1d54e669ce02bdc73915f` | **the certificate's partially-specified form** |

| 15 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | state pin, process law, S01–S37 — **restored to the closure; V002 dropped all three** |
| 16 | `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md` | `4710453efd3fdb96e029aa1991a7e57b34f49e71b0d48184475bd98fb0f5ec15` | my 983 — **restored; V002 dropped it** |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
THIS IS THE DEMAND SIDE ONLY.  NO DESCENT OBJECT IS CONSTRUCTED, PROPOSED, SKETCHED, OR SCORED.
NO SUPPLY CALL IS MADE AND NO SELECTOR IS EXERCISED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — THE DEMAND AT BYTES — DARIO LANE — V003
## RELAY 993 — `[PLAN:DESC-9]` — THE U12 SPAN AND AN HONEST CARRIAGE CERTIFICATE

Date: 2026-08-11
Status: **ACCEPTANCE SHAPE V002: 35 conditions binding one descent object, each pinned at bytes, plus
1 verdict-relative condition binding axis closure only. V001's 18+1 are carried whole and re-typed as
the U2/P5 subassembly they are; the six omitted consumer groups are folded at their own bytes; three
corrections applied. The four-field stack stays separate, and K7 stays a downstream receiver rather
than a field.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

---

## 0. What V002 is, and what V001 got wrong [PROVABLE]

The check rules my V001 a **sound, byte-grounded U2/P5 subassembly** that is **not right-closed for
the whole descent section**. I accept that in full and state the failure in my own words rather than
its kinder ones.

V001's title claimed the section. Its content read the demand at the receiver I happened to have in
hand — P5, the port, K7, the four fields, the B0 role bar — and stopped there. **A correct subassembly
presented as the whole is an under-demand**, and it is precisely the failure V001 was built to catch:
its own §8 named six under-demands on the ground that no single source carries the demand, then
missed six more because I ran right-closure on every *span* and never on the *section*. The discipline
was right and I applied it one level too low.

Three things follow, and I record them before the folds so nothing below reads as a defence:

- **V001's 18+1 stand.** Every condition, every ground, every O/U call survives the check. Nothing
  here weakens a condition; the count grows because the receiver did.
- **Condition 13 survives its own flag.** V001 closed by naming condition 13 as the thing to press —
  whether the B0 U2 signature types a *slot* or constrains *delivery*. The check pressed it and it
  holds: `d_U2` has the complete U2 role as codomain, so a total map must deliver the inclusive
  identity and the contact rules. Flagging it was right and it cost nothing to flag.
- **One law-9b call of mine was too strong**, and §1.2 corrects it.

---

## 1. The three corrections [PROVABLE]

### 1.1 The no-inspection split — a prose label, not a condition

**V001 said:** the no-inspection clause *"bars two distinct moves"* — the temporal one and the ad-hoc
cross-row assembly — attributing both to that clause.

**The bytes say** the two bars have different grounds, and only the first comes from the words
*"without output inspection"*:

1. **Temporal/causal bar** — from the clause's own words at member 05 `[23900,24472)`: state,
   effects, domains, dynamics and the admission rule may not be fixed by looking at a response or a
   descendant result.
2. **Compositional/provenance bar** — from **common-origin provenance**, not from timing: unrelated
   artifacts do not become one derivational origin merely because they were assembled before any
   output. Member 11's A15 states the same thing from the B0 side, and states it as a conjunction:
   *"A datum bagged independently before output may pass literal timing while still failing A03/A05
   common derivational provenance; both predicates are required."*

**What moves:** the prose label only. V001 already gave the two bars **separate condition IDs 15 and
16**, and both keep their numbers, their text, and their grounds. The correction matters because a
builder who reads the compositional bar as a timing rule will think an early enough assembly passes
it. It does not — A15 says so in terms.

### 1.2 `PathCert(Omega)` — the certificate's form is partially specified, not open

**V001 said:** *"no member of this closure fixes the certificate's **form** — no schema,
serialization, field list, or digest convention for `Cert_P5` appears at any span I read."*

**That was true of my ten-member closure and too strong of the corpus.** Member 14 seals a field
schema at `[19909,21513)`:

```text
PathCert(Omega) := (
  Omega, d_C0, d_U1,
  d_state, d_effect, d_domain, d_dynamics,
  {d_fin,N}, {iota_NM}, d_out,
  Cert_carrier, Cert_state, Cert_effect, Cert_domain, Cert_dyn,
  Cert_restriction, Cert_derivation_covariance, Cert_limit,
  Cert_target_independence, Cert_no_post_output_supplementation
).
```

I fold it as the certificate's **partially-specified form** and re-invent nothing: the schema is
specified and uninstantiated, and it narrows the open form without closing it. The corrected law-9b
result:

```text
CONTENT                                          = specified   (P5's eight conditions)
PATH-LEVEL FIELD SCHEMA                          = specified   (member 14, [19909,21513))
EXACT Cert_P5 CANONICAL SERIALIZATION / DIGEST   = OPEN        (the remaining open specification)
INSTANCE                                         = absent
```

Note what the schema adds beyond P5's four maps: it carries `d_C0` and `d_U1`, the finite family
`{d_fin,N}` with its `{iota_NM}`, `d_out`, and **ten** named certificates including
`Cert_target_independence` and `Cert_no_post_output_supplementation`. That is corroboration from a
second direction for the folds in §2 — the record's own path-level certificate already reaches past
the U2/P5 tuple.

### 1.3 The two quote attributions

Both quotes are verbatim; both were bound to the wrong bytes.

| Quote | V001 bound it to | Its actual home |
|---|---|---|
| *"Identity-supported Dirac: algebraic K4 PASS, but no independent promotion or provenance credit."* | member 06's G7 span `[40368,41551)` | member 06's **G6** bytes `[39637,39732)`, digest `7803f1a8cf15293faf673e4c9daedbb1cbe13f3b22c734a1fea3bee2941048e8` |
| *"only algebraic K4 compatibility is disclosed, with no provenance or K7 consequence"* | member 06, the anchor instrument | **member 07**, entry-decision V002 `[20579,20661)`, digest `5895a4ab33eccfb7fb533d8c5ba2ce98f16119df0aea7d21c0cc949f293252dc` |

Both are corrected in §4 below, and member 07 is added to the closure to carry the second. **Neither
moves K7's content**: K7's own row independently rejects a status flag or a record-only Q7, so the
no-provenance-credit boundary stands on K7's bytes whether or not the two disclosure sentences are
quoted alongside it. The defect was in my citation discipline, not in the reading.

---

## 2. The six folded groups, at their consumers' bytes [PROVABLE]

Right-closure run both ways at every fold. **All six spans open exactly on a clause heading and close
exactly on the next**, so none truncates a demand and none overruns its entry. I state that as a
checked result rather than an assumption, because it is the check I failed to run at section scope in
V001.

### 2.1 `U7` — B0 A01–A05, the structural origin — member 11 `[7262,10112)`

Five clauses, each a separate deliverable:

- **A01** an inhabited, content-addressed, pre-test-frozen candidate
  `(Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)` — *"A schema, family, codomain fiber, or empty
  candidate slot does not pass inhabitance."* And an express non-demand: *"acceptance does not demand
  a unique microscopic ontology."*
- **A02** a content-addressed primitive inventory frozen before construction or target inspection,
  each row naming primitive, type, domain, authority and digest, with a stated coverage boundary; it
  *"may not certify its own membership"*.
- **A03** an executable or mechanically replayable rule from A02 to A01, **plus a frozen trace**,
  consuming no descendant, response output, discharge flag, or target-selected success value.
- **A04** a **filled** — *"not merely quote[d]"* — signature `Sig_B0`, with a conformance witness and
  an independent signature/domain/codomain oracle for T1.
- **A05** a provenance and domain certificate binding `Obj_B0` to A02/A03's exact inventory and trace,
  proving every declared property **at its actual scope** — *"Finite bounded-domain receipts do not
  certify later unbounded endpoint domains."*

### 2.2 `U8` — the complete C0, U1, U3 descendants — member 11 `[10112,12021)`

A06/A07/A09 give the exact content sets for `C0`, `U1`, `U3`. **A08, the complete U2 descendant, is
already V001's condition 13** and is not double-counted. Each clause carries its own non-substitution
rule: *"Formal conventions alone do not pass the physical U1 receiver"*; the finite U3 skeleton
*"does not supply the complete measure, interacting contour, boundary/edge carrier, or unbounded
endpoint-domain package."*

### 2.3 `U9` — the right-domain `d_C0`, `d_U1`, `d_U3` — member 11 `[12021,13587)`

A10/A11/A13 fix the exact arrows. **A12, `d_U2`, is already V001's condition 5.** The clauses close
the obvious substitutions by name: the existing total `d_C0^P : P_008 -> C0_008` *"has the wrong
domain and cannot pass by renaming"*; *"A premise-level projection or map that never consumes the
candidate is the wrong receiver."*

### 2.4 `U10` — B0 A14–A17 — member 11 `[13587,19040)`

- **A14** a closed property-certificate inventory with a fixed seven-field row shape, covering A04/A05
  properties, the C0/U1/U2/U3 content predicates, and the totality/typing/correspondence predicates
  for A10–A13.
- **A15** no-post-output supplementation **as an executable test**, over a frozen six-item set, with
  the conjunction quoted in §1.1.
- **A16** a full-package equivalence/naturality rule, frozen before testing, comparing *"the **whole**
  C0/U1/U2/U3 presentation and all four descent witnesses, not only U2/P5-facing fields"* — and
  carrying its own express non-demand, *"unique-origin reconstruction remains unnecessary."*
- **A17** an applicable T1–T6 ledger, one row per sealed leg, each with applicability digests,
  transcript, result, failure owner and citations.

### 2.5 `U11` — MD-3 descent non-degeneracy — member 12 `[8794,11563)`

Faithfulness on the tested quotient:

```text
r not~_B0 r'  ==>  DESCEND_B0(r) not~_DESC DESCEND_B0(r')
```

with three teeth worth carrying exactly: differences *"only in names, labels, display conventions,
file paths, or self-asserted provenance strings do not discharge MD-3"*; an empty probe set returns
`NO_VERDICT`, which *"is not evidence against `B0`. It is also not a pass."*; and MD-3 is *"an
acceptance condition over the tested family, not a global uniqueness theorem"* — a fourth express
non-demand, and one that belongs beside O1.

### 2.6 `U12` — the StatePort placement binding — member 13 `[16298,17631)`, member 06 `[36914,38169)`

The supplied `omega_phys` must be **bound** to the port's actual representation and density by a
declared placement relation with its common-origin certificate; specification alone is not an
instance, and *co-location of unbound fields is not a state construction*.

**This does not reinstate the restriction pinning corrected at Q-900**, and I am the lane that must
say so plainly, having been the one corrected. A lawful new supply receiver may differ from
restriction. What it may not do is leave the supplied functional unbound to the representation and
density it is supposed to be the history content of.

**The span is now the complete heading-to-heading section**, `[16298,17631)`, replacing V002's
`[16822,17244)`. The added bytes carry the `MISSING_COMPOSITION` block at `[17244,17540)`:

```text
MISSING_COMPOSITION =
  a target-independent common-origin producer d_state_Omega/d_joint
  + one concrete positive normalized joint state/density
  + the commuting placement square tying its B-restriction to omega_phys
  + provenance and no-post-output-supplementation certificates.
```

**Effect on condition 35, displayed rather than assumed: the row text stands unchanged, and it is
narrower than the fuller span.** Condition 35 names the binding, the declared relation, and the
common-origin certificate. The added block names three things the row does not: the **producer**
`d_state_Omega`/`d_joint`; that the joint state/density must be **positive and normalized**; and
that the certificate obligation is **provenance *and* no-post-output-supplementation**, not
provenance alone. Two of those three are already carried elsewhere in the shape — the producer by
condition 21 (A03's target-independent rule and frozen trace), the no-post-output certificate by
condition 31 (A15) — so the shape as a whole is intact. What the full span adds that the shape did
**not** carry at this receiver is the positivity/normalization of the joint state and the word
*commuting* on the square. I record that as a displayed refinement of what condition 35 points at,
and leave the row text byte-identical, because this relay changes span fields and nothing else.

---

## 3. The whole-section acceptance shape [PROVABLE]

**35 conditions bind one descent object. Each is pinned to a member and span.** Conditions 1–18 are
carried from V001 byte-verbatim; 19–35 are the folds.

**A — the object that must exist**

| # | Condition | Ground |
|---:|---|---|
| 1 | Four typed descent maps `d_state`, `d_effect`, `d_domain`, `d_dynamics` | 04 `[3001,5172)` |
| 2 | An actual common-origin construction trace `Omega` — concrete, not existential | 04 `[2044,6922)`, §4 close |
| 3 | `Cert_P5(Omega)`, establishing conditions 4–11 | 04 `[3001,5172)` |
| 4 | The target-independent admissibility **manifest**, as a delivered component | 04 §3, within `[2044,6922)` |
| 5 | The descent witness `d_U2 : (B0_candidate,C0) -> U2` | 10 `[6266,6883)`; 09 `[38121,38437)`; A12 in 11 `[12021,13587)` |

**B — the certificate's content**

| # | Condition | Ground |
|---:|---|---|
| 6 | Common carrier | 04 `[3001,5172)` |
| 7 | State properties: positive, trace-class where applicable, normalized | 04 `[3001,5172)`; 05 `[7290,7829)` |
| 8 | Effect properties: `0 <= E_r <= I`; completeness **only where the family is declared exhaustive** | 04 `[3001,5172)`; 10 `[6266,6883)` |
| 9 | Domain compatibility, dynamics preserving the declared domain | 04 `[3001,5172)` |
| 10 | Dynamic compatibility, same branch/quotient/measure conventions | 04 `[3001,5172)` |
| 11 | Covariance and causality **commuting with the descent maps** | 04 `[3001,5172)` |
| 12 | Provenance: shared primitive inputs and construction record; no post hoc import | 04 `[3001,5172)` |

**C — the interface the maps must land on**

| # | Condition | Ground |
|---:|---|---|
| 13 | The U2 role signature, including **action/source contact rules** and the **inclusive identity** — a delivery constraint on `d_U2`, confirmed by the check | 09 `[37523,38115)`; 10 `[6266,6883)`; A08 in 11 `[10112,12021)` |

**D — process conditions**

| # | Condition | Ground |
|---:|---|---|
| 14 | Target-independent admissibility: frozen before downstream response or root information exists | 04 `[3001,5172)` |
| 15 | One trace supplies state, effects, domains, dynamics **before any response output is inspected** — the **temporal** bar | 05 `[23900,24472)` |
| 16 | No **ad hoc cross-row assembly** — the **compositional** bar, from common-origin provenance, whenever it occurs | 05 `[23900,24472)`; A15 in 11 `[13587,19040)` |
| 17 | No **undeclared external** supplement to any required datum after a descendant output is inspected | 09 `[38121,38437)` |

**E — the downstream receiver that consumes the descent**

| # | Condition | Ground |
|---:|---|---|
| 18 | K7: the **base and weighted** family descend from the frozen joint common-origin trace; record-only Q7 or a provenance status flag cannot substitute | 06 `[40368,41551)` |

**F — `U7`: the structural origin the descent descends *from***

| # | Condition | Ground |
|---:|---|---|
| 19 | An **inhabited** frozen B0-role candidate `(Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)` | A01 in 11 `[7262,10112)` |
| 20 | A content-addressed primitive inventory frozen before construction, with its coverage boundary | A02, same span |
| 21 | A target-independent construction rule from the inventory to the candidate, **plus the frozen trace** | A03, same span |
| 22 | A **filled** candidate signature with conformance witness and independent T1 oracle | A04, same span |
| 23 | A provenance/domain certificate binding the candidate to the inventory and trace, proved at actual scope | A05, same span |

**G — `U8`: the complete descendants**

| # | Condition | Ground |
|---:|---|---|
| 24 | Complete `C0` descendant content at its role receiver | A06 in 11 `[10112,12021)` |
| 25 | Complete `U1` descendant content | A07, same span |
| 26 | Complete `U3` descendant content | A09, same span |

**H — `U9`: the remaining right-domain maps**

| # | Condition | Ground |
|---:|---|---|
| 27 | Total right-domain `d_C0`, compared to the independent C0 reference on the identical declared input | A10 in 11 `[12021,13587)` |
| 28 | Total right-domain `d_U1`, consuming the candidate and the A10-descended `C0` | A11, same span |
| 29 | Total right-domain `d_U3`, outputting the complete A09 package | A13, same span |

**I — `U10`: the closing certificate machinery**

| # | Condition | Ground |
|---:|---|---|
| 30 | A closed property-certificate inventory at the fixed row shape, covering A04/A05, the four role contents, and the A10–A13 predicates | A14 in 11 `[13587,19040)` |
| 31 | No-post-output supplementation **as an executable test** over the frozen six-item set | A15, same span |
| 32 | A full-package equivalence/naturality rule comparing the **whole** presentation and all four witnesses | A16, same span |
| 33 | An applicable T1–T6 ledger, one row per leg with digests, transcript, result, failure owner | A17, same span |

**J — `U11` and `U12`**

| # | Condition | Ground |
|---:|---|---|
| 34 | MD-3 descent non-degeneracy: `DESCEND_B0` faithful on the tested quotient; label-only differences do not discharge it | 12 `[8794,11563)` |
| 35 | The StatePort placement binding: the supplied `omega_phys` bound to the actual representation and density by a declared relation with its common-origin certificate | 13 **`[16298,17631)`**; 06 `[36914,38169)` |

**K — verdict-relative, binding the axis-closure verdict only**

| # | Condition | Ground |
|---:|---|---|
| 36 | The coverage theorem: every admitted target-independent common-origin P5 package generated by the manifest, proven response-equivalent to a listed package, or excluded upstream | 04 §3 and §6, within `[2044,6922)` |

### 3.1 The four-field stack stays separate — unchanged from V001

Member 08's four `TYPE-U` signatures — measure, interacting contour, boundary/edge, unbounded
endpoint domains — are **not** folded into the numbered list. `P5_COMPLETE_INPUT_READY` stacks them
upstream of P5's input readiness, and merging the two stacks would misreport both. The check agrees
and adds the load-bearing qualifier I now carry: for **whole-section closure** they are **mandatory
prerequisites, not optional context.** Conditions 26 and 29 are where they bite — a complete `U3`
descendant and a total `d_U3` cannot exist while the four fields are `TYPE-U`.

### 3.2 K7 is a receiver, not a field

Condition 18 is a **downstream receiver conjunct**, not a sixth component of
`P5_COMMON_ORIGIN_DESCENT_PRESENTATION`. V001 had this right and the check confirms it; it is
restated because a 35-row list invites the reader to flatten every row into one tuple.

---

## 4. Over-demands and under-demands [PROVABLE]

### 4.1 `O1`–`O4` carried, and a fourth non-demand joins them

`O1` unique/full source determination; `O2` unconditional effect completeness; `O3` the coverage
theorem demanded of a single package; `O4` *"no post-output supplementation"* read absolutely — all
four carried from V001 with their scopes, all four confirmed by the check.

Two of the folds strengthen `O1` from new directions, which is worth recording because `O1` is the
over-demand a 35-row list most invites:

- **A01:** *"acceptance does not demand a unique microscopic ontology."*
- **A16:** *"unique-origin reconstruction remains unnecessary."*
- **MD-3:** *"an acceptance condition over the tested family, not a global uniqueness theorem over all
  possible future B0 constructions."*

Four sealed consumers now decline uniqueness in their own words. A lane that demands it is
over-demanding against the record four times over.

### 4.2 `U1`–`U6` carried; `U7`–`U12` folded

`U1`–`U6` stand exactly as V001 stated them. `U7`–`U12` are now **conditions 19–35** rather than
warnings, which is the whole point of V002: an under-demand that has been folded is no longer an
under-demand.

### 4.3 The coverage theorem still occupies both columns

Unchanged and re-affirmed: an over-demand against a package verdict, an under-demand against the
axis-closure verdict, with the column fixed by the verdict in play and never by the object. It is
condition 36 and it is deliberately outside the 35.

---

## 5. The corrected quotations, in place [PROVABLE]

```text
Q20  "Identity-supported Dirac: algebraic K4 PASS, but no independent promotion or provenance credit."
     -> member 06, G6 bytes [39637,39732), 7803f1a8cf15293faf673e4c9daedbb1cbe13f3b22c734a1fea3bee2941048e8

Q21  "only algebraic K4 compatibility is disclosed, with no provenance or K7 consequence"
     -> member 07, entry-decision V002 [20579,20661), 5895a4ab33eccfb7fb533d8c5ba2ce98f16119df0aea7d21c0cc949f293252dc
```

Both recomputed from source bytes at seal time.

---

## 6. The carriage certificate, stated precisely [PROVABLE]

V002's final lines read `CARRIED = VERBATIM` and its §6 said *"Nothing was deleted."* **Both were too
strong, and this section replaces them with what is true.** The claim was mine, in my own voice, and
overstating a carriage certificate is the defect I have twice returned to the other lane. It is worth
more to say so plainly than to repair it quietly.

### 6.1 What kind of carriage this actually is

Measured by comparison, not recollection:

```text
CONDITIONS 1-18, literal byte-identity of the whole row:   0 of 18
CONDITIONS 1-18, condition TEXT byte-identical:           15 of 18   (ground column re-keyed)
CONDITIONS 1-18, text carrying an AUTHORED ANNOTATION:     3 of 18   (13, 15, 16)
O/U TABLES:                                    semantic carriage, not byte carriage
```

**CARRIAGE = SEMANTIC, with authorized annotations on conditions 13, 15 and 16.** The three
annotations are: condition 13's *"a delivery constraint on `d_U2`, confirmed by the check"*;
condition 15's *"the **temporal** bar"*; condition 16's *"the **compositional** bar, from
common-origin provenance"*. Each was authorized by a check finding and each is a label on an
unchanged demand, but a label is text and text that changed is not verbatim.

Every one of the eighteen ground columns was re-keyed when the closure grew, which changes the row
even where the referenced span is identical. That is why literal identity is 0 of 18 and why the
honest word is *semantic*.

### 6.2 The change-class table — correctly labelled

The table V002 called a hunk table is **a change-class table**: it enumerates the *kinds* of change,
not unified-diff hunks. It is retained under its true name and its 13 rows are unchanged in content.

| # | Change class | V001 | V002 | Reason |
|---:|---|---|---|---|
| C1 | condition count | 18 + 1 | 35 + 1 | six consumer groups folded as conditions 19–35 |
| C2 | conditions 1–18 | as stated | text carried; ground column re-keyed to V002 member numbers | member list grew from 10 to 14 |
| C3 | condition 5 | grounds 07, 06 | grounds 10, 09 plus A12 | the same witness is also a B0 acceptance clause |
| C4 | condition 13 | grounds 06, 07 | grounds 09, 10 plus A08, **and an annotation** | the check pressed and confirmed it |
| C5 | condition 16 | as stated | **annotation** plus A15 ground | correction 1.1 |
| C6 | condition 19 (V001) | the coverage theorem | renumbered to 36 | 19–35 are now the folds |
| C7 | §3.1 prose label | both bars attributed to the no-inspection clause | temporal from the words; compositional from provenance | correction 1.1 |
| C8 | law-9b on certificate form | *"no member … fixes the certificate's form"* | PARTIALLY-SPECIFIED | correction 1.2 |
| C9 | Q20 binding | G7 span | G6 bytes `[39637,39732)` | correction 1.3 |
| C10 | Q21 binding | member 06 | member 07 `[20579,20661)` | correction 1.3 |
| C11 | byte-audit numeral | *"all fourteen cited spans"* | 13 unique intervals | the check's count is right |
| C12 | closure | 10 rows | 14 rows | see §6.4 — **this is where "nothing was deleted" fails** |
| C13 | four-field stack | separate | separate, plus the mandatory-prerequisite qualifier | the check's addition |

### 6.3 The real unified-diff certificate

Computed over the sealed files, not asserted:

```text
DIFF V001 -> V002   (GNU unified, 3 context lines)
  V001 lines = 505 ; V002 lines = 530
  HUNKS      = 2
  INSERTIONS = 457
  DELETIONS  = 422

DIFF V002 -> V003
  HUNKS      = 10
  INSERTIONS = 158
  DELETIONS  = 37
```

Two hunks against 505 lines is itself the finding: **V002 was not a byte-copy of V001 with named
replacements.** It was a substantial rewrite that preserved meaning. The change-class table describes
it correctly; the word *verbatim* did not.

### 6.4 The exact closure-set delta

V002's fourteen members are **not** V001's ten plus four. At the set level:

```text
RETAINED  6 files
ADDED     8 files
DROPPED   6 files   <- V002's "nothing was deleted" was FALSE at this level
```

Dropped from the verdict-bearing set in V002, each named:

| Dropped in V002 | Consequence | Status in V003 |
|---|---|---|
| `PROGRAM_STATE_BRIEF_V005.md` | the state pin left the verdict-bearing set | **restored**, member 15 |
| `LOCKED_PROCESS.md` | the process law left the set | **restored**, member 15 |
| `DECLINE_REGISTER_V002.md` | the S01–S37 flattening guard left the set | **restored**, member 15 |
| `QUESTIONS_SETTLED_REGISTER_V001.md` | the register left the set | not restored: V003 relies on no register entry, and a live-append file pinned without use invites a stale-snapshot claim |
| `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md` | my 983 left the set | **restored**, member 16 |
| `relay_inbox/RELAY_PASTE_986_DESCENT_DEMAND_DARIO_V001.md` | superseded by the 989 assignment | lawfully superseded; the 993 assignment is member 01 |

Three of the six drops were the process-law and state-pin files that every artifact in this lane
pins. Dropping them silently is the part that mattered, and they are back.

**For V002 -> V003 the claim "nothing removed" is TRUE at the set level**, and it is stated only
because it was checked: V003's sixteen members are V002's fourteen plus two restorations.

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  V001's eighteen conditions, its verdict-relative nineteenth, and its O1-O4 / U1-U6 calls, all
  byte-verbatim; the P5 requirement and its non-demands; the port's two conditions and C6; K7 and the
  two disclosure sentences at their corrected homes; the four-field signatures; the B0 role bar and
  interface; A01-A17 whole; MD-3; the placement square and SM-4; the PathCert field schema; all gates.

DERIVED HERE:
  the six folds, each right-closed at both edges at its consumer's bytes;
  the renumbered 35 + 1 shape with every ground re-keyed;
  the temporal/compositional split as an attribution correction rather than a condition change;
  the partially-specified reading of the certificate's form, with the remaining open item named;
  the two quote rebindings;
  the observation that four sealed consumers now decline uniqueness in their own words;
  the thirteen-hunk V001 -> V002 accounting.

SELECTED HERE:
  nothing.  No descent object, candidate, trace, map, certificate, manifest, measure, or omega_phys is
  constructed, proposed, supplied, or scored.  No supply status is asserted.  No selector is exercised.
  No feasibility verdict is offered and none is implied by the count.

SCALING WEIGHTS: none consumed, fixed, or substituted.  No rank, ratio, or physical quantity was read,
  formed, or evaluated.
```

---

## 8. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A subassembly was not flattened into a whole section
  — the correction this artifact exists to make. A folded under-demand was not flattened into a new
  demand: every added condition is an already sealed consumer clause. A field schema was not flattened
  into a serialization, nor a specification into an instance. A prose-label correction was not
  flattened into a condition change. A placement binding was not flattened into the restriction
  pinning Q-900 corrected. Two stacked demand sets were kept apart. K7 was not flattened into a field.
- **CARRIED = SEMANTIC, precisely stated at §6.1:** 15 of 18 condition texts are byte-identical with
  their ground column re-keyed; 3 (13, 15, 16) carry authorized annotations; the O/U tables carry
  semantically. Literal row identity is 0 of 18, and §6 says so rather than claiming verbatim.
- **BUILDER-NEVER-VERIFIES:** the check is the opposite lane's and I did not re-adjudicate it. I
  re-verified every span it asked me to fold — 10 spans, 6 files, **all MATCH** — because folding an
  unverified span would import a claim rather than check one.
- **ONE DEFECT RETURNED TO THE CHECK, as a courtesy, not a challenge:** its closure member 18 prints
  the entry-instrument digest as `af26ab0354420f64718942b9bdcc61a4e6826a885acd5c02a3f57a66c91ba8` —
  **62 hex characters, not 64**, diverging from the true digest after 41. The file it names is right
  and its Q21 correction is independently verified here at `[20579,20661)`, so the **finding stands**;
  but a closure table's whole function is content-addressing, and a 62-character string addresses
  nothing. Member 07 above carries the well-formed digest.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **PE-1..PE-15:** pointer-only, zero verdict weight.
- **LANE SEPARATION:** demand side only. The supply audit and its cross-check are the other pair.
- **CUSTODY:** archive-side only. One artifact, lane-tagged, sealed. V001 remains sealed and on the
  books; this artifact supersedes it as the reading of record and deletes nothing.
- **BYTE AUDIT:** all 14 members re-hashed at seal time; **23 unique intervals** cited and recomputed.
  The interval count is stated after a scoped count, not by eye — H11 exists because V001 counted by
  eye. Closure declared at byte 0, end computed on bytes as a fixed point; 27-token pre-closure scan
  over authored prose, 0 hits; numeral grep run **scoped per table region**, per my own 986 lesson.

---

## 9. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 6784; scan 0 hits)
GROUPS = U7-U12 FOLDED.  U7 B0 A01-A05 [7262,10112) 843c25b5... -> conditions 19-23;
     U8 complete C0/U1/U3 descendants A06-A09 [10112,12021) 5d3a233b... -> conditions 24-26 (A08/U2
     already condition 13, not double-counted); U9 right-domain d_C0/d_U1/d_U3 A10-A13 [12021,13587)
     1944cfd0... -> conditions 27-29 (A12/d_U2 already condition 5); U10 B0 A14-A17 [13587,19040)
     c48efc31... -> conditions 30-33; U11 MD-3 [8794,11563) d37bddd3... -> condition 34; U12 StatePort
     placement binding [16298,17631) 2fd79b2e... (full heading-to-heading span, containing
     MISSING_COMPOSITION [17244,17540) d3d545b0...) + SM rows [36914,38169) 35030c25... -> condition 35.
     RIGHT-CLOSURE RUN BOTH WAYS AT ALL SIX: every span opens on a clause heading and closes on the
     next; no truncation, no overrun.
CORRECTIONS = 3 APPLIED.  (1) no-inspection SPLIT: temporal bar from the clause's words; compositional
     bar from common-origin provenance and A15 ("A datum bagged independently before output may pass
     literal timing while still failing A03/A05"), regardless of timing -- a PROSE-LABEL fix; conditions
     15 and 16 keep their numbers, text and grounds.  (2) PathCert.  (3) Q20 -> G6 [39637,39732)
     7803f1a8...; Q21 -> entry-instrument V002 [20579,20661) 5895a4ab...  Neither moves K7's content.
PATHCERT = FOLDED-AS-PARTIAL.  Field schema sealed at member 14 [19909,21513) 02e56051..., specified
     and uninstantiated, carrying d_C0/d_U1, the four P5 maps, {d_fin,N}, {iota_NM}, d_out and ten
     named certificates.  OPEN REMAINDER, stated and not re-invented: the exact Cert_P5 canonical
     serialization and digest convention.  CONTENT specified / SCHEMA specified / SERIALIZATION open /
     INSTANCE absent.
COUNT = 35 conditions + 1 verdict-relative (the coverage theorem, condition 36), each at bytes.
     The four-field stack is kept SEPARATE and upstream, now carrying the check's qualifier that for
     whole-section closure it is a mandatory prerequisite; K7 stays a downstream receiver conjunct.
CARRIAGE = STATED-PRECISELY.  SEMANTIC, not literal: 15/18 condition texts byte-identical with ground
     columns re-keyed, 3/18 (conditions 13, 15, 16) carrying AUTHORIZED ANNOTATIONS, O/U tables carried
     semantically; literal whole-row identity is 0 of 18.  V002's "CARRIED = VERBATIM" and "nothing was
     deleted" were BOTH too strong and are corrected at section 6.
H_TABLE = RELABELED + REAL-DIFF-ADDED.  The 13-row table is a CHANGE-CLASS table (C1-C13), not a hunk
     table, and is retained under its true name.  The real unified diff is now certified from the
     sealed files: V001 -> V002 = 2 hunks, 457 insertions, 422 deletions over 505/530 lines -- two
     hunks against 505 lines being itself the finding that V002 was a substantial rewrite preserving
     meaning, not a byte-copy with named replacements.  V002 -> V003 = 10 hunks, 158
     insertions, 37 deletions.
CLOSURE_DELTA = EXACT (rows named).  V002 = 6 RETAINED + 8 ADDED + 6 DROPPED against V001, so its
     "nothing was deleted" was FALSE at the set level.  The six dropped, each named: PROGRAM_STATE_
     BRIEF_V005.md, LOCKED_PROCESS.md, DECLINE_REGISTER_V002.md (the state pin, the process law and
     the S01-S37 guard -- all three RESTORED here as member 15), QUESTIONS_SETTLED_REGISTER_V001.md
     (not restored, and why), STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md (RESTORED as member 16),
     and the superseded 986 relay paste (lawfully superseded).  For V002 -> V003 "nothing removed" is
     TRUE at the set level, and is stated only because it was checked.
SUBSTANCE = BYTE-CARRIED-EXCEPT-U12.  The 36 rows, their grounds and the six folds carry byte-identical
     from V002 except condition 35's ground field and member 13's span fields.
NUMERAL_GREP = SCOPED-RUN (per table region; V001's "fourteen cited spans" corrected to 13 unique
     intervals at H11 -- the check's count is right and mine double-counted)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is the **fold boundary
itself**: I folded exactly the six groups the check named, on the ground that each is an already
sealed consumer the section's B0 domain makes applicable. That test — *applicable because the domain
reaches it* — is the same test that, applied one notch wider, would pull in T5A/T5B's
Boundary-Resolved generator legs and the item-1 realization leg named inside A17's own ledger. I read
those as **rows of condition 33** rather than as separate conditions, because A17 carries them as legs
of one ledger. If a lane reads them as independent consumers, the count grows again and V002 is a
subassembly of a wider shape in exactly the way V001 was of this one. That is the sentence to check.
