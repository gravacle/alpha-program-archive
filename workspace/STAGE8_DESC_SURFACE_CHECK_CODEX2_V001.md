CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00004731
VERDICT_BEARING_SET = exactly the 22 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LIVE_MEMBERS = NONE
LANE = CODEX 2
ROLE = OPPOSITE-LANE TERMINUS CHECK (relay 1051)
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `cleanroom/relay_inbox/RELAY_PASTE_1051_SURFACE_CHECK_CODEX2_V001.md` | `2d37f6dbf27633f06459a1a7d85cf56cd703875689c0850364adcdca357c903d` | assignment |
| 02 | `cleanroom/STAGE8_DESC_SURFACE_FIRST_DARIO_V001.md` | `953aa85b3aa7eac5f4763a8b26d85754270a114a38094a234ba33750af3608bd` | subject |
| 03 | `cleanroom/gen_stage8_desc_surface_first_dario_v001.py` | `eef7820939b388038679cd9b6cb12e17bb86979096bea906b35c8c273d73d1fe` | subject generator |
| 04 | `cleanroom/STAGE8_DESC_SURFACE_FIRST_STEP12_PREFORM_DARIO_V001.md` | `73ef21bdbfef5fc9a3e602aedbf3a54e32a0274a4dd2fcc7214750b5b59ffe31` | quarantine preform |
| 05 | `archive/relay_inbox/RELAY_PASTE_1050_SURFACE_FIRST_DARIO_V001.md` | `ce71155755ae13da1c977e094b59af457772c665154245ce66ce7c428ccad73c` | construction assignment |
| 06 | `archive/relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | bootstrap and form-source bar |
| 07 | `archive/supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 08 | `archive/workspace/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | record-action law |
| 09 | `archive/workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md` | `2be95d5b58ea000df9a30b717a809374a90693f78a893d87b73a12e4be97fa21` | endpoint and arbitrary-N theorem |
| 10 | `archive/workspace/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | write geometry and causal conditionality |
| 11 | `archive/workspace/R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_RESULT_V001.md` | `321c52bc3f5cf8d66b2ca4a7f14811a41e905048ef89415f8c76e22837261c58` | analytic-tail conditionality |
| 12 | `archive/workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | A0, family, maps, kappa form |
| 13 | `archive/workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | read rule |
| 14 | `archive/supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | family-level ratification and TYPE-P premise |
| 15 | `archive/workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | scalarization survivors and state-port typing |
| 16 | `archive/workspace/STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md` | `ab156ee764db9d0bd48f54f1b879f1bafcfac08b45520ca6c4fb582e48edf572` | Attach adjudication |
| 17 | `archive/workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | tensor factors, restriction, non-uniqueness |
| 18 | `archive/workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | completed-object demand legs |
| 19 | `archive/workspace/STAGE8_P5_SECTOR_ASSIGNMENT_V001.md` | `898cd5f40b5729577f5acf74fa6496929c58b1a39c2acc611ee7f920a9e9e192` | P5 receiver typing |
| 20 | `archive/workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | PhysicalSig/Obj0 receiver typing |
| 21 | `archive/workspace/STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md` | `c49d31200460e96209375f06a7a655d343767695ef09805f1e396d1814833b6b` | full cell action and projectors |
| 22 | `archive/workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_SPEC_V002.md` | `4a7600caa23d0c7a98eeef8a79941c20ca4e28a4f5a2c1cf5c2362e88c7d4721` | charge-projector definition |

```text
BLIND HELD. EVERY SCALE REMAINS SYMBOLIC.
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
NO MEMBER BOUND. NO PHYSICAL FIXED POINT EXECUTED. NO END TEST. NO FREEZE.
NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEASURED-CONSTANT COMPARISON.
PE-1..17 = POINTER-ONLY. omega_phys = UNTOUCHED.
```

CLOSURE_DECLARATION_END

# STAGE 8 DESC — SURFACE-FIRST OPPOSITE-LANE TERMINUS CHECK

Date: 2026-08-11  
Lane: Codex 2  
Status: CHECKED — CORRECTED

## 1. Preflight and mechanical custody

The three assigned cleanroom inputs rehash to their assigned seals: subject `953aa85…`, generator
`eef78209…`, and preform `73ef21bd…`. The subject closure begins at byte 0 and ends at byte 5828;
an independent byte scan reproduced that endpoint. All eighteen closure rows were parsed from the
subject and rehashed at their full archive-root paths: `18/18` matched. With zero live members,
STRICT and STABLE coincide.

The generator was attacked without giving it a write path: its digest function was replaced by a
deterministic wrong digest. It returned refusal code `3`, printed `UNGROUNDED EMISSION BLOCKED`, and
left the subject digest unchanged at `953aa85…`. The closure substitution was then run in memory on
the unmodified subject: it reproduced byte 5828 and made zero byte changes. Thus refusal and closure
idempotence work as claimed. This did not invoke the subject chain or write the archive.

## 2. Mating re-derived and attacked

### 2.1 Direct discharge

Member 09 `:59-84` proves, for arbitrary finite `N`, that orthogonal charge projectors and an
all-ready record state give

```text
rho_N = P_0 rho_S P_0 tensor |r><r|^tensor_N
      + P_ch rho_S P_ch tensor |p_Q><p_Q|^tensor_N,
```

whenever `rho_S=P_0 rho_S P_0+P_ch rho_S P_ch`. Member 12 `:628-635` gives
`rho_S=omega_tr=I_src/Tr_A(I_src)`. A0 supplies orthogonal `P_0,P_ch` with sum `I_src`
(member 12 `:352-363`), so

```text
P_0 omega_tr P_0 + P_ch omega_tr P_ch
  = (P_0+P_ch)/Tr_A(I_src)
  = omega_tr.
```

The ready premise is member 12 `:641-643`. At zero history the law at member 12 `:650-654`
reduces to the product of the same full cell maps. The full third-basis action and
`P_0=I-P_ch` are explicit at member 21 `:145-169`; `P_ch` is the spectral projector of the same
charge operator at member 22 `:31-36`. This establishes the advertised joint source-record state,
conditionally on A0 and the ratified family-level premises.

### 2.2 New attack route

| Attack | Byte test | Result |
|---|---|---|
| Representation mismatch | A0's `q_src` has the exact finite source-law codomain and is explicitly only the source preparation port (member 12 `:352-371`). The incidence endpoint is identity on the chiral and source-spin spectator factors (member 09 `:22-43`). | Does not break the endpoint state formula. No nontrivial spectator action remains to conjugate `omega_tr`. |
| Domain/trace subtlety | A0 supplies finite `End(H_src^A)` and faithful ordinary trace (member 12 `:352-363`); the R3.4 theorem consumes a normalized density matrix (member 09 `:68-80`). | Typed under A0. The same point becomes fatal if A0 fails; it is not an unconditional result. |
| `N` dependence | `omega_tr` and the projectors carry no `N`; `d_ready` and `d_law` are defined for each finite `N` (member 12 `:607-654`); R3.4 is analytic for arbitrary `N` and traces back exactly (member 09 `:59-84`). | No hidden `N`-dependent state or projector is required. |
| Endpoint restoration | The computed endpoint is source-spin identity and later-cell pulses restore source-spin observables at completion (member 09 `:40-43,86-88`). | The restoration step does not alter the source marginal. |

The new attack therefore fails to break a mating hypothesis. This is a TYPE-P result, not an
unconditional physical completion: member 14 `:26-39` binds every downstream use to DoR-008,
DoR-009, and DoR-013, and A0 is authored physics.

## 3. Both marginals, and the claimed cross-check

For

```text
Omega_SR,N = [P_0 tensor R_N + P_ch tensor P_N] / Tr_A(I_src),
R_N = |r><r|^tensor_N,
P_N = |p_Q><p_Q|^tensor_N,
```

the two symbolic partial traces are:

```text
Tr_record(Omega_SR,N)
  = (P_0+P_ch)/Tr_A(I_src)
  = omega_tr,

Tr_source(Omega_SR,N)
  = [Tr_A(P_0)/Tr_A(I_src)] R_N
    + [Tr_A(P_ch)/Tr_A(I_src)] P_N
  = (1-kappa_ch) R_N + kappa_ch P_N.
```

Both marginals are therefore re-forced under the same TYPE-P premises. All quantities remain
symbolic. The record state is on `R_N`/`R_inf`, not on `B`; member 17 `:150-167` explicitly bars
that flattening.

The claimed *independence* of two `kappa_ch` routes does not survive. Member 12 `:812-826` defines
`kappa_ch=Tr_A(rho_P P_ch)` and substitutes `rho_P=I_src/Tr_A(I_src)`. The record marginal above
uses the coefficient `Tr_A(P_ch)/Tr_A(I_src)`. That is the same projector expectation under the
same normalized identity, exposed after the other partial trace. It is a valid consistency check,
but not an independent derivation from independent premises or a different receiver. The correct
classification is `ONE-COMPUTATION`.

## 4. A0 finiteness exposure

The sealed record bears on the exposure, but does not settle its physical antecedent:

- Member 12 `:330-345` says the current `C0_008` carrier is a Hilbert C-star module that exports no
  scalar trace, positive functional, or trace-class density; two inequivalent scalarizations are
  available and neither is selected. It calls the scope conflict unresolved.
- Member 12 `:352-386,896-906` identifies finite dimensionality, scalar trace, and source-port
  typing as authored physics and states the self-kill precisely: if the physical source port is
  necessarily the unscalarized full module, A0 fails and V003 dies.
- Member 14 ratifies A0 knowingly; ratification is not a derivation that the self-kill antecedent is
  false.

If A0 fails, `omega_tr` is not supplied as the claimed normalized finite-trace state; the
Gen_Omega fixed-state output, the displayed normalized `Omega_SR,N`, both forced-marginal claims,
the dimension-ratio form of `kappa_ch`, and the SR-face common-origin construction all fall.

What survives is narrower: R3.4's conditional arbitrary-`N` theorem for any independently supplied
compatible normalized charge-superselected `rho_S`; the computed cell endpoint; the finite-state
restriction identity; the algebraic tensor-factor and restriction-map typing; the continuum
non-uniqueness theorem; the typed Attach absence; and the completed-object receiver standard. No
closed member proves or refutes that the physical source port is necessarily the unscalarized full
module. The physical condition remains open at the cited bytes.

## 5. Ruling standard from the receivers

The standard stands. Member 20 `:598-635` defines the nine fields as `PhysicalSig_0`, uses them to
form `ObjType_0[I_prim]`, and types `Obj_0` as an inhabitant of that completed type. Member 18
`:229-263` requires one package whose P1 is the completed source-record-field carrier, whose P5 is
a state/effects/domains package on the completed object, and whose P6 acts on the common domain.
Member 19 `:30-61,204-233` independently types P5 as straddling the completed object and common
origin. Finally, the K4-facing restriction is explicit at member 17 `:209-221`: `res_B` consumes
`Omega_C0 in State(A_C0)`; an SR state cannot be passed to that receiver. The common-origin/K7
content has the same completed-state requirement in members 18 and 19.

Accordingly an SR-face determination is a restriction or lower bound, not a populated completed
`Obj_0` entry. The tally remains `0 EXHIBITED / 9 NOT-DETERMINED / 0 N-A`; no field moves.

## 6. Attach absence and localization

### 6.1 Typed absence confirmed

Member 16 `:314-334` states the missing object and adjudication in its own words:

```text
Attach : (connection/source history, finite incidence, U_N^0)
         -> U_N[A,J,g]

together with its representation, carrier embeddings, orientation law,
time-ordering, and common-origin certificate. No such Attach exists.

SEALED_ATTACH_MAP_EXISTS = false | TYPE-S |
```

The same member distinguishes the fixed history-free `U_N^0` from the connection-bearing maps and
says their signatures cannot be identified (`:295-334`). Its placement table gives at least four
locations and no exhaustive grammar (`:336-365`). Member 17 `:260-269` proves the continuum of
state extensions with one fixed record marginal; member 15 `:480-487` and member 16 `:356-360`
corroborate the permissive history side.

### 6.2 The residual is wider than one factor

The algebraic *state-extension* freedom displayed by member 17 is indeed in the `B` factor. The
subject's stronger claim—that all nine fields fail only once and that all residual freedom is
exactly one tensor factor wide—is contradicted by its own closed members:

- A0's finite scalar carrier remains an authored, self-killing premise (member 12 `:330-386,
  896-906`).
- On the reached SR face, the stationary incidence parent moves the product state to a disjoint
  infinite-product sector at generic nonzero time, so representation data are not closed
  (member 09 `:108-128`).
- Moving-front profile/order results are conditional because the parent has not derived the
  one-use moving-front action (member 10 `:77-101`).
- The physical write/tail join, continuum ordering, and connected preparation remain underived
  (member 11 `:27-44`; member 09 `:123-133`).
- The ratified family intentionally leaves transient channel dynamics unbound, and the character
  alternatives are not selected by the finite proof (member 12 `:678-715`; member 16 `:273-290`).

Thus `Attach` is a genuine typed wall, but it is not the sole unresolved load on the completed
physical signature. Localization must be widened.

## 7. Quarantine test

The preform file itself is sealed at the advertised digest, but the promised carriage is false.
The preform is not an exact byte substring of the subject, and the subject's §§1–2 byte region is
not byte-identical to it. A line-level comparison found 43 non-equal edit groups. Material examples
are sufficient to decide the issue:

1. Preform `:47-52` says the missing `S|e>` entry is filled from the incidence result as a downstream
   computation. Subject §1.1 instead adds member 21, changes the provenance to a common baseline,
   and expressly says it is *not* a silence filled from a second document.
2. The subject adds the same-projector attack and A0-finiteness residual in §1.3; neither block is
   in the pinned preform.
3. Preform `:198-202` states the two marginals are forced without qualification. The subject adds
   the TYPE-P premise block and the explicit nonclaim of A0-independent forcing in §2.3.

The third change is the subject's recorded verb correction. It improves semantic honesty, but it
also proves that the frozen Steps 1–2 were revised. “Corrected before final sealing” cannot satisfy
the stronger claim that the preform bytes were carried unchanged after the form was opened.

The chronology claim for the exclusion-by-path list is also not sealed by the preform: the list
appears only in the later subject §0.1, not in the preform or a separately pinned prereading receipt.
Its asserted priority therefore cannot be confirmed from the declared bytes. The adjacent-schema
disclosure is accurate—member 15 `:316-321` is a seven-field StatePort quotation, not the nine-field
`PhysicalSig_0`—but that accurate disclosure does not repair the failed byte quarantine.

## 8. Residue, flattening, and custody

A fresh fourteen-pattern scan was run over authored prose in both subject and preform. The patterns
covered fine-structure/one-over-137 forms, target or measured/experimental-value phrases, CODATA,
PDG, best-fit and agreement language, and numeric alpha/kappa assignment shapes. Result: zero hits
in each artifact. This was a structural text scan only.

Flattening check:

- SR state was not flattened into a completed `A_C0` state.
- A tensor factor was not flattened into state factorization.
- A record marginal was not flattened into a history/field marginal.
- A family-level result was not flattened into a selected member.
- A symbolic trace ratio was not flattened into a value.
- Missing construction was not flattened into physical impossibility.
- Algebraic state-extension freedom in `B` was not flattened into the wider claim that every
  physical-signature gap lies only in `B`.

```text
FREEDOMS-CONSUMED
  cited inputs: the 22 closed members and only their stated roles
  derived here: mating re-check; two symbolic partial traces; receiver typing;
                A0 failure propagation; localization counter-audit; quarantine byte comparison
  not consumed: member selection, physical fixed point, end test, numeric scale, measured target,
                PE content, freeze, omega_phys
  JOINT_ANCHOR_DERIVED = false
```

The subject's eighteen declared prose members are `18/18` strict-and-stable. The three assigned
artifact seals (subject, generator, preform) are `3/3`. The preform pin verifies as a file; it does
not verify the false claim that its bytes were carried into the subject.

## 9. Terminus verdict

The central source-record composition survives the new attack, and the completed-object ruling and
typed Attach absence stand. The result nevertheless requires correction on three load-bearing
statements: `kappa_ch` is one computation exposed twice rather than an independent derivation;
the unresolved physical load is wider than the `B` factor; and the claimed Step-1/2 byte quarantine
is broken. This is not a refutation of the conditional SR joint state. It is a correction of the
scope and custody claims around it.

CLOSURE = declared-first (byte 0; end byte 00004731; 22 members; scan reproduced subject closure byte 5828)
MATING = HOLDS-UNDER-NEW-ATTACK (representation/domain/N-dependence/endpoint-restoration)
MARGINALS = BOTH-REFORCED (TYPE-P: DoR-008/009/013 + A0 finite scalar source realization)
KCH_ROUTES = ONE-COMPUTATION
A0_EXPOSURE = STATUS-REPORTED (joint state, both forced marginals, and dimension-ratio form fall with A0; conditional R3.4 theorem, algebra typing, Attach absence, and receiver standard survive)
RULING_STANDARD = STANDS (tally remains 0/9/0; completed Obj0/A_C0 receivers do not accept an SR face)
LOCALIZATION = WIDER (A0 finiteness; SR representation; moving-front causal parent; write/tail and continuum-preparation conditions; residual family/character data)
ATTACH_ABSENCE = TYPED-CONFIRMED ("No such Attach exists"; full type and required adjuncts at member 16 :314-334)
QUARANTINE = BROKEN
PROSE_DIGESTS = 18/18 STRICT==STABLE; assigned subject/generator/preform seals 3/3
RESIDUE_GREP = CLEAN (14 fresh patterns, 0 hits in subject and preform)
VERDICT = CORRECTED
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
