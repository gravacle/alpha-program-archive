# STAGE 8 — ADVERSARIAL AUDIT OF THE BIT DETERMINATION (RK_LT_BIT S9AD) — V001

## ADVERSARIAL AUDITOR — CODENAME RKB-AUDIT — [SEALED]

Date: 2026-08-15 (session UTC). Target: `STAGE8_RK_LT_BIT_S9AD_V001.md`
(seal `ff523db5…` MATCH at path before opening). Default posture:
REFUTE. Independence honored: the bit was worked from the sealed
carriers FIRST and the auditor's verdict-mapping was FIXED AND HASHED
BEFORE the build was opened —

```text
PRE-VERDICT FIXED 2026-08-15T06:56Z, BEFORE FIRST READ OF THE BUILD:
  SHA-256(RKB_AUDIT_PREVERDICT.md)
  = 1a60b48ef3c3d224f1f16624983b78cf3c0e96da7dc9a460fac36cd57d60bb23
PRE-VERDICT CONTENT (summary): own determination = the bit
(Rhat_K,N in R·L_T,N inside O_R5,N = R_K_EQUALS_L_T, TYPE-U at
70dde1c0 :876 / 041498bb :354) is NOT DERIVABLE from sealed record at
cutoff — no E-2 identity (no displayed L_T formula anywhere; the only
normalization runs through the P_adm family whose chi is free, (S1-4);
the rank-1 "Hom is 1-dim" shortcut barred at 041498bb :303-305), no
E-1 separator (complement placement unproved at :549-556; Schur kills
the block split; chi_K TYPE-U; no degeneracy/wavefront typing bytes —
own sweeps zero hits), all three (S1-5) shapes blocked. FIXED MAPPING:
build=UNDECIDABLE+adequate blockers => CONFIRMED (or C-W-C if the list
is defective); build=E-1 via complement language or E-2 via unsealed
normalization => REFUTED-AT; build absent/seal-fail => UNDETERMINED-AT.
```

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION AUDIT ONLY —
no member bound, no adoption, no authored physics, no route selected,
no score/gate moved. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…"
tokens EXPECTED-UNLOCATABLE. No git; no existing file edited; output +
sidecar probed ABSENT at session start and re-probed ABSENT at
2026-08-15T07:01:47Z immediately before this write; ONE output file
plus its seal sidecar. CAS in a FRESH venv (sympy 1.14.0): the build's
battery re-run VERBATIM (21/21 PASS reproduced, names matching the
transcript exactly) + the auditor's OWN battery (17/17 PASS, exact
symbolic, no float, no value frozen).

**SWEEP CUTOFF: 2026-08-15T07:01Z.** Exhaustiveness sweep-displayed
only; nothing absolute is claimed.

---

## 0. VERDICT

```text
VERDICT = CONFIRMED-WITH-CORRECTIONS

The build's verdict UNDECIDABLE-TODAY STANDS: it coincides with the
auditor's independently fixed determination (pre-verdict branch 1
fired), every load-bearing byte citation is verbatim at path, every
seal MATCHES, the blocker list covers the flags' own (S1-5) shape by
shape at named NOT_BUILT/UNDECLARED objects, the TYPE-U flag pair is
CONFIRMED — never superseded by re-reading — and the two-model
non-entailment certificate survives the auditor's strongest attack
(C-3 below). No E-1 separator and no E-2 identity is claimed anywhere
in the build, so neither named kill has a surface to land on.

THREE CORRECTIONS (none moves the verdict):

C-1  SWEEP MISS, DISPOSED HARMLESS. `ccb94dfa` §0.4 l.143 — "L_T
     normalized local Maxwell kernel, not R_K" — is an L_T-vs-R_K site
     RS-2/RS-3 did not surface (the patterns could not match "not
     R_K"). Disposal: it is a symbol-glossary disambiguation line in a
     "Symbol distinctions" table (l.133-146 read at bytes), comparing
     BARE R_K — an ill-typed comparison for the bit by the build's own
     T2-3 guard — with no derivation and no authority; it cannot
     supersede the TYPE-U flags (supersession is by derivation only).
     The bit's standing is unaffected; the sweep protocol is the
     defect.
C-2  BATTERY VACUITY. Checks B3b, B3c, B6c, B6d, and the first
     conjunct of B7 are structurally vacuous as coded: a sympy Symbol
     compares `!= 0` as True REGARDLESS of assumptions (auditor check
     A8: the comparison is True even without `nonzero=True`), so those
     five checks cannot fail and the transcript's "21/21 PASS"
     overstates its evidentiary weight. The CLAIMS they label are
     nevertheless correct — re-derived non-vacuously by the auditor
     (A2a/A2b: the E-2 separator system is inconsistent under k != 0
     by solve, not by structural comparison; A5: the mixing value
     -(1/a_K)Kern(R_K) admits no vanishing solution; A3a/A3b: the
     descent equivalence at the presentation, both directions). No
     conclusion moves; the battery's coding discipline is the defect.
C-3  CERTIFICATE CLAUSE SET INCOMPLETE — CONCLUSION SURVIVES. §6's
     clause set S omits the positive reading of `041498bb` l.303-304
     ("The live domain explicitly includes nonlocal and higher-
     derivative structures…") / (R1-1)'s declared-structures clause.
     The coded M_E2 (O literally one-dimensional) violates that
     reading. The non-entailment theorem survives the omission: the
     auditor's extension model M_E2' — O two-dimensional, a NONZERO
     higher/nonlocal line independent of L_T present, Rhat_K = a_K L_T
     — satisfies S PLUS the omitted clause with the bit TRUE (A9a-A9e:
     mixing line present, X-1's slice closure still holds, both
     objects nonzero), while M_E1 satisfies it with the bit FALSE.
     "Any future decision must consume a sealed object outside S"
     therefore stands with S enlarged; §6's model display is the
     defect, not its theorem.

SCOPING REMARK (no correction): T-1's "exact both ways" equivalence is
exact AT THE PRESENTATION (dim E_N = 3 at rank 1, every functional
continuous, descent per (LM2-3) — auditor A3a/A3b); extending a
separator from im(qtilde_N) to all of O_R5,N is a topology question
outside record. Only the direction "separator => E-1" is load-bearing
for the circularity conclusion, and that direction is unconditional.

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED.
```

---

## 1. SEALS RE-VERIFIED AT PATH BY THE AUDITOR (shasum -a 256), BEFORE RELIANCE

```text
TARGET
ff523db5  STAGE8_RK_LT_BIT_S9AD_V001.md                  MATCH (opened only
          after the pre-verdict hash was fixed)
COMMISSION STOCK / SIBLINGS
cfa2fb97  STAGE8_IDEAL_EXTENT_S9AD_V001.md               MATCH (read in full
          BEFORE the build, as the bit's framing of record)
eda2ba74  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md         MATCH (verdict row
          + flag-cite rows read)
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md              MATCH
a7255f38  STAGE8_EXTSRC_ROUND3_S9AD_AUDIT_V001.md        MATCH
FLAG SITES AND CHASED CARRIERS (all independently re-hashed)
70dde1c0  …LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001   MATCH (read in full)
041498bb  …CONSUMPTION_VERIFICATION_AND_PUSHFORWARD…     MATCH (§3.1-3.5,
          §5.3 at bytes)
b53d9e93  …EQ6_THE_LOC_BUILD_LANE3_V001                  MATCH (l.130-146,
          l.938-944 at bytes)
bae34116  …R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001          MATCH (l.393-428,
          478-488, 550-560, 600-614 at bytes)
be570c18  …STATIONARY_RESPONSE_COMPUTATION_LANE1_V001    MATCH (l.209-215)
6b7f8f98  …RETEXTRACT_KERNEL_CERTIFICATE_LANE1_V001      MATCH
d662889b  …EXTSRC_BUILD_LANE3_V002                       MATCH
790ae95b  …BIND_INPUT_SIGNATURE…DETERMINATION_V001       MATCH (l.295)
e287b057  …EQ6_SEED_ADJUDICATION_DARIO_V001              MATCH (l.153-157,
          291-297)
ccb94dfa  …R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS…    MATCH (l.128-150)
88854f08  …B_IND_ANALYTIC_STRUCTURE_LANE1_V001           MATCH (l.553-557,
          638-642)
MISMATCHES = 0 (16/16). Every line number the build cites was re-read at
bytes by the auditor and found VERBATIM: 70dde1c0 :876 flag; 041498bb :354
flag, :303-305 not-declared-one-dimensional, :320 (R2-4), :549-556
does-not-prove; b53d9e93 :136-138 the (S2a) scoping sentence ("It does not
define its formula on the separate L_T,N summand"), :941-943 route bar
("not admissible alternatives: … R_K=L_T …"); 790ae95b :295 BIND row;
ccb94dfa :142 sigma_loc unbuilt; 88854f08 :555 (U3-11), :640 the
would-decide evaluation. The held construction 506bfe2a untouched.
```

## 2. THE FIVE COMMISSIONED ATTACKS, RUN TO GROUND

```text
ATT-1  E-1 SEPARATOR (the named kill: a property asserted of Rhat_K
   from unproved complement language). NO SURFACE: the build claims
   E-1 NOT DERIVED and handles the complement line as
   REFUSED-AS-UNFORMED — verified sound at bytes: (R2-4) is stated as
   MISSING at 041498bb :320 (auditor re-read), sigma_loc typed unbuilt
   at both definitional sites (ccb94dfa :142; 88854f08 :555), so the
   complement proposition has no subject of record. The build never
   asserts the property; it refuses the question. Separator-class
   closure re-checked: auditor's own sweeps for transversality/
   degeneracy/wavefront/support typing of Rhat_K,N and for any
   displayed L_T formula -> ZERO hits (independently of RS-3/RS-8);
   one glossary near-hit found and disposed (C-1). KILL DOES NOT FIRE.
ATT-2  E-2 IDENTITY (the kill: proportionality through an unsealed
   normalization). NO SURFACE: a_K has no display; LINK-1's four rows
   re-verified at bytes (p_loc: chi_K TYPE-U at 70dde1c0 §3.6/§8;
   Kernbar: the (S2a) scoping sentence excludes L_T,N from its domain
   of record; Sch_N: [YOURS]-block (S4); sigma_car: no L_T image).
   The rank-1 one-dimensionality shortcut — the auditor's pre-named
   E-2 trap — is NOT taken (RS-9 zero hits re-confirmed; 041498bb
   :303-305 at bytes). KILL DOES NOT FIRE.
ATT-3  UNDECIDABLE BLOCKER LIST vs THE FLAGS' OWN (S1-5) AND THE
   CORPUS. COMPLETE: shape 1 — the free (LM2-17) variant proves
   nothing (re-derived, A-battery B4-analogue); the deciding variant
   needs a certified complement/separating functional (circular per
   T-1, absent per RS-5/auditor sweeps). Shape 2 — sigma_loc NOT_BUILT
   (both sites at bytes; (LM2-20) no named authority, re-read
   l.600-614). Shape 3 — T-5's direction-reversal VERIFIED in general
   finite-dim Hausdorff models (A4: zero quotient dual <=> R
   proportional to L — the shape lands in E-2's configuration, and is
   inert without TOP-1/TOP-2; the conditional labeling in CL-6 is
   honored). The blocker map subsumes the auditor's pre-fixed list
   (S1-5 x3 + Schur non-separation §3.3 + P_adm chi-freedom) exactly.
ATT-4  TYPE-U FLAG AUTHORITY. RESPECTED: both flags re-confirmed at
   bytes and never re-read into a horn; the build's only upgrade — the
   non-entailment certificate — is a NEW derivation about the clause
   set, moves no consumed verdict, and assigns no horn (the models are
   quarantined; neither is preferred). Consistent with the flags
   remaining the record's standing.
ATT-5  SEALS/BYTES/CAS. 16/16 seals MATCH (§1); 12 cited spans
   verbatim; battery re-run VERBATIM in a fresh venv: 21/21 PASS,
   transcript names identical; auditor's own battery 17/17 PASS
   (source displayed in the appendix). Defects found: C-2 (vacuous
   structural checks — labeled claims independently re-proved).
```

## 3. SWEEPS RUN BY THE AUDITOR (cutoff 2026-08-15T07:01Z; roots = workspace, supervision, cleanroom_output, relay_inbox, relay_outbox; fenced filename classes excluded; a32_holdout not content-swept)

```text
AS-1  displayed L_T formula ("L_T = <object>" beyond p_loc[L_T]=1)
      -> ZERO hits. No E-2 chain can start; confirms §3.2's three-
      bytes-only display.
AS-2  proportionality family (a_K L_T | R_K=L_T | proportional) ->
      the three sites of record + the extent pair + e287b057 (quotes
      only) + the build itself. No identity, no refutation. RS-2
      CONFIRMED, with the C-1 near-miss added and disposed.
AS-3  degeneracy/rank/invertibility typing of L_T; wavefront/support
      typing of Rhat_K -> ZERO hits. RS-3/RS-8 CONFIRMED.
AS-4  sigma_loc built anywhere -> ZERO hits (both definitional sites
      type it unbuilt). RS-4 CONFIRMED.
AS-5  covariant-dual-zero theorem -> only the NEGATIVE statement
      (041498bb :305). RS-5-adjacent CONFIRMED.
AS-6  dimension typing of O_R5 -> ZERO hits. RS-9 CONFIRMED.
AS-7  new sealed .md between the extent display's 05:30Z cutoff and
      the build's write -> exactly ONE non-fenced object: the extent
      AUDIT (eda2ba74). RS-6 CONFIRMED. (Fenced-class basenames
      appeared in listings; none opened.)
AS-8  post-build landings bearing on the bit at this cutoff -> none
      (the only newer non-fenced object is this audit's own absence,
      probed twice).
```

## 4. INDEPENDENT RE-DERIVATIONS (auditor battery, 17/17 PASS, fresh venv)

```text
A1  the two-entry extent menu at rank 1 (kernel dims 1 and 2; mixing
    line (-a_K,1,0)) — re-derived by nullspace, exact.
A2  E-2 separator nonexistence — by solve: {lam=1, a_K·lam=0} is
    inconsistent under a_K != 0 (NOT by structural comparison; cf C-2).
A3  T-1 descent equivalence at the presentation — tuple (1,0,0)
    annihilates E-1's Rel and fails E-2's mixing line, exactly.
A4  T-5 shape-3 direction — general 2-dim Hausdorff model: zero
    quotient dual <=> Rhat_K proportional to L_T. Direction-reversal
    CONFIRMED under TOP-1/TOP-2; inert without them.
A5  X-3 value — -(1/a_K)Kern(R_K) admits no vanishing solution in
    nonzero symbols; equality to -(kappa/a_K) exact.
A6  T-3 coset identity — (0,1,0)-(a_K,0,0) lies in E-2's Rel_N.
A7  B9's gap closed — covariance is vacuous at rank 1 in the COMPLEX
    reading too: e^{i th} · C · e^{-i th} = C on Hermitian 1x1 (the
    build checked only U = ±1; consistent with CL-4's rank-1
    real/complex coincidence).
A8  the C-2 vacuity demonstration (Symbol != 0 is structurally True).
A9  the C-3 extension model M_E2' — certificate survives the omitted
    positive-structure clause with the bit TRUE; X-1's slice closure
    holds inside it.
```

## 5. CONSISTENCY

Nothing here binds a member, adopts anything, authors physics, selects
a route/branch/address/representative, values a_K or chi or kappa, or
touches the held construction. The auditor's models are verification
instruments quarantined below. The build's UNDECIDABLE-TODAY is
confirmed, its three defects are display/protocol-grade, and the
missing-closure-object list N-1..N-3 stands as the exact frontier.
B-1 remains UNDECIDABLE-STILL of record. SK-2 does not fire; ACT-3 is
not forced; the D-1 menu is unchanged. R9 and A8 remain
UNEXECUTABLE/PENDING. No score, flag, criterion, or gate moves.

## 6. CHOICE LEDGER (terminal)

```text
ACL-1  INDEPENDENCE MECHANICS: the bit was worked from the sealed
       carriers first (70dde1c0 and 041498bb read in full/at spans,
       the extent pair, LM2/W1 bytes); the verdict mapping was written
       and SHA-256-fixed (1a60b48e…) before the first byte of the
       build was read; the build's outcome was not known to the
       auditor at fix time. The extent pair predates the build and
       frames the commission; reading it is not outcome knowledge.
       PREMISE(named).
ACL-2  CORPUS BOUNDARY: five roots minus fenced filename classes;
       a32_holdout not content-swept (holdout discipline); sweeps
       AS-1..AS-8, cutoff 2026-08-15T07:01Z. A wider sweep changes
       this audit only by exhibiting a sealed deciding object (N-1..
       N-3 shaped) that both RS- and AS-sweeps missed.
       PREMISE(named).
ACL-3  CONSUMPTION GRADES: extent pair and round-3 pair at audited
       grade; flag/carrier bytes re-verified at path before reliance;
       6b7f8f98/d662889b consumed via the extent display's verified
       spans (as the build did). PREMISE(named).
ACL-4  EXTERNAL-MATH IMPORTS: finite-dimensional exact linear algebra
       (nullspace, linsolve, solve on dim <= 3) and, ONLY inside the
       conditional T-5/A4 display, the locally-convex-Hausdorff
       separation fact — same conditional footing as the build's CL-6.
       PREMISE(named).
ACL-5  C-1's DISPOSAL RULE: a glossary/notation line is not a
       derivation and cannot supersede a TYPE-U flag; and a bare-R_K
       comparison is ill-typed for the bit by the sealed T2-3 guard.
       Both legs are at bytes; the disposal uses no other authority.
       PREMISE(named).
ACL-6  DISCLOSURE: an early broad grep for TYPE-U/RK_LT listed
       fenced-class BASENAMES (supervision handoff files) among hits;
       none was opened — the two handoff addenda whose names matched
       were left unread precisely to protect ACL-1. One register-class
       filename appeared in a directory listing; not opened.
       PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The auditor's model instruments (2-dim and 1-dim codomains, the
  extension model M_E2', symbolic a_K/chi/kappa/m1/m2/theta, never
  evaluated) exist only to (i) re-prove the build's labeled claims
  non-vacuously and (ii) test the non-entailment certificate against
  an enlarged clause set. No toy value, dimension, or number enters
  the verdict or the corrections; neither horn of the bit is
  preferred, scored, or recommended; a_K remains undisplayed of
  record.
```

## FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS ; target
  STAGE8_RK_LT_BIT_S9AD_V001.md (ff523db5) ; its
  UNDECIDABLE-TODAY STANDS ; E-1 NOT CLAIMED ; E-2 NOT CLAIMED ;
  a_K NO DISPLAY ; R_K_EQUALS_L_T remains NO_VERDICT / TYPE-U
INDEPENDENCE = pre-verdict fixed+hashed BEFORE build opened
  (1a60b48ef3c3d224f1f16624983b78cf3c0e96da7dc9a460fac36cd57d60bb23,
  06:56Z) ; auditor's own determination coincided: NOT DERIVABLE
CORRECTIONS = C-1 sweep miss (ccb94dfa §0.4 l.143 "not R_K" glossary
  line — disposed harmless at bytes) ; C-2 battery vacuity (B3b/B3c/
  B6c/B6d/B7-first structurally unfailable; claims re-proved
  non-vacuously A2/A3/A5) ; C-3 certificate clause set omits the
  positive nonlocal/higher-structure byte (041498bb :303-304) —
  non-entailment SURVIVES via extension model M_E2' (A9)
NAMED_KILLS = neither fires (no complement-language separator
  asserted ; no proportionality through any normalization appears)
BLOCKER_MAP = covers (S1-5) shape-by-shape at named NOT_BUILT/
  UNDECLARED objects ; T-5 direction-reversal VERIFIED (A4) ;
  Schur non-separation and P_adm chi-freedom present ; subsumes the
  auditor's pre-fixed blocker list exactly
FLAG_AUTHORITY = respected ; flags confirmed, not superseded ; the
  certificate is a new derivation about the clause set, not a
  re-reading ; no horn assigned anywhere
SEALS = 16/16 MATCH re-hashed at path ; 12 cited spans VERBATIM ;
  MISMATCHES = 0
SWEEPS = AS-1..AS-8 ; CUTOFF 2026-08-15T07:01Z ; RS-1..RS-9 spot-
  reproduced ; one near-miss added and disposed (C-1)
BATTERY = build's source re-run VERBATIM, fresh venv, sympy 1.14.0,
  21/21 PASS reproduced ; auditor battery 17/17 PASS ; exact symbolic
  only ; no float ; no value frozen ; NUMERIC_EVALUATION = exact
  integers/rationals/symbols only ; MEASURED_CONSTANT_COMPARISON = none
DETERMINATION_ONLY = held ; MEMBER_BOUND = false ; CHAIN_INVOKED =
  false ; HELD_CONSTRUCTION untouched (506bfe2a)
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
R9 = UNEXECUTABLE/PENDING ; A8 = UNEXECUTABLE/PENDING
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the determination survives its adversary: the auditor arrived
  at the same closed door independently, then failed to break the
  build at any byte, seal, or derivation. The bit stays exactly one
  new sealed object wide (N-1 | N-2 | N-3), and the record's TYPE-U
  standing is now double-locked — no sealed byte fixes it, and the
  displayed clauses provably entail neither horn. Nothing moved; the
  gates are where the principal left them.
```

---

## APPENDIX — AUDITOR BATTERY SOURCE (VERBATIM) AND TRANSCRIPT

```python
# RKB-AUDIT independent battery — exact symbolic only; no float; no value frozen.
# Purpose: (A) re-derive the build's load-bearing claims with NON-vacuous checks;
# (B) demonstrate the vacuity of the build's structural !=0 checks (finding C-B);
# (C) attack the two-model certificate with the omitted-clause extension model (C-C).
import sympy as sp

k, chi, kappa, lam, m, th = sp.symbols('k chi kappa lam m th', nonzero=True)
a, b, c = sp.symbols('a b c')
P = []


def check(name, cond):
    assert cond, name
    P.append(name)


# ---- A1: E-1 / E-2 kernel menu re-derived (X-2 shape) ----
L1 = sp.Matrix([1, 0]); R1 = sp.Matrix([0, 1])
ns1 = sp.Matrix.hstack(L1, R1, R1).nullspace()
check("A1a: E-1 ker dim 1, = R4 line", len(ns1) == 1 and ns1[0][0] == 0
      and sp.simplify(ns1[0][1] + ns1[0][2]) == 0)
ns2 = sp.Matrix([[1, k, k]]).nullspace()
check("A1b: E-2 ker dim 2", len(ns2) == 2)
span2 = sp.Matrix.hstack(*ns2)
check("A1c: mixing (-k,1,0) in E-2 ker",
      len(sp.linsolve((span2, sp.Matrix([-k, 1, 0])))) == 1)

# ---- A2: B3b done RIGHT — E-2 separator nonexistence via solve ----
# ell determined on 1-dim O by ell(L)=lam; constraints: lam=1 and k*lam=0.
sols = sp.solve([sp.Eq(lam, 1), sp.Eq(k * lam, 0)], [lam, k], dict=True)
check("A2a: E-2 separator system forces k=0 (excluded): no admissible sol",
      all(s.get(k, k) == 0 for s in sols) if sols else True)
# and with k held nonzero the system is inconsistent:
check("A2b: inconsistent under k!=0",
      sp.solve([sp.Eq(lam, 1), sp.Eq(k * lam, 0)], [lam]) == [])

# ---- A3: T-1 descent equivalence at the presentation ((LM2-2)/(LM2-3)) ----
# separator tuple (lambda,chi,T)=(1,0,0): annihilates E-1's Rel; fails E-2's.
ell = lambda v: 1 * v[0] + 0 * v[1] + 0 * v[2]
check("A3a: (1,0,0) annihilates R4 line (descends under E-1)",
      sp.simplify(ell(sp.Matrix([0, 1, -1]))) == 0)
check("A3b: (1,0,0) fails mixing line (no descent under E-2)",
      sp.simplify(ell(sp.Matrix([-k, 1, 0])) + k) == 0 and
      sp.solve(sp.Eq(-k, 0), k) == [])

# ---- A4: T-5 shape-3 direction, general 2-dim Hausdorff model ----
# R=(m1,m2); functionals vanishing on L=(1,0) are multiples of (0,1);
# zero quotient dual <=> (0,1) kills [R] <=> m2=0 <=> R = m1*L (E-2 config).
m1, m2 = sp.symbols('m1 m2')
check("A4: zero quotient dual <=> m2=0 <=> R proportional to L",
      sp.solve(sp.Eq(m2, 0), m2) == [0] and
      sp.simplify(sp.Matrix([m1, 0]) - m1 * L1) == sp.zeros(2, 1))

# ---- A5: X-3 value done RIGHT — mixing value cannot vanish ----
value = -(sp.Integer(1) / k) * kappa
check("A5: -(1/k)*kappa = 0 has no solution in nonzero (k,kappa)",
      sp.solve(sp.Eq(value, 0), kappa) == [] and
      sp.simplify(value * k + kappa) == 0)

# ---- A6: E-2 coset identity Q(0,1,0) = Q(k,0,0) (T-3) ----
check("A6: (0,1,0)-(k,0,0) = (-k,1,0) lies in E-2's Rel",
      len(sp.linsolve((span2, sp.Matrix([0 - k, 1 - 0, 0 - 0])))) == 1)

# ---- A7: covariance vacuous at rank 1, complex reading too (B9 gap) ----
U = sp.exp(sp.I * th)
C1 = sp.Symbol('t', real=True)
check("A7: U C U^* = C for U=e^{i th} on Hermitian 1x1",
      sp.simplify(U * C1 * sp.conjugate(U) - C1) == 0)

# ---- A8 (C-B finding): the build's structural checks are vacuous ----
z_free = sp.Symbol('z_free')            # NO nonzero assumption
check("A8: 'Symbol != 0' is True even WITHOUT the nonzero assumption "
      "(so B3b/B6c/B7-clause-1 as coded cannot fail)", (z_free != 0) is True)

# ---- A9 (C-C finding): certificate survives the omitted clause ----
# Extension model M_E2': O = R^2, L=(1,0), independent higher/nonlocal
# structure H=(0,1) PRESENT and nonzero (041498bb l.303 read positively),
# Rhat_K := k*L. All S-clauses + positive-structure clause hold; bit TRUE.
Rk2 = k * L1
H2 = sp.Matrix([0, 1])
M = sp.Matrix.hstack(L1, Rk2, Rk2)      # qtilde = a*L + (b+c)*k*L
nsE = M.nullspace()
check("A9a: M_E2' ker dim 2 (mixing present: bit TRUE)", len(nsE) == 2)
spanE = sp.Matrix.hstack(*nsE)
check("A9b: mixing (-k,1,0) in M_E2' ker",
      len(sp.linsolve((spanE, sp.Matrix([-k, 1, 0])))) == 1)
cyc = sp.Matrix([[k, k]]).nullspace()
check("A9c: M_E2' pure-cycle slice = R4 line (X-1 holds)",
      len(cyc) == 1 and sp.simplify(cyc[0][0] + cyc[0][1]) == 0)
check("A9d: higher/nonlocal structure nonzero and independent of L",
      len(sp.linsolve((sp.Matrix.hstack(L1), H2))) == 0)
check("A9e: L!=0, Rhat_K!=0 in M_E2'",
      L1 != sp.zeros(2, 1) and sp.solve(sp.Eq(k, 0), k) == [])

print(f"{len(P)}/{len(P)} PASS")
for x in P:
    print(" ", x)
```

```text
TRANSCRIPT (fresh venv, sympy 1.14.0):
17/17 PASS
  A1a A1b A1c A2a A2b A3a A3b A4 A5 A6 A7 A8 A9a A9b A9c A9d A9e
BUILD BATTERY RE-RUN (same venv, source extracted verbatim from the
target's appendix): 21/21 PASS, check names identical to the target's
transcript.
```
