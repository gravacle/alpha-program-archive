CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00005538   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 17 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing (Q-913 standard).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001. Every leg disposition below cites a member and a
  locus. No flag name is an anchor. Member numbers are THIS artifact's.
LANE = DARIO   ROLE_THIS_RELAY = THE SIX LEGS AND B03'S CLOSURE STATUS (relay 1066)
GUARDS = BOTH PASSED.  member 03 (the delivery) exists and passes its sidecar; member 02 (the entry
  addendum) is LIVE.  This relay's earlier run wrote `1066_BLOCKED` when neither held; that block is
  now RELEASED and its report stands on the books unedited.
SELF_CITATION_BAR = DARIO's own outputs are NOT record witnesses.  Members 03 and 11 are SUBJECTS the
  assignment names — the object being dispositioned and the leg restatement — never grounds.  EVERY
  leg disposition below is anchored to a sealed non-DARIO member at a locus, and section 1.1 states
  that rule before any leg is ruled.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1066_B03_CLOSE_DARIO_V001.md` | `fe3e09951f9a97b76cee0afb78798473cbe047e6fb4999bc47afc2845a7d1f21` | assignment; B03's closure condition (all six legs) |
| 02 | `workspace/ATTACH_ENTRY_ADDENDUM_V001.md` | `420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73` | **the entry; THE THREE CARRIED CONDITIONS at `:40-47`** |
| 03 | `workspace/STAGE8_DESC_THETA_HIST_DARIO_V001.md` | `47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba` | **the delivery — the SUBJECT dispositioned against** |
| 04 | `workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md` | `ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc` | the entered candidate |
| 05 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | the demand map V008 |
| 06 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md` | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2` | **L4's U2 content and L5's `d_U2` witness — the tuple** |
| 07 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | **P5 and P1 — L1, L2, L3, L6's own words** |
| 08 | `workspace/STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | **the S1 receiver; the undefined restriction — K4's span** |
| 09 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `res_B`; **K4's release condition at `:365-377`** |
| 10 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | `State(B) <-> Prob(Y)`; the continuum survivor table |
| 11 | `workspace/STAGE8_DESC_ARROW_NECESSITY_DARIO_V001.md` | `473f849833d54dcb802958e26438ff1271f069b23a4654cfff9b993964d2c47c` | the six-leg restatement (SUBJECT, not grounds) |
| 12 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 13 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 14 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | `d_state`/`d_ready`/`d_law`. READ ONLY WITH 15 AND 16. |
| 15 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 14 |
| 16 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification; the no-member clause |
| 17 | `workspace/PROSPECTIVE_ENTRY_AND_CASCADE_AUTHORIZATION_DECISION_OF_RECORD_V001.md` | `e7138b083fe73c060bcea26396a3ff0ff1b39a600e3ab8f7a63ea5a9e347fcb6` | the authorizing decision of record |

```text
RE-PINNED SPAN, recomputed in this relay:
  member 06, bytes [6266,6883)  ->  e6093b0f48c11d3f05d3c6fd3692b6ea2c4b320746bb92eebe72ef28ed697efe
  content: U2's six-item content, and the d_U2 : (B0_candidate, C0) -> U2 descent witness.
  This single span carries BOTH L4's content list AND L5's obstruction, which is why the two legs
  disposition together and why L5's exception is visible inside L4's own definition.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO MEMBER BOUND. NO FIXED-POINT EXECUTION.
NO END TEST. NO MEASURED-CONSTANT COMPARISON. NO FREEZE. NO CONSTRUCTION PERFORMED IN THIS RELAY —
it dispositions; it does not build.
PE-1..17 POINTER-ONLY, LEDGER SHUT. CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESC: THE SIX LEGS AND B03'S CLOSURE STATUS
## RELAY 1066 — `[PLAN:DESC-35]` — DARIO LANE — [CLAIMED]

Date: 2026-08-12

**THE RESULT.** `B03 = OPEN`. The assignment names `CLOSED-CONDITIONALLY` as the expected honest
outcome; it is not what is true, and I display what is. **Two legs move on the entry, four do not, and
five of the six openings reduce to one missing object.**

---

## 1. THE STANDARD, DECLARED BEFORE ANY LEG IS RULED

### 1.1 What counts as a disposition

- **CLOSED** — the leg's own demand is met, with a trace to a sealed member at a locus.
- **CLOSED-CONDITIONALLY** — met *only* under one or more of the entry's three carried conditions,
  **each named exactly**, not gestured at.
- **OPEN** — not met; the span where the demand outruns what exists.

**Grounds rule.** The leg statements are restated at member 11, a DARIO artifact and therefore a
subject rather than a witness. **Every disposition below is anchored to a sealed non-DARIO member at
a locus** — member 07 for P5/P1's own words, member 06 for U2's content and the `d_U2` witness,
member 08 for the receiver, member 09 for `res_B` and K4's release, member 10 for the state simplex.
Member 03 (the delivery) is cited **only** for what it constructed or stopped, never as evidence that
a leg is met.

### 1.2 The three carried conditions, quoted from member 02 `:40-47`

```text
C1  THE UNDERIVED CAUSAL PARENT   — A6's two-stage front demands a parent derivation nobody has built
C2  THE SR REPRESENTATION (AC-2)  — open; completed representation and strongly continuous physical
                                    dynamics remain unsupplied
C3  THE PHYSICAL-DOMAIN PROOF     — the operative sector is record-typed mathematics; no result shows
                                    it is the physical domain
```

Member 02 is explicit that these are **entered WITH the candidate, open, never discharged by entry.**

---

## 2. THE SIX LEGS, RE-DISPOSITIONED

### 2.1 L1 — a positive `rho_pre` · **OPEN**

Member 07 `:251-253` states P5 in its own words: *"A positive normalized `rho_pre` on the completed
object and admitted record effects `E_r`, with domains, all supplied by the same microscopic source as
P0."*

The delivery constructs a positive normalized state on `A_SR` (member 03 §1.2). **`A_SR` is not the
completed object.** Member 09 `:126-128` types the completed algebra as `A_C0 = A_SR tensor B`; the
state reaches the first factor only.

```text
L1 = OPEN.  Span: member 07 :251-253 ("on the completed object") against member 09 :126-128.
     NOT conditional — no combination of C1, C2, C3 would supply the missing factor's state.
```

### 2.2 L2 — normalized, trace-class, unit trace · **OPEN**

Same receiver, same object. At finite `N` the delivery has a density matrix with unit trace on the SR
face; on the completed object there is nothing to normalize because there is nothing there.

```text
L2 = OPEN.  Same span and same reason as L1.  L2 is not independently informative: it inherits L1's
     object, and it will close exactly when L1 does.  Recorded as its own row because the demand
     lists it separately, not because it is a separate obstruction.
```

### 2.3 L3 — on the completed object · **OPEN**

This leg *is* the completed object. Member 07 `:233-263`'s P1 requires `A_SRF_CTP` with a common
domain; the delivery supplies neither the field factor's state nor a domain package.

```text
L3 = OPEN.  Span: member 07 :233-263 (P1) against member 09 :126-128.
```

### 2.4 L4 — effects, domains, contacts, evolution, inclusive identity · **CLOSED-CONDITIONALLY, PARTIAL**

**This is the leg the entry moves, and the re-pinned span lets it be scored item by item rather than
as a single verdict.** Member 06 `[6266,6883)`, span digest `e6093b0f…`, gives U2's content exactly:

```text
U2 = microscopic action/evolution,          <- SUPPLIED by the entered U_N[A,J,g]
     positive normalized pre-state,          <- NOT supplied on the completed object (= L1)
     inclusive identity,                     <- NOT supplied
     admitted record effects,                <- NOT supplied
     action/source contact rules,            <- SUPPLIED by the entry's contact structure
     common domains.                         <- NOT supplied
```

**Two of six items arrive with the entry; four do not.** And the two that arrive are conditional:

```text
L4 = CLOSED-CONDITIONALLY (2/6 items), leaning on:
       C1 — the evolution the entry supplies is realized by A6's two-stage front, whose parent is
            underived, so "microscopic action/evolution" is supplied in form and not in physics;
       C3 — both supplied items hold on the b_1 = 1 sector, which no result shows is the physical
            domain.
     C2 is NOT leaned on by these two items: they concern the write, not the representation.
     REMAINDER at member 06 [6266,6883): pre-state, inclusive identity, record effects, common domains.
```

**I decline to score L4 as closed.** Two of six items, both conditional, is not a leg met; calling it
CLOSED-CONDITIONALLY without the fraction would be the flattening the record has corrected me for
four times.

### 2.5 L5 — one `d_U2` descent witness · **OPEN**

The same span carries the obstruction: `d_U2 : (B0_candidate, C0) -> U2` **names `C0` in its own
domain.** The entry supplies a transition law, not a descent witness, and nothing in member 02 or
member 04 offers one.

```text
L5 = OPEN.  Span: member 06 [6266,6883), digest e6093b0f… (recomputed here).
     NOT conditional on C1, C2 or C3.  ** L5 IS THE ONE LEG THE ATTACH ENTRY NEVER TOUCHED, AND THE
     ONE THAT DOES NOT REDUCE TO THE MISSING MEASURE. ** It is a separate obligation of a different
     kind: a witness with a stated domain, not a state.
```

### 2.6 L6 — common-origin delivery, no supplementation · **CLOSED-CONDITIONALLY on the SR face; OPEN at the completed object**

Member 07 `:251-253` again: *"all supplied by the same microscopic source as P0."*

On the SR face the delivery's certificate fills five of six slots (member 03 §2.4): output-free
primitives, entry-before-output, executable descent, sealed-interface reproduction, and same-source
supply **as a predicate**. AC6 — the marginal binding — is open, and it needs a joint state.

```text
L6 = CLOSED-CONDITIONALLY on the SR face, leaning on C1 and C3 (and on C2 nowhere);
     OPEN at the completed object, which is what the leg actually demands.
     Span: member 07 :251-253; AC6's gap at member 09 :209-221.
```

### 2.7 Tally

```text
LEGS = 0/6-CLOSED ; 2/6-CLOSED-CONDITIONALLY (L4 at 2 of 6 items, L6 on the SR face only) ; 4/6-OPEN
CONDITION ATTRIBUTION:  C1 -> L4, L6.   C3 -> L4, L6.   C2 -> none of the six.
```

**C2 is attributed to no leg, and that is worth stating rather than leaving as a silent zero.** The SR
representation condition does not gate any of the six; it gates what a *later* construction could do
with them. A condition that binds nothing at this receiver has not been discharged — it has simply
not been reached.

---

## 3. K4's `omega_hist` — **NOT MET**, shown at bytes

K4 requires an actual `omega_hist in State(C(Y))`.

**The receiver's own words**, member 08 `[7882,8932)`:

> These are genuine states. Neither is a state on `B_N = C(Y_N)` … The closed sources also provide no
> star-homomorphism between either record system and `C(Y)`. Consequently the restriction operation
> needed here is not typed: `omega_record | B_N = UNDEFINED`.

**The release condition**, member 09 `:365-377`: build an `SM-1..SM-8`-conformant `d_joint`/
`d_state_Omega`, compute `omega_hist = Omega_C0 compose i_B`, and *only then* run the comparison.

The delivery (member 03 §2) stopped before producing any element of `State(C(Y))`, and both routes
remain as the receiver left them: `res_B`'s input uninstantiated, and `State(B) <-> Prob(Y)` uncut —
member 10 `:480-487` still returns *continuum* on every row.

```text
K4 = NOT-MET.  Spans: member 08 [7882,8932) ; member 09 :365-377 ; member 10 :480-487.
```

---

## 4. B03's CLOSURE STATUS

Member 01 conditions B03's closure on **all six legs**. Four are open; the two that move are
conditional and partial; K4 is not met.

```text
B03 = OPEN

REMAINDER
  L1, L2, L3   open at the completed object      — one missing object (§5)
  L6           open at the completed object      — same missing object
  K4           not met                            — same missing object
  L5           open, and SEPARATE                 — a d_U2 witness whose domain names C0
  L4           conditional and 2/6 partial        — remainder listed at §2.4
```

**The expected outcome was `CLOSED-CONDITIONALLY`.** It is not true, and the honest display is the
one above. What the entry bought is real and narrow: L4's evolution and contact rules arrived, and
L6's SR-face certificate filled five slots. Neither reaches the completed object, which is what four
legs and K4 demand.

---

## 5. THE ARITHMETIC OF THE REMAINDER

```text
L1, L2, L3, L6, K4   ->  ALL FIVE REDUCE TO ONE OBJECT:
                          a MEASURE on Y — omega_phys — with its descent map and provenance
                          certificate.  Not an arrow: member 02's entry supplied the arrow.
L5                   ->  SEPARATE: the d_U2 descent witness (member 06 [6266,6883)).
L4's remainder       ->  four U2 items, which are the same measure (pre-state) plus effects,
                          inclusive identity and domains.
```

**So the section's remainder is two objects, not six legs** — one measure and one witness — and the
measure accounts for everything except L5.

**And the measure is not merely unbuilt; the record says it is undetermined.** Member 10 `:480-487`
runs every executable constraint — raw positivity, inclusive normalization, U1 reality, the one-cell
restriction, weak `rho_pre` type-only existence — and each returns *continuum*. That is the record
stating that nothing it contains selects the measure, which is a different fact from the measure being
hard to construct, and it is the fact that governs what a next relay can honestly attempt.

---

## 6. WHAT THE SECTION STILL REQUIRES — THE B04–B15 SWEEP IS **NOT RUN**

Member 01 conditions the sweep explicitly: *"if B03 closes conditionally, state what the section still
requires before the section-close certificate (B04–B15 status sweep …)."*

**`B03 = OPEN`. The antecedent fails, and the sweep is not performed.**

I record the reason rather than the omission. The assignment also constrains the sweep to *"each basis
row's current standing from the sealed record, one line each, no construction"* — so running it here
would mean either reading fifteen basis rows this relay's closure does not declare, or writing fifteen
lines from memory. **The first exceeds the declared read set; the second is invention wearing a
template.** Neither is available, and a conditional obligation whose condition failed is discharged by
saying so.

---

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN (unchanged)   JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 02's entry and its three carried conditions; member 03's delivery result and its stop;
  member 04's entered map and domain; members 06-10's spans in their declared roles; member 11's leg
  restatement as a subject; all charter fences.

DERIVED HERE:
  the item-by-item scoring of L4 against U2's six-item content at the re-pinned span;
  the attribution of C1/C3 to L4 and L6 and of C2 to NO leg;
  the observation that L1, L2, L3, L6 and K4 reduce to ONE object and L5 to a second;
  the recomputation of member 06's span digest e6093b0f….

SELECTED HERE:
  NOTHING.  No measure, no witness, no state, no member of the origin family, no domain proof.
  NO CONSTRUCTION WAS PERFORMED — this relay dispositions.  NOTHING ADOPTED, ENTERED OR FROZEN.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

## 8. FLATTENING CHECK — S01–S37 walked

- A **partial leg** was not flattened into a **closed** one: L4 is scored 2 of 6 items (§2.4).
- **Supplying evolution in form** was not flattened into **supplying it in physics**: C1 is named on
  the very items the entry delivers.
- An **SR-face certificate** was not flattened into a **completed-object one** (L6).
- A **carried condition** was not flattened into a **discharged** one (member 02 `:40`).
- An **unattributed condition** was not flattened into a **satisfied** one: C2 gates no leg, and §2.7
  says so rather than recording a silent zero.
- **Five legs reducing to one object** was not flattened into **one leg**: the demand lists six and
  the tally reports six.
- An **expected outcome** was not flattened into a **displayed** one (§4).
- A **failed antecedent** was not flattened into a **skipped obligation** (§6).
- A **subject** was not flattened into a **witness**: members 03 and 11 ground nothing (§1.1).

## 9. AUDITS AND CUSTODY

```text
GUARDS            = BOTH PASSED (delivery sidecar verified; addendum LIVE) — the earlier
                    1066_BLOCKED is RELEASED and stands unedited
PROSE_DIGESTS     = 17/17, STRICT==STABLE (0 live members)
RE-PINNED SPAN    = member 06 [6266,6883) -> e6093b0f… recomputed in this relay
RESIDUE           = 0 hits (27-token scan over authored prose)
CLOSURE           = declared-first, byte 0, fixed point
OUTPUT_INSPECTION = NONE-CERTIFIED — every disposition is anchored upstream; the battery,
                    kappa_record and alpha remain pointers only and were not opened
VERB_AUDIT_SELF   = CLEAN
```

- **BUILDER NEVER VERIFIES OWN WORK.** CLAIMED until the Codex 2 check.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- PE-1..17 pointer-only; no member bound; no freeze; nothing constructed.

---

## 10. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00005538 computed as a fixed point on bytes; 17 members;
     0 live; residue scan 0 hits from its own run)

LEGS = 0/6-CLOSED ; 2/6-CLOSED-CONDITIONALLY ; 4/6-OPEN
  L1  OPEN                     member 07 :251-253 vs member 09 :126-128 — "on the completed object"
  L2  OPEN                     same object as L1; closes exactly when L1 does
  L3  OPEN                     member 07 :233-263 (P1) vs member 09 :126-128
  L4  CLOSED-CONDITIONALLY     2 of U2's 6 items supplied (evolution; contact rules), leaning on
                               C1 and C3; remainder — pre-state, inclusive identity, record effects,
                               common domains — at member 06 [6266,6883)
  L5  OPEN                     member 06 [6266,6883), e6093b0f… — d_U2 names C0 in its own domain.
                               THE ONE LEG THE ENTRY NEVER TOUCHED and the one that does not reduce
                               to the missing measure.
  L6  CLOSED-CONDITIONALLY on the SR face (5/6 certificate slots), leaning on C1 and C3;
                               OPEN at the completed object, which is what the leg demands
  CONDITION ATTRIBUTION: C1 -> L4, L6 ; C3 -> L4, L6 ; C2 -> NO LEG (stated, not left as a silent zero)

K4 = NOT-MET.  member 08 [7882,8932) "omega_record | B_N = UNDEFINED"; release at member 09 :365-377;
     member 10 :480-487 still continuum on every row.

B03 = OPEN.  The assignment named CLOSED-CONDITIONALLY as the expected honest outcome; it is not true
     and the displayed status is OPEN.
     REMAINDER: L1, L2, L3, L6 open at the completed object; L5 open and separate; L4 conditional and
     2/6 partial; K4 not met.
     THE ARITHMETIC: five of the six openings (L1, L2, L3, L6, K4) reduce to ONE object — a MEASURE on
     Y, omega_phys, with its descent map and provenance certificate.  L5 reduces to a SECOND — the
     d_U2 witness.  THE SECTION'S REMAINDER IS TWO OBJECTS, NOT SIX LEGS.
     And member 10 :480-487 says every executable constraint on the measure passes for all — the
     record stating the measure is UNDETERMINED, not merely unbuilt.

BASIS_SWEEP = NOT-RUN.  Member 01 conditions it on B03 closing conditionally; B03 is OPEN, so the
     antecedent fails.  Running it would require either reading fifteen basis rows outside this
     relay's declared closure or writing them from memory — the first exceeds the read set, the
     second is invention wearing a template.  Stated with its reason.

OUTPUT_INSPECTION = NONE-CERTIFIED
PROSE_DIGESTS = 17/17, STRICT==STABLE
CHAIN_INVOKED = true (chained pickup proceeds to RELAY_PASTE_1074_DIAGB_CHECK_V002_DARIO_V001.md,
     the next DARIO paste; 1072 and 1073 are CODEX 2 assignments this lane may not obey)
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

All findings **CLAIMED** until the opposite-lane check.

**What this disposition is worth.** Not the tally — the arithmetic. Before it, the section's remainder
read as six legs and a K-row, which invites the impression that six different things are missing and
that grinding at each might close them one by one. **It is two objects.** One of them, the measure, the
record explicitly declines to determine; the other, the `d_U2` witness, has a stated domain that names
`C0` and has never been in the Attach map's reach at all. **No further relay of mine closes either by
construction**, and saying that plainly is more useful than a conditional closure that would have to be
withdrawn at the next check.
