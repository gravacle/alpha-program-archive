# STAGE 8 — CANC-PANEL-CLASSIFICATION: BLIND CLASSIFICATION AUDIT OF THE CANCELLATION BUILD'S NET (B4) — WARRANTED BY THE SEALED CORPUS, OR AN ARTIFACT OF THE REGISTRAR'S BRIEF?

## BLIND CROSS-LINEAGE VERIFIER — ONE LENS OF THREE — DEFAULT REFUTE — [SEALED]

Date: 2026-08-13
Role: BLIND adversarial verifier, cross-lineage to the builder. Lens:
CLASSIFICATION AUDIT (codename CANC-PANEL-CLASSIFICATION). Target: B4 of
STAGE8_REQUIRE_BUILD_CANCELLATION_V001.md — the filing of the OPEN
composition question (is the surviving degree -2 residual rendered HS-finite
by the operator composition C(V(a)-V(0))C / the commutator route) under
NET = DOES_NOT_FULLY_CANCEL (alpha runs). The registrar's instruction to the
builder ("there is no third 'needs a scale' road: needing a scale to see the
residual just means the residual survives — the RUNS horn") is ITSELF under
audit and carries NO authority here. Blind to co-panelists: no
STAGE8_CANC_PANEL_* artifact opened (none exists at path at write time —
probed by glob, not opened).

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held: connection-only, symbolic. NO value of n, kappa_record, alpha,
any exponent, coupling, norm, scale, length, or spectrum computed, bounded,
estimated, evaluated, or compared — symbolic order/parity/degree bookkeeping
only (algebra, not evaluation). NO scale, imported GR, or faithfulness
premise used as authority BY ME; scale-bearing sealed text (ECO/RFA sea
covariance, diamonds, |D|_4) read SUSPECT-ONLY to classify and attack, never
as a positive certifier. No register/tracker/plan/road/ledger/lens read.
Every seal recomputed at path (shasum -a 256) BEFORE reliance. No git action.

---

## 0. VERDICT IN ONE LINE

**The SYMMETRY binary is SOUND (decided NO, on connection-only bedrock, and
at the kernel level the build in fact proves MORE than "no symmetry": bounded-
kernel full cancellation is positively EXCLUDED). But B4's "(alpha runs)"
attachment is OVER-CLAIMED: the sealed corpus stamps the deciding cancellation
UNKNOWN/OPEN in three independent sealed places, RFA's own kernel-to-alpha
dichotomy is self-disclaimed power counting whose necessity direction was
never derived, and the ONLY authority the build cites for filing the open
composition question under RUNS is the registrar's brief — quoted three times,
with no sealed span behind it, because none exists. The composition's HS-fate
is OPEN_UNBUILT: not structurally decided (an unattempted connection-only
commutator exhibition exists but does not close it), not established as
genuinely metric (ROUTE_NOT_LEMMA), and the corpus's only datum about composed
objects — "commutators with C ARE Hilbert-Schmidt in this program's own worked
cases" — leans toward the CLOSES horn, against the filing. The honest NET is:
kernel-level full cancellation EXCLUDED; RUNS-vs-CLOSES UNDETERMINED of record
pending the unbuilt composition estimate. B4 = REFUTED as stated; the
classification was done by the tasking, not the corpus.**

---

## 1. SEALS RECOMPUTED AT PATH (shasum -a 256), BEFORE READING

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. CONN =
connection-only; SCALE = scale-bearing (read SUSPECT-ONLY, to classify and
attack).

```text
PRIMARY (build under test):
  STAGE8_REQUIRE_BUILD_CANCELLATION_V001.md
    4971e2739666bd95917163763f42cadf427442a7c710f8b22353d65ead9fe5fe  MATCHES-TASKED

TASKED SECONDARY:
  STAGE8_REQUIRE_BUILD_CLUSTER_SUMMABILITY_V001.md ("the wall")
    5cdd5dafccd1dfd5075426cce384cb84136a21df1cecad56576da3799cea9455  MATCHES-TASKED  CONN
  STAGE8_REQUIRE_CLUSTER_CHECK_V001.md ("the check", SEALED verifier artifact)
    a7f75d0f2c4ed9604be78a9024461d93ff097c77fbf9b46e0207c4e7eea93c13  MATCHES-TASKED  CONN
  STAGE8_R_RECORD_L_FORM_FABLE_V001.md ("FORM")
    5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  MATCHES-TASKED  CONN

FROM THE BUILD'S REFERENCES (recomputed at path; sidecars read and matched):
  STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md ("ECO")
    0f3082cab910f2eb6769698fc03cdb0201830c2551ecd8201fa6748b24e07505  MATCHES build §1 + sidecar  SCALE
  STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md ("RFA")
    2ede02aea415157ada9edd6f685aabcc824acf2716777f4aa2dc98467fe92840  MATCHES build §1 + sidecar  SCALE
```

Six seals recomputed; all match. Cited spans opened and compared at the bytes:
the build's quotations of ECO :151-155, :162-165, :168-171, :174-179,
:189-201; RFA :66, :119-131, :137-140; FORM B5 :96-105, B6 :107-112,
D2 :159-176, D3 :180-199, :497-510 (R(n) = R(-n) at :505) — ALL faithful.
No misquotation found anywhere in the build. No register/tracker/plan/road/
ledger/lens file read.

---

## 2. ATTACK A — THE SYMMETRY BINARY (tasking Q3a): IS "DECIDED NO" SOUND?  →  YES, and it is STRONGER than stated

Attacked at all four mechanisms plus exhaustiveness:

```text
(A) phase/gauge structure. FORM B5 :96-105 ("indexed by CONNECTION/HOLONOMY
    HISTORIES ALONE"; the law's ONLY a-dependence is the per-cell character
    chi_n(h_j[a_j])) + D2 :168 (d/ds z|0 = i n <ell_j,a> z ; chi_n(identity)=1)
    + D3 :198-199 (abelian relative holonomy on the doubled tangents) force the
    difference kernel to vanish at the diagonal to EXACTLY first order with
    leading coefficient the write current n<ell,r>. Full (bounded-kernel)
    cancellation would need third-order vanishing, i.e. a locally constant
    phase, i.e. a TRIVIAL write — contradicting B6 :110-112 (the trivial
    character n=0 "has zero variation ... and fails the surface's own
    charge/flux-access requirement", eliminated). CONFIRMED at the bytes.
    ONE PRESENTATIONAL LOOSENESS, non-load-bearing: build §3.1 writes the
    dressing as an exact point-local conjugation V(a)(x,y) =
    exp(i[Theta_a(x)-Theta_a(y)]) V(0)(x,y), which presumes the abelian phase
    is representable as a point function (an exactness presumption B5 does not
    literally seal — for non-exact a the dressing is a path/Wilson-line
    phase). BUT the load-bearing claim survives in the weaker form: ANY
    abelian unimodular line/relative-holonomy phase between coincident points
    is trivial and expands as 1 + i n <ell,r> + O(r^2) — first-order vanishing
    with the write-current coefficient holds regardless of exactness. The
    degenerate direction (<ell_j,a> = 0 for all j) makes the difference
    IDENTICALLY zero, so on every direction where the difference is nonzero
    the vanishing is exactly first order. SOUND.
(B) current conservation d ell = 0: divergence-free is not zero; does not
    raise the diagonal vanishing order. SOUND (elementary; no sealed text
    contradicts it).
(C) charge conjugation n -> -n: V(0) is n-blind — verified at FORM D2 :168
    and :497-505 ("the base generator, write operators, base state, kernel C,
    and chains Phi all carry no n because every character is trivial at the
    identity"); R(n) = R(-n) (FORM :505) is verified to be a property of the
    FULL quadratic response (the global n^2 prefactor), exactly as the build
    says — NOT a cancellation of the fixed-n middle kernel. A +/-n average is
    unavailable at fixed winding. SOUND.
(D) oddness of C_off: C_off(r) = -i alpha r/(2 pi^2 |r|^4) is odd and
    imaginary (ECO :153, verified). Parity cancels in the operator/L^2 sense —
    exactly the sealed CZ classification "L^2-BOUNDED BUT NOT HILBERT-SCHMIDT"
    (ECO :168-171, verified) — and the HS integrand |C_off|^2 is even and
    positive, so parity contributes NOTHING to the HS norm (RFA :66-67
    definition, verified). SOUND. Elementary parity, no evaluation.
EXHAUSTIVENESS: the candidate family (Ward/gauge, conservation, conjugation,
    antisymmetry) covers the sealed corpus's symmetry inventory; branch
    exchange a_+ <-> a_- on the doubled contour maps a_Delta -> -a_Delta and
    reduces to conjugation-type (C). No fifth candidate is of record.
```

**SYMMETRY_BINARY_SOUND = YES.** And note it is STRONGER than "no symmetry
found": mechanism (A) positively EXCLUDES bounded-kernel full cancellation
(the survivor at degree -2 is FORCED by the first-order phase structure at
every non-degenerate direction). So RFA :119-121's campaign target at the
KERNEL level — "does V(a)-V(0) have a bounded kernel on a diamond" — is newly
decided NO by this build. That half of B4's headline (DOES_NOT_FULLY_CANCEL,
read at the kernel level) is WARRANTED. B3 (degree -2 survivor, still
HS-singular at the kernel level: |r|^-2 squared = |r|^-4, non-summable in 3d
by symbolic degree bookkeeping) is likewise sound FOR THE MIDDLE OBJECT.

---

## 3. ATTACK B — THE COMPOSITION'S FATE (tasking Q2): STRUCTURALLY DECIDABLE, GENUINELY METRIC, OR OPEN?  →  OPEN_UNBUILT

The key sub-question: did the build misclassify a possibly-connection-only
STRUCTURAL estimate as "a metric computation"? Three structural moves the
build never attempted, each tested:

```text
(i) THE COMMUTATOR EXHIBITION — available connection-only, UNATTEMPTED, and
    it does NOT close. On the build's OWN §3.1 multiplicative form the middle
    difference is EXACTLY a commutator: with U_a the unimodular multiplication
    by exp(i Theta_a),
        V(a) - V(0) = U_a V(0) U_a* - V(0) = U_a [ V(0), U_a* ].
    This is pure algebra (connection-only bedrock: the unimodular character
    dressing) and it PARTIALLY discharges RFA :132-136's named next step
    ("exhibit C(V(a)-V(0))C in commutator form") — the build did not notice
    it. BUT: (a) it is a commutator with the PHASE unitary, not with C — the
    corpus's worked identity ||[C,P]||_2^2 = 2 sum_i sigma_i(1-sigma_i)
    (ECO :192) is for commutators WITH C, and no sealed identity reorganizes
    C·U_a[V(0),U_a*]·C into [C, ·] form; (b) symbolic degree bookkeeping on
    the commutator's kernel, V(0)(x,y)(e^{-iTheta(y)} - e^{-iTheta(x)}),
    reproduces the SAME degree -2 survivor, whose HS integrand |r|^-4 is
    still non-summable in 3d — so the middle object remains HS-infinite in
    commutator form, and whatever renders R-L2b's object finite must come
    from the OUTER C-sandwich. The exhibition reorganizes; it does not decide.
    (On the weaker Wilson-line form of §2(A) the same first-order structure
    holds and the exhibition question stays open — either way, undecided.)
(ii) SPECTRAL FLATTENING sigma(1-sigma) <= 1/4 — structural, and POWERLESS
    for HS, by the build's own (D) lesson one level up. The flattening bounds
    each SUMMAND of the corpus's identity (an operator-norm-type statement);
    HS-finiteness of the composed object is the SUMMABILITY of
    sigma_i(1-sigma_i) — a spectral-DENSITY fact (how much spectrum sits away
    from {0,1}) that no parity, algebra, or per-term bound decides. A
    structural bound in the wrong norm is exactly what the build itself
    correctly rejected in mechanism (D). Does not decide, in either direction.
(iii) THE SANDWICH'S PROJECTOR STRUCTURE — C has the projector-like form
    (1/2)I + (odd CZ part) (ECO :153, suspect-only), but no sealed relation
    between V(0) and C licenses a reorganization of the sandwich, and
    ||C X C||_2 <= ||C||_op^2 ||X||_2 is one-sided in the USELESS direction
    (an infinite middle bound forces nothing about the sandwich). Does not
    decide.
```

**COMPOSITION_FATE = OPEN_UNBUILT.** Not STRUCTURALLY_DECIDABLE: every
structural move visible from the sealed corpus (including one the build
missed) converts or bounds but cannot decide the composed object's HS
summability in either direction. Not established GENUINELY_METRIC either:
the corpus stamps the route ROUTE_NOT_LEMMA — "no bound is claimed, no
exponent is derived, and R-L2b remains OPEN" (ECO :198-201, verified) — and
an unattempted structural exhibition (i) exists, so "a metric computation" is
the build's assertion, not a sealed fact. DIRECTIONAL NOTE, suspect-only,
weighed as classification not certification: the corpus's ONLY datum about
composed objects of this type — "COMMUTATORS WITH C ARE HILBERT-SCHMIDT IN
THIS PROGRAM'S OWN WORKED CASES, while products of restrictions are not"
(ECO :193-196) and "the finite object is the natural one" (ECO :196-197) —
leans toward the CLOSES horn if the exhibition succeeds. The corpus's lean,
such as it is, points AGAINST the RUNS filing, not for it.

---

## 4. ATTACK C — THE NET CLASSIFICATION (tasking Q1, Q3b): DOES "alpha RUNS" FOLLOW FROM THE SEALED CORPUS?  →  NO. OVER-CLAIMED.

### 4.1 What the sealed corpus actually supports

The chain the build needs: kernel-level partial cancellation -> alpha < 1 ->
aggregate divergence -> runs. Its sole sealed carrier is RFA's dichotomy
(:122-128), whose alpha assignments rest on RFA §3-§4. Attacked at the bytes:

```text
1. RFA's kernel-to-alpha equivalence is SELF-DISCLAIMED. RFA :86-88, verbatim:
   "§4 is POWER COUNTING, NOT A DERIVATION, and it does not discharge R-L2b"
   — with the A-L4 strength mechanism "REUSE OUTSIDE ITS SEALED SCOPE"
   disclosed (:82-88). And RFA :137-140, verbatim: "STILL OPEN, STATED
   PLAINLY: no bound is claimed, alpha is not derived, and whether the
   singularity fully cancels is UNKNOWN. §3-§4 are power counting and
   aggregation arithmetic; they ... do NOT say what alpha is."
2. THE NECESSITY DIRECTION WAS NEVER DERIVED — the converse-of-a-one-sided-
   bound fallacy. RFA §4's argument runs: IF the difference kernel is bounded
   by M, THEN ||C(V(a)-V(0))C||_2 <~ M|D|_4 (alpha = 1 sufficient). An
   UNBOUNDED middle kernel makes that upper bound vacuous and forces NOTHING
   about the sandwich: the sandwich can be HS-finite with volume scaling while
   the middle object alone is not — which is precisely the commutator route's
   entire point (ECO :193-197). No sealed span derives "unbounded middle
   kernel -> alpha < 1 for the SANDWICH." The build's B4 uses the disclaimed
   IFF in exactly its underived direction.
3. THE CORPUS KEEPS A THIRD STATE, IN THREE SEALED PLACES. RFA :137-138
   ("whether the singularity fully cancels is UNKNOWN"); ECO :200-201
   ("COMMUTATOR ROUTE IS A ROUTE, NOT A LEMMA ... R-L2b remains OPEN"); and
   the sealed check's Joint 3 correction, verbatim (check :171-172): "The
   correct reading: cancellation UNKNOWN; IF full -> alpha=1 -> closes; IF
   partial -> alpha<1 -> running-kappa/scale-entangled." OPEN/UNKNOWN is the
   corpus's OWN third road, stamped as such by its own verifier layer.
4. ECO :176-179 ("alpha IS SET BY THE CANCELLATION RATE") types alpha to the
   cancellation rate OF R-L2b's OBJECT — the sandwich ||C(V(a)-V(0))C||_2
   (ECO :174) — which INCLUDES whatever the composition contributes. If the
   kernel-level binary alone settled alpha, RFA §6's named next step ("exhibit
   C(V(a)-V(0))C in commutator form and READ THE CANCELLATION OFF IT",
   :132-136) would have nothing left to read off.
5. PRECEDENT: this exact doom-typing pattern was ALREADY corrected once, one
   level down, in a SEALED artifact. The wall (§3.4 pt 2 :275-276, flag
   :444-446) routed even the FULL-cancellation horn into the marginal/
   running-kappa branch; the check's Joint 3 (:159-179) corrected it as an
   OVER-STATEMENT contradicting RFA :122-125 — full cancellation is a "CLEAN
   SUCCESS that CLOSES", and the horn is UNKNOWN. B4 repeats the same pattern
   one level up: routing the open COMPOSITION question into RUNS.
```

Nuance kept precise: the build DID newly move the corpus's state. The
kernel-level half of RFA's campaign target is now decided (bounded kernel:
NO — §2 above, sound). What the corpus does NOT license is the second hop:
kernel-level partial -> the sandwich's alpha < 1 -> runs. That hop is carried
only by the disclaimed power counting read backwards (item 2) — or by the
tasking's mapping (§5).

### 4.2 So does (b) follow from (a) alone?

NO. The NET "alpha runs" needs the unresolved composition fate. The build's
own §3.4 CONCEDES this — "What remains a COMPUTATION ... is whether the
surviving degree -2 residual is nonetheless rendered HS-finite by the OPERATOR
COMPOSITION in the sandwich" — and its own B4 caveat flags it. The honest NET
per the sealed corpus is: **SYMMETRY-NO; kernel-level full cancellation
EXCLUDED; the HS-fate of the composed object C(V(a)-V(0))C — and with it
RUNS-vs-CLOSES — UNDETERMINED of record pending the unbuilt composition/
commutator estimate.** NET_CLASSIFICATION = OVER-CLAIMED.

---

## 5. ATTACK D — WHO DID THE CLASSIFYING? (the tasking-injection question)  →  THE TASKING DID

At the bytes of the build, the RUNS filing of the open composition question is
attributed to the tasking THREE times, and to a sealed span ZERO times:

```text
build §3.4 :237-242  "Per the tasking's own mapping — 'needing a scale/
    computation to see the residual just means the residual survives (the
    RUNS horn); there is no third road' — this computation-openness is the
    RUNS horn, not a fence-undetermined ..."
build FLAG NET :309-314  "... per the tasking's mapping ('needing a
    computation/scale to see the residual = the residual survives = RUNS
    horn; no third road') this is the RUNS horn, NOT a fence-undetermined ..."
build FLAG FENCE_PRESSURE :319-323  "... per the tasking that computation-
    openness is the RUNS horn ..."
```

No sealed span is cited for the mapping because none exists: the sealed spans
on point say the opposite (UNKNOWN — RFA :137-138; OPEN/ROUTE_NOT_LEMMA — ECO
:198-201; "cancellation UNKNOWN; IF full -> closes; IF partial -> runs" — the
check :171-179). The instruction is also epistemically invalid on its face:
"needing a computation to SEE whether the residual survives" is a statement
about our knowledge; "the residual survives" is a statement about the object.
The corpus separates the two with care ("They do not say what alpha is",
RFA :139-140); the brief collapses them by fiat. The builder demonstrably SAW
the gap (the caveat is honestly flagged in B4 itself) and deferred to the
brief for the horn-assignment. That is classification by instruction, not by
corpus. **TASKING_INJECTION = YES**; span: build :237-242, :309-314, :319-323.

Scope note, for fairness: the injection contaminates ONLY the "(alpha runs)"
attachment and the horn-filing of the open composition question. B1, B2, B3,
and the kernel-level half of B4's headline stand on sealed connection-only
bedrock independent of the brief (§2 above), and the build's honesty in
quoting its instruction verbatim is what made this audit decidable.

---

## 6. IMPORT / MACHINERY AUDIT (MINE)

```text
No value of n, kappa_record, alpha, any exponent, coupling, norm, length,
  scale, or spectrum computed, bounded, estimated, evaluated, or compared.
  The degrees used (-3 sealed; +1 phase-gradient; -2 product; |r|^-4 HS
  integrand non-summable in 3d) are symbolic orders of homogeneous factors —
  the same bookkeeping class the build used; sigma(1-sigma) <= 1/4 is
  elementary algebra cited only to show it CANNOT decide summability.
No scale/GR/faithfulness used as authority by me. ECO/RFA (SCALE) read
  SUSPECT-ONLY to classify the build's classification and to locate the
  disclaimers at the bytes; the check's Joint 3 and RFA's own disclosure
  lines are quoted as the corpus's self-description, never as value
  authorities. Every positive step of my refutation rests on connection-only
  bedrock (FORM B5/B6/D2/D3 character structure; the U_a[V(0),U_a*] algebra;
  parity; degree bookkeeping) plus the sealed texts' own OPEN/UNKNOWN stamps.
No register/tracker/plan/road/ledger/lens read. No STAGE8_CANC_PANEL_*
  artifact opened (existence probed by name only). Six seals recomputed at
  path before reading. No git action; output name probed before write: ABSENT.
```

---

## 7. FLAG BLOCK

```text
SYMMETRY_BINARY_SOUND = YES(all four mechanisms verified at the bytes on connection-only
  bedrock — FORM B5 :96-105 / B6 :107-112 / D2 :168 / D3 :198-199 for the first-order
  phase structure and the nonzero write at n != 0; FORM :505 for R(n)=R(-n) as a full-
  response property; ECO :153, :168-171 + RFA :66 for oddness being operator-norm-only
  while |C_off|^2 is even. Candidate list exhaustive of record (branch exchange reduces to
  conjugation). One non-load-bearing presentational looseness: build §3.1's point-local
  exact dressing presumes exactness B5 does not seal, but the load-bearing first-order
  vanishing with write-current coefficient survives in the Wilson-line/relative-holonomy
  form. The result is in fact STRONGER than stated: bounded-kernel full cancellation is
  positively EXCLUDED, so the KERNEL-LEVEL half of RFA :119-121's target is newly decided
  NO.)

COMPOSITION_FATE = OPEN_UNBUILT(not structurally decidable by any move visible from the
  sealed corpus, including one the build never attempted: on the build's own §3.1 form the
  middle difference is EXACTLY U_a[V(0),U_a*] — a connection-only commutator exhibition
  partially discharging RFA :136's named next step — but it is a commutator with the phase
  unitary, not with C; its kernel reproduces the same degree -2 survivor whose HS integrand
  |r|^-4 stays non-summable in 3d by symbolic degree bookkeeping, so the fate rests entirely
  on the OUTER C-sandwich's spectral density. Spectral flattening sigma(1-sigma) <= 1/4
  bounds each summand of ECO :192's identity — a wrong-norm structural bound, powerless for
  the HS SUM, the build's own mechanism-(D) lesson one level up. No sealed identity
  reorganizes the sandwich into [C, ·] form. Not GENUINELY_METRIC either: ROUTE_NOT_LEMMA,
  "no bound is claimed ... R-L2b remains OPEN" (ECO :198-201), and the unattempted
  exhibition means "a metric computation" is the build's assertion, not a sealed fact.
  Directional lean of the corpus's only composed-object datum (ECO :193-197, worked
  commutators with C ARE HS; "the finite object is the natural one"): toward CLOSES —
  against the RUNS filing.)

NET_CLASSIFICATION = OVER-CLAIMED(honest NET = SYMMETRY-NO; kernel-level full cancellation
  EXCLUDED; RUNS-vs-CLOSES UNDETERMINED pending the unbuilt composition estimate. The
  corpus does not carry the second hop kernel-partial -> alpha<1 for the SANDWICH: RFA's
  kernel-to-alpha dichotomy rests on §3-§4, self-disclaimed as "POWER COUNTING, NOT A
  DERIVATION ... does not discharge R-L2b" (RFA :86-88) and "They do not say what alpha is"
  (RFA :139-140); the necessity direction (unbounded middle kernel -> alpha<1) is the
  underived converse of a one-sided bound — an infinite middle bound forces nothing about
  the sandwich, which is the commutator route's entire point (ECO :193-197). The corpus
  keeps the third state in three sealed places: UNKNOWN (RFA :137-138), OPEN/ROUTE_NOT_LEMMA
  (ECO :198-201), and the check's Joint 3 CORRECTION ("cancellation UNKNOWN; IF full ->
  alpha=1 -> closes; IF partial -> alpha<1 -> running-kappa", check :171-179) — the sealed
  precedent that corrected exactly this doom-typing one level down. alpha IS SET BY THE
  CANCELLATION RATE of the SANDWICH (ECO :174-179), which includes the composition.)

TASKING_INJECTION = YES(the instruction did the classifying. Span: build §3.4 :237-242,
  flag NET :309-314, flag FENCE_PRESSURE :319-323 — the RUNS filing of the open composition
  question is attributed to "the tasking's mapping" three times and to a sealed span zero
  times; the sealed spans on point say UNKNOWN/OPEN. The brief's mapping collapses an
  epistemic state ("needing a computation to see") into an ontic fact ("the residual
  survives") — a collapse the corpus itself refuses (RFA :139-140) — and its "no third road"
  clause contradicts the corpus's own third state, stamped UNKNOWN by its own sealed
  verifier layer. The builder saw the gap (the B4 caveat is honestly flagged) and deferred
  to the brief for the horn-assignment. Contamination scope: ONLY the "(alpha runs)"
  attachment; B1/B2/B3 and the kernel-level half of B4 stand on sealed bedrock.)

CLASSIFICATION_VERDICT = B4_REFUTED(corrected NET = "DOES_NOT_FULLY_CANCEL AT THE KERNEL
  LEVEL (bounded-kernel full cancellation EXCLUDED by the first-order phase structure —
  sound, and stronger than the build's own 'no symmetry found'); the HS-fate of the composed
  object C(V(a)-V(0))C, and with it the RUNS-vs-CLOSES horn, UNDETERMINED of record pending
  the unbuilt composition/commutator estimate." B1 = CONFIRMED (with the non-load-bearing
  exactness-presentation note); B2 = CONFIRMED; B3 = CONFIRMED for the middle object; B4's
  binary halves separate exactly as the tasking suspected: the symmetry/kernel half is
  sound, the "(alpha runs)" half is an artifact of the registrar's brief.)

PROVENANCE = CLEAN(no scale, GR, or faithfulness entered my audit as authority — ECO/RFA
  read suspect-only, quoted for their own disclaimers and classifications; every positive
  refutation step rests on connection-only bedrock (FORM character structure, the
  U_a[V(0),U_a*] algebra, parity, symbolic degree bookkeeping) plus the sealed OPEN/UNKNOWN
  stamps. The build's OWN provenance re: scale is likewise clean on B1-B3; its one
  provenance defect is the B4 horn-assignment resting on the unsealed tasking — captured
  under TASKING_INJECTION. Six seals recomputed at path before reliance; no register/
  tracker/plan/road/ledger/lens read; no co-panelist artifact opened.)

MACHINERY_USED_BY_ME = no(nothing computed, bounded, estimated, evaluated, or compared;
  n, kappa_record, alpha, and every scale/norm/spectrum left symbolic; degrees, parity,
  and sigma(1-sigma) <= 1/4 used only as algebra to show what structure CANNOT decide;
  shasum -a 256 is hash-integrity, not physics.)

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
CANC_PANEL_CLASSIFICATION_RESULT = SEALED.
```
