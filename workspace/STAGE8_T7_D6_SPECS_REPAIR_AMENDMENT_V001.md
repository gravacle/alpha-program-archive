# Stage-8 T7 D6 Specs Repair Amendment V001

Date: 2026-07-26 (autonomous window)

## Status

```text
APPEND_ONLY_PRE_EXECUTION_AMENDMENT
```

Repairs the two sealed D6 specs and their interface note per the joint
hostile review (verdicts NOT_READY / NOT_READY / I3 MISMATCH; the review
return is recorded in the supervision transcript sealed alongside). The
sealed specs are preserved unexecuted; the review confirmed EVERY
displayed identity (FK-1/2/3, L-ADD, all six Route-1 anchors) by
independent rederivation — the repairs below touch only frozen criteria,
typing, and the interface; no sealed mathematics changes. Where this
amendment and the sealed specs differ, this amendment governs.

## Part I - Majorant spec repairs (STAGE8_T7_CONNECTED_LINKED_CLUSTER_MAJORANT_DERIVATION_SPEC_V001)

```text
M-1 (W1 comparator): the frozen W1 comparator becomes the certified
    spanning-tail bound |C|_4 * eta^2 / (1 - eta) evaluated at the
    frozen certified eta (W1 sums ALL spanning anchored clusters
    n >= 2). Inside sharpens; outside refutes — against THAT bound.
M-2 (pair polydisc / the I3 seam): per-cell histories are CTP PAIRS
    (a_{c,+}, a_{c,-}); the polydisc constraint is max(|a_{c,+}|,
    |a_{c,-}|) <= epsilon_star per cell; the bra branch is complexified
    by the adjoint-continued convention of the Duhamel spec's (H1)
    (Ktilde(w) = [K_pointer(conj w)]^dagger), making -Log Z_hat_comp
    jointly holomorphic; restriction to real pairs recovers the sealed
    D1 display. Clause (3)'s differentiated series and clause (4)(iii)
    are typed on this holomorphic object.
M-3 (states): the admitted states are the two Phase-A pinned finite
    schemes; every claim is per-state, reported separately, never
    promoted (Duhamel S0.6 discipline inherited verbatim).
M-4 (O2 quantifier): the re-aggregation identity is proved over the
    full common-refinement poset by induction on refinement steps, with
    the closure argument an explicit obligation (one step does not
    reach A-with-B common refinements).
M-5 (NC5 witness): the detuned-relay control's witness is EXACT:
    weight-sum restoration m0' = sum_lambda p_lambda = 1 (vs the
    completed chain's exact 0) AND/OR diagonal-unitality restoration
    R_erased(a,a) = I, each certified exactly; the imprecise "toward"
    clause is void.
M-6 (fences): L2 freeze fences 1-5 inherited (fence 4's L-ADD
    obligation is owned by the Duhamel spec, recorded here).
M-7 (constants tuple): tuples read |w_lambda|; the identity
    |w_lambda| = p_lambda on the sealed A3 chain is recorded.
M-8 (E1 witness): named witness ETA_MONOTONICITY_UNCERTIFIED added; the
    E1 step-2 grid max is well-posed regardless of monotonicity.
M-9 (W1 fixture pinned): Z_hat_comp^(12) = the TWO-CELL relayed member
    (cells 1 and 2 of the relayed exhaustion, relay-ordered), per
    pinned state, superscript = cell set; fallback history pair =
    (epsilon_star, -epsilon_star) on the closed polydisc; the frozen
    pair (7/100, -11/100) is used iff epsilon_star >= 11/100 under the
    M-2 pair-modulus reading.
M-10 (cross-reference): the E1 threshold's users are M3 (inside O4) and
    O5; O3 consumes epsilon_star only.
M-11 (disclaimer): the compliance citation of the amendment's A4
    relocation carries the Duhamel-S10 disclaimer verbatim (A4(3)
    erratum PROPOSED/unsealed; the freeze note governs).
```

## Part II - Duhamel spec repairs (STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_IDENTITY_AND_INTERCHANGE_SCHEMA_SPEC_V001)

```text
D-1 ((H1) restated): (H1) is PER-K ZERO-FREENESS of Ghat_K on the
    closed pair polydisc P at a common epsilon_star; any quantitative
    floor is K-DEPENDENT (e.g. >= exp(-N_4(K) Gamma_star)); the
    K-uniform delta is DELETED everywhere (it was never used by the
    schema proof, and it is undischargeable whenever the intensive
    attenuation Hessian is nonzero — the review's decisive finding).
    The S6.1 (H1)-checker tests zero-freeness; the GHZ witness still
    fails it (zeros pi/(2 N tau_R) enter any fixed P).
D-2 ((H2)(ii) restated): pointwise convergence on a PRODUCT set
    E_1 x E_2 with each factor having an accumulation point in the
    open disc (the real bidisc slice is the canonical choice); the M7
    formal-lane proof is the iterated one-variable Vitali argument
    plus Osgood. The sealed text's accumulation-point-per-variable
    clause is void (review counterexample: i(-1)^K (z - w) on the
    diagonal).
D-3 (N_4 defined): N_4(K) := |X(K)|_4, the cellulation 4-volume, with
    the unit-cell-skeleton equivalence to V011's 4-cell count recorded.
D-4 (F-A state pinned): the F-A conditioned quantities use the
    negative-energy Slater fill per the sealed finite-holonomy
    protocols; the defining diagnostic spec is added to the authority
    table: COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_DIAGNOSTIC_SPEC_
    V001.md = d1b5ab24ebf8c1bc9b7598449596a6431c2152cab7edcf4d0cefcf
    d64e3815a5. M3 and M5 run on F-A with that state and on F-C with
    the two Phase-A pinned states; M4 runs on F-B (stateless closed
    forms).
D-5 (S6.3(b) teeth): the wrong-ordering control requires the
    Im<X_0,eta> enclosure to EXCLUDE ZERO on the frozen pair, and the
    FK-2 cross-check mismatch rule is the same 3x-enclosure-radius
    criterion as variants (a) and (c).
D-6/D-9 (labels): the functional's ordering label is unified to the
    frozen display omega(V[A_-]^dagger Q V[A_+]); the S5.2 phrase is
    read as three separately normalized limits per the display.
D-7 (verb): "(H1) presupposes and extends B0" replaces "subsumes".
D-8: recorded as accepted (predeclared, errs toward block).
```

## Part III - I3 interface tuple V002 (supersedes the note's tuple)

```text
I3 = { epsilon_star;
       per-K zero-freeness of Ghat_K on the closed pair polydisc P
         (common epsilon_star; K-dependent floors only);
       Gamma_star = eta(epsilon_star) / (1 - eta(epsilon_star));
       eta(epsilon_star) with cluster sums bounded by eta^n;
       per-cell activities in ACTION-DENSITY form;
       N_4(K) := |X(K)|_4 (both specs);
       LEMMA 0 as the majorant spec's first numbered obligation }.
```

The supplier (majorant spec, as repaired by Part I) now supplies exactly
what the consumer (Duhamel spec, as repaired by Part II) consumes; the
diagonal typing delta of the original note stands unchanged and both
specs continue to honor it.

## Protected status

```text
connected_linked_cluster_density_proved = false
Duhamel_intensive_Hessian_equality_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
