# STAGE 8 — THE JOIN (O42SR V001): DEFAULT-REFUTE ADVERSARIAL AUDIT — V001

## AUDIT LANE — 2026-08-16 — DEFAULT VERDICT REFUTED, ADVERSARIAL BY MANDATE

**TARGET.** `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_THE_JOIN_O42SR_V001.md`
sha256 `39bc4adff5236c896903bca74aec08fce621506ef8bc0f196a85c104474241c4` (recomputed
here; both target sidecars carry that digest and verify OK).

Every load-bearing claim below was re-derived at bytes from the primary files, not
from the build report. Where the build's bytes are right I say so and say why; where
they are not I name the deciding file:line.

Fences declared and honoured: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No numeric value of any coupling, scale, root,
eigenvalue, norm, or constant is computed anywhere in this audit, and no magnitude is
approached. Fence tokens appear below only inside quotation or inside a status
declaration.

---

## §0 — SEALS, INDEPENDENTLY RE-VERIFIED

`shasum -a 256 -c` run from each artifact's own directory, before any reliance.

```text
ROOT-1 GROUND (workspace)                                          16/16 OK
  BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md                OK
  SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md                   OK
  PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md               OK
  PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md                 OK
  CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md                          OK
  STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md         OK
  STAGE8_C1_U_OMEGA_TYPED_CODEX2_V001.md                              OK
  STAGE8_C1_COMMON_CELL_POSED_CODEX2_V001.md                          OK
  STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md                   OK
  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md            OK
  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md                         OK
  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md               OK
  STAGE8_FORCING_NOTION_O12SR_V001.md                                 OK
  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                           OK
  STAGE8_CERTIFICATION_RULES_O8SR_V001.md                             OK
  STAGE8_INGREDIENT_CENSUS_O17SR_V001.md                              OK

ROOT-1 CROSS-CHECK / PRIOR-COMMISSION CARRIERS                       6/6 OK
  STAGE8_AXN_BUILD_DESCENDANT_CALCULUS_DARIO_V001.md                  OK
  STAGE8_PRODUCTION_VS_DESCRIPTION_O13SR_V001.md                      OK
  STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md          OK
  ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.md                    OK
  STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md                   OK (both sidecars)

SUPERVISION, DOR_* ONLY (expressly permitted)                      14/14 OK
  (loop over every DOR_*.seal.sha256; 14 checked, 14 OK, 0 FAIL)

GRAND SEAL TALLY: 36/36 OK, ZERO MISMATCHES — the build's tally CONFIRMED.

NO SIDECAR PRESENT, INDEPENDENTLY CONFIRMED (the build declared both):
  PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md        NO SIDECAR
  BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md      NO SIDECAR
```

---

## §1 — WHAT SURVIVED THE ATTACK

Stated first, because a default-refute lane that reports only defects is not an audit.
Each item below was re-derived at bytes and stands.

```text
S-1  EVERY DIRECT QUOTATION I CHECKED IS ACCURATE AND WHOLE, INCLUDING THE ADVERSE
     CLAUSES.  I verified, span by span: onset gate :23-28, :70-71, :92-95, :99-108,
     :112-131, :137-139, :159-166, :171-173, :190; flux gate :24-33, :35-43, :47-51,
     :53-58, :67-89; CIS :12-13, :18-24, :77-79, :107; PRRWP :10-12, :16-24, :34-35,
     :39-45, :69-73; PRCK :23-34, :36-38, :83, :105-107; bridge gate :45-47, :92-119,
     :136-154, :164-165, :177-189, :209-211; BID :16-27, :29-36, :38-47, :49-60,
     :62-67, :83-87, :134-142, :144-152, :164; WBC :48, :112-113, :116-179, :245-252;
     split correction :29-41; T14SR :114-116, :129-138; forcing adjudication :54-58;
     O8SR :255-259; AXN :40-46, :52-65, :74-79, :83-93; census :182-254, :457, :460-461;
     GVF :26-35, :43, :693, :704-714, :719-730; C1_U_OMEGA :42, :157-160, :181, :314,
     :319; ONSET_SAT_STEP3 :136-142; FAM-2 B1A :103, FAM-3 GC2 :394, FAM-4 :296-300,
     FAM-5 :596.  NO QUOTATION IS TRUNCATED SO AS TO REVERSE ITS SENSE.  NO
     MID-SENTENCE LINE WRAP IS MIS-HANDLED — including the one that matters most,
     "unique physical record / cell" wrapped across onset gate :100-101, which the
     build quotes across the wrap at its §3.1.

S-2  THE THREE DECISIVE NEGATIVE SWEEPS REPRODUCE EXACTLY, RE-RUN BY ME.
       SWEEP F cross-citation:  onset gate -> (CAUSAL_INCIDENCE|Omega_c|
         PRIMITIVE_REVERSIBLE|M_2|carrier|FORMATION)                = 0  (build: 0)
         flux gate -> (REVERSIBLE|PRIMITIVE_REVERSIBLE|P_0^S|controlled) = 0  (build: 0)
         reversible write -> (FLUX|ONSET|Fubini|J_FS)               = 0  (build: 0)
       SWEEP E-1 {allow/require} ∩ {Omega_c}   = 4 files, and they are exactly the
         four the build names.  RE-RUN OVER BOTH COMMISSIONED ROOTS: the second root
         adds NO new file.
       SWEEP E-2 {allow/require} ∩ {support(L_c)} = 2 files, exactly the two named.
         Second root adds none.
       SWEEP D contact test over the nine consumer artifacts: 3 of 9 hit, and the
         three hit LINES are exactly the three the build discloses — onset :92,
         flux :83, split correction :36.  The build reported the raw count before
         disposing it, as required, and the disposition (a type denial, a token
         collision, a codomain clause) is correct at bytes.

S-3  FLAG-2 IS A REAL AND CORRECT CORRECTION TO THE PRIOR BUILD.  O36SR :237 does say,
     verbatim, "SECOND-HAND-QUOTE (the adjudication file is not itself in the readable
     roots under the declared sweep)".  The file IS in ROOT-1, its seal verifies, and
     the criterion sentence sits at STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md
     :54-58.  Further: O36SR's quotation STOPS at "(surplus conditions)." and drops the
     closing sentence; the build restores it whole.  PRIMARY, not second-hand.  UPHELD.

S-4  THE M-4 PROOF/RULE SEPARATION IS THE STRONGEST FINDING IN THE ARTIFACT AND IT IS
     CORRECT AT BYTES.  WBC :245-252 verified verbatim, including "NOT A MAJORANT."
     Ground (i) is a fact about an inequality's direction.  Ground (iii) is quoted from
     the onset gate's own :173 — "No geometric uncertainty budget may be substituted for
     physical action without a theorem" — which forbids an act pending a theorem and
     proves nothing false.  The build's refusal to let a rule read as an impossibility
     is exactly right, and the headline "NOTHING IN THIS CORPUS IS PROVED UNCOMPOSABLE"
     survives my attack on that row.

S-5  THE KEPT-DISTINCT HANDLING AT §3.1 IS EXEMPLARY.  C1_U_OMEGA :314 and :319 verified
     verbatim; §2.3 item 3 verified whole, including "It is not a proof of `Omega_c`."
     The build's insistence that a lane-discipline declaration is "a refusal to identify,
     not a proof of non-identity", and its INDETERMINATE-AT-BYTES on whether a coercion
     could exist, are both right and both survive.

S-6  FENCES CLEAN.  A scan of the whole artifact for `t_P|l_P|m_P|E_P|sqrt(pi)|137|Planck`
     returns nothing but the CHOICE-LEDGER sentence explaining the fence (:1429).  BID's
     root, radius, energy, mass and volume closed forms are cited at :83-106 and NOT
     reproduced.  The half-line's endpoint is bracketed, not written.  NO MAGNITUDE IS
     APPROACHED ANYWHERE IN THE ARTIFACT.

S-7  NO AUTHORING.  A scan for authoring verbs ("define/construct/propose/adopt the
     missing…", "should read", "would read as") returns only the build's own
     self-prohibition at :1348 and its consumer-set definition at :173.  §5 names the
     hole's class, its two ranges, and the objects it would touch, and stops.  Close to
     the line; not over it.  NO OBJECT, COERCION, OR CLAUSE IS DRAFTED.

S-8  ARITHMETIC AND DEDUPLICATION SOUND.  24 × 16 = 384; 8 + 368 + 8 = 384.
     G2 = P-1 verified at AXN :52-53 (it quotes CIS's own clause).  B-5 = P-2 verified
     at census :220-230 (its BYTES line points at PRIMITIVE_REVERSIBLE_RECORD_WRITE_
     PRINCIPLE_V002.md :17-45).  Both deduplications are correct, and FLAG-5(a)'s
     self-reported 26→24 / 416→384 correction is the right one.
     The 6 names → 8 cells → 7 grades shape reconciles exactly and is declared at
     FLAG-5(b).

S-9  SELF-EXCLUSION AND THE REGISTER BAR HELD.  No barred basename is cited, quoted, or
     counted anywhere in the artifact.  My own re-runs confirm the artifact does not
     appear in its own sweep results when self-exclusion is applied.
```

---

## §2 — D1: CONSUMER ARGUMENT TYPES (Q1)

### §2.1 — What checks out

C-1's three argument-bearing spans, C-2, C-3, C-4, C-5, the nine WBC clause types
C-6…C-14, and C-15 are all quoted accurately from defining text at the cited primary
bytes (S-1). C-15 is correctly and voluntarily marked SECOND-HAND; its true primary is
W3 §3 :298-303 from CERT §3.2, and O8SR :255-259 says so at bytes. The Q1 headline —
that the corpus displays five distinct consumer argument types where the prior build
displayed one — is **established at bytes**.

### §2.2 — COR-C: A CONSUMER THE BUILD MISSED, AND THE CORPUS ITSELF PAIRS IT

The build's own producer P-3, the formation ordering, has a **displayed argument type**
and is therefore a consumer. Deciding bytes, on a seal-verified artifact the build
consumed and quoted elsewhere — `STAGE8_INGREDIENT_CENSUS_O17SR_V001.md` **:457**,
quoted whole:

> "TAKES an antecedent carrier plus a ready state; YIELDS a durable public record.
> TYPE: RULE (a three-step composite)."

carried a second time in the primary at `STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_
TYPING_V001.md` :26-27 ("a carrier and ready state precede the write") and at its :723
("carrier:          antecedent factor / ready state").

The same census then **performs the pairing the build's matrix cannot see**, at
**:749-752**, quoted whole:

> "REFERENCED BY  B-5 on its input side (|0_R> is the ready register); and by the
> formation ordering of §2.2, whose first term is "antecedent carrier".
> YIELDED BY     A-4 yields a carrier from six hypotheses (see U-8), and nothing
> in the census yields the ready state as such."

A-4 is the build's P-4. So a seal-verified artifact states at bytes that the carrier
derivation's output is referenced on the input side of two other rows the build carries
— and the build graded neither pairing, because it never opened a consumer column for
either. The same census supplies a second such pairing at **:799-801**: "B-1's rule-level
input (pairwise disjoint components) is satisfied by A-1, A-2, A-3 as exhibited objects."

This is the mirror image of the error the build was chartered to correct. Its CH-2
declares "a census that refuses to look at a consumer's output side is the exact error
this commission was chartered to correct" — and then refuses to look at a producer's
**input** side. The refusal is undisclosed in CH-1, CH-2, or the FLAG BLOCK.

**A third missed consumer sits inside the build's own declared scope and its own sealed
ground list.** `STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md` (seal OK) is an
onset-machinery artifact that **takes two producers' outputs at bytes and returns a
verdict on them**. Its :104-118 consumes the flux gate's first-orthogonality output by
file and line ("`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:47-78` studies an
affine constant-axis representative on the primitive record factor"); its :125-131
consumes the bridge gate's ("`PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md:80-118`
independently derives…"). The build read this file and used it only as a limit-quote for
M-3. As a consumer column it is absent.

### §2.3 — COR-F: THE FROZEN-INVENTORY CORROBORATION OF C-1's TYPE IS MISDESCRIBED

The build writes at :224-228 that the onset gate's cell "is independently typed that
way in the frozen inventory:
`STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md` :149 (seal OK) lists the
onset-saturation rule's **inputs** verbatim as 'imported FS lower bound and unique
physical record cell'".

At bytes, two things are wrong and one is right.

```text
RIGHT   :149 does carry the string "imported FS lower bound and unique physical
        record cell", verbatim.
WRONG 1 THE COLUMN IS NOT "inputs".  The table header at :142 reads, verbatim:
        "| # | Primitive or premise class | Kind | Sealed/as-used status |
         Presupposes | Value path? |"
        The word is PRESUPPOSES.  The build applies "verbatim" to a row while
        renaming its column, and then leans on the renamed label — "so the cell is
        an input, and the gate is a consumer of it".
WRONG 2 THE ROW DOES NOT TYPE THE CELL AS AN INTERVAL.  It repeats the phrase
        "unique physical record cell" unchanged.  So it cannot be, and is not,
        an independent typing "that way".
```

Consequence for the load-bearing §3.1 row: the one-dimensional typing of C-1's third
argument now rests on exactly two things — the subscript of `integral_cell d tau`
(:70-71), and the gate's own not-established line :162, which reads "**the numerical
causal-cell interval**". That second support cuts both ways: the gate's own noun is
CAUSAL-CELL, and BID :35-36 displays exactly the geometry in which a causal cell
*carries* an interval ("`R_R` is the maximum areal radius of the causal diamond with
tip-to-tip proper-time separation `T_R`"). The build itself displays that adjacency at
its :804-806 and declines to build on it, correctly. **The MISMATCH grade is not
refuted; one of its two corroborations is.**

### §2.4 — COR-H: C-16's ARGUMENT TYPE IS OVERSTATED

The build states at :394-395: "Every row's content is of one shape — *'the admissible
set is X'*". At `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md` bytes, four of the ten
rows display an admissible set (R-1 :129, R-3 :131, R-5 :133, R-6 :134). The other six
do not: R-2 :130 "cocycle consistency + single-valuedness jointly pin the character
lattice"; R-4 :132 "two independent mechanisms deliver the same exact zeros — surplus
conditions agreeing"; R-7 :135 "each a single displayed derivation with its own unique
conclusion"; R-8 :136 "the first is a kinematic identity"; R-9 :137 "conditional
forcing"; R-10 :138 "nine would-be require-chains". The column's type is displayed;
"every row" is not carried.

### §2.5 — COR-D: ONE OF THE TWO COMMISSIONED ROOTS WAS NEVER SWEPT

The commission's corpus is two roots plus the DOR rulings. The build's §0 :32-34
declares "Two directories were used: `.../workspace` (ROOT-1) and `.../supervision`
(the DOR rulings only)", and **every sweep block is headed "ROOT-1, all files."** The
second commissioned root —
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`
— was never swept, and its omission is nowhere declared.

The artifact nonetheless makes **either-root** claims: :806 "no clause of this corpus
takes `Omega_c` and returns the onset gate's integration domain"; :828-830 "No artifact
in either root proves that `Omega_c` cannot be the onset gate's cell"; :1280 "no
artifact in either root ever performs the application".

I ran the second root. Mitigation and residue, both reported:

```text
MITIGATION  the second root holds ~1,522 .md files against ROOT-1's ~1,804, and all
            but 35 are same-named.  SWEEPS E-1, E-2 AND F RE-RUN OVER BOTH ROOTS
            RETURN THE SAME SETS.  No "either root" negative is falsified by the
            omission.
RESIDUE     of the 35, nine are bar-filtered out unread (DECLINE_REGISTER ×2,
            QUESTIONSSETTLED_REGISTER, REGISTER_HEAD_SNAPSHOT ×2, THE_PLAN_TO_ALPHA
            ×4).  Of the remaining 26, ONE that existed when the build ran carries
            contact tokens — a consumer of the cell-producer's output that the
            matrix never sees:
            STAGE8_AXN_DESCENT_INVERSE_CODEX2_V001.md
              :139-140  "**every** admitted `Omega_c` closure face, with an
                        enumeration/completeness certificate.  A prose placeholder
                        `{closure face of Omega_c}` does not meet the C4 contract's
                        addressing requirement."
              :182      "support(rho_c B_global) subset Omega_c."
            It carries zero hits for allow/require, onset, integral_cell,
            closed-dilation, or J_FS, so it does not bear on the commissioned row.
NOT COUNTED AGAINST THE BUILD, DECLARED RATHER THAN HIDDEN: a second contact-bearing
            file, STAGE8_COLD_PROCESS_CHALLENGE_1122_CODEX2_V001.md (Date: 2026-08-16),
            appeared in the second root at 02:17, TWELVE MINUTES AFTER the target
            sealed at 02:05.  It postdates the artifact and is excluded from every
            count above.  Recorded only because it is a parallel lane on the same
            question and its :281 reaches the same verdict on the commissioned row by
            an independent route — "P-01 `Lorentz-covariant causal cell + interaction
            density` | J-23 `onset/path on the unique physical record cell` | **NO
            JOIN.** No equality identifies `Omega_c` with that record cell".  A
            convergence, not evidence against the build.
```

**D1 GRADE: REFUTED.** Not because the sixteen displayed types are wrong — they are
right, and the five-type headline is established — but because the consumer set is
incomplete in a way that is decision-relevant: the corpus's own seal-verified census
displays a producer→consumer pairing (census :749-752) whose consumer column the matrix
does not have, and a commissioned root was never swept while either-root claims were
made.

---

## §3 — D2: PRODUCER OUTPUT TYPES (Q2)

### §3.1 — What checks out

P-1 through P-8 are each quoted accurately from defining text with their own limiting
clauses attached (S-1). The four [NEW] producers are real: P-5's `K_write,rel` and
`U_write` sit at flux gate :67-89; P-6's `(U_0, U_1)` at onset gate :112-131; P-7's
`|r_p>` at bridge gate :92-115; P-8's root and diamond geometry at BID :83-106. The
structural finding — that the onset gate and the flux write gate each have an output
side the prior build never displayed — is **established at bytes**, and it is the best
thing in the artifact.

G1…G6 verified at AXN :40-93; FAM-2 at B1A :103; FAM-3 at GC2 :394; FAM-4 at
AXN_STAGE_RULE :296-300; FAM-5 at MEASURE :596. The 5-families / 10-rules count is
reproduced, not inherited, and my own uncapped re-sweep lands in the same neighbourhood
(`generation rule` 27 net files, `generation rules` 7, `generating rule` 6).

### §3.2 — COR-G: THE CENSUS'S 1.A BAND IS DROPPED WITHOUT DISCLOSURE

The build carries the census RULE/MAP band B-1…B-8 into the matrix and states so at
:754-758. It carries A-4 (as P-4). It **silently drops the rest of census band 1.A**,
whose heading at :125 reads "OUTPUT SIDE = A COMPLEX OR CARRIER (these are OBJECTS:
yielded, taking nothing)" and whose four remaining rows each display an output type at
bytes:

```text
A-1 :128-137  YIELDS "a four-vertex, four-edge oriented 1-complex with the bounding
              2-cell deliberately omitted"
A-2 :139-147  YIELDS "the oriented 2-skeleton of (Z/LZ)^4, L >= 3"
A-3 :149-154  YIELDS "five exhibited single-cell complexes"
A-5 :168-176  YIELDS "a DECLARATION of a class … No member is displayed anywhere of
              record" — an empty-extension object, and the census says so
```

Three of those four are precisely what the census pairs with B-1 at :799-801. Dropping
them is defensible as a scope choice; dropping them without a CHOICE-LEDGER entry is
not, because the producer count 24 and hence the cell count 384 depend on it.

Minor, recorded not scored: the census types P-4 at :165 as "RULE (it takes hypotheses
and yields a carrier)", i.e. P-4 too has a displayed input side (the six pre-alpha
hypotheses, PRCK :12-20, census U-8 :763-772). The build displays P-4's output only.

**D2 GRADE: CONFIRMED-WITH-CORRECTIONS.** Every producer row displayed is carried at
bytes; the counts are independently reproduced and the two deduplications are correct.
The census band 1.A is dropped undisclosed (COR-G).

---

## §4 — D3: THE MATRIX (Q3) — THE HEAVIEST DIMENSION

### §4.1 — COR-A: THE PRIOR COMMISSION'S ERROR IS REPEATED IN A SUBTLER FORM

This is what the dimension was written to catch, and it is present.

The build's §1.2 table at :363-373 displays **nine distinct clause argument types** for
C-6…C-14 — a weighted operator family plus a projector plus a localizer condition
(CS-1); a pre-existing operator family plus a phase-pair partition (CS-2); an HS-class
operator pair (CS-3); a baseline resolvent/determinant (CS-4); **a C-infinity compactly
supported function on the open unit diamond** plus operators (CS-5); **`lambda_max` and
a time extent `tau_R`** (CS-6); an insertion-graded operator family (CS-7); the onset
gate's own budgets (CS-8); a localizer, an indicator, and a locus (CS-9). I verified all
nine against WBC :116-179; the typing is accurate.

Then §3.4 :1029-1044 grades **all 24 producers against nine consumer columns
{C-6…C-12, C-14, C-15} — 216 cells — under a single displayed reason, and that reason
quotes only C-15's argument type**:

> "CONSUMER ARGUMENT TYPE (C-15, quoted): "quadratic forms t on Q = D(h_0) =
> H^1(R^3) (x) C^4", consumed at trace/HS grade. … NOT ONE OF THESE IS A QUADRATIC FORM
> ON H^1(R^3) ⊗ C^4."

Eight of those nine columns are thereby graded against a type that is not theirs. The
build displayed the other eight types and then did not use them. That is the O36SR
error at one remove: no longer "one type for the whole corpus", but "one type for
fifty-six percent of the matrix".

**Two concrete rows where the collapse suppresses a live question, both read off
displayed text only:**

```text
C-11 = CS-6.  Its displayed argument at WBC :154-156 is a spectral datum and a TIME
  EXTENT: "lambda_max tau_R = pi IS the first-orthogonality criterion — one origin,
  not two derivations".  Producers carrying a time extent and a first-orthogonality
  datum at bytes:
      P-2  PRRWP :59 "A constant profile over an assumed `tau_R`"; :62
           "H_selected = hbar K_selected/tau_R"; :65 "It does not derive `tau_R`";
           status :84 `tau_R_derived = false`.
      P-4  PRCK :83 "with first orthogonality at `Delta S/hbar=pi`".
  These rows are graded MISMATCH under "not a quadratic form on H^1(R^3) ⊗ C^4".
  That reason does not address C-11's displayed type at all.  Whether the closure
  lane's `tau_R` and the write lane's `tau_R` are the same object is NOT FIXED BY
  EITHER SIDE'S DEFINING TEXT.  The correct grade at bytes is
  INDETERMINATE-AT-BYTES, not MISMATCH.  I assert no identification and author none.

C-10 = CS-5.  Its displayed argument at WBC :146-149 is "a C-infinity compactly
  supported function ON THE OPEN UNIT DIAMOND".  P-1 yields a continuum causal
  diamond.  Graded MISMATCH under the quadratic-form reason, which again is not
  C-10's type.  INDETERMINATE-AT-BYTES.
```

Note the asymmetry that makes this a finding rather than a quibble: the build **did**
pull C-13/CS-8 out of the band and grade it individually — the one column where the
contact is unmistakable because it is made by digest. The eight columns where the
contact would have to be read off a type were left in the band.

### §4.2 — COR-B: 126 CELLS CARRY A GRADE WITH NO DISPLAYED REASON AT ALL

The build fixes its own rule before applying it, at :768-770:

> "MISMATCH     they are not the same type.  Both displayed; the exact locus of the
> difference named."

Its §3.4 opens "Every remaining producer graded against every remaining consumer."
It is not. The displayed bands do not reach seven of the sixteen columns for most
producers. Exhaustive arithmetic over the build's own row and column sets:

```text
INDIVIDUALLY REASONED, both types quoted
  §3.1  P-1 × C-1                                                          1
  §3.2  P-4×C-2, P-4×C-1, P-7×C-1, P-6×C-13, P-7×C-13, P-8×C-5, P-8×C-3    7
  §3.3  P-2×C-1, P-5×C-1, P-3×C-4, P-1×C-4                                 4
                                                                        ────  12

BAND-REASONED
  §3.4 band 1  24 producers × {C-6..C-12, C-14, C-15}                    216
  §3.4 band 2  {G3,G4,G5} × all 16   (48 cells; 27 overlap band 1)        +21
  §3.4 band 3  {G1,FAM-2,FAM-3,FAM-4} × {C-3,C-16}                         +8
  §3.4 band 4  FAM-5 × C-3                                                  +1
                                                                        ────  246

TOTAL CELLS REACHED BY ANY DISPLAYED TEXT                    12 + 246  =  258
TOTAL CELLS IN THE DECLARED MATRIX                             24 × 16 =  384
CELLS WITH NO DISPLAYED REASON AND NO DISPLAYED LOCUS                   =  126
```

The 126 are the cells of columns {C-1, C-2, C-3, C-4, C-5, C-13, C-16} for the
producers not individually named at §3.1-§3.3 and not covered by bands 2-4 — for
instance B-2 (census :190-199, "one N-cell write operator `W_N^(n)[a]`") against C-1
(the onset gate, which asks for source-conditioned unitaries), or P-4 against C-5, or
B-6 (a normalized lift `z_i : U_i -> C^2`) against C-2. Each is booked into the
MISMATCH column of the :1086-1092 tally and none is shown.

### §4.3 — COR-E: THE BUILD'S STATED FALLBACK FOR M-1 AND M-2 IS FALSE AT BYTES

IMPORT AUDIT entry IMP-2, at :1467-1474, concedes the Lie-algebra gloss and then says:

> "If a reader declines the gloss, MATCH 1 and MATCH 2 fall back to the corpus's own
> words — "the primitive record factor" on both sides — which is still a MATCH, on the
> noun rather than on the algebra.  **The grade does not depend on the import.**"

The producer side does not say that. `PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md`
contains **zero** occurrences of "primitive record factor" (grep -c = 0). Its nouns are
"a primitive faithful carried block", "a real two-plane", "the capacity body", "its real
order-unit space", "the primitive observable algebra". Corpus-wide, the phrase occurs in
nine files — the onset gate V001/V002/V003, the flux gate V002/V003, LOAD_BEARING_
HOLONOMY, ONSET_SATURATION_STEP3, PRODUCTION_VS_DESCRIPTION, and the build's own
artifact — and **in none of them is it equated with `M_2(C)`, the real two-plane, or the
Bloch ball**. The two sides never share the noun. So M-1 and M-2 stand only on IMP-2,
and the sentence "The grade does not depend on the import" is refuted at bytes.

This matters beyond bookkeeping: M-1 and M-2 are two of the six named matches, and the
build's own Q4 grades them COMPOSABLE-BUT-NEVER-COMPOSED on the strength of a type fit
that turns out to be an imported one.

### §4.4 — COR-I: M-5′ GRADES ONE SET AND CITES ANOTHER

CH-4 declares the producer output for M-5/M-5′ to be **the half-line** at BID :134-142
("replacing `C_R=1` by only `C_R<=1` leaves […] and does not derive a unique cell").
M-5′ then grades P-8's half-line × C-3 COMPOSED-OF-RECORD because the forcing criterion
"names this case inside its own defining sentence".

At bytes the criterion names **the exact-joint determination** — "EXACT JOINT
DETERMINATION (n conditions, n freedoms — the marker/C_R case…)", adjudication :55-56 —
i.e. the `C_R = 1` case whose admissible set **is a point**. The half-line is precisely
the case BID says is *not* forcing. The type-level MATCH (an admissible set meets a
consumer of admissible sets) stands; the composition grade is applied to a different set
than the one CH-4 declares as the producer output.

### §4.5 — WHAT THE MATCHES SURVIVE

Attacked equally hard, per the mandate:

```text
M-3  P-7 × C-1.  Both sides quoted from defining text (bridge :92-119; onset :23-28).
     The composition is performed at bridge :151-154 and its limit is quoted whole at
     :164-165.  Independently corroborated at ONSET_SAT_STEP3 :136-142.  STANDS.
M-4  P-6/P-7 × C-13.  Contact by digest `cc113a16`, verified at C1_U_OMEGA :131 (S13)
     and at WBC :167-173.  Both sides quoted from definitions.  STANDS.
M-6  P-1 × C-4.  Producer's self-typing CIS :12-13 ("fixes the support and reuse law")
     against consumer's displayed argument "declared support" (split correction :35-36)
     and its status `CELL_CONSTRAINT_ONLY` (:111).  The match is on the SUPPORT noun,
     and the build says so.  STANDS.
M-5  P-8 × C-5.  Performed inside one artifact — and the build discloses the decisive
     qualification itself (:1263-1265): the diamond consumed is DECLARED at BID :29-36,
     not obtained from P-1.  STANDS, with that qualification.
M-1, M-2  stand only on IMP-2 (COR-E).
M-5′ type-level MATCH stands; its Q4 grade is regraded (COR-I).
```

**D3 GRADE: REFUTED.** The eight MATCH cells are not shown to be wrong — five of them
survive a hard attack. But the matrix as an object is not carried at bytes: 168 cells
are asserted MISMATCH under a reason drawn from a different consumer's argument type,
and 126 more are asserted with no reason at all. The corrected tally is at §7.

---

## §5 — D4: COMPOSITION (Q4)

### §5.1 — What checks out

The COMPOSITION-BARRED analysis is the artifact's strongest work and it survives (S-4).
The "never composed" negatives for M-1, M-2 and M-6 rest on Sweeps E and F, which I
re-ran over **both** commissioned roots and which reproduce exactly (S-2). The contact
test's raw-count-before-disposition discipline is honoured and the raw count is right.
`STAGE8_PRODUCTION_VS_DESCRIPTION_O13SR_V001.md` :229-237's typing refusal — "Neither
takes a making as input nor returns a record as output" — is correctly read as a refusal
rather than an application.

### §5.2 — COR-C bites here too: the hunt for missed compositions was not exhaustive

The mandate is "For every COMPOSABLE-BUT-NEVER-COMPOSED, hunt independently for a
composition the build missed." The build's hunt is scoped to its own sixteen columns, so
it cannot see the pairings the corpus displays outside them:

```text
census :749-752  A-4's carrier is REFERENCED ON THE INPUT SIDE of B-5 and of the
                 formation ordering.  A-4 = P-4; B-5 = P-2; the formation ordering
                 = P-3.  Three rows the build already carries; a pairing it cannot
                 grade because two of them have no consumer column.
census :799-801  "B-1's rule-level input (pairwise disjoint components) is satisfied
                 by A-1, A-2, A-3 as exhibited objects."  A-1..A-3 are the rows
                 dropped at COR-G.
ONSET_SAT_STEP3  :104-131 takes the flux gate's and the bridge gate's first-
                 orthogonality outputs BY FILE AND LINE and adjudicates them.  The
                 build read this artifact and used one sentence of it.
```

None of these is a composition of the *commissioned* row (cell-producer × onset gate),
and none disturbs the M-4 bar. But "NOT ONE COMPOSITION IN THIS CORPUS IS IMPOSSIBLE"
is a claim about the corpus, and the frame in which it was tested is demonstrably
narrower than the corpus's own census.

### §5.3 — COR-I regrade

M-5′ (P-8's half-line × C-3): **INDETERMINATE-AT-BYTES**, not COMPOSED-OF-RECORD. The
criterion is applied of record to the marker/`C_R` **exact-joint** case; whether it is
applied to the half-line the build declares as the producer output is not fixed by
either side's text.

**D4 GRADE: CONFIRMED-WITH-CORRECTIONS.** Every negative that was tested reproduces
exactly at bytes, and the proof/rule separation on the single bar is correct and
important. The hunt for missed compositions was bounded by a consumer set the corpus
itself over-runs, and one composition grade is regraded.

---

## §6 — D5: THE GAP (Q5) AND THE BARS

```text
FENCES              CLEAN.  Verified by scan (S-6).  No coupling, scale, root,
                    eigenvalue, norm or constant is computed; no magnitude approached.
                    BID's closed forms cited at :83-106, not reproduced.  The
                    half-line endpoint is bracketed.  CH-6's treatment of `pi hbar/2`
                    and `pi hbar` as quotation-in-units-of-hbar is the right call.
AUTHORING           NONE.  Verified by scan (S-7).  §5 names the missing object's
                    CLASS (an intermediate object of dilation type; an admissibility
                    proof of identification type), its two RANGES, and the named
                    objects it would touch, and stops.  TOY_SEPARATION declares the
                    two places a toy was tempting and refuses both — including the
                    (-iY_R) / -iY coincidence, which is displayed and left as
                    MISMATCH rather than bridged.  BAR RESPECTED.
ADVOCACY            NONE FOUND.
RE-READ NEGATIVES   NONE.  Prior negatives are cited for their bytes.
SEALS               36/36 OK, independently reverified (§0).
SELF-EXCLUSION      HELD.
OWN-DRAFT DEFECTS   DECLARED, and FLAG-5(a)'s 26→24 / 416→384 correction is the
                    right one (S-8).
GAP DESCRIBED,      YES.  Q5 does not fill the hole and does not soften it.  Its
NOT FILLED          claim that the two sides "differ in arity" is read off displayed
                    text (two-factor vs three-factor; a spacetime region vs a
                    single-parameter domain) and is carried.

COR-J  SWEEP CUTOFFS: UNITS UNDECLARED, SEVEN COUNTS UNREPRODUCIBLE.
       Blocks A and B report "NET HITS" / "TOTAL / NET" without ever saying whether a
       hit is a FILE or a LINE, and without saying whether the basis is `*.md` or all
       file types.  Re-running with the same bar and self-exclusion:
         PATTERN                        BUILD   MY md-FILES   MY LINES   ALL-TYPES
         boundary-closure                 44         44          —          —   OK
         forcing criterion                 7          7          —          —   OK
         cell assignment                   6          6          —          —   OK
         formation ordering                2          2          —          —   OK
         carrier derivation                6          6          —          —   OK
         generation rule                  27         27          —          —   OK
         production rule / map           3 / 3      3 / 3        —          —   OK
         K_write                           8          8          —          —   OK
         closed-dilation                   4          4          —          —   OK
         declared support                  6          6          —          —   OK
         unique physical record cell       8          7          9          8
         allow/require                    84         75        163         80
         boundary closure                 47         36         71         37
         U_write                          12          9         22          9
         ready state                      75         64         89         71
         record cell                     185        179        365          —
         record-changing                  16         15         26          —
       Eleven patterns reproduce exactly on a net-md-file basis; seven do not
       reproduce on either basis.  NO GRADE IN THE ARTIFACT TURNS ON ANY OF THE SEVEN
       — the decisive sweeps D, E and F reproduce EXACTLY (S-2) — so this is a
       reporting defect, not an evidentiary one.  Reported because the standing
       discipline requires the actual hit count for every pattern.

COR-K  LINE-CITATION DRIFT, CONTENT-EXACT THROUGHOUT.  flux gate cited :26-33, actual
       :24-33; :36-45, actual :35-43.  PRRWP cited :38-45, actual :39-45; :68-73,
       actual :69-73; :33-35, actual :34-35.  bridge gate cited :162-163, actual
       :164-165; :208-210, actual :209-211.  WBC CS-9 cited :174-183, actual :174-179.
       In every case the quoted TEXT is exact.  D01's "Lorentz-covariant continuum
       diamond, not a CW object" is bolded by the build; the source at C1_U_OMEGA :42
       carries no emphasis.  No grade turns on any of these.
```

**D5 GRADE: CONFIRMED-WITH-CORRECTIONS.**

---

## §7 — THE CORRECTED MATRIX TALLY

I do not re-grade 384 cells; I state what the build's own tally carries at bytes.

```text
THE BUILD'S TALLY                     MATCH 8 · MISMATCH 368 · CANNOT-DETERMINE 8 = 384

CARRIED AT BYTES (a displayed reason that addresses the consumer's own displayed type)
  individually reasoned, both types quoted ..............................  12
  {G3,G4,G5} × all 16 — "these produce no object at all", a reason that
    is general and does apply to every column ...........................  48
  column C-15 under the quadratic-form reason (24 cells, less the 3
    already counted above) .............................................  21
  {G1,FAM-2,FAM-3,FAM-4} × {C-3,C-16}, CANNOT-DETERMINE, reasoned .......   8
  FAM-5 × C-3, reasoned from the producer's own self-typing .............   1
                                                                        ────
  CARRIED ...............................................................  90

NOT CARRIED
  columns C-6..C-12 and C-14 (8 columns × 24 producers = 192), less the
    24 G3/G4/G5 cells already carried — asserted MISMATCH under a reason
    drawn from a DIFFERENT consumer's argument type (COR-A) .............. 168
  cells with no displayed reason and no displayed locus (COR-B) .......... 126
                                                                        ────
  NOT CARRIED ........................................................... 294

  90 + 294 = 384.   TWENTY-THREE PERCENT OF THE MATRIX IS CARRIED AT BYTES.

THE MATCH COLUMN, CORRECTED
  MATCH cells that survive attack unqualified .............  5   M-3, M-4 (×2),
                                                                 M-5, M-6
  MATCH cells standing only on IMP-2, contrary to the
    artifact's own claim that they do not (COR-E) .........  2   M-1, M-2
  MATCH cell whose type-level grade stands but whose Q4
    grade is regraded (COR-I) ...........................    1   M-5′ (P-8 × C-3)
                                                            ───
                                                              8

TWO ROWS REGRADED OUT OF MISMATCH, INTO INDETERMINATE-AT-BYTES (COR-A)
  P-2 × C-11 and P-4 × C-11 — the consumer's displayed argument is a spectral
  datum plus a TIME EXTENT `tau_R`, and both producers display a time extent or a
  first-orthogonality datum at bytes.  Whether they are the same object is not
  fixed by either side's defining text.  NO IDENTIFICATION IS ASSERTED OR AUTHORED.
  P-1 × C-10 likewise — a C-infinity function on the open unit diamond against a
  continuum causal diamond — INDETERMINATE-AT-BYTES.

THE FRAME ITSELF IS NOT ESTABLISHED AS COMPLETE
  at least 3 consumer columns are missing (the formation ordering as consumer,
  census :457; B-5/P-2 as consumer, census :221; ONSET_SAT_STEP3 as consumer,
  its :104-131), and at least 4 producer rows are dropped undisclosed (census
  band 1.A, :125-177).  The corpus's own census displays two producer→consumer
  pairings that lie outside the 24 × 16 frame entirely (census :749-752, :799-801).

Q4, CORRECTED
  COMPOSED-OF-RECORD ............ 2   M-3, M-5      (was 3)
  COMPOSABLE-BUT-NEVER-COMPOSED . 3   M-1, M-2, M-6 (unchanged as to the sweeps run;
                                       the sweeps do not cover the missing columns)
  COMPOSITION-BARRED ............ 1   M-4           (unchanged; CONFIRMED at bytes)
  INDETERMINATE-AT-BYTES ........ 1   M-5′          (new)

  THE BUILD'S HEADLINE — "NOTHING IN THIS CORPUS IS PROVED UNCOMPOSABLE" — SURVIVES.
  ITS HEADLINE "THE COMMISSIONED ROW IS MISMATCH" SURVIVES, WITH ONE OF ITS TWO
  CORROBORATIONS REFUTED (COR-F).
```

---

## §8 — REQUIRED BLOCKS

### CHOICE LEDGER

```text
AC-1  I GRADED THE §3.4 BULK BAND AS A REPEAT OF THE PRIOR ERROR RATHER THAN AS A
      PERMISSIBLE COMPRESSION.  ALTERNATIVE REJECTED: accept that the WBC clauses all
      live in one closure lane whose objects act on Q = D(h_0) = H^1(R^3) ⊗ C^4, so
      one reason covers them.  REASON FOR REJECTING: that common ambient space is
      NOWHERE DISPLAYED in the build as the reason; the displayed reason is C-15's
      type, and C-11's displayed argument (a time extent) and C-10's (a function on a
      diamond) are not of that type.  A reader who prefers the alternative should note
      that the compression is still undeclared, and that C-11 remains unaddressed on
      its own terms either way.

AC-2  I COUNTED THE 126 UNREASONED CELLS AGAINST THE BUILD RATHER THAN TREATING §3.4's
      OPENING SENTENCE AS COVERING THEM.  ALTERNATIVE REJECTED: read "Every remaining
      producer graded against every remaining consumer" as a blanket assertion that
      discharges the requirement.  REASON: the build fixed its own MISMATCH rule at
      :768-770 — "Both displayed; the exact locus of the difference named" — before
      applying it.  A blanket sentence does not name a locus.

AC-3  I TREATED THE CENSUS'S :749-752 AND :799-801 AS DISPLAYED PAIRINGS.  ALTERNATIVE
      REJECTED: treat them as the census's bookkeeping about its own rows rather than
      as corpus-level compositions.  REASON: they are on a seal-verified artifact the
      build itself consumed and quoted, and they name the same objects the build
      carries as P-4, P-2 and P-3.  I report them as PAIRINGS DISPLAYED, and I do NOT
      grade them as compositions performed — that would require reading the pairing as
      an application, which the bytes do not say.

AC-4  I DID NOT RE-GRADE THE 294 NOT-CARRIED CELLS.  Doing so would require deciding
      168 rows against eight consumer types and 126 against seven more.  I report the
      evidentiary state and name two rows where the bytes visibly do not decide.  RISK
      CARRIED: the build's 8/368/8 split may be substantially correct; I say it is not
      ESTABLISHED, not that it is wrong.

AC-5  I RAN THE SECOND COMMISSIONED ROOT ONLY FOR THE DECISIVE SWEEPS (E, F, contact
      tokens over its 34 unique files) RATHER THAN EXHAUSTIVELY.  RISK CARRIED AND
      STATED: a match could hide in the 34, though none carries allow/require, onset,
      integral_cell, closed-dilation, or J_FS.
```

### TOY_SEPARATION

```text
NO TOY WAS BUILT, RUN, OR REASONED FROM.  Every type, count and grade in this audit is
read off defining text at primary bytes, or is arithmetic over the build's own declared
row and column sets.  No model, example, schematic, worked case, or "suppose" appears.

THE PLACE A TOY WOULD HAVE BEEN TEMPTING, DECLARED: at §4.1 the closure lane's `tau_R`
(WBC :155) and the write lane's `tau_R` (PRRWP :59-65) are the same token, and both
sides display a first-orthogonality condition at pi.  Writing down the map that would
identify them — or exhibiting a case where they differ — would be AUTHORING THE MISSING
OBJECT or authoring its refutation.  Both are barred and neither was done.  The row is
regraded INDETERMINATE-AT-BYTES and left there.

THE SECOND PLACE, DECLARED: the gate's own phrase "the numerical causal-cell interval"
(onset :162) is compatible with a causal cell that CARRIES an interval, and BID :35-36
displays that geometry.  Constructing the coercion would be authoring the missing
admissibility proof.  Barred, not done.  I report only that one of the build's two
corroborations for the interval typing fails (COR-F), and I leave the MISMATCH grade
standing on its remaining support.

ACTUAL SURFACE: every artifact consumed here is a seal-verified member of the
commissioned corpus, or one of the two artifacts declared sidecar-less by the build and
confirmed sidecar-less by me.  Nothing rests on a reconstruction, paraphrase, or summary.
```

### IMPORT AUDIT

```text
AIMP-1  "argument type" / "output type" / "codomain" / "carried" / "frame".  ORDINARY
        AUDIT VOCABULARY, supplied by the commission.  Used only as labels.  No type
        theory, no coercion or variance rule, is imported or used.
AIMP-2  ARITHMETIC over the build's own declared sets (24 × 16; the band overlaps at
        §4.2 and §7).  Counting only.  Not physics, not a magnitude.
AIMP-3  I DECLINE THE BUILD'S IMP-2 GLOSS ("the Lie algebra of the unitary group of
        M_2(C)") rather than adopting it.  Declining an import is not itself an
        import.  The consequence I draw — that the two sides never share a noun — is
        a grep result over corpus bytes, not a mathematical claim.
AIMP-4  Fubini-Study / Mandelstam-Tamm, Misner-Sharp.  NOT IMPORTED BY ME.  Quoted
        where the corpus imports and labels them.
NO PHYSICS, NO ANALYSIS, NO CATEGORY THEORY DECIDES ANY GRADE IN THIS AUDIT.
```

### SWEEP CUTOFFS — PATTERN, HIT COUNT, LEAK COUNTER

All sweeps run by me, in ROOT-1 unless marked, `*.md` basis, bar-filtered as an ARRAY,
self-excluding both the target artifact and this one.

```text
PATTERN / TEST                                     NET     LEAKS DROPPED UNREAD
"primitive record factor", corpus-wide, file basis    9              0
  (and 0 occurrences in PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md — COR-E)
"M_2(C)|real two-plane|Bloch", file basis             17             0
Sweep-A/B re-runs, 23 patterns                    see COR-J table    2
  (leaks: 1 on "boundary-closure", 1 on "record cell" — the same two the build
   reports; both dropped by the basename filter, neither opened)
SWEEP F re-run (3 cross-citation probes)          0 / 0 / 0          0
SWEEP E-1 re-run, BOTH ROOTS                      4 files / +0       0
SWEEP E-2 re-run, BOTH ROOTS                      2 files / +0       0
SWEEP D contact test re-run, 9 consumer artifacts 3 of 9 hit         0
SECOND-ROOT DELTA: .md files unique to the cleanroom root            35
  of which bar-filtered out unread (THE_PLAN* ×4, *REGISTER* ×5)      9
  of which carrying any contact token                                 2
    STAGE8_AXN_DESCENT_INVERSE_CODEX2_V001.md, 6 hits, opened
    STAGE8_COLD_PROCESS_CHALLENGE_1122_CODEX2_V001.md, 13 hits,
      opened — POST-SEAL (02:17 vs the target's 02:05), excluded from
      every count against the build; see COR-D
DOR_* seals in supervision                        14/14 OK           n/a
NOTHING ELSE WAS OPENED UNDER THE REGISTER BAR.  TOTAL LEAK COUNT: 2, both dropped
unread.  No git action was taken.
```

### FLAG BLOCK

```text
FENCES, TERMINAL:
  alpha_computed = false
  proof_authorized = false
  kappa_record_computed = false
  coupling_computed = false
  scale_computed = false
  root_computed = false
  eigenvalue_computed = false
  norm_computed = false
  magnitude_approached = false
  measured_constant_comparison_performed = false
  git_command_run = false

SEAL TALLY: 36/36 OK, ZERO MISMATCHES (16 ground + 6 cross-check + 14 DOR),
  independently reverified from each artifact's own directory.
  Plus the target's own two sidecars: 2/2 OK.

AFLAG-1  BAR INCIDENTS: ZERO.  No barred basename was opened, quoted, or counted.
         Exclusions passed as an ARRAY throughout.  Per-pattern leak counters carried;
         total leaks 2, both dropped unread.
AFLAG-2  SELF-EXCLUSION: this audit excluded BOTH the target artifact and itself from
         every sweep.
AFLAG-3  TWO LOAD-BEARING ARTIFACTS CARRY NO SIDECAR — confirmed independently, exactly
         as the build declares at its FLAG-1.  The build's mitigation (each dependent
         row independently carried on a seal-verified artifact) is real: P-4's output
         type at census :460-461, C-5's defining sentence at GVF :704-714.  Both
         verified.  The build's decision to grade those rows one confidence step lower
         is correct and is not disturbed here.
AFLAG-4  OWN-DRAFT DEFECT, DECLARED: my first pass computed the not-carried count as
         126 + 192 = 318 by counting the {G3,G4,G5} cells only once.  Corrected before
         sealing: those 48 cells ARE carried by band 2's general reason, of which 24 lie
         inside the band-1 columns, giving 168 + 126 = 294 not carried and 90 carried.
         I record the error rather than normalize it silently.
AFLAG-5  I DID NOT RE-GRADE THE 294 NOT-CARRIED CELLS (AC-4).  Stated as exposure.
AFLAG-6  BARS RESPECTED.  No missing object, coercion, intermediate object,
         admissibility proof, ruling, or clause was authored, drafted, sketched, or
         named-as-it-should-read — not in a toy, a footnote, or an aside.  Where I found
         a suppressed row (C-11, C-10) I regraded it INDETERMINATE-AT-BYTES and stopped.
         No advocacy.  No adoption.  No catalogued negative re-read or re-litigated.
```

---

## §9 — CORRECTIONS IN SEVERITY ORDER

```text
COR-A  THE PRIOR COMMISSION'S SINGLE-TYPE ERROR IS REPEATED IN A SUBTLER FORM.
       216 of 384 cells are graded in one band whose sole displayed reason quotes
       C-15's argument type, while the build's OWN table displays nine distinct
       clause types at C-6…C-14 and uses eight of them nowhere.
       DECIDING: STAGE8_THE_JOIN_O42SR_V001.md :1029-1044, against its own :363-373
       and STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md :146-149 (CS-5) and :154-156
       (CS-6).  Rows P-2×C-11, P-4×C-11, P-1×C-10 regraded INDETERMINATE-AT-BYTES.

COR-B  126 OF 384 CELLS CARRY A GRADE WITH NO DISPLAYED REASON AND NO DISPLAYED LOCUS,
       contrary to the build's own grading rule.
       DECIDING: STAGE8_THE_JOIN_O42SR_V001.md :1025-1032 (the band's stated column
       set) against :768-770 (the rule) and :1086-1092 (the tally).

COR-C  A CONSUMER THE BUILD MISSED, WHOSE PAIRING THE CORPUS ITSELF DISPLAYS.
       The formation ordering has a displayed argument type and the census pairs the
       carrier derivation's output to it and to B-5 on their input sides.
       DECIDING: STAGE8_INGREDIENT_CENSUS_O17SR_V001.md :457 and :749-752
       (and :799-801 for the second pairing; :221 for B-5's input side).
       Third missed consumer, inside the build's own declared scope and sealed ground:
       STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md :104-131.

COR-D  ONE OF THE TWO COMMISSIONED ROOTS WAS NEVER SWEPT, WHILE "EITHER ROOT" CLAIMS
       ARE MADE.  DECIDING: STAGE8_THE_JOIN_O42SR_V001.md :32-34 ("Two directories were
       used") against :806, :828-830, :1280.  MITIGATED: I ran it; Sweeps E and F are
       unchanged; residue is one file, STAGE8_AXN_DESCENT_INVERSE_CODEX2_V001.md
       :139-140, :182, which consumes Omega_c and does not touch the commissioned row.

COR-E  THE STATED FALLBACK FOR M-1 AND M-2 IS FALSE AT BYTES.  The producer artifact
       contains ZERO occurrences of "primitive record factor", so the two sides never
       share the noun and the grade DOES depend on the import.
       DECIDING: PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md (0 occurrences;
       its nouns are at :25-26, :32-34) against STAGE8_THE_JOIN_O42SR_V001.md
       :1471-1474.

COR-F  THE FROZEN-INVENTORY CORROBORATION OF C-1's ARGUMENT TYPE IS MISDESCRIBED.
       The column is "Presupposes", not "inputs", and the row does not type the cell
       as an interval — it repeats "unique physical record cell".
       DECIDING: STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md :142
       (header) and :149 (row) against STAGE8_THE_JOIN_O42SR_V001.md :224-228.

COR-G  CENSUS BAND 1.A IS DROPPED FROM THE PRODUCER SET WITHOUT A CHOICE-LEDGER ENTRY,
       though the producer count and hence the cell count depend on it.
       DECIDING: STAGE8_INGREDIENT_CENSUS_O17SR_V001.md :125-177 (A-1, A-2, A-3, A-5)
       against STAGE8_THE_JOIN_O42SR_V001.md :754-758 and :1080-1084.

COR-H  C-16's ARGUMENT TYPE IS OVERSTATED: "Every row's content is of one shape" is
       true of four of ten rows.
       DECIDING: STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md :130, :132, :135, :136,
       :137, :138 against STAGE8_THE_JOIN_O42SR_V001.md :394-395.

COR-I  M-5′ GRADES THE HALF-LINE AND CITES THE EXACT-JOINT POINT.  The forcing
       criterion names the marker/C_R case as EXACT JOINT DETERMINATION — the C_R = 1
       case whose admissible set is a point — not the C_R <= 1 half-line the build
       declares as the producer output at CH-4.
       DECIDING: STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md :55-56 against
       BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md :136-142 and
       STAGE8_THE_JOIN_O42SR_V001.md :1412-1418.  Q4 grade regraded
       INDETERMINATE-AT-BYTES.

COR-J  SWEEP-COUNT UNITS UNDECLARED; SEVEN OF TWENTY-THREE PATTERN COUNTS DO NOT
       REPRODUCE ON EITHER A FILE OR A LINE BASIS.  Eleven reproduce exactly.
       NO GRADE TURNS ON ANY OF THE SEVEN; the decisive sweeps D, E, F reproduce
       exactly.  DECIDING: STAGE8_THE_JOIN_O42SR_V001.md :100-132; table at §6.

COR-K  LINE-CITATION DRIFT, CONTENT-EXACT THROUGHOUT, no grade affected; and one
       added emphasis not marked as added (D01).
       DECIDING: examples at §6 COR-K.
```

---

## §10 — GRADES

```text
D1  CONSUMER ARGUMENT TYPES (Q1) ................. REFUTED
      The sixteen displayed types are accurate and the five-type headline is
      established.  But the consumer set is incomplete in a decision-relevant way
      (COR-C: three missed consumers, one of them paired to a carried producer on
      seal-verified bytes at census :749-752), C-16's type is overstated (COR-H),
      C-1's corroboration is misdescribed (COR-F), and a commissioned root was never
      swept while either-root claims were made (COR-D).

D2  PRODUCER OUTPUT TYPES (Q2) ................... CONFIRMED-WITH-CORRECTIONS
      Every producer row is carried at bytes; the four [NEW] producers are real and
      the structural finding about consumers' output sides is the artifact's best
      work.  Counts reproduced not inherited; both deduplications correct.
      COR-G: census band 1.A dropped undisclosed.

D3  THE MATRIX (Q3) .............................. REFUTED
      Five of eight MATCH cells survive a hard attack; two stand only on an import
      the artifact says they do not depend on (COR-E); the commissioned row's
      MISMATCH survives with one corroboration refuted (COR-F).  But 168 cells are
      asserted under a reason drawn from a different consumer's argument type
      (COR-A — the prior error, subtler) and 126 more under no reason at all
      (COR-B).  90 of 384 cells are carried at bytes.

D4  COMPOSITION (Q4) ............................. CONFIRMED-WITH-CORRECTIONS
      Every negative tested reproduces exactly over both roots; the contact test
      reproduces line for line; the COMPOSITION-BARRED proof/rule separation is
      correct at bytes and its headline survives.  The hunt for missed compositions
      was bounded by a consumer set the corpus's own census over-runs (COR-C), and
      M-5′ is regraded INDETERMINATE-AT-BYTES (COR-I).

D5  THE GAP (Q5) AND BARS ........................ CONFIRMED-WITH-CORRECTIONS
      Fences clean, no authoring, no advocacy, no re-read negatives, seals 36/36,
      self-exclusion held, own-draft defect declared, gap described and not filled.
      COR-J (sweep units and seven unreproducible counts) and COR-K (citation drift)
      are reporting defects, not evidentiary ones.

*** OVERALL: REFUTED ***
      Two dimensions refuted, including the heaviest.  The artifact's individual
      readings are honest and its quotations are exact — nothing here is a
      fabrication, a softened negative, or a filled gap, and several of its findings
      (the output sides of the two gate-consumers; the proof/rule split on the single
      bar; the PRIMARY correction to O36SR's forcing row) are real advances that
      survive attack.  But the object it was commissioned to build — a matrix of
      every producer against every consumer, each cell's grade carried at bytes — is
      not built.  Seventy-seven percent of its cells are asserted rather than
      displayed, and the specific failure the commission named in advance, grading
      against one argument type, recurs across fifty-six percent of them.  The
      corrected tally is at §7.
```

---

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
