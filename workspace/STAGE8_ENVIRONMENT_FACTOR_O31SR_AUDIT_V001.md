# STAGE 8 — ENVIRONMENT FACTOR O31SR — ADVERSARIAL AUDIT
# COMMISSION O31SR — ENVIRONMENT-AUDIT — 2026-08-15
# DEFAULT-REFUTE. TESTIMONY ZERO WEIGHT. RE-DERIVED AT BYTES.
# DETERMINATION ONLY. NOTHING CONSTRUCTED, NOTHING PROPOSED, NOTHING ADOPTED.

---

## §0 — STEP 0, SEALS, CUTOFF, PATTERNS

### §0.1 TARGET AND OUTPUT PROBE

```text
TARGET   /Users/bgm/MB Work/alpha-program-archive/workspace/
         STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md
SEAL     STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md.seal.sha256
VERIFY   `shasum -a 256 -c` run FROM THE ARTIFACT'S OWN DIRECTORY
         -> STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md: OK
SEALED SHA256
         c99759e075750fdf01df6c935dd0d705b6b15a38cdac26b7392c0b1995d5b0e4

MY OUTPUT PATH PROBED ABSENT BEFORE ANY WRITE:
  STAGE8_ENVIRONMENT_FACTOR_O31SR_AUDIT_V001.md
    -> No such file or directory
  STAGE8_ENVIRONMENT_FACTOR_O31SR_AUDIT_V001.md.seal.sha256
    -> No such file or directory
```

Step 0 passed. The target's bytes are the bytes the build sealed.

### §0.2 THE BUILD'S VERDICT IS NOT THE ONE THE COMMISSION ANTICIPATED

The audit commission names NAMED-ONLY as "the dramatic answer and therefore
the suspect one". The build did **not** return NAMED-ONLY. It returned
`*** SPECIFIED-NOT-CONSTRUCTED ***` (:1345) and expressly refutes NAMED-ONLY
at :1356-1359. The dramatic claim actually on offer is therefore the pair
**RUNG 5 EMPTY** (§3.1) and **RUNG 3 OCCUPIED** (§3.3/§4). Both were attacked
directly and are reported at §1 and §2 below.

### §0.3 MY SWEEP CUTOFF

```text
CUTOFF (newest file, either root, at my sweep time, by mtime):
  2026-08-15 18:36:48
  /Users/bgm/MB Work/alpha-program-archive/workspace/
    STAGE8_SIGN_AUDIT_O33SR_V001.md

ROOT 1 (primary) /Users/bgm/MB Work/alpha-program-archive/workspace
                 6870 files at my sweep time
ROOT 2           /Users/bgm/Documents/New project/
                 gravity_emergence_evidence_program/
                 alpha_fundamental_record_action_cleanroom_v003
                 7805 files
TOTAL            14675
```

ROOT 1 measured 6870 where the build measured 6867. The three-file delta is
the build's own two output files plus a concurrent sibling. **DISCLOSED AND
EXCLUDED FROM EVERY COMPLETENESS JUDGMENT AGAINST THE BUILD**: three sibling
commission artifacts post-date the target and could not have been read by it —
`STAGE8_BOUNDARY_SHAPES_O32SR_V001.md` (18:28:44),
`STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md` (18:34:15),
`STAGE8_SIGN_AUDIT_O33SR_V001.md` (18:36:48). They appear in my sweep output
and are never counted against the build.

### §0.4 SWEEP ENGINE

`/usr/bin/grep` (BSD grep 2.6.0-FreeBSD) by absolute path, as the build
declared and for the reason the build gave. I confirm the hazard is real and
add one of my own: **one of my early sweep pipelines returned 0 on a pattern
that in fact has 16 hits** (`BOX_environment`), through a shell-variable
expansion fault. I caught it by cross-checking a pattern whose hits I already
knew from quoted text. Every count below was re-run directly and any sweep
returning zero was independently falsified against a known-positive control.
This is recorded because an undetected silent-zero would have manufactured
exactly the "absence" this commission exists to test.

### §0.5 EVERY PATTERN I RAN, DECLARED VERBATIM

All recursive over BOTH roots, uncapped, no `head`, no result cap.

```text
A1  H_boundary|H_environment|H_env|H_E\b|boundary/environment|
    edge/environment|B_boundary                        (-rInE, and -rIlE)
E2  environment                                        (-rIni, uncapped)
X1  environment[^.]{0,60}(dimension|basis|algebra|state|spectrum|dynamics|
    Hamiltonian|Hilbert)[^.]{0,20}(is|=|:=)
X2  BOX_environment
X3  (exterior|complement|ambient|reservoir|surroundings)[^.]{0,40}
    (factor|sector|Hilbert|carrier)
X4  environment[^|]{0,80}(:=|is defined|we define|we construct|we exhibit|
    we take|is given by|is the space|is a Hilbert|has dimension|dimension |
    basis|spanned by|generated by)                     (over the Sense-A set)
X5  FAILS-AT                                           (-rIl)
X6  would-build:                                       (-rIn -A2, then env filter)
X7  MAJORANT_LEMMA0|MAJORANT_PHASE2                    (path resolution)

INFRASTRUCTURE FILTER applied to E2 before Sense-A work, declared verbatim:
  venv|os\.environ|PYTHONHASH|sympy|virtualenv|ignore_environment|
  environment_event_ledger|align\*|eqnarray|IEEEeqnarray|multiline_latex|
  fresh python|fresh venv|conda|pip |sys\.flags
```

### §0.6 STANDING ORDER HELD

No register, tracker, road, plan or continuation file was opened.
`STAGE8_DEMAND_REGISTER_O25SR_V001.md` and
`STAGE8_DEMAND_REGISTER_O25SR_AUDIT_V001.md` both surfaced in my X5 sweep and
**neither was opened**. No `Q-...` artifact was sought; `Q-733` is named
inside a quoted sentence at §1.2 below and was not chased:
EXPECTED-UNLOCATABLE, as commissioned.

### §0.7 EVERY SEAL I VERIFIED MYSELF, FROM THE ARTIFACT'S OWN DIRECTORY

Testimony carried zero weight: every seal the build asserted was re-run by me,
plus every seal for sources the build did not cite.

```text
SIDECAR (NAME.md.seal.sha256 or NAME.seal.sha256)                   RESULT
STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md                             OK
PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002 OK
R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_SPEC_V001.md                  OK
BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md                OK
STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md              OK
FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md             OK
CAUSAL_DIRECT_LIMIT_ARCHITECTURE_TEST_SPEC_V001.md                  OK
STAGE8_AXN_BUILD_SKELETON_V2_DARIO_V001.md                          OK
STAGE8_AXN_BUILD_SKELETON_CODEX2_V001.md                            OK
STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md                           OK
STAGE8_TASK2F_..._SRE_BRIDGE_BUILD_DETERMINATION_V001.md            OK
STAGE8_COMPLETION_MAP_T17SR_V001.md                                 OK
STAGE8_EARN_THE_TRACE_S9AD_V001.md                                  OK
STAGE8_SEAM10_NARROWING_ADVERSARIAL_RECOMPUTATION_V001.md           OK
STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md                        OK
STAGE8_BOUNDARY_FORCING_CHARACTERIZATION_FABLE_V001.md              OK
COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_SPEC_V001.md      OK
COMPLETE_QSPEC_CANONICAL_FULL_ZERO_FREE_PROMOTION_SPEC_V001.md      OK
STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md                   OK
STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md     OK
STAGE8_GAMMA_RECORD_MATTER_GRAVITY_BARREDNESS_KT4_DETERMINATION_V001 OK
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md                        OK
MAJORANT_LEMMA0_PROOF_DRAFT_V001.md                                 OK
MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md                           OK   *NEW*
AXN_BUILD_CHARTER_V001.md                                           OK   *NEW*
STAGE8_C_R_SELECTOR_S4_REFERENCE_CO_PARTICIPATION_SPEC_V001.md      OK   *NEW*
STAGE8_TASK3D_COUPLING_RAY_FORCING_PROTOCOL_AND_COVERAGE_FAILURE_V001 OK *NEW*
STAGE8_BRIDGE_ITEM4_SIX_OBJECT_DEPTH_REPORT_V001.md                 OK   *NEW*

BUNDLE SEALS
R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_V001.seal.sha256   all 5 lines OK,
  RESULT_V001.md included -> the build's "all 5 bundle lines OK" CONFIRMED
FORK_8_CAUSAL_DIRECT_LIMIT_PROMOTION_V001.seal.sha256    all 10 lines OK,
  CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md included  *SEE F-2*
CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001.md      BUNDLE OK

UNSEALED, CONFIRMED UNSEALED BY ME
BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md
  NO SIDECAR OF EITHER CONVENTION, NO BUNDLE LINE.  The build's disclosure
  of this file's unsealed status is ACCURATE and was made at every use.
```

---

## §1 — HUNT (A): A CONSTRUCTION MISSED

### §1.1 THE ATTACK, AND ITS RESULT

I re-ran the construction hunt uncapped over both roots with my own wider
vocabulary (A1, X1, X3, X4 at §0.5), including exterior/complement/ambient/
reservoir/surroundings and the factor's own symbol. The decisive sweep is X4,
run over the whole infrastructure-filtered Sense-A set with twelve definitional
verbs and constructions rather than the build's five patterns.

```text
X4 OVER THE SENSE-A SET, BOTH ROOTS, UNCAPPED — TOTAL DISTINCT HITS: 1

COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_SPEC_V001.md:86
  "The composite record environment has dimension nine."
```

That is the single hit, and it is the one the build itself located and
disqualified at its §3.4. I re-derived the disqualification at bytes: the same
file's frozen-constants block carries, verbatim at :33,
`two-cell record outcomes  = 9`, and :86-87 continues "The Hilbert-Schmidt
partial-trace bound therefore gives". The noun is the traced-out factor in a
partial-trace bound, and the number is the record's own outcome count.
**The build's disqualification is CONFIRMED at bytes.**

*** SO THE DRAMATIC CLAIM SURVIVES THE ATTACK. Across both roots, by patterns
    wider than the build's, RUNG 5 IS EMPTY. No sentence in either root
    defines the third factor, states its dimension, its algebra, its state or
    its dynamics. I re-derived this independently and I confirm it. ***

But the sweeps that support it were narrower than the claim they carry, and
they passed over one artifact. That is F-6 and §1.2.

### §1.2 WHAT THE BUILD PASSED OVER — A FOURTH RUNG-4 OBJECT

`MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md` — **SIDECAR VERIFIED OK BY ME** —
is never named, quoted or counted anywhere in the target. It carries the
environment factor symbol. Its :53-58, quoted whole to the end of its sentence:

```text
2. the telescoping identity Lemma 0(a)-(d) as an EXACT matrix identity on
   an explicit two-cell relayed model on the full 54-dimensional space
   `F (x) H_R^(1) (x) H_E^(1) (x) H_R^(2)` (F = C^2; Gamma = identity
   functor, which satisfies (S3); one-particle symbols chosen exactly
   unitary over Q(i, sqrt2) and NON-commuting): the chain compression
   equals the relay-ordered product `K^(2) K^(1)` exactly;
```

```text
WHAT THIS IS:  the environment factor placed as a named slot in an EXPLICIT
  FINITE-DIMENSIONAL MODEL whose TOTAL dimension the corpus states outright,
  and on which an exact matrix identity is then verified in exact arithmetic.
  It is the most concretely realized environment slot in either root.

WHY IT DOES NOT INVERT THE VERDICT — BOTH REASONS AT BYTES:
  (i)  The file's own :3, verbatim: "Date: 2026-07-26. Status: DRAFT — NOT
       SEALED, NOT A RESULT ARTIFACT."  Self-disclaimed, exactly as its
       Phase-1 sibling is.
  (ii) `H_E^(c)` is glossed by the governing Phase-1 draft as the
       "(relayed-record) copy".  It is the record again, not a third system.
  By the build's OWN rung definitions this lands on RUNG 4, not RUNG 5.

WHAT I DID NOT DO:  the corpus states the TOTAL space's dimension and the
  other three factors.  It does not state the environment factor's dimension.
  I DID NOT SOLVE FOR IT.  Deriving a dimension for the third factor from the
  total and the complement would be supplying part of the factor, which this
  commission forbids.  No dimension for the factor is emitted here.
```

**CORRECTION C-1.** The build's `partial_constructions_or_surrogates_located
= 3` and its §3.6 rung ledger entry "RUNG 4 ... THREE" are UNDERCOUNTS. A
fourth object exists, in a file the build never opened. The build's §3.2
sentence "This is the closest thing in either root to a built environment"
(of `H_E^(c)` in the Phase-1 draft) remains true of the symbol but was written
without knowledge that the same symbol is carried into an explicit
finite-dimensional model in the Phase-2 draft.

**CORRECTION C-2.** The build's §3.2(4b) reports of `H_E^(c)` only "a named
factor, a home in the total space, and an exact isometry landing in it". At
bytes the Phase-1 draft gives it more: a fixed orthonormal-basis map
`h -> e_(c,h)` (:102-103), a commutation law — (S2) at :112-115, "every
operator of a later cell commutes exactly with the environment copies and
pointer observables of earlier cells" — and a bra `<e_(c,pointer)|` that reads
the record out of it (:129-133). The strongest object in the corpus is
stronger than the build displayed. This cuts AGAINST the build's own thesis
and it is recorded for that reason. It remains disclaimed on both grounds.

### §1.3 A NAMING OF HIGHEST GOVERNANCE RANK, ABSENT FROM DELIVERABLE 1

Deliverable 1 is "EVERY NAMING, GATHERED AND TYPED". The build's §3.5 quotes
two artifacts that speak of "**the charter's** four-sector phrase" and "**the
charter's** environment sector" — and never locates, quotes, seals or types
the charter itself. It exists, in ROOT 2, and I verified its seal:

`AXN_BUILD_CHARTER_V001.md` — **SEAL OK** — :3-4, quoted whole to the end of
its sentence:

```text
The derivation of the complete compact source/gauge/gravity/environment action —
the program's one remaining construction (Q-733), the node every road converges on.
```

```text
ROLE-TYPE:  INDEX IN A COMPOUND SECTOR NAME — the build's own census class —
            but borne by THE GOVERNING CHARTER, which types the action
            containing this sector as "the program's ONE REMAINING
            CONSTRUCTION".  This is the highest-standing naming of the factor
            in either root, and Deliverable 1 does not contain it.
```

**CORRECTION C-3.** Deliverable 1 is incomplete: the charter naming is
missing. The omission does not change the type — it is a compound-index
naming, the class the build already counts at roughly 90 lines — but it is the
naming from which the §3.5 "declined attempt" chain hangs, and the build
quoted the answers to the charter without ever quoting the charter.

---

## §2 — HUNT (B): A NAMING COUNTED AS A SPECIFICATION

The build claims `buildability_condition_sites = 3` and rests its verdict on
them. I checked each against its own sentence.

### §2.1 SITE 1 — THE SIX-ITEM REOPEN CONDITION: **HOLDS, AT BYTES**

`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md` :141-155. I dumped
the range and compared character by character against the build's quotation.
**IT IS QUOTED WHOLE AND EXACT**, heading, six numbered items, and the closing
failure test. The build's §4.1 is accurate.

```text
IS IT A SPECIFICATION?  YES, ON ITS OWN SENTENCE.  The governing verb is
  "The next constructive gate MUST DERIVE, without alpha or mass input:"
  followed by six numbered obligations, closed by a stated failure test at
  :153-155 ("If the constrained coefficient space has dimension greater than
  one, or if an independent rescaling leaves either output deformable, the
  route remains blocked.").  That is a checkable predicate, not a wish.
IS ITEM 1 THE FACTOR?  Item 1 is "the physical edge/environment degrees
  required by durable closure".  It is the compound token.  The build's own
  §2.4 establishes that no sealed sentence anywhere separates the environment
  from the edge.  Reading item 1 as the third factor is therefore a reading
  of a compound, and the build should have said so here as it said so there.
  I do not disturb the reading; I record that it rests on the compound.
```

**SITE 1 CONFIRMED.** This site alone is sufficient to occupy RUNG 3.

### §2.2 SITE 2 — THE SRE SIGNATURE: **A SPECIFICATION OF THE DISCLAIMED
SURROGATE, PRESENTED AS A SPECIFICATION OF THE FACTOR**

The quotation itself is exact: `STAGE8_TASK2F_..._DETERMINATION_V001.md`
:398-415 is reproduced whole and correctly, as are :417-426 and :429-437.
The defect is not in the bytes quoted. It is in what the object is.

```text
THE OBJECT'S OWN DOMAIN, :404, verbatim:
  "End(C^2_S tensor C^2_R tensor C^2_E), with its declared Pauli grading"

THE OBJECT'S OWN REQUIRED COMPONENT, :413, verbatim:
  "proof that E retains the edge/witness role"
```

`C^2_E` is the E-slot of the reduced three-factor ansatz — the very object the
build's own §7 TOY_SEPARATION item 1 calls "`C^2_edge/witness` — a
TWO-DIMENSIONAL STAND-IN ... **A toy in the strict sense. NOT PROMOTED
HERE.**", on the strength of its own file's sealed disclaimer that "a genuine
environment spectrum remain[s] outside it". The role the SRE signature
requires be PRESERVED is, in its own words, the **edge/witness** role.

*** SO THE BUILD DECLARES `C^2_E` A TOY IN §7 AND THEN COUNTS A SPECIFICATION
    OF `C^2_E`'S EMBEDDING AS ONE OF THREE SETS OF BUILDABILITY CONDITIONS
    FOR THE PHYSICAL THIRD FACTOR IN §4.2, WITHOUT DISCLOSING THE TENSION IN
    EITHER PLACE. That is a surrogate's specification promoted to the
    factor's. ***

In fairness, and displayed rather than suppressed: the same file's own
alternative table at :423 labels the row "B3: new environment factor" for
"enlarge C0 by an explicit `C^2_E` factor", so the corpus does itself connect
adding `C^2_E` with an environment factor; and :391-393 states that "The
complete edge/environment carrier is one of the governing gate's explicit
reopen requirements". The connection is real. What is not sustained is
counting the SRE certificates as conditions on constructing the third factor
without recording that the object they certify is the disclaimed stand-in and
that the role they preserve is edge/witness.

**CORRECTION C-4.** Site 2 is a specification of the reduced surrogate's
realization embedding, not of the physical third factor, and the build's §4.2
finding ("A construction of the factor would have to carry: ...") overstates
its own bytes. The TYPE-C / principal-ratification observation the build draws
from :429-433 is accurate as to the flag and is not disturbed.

### §2.3 SITE 3 — THE CLOSURE DISJUNCTION: **AN OBLIGATION, NOT A CONDITION SET**

`STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md:63`, at bytes, is a five-cell table
row. Its operative text states two discharge routes — bind a contribution
already derivable from named stock, **or** prove exhaustion. It states what
must be *done*, not what a construction must *satisfy*. Measured against the
build's own RUNG 3 definition ("What a construction would have to satisfy is
stated as a checkable obligation list, signature, or disjunction"), it
qualifies only because that definition admits "or disjunction". It carries no
certificates, no signature, and no failure test.

**CORRECTION C-5.** Site 3 is the weakest of the three and sits at the
RUNG 2 / RUNG 3 boundary on the build's own definitions.

### §2.4 THE NET EFFECT ON THE VERDICT

```text
SITE 1  the six-item reopen condition   SPECIFICATION.  CONFIRMED AT BYTES.
SITE 2  the SRE signature               SPECIFICATION OF THE SURROGATE.
SITE 3  the closure disjunction         OBLIGATION / BOUNDARY CASE.
```

RUNG 3 remains **occupied** — Site 1 alone occupies it, quoted whole, sealed,
and independently verified by me. The verdict SPECIFIED-NOT-CONSTRUCTED
therefore stands. The count of three does not.

---

## §3 — HUNT (C): THE ROUTING TEST, RE-DERIVED FROM EACH OPERATIVE SENTENCE

This deliverable decides whether the absence bears on the standing
obstructions at all. I re-derived all six calls from the demands' own bytes.

### §3.1 THE FRAMING MEASUREMENTS, RE-RUN

```text
/usr/bin/grep -ic environment                                     RE-MEASURED
STAGE8_COMPLETION_MAP_T17SR_V001.md                     0   CONFIRMED
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md               0   CONFIRMED
STAGE8_STRATIFICATION_O27SR_V001.md                     0   CONFIRMED
STAGE8_STRATIFICATION_O27SR_AUDIT_V001.md               1   CONFIRMED, and
  the single hit is :765 "ENVIRONMENT   fresh python venv created this
  session; sympy 1.14.0." — SENSE-B, not physics.  CONFIRMED.
STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md            0   CONFIRMED
  (the build's ACCOUNT 6 "NOT A NAMING" marking is sound at bytes)
```

**CORRECTION C-6 — A DECLARED SWEEP COUNT IS WRONG BY A FACTOR OF SIX.**
The build's §5.0 states "of the **12** artifacts in either root containing
`FAILS-AT`". Re-measured with `/usr/bin/grep -rIl "FAILS-AT"` over both roots:
**70 paths, 70 distinct basenames, all `.md`.** Not twelve.

I then tested the build's substantive claim across all 70. Nine artifacts
besides the target carry an `environment` token; I opened every one of the
nine and every hit is SENSE-B or SENSE-D — "fresh venv", "fresh python venv",
"independently created environments", "auditor's own fresh environment".
Exactly one FAILS-AT artifact carries a Sense-A token:
`STAGE8_EARN_THE_TRACE_S9AD_V001.md`, and I confirm its count of 5 at
:118, :348, :417, :456, :574.

*** THE BUILD'S CONCLUSION SURVIVES AND IS STRENGTHENED — one in seventy, not
    one in twelve — BUT A DECLARED MEASUREMENT IN A DECLARED SWEEP WAS WRONG,
    AND UNDER DEFAULT-REFUTE THAT IS A CORRECTION WHETHER OR NOT IT HELPED. ***

### §3.2 THE SIX CALLS, RE-DERIVED

```text
ITEM  OPERATIVE SENTENCE RE-READ AT BYTES         MY CALL   BUILD'S   AGREE?
--------------------------------------------------------------------------
FP-1  COMPLETION_MAP :81-85 — a rate converting
      operator grade to trace/HS grade, one
      carrier, one operator, arithmetic ceiling   INTERNAL  INTERNAL   YES
FP-2  COMPLETION_MAP :91-98 — a |x-y|^-3
      majorant at the VOLUME DIAGONAL x = y       INTERNAL  INTERNAL   YES*
FP-3  COMPLETION_MAP :102-106 — a C-L2
      quadratic-form certification of the
      error layer; [h_0, 1_B] is a commutator
      with the record's own sharp projector       INTERNAL  INTERNAL   YES
W-1   COMPLETION_MAP :324-329 — a TOPOLOGY on
      dressed composites with continuity proofs
      for four named localization maps            INTERNAL  INTERNAL   YES
W-2   COMPLETION_MAP :330-334 — a prequotient
      rule evaluated on a named oriented k-cell   INTERNAL  INTERNAL   YES
W-3   COMPLETION_MAP :335-339 — C-L2
      certification at the consuming (HS) type    INTERNAL  INTERNAL   YES
--------------------------------------------------------------------------
                        6 of 6 INTERNAL.  0 of 6 ROUTE THROUGH THE
                        ENVIRONMENT SIDE.  RE-DERIVED AND CONFIRMED.
```

W-1, W-2 and W-3 are quoted by the build **whole and exact** from :324-339;
I compared them line by line and found no alteration.

### §3.3 *THE ONE ROUTING CALL WHOSE STATED REASON DOES NOT HOLD

The FP-2 **answer** is right. The **reason** the build gives is not, and it is
wrong in a way the build itself forbids elsewhere.

At FP-3 the build lays down the governing distinction, in its own words at
:1022-1023: "bearing on a boundary is not bearing on H_boundary/environment."
It applies that distinction rigorously at FP-3, and discloses the
qualification in its ledger at D-7.

At FP-2 it abandons it. Its §5.1 FP-2 reads: "A boundary/environment relation
is not merely absent from FP-2 — it is AFFIRMATIVELY EXCLUDED as insufficient
by a sealed adopted ruling." But the sealed adopted ruling it cites excludes
**boundary smoothing** and **boundary-supported structure** — not a relation
to the named environment factor. That is precisely the identification the
build rules impermissible one subsection later.

**CORRECTION C-7.** The FP-2 routing call is CONFIRMED as INTERNAL; the stated
ground "AFFIRMATIVELY EXCLUDED" is an over-claim that uses the boundary /
environment-factor conflation the build forbids at FP-3. The correct ground is
the weaker and sufficient one: FP-2's demanded quantity is a coincidence-locus
majorant inside the volume, and no second system enters the demand.

**CORRECTION C-8 — A SELECTION INSIDE FP-2.** The FP-2 section of the
completion map contains two quoted verdicts. The build quoted the second
(:96-98) and called it "Operative sentence", passing over the first, which is
the block labelled at bytes "WBC verdict, verbatim" at :91-95 — the same label
whose block the build DID quote for FP-1 and FP-3. The passed-over block opens,
verbatim at :91: "every closure decay asset is boundary-supported". That is the
most boundary-flavoured sentence in the wall and it was the one not displayed
in a deliverable about whether the boundary side bears on the wall. Re-read in
full, it does not change the call — it says the boundary-supported assets are
the ones that do NOT reach the volume diagonal — but the selection was
inconsistent with the build's own practice at FP-1 and FP-3 and is corrected
here rather than left standing.

### §3.4 §5.4 — WHERE THE FACTOR IS LIVE — CONFIRMED

`STAGE8_EARN_THE_TRACE_S9AD_V001.md` :455-458 and :348-350 are both quoted
**whole and exact**; I verified each against bytes. The build's finding that
the compound "source-record-environment action" is a named demanded object on
the holonomy/phase-side thread, and not at FP-1/2/3 or W-1/2/3, is CONFIRMED.
Displaying this — a place where the absence IS live — cut against the build's
own headline and it displayed it anyway. Recorded to its credit.

---

## §4 — HUNT (D): THE DISAGREEMENT SMOOTHED

### §4.1 THE PRIOR FINDINGS, CHECKED AT BYTES

```text
O30SR §3.5 heading, at bytes :533, verbatim:
  "### §3.5 AND THE NAME IS NOT STABLE — THREE DIFFERENT SECOND SIDES"
  ANSWER 1 :539, ANSWER 2 :543, ANSWER 3 :553.       THREE — CONFIRMED.

O30SR AUDIT COR-7 heading, at bytes :415, verbatim:
  "### COR-7 — TWO OF THE THREE "SECOND SIDES" ARE READINGS, NOT SENTENCES"
  and its flag at :558, verbatim:
  "boundary_second_side_carried_by_a_sentence            = 1 of 3 (COR-7)"
                                                      CONFIRMED.
```

The build's characterization of both prior findings is accurate at bytes.

### §4.2 WAS THE DISAGREEMENT DISPLAYED OR RECONCILED?

**DISPLAYED.** The build raises three accounts to six, states each with date,
seal state and bearer, marks ACCOUNT 6 "NOT A NAMING" rather than dropping it,
and closes at :474 "I reconcile nothing and supply nothing." I verified the
load-bearing bytes of the accounts it could most easily have smoothed:

```text
ACCT 3  REDUNDANT_RECORD_RESULT (BUNDLE OK) :18 verbatim "The two conditional
        environment sectors are exactly orthogonal for every `N>=1`." and
        :14-15 the copy map |h>_source |0>^tensor_N -> |h>_source |h>^tensor_N.
        The build's reading — the sectors ARE the record's copies — CONFIRMED.
ACCT 4  RECORD_PRINCIPLE_V001 :32-34 quoted whole and EXACT.  CONFIRMED.
ACCT 5  BOUNDARY_FORCING_FABLE (SEAL OK) :127-128 quoted EXACT.  CONFIRMED.
ACCT 6  R2_KAPPA_N_DETERMINATION carries ZERO occurrences of the token,
        re-measured by me.  The "NOT A NAMING" marking is CORRECT, and
        retaining the account rather than pruning it was the right call.
```

No two accounts were merged. Accounts 1 and 2 share a date, accounts 3 and 4
share a family, and all four are kept distinct with their differences shown.

### §4.3 ONE ACCOUNT NOT LISTED AMONG THE ACCOUNTS

`MAJORANT_LEMMA0_PROOF_DRAFT_V001.md` :86-87 glosses `H_E^(c)` as "the c-th
environment (relayed-record) copy" — a per-cell factor with a home in a total
space, an isometry into it, and a commutation law. That is a distinct account
of what the environment *is*, and it differs from ACCOUNT 3 (N copies of one
record state) in structure. The build treats it at §3.2(4b), §3.4 and §7, but
does not list it among the six accounts in §2.5, whose stated subject is "WHAT
THE ENVIRONMENT IS SAID TO BE".

**CORRECTION C-9.** A seventh account exists and is treated elsewhere in the
artifact but is absent from the account census. This is a placement defect,
not a merge: nothing was reconciled away, and the build's §3.4 pattern finding
("the thing exhibited turns out to be THE RECORD UNDER A SECOND NAME") in fact
depends on it. **NO SMOOTHING FOUND.**

---

## §5 — HUNT (E): ANY CONSTRUCTION BY THE BUILD ITSELF

I read the target end to end looking for the build supplying the factor or any
part of it. The three places where a formula appears in the artifact:

```text
:228-232  `B_parent = B_Dirac(g,A) + sum_c B_c + B_boundary/environment(g,A,
          record).`  — QUOTED CORPUS CONTENT, from the UNSEALED BID file,
          carried with its unsealed status at every use.  DISPLAY.
:626      `rho_5(E) = exp(-E), A_5(t) = (1 + i t)^(-1).` — QUOTED CORPUS
          CONTENT under its own seal, displayed with its own file's
          prohibition ("must not be promoted to a microscopic law"),
          disclosed at ledger D-6, fenced at TOY_SEPARATION item 3.  DISPLAY.
:648      `|<E_0^(N)|E_1^(N)>|=|gamma|^N` — QUOTED CORPUS CONTENT under a
          verified bundle seal.  DISPLAY.
```

```text
NO DIMENSION, ALGEBRA, STATE, DYNAMICS, TERM, FORMULA, CANDIDATE OR PROPOSAL
IS SUPPLIED BY THE BUILD FOR THE THIRD FACTOR.
No account is reconciled.  No alternative among TASK2F's four is selected.
No value and no program quantity is emitted.
The four senses are separated and the toys are named and fenced.
FINDING: NO CONSTRUCTION, NO PARTIAL CONSTRUCTION, AND NO SUPPLY BY THE BUILD.
The artifact is NOT VOIDED on ground (E).
```

The nearest approach is the verbatim display of `rho_5(E) = exp(-E)`. It is
corpus content, it is disclaimed in its own sealed file, and the build
disclosed the choice to display it and its reason. I hold that lawful under a
determination that is required to display the strongest object located.

---

## §6 — HUNT (F): QUOTATION INTEGRITY TO SPAN END, AND EVERY SEAL CLAIM

I dumped the cited byte range for every quotation in the target and compared
it character by character to the quoted text. Twenty-six quotations checked.

### §6.1 QUOTED WHOLE AND EXACT — VERIFIED

```text
PRIMITIVE_..._V002 :27-35 (the three-factor statement)              EXACT
R3_4_..._SPEC_V001 item 4                                           EXACT
R3_4_..._RESULT_V001 item 4, under the heading verified at :89      EXACT
BID_..._V001 :132-143 (the only signature the factor has)           EXACT
BOUNDARY_..._GATE_V003 :164                                         EXACT
CROSSCHECK_CODEX2 :211-213, :247, :209                              EXACT
FULL_CARRIER_..._GATE_V001 :15-33 (the C^2_edge/witness block)      EXACT
FULL_CARRIER_..._GATE_V001 :141-155 (the reopen condition)          EXACT
FULL_CARRIER_..._GATE_V001 :127-135 (the Result)                    EXACT
FULL_CARRIER_..._GATE_V001 :160-161 (the two flags)                 EXACT
ARCHITECTURE_TEST_SPEC :74-84 (the rho_5 block)                     EXACT
SKELETON_V2_DARIO :51-56 (the declined attempt)                     EXACT
SKELETON_CODEX2 :66-69 (the argument deferred to)                   EXACT
TASK2F :398-415, :417-426, :423, :429-437                           EXACT
COMPLETION_MAP :96-98 (FP-2), :324-339 (W-1, W-2, W-3)              EXACT
EARN_THE_TRACE :455-458, :348-350                                   EXACT
QSPEC PERIODIC :86 and frozen constants; QSPEC CANONICAL :146       EXACT
REDUNDANT_RECORD_RESULT :14-15, :18, :25                            EXACT
BOUNDARY_FORCING_FABLE :127-128                                     EXACT
RECORD_PRINCIPLE_V001 :32-34                                        EXACT
LOAD_BEARING_HOLONOMY :291-293; GAMMA_..._KT4 :382-383              EXACT
```

*** ON THE CENTRAL QUESTION THE BUILD IS HONEST. Every load-bearing quotation
    — the founding three-factor statement, the reopen condition, the SRE
    signature, the declined attempt, the three demands — is reproduced whole
    and unaltered. No favourable clause was cut from any of them. ***

Four defects were found, and they are reported without softening.

### §6.2 F-1 — MATERIAL ELIDED FROM INSIDE A CITED SPAN (BID :145-147)

The build cites `:145-147` and quotes:

```text
"Its overlap terms, ordering, preparation, and connected
 cumulants are outputs of that action."
```

The bytes at :145-147 continue past that full stop, verbatim:

```text
Its overlap terms, ordering, preparation, and connected cumulants are outputs
of that action. They are not inferred from the one-cell incidence matrix and
may not be set to zero merely to preserve a preferred result.
```

The elided sentence is normative and it cuts against the use the build makes
of the span. The build offers :145-147 as a sentence that "**withdraws** any
content it might have seemed to carry". The elided half says the opposite in
kind: the term's outputs "may not be set to zero merely to preserve a
preferred result" — the corpus treating the term as load-bearing, not vacuous.
**This is the one elision in the artifact that removes material qualifying the
build's own reading, and it is corrected here.**

### §6.3 F-2 — A SEAL CLAIM CARRIED BY THE WRONG FILE

The build's §2.5 ACCOUNT 4 carries seal state `OK` and the parenthetical
"(V002 sealed OK)", and its §3.2(4c) writes "CAUSAL_DIRECT_LIMIT_RECORD_
PRINCIPLE_V001 :32-34 (V002 SEAL OK)". **The quoted sentence is in V001. The
seal verified and reported is V002's.** V002 is a different file with
different content — its :32-36 is a Status flag block, not the quoted passage.

I verified the quoted file independently:

```text
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md
  sidecar of either convention              ABSENT
  bundle line in FORK_8_CAUSAL_DIRECT_LIMIT_PROMOTION_V001.seal.sha256
                                            PRESENT — all 10 lines OK
  -> V001 IS SEALED.
```

**The sentence is sealed and the account stands** — but the warrant the build
printed was for a different artifact, and only my independent bundle check
rescues it. Under a standard where twelve builds today mislabelled seal states,
a seal claim discharged against the wrong file is a defect regardless of
outcome.

### §6.4 F-3 — A QUOTATION ENDING MID-PHRASE ON A DANGLING SLASH

§4.4, in a block the build titles "THE WOULD-BUILD NOTES (RUNG 2), **COMPLETE**",
cites `STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md :234-238` and ends
its quotation:

```text
                 projective path and therefore attain the Fubini-Study /
```

The bytes run :235-240, and :240 completes the phrase: `Mandelstam-Tamm lower
bound`. The quotation stops on a dangling solidus mid-name, and the cited
range :234-238 does not even cover the text quoted (which reaches :239). The
elided words are a bound's name and change nothing substantively.

### §6.5 F-4 — "QUOTED WHOLE" APPLIED TO PARTIAL EXTRACTS, AND LOOSE RANGES

A recurring pattern rather than a single defect. In each case the *content*
quoted is exact; the *label* or the *range* is not.

```text
TASK2F :390-391 labelled "quoted whole" — line 391 continues past the quote
  with "The complete edge/environment carrier is one of the governing gate's
  explicit reopen requirements".  (The build does quote this material
  separately at its §4.1, so nothing is concealed.)
PACKAGE_V3 :63 labelled "quoted whole" — the bytes are a five-cell table row;
  one cell is quoted.  The build also silently capitalizes "either" -> "Either",
  inserts a comma into "causal-cell and P5 stock", and drops the row's own
  lead-in "**A DISJUNCTION:**" and its supplier-status cell ("**none.**
  Absence of a ninth independently named field is *not* an exhaustion theorem
  — that is the gap").  The dropped cell's content is carried by the build
  elsewhere via CROSSCHECK :247, so again nothing is concealed.
COMPLETION_MAP :81-85 (FP-1) labelled "quoted whole" — :85 continues
  `MO-MAP at the same bytes: "F1 = MO-3 exactly."`
COMPLETION_MAP :102-106 (FP-3) labelled "quoted whole" — :106 continues with
  the fire-time sharpening, which contains the FAVOURABLE clause "form bound
  CERTIFIED exactly".  This is the class of elision the commission flagged;
  it does not alter the FP-3 routing call, which turns on the C-L2 layer.
MAJORANT_LEMMA0 ":99-104" — content begins :100 and stops mid-:104, eliding
  "iota_1(psi) = psi tensor |r_1> is the tensor-form ready-root supply".
ONSET_SAT ":23-25" — the sentence ends mid-:24; the build's narrower claim
  ("quoted whole to the end of its sentence") is TRUE, the range is loose.
Minor range slips also at PRIMITIVE :44-47, FABLE :127, TASK2F :392-393.
```

**CORRECTION C-10.** The build's "quoted whole" is used in two different
senses — whole-to-sentence-end and whole-to-span-end — and several spans
satisfy only the first. No instance was found where the elided material
reverses the build's finding, and one instance (F-1) removes material that
qualifies it.

### §6.6 F-5 — THE IMPORT AUDIT'S INTEGER INVENTORY IS NOT FAITHFUL

§8 declares "Integers appearing in this artifact (9, 16, 2, 64, 70, 5, 6,
4n^3, and the sweep counts)". Re-measured: `16`, `64` and `70` each occur
exactly **once** in the whole artifact — inside that declaration itself. They
appear nowhere in the body. The inventory lists integers the artifact does not
use. Harmless to the fences, but it is not the faithful inventory it claims.

### §6.7 F-6 — THE ABSENCE CLAIM IS BROADER THAN THE SWEEP BEHIND IT

§3.1 asserts "ACROSS 14672 FILES IN BOTH ROOTS THERE IS NO SENTENCE THAT
DEFINES H_boundary/environment, STATES ITS DIMENSION, ..." on the strength of
five patterns, all of which require the factor symbol to stand immediately
next to a copula or the word `dim`. None of them can see a factor whose
dimension is fixed by the stated dimension of a product space — which is
exactly the form the Phase-2 draft uses (§1.2). The claim as written survives
literally, because no sentence states the factor's own dimension. It survives
narrowly, and by luck rather than by the sweep's reach.

---

## §7 — A COMPLETENESS CLAIM THAT FAILS: §4.4 IS NOT COMPLETE

§4.4 is headed "THE WOULD-BUILD NOTES (RUNG 2), **COMPLETE**" and states
"Every would-build note in either root that names the factor." It lists three.

Sweep X6 (`would-build:` with two lines of trailing context, environment
filter, both roots, uncapped) returns further would-build notes in **sealed**
source artifacts that name the factor. Three verified at bytes, seals checked
by me, quoted whole:

```text
STAGE8_C_R_SELECTOR_S4_REFERENCE_CO_PARTICIPATION_SPEC_V001.md — SEAL OK
:776-779
  S4_complete_sector_and_support_inventory_derived = false | TYPE-U |
    would-build: enumerate every record, source, vacuum, binding, boundary,
    edge, environment, and other physical contribution admitted by S1, with its
    action-difference and closure support roles

STAGE8_BRIDGE_ITEM4_SIX_OBJECT_DEPTH_REPORT_V001.md — SEAL OK :188-191
  S4_terminates_in_derived_ground = false | TYPE-U
  would-build: fixed reference subtraction plus theorem excluding spectator,
               vacuum, binding, edge, and environment energy unless it also
               enters the record action difference

STAGE8_TASK3D_COUPLING_RAY_FORCING_PROTOCOL_AND_COVERAGE_FAILURE_V001.md
— SEAL OK :272-274
  same_plane_grading_law_equivalence_derived = false | TYPE-U |
    would-build: a complete-carrier physical transformation theorem preserving
                 source, record, environment, grading, and response meanings
```

Two further environment-naming would-build contexts sit in
`STAGE8_C_R_SELECTOR_NONSELECTION_DIAGNOSIS_V001.md` and
`STAGE8_SEAM10_NARROWING_ADVERSARIAL_RECOMPUTATION_V001.md`.

**CORRECTION C-11.** §4.4's completeness claim is FALSE at bytes. At least
three further sealed would-build notes name the factor. These are not trivia:
the BRIDGE_ITEM4 note is a **theorem EXCLUDING environment energy** and the
C_R_SELECTOR note is an **enumeration of every contribution including the
environment** — both bear directly on the exhaustion branch of the closure
disjunction the build reports at §4.3 as the discharge route requiring no
construction. Displaying them would have strengthened the build's own §4.5
finding that the hole is well specified.

---

## §8 — HUNT (G): LENS TOKENS AND PROGRAM IMPORTS

```text
LENS TOKENS in the target: /usr/bin/grep -icE "\blens\b"        = 0
  no "lens", no "lensing", no "through the lens".               CLEAN.

PROGRAM NUMBERS: grep for 137 | 0\.0072 | 1/137 | fine.structure |
  alpha *= | coupling *=                                        = 0 hits
  No measured constant, no comparison to one, no coupling value. CLEAN.

FENCES, re-read in the target's own bytes at :1336-1337:
  alpha_computed = false;  proof_authorized = false;
  kappa_record_computed = false.                                HELD.
```

Every integer surviving in the body is either quoted corpus content displayed
under a seal I verified, or a count of lines and files from a declared sweep.
No number in the target functions as a program quantity. **(G) CLEAN**, save
the inventory inaccuracy at F-5.

---

## §9 — CHOICE LEDGER

```text
D-1  FORCED.  Re-verified every seal myself, from each artifact's own
     directory, both sidecar conventions plus bundle membership, and did not
     carry a single seal state on the build's word.  This produced F-2.

D-2  FORCED.  Used /usr/bin/grep by absolute path.  Additionally cross-checked
     every zero-returning sweep against a known-positive control after one of
     my own pipelines returned a false zero (§0.4).  A silent zero would have
     fabricated the very absence under test.

D-3  UNFORCED, AND THE MOST CONSEQUENTIAL RESTRAINT HERE.  The Phase-2 draft
     states the total dimension of an explicit product space in which the
     environment factor is one slot, and states the other slots.  I DID NOT
     SOLVE FOR THE FACTOR'S DIMENSION and I do not print one.  Doing the
     arithmetic would have supplied part of the factor — the one thing this
     commission forbids absolutely — and would have been indistinguishable in
     the record from a construction.  I display the corpus's sentence and stop.

D-4  UNFORCED.  I report C-1/C-2 (the Phase-2 draft and the extra structure in
     the Phase-1 draft) even though both CUT AGAINST the build's headline and
     neither changes the verdict.  A DEFAULT-REFUTE audit that suppressed
     evidence for the strongest object because it lost anyway would be doing
     the build's advocacy.

D-5  UNFORCED.  I hold the build's RUNG 4 placement of the MAJORANT drafts
     CORRECT, on the ground the build gave second rather than first: both
     drafts declare themselves "NOT SEALED, NOT A RESULT ARTIFACT" in their own
     bytes AND the object is glossed by its own file as a "(relayed-record)
     copy".  The second ground is independent of the seal question and I rest
     on it.  A reviewer weighting the verified sidecar over the self-declaration
     would reach RUNG 5 and invert the verdict; the build flagged this as its
     own most contestable point at D-5, and I confirm that is where the
     determination is thinnest.  It is now thinner than the build knew, because
     there are two such drafts, not one.

D-6  UNFORCED.  I confirmed the FP-2 routing ANSWER while refusing its stated
     REASON (C-7).  The easier report was "6 of 6 confirmed" with no comment.
     The distinction the build itself draws at FP-3 — a boundary is not the
     named factor — is the whole content of the deliverable, and a build may
     not use an identification at one item that it forbids at the next.

D-7  FORCED.  Did not open any register, tracker, road, plan or continuation
     file.  Two DEMAND_REGISTER artifacts surfaced in sweep X5 and neither was
     opened.  Q-733 is named inside a quoted charter sentence and was not
     chased.

D-8  UNFORCED, DISCLOSED.  Three sibling commission artifacts post-date the
     target and appeared in my sweeps.  I excluded all three from every
     completeness judgment against the build, since it could not have read
     them.  Their timestamps are printed at §0.3 so the exclusion is auditable.

D-9  UNFORCED.  My Sense-A line count (954 raw across both mirrored roots,
     160 basenames, my filter) differs from the build's 282 / 69.  I do NOT
     report this as an error: the build's infrastructure filter was tighter
     than mine and the two roots mirror each other.  No finding in either
     document turns on the marginal lines, and I decline to manufacture a
     discrepancy out of a methodology difference.  The FAILS-AT count (C-6) is
     reported as an error because it is a single unambiguous file count.
```

---

## §10 — TOY_SEPARATION

```text
THE ACTUAL SURFACE — what this audit was about:
  Whether the build's determination about the third factor
  H_boundary/environment is true at bytes: whether a construction exists that
  it missed, whether what it called specifications are specifications, whether
  its routing calls hold, whether it smoothed a disagreement, whether it built
  anything itself, and whether its quotations and seals survive checking.
  I SUPPLIED NOTHING FOR THE FACTOR.  No dimension — including the one the
  arithmetic of §1.2 would have yielded, which I deliberately did not perform.
  No algebra, state, dynamics, term, formula, candidate or proposal.

TOYS AND SURROGATES, NAMED SO NONE IS MISTAKEN FOR THE FACTOR:
  1. `C^2_edge/witness` — the two-dimensional stand-in, disclaimed in its own
     sealed file as leaving "a genuine environment spectrum ... outside it".
     NOT PROMOTED HERE — and see C-4, where the build's §4.2 rests a claimed
     buildability site on this toy's embedding.
  2. `H_E^(c)` in MAJORANT_LEMMA0 and MAJORANT_PHASE2 — a "(relayed-record)
     copy" in two documents that each declare themselves NOT A RESULT
     ARTIFACT.  The strongest environment-slot object in either root.
     NOT PROMOTED HERE.
  3. `rho_5(E) = exp(-E)` — an effective density its own file forbids
     promoting to a microscopic law.  NOT PROMOTED HERE.
  4. The nine-dimensional "record environment" of the QSPEC specs — the record
     register in a partial-trace role.  NOT A DIMENSION FOR THE FACTOR.
  5. The "two conditional environment sectors" — the record's own copies.
  6. SENSE-B/C/D tokens — venvs, os.environ, vendored SymPy LaTeX helpers, the
     fence-hazard sense.  Nine of the ten FAILS-AT artifacts that carry the
     token carry only these.  NOT PHYSICS.

THE CONFUSION THIS AUDIT ADDS TO THE BUILD'S:  a specification OF A SURROGATE
  reads exactly like a specification OF THE FACTOR when the surrogate wears the
  factor's slot letter.  `C^2_E` is E for edge/witness, not E for environment,
  and its own certificate list says so: "proof that E retains the edge/witness
  role."  That is C-4, and it is the same class of error as the build's own
  "the second factor is built; the third is named" — one level up.
```

---

## §11 — THE CORRECTIONS, GATHERED

```text
ID    DEFECT                                          INVERTS THE VERDICT?
--------------------------------------------------------------------------
C-1   RUNG 4 census undercounts: a fourth object,     NO — the fourth object
      MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md,      is disclaimed on both
      never opened by the build                       of the build's grounds
C-2   §3.2(4b) understates H_E^(c): omits the basis   NO — cuts against the
      map, the (S2) commutation law, the pointer      build, changes nothing
      read-out                                        in the rung placement
C-3   Deliverable 1 omits the charter naming          NO — same role-type as
      (AXN_BUILD_CHARTER_V001.md:3, SEAL OK)          ~90 lines already typed
C-4   §4.2 counts a specification of the DISCLAIMED   NO — Site 1 alone
      surrogate's embedding as a buildability site    occupies RUNG 3
      for the factor; role preserved is edge/witness
C-5   §4.3 site is an obligation, not a condition     NO
      set; RUNG 2/3 boundary on the build's own defs
C-6   §5.0 FAILS-AT family stated as 12; measured     NO — conclusion is
      at 70                                           strengthened, 1 in 70
C-7   FP-2's stated ground uses the boundary /        NO — the FP-2 call is
      factor conflation the build forbids at FP-3     independently confirmed
C-8   FP-2 quotes the second verdict block and        NO — full re-read of
      passes over the "WBC verdict, verbatim" one     :91-95 does not move it
C-9   A seventh account (H_E^(c)) is treated but      NO — no merge, no
      not listed in the §2.5 account census           reconciliation
C-10  "quoted whole" used for partial extracts;       NO — one instance (F-1)
      loose ranges; small silent alterations          removes qualifying
                                                      material and is corrected
C-11  §4.4's "COMPLETE" is false; >=3 further         NO — the omitted notes
      sealed would-build notes name the factor        support the finding
--------------------------------------------------------------------------
F-2   A SEAL CLAIM DISCHARGED AGAINST THE WRONG       NO — I verified the
      FILE (V002's seal printed for a V001 quote)     quoted file is bundle-
                                                      sealed, 10/10 OK
```

Eleven corrections and one seal-warrant defect. **None inverts the verdict.**
No finding of the build was reversed at bytes; three were strengthened by my
own measurements (§3.1's one-in-seventy, §3.1's RUNG 5 emptiness under wider
patterns, §7's additional would-build notes).

---

## §12 — FLAG BLOCK — STAGE8_ENVIRONMENT_FACTOR_O31SR_AUDIT_V001

```text
step0_probe_and_shasum_from_artifact_own_directory   = true
target_seal_verified                                 = true (OK)
output_path_probed_absent_before_write               = true
every_consumed_seal_reverified_independently         = true
seals_verified_by_me                                 = 28 sidecar + 3 bundles
seal_claims_by_the_build_found_defective             = 1  (F-2)
unsealed_source_confirmed_unsealed                   = 1  (BID; build honest)

sweep_engine_declared                                = /usr/bin/grep, abs path
sweep_cutoff_declared                                = 2026-08-15 18:36:48
every_pattern_declared_verbatim                      = true (7 declared)
false_zero_sweep_caught_in_my_own_work               = 1, disclosed §0.4
roots_swept                                          = 2 (6870 + 7805)

hunt_A_construction_missed                           = NO CONSTRUCTION FOUND
rung5_empty_independently_re_derived                 = true, CONFIRMED
rung4_objects_located_by_me                          = 4 (build reported 3)
namings_missed_by_the_build                          = 2 (charter; PHASE2)
hunt_B_naming_counted_as_specification               = 1 of 3 sites (C-4)
buildability_sites_surviving_at_bytes                = 1 firm, 1 defective,
                                                       1 boundary case
hunt_C_routing_calls_re_derived                      = 6 of 6
routing_calls_confirmed                              = 6 of 6 INTERNAL
routing_grounds_corrected                            = 1 (FP-2, C-7)
hunt_D_disagreement_smoothed                         = false, NO SMOOTHING
accounts_merged_by_the_build                         = 0
hunt_E_construction_by_the_build                     = false
artifact_voided_on_ground_E                          = false
hunt_F_quotations_checked_to_span_end                = 26
quotations_exact_and_whole                           = 22
elision_removing_qualifying_material                 = 1  (F-1, BID :145-147)
favourable_clause_elided_inside_a_cited_span         = 1  (F-4, FP-3 :106)
unsealed_sentence_labelled_sealed                    = 0
hunt_G_lens_tokens                                   = 0
hunt_G_program_numbers_or_measured_constants         = 0

factor_supplied_by_this_audit                        = false
any_part_of_factor_supplied_by_this_audit            = false
factor_dimension_derived_by_me_from_the_product      = false, DELIBERATELY
value_or_program_quantity_emitted                    = false
alpha_computed = false;  proof_authorized = false
kappa_record_computed = false
```

---

## §13 — IMPORT AUDIT

```text
CONSUMED (every seal verified by me, from the artifact's own directory, §0.7):
  the target; and the sealed sources quoted in §§1-7, each named with the seal
  state I measured at its point of use.

CONSUMED AND CARRIED AS UNSEALED, EXPLICITLY:
  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md — confirmed unsealed by
  me under both sidecar conventions and against every bundle in its directory.

CONSUMED AND CARRIED AS SELF-DISCLAIMED:
  MAJORANT_LEMMA0_PROOF_DRAFT_V001.md and
  MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md — sidecars verify OK, and each
  declares in its own bytes "DRAFT — NOT SEALED, NOT A RESULT ARTIFACT".
  Bytes intact; authority disowned by the documents themselves.

NOT IMPORTED:
  no value; no number treated as a program quantity; no measured constant and
  no comparison to one; no alpha; no coupling; NO DIMENSION FOR THE FACTOR —
  including the one obtainable by arithmetic from §1.2, which I did not
  perform; no algebra, state, dynamics, term, formula or candidate supplied,
  proposed or adopted for the factor; no reconciliation of the accounts; no
  selection among TASK2F's four alternatives; no git.

  Integers in this artifact are counts of files, lines, seals, quotations and
  corrections produced by my declared sweeps, or quoted corpus content under a
  seal I verified.  None is a program quantity.

FENCES HELD:
  alpha_computed = false;  proof_authorized = false;
  kappa_record_computed = false.
  DETERMINATION ONLY — located, typed, displayed.  Constructed nothing.
```

---

## §14 — VERDICT

### §14.1 PER DELIVERABLE

```text
D1  EVERY NAMING, GATHERED AND TYPED
    CONFIRMED-WITH-CORRECTIONS.  The census and its six role-types hold at
    bytes.  Two namings are missing: the governing charter (C-3) and the
    Phase-2 draft (C-1).

D2  WHAT THE CORPUS SAYS THE FACTOR IS
    CONFIRMED.  Four incompatible typings verified.  Six non-agreeing accounts
    verified at bytes, including the zero-token measurement behind ACCOUNT 6.
    Displayed, not reconciled.  One further account treated but unlisted (C-9).

D3  ANY CONSTRUCTION, ANY ATTEMPT
    CONFIRMED-WITH-CORRECTIONS.  *** RUNG 5 EMPTY IS RE-DERIVED AND CONFIRMED
    UNDER PATTERNS WIDER THAN THE BUILD'S. *** The declined attempt of §3.5 is
    verified whole and exact in all four of its quoted sources.  RUNG 4 is
    undercounted at three; there are four (C-1, C-2).

D4  WHAT CONSTRUCTING IT WOULD REQUIRE
    CONFIRMED-WITH-CORRECTIONS.  The six-item reopen condition is quoted whole,
    sealed, and is a genuine checkable specification with a stated failure
    test — RUNG 3 IS OCCUPIED.  But one of the three claimed sites specifies
    the disclaimed surrogate's embedding (C-4), one is a boundary case (C-5),
    and the "COMPLETE" would-build inventory is not complete (C-11).

D5  THE ROUTING TEST
    CONFIRMED-WITH-CORRECTIONS.  All six calls re-derived from their own
    operative sentences and all six confirmed INTERNAL; 0 of 6 route through
    the environment side.  The FAILS-AT denominator is wrong (C-6) and the
    FP-2 ground is over-claimed by an identification the build forbids one
    subsection later (C-7, C-8).
```

### §14.2 OVERALL

```text
*** CONFIRMED-WITH-CORRECTIONS ***
```

The build's verdict — **SPECIFIED-NOT-CONSTRUCTED** — **STANDS AT BYTES.**

I attacked the dramatic claim directly and it survived. Under my own wider
vocabulary, uncapped over both roots, exactly one construction-shaped sentence
exists in either root and it is the one the build itself located and correctly
disqualified as the record register in a partial-trace role. There is no
construction of the third factor. The buildability conditions are stated, in a
sealed six-item predicate with a stated failure test, quoted whole and exact.
The absence is beside the wall: six of six demands re-derived internal.

The corrections are real and several are the kind that would matter if the
verdict were closer. The one that would matter most is not a correction at all
but a confirmation of where this determination is thin: **the whole verdict
turns on letting two documents' self-declaration — "NOT SEALED, NOT A RESULT
ARTIFACT" — govern over their verifying sidecars.** The build named that as its
most contestable judgment. It is more contestable than the build knew, because
there are two such drafts and the second one carries the environment factor
into an explicit finite-dimensional model. A reviewer who weighted the sidecar
over the self-declaration would reach RUNG 5 and invert the verdict. I do not
weight it that way, for the reason that is independent of seals: both documents
gloss the object as the record, copied.

*** THE FACTOR IS STILL NAMED INTO A WELL-SPECIFIED HOLE THAT HAS NEVER BEEN
    ENTERED. Nothing in this audit supplies any part of it. ***

```text
END — STAGE8_ENVIRONMENT_FACTOR_O31SR_AUDIT_V001
```
