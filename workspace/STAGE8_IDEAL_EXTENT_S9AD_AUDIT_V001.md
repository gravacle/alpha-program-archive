# STAGE 8 — IDEAL EXTENT: ADVERSARIAL AUDIT — V001

## ADVERSARIAL AUDITOR — CODENAME IDX-AUDIT — [SEALED]

Date: 2026-08-15 (session UTC). Target: `STAGE8_IDEAL_EXTENT_S9AD_V001.md`
(`cfa2fb97…`, sidecar MATCH, verified at path before opening). Default
posture: REFUTE. Stakes symmetric per the sealed biconditional: a false
PURE-CYCLE-COMPLETE falsely grounds an authoring act; a false MIXING-FOUND
falsely kills two families.

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION-AUDIT ONLY: no
member bound, no adoption, no authored physics, no route/branch/address/
representative selected, no score/gate moved; the principal's acts
untouched. The held construction `506bfe2a…` untouched, unopened,
unpresupposed. "Q-…" tokens EXPECTED-UNLOCATABLE (one fence disclosure at
CL-5). No git; ONE output file plus seal sidecar; output path probed
ABSENT immediately before this write. Bytes-level + exact symbolic only;
own battery in a FRESH venv (sympy; 12/12 PASS; exact integers/rationals/
symbols; no float; no value frozen).

**SWEEP CUTOFF: 2026-08-15T05:48Z** (A-1..A-4 below plus the pre-verdict
sweeps). Exhaustiveness is sweep-displayed only.

---

## 0. VERDICT

```text
VERDICT = CONFIRMED-WITH-CORRECTIONS

The determination STANDS: NOT-CLOSABLE-TODAY is correct and correctly
scoped; the extent was independently re-derived BLIND and every closure
the build claims (X-1..X-4) is genuine, exact, and consumes sealed
structure; neither PURE-CYCLE-COMPLETE nor MIXING-FOUND is claimed, so
neither false-grounding arm of the biconditional fires. The named hazard
— a presentation treated as closed when the sealed data leaves it open —
did NOT materialize: the only closure claimed (the a=0 slice, X-1) is
exactly the one the sealed structure forces, and the open bit is
displayed AS open with its missing object named at sealed flag sites.
Three corrections, C-1..C-3 (§4), all verdict-neutral.
```

## 1. INDEPENDENCE PROTOCOL (executed before the build was opened)

```text
The extent was worked independently from the sealed carriers (round-3/
B-3 pairs at audited grade; d662889b (S1)/(S2)/(Q5)/(R4)/(R8); b53d9e93
(S1)-(S8); 70dde1c0 §2/(S1-1)-(S1-5)/(S2-14); ccb94dfa (B2-5)-(B2-7)
as quoted) WITHOUT opening the build. Pre-verdict FIXED and hashed
BEFORE the build's seal was checked:

  SHA-256(IDX_AUDIT_PREVERDICT.md)
   = abd3fbce0b1b130c3304c98cb2dd70aaf206e6c20370d337a71b57bf61a368f2
  fixed at 2026-08-15T05:48:25Z (build written 05:34:46 local; unopened
  and unread until after the fixing).

PRE-VERDICT CONTENT (summary): own elimination route at the four anchor
stages (rank 1, dim E_N = 3): (i) ker(qtilde_N) ⊇ the (R4) line J0 =
span{(0,1,-R_K,N)}, exact; (ii) THE PURE-CYCLE SLICE CLOSES EXACTLY —
ker ∩ E^cyc = J0 — via ker(qtilde) ⊆ Rel_N = ker(Q_N) ((S1), closure =
ker at the census) + Kernbar_N^cyc well-defined on O_N^cyc ((S2a)
[PROVABLE]) + (B2-7) faithfulness; corollary Rhat_K,N != 0; (iii) the
mixing slice does NOT close: a mixing element exists iff L_T,N and
Rhat_K,N are dependent in O_R5,N, forced coefficient -(b+c)/a, dim ker
in {1,2}; NO sealed byte decides the bit either way (TASK4B (S1-4)/
(S1-5) all unproved, (S2-14) absent, (Q6), (R8)/§4.4, B-3 §4.3, round-3
SW-2, own sweeps incl. tonight's landings). Lawful outcome pre-declared:
NOT-CLOSABLE at the mixing bit, with at most the pure-cycle-slice
closure displayable; PURE-CYCLE-COMPLETE => REFUTED-AT the mixing bit
unless a sealed independence byte is exhibited; MIXING-FOUND =>
REFUTED-AT unless a sealed dependence byte with exact coefficient and
cycle-side value is exhibited; absent/seal-mismatch => UNDETERMINED-AT.

MATCH WITH THE BUILD: verdict identical (NOT-CLOSABLE-TODAY at the same
bit); the pure-cycle-slice closure identical in every load-bearing step
(same sealed consumption, same faithfulness bound, same J0); the
two-entry menu and forced coefficient identical (build E-2 basis
(-a_K,1,0) = my (1,-λ,0) under λ = 1/a_K, both directions equivalent
since both vectors are nonzero). DIVERGENCES, all in the BUILD'S favor
(verified genuine, §3): X-3 (zero-value mixing excluded — I derived the
implication shape "value 0 => L_T = 0" but left the typing cautious;
the build grounds L_T,N != 0 at sealed-typing grade, the same grade
OB-2 CL-5 consumed M_N^loc != 0, audit-passed precedent); X-4 (the
anchor-bit collapse — my pre-verdict said mixing-bit transport lacks a
sealed square on the S_N-realization route; the build found a DIFFERENT
sealed square, the ideal-level covariance (LM1-12)/(LM1-13), which is
verbatim at bytes and preserves the a-coordinate); and the sharper
identification of the bit with the sealed R_K_EQUALS_L_T flag. The
build is STRICTER and SHARPER than my independent run, not looser.
```

## 2. SEALS, BYTES, PROBES (this session, at path)

```text
cfa2fb97  STAGE8_IDEAL_EXTENT_S9AD_V001.md      MATCH (sidecar verbatim)
Re-hashed ALL 14 artifacts the build lists (§1 commission + chased):
824eb72f, a7255f38, 98ee31ba, 68202c7b, d662889b, b53d9e93, bae34116,
70dde1c0, 041498bb, be570c18, 6b7f8f98, fdf20bd4, 5de94e16, abf6d366 —
14/14 MATCH, zero mismatches.
Output-path uniqueness: no competing extent artifact (A-1); the build's
write (05:34:46) postdates its own final ABSENT probe (05:32:22Z);
probe discipline consistent at mtimes. Post-build landings: NONE in any
root up to my cutoff.
QUOTE SITES WALKED AT BYTES (each hash-verified first):
  (LM1-9)/(LM1-10)/(LM1-12)/(LM1-13)  bae34116 :275-330 VERBATIM
    ("does not assume Rhat_K=L_T, does not assume they are independent";
     alpha_U(Rel_G)=Rel_G' for an ADMITTED ISOMETRY, a-coordinate fixed)
  R_K_EQUALS_L_T = NO_VERDICT / TYPE-U  70dde1c0 :876 and 041498bb :354
  "It does not prove: ... R_K lies wholly in the nonlocal/higher-
    derivative complement"  041498bb :549-555 VERBATIM (but see C-1)
  (R2-3)/(R2-4) named ABSENT ("no map ... and no declared direct sum")
    041498bb :310-323 — the why-open account is verbatim-faithful
  (S1-5) three shapes + "No authority proves any line" 70dde1c0 :244-252
  (S8) "plus sign ... not declared a direct sum. Mixed relations may
    identify"  b53d9e93 :226 ; route bar "not admissible alternatives:
    ... R_K=L_T ..."  b53d9e93 §2.4 :941-942
  (S2a) "[PROVABLE] The sealed Q-408 formula constructs ... Write that
    exact scoped map as Kernbar_N^cyc"  b53d9e93 :136-144
  Schur/RetExtract home: (W1-4)/(W1-5) be570c18 :210-214 ; "S := an
    admitted K/K Schur form K->K^*" 6b7f8f98 :147
  S_nm "isometric isomorphism ... S_nm^* R_K,m S_nm = R_K,n (Q13)"
    474bf721 :577-581 ; u typed only "the actual reciprocal
    rank-preserving generator" fdf20bd4 :1257 (no isometry byte — the
    build's CONDITIONAL gating of the u-pair collapse is honest)
  BIND row "L_T ... COMPLETED ... it spans the local subspace"
    TASK4A_BIND_INPUT_SIGNATURE... :295 VERBATIM (but see C-2)
```

## 3. THE ATTACKS RUN (commissioned battery)

### 3.1 Attack on PURE-CYCLE-COMPLETE (the completeness, hardest)

```text
The build does NOT claim it — the false-grounding arm cannot fire. The
closure it DOES claim (X-1, the a=0 slice) was attacked as if it were
the hazard: re-derived with my OWN elimination route BEFORE opening the
build (§1) — identical: well-definedness of the sealed scoped map
(S2a) bounds Rel_N ∩ E^cyc above by the (B2-7) faithfulness kernel
{b R_K + C = 0}; (R4) meets the bound below; the step consumes sealed
structure and is exact at any displayed rank (own battery: rank-1 and
rank-2 kernel dim 1, J0 direction exact, generic-SPD choice-invariant).
The closure ADDS no premise beyond the reading B-3 §3.2's audited exact
dimension display already consumed (build CL-6 states this correctly —
the cross-check "quotient loses exactly one cycle dimension" is my
computation too). X-2's menu: exact rank-nullity on dim E = 3 with both
generators nonzero — dim ker in {1,2}, no third extent; E-2's exhibited
basis verified by substitution; the mixing coefficient forced. CLEAN.
```

### 3.2 Attack on MIXING-FOUND

```text
Not claimed — the false-kill arm cannot fire. The conditional mixing
display (E-2) was attacked as if asserted: its relation (-a_K,1,0)
re-derived exactly under the dependence hypothesis; its cycle-side
value -(1/a_K) Kern(R_K,N) re-derived exactly ((R8) definition at
d662889b bytes; nonzero by (B2-7) + R_K != 0); a_K is never valued, no
sealed dependence byte is cited as existing, and my own sweeps confirm
none exists (A-2/A-3: the three R_K=L_T sites are NO_VERDICT/TYPE-U/
bar; no proportionality display anywhere; tonight's landings MO4/
WALL_SUMMED_REQUANT/PIN2H-R-A contain no qtilde/Rhat_K/mixing/ideal
byte). CLEAN.
```

### 3.3 NOT-CLOSABLE — the missing closure object against the corpus

```text
VERIFIED at bytes, independently: the bit (Rhat_K,N in R L_T,N inside
O_R5,N) is exactly the sealed question R_K_EQUALS_L_T, standing
NO_VERDICT / TYPE-U at BOTH flag sites, with both directions explicitly
unproved (041498bb "does not prove" list), every (S1-5) closure shape
unproved ("No authority proves any line"), the (S2-14)/(R2-4) arrow
declared absent, and assuming the identity BARRED as a route (b53d9e93
§2.4). My pre-verdict located the same open bit at the same sites blind.
The structural why-open account (O_R5,N = prior completed K/K Schur
class post-RetExtract; C-sector consumed inside Inv_CC; readers
downstream of Rel_G; sigma_loc unbuilt) is verbatim-faithful to
be570c18/6b7f8f98/041498bb/bae34116. X-3 verified at its stated grade:
value 0 <=> b R_K + C = 0 => a L_T,N = 0 => L_T,N = 0 (exact, own
battery), against the sealed typing L_T,N != 0 — consumed at CL-3 at
the SAME grade OB-2 CL-5 consumed M_N^loc != 0 (precedent audit-
passed); the build correctly spends the typing ONLY on the zero-value
branch, never on the bit. Consequence chain checked: retiring NG-3's
antecedent leaves NG-3' (dim M_N >= 2 cell) as the entire live mixing
kill — consistent with round 3 §5.1-5.2 at bytes; no kill surface is
widened, only de-duplicated. CLEAN.
```

### 3.4 Anchor-stage quantifier

```text
EXACT. The two-entry menu is displayed AT the four rank-1 anchor stages
(dim E_N = 3, dim S_(1,sa) = 1, field-reading-invariant at this rank —
verified); the rank-2 diamond slots are explicitly OUT of anchor scope
with the honestly-widened question (L_T in a 3-dim image span) stated,
not decided; X-1's every-displayed-stage scope is lawful (the slice
argument is rank-uniform at the finite census — my battery verified
rank 2 exactly). X-4's collapse: S_nm leg VERIFIED (sealed isometric
isomorphism + a-coordinate-preserving ideal covariance); u leg
correctly CONDITIONAL (no isometry byte at fdf20bd4 :1257 — swept).
No (SA-1)-closure overreach: carriage beyond the census is nowhere
claimed. CLEAN.
```

### 3.5 My own battery and sweeps

```text
BATTERY (fresh venv, sympy): 12/12 PASS — (R4) argument vanishes
  (symbolic t; rank 1 + generic SPD rank 2); case-A kernel = J0 dim 1;
  case-B kernel dim 2 with mixing element exact; forced coefficient
  -(b+c)/a; composite-kernel dim 1 at rank 1 AND rank 2 with J0
  direction exact (choice-invariant); Rhat_K != 0 route; dim menu
  {1,2}. Every named exact claim of the build's 15/15 battery that is
  load-bearing reproduces independently.
SWEEPS (roots = workspace, supervision, cleanroom_output, relay_inbox,
  relay_outbox, minus fenced classes; cutoff 05:48Z):
  A-1 "IDEAL_EXTENT|ideal extent" -> target only; no competing artifact.
  A-2 "proportional" x "L_T|Rhat|R_K" -> 70dde1c0's own unproved-
      conditional and one killed attack row; no dependence display.
  A-3 "independen" x "L_T|cycle" -> no independence certificate.
  A-4 mtime sweep of tonight's window -> MO4/WSR pairs + PIN2H-R-A
      only; none contains an extent-touching byte; nothing landed
      after the build in any root.
  Pre-verdict sweeps concur with the build's SW-1..SW-6 line by line.
```

## 4. CORRECTIONS (all verdict-neutral; none fires)

```text
C-1  CITATION TRANSPOSITION. The build cites "041498bb §3.5" for the
     both-directions-unproved display ("It does not prove: ... R_K=L_T;
     R_K lies wholly in the nonlocal/higher-derivative complement").
     The quote is VERBATIM at 041498bb §5.3 (:538-555); §3.5 is
     "DoR-008 applied to the scalar consumer". The §3.4 flag citation
     is correct. Byte content real; section number wrong. REPAIR:
     registrar note; no content moves.
C-2  UNLISTED SEAL FOR ONE CONSUMED CARRIER. CL-3's third typing site
     (the BIND row "COMPLETED ... it spans the local subspace") is
     consumed from STAGE8_TASK4A_BIND_INPUT_SIGNATURE_AND_DOMAIN_TAIL_
     BLINDNESS_DETERMINATION_V001.md :295 — verbatim at path — but
     that file appears NOWHERE in §1's seal table (the same pattern
     B-3 disclosed for MANIFESTS at its CL-9; here it is not even
     disclosed). Mitigated: the other two CL-3 sites live inside
     seal-verified 70dde1c0 ((S1-1) p_loc[L_T]=1 + §2 typing) and
     carry the typing alone; X-3 does not depend on the BIND row.
     REPAIR: registrar carries the citation with its hash; pattern-
     level (list every consumed carrier, or disclose the omission).
C-3  CAS NOT DISPLAYED VERBATIM. "15/15 PASS" with no battery source
     inline or sidecarred — the round-3 audit's C-2 pattern persists a
     third time. Mitigated: my independent battery reproduces every
     load-bearing exact claim (12/12). REPAIR: sidecar battery source
     in future S9AD builds (standing pattern repair).
```

## 5. WHAT WOULD HAVE REFUTED (and did not fire)

```text
R-a  PURE-CYCLE-COMPLETE claimed via presentation-as-closed (the named
     hazard), or any closure step consuming no sealed structure — DID
     NOT FIRE (only the a=0 slice is claimed closed; the step is (S2a)
     + (B2-7), exact, independently re-derived blind).
R-b  MIXING-FOUND claimed without a sealed dependence byte, or with a
     wrong cycle-side value — DID NOT FIRE (not claimed; the
     conditional value re-derives exactly).
R-c  The faithfulness argument run THROUGH the L_T summand (where
     Kernbar is not a term) — DID NOT FIRE (checked step by step; the
     slice bound never touches the a-coordinate).
R-d  The extent menu missing an entry (e.g. Rhat_K = 0, or a dim-3
     kernel) — DID NOT FIRE (Rhat_K != 0 is forced by the slice
     closure; L_T != 0 consumed at typing grade; rank-nullity closes
     the menu at two — my own derivation).
R-e  Anchor quantifier exceeded (diamond or (SA-1) stages smuggled in)
     — DID NOT FIRE (§3.4).
R-f  X-4 collapse asserted on the u pair without the isometry byte —
     DID NOT FIRE (gated CONDITIONAL in so many words).
R-g  A sealed extent-fixing byte existing before the cutoff and missed
     — DID NOT FIRE (my own sweeps at 05:48Z concur with SW-1..SW-6).
R-h  Build absent or seal mismatch — DID NOT FIRE (cfa2fb97 MATCH).
```

## 6. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: the five archive roots minus fenced filename
      classes; a32_holdout not content-swept (holdout discipline).
      Sweeps A-1..A-4 + pre-verdict sweeps, cutoff 2026-08-15T05:48Z.
      A wider sweep could change this audit only by exhibiting a
      pre-cutoff sealed R_K_EQUALS_L_T determination, an (S1-5)
      certificate, an exhibited mixing relation, an isometry typing
      for u, or a competing extent artifact. PREMISE(named).
CL-2  INDEPENDENCE INSTRUMENT: the pre-verdict hash (§1) is the
      commitment; the pre-verdict file lives in the session scratchpad
      (commission-distinct, outside the corpus), content summarized
      verbatim-faithfully in §1. PREMISE(named).
CL-3  AUDITED-GRADE CONSUMPTION: round-3/B-3/OB-2/PIN pairs at their
      audited grades; KER (B2-5)-(B2-7), LOC-NAT (Q13), PJ2 clause
      bytes consumed as quoted inside seal-verified artifacts except
      where re-walked directly (§2's quote-site list — each re-walk
      hash-verified first). PREMISE(named).
CL-4  TYPING-GRADE ACCEPTANCE: L_T,N != 0 accepted at sealed-typing
      grade for X-3 adjudication, on the OB-2 CL-5 precedent (M_N^loc
      != 0, audit-passed); noted that X-3's standing is exactly as
      strong as that grade and no stronger. PREMISE(named).
CL-5  FENCE DISCLOSURE: before commission constraints were fully
      integrated, the registrar-class QUESTIONS_SETTLED_REGISTER was
      grepped ONCE for commission context; the lines read were the
      Q-1097 (round-3) row ONLY — sealed BEFORE the target build
      existed, containing NOTHING about the target's outcome (verified:
      the grep hits contain no later row); every fact taken from it was
      independently re-verified at the round-3 pair's own bytes.
      Independence of the pre-verdict from the build's outcome is
      unaffected. Disclosed here in full. PREMISE(named).
CL-6  DISCLOSURE: sweep listings displayed fenced-class BASENAMES; no
      fenced content beyond CL-5 opened, read, or used. My battery ran
      in a scratchpad venv (sympy version differs from the build's
      pin; all claims are exact-symbolic, version-insensitive).
      PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  My battery's model objects (one generic SPD 2x2, a rank-1 carrier,
  two abstract codomain models for the independent/dependent cases,
  symbols t/a/b/c/lam never evaluated) are verification instruments
  only: they check that the displayed kernels are FORCED by the stated
  hypotheses and decide nothing about the sealed carriers. No toy
  value, stage, address, dimension, or number enters any conclusion;
  no float anywhere; no value frozen; a_K never valued.
```

## FLAG BLOCK

```text
IDX_AUDIT = CONFIRMED-WITH-CORRECTIONS (C-1 citation transposition;
  C-2 unlisted seal for the BIND-row carrier; C-3 CAS not verbatim —
  all verdict-neutral, none fires)
TARGET_VERDICT_STANDS = NOT-CLOSABLE-TODAY at the one bit
  (R_K_EQUALS_L_T instanced at the anchor stages); X-1 pure-cycle
  slice closure GENUINE-EXACT (sealed structure consumed: (S2a) +
  (B2-7)); X-2 two-entry menu FORCED (dim ker in {1,2}); X-3 sound AT
  TYPING GRADE (zero-value mixing excluded; NG-3 antecedent empty;
  live mixing kill = NG-3' on the dim M_N >= 2 cell only); X-4 sound
  AS GATED (S_nm leg sealed; u leg conditional)
HAZARD_CHECK = the commissioned hazard (presentation-as-closed) did
  NOT materialize; neither PURE-CYCLE-COMPLETE nor MIXING-FOUND is
  claimed; the biconditional's false-grounding arms cannot fire
INDEPENDENCE = pre-verdict fixed + hashed BEFORE opening the build
  (abd3fbce…, 05:48:25Z); independent verdict = NOT-CLOSABLE at the
  same bit; every divergence resolves IN THE BUILD'S FAVOR (X-3
  typing-grounding; X-4 ideal-level covariance square — verified at
  bae34116 bytes)
ANCHOR_QUANTIFIER = exact (four rank-1 stages; diamond slots out of
  scope and honestly widened; no closure-reach overreach)
SEALS = target 1/1 + relied-upon 14/14 MATCH ; MISMATCHES = 0 ;
  output path first-use, probed ABSENT
BATTERY = own, fresh venv, 12/12 PASS, exact only ; no float
SWEEPS = A-1..A-4 + pre-verdict sweeps ; CUTOFF 2026-08-15T05:48Z ;
  no post-build landing in any root ; no extent-fixing byte anywhere
KILL_SURFACE_AFTER_THIS_PAIR = the deciders stand exactly as the build
  names them: one sealed R_K_EQUALS_L_T determination per isometry
  class (=> E-1 INHABITED mod TOP-1/TOP-2 + dim M_N, or => E-2 NG-3'
  kill on the dim >= 2 cell); TOP-1/TOP-2; dim M_N; (SA-1)
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, nothing numeric beyond exact
  integers/symbols) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a by bar)
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the deciding display survives an audit built to kill it: the
  extent was re-derived blind and matched line for line; the one
  closure claimed is the one the sealed bytes force; the one bit left
  open is genuinely open at its own sealed flag sites, now named,
  de-duplicated to at most two stage-instances, and stripped of its
  zero-value branch. B-1's fiber still hangs on that bit, exactly as
  round 3 printed — no gate moved.
```
