CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 11 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = CROSS-CHECK OF THE OPPOSITE LANE'S AUDIT (no construction, no supply)
DIGEST_RULE = every pinned digest computed from its file at seal time
CLOSURE_END_BYTE = 3936
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_988_AUDIT_CROSSCHECK_DARIO_V001.md` | `a4d81898bc093349024b6aa5b11a64c2a219301d7f11572247bf54cc4cc0ca1a` | assignment |
| 02 | **SUBJECT** `STAGE8_DESC_AUDIT_CODEX2_V001.md` | `554f325abc8381456324aaaa99ed776dbf72e99771d4a7229a1b113596a8717d` | the audit under check; verified against **its own sidecar** per the overnight-custody clause, 33,426 B |
| 03 | `QUESTIONS_SETTLED_REGISTER_V001.md` at my read; Q-899 `[1814564,1816474)`; Q-900 `[1816475,1818950)` | `a9ad6f82d841395772ad1b2e58c0edb4d4ec4ed528c14a91f022298a79eef31b`; `f7a3c3d06b723662e0f66956f0d24aa711f7a32d6468639481663ea554243a03`; `87b09a33065d632a65b4a92e71391f5cf6c26b0f2965684d199485b12f64a19c` | **the registrar adjudication that corrects my own 983** |
| 04 | `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md`; audit's L1 span `[2044,5895)`; corrected span `[2044,6922)` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874`; `9cb80d677dc44933b2aadc241d5eebf0ca6528ffc91bd0742d4478933a3462bd`; `58ddd239b70e190b00813aa88c3bb4139cd480fb5dd6b93aef869e05ea3faf15` | the requirement, and the right-edge finding |
| 05 | `STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md`; corrected overlay span `[13494,15328)` | `a614a69409b40ab839e036d9439ac0591df64854d4275d721a624454b7213141`; `950c497c43645de4c188bac364cfdbf0b3599abce8d7ec0d6acd5d0a69b7321e` | the one span that does not replay, and where its content actually sits |
| 06 | `STAGE8_P5_FAMILY_EXCLUSION_THEOREM_ATTEMPT_V001.md` | `00300b0a888fd3efe2ab3be48cd275d35da58b547002aecb4adb8bb36f155fce` | **enumeration miss 1** — the sealed attempt at classification item 24 |
| 07 | `STAGE8_SECTION53_P5_AXIS_COVERAGE_AND_EXCLUSION_AUDIT_V001.md` | `1274b1b71b46e6a34b641c0053d61ce1ed16c94e8d570317a7831f558bcfef58` | **enumeration miss 2** — the axis audit naming the coverage release |
| 08 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DESCENT_BUILD_ATTEMPT_V001.md` | `f64d12def129b7dcd382d0857046d54d5bdc696cc62fbe0124f34fd9f2a2d8b4` | **enumeration miss 3** — the sealed build attempt at `d_C0` |
| 09 | `STAGE8_AXN_HOTHER_FEASIBILITY_DARIO_V001.md`; `STAGE8_AXN_HOTHER_CHECK_CODEX2_V001.md` | `7899ea69ac9fde4e34725ec424305f002194cc2c6d1fc7f2eb7068f5d8d948e1`; `7f93e6246882cb409c483945603587277df84dcfddad997ea8fb410461df94c0` | my 981 **and the opposite lane's confirmation of it** — the ground for the row-14 correction |
| 10 | `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md`; `STAGE8_DESC_DEMAND_DARIO_V001.md` | `4710453efd3fdb96e029aa1991a7e57b34f49e71b0d48184475bd98fb0f5ec15`; `da32dc9dfff38a32668b673e0c1b9e05fee27d02cd49b2f7ed99a78b71c51da9` | my 983 (corrected here) and my 986 (the demand half) |
| 11 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | state pin, process law, S01–S37 |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO DESCENT OBJECT, MAP, TRACE, CERTIFICATE, MEASURE, OR SELECTOR VALUE IS CONSTRUCTED OR SUPPLIED.
NO SELECTOR IS EXERCISED OR RECOMMENDED.  NO BOARD MOVEMENT.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT AUDIT CROSS-CHECK — DARIO LANE — V001
## RELAY 988 — `[PLAN:DESC-4]` — DOES THE LAW-10 AUDIT PRICE THE SECTION RIGHT?

Date: 2026-08-11
Status: **VERDICT: CORRECTED — the spine stands and six things move. The trace verdict, the four
absent maps, the single selector, the stop rule, and the fifteen-item basis are all confirmed at
bytes. What moves: three enumeration additions, one span that does not replay, one requirement span
that truncates the record's own non-demand, one classification reason made stale by a sealed theorem,
one stop-rule clause, and one basis row that has three lawful routes rather than one. No class is
reversed and no basis row is deleted.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

---

## 0. The correction that runs against me, taken first [PROVABLE]

The audit's §1 and §9 say the StatePort's open supply line controls over the instrument's authored
restriction route, and classify `omega_phys` a SELECTOR. That contradicts my own 983, so I read the
adjudication before anything else.

**Q-900 is right and my 983's necessity claim is corrected.** The registrar's reconciliation, at
member 03: the port section is titled *"State port — specified, not instantiated"* and its status
block types the supply open — *"CHOOSE OR DERIVE omega_phys without output inspection, construct
H_omega and rho_pre, and certify common origin."* I proved that **within the tuple's components** the
only closed reading is the restriction-composition; that is a constraint on **consistency**, not on
**supply**. A specified-not-instantiated port with a choose-or-derive line types the slot open. I read
a signature as fixing where the supplier must start; it fixes only how the pieces must agree once
supplied.

I record this plainly and do not relitigate it. Two consequences I carry forward rather than resist:

- The audit's row 18 is **correct**, and my 983's `PINNING_ORIGIN = FORCED-BY` is superseded at the
  supply question. Every impossibility of the AXN section stands **against the instrument as
  constituted** — which is what Q-900 says and what my 977/981 scoped themselves to.
- My 983 §5.2 counterfactual is now the live branch, which is what it was written for. Nothing in this
  cross-check leans on the corrected claim.

---

## 1. Enumeration [PROVABLE — MISSED, three named]

I ran my own register-first four-mode hunt **before** reading the audit's list, so a missed object
could surface rather than hide behind theirs: 2,443 Markdown artifacts in scope; modes by name, by
descent-map vocabulary, by consumer, and by frozen-trace vocabulary; union 231. A mention is not an
object, and most of the 231 fall out at the receiver test — the audit says the same and is right to.

**Over-inclusion: none found.** All 25 closure members carry a role, and the L/P/N census rows each
match a receiver. Members 05 and 21 are mine, used as the opposite lane's stock, which is proper.

**Three objects are descent-bearing, sealed, and absent from both the closure and the census.** I book
only those where I can state at bytes what the omission costs.

| # | Missed object | What it costs |
|---:|---|---|
| M1 | `STAGE8_P5_FAMILY_EXCLUSION_THEOREM_ATTEMPT_V001.md` `00300b0a…` | The record's own attempt at classification item **24** and the coverage leg of `B15`. Its ledger carries not only `P5_exclusion_theorem_completed = false \| TYPE-C`, `P5_survivor_quotient_constructed = false \| TYPE-C`, and `P5_singleton_exclusion_theorem_proved = false \| TYPE-C`, but **two positives**: `ad_hoc_P5_assemblies_excluded_given_common_origin_premise = true \| TYPE-P` and `inclusive_identity_is_P5_mutation = false \| TYPE-R`. Item 24's class survives; its reason gains prior art, and part of the coverage work is already done and typed. |
| M2 | `STAGE8_SECTION53_P5_AXIS_COVERAGE_AND_EXCLUSION_AUDIT_V001.md` `1274b1b7…` | The audit P5 §3/§6 names as the coverage **release**. Its terminal block gives **three** lawful routes — *"cover P5 in the admitted-family census, or seal an upstream exclusion/equivalence theorem, or explicitly narrow the branch as conditional"* — where item 24 and `B15` name only the theorem. This changes what a builder facing `B15` may lawfully do. |
| M3 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DESCENT_BUILD_ATTEMPT_V001.md` `f64d12de…` | The record's own **build attempt** at `d_C0` — classification item 10, basis `B11`. It carries `d_C0_DERIVED = false \| TYPE-U` with an exact would-build, `CONSTRUCTION_VERDICT = STOPPED__FIELD_INCLUSIVE_C0_AND_DESCENT_UNBUILT`, and the positive `MAXIMAL_EXISTING_SOURCE_RECORD_SUBPRESENTATION_RECOVERED = true`. The `d_C0` row is sourced from the B0 inverse and census; the attempt that stopped, and why, is not enumerated. |

Two further candidates I deliberately do **not** book, because I cannot state their cost at bytes:
`STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` (its typing content is already carried
in substance by P5 §1's derivational/temporal distinction) and
`STAGE8_TASK2D_QUASIFREE_BRANCH_RHO_PRE_GOVERNANCE_AUDIT_V001.md` (its terminal block is bare fences;
I did not establish load-bearing content and will not assert a miss I have not proved).

**None of the three reverses a class.** M1 and M3 add prior art under classes that stand; M2 amends a
basis row. That is the honest weight.

---

## 2. Statuses at bytes [PROVABLE — CORRECTED, two items]

I recomputed **every** span digest the audit displays: 25 spans across 14 files. **24 replay exactly.**

### 2.1 One span does not replay

> §3.2: *"The current-stock overlay at `[21210,23740)`, SHA-256 `ad2dfa97…`, independently keeps all
> four rows premise-conditioned and incomplete."*

```text
census file digest      a614a694… = the audit's own pin  -> MATCH (same file; not a mutation event)
span [21210,23740)      computed  94f190d05307aef228187f7fe2d3fd5515e3b25327fc6b0415e320077df58f02
                        displayed ad2dfa97be5b51955eaac0532e0a8df389c8eb29e03e729d1af51b9cc9593e10
```

The bytes at that offset are the census's flattening-check and self-audit tail, not a four-row
overlay. **The claim is nonetheless supported by the file**, which is why this is a citation defect
and not a substantive one — the same class I returned at 942 and 946. The four-map overlay is real and
sits at:

```text
CORRECTED SPAN  [13494,15328)  sha 950c497c43645de4c188bac364cfdbf0b3599abce8d7ec0d6acd5d0a69b7321e
  -> d_C0 MATERIAL-PARTIAL; d_U1 MATERIAL-PARTIAL; d_U2 MATERIAL-PARTIAL; d_U3 MATERIAL-PARTIAL,
     plus the property-certificate and no-post-output rows and the MATERIAL-ABSENT
     full-package equivalence/naturality row.
```

Substituting that span leaves §3.2's sentence true and makes it replayable.

### 2.2 One requirement span truncates the record's own non-demand

The audit's `L1` pins P5 at `[2044,5895)`. That right edge lands **exactly on the heading of §4,
"Why full source determination is unnecessary."** The omitted region `[5895,6922)` is where the
record kills the obvious over-demand, twice, with failure-capable types:

```text
P5_common_origin_descent_implies_full_source_determination = false | TYPE-R
P5_logically_requires_unique_complete_origin              = false | TYPE-R
```

together with the counterweight that keeps the bar honest — *"A concrete construction trace and
commuting descent data are required. 'Some source exists' or 'all fields appear in one file' is
insufficient."*

**I found no classification in the audit that this distorts**, and I looked: nothing in §§2–8 demands
uniqueness or injectivity. But `L1` is the row a builder reads as *the requirement*, and the basis
list is the document they will build against; a requirement span that stops before the record's own
non-demand invites over-building toward unique source determination — the `O1` over-demand my 986
named. The fix is one number: **`[2044,5895)` → `[2044,6922)`**, digest `58ddd239…`.

Everything else in §§2–5 of the audit — the trace verdict, the four map rows, the four-field
signatures, the placement square, the StatePort status block — replays exactly and I confirm it.

---

## 3. Classification pressed [PROVABLE — CONFIRMED, one reason corrected]

### 3.1 The selector, both directions

**Is the one SELECTOR genuinely unforced?** Yes — Q-900 settles it at the port's own bytes, against
my own prior ruling. `omega_phys` is an open supply with two conditions. And the audit does not
over-read it: the tally note and `B03` both carry the **common-origin certificate** condition, so the
selector is not a free choice of functional but a supply conditioned on descent. That distinction is
the whole difference between the reopener being real and the reopener being a way around K7, and the
audit has it right.

**Is anything else a selector in disguise?** I pressed all 27 other rows. The nearest neighbour is
**row 23, the target-independent admissibility manifest** — INPUT, on the reason that *"current law
does not choose its complete member set"*, and required to be frozen before downstream output. An
object whose member set law does not determine, which must be frozen prospectively, has exactly the
shape of a supply act. The audit's bright line — *"those remain construction/proof inputs until a
governing act expressly reserves supply"* — is defensible and I do **not** overturn it. I record row
23 as the one row to re-test at build time: if the member set proves underdetermined by law when
someone tries to construct it, it converts, and converting it late is worse than watching it now.

**The F_cyl lesson the other way** — a forced condition wrongly called a selector — returns nothing.
Only one row is typed SELECTOR and the registrar has ruled it open.

### 3.2 One reason is stale, and it matters at the build face

> Row 14: *"S1 measure package — **INPUT** — amplitudes and constraints admit no uniquely forced base
> family."*

My 981 proved, and the opposite lane's own check re-proved (`THEOREM = RE-PROVED`,
`IMPOSSIBILITY-CONFIRMED`, member 09), that for **positive** measures K4's complete solution set is

```text
lambda_N = mu tensor delta_1^(N-1),   mu an arbitrary finite positive measure on the first circle.
```

So once K4 is imposed the base family is forced **up to a single circle's worth of freedom** — and
every member of that class is unfaithful and refused by K7 as a selected history law. The **class
INPUT stands** (the package must still be constructed, and must still satisfy covariance, descent, and
the rest). **The reason does not.** A builder reading row 14 and `B06` would take the S1 arm to be
wide open; it is very nearly a point, and the shape of what remains is known.

Corrected reason, for row 14 and for `B06`: *the K4-admissible positive base family is determined up
to one circle by a sealed theorem; the package remains INPUT because covariance, common-origin
descent, and the K5–K7 legs are construction and proof work, not because the measure is free.*

The remaining 26 rows and the `17 INPUT / 4 FORCED-DOWNSTREAM / 1 SELECTOR / 6 MIXED` tally are
confirmed as typed.

---

## 4. The stop rule [PROVABLE — one clause refined]

Items 1–5 and 7 are right, minimal, and in the right order. Item 6 needs one distinction:

> *"6. A candidate route closes only by (a) a complete receiver-bound construction and replay, or
> (b) a TYPE-R refutation at the same receiver."*

Q-900 draws a line item 6 does not carry: my 977 and 981 verdicts are **IMPOSSIBLE-OF-INSTRUMENT**,
scoped to the instrument as constituted, and Q-900 says so in terms — *"every impossibility stands
AGAINST THE INSTRUMENT AS CONSTITUTED"* while *"THE REOPENER EXISTS OF RECORD."* An instrument-scoped
impossibility and a receiver-scoped `TYPE-R` close different amounts of the board. As written, item 6
lets a lane book the former as the latter and treat a route as permanently shut that a lawful
amendment reopens.

**Refinement, one clause:** *(b) a `TYPE-R` refutation at the same receiver closes the route
wall-to-wall; an impossibility scoped to a governing instrument closes it only under that instrument,
and the route reopens if a lawful amendment changes the clause the proof consumed — the reopening
being a principal act, not a lane's.*

One reading note, not a correction: item 6's *"Repeated pre-input attacks are barred"* is right as
written, and `repeated` is carrying the weight. Pre-input attacks are exactly what closed fork A;
the clause bars re-running one, not mounting a new one at a receiver where an earlier attempt stopped.

---

## 5. The basis list [PROVABLE — STANDS at 15, one row amended]

**Order: right.** I checked every dependency arrow. No map is asked to manufacture its domain or its
reference codomain; `B10` follows `B06`–`B09`; `B14` follows `B10`; every certificate/replay row
(`B15`) follows its proof objects; `B03` follows `B02`, which is necessary now that Q-900 makes the
common-origin certificate the reopener's wall rather than an afterthought.

**Minimal: yes, at the audit's object cut.** No row decomposes into two rows already present, and
none is implied by its predecessors.

**Downstream smuggled upstream: none found.** No verdict row precedes its proof object; `FORCED-
DOWNSTREAM` rows sit after the inputs that determine them; the existing C0/U1 references are consumed
at premise scope and correctly not counted as new build items.

**One amendment, from enumeration miss M2.** `B15` carries the P5 coverage leg as though the only
lawful discharge were the exclusion theorem. The axis audit gives **three**: cover P5 in the
admitted-family census; seal an upstream exclusion/equivalence theorem; or explicitly narrow the
branch as conditional. `B15`'s coverage leg should read as the disjunction, because two of the three
routes are cheaper than the one currently named and a builder is entitled to know that before
attacking the theorem.

**One note on `B03`.** Its *"common-origin provenance"* for `omega_phys` is a narrower object than
`B15`'s full certificate suite, so `B03` does not depend on `B15` and the order is sound — but the
basis does not say so, and a reader can see a cycle where there is none. Worth one clause.

---

## 6. Verdict [PROVABLE]

**AUDIT = CORRECTED. The spine stands.** Trace absent with subtraces partial; four required maps
absent with material partial; four-field signatures exact and all gated; placement square specified
and unfilled; one selector, conditioned on common-origin certification; a stop rule that installs
cleanly; a fifteen-item ordered basis that is minimal and acyclic. On the section's price, the audit
is right, and my 986 demand reading assembles against it without conflict.

Six moves, none reversing a class or deleting a row: three enumeration additions (M1–M3), one span
that does not replay (corrected span supplied), one requirement span truncating the record's own
non-demand (one number), one classification reason made stale by a sealed and confirmed theorem, one
stop-rule clause refined, one basis row widened from one route to three.

**Selectors remain the principal's.** Nothing here exercises, ranks, or recommends one.

---

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the audit's 25-member closure, its L/P/N census, its four-map table, its four-field spans, its
  28-row classification and tally, its stop rule and its fifteen-item basis; Q-899 and Q-900; the P5
  requirement and its status block; the census; the three missed objects; my own 981 theorem and its
  opposite-lane confirmation; all gates.

DERIVED HERE:
  an independent four-mode enumeration and its comparison against the audit's, in both directions;
  three named enumeration misses, each with its cost stated at bytes;
  the replay result over all 25 displayed spans, and the corrected span for the one that fails;
  the right-edge finding on the P5 requirement span and the one-number fix;
  the row-14 reason correction against a sealed theorem, with the class left standing;
  the row-23 watch item;
  the instrument-scoped versus receiver-scoped closure distinction for stop-rule item 6;
  the B15 three-route amendment and the B03 acyclicity note.

SELECTED HERE:
  nothing.  No descent object, map, trace, measure, certificate, manifest, or selector value is
  constructed, supplied, chosen, ranked, or recommended.  No class is reversed and no basis row is
  deleted.  No amendment is designed.

SCALING WEIGHTS: none consumed, fixed, or substituted.  No rank, ratio, or physical quantity was
  read, formed, or evaluated.
```

---

## 8. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A citation defect was not flattened into a
  substantive one; a supported claim with a wrong pin was not flattened into a wrong claim. A stale
  reason was not flattened into a wrong class. An instrument-scoped impossibility was not flattened
  into a receiver-scoped refutation — the entire point of §4. A cross-check was not flattened into a
  re-audit, and confirmation of a spine was not flattened into endorsement of every sentence.
- **THE CORRECTION AGAINST ME WAS TAKEN FIRST, at §0**, before any finding that might have been read
  as defending my 983. Q-900 supersedes my necessity claim; nothing in this artifact rests on it.
- **BUILDER-NEVER-VERIFIES:** the audit is the opposite lane's and the check is mine. Where the audit
  and my own 986 touch the same span I re-read the span rather than my own conclusion about it. The
  one place I cite my own prior work as ground (row 14) is a result the **opposite lane independently
  re-proved**, and I pin their confirmation alongside mine rather than resting on my own seal.
- **Q-898 READING RULE, both directions**, applied to every provenance call in the audit; the audit
  applies it correctly in its §1 and §9 and I found no locus/provenance conflation.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **PE-1..PE-15:** pointer-only, zero verdict weight.
- **CUSTODY:** archive-side only. One artifact, lane-tagged, sealed. The subject was verified against
  its own sidecar before reading, per the overnight clause, and its digest is recorded at member 02
  for the registrar's re-pin. No register, plan, tracker, git, commit, or push action.
- **BUILDER-B INDEPENDENCE:** `evaluator_build_A/` and `checks/` were not read.
- **REGISTER MOVEMENT NOTED, NOT CHARGED:** the audit pinned the register at `4a990973…`; at my read
  it is `a9ad6f82…`. The register is live-append, the audit says so, and its snapshot discipline is
  correct. Both digests are recorded so the registrar can see the interval.
- **BYTE AUDIT:** all 11 members re-hashed at seal time; 25 audit spans recomputed with 24 matching
  and 1 corrected; 2 new spans computed and pinned. Closure declared at byte 0, end computed on bytes
  as a fixed point; 27-token pre-closure scan over authored prose, 0 hits.

---

## 9. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 3936; scan 0 hits)
SUBJECT_DIGEST = 554f325abc8381456324aaaa99ed776dbf72e99771d4a7229a1b113596a8717d
                 (33,426 B; verified against its own sidecar before reading, per the overnight clause)
ENUMERATION = MISSED (3): M1 STAGE8_P5_FAMILY_EXCLUSION_THEOREM_ATTEMPT_V001.md 00300b0a... -- the
              record's own attempt at item 24, carrying a TYPE-P ad-hoc-assembly exclusion and a
              TYPE-R on the inclusive-identity mutation; M2 STAGE8_SECTION53_P5_AXIS_COVERAGE_AND_
              EXCLUSION_AUDIT_V001.md 1274b1b7... -- the coverage RELEASE, which names THREE lawful
              routes where the audit names one; M3 STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DESCENT_BUILD_
              ATTEMPT_V001.md f64d12de... -- the sealed build attempt at d_C0 with its stop reason.
              OVER = none.  Two further candidates deliberately not booked for want of proved cost.
STATUSES = CORRECTED (2 of 25 spans).  24/25 replay exactly.  (i) census overlay [21210,23740)
              ad2dfa97... does NOT replay (computed 94f190d0...); the file matches the audit's own
              pin, so this is a CITATION defect and the claim stands -- corrected span [13494,15328)
              950c497c... displays the four MATERIAL-PARTIAL map rows.  (ii) P5 requirement span
              [2044,5895) stops exactly at the "## 4." heading, truncating the record's own two
              TYPE-R non-demands; no classification distorted, but L1 is what a builder reads --
              fix [2044,5895) -> [2044,6922) 58ddd239...
CLASSIFICATION = CONFIRMED (28 rows, 17/4/1/6 tally).  The one SELECTOR is genuinely unforced --
              Q-900 settles it at the port's bytes AGAINST MY OWN 983, which I record at section 0 --
              and it is correctly conditioned on the common-origin certificate.  MOVES: row 14's
              REASON only ("no uniquely forced base family") -- my 981, re-proved by the opposite
              lane, forces the K4-admissible positive family to mu (x) delta_1^(N-1); class INPUT
              stands, reason corrected.  WATCH: row 23 (admissibility manifest) is the nearest
              neighbour to a selector; the audit's bright line is defensible and not overturned.
STOP_RULE = CORRECTED (item 6 only).  Add: a TYPE-R at the same receiver closes wall-to-wall, but an
              impossibility scoped to a governing instrument closes only under that instrument and
              reopens on a lawful amendment -- the distinction Q-900 draws and item 6 lacks.
BASIS = STANDS (15 items; order right, minimal, acyclic, nothing downstream smuggled upstream), with
              B15's coverage leg AMENDED from one route to the axis audit's three, and a one-clause
              note that B03's provenance object is narrower than B15's suite so the order is sound.
VERDICT = CORRECTED (spine stands; six moves, none reversing a class or deleting a row)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is §1's enumeration
standard: I booked three misses and declined two more for want of a provable cost, which means my
threshold — *name it only if I can state at bytes what the omission changes* — is itself doing work.
A lane applying a looser threshold would book five, a stricter one perhaps only M2. If the registrar
wants the census exhaustive rather than consequential, M4 and M5 return; that is a standard to set,
not a fact to find, and it belongs to whoever assembles the section's basis list.
