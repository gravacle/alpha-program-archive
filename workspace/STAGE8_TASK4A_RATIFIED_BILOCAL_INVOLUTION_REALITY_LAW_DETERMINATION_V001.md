# STAGE8 TASK 4A RATIFIED BILOCAL INVOLUTION REALITY LAW DETERMINATION V001

Date: 2026-08-02

Lane: CODEX LANE 1

Task: PASTE 385 / Task 4a

Register head tested: Q-302

## 0. LEAD VERDICT

`REALITY_LAW_VERDICT = D_GENUINE_INCOMPATIBILITY`

The ratified involution requires a bilocal **source functional** in the germ
exponent to be a symmetric complex-bilinear, `Theta_R`-even functional:

```text
Q_(-n)(Theta_R R) = conjugate(Q_n(R)).
```

On rank-one symmetric sources this is:

```text
b_(-n)(Theta_J f,Theta_J h) = conjugate(b_n(f,h)).
```

The ratified `Theta_R` acts slotwise by branch exchange, character inversion,
and coefficient conjugation. It does **not** exchange the two bilocal slots.
Slot exchange is a separate operation, called `tau_R` in U1_008. Because the
admitted source and the derived tensor are symmetric, inserting an argument
exchange gives the same equation and cannot change the failed sign.

The sealed finite controls distinguish this source law from the law of an
output Hessian. The exact finite amplitude is `Theta`-even, while
`W=-i hbar Log_0 Z` and its symmetric second derivative are `Theta`-odd:

```text
H_(-n)(Theta_J f,Theta_J h)
  = -conjugate(H_n(f,h)).
```

The Amendment-1 tensor

```text
b_n = i hbar L_n tensor L_n
```

has exactly this **output-Hessian odd parity**, but Amendment 1 inserts it as
the coefficient of an **even bilocal source functional** in the exponent.
That is the incompatibility. The Q-302 execution tested the correct source
law; it did not omit a load-bearing argument swap.

Candidate verdicts:

```text
(a) SESQUILINEAR_REQUIRED = false | TYPE-R
(b) EXECUTED_TEST_MIS_TRANSCRIBED_BY_OMITTED_SWAP = false | TYPE-R
(c) SEALED_INVOLUTION_ODD_NORMALIZER_EXISTS = false | TYPE-S
(d) CURRENT_RATIFIED_CLAUSES_JOINTLY_COMPATIBLE = false | TYPE-R
```

The provable answer is candidate (d). Any repair is a principal supersession
choice. No repair is executed here.

## 1. GATES AND CUSTODY

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The terminal flags above are process declarations and are Q-54-exempt.

```text
DOOR_D_U1_REALITY = FAIL | inherited from Q-302
DOOR_D_EXECUTION = FAIL | inherited from Q-302
DOR015_FREEZE_LIFTS = false | TYPE-C |
  constraint: no principal disposition and no U1-real amended source germ
DOWNSTREAM_DOR015_USE = false | TYPE-C |
  constraint: Q-302 freeze
NO_REPAIR_EXECUTED = true
```

Custody follows
`/Users/bgm/MB Work/alpha_supervision/LOCKED_PROCESS.md`: seal, verify,
mirror artifact and sidecar to both archive destinations, report, and stop.
No register, tracker, plan-file, git, commit, push, gate, or deployment action
is performed.

## 2. SCOPE

### 2.1 Roots entered

1. `/Users/bgm/MB Work/alpha_supervision/LOCKED_PROCESS.md`
2. `/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md`
3. `/Users/bgm/MB Work/alpha_supervision/DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md`
4. `/Users/bgm/MB Work/alpha_supervision/DECISION_OF_RECORD_014_SOURCE_GERM_PHYS_RATIFIED_2026-08-02_V001.md`
5. `/Users/bgm/MB Work/alpha_supervision/DECISION_OF_RECORD_014_AMENDMENT_1_CB_DERIVED_PAIRING_2026-08-02_V001.md`
6. `/Users/bgm/MB Work/alpha_supervision/DECISION_OF_RECORD_015_FIELD_SIGNATURE_PHYS_RATIFIED_2026-08-02_V001.md`
7. `STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md`
8. `STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md`
9. `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V001.md`
10. `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V004.md`
11. `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V005.md`
12. `STAGE8_TASK4A_DOR014_AMENDMENT1_DERIVED_PAIRING_AND_DOOR_D_REEXECUTION_V001.md`
13. `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V005.md`
14. `STAGE8_TASK4A_RECORD_SURFACE_TO_PHYSICAL_FIELD_SIGNATURE_DERIVATION_AND_BETA_GAP_ATTACK_V001.md`
15. `STAGE8_TASK4A_FINITE_HESSIAN_TO_DELTAPHI_TRANSPORT_MAP_CONSTRUCTION_AND_P_CH_TRACE_V001.md`
16. `STAGE8_TASK4A_FINITE_NONZERO_R_REFERENCE_EXACT_PROBE_SOURCE_COMPUTATION_V001.md`
17. `COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md`
18. `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md`
19. `../primitive_record_cell_selection_principle_v004.md`

All unqualified Stage-8 paths are rooted at:

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`

### 2.2 Exclusions

- `a32_holdout/custodian_private/` was not entered.
- No alternative involution, source space, pairing, coefficient, character
  law, Legendre convention, or physical source family was installed.
- No DoR-015-dependent downstream construction was entered.
- No physical response, inverse, fixed point, coupling, scale, root, spectrum,
  interval, or measured comparison was evaluated.
- No family member, rank pair, orientation, background, or endpoint frame was
  selected.

### 2.3 Word-bounded queries

```text
Theta_J
Theta_R
tau_R
r^sharp
symmetric bilocal
reality
Hermiticity
argument exchange
slot exchange
sesquilinear
odd normalization
involution-odd
hbar with Theta
Z_K[A_+,A_-]^*
W_(-n)
second derivative
```

### 2.4 F-GK3 premise declaration

`PREMISES_BEYOND_CURRENT_STACK = none`

No standard-QFT reality convention is imported. The law below is derived
only from the ratified definitions and checked against the corpus's exact
finite amplitudes.

## 3. CURRENCY AND AUTHORITY

The settled-question register was at Q-302 before analysis and immediately
before authoring.

```text
REGISTER_HEAD = Q-302
REGISTER_SHA256 = 9327028ef64e9cd6a25c855afcf83618f26ea669ca8642b38ee5316f6d45ebe7
```

| Authority | SHA-256 | Use |
|---|---|---|
| Q-302 Door-D re-execution | `365db191e3867129a02b2216fcf10b5fc83f53b0a03cef590dfe0671c63588d9` | Exact executed reality test |
| Attempted source germ V005 | `b3b0a7d8f6694221a2857b0ed68fe295f2a99232cf1d6a2ba5f32d810c12f67e` | Derived tensor and failed U1 certificate |
| DoR-008 | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | Ratified CTP carrier and source maps |
| Field/CTP presentation V002 | `76916244bdbcac7c2a6d4afae40f35127540d1d9e4cfc86fb72318506671161f` | Complex bilocal source and adjoint law |
| U1_008 | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` | Exact source involutions and physical domains |
| DoR-014 Amendment 1 | `8191feb4316ddeb08ef832b644c41847b31698224f6ab16b65dc1e1e2a574e56` | Required `i hbar L tensor L` pairing |
| Source germ V004 | `d4cdbb6623797df6accb7dc9b24134d179bfc8e8d039c585d5c91ae23255869e` | Live exponent reality law |
| Field signature V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | CTP reality seam and Door D |
| Q-290 derivation | `65e4dd6a6e2926c9c100edb162f57352311e73438f23dd19509dda0403e2e4f6` | Derived finite CTP signature |
| Q-243 finite transport | `70185aa842bc500724719c65bd66b5f07005e2214b97be0d35e07fd029d5c68c` | Exact finite Hessian and Keldysh blocks |
| Q-279 nonzero-R reference | `c7624d88aedaa0659755b5c566121b24c2ad0c29a0060daada43a31b22a6ddfb` | Finite source reality and derivative laws |
| Complete-Qspec finite amplitude | `273e1473a1a8bf0be0467634411cec1b7daeee0c9f24c330fad5d288d191dcbb` | Independent finite Hermiticity control |
| Raw-map spec | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | Raw-source domain conditions |
| Active v004 principle | `13d227ceb2198d96b0e4e2fef57b874cea71cb755320508fbf4c6d64a00c507e` | Symmetric bilocal Legendre convention |

`LATER_BEARING_RULING_FOUND = false | TYPE-S | roots: QUESTIONS_SETTLED_REGISTER_V001.md after Q-302; exclusions: unregistered drafts; fences: send-time currency; query: later U1, bilocal pairing, Door-D, Amendment-1, or DoR-015 disposition`

## 4. SYMBOL DISTINCTIONS

1. `Theta_R` is the anti-linear CTP **source** involution. It is not the
   operator adjoint `sharp`.
2. `tau_R` in U1_008 is a two-slot transposition. It is not the record-time
   symbol called `tau_R` elsewhere in the corpus. This artifact calls it
   `tau_slot` after quoting the source.
3. `R` is the symmetric bilocal source. Its letter does not mean retarded.
4. `L_n^Theta` is the accumulated linear-source covector. It is not the
   field-signature incidence map also called `L_N`.
5. `b_n` is the Amendment-1 bilinear coefficient inserted through `Q_n(R)`.
   It is not automatically a `W`-Hessian merely because it has the same
   parity as one.
6. `bar L` has no definition in the ratified source package. Complex
   conjugation of a linear functional is conjugate-linear on the same source
   space unless a separate conjugate-space or `Theta` transport is supplied.

These distinctions decide all three proposed rescues.

## 5. EXECUTED TEST, VERBATIM

The Q-302 re-execution states at
`STAGE8_TASK4A_DOR014_AMENDMENT1_DERIVED_PAIRING_AND_DOOR_D_REEXECUTION_V001.md:328-367`:

```text
L_(-n)(Theta_J f) = conjugate(L_n(f)),
Q_(-n)(Theta_R R) = conjugate(Q_n(R)).
```

For `R=f symtensor h`, it demanded:

```text
Q_(-n)^L(Theta_R R)
  = i hbar conjugate(L_n(f))conjugate(L_n(h)),

conjugate(Q_n^L(R))
  = -i hbar conjugate(L_n(f))conjugate(L_n(h)).
```

The transformation used branch/character exchange and conjugation in both
slots. It did not write an additional slot exchange.

```text
EXECUTED_SOURCE_REALITY_LAW = THETA_R_EVEN
EXECUTED_ARGUMENT_EXCHANGE_PRESENT = false | TYPE-R |
  test: exact displayed transformation law at Q-302 :337-352
```

The second flag describes the literal test, not yet whether omission was an
error. That question is decided below.

## 6. RATIFIED INVOLUTION, FROM ITS OWN DEFINITIONS

### 6.1 The C0 source carrier

The DoR-008-ratified field/CTP presentation defines at
`STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md:475-508`:

```text
D_J = C_c({+,-} x (Lambda without {0}); C),
D_R = D_J tensor_alg D_J,

s_R(r) = (1/2) sum r((a,lambda),(b,mu))
                  e_a(U_lambda)e_b(U_mu),

r^sharp((a,lambda),(b,mu))
  = conjugate(r((b,-mu),(a,-lambda))).
```

Thus the kinematic bilocal source is complex bilinear. The operator-adjoint
operation `sharp` conjugates, inverts characters, and exchanges slots.

### 6.2 The U1 source involution

U1_008 then defines at `:244-264`:

```text
(Theta_J j)(a,lambda)
  = conjugate(j(bar(a),-lambda)),

(Theta_R r)((a,lambda),(b,mu))
  = conjugate(r((bar(a),-lambda),(bar(b),-mu))).
```

The same source expressly says `Theta_R` is distinct from `r^sharp`.
`Theta_R` exchanges CTP branches and inverts both character labels but keeps
the bilocal slot order.

At `:322-347`, U1_008 separately defines slot exchange:

```text
(tau_R r)(I,J) = r(J,I),

D_R_U1 = {r in D_R : tau_R r=r and Theta_R r=r}.
```

After this quotation, write `tau_slot` for that `tau_R` to avoid the live
record-time symbol collision.

### 6.3 Derived finite signature and V005 seam

Q-290 restates the derived finite signature at
`STAGE8_TASK4A_RECORD_SURFACE_TO_PHYSICAL_FIELD_SIGNATURE_DERIVATION_AND_BETA_GAP_ATTACK_V001.md:262-282`:

```text
Theta_F: branch exchange plus adjoint,
R symmetric and Theta-real.
```

Field-signature V005 carries the same seam at `:527-534`: branch exchange and
conjugation preserve the invariant cylinder algebra and reverse cycle
orientation. It does not replace `Theta_R` with `sharp` or merge it with
`tau_slot`.

### 6.4 The induced law on a represented bilinear functional

For a symmetric rank-one source:

```text
R = f symtensor h,
Theta_R R = (Theta_J f) symtensor (Theta_J h).
```

This follows directly by substituting the slotwise definitions. If a
symmetric complex-bilinear kernel `b_n` represents `Q_n`, then the exponent's
ratified plus law is equivalent to:

```text
b_(-n)(Theta_J f,Theta_J h)
  = conjugate(b_n(f,h)).                              (RL-1)
```

Because `b_n` is symmetric, the argument-exchanged form is identical:

```text
b_(-n)(Theta_J h,Theta_J f)
  = b_(-n)(Theta_J f,Theta_J h).                     (RL-2)
```

Therefore adding a swap to (RL-1) cannot change its sign.

```text
RATIFIED_BILOCAL_SOURCE_LAW = symmetric_complex_bilinear_and_Theta_even
THETA_R_CONTAINS_SLOT_EXCHANGE = false | TYPE-R |
  test: U1_008 :244-264 versus separate tau_slot at :322-340
ARGUMENT_EXCHANGE_CHANGES_THE_LAW_ON_ADMITTED_SYMMETRIC_SOURCES = false | TYPE-R
```

## 7. FINITE GOLD-STANDARD COMPUTATION

### 7.1 Exact amplitude

The complete-Qspec finite authority states at
`COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md:26-37`:

```text
Z_K[A_+,A_-]^* = Z_K[A_-,A_+].
```

The finite source family used by Q-243/Q-279 has the same form:

```text
Z_n = (1-p) + p exp(Xi_n),
Xi_(-n)(Theta_src s) = conjugate(Xi_n(s)).
```

Hence:

```text
Z_(-n)(Theta_src s) = conjugate(Z_n(s)).             (FG-1)
```

This is the even source/amplitude law.

### 7.2 Logarithm and derivative parity

On the anchored `Log_0` branch, Q-279 proves at `:596-617`:

```text
W_(-n)(Theta_src s) = -conjugate(W_n(s)),
W_n = -i hbar Log_0 Z_n.                             (FG-2)
```

Differentiate (FG-2) along real parameters in directions `f,h`. The first
and second derivatives obey:

```text
D W_(-n)[Theta_J f]
  = -conjugate(D W_n[f]),

D^2 W_(-n)[Theta_J f,Theta_J h]
  = -conjugate(D^2 W_n[f,h]).                        (FG-3)
```

Since the Hessian is symmetric, exchanging `f,h` in (FG-3) changes nothing.

### 7.3 Exact Q-243 block

Q-243 gives at `:145-179`:

```text
H_C,N = i p(1-p) ell_delta tensor ell_delta.
```

Branch exchange sends the difference covector to minus its conjugate. Two
difference legs cancel that pair of minus signs, while anti-linearity changes
`i` to `-i`. Therefore:

```text
H_C,N(Theta_J f,Theta_J h)
  = -conjugate(H_C,N(f,h)).                           (FG-4)
```

This is exactly (FG-3). It is not the even source law (RL-1).

### 7.4 Old finite bilocal source control

The historical C-B source functional was explicitly symmetrized at Q-279
`:215-239`:

```text
Q_N(R)
  = (1/2)[B_N(R)+conjugate(B_N(Theta_R R))].
```

It therefore obeyed the even source law by construction. Its raw-G tensor
content was wrong, as Q-301 proved, but its U1 parity was the parity required
of a source insertion.

### 7.5 Independent exact sign table

Represent the coefficient `i` by the exact Gaussian-integer pair `(0,1)`.
The independent finite sign check returned:

```text
source-even law:
  transformed coefficient = (0, 1)
  conjugated coefficient   = (0,-1)
  PASS = false | TYPE-R

source-even law with argument exchange:
  transformed coefficient = (0, 1)
  conjugated coefficient   = (0,-1)
  PASS = false | TYPE-R

W-Hessian odd law:
  transformed coefficient = (0, 1)
  minus-conjugated         = (0, 1)
  PASS = true
```

No floating-point value or physical parameter entered this check.

### 7.6 Finite verdict

```text
FINITE_AMPLITUDE_THETA_EVEN = true | TYPE-P |
  premises: sealed complete-Qspec amplitude and DoR-008 U1
FINITE_W_HESSIAN_THETA_ODD = true | TYPE-P |
  premises: Q-243 and Q-279 exact finite formulas
AMENDMENT1_TENSOR_HAS_SOURCE_EVEN_PARITY = false | TYPE-R
AMENDMENT1_TENSOR_HAS_W_HESSIAN_ODD_PARITY = true | TYPE-P |
  premises: fixed anti-linear involution and exact coefficient
```

The finite structure does not rescue the amendment. It identifies the exact
object-role mismatch.

## 8. CANDIDATE (a): SESQUILINEAR DERIVATION

### 8.1 Domain test

The proposed expression

```text
s_n(f,h) = i hbar L_n(f) conjugate(L_n(h))
```

is linear in `f` and conjugate-linear in `h`. The ratified bilocal source is
`D_R=D_J tensor_alg D_J`, restricted to its symmetric complex-linear part.
A sesquilinear form acts on `D_J tensor conjugate(D_J)`, which is a different
source object.

It also is not symmetric in general:

```text
s_n(h,f) != s_n(f,h).
```

With the factor `i`, its argument-exchange adjoint is anti-Hermitian:

```text
conjugate(s_n(h,f)) = -s_n(f,h).
```

### 8.2 Same-correlator test

Q-301's pointwise reduction and Amendment 1 require, on every complex source
pair:

```text
b_n(f,h) = i hbar L_n(f)L_n(h).
```

The second Frechet derivative in that identity is complex bilinear. Replacing
one factor by its conjugate changes the tensor and fails the identity on the
complete complex source family.

A `Theta`-twisted operation could turn a conjugated covector back into a
linear one, but then one of two things happens:

1. it equals `L_n` and reduces to the already-failed Amendment-1 tensor; or
2. it differs from `L_n` and violates the unique same-correlator tensor.

No third case exists under the universal identity.

### 8.3 Candidate (a) verdict

```text
SESQUILINEAR_REQUIRED = false | TYPE-R
SESQUILINEAR_FORM_LIES_IN_RATIFIED_SYMMETRIC_BILOCAL_DUAL = false | TYPE-R
SESQUILINEAR_FORM_PRESERVES_UNCHANGED_SAME_CORRELATOR_IDENTITY = false | TYPE-R
SEALED_BAR_L_DEFINITION_FOUND = false | TYPE-S |
  roots: sections 2.1 and 6; exclusions: unrelated state-covariance forms;
  fences: no imported conjugate-space convention;
  query: bar L, conjugate-linear source leg, sesquilinear bilocal source
```

Candidate (a) is not forced and does not solve the current conjunction as
stated.

## 9. CANDIDATE (b): ARGUMENT EXCHANGE

### 9.1 What is actually ratified

The corpus already has an argument-exchanging operation: `r^sharp`. It is the
operator-adjoint covariance within a branch. The CTP source involution
`Theta_R` is expressly distinguished from it.

The physical bilocal source separately satisfies:

```text
tau_slot R = R,
Theta_R R = R.
```

Thus a kernel-style Hermiticity statement with argument exchange is allowed
as an equivalent notation on the symmetric source class, but it is not a
different law.

### 9.2 Direct sign test

For the Amendment-1 tensor:

```text
b_(-n)(Theta_J h,Theta_J f)
  = i hbar conjugate(L_n(h))conjugate(L_n(f))
  = i hbar conjugate(L_n(f))conjugate(L_n(h)),

conjugate(b_n(f,h))
  = -i hbar conjugate(L_n(f))conjugate(L_n(h)).
```

The scalars commute, so the swap leaves the failure unchanged.

### 9.3 Candidate (b) verdict

```text
ARGUMENT_EXCHANGE_IS_EQUIVALENT_ON_SYMMETRIC_B = true | TYPE-P |
  premises: tau_slot symmetry
ARGUMENT_EXCHANGE_REPAIRS_THE_SIGN = false | TYPE-R
EXECUTED_TEST_MIS_TRANSCRIBED_BY_OMITTED_SWAP = false | TYPE-R
```

Candidate (b) is refuted as a disposition. The executed test used the correct
source law.

## 10. CANDIDATE (c): INVOLUTION-ODD NORMALIZATION

### 10.1 Necessary algebra

Let a proposed real normalization `nu_n` modify the tensor:

```text
b_n^nu = i hbar nu_n L_n tensor L_n.
```

The even source law requires:

```text
nu_(-n) = -nu_n.                                    (C-1)
```

The unchanged same-correlator identity independently requires the exact
coefficient in both character sectors:

```text
nu_n = 1,
nu_(-n) = 1.                                        (C-2)
```

Equations (C-1) and (C-2) have no solution. An exact sign enumeration over
`{-1,0,1}` returned an empty survivor set; the symbolic contradiction does
not depend on that finite enumeration.

### 10.2 Search result

Word-bounded searches over the roots in section 2.1 found no sealed
involution-odd scalar line, character-odd normalization, or transformation
law in which `hbar` acquires an additional odd parity. The only occurrences
bearing on this question are the Q-302 proposal itself and the failed
Amendment-1 tensor.

### 10.3 Candidate (c) verdict

```text
SEALED_INVOLUTION_ODD_NORMALIZER_EXISTS = false | TYPE-S |
  roots: sections 2.1 and 3 authority set;
  exclusions: unrelated odd fields and record-time uses;
  fences: word-bounded search, no imported pseudoscalar;
  query: involution-odd, odd normalization, Theta with hbar,
         normalization with involution

ODD_NORMALIZER_PRESERVES_UNCHANGED_SAME_CORRELATOR_IN_BOTH_CHARACTERS
  = false | TYPE-R

CANDIDATE_C_IMPLEMENTED = false | TYPE-U |
  would-build: an explicitly authored odd line/normalizer, its source-domain
               transformation law, an amended same-correlator normalization,
               and complete finite U1 and Door-D certificates
```

Candidate (c) is not a latent sealed solution. It becomes a principal theory
change because it must modify more than the tensor coefficient.

## 11. CANDIDATE (d): GENUINE INCOMPATIBILITY

The current stack imposes all four clauses:

1. `Theta_R`-even symmetric bilocal source functional.
2. Nontrivial involution-covariant linear source `L_n`.
3. Exact same-correlator coefficient `i hbar L_n tensor L_n` in both
   character sectors.
4. No additional odd normalizer or altered character law.

Clauses 1-4 imply simultaneously:

```text
i hbar conjugate(L_n(f)L_n(h))
  = -i hbar conjugate(L_n(f)L_n(h)).
```

The retained charge/flux access supplies admitted directions with nonzero
product. Therefore the conjunction is refuted.

```text
CURRENT_RATIFIED_CLAUSES_JOINTLY_COMPATIBLE = false | TYPE-R
GENUINE_INCOMPATIBILITY_PROVED = true | TYPE-P |
  premises: DoR-008 U1, DoR-014 Amendment 1, active v004 source convention
```

The incompatibility is not evidence that the finite amplitude violates its
own symmetry. The finite amplitude passes. It is evidence that a tensor with
the parity of the finite `W`-Hessian was inserted into a source slot requiring
the parity of the finite exponent.

## 12. PROVABLE VERSUS PRINCIPAL CHOICE

### 12.1 PROVABLE

```text
PROVABLE_1 = Theta_R is slotwise; tau_slot is separate.
PROVABLE_2 = the physical bilocal source is symmetric and Theta_R-even.
PROVABLE_3 = argument exchange is redundant on that symmetric source class.
PROVABLE_4 = the sealed finite amplitude/exponent is Theta-even.
PROVABLE_5 = W and its finite symmetric Hessian are Theta-odd.
PROVABLE_6 = i hbar L tensor L has the odd output-Hessian parity.
PROVABLE_7 = candidates (a), (b), and (c) do not satisfy all unchanged clauses.
PROVABLE_8 = at least one ratified/amended clause must be superseded.
```

### 12.2 YOURS: neutral disposition options for the principal

No option is selected.

#### Option Y1: preserve the U1 source package

Keep `Theta_R`, `tau_slot`, the symmetric complex-bilinear source, and the
finite amplitude law. Supersede Amendment 1's use of the same-correlator
coefficient as an exponent-source pairing, and re-specify the raw
same-correlator normalization or source-to-output map.

Price: the Q-301 pointwise identity is no longer implemented by the current
bilocal source derivative without an additional derived map.

#### Option Y2: preserve Amendment 1's same-correlator tensor

Change the physical bilocal source parity or source carrier so that the odd
tensor is admissible, then rebuild U1 source symmetry and rerun every finite
reality restriction.

Price: this supersedes a DoR-008-derived U1 component and the active v004
source domain. The complete-Qspec amplitude remains the standing falsifier.

#### Option Y3: author an odd orientation line

Introduce a declared involution-odd line or normalizer and amend the
same-correlator identity to consume it consistently in both character
sectors.

Price: this is new authored structure, not a derived normalization. The
current same-correlator equation cannot remain unchanged.

#### Option Y4: separate source and output tensors explicitly

Retain an even bilocal source functional in the exponent and construct a
separate, certified map from its source derivative to the odd physical
`W`-Hessian/raw output tensor.

Price: the map is a new load-bearing object and must prove the Door-D identity
rather than naming two differently transforming tensors as equal.

```text
PRINCIPAL_DISPOSITION_SELECTED = false | TYPE-C |
  constraint: principal act required
```

## 13. FINAL TYPED LEDGER

```text
REALITY_LAW_VERDICT = D_GENUINE_INCOMPATIBILITY

RATIFIED_BILOCAL_SOURCE_LAW = THETA_EVEN_SYMMETRIC_COMPLEX_BILINEAR
FINITE_AMPLITUDE_REALITY_LAW = THETA_EVEN
FINITE_W_HESSIAN_REALITY_LAW = THETA_ODD

SESQUILINEAR_REQUIRED = false | TYPE-R
SESQUILINEAR_FORM_LIES_IN_RATIFIED_BILOCAL_DUAL = false | TYPE-R
SESQUILINEAR_PRESERVES_SAME_CORRELATOR = false | TYPE-R

ARGUMENT_EXCHANGE_IS_EQUIVALENT_ON_SYMMETRIC_B = true | TYPE-P |
  premises: tau_slot symmetry
ARGUMENT_EXCHANGE_REPAIRS_SIGN = false | TYPE-R
EXECUTED_TEST_MIS_TRANSCRIBED_BY_OMITTED_SWAP = false | TYPE-R

SEALED_INVOLUTION_ODD_NORMALIZER_EXISTS = false | TYPE-S
  roots: section 2.1
  exclusions: unrelated odd fields and record-time symbols
  fences: word-bounded search; no imported pseudoscalar
  query: involution-odd, odd normalization, Theta/hbar parity

ODD_NORMALIZER_PRESERVES_UNCHANGED_IDENTITY = false | TYPE-R
ODD_NORMALIZER_IMPLEMENTED = false | TYPE-U
  would-build: odd line/normalizer plus amended source and identity package

CURRENT_RATIFIED_CLAUSES_JOINTLY_COMPATIBLE = false | TYPE-R
GENUINE_INCOMPATIBILITY_PROVED = true | TYPE-P |
  premises: DoR-008 U1, DoR-014 Amendment 1, active v004

THETA_R_CONTAINS_SLOT_EXCHANGE = false | TYPE-R
ARGUMENT_EXCHANGE_CHANGES_LAW_ON_SYMMETRIC_SOURCE = false | TYPE-R

SEALED_BAR_L_DEFINITION_FOUND = false | TYPE-S
  roots: section 2.1
  exclusions: unrelated state-covariance forms
  fences: no conjugate-space import
  query: bar L, sesquilinear bilocal source, conjugate-linear source leg

LATER_BEARING_RULING_FOUND = false | TYPE-S
  roots: QUESTIONS_SETTLED_REGISTER_V001.md after Q-302
  exclusions: unregistered drafts
  fences: send-time currency
  query: U1, bilocal pairing, Door D, Amendment 1, DoR-015 disposition

DOR015_FREEZE_LIFTS = false | TYPE-C
  constraint: Door D remains failed pending principal disposition
DOWNSTREAM_DOR015_USE = false | TYPE-C
  constraint: Q-302 freeze

PRINCIPAL_DISPOSITION_SELECTED = false | TYPE-C
  constraint: principal act

PHYSICAL_RETHESS_P_CLEAN = NO_VERDICT
  reason: no admissible U1-real raw source object reaches the lift
```

## 14. TERMINAL PROCESS FLAGS

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The terminal process flags are Q-54-exempt. The holdout was untouched.
