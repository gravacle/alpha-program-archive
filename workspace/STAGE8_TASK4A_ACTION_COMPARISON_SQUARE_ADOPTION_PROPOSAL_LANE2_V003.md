# STAGE8 TASK 4A: ACTION-COMPARISON SQUARE ADOPTION PROPOSAL — LANE 2 V003

Date: 2026-08-03  
Task: PASTE 445 / Task 4a / bounded R1 automorphism-covariance repair  
Lane: CODEX LANE 2  
Register head at preflight: Q-362  
Reserved ruling: DoR-017  
Custody: builder repair; Lane 1 review required

**PROPOSED_NOT_ADOPTED — PENDING REVIEW AND PRINCIPAL RATIFICATION
(DoR-017 RESERVED)**

```text
LEAD_RESULT = R1_AUTOMORPHISM_COVARIANCE_CERTIFICATE_INSTALLED

LIVE_ROUTE = Variant Q | FORCED_BY_Q358 | not an authored choice
VARIANT_QE = BANKED_RECORD_ONLY | no live field, leg, or choice row

FORCED_OPERATION = D_G^*:f |-> f compose D_G
ALGEBRAIC_TENSOR_COMPARISON_FAMILY = DELETED_FROM_LIVE_PROPOSAL

LIVE_AUTHORED_RESIDUE = EXACTLY_FIVE_ITEMS
MEMBER_NORMALIZATION_UNIQUENESS_FROM_RATIFIED_STOCK = REFUTED
MEMBER_NORMALIZATION = EXPLICIT_COVARIANT_DOR017_RATIFICATION_ITEM |
  none selected

R1_REALIZATION_AUTOMORPHISM_COVARIANCE = REQUIRED
RANK_TWO_CYCLE_SELECTIVE_MEMBER = EXCLUDED | permanent regression

PHYSICAL_BOTTOM_LEG_FALSIFIER = PASS_WITHIN_PROPOSAL_ON_CORRECTED_SQUARE
READY_FOR_LANE1_REVIEW = yes
READY_FOR_DOR017_RULING = conditional_on_review

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

V003 retains V002's five-kill repair verbatim except for the bounded covariance
changes listed in Section 8A.  It does not attempt to rescue V001's comparison-map family.  The canonical
comparison is already derived.  This proposal contains that forced operation
exactly and authors only the five missing physical objects named by Q-358 and
Q-360.  The one genuinely outcome-bearing freedom is now restricted to
realization-covariant members and placed before the principal as the DoR-017
item; no lane choice is made.

---

## 0. Preflight, authority, and collision ledger

### 0.1 Preflight

The Q-362 register head and all load-bearing hashes were verified before the
re-adjudication was read:

| Authority | Verified SHA-256 | Role |
|---|---|---|
| Q-362 V002 re-adjudication | `8629dd235a7ea072862381c9ba09128a3dd1fe650ec03ff5af900f7c54660c62` | bounded M6/M7 kill and rank-two witness |
| Q-360 adjudication | `9521e9970704beca8818389df972e099dc1d2f7cd1c0c5b1254dd09fb25c9364` | five kills and repair contract |
| Q-358 derive standard | `a9b733c711a692d5eedad8ae6acb5e2829c357c2c6aa3870c1aca2c570604136` | forced diagram and five-item residue |
| descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | `D_G`, kernel, restrictions, regressions |
| extension V002 | `eb3675d525af7d1420c4ed033a5e5b94eb7494c1bac1305029b25ac9169567a0` | scoped `T^char` and `j_NM^Q` square |
| DoR-015 / FIELD_SIGNATURE V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical quotient and two-route typing |
| DoR-016 / network law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled CTP access and prefix traces |
| transverse-action draft | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | `phi_div`, `Flat(S)`, bridge requirements |
| V002 under repair | `5b4229fd4ba5cc5d8180a91a923c6293c95d71b929f003626363603803a6a30c` | bounded delta base |

`LOCKED_PROCESS.md` was read in full.  The live register sidecar verified from
its own supervision directory, and its head was Q-362.

```text
DOES_THE_OBJECT_EXIST = yes | V002 plus one bounded covariance repair
IS_THE_VERSION_CURRENT = yes | Q-362
ARE_THE_INPUTS_PRESENT = yes | explicit witness and covariance standard
PREFLIGHT = PASS
```

### 0.2 Load-bearing symbol collisions

```text
D_G^*       = pullback of scalar quotient actions, not a freely authored map;
D_G^*       != the cycle-lattice dual carrying the same typography elsewhere;
rho_f       = Q_M -> Q_N, so rho_f^*F_N=F_N compose rho_f;
rho_Gamma,N = proposed physical-action restriction, not rho_raw,N;
rho_H,N     = proposed physical-Hessian restriction, not a source Hessian map;
Gamma_fin,N = finite source log, not the independent physical bottom action;
R_CTP       = relative endpoint endomorphism, not bilocal probe R;
phi_div     = quotient transverse-action correction, not Z_N or -Log Z_N;
T^char      = scoped terminal character, not D_G on pendant content.
family naturality = covariance under signed realization isomorphisms;
stage covariance  = restriction behavior under N<=M;
family naturality != stage covariance.
```

### 0.3 Exact adjudication disposition

```text
J1 -> K1: forced diagram and contravariance repaired in Section 1;
J3 -> K2: exactly five residue items in Section 2;
J4 -> K3: A3 withdrawn; A8/A10 retained verbatim in Section 3;
J6 -> K4: physical bottom-leg falsifier executed in Section 4;
J7 -> K5: uniqueness attacked and DoR-017 freedom exposed in Section 5;
J2 PASS: Variant Q forced; QE banked only;
J5 PASS: hostile witnesses retained and rerun.
M6 -> N1/N3: R1 and the DoR-017 fiber restricted to covariant members;
M7 -> N2/N4: rank-two cycle-selective witness installed and battery rerun.
```

---

## 1. K1 — the forced diagram, exactly and with lawful variance

### 1.1 Verbatim derived-standard block

The following block is transcribed from Q-358's forced construction.  Its
mathematics is premise-marked derived content, not proposal content.

```text
Tr_pref,N^009 : H_N^CTP -> P_G^fam,
Tr_pref,N^009(h) = (Z_0^CTP,...,Z_N^CTP),
Z_0^CTP=1.

r_j^n = (Z_(j-1)^CTP)^(-1) Z_j^CTP.              (S-1)

D_G:P_G^fam -> Q_G,
D_G([eta,Z])=q_G(Phi_eta(Z)),

h_(epsilon(j))=r_j^(s_j),

Hol_G(D_G([eta,Z]))(c)
 =product_j r_j^(s_j c_(epsilon(j))).             (S-2)

Act_N^quot := {f:Q_G->C with the declared action regularity and reality law}.

Comp_N^forced := D_G^*,
D_G^*:Act_N^quot -> Fun(P_G^fam,C),
(D_G^* f)(Z)=f(D_G(Z)).                           (S-3)

D_G^* f=D_G^* g  implies f=g.                     (S-4)

rho_f^*(a_N)=a_N compose rho_f,
P_f^*(b_N)=b_N compose P_f.

D_M^* rho_f^* = P_f^* D_N^*.                     (S-5)
```

The corresponding forced diagram is also carried exactly:

```text
H_N^CTP
  | Tr_pref,N^009
  v
P_G^fam ----------------D_G----------------> Q_G
  |                                          |
  | evaluate D_G^* f                         | f in Act_N^quot
  v                                          v
  C -------------------------identity-------> C
```

The scalar composite for an already-instantiated finite quotient action is

```text
f compose D_G compose Tr_pref,N^009.              (S-8)
```

No `F_M compose rho_f` occurs anywhere.  Since

```text
rho_f:Q_M->Q_N,
F_N:Q_N->C,
```

the only lawful coefficient pullback is

```text
rho_f^*F_N=F_N compose rho_f:Q_M->C.               (K1-1)
```

### 1.2 Direct proof of the corrected square

For `Z_M in P_M^fam`, use `D_N P_f=rho_f D_M`:

```text
[D_M^* rho_f^*F_N](Z_M)
 =(rho_f^*F_N)(D_M Z_M)
 =F_N(rho_f(D_M Z_M))
 =F_N(D_N(P_f Z_M))
 =[P_f^*D_N^*F_N](Z_M).                           (K1-2)
```

Every domain and codomain now matches.  `(K1-2)` proves `(S-5)` rather than
using it as a membership condition.

```text
V001_B1_18A = DELETED
F_M_COMPOSE_RHO_F = absent
D_G_STAR = INSTALLED_VERBATIM_FROM_DERIVED_STANDARD
CORRECTED_PULLBACK_SQUARE = PASS | derived
```

### 1.3 `T^char` scope, unchanged

On the sealed square and exactly on `im(j_NM^Q)`, Q-313/extension V002 give

```text
T_N^char = Hol_(c_square)^(-1) compose Z_N,
D_(G_square)=T_N^char,
f compose D_G=f compose T_G^char.                 (K1-3)
```

Off that image, especially on the pendant witness, `T^char` is not used as
the physical descent.  No cycle-creating upward quotient map is asserted.

### 1.4 Complete Q-408 placement

The formal placement is carried in full, conditionally on the proposed
stationary package in R5:

```text
Gamma_phi = Gamma_base + phi_div,
Y=C_prop direct-sum K_cycle,

Delta H_CC = D_C^2 phi_div,
Delta H_CK = D_C D_K phi_div,
Delta H_KC = D_K D_C phi_div,
Delta H_KK = D_K^2 phi_div,                       (K1-4)

delta c_phi[psi]
 =-[D_C^2 Gamma_phi]^(-1) D_C psi(c_phi,k).       (K1-5)
```

`(K1-5)` is licensed only on the separately certified complement reducing
domain.  It never inverts a cycle direction.

### 1.5 Full DoR-008 restriction obligations

V003 carries all five derived-standard obligations:

```text
rho_Gamma,N : Act_phys^017 -> Act_N^quot,
rho_H,N     : RetHess_phys^017 -> RetHess_N^bot.

1. rho_Gamma,N is compatible with (S-5), reality, and batching;
2. differentiation commutes with rho_Gamma,N on the declared domain;
3. rho_H,N transports the physical block/Schur construction to an
   independently instantiated finite physical action bottom leg;
4. finite restriction does not silently commute with inversion without a
   reducing/intertwining theorem;
5. every corresponding Q-243/Q-279 shadow is reproduced exactly:
     equal-history identity;
     exact DoR-009 trace upstream;
     zero and p-free finite cycle/source mixed block;
     exact symbolic noise/dephasing block;
     reality, identity extension, and batching.
```

R2–R5 below instantiate these interfaces as proposal fields.  Their tests
are executed in Section 4; no source Hessian is promoted to an action
Hessian.

---

## 2. K2 — exactly five live residue items

Define one proposed package, indexed by an unselected ratification member
`m`:

```text
ACS_017(m):=(R1_m,R2_m,R3_m,R4_m,R5_m).           (K2-0)
```

There is no sixth live authored object.  `D_G^*` is derived, and all QE
material is segregated in Section 6.

### R1 — divergence/action datum and generated quotient member

An R1 member is the tuple

```text
R1_m:=(delta_div,m,Depth_m,Accum_m,Gen_m,phi_m,Norm_m,nu_m),

Gen_m(delta_div,m)=phi_m in Act_phys^017,          (R1-1)
```

with the following exact certificate:

```text
delta_div,m = certified divergence/action datum with declared units;
Depth_m     = typed common-origin depth map into the record system;
Accum_m     = stage-compatible accumulation/subtraction rule;
phi_m       = scalar quotient action on X_phys^005/K_cycle only;
phi_m       is smooth, reality-covariant, quotient-compatible,
              all-orders flat on the active section, and anchored by
              phi_m(identity)=0;
Norm_m      = declared homogeneous normalizer on the generated image;
Norm_m(phi_m)=nu_m with units declared;
Gen_m       is target-independent and uses no response or alpha datum.
```

The R1 certificate now also includes **realization-automorphism covariance**.
Let `Aut_adm` be the full admitted signed-realization groupoid.  An arrow

```text
alpha:G->G'
```

acts by the signed edge/cycle map `alpha_*`, the quotient isomorphism
`alpha_Q:Q_G->Q_(G')`, the induced physical-action transport

```text
alpha_Act(phi_G):=phi_G compose alpha_Q^(-1),
```

and the corresponding actions on divergence/depth data, finite reference
tables, jets, and bottom legs.  Every fixed member admitted to alternative N
must be a natural section satisfying

```text
Gen_(G',m)(alpha_Div delta_(G,m))
  =alpha_Act Gen_(G,m)(delta_(G,m)),

phi_(G',m) compose alpha_Q = phi_(G,m),

Norm_(G',m)(alpha_Act phi_(G,m))
  =Norm_(G,m)(phi_(G,m))=nu_m,

I_(G',m)^bot compose alpha_Ref
  =alpha_Jet compose I_(G,m)^bot,

Gamma_base,(G',m)^bot compose alpha_Q
  =Gamma_base,(G,m)^bot,

b_(G',m)^bot compose alpha_Q=b_(G,m)^bot,

H_(G',m)^bot alpha_D
  =alpha_D H_(G,m)^bot,

rho_Gamma,(G') alpha_Act
  =alpha_Act,fin rho_Gamma,G.                     (R1-COV)
```

The same equations cover edge exchanges and simultaneous relabelings.  For
an orientation reversal, `alpha_*c=-c` and the quotient character obeys
`H_(-c)=conjugate(H_c)`; `(R1-COV)` therefore carries the sign action rather
than erasing it.

There are exactly two lawful covariance standings:

```text
fixed-member mode:
  one natural section obeying (R1-COV) at every object and arrow;

family mode:
  the complete Aut_adm orbit/fiber, closed under every alpha action,
  with no member selected and no member-sensitive result emitted.
```

A bare cycle label, cycle basis, realization name, oriented representative,
or persistent-path label is not a covariant datum.  Any member needing one is
void.  A newly authored physical symmetry-breaking datum could change that
only through its own future gate; none is present here.

The admissible R1 fiber is called `M_017`.  It includes a distinguished zero
member and only those certified nonzero generated members satisfying
`(R1-COV)`.  Nothing in this artifact selects an element of `M_017`.

```text
R1_STANDING = PROPOSED_NOT_ADOPTED
R1_MEMBER_SELECTED = false
R1_REALIZATION_AUTOMORPHISM_COVARIANCE = REQUIRED
R1_COVARIANT_FAMILY_WITHOUT_SELECTION = permitted_only_in_nonclosing_F
```

### R2 — completed-to-finite physical action restrictions

The proposed completed action class is the class of scalar quotient actions
equipped with a unique finite-coordinate family and vertical increments:

```text
Act_phys^017 := {
  a:X_phys^005->C;
  (a_N)_N, a_N in Act_N^quot;
  (v_(M|N))_(N<=M);
  rho_Gamma,N(a)=a_N;
  a_M=a_N compose rho_(M,N)+v_(M|N);
  v_(L|N)=v_(M|N) compose rho_(L,M)+v_(L|M);
  v_(M|N)=0 on cycle-rank-preserving identity extensions;
  a(x) is the declared renormalized limit of a_N(x_N)
}.
                                                               (R2-1)
```

`rho_(M,N):Q_M->Q_N` is the physical carrier restriction.  A cycle-creating
addition may carry a finite-tested vertical increment; no representative-
independent upward quotient map is invented.  The topology is the subspace
topology generated by all finite seminorms plus convergence of the declared
renormalized scalar net.  The common finite-restriction tail is zero because
two members with every `a_N` equal are equal in this class.

Differentiation is part of the R2 certificate:

```text
rho_H,N(D^2 a)=D^2(rho_Gamma,N a)                 (R2-2)
```

on the common domain specified in R5.  Reality and batching act
coordinatewise.  `rho_Gamma,N` is therefore an explicit coordinate
restriction, not V001's partially typed `rho_f^Act`.

```text
R2_STANDING = PROPOSED_NOT_ADOPTED
R2_COMPLETION_TOPOLOGY = finite-seminorm_plus_declared-renormalized-limit
R2_UNFLAGGED_COMPLETION = false
```

### R3 — independent finite physical action bottom leg

For every finite `G_N`, R3 supplies a primitive physical-action object

```text
Jet_N^phys:=Act_N^quot/Flat(S_N),
pi_Jet,N:Act_N^quot->Jet_N^phys,
Ref_N^sealed:=the complete basis-free Q-243/Q-279 finite reference table,
I_N^bot:Ref_N^sealed->Jet_N^phys,
BaseJet_N^bot:=I_N^bot(Ref_N^sealed),
Gamma_base,N^bot in pi_Jet,N^(-1)(BaseJet_N^bot),

Bottom_N^m:=(I_N^bot,BaseJet_N^bot,Gamma_base,N^bot,b_N^m,
             Gamma_phi,N^bot,H_N^bot),

b_N^m:Q_G->C,
Gamma_phi,N^bot=Gamma_base,N^bot+b_N^m.           (R3-1)
```

`I_N^bot` is the independently authored role-realization from the sealed
finite table into the physical action-jet carrier.  It is not `-Log Z_N`,
`Gamma_fin,N`, a source Hessian, an identity, or a pullback.  Its provenance
field is the sealed reference as a falsifier target plus the independently
declared physical quotient jet space.  Injectivity and exact coefficient
reproduction are failure-capable requirements, not source/action
identification.  `Gamma_base,N^bot` is an independently supplied member of
the displayed jet fiber; it is part of the unselected DoR-017 package, not a
silently chosen section of `pi_Jet,N`.

Its finite target table is:

```text
equal history: normalized identity and b_N^m(identity)=0;
retarded candidate: finite (delta,c) block exactly zero and p-free;
noise/probe: exact Q-279 symbolic block table, including
  kappa_eta=p(1-p)exp(-eta/2)/(1-p+p exp(-eta/2))^2;
reality: covariant under the ratified involution;
extension: identity zero-extension exact;
batching: exact on every declared finite prefix.
                                                               (R3-2)
```

`BaseJet_N^bot` owns the nonzero sealed noise/dephasing terms.  Because
`b_N^m` is flat on the active section, it changes none of those sealed jets.
This ownership prevents a rescaled transverse correction from rescaling the
finite base physics.  The use of the quotient `Jet_N^phys` also makes the
independence exact: the reference fixes the physical finite germ,
`Gamma_base,N^bot` realizes that germ independently, and R4 separately tests
the flat off-section member.  Moving a flat term between
`Gamma_base,N^bot` and `b_N^m` changes the R1/R4 member split and is therefore
part of the disclosed member/normalization freedom, not a gauge equivalence.

```text
R3_STANDING = PROPOSED_NOT_ADOPTED
FINITE_SOURCE_ACTION_IDENTITY_USED = false
```

### R4 — scalar comparison equality and normalization

The independent bottom leg and completed restriction must satisfy

```text
D_G^* b_N^m
 =D_G^*(rho_Gamma,N phi_m)                        (R4-1)
```

as functions on `P_G^fam`.  Since `D_G` is surjective, `(R4-1)` is equivalent
to the physical scalar equality

```text
b_N^m=rho_Gamma,N phi_m on Q_G.                   (R4-2)
```

The normalization and unit clauses are

```text
Norm_N:Flat(S_N)->U_action is homogeneous;
Norm_N compose rho_Gamma,N = Norm_m on the generated R1 line;
Norm_N(b_N^m)=Norm_m(phi_m)=nu_m,
[b_N^m]=[phi_m]=action,
[D_G^*]=dimensionless pullback.                   (R4-3)
```

The equality is a failure-capable certificate because `b_N^m` is supplied
independently in R3.  It is not made true by defining the bottom leg as the
right-hand side.  A failure at any finite stage voids the proposed member
`m` and therefore its DoR-017 adoption.

It is also stable under the new certificate.  From

```text
D_(G') alpha_P=alpha_Q D_G
```

and `(R1-COV)`, both sides of `(R4-1)` at `G'`, precomposed with
`alpha_P`, equal their corresponding sides at `G`.  The normalizer equality
is preserved by the third line of `(R1-COV)`.  Thus no presentation label can
enter through R4 after being excluded from R1.

```text
R4_STANDING = PROPOSED_NOT_ADOPTED
R4_NORMALIZATION_MEMBER_SELECTED = false
R4_REALIZATION_COVARIANCE = PASS_WITHIN_PROPOSAL
```

### R5 — stationary physical 2PI package

For each admitted R1 member, R5 is the tuple

```text
R5_m:=(Y,D_017,Gamma_base,Gamma_phi,Crit_m,G_star,m,
       H_CC,H_CK,H_KC,H_KK,C_red,Inv_CC,Schur_m,
       RetExtract_m,rho_H,N,RestrictDiffCert,RestrictInvCert,
       Contour_m,Boundary_m,Closure_m,Tail_m),      (R5-1)
```

with:

```text
Y=C_prop direct-sum K_cycle;
D_017 = common invariant dense domain for every displayed block;
Gamma_phi=Gamma_base+phi_m;
Crit_m={y in D_017:D_C Gamma_phi(y)=0};
G_star,m = stationary two-point member/family on Crit_m;

pi_Jet,N(rho_Gamma,N(Gamma_base))=BaseJet_N^bot;
rho_Gamma,N(Gamma_phi)=Gamma_phi,N^bot;

H_m=[[H_CC,H_CK],[H_KC,H_KK]];
C_red is reducing for H_CC;
Inv_CC is the two-sided inverse/prescription on C_red only;
Schur_m=H_KK-H_KC Inv_CC H_CK;
RetExtract_m = sealed Keldysh/retarded extraction on Schur_m;
no inverse acts on K_cycle merely because it is a cycle direction.
                                                               (R5-2)
```

The completed contour is the E_post-oriented doubled CTP contour; boundary
data preserve the ratified branch glue; `Closure_m` is the graph closure on
`D_017` in P2's topology; `Tail_m` is required to have zero image under all
alpha-facing finite restrictions.  These are proposal fields inside R5, not
silent imports.

The restriction/inverse certificate is the reducing intertwiner theorem:

```text
rho_H,N H_AB = H_AB,N rho_D,N for AB in {CC,CK,KC,KK};
rho_D,N C_red subset C_red,N;
rho_D,N Inv_CC = Inv_CC,N rho_D,N on C_red;

rho_H,N(Schur_m)=
  H_KK,N-H_KC,N Inv_CC,N H_CK,N.                  (R5-3)
```

Together with `(R2-2)`, this instantiates the complete Q-408 placement
`(K1-4)`–`(K1-5)` without silently commuting restriction and inversion.

```text
R5_STANDING = PROPOSED_NOT_ADOPTED
R5_CYCLE_INVERSION = forbidden
R5_RESPONSE_OR_P_VERDICT_COMPUTED = false
```

### 2.6 One-to-one residue board

| Q-358 residue | V003 live field | Proposal status | Extra live authorship |
|---|---|---|---|
| 1. divergence/action datum and generated member | R1 | complete typed ratification fiber; member unselected | none |
| 2. `rho_Gamma,N` | R2 | explicit coordinate restrictions, topology, vertical cocycle | none |
| 3. independent finite bottom leg | R3 | independent role map into the physical action-jet quotient plus the flat member and exact target table | none |
| 4. scalar equality and normalization | R4 | failure-capable equality; normalizer is DoR-017 item | none |
| 5. stationary physical 2PI package | R5 | complete tuple, domain, blocks, inverse and restrictions | none |

```text
FIVE_ITEM_COVERAGE = 5_PROPOSED / 5
ALGEBRAIC_TENSOR_ANSATZ = absent
COLLECTIVE_SEPARATION_MEMBERSHIP_RULE = absent
QE_LIVE_CONTENT = absent
```

---

## 3. K3 — corrected choice table

### 3.1 Live choices, exactly within R1–R5

| Row | Live proposal field | Alternatives retained | Minimality | Void condition |
|---|---|---|---|---|
| R1 | one automorphism-covariant `M_017` divergence/action member at ratification | zero member; fully specified nonzero natural section; covariant-family-only nonclosing record | exactly the datum and member Q-358 says are absent, now scoped by Q-362 | target-aware generator, untyped units, no generated quotient member, or failed `(R1-COV)` |
| R2 | finite-coordinate completed action with explicit `rho_Gamma,N` and vertical cocycle | prohibit all new-cycle increments; unconstrained product family | least class permitting new physical cycles without a false upward map | nonunique finite representative, unflagged limit, or nonzero common restriction tail |
| R3 | independently authored finite physical action bottom legs | rename source log; define bottom as R4 pullback | independence is the DoR-008 falsifier's authority | source/action relabeling or missing Q-243/Q-279 row |
| R4 | scalar equality plus selected-at-ratification normalizer | no normalizer; target-tuned scale; family-invariant-only use | exact missing comparison/units certificate | finite equality or units fail; member selected by outcome |
| R5 | one common-domain stationary 2PI package | naive full inverse; source Hessian substitution; no closure | least package that makes Q-408 and restriction/inverse lawful | cycle inversion, nonreducing inverse, or failed finite Schur restriction |

### 3.2 A3 disposition

```text
A3_V001_ALGEBRAIC_TENSOR_FAMILY = WITHDRAWN_AS_UNFORCED
A3_NATURALITY_PROOF = DELETED
A3_REPLACEMENT = none | canonical operation is derived D_G^*
```

There is no retyped A3 row because no authored comparison-law row remains.

### 3.3 Passed rows retained verbatim

| ID | Authored field | Proposed content | Alternatives considered | Minimality | Void condition |
|---|---|---|---|---|---|
| A8 | stage covariance | contravariant carrier restriction; upward action transport only on rank-preserving isomorphisms; vertical new-cycle increments finite-tested independently | universal upward map; selected representative; ignore new cycles | exactly the V003/Z7 boundary | any cycle-creating upward square is asserted |
| A10 | family-invariance discipline | downstream result valid only if invariant across route/member/normalization family or after later ratification | silently choose a member; average members | no-selection analogue of the standing realization discipline | member-dependent result reported as physical |

In V003, A10 means no member-sensitive Q-408 statement can issue before the
principal chooses an R1/R4 member or proves invariance.  It does not reinstate
V001's deleted comparison family.

---

## 4. K4 — physical bottom-leg falsifier, executed

### 4.1 Correct finite square

Let

```text
a_N^m:=rho_Gamma,N(phi_m):Q_N->C,
b_N^m:Q_N->C                                      (independent R3 leg).
```

The physical bottom-leg discrepancy is

```text
E_N^m(Z):=[D_N^*b_N^m-D_N^*a_N^m](Z)
         =b_N^m(D_N Z)-a_N^m(D_N Z).              (K4-1)
```

R4 requires `E_N^m=0` for every prefix history.  Surjectivity of `D_N` makes
this a test of the full finite physical bottom leg, not merely of a selected
history.

For a restriction `P_f:P_M->P_N`, the corrected square gives

```text
[D_M^*rho_f^*b_N^m](Z_M)
 =b_N^m(rho_f(D_M Z_M))
 =b_N^m(D_N(P_f Z_M))
 =[P_f^*D_N^*b_N^m](Z_M),                         (K4-2)
```

and identically for `a_N^m`.  Subtracting proves

```text
D_M^*rho_f^*(b_N^m-a_N^m)
 =P_f^*D_N^*(b_N^m-a_N^m)=0.                     (K4-3)
```

This is the finitely checkable restriction-square test V001 failed.  Every
composition in `(K4-2)` is typed.

### 4.2 Explicit finite regressions

**Equal history and one-edge tree.**  On one connected edge,

```text
P_G^fam=(Z_0=1,Z_1=r_1),
Q_G={*},
D_G(P_G^fam)=*,
b_1^m(*)=a_1^m(*)=0                               (anchor).
```

Thus `E_1^m=0`; varying endpoint-matched data cannot enter this quotient
bottom leg.

**Two-prefix reconstruction.**  For `Z=(1,u,uv)`,

```text
r_1=u,
r_2=v,
E_2^m(Z)=b_2^m(D_2(1,u,uv))-a_2^m(D_2(1,u,uv))=0.
```

The terminal product `uv` is never substituted for the prefix family.

**Pendant witness.**  With all cycle characters one and pendant `w!=1`,

```text
D_G(Z_w)=D_G(Z_1)=identity cycle class,
E_N^m(Z_w)=E_N^m(Z_1)=0.
```

The bottom leg cannot re-import the pendant character seen by unrestricted
`T^char`.

**S8-A.**  The three quotient characters are

```text
H_(c_1)=Z_2,
H_(c_2)=Z_1^(-1)Z_3,
H_(c_3)=Z_1 Z_2 Z_3^(-1).
```

R4 tests `b_N^m=a_N^m` on the full triple.  In particular `c_3` is not
deleted; under the admitted exchange it is inverted covariantly on both
sides.

**Q-243/Q-279.**  By R3 ownership, the transverse correction has zero active
jets while the independent base bottom leg retains the exact finite table:

```text
H_(delta,c)^bot=0                                  (p-free),
kappa_eta=p(1-p)exp(-eta/2)/(1-p+p exp(-eta/2))^2,
Z_ref,N[a,a,R]=1-p+p exp[-Q_N(R)/2].
```

No value of `p`, rank, or ratio is chosen.  The comparison tests the physical
action leg; it does not identify that leg with the source functional that
originally supplied the reference.

```text
FINITE_RESTRICTION_TYPECHECK = PASS
PHYSICAL_BOTTOM_LEG_DISCREPANCY = ZERO_AS_PROPOSAL_CERTIFICATE
DOR008_ACTIVE_REFERENCE_TABLE = PASS_WITHIN_PROPOSAL
DOR008_FALSIFIER_FIRED = false
NO_FINITE_CHECK_FAILURE = true | scope: corrected proposal fields
```

---

## 5. K5 — member and normalization: derivation attempt and ratification item

### 5.1 Three candidate authorities, checked

**The transverse-action draft.**  It says that a compatible flat member and a
renormalization are not sealed; multiplying by any symbolic scalar or
replacing the flat member survives unless an additional generator and
normalization theorem is supplied.  Its successor package explicitly asks
for a generated member and “either uniqueness or an explicit residual family
with no member selected.”  It does not fix `m`, `Norm_m`, or `nu_m`.

**DoR-009 conventions.**  They fix the faithful character, doubled trace,
branch reality, E_post orientation, and finite normalization.  Those data
determine the input to `D_G`; they do not map a quotient cycle class to a
transverse action member or fix that member's action units.

**DoR-008 falsifier.**  It fixes exact finite restrictions.  The transverse
correction is all-orders flat on the finite active section, so for any
admitted nonzero symbolic `lambda`,

```text
phi_m -> lambda phi_m,
b_N^m -> lambda b_N^m,
nu_m -> lambda nu_m                                (K5-1)
```

preserves the corrected square, all zero active jets, reality, batching, and
the independently owned base-action Q-243/Q-279 table.  Hence this falsifier
does not determine `lambda` or the choice among inequivalent flat members.

```text
UNIQUE_MEMBER_FROM_DRAFT_NORMALIZATION_CLAUSE = false | TYPE-R
UNIQUE_MEMBER_FROM_DOR009_CONVENTIONS = false | TYPE-R
UNIQUE_MEMBER_FROM_DOR008_FALSIFIER = false | TYPE-R
UNIQUE_MEMBER_NORMALIZATION_DERIVED = false
ACTION_MEMBER = TYPE-U pending DoR-017
```

### 5.2 Exact DoR-017 ratification fiber

The principal's item is not “choose a convenient coefficient.”  It is:

```text
DOR017_ITEM := choose one realization-automorphism-covariant R1/R4 pair
  m=(delta_div,Depth,Accum,Gen,phi,Norm,nu)
  together with independent bottom legs b_N
  satisfying R1-COV, R1, R3, R4 and the complete falsifier;
  then adopt R2 and R5 as that member's restriction/stationary package.
                                                               (K5-2)
```

The alternatives are:

| Alternative | Exact content | Consequence | Closing status |
|---|---|---|---|
| `Z` zero member | `phi_G=0`, `b_G=0`, `nu=0` at every realization, with the remaining R3 base legs covariant | every transverse Q-408 shift in `(K1-4)` is zero; base action remains | closes the square with zero transverse correction if ratified |
| `N` normalized nonzero member | one fully certified nonzero natural section satisfying `(R1-COV)`, its generator, units, homogeneous natural normalizer and symbolic `nu`; all finite equalities pass | Q-408 shifts are the displayed derivatives of that covariant member; no numerical response is inferred | closes the square for that member if ratified |
| `F` family-only record | retain the complete automorphism-covariant orbit/fiber, including zero and nonzero rescalings, select none | by J7, every member-sensitive linear Q-408 contribution is non-invariant; only member-independent claims pass | does **not** close the physical square; banked only |
| reject | adopt no R1/R4 member | forced map `D_G^*` remains, action/2PI square stays `TYPE-U` | nonclosing |

No alternative is recommended.  In particular, the zero member is not
called minimal physics, and a nonzero member is not selected for any desired
`p`, response, or coupling consequence.

The Z covariance check is exact:

```text
0_(G') compose alpha_Q=0_G,
Gen_(G')(alpha_Div 0)=0=alpha_Act Gen_G(0),
Norm_(G')(0)=0=Norm_G(0),
b_(G')^bot compose alpha_Q=0=b_G^bot.             (K5-3)
```

Thus Z is trivially covariant in its transverse fields.  Its nonzero base
bottom legs remain subject to the same R3 covariance line in `(R1-COV)`;
zero transverse content does not waive that independent certificate.

```text
DOR017_ALTERNATIVE_SELECTED = none
TARGET_AWARE_NORMALIZATION = false
MEMBER_SENSITIVE_Q408_BEFORE_RATIFICATION = forbidden
DOR017_FIBER_DOMAIN = realization_automorphism_covariant_members_only
DOR017_FOUR_CONSEQUENCES_CARRY_OVER = true
DOR017_RECOMMENDATION = none
```

### 5.3 N2 — permanent rank-two automorphism regression

At the admitted S8-A rank-two stage, let

```text
sigma(c_1)=c_2,
sigma(c_2)=c_1,
sigma(c_3)=-c_3,

g(t)=exp(-1/t^2) for t!=0,
g(0)=0,

phi_(c_1)(q,t)=g(t)(Re H_(c_1)(q)-1).             (N2-1)
```

`(N2-1)` remains smooth, real/reality-covariant, quotient-compatible,
all-orders flat, identity-anchored, target-independent, and finite-visible
off section.  It is therefore the exact member that defeated V002's former
certificate list.

The new certificate computes

```text
[phi_(c_1) compose sigma_Q](q,t)
 =g(t)(Re H_(c_2)(q)-1),                          (N2-2)
```

whereas

```text
phi_(c_1)(q,t)=g(t)(Re H_(c_1)(q)-1).             (N2-3)
```

Choose any quotient point with
`Re H_(c_1)(q)!=Re H_(c_2)(q)` and any symbolic `t!=0`.  Such points exist
because the rank-two characters are separated by `D_G`.  Equations
`(N2-2)` and `(N2-3)` differ, so

```text
phi_(c_1) compose sigma_Q != phi_(c_1).
```

The member fails `(R1-COV)` and is excluded from N.  Its full automorphism
orbit may appear only inside F, where no member is selected and the square
remains nonclosing.  The sign action on `c_3` is also carried correctly:
`H_(-c_3)=conjugate(H_(c_3))`; no orientation is frozen.

```text
RANK_TWO_CYCLE_SELECTIVE_WITNESS = FAILS_R1_COV | permanent regression
BARE_CYCLE_LABEL_ADMITTED_IN_N = false
FULL_ORBIT_ALLOWED_IN_F = true | no member selected | nonclosing
```

---

## 6. QE banked record — wholly outside the live proposal

Q-358 settles the live consumer.  The following V001 rows are retained only
so lawful endpoint-consumer typing is not lost.  They are not DoR-017 live
choices and feed no `phi_div` leg.

| ID | Authored field | Proposed content | Alternatives considered | Minimality | Void condition |
|---|---|---|---|---|---|
| A1 | consumer-route family | retain `{Q,QE}` with no member selected | quotient only; endpoint only; additive forced sum | DoR-015 supplies exactly two lawful scalar routes | hidden selection or a third untyped route |
| A6 | endpoint carrier | full orbit family `(v_t,R_e,v_s)/Gamma`, all edges, matching data | bare `R_e`; selected endpoint frame; already-contracted global scalar | least carrier making route 2 gauge invariant | F7 covariance fails or an endpoint/frame is selected |
| A7 | mixed QE dependence | one joint comparison-map family; no additive decomposition assumed | `phi_Q+phi_E`; product ansatz; no mixed terms | avoids double counting and hidden contact assumptions | a mixed term is smuggled outside `Comp^QE` or finite restriction fails |
| A9 | endpoint batching | E_post-ordered transport composition with matching endpoints | reversed order; commutative product; selected path subdivision | forced interface shape of the ratified ordered CTP law | batching or E_post regression fails |

```text
QE_SECTION_STANDING = BANKED_NOT_ADOPTED
QE_FIELDS_IN_LIVE_PACKAGE = none
QE_COMPARISON_MEMBER = none
QE_COMPLETED_CONTRACTION = TYPE-U | unchanged
```

---

## 7. K6 — hostile battery and permanent regressions

### 7.1 Six self-attacks

| Attack | Execution | Verdict |
|---|---|---|
| target tuning | searched R1 membership for `p`, alpha, response, or desired sign/value; none occurs | PASS |
| hidden normalization | scaling countermodel `(K5-1)` survives; uniqueness withdrawn and ratification item exposed | PASS_BY_DISCLOSURE |
| source/action relabeling | R3 is primitive and expressly not `-Log Z_N`; R4 compares independent legs | PASS |
| ill-typed contravariance | every pullback starts with `F_N:Q_N->C`; equation `(K1-2)` recomputed | PASS |
| additive/contact double count | no QE leg and no algebraic tensor sum exists in the live package | PASS |
| family-invariance paralysis | alternatives Z/N require principal selection; F is explicitly nonclosing | PASS_BY_BOUNDARY |

The six original attacks therefore retain their V002 verdicts.  The new
automorphism attack is run separately below so the battery is strengthened,
not renumbered.

### 7.2 Permanent witnesses, including N2

| Witness | Re-execution | Verdict |
|---|---|---|
| pendant `w!=1` | `D(Z_w)=D(Z_1)`; both bottom discrepancies vanish; `T^char` not substituted | PASS |
| cycle-creating identity edge | no upward physical/action map; vertical increment independently tested at the new stage | PASS |
| F7 one-edge endpoint pair | `Q_G={*}`; live `phi_div` cannot vary; endpoint scalar remains in banked QE record | PASS |
| rank-two cycle-selective member | `(N2-2)!=(N2-3)` at separated quotient points; member excluded from N, orbit confined to F | PASS_BY_EXCLUSION |

### 7.3 Additional regressions

```text
PREFIX_TRIANGULAR_INVERSE_N2 = PASS
S8A_C3_SURVIVAL = PASS
REALITY_COVARIANCE = PASS
EPOST_BATCHING = PASS
IDENTITY_ZERO_EXTENSION = PASS_ON_LAWFUL_SCOPE
TCHAR_SCOPE = PASS
PHYSICAL_BOTTOM_LEG = PASS_WITHIN_PROPOSAL
R1_AUTOMORPHISM_COVARIANCE = PASS_WITHIN_PROPOSAL
RANK_TWO_WITNESS_FAILS_NEW_CERTIFICATE = PASS
```

### 7.4 No-selection scan

```text
member selected = false
normalizer selected = false
route selected by outcome = false | Q forced by signature
rank or ratio selected = false
p evaluated = false
orientation selected = false
frame selected = false
filtration selected = false
realization member selected = false
cycle basis selected = false
cycle label selected by an N member = false
realization automorphism orbit truncated = false
generator naturality required = true
normalizer naturality required = true
bottom-leg naturality required = true
endpoint tuple selected = false
measure or contour selected outside R5 = false
response, alpha, K_*, or root evaluated = false
```

---

## 8. Delta table from V001

| V001 location/content | V002 disposition | Kill repaired |
|---|---|---|
| two live variants | Q forced live; QE wholly banked | J2 carried; J3/J4 |
| B1-10–B1-14 algebraic tensor family | deleted | J3/J4 |
| B1-18a `F_M compose rho_f` | deleted; replaced by `(K1-1)`–`(K1-2)` | J1/J6 |
| missing `D_G^*` | installed verbatim as `(S-3)` | J1 |
| partial Q-408 placement | all four blocks plus stationary shift installed | J1 |
| partial DoR-008 obligations | complete five-row obligations installed | J1 |
| A3 comparison law | withdrawn, not retyped | J4 |
| A8 | retained verbatim | J4 PASS content preserved |
| A10 | retained verbatim | J4 PASS content preserved |
| A1/A6/A7/A9 | moved verbatim to banked QE section | J3/J4 |
| AC-3/AC-4 open descriptions | replaced by explicit R2/R5 proposal fields | J3 |
| no independent finite action bottom leg | R3 constructed as proposal primitive | J3/J6 |
| no scalar equality/normalization | R4 plus exact DoR-017 fiber | J3/J7 |
| “no finite check fails” with type defect | withdrawn; corrected square and bottom falsifier executed | J6 |
| zero/rescaling family treated as usable | F alternative marked nonclosing; Z/N require ratification | J7 |

No reviewed V001 mathematics outside this table was silently promoted.

---

## 8A. Bounded delta from V002 to V003

| V002 location | V003 bounded change | M6/M7 effect |
|---|---|---|
| metadata/preflight | register Q-362 and re-adjudication authority added | custody current |
| R1 certificate list | `(R1-COV)` added for member, generator, normalizer, role map, base action, bottom correction, Hessian, and restriction | closes missing covariance certificate |
| R1 fiber sentence | restricted from all listed members to automorphism-covariant natural sections or complete unselected orbits | removes presentation-selective N members |
| R4 | covariance stability proof added | prevents comparison leg from reintroducing labels |
| R1 live choice row | N requires a natural section; F requires complete covariant fiber | fiber exactly typed |
| DoR-017 table | Z covariance verified; N covariance required; F remains nonclosing; reject unchanged | four consequences preserved, none recommended |
| regressions | rank-two cycle-selective witness `(N2-1)`–`(N2-3)` installed | offending member now fails permanently |
| no-selection scan/battery | generator, normalizer, bottom-leg naturality and full-orbit accounting added | M7 repaired |
| six-account restriction cells | covariance references added to R2/R4/R5 rows | propagation disclosed |

Everything else—including the forced diagram, R2–R5 architecture, physical
bottom-leg falsifier, QE banking, A8/A10, the V001 delta, and all prior
regressions—is carried verbatim from sealed V002.

```text
BOUNDED_DELTA_ONLY = true
V002_CONFIRMED_M1_TO_M5_CHANGED = false
NEW_LIVE_TOP_LEVEL_AUTHORED_OBJECT = none
```

---

## 9. Accounts, door flags, and final board

### 9.1 Six-account table

| Operation | Kernel/domain | Image | Sector transfer | Restriction | Tail/topology | Standing |
|---|---|---|---|---|---|---|
| prefix trace | faithful prefix family | cellwise characters | CTP histories to prefix data | exact | finite | ratified |
| `D_G` | vertex rephasing exactly | all physical cycles | prefix data to quotient | V003 exact | no tail | derived |
| `D_G^*` | zero by surjectivity | quotient-dependent scalar functions on prefix data | action values only | `(K1-2)` exact | no class completion | derived |
| `rho_Gamma,N` | common finite tail zero in R2 class | finite quotient actions | completed action to finite action | explicit coordinate projection plus `(R1-COV)` | finite-seminorm/renormalized-limit topology | PROPOSED |
| independent bottom leg/R4 | discrepancy kernel tested on full surjective `D_G` | finite physical action | none | `(K4-1)`–`(K4-3)` plus `(R1-COV)` | finite | PROPOSED |
| R5 Schur/retarded | complement reducing domain only | physical stationary response class | complement to cycle Schur, no cycle inverse | `(R5-3)` and automorphism intertwining | graph closure, zero alpha-facing tail required | PROPOSED |

### 9.2 Door flags

```text
DOOR_PREFIX_TRACE = CLOSED
DOOR_PREFIX_TO_CYCLE = CLOSED
DOOR_D_G_STAR = CLOSED
DOOR_MEMBER_NORMALIZATION = OPEN_FOR_DOR017_SELECTION |
  exact automorphism-covariant fiber K5-2
DOOR_R2_TO_R5 = PROPOSED_NOT_ADOPTED | no hidden missing field
DOOR_QE_COMPLETED_CONTRACTION = TYPE-U | banked, not live
DOOR_NON_EDGE_RESOLVED_EXTENSION = TYPE-U | boundary unchanged
DOOR_WEAK_STAR = NOT_USED
DOOR_BIDUAL = NOT_USED
DOOR_DISTRIBUTIONAL_COMPLETION = NOT_USED
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

### 9.3 Final board

```text
PROPOSED_NOT_ADOPTED -- PENDING REVIEW AND PRINCIPAL RATIFICATION

K1_FORCED_DIAGRAM = REPAIRED_AND_CONTAINED_EXACTLY
K2_FIVE_RESIDUES = 5_PROPOSED / 5 | no extras
K3_A3 = WITHDRAWN
K3_A8_A10 = RETAINED_VERBATIM
K4_PHYSICAL_BOTTOM_LEG_FALSIFIER = PASS_WITHIN_PROPOSAL
K5_UNIQUE_MEMBER_NORMALIZER = NOT_DERIVED
K5_DOR017_RATIFICATION_ITEM = EXACT_AND_UNSELECTED
K6_DELTA_REGRESSIONS_BATTERY = COMPLETE
N1_R1_AUTOMORPHISM_COVARIANCE = INSTALLED
N2_RANK_TWO_WITNESS = EXCLUDED_AND_PERMANENT
N3_COVARIANT_DOR017_FIBER = Z_N_F_REJECT | none recommended
N4_NO_SELECTION_AND_BATTERY = PASS_WITHIN_PROPOSAL

VARIANT_Q = FORCED_LIVE_ROUTE | TYPE-P premise from Q-358
VARIANT_QE = BANKED_RECORD_ONLY
ALGEBRAIC_TENSOR_COMPARISON_FAMILY = absent
ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY = true |
  scope: phi_div square only

DOR017_SELECTION_REQUIRED_TO_CLOSE_PHYSICAL_SQUARE = Z_or_N
DOR017_SELECTION_MADE_BY_LANE = none
FAMILY_ONLY_ALTERNATIVE_F = NONCLOSING
NONCOVARIANT_MEMBER_ADMISSIBLE = false

ACTION_COMPARISON_SQUARE_V003 = READY_FOR_LANE1_REVIEW
READY_FOR_DOR017_RULING = conditional_on_review
PHYSICAL_2PI_OR_RESPONSE_EXECUTED = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No alpha, `K_*`, root, response value, rank ratio, or measured constant was
evaluated.  No register, plan, tracker, git, commit, or push action was
performed.
