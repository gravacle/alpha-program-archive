CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00006046   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 15 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member and every pinned span is rehashed AT ITS PATH from its own bytes before
  sealing; nothing is copied from another artifact's table or completed from a display.
LANE = DARIO   ROLE_THIS_RELAY = SURFACE-NECESSITY HUNT (relay 1081)
AUTHORS NOTHING. PROPOSES NOTHING. ADOPTS NOTHING. BINDS NO MEMBER.
  omega_phys is the SUBJECT of the hunt: not entered, not selected, not authored, not evaluated.
  No candidate, no measure, no descent, no producer, no certificate content is supplied here.
SELF_CITATION_BAR = members 07, 08, 09 and 12 are DARIO-authored SUBJECTS. They are read as objects
  under examination, never as witnesses. ** One of them is CORRECTED here against its own source. **
BLIND EXPOSURE = kappa_record, alpha and the battery are pointers only; none was opened, consulted,
  imagined, or optimized toward.
NO_NUMERIC_PATH = nothing is computed. The generator scans its own code and refuses on any sampling,
  numeric library, or numeric literal path.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1081_SURFACE_NECESSITY_HUNT_DARIO_V001.md` | `04d833af71dfb3547a0c1ca49184cc806b41138048959186f730d548ab92f927` | assignment |
| 02 | `workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | the mandate and its open slot |
| 03 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | **THE STATE PORT — the deciding span** |
| 04 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | **THE DYNAMICS PORT — `F_N` and its interface** |
| 05 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `i_R`, `i_B`, the placement square, SM-1..SM-8 |
| 06 | `workspace/STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874` | **the ceiling the port span names** |
| 07 | `workspace/STAGE8_DESC_NECESSITY_PROBE_DARIO_V001.md` | `cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577` | SUBJECT — **corrected here** |
| 08 | `workspace/STAGE8_DESC_SURFACE_FIRST_DARIO_V002.md` | `801bc1683f08f1c7d4a94e49d8dc8685acf974c33c3a1ab1041182bdb7172f72` | SUBJECT — the surface terminus |
| 09 | `workspace/STAGE8_MEASURE_CANDIDATE_PROPOSAL_DARIO_V002.md` | `82c86bf02717652126da9cc56baff0b0d4a7adcafa5543abb1f7a637778b4e34` | SUBJECT — the descent work under suspicion |
| 10 | `workspace/STAGE8_MEASURE_V002_DESCENT_CHECK_CODEX2_V001.md` | `6a3297513f9a1d3981cf85553062f2b1137584d76c2a2d8bdd66a9e62b3cc15c` | the 1080 opposite-lane check |
| 11 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | `B iso C(Y)`, `Prob(Y)`, the licensed standard imports |
| 12 | `workspace/STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md` | `03dcaa17c78922f90bb905cd1ea7d9e9673514b74a229f0e6808a7a1dad1edd8` | SUBJECT — the still-continuum result |
| 13 | `workspace/STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | the S1 receiver typing |
| 14 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 15 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |

```text
PINNED SPANS — each recomputed in this relay from the member's own bytes at its own path.
  member 03 [10824,11934)  4313fb92fd78f6b26a5eba216e7ae7540f6b0698dc82eddd98236ceee5d7cf1b
                           StatePort_U2_008 — the full field list
  member 04 [12501,13326)  af7c1cdda2417e0cc25ee923ed361f5a7d3aed81363008977e65973463030b89
                           FiniteDynPort_N — what the dynamics port hands the state port
  member 04 [5939,6270)    c96e0be784481915cdd6790a5028dfdb4e2a58d0e0dd760cc9429e89e1a9b600
                           equation (4): F_N as a history-pair-indexed source-sector operator
  member 04 [4243,5977)    ed170b5ff7d6dcf236db7ca39fffc239091a2377a02e5aaf66413b75072a60bf
                           the controlled law and the exact influence functional
  member 05 [17865,20008)  aa04e0ea2c901d0a33312a23d6ade023f8c1e92f2e90d1ff6b447f4e98cea171
                           SM-1..SM-8, read here AS A SYSTEM
  member 06 [6923,8532)    c522dab9f57f152a6a2df34c2635924eea8f7759267baa929b3ba818539d12dc
                           the ceiling: what the P5 functionals CONSUME
  member 02 [1470,2433)    758023b00d6bfebf0d518036370dd625a26d206acd1c17813b8e0eac576c4c97
                           the object the mandate types, with its descent map and certificate

ESTABLISHED IN THIS RELAY — BY READING PORTS, NOT BY COMPUTING:
  the ruling of section 3, the SM-system reading of section 4, the fork of section 5,
  and one CORRECTION to member 07's supporting prose (section 2.2).

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO OUTPUT INSPECTION. NO MEMBER BOUND.
NO FIXED POINT. NO END TEST. NO FREEZE. NOTHING ENTERED, ADOPTED, PROPOSED OR SELECTED.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — THE SURFACE-NECESSITY HUNT
## RELAY 1081 — `[PLAN:DESC-38]` — DARIO LANE — [CLAIMED]

Date: 2026-08-12

**THE QUESTION.** Six relays have run under an SM-6 demand on the descent. The principal asks
whether that demand is a **surface requirement** or **machinery** — a from-trace determination
re-imposed on an object the surface deliberately left undetermined. It is settled here by reading
what the ports consume, at bytes.

## 0. THE GUARD, DECLARED BEFORE THE RULING

```text
(b) JOINT-STATE is the answer that VALIDATES my own last six relays.
(a) Y-MARGINAL is the answer that demotes them.
UNDETERMINED also preserves them, so it is not a neutral resting place either.
```

**So the flattering answers are two of the three, and I adopted the rule that a convenient verdict
needs better evidence than an inconvenient one:** rule (b) only on a sealed span showing a port
consuming a joint object, attack (b) first, and publish the attack whether or not it succeeds. The
ruling below is (b). Section 3.4 is the attack I ran on it, section 6 is what it does **not** buy,
and the correction at section 2.2 is against my own probe. **A reader should discount this ruling
for the direction it points and press section 3.3 hardest.**

---

## 1. WHAT WAS ALREADY SETTLED, AND WHAT WAS NOT

```text
SETTLED   the measure is UNDETERMINED by the sealed corpus (member 12)
SETTLED   the measure is REQUIRED constructively (member 07)
SETTLED   the port consumes the FUNCTIONAL, not an invariant of it (member 07, at the port span)
NOT SETTLED, and the subject of this hunt:
          does the read-off consume ONLY a state on Y — the marginal paired with the fixed
          influence functional — or the FULL JOINT source-record state?
```

Member 07 answered *full measure versus forced invariant*. That is a different axis from *marginal
versus joint*, and nothing in it decides today's question.

---

## 2. THE STATE PORT, READ AT BYTES

### 2.1 The field list

Member 03 `[10824,11934)`, digest `4313fb92…` recomputed here:

```text
StatePort_U2_008 := (
  omega_phys : B -> C,
  H_omega := completion of E_C0 / N_omega,
  pi_omega : A_C0 -> B(H_omega),
  rho_pre in trace-class(H_omega),
  rho_pre >= 0,
  Tr_omega(rho_pre)=1,
  state_domain,
  state_descent_map,
  state_provenance_certificate
).
```

Three observations, each read off the field list rather than argued:

```text
i    omega_phys is a state on B alone.  H_omega and pi_omega are constructed FROM it -- the
     completion is of the C0 module by the null space of the form omega_phys scalarizes.
ii   pi_omega represents the JOINT algebra A_C0, not B.
iii  rho_pre is a SEPARATE FIELD.  The port constrains it to be positive, trace-class and
     unit-trace ON H_omega.  ** It does not define it from omega_phys. **
```

Point iii is the hinge of this relay.

### 2.2 THE CORRECTION I OWE MY OWN PROBE

Member 07 wrote, of this same span, that `H_omega`, `pi_omega` **and `rho_pre`** are *"all BUILT
FROM it"*, and again that *"three further fields are defined from it."*

**That is wrong for `rho_pre`, and it is wrong in the direction that would have made today's answer
come out (a).** The port places `rho_pre` **in** the carrier built from `omega_phys`; it does not
derive it. A density living in a space is not a density determined by the state that built the
space. Member 07's own annotated block says the accurate thing — *"the density lives in that
carrier"* — and its surrounding prose over-reads it.

```text
MEMBER 07's DISPOSITION STANDS:  CONSUMES-FULL-MEASURE is correct and is not disturbed.
MEMBER 07's SUPPORTING SENTENCE IS WITHDRAWN:  rho_pre is an INPUT to the port, not an output of
  omega_phys.  The correction is recorded here because it is load-bearing for THIS ruling, and
  because a sentence that overstates in my own favour is worth more attention than one that does not.
```

Member 07 is not edited: it stays sealed and byte-untouched, and this is a supersession-shaped
correction of one sentence, appended where the correction is used.

### 2.3 The ceiling, in the record's own words

Member 06 `[6923,8532)` states what the P5 state/effect/probability-CTP functionals do with these
objects: *"They consume `rho_pre`, `E_r`, and `U_BR`; they do not derive them from one microscopic
source."* The source-sector GNS *"does not derive the completed source-record-field `rho_pre`"*.

**So the read-off's declared input is `rho_pre` — a density on the joint carrier — and the sealed
text says in terms that it is consumed rather than derived.** Member 05's placement square says the
same from the algebra side: `Omega_C0(a) := Tr(rho_pre pi_omega(a))`, with
`omega_phys(b) = Omega_C0(i_B(b))`. The joint state is what `rho_pre` delivers; `omega_phys` is its
restriction.

---

## 3. THE DECIDING OBSERVABLE

Type-level evidence can be argued about. There is a content-level fact that settles it.

### 3.1 What the dynamics port hands the state port

Member 04 `[12501,13326)`:

```text
FiniteDynPort_N := (U_N[a],F_N[a_+,a_-],C1,C3,C4,C7,
                    one-cell restriction certificate).
```

and member 04 `[5939,6270)`, equation (4):

```text
F_N[a_+,a_-] = P_0 + Z_N[a_+,a_-] P_ch.
```

### 3.2 Where `F_N` lives

`P_0` and `P_ch` are source-sector projectors; `Z_N` is a function of the history pair, and member 11
types the history algebra as `B iso C(Y)` with `Y = X_+ x X_-`. So `F_N` is a continuous
`A_src`-valued function on `Y`. Under the standard identification of `C(Y,A_src)` with
`A_src tensor_min C(Y)` — a licensed standard import, member 11 `:81-96` —

```text
F_N  is an element of  A_src tensor B  contained in  A_C0,
and it lies in NEITHER  i_B(B)  NOR  i_R(R_inf)  NOR  A_SR:
     it has a nontrivial source-sector part, so it is not in the history factor;
     it depends on the history pair, so it is not in the source-record factor.
```

### 3.3 THE RULING

For any state `Omega_C0` on `A_C0`,

```text
Omega_C0(F_N) = Omega_C0(P_0 tensor 1) + Omega_C0(P_ch tensor Z_N).
```

The first term is fixed by the source marginal. **The second term is a genuine product-observable
expectation, and it is not determined by the two marginals.** It factorizes into
`omega_SR(P_ch) . mu(Z_N)` exactly when the state is uncorrelated across the cut — and *whether it
is* is precisely the descent question.

```text
READOFF_CONSUMES = JOINT-SOURCE-RECORD-STATE.
The ratified dynamics port exports an observable of A_C0 that lies outside both marginal
subalgebras, and the state port's own field list takes a joint density as an input rather than
producing one from omega_phys.  A state on Y alone cannot evaluate what the record hands over.
```

**And it strengthens rather than disturbs member 07.** That probe ruled the port needs the full
measure and not an invariant; this hunt finds it needs more than the measure. The two are the same
direction, one step further.

### 3.4 THE ATTACK I RAN ON MY OWN RULING

**The strongest case for (a) is the standard influence-functional move.** Integrate the histories
first, against their own measure, to get an effective source channel, then apply the source state.
In that formalism no joint state ever appears: one supplies a source state and a history measure,
and `mu` enters only through `mu(Z_N)`.

**It does not rescue (a), and the reason is exact.** That move *is* a joint state — the product one.
It computes `Omega_C0(F_N)` for the uncorrelated extension and gets
`omega_SR(P_0) + omega_SR(P_ch) . mu(Z_N)`. So it does not show the read-off consumes only a
marginal; it shows what the answer becomes **under an assumption about the joint state** — and that
assumption is exactly the one the opposite lane found unledgered in candidate V001, and exactly the
one candidate V002 could only derive inside an authored quantifier. **A formalism's habit of
factorizing is not a sealed licence to factorize.**

The honest residue of the attack: **whether the correlation changes the number is not decidable
here.** That is member 07's own instrument bound, symmetric as it was stated there, and it applies
to me now:

```text
CONSTRUCTIVE  the port cannot be instantiated without a joint density   -- SETTLED HERE, at spans
OUTPUT        does the number depend on the correlation                 -- NOT PROVABLE HERE;
                                                                           evaluating is barred
```

---

## 4. WHAT (b) BUYS, READ FROM THE SM SYSTEM

Member 05 `[17865,20008)` carries SM-1..SM-8. Read as a system rather than row by row:

```text
SM-1  typing               a condition on the PRODUCER's source and codomain
SM-2  positivity/norm      a condition on the OBJECT
SM-3  record marginal      a MARGINAL condition
SM-4  history binding      a MARGINAL condition
SM-5  finite/quotient      a MARGINAL condition, inside the history factor
SM-6  common provenance    a condition on the PRODUCER -- carrier, law, record state, history
                           state, domains and effects from ONE FROZEN TRACE
SM-7  target independence  a condition on the PRODUCER's generation and admission
SM-8  coverage             a condition on the PRODUCER's family
```

**No row constrains the correlation's CONTENT.** Every row is a marginal condition, a typing
condition, or a condition on how the object was produced. So what the surface requires of the joint
state is: that it exist, that it be positive and normalized, that its two marginals be the sealed
ones — and that it come from somewhere legitimate.

```text
SM6_IS = SURFACE (required).
It is a requirement, it is not lane machinery, and the descent question is real -- the read-off
consumes the joint object whose correlation SM-3..SM-5 leave free.
BUT IT IS A CONDITION ON A PRODUCER, NOT ON A MEASURE.  SM-6 does not range over measures at all,
which is why no supplied measure can either satisfy or violate it.
```

---

## 5. THE SELF-CONSISTENCY TELL

The principal asks whether *supply because undetermined* and *satisfy SM-6 because from-trace
determined* can both hold.

```text
THE SEALED SYSTEM ASKS FOR A PRODUCER.
  Member 05's missing composition names it: a target-independent common-origin producer, one
  concrete positive normalized joint state, the commuting placement square, and provenance and
  no-post-output-supplementation certificates.
  Member 03's port lists state_descent_map and state_provenance_certificate as FIELDS.

THE MANDATE AUTHORIZES AN OBJECT.
  Member 02 [1470,2433) types the thing to be supplied as a U1-invariant probability measure on Y,
  with a descent map and a provenance certificate, and requires every ingredient to be declared
  FORCED or AUTHORED.  Its existence presupposes that the corpus does not determine the object.

AN AUTHORED OBJECT CANNOT CARRY FROM-TRACE PROVENANCE.
  Provenance is a fact about where a thing came from.  An ingredient authored under a supply
  mandate came from the author.  No certificate can make it descend from a frozen trace it did
  not descend from, and calling the authoring lawful does not change its origin.
```

```text
SUPPLY_AND_SM6 = CONTRADICTION-CONFIRMED.
Not because SM-6 is machinery -- section 4 rules it a genuine surface requirement -- but because
the two demands do not range over the same kind of thing.  SM-6 asks how the joint state was
PRODUCED; the mandate authorizes a measure to be SUPPLIED.  One object cannot be both
record-undetermined and trace-determined, and no ordering of the two relays that.
```

**THE SCOPE OF THIS RULING, STATED AGAINST MY OWN INTEREST.** Member 10 voided candidate V002's
`STOP` and refuted its universal positivity claim with a positive correlated weighting the sealed
corpus already carried. **That void stands, I accept it, and nothing here reopens it.** The
contradiction ruled above is narrower and does not depend on the refuted clause: it is about what
kind of predicate SM-6 is, not about which joint states are positive. Member 10 also observed that
the mandate's open slot — a future target-independent derivation — is live; that route is untouched
here, and it is the one route that dissolves the contradiction, because a derived measure is not a
supplied one.

---

## 6. WHAT THIS HUNT DOES NOT SAY

```text
IT DOES NOT say the number depends on the correlation.  That is the output question and it is
   barred here (section 3.4).
IT DOES NOT supply, propose, adopt or favour any joint state, measure, producer or certificate.
IT DOES NOT rule the six descent relays correct.  It rules the QUESTION they addressed real.
   Candidate V002's central claims were voided by member 10 on other grounds and stay voided.
IT DOES NOT rule the mandate unlawful.  It rules that a measure supplied under it cannot satisfy
   a producer-provenance condition, which is a statement about two sealed demands, not about
   anyone's conduct.
IT DOES NOT decide the fork.  Section 7 reports it.
```

---

## 7. THE IMPLICATION, REPORTED AND NOT DECIDED

The paste offers two implications. **The ruling forces the second, conditionally, and names exactly
what the condition is — which is a decision only the principal can take.**

```text
IF the port's provenance and descent-map fields are held as written for the history factor:
   the supply route cannot produce a port-admissible object, and the only remaining route to the
   joint state is a from-trace construction.
   => IMPLICATION = RUN-FROM-TRACE-FEASIBILITY-HUNT
      (does the frozen trace admit a coherent history state carrying nontrivial phase -- which
      member 09's own COST-1, whatever else it got wrong, located at the source-sector coherence)

IF the principal rules those fields inapplicable to a history state the corpus deliberately left
   undetermined -- i.e. relaxes SM-6 for this one factor and requires only SM-1..SM-5 plus a
   disclosed authored provenance:
   the measure question CLOSES to the mandate decision.  The surface then needs a lawful state on Y
   and a declared joint extension, nothing more.
   => IMPLICATION = CLOSE-TO-MANDATE-DECISION
```

**The two are exhaustive and mutually exclusive on the present corpus, and choosing between them is
a decision of record.** I decline to pick, and I decline for a reason stronger than the fence: the
choice is between believing the port's field list and believing the mandate's premise, and both are
sealed principal acts.

---

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the 15 closed members; the state port's field list; the dynamics port's interface and equation
  (4); the P5 ceiling; SM-1..SM-8; the mandate's object clause and open slot; member 10's void of
  candidate V002 and its refutation of the universal positivity claim; all charter fences.

DERIVED HERE:
  that F_N lies in A_src tensor B and in neither marginal subalgebra, hence that its expectation is
    not determined by the two marginals;
  the ruling that the read-off consumes the joint source-record state;
  the SM-system reading -- no row constrains correlation content, and SM-6 is a producer predicate;
  the contradiction between producer-provenance and mandated supply;
  the correction to member 07's supporting sentence about rho_pre.

AUTHORED OR SELECTED HERE:
  nothing.  No measure, joint state, producer, descent, certificate, origin-family member, scale,
  selector, entry or status.  No stop is lifted and none is imposed.

NOT CONSUMED:
  kappa_record, alpha, any battery output, any downstream score, any measured datum, any member
  selection, any fixed point, any end test, any entry addendum, any freeze.
```

## 9. FLATTENING CHECK

- **A port's declared input type was not flattened into a claim about the answer's dependence**
  (section 3.4): constructive necessity is settled, output dependence is not.
- **A formalism's factorization habit was not flattened into a sealed licence to factorize.**
- **"The question is real" was not flattened into "the answers given to it were right"** — member
  10's void of candidate V002 stands untouched.
- **A producer condition was not flattened into an object condition**, which is the whole content of
  section 5.
- **A contradiction between two sealed demands was not flattened into a fault of either one.**
- **A correction to my own artifact's prose was not flattened into a correction of its disposition**
  (section 2.2), and the artifact itself was not edited.
- **A fork was not flattened into a recommendation.**

## 10. CUSTODY

- **BUILDER NEVER VERIFIES OWN WORK.** Everything here is CLAIMED until the Codex 2 check.
- Members 07, 08, 09 and 12 are my own lane's outputs, read as SUBJECTS and never as witnesses; the
  ruling rests on members 03, 04, 05, 06 and 11, none of which is mine.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- Archive-side only: one artifact and one generator in `workspace/`, both sealed.

---

## 11. FINAL LINES

```text
READOFF_CONSUMES = JOINT-SOURCE-RECORD-STATE.  The ratified dynamics port hands the state port
     F_N[a_+,a_-] = P_0 + Z_N[a_+,a_-] P_ch (member 04 [12501,13326) and [5939,6270)), an element
     of A_src tensor B lying in NEITHER marginal subalgebra, so its expectation is not determined by
     the source and history marginals separately.  Independently, the state port's own field list
     (member 03 [10824,11934)) takes rho_pre -- a positive unit-trace density on the JOINT carrier,
     with pi_omega representing A_C0 -- as an INPUT, and member 06 [6923,8532) says the P5
     functionals CONSUME rho_pre rather than deriving it.  A state on Y alone cannot evaluate what
     the record hands over.

NECESSITY_PROBE_READ = member 03 [10824,11934), 4313fb92… : field 1 is omega_phys : B -> C, taken
     whole; H_omega and pi_omega are built FROM it; rho_pre is a SEPARATE FIELD constrained to lie
     in that carrier.  ** CORRECTION TO MEMBER 07, section 2.2: its prose that rho_pre is "built
     from" omega_phys is WITHDRAWN.  Its disposition CONSUMES-FULL-MEASURE stands. **  The
     over-reading pointed toward the Y-marginal answer, which is the answer that would have demoted
     six of my own relays; I record it in that direction rather than the flattering one.

SM6_IS = SURFACE (required) -- and it is a condition on the PRODUCER, not on a measure.  Section 4
     reads SM-1..SM-8 as a system: SM-3, SM-4 and SM-5 are marginal conditions, SM-2 is an object
     condition, and SM-1, SM-6, SM-7, SM-8 are producer conditions.  NO ROW CONSTRAINS THE
     CORRELATION'S CONTENT.  So the descent question is real -- the read-off consumes the very
     object whose correlation the marginal rows leave free -- and the suspicion that SM-6 is lane
     machinery is NOT confirmed.  What is confirmed is stranger and is the next line.

SUPPLY_AND_SM6 = CONTRADICTION-CONFIRMED.  The sealed system asks for a PRODUCER with from-trace
     provenance (member 05's missing composition; member 03's state_descent_map and
     state_provenance_certificate fields).  The mandate authorizes an OBJECT to be SUPPLIED, and
     exists because the corpus does not determine it (member 02 [1470,2433)).  An authored object
     cannot carry provenance it does not have.  The two demands do not range over the same kind of
     thing, so no ordering, no certificate and no better candidate satisfies both.  This ruling is
     NARROWER than the STOP member 10 voided and does not rest on the refuted positivity universal;
     member 10's void stands and is not reopened here.

IMPLICATION = RUN-FROM-TRACE-FEASIBILITY-HUNT, CONDITIONAL ON A PRINCIPAL DECISION, with the other
     prong named exactly.  If the port's provenance and descent-map fields hold as written for the
     history factor, the supply route cannot produce a port-admissible object and the from-trace
     feasibility hunt is the only remaining route.  If the principal rules those fields inapplicable
     to a factor the corpus deliberately left undetermined -- requiring SM-1..SM-5 plus a disclosed
     authored provenance -- then IMPLICATION = CLOSE-TO-MANDATE-DECISION and the six descent relays
     resolve to "the surface needs a lawful state on Y and a declared joint extension."  ** The two
     prongs are exhaustive and mutually exclusive on the present corpus.  Choosing is a decision of
     record and I decline to make it. **

VERDICT = NECESSITY-MAPPED (implication stated).
CLOSURE = declared-first (byte 0; end byte 00006046 computed as a fixed point on bytes; 15 members;
     0 live; 7 pinned spans recomputed at their paths; residue scan 0 hits from its own run)
PROSE_DIGESTS = 15/15 STRICT==STABLE (every digest computed at its path)
PINNED_SPANS = 7/7
NO_NUMERIC_PATH = CLEAN (the generator scans its own code; nothing in this relay was computed)
RESIDUE_GREP = CLEAN (27 own patterns; 0 operative hits)
OUTPUT_INSPECTION = NONE-CERTIFIED.  Ports were read for what they DEMAND, never for what they
     would return; kappa_record, alpha and the battery stayed closed.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN.  "Consumes" is used only of a declared port field or a sealed sentence
     saying so; "settled" is confined to the constructive question; "contradiction" is confined to
     the two demands' incompatibility and asserts no fault; nothing is claimed produced, supplied,
     adopted or entered by this hunt.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**The uncomfortable shape of this result.** I went in expecting to demote my own work, and the guard
at section 0 exists because that expectation is itself a bias. The bytes said the descent question is
real. **But the same reading also says the route my last six relays took cannot reach it** — a
measure supplied under the mandate cannot satisfy a producer condition, no matter how carefully it is
authored or how well its descent is derived. If that survives the check, the six relays were neither
machinery nor progress: they were a well-executed attempt on a door that opens the other way.

All findings **CLAIMED** until the opposite-lane check.
