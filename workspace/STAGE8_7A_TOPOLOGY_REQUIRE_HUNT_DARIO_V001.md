# STAGE 8 / 7A / STEP 8 — THE TOPOLOGY REQUIRE-HUNT

**Lane:** Dario (Opus 5, cross-family from the Codex build lanes)
**Date:** 2026-08-08
**Scope:** PASTE 719 only — DD1 enumerate, DD2 matrix, DD3 verdict, DD4 lambda
**Custody:** archive-side read and write; the registrar owns any mirror
**Governing:** frozen prereg `9f0d12b4…`; carrier `5f4979d5…`; my 716 result `5234141f…`
**Charge:** re-derive, not trust. Nothing is adopted under any verdict.

## Lead determination

```text
VERDICT = UNDERDETERMINED, but the set is REDUCED 3 -> 2.

  tau_cyl     ELIMINATED.  A sealed REQUIRE forbids exactly the property the
              sealed text attributes to it.  Elimination certificate at §2.2.
  tau_norm    SATISFIES every enumerated require, and is additionally the class
              the only existing composition theorem is already written over.
  tau_density UNDETERMINED -- not eliminated, not surviving: it is not yet
              defined as a topological carrier, so it cannot be tested.

THE PRINCIPAL'S REFRAME WAS RIGHT, AND PARTLY RIGHT.  My 716 cost list did
contain a disqualification in disguise -- but ONE of three, not three.
  "admits a bidual tail"          -> a REQUIRE violation.  Elimination.
  "lacks geometric realization"   -> a missing-object condition (J_ref), not a
                                     violated require.  Not an elimination.
  "not defined as a carrier"      -> a definitional gap.  Not an elimination.

I DO NOT BOOK FORCED.  Exactly one candidate survives every require, which is
the shape of FORCED -- but the third candidate is unevaluable, not eliminated.
Counting "cannot be tested" as "killed" would convert inability to see into a
negative verdict, which is the one move the jurisdiction check forbids.  The
asymmetry is displayed instead, at full strength, for the principal.

LAMBDA = untouched.  No enumerated require mentions a scale law.  Eliminating
a topology does not constrain (D2-4)'s freedom; the two decisions are
independent.
```

---

## 0. Preflight

[PROVABLE] `relay_outbox/719_ACK.md` was written **before** source work. The relay
header names **DARIO**; lane guard satisfied. The relay was read only after its
sidecar verified:

```text
relay_inbox/RELAY_PASTE_719_TOPOLOGY_REQUIRE_HUNT_DARIO_V001.md
  2b8ec6ee52830a7e62fd511a3ea5992117cca36b514d1c7291a8fbde9d15da92   shasum -c OK
```

[PROVABLE] `STAGE8_7A_TOPOLOGY_REQUIRE_HUNT_DARIO_V001.md` and its seal sidecar were
probed before the write and returned ABSENT.

### 0.1 Sources verified before use

```text
DoR7  supervision/DECISION_OF_RECORD_007_SMOOTH_FORK_...V001.md   17dc023e…  sidecar OK
V011  review_packets/.../BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  aa7c6d49…
4d    STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md     430f0971…  (Lane 1)
4dCI  STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md
                                                                        f20639a6…  (Lane 2)
R4K   STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md  ccb94dfa…
JREF  STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md        8dd59b35…
CARR  STAGE8_7A_R9JII_JOINT_LANDING_TEST_V001.md                        5f4979d5…
716   STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md                        5234141f…  (mine)
PREREG STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md                9f0d12b4…  unchanged
```

[YOURS] **The object-names probe — now a standing law from my own 716 disclosure —
paid off on its first use.** Probing `tau_norm`/`tau_cyl`/`tau_density`/`T_ref`/
`T_phys`/`T_cyl`/`one frozen topology` surfaced **two artifacts my 716 search never
saw**: `4dCI` (Lane 2's cellulation-independence/O-D3 invariance theorem, which names
the five public consumers of the limit) and `R4K` (which carries a void condition on
how the topology may be chosen). Both are load-bearing below. The law works.

### 0.2 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No common cell formed, no junction map evaluated, no member bound, no fixed point, no
end test, no numeric evaluation, no measured constant. **Nothing adopted, under any
verdict.** No register, plan, tracker, git, commit or push action. The frozen
preregistration was not altered.

---

## 1. DD1 — THE REQUIRES, ENUMERATED AND MODALITY-TAGGED

Tagged by the source's **own verbatim modality**, not by my reading of its importance.
`REQUIRE` = must / is to be / package field / void condition / falsifier clause.
`ALLOW` = permits, or records an absence. `WANT` = desirable, unmodalised.

### R1 — DoR-007: the theorem content [REQUIRE]

[PROVABLE] `17dc023e…[253,529)`:

```text
The smooth-required subset (Q-199: the alpha-facing chain — public EM connection,
local Maxwell tensor, continuum Ward/transversality, Thomson matching) is to be met
by a DERIVED DISCRETE-TO-CONTINUUM EQUIVALENCE THEOREM — the stitching rule as a
theorem over refinements.
```

and `17dc023e…[1194,1310)`:

```text
base-smoothness exists only as a refinement limit; the limit's
cellulation-independence is the theorem's content.
```

Modality: *"is to be met by"* — REQUIRE. Scope note: it requires a **derived
theorem**; it names no topology.

### R2 — V011: the density statement [REQUIRE ×2]

[PROVABLE] `aa7c6d49…[47025,47247)`:

```text
The response map must commute with pullback to a common refinement, and the
intensive quadratic coefficient must be invariant under each elementary
refinement up to a boundary term whose ratio to four-volume tends to zero.
```

Modality: *"must"* twice — REQUIRE. This is the only require whose statement
involves four-volume, and therefore the only one that could bear on `lambda` (§4).

### R3 — 4d §8 field 3: the topology's own condition [REQUIRE]

[PROVABLE] `430f0971…[23693,23835)`:

```text
3. tau_ref, one frozen topology with proofs that all structure maps and
   limits use that topology and do not acquire a bidual moving tail;
```

Modality: a numbered field of the would-build package — REQUIRE. **This is the only
enumerated require that constrains a topology by a property a topology can have.**

### R4 — 4d §3.2: cofinal independence in one frozen topology [REQUIRE]

[PROVABLE] `430f0971…[11040,11150)`:

```text
The resulting compatible response
must be independent of the cofinal refinement family in one frozen topology.
```

Modality: *"must"* — REQUIRE. It requires *a* frozen topology; it selects none.

### R5 — 4d §9 premise 1: the class the composition is written over [REQUIRE, conditional]

[PROVABLE] `430f0971…[25514,25583)`:

```text
1. `J_ref` is functorial and lands in the ratified norm/module class;
```

Modality: a premise of the conditional composition theorem — REQUIRE **within that
theorem**, not a standing requirement on the program. Recorded at that strength and
no higher (§2.3).

### R6 — 4d §9 conclusion 4: separation as a proof step [REQUIRE]

[PROVABLE] `430f0971…[26183,26282)`:

```text
4. P7 separation rules out nonzero norm-class content invisible to every
   finite restriction; and
```

Modality: a step the composition proof executes — REQUIRE of whatever class the
composition runs in.

### R7 — R4K: the void condition on the act of choosing [REQUIRE, on the chooser]

[PROVABLE] `ccb94dfa…[22630,22730)`, the void condition of the derive-through-R12
route:

```text
filtration member, topology chosen from desired coefficient, failed
packing/cellulation independence
```

Modality: a **void condition** — REQUIRE. It constrains *how* a topology may be
selected (not by its downstream coefficient), and binds equally on all three
candidates and on the principal's act.

### R8 — R9-JII J2: the invariance demand [REQUIRE, negative form]

[PROVABLE] `CARR` `5f4979d5…`, J2 fires iff the common value *"is not invariant under
cell re-presentation or depends on ell, truncation level, cellulation-family index, or
cellulation geometric datum."* Modality: falsifier clause — REQUIRE. It demands
invariance; it does not name the sense in which the limit is taken. **That is
precisely the lack my 716 identified**, and this hunt does not remove it.

### Facts the matrix consumes (not requires)

[PROVABLE] **F1** — `430f0971…[17490,17655)`, the Q-247 category split:

```text
norm C-star / standard module / left multipliers: separation holds;
bidual weak-star class: separation fails by z_tail B**.
```

[PROVABLE] **F2** — `430f0971…[11420,11669)`, the three candidates' properties:

```text
No authority proves these equivalent or selects one. `tau_cyl` alone admits a
bidual tail; `tau_norm` excludes that tail but lacks geometric realization;
`tau_density` is named operationally by V011 but not defined as a complete
topological carrier.
```

[PROVABLE] **F3** — `430f0971…[18031,18171)`: the tail's exclusion in the proved half
is carried as a **premise**, not a theorem:

```text
T_CYL_BIDUAL_TAIL_EXCLUDED_BY_RATIFIED_CLASS = true | TYPE-P |
  premise: Task 4d uses only the ratified norm/module completion at this step
```

[PROVABLE] **A1** [ALLOW / absence] — `8dd59b35…[12810,12879)`:

```text
Neither DoR-007 nor DoR-008 chooses `lambda` or
supplies a scale law.
```

```text
REQUIRES = 8 enumerated  (R1-R8 all REQUIRE; A1 ALLOW; F1-F3 facts)
WANT-tagged = 0.  Nothing in the enumerated set is merely desirable; the
              modalities are all mandatory or absence-recording.
```

---

## 2. DD2 — THE MATRIX

`S` satisfies · `V` violates · `U` undetermined · `–` does not discriminate

| | R1 derived theorem | R2 V011 density | **R3 no bidual tail** | R4 one frozen topology | R5 §9 class premise | R6 separation | R7 anti-tuning | R8 J2 invariance |
|---|---|---|---|---|---|---|---|---|
| **tau_norm** | – | U | **S** | S | **S** | **S** | S | U |
| **tau_cyl** | – | U | **V** | S | U | **V** | S | U |
| **tau_density** | – | U | U | U | U | U | S | U |

### 2.1 Why R1 does not discriminate

[PROVABLE] R1 requires a *derived theorem*. All three candidates are equally
compatible with a theorem being derived; none is named. Recorded as non-discriminating
rather than silently dropped.

### 2.2 The elimination certificate — `tau_cyl` VIOLATES R3 and R6

This is the hunt's one hard result, so it is displayed as a chain with each link's
source.

```text
ELIMINATION OF tau_cyl

  L1 [R3, REQUIRE]  430f0971…[23693,23835)
     The frozen topology must be one in which structure maps and limits
     "do not acquire a bidual moving tail."

  L2 [F2, FACT]     430f0971…[11420,11669)
     "`tau_cyl` alone admits a bidual tail."

  L3 therefore      tau_cyl is a topology in which the limits acquire exactly the
     object R3 forbids.  VIOLATION of R3.

  L4 [R6, REQUIRE]  430f0971…[26183,26282)
     The composition's step 4 is "P7 separation rules out nonzero norm-class
     content invisible to every finite restriction."

  L5 [F1, FACT]     430f0971…[17490,17655)
     Separation HOLDS in norm C-star / standard module / left multipliers;
     it FAILS in the bidual weak-star class, "by z_tail B**".

  L6 therefore      in tau_cyl the very step R6 requires is unavailable: the
     content separation must rule out is precisely what the tail admits.
     VIOLATION of R6.

  CERTIFICATE = tau_cyl violates two independent enumerated requires, one a
  package field and one a proof step, on two independently sealed facts.
```

[YOURS] **This is the principal's hypothesis, confirmed for one of the three.** In my
716 artifact I listed *"admits a bidual tail"* in a **cost** column beside two other
costs, as though the three were commensurable. They are not: this one meets a stated
require head-on. Listing a disqualification as a cost is a real error of presentation,
and it is mine — §5 disclosure 1.

[YOURS] One honest weakening, stated because it bears on how hard the elimination is.
F3 shows the tail's exclusion in the *already-proved* half is carried as `TYPE-P`, a
premise — Task 4d simply used the norm class at that step. So R3 is a **forward
requirement on the package**, not a theorem that the tail is impossible. The
elimination is therefore an elimination *against the stated package*, which is the
only package of record. If the principal replaced R3, `tau_cyl` would return. That is
his to do and nobody else's.

### 2.3 Why `tau_norm` is `S` on R5 and R6 — and why that is not FORCED

[PROVABLE] R5's premise names *"the ratified norm/module class"*; R6's step is P7
separation, which F1 places in the norm/module/multiplier classes. `tau_norm` is *the
C-star/module norm on the ratified completion* (F2). So the only conditional
composition theorem of record is written over `tau_norm`'s class, and both of its
class-sensitive steps hold there.

[YOURS] **This is strong and it is not forcing.** R5 is a *premise of a conditional
theorem*, not a standing require: it says *assume* J_ref lands in the norm/module
class, *then* T_ref and T_phys discharge by composition. A different package landing
in a different class would need a different composition theorem — one that does not
exist, and whose non-existence is not a prohibition. I record the asymmetry at full
strength and decline to convert it into force.

[PROVABLE] `tau_norm` is `U` on R2 for a locatable reason: R2's requirement is stated
using four-volume, and `4d §6.1` records that the ratified completion's labels contain
*"no oriented edge set, path matrix, smooth base, spatial metric, or measure."* So R2
cannot presently be **stated** in `tau_norm`'s carrier — which is a missing-object
condition awaiting `J_ref`, not a violated require. This is exactly what *"lacks
geometric realization"* amounts to, and it is why I do not score it `V`.

### 2.4 Why `tau_density` is `U` almost everywhere

[PROVABLE] F2: `tau_density` is *"named operationally by V011 but not defined as a
complete topological carrier."* An object that is not yet a topology cannot be tested
for whether its limits acquire a tail (R3), whether structure maps land in a class
(R5), or whether separation holds (R6). Its `U`s are a **definitional gap**, not
evidence either way.

[YOURS] It is worth saying plainly what would change this: R2 is V011's own
requirement and is written in density terms, so `tau_density` is the candidate whose
native language matches the one require that mentions four-volume. If it were defined
as a carrier, it would be the natural home for R2. That is a reason it may not be
eliminated cheaply — not a reason to prefer it.

---

## 3. DD3 — VERDICT

```text
VERDICT = UNDERDETERMINED ( { tau_norm , tau_density } )
          with tau_cyl ELIMINATED by certificate §2.2.
```

[YOURS] The reasoning for choosing this over `FORCED`, stated because the choice is
close and the principal should see the seam.

Exactly one candidate satisfies every require it can be tested against. That is the
shape of `FORCED`, and DD3's own definition — *"exactly one survives"* — can be read
to license it. I decline, for one reason: **`tau_density` has not survived and has not
failed; it has not been evaluated.** Booking `FORCED(tau_norm)` would require treating
"not yet a defined carrier" as equivalent to "eliminated", and that converts *cannot
see this* into *false*. The jurisdiction check's second question exists for exactly
this, and R9's whole discipline is that an unevaluable object stays PENDING rather
than becoming a negative verdict.

So the honest report is: the free set is **halved**, one member is killed with a
two-link certificate, and of the two survivors one satisfies every testable require
while the other cannot yet be tested. **The principal's decision is now between a
candidate that works and a candidate that is not yet an object** — which is a far
better-posed choice than the three-way I handed back at 716, and it is still his.

[YOURS] What would convert this to `FORCED`, and it is small: **define `tau_density`
as a complete topological carrier, then re-run this matrix.** If it then violates R3
or R6 the verdict is `FORCED(tau_norm)` by elimination; if it satisfies them the
verdict is a genuine two-way free choice. Either outcome is a result. That is the
next relay-sized target on this thread, and it is cheaper than anything in the RA27
chain.

---

## 4. DD4 — WHAT THE HUNT SHOWS ABOUT `lambda`

[PROVABLE] **No enumerated require mentions a scale law.** R1 and R4 are about
derivation and cofinal independence; R3, R5 and R6 are about class membership and
separation; R7 constrains the chooser; R8 demands invariance. A1 is the sealed
absence: *"Neither DoR-007 nor DoR-008 chooses `lambda` or supplies a scale law."*

[PROVABLE] **Eliminating `tau_cyl` does not touch `lambda`.** The elimination runs
entirely through tail/separation properties of the completion class. (D2-4)'s freedom
is a rescaling of `ell_e` and `V_cell` on the finite data, which is upstream of every
link in §2.2's chain.

[PART-PROVABLE] **R2 is the only require that could bear on `lambda`, and I cannot
determine whether it does.** R2 demands a boundary term *"whose ratio to four-volume
tends to zero."* (D2-4) supplies `V_cell -> lambda^d V_cell` but **supplies no scaling
law for the boundary term**. Without one I cannot say whether the ratio's limit is
`lambda`-sensitive, and deriving a boundary scaling here would be authoring physics
this relay is not commissioned to author — and would touch a numeric evaluation the
gates forbid. Recorded as **UNDETERMINED, with the missing datum named**: a scaling
law for the boundary term under (D2-4).

[YOURS] The consequence for the program: **the topology decision and the `lambda`/`beta`
decision are independent.** Freezing a topology will not fix `lambda`, and fixing
`lambda` will not freeze a topology. My 716 question — whether (D2-4)'s `lambda` is
U1's `beta` — is untouched by this hunt and remains with the principal. Zero adoption
here, as commissioned.

---

## 5. SEARCHED SPACES, GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Searched spaces

| Claim | Searched space | Method |
|---|---|---|
| The three candidates are the only ones of record | `workspace/` + `supervision/`, `*.md`, recursive | object-names probe: `tau_norm`, `tau_cyl`, `tau_density`, `T_ref`, `T_phys`, `T_cyl`, `one frozen topology`, `frozen topology`, `response topology`, `named response topology` |
| R1–R8 are every sealed require touching the limit's sense | same | the above ∩ modality probes (`must`, `is to be`, void conditions, package fields, falsifier clauses) across the six consumer families the relay names |
| No require mentions a scale law | same | fixed-string `lambda`, `scale law`, `scaling`, cross-read against R1–R8 |

[YOURS] Not searched, therefore not claimed about: the cleanroom mirror, the origin
corpus outside the archive, non-`.md` files, and `rd22_run_*/evidence/` digest-prefixed
mirrors. Per `STAGE8_LANE_STATUS.md`'s standing caveat, every absence here is a **lower
bound**, and this relay is the second in a row where the object-names probe found
material a demand-vocabulary search had missed.

### 5.2 Grounding

| # | Step | Source + span | Tag |
|---|---|---|---|
| 1 | R1 DoR-007 theorem content | `17dc023e…[253,529)`, `[1194,1310)` | PROVABLE |
| 2 | R2 V011 density require | `aa7c6d49…[47025,47247)` | PROVABLE |
| 3 | R3 topology field | `430f0971…[23693,23835)` | PROVABLE |
| 4 | R4 cofinal independence | `430f0971…[11040,11150)` | PROVABLE |
| 5 | R5 §9 premise 1 | `430f0971…[25514,25583)` | PROVABLE |
| 6 | R6 §9 separation step | `430f0971…[26183,26282)` | PROVABLE |
| 7 | R7 void condition | `ccb94dfa…[22630,22730)` | PROVABLE |
| 8 | R8 J2 invariance | `5f4979d5…` J2 clause | PROVABLE |
| 9 | F1 Q-247 separation split | `430f0971…[17490,17655)` | PROVABLE |
| 10 | F2 the three candidates' properties | `430f0971…[11420,11669)` | PROVABLE |
| 11 | F3 tail exclusion is TYPE-P | `430f0971…[18031,18171)` | PROVABLE |
| 12 | A1 no scale law | `8dd59b35…[12810,12879)` | PROVABLE |
| 13 | `tau_cyl` violates R3 | §2.2 L1–L3, from 3 + 10 | PROVABLE |
| 14 | `tau_cyl` violates R6 | §2.2 L4–L6, from 6 + 9 + 10 | PROVABLE |
| 15 | `tau_norm` S on R5, R6 | from 5, 6, 9, 10 | PROVABLE |
| 16 | `tau_norm` U on R2 (missing object, not violation) | §2.3, 4d §6.1 | PART-PROVABLE |
| 17 | `tau_density` U (definitional gap) | from 10 | PROVABLE |
| 18 | Verdict UNDERDETERMINED, not FORCED | §3 | **YOURS** |
| 19 | The next target: define `tau_density` as a carrier | §3 | **YOURS** |
| 20 | `lambda` untouched; R2's bearing undetermined | §4 | PART-PROVABLE |

```text
GROUNDED_STEPS = 18 / 20
YOURS, NAMED: 18 (the verdict call between FORCED and UNDERDETERMINED),
19 (the proposed next target).  Neither is a premise for any PROVABLE row.
```

### 5.3 Jurisdiction check

**R3 and R6, the eliminating requires.** Written to keep the limit inside a class where
separation works and no invisible tail survives — the risk is squarely present, since a
tail is exactly content that no finite restriction can see. Their outcome space
distinguishes false from cannot-see: a topology either admits the tail or does not, and
`tau_density`'s inability to be tested returned `U`, not `V`. And they permit the
evidence: a lawful limit in a separating class satisfies them.

**R7, the void condition.** Written against choosing the topology by the coefficient it
yields. Present and binding on the principal's act. Nothing in this artifact consults a
coefficient, and no candidate was preferred by any downstream output.

**R9 / R9-JII.** Unchanged. R8 remains unsatisfiable by any candidate because the
carrier's common-cell quantifier is still PENDING; this hunt does not touch that, and
§3's refusal to book `FORCED` is the same discipline applied one level up.

**DoR-007.** No smooth structure was imported, requested, or assumed. R1 was used only
as a non-discriminating require, which is what its text supports.

### 5.4 Self verb audit

| Verb or status | Warrant |
|---|---|
| `ELIMINATED` | used once, with a two-link certificate quoting the require and the fact |
| `VIOLATES` | only where a require's text and a sealed property statement conflict directly |
| `UNDETERMINED` | where the candidate cannot be tested — never used to mean "fails" |
| `SATISFIES` | only against requires the candidate can be tested against; `U` elsewhere |
| `UNDERDETERMINED` | the verdict, chosen over `FORCED` with the seam displayed at §3 |
| `untouched` | `lambda` — no require mentions a scale law, and R2's bearing is marked undetermined rather than computed |

[YOURS] Disclosures against myself:

1. **I presented a disqualification as a cost.** At 716 I put *"admits a bidual tail"*
   in a costs column beside *"lacks geometric realization"* and *"not defined as a
   carrier"*, as if the three were the same kind of thing. One of them meets a stated
   require head-on and kills its candidate; the other two are missing-object and
   definitional gaps. The principal's reframe found this, not I. **A cost column
   flattens modality, and modality is exactly what an allow/require law turns on** —
   I should have tagged each by its own verbatim modality at 716, which is what this
   relay had to do afterwards.
2. **I nearly booked FORCED.** Exactly one candidate survives every testable require,
   and the pull toward the stronger headline was real. `tau_density` is unevaluated,
   not eliminated, and §3 says so. If the principal reads the asymmetry as decisive,
   that is his call to make on a displayed record — not mine to make by scoring an
   untested candidate as dead.
3. **The object-names probe found two artifacts my 716 search missed**, one of which
   (`4dCI`) names the five public consumers of the limit and one of which (`R4K`)
   carries the anti-tuning void condition on the choice itself. Both are load-bearing
   here. That is the second consecutive relay where my own newly-installed law caught
   what my previous search had not — which is evidence the law was needed, and also
   evidence about how much of my earlier scoping rested on vocabulary luck.
4. **Two of my probes failed on line wraps again** while generating R4 and R6's spans;
   the whitespace rule caught them before they entered the artifact. The spans here
   were all generated end-from-last-token per my own 716 rule, and block-covered.
5. No verb here proves, authorizes, computes, binds a member, forms a common cell,
   evaluates a junction map, adopts anything, or grants a seal.

```text
REQUIRES = 8 enumerated (R1-R8 REQUIRE; A1 ALLOW; F1-F3 facts; WANT = 0)
MATRIX = 3 x 8 displayed at §2
VERDICT = UNDERDETERMINED({tau_norm, tau_density}) -- tau_cyl ELIMINATED by
          certificate §2.2 (violates R3 and R6 on two independently sealed facts);
          tau_norm satisfies every testable require and is the class the only
          composition theorem of record is written over; tau_density is
          unevaluable, not eliminated, and §3 states why that is not FORCED
LAMBDA_NOTE = stated (§4): no enumerated require mentions a scale law; the
          elimination does not touch (D2-4); R2's bearing is UNDETERMINED for a
          named missing datum -- a boundary-term scaling law; the topology and
          lambda decisions are independent
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+5 disclosures at §5.4)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
