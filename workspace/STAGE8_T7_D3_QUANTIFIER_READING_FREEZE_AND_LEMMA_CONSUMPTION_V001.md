# Stage-8 T7 D3 Quantifier-Reading Freeze, and Consumption of Two Codex Lemmas V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_TYPING_FREEZE (part 1) — SEALED BEFORE PART 2
+ CONSUMPTION OF TWO INDEPENDENT-SYSTEM LEMMA RETURNS (part 2)
```

Order of operations is deliberate and is the reviewer's requirement: the
D3 reading is frozen FIRST, on grounds that PREDATE both lemmas, and only
then are the lemmas consumed. The reviewer's warning is recorded and
honoured: this must not be settled implicitly by whichever lemma is
convenient.

## PART 1 — THE D3 QUANTIFIER READING, FROZEN

```text
FROZEN READING (the standard one): "all common refinements of members of
A and B" means ANY cellulation refining BOTH parents — unrestricted,
universally quantified. It is NOT the minimal overlay (A-with-B meet
cells only).
```

GROUNDS, all antedating the lemmas that consume this freeze:

```text
G1 THE SEALED TEXT ITSELF. D3 reads "plus: all common refinements of
   members of A and B", and "The theorem's quantifier ranges over the
   skeleton, A, B, and common refinements." "All ... refinements" is
   plural and unrestricted; no cardinality or construction is attached.
G2 NO NARROWING DEFINITION EXISTS ANYWHERE IN THE CORPUS. A search of
   the sealed artifacts for overlay / meet-cell / minimal-common /
   coarsest-common language returns NOTHING. The standard mathematical
   reading is therefore the only one the corpus supplies.
G3 FENCE F-2, SEALED IN THE SAME SPEC, FORBIDS THE NARROW READING'S
   EFFECT: "Pinning the quantifier to any finite list is NOT available to
   any lane under any outcome." The minimal overlay of two given members
   is essentially a single cellulation; restricting to it is exactly the
   finite-list narrowing F-2 prohibits. F-2 was sealed long before either
   lemma existed.
G4 T13 DOES NOT NARROW IT. The battery's "the pullback-commutation check
   runs on a common refinement" locates ONE CHECK on ONE witness; it says
   nothing about the theorem's quantifier. Reading it as a restriction
   conflates a check's venue with a theorem's scope.
```

ANTI-CONVENIENCE STATEMENT, required by the reviewer's caveat: this
freeze makes one of the two consumed lemmas BITE (it validates Q6's
refutation and closes the raw-degree route). That is a consequence, not a
motive. The grounds are G1-G4, every one of which is sealed text older
than the lemma it now supports, and G3 in particular argues for the
standard reading in a direction OPPOSED to this lane's convenience — a
narrow reading would have retired a blocker rather than confirming one.
If the principal holds that the sealed INTENT differed from the sealed
TEXT, that is his to say and this freeze is superseded on his word; no
lane may make that call.

## PART 2 — CONSUMPTION (valid only under the Part-1 freeze)

### 2.1 Q6 is REFUTED AS STATED on full D3

```text
Q6_UNIFORM_RAW_ADJACENCY_DEGREE_FALSE_ON_FULL_D3
  Construction (Codex, independent system): arbitrary further common
  refinements admit star-refined atoms of unbounded facet count, so NO
  uniform raw adjacency-degree bound exists over full D3.
DECISIVE CONSEQUENCE: the derive-D5-from-local-weights-plus-raw-degree
  route is IMPOSSIBLE on full D3. RAW-DEGREE KP IS CLOSED.
```

The two-readings question is thereby FORCED rather than chosen:

```text
READING A (adopted, and now FORCED not preferred): M2/D5 supplies the
  ANCHORED-SUM bound DIRECTLY; M3's closure is a GEOMETRIC SERIES and the
  KP paragraph is rewritten as that. This was already committed in
  STAGE8_T7_PRODUCTION_GATE_NOGO_AND_PIPELINE_REPAIR_BINDING_V001 (D1);
  that commitment is now compelled by the refutation, which strengthens
  it — and its honest cost is unchanged: THE BURDEN SITS ENTIRELY ON M2,
  which is the open E1 obligation.
READING B (admissible only in one form): a WEIGHTED,
  SUBDIVISION-INSENSITIVE polymer-counting replacement, PROVED. Raw
  degree may not appear in it. Absent that proof, reading B is not
  available.
Q6 IS NOT RETIRED: it is RE-CAST as the weighted/subdivision-insensitive
  counting obligation, live only if reading B is ever pursued. Its old
  raw-degree form is closed as FALSE, which is a result, not a gap.
```

### 2.2 A-L0 arm 2 is CONCRETIZED (the only surviving certification route)

```text
NAMED TARGET: E1_ONE_LINE_CONNECTED_CROSS_TERM_FREE_HUYGENS_BOUND
The free/Dyson propagation bridge must supply BOTH:
  (i)  LIGHT-CONE / HUYGENS COLLAR SUPPORT, reducing the shell count
       from R^3 to R^2; AND
  (ii) AMPLITUDE DECAY of at least R^-1 after the cell-time integrations.
Then the anchored sum carries R^2 * R^-3 * R^-1 = R^-2, which is
summable. Neither factor alone suffices: (i) without (ii) leaves R^-1
(divergent); (ii) without (i) leaves R^-1 (divergent).
FAILURE WITNESS unchanged: E1_ONE_LINE_CONNECTED_CROSS_TERM_UNCERTIFIED.
```

CANCELLATION INVENTORY, verified consistent with the sealed record:
CTP/Moebius kills disconnected terms ONLY; the spinor-odd trace and
m0 = m1 = 0 are SATURATED at full tau_R (this lane's own C4);
time-integration smoothing is the ONLY remaining summability source.

CONVERGENCE OF RECORD: that conclusion is independently identical to this
lane's own §1.6 finding that only TWO-TIME / scattering-type objects can
work, the equal-time object provably failing Shale-Stinespring. Two
systems reached it by different routes; it is now the organizing
constraint of arm 2 rather than an observation.

### 2.3 Calibration

Codex's pre-frozen P1 (honest split) and P2 (no raw-degree bound) BOTH
LANDED. Logged in CALIBRATION_LEDGER.md as an independent-system
prediction family performing well — the second consecutive cycle in which
its predictions held while this lane's magnitude/route-survival
predictions did not.

## Effect on authoring

```text
- The E1 successor spec v002 authors A-L0 as arm 2 ONLY, with the
  Huygens-bound target of 2.2 as its exact statement.
- Reading A is authored as the compelled reading, with the M2 burden
  stated in the verdict section, not buried.
- Q6 appears in its RE-CAST form only; its raw-degree form is recorded
  as closed-false.
- All prior bindings (R1-R8, the Codex supplement, the pipeline binding's
  A1-A5) stand.
```

## Protected status

```text
D3_quantifier_reading_frozen = standard_all_common_refinements
Q6_raw_degree_form = CLOSED_FALSE
Q6_recast_weighted_form = open (reading B only)
reading_A_compelled = true
A_L0_arm2_target = E1_ONE_LINE_CONNECTED_CROSS_TERM_FREE_HUYGENS_BOUND
E1_one_line_cross_term_uncertified = true   (open)
connected_linked_cluster_density_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
