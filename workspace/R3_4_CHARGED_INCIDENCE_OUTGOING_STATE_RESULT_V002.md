# R3.4 Charged-Incidence Outgoing-State Result v002

## Verdict

```text
CHARGED_INCIDENCE_OUTGOING_STATE_CONDITIONAL_ON_CAUSAL_SUPPORT
```

The later charged incidence operator repairs the superseded controlled-X
dependency at the endpoint and derives a compatible quasi-local state lift.
It does not yet derive durable continuous parent dynamics.

## Exact endpoint map

Starting only from

```text
B_ch=P_ch tensor gamma^5 tensor c_partial,
tau_R=pi/sqrt(2),
```

the executable diagonalizes the full operator. On both chiral sectors it
finds

```text
exp(-i tau_R gamma^5 tensor c_partial)
  =I_chiral tensor
    [[0,1,0],
     [1,0,0],
     [0,0,-1]].
```

Thus the complete record action is:

```text
|r> <-> |p_Q>;
|e_Q> -> -|e_Q>.
```

The operation was computed rather than inserted. Its numerical matrix error
is `1.79e-15`. The source-controlled version has error `2.53e-15`, commutes
with charge exactly, is neutral-sector identity, and is source-spin identity
at the completed endpoint.

This is a stronger and cleaner endpoint result than the retired controlled-X
candidate: the unused edge-sector action is fixed by the incidence operator.

## Compatible finite completed states

For charge-superselected source states and `N` distinct ready record cells,
the finite completed states are generated by the product of the derived
edge-local unitaries. The exact restriction errors are:

```text
omega_2 -> omega_1: 4.37e-16;
omega_3 -> omega_2: 4.46e-16.
```

The arbitrary-`N` result is analytic. Write `P_0=I-P_ch` and let `S_j` be the
computed full root/public swap on cell `j`. Orthogonality of the charge
projectors gives:

```text
product_(j=1)^N [P_0 tensor I+P_ch tensor S_j]
  =P_0 tensor I+P_ch tensor product_(j=1)^N S_j.
```

For any normalized source density matrix satisfying charge superselection,

```text
rho_S=P_0 rho_S P_0+P_ch rho_S P_ch,
```

and the all-ready record state, the completed state is:

```text
rho_N
 =P_0 rho_S P_0 tensor |r><r|^tensor_N
  +P_ch rho_S P_ch tensor |p_Q><p_Q|^tensor_N.
```

Tracing the final record factor returns `rho_(N-1)` identically. The
executable's `N=1,2,3` calculations are regression checks of this formula,
not the basis for the arbitrary-`N` statement.

Every later-cell pulse commutes exactly with every earlier public-record
observable. At the completed endpoint it also restores every source-spin
observable, with the tested commutator below `2e-15`.

The finite states therefore define a charge-superselected quasi-local state.
For

```text
M_N=(1/N) sum_(j=1)^N Z_j,
```

and any observable `O` supported on at most `m` record factors,

```text
||[M_N,O]|| <= 2m ||O||/N -> 0.
```

The charged and neutral state families have distinct limiting expectations,
so this is the same macroscopic central-sequence theorem re-established for
the incidence-derived states rather than imported from the retired
controlled-X map.

## Why durability is not yet closed

If the stationary incidence parent acts on every completed cell, the
one-cell endpoint overlap at

```text
t=pi/(2 sqrt(2))
```

is exactly `1/2`. For `N` completed cells the overlap is `2^-N`, which tends
to zero. Thus the stationary evolution moves the endpoint product state into
a disjoint infinite-product sector at generic nonzero time. It does not
implement one strongly continuous outgoing dynamics in the same GNS
representation.

The live parent lineage also still reports:

```text
time_dependent_continuum_ordering_derived = false;
connected_preparation_derived = false.
```

A finite causal cell specifies where the first transfer occurs. It does not
by itself prove that the interaction switches off, that the pointer algebra
is invariant, or that a Moller/scattering limit exists. Those must descend
from the complete parent under Parent-State Covariance.

## Earned statement

Inside the declared primitive pure-charge, vacuum-plus-one-source,
single-incidence branch:

```text
the full charged endpoint unitary is derived;
the charge-superselected finite endpoint states are restriction-compatible;
and their quasi-local public-record state exists.
```

The parent selection, preparation, post-write persistence, continuous
outgoing GNS dynamics, and physical response spectral measure remain open.

## Status

```text
retired_controlled_X_dependency_removed_at_endpoint = true
full_incidence_endpoint_unitary_derived = true
endpoint_map_charge_nondemolition = true
endpoint_map_spin_independent = true
finite_completed_state_family_restriction_compatible = true
quasilocal_public_record_state_derived = true
parent_selects_connected_preparation = false
one_use_causal_support_derived = false
stationary_parent_implemented_in_endpoint_GNS = false
complete_continuous_parent_covariance_derived = false
physical_durability_derived = false
complete_parent_action_derived = false
physical_response_spectral_measure_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
