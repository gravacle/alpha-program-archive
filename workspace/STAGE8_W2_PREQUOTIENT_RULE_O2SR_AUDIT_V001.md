# STAGE 8 — AUDIT OF W-2: THE MEMBER-SUPPLIED SEALED PREQUOTIENT RULE — COMMISSION O2SR — V001

## W2-AUDIT, 2026-08-15 — DEFAULT-REFUTE; TESTIMONY ZERO WEIGHT

TARGET: `STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md` (eacd810c…), audited against
its own sources at bytes. **Every claim was re-derived from the sealed sources
independently; nothing in the target was accepted on its own say-so.** Where the
target quotes, the quotation was pulled from the source artifact and compared;
where the target computes, the computation was re-executed in an independent
fresh venv; where the target reports a sweep, the sweep was re-run by a
different and stronger method.

```text
OVERALL VERDICT = CONFIRMED-WITH-CORRECTIONS
```

**THE TARGET'S CENTRAL RESULT SURVIVES INTACT.** W-2 is partly supplied by the
record's ratified authored premise: the rule half is on lawful stock; the member
half and the evaluation half are owed; the evaluation half is blocked at named
stopped joints rather than merely unperformed. Seven corrections are issued below,
all of DISPLAY grade — two substantive omissions, three labelling/display
defects, one spliced quotation, and one citation slip. **None of the four
commissioned hunts found a kill.**

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
COMMON_CELL_FORMED = false ; MEMBER_BOUND = false ; W2_SUPPLIED = false
MEMBER_LANDED = false ; FP2_DISCHARGED = false
```

This audit declares nothing, adopts nothing, lands nothing. No joint is
discharged, no member landed, no absence closed, no gate moved, no witness
retired. **SK-7 carried through: THE MEMBER IS NOT LANDED; Joints A/B/C/HJ7/E/F
stand.**

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md` and its `.seal.sha256` sidecar —
both probed **ABSENT** before any authored byte (`ls` exit 1 on both).

**SWEEP CUTOFF: 2026-08-15T16:56:26Z.** Exhaustiveness is sweep-relative;
nothing absolute is claimed.

---

## §0 — GROUND CONSUMED, SEALS RE-VERIFIED INDEPENDENTLY; SWEEP CUTOFF

Every file below was re-verified THIS session by `shasum -a 256 -c` **run from
the artifact's own directory** (the sidecars name basenames; running from a
parent reports false failures), full digest, BEFORE reliance.
**12/12 OK, zero mismatches, none unverifiable.**

```text
THE TARGET (1)
eacd810c76123cc8227e98bceab2c90947211fcd8bee374a68edd6c1e6cebeb6  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md           OK

THE COMMISSIONING GROUND (2) — §3.4 and §3.5 read IN FULL, per commission
afc3a79c646c044d9c34ede25fbc7166d43807c9ac91b64f078900f9309516cd  STAGE8_COMPLETION_MAP_T17SR_V001.md               OK
4504cd1448049496f0d5db9828fbed09b51b696cd11d0da1e978ad147964693a  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md         OK

CONSUMED INDEPENDENTLY (3) — the ratified member premise pair; the obstruction
d422fc453a8863ca5bcf001b6201d4342a53b6af7d9966ed41ef64930992d567  STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md           OK
e719044c8963adbbe0fdfece95d9d1f6429b2235ffd28bf0dcaa871c9bba37df  STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001.md     OK
fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md        OK

THE TARGET'S REMAINING CITED SOURCES (6), re-verified at path
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                     OK
80db260fa1561d76296d5f54e1e52397b79009b8a0d12bc060c140818c38fdf7  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md         OK
b72d7bfd047dd901ee9178635e450d6ba9d9f177382901f11defb37903e18f98  STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_V001.md         OK
c244d0d12057c51bc72e7ada97e42347ff0ccd4aacf93a928b841607795a5161  STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_AUDIT_V001.md   OK
9f0d12b4556427eb965bcd9c869f645b984009fdbcc0d1d19a2c7216d31c51f1  STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md OK
5f4979d50c905c009c1fa18cec65cde6d9812b7f6c8e7c6870e4e6bea6cf78d5  STAGE8_7A_R9JII_JOINT_LANDING_TEST_V001.md         OK
```

**DIGEST CROSS-CHECK, RUN NOT ASSUMED.** All nine digests the target lists
outside the commissioning ground were RECOMPUTED here with `shasum -a 256` and
compared character-by-character against the target's own §0 table: **9/9 EXACT
MATCH.** The target's §0 arithmetic is correct (2 + 2 + 1 + 6 = 11; the target
verified 11, this audit verified those 11 plus the target itself = 12). Note for
the record that this is a cleaner count than the commissioning ground's own,
whose `(12)`/`39` numerals its audit corrected to `(14)`/`41` at C-1.

```text
SWEEP_CUTOFF = DECLARED. Universe: the seal-verified files above at their bytes,
  2026-08-15T16:56:26Z, primary root /Users/bgm/MB Work/alpha-program-archive/
  workspace. Scoped sweeps run in the primary root ONLY:
    AS-1  output collision probe (artifact + sidecar) -> ABSENT before any byte.
    AS-2  filename sweep "ratif|addend|entr|adopt|DECISION_OF_RECORD" -> ~70
          artifacts enumerated at FILENAME GRADE (see W-C1); none opened beyond
          ATTACH_ENTRY_ADDENDUM_V001.
    AS-3  content sweep "d422fc45" across the WHOLE primary root -> the decisive
          re-derivation of §2.3 below.  Barred-pattern files were tested by
          filename-classified grep ONLY and NONE was opened.
    AS-4  token sweep of the completion map and its audit for
          "JD-3|T2SR|member-supplied|prequotient|d422fc45|e719044c|
          JD_MEMBER_CANDIDATE" -> §2.2 below.
    AS-5  numerics sweep of the target for any number carried as a value of a
          program quantity -> §5.4 below.
  Root 2 (the cleanroom mirror) NOT consumed — every named artifact resolved in
  the primary root at verified seals.  NO register/tracker/road/plan/
  continuation file was read; the class was excluded at pattern level and its
  members appear here only as counts.  "Q-..." tokens EXPECTED-UNLOCATABLE,
  carried as opaque labels, never chased.  Artifacts sealed after this
  commission's fire time UNSWEPT.  No git.
```

---

## §1 — THE COMMISSIONING GROUND, READ IN FULL AND RE-DERIVED

Per commission, the completion map's W-set section **§3.4** and minimal-completion
section **§3.5** were read IN FULL at bytes. Their derived content, as it bears on
this audit:

```text
§3.4  ZERO of the twenty-one (absence x live-failure-point) edges is DISCHARGES
      or PARTIALLY-DISCHARGES.  The closure's own dischargers are W-1/W-2/W-3.
      W-2's display, verbatim (map lines 330-334):
        "W-2 (discharges FP-2): the member-supplied sealed prequotient rule —
         CN-2 (R1): 'a member-supplied sealed prequotient rule (JD-3/JD-3a) with
         one displayed evaluation on a named oriented k-cell — absent from the
         scored corpus'; 'a sealed member evaluation on a named cell would
         replace the diagonal-majorant accounting (F2's blocked side) with the
         junction object's own symbol-side value.'"

§3.5  AT THE COMMISSIONED INVENTORY: no subset of A-1..A-7, INCLUDING THE FULL
      SET, discharges the closure at any live failure point -> EMPTY-INFEASIBLE.
      AT THE CLOSURE'S OWN NAMED DISCHARGERS: the minimal completion set is
      UNIQUE = {W-1, W-2, W-3}, because each failure point's discharger is
      displayed as SOLE at the closure's own bytes — F1's only escape is the
      sub-volume rate; F2's only displayed replacement is the symbol-side
      evaluation (boundary smoothing "ruled ... insufficient"); F3's route is
      "only permitted" C-L2 with the operator-norm alternative "EXCLUDED of
      record."  "No alternative minimal set exists at the displayed bytes; none
      is invented."
```

**RE-DERIVED, NOT ACCEPTED: the record's famous absences cannot touch W-2.** The
map's edge derivation is that the commissioned absence inventory and the
closure's failure points live on DISJOINT threads, and the map's audit re-walked
both edge directions at bytes: forward, the WBC build contains ZERO occurrences
of any commissioned absence's object tokens; reverse, none of the twelve
absence-side naming artifacts contains `prequotient` (or MO-3, C-L2, VOLUME
DIAGONAL, boundary-closure, four-quantity, rank-x-op), **sole hit being the G1
certificate's own gate description "HS bound at the volume diagonal" — its own
blocked item, adjudicated substring-of-its-own-gate at ACH-4.** All twenty-one
live edges are CANNOT-TOUCH at both endpoints' bytes. This audit re-ran the
`prequotient` side of that sweep and reproduces it.

**CONSEQUENCE, HELD AGAINST THE TARGET.** Because {W-1, W-2, W-3} is UNIQUE and
each member is SOLE, anything the target did to W-2 that (a) landed it,
(b) removed it, or (c) created a twenty-second edge would move the commissioning
ground's central derived result. The target does none of the three, and §6 below
confirms that at bytes.

---

## §2 — THE LEAD RE-DERIVED: WAS THE PREMISE REALLY UNCONSUMED, AND REALLY RATIFIED

### §2.1 W-2's decomposition into W-2a/b/c — **CONFIRMED as forced in substance**

The target splits W-2 into rule-as-bytes (W-2a), member-status of the supplier
(W-2b), and one displayed evaluation on a named oriented k-cell (W-2c). Checked
against the three sources that own the demand:

```text
FROZEN SPEC row R1 (9f0d12b4, line 95) — recovered verbatim at bytes:
  "| **R1** | **The cell rule exists** | A member supplies the sealed
   prequotient rule `Phitilde_N(s)(delta_e) := incidence/integration/evaluation
   of s on the named k-cell e` (JD-3) and its linear extension (JD-3a) | the
   sealed rule itself, as bytes, plus one displayed evaluation on a **named**
   oriented k-cell | `[45349,45957)` | PROVABLE |"
  -> the CRITERION cell says "A MEMBER supplies"; the EVIDENCE-FORM cell carries
     two halves joined by "plus".  The split is the row's own.

WBC CN-2 (80db260f, lines 502-508) — recovered verbatim, matches the target's
  §1 quotation character for character, including the parenthetical "(the spec's
  own hunt: MEMBER_FOUND = none)" and the whole Bearing sentence.

OBSTRUCTION §3.5 (fdf20bd4) — the (JD-3)/(JD-3a) block and the "must prove"
  sentence (orientation covariance, presentation independence, continuity,
  contact-kernel) recovered verbatim.
```

The target's decomposition is therefore not an authored reading imposed on the
demand — it is the demand's own sentence structure. **CONFIRMED.** (Ledgered by
the target at CL-3 as FORCED-in-substance; this audit agrees.)

### §2.2 "The completion map never consumed this premise" — **CONFIRMED**

Re-run independently (AS-4), not read from the target:

```text
grep of the MAP for "JD-3|T2SR|member-supplied|prequotient"
  -> exactly FIVE hits: lines 330, 331 (inside the CN-2 quotation of §3.4),
     390 (inside CONV-5's quotation of T14SR R-3), 614, 627 (inside the FLAG
     BLOCK's MINIMAL_COMPLETION_SET and CONVERGENCES restatements).
     EVERY hit is inside a quotation of CN-2 or of T14SR's target-form display.
grep of the MAP for "d422fc45|e719044c|JD_MEMBER_CANDIDATE"   -> ZERO hits.
grep of the MAP AUDIT for the same three tokens AND for "T2SR" -> ZERO hits.
```

The map's §0 enumerates `THE NAMED GROUND (27)` and `LOCATED BY DECLARED SCOPED
SWEEP (12)` with tally `39/39`; its audit's C-1 resolves the enumeration to 41
files. **The candidate and its check appear in neither list, are never named,
never digested, never consumed** — CONFIRMED at bytes by direct grep of both
artifacts. The audit's reverse-edge sweep was indeed scoped to "the twelve
absence-side naming artifacts" (audit §A3 region, line 136), a set that by
construction excludes the premise; and ACH-4's `prequotient` row does read
"(one was; adjudicated substring-of-its-own-gate)", that hit being the G1
certificate's own gate description. **The target's §2.1 is CONFIRMED in every
particular.**

### §2.3 The ratification's standing — **CONFIRMED, and re-derived more strongly**

G-2 was recovered from T9SR at bytes (lines 61-67) and matches the target's
quotation exactly, including the load-bearing clause **"LAWFULLY CONSUMABLE AS
PREMISE, always as AUTHORED PHYSICS, never as derivation. Its SK-7 display is
PART OF THE PREMISE: the member is NOT landed; Joints A/B/C/HJ7/E/F remain
stopped."** The T9SR audit's independent re-verification of both digests is
present at its own bytes.

The target reports the ratifying instrument itself NOT FOUND. **This audit
re-derived that finding by a stronger method than the target used**, and it
holds:

```text
AS-3, the decisive sweep.  G-2 states the ratification was made "by the
principal's ratifying addendum NAMING BOTH DIGESTS."  Any such instrument must
therefore contain the digest string.  Content sweep of "d422fc45" across the
ENTIRE primary root returns exactly FIVE files:
    STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md.seal.sha256   (its own sidecar)
    STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001.md         (its check)
    STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_V001.md             (the consuming build)
    STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_AUDIT_V001.md       (its audit)
    STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md               (the target itself)
and — tested by filename classification only, NONE opened — ZERO files of the
register/tracker/road/plan/continuation class contain it either.
NO RATIFYING INSTRUMENT EXISTS IN THE PRIMARY ROOT AT THIS CUTOFF.
```

ATTACH_ENTRY_ADDENDUM_V001 was opened and confirms the target's reading: it is
dated 2026-08-12 and its ENTERED ARTIFACT is
`STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md`, SHA-256 `ccca6bb4…` — **a
different candidate under a different authority chain.** Not the JD entry.

**AUDITED STANDING, stated without upgrade:** the ratification is CARRIED OF
RECORD by two independently sealed artifacts naming and path-verifying both
digests; the instrument itself is UNLOCATED. The premise's own face bars
consumption "until that entry addendum exists" — a bar the target displays in
full at §2.2 rather than eliding. The target consumes on the same footing the
record's own consuming build (T9SR) did, discloses the gap in three places
(§2.2, CL-2, FLAG BLOCK), and no verdict it reaches depends on the distinction.
**This is the correct handling of the situation and this audit CONFIRMS it.**
See W-C1 for the one display defect in how the search was shown.

---

## §3 — THE FOUR COMMISSIONED HUNTS, HARDEST FIRST

### §3.1 HUNT 1 — THE GRADE: is the AUTHORED premise anywhere consumed as DERIVED?

**NO KILL FOUND.** Walked every occurrence in the target of `deriv*` and every
step that consumes premise content:

```text
SUBSTANCE TEST (the one that decides).  A grade kill requires the target either
(a) to treat the (JD-3)/(JD-3a) rule as following from sealed stock, or (b) to
present its own construction as a derivation from sealed stock.  Neither occurs:
  - §2.2 states the consumption footing explicitly: "AS PREMISE, always as
    AUTHORED PHYSICS, never as derivation."
  - §3.1 states it again inside the grade itself: "authored physics, lawfully
    consumable as premise, never as derivation."
  - §6 states it a third time in the verdict block.
  - §4's construction is labelled throughout as a construction ON an entered
    premise, with an explicit lawfulness line (§4 preamble) forbidding new
    authored data, unformed objects, and unselected branches — and every step
    classified against it.
  - The FLAG BLOCK carries the premise-pair provenance and RATIFICATION_STANDING
    rather than asserting derivation.
The target's own derivations are ABOUT the record (that the map's W-2 display is
stale; that the evaluation is not constructible), never OF the premise.
```

**One display defect, not a kill (W-C6):** the grade LABEL coined at §3.1 —
`AUTHORED-PREMISE-DERIVED` — is glossed correctly at both §3.1 and §6, but
appears UNGLOSSED in the FLAG BLOCK (`SUPPLIED (grade AUTHORED-PREMISE-DERIVED)`),
which is the one block written for fast scanning. A reader taking the flag block
alone sees `DERIVED` attached to a `SUPPLIED` verdict on an authored object.

### §3.2 HUNT 2 — THE SK-7 BAR: is the member anywhere claimed landed?

**NO KILL FOUND. THE BAR IS HELD EVERYWHERE.** Recovered the banner at the
premise's own bytes (lines 17-22) and compared:

```text
PREMISE, verbatim:  "**SK-7 BANNER, ON THE FACE: THIS CANDIDATE DOES NOT LAND
THE MEMBER.** Joint A (`SOURCE_EXTENSION_INHABITANCE`) remains STOPPED and
blocks both routes, and Joints B, C, HJ7, E, and F remain STOPPED/open of record
(fdf20bd4 line-1723 determination; ba9430c1 K5). This candidate targets Joint D
only, and even that discharge is proposed, not performed. Any presentation of
this artifact as the member landing is false and self-killing."
```

The target's §3.2 quotation is byte-faithful and its one ellipsis elides only
`(fdf20bd4 line-1723 determination; ba9430c1 K5). This candidate targets Joint D
only, and even that discharge is proposed, not performed.` — **material ADVERSE
to the candidate, not favourable to it.** An elision that drops the candidate's
own additional self-limitation cannot inflate the candidate; it is a faithful
shortening.

Positive checks, all passing: the target grades W-2b **NOT SUPPLIED**; carries
`MEMBER_LANDED = false` and `MEMBER_BOUND = false` in the face fence block and
again in the FLAG BLOCK; names all six joints (A, B, C, HJ7, E, F) at OWED-1;
reproduces G-2's "SK-7 display is PART OF THE PREMISE" clause; and states on its
own face "**THIS ARTIFACT DECLARES NOTHING, ADOPTS NOTHING, AND LANDS
NOTHING.**" **No claim anywhere that W-2's supply lands the member.** Premise §9
`DOES NOT: land the member (SK-7 banner)` recovered verbatim and confirms the
premise's side.

### §3.3 HUNT 3 — a "displayed evaluation on a named oriented k-cell" claimed without cell and evaluation at bytes?

**NO KILL FOUND — the target claims the opposite, and holds the line where it
would have been easiest to cross.** §3.3 grades W-2c **ABSENT**; §4.2 confines
CE-2 to "the FORM level — i.e. as an identity between expressions, **with no cell
of record identified and no symbol datum supplied**"; §4.2 then quotes the
obstruction's own §5.1 pass criterion against its own construction and concludes
"**CE-1/CE-2 are NOT the demanded evaluation, and this artifact does not present
them as one.**" The §5.1 pass criterion was recovered verbatim at bytes and the
quotation is exact, including the final sentence "Where an actual manifest is
absent, totality is not inferred."

**One labelling defect, not a kill (W-C4).** The §7 battery summary calls
W2-CAS-2 "reversal identity on ONE **named** oriented cell" and W2-CAS-3a
"(JD-3a) on a **named** chain", and the appended source carries the comments
`# the record's named oriented pair (primitive gamma_RL, reverse -gamma_RL)` and
`# the record's named chain c_RL = sum_e c_e e and its reverse -c_RL`. **No cell
is named in either model.** Both are generic fixtures (symbolic path
coefficients `p0:12`, 1-form coefficients `q0:30`, chain coefficients `c0:5`,
per-cell values left as free symbols `I0:5`). The word "named" is used there in
exactly the sense W-2c demands, on objects that are not named — against §4.2's
own "no cell of record identified" and against H-2. It is a CORRECTION rather
than a kill because the artifact disclaims it in prose in two places (§7's
reading discipline: "no PASS above is an evaluation in W-2c's sense — they are
algebraic identities on generic placeholders"; §9: the fixtures "stand for
nothing"), and because no record cell is in fact instantiated anywhere.

### §3.4 HUNT 4 — new authorship smuggled as construction

**NO KILL FOUND.** Tested at the byte level, clause by clause.

```text
THE AUTHORED CORE, isolated at the premise's own bytes.  Premise §3.8 (T2-8)
contains exactly TWO authored definitional clauses against W-2's demand:
      Phitilde_N(s)(delta_e) := integral_(gamma_(cell_N(e))) a_s   (T2-8/JD-3)
      Phitilde_N(s)(sum_e a_e delta_e) := sum_e a_e Phitilde_N(s)(delta_e)
                                                                  (T2-8/JD-3a)
Everything else in that block — W_N, Phi_N, iotatilde_N^H, iota_N^H — is marked
by the premise itself as consequence, not authorship: "(well-defined by P-2/P-4)"
and "((JD-2) display)".

THE DECISIVE COMPARISON.  The display block reproduced at the target's §3.1
(lines 258-269) was compared line-by-line against premise §3.8 (lines 337-347):
**BYTE-IDENTICAL, all eleven lines.**  The target adds NOTHING to it.  And the
target's own prose separates the two clauses from the rest correctly: "the
premise's §3.8 (T2-8) instantiates (JD-3) and extends it by (JD-3a), verbatim on
the forced target form, WITH THE INDUCED (JD-2) COMPOSITES."

CE-1, tested for smuggled data.  Its substitution chain was re-walked against
the premise's entered definitions at bytes:
    T2-7 (premise §3.7): rho_N := lambda_JD . ( Pi_iR o flat_g o M_1 o sect_N )
    T2-2 (premise §3.2): sigma_field,N([s]_(Ctc_N^sym))
                            := [gr_N(s)]_(closure(gr_N(Ctc_N^sym)))
Both recovered verbatim; CE-1's four lines are exactly these two substituted
into T2-8/JD-3, plus the unit pull-out.  NO new symbol, NO new datum, NO new
map, NO new law.  The pull-out's licence — rho_N's own R-scalar structure — is
the premise's own, and is re-verified symbolically at W2-CAS-1 (re-executed, §5).

CE-2a..d, tested the same way.  CE-2a = P-1 at a single cell; CE-2b = (JD-3a)
itself; CE-2c = (JD-3a) on a reversed chain; CE-2d = P-4 at a single cell.  All
four are instantiations of premise obligations, not new obligations.

CL-5 refuses the one step that WOULD have been new authorship — assigning
cell_N(e) = gamma_RL for a named record cell — and displays the road not taken
instead of walking it.  Premise §3.6 (T2-6)'s CAUTION, recovered verbatim,
confirms that this step would form the common cell and advance R9-JII.
```

**Verdict on hunt 4: the construction stays inside the premise's entered content
throughout. Nothing beyond the authored core is authored here.** One material
omission is issued as W-C3 below — not new authorship, but an undisclosed
caveat carried by an authored clause the target consumes.

---

## §4 — THE THREE HALTS, RE-DERIVED AT THEIR SOURCE BYTES

The target's §4.3 is the load-bearing part of its structural claim. Each halt was
re-derived from the source artifact, including the byte-offset citations.

```text
H-1 THE DOMAIN HALT — CONFIRMED, and the quotation is CONTIGUOUS AND COMPLETE.
    The obstruction's §5.2 table row 1 was recovered raw (fdf20bd4 line 1411).
    Its middle cell reads, in full and in order:
      "form actual `kappa_T,RL`; display `Loc_RL^phys(kappa_T,RL)`; give the
       cell rule `(JD-3)`; display `Loc_RL^C(kappa_T,RL)`; carry both
       `E_C,RL c_RL=0` and `E_C,RL c_RL=c_RL` without selection"
    The target reproduces all five semicolon-separated demands with NOTHING
    dropped and NOTHING reordered — the contiguity claim survives inspection.
    The row's Current-result cell reads "`STOPPED` at Joints A--D" — verbatim.
    THE ORDERING POINT IS REAL: "form actual kappa_T,RL" precedes "give the cell
    rule (JD-3)" in the row's own sequence, so the actual datum is demanded
    BEFORE the rule, exactly as the target argues.
    Premise §9 confirms from its own side, verbatim: "THE COMPOSITE'S DOMAIN IS
    EMPTY ON ACTUAL STOCK UNTIL JB-2 LANDS".  Check F-4, verbatim: "JB-2
    undischarged of record (line 1723; D-1 §2.3 row B); the actual-stock domain
    is empty until it lands".  Both exact.

H-2 THE NAMED-CELL HALT — CONFIRMED, both byte spans verified by direct read.
    [55251,55373) of fdf20bd4 reads exactly:
      "| A8/R9 | one common physical cell on which both typed returns exist
       independently | no common cell is presently formed |"
    [64793,64929) of fdf20bd4 reads exactly:
      "keep R9 PENDING until one common physical cell exists;
         add no route, branch, address, or member selection.        (CARRY-1)"
    Premise §3.6 (T2-6)'s CAUTION recovered verbatim, including "OVERLAPPING,
    not IDENTICAL", "nothing here forms a common cell", and
    "`COMMON_CELL_FORMED = false` throughout."  All exact.

H-3 THE GAUGE HALT — CONFIRMED.  Premise §3.3, verbatim: "The section is an
    authored gauge of the construction: distinct admissible sections are
    DISTINCT CANDIDATES; no canonical section is claimed".  Premise §3.5 (T2-5)
    carries "VALUE: NOT ASSIGNED. No number appears; 'silently fixed to 1' is
    excluded on this artifact's face" and the SK-10 anti-tuning row, which
    names R9-JII J1/J3 as "a FALSIFIER, never a constructor".  Exact.
    The target correctly grades H-3 residual and NON-BINDING rather than
    treating it as a third independent block — H-1 and H-2 each suffice alone.
```

**AUDITED CONCLUSION ON §4.** The step from CE-1's evaluation form to a displayed
evaluation is blocked by two independently sufficient obstructions of record,
each recovered at its own bytes, plus a displayed gauge residual. The target's
claim that the absence is **STRUCTURAL rather than merely un-performed** is
earned: it is not asserted ahead of the attempt, it is read off the attempt's
halt. The scoping is correct and not overstated — "NOT constructible **on the
ratified rule today**", "on the premise alone" — never "impossible".

---

## §5 — THE CAS BATTERY, RE-EXECUTED INDEPENDENTLY

### §5.1 Provenance chain — **CONFIRMED**

```text
Script located at path; recomputed digest:
  ac359bc4808d54c30b3313770391067fd017c3102eea56fe59cb0f704ec3ecf5
Target's claimed digest (§7):
  ac359bc4808d54c30b3313770391067fd017c3102eea56fe59cb0f704ec3ecf5   MATCH.
§7.1's appended source was extracted from the artifact's own bytes (lines
627-745) into a separate file and DIFFED against the script on disk:
  IDENTICAL, zero lines differing; the extract's own sha256 is ac359bc4…
  -> the "appended verbatim by file-append" claim is TRUE at bytes, and the
     appendix is a faithful copy rather than a retyped paraphrase.
```

### §5.2 Re-execution — **CONFIRMED, 7/7, including the exact residual**

Run in an **independent fresh venv created by this audit** (sympy 1.14.0), from
the extract taken out of the artifact — not from the build's own file:

```text
W2-CAS-1  unit pull-out lambda_JD (T2-7 into T2-8):              PASS
W2-CAS-2  reversal identity on ONE oriented cell:                PASS
W2-CAS-3a (JD-3a) on the chain: c and -c sum to zero:            PASS
W2-CAS-3b single-cell reversal flips exactly that summand:       PASS
W2-CAS-4  contact-kernel vanishing at the single cell (P-4):     PASS
W2-CAS-5a DOMAIN HALT: unformed datum leaves an unresolved symbol,
          not a value:                                           HALT-CONFIRMED
W2-CAS-5b SECTION-GAUGE HALT: distinct admissible sections are
          not identically equal:                                 HALT-CONFIRMED

W2-CAS-5b residual (symbolic): Integral(delta_sect(s), (s, 0, 1))
ALL_IDENTITIES_HOLD = True  (7/7)
```

The residual printed by this audit's independent run is
`Integral(delta_sect(s), (s, 0, 1))` — **character-for-character the expression
the target displays at §7.** A displayed CAS output that reproduces exactly on
re-execution is the strongest grade of confirmation available here.

### §5.3 Weight of the battery, stated honestly (not a correction — a calibration)

```text
SUBSTANTIVE:  W2-CAS-1 (homogeneity of the line-integral functional in the
  entered R-scalar) and W2-CAS-2 (exact closed-form change of variables, generic
  cubic path against generic quadratic 1-form components, integrated in closed
  form and shown to cancel identically against the reversed path).
BOOKKEEPING ONLY:  W2-CAS-3a, 3b, 4 are trivial symbol algebra — a sum plus its
  negation, a single flipped summand, a zero integrand.  They cost nothing and
  prove correspondingly little.
DISPLAYS, NOT PROOFS:  W2-CAS-5a asserts non-numberhood of an expression built
  to be non-numeric; W2-CAS-5b models the section-gauge difference as an
  additive integrand perturbation, which is a modelling choice.
```

The target does not overclaim on any of these: §7's own reading discipline
confines the battery to "verify that CE-1/CE-2 are what §4 says they are" and
"that the halts are what §4.3 says they are," and explicitly denies that any
PASS is an evaluation in W-2c's sense. **The calibration above is offered so the
battery is not later weighed above its grade; it is not a defect of the target.**

### §5.4 Fences and numerics — **CONFIRMED CLEAN**

```text
AS-5, run over the whole target.  Every digit-bearing line was classified.
Digits occur ONLY as: seal digests, seal counts (11/11, 7/7, 9/9), dates and the
sweep-cutoff timestamp, line/byte references, artifact version tags, section and
row labels, the sympy version, and inside verbatim quotations of record text
(e.g. the closure's own "E_C,RL c_RL=0 and E_C,RL c_RL=c_RL", carried WITHOUT
selection, exactly as the source carries it).
NO NUMBER APPEARS AS A VALUE OF ANY PROGRAM QUANTITY.  CONFIRMED.

lambda_JD:  no assignment, no rescaling, no normalization, no silent-to-1 site
  anywhere in the target or in its CAS source (in the script it is a bare
  sp.Symbol and is never substituted).  CONFIRMED — matches check F-6's finding
  on the premise.
No measured-constant comparison.  No evaluation of any authored map on any
  record object.  No E_C branch selected.  No route or address selected.
  No common cell formed.  No git.  CONFIRMED, all.
The three named fences (alpha_computed, proof_authorized, kappa_record_computed)
  are carried false on the face and again in the FLAG BLOCK.  CONFIRMED.
No register/tracker/road/plan/continuation file appears in the target's §0 or in
  any of its five declared sweeps.  CONFIRMED.
```

---

## §6 — THE CONSEQUENCE CLAIMS, TESTED AGAINST THE COMMISSIONING GROUND

```text
CLAIM: "the minimal completion set {W-1, W-2, W-3} STANDS unchanged"
  -> CONFIRMED.  §3.5 derives UNIQUENESS from each discharger being displayed
     SOLE at the closure's own bytes.  The target lands nothing, removes
     nothing, and proposes no alternative set.  MINIMAL_COMPLETION_SET_MOVED
     = false is true at bytes.

CLAIM: "the premise is not one of the commissioned absences A-1..A-7, and it
        does not create a twenty-second edge"
  -> CONFIRMED.  The absence inventory (§2 of the map, A-1..A-7) was read at its
     headers; the premise is none of them.  The 21 edges are (7 absences x 3
     live failure points); a premise that is not an absence adds no row and no
     column.  It sits INSIDE W-2, on the closure's own stratum — correct.

CLAIM: "W-2's description in the completion map is STALE AT BYTES ... not wrong,
        not refuted, simply un-updated"
  -> CONFIRMED, and the dating is right.  The map's W-2 display quotes CN-2;
     CN-2 quotes the first scoring pass's missing_carrier for R1 (baseline
     ee91d85c, tally CONFIRMED 0 / INERT 0 / NOT_RECOGNIZABLE_AS_STATED 15);
     both pre-date the 2026-08-15 ratification recorded at G-2.  The target
     issues NO correction against the map or its audit, and none is owed —
     a restraint this audit endorses.  (One citation slip: W-C5.)

CLAIM: "FP-2 is NOT discharged.  The boundary-closure still FAILS-AT F2."
  -> CONFIRMED.

OWED-3, the bearing-side display — SINGLED OUT FOR ENDORSEMENT.  The target
  declines to treat CN-2's "WOULD replace the diagonal-majorant accounting" as a
  proved substitution, names it a would-clause, and warns that a future W-2
  landing must not be mistaken for an automatic FP-2 discharge.  Checked against
  the WBC's own bytes: CN-2 does state the replacement conditionally.  This is
  the single most easily-skipped honesty in the artifact and the target does not
  skip it.
```

---

## §7 — CORRECTIONS ISSUED (seven, all DISPLAY grade)

**W-C1 — SW-2's displayed breadth understates its declared breadth.**
§0 declares SW-2's pattern as `"ratif|addend|entry|entered|ADOPT|
DECISION_OF_RECORD"`. Re-run here, that pattern matches roughly SEVENTY
artifacts in the primary root (ADOPTION_PROPOSAL families, DECISION_OF_RECORD
instruments, ENTRY_ROUND_2, PROSPECTIVE_ENTRY_AND_CASCADE_AUTHORIZATION,
STAGE8_SUMMED_S2_ENTRY_ADDENDUM, CONSTRUCTION_CONVENTIONS_C1_C5_RATIFICATION_
RECORD, and more). §2.2 names and opens exactly ONE of them. The display reads
as though the sweep's yield was a single instrument.
*Correction, not a refutation:* the CONCLUSION is independently re-derived and
CONFIRMED at §2.3 by the stronger digest sweep (AS-3), which is decisive because
G-2 requires any ratifying instrument to NAME BOTH DIGESTS. **Corrected reading:
SW-2 yielded ~70 filename-grade candidates, one opened; the NOT-FOUND finding
rests on the digest sweep, not on the filename sweep.**

**W-C2 — the R3 omission in the sentence that underwrites SUPPLIED.**
§3.1 states: *"The record's own re-scoring agrees at bytes: T9SR scores **R2
CONFIRMED** and **R4 CONFIRMED** on exactly these proof objects."* T9SR also
scores **R3 (presentation independence — i.e. P-2, the third of the four
obligations) NOT_RECOGNIZABLE_AS_STATED.** The target never mentions R3.
Re-derived at bytes, the non-confirmation lies wholly on R3's INSTANCE half:
T9SR's R3 row affirms *"The PROOF OBJECT EXISTS — P-2 (candidate §4) ... Check
§3: P-2 CONFIRMED (MC-6)"* and locates the failure at *"The DISPLAYED PAIR with
equal value DOES NOT EXIST — an instance display is an evaluation, barred by
mandate clause 3, and the actual-stock domain is empty until JB-2 lands."*
*So the substantive claim survives:* P-2 is proved and check-confirmed, and
§5's "presentation independence (P-2, proved)" is accurate. But citing the two
CONFIRMED rows while silent on the one NOT_RECOGNIZABLE row in the same block of
four is a selective display in precisely the sentence that carries W-2a's
SUPPLIED grade. **Corrected reading: T9SR scores R2 CONFIRMED, R4 CONFIRMED, and
R3 SPLIT — proof half supplied, instance half absent for the same
evaluation-barred reason as R1.** Displayed properly, R3 STRENGTHENS the target:
it is a second, independent instance of the very evaluation-halt §4.3 derives.

**W-C3 — CONF-1 / (JD-3c) is nowhere disclosed.**
The target contains **zero** occurrences of `CONF-1`, `JD-3c`, `F-5`, or
`confinement`. But the arrow CE-1 unfolds, and which §3.1 certifies as
"symbol-side in the exact sense required", is the premise's `rho_N` — and
premise §3.7 states at bytes that it is *"corestricted by CONF-1 (§3.10) to the
confinement subspace `T_B^conf,N`"*. CONF-1 is the premise's own AUTHORED LAW,
displayed there as *"NOT provable from the consumed sealed stock at authoring
time ... a genuine physical claim"*, with falsifier F-5 ARMED, and the check
records *"F-5 no actual-stage confinement display exists anywhere in sealed
stock. ARMED, UNTESTED; candidate correctly names it the most exposed clause."*
*Why this is a correction and not a kill:* (JD-3c)/CONF-1 is NOT among the
obstruction's four "must prove" obligations and is NOT demanded by R1's evidence
form, so W-2a's SUPPLIED grade is not overclaimed against W-2's stated demand.
*Why it is still owed:* §5's "**NOT OWED** (supplied, and to be scored as
supplied)" list is written as an instruction to a future scorer, and a scorer
reading it alone would not learn that the supplied rule's crossing arrow carries
an unproved authored confinement law with the premise's most exposed falsifier
armed against it. **Corrected reading: append to §5's NOT-OWED block — "carried
with it, and NOT proved: CONF-1 / (JD-3c), the premise's authored confinement
law, F-5 armed and untested."**

**W-C4 — "named" applied to unnamed fixtures in §7 and in the CAS comments.**
Stated in full at §3.3 above. **Corrected reading: W2-CAS-2 is a reversal
identity on a GENERIC oriented cell; W2-CAS-3a is (JD-3a) on a GENERIC chain;
the source comments naming `gamma_RL` and `c_RL` describe the record objects
these fixtures are ABOUT, not objects instantiated in them.** No record cell is
instantiated anywhere in the battery — verified by reading the source.

**W-C5 — two citation slips in §2.1 and §5.1.**
(i) §2.1 attributes the baseline tally to "T9SR §1"; at bytes it sits in T9SR's
§0 seal table (line 39). (ii) The baseline's tally is `CONFIRMED 0 / INERT 0 /
NOT_RECOGNIZABLE_AS_STATED 15`, which the target renders "15/15" — fair — but
T9SR's own re-scoring runs **R1-R14**, fourteen rows, not fifteen ("The tally
below therefore runs over R1-R14 exactly, per commission"). Nothing turns on
either. **Corrected reading: baseline ee91d85c, 15 rows, all NOT_RECOGNIZABLE,
cited at T9SR §0; T9SR's own re-scoring runs R1-R14.**

**W-C6 — the grade label is unglossed in the FLAG BLOCK.**
Stated at §3.1 above. **Corrected reading: in the FLAG BLOCK, render it
"SUPPLIED (grade: AUTHORED PREMISE — authored physics, consumable as premise,
never as derivation)".** The substance is right in all three places; only the
fast-scan rendering invites the wrong reading.

**W-C7 — §5.1's T9SR quotation is spliced across a table-cell boundary.**
§5.1 presents one continuous passage, ellipsed once, ending "…the carrier
NARROWS to exactly these two absences". At bytes (T9SR line 117) that text
spans TWO NON-ADJACENT CELLS of the R1 table row: the material before the
ellipsis is the row's Result cell, the material after it is the row's
missing-carrier cell, and the ellipsis silently swallows the intervening
verdict cell plus the sentence *"The rule-as-bytes half of the evidence form is
now supplied;"*. The target does render the verdict separately ("— verdict
NOT_RECOGNIZABLE_AS_STATED"), and CL-7 declares that cell separators are
retained for table quotations — but they are not retained here.
*Not a distortion:* the swallowed material is consistent with, and if anything
more favourable to, the target's own reading. **Corrected reading: mark the
passage as spliced from T9SR R1's Result and missing-carrier cells, with the
verdict cell between them.**

```text
NO CORRECTION ABOVE TOUCHES A VERDICT.  W-2a SUPPLIED, W-2b NOT SUPPLIED,
W-2c ABSENT-AND-NOT-CONSTRUCTIBLE, W2-PARTIAL, and every FLAG BLOCK boolean
survive all seven unchanged.
```

---

## §8 — VERDICT, PER CLAIM AND OVERALL

```text
PER CLAIM
  C-1  §0 seal ground, 11 files, 11/11 OK, digests as listed
                                                    CONFIRMED (12/12 here; 9/9
                                                    digests recomputed MATCH)
  C-2  §1   W-2's demand decomposes into rule / member / evaluation
                                                    CONFIRMED (forced by R1's
                                                    own row and CN-2's sentence)
  C-3  §2.1 the completion map never consumed the premise
                                                    CONFIRMED (5 hits, all
                                                    quotations; 0 digest hits in
                                                    map AND audit)
  C-4  §2.2 ratification carried of record; instrument UNLOCATED
                                                    CONFIRMED (re-derived by the
                                                    stronger digest sweep)
  C-5  §3.1 W-2a SUPPLIED, rule + four proved obligations
                                                    CONFIRMED-WITH-CORRECTIONS
                                                    (W-C2 R3 omission; W-C3
                                                    CONF-1 undisclosed)
  C-6  §3.2 W-2b NOT SUPPLIED; SK-7 part of the premise
                                                    CONFIRMED
  C-7  §3.3 W-2c ABSENT                             CONFIRMED
  C-8  §4.1/§4.2 CE-1 and CE-2 lawful constructions on the premise
                                                    CONFIRMED (byte-identical to
                                                    premise stock; nothing added)
  C-9  §4.3 H-1, H-2, H-3 at their source bytes     CONFIRMED (both byte spans
                                                    verified by direct read; the
                                                    H-1 quote IS contiguous and
                                                    complete)
  C-10 §4.4 the displayed evaluation is not constructible on the ratified rule
             today; the absence is STRUCTURAL       CONFIRMED
  C-11 §5   what remains owed: OWED-1 member, OWED-2 (2a/2b/2c), OWED-3 bearing
                                                    CONFIRMED-WITH-CORRECTIONS
                                                    (W-C3: the NOT-OWED block
                                                    omits CONF-1's caveat)
  C-12 §7   CAS 7/7, fresh venv, source appended verbatim
                                                    CONFIRMED BY RE-EXECUTION
                                                    (independent venv; identical
                                                    residual; diff-identical
                                                    appendix; digest MATCH)
                                                    W-C4 on two battery labels
  C-13 §6   {W-1,W-2,W-3} stands; no 22nd edge; FP-2 not discharged
                                                    CONFIRMED against §3.4/§3.5
  C-14      fences, numerics, lambda_JD, no-git, no barred-file reads
                                                    CONFIRMED CLEAN
  C-15      display discipline (sweep display, citations, grade label, a
             spliced table quotation)              CORRECTIONS W-C1, W-C5,
                                                    W-C6, W-C7

OVERALL = CONFIRMED-WITH-CORRECTIONS
```

**WHAT THE TARGET GOT RIGHT THAT IT WOULD HAVE BEEN EASY TO GET WRONG.** It
attempted the construction rather than declaring the absence and stopping; it
carried the attempt to its actual halt and then refused the one step that would
have manufactured a result (CL-5); it quoted the obstruction's own pass
criterion AGAINST its own construction; it declined to upgrade CN-2's
would-clause (OWED-3); it displayed the unlocated ratifying instrument instead of
asserting one; and it left the completion map uncorrected where no correction was
owed. The one thing it adds to the record beyond T9SR — that the missing
evaluation is structurally blocked rather than merely unperformed — is earned at
bytes and survives this audit.

**WHAT IS NOT ESTABLISHED, RESTATED SO IT CANNOT BE MISREAD.** The member is not
landed. W-2 is not landed. FP-2 is not discharged. The boundary-closure still
FAILS-AT F2. The minimal completion set is unchanged. Nothing here licenses any
downstream consumer to treat W-2 as supplied: **one of its three components is
supplied, and it is the component that does not, by CN-2's own bearing sentence,
replace F2's blocked side.**

---

## §9 — CHOICE LEDGER (commission O2SR, audit lane; every unforced choice)

```text
AL-1  CORPUS BOUNDARY = the twelve seal-verified files of §0 plus the five
      declared sweeps AS-1..AS-5, cutoff 2026-08-15T16:56:26Z, primary root
      only.  PREMISE(named).  A wider sweep could overturn this audit only by
      exhibiting a located JD-specific ratifying instrument naming both digests
      (which would upgrade §2.3's standing and move no verdict) or a displayed
      (JD-3) evaluation on a named oriented record cell somewhere unswept.

AL-2  RE-EXECUTION OF THE CAS RATHER THAN INSPECTION OF ITS REPORTED OUTPUT:
      FORCED by DEFAULT-REFUTE.  A fresh venv was built by this audit and the
      script was run from an extract taken out of the ARTIFACT's bytes, not from
      the build's file, so that the appendix — not the build's disk state — is
      what was tested.

AL-3  THE DIGEST SWEEP (AS-3) SUBSTITUTED FOR THE TARGET'S FILENAME SWEEP as the
      ground of the UNLOCATED finding: unforced, CHOSEN.  Reason: G-2 states the
      ratification named both digests, so the digest string is a necessary
      feature of any ratifying instrument and sweeping on it is decisive where a
      filename sweep is merely suggestive.

AL-4  BARRED-CLASS FILES TESTED BY FILENAME CLASSIFICATION ONLY, NEVER OPENED:
      FORCED by the commission's absolute bar.  The register/tracker/road/plan/
      continuation class was checked for the digest string by classified grep so
      that the NOT-FOUND finding is not silently scoped around them; zero hits;
      no such file was read.

AL-5  CORRECTIONS GRADED "DISPLAY" RATHER THAN "SUBSTANCE": FORCED in each case
      by the byte check — for W-C2, because T9SR's R3 row affirms the proof
      object; for W-C3, because (JD-3c) is not among the four obligations W-2
      demands; for W-C4, because no record cell is instantiated anywhere in the
      battery.  Each is reasoned to at its own entry rather than assigned.

AL-6  THE CAS WEIGHT CALIBRATION (§5.3) ISSUED AS A NOTE, NOT A CORRECTION:
      unforced, CHOSEN.  The target does not overclaim the battery; the note
      exists so a later consumer does not weigh trivial identities as evidence.

AL-7  QUOTATION DISCIPLINE: quotations are byte-faithful modulo line-wrap; table
      rows are reproduced with their cell separators retained or with the cell
      boundary stated in prose.  PREMISE(named).

ZERO entries in class OPEN.
```

---

## §10 — TOY_SEPARATION

```text
TOY_SEPARATION = clean.

This audit consumed NO toy object as ground.  Every finding above is one of:
a byte-read of a seal-verified artifact; a recomputed sha256; a declared
filename or content sweep; a direct byte-offset read (`dd`) against a cited
span; or an exact symbolic re-execution on GENERIC PLACEHOLDER fixtures that
are labelled as such and never promoted to record content.

THE ACTUAL SURFACE WAS HELD, NOT SUBSTITUTED.  The named oriented cell at issue
is the record's own `gamma_RL / -gamma_RL` at reciprocal stage RL, and this
audit reports that surface as `STOPPED` at Joints A--D — read out of the
obstruction's own §5.2 row rather than inferred.  No convenient cell was
invented, no convenient symbol datum was invented, and the one place where the
target's own display drifts toward calling a fixture "named" is issued as a
correction (W-C4) rather than passed over.

THE CAS FIXTURES RE-RUN HERE (a generic cubic path, generic polynomial 1-form
coefficients, symbolic chain coefficients, an uninterpreted symbol datum) are
ALGEBRA-CHECKING FIXTURES.  They stand for nothing, no record object is
instantiated in any of them, no output of any of them is compared to anything,
and none is an evaluation in W-2c's sense.  W2-CAS-5a exists precisely to
display that the demanded evaluation CANNOT be modelled, its argument being
unformed of record.  Their evidential weight is calibrated explicitly at §5.3
so that trivial identities are not later read as substantive confirmation.

An audit that re-ran a battery and reported "7/7 PASS" without saying which of
the seven are trivial would itself be a toy.  §5.3 is that separation.
```

## §11 — FLAG BLOCK

```text
COMMISSION = O2SR (W2-AUDIT; adversarial audit of
  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md at bytes, DEFAULT-REFUTE, testimony
  zero weight).
DATE = 2026-08-15.  SWEEP_CUTOFF = 2026-08-15T16:56:26Z.

STEP 0 = target PRESENT; `shasum -a 256 -c` run FROM THE ARTIFACT'S OWN
  DIRECTORY -> OK.  Own output path + sidecar probed ABSENT before any byte.

SEALS = 12/12 OK, re-verified this session from each artifact's own directory;
  zero mismatches; none unverifiable.  9/9 of the target's independently-listed
  digests RECOMPUTED and MATCHED character-by-character.

OVERALL VERDICT = CONFIRMED-WITH-CORRECTIONS.
  Per-claim board at §8 (C-1..C-15).  Eleven CONFIRMED, two
  CONFIRMED-WITH-CORRECTIONS (C-5, C-11), one CONFIRMED-BY-RE-EXECUTION (C-12),
  one corrections-only row (C-15).  ZERO REFUTED.

HUNTS (all four run, hardest first; ZERO KILLS):
  THE GRADE            : NO KILL.  The premise is consumed as AUTHORED PREMISE
                         in every step; the target says so in three places; no
                         construction is presented as derivation from sealed
                         stock.  One unglossed label -> W-C6.
  THE SK-7 BAR         : NO KILL.  Member NOT landed anywhere; W-2b graded NOT
                         SUPPLIED; Joints A/B/C/HJ7/E/F named and carried; the
                         one ellipsis in the banner quotation drops material
                         ADVERSE to the candidate, not favourable.
  NAMED-CELL/EVALUATION: NO KILL.  W-2c graded ABSENT; CE-2 confined to the form
                         level with no record cell identified; the obstruction's
                         own pass criterion quoted AGAINST the construction.
                         Two battery labels misuse "named" -> W-C4.
  SMUGGLED AUTHORSHIP  : NO KILL.  The §3.1 display block is BYTE-IDENTICAL to
                         premise §3.8 (all eleven lines); the two authored
                         clauses are correctly separated from the induced (JD-2)
                         composites; CE-1 is pure substitution of T2-7/T2-2 into
                         T2-8 with no new symbol, datum, map, or law; CL-5
                         refuses the one step that would have been authorship.
  HIDDEN NUMERICS      : NONE FOUND.  No number carried as a value of any
                         program quantity anywhere in the target.

CORRECTIONS = SEVEN, all DISPLAY grade, none moving any verdict:
  W-C1 SW-2's displayed breadth understates its declared breadth (~70 filename
       matches, one opened); the NOT-FOUND conclusion is re-derived and
       CONFIRMED here by the decisive digest sweep instead.
  W-C2 §3.1 cites T9SR's R2 and R4 CONFIRMED and is silent on R3
       NOT_RECOGNIZABLE_AS_STATED; at bytes R3 is SPLIT (P-2 proof object
       CONFIRMED at check MC-6; instance half absent, evaluation-barred).
       Displayed properly it STRENGTHENS the target.
  W-C3 CONF-1 / (JD-3c) / F-5 nowhere disclosed, though premise §3.7
       corestricts rho_N by CONF-1, an authored law NOT provable from sealed
       stock with its falsifier ARMED and UNTESTED.  Outside W-2's four demanded
       obligations, hence a correction; owed in §5's NOT-OWED block.
  W-C4 "named" applied to unnamed generic fixtures in two §7 battery labels and
       two CAS source comments.
  W-C5 two citation slips (baseline tally located at T9SR §0 not §1; T9SR's own
       re-scoring runs R1-R14, not 15).
  W-C6 the grade label AUTHORED-PREMISE-DERIVED is unglossed in the FLAG BLOCK.
  W-C7 §5.1's T9SR quotation is spliced across a table-cell boundary without
       the separators CL-7 declares.

CAS = RE-EXECUTED INDEPENDENTLY.  Script digest ac359bc4… MATCH; §7.1's appended
  source DIFF-IDENTICAL to the script on disk; fresh venv built by this audit,
  sympy 1.14.0; 7/7 identities hold; the displayed residual reproduces exactly
  as Integral(delta_sect(s), (s, 0, 1)).  Weight calibration at §5.3.

CARRIED THROUGH, UNCHANGED BY THIS AUDIT:
  MEMBER_LANDED = false (SK-7).  W2_SUPPLIED = false.  MEMBER_BOUND = false.
  COMMON_CELL_FORMED = false.  R9JII_RUN_STATE = NOT_RUN.
  MINIMAL_COMPLETION_SET_MOVED = false ({W-1, W-2, W-3} stands, UNIQUE, each
    discharger SOLE at the closure's own bytes).
  FP2_DISCHARGED = false.  The boundary-closure still FAILS-AT F2.
  ABSENCE_CLOSED = none.  GATE_MOVED = none.  WITNESS_RETIRED = none.
  JOINT_DISCHARGED = none.  E_C_BRANCH_SELECTED = none.
  ROUTE_OR_ADDRESS_SELECTED = none.
  lambda_JD = EXPLICIT, SYMBOLIC, UNASSIGNED (no assignment, normalization, or
    silent-to-1 site in the target or in its CAS source).
  RATIFICATION_STANDING = CARRIED OF RECORD by T9SR (b72d7bfd) and its audit
    (c244d0d1); the ratifying INSTRUMENT itself UNLOCATED at this cutoff,
    re-derived here by digest sweep.  Displayed, not upgraded.
  COMPLETION_MAP_STATUS = its W-2 display is STALE AT BYTES; NO CORRECTION
    ISSUED against the map or its audit by the target or by this audit; none is
    owed.  The map's derived results are untouched.

FENCES = alpha_computed false ; proof_authorized false ; kappa_record_computed
  false — held from the first byte of this audit.  No value, no number as a
  value of any program quantity, no measured-constant comparison, no evaluation
  of any authored map on any record object, no authored physics.  No git.
  NO register/tracker/road/plan/continuation file read — the class was excluded
  at pattern level and tested by filename classification only.
  "Q-..." tokens EXPECTED-UNLOCATABLE, carried as opaque labels, never chased.

OUTPUT = one artifact plus one seal sidecar; output path probed ABSENT before
  any authored byte; written incrementally by file-append.
```

---

*END OF ARTIFACT — STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001*
