# Stage 8 Bridge Item 4 Six-Object Depth Report v001

CODEX 1 artifact for Paste 167, Item 4.

Status: DEPTH REPORT ONLY. No bridge object is derived, adopted, repaired, or
retired here.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No computation or evaluation of alpha, `kappa_record`, `kappa_Thomson`, a
coupling, a radius, a scale, a root, an eigenvalue, a beta function, `E_R`,
`T_R`, or any absolute interval was performed. No measured constant comparison
was performed. `a32_holdout/custodian_private/` was not opened.

## Scope

Search roots:

```text
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha_supervision
```

Exclusions:

```text
/a32_holdout/custodian_private/
Codex 2 live CTP_PHYS_INPUT_PACKAGE artifacts
Einstein live relay artifacts beyond the Q-64 register row
```

Search/read set:

```text
QUESTIONS_SETTLED_REGISTER_V001.md rows Q-58 through Q-64
BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md
STAGE8_C_R_SELECTOR_NONSELECTION_DIAGNOSIS_V001.md
STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md
STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md
BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md
STAGE8_C_R_FALSIFIER_DESIGN_AND_FLOOR_SOURCE_VALUE_RECONCILIATION_V001.md
```

## Overall Finding

No one of the six bridge chains reaches derived ground within the scoped
corpus. All six objects have named partial material, but their load-bearing
leaves terminate in either TYPE-U unbuilt objects or one TYPE-C adopted
marginality condition.

```text
bridge_chain_reaches_derived_ground = false | TYPE-S
roots: /Users/bgm/MB Work/alpha-program-archive/workspace;
       /Users/bgm/MB Work/alpha_supervision
exclusions: /a32_holdout/custodian_private/; live Codex 2 CTP package artifacts;
            Einstein live relay artifacts beyond Q-64 register row
query: bridge object names from Q-59; exact status flags from
       BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:189-203;
       C_R selector status flags from
       STAGE8_C_R_SELECTOR_NONSELECTION_DIAGNOSIS_V001.md:54-59 and 145-173
finding: no scoped artifact derives any of S1-S6; the Q-64 row's report that
         the bridge boundary is unbuilt is not contradicted by this pass
```

This is not a physical refutation. It is a depth report over a scoped bridge
chain.

## Dependency Spine

Item 1 established the dependency order:

```text
S1 complete Lorentzian CTP action / boundary / time-flow data
  -> S2 constant Hamilton-Jacobi record energy on stationary cell
  -> S4 reference-subtraction / no-spectator theorem
  -> S3 branch-energy difference equals gravitating closure energy
  -> S5 derived marginal first durable-public closure
  -> S6 one isolated stable positive interval solution
```

The edge `S3 -> S5` is inferred from the bridge gate's closure order and from
the adopted marginal public-closure rule. The other edges are read directly
from the bridge-gate text.

## Per-Object Depth

### S1. Complete Lorentzian CTP action / boundary / time-flow data

Depth below S1: 1.

Immediate leaves:

```text
complete source-record-gravity Lorentzian CTP action = false | TYPE-U
state/preparation for stationary record-cell problem = false | TYPE-U
global/subregion boundary terms = false | TYPE-U
time-flow vector = false | TYPE-U
```

Ground check:

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:16-31` says the HJ relation
is conditional on all four. `...:169-175` requires one target-independent
complete action fixing boundary terms and time-flow. `...:191` records
`complete_CTP_action_and_boundary_data_derived = false`.

Depth verdict:

```text
S1_terminates_in_derived_ground = false | TYPE-U
would-build: complete target-independent Lorentzian CTP action package with
             boundary data, state/preparation, and time-flow vector
```

### S2. Constant Hamilton-Jacobi record energy on stationary cell

Depth below S2: 2.

Dependency chain:

```text
S2 constant HJ energy certificate
  -> S1 complete Lorentzian CTP action / boundary / time-flow data
     -> S1 leaves
```

Additional leaves:

```text
stationary solution/saddle class = false | TYPE-U
HJ differentiability and conjugacy certificate = false | TYPE-U
constancy identity over stationary class = false | TYPE-U
```

Ground check:

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:175-177` requires deriving
the CTP HJ energy and proving it constant on the first durable-record saddle.
`...:192` records `record_energy_constant_on_stationary_cell_derived = false`.
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:314-380` states that the required HJ
conjugate energy does not exist yet and must be specified and derived.

Depth verdict:

```text
S2_terminates_in_derived_ground = false | TYPE-U
would-build: a certificate, consuming S1, proving the HJ record energy is
             constant on the stationary record-cell solution class
```

### S4. Reference-subtraction / no-spectator theorem

Depth below S4: 2.

Dependency chain:

```text
S4 reference-subtraction / no-spectator theorem
  -> S1 complete Lorentzian CTP action / boundary / time-flow data
     -> S1 leaves
```

Additional leaves:

```text
baseline/reference subtraction rule = false | TYPE-U
spectator/vacuum/binding/edge/environment exclusion theorem = false | TYPE-U
proof that compactness and record-action energy use the same support = false | TYPE-U
```

Ground check:

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:155-167` states the
reference-energy obstruction: the record phase is insensitive to adding the
same constant Hamiltonian to both CTP branches, while compactness need not be
insensitive to physical spectator energy. `...:180-181` requires deriving the
baseline/reference subtraction and excluding spectator energy. `...:194`
records `reference_subtraction_and_no_spectator_theorem_derived = false`.

Depth verdict:

```text
S4_terminates_in_derived_ground = false | TYPE-U
would-build: fixed reference subtraction plus theorem excluding spectator,
             vacuum, binding, edge, and environment energy unless it also
             enters the record action difference
```

### S3. Branch-energy difference equals gravitating closure energy

Depth below S3: 3.

Dependency chain:

```text
S3 branch-energy equals gravitating closure energy
  -> S2 constant HJ energy certificate
     -> S1 complete Lorentzian CTP action / boundary / time-flow data
        -> S1 leaves
  -> S4 reference-subtraction / no-spectator theorem
     -> S1 complete Lorentzian CTP action / boundary / time-flow data
        -> S1 leaves
```

Additional leaves:

```text
gravitating closure energy definition in the complete action = false | TYPE-U
proof branch-energy difference is that closure energy = false | TYPE-U
```

Ground check:

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:33-48` requires the CTP
branch-energy difference to equal the complete gravitating cell energy after
one fixed reference subtraction. `...:178-179` requires proving equality to
the gravitating energy entering the closure condition. `...:193` records
`record_energy_equals_total_gravitating_energy_derived = false`.

Depth verdict:

```text
S3_terminates_in_derived_ground = false | TYPE-U
would-build: proof that the CTP branch-energy difference is the same energy
             entering the gravitational recoverability/closure condition
```

### S5. Derived marginal first durable-public closure

Depth below S5: 4.

Dependency chain:

```text
S5 derived marginal first durable-public closure
  -> S3 branch-energy equals gravitating closure energy
     -> S2 and S4
        -> S1
           -> S1 leaves
  -> independent first durable/public closure theorem
```

Current marginality status:

```text
marginal_closure_condition_derived_rather_than_assumed = false | TYPE-C
constraint: C_R = 1 is currently an adopted Level-1 marginal public-closure rule
release: target-independent complete source-record-gravity action derives
         first durable/public closure at the marginal boundary without
         receiving marginality as an input
```

Ground check:

`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:49-67` supplies the adopted
Level-1 marginal public-closure rule. `BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:181`
requires deriving, rather than assuming, the marginal closure condition. Item 0
resolved the alias between the compact `C_R` marginal selector label and the
expanded marginal closure label.

Depth verdict:

```text
S5_terminates_in_derived_ground = false | TYPE-C
constraint: current marginality is an adopted Level-1 rule, not a derived
            first durable/public closure theorem
release: a target-independent complete source-record-gravity action derives
         first durable/public closure at the marginal boundary without
         marginality as an input
```

### S6. One isolated stable positive interval solution

Depth below S6: 5.

Dependency chain:

```text
S6 isolated stable positive interval solution
  -> S5 derived marginal first durable-public closure
     -> S3
        -> S2 and S4
           -> S1
              -> S1 leaves
  -> Lorentz-scalar scale-selection equation
```

Additional leaves:

```text
parameter-free Lorentz-scalar scale-selection equation = false | TYPE-U
single isolated positive stable solution proof = false | TYPE-U
absolute record interval derived in declared branch = false | TYPE-U
```

Ground check:

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:181-182` requires yielding
one isolated stable positive solution. `BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:66-79`
states that the absolute scale closes only if a complete parameter-free parent
supplies a Lorentz-scalar equation with one isolated positive stable solution
before any endpoint. `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:144-170`
keeps `absolute_record_interval_derived_in_declared_branch = false`.

Depth verdict:

```text
S6_terminates_in_derived_ground = false | TYPE-U
would-build: a target-independent Lorentz-scalar scale-selection equation with
             one isolated positive stable solution, downstream of S1-S5
```

## Cross-Checks Against Existing Partial Material

The scoped corpus does contain partial material that must not be counted as
derived ground:

```text
conditional_absolute_record_interval_formula_derived = true
```

from `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:144-170`, but the same
block keeps:

```text
absolute_record_interval_derived_in_declared_branch = false
```

The bridge gate also names ordinary-branch and stationarity assumptions, but
`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:189-203` keeps the bridge
flags false.

Therefore these partial results are not counted as derived bridge ground.

## Final Depth Verdict

```text
six_bridge_objects_complete = false | TYPE-U
complete_objects_count = 0
partial_objects_count = 6
absent_objects_count = 0
chains_reaching_derived_ground_count = 0
first_load_bearing_object = S1 complete Lorentzian CTP action / boundary / time-flow data
second_load_bearing_object = S2 constant Hamilton-Jacobi record energy on stationary cell
```

The chain is now structured enough to test, but not to compute.

