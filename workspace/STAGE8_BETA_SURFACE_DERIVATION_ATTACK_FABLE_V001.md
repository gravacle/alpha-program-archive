CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00006558   (patched in place at seal time; field width fixed, offsets stable)
VERDICT_BEARING_SET = exactly the 6 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member rehashed at its path, two passes, STRICT==STABLE. Members 05 and 06 of
  this table were cross-checked against the hashes recorded in member 02's own closure table and
  MATCH (custody chain confirmed, not asserted).
LANE = FABLE (blind constructor FABLE-BUILD-1107)
ROLE_THIS_RELAY = ATTACK — the never-run beta-gap surface derivation (PLAN:LINE-4). A DIRECT
  TRAVERSAL of the connection-only record surface, adjudicating the provenance of the
  internal/external metric conversion of a record cell.
BLIND HELD. EVALUATES NOTHING. Every scale symbolic. Alpha, scale, metric, beta are SUBJECTS by
  type. No measured value, no constant, no imported length is read, used, or compared. Every
  numeric string in this artifact is a sealed-text quotation or a file digest, never a computed
  physical quantity.
CARRIED GIVENS (registrar hand-off, taken as GIVEN, not re-established): (i) shape does not
  determine scale (Q-12); (ii) the record is connection-only, operator-valued, no metric argument
  (1101/1102, both-lane); (iii) the absolute-normalization obstruction survives without the
  logarithm on the response/matching channel (Q-1007); (iv) the Q-11 hazard: the words
  "internal"/"external" are not in the cited surface text; (v) the blindness principle:
  traditional formalisms have never traversed the record surface and are by construction blind to
  surface-native content; only a direct surface traversal adjudicates.
RECORD-NATIVE FENCE = LIVE. No continuum machinery, no smooth structure, no imported
  metric/length/normalization, no KK/radion machinery used to BUILD anything, no variational or
  additive notation anywhere in what is constructed here.
REGISTER = NOT READ (barred). No tracker, plan, or road file read. No broad archive grep run;
  every read was a named artifact or a file the named artifacts explicitly cite.
NO_NUMERIC_PATH = nothing is computed; no sampling, no numeric library, no numeric literal enters
  any constructed object. File digests are custody data, not physical values.
ALL_RESULTS = CLAIMED until the two independent adversarial checks.
LIVE_MEMBERS = NONE.
```

| # | Closed member (all under `/Users/bgm/MB Work/alpha-program-archive/workspace/`) | SHA-256 | Role |
|---:|---|---|---|
| 01 | `SPEC_1107_BETA_SURFACE_DERIVATION_ATTACK_V001.md` | `fbd5d4810e7c548eacce84883022fd66a8535980efb6ce0dc8513727064b0a8c` | assignment |
| 02 | `STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` | `1d11f15040f8b85b7e081fccfeddb995c41941c55464d759a2fa91a8feffc775` | the connection-only ground; the one-argument import-block finding |
| 03 | `BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md` | `8f9bdd93cd32fbf116e9c9f645df56e3c1608c83883238f524be94ec8d999b37` | the surface-native scale orbit |
| 04 | `BID_BOUNDARY_METRIC_TRANSPORT_DERIVATION_V001.md` | `52187f8174b203d3cf2acb641d800f45ae733331cc0f3d56658898ed7daec244` | the metric-nearest object; derived-or-imported determination required |
| 05 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | THE RATIFIED CONNECTION-ONLY SURFACE OBJECT (cited by name in member 02, hash-matched) |
| 06 | `STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` | `04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d` | the junction inventory of the record cell (cited by name in member 02, hash-matched) |

```text
PINNED QUOTES (file:line, verified at path in this session; quoted exactly):
  member 02 :168-170  "the sector-CONTROLLED transition and the exact finite influence functional,
                       indexed by CONNECTION / HOLONOMY HISTORIES ALONE.
                       ** THERE IS NO METRIC ARGUMENT ANYWHERE IN IT. **"
  member 03 :53-56    "The current record kinematics do not identify one SI value of `T_R`."
  member 03 :87-88    "first_opening_data_break_scale_orbit = false /
                       absolute_SI_record_duration_derived = false"
  member 04 :49-51    "Boundary-metric compatibility follows from standard spin covariance once the
                       endpoint normal is required to be the Lorentz transport of the root normal.
                       It is not an independent numerical assumption."
  member 05 :161-165  "The symbols `a_+,a_-` in this artifact denote **connection/holonomy
                       histories**."
  member 05 :496-497  "It does not supply the scalar state, nontrivial effect port, complete
                       domains/contacts, metric dependence, or common-origin trace"
  member 06 :78-81    "SO THE INTERNAL NORMALIZATION IS FULLY PINNED AND DIMENSIONLESS; THE ENTIRE
                       CONVERSION FREEDOM SITS AT THE JUNCTION, NOT INSIDE THE INTERNAL GEOMETRY."
  member 06 :102      "`causal_diamond_shape_derived = true` — **the shape is derived; the
                       conversion is not.**"
  member 06 :32-33    "EVERY JUNCTION THAT IS DERIVED IS beta-INVARIANT, AND EVERY JUNCTION WHERE
                       beta COULD APPEAR IS ADOPTED, GAP, OR UNBUILT. NO JUNCTION IS SIMULTANEOUSLY
                       DERIVED AND beta-SENSITIVE."
  member 06 :86-87    "'Connections on a fixed line bundle form an affine space' — no fiber metric,
                       and a radius is a metric datum."

ESTABLISHED IN THIS RELAY: the construction attempt of section 2 and its stop point; the
  surface-native statement of the obstruction in section 3; the derived-vs-imported determination
  on member 04 in section 4; the decision of section 5 (framing laid on top, with evidence); the
  splits and audits of sections 6-7.
AUTHORED IN THIS RELAY: nothing physical. No object, no junction, no conversion, no scale is
  authored. The only new content is adjudication of provenance.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO OUTPUT INSPECTION. NO MEASURED-VALUE
COMPARISON. NO IMPORTED LENGTH. NO MEMBER BOUND. NO FIXED POINT. NO END TEST. NO FREEZE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — THE BETA-GAP SURFACE-DERIVATION ATTACK: THE INTERNAL/EXTERNAL CONVERSION, TRAVERSED ON THE CONNECTION-ONLY SURFACE
## RELAY 1107 — `[PLAN:LINE-4 / ATTACK]` — FABLE LANE (BLIND CONSTRUCTOR) — [CLAIMED]

Date: 2026-08-12

**WHAT WAS ASKED.** The absolute-scale constraint (Q-1007) is stated as riding on "the
internal/external metric conversion of a record cell." The record surface is connection-only.
Is that conversion STRUCTURE derived from the connection-only surface data, or a declared rail
import? Adjudicate by direct surface traversal only; a "cannot build it" carrying any residue of
traditional formalism is a false rail and does not count.

**WHAT I FOUND, IN ONE LINE:** the conversion STRUCTURE is a RAIL — the surface supplies both feet
of the junction and every derived junction between them, all dimensionless and all invariant under
the very rescalings the conversion would have to fix; what the surface natively carries at that
locus is a free presentation orbit, and the "metric conversion" is a rail-side chart laid on top
of that orbit, one of whose two moduli (the fiber radius) is a modulus of the import itself.

---

## 0. LEAD

The traversal reached every derived object at the junction and stopped at exactly one step: no
junction of the record cell is simultaneously DERIVED and SCALE-SENSITIVE. That stop is not a
failure to find; it is a positive, surface-native invariance, constructed below from the surface's
own objects (unit-modulus holonomy characters, the exact refinement law, the affine connection
space, the scale-orbit identity). An inventory all of whose derived members are fixed points of
the scale action cannot produce an object whose job is to select a point of the orbit. The missing
ingredient is exhibited by name (section 2.4), and the traversal itself produces the obstruction
rather than merely failing to produce the conversion (section 3) — which is what rules out the
blind-tool false negative and licenses RAIL-DECLARED rather than UNDETERMINED.

Numeric strings below (`pi/sqrt(2)`, `pi hbar`, `m_* T_R = pi`, `N=1,2`, `k = +1`, `C_R = 1`,
thirteen, twelve) are sealed-text quotations from the members, never computations.

---

## 1. THE CONNECTION-ONLY SURFACE DATA, NAMED (part 1 of the attack)

Walked directly, object by object, at the member paths — not summarized from secondary prose.

From **member 05** (the ratified surface object, `TYPE-P | premises: DoR-008, DoR-009`):

```text
S1  the one-cell record basis (|r>, |p_Q>, |e_Q>) — the ready, pointer/written, and e_Q
    configurations of a record cell                                    (member 05 :121-126)
S2  the exact one-cell write S (the ratified swap-with-sign operator)  (member 05 :124-126)
S3  connection/holonomy histories a_+, a_- — the load-bearing symbol ruling: these ARE
    connection/holonomy histories, not endpoint labels                 (member 05 :161-165)
S4  the faithful holonomy characters z_j^(n) = chi_n(h_j[a_j]), unit modulus, n in {+1,-1}
                                                                       (member 05 :14-21, 128-133)
S5  the orthogonal neutral/charged source-sector projectors P_0, P_ch  (member 05 :142-144)
S6  the ratified sector-CONTROLLED transition
        U_N[a] = P_0 (tensor) I + P_ch (tensor) W_N[a]                 (member 05 :146-150)
S7  the exact finite influence functional
        F_N[a_+,a_-] = P_0 + Z_N[a_+,a_-] P_ch,
        Z_N = product_j conjugate(z_(-,j)) z_(+,j)                     (member 05 :24-41, 199-203)
S8  CTP Hermiticity F_N[a_+,a_-]^dagger = F_N[a_-,a_+]                 (member 05 :434)
S9  gauge covariance and common-gauge invariance of Z_N, F_N           (member 05 :321-354)
S10 the refinement/zero-extension law (C7): appended cells contribute exact unit factors;
        F_M[(a_+,0),(a_-,0)] = F_N[a_+,a_-]                            (member 05 :387-398)
S11 the one-cell restriction falsifier (exact, standing)               (member 05 :408-427)
```

From **member 02** (the ratified ground): the two-branch history algebra (member 02 :199-209,
sealing its member 06); and the decisive ratified sentence — the record's own interacting object
is "indexed by CONNECTION / HOLONOMY HISTORIES ALONE. THERE IS NO METRIC ARGUMENT ANYWHERE IN IT"
(member 02 :168-170).

From **member 03** (the identifiability gate):

```text
S12 the dimensionless first-opening interval tau_R = pi/sqrt(2) and the dimensionless
    generator B, with the surface identity exp(-i H_R' T_R'/hbar) = exp(-i tau_R B)
                                                                       (member 03 :14-37)
S13 the orbit-invariant surface inventory: first-opening probabilities, orthogonality,
    endpoint transfer, incidence normalization, projective path lengths (member 03 :39-41)
S14 the causal diamond's DIMENSIONLESS shape coefficients (derived; the dimensionful radius
    and volumes ride the orbit)                                        (member 03 :44-50, 85)
```

From **member 06** (the junction inventory, its degeneracy test executed twice by independent
agents, TYPE-R):

```text
S15 the primitive projective record degree — a ray in the primitive complex two-dimensional
    carrier, normalized lift (carrier uniqueness NOT derived; sealed)  (member 06 :74-77)
S16 the derived counting metric H_record(X) = ell^2(X) (Hilbertization) — the internal
    normalization "FULLY PINNED AND DIMENSIONLESS"                     (member 06 :78-81)
S17 the record bundle: connection forced, curvature global, "Connections on a fixed line
    bundle form an affine space" — no fiber metric; a radius is a metric datum
                                                                       (member 06 :85-87)
S18 the derived dimensionless junction locks: |Delta S_record| = pi hbar (an ACTION, its
    non-identifications sealed); m_* T_R = pi (the one cross-geometry lock); the
    balanced-geodesic identities; the onset bound                      (member 06 :82-88, 110-115)
S19 the thirteen sealed interface quantities of the one cell, with the executed split: every
    DERIVED junction scale-orbit/beta-invariant; every scale-sensitive junction ADOPTED
    (fiber radius in K_KK), UNBUILT (the D_BR spectrum), or GAP (the skeleton-to-cell
    embedding)                                                         (member 06 :30-33, 108-120)
```

From **member 04** (the metric-nearest object): oriented edges carrying transport
(`Lambda_e`, spin lift `S_e`, unit `U(1)` phase); the endpoint-normal transport law
`n_p = Lambda_e n_r` (typed: the endpoint normal is not independent data); the hypersurface form
`h_n(psi,phi) = psi^dagger gamma^0 slash(n) phi`; and the derived isometry of edge transport on
`h_n` (member 04 :14-52).

That is the complete surface footing used. Nothing else adjudicates.

---

## 2. THE CONSTRUCTION ATTEMPT — HOW FAR THE CONVERSION BUILDS FROM SURFACE DATA ALONE (part 2)

The target object, as the program types it: a conversion between the internal geometry of a
record cell and its external metric geometry — the junction slot whose selection the absolute
scale rides on.

### 2.1 The internal foot — REACHED, dimensionless

From S1-S7, S15-S17: the full interacting functional `F_N` constructs exactly at every finite N;
the internal metric that is DERIVED is the counting metric `ell^2(X)` (S16); the phase/action
content is the sealed action quantum `|Delta S_record| = pi hbar` (S18), an action whose
non-identifications (not a duration, not a volume) are sealed. Everything on this foot is
dimensionless or an action in `hbar` units. Member 06 :78-81 states the traversal's own result in
sealed text: the internal normalization is fully pinned and dimensionless; the entire conversion
freedom sits at the junction, not inside the internal geometry. **The internal foot supplies no
conversion freedom to fix and no dimensionful datum to fix it with.**

### 2.2 The external foot — REACHED as SHAPE only

From S12-S14: `tau_R = pi/sqrt(2)` and the diamond's dimensionless shape coefficients are derived.
The dimensionful presentation (radius, three-volume, four-volume) transforms along the orbit
`T_R -> lambda T_R` and is therefore a coordinate on the presentation space, not surface content.
Member 06 :102, sealed: "the shape is derived; the conversion is not." **The external foot exists
on the surface only as dimensionless shape.**

### 2.3 The junctions — REACHED, all of them, all invariant

Every junction between the two feet that is derived is one of S18's locks: action-times-phase
(`|Delta S_record| = pi hbar`), mass-times-interval (`m_* T_R = pi` — contains no radius,
trivially invariant), the balanced-geodesic identities, the onset bound. Each pairs its two feet
into a dimensionless, scale-orbit-invariant object. Member 06 executed the degeneracy test twice,
independently (attacker and verifier, TYPE-R): NO junction is simultaneously derived and
scale-sensitive (member 06 :30-33). The construction from surface data alone therefore reaches
the COMPLETE two-sided dimensionless junction inventory.

### 2.4 The stop point — the conversion itself, with every surface route walked

The conversion requires one junction that is both (i) derived from surface data and (ii) NOT
invariant under the presentation rescalings — an orbit-breaking dimensionful equation. Every
surface route to such a junction was walked:

```text
R1  THROUGH THE INTERACTING OBJECT.  F_N is a functional of unit-modulus characters and sector
    projectors only (S4, S7). Its interface explicitly does NOT supply the "metric dependence"
    field of the dynamics port (member 05 :493-497), and even producing a SCALAR from it requires
    a separately named state/effect realization (member 05 :47-50, :554-568 — the typed
    scalarization boundary). Unit-modulus products are scale-free. NOTHING DIMENSIONFUL EXITS.
R2  THROUGH REFINEMENT.  The C7 law (S10) composes appended cells as exact unit factors; the
    surface's own refinement tower never consults a cell size. NO SCALE LADDER EXISTS IN THE
    COMPOSITION LAW.
R3  THROUGH GAUGE STRUCTURE.  Common-gauge invariance (S9) cancels endpoint characters relatively;
    nothing new is exposed.
R4  THROUGH THE CONNECTION SPACE.  Connections on the fixed line bundle form an AFFINE space with
    no fiber metric, and a radius is a metric datum (S17, sealed). THE CONNECTION SPACE CARRIES NO
    DISTINGUISHED NORM.
R5  THROUGH BOUNDARY TRANSPORT.  h_n (member 04) is a fiber bilinear form with no duration or
    cell-size dependence anywhere in it; even granted whole, it is constant along the orbit and
    supplies no normalization (section 4). And an invariant form of a transport group is fixed
    only up to a positive scale, so no group-invariance route yields a UNIT.
```

```text
CONSTRUCTION_REACHED: the full interacting functional F_N with its exact identities; the internal
dimensionless geometry (counting metric, action quantum); the external dimensionless shape (tau_R,
diamond shape coefficients); ALL derived junction locks. STOPPED at the first orbit-breaking step:
the surface supplies no junction that is simultaneously derived and scale-sensitive, and each of
the five surface routes to one terminates in an exact invariance, quoted at source.
```

**The missing ingredient, named:** an orbit-breaking dimensionful datum — a metric/normalization
typing of the record cell's external side carrying a selected unit; equivalently, a derived
section of the presentation orbit (a selection of `lambda` for the diamond scale, and jointly of
`beta` for the fiber). Connection-only data cannot supply it, because everything connection-only
data yields at this locus — unit-modulus characters (S4), affine connection data (S17), and the
derived locks (S18) — is a fixed point of the rescalings the ingredient must break.

`RAIL_IMPORT_REQUIRED = YES`, with that ingredient exhibited — not performed.

---

## 3. THE OBSTRUCTION IS SURFACE-NATIVE — THE BLINDNESS-PRINCIPLE DISCHARGE

The carried given (v) voids any "cannot build it" that carries traditional-formalism residue. This
one carries none, and the difference is structural:

1. **The obstruction is a construction, not an absence.** The traversal derives, from surface
   objects alone, a positive invariance: the scale-orbit identity is the surface's own exponential
   identity on its own presentation (S12, member 03 :31-37 — an algebraic identity, no metric, no
   variation, no continuum); the unit-modulus character structure and the C7 unit-factor
   refinement law are exact ratified algebra (S4, S10); the affine-space statement is sealed
   surface text (S17); the junction split was EXECUTED, twice, independently (S19, TYPE-R). A
   blind tool fails to see; this traversal SEES and what it sees is the invariance.
2. **The logical step is surface-internal.** From "every derived object at the locus is invariant
   under the scale action" it follows that no construction from those objects can select a point
   of the orbit — a fixed-point argument on the surface's own inventory, of the same type as the
   surface's own gauge-invariance arguments. No metric, length, normalization, smooth structure,
   or variational notation occurs in it.
3. **The traversal found the surface-native content the traditional lens misses** — which is the
   blindness principle's own test. Where the traditional framing sees "a conversion waiting to be
   derived," the surface carries: two dimensionless feet, four derived dimensionless locks, and a
   free presentation orbit. The surface-native content at the beta locus is not a hidden metric;
   it is the ORBIT. The long-standing "block" is thereby re-typed, not merely re-affirmed: the
   surface wall is not "the conversion resists derivation" but "the derived inventory is
   orbit-invariant" — a wall with a different shape, native to the surface, and one the corpus's
   named would-builds (the F-equation, the joint saddle system) attempt to breach from the rail
   side, not the surface side (member 06 :30-33, :228-233; member 03 :68-79).

The false-rail escape is therefore closed on both sides: the RAIL verdict does not rest on a
traditional tool's failure, and the direct traversal does not produce the conversion — it produces
the obstruction to it.

One honest residue, marked: member 03 states the orbit in presentation variables (`T_R`, `H_R`,
`hbar`). Those variables are themselves rail-side chart coordinates (section 5). The orbit's
CONTENT — that the surface object `exp(-i tau_R B)` exhausts the surface-visible content of every
`(T_R, H_R)` pair — is a statement about surface objects; its dress is the chart. This is noted so
the reviewers can weigh it; it does not touch the fixed-point step, which runs entirely on S4,
S10, S17, S18.

---

## 4. MEMBER 04, READ CLOSELY — DERIVED OR IMPORTED (the required determination)

**Determination: SPLIT — the transport-COMPATIBILITY is derived; the metric itself is imported at
three named points; and even granted whole, the object supplies nothing toward the conversion.**

Derived (genuinely earned, from the artifact's own displayed steps): given the typed inputs, edge
transport preserves `h_n` — via spin pseudounitarity (`S_e^dagger gamma^0 = gamma^0 S_e^-1`),
Clifford normal covariance (`S_e^-1 slash(n_p) S_e = slash(n_r)`), `U(1)` cancellation between the
two arguments, and the typed law `n_p = Lambda_e n_r` (member 04 :26-45). The artifact's own
boundary sentence types the earned content exactly: compatibility "is not an independent numerical
assumption" (member 04 :49-51). Its own status flag agrees: what is set true is
`boundary_metric_edge_transport_isometry_derived` — the ISOMETRY, not the metric.

Imported (each a typing the connection-only surface does not carry):

```text
I1  the Lorentzian Clifford structure itself — gamma^0 and slash(n) presuppose the Clifford
    relation with the flat metric as its defining bilinear form; the metric enters at the level
    of the ALGEBRA, before any derivation begins.
I2  the typing of edge transport as proper-orthochronous LORENTZ with spin lift — the group is
    defined as that metric's isometry group; declaring the edges Lorentz-valued installs the
    metric's conformal class by fiat of the carrier choice.
I3  the root normal n_r as "future unit timelike" — three metric predicates (time orientation,
    causal type, and UNIT normalization); "unit" is precisely a selected normalization, the class
    of datum sections 2-3 show the surface cannot supply.
```

And the artifact's own question line concedes the direction of use: `h_n` is "the positive
boundary hypersurface metric USED BY the source-record incidence operator" (member 04 :5-8) — the
metric is antecedently in use by the formalism's operator; the artifact certifies the transport's
consistency with it. So the most metric-like object adjacent to the surface is: derived in its
compatibility, DECLARED in its existence and typing.

Robustness note, stated so the verdict does not lean on an unresolved fact: whether the edge data
`(Lambda_e, S_e, U(1))` is itself ratified surface data or blueprint-side is not decidable from
the six members. It does not matter. If blueprint-side, the metric typing is wholly read-in. If
surface-ratified, group invariance fixes the invariant form only UP TO POSITIVE SCALE — a
conformal class, never a unit — so I3's normalization remains unsupplied on either branch, and
`h_n` in any case contains no duration or cell-size dependence and is constant along the orbit.
**Either branch leaves the conversion exactly where sections 2-3 left it.**

---

## 5. THE SUB-QUESTION DECIDED: THE SAME OBJECT AS THE ORBIT, OR A FRAMING ON TOP?

**Decision: a metric framing laid ON TOP of the surface-native scale freedom — not the same
object. Four pieces of evidence, each anchored:**

```text
E1  DIFFERENT DERIVATIONAL CONTENT. The orbit is derived with zero metric ingredients — one
    exponential identity on the surface's own presentation (member 03 :14-37). The conversion
    requires two metric-typed feet; the surface supplies zero dimensionful ones (member 06
    :78-81, :102). An object derivable without metric ingredients cannot BE the object whose
    definition consists of them.
E2  OPPOSITE TYPE. The orbit is an INVARIANCE of surface content (a statement that nothing
    surface-visible moves); the conversion is a COORDINATE on the presentation space (a selection
    of how much things move). One is a fiber; the other is a chart on the fiber, plus the metric
    typing of its endpoints.
E3  THE FREEDOM LIVES WHERE THE RAIL LIVES. Sealed, executed: the entire conversion freedom sits
    AT THE JUNCTION, not inside the internal geometry (member 06 :78-81) — exactly the locus
    where every scale-sensitive quantity is ADOPTED, GAP, or UNBUILT and no derived quantity is
    scale-sensitive (member 06 :30-33). The freedom is real; its metric dress is supplied
    entirely by the adopted/unbuilt side of the inventory.
E4  THE CONVERSION AS TYPED IS LARGER THAN THE SURFACE FREEDOM — and the excess is the import's
    own modulus. As the program types it, the conversion bundles TWO presentation moduli: the
    diamond scale (the lambda orbit — surface-real, member 03) and the fiber radius (the beta
    freedom — a modulus OF the imported framing: member 02 :130-132 records the framing imported
    and the internal direction NOT derived to be a spatial dimension with a physical radius;
    member 06 sealed corroboration at :49-51, deriving T_R absolutely does NOT reopen the fiber).
    No fiber import, no beta modulus. Half of the conversion's freedom is generated by the rail
    itself — which a surface-native object could not be.
```

So the exact relation, stated once: **the surface natively carries the FREEDOM (the orbit); the
rail carries the FRAME (the metric typing of its two ends, the fiber that doubles its moduli, and
the chart that renders selecting a point of it as "converting internal to external metric").**
The absolute-scale constraint Q-1007 is correspondingly two-layered: its invariant core — surface
data does not identify an absolute scale — is SURFACE-REAL (it is the orbit, and given (iii)'s
response/matching channel is consistent with this: the "metric dependence" field that channel
consumes is an interface field the surface object explicitly leaves unfilled, member 05 :493-497).
Its STATEMENT as "the internal/external metric conversion of a record cell" is the rail's
rendering of that core, and the beta half of it is the rail's own contribution.

---

## 6. SURFACE-GEOMETRY-VERSUS-RAILS SPLIT (Q-427 discipline, per piece)

| Piece | Split | Anchor |
|---|---|---|
| record carriers, exact write S, sector projectors, controlled transition U_N | SURFACE | member 05 :119-150 |
| connection/holonomy histories; unit-modulus faithful characters | SURFACE | member 05 :161-165, :14-21 |
| exact finite influence functional F_N, Z_N; CTP Hermiticity; common-gauge invariance; C7 refinement law; one-cell falsifier | SURFACE | member 05 :24-41, :321-434 |
| two-branch history algebra | SURFACE | member 02 :199-209 |
| tau_R = pi/sqrt(2); generator B; the scale orbit and orbit-invariant inventory | SURFACE | member 03 :14-41 |
| causal-diamond DIMENSIONLESS shape coefficients | SURFACE | member 03 :44-50 |
| counting metric ell^2(X) (Hilbertization) | SURFACE (derived, dimensionless) | member 06 :78-79 |
| affine space of connections; no fiber metric | SURFACE | member 06 :86-87 |
| derived junction locks (action quantum; m_* T_R = pi; balanced-geodesic; onset) | SURFACE (all dimensionless, all invariant) | member 06 :82-88, :110-115 |
| isometry of edge transport on h_n, GIVEN its typed inputs | SURFACE-CONDITIONAL (derived relative to imported typing) | member 04 :26-51 |
| the metric argument g in Z_r[A_±,g_±]; the dynamics port's "metric dependence" field | RAIL | member 02 :155-177; member 05 :493-497 |
| KK/fiber framing; the physical fiber radius; hence the beta modulus itself | RAIL | member 02 :130-132; member 06 :116-118 |
| Lorentzian Clifford structure; Lorentz typing of edges; "future unit timelike" root normal | RAIL (I1-I3) | member 04 :14-24 |
| Fubini-Study convention | RAIL (sealed as imported) | member 06 :78-79 |
| compactness selector C_R = 1 | RAIL (adopted, Level-1) | member 06 :92-94 |
| winding k = +1 / period 2 pi hbar premise | RAIL (imported premise) | member 06 :82-83 |
| dimensionful presentation coordinates (T_R, H_R, radius, volumes) | RAIL-SIDE CHART on the surface orbit | member 03 :14-50 |
| the words "internal"/"external" as METRIC slots of one cell, and the conversion factor between them | RAIL | given (iv); member 06 :106-108 (the brief's framing, program-side text) |

---

## 7. NATIVE-TYPING AUDIT (Q-11 discipline — every internal/external distinction used anywhere above)

| # | Distinction | Verdict | Carrier or absence |
|---|---|---|---|
| D1 | carrier-side vs diamond-shape-side of one cell | SURFACE-CARRIED | both feet are derived surface objects (S15-S16; S14); same-object ground per member 06 :106-108; BOTH dimensionless |
| D2 | source sector vs record cell (control vs target) | SURFACE-CARRIED | the ratified controlled transition U_N (S6) — a role split, NOT a metric distinction |
| D3 | ready vs written record | SURFACE-CARRIED | \|R_N> vs \|P_N> (member 05 :152-157) — not metric |
| D4 | the CTP branch pair | SURFACE-CARRIED | member 02 :199-209 — not metric |
| D5 | "internal metric" | SPLIT | as the counting metric ell^2: SURFACE-CARRIED (derived, dimensionless); as the Fubini-Study-normalized metric: READ-IN (convention sealed as imported) |
| D6 | "external metric" (dimensionful Lorentzian cell metric: radius, duration, volumes) | READ-IN | the shape is derived, the conversion is not (member 06 :102); dimensionful values are orbit coordinates (member 03 :44-50); the fiber radius is the import's own modulus (member 02 :130-132) |
| D7 | the internal/external METRIC CONVERSION (the beta object) | READ-IN | no surface junction carries it; every derived junction is invariant under exactly the rescalings it would fix (member 06 :30-33); the words are absent from the cited surface text (given iv) |
| D8 | root vs endpoint of an oriented edge | SPLIT | the orientation: SURFACE-CARRIED (incidence data); the normals and their "future unit timelike" typing: READ-IN (member 04 :14-24, I3) |

The audit's one-sentence summary: **the surface is rich in two-sided structure (D1-D4, all
carried), and every one of its carried two-sidednesses is metric-free; the metric reading of any
of them — and the conversion between the readings — is where the rail begins (D5b-D7).**

---

## 8. WHAT THIS DOES NOT SAY (calibration against the 1101/1105 over-reach cases)

```text
IT DOES NOT say the scale question is machinery. The opposite: the scale non-identifiability is
   SURFACE-REAL — it is the orbit, derived. Only the metric-conversion FORM of the constraint,
   and the beta half of its freedom, are rail.
IT DOES NOT fix, estimate, bound, or begin to fix any scale, and it does not touch Q-12: no
   attempt is made anywhere to obtain a scale from shape. The construction target was the
   STRUCTURE'S provenance, and where the attempt met scale-selection it stopped and cited the
   orbit.
IT DOES NOT say a future ratified surface object could never be orbit-sensitive. It says the
   present ratified inventory at the junction contains none (executed twice, member 06), the five
   surface routes walked here each terminate in an exact invariance, and the corpus's own named
   candidates for an orbit-breaker are rail-side would-builds, not surface data (member 03
   :68-79; member 06 :228-233).
IT DOES NOT re-decide member 02's finding; it is consistent with it and sharpens it one step:
   1101 located the import at the metric ARGUMENT g; this attack finds that the internal/external
   CONVERSION the absolute-scale constraint rides on is the same rail seen from the record cell —
   the slot g would fill, typed onto one cell's junction.
IT DOES NOT declare Finish-A or Finish-B. Verdict logic is the spec's. The one consequence stated:
   if Finish-A reopens on the true surface, the reopened question is connection-only in form —
   "does any surface-native object break the presentation orbit" — and NOT "derive the metric
   conversion," which this artifact types as the rail's rendering of that question.
IT DOES NOT evaluate anything. No number of any physical kind appears except as sealed-text
   quotation; the only computed strings are file digests (custody, not physics).
```

---

## 9. CUSTODY

- CONSTRUCTOR NEVER VERIFIES OWN WORK. Everything above is CLAIMED until the two independent
  adversarial checks.
- Reads this session: exactly the six closure members, at their paths. Members 05 and 06 were
  entered because member 02's closure table cites them by name; their hashes match member 02's
  recorded values. No register, tracker, plan, road, or expectations file was read or written. No
  broad grep was run. No git action of any kind, read-only included.
- No file was written except this artifact. Sealing = this closure declaration plus the digest
  reported by the constructor on this file at its path.

---

## 10. FINAL LINES

```text
SURFACE_DATA_NAMED = the one-cell record basis (|r>,|p_Q>,|e_Q>) and the exact write S; the
     connection/holonomy histories a_+,a_- with unit-modulus faithful characters chi_n(h_j[a_j]);
     the neutral/charged sector projectors and the ratified sector-CONTROLLED transition U_N; the
     exact finite influence functional F_N = P_0 + Z_N P_ch with CTP Hermiticity, common-gauge
     invariance, the C7 refinement law, and the one-cell falsifier; the two-branch history
     algebra; tau_R = pi/sqrt(2) and the generator B with the scale-orbit identity and the
     orbit-invariant inventory; the causal diamond's dimensionless shape coefficients; the derived
     counting metric ell^2(X); the affine space of connections on the fixed line bundle (no fiber
     metric); the derived junction locks |Delta S_record| = pi hbar, m_* T_R = pi, the
     balanced-geodesic identities, and the onset bound; the thirteen-quantity junction inventory
     with its executed derived-vs-scale-sensitive split.

CONSTRUCTION_REACHED = the complete dimensionless two-sided junction: the full interacting
     functional F_N with all exact identities; the internal foot (counting metric, action
     quantum) fully pinned and dimensionless; the external foot as derived shape (tau_R, diamond
     shape coefficients); all four derived junction locks. STOPPED at the first orbit-breaking
     step, after walking all five surface routes to one (through F_N; through refinement C7;
     through gauge structure; through the affine connection space; through boundary transport) —
     each terminates in an exact, quoted invariance.

RAIL_IMPORT_REQUIRED = YES (an orbit-breaking dimensionful datum: a metric/normalization typing
     of the record cell's external side carrying a selected unit — equivalently a derived section
     of the presentation orbit, selecting lambda for the diamond scale and jointly beta for the
     fiber. Connection-only data cannot supply it: everything it yields at the junction —
     unit-modulus characters, affine connection data, dimensionless derived locks — is a fixed
     point of the rescalings the datum must break; and group invariance can fix a form only up to
     positive scale, never a unit.)

GEOMETRY_VS_RAILS = section 6 table. SURFACE: the interacting object and all its exact identities;
     the orbit; the shapes; the counting metric; the affine connection space; the four locks; the
     transport isometry GIVEN its typing. RAILS: the metric argument g / the port's
     metric-dependence field; the KK/fiber framing and the beta modulus it generates; the
     Clifford/Lorentz/unit-normal typing (I1-I3); the Fubini-Study convention; the C_R = 1
     selector; the winding premise; the dimensionful presentation chart; the internal/external
     metric wording itself.

NATIVE_TYPING_AUDIT = D1 carrier-vs-diamond-shape SURFACE-CARRIED (both feet dimensionless);
     D2 control-vs-target SURFACE-CARRIED (not metric); D3 ready-vs-written SURFACE-CARRIED (not
     metric); D4 branch pair SURFACE-CARRIED (not metric); D5 "internal metric" SPLIT (counting
     metric SURFACE-CARRIED; FS normalization READ-IN); D6 "external metric" READ-IN; D7 the
     conversion itself READ-IN; D8 edge orientation SURFACE-CARRIED, normals-and-typing READ-IN.

VERDICT = RAIL-DECLARED. The internal/external metric conversion STRUCTURE is a declared rail
     import: its metric typing has no surface carrier (D5b-D7), half its freedom (beta) is a
     modulus of the import itself, and the specific missing ingredient is exhibited (an
     orbit-breaking normalization) together with the surface-native obstruction showing
     connection-only data cannot supply it — an obstruction the direct traversal CONSTRUCTS
     (positive invariance, executed at the junction) rather than infers from a blind tool's
     failure, which is what rules out the false-rail reading. STATED WITH ITS BOUND, to avoid the
     known over-reach failure mode: the constraint's invariant core — that surface data does not
     identify an absolute scale — is SURFACE-REAL (it is the orbit, derived); what is an import
     artifact is the constraint AS TYPED — the metric-conversion form, and the fiber half of its
     freedom. Finish-A's reopened question on the true surface is therefore the connection-only
     one: whether any surface-native object breaks the presentation orbit. Within the ratified
     corpus as carried, none does.

IMPORTED_NOTHING = CERTIFIED
NO_METRIC_IMPORTED_TO_BUILD = CERTIFIED (the construction attempt used surface objects only; where
     it needed a metric it STOPPED and reported the need as the finding)
TARGET_INDEPENDENT = CERTIFIED (no output, measured value, or physical constant is referenced,
     approached, or compared)
EVALUATED_NOTHING = CERTIFIED (every scale symbolic; the only computed strings are file digests)
CLOSURE = declared-first (byte 0; end byte patched in place at seal, fixed width; 6 members; 0
     live; 10 pinned quotes verified at path and line)
PROSE_DIGESTS = 6/6 STRICT==STABLE (two independent hash passes, identical; members 05 and 06
     additionally match member 02's recorded hashes)
NO_NUMERIC_PATH = CLEAN (nothing computed; no sampling, no numeric library; numeric strings are
     sealed-text quotations or digests)
SURFACE_ANCHOR = present (objects named — S1-S19, at file and line)
RESIDUE_GREP = CLEAN (self-scan for measured-value and target residue patterns: 0 operative hits;
     scan record in the constructor's report)
REGISTER_READ = NONE-CERTIFIED (no register, tracker, plan, road, or expectations file touched;
     no broad grep)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN ("derived" used only of sealed-derived items at their citations;
     "adopted"/"imported" per the sealed source's own typing; "exhibited"/"constructed" used of
     this attack's own steps; "RAIL-DECLARED" confined to the conversion STRUCTURE and explicitly
     NOT extended to the scale question itself; nothing is claimed evaluated, fixed, or decided
     beyond the spec's verdict slot)

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**The plain statement.** The record cell's junction was walked directly, on the surface's own
objects, for the first time with this question in hand. The surface turns out to carry everything
except the one thing the constraint's wording presupposes: it carries both feet of the junction,
every derived lock between them, and a free orbit of dimensionful presentations — and it carries
no metric typing of either foot and no datum that selects a point of the orbit. The
"internal/external metric conversion" is therefore not a surface object awaiting derivation; it is
the rail's chart on a surface-native freedom, and half of that chart's freedom is manufactured by
the rail's own fiber import. The freedom is real. The conversion is declared.

All findings **CLAIMED** until the two independent adversarial checks.
