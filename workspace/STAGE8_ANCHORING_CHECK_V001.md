# STAGE 8 / ANCHORING DERIVATION — BLIND ADVERSARIAL CHECK (ANCHOR-CHECK)

Lane: FABLE blind adversarial verifier, codename ANCHOR-CHECK, cross-lineage.
DEFAULT = REFUTE. Date: 2026-08-13.
Under test: `STAGE8_ANCHORING_DERIVATION_V001.md`, sha256 recomputed at path THIS
session = `ca7fa4572a8b7037b40dd280700827081fec0e369ac0a8f9be1607113d719763` — MATCHES
the tasked digest.
**ALL_RESULTS = CLAIMED until checked (this artifact's own results included).**

## Verdict in one line

**AN_VERDICT = CONFIRMED.** The anchoring law (*) is exact and independently
re-derived; the fork is real and honestly held; the entered-horn collapse and the
open-horn conditional uniqueness both survive independent exhaustive re-derivation
(including three adversarial extensions the artifact did not run); the ledger
findings are forced item by item, with the L-1 tag correction verified against the
sealed ledger text; no circularity found; provenance clean (one sub-span citation
nit, non-load-bearing, recorded below); no injection observed.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       No physical quantity computed, bounded, or evaluated anywhere in this
       check; exact integer/symbolic structural algebra only.
```

---

## 0. Preflight and seals

Output name probed before write: `STAGE8_ANCHORING_CHECK_V001.md` ABSENT (artifact
and sidecar). No register, tracker, plan, road, route-ledger, or lens file read. No
git action.

All 12 seals recomputed by `shasum -a 256` at path THIS session BEFORE reliance;
every digest matched the artifact's §0 list exactly:

```text
22a2a478...ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md              MATCH
a5c71b2a...110c  STAGE8_TRANSPORT_LAW_POSE_CHECK_V001.md         MATCH
3e35ffe2...b970  STAGE8_G3_REALIZATION_BUILD_V001.md             MATCH
2cd1ffce...6d0f  STAGE8_RATIFIED_SOURCE_...FUNCTIONAL_RESULT (M03)  MATCH
f1881511...d647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md            MATCH
bb1b88ad...08d4  STAGE8_D0_SQUARE_CERTIFICATE_V001.md            MATCH
420ab02f...4c73  ATTACH_ENTRY_ADDENDUM_V001.md                   MATCH
ccca6bb4...00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md  MATCH
ad9fc14e...2213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001   MATCH
1892c08e...755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md              MATCH
aa7c6d49...108a  review_packets/.../BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  MATCH (packet member)
20a3a17d...1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md   MATCH (top-level; T6 non-member, no span cited against it)
```

All EIGHT V011 byte spans rehashed against the PACKET MEMBER aa7c6d49 only: all
eight sha256 digests MATCH the artifact's §0 table, and the decoded bytes carry
exactly the content the artifact quotes (d_0 law; K_square census; the two forward
paths; the loop holonomy word; the per-edge rule; the no-filled-2-cell line; the
frozen bases; the associated-bundle/vertex-gauge display). T6 honored end to end.

Quotes checked AT THE BYTES in the non-span sources: M03 :121-133 (S, D_n, W),
:177-181 (D_n S |r> = z |p_Q>), :235-248 (N=1 blocks, eraser -1), :321-354 (the
two-port covariance and the :352-354 independent-branch sentence — verbatim), :516
("admitted finite histories"); GB :165-183 (R4 incl. the prior YOURS-exact
single-branch boundary display and "NOT SEALED" port identification), :225-246
(S1-S4), :402-403 (flag); V007 :155-172 (the withdrawn b_1>=1 claim, the
no-selector sentence "choosing among them would be exactly the selector I am
forbidden to freeze", the b_1 = 1 operative domain, b_1 > 1 OUT OF SCOPE), :633-649
(the entered construction: theta_j = integral over gamma_j; R_j; E_j(e_s) = |r>,
E_j(e_t) = |p_Q>; n = +1); addendum :37 (future_derivation_can_select_the_attach_map
= NO_VERDICT) and :52-55 (void triggers: cascade blind test; structural
contradiction); MIN-CARRIER :394-418 (the ledger L-1..L-5 verbatim, L-1 tagged
"O12-shaped" in the source, L-2 "O11-shaped") and :11-19, :126, :230-236 (K_dd,
e_0b', w, b_1 = 2); hunt :196-206 (G-4, the nothing-licenses/nothing-forbids
sentence verbatim). All match the artifact's Q1-Q7 quotations.

The artifact's §7 script was extracted verbatim and executed: exit 0, and its
printed output matches §8 BYTE-FOR-BYTE (programmatic comparison, True).

---

## 1. ATTACK 1 — CIRCULARITY (hunted first). FINDING: NONE.

The classic failure would be a "derived" anchoring that silently supplies the
port-to-0-cell identification, or an O11 "display" that silently supplies the
incidence set S1 says is never displayed. Findings:

```text
1a  THE LAW LEG. (*) consumes: the sealed covariance z^g = t z s^dagger with
    G_out = D(t), G_in = S D(s) S (M03 :321-354, sealed); the sealed d_0 law and
    incidence sign (V011 packet spans); the sealed vertex-gauge display ("changes
    of local section", packet span [10886,11127)). The one definitional element —
    what "structure-preserving anchoring" means — is the reading already assembled
    YOURS-exact in the sealed GB R4 (:173-174: "reading t, s as values of a gauge
    0-cochain at the two ports gives d_0^dagger ell_j = e_out - e_in"), which the
    artifact cites, not invents. On the record there is no alternative
    non-vacuous reading: an "identification" with no realization demand would
    make O12 dischargeable by fiat, which is exactly the ledger's unlicensed-act
    description. NOT circular.
1b  THE VALUE LEG. The derivation supplies exactly two premises beyond sealed
    content and NAMES both: the open typing (horn hypothesis, never asserted) and
    the Step-4 canonicity demand (flagged PART-PROVABLE, its beyond-original-scope
    application to the V007 bar disclosed in §5). The unconditional verdict is
    NOT-DERIVABLE with gap and holder named. No horn is promoted. NOT circular.
1c  THE O11 LEG. The entered display is typed as the entry's own content unfolded
    (a_j := theta_j(A), V007 :638-641 verbatim), discharge claimed at ENTERED
    strength only, S1 explicitly left standing at law strength. The flattening
    check in the artifact's §6 is accurate against its own flag block. NOT
    circular.
1d  RESIDUAL SCOPE NOTE (does not rise to a finding). §2.1's structure-
    preservation display fixes the port characters as chi_n up front, and §2.1(iii)
    then reports "ports carry the cell's own charge n" among the corollaries. For
    the operative chains this is genuinely forced, not definitional echo — my
    extension X3 (below) proves (p,q) = (n,n) is the ONLY character-weight pair
    realizing the ports for every member of the unit-boundary family. The residue:
    read chains with boundary k(e_out - e_in), |k| >= 2, would admit chi_(kn)-
    weighted port readings under a bare "identification" construal; such chains
    are outside (*) as stated. Both horns' conclusions are k-robust (the entered
    collapse holds for every k != 0; boundary-support forcing is coefficient-
    blind), so nothing claimed depends on the normalization; it folds into the
    already-disclosed Step-4 leg. Recorded, not charged.
```

**CIRCULARITY = NONE.**

## 2. ATTACK 2 — THE BOUNDARY-EQUATION LEVER. FINDING: SOUND.

```text
2a  The adjoint identity <d_0 lambda, x> = <lambda, d_0^dagger x> re-verified
    independently on all 16 basis pairs over Z (I-B5/B6 use it live); the
    incidence sign (-1 source / +1 target) is exactly the sealed per-edge rule
    partial_rho,e x = u_e x v_target - x v_source (packet span [63009,63054)).
2b  The matching step is exact: with unit-modulus characters and lambda ranging
    over all real vertex phases, exp(i n <lambda, d_0^dagger ell - (e_out -
    e_in)>) = 1 for ALL lambda forces the integer 0-chain identity exactly (a
    linear form with integer coefficients vanishing mod 2pi at every real
    argument vanishes identically; n in {+1,-1} sealed). No mod-2pi slack
    survives the all-lambda quantifier.
2c  Both directions of the iff hold, including the degenerate case (e_out =
    e_in gives both sides zero and the trivially-satisfied law) — re-verified by
    enumeration (X2): under ell = gamma exactly the 4 diagonal vertex pairs are
    structure-preserving, no off-diagonal pair.
2d  The typing corollary (i) (0-chain, augmentation zero, sign placement), (ii)
    (O12 a function of O11 whenever the boundary is a difference of two distinct
    vertices), and (iii) (charge n at the ports — with the 1d scope note) are
    each exact.
2e  OVER-READ HUNT, negative: the lever does NOT over-read the sealed equation.
    It does not claim (*) selects the pair (B8 concedes all 12 pairs), does not
    claim the sealed text types h_j (the fork carries it), and does not convert
    GB R4's YOURS reading into sealed authority (it re-derives it and says so).
```

**LEVER_AUDIT = SOUND.**

## 3. ATTACK 3 — UNIQUENESS/FAMILY CLAIMS. FINDING: CONFIRMED, no missed anchoring.

Independent script (§6 below), written from the sealed census directly, plus
exhaustive enumeration the artifact did not run:

```text
I-A1/A2   G_in = diag(s,1,1), G_out = diag(1,t,1); covariance identical in
          (t,s,z); ports visible only on the ready->pointer entry.      PASS
I-B2      bd(gamma) = 0; bd(P1) = bd(P2) = e_v11 - e_v00; gamma = P1 - P2. PASS
I-B3      EXHAUSTIVE integer box [-4,4]^4: kernel members are EXACTLY the
          multiples of gamma (9 of 6561 chains; no stray member).       PASS
I-B4      EXHAUSTIVE box: every solution of (*) at (v_00, v_11) is P1 +
          c.gamma; nonnegative members exactly {P1, P2}.                PASS
I-B5/B6   Entered reading gauge-invariant; P1, P2 shift equally under every
          vertex generator (intra-cell common-port cancellation).       PASS
I-B7      v_00 unique source, v_11 unique sink, v_10/v_01 pass-through. PASS
I-B8      All 12 ordered pairs chain-admissible: (*) alone selects none. PASS

ADVERSARIAL EXTENSIONS (mine):
X1  The REVERSED anchoring (in = v_11, out = v_00) admits NO nonnegative read
    chain at all (exhaustive nonnegative box) — Step 3's flow match is backed by
    an exact nonexistence fact, stronger than the convention argument the
    artifact gives.                                                     PASS
X2  Entered reading: the full set of structure-preserving anchorings is exactly
    the 4 diagonal pairs — the artifact's four-member degenerate family is
    complete and nothing off-diagonal was missed.                       PASS
X3  Generalized character weights (p,q) at the ports: (n,n) is forced for every
    member of the unit-boundary family — corollary (iii) derived, not assumed,
    on the operative chains (see 1d for the k-residue).                 PASS
X4  The response word w = +e_a0 +e_ab -e_ba -e_0b' is closed on the K_dd census
    — the L-1 remark that even the two-slot shadow is degenerate under the
    entered reading is exact.                                           PASS
```

Hunt for anchorings the build missed or excluded without sealed grounds: the only
exclusions in the build are (a) pairs involving v_10/v_01 and sub-chain reads —
excluded by the DISCLOSED Step-4 demand, exactly as the artifact says (B8 proves
the exclusion is NOT free); (b) the reversed pair — X1 shows this exclusion is in
fact exact, needing no demand; (c) k-scaled reads — outside (*) as normalized,
1d recorded. Nothing was excluded silently. The DERIVED-FAMILY(degenerate) /
DERIVED-UNIQUE-CONDITIONAL / NOT-DERIVABLE trichotomy is honestly typed, and the
conditional's two premises are the true and complete premise set.

**UNIQUENESS = CONFIRMED.**

## 4. ATTACK 4 — LEDGER LICENSING L-1..L-5. FINDING: CONFIRMED, all five.

```text
L-1  ACT-REQUIRED with the tag correction: VERIFIED FORCED. The sealed ledger
     tags L-1 "O12-shaped" (MIN-CARRIER :394); the derived law governs exactly
     two port slots per cell (GB R4 :167-168 sealed); L-1 performs FOUR vertex
     identifications of the response face's attaching walk (MIN-CARRIER
     :394-396, :232) — C_2-attaching data, outside (*)'s reach. X4 confirms the
     w-word is closed, so even the two-slot shadow collapses under the entered
     reading. The correction is right and properly tagged YOURS.
L-2  ACT-REQUIRED: VERIFIED FORCED. The entered display's domain is connected
     admitted b_1 = 1 cells (V007 :162-172 verbatim); b_1 > 1 is OUT OF SCOPE by
     the entry's own words (:169-171); K_dd has b_1 = 2 (MIN-CARRIER :15, :126);
     the response circuit is not an admitted cell of any sealed domain. The
     named conditional path is correctly typed as an authored/principal act.
     ONE COMPLETENESS NOTE: the w-circuit contains the AUTHORED edge e_0b'
     (L-3), so the named admission would presuppose L-3's act as well — the
     conditional path is a two-act path, not one. Does not change the tag.
L-3  ACT-REQUIRED (untouched): CORRECT — no O11/O12 content speaks to cell
     existence.
L-4  ACT-REQUIRED (untouched): CORRECT — role import is outside the derived
     content entirely.
L-5  PARTIAL: VERIFIED. The derived SHAPE (port-chaining = anchored-boundary
     matching per (*)) is a direct consequence of the law leg alone; the three
     named residues are each real of record (entered ports inert per B5/X2; the
     open supply is the unsupplied horn; disjoint-only composition per hunt
     G-1). Nothing is issued; the shape/license distinction is kept.
```

**LEDGER_AUDIT = CONFIRMED (L-2 conditional path noted as presupposing L-3;
tag unchanged).**

## 5. ATTACK 5 — TRAPS T1-T7, PROVENANCE, INJECTION.

```text
T1  C_ref never used, never quantified over.                        HONORED.
T2  Neither C_ref nor D3 class is quantified over anywhere.         NOT IMPLICATED.
T3  The charter transport is never cited as authority for (*).      HONORED.
T4  No stitching/continuum progress booked; the law leg is O12-form
    progress only and says so.                                      HONORED.
T5  No R33/naturality premise appears anywhere in the derivation.   HONORED.
T6  All eight V011 spans hash-verified against packet member aa7c6d49; the
    top-level 20a3a17d is listed as non-member only, no span cited
    against it (verified).                                          HONORED.
T7  FR relay-751 negatives carried inside GB quotes with the B1-arc partials
    present in context Q7; nothing registered either way.           HONORED.

PROVENANCE: CLEAN. Every seal recomputed and matched; every load-bearing quote
  verified at the bytes. ONE NIT, recorded: the artifact's Q3 attributes the
  phrase "relative phases on the associated vertex fibers" to span [10886,11127);
  the phrase actually sits at bytes ~11330-11390 of the SAME sealed packet
  member, ~200 bytes after the span's end ("Thus rho_joint converts action
  comparisons into relative phases on the associated vertex fibers"). The
  load-bearing sentence of Q3 ("Vertex gauge maps are changes of local section
  of P") IS inside the cited span. Citation imprecision, source genuine,
  content verbatim, nothing rides on the misplaced phrase alone. NOT a break.
INJECTION: none. Nothing in any source read attempts to direct this check;
  all content is technical.
```

## 6. INDEPENDENT SCRIPT, VERBATIM (`anchor_check_independent.py`, Python 3 stdlib)

```python
#!/usr/bin/env python3
# anchor_check_independent.py — ANCHOR-CHECK independent re-derivation.
# Written from the sealed census directly, NOT from the artifact's script.
# Exact integer / symbolic algebra only. stdlib only.
from itertools import product
from fractions import Fraction

# ---- sealed one-cell algebra, independent encoding: entries are frozensets of
# (coeff, (et,es,ez)) accumulated in plain dicts keyed differently (exp tuple last)
def M(entries):  # entries: 3x3 list of dicts {(et,es,ez): coeff}
    return entries
def mzero(): return {}
def mone(t=0,s=0,z=0,c=1): return {(t,s,z): c}
def padd(p,q):
    r=dict(p)
    for k,v in q.items():
        r[k]=r.get(k,0)+v
        if r[k]==0: del r[k]
    return r
def pmul(p,q):
    r={}
    for (a,x) in p.items():
        for (b,y) in q.items():
            k=tuple(a[i]+b[i] for i in range(3)); r[k]=r.get(k,0)+x*y
            if r[k]==0: del r[k]
    return r
def matmul(A,B):
    return [[ (lambda cells: (lambda acc: acc)( 
              __import__('functools').reduce(padd,cells,{}) ))([pmul(A[i][k],B[k][j]) for k in range(3)])
             for j in range(3)] for i in range(3)]
def matdag(A):
    return [[{tuple(-e for e in k):v for k,v in A[j][i].items()} for j in range(3)] for i in range(3)]
def mateq(A,B):
    return all(A[i][j]==B[i][j] for i in range(3) for j in range(3))

# sealed M03 :124-133 on basis (|r>,|p_Q>,|e_Q>)
S=[[mzero(),mone(),mzero()],[mone(),mzero(),mzero()],[mzero(),mzero(),mone(c=-1)]]
def Dx(t=0,s=0,z=0):
    return [[mone(),mzero(),mzero()],[mzero(),mone(t=t,s=s,z=z),mzero()],[mzero(),mzero(),mone()]]

# I-A1: G_in = S D(s) S = diag(s,1,1); G_out = D(t) = diag(1,t,1)
G_in=matmul(matmul(S,Dx(s=1)),S); G_out=Dx(t=1)
assert mateq(G_in,[[mone(s=1),mzero(),mzero()],[mzero(),mone(),mzero()],[mzero(),mzero(),mone()]])
assert mateq(G_out,[[mone(),mzero(),mzero()],[mzero(),mone(t=1),mzero()],[mzero(),mzero(),mone()]])
print("I-A1 PASS  G_in = diag(s,1,1) on |r>;  G_out = diag(1,t,1) on |p_Q>; eraser silent")

# I-A2: covariance identity W_post(t z s^dagger) = G_out W_post(z) G_in^dagger
W=lambda t=0,s=0,z=0: matmul(Dx(t=t,s=s,z=z),S)
assert mateq(W(t=1,s=-1,z=1), matmul(matmul(G_out,W(z=1)),matdag(G_in)))
RHS=matmul(matmul(G_out,W(z=1)),matdag(G_in))
assert RHS[1][0]==mone(t=1,s=-1,z=1) and RHS[0][1]==mone() and RHS[2][2]==mone(c=-1)
print("I-A2 PASS  covariance identical in (t,s,z); ports only on the ready->pointer entry")

# ---- sealed K_square census, entered independently from V011 packet spans:
# vertices v_00,v_10,v_01,v_11 ; e_a0: v_00->v_10, e_0b: v_00->v_01,
# e_ab: v_10->v_11, e_ba: v_01->v_11   [62567,62693)
VN=['v_00','v_10','v_01','v_11']
ED={'e_a0':('v_00','v_10'),'e_0b':('v_00','v_01'),'e_ab':('v_10','v_11'),'e_ba':('v_01','v_11')}
EO=['e_a0','e_0b','e_ab','e_ba']  # frozen C_1 basis order  [63368,63429)
def boundary(chain):  # chain: dict edge->int ; returns dict vertex->int (-1 src,+1 tgt)
    out={v:0 for v in VN}
    for e,c in chain.items():
        s,t=ED[e]; out[s]-=c; out[t]+=c
    return {v:c for v,c in out.items() if c!=0}
gamma={'e_a0':1,'e_0b':-1,'e_ab':1,'e_ba':-1}   # sealed loop word, F08
P1={'e_a0':1,'e_ab':1}; P2={'e_0b':1,'e_ba':1}  # the two sealed forward paths [62735,62805)

# I-B2: closedness / boundaries / difference
assert boundary(gamma)=={}
assert boundary(P1)=={'v_00':-1,'v_11':1}==boundary(P2)
assert all(P1.get(e,0)-P2.get(e,0)==gamma.get(e,0) for e in EO)
print("I-B2 PASS  bd(gamma)=0 ; bd(P1)=bd(P2)=e_v11-e_v00 ; gamma=P1-P2")

# I-B3: EXHAUSTIVE kernel over the integer box [-4,4]^4: kernel members = Z*gamma only
ker=[]
for x in product(range(-4,5),repeat=4):
    ch={EO[i]:x[i] for i in range(4) if x[i]!=0}
    if boundary(ch)=={}: ker.append(x)
gm=(1,-1,1,-1)
assert all(any(all(x[i]==c*gm[i] for i in range(4)) for c in range(-4,5)) for x in ker)
assert len(ker)==9  # c in -4..4
print("I-B3 PASS  exhaustive box: ker(d_0^dagger)|C_1 = Z.gamma (no other kernel member)")

# I-B4: EXHAUSTIVE solution hunt d_0^dagger x = e_v11 - e_v00 over the box:
sols=[]
for x in product(range(-4,5),repeat=4):
    ch={EO[i]:x[i] for i in range(4) if x[i]!=0}
    if boundary(ch)=={'v_00':-1,'v_11':1}: sols.append(x)
p1=(1,0,1,0)
assert all(any(all(x[i]==p1[i]+c*gm[i] for i in range(4)) for c in range(-6,7)) for x in sols)
nonneg=[x for x in sols if all(v>=0 for v in x)]
assert sorted(nonneg)==sorted([(1,0,1,0),(0,1,0,1)])   # exactly P1 and P2
print("I-B4 PASS  exhaustive box: every solution is P1 + c.gamma; nonneg members = {P1, P2} only")

# I-B5/B6: gauge shifts of the readings under all 4 vertex generators
for v in VN:
    lam={v:1}
    d0lam={e: lam.get(ED[e][1],0)-lam.get(ED[e][0],0) for e in EO}  # (d_0 lam)_e = lam_t - lam_s
    shift=lambda ch: sum(d0lam[e]*ch.get(e,0) for e in EO)
    assert shift(gamma)==0
    assert shift(P1)==shift(P2)
print("I-B5/B6 PASS  entered reading gauge-invariant; P1,P2 shift equally under every generator")

# I-B7: degree census
indeg={v:0 for v in VN}; outdeg={v:0 for v in VN}
for e,(s,t) in ED.items(): outdeg[s]+=1; indeg[t]+=1
assert [v for v in VN if indeg[v]==0]==['v_00'] and [v for v in VN if outdeg[v]==0]==['v_11']
assert indeg['v_10']==outdeg['v_10']==1 and indeg['v_01']==outdeg['v_01']==1
print("I-B7 PASS  v_00 unique source, v_11 unique sink, v_10/v_01 pass-through")

# I-B8: every ordered pair (u,v), u!=v admits a chain with boundary e_v - e_u
cnt=0
for u in VN:
    for v in VN:
        if u==v: continue
        found=False
        for x in product(range(-2,3),repeat=4):
            ch={EO[i]:x[i] for i in range(4) if x[i]!=0}
            if boundary(ch)=={u:-1,v:1}: found=True; break
        assert found; cnt+=1
assert cnt==12
print("I-B8 PASS  all 12 ordered pairs chain-admissible: (*) alone selects nothing")

# ---- ADVERSARIAL EXTENSIONS -------------------------------------------------
# X1: REVERSED anchoring (in=v_11, out=v_00): boundary target e_v00 - e_v11 —
# does ANY orientation-respecting (nonnegative) read chain exist?  (Step 3 probe)
rev=[]
for x in product(range(0,5),repeat=4):
    ch={EO[i]:x[i] for i in range(4) if x[i]!=0}
    if boundary(ch)=={'v_00':1,'v_11':-1}: rev.append(x)
assert rev==[]
print("X1  PASS  the reversed pair (in=v_11,out=v_00) admits NO nonnegative read chain")

# X2: DEGENERATE anchorings enumerated: under ell=gamma, which ordered pairs (a,b)
# satisfy 'z^g = chi(g_a) z chi(g_b)^dagger for all gauge cochains'?  Answer: a=b only
ok=[]
for a in VN:
    for b in VN:
        good=True
        for v in VN:
            lam={v:1}
            d0lam={e: lam.get(ED[e][1],0)-lam.get(ED[e][0],0) for e in EO}
            lhs=sum(d0lam[e]*gamma.get(e,0) for e in EO)     # induced shift on theta
            rhs=lam.get(a,0)-lam.get(b,0)                    # claimed port shift
            if lhs!=rhs: good=False; break
        if good: ok.append((a,b))
assert ok==[(v,v) for v in VN]
print("X2  PASS  entered reading: structure-preserving anchorings = the 4 diagonal pairs only")

# X3: GENERALIZED port characters chi_p, chi_q (p,q integer weights): matching
# n<lam, bd ell> = p lam_out - q lam_in for all lam forces p=q (augmentation) and
# bd ell = (p/n)(e_out - e_in).  Probe on the open family: for ell = P1 + c.gamma
# the ONLY (p,q) with n=+1 realizing ports at (v_11, v_00) is p=q=1.
n=1
for c in range(-3,4):
    ell={EO[i]:p1[i]+c*gm[i] for i in range(4)}
    solset=[]
    for p in range(-3,4):
        for q in range(-3,4):
            good=True
            for v in VN:
                lam={v:1}
                d0lam={e: lam.get(ED[e][1],0)-lam.get(ED[e][0],0) for e in EO}
                lhs=n*sum(d0lam[e]*ell.get(e,0) for e in EO)
                rhs=p*lam.get('v_11',0)-q*lam.get('v_00',0)
                if lhs!=rhs: good=False; break
            if good: solset.append((p,q))
    assert solset==[(1,1)]
print("X3  PASS  generalized character weights: (p,q)=(n,n) forced for the unit-boundary family;")
print("          the cell's own charge n at both ports is DERIVED for these chains, not assumed")

# X4: response word w = +e_a0 +e_ab -e_ba -e_0b' (MIN-CARRIER): with e_0b' parallel
# to e_0b (v_00->v_01), is w closed?  (backs the L-1 'degenerate shadow' remark)
ED2=dict(ED); ED2["e_0b'"]=('v_00','v_01')
def boundary2(chain):
    out={v:0 for v in VN}
    for e,cc in chain.items():
        s,t=ED2[e]; out[s]-=cc; out[t]+=cc
    return {v:cc for v,cc in out.items() if cc!=0}
w={'e_a0':1,'e_ab':1,'e_ba':-1,"e_0b'":-1}
assert boundary2(w)=={}
print("X4  PASS  the response word w is closed: its two-port shadow collapses under the")
print("          entered reading exactly as the artifact's L-1 remark states")
print()
print("ALL INDEPENDENT CHECKS AND ADVERSARIAL EXTENSIONS PASSED")
```

## 7. EXACT SCRIPT OUTPUT, VERBATIM (exit code 0)

```text
I-A1 PASS  G_in = diag(s,1,1) on |r>;  G_out = diag(1,t,1) on |p_Q>; eraser silent
I-A2 PASS  covariance identical in (t,s,z); ports only on the ready->pointer entry
I-B2 PASS  bd(gamma)=0 ; bd(P1)=bd(P2)=e_v11-e_v00 ; gamma=P1-P2
I-B3 PASS  exhaustive box: ker(d_0^dagger)|C_1 = Z.gamma (no other kernel member)
I-B4 PASS  exhaustive box: every solution is P1 + c.gamma; nonneg members = {P1, P2} only
I-B5/B6 PASS  entered reading gauge-invariant; P1,P2 shift equally under every generator
I-B7 PASS  v_00 unique source, v_11 unique sink, v_10/v_01 pass-through
I-B8 PASS  all 12 ordered pairs chain-admissible: (*) alone selects nothing
X1  PASS  the reversed pair (in=v_11,out=v_00) admits NO nonnegative read chain
X2  PASS  entered reading: structure-preserving anchorings = the 4 diagonal pairs only
X3  PASS  generalized character weights: (p,q)=(n,n) forced for the unit-boundary family;
          the cell's own charge n at both ports is DERIVED for these chains, not assumed
X4  PASS  the response word w is closed: its two-port shadow collapses under the
          entered reading exactly as the artifact's L-1 remark states

ALL INDEPENDENT CHECKS AND ADVERSARIAL EXTENSIONS PASSED
```

---

## FLAG BLOCK

```text
CIRCULARITY = NONE. The derivation supplies exactly two premises beyond sealed
  content — the open typing (held as a horn hypothesis, never asserted) and the
  Step-4 canonicity demand (disclosed, PART-PROVABLE) — and both are named and
  quarantined in the artifact itself; the structure-preservation definition is
  GB R4's prior sealed YOURS-exact reading, cited not invented; the entered O11
  display is typed entry-content-unfolded with S1 left standing at law strength;
  no horn is promoted; the unconditional verdict is NOT-DERIVABLE with gap and
  holder named. Residual scope note recorded (not charged): the chi_n port
  normalization is fixed in the definition and §2.1(iii) restates it; my X3
  proves (p,q) = (n,n) is forced for every unit-boundary family member, so the
  corollary is genuinely derived on the operative chains; the |k| >= 2 scaled-
  boundary variant folds into the disclosed Step-4 leg and no conclusion
  depends on it.

LEVER_AUDIT = SOUND. The adjoint identity is exact on all 16 basis pairs over Z
  with the sealed incidence sign; the all-lambda matching forces the integer
  0-chain identity with no mod-2pi slack (n in {+1,-1} sealed); both iff
  directions verified including the degenerate case (X2: exactly the 4 diagonal
  pairs under the entered reading); the artifact does not over-read — B8
  concedes (*) alone selects nothing, the fork carries the unsupplied typing,
  and GB R4's YOURS status is re-derived, not promoted.

UNIQUENESS = CONFIRMED. Independent implementation + exhaustive enumeration:
  kernel = Z.gamma exactly (I-B3); the solution family is P1 + c.gamma exactly
  with nonnegative members {P1, P2} only (I-B4); the four-member entered family
  is complete (X2); the reversed pair (v_11, v_00) admits NO orientation-
  respecting chain (X1 — an exact fact BACKING Step 3 beyond the artifact's
  convention argument); the only non-exact exclusions are the ones Step 4
  discloses (B8/I-B8 prove the demand is necessary). No missed anchoring.

LEDGER_AUDIT = CONFIRMED (all five): L-1 ACT-REQUIRED with the tag correction
  VERIFIED (sealed ledger says "O12-shaped"; the derived law governs two slots;
  L-1 is four C_2-attaching identifications; X4 confirms w closed so even the
  shadow is degenerate); L-2 ACT-REQUIRED VERIFIED (b_1 = 1 domain and
  b_1 > 1 OUT OF SCOPE verbatim at V007 :162-172; K_dd b_1 = 2 sealed) with one
  completeness note: the named conditional path (admission of the w-circuit)
  also presupposes L-3's authored edge e_0b' — a two-act path, tag unchanged;
  L-3 and L-4 untouched, correct; L-5 PARTIAL verified (shape derived from the
  law leg alone; the three residues each real of record; nothing issued).

AN_VERDICT = CONFIRMED(ANCHORING = LAW-DERIVED, VALUE-FORKED: law DERIVED-
  UNIQUE exact; entered horn DERIVED-FAMILY(degenerate) TYPE-P; open horn
  DERIVED-UNIQUE-CONDITIONAL on the two disclosed premises; unconditionally
  NOT-DERIVABLE(gap = the loop/edge typing of h_j; HOLDER = authored-physics
  entry or principal decision); SUCCESSOR_DISPLAY = CONDITIONAL exactly as
  flagged, S1 standing at law strength; KERNEL IDENTITY exact and horn-
  independent; LEDGER_LICENSING as corrected; TYPING chains verified — the law
  leg rides M03 + V011 packet + GB only and survives an Attach void, the
  entered-horn values void with the entry per addendum :52-55).

PROVENANCE = CLEAN. 12/12 seals recomputed at path this session, all matched;
  8/8 V011 spans rehashed against packet member aa7c6d49 only, all matched,
  contents verbatim; all load-bearing quotes verified at the bytes (M03, GB,
  V007, addendum, MIN-CARRIER, hunt); the artifact's script re-executed, exit 0,
  output matches its §8 byte-for-byte. One nit, non-load-bearing: Q3's phrase
  "relative phases on the associated vertex fibers" sits ~200 bytes after the
  cited span [10886,11127) in the same sealed member — citation imprecision,
  content genuine and verbatim; the span's own sentence carries the claim.

INJECTION = none. All sources technical; no embedded directive encountered.

MACHINERY_USED_BY_ME = no (exact integer/symbolic structural algebra only —
  monomial-matrix identities and exhaustive integer-box chain enumeration,
  stdlib; no numeric evaluation of any physical quantity, no member binding,
  no fixed-point execution, no end test; output name probed ABSENT before
  write; no register/tracker/plan/road/route-ledger/lens read; no git action).

alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
