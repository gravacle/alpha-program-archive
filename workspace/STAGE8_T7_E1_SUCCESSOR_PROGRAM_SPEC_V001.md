# Stage-8 T7 E1 Successor Program — Replacement Activity Architecture and Three-Route IR Attack V001

Date: 2026-07-26
Root: `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/`

## Status

```text
SPECIFICATION_SEALED_BEFORE_EXECUTION
A hostile fresh-context lane reviews this text PRE-EXECUTION; execution is
fresh-context from the sealed text only. Named blocks are victories.
```

This is the successor program authorized by the principal's relay of 2026-07-26
("proceed with the E1 successor program along routes IR-A/B/C as scoped in
`ROUTE_MEMO_2026-07-26_ir_sea_kernel_attack.md`, with the corrected C1-C6
baseline as the DERIVED STARTING POINT"). It is the append-only successor
contemplated at `STAGE8_T7_E1_RECLASSIFICATION_CORRECTIONS_AND_ESCALATION_V001.md`
§"Next step". It supplies E1 into the parent majorant spec; it does not amend
any sealed file. Where this spec and the parent differ, this spec governs **only
for the E1/O4-M2 obligation it owns**, and every divergence is stated explicitly
in §S (Scoped supersessions).

It owns exactly one named blocker:

```text
E1_SEA_KERNEL_ACTION_DENSITY_UNCERTIFIED   (live blocker of O4-M2 in the parent
                                            majorant spec; per C2, the Phase-1
                                            functional that carried it is
                                            DIVERGENT and is REPLACED here, not
                                            certified)
```

---

## Pinned authorities

All hashes computed with `shasum -a 256` by this drafting lane on 2026-07-26.
The executor re-verifies every row before any computation; **drift blocks**.
Rows marked `SEAL_OK` were checked against their adjacent `.seal.sha256` and
matched. Rows marked `no adjacent seal` have no `.seal.sha256` in the cleanroom;
they are pinned by hash here and the construction lane must decide at seal time
whether to require a seal file (this drafting lane flags the absence rather than
treating the hash as equivalent to a seal).

| Role | Path | SHA-256 | Seal |
|---|---|---|---|
| **Corrected baseline C1-C6 (this spec's derived starting point)** | `STAGE8_T7_E1_RECLASSIFICATION_CORRECTIONS_AND_ESCALATION_V001.md` | `6c3e125ba7a43655b72ec75b218e64d1d5062213d5a10f740e89199cc1033aa1` | SEAL_OK |
| **Route scoping IR-A/B/C; graceful-block witness table** | `/Users/bgm/MB Work/alpha_supervision/ROUTE_MEMO_2026-07-26_ir_sea_kernel_attack.md` | `51f655a0a1bd3f20d7789f79e3ced16e08e56305521ebcafd4e9a7d4b8a3e528` | SEAL_OK |
| **Parent spec supplied into (D3, D4, D5, E1 rule, O4 M1-M3, spec-header scoping, F-1..F-8)** | `STAGE8_T7_CONNECTED_LINKED_CLUSTER_MAJORANT_DERIVATION_SPEC_V001.md` | `818083a52165bc5c2ee86bd43e3b7e30d87f5c9eb82e54935e7829bb6f1f84e3` | SEAL_OK |
| **Parent repairs M-1..M-11; I3 tuple V002** | `STAGE8_T7_D6_SPECS_REPAIR_AMENDMENT_V001.md` | `60223e6a175c5fee122f253491fd279daccfa15f8771af12104710f57ce09e5d` | SEAL_OK |
| **DERIVED arm = Route-Q predicate; F-8 is the gate; O7/O3 removed from predicate** | `STAGE8_T7_MAJORANT_ARM_RESCOPE_PRINCIPAL_DECISION_V001.md` | `209aa39015c955e46ec84519c05b8de52e65288ba883e01590b245fcbfe0bb53` | SEAL_OK |
| Exact sea covariance `C = 1_(-inf,0)(h_0)` (C1's source) | `STAGE8_T7_PARENT_STATE_REGULATOR_RESTRICTION_RESULT_V001.md` | `3a6ff6173573a7d9dd99bdd1a6bb7eaa02c433ab50bc769e144d94c983b4f0ff` | no adjacent seal |
| Phase-A spec A1/A2/A3/A4 (`h_0`, `M(t)`, `v(t)`, `b_D`, `J`, `S`, `w_lambda`) | `STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md` | `789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3` | SEAL_OK |
| L2 typing freeze (completed chain; fences 1-5) | `STAGE8_T7_L2_INTENSIVE_HESSIAN_TYPING_FREEZE_V001.md` | `cdbacdaf5efcd3a70a6ffa456bc790753e0ff36609c40c67b9bb606b79624746` | SEAL_OK |
| L1 assessment (Lemma 0 constitutive) | `STAGE8_T7_L1_MULTICELL_LEMMA_ASSESSMENT_V001.md` | `03fadd3634cf8dbc729795c4009012933d3ad836e663c9c509e4e43b7b4883bc` | SEAL_OK |
| Determinant fence, gate item 9 (mandatory M1 citation (i)) | `STAGE8_ROUTE2_CAR_STATE_BRIDGE_SCOPE_CORRECTION_V001.md` | `4e1282bc800c47441d255e9d9d576958608d955dce15f02969261cd6e601e268` | SEAL_OK |
| A4 obligation relocation (mandatory M1 citation (ii); carries the M-11 disclaimer) | `STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md` | `8a7f52ffa2500d20ad834b11e3762ed114ee1a201f2fec18bcb119e3c7ead860` | SEAL_OK |
| O7 obstruction record (Route-T mechanism; `(3/8)^k`; `e^{+-i pi}`) | `STAGE8_T7_MAJORANT_PHASE2_STATUS_AND_O7_ESCALATION_V001.md` | `3c81647eb55ad01d4d71e23b2b45161427f82390098c95afa5721065206a7df2` | SEAL_OK |
| `tau_R = pi/sqrt(2)` derivation | `BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md` | `b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f` | no adjacent seal |
| CTP propagator typing (IR-B) | `BID_FREE_QUASIFREE_CTP_PROPAGATOR_DERIVATION_V001.md` | `6f6b822ac8ccf9ea19659f4ccf811268f60a27a361f817ee6513479d63b62546` | no adjacent seal |
| Envelope-profile class (`v_A`, `v_B`) | `STAGE8_T7_ENVELOPE_REALIZATION_COMPARISON_SPEC_V001.md` | `4059eff522426d06d41d2a0542ddf6be309867bd077491d84c6998b2098ede31` | no adjacent seal |
| Relay necessity (relay isometries, ready-root supply) | `STAGE8_T7_OPEN_EXHAUSTION_ATTACHMENT_AND_RELAY_NECESSITY_RESULT_V001.md` | `0df721a170f4f4a1ec630775a3ae47b0d793c82e100b326e681030389eaf0694` | no adjacent seal |
| Relayed-family resolution (causal order; disjoint-cell commutation) | `STAGE8_T7_RELAYED_FAMILY_RESOLUTION_RESULT_V001.md` | `52401eefc3ff84e22f04bbc329a619b830058661db7f969d2d43d167d97d893d` | no adjacent seal |
| Monoidality on disjoint cells | `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md` | `451550c3825288d699db35c7289e408e8314ad042450253b58f32722c4ead46b` | no adjacent seal |
| Closure result (GHZ witness for NC1; `t^-3` insufficiency for NC3) | `STAGE8_T7_CONNECTED_ANALYTIC_CLOSURE_RESULT_V001.md` | `f891d3afc58e695529d8d569b5d2ba4b853e30c9cfa4296817cb17b60f38b054` | no adjacent seal |
| Phase-1 draft (the REPLACED `eta`/`K_sea` architecture; O1/O2, R1-R4, `\|\|b_D\|\|=1`) | `stage8_execution/work/MAJORANT_LEMMA0_PROOF_DRAFT_V001.md` | `679ba036b8c6c820a5367ae460f369c8bd6c0e03eb9de395b99e60472e3a7b9c` | SEAL_OK |
| Phase-2 draft (NC3 `24 H_K`; exact `e^{+-i pi}` witness; O7) | `stage8_execution/work/MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md` | `08b91543fdb72f656c756ca5f8df8233b87eb5487284c7fd2170cba67f7e0e3b` | SEAL_OK |
| Phase-1 primary record | `stage8_execution/work/T07_majorant_phase1_primary_v001.json` | `e4a725a9168129ffdd8128a153b557a3d4549225375ba1607d88f9428187a0e2` | SEAL_OK |
| Phase-2 primary record | `stage8_execution/work/T07_majorant_phase2_primary_v001.json` | `8d986314a93a2e5378a243a55ed919b37e859b1646f34b96fa315b40e1b30339` | SEAL_OK |
| Calibration ledger (governs §P) | `/Users/bgm/MB Work/alpha_supervision/CALIBRATION_LEDGER.md` | `fcf35d0d458d6720fa19965a4ac976ea85fc52ad5c6ba08db29594b3fd336a04` | living document, unsealed by design |

**Drafting-lane verification evidence** (exact arithmetic, pinned runtime
`/Users/bgm/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3`,
no scipy; scratch under
`/private/tmp/claude-501/-Users-bgm-MB-Work/578abe61-ea1d-48e6-8fb1-b06509d933a1/scratchpad/e1_spec/`):

| script | sha256 | independently re-verified this session |
|---|---|---|
| `verify.py` | `1acfda267c0b228d93aefa857855338b08c0f031284cc5a37dd881ae9303fb74` | `m_0=m_1=0`, `m_{2j}=-2^{j-1}`, all odd moments zero (exact `Q(sqrt2)`); the second-difference identity on monomials of degree 0..8; the CTP double-moment table; the `tau_R` phase saturation `sum w_lambda phase_lambda = sum \|w_lambda\| = 1`; the Chebyshev shell identities `24k^2+2` / `64k^3+16k` (exact, k=1..59); exact-rational partial sums `S(3000)` for the two-line 3-D and 4-D anchored sums; `24 H_K` reproduction for the one-line 3-D sum; the `(2/pi) log(R/a)` marginal coefficient; the `L`-independent `eta^2 = eps^2/2` refinement scaling |
| `verify2.py` | `6221997577163b1b06601f8498ec83e0847cc193ebec29871ef05ed18b36c0cf` | the baseline bookkeeping identity `N(0) = (1-D)/2` (exact rational projector pairs, 6 trials); the determinant bound `D^2 <= exp(-2 \|\|[C,P]\|\|_2^2)` (200 exact trials, no violation); the commutator identity `\|\|[C,P]\|\|_2^2 = 2 sum sigma_i(1-sigma_i)` (exact, 50 trials) |

These are drafting-lane checks. They are **not** certifications; every quantity
below still requires certified outward enclosures under §N (Frozen Numerics).

---

## §B — Derived starting point (C1-C6, carried verbatim in substance)

The following are **inputs**, not obligations. They are the sealed corrections of
record at `6c3e125b…`, re-verified independently by this lane where checkable.
No obligation below may re-litigate them; an obligation that contradicts one of
them without a new exact witness is a spec violation and BLOCKS.

```text
C1. SEA COVARIANCE PINNED EXACTLY, NOT BY CLASS.
    C = 1_(-inf,0)(h_0),  C(p) = (I - alpha·p-hat)/2,
    C(r) = (1/2) delta^3(r) I  -  i alpha·r / (2 pi^2 |r|^4);
    off-diagonal modulus EXACTLY 1/(2 pi^2 |r|^3); homogeneous degree -3;
    odd (zero spherical mean; Calderon-Zygmund).
    E1 is a LEMMA UNPROVEN ABOUT A FULLY KNOWN OBJECT, not an absent input.
    Marginality quantified: int_{a<|r|<R} |C_off| d^3 r = (2/pi) log(R/a)
    exactly — logarithmic at BOTH ends; "infrared" in the parent's O4 note is
    inherited from masslessness, and the divergence sits at the UV/coincidence
    end.

C2. Phase-1 K_sea IS DIVERGENT, NOT UNCERTIFIED.
    The trace norm of (sea) x (sharp diamond-slice localizer) diverges as a
    power (~ |D_t| Lambda^3) under the parent's own carrier-blindness
    requirement; it is finite only on a fixed carrier, which the parent's
    spec-header scoping clause 1 forbids. THE PHASE-1 eta(eps) ARCHITECTURE IS
    REPLACED BY §R BELOW, NOT CERTIFIED. Nothing in this spec may cite
    (K_sea, T_R, b_0) as a target of certification.

C3. NC3 DOES NOT CORROBORATE E1 FROM BELOW.
    NC3 is a ONE-LINE control; its exact 24·H_K divergence is a one-line
    counting artifact. The lowest connected two-cell cumulant is a TWO-LINE
    object (the first-order determinant term has no cross term). NC3 remains a
    correct PASS of its own charter and says nothing about E1. Any artifact
    repeating the "corroborates from below" reading is a spec violation.

C4. TWO MOMENTS VANISH; THE CANCELLATION IS EXACTLY SATURATED AT FULL tau_R.
    m_0 = m_1 = 0 exactly; m_{2j} = -2^{j-1} (j >= 1); all odd moments zero.
    sum_lambda w_lambda f(lambda) = -(1/4)[f(+sqrt2) - 2 f(0) + f(-sqrt2)]:
    the completed per-cell Kraus operator IS a symmetric second difference in
    the record color. Consequences: every Dyson term with fewer than two record
    insertions PER CTP BRANCH is annihilated (double moments m_j m_k nonzero
    iff j,k both even and >= 2); the bare a-linear tadpole vanishes identically
    (also killed independently by the odd spinor trace tr_spinor[C(p) alpha_x]
    = -2 p-hat_x).
    BUT at full tau_R, lambda·tau_R in {0, +pi, -pi} and, on the ball where
    M(t) = 1, exp(-i lambda tau_R S) = (+1, -1, -1)·I EXACTLY (S^2 = I), so
      sum_lambda w_lambda · phase_lambda = 1 = sum_lambda |w_lambda|.
    Phase-1's l1-mass step is TIGHT, not lossy; the zero-sum weights buy NO
    decay improvement; and at the operator level
      sum_lambda w_lambda u_lambda = m_0 (I-P) + 1·P = P,
    i.e. the weights PROMOTE the sharp causal-ball projector P = M(t) to the
    leading in-cell object.

C5. THE LONG-DISTANCE DIRECTION HAS MARGIN.
    Two-line pair activities carry R^-6. Anchored sums converge with exact
    constants
      3-D:  24 zeta(4) + 2 zeta(6)  =  4 pi^4/15 + 2 pi^6/945
      4-D:  64 zeta(3) + 16 zeta(5)
    against Chebyshev shell counts 24k^2+2 and 64k^3+16k. Under refinement the
    action-density form absorbs the cell scale with an L-INDEPENDENT
    eta^2 = eps^2/2. Clustering was never the missing ingredient.

C6. THE BLOCK IS CONFINED TO n = 1.
    Specifically to the pairing of the marginal degree-(-3) sea with the SHARP
    localizers M(t) = Q 1_{|x| <= r(t)} Q and 1_{D_t} of the sealed Phase-A A1
    construction. Equal-time localization of the 3-D massless Dirac sea fails
    Shale-Stinespring: ||[C, 1_B]||_2 = +infinity (and even a Lipschitz cutoff
    gives int d^3 r · r^2/r^6 = int dr/r^2, divergent). Only TWO-TIME /
    scattering-type objects, where the cell time integration supplies the
    missing decay, can work.
```

---

## §D — Frozen definitions

Inherited verbatim from the parent spec as repaired: **D1** (completed chain,
`m_0 = 0`, T7(i) anchor `Z_hat_comp = Z_comp(a)/Z_comp(0)`, named hypothesis
H-B, no unitality anchor), **D2** (relayed causal exhaustion, every cell at full
`tau_R`), **D3** (skeleton + families A, B + all common refinements; pinning the
quantifier is unavailable to any lane), **D4** as repaired by **M-2** (CTP pairs
`(a_{c,+}, a_{c,-})`, polydisc `max(|a_{c,+}|,|a_{c,-}|) <= eps_*`,
adjoint-continued bra branch `Ktilde(w) = [K_pointer(conj w)]^dagger`, joint
holomorphy), **M-3** (the two Phase-A pinned finite schemes `C_mix = Q P_- Q`,
`C_pure = 1_(-inf,0)(Q h_0 Q)`; **per-state, reported separately, never
promoted**), **M-7** (tuples read `|w_lambda|`; the identity
`|w_lambda| = p_lambda` on the sealed A3 chain recorded).

Three definitions are added or scoped here.

### D5' — Action-density activity form, SPLIT (scoped supersession of D5)

The parent's D5 demands a **single** `eta` for all `n`. For the obligations this
spec owns, D5 is read with a **split**:

```text
  sum_{gamma ni C, |gamma| = 1} |Phi_gamma(a)|  <=  |C|_4 · eta_1
  sum_{gamma ni C, |gamma| = n} |Phi_gamma(a)|  <=  |C|_4 · eta_{>=2}^n   (n >= 2)
```

with `|C|_4` the cell 4-volume and both `eta_1`, `eta_{>=2}` functionals of
`(||b_D||, tau_R, sea-kernel decay data, |w_lambda|)` **only**, carrier-index-blind,
stated over the envelope-profile class.

**The split ISOLATES `n = 1`; it does NOT evade it.** Stated as an explicit,
non-negotiable clause of this definition:

```text
KOTECKY-PREISS DOES NOT RELEASE n = 1. The aggregate margin the parent supplies
to the I3 interface is Gamma_star = eta/(1-eta) at eta <= 1/2, i.e.
Gamma_star <= 1. Under the split the SAME aggregate must be delivered:

  Gamma_star^split  :=  eta_1  +  eta_{>=2}^2 / (1 - eta_{>=2})   <=  1
  certified, with eta_{>=2} < 1 certified.

At eta_{>=2} <= 1/2 the tail term is <= (1/4)/(1/2) = 1/2, so the criterion
reduces to eta_1 <= 1/2 AND eta_{>=2} <= 1/2 — EXACTLY the parent's single
threshold on BOTH legs. No number is loosened by the split. Its entire value is
diagnostic and graceful-block: n >= 2 may certify outright while the residue is
NAMED at n = 1. Any artifact that reads the split as weakening the n = 1
requirement, or that reports a verdict with eta_1 uncertified, is a spec
violation and BLOCKS with witness D5_SPLIT_MISREAD_AS_RELEASE.
```

The consumer interface is preserved: the Duhamel spec's I3 tuple V002 receives
`Gamma_star := Gamma_star^split` and `eps_* := min(eps_*^{(1)}, eps_*^{(>=2)})`
per §E1'. The consumer sees an unchanged contract.

### D6' — Sharp localizers (frozen; not softenable by any lane)

```text
M(t) = Q 1_{|x| <= r(t)} Q,   r(t) = min(t, 1-t),   v(t) = tau_R · 32 r(t)^3,
int_0^1 v(t) dt = tau_R = pi/sqrt(2) EXACTLY (int_0^1 32 min(t,1-t)^3 dt = 1,
exact rational), 1_{D_t} the sharp diamond slice, all per sealed Phase-A A1/A2
(789338ad…). THESE ARE SEALED DEFINITIONS. No obligation, lemma, control,
repair, or enclosure in this spec may replace, mollify, smooth, or regularize
M(t) or 1_{D_t}. See §Q2-STOP and §NC9.
```

### D7' — The three-line reading of E1 (a) / (b) / (c)

Frozen, per the memo §1.4, so that verdicts attach to the right sub-object:

```text
(a) REMOVABLE: the a-independent, cell-local, carrier-blind-divergent part of
    the Phase-1 functional. Cancels in the T7(i) ratio Z_hat and again in the
    Moebius truncation for |gamma| >= 2. This spec removes it BY ARCHITECTURE
    (§R), never by enclosure.
(b) MARGINAL: the logarithmic residue of the degree-(-3) kernel against the
    in-cell SHARP localizers. Undecided by any sealed authority. This is the
    substantive n = 1 target.
(c) CONVERGENT: the long-distance n >= 2 cluster sums. Two spare powers in 3-D,
    three in 4-D (C5). IR-A certifies these.
```

---

## §R — REPLACEMENT ACTIVITY ARCHITECTURE (frozen; supersedes `K_sea`)

This is the architecture that supersedes the Phase-1 `eta(eps)` /
`(K_sea, T_R, b_0)` functional declared DIVERGENT by C2. It is frozen here,
outcome-blind, before any verdict-bearing computation.

**Name:** `SCAD` — *Subtracted Carleman Action-Density*.

### R.0 — Reduction and the subtracted object

Per admitted state (M-3, per-state), per admitted cell `C` in the D3 quantifier,
per record-color pair `(mu, lambda)`, per CTP pair on the closed pair polydisc
(M-2), with `V_{mu lambda}(a) := u_mu^{(c)}(a_-)^dagger u_lambda^{(c)}(a_+)`
(adjoint-continued):

```text
Block-triangular identity (obligation R-L1):
    1 + C(V - 1)  =  [[ C V C ,  C V (1-C) ] , [ 0 , I ]]
  =>  det( 1 + C(V-1) )  =  det_{ran C}( C V C ).

Per-pair operator on ran C, and its SUBTRACTED difference:
    A_{mu lambda}(a)      := C ( V_{mu lambda}(a) - 1 ) C
    Delta_{mu lambda}(a)  := A_{mu lambda}(a) - A_{mu lambda}(0)
                           = C ( V_{mu lambda}(a) - V_{mu lambda}(0) ) C
    A_{mu lambda, s}(a)   := A_{mu lambda}(0) + s Delta_{mu lambda}(a),  s in [0,1].
```

Carleman regularization (`det_2`), with **the a-difference and the Moebius
difference taken BEFORE any norm is applied** — this is the architectural
content of the replacement:

```text
    det(1 + A) = det_2(1 + A) · e^{tr A},     Log det_2(1+A) = tr[ Log(1+A) - A ]
    per-pair RATIO  rho_{mu lambda}(a) := det(1+A_{mu lambda}(a)) / det(1+A_{mu lambda}(0))
    Log rho_{mu lambda}(a)
        = [ tr A(a) - tr A(0) ]                                 (SUBTRACTED TRACE)
        + [ Log det_2(1+A(a)) - Log det_2(1+A(0)) ]              (SUBTRACTED det_2)
        = tr[ Delta ]  -  int_0^1 tr[ (1 + A_s)^{-1} A_s Delta ] ds .
```

**Drafting-lane finding, load-bearing, and a correction to the memo's IR-B
display.** The memo's §2 IR-B writes the subtracted display for
`Log Z_hat_comp^{(C)}(a)` directly. That display is valid **per color pair**, not
for the color-summed object: `Z_comp^{(C)}(a) = sum_{mu,lambda} w_mu^* w_lambda
det(1 + A_{mu lambda}(a))` is a **sum** of determinants, and the log of a sum is
not a sum of logs. Moreover the unsubtracted per-pair baseline trace
`tr A_{mu lambda}(0)` is itself divergent at full `tau_R` (see R.2), so a
per-pair Carleman split of the *unsubtracted* baseline is singular. The
architecture therefore routes the color sum through the **baseline balance
ratio** of R.1. Any execution artifact that applies the memo's IR-B display
directly to the color-summed object is a spec violation and BLOCKS with witness
`SCAD_COLOR_SUM_LOG_MISUSE`.

### R.1 — The frozen SCAD constants (the permitted tuple ONLY)

All five are `sup`/`inf` over: the admitted states (M-3), the admitted cells `C`
of the D3 quantifier, the color pairs `(mu, lambda)`, and the closed pair
polydisc at radius `eps`. All are, by their defining expressions, functionals of
`(||b_D||, tau_R, sea-kernel decay data, |w_lambda|)` **only** — no Hermite
carrier index `n`, no `ell`, no truncation level, no cellulation-family index
appears. Each requires a certified outward enclosure under §N.

```text
S1.  G_tr(eps)   := sup  |C|_4^{-1}   · | tr_{ran C}[ C ( V_{mu lambda}(a)
                                          - V_{mu lambda}(0) ) C ] |
                    [SUBTRACTED TRACE DENSITY. The a-independent sea-depth
                     divergence is absent by construction; the leading residue
                     is killed TWICE — odd spinor trace tr_spinor[C(p) alpha_x]
                     = -2 p-hat_x, and |m_0|^2 = 0 (C4).]

S2.  G_hs(eps)   := sup  |C|_4^{-1/2} · || C ( V_{mu lambda}(a)
                                          - V_{mu lambda}(0) ) C ||_2
                    [SUBTRACTED HILBERT-SCHMIDT DENSITY. Must be a TWO-TIME
                     (cell-S-matrix) object; the equal-time version is FALSE
                     by C6.]

S3.  G_bl        := sup  |C|_4^{-1/2} · || C ( V_{mu lambda}(0) - 1 ) C ||_2
                    [BASELINE HS DENSITY = the cell-level Shale-Stinespring
                     quantity. Records at full tau_R.]

S4.  rho_res     := sup_{s in [0,1]} || ( 1 + A_{mu lambda, s}(a) )^{-1} ||_op
                    [RESOLVENT BOUND. NAMED HYPOTHESIS H-R. Not discharged
                     here. May NEVER default to 1 or to any other value; see
                     F'-3.]

S5.  kappa_bal   := sup  [ sum_{mu,lambda} |w_mu^* w_lambda| · |det(1+A_{mu lambda}(0))| ]
                       /  | sum_{mu,lambda}  w_mu^* w_lambda  · det(1+A_{mu lambda}(0)) |
                    [BASELINE BALANCE RATIO. NAMED HYPOTHESIS H-B0. This
                     REPLACES Phase-1's b_0 = |Z_comp^{(C)}(0)|: it is
                     dimensionless, scale-free, and equal to 1 exactly in the
                     exact-record-collapse limit (R.2). The denominator is the
                     T7(i) baseline, so H-B0 is H-B in ratio form and inherits
                     its undischarged status (L2 fence 3).]
```

### R.2 — Derived structure of the baseline, and why `kappa_bal` replaces `b_0`

**Drafting-lane derivation, verified in exact arithmetic (`verify2.py`), offered
as obligation R-L4/C-L4 rather than as a result.** Under the exact record-phase
collapse of C4, with `P` the cell's sharp record projector and phases
`(+1,-1,-1)` on `lambda = (0, +sqrt2, -sqrt2)`:

```text
V_{mu lambda}(0) - 1 = ( phase_mu · phase_lambda - 1 ) P
                     = 0        for same-phase pairs
                     = -2 P     for opposite-phase pairs.

Weight bookkeeping (exact): S_+ := sum_{phase=+1} w_lambda = 1/2,
                            S_- := sum_{phase=-1} w_lambda = -1/2.
   same-phase pair weight total  = S_+^2 + S_-^2 = 1/2
   opposite-phase pair weight total = 2 S_+ S_-  = -1/2

  =>  N(0) := Z_comp^{(C)}(0) = (1/2)·1 + (-1/2)·D = ( 1 - D ) / 2 ,
      D := det_{ran C}( 1 - 2 C P C )      [VERIFIED exactly on 6 random
                                            rational projector pairs]
```

and, with `sigma_i in [0,1]` the eigenvalues of the positive contraction `C P C`:

```text
  D^2 = prod_i (1 - 2 sigma_i)^2 = prod_i ( 1 - 4 sigma_i (1 - sigma_i) )
      <= exp( -4 sum_i sigma_i (1 - sigma_i) )
      = exp( -2 || [C, P] ||_2^2 )
  using the exact identity  || [C,P] ||_2^2 = 2( tr(CP) - tr((CP)^2) )
                                            = 2 sum_i sigma_i (1 - sigma_i)
  [inequality: 200 exact trials, no violation; identity: 50 exact trials]
```

**Consequence to be certified, not assumed.** C6 states
`||[C, 1_B]||_2 = +infinity` for the 3-D massless Dirac sea against a sharp ball.
If that transfers to the cell's record projector, then `D = 0` **exactly**, hence

```text
      b_0 = |N(0)| = 1/2   EXACTLY,        kappa_bal = (1 + |D|)/|1 - D| = 1   EXACTLY.
```

Two things follow, and both are recorded as findings of record for the successor
program rather than as claims:

1. **The very non-Hilbert-Schmidt-ness that is fatal to the numerator (B-L2) is
   benign — indeed favourable — for the baseline.** The `m_0 = m_1 = 0`
   second-difference structure of C4, which "buys nothing for decay," is exactly
   what protects the denominator: the same-phase sector contributes weight `1/2`
   with determinant `1`, and no cancellation against it can occur.
2. **This is a candidate partial discharge of H-B** — currently a named,
   undischarged hypothesis of the parent theorem — **at the single-cell,
   per-state, exact-collapse level only.** It is NOT a discharge of H-B on every
   admitted finite complex (the multi-cell baseline is a determinant of a
   relay-ordered product, not a product of determinants; see obligation C-L5).
   No artifact may report H-B as discharged.

The sufficient condition for a certified lower bound on the baseline is
therefore **`D` bounded away from `1`** — equivalently, a single eigenvalue of
`C P C` bounded away from `0`. That is a far softer requirement than any
trace-class bound. This is the concrete architectural gain of replacing `b_0` by
`kappa_bal`.

### R.3 — The derived per-cell (`n = 1`) majorant

```text
  x(C, eps) := |C|_4 · g(eps),
  g(eps)    := G_tr(eps)  +  rho_res · G_hs(eps) · ( G_bl + G_hs(eps) ) .
```

Derivation (the normalization is the point): `||A_s||_2 <= ||A(0)||_2 +
||Delta||_2 <= |C|_4^{1/2}( G_bl + G_hs )` and `||Delta||_2 <= |C|_4^{1/2} G_hs`
by S2/S3, and `|tr[B X Y]| <= ||B||_op ||X||_2 ||Y||_2`, so the Carleman term
contributes `|C|_4 · rho_res · G_hs ( G_bl + G_hs )` and S1 contributes
`|C|_4 · G_tr`. The two `|C|_4^{1/2}` normalizations multiply to `|C|_4`
**exactly**: the quadratic Carleman structure recovers D5 action-density form on
its own. *This is the structural reason Hilbert-Schmidt replaces trace-norm, and
it is the answer to C2.*

Then, with `Z_hat_comp^{(C)}(a) = sum_{mu,lambda} c_{mu lambda} rho_{mu lambda}(a)`,
`c_{mu lambda} := w_mu^* w_lambda det(1+A_{mu lambda}(0)) / N(0)`,
`sum c_{mu lambda} = 1`, `sum |c_{mu lambda}| = kappa_bal`:

```text
  |Log rho_{mu lambda}(a)| <= x(C, eps)                                 (R.3.a)
  |rho_{mu lambda}(a) - 1| <= x e^{x}                                   (|e^z - 1| <= |z| e^{|z|})
  |Phi_C(a)| = |Log Z_hat_comp^{(C)}(a)|
             <= kappa_bal · x e^{x} / ( 1 - kappa_bal · x e^{x} )       (R.3.b)
                provided kappa_bal · x e^{x} < 1.
```

### R.4 — The cell-scale normalization hazard, named and NOT absorbed

`x(C, eps) = |C|_4 g(eps)` carries the cell 4-volume. To convert (R.3.b) into a
D5' bound `|Phi_C| <= |C|_4 · eta_1` with `eta_1` in the permitted tuple, the
nonlinear factors must be dominated by a bound `X_*(eps)` on `x`. **A bound on
`x` obtained from the coarsest admitted cell's 4-volume would be a cellulation
datum, which the parent's spec-header scoping clause 1 forbids.** This is stated
as an obligation, not resolved by fiat:

```text
R-L0  CELL-SCALE NORMALIZATION OBLIGATION (load-bearing; new).
      Exhibit X_*(eps), a functional of (||b_D||, tau_R, sea-kernel decay data,
      |w_lambda|) ONLY, with
            x(C, eps) = |C|_4 g(eps)  <=  X_*(eps)
      for EVERY admitted cell of the D3 quantifier, and with
            kappa_bal · X_*(eps) · e^{X_*(eps)}  <=  1/2   certified
      (the 1/2 is the parent's frozen E1 threshold, re-used; NO new number is
      introduced). Two admissible grounds, and only these:
        (i)  SCALE COVARIANCE: g(eps) carries a compensating negative scaling
             weight, so |C|_4 g(eps) is scale-invariant. Available inputs: the
             sealed construction is scale-covariant per cell and
             ||b_D^{(c)}||_inf = 1 EXACTLY for every admitted cell (Phase-1
             draft §3.1, 679ba036…); v(t) is scale-covariant; the diamond
             profile is scale-free.
        (ii) UNIFORM SMALLNESS: the raw un-normalized quantities are uniformly
             small in the cell scale.
      THE HAZARD, STATED: tau_R is scale-INVARIANT — every refined cell inserts
      a FULL record cycle at every refinement depth, with exact phase
      e^{+-i pi} = -1. This is precisely the structural fact recorded as the
      root of the O7 obstruction (3c81647e…) and, per memo §1.6 Step 4, the same
      fact that saturates the C4 cancellation. It is therefore NOT a priori
      true that g(eps) carries the compensating weight, and R-L0 may fail.
      NAMED WITNESS on failure: E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED.
      FORBIDDEN: substituting any coarsest-cell 4-volume, any V_max, any
      skeleton datum, or any cellulation-family quantity for X_*. Doing so is a
      spec-header scoping violation and BLOCKS with witness
      SCOPING_CLAUSE_1_VIOLATION_CELLULATION_DATUM.
```

Recorded honestly: **R-L0 is not an artefact of this architecture. Scale-uniformity
of `|Phi_C| / |C|_4` across the admitted scales IS the content of E1** — it is,
in the memo §3(iii) framing, the Route-Q encoding of refinement uniformity. Any
activity architecture whatsoever must deliver it. The replacement's contribution
is to make it a single named lemma with a checkable sufficient condition,
instead of a divergent trace norm.

With R-L0 discharged:

```text
  eta_1(eps) := 2 · kappa_bal · g(eps) · e^{X_*(eps)}
  [from (R.3.b) with kappa_bal x e^x <= 1/2, so the denominator is >= 1/2]
```

### R.5 — Frozen supersession of the Phase-1 functional

```text
The functional  eta(eps) = (2 eps ||b_D||_inf K_sea / b_0) · exp(1 + T_R
+ 2 eps ||b_D||_inf K_sea)  and its constants (K_sea, T_R, b_0) are RETIRED,
not certified. They may appear in this program's artifacts in exactly two
places: the historical record of C2, and negative control NC10. Any bound,
enclosure, tolerance, or verdict that consumes them is a spec violation and
BLOCKS with witness PHASE1_KSEA_ARCHITECTURE_REANIMATED.
```

---

## §E1' — Frozen derivation rule for the tolerances (outcome-blind)

Frozen before any verdict-bearing computation and sealed in a pre-execution
constants file. **No new number is invented: every threshold is inherited from
the parent's E1 step 2 (`1/2`) or derived from it (`Gamma_star <= 1`).**

```text
1. Derive G_tr(eps), G_hs(eps), G_bl, rho_res, kappa_bal, X_*(eps) as explicit
   functionals of (||b_D||, tau_R, sea-kernel decay data, |w_lambda|), each with
   a certified outward enclosure (§N). Assemble g(eps), eta_1(eps) per R.3-R.4
   and eta_{>=2}(eps) per the route taken (§O.A for IR-A).

2. On the parent's frozen dyadic grid  eps in { 2^-k : k = 1, ..., 64 }:
       eps_*^{(1)}    := max { 2^-k : eta_1(2^-k)      <= 1/2  certified }
       eps_*^{(>=2)}  := max { 2^-k : eta_{>=2}(2^-k)  <= 1/2  certified }
       eps_*          := min ( eps_*^{(1)} , eps_*^{(>=2)} )
   The threshold 1/2 is the parent's, frozen, outcome-blind, on BOTH legs.

3. Certify the aggregate KP margin supplied to I3:
       Gamma_star^split := eta_1(eps_*) + eta_{>=2}(eps_*)^2 / (1 - eta_{>=2}(eps_*))
                        <= 1   certified.

4. If no grid point qualifies on a leg, the verdict on that leg is
   E1S_BLOCKED with witness EPSILON_STAR_VACUOUS_{n1 | nge2}. This is DISTINCT
   from "the enclosure could not be certified at all", which is
   E1S_BLOCKED with the route's own named witness. The distinction is
   mandatory in the artifact (it is the distinction Phase-1 correctly drew).

5. eps_*, eta_1, eta_{>=2}, Gamma_star^split may NEVER be tuned by any output —
   not by W1, not by any control, not by the sharp-vs-smooth diagnostic NC9, not
   by any downstream consumer. Re-derivation requires a further append-only
   successor spec citing this rule.

6. The named witness ETA_MONOTONICITY_UNCERTIFIED (parent M-8) is carried: the
   step-2 grid max is well-posed regardless of monotonicity, and monotonicity is
   REPORTED, never assumed.

7. No measured constant appears anywhere in this spec, its constants file, or
   any admissible execution artifact. Permitted exact quantities: rationals,
   pi, sqrt(2), zeta values, e — all with certified outward enclosures.
```

---

## §O — Obligations (numbered; the three routes)

Inherited and unchanged from the parent, cited not re-proved: **O1 (Lemma 0)**,
**O2 (action-density construction and the re-aggregation identity over the full
common-refinement poset per M-4)**, **R1-R4** — all discharged at Phase-1 level
(`679ba036…`). This spec consumes them.

**M1's two compliance citations are mandatory verbatim in every result artifact
of this program**: gate item 9 (`4e1282bc…`, determinant fence satisfied
TERMWISE — each single term IS the corresponding second-quantized Gaussian
operator, and no single postselected determinant replaces the sum) and the A4
obligation relocation (`8a7f52ff…`), the latter carrying the **M-11 disclaimer
verbatim** (A4(3) erratum PROPOSED/unsealed; the freeze note governs).

### O.R — Architecture obligations (common to all three routes; must close first)

```text
R-L0  Cell-scale normalization. AS STATED IN R.4. Witness on failure:
      E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED.
R-L1  Block-triangular reduction det(1 + C(V-1)) = det_{ran C}(C V C), proved
      on the sealed forms, for the adjoint-continued pair (M-2). Witness:
      SCAD_BLOCK_TRIANGULAR_REDUCTION_UNCERTIFIED.
R-L2  Certified Carleman machinery: (i) det(1+A) = det_2(1+A) e^{tr A};
      (ii) |det_2(1+A)| <= exp(||A||_2^2 / 2); (iii) the differentiated
      identity d/ds Log det_2(1+A_s) = tr[ ((1+A_s)^{-1} - 1) Delta ], with the
      trace-of-product estimate |tr[B X Y]| <= ||B||_op ||X||_2 ||Y||_2. Every
      constant explicit and outward-enclosed; NO textbook constant is admitted
      by citation alone. Witness: SCAD_CARLEMAN_CONSTANTS_UNCERTIFIED.
R-L3  Colour-sum handling: the c_{mu lambda} decomposition, sum c = 1,
      sum |c| = kappa_bal, and the (R.3.b) closure. Must include the explicit
      refutation of applying the memo's per-pair IR-B display to the summed
      object. Witness: SCAD_COLOR_SUM_LOG_MISUSE.
R-L4  Baseline collapse (R.2): N(0) = (1-D)/2 exactly; D^2 <= exp(-2 ||[C,P]||_2^2);
      the exact commutator identity; hence kappa_bal = 1 iff D = 0. Reported
      per-state. MUST be reported as a CANDIDATE partial statement about the
      single-cell baseline, never as a discharge of H-B.
      Witness: E1_BASELINE_COLLAPSE_UNCERTIFIED.
R-L5  Named-hypothesis register: H-B (parent, undischarged), H-B0 (= kappa_bal
      finite), H-R (= rho_res finite), H-IND (discharged by Lemma 0). Every
      artifact carries the register with its discharge status. Silently
      assigning a value to an undischarged hypothesis BLOCKS (F'-3).
```

### O.A — ROUTE IR-A: split the D5 threshold; certify `n >= 2` outright

*Ranked FIRST for expected certification yield (memo §2 IR-C closing paragraph).*

```text
A-L1  CROSS-CELL HILBERT-SCHMIDT BOUND. Certify, in outward-enclosed exact
      rational arithmetic from the closed-form kernel of C1:
          || 1_{D_1} C 1_{D_2} ||_2^2   <=   |D_1| |D_2| / ( pi^4 R^6 ),
          R = dist(D_1, D_2).
      *** DRAFTING-LANE CORRECTION TO THE MEMO, MANDATORY. *** The memo's A-L1
      states the constant 1/(4 pi^4 R^6). Verified this session: with
      C_off(r) = -i alpha·r/(2 pi^2 |r|^4) and (alpha·r)^2 = |r|^2 I_4,
          ||C_off(r)||_op  = 1/(2 pi^2 |r|^3)          [the C1 "modulus"]
          ||C_off(r)||_F^2 = 4|r|^2/(4 pi^4 |r|^8) = 1 / ( pi^4 |r|^6 )
      i.e. the FULL SPINOR TRACE carries a factor 4 relative to the
      per-component reading. THIS SPEC FREEZES THE CONSERVATIVE (LARGER)
      CONSTANT 1/(pi^4 R^6). The execution must PROVE which convention the
      Hilbert-Schmidt pairing requires; using the smaller constant without that
      proof BLOCKS with witness A_L1_SPINOR_TRACE_CONVENTION_UNPINNED.
      SECOND UNPINNED ITEM: |D_1|, |D_2| must be pinned as EQUAL-TIME spatial
      volumes or SPACETIME 4-volumes, consistently with the two-time reading
      demanded by C6 and S2. Witness if unpinned:
      A_L1_VOLUME_TYPING_UNPINNED.

A-L2  LIGHT-CONE / NULL-SEPARATION CONTROL. *** THE SUBSTANTIVE NEW LEMMA. ***
      The C5 shell counting assumes decay uniform in direction; cells at NULL
      separation get no help from an equal-time bound. Certify that the diamond
      profile b_D = exp(16 - 1/s) — Gevrey, vanishing to all orders at the
      diamond boundary and at both tips, ||b_D||_inf = 1 exactly — TOGETHER WITH
      the cell time integration restores decay along the light cone, uniformly
      over the D3 quantifier and over the envelope-profile class.
      Witness on failure: E1_NULL_SEPARATION_CLUSTER_DECAY_UNCERTIFIED.

A-L3  SPLIT KOTECKY-PREISS CLOSURE. Assemble the anchored n >= 2 sums with the
      EXACT shell identities and constants (all re-verified this session):
          3-D shell(k) = (2k+1)^3 - (2k-1)^3 = 24 k^2 + 2   [exact, k=1..59]
          4-D shell(k) = (2k+1)^4 - (2k-1)^4 = 64 k^3 + 16 k [exact, k=1..59]
          two-line 3-D:  sum_k (24k^2+2)/k^6 = 24 zeta(4) + 2 zeta(6)
                                             = 4 pi^4/15 + 2 pi^6/945
          two-line 4-D:  sum_k (64k^3+16k)/k^6 = 64 zeta(3) + 16 zeta(5)
      with exact-rational partial sums plus a CERTIFIED OUTWARD TAIL enclosure
      (no floating-point tail). Deliver eta_{>=2}(eps) in D5' form and close
      Gamma_star^split <= 1 per §E1' step 3. Note that zeta(3) and zeta(5) admit
      no closed form: the 4-D constant is certified by enclosure, not by a
      closed expression, and the artifact must say so.
      Witness on failure: D5_SINGLE_ETA_OVERBINDS_SPLIT_AMENDMENT_REQUIRED.

A-L4  REFINEMENT SCALING. Re-derive, exactly, the memo §1.5 scaling:
      shell count R^3 dR / L^4; pair activity s^2 L^8 / R^6; in-cell strength
      s <= eps ||b_D||_inf x (cell time extent) ~ eps L; int_L^inf R^-3 dR
      = 1/(2 L^2); total = eps^2 L^4 / 2 = |C|_4 · (eps^2 / 2). The resulting
      eta^2 is L-INDEPENDENT [verified this session]. This is the certified
      statement that D5 plus R1-R4 genuinely absorbs refinement on the n >= 2
      leg, and it must be stated as such and NOT extended to n = 1.

SEALED AUTHORITIES CITABLE: 3a6ff617 (exact C); 789338ad (A1/A2: b_D, M, v, J, S);
679ba036 (O1/O2, R1-R4, ||b_D||=1); 52401eef; 0df721a1; 451550c3; b786db3a;
plus M1's two mandatory compliance citations with the M-11 disclaimer.

NEW OBLIGATIONS CREATED: (i) the D5' split (a scoped supersession of D5, §S);
(ii) A-L2; (iii) the E1' grid rule restated over two constants; (iv) A-L1's two
unpinned conventions.

LIKELY FAILURE MODE: A-L2. If null-separated cell chains admit no certified
decay, the shell counting collapses and the n >= 2 sums are not certifiable
either — which moves the whole obligation back into D7'(a).

ADOPTION FLAG: none.
```

### O.B — ROUTE IR-B: subtracted / regularized determinant (discharge `n = 1`)

```text
B-L1  TRACE PART. Certify tr[ C ( V(a) - V(0) ) C ] finite (S1), using BOTH
      exact cancellations: the odd spinor trace tr_spinor[C(p) alpha_x]
      = -2 p-hat_x (so the leading sea-depth divergence integrates to zero over
      d^3 p) AND |m_0|^2 = 0 (the bare a-linear tadpole annihilated; C4, exact,
      re-verified). Both are derivable from sealed data.
      Witness: E1_SUBLEADING_SEA_TRACE_LOG_UNCERTIFIED.

B-L2  HILBERT-SCHMIDT PART. *** THE WALL. *** Certify a CARRIER-UNIFORM bound
      on the off-diagonal HS norm of the cell evolution — the cell-level
      Shale-Stinespring condition (S2, S3), stated as a TWO-TIME (cell-S-matrix)
      object. The equal-time version is FALSE (C6), and the artifact must say so
      rather than route around it.
        - a-VERTEX SECTOR: plausible. J = -(Q b_D Q) ⊗ alpha_x with b_D Gevrey,
          all derivatives vanishing at boundary and tips, so its spacetime
          transform decays faster than any power and the sea-depth integral
          converges absolutely.
        - RECORD VERTEX: M(t) = Q 1_{|x| <= r(t)} Q is a SHARP indicator, and by
          C4 the pointer weights put it in the LEAD at full tau_R
          (sum_lambda w_lambda u_lambda = P exactly).
      Witness on failure: E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER.

B-L3  MOEBIUS / CONNECTED TRUNCATION removes the remaining cell-local residue
      for |gamma| >= 2 (available from O2; cite, do not re-prove).

SEALED AUTHORITIES CITABLE: as IR-A, plus 6f6b822a (CTP propagator typing).

LIKELY FAILURE MODE: B-L2 fails on the record vertex, and the only repair is to
soften M(t) — a SEALED PHASE-A A1 DEFINITION. NOT A LANE'S TO CHANGE.
ADOPTION FLAG: none, and F-1 is NOT triggered (no clustering axiom). But this
failure mode is Axis-3-adjacent and routes DIRECTLY to §Q2-STOP.
```

### O.C — ROUTE IR-C: exact record-cycle collapse (decide it either way)

*Ranked FIRST for expected information yield (memo §2 IR-C; it is the only route
that returns information under failure).*

```text
C-L1  EXACT RECORD-PHASE COLLAPSE, PROMOTED FROM PHASE TO OPERATOR. At
      lambda tau_R in {0, +pi, -pi} (exact: int_0^1 v = tau_R = pi/sqrt2, S^2 = I),
      exp(-i lambda tau_R S) = (+1,-1,-1)·I exactly on the ball where M(t) = 1.
      Already an exact Phase-2 witness (08b91543…); extend from scalar phase to
      operator statement u_lambda = (I - P) + phase_lambda · P + [error].
      Witness: E1_RECORD_PHASE_OPERATOR_COLLAPSE_UNCERTIFIED.

C-L2  COMMUTATOR ERROR. *** THE WHOLE CONTENT OF THIS ROUTE. *** Control
      [ h_0 , M(t) ⊗ S ] in a QUADRATIC-FORM / Besov-type norm. Norm-based
      control is unavailable: ||[h_0, 1_B]|| = +infinity for a first-order h_0
      against a sharp indicator. The certification must therefore be in a
      quadratic form, and the artifact must state which form and why the
      operator-norm route is excluded.
      Witness: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED.

C-L3  SHARP-KINK LOG COEFFICIENT. Evaluate EXACTLY the coefficient of the
      logarithm in Z_comp^{(C)} read as the determinant of a sharp gauge kink on
      the Dirac sea, as a functional of the frozen tuple. Reference scale
      derived, not measured: the marginal kernel's absolute integral has
      coefficient EXACTLY 2/pi (C1).
      - If the coefficient is certified ZERO: E1 is certified at the sharp
        localizer and the n = 1 leg closes.
      - If the coefficient is certified NONZERO: E1 is UPGRADED from
        "uncertified" to CERTIFIED DIVERGENT AT THE SEALED DEFINITION OF M(t) —
        a refutation of the current activity architecture WITH AN EXACT WITNESS.
        Witness: E1_RECORD_KINK_LOG_COEFFICIENT_NONZERO_CERTIFIED.
        *** THIS OUTCOME TRIGGERS §Q2-STOP IMMEDIATELY. ***

C-L4  BASELINE COLLAPSE (= R-L4, owned here because C-L1 supplies its input).
      Certify N(0) = (1-D)/2, D^2 <= exp(-2 ||[C,P]||_2^2), and the resulting
      kappa_bal. Report per-state. Never as a discharge of H-B.

C-L5  MULTI-CELL BASELINE EXTENSION. The multi-cell baseline is
      det(1 + C(prod_c V_c - 1)C) summed over colour TUPLES, not a product of
      per-cell determinants; the per-cell sharp projectors P_c live at distinct
      times and do not commute with the h_0 evolution. State the extension as
      an obligation and, if it cannot be certified, scope C-L4 explicitly to the
      single cell. Witness: E1_MULTICELL_BASELINE_EXTENSION_UNCERTIFIED.

LIKELY FAILURE MODE: C-L2 defeats norm-based control. But this route returns
information under failure — see the ranking in §P.
ADOPTION FLAG: none. A certified-divergent outcome forces the SAME
definition-level escalation as IR-B's failure mode, via §Q2-STOP.
```

### O.NC — Negative controls (all mandatory; any misbehaviour BLOCKS)

Parent NC1-NC7 are carried where applicable; NC8-NC10 are added by this spec.

```text
NC1  GHZ REFUSAL WITH THE NAMED DISCRIMINATING HYPOTHESIS. CARRIED VERBATIM.
     Preparation (|0...0> + |1...1>)/sqrt2, Z_N(A) = cos(N tau_R A), first zero
     pi/(2 N tau_R), fed to the pipeline. PASS: the pipeline REFUSES, naming
     H-IND (per-cell independent record colours; GHZ is the
     perfectly-correlated-colour limit). Numeric failure, silent acceptance, or
     refusal on any other ground = control failure.

NC2  CORRELATED-COLOUR VARIANT. CARRIED VERBATIM. Replace the per-cell
     independent colour sums by a single shared colour across cells; the
     composition identity must DETECTABLY fail (nonzero cross-cell
     record-colour correlation witness, certified outward enclosure excluding
     zero). Silence = control failure.

NC3  t^-3 INSUFFICIENCY. CARRIED VERBATIM, WITH C3's READING FROZEN. An
     activity bound built ONLY from the sealed one-root temporal-return t^-3
     results must FAIL the §E1' criterion, with a named witness. THE ARTIFACT
     MUST STATE that this PASS says nothing about E1 (C3): it is a one-line
     counting artifact and does NOT corroborate E1 from below.

NC4  PERIODIC POSITIVE REGRESSION. CARRIED AS A NAMED NON-APPLICABILITY, NOT
     DROPPED. Per the majorant-arm re-scope (209aa390…), the DERIVED arm
     requires NO Route-T artifact: not TT1, not TT2, not TT3, not O7. This spec
     builds no anchored transfer operator, so NC4's target machinery is absent.
     THE CONTROL IS CONVERTED INTO A CHECKABLE FENCE: the evaluator greps every
     artifact of this program for citations of the anchored transfer operator,
     the spectral gap, TT1/TT2/TT3, the refinement intertwiner, or the periodic
     promotion results, and FAILS the gate on any hit that is load-bearing for a
     bound, a constant, or a verdict. Historical citation in the record section
     is permitted. Witness: ROUTE_T_ARTIFACT_CONSUMED_IN_ROUTE_Q_PREDICATE.

NC5  DETUNED RELAY: RECORD-ERASING ADVANCE. CARRIED WITH PARENT M-5's EXACT
     WITNESS (the imprecise "toward" clause is void). Replace the D2 relay by
     the record-erasing advance (ready root supplied, completed record erased).
     The witness is EXACT: weight-sum restoration m_0' = sum_lambda p_lambda = 1
     (versus the completed chain's exact 0) AND/OR diagonal-unitality
     restoration R_erased(a,a) = I, each certified exactly. The pipeline must
     detect and REFUSE.

NC6  REFINEMENT-STABILITY EXECUTABLE CONTROL. CARRIED VERBATIM. A deliberately
     NON-action-density activity assignment (per-cell constant activity) must
     FAIL uniformity under one common refinement of a family-A and a family-B
     member, exhibited by certified enclosure.

NC7  NO-TARGET FENCE. CARRIED VERBATIM. No kappa_record value, no alpha, no
     function of either, no target-adjacent numeric in any artifact. The
     evaluator runs the battery's T15-style transform-grep over every artifact
     and FAILS the gate on any hit. This lane's permitted numerical outputs are
     EXACTLY: eps_*, eta_1, eta_{>=2}, Gamma_star^split, the SCAD constants
     (G_tr, G_hs, G_bl, rho_res, kappa_bal, X_*), the A-L1/A-L3 geometric and
     zeta constants, the control witnesses, the C-L3 log coefficient, and W1.

NC8  *** NEW: ONE-LINE VS TWO-LINE EXECUTABLE DISTINCTION. *** MANDATORY.
     A one-line-only activity bound MUST FAIL, and must fail by REPRODUCING
     NC3's divergence EXACTLY:
         3-D one-line anchored sum  sum_{k=1}^{K} (24k^2+2)/k^3
             =  24 H_K  +  2 sum_{k<=K} k^-3   ->  24 H_K + 2 zeta(3),
             divergent as 24 log K   [re-verified this session: at K = 1000,
             S(K) - 24 H_K = 2.40411…, converging on 2 zeta(3) = 2.40411…;
             24 H_K = 179.651…]
         4-D one-line anchored sum  sum (64k^3+16k)/k^3  ->  64 K + …,
             divergent LINEARLY.
     and the TWO-LINE bound MUST PASS with the exact constants of A-L3.
     PASS CONDITION: the one-line leg FAILS with the reproduced 24 H_K, the
     two-line leg PASSES, both in exact rational partial sums with certified
     outward tails. A one-line leg that passes, or a two-line leg that fails, is
     a control failure. THIS CONTROL IS WHAT MAKES C3 EXECUTABLE.
     Witness: ONE_LINE_TWO_LINE_DISTINCTION_NOT_EXECUTABLE.

NC9  *** NEW: SHARP-VS-SMOOTH LOCALIZER DIAGNOSTIC — REPORTED, NOT ADOPTED. ***
     MANDATORY. Recompute the n = 1 diagnostic quantities (S2, S3, the
     commutator ||[C,P]||_2, and — if C-L3 reaches it — the log coefficient)
     with a MOLLIFIED localizer in place of 1_{|x| <= r(t)}, and REPORT the
     difference. The mollifier must be named, hashed, and frozen before the
     computation, and must be a one-parameter family reducing to the sharp
     indicator.
     *** THE SMOOTHED VALUE IS DIAGNOSTIC ONLY. *** It may NOT enter any bound,
     any constant, any enclosure, any tolerance, any verdict, or any I3 supply.
     Its sole permitted use is to quantify the COST side of §Q2-STOP option (ii)
     for the principal. An artifact that uses a smoothed-localizer value in a
     certified bound, or that reports a verdict computed against a smoothed
     localizer, is a spec violation and BLOCKS with witness
     SMOOTHED_LOCALIZER_ADOPTED_WITHOUT_PRINCIPAL_DECISION.
     Recorded honestly for the diagnostic's interpretation: C6 notes that even a
     LIPSCHITZ cutoff gives int d^3 r · r^2/r^6 = int dr/r^2, divergent — so the
     diagnostic may show that mild smoothing does NOT rescue B-L2, and the
     report must state the mollifier's smoothness class explicitly.

NC10 *** NEW: SUBTRACTION IS LOAD-BEARING. *** MANDATORY. Run the SCAD
     architecture with the a-subtraction REMOVED (i.e. norm the unsubtracted
     A(a) rather than Delta(a)). It MUST reproduce C2's divergence: the
     trace-norm leg must diverge as a power (~ |D_t| Lambda^3) in the
     carrier-blind reading, and be finite only on a fixed carrier. PASS: the
     unsubtracted variant DIVERGES and the pipeline REFUSES it by naming
     PHASE1_KSEA_ARCHITECTURE_REANIMATED. An unsubtracted variant that appears
     to certify is a control failure and indicates a carrier index has been
     smuggled in. Witness: SUBTRACTION_NOT_LOAD_BEARING.
```

### O.W — Preregistered sharpening witness W1 (computed under every verdict arm)

Carried from the parent O9 as repaired by **M-1** and **M-9**:

```text
W1 := -Log Z_hat_comp^{(12)}(a) + Log Z_hat_comp^{(1)}(a) + Log Z_hat_comp^{(2)}(a),
      Z_hat_comp^{(12)} = the TWO-CELL relayed member (cells 1 and 2 of the
      relayed exhaustion, relay-ordered), per pinned state, superscript = cell
      set (M-9);
COMPARATOR (M-1): the certified spanning-tail bound  |C|_4 · eta^2 / (1 - eta)
      evaluated at the frozen certified eta — W1 sums ALL spanning anchored
      clusters n >= 2. UNDER THE D5' SPLIT the comparator reads
          |C|_4 · eta_{>=2}^2 / ( 1 - eta_{>=2} )
      at the frozen certified eta_{>=2}. Inside SHARPENS; outside REFUTES —
      against THAT bound.
HISTORY (M-9): the frozen pair (7/100, -11/100) is used IFF eps_* >= 11/100
      under the M-2 pair-modulus reading; otherwise the fallback pair
      (eps_*, -eps_*) on the closed polydisc.
W1 is computed under EVERY verdict arm in which R-L1..R-L3 survive, so a route
failure FALSIFIES rather than merely returning inconclusive. W1 is not a target
quantity; it is permitted by NC7's explicit list.
```

---

## §Q2-STOP — ABSOLUTE FENCE 1: THE PRINCIPAL'S Q2 STOP RULE (NON-NEGOTIABLE)

```text
TRIGGER. If ANY route terminates in a CERTIFIED FINDING that the SHARP record
localizer M(t) is itself the obstruction — i.e. a certified-divergent E1 against
the sharp localizer — the gate STOPS AT THAT FINDING.

Certified findings that trigger this rule include, and are not limited to:
  - C-L3 returning a certified NONZERO sharp-kink log coefficient
    (E1_RECORD_KINK_LOG_COEFFICIENT_NONZERO_CERTIFIED);
  - B-L2 returning a certified FAILURE on the record vertex
    (E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER) where the failure is
    certified rather than merely uncertified;
  - any certified demonstration that no X_*(eps) exists for R-L0 BECAUSE of the
    sharp localizer;
  - any certified demonstration that the baseline or the numerator is
    unsatisfiable at the sealed D6' definitions.

ON TRIGGER, IN THIS ORDER, WITH NO OTHER ACTION PERMITTED:
  1. STOP. No further obligation is attempted. No route is switched to. No
     repair is attempted. No bound is re-derived. No tolerance is revisited.
  2. SEAL THE FINDING with its exact witness, its enclosures, and its lineage.
     Failures are preserved, never repaired.
  3. ESCALATE TO THE PRINCIPAL with BOTH options and their FULL COSTS stated,
     AND NO ARGUMENT FOR EITHER.

NO LANE MAY ANSWER Q2, PRE-EMPT IT, OR DESIGN AROUND IT SILENTLY. Selecting an
option, ranking the options, recommending one, implying one, continuing work
that presupposes one, or scoping a subsequent obligation so that only one option
remains available, is a spec violation and BLOCKS with witness
Q2_ANSWERED_BY_A_LANE.
```

### Q2 option (i) — SHARP LOCALIZER INVIOLABLE: certified-divergent E1 stands as a refutation of the current activity architecture

Full costs and consequences, stated for the principal's decision:

```text
- T7(iii) clause (2) has NO certified majorant on any route. The verdict is a
  named REFUTATION rather than a named uncertainty — strictly more information
  than the status quo, and strictly less than a theorem.
- The re-scoped DERIVED arm (M1 + M2-certified + M3-activated over the full D3
  quantifier) becomes UNSATISFIABLE at the sealed M(t). The re-scope decision's
  own stated contingency ("contingent on an E1 architecture that does not yet
  exist") resolves NEGATIVELY.
- Route T offers no fallback: O7 is already refuted as a MECHANISM (3c81647e…)
  and the O3 TT-certificates are blocked by ordering
  (TT2_E*_BLOCKED_BY_ORDERING_PHASE_A_BUNDLE_ABSENT). With Route Q refuted at
  E1, no route to clause (2) survives.
- Downstream, all of the following remain FALSE and acquire a refutation-grade
  rather than block-grade obstruction: connected_linked_cluster_density_proved,
  volume_uniform_zero_free_neighborhood_proved, T7(iv),
  Duhamel_intensive_Hessian_equality_proved, kappa_record_computed,
  alpha_computed, proof_authorized.
- The Duhamel consumer's I3 tuple V002 has NO SUPPLIER: no eps_*, no
  Gamma_star, no eta, hence no radius at which its (H1) per-K zero-freeness can
  be stated (D-1).
- WHAT IS *NOT* REFUTED, and must be stated so the cost is not overstated:
  H-B / the T7(i) amplitude normalization is NOT refuted. If R-L4/C-L4 certifies,
  the single-cell baseline is 1/2 exactly and kappa_bal = 1 exactly, and what is
  refuted is the ACTIVITY architecture, not the baseline. The sealed Phase-A A1
  definition of the record cell stands entirely unchanged, and EVERY sealed
  result depending on it is untouched.
- WHAT IS GAINED: an exact witness naming the MECHANISM (sharp record localizer
  versus marginal degree-(-3) sea) rather than the symptom. The program's true
  bottom is named, with a certified reason, and cannot be mistaken for an
  infrared clustering problem again.
```

### Q2 option (ii) — SMOOTHED-LOCALIZER SUCCESSOR: a scoped DEFINITION change to what a record cell is

Full costs and consequences, stated for the principal's decision:

```text
- IT CHANGES A SEALED PHASE-A A1 DEFINITION: M(t) = Q 1_{|x| <= r(t)} Q. This is
  a change to what a RECORD CELL IS, hence to what kappa_record measures, hence
  to the meaning of the program's target. It is Axis-3-adjacent.
- IT REQUIRES, per the principal's own framing: its own spec, its own hostile
  review, and an EXPLICIT STATEMENT OF EVERY SEALED RESULT IT TOUCHES. This
  drafting lane's reading of the corpus identifies the following as candidates
  that the escalation must enumerate and adjudicate — the list is offered as a
  starting point for that enumeration and is NOT claimed complete:
    * Phase-A spec A1/A2/A3/A4 (789338ad…) — M(t), v(t) = tau_R·32 r(t)^3,
      1_{D_t}, and every object built on them;
    * Phase-A execution binding (production gating);
    * the state-regulator restriction result (3a6ff617…) insofar as the
      localization C_n = Q_n C Q_n is used;
    * relay necessity (0df721a1…) and relayed-family resolution (52401eef…)
      insofar as cell closure at full tau_R uses M(t);
    * the tau_R = pi/sqrt(2) derivation (b786db3a…) — whether tau_R remains the
      UNIQUE controlled coupling once v(t) M(t) is smoothed;
    * the envelope-realization comparison spec (4059eff5…) and the entire
      ER-A/ER-B fork: v_A(t) = tau_R·32 r(t)^3 is tied to the sharp ball radius
      r(t) = min(t, 1-t);
    * the connected analytic closure result (f891d3af…) and its GHZ witness;
    * the L2 typing freeze (cdbacdaf…);
    * NC1's and NC5's exact witnesses;
    * the O7 obstruction record (3c81647e…): its exact witnesses
      (record-tier tensor powers (3/8)^k versus 3/8; every refined cell
      inserting an O(1) full record cycle at exact phase e^{+-i pi} = -1) depend
      on int_0^1 v = tau_R holding ON THE BALL WHERE M = 1;
    * the C1-C6 corrections themselves: C4's exact saturation and C6's
      confinement to n = 1 are both statements ABOUT the sharp localizer.
- SPECIFIC MATHEMATICAL COST: the exact record-phase collapse (+1, -1, -1) at
  lambda tau_R in {0, +-pi} is EXACT only where M(t) = 1. Smoothing creates a
  transition region in which the phase is not +-1. Therefore: C4's exactly
  saturated l1 step becomes an inequality OF UNKNOWN DIRECTION; the exact
  Phase-2 route-(b) witness becomes approximate; R-L4/C-L4's derivation of
  N(0) = (1-D)/2 (which uses phase_mu phase_lambda in {+1,-1} exactly) no longer
  applies as written; and the O7 record's exact witnesses become approximate.
  Exact arithmetic in Q(sqrt2) may no longer suffice.
- SPECIFIC DIAGNOSTIC COST, from C6: even a LIPSCHITZ cutoff gives
  int d^3 r · r^2 / r^6 = int dr / r^2, DIVERGENT. Mild smoothing may therefore
  fail to rescue B-L2 at all. The smoothness class required is itself unknown
  until NC9's diagnostic is read, and NC9 is diagnostic only.
- IT DOES NOT TRIGGER F-1 / F'-1: no clustering axiom is involved.
- POSSIBLE GAIN: if the smoothing class is sufficient, [C, M] becomes
  Hilbert-Schmidt, the cell-level Shale-Stinespring condition holds, B-L2
  closes, and E1 is certified — which would deliver clause (2) over the FULL D3
  quantifier via Route Q alone, per the re-scope.
- COST OF THE DECISION ITSELF: the front stops until the principal decides. No
  lane may proceed on either branch in the interim.
```

---

## §F' — ABSOLUTE FENCE 2 and the fence register

```text
F'-1  *** CLUSTERING-ADOPTION FENCE (ABSOLUTE; the principal's flag (a),
      standing REFUSAL, carried from parent F-1 and strengthened). ***
      If any route, lemma, obligation, control, repair, enclosure, or artifact
      of this program reaches for BUZZERED/BUFFERED_EXHAUSTION_CLUSTERING or ANY
      clustering axiom or clustering principle whatsoever, the gate BLOCKS
      IMMEDIATELY and escalates to the principal. Adoption is principal-only.
      No lane may adopt to rescue any route.
      RECORDED FOR THE REVIEWER: the memo establishes that NONE of IR-A, IR-B,
      IR-C needs one — the long-distance direction has margin (C5), and
      clustering was never the missing ingredient. A reach for a clustering
      axiom in this program is therefore not merely forbidden, it is evidence
      that the reaching obligation has been mis-derived, and the escalation must
      say so. Witness: CLUSTERING_ADOPTION_REACHED.

F'-2  Q2 STOP RULE per §Q2-STOP. Absolute.

F'-3  NAMED HYPOTHESES MAY NOT BE SILENTLY VALUED. H-B, H-B0 (kappa_bal), H-R
      (rho_res) are undischarged. Assigning any of them a numeric value, a
      default (including 1), or an "assume for now" placeholder inside a
      certified bound BLOCKS with witness
      NAMED_HYPOTHESIS_SILENTLY_DISCHARGED. They may be carried as DISCLOSED
      PREMISES on a conditional statement, clearly marked, and only that.

F'-4  eps_*, eta_1, eta_{>=2}, Gamma_star^split per §E1' ONLY; never tuned by
      any output (not by W1, not by NC9, not by any control, not by any
      consumer). No measured constants anywhere (no 137.03…, no 0.00729…, no
      CODATA, no experimental input of any kind). Frozen Numerics culture binds
      (§N). Inherited from parent F-4.

F'-5  SPEC-HEADER SCOPING CLAUSE 1 BINDS VERBATIM. Every constant, radius,
      decay rate, and tolerance is an explicit functional of
      (||b_D||, tau_R, sea-kernel decay data, |w_lambda|) ONLY. No Hermite
      carrier index n, no ell, no truncation level, no cellulation-family index,
      AND NO CELLULATION GEOMETRIC DATUM (see R-L0). Violation BLOCKS.

F'-6  ENVELOPE-PROFILE CLASS. The statement is over the sealed class containing
      both branches (v_A(t) = tau_R·32 r(t)^3; v_B = 24 tau_R/pi). ER-A is
      carried as a DISCLOSED PREMISE on any downstream consumer; this program
      neither selects nor closes the ER fork.

F'-7  L2 FREEZE FENCES 1-5 BIND VERBATIM (parent F-3, extended by M-6):
      exhaustive zero-free results never cited against completed-chain
      obligations; the completed Hessian never checked against the exhaustive
      three-way agreement; no unitality-anchored wording inherited (the D1
      restatement — m_0 = 0, T7(i) anchor, named H-B — governs); fence 4's L-ADD
      obligation is owned by the Duhamel spec and recorded here only.

F'-8  COMPANION-OBJECT FENCE (parent F-6). The exhaustive closure
      Z_K = omega_in(W_-^dagger W_+) is COMPANION ONLY; it appears here solely
      inside NC1/NC2/NC5. Substitution between completed and exhaustive objects,
      IN EITHER DIRECTION, blocks.

F'-9  NO-TARGET FENCE per NC7, with the evaluator transform-grep. VERDICT
      AUTHORITY RESTS WITH THE HASHED EVALUATOR ALONE — no in-execution PASS
      string has authority (parent F-5).

F'-10 DEPENDENCY / INTERFACE. CONFIRMED, per the memo §5 and parent F-7: this
      program needs NO Phase-A/B production output; every input is a sealed
      DEFINITION. Two caveats carried verbatim: (i) the absence of the Phase-A
      bundle costs this work NOTHING and could not help it — any bound evaluated
      on the n = 2, ell in {1, sqrt2} carriers is carrier-indexed and forbidden
      by F'-5; the TT2_E*_BLOCKED_BY_ORDERING_PHASE_A_BUNDLE_ABSENT blocks
      belong to O3 (Route T), a DIFFERENT obligation, and must not be conflated
      with E1; (ii) F-7's demotion clause DOES NOT APPLY — this work is a
      statement about the abstract sealed form by construction, so there is
      nothing to demote.

F'-11 NO PRODUCTION-LANE INVOCATION. This program invokes no production lane and
      writes no production artifact. Sealed and production files are read-only
      to every lane of this program.

F'-12 F-8 IS THE GATE, PER THE PRINCIPAL'S RE-SCOPE. Fresh-context execution
      from the sealed text; hostile pre-execution review of this spec; and an
      INDEPENDENT fresh lane re-derives the SCAD constants, the A-L3 enclosures,
      the R-L4 baseline collapse, and the W1 enclosure from the sealed text
      WITHOUT the primary's worksheets, before anything seals. The re-scope
      decision's accompaniment (b) is explicit that the Route-Q independence
      finding currently rests on DRAFT artifacts authored by the claiming lane;
      F-8 discharges exactly that exposure. Failures preserved, never repaired.

F'-13 NO ROUTE-T ARTIFACT IN THE PREDICATE, per NC4-as-fence and the re-scope.

F'-14 THE MEMO IS A ROUTE MEMO, NOT AN AUTHORITY FOR ITS OWN CONSTANTS. Where
      this spec records a drafting-lane correction to the memo (A-L1's spinor
      factor; the IR-B per-pair-versus-summed display), the CORRECTED form
      governs, and the execution must certify the convention rather than inherit
      it. Witness: MEMO_CONSTANT_INHERITED_WITHOUT_CERTIFICATION.
```

---

## §N — Frozen Numerics (implementation, binding)

```text
- Exact arithmetic decides: exact rationals (Fractions), exact symbolic
  arithmetic, and exact Q(sqrt2) for the record-colour algebra. Certified
  OUTWARD interval enclosures over exact rationals for every transcendental
  bound (pi, e, exp, log, zeta values). FLOATS MAY INFORM ONLY and may never
  appear in a verdict-bearing enclosure, a threshold comparison, or a control
  pass/fail decision.
- Pinned runtime:
  /Users/bgm/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/bin/python3
  No scipy. No numpy dependency for any verdict-bearing quantity. Any matrix
  exponential required (e.g. for a finite diagnostic of the cell propagator)
  must be a SELF-WRITTEN scaling-and-squaring implementation carrying a
  CERTIFIED OUTWARD REMAINDER ENCLOSURE; a float expm is diagnostic only and
  must be labelled as such.
- Every implementation is named and content-hashed at sealing; the evaluator is
  hashed and holds sole verdict authority (F'-9).
- Tail enclosures: every infinite sum (A-L3's zeta sums, NC8's partial sums) is
  an exact rational partial sum PLUS a certified outward tail bound. No
  extrapolated, fitted, or floating tail is admissible.
- The zeta-value dependence is stated honestly: 24 zeta(4) + 2 zeta(6) has the
  closed form 4 pi^4/15 + 2 pi^6/945; 64 zeta(3) + 16 zeta(5) does NOT admit a
  closed form and is certified by enclosure only. The artifact must say so.
```

---

## §V — Predeclared verdicts (frozen; not revisable)

```text
E1S_REPLACEMENT_ARCHITECTURE_CERTIFIED
    iff R-L0..R-L5 discharge; at least one of {IR-A closing n>=2 AND a route
    closing n=1, IR-B closing n=1, IR-C closing n=1 with a certified ZERO log
    coefficient} closes; eta_1 and eta_{>=2} certified at the §E1' thresholds
    with Gamma_star^split <= 1; ALL controls NC1-NC10 behave; W1 computed and
    inside its M-1/D5' comparator; §Q2-STOP not triggered
    => E1 supplied into the parent's O4-M2, per state, under the named
       hypotheses H-B, H-B0, H-R as disclosed premises; the re-scoped DERIVED
       arm's E1 dependency is met, subject to F'-12.

E1S_N_GE_2_CERTIFIED_BLOCK_ISOLATED_TO_N1
    iff IR-A's A-L1..A-L4 close with eta_{>=2} certified, but the n = 1 leg
    does not close and §Q2-STOP is not triggered
    => the anchored n >= 2 sums stand CERTIFIED with the exact constants; the
       entire obligation is NAMED at n = 1; the residue is
       E1_SEA_KERNEL_ACTION_DENSITY_UNCERTIFIED restricted to n = 1, PLUS the
       added mechanism witness E1_SHARP_RECORD_LOCALIZER_VS_MARGINAL_SEA.
       eta_1 remains REQUIRED (D5' clause); nothing is released.

E1S_CERTIFIED_DIVERGENT_AT_SHARP_LOCALIZER
    on any §Q2-STOP trigger
    => STOP, SEAL, ESCALATE per §Q2-STOP with both options and full costs and
       NO ARGUMENT FOR EITHER. This verdict is NOT a block and NOT a derivation;
       it is a REFUTATION with an exact witness, and it terminates the program
       pending the principal's decision.

E1S_BLOCKED
    on any other obligation failure, any control misbehaviour, any authority
    drift, EPSILON_STAR_VACUOUS_{n1|nge2}, R-L0 failure, or any spec violation
    named in §F' — ALWAYS with a named witness. Failures preserved, never
    repaired.

E1S_CLUSTERING_REACH_BLOCK
    on any F'-1 trigger => immediate block and escalation, with the note that
    the reach is itself evidence of a mis-derivation (F'-1).
```

---

## §G — Graceful-block witness table (from the memo §4, verbatim in substance, extended)

```text
| route fails                        | named witness the program carries                    | honest partial result that survives                                                                                                                                             |
|------------------------------------|------------------------------------------------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| IR-A (light-cone lemma A-L2)       | E1_NULL_SEPARATION_CLUSTER_DECAY_UNCERTIFIED         | Spacelike-separated anchored n >= 2 sums certified with exact constants 24 zeta(4)+2 zeta(6) (3-D) / 64 zeta(3)+16 zeta(5) (4-D); the obligation narrows to null-separated cell chains. |
| IR-A (split rejected)              | D5_SINGLE_ETA_OVERBINDS_SPLIT_AMENDMENT_REQUIRED     | The entire block localizes to n = 1; n >= 2 certified.                                                                                                                            |
| IR-B (record vertex)               | E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER         | The a-VERTEX SECTOR (smooth Gevrey b_D) is certified carrier-uniformly; the block is confined to the sharp M(t) of A1 — a DEFINITION-level, not a clustering-level, obstruction.  |
| IR-B (trace part)                  | E1_SUBLEADING_SEA_TRACE_LOG_UNCERTIFIED              | The leading sea-depth divergence certified to cancel exactly (odd spinor trace -2 p-hat_x; |m_0|^2 = 0); only the subleading log remains.                                        |
| IR-C (commutator)                  | E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED           | Exact record-phase collapse certified; error term isolated in ONE named commutator.                                                                                              |
| IR-C (coefficient nonzero)         | E1_RECORD_KINK_LOG_COEFFICIENT_NONZERO_CERTIFIED     | A REFUTATION, NOT A BLOCK: E1 is unsatisfiable at the sealed M(t). Escalates to the principal as a definition decision (sharp vs smoothed record localizer) via §Q2-STOP. Strictly better than the status quo. |
| all fail                           | retain E1_SEA_KERNEL_ACTION_DENSITY_UNCERTIFIED, ADD E1_SHARP_RECORD_LOCALIZER_VS_MARGINAL_SEA | The MECHANISM is named rather than merely the symptom.                                                                          |
```

Rows added by this spec for its own new obligations:

```text
| R-L0 fails (scale normalization)   | E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED              | The SCAD architecture stands as the correct replacement for the divergent K_sea (subtraction + HS + Carleman recovers D5 action-density form exactly, R.3); what is uncertified is narrowed to ONE scalar bound X_*(eps), with the full-tau_R-per-cell hazard named as its cause — the SAME structural fact as the O7 root. |
| R-L4 / C-L4 fails                  | E1_BASELINE_COLLAPSE_UNCERTIFIED                     | The exact bookkeeping N(0) = (1-D)/2 and the bound D^2 <= exp(-2 ||[C,P]||_2^2) stand as derived identities; only their transfer to the sealed cell propagator is uncertified. H-B is unaffected either way. |
| C-L5 fails                         | E1_MULTICELL_BASELINE_EXTENSION_UNCERTIFIED          | The single-cell baseline result stands, explicitly scoped to one cell and one state; H-B remains named and undischarged on multi-cell complexes. |
| A-L1 conventions unpinned          | A_L1_SPINOR_TRACE_CONVENTION_UNPINNED / A_L1_VOLUME_TYPING_UNPINNED | The kernel is exact and the HS pairing is the right tool; only a factor-4 spinor convention and an equal-time-vs-spacetime volume typing are open, and both are decidable by inspection of the sealed forms. |
| NC8 not executable                 | ONE_LINE_TWO_LINE_DISTINCTION_NOT_EXECUTABLE         | C3 stands as a correction of record; what fails is only its executable encoding. |
```

---

## §P — Frozen predictions (per route; outcome-class FIRST; magnitudes honestly wide)

**Calibration statement, mandatory and stated in the spec text per the ledger's
rule in force (`fcf35d0d…`):**

```text
THIS LANE FAMILY'S MAGNITUDE / SCALING PREDICTIONS HAVE MISSED THREE TIMES
CONSECUTIVELY. The third miss (Control-4 v3 S3(a): predicted central 1e-6 in a
1e-8..1e-4 window; measured null) was caused by a PINNED DIRECTION that was
trace-orthogonal to the detector — a DIRECTION-PIN DEFECT, NOT A MAGNITUDE-MODEL
ERROR (the same model on the supported direction gave 4.57e-6, inside the window;
POST HOC and NOT CREDITED). The two earlier misses were genuine magnitude
misses. BY CONTRAST, this lane's SIGN / DIRECTION / OUTCOME-CLASS predictions
have been RELIABLE, and the strongest to date — the O7 block prediction (P2) —
LANDED EXACTLY ON ITS PREDICTED OBLIGATION with an exact refuting witness.
CONSEQUENCE, APPLIED BELOW: outcome-class predictions are stated first and are
the register this lane stands behind. Magnitude windows below are DELIBERATELY
WIDENED relative to this lane's instinct, and the reason is named in each. A
magnitude prediction landing inside these windows should be credited weakly.
These predictions are not revisable after seal.
```

### Route ranking (frozen, per the memo)

```text
IR-C  FIRST FOR EXPECTED INFORMATION YIELD. It is the ONLY route that returns
      information under failure: a certified nonzero log coefficient converts
      "uncertified" into "certified divergent with an exact witness".
IR-A  FIRST FOR EXPECTED CERTIFICATION YIELD. Its n >= 2 leg rests on exact,
      already-verified constants and a single substantive new lemma (A-L2).
IR-B  THIRD. Its a-vertex sector is very likely certifiable and its record
      vertex is the wall; it is most valuable as the route that SPLITS the
      obligation cleanly into a certified sector and a named definition-level
      residue.
```

### Architecture-level predictions (all three routes)

```text
PA-1  OUTCOME-CLASS. The SCAD architecture REPLACES K_sea successfully at the
      structural level: R-L1, R-L2, R-L3 discharge. Confidence: high. Ground:
      the |C|_4^{1/2} x |C|_4^{1/2} = |C|_4 bookkeeping of R.3 is exact and was
      re-derived here.
PA-2  OUTCOME-CLASS. R-L0 (cell-scale normalization) is THE NEW HONEST BLOCK
      CANDIDATE alongside the sharp localizer. Predicted: R-L0 does NOT close on
      ground (i) (scale covariance) because tau_R is scale-invariant and every
      refined cell carries a full record cycle — the O7 root. Confidence:
      moderate-to-high. This is a BLOCK-vs-DERIVE prediction, the register in
      which this lane recorded an early optimism bias and has been improving; it
      is deliberately stated on the pessimistic side.
PA-3  OUTCOME-CLASS. R-L4 / C-L4 CERTIFIES, and D = 0 exactly, giving
      b_0 = 1/2 and kappa_bal = 1 EXACTLY. Confidence: moderate-high. Ground:
      the inequality D^2 <= exp(-2 ||[C,P]||_2^2) is exact and the
      non-Hilbert-Schmidt-ness of [C, 1_B] is already on the record (C6). This
      is an EXACT-VALUE structural prediction, not a magnitude prediction.
PA-4  OUTCOME-CLASS. H-R (rho_res) is NOT discharged by this program and is
      carried as a disclosed premise. Confidence: high.
PA-5  OUTCOME-CLASS. NO route reaches for a clustering axiom; F'-1 does not
      fire. Confidence: high. Ground: C5 — the long-distance direction has
      margin, and the memo establishes no route needs one.
```

### IR-A predictions

```text
PA-A1 OUTCOME-CLASS. A-L1 and A-L3 CERTIFY. The exact constants
      24 zeta(4)+2 zeta(6) and 64 zeta(3)+16 zeta(5) are delivered with
      certified outward enclosures. Confidence: high (already re-verified in
      exact partial sums this session).
PA-A2 OUTCOME-CLASS. A-L1's spinor-trace convention resolves in favour of the
      CONSERVATIVE constant 1/(pi^4 R^6) — i.e. the memo's 1/(4 pi^4 R^6) is a
      per-component reading and is a factor 4 optimistic. Confidence: moderate-
      high. Recorded as a direction prediction, not a magnitude.
PA-A3 OUTCOME-CLASS. A-L2 (light cone) IS THE HONEST BLOCK OF THIS ROUTE. The
      predicted route outcome is E1S_N_GE_2_CERTIFIED_BLOCK_ISOLATED_TO_N1 with
      witness E1_NULL_SEPARATION_CLUSTER_DECAY_UNCERTIFIED, NOT full
      certification. Confidence: moderate. This is the P2-shaped prediction that
      landed exactly last time; it is stated in the same register.
PA-A4 MAGNITUDE — WINDOW DELIBERATELY WIDE. eps_*^{(>=2)} lands on a dyadic
      grid point in 2^-1 .. 2^-14, with 2^-4 .. 2^-8 the widened central band.
      REASON THE WINDOW IS WIDE AND NAMED: (a) this lane's magnitude predictions
      have missed three consecutive times; (b) eps_*^{(>=2)} depends
      multiplicatively on rho_res and kappa_bal, of which rho_res is an
      undischarged named hypothesis (H-R) with NO derived scale at all — a
      window narrower than several dyadic decades would be dishonest, not
      confident; (c) the A-L1 factor-4 convention alone moves the answer by up
      to one grid point.
PA-A5 MAGNITUDE — WINDOW DELIBERATELY WIDE. eta_{>=2}(eps_*^{(>=2)}) lands in
      1/64 .. 1/2. REASON: the grid rule selects the LARGEST qualifying dyadic
      point, so eta_{>=2} is pushed toward but not to the 1/2 ceiling by
      construction; where in that range it lands is governed by the same
      undetermined rho_res.
```

### IR-B predictions

```text
PA-B1 OUTCOME-CLASS. B-L1 (trace part) CERTIFIES; both cancellations (odd spinor
      trace -2 p-hat_x, and |m_0|^2 = 0) hold exactly. Confidence: high (the
      moment structure was re-verified exactly this session).
PA-B2 OUTCOME-CLASS. The a-VERTEX SECTOR of B-L2 CERTIFIES carrier-uniformly
      (Gevrey b_D, all derivatives vanishing at boundary and tips). Confidence:
      moderate-high.
PA-B3 OUTCOME-CLASS. THE RECORD VERTEX OF B-L2 IS THE WALL and does NOT certify.
      Predicted witness: E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER.
      Confidence: high. Ground: C6 is already on the record and C4 shows the
      weights put M(t) in the lead.
PA-B4 OUTCOME-CLASS. Whether the B-L2 record-vertex failure is CERTIFIED (hence
      §Q2-STOP) or merely UNCERTIFIED (hence E1S_BLOCKED) is genuinely open, and
      this lane predicts MERELY UNCERTIFIED for the two-time object — the
      equal-time failure is certified, the two-time failure is not. Confidence:
      low-moderate. STATED EXPLICITLY BECAUSE IT DETERMINES WHETHER Q2 FIRES,
      and this lane must not be read as pre-empting Q2: the prediction is that
      Q2 does NOT fire on IR-B, and this prediction carries no weight and grants
      no permission if it is wrong.
```

### IR-C predictions

```text
PA-C1 OUTCOME-CLASS. C-L1 (phase -> operator collapse) CERTIFIES. Confidence:
      high (the phase-level statement is already an exact Phase-2 witness).
PA-C2 OUTCOME-CLASS. C-L2 (commutator in quadratic form) IS THE BLOCK OF THIS
      ROUTE. Confidence: moderate-high. Ground: ||[h_0, 1_B]|| = infinity
      excludes norm control, and the correct quadratic form is not supplied by
      any sealed authority.
PA-C3 OUTCOME-CLASS. IF C-L2 is surmounted, C-L3's log coefficient is NONZERO.
      Confidence: moderate-high. Ground: the degree-(-3) kernel's absolute
      integral is logarithmic with derived coefficient exactly 2/pi at BOTH
      ends, and nothing in the sealed structure supplies a cancellation at the
      UV end — C4 shows the one candidate cancellation is exactly saturated.
      *** THIS PREDICTION, IF IT LANDS, TRIGGERS §Q2-STOP. THIS LANE STATES IT
      AS A PREDICTION AND EXPRESSES NO PREFERENCE BETWEEN Q2's TWO OPTIONS. ***
PA-C4 MAGNITUDE — WINDOW DELIBERATELY VERY WIDE. If nonzero, the sharp-kink log
      coefficient has modulus in 1e-2 .. 1e+2 times (2/pi), i.e. roughly four
      decades. REASON THE WINDOW IS THIS WIDE AND NAMED: (a) three consecutive
      magnitude misses; (b) the coefficient is a product of the derived 2/pi
      with UNKNOWN spinor-trace and record-colour group factors, none of which
      this lane has derived; (c) the most recent miss was caused by a pinned
      direction being orthogonal to the detector — the analogous hazard here is
      that the leading spinor trace vanishes by parity (it does at first order,
      per C4/B-L1), so the surviving coefficient may sit two orders below the
      naive estimate. A narrower window would repeat exactly the error the
      ledger records.
PA-C5 OUTCOME-CLASS. C-L5 (multi-cell baseline extension) does NOT close in this
      program; C-L4 is scoped to the single cell. Confidence: moderate.
```

### Control predictions

```text
PA-N1 OUTCOME-CLASS. NC1 refuses by naming H-IND; NC2 and NC5 detectably fail
      with their exact M-5 witnesses; NC6 exhibits the D5 trap; NC7 finds no
      target-adjacent numeric. Confidence: high (the parent's P4 analogue HELD:
      NC3-NC7 all passed).
PA-N2 OUTCOME-CLASS. NC8 IS EXECUTABLE and behaves: the one-line leg fails
      reproducing 24 H_K exactly, the two-line leg passes. Confidence: high
      (re-verified in exact arithmetic this session).
PA-N3 OUTCOME-CLASS. NC10 behaves: the unsubtracted variant DIVERGES, confirming
      the subtraction is load-bearing. Confidence: high (this is C2 restated as
      a control).
PA-N4 OUTCOME-CLASS. NC9's diagnostic shows that a LIPSCHITZ-class mollifier
      does NOT restore Hilbert-Schmidt-ness, and that a stronger smoothness
      class is required. Confidence: moderate. Ground: C6's
      int d^3 r · r^2/r^6 = int dr/r^2 remark. REPORTED ONLY; no adoption; this
      prediction grants no permission to smooth anything.
PA-N5 OUTCOME-CLASS. W1 is nonzero and inside its M-1/D5' spanning-tail
      comparator, IN ANY ARM THAT REACHES IT. Confidence: moderate. Note the
      parent's P3 was UNTESTED (blocked by ordering); this restatement is not a
      re-prediction of a hit.
```

### Overall predicted outcome (frozen)

```text
PREDICTED PROGRAM OUTCOME: E1S_N_GE_2_CERTIFIED_BLOCK_ISOLATED_TO_N1, with the
n >= 2 leg certified via IR-A at the exact zeta constants, the n = 1 leg blocked,
and the named residue being the pair
    E1_SEA_KERNEL_ACTION_DENSITY_UNCERTIFIED (restricted to n = 1)
  + E1_SHARP_RECORD_LOCALIZER_VS_MARGINAL_SEA (mechanism)
plus, as an independent second block candidate,
    E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED (R-L0).
SECOND-MOST-LIKELY: E1S_CERTIFIED_DIVERGENT_AT_SHARP_LOCALIZER via IR-C's C-L3,
triggering §Q2-STOP. THIS LANE EXPRESSES NO PREFERENCE BETWEEN Q2's OPTIONS AND
ITS PREDICTION OF THIS OUTCOME IS NOT A DESIGN CHOICE.
LEAST LIKELY: E1S_REPLACEMENT_ARCHITECTURE_CERTIFIED.
This ordering errs toward blocks, per the standing calibration.
```

---

## §S — Scoped supersessions of the parent spec (complete list)

```text
S-1  D5 single eta  ->  D5' split (eta_1, eta_{>=2}) with the mandatory
     Gamma_star^split <= 1 clause. Scope: this spec's obligations only. The
     parent's D5 requirement — action-density form, per unit 4-volume, constants
     in the permitted tuple — is UNCHANGED and BINDING on both legs.
S-2  The parent's E1 rule steps 1-4 -> §E1' steps 1-7. The parent's dyadic grid
     and its 1/2 threshold are inherited UNCHANGED; only the two-leg structure
     and the aggregate margin clause are added. No new number.
S-3  The parent's O4-M2 constant architecture (Phase-1 eta, K_sea, T_R, b_0) ->
     §R SCAD (G_tr, G_hs, G_bl, rho_res, kappa_bal, X_*). RETIREMENT, not
     amendment (C2). Enforced by F'-3 and NC10.
S-4  The parent's O8 NC4 -> NC4-as-fence, per the majorant-arm re-scope removing
     every Route-T artifact from the DERIVED arm's predicate.
S-5  The parent's O9 / M-1 comparator -> the same comparator read on eta_{>=2}
     under the split.
S-6  The memo's A-L1 constant and its IR-B per-pair display -> corrected per
     F'-14. The memo remains the ROUTE authority; it is not an authority for
     these two constants/displays.
NOT SUPERSEDED, AND EXPLICITLY REAFFIRMED: D1, D2, D3, D4-as-M-2, M-3, M-4, M-7,
M-8, M-11, O1, O2, R1-R4, the parent's fences F-1..F-8 (carried as F'-1..F'-13),
the L2 typing freeze fences 1-5, and the T7(i) anchor with named H-B.
```

---

## §X — What this program does NOT claim

```text
- It does not discharge H-B, H-B0, or H-R.
- It does not close, select, or narrow the ER fork (F'-6).
- It does not pin the D3 quantifier to any finite list, under any outcome.
- It does not answer Q1 (answered by the principal at 209aa390…: NO, the DERIVED
  arm does not require O7) — it consumes that answer.
- It does not answer Q2, rank Q2's options, or design around Q2 (§Q2-STOP).
- It says nothing about kappa_record, alpha, any coupling, T7(ii), or T7(iv).
- It does not revive Route T, and consumes no Route-T artifact (F'-13).
- It does not adopt any clustering axiom (F'-1).
- It does not smooth, mollify, or otherwise alter the sealed sharp localizers
  (D6', NC9).
```

---

## Protected status

```text
E1_reclassified = true
E1_replacement_activity_architecture_frozen = true      (this draft; unsealed)
E1_replacement_activity_architecture_certified = false
cell_scale_normalization_certified = false              (R-L0)
subtracted_carleman_constants_certified = false         (S1-S5)
baseline_collapse_certified = false                     (R-L4 / C-L4)
IR_A_n_ge_2_certified = false
IR_A_light_cone_lemma_certified = false                 (A-L2)
IR_B_a_vertex_sector_certified = false
IR_B_record_vertex_certified = false                    (B-L2)
IR_C_record_phase_operator_collapse_certified = false   (C-L1)
IR_C_commutator_form_certified = false                  (C-L2)
IR_C_log_coefficient_evaluated = false                  (C-L3)
epsilon_star_frozen = false
Gamma_star_split_certified = false
H_B_discharged = false
H_B0_discharged = false
H_R_discharged = false
Q2_triggered = false
Q2_answered_by_principal = false
clustering_adoption_reached = false
majorant_derived_arm_rescoped = true
O7_escalation_retired = true
refinement_intertwiner_derived = false   (unchanged; now not required)
routeT_skeleton_gap_certified = false    (unchanged; now not required)
F8_rederivation_and_hostile_review_discharged = false   (the gate)
connected_linked_cluster_density_proved = false
volume_uniform_zero_free_neighborhood_proved = false
Duhamel_intensive_Hessian_equality_proved = false
ER_fork_closed = false
kappa_record_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```

---

## Handoff notes to the construction lane (seal-time checklist) and the hostile reviewer

```text
FOR THE CONSTRUCTION LANE, AT SEAL:
 1. Re-verify every hash in the Pinned authorities table. Decide the disposition
    of the ELEVEN rows marked "no adjacent seal" — this drafting lane flags the
    absence rather than treating a computed hash as a seal.
 2. Decide whether the drafting-lane verification scripts
    (verify.py 1acfda26…, verify2.py 62219975…) are relocated into the cleanroom
    and hashed as part of the sealed record, or excluded as scratch. They are
    scratch as written and carry no verdict authority.
 3. Confirm the two drafting-lane corrections to the memo (§O.A A-L1 spinor
    factor 4; §R.0 per-pair-versus-summed IR-B display) are acceptable as
    CORRECTIONS OF RECORD rather than requiring a separate correction artifact.
 4. Confirm the D5' split is within the scope of an append-only successor spec,
    or route it as an amendment to the parent.

FOR THE HOSTILE PRE-EXECUTION REVIEWER, the four places this draft is most
likely wrong, named by its author:
 A. R-L0. The cell-scale normalization may be UNDISCHARGEABLE, in which case
    the SCAD architecture is no better off than K_sea and this draft has moved
    the divergence rather than removed it. Attack ground (i) first: does g(eps)
    actually carry a compensating scaling weight, given that tau_R is
    scale-invariant?
 B. R.3's kappa_bal closure. The step sum|c| = kappa_bal followed by (R.3.b)
    assumes the per-pair ratios are simultaneously controlled; check whether a
    single pair with |Log rho| large but weight small defeats the bound.
 C. R.2 / R-L4. The exact-collapse computation N(0) = (1-D)/2 was verified on
    FINITE RATIONAL PROJECTOR PAIRS. Its transfer to the sealed cell propagator
    presumes C-L1 and C-L2. If the commutator error is not negligible in the
    relevant form, the whole baseline argument — and with it the claim that
    kappa_bal = 1 — collapses.
 D. §P's confidence assignments. This lane's magnitude register has missed three
    times consecutively; check whether any OUTCOME-CLASS prediction above is
    actually a disguised magnitude prediction.
```


---

## Z. Q2 STANDARD — REVISED AT SEAL BY THE CONSTRUCTION LANE (disclosure)

DISCLOSURE OF RECORD: this section was AUTHORED AT SEAL TIME by the
construction lane, not by the drafting lane. The drafting lane's charter
predated the principal's pre-registered Q2 classification standard
(STAGE8_T7_Q2_TRIGGER_AND_CLASSIFICATION_STANDARD_V001, sealed; issued
before any IR route terminated, deliberately, so that it is principle and
not rescue). That standard GOVERNS this spec and supersedes any narrower
Q2 language elsewhere in this text.

### Z.1 Option (iii) exists and is ordered FIRST

```text
(i)   sharp localizer inviolable;
(ii)  smoothed-localizer successor by decision;
(iii) DERIVE the spatial localizer profile from sealed principles — the
      treatment the TEMPORAL write profile already received (the
      diamond-marginal pulse w(s) = 32(1/2-|s-1/2|)^3 was DERIVED, not
      chosen), while the spatial M(t) was POSTULATED sharp.
ORDERING: if Q2 triggers, (iii) is evaluated BEFORE (ii) may be adopted;
(i) remains available throughout. A FAILED DERIVATION ATTEMPT IS A
RECORDED RESULT, NOT A DEFEAT, and cleanly licenses (ii) as an honestly
disclosed decision.
```

### Z.2 OBJECT-VS-BOUND TRIPWIRE (binding on every route of this spec, IR-C especially)

```text
A DIVERGENT MAJORANT IS NOT A DIVERGENT THEORY.
Any artifact of this program that would EXCLUDE the sharp localizer MUST
state explicitly which of these it establishes:
  (a) the DEFINED QUANTITY ITSELF (the completed amplitude / its a = 0
      Hessian) is divergent or ill-defined under the sharp localizer
      — require-grade; or
  (b) every bound of a STATED CLASS fails — require-grade ONLY IF the
      finiteness demanded is itself a sealed principle rather than a
      convenience of the proof; or
  (c) only the CURRENT architecture's bound fails — which EXCLUDES
      NOTHING and licenses only further IR work.
Excluding the sharp localizer BECAUSE IT RESISTS PROOF, absent (a) or a
principled (b), is SELECTING FOR PROVABILITY: route-shopping at the
definition level. Any route return omitting the (a)/(b)/(c) declaration
BLOCKS. IR-C's certified-divergent arm may not be reported without it.
```

### Z.3 Tier classification is NOT this spec's to claim

```text
TIER 1 forced profile => theorem, Q2 dissolves, no conditionality.
TIER 2 forced exclusion + allowed family => a DERIVED EXCLUSION and a
        SELECTION recorded as TWO SEPARATE THINGS, never blurred into one
        "DERIVED" (the ER-B failure class; excluded-by-theorem is never
        conflated with excluded-by-postulate).
TIER 3 nothing forced => a definition decision, labeled as such, with
        conditionality on every headline downstream of kappa_record,
        entered in the cumulative-conditionality register beside the ER-A
        premise and the complex-vs-real selection.
THE AUTHORING LANE PROPOSES; A FRESH-CONTEXT HOSTILE REVIEW CLASSIFIES.
No execution of this spec may assign its own tier.
```

### Z.4 Tier-2 residual: record-invariance criterion, with its scope limit

```text
GAUGE   iff every record-level output (kappa_record and every quantity on
        its chain) is PROVABLY INVARIANT across the allowed family,
        established AT THE OPERATOR LEVEL — THE GAMMA STANDARD APPLIES
        VERBATIM: a numerical coincidence across sampled members is NOT
        the theorem and may not be labeled as one. Then no conditionality
        attaches.
PREMISE otherwise (including when invariance cannot be proven): a
        disclosed premise with full conditionality in the cumulative
        register.
SCOPE LIMIT, reproduced as the principal requires: this criterion is a
CLASSIFICATION RULE for Q2's residual ONLY. It is NOT a sealed physical
principle, and neither it nor the sealed standard may be cited as
authority for the underlying idea anywhere else; that idea must earn its
own derivation under its own spec.
```

### Z.5 STOP rule

```text
If ANY route of this spec terminates in a certified finding that the
sharp record localizer M(t) is itself the obstruction, EXECUTION STOPS AT
THAT FINDING. The finding is sealed and escalated to the principal with
options (i), (ii), (iii) and their FULL COSTS stated, and WITH NO
ARGUMENT FOR ANY OF THEM. No lane may answer Q2, pre-empt it, or design
around it silently.
```

### Z.6 Second contingency of record (F-8 finding, added at seal)

```text
This spec owns E1 only. The F-8 re-derivation independently established a
SECOND underived Route-Q obligation:
  Q6_UNIFORM_CELL_ADJACENCY_DEGREE_UNDERIVED — a uniform bound on the
  cell-adjacency degree over the D3 quantifier, required by the polymer
  combinatorics.
It is NOT owned here and NOT absorbed by any route below. Discharging
every obligation of this spec does not by itself deliver the Route-Q
predicate.
```
