# STAGE 8 — THE JOIN FRONTIER: THE FLUX GATE'S OUTPUT SIDE, THE THREE UNPERFORMED JOINS, THE ONE PERFORMED ONE, AND THE CENSUS OF UNDISPLAYED OUTPUT SIDES — O51SR V001

## BUILD LANE — COMMISSION O51SR — 2026-08-16

**READ AND REPORT AT BYTES. DISPLAY ONLY.** Nothing here is authored, adopted,
advocated, proposed, repaired, coerced, or reclassified. **No composition is
performed.** Where this artifact says what a composition *would* yield, that is a
report of two displayed types and of what the corpus's own text says the join
would discharge; the join itself is a principal act and is not taken. Where the
bytes do not decide, the row reads **INDETERMINATE-AT-BYTES**.

Fences declared and honoured throughout: `alpha_computed = false`;
`proof_authorized = false`; `kappa_record_computed = false`. No numeric value of
any coupling, scale, root, eigenvalue, norm, or constant is computed, and no
magnitude is approached. Symbols the corpus displays in units of `hbar` are
quoted as the corpus writes them; closed-form scales and roots are cited by
file:line and **not reproduced** — marked [MAGNITUDE-FENCED]. Fence tokens appear
below only inside quotation.

---

## §0 — GROUND CONSUMED, SEALS VERIFIED, AND THE SIDECAR CONVENTION SETTLED

Every sidecar was verified with `shasum -a 256 -c` **run from the artifact's own
directory**, before any reliance. **Both sidecar forms were probed for every
artifact** — `<stem>.md.seal.sha256` and `<stem>.seal.sha256` — before anything
was called unsealed.

### §0.1 — THE SIDECAR CONVENTION IS SPLIT ALONG A LANE BOUNDARY, AND THE SPLIT IS ITSELF A FINDING

```text
CONVENTION A — <stem>.seal.sha256   (no .md before .seal)
  USED BY: the cleanroom primitive-gate family, all dated 2026-07-23.
  BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md              OK
  SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md                 OK
  PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md             OK
  PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md               OK
  TYPED_CHARGE_CHIRAL_RECORD_INVARIANT_FORM_GATE_V001.md            OK
  CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md                        OK

CONVENTION B — <stem>.md.seal.sha256
  USED BY: the STAGE8 lane artifacts and the supervision rulings.
  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                         OK
  STAGE8_EARN_THE_TRACE_S9AD_V001.md                                OK
  STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md   OK
  STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md                 OK
  STAGE8_INGREDIENT_CENSUS_O17SR_V001.md                            OK
  STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md       OK
  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md          OK
  STAGE8_THE_JOIN_O42SR_V001.md                                     OK

NEITHER FORM PRESENT — probed in BOTH roots, declared not hidden (FLAG-1):
  PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md      NO SIDECAR, EITHER FORM
  BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md    NO SIDECAR, EITHER FORM
  BID_ACTIVE_HANDLE_CONTROL_GATE_V001.md               NO SIDECAR, EITHER FORM

FULL TALLY OF ARTIFACTS CITED IN THIS COMMISSION
  Convention A (cleanroom primitive gates)        8/8    OK
  Convention B (STAGE8 lane + LOCKED_PROCESS)    13/13   OK
  Supervision rulings, both prefixes             28/28   OK
  GRAND TALLY                                    49/49   OK, ZERO MISMATCHES
  NO SIDECAR IN EITHER FORM, IN EITHER ROOT          3   (named above)

SUPERVISION RULINGS — ALL TWENTY-EIGHT, BOTH FILENAME CONVENTIONS
  DOR_NNN_*                     14 files    14/14 seals OK
  DECISION_OF_RECORD_NNN_*      14 files    14/14 seals OK
  RULINGS SEAL TALLY: OK=28  FAIL=0  NO-SIDECAR=0
```

**THE SIDECAR TRAP DID REAL DAMAGE OF RECORD, AND HERE IS THE INSTANCE.**
`STAGE8_EARN_THE_TRACE_S9AD_V001.md` (Convention B, seal OK) ran its own sweep
for record-changing-operator carriers, **found the flux gate**, and refused it.
Quoted whole, :598-605, including the surrounding sweep line so the clause is not
cut:

> "SWEEPS_DISPLAYED = J_FS carriers (18 files, co-occurrence only in surveys);
> W(tau)/U_0^dag U_1 carriers (3 files, zero spine co-occurrence); unit-modulus-built
> literal (0 hits — the phrase is the tasking's, not the corpus's; the substance
> located at the KCH/E1/C4 carriers); m_*/spectrum cross-lineage carriers (the brief
> + S13 lineage; no booked m_*-to-branch-mass identification, non-identification
> carried); **record-changing-operator carriers (2: the onset gate + the source-flux
> write gate, the latter unsidecared and NOT consumed).**"

**AT BYTES THE FLUX GATE IS SIDECARED AND ITS SEAL VERIFIES.** The sidecar is
`SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.seal.sha256` — Convention A —
and `shasum -a 256 -c` run from the directory returns OK. Timestamps, displayed
because the ordering is the whole point:

```text
SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md          2026-07-23 10:08
SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.seal.sha256 2026-07-23 10:10
STAGE8_EARN_THE_TRACE_S9AD_V001.md                         2026-08-14 17:44
```

The seal predates the refusal by three weeks. **A closure-lane artifact declined
to consume the flux gate on a ground that is false at bytes, and the ground is
exactly the sidecar-convention split.** This is reported, not repaired: I do not
re-run EARN_THE_TRACE's determination and I do not consume the flux gate on its
behalf. It is carried into §2 as the displayed reason one join was not attempted.

**PRIMARY-VS-REPORT DISCIPLINE.** Every type below is taken from **defining text
at its own primary bytes**. `STAGE8_THE_JOIN_O42SR_V001.md` is read as a **prior
lane report**, not as authority: every row of it I rely on, I re-open at the
primary artifact and re-read at the cited line. Two of its locators are corrected
below and the corrections are flagged, not folded in.

---

## §0.2 — SWEEP DECLARATION: PATTERNS, HIT COUNTS, PER-PATTERN LEAK COUNTERS

Every sweep ran through a basename filter carrying the exclusion list **as a zsh
ARRAY**, never as a string variable. The array was:

```text
BAR=( REGISTER  TRACKER  THE_PLAN  ROAD_REMAINING  THE_HANDOFF
      OBSERVATIONS_REGISTER  DECISION_SHEET  STAGE8_JOIN_FRONTIER_O51SR )
```

The last element is **this artifact's own basename**: it self-excluded from every
sweep. (It also did not exist at sweep time; the output-path probe returned "No
such file or directory" before any write.) Barred files matching a pattern were
counted as leaks and **not opened**.

```text
SWEEP A — WHO NAMES THE FLUX GATE'S OUTPUT OBJECT?
  Roots: ROOT-1 (workspace) + ROOT-2 (cleanroom v003), *.md and *.json.
  PATTERN                                    NET HIT FILES   LEAKS EXCLUDED
  "record-changing operator"                       5                0
     = 3 distinct artifacts (each root carries a mirror copy):
       BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md
       SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V001.md
       STAGE8_EARN_THE_TRACE_S9AD_V001.md
     ALL THREE OPENED AT THEIR HIT LINES.
  "record-changing" (any)                         30                1
  "source-flux-conditioned" (prose)                2                0
     = 1 distinct artifact, both roots:
       BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md
     OPENED.  *** THE ONSET GATE IS THE ONLY ARTIFACT IN THE CORPUS THAT NAMES
     THE FLUX GATE'S OBJECT IN PROSE. ***
  "SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE" (filename citation)
                                                  45                3
  CUTOFF: the three low-count patterns were opened at every hit FILE.  For
  "record-changing" (30) and the filename citation (45) the cutoff was: open
  every hit that is a PRIMARY defining artifact rather than a lane survey or an
  authority ledger; 9 opened.  MEMBERS NOT OPENED, NAMED: the STAGE8_DESC_B01_*
  family (5), STAGE8_7A_* (3), the three CURRENT_AUTHORITY_LEDGER_V0{11,12,13}
  .json, STAGE8_RADION_*, STAGE8_STANDING_PREMISE_TEST_*, R3_4_PARENT_STATE_*,
  STAGE8_DOWNSTREAM_SMOOTH_*, STAGE8_FROZEN_PRIMITIVE_INVENTORY_* (read at its
  :149 only, via O42SR's locator, re-verified), the O36SR/O42SR pair and their
  audits (read as reports, §0.1).  REASON NOT OPENED: each cites the gate by
  FILENAME in a provenance or inventory list, not by consuming its output.  This
  is a declared cutoff, not a claim of exhaustiveness — see CH-5.

SWEEP B — CROSS-CITATION PROBES (does X name Y at bytes?), run by me, complete.
  flux gate V003  -> (TYPED_CHARGE|W_X|W_Y|invariant.form|chiral):    0 hits
  invariant-form gate -> (SOURCE_FLUX|Q_Sigma|K_write|U_write|Y_R|
                          onset|holonomy|BOUNDARY_RECORD):            0 hits
     (one lexical hit at its :151 is the token `record_onset_to_source_self_
      energy_ratio_derived` inside its own status block — a flag name, not a
      citation.  DISPLAYED, not silently dropped.)
  CUTOFF: the two artifacts; complete.

SWEEP C — THE TWENTY-EIGHT RULINGS, BOTH PREFIXES, EVERY UNIVERSAL NEGATIVE.
  Enumerated as DOR_*.md (14) + DECISION_OF_RECORD_*.md (14) = 28.
  PATTERN                                          RULING HIT FILES / 28
  "M_2(C)|Bloch|two-plane|primitive record carrier"          0
  "SOURCE_FLUX|flux-conditioned|record write gate"           0
  "J_FS|onset gate|onset saturation|BOUNDARY_RECORD_ONSET"   0
  "allow/require|support(L_c)|Omega_c"                       2
  "kappa_M|kappa_W|invariant-form|invariant form"            0
  "K_write|record-changing holonomy|Q_Sigma"                 0
  BOTH HITS OPENED AT THEIR LINES:
    DECISION_OF_RECORD_014_AMENDMENT_1_CB_DERIVED_PAIRING :9 — "Per the
      principal's allow/require reading (recorded" — an incidental appeal to a
      reading, not a ruling on any join.
    DECISION_OF_RECORD_015_FIELD_SIGNATURE_PHYS_RATIFIED :18 — "The
      allow/require convergence is recorded as consistency evidence (OBS-17)."
      Consistency evidence, not a ruling on any join.
  *** RESULT, AND IT IS A UNIVERSAL NEGATIVE RUN OVER ALL TWENTY-EIGHT AND NOT
      OVER FOURTEEN: NO RATIFIED RULING BEARS ON THE FLUX GATE'S OUTPUT SIDE,
      ON ANY OF THE THREE UNPERFORMED JOINS, OR ON THE ONE PERFORMED ONE. ***
  BOTH HITS FALL UNDER THE `DECISION_OF_RECORD_` PREFIX — the half a recent
  commission and the registrar are reported to have missed.  Had I swept only
  `DOR_*`, this negative would have been run over 14 of 28 and both hits would
  have been invisible.  The negative stands; the method that would have reached
  it by luck does not.

SWEEP D — TOKEN-COLLISION CONTROL on `K_write`, run by me, uncapped.
  `K_write` occurs in two unrelated senses and they must not be merged:
    SENSE 1 (the flux gate's): `K_write,rel = (pi/2) Q_Sigma tensor Y_R`.
    SENSE 2: `K_write,N`, "the already-existing D1/D2 finite signed write
      system" — STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V002.md :286,
      carried to V003 :237, :250 (`iota_N:K_write,N -> M`).
  A COMBINATORIAL/SIGNED-WRITE-SYSTEM OBJECT, NOT AN OPERATOR.  Not counted as
  a consumer of Sense 1 anywhere below.
  A third collision is carried from the prior report and re-verified here:
  `U_write` names TWO DIFFERENT OPERATORS — `P_0^S ⊗ I_R + P_1^S ⊗ X_R`
  (PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002 :42-45) and
  `(I-Q_Sigma) ⊗ I_R + Q_Sigma ⊗ (-iY_R)` (flux gate :83-86).  Sweep B confirms
  zero cross-citation in either direction.

NOTHING ELSE OPENED UNDER THE REGISTER BAR.  No file whose basename contains
REGISTER or TRACKER, and no THE_PLAN*, ROAD_REMAINING*, THE_HANDOFF*,
OBSERVATIONS_REGISTER*, or *DECISION_SHEET* file was opened, quoted, or counted.
QUESTIONSSETTLED_REGISTER_V001.md, which sits in ROOT-2, was NEVER OPENED.
TOTAL LEAK COUNT ACROSS ALL PATTERNS: 4.  No git action was taken.
```

---

## §1 — Q1: THE FLUX GATE'S OUTPUT SIDE, DISPLAYED

Primary: `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md`, seal OK under
Convention A. Its own title line, :1, is **"Conditional Source-Flux
Record-Holonomy Gate v003"** — the gate names itself after a **holonomy**, i.e.
after the thing it *produces*, not after the thing it consumes. Versions 001 and
002 are read for provenance only; the gate rules at :19-20 **"Versions 001 and
002 are not authority."**

### §1.1 — WHAT IT PRODUCES: THE OBJECT, ITS TYPE, ITS CONSTRUCTION

**THE CONSTRUCTION, QUOTED WHOLE FROM :47-78, INCLUDING THE CLAUSE THAT CUTS
AGAINST IT.**

> ":47-51 — For an affine constant-axis representative on the primitive record
> factor,
> ```text
> h' = v_x X + v_y Y + v_z Z.
> ```
> :55-58 — The ready-state survival amplitude is
> ```text
> cos(theta)-i(v_z/|v|)sin(theta),
> theta=|v| tau_*/hbar.
> ```
> :60-65 — First orthogonal onset gives
> ```text
> theta=pi/2,
> v_z=0.
> ```
> :67-74 — Endpoint rephasing rotates the equatorial pair `(v_x,v_y)`. Choosing
> `Y_R` as representative, the conditional integrated record-changing holonomy is
> ```text
> K_write,rel = (pi/2) Q_Sigma tensor Y_R
> ```
> up to endpoint rephasing and orientation reversal.
> :76-78 — **The equatorial classification is an analytic consequence imported
> into the executable gate. The executable audit verifies the chosen `Y`
> representative, not a symbolic derivation of the full generic formula.**"

**THE SECOND PRODUCED OBJECT, :80-89, QUOTED WHOLE WITH ITS LIMITING SENTENCES.**

> "The ready-subspace unitary is
> ```text
> U_write
>   = (I-Q_Sigma) tensor I_R
>     + Q_Sigma tensor (-iY_R).
> ```
> **This is conditional on the inherited/adopted rules above. It is not a
> complete microscopic action or a durable record instrument.**"

**THE THIRD PRODUCED OBJECT, :91-121, QUOTED WHOLE.**

> "The family
> ```text
> U_chi
>   = (I-Q_Sigma) tensor I_R
>     + exp(-i chi) Q_Sigma tensor (-iY_R)
> ```
> has the same conditional record projectors for every `chi`. In a fixed source
> basis, a coherent zero/one-flux superposition produces different joint
> source-record density matrices for different `chi`, while the reduced record
> density is unchanged. … **This algebra does not establish that the phase is
> physically observable.** Coherence between the local-flux alternatives must
> belong to one admissible total-charge sector, and an allowed phase reference or
> recombination operation must exist. If the two alternatives are different
> total-charge sectors, a charge-superselection rule can obstruct the comparison.
> Therefore the current conclusion is only:
> ```text
> chi is not fixed by the conditional record projectors.
> ```
> **Its gauge-invariant physical effect is open.**"

**AND THE GATE'S OWN SUMMARY OF ITS OUTPUT SIDE, :124-128, QUOTED WHOLE:**

> "## What is fixed
> ```text
> conditional record-changing holonomy class on the declared ready subspace;
> the selected Y representative passes exact unitary and flux-sector checks.
> ```"

```text
*** THE FLUX GATE'S OUTPUT TYPE, STATED ONCE ***

O-A  K_write,rel  — A HERMITIAN RECORD-CHANGING GENERATOR ON
     (FLUX SECTOR) ⊗ (PRIMITIVE RECORD FACTOR), of the form
     [a real coefficient] · Q_Sigma tensor Y_R, where Q_Sigma is the inherited
     flux operator with spec(Q_Sigma) = {0,1} and Y_R is an equatorial
     generator on the primitive record factor.  Fixed only up to endpoint
     rephasing and orientation reversal — i.e. it is a CLASS, not a point.
     The gate's own word for it: a "holonomy class".

O-B  U_write     — THE READY-SUBSPACE UNITARY on the same two factors, block-
     decomposed over the two spectral projectors of Q_Sigma, with OPERATOR-
     VALUED coefficients I_R and (-iY_R) on the record factor.

O-C  U_chi       — A ONE-PARAMETER FAMILY of such unitaries, indexed by an
     unfixed phase chi, all sharing the same conditional record projectors.
     I.e. AN ADMISSIBLE SET FOR THE QUANTITY chi, expressly not a point.

THE GATE IS TYPED AS A CONSUMER BY EVERY COMMISSION.  ITS OUTPUT SIDE IS
THREE OBJECTS DEEP.
```

**ONE CORRECTION TO THE COMMISSION'S OWN PREMISE, MADE HERE AND NOT INHERITED.**
The commissioning background states that the flux gate's output side has been
displayed by **no** commission. **That is false at bytes as of O42SR.**
`STAGE8_THE_JOIN_O42SR_V001.md` (seal OK) §2.1 row **P-5**, :563-583, quotes both
`K_write,rel` and `U_write` and types them: "**OUTPUT TYPE: a source-conditioned
unitary on flux-sector ⊗ primitive record factor, plus its Hermitian generator
`K_write,rel`.** **FINDING: C-2 is BOTH a consumer and a producer.**" The
three-times-repeated blind spot is real for the three commissions *before* O42SR;
O42SR closed it. **What O42SR did not do — and what this commission asks — is
test that output against the consumer set.** It graded P-5 against C-1 alone
(MISMATCH) and swept the rest into a uniform bulk band. The census of §1.2 is
therefore new work, and it does not rest on the premise being true.

### §1.2 — IS THAT OUTPUT THE INPUT TYPE OF ANY CONSUMER IN THIS CORPUS?

**GRADING RULE, FIXED BEFORE APPLICATION.**

```text
MATCH        the flux gate's output type IS the type this consumer's defining
             text says it consumes.  Both types displayed side by side, quoted.
MISMATCH     they are not the same type.  Both displayed; the exact locus of the
             difference named.
CANNOT-DETERMINE-AT-BYTES   one side's type is not at bytes, or the bytes do not
             fix whether the two are the same.
```

Nineteen consumer rows tested. Sixteen are the roster O42SR established (C-1 …
C-16), re-opened by me at primary bytes. **Three are consumers no commission has
ever enumerated** — C-17, C-18, C-19 — and I found them by SWEEP A rather than
inheriting them.

---

#### ★ THE MATCH. C-17 — THE TYPED CHARGE-CHIRAL-RECORD INVARIANT-FORM GATE.

Primary: `TYPED_CHARGE_CHIRAL_RECORD_INVARIANT_FORM_GATE_V001.md`, seal OK under
Convention A, dated 2026-07-23 — **the same day as the flux gate**. Its argument
type, quoted whole from :14-64 including the declaration clause that limits it:

> "For this identifiability calculation, keep three logically distinct binary
> factors:
> ```text
> H_typed
>   = C^2_flux
>     tensor C^2_chirality
>     tensor C^2_record.
> ```
> They represent:
> ```text
> flux:
>   the declared zero/one local unit-character source sector;
> chirality:
>   the left/right factor of the vectorlike source;
> record:
>   the two-endpoint comparison factor.
> ```
> **This reduced tensor product is declared for the calculation. It is not the
> complete Dirac/Fock/environment carrier.**
> Let
> ```text
> Q = (I-Z_flux)/2.
> ```
> …
> **The two real flux-conditioned record-changing directions are**
> ```text
> W_X = Q tensor I_chi tensor X_record,
> W_Y = Q tensor I_chi tensor Y_record.
> ```
> All four commute with the flux projector and are vector-`U(1)` neutral as
> operators. The `M` plane rotates under chiral rephasing; the `W` plane rotates
> under record-basis rephasing."

and what it does with them, :66-99:

> "On the real coefficient space
> ```text
> V = span_R{M_X,M_Y,W_X,W_Y},
> ```
> the independent infinitesimal rephasings act as … For a real symmetric
> quadratic form `G`, invariance requires … The exact linear system on the
> ten-dimensional symmetric-matrix space has rank `8` and nullity `2`. Its
> invariant forms are
> ```text
> G = diag(kappa_M,kappa_M,kappa_W,kappa_W).
> ```
> Thus the typed symmetries permit independent source-odd and record-write
> normalizations. **Positivity restricts their signs; it does not force
> `kappa_M=kappa_W`.**"

```text
PRODUCER OUTPUT, quoted (flux gate :67-72):
  "the conditional integrated record-changing holonomy is
     K_write,rel = (pi/2) Q_Sigma tensor Y_R
   up to endpoint rephasing and orientation reversal."
  ==> A REAL FLUX-CONDITIONED RECORD-CHANGING DIRECTION: a flux projector
      tensored with an equatorial record generator, fixed up to record-basis
      rephasing.

CONSUMER ARGUMENT, quoted (invariant-form gate :55-60):
  "The two real flux-conditioned record-changing directions are
     W_X = Q tensor I_chi tensor X_record,
     W_Y = Q tensor I_chi tensor Y_record."
  with Q = (I-Z_flux)/2 (:45), spectrum {0,1}, on "the declared zero/one local
  unit-character source sector" (:29-30).
  ==> A REAL FLUX-CONDITIONED RECORD-CHANGING DIRECTION: a flux projector
      tensored with an equatorial record generator, and the gate's own note
      that "the `W` plane rotates under record-basis rephasing" (:63-64).
```

**GRADE: MATCH.** Stated plainly, as commissioned: **the flux gate's output type
IS the input type of a consumer in this corpus, and the consumer is the typed
charge-chiral-record invariant-form gate.** The two texts agree noun for noun.
Both call the object a *flux-conditioned record-changing* direction. Both build
it as a flux projector of spectrum `{0,1}` tensored with an equatorial generator
on a two-endpoint record factor. Both declare the same flux sector in the same
words — the flux gate at :22-30 ("the declared zero-or-one local unit-character
source-crossing sector"), the invariant-form gate at :29-30 ("the declared
zero/one local unit-character source sector"). And **both** say the record-side
axis is fixed only up to rephasing: the flux gate "up to endpoint rephasing and
orientation reversal" (:74), the invariant-form gate "the `W` plane rotates under
record-basis rephasing" (:63-64). The invariant-form gate's consumption is not
nominal — it spans `V` on these directions and solves an exact invariance system
on them.

**THE ONE DIFFERENCE, DISPLAYED AND NOT SUPPRESSED.** The consumer's carrier
carries a **third** factor, `C^2_chirality`, which the flux gate's output does
not mention; the consumer's `W` directions carry `I_chi` in that slot. Whether
inserting an identity on a factor the producer never declared is a type identity
or a type extension is **not decided by either text**, and writing the map that
carries one to the other would be authoring the missing coercion. **It is barred
and was not done.** The grade above is read off the two displayed nouns and
constructions, which agree; the residual arity question is
**INDETERMINATE-AT-BYTES** and is recorded here rather than absorbed into the
grade. Under the stricter reading the row is MATCH-UP-TO-A-DECLARED-IDENTITY-SLOT.

**DOES THE CORPUS ANYWHERE COMPOSE THEM? NO.** SWEEP B, run by me and complete
over the two artifacts: the flux gate names `TYPED_CHARGE`, `W_X`, `W_Y`,
`invariant form`, or `chiral` **zero times**; the invariant-form gate names
`SOURCE_FLUX`, `Q_Sigma`, `K_write`, `U_write`, `Y_R`, `onset`, `holonomy`, or
`BOUNDARY_RECORD` **zero times**. The invariant-form gate **declares its own flux
factor** (`Q = (I-Z_flux)/2`, :45) rather than importing the flux gate's
`Q_Sigma`. Two gates written the same day, on the same declared sector, producing
and consuming the same object, with **no citation in either direction**. And
SWEEP C: **no ruling of the twenty-eight names either gate.**

---

#### ★★ THE SECOND MATCH, ON A DIFFERENT ONE OF THE THREE OUTPUTS.
#### C-3 — THE FORCING CRITERION, and C-16 — THE JUNCTION'S REQUIRE ROWS.

The match is on **O-C**, the `U_chi` family, not on the operator.

```text
PRODUCER OUTPUT, quoted (flux gate :96-101 and :115-121):
  "The family  U_chi = (I-Q_Sigma) tensor I_R + exp(-i chi) Q_Sigma tensor
   (-iY_R)  has the same conditional record projectors for every `chi`."
  "Therefore the current conclusion is only:  chi is not fixed by the
   conditional record projectors.   Its gauge-invariant physical effect is open."
  ==> AN ADMISSIBLE SET FOR THE QUANTITY chi, DISPLAYED AS NOT A POINT.

CONSUMER ARGUMENT, C-3, quoted whole from its own primary bytes
  (STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md :54-58, re-read at the
  cited line):
  "Forcing has ONE criterion (the admissible set is a point) and THREE shapes: a
   single require with a unique root; EXACT JOINT DETERMINATION (n conditions, n
   freedoms — the marker/C_R case, which the reviewer's "two ways" silently folds
   in); and overdetermined joint determination (surplus conditions). The miscount
   is harmless here but would matter in a registry of forcing candidates:
   exact-joint cases are forcing candidates too."
  ==> AN ADMISSIBLE SET, with a count of conditions and a count of freedoms.

CONSUMER ARGUMENT, C-16, quoted (STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md
  §2 :128-139): every require row is of one shape — "the admissible set is X".
  R-1: "one condition (single-valued closure) whose admissible set is the
  integer lattice ℤ — a CLASS, not a point."
  ==> THE SAME ARGUMENT TYPE AS C-3.
```

**GRADE: MATCH, against both consumers.** The flux gate's third output is an
admissible set for a quantity, displayed by the gate itself as not a point
("chi is not fixed by"); C-3 and C-16 consume admissible sets and return
point/not-a-point together with which of three shapes produced the verdict.

**A SECOND INSTANCE OF THE SAME MATCH, ON THE SAME GATE.** The equatorial class
is also an admissible set: ":67 — Endpoint rephasing rotates the equatorial pair
`(v_x,v_y)`", and `STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md`
:136 (seal OK) types it as such at bytes: "**Equatorial class analytically fixed;
the `Y_R` axis is a convention up to rephasing/orientation.**" A class, not a
point.

**DOES THE CORPUS ANYWHERE COMPOSE THEM? NO.** No artifact in either root runs
the forcing criterion on `chi` or on the equatorial class. SWEEP A: the only
artifacts naming the flux gate's object in prose are the onset gate and
EARN_THE_TRACE, and neither is C-3 or C-16. SWEEP C: no ruling of the
twenty-eight names `K_write`, `Q_Sigma`, or `record-changing holonomy`. C-3's own
defining sentence names its exemplar — "the marker/C_R case" — and that is a
different producer's output entirely (see §3).

---

#### THE MISMATCHES, EACH WITH ITS LOCUS NAMED

**C-1 — THE ONSET GATE.** Argument quoted whole from primary bytes,
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` :21-36 and :69-72:

> "Let the source, designated record subsystem, and required environment form one
> closed Hilbert space. From one common ready state `|r>`, the two
> source-conditioned closed-dilation unitaries are `U_0(tau), U_1(tau)`." …
> "arccos |<r|W(tau_*)|r>| <= integral_cell d tau Delta H_W(tau)/hbar."

**GRADE: MISMATCH. LOCUS: THE DILATION AND THE τ-PARAMETER.** `U_write` is a
single endpoint unitary on **two** factors (flux ⊗ record); the onset gate asks
for an **ordered pair** of **τ-parametrized** unitaries on **three** factors
(source ⊗ record ⊗ required environment) plus a common ready state plus a
τ-integration interval. Two arities short and one parameter short.
**THE RECORD-SIDE GENERATORS NEVERTHELESS COINCIDE, AND I DISPLAY THAT BECAUSE IT
IS THE CLOSEST THE TWO LANES COME:** the flux gate's `Q_Sigma = 1` branch acts by
`(-iY_R)` (:85-86) and the onset gate's relative unitary is `U_0^dagger U_1 =
-iY` (:129). Writing the dilation that would carry either to the other would be
authoring the missing intermediate object. **Barred, not done.** The row remains
MISMATCH.

**BUT THE ONSET GATE NAMES THE FLUX GATE'S OBJECT AS ITS OWN NEXT STEP, AND THIS
IS THE Q1 FINDING THE ROSTER ALONE HIDES.** Quoted whole, :168-173, including the
bar sentence:

> "## Exact next gate
> Construct the source-flux-conditioned record-changing operator, then derive a
> complete action that fixes its source-conditioned identity phase, post-closure
> pointer block, causal cell, and durability map. No geometric uncertainty budget
> may be substituted for physical action without a theorem."

This is the **only** occurrence of "source-flux-conditioned" in prose anywhere in
either root (SWEEP A: 1 distinct artifact). So the corpus **does** name a receiver
for the flux gate's output — and the receiver is not the onset gate itself but
"**a complete action**" that would fix, of the constructed operator, its
(i) source-conditioned identity phase, (ii) post-closure pointer block,
(iii) causal cell, and (iv) durability map. Item (i) is exactly the flux gate's
own `chi`, and the flux gate's own next-gate clause, :140-144, takes up the same
baton: "Determine from the complete charge/CPT/source branch whether `chi` is
forbidden, fixed, or physically irrelevant to the final response. **Do not set it
to zero merely because the record-only reduced density cannot see it.**"

```text
*** THE NAMED CONSUMER OF THE FLUX GATE'S OUTPUT IS "A COMPLETE ACTION",
    AND IT DOES NOT EXIST.  It is not typed, not built, and carries no
    argument slot at bytes.  The flux gate books its absence itself:
      complete_physical_write_operator_derived = false        (:163)
      complete_source_record_environment_operator_derived = false (:165)
      physical_dynamical_action_fixed = false                 (:164)
    GRADE FOR THE NAMED RECEIVER: A-PREREQUISITE-IS-UNBUILT — the
    prerequisite being the complete action itself.
```

**C-6 — CS-1, THE PROJECTOR PROMOTION. The closest shape in the closure lane, and
it still misses.** Argument quoted whole from primary bytes,
`STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` :116-123 (seal OK, Convention B),
with the WBC's own scope limit at :112-113 carried: *"Sweep basis: the tasked
stock of §1, read at the cited bytes; this typing claims exhaustiveness at that
displayed sweep only."*

> "CS-1 THE PROJECTOR PROMOTION (E1 C4 :339-352, frozen input, reviewer-confirmed
> in exact Q(sqrt2)): at full tau_R, lambda tau_R in {0, +pi, -pi}, on the ball
> where M(t) = 1, exp(-i lambda tau_R S) = (+1,-1,-1)·I EXACTLY (S^2 = I);
> sum_lambda w_lambda phase_lambda = 1 = sum_lambda |w_lambda| (l1 step TIGHT,
> zero-sum weights buy NO decay); operator level: sum_lambda w_lambda u_lambda =
> m_0(I-P) + 1·P = P. Re-pinned WB2 (linear in P; no idempotency needed).
> GRADE: exact, WEIGHT-SUM-level (needs the sum over lambda)."

**GRADE: MISMATCH, on two loci, and I name both because the shapes are cognate
enough that a reader will otherwise see a match.** (i) **THE WEIGHTS.** CS-1's
`w_lambda` are **scalars**, and the clause's own l1 identity `sum_lambda
|w_lambda| = 1` is a statement about scalars; `U_write`'s coefficients are
**operators on the record factor** (`I_R` and `-iY_R`), for which that identity is
not stated and is not available. (ii) **THE CARRIER.** CS-1 lives on the
closure's space, fixed at C-15 as quadratic forms on `Q = D(h_0) = H^1(R^3) ⊗
C^4`, localized "on the ball where `M(t) = 1`"; `U_write` lives on
(flux sector) ⊗ (two-level primitive record factor). Nothing in either text
identifies those carriers. The resemblance is that both are two-term operator
sums over a projector and its complement — a **shape**, not a type.

**C-7 … C-14 — CS-2 … CS-9, AND C-15 — THE ADMITTED ASSET CLASS `H(c)`.
GRADE: MISMATCH, uniformly, for one displayed reason.**

```text
CONSUMER ARGUMENT TYPE, quoted (C-15, carried at
  STAGE8_CERTIFICATION_RULES_O8SR_V001.md :255-259 — SECOND-HAND, the W3/CERT
  primary lies outside the readable roots):
  "H(c) := { quadratic forms t on Q = D(h_0) = H^1(R^3) (x) C^4 :
             |t[psi, psi]| <= 2c ||h_0 psi|| . ||psi||  for all psi in Q } ,
   c = sqrt(3) ; the C-L2 generator's q_t lies in H(sqrt3)"
  consumed at trace/HS grade.

FLUX GATE OUTPUT TYPE, quoted (:71, :83-86): a Hermitian generator and a
  unitary on (flux sector) ⊗ (primitive record factor), both finite-rank
  objects on a declared two-level factor.

NEITHER K_write,rel NOR U_write NOR U_chi IS A QUADRATIC FORM ON H^1(R^3) ⊗ C^4.
```

Row-specific loci where a reader might expect otherwise, named rather than
banded: **CS-6** takes `lambda_max` and a **time extent** `tau_R` and states
"lambda_max tau_R = pi IS the first-orthogonality criterion — one origin, not two
derivations" (:154-161); the flux gate also reaches a first-orthogonality
condition (`theta = pi/2`, :62-63) — but that condition is an **intermediate** of
the flux gate, not its output, and comparing the two would be approaching a
magnitude. **[MAGNITUDE-FENCED — not compared, not computed.]** **CS-8** takes
`J_FS,rel` and `Delta S_record`, which are the **onset and bridge** gates'
outputs, not the flux gate's; the flux gate produces neither.

**C-4 — THE ALLOW/REQUIRE CONSTRAINT (`CELL_CONSTRAINT_ONLY`).** Argument quoted
from `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md` :31-41 (seal OK):
"**It constrains when declared support can become a durable public record.**"
**GRADE: MISMATCH.** A unitary on two factors is not declared support.

**C-5 — THE MARGINAL PUBLIC-CLOSURE RULE.** Argument: the recoverable set of
admissible public record cells — a half-line of allowed durations `T_R`.
**GRADE: MISMATCH.** A record-changing generator is not a set of causal cells.
*(Its primary, `BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md`, carries NO
SIDECAR IN EITHER FORM — FLAG-1. The row is graded one confidence step lower.)*

**C-2 — THE FLUX GATE ITSELF.** Not tested: it is the producer of the row.

#### THE TWO REMAINING NEW CONSUMERS, GRADED CANNOT-DETERMINE

**C-18 — THE BID ACTIVE-HANDLE CONTROL GATE.** Primary:
`BID_ACTIVE_HANDLE_CONTROL_GATE_V001.md`, dated 2026-07-23, **carrying NO SIDECAR
IN EITHER FORM IN EITHER ROOT — FLAG-1**. Argument quoted whole
from :39-52 including the sentence that makes it conditional:

> "The strict route requires a source/access carrier with orthogonal projectors
> `P_h^S` and a controlled record generator
> ```text
> B_controlled
>   =sum_(h in {M,Q,G}) P_h^S tensor B_h
> ```
> on the single-active-handle sectors. For a source superposition, this operator
> entangles source support with orthogonal record endpoints. **For the pure
> charged branch, `P_Q^S` must follow from the compact charge-access source
> operator before the `Q`-conditioned interval is used.**"

**GRADE: CANNOT-DETERMINE-AT-BYTES.** The demanded object `sum_h P_h^S ⊗ B_h` is
of `U_write`'s exact shape — a sum of source-side projectors tensored with
record-side operators — and this gate **already consumes the flux gate's input**
`Q_Sigma` at its own :67 (`P_ch=1_(R\{0})(Q_Sigma)`, "which reduces to
`Q_Sigma^2` only on the vacuum-plus-one-excitation quotient", :70). But its
projector family is indexed over **three handles** `{M,Q,G}` on a *source/access*
carrier, while `Q_Sigma` is a **two-valued flux** operator; **no text in either
root says whether the flux projector is one of the `P_h^S`.** Deciding it would
require identifying the flux sector with the charge-access handle sector, which no
artifact does. INDETERMINATE.

**C-19 — THE REDUCED SOURCE-RECORD GENERATOR STRUCTURE GATE.** Primary:
`SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md`. Argument quoted whole from
:15-42 including its own disclaimer:

> "For this structure calculation only, declare
> ```text
> H_reduced = C^2_source-grading tensor C^2_record-endpoint.
> ```
> All suppressed spin and multiplicity factors are assumed to carry the identity.
> **This reduced factorization is not derived from the complete closure carrier.
> Consequently the computed dimensions are not dimensions of the full `G_closure`
> operator space.**
> Let `Z_S = source left/right grading, Z_R = record endpoint grading`. …
> On the real vector space of all 16 Hermitian Pauli products, impose
> `{G,Z_S tensor I}=0, {G,I tensor Z_R}=0`."

**GRADE: CANNOT-DETERMINE-AT-BYTES.** The consumer takes a **Hermitian operator
on a two-factor qubit ⊗ qubit carrier whose second factor is a record endpoint
factor** — which is `K_write,rel`'s arity and grade exactly. The undecided point
is the **first** factor: the consumer's is a *source left/right grading* `Z_S`,
the producer's is a *flux sector* `Q_Sigma`. No text identifies them. Testing
whether `K_write,rel` lies in the displayed kernel would be **performing the
composition**; it is barred and was not done.

### §1.3 — Q1 TALLY

```text
CONSUMER ROWS TESTED AGAINST THE FLUX GATE'S OUTPUT                     19
  MATCH .....................................................  3 rows, 2 objects
     C-17 invariant-form gate   × O-A (K_write,rel)     [NEVER ENUMERATED BEFORE]
     C-3  forcing criterion     × O-C (the U_chi family)
     C-16 junction require rows × O-C (the U_chi family)
  MISMATCH .................................................. 14
     C-1, C-4, C-5, C-6 … C-14 (CS-1 … CS-9), C-15
  CANNOT-DETERMINE-AT-BYTES ..................................  2
     C-18 active-handle control gate, C-19 generator structure gate
  (C-2 is the producer and is not graded against itself.)

*** THE HEADLINE Q1 FINDING, STATED PLAINLY AS COMMISSIONED ***
  YES.  THE FLUX GATE'S OUTPUT IS THE INPUT TYPE OF A CONSUMER IN THIS CORPUS.
  Its Hermitian generator K_write,rel = (pi/2) Q_Sigma ⊗ Y_R is, noun for noun
  and construction for construction, one of the "two real flux-conditioned
  record-changing directions" W_X, W_Y that the typed charge-chiral-record
  invariant-form gate spans its coefficient space V on.
  AND THE CORPUS NOWHERE COMPOSES THEM.  Zero citations in either direction;
  the consumer declares its own flux projector instead of importing the
  producer's; no ruling of the twenty-eight names either gate.
  Separately, the gate's chi-family is an admissible set and TWO admissible-set
  consumers stand unfed by it.
```

---

## §2 — Q2: THE THREE COMPOSABLE-NEVER-COMPOSED JOINS, AT BYTES

Identified at bytes, not inherited. **No composition below is performed.** Each
"what it would yield" row reports the two displayed types and what the corpus's
own text says the join would discharge or supply; taking the join is a principal
act and is barred to me absolutely.

### §2.0 — THE WITHDRAWN PRODUCER, VERIFIED AT BYTES, WITH ITS EXACT STATUS

Two of the three joins rest on one producer. It is
`PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md` — **the carrier derivation**.
Its output, quoted whole from :23-38 **including the scope clause that
immediately follows and cuts against it**:

> "Under those hypotheses, zero carried dimensions cannot represent the non-null
> action marker, and a bounded one-dimensional interval has no nontrivial
> connected reversible flow. A primitive faithful carried block is therefore a
> real two-plane. Its normalized orientation generator obeys
> ```text
> J^2 = -I.
> ```
> Together with the endpoint population coordinate, the capacity body is the
> three-dimensional Bloch ball. Its real order-unit space is `Herm_2(C)`, and the
> primitive observable algebra is `M_2(C)`.
> **This is a branch-scoped carrier result. It does not prove that a complete
> multi-handle record cell, its environment, or the `g,A,psi` carrier contains
> only two states.**"

Its own claim, :105-109: `primitive_single_handle_real_carrier_dimension = 2`;
`primitive_single_handle_order_unit_dimension = 4`;
`primitive_single_handle_record_algebra = M_2(C)`;
`primitive_comparator_kinematics_derived = true`.

**THE WITHDRAWAL, VERIFIED AT BYTES.** The successor is
`PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` (Date: 2026-07-23; seal
OK, Convention A). It lists the carrier result among its inputs and then declines
it. Quoted whole, :45-51, including the neighbouring sentences so the clause is
not cut:

> "**The first item is branch-scoped. The affine irreducibility, Bloch-ball
> geometry, and `M_2(C)` effect algebra used in older wording are not derived
> here.** The second item imports the ordinary quantum phase law and does not
> derive `hbar`. The third fixes the primitive character after that phase
> coordinate has been declared; it does not determine an electromagnetic response
> strength. The fifth item is standard quantum kinematics imported for this
> conditional bridge; it is not supplied by V156."

and books it three times in its own status block, :208-210:

> "```text
> unique_primitive_carrier_derived = false
> bloch_ball_derived = false
> M2C_algebra_derived = false
> ```"

and once more inside its **"Not established"** block, :180-182, quoted whole in
the relevant part: "the V156 boundary hypotheses are universal; affine
irreducibility or one-plane exhaustion; **the Bloch ball or M_2(C) algebra**;".

```text
*** EXACT STATUS OF THE WITHDRAWN PRODUCER ***

FILE            PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md
SEAL            NO SIDECAR IN EITHER FORM, IN EITHER ROOT.
                Probed: <stem>.md.seal.sha256 — ABSENT in both roots.
                Probed: <stem>.seal.sha256    — ABSENT in both roots.
                It is one of only two members of the primitive-gate family that
                carry neither convention (the other is
                BID_ACTIVE_HANDLE_CONTROL_GATE_V001.md, the C-18 / U-2 row).
                It also carries NO "Date:" line, where every sealed member of
                that family carries one (all 2026-07-23/24) — including the
                other unsealed member, which is dated 2026-07-23.  The carrier
                derivation is therefore the ONLY artifact cited in this
                commission that carries neither a seal nor a date.
ASSERTED BY ITSELF    primitive_single_handle_record_algebra = M_2(C)   (:107)
                      primitive_comparator_kinematics_derived = true    (:109)
WITHDRAWN BY THE SUCCESSOR  M2C_algebra_derived = false                 (:210)
                            bloch_ball_derived = false                  (:209)
                            unique_primitive_carrier_derived = false    (:208)
                            — bridge gate V002, SEALED, seal OK.
INDEPENDENTLY CARRIED ON SEALED BYTES  STAGE8_INGREDIENT_CENSUS_O17SR_V001.md
                      :460-461 (seal OK, Convention B), quoted whole:
                      "HIT 3 — the carrier itself (A-4). TAKES six declared
                       hypotheses; YIELDS the record algebra M_2(C) and the real
                       two-plane carrier. TYPE: RULE."

=> THE STATUS IS: ASSERTED BY AN UNSEALED PRODUCER, WITHDRAWN BY A SEALED
   SUCCESSOR, AND THE OUTPUT TYPE INDEPENDENTLY CARRIED ON A THIRD SEALED
   ARTIFACT THAT DOES NOT RE-DERIVE IT.  The withdrawal is a status flag, NOT a
   refutation: nothing at bytes says M_2(C) is WRONG; the successor says it is
   NOT DERIVED HERE.  THAT DISTINCTION IS LOAD-BEARING FOR BOTH GRADES BELOW AND
   I DO NOT COLLAPSE IT.
```

**AND THE ONE RULE THAT MIGHT HAVE BARRED IT DOES NOT EXIST.** I searched
`LOCKED_PROCESS.md` (ROOT-2, seal OK) for a rule barring consumption of an
unsealed artifact. It carries the LOCAL-SIDECAR RULE (:123-131), the PRE-SEAL PIN
CHECK (:253), the SPAN-COVERAGE RULE (:426-435), the OBJECT-NAMES PROBE
(:437-444), the WRITER-EXCLUSION RULE (:446-450), and STATE THE GLOB (:452-458).
**None of them bars consuming an unsealed artifact.** EARN_THE_TRACE's
"unsidecared and NOT consumed" is therefore a **lane practice displayed once**,
not a rule of record — which matters, because the same practice is what caused
the §0.1 misfire.

---

### §2.1 — JOIN-1. THE CARRIER DERIVATION → THE FLUX GATE.

```text
THE TWO TYPES, QUOTED.

PRODUCER OUTPUT (PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md :25-34):
  "A primitive faithful carried block is therefore a real two-plane. Its
   normalized orientation generator obeys  J^2 = -I.  Together with the endpoint
   population coordinate, the capacity body is the three-dimensional Bloch ball.
   Its real order-unit space is `Herm_2(C)`, and the primitive observable algebra
   is `M_2(C)`."

CONSUMER ARGUMENT (SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :47-51):
  "For an affine constant-axis representative ON THE PRIMITIVE RECORD FACTOR,
     h' = v_x X + v_y Y + v_z Z."
```

**WHAT THE COMPOSITION WOULD YIELD.** The object: **the primitive record factor
itself, identified** — the two-level factor whose real order-unit space is
`Herm_2(C)` and whose observable algebra is `M_2(C)`. Its type: a **carrier**,
supplied into a slot the flux gate names but never fills. What it would
**discharge**: the flux gate's phrase "the primitive record factor" is, at the
gate's own bytes, an **undischarged reference** — the gate writes `X, Y, Z` and
`Y_R` and `I_R` on a factor it never identifies, and nothing in the gate says
which object that factor is. The join would supply that object and would give
`Y_R` a derived home rather than a declared one. What it would **not** discharge:
**not one** of the flux gate's own negative flags. `complete_physical_write_
operator_derived = false` (:163), `complete_source_record_environment_operator_
derived = false` (:165), `physical_dynamical_action_fixed = false` (:164),
`gauge_invariant_phase_effect_derived = false` (:162) all survive the join
untouched, because the carrier derivation supplies a factor and none of those
flags is about the factor.

**WHY IT HAS NEVER BEEN PERFORMED.**
**GRADE: A-PREREQUISITE-IS-UNBUILT.**
**THE PREREQUISITE, NAMED:** *the derivation of the primitive record algebra
`M_2(C)` and of a unique primitive carrier at the current authority head.* It is
booked unbuilt three times over, at `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_
GATE_V002.md` :208-210 (`unique_primitive_carrier_derived = false`,
`bloch_ball_derived = false`, `M2C_algebra_derived = false`), by the sealed
successor to the unsealed producer that asserts it.

**IT IS NOT BLOCKED BY A RULE:** no clause in either root, and no clause in any of
the twenty-eight rulings (SWEEP C, 0/28 on the carrier pattern), forbids the join.
**IT IS NOT BLOCKED BY A PROOF:** nothing at bytes proves the flux gate's
"primitive record factor" is *not* the carrier derivation's two-plane; the
successor's clause is "**are not derived here**", which is a scope statement about
that gate, not a negation. **AND NOBODY DID IT:** SWEEP B, complete over the two
artifacts, returns zero citations in either direction; the flux gate is referenced
by name in 45 files (SWEEP A) and the carrier derivation appears in none of them
as a supplier.

---

### §2.2 — JOIN-2. THE CARRIER DERIVATION → THE ONSET GATE'S REALIZATION CLAUSE.

```text
THE TWO TYPES, QUOTED.

PRODUCER OUTPUT: as JOIN-1.

CONSUMER ARGUMENT (BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md
  :110-116), quoted whole:
  "## Exact two-state realization
   ON THE PRIMITIVE RECORD FACTOR, take
     H_W = epsilon_rel Y,
     epsilon_rel tau_*/hbar = pi/2."
```

**WHAT THE COMPOSITION WOULD YIELD.** The same object as JOIN-1 — the identified
two-level record factor — supplied into the onset gate's **realization** slot,
not its **declaration** slot. What it would **discharge**: the realization
clause's own undischarged reference, exactly as in JOIN-1. What it would **not**
discharge, and this is the sharper half: **it would not close the gate's own
internal type tension.** The gate *declares* its domain at :21-25 as "the source,
designated record subsystem, and required environment form one closed Hilbert
space" — three factors — and *realizes* it at :112 "On the primitive record
factor" — one factor. Supplying the record factor fills the realization slot and
leaves the declaration two factors short. The gate books that gap itself, :164:
`the complete source-record-environment action` under **"Not established"**, and
:189 `physical_durability_derived = false`.

**WHY IT HAS NEVER BEEN PERFORMED.**
**GRADE: A-PREREQUISITE-IS-UNBUILT.** Same prerequisite, same three flags at
`PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` :208-210. Not blocked by
a rule; not blocked by a proof. **A SECOND, INDEPENDENT REASON IS ALSO AT BYTES
AND I DISPLAY IT RATHER THAN CHOOSE BETWEEN THEM:** the onset gate names
`M_2`, `carrier`, `CAUSAL_INCIDENCE`, `Omega_c`, `PRIMITIVE_REVERSIBLE`, or
`FORMATION` **zero times** — so nobody did it either. Where two reasons hold, both
are reported; the prerequisite is the primary grade because it is the one the
corpus books in a status flag.

---

### §2.3 — JOIN-3. THE CELL ASSIGNMENT → THE ALLOW/REQUIRE CONSTRAINT.

```text
THE TWO TYPES, QUOTED.

PRODUCER OUTPUT (CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md, Date 2026-07-24,
  seal OK Convention A, :18-24), quoted whole:
  "For every primitive record-forming incidence `c`, the complete microscopic
   parent assigns one Lorentz-covariant causal cell `Omega_c` and one interaction
   density `L_c` such that
     support(L_c) is contained in Omega_c."
  and its own self-typing at :12-13, quoted whole:
  "This principle fixes the support and reuse law of a primitive incidence. It
   does not assume durability, select an outgoing state, supply a spectral
   measure, or compute a coupling."

CONSUMER ARGUMENT (STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  :31-41, seal OK Convention B), quoted whole:
  "The object called "allow/require" must be split into two differently typed
   objects.
   1. The allow/require constraint: real, argued, falsifiable, and already run
      against the program's own preferred result. IT CONSTRAINS WHEN DECLARED
      SUPPORT CAN BECOME A DURABLE PUBLIC RECORD. It is not, by itself, an
      absolute stiffness selector.
   2. The local minimality or saturation adoptions named after allow/require.
      These are local equality, least-member, or single-normalization moves.
      They are not instances of the allow/require constraint merely because they
      share the same vocabulary."
  Its status, :110-111: allow_require_constraint_retained_as_cell_constraint
  = true ; allow_require_standalone_selector_status = CELL_CONSTRAINT_ONLY.
```

**WHAT THE COMPOSITION WOULD YIELD.** The object: **a verdict** — whether the
declared, Lorentz-covariant support `support(L_c) ⊂ Omega_c` may become a durable
public record. Its type: the consumer's own codomain, a modal verdict on
durability, **not** a record and **not** a cell. What it would **supply**: the
allow/require constraint is typed of record as `CELL_CONSTRAINT_ONLY` and the cell
assignment is the corpus's one producer of a declared covariant support inside a
cell; the join would be the **only** place the corpus's cell constraint is applied
to the corpus's cell-typed declared support. What it would **discharge**:
**nothing on the alpha path.** The consumer's own bytes say it "is not, by
itself, an absolute stiffness selector" (:37-38), and the producer's own bytes say
it "does not assume durability, select an outgoing state, supply a spectral
measure, or compute a coupling" (:13-14). A verdict returned by this join would be
conditional on an object the producer books as underived — `complete_causal_
parent_derived = false` (CIS :107) — because it is "**the complete microscopic
parent**" that assigns `Omega_c` and `L_c`, and that parent does not exist.

**WHY IT HAS NEVER BEEN PERFORMED.**
**GRADE: NOTHING-BLOCKS-IT-AND-NOBODY-DID-IT.**
Both artifacts are in both roots. **Both seals verify** — the producer under
Convention A, the consumer under Convention B. Both types are displayed at
primary bytes. **No rule forbids the join** (SWEEP C: the two ruling hits on
`allow/require` are an appeal to a reading and a note of consistency evidence,
neither a rule about this pair). **No proof forbids it**: the nearest thing at
bytes is a *typing refusal about a different question*, and I quote it whole so it
is not mistaken for one — `STAGE8_PRODUCTION_VS_DESCRIPTION_O13SR_V001.md`
:229-237: "A support law is a containment (`support(L_c) subset Omega_c`) — an
INCLUSION. A reuse law is a prohibition on re-indexing. **Neither takes a making
as input nor returns a record as output.**" That adjudicates whether the cell
assignment *makes a record*; it does not adjudicate whether its declared support
may be fed to a constraint on declared support. **And nobody did it**: the
co-occurrence tests return only lane reports, never an application.

**THE PREREQUISITE IS NAMED HERE AND DELIBERATELY NOT MADE THE GRADE.** The
complete microscopic parent is unbuilt (CIS :107), so a join performed today
would return a verdict on a *schematic* support rather than an *instance*. That
limits what the join would be worth; it does not block the join, and the rubric
grades why the join was not performed. Grading this row
A-PREREQUISITE-IS-UNBUILT would over-report an obstruction the bytes do not
carry. **The distinction between JOIN-1/JOIN-2 and JOIN-3 is exactly this: there
the prerequisite is the producer's own claim, withdrawn; here the prerequisite is
downstream of the join and the join itself is unobstructed.**

### §2.4 — Q2 TALLY

```text
JOIN   PRODUCER → CONSUMER                       GRADE
J-1    carrier derivation → flux gate            A-PREREQUISITE-IS-UNBUILT
       prerequisite: M2C_algebra_derived = false / unique_primitive_carrier_
       derived = false, bridge gate V002 :208-210
J-2    carrier derivation → onset gate           A-PREREQUISITE-IS-UNBUILT
       (realization clause)                      same prerequisite, same flags
J-3    cell assignment → allow/require           NOTHING-BLOCKS-IT-AND-NOBODY-
       constraint                                DID-IT

BLOCKED-BY-A-RULE .......... 0 of 3
BLOCKED-BY-A-PROOF ......... 0 of 3
CANNOT-DETERMINE ........... 0 of 3

*** NOT ONE OF THE THREE IS BLOCKED BY A RULE OR BY A PROOF.  Two rest on a
    producer whose supply a sealed successor WITHDREW WITHOUT REFUTING, and whose
    own file carries no seal in either sidecar convention.  The third is
    unobstructed and simply was never performed. ***
```

---

## §3 — Q3: THE ONE PERFORMED COMPOSITION, EXAMINED

### §3.1 — WHAT IS COMPOSED WITH WHAT

**THE PRODUCER.** `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md`
(Date: 2026-07-23; seal OK, Convention A). It derives a balanced ready state.
Quoted whole, :105-119, including the sentence that fixes the derivation's status:

> "```text
> (2p-1)^2,
> ```
> attained at `theta = pi` modulo `2 pi`. Therefore exact orthogonality under one
> primitive phase character is possible if and only if
> ```text
> p = 1/2,
> theta = pi mod 2 pi.
> ```
> **The balanced state and the first half-turn are deductions from the declared
> orthogonality task. This calculation contains no alpha value. Historical target
> blindness is not claimed.**"

**THE CONSUMER.** `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (seal
OK, Convention A). Its ready-state slot, :23-29: "From one common ready state
`|r>`, the two source-conditioned closed-dilation unitaries are `U_0(tau),
U_1(tau)`." Its integral, :69-72 and :83-86: "arccos |<r|W(tau_*)|r>| <=
integral_cell d tau Delta H_W(tau)/hbar" and "`J_FS,rel := integral_cell d tau
Delta H_W(tau) >= pi hbar/2`."

**THE COMPOSITION ITSELF.** The bridge gate evaluates the onset gate's own
integral on its own derived state. Quoted whole, :121-154:

> "Use the centered Hilbert-space dimensionless generator
> ```text
> G = diag(-1/2,+1/2).
> ```
> On the forced balanced state,
> ```text
> <G> = 0,
> Delta G = 1/2.
> ```
> Along the primitive half-turn `0 <= theta <= pi`, the projective budget is
> ```text
> J_FS
>   = hbar integral_0^pi Delta G d theta
>   = pi hbar/2.
> ```
> The eigenphase gap of the same relative holonomy is `pi`, so its recoverable
> action-marker interval is
> ```text
> Delta S_record = pi hbar = 2 J_FS.
> ```
> **This agrees exactly with the sealed relative-onset gate's conditional value
> `J_FS,rel = pi hbar/2`. The factor of two is the difference between the
> projective speed, set by the generator uncertainty, and the gap between the two
> generator eigenvalues.**"

```text
*** WHAT EXACTLY IS COMPOSED ***
  The bridge gate DERIVES an object of the consumer's argument type (a normalized
  ready state on a two-level record factor, forced balanced), PLACES it in the
  consumer's ready-state slot, and EVALUATES THE CONSUMER'S OWN INTEGRAL ON IT.
  It is the only composition in this corpus that is (i) performed of record,
  (ii) between two distinct sealed artifacts, and (iii) wholly on bytes that
  carry seals verifying under one convention or the other.  Both seals: OK.

*** WHAT THE COMPOSITION PRODUCES ***
  J_FS = pi hbar/2 ; Delta S_record = pi hbar = 2 J_FS ; equivalently, along the
  balanced two-level geodesic, J_FS = |Delta S|/2 (:156-160).
  TYPE: a projective (Fubini-Study) budget and a recoverable action-marker
  interval, both displayed in units of hbar.  [The corpus displays these symbols;
  they are quoted, not computed.  No magnitude is approached.]
```

### §3.2 — WHAT THE CORPUS DOES WITH THE RESULT

Three things, all at bytes, and the second is the one that matters.

**(i) IT IS DISPLAYED AS AN AGREEMENT** — bridge gate :151, quoted above.

**(ii) THE BOUNDARY-CLOSURE LANE REACHES IT BY DIGEST AND THEN REFUSES IT.**
`STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` (seal OK, Convention B) carries it as
its own clause CS-8, :167-173, quoted whole:

> "CS-8 THE ONSET/ACTION BUDGETS AT THE CELL EDGE (ae2202e0 Z-2/Z-5, gate cc113a16
> at bytes): J_FS,rel >= pi hbar/2 — a LOWER bound, derived from the imported
> FS/Mandelstam-Tamm theorem, saturation ADOPTED not derived; |Delta S_record| =
> pi hbar with the bridge gate's bar ("No geometric uncertainty budget may be
> substituted for physical action without a theorem"); the entry-15 vacancy bars
> every magnitude-typed landing on today's booked surface (Z-7)."

**(iii) AN INDEPENDENT CHECK RE-LIMITS IT.**
`STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md` (seal OK, Convention B)
:135-142, quoted whole:

> "But it immediately limits that equality: it is not asserted for arbitrary
> weighted states, reducible multi-plane carriers, or non-geodesic evolution.
> **That limitation is decisive.** First orthogonality fixes the endpoint in the
> declared two-level/geodesic representative. It does not, by itself, exclude a
> non-geodesic physical trajectory, a larger carrier, a reducible carrier, or a
> complete source-record-environment dynamics that reaches the same orthogonal
> endpoint with larger relative Fubini-Study budget."

### §3.3 — THE LIMITING CLAUSES, QUOTED WHOLE

**THE RESTRICTION THE COMMISSION NAMES IS AT THE BRIDGE GATE'S :162-163, AND IT
READS EXACTLY AS REPORTED — THREE EXCLUSIONS IN ONE SENTENCE, ACROSS A LINE WRAP
WHICH I VERIFIED BY RE-READING AT BOTH LINES:**

> "This equality is not asserted for an arbitrary weighted state, a reducible
> multi-plane carrier, or a non-geodesic evolution."

**THE CONSUMER'S OWN LIMIT, :97-108, QUOTED WHOLE:**

> "The allow/require boundary is adopted to select first admissible record onset
> through a shortest **relative** projective path on the unique physical record
> cell. Conditional on this target-value-free, target-aware Level-1 rule,
> ```text
> J_FS,rel = pi hbar/2.
> ```
> **The lower bound is derived from the imported theorem. Saturation is adopted,
> not derived. Historical target blindness is not claimed.**"

**THE PRODUCER'S OWN LIMIT ON WHAT IT FIXED, :193-194, QUOTED WHOLE:**

> "In particular, this gate fixes a relative holonomy/action-marker interval. It
> does not yet prove which microscopic Hamiltonian and cell realize that marker."

**AND ITS "NOT ESTABLISHED" BLOCK, :177-191, QUOTED WHOLE:**

> "Not established:
> ```text
> the V156 boundary hypotheses are universal;
> affine irreducibility or one-plane exhaustion;
> the Bloch ball or M_2(C) algebra;
> the standard exp(i Delta S/hbar) phase law is derived from deeper dynamics;
> Delta S_record is either branch's complete microscopic Lagrangian action;
> the complete source-record-environment generator;
> orthogonal reduced record supports or physical durability;
> the causal-cell duration or volume;
> the chiral-odd source mass;
> the finite electromagnetic response;
> alpha.
> ```"

### §3.4 — IS THIS COMPOSITION A TEMPLATE?

**WHAT WOULD HAVE TO BE TRUE OF ANOTHER PRODUCER/CONSUMER PAIR FOR THE SAME MOVE
TO APPLY — READ OFF THE MOVE'S OWN BYTES, FOUR CONDITIONS:**

```text
T-1  THE CONSUMER MUST DISPLAY ITS OWN FUNCTIONAL IN EVALUABLE FORM.
     Here: "integral_cell d tau Delta H_W(tau)/hbar" (onset :71) and
     "J_FS,rel := integral_cell d tau Delta H_W(tau) >= pi hbar/2" (:83-86).
     The producer does not describe the consumer's rule; it RUNS it.

T-2  THE PRODUCER MUST DERIVE ITS OBJECT FROM THE CONSUMER'S OWN DECLARED TASK,
     NOT SUPPLY IT EXTERNALLY.
     Bridge :117-119, quoted whole: "The balanced state and the first half-turn
     are DEDUCTIONS FROM THE DECLARED ORTHOGONALITY TASK."  This is what
     distinguishes the one performed join from the three unperformed ones: in
     J-1, J-2 and J-3 the producer's object is derived on its OWN hypotheses and
     would have to be TRANSPORTED into the consumer.  Here it is not
     transported; it is deduced from the consumer's task.

T-3  BOTH MUST STAND IN ONE DECLARED REPRESENTATION.
     Bridge :37-39, quoted whole: "The bridge is evaluated in the standard pure
     `C^2` two-path comparison representation with its Hermitian inner product,
     Born overlap, and Fubini-Study convention."  Onset :112: "On the primitive
     record factor."  One representation, declared on both sides.

T-4  THE THREE EXCLUSIONS MUST NOT BITE: the state balanced, the carrier an
     irreducible one plane, the evolution geodesic (bridge :162-163, by
     negation).
```

**DOES THE CORPUS SAY ANYTHING ABOUT ITS GENERALITY?**

**IT DOES, AND WHAT IT SAYS IS THAT THE GENERALITY IS NOT ESTABLISHED.** Two
statements at bytes, and no third:

1. The bridge gate's **"Not established"** block, :180, first line, quoted whole:
   "**the V156 boundary hypotheses are universal;**" — universality of the
   hypotheses under which the move runs is expressly booked NOT ESTABLISHED. And
   :181, the next line: "**affine irreducibility or one-plane exhaustion;**" —
   i.e. the carrier condition T-4 depends on is itself not established.
2. The independent force check, :138: "**That limitation is decisive.**"

**AND THERE IS NO AFFIRMATIVE GENERALITY CLAUSE ANYWHERE.** SWEEP, run by me over
the bridge gate, the onset gate, the force check, and the flux gate, for
`template|applies whenever|same move|in general|generalis|generaliz`:
**ZERO HITS, all four files.** No artifact states a rule of the form "this move
applies whenever ___". SWEEP C: no ruling of the twenty-eight names `J_FS` or the
onset gate at all.

```text
*** Q3 VERDICT ON GENERALITY ***
  NOT A TEMPLATE OF RECORD.  The four conditions T-1 … T-4 are what I READ OFF
  the move's own bytes; they are a DESCRIPTION of the one performed instance, not
  a rule the corpus states.  The corpus does speak to the move's generality — in
  exactly two places — and in both it books the generality as NOT ESTABLISHED
  ("the V156 boundary hypotheses are universal" under Not established; "That
  limitation is decisive").  Stating T-1 … T-4 as a licence to apply the move
  elsewhere would be AUTHORING A RULE THE CORPUS DOES NOT CARRY.  It is barred
  and was not done: they are displayed as conditions the one instance satisfied.
```

---

## §4 — Q4: THE CENSUS OF UNDISPLAYED OUTPUT SIDES

**THE TEST, FIXED BEFORE APPLICATION.** An object is a census item when all three
hold: **(a)** it is typed of record as a consumer — some artifact states what it
takes; **(b)** its own defining text **produces** something; **(c)** **no**
artifact anywhere displays its **output type**.

### §4.1 — THE CENSUS

```text
*** COUNT: TEN. ***

CS-1 … CS-9 — THE NINE BOUNDARY-CLOSURE CLAUSES.
  Primary: STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md §2, :109-180 (seal OK).
  The section's own heading, :109, quoted: "SUB-TASK (1) — THE CLOSURE STRUCTURE,
  TYPED EXACTLY".  Every row states what it TAKES and assigns a GRADE.  NOT ONE
  ROW STATES AN OUTPUT TYPE.  O42SR §1.2 tabulates all nine by argument alone.
  Each defining text nevertheless produces:

  CS-1  PROJECTOR PROMOTION      (:116-123) produces THE PROMOTED PROJECTOR at
        operator level — "operator level: sum_lambda w_lambda u_lambda =
        m_0(I-P) + 1·P = P" — plus the l1-tightness statement.  OUTPUT
        UNDISPLAYED.
  CS-2  R.2 COLLAPSE DISPLAY     (:124-130) produces "V_{mu lambda}(0) - 1 =
        (phase_mu phase_lambda - 1)P = 0 on SAME-phase pairs, -2P on
        OPPOSITE-phase pairs", the weight bookkeeping "S_+ = 1/2, S_- = -1/2",
        and "tr A(0) = -2 tr(CPC) = -infinity".  OUTPUT UNDISPLAYED.
  CS-3  BOUNDARY-SUPPORTED COMMUTATOR (:131-138) produces "kappa_n :=
        ||[C_n, P]||_2^2 = 2 sum_i s_i(1-s_i)" and its family behaviour.
        [MAGNITUDE-FENCED: the divergence statement is quoted as the corpus
        writes it; no norm is computed.]  OUTPUT UNDISPLAYED.
  CS-4  POISONING CEILING        (:139-145) produces the determinant bound
        "|det_n(0)|^2 <= exp(-2 kappa_n)" and the resolvent ceiling.  OUTPUT
        UNDISPLAYED.
  CS-5  b_D PROFILE              (:146-153) produces b_D, the derived range
        "0 <= b_D <= 1 with equality ONLY at the diamond center", and THE RECORD
        VERTEX "J = -(Q b_D Q) tensor alpha_x" with its operator-norm bound.
        *** THE RECORD VERTEX IS AN OPERATOR, AND IT IS THE LARGEST UNDISPLAYED
        OUTPUT IN THE CENSUS. ***  OUTPUT UNDISPLAYED.
  CS-6  PHASE-SKELETON SATURATION (:154-161) produces "The exact monodromies
        (+1,-1,-1) and the parity grading of the dressed objects", which the
        clause says "are DERIVED of record from this one condition".  OUTPUT
        UNDISPLAYED.
  CS-7  PARITY GRADING           (:162-166) produces the swap law "G_{-+}(m) =
        G_{+-}(-m)" and the even/odd insertion strata "K_H = even-insertion
        strata, H_A = -i K_A = odd-insertion strata".  OUTPUT UNDISPLAYED.
  CS-8  ONSET/ACTION BUDGETS     (:167-173) produces A TYPED REFUSAL, not an
        object: "NOT A MAJORANT" (:245-252).  BOUNDARY ROW — see below.
  CS-9  LOCUS RULING             (:174-180) produces A BINDING MANDATE, not an
        object: "sharp consumption of M(t) and 1_{D_t} is MANDATED (D6')".
        BOUNDARY ROW — see below.

G_closure — THE COMPLETE RECORD-FORMING GENERATOR.  THE TENTH.
  Primary: PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md
  :29-40 (seal OK, Convention A), quoted whole:
    "The complete record-forming generator must act on the source, primitive
     record carrier, and all required boundary/environment degrees:
       G_closure acts on
         H_source tensor H_record tensor H_boundary/environment.
     It must generate source-conditioned distinguishable record states, stable
     pointer sectors, persistence, and recoverability or redundancy. A
     source-only mass block is not this complete generator and is not a record
     instrument."
  ITS DOMAIN IS DISPLAYED — three factors, named.  ITS CODOMAIN IS NOT: "must
  generate ... states, sectors, persistence, and recoverability" names FOUR
  HETEROGENEOUS THINGS AND NO TYPE.  OUTPUT UNDISPLAYED.

*** CENSUS COUNT: 10 ***
  of which the produced thing is a DISPLAYED OBJECT ................... 7
       (CS-1 … CS-7 — projector, collapse display, commutator, ceiling,
        record vertex, monodromies/parity, swap law)
  of which the produced thing is A VERDICT OR A MANDATE ............... 2
       (CS-8 refusal, CS-9 mandate)
  of which the produced thing is NAMED BUT UNTYPED .................... 1
       (G_closure)
```

### §4.2 — ROWS TESTED AND EXCLUDED, EACH WITH ITS REASON

```text
C-1  THE ONSET GATE.        EXITED THE CENSUS.  Consumer-typed by every
     commission through O36SR; ITS OUTPUT SIDE IS NOW DISPLAYED — the pair
     (U_0, U_1) at its own :119-131, typed at STAGE8_THE_JOIN_O42SR_V001.md
     §2.1 row P-6 (seal OK).  Fails test (c).
C-2  THE FLUX GATE.         EXITED THE CENSUS.  Same: its output side is
     displayed at O42SR §2.1 row P-5, :563-583, which quotes both K_write,rel
     and U_write.  Fails test (c).  ** THESE TWO ARE THE INSTANCE THE
     COMMISSION NAMES.  AT BYTES THEY WERE CENSUS ITEMS UNTIL O42SR AND ARE NOT
     NOW.  THE COUNT BEFORE O42SR WAS TWELVE; TODAY IT IS TEN. **
C-3  THE FORCING CRITERION. EXCLUDED.  Its output side is in its own defining
     sentence: it returns point / not-a-point and which of three shapes produced
     the verdict (:54-56).  Fails (c).
C-4  THE ALLOW/REQUIRE CONSTRAINT.  EXCLUDED.  Its codomain is displayed in the
     same clause as its domain — "can become a durable public record" (:35-36).
     Fails (c).
C-5  THE MARGINAL PUBLIC-CLOSURE RULE.  EXCLUDED.  Output displayed (it returns
     the boundary point of the recoverable set) AND it is typed as a producer.
     Fails (a) and (c).
C-15 THE ADMITTED ASSET CLASS H(c).  NOT COUNTED — CANNOT-DETERMINE.  It is an
     admission class; whether an admission class "produces" is not fixed by the
     bytes, and its primary lies outside the readable roots (SECOND-HAND).
C-16 THE JUNCTION'S REQUIRE ROWS R-1 … R-10.  EXCLUDED.  Each displays its own
     admissible set as its output ("the admissible set is the integer lattice
     ℤ — a CLASS, not a point").  Fails (c).
```

### §4.3 — A THIRD CLASS, FOUND IN THIS COMMISSION, REPORTED SEPARATELY AND NOT ADDED TO THE COUNT

The census counts objects **typed as consumers** with an undisplayed output side.
SWEEP A surfaced three artifacts with a **different** defect: they are **typed as
neither** — never enumerated as consumers by any commission, never enumerated as
producers — while **both** sides sit at bytes.

```text
U-1  TYPED_CHARGE_CHIRAL_RECORD_INVARIANT_FORM_GATE_V001.md  (seal OK)
     ARGUMENT DISPLAYED: the real flux-conditioned record-changing directions
       W_X, W_Y spanning V (:55-71).
     OUTPUT DISPLAYED: the invariant forms G = diag(kappa_M,kappa_M,kappa_W,
       kappa_W), nullity 2 (:90-95).
     APPEARS IN NO COMMISSION'S CONSUMER SET OR PRODUCER SET.  It is the
     consumer of §1.2's MATCH.
U-2  BID_ACTIVE_HANDLE_CONTROL_GATE_V001.md   [NO SIDECAR, EITHER FORM — FLAG-1]
     ARGUMENT DISPLAYED: B_controlled = sum_h P_h^S tensor B_h (:39-52).
     OUTPUT DISPLAYED: the full-star spectrum and the required-operator
       specification.  [MAGNITUDE-FENCED: the gate displays decimal amplitudes
       at :31-33; they are NOT reproduced here and were not used.]
U-3  SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md
     ARGUMENT DISPLAYED: a Hermitian Pauli product G on H_reduced (:15-42).
     OUTPUT DISPLAYED: the odd/odd kernel span and E_1, E_2 (:44-70).

THESE THREE ARE NOT ADDED TO THE COUNT OF TEN, because they fail test (a):
nothing types them as consumers, so they are not "typed ONLY as consumers".
THEY ARE THE COMPLEMENTARY DEFECT AND THEY ARE REPORTED, NOT ABSORBED.
```

### §4.4 — Q4 TALLY

```text
OBJECTS TYPED ONLY AS CONSUMERS WHOSE DEFINING TEXT ALSO PRODUCES ...... 10
  CS-1 projector promotion            CS-6 phase-skeleton saturation
  CS-2 R.2 collapse display           CS-7 parity grading
  CS-3 boundary-supported commutator  CS-8 onset/action budgets  [verdict]
  CS-4 poisoning ceiling              CS-9 locus ruling          [mandate]
  CS-5 b_D profile / the record vertex
  G_closure the complete record-forming generator                [untyped]

EXITED THE CENSUS WHEN O42SR DISPLAYED THEM ............................  2
  the onset gate, the flux gate  (count before O42SR: 12)

TYPED AS NEITHER, BOTH SIDES AT BYTES (complementary defect) ...........  3
  U-1 invariant-form gate, U-2 active-handle control gate,
  U-3 generator structure gate

*** THE GENERAL FORM OF THE BLIND SPOT, AND ITS SIZE ***
  THE FLUX GATE WAS NOT AN ISOLATED MISS.  THE WHOLE BOUNDARY-CLOSURE STRUCTURE
  IS TYPED ONE-SIDED: nine clauses stated as what they TAKE and graded on how
  exactly they take it, with not one output type displayed — although seven of
  them produce a displayed object and one of those objects (CS-5's record vertex
  J = -(Q b_D Q) tensor alpha_x) is an operator with a displayed norm bound.
  Adding G_closure, the count is TEN.
```

---

## §5 — REQUIRED BLOCKS

### CHOICE LEDGER

```text
CH-1  I ADDED THREE CONSUMER ROWS NO COMMISSION HAS ENUMERATED (C-17, C-18,
      C-19) AND TESTED THE FLUX GATE'S OUTPUT AGAINST NINETEEN ROWS, NOT
      SIXTEEN.  ALTERNATIVE REJECTED: test against O42SR's sixteen-row roster.
      REASON: the commission asks whether the output is the input type of ANY
      consumer in this corpus, and a roster assembled for a different question
      is not that set.  CONSEQUENCE IF REJECTED: the §1 headline MATCH
      disappears entirely — C-17 is the match — and Q1 returns "no consumer
      takes it", which is the answer three commissions would have given.  This
      is the single most consequential choice in the artifact and I put it first.

CH-2  I GRADED THE C-17 ROW **MATCH** DESPITE THE CONSUMER'S CARRIER HAVING A
      THIRD FACTOR (C^2_chirality) THAT THE PRODUCER'S OUTPUT DOES NOT MENTION.
      ALTERNATIVE REJECTED: grade CANNOT-DETERMINE on the arity difference.
      REASON: both texts name the object with the same noun phrase
      ("flux-conditioned record-changing direction"), build it the same way
      (flux projector of spectrum {0,1} tensor an equatorial record generator),
      declare the same flux sector in near-identical words, and both fix the
      record axis only up to rephasing.  The consumer's own W directions carry
      I_chi — an IDENTITY — in the extra slot.  CONSEQUENCE IF REJECTED: the row
      becomes the third CANNOT-DETERMINE and Q1's plain answer becomes "no".
      I display the arity question in place, at §1.2, rather than burying it,
      and I state the stricter grade there
      (MATCH-UP-TO-A-DECLARED-IDENTITY-SLOT) so a reader can take it.

CH-3  I GRADED THE U_chi FAMILY AS AN ADMISSIBLE SET, PRODUCING A SECOND MATCH
      AGAINST C-3 AND C-16.  ALTERNATIVE REJECTED: treat only the operators
      K_write,rel and U_write as the gate's output, making both rows MISMATCH.
      REASON: the gate itself displays the family as an object and states its
      own conclusion as a non-fixing ("chi is not fixed by the conditional
      record projectors"), which is a statement about a set's cardinality —
      exactly what C-3 consumes.  CONSEQUENCE IF REJECTED: MATCH count falls
      from 3 rows to 1.

CH-4  I GRADED J-3 **NOTHING-BLOCKS-IT-AND-NOBODY-DID-IT** RATHER THAN
      A-PREREQUISITE-IS-UNBUILT, THOUGH AN UNBUILT PREREQUISITE IS NAMED IN THE
      ROW.  ALTERNATIVE REJECTED: grade all three joins the same.  REASON: the
      unbuilt parent sits DOWNSTREAM of the join and limits the join's WORTH,
      not its performability; the rubric grades why the join was not performed.
      CONSEQUENCE IF REJECTED: the Q2 tally reads 3/3
      A-PREREQUISITE-IS-UNBUILT and the real asymmetry between J-1/J-2 and J-3
      is lost.  A reader who prefers uniformity can take it; §2.3 states the
      prerequisite explicitly so nothing is hidden by the choice.

CH-5  I DECLARED A CUTOFF ON THE TWO HIGH-COUNT SWEEP-A PATTERNS ("record-
      changing", 30 net; the filename citation, 45 net) AND OPENED NINE.  RISK
      CARRIED AND STATED: a consumer could hide among the members I named but
      did not open.  I do not claim exhaustiveness over that band.  The three
      DECISIVE patterns ("record-changing operator", "source-flux-conditioned",
      the cross-citation probes) were opened at EVERY hit and re-run RECURSIVELY
      — see FLAG-3.

CH-6  I READ STAGE8_THE_JOIN_O42SR_V001.md AS A PRIOR LANE REPORT, NOT AS
      AUTHORITY, AND RE-OPENED EVERY ROW OF IT I RELY ON AT ITS PRIMARY BYTES.
      CONSEQUENCE: two locator corrections (FLAG-2) and one correction to the
      COMMISSION'S OWN premise (§1.1) that would have been invisible had I
      inherited the report.

CH-7  I QUOTED "J_FS = pi hbar/2", "Delta S_record = pi hbar", "theta = pi/2",
      AND "K_write,rel = (pi/2) Q_Sigma tensor Y_R" AS THE CORPUS DISPLAYS THEM,
      AND I DID NOT REPRODUCE BID's closed-form root or scale expressions, nor
      BID_ACTIVE_HANDLE_CONTROL_GATE's decimal amplitudes at its :31-33.
      REASON: the fence bars computing a scale, root, eigenvalue or norm and
      bars approaching a magnitude.  Quoting a symbol the corpus writes in units
      of hbar or of pi is quotation; re-displaying a decimal amplitude or a
      Planck-unit scale would approach the magnitude.  Both treatments are
      marked in place.
```

### TOY_SEPARATION

```text
NO TOY WAS BUILT, RUN, OR REASONED FROM.  Every type in this artifact is read off
defining text at primary bytes in one of the two commissioned roots or the
twenty-eight rulings.  No model, no example, no illustrative instance, no
schematic, no worked case, no "suppose" was constructed anywhere.

THE FOUR PLACES A TOY OR A COMPOSITION WOULD HAVE BEEN TEMPTING, EACH DECLARED:

(1) §1.2, THE C-17 MATCH.  Writing the map that carries K_write,rel into the
    invariant-form gate's V — inserting the identity on the chirality slot —
    would settle the arity question cleanly.  IT WOULD ALSO BE AUTHORING THE
    MISSING COERCION AND PERFORMING THE COMPOSITION.  Barred twice over, and not
    done.  The residual is left INDETERMINATE-AT-BYTES.

(2) §1.2, C-19.  Testing whether K_write,rel lies in the displayed odd/odd kernel
    span_R{X_S⊗X_R, X_S⊗Y_R, Y_S⊗X_R, Y_S⊗Y_R} is a one-line check and it is
    EXACTLY THE COMPOSITION.  Barred.  Not done.  Row graded CANNOT-DETERMINE
    on the declared identity of the first factor, which is the honest ground.

(3) §1.2, C-1.  The flux gate's (-iY_R) and the onset gate's -iY are the same
    operator on the same factor.  Writing the dilation that would carry either
    to the other would be authoring the missing intermediate object.  Barred.
    Not done.  The coincidence is DISPLAYED and the row remains MISMATCH.

(4) §3.4, THE TEMPLATE QUESTION.  T-1 … T-4 are a DESCRIPTION of the conditions
    the one performed instance satisfied, read off its own bytes.  Stating them
    as a licence would be authoring a rule the corpus does not carry — and the
    corpus's only two statements on this move's generality book it NOT
    ESTABLISHED.  Barred.  Not done.

ACTUAL SURFACE: every artifact consumed is a sealed member of the commissioned
corpus, verified in this session under one convention or the other, or is
expressly declared unsealed at FLAG-1.  Nothing here rests on a reconstruction,
a paraphrase, or a summary.  The one prior lane report used (O42SR) is used for
its BYTES and every row relied on was re-opened at its primary.
```

### IMPORT AUDIT

```text
Every notion used that is not defined in the corpus, declared, with a statement
of whether the finding survives without it.

IMP-1  "argument type" / "output type" / "input type" / "codomain" / "arity".
       ORDINARY TYPE VOCABULARY, supplied by the commission itself.  Used only
       as labels for what the corpus's own defining text displays.  No type
       theory is imported; no coercion, subtyping, or variance rule is used.
       SURVIVES WITHOUT IT: yes — every grade can be restated as "the two
       displayed nouns and constructions agree / do not agree".

IMP-2  "projector" for Q_Sigma, from spec(Q_Sigma) = {0,1}.  ORDINARY-MATHEMATICS
       GLOSS on the corpus's own display.  Used at §1.2 to say CS-1's two-term
       structure and U_write's two-term structure are cognate in SHAPE.
       LOAD-BEARING? NO.  The CS-1 row is graded MISMATCH on two loci (scalar vs
       operator-valued weights; the carrier space), and both loci are read off
       displayed text without the gloss.  THE C-17 MATCH DOES NOT USE IT EITHER:
       the invariant-form gate writes "Q = (I-Z_flux)/2" and calls it "the flux
       projector" in its own words at :62.  SURVIVES WITHOUT IT: yes.

IMP-3  "the traceless part of M_2(C)" / "Lie algebra" language.  NOT USED HERE.
       O42SR used it for its MATCH 1; I do not, and I record that I do not: the
       J-1 row at §2.1 is graded on the two displayed nouns ("the primitive
       record factor" against "the primitive observable algebra is M_2(C)"), not
       on an algebraic gloss.  NOTHING IN THIS ARTIFACT DEPENDS ON IT.

IMP-4  Fubini-Study / Mandelstam-Tamm.  NOT IMPORTED BY ME.  The corpus imports
       it and flags its own import — onset gate :67 "Import the standard
       Fubini-Study/Mandelstam-Tamm theorem"; :180
       relative_orthogonalization_bound_imported = true.  I quote the corpus's
       import and its own flag; I add nothing and evaluate nothing.

IMP-5  Misner-Sharp compactness (the C-5 / BID row).  Same: the corpus imports
       and labels it.  Quoted at second hand only, not used to decide any grade.
       The C-5 row is graded MISMATCH on a type difference that does not touch it.

IMP-6  File modification timestamps (§0.1).  A FILESYSTEM FACT, NOT A CORPUS
       NOTION.  Used to establish that the flux gate's sidecar predates
       EARN_THE_TRACE's refusal.  SURVIVES WITHOUT IT: the finding that the
       refusal is false at bytes stands on the seal verifying TODAY, regardless
       of ordering; the timestamps only establish that it was ALSO false when
       written.  If a reader declines filesystem metadata, the finding narrows to
       "false as of this reading" and the §2 grades are unchanged.

NO PHYSICS, NO ANALYSIS, NO CATEGORY THEORY, AND NO NUMERICAL EVALUATION IS
IMPORTED TO DECIDE ANY GRADE.
```

### SWEEP CUTOFFS — PATTERN, HITS, LEAK COUNTER

Displayed in full at **§0.2** (blocks A, B, C, D) and supplemented by the
recursive re-run at FLAG-3. Consolidated:

```text
PATTERN                                        SCOPE       NET   LEAKS  OPENED
"record-changing operator"                     top-level     5     0    all 5
"record-changing operator"                     RECURSIVE     5     1    all 5
"record-changing" (any)                        top-level    30     1    9 (cutoff)
"source-flux-conditioned" (prose)              top-level     2     0    all 2
"source-flux-conditioned" (prose)              RECURSIVE     2     1    all 2
"SOURCE_FLUX_..._GATE" filename citation       top-level    45     3    9 (cutoff)
"TYPED_CHARGE_CHIRAL|kappa_M|kappa_W|W_X|W_Y"  RECURSIVE    12     2    all 12
flux gate -> invariant-form gate tokens        2 files       0     —    complete
invariant-form gate -> flux gate tokens        2 files       0     —    complete
"K_write" sense-2 collision control            top-level     3     0    all 3
"template|applies whenever|in general|
 generalis|generaliz"                          4 files       0     —    complete
RULINGS, all 28, six patterns                  28 files      2     0    both

TOTAL LEAK COUNT ACROSS ALL PATTERNS: 11 (top-level 4 + recursive 7; the
recursive runs re-count the same barred basenames plus subtree copies).  NOT ONE
BARRED FILE WAS OPENED, QUOTED, OR COUNTED AS EVIDENCE.
```

**RECONCILIATION OF EVERY HEADLINE AGAINST ITS OWN SWEEP BLOCK:**

```text
HEADLINE                                  SWEEP BLOCK IT MUST MATCH        OK?
"the flux gate's output IS a consumer's   §1.2 C-17 row: two argument
 input type"                               types quoted from primary bytes  OK
"and the corpus nowhere composes them"    SWEEP B, 0/0 both directions,
                                           plus the recursive TYPED_CHARGE
                                           run: 12 net hits, 6 non-self
                                           carriers checked for flux-gate
                                           tokens, 0 for 6                  OK
"the onset gate is the ONLY artifact      SWEEP A "source-flux-conditioned"
 naming the object in prose"                2 net (1 artifact, both roots);
                                            recursive re-run agrees          OK
"no ruling of the twenty-eight bears on   SWEEP C, six patterns over 28
 any of this"                              files, 2 hits, both opened,
                                           both incidental                   OK
"three joins, 0 rule-blocked, 0           §2.1-§2.3, each graded from the
 proof-blocked"                            two artifacts' own bytes plus
                                           LOCKED_PROCESS search             OK
"census count 10"                         §4.1 itemization: 9 + 1           OK
"count before O42SR was 12"               §4.2 rows C-1, C-2                OK
"NOT A TEMPLATE OF RECORD"                §3.4: 2 negative generality
                                           statements quoted; template
                                           sweep 0 hits over 4 files         OK
```

The headline never claims a zero without first displaying the raw hits that were
disposed.

### FLAG BLOCK

```text
FENCES, TERMINAL:
  alpha_computed = false
  proof_authorized = false
  kappa_record_computed = false
  coupling_computed = false
  scale_computed = false
  root_computed = false
  eigenvalue_computed = false
  norm_computed = false
  magnitude_approached = false
  measured_constant_comparison_performed = false
  composition_performed = false
  git_command_run = false

SEAL TALLY, THIS COMMISSION:
  Cleanroom primitive-gate family (Convention A)              8/8   OK
  STAGE8 lane artifacts + LOCKED_PROCESS (Convention B)     13/13   OK
  SUPERVISION RULINGS, BOTH PREFIXES                        28/28   OK
  GRAND TALLY: 49/49 OK, ZERO MISMATCHES.
  NO SIDECAR IN EITHER FORM, IN EITHER ROOT (declared, FLAG-1): 3.

FLAG-1  THREE LOAD-BEARING ARTIFACTS CARRY NO SIDECAR IN EITHER CONVENTION.
        PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md   (the J-1/J-2 producer)
        BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md (the C-5 primary)
        BID_ACTIVE_HANDLE_CONTROL_GATE_V001.md            (the C-18 / U-2 row)
        BOTH FORMS PROBED IN BOTH ROOTS BEFORE ANY WAS CALLED UNSEALED.
        None is hidden and none is defaulted.  The carrier's output type is
        independently carried on the seal-verified INGREDIENT CENSUS :460-461;
        C-5's defining sentence on the seal-verified GENERATIVE_VS_FORMATION
        typing :703-714.  THE THIRD HAS NO INDEPENDENT SEALED CARRIER, and the
        C-18 row is graded CANNOT-DETERMINE-AT-BYTES in any case, so no grade
        rests on it.  J-1, J-2 and the C-5 row are each graded ONE CONFIDENCE
        STEP LOWER on this account, and I say so here rather than in a footnote.
        I ALSO RECORD THAT MY OWN FIRST TALLY SAID **TWO**, and that the third
        surfaced only on the comprehensive both-forms re-probe run before
        sealing — the same trap that caught EARN_THE_TRACE at FLAG-5, caught
        once in my own draft and corrected here rather than normalized away.

FLAG-2  TWO LOCATOR CORRECTIONS, MADE NOT INHERITED.
        (a) STAGE8_PRODUCTION_VS_DESCRIPTION_O13SR_V001.md :230-231 cites the
            CIS self-typing sentence at "(:16-17)".  AT BYTES IT IS AT CIS
            :12-13.  I re-read at both and quote from :12-13.  The correction
            does not change O13SR's adjudication, only its pin.
        (b) O42SR §3.4 quotes the disjointness sentence and the C-5 carrier at
            "GENERATIVE_VS_FORMATION :704-714".  At bytes the block runs
            :703-714 (the label line "The marginal-closure carrier," sits at
            :703).  A one-line span truncation of the kind LOCKED_PROCESS's
            SPAN-COVERAGE RULE (:426-435) exists to catch.  Corrected in place.

FLAG-3  MY FIRST SWEEP GLOB WAS TOP-LEVEL ONLY, AND I CAUGHT IT AND RE-RAN.
        ROOT-1 holds 1960 *.md recursively against 1819 at top level (25
        subdirectories); ROOT-2 holds 2312 against 1523 (62 subdirectories).  A
        top-level glob therefore missed 141 + 789 files.  Under LOCKED_PROCESS's
        STATE THE GLOB law (:452-458) — "a searched-space declaration states its
        RECURSION and pattern, not just its directories" — I re-ran the three
        DECISIVE patterns RECURSIVELY over both roots.  THE RESULT WAS IDENTICAL
        AT THE LEVEL OF DISTINCT ARTIFACTS.  The high-count patterns under CH-5's
        cutoff were NOT re-run recursively; that exposure is carried, not hidden.

FLAG-4  I CORRECTED THE COMMISSION'S OWN PREMISE.  The tasking states that no
        commission has ever displayed the flux gate's output side.  O42SR
        §2.1 P-5 :563-583 displays it and quotes both objects.  The blind spot is
        real for the three commissions BEFORE O42SR; O42SR closed it.  What was
        never asked, and is asked here, is whether that output is any consumer's
        INPUT type.  §1's finding does not rest on the premise being true, and I
        report the correction rather than write to the premise.

FLAG-5  A CLOSURE-LANE ARTIFACT REFUSED THE FLUX GATE ON A FALSE GROUND.
        STAGE8_EARN_THE_TRACE_S9AD_V001.md :604-605 records the flux gate as
        "unsidecared and NOT consumed".  Its sidecar exists under Convention A
        and verifies OK; it was written 2026-07-23 10:10, three weeks before the
        refusal.  I REPORT THIS; I DO NOT REPAIR IT.  I did not re-run that
        determination and did not consume the flux gate on its behalf.  Whether
        the determination's conclusions change on consumption is
        INDETERMINATE-AT-BYTES and is not adjudicated here.

FLAG-6  REGISTER-BAR INCIDENTS: ZERO FILES OPENED IN VIOLATION.  Eleven barred
        basenames surfaced across all sweeps and were dropped unread (counted per
        pattern above).  Exclusions were passed as a zsh ARRAY throughout, never
        as a string.  QUESTIONSSETTLED_REGISTER_V001.md, which sits in ROOT-2, was
        never opened.  SELF-EXCLUSION: this artifact's own basename was the last
        element of the exclusion array, and it did not exist at sweep time — the
        output-path probe returned "No such file or directory" before any write.

FLAG-7  OWN-DRAFT DEFECTS, DECLARED.
        (a) The §1.3 tally reads 3 MATCH ROWS carrying 2 DISTINCT OUTPUT OBJECTS
            (C-17 takes K_write,rel; C-3 and C-16 both take the U_chi family).
            The two counts are deliberately different and must not be reconciled
            away.
        (b) CH-5's cutoff on the two high-count patterns is real exposure and is
            not re-run recursively.  I do not claim §1's consumer census is
            exhaustive over the corpus; I claim it is exhaustive over the
            nineteen rows displayed.
        (c) The C-15 argument type is SECOND-HAND (its W3/CERT primary lies
            outside the readable roots).  Every MISMATCH in the CS-2 … CS-9 and
            C-15 band rests on it.  If that primary ever reads differently, the
            band re-opens.
        (d) §4's count of TEN rests on my reading that the WBC's per-clause
            GRADE lines ("GRADE: exact, WEIGHT-SUM-level") are not output-type
            displays.  A reader who counts a grade as an output type gets a
            census of ONE (G_closure alone).  I state the alternative rather than
            suppress it; the grades describe how exactly a clause takes its
            argument, not what type it returns, which is why I do not count them.

BARS RESPECTED:
  NO COMPOSITION WAS PERFORMED.  Not one of the three unperformed joins was
  taken, not the C-17 match, not the C-19 kernel test, not the U_chi forcing
  test.  Where this artifact says what a join WOULD yield, it reports two
  displayed types and what the corpus's own text says the join would discharge,
  and stops.
  No missing object, coercion, intermediate object, admissibility proof, rule,
  ruling, or clause was authored, drafted, sketched, or named-as-it-should-read —
  not in a toy, a footnote, or an aside.
  No advocacy.  No adoption.  No reclassification.  No reading is recommended,
  preferred, or argued for.  No catalogued negative was re-litigated: where a
  prior negative is cited it is cited for its BYTES.
  No git action was taken.
```

---

## §6 — RESULT

```text
Q1  THE FLUX GATE'S OUTPUT SIDE IS THREE OBJECTS DEEP, AND ONE OF THEM IS A
    CONSUMER'S INPUT TYPE.
    OUTPUT, quoted (SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :67-72,
    :80-86, :93-101):
      K_write,rel = (pi/2) Q_Sigma tensor Y_R   — "the conditional integrated
        record-changing holonomy", up to endpoint rephasing and orientation
        reversal;
      U_write = (I-Q_Sigma) tensor I_R + Q_Sigma tensor (-iY_R)  — "the
        ready-subspace unitary";
      U_chi   = (I-Q_Sigma) tensor I_R + exp(-i chi) Q_Sigma tensor (-iY_R) — a
        one-parameter family, "chi is not fixed by the conditional record
        projectors".
    TESTED AGAINST 19 CONSUMER ROWS: MATCH 3 · MISMATCH 14 · CANNOT-DETERMINE 2.
    *** THE MATCH: K_write,rel IS ONE OF THE "two real flux-conditioned
        record-changing directions" W_X = Q ⊗ I_chi ⊗ X_record,
        W_Y = Q ⊗ I_chi ⊗ Y_record THAT
        TYPED_CHARGE_CHIRAL_RECORD_INVARIANT_FORM_GATE_V001.md :55-60 (seal OK)
        SPANS ITS COEFFICIENT SPACE V ON.  Two gates, same day, same declared
        zero/one flux sector, one producing and one consuming the same object —
        AND ZERO CITATIONS IN EITHER DIRECTION.  The consumer declares its own
        flux projector rather than importing the producer's.  THE CORPUS
        NOWHERE COMPOSES THEM. ***
    Second match: the U_chi family is an admissible set, and TWO admissible-set
    consumers (the forcing criterion; the junction's require rows) stand unfed
    by it.  Also uncomposed.

Q2  THE THREE COMPOSABLE-NEVER-COMPOSED JOINS.
    J-1  carrier derivation -> flux gate.  Would yield: the primitive record
         factor, identified, discharging the flux gate's undischarged reference
         and giving Y_R a derived home.  Discharges none of the gate's negative
         flags.  GRADE: A-PREREQUISITE-IS-UNBUILT — M2C_algebra_derived = false.
    J-2  carrier derivation -> onset gate's realization clause.  Would yield the
         same object in the realization slot; would NOT close the gate's own
         three-factor declaration.  GRADE: A-PREREQUISITE-IS-UNBUILT — same flag.
    J-3  cell assignment -> allow/require constraint.  Would yield a verdict on
         whether support(L_c) ⊂ Omega_c may become a durable public record — the
         only place the corpus's cell constraint would meet the corpus's
         cell-typed declared support.  GRADE:
         NOTHING-BLOCKS-IT-AND-NOBODY-DID-IT.
    0 of 3 BLOCKED-BY-A-RULE.  0 of 3 BLOCKED-BY-A-PROOF.  0 CANNOT-DETERMINE.
    THE WITHDRAWN PRODUCER, EXACT STATUS: PRIMITIVE_RECORD_CARRIER_AND_
    KINEMATICS_V001.md carries NO SIDECAR IN EITHER FORM IN EITHER ROOT; it
    asserts primitive_single_handle_record_algebra = M_2(C) at its own :107; its
    SEALED successor PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md
    withdraws it at :45-48 ("are not derived here") and books
    unique_primitive_carrier_derived = false, bloch_ball_derived = false,
    M2C_algebra_derived = false at :208-210.  A WITHDRAWAL, NOT A REFUTATION.

Q3  THE ONE PERFORMED COMPOSITION.  The bridge gate derives a balanced ready
    state (p = 1/2, theta = pi) as "deductions from the declared orthogonality
    task", places it in the onset gate's ready-state slot, and evaluates the
    onset gate's own integral on it, yielding J_FS = pi hbar/2 and
    Delta S_record = pi hbar = 2 J_FS.  The corpus displays the agreement, then
    the boundary-closure lane reaches the result BY DIGEST at CS-8 and REFUSES
    it as a majorant, and an independent force check calls the limitation
    "decisive".
    IS IT A TEMPLATE?  NOT A TEMPLATE OF RECORD.  What would have to be true of
    another pair, read off the move's own bytes: the consumer displays its
    functional in evaluable form; the producer DEDUCES its object from the
    consumer's own declared task rather than transporting it; both stand in one
    declared representation; and the three exclusions do not bite.  THE CORPUS
    DOES SPEAK TO THE MOVE'S GENERALITY, IN EXACTLY TWO PLACES, AND BOTH ARE
    NEGATIVE: "the V156 boundary hypotheses are universal" sits under **Not
    established** (:180), and the force check rules the limitation "decisive"
    (:138).  A sweep for template/generality language over the four relevant
    artifacts returns ZERO HITS.

Q4  CENSUS OF OBJECTS TYPED ONLY AS CONSUMERS WHOSE DEFINING TEXT ALSO
    PRODUCES: *** TEN. ***
      CS-1 projector promotion — the promoted projector at operator level
      CS-2 R.2 collapse display — the collapse values and weight bookkeeping
      CS-3 boundary-supported commutator — kappa_n and its family behaviour
      CS-4 poisoning ceiling — the determinant bound and resolvent ceiling
      CS-5 b_D profile — b_D, its derived range, AND THE RECORD VERTEX
           J = -(Q b_D Q) tensor alpha_x with its operator-norm bound
      CS-6 phase-skeleton saturation — the exact monodromies and parity grading
      CS-7 parity grading — the swap law and the insertion strata
      CS-8 onset/action budgets — a typed REFUSAL ("NOT A MAJORANT")
      CS-9 locus ruling — a binding MANDATE (D6')
      G_closure — the complete record-forming generator: domain displayed on
           three factors, codomain four heterogeneous things and no type
    7 produce a displayed OBJECT · 2 produce a verdict or mandate · 1 untyped.
    EXITED THE CENSUS WHEN O42SR DISPLAYED THEM: 2 (the onset gate, the flux
    gate).  THE COUNT BEFORE O42SR WAS TWELVE.
    COMPLEMENTARY DEFECT, REPORTED NOT COUNTED: 3 artifacts typed as NEITHER
    consumer nor producer while both sides sit at bytes — the invariant-form
    gate, the active-handle control gate, the generator structure gate.
    *** THE GENERAL FORM: THE WHOLE BOUNDARY-CLOSURE STRUCTURE IS TYPED
        ONE-SIDED.  Nine clauses stated as what they TAKE, graded on how exactly
        they take it, with not one output type displayed — although seven produce
        a displayed object and one of those is an operator with a norm bound. ***
```

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
composition_performed = false
```
