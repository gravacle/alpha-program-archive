# STAGE 8 — THE DISCHARGERS AGAINST THE PARTITION (O11SR) V001

DATE: 2026-08-15
COMMISSION: O11SR (PARTITION-CROSS-BUILD). OBJECT: the closure's unique minimal discharger
set {W-1, W-2, W-3}, classified against the junction survey's own two inventories, and the
principal's never-taken-up question: IS THE CLOSURE'S NON-LOCALIZABILITY A CONSEQUENCE OF THE
ALLOW/REQUIRE PARTITION ITSELF, RATHER THAN A GAP IN OUR TOOLS?
STATUS: DETERMINATION ONLY — display of sealed content and classification DERIVED from the
inventories' own stated criteria. No adoption, no authored physics, no value, no number as a
value of anything, no measured-constant comparison. Nothing here discharges any absence.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: /Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md — ABSENT at start of commission (probed before
any write; `ls` returned "No such file or directory"). No existing file edited. No git.

---

## §0 — GROUND CONSUMED, SEALS VERIFIED; SWEEP CUTOFF

All seals verified by `shasum -a 256 -c` RUN FROM THE ARTIFACT'S OWN DIRECTORY
(`/Users/bgm/MB Work/alpha-program-archive/workspace`), before any reliance:

```text
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md          (the two inventories; 8 sites)
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md    (CONFIRMED-WITH-CORRECTIONS)
OK  STAGE8_PARTITION_THEOREM_T16SR_V001.md               (the partition, derived; scope §6)
OK  STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md         (CONFIRMED-WITH-CORRECTIONS)
OK  STAGE8_COMPLETION_MAP_T17SR_V001.md                  (the discharger set, §3.4/§3.5)
OK  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md            (CONFIRMED-WITH-CORRECTIONS)
OK  STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md                (W-1's own displayed demand)
OK  STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md          (CONFIRMED-WITH-CORRECTIONS; 2 KILLS)
OK  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md              (W-2's own displayed demand)
OK  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md        (CONFIRMED-WITH-CORRECTIONS)
OK  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                   (W-3's own displayed demand)
OK  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md             (its audit)
OK  STAGE8_CERTIFICATION_RULES_O8SR_V001.md              (route/regime/class exclusions)
OK  STAGE8_CERTIFICATION_RULES_O8SR_AUDIT_V001.md        (CONFIRMED-WITH-CORRECTIONS; K-1..K-5)
OK  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md               (the localization shortfall, as claimed)
OK  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md         (CONFIRMED-WITH-CORRECTIONS; F-2/F-3)
16/16 MATCH.  MISMATCHES = 0.
```

CARRIED-GRADE NOTE (declared before use): artifacts cited INSIDE the sixteen — the WBC pair
`80db260f`/`46938251`, the WSR `6e5762a8`, the frozen spec `9f0d12b4`, the obstruction
`fdf20bd4`, the JD premise `d422fc45`, CERT/E1/W3-internal carriers — are consumed AT CARRIED
GRADE, i.e. through the sealed bytes of the sixteen, whose own §0 blocks record their
verification (T17SR-A: 41/41 re-verified; O6SR-A: 15/15). Their FILES were not opened here.
This commission makes no claim it did not perform.

### §0.1 — SWEEP DECLARATION AND CUTOFF

```text
CUTOFF: 2026-08-15, this session. Primary root only for content
  (/Users/bgm/MB Work/alpha-program-archive/workspace); root 2
  (/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003) NOT consumed — no artifact
  unique to it was needed, and the named ground resolved wholly in the primary
  root at verified seals.
SWEEP 1 (filename, primary root): pattern (discharg|cross.?ref|partition|
  junction|allow_require), case-insensitive; register/tracker/road/plan/
  continuation names EXCLUDED UNREAD; full listing, no cap. Result: 32 names
  (16 artifacts + 16 sidecars). Every name is either named ground, prior
  junction/partition ground already consumed inside the named ground, or a
  different object (7A relay files, S2/Banach/clothing discharge lanes, the
  collar charter). NO artifact cross-referencing the discharger set against the
  two inventories exists in the listing.
SWEEP 2 (content, primary root): pattern (W-1.*W-2.*W-3|\{W-1, W-2, W-3\}),
  same exclusions, full listing, no cap. Result: 23 files. Twelve are named
  ground. Eleven are not, and each was disposed WITHOUT being consumed as
  ground, by name and by the sweep line alone, EXCEPT one (below).
DECLARED SWEEP HIT, CONSUMED AT VERDICT-LINE GRADE ONLY:
  STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md (seal OK, verified this session) —
  the immediately prior commission. Its flag block was read; its body was not.
  ITS QUESTION IS A DIFFERENT ONE: whether an input from OUTSIDE the form class
  is POSABLE, and what record-native types exist. It does NOT classify the
  dischargers against the allow/require inventories. Its verdict is carried
  here as CORROBORATION ONLY, never as ground, at exactly two lines (§4.4).
  THE COMMISSION'S PREMISE IS THEREFORE CONFIRMED AT BYTES: the two inventories
  and the discharger set have never been cross-referenced.
NOT READ, by rule: any register/tracker/road/plan/continuation file.
"Q-..." register objects: EXPECTED-UNLOCATABLE; cited only where a sealed
  artifact carries the token inside its own text.
```

---
## §1 — THE CRITERIA, READ OFF THE INVENTORIES' OWN BYTES (so §2 derives, not asserts)

The commission forbids asserting a classification. So the criteria are lifted first, verbatim,
from the survey and the demand-reading it consumes; §2 then applies them and shows the work.

**C-REQUIRE (the forcing criterion).** From the forcing-boundary adjudication as the survey
consumes it (T14SR §1, its lines 24-25 and 54-57 — lane-adjudication status displayed, not
upgraded):

> "*** A REQUIRE-SHAPED CONDITION WITH **DERIVED EXISTENCE AND UNIQUENESS** FORCES A NUMBER. ***"
> "Forcing has ONE criterion (**the admissible set is a point**) and THREE shapes: a single
> require with a unique root; EXACT JOINT DETERMINATION (n conditions, n freedoms ...); and
> overdetermined joint determination (surplus conditions)."

Two halves, and BOTH are needed: EXISTENCE **and** UNIQUENESS. §2 turns on the fact that the
record displays one half for each discharger and not the other.

**C-ALLOW (what counts as a permission of record).** T14SR §3's own header:

> "'Permission of record' = **a quantity the sealed surface leaves unfixed, displayed as such
> at bytes**; per §1, never a licence from the allow/require cell constraint."

and its own four admissible witness forms (T14SR §3's conservativity-pattern note):

```text
(a) TWO-MODEL WITNESS   — models agreeing on every displayed clause, differing on the
                          quantity (A-1 executed at CAS grade; A-2 sealed certificate)
(b) VACANCY DISPLAY     — the ground carries NO clause mentioning the quantity, so model
                          freedom is textual: "no clause to differ on" (A-3/4/5/8/9/10/13)
(c) EXERCISED PERMISSION— an authored act displayed AS authored (A-6, A-7, A-12)
(d) INTERVAL PERMISSION — a derived pin with the location inside it undecided (A-11)
```

**C-FOUR-CASE (the survey's own disposition map, T14SR §1).** This is the criterion that does
the most work below, because it disposes of a RANGE without needing a two-model witness:

```text
ALLOW + RANGE              FORCES?  NO       ("any selection is a choice")
ALLOW + UNIQUE SOLUTION    WITHIN THE CLASS  ("number rides on the class adoption")
REQUIRE + UNIQUE SOLUTION  YES               (via the certificates)
OVERDETERMINED             YES               (with structural evidence)
```

**C-MEMBER (the survey's supplier criterion).** T14SR A-7, at its own bytes: "distinct
admissible sections are DISTINCT CANDIDATES; **no canonical section is claimed**" (T2-3); "the
record's type slots admit many suppliers; **only TYPE slots are of record** (SK-3)." An object
the record specifies as a TYPE SLOT to be inhabited by a MEMBER is, by this criterion, on the
allow side: the record fixes the type, not the inhabitant.

**C-SCOPE (what "the partition" is, and is not).** The partition theorem (T16SR §2) is proven
over Σ = the sealed surface's DISPLAYED EQUATION SET (Σ1–Σ7, §1 of that artifact), and its own
scope clause (§6) confines it: "CURRENT GROUND ONLY ... The theorem's rigid sub-region is J-6
and only J-6"; "J-2 OUT OF SCOPE". **The three dischargers live on the wall/boundary-closure
thread, not on Σ.** No clause of Σ mentions a trace, an HS norm, a cell evaluation, or a
commutator-form certification. This is stated here, before §2, because it caps every claim
below: the partition theorem does not reach the dischargers, and §2 does not pretend it does.
The classification below is made against the SURVEY's inventories (which are corpus-wide), not
against the THEOREM's Σ (which is surface-local).

---
## §2 — DELIVERABLE 1: EACH DISCHARGER CLASSIFIED, DERIVED AT BYTES

The set, at the completion map's own bytes (T17SR §3.4/§3.5; audit: "UNIQUE {W-1, W-2, W-3}
re-derived sole-per-step at WBC bytes, no smaller set possible, no alternative exhibited"):

```text
W-1 (discharges FP-1): "the certified sub-volume trace/HS rate for the dressed
    composite class — 'verbatim MO-3, open of record'; carrier named CN-1 (R5):
    'MEMBER-NAMED TOPOLOGIES with continuity proofs for Loc_N^phys, iotatilde_N^H,
    iota_N^H, Loc_N^C — NO MEMBER EXISTS IN THE SCORED CORPUS TO NAME THEM.'"
W-2 (discharges FP-2): "the MEMBER-SUPPLIED sealed prequotient rule — CN-2 (R1):
    'a member-supplied sealed prequotient rule (JD-3/JD-3a) WITH ONE DISPLAYED
    EVALUATION ON A NAMED ORIENTED k-CELL — ABSENT FROM THE SCORED CORPUS.'"
W-3 (discharges FP-3): "the C-L2 certification at its consuming type — G_cm derived
    at the consuming (HS) type; 'its only permitted certification (C-L2 quadratic
    form)' with 'the operator-norm route excluded'."
```

### §2.1 THE PIVOT, BEFORE THE THREE ROWS: UNIQUENESS IS DISPLAYED, EXISTENCE IS NOT

C-REQUIRE needs **both** derived existence and derived uniqueness. Read the two halves apart:

```text
UNIQUENESS  — DISPLAYED, 3/3, and strongly.  T17SR §3.5: "the minimal completion
  set is UNIQUE ... because each failure point's discharger is displayed as SOLE at
  the closure's own bytes: F1's only escape is the sub-volume rate ('rank x op is
  the ONLY sealed conversion'); F2's only displayed replacement is the symbol-side
  evaluation; F3's certification route is 'only permitted' C-L2 with the
  operator-norm alternative 'EXCLUDED of record.'"
EXISTENCE   — NOT DERIVED, 3/3, and each artifact says so in its own words.
  W-1: "The rate is NOT certified: no sub-volume exponent for the dressed composite
       class is derived here, and NONE IS DERIVABLE FROM THE STOCK this commission
       may consume" (§0); "RATE_CERTIFIED = false".
  W-2: "THE DISPLAYED EVALUATION IS NOT CONSTRUCTIBLE ON THE RATIFIED RULE TODAY"
       (§4.4); "MEMBER_FOUND = none"; the evidence half "is entirely owed" (§5).
  W-3: "(o4) — 'Supplies S2b' — is now shown to be NOT A CONSEQUENCE of (o1)-(o3)
       AT ANY STRENGTH"; the deciding object "is a MEMBER-SUPPLIED object; no
       non-fenced carrier in either corpus root contains it" (§8).
```

**THE SOLE-NESS IS A PROPERTY OF THE DEMAND, NOT OF THE OBJECT.** "Only sealed conversion",
"only displayed replacement", "only permitted certification" quantify over the record's
displayed ROUTES; they say which object WOULD discharge, not that the object exists. The
completion map itself keeps the mood: a landing "at derived grade with derived
existence-and-uniqueness **WOULD** instantiate SHAPE-1 (single require, unique root) per leg"
(§4.1) — conditional, and its antecedent is precisely what is missing. [CAS C3.]

So no discharger meets C-REQUIRE. That is not yet a classification; §2.2–§2.4 must show each
meets C-ALLOW positively, on the inventory's own witness forms, and §5 must show that failing
C-REQUIRE is not merely a matter of the object being UNBUILT.

### §2.2 W-1 — THE CERTIFIED SUB-VOLUME RATE: **ALLOW-SIDE**, by C-FOUR-CASE + C-MEMBER

```text
CRITERION (b) VACANCY DISPLAY — met, at the closure's own bytes.  CN-1's own
  sentence is a vacancy in the survey's exact form: "no member exists in the scored
  corpus to name them."  The scoring artifact's G-2 note "marks the same absence from
  the other side" (W-1 §2.3, refusing to pre-fix a topology from the HS divergences).
CRITERION C-MEMBER — met, verbatim.  CN-1 demands MEMBER-NAMED topologies, and a
  MEMBER is defined of record (carrier triage §2.1, adopted from the spec) as a
  package supplying "the physical Loc_N^phys/contact-quotient member, MEMBER-NAMED
  TOPOLOGIES, Sch_N representation data, and the nonidentity transports."  The record
  fixes a TYPE SLOT and names no inhabitant: allow-side by SK-3's own rule.
CRITERION C-FOUR-CASE — met, and this is the sharpest of the three.  The demanded
  object's admissible set, as displayed at W-1 §2.2, is "an exponent 3 - delta with
  delta > 0, holding UNIFORMLY in n" — A HALF-LINE, NOT A POINT.  The four-case map
  disposes of exactly this shape: "ALLOW + RANGE — FORCES? NO (any selection is a
  choice)."  [CAS W1c.]  Even a landed W-1 would land somewhere in a range the record
  does not narrow to a point.
DERIVED: W-1 is ALLOW-SIDE.  Its own build's word for its status is the same:
  the rate is "open of record", the carrier is owed, and nothing derives it.
```

**ONE CORRECTION CARRIED, AND IT CUTS AGAINST AN EASY VERSION OF THIS ROW.** W-1's §5.2 gate
— "every admissible topology for W-1 must be generated by SYMMETRY-BREAKING seminorms" — is
**REFUTED** by its own audit (W1-AUDIT KILL 2, A-2): hypothesis (a) is inert in the displayed
proof, "Delete (a) and the proof is unchanged and still valid"; the real escape is "to SHRINK
THE CLASS K", not to break a seminorm's symmetry. And §6.3's "DERIVED ALIGNMENT" (supplied
half inert, owed half load-bearing) is **DOWNGRADED** to OBSERVATION grade (A-4). What survives
at derivation grade is only this: "**the two maps the premise leaves owed are, by their own
names of record, the two LOCALIZATION maps** — ... a NAMING COINCIDENCE OF RECORD, not a
derived structural theorem." §4 below is bound by that downgrade and does not re-inflate it.

### §2.3 W-2 — THE MEMBER RULE + DISPLAYED EVALUATION: **MIXED, EXACTLY** — a require-side
form ALREADY SUPPLIED, and an allow-side remainder that is the whole of what is owed

W-2 is the one discharger the record itself decomposes, and the decomposition falls across the
partition. W-2 §1.1, verbatim, three independently-owed components:

```text
W-2a  THE RULE AS BYTES — (JD-3) + (JD-3a) with four obligations PROVED
      (orientation covariance, presentation independence, continuity, contact-kernel).
W-2b  THE SUPPLIER IS A MEMBER — "A MEMBER supplies..."; "MEMBER_FOUND = none".
W-2c  ONE DISPLAYED EVALUATION ON A NAMED ORIENTED k-CELL.
```

**W-2a IS REQUIRE-SIDE, AND IT IS THE SURVEY'S OWN ROW R-3.** T14SR R-3 types exactly this
object: "THE CROSSING'S FORM OBLIGATIONS: the (JD-1)/(JD-2)/(JD-3)/(JD-3a)/(JD-3c) target form
— '**The target form is FORCED; nothing in it is authored here**' (JD §1.3) — plus the
obligation sentence: orientation covariance, presentation independence, continuity,
contact-kernel", SHAPE-2 on the form. And the completion map already found the identity:
CONV-5, verbatim — "**W-2'S OBJECT IS THE SECTOR-CROSSING SUPPLIER**: the JD-3/JD-3a
prequotient rule demanded by FP-2's discharger is the same form-obligation object the junction
survey types at its J-3 site (T14SR R-3)." **This commission's contribution here is to notice
that the require-side half is the half already SUPPLIED**: W-2 §3.1 scores W-2a "**SUPPLIED**"
at authored-premise grade, and its §5 lists the rule, the linear extension, and all four
obligations under "NOT OWED (supplied, and to be scored as supplied)."

**W-2b AND W-2c ARE ALLOW-SIDE, AND ON THE STRONGEST WITNESS FORM THE INVENTORY HAS.**

```text
CRITERION (a) TWO-MODEL WITNESS — MET, on W-2's own deliverable, at its own bytes.
  W-2 §4.3 H-3: the value CE-1 would produce "is gauge-dependent on an authored
  datum: sect_N is 'an authored gauge of the construction: DISTINCT ADMISSIBLE
  SECTIONS ARE DISTINCT CANDIDATES; NO CANONICAL SECTION IS CLAIMED' (premise §3.3)",
  and W2-CAS-5b displays two admissible sections whose per-cell expressions "are NOT
  IDENTICALLY EQUAL."  That is precisely the survey's conservativity pattern: two
  admissible structures satisfying every displayed clause and differing on the
  quantity.  [CAS W2b.]  It is the SAME sentence the survey quotes to license A-7.
  CORRECTION CARRIED: the residual is NOT shown nonzero.  O6SR-AUDIT F-4b, and W-2's
  own weaker byte, govern: the difference EQUALS Integral(delta_sect), which CAN
  vanish — delta_sect(u) = u - 1/2 gives exactly 0 [CAS W2a].  The surviving claim is
  "not identically equal", which is all the two-model criterion needs.  [CAS W2a/W2b.]
CRITERION (c) EXERCISED PERMISSION — MET twice over: sect_N is an "authored gauge"
  (= A-7 verbatim), and the unit lambda_JD rides the delivered object "EXPLICIT,
  SYMBOLIC, UNASSIGNED" with SK-10 anti-tuning standing (= A-6 verbatim) [CAS W2c].
CRITERION (b) VACANCY — MET for both W-2b and W-2c: "MEMBER_FOUND = none"; "sealed
  stock contains no (JD-3) evaluations at all"; the premise "Does not evaluate
  anything, anywhere, on any record object" (its own §9 DOES NOT block).
AND THE RECORD NAMES THE BLOCKING ACT AS AN AUTHORSHIP ACT, in so many words.
  W-2 §4.3 H-2: "Setting cell_N(e) = gamma_RL for a named record cell e would BE the
  identification — it forms the common cell, advances R9-JII past PENDING, and IS NEW
  AUTHORSHIP.  REFUSED here."  COMMON_CELL_FORMED = false throughout.
DERIVED: W-2's OWED remainder (W-2b + W-2c) is ALLOW-SIDE.  W-2's SUPPLIED form
  (W-2a) is REQUIRE-SIDE and is not owed.  The discharger as a demand is therefore
  MIXED — and every part of it that remains outstanding is on the allow side.
```

### §2.4 W-3 — THE COMMUTATOR-FORM CERTIFICATION AT ITS CONSUMING TYPE: **ALLOW-SIDE**, on
the weakest evidence of the three, with a displayed forced reading refused by rule

```text
CRITERION (b) VACANCY + BARRED VALUE — MET, and it is the survey's A-4/A-9 pattern
  exactly.  W-3's beta: "a SYMBOL until R-L2b closes" (d-3); its own carrier says
  "NOTHING WAS CONSTRUCTED.  No exponent, no estimate, no conversion supplied.
  PRODUCTION PROHIBITED."  Compare A-4 mu ("principal value-selection BARRED") and
  A-9 the rank pair ("value selection barred") — same criterion, same words.
CRITERION C-MEMBER — MET, verbatim: the deciding object "is a MEMBER-SUPPLIED object;
  no non-fenced carrier in either corpus root contains it" (W-3 §8).
DERIVED: W-3 is ALLOW-SIDE.
```

**AND THE HONEST RESIDUE, WHICH IS THE SHARPEST THING IN THIS ARTIFACT AGAINST ITS OWN
VERDICT.** At W-3's sub-object beta, the record displays a **unique root**:

> "(g-2) A CELL-CONSTANT BOUND DETERMINES NO EXPONENT [DERIVED]. ... Evaluating on two admitted
> cells of distinct 4-volumes V1 != V2 gives V1^beta G = K = V2^beta G, whose **unique
> solution** is beta = 0, G = K." — and "(g-3) THAT READING IS REFUSED, NOT ADOPTED ... It is
> displayed as **the forced reading** and left unadopted."

That is derived existence AND derived uniqueness on the admitted asset [CAS W3a/W3b] — the
C-REQUIRE configuration, in the record's own word FORCED. It does not reclassify W-3, for two
reasons the record itself supplies: (i) the root is at the WRONG TYPE — "an object that is at
the WRONG TYPE anyway (form, not HS)" — so it is not a root of the DEMANDED object, which is
the HS-type G_cm; and (ii) what stops it is a rule, not mathematics — O8SR §4.4: "it is F3-d,
not mathematics, that stops it." **Both facts are carried forward into §5 unchanged, because
this is the single place in the discharger set where a forcing shape is displayed at all.**

### §2.5 THE ANSWER TO DELIVERABLE 1, IN ONE BLOCK

```text
W-1  ALLOW-SIDE   vacancy display (CN-1 "no member exists ... to name them")
                  + C-MEMBER (a type slot, no inhabitant)
                  + C-FOUR-CASE (admissible set = the half-line 3 - delta, delta > 0
                    — ALLOW + RANGE, "any selection is a choice")
W-2  MIXED, exact W-2a  REQUIRE-SIDE — and SUPPLIED, not owed (= survey row R-3,
                    "The target form is FORCED; nothing in it is authored here")
                  W-2b  ALLOW-SIDE — vacancy ("MEMBER_FOUND = none")
                  W-2c  ALLOW-SIDE — TWO-MODEL WITNESS on its own deliverable
                    (distinct admissible sections, per-cell values not identically
                    equal) + exercised permission (sect_N authored gauge; lambda_JD
                    unassigned) + the blocking act named "NEW AUTHORSHIP"
                  => EVERYTHING STILL OWED ON W-2 IS ALLOW-SIDE.
W-3  ALLOW-SIDE   vacancy + barred value (beta "a SYMBOL", "PRODUCTION PROHIBITED")
                  + C-MEMBER ("a MEMBER-SUPPLIED object")
                  RESIDUE: a displayed FORCED READING (beta = 0, unique) at the WRONG
                  TYPE, refused by rule F3-d — carried, not suppressed.
NONE of the three meets C-REQUIRE: uniqueness is displayed 3/3, EXISTENCE IS DERIVED
  0/3, and the forcing criterion needs both.  [CAS C3.]
```

---
## §3 — DELIVERABLE 2: THE STRUCTURAL QUESTION — IT IS SO, AND HERE IS THE CATEGORY
STATEMENT WITH ITS EXACT SCOPE

The commission's conditional fires: dischargers ARE allow-side (§2). So the closure's
certification rules demand PERMITTED content as a prerequisite. The statement must now be made
at exactly the strength the bytes carry — no more.

### §3.1 THE DERIVATION, THROUGH THE RECORD'S OWN WEAKEST-LEG RIDER

```text
STEP 1  The closure's completed conclusion has a shape, and the record names it.
        T17SR §4.1: "JOINTLY (the closure whole): three conditions, three freedoms,
        none surplus — the completed per-composite closure would instantiate SHAPE-2
        (exact joint determination)."  [CAS C1.]
STEP 2  SHAPE-2 carries a grade rider, and the record quotes it in the same breath.
        T17SR §4.1, quoting the demand-reading: the exact-joint case "behaves as
        REQUIRE+UNIQUE with the require DISTRIBUTED OVER LEGS; each leg's status is
        audited separately; the ... GRADE RIDES ON ITS WEAKEST LEG."
STEP 3  Every leg that remains outstanding is allow-side (§2.5).  So the weakest leg
        is a permission, on all three legs.
STEP 4  The four-case map prices a conclusion resting on a permission: "ALLOW +
        UNIQUE SOLUTION — WITHIN THE CLASS (number rides on the class adoption)."
        Not "YES".  [CAS C2.]
```

**THE CATEGORY STATEMENT, AT ITS EXACT SCOPE:**

> **AT THE BOUNDARY-CLOSURE, AND THERE ONLY, THE RECORD'S CERTIFICATION APPARATUS MAKES A
> REQUIRE-SHAPED CONCLUSION CONDITIONAL ON PERMITTED CONTENT. THE COMPLETED PER-COMPOSITE
> CLOSURE IS A SHAPE-2 (EXACT JOINT DETERMINATION) WHOSE THREE LEGS ARE, IN EVERYTHING STILL
> OWED, PERMISSIONS OF RECORD; BY THE DEMAND-READING'S OWN WEAKEST-LEG RIDER ITS GRADE IS
> THEREFORE CAPPED AT "ALLOW + UNIQUE SOLUTION — WITHIN THE CLASS", NEVER AT "REQUIRE +
> UNIQUE SOLUTION — YES". A DISCHARGED CLOSURE WOULD BE FORCED *WITHIN THE ENTERED CLASS*, AND
> THE ENTERED CLASS IS AN AUTHORSHIP OF RECORD, NOT A DERIVATION FROM IT.**

### §3.2 THE EXACT SCOPE — WHICH DISCHARGERS, WHICH SITES, AND WHERE IT STOPS

```text
SITES IT COVERS — three, all per-composite, all on the wall/boundary-closure thread:
  FP-1 (F1, the conversion step)   via W-1: member-named topologies (CN-1)
  FP-2 (F2, the locus step)        via W-2b/W-2c: member + displayed evaluation (CN-2)
  FP-3 (F3, the error-term step)   via W-3: a member-supplied non-form-class input
SITES IT DOES NOT COVER:
  FP-S (the summed quantifier) — DECIDED, not failing; no barrier, no discharger, and
    the one-way quantifier record bars transport in either direction ("the V-2-barred
    bridge is not crossed"; WSR flag).  The category statement says NOTHING about the
    summed level, where the record's one SHAPE-3 (two independent mechanisms agreeing)
    already stands WITHOUT any permission in its legs (T14SR R-4).  THAT IS THE
    CONTROL CASE, and it matters: the record CAN and DOES force at a quantifier where
    no member is demanded.  The dependency on permission is a property of the
    per-composite closure, NOT of the record's forcing apparatus as such.
  J-6, the magnitude site — the partition theorem's rigid sub-region.  It is reached
    by no discharger: T17SR §4.2, "NO require shape crosses from a discharged
    boundary-closure to the magnitude site at the ground as sealed", and "the
    dependency points FROM the magnitude site INTO the closure, never out of it."
  J-2, the extent site — expressly out of the theorem's scope (T16SR §6) and reached
    by none of the three (its decider is its own named certificate, T14SR §6).
  THE PROGRAM VERDICT — "does not move by the closure alone" (T17SR §4.3); of the nine
    D-links only D7 is wall-adjacent, and W-3 moves it "in exactly ONE member (CONV-2)
    — a D7-leg PARTIAL movement, not a link landing."
```

### §3.3 WHAT THE STATEMENT IS NOT — three inflations refused

```text
NOT "the record forces nothing without permission."  FALSE at bytes: FP-S is decided
  by two independent exact mechanisms with no member in any leg; R-1's quantization
  class, R-2's charge structure, R-5/R-6's extent dimensions, R-8's carrier vanishings
  all stand with no permission in their chains.  The dependency is LOCAL TO THE
  BOUNDARY-CLOSURE, and §3.2 names its three sites.
NOT "the closure is therefore impossible."  FALSE at bytes, and §5 turns on it:
  O8SR §4.6, "Not one of them ranges over objects of the required kind.  Each excludes
  a ROUTE ..., a REGIME ..., or a CLASS."  Allow-side means UNFORCED.  It does not
  mean excluded.  [CAS C5.]
NOT "this is a defect of the certification rules."  This commission proposes nothing,
  scores no rule wrong, and names no repair.  O8SR's own §5.4 finding — that at FP-3
  the blindness "is CREATED AT THE ADMISSION STEP, NOT FOUND IN THE COMMUTATOR" — is
  displayed in §4.3 as a byte, and is not converted here into a recommendation.
```

---
## §4 — DELIVERABLE 3: THE REFRAME TEST — **IT FAILS**, AND THE COUNTEREXAMPLES ARE EXACT

THE PROPOSITION UNDER TEST, as commissioned: the record's require-side content is
systematically NON-LOCALIZABLE (counting, winding, single-valuedness, index-type — global by
nature) while its allow-side content is systematically LOCALIZABLE (density-like, metric-like,
per-region). If it holds, localization-blindness of the derived assets follows from the
partition rather than from any deficiency.

Both inventories are crossed against the localizability axis, row by row, at each row's own
displayed content. The axis itself is DECLARED IMPORTED (see §7 IMPORT AUDIT): the record uses
"localization variable", "spatial localization", "per-cell", "sub-volume", "boundary-supported",
"translation-invariant" — but the abstract two-valued classification of CONTENT as localizable
or not is this commission's construction, erected only to run the commissioned test.

### §4.1 THE REQUIRE-SIDE INVENTORY AGAINST THE AXIS (T14SR §2, all ten rows)

| # | Forced content, at its own displayed type | Localizable? |
|---|---|---|
| R-1 | winding labels integer, "integer-quantized by single-valuedness"; solution set exactly ℤ | NO — counting/index |
| R-2 | U(1) characters, cocycle/transition covariance, conjugate closure, unit modulus, G = 1 | NO — character lattice, global consistency |
| R-3 | the crossing's FORM obligations: **orientation covariance** (per oriented k-cell), presentation independence, continuity, contact-kernel | **PARTLY YES — orientation covariance is a per-cell condition** |
| R-4 | tr H_A^Σ = 0, ‖H_A^Σ‖₂² = 0 EXACT, **at the summed quantifier; per-composite untouched** | NO — and the record proves the non-transport |
| R-5 | extent slice: ker dimension exactly 1, one displayed generator | NO — dimension |
| R-6 | extent menu: dim ker ∈ {1,2}, "no third extent exists" | NO — dimension |
| R-7 | ladder rungs: κ_n → +∞ full-family; **DERIVED p = −3**; trace-parity lemma; **α-k pinned in [1, 3/2]** | **PARTLY YES — p = −3 is a degree in a spatial variable; α-k is a rate exponent** |
| R-8 | cross-block "vanishes IDENTICALLY, dim H = 0 exact"; the **b₁ = 1** cell's mirror collapse | NO — dimension and a Betti index |
| R-9 | a surviving cross-block forces the CHANNEL, not \|n\|: "n enters ... as the **GLOBAL n² prefactor of an n-BLIND form**" | NO — the record's own words "global", "blind" |
| R-10 | the nine forced-failure displays D1–D9 | n/a — process displays, not quantities |

**Nine live rows: 7 non-localizable, 2 localizable (R-3, R-7).** [CAS Y1.]

### §4.2 THE ALLOW-SIDE INVENTORY AGAINST THE AXIS (T14SR §3, all thirteen rows)

| # | Permission of record, at its own displayed type | Localizable? |
|---|---|---|
| A-1 | the magnitude profile — a **continuum response magnitude** as a function of a phase datum | YES |
| A-2 | **the extent bit** — whether R̂_K,N ∈ ℝ·L_T,N; its two models differ by **ker dimension 1 vs 2** | **NO — a dimension/index bit** |
| A-3 | **the winding magnitude \|n\|** — an **integer label** | **NO — counting, the reframe's own archetype** |
| A-4 | μ, magnitude and sign | YES (magnitude); sign discrete |
| A-5 | the coupling content, the magnitude payload | YES |
| A-6 | λ_JD, the cross-sector **unit** — "any assignment, rescaling, or normalization ... is a tuning surface" | YES — a scale |
| A-7 | the authored core: sect_N, **cell_N**, ρ_N, **the per-cell rule** | YES — per-cell |
| A-8 | the mixing coefficient a_K | YES (a scalar value) |
| A-9 | **the ordered rank pair (r₀, r_ch)** | **NO — ranks, index-type** |
| A-10 | **α′, at the summed quantifier** — "a QUANTIFIER/TEST, not a verdict, a value, or a flag" | **NO — global quantifier** |
| A-11 | the clock exponent's location within [1, 3/2] | YES — a rate exponent |
| A-12 | **the five minimality selections: d = 4 (a dimension), C_R = 1, saturation, τ = 1, spin-1/2 (a representation class)** | **NO — dimension and class labels** |
| A-13 | the running target — a flow anchor | YES — a scale |

**Thirteen rows: 8 localizable, 5 NON-localizable (A-2, A-3, A-9, A-10, A-12).** [CAS Y2.]

### §4.3 THE VERDICT ON THE REFRAME: **BOTH LEGS FAIL, AND THE FAILURES ARE PAIRED**

```text
LEG 1 ("require-side is systematically non-localizable") — FAILS.  Counterexamples,
  exact: R-3's ORIENTATION COVARIANCE is a per-oriented-cell condition, forced;
  R-7's DERIVED p = -3 is a degree in a spatial separation variable, forced, and
  R-7's alpha-k pin is a rate exponent, forced.  [CAS Y3.]
LEG 2 ("allow-side is systematically localizable") — FAILS HARDER, five ways.
  Counterexamples, exact: A-3 |n| (an INTEGER — counting, the very item the reframe
  lists as non-localizable, sitting ALLOW-side); A-2 the extent bit (a DIMENSION bit,
  and the record's single strongest allow-side certificate — "positive two-model
  certificate", twice-determined); A-9 the rank pair (INDEX data); A-12's d = 4 and
  spin-1/2 (a DIMENSION and a REPRESENTATION CLASS, both adopted); A-10 alpha' (a
  SUMMED-quantifier object).  [CAS Y4.]
=> THE CORRELATION IS REFUTED AT BYTES.  [CAS Y8.]
```

**AND THE FAILURES ARE NOT SCATTERED — THEY ARE THE SAME THREE SITES, TWICE EACH.** The
counterexamples pair off with the confirmations at the survey's own termination sites:

```text
J-1:  R-1 forces the CLASS Z (counting).      A-3 leaves the MEMBER |n| free (counting).
      SAME TYPE ON BOTH SIDES.  [CAS Y5, X1a-X1d.]
J-2:  R-6 forces the MENU {1,2} (dimension).  A-2 leaves the BIT free (dimension).
      SAME TYPE ON BOTH SIDES.  [CAS Y6, X2a-X2b.]
J-5:  R-7 forces the PIN [1,3/2] (exponent).  A-11 leaves the LOCATION free (exponent).
      SAME TYPE ON BOTH SIDES.  [CAS Y7, X3a-X3b.]
```

At each of the three the require half and the allow half are objects of the *same*
localizability type and differ only on force. Localizability therefore cannot be what the
partition is tracking there — a single axis cannot separate two things that sit on the same
point of it.

**WHAT THE PARTITION IS ACTUALLY TRACKING, DERIVED FROM THE SAME THREE SITES:** the record
forces the **ADMISSIBLE SET** and leaves the **SELECTION WITHIN IT** free. Class / member. Menu
/ bit. Pin / location. Three sites, one pattern, and never a point on the require side [CAS X4].
The record says it in its own words at the partition theorem's class-vs-member note: "every
phase-law clause of Σ holds at SYMBOLIC integer n [CAS PT4] — **the clauses force the CLASS and
pin no member.** The member \|n\| stays allow-side." The axis is CLASS/MEMBER, i.e.
admissible-set-shape versus selection-within-it — which is exactly the forcing criterion's own
axis ("the admissible set is a point") and has no localizability content whatsoever.

### §4.4 THE RECORD HAS ALREADY BEEN CORRECTED FOR MAKING THIS EXACT CONFLATION

This is the strongest single byte against the reframe, and it is the correction the commission
named. O8SR's build stated blindness at "FP-1 and FP-3 ONLY — two of three". Its audit
**REFUTED the count** (K-4), and the ground of the refutation is precisely the axis question:

> "**FP-1's variable is a CARRIER COUNT.** Degree 0 in a carrier count is **n-UNIFORMITY** —
> ORIG's own annotation says so in the same cell — and **n-uniformity is a property in the
> carrier-size direction, not in a localization direction.** ... CORRECTED COUNT:
> **LOCALIZATION-blindness proper survives at FP-3 ONLY — ONE of three.**"

So the record has already, at bytes, refused to identify **counting-uniformity** with
**localization-blindness**, and narrowed a claim from two failure points to one for exactly
that reason. [CAS Z3/Z4/Z5.] The reframe's premise — that counting and localization are two
names for one "global" side — is the conflation that correction forbids.

TWO FURTHER LAYERS OF THE SAME CORRECTION, carried in full:
- The ORIGINAL universal is dead and stays dead: "EVERY CERTIFIED ASSET ... HAS DEGREE >= 0"
  is **FALSE on its own table** — FP-2's asset degree is **−1** (O6SR-A F-2), and the CAS check
  that would have caught it omitted the failing term (F-3). A degree −1 symbol "is NOT
  invariant, NOT uniform, NOT constant, and NOT blind ... it decays, merely too slowly."
  **FP-2's certified asset is thus a piece of CERTIFIED (require-grade) record content that IS
  graded in a localization variable** — a counterexample to the reframe from outside the survey's
  ten rows, at the closure's own stock.
- What survives is a SHORTFALL statement only, and it is weaker in KIND: "AT EACH OF THE THREE
  FAILURE POINTS, THE ADMITTED ASSET'S DISPLAYED DEGREE ... LIES OUTSIDE THAT POINT'S OWN
  ADMISSIBLE HALF-LINE, AT POSITIVE DISTANCE — (3/2, 2, 2)." Re-derived here exactly, with the
  one orientation-free definition [CAS Z1/Z2]. "The blindness claim survives only on the
  sub-family {FP-1, FP-3}" — and K-4 then narrows that to {FP-3}.

CORROBORATION, declared sweep hit, verdict-line grade only, NOT ground: O10SR's flag block
separates the same two axes at the same point — "FP-1 = UNPOSABLE (topological) /
POSABLE-BUT-UNMATCHED (counting)" — and reports "MATCH TEST = NO-CANDIDATE on the three-way
conjunction (localizing + scale-free + magnitude-carrying)". Cited as agreement, load-bearing
nowhere.

### §4.5 WHAT THIS DOES NOT REFUTE — the reframe's true residue, stated so it is not lost

```text
The reframe FAILS as a correlation between the partition and localizability.  It does
NOT fail as an observation about the CLOSURE'S OWN THREAD, where a weaker version
does survive on the record's own bytes and is worth keeping distinct:
  (i)  The three escapes ARE of one type in the closure's own words: "sub-volume",
       "at the volume diagonal rather than the boundary", "breaks translation
       invariance / consumes spatial localization" are displayed as "three phrasings
       of ONE demand: AN ASSET THAT CARRIES A LOCALIZATION SCALE" (O6SR §5.2).  That
       survives its audits.
  (ii) At FP-3 the blindness IS created by the admission rule, not found in the
       object: "THE BLINDNESS IS CREATED AT THE ADMISSION STEP, NOT FOUND IN THE
       COMMUTATOR.  The localization the three escapes ask for is PRESENT in the
       certified object's OWN SEALED DISPLAY, and is discarded by the admission
       rule's choice of majorant" (O8SR §5.4, CONFIRMED by its audit as to
       qualitative content).
  BUT NOTE WHAT (ii) SAYS: the culprit is the ADMISSION RULE (F3-b's two-slot
  majorant), which is a CERTIFICATION rule — NOT the allow/require partition.  These
  are different objects, and the commission's question turns on not confusing them.
  The allow/require constraint is typed CELL_CONSTRAINT_ONLY of record and is not a
  selector at all (T14SR §1, the minimality-split correction).  A certification rule
  discarding r-information is not the partition doing anything.
```

---
## §5 — DELIVERABLE 4: THE COUNTER-TEST — COULD EACH ALLOW-SIDE DISCHARGER INSTEAD BE A
REQUIRE-SIDE OBJECT THAT IS MERELY **UNBUILT**?

The error to avoid: an unbuilt FORCED object misclassified as a permission converts a real
obstacle into false comfort. The test must therefore be run positively, per discharger, and its
residues carried whether or not they are comfortable.

**THE TEST, STATED BEFORE IT IS APPLIED.** An UNBUILT REQUIRE-SIDE object is one whose
**existence is derived** but whose **construction has not been performed**. It has a determinate
subject and a displayed chain terminating with the admissible set a point; only the work is
missing. A PERMISSION is one whose existence is not derived at all: the record fixes a type and
leaves the inhabitant to authorship. **The discriminator is therefore DERIVED EXISTENCE, and
its subject.** (Uniqueness does not discriminate: §2.1 showed all three have displayed
sole-ness while none has derived existence.)

### §5.1 W-1 — could a certified sub-volume rate be forced-but-unbuilt?

```text
THE STRONGEST CASE FOR "UNBUILT", AND IT IS REAL — displayed, not minimised:
  W1-AUDIT KILL 1 exhibits a SUFFICIENT CONDITION of record.  With the sealed rank
  cap N_n(s) <= 4n^3 reinstated and a q = 2 counting bound with a sub-volume
  prefactor, splitting the layer-cake integral gives sub-volume [O8SR re-derivation
  at CAS R4c-R4e].  O8SR §4.2 concludes: "AT THE FAILURE POINT = RULE-IMPOSED-BY-
  ABSENCE.  NO MATHEMATICS EXCLUDES THE REQUIRED OBJECT; A SUFFICIENT CONDITION FOR
  IT IS EXHIBITED OF RECORD."  That is the closest any discharger comes to being an
  unbuilt derived object, and it must be stated at full strength.
WHY IT IS STILL NOT ONE — three grounds, each at bytes:
  (1) NO DETERMINATE SUBJECT.  An unbuilt forced fact needs something of record for
      it to be a fact ABOUT.  The dressed composites reach the four CN-1 maps through
      a composite whose domain is EMPTY: "THE COMPOSITE'S DOMAIN IS EMPTY ON ACTUAL
      STOCK UNTIL JB-2 LANDS" (JD premise §9), confirmed independently by its check
      ("JB-2 undischarged of record ...; the actual-stock domain is empty until it
      lands", F-4), and W-1's own §6.2 finds the localization member "presupposed at
      its slot, NOT SUPPLIED."  There is no inhabited subject over which the rate
      could already be true-but-unproved.
  (2) THE ANTECEDENT IS SUPPLIED BY NOBODY.  W1-AUDIT §3.4 on its own sufficient
      condition: it is "an implication whose antecedent ... NO ARTIFACT IN THE SWEPT
      STOCK SUPPLIES."  A sufficient condition with an unsupplied antecedent derives
      no existence.
  (3) THE TARGET IS A RANGE, NOT A POINT.  Even granting existence, C-FOUR-CASE
      disposes: "ALLOW + RANGE — FORCES? NO (any selection is a choice)" [CAS W1c].
      A forced object must have a point-valued admissible set; 3 - delta, delta > 0,
      is a half-line.
  AND THE RECORD'S OWN WORD FOR THE CARRIER IS "MEMBER-NAMED" — a type slot (C-MEMBER).
COUNTER-TEST RESULT AT W-1: NOT AN UNBUILT FORCED OBJECT — but this is the leg where
  the residue is largest, and §6 prices it accordingly.  The record exhibits a route
  IN; it derives no existence AT.
```

### §5.2 W-2 — could the displayed evaluation be forced-but-unbuilt?

```text
THIS IS THE ONE LEG WHERE THE COUNTER-TEST IS ANSWERED POSITIVELY, NOT BY ABSENCE.
  A forced per-cell value would have to be the SAME value under every admissible
  section — a forced object cannot depend on an authored gauge.  It does not:
  "distinct admissible sections are DISTINCT CANDIDATES; no canonical section is
  claimed", and two admissible sections give per-cell expressions that "are NOT
  IDENTICALLY EQUAL" (W2-CAS-5b; audit-corrected form) [CAS W2a/W2b].  That is a
  positive display of non-forcing, in the survey's own two-model shape — not a
  vacancy, not a failure to find.
  SECOND POSITIVE GROUND: the blocking step is named by the record as an AUTHORSHIP
  ACT, not as unperformed work.  "Setting cell_N(e) = gamma_RL ... would BE the
  identification ... and IS NEW AUTHORSHIP.  REFUSED here" (H-2); COMMON_CELL_FORMED
  = false; R9-JII PENDING with the carried instruction "add no route, branch,
  address, or member selection."  An unbuilt forced object is blocked by labour; this
  one is blocked by a permission nobody has exercised.
  THIRD: the delivered object would ride an UNASSIGNED unit — lambda_JD "EXPLICIT,
  SYMBOLIC, UNASSIGNED", anti-tuning standing [CAS W2c].  A forced value does not
  arrive carrying a free unit.
  AND THE ONE HALF THAT *IS* REQUIRE-SIDE IS ALREADY BUILT: W-2a, the form with its
  four obligations, is SUPPLIED (§2.3).  The partition runs cleanly through the
  middle of W-2: what is forced is done; what is owed is permitted.
COUNTER-TEST RESULT AT W-2: NOT AN UNBUILT FORCED OBJECT.  Cleanest of the three.
```

### §5.3 W-3 — could G_cm at the consuming type be forced-but-unbuilt?

```text
THE CASE FOR "UNBUILT" IS THE STRONGEST HERE IN ONE RESPECT AND THE WEAKEST IN ANOTHER,
and both must be displayed.
STRONGEST: the record displays a FORCED READING at the sub-object.  beta = 0 is the
  unique solution of the two-cell system, derived existence and uniqueness [CAS
  W3a/W3b], and W-3 calls it "the forced reading", left unadopted.  What stops it is
  F3-d, a rule: "it is F3-d, NOT MATHEMATICS, that stops it" (O8SR §4.4).  If a
  forcing shape is displayed anywhere in the discharger set, it is here.
WHY IT DOES NOT CONVERT W-3 TO REQUIRE-SIDE-UNBUILT — two grounds, both the record's:
  (1) WRONG TYPE.  The root is forced on the ADMITTED FORM asset; the demanded object
      is HS-typed.  W-3 g-3: it would be "an authored assignment of a reserved symbol
      on the strength of an object that is at the WRONG TYPE ANYWAY (form, not HS)".
      A unique root of the wrong object is not existence for the right one.  W-3 §8
      says the same structurally: "(o4) ... is NOT A CONSEQUENCE of (o1)-(o3) AT ANY
      STRENGTH", so no strengthening of the certified content reaches the demand.
  (2) RESERVED SYMBOL, PRODUCTION PROHIBITED.  beta is reserved to R-L2b, whose own
      carrier says "PRODUCTION PROHIBITED ... No exponent."  The survey's A-4/A-9
      criterion for a permission is exactly a barred value-selection.
  AND THE OBJECT IS TYPED MEMBER-SUPPLIED at W-3's own bytes.
COUNTER-TEST RESULT AT W-3: NOT AN UNBUILT FORCED OBJECT — with the displayed forced
  reading carried forward as a live residue, not disposed of.
```

### §5.4 THE COUNTER-TEST'S OWN RESIDUE, STATED WITHOUT COMFORT

```text
The counter-test returns NEGATIVE 3/3: none of the three is an unbuilt forced object,
because derived existence is absent 3/3 and the record positively displays non-forcing
at W-2.  BUT THE NEGATIVE MUST BE READ AT ITS EXACT STRENGTH, AND IT IS ONE-SIDED:
  NO DISPLAYED IMPOSSIBILITY RANGES OVER ANY OF THE THREE OBJECTS AS SUCH.  O8SR §4.6,
  audited and confirmed: "Every displayed impossibility in the inventory is REAL.  NOT
  ONE OF THEM RANGES OVER OBJECTS OF THE REQUIRED KIND.  Each excludes a ROUTE (F1-b,
  impossibility 1), a REGIME (C6's equal-time localizers), or a CLASS (impossibilities
  2-4)."  [CAS C5.]
THEREFORE, EXACTLY: the three dischargers are UNINHABITED TYPE SLOTS — neither forced
  by the record nor excluded by it.  "Allow-side" here is a statement about the
  record's FORCING RELATION.  It is NOT a proof that no such object can exist, and it
  supplies NO comfort about the closure's prospects in either direction.
  THE FALSE COMFORT THE COMMISSION FEARS WOULD BE THIS SENTENCE: "they are permissions,
  so the obstacle is not real."  That sentence does not follow and is refused here.
  The obstacle is exactly as large as it was; what §2 changes is its TYPE — from work
  not yet done to content not yet entered.
```

---
## §6 — DELIVERABLE 5: CONSEQUENCE FOR THE DISCHARGE ROUTE UNDER EACH CLASSIFICATION —
DISPLAY ONLY, NOTHING PROPOSED

Both branches are displayed at equal length. No route is selected, recommended, costed, or
opened; no object is proposed; no absence is discharged.

```text
UNDER THE ALLOW-SIDE CLASSIFICATION (the verdict of §2), what follows for the route:
  (1) THE ROUTE IS AN ENTRY ROUTE, NOT A DERIVATION ROUTE.  No amount of derivation on
      sealed content reaches the three: each build says its own object is not
      derivable from the stock it may consume, and the completion map's cross-map is
      EMPTY of discharge edges — "NO subset of the named absences A-1..A-7 — including
      the full set — discharges the boundary-closure at any displayed live failure
      point" (T17SR §3.5, EMPTY-INFEASIBLE).
  (2) THE ENTERING CONTENT IS MEMBER-SHAPED AT ALL THREE SITES: member-named
      topologies (W-1), a member with a displayed evaluation (W-2), a member-supplied
      non-form-class input (W-3).  Their entry is, in the map's own words, "supply-
      route work" — content absent FROM THE SCORED CORPUS.
  (3) THE CONCLUSION'S GRADE IS CAPPED IN ADVANCE, by §3.1's weakest-leg derivation:
      ALLOW + UNIQUE = "WITHIN THE CLASS", never REQUIRE + UNIQUE = "YES".  A
      discharged closure would deliver "the n-uniform four-quantity enclosure via the
      boundary-closure structure" at the per-composite quantifier, forced WITHIN the
      entered class.
  (4) THE PARTITION THEOREM'S SCOPE CLAUSE THEN GOVERNS WHAT THE ENTRY RE-POSES:
      "A change of ground — a new booked displayed clause ... RE-POSES the question;
      the theorem forecloses nothing about changed ground" (T16SR §6).  And it changes
      nothing at the magnitude: no discharger carries the booked co-occurrence clause
      that a magnitude arrival would need (T17SR §4.2).
  (5) THE SUMMED LEVEL IS UNAFFECTED IN BOTH DIRECTIONS — FP-S is decided and the
      one-way quantifier record is respected; "the summed decisions transport NOTHING
      down to the per-composite failure points", and a per-composite landing
      transports nothing up.
UNDER THE REQUIRE-SIDE-UNBUILT CLASSIFICATION (refuted at §5, displayed for symmetry):
  (1) THE ROUTE WOULD BE A WORK ROUTE: the objects would exist of record and the
      obligation would be construction, schedulable against the sealed stock.
  (2) THE CONCLUSION'S GRADE WOULD NOT BE CAPPED: three SHAPE-1 legs landing at
      derived grade would compose to SHAPE-2 at REQUIRE + UNIQUE = "YES" (T17SR §4.1).
  (3) NOTHING WOULD NEED TO ENTER — no member, no new booked structure, and the
      partition theorem's re-posing clause would never fire.
  THE DIFFERENCE BETWEEN THE TWO BRANCHES IS THEREFORE NOT EFFORT BUT AUTHORITY: who
  may act, and whether the result is forced absolutely or only within an entered class.
  §5 places the record on the first branch, 3/3, with the residues it names.
NOTHING IS PROPOSED.  No door is opened.  No arrival is asserted to be likely,
  available, or authorized.  No rule is scored wrong and no repair is named.
```

---
## §7 — IMPORT AUDIT: EVERY NOTION USED, DECLARED RECORD-NATIVE OR IMPORTED

```text
RECORD-NATIVE (the notion and its word are the record's own, at cited bytes):
  require-side / allow-side ......... T14SR §2/§3 (the two inventories)
  permission of record .............. T14SR §3 header
  forcing criterion; the three shapes  the forcing-boundary adjudication, via T14SR §1
  "admissible set is a point" ....... same
  the four-case map (ALLOW+RANGE ...)  same
  derived existence and uniqueness .. same
  weakest-leg rider ................. same, quoted at T17SR §4.1
  two-model witness / conservativity  AJ Theorem II; T14SR §3; T16SR §3.2
  class vs member ................... T16SR §3.1 class-vs-member note; SK-3; "MEMBER_FOUND"
  type slot ......................... SK-3, "only TYPE slots are of record"
  vacancy display; exercised permission; interval permission ... T14SR §3's own note
  sole / only sealed / only permitted  WBC verdict, via T17SR §3.5
  localization variable; spatial localization; localization-blind ... O6SR §5, W-3 ADDS(3)
  sub-volume; carrier count; n-uniformity ... WBC/W-1/O8SR-A K-4
  per-cell / per-composite / per-diamond / per-stratum / summed quantifier ... corpus-wide
  degree; admissible half-line; shortfall ... O8SR §5.2 (as corrected)
  winding, single-valuedness, integer class ... AJ P3a/P3b; T16SR §3.1
  rank, dimension, Betti number b_1 ... T14SR R-5/R-6/R-8
  RULE-IMPOSED vs MATHEMATICAL barrier ... O8SR §4.1
  route / regime / class exclusion .. O8SR §4.6
IMPORTED, and declared as such — machinery this commission brought, not the record's:
  "LOCALIZABLE / NON-LOCALIZABLE" AS A TWO-VALUED CLASSIFICATION OF CONTENT.  THIS IS
    THE COMMISSION'S OWN CONSTRUCT, built solely to run the commissioned reframe test
    (§4).  The record grades objects in NAMED variables; it nowhere sorts its content
    into a localizable/non-localizable dichotomy.  Every §4.1/§4.2 cell is my reading
    of a row's own displayed type against my imported axis, and the verdict of §4 is
    that the axis DOES NOT track the partition — a negative result about an imported
    notion, which is the safest kind to return.
  "metric-like", "density-like", "index-type", "global by nature" — the commission's
    own vocabulary, carried from the commissioning text; not record words.  ("density"
    appears once in W-1 §5.3 as a reading, and is not used as a classifier here.)
  "axis", "correlation", "counterexample", "discriminator" — my analytic scaffolding.
  Singular values, layer-cake identities, Chebyshev, nullspace/rank-nullity, solveset,
    Hilbert-Schmidt, Shale-Stinespring, Besov, Gronwall, Calderon-Zygmund,
    translation-invariance/compactness — ALL analytic machinery, consumed ONLY as
    sealed displays inside the sixteen named artifacts, never as independent authority.
    Where §8 re-executes any of them (layer cake, the two-cell system, the closure
    solution set) it re-derives a DISPLAYED identity and asserts nothing new.
NOTIONS DELIBERATELY NOT USED: any lens reading (none named, consumed, or motivated);
  faithfulness as authority (engaged nowhere); any topological-corpus notion beyond
  what a named artifact displays.
```

---
## §8 — CAS BATTERY (fresh venv; exact symbolic; zero numerics)

Environment: fresh venv `o11venv` under the session scratchpad, sympy 1.14.0; no `evalf`, no
`N()`, no float literal. Every integer/rational appearing is an algebra residual of an identity
check on ground-symbolic or record-displayed STRUCTURAL data — degrees, dimensions, exponents,
counts — never a physical value, never a member bound, never a measured constant. Script
sha256 `83a8728462e403c74b4168caec2f7d8371e6ad31e27b0dfbb791897135d79e72`. Output, verbatim:

```text
O11SR PARTITION-CROSS CAS -- exact symbolic, zero numerics (sympy)
PASS  [X1a] R-1: single-valued closure solves to EXACTLY Z (a CLASS)
PASS  [X1b] R-1: every integer n passes at SYMBOLIC n -- the class is forced, no member pinned
PASS  [X1c] R-1: non-integer fails exactly (residual -2 at 1/2)
PASS  [X1d] A-3: the member |n| ranges over a non-singleton admissible set -> NOT a point
PASS  [X2a] R-6: extent menu is a TWO-point admissible set -- not a point
PASS  [X2b] A-2: the bit is unfixed inside the forced menu -> selection is not forced
PASS  [X3a] R-7: the exponent pin is an INTERVAL of nonzero extent -- not a point
PASS  [X3b] A-11: location within the pin is unfixed
PASS  [X4] PAIRED SITES: require fixes the admissible SET (never a point); allow is the SELECTION within it (3/3)
PASS  [Y1] REQUIRE side: 7 non-localizable, 2 localizable (R-3, R-7) of 9 live rows
PASS  [Y2] ALLOW side: 8 localizable, 5 NON-localizable (A-2, A-3, A-9, A-10, A-12) of 13
PASS  [Y3] REFRAME leg 1 FAILS: require-side content is NOT systematically non-localizable
PASS  [Y4] REFRAME leg 2 FAILS: allow-side content is NOT systematically localizable
PASS  [Y5] DECISIVE: R-1 (class) and A-3 (member) are BOTH counting-type -- same axis, opposite sides
PASS  [Y6] DECISIVE: R-6 (menu) and A-2 (bit) are BOTH dimension-type -- same axis, opposite sides
PASS  [Y7] DECISIVE: R-7 (pin) and A-11 (location) are BOTH exponent-type -- same axis, opposite sides
PASS  [Y8] CORRELATION REFUTED at bytes: 2 require-side localizable + 5 allow-side non-localizable
PASS  [Z1] SHORTFALL triple re-derived exactly: (3/2, 2, 2), one orientation-free definition
PASS  [Z2] all three shortfalls STRICTLY POSITIVE (the surviving universal)
PASS  [Z3] under the OWN-GRADING-VARIABLE reading: degree-0 at 2 of 3 (FP-1, FP-3)
PASS  [Z4] K-4 CORRECTION re-derived: LOCALIZATION-blindness proper at FP-3 ONLY -- 1 of 3
PASS  [Z5] THE RECORD SEPARATES THE AXES: FP-1 degree-0 is n-UNIFORMITY in a CARRIER COUNT, not blindness in a localization direction
PASS  [W1a] W-1: layer-cake identities exact -- a sub-volume rate IS a singular-value COUNTING statement
PASS  [W1b] W-1: its own grading variable is the CARRIER COUNT -- W-1 is counting-typed, not localization-typed
PASS  [W1c] W-1: the demanded exponent 3-delta ranges over a RANGE, not a point -> ALLOW+RANGE by the four-case map ("any selection is a choice")
PASS  [W2a] W-2: the residual CAN vanish (delta_sect = u - 1/2) -- audit-corrected form carried
PASS  [W2b] W-2: the residual is NOT identically zero over admissible sections -> the per-cell value is SECTION-DEPENDENT: two admissible sections, every displayed clause held, values differ
PASS  [W2c] W-2: the delivered value carries lambda_JD linearly -- an UNASSIGNED unit rides the object
PASS  [W3a] W-3: two-cell system reduces exactly to beta*log(V1/V2) = 0
PASS  [W3b] W-3: unique solution beta = 0 -- derived EXISTENCE and UNIQUENESS on the ADMITTED asset
PASS  [W3c] W-3: that unique root is REFUSED by rule F3-d and is at the WRONG TYPE (form, not HS) -- displayed as the forced reading, left unadopted
PASS  [C1] THREE legs, three conditions, three freedoms -> SHAPE-2 (exact joint determination)
PASS  [C2] weakest-leg rider: the grade rides on the weakest leg -> a permission-grade leg caps the conclusion at ALLOW+UNIQUE = "WITHIN THE CLASS", not REQUIRE+UNIQUE
PASS  [C3] THE PIVOT: uniqueness DISPLAYED (sole discharger per step) but existence NOT DERIVED for any of the three -> the forcing criterion (existence AND uniqueness) is unmet 3/3
PASS  [C4] COUNTER-TEST: an UNBUILT FORCED object needs DERIVED EXISTENCE; none is displayed -> the three are uninhabited TYPE SLOTS, not unbuilt forced objects
PASS  [C5] RESIDUE, carried: no displayed impossibility ranges over objects of the required kind (each excludes a ROUTE, a REGIME, or a CLASS) -- allow-side means UNFORCED, not IMPOSSIBLE
TOTAL: 36 checks; FAIL count = 0
```

**WHAT THE BATTERY DOES AND DOES NOT CARRY.** Blocks X, Z, W re-derive DISPLAYED identities of
the sealed record (the closure solution set; the corrected shortfall triple and its blindness
counts; the layer-cake identities; the two-cell beta system; the section-difference residual and
its audit-corrected vanishing witness). Blocks Y and C are BOOKKEEPING CHECKS on this
commission's own tabulations — they verify that §4.1/§4.2 and §2.5 say what §4.3/§3.1 conclude
from them; they are not independent evidence for the classifications, which rest on the quoted
bytes. Two formulation notes, declared: (i) sympy's Reals-domain `solveset` returns {0} for
exp(2πix) = 1, a solver artifact — the class is established over ℂ as the sealed PT3d battery
does, with the real restriction re-established by the symbolic-n pass and the exact
non-integer failure; (ii) `beta` is declared real, not positive, so that 0 lies in its domain —
declaring it positive silently empties the solution set.

---
## §9 — CHOICE LEDGER (commission O11SR; every unforced choice, classified)

```text
CH-1  CLASSIFYING AGAINST THE SURVEY'S INVENTORIES RATHER THAN THE THEOREM'S Sigma:
      FORCED by the ground.  The partition theorem is scoped to Sigma and to J-6 by
      its own §6 ("J-2 OUT OF SCOPE"; "CURRENT GROUND ONLY"), and no clause of Sigma
      mentions a trace, an HS norm, a cell evaluation, or a form certification.  The
      dischargers are corpus objects, so only the corpus-wide inventories can reach
      them.  Declared at §1 C-SCOPE before any classification is made.
CH-2  TAKING "DERIVED EXISTENCE AND UNIQUENESS" AS TWO SEPARABLE HALVES: FORCED by
      the demand-reading's own sentence, which conjoins them.  The whole of §2.1
      turns on the separation, so it is flagged rather than buried.  The alternative
      reading (sole-ness alone suffices) would make every named absence in the corpus
      a forced object, which no consumed artifact asserts.
CH-3  SCORING W-2 AS **MIXED** RATHER THAN ALLOW-SIDE: FORCED in substance by W-2's
      own three-component decomposition (§1.1) plus CONV-5's identification of its
      form half with survey row R-3.  A flat "allow-side" would have hidden that the
      require-side half is the half already supplied — the most informative fact in
      the row.
CH-4  LOCALIZABILITY ASSIGNMENTS IN §4.1/§4.2: PREMISE(named), and the largest
      unforced element in this artifact.  Each cell is my reading of a row's own
      displayed type against an IMPORTED axis (§7).  MATERIALITY TESTED: the verdict
      of §4 does not depend on the marginal cells.  A-3 (an integer), A-2 (a
      dimension bit), A-9 (ranks) and A-12 (d = 4, spin-1/2) are non-localizable on
      any reading, and any ONE of them refutes leg 2; R-7's p = -3 is a spatial degree
      on any reading, and refutes leg 1.  Re-scoring A-8, A-10, A-4 or R-3 either way
      changes no verdict.
CH-5  RUNNING THE PAIRED-SITE DISPLAY (§4.3) AS THE POSITIVE RESULT: FORCED by the
      displays — the three pairs are the survey's own J-1/J-2/J-5, and the
      class-vs-member reading is the partition theorem's own note, quoted.  Nothing
      new is constructed; a pattern already in two sealed artifacts is named.
CH-6  CONSUMING O10SR AT VERDICT-LINE GRADE FROM A DECLARED SWEEP: UNFORCED.
      Grounds for reading it: it is the immediately prior commission and a sweep hit;
      failing to check it risked asserting "never cross-referenced" falsely.  Grounds
      against: it is not named ground.  RESOLUTION: flag block only, cited at two
      lines as corroboration, load-bearing nowhere.  Both readings displayed; the
      registrar may strike §4.4's last paragraph with no effect on any verdict.
CH-7  NOT OPENING ROOT 2: FORCED in substance — every named artifact resolved in the
      primary root at verified seals; the prior junction commission established root
      2 is a proper subset on the adjacent sweep.
CH-8  CARRYING W-3's beta = 0 FORCED READING PROMINENTLY (§2.4, §5.3) RATHER THAN AS
      a footnote: FORCED by the commission's own weighting of the counter-test.  It
      is the single displayed forcing shape anywhere in the discharger set, and
      burying it would be the exact false comfort the commission names.
CH-9  DECLARING THE LOCALIZABILITY AXIS IMPORTED (§7): FORCED by the commissioned
      IMPORT AUDIT.  The consequence is that §4's negative verdict is a negative about
      an imported notion — which is stated plainly rather than dressed as a discovery
      about the record.
ZERO entries in class OPEN.
```

## §10 — TOY_SEPARATION

```text
TOY_SEPARATION = clean.
  NO toy object is consumed as ground anywhere in this artifact.  The AJ's quarantined
  profile pair f1/f2 is NOT re-executed here; it is referenced only where the survey's
  own A-1 row and the partition theorem's §3.2 cite it, at their grade, as a
  meta-level statement about the derivability relation.
  The battery's scaffolds are: four rational singular-value lists (a generic ladder,
  re-verifying the sealed layer-cake identities); two symbolic cell volumes V1 != V2
  with a nonzero log-ratio (re-verifying W-3's two-cell system); one polynomial
  section-difference witness delta_sect(u) = u - 1/2 (the O6SR-AUDIT F-4b
  counterexample, reproduced exactly as that audit states it) and one generic profile.
  Every one is a PLACEHOLDER used to re-derive a displayed identity; none is asserted
  as surface content, as a candidate, as physics, or as a value of anything; none
  leaks into any classification, verdict, flag, or consequence.
  Every load-bearing object in §2-§6 is an actual sealed carrier or an actual sealed
  artifact's own verdict line.
```

---
## §11 — FLAG BLOCK AND VERDICT

```text
COMMISSION = O11SR (PARTITION-CROSS-BUILD; the discharger set crossed against the
  allow/require inventories; the reframe test; DETERMINATION ONLY).
SEALS = 16/16 named ground MATCH, shasum -a 256 -c run FROM THE ARTIFACT'S OWN
  DIRECTORY, before any reliance; MISMATCHES = 0.  One declared sweep hit (O10SR)
  seal-verified and consumed at flag-block grade only.  Inner carriers consumed at
  CARRIED GRADE, declared at §0.
SWEEP_CUTOFF = DECLARED (§0.1): 2026-08-15, primary root, two uncapped sweeps
  (filename + content) with exclusions; root 2 NOT consumed; no register/tracker/
  road/plan/continuation file read; "Q-..." EXPECTED-UNLOCATABLE.
COMMISSION_PREMISE = CONFIRMED AT BYTES — the two inventories and the discharger set
  have never been cross-referenced; the nearest neighbour (O10SR) asks a different
  question and does not classify the dischargers.

VERDICT = **MIXED**, exact:
  W-1  ALLOW-SIDE.  Vacancy display at the closure's own bytes ("no member exists in
       the scored corpus to name them"); a MEMBER-NAMED type slot (C-MEMBER); and the
       demanded exponent's admissible set is the HALF-LINE 3 - delta, delta > 0 —
       ALLOW + RANGE, "any selection is a choice", by the survey's own four-case map.
  W-2  MIXED WITHIN ITSELF, and the split is the finding.  W-2a (the (JD-3)/(JD-3a)
       form with its four obligations) is REQUIRE-SIDE — it IS survey row R-3, "The
       target form is FORCED; nothing in it is authored here" — AND IT IS ALREADY
       SUPPLIED, scored NOT OWED.  W-2b (the member) and W-2c (the displayed
       evaluation on a named oriented cell) are ALLOW-SIDE, on the strongest witness
       form the inventory has: a TWO-MODEL display on W-2's own deliverable (distinct
       admissible sections; per-cell values not identically equal), an exercised
       permission (sect_N an authored gauge; lambda_JD unassigned), and a blocking act
       the record itself names "NEW AUTHORSHIP".  EVERYTHING STILL OWED ON W-2 IS
       ALLOW-SIDE.
  W-3  ALLOW-SIDE, on the weakest evidence of the three.  Vacancy plus a barred value
       (beta "a SYMBOL", "PRODUCTION PROHIBITED"), and a MEMBER-SUPPLIED object.
       RESIDUE CARRIED, NOT DISPOSED: a displayed FORCED READING beta = 0, derived
       existence and uniqueness on the ADMITTED asset, refused by rule F3-d and at
       the WRONG TYPE (form, not HS).  The one forcing shape anywhere in the set.
  NONE meets the forcing criterion: uniqueness DISPLAYED 3/3 ("only sealed
  conversion", "only displayed replacement", "only permitted certification"),
  EXISTENCE DERIVED 0/3.  Sole-ness is a property of the demand, not of the object.

STRUCTURAL_QUESTION = YES, AND HERE IS ITS SCOPE.  At the boundary-closure, and there
  only, the certification apparatus makes a require-shaped conclusion conditional on
  permitted content.  The completed per-composite closure is SHAPE-2 whose three legs
  are, in everything still owed, permissions; by the demand-reading's own weakest-leg
  rider its grade is capped at ALLOW + UNIQUE = "WITHIN THE CLASS", never REQUIRE +
  UNIQUE = "YES".  SITES: FP-1/W-1, FP-2/W-2b+W-2c, FP-3/W-3 — three, per-composite.
  NOT COVERED: FP-S (decided, and forced with NO permission in its legs — the control
  case), J-6, J-2, the program verdict.

REFRAME_TEST = **FAILS AT BYTES; THE REFRAME IS REFUTED.**
  LEG 1 (require-side systematically non-localizable) FAILS: R-3's orientation
    covariance is per-oriented-cell and forced; R-7's DERIVED p = -3 is a degree in a
    spatial variable and forced.  Adjacent, from the closure's own certified stock:
    FP-2's admitted asset has degree -1 — certified content that IS graded in a
    localization variable (O6SR-A F-2, which killed the original universal).
  LEG 2 (allow-side systematically localizable) FAILS FIVE WAYS: A-3 |n| (an INTEGER
    — the reframe's own archetype of a global object, sitting ALLOW-side); A-2 the
    extent bit (a DIMENSION bit, and the record's strongest allow-side certificate);
    A-9 the ranks; A-12's d = 4 and spin-1/2; A-10's alpha' at the summed quantifier.
  AND THE FAILURES PAIR OFF AT THE SURVEY'S OWN SITES: J-1 R-1 class / A-3 member,
    both counting; J-2 R-6 menu / A-2 bit, both dimension; J-5 R-7 pin / A-11
    location, both exponent.  At each site the two halves are the SAME localizability
    type and differ only on force.  One axis cannot separate two objects sitting on
    the same point of it.
  WHAT THE PARTITION ACTUALLY TRACKS, derived from those three pairs: it forces the
    ADMISSIBLE SET and leaves the SELECTION WITHIN IT free — class/member, menu/bit,
    pin/location; never a point on the require side.  That is the forcing criterion's
    own axis ("the admissible set is a point"), and it carries no localizability
    content.  The record states it: "the clauses force the CLASS and pin no member."
  AND THE RECORD HAS ALREADY BEEN CORRECTED FOR THE REFRAME'S OWN CONFLATION: O8SR-A
    K-4 narrowed localization-blindness from 2 of 3 to **FP-3 ONLY, 1 of 3**, on the
    ground that "FP-1's variable is a CARRIER COUNT ... n-uniformity is a property in
    the carrier-size direction, NOT IN A LOCALIZATION DIRECTION."  Counting is not
    localization, at bytes, by the record's own correction.
  THEREFORE: the closure's localization-blindness does NOT follow from the partition.
    What DOES survive, and is a different statement: at FP-3 the blindness "is CREATED
    AT THE ADMISSION STEP, NOT FOUND IN THE COMMUTATOR" — a CERTIFICATION rule, not
    the partition.  The allow/require constraint is typed CELL_CONSTRAINT_ONLY of
    record and is not a selector at all.

COUNTER_TEST = NEGATIVE 3/3, one-sided, residues carried.  No discharger is an unbuilt
  forced object: derived existence is absent 3/3; the subject at W-1 has an EMPTY
  actual-stock domain until JB-2 lands; W-2 is answered POSITIVELY by section-
  dependence (a forced value cannot ride an authored gauge) and by the record naming
  its blocking act "NEW AUTHORSHIP"; W-3's forced reading is at the wrong type and on
  a reserved symbol.  LARGEST RESIDUE: at FP-1 "NO MATHEMATICS EXCLUDES THE REQUIRED
  OBJECT; A SUFFICIENT CONDITION FOR IT IS EXHIBITED OF RECORD" — with its antecedent
  supplied by no artifact in the swept stock.  AND, GENERALLY: no displayed
  impossibility ranges over any of the three objects as such — "each excludes a ROUTE,
  a REGIME, or a CLASS."  The three are UNINHABITED TYPE SLOTS: neither forced by the
  record nor excluded by it.  ALLOW-SIDE MEANS UNFORCED, NOT IMPOSSIBLE, AND SUPPLIES
  NO COMFORT — the obstacle is the same size; only its TYPE changes, from work not yet
  done to content not yet entered.

CONSEQUENCE = displayed both ways (§6); NOTHING PROPOSED.  Under allow-side the route
  is an ENTRY route, member-shaped at all three sites, with the conclusion's grade
  capped in advance and the partition theorem's re-posing clause governing the entry.
  Under require-side-unbuilt (refuted) it would be a WORK route with no cap.  The
  difference is authority, not effort.
CAS = fresh venv o11venv, sympy 1.14.0, 36/36 PASS, exact symbolic, no float, no
  evalf, no N(); script sha256 83a87284...; two formulation notes declared; blocks Y
  and C flagged as bookkeeping, not independent evidence.
IMPORT_AUDIT = §7.  Record-native: require/allow, the forcing criterion and its three
  shapes, the four-case map, class/member, type slot, the witness forms, sole-ness,
  the weakest-leg rider, degree/half-line/shortfall, the named grading variables.
  IMPORTED AND DECLARED: the LOCALIZABLE/NON-LOCALIZABLE dichotomy itself (this
  commission's construct, built only to run the test — and the test's result is that
  it does not track the partition); "metric-like"/"density-like"/"index-type"; all
  analytic machinery (layer cake, Chebyshev, HS, Shale-Stinespring, Gronwall,
  translation-invariance), consumed only as sealed displays.
CHOICE_LEDGER = CH-1..CH-9 (§9): 6 FORCED/FORCED-in-substance, 2 PREMISE(named), 1
  UNFORCED with both readings displayed (CH-6).  ZERO OPEN.  TOY_SEPARATION = clean.
FENCE_STATE = alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false — ALL REMAIN.
DETERMINATION_ONLY = held.  No adoption, no authored physics, no value, no number as a
  value of anything, no measured-constant comparison, no member bound.  No gate, flag,
  witness, fence, V-clause or quantifier of any consumed artifact moved.  FP-1/FP-2/
  FP-3 stand; FP-S stands decided; {W-1, W-2, W-3} remains the unique minimal set;
  every witness named in the ground stands untouched.  Nothing here discharges any
  named absence.  No route selected; no object proposed; no door opened.
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT before first
  write; no git; no existing file edited.

NET = the dischargers do lie on the allow side — W-1 and W-3 wholly, W-2 in
  everything it still owes — so the closure's certification rules do demand permitted
  content as a prerequisite for a require-shaped conclusion, and that conclusion's
  grade is capped at "within the class" by the record's own weakest-leg rider.  But
  the principal's reframe does not follow and is refuted at bytes: the partition does
  not sort the record into global and local content.  It sorts it into ADMISSIBLE SETS
  and SELECTIONS WITHIN THEM — the class is forced, the member is free — and at all
  three of the survey's paired sites both halves are the same localizability type.
  The closure's localization-blindness is therefore not a consequence of the
  allow/require partition.  Nor is it, on this ground, a gap in our tools: at FP-3 the
  record derives that the blindness is created by an ADMISSION RULE and that the
  localization the escapes ask for is present in the certified object's own sealed
  display and discarded at the admission step.  The obstacle is real, it is
  rule-shaped rather than partition-shaped, and no impossibility displayed anywhere
  reaches the three objects themselves.
```
