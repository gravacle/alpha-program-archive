# C4 Refuted-Clause Strike Repair Binding V001

STATUS: APPEND-ONLY REPAIR / ERRATUM BINDING. PRINCIPAL ACT APPLIED.
DATE: 2026-07-29.
LANE: Codex construction lane.

The sealed successor spec text is **not** edited in place. This artifact records
the principal's clause-scoped strike, in the C3/C5 repair-binding form, and
adds successor-visible records required by that ruling.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
coupling_evaluation_authorized = false
C4_frozen_status_revoked = false
C1_C2_C6_touched = false
CL1_replacement_identities_adopted_as_frozen_inputs = false
O1_F5_touched = false
CL2_touched = false
CL3_computed = false
```

## 1. Authority

```text
Principal decision:
  /Users/bgm/MB Work/alpha_supervision/C4_REFUTED_CLAUSE_STRIKE_PRINCIPAL_DECISION_2026-07-29.md
  sidecar/hash: 5c9b2819537fc3df99d9525426b10c4c2ce275fa967fa1d3364fc8355553d720

Supporting brief:
  /Users/bgm/MB Work/alpha_supervision/C4_DISPOSITION_BRIEF_2026-07-28.md

Supporting consumer sweep:
  /Users/bgm/MB Work/alpha_supervision/BOHM_C4_CONSUMER_SWEEP_RESULTS_2026-07-28.md

Witness:
  STAGE8_CL1_ATTEMPT_RESULT_REFUTED_V001.md
  hash: efb08860b888e24acaa50fdafdbe4afdb868450f79ec23120c2bd3eb1d40ddbb

T7(ii) obstruction:
  STAGE8_T7II_ATTEMPT_RESULT_OBSTRUCTION_NAMED_V001.md
  hash: 0f76ab73d7b226899ad00e24a0be10636b49982620375bd10491b898ecc917c7

H-ANGLE attempt:
  STAGE8_T7II_H_ANGLE_ATTEMPT_RESULT_OBSTRUCTION_NAMED_V001.md
  hash: 9f29088e31267f6d3c9ef28219c5a4b9ef1e78082657f9e10d8b0e9151809aff

R-L4 / C-L4 rebuild:
  STAGE8_RL4_CL4_REBUILD_CHARTER_AND_BALANCE_RESULT_V001.md
  hash: 34c2123d55838b5c9fe2dd2b8c9012b0e85e29be904922becff018bd5c9b149a
```

The principal decision is authority. It is disclosed as authority. It is not a
reading of sealed text and not a physical principle.

## 2. Clause-Scoped Strike

STRUCK from C4 at `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md:346-352`:

```text
1. the ball-locus qualifier:
   "on the ball where M(t) = 1"

2. the operator display and promotion claim:
   "sum_lambda w_lambda u_lambda = m_0(I-P) + 1·P = P"
   and the claim it carries, namely that the weights PROMOTE the sharp
   causal-ball projector to the leading in-cell object.
```

STRUCK also in prose from C4's parent,
`STAGE8_T7_E1_RECLASSIFICATION_CORRECTIONS_AND_ESCALATION_V001.md:58-60`:

```text
"Worse, they promote the SHARP causal-ball projector to the leading in-cell
object."
```

This is a clause-scoped strike. It does not revoke C4 as a frozen input. It
strikes only the operator-level promotion / ball-locus overreach.

## 3. Retained And Still Frozen

C4's scalar clauses are RETAINED AND STILL FROZEN:

```text
m_0 = m_1 = 0
m_{2j} = -2^{j-1}
all odd moments zero
the symmetric-second-difference form of the completed per-cell Kraus operator
annihilation of every Dyson term with fewer than two record insertions per CTP branch
the vanishing bare a-linear tadpole
lambda*tau_R in {0,+pi,-pi} arithmetic
exact saturation sum_lambda w_lambda phase_lambda = 1 = sum_lambda |w_lambda| at full tau_R
```

Nothing in this artifact touches C1, C2, or C6.

## 4. Ground Of Record

The struck text never had parent authority. The ball-locus qualifier and the
operator display first appear at spec drafting, `SPEC_V001:131-136`, and are
carried forward into `SPEC_V002:346-352`. The parent artifact contains scalar
C4 and only the prose promotion sentence struck above.

The exact witness is `STAGE8_CL1_ATTEMPT_RESULT_REFUTED_V001.md`
(`efb08860...`), whose counter-identity is:

```text
f(rho) = tau_R * (1 - 16 rho^4)
sum_lambda w_lambda R_lambda = cos^2(8 pi rho^4) * I
inf_P sup_lambda ||error||_op = sqrt(2)
```

The witness satisfies `SPEC_V002:313-315`'s requirement of a new exact witness
for contradicting a frozen input. The result refutes C4's operator-level
promotion only; it explicitly leaves C4's scalar clauses untouched.

## 5. Lead Flagged, Not Ruled

The replacement object's radial profile,

```text
cos^2(8 pi rho^4)
```

is unassessed decay information. The principal has explicitly **not** ruled on
whether it bears on A-L0 arm 2. It enters as a fresh question or not at all.

## 6. R1 -- Register The Two Lane Results

The principal ruling requires `efb08860` and `0f76ab73` to be findable.

```text
efb08860... = C-L1 refuted as written, with exact counter-identity.
0f76ab73... = T7(ii) obstruction named; conclusion stands, supporting
              idempotence clause corrected by the later erratum.
```

Convention C1 would file these as lane findings rather than Part 2 ruled rows.
But `STAGE8_CONSTRUCTION_CONVENTIONS_C1_C5_PROPOSED_RULES_V001.md` remains:

```text
rules_adopted = false
```

Therefore this artifact and the same-act `STAGE8_LANE_STATUS.md` update use a
Part 2 row only for the principal act itself. The two lane results are indexed
inside that row as rider-required lane findings, not as independent principal
rulings. The dependency on unratified C1 is flagged explicitly rather than
invoked as an adopted rule.

## 7. R2 -- Q2 Option-(ii) Cost Block Restatement

`SPEC_V002:1439-1447` presupposes the struck ball locus:

```text
the O7 obstruction record's exact witnesses depend on int_0^1 v = tau_R
holding ON THE BALL WHERE M = 1;
...
the exact record-phase collapse (+1,-1,-1) at lambda tau_R in {0,+-pi}
is EXACT only where M(t) = 1. Smoothing creates a transition region where
the phase is not +-1. Therefore C4's exactly saturated l1 step becomes an
inequality OF UNKNOWN DIRECTION; the exact Phase-2 route-(b) witness becomes
approximate; R.2's same-phase collapse V(0) = I and the sector-vanishing
obligation R-L4b no longer apply as written; the O7 record's exact witnesses
become approximate; exact arithmetic in Q(sqrt2) may no longer suffice.
```

Restated after the strike:

```text
Q2 option-(ii) can no longer cost smoothing as a perturbation of an exact
"ball where M(t)=1" collapse. Under D6' the record phase is radially profiled,
not two-valued on a positive-measure ball. The C4 scalar saturation at the
centre remains frozen, but the struck operator-level promotion and ball-locus
claim cannot be used to cost smoothing, O7 exact witnesses, R.2 same-phase
collapse, R-L4b sector-vanishing, or exact-Q(sqrt2) sufficiency.
```

No Q2 option is decided here. No Q2 cost is recomputed.

## 8. R3 -- C-L4 / kappa_bal Ownership

The construction lane (Codex) owns the R-L4/C-L4 rebuild after C-L1's supply is
withdrawn. The rebuild has already been executed in:

```text
STAGE8_RL4_CL4_REBUILD_CHARTER_AND_BALANCE_RESULT_V001.md
hash: 34c2123d55838b5c9fe2dd2b8c9012b0e85e29be904922becff018bd5c9b149a
```

The replacement balance is:

```text
kappa_bal = (3/8) / (3/8) = 1
```

This is not the old derivation `(1/2)/(1/2)`. It is over the corrected `n = 0`
class of the real `n = (lambda-mu)/sqrt2` partition. R-9 applies:

```text
kappa_bal is NOT kappa_record.
```

This artifact does not compute `kappa_record`, `kappa_Thomson`, or alpha.

## 9. R4 -- H-ANGLE Lapsed As Written; Route Class Open

H-ANGLE as written is recorded as LAPSED. The route class remains OPEN.

Ground:

```text
STAGE8_T7II_H_ANGLE_ATTEMPT_RESULT_OBSTRUCTION_NAMED_V001.md
hash: 9f29088e31267f6d3c9ef28219c5a4b9ef1e78082657f9e10d8b0e9151809aff
result: DOES NOT CLOSE
obstruction: H_ANGLE_RANGE_OBJECT_UNDEFINED_AND_NATURAL_PARENT_CANDIDATES_COLLAPSE
```

The obstruction is typing, not bounding. Every parent-visible range candidate
checked there collapses or is unsealed, including the corrected
`cos^2(8 pi rho^4) * I` object.

The four reopen requirements are preserved verbatim:

```text
1. derived from parent acts, not assumed as clustering or decay;
2. compatible with the Q-compressed sharp localizer;
3. not a thresholded carrier-dependent spectral cut unless F'-5 is addressed;
4. strong enough to produce the simple leading mode/gap T7(ii) needs.
```

## 10. R5 -- PA-C1 Falsified Prediction Of Record

`SPEC_V002:2007` is now recorded as a falsified prediction of record:

```text
PA-C1 OUTCOME-CLASS. C-L1 CERTIFIES. Confidence: high.
```

The outcome was:

```text
C-L1 is false as written.
```

This is the pre-registration system working. The prediction is not quietly
dropped.

`SPEC_V002:1998` PA-B3's ground is half-withdrawn:

```text
Ground: C6 with the volume-diagonal locus, plus C4's promotion of P to the
leading in-cell object.
```

The C6 volume-diagonal ground is untouched. The C4 promotion ground is struck.
Therefore PA-B3's ground is half-withdrawn; the prediction carries no operative
weight by its own terms and is not re-costed here.

## 11. Protected Status

```text
C4_clause_scoped_strike_applied = true
C4_operator_level_promotion_struck = true
C4_ball_locus_qualifier_struck = true
C4_parent_prose_promotion_claim_struck = true
C4_scalar_clauses_retained_and_frozen = true
C4_frozen_status_revoked = false
C1_C2_C6_touched = false
CL1_replacement_identities_adopted_as_frozen_inputs = false
radial_profile_decay_relevance_ruled = false

efb08860_registered_same_act = true
0f76ab73_registered_same_act = true
registration_form_depends_on_unratified_C1 = true

Q2_option_ii_cost_block_restatement_done = true
Q2_decided = false

CL4_kappa_bal_owner = Codex_construction_lane
kappa_bal_replacement_value = 1
kappa_bal_is_kappa_record = false

H_ANGLE_as_written = LAPSED
H_ANGLE_route_class = OPEN
PA_C1_prediction_status = FALSIFIED_PREDICTION_OF_RECORD
PA_B3_ground_status = HALF_WITHDRAWN

O1_F5_touched = false
CL2_touched = false
CL3_computed = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
