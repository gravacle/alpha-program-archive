# STAGE 8 — THE ENVIRONMENT FACTOR: EVERY NAMING, AND THE ABSENCE BEHIND IT
# COMMISSION O31SR — ENVIRONMENT-BUILD — 2026-08-15
# DETERMINATION ONLY. NOTHING CONSTRUCTED, NOTHING PROPOSED, NOTHING ADOPTED.

---

## §0 — TARGET, SEALS, CUTOFF, PATTERNS

### §0.1 THE TARGET

The earliest sealed statement of the boundary gives a three-factor structure.
Factor 1 = the source. Factor 2 = the record. Factor 3 = the
**boundary/environment**. This commission establishes what the corpus says
factor 3 *is*, and what standing that saying has.

Output path probed ABSENT before any write:
`STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md` — `No such file or directory`;
`.seal.sha256` — `No such file or directory`.

### §0.2 SEALS VERIFIED THIS COMMISSION

All verified with `shasum -a 256 -c` **from the artifact's own directory**.
Both sidecar conventions probed (`NAME.md.seal.sha256` and `NAME.seal.sha256`),
and bundle seals probed where a per-file sidecar was absent.

```text
COMMISSIONED GROUND
STAGE8_RECORDS_PLURAL_O30SR_V001.md                          OK
STAGE8_RECORDS_PLURAL_O30SR_AUDIT_V001.md                    OK
STAGE8_COMPLETION_MAP_T17SR_V001.md                          OK
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md                    OK
STAGE8_STRATIFICATION_O27SR_V001.md                          OK
STAGE8_STRATIFICATION_O27SR_AUDIT_V001.md                    OK

SOURCES CONSUMED BY THE SWEEPS
PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md
                        OK  (older convention: NAME.seal.sha256)
R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_SPEC_V001.md           OK
R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_RESULT_V001.md         OK
                        (no per-file sidecar; sealed inside the BUNDLE seal
                         R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_V001.seal.sha256,
                         all 5 bundle lines OK)
STAGE8_THEORY_CANDIDATE_001_ENUMERATION_AUDIT_EINSTEIN_V001.md  OK
BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md         OK
```

`BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md` — **NO SIDECAR OF ANY
CONVENTION, NO BUNDLE SEAL. UNSEALED.** It is quoted below because it carries
the strongest object located; its unsealed status is carried with every use of
it and is decisive for the verdict.

### §0.3 CUTOFF AND ROOTS, MEASURED

```text
CUTOFF (newest file, either root, by mtime):
  2026-08-15 17:45:00
  /Users/bgm/MB Work/alpha-program-archive/workspace/
    STAGE8_RECORDS_PLURAL_O30SR_AUDIT_V001.md.seal.sha256

ROOT 1 (primary) /Users/bgm/MB Work/alpha-program-archive/workspace
                 6867 files
ROOT 2           /Users/bgm/Documents/New project/gravity_emergence_evidence_
                 program/alpha_fundamental_record_action_cleanroom_v003
                 7805 files
TOTAL SWEPT      14672 files
```

### §0.4 GREP ENGINE — DECLARED, AND WHY IT MATTERS

The interactive `grep` on this machine is a **shell function wrapping ugrep**
with `--ignore-files --hidden -I --exclude-dir=...`. `--ignore-files` honours
ignore-files and can silently drop corpus files from an "uncapped" sweep.
**Every sweep below was run with `/usr/bin/grep` (BSD grep 2.6.0-FreeBSD)
as the authority**, invoked by absolute path so the wrapper cannot intercept.
This follows D-3 of the O30SR audit.

### §0.5 EVERY SWEEP PATTERN, DECLARED VERBATIM

All sweeps recursive over BOTH roots, uncapped, `-rInE` (or `-rIniE` where
noted case-insensitive), no `head`, no result cap.

```text
E1  H_boundary|H_environment|H_env|boundary/environment
E2  environment                                        (case-insensitive)
E3  (further patterns declared at their point of use in §1.4 and §3)
```

### §0.6 SWEEP YIELD

```text
E1  (literal factor token)        21 lines, 11 distinct paths / 7 basenames
E2  (environment, any case)     1802 lines, 266 distinct basenames
    less 3 forbidden files       1777 lines   (see §0.7)
    distinct line-texts            470
    SENSE-A (physical factor)      282 lines, 69 distinct .md files
```

### §0.7 FILES EXCLUDED BY STANDING ORDER (never opened)

```text
LOCKED_ALPHA_PLAN_DEPENDENCY_REPAIR_V001.md      (plan)
QUESTIONSSETTLED_REGISTER_V001.md                (register)
STAGE8_COMPLETION_PLAN_PROPOSAL_V001.md          (plan)
```
A fourth basename matched the exclusion regex on a false positive —
`STAGE8_TASK3D_FOUR_PLANE_STEP3_RATIFIED_TYPING_DETERMINATION_V001.md`
matches `PLAN` inside `FOUR_PLANE`. It is a determination, not a plan, and was
read. Disclosed so the exclusion count is auditable.
No `Q-...` artifact was sought or found: EXPECTED-UNLOCATABLE, as commissioned.

---

## §1 — DELIVERABLE 1: EVERY NAMING, GATHERED AND TYPED

### §1.0 THE SENSE SPLIT, DECLARED BEFORE ANY COUNT

The token `environment` carries **four** unrelated senses in these roots. Only
the first is the commissioned subject. The split is stated first because the
majority of raw hits are not about physics at all, and a count that did not
separate them would be worthless.

```text
SENSE-A  THE PHYSICAL THIRD FACTOR — boundary/environment, edge/environment,
         the environment sector of the action, environment degrees.
         *** THE SUBJECT OF THIS COMMISSION. ***           282 lines / 69 .md

SENSE-B  THE COMPUTATIONAL ENVIRONMENT — virtualenvs, `os.environ`, cleared
         child environments, `environment_event_ledger`, PYTHONHASHSEED,
         `sys.flags.ignore_environment`.  This is the audit harness's process
         hygiene.  NOT PHYSICS.                    ~700 .py + 108 .json lines

SENSE-C  THE LaTeX ENVIRONMENT — `align*`, `eqnarray`, `IEEEeqnarray`,
         `multiline_latex(..., environment=...)`.  Vendored SymPy source.
         NOT PHYSICS, NOT THIS CORPUS'S WRITING.            158 infra lines

SENSE-D  THE OPERATING ENVIRONMENT AS A HAZARD — "an environment that resets
         these bits unprompted", "the state does not hold itself in this
         environment".  A fence-integrity observation.  NOT PHYSICS.
```

Sense-B/C/D are recorded here and then set aside. Everything below is Sense-A.

### §1.1 THE EARLIEST SEALED STATEMENT — THE THREE-FACTOR STRUCTURE ITSELF

`PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md`
(2026-07-23) — **SEAL OK** — heading and body, :27-35, verbatim and whole:

```text
## Complete closure object

The complete record-forming generator must act on the source, primitive
record carrier, and all required boundary/environment degrees:

G_closure acts on
  H_source tensor H_record tensor H_boundary/environment.
```

```text
ROLE-TYPE:  ARGUMENT (third tensor slot of the domain of G_closure)
            + CARRIER (it is an `H_`, a Hilbert space)
GOVERNING VERB: "must act on" — the factor is DEMANDED into existence by an
            obligation on a DIFFERENT object (the generator), not asserted to
            exist on its own account.
QUANTIFIER: "all required ... degrees" — quantified over, never enumerated.
```

Two further facts about this file, both load-bearing and neither displayed by
the O30SR build:

```text
(a) THE FACTOR IS NAMED ONCE AND NEVER RETURNS.  `boundary/environment`
    occurs at :30 and :34 and NOWHERE ELSE in the file.  The remaining 147
    lines develop the source-odd bilinear, phi_R, m_free, and the no-retuning
    rule.  The third factor is introduced and abandoned inside its own
    founding document.
(b) THE FILE'S OWN "boundary closure field" IS A DIFFERENT OBJECT.  At :44-47
    — "the local chiral-odd scalar component of `G_closure` is supplied by the
    same boundary closure field that participates in making the record
    durable." — the noun is a FIELD (phi_R = a_R + i b_R, a coefficient
    function), not the Hilbert factor `H_boundary/environment`.  NO SENTENCE
    IN THE FILE IDENTIFIES THEM.  Any reading that treats the constructed
    phi_R as content for the third factor is an inference, not a quotation.
    I make no such identification.
```

### §1.2 THE FACTOR AS A CARRIER OF DURABILITY — TWO SEALED SENTENCES, BOTH NEGATIVE

`R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_SPEC_V001.md` — **SEAL OK** — :83-87,
item 4 of a list of things that must ALREADY be derived, quoted whole to the
end of its sentence:

```text
4. the identification of that sector, rather than a gapped source or another
   boundary/environment sector, as the carrier of record durability.
```

`R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_RESULT_V001.md` — **SEAL OK** (no
per-file sidecar; carried by the bundle seal, all 5 lines OK) — :95-101, under
the heading `## Why this is not yet the physical spectral theorem`, item 4 of
what has **not yet** been derived, quoted whole:

```text
4. why this continuum sector, rather than a gapped source mode or a distinct
   boundary/environment sector, carries durability.
```

```text
ROLE-TYPE:  SIDE OF A RELATION — specifically the REJECTED side.  In both
            sentences the environment sector appears as the ALTERNATIVE that
            the continuum sector must be distinguished FROM.
STANDING:   Both sit inside NOT-YET-DERIVED lists.  The corpus does not know
            which sector carries durability; the environment sector is a live
            rival that has never been ruled in or out.
```

### §1.3 THE FACTOR AS A TERM IN AN ACTION — THE STRONGEST OBJECT, AND IT IS UNSEALED

`BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md` — *** NO SIDECAR OF
EITHER CONVENTION, NO BUNDLE SEAL, **UNSEALED** *** — :132-143, quoted whole:

```text
If no shared boundary, gauge, or environment term couples them, their
evolution factorizes and the completed-record transition amplitudes multiply.

For overlapping supports or shared fields, the commutator need not vanish.
The connected parent must therefore be generated by one complete action:

B_parent
  =B_Dirac(g,A)
   +sum_c B_c
   +B_boundary/environment(g,A,record).
```

```text
ROLE-TYPE:  SUMMAND / FUNCTION SYMBOL APPLIED TO A DECLARED ARGUMENT LIST.
            This is the ONLY place in either root where the factor is given a
            SIGNATURE: it is a term depending on (g, A, record) — metric,
            gauge field, and the record.
STANDING:   UNSEALED.  And the sentence that follows it, :145-147, withdraws
            any content it might have seemed to carry:
              "Its overlap terms, ordering, preparation, and connected
               cumulants are outputs of that action."
            The term is named as an obligation on an action that does not
            exist.  No formula for `B_boundary/environment` is given here or
            anywhere else located by any sweep.
```

### §1.4 THE FACTOR AS A CLOSED-SYSTEM MEMBER

`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` — **SEAL OK** — :23-25,
under `## Common closed dilation`, quoted whole to the end of its sentence:

```text
Let the source, designated record subsystem, and required environment form
one closed Hilbert space.
```

```text
ROLE-TYPE:  ARGUMENT / CARRIER, in the IMPERATIVE-HYPOTHETICAL mood ("Let ...
            form").  A supposition made for the calculation, not a claim that
            such a space has been built.
```

Same file, :164, inside a ````text` block listing what the gate does **not**
supply, quoted whole:

```text
the complete source-record-environment action;
```

```text
ROLE-TYPE:  MENTION, inside an inventory of debts.
```

### §1.5 THE ROLE-TYPE CENSUS ACROSS ALL SENSE-A HITS

Every Sense-A occurrence, without exception, falls into one of six role-types.
The census is over the 282 Sense-A lines; representative bearers named.

```text
ROLE-TYPE          COUNT-CLASS   GOVERNING MOOD        BEARER (example)
--------------------------------------------------------------------------
ARGUMENT of a      the founding  "must act on"         PRIMITIVE_..._PAIRED_
generator/action   statement     "Let ... form"        RETURN_V002 :29-34
                   + ~40 lines                         ONSET_SAT_GATE_V003 :23

CARRIER (an `H_`,  ~25 lines     "the complete         FULL_CARRIER_..._
a factor, a                      edge/environment      OBSTRUCTION_GATE_V001
sector, degrees)                 carrier" — always     :131, :145
                                 under "complete",
                                 "required", "physical"

SIDE OF A          4 lines       "rather than another  R3_4_..._PULLBACK_
RELATION                         boundary/environment  SPEC/RESULT :87/:101
(the rejected                    sector"
alternative)

SUMMAND / TERM     1 line        "+B_boundary/          BID_GLOBAL_CAR_...
with a signature   *** ONE ***   environment(g,A,      _V001 :142  UNSEALED
                                 record)."

INDEX / LABEL in   ~90 lines     "source-record-       every flag named
a compound flag                  environment"          `complete_source_
or slot name                     "source/gauge/        record_environment_
                                 gravity/environment"  operator_derived`

MENTION inside a   ~120 lines    "would-build",        ONSET_SAT_GATE_V003
DEBT LIST, a                     "does not yet         :164; AXN skeleton
would-build, or                  prove", "no ...       crosscheck :213
an ABSENCE claim                 formula", "FREE",
                                 "UNBUILT", "= false"
```

*** THE SHAPE OF THE CENSUS IS THE FINDING. The two largest classes are
    INDEX-IN-A-FLAG-NAME and MENTION-IN-A-DEBT-LIST — together roughly three
    quarters of all Sense-A lines. The factor is overwhelmingly named as part
    of the NAME OF SOMETHING THAT DOES NOT EXIST (`complete_source_record_
    environment_operator_derived = false`) or as an ITEM IN A LIST OF WHAT IS
    OWED. It is named as a subject of a positive assertion in no sealed
    sentence located by any sweep. ***

---

## §2 — DELIVERABLE 2: WHAT THE CORPUS SAYS THE FACTOR *IS*

Assembled from the located statements alone. Where accounts disagree the
disagreement is displayed, not reconciled.

### §2.1 ITS TYPE — FOUR INCOMPATIBLE TYPINGS, ALL SEALED

```text
TYPING 1  A HILBERT SPACE.  "H_source tensor H_record tensor
          H_boundary/environment."   PRIMITIVE_..._PAIRED_RETURN_V002 :34
TYPING 2  A SET OF DEGREES OF FREEDOM.  "all required boundary/environment
          degrees"                     ibid. :30
          "the physical edge/environment degrees required by durable closure"
                                       FULL_CARRIER_..._GATE_V001 :145
TYPING 3  A SECTOR.  "another boundary/environment sector"
                                       R3_4_..._PULLBACK_SPEC_V001 :87
TYPING 4  A TERM IN AN ACTION.  "+B_boundary/environment(g,A,record)."
                                       BID_GLOBAL_CAR_..._V001 :142  UNSEALED
```

A Hilbert space, a set of degrees, a sector, and a functional are four
different kinds of object. No sealed sentence relates them. The corpus uses
the four interchangeably.

### §2.2 WHAT ACTS ON IT

```text
G_closure, the record-forming generator — "must act on"
                                       PRIMITIVE_..._PAIRED_RETURN_V002 :29-34
U_0(tau), U_1(tau), the source-conditioned closed-dilation unitaries, on the
  space the source, record and "required environment" are LET to form
                                       ONSET_SATURATION_ACTION_GATE_V003 :23-27
B_parent, via its own environment summand (unsealed)
                                       BID_GLOBAL_CAR_..._V001 :139-142
```

Every one of these is an object the corpus separately records as **not
derived**. Nothing that has been built acts on the factor.

### §2.3 WHAT IT CARRIES — CONTESTED, AND THE CONTEST IS SEALED ON BOTH SIDES

```text
CLAIMED CARGO   "durability" / "the carrier of record durability"
CONTEST         Both sentences that attach durability to the environment
                sector attach it AS THE REJECTED ALTERNATIVE:
                  "the identification of that sector, RATHER THAN a gapped
                   source or another boundary/environment sector, as the
                   carrier of record durability."   SPEC :87
                  "why this continuum sector, RATHER THAN a gapped source mode
                   or a distinct boundary/environment sector, carries
                   durability."                     RESULT :101
RESOLUTION      NONE.  The RESULT file lists this as item 4 of what has
                "not yet" been derived.  The corpus does not know whether the
                environment sector carries durability or not.
```

Its only declared functional dependence, from the unsealed line: `(g, A,
record)` — metric, gauge field, record. Nothing states its dimension, its
algebra, its state, or its dynamics. **§3.4 below reports the one apparent
counterexample to "no dimension" and shows why it is not one.**

### §2.4 WHAT IT IS DISTINGUISHED FROM — AND WHERE THE DISTINCTION COLLAPSES

Distinguished from, by sealed sentence: the source; the record; "a gapped
source mode"; "this continuum sector".

The distinction **collapses** in the one file that puts a concrete factor in
the third slot. `FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md`
(SEAL OK) :19-24 names the third factor `C^2_edge/witness`, and :390-391 of
`STAGE8_TASK2F_..._DETERMINATION_V001.md` (SEAL OK) says of it, quoted whole:

```text
3. `C^2_E` is edge/witness. It is neither a CTP branch nor a charge sector.
   C0 has no identified edge/environment factor.
```

*** EDGE, WITNESS AND ENVIRONMENT ARE RUN TOGETHER INTO ONE SLOT AND ONE
    SYMBOL. The corpus's compound tokens do this everywhere —
    `edge/environment`, `boundary/environment`, `source-record-environment/
    edge`, `spectator/vacuum/binding/edge/environment`. There is no sealed
    sentence anywhere in either root that separates the environment from the
    edge, or from the witness, or that says what distinguishes them. ***

### §2.5 WHAT IT LIES BETWEEN — *** SIX NON-AGREEING ACCOUNTS, NOT THREE ***

The prior determination (O30SR §3.5) found three accounts and its audit
(O30SR AUDIT COR-7) found that only one of the three was carried by a
sentence. Both findings are confirmed at bytes and **both are understated**.
Sweeping for the factor itself rather than for "the second side" raises the
count to six.

```text
ACCT  DATE        SEAL   WHAT THE ENVIRONMENT IS SAID TO BE
----------------------------------------------------------------------------
 1    2026-07-23  OK     A THIRD HILBERT TENSOR FACTOR beside source and
                         record.  "H_source tensor H_record tensor
                         H_boundary/environment."
                         PRIMITIVE_..._PAIRED_RETURN_V002 :34
                         *** CARRIED BY A DISPLAY BLOCK, NOT A SENTENCE.
                             No sentence in that file says the record's
                             boundary is a boundary between the record and
                             the environment.  (O30SR AUDIT COR-7, confirmed
                             by me at bytes.) ***

 2    2026-07-23  OK     A BINARY EDGE/WITNESS FACTOR, `C^2_edge/witness`,
                         declared "only for this identifiability
                         calculation", and in the SAME FILE :32-33 declared
                         to EXCLUDE the environment: "a genuine environment
                         spectrum remain[s] outside it."
                         FULL_CARRIER_..._GATE_V001 :19-24, :31-33

 3    2026-07-24  OK     THE COPIED RECORD CELLS THEMSELVES.  The "two
                         conditional environment sectors" ARE the N copies
                         |h>^tensor_N of the record.  The environment is not
                         a third system at all — it is the record, counted
                         again.
                         CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001
                         :14-20  (bundle seal, all 5 lines OK)

 4    2026-07-24  OK     AN EFFECTIVE ONE-SIDED CONTINUUM, EXPLICITLY
                         DOWNSTREAM.  "An effective environment is
                         downstream."  CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE
                         _V001 :33  (V002 sealed OK)

 5    2026-08-12  OK     THE SAME CELL UNDER A SECOND DESCRIPTION.  "The
                         boundary is the record cell's junction — the
                         interface between the cell's internal
                         (projective/phase) description and its external
                         (diamond-shape) description."  No second system
                         exists on this account.
                         STAGE8_BOUNDARY_FORCING_CHARACTERIZATION_FABLE_V001
                         :127  (verified verbatim by me)

 6    2026-08-14  OK     THE COMPLEMENT OF A CAUSAL BALL INSIDE ONE CELL.
                         *** BUT: the word "environment" occurs ZERO TIMES
                         in the whole of STAGE8_R2_KAPPA_N_DETERMINATION_
                         S9AD_V001.md (`/usr/bin/grep -c` = 0, measured).
                         This account is a READING OF A PROJECTOR
                         DEFINITION, not a naming of the factor.  It is
                         listed so the disagreement is complete, and marked
                         NOT A NAMING. ***
```

*** THE DISAGREEMENT, STATED EXACTLY. Of six accounts of what the third
    factor is: ONE is carried by a full sentence and asserts the environment
    is the record recounted (3); ONE is a sentence and asserts there is no
    second system at all (5); ONE is a display block with no sentence (1);
    ONE explicitly excludes the environment from itself (2); ONE declares
    itself downstream and non-fundamental (4); and ONE never uses the word
    (6). No sealed statement in either root reconciles any two of them.
    I reconcile nothing and supply nothing. ***

---

## §3 — DELIVERABLE 3: ANY CONSTRUCTION, ANY ATTEMPT

The commission requires a sharp distinction between a construction, a
specification of what a construction would have to satisfy, a would-build
note, and a bare naming. The five rungs are defined first, then everything
located is placed on exactly one rung.

```text
RUNG 5  CONSTRUCTION.  The factor is exhibited: a space with a stated
        dimension or basis, an algebra, a state, or a dynamics, asserted of
        THE PHYSICAL THIRD FACTOR and not disclaimed.
RUNG 4  PARTIAL CONSTRUCTION / SURROGATE.  A concrete object is exhibited in
        the third slot, but the artifact itself disclaims that it is the
        physical factor.
RUNG 3  SPECIFICATION.  What a construction would have to satisfy is stated
        as a checkable obligation list, signature, or disjunction.
RUNG 2  WOULD-BUILD NOTE.  A named intention with no obligations attached.
RUNG 1  BARE NAMING.  The token appears in a slot, a flag name, or a list.
```

### §3.1 RUNG 5 — CONSTRUCTION: *** NONE. THE ABSENCE IS MEASURED, NOT ASSERTED. ***

Sweep E3, uncapped over both roots, five definitional patterns, infrastructure
senses filtered out:

```text
PATTERN                                                        HITS
H_(boundary|environment|env|E) *(:?=|is defined|:=)              12 -> 0
(define|definition of|let|set) +(the +)?(boundary/)?environment   0
dim *\(? *H_(E|env|environment)                                   0
(the )?environment (algebra|state|Hilbert space|dimension|
  spectrum) (is|=)                                                0
environment (is|shall be|will be) (given|taken|defined|
  constructed|realized)                                           0
```

The 12 hits on the first pattern are **all false positives** and are listed
so the reduction to zero is auditable: six distinct lines, every one matching
`h_e` (an EDGE HOLONOMY representative — `h_e=1`, `h_e=w`, `u_e^aug = -i
h_e^(-1) d h_e`) or the flag `C_BI_COMPATIBLE_WITH_E = true`. Not one is the
environment Hilbert space.

*** ACROSS 14672 FILES IN BOTH ROOTS THERE IS NO SENTENCE THAT DEFINES
    H_boundary/environment, STATES ITS DIMENSION, STATES ITS ALGEBRA, STATES
    ITS STATE, OR STATES ITS DYNAMICS. RUNG 5 IS EMPTY. ***

The corpus says so itself, in sealed bytes.
`STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md` — **SEAL OK** —
:211-213, under the heading `NOT SELECTED OR ADOPTED:`, quoted whole:

```text
no action member, term formula, coefficient, decomposition, or descendant roster
no environment field or environment-action formula
```

### §3.2 RUNG 4 — THE STRONGEST OBJECTS THAT EXIST. THREE. ALL DISCLAIMED.

*** THESE ARE THE STRONGEST THINGS IN THE CORPUS. Each puts a concrete object
    in the environment slot. Each is disclaimed, in its own file, in sealed
    bytes, as not being the physical third factor. ***

**(4a) `C^2_edge/witness` — a two-dimensional surrogate, with an algebra,
used in a real calculation.**
`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md` — **SEAL OK** —
:15-33, quoted whole:

```text
## Reduced carrier under test

Declare only for this identifiability calculation:

H_red
  = C^2_source-grading
    tensor C^2_record-endpoint
    tensor C^2_edge/witness.

The first two factors are the reduced factors already used in the sealed
source-record structure gate. The third is the smallest binary witness factor
on which one could test whether an additional closure/edge degree removes the
coefficient ambiguity.

This three-factor ansatz is not derived as the complete physical carrier.
Spin, spacetime, gauge transport, topology, ghosts, and a genuine environment
spectrum remain outside it.
```

This surrogate is genuinely worked: :45-48 gives it the operator span
`{X_S,Y_S} tensor {X_R,Y_R} tensor {I_E,X_E,Y_E,Z_E}`, :57 gives it a grading
weight `w_E` in `Z_w = w_S Z_S + w_R Z_R + w_E Z_E`, and the status block
records outcomes turning on it (`unit_weight_candidate_writes_edge = false`,
`source_doubled_candidate_writes_edge = true`).

```text
DISCLAIMER, SAME FILE, SEALED, :31-33:  "a genuine environment spectrum
remain[s] outside it."  THE FILE DENIES THAT ITS OWN THIRD FACTOR IS THE
ENVIRONMENT.  And :160-161: `reduced_three_factor_carrier_declared = true`,
`complete_physical_carrier_derived = false`.
```

**(4b) `H_E^(c)` — a per-cell environment factor inside a total Hilbert space,
with an isometry into it.**
`stage8_execution/work/MAJORANT_LEMMA0_PROOF_DRAFT_V001.md` — sidecar
`shasum -a 256 -c` **OK** — :80-104, quoted whole to the end of its sentence:

```text
H_tot = F  tensor  ( tensor_c H_R^(c) )  tensor  ( tensor_c H_E^(c) ),

with F the CAR/Fock factor over the one-particle carrier, H_R^(c) = C^3
the c-th record factor, H_E^(c) the c-th environment (relayed-record)
copy.
```

and the map into it, :99-104, quoted whole:

```text
- (relay, sealed type) R_c |p_(c,h)> = |e_(c,h)> tensor |r_(c+1)>:
  an exact isometry from the cell-c public endpoint into
  H_E^(c) tensor H_R^(c+1), record-preserving (h -> e_(c,h) is a fixed
  orthonormal-basis map), acting as the identity on F and on all other
  factors;
```

This is the closest thing in either root to a built environment: a named
factor, a home in the total space, and an exact isometry landing in it. **Two
disclaimers, both in the file's own bytes:**

```text
(i)  IT IS THE RECORD, COPIED.  The file's own gloss is
     "(relayed-record) copy".  H_E^(c) is populated by |e_(c,h)>, the image
     of the record endpoint under a fixed orthonormal-basis map.  It carries
     no datum the record does not already carry.  It is not a third system.
(ii) *** THE FILE DECLARES ITSELF NOT SEALED AND NOT AUTHORITY, AT :3,
     VERBATIM: "Date: 2026-07-25. Status: DRAFT — NOT SEALED, NOT A RESULT
     ARTIFACT." ***  The sidecar hash verifies — the BYTES are intact — but
     the artifact disowns its own standing.  Seal-verified is not the same
     as authority-bearing, and here the two come apart explicitly.
```

**(4c) `rho_5(E) = exp(-E)` — an effective continuum environment with an
actual spectral density.**
`CAUSAL_DIRECT_LIMIT_ARCHITECTURE_TEST_SPEC_V001.md` — **SEAL OK** — :74-84,
quoted whole:

```text
### A5: effective continuum environment

The normalized one-sided continuum representative is

rho_5(E) = exp(-E),
A_5(t) = (1 + i t)^(-1).

Its spectral density is an effective-model choice and must not be promoted
to a microscopic law.
```

```text
DISCLAIMER, SAME FILE, SEALED, :83-84 — quoted above in full.  And
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001 :32-34 (V002 SEAL OK), quoted whole:
  "Finite half-line, tree, and spatial-lattice models are regulators or
   reductions. An effective environment is downstream. None supplies an
   independent damping law or spectral density."
This is displayed as CORPUS CONTENT ONLY.  It is an effective-model form the
corpus forbids promoting; it is not adopted here, not used, and is not a
program quantity.
```

**Also on rung 4, and already typed in §2.5 as ACCOUNT 3:** the "two
conditional environment sectors" of
`CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001.md` (bundle seal, all 5
lines OK), :18-26 — orthogonality asserted for every `N>=1` and an overlap law
`|<E_0^(N)|E_1^(N)>|=|gamma|^N`. Same disclaimer as (4b) applies from the
bytes themselves: the sectors ARE the copies `|h>^tensor_N` of the record.

### §3.3 RUNG 3 — SPECIFICATION: *** THIS RUNG IS OCCUPIED, AND STRONGLY. ***

This is the finding that fixes the verdict. See §4, which quotes the
obligations in full. Three distinct specifications exist:
the six-item exact reopen condition
(`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md:141-155`); the
missing-object signature `SRE_PHYSICAL_REALIZATION_EMBEDDING` with domain,
codomain and required components
(`STAGE8_TASK2F_..._DETERMINATION_V001.md:398-415`); and the
`ENVIRONMENT_SECTOR_CLOSURE` disjunction (§3.5).

### §3.4 THE ONE APPARENT DIMENSION STATEMENT, AND WHY IT IS NOT ONE

Two sealed files appear to give the environment a dimension. Both are
disqualified at bytes, and the disqualification is displayed because the
sentences would otherwise refute §3.1.

```text
COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_SPEC_V001.md (SEAL OK) :86
  "The composite record environment has dimension nine."
COMPLETE_QSPEC_CANONICAL_FULL_ZERO_FREE_PROMOTION_SPEC_V001.md (SEAL OK) :146
  "The completed-record environment has nine outcomes."
```

Both are **the record**, not the third factor. The first file's own frozen
constants block, :26-33, settles it — `two-cell record outcomes = 9`. The
"environment" here is the composite of two cells' RECORD registers, named
"environment" only because it is the factor being traced out in a
Hilbert-Schmidt partial-trace bound. The noun is relative ("what is traced
out"), not the three-factor structure's third slot.

*** THE PATTERN IS NOW VISIBLE AND IT IS THE SAME PATTERN THREE TIMES.
    Wherever the corpus succeeds in exhibiting something it calls an
    environment — §3.2(4b), §3.2 ACCOUNT 3, §3.4 — the thing exhibited turns
    out to be THE RECORD UNDER A SECOND NAME. The third factor is never what
    gets built. What gets built is the second factor, relabelled. ***

### §3.5 THE ONE RECORDED ATTEMPT — AND IT WAS DECLINED, ON THE RECORD

An attempt to construct the environment term was contemplated and **refused**,
with reasons stated. `STAGE8_AXN_BUILD_SKELETON_V2_DARIO_V001.md` — **SEAL
OK** — :51-56, quoted whole to the end of its sentence:

```text
**Environment sector — note-level state disclosed as such.** 829 §1 argues at the **variable** level
that no ninth environment field is supplied and *"Adding another field would be authorship."* That
argument is sound and I do not disturb it. At the **term** level the environment sector likewise has
no row. **I record this as note-level, not as an eleventh hole**: unlike gravity, it received an
argument rather than a one-line note, and manufacturing `BOX_environment` on the strength of the
charter's four-sector phrase alone would be shaping a hole rather than finding one.
```

The argument it defers to, `STAGE8_AXN_BUILD_SKELETON_CODEX2_V001.md` —
**SEAL OK** — :66-69, quoted whole:

```text
The record does **not** supply a ninth independently named environment field.
The presently named environment/record participation is carried through `R`,
the causal cells, and the common-origin P5 package. Adding another field would
be authorship.
```

And the crosscheck that refuses to let that absence count as a result,
`STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md` — **SEAL OK** —
:247, quoted whole:

```text
absence of ninth environment field != environment-sector exhaustion theorem
```

The live obligation is a **disjunction**, not a term. Same file :209:
"environment closure is a disjunction, not a fabricated term". Its two
branches, quoted whole from
`STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md:63`:

```text
Either bind an environment action/carrier contribution already derivable from
`R`, causal-cell, and P5 stock into the complete action, **or** prove that
`BOX_record`/P5 **exhausts** the charter's environment sector. **No ninth
field invented; no `BOX_environment` formula authored.**
```

*** SO THE CORPUS'S SILENCE ABOUT THE FACTOR IS NOT AN OVERSIGHT. IT IS A
    DELIBERATE, ARGUED, SEALED REFUSAL. The corpus considered building the
    environment term, wrote down why building it would be authorship rather
    than derivation, declined, and then — in the crosscheck — refused to let
    the declining be scored as a proof that nothing is missing. That is the
    exact shape of the absence: not a gap nobody noticed, but a gap held
    open on purpose and marked "not closed". ***

### §3.6 THE RUNG LEDGER

```text
RUNG 5  CONSTRUCTION                    *** EMPTY.  Measured, §3.1. ***
RUNG 4  PARTIAL / SURROGATE             THREE, ALL DISCLAIMED IN THEIR OWN
                                        SEALED BYTES: C^2_edge/witness;
                                        H_E^(c) (a record copy, in a
                                        self-declared non-artifact);
                                        rho_5 (effective, forbidden to
                                        promote)
RUNG 3  SPECIFICATION                   THREE.  Occupied.  §4.
RUNG 2  WOULD-BUILD NOTE                MANY (§4.4)
RUNG 1  BARE NAMING                     THE BULK — ~210 of 282 Sense-A lines
ATTEMPT RECORDED                        ONE.  DECLINED ON THE RECORD, §3.5.
```

---

## §4 — DELIVERABLE 4: WHAT CONSTRUCTING IT WOULD REQUIRE

In the corpus's own terms, from its own statements only. The commission
allows for the finding that nothing is stated. **That is not the finding.**
The corpus states buildability conditions in three places, and they are exact.

### §4.1 THE SIX-ITEM EXACT REOPEN CONDITION — THE PRIMARY OBLIGATION SET

`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md` — **SEAL OK** —
:141-155, heading and body, quoted whole and complete:

```text
## Exact reopen condition

The next constructive gate must derive, without alpha or mass input:

1. the physical edge/environment degrees required by durable closure;
2. their Lorentz, vector-`U(1)`, CPT, and axial/inflow transformations;
3. the full allowed operator space and its invariant positive forms;
4. a unique coupling ray up to an overall scale;
5. the closure-sector spectrum and causal-cell interval;
6. the exact record channel and chiral-odd Schur self-energy from the same
   unretuned matrix elements.

If the constrained coefficient space has dimension greater than one, or if an
independent rescaling leaves either output deformable, the route remains
blocked.
```

*** ITEM 1 IS THE FACTOR ITSELF. The corpus does state, in sealed bytes, what
    building the environment would require: the degrees, their transformation
    law under four named symmetry classes, an operator space with invariant
    positive forms, a unique ray, a spectrum and interval, and a specified
    record channel — all without alpha or mass input, and with a stated
    failure test (coefficient-space dimension > 1, or deformability under
    rescaling). This is a checkable predicate, not an aspiration. ***

The same file's `## Result`, :127-135, states which of these are outstanding,
quoted whole:

```text
## Result

The current sealed constraints do not yet derive:

1. the complete edge/environment carrier;
2. the axial Ward/inflow realization on that carrier;
3. the physical grading weights;
4. the closure-sector spectrum;
5. one invariant coupling normalization.
```

This reopen condition is **live and cited forward**. Two independent later
sealed artifacts route their own obligations back to it:
`STAGE8_TASK2F_..._DETERMINATION_V001.md:392-393` ("The complete
edge/environment carrier is one of the governing gate's explicit reopen
requirements") and `STAGE8_SEAM10_NARROWING_ADVERSARIAL_RECOMPUTATION_V001.md`
— **SEAL OK** — :227-230, quoted whole to the end of its sentence:

```text
The six-item reopen condition requires the physical environment degrees; their Lorentz, vector-`U(1)`,
CPT, and axial/inflow transformations; the full operator space and invariant positive forms; a unique ray;
the closure spectrum and cell interval; and the record channel plus chiral-odd self-energy from the same
unretuned matrix elements (`FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md:141-155`).
```

### §4.2 THE MISSING OBJECT'S EXACT SIGNATURE

`STAGE8_TASK2F_PRESENTATION_TO_FINITE_AND_SRE_BRIDGE_BUILD_DETERMINATION_V001
.md` — **SEAL OK** — :398-415, quoted whole:

```text
The exact missing object is:

SRE_PHYSICAL_REALIZATION_EMBEDDING

domain:
  End(C^2_S tensor C^2_R tensor C^2_E), with its declared Pauli grading

codomain:
  L_B(E_C0), or a later lawful scalar physical descendant

required components:
  factor realizations i_S, i_R, i_E;
  a star/operator-system embedding iota_SRE_C0;
  grading, adjoint, and four-plane preservation;
  proof that E retains the edge/witness role;
  compatibility with the finite-authority restriction layer
```

The same file, :417-426, enumerates four alternatives and closes with **"No
alternative is selected."** The one that would supply the factor outright is
listed as, quoted whole:

```text
| B3: new environment factor | enlarge C0 by an explicit `C^2_E` factor | changes the ratified narrow carrier and requires a new adoption |
```

And its typed status, :429-437, quoted whole:

```text
SRE_PHYSICAL_REALIZATION_EMBEDDING_ADOPTED = false | TYPE-C |
  constraint: only the principal may add a physical sector realization not
              licensed by DoR-008 |
  release: principal ratification of one instantiated realization with all
           factor and plane-preservation certificates

SRE_PHYSICAL_REALIZATION_EMBEDDING_DERIVED = false | TYPE-U |
  would-build: derive the same factor maps and operator embedding from the
               current reduced and C0 authorities without adding a choice
```

*** THE CERTIFICATES ARE NAMED. A construction of the factor would have to
    carry: factor realizations, a star/operator-system embedding, grading and
    adjoint and four-plane preservation, a proof that E retains its role, and
    compatibility with the finite-authority restriction layer — and, because
    the flag is TYPE-C, PRINCIPAL RATIFICATION. The corpus has typed the act
    of supplying this factor as one only the principal may authorize. ***

### §4.3 THE CLOSURE DISJUNCTION

Quoted in full at §3.5: bind an environment contribution already derivable
from `R`, causal-cell and P5 stock into the complete action, **or** prove that
`BOX_record`/P5 exhausts the charter's environment sector. The second branch
is the discharge route that requires building nothing — and it, too, is
unbuilt: "absence of ninth environment field != environment-sector exhaustion
theorem".

### §4.4 THE WOULD-BUILD NOTES (RUNG 2), COMPLETE

Every would-build note in either root that names the factor. All seals OK.

```text
STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md :234-238
  physical_shortest_onset_selection_theorem_derived = false | TYPE-U |
    would-build: theorem that the complete physical source-record-environment
                 dynamics, at the first admissible durable record onset on the
                 unique physical record cell, must realize the shortest relative
                 projective path and therefore attain the Fubini-Study /

STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md :291-293
  complete_physical_write_operator_derived = false | TYPE-U |
    would-build: complete charge/CPT/source-record-environment operator with
                 Ward-compatible domains, durability, and phase-reference test

STAGE8_GAMMA_RECORD_MATTER_GRAVITY_BARREDNESS_KT4_DETERMINATION_V001.md :382-383
  durable_record_from_displayed_trial_potential = not established for 2/2 | TYPE-U |
    would-build: complete record/environment dynamics and durability tests
```

### §4.5 THE DELIVERABLE-4 FINDING

*** THE COMMISSION'S ANTICIPATED FINDING — "a factor named everywhere with no
    stated buildability conditions" — IS REFUTED AT BYTES. The conditions ARE
    stated, in sealed text, as a six-item checkable predicate with a stated
    failure test, plus an exact domain/codomain signature with five named
    certificates, plus a two-branch closure disjunction. The factor is not
    named into a void. It is named into a WELL-SPECIFIED HOLE. ***

*** AND THE HOLE HAS NEVER BEEN ENTERED. Every one of the three
    specifications is attached to a flag reading `false`, and the only
    recorded approach to it was a deliberate refusal (§3.5). ***

---

## §5 — DELIVERABLE 5: THE ROUTING TEST

**The question, exactly.** For each of the three failure points and each of
the three demands: is the quantity it demands a RELATION TO THE ENVIRONMENT
SIDE, or is it INTERNAL TO THE RECORD? This decides whether the factor's
absence bears on the standing obstructions or is beside them.

### §5.0 THE MEASUREMENT THAT FRAMES THE TEST

Before typing any single item, the token count across the four commissioned
obstruction artifacts, `/usr/bin/grep -ic "environment"`:

```text
STAGE8_COMPLETION_MAP_T17SR_V001.md              0
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md        0
STAGE8_STRATIFICATION_O27SR_V001.md              0
STAGE8_STRATIFICATION_O27SR_AUDIT_V001.md        1  <- SENSE-B, not physics:
    ":765  ENVIRONMENT   fresh python venv created this session; sympy 1.14.0."
```

*** THE WORD DOES NOT OCCUR, IN THE PHYSICAL SENSE, ANYWHERE IN THE ARTIFACTS
    THAT STATE THE THREE FAILURE POINTS, THE THREE DEMANDS, THE LEVEL
    ASSIGNMENTS, OR THE ABSENT BRIDGES. Not once, across four artifacts. ***

I extended the check to the whole wall family: of the 12 artifacts in either
root containing `FAILS-AT`, exactly ONE contains any Sense-A environment
token (`STAGE8_EARN_THE_TRACE_S9AD_V001.md`, 5 hits) — and those 5 sit on the
HOLONOMY/PHASE route, not on FP-1/2/3 or W-1/2/3. It is displayed at §5.4.
(`STAGE8_DEMAND_REGISTER_O25SR_V001.md` appeared in that file list and was
NOT opened — it is a register.)

### §5.1 THE THREE FAILURE POINTS, EACH ROUTED AT BYTES

**FP-1 — F1, THE CONVERSION STEP.** Operative sentence,
`STAGE8_COMPLETION_MAP_T17SR_V001.md` :81-85, quoted whole:

```text
"rank x op is the only sealed conversion from the delivered operator
grade to trace/HS grade; it carries the full carrier volume 4n^3 against an n-free target,
and the r-3 pair PROVED the same-power arithmetic cannot close (WB6 = D10 re-pinned; the
tasked display made: THE SAME CEILING BINDS). Escape requires a sub-volume trace/HS rate:
verbatim MO-3, open of record."
```

```text
DEMANDED QUANTITY: a rate converting operator grade to trace/HS grade for the
  dressed composite class — i.e. norms and traces (tr K_H, tr H_A,
  ||K_H||_2^2, ||H_A||_2^2) of operators on the record's own carrier.
ROUTING:  *** INTERNAL TO THE RECORD. *** The obstruction is an arithmetic
  ceiling between two grades of the SAME operator on the SAME carrier. No
  second system enters the statement. No environment relation is demanded.
```

**FP-2 — F2, THE LOCUS STEP.** Operative sentence, :96-98, quoted whole:

```text
"THE FATAL LOCAL INTEGRAL IS THE VOLUME DIAGONAL x = y, NOT THE SHARP BOUNDARY.
SMOOTHING ONLY THE BOUNDARY WILL NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT" — "which
boundary-supported structure provably does not reach."
```

```text
DEMANDED QUANTITY: control of a |x-y|^-3 majorant at the coincidence locus
  x = y inside the volume.
ROUTING:  *** INTERNAL TO THE RECORD — AND THE CORPUS RULES SO EXPRESSLY. ***
  This is the decisive item.  FP-2 is the failure point that SOUNDS like a
  boundary problem, and the adopted R5 locus refinement (E1 C6 :359-366)
  MOVED IT OFF THE BOUNDARY AND INTO THE VOLUME, in capitals, and further
  held that "boundary-supported structure provably does not reach" it.
  A boundary/environment relation is not merely absent from FP-2 — it is
  AFFIRMATIVELY EXCLUDED as insufficient by a sealed adopted ruling.
```

**FP-3 — F3, THE ERROR-TERM STEP.** Operative sentence, :102-106, quoted whole:

```text
"the closure's exact identities hold modulo the C-L1 [error]; its
only permitted certification (C-L2 quadratic form) is UNCERTIFIED of record with the
operator-norm route excluded (||[h_0, 1_B]|| = +infinity); the odd sector's would-be
single-composite majorant lives in exactly that layer. Witness
E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS."
```

```text
DEMANDED QUANTITY: a C-L2 quadratic-form certification of the error layer;
  the excluded route is the operator norm of [h_0, 1_B].
ROUTING:  *** INTERNAL TO THE RECORD — WITH ONE HONEST QUALIFICATION,
  DISPLAYED RATHER THAN SUPPRESSED. ***
  `1_B` is the record's OWN sharp projector — "P (the record projector): the
  SHARP causal-ball projector ... P = multiplication by 1_(|x| <= r(t))"
  (STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md :150-157, SEAL OK, verified
  by me at bytes).  So [h_0, 1_B] is a commutator of the free generator with
  the RECORD'S OWN CUT.  It is a quantity about the record's boundary.
  QUALIFICATION: a commutator with 1_B does straddle the ball and its
  complement, so one may say FP-3 touches "the outside".  But the outside
  here is ACCOUNT 6 of §2.5 — the account in which the word "environment"
  occurs ZERO TIMES in the entire artifact (measured).  FP-3 therefore routes
  through THE RECORD'S OWN BOUNDARY, and NOT through the named environment
  factor.  The distinction is the whole point of this deliverable and I keep
  it sharp: bearing on a boundary is not bearing on H_boundary/environment.
```

### §5.2 THE THREE DEMANDS, EACH ROUTED AT BYTES

All three quoted whole from `STAGE8_COMPLETION_MAP_T17SR_V001.md` :323-339.

**W-1 (discharges FP-1).**

```text
W-1 (discharges FP-1): the certified sub-volume trace/HS rate for the dressed
    composite class — "verbatim MO-3, open of record"; carrier named CN-1 (R5):
    "member-named topologies with continuity proofs for Loc_N^phys, iotatilde_N^H,
    iota_N^H, Loc_N^C — no member exists in the scored corpus to name them";
    "the F1 escape is a certified sub-volume rate, i.e. a TOPOLOGY in which the
    dressed composites are controlled uniformly."
```

```text
ROUTING: *** INTERNAL. *** What is demanded is A TOPOLOGY on the dressed
  composites, with continuity proofs for four named localization maps. Every
  named object is a map of the record's own presentation. No environment.
```

**W-2 (discharges FP-2).**

```text
W-2 (discharges FP-2): the member-supplied sealed prequotient rule — CN-2 (R1):
    "a member-supplied sealed prequotient rule (JD-3/JD-3a) with one displayed
    evaluation on a named oriented k-cell — absent from the scored corpus";
    "a sealed member evaluation on a named cell would replace the diagonal-majorant
    accounting (F2's blocked side) with the junction object's own symbol-side value."
```

```text
ROUTING: *** INTERNAL. *** A prequotient rule evaluated on an oriented k-cell
  of the record complex, replacing a diagonal-majorant accounting with a
  symbol-side value. The "junction object" is the record cell's own junction
  — ACCOUNT 5 of §2.5, where both sides are DESCRIPTIONS OF ONE CELL and, in
  that artifact's own words, there is no second system. No environment.
```

**W-3 (discharges FP-3).**

```text
W-3 (discharges FP-3): the C-L2 certification at its consuming type — G_cm derived
    at the consuming (HS) type; "its only permitted certification (C-L2 quadratic
    form)" with "the operator-norm route excluded (||[h_0, 1_B]|| = +infinity)";
    retirement of witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED is the
    registrar's upon its landing.
```

```text
ROUTING: *** INTERNAL, same qualification as FP-3. *** A quadratic-form
  certification of the record's own error layer at the HS type. No relation
  to a second system is demanded.
```

### §5.3 THE ROUTING TABLE

```text
ITEM   DEMANDED QUANTITY IS...                          ROUTES THROUGH THE
                                                        ENVIRONMENT FACTOR?
--------------------------------------------------------------------------
FP-1   a grade-conversion rate on the record carrier    NO — internal
FP-2   a |x-y|^-3 majorant at the VOLUME DIAGONAL       NO — internal, and
                                                        boundary structure
                                                        EXPRESSLY RULED
                                                        INSUFFICIENT
FP-3   a C-L2 form certification of the error layer     NO — the record's own
                                                        boundary, not the
                                                        named factor
W-1    a topology controlling dressed composites        NO — internal
W-2    a prequotient rule on an oriented k-cell         NO — internal
W-3    a C-L2 certification at the HS type              NO — internal
--------------------------------------------------------------------------
                                            6 of 6 INTERNAL.  0 of 6 ROUTE
                                            THROUGH THE ENVIRONMENT SIDE.
```

### §5.4 WHERE THE FACTOR *DOES* SIT IN OBSTRUCTION LANGUAGE — DISPLAYED, NOT HIDDEN

The factor is not absent from all obstruction talk. It is absent from THESE
six. `STAGE8_EARN_THE_TRACE_S9AD_V001.md` — **SEAL OK** — :455-458, quoted
whole to the end of its sentence:

```text
(3) the bridge/onset gates' common demanded object (the
  complete source-record-environment action) — its construction or refusal
  adjudicates the action-grade lifts. Neither horn is collapsed here; the
  fork stays open.
```

Same file :348-350, quoted whole: "gate's (the complete
source-record-environment action/dilation). STATUS: NOT-DERIVABLE-TODAY.
Blocker: the bridge gate's own named theorem; nothing new."

```text
This is the HOLONOMY / PHASE-SIDE route and the ACTION-GRADE LIFTS — a
different thread from the wall's F1/F2/F3. On THAT thread the compound
"source-record-environment action" is the named demanded object. So the
factor's absence is live somewhere. It is not live at the three failure
points or the three demands.
```

### §5.5 THE ROUTING FINDING

*** THE ENVIRONMENT FACTOR'S ABSENCE IS BESIDE THE STANDING OBSTRUCTIONS, NOT
    UPON THEM. All three failure points and all three demands are internal to
    the record: grade conversion on one carrier, a coincidence-locus majorant
    the corpus expressly moved OFF the boundary, and a quadratic-form
    certification of the record's own error layer. Supplying
    H_boundary/environment tomorrow — with dimension, algebra, state and
    dynamics — would not, on the corpus's own displayed statements, discharge
    FP-1, FP-2 or FP-3, nor supply W-1, W-2 or W-3. ***

*** THE CONVERSE IS ALSO TRUE AND IS THE HARDER HALF: discharging all six
    would not construct the factor, and would not touch the six-item reopen
    condition of §4.1. The wall and the missing third factor are two
    independent unfinished things. The corpus's own quantifier discipline
    already models this shape — "The summed decisions transport NOTHING down
    to the per-composite failure points" (completion map :145). The same
    non-transport holds here, and neither direction is bridged by any sealed
    sentence located in either root. ***

---

## §6 — CHOICE LEDGER

```text
D-1  FORCED.  Verified every consumed seal with `shasum -a 256 -c` from the
     artifact's own directory.  Probed BOTH sidecar conventions
     (`NAME.md.seal.sha256`, `NAME.seal.sha256`) and, where neither existed,
     probed for a BUNDLE seal.  This recovered two files the naive probe
     would have called unsealed (both R3_4 pullback RESULT and the
     REDUNDANT_RECORD RESULT are sealed inside bundles).

D-2  FORCED (by the prior audit's D-3, confirmed here).  Used `/usr/bin/grep`
     by absolute path as the sweep authority.  The interactive `grep` is a
     shell function wrapping ugrep with `--ignore-files`, which can silently
     drop corpus files from a sweep declared uncapped.  Declared at §0.4.

D-3  UNFORCED.  I split the token into four senses BEFORE counting, and
     report per-sense.  A single undifferentiated count would have been
     ~1800 lines of which the large majority are virtualenvs, `os.environ`
     and vendored SymPy LaTeX helpers.  Named because the split is mine and
     a different splitter would get different totals at the margins; no
     finding turns on a marginal line.

D-4  UNFORCED.  I raised the count of non-agreeing accounts from three to six
     rather than reporting the prior determination's three.  I did NOT drop
     the prior third account even though the word "environment" never occurs
     in its artifact; I retained it and marked it NOT A NAMING.  Displaying a
     disagreement is the commission; pruning it would have been reconciling.

D-5  UNFORCED, and the most consequential judgment here.  I placed
     MAJORANT_LEMMA0_PROOF_DRAFT_V001 at RUNG 4, not RUNG 5, even though its
     sidecar verifies OK, because its own line 3 reads "Status: DRAFT — NOT
     SEALED, NOT A RESULT ARTIFACT."  I let the artifact's self-declaration
     govern over the hash.  A reviewer could reasonably hold the opposite and
     score it a construction; if they did, the verdict would move toward
     MIXED.  I flag this as the single point on which this determination is
     most contestable, and I do not conceal that H_E^(c) is the strongest
     object in either root.  My reason for RUNG 4 does not rest on the seal
     question alone: the file's own gloss calls H_E^(c) a "(relayed-record)
     copy", so even read as authority it constructs the record again, not a
     third factor.

D-6  UNFORCED, DISCLOSED.  I quoted `rho_5(E) = exp(-E)` verbatim.  It is a
     formula.  I quote it because deliverable 3 requires displaying the
     strongest thing that exists, and concealing it would understate the
     corpus.  It is displayed as CORPUS CONTENT, is disclaimed in its own
     file as "an effective-model choice [that] must not be promoted to a
     microscopic law", and is not adopted, used, or treated as a program
     quantity here.

D-7  UNFORCED, DISCLOSED.  At FP-3/W-3 I displayed the qualification that
     [h_0, 1_B] straddles the ball and its complement, instead of returning a
     flat "6 of 6 internal, no qualifications".  The flat answer was
     available and would have read stronger.  The qualified answer is the
     true one and the distinction it preserves — the record's own boundary is
     not the named environment factor — is the deliverable.

D-8  FORCED.  Did not open the three register/plan files that the sweep
     surfaced (§0.7), and disclosed the one basename that matched the
     exclusion regex as a false positive so the exclusion is auditable.
```

---

## §7 — TOY_SEPARATION

```text
THE ACTUAL SURFACE — what this commission was about:
  The third factor of the corpus's earliest three-factor statement:
  H_boundary/environment.  Its type, what carries it, what acts on it, what
  it is distinguished from, whether it is built, and what building it would
  require.  NOTHING WAS SUPPLIED FOR IT HERE.  No dimension, no algebra, no
  state, no dynamics, no term, no candidate, no proposal.

TOYS, SURROGATES AND FOREIGN SENSES — named so none is mistaken for it:
  1. `C^2_edge/witness` — a TWO-DIMENSIONAL STAND-IN declared "only for this
     identifiability calculation", disclaimed by its own sealed file as
     leaving "a genuine environment spectrum ... outside it".  A toy in the
     strict sense.  NOT PROMOTED HERE.
  2. `H_E^(c)` — a per-cell factor that its own file glosses as a
     "(relayed-record) copy", inside a document that declares itself NOT A
     RESULT ARTIFACT.  NOT PROMOTED HERE.
  3. `rho_5(E) = exp(-E)` — an EFFECTIVE continuum density its own file
     forbids promoting to a microscopic law, and which a sealed principle
     calls "downstream".  NOT PROMOTED HERE.
  4. The nine-dimensional "record environment" of the QSPEC specs — the
     RECORD REGISTER in a partial-trace role (`two-cell record outcomes =
     9`), not the third factor.  NOT A DIMENSION FOR THE FACTOR.
  5. The "two conditional environment sectors" — the N COPIES of the record.
     NOT A SECOND SYSTEM.
  6. SENSE-B/C/D tokens — virtualenvs, `os.environ`, `environment_event_
     ledger`, LaTeX `align*`/`eqnarray` in vendored SymPy, and the
     fence-hazard sense.  INFRASTRUCTURE AND FOREIGN VOCABULARY.  NOT PHYSICS
     AND, IN THE SymPy CASE, NOT THIS CORPUS'S WRITING AT ALL.

THE RECURRING CONFUSION, NAMED ONCE:  in every case where the corpus exhibits
something it calls an environment, the thing exhibited is THE RECORD UNDER A
SECOND NAME (items 2, 4, 5).  A reader sweeping for "environment" and finding
dimensions, orthogonality proofs and spectral densities would conclude the
factor is built.  It is not.  The second factor is built; the third is named.
```

---

## §8 — IMPORT AUDIT

```text
CONSUMED (all seals verified from own directory, §0.2):
  the six commissioned ground artifacts; the earliest sealed source; and the
  sealed artifacts quoted in §§1-5, each named with its seal state at its
  point of use.

CONSUMED AND CARRIED AS UNSEALED, EXPLICITLY:
  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md — no sidecar of either
  convention, no bundle seal.  Carries the ONLY signature the factor has.
  Its unsealed status is stated at every use and is decisive: the corpus's
  single strongest naming of the factor as a term is not sealed.

NOT IMPORTED:
  no value; no number treated as a program quantity; no measured constant; no
  comparison to any measured constant; no alpha; no coupling; no dimension
  adopted for the factor; no algebra, state, dynamics, term or formula
  supplied, proposed or adopted for the factor; no reconciliation of the six
  accounts; no selection among the four TASK2F alternatives; no git.

  Integers appearing in this artifact (9, 16, 2, 64, 70, 5, 6, 4n^3, and the
  sweep counts) are either QUOTED CORPUS CONTENT displayed under its own
  seal, or COUNTS OF LINES AND FILES produced by the declared sweeps.  None
  is a program quantity, and none is computed by me from any program object.

FENCES HELD:
  alpha_computed = false;  proof_authorized = false;
  kappa_record_computed = false.
  DETERMINATION ONLY — located, typed and displayed.  Constructed nothing.
```

---

## §9 — FLAG BLOCK — STAGE8_ENVIRONMENT_FACTOR_O31SR_V001

```text
output_path_probed_absent_before_write            = true
every_consumed_seal_verified_own_directory        = true
both_sidecar_conventions_and_bundle_seals_probed  = true
sweep_engine_declared_and_non_default             = true  (/usr/bin/grep)
sweep_cutoff_declared                             = true  (2026-08-15 17:45:00)
every_sweep_pattern_declared_verbatim             = true
roots_swept                                       = 2 (6867 + 7805 = 14672)

three_factor_statement_located                    = true
factor_named_in_sealed_bytes                      = true
factor_role_types_distinguished                   = 6
sense_split_declared_before_counting              = true

factor_CONSTRUCTED                                = false
factor_construction_definitional_sweep_hits       = 0 (12 raw, all false pos.)
factor_dimension_stated_anywhere                  = false
factor_algebra_stated_anywhere                    = false
factor_state_stated_anywhere                      = false
factor_dynamics_stated_anywhere                   = false
factor_signature_stated_anywhere                  = true  (UNSEALED, one line)

partial_constructions_or_surrogates_located       = 3 (ALL DISCLAIMED IN
                                                       THEIR OWN BYTES)
buildability_conditions_STATED                    = true  *** RUNG 3 OCCUPIED
buildability_condition_sites                      = 3
construction_attempt_recorded                     = true
construction_attempt_outcome                      = DECLINED ON THE RECORD

accounts_of_what_the_factor_is                    = 6, NON-AGREEING
accounts_carried_by_a_full_sentence               = 2 (and they contradict)
accounts_that_never_use_the_word                  = 1
prior_finding_of_three_accounts                   = CONFIRMED AND EXTENDED
disagreement_reconciled_by_this_commission        = false

failure_points_routing_through_the_factor         = 0 of 3
demands_routing_through_the_factor                = 0 of 3
environment_token_in_completion_map               = 0
environment_token_in_stratification_build         = 0
environment_token_in_stratification_audit         = 1 (SENSE-B, venv)
factor_absence_bears_on_standing_obstructions     = false
factor_live_on_a_DIFFERENT_thread                 = true (holonomy/phase-side,
                                                    action-grade lifts)

factor_supplied_by_this_commission                = false
any_part_of_factor_supplied                       = false
value_or_program_quantity_emitted                 = false
alpha_computed = false;  proof_authorized = false
kappa_record_computed = false
```

---

## §10 — VERDICT

```text
*** SPECIFIED-NOT-CONSTRUCTED ***
```

The buildability conditions are STATED — in sealed bytes, three times, as a
six-item checkable predicate with a stated failure test (§4.1), an exact
domain/codomain signature with five named certificates (§4.2), and a
two-branch closure disjunction (§4.3). The construction is ABSENT — measured
at zero across 14672 files by five definitional patterns (§3.1), and conceded
by the corpus in its own words: "no environment field or environment-action
formula".

So the verdict is not NAMED-ONLY. The commission anticipated the strongest
form of the absence — a factor named everywhere with no stated buildability
conditions — and that anticipation is **refuted at bytes**. The absence is of
a different and more exacting kind:

*** THE FACTOR IS NAMED INTO A WELL-SPECIFIED HOLE THAT HAS NEVER BEEN
    ENTERED — AND THE NOT-ENTERING IS DELIBERATE, ARGUED AND SEALED. The
    corpus knows precisely what would have to be derived, has typed the act
    of supplying it as one only the principal may authorize, considered
    manufacturing the term, wrote down that doing so "would be shaping a hole
    rather than finding one" and that "Adding another field would be
    authorship", declined — and then refused to let the declining count as a
    proof that nothing is missing. ***

Three further findings stand with the verdict:

```text
(1) THE FACTOR IS NAMED ONCE AND ABANDONED IN ITS OWN FOUNDING DOCUMENT.
    `boundary/environment` occurs at :30 and :34 of the earliest sealed
    source and nowhere in its remaining 147 lines.

(2) WHENEVER THE CORPUS EXHIBITS AN "ENVIRONMENT", IT HAS EXHIBITED THE
    RECORD AGAIN.  Three times — H_E^(c) the "(relayed-record) copy", the
    "conditional environment sectors" that are the N record copies, and the
    nine-dimensional "record environment" that is the two-cell record
    register.  The second factor is built; the third is named.

(3) THE ABSENCE IS BESIDE THE WALL, NOT UPON IT.  0 of 3 failure points and
    0 of 3 demands route through the environment side; the word does not
    occur, in its physical sense, anywhere in the four artifacts that state
    them.  Supplying the factor would not discharge the wall, and
    discharging the wall would not construct the factor.  The factor's
    absence is live — but on the holonomy/phase-side thread, not this one.
```

Nothing was constructed, proposed, adopted or supplied. The six accounts are
displayed in disagreement and are not reconciled.

```text
END — STAGE8_ENVIRONMENT_FACTOR_O31SR_V001
```
