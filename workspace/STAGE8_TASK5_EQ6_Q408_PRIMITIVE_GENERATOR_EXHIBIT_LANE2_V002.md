# STAGE 8 TASK 5 / EQ6 — GEOMETRY-ANCHORED Q-408 GENERATOR EXHIBIT — LANE 2 V002

Date: 2026-08-04  
Lane: Codex Lane 2  
Task: 5 / EQ6 / `B_Q408_REFINEMENT`  
Custody: bounded builder repair after Q-428; hostile check required

## Lead result

```text
GENERATORS =
  REPAIRED_AND_EXHIBITED_ON_THE_RATIFIED_EDGE_RESOLVED_SURFACE_CATEGORY;
  STOPPED_AT(
    NONEMPTY_ACTUAL_INTERIOR_CELL_REFINEMENT_MEMBER
    + ITS_COHERENT_STAGEWISE_SURFACE_LIFTS
  )

GEOMETRY_ANCHORS = CITED_PER_GENERATOR

RAIL_ARTIFACT_ROOTS = none
RAIL_ARTIFACT_CLAUSES_REMOVED =
  B_Q408_MONOLITHIC_ALL_ARROW_RESTRICTION
  + FORMAL_OVERLAY_CALLED_PHYSICAL_COMMON_REFINEMENT
  + REPRESENTATIVE_ORBIT_CALLED_A_NATURAL_FAMILY

CYCLE_CREATING_KERNEL_SQUARE = PROVEN_ON_OLD_SURFACE_TEST_IMAGE
NEW_CYCLE_KERNEL_SUPPORT = PRESERVED_AND_EXHIBITED
COVARIANCE_FAMILY = REPAIRED_AS_THE_RATIFIED_SURFACE_FAMILY
ALL_STAGE_SKELETON = REPLACED_BY_A_STAGEWISE_ACTUAL_SURFACE_DIAGRAM

B_Q408_REFINEMENT_FULL_ROOT = OPEN / TYPE-U
JOINT_EQ6 = OPEN
EQ6_MEMBER_BOUND = false
```

V001's strongest claim is withdrawn.  There is no literal identity test-form
map across a cycle-creating stage, no arbitrary-profile equality on all fine
test forms, no single covariance orbit standing in for physical moduli, and
no embedded “complete skeleton.”

The repaired object starts from the ratified surface itself: its signed
incidence map, Gate-4 cycle quotient, conserved current family, actual finite
Q-408 kernels, DoR-019 metric, and declared signed refinement maps.  Each
stage has its own physical test-form quotient.  For a cycle-creating arrow,
the old surface-visible test classes embed canonically into the fine classes;
the fine kernel pulls back along that embedding.  A new-cycle test class is
outside the old image and retains its nonzero physical kernel.  This is the
geometry the killed square erased.

The repair is complete on every ratified signed edge-resolved **refinement
arrow whose signed cycle map preserves the old record-visible cycle family**,
including cycle creation and rank-preserving `Ref_path`.  A general cellular
map that collapses an old physical cycle still has V003's contravariant
restriction, but it is not a primitive refinement generator and does not
receive the injection constructed below.  The build stops where the ratified
surface stops: a formal interior PL cell with no signed map into an actual
finite surface has no physical path, coframe/density, connection, curvature,
or kernel datum.  Its moduli groupoid is typed below but is not passed off as
an inhabitant.

The Q-427 rail audit finds no entire new-physics root removable.  It removes
three rail clauses and narrows their domains.  The physical residues remain:
actual local-cell geometry for Q-408, a representative-sensitive completed
action family for R1, and physical response/boundary data for C2.

---

## 0. Preflight, authorities, and register sweep

### 0.1 Three-line preflight

```text
DOES THE OBJECT EXIST?  The repaired surface generator and square are built
                        below; the full formal-interior-cell member remains open.
IS THE VERSION CURRENT? YES, through register head Q-428.
ARE ITS INPUTS PRESENT? YES: the hostile check, ratified surface, Q-408,
                        DoR-019, signed descent, Ref_path, and DoR-020.
```

Verified authorities:

| Authority | SHA-256 | Use |
|---|---|---|
| locked process with Q-427 rule | `d537e294c03b3fc50fa49844f5b166bdcee1d64fe1513b9201047f602cd1518f` | surface/rails split and custody |
| V001 hostile check | `505bd329b29a45bf57feec84b89c2fa53481a8117808df81872e15194b16d825` | kill anatomy and permanent witness |
| killed V001 | `cd03cc87554b5ec0372e7b3b62662fac12e523f0ce9f63b08200aea9c2bd3e8c` | bounded delta source |
| field signature V005 / DoR-015 object | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | signed incidence, quotient, paths, cycle currents |
| DoR-015 decision | `617bd51a70860d2cfb2066640630ba37ec289d56255ebbe1433bc0dfcba0159d` | ratified surface provenance |
| Q-408 finite physical kernel | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual surface kernels and Ward calculus |
| rank-preserving `Ref_path` square | V004 package section M1, `fa73114214d7653d9a5d181a684b3bd65f0d6e9f91fd2256fb159ca97e56c4bf` | exact existing-path boundary |
| family-natural cycle descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | signed cellular restriction and Z7 non-certificate |
| DoR-019 metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | finite Riesz maps and surface-visible metrics |
| continuum package V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | P4 interface and J families |
| Q-424 frontier | `b8294e917531dd4f1731aedd54fd1c52a320840335061bbb4ab872830d001028` | three-root classification under re-examination |
| DoR-020 | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | conditional package and prohibitions |

The register SHA-256 at preflight was
`d7c0a37862ea2daa849cbde2eea7ae9494e5de332a4a50719f4ee85dea740934`,
with Q-428 as its last entry.

### 0.2 Register sweep

```text
Q-297  physical carrier is the Gate-4/path-visibility quotient;
Q-348  fixed-stage prefix family contains the cellwise characters;
Q-355  cycle-creating upward extension is impossible; contravariant
       restriction remains lawful;
Q-396  completed action representatives with identical finite shadows can
       be physically distinguished;
Q-408  every finite surface has its actual Q-408 current/kernel calculus;
Q-410  formal geometric refinement/density/common-response data are absent;
Q-418  rank-preserving actual-path subdivision square is derived;
Q-421  six roots were minimal under the then-current rail formulation;
Q-422  DoR-020 conditionally adopts the joint equalizer;
Q-423  finite and Ref_path positive subpackages are built;
Q-424  three roots typed genuinely new physics, subject now to Q-427;
Q-425  C1 stops at the full P4 core plus C1-local data;
Q-426  V001 lands and is sent to hostile check;
Q-427  surface geometry must be separated from rails;
Q-428  V001 is killed by the cycle square, orbit, and skeleton defects.
```

### 0.3 Bearing symbol distinctions

```text
G                  = one actual ratified finite record surface;
B_G                = its signed incidence/boundary map;
K_G=ker(B_G^T)     = its complete conserved cycle-current carrier;
Q_G                = its Gate-4 physical cycle quotient;

T_G                = actual Q-408 connection-test space at G;
Tbar_G=T_G/ker A_G = surface-visible test-form quotient;
Abar_G              = induced isomorphism Tbar_G -> K_G^*;
R_G                 = ratified positive Riesz map on K_G;
Kern_G              = actual Q-408 physical bilocal kernel;

Ref_path            = rank-preserving subdivision of already realized paths;
Ext_cycle           = actual signed surface arrow that creates cycle rank;
Ref_formal          = PL rail arrow with no actual surface anchor;

I_R^T               = old surface-test injection into a fine stage;
(I_R^T)^*           = lawful bilocal pullback/restriction;
rho_R^H             = profile compression along the old current image;

new-cycle test form != old test form;
contravariant restriction != upward quotient extension;
full moduli groupoid != one covariance orbit;
stagewise diagram != one embedded all-stage skeleton;
rail certificate != surface-geometry theorem.
```

---

## 1. V1 — repair against the hostile anatomy

### 1.1 Actual finite surface objects, not a master skeleton

For every ratified finite stage, define the surface object

```text
S_G=(G,B_G,K_G,Q_G,Tbar_G,Abar_G,R_G,Kern_G).      (V1-1)
```

Every entry is anchored before it is used:

| Entry | Ratified surface anchor |
|---|---|
| `G,B_G` | DoR-015/V005 signed incidence and locality families |
| `Q_G` | V005 Gate-4 quotient `U(1)^E/Gamma_G` |
| `K_G` | V005 complete current family `ker(B_G^T)` |
| `u_c` | V005 actual path current `sum c_e integral_(gamma_e)` |
| `T_G,A_G` | Q-408 actual physical connection tests and line-integral analysis |
| `Tbar_G,Abar_G` | Q-408 quotient by physically invisible test forms; surjectivity makes `Abar_G` an isomorphism |
| `R_G` | DoR-019 finite carrier metric/Riesz map |
| `Kern_G` | Q-408 physical bilocal distribution kernel |

There is no map from an infinite “complete one-skeleton” into spacetime.
Each `S_G` has its own actual paths.  A surface arrow carries a declared
signed chain map and compares two finite objects.  Coarse and refined edges
may overlap across different stages; edge-interior separation is required
only inside one finite surface, exactly where Q-408 uses local test forms.

```text
ALL_STAGE_MASTER_EMBEDDING = withdrawn
STAGEWISE_ACTUAL_SURFACE_DIAGRAM = constructed on ratified arrows
U6_COMPLETE_SKELETON_REGRESSION = PASS
```

### 1.2 The repaired covariance family

V001's `Q_o=[q]_Cov` is deleted.  The repaired domain is the already-ratified
no-selection surface family

```text
Surf_adm={S_G over every admitted DoR-015 realization member}, (V1-2)
```

with the morphisms the ratified simultaneous relabelings, frame-torsor maps,
CTP reality maps, and signed cellular maps.  The generator below is a natural
formula on `Surf_adm`; it does not choose an orbit or claim inequivalent Gram
fields are one orbit.

For a genuinely new formal interior-cell realization, define only its
candidate carrier

```text
Mod_R^phys={actual surface tuples satisfying the P4 anchors and squares},
                                                               (V1-3)
```

as a full moduli groupoid.  No quotient collapses conformal/density moduli,
and no representative is selected.  Nonemptiness of `(V1-3)` is the exact
full-root stopping point, not a premise recycled as a proof.

```text
ONE_COVARIANCE_ORBIT_CLAIM = withdrawn
RATIFIED_SURFACE_FAMILY_NATURALITY = proven below
FULL_INTERIOR_CELL_MODULI_NONEMPTY = not_proved / TYPE-U
U1_COVARIANCE_REGRESSION = PASS
```

### 1.3 Surface arrows: two physically different classes

An **actual signed surface arrow** `R:G->G'` carries

```text
s_R:C_1(G)->C_1(G'),
partial_(G') s_R=s_R^0 partial_G.                 (V1-4)
```

Its cycle map is

```text
S_R=s_R|_(K_G):K_G->K_(G').                      (V1-5)
```

For the primitive-refinement exhibit, admissibility additionally requires

```text
ker(S_R)={0}.                                     (V1-5a)
```

This is not smuggled into the general cellular category.  It says precisely
that refinement may add target cycles but may not erase a record-visible old
cycle.  A declared signed map failing `(V1-5a)` retains V003's lawful
contravariant restriction and is excluded from this exhibit.  The exclusion
is failure-capable: any purported primitive refinement that collapses an old
V005-separated cycle fails membership.

Two cases are retained and never conflated.

1. `R in Ref_path`: `S_R` is a Riesz isomorphism and the physical paths are
   parameter restrictions of already realized paths.  Q-418 supplies the
   exact identity test-form square.
2. `R in Ext_cycle`: `K_(G')` has new directions.  The target surface and its
   actual Q-408 kernel are ratified stage data.  Only the old current image
   is comparable to `G`; no upward quotient map or identity test-form map is
   claimed.

This is the first rail repair: cycle creation is a surface extension with a
contravariant old-surface restriction, not a rank-preserving refinement
isomorphism.

### 1.4 The lawful test-class injection

Q-408's kernel depends on a test form only through its surface-visible class.
Write

```text
Abar_G:Tbar_G -> K_G^*,
Abar_(G'):Tbar_(G') -> K_(G')^*.                  (V1-6)
```

Both are isomorphisms.  Define the old-surface injection into the fine test
classes by the actual surface data

```text
I_R^T
 :=Abar_(G')^(-1) R_(G') S_R R_G^(-1) Abar_G.
                                                               (V1-7)
```

Every factor in `(V1-7)` is anchored: `S_R` is the signed incidence map,
`Abar` is the actual path-current analysis, and `R` is the ratified surface
metric.  No complement, test-form representative, or frame is selected.
Since `S_R` is injective on the old physical current class,
`I_R^T` is injective.

For `Ref_path`, Riesz naturality and the Q-418 analysis square reduce
`I_R^T` to the class of `P_R=id`.  For `Ext_cycle`, `(V1-7)` lands exactly in
the old normalized current image and excludes pure new-cycle test classes.

### 1.5 The repaired arbitrary-profile kernel square

For any fine-stage bilinear profile `H'`, define its coarse compression

```text
(rho_R^H H')(x,y)=H'(S_R x,S_R y).               (V1-8)
```

Then, for all actual coarse surface-visible tests `a,b in Tbar_G`,

```text
Kern_G(rho_R^H H')[a,b]
 =H'(S_R R_G^(-1)Abar_G a,
     S_R R_G^(-1)Abar_G b)

 =Kern_(G')(H')[I_R^T a,I_R^T b].                (V1-9)
```

The second equality is direct substitution of `(V1-7)` into Q-408's kernel
formula.  Thus the physical restriction is

```text
(I_R^T)^* Kern_(G')(H')=Kern_G(rho_R^H H'),      (V1-10)
```

not literal evaluation of the fine kernel on every fine test form.

### 1.6 First permanent regression — the killed new-cycle case

Re-run the hostile witness as actual surface-visible test classes:

```text
K_G=R,
K_(G')=R^2,
S_R x=(x,0),
Tbar_G=R,
Tbar_(G')=R^2,
Abar_G=id,
Abar_(G')=id,
R_G=id,
R_(G')=id,
I_R^T(t)=(t,0),
H_new=diag(0,1).                                  (V1-11)
```

Compression still vanishes:

```text
rho_R^H(H_new)=0.                                 (V1-12)
```

On every old test `t`, the real fine kernel restricts correctly:

```text
Kern_(G')(H_new)[I_R^T t,I_R^T t]=0
 =Kern_G(0)[t,t].                                 (V1-13)
```

But the new-cycle test class `eta_new=(0,1)` is physical at `G'` and is not
in `image(I_R^T)`.  Its fine kernel is nonzero:

```text
Kern_(G')(H_new)[eta_new,eta_new]=1 !=0.          (V1-14)
```

Equation `(V1-14)` is not discarded as “outside restriction”; it is the
target surface's own record-visible new-cycle response.  Equations
`(V1-13)` and `(V1-14)` together are the repaired certificate: the old
surface pulls back correctly and the new physical geometry survives.

```text
CYCLE_CREATING_RESTRICTION_REGRESSION = PASS
PURE_NEW_PROFILE_DELETED = false
NEW_CYCLE_TEST_FORM_PHYSICAL_KERNEL = nonzero
V001_T1_13_LITERAL_IDENTITY_SCOPE = withdrawn
```

### 1.7 Geometry/covariance proof

Let admitted surface isomorphisms act on currents by `U_G,U_(G')` and on
test classes by `V_G,V_(G')`, with

```text
U_(G') S_R=S_(fR) U_G.                            (V1-15)
```

Write their ratified covariance squares explicitly as

```text
Abar_(fG) V_G=U_G^(-*) Abar_G,
R_(fG) U_G=U_G^(-*) R_G,                          (V1-15a)
```

and likewise at `G'`.  Applying `(V1-15)`--`(V1-15a)` to `(V1-7)` gives

```text
V_(G') I_R^T=I_(fR)^T V_G.                       (V1-16)
```

Indeed, after applying the injective `Abar_(fG')`, the left side is
`U_(G')^(-*) R_(G') S_R R_G^(-1)Abar_G`, while the right side becomes the
same expression after cancellation of `U_G^*U_G^(-*)`.  Thus `(V1-16)` is a
calculation, not an orbit assertion.

Profile compression and kernel pullback then commute with `(V1-16)`.
Orientation reversal supplies the signed action on `S_R`; CTP reality
conjugates `Abar,H,Kern`; density/Riesz maps are even.  The equations remain
covariant.  This is a natural formula on the full ratified surface family,
not a selected covariance orbit.

---

## 2. V2 — per-generator surface anchors

### 2.1 Generator ledger

| Generator | Definition | Actual surface anchor | Geometry result |
|---|---|---|---|
| identity | identity on every term of `S_G` | finite signed surface `G,B_G` | exact |
| admitted relabeling/frame/reality | ratified action on `S_G` | DoR-015 quotient/torsor/CTP families | covariant; no representative |
| finite restriction | contravariant pullback of edge/cycle data | Door F and DoR-008 finite restrictions | exact bottom square |
| `Ref_path` subdivision | parameter restriction of actual `gamma_e`; Q-418 `S_R` | V005 A2 paths + Q-418 M1 | rank-preserving square exact |
| zero-extension | V005 current inclusion and adjoint restriction | V005 S6 and W3 precision | exact on old finite core |
| `Ext_cycle` | signed chain map `(V1-4)` satisfying `(V1-5a)`, target's full `K_(G')` | V003 contravariant cellular restriction + target finite V005/Q-408 surface | repaired square `(V1-10)`; new block retained |
| quotient descent | `Q_(G')->Q_G` contravariantly | V003 equation `(2.14)` and Gate-4 quotient | representative-independent |
| actual common-refinement leg | a declared pair of signed chain maps to one ratified target surface | signed incidence maps and target Q-408 kernel | Section 4 diamond |
| formal interior PL edge/cell | **not defined as physical** without a signed map and actual target surface | no ratified anchor found | exact stop / TYPE-U |

The table enforces U7a at the point of definition.  A generator without the
third-column anchor is not admitted into the physical category.

### 2.2 Coframe, density, connection, and curvature scope

At each actual finite surface object, these fields are not invented anew:

```text
external realization + connection carrier + source rigging
  = the six ratified DoR-015 amplitude-side structures;

finite positive carrier metric/Riesz map
  = DoR-019;

connection test forms and curvature/Ward action
  = Q-408 on that actual realization.             (V2-1)
```

For a ratified surface arrow, their transport is the transport already
present in the source/target realization square.  For `Ref_path`, it is
restriction along the same actual path.  For an `Ext_cycle` arrow, the new
target geometry is target-stage data; only the old image is pulled back.

A formal PL edge does not acquire a coframe, density, connection, or
curvature because a rail names it.  The remaining physical choice carrier is

```text
Mod_R^phys={
  signed actual target surface,
  actual path/current realization,
  coframe/density and connection/curvature data,
  all surface squares and covariance certificates
}.                                                 (V2-2)
```

Its alternatives and void conditions are disclosed, but none is selected:

| Residual datum | Alternatives | Carrier-only condition | Void condition |
|---|---|---|---|
| actual interior-cell target surface | signed edge-resolved extension; topology-preserving local cell refinement; reject formal arrow | must extend the ratified boundary/incidence surface | no actual signed map or target surface |
| path realization | target's ratified A2 paths; an explicitly authored physical lift | must reproduce actual Q-408 line integrals | abstract edge/operator only |
| coframe/density | target's DoR-015 realization data; explicitly authored positive local geometry | must satisfy surface restriction and units | fitted/response-defined scale |
| connection/curvature | target connection carrier and its curvature; explicitly authored compatible extension | must satisfy gauge/reality/edge restriction | selected gauge potential or failed curvature naturality |
| common refinement | actual target surface with two signed legs | both legs must be incidence-compatible and Q-408 exact | formal overlay without physical target |

```text
FORMAL_PL_ARROW_PHYSICAL_BY_DEFINITION = false / TYPE-R
INTERIOR_CELL_PHYSICAL_MEMBER = not_exhibited / TYPE-U
GEOMETRY_ANCHORS = cited_per_generator
```

---

## 3. V3 — rail-artifact re-examination of the three roots

### 3.1 Audit criterion

A whole root moves off the declaration ledger only if every datum it asks for
is demanded solely by the chosen rail representation and no ratified or
downstream physical surface consumer distinguishes its presence.  A
categorical equation may be a rail encoding while the datum it encodes is
still physical.  In that case the rail clause is removed or retyped, but the
root remains.

### 3.2 `B_Q408_REFINEMENT`

**Surface-geometry demand.**  Q-408 kernels are line integrals on actual
paths, V005 physical currents are the complete `ker(B^T)` family, and the
local-symbol/continuum route needs actual local support, density, and
connection data.  A formal interior edge with no target surface cannot
define any of these.  Two choices of actual cell realization can have the
same rail incidence while differing in density, connection, or local kernel
support.  Therefore the missing actual local-cell realization is physical.

**Rail content found.**  V001 required one monolithic refinement functor with
`P_R=id`, an arbitrary-profile literal kernel restriction, a formal
barycentric overlay, and one covariance orbit.  The pure-new-profile witness
shows these are not surface physics.  They are removed.

**Rail fix.**  Replace the monolithic rail by the two-sorted actual surface
category:

```text
Ref_path  = rank-preserving physical refinements;
Ext_cycle = cycle-creating surface extensions with contravariant pullback;
Ref_formal is not physical until equipped with an actual target surface.
                                                        (V3-1)
```

**Root verdict.**  The root remains because `(V2-2)` is physical and is not
ratified.  Its false rail clauses do not cost declaration.

```text
B_Q408_REFINEMENT = GENUINELY_NEW_PHYSICS / NARROWED
RAIL_ARTIFACT_ROOT = false
RAIL_ARTIFACT_CLAUSES = V001_all_arrow_identity_restriction + formal_overlay
```

### 3.3 `B_R1_NATURAL`

**Surface-geometry demand.**  Q-396 exhibits two completed action/response
representatives with identical sealed finite shadows and different physical
operator blocks.  The representative is therefore physical input, not a
gauge presentation.  A rule relating that input across actual surface
restrictions cannot be deleted without leaving the physical action
stage-dependent.

**Rail content found.**  Requiring a strict set-valued section on every
formal `Ref_PL` arrow is stronger than physics.  The correct domain is the
actual surface category, and covariance may be carried as a groupoid-valued
family until a physical member rule is ratified.  A formal arrow has no R1
naturality equation before it has a surface anchor.

**Rail fix.**  Retype the section clause as a descent/coherence object on
`Surf_adm`; demand a representative only where the representative-sensitive
consumer is actually evaluated.  This removes the formal-arrow burden but
does not choose the physical completion.

**Root verdict.**  The Q-396 witness prevents deletion of the root.

```text
B_R1_NATURAL = GENUINELY_NEW_PHYSICS / DOMAIN_NARROWED_TO_ACTUAL_SURFACE
RAIL_ARTIFACT_ROOT = false
RAIL_ARTIFACT_CLAUSE = strict_section_over_unanchored_formal_arrows
```

### 3.4 `B_C2_RESPONSE_BOUNDARY`

**Surface-geometry demand.**  The finite Ward identity fixes exact-current
annihilation but does not determine how a completed response splits into
bulk, contact, and physical boundary terms.  Actual local support, oriented
incidence boundary, density, and the subextensive boundary theorem affect a
physical response.  Q-410 proves these are absent rather than conventional.

**Rail content found.**  A cocycle on every formal free-category word is a
rail overreach.  Boundary coherence is required only on actual same-region
surface diamonds.  A cycle-creating target carries its own new boundary
sector; it is not forced to be the image of an old response.

**Rail fix.**  Define `beta_R` only after an actual surface response map
exists; impose its cocycle on actual composable arrows and its Stokes/incidence
anchor on every term.  No beta is required for an unphysical formal arrow.

**Root verdict.**  The physical boundary/contact datum remains open.

```text
B_C2_RESPONSE_BOUNDARY = GENUINELY_NEW_PHYSICS / DOMAIN_NARROWED
RAIL_ARTIFACT_ROOT = false
RAIL_ARTIFACT_CLAUSE = cocycle_on_unanchored_formal_words
```

### 3.5 Rail audit board

| Root | Physics anchor | Rail excess removed | Root removed? |
|---|---|---|---|
| `B_Q408_REFINEMENT` | actual path/current/local geometry and kernel support | all-arrow identity restriction; formal overlay; one-orbit fiction | no |
| `B_R1_NATURAL` | Q-396 representative-sensitive completed block | strict section on unanchored formal arrows | no |
| `B_C2_RESPONSE_BOUNDARY` | physical bulk/contact/boundary split and scaling | cocycle on formal words without a surface | no |

```text
RAIL_ARTIFACT_ROOTS = none
DECLARATION_LEDGER_ROOT_COUNT_CHANGE = 0
RAIL_LEVEL_FIX = replace_formal_all_arrow_rails_with_actual_surface_domains
```

The result is not “rails are harmless.”  Three rail clauses are refuted and
removed.  None exhausts the physical content of its root, so Q-427 does not
license deleting a root.

---

## 4. V4 — composition and common refinement on repaired generators

### 4.1 Composition

For actual signed surface arrows

```text
G --R--> G' --S--> G'',
S_(S o R)=S_S S_R,                                (V4-1)
```

profile compression is contravariant:

```text
rho_(S o R)^H=rho_R^H rho_S^H.                   (V4-2)
```

The test-class injections compose without an isometry assumption:

```text
Abar_(G'') I_S^T I_R^T
 =R_(G'') S_S R_(G')^(-1)
    R_(G') S_R R_G^(-1) Abar_G
 =R_(G'') S_S S_R R_G^(-1) Abar_G
 =Abar_(G'') I_(S o R)^T.                        (V4-3)
```

Since `Abar_(G'')` is injective,

```text
I_(S o R)^T=I_S^T I_R^T.                         (V4-4)
```

Applying `(V1-10)` twice gives the composite kernel pullback.  Identities are
immediate.  Reality, signed orientation, quotient descent, and batching
commute componentwise.  Thus composition is proved on the actual surface
category, including paths whose second step creates a cycle.

```text
COMPOSITION_ACTUAL_SURFACE = PROVEN
COMPOSITION_UNANCHORED_FORMAL_CATEGORY = not_claimed
```

### 4.2 Actual common-refinement diamonds

Let an actual surface diamond be supplied by ratified signed maps

```text
G_0 --R_1--> G_1 --T_1--> G_12,
G_0 --R_2--> G_2 --T_2--> G_12,

S_(T_1)S_(R_1)=S_(T_2)S_(R_2).                  (V4-5)
```

Each displayed leg is a primitive-refinement leg and therefore satisfies
`(V1-5a)`; a cycle-collapsing cellular map is outside this covariant
injection diamond and keeps only its separately ratified contravariant
restriction.

The common target `G_12` is an actual finite surface object with its own
Q-408 kernel.  Equation `(V4-3)` gives

```text
I_(T_1)^T I_(R_1)^T=I_(T_2)^T I_(R_2)^T.        (V4-6)
```

Profile compressions agree on the common coarse image, so pulling the target
kernel back along either route gives the same coarse kernel.  Pure cycle
directions created along either leg remain physical at `G_12`; `(V4-6)` does
not identify or erase them.

For rank-preserving `Ref_path`, `(V4-6)` reduces to Q-418's parameter
restriction diamond.  For a cycle-creating leg, it is the repaired
old-surface image square `(V1-10)`.

If two formal PL cellulations have no actual signed common target, no
physical diamond is claimed.  Constructing that target is exactly the
interior-cell member stop `(V2-2)`.

```text
COMMON_REFINEMENT_ACTUAL_SURFACE = PROVEN_WHEN_ACTUAL_DIAMOND_EXISTS
FORMAL_OVERLAY_IMPLIES_PHYSICAL_COMMON_REFINEMENT = false / TYPE-R
REFINEMENT_FULL_FORMAL_SCOPE = OPEN / TYPE-U
```

---

## 5. Falsifiers and permanent regressions

### 5.1 Anatomy regressions

| Regression | Re-execution | Result |
|---|---|---|
| pure new-cycle profile | `(V1-11)`--`(V1-14)` | **PASS; old pullback zero, new physical kernel nonzero** |
| covariance-orbit/moduli | one-orbit claim deleted; `Surf_adm` and `Mod_R^phys` distinguished | **PASS** |
| all-stage skeleton | stagewise objects/arrows replace embedded union | **PASS** |
| rank-preserving `Ref_path` | `I_R^T` reduces to Q-418 identity class | **PASS** |
| cycle-creating upward quotient | no upward quotient map claimed; V003 Z7 retained | **PASS** |
| arbitrary-profile restriction | exact only after pullback by `I_R^T` | **PASS** |

### 5.2 Five permanent regressions plus joint equalizer

| Regression | Result |
|---|---|
| abstract kernel | **PASS** — `Abar,Kern` are the actual Q-408 surface maps |
| circular `pi_Mx` | **PASS** — no reader/localization defines a generator |
| false nonemptiness | **PASS** — `Mod_R^phys` is TYPE-U, not called inhabited |
| Hodge from isometry | **PASS** — no local-symbol/Hodge result inferred |
| objectwise minimum/restriction | **PASS** — no minimum or representative chosen |
| joint equalizer | **PASS** — other roots and all joint J rows remain open |

### 5.3 Surface-geometry falsifiers

1. A nonzero target cycle must have a nonzero target physical test class.
   Q-408 separation and `(V1-14)` pass.
2. A tree/pendant coboundary must remain absent from the scalar cycle
   quotient.  V005's `ker(B^T)` anchor passes.
3. The one-edge-to-parallel-edge Z7 case must not acquire an upward quotient
   map.  None is defined.
4. S8-A's complete covariant cycle family must survive exchange.  `S_R`,
   `Abar`, and `I_R^T` transform as full families; no coordinate is selected.
5. A formal edge without an actual target surface must fail membership.
   `(V2-2)` enforces this.
6. A common refinement must have an actual target Q-408 kernel before the
   square is asserted.  `(V4-5)` enforces this.
7. A primitive-refinement leg must preserve every old V005-separated cycle.
   The injectivity gate `(V1-5a)` rejects a cycle-collapsing map before
   `(V1-7)` is formed.

### 5.4 Fresh attack — non-isometric cycle creation

The repaired square must not secretly assume
`R_G=S_R^*R_(G')S_R`.  Let the old-image Gram in `G'` differ from `R_G` by an
arbitrary positive operator.  Formula `(V1-7)` contains both metrics and
still gives

```text
R_(G')^(-1) Abar_(G') I_R^T
 =S_R R_G^(-1)Abar_G.                            (V5-1)
```

Therefore `(V1-9)` remains exact.  The repair survives without a
cycle-creating W3 isometry.  This is precisely the case V001's metric-left-
inverse presentation obscured.

```text
FRESH_ATTACK = NONISOMETRIC_OLD_IMAGE
RESULT = PASS
```

---

## 6. Anti-tuning, delta, and exact stop

### 6.1 Anti-tuning order

```text
1  Read Q-428's pure-new-profile witness before proposing a repair.
2  Freeze V005 incidence/cycle data, Q-408 kernels, and DoR-019 metrics.
3  Separate stagewise surfaces from the deleted all-stage skeleton.
4  Separate Ref_path from cycle-creating Ext_cycle.
5  Derive I_R^T from signed incidence, analysis, and Riesz maps.
6  Prove the kernel square and run the pure-new-profile case.
7  Prove covariance and composition.
8  Admit only actual signed common-refinement diamonds.
9  Run the three-root rail audit from physical consumers backward.
10 Inspect no response value, fixed point, end test, or measured datum.
```

### 6.2 Delta versus V001

| V001 claim | V002 disposition |
|---|---|
| one covariance orbit `Q_o` | deleted; ratified family plus honest residual moduli groupoid |
| one embedded complete skeleton | deleted; stagewise actual surface diagram |
| `P_R=id` for all refinements | retained only on rank-preserving `Ref_path` |
| arbitrary-profile literal restriction | replaced by pullback along `I_R^T` |
| pure new profile vanishes physically | refuted; `(V1-14)` preserves it |
| formal overlay is physical common refinement | refuted; actual target required |
| full generator family exhibited | withdrawn; actual signed scope built, interior-cell member open |
| free composition | re-proved on actual surface category |
| common refinement | re-proved on actual diamonds only |
| five regressions | retained and rerun |
| rail audit | new full section, per Q-427 |

### 6.3 Exact stopping point

Everything that follows from existing actual surface objects is built.  The
remaining `B_Q408_REFINEMENT` member is exactly:

```text
NONEMPTY Mod_R^phys for every genuinely new interior-cell primitive,
with:
  an actual signed target surface;
  actual edge/path/current realization;
  coframe/density and connection/curvature data;
  covariance over the retained physical family;
  actual common-refinement targets;
  DoR-008 and Q-408 squares.
                                                        (V6-1)
```

Local existence for one finite graph does not prove `(V6-1)` jointly.  No
member, scale, frame, chart, or topology is selected to close it.

```text
GENERATORS_RATIFIED_SURFACE_SCOPE = EXHIBITED
GENERATORS_FULL_FORMAL_INTERIOR_SCOPE = STOPPED_AT_(V6-1) / TYPE-U
COMPOSITION_REPAIRED_SCOPE = PROVEN
REFINEMENT_REPAIRED_SCOPE = PROVEN

RAIL_ARTIFACT_ROOTS = none
RAIL_ARTIFACT_CLAUSES_REMOVED = 3
JOINT_EQ6 = OPEN
READY_FOR_HOSTILE_CHECK = yes

TARGET_TUNING_USED = false
ABSTRACT_STANDIN_USED = false
CIRCULAR_CERTIFICATE_USED = false
MACHINERY_APPEAL = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
measured_constant_compared = false
```

Seal this artifact, mirror artifact and sidecar byte-identically to
`alpha-program-archive/workspace/`, report hashes, and stop.  No register,
plan, tracker, git, commit, or push action belongs to this lane.
