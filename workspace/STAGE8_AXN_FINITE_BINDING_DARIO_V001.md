# STAGE 8 — AXN BUILD — THE FINITE MEMBERSHIP BINDING
## DARIO LANE — RELAY 911 — `[PLAN:AXN-BUILD-C9]`

## 0. Preflight

Relay 911 verified before reading at
`5c52c29f8aa1530f23ede33f1f5385b76023cc10cd44c2876f94eb1aaf1b1a32`. Lane guard read DARIO; the
pickup ACK was written before content access. `PROGRAM_STATE_BRIEF_V005.md` verified and read before
task work at `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`. The requested
output name and its sidecar were clear at pickup.

---

## 1. Law-9b closure — declared first, exact members, full digests

This is the first substantive content in this artifact. Every determination below is taken **at a
named receiver inside this closure**. The generator-level statements in §3 are taken from the
carrier and algebra declarations quoted at their bytes, and the membership decisions in §4 are taken
against those declarations element by element.

```text
C_911 = {
 1  RELAY_PASTE_911_FINITE_BINDING_DARIO_V001.md
      5c52c29f8aa1530f23ede33f1f5385b76023cc10cd44c2876f94eb1aaf1b1a32
 2  supervision/PROGRAM_STATE_BRIEF_V005.md
      e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
 3  R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md
      40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9
 4  STAGE8_GAMMA_K_COMPLETED_ALGEBRA_EXTENSION_ATTEMPT_V001.md
      2198524f9f4b4f048953526e4084ae7282e85edce1767e410aef50b013ed0ab1
 5  STAGE8_AXN_AFFILIATION_CROSSCHECK_CODEX2_V001.md                    [886]
      f194da93cf7d445e3a5cda41fa86e7ab803afd4ff1042bd434d7e8e9ad8f6520
 6  STAGE8_AXN_BUILD_INFINITE_INCIDENCE_CROSSCHECK_CODEX2_V001.md       [877]
      d184ca491086519eec16856b45456805c4a5f3c23f53db765a0c27350753134c
 7  PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md                           [Level-1]
      532b0f0eac4ac749ba3e24954db356f7ca0f98c4f730030075c463efa3158efb
 8  R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md
      10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995
 9  STAGE8_AXN_AFFILIATION_ATTACK_DARIO_V001.md         [my 884 — pointer only, see §2]
      19aaf7fc37f7e8ad941406abdfc6c94b3380448d15d06076f57ac0b7e6f340d3
10  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md  [UNSEALED — reached via member 4, see §2.3]
      b5a60c354d05e84e91d4f1fbeb45c3bce83776c59df7fc307e65ef2a529d5287
}
```

**Exact span index** — each span recomputed here from the sealed bytes:

| key | sealed bytes | span SHA-256 | content |
|---|---|---|---|
| `CARRIER` | member 3 `[4287,4905)` | `a4953cb66daa6dacf588351447c24defea88068681cbd586e7dd8452a1207f6a` | `K_Sigma`, `R(K)`, `R_c`, the global CAR declaration, and the explicit cell matrices |
| `PARENT` | member 3 `[5711,6867)` | `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3` | `h_0[g,a]`, `h_K(t)`, the Galerkin remark, and the lift `H_K = dGamma_R(h_K)` |
| `DESCEND` | member 3 `[6867,7879)` | `827cf361f052d36c62e7fc6ea57e61c04cf8c18fc552dcde18bd7ee5e5ef8e3a` | `D_K`, `C_K(x)`, `D_K^2`, and the curvature-content sentence |
| `FIN-886` | member 5 `[6893,7174)` cited therein | `f9f9c68aacd978df…` (member 3's own bytes) | the `D_K` / `D_K^2` pin 886 used |

Members 3, 5, 6, 7 and 8 reproduce digests independently pinned in member 5's own closure table;
each was recomputed here rather than transcribed, and each matched.

**Name probe.** `STAGE8_AXN_FINITE_BINDING_DARIO_V001.md` and its sidecar: the workspace listing at
pickup returned a clear name for both. This artifact creates the first instance.

---

## 2. Gates, claim status, and three custody guards

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

Every headline determination here is **CLAIMED** pending opposite-lane cross-check. This report
installs no algebra, generator, representation, state, carrier map, truncation, second-quantization
map, domain, affiliation, or physical object, and moves no program flag. No member was bound; no
fixed point was executed; no end test was run; no smooth carrier was imported; no common cell was
formed; no junction map was evaluated; no physical quantity was numerically evaluated; no measured
constant was consulted. PE-1 through PE-13 remained pointer-only and were not opened. Builder-A code
paths (`evaluator_build_A/*.py`, `checks/`) were not accessed.

### 2.1 BR-1 guard against member 9 (my 884)

Member 5 is the opposite-lane cross-check that **corrected** my 884's finite conclusion. Its
correction is carried as governing and is applied throughout:

```text
boundedness       = established at finite realization scope;
B(H_K) membership = automatic;
A_K/R_N membership and iota image = not established by boundedness;
affiliation       = conditional on that missing membership binding.
```

Member 9 is cited **as a pointer only**, to name what item 14 inherits. Its index-set finding gets no
confirming weight, and my 901 disclosure that basis element B9 rests on it un-cross-checked stands
unchanged and unrelieved by anything in this report.

### 2.2 BR-1 guard against my own machine record

I hold prior machine results from relays 860/862/871 concerning a finite packet realization and its
grading. **None of that is used as evidence here.** Every matrix in §4 is quoted from member 3's
`CARRIER` span and re-verified in this relay from the quoted entries. Where a dimensional fact would
require my earlier replay to be load-bearing, §5.3 states it as a SPEC GAP instead of asserting it.

### 2.3 A custody disclosure about member 10

Member 10 is the file that defines the record carrier and the finite source-record algebra. It has
**no sidecar of its own**, and the law-8 probe was run in all four modes plus two more: own sidecar
in any directory; any sidecar naming the file; group sidecars under truncated `BID_GLOBAL_CAR*` and
`BID_GLOBAL*` spellings; any sidecar carrying its digest; and any `.md` carrying its digest as a
source pin. Only the last returned a hit —
`STAGE8_T7_PARENT_TO_PRIMITIVE_RESPONSE_LIFT_AUDIT_V001.md` carries
`composition: b5a60c354d05e84e…` in an authority-custody block — **and that artifact has no sidecar
either**, so the chain does not terminate in a seal.

Context, so this is not overstated: the workspace holds 1310 `.md` files and 1013 own sidecars, and
the `BID_*` legacy layer is systematically unsealed. Member 10's status is **ordinary for its class,
not anomalous**. Accordingly this report does **not** read member 10 as an authority. Every
definition attributed to it below is taken from **member 4's verbatim quotation**, at member 4's own
verified seal `2198524f…`, and member 10's digest is recorded above only so a registrar can bind it
later. Where member 3 states the same objects independently, member 3 is used in preference.

---

## 3. The declared algebra at source — generators, typing, membership demand

### 3.1 What each authority actually declares

| authority | what it declares about the finite algebra |
|---|---|
| member 7 (Level-1 PSC) | For every finite boundary complex `K`, one parent supplies *"the observable algebra `A_K`; the parent-selected state `omega_K`; the parent derivation `delta_K`; the public record subalgebra `R_K`; and the first-opening root data `q_K`."* **This declares `A_K` as a SLOT — a name and a role. It lists no generators and no relations**, and member 5 confirms at its bytes that PSC neither identifies `A_K` with `B(H_K)` nor states `D_K^2 in A_K`. |
| member 3, `CARRIER` | `K_Sigma = L2(Sigma, S tensor L^q)`; `R(K) = tensor_(c in K) R_c`; `R_c = span{|r_c>,|p_c>,|e_c>}`; *"There is one global source CAR algebra `CAR(K_Sigma)`. Record factors are even and distinguishable; no source carrier is copied per record."* |
| member 4, quoting member 10 | `R(C) = tensor_(c in C) B(R_c)`; `A_SR(C) = CAR(K_Sigma,q) graded-tensor R(C)`, which *"reduces canonically to the ordinary tensor product"* because record operators have trivial fermion grading, so *"every record operator commutes with the source algebra."* The CAR generators are `a(f)` with `{a(f),a(g)}=0`, `{a(f),a(g)^*}=<f,g>_Sigma I`. |
| member 8 | `R_N = tensor_(j=1)^N M_3(C)` with canonical embeddings `iota_NM(A) = A tensor I_(M-N)`; `R_infinity` is the inductive limit and its quasi-local C*-completion. |
| member 4, determination block | `field_CTP_quotient_as_complete_algebra_specified = false`; `extension_type_to_full_source_record_field_CTP = NOT_SPECIFIED`; `OBSTRUCTION = FULL_SOURCE_RECORD_FIELD_CTP_EXTENSION_NOT_TYPED`. |

### 3.2 The assembled declaration, and the membership demand it fixes

```text
DECLARED (finite):   A_SR(C) = CAR(K_Sigma,q)  tensor  R(C),
                     R(C) = tensor_(c in C) B(R_c),   dim R_c = 3.

GENERATORS:  source  -- a(f), a(f)^*, f in K_Sigma, with the CAR relations;
             record  -- all of B(R_c) per cell, i.e. a full M_3(C) each;
             field   -- NONE DECLARED.

MEMBERSHIP DEMAND:  X in A_SR(C)  iff  X decomposes as a (norm-limit of) sums of
                    products (source element) tensor (record element), with each
                    factor drawn from the two generator sets above.
```

**Two features of this declaration decide most of §4 before any operator is examined.**

**(i) The record sector's generator set is everything.** `B(R_c)` is the *full* operator algebra on a
3-dimensional space, not a proper subalgebra. So for any operator acting on record factors alone,
membership is decidable immediately and affirmatively — it is a question of writing coefficients, not
of proving a theorem. Member 8's `M_3(C)` agrees with member 3's `dim R_c = 3` exactly.

**(ii) The declaration has three sectors and generators for two.** The field/CTP sector is named as a
requirement by member 4's Section 1 and typed as *not specified*. **A membership question about an
operator with field content therefore has no target to be a member of.** This is not a missing proof;
it is a missing codomain.

---

## 4. The operators against the declaration — element by element

### 4.1 The record-sector operators: membership DERIVED

Member 3's `CARRIER` span states the cell operators as explicit matrices:

```text
Gamma_c = diag(+1,+1,-1);
b_c     = [[0,0,-1],[0,0,+1],[-1,+1,0]];
c_c     = i Gamma_c b_c;
```

with *"`c_c` is extended by the identity on all other record factors"* and *"No new cell coefficient
is admitted."* Re-verified in this relay from those quoted entries:

```text
b_c Hermitian                          : True
Gamma_c^2 = I                          : True
|| Gamma_c b_c + b_c Gamma_c ||        = 0.000e+00      (exact anticommutation)
c_c Hermitian                          : True
|| c_c^2 - b_c^2 ||                    = 0.000e+00
spec(c_c) = { -1.41421356, 0, +1.41421356 } ;   || c_c || = 1.414213562373
```

Expanding in the matrix-unit basis `{E_ij}` of `B(R_c)`:

```text
Gamma_c = +1 E_00  +1 E_11  -1 E_22
b_c     = -1 E_02  +1 E_12  -1 E_20  +1 E_21
c_c     = -i E_02  +i E_12  +i E_20  -i E_21
```

**Every coefficient is explicit and finite. `Gamma_c`, `b_c`, `c_c` are members of `B(R_c)`, and
`iota_c(c_c)` is a member of `R(C) = tensor_c B(R_c)`. BINDING DERIVED.** This is a genuine
membership derivation, not a boundedness argument, and it is exactly what member 5 said boundedness
could not supply. It is the first component of basis line 6 to close.

### 4.2 The one-particle operators: a level mismatch, named

Member 3's `PARENT` span gives the finite one-particle parent and its lift:

```text
h_K(t) = h_0[g,a] + sum_(c in K) v_c(t) M_c(t) tensor S_n tensor iota_c(c_c),
H_K(t) = dGamma_R(h_K(t)) ,       S_n = -i slash(n) gamma^5 ,
```

with `h_0[g,a]` *"the Dirac Hamiltonian obtained from the same spin-plus-U(1) connection"*, `M_c(t)`
multiplication by the cell's spatial section, and `H_K` *"the operator-valued number-preserving
quasifree lift."*

**Two levels are in play, and the declared algebra lives at only one of them.**

```text
LEVEL 1  operators on  K_Sigma tensor R(K)   -- h_0, M_c, S_n, iota_c(c_c), h_K, D_K
LEVEL 2  operators on  Fock(K_Sigma) tensor R(K)  -- H_K = dGamma_R(h_K)
DECLARED ALGEBRA A_SR(C) = CAR(K_Sigma,q) tensor R(C)   lives at LEVEL 2.
```

`CAR(K_Sigma)`'s generators `a(f)` act on Fock space; `h_K` and `D_K` act on the one-particle
carrier. **So "is `D_K^2` in `A_K`?" is, as literally posed, a question about operators on two
different Hilbert spaces.** The record supplies the bridge — `dGamma_R` — and supplies it explicitly
for `h_K(t)`. It does not state it for `D_K`. Member 5's requirement of *"a finite-carrier membership
map"* is therefore sharpened here to a named object:

```text
MISSING OBJECT (named): dGamma_R applied to D_K rather than to h_K(t),
  together with the statement that D_K's descendants are lifted by the same functor.
```

The record-sector factor `iota_c(c_c)` in the write term rides through unchanged, since `dGamma_R` is
fiber-preserving and record factors are not copied per source. **So the write term's shape —
`(source operator) tensor (record operator)` — matches the declared tensor decomposition exactly.**
That is a real positive structural finding: the write term is the *right shape* for `A_SR(C)`, and
only the lift's scope is at issue.

### 4.3 The C*-membership question has a structural answer at the declared carrier

`K_Sigma = L2(Sigma, S tensor L^q)` is infinite-dimensional. For any one-particle operator `T != 0`,
`dGamma(T)` restricted to the `n`-particle sector has norm growing like `n·||T||`:

```text
n =   1  ->  ~ 1·||T||
n =   2  ->  ~ 2·||T||
n =  20  ->  ~ 20·||T||
n = 100  ->  ~ 100·||T||
```

so `dGamma(T)` is **unbounded** on full Fock space and is therefore **not an element of the
C*-algebra `CAR(K_Sigma)`** — at best it is affiliated to the von Neumann closure `CAR(K_Sigma)''`.
This holds for every nonzero term of `h_K`, including the *bounded* write terms.

**Consequence, derived.** At the declared infinite-dimensional carrier, the demand "the finite
operators SIT IN the declared algebra" has a negative answer on the source side **for structural
reasons that have nothing to do with any unresolved physics**. The lawful restatement is affiliation
to `CAR(K_Sigma)'' tensor R(K)`.

**And the finite stage behaves oppositely.** Member 3 permits *"the Galerkin compression"* as a
finite incidence regulator. If that compression truncates to `dim K_n = n < infinity`, then
`CAR(K_n) ≅ M_(2^n)(C)` is the **full** matrix algebra, `dGamma` is bounded, and membership is
automatic on both factors:

```text
dim K_n = 1 -> CAR = M_2(C) ;  = 2 -> M_4(C) ;  = 3 -> M_8(C) ;  = 4 -> M_16(C)
```

**So the binding holds at every finite Galerkin stage and fails to survive the limit as membership.**
That is exactly the shape of item 14, now with the mechanism named: the failure is `dGamma`'s
unboundedness, not an absent proof.

### 4.4 `D_K` and `D_K^2`: the field sector, and the missing codomain

Member 3's `DESCEND` span:

```text
D_K   = i gamma^mu nabla_mu + i gamma^5 C_K(x),
C_K(x) = sum_c v_c(x) M_c(x) iota_c(c_c),
D_K^2 = (i gamma^mu nabla_mu)^2 - C_K^2 - gamma^mu gamma^5 nabla_mu C_K,
```

and, in the record's own words about the first term:

```text
"The first term contains the fixed spin-curvature and U(1)-curvature Clifford contractions."
```

`nabla_mu` is the covariant derivative of *"the same spin-plus-U(1) connection"* named in `PARENT`,
and `h_0[g,a]` carries the metric and connection in its argument list. FBRA declares that connection
as **adopted Level-1 field content**. Therefore:

```text
D_K   has a component in the FIELD sector;
D_K^2 has U(1)-curvature explicitly, by member 3's own sentence;
the FIELD sector has NO DECLARED ALGEBRA (member 4: NOT_SPECIFIED).
```

**`D_K^2`'s membership is therefore undecidable in the declared system — not because the proof is
hard, but because one of its three components has no codomain to be a member of.** The obstruction is
member 4's own:

```text
OBSTRUCTION = FULL_SOURCE_RECORD_FIELD_CTP_EXTENSION_NOT_TYPED
Required missing object: a complete microscopic operator/CTP construction supplying
  the source-record-field Hilbert space, rho_pre, the gauge-fixed physical quotient and
  invariant contour/spacetime measure, admitted record effects and domains, dynamics /
  parent-selected GNS, and the raw-correlator-to-Dyson map.
```

**That required missing object is the common-origin core.** PROVABLE.

### 4.5 The binding table

| operator | level | sector | binding |
|---|---|---|---|
| `Gamma_c` | 1 | record | **MEMBER — DERIVED** (explicit `E_ij` expansion) |
| `b_c` | 1 | record | **MEMBER — DERIVED** |
| `c_c = i Gamma_c b_c` | 1 | record | **MEMBER — DERIVED**; Hermitian; `spec = {−√2,0,+√2}` |
| `iota_c(c_c)` | 1 | record | **MEMBER of `R(C)` — DERIVED** |
| `M_c(t)`, `S_n` | 1 | source | shape correct; needs `dGamma_R`. **CONDITIONAL** |
| `v_c(t) M_c(t) ⊗ S_n ⊗ iota_c(c_c)` | 1 | source ⊗ record | **factorizes exactly as the declaration demands**; conditional on the lift |
| `h_0[g,a]` | 1 | source **+ field** | **BLOCKED** — carries `[g,a]`; unbounded |
| `h_K(t)` | 1 | mixed | **BLOCKED** through `h_0` |
| `H_K(t) = dGamma_R(h_K(t))` | 2 | field algebra | unbounded at the declared carrier ⇒ **affiliation, not membership** |
| `D_K` | 1 | source **+ field** | **BLOCKED** — `nabla_mu` carries the connection |
| `D_K^2` | 1 | source **+ field** | **BLOCKED** — U(1)-curvature, per member 3's own sentence |

---

## 5. Inclusion coherence — `A |-> A tensor I`

### 5.1 The record direction: COHERENT, derived

Member 8's embedding is `iota_NM(A) = A tensor I_(M-N)`. Verified on the sealed cell operators:

```text
|| iota(c_c)^2 - iota(c_c^2) ||                       = 0.000e+00     (unital *-homomorphism)
|| iota(Gamma_c) iota(c_c) + iota(c_c) iota(Gamma_c)|| = 0.000e+00     (anticommutation preserved)
```

**The sealed cell relations are preserved by `A |-> A tensor I`.** So the finite record memberships of
§4.1 feed the inductive system `R_infinity = inductive_limit_N tensor_(j=1)^N M_3(C)` coherently, and
their images are members at every later stage. This closes item 14's first component **for the record
sector**. DERIVED.

### 5.2 The source direction: OBSTRUCTED, and the mechanism is named

The source side's directed system is not `A |-> A tensor I` over `M_3(C)` factors; it is the CAR
inductive system over subspaces of `K_Sigma`. Membership is preserved along that system at every
finite stage (§4.3, full matrix algebras) and is **lost in the limit**, because `dGamma` of any
nonzero one-particle operator is unbounded on the infinite-dimensional Fock carrier. The coherent
statement that survives is affiliation to `CAR(K_Sigma)'' tensor R(K)`.

### 5.3 What I decline to assert

A dimension-counting remark is available — a CAR carrier over a finite-dimensional one-particle space
has dimension `2^n`, so a factor whose dimension is not a power of two cannot be one. Applying it to
any *particular* finite realization would require that realization's dimensions, and the only such
figures I hold are from my own 860/862 replay. **Under BR-1 I decline to use them**, and record the
question as SPEC GAP 1 instead of asserting a conclusion my own un-cross-checked work would have to
carry.

---

## 6. Item-14 impact, and what affiliation inherits

Member 5's corrected item-14 receiving statement has three components. Their status after this
report:

| component | status |
|---|---|
| **(1) a finite-carrier membership map** | **SPLIT, one third CLOSED.** Record sector: **DERIVED** (§4.1, §5.1). Source sector: reduces to naming the Galerkin truncation — a spec act, not a theorem (§4.3, SPEC GAP 1). Field sector: **BLOCKED** by the untyped sector (§4.4). |
| **(2) a scaling-to-record-system resolvent intertwiner** | **UNTOUCHED.** This is the cross-system carrier map; my 909 found the adjacent resolvent family sits on the free tail at its own receiver. |
| **(3) the generic write-plus-tail attachment at `R_INF`** | **UNTOUCHED.** This is the interacting operator, whose joining law my 905 found blocked. Pointer only. |

**What affiliation inherits.** The correct target for the source side is the **von Neumann** closure
`CAR(K_Sigma)'' tensor R(K)`, not the C*-algebra — §4.3 makes that a structural fact rather than a
convenience. Member 9's index-set finding attaches to component (2) and remains un-cross-checked;
this report neither confirms nor uses it.

**S4 stays distinct.** Member 5's `SAME-FAMILY-DISTINCT-RECEIVERS` and member 6's Q-784 typing are
carried unchanged: `R_INF` and `R_S4` share suppliers and neither implies the other. Nothing here
merges them.

---

## 7. The independence claim, tested

The relay types basis line 6 as *"the most independent atom"* and hedges: it *"looks maximally
independent of the common-origin core: finite, algebraic, no measure, no state, no interacting
content."* Tested against the closure, the atom **splits into three components with three different
answers**:

```text
RECORD component  -- GENUINELY INDEPENDENT, and now CLOSED.  Explicit 3x3 matrices in a
                     full matrix algebra, with inclusion coherence verified.  It needed no
                     measure, no state, and no interacting content, exactly as advertised.

SOURCE component  -- CONDITIONALLY INDEPENDENT.  It needs the Galerkin truncation named,
                     and it needs dGamma_R's scope extended from h_K to D_K.  Both are spec
                     acts rather than core results, so the independence claim survives here
                     in weakened form.

FIELD component   -- NOT INDEPENDENT.  D_K carries nabla_mu and D_K^2 carries U(1)-curvature
                     by the record's own sentence, and the field/CTP sector has no declared
                     algebra.  Its required missing object is a complete microscopic
                     operator/CTP construction -- the common-origin core itself.
```

**So the hedge was correct to hedge, and the answer is that the atom is one-third independent.** The
consequence for the campaign is concrete and is the reason this matters: **basis line 6 cannot be run
to completion ahead of the core**, because one of its three components is downstream of the core's
central obstruction. A cascade ordering that schedules this atom as fully independent would be
scheduling a third of it against an input that is not there — which is the wrong-INPUT failure I
disclosed at 901 as the error that wastes a campaign. The record third can and should be booked now;
the field third cannot be booked at all until the extension is typed.

I state this as a **finding about the atom**, not as an amendment to the cascade. The ordering is the
producing lane's and the registrar's to set.

---

## 8. Spec gaps and disclosures

**SPEC GAP 1 — the Galerkin truncation.** Member 3 permits *"the Galerkin compression of this
multiplication operator"* without naming the finite subspace, its dimension, or whether the
compression is applied to the whole one-particle carrier. Every source-side membership statement in
§4.3 is conditional on it. Only a spec statement can settle this; per builder-B independence it is
reported rather than sought in A's code.

**SPEC GAP 2 — `dGamma_R`'s scope.** The lift is stated for `h_K(t)`. Whether the same functor
carries `D_K` and its descendants, and whether membership is meant to be asserted of `D_K` or of
`dGamma_R(D_K)`, is unstated. §4.2 names this as the sharpened form of member 5's *"finite-carrier
membership map."*

**SPEC GAP 3 — the field/CTP sector's algebra.** The standing obstruction, carried from member 4.

**DISCLOSURE 1 — the membership demand may need restating.** At the declared infinite-dimensional
carrier no lifted one-particle operator is an element of the C*-algebra. If basis line 6 is meant to
be answerable in the affirmative, its demand should be affiliation to the von Neumann closure. This
is offered to the producing lane; I amend no sealed system.

**DISCLOSURE 2 — a custody observation, not a complaint.** §2.3: the file defining the record carrier
is unsealed, its only digest pin sits in an artifact that is also unsealed, and roughly a quarter of
the workspace shares that status. My route ran through a sealed intermediary. A registrar may wish to
know that the algebra's primary definition is reachable only that way.

---

## 9. Typed controls (Q-797 discipline)

| control | type | source index |
|---|---|---|
| `E_ij` expansion of the cell operators, §4.1 | **ELIMINATES** — removes "membership unproven" for the record sector by exhibiting coefficients | `CARRIER` |
| the two-level ladder, §4.2 | **EXPLAINS** — shows why member 5's question was posed across two Hilbert spaces, and names the bridging functor | `PARENT`; member 5 |
| `dGamma` unboundedness, §4.3 | **ELIMINATES** — removes C*-membership as an available answer on the source side at the declared carrier | `CARRIER`; `PARENT` |
| finite-stage full-matrix observation, §4.3 | **EXPLAINS** — shows why the finite anchor looked bindable and why the limit is where it breaks | `PARENT` |
| the curvature sentence, §4.4 | **ELIMINATES** — removes the possibility that `D_K^2` is field-free, using member 3's own words rather than an inference | `DESCEND` |
| `iota` homomorphism check, §5.1 | **EXPLAINS** — shows the finite record bindings survive the inductive system | member 8 |

---

## 10. FREEDOMS-CONSUMED (law 2a)

```text
CARRIED-AS-PARAMETER:
  the declared carrier K_Sigma, R(K), R_c and the global CAR algebra, at member 3's scope;
  A_SR(C) = CAR tensor R(C), at member 4's quotation of member 10;
  R_N, iota_NM and R_infinity, at member 8's scope;
  A_K as a PSC slot with no generators;
  the field/CTP sector as NOT_SPECIFIED, with member 4's obstruction;
  member 5's finite-anchor correction, as governing;
  Q-784 / SAME-FAMILY-DISTINCT-RECEIVERS, with S4 distinct;
  my 884 and my 905 as pointer-only dependency disclosures;
  my own 860/862/871 machine record as EXPLICITLY UNUSED.

CONDITIONED-ON:
  every source-side membership statement on SPEC GAP 1 (the Galerkin truncation);
  every statement about D_K's lift on SPEC GAP 2 (dGamma_R's scope);
  the unboundedness argument on K_Sigma being infinite-dimensional as declared;
  the record-sector bindings on member 4's quotation being faithful to member 10,
    which is unsealed and was therefore not read as an authority.

SUBSTITUTED:
  NOTHING. No algebra, generator set, representation, state, truncation, second-quantization
  map, carrier map, intertwiner, domain, affiliation, cascade ordering, or amendment to the
  sealed 24-system or to item 14 was authored or selected. The record-sector membership was
  DERIVED from quoted matrices, not chosen.

SCALING WEIGHTS:
  NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 11. Flattening check, verb audit, byte audit

**FLATTENING CHECK — S01–S37 walked, clean.** Boundedness was not identified with membership —
member 5's correction is applied, not re-litigated. `B(H_K)` membership was not identified with `A_K`
membership. A one-particle operator was not identified with its second quantization. A finite
Galerkin stage was not identified with the declared infinite carrier. Membership in a C*-algebra was
not identified with affiliation to its von Neumann closure. The record sector's automatic membership
was not generalized to the source or field sectors. A shape match — the write term factorizing
correctly — was not identified with a membership proof. `R_INF` was not merged with `R_S4`. An
untyped sector was not identified with an unproven theorem: a missing codomain and a missing proof
are different failures. My own machine record was not allowed to carry a dimensional claim.

**BUILDER-B INDEPENDENCE:** constructed from sealed sources and opposite-lane receivers. A's code was
not accessed; three details that only a spec statement could settle were reported as SPEC GAPs. **This
lane verifies nothing of its own:** member 9 is my 884 and is pointer-only, with member 5's correction
of it carried as governing; my 860/862/871 results are excluded by name in §2.2 and §5.3.

**SELF VERB AUDIT.** "Derived" is used for the record-sector memberships, the inclusion coherence, the
level mismatch, and the unboundedness consequence — each with a proof in text and a machine residual
where checkable. "Blocked" is used where a codomain is absent. "Conditional" is used where a spec act
would settle the matter. No algebra, representation, membership beyond the record sector, affiliation,
or authorization is claimed. `VERB_AUDIT_SELF = CLEAN`.

**BYTE-POSITION SELF-AUDIT.** Measured on the sealed bytes of this file: §1's heading begins at byte
**514**, its closure fence opens at byte **935**, and the exact member list ends at byte
**2363**. A token scan of bytes `[0,935)` for absence-shaped forms — `no `, `not `, `none`,
`never`, `absent`, `missing`, `without`, `lack`, `fail`, `gap`, ` open`, `unresolved`, `underived`,
`false`, `cannot`, `zero`, `stop`, `block`, `wrong`, `refus`, `unsealed` — returns **zero hits**.

---

## 12. Final lines

```text
CLOSURE = declared-first (byte position: sec-1 heading 514, closure fence 935, members end 2363; pre-closure absence-token scan over 21 forms = 0 hits)
ALGEBRA = displayed (A_SR(C) = CAR(K_Sigma,q) tensor R(C); generators: a(f)/a(f)* with the CAR relations on the source side, ALL of B(R_c) = M_3(C) per cell on the record side, and NONE declared on the field side; membership demand = decomposition into source tensor record factors drawn from those generator sets)
BINDINGS = Gamma_c, b_c, c_c, iota_c(c_c): MEMBER — DERIVED with explicit E_ij coefficients | M_c(t), S_n and the full write term v_c M_c (x) S_n (x) iota_c(c_c): shape matches the declaration exactly, CONDITIONAL on dGamma_R's scope | h_0[g,a], h_K(t), D_K, D_K^2: BLOCKED — field content with no declared codomain | H_K = dGamma_R(h_K): affiliation, not membership, at the declared infinite-dimensional carrier
INCLUSION = COHERENT on the record side (iota is a unital *-hom preserving the sealed cell relations, residuals 0.000e+00) / OBSTRUCTED on the source side in the limit (dGamma of any nonzero one-particle operator is unbounded on infinite-dimensional Fock; membership holds at every finite Galerkin stage and does not survive as membership)
ITEM14_IMPACT = displayed. Component (1) finite-carrier membership map SPLITS three ways with the RECORD third CLOSED, the source third reduced to a spec act, the field third blocked. Components (2) resolvent intertwiner and (3) generic write-plus-tail UNTOUCHED. Affiliation inherits a corrected target: the von Neumann closure CAR(K_Sigma)'' tensor R(K), not the C*-algebra. S4 stays distinct.
VERDICT = PARTIAL (record-sector bindings DERIVED and inclusion-coherent; source-sector bindings CONDITIONAL on two named spec acts; field-sector bindings BLOCKED by a missing codomain rather than a missing proof. THE INDEPENDENCE CLAIM IS TESTED AND FAILS FOR ONE OF THREE COMPONENTS: D_K^2 carries U(1)-curvature by the record's own sentence, the field/CTP sector is NOT_SPECIFIED as an algebra, and its required missing object is the common-origin core. Basis line 6 is one-third independent and cannot be run to completion ahead of the core.)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
