# STAGE 8 / ATTACH PREMISE / B1-CELL — THE FIRST BUILT b_1 = 1 CELL INSTANCE AND THE AND-CLASS DECISION ON IT

Lane: FABLE blind construction, codename B1-CELL. One entered premise instantiated; one
exact question decided on the instance. Date: 2026-08-13.
ALL_RESULTS = CLAIMED until the opposite-lane check.

## Lead determination — CLAIMED

The ENTERED Attach premise's cell is REALIZED for the first time. The sealed text itself
decides the shape — not an annulus, not a cylinder, but **the unfilled oriented
1-skeleton `K_square` with NO 2-cell** (V011 :1840, :1881-1882), census **V = 4, E = 4,
F = 0**, pinned to the byte by the frozen ordered bases and the displayed incidence
matrix. On this instance, exactly over Q: **CONNECTED (components = 1), b_1 = 1,
H^1 = 1 EXACT**; the unique cycle class `gamma_j` is exhibited
(`+e_a0 +e_ab -e_ba -e_0b`, orientation fixed by the sealed ordered loop word);
`d_1 d_0 = 0` holds. The AND-class block decision re-run on the instance gives
**dim H = 1** (basis exhibited) and:

```text
AND_SATISFIABLE = PARTIAL — the MIRROR of the working-class collapse:
  phi_H sector:  SATISFIABLE, and in fact FORCED nonzero on every admissible write
                 (witness exhibited; all four sealed write constraints verified exactly);
  phi_f sector:  FAILS IDENTICALLY — im(d_1^dagger) = {0} because the premise itself
                 mandates F = 0, so phi_f = 0 for EVERY 1-cochain; no witness can exist.
  AND = [phi_f != 0 AND phi_H != 0] = [FALSE AND TRUE] = FALSE at every admissible
  member of THIS instance too — constant by rank identity, exactly as on the working
  class, but by the OPPOSITE dead block.
```

The Q-1027 result stands untouched for its own complexes; its own scope clause (AND §7:
"A sealed record complex with dim H > 0 (nontrivial first cohomology) would give phi_H
a nonzero-dimensional range and REOPEN the AND-class question at that level") is here
realized for the first time — on an ENTERED-AUTHORED carrier, not the working class —
and the reopened question immediately re-closes to the same boolean value FALSE.

PROVENANCE, carried explicitly: this instance realizes an ENTERED AUTHORED premise
(disclosed input of record — addendum 420ab02f, 2026-08-12). Nothing below is
premise-free; every claim is TYPE-P on the Attach entry and voids with it.

---

## 0. Preflight, seals, and fences

Output name probed before any write: `STAGE8_B1_CELL_INSTANCE_V001.md` ABSENT (artifact
and sidecar). No register, tracker, plan, road, ledger, or lens file read. All
verification by `shasum -a 256` at path BEFORE reliance; every digest below recomputed
from bytes this session.

```text
SEALS VERIFIED (recomputed at path; sidecar match where a sidecar exists):
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                     MATCH (the ENTERED premise of record)
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md  MATCH (the supply-mandate DoR)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md    MATCH (the ENTERED artifact)
5154c203ee237c4dee1b02176f8fa64c84b3ec97e561b720304f1a58219ad29b  STAGE8_ATTACH_CANDIDATE_V007_CHECK_CODEX2_V001.md MATCH (certifying check; sealed final lines confirmed at bytes: VERDICT = SURVIVES-FOR-ENTRY :232, SCOPE = REPAIR-ONLY :228, CONTENT_IDENTITY = RATIFIED-CONTENT-EXACT :227)
82d5c5dd59d1d0d6981a2cde7244c1dad1a66352c4159b8543f0554777abea31  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V003.md    MATCH (ratified-content byte source; V007 table row 05)
96ec8bf4e2706eced5b17489d53f3844402331854ed4ea82d54c212dec3a22d7  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V005.md    MATCH (carrier of the 27-row member table; V007 table row 29)
56d9c9b68dcde555fe3157a4ae09545418b7b8f35766761a91184a534ce42f1b  STAGE8_ATTACH_CANDIDATE_V002_CHECK_CODEX2_V001.md MATCH (rebuilt incidence matrices; b_1(K_square) = 1; selector-free circuit)
5f55c27ffb9e08e49f964b9c1640436c5a4631010a1404ec8bb6d8bd622c952c  STAGE8_ATTACH_CANDIDATE_V004_CHECK_CODEX2_V001.md MATCH (probed for citation resolution — note N-1 below)
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md     MATCH (the K_square specification of record; V003 table row 10)
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md                MATCH (the census)
a22ef820c5e665cbf9a5c941d24be57efb06c9c921ee579e5291d0cdc65feaa3  STAGE8_GLUED_TOPOLOGY_CHECK_V001.md               MATCH (the check; its Attach-lineage citation digests :69-72 confirmed against the four lineage files above)
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md             MATCH (the method replicated; the working-class baseline)
c0e7192be7d5d93d74a28d66c9c0e39543f4cb755c222e7b1a9cb7f3b4b72f46  STAGE8_AND_CLASS_CHECK_V001.md                    MATCH vs its own sidecar at path (task gave no digest; AND_VERDICT = CONFIRMED(CONSTANT_FALSE) at :10, :264)
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md              MATCH (FORM D1 — the block definitions of record)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md               MATCH (the sealed write constraints)
UNVERIFIABLE: none. Every relied-on file verified at path.
```

(Transcription note on the seal table: digests are pasted from `shasum` output;
the authoritative bytes are the sidecars and the recomputation commands in §6's
environment, not this display.)

FENCES HELD: exact structural algebra only (integer/rational; one indeterminate
coefficient bookkept as an integer multiplier — no value ever assigned); NO physical
quantity computed, bounded, or evaluated; no scale, no imported GR, no faithfulness
authority; no continuum object consumed; the register/tracker/plan/road/ledger/lens
families untouched.

NOTE N-1 (citation resolution across the version tables, verified both ways at bytes):
V007's carried §0 text cites "Circuit canonicity for `K_square` at `b_1 = 1` (member 04
`:88-108`)". Under V007's OWN member table, member 04 = the V004 check (5f55c27f), whose
:88-108 is alias-audit content — NOT the cited material. The §0 text is byte-carried
from V003 (V003 :91, identical sentence), and under V003's member table member 04 = the
V002 check (56d9c9b6), whose :88-114 contains exactly the cited content (rebuilt
incidence matrices, `b_1(K_square) = 1`, the selector-free circuit). Likewise the
operative-domain block's "member 10 :1840-1852" resolves under V003's table to
BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md (20a3a17d), where the span exists and
carries the K_square specification. Both resolutions were confirmed at bytes; reliance
below is on the resolved files at their verified digests.

---

## 1. THE PREMISE'S EXACT CONSTRAINTS ON THE CELL — quoted at spans

### 1.1 The entry of record (ATTACH_ENTRY_ADDENDUM_V001.md, 420ab02f)

:32-35 (the entered content and its domain):

> The source-history attachment map, as proposed and six-times-checked: a_j := θ_j(A) — the circuit
> holonomy of the connection on the cell's closed circuit; χ_n(a_j) = exp(i n θ_j(A)) per the ratified
> holonomy rule; U_N[A,J,g] = P_0⊗I + P_ch⊗W_N^(n)[A] at n = +1; OPERATIVE DOMAIN: connected admitted
> b₁ = 1 cells (trees inert by the record's own determination; b₁ > 1 out of scope).

CONSTRAINTS EXTRACTED: (i) the cell is CONNECTED; (ii) b_1 = 1; (iii) the reading is
the circuit holonomy θ_j(A) on the cell's closed circuit; (iv) trees (b_1 = 0) are
inert; (v) b_1 > 1 is out of scope.

### 1.2 The entered artifact (STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md, ccca6bb4)

:162-172 (the operative-domain block — this is what names the cell's SHAPE):

> OPERATIVE DOMAIN OF THE CANDIDATE (V003):
>   connected admitted cells with b_1 = 1 — the specified square sector, member 10 :1840-1852.
>   There the cycle class is unique, so gamma_j is canonical up to the F08 orientation reversal and
>   NO SELECTOR IS FROZEN, because there is nothing to select.
>
> DISPOSITION OF b_1 = 0 (tree, incl. first opening): the candidate is INERT and returns U_N^0.
>   Correct behaviour — member 11 :70-77 rules the emptiness a DETERMINATION made on purpose.
>
> DISPOSITION OF b_1 > 1: OUT OF SCOPE. Not claimed, not covered, not silently included. Admitting it
>   would require a lawful cycle selector, which this artifact does not supply and may not freeze.

:634-637, :648 (the operative construction's domain and reading):

> DOMAIN: connected admitted cells with b_1 = 1; gamma_j the unique cycle class, orientation per F08.
>
>   theta_j := contour-integral over gamma_j of A
>   Hol     := exp( i theta_j )
> ...
>   J-dependence: exactly P_ch.   g-dependence: exactly through gamma_j.

:149-150 (circuit canonicity, carried as settled):

> **Circuit canonicity for `K_square` at `b_1 = 1`** (member 04 `:88-108`), from independently rebuilt
> incidence matrices. Carried.

:584 (F08's role): "orientation law          ** THE ONE FULLY FORCED-MET REQUIREMENT **   F08, F09"

CONSTRAINTS EXTRACTED: (vi) the operative sector is THE SPECIFIED SQUARE SECTOR at
member 10 :1840-1852 — the sealed text names the concrete complex; (vii) gamma_j is the
UNIQUE cycle class, canonical up to one orientation reversal (F08); (viii) the reading
theta_j is the contour integral of the connection over gamma_j; (ix) the cell's whole
g-dependence enters through gamma_j.

### 1.3 The specified square sector (BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md, 20a3a17d — resolved per note N-1)

:1840-1851 (the shape, the census, the orientations — the sealed text's decision):

> Use the unfilled oriented `1`-skeleton `K_square` with no `2`-cell and:
>
> ```text
> vertices:
>   v_00, v_10, v_01, v_11
>
> edges:
>   e_a0: v_00 -> v_10
>   e_0b: v_00 -> v_01
>   e_ab: v_10 -> v_11
>   e_ba: v_01 -> v_11
> ```

:1860-1863 (the cycle class and its orientation — the ordered loop word):

> Let the ordered loop holonomy be
>
> ```text
> u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi).
> ```

:1881-1882 (the 2-cell/filling condition — an EXCLUSION, not a filling):

> Use the Hilbert forms produced by Gate 3. No filled `2`-cell is included in
> the trace carrier.

:1896-1901 (the frozen ordered bases — dimension and basis order pinned):

> Freeze the ordered bases:
>
> ```text
> C_0: (v_00, v_10, v_01, v_11),
> C_1: (e_a0, e_0b, e_ab, e_ba).
> ```

:1903-1911 (the exact incidence matrix; at trivial holonomy u = 1 this is the integer
vertex-edge incidence used below):

> In those bases the exact incidence matrix is:
>
> ```text
> D_square(Phi) =
> [[-1, -1,  0,              0],
>  [ 1,  0, -1,              0],
>  [ 0,  1,  0,             -1],
>  [ 0,  0,  1,    exp(-i Phi)]].
> ```

### 1.4 Lineage corroboration (STAGE8_ATTACH_CANDIDATE_V002_CHECK_CODEX2_V001.md, 56d9c9b6 — the "member 04 :88-108" content)

:97-99, :108-109:

> K_square:
>   d_1 = [[-1,-1,0,0],[1,0,-1,0],[0,1,0,-1],[0,0,1,1]]
>   rank(d_1)=3, |E|=4, b_1=1.
> ...
> Exactly one independent cycle makes the circuit selector-free on `K_square`, or more generally on a
> connected cell with `b_1=1`.

(Notation note: that check writes the vertex-edge boundary as "d_1"; in the AND-class
convention used below the same integer matrix is `bdry = d_0^T`. The bytes agree.)

### 1.5 The gap this instance fills (STAGE8_GLUED_TOPOLOGY_CHECK_V001.md, a22ef820)

:279-283:

> ... the ENTERED Attach supplies the intra-cell holonomy-reading half
> (which edges: the unique cycle class gamma_j of the b_1 = 1 cell) at
> entered-authored-premise strength, on a typed domain, with no built instance. What
> remains unsupplied of record: any DERIVATION of the constructor; the INTER-cell
> successor/anchoring (O12 across cells); a built b_1 = 1 physical cell instance; ...

### 1.6 Size/minimality statements

The sealed premise contains NO separate minimality clause. It does something stronger:
the candidate's operative-domain block names "the specified square sector, member 10
:1840-1852" (§1.2), and that span pins the census EXACTLY — 4 named vertices, 4 named
oriented edges, "no 2-cell", frozen ordered bases. There is NO size freedom left to
minimize over. Abstractly smaller connected b_1 = 1 complexes exist (one vertex + one
loop edge; the two-vertex bigon) and would satisfy the addendum's one-line domain
typing (§1.1) alone — but they are NOT the specified sector the entered candidate
operates on. CONFORMANCE OVER CONVENIENCE: the instance below is the sealed one.

---

## 2. THE INSTANCE — exhibited in full

The minimal combinatorial 2-complex conforming to the sealed constraints is the sealed
specification itself, taken as a 2-complex with EMPTY 2-cell set (the premise mandates
the absence: §1.3 :1840, :1881-1882 — an annular/cylindrical 2-skeleton would ADD
2-cells the sealed text excludes from the carrier).

```text
K_j  (the admitted physical cell instance, per the ENTERED premise):

  0-cells (V = 4, frozen order):   v_00, v_10, v_01, v_11
  1-cells (E = 4, frozen order, oriented source -> target):
      e_a0 : v_00 -> v_10
      e_0b : v_00 -> v_01
      e_ab : v_10 -> v_11
      e_ba : v_01 -> v_11
  2-cells (F = 0):                 NONE  (premise-mandated)

  chain/cochain spaces over Q:  C_0 = C^0 = Q^4,  C_1 = C^1 = Q^4,  C_2 = C^2 = Q^0 = {0}

  boundary  bdry : C_1 -> C_0   (rows = vertices, cols = edges; -1 source, +1 target):

              e_a0  e_0b  e_ab  e_ba
      v_00  [  -1    -1     0     0 ]
      v_10  [   1     0    -1     0 ]
      v_01  [   0     1     0    -1 ]
      v_11  [   0     0     1     1 ]

      == the sealed D_square(Phi) at u = 1 (V011 :1903-1911), asserted entrywise in §6.

  coboundaries (the AND-class convention):
      d_0 = bdry^T : C^0 -> C^1     (4 x 4 integer matrix)
      d_1 : C^1 -> C^2 = {0}        (the 0 x 4 zero map — F = 0)

  the unique cycle class (orientation per the sealed ordered loop word, §1.3 :1860-1863,
  whose exponent vector on the frozen edge order (e_a0, e_0b, e_ab, e_ba) is
  (+1, -1, +1, -1)):

      gamma_j = + e_a0 - e_0b + e_ab - e_ba          (1-cycle in C_1;  bdry gamma_j = 0)
      chi_j   = (+1, -1, +1, -1)                     (dual 1-cochain in C^1; generates H^1)

  the intra-cell reading, realized:  theta_j(A) = <chi_j, A> = A(e_a0) - A(e_0b) + A(e_ab) - A(e_ba)
      — in the sealed fixed gauge (V011 :1866-1873: u_a0 = u_ab = u_0b = 1,
      u_ba = exp(-i Phi)) this reads theta_j = +Phi, matching the sealed loop word
      exp(i Phi) exactly (coefficient arithmetic only; Phi is an indeterminate).
```

FREEDOMS THE PREMISE LEFT FREE — named, and how each was fixed:

```text
FR-1  F08 orientation reversal of gamma_j (V007 :165: canonical "up to the F08
      orientation reversal").  FIXED to the sealed ordered loop word's traversal
      (V011 :1863), i.e. the a-side forward.  The reversed choice negates chi_j and
      theta_j; every dimension, rank, and verdict below is invariant under it.
FR-2  The inner product realizing "perp" in H = ker(d_1) ∩ im(d_0)^perp.  TAKEN as the
      standard (counting) inner product on the frozen ordered bases — the AND-class
      method's own convention (and FORM D1's "sealed Gate-3 counting metric" clause,
      FORM :136-137).  Block DIMENSIONS are inner-product-independent.
CONV  Coefficients Q (the AND-class rank arithmetic); d_1 represented as the 0 x 4
      empty matrix.  Bookkeeping conventions, not premise freedoms.
NONE ELSE: the census, the basis orders, and the edge orientations are all pinned by
      the sealed spec (§1.3); there is no placement, size, or filling freedom.
```

---

## 3. VERIFICATION — the instance realizes the premise (all exact over Q; script §6)

```text
V-1  CHAIN COMPLEX:   d_1 d_0 = 0  — the product is the 0 x 4 matrix; every entry zero
     (F = 0), dimensions asserted.  [CHECK 1]
V-2  CONNECTED:       rank_Q d_0 = 3 (full Fraction RREF, AND 3-prime ranks at 807's
     instrument primes 1000003/1000033/1000037, sandwiched exactly by
     d_0.(constants) = 0  =>  rank <= V-1 = 3); components = V - rank = 4 - 3 = 1.
     [CHECKS 2,3,5]
V-3  b_1 = 1:         b_1 = E - rank d_0 = 4 - 3 = 1.  [CHECK 6]
V-4  H^1 = 1 EXACT:   ker d_1 = C^1 (dim 4, since F = 0);
     dim H^1 = dim ker d_1 - rank d_0 = 4 - 3 = 1.  [CHECKS 4,6]
V-5  THE UNIQUE CYCLE CLASS, EXPLICIT:  gamma_j = +e_a0 -e_0b +e_ab -e_ba;
     bdry(gamma_j) = (0,0,0,0) verified per vertex; chi_j = (+1,-1,+1,-1) verified
     orthogonal to all four coboundary generators, nonzero, hence [chi_j] generates
     H^1; pairing <chi_j, gamma_j> = 4 != 0.  Uniqueness: the exact Fraction null
     space of bdry is ONE-dimensional with primitive integer basis (+1,-1,+1,-1) —
     recomputed, not assumed.  [CHECKS 7,8,9,12]
V-6  ORIENTATION/READING CONFORMANCE:  the sealed fixed gauge yields theta_j = +Phi,
     the sealed loop word's exp(i Phi) — the F08-fixed orientation is the sealed one.
     [CHECK 10]
V-7  SEALED-BYTES CONFORMANCE:  the rebuilt incidence equals the sealed D_square(u=1)
     entrywise [CHECK 0]; it also equals the V002-check's independently rebuilt matrix
     (§1.4) entrywise; and the glued-topology census row for K_square
     (HUNT :294-297: V=4, E=4, F=0, rank d_0 = 3, rank d_1 = 0, H^1 = 1, loop cochain
     "+e_a0 +e_ab -e_ba -e_0b") is reproduced exactly.
```

VERDICT: **INSTANCE = EXHIBITED. The premise IS realizable combinatorially, and its
realization is the sealed K_square itself: connected, b_1 = 1, H^1 = 1 exact,
d_1 d_0 = 0, unique cycle class exhibited with the sealed orientation.**

---

## 4. THE AND-CLASS DECISION RE-RUN ON THE INSTANCE

### 4.1 The blocks (FORM D1, 5e49d209 :140-142, quoted)

> A^1 = im(d_0)  ⊕  H  ⊕  im(d_1^dagger),
> H := ker(d_1) ∩ im(d_0)^perp        (the flux-free physical block),
> im(d_1^dagger) = ker(d_1)^perp      (the flux-carrying physical block).

On the instance, exactly [CHECKS 11,12]:

```text
dim im(d_0)        = rank d_0            = 3
dim H              = dim ker d_1 - rank d_0 = 4 - 3 = 1     BASIS: { chi_j = (+1,-1,+1,-1) }
dim im(d_1^dagger) = rank d_1            = 0                (F = 0  =>  the flux block is {0})
sum check:  3 + 1 + 0 = 4 = E   (exact)
```

**DIM_H_ON_INSTANCE = 1 EXACT.** The harmonic block IS the unique cycle class's line:
H = ker(bdry) = span{chi_j}. (On this carrier ker d_1 = C^1, so H coincides with
im(d_0)^perp; with the standard inner product that is exactly ker(bdry).)

### 4.2 The sealed write constraints (G3, 3e35ffe2 :130-135, quoted)

> **Q6 — the carried constraint inventory (GB, GC; not re-derived).** The record fixes
> `ell_j` gauge-invariant on the difference branch, cell-local, conserved-typed
> (`∂ell_j = 0` = the same `⊥ im(d_0)` condition, GC :86-91), total-nonzero for
> `n != 0`; it does NOT fix the block-split ...

Applied to the instance:

```text
conserved-typed / gauge-invariant:  d_0^dagger ell = 0  <=>  ell ⊥ im(d_0)
                                    =>  ell ∈ H ⊕ im(d_1^dagger) = H ⊕ {0} = H
cell-local:                         the instance IS the single cell j's own carrier;
                                    support inside the writing cell is trivially
                                    satisfied ON-CELL (the inter-cell half is not
                                    posed here — §5)
total-nonzero (n != 0):             phi_f + phi_H != 0
ADMISSIBLE SET = { ell ∈ H : ell != 0 }  =  H \ {0}   — a dim-1 punctured line, NONEMPTY.
```

### 4.3 The two sectors, decided (script §6)

```text
phi_H SECTOR — SATISFIABLE, AND FORCED:
  WITNESS  ell_H := chi_j = (+1, -1, +1, -1)  on (e_a0, e_0b, e_ab, e_ba).
  VERIFIED EXACTLY:  d_0^dagger ell_H = (0,0,0,0)   (conserved / gauge-invariant);
                     ell_H != 0;  ell_H ∈ H  (the computed null-space basis itself);
                     phi_H(ell_H) = P_H ell_H = ell_H != 0;
                     phi_f(ell_H) = 0;  total phi_f + phi_H = ell_H != 0  (n != 0 OK);
                     cell-local on-cell (support = the four edges of cell j's carrier).
  MOREOVER FORCED:   with phi_f ≡ 0 (below), total-nonzero forces phi_H != 0 at EVERY
                     admissible member — the exact mirror of the working-class argument
                     (AND :214-216), with the roles of the two blocks exchanged.

phi_f SECTOR — FAILS IDENTICALLY:
  im(d_1^dagger) = {0} exactly, because rank d_1 = 0, because F = 0 — and F = 0 is the
  PREMISE'S OWN mandate ("no 2-cell", "No filled 2-cell is included in the trace
  carrier", §1.3).  P_f is the ZERO MAP; phi_f(ell) = 0 for EVERY 1-cochain, a fortiori
  for every admissible write.  No witness can exist.  This is IDENTICAL vanishing —
  the same strength the working class had for phi_H, now for phi_f.

THE AND-BOOLEAN ON THIS INSTANCE:
  AND = [phi_f != 0 AND phi_H != 0] = [FALSE AND TRUE] = FALSE at every admissible
  member.  CONSTANT — certified by the rank identities (rank d_1 = 0; rank d_0 = 3),
  not by sampling.  Member-independence on THIS carrier is trivial: both sectors'
  fates are forced by exact dimensions.
```

**AND_SATISFIABLE = PARTIAL: the phi_H sector is satisfiable (witness exhibited — and
in fact forced nonzero); the phi_f sector fails identically (zero-dimensional block).
Both-nonzero has NO realization on this instance. Neither "the AND-class is satisfiable
here" nor "phi_H is dead" survives — the truth is the mirror collapse.** Satisfiable
vs forced, said exactly: phi_H != 0 is FORCED on the admissible set (not merely
satisfiable); phi_f != 0 is UNSATISFIABLE; their conjunction is CONSTANT_FALSE.

### 4.4 What was replicated from the method of record

Same block definitions (FORM D1); same admissible-space typing (G3 §6 / Q6); same rank
instruments (full Fraction-exact RREF over Q AND mod-p ranks at 807's three primes
1000003/1000033/1000037 with the exact rational sandwich, AND :174-182); same
sum-check discipline (dim im(d_0) + dim H + dim im(d_1^dagger) = E, AND :198); same
witness discipline (explicit exact integer cochain, all constraints verified, AND §4).
Orientation-convention invariance of every rank statement holds as in AND :185-187.

---

## 5. THE STRATUM CONSEQUENCE — stated honestly

```text
S-1  Q-1027 STANDS, UNTOUCHED, FOR ITS OWN COMPLEXES.  The AND-class result
     (AND_CLASS = CONSTANT_FALSE; dim H = 0 on parent K, A1, A2-F, A2-B, Z — the
     table at AND :192-199) binds to the sealed working-class instance and its
     licensed refinements (AND §7 "BINDING" clause).  This instance is NOT in that
     class and contradicts nothing in it.

S-2  THE REOPENING CONDITION IS NOW REALIZED — ON AN AUTHORED CARRIER.  AND §7 stated
     it prospectively: "A sealed record complex with dim H > 0 (nontrivial first
     cohomology) would give phi_H a nonzero-dimensional range and REOPEN the AND-class
     question at that level.  NO such complex is sealed of record."  As of this build,
     a b_1 = 1 instance EXISTS with dim H = 1 — but it enters at
     ENTERED-AUTHORED-PREMISE strength (the Attach entry), not as a derived physical
     carrier.  The reopened question immediately re-closes: AND = CONSTANT_FALSE here
     too, by the mirror collapse (phi_f dead instead of phi_H).  On NO carrier now of
     record is the AND-class satisfiable.

S-3  WHAT THIS INSTANCE DOES NOT ESTABLISH.
     (a) It does NOT make K_square a physical carrier.  The glued-topology check's
         finding survives verbatim: "NONE_SEALED at the physical write-carrier
         stratum ... no complex instance with H^1 > 0 is sealed as a physical
         carrier" (check :288-291), and the entry itself carries the open condition
         "no result shows [the operative sector] is the physical domain"
         (addendum :46-47).
     (b) It does NOT realize the WRITE on this carrier.  The sealed write chain (G3)
         is realized on the working-class record carrier; no sealed map sends K_square
         into the write-carrier complex (check :325 context).  §4 carried the write
         CONSTRAINTS across by their sealed TYPE (Q6) — a typed re-run, not a sealed
         embedding.  The write's actual realization on THIS carrier is OPEN.
     (c) Member-independence on THIS carrier is decided trivially (rank-forced,
         §4.3) and binds ONLY this instance; it does not extend the Q-1027
         certificate, and the working-class certificate does not extend here.
     (d) The POST-LIMIT is untouched (the Gate-5 kernel question of the pose-check's
         Correction A concerns the working class; nothing here bears on it).
     (e) The composition-loop result on this very complex remains UNSEALED of record
         (V011 :2321 `composition_loop_prediction_sealed = false`); this artifact
         instantiates the carrier and decides the block question ONLY.
     (f) Everything here is TYPE-P on the Attach entry (420ab02f) and its chain; a
         failed blind score or a structural contradiction voids the entry and this
         instance with it (addendum :49-57).

S-4  THE ONE-LINE CONSEQUENCE.  The entered premise's cell is buildable and behaves
     exactly as its sealed lineage computed (b_1 = 1, H^1 = 1); on it the AND-class
     collapses AGAIN — by the opposite block — so the flux/harmonic conjunction has
     no admissible realization on ANY carrier now of record; the live gaps after this
     build are the write's realization on this carrier, the inter-cell half (O12),
     the physical-domain proof, and the post-limit.
```

---

## 6. SCRIPT, VERBATIM (`b1_cell_instance_v001.py`, Python 3 stdlib only)

```python
#!/usr/bin/env python3
# b1_cell_instance_v001.py — B1-CELL: instantiate the ENTERED Attach premise's cell and
# re-run the AND-class block decision on the instance. Exact integer/rational arithmetic
# only (Python 3 stdlib: fractions). No physical quantity is computed or bounded.
from fractions import Fraction

# ---------------------------------------------------------------------------
# 0. THE SEALED SPECIFICATION (BID V011, digest 20a3a17d..., spans :1840-1911)
#    "Use the unfilled oriented 1-skeleton K_square with no 2-cell"  (:1840)
#    frozen ordered bases  C_0: (v_00, v_10, v_01, v_11)             (:1899)
#                          C_1: (e_a0, e_0b, e_ab, e_ba)             (:1900)
#    "No filled 2-cell is included in the trace carrier."            (:1881-1882)
# ---------------------------------------------------------------------------
V = ['v_00', 'v_10', 'v_01', 'v_11']
E = [('e_a0', 'v_00', 'v_10'),   # :1847
     ('e_0b', 'v_00', 'v_01'),   # :1848
     ('e_ab', 'v_10', 'v_11'),   # :1849
     ('e_ba', 'v_01', 'v_11')]   # :1850
F = []                            # NO 2-cell — premise-mandated (:1840, :1881-1882)
nV, nE, nF = len(V), len(E), len(F)
vi = {v: i for i, v in enumerate(V)}

# boundary  bdry : C_1 -> C_0   (rows = vertices, cols = edges; -1 at source, +1 at target)
bdry = [[0] * nE for _ in V]
for j, (e, s, t) in enumerate(E):
    bdry[vi[s]][j] -= 1
    bdry[vi[t]][j] += 1

# sealed exact incidence matrix D_square(Phi) at trivial holonomy u = 1 (V011 :1903-1911)
D_sealed_u1 = [[-1, -1, 0, 0], [1, 0, -1, 0], [0, 1, 0, -1], [0, 0, 1, 1]]
assert bdry == D_sealed_u1
print('CHECK 0  rebuilt vertex-edge incidence == sealed D_square(u=1)      :', bdry == D_sealed_u1)

# cochain maps of the AND-class convention:  d_0 = bdry^T : C^0 -> C^1  (rows=edges, cols=vertices)
#                                            d_1 : C^1 -> C^2 = {0}    (F = 0  =>  0x4 zero map)
d0 = [[bdry[i][j] for i in range(nV)] for j in range(nE)]
d1_shape = (nF, nE)  # (0, 4)

# ---------------------------------------------------------------------------
# 1. d_1 d_0 = 0  entrywise  (d_1 has zero rows: the product is the 0 x 4 empty
#    matrix; every entry of it is zero vacuously — dimension bookkeeping asserted)
# ---------------------------------------------------------------------------
prod_shape = (d1_shape[0], nV)
assert prod_shape == (0, 4)
print('CHECK 1  d_1 d_0 = 0 entrywise (shape %dx%d, all entries zero)      : True' % prod_shape)

# ---------------------------------------------------------------------------
# exact rank instruments (the AND-class method of record):
#   (a) full Fraction-exact RREF rank over Q
#   (b) mod-p ranks at 807's three primes, with the rational sandwich
# ---------------------------------------------------------------------------
def rankQ(M, rows, cols):
    if rows == 0 or cols == 0:
        return 0
    A = [[Fraction(x) for x in row] for row in M]
    r = 0
    for c in range(cols):
        p = next((i for i in range(r, rows) if A[i][c] != 0), None)
        if p is None:
            continue
        A[r], A[p] = A[p], A[r]
        pv = A[r][c]
        A[r] = [x / pv for x in A[r]]
        for i in range(rows):
            if i != r and A[i][c] != 0:
                f = A[i][c]
                A[i] = [a - f * b for a, b in zip(A[i], A[r])]
        r += 1
    return r

def rankp(M, rows, cols, p):
    if rows == 0 or cols == 0:
        return 0
    A = [[x % p for x in row] for row in M]
    r = 0
    for c in range(cols):
        piv = next((i for i in range(r, rows) if A[i][c] % p), None)
        if piv is None:
            continue
        A[r], A[piv] = A[piv], A[r]
        inv = pow(A[r][c], p - 2, p)
        A[r] = [(x * inv) % p for x in A[r]]
        for i in range(rows):
            if i != r and A[i][c]:
                f = A[i][c]
                A[i] = [(a - f * b) % p for a, b in zip(A[i], A[r])]
        r += 1
    return r

PRIMES = (1000003, 1000033, 1000037)

# d_0 kills constants:  d_0 . (1,1,1,1)^T = 0   =>  rank_Q d_0 <= V - 1
const_img = [sum(d0[i][j] for j in range(nV)) for i in range(nE)]
assert const_img == [0] * nE
print('CHECK 2  d_0 . (constants) = 0  =>  rank_Q d_0 <= V-1 = 3          :', const_img == [0] * nE)

r0Q = rankQ(d0, nE, nV)
r0p = [rankp(d0, nE, nV, p) for p in PRIMES]
r1Q = rankQ([], *d1_shape)
r1p = [rankp([], *d1_shape, p) for p in PRIMES]
assert r0Q == 3 and all(r == 3 for r in r0p)
assert r1Q == 0 and all(r == 0 for r in r1p)
print('CHECK 3  rank_Q d_0 = %d (Fraction RREF); rank_p d_0 = %s @ p=%s' % (r0Q, r0p, list(PRIMES)))
print('         sandwich: rank_p d_0 = 3 = V-1 forces rank_Q d_0 = 3 exactly : True')
print('CHECK 4  rank d_1 = %d exactly (d_1 is the empty 0x4 map; F = 0)' % r1Q)

# ---------------------------------------------------------------------------
# 2. connectivity and b_1 (exact over Q)
# ---------------------------------------------------------------------------
components = nV - r0Q                      # = nV - rank(bdry)
b1 = nE - r0Q                              # = dim ker(bdry) for a graph
dim_ker_d1 = nE - r1Q                      # ker d_1 = C^1 when F = 0
dim_H1 = dim_ker_d1 - r0Q                  # H^1 = ker d_1 / im d_0
assert components == 1 and b1 == 1 and dim_H1 == 1
print('CHECK 5  connected components = V - rank d_0 = 4 - 3 = %d           : CONNECTED' % components)
print('CHECK 6  b_1 = E - rank d_0 = 4 - 3 = %d ;  dim H^1 = dim ker d_1 - rank d_0 = %d - %d = %d  EXACT'
      % (b1, dim_ker_d1, r0Q, dim_H1))

# ---------------------------------------------------------------------------
# 3. the unique cycle class gamma_j, explicit
#    F08 orientation fixed by the sealed ordered loop word (V011 :1860-1863):
#        u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi)
#    exponent vector on the frozen edge order (e_a0, e_0b, e_ab, e_ba):
# ---------------------------------------------------------------------------
loop_word_exponents = [+1, -1, +1, -1]     # e_a0^+1  e_0b^-1  e_ab^+1  e_ba^-1
gamma = loop_word_exponents                # the 1-cycle (chain) in C_1
chi = loop_word_exponents                  # the dual 1-cochain in C^1 (same frozen basis)

# gamma is a cycle: bdry . gamma = 0, verified per vertex
bg = [sum(bdry[i][j] * gamma[j] for j in range(nE)) for i in range(nV)]
assert bg == [0] * nV
print('CHECK 7  bdry(gamma) per vertex = %s  =>  gamma is an exact 1-cycle' % bg)

# chi is orthogonal to every coboundary: chi . (column of d_0) = 0 for each vertex generator
perp = [sum(chi[i] * d0[i][j] for i in range(nE)) for j in range(nV)]
assert perp == [0] * nV
print('CHECK 8  <chi, d_0 delta_v> per vertex = %s  =>  chi ⊥ im(d_0)' % perp)

# chi is a cocycle (d_1 = 0), nonzero, and perpendicular to im(d_0)  =>  [chi] generates H^1
assert any(c != 0 for c in chi)
pairing = sum(chi[j] * gamma[j] for j in range(nE))
assert pairing == 4
print('CHECK 9  chi != 0, chi ⊥ im(d_0)  =>  [chi] != 0 generates H^1; <chi, gamma> = %d != 0' % pairing)

# orientation consistency with the fixed gauge (V011 :1866-1873):
# a = (0, 0, 0, -Phi) on (e_a0, e_0b, e_ab, e_ba)  =>  theta_j = <chi, a> = +Phi  (symbolic in Phi:
# coefficient arithmetic only; Phi is an indeterminate, no value assigned)
a_coeff_of_Phi = [0, 0, 0, -1]
theta_coeff = sum(chi[j] * a_coeff_of_Phi[j] for j in range(nE))
assert theta_coeff == +1
print('CHECK 10 fixed-gauge reading: theta_j = (+%d) Phi  ==  the sealed exp(i Phi) loop word' % theta_coeff)

# ---------------------------------------------------------------------------
# 4. the three blocks of FORM D1:  A^1 = im(d_0) (+) H (+) im(d_1^dagger)
#    H := ker(d_1) ∩ im(d_0)^perp   (standard inner product on the frozen basis,
#    the AND-class method's convention).  ker d_1 = C^1 (F = 0), so
#    H = im(d_0)^perp = ker(d_0^T) = ker(bdry)  — computed exactly.
# ---------------------------------------------------------------------------
dim_im_d0 = r0Q
dim_im_d1dag = r1Q
dim_H = dim_ker_d1 - r0Q          # ker d_1 = C^1 entire; intersect with im(d_0)^perp of codim r0Q
assert dim_im_d0 + dim_H + dim_im_d1dag == nE == 4
print('CHECK 11 blocks: dim im(d_0) = %d, dim H = %d, dim im(d_1^dagger) = %d; sum = %d = E'
      % (dim_im_d0, dim_H, dim_im_d1dag, nE))

# exact basis of H: null space of bdry over Q (Fraction RREF), primitive integer form
A = [[Fraction(x) for x in row] for row in bdry]
rows, cols = nV, nE
pivots = []
r = 0
for c in range(cols):
    p = next((i for i in range(r, rows) if A[i][c] != 0), None)
    if p is None:
        continue
    A[r], A[p] = A[p], A[r]
    pv = A[r][c]
    A[r] = [x / pv for x in A[r]]
    for i in range(rows):
        if i != r and A[i][c] != 0:
            f = A[i][c]
            A[i] = [a - f * b for a, b in zip(A[i], A[r])]
    pivots.append(c)
    r += 1
free = [c for c in range(cols) if c not in pivots]
basis = []
for fc in free:
    v = [Fraction(0)] * cols
    v[fc] = Fraction(1)
    for ri, pc in enumerate(pivots):
        v[pc] = -A[ri][fc]
    basis.append(v)
assert len(basis) == 1
h = basis[0]
scale = 1
for x in h:
    scale = scale * x.denominator // __import__('math').gcd(scale, x.denominator)
h_int = [int(x * scale) for x in h]
if h_int[0] < 0:
    h_int = [-x for x in h_int]
assert h_int == chi
print('CHECK 12 exact null-space basis of bdry over Q (primitive, sign per F08 loop word): %s == chi' % h_int)

# ---------------------------------------------------------------------------
# 5. the AND-class decision on the instance (sealed write constraints, G3 Q6):
#    conserved-typed / gauge-invariant  <=>  ell ⊥ im(d_0)  (d_0^dagger ell = 0)
#    cell-local: the instance IS the single cell j's carrier  (trivially satisfied)
#    total-nonzero for n != 0:  phi_f + phi_H != 0
# ---------------------------------------------------------------------------
# phi_f: the projector onto im(d_1^dagger) = {0}  (rank d_1 = 0)  — the ZERO MAP.
print('DECIDE   phi_f: im(d_1^dagger) = {0} (dim 0, since F = 0) => P_f = 0; phi_f(ell) = 0 for EVERY 1-cochain')
# phi_H: witness ell_H := chi
ellH = chi
d0dag_ell = [sum(d0[i][j] * ellH[i] for i in range(nE)) for j in range(nV)]  # d_0^dagger ell = bdry ell
assert d0dag_ell == [0] * nV
phi_f_wit = [0] * nE                       # projection onto {0}
phi_H_wit = ellH                            # ell_H lies in H exactly (CHECK 12), so P_H ell_H = ell_H
total = [a + b for a, b in zip(phi_f_wit, phi_H_wit)]
assert any(t != 0 for t in total)
print('DECIDE   phi_H witness ell_H = %s: d_0^dagger ell_H = %s (conserved/gauge-invariant),' % (ellH, d0dag_ell))
print('         phi_H(ell_H) = ell_H != 0, phi_f(ell_H) = 0, total phi_f+phi_H = %s != 0 (n != 0 OK)' % total)
print('DECIDE   admissible set = { ell in H (+) im(d_1^dagger) : phi_f + phi_H != 0 } = H \\ {0}  (dim-1 line, nonempty)')
print('DECIDE   phi_H != 0 is FORCED on every admissible write (total-nonzero with phi_f == 0)')
print('DECIDE   phi_f != 0 is UNSATISFIABLE (the block is zero-dimensional)')
print('DECIDE   AND = [phi_f != 0 AND phi_H != 0] = [FALSE AND TRUE] = FALSE at every admissible member')
print()
print('RESULT   B1_VERIFIED: connected, b_1 = 1, H^1 = 1 EXACT')
print('RESULT   DIM_H_ON_INSTANCE = 1 EXACT, basis {(+1,-1,+1,-1)} on (e_a0,e_0b,e_ab,e_ba)')
print('RESULT   AND_SATISFIABLE = PARTIAL: phi_H sector satisfiable (and forced nonzero); phi_f sector fails identically')
print('RESULT   the collapse MIRRORS the working class: there H was {0} and flux alive; here flux is {0} and H alive')
print('ALL ASSERTIONS PASSED')
```

## 7. EXACT SCRIPT OUTPUT, VERBATIM (`python3 b1_cell_instance_v001.py`, exit code 0)

```text
CHECK 0  rebuilt vertex-edge incidence == sealed D_square(u=1)      : True
CHECK 1  d_1 d_0 = 0 entrywise (shape 0x4, all entries zero)      : True
CHECK 2  d_0 . (constants) = 0  =>  rank_Q d_0 <= V-1 = 3          : True
CHECK 3  rank_Q d_0 = 3 (Fraction RREF); rank_p d_0 = [3, 3, 3] @ p=[1000003, 1000033, 1000037]
         sandwich: rank_p d_0 = 3 = V-1 forces rank_Q d_0 = 3 exactly : True
CHECK 4  rank d_1 = 0 exactly (d_1 is the empty 0x4 map; F = 0)
CHECK 5  connected components = V - rank d_0 = 4 - 3 = 1           : CONNECTED
CHECK 6  b_1 = E - rank d_0 = 4 - 3 = 1 ;  dim H^1 = dim ker d_1 - rank d_0 = 4 - 3 = 1  EXACT
CHECK 7  bdry(gamma) per vertex = [0, 0, 0, 0]  =>  gamma is an exact 1-cycle
CHECK 8  <chi, d_0 delta_v> per vertex = [0, 0, 0, 0]  =>  chi ⊥ im(d_0)
CHECK 9  chi != 0, chi ⊥ im(d_0)  =>  [chi] != 0 generates H^1; <chi, gamma> = 4 != 0
CHECK 10 fixed-gauge reading: theta_j = (+1) Phi  ==  the sealed exp(i Phi) loop word
CHECK 11 blocks: dim im(d_0) = 3, dim H = 1, dim im(d_1^dagger) = 0; sum = 4 = E
CHECK 12 exact null-space basis of bdry over Q (primitive, sign per F08 loop word): [1, -1, 1, -1] == chi
DECIDE   phi_f: im(d_1^dagger) = {0} (dim 0, since F = 0) => P_f = 0; phi_f(ell) = 0 for EVERY 1-cochain
DECIDE   phi_H witness ell_H = [1, -1, 1, -1]: d_0^dagger ell_H = [0, 0, 0, 0] (conserved/gauge-invariant),
         phi_H(ell_H) = ell_H != 0, phi_f(ell_H) = 0, total phi_f+phi_H = [1, -1, 1, -1] != 0 (n != 0 OK)
DECIDE   admissible set = { ell in H (+) im(d_1^dagger) : phi_f + phi_H != 0 } = H \ {0}  (dim-1 line, nonempty)
DECIDE   phi_H != 0 is FORCED on every admissible write (total-nonzero with phi_f == 0)
DECIDE   phi_f != 0 is UNSATISFIABLE (the block is zero-dimensional)
DECIDE   AND = [phi_f != 0 AND phi_H != 0] = [FALSE AND TRUE] = FALSE at every admissible member

RESULT   B1_VERIFIED: connected, b_1 = 1, H^1 = 1 EXACT
RESULT   DIM_H_ON_INSTANCE = 1 EXACT, basis {(+1,-1,+1,-1)} on (e_a0,e_0b,e_ab,e_ba)
RESULT   AND_SATISFIABLE = PARTIAL: phi_H sector satisfiable (and forced nonzero); phi_f sector fails identically
RESULT   the collapse MIRRORS the working class: there H was {0} and flux alive; here flux is {0} and H alive
ALL ASSERTIONS PASSED
```

---

## FLAG BLOCK

```text
PREMISE_CONSTRAINTS = QUOTED AT SPANS: addendum 420ab02f :32-35 ("OPERATIVE DOMAIN:
  connected admitted b₁ = 1 cells (trees inert by the record's own determination;
  b₁ > 1 out of scope)"; a_j := θ_j(A) the circuit holonomy on the cell's closed
  circuit); V007 ccca6bb4 :162-172 (operative domain = "the specified square sector,
  member 10 :1840-1852"; gamma_j unique, canonical up to F08 reversal; b_1 = 0 inert;
  b_1 > 1 out of scope), :634-637 (DOMAIN line; theta_j := contour-integral over
  gamma_j of A), :648 (g-dependence exactly through gamma_j), :149-150 (K_square
  circuit canonicity carried); BID V011 20a3a17d :1840-1851 ("the unfilled oriented
  1-skeleton K_square with no 2-cell" + the vertex/edge/orientation display),
  :1860-1863 (the ordered loop word = the cycle class's orientation), :1881-1882
  ("No filled 2-cell is included in the trace carrier"), :1896-1901 (frozen ordered
  bases), :1903-1911 (the exact incidence matrix); V002-check 56d9c9b6 :97-99, :108-109
  (rebuilt incidence, b_1 = 1, selector-free circuit); NO minimality clause — the
  sealed spec pins the census exactly (no size freedom).
INSTANCE = EXHIBITED(V = 4, E = 4, F = 0 — the sealed text decides the shape: the
  unfilled K_square, NOT an annulus/cylinder; the census is premise-pinned so the
  specified instance is the minimal conforming one; freedoms named: FR-1 the F08
  orientation reversal of gamma_j, fixed to the sealed loop word; FR-2 the inner
  product realizing ^perp, taken standard on the frozen bases per the method of
  record; nothing else free).
B1_VERIFIED = H^1 = 1 EXACT (connected: components = 1; rank_Q d_0 = 3 by Fraction
  RREF + three-prime sandwich; rank d_1 = 0; d_1 d_0 = 0; b_1 = 1; unique cycle class
  gamma_j = +e_a0 -e_0b +e_ab -e_ba exhibited, bdry(gamma_j) = 0 per vertex).
DIM_H_ON_INSTANCE = 1 EXACT (H = ker(d_1) ∩ im(d_0)^perp; basis
  { chi_j = (+1,-1,+1,-1) on (e_a0,e_0b,e_ab,e_ba) }, recomputed as the exact
  Fraction null space; sum check 3 + 1 + 0 = 4 = E).
AND_SATISFIABLE = PARTIAL(phi_f sector FAILS identically: im(d_1^dagger) = {0}
  because the premise mandates F = 0, so phi_f = 0 for every 1-cochain and no witness
  can exist; phi_H sector SATISFIABLE and FORCED nonzero on every admissible write:
  witness ell_H = (+1,-1,+1,-1), verified conserved/gauge-invariant
  (d_0^dagger ell_H = 0), cell-local on-cell, total-nonzero; hence
  AND = CONSTANT_FALSE on this instance too — the MIRROR of the working-class
  collapse, with the opposite dead block).
STRATUM_CONSEQUENCE = the AND §7 reopening condition (dim H > 0) is realized for the
  first time — at entered-authored-premise strength, outside the working class — and
  the reopened AND-class immediately re-closes to FALSE by the mirror collapse;
  Q-1027 stands untouched for its own complexes; open: the write's realization on
  this carrier, the inter-cell half (O12), the physical-domain proof, the post-limit.
PROVENANCE_CARRIED = entered-authored-premise (disclosed input of record; every claim
  TYPE-P on addendum 420ab02f and voiding with it; not premise-free).
SEALS_VERIFIED = 420ab02f (addendum), ad9fc14e (DoR), ccca6bb4 (V007), 5154c203
  (V007 check), 82d5c5dd (V003), 96ec8bf4 (V005), 56d9c9b6 (V002 check), 5f55c27f
  (V004 check), 20a3a17d (BID V011), 1892c08e (hunt), a22ef820 (glued check),
  38bbb9fc (AND-class), c0e7192b (AND-class check, vs own sidecar), 5e49d209 (FORM),
  3e35ffe2 (G3) — all recomputed at path; none unverifiable.
SCRIPT_REPRODUCED = yes (in-artifact, verbatim, with verbatim output; exit 0; stdlib
  only; exact integer/rational arithmetic; 12 checks + witness, all asserted).
FORBIDDEN_IMPORTS = none (no scale, no imported GR, no faithfulness authority, no
  continuum object; Phi kept an indeterminate — coefficient bookkeeping only).
MACHINERY_INVOKED = no.
alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
