# STAGE 8 / TASK 5 / STEP 4 — EXTSRC, ROUND 2 OF THE TIMEBOX

Lane: **Codex Lane 3 (Sol, high effort)**  
Artifact: `STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md`  
Register preflight head: **Q-524**  
Mode: **symbolic only; construction/determination; no member binding**

## Lead determination

Round 2 executes every attack prescribed in Section 5 of the round-one
artifact.  It finds three genuine positive pieces:

```text
(i)  Abar_N^(-1):(K_N^fin)^* -> Tbar_N
     is the canonical inverse on the surface-visible quotient;

(ii) (K_N^fin)^* --(R_K,N^fin)^(-1)--> K_N^fin
                   --I_K,N--> J_cyc,N
     is a sealed one-current prefix;

(iii) the named rank-one pure-cycle relation at the reciprocal stage,
      and its q=2 refinement image, are sealed `qtilde/Q` zero classes;
      any future linear common-target maps would send those zero classes
      to zero.
                                                               (L0)
```

None of these pieces supplies the physical source-extension member.  The
quotient inverse begins only after a missing source-to-cycle-dual map; the
current prefix ends before the missing current-to-kernel landing; and the
conditional pure-cycle regression does not form the common target or prove
the mixed `L_T`/cycle relation theorem.
The inherited topology, addressed factor, and actual ExtSrc generator bases
remain separately uninhabited.

The registrar-suggested metric-section attack is lawful to ask but fails on
its premise.  DoR-019 metrizes the finite `K` carrier and its finite `C`
dual, not

```text
T_N=Omega_c^1(M_N;iR),
```

the domain of `A_N`.  Pulling the target metric back through `A_N` gives a
semiform with radical `ker(A_N)`; it gives the quotient metric already encoded
by `Abar_N`, not an orthogonal representative in `T_N`.  Thus it supplies no
Moore--Penrose inverse, no representative-valued section, and neither adjacent
bridge `b_N` nor `e_N`.

The Round-2 determination is therefore another sealed-stock `TYPE-U` stop,
not a proof that the ExtSrc family is empty.  Two of four authorized rounds
have been spent.  This artifact recommends the requested principal checkpoint
before any Round 3 authors new carrier or extension data; the standing
`CARRY-1` explicit-incompleteness route remains intact.

## 0. Preflight, authorities, and determination discipline

### 0.1 Access, register, seals, and no-clobber

Before substantive work the following checks were executed:

```text
cleanroom workspace = readable and writable
archive workspace   = readable
supervision         = readable
register tail/head  = Q-524

round-one artifact SHA-256
 =075a2b16e333ae0baad38f38d7a458290e5efacebf4f9b53470ef4177b8b0d97
round-one sidecar check = OK

requested V002 artifact = absent in cleanroom and archive
requested V002 sidecar  = absent in cleanroom and archive.      (P0)
```

Q-524 records the Round-1 honest stop, the exact Round-2 plan, and the
registrar's metric-section suggestion as a suggestion rather than law.

### 0.2 Verified load-bearing authorities

| Key | Sealed authority | Verified SHA-256 | Use in Round 2 |
|---|---|---|---|
| `R1` | `STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md` | `075a2b16e333ae0baad38f38d7a458290e5efacebf4f9b53470ef4177b8b0d97` | specification and Section-5 attack plan |
| `SEQ` | `DECISION_SEQUENCED_PROGRAM_2026-08-06.md` | `7984d6aba553d50bb1bf5013c687e14e7d77a8a43f53e5101fcece1fc98ba5ac` | four-round ExtSrc timebox, fallback, unit obligation |
| `MET` | `DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md` | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | exact metric and unit scope |
| `MET-V005` | `STAGE8_TASK4B_CARRIER_METRIC_AND_UNITS_PROPOSAL_LANE2_V005.md` | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | finite `K/C` metrics, quotient norm, conversion prohibition |
| `KER` | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | `A_N`, currents, finite bilocal realization, completion stop |
| `Q408` | `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V004.md` | `c5254f663a1e3a6d1079027184c6dea96a35ab7255a856aa39e584b3d7c32dc2` | quotient tests, `P_alg`, and its exact physical scope |
| `SEAM1` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md` | `5de94e16db09b982f85fdf117281af62df6ab0d05c9e7577ac258d47594b69b7` | raw source, topology boundary, ExtSrc declaration |
| `SEAM2` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V002.md` | `2525096ba06973b70064d6b9f9578470e0afca9c48e2cca6cf0f5c1194d12c52` | addressed orbit and repaired R9 quantifier |
| `JII` | `STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md` | `fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70` | consolidated Joint-A fields and consumers |
| `JII-R` | `STAGE8_TASK5_JII_REVIEW_DARIO_V001.md` | `ba9430c17c61adbfce41df21dc5a14a34707c210cb69fc9c7455dd0530c19236` | `A_G` near-miss and sector-not-scope correction |
| `LOC-NAT` | `STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md` | `474bf721517f77b240e2a215325a86227d4e0fd4934e89ba910d955627b5ab60` | named rank-one relation and q=2 transport display |

Every local sidecar used above was checked.  The round-one cleanroom and
archive bytes agree at the cited digest.

### 0.3 Status vocabulary and gates

```text
[PROVABLE]      = displayed consequence of verified sealed stock.
[PART-PROVABLE] = displayed conditional or strict subscope result.
[YOURS]         = candidate type or family coordinate; not adopted or bound.

TYPE-U = a needed map or theorem is not produced by sealed stock.
TYPE-R = stated requirements are mutually inconsistent.

Round-2 outcome = TYPE-U, not TYPE-R.
```

The gates remain:

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
comparison_to_measured_constants = none.                       (P1)
```

No fence blocks any structural result below.  Therefore no
`MACHINERY-APPEAL` is entered.

## 1. Round-2 target and assembly-first piece ledger

### 1.1 Joint-A target retained without strengthening

The construction-minimal physical target from Round 1 remains

```text
ExtSrc_N^JA
 :=(E_N,qtilde_N,Q_N^quot,O_prof,N,
    O_R5,N^src,D_N^ker,D_N^Loc,
    iota_N^Q408,Sch_N,S_N,Kraw_N,Sbar_N,Kernbarhat_N,
    Ker_(a,K):O_a^resp->D_N^Loc,
    PhysAgree_(a,K),AddrOrb_N,
    etahat_f:D_N^Loc->D_M^Loc;
    source agreement, inherited topology and image topology,
    physical addressed agreement,
    identity, composition, and admitted arrow carriage).       (S1)
```

The exact raw source and demanded agreement are

```text
E_N
 :=R L_T,N direct-sum R Rhat_K,N
      direct-sum S_(1,sa)(K_N),

qtilde_N(a,b,C)
 :=a L_T,N+j_R4,N(b R_K,N+C),

S_N(qtilde_N(a,b,C))
 =a Sch_N(M_N^loc)
  +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                  (S2)
```

The longer `Res/QuotResCert/Ker^im/Ker^ker/tau/rho` package is retained as
the declared sufficient realization route.  Round 2 does not promote that
longer witness to a syntactic requirement of a bare route prefix, and it does
not weaken the physical agreement in `(S2)`.

### 1.2 Pieces before joints

| # | Piece and type | Round-2 standing |
|---:|---|---|
| 1 | `T_N:=Omega_c^1(M_N;iR)` | `SEALED`; nuclear compact-test space, not a DoR-019 Hilbert carrier |
| 2 | `A_N:T_N->(K_N^fin)^*`, `(A_N a)(c)=<J_(N,c),a>` | `SEALED`, onto |
| 3 | `q_N^test:T_N->>Tbar_N:=T_N/ker(A_N)` | `SEALED` quotient |
| 4 | `Abar_N:Tbar_N isomorphic_to (K_N^fin)^*` | `SEALED`; inverse exists on quotient |
| 5 | finite `R_K,N^fin:K_N^fin isomorphic_to (K_N^fin)^*` | `SEALED` DoR-019 Riesz map |
| 6 | `I_K,N:K_N^fin->J_cyc,N`, `c|->J_(N,c)` | `SEALED`, faithful finite current map |
| 7 | `P_alg,f:Tbar_N->Tbar_M` | `SEALED` only on the admitted actual finite/`Ref_path` construction after its path/current datum; physical use remains gated by `OLD_FID/LR/RNL` |
| 8 | `sigma_cyl,N:Hom(K_N^fin,(K_N^fin)^*)->D'_biloc,N` | `SEALED` finite bilocal realization |
| 9 | completed current/distribution continuity | `NOT_BUILT / TYPE-U` in `KER (B2-15)--(B2-16)` |
| 10 | `D_N^ker`, `Sch_N`, `iota_N^Q408` | `REQUIRED`; no common physical members |
| 11 | `bbar_N:O_R5,N^src->Tbar_N` | Round-2 quotient-domain target; no member |
| 12 | `ell_N^(cyc->ker):J_cyc,N->D_N^ker` | Round-2 one-current landing target; no member |
| 13 | `i_cyl,N:D'_biloc,N->D_N^ker` | Round-2 bilocal landing target; no member |
| 14 | `kappa_N:I_N^q->I_N^R5`, underlying-set identity | continuity/embedding unproved |
| 15 | addressed `Res`, `S_(w->N,K)`, `Ker^im`, `Ker^ker`, `Ker` | `REQUIRED`; no members |
| 16 | reciprocal/refinement `tau` bases and cycle-creating old-image `rho` | `REQUIRED`; no ExtSrc members |
| 17 | `AddrOrb_N={a,a^-}`, `mu(a^-)=-mu(a)` | `SEALED` orbit; no representative selected |
| 18 | cross-sector unit torsors and arrows | `[YOURS]` declared in Section 9; no scale or representative selected |

No declaration is counted as an inhabitant in this ledger.

## 2. Attack Q — quotient-domain bridge

### 2.1 [PROVABLE] The maximal canonical quotient arrow

The quotient construction gives

```text
T_N --q_N^test->> Tbar_N
    --Abar_N--> (K_N^fin)^*,

Abar_N q_N^test=A_N.                                           (Q1)
```

Because `Abar_N` is an isomorphism, the canonical inverse

```text
sbar_N:=Abar_N^(-1):(K_N^fin)^*->Tbar_N                         (Q2)
```

is formed.  It selects a quotient class, not a representative test one-form.

### 2.2 [PROVABLE] The source gap is equivalent to a missing covector map

A candidate quotient-domain bridge would be

```text
bbar_N:O_R5,N^src->Tbar_N.                                     (Q3)
```

Since `Abar_N` is invertible, `(Q3)` is equivalent to a candidate

```text
lambda_N:=Abar_N bbar_N
 :O_R5,N^src->(K_N^fin)^*,

bbar_N=sbar_N lambda_N.                                        (Q4)
```

Thus `sbar_N` can only invert a covector after `lambda_N` is supplied.  It
does not construct `lambda_N`.

The raw response contains

```text
qtilde_N(a,b,C)=a L_T,N+j_R4,N H,
H:=b R_K,N+C in Hom(K_N,K_N^*).                                (Q5)
```

No sealed linear operation sends `(Q5)` to a cycle covector.  In particular,

```text
H:K_N->K_N^*

H(c,-) in K_N^*       requires a chosen c in K_N;
trace(R_K,N^(-1)C)    where defined on the trace-class summand
                       is a scalar, not an element of K_N^*;
trace(R_K,N^(-1)H)    on a finite carrier is likewise scalar,
                       while on the completed identity branch it may be
                       undefined rather than a covector;
L_T,N                 has no sealed image in (K_N^fin)^*.      (Q6)
```

On the reciprocal rank-one carrier, the addressed orbit carries both signs.
Replacing `c_RL` by `-c_RL` gives

```text
H(-c_RL,-)=-H(c_RL,-),                                         (Q7)
```

so contraction with a chosen oriented generator does not descend to the
unaddressed response source without extra addressed data.  The metric does
not choose one address as positive.

### 2.3 [PROVABLE on the sealed finite/Ref_path core] Quotient covariance

For an admitted rank-preserving current map

```text
S_f:K_N^fin->K_M^fin,

C_f:=R_K,M^fin S_f (R_K,N^fin)^(-1)
 :(K_N^fin)^*->(K_M^fin)^*,                                    (Q8)
```

the Q-408 algebraic test transport is

```text
P_alg,f
 :=Abar_M^(-1) C_f Abar_N:Tbar_N->Tbar_M.                       (Q9)
```

The quotient inverses obey the fully displayed covariance

```text
P_alg,f sbar_N
 =Abar_M^(-1) C_f Abar_N Abar_N^(-1)
 =Abar_M^(-1) C_f
 =sbar_M C_f.                                                   (Q10)
```

At the actual `q=2` `Ref_path` identification this is the sealed quotient
transport (and is `id` under the admitted carrier identification).  Physical
use of `(Q9)` remains subject to the fixed `OLD_FID_f`, `LR_f`, and, wherever
consumed, `RNL_f` tests.  Outside the actual finite/`Ref_path` core, `(Q9)` is
conditional on the still-proposed path/current package.

Even on the formed scope, source covariance would require

```text
lambda_M tau_f^src=C_f lambda_N,

bbar_M tau_f^src
 =sbar_M lambda_M tau_f^src
 =sbar_M C_f lambda_N
 =P_alg,f sbar_N lambda_N
 =P_alg,f bbar_N.                                               (Q11)
```

Neither `lambda_N` nor the actual ExtSrc `tau_f^src` is sealed, so `(Q11)`
cannot be instantiated at the reciprocal or refinement source.

```text
ATTACK_Q = STOPPED
formed Q.0 = sbar_N=Abar_N^(-1), with scoped covariance (Q10)
gap Q.1 = SOURCE_TO_CYCLE_DUAL_MAP lambda_N
gap Q.2 = bbar_N=sbar_N lambda_N
gap Q.3 = ACTUAL_SOURCE_COVARIANCE (Q11)
gap Q.4 = L_T_AND_PROFILE_SOURCE_AGREEMENT.                     (Q12)
```

## 3. Attack C — current/kernel landing

### 3.1 [PROVABLE] Longest sealed one-current prefix

DoR-019 and `KER` give

```text
(K_N^fin)^*
 --(R_K,N^fin)^(-1)--> K_N^fin
 --I_K,N--> J_cyc,N,

I_K,N(c)=J_(N,c).                                              (C1)
```

The first arrow is finite Riesz inversion and the second is the actual
path-current injection.  This prefix is basis-free and reader-free.

The desired continuation is a different, unbuilt arrow:

```text
ell_N^(cyc->ker):J_cyc,N->D_N^ker.                              (C2)
```

If it existed, then and only then one could form

```text
e_N^(2)
 :=ell_N^(cyc->ker) I_K,N (R_K,N^fin)^(-1)
 :(K_N^fin)^*->D_N^ker.                                       (C3)
```

### 3.2 Arity attack

A current is a one-leg distribution.  The sealed physical kernel is bilocal.
The tempting assignments fail the linear target type:

```text
J_c |-> J_c tensor J_c            is quadratic in c;

J_c |-> J_c tensor J_0            is linear only after selecting
                                  a new current J_0;

J_c |-> D_N^ker                    has no sealed one-current-to-kernel
                                  realization law.             (C4)
```

The arity-matched finite route starts one level higher:

```text
Hom(K_N^fin,(K_N^fin)^*)
 --sigma_cyl,N--> D'_biloc,N
 --i_cyl,N?--> D_N^ker.                                       (C5)
```

`sigma_cyl,N` is sealed.  `i_cyl,N` is not.  Moreover, extending the finite
path kernel to the completed carrier requires the uniform seminorm estimates

```text
p_q(J_c)<=C_q ||c||_K,
p_Q(Kern_N^fin(H))<=C_Q ||H||_prof,                             (C6)
```

which `KER (B2-15)--(B2-16)` records as `NOT_BUILT / TYPE-U`.
Neither notation nor Riesz duality identifies `D'_biloc,N` with `D_N^ker`.

### 3.3 Full-source regression

Even granting `(C2)` and the still-missing `bbar_N`, the candidate

```text
S_N^(2):=e_N^(2) Abar_N bbar_N
 :O_R5,N^src->D_N^ker                                           (C7)
```

must satisfy

```text
S_N^(2)(qtilde_N(a,b,C))
 =a Sch_N(M_N^loc)
  +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                  (C8)
```

The one-current prefix proves neither the `L_T` term nor equality with the
Q-408 cycle-kernel image.  It also supplies no support or transport square
for `D_N^ker`.

```text
ATTACK_C = STOPPED
formed C.0 = finite Riesz/current prefix (C1)
formed C.1 = finite bilocal sigma_cyl,N only
gap C.1 = ell_N^(cyc->ker):J_cyc,N->D_N^ker
gap C.2 = i_cyl,N:D'_biloc,N->D_N^ker
gap C.3 = UNIFORM_COMPLETION/CLOSABILITY (C6)
gap C.4 = FULL_SOURCE_AGREEMENT (C8).                           (C9)
```

## 4. Attack R — mixed relations on the actual bottoms

### 4.1 Exact condition, formed only conditionally

If the common target pieces existed, define

```text
F_N:E_N->D_N^ker,

F_N(a,b,C)
 :=a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                 (R1)
```

The physical image map

```text
S_N^I(qtilde_N z):=F_N(z)                                     (R2)
```

is well defined exactly when

```text
ker(qtilde_N) subset ker(F_N),

qtilde_N z=qtilde_N z'
 =>F_N(z)=F_N(z').                                              (R3)
```

Before any relation test, `(R1)` itself is unformed because `D_N^ker`,
`Sch_N`, and `iota_N^Q408` have no sealed common member.  The calculations
below are therefore exact conditional relation calculations, not an ExtSrc
inhabitance claim.

### 4.2 [PART-PROVABLE] Reciprocal zero class and conditional image regression

On the carried reciprocal rank-one line, let `e_n` denote either temporary
normalized orbit representative inside the invariant calculation, let
`x=r e_n`, and put `t:=r^2` symbolically.  Then

```text
P_x=t R_K,n,
r_x:=(0,-t,P_x),

qtilde_n(r_x)
 =j_R4,n(P_x-t R_K,n)
 =j_R4,n(t R_K,n-t R_K,n)
 =0.                                                           (R4)
```

Hence `r_x in ker(qtilde_n) subset Rel_n` and

```text
Q_n^quot(r_x)=0.
```

Because the `L_T` coefficient of `r_x` is zero, its conditional cycle image is

```text
F_n(r_x)
 =iota_n^Q408 Kernbar_n^cyc(Q_n^quot(r_x))
 =iota_n^Q408 Kernbar_n^cyc(0)
 =iota_n^Q408(0)
 =0.                                                           (R5)
```

Equation `(R4)` is a genuine `qtilde/Q` zero-class pass on the named actual
relation.  Equation `(R5)` is only the conditional regression that any future
linear common-target maps send that zero class to zero.  It is not an ExtSrc
relation `PASS`, because those maps are unformed.  The calculation chooses no
address, evaluates no `t`, and constructs no common target map.

### 4.3 [PART-PROVABLE] The q=2 zero class and conditional image regression

For the actual nontrivial `Ref_path` arrow `s_nm:n->m`, `LOC-NAT` seals the
rank-preserving isometry `S_nm` and the form transport

```text
J_nm^form(P_x)=t R_K,m,

j_nm^E(r_x)
 =(0,-t,J_nm^form(P_x))
 =(0,-t,t R_K,m).                                               (R6)
```

Therefore

```text
qtilde_m(j_nm^E(r_x))
 =j_R4,m(t R_K,m-t R_K,m)
 =0,

Q_m^quot(j_nm^E(r_x))=0,

F_m(j_nm^E(r_x))
 =iota_m^Q408 Kernbar_m^cyc(0)
 =0.                                                           (R7)
```

Thus the named reciprocal relation and its named refinement image pass only
as actual `qtilde/Q` zero classes.  Conditionally, future linear common-target
maps would send them to zero; no ExtSrc common-target `PASS` is claimed.

### 4.4 General mixed relation stops

For a general relation `r=(a,b,C)` the required implication is

```text
a L_T,N+j_R4,N(b R_K,N+C)=0

 =>a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C))=0.               (R8)
```

The quotient equality `Q_N^quot(a,b,C)=0` does not turn `(R8)` into a
pure-cycle equality because `Kernbar_N^cyc` is scoped to
`Q_N^quot(0,b,C)`, while the `L_T` branch is sent through `Sch_N`.  No sealed
law identifies their images in a common target.  Nor does sealed stock prove
that the rank-one relation `(R4)` generates every mixed kernel relation.

Passing from `ker(qtilde_N)` to

```text
Rel_N=closure(ker(qtilde_N)) subset ker(F_N)                    (R9)
```

would additionally require `F_N` continuous into a Hausdorff `D_N^ker`.
That is precisely part of the missing construction.  Since `(R8)` is absent
at both the reciprocal and refinement bases, no finite-pasting induction can
establish it generally.

```text
ATTACK_R = STOPPED/PARTIAL
formed R.0 = named reciprocal qtilde/Q zero class (R4)
formed R.1 = its q=2 qtilde/Q zero class (R6) and the qtilde/Q lines of (R7)
conditional R.2 = future linear common-target images vanish (R5),(R7)
gap R.1 = COMMON_DKER/SCH/IOTA_FORMATION
gap R.2 = GENERAL_MIXED_L_T/CYCLE_RELATION (R8)
gap R.3 = RELATION_GENERATION
gap R.4 = CLOSURE_CONTINUITY (R9).                              (R10)
```

Absence of a displayed counterexample is not counted as a pass.

## 5. Attack T — topology and minimal source scope

### 5.1 Quotient versus inherited topology

Retain the distinct carriers

```text
I_N:=im(qtilde_N),

I_N^q :=I_N with the qtilde_N quotient topology,
I_N^R5:=I_N with the O_R5,N subspace topology,

Q_N^core:E_N->>I_N^q,
kappa_N:I_N^q->I_N^R5,  kappa_N(x)=x.                          (T1)
```

Because `Q_N^core` is quotient, the universal property gives the exact test

```text
kappa_N continuous
 <=>kappa_N Q_N^core:E_N->I_N^R5 continuous
 <=>corestr_(I_N^R5)(qtilde_N) continuous.                     (T2)
```

The sealed source types `qtilde_N` algebraically but does not prove its
continuity for the inherited physical `R5` topology.  Even granting that
one direction, a topological embedding additionally requires

```text
topology(I_N^q)=topology(I_N^R5),

equivalently qtilde_N:E_N->I_N^R5 is quotient onto its image,  (T3)
```

or a sufficient open-map/closed-range/two-sided seminorm certificate.  No
such certificate is sealed.

The first-isomorphism arrow also retains its premise:

```text
Rel_N=ker(qtilde_N)
 =>beta_N:O_prof,N->I_N^q,
   beta_N Q_N^quot=Q_N^core.                                  (T4)
```

Closure in the definition of `Rel_N` cannot be deleted without proving the
kernel closed.

### 5.2 Minimal source coverage

The smallest candidate scope is

```text
O_R5,N^src:=I_N^R5.                                            (T5)
```

But the physical agreement route also consumes unaddressed responses through

```text
S_(w->N,K)|_(iota_w^resp(O_R5,w^src))
 =T_(w->N,K)^D S_w (iota_w^resp)^(-1).                         (T6)
```

The sealed registry does not exhibit every response in `(T6)` as an element
of `I_N`.  Thus `(T5)` is a candidate subspace, not proved consumer coverage.

### 5.3 Image topology

Even if a continuous `S_N` were granted, automatic quotient descent only
forms `Sbar_N`.  The physical image condition still requires

```text
D_N^Loc:=im(Kraw_N)=im(Sbar_N),

topology on D_N^Loc from E_N/ker(Kraw_N)
 =subspace topology inherited from D_N^ker.                    (T7)
```

A continuous linear map need not be open onto its image.  No finite-dimensional
or closed-range hypothesis applies to the declared general `D_N^ker`, so
`(T7)` does not follow from continuity.

```text
ATTACK_T = STOPPED
formed T.0 = Q_N^core and physical inclusion jmath_N
gap T.1 = inherited-topology continuity in (T2)
gap T.2 = quotient/inherited topology equality (T3)
gap T.3 = Rel_N=ker(qtilde_N) for beta_N
gap T.4 = source-scope coverage (T6)
gap T.5 = DLoc image-topology equality (T7).                    (T8)
```

## 6. Attack A — addressed quotient and extension

### 6.1 Actual addressed data present

The sealed addressed datum is the whole orbit

```text
AddrOrb_N=[a]:={a,a^-},
mu(a^-)=-mu(a),
no positive representative selected.                           (A1)
```

At the actual reciprocal address `a_RL`, the unaddressed response relabelling

```text
O_w^resp:=O_R5,w,
iota_w^resp=id                                                 (A2)
```

does not construct the addressed restriction

```text
Res_(w,a)^resp:O_w^resp->O_a^resp.                              (A3)
```

### 6.2 [PART-PROVABLE] The conditional quotient factor

If actual continuous linear maps `Res` and `S` existed, `Res` carried the
declared quotient topology on its image, and the kernel inclusion held,

```text
QuotResCert_(w,a):
 Res_(w,a)^resp is quotient onto im(Res_(w,a)^resp),

ker(Res_(w,a)^resp) subset ker(S_(w->N,K)),                    (A4)
```

then the factor is well defined by

```text
Ker_(a,K)^im(Res(Y)):=S(Y).                                    (A5)
```

Indeed,

```text
Res(Y)=Res(Y')
 =>Y-Y' in ker(Res)
 =>Y-Y' in ker(S)
 =>S(Y)=S(Y')
 =>Ker^im(Res(Y))=Ker^im(Res(Y')).                             (A6)
```

Equations `(A4)`--`(A6)` give algebraic well-definedness and linearity; the
displayed `QuotResCert` plus continuity of `S` gives continuity of `Ker^im`
for the quotient image topology.  This conditional quotient universal
property supplies none of `(A3)`, `S`, `QuotResCert`, or `(A4)` as actual
members.

### 6.3 Full addressed extension stops

The consumer requires either a directly physically agreed

```text
Ker_(a,K):O_a^resp->D_N^Loc                                   (A7)
```

or the longer witness

```text
Ker_(a,K)^ker:O_a^resp->D_N^ker,
Ker_(a,K)^ker|_(im Res)=Ker_(a,K)^im,
im(Ker_(a,K)^ker) subset D_N^Loc.                              (A8)
```

No sealed complement of `im(Res)` in `O_a^resp`, injectivity property of
`D_N^ker`, or continuous extension theorem produces `(A8)`.  Hahn--Banach
extends scalar functionals, not arbitrary vector-valued maps into an
unbuilt target with a prescribed image.  Choosing a complement would add
unratified structure.

The built orbit `(A1)` also does not provide the conditional sign-odd maps
or square

```text
r_N^D Ker_(a,K)|_(O_(a,K)^odd)
 =Ker_(a^-,K^-) r_(a,K)^O.                                    (A9)
```

A bare zero `Ker` is rejected because it supplies no physical agreement with
the stage source.

```text
ATTACK_A = STOPPED
formed A.0 = carried two-address orbit (A1)
formed A.1 = conditional quotient proof (A4)--(A6)
gap A.1 = ACTUAL_RES/SUPPORT/QUOTIENT_MEMBER
gap A.2 = ACTUAL_S_AND_KERNEL_INCLUSION (A4)
gap A.3 = FULL_CONTINUOUS_EXTENSION (A8)
gap A.4 = PHYSICAL_AGREEMENT
gap A.5 = ADDRESSED_REVERSAL_MAPS/SQUARE (A9).                  (A10)
```

## 7. Attack G — generator bases and finite-pasting carriage

### 7.1 Reciprocal generator

On the actual reciprocal generator `u:N_RL->M_RL`, represented-kernel stock
reaches the Q-408 square

```text
eta_u^K Kernbar_N^cyc(H)
 =Kernbar_M^cyc(j_u^prof H)                                   (G1)
```

on its proved scope.  ExtSrc would additionally require

```text
tau_u^src qtilde_N=qtilde_M tau_u^E,
tau_u^prof Q_N^quot=Q_M^quot tau_u^E,

tau_u^D iota_N^Q408=iota_M^Q408 eta_u^K,

tau_u^D Sch_N=Sch_M tau_u^Op,

tau_u^D S_N=S_M tau_u^src,

tau_u^Loc Ker_(a,K)=Ker_(u.a,u.K) tau_u^a.                    (G2)
```

The target `D^ker`, its inclusions, `S`, `D^Loc`, `Ker`, and the displayed
ExtSrc transports are unformed.  Thus `(G1)` cannot be promoted to `(G2)`.

### 7.2 Nontrivial q=2 refinement

For `r_nm:N_n->N_m`, Q-408 supplies the actual finite quotient-test transport,
and Section 4 proves the named pure-cycle relation is preserved.  ExtSrc
still requires

```text
tau_rnm^src qtilde_n=qtilde_m tau_rnm^E,
tau_rnm^prof Q_n^quot=Q_m^quot tau_rnm^E,

tau_rnm^D iota_n^Q408=iota_m^Q408 eta_rnm^K,

tau_rnm^D Sch_n=Sch_m tau_rnm^Op,

tau_rnm^D S_n=S_m tau_rnm^src,

tau_rnm^Loc Ker_(a,K)
 =Ker_(r_nm.a,r_nm.K) tau_rnm^a.                              (G3)
```

`P_alg,rnm=id` on the identified test quotient is not a `tau^D` or
`tau^Loc` member and does not establish `(G3)`.

### 7.3 Composition, presented relations, and cycle creation

Only after the actual base maps exist may one prove

```text
tau_id^Loc=id,
tau_(g compose f)^Loc=tau_g^Loc tau_f^Loc,                     (G4)

g_1 f_1=g_2 f_2
 =>tau_g1^Loc tau_f1^Loc
   =tau_(g_1 f_1)^Loc
   =tau_(g_2 f_2)^Loc
   =tau_g2^Loc tau_f2^Loc.                                    (G5)
```

Identity and composition do not bootstrap absent nonidentity bases.  The
named relation pass in Section 4 is one regression, not a proof that every
presented relation is respected.

For a cycle-creating `h:N->M`, the lawful orientation remains downward on
the old image:

```text
rho_h^D S_M|_(O_R5,M^(src,old))=S_N rho_h^src,

rho_h^Loc Ker_(a_M^old,K_M)
 =Ker_(a_N,rho_h^K(K_M)) rho_h^a.                              (G6)
```

The finite old-cylinder restriction in `KER` does not construct the ExtSrc
maps in `(G6)`.  No upward target-new-cycle lift is introduced.

```text
ATTACK_G = STOPPED
formed G.0 = represented Q-408 reciprocal square (G1)
formed G.1 = finite q=2 quotient transport and named relation regression
gap G.1 = RECIPROCAL_EXTSRC_BASES (G2)
gap G.2 = Q2_EXTSRC_BASES (G3)
gap G.3 = FUNCTOR_EXTENSION/RELATION_RESPECT (G4)--(G5)
gap G.4 = CYCLE_CREATING_OLD_IMAGE_EXTSRC_MEMBERS (G6).         (G7)
```

## 8. Registrar-suggested attack — the `A_N` metric section

### 8.1 Determination question and theorem requirements

The suggestion proposes the Hilbert-space formula

```text
s_N^perp
 :=(A_N|_((ker A_N)^perp))^(-1)
 :(K_N^fin)^*->T_N.                                            (M1)
```

For the suggested canonical Hilbert/Moore--Penrose derivation, a sufficient
standard package is

```text
(T_N,h_T) a positive complete metric carrier;
A_N bounded for h_T and the target metric;
ker(A_N) h_T-closed;
T_N=ker(A_N) direct-sum (ker A_N)^perp_hT;
A_N|_((ker A_N)^perp_hT) onto.                                (M2)
```

Equivalently, a Moore--Penrose expression would need a Hilbert adjoint:

```text
s_N^perp
 =A_N^(star,h_T)
   (A_N A_N^(star,h_T))^(-1).                                 (M3)
```

The fact that `A_N` is algebraically onto does not by itself supply this
package or `(M3)`.  A weaker pre-Hilbert orthogonal splitting could also
produce `(M1)`, but sealed stock supplies no inner product or splitting on
`T_N` at all.

### 8.2 [PROVABLE] DoR-019 metric scope

DoR-019 supplies

```text
g_K,N on K_N^fin,
R_K,N^fin:K_N^fin->(K_N^fin)^*,

g_C,N on C_N=Q_N^lin isomorphic_to (K_N^fin)^*,
R_C,N:C_N->C_N^*.                                               (M4)
```

It does **not** supply

```text
h_T on T_N=Omega_c^1(M_N;iR),
R_T,N:T_N->T_N^*,
A_N^(star,h_T):(K_N^fin)^*->T_N.                               (M5)
```

`KER` types `T_N` as the nuclear compactly supported smooth test space; its
transpose sends a cycle to the path-supported distribution

```text
A_N':K_N^fin->T_N^*,
A_N'(c)=J_(N,c),                                                (M6)
```

not to a smooth compactly supported one-form.  `R_K,N` cannot be used as a
missing `R_T,N`; DoR-019 expressly says its Riesz maps act inside their own
sectors and license no new physical cross-sector map.

### 8.3 [PROVABLE] Pullback semiform gives no complement

The strongest metric pulled back from the sealed target is

```text
h_A,N(a,b)
 :=g_(K_N^fin)^*(A_N a,A_N b).                                 (M7)
```

Its radical is exactly

```text
rad(h_A,N)=ker(A_N).                                           (M8)
```

For every `v in T_N` and `k in ker(A_N)`,

```text
h_A,N(v,k)
 =g_(K_N^fin)^*(A_Nv,A_Nk)
 =g_(K_N^fin)^*(A_Nv,0)
 =0.                                                           (M9)
```

Therefore

```text
(ker A_N)^(perp,h_A)=T_N,                                     (M10)
```

not a complementary subspace.  Moreover, for every `k in ker(A_N)`,

```text
h_A,N(a+k,a+k)
 =g_(K_N^fin)^*(A_Na,A_Na)
 =h_A,N(a,a).                                                  (M11)
```

All representatives of a quotient class have the same pullback seminorm.
Thus “choose the minimal-norm representative” selects none of them.

The positive quotient result is exactly `(Q2)`:

```text
METRIC_SECTION_QUOTIENT
 =sbar_N=Abar_N^(-1):(K_N^fin)^*->Tbar_N
 =FORMED.                                                       (M12)
```

It is not a right inverse into `T_N`.

### 8.4 Underdetermination regression

Algebraically, if one section `s_0` is chosen, every

```text
s_0+kappa,
kappa in Hom((K_N^fin)^*,ker(A_N))                             (M13)
```

is another section.  This does not prove that the section family is empty;
it proves that onto alone does not identify a canonical member.

The dependence on an unratified source metric is visible in the model

```text
T=Y direct-sum Z,
A(y,z)=y,

h_L((y,z),(y',z'))
 :=<y,y'>+<z-Ly,z'-Ly>,                                       (M14)
```

where `L:Y->Z` is arbitrary.  Every `h_L` is positive, but

```text
(ker A)^(perp,h_L)=graph(L),
s_L(y)=(y,Ly).                                                  (M15)
```

Different lawful metrics select different sections.  DoR-019 fixes no
`h_L`-analogue on `T_N`, so it fixes no `s_L`-analogue.

Even importing a naive local `L^2` metric would not be a sealed repair.  In a
tubular coordinate `(s,y)` around an actual surface path, choose smooth
compactly supported `phi,psi` with

```text
integral phi(s) ds=1,
psi(0)=1,

a_epsilon
 :=lambda phi(s) psi(y/epsilon) ds,
lambda!=0 symbolic.                                             (M15a)
```

Then, for the path at `y=0` and a fixed local metric-equivalent density,

```text
integral_gamma a_epsilon
 =lambda integral phi(s)psi(0) ds
 =lambda,

||a_epsilon||_L2^2
 <=C |lambda|^2 epsilon
 ->0 as epsilon->0.                                             (M15b)
```

Thus a nonzero fixed path period has `L^2`-norm infimum zero but no zero-norm
representative.  Path evaluation is not continuous for that topology, so it
does not supply the proposed minimizer.  A Sobolev repair would author an
order, completion, boundary/gauge data, support behavior, and transport laws;
none is ratified here.

### 8.5 Even a hypothetical section does not form the bridge

Grant a candidate section temporarily.  A source bridge would still need

```text
lambda_N:O_R5,N^src->(K_N^fin)^*,

b_N:=s_N^perp lambda_N:O_R5,N^src->T_N,                         (M16)
```

and the landing would still need

```text
e_N:(K_N^fin)^*->D_N^ker.                                      (M17)
```

Thus a section forms neither adjacent missing arrow.  If a
representative-valued transport were separately supplied with

```text
P_f^T:T_N->T_M,
A_M P_f^T=C_f A_N,                                               (M18a)
```

then its comparison with sections would carry a kernel correction:

```text
s_M C_f-P_f^T s_N
 : (K_N^fin)^*->ker(A_M),

A_M(s_M C_f-P_f^T s_N)
 =C_f-C_f A_N s_N
 =C_f-C_f
 =0.                                                           (M18)
```

No sealed `P_f^T` satisfying `(M18a)` or kernel-cocycle law is provided.  Quotient
covariance `(Q10)` does not imply representative covariance.  Cycle creation
continues to have only downward old-image carriage.

```text
METRIC_SECTION_ATTACK = FAILED
formed M.0 = quotient inverse sbar_N and scoped covariance (Q10)
reason M.1 = T_N is not a DoR-019 metric carrier
reason M.2 = no R_T or Hilbert adjoint exists in sealed stock
reason M.3 = induced h_A is degenerate and gives (ker A)^perp=T_N
reason M.4 = quotient inverse is not a representative lift
reason M.5 = hypothetical section forms neither lambda_N/b_N nor e_N
reason M.6 = representative covariance/support is unproved.                  (M19)
```

This is failure of the proposed derivation, not nonexistence of all abstract
linear sections.

## 9. Standing cross-sector-unit declaration

Let the unselected unit torsors of a future candidate be

```text
U_src,N, U_T,N, U_K,N, U_J,N, U_Dker,N, U_biloc,N,

U_Kstar,N:=U_K,N^(-1).                                        (U1)
```

No torsor representative or numerical conversion is selected.  The sealed
analysis and quotient arrows carry

```text
[A_N]=[Abar_N]
 =U_Kstar,N U_T,N^(-1),

[sbar_N]
 =U_T,N U_Kstar,N^(-1)
 =U_T,N U_K,N.                                                  (U2)
```

Any source candidate must declare

```text
[lambda_N]=U_Kstar,N U_src,N^(-1),
[bbar_N]=[sbar_N lambda_N]
 =U_T,N U_src,N^(-1),

[b_N]=U_T,N U_src,N^(-1).                                     (U3)
```

For the current landing route,

```text
[I_K,N]=U_J,N U_K,N^(-1),
[(R_K,N^fin)^(-1)]=U_K,N U_Kstar,N^(-1),
[ell_N^(cyc->ker)]=U_Dker,N U_J,N^(-1),

[e_N^(2)]
 =[ell_N][I_K,N][(R_K,N^fin)^(-1)]
 =U_Dker,N U_Kstar,N^(-1).                                    (U4)
```

For the bilocal route,

```text
[i_cyl,N]=U_Dker,N U_biloc,N^(-1).                             (U5)
```

The full candidate has

```text
[e_N Abar_N bbar_N]
 =U_Dker,N U_Kstar,N^(-1)
  U_Kstar,N U_T,N^(-1)
  U_T,N U_src,N^(-1)
 =U_Dker,N U_src,N^(-1)
 =:[S_N].                                                       (U6)
```

The direct sum in the physical source equality additionally requires

```text
[Sch_N(M_N^loc)]
 =[iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C))]
 =U_Dker,N.                                                     (U7)
```

Equations `(U1)`--`(U7)` declare every crossed sector.  They create no
arrow, set no conversion to `1`, and tune no unit to a downstream result.
In particular, `R_K` remains a same-sector duality and cannot absorb
`lambda_N`, `ell_N`, or `i_cyl,N`.

## 10. Consequence board and timebox checkpoint

### 10.1 Per-attack board

| Attack | Strongest lawful result | Exact stop |
|---|---|---|
| quotient domain | `sbar_N=Abar_N^(-1)` and scoped `(Q10)` | no `lambda_N`, hence no `bbar_N`; source covariance absent |
| current landing | finite Riesz/current prefix; finite `sigma_cyl` | no `ell_N`, `i_cyl,N`, or completion certificate |
| mixed relation | named reciprocal and q=2 `qtilde/Q` zero classes; conditional future-image regressions | common target unformed; general mixed relation and closure theorem absent |
| topology/scope | literal corestriction and conditional descent | no `kappa` topology equality, coverage, or `DLoc` image-topology certificate |
| addressed factor | orbit plus conditional quotient argument | no actual `Res/S/Ker` extension or reversal square |
| generator bases | represented Q-408 and finite quotient-test sub-squares | no reciprocal/q=2 ExtSrc bases, functor extension, or old-image ExtSrc `rho` |
| metric section | quotient inverse only | no metric/adjoint on `T_N`; pulled-back form degenerate; adjacent bridges remain missing |

Consequently,

```text
Gamma^H Joint I = STOPPED at addressed Ker/DLoc;
HOL common prefix = STOPPED at the same Joint-A arrow;
S22/S22a = specified but not inhabited;
A9 ExtSrc gate = open, not supplied by A9 adoption;
R9 = not yet executable;
both A7 E_C branches = retained, neither selected.             (C10)
```

No H-only or HOL-only tail is used to repair the common source.

### 10.2 Round-2 checkpoint recommendation

```text
EXTSRC TIMEBOX = ROUND 2 OF 4 COMPLETE
ROUNDS SPENT = 2
TIMEBOX EXPIRED = false
ROUND-2 OUTCOME = HONEST STOP
CARRY-1 EXPLICIT INCOMPLETENESS = retained and available.       (C11)
```

Because two independent sealed-stock rounds now stop at the same common
carrier/landing boundary and the registrar's strongest suggested derivation
has been exhausted, the recommendation is:

```text
CHECKPOINT_RECOMMENDATION
 =return to the principal before Round 3;

principal options
 =(carry explicit incompleteness now,
   or authorize Round 3 to test an AUTHORABLE common-kernel/equalizer family).
                                                                    (C12)
```

If Round 3 is expressly continued, its first dependency must be one family of
Hausdorff carriers `D_N^ker` with named embeddings of both the local `L_T`
image and the finite bilocal/current image, together with:

```text
uniform seminorm/completion estimates;
a failure-capable mixed-relation void condition;
inherited/source/image topology certificates;
addressed Res/Ker members;
actual reciprocal/refinement/old-image carriage.               (C13)
```

It should not retry the metric section: quotient inversion is already
maximal on sealed metric stock.  `(C13)` is a checkpoint option, not an
adoption, plan/tracker mutation, or member binding.

## 11. Mandatory SURFACE ANCHOR

### 11.1 Named actual-object board

A `PASS` is counted only where the equality is displayed on a named actual
stage, relation, address, arrow, or certified scope.

| Actual anchor | Required display | Round-2 result |
|---|---|---|
| reciprocal stage `RL` | `A_RL`, `Abar_RL`, and its quotient inverse | `PASS` on the quotient test carrier only |
| reciprocal rank-one pure-cycle relation | `(R4)`--`(R5)` | `PASS` for the actual `qtilde/Q` zero class; its candidate `Dker` image is `PART-PROVABLE` after common-target formation |
| actual Q-408 reciprocal kernel | landing in the same `D_RL^ker` as `Sch_RL(M_RL^loc)` | `STOPPED`; kernel exists, common landing does not |
| actual `L_T,RL` generator | source agreement in `(S2)` | `STOPPED`; no `lambda_RL`, `Sch_RL`, or `S_RL` member |
| actual q=2 refinement | relation image `(R6)`--`(R7)` and quotient covariance `(Q10)` | `PASS` for the finite/quotient zero-class and quotient-transport cells; candidate `Dker` image is conditional and full ExtSrc base `(G3)` is stopped |
| actual address `a_RL` and `a_RL^-` | `Res`, kernel inclusion, extension, and conditional reversal square | orbit `PASS`; maps/squares `STOPPED` |
| reciprocal generator `u` | all displayed ExtSrc base equalities `(G2)` | represented Q-408 square only; ExtSrc base `STOPPED` |
| cycle-creating arrow | downward old-image equations `(G6)` | typed only; no upward lift |
| generated finite-pasting family | relation-respecting induction `(G4)`--`(G5)` | `STOPPED` at missing bases/general relation theorem |

The structural zero in the pure-cycle row is not a numeric evaluation and
does not execute any end test.

### 11.2 Geometry versus rails

**Physical geometry already present:** actual paths `gamma_e`, currents
`J_(N,c)`, finite cycle carrier and Riesz metric, compact-test space `T_N`,
onto `A_N`, quotient `Tbar_N`, `Abar_N`, reciprocal rank-one relation, q=2
transport, Q-408 kernels, the addressed sign orbit, and actual
reciprocal/refinement/cycle-creating arrows.

**Organizing rails awaiting members:** `lambda_N/bbar_N`, the optional
representative section into `T_N` tested only by the `A_N` attack,
`ell_N/i_cyl,N`, common `D_N^ker`, `Sch_N`,
`iota_N^Q408`, `S_N/Sbar_N`, `D_N^Loc`, inherited/image topology
certificates, addressed `Res/Ker`, and their nonidentity transports.

The formed quotient inverse is genuine geometry at its exact quotient type.
It is not renamed a test-form lift.

### 11.3 R9 quantification check

The repaired falsifier remains

```text
for every common physical cell c
on which both typed period-return constructions are formed,

Bbar^H(c)=Bbar^Hol(c).                                         (R9-1)
```

Round 2 forms only finite/quotient pieces of the common prefix; it forms
neither full route return.  Therefore

```text
R9_STATUS = UNEXECUTABLE/PENDING
R9_COMMON_PHYSICAL_CELL = none yet
R9_PASS = not asserted
R9_FAILURE = not asserted
R9_VACUOUS_TRUTH = not asserted.                                (R9-2)
```

No quantifier over `Triv_[a]`, a route-family product, or an independently
chosen orientation is introduced.

## 12. Mandatory dependency re-audit

Every argument touching a Round-2 positive piece or stop was re-walked.

| Root datum or clause | Re-audited dependents | Result |
|---|---|---|
| `sbar_N=Abar_N^(-1)` | `bbar_N`, test representatives, covariance | quotient inverse retained; no source map or representative lift inferred |
| scoped quotient covariance `(Q10)` | reciprocal/refinement bases and global closure | kept on actual finite/`Ref_path` scope; no globalization |
| missing `lambda_N` | quotient bridge and metric-section salvage | both routes stop before `bbar_N`; no contraction/member selected |
| current prefix `(C1)` | `e_N`, bilocal realization, `S_N^(2)` | one-current arity retained; no quadratic/frozen-leg shortcut |
| `sigma_cyl,N` | `i_cyl,N`, completion, local `L_T` image | finite bilocal landing only; no codomain identification |
| pure-cycle zero-class display `(R4)`--`(R5)` | general mixed relation and finite-pasting | `qtilde/Q` pass only; `Dker` image conditional; no generation claim |
| q=2 zero-class display `(R6)`--`(R7)` | full quotient map and ExtSrc base | finite/quotient class only; `Dker` image conditional; general debt retained |
| topology equivalence `(T2)` | `beta_N`, source scope, automatic descent | all premises kept separate; descent not counted as source extension |
| addressed quotient proof `(A6)` | full-domain `Ker`, physical agreement | conditional factor only; no Hahn--Banach/complement shortcut |
| Q-408 reciprocal square `(G1)` | `tau^D/iota`, `tau^Loc/Ker` | represented kernel square not relabelled an ExtSrc square |
| DoR-019 `K/C` metric | `T_N` metric, section, units | scope not enlarged; `R_K` stays same-sector |
| pullback semiform `(M7)` | orthogonal complement/minimizer | radical displayed; no representative selected |
| hypothetical section `(M16)` | `b_N`, `e_N`, support/covariance | adjacent gaps and kernel-cocycle debt remain |
| unit declarations `(U1)`--`(U7)` | every proposed cross-sector composite | types visible; no conversion or normalization created |
| both A7 branches | downstream route families | both retained; none selected or evaluated |
| checkpoint `(C12)` | remaining timebox | recommendation only; no register, plan, or tracker action |
| R9 `(R9-1)` | source construction | pending and unused; no tuning |

Permanent regressions rerun:

1. an onto map was not read as a canonical section;
2. a quotient inverse was not called a representative lift;
3. a degenerate pullback semiform was not called a source metric;
4. a one-current was not called a bilocal kernel;
5. one named pure-cycle relation was not called a general relation theorem;
6. continuity was not called quotient/inherited topology equality;
7. an addressed orbit was not called an addressed response map;
8. a quotient-test transport was not called an ExtSrc transport;
9. a downward old-image restriction was not reversed into an upward lift;
10. an explicit unit class was not called an arrow member.

## 13. Battery

### 13.1 F_PLDEC and false-anchor regression

The complete dependency direction was

```text
actual paths/currents and supports
 + raw qtilde/Q quotient data
 + Q-408 finite tests, kernels, and represented transports
 + DoR-019 K/C metrics and unit classes
 + addressed orbit
 -> attempted ExtSrc source/landing/topology/address/transport family. (B1)
```

The following occur in no defining line:

```text
reader_f, chi, reader T, desired coefficient,
pi_Mx compose Loc compose Kernbar compose Q = 1,
seed polarity or nonvanishing,
Xi/HOL return, R9 equality,
threshold, fixed point, end test,
numeric value, measured constant.                               (B2)
```

Neither the false anchor nor its shadow supplies `lambda_N`, a metric on
`T_N`, a current/kernel landing, or a mixed relation.

### 13.2 Anti-tuning and no-selection ledger

| Attack | Check | Result |
|---|---|---|
| cross-sector unit or normalization tuned to a desired return | all classes carried symbolically in `(U1)`--`(U7)` | clean |
| implicit dimensionless section/landing | `sbar`, `lambda`, `ell`, `i_cyl` each typed separately | clean |
| metric chosen to obtain a preferred section | no `h_T` authored; family `(M14)` exposes dependence | clean |
| quotient representative selected | only `Tbar_N` class formed | clean |
| reciprocal orientation selected | both addresses retained; `(Q7)` used as an obstruction | clean |
| profile contracted with a selected cycle | rejected at `(Q6)` | clean |
| one-current tensor leg frozen | rejected at `(C4)` | clean |
| bilocal distribution identified with `D_N^ker` | rejected at `(C5)` | clean |
| pure-cycle relation used to delete mixed relations | rejected at `(R8)` | clean |
| inherited and quotient topologies identified by notation | rejected at `(T2)`--`(T3)` | clean |
| source scope shrunk to evade consumers | coverage remains failure-capable in `(T6)` | clean |
| addressed complement selected | rejected before `(A8)` | clean |
| quotient transport globalized | exact Q-408 scope retained | clean |
| cycle-creating upward lift invented | only `(G6)` downward old image retained | clean |
| A7 branch selected | both branches retained | clean |
| reader/fixed-point/end-test/numeric/measured data consumed | none | clean |

The cross-sector-unit row is failure-capable: any future candidate that
cannot display the full factorization `(U6)` is outside the family.

### 13.3 Fence and obstruction board

```text
FENCE_BLOCKED_STRUCTURAL_RESULT = false

TYPE-U GAPS AFTER ROUND 2 =
  source-to-cycle-dual lambda_N / bbar_N;
  current/bilocal-to-Dker landing and completion;
  common Dker/Sch/iota and general mixed-relation theorem;
  inherited/source/image topology certificates;
  addressed Res/S/Ker extension and reversal square;
  reciprocal/refinement/functor/old-image ExtSrc members;
  T_N metric/adjoint needed by the suggested representative section.

ABSTRACT_EXTSRC_FAMILY_EMPTY = not proved.                       (B3)
```

### 13.4 Self verb audit

| Verb used | Supporting display | Audit |
|---|---|---|
| `formed` for quotient inverse | `(Q1)`--`(Q2)` and `(M12)` | clean; codomain is `Tbar_N`, not `T_N` |
| `formed` for current prefix | `(C1)` | clean; stops at `J_cyc,N` |
| `passes` for named `qtilde/Q` zero classes | `(R4)`, `(R6)`--`(R7)` | clean; candidate `Dker` images remain conditional |
| `covariant` for quotient inverse | `(Q8)`--`(Q10)` | clean; finite/`Ref_path` scope stated |
| `automatic` for quotient factor | `(A4)`--`(A6)` | clean; premises explicit |
| `failed` for metric-section attack | `(M2)`--`(M19)` | clean; derivation failure, not abstract nonexistence |
| `stopped` for ExtSrc | each exact gap in `(Q12)`, `(C9)`, `(R10)`, `(T8)`, `(A10)`, `(G7)` | clean |
| `declared` for units | `(U1)`--`(U7)` | clean; no scale or arrow member supplied |
| `recommends` checkpoint | `(C11)`--`(C13)` | clean; no principal decision presumed |
| `not proved empty` | `(B3)` and section conclusions | clean; Type-U is not a no-go theorem |
| `pending` for R9 | `(R9-1)`--`(R9-2)` | clean; no vacuous execution claimed |

Every status verb is matched to a display above it.  No `constructs`,
`inhabits`, `proves`, `closes`, or `selects` verb exceeds its stated scope.

EXTSRC = STOPPED (+per-attack gaps: Q=no source-to-cycle-dual lambda_N/bbar_N; C=no current/bilocal-to-Dker landing or completion; R=common target/general mixed relation/closure theorem absent despite the named qtilde/Q zero-class regressions; T=no inherited/source/image topology certificates; A=no addressed Res/S/Ker extension or reversal square; G=no actual ExtSrc bases/functor/old-image members; +checkpoint recommendation after Round 2, with CARRY-1 retained)
METRIC_SECTION_ATTACK = FAILED (+reason: DoR-019 metrizes K/C; pullback through Abar_N metrizes only the quotient Tbar_N, not T_N=Omega_c^1; the pulled-back semiform on T_N has radical ker(A_N), so only Abar_N^(-1) is canonical and neither b_N nor e_N is formed)
CROSS_SECTOR_UNIT = declared
VERB_AUDIT_SELF = CLEAN
