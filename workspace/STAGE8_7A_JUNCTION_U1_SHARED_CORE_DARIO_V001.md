# STAGE 8 / 7A / STEP 6 / JUNCTION ARC — U1: THE SHARED-CORE OBLIGATION

**Lane:** Dario (Opus 5, cross-family from the Codex build lanes)
**Date:** 2026-08-08
**Scope:** PASTE 713 only — Y1 derive the shared core, Y2 type the result, Y3 ledger
**Custody:** archive-side read and write; the registrar owns any mirror
**Governing:** the frozen OBS-22 preregistration `9f0d12b4…`, whose criteria are not
altered here
**Charge:** re-derive, not trust. No compatibility criterion is invented.

## Lead determination

```text
U1 = PART-DERIVED
  the shared-core OBLIGATION derives from sealed non-Dario sources and is displayed;
  the joint-discharge TEST does not close, for two independent reasons (§3.3).

SHARED CORE      = the cross-sector-unit declaration on one formed record cell
BINDING RULE     = DoR-020-A2 frozen clause 8 -> the sequenced-program decision,
                   which names (JD-3) EXPLICITLY, per DoR-019's
                   NO_IMPLICIT_CROSS_SECTOR_UNIT, R4-ONLY routing
JD-3 OBLIGATION  = presentation independence (the only one of the four the
                   normalization can touch; the other three are provably blind to it)
ADMISSIBILITY    = §3 Requirement I + fence F'-5 + falsifier F1
MISSING CARRIER  = the COMMON-CELL QUANTIFIER for this pair: no sealed text
                   identifies (JD-3)'s named oriented k-cell with the admissibility
                   spec's "record cell"; the register types that identification an
                   UNCHECKED HYPOTHESIS and its commissioned determination returned
                   OVERLAPPING, not IDENTICAL
SECOND BLOCKER   = Q-126's sealed census: NO junction on the record cell is
                   simultaneously DERIVED and beta-SENSITIVE, so no presently derived
                   junction can run the test non-vacuously
SMALLEST ADDITION= one R9-shaped sealed line (displayed at §3.4), which states the
                   test without making it runnable
```

The corpus does not merely fail to state U1's condition. It **forbids me from
assuming the step that would close it** — `R9`: *"no equality assumed before a common
cell."* That is a better answer than a bridged one, and it is the answer.

---

## 0. Preflight

### 0.1 Lane guard, relay, output

[PROVABLE] The relay header names **DARIO**. The lane guard is satisfied; no
misdelivery report is due.

[PROVABLE] The relay was read only after its sidecar verified:

```text
relay_inbox/RELAY_PASTE_713_JUNCTION_ARC_U1_DARIO_V001.md
  945f38bb69464bf331e7abf069ac2c2c23061642359031ee6cd44a2f44d50a47   shasum -c OK
```

[PROVABLE] `STAGE8_7A_JUNCTION_U1_SHARED_CORE_DARIO_V001.md` and its seal sidecar
were probed before the write and returned ABSENT.

### 0.2 Pins verified before reading

[PROVABLE] Every digest the relay states was recomputed and matched before the
corresponding bytes were consumed:

| Ref | Artifact | SHA-256 |
|---|---|---|
| P05 | `STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md` | `9f0d12b4556427eb965bcd9c869f645b984009fdbcc0d1d19a2c7216d31c51f1` |
| P07 | `STAGE8_TASK6_JII_BETA_IDENTIFICATION_DARIO_V001.md` | `8e31df94fd2076cdb7803b0c516457fee577538f290f2ace3679cf1f4cfa4a76` |
| — | `STAGE8_7A_HANDOFF_PACKAGE_V001.md` | `a0a7e1d1…` (sidecar OK) |
| J-II | `STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md` | `fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70` |
| ADM | `STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md` | `3c008ecccc2b01ac72c8583e0fdea6601a12db7e2755edaa25bd8174f314525a` |

[PROVABLE] Five further primaries were located during the derivation, seals verified
where a sidecar exists:

```text
SEQ   supervision/DECISION_SEQUENCED_PROGRAM_2026-08-06.md
      eaeffd37982cc0063a42095a6880c34d6a95a95b7240cd02b61e485f4b17482a
A2    supervision/DOR_020_A2_COMPLETED_EXISTENCE_ADOPTED_2026-08-06.md
      5c945cae9ae8afa60080ee08d569324d350d6717bf2a3058460bc00451b86f8e
019   supervision/DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md
      6ab72b0c…  (sidecar OK)
SEAM2 workspace/STAGE8_TASK5_COMMON_SEAMS_LANE3_V002.md
      2525096ba06973b70064d6b9f9578470e0afca9c48e2cca6cf0f5c1194d12c52
REG   supervision/QUESTIONS_SETTLED_REGISTER_V001.md
      6bf7c3bdf28b3b856c49...  (1,254,120 B; read as corpus only -- no register
      action was taken, and none is authorized to this lane)
```

[YOURS] One pointer correction, recorded and not charged: the relay cites the
handoff at **§X5**. The handoff has no §X5; the matching content is **§5**
(§5.1 day-one object, §5.2 U1 joint landing). The content matches the relay's
description exactly, so I read §5 and proceeded rather than stopping on a label.

### 0.3 Recusal — the reason this derivation avoided my own artifacts

[YOURS] This is the load-bearing custody fact of this relay, so it goes first.

`P05` (the frozen preregistration) and `P07` (the identification determination) are
**this lane's own artifacts**, and they are the two documents that state U1's
problem. A derivation of U1 that grounded on them would be this lane verifying its
own construction.

So I derived the shared core from **non-Dario primaries only** — the sequenced-program
decision, DoR-020-A2, DoR-019, the Lane-3 J-II realization, the Lane-3 common seams,
and the admissibility spec — and used `P07` afterwards only to check whether the
independent result *agrees* with what this lane previously claimed. It does (§2.6).
That direction is lawful; the reverse would not have been.

### 0.4 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No junction map was evaluated. No numeric was produced. No member was bound, no
fixed point executed, no end test run, no measured constant consulted. The frozen
preregistration was not altered, and no criterion of it was restated as changed. No
register, plan, tracker, git, commit or push action was taken.

---

## 1. THE TWO OBJECTS, AT THEIR PRIMARY SOURCES

### 1.1 Object (A) — the Ward-symbol map and its four obligations

[PROVABLE] `J-II` `fdf20bd4…[45349,45957)`, verbatim:

```text
Phitilde_N(s)(delta_e)
 :=incidence/integration/evaluation of s on the named k-cell e,   (JD-3)

Phitilde_N(s)(sum_e a_e delta_e)
 :=sum_e a_e Phitilde_N(s)(delta_e).                    (JD-3a)
```

> "and must prove orientation covariance, presentation independence, continuity, and
> `Ctc_N^sym subset ker(Phitilde_N)`; the latter two clauses make `Phi_N` and
> `iota_N^H` presentation-independent quotient maps."

The four obligations, numbered here for reference only:

```text
O1 orientation covariance
O2 presentation independence
O3 continuity
O4 Ctc_N^sym subset ker(Phitilde_N)     (contact-kernel annihilation)
```

The map is indexed by a **named oriented k-cell `e`**, through its basis cochain
`delta_e in C_N^k`.

### 1.2 Object (B) — the length normalization

[PROVABLE] `ADM` `3c008ecc…[2934,3131)`:

```text
The target is a derived cross-sector metric rule on one record cell:

internal/projective geometry of the record degree on the cell
  -> dimensional spacetime length normalization for the same cell
```

It determines the dimensionless `beta` in `R = beta c Delta tau, beta > 0`
(modulus gate `52664ddb…`, L54–61). The spec's own §3 demand,
`3c008ecc…[6182,6516)`:

```text
Requirement I:  an internal/projective record-cell requirement that determines how
                a dimensionless FS/phase quantity couples to a length normalization.
Requirement II: an external Lorentzian/dynamical requirement on the same cell that
                fixes or stabilizes the same length normalization.
Pass condition: the two requirements have exactly one common positive beta, and the
                uniqueness proof does not use any downstream value.
```

The object is indexed by **one record cell**.

---

## 2. Y1 — THE DERIVATION

### 2.1 Which of the four obligations can the normalization touch at all?

[PROVABLE] `beta > 0` is a positive scalar. Test each obligation against
multiplication of the cell-indexed pairing by a positive scalar:

| # | Obligation | Effect of a positive rescaling | Binds? |
|---|---|---|---|
| O1 | orientation covariance | `Phi(delta_{-e}) = -Phi(delta_e)`; a positive factor commutes with the sign, so the identity is preserved for every `beta > 0` | **no** |
| O3 | continuity | multiplication by a fixed positive constant is continuous in any topology making the pairing continuous | **no**, if `beta` is one constant |
| O4 | contact-kernel annihilation | `ker(lambda·Phi) = ker(Phi)` for every `lambda != 0`, and `beta > 0`, so `Ctc_N^sym subset ker` is preserved | **no** |
| O2 | presentation independence | the pairing must not depend on the presentation of the cell; a normalization that assigns magnitude *per presentation* would break it | **yes — the only one** |

[PROVABLE] O1, O3 and O4 are therefore **provably blind** to a global positive
length normalization: no choice of `beta > 0` can violate any of them. This is the
exact formal content of the corpus's own sharp statement that a spacetime length
normalization "is not progress on (JD-3)" — three of the four obligations cannot even
see it.

[PART-PROVABLE] O2 is the one that shares structure, because presentation
independence is a statement about the *cell's presentation class*, and a length
normalization is an assignment of magnitude to a cell. Whether the two are the *same*
demand is not sealed; §3.2 is where that gap is stated.

[PROVABLE] **This is not merely an elementary observation; the corpus has already
sealed the general fact.** `REG` `6bf7c3bd…[328133,328405)`, the Q-126 ruling,
established 2026-07-31 and confirmed by all four adversarial verifiers:

```text
OF THE THIRTEEN SEALED INTERFACE QUANTITIES ON THE RECORD CELL, EVERY JUNCTION THAT
IS DERIVED IS `beta`-INVARIANT, AND EVERY JUNCTION WHERE `beta` COULD APPEAR IS
ADOPTED, GAP, OR UNBUILT. NO JUNCTION IS SIMULTANEOUSLY DERIVED AND `beta`-SENSITIVE.
```

[YOURS] My §2.1 table is the special case of this at (JD-3): three of the four
obligations are `beta`-blind, and I reached that by elementary means before finding
the sealed census. The census says the same thing across all thirteen interface
quantities — and it carries a consequence for U1 that the table alone does not:

> **A joint-discharge test in which the normalization can matter must be run at a
> `beta`-SENSITIVE junction, and by the sealed census every such junction is
> currently ADOPTED, GAP, or UNBUILT.**

So U1 cannot be closed at any presently *derived* junction — not because the
condition is unstated, but because the record cell has no junction that is both
derived and able to feel `beta`. That is a second, independent reason the test does
not close, and it is sealed rather than mine.

[YOURS] **A conditional that matters more than it looks.** The O3 row is conditional
on `beta` being a single constant. `ADM` writes `beta` in the singular but scopes the
rule "on one record cell" and Requirement II "on the same cell", which reads as
per-cell. **The corpus does not seal `beta`'s arity over cells.** If `beta` is a
per-cell function rather than one number, then O3 (continuity in member-named
topologies) and O2 both bind it directly, and the joint condition acquires content
the constant reading does not have. This is a second, smaller underdetermination and
it is recorded, not resolved.

### 2.2 The binding rule is not among the four — it is imposed from outside

[PROVABLE] `SEQ` `eaeffd37…[1069,1360)`, verbatim:

```text
THE CROSS-SECTOR-UNIT DECLARATION: any supplier of a cross-sector arrow — (JD-3) or
otherwise — must DECLARE its cross-sector unit per DoR-019's
NO_IMPLICIT_CROSS_SECTOR_UNIT; the anti-tuning ledger of every such build carries the
cross-sector-unit row. No invisible dimensionless parameter.
```

This is principal-level, sealed, and **names `(JD-3)` explicitly**. It is the fifth
obligation on the Ward-symbol map, and it is the one that reaches the normalization,
because `beta` *is* a dimensionless parameter of exactly the kind the last sentence
forbids leaving invisible.

[PROVABLE] It is frozen as **clause 8** of the completed-existence ruling, `A2`
`5c945cae…[1674,1753)`:

```text
8 the cross-sector-unit obligation (Q-522; the sequenced-program decision);
```

[PROVABLE] And the clause immediately before it, `A2 [1627,1678)`, freezes the J-II
carriage:

```text
7 CARRY-1 (the J-II §7.4 carriage, verbatim);
```

[YOURS] The two halves of U1 are **adjacent clauses of one principal ruling**. The
same act froze the J-II carriage and the cross-sector-unit obligation, and never
joined them. That is where U1 has been sitting.

[PROVABLE] `019` supplies the rule clause 8 defers to, `6ab72b0c…[1140,1325)`:

```text
Derived-or-declared, never implicit (the principal's clarification of record,
Q-382): any future cross-sector conversion arrow must be separately declared
```

and, at `[737,878)`, the routing that makes the channel unique:

```text
R4-ONLY conversion routing with the failure-capable NO_IMPLICIT_CROSS_SECTOR_UNIT
certificate (residual-parameter audit: none exists undeclared; nu remains symbolic)
```

[PROVABLE] And, decisively for a *joint* obligation, `019 [1454,1549)` lists among
its permanent regressions:

```text
the forbidden implicit-unit construction, the paired-hidden-conversion attack.
```

[YOURS] **The paired-hidden-conversion attack is U1's failure mode, already named and
already permanently armed.** It is the attack in which no single object hides a
conversion but a *pair* does — each declaring half, or each assuming the other
declared. That is exactly what "the two maps land separately" would be. The corpus
armed the falsifier before it had the pair to point it at.

### 2.3 The admissibility clauses on the same structure

[PROVABLE] Two clauses of `ADM` bear on the same cell-presentation structure as O2:

Fence **F'-5**, `3c008ecc…[5052,5174)` (imported by `ADM §2`):

```text
no ell, no truncation level, no cellulation-family index, AND NO CELLULATION
GEOMETRIC DATUM.
```

Falsifier **F1**, `3c008ecc…[10484,10602)`:

```text
F1 beta_family_survives:
  after applying the candidate, beta remains a positive continuous family.
```

[PART-PROVABLE] F'-5 forbids `beta`'s determining constants from carrying any
cellulation geometric datum — the external-side analogue of O2's demand that the
pairing not depend on the cell's presentation. F1 requires the `beta` family to
*collapse*. Together they are the normalization-side statement that the shared object
is presentation-invariant **and** determinate. O2 is the symbol-side statement that it
is presentation-invariant. Parallel demands on one structure — which is why they are
the pair Y1 asks for.

### 2.4 The sealed template for what a joint condition must look like

[PROVABLE] The corpus has already solved a structurally identical problem, and the
solution is sealed. `SEAM2` `2525096b…[1786,2042)`:

```text
old R9
 =RETIRED: its independent Triv_[a] x Scal^Hol quantifier
  compares a varying H representative with a frozen HOL return and
  therefore annihilates the common period;

R9-V002
 =REPAIRED: compare the two equivariant route-return sections as one
  associated-orbit object on each A8-common formed instance;
```

[YOURS] Read what killed the old R9: **comparing two returns independently — one
varying, one frozen — annihilated the shared invariant.** That is precisely the shape
of "the Ward-symbol map lands, and separately the length normalization lands." The
repair is the template: compare them **as one associated object on each common formed
instance**, not as two independently formed returns.

[PROVABLE] And the discipline that governs when such a comparison may be made at all,
`J-II` `fdf20bd4…[55251,55380)`:

```text
| A8/R9 | one common physical cell on which both typed returns exist independently |
  no common cell is presently formed |
```

with the typing at `fdf20bd4…[30043,30084)` — **"R9 is a falsifier, not a
constructor"** — and the carriage clause frozen as `A2` clause 7, at
`fdf20bd4…[64793,64851)`: **"keep R9 PENDING until one common physical cell
exists"**.

### 2.5 THE SHARED-CORE OBLIGATION, DERIVED AND DISPLAYED

[PART-PROVABLE] Assembling §2.2–§2.4, the obligation both objects jointly discharge
is the **cross-sector-unit declaration on one formed record cell**, in this typed
form:

```text
Let e be a cell of the record complex N and [e] its presentation class.

  (A)  Phitilde_N(.)(delta_e) : Sigma_N^(2),Ward -> C        the Ward-symbol map at e
  (B)  beta                    : -> R_(>0)                    the length normalization
                                                              for the same cell,
                                                              R = beta c Delta tau

JOINT LANDING at e holds iff there is a SINGLE declared cross-sector unit u(e),
routed through the one R4 seam, such that:

  J1  DECLARATION IS SHARED.  The unit (A) must declare under clause 8 and the
      normalization (B) determines are the same object u(e) — one declaration, not
      two.  Two separately declared units on one cell is the paired-hidden-conversion
      attack, permanently armed by DoR-019.

  J2  PRESENTATION-INVARIANT.  u(e) depends only on [e]: invariant under
      re-presentation of the cell (O2), and a functional of admissible data only,
      carrying no cellulation geometric datum (F'-5).

  J3  NOT IMPLICITLY ONE, AND DETERMINATE.  Neither map may set u(e) to 1 implicitly
      ("no invisible dimensionless parameter"), and u(e) may not survive as a positive
      continuous family (F1).

  J4  ONE OBJECT, NOT TWO RETURNS.  (A) and (B) are compared as one associated object
      on e, per R9-V002's repair — never as two independently formed returns, which is
      the failure that retired the old R9.

TYPING.  Joint landing is a FALSIFIER, not a constructor.  Failure of J1–J4 kills;
satisfaction of J1–J4 builds neither map.  Neither map's residue is discharged by the
other's (both residues stand, per the OVERLAPPING determination).
```

[YOURS] J1–J3 are each grounded on a sealed clause; J4 is grounded on a sealed repair
of a structurally identical comparison. The falsifier typing is not mine either — it
is R9's own typing, carried across with the template.

### 2.6 Cross-check against this lane's own prior claim — agreement, not reliance

[PROVABLE] Only after the above was derived from non-Dario sources did I read `P07`'s
surrounding sentence, `8e31df94…` near `[12199,12257)`:

> "a general cross-sector unit/conversion discipline at the record boundary — the
> genus both objects belong to — **is joint progress**. A symbol-evaluation rule is
> not progress on `β`, and a spacetime length normalization is not progress on
> `(JD-3)`."

[YOURS] This lane predicted the *shape* of the answer and did not supply it. The
independent sealed sources supply it. Recording the agreement is lawful; grounding on
the prediction would not have been, and §0.3 is why I ran it in that order.

---

## 3. Y2 — TYPING THE RESULT

### 3.1 What lands

[PART-PROVABLE] The **obligation** derives. There is a sealed, principal-level rule
that binds both objects at one junction, names `(JD-3)` explicitly, routes conversion
through a single seam, forbids an invisible dimensionless parameter, and permanently
arms the exact paired failure mode. Nothing in §2.5 is invented; every conjunct cites
a sealed clause.

### 3.2 What does not land, and why I may not bridge it

[PROVABLE] Every clause of §2.5 is quantified **"on the same cell `e`"**. That
quantifier is not sealed for this pair.

Searched, not assumed:

- `ADM` uses "record cell" twice and "same cell" twice; it never says "k-cell".
- `J-II` uses "named k-cell" once; it never says "record cell".
- The modulus gate uses neither term.
- Across the whole archive — **workspace and supervision both**, after my first
  search covered only the workspace and I corrected it (§7 disclosure 1) — exactly
  **five** artifacts contain both "JD-3" and "record cell": this artifact, the two
  OBS-22 preregistrations, `STAGE8_TASK6_JII_BETA_IDENTIFICATION_DARIO_V001.md` — all
  four this lane's own — and one that is not: the settled-questions register,
  `QUESTIONS_SETTLED_REGISTER_V001.md` (`REG` `6bf7c3bd…`).

[PROVABLE] **The register does not supply the identification. It registers it as an
explicitly unchecked hypothesis.** `REG [1070861,1071250)`:

```text
THE IDENTIFICATION HYPOTHESIS, unchecked and now registered: LP-JII = THE BETA GAP
(the internal/external metric conversion of a record cell ...). Same shape; if the
same object of record, one thing has blocked both routes since the beginning ...
Determination commissioned when a lane frees
```

[PROVABLE] That commissioned determination is `P07`, and it returned **OVERLAPPING —
not IDENTICAL**. So the one non-Dario artifact that puts the two terms together
posed the identification as a question and had it answered in the negative. `P07`
itself then declined to assert it, recording the register's convergence finding as
naming them *"the same object **family**"* and noting explicitly that the sealed
phrase is "object *family*, not object".

[PROVABLE] **How close the corpus gets, and where it stops.** `REG [13683,13986)`,
the Q-10 ruling, does state a one-cell-carries-both fact — but for the
normalization's *own* two structures, not for our pair:

```text
ONE record cell already carries both structures — it is a causal diamond bearing a
primitive projective record degree. Both constructions already agree that object
exists.
```

[YOURS] This is worth stating precisely, because it halves the missing carrier. The
`beta` side's common cell **exists of record**: the record cell demonstrably carries
both the internal projective structure and the external Lorentzian diamond, which is
exactly `ADM`'s Requirement I / Requirement II pair. What has never been sealed is
the **other** half — that `(JD-3)`'s named oriented k-cell *is* that record cell. The
missing carrier is therefore one identification, not two.

[PROVABLE] And the corpus's standing habit with precisely this kind of identification
is to refuse it. `REG [10301,10391)`, Q-08 — *"Is the bundle fiber radius the same
object as the record cell radius?"* — rules **"TWO OBJECTS, NO BRIDGE"**, and reopens
only if a derived cross-sector metric rule appears.

[PROVABLE] And the corpus does not merely omit the step; it **forbids** it. `R9`'s
standing discipline is *"no equality assumed before a common cell"*, and `A2` clause 7
freezes *"keep R9 PENDING until one common physical cell exists"*. Identifying
(JD-3)'s named oriented k-cell with the admissibility spec's record cell, on my own
authority, is precisely the assumption R9 exists to block.

[YOURS] This is the whole result, stated plainly: **U1 is not open because nobody got
round to writing the condition. It is open because the condition is quantified over a
common cell that the corpus has never formed, and has an armed falsifier standing
guard over the temptation to assume one.**

### 3.3 The NOT-RECOGNIZABLE-AS-STATED form

Per the frozen preregistration's third verdict, which requires a *named* missing
carrier:

```text
U1 JOINT LANDING = NOT-RECOGNIZABLE-AS-STATED

WHAT IS RECOGNIZABLE:
  the shared-core OBLIGATION -- the cross-sector-unit declaration on one formed
  record cell, in the J1-J4 form displayed at §2.5, typed as a falsifier.

WHY THE TEST DOES NOT CLOSE -- TWO INDEPENDENT REASONS:

  (i)  QUANTIFIER.  J1-J4 are quantified "on the same cell e".  No sealed text
       identifies (JD-3)'s named oriented k-cell with the admissibility spec's
       record cell.  The register poses that identification as an UNCHECKED
       HYPOTHESIS, and the determination it commissioned returned OVERLAPPING, not
       IDENTICAL.  R9 forbids assuming the equality before a common cell is formed,
       and no common cell is presently formed.

  (ii) SENSITIVITY.  Even granted the quantifier, the test must be run where the
       normalization can matter.  The sealed Q-126 census holds that NO JUNCTION ON
       THE RECORD CELL IS SIMULTANEOUSLY DERIVED AND beta-SENSITIVE.  So no presently
       derived junction can run J1-J4 non-vacuously.

  These are independent: sealing the quantifier would not by itself supply a
  beta-sensitive derived junction, and building one would not by itself supply the
  quantifier.

MISSING CARRIER:
  the COMMON-CELL QUANTIFIER for the pair (Ward-symbol map, length normalization) --
  and, by Q-10, only ONE HALF of it is missing: the record cell already carries the
  normalization's internal and external structures of record; what is unsealed is
  that (JD-3)'s named oriented k-cell is that same record cell.

SECOND, SMALLER GAP (recorded, not the blocker):
  beta's arity over cells is unsealed -- one constant, or a per-cell function.
  Under the per-cell reading, obligations O2 and O3 bind it directly and J2 acquires
  content it does not have under the constant reading.
```

### 3.4 The smallest sealed addition that would determine it

[YOURS] One line, in the shape the corpus already uses:

```text
PROPOSED CARRIER (not adopted here; the principal's to seal or refuse)

  R9-JII :  one common formed record cell e on which BOTH
              (A) the Ward-symbol map's declared cross-sector unit, and
              (B) the length normalization's beta
            exist independently.

            By Q-10 the record cell already carries (B)'s internal projective and
            external Lorentzian structures of record, so the open half is exactly:
            (JD-3)'s named oriented k-cell IS that record cell.

            On such an e, agreement of (A)'s declared unit with (B)'s beta is a
            FALSIFIER, not a constructor: disagreement kills; agreement builds
            neither map and discharges neither residue.

  NOTE, carried with it:  by the sealed Q-126 census, R9-JII is satisfiable
  non-vacuously only at a junction that is both DERIVED and beta-SENSITIVE, and no
  such junction presently exists.  Sealing R9-JII therefore states the test; it does
  not make it runnable.  Both facts should travel together, or the carrier will read
  as more progress than it is.
```

[YOURS] Why this is the *smallest* addition: it adds no route, member, branch or
selection; it constructs nothing; it takes the quantifier from A8/R9 verbatim and the
falsifier typing from R9's own words. It is `CARRY-1`-compatible — it neither forms a
common cell nor assumes one exists, it only says what would count as one for this
pair. If no such cell is ever formed, `R9-JII` stays PENDING and U1 stays open
honestly, which is the correct behaviour and not a defect of the carrier.

[YOURS] What it would **not** do: it would not derive `beta`, would not supply
(JD-3), and would not discharge either residue. U1 would move from *"no statement of
what joint landing means"* to *"a stated test, pending its first common cell"* — which
is exactly the status the rest of the junction arc already carries.

---

## 4. Y3 — CORRESPONDENCE LEDGER (verdict weight = 0)

Likenesses met on the way, logged and moved past. Per the frozen preregistration the
correspondence channel carries **zero verdict weight**; none of these is evidence, and
none is used above.

| # | Encountered at | Classical likeness | Weight |
|---|---|---|---|
| L1 | `beta` converting a cell's internal geometry to a spacetime length | a lattice spacing relating a dimensionless lattice coupling to physical distance | 0 |
| L2 | "No invisible dimensionless parameter" (`SEQ`) | naturalness-style discipline against hidden dimensionless ratios | 0 |
| L3 | R9-V002's "one associated-orbit object" rather than two independent sections | a global section versus two local trivializations that must agree on their overlap | 0 |
| L4 | `Phitilde_N(s)(delta_e)` as incidence/integration over a named oriented cell | the de Rham / Stokes pairing of a form against a chain | 0 |

[YOURS] L3 is the one a reader will find most suggestive and is therefore the one
most worth fencing: the resemblance played no part in §2.4, which rests on the sealed
text of the R9 repair and not on the analogy. I log it because the preregistration's
discovery channel is where such things belong, and because a likeness that goes
unlogged is a likeness that later gets mistaken for a reason.

---

## 5. GROUNDING TABLE — EVERY STEP

| # | Step | Source + span | Tag |
|---|---|---|---|
| 1 | (JD-3), (JD-3a) and the four obligations | `fdf20bd4…[45349,45957)` | PROVABLE |
| 2 | O1 blind to a positive rescaling | elementary: a positive factor commutes with the orientation sign | PROVABLE |
| 3 | O4 blind to a positive rescaling | elementary: `ker(lambda·Phi)=ker(Phi)`, `lambda != 0` | PROVABLE |
| 4 | O3 blind, **conditional** on `beta` being one constant | elementary; arity unsealed (§2.1) | PART-PROVABLE |
| 5 | O2 is the only obligation sharing structure with a normalization | §2.1 by elimination over 2–4 | PART-PROVABLE |
| 6 | The cross-sector-unit declaration, naming (JD-3) | `eaeffd37…[1069,1360)` | PROVABLE |
| 7 | It is frozen as clause 8 | `5c945cae…[1674,1753)` | PROVABLE |
| 8 | Clause 7 freezes the J-II carriage in the same ruling | `5c945cae…[1627,1678)` | PROVABLE |
| 9 | "Derived-or-declared, never implicit" | `6ab72b0c…[1140,1325)` | PROVABLE |
| 10 | R4-ONLY routing, single conversion channel | `6ab72b0c…[737,878)` | PROVABLE |
| 11 | The paired-hidden-conversion attack is permanently armed | `6ab72b0c…[1454,1549)` | PROVABLE |
| 12 | Target object: a metric rule on **one record cell** | `3c008ecc…[2934,3131)` | PROVABLE |
| 13 | Requirements I/II and the pass condition | `3c008ecc…[6182,6516)` | PROVABLE |
| 14 | Fence F'-5 — no cellulation geometric datum | `3c008ecc…[5052,5174)` | PROVABLE |
| 15 | Falsifier F1 — `beta` family must not survive | `3c008ecc…[10484,10602)` | PROVABLE |
| 16 | F'-5 + F1 are the external-side analogue of O2 | §2.3, by comparison of the clause texts | PART-PROVABLE |
| 17 | R9-V002's repair; the retired R9's failure mode | `2525096b…[1786,2042)` | PROVABLE |
| 18 | A8/R9's common-cell quantifier; no cell formed | `fdf20bd4…[55251,55380)` | PROVABLE |
| 19 | R9 is a falsifier, not a constructor | `fdf20bd4…[30043,30084)` | PROVABLE |
| 20 | CARRY-1 keeps R9 PENDING until a common cell exists | `fdf20bd4…[64793,64851)` | PROVABLE |
| 21 | The J1–J4 assembly | §2.5, from 1 and 5–20 | PART-PROVABLE |
| 22 | No sealed text identifies the two cells | §3.2, archive-wide search, result displayed | PROVABLE |
| 23 | Five artifacts join the two terms; four are this lane's, one is the register | §3.2, all five named | PROVABLE |
| 24 | The register types the identification an **unchecked hypothesis** | `REG` `6bf7c3bd…[1070861,1071250)` | PROVABLE |
| 25 | Its commissioned determination returned OVERLAPPING, not IDENTICAL | `P07` `8e31df94…`, §J2.3 | PROVABLE |
| 26 | Q-126: no junction is simultaneously derived and `beta`-sensitive | `REG` `6bf7c3bd…[328133,328405)` | PROVABLE |
| 27 | Q-10: one record cell already carries both `beta`-side structures | `REG` `6bf7c3bd…[13683,13986)` | PROVABLE |
| 28 | Q-08: the corpus's habit is "TWO OBJECTS, NO BRIDGE" | `REG` `6bf7c3bd…[10301,10391)` | PROVABLE |
| 29 | Sensitivity is a second, independent reason the test does not close | §3.3, from 26 | PART-PROVABLE |
| 30 | The proposed `R9-JII` carrier | §3.4 — authored by me, adopted by nobody | **YOURS** |
| 31 | `beta`'s arity over cells is unsealed | §2.1, §3.3 — searched, absent | PART-PROVABLE |

```text
GROUNDED_STEPS = 30 / 31
UNGROUNDED, NAMED, NOT BRIDGED:
  step 30 -- the proposed R9-JII carrier is authored, not sealed.  It is a PROPOSAL
  to the principal and is used nowhere above as a premise; §2.5 and §3.3 stand
  without it.
```

---

## 6. THE JURISDICTION CHECK ON EVERY RULE INVOKED

Applied to each load-bearing rule, in the handoff's three questions.

**Clause 8 / the cross-sector-unit declaration.** Written to protect against a
conversion smuggled in as a factor of one at a sector boundary — the risk is squarely
present, since U1 *is* a sector boundary with a dimensionless factor on it. Its
outcome space distinguishes false from cannot-see: an undeclared unit fires the
falsifier, a declared one passes, and silence is neither. If the theory is right and
the maps do land jointly, the rule permits that evidence to appear — it demands the
unit be *declared*, not that it be absent or of any particular size.

**DoR-019's `NO_IMPLICIT_CROSS_SECTOR_UNIT` and R4-ONLY routing.** Protects the same
risk one layer down, at the carrier metric. Present here. Failure-capable by
construction ("failure-capable … certificate"). It permits a lawful crossing; it bars
only an invisible one.

**JD-3's four obligations.** Written to protect the Ward-symbol map's
well-definedness. §2.1 shows three of the four are blind to a positive normalization —
which is a finding about their jurisdiction, not a weakening of them. **I did not
stretch any of the four to reach the normalization**; that is exactly the bridging
this relay forbids, and the reason §2.2 goes outside the four for the binding rule.

**`ADM` F'-5 and F1.** Protect against carrier-indexed constants and against a
`beta` that is merely permitted rather than forced. Both risks present. Both
distinguish failure from silence, and both permit a derived `beta` to appear.

**R9 / R9-V002.** Protects against declaring two independently formed returns equal
before there is a cell on which both exist. That risk is not merely present here — it
is the risk U1 *is*. Its outcome space is three-valued in effect (kills on
disagreement, PENDING with no common cell, and never constructs), so it distinguishes
false from cannot-see-this. And it would permit the evidence: if the two maps do land
on one cell, R9's quantifier is satisfiable and the test runs.

[YOURS] No rule was invoked outside its jurisdiction, and one rule family — the four
JD-3 obligations — was expressly held *inside* its jurisdiction at the cost of a
shorter answer.

---

## 7. SELF VERB AUDIT

| Verb or status | Warrant |
|---|---|
| `derived` | applied only to the shared-core **obligation** (§2.5), each conjunct citing a sealed clause |
| `PART-DERIVED` | the obligation lands; its quantifier does not — both halves stated |
| `NOT-RECOGNIZABLE-AS-STATED` | the frozen prereg's third verdict, used with a named missing carrier as it requires |
| `provably blind` | O1/O3/O4 — elementary consequences of positivity, displayed, not asserted |
| `forbidden` | R9's own words: "no equality assumed before a common cell" |
| `proposed` | §3.4 only; authored, adopted by nobody, and used as a premise nowhere |
| `agreement` | §2.6 — checked after the fact, in the lawful direction only |

[YOURS] Disclosures against myself:

1. **I searched the wrong space and wrote a claim about the right one.** My first
   corpus-wide search for artifacts joining "JD-3" and "record cell" ran inside
   `workspace/` only, returned three files, and I drafted §3.2 as *"across the whole
   archive … all three of them this lane's own."* My own pin check caught it: the
   count is **five**, and one of them — the settled-questions register — is not mine
   and sits in `supervision/`. This is the same defect class as the whitespace
   incidents I made a standing rule about at 707: **concluding absence from a search
   that did not cover the space.** Corrected in §3.2 before sealing, and the register
   turned out to *strengthen* the result rather than overturn it — which is luck, not
   method, and does not make the error smaller. Second time in two relays that a
   scope-of-search question has bitten this lane; the rule now needs a companion
   clause: *state the searched space in the same sentence as the absence.*
2. **I nearly grounded this on my own artifacts.** The two documents that state U1's
   problem are both mine. Had I derived the joint condition by identifying the two
   cells, I would have been verifying this lane's own construction — and the result
   would have read as a derivation while resting on an identification the corpus
   explicitly types as an *unchecked hypothesis*. §0.3 is the recusal that avoided it;
   §3.2 is what looking for it actually found.
3. **I wanted the answer to be one of the four obligations.** The relay's Y1 is phrased
   as though one of them binds, and the tidy answer would have been to name O2 and
   move on. Three of the four are provably blind to the normalization, and even O2
   only shares *structure* with F'-5 rather than being sealed as the same demand. The
   binding rule is a fifth obligation from outside the list. I record that the question's
   framing and the corpus's answer differ.
4. **`beta`'s arity is a gap I found late** and did not chase to ground, because it
   does not change the blocker. It is recorded at §2.1 and §3.3 so it is not lost.
5. **The relay's "§X5" pointer does not resolve**; I read §5 on content match rather
   than stopping. If the registrar intended a different section, this derivation read
   the wrong one — though the quoted content matches §5 exactly.
6. **My first register spans were character offsets, not byte offsets.** The
   settled-questions register carries non-ASCII (`≡`, em-dashes), so the two differ —
   by 8,690 bytes at the far end of a 1.25 MB file. All four register citations were
   wrong on first write and would have resolved a reader to unrelated text. My own
   pin check caught it before sealing and every span here is now recomputed on bytes
   and re-tested against its quoted text. Every other span in this artifact was
   generated on bytes from the start; only the register ones went through a decoded
   string.
7. No verb here proves, authorizes, computes, binds a member, evaluates a junction
   map, or grants a seal.

```text
U1 = underdetermined (missing carrier named: the COMMON-CELL QUANTIFIER for the pair;
     the shared-core OBLIGATION itself is derived and displayed at §2.5)
GROUNDED_STEPS = 30 / 31 (+1 ungrounded named: step 30, the proposed R9-JII carrier, authored not sealed)
LEDGER_ENTRIES = 4
PREREG_UNTOUCHED = true
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+7 disclosures at §7)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
