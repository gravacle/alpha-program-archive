CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00003945
VERDICT_BEARING_SET = exactly the 15 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LIVE_MEMBERS = NONE
LANE = CODEX 2
ROLE = OPPOSITE-LANE ENTRY-GATE CHECK OF ATTACH CANDIDATE V003 (relay 1060)
PATH_ROOT_cleanroom = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
PATH_ROOT_archive = /Users/bgm/MB Work/alpha-program-archive
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001; every substantive citation names a member and line locus
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `cleanroom/relay_inbox/RELAY_PASTE_1060_V003_CHECK_CODEX2_V001.md` | `a11f460c01024a8c31ca3608980344cd62177d206d4d30a5908cf08304f3ba04` | assignment |
| 02 | `cleanroom/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V003.md` | `82d5c5dd59d1d0d6981a2cde7244c1dad1a66352c4159b8543f0554777abea31` | candidate under attack |
| 03 | `cleanroom/gen_stage8_attach_candidate_proposal_dario_v003.py` | `f641ded1f4769b2906ac5c9a9869145a7c297a655f41e824d7e5f1d719930c57` | candidate generator and twelve refusals |
| 04 | `cleanroom/STAGE8_ATTACH_CANDIDATE_V002_CHECK_CODEX2_V001.md` | `56d9c9b68dcde555fe3157a4ae09545418b7b8f35766761a91184a534ce42f1b` | five-repair standard and two settlements |
| 05 | `cleanroom/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | mandate, convertibility, and entry law |
| 06 | `cleanroom/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V001.md` | `8c257818b55c66aef1842024601c51f3a22599a949db7de4280d7763fc9dcdbc` | first candidate and first appearance of the residual objects |
| 07 | `cleanroom/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V002.md` | `14a811a8d5a507c5d20e26ef40fa53661e22b0b39f869edd68ad700bc3765f82` | superseded candidate and carried ledger rows |
| 08 | `archive/supervision/DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md` | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | ratified-premise-with-standing-falsifier precedent |
| 09 | `cleanroom/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | `rho_joint`, associated lines, circuit carriers, and domain typing |
| 10 | `cleanroom/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | adopted connection and physical-cell obligation |
| 11 | `cleanroom/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | base pulse and underived parent |
| 12 | `cleanroom/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md` | `2be95d5b58ea000df9a30b717a809374a90693f78a893d87b73a12e4be97fa21` | SR representation obstruction |
| 13 | `cleanroom/STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md` | `ab156ee764db9d0bd48f54f1b879f1bafcfac08b45520ca6c4fb582e48edf572` | Attach type, absence, and prior refusal |
| 14 | `cleanroom/STAGE8_DESC_OPEN_CONDITIONS_SWEEP_CODEX2_V001.md` | `85514b04912f77e9276aca131b41d62d400a3e555e21492eb43a1df53dd5d588` | six-check acceptance registry |
| 15 | `cleanroom/STAGE8_ATTACH_CANDIDATE_CHECK_CODEX2_V001.md` | `617e0144c0adfad97ec14ae85fbb76efd9787a63f1c0b77b778c9768c747a528` | first opposite-lane candidate check |

```text
BLIND HELD. EVERY SCALE REMAINS SYMBOLIC.
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
NO MEMBER BOUND. NO PHYSICAL FIXED POINT EXECUTED. NO END TEST. NO FREEZE.
NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEASURED-CONSTANT COMPARISON.
PE-1..17 = POINTER-ONLY. omega_phys = UNTOUCHED.
CANDIDATE_STATUS = PROPOSED_NOT_ADOPTED
```

CLOSURE_DECLARATION_END

# STAGE 8 — ATTACH CANDIDATE V003 OPPOSITE-LANE ENTRY-GATE CHECK

Date: 2026-08-12  
Lane: Codex 2  
Status: **CHECKED — CORRECTED, NOT YET ENTRY-READY**

## 1. Preflight, closure, and custody

Members 01–07 rehashed to the assigned digests before semantic use. Member 02's declared-first
closure begins at byte 0 and ends immediately after `CLOSURE_DECLARATION_END` at byte 7086. Its table
was parsed independently: all `25/25` paths under the archive root exist and hash to their declared
digests, with zero live members. The endpoint was recomputed from bytes rather than copied from the
display. Member 03 contains twelve distinct refusal routes R1–R12.

Members 06 and 07 remained byte-identical after every read-only check at their declared digests.
The mandate authorizes authoring and a later principal entry mechanism; it does not authorize this
check to enter, ratify, promote, or bind the candidate (member 05 `:17-23,60-66`).

## 2. Five-repair check

### 2.1 Domain — verified

Member 02 `:91-128` withdraws the broader `b_1 >= 1` claim, makes connected admitted `b_1=1` cells
the operative domain, makes the candidate inert on `b_1=0`, and places `b_1>1` out of scope. This is
the exact bounded repair required by member 04 `:76-108`: the one-cycle class is selector-free, while
more than one independent cycle would require a selector the candidate does not supply.

The narrowing does not erase any residual. Member 02 `:117-128` keeps R1–R3: the loop-bearing class
is specified/mandated but unsealed, no proof identifies the mathematical restriction with the
physical causal-cell domain, and admissibility is not universality. Member 10 `:103-111,159-170`
still requires an explicit physical causal cell and still reports it underived.

### 2.2 A5 and A4 — algebraically verified

I rebuilt the calculation over `Z[z]` with a separate coefficient-tuple polynomial implementation.
With

```text
E = [[1,0],[0,1],[0,0]],
D = diag(1,z,1),
R_old = diag(z,z),
R_new = diag(1,z),
```

the two residuals are

```text
E R_old - D E = [[z-1,0],[0,0],[0,0]],
E R_new - D E = [[0,0],[0,0],[0,0]].
```

Thus the old scalar receiver fails away from `z=1`; the new two-line receiver holds identically.
This reproduces member 02 `:135-175` without using member 03's polynomial implementation. The forced
change in A4 is also coherent: on `L_s (+) L_t`, `1 (+) chi_1` is trivial on the root line and carries
the character on the successor line (member 02 `:177-189`). It does not turn the relative receiver
into a common scalar.

### 2.3 A6 sign — algebraically verified

For a projector `Q`, the formal identity

```text
exp(i c theta Q) = I + (exp(i c theta)-1) Q
```

gives

```text
H_2 = +w theta Q, integral w=1  -> exp(-i theta Q) = diag(1,e^(-i theta),1), n=-1;
H_2 = -w theta Q, integral w=1  -> exp(+i theta Q) = diag(1,e^(+i theta),1), n=+1.
```

The correction at member 02 `:194-221` is exact. It makes the authored carrier executable at the
adopted `n=+1`; it does not derive the two-stage parent. Member 11 `:77-101` still withholds that
parent, exactly as member 02 `:223-234` says.

### 2.4 F14 and F15 — classifications verified; closed map not verified

F14 does not migrate candidate-authored content into FORCED. The named `rho_joint`, its faithful
character, the associated line bundle, and its projective-quotient scope pre-exist the candidate at
member 09 `:210-239`. A4 remains AUTHORED; F14 carries only the pre-existing receiver data.

F15 is stronger still: member 10 `:47-51` expressly calls the compact connection on the
`U(1)_rel` bundle adopted Level-1 field content. The construction's capital `A` is the candidate's
notation for that carried connection; the per-cell identification through `theta_j` remains A1 and
is not reclassified. Hence the forced row increase `13 -> 15` is not generosity.

The associated closed-symbol claim nevertheless fails under the independent check in §3.

### 2.5 Recount — verified

The registry at member 02 `:319-340` is accurately restated:

```text
AC-1 PASS
AC-2 NOT CLOSED
AC-3 PARTIAL, NOT CLOSED
AC-4 NOT-YET-TESTABLE
AC-5 NOT-YET-TESTABLE
AC-6 PASS
TOTAL = 2 PASS / 2 NOT-CLOSED (one partial) / 2 NOT-YET-TESTABLE.
```

Member 14 `:249-254` requires more than A4's group bridge for AC-2, and member 12 `:108-133` keeps
the same-GNS/continuum obstruction open. The requirements count at member 02 `:342-353` is also
honest: orientation is the only wholly forced-met requirement; a forced inter-cell half does not
make the combined time-ordering requirement met. `REQUIREMENTS = 1/5 FORCED-MET`.

The core remains eight AUTHORED rows, seven new. F14 and F15 add two already-existing source
ingredients to the FORCED ledger, making `13 -> 15`; no authored row changes category. The accurate
description is therefore **more complete, not more generous**. Adding the missing symbol-map links
identified below would add no authored ingredient and change no row count.

## 3. Independent closed-claim audit — escape found

### 3.1 Independent method

The checker did not call or translate R11. It isolated the bytes between `CONSTRUCTION_BEGIN` and
`CONSTRUCTION_END`, lexed all mathematical identifiers generically, removed English/operator words,
the explicit row reference `F08`, bound indices `j,t`, the defined abbreviation `z`, the domain
statistic `b_1`, and displayed basis labels `e_s,e_t,r,p_Q`, then joined the remaining identifiers
against `SYMBOL_TABLE_MAP_BEGIN/END`.

This conservative extraction returned 25 construction symbols. The explicit map contains 22 symbols
covering 15 distinct rows. The difference is:

```text
B_j   -- appears in H_stage stage 1; pre-existing base generator, member 11 :16-34
v_j   -- appears in H_stage stage 1; pre-existing pulse profile, member 11 :16-34
w_j   -- appears in H_stage stage 2; authored second-stage profile, member 02 :210-219
```

These are not prose, indices, row labels, or locally bound dummy variables. They are consumable
operators/profiles in the operative Hamiltonian. In particular, `B_j` and `v_j` are source objects
from the forced base-pulse law, while `w_j` is part of authored A6. The map at member 02 `:278-307`
names none of them.

### 3.2 Why R11 did not catch them

Member 03 `:253-270` does not generically extract construction symbols. It admits only a hard-coded
regular-expression inventory and then expressly places `B_j`, `v_j`, and `w_j` in `ALIASES` with the
comment “bound/local, not ingredients.” The construction bytes do not support that characterization:
member 11 defines the first two as load-bearing pulse data, and member 02's A6 defines the third as
load-bearing authored data. The exemption is therefore the exact escape the independent test was
required to detect.

A bounded correction is available: map `B_j` and `v_j` to the carried F10 base-pulse row, map `w_j`
to A6, and replace the whitelist/alias test with a generated lexical inventory or state a closed
symbol grammar that does not contradict the literal “every symbol” claim. Under the direct mapping,
the truthful display becomes `25 symbols / 16 distinct rows / 0 unmapped`; the row census remains
`15 FORCED + 8 AUTHORED`.

## 4. The three ungrounded objects

The convergence list is stable and real even though the closed-map defect is an additional
correctable bookkeeping failure.

| Object | Byte evidence | Why candidate text cannot supply it | V001 -> V003 stability |
|---|---|---|---|
| Complete two-stage causal parent | Member 11 `:77-101` says the complete microscopic parent has not been shown to use even the one-use moving front; member 02 `:223-234` strengthens the demand to two stages without deriving it. | The needed object is a result about the external complete source/gauge/gravity/environment action, not a restatement of A6. | V001 member 06 `:306-308,386-388,448`; V002 member 07 `:344-346,450-452,544-546`; V003 member 02 `:223-234,561-565`. |
| SR/physical representation | Member 12 `:108-133` proves the stationary endpoint leaves the same GNS sector and says switch-off, invariance, and scattering must descend from the parent. | A4 supplies a group bridge; it cannot manufacture the moving-front or sector-changing representation AC-2 requires. | V001 member 06 `:447`; V002 member 07 `:443-446`; V003 member 02 `:323-329,561-565`. |
| Physical-domain proof | Member 10 `:103-111,159-170` requires a derived physical causal cell and reports it absent. | Declaring the record-typed `b_1=1` mathematical sector does not prove physical domain membership. | V001 member 06 `:379,472-476`; V002 member 07 `:207-229`; V003 member 02 `:117-128,561-565`. |

The names become sharper as the candidate narrows, but the objects do not change: underived parent,
unclosed representation, and unproved physical domain are present from V001 through V003. No further
pass over the proposal's own prose can convert any into an external derivation.

## 5. Conditional-entry shape — lawful in principle, not exercised here

DoR-008 ratified declared premises while attaching a standing finite-restriction falsifier; any
disagreement voids the decision and everything TYPE-P on it (member 08 `:6-22,33-37`). The Attach
mandate expressly imports that open-slot pattern, requires blind and structural falsifiers, and
reserves content entry to a separate principal addendum naming the candidate and check digests
(member 05 `:45-66`). Nothing in those bytes requires every carried condition to be discharged before
entry. Conditional entry is therefore a lawful *shape*, provided the principal explicitly chooses it
and the addendum carries each condition rather than silently converting it to a fact.

The entry display would have to carry:

| Condition | Falsifier | Immediate void trigger | Settling route |
|---|---|---|---|
| Two-stage causal parent | A sealed complete-parent derivation whose cell event cannot realize the ordered stage-1 base pulse followed by the stage-2 phase pulse. | Byte-level incompatibility with A6 voids A6, the Attach entry, and everything TYPE-P on that entry under member 05 `:47-50`. | Derive the complete causal parent and replay the ordered two-stage cell, write/tail, and parent-state-covariance tests. |
| SR representation | A sealed representation result showing that neither a parent-derived moving front nor a sector-changing implementation realizes the candidate endpoint consistently. | Failure of the required representation/implementation voids the Attach entry and its dependent TYPE-P claims. | Construct and verify a moving-front or sector-changing representation, including switch-off, invariant pointer algebra, and the scattering/continuum conditions named at member 12 `:123-133`. |
| Physical domain | A sealed causal-cell derivation excluding the candidate's connected `b_1=1` sector, or requiring a different/noncanonical cycle carrier. | Incompatibility between the derived physical cell and the candidate's operative domain voids the domain-dependent entry. | Derive the physical causal-cell domain and prove its admitted cells possess the candidate's unique-cycle carrier; otherwise amend or retire the candidate. |

The addendum must also retain the mandate's blind battery falsifier and open forcing slot. If a future
target-independent derivation selects the map, the authored core dissolves into that derivation
(member 05 `:47-58`). This check decides neither entry nor any condition. Because §3 found an
independent closed-map defect, V003 must be corrected and rechecked before it can be the object named
by such an entry addendum.

## 6. Mechanics, output-inspection audit, and residue

- Candidate closure: `25/25`, strict equals stable, zero live members; byte endpoint 7086.
- V001/V002: byte-untouched at `8c257818...` and `14a811a8...` after all checks.
- Algebra: exact polynomial coefficients and projector identity only; no physical quantity evaluated.
- Output-inspection audit: the declared candidate closure contains its assignment, mandate,
  predecessors/checks, and record sources. No scored downstream output, cascade result, PE content,
  `alpha`, or `kappa_record` value was opened or used. The circuit/domain review used only topology,
  status, and carrier form. The finding in §3 comes solely from candidate and generator bytes.
- Fresh residue scan: 18 independently worded strings for after-the-fact acceptance, optimization,
  measured-target fitting, evaluator-output use, battery verdicts, numeric prediction, error/sigma,
  and fitness language were searched over member 02; `0` hits.

## 7. Flattening and freedoms consumed

Flattening check:

- exact A5 algebra was not flattened into completeness of the symbol ledger;
- an explicit row count was not flattened into exhaustive construction coverage;
- a generator's hard-coded alias declaration was not flattened into a binding rule supplied by the construction;
- a pre-existing premise was not flattened into a derivation, and an authored A4/A6 was not relabeled forced;
- mathematical `b_1=1` canonicity was not flattened into the derived physical domain;
- executable A6 was not flattened into a derived parent;
- a lawful conditional-entry pattern was not flattened into an entry decision.

```text
FREEDOMS-CONSUMED
  cited inputs: exactly the 15 closed members and only their declared roles
  derived here: closure rehash; two Z[z] residuals; projector-sign check; independent lexical map;
                recount; three-object stability classification; conditional-entry shape
  selected here: nothing
  authored physics here: nothing
  not consumed: candidate entry; physical cell selection; cycle selector; member binding;
                physical fixed point; end test; physical scale; measured target; PE content;
                freeze; omega_phys
  JOINT_ANCHOR_DERIVED = false
```

CLOSURE = declared-first (candidate byte 0; end byte 00007086; 25/25 strict-stable; own closure byte 0, fixed-point endpoint displayed)
REPAIRS = 4/5-VERIFIED (repair 4 classifications stand, but its closed-map claim escapes B_j, v_j, w_j)
ALGEBRA = REPRODUCED (old residual z-1; new residual 0; corrected sign gives exp(+i theta Q)=D at n=+1)
CLOSED_CLAIM = ESCAPE (B_j, v_j, w_j; independent result 25 symbols / explicit map 22 / 3 unmapped)
RECOUNT = HONEST (2/2/2; 1/5; AUTHORED 8/7 unchanged; FORCED 13->15 with source spans)
THREE_OBJECTS = STABLE-AND-REAL (underived parent; SR representation; physical-domain proof)
CONDITIONAL_ENTRY = LAWFUL-SHAPE-STATED (per-condition falsifier, void trigger, and settling route; no entry decided)
OUTPUT_INSPECTION_AUDIT = CLEAN
PROSE_DIGESTS = 25/25 STRICT==STABLE
RESIDUE_GREP = CLEAN (18 own patterns; 0 hits)
VERDICT = CORRECTED (map B_j,v_j->F10 and w_j->A6; replace the whitelist/alias escape; recheck before entry)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
