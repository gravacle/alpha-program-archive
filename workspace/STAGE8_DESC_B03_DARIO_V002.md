CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep) -- CLAIMED, not clean (Q-921)
VERDICT_BEARING_SET = exactly the 26 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = full archive-root paths, rehashed at seal time (Q-913).
LANE = DARIO   ROLE_THIS_RELAY = B03 V002 -- THE PORT REFRAMED TO THE RECORD'S OWN DEMAND.
SUPERSESSION = this artifact supersedes STAGE8_DESC_B03_DARIO_V001.md APPEND-ONLY.  V001 remains
  sealed, on the books, and BYTE-UNTOUCHED (581ec505…, 74,533 B, rehashed here).  Its computations
  are carried; its FRAMING is replaced.
DIGEST_RULE = every digest and offset COMPUTED by member 26 and quoted from its output.
SPAN_CONVENTION = DECLARED: ROW (leading pipe through terminating newline), ANCHOR (unique start
  anchor to/through a named end anchor), FIXED (literal [a,b) re-verifying an upstream pin; a
  mismatch is a refusal).  16 of 25 spans are FIXED; 16/16 MATCH.
SUBJECT_READ_RULE = member 11 read ONLY WITH member 12 (overlay V002) and member 13 (DoR-013), per
  Q-931/Q-935.  status(member 11) := RATIFIED_FAMILY_LEVEL_BY_DOR_013, no-member clause carried.
  Section 3 quotes a span of member 11 CONTAINING a stale status surface (F2 inside F1); it is
  carried whole and flagged, never trimmed.
REGISTER_SNAPSHOT = member 22 is live-append and was observed ADVANCING TWICE during this relay.
  Only the Q-212 and Q-223 spans carry weight, and both recompute BYTE-IDENTICAL across both
  advances -- a checked result, not an assumption, and what append-only is supposed to mean.
GENERATOR_REFUSAL = member 26 emits nothing on an unreadable input, an absent OR AMBIGUOUS anchor,
  or a FIXED span missing its upstream digest (Q-920/Q-924).  It refused once this relay, on an
  anchor sought in the wrong member.
ALL_RESULTS = CLAIMED until the opposite-lane check.
CLOSURE_END_BYTE = 00007170
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1029_B03_V002_REFRAME_DARIO_V001.md` | `e762a5bc7a13a44cdc0b20a5e0fdb02ca3594e856c6b8eb4ea7fa07a13db35c9` | the assignment |
| 02 | `workspace/STAGE8_DESC_B03_DARIO_V001.md` | `581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506` | **V001, superseded append-only, byte-untouched** |
| 03 | `workspace/STAGE8_DESC_B03_CHECK_CODEX2_V001.md` | `ecf6f4fecfac60804246203ebdc2606684f3a80997fffcb380345b49ae1c901e` | **the attack that confirmed V001 and ordered four narrowings** |
| 04 | `workspace/STAGE8_DESC_DIAG_B_CODEX2_V001.md` | `ab23d7c09e844712f04e64b79beaefcadd3b14979d960407b24bbc5c2ad609b6` | arm B — the reframe's source |
| 05 | `workspace/STAGE8_DESC_DIAG_A_DARIO_V001.md` | `a5d699ae6dc1f634c180ef6053a751b019dce8994b5968f0d96cbd22ba0fae62` | arm A — the consumer ledger |
| 06 | `workspace/STAGE8_DESC_DIAG_B_CHECK_DARIO_V001.md` | `cf9b577fdefd8704ab10801eef85819b3f24a12257092b95830bb54434951229` | **my cross-check; the reframe is executed on its verdict** |
| 07 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | **THE OLDER DEMAND: P0/P1/P5 — and what "the completed object" IS** |
| 08 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | **v004's `rho_pre` clause, quoted at its span** |
| 09 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` | older signature 1 — the U2 role |
| 10 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md` | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2` | older signature 2 — and `d_U2` |
| 11 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **THE FORCED OBJECT.** READ ONLY, with 12 and 13 |
| 12 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the overlay of record; the read rule |
| 13 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification, by digest |
| 14 | `workspace/STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | **the scope verdict and the provenance row** |
| 15 | `workspace/STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md` | `35f581dee0683add232367f953ec7807e89bb5eac47e34f4ad5d781a87c95be0` | `P_src`'s carrier |
| 16 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | the one exhaustive proof; P's two record spans |
| 17 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | the extension theorem; `res_B` |
| 18 | `workspace/STAGE8_TASK2D_QUASIFREE_BRANCH_RHO_PRE_GOVERNANCE_AUDIT_V001.md` | `8aad619a542aba5991288485509c91a41425aa2fed81fb77d95c73119c0db84d` | **Q-223's artifact — the one the supply map declined on the wrong block** |
| 19 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | the demand map, CLOSED at V008 |
| 20 | `workspace/STAGE8_DESC_AUDIT_CODEX2_V002.md` | `078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d` | the supply map |
| 21 | `workspace/STAGE8_DESC_B02_DARIO_V001.md` | `80fc23ed9687b4ccde1a51e4455e0c1878837c535cd4c5594e839c43f0743d1a` | B02's G2 |
| 22 | `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `6a21aaa115fabf4dc2cc16ff0e1f544d2cb9d130a3590feab2707bfe61d26d1f` | live-append, **observed advancing twice during this relay** (…`6cab1ca8` → …`6a21aaa1`); **the Q-212 and Q-223 spans are byte-identical across both advances** |
| 23 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 24 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 25 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 26 | `workspace/build_b03_v002_pins_v001.py` | `5d6ef64f105a54cf8cd7322c74b3773e7b603c1d67110782b7c1104bb9bd8d1f` | the pin generator; inputs are exactly members 01–26 |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION.  NO COMPARISON TO A MEASURED CONSTANT.
NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.  NO FREEZE.  THE CHOOSE BRANCH UNTOUCHED.
omega_phys AS A JOINT OBJECT IS NEITHER DERIVED NOR CHOSEN.  NO CANDIDATE BEYOND THE RECORD'S OWN
RATIFIED DEFINITION IS CONSIDERED.  NOTHING ADOPTED.  NOTHING REGISTERED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — B03 — DARIO LANE — V002
## RELAY 1029 — `[PLAN:DESC-25]` — THE PORT REFRAMED; THE GAP SURVIVES THE REFRAME

Date: 2026-08-11
Status: **PARTIAL, and the finding is sharper than either outcome the section expected. Restated
from the record's own older receivers, the demand is a positive normalized `rho_pre` ON THE COMPLETED
OBJECT — and the record says what that object is: `A_SRF_CTP`, a completed source-record-field
carrier/algebra, `rho_pre` living on the FULL source-record-field Hilbert space. The forced object —
`d_state` := the unique normalized fixed state of `P_src` — is POSITIVE and NORMALIZED, both proved,
and it lives on the authored finite scalar SOURCE carrier. So two adjectives are satisfied at the
wrong scope and the third adjective IS the scope. THE REFRAME DOES NOT DISSOLVE THE GAP: "completed"
is not scaffolding. It sits in producer P1/P5 and in v004 — receivers arm B itself certifies as
record residue, and older than the U2 assembly that wrote the StatePort. BUT THE FREEDOM CHANGES
CHARACTER, and this is the result that matters: a completed pre-state on the full space DETERMINES
its `B`-restriction, because restriction is exact. The continuum was never a menu of physical
choices — it was the shadow of a missing object. `omega_phys` as a SELECTOR is dissolved; the
completed object is what is open.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 1. What V002 changes, and what it carries [PROVABLE]

V001 is superseded **append-only** and is byte-untouched at `581ec505…`. What carries and what is
replaced, stated before any argument so nothing rides in unlabelled:

```text
CARRIED FROM V001, UNCHANGED
  the 38-constraint ledger and every row's disposition -- re-derived by member 03 with ZERO
    overturns and zero collapsing constraints;
  the record's TYPE-R typings, including UNIQUE_SCALARIZATION_FORCED = false;
  C34's three legs (G4 computes from an object the corpus does not supply; Q-900 typed the pinning
    authored; instrument scope is not record scope);
  C18/C19 -- the source-state uniqueness is real and its codomain bar holds as typed.

REPLACED IN V002
  THE FRAMING.  V001 measured the record against the U2 assembly's nine-field StatePort and its
    "choose or derive" line.  Arm B showed both were authored there; my 1027 cross-check confirmed
    it on a wider search than arm B ran.  V002 measures the record against the OLDER receivers only.
  THE HEADLINE.  V001's §3.1 typed the census TYPE-P | premises: DoR-008; its final lines did not.
    V002 carries the conditionality AND Q-212's void trigger in the headline itself.
```

**I am not defending V001's stop and this artifact is not written to preserve it.** Where the
reframe changes the answer it changes it; where it does not, the reason is at bytes and not at my
preference.

---

## 2. DEMAND = RESTATED FROM THE RECORD [PROVABLE — quoted]

Only older receivers are admitted below. The StatePort packaging and the "choose or derive" line are
excluded as authored, per member 04 as cross-checked at member 06.

**(a) What must exist, and ON WHAT.** Member 07 `[8085,8283)` (ANCHOR), span SHA-256
`375dd96a7b7c3806c063075f7346685efa8fbd8c1eddab9bbb19c5e723157d6d`:

```text
P0. One microscopic source-record-field operator/dynamics B0-like source.

P1. A completed source-record-field carrier/algebra A_SRF_CTP with a common
    dense domain or equivalent domain object.
```

**(b) The state leg.** Member 07 `[8954,9569)` (FIXED), span SHA-256
`8917c67f455bb0b152950c1931891311c3c96029c1ba4100219370038198dbb0`:

```text
P5. A positive normalized `rho_pre` on the completed object and admitted record
    effects `E_r`, with domains, all supplied by the same microscopic source as P0.
```

**(c) The same demand, from the governing principle, at member 08 `[7290,7829)` (FIXED), span
SHA-256 `f9fb7a84ce4e3b954e5444baedc6703d9cc8ec52fe04f41e1b3b6d915b3d5372`** — quoting
`primitive_record_cell_selection_principle_v004.md`:

```text
rho_pre is a positive trace-class initial density operator on the full
source-record-field Hilbert space, normalized by Tr rho_pre=1.
```

and the same span's own closing sentence: *"the completed Hilbert space, physical domain, and
common-origin producer are absent."*

**(d) The role content and the witness.** Member 09 `[37523,38115)` (FIXED) `e16f8b94…` gives U2 as
*"microscopic action/evolution, positive normalized pre-state, inclusive identity, admitted effects,
action/source contact rules, and common domains"*; member 10 `[6266,6883)` (FIXED) `e6093b0f…` adds
the descent witness `d_U2 : (B0_candidate,C0) -> U2`.

```text
THE REFRAMED DEMAND, SIX LEGS, ALL AT OLDER RECEIVERS
  L1  a POSITIVE                                    rho_pre
  L2  NORMALIZED (trace-class, unit trace)          rho_pre
  L3  ON THE COMPLETED OBJECT A_SRF_CTP / the full source-record-field Hilbert space
  L4  admitted record effects E_r, with domains; action/evolution, inclusive identity, contacts
  L5  ONE U2 descent witness d_U2
  L6  COMMON-ORIGIN DELIVERY -- "all supplied by the same microscopic source as P0"
      (+ no post-output supplementation)
```

**L3 is the leg this relay turns on, and it is worth saying why before the answer.** L3 is not a
consequence of the StatePort. It is written into P1, into P5's phrase *"on the completed object"*,
and into v004's *"on the full source-record-field Hilbert space"* — three older loci, one of them a
governing principle. **Arm B removed the route and the packaging. It did not, and could not, remove
L3.**

---

## 3. THE FORCED OBJECT, AND ITS SCOPE [PROVABLE — quoted]

Member 11 `[22842,24541)` (FIXED), span SHA-256
`84a5b7050188448e2a0cc526de0131b297cb734bc96b292968e1e37080b61cf0`, §7.1:

```text
d_state(Omega_prim,N^v003)
  := the unique normalized fixed state of P_src.

The anchor theorem then proves, rather than defines,

d_state(Omega_prim,N^v003)=I_src/Tr_A(I_src).
```

**CUSTODY, DISCHARGED IN PLACE.** That span CONTAINS a stale status surface — overlay pin #7, member
11 `[22882,22958)` (FIXED), span SHA-256
`65dfdedb8e8dc7a513904f4443ab215c08aa9d6c59d5a42ae75eb1395ede0662`, reading
`PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)**`. **Stale, not
governing**: member 12's status predicate at `[15736,15834)` (ROW) `a9675e42…` fixes
`status := RATIFIED_FAMILY_LEVEL_BY_DOR_013`, and member 13 `[355,992)` (FIXED) `bc83e53d…` ratifies
this file by the digest member 26 recomputed and matched. Quoted whole and flagged; trimming a
ratified object to make a quotation tidy is the edit the overlay exists to bar.

**Where the forced object lives.** Member 15 `[16571,16744)` (ANCHOR), span SHA-256
`155721b5ad376b6fb6fc15387603ba4b92d69e9da98ceb882b7d0a2a4fc08df9`:

> *"`P_src` is a normal CPTP preparation channel on the authored finite scalar **source** carrier.
> DoR-013's `d_state` solves its unique invariant state in `State(A_src)`."*

---

## 4. LEG BY LEG — `FORCED_OBJECT = PARTIAL` [PROVABLE]

| leg | disposition | ground |
|---|---|---|
| **L1 positive** | **SATISFIED, AT SOURCE-SECTOR SCOPE** | member 14 `[8902,10649)` (FIXED) `20b0b650…`: *"positivity and unit trace \| independently proved for the source density at fixed A0"* |
| **L2 normalized** | **SATISFIED, AT SOURCE-SECTOR SCOPE** | same row; and §3's anchor theorem proves `d_state = I_src/Tr_A(I_src)`, a normalized state |
| **L3 on the completed object** | **FAILS** | the forced object is a state on `A_src` (member 15). The demand's object is `A_SRF_CTP` / the full source-record-field Hilbert space (member 07 `[8085,8283)`, member 08 `[7290,7829)`). Member 14's own verdict on exactly this: **"PASS ONLY AT SOURCE-SECTOR SCOPE; not a joint-state certificate"** |
| **L4 effects, domains, contacts, evolution** | **NOT SUPPLIED** | DoR-013's ratified content is a state map, a ready map and a law map (member 13 `[355,992)`). Admitted record effects with domains, contact rules and common domains are separate legs of the U2 role and no ratified map delivers them |
| **L5 one `d_U2` witness** | **ABSENT** | member 20's four-map row types `d_U2` **MATERIAL-PARTIAL / REQUIRED MAP ABSENT**; member 21's G2 `[32608,33541)` (FIXED) `5575f6ba…` stops the joint trace at the same place |
| **L6 common-origin delivery** | **PARTIAL** | member 14's provenance row: Gen_Omega *"certifies common-input descent of source state, ready ray, and law"* — **PARTIAL**, with four named absences. Under the reframe the `omega_phys` clause of that row is scaffolding, **but the C0 scalarization and the joint domain are not** — they are L3's object under another name |

```text
FORCED_OBJECT = PARTIAL
  SATISFIED  L1, L2 -- both AT THE WRONG SCOPE
  FAILS      L3 -- and L3 is the scope the other two attach to
  ABSENT     L4, L5
  PARTIAL    L6
```

**The precise statement, because "two of six satisfied" would be a flattering way to read this
table.** L1 and L2 are adjectives *of an object*. Satisfying them for the source-sector object does
not carry them to the completed object, because there is no completed object for them to be true of.
**The honest reading is that the forced object is a correct and proved state of the wrong algebra,
and the reframe did not change which algebra the demand names.**

---

## 5. THE RESULT THE REFRAME PRODUCES [PROVABLE]

### 5.1 The gap is not scaffolding

Arm B's finding, confirmed at 1027, is that the **route** (`omega_phys = Omega_C0 compose i_B`) and
the **packaging** (nine fields, "choose or derive") were authored in the U2 assembly. **Neither is
L3.** L3 is in producer P1/P5 and in v004 — and member 06 §2.4 lists producer P5 among the three
signatures that carry record residue *surviving* the removal of the import.

```text
WHAT THE REFRAME REMOVED   the ROUTE and the PACKAGING
WHAT IT LEFT STANDING      L3 -- a state on the COMPLETED source-record-field object
CONSEQUENCE                the section's obstruction is NOT an artifact of the frame.
                           It is older than the frame.
```

### 5.2 But the freedom changes character — and this is the finding

Member 17 §3.2 makes `res_B : State(A_C0) -> State(B)`, `res_B(Omega) := Omega compose i_B`, exact:
it preserves positivity because `i_B` is a star-homomorphism and normalization because
`i_B(1_B) = 1`. **A state on the completed object therefore DETERMINES its `B`-restriction.** No
choice is taken at the restriction step; the map is a function.

```text
THE CONTINUUM, RE-READ
  V001 displayed a continuum of states on B and asked which one the record forces.
  THE CONTINUUM IS OVER INDEPENDENTLY CHOSEN omega_B.  Member 17's extension theorem
  [15554,15965) (FIXED) e9dafa59… is exactly the statement that a RECORD MARGINAL does not
  determine the other factor -- i.e. that the freedom lives in the JOINT object, not in B.
  ONCE THE COMPLETED OBJECT EXISTS, omega_phys IS ITS RESTRICTION AND IS NOT SELECTED.
  The continuum was never a menu of physical alternatives.  IT WAS THE SHADOW OF A MISSING OBJECT.
```

**This is consistent with C34 rather than a reversal of it, and the distinction matters.** G4 said
*"no independent `omega_B` may enter"* — right about the DIRECTION. Q-900 typed its *pinning*
authored — right about its PROVENANCE. V002 adds the third thing: the record's own older demand
reaches the same direction without the instrument, because L3 plus exact restriction gives it.

### 5.3 What this does to Property P

V001 asked for a condition whose admissible set over `State(B)` is a singleton. **Under the reframe
that is the wrong question** — not because such a condition would be unwelcome, but because the
object it would range over is not where the record locates the freedom. Q-223 (member 22
`[570087,571700)` (ANCHOR) `4bd04c51…`) already narrowed the live object to *"the JOINT STATE/
COVARIANCE CLASS · its COMMON-ORIGIN DESCENT · the RESPONSE REDUCTION it supports"*, and records
that a quasifree-class joint state would make **"the ask = ONE TWO-POINT COVARIANCE"** (member 18
`[25126,25677)` (FIXED) `0668d9d5…`). It also forecloses by name the obvious selector family: *"NO
sealed KMS, passivity, Hadamard, extremality, Gibbs, or thermal selector exists in scope."*

---

## 6. THE ELEVEN CONSUMERS, RE-DISPOSITIONED UNDER THE REFRAME [PROVABLE]

Arm A's eleven, carried at member 05 `[20891,20987)` (ROW) `3118a5ba…` — **a count my own 1027
cross-check WITHDREW as frame-dependent** (member 06 `[36853,36890)` (ROW) `e2e1a755…`). They are
re-dispositioned here against the reframed demand, not restated.

Two dispositions, and the second turns out to be empty:

```text
PRE-STATE(-AND-MEASURE)      the consumer needs the completed pre-state, or a measure descended
                             from it.  Satisfiable from an L3 object -- NOT from the forced object,
                             which fails L3.
STILL-REQUIRES-INDEPENDENT-CHOICE   the consumer needs an omega_B that a completed pre-state would
                             NOT determine.
```

| consumer | disposition |
|---|---|
| B04 StatePort | **PRE-STATE** — the packaging dissolves; its content (a state with representation, density and domain on the completed object) restates as L3 |
| B06 S1 measure | **PRE-STATE-AND-MEASURE** — its would-build names *"the common-origin action/**state**/history-domain package"* |
| B09 S4 domain | **PRE-STATE** — the "scalar physical representation" is a representation *of* the pre-state |
| B13 `d_U2` | **PRE-STATE** — U2's role content carries the pre-state; = L5 + L3 |
| condition 1 | **PRE-STATE** — `d_state(Omega) = rho_pre` |
| condition 7 | **PRE-STATE** — the state's own properties, = L1/L2 at L3's object |
| condition 13 | **PRE-STATE** — the U2 role signature as a delivery constraint |
| condition 18 / K7 | **PRE-STATE-AND-MEASURE** — the base must arise from the common-origin action/state/history-domain package and descend through the orbit map |
| condition 35 | **PRE-STATE (narrowed)** — the route half is scaffolding; the surviving half binds a supplied state to representation and density, which is L3's object |
| K4 | **PRE-STATE (by restriction)** — it requires an actual `omega_hist`, and `omega_hist` is the exact restriction of an L3 object, not an independent choice |

```text
CONSUMERS = 11-DISPOSITIONED-UNDER-REFRAME
  PRE-STATE(-AND-MEASURE)              11
  STILL-REQUIRES-INDEPENDENT-CHOICE     0
```

**Zero is the substantive result of this section, and it is worth stating as plainly as it deserves:
under the reframed demand NO consumer requires a choice. Every one of them is served by a completed
pre-state, which the record demands and does not have.** The selector was an artifact of asking for
`omega_phys` directly instead of asking for the object whose restriction it is.

---

## 7. THE FOUR NARROWINGS, FOLDED [PROVABLE]

Member 03 ordered four documentary repairs. All four are folded here; none is argued with.

**(1) The proof dependency, restated.** V001 advertised *"two independent proofs at two receivers."*
That is too strong. Member 16 `[6680,8157)` (FIXED) `ba3c5741…` is **one exhaustive proof** — the
Riesz–Markov census on the adopted carrier. Member 17 `[15554,15965)` (FIXED) `e9dafa59…` is **an
extension theorem that imports member 16's receiver**: it needs `B iso C(Y)` and the simplex from
member 16 to say the extensions form a continuum. **Correct statement: ONE EXHAUSTIVE PROOF, PLUS A
VERIFIED EXTENSION THEOREM THAT RESTS ON ITS RECEIVER.** The opposite-lane verification of the
extension theorem stands; its independence does not.

**(2) The five unpublished span digests, published.** V001's generator computed 38 spans and its
prose quoted 33; five were generated and never published against an all-quoted claim. They are
published here, re-verified from source:

```text
V001 S05  member 20 [27077,27288)  b1328f6ffe1dcbf6f7202d1929528089bc3f3f34b10f5679d81f363afef59bfa
V001 S16  member 16 [17311,17617)  5a81d03d2c628d39b920aef863688ec21204adccb0efbf6b86b478e2ce02f7d8
V001 S22  member 17 [20615,21172)  ea287648c57fe67944612dc0f0fc819196902acc5f5b5746f83d4560aec3cb2f
V001 S23  member 17 [17252,17629)  37f573355e9ddd08fa59d883fae490149543fbea952507ffb649409c6fe83d8e
V001 S34  member 21 [32608,33541)  5575f6ba332dc8a73083e46b0f8138b235b0a6246333de6173051d26c1b7637b
```

**(3) P is COMPOSED, and labelled so.** V001 presented P as quoted rather than composed. The truth
is mixed and is now stated: **the collapse core is quoted** — member 16 `[18972,19937)` (ANCHOR)
`a8e6fff4…`, the record's own falsifier item 2 — and **the residual package is quoted** — member 16
`[18260,18656)` (ANCHOR) `349938c5…`. **The five-part assembly (P1–P5), the RECORD-FORCED clause, and
the third would-live site are MINE.** The third site was an analogy to the source-sector anchor, not
a receiver, and V001 should not have listed it beside two real loci. `COLLAPSE_PROPERTY` is therefore
typed **COMPOSED**, not quoted — and §5.3 supersedes it in any case.

**(4) The residue meta-label corrected.** V001 reported eight hits and typed all eight as the verb
carrying a live receiver. One of the eight was **meta-language** — the artifact's own naming of the
class it was counting — not a live-receiver occurrence. The corrected typing: seven live-receiver
occurrences and one meta-label.

```text
NARROWINGS = 4/4-FOLDED
```

---

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  V001's 38-row ledger and its dispositions, re-derived by member 03 with zero overturns; C34,
  C18/C19; arm B's three provenance findings as cross-checked at member 06; producer P0/P1/P5;
  v004's rho_pre clause; the U2 role and d_U2; DoR-013's ratified maps, family-level adoption and
  no-member clause; member 14's field-by-field inhabitance audit; res_B's exactness; Q-212's void
  condition; Q-223's narrowing, its different-algebras finding and its thermal-selector foreclosure;
  member 12's read rule; all fences.

DERIVED HERE:
  the six-leg restatement of the demand from older receivers only, with L3 identified as the leg
    the reframe cannot remove because it predates the frame;
  the leg-by-leg disposition of the forced object, and the statement that L1/L2 are adjectives of
    an object that L3 denies exists;
  THE CHARACTER FINDING: res_B is exact, so a completed pre-state DETERMINES its B-restriction --
    the continuum is the shadow of a missing object, not a menu of alternatives;
  the re-disposition of all eleven consumers under the reframe, and the zero;
  the reconciliation of that finding with C34 and Q-900 (direction, provenance, and the record's own
    older route to the same direction being three different claims).

SELECTED HERE:
  nothing.  No state, member, measure, covariance class member, carrier, route, amendment, selector,
  freeze, rank, preference or candidate is chosen, supplied, or approached.  No candidate beyond the
  record's own ratified definition was considered.  No stop is lifted by fiat and none is defended.

SCALING WEIGHTS: none consumed, formed, fixed, compared, or evaluated.  Every scale symbolic.
```

---

## 9. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.**
  - **A frame defect was not flattened into a dissolved obstruction** — §5.1: the route fell, L3 did
    not, and reporting "the reframe dissolves the problem" would have been the flattering error this
    relay was most exposed to.
  - **Satisfaction at the wrong scope was not flattened into satisfaction** (§4).
  - **A determination was not flattened into a choice, nor a choice into a determination** (§5.2):
    `res_B` is a function, and saying so does not supply its input.
  - **An extension theorem was not flattened into an independent proof** (§7.1).
  - **A composed property was not flattened into a quoted one** (§7.3).
  - **A superseded artifact was not flattened into a withdrawn one**: V001's computations stand and
    are carried; only its framing is replaced.
  - **A stale surface was not flattened into a governing status** (§3), and a **ratified object was
    not trimmed** to make a quotation read better.
- **BUILDER-NEVER-VERIFIES:** CLAIMED until the opposite-lane check. I did not re-adjudicate member
  03's narrowings, arm B's findings, or Q-900/Q-212/Q-223; I executed and folded them.
- **BLIND held.** `omega_phys` untouched as a joint object — neither derived nor chosen. No consumer
  was evaluated; §6's dispositions are structural, and arm A's asymmetry finding carries: a
  structural ledger can refute gauge, never confirm it.
- **CUSTODY:** archive-side only. Two files written in `workspace/`, both sealed. V001 byte-untouched.
  No register, plan, tracker, git, commit or push action. **NO SUBAGENT DELEGATION.**
- **BYTE AUDIT:** all 26 members rehashed at full paths at seal time by member 26; all 25 spans
  computed by it; **16 FIXED spans re-verify upstream pins, 16/16 MATCH.** Closure declared at byte 0
  with its end computed as a fixed point under a fixed-width field; 27-token pre-closure scan, 0 hits.
- **RESIDUE, per Q-921:** removed-class grep run over authored prose (fenced blocks excluded — scope
  declared) before sealing. **ONE RAW HIT: this bullet's own naming of the verb *reproduce*, one
  clause above.** I count my own sentence rather than exempt it, and I read the loci rather than
  predict them — a draft of this bullet said two, and the second was a §7 occurrence I expected to
  be there and which the scan does not find. Fenced blocks carry none. **The single hit is not the
  retired process-history class**; this document makes no claim that any build re-runs to identical
  bytes and carries no historical subsection for that vocabulary. **CLAIMED, not clean** — the hand
  that wrote the prose ran the check, and the gate settles opposite-lane.
- **GENERATOR CUSTODY (Q-920/Q-924):** member 26's declared inputs are exactly members 01–26; it
  refused once this relay, on an anchor sought in the wrong member, and emitted nothing until fixed.

---

## 10. The certificate

Per Q-913/Q-917/Q-921/Q-930: **the certificate is the digest table.** Members 01–26 at full paths and
the twenty-five span digests quoted in place with computed offsets and declared shapes — **including
the five V001 generated but did not publish.** None of the retired claim classes is asserted.
**There is nothing below the digests.**

---

## 11. Final lines

```text
CLOSURE = declared-first (byte position, scan)
     byte 0; closure end 7170 under a fixed-width field; 27-token pre-closure scan, 0 hits, CLAIMED.
DEMAND = RESTATED-FROM-RECORD (spans)
     SIX LEGS, older receivers only, StatePort packaging excluded as authored.  L1 positive, L2
     normalized/trace-class/unit-trace, L3 ON THE COMPLETED OBJECT, L4 effects/domains/contacts/
     evolution, L5 one d_U2 witness, L6 common-origin delivery with no post-output supplementation.
     THE COMPLETED OBJECT IS NAMED BY THE RECORD: member 07 [8085,8283) 375dd96a… -- "P1. A
     completed source-record-field carrier/algebra A_SRF_CTP with a common dense domain"; member 07
     [8954,9569) 8917c67f… -- "a positive normalized rho_pre ON THE COMPLETED OBJECT"; member 08
     [7290,7829) f9fb7a84… -- v004: "on the FULL SOURCE-RECORD-FIELD Hilbert space", and in the same
     span, "the completed Hilbert space, physical domain, and common-origin producer are absent."
FORCED_OBJECT = PARTIAL (legs)
     L1 SATISFIED and L2 SATISFIED -- both AT SOURCE-SECTOR SCOPE, member 14 [8902,10649) 20b0b650…
     ("positivity and unit trace … PASS ONLY AT SOURCE-SECTOR SCOPE; not a joint-state certificate").
     L3 FAILS: d_state is the unique normalized fixed state of P_src (member 11 [22842,24541)
     84a5b750…) and P_src is a channel on the SOURCE carrier (member 15 [16571,16744) 155721b5…), so
     the forced object is a state of A_src while the demand names A_SRF_CTP.  L4 NOT SUPPLIED, L5
     ABSENT, L6 PARTIAL.  THE HONEST READING, not the flattering one: L1/L2 are adjectives OF an
     object, and L3 is the object -- two adjectives satisfied at the wrong scope is not two legs
     closed.  THE FORCED OBJECT IS A CORRECT, PROVED STATE OF THE WRONG ALGEBRA.
COMMON_ORIGIN = GAP (span)
     member 14 [8902,10649) 20b0b650…: Gen_Omega certifies common-input descent of source state,
     ready ray and law -- PARTIAL, four absences named.  Under the reframe the omega_phys clause of
     that row is scaffolding; THE C0 SCALARIZATION AND JOINT DOMAIN ARE NOT -- they are L3 under
     another name.  No certificate shape is stated because no object exists to certify.
CONSUMERS = 11-DISPOSITIONED-UNDER-REFRAME
     PRE-STATE(-AND-MEASURE) 11 | STILL-REQUIRES-INDEPENDENT-CHOICE 0.  UNDER THE REFRAMED DEMAND NO
     CONSUMER REQUIRES A CHOICE.  Every one is served by a completed pre-state.  K4 is the decisive
     row: it needs an actual omega_hist, and omega_hist is the EXACT RESTRICTION of an L3 object,
     not an independent selection.  Arm A's count of eleven, which my own 1027 check withdrew as
     frame-dependent, is REPLACED by this re-disposition rather than restated.
JOINT_QUESTION = OPEN (span)
     OPEN AS AN OBJECT, DISSOLVED AS A SELECTOR, and the two must not be merged.  DISSOLVED: res_B
     is exact -- positivity from i_B being a star-homomorphism, normalization from i_B(1_B)=1 -- so
     a state on the completed object DETERMINES its B-restriction; omega_phys is not chosen once L3
     exists.  THE CONTINUUM WAS THE SHADOW OF A MISSING OBJECT, NOT A MENU OF PHYSICAL ALTERNATIVES,
     and member 17's theorem [15554,15965) e9dafa59… says as much: it is the statement that a
     MARGINAL fails to determine the other factor, i.e. that the freedom lives in the JOINT object.
     OPEN: L3 itself, at member 07 [8085,8283) 375dd96a… and member 08 [7290,7829) f9fb7a84…, whose
     own words are that the completed Hilbert space is ABSENT.  Q-223 (member 22 [570087,571700)
     4bd04c51…) narrows what is open to the joint state/covariance class, its common-origin descent
     and the response reduction, with the reduction's shape at member 18 [25126,25677) 0668d9d5…:
     a quasifree-class joint state makes "the ask = ONE TWO-POINT COVARIANCE".
     THE SECTION'S OBSTRUCTION IS OLDER THAN THE FRAME.  Arm B removed the route and the packaging;
     L3 sits in producer P1/P5 and in v004, and member 06 §2.4 lists producer P5 among the residue
     that SURVIVES the removal.  The reframe did not dissolve the gap; it correctly renamed it.
NARROWINGS = 4/4-FOLDED
     (1) ONE exhaustive proof (member 16 [6680,8157) ba3c5741…) PLUS a verified extension theorem
     that imports its receiver (member 17) -- "two independent proofs" was too strong.  (2) The five
     generated-but-unpublished span digests S05/S16/S22/S23/S34 are PUBLISHED at §7 and re-verified
     from source.  (3) P is typed COMPOSED: collapse core and residual package quoted at member 16
     [18972,19937) a8e6fff4… and [18260,18656) 349938c5…, but the five-part assembly, the
     RECORD-FORCED clause and the third would-live site are MINE -- and the third was an analogy,
     not a receiver.  (4) The residue meta-label corrected: seven live-receiver occurrences and one
     meta-label, not eight of one kind.
OUTPUT_INSPECTION = NONE-CERTIFIED
     Consumers read structurally, never evaluated.  No downstream object opened for what it would
     need omega_phys to be.  No candidate beyond the record's own ratified definition considered.
     No freeze proposed; the choose branch untouched and, under §6's zero, unnecessary.
CHAIN_INVOKED = false
     No unstarted paste addressed to this lane sits behind 1029; 1028 is Codex 2's.
VERB_AUDIT_SELF = CLEAN
     "Derived" occurs only in the FREEDOMS block for reading work performed, in sealed status names,
     and in quotations.  No selector selected, no member bound, no stop lifted or defended.
```

All findings CLAIMED until the opposite-lane check.

**The step to press first is §5.2, because it is the load-bearing new claim and it is mine.** That
`res_B` is exact is member 17's, and that a completed pre-state determines its restriction follows
from it. What is mine is the inference that this **dissolves the selector**, and a lane could
reasonably press it from two directions: whether every consumer that appears to need `omega_phys`
truly needs only the restriction (§6 says yes for all eleven, structurally, and a structural reading
cannot see a dependence that survives restriction), and whether `A_C0` and `A_SRF_CTP` are the same
completed object — **I have treated them as the same receiver and the record uses both names.** If
they are not the same, §5.2's restriction argument needs a transport it does not have, and that is
the sentence to check first.

**The second thing to press is L4/L5**, which I dispositioned NOT SUPPLIED and ABSENT quickly
because L3's failure already decided the outcome. Neither disposition was attacked, and a reframed
build that repaired L3 would meet them next.
