CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 11 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = PROVENANCE AUDIT ONLY (no challenge, no amendment, no entry)
DIGEST_RULE = every pinned digest computed from its file at seal time
CLOSURE_END_BYTE = 4879
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_983_INDUCTION_PROVENANCE_DARIO_V001.md` | `430c20271b76eee38379bc7c55688bfc598c7a7ab68c121eacbd4bb6c60b161b` | assignment |
| 02 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md`, state port `[10824,11928)`, `rho_pre` role `[7290,7829)` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab`; `63f743bfcc1df70bccb8cf1c0e3041c7a9202009c944c33adbfe8ae75da3535e`; `f9fb7a84ce4e3b954e5444baedc6703d9cc8ec52fe04f41e1b3b6d915b3d5372` | **the forcing consumer: `StatePort_U2_008` and its single density** |
| 03 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md`, placement `[11354,13537)` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98`; span `ad81601717e80b8872c367e95af2fce50129ed10e2601455a6b6356cf2f786f7` | the sealed equation form `omega_phys(b) = Tr(rho_pre pi_omega(b))` |
| 04 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md`, `res_B` `[13916,14477)`, non-uniqueness `[15554,15965)`, composition `[16298,16972)`, SM-4 `[18649,18864)` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782`; `684efbe4784bcf745711c2a99acdea43ce1ae78d0395bd60e22155d20cf5742a`; `e9dafa5963128305d212f83b8c6617bfa47293f70bf91f5d7c0729fdb6828532`; `67b224f03a4250db1f9e93b9c92dd2a7566768db91b8a74c7f1a1d9240c99c39`; `1f49a5ae2bf1d776778dbe21408909917537e12d3146397df1678e81a48b9deb` | the proximate level: `res_B`, SM-4, and the record's own non-uniqueness theorem |
| 05 | `STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md`, firewall `[7027,8499)` | `8e9a09c104f4b6352263591037b2e0bb9a82b659aa1b6276cdd48117f872acec`; span `e6926428b9c66c0a5e78f51daae4903cd3be2cebbd17bda0c9ce05880529e8b6` | the record/history direction firewall, closing one named wider route |
| 06 | `STAGE8_LAW_READY_STATE_SEALED_COMMON_ORIGIN_DETERMINATION_V001.md`, `OriginLawState_N` `[18627,20232)` | `6745b3c5d08d1c1df89dbcbce301ce66315bf640c26ff41332f77e456f477c7a`; span `f18549f66fb0babb9fec4f79aa514b6da85f97607e9d025cde517e45c9c8bbc2` | `Cert_target_independence` and `Cert_no_post_output_supplementation` |
| 07 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md`, propagation `[21900,22459)` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1`; span `891a21c7eac0ca5d78a7d18a4b6f4a233bf0e6a0a8bcad909c139fd3d94421a8` | the proximate pinning under audit |
| 08 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md`, G4/G5 `[38169,39014)` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab`; span `ae1f79d9b7f24d2e0899f1116e38db3b51f72f0d89cc9983f8a00399f0076cee` | `res_B` in the governing gauntlet; the marginal-equality demand |
| 09 | `STAGE8_AXN_DIRAC_FEASIBILITY_DARIO_V001.md`; `STAGE8_AXN_HOTHER_FEASIBILITY_DARIO_V001.md`; `STAGE8_AXN_K4_PROVENANCE_DARIO_V001.md` | `19021e25a0c971a783e64096c328ec4ee199748073c80cdcf5e9aad5728bb403`; `7899ea69ac9fde4e34725ec424305f002194cc2c6d1fc7f2eb7068f5d8d948e1`; `bb503ebe33b499e9146fbe691b84001f10072cac7c44a16d383ce803e6e0fda4` | my 977 and 981, whose load-bearing premise this audits; my 978, whose method it repeats |
| 10 | `STAGE8_AXN_DIRAC_FEASIBILITY_CHECK_CODEX2_V001.md`; `STAGE8_AXN_HOTHER_CHECK_CODEX2_V001.md` | `a522ca5392227e1f6fc353d57c9af14bc15593a2779165f7315dd50c670a7f71`; `7f93e6246882cb409c483945603587277df84dcfddad997ea8fb410461df94c0` | the opposite lane's checks of 977 and 981 — pointers, quoted for status only |
| 11 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md`; `QUESTIONS_SETTLED_REGISTER_V001.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`; `68e5d002747db3f7f077c5d4f256dc09e07b62827f65cc22614baa51df9c4b7a` | state pin, process law, S01–S37, register (Q-814, Q-818 read before classification) |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO CHALLENGE IS FILED, NO AMENDMENT IS PROPOSED, NO ENTRY IS MADE.
THE AUTHOR'S 979 TESTIMONY IS RECORDED AS AUTHORSHIP EVIDENCE AND CARRIES ZERO RULING WEIGHT (BR-1).
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN INDUCTION-PINNING PROVENANCE — DARIO LANE — V001
## RELAY 983 — `[PLAN:AXN-BUILD-D82]` — FORCED, OR AUTHORED ONTO THE RECORD SURFACE?

Date: 2026-08-10
Status: **RULING: PINNING_ORIGIN = FORCED-BY. The sealed U2 state port is a single closed tuple
carrying one density, one representation of the joint algebra, and the history functional together;
restriction is the only map that reads that tuple with the tuple's own components. The pinning is a
transcription. My 977 and 981 stand on record ground, and the reopener the relay names does not
exist at this receiver.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

This audit runs against my own load-bearing premise. I recorded at pickup that the outcome cuts both
ways and that I have no stake in which; the ruling below would have been written the same way had the
bytes gone the other direction, and §5 states plainly what my two verdicts would have lost.

---

## 1. The proximate readings, displayed even though they are the wrong level [PROVABLE]

My 978 established the discipline: display the proximate reading, then rule at the source. Here the
proximate reading survives **two** levels before it breaks.

### 1.1 Level 0 — the instruments

Member 07 at `[21900,22459)`: `-> induced_history_functional_sha256 = sha256(exact restriction
object)`. Member 08's G4: `omega_hist := res_B(Omega_C0) := Omega_C0 compose i_B`, with *"Verify
positivity, normalization, declared representation, and equality to the `JAC-07` history marginal"*
and *"The slice `E_out` is not a substitute and no independent `omega_B` may enter."*

Both are governing acts authored by the producing lane. The author's 979 testimony that the pinning
was intended as written is, as the relay states and as BR-1 requires, evidence of authorship and not
of forcing; a producer-declared object may accuse, never exculpate. **Read at this level alone, the
ruling is AUTHORED-UNFORCED.**

### 1.2 Level 1 — the state-algebra map, which claims the derivation for itself

Member 04, relay 907, predates both instruments. It states the map at `[13916,14477)`:

> *"The canonical contravariant state map is `res_B : State(A_C0) -> State(B)`,
> `res_B(Omega_C0) := Omega_C0 compose i_B`. It preserves positivity because `i_B` is a
> star-homomorphism, and it preserves normalization because `i_B(1_B)=1_A_C0`."*

and enforces it at `[18649,18864)`:

> *"`SM-4` | **History binding:** `omega_phys=Omega_C0 compose i_B`; this equality is a construction
> output, not a later preference. | prevents an independently chosen `omega_phys` from being attached
> after the fact"*

Its own FREEDOMS block then reads *"DERIVED HERE: the canonical tensor-factor inclusions `i_R` and
`i_B`; **the contravariant restriction `res_B`**; the record-slice conditional expectation `E_out`
…"*. **Read at this level alone, the ruling is still AUTHORED-UNFORCED, and now with an explicit
authorship claim in the producing lane's own voice.**

### 1.3 The pattern this makes, which is worth more than this relay

This is the third occasion in the same chain. At 978 the seven-condition source claimed *"DERIVED
HERE: … the weighted-projectivity equation"* while the condition was forced one document up. Here
907 claims `res_B` while it is forced two documents up. **A `DERIVED HERE` line marks where a
condition was first written down in the AXN line, not where it was first required.** A provenance
audit that stops at the first document claiming derivation will return AUTHORED-UNFORCED on
conditions that are forced, and the error is systematic rather than occasional. I record it for the
registrar as a durable reading rule.

---

## 2. The forcing consumer, at bytes [PROVABLE]

### 2.1 The port

Member 02 at `[10824,11928)`, sealed and predating every instrument in this line:

```text
StatePort_U2_008 := (
  omega_phys : B -> C,
  H_omega := completion of E_C0 / N_omega,
  pi_omega : A_C0 -> B(H_omega),
  rho_pre in trace-class(H_omega),
  rho_pre >= 0,
  Tr_omega(rho_pre)=1,
  state_domain,
  state_descent_map,
  state_provenance_certificate
).
```

Read the signature rather than the prose. The tuple carries **exactly one** density `rho_pre`,
**exactly one** representation `pi_omega`, and that representation is of the **joint** algebra
`A_C0` — and it carries `omega_phys : B -> C` **in the same tuple**. It has no second state, no
second density, no independent `omega_B` slot, and no automorphism, expectation, limit, or
normalization component. The role of `rho_pre` is fixed at `[7290,7829)`: *"a positive trace-class
initial density operator on the full source-record-field Hilbert space, normalized by
`Tr rho_pre=1`."*

### 2.2 The equation form, sealed

Member 03 at `[11354,13537)` names what the port would have to instantiate — *"an equation of the
form"* —

```text
omega_phys(b) = Tr(rho_pre pi_omega(b)).
```

`b` lies in `B`, while `pi_omega` is defined on `A_C0`; the expression typechecks only through the
booked inclusion. Member 04 at `[16298,16972)` writes the same composition with the inclusion
restored:

```text
Omega_C0(a) := Tr(rho_pre pi_omega(a)),
omega_phys(b) = Omega_C0(i_B(b)) = Tr(rho_pre pi_omega(i_B(b))).
```

**That is the restriction, and it is what the sealed port already said.** The instruments did not
introduce it; they transcribed a composition the U2 receiver's own signature had fixed.

### 2.3 Why restriction is the unique satisfier

Let `Phi : State(A_C0) -> State(B)` be any candidate induction. To be evaluated at all, `Phi` must be
built from components the port supplies. The port supplies `rho_pre`, `pi_omega`, and the booked
`i_B` — nothing else that maps between the two algebras. Every alternative therefore has to import a
component the tuple lacks, and each import is closed by name in the record:

| Candidate wider route | What it must import | Where the record closes it |
|---|---|---|
| `Omega_C0 compose i_B compose alpha`, `alpha` a `*`-automorphism of `B` | `alpha`, a choice from a continuum of homeomorphisms of `Y` | member 06's `Cert_no_post_output_supplementation` and `Cert_target_independence`; SM-4's rejection test |
| `omega_B compose E`, `E` a conditional expectation onto `B` | a scalar functional `omega_B` | member 04, at bytes: `E_out(1_R tensor b)=b`, so it *"returns the input history observable, not a scalar functional on it. To obtain a scalar one must still compose with some `omega_B in State(B)`, which imports exactly the member being sought"* |
| the record state's own restriction | transport of a record-algebra state into the history slot | member 05's direction firewall: *"record-state restriction is not a doubled-history marginal"*; and *"It is not transported into the U3 measure slot"* |
| a limit or alternate normalization | a normalization not already fixed | `Tr_omega(rho_pre)=1` with `i_B(1_B)=1_A_C0`; member 04 proves `res_B` preserves normalization for exactly this reason |
| an `omega_phys` chosen and attached afterwards | the functional itself | SM-4's rejection test names this case verbatim |

My 977 ran four independent probes across both instruments for an alternative induction route —
conditional expectation, limit, alternate normalization, and any *"other than restriction"*
permission — and found none. Member 02 supplies the reason those probes came back the way they did:
**the port has no slot for one.**

### 2.4 The record's own reason, which is sharper than any of mine

Member 04 at `[15554,15965)` proves why a wider receiver is not a small liberty:

> *"every `omega_B in State(B)` produces a product state `Omega_(out,B) := omega_out tensor omega_B`
> on `R_inf tensor_min B` with the same record marginal `omega_out`. Since the sealed history-state
> simplex is a continuum, the record marginal alone admits a continuum of such extensions. This is an
> algebraic non-uniqueness theorem at the current receiver."*

An induction receiver wider than restriction is therefore not an alternative computation — **it is a
free selection from a continuum the record has already proved is undetermined by record data.** That
is the VOID CONDITION's exact target, and it is why the port was specified with a single density
rather than with a state and a history functional side by side. The register carries the same reading
at Q-814: *"record data plus tensor structure leave the full continuum; the physical selection MUST
come from the origin side."*

**PINNING_ORIGIN = FORCED-BY**, member 02 `[10824,11928)`, with the equation form at member 03
`[11354,13537)` and the enforcement row at member 04 `[18649,18864)`.

---

## 3. What this settles [PROVABLE]

The pinning that carries my 977 and my 981 is a record fact at this receiver, not an authorial
preference. The two impossibility results therefore rest on record ground wall to wall, and the
governing-act amendment the relay describes — a lawful second induction receiver — has no consumer
behind it to satisfy: it would have to supply the very continuum selection member 04 proved is
undetermined, and the port that would receive it has no field to put it in.

The opposite lane's checks are cited as pointers only, for status: member 10 records
`VERDICT = IMPOSSIBILITY-CONFIRMED` on both my 977 and my 981. I take no verification credit from
them and no ruling weight; they are quoted so this artifact states the section's condition accurately.

Subject to 982, which is another lane's and which I do not anticipate, the section closes as answered.

---

## 4. The constraint envelope [PROVABLE — N-A]

The relay conditioned the envelope on an AUTHORED-UNFORCED ruling. The conditional did not fire, so
**no envelope is displayed.** I note the reason explicitly rather than supplying one anyway: an
envelope is amendment-design input, and the relay barred proposing the amendment. The reach statement
in §5 is a different kind of object — a fact about a theorem already proved — and is displayed.

---

## 5. The reach of 981's theorem, stated honestly [PROVABLE]

The relay asks whether **any** induction landing on a positive stage-marginal family hits the same
wall. It does, and the reason is that the theorem was never about the induction.

### 5.1 What it binds

981's theorem is a statement about the **output**: any family `(lambda_N)` of positive measures
satisfying K4 in the sealed coordinates is `lambda_N = mu tensor delta_1^(N-1)`. The proof takes
`m = 0` in the charged-transfer condition, gets `integral (1 - r_(N+1)) d lambda_(N+1) = 0`, takes
real parts, and uses `Re(1-r) >= 0` against a positive measure. **It never mentions how `lambda`
arose.** Any induction receiver whatever — restriction, a wider one, or one not yet written — whose
output is a positive stage family lands inside that class or fails K4. On this point a wider receiver
buys nothing at all.

### 5.2 What it does not bind, including against my own verdict

The theorem does **not**, by itself, make H-OTHER empty, and I state this plainly because it bounds
what my 981 established:

- The solution class is a **one-circle family**, not a single point. For `mu` other than `delta_1`
  the functional `mu tensor delta_1 tensor delta_1 tensor ...` is equivalent to **neither** named
  control, so it could carry `NEITHER_EQUIVALENCE_PROVED` honestly, and it passes K4 (my 981's
  machine check C returned `0.000e+00` for random `mu`).
- Emptiness in 981 came from **faithfulness**, and faithfulness reached the H-OTHER functional
  through the pinning: faithful pairing, plus the pinned restriction route, plus my 977 theorem that
  the restriction of a faithful pairing along `i_B` is faithful. The H-OTHER row itself demands
  sealing, not faithfulness — I said so in 981 §1.1 and it remains true.
- **So the relay's premise is exactly right, and I confirm it against my own results rather than
  defending them: the pinning is load-bearing for both 977 and 981.** Had it been authored-unforced,
  a receiver that broke the faithfulness inheritance would have left H-OTHER with a nonempty,
  positive, K4-passing candidate class.

The opposite lane reached the same observation independently: member 10's H-OTHER check records
`HORNS = CONFIRMED (pass/fail controls; not exhaustive of the free-mu solution class)`.

### 5.3 What would still stand in that counterfactual

Two things, and both are already of record:

1. The free-`mu` class is still **not faithful** — its support is `supp(mu) x {1} x {1} x ...`, proper
   in `Y` — so it would satisfy an H-OTHER row only if an amendment also released the faithfulness
   inheritance, which is a second and separate act.
2. **K7 still refuses it.** Member 04 of my 981's closure classifies identity-supported Dirac
   families as *"a newly selected history law with no common-origin descent … a consistency control,
   not an admissible S1 construction"*, and the tower with a free first circle is the same kind of
   object with one coordinate relabelled. The descent materials K7 would need are themselves
   `TYPE-U`.

### 5.4 One scope refinement to the class description

The freedom in the first circle is an artifact of the family index beginning at `N = 1`: the first
coordinate is never an appended coordinate, so the charged-transfer condition never reaches it. Under
an index admitting a stage-0 trivial group the same argument would force it too and the class would
collapse to the identity-supported Dirac control exactly. This refines the description of the class
and changes no verdict in 981, because faithfulness fails for every `mu`.

---

## 6. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  StatePort_U2_008 and the rho_pre role; the scalarization placement equation form; res_B, SM-4, and
  the non-uniqueness theorem; the direction firewall; OriginLawState_N's two certificates; the
  propagation chain; G4's res_B and marginal-equality demand; my own 977 theorem and 981 theorem as
  already-sealed results; all gates.

DERIVED HERE:
  the three-level provenance trace and its ruling;
  the unique-satisfier argument read off the port's signature, with each wider route matched to the
    named record clause that closes it;
  the reading rule that a DERIVED-HERE line marks locus and not provenance;
  the reach statement separating what 981's theorem binds (the output class, for every induction)
    from what it does not (emptiness, which came from faithfulness and hence from the pinning);
  the stage-index refinement of the free-mu class.

SELECTED HERE:
  nothing.  No amendment, no envelope, no challenge, no disposition, no entry, no branch, no
  functional, no measure.  Neither control is promoted.  No fork is ranked.

SCALING WEIGHTS: none consumed, fixed, or substituted.  p_[A], r_0, r_ch were not read, evaluated,
  or formed into a ratio.
```

---

## 7. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A transcription was not flattened into an authorship,
  nor an authorship into a forcing. A `DERIVED HERE` line was not flattened into a provenance verdict.
  An existence statement (`res_B` exists in closed form) was not flattened into a forcing statement;
  the forcing came from the port's signature, not from the map's existence. A theorem about outputs
  was not flattened into a theorem about induction routes. A counterfactual reach statement was not
  flattened into an amendment proposal. The opposite lane's confirmations were not flattened into
  verification credit for my own work.
- **BR-1 held, and it cost something.** The author's 979 testimony is the natural exculpatory object
  here and it received zero weight; the ruling rests on member 02's signature, which is not the
  producer's declaration about intent but a sealed specification predating the act.
- **VOID CONDITION held**, and named as the law that the wider-receiver route would instantiate.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **PE-1..PE-15:** pointer-only, zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** this relay audits the premise of my own 977 and 981 rather than
  re-verifying their arithmetic; the opposite lane's checks are cited for status only, and I claim no
  credit from them. The reach statement in §5 narrows what my own results established.
- **CUSTODY:** archive-side only. One artifact, lane-tagged, sealed. No register, plan, tracker, git,
  commit, or push action. Nothing adopted, nothing registered.
- **BUILDER-B INDEPENDENCE:** `evaluator_build_A/` and `checks/` were not read.
- **BYTE AUDIT:** all eleven members re-hashed at seal time; all eleven cited spans recomputed and
  matched. The register's digest has advanced since my 981 and is pinned here at its current value.
  Closure declared at byte 0, end computed on bytes as a fixed point; pre-closure 27-token scan run
  over authored prose, 0 hits.

---

## 8. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 4879; scan 0 hits)
PINNING_ORIGIN = FORCED-BY (STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_
          DETERMINATION_V001.md, 2b6291227b7ac596..., StatePort_U2_008 at [10824,11928)
          63f743bfcc1df70b...: one rho_pre, one pi_omega on the JOINT algebra A_C0, and
          omega_phys : B -> C in the same closed tuple; the equation form sealed at TASK2D
          [11354,13537) ad81601717e80b88...; enforced by SM-4 at [18649,18864) 1f49a5ae2bf1d776...)
PROXIMATE = displayed, at TWO levels: the instruments (level 0, where the reading is authorship and
          the author's 979 testimony agrees) and the state-algebra map (level 1, whose own FREEDOMS
          block claims "DERIVED HERE: ... the contravariant restriction res_B").  Both read
          AUTHORED-UNFORCED; the forcing sits one level further up.  Reading rule recorded: a
          DERIVED-HERE line marks locus, not provenance -- third instance in this chain.
IF_FORCED = unique-satisfier shown.  Every wider route must import a component the port lacks, and
          each import is closed by a named clause: automorphism -> Cert_no_post_output_supplementation
          and SM-4; conditional expectation -> E_out returns an observable, not a scalar, and needs
          the sought member; record-state transport -> the direction firewall [7027,8499); alternate
          normalization -> Tr(rho_pre)=1 with i_B(1_B)=1.  The record's own non-uniqueness theorem
          [15554,15965) supplies the reason: a wider receiver is a free selection from a continuum
          proved undetermined by record data.
IF_AUTHORED = N-A (ruled FORCED; no envelope displayed, since an envelope is amendment-design input).
          THEOREM REACH STATED ANYWAY at §5: 981's theorem is about the OUTPUT and binds every
          induction landing on a positive stage family -- but it does NOT by itself empty H-OTHER.
          The class is a one-circle family; for mu != delta_1 it is equivalent to neither control.
          Emptiness came from faithfulness, which came from the pinning.  The relay's premise that
          977 and 981 both rest on the pinning is CONFIRMED, against my own results.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is §2.3's unique-satisfier
argument: it reads necessity off a tuple's **signature**, and a signature constrains what the tuple
contains rather than what a future receiver might be given. If a lane can name a sealed consumer that
accepts a history functional NOT drawn from the joint density — one this hunt did not surface — then
the ruling weakens to AUTHORED-UNFORCED and §5.2's counterfactual becomes live. That is the sentence
to attack, and I would rather it were attacked than assumed.
