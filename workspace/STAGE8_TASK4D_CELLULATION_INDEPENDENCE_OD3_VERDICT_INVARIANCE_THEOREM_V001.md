# Stage 8 Task 4d Cellulation Independence / O-D3 Verdict-Invariance Theorem v001

Date: 2026-08-02  
Lane: CODEX LANE 2  
Task: 4d  
Status: RESULT / MAXIMAL INVARIANCE THEOREM PROVED / O-D3 NOT DISCHARGED  
Register head checked at completion: Q-259

## 0. Lead determination

**The Q-241 move proves a maximal kinematic theorem, but it does not discharge
cellulation independence or charter O-D3.** Every final-`K_square` verdict that
depends only on the unitary-equivalence class of the completed square data is
invariant across all 1,088 support filtrations. This includes exactly the
Q-241 result class: cellular counts and grading up to transport, homological
ranks, compact group, Gate-3 form class, Gate-4 differential class, named loop
holonomy, and unitary invariants of the completed operator.

The five public continuum consumers do **not** factor through `T_cyl` or that
unitary-orbit quotient. Their required maps are not merely unproved: their
domains require geometric/refinement and physical-response fields absent from
`T_cyl`. Moreover, the 1,088 objects are linear-extension **support
filtrations of one fixed unfilled `K_square`**, not full `J_ref` realizations,
not cellulations of a continuum region, and not the charter's decompositions
of an admitted atom into causal diamonds. No sealed map identifies these
three choice spaces.

Consequently there is no lawful pair `(J,J')` in the 1,088 on which any of the
five public consumer requirements can presently be evaluated. Reporting five
identical `TYPE-U` outcomes as “verdict invariance” would turn common
unavailability into a theorem. The exact residue is two-stage:

```text
R1  support filtration -> full geometric realization/refinement functor
    missing: finite algebra, representation, source incidence, intertwiner,
             refinement maps, and one frozen topology;

R2  geometric realization -> decomposition-independent completed response
    missing: connection/curvature transport, response naturality,
             common-refinement coherence, boundary subextensivity, and the
             public physical interface.
```

Charter O-D3 lives in `R2`, over causal-diamond decompositions. The 1,088-way
Q-241 theorem lives strictly before `R1`, over coordinate filtrations. Thus:

```text
MAXIMAL_K_SQUARE_VERDICT_INVARIANCE_THEOREM_PROVED = true

THE_1088_ARE_FULL_J_REF_REALIZATIONS = false | TYPE-R |
  test: compare Q-217/Q-241 member fields with Q-258 T_ref signature

THE_1088_ARE_CHARTER_OD3_DIAMOND_DECOMPOSITIONS = false | TYPE-R |
  test: fixed-square linear extensions versus disjoint causal-diamond covers
        of an admitted atom with exact four-volume sum

PUBLIC_CONSUMERS_FACTORING_THROUGH_T_CYL = 0_of_5 | TYPE-R |
  test: domain/codomain factorization audit in Section 5

PUBLIC_CONSUMER_VERDICT_INVARIANCE_PROVED = 0_of_5 | TYPE-U |
  would-build: R1 and R2 above, then execute the five pairwise theorems

CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U |
  would-build: instantiated Ref/J_ref/tau_ref plus response naturality,
               BoundaryCert and public-interface diagrams

CHARTER_OD3_DISCHARGED = false | TYPE-U |
  would-build: completed-response independence across every admissible
               causal-diamond decomposition of the same atom

CHARTER_DEFINITION_STATUS_CHANGED = false | TYPE-U |
  constraint: the charter's own iff condition has not fired

DISCHARGE_ACCOUNTING = KINEMATIC_RESULT_CLASS_ONLY__0_OF_5_PUBLIC__0_OF_1_OD3
```

This is not a physical refutation of cellulation independence. It is a
`TYPE-U` result with the quantifier domains and the first non-factoring arrows
now named exactly.

No physical response, root, coupling, scale, or measured comparison is
evaluated.

## 1. Preflight, currency, authorities, and scope

### 1.1 Preflight

```text
DOES_THE_OBJECT_EXIST = PARTLY
  O-D3, T_cyl, the 1,088 support family, and five consumer signatures exist;
  full J_ref realizations and consumer evaluations do not

IS_THE_VERSION_CURRENT = true_through_Q259
  Q-259 is a Gen_Omega proposal under review and is not assumed

ARE_THE_INPUTS_PRESENT = YES_FOR_THE_MAXIMAL_KINEMATIC_THEOREM;
                         NO_FOR_THE_REQUESTED_PUBLIC_THEOREMS
```

### 1.2 Controlling authorities

| Authority | Lines / content used |
|---|---|
| `STAGE8_TRANSPORT_FUNCTOR_CHARTER_V001.md` | `:41-68`: definition and five pins; `:169-190`: O-D1–O-D4; `:231`: definition iff O-D3 discharged |
| `STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md` | `:213-243`: `T_cyl`; `:245-299`: `T_ref`, `T_phys`, five consumers; `:303-342`: discrete/completion type split; `:459-532`: scope ceiling and missing realization; `:555-615`: response/boundary residue |
| `STAGE8_TASK2F_K_SQUARE_CHOICE_INVARIANT_FALSIFIER_CONTENT_DETERMINATION_V001.md` | `:192-264`: result/object split and arbitrary-pair conjugacy theorem; `:266-287`: complete result table |
| `STAGE8_TASK2F_FINITE_INCIDENCE_REALIZATION_FUNCTOR_FORCING_PROTOCOL_RESULT_V001.md` | `:164-200`: seven-field full realization signature and missing middle fields; `:202-249`: support-family definition and scope |
| `STAGE8_DOWNSTREAM_SMOOTH_CONNECTION_REQUIREMENT_AUDIT_V001.md` | `:199-217`: downstream obligation audit and exact smooth/continuum consumers |
| Q-258 | `T_cyl` 5/5; public consumers 0/5; cellulation independence TYPE-U |
| Q-259 | proposed signed-incidence primitive; not ratified and no theorem-level transport supplied |

### 1.3 Search scope

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha-program-archive/cleanroom_output
/Users/bgm/MB Work/alpha-program-archive/supervision
/Users/bgm/MB Work/alpha_supervision
```

Excluded:

```text
a32_holdout/custodian_private/ [not entered]
.git/
binary/media payloads
archive mirrors as independent authorities
superseded versions except for lineage comparison
```

Case-insensitive query families actually run:

```text
O-D3 | decomposition-independence | cellulation independence
T_cyl | T_ref | T_phys | J_ref | Ref | retraction naturality
public electromagnetic connection | Maxwell tensor | retarded CTP Hessian
Ward | transversality | Thomson | response pullback
1,088 | filtration | linear extension | support map | unitary conjugacy
diamond decomposition | common refinement | boundary subextensive
Gen_Omega | signed-incidence
```

`rg` was unavailable; `find`, `grep`, `sed`, and `nl` were used. The earlier
space-containing-path `xargs` attempt was discarded after it split paths; all
load-bearing sources were then opened directly by exact path.

### 1.4 Symbol/domain separations

```text
o in LE(K_square)   a linear extension/support filtration of one fixed complex
J_ref               a full seven-field geometric realization functor member
cellulation         an object of the unbuilt Ref category
delta in Dec(C)     a causal-diamond decomposition of one charter atom C
T_cyl               sequential label-algebra norm colimit
T_ref               geometric refinement stitching theorem family
T_phys              public-response equivalence theorem family
verdict invariance  equality of a stated predicate's output
object invariance   identity/equivalence of the carrier itself
```

No shared word identifies any pair in this table.

## 2. The three choice spaces are different

### 2.1 The 1,088 family

Q-217/Q-241 define, for the fixed unfilled square,

```text
F_support(K_square)
  = {(o,{T_j^o}) : o is a linear extension of the face order}.
```

A member supplies support, degree, prefix naturality, orientation convention
and unit-cell basis transport. It does not supply the full realization
signature

```text
(stage/object assignment,
 label/degree map,
 finite algebra map,
 carrier map,
 representation intertwiner,
 incidence/form/operator witness,
 inclusion/refinement naturality witness).
```

The missing middle fields are exactly why Q-217 calls the full family
uninstantiated.

### 2.2 The Task-4d realization family

Q-258 requires a category `Ref` of finite oriented record complexes and
certified refinements, together with

```text
D_ref, eta_K, P_KR, Resp_K,
```

one convergence topology, connection/curvature maps, response naturality and
common-refinement coherence. These data range across different complexes and
their refinements. A linear extension of the cells of one already-completed
square supplies none of those morphisms.

### 2.3 Charter O-D3

The charter's exact O-D3 family is the family of admissible decompositions of
the **same atom `C`** into disjoint causal diamonds, each carrying full local
record time, with exact four-volume sum. O-D3 asks whether the **completed
response** is independent of which such decomposition is chosen
(`STAGE8_TRANSPORT_FUNCTOR_CHARTER_V001.md:182-188`).

This family varies diamonds and their covering of an atom. The 1,088 family
varies only the order in which the eight fixed vertices/edges of one unfilled
square are assigned sequential coordinates.

### 2.4 No bridge among them

No sealed map was found of either type

```text
F_support(K_square) -> Ob(Ref) or Mor(Ref),
F_support(K_square) -> Dec(C),
```

and their declared fields are incompatible even before a search result is
used. Therefore:

```text
SUPPORT_FILTRATION_EQUALS_GEOMETRIC_CELLULATION = false | TYPE-R |
  test: one fixed-complex coordinate order versus a complex/refinement object

SUPPORT_FILTRATION_EQUALS_DIAMOND_DECOMPOSITION = false | TYPE-R |
  test: cell-order data contain no diamonds, atom cover, disjointness, volume
        sum, exhaustion unit, or completed response

SEALED_SUPPORT_TO_REF_MAP_FOUND = false | TYPE-S |
  scope: roots and queries in Section 1.3

SEALED_SUPPORT_TO_OD3_DECOMPOSITION_MAP_FOUND = false | TYPE-S |
  scope: roots and queries in Section 1.3
```

This domain mismatch alone prevents the 1,088 theorem from quantifying over
O-D3's admitted family.

## 3. The maximal theorem that does prove

### Theorem 3.1 — final-square unitary-verdict invariance

Let `o,o'` be arbitrary members of the 1,088 support family. Q-241 proves

```text
B_o' = P_(o,o')^* B_o P_(o,o')
```

for a unitary permutation/fiber map `P_(o,o')`, and gives the analogous
degreewise transport for the differential. Let `V` be any predicate/function
on the completed square data satisfying

```text
V(P^* B P, transported differential, transported grading)=V(B,D,grading)
```

for every permitted unitary/gauge/orientation transport. Then

```text
V(o)=V(o')
```

for every pair `o,o'`.

**Proof.** Substitute the Q-241 conjugacy equation into the invariance property
of `V`. The hypothesis removes the coordinate transport. Since `o,o'` were
arbitrary and Q-241's family coverage reaches every linear extension, the
result holds over all 1,088. QED.

The theorem reaches:

```text
cell count and absence of C_2;
degree split up to transported grading;
Gate-1 comparison group;
Gate-3 form/adjoint class;
Gate-4 unit-weight differential class;
incidence rank and cycle rank;
named path-ratio holonomy;
unitary invariants of the completed square operator.
```

It does not say the raw matrices, prefix complexes, or support maps are equal.
Q-241 proves the raw displays differ.

```text
MAXIMAL_K_SQUARE_UNITARY_VERDICT_CLASS_COVERED = true
RAW_SUPPORT_OBJECTS_IDENTICAL_ACROSS_1088 = false | TYPE-R |
  test: distinct linear extensions and distinct prefix complexes remain
```

### Theorem 3.2 — conditional `T_cyl` factorization lemma

Let a future consumer requirement `C` have, for every full geometric
realization `J`, a proved factorization

```text
C_J = C_bar compose L_J,
```

where `L_J` lands in the fixed `T_cyl` norm/module colimit and where a proved
natural isomorphism `u_(J,J')` satisfies

```text
L_J' = u_(J,J') L_J,
C_bar u_(J,J') = C_bar.
```

Then `C_J=C_J'` for every pair. This is ordinary substitution plus the colimit
universal property; no canonical `J` is needed.

This lemma is useful but conditional. Its first premise is exactly what must
be checked for each public consumer, not assumed from the existence of
`T_cyl`.

## 4. Charter-level accounting

### 4.1 Which five pins O-D3 governs

The charter names five pins at `:65-68`:

1. cell time extent;
2. the volume symbol;
3. minimum separation;
4. `tau_R` units; and
5. the diamond associated with a non-diamond atom.

Items 1-5 of the charter definition already fix those pins. O-D3 does not
choose or alter any of their values. It governs a different statement: whether
the **completed response** is independent of the nonunique admissible
decomposition and therefore whether the charter remains a definition rather
than becoming a new principle.

```text
CHARTER_PINS_WHOSE_VALUES_ARE_SELECTED_BY_OD3 = 0_of_5 | TYPE-R |
  test: charter items 1-5 fix the pins before O-D3 is stated

OD3_GOVERNS_CHARTER_DEFINITION_STATUS = true
OD3_GOVERNS_COMPLETED_RESPONSE_DECOMPOSITION_INDEPENDENCE = true
```

### 4.2 The iff clause

The protected-status line is exact:

> `charter_remains_a_definition_iff = O-D3_discharged`

Because no completed-response map on the O-D3 decomposition family exists,
the antecedent has not been established. The iff does not fire in any public
scope.

```text
OD3_IFF_ANTECEDENT_PROVED = false | TYPE-U |
  would-build: an executable response for every admitted decomposition and a
               proof that all results agree, or a canonical scheme theorem

CHARTER_PROMOTED_FROM_DEFINITION_TO_DERIVED_RULE = false | TYPE-U |
  constraint: no O-D3 discharge exists
```

The five pins remain in-force adopted definitions. This artifact neither
invalidates them nor upgrades them to derived results.

## 5. Five public-consumer factorization theorems

For each consumer `C_i`, define the desired statement

```text
Inv_i : for all full admissible J,J', Verdict(C_i through J)
        = Verdict(C_i through J').
```

The proposed quantifier cannot lawfully use the 1,088 support members in place
of full `J_ref` members. The table gives the first exact non-factoring term.

| Consumer from Q-258 | Required field absent from `T_cyl` | Factorization verdict | Invariance verdict |
|---|---|---|---|
| public electromagnetic connection (`Q-258:288-289`; downstream audit `:210-214`) | cell/edge/path realization, local source calculus, physical connection one-form and public quotient | `false | TYPE-R` — `T_cyl` has character labels but no edge/path/base or physical connection map | `NO_VERDICT | TYPE-U` — build `eta_conn` and public equivalence |
| local spacetime curvature/Maxwell tensor (`Q-258:290-291`) | filled `2`-cells, face area/coframe, exterior derivative/Hodge data, curvature naturality | `false | TYPE-R` — unfilled `K_square` and label colimit do not carry the required two-cell data | `NO_VERDICT | TYPE-U` — build `eta_curv`, Hodge/locality and refinement coherence |
| completed connection-history response and retarded CTP Hessian (`Q-258:292`) | physical source topology/calculus, quotient, measure/contour, boundary/domains, `Resp_K` | `false | TYPE-R` — no response object exists in the `T_cyl` signature | `NO_VERDICT | TYPE-U` — build P2-P6 plus response pullback |
| continuum Ward/transversality (`Q-258:293`) | spacetime-indexed response kernel, longitudinal differential, physical domain and boundary certificate | `false | TYPE-R` — finite graph/gauge covariance is not the continuum differential identity | `NO_VERDICT | TYPE-U` — build response class and Ward naturality theorem |
| compact-flux-to-Thomson matching (`Q-258:294`) | physical compact-flux restriction, completed response, regime/matching map and public convention | `false | TYPE-R` — no matching or physical evaluation map is in `T_cyl` | `NO_VERDICT | TYPE-U` — build public matching after response and threshold control |

### 5.1 Proof pattern for each `false | TYPE-R`

`T_cyl`'s domain and codomain are fixed at Q-258 `:213-241`: sequential
character-label groups, their group C-star algebras, the doubled CTP algebra,
standard modules, and canonical finite-coordinate retractions. Q-258
`:459-469` proves that this signature contains no cell, edge, boundary,
incidence operator, coframe, local derivative, or response assignment.

Each consumer signature contains at least one of those absent fields as a
required input. Therefore no map of the stated consumer type can factor solely
through the present `T_cyl` object. Adding the missing field to the supposed
factorization changes the middle object from `T_cyl` to the unbuilt
`T_ref/T_phys` package. That is not factorization through `T_cyl`; it is the
would-build.

### 5.2 Why identical unavailability is not invariance

All five consumer evaluations presently stop before an output. Their common
status is `TYPE-U`, not a common pass/fail verdict. The equality

```text
NO_VERDICT = NO_VERDICT
```

is metadata equality, not equality of consumer outputs. Treating it as
`Inv_i` would make every unbuilt family automatically independent of every
choice.

```text
COMMON_TYPE_U_STATUS_COUNTS_AS_VERDICT_INVARIANCE = false | TYPE-R |
  test: the consumer maps have no values to compare
```

## 6. Located residue and counterexample hunt

### 6.1 No variant pair can yet be executed

A minimal `(J,J')` pair with different public verdicts cannot be exhibited
because the full `J_ref` family is not instantiated. This is `TYPE-U`, not
evidence that no variant pair exists.

```text
PUBLIC_CONSUMER_VARIANT_PAIR_EXECUTABLE = false | TYPE-U |
  would-build: two full admissible J_ref members plus the relevant consumer
               map and certificates

PUBLIC_CONSUMER_VARIANCE_REFUTED = NO_VERDICT
```

### 6.2 What the 1,088 actually disagree about

They disagree about exactly the sequential order and prefix support history:

```text
o=(c_1,...,c_8),
K_j^o={c_1,...,c_j},
T_j^o(e_i)=the cell basis vector selected at prefix j.
```

They do **not** disagree about the completed target square or its
unitary-equivalence invariants. They contain no data with which to disagree
about geometric refinement, a filled face, a coframe, physical response,
boundary behavior, or a public matching map.

Thus the located variance is **coordinate/prefix variance**, and Q-241 already
proves it becomes inert for final-square unitary verdicts. It is not O-D3's
residue.

### 6.3 O-D3's actual residue

O-D3's residue is the response-bearing difference between two distinct
admissible causal-diamond decompositions of the same atom after completion:

```text
Delta_resp(delta,delta')
  := Resp_complete(C;delta)-Resp_complete(C;delta').
```

To show it vanishes requires:

1. the admitted decomposition family and common-refinement relation;
2. an executable response on every decomposition;
3. pullback/naturality across common refinements;
4. exact volume-weight compatibility; and
5. a boundary/tail estimate making the intensive residual vanish.

None of these is supplied by a choice of cell order on `K_square`.

```text
OD3_RESIDUE_OBJECT = COMPLETED_RESPONSE_DIFFERENCE_ACROSS_DIAMOND_DECOMPOSITIONS
OD3_RESIDUE_OBJECT_BUILT = false | TYPE-U |
  would-build: the five fields immediately above
```

## 7. Bearing of Q-259 / proposed `Gen_Omega`

Q-259 reports a proposal, not an adoption. Its rooted signed-incidence
primitive supplies a proposed **one-cell carrier-descent presentation** and the
local signed write. Even if it survives review and is ratified, it does not by
itself supply:

```text
a family of finite complexes and refinements;
the seven-field J_ref realization functor;
a map from the 1,088 support filtrations to admissible cellulations;
causal-diamond decompositions of a general atom;
connection/curvature response naturality;
boundary subextensivity; or any public consumer map.
```

Therefore it does not equal either residue `R1` or `R2`.

```text
GEN_OMEGA_ASSUMED_IN_THIS_THEOREM = false | TYPE-S |
  scope: every proof and verdict in this artifact

GEN_OMEGA_SIGNED_INCIDENCE_SUPPLIES_OD3_RESIDUE = false | TYPE-R |
  test: compare its one-cell state/law output signature with Section 6.3

GEN_OMEGA_SIGNED_INCIDENCE_SUPPLIES_FULL_J_REF = false | TYPE-R |
  test: the proposal contains no refinement category, algebra/representation
        family, response naturality, or topology
```

## 8. Discharge accounting

| Scope | Family quantified over | Result |
|---|---|---|
| final `K_square` unitary/gauge/orientation verdict class | all 1,088 support filtrations | **DISCHARGED** by Theorem 3.1 |
| raw prefix/support object | all 1,088 support filtrations | **NOT INVARIANT, TYPE-R**; orders/prefixes differ |
| five public continuum consumers | full `J_ref` family | **0/5, TYPE-U**; family and maps unbuilt |
| Task-4d geometric cellulation independence | `Ref` and its cofinal refinement families | **TYPE-U** |
| charter O-D3 | causal-diamond decompositions of each admitted atom | **TYPE-U** |

The charter's definition-status iff fires in none of the last three rows. The
kinematic row is a real theorem, but it is not a scoped O-D3 discharge because
its family and output type are different.

```text
OD3_FULLY_DISCHARGED = false | TYPE-U
OD3_DISCHARGED_FOR_NAMED_PUBLIC_CONSUMERS = 0_of_5 | TYPE-U
OD3_RESIDUE_LOCATED = true
  [STRUCTURAL LOCATION RESULT; NOT A DISCHARGE]

TASK4D_GEOMETRIC_CAP_LIFTED = false | TYPE-U |
  would-build: R1 plus R2

J_REF_NEEDS_TO_BE_FORCED_BEFORE_ANY_INVARIANCE_THEOREM = false | TYPE-R |
  test: a full family plus verdict invariance would suffice without choosing a
        member, exactly as Q-241 demonstrates

J_REF_FULL_FAMILY_MUST_BE_INSTANTIATED_BEFORE_VERDICT_INVARIANCE = true
  [STRUCTURAL REQUIREMENT]
```

The correct next theorem does not select `J_ref`. It first instantiates the
full outcome-blind realization family, then proves that each completed response
consumer factors through a common-refinement cocone or locates a genuine
variant pair.

## 9. Kill-passes

### 9.1 No weakened consumer

No public connection was replaced by finite edge transport; no Maxwell tensor
was replaced by loop holonomy; no continuum Ward identity was replaced by
finite graph covariance; no Thomson matching was replaced by a compact flux
label. Those weaker objects are legitimate finite results but are not the
named consumers.

### 9.2 Verdict invariance is not object invariance

Theorem 3.1 explicitly permits different coordinate matrices and prefix
objects. It proves equality only after applying a unitary-invariant verdict.
No object identity is transported.

### 9.3 No symmetry-only proof

Every positive theorem uses the exact Q-241 conjugacy equation or the explicit
conditional factorization diagram. “Plausible by symmetry” is used nowhere.

### 9.4 No imported realization or continuum

No smooth base, coframe, measure, contour, filled face, Hodge star,
refinement category, or physical response is supplied by standard practice.

### 9.5 No hypothetical flag leakage

Every conditional or future `true` statement is marked as a structural
requirement or placed inside a theorem hypothesis. Asserted results are
separated from would-builds.

## 10. Final typed board

```text
T_CYL_RETRACTION_NATURALITY_AVAILABLE = true | TYPE-P |
  premises: DoR-008; inherited from Q-258

K_SQUARE_ARBITRARY_PAIR_CONJUGACY_AVAILABLE = true
MAXIMAL_K_SQUARE_VERDICT_INVARIANCE_THEOREM_PROVED = true
K_SQUARE_FINAL_VERDICT_CLASS_DISCHARGED_ACROSS_1088 = true

K_SQUARE_SUPPORT_OBJECTS_IDENTICAL = false | TYPE-R
FULL_J_REF_FAMILY_INSTANTIATED = false | TYPE-U
THE_1088_ARE_FULL_J_REF_MEMBERS = false | TYPE-R
THE_1088_ARE_OD3_DECOMPOSITIONS = false | TYPE-R

PUBLIC_EM_CONNECTION_FACTORS_THROUGH_T_CYL = false | TYPE-R
LOCAL_MAXWELL_TENSOR_FACTORS_THROUGH_T_CYL = false | TYPE-R
PHYSICAL_RETHESS_FACTORS_THROUGH_T_CYL = false | TYPE-R
CONTINUUM_WARD_FACTORS_THROUGH_T_CYL = false | TYPE-R
THOMSON_MATCHING_FACTORS_THROUGH_T_CYL = false | TYPE-R

PUBLIC_CONSUMER_INVARIANCE_PROVED = 0_of_5 | TYPE-U
CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U
CHARTER_OD3_DISCHARGED = false | TYPE-U
CHARTER_DEFINITION_IFF_FIRED = false | TYPE-U
TASK4D_GEOMETRIC_CAP_LIFTED = false | TYPE-U

LOCATED_VARIANCE_WITHIN_1088 = PREFIX_AND_COORDINATE_SUPPORT_ONLY
LOCATED_OD3_RESIDUE = COMPLETED_RESPONSE_DIFFERENCE_ACROSS_DIAMOND_DECOMPOSITIONS

GEN_OMEGA_USED_AS_PREMISE = false | TYPE-S |
  scope: this artifact
TARGET_OUTPUT_USED_TO_DEFINE_INVARIANCE = false | TYPE-S |
  scope: this artifact
FENCE_BLOCKED_STRUCTURAL_RESULT = false | TYPE-S |
  scope: this artifact

alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
```

No gate, register, plan, tracker, decision, or sealed source was edited.
