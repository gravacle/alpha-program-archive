# STAGE 8 — AXN — THE DECISION-INDEPENDENT CARRIER CONSTRUCTIONS
## DARIO LANE — RELAY 928 — `[PLAN:AXN-BUILD-D27]`

## 0. Preflight

Relay 928 verified before reading at
`ef929c06971f0c8ae1d0da8debd441713471c5d0acdfd4487057dd15f40e49bf`. Lane guard read DARIO; the
pickup ACK was written before content access. `PROGRAM_STATE_BRIEF_V005.md` verified and read before
task work at `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`. All subjects
verified against their own sidecars. The output name was clear.

---

## 1. Law-9b closure — declared first

```text
C_928 = {
 1  RELAY_PASTE_928_CARRIER_CONSTRUCTIONS_DARIO_V001.md
      ef929c06971f0c8ae1d0da8debd441713471c5d0acdfd4487057dd15f40e49bf
 2  supervision/PROGRAM_STATE_BRIEF_V005.md
      e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
 3  STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md                     [my 927 — the field list]
      4440af4f8d4a75c7e4b026d4fd901e4e290676f928275897bb55ce803b4c03db
 4  JOINT_ANCHOR_DECISION_INSTANCE_V002.md                              [my 925 — the stops]
      72191e0115d6f36d2327236e7a6d16e21f953422ba3fb2188b75e3db009cea99
 5  STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md                         [the schema]
      58b966ed371b23b29b9e3ceed280eb30c804484becb2b17ca6ea465668e951bc
 6  STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md                         [tensor presentation]
      a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782
 7  STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DOR008_ASSEMBLY_AND_FIRST_RESTRICTION_TEST_V001.md
      1467ab9850022931e68dc9ffa625d95099d8f6bb74d734dc9d0466f169bd00b6
 8  STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md   [E_ch's declared scope]
      1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0
 9  R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md            [R_c, one labelling]
      40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9
10  STAGE8_GAMMA_K_COMPLETED_ALGEBRA_EXTENSION_ATTEMPT_V001.md          [R_c, the other labelling]
      2198524f9f4b4f048953526e4084ae7282e85edce1767e410aef50b013ed0ab1
11  R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md                  [R_N and iota_NM]
      10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995
}
```

| key | bytes | span SHA-256 | content |
|---|---|---|---|
| `TENS` | member 6 `[10436,10550)` | `7cae6616e1efa006575161d98811d8e3c1beb588dde2b1f820e0787877dc8235` | `A_SR := A_src (x)_min R_inf`, `B ≅ C(Y)`, `A_C0 := A_SR (x)_min B` |
| `EMB` | member 6 `[10550,11209)` | `f3a376bab6831abfb4a85e2fb91eff37f4b6280ed5be6b5ccf1bd0d683498cb5` | the canonical **unital** arrows `i_R`, `i_B` with their formulas |

**Name probe.** `STAGE8_AXN_CARRIER_CONSTRUCTIONS_DARIO_V001.md` and its sidecar: clear at pickup.

---

## 2. Gates, standing, and the fence

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

All constructions **CLAIMED** until the opposite-lane check. **The bar is DERIVED WITH ZERO FREEDOM,
every step displayed from a pinned sealed source.** An item that is natural, canonical, or the only
thing anyone would write is still a freedom if the sources do not force it; each such item stops with
its freedom named.

**Fence held.** The four decision-dependent constructions — the structural rank record, the entry
freeze receipt, the induced history functional, the equivalence/nonclassification certificate — were
**not touched**. §6 records where one of my items ran into that fence and stopped rather than reached.

**BLIND held.** No rank read, no ratio computed or stated, no fiber compared. §5 runs closest to the
fiber and stays at the algebra's structure.

---

## 3. Item 1 — `joint_identity` `I_C0` — **DERIVED, zero freedom**

### 3.1 The three factors are unital, evidenced by the sealed formulas themselves

`TENS` gives the presentation and `EMB` gives the arrows:

```text
A_SR := A_src (x)_min R_inf ,   B ≅ C(Y) ,   A_C0 := A_SR (x)_min B
i_R : R_inf -> A_C0 ,  i_R(r) := 1_A_src (x) r (x) 1_B
i_B : B     -> A_C0 ,  i_B(b) := 1_A_SR (x) b
```

I do not need to argue unitality from outside: **the sealed formulas use `1_A_src`, `1_B` and
`1_A_SR` as existing elements**, and member 6 calls both arrows *"canonical **unital** injective
star-homomorphisms."* Independently: `R_inf` is the inductive limit of `R_N = (x)_(j=1)^N M_3(C)`
under `iota_NM(A) = A (x) I_(M-N)` (member 11) — unital algebras under unital embeddings; and
`C(Y)` for compact `Y` has the constant function `1`.

### 3.2 The derivation, and where the freedom would have been

```text
1. Each factor is unital.                                    [EMB uses 1_A_src, 1_B, 1_A_SR]
2. The minimal tensor product of unital C*-algebras is unital, with unit the tensor of the units.
3. A two-sided unit in any ring is UNIQUE: if e and f are both units, e = e f = f.
4. Therefore  I_C0 = 1_A_src (x) 1_R_inf (x) 1_B = 1_A_SR (x) 1_B,  and no alternative exists.
```

**Step 3 is the whole zero-freedom claim**, and it is a one-line ring argument admitting no choice: a
second unit is provably the same element, not an alternative. Nothing here selects, defaults, or
interprets.

### 3.3 A second, independent route from the same span

The sealed arrows are unital, so each carries its own unit to the joint algebra:

```text
i_R(1_R_inf) = 1_A_src (x) 1_R_inf (x) 1_B
i_B(1_B)     = 1_A_SR (x) 1_B
```

Both land on the same element. Verified on a finite model of the presentation:

```text
|| I_C0 - identity ||                                  = 0.000e+00
|| 1_A_SR (x) 1_B  -  1_A_src (x) 1_R_inf (x) 1_B ||   = 0.000e+00   (associativity)
|| i_R(1_R_inf) - I_C0 ||                              = 0.000e+00
|| i_B(1_B)     - I_C0 ||                              = 0.000e+00
```

**The object the survey found to have zero declaration sites is forced by the presentation that was
entered.** It is constructed here, not chosen.

```text
IDENTITY = DERIVED
  I_C0 := 1_A_src (x) 1_R_inf (x) 1_B  =  1_A_SR (x) 1_B
  sources: TENS and EMB, member 6.   freedoms consumed: NONE.
```

---

## 4. Item 2 — `stage_index_and_limit_rule` — **STOPPED, and the source names the freedom**

### 4.1 What the entered realization does supply

Member 7 supplies a stage category and passes a first restriction check:

> *"at stage `N=1`, the assembled object retains the sealed one-record factor `M_3(C)`, the finite
> source-record algebra, and its represented action exactly through the tensor-unit embedding."*

Member 6 §2.3 supplies the history leg's cylindrical system: `X_N = Hist_+,N x Hist_-,N --q_N--> Q_N
≅ U(1)^N`, with `B ≅ C(Y)` as its limit. Member 11 supplies the record leg's `R_N` and `iota_NM`
with `R_inf` as its limit.

### 4.2 The freedom, in the realization's own words

Member 7 continues:

> *"The ratified presentation has only Q-201's sequential record-stage category `N<=M`; it
> **deliberately has no finite-incidence-complex restriction functor**. The sealed composition-loop
> carrier is the different object `C_0(K_square) direct-sum C_1(K_square)`. **No sealed map relates
> that carrier to the sequential C0 module. Choosing `N=4` from the four vertices or four edges would
> be an unratified identification.**"*

**The realization anticipates the exact act the field would require and forbids it.** The schema asks
for the *"exact finite/cylindrical system and limit target"* of the joint carrier; supplying one
would mean fixing the stage index across the carrier, and member 7 states that choosing the index
from the complex is unratified.

A second freedom sits alongside it: the **source leg has no sealed finite chain at all** — my 924
established, from that lineage's own authority correction, `genuine_finite_rank_continuum_restriction_
constructed = false`. So a joint stage rule would have to stage two legs from sealed systems and the
third from an object the record says is unbuilt.

```text
STAGE_RULE = STOPPED
  FREEDOM 1 : the stage index for the joint carrier. Member 7 supplies a sequential record-stage
              category, DELIBERATELY no finite-incidence-complex restriction functor, and names
              "choosing N=4 from the four vertices or four edges" as an unratified identification.
  FREEDOM 2 : the source leg's finite chain is unconstructed of record (924's authority table).
```

---

## 5. Item 3 — `joint_superselection_map` `E_joint` — **STOPPED, two freedoms**

### 5.1 `E_ch` is declared on the source sector only

Member 8 declares the superselection data at the source: `E_ch` appears as a component of the
primitive tuple, and the traceless superselected space is

```text
Delta_0^ss := { Delta in T_1(H_src^A) : Tr_A(Delta) = 0 and E_ch(Delta) = Delta }
```

with the family condition `P_src E_ch = E_ch P_src`. Every occurrence is on `T_1(H_src^A)`. **No
sealed source extends `E_ch` across the tensor factors or declares a joint superselection.** My 927
recorded `E_joint` as occurring only inside the instrument, where it is demanded.

### 5.2 Freedom 1 — triviality off the source factor is a claim, not a consequence

Writing `E_joint = E_ch (x) id_R_inf (x) id_B` is the obvious extension. **Obvious is not forced.**
It asserts that joint superselection acts trivially on the record and history factors, which is a
statement about where charge lives, not an algebraic consequence of the tensor presentation. The
presentation is compatible with a nontrivial action on either factor.

### 5.3 Freedom 2 — two sealed sources label the record carrier differently

Checked at bytes, and this is the sharper of the two:

```text
member 9  (the parent spec lineage)   R_c = span{ |r_c>, |p_c>, |e_c> }
member 10 (the composition derivation) R_c = span_C{ |r_c>, |p_Q,c>, |e_Q,c> }
```

Same cell, same dimension, same role — **different labelling, and the difference is exactly a charge
subscript on the pointer and edge states.** Whether the record carrier's basis carries charge labels
bears directly on whether `E_joint` may act trivially there. Resolving the discrepancy is a
determination about the record sector's charge content; adopting either labelling to derive
`E_joint` would decide it silently.

I report the discrepancy and resolve nothing.

```text
SUPERSELECTION = STOPPED
  FREEDOM 1 : triviality of the joint superselection off the source factor — a physical claim.
  FREEDOM 2 : two sealed sources label R_c differently, with and without the charge subscript,
              and the question bears directly on FREEDOM 1.
```

---

## 6. Item 4 — `restriction_and_limit_certificates` — **STOPPED, gated twice**

The relay asks for certificates in JAC-14's exact-replay format, *"each certificate quantified over
the entered carrier."* Member 5's `JointAnchorCertificateSuite.v001` requires, among its fields:

```text
a0_rank_fiber_sha256 : lowercase_sha256
finite_stage_inventory : exhaustive ordered content-addressed list
for_each_stage : { delta0_basis, phi_restriction_matrix, ... }
restriction_and_limit_square_receipts : exhaustive ordered list
```

**Gate 1 — the stage inventory.** `finite_stage_inventory` and the per-stage block quantify over the
carrier's stages. §4 stopped that rule, so there is no stage set to quantify over.

**Gate 2 — the fence.** `a0_rank_fiber_sha256` is a field of the suite itself, and the A0 fiber is
among the four decision-dependent objects this relay expressly fences off. **My item ran into the
fence and I stopped rather than reached across it.**

What can be said without producing the suite, and is recorded as material rather than as a
certificate: the record leg's restriction squares are verifiable in isolation — at 924 I re-ran the
tensor-unit inclusion and its relations at `0.000e+00` residuals, and `TENS`/`EMB` give the
commuting-range statement for `i_R` and `i_B`. Those are legs, not the quantified suite the field
requires.

```text
CERTIFICATES = STOPPED (PRODUCED-0)
  GATE 1 : no stage inventory exists to quantify over — item 2's freedom.
  GATE 2 : the suite requires a0_rank_fiber_sha256, which is decision-dependent and fenced
           off from this relay. Stopped at the fence, not reached across it.
```

---

## 7. Tally

```text
DERIVED (1) : joint_identity I_C0, with the ring-uniqueness step carrying the zero-freedom claim
              and a second independent route through the two sealed unital arrows.
STOPPED (3) : stage_index_and_limit_rule — freedom named BY THE SOURCE ITSELF;
              joint_superselection_map — two freedoms, one of them a discrepancy between
                two sealed sources' labelling of the record carrier;
              restriction_and_limit_certificates — gated on item 2 and on the fenced fiber.
```

The relay's standard was that a partial delivery with honest stops beats a full one with a quiet
choice. This delivers **one of four**. I record that plainly rather than stretching: two of the three
stops are named by the sealed sources in their own words, and the third is the relay's own fence.

---

## 8. FREEDOMS-CONSUMED (law 2a)

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN ; JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the tensor presentation and the two unital arrows at TENS and EMB;
  member 7's stage category and its deliberate omission;
  member 8's E_ch at its source-sector scope;
  members 9 and 10's two labellings of R_c, carried as a displayed discrepancy;
  member 11's R_N / iota_NM; member 5's suite schema.

CONDITIONED-ON:
  section 3 on each tensor factor being unital, which the sealed formulas evidence by using
  their units; the finite model in section 3.3 illustrates the identity and is not its proof —
  the proof is the ring argument.

SUBSTITUTED:
  NOTHING. No stage index, restriction functor, superselection extension, record labelling,
  certificate, fiber datum, or default was authored or chosen. I_C0 was DERIVED, not selected;
  three items STOPPED with their freedoms named; the four decision-dependent constructions
  were not touched.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 9. Flattening check, verb audit, byte audit

**FLATTENING CHECK — S01–S37 walked, clean.** A canonical extension was not identified with a forced
one — §5.2 is exactly that distinction. A stage category for one leg was not identified with a stage
rule for the carrier. Two sealed labellings of one carrier were not reconciled into one. A finite
model was not identified with a proof: §3.3 says which is which. Verified leg-level squares were not
identified with the quantified suite the field requires. A fenced object was not reached for when an
item needed it. "Derived" was used for exactly one item.

**BUILDER-B INDEPENDENCE:** every step is from a sealed source at its digest and span. Members 3 and
4 are mine and supply only the field list being worked.

**SELF VERB AUDIT.** "Derived" is used once, for `I_C0`, with each step displayed and the
zero-freedom step identified as the ring argument. "Stopped" is used three times, each with its
freedom named and, where the source names it, quoted. "Discrepancy" is used for the `R_c` labelling
and is left unresolved. No stage rule, superselection map, or certificate is claimed.
`VERB_AUDIT_SELF = CLEAN`.

**BYTE-POSITION SELF-AUDIT.** §1's heading begins at byte **539**, its closure fence opens at byte
**588**, and the exact member list ends at byte **2321**. A scan of bytes `[0,588)` over `no `,
`not `, `none`, `never`, `absent`, `missing`, `without`, `lack`, `fail`, `gap`, ` open`,
`unresolved`, `underived`, `false`, `cannot`, `zero`, `stop`, `block`, `wrong`, `refus`, `unswept`,
`unsealed` returns **zero hits**.

---

## 10. Final lines

```text
CLOSURE = declared-first (sec-1 heading 539, closure fence 588, members end 2321; pre-closure scan over 22 forms = 0 hits)
IDENTITY = DERIVED. I_C0 := 1_A_src (x) 1_R_inf (x) 1_B = 1_A_SR (x) 1_B, from the entered realization's own presentation. The three factors are unital on the sealed text's own evidence — the formulas at EMB USE 1_A_src, 1_B and 1_A_SR, and member 6 calls both arrows "canonical unital injective star-homomorphisms". THE ZERO-FREEDOM STEP IS RING UNIQUENESS: if e and f are both two-sided units then e = e f = f, so a second unit is provably the same element rather than an alternative. A second independent route from the same span: both sealed unital arrows carry their own units to the same element, i_R(1_R_inf) = i_B(1_B) = I_C0, all residuals 0.000e+00 on a finite model of the presentation. The object my 927 found to have ZERO declaration sites is forced by the presentation that was entered.
STAGE_RULE = STOPPED, and THE SOURCE NAMES THE FREEDOM IN ITS OWN WORDS. Member 7: the ratified presentation "has only Q-201's sequential record-stage category N<=M; it DELIBERATELY HAS NO finite-incidence-complex restriction functor... No sealed map relates that carrier to the sequential C0 module. CHOOSING N=4 FROM THE FOUR VERTICES OR FOUR EDGES WOULD BE AN UNRATIFIED IDENTIFICATION." The realization anticipates the exact act the field requires and forbids it. SECOND FREEDOM: the source leg has no sealed finite chain at all — genuine_finite_rank_continuum_restriction_constructed = false per that lineage's own authority correction — so a joint rule would stage two legs from sealed systems and the third from an unbuilt object.
SUPERSELECTION = STOPPED, two freedoms. E_ch is declared on the SOURCE sector only (Delta_0^ss on T_1(H_src^A); P_src E_ch = E_ch P_src) and no sealed source extends it across the tensor factors. FREEDOM 1: writing E_joint = E_ch (x) id (x) id asserts that joint superselection acts TRIVIALLY on the record and history factors — a claim about where charge lives, not a consequence of the presentation. FREEDOM 2, the sharper one: TWO SEALED SOURCES LABEL THE RECORD CARRIER DIFFERENTLY — member 9 gives R_c = span{|r_c>,|p_c>,|e_c>} while member 10 gives R_c = span_C{|r_c>,|p_Q,c>,|e_Q,c>}, the difference being a CHARGE SUBSCRIPT on the pointer and edge states, which bears directly on freedom 1. Discrepancy reported, resolved nowhere.
CERTIFICATES = STOPPED (PRODUCED-0), gated twice. GATE 1: JAC-14's finite_stage_inventory and per-stage block quantify over the carrier's stages, and item 2 stopped that rule — there is no stage set to quantify over. GATE 2: the suite itself requires a0_rank_fiber_sha256, and the A0 fiber is among the four decision-dependent objects THIS RELAY EXPRESSLY FENCES OFF. My item ran into the fence and I stopped rather than reached across it. Recorded as material and not as certificates: the record leg's restriction squares verify in isolation at 0.000e+00, but legs are not the quantified suite the field requires.
NEW_CONTENT = NONE-BEYOND-DERIVATION. One item derived with every step displayed; three stopped with their freedoms named; the four decision-dependent constructions untouched. DELIVERED 1 OF 4, recorded plainly rather than stretched — two of the three stops are named by the sealed sources themselves and the third is the relay's own fence.
BLIND = HELD. No rank read, no ratio computed or stated, no fiber compared. The superselection item ran closest to the fiber and stayed at the algebra's structure; the certificate item stopped AT the fiber rather than opening it.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
