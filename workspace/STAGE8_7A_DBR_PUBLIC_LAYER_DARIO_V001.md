# STAGE 8 / 7A / Q-126 ROUTE — THE PRIMARIES READ: MY 760 TOWER DIVERGES IN THREE TERMS; THE PUBLIC LAYER SCOPED

Lane: DARIO (Builder B, independent verifier). Relay 762.
Nothing adopted. The frozen prereg and R9-JII untouched.

## Lead determination

**The primaries refute part of my own 760 tower, in three named terms, and the locked spectrum
is the instrument that does it.**

The two-cutoff theorem carries the object's **locked spectrum, reconstructed exactly**:

```text
lambda_a = p_t^2 + p_q^2 + lambda_S2(flux,ell)^2 + 1 + C2_parent,
lambda_a >= 1 + p_t^2 + p_q^2 + ell(ell+1).
```

Against my 760 form, term by term: **the two circle towers agree**; **three terms diverge.**

1. **The `S^2` term.** I used the **untwisted** sphere spectrum `(k+1)^2/r_F^2`. The locked term
   is `lambda_S2(**flux**, ell)^2` — **flux-twisted**, with the weaker floor `ell(ell+1)` rather
   than `(ell+1)^2`. `S2_flux` is by the principle's own words the *"exterior source-flux
   comparison"* sphere: it **carries flux**, so its Dirac operator is twisted and may have zero
   modes. My untwisted spectrum was the wrong operator on that factor.
2. **`C2_parent` — omitted entirely.** I treated `E_parent` as affecting only multiplicities. The
   locked spectrum has the parent Casimir as an **additive eigenvalue term**.
3. **The cross term.** The principle displays `D_BR^2 = D_(Sigma,A)^2 + Gamma_Sigma [D_(Sigma,A),
   Phi] + Phi^dagger Phi` — **three grades**. My 760 asserted the middle one cancels for constant
   `Phi`. The principle does not grant that, and the corpus leaves `Phi` unselected.

**And one 760 claim was too strong in the other direction.** I wrote that multiplicities *"cannot
be pinned at all"* because `rank(E_parent)` is unselected. The principle **fixes it**: `E_parent`
is *"the completed `Spin(10)` chiral **16** generated by `Lambda^even(C5)`."* The **rank is 16**;
what remains free is the bundle and its characteristic class.

**The public layer is scoped and every element is FREE or UNBUILT — none determined.** Four
shortcuts are TYPE-R refuted with displayed witnesses, and one of them names the raw carrier's
bottom as **degenerate**, which is a positive structural fact about the tower rather than a mere
refusal.

**And the route carries its own falsifier**, which I had not seen before this relay: *"The route
fails to compute alpha if two inequivalent choices of radii, spin structure, bundle class, or odd
profile satisfy all adopted record rules while producing different weighted charged
determinants."* The unselected moduli are not an inconvenience — they are a **named failure
mode**.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively: **absent**, artifact and sidecar both.

### 0.2 Sources verified before use

```text
PRIN   workspace/mirrored_sources/alpha_global_record_surface_superconnection_principle_v001.md
                                                                     ae1d0492…  see provenance
TWO    workspace/mirrored_sources/alpha_br_full98_c2_two_cutoff_continuum_theorem_v001.md
                                                                     7a3c6bc6…  see provenance
GAP    workspace/mirrored_sources/…BR_CLOSURE_OPERATOR_STRUCTURAL_SPECTRAL_GAP_DETERMINATION_V001.md
                                                                     e2fc00d2…  SEALED-OK
SPEC   workspace/STAGE8_COMPLETE_PUBLIC_DBR_LBR_SPECTRAL_THEOREM_SPEC_V001.md   a751b72c…  SEALED-OK
SPEC1  workspace/STAGE8_BR_CLOSURE_OPERATOR_SPECTRUM_AND_SEAM10_IDENTITY_DETERMINATION_V001.md
                                                                     de88ba68…  SEALED-OK
BETA   workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md  04c10c0d…  SEALED-OK
760    workspace/STAGE8_7A_DBR_SPECTRUM_DARIO_V001.md                a2745310…
```

**Provenance of the two new primaries, stated precisely rather than assumed.**

- `PRIN` carries **no sidecar**. Its digest `ae1d0492…` **matches, exactly, the digest listed for
  `alpha_global_record_surface_superconnection_principle_v001.md` among `SPEC`'s Primary
  Authorities** — and `SPEC` is sidecar-sealed. So `PRIN` is authenticated **by a sealed
  artifact's own manifest**, which is a lawful path and not a sidecar. I verified the match; I did
  not infer it.
- `TWO` carries **no sidecar and no digest match I could find** in the sealed corpus. Its own
  status line reads **`PROPOSED_NOT_CLOSED`**. **I therefore treat it as an unsealed primary**:
  its *locked spectrum* is used below as a **test instrument against my own prior work**, which is
  the strongest use it can lawfully bear, and **not** as a sealed ground for any new claim.
- `GAP` in `mirrored_sources/` is byte-identical to the archive copy (`e2fc00d2…`, SEALED-OK).

**Still absent:** `reports/alpha_global_surface_dirac_superconnection_v001.md` — the report AM1
names as holding *"an exact discrete product-spectrum family"*. **It was not mirrored.** The
term-by-term comparison below is therefore run against **`TWO`'s locked spectrum**, not against
that report, and §1.3 says so.

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities.  No comparison to measured constants.  No common cell formed.
No junction map evaluated.  No smooth data derived.  Nothing adopted; §2's forks are
displayed for the principal and not taken.  No register, plan, tracker, or git action.
```

---

## 1. AM1 — THE PRIMARIES READ

### 1.1 What the defining principle fixes that the geometry quotation did not

[PROVABLE] `PRIN` `ae1d0492…`. Four things beyond the `Sigma_BR`/`H_BR`/`D_BR` lines already
quoted at 760:

**(a) The factors have physical roles, and one is shared.** *"`S1_T` carries closed
proper-time/source return, `S2_flux` carries the minimum closed orientable exterior source-flux
comparison, and `S1_Q` carries compact charge holonomy. **The proper-time factor is shared by the
gravity and matter reads; it is not duplicated.**"* And `Sigma_BR = S1_T x S2_flux x S1_Q = **T2_TQ
x S2_flux**` — the two circles are grouped as one torus.

**(b) `D_BR^2` has THREE grades, displayed.**

```text
D_BR^2 = D_Sigma,A^2 + Gamma_Sigma [D_Sigma,A, Phi] + Phi^dagger Phi.
```

**(c) `E_parent` is fixed.** *"`E_parent` is the completed `Spin(10)` chiral **16** generated by
`Lambda^even(C5)`."* And: *"Gauge, source/proper-time, finite-mass, and index data must therefore
be computed from **one spectrum and one induced trace**."*

**(d) The branch is declared, and narrow.** *"This is the global carrier for the declared ordinary
charged durable-source branch. **It is not asserted for source-free, uncharged,
enlarged-topology, or non-durable branches.**"*

[PROVABLE] **What the principle leaves unselected**, verbatim: *"`R_T, R_flux, R_Q`, the spin
structure on `T2_TQ`, the parent bundle and its characteristic class, the odd vacuum profile
`Phi`, or the chiral index"* — with the guard *"A value may not be called primitive merely
because it is an integer or a compact radius."*

[PROVABLE] **The route's own falsifier**, which I had not seen before this relay:

```text
The route fails to compute alpha if two inequivalent choices of radii, spin
structure, bundle class, or odd profile satisfy all adopted record rules while
producing different weighted charged determinants. In that case a further
physical selection principle is required; the desired coupling may not select
the missing data retroactively.
```

[YOURS] That last clause is the void condition in this route's own words, pre-armed against the
temptation the unselected moduli create.

### 1.2 The locked spectrum

[PROVABLE, from an **unsealed** primary — see §0.2] `TWO` `7a3c6bc6…`: *"Its locked spectrum is
reconstructed exactly as"*

```text
lambda_a = p_t^2 + p_q^2 + lambda_S2(flux,ell)^2 + 1 + C2_parent,
lambda_a >= 1 + p_t^2 + p_q^2 + ell(ell+1).
```

### 1.3 TOWER CHECK — term by term

My 760 form: `spec(D^2) = { (n_T+a_T)^2/R_T^2 + (k+1)^2/r_F^2 + (n_Q+a_Q)^2/R_Q^2 }`, plus a
uniform `Phi^dagger Phi` shift, with the cross term asserted to cancel for constant `Phi`.

| Slot | My 760 | Locked spectrum | Verdict |
|---|---|---|---|
| `S1_T` | `(n_T+a_T)^2/R_T^2` | `p_t^2` | **AGREES** — same tower; the locked form is written at unit radii (§3.1) |
| `S1_Q` | `(n_Q+a_Q)^2/R_Q^2` | `p_q^2` | **AGREES** — same |
| `S2_flux` | `(k+1)^2/r_F^2`, **untwisted** | `lambda_S2(**flux**,ell)^2`, floor `ell(ell+1)` | **DIVERGES** |
| odd term | `Phi^dagger Phi`, cross term assumed to cancel | `+ 1` | **PARTIAL** — the constant is there; the cancellation is not granted |
| parent | multiplicity only | `+ C2_parent`, **additive** | **DIVERGES — omitted** |
| grades | two (`D^2 + Phi^dagger Phi`) | three (`PRIN`: `D^2 + Gamma[D,Phi] + Phi^dagger Phi`) | **DIVERGES** |

[YOURS] **Divergence 1 — the sphere is twisted and I used the untwisted operator.** `S2_flux`
carries flux by the principle's own description, so its Dirac operator is twisted by the flux line
bundle. The arithmetic shows it: the untwisted `S^2` Dirac spectrum gives
`lambda_S2^2 = (ell+1)^2 = ell^2 + 2 ell + 1`, while the locked floor is
`ell(ell+1) = ell^2 + ell` — **strictly smaller**. A twisted spectrum that can descend to the
lower floor is exactly what a flux twist produces, and it is why the `+1` is needed to hold
`lambda_a >= 1`. **My 760 S^2 term was the right slot with the wrong operator.**

[YOURS] **Divergence 2 — `C2_parent` is an eigenvalue term, not a multiplicity.** 760 §2.3 put
`E_parent` entirely into the multiplicity bookkeeping. The locked spectrum adds its quadratic
Casimir to `lambda_a` directly. This also explains a flag I quoted at 760 without connecting:
`parent_Casimir_forces_uniform_positive_public_gap = false | TYPE-R` — the Casimir is in the
*spectrum*, which is why anyone would try to lift the bottom with it.

[YOURS] **Divergence 3 — the cross term.** `PRIN` writes `Gamma_Sigma [D_(Sigma,A), Phi]` as its
own grade. My 760 conditional (*"if `Phi` is constant and `Gamma_Sigma` anticommutes"*) was stated
as a condition, but I never checked it against the principle — and the principle both displays the
term and leaves `Phi` **unselected**, so the condition is not met of record.

```text
TOWER_CHECK = DIVERGES (term-by-term above).  Two of five slots agree; the S^2
  factor, the parent Casimir, and the grade count all diverge.
```

[YOURS] **What survives of 760.** The *form* — a discrete sum-of-squares tower over two circle
towers and one sphere tower, with an additive constant — survives, and the two circle towers
survive exactly. What fails is my choice of operator on the flux sphere, my placement of
`E_parent`, and an assumed cancellation. I record this as a **refutation of three terms, not of
the shape**.

---

## 2. AM2 — THE PUBLIC LAYER, SCOPED

### 2.1 The four refuted shortcuts, displayed so nobody re-walks them

[PROVABLE] `GAP` `e2fc00d2…`, each with its test name and witness:

```text
Phi_dagger_Phi_forces_uniform_positive_public_gap = false | TYPE-R |
  test: ADMISSIBLE-KERNEL-COUNTERMODEL;
  witness: the corpus leaves Phi unselected and permits a nontrivial kernel,
  so positive semidefiniteness is not uniform coercivity

parent_Casimir_forces_uniform_positive_public_gap = false | TYPE-R |
  test: INTERNAL-SECTOR-COERCIVITY-TEST;
  witness: the current parent inventory includes a sector not lifted by the
  quadratic invariant

unquotiented_skeleton_satisfies_public_cardinality_one = false | TYPE-R |
  test: LINEAR-TRACE-MULTIPLICITY-TEST;
  witness: the raw carrier has nontrivial direct-sum multiplicity, and the
  current ledger records the unquotiented capacity trace as non-unit

compact_topology_forces_simple_public_bottom = false | TYPE-R |
  test: BOTTOM-MULTIPLICITY-COUNTERMODEL;
  witness: compact elliptic operators may have degenerate lowest eigenspaces;
  this corpus's raw product carrier does
```

[YOURS] **The fourth witness is a positive structural fact, not only a refusal:** *"this corpus's
raw product carrier **does**"* have a degenerate lowest eigenspace. So the bottom of §1.2's tower
is **degenerate**, and any route to a simple public bottom must break that degeneracy by the
quotient, not inherit it from compactness.

[YOURS] And the first two witnesses share a root: **both fail because `Phi` and the parent sector
are unselected.** They are not independent obstructions; they are the same unselected-moduli
problem seen at the odd grade and the internal grade.

### 2.2 Per element

| Element | Sealed demand | Determined by stock? |
|---|---|---|
| **Public quotient / equivalence** | `SPEC1`'s item 3, *"derived public equivalence relation/quotient Hilbert space"*; `GAP` types it **PUBLIC-IDENTITY-CRITICAL** — *"It decides which modes survive and whether the bottom public space is simple"* | **FREE / UNBUILT.** No sealed text derives it. `unquotiented_skeleton_satisfies_public_cardinality_one = false | TYPE-R` closes the do-nothing option |
| **Linear `Tr_BR`** | `SPEC1`'s item 4; `GAP`: *"Yes for the capacity predicate, because public cardinality and multiplicity are part of that predicate"* | **FREE / UNBUILT.** `public_trace_rule_satisfied = false | TYPE-R` (`SPEC`) |
| **Null/private-mode rule** | `SPEC1`'s item 5; `GAP`: **PUBLIC-IDENTITY-CRITICAL** — *"Removing modes can change the public bottom and its multiplicity"* | **FREE / UNBUILT.** And it interacts with §1.3's finding: a **flux-twisted** `S^2` factor can carry zero modes, which is exactly what a null-mode rule must adjudicate |
| **Branch selection** | `PRIN`: the carrier is asserted only for *"the declared ordinary charged durable-source branch"*; `GAP` lists *"selected admissible branch"* as PUBLIC-IDENTITY-CRITICAL | **PARTIALLY DETERMINED.** The **branch is declared** by `PRIN`; the *admissible data within it* (radii, spin structure, bundle class, `Phi`, index) are **not** |
| **Bottom positivity** | `GAP`: *"proof of bottom positivity … in the selected branch"* | **FREE / UNBUILT.** Both coercivity routes TYPE-R refuted (§2.1) |
| **Bottom simplicity** | `GAP`: *"one-dimensional public multiplicity"* | **FREE / UNBUILT**, and **harder than it looks**: the raw bottom is **degenerate** of record, so simplicity must be *produced* by the quotient |

[YOURS] **No element of the public layer is determined by sealed stock.** One is partially fixed
(the branch is declared, its data unselected); five are free-or-unbuilt.

### 2.3 What a lawful derivation requires — and the fork, displayed not taken

[YOURS] The public layer's five open elements are not five independent problems. **Four of them
turn on the same missing input**: a rule that says **which modes are public**. The quotient
defines it, `Tr_BR` counts it, the null/private rule prunes it, and bottom simplicity is a
statement about it. So a lawful derivation needs **one object** — a derived publicity criterion
— from which the four follow, plus **branch data selection** for positivity.

**THE FORK, for the principal, displayed and not taken:**

```text
FORK A — DERIVE THE PUBLICITY CRITERION.  One object, four consequences.  Requires
  a target-blind rule assigning public/private status to modes of H_BR.  Nothing
  in sealed stock supplies it; nothing bars it.

FORK B — SELECT THE BRANCH DATA FIRST.  Radii, spin structure, bundle class, odd
  profile, index.  DANGER, and PRIN arms it: if two inequivalent choices satisfy
  all adopted record rules and give different weighted charged determinants, the
  ROUTE FAILS and a further selection principle is required.  Selection is the
  route's own named failure mode, not merely a risk.

FORK C — NEITHER YET: record that both are open and that FORK A is prior, since
  positivity and simplicity are statements ABOUT the public space.
```

[YOURS] I state that Fork A is **prior in dependency order** — that is a structural reading of the
sealed demands, not a recommendation between forks, which is the principal's.

---

## 3. AM3 — THE NORMALIZATION'S DEMAND

### 3.1 The sealed statement, and where `beta` hides

[PROVABLE] `GAP` classifies it VALUE/CONSUMER-CRITICAL: *"**normalized transfer from `D_BR^2` to
the `L_BR` proper-time consumer**"*, marked **TRANSFER, not numerical value**. `SPEC` carries the
target: `L_BR = Delta_BR,public tensor I_E + I_public tensor C2,parent`.

[YOURS] **And §1.2's locked spectrum is written with the radii absorbed** — `p_t^2`, not
`p_t^2/R_T^2`. The three radii `R_T, R_flux, R_Q` do not appear. That is a **unit convention**,
and it is exactly where 760 §3 located `beta`.

**So the beta-carrying step is the restoration of the radii.** The locked spectrum is
dimensionless; `L_BR`'s consumer is **proper time**; converting between them reinstates
`R_T, R_flux, R_Q` as proper lengths, and `beta` (`R = beta c Delta tau`) is the conversion. The
locked form does not remove `beta`-sensitivity — **it hides it in a convention**, which is a
sharper statement of 760 §3 than 760 could make.

### 3.2 What the beta-carrying step must preserve

[YOURS] Three constraints, each read off a sealed demand:

1. **One spectrum and one trace.** `PRIN`: *"Gauge, source/proper-time, finite-mass, and index
   data must therefore be computed from **one spectrum and one induced trace**."* The transfer may
   not introduce a second normalization for the proper-time consumer.
2. **The public quotient must survive it.** The transfer acts after the quotient (`Delta_BR,public`),
   so it must commute with whatever §2 eventually derives — an obligation on an object that does
   not yet exist.
3. **No retroactive selection.** `PRIN`'s hard-failure rule bars the desired coupling from
   selecting radii. Since the transfer *is* the radii's reintroduction, this is precisely where
   that bar bites.

### 3.3 Where the S5.3-class burden lands

[PROVABLE] `TWO`'s convergence bar:

```text
Convergence is a two-parameter net: for every tolerance there must be
independent N_T* and N_S* such that the actual partial sums are Cauchy for
all larger (N_T,N_S), including asymmetric cutoff sequences. The analytic
incomplete-gamma inequalities certify the infinite remainder; floating-point
samples are regressions only.
```

with *"explicit failure when either cutoff is held fixed while the other is enlarged"*, and the
whole document stamped **`PROPOSED_NOT_CLOSED`** — *"the actual operator-level partial sums and
their constants have not yet passed the gate."*

[YOURS] **That is the S5.3-class burden, located exactly**: a **two-parameter** limit that must be
**proved** Cauchy over asymmetric sequences, not assumed — and `TWO` additionally records that
passing it *"does not yet establish … alpha"*, nor the nonlinear family, the differentiated Ward
term, an absolute parameter measure, a determinant, or threshold flow. **The burden is not
discharged and the theorem that would carry it is not closed.**

```text
NORMALIZATION = demand displayed (burden located).
```

---

## 4. GROUNDING, JURISDICTION, VERB AUDIT

### 4.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | `PRIN` authenticated by digest match to `SPEC`'s manifest | `ae1d0492…` vs `a751b72c…` | PROVABLE |
| 2 | `TWO` unsealed, `PROPOSED_NOT_CLOSED` | `7a3c6bc6…` | PROVABLE |
| 3 | The factors' roles; `T2_TQ x S2_flux`; shared proper-time factor | `ae1d0492…` | PROVABLE |
| 4 | `D_BR^2`'s three grades | `ae1d0492…` | PROVABLE |
| 5 | `E_parent` = `Spin(10)` chiral **16**; one spectrum, one trace | `ae1d0492…` | PROVABLE |
| 6 | The unselected list; the hard-failure rule | `ae1d0492…` | PROVABLE |
| 7 | The locked spectrum | `7a3c6bc6…` | PROVABLE (unsealed source, used as test instrument) |
| 8 | Term-by-term divergence in three slots | §1.3 | YOURS |
| 9 | `(ell+1)^2 > ell(ell+1)`, so the floor is a twisted floor | §1.3 | YOURS, exact |
| 10 | The four TYPE-R witnesses | `e2fc00d2…[11120,…)`, `[11364,…)`, `[12563,…)`, `[12822,…)` | PROVABLE |
| 11 | The raw bottom is degenerate of record | `e2fc00d2…[12822,13050)` | PROVABLE |
| 12 | Per-element scoping; none determined | §2.2 | PART-PROVABLE |
| 13 | The radii are absorbed; the transfer restores them | §3.1 | YOURS |
| 14 | The two-parameter net and `PROPOSED_NOT_CLOSED` | `7a3c6bc6…` | PROVABLE |

**Pin check: 14/14.**

### 4.2 Jurisdiction check

**On using an unsealed primary.** `TWO` has no sidecar and no digest match I could locate. I use
its locked spectrum **only** to test my own prior claim — the one direction in which an unsealed
source can do no harm, since it can only cost me a result, never grant one. No new positive claim
rests on it, and §0.2 says so before §1.2 uses it.

**On the VOID CONDITION.** Nothing chosen from a desired coefficient. §2.3's forks are displayed
and not taken; I state only a **dependency** ordering, which is structural. `PRIN`'s own
hard-failure rule is quoted in full precisely because it is the void condition for this route.

**On the four TYPE-R refusals.** Displayed with witnesses, per the commission, so the shortcuts
are not re-walked — and §2.1 adds that two of them share one root and the fourth carries a
positive fact.

**On R9 / R9-JII and the frozen prereg.** Untouched.

### 4.3 Self verb audit — **CLEAN, with four disclosures**

1. **Three terms of my 760 tower are refuted** (§1.3) — the flux-twisted sphere, the omitted
   parent Casimir, and an assumed cancellation the principle does not grant. The shape survives;
   three of five slots do not. **This is the second consecutive relay in which reading a primary
   reversed part of my own prior artifact**, and the first in which the reversing instrument was
   handed to me by the registrar in response to my own custody finding.
2. **And one 760 claim was too strong the other way.** *"Multiplicities cannot be pinned at all"*
   — `rank(E_parent) = 16` is fixed by the principle. Only the bundle class is free. I over-stated
   the gap in the direction of caution, which is still over-stating.
3. **The report AM1 names was not mirrored.** The commission asks for a comparison against
   `reports/alpha_global_surface_dirac_superconnection_v001.md`; it is not in
   `mirrored_sources/`. I ran the check against `TWO`'s locked spectrum instead and say so
   rather than letting the commissioned comparison appear to have been performed.
4. **My 760 restraint on `Phi^dagger Phi` was right for a better reason than I gave.** I stopped
   at the TYPE-R line; the witness now shows *why* — *"the corpus leaves `Phi` unselected and
   permits a nontrivial kernel."* The same unselected `Phi` is what invalidates my cross-term
   cancellation. One missing selection, two of my errors.

---

```text
PRIMARIES = read (seals verified; principle's fixings displayed).  PRIN ae1d0492...
  carries NO sidecar and is authenticated by an exact digest match to SPEC's sealed
  Primary-Authorities manifest.  TWO 7a3c6bc6... carries NO sidecar and NO digest match;
  its own status is PROPOSED_NOT_CLOSED, so it is used ONLY as a test instrument against
  my own prior work, never as a ground.  The report AM1 names was NOT mirrored.
  PRIN FIXES, beyond the geometry: the factors' physical roles and the shared,
  non-duplicated proper-time factor (Sigma_BR = T2_TQ x S2_flux); THREE grades of
  D_BR^2 = D^2 + Gamma_Sigma[D,Phi] + Phi^dagger Phi; E_parent = the completed Spin(10)
  chiral 16 from Lambda^even(C5), with "one spectrum and one induced trace"; the branch
  as ordinary-charged-durable-source ONLY; and the route's OWN FALSIFIER — two
  inequivalent admissible choices giving different weighted charged determinants means
  THE ROUTE FAILS and "the desired coupling may not select the missing data
  retroactively".
TOWER_CHECK = DIVERGES (term-by-term).  Against the locked spectrum lambda_a = p_t^2 +
  p_q^2 + lambda_S2(flux,ell)^2 + 1 + C2_parent (floor 1 + p_t^2 + p_q^2 + ell(ell+1)):
  BOTH CIRCLE TOWERS AGREE.  THREE DIVERGENCES — (i) I used the UNTWISTED S^2 spectrum
  (k+1)^2 where the factor CARRIES FLUX and the locked floor ell(ell+1) is strictly
  below (ell+1)^2, so a twisted operator with possible zero modes is the right one;
  (ii) C2_parent is an ADDITIVE EIGENVALUE TERM and I had placed E_parent in the
  multiplicities only; (iii) I assumed the Gamma_Sigma[D,Phi] cross term cancels for
  constant Phi, which PRIN displays as its own grade and which the unselected Phi does
  not grant.  The SHAPE survives; three of five slots do not.  Separately, my 760
  "multiplicities cannot be pinned at all" was TOO STRONG: rank(E_parent) = 16 is fixed.
PUBLIC_LAYER = scoped (per-element).  NONE of the five is determined by sealed stock;
  the branch alone is PARTIALLY determined (declared by PRIN; its data unselected).
  Four refuted shortcuts displayed with witnesses so they are not re-walked — and two of
  them share ONE root (Phi and the parent sector unselected), while the fourth carries a
  POSITIVE fact: this corpus's raw product carrier DOES have a degenerate lowest
  eigenspace, so simplicity must be PRODUCED by the quotient, never inherited.
  STRUCTURAL READING: four of the five elements turn on one missing object — a derived
  PUBLICITY CRITERION — so Fork A (derive it) is PRIOR to Fork B (select branch data),
  and Fork B is the route's own named failure mode.  Forks displayed, none taken.
NORMALIZATION = demand displayed (burden located).  The locked spectrum is written with
  the radii ABSORBED, so it does not remove beta-sensitivity — IT HIDES IT IN A
  CONVENTION, and the beta-carrying step is precisely the RESTORATION of R_T, R_flux,
  R_Q as proper lengths for L_BR's proper-time consumer.  It must preserve: one spectrum
  and one induced trace; commutation with a public quotient that does not yet exist; and
  PRIN's bar on retroactive selection — which bites exactly here, since the transfer IS
  the radii's reintroduction.  S5.3-CLASS BURDEN LOCATED: TWO's TWO-PARAMETER NET —
  independent N_T*, N_S*, Cauchy over ASYMMETRIC cutoff sequences, analytic remainder
  certified and floating-point samples demoted to regressions — with explicit failure
  required when one cutoff is frozen.  Undischarged; the theorem carrying it is
  PROPOSED_NOT_CLOSED and earns, on passing, neither alpha nor the nonlinear family.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+4 disclosures at section 4.3, including three refuted terms of
  my own 760 tower and one 760 claim that was too strong in the cautious direction)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
