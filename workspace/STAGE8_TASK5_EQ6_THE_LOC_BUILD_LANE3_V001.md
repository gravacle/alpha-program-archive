# STAGE 8 / TASK 5 / EQ6 — THE LOC BUILD: THE PHYSICAL LOCALIZATION MAP, THE LAST OBJECT

Lane: **Codex Lane 3 (Sol, high effort)**  
Artifact: `STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md`  
Register preflight head: **Q-495**  
Mode: **symbolic only; no member binding; no end-test execution**

## Lead determination

The map required by physical J2 is not derivable from the sealed stock. The
stock reaches an actual path-bilocal Q-408 kernel and a reader-free finite
symbol shadow, but it does not supply the required source extension or
either of the two maps after it:

```text
completed L_T/profile class
  --extended Kernbar--> one common physical kernel domain
  --physical Loc--> local Maxwell/contact symbol carrier
  --Hodge realization--> finite Hodge cochain carrier.             (R0)
```

The A1 path/current correspondence does not close either missing arrow. It
transports paths, conserved currents, bundles, connections, and curvature;
it does not prove that the bilocal distribution is a properly supported
conormal Schwartz kernel, fix the operator/density realization, or construct
the fiber-Fourier principal-symbol and contact-quotient maps. DoR-019 does
not license the later symbol-to-Hodge cross-sector conversion, A5 acts only
after a cochain already exists, and A6 retains full J2 as post-scope pending
an independent `Loc/pi_Mx`.

The determination therefore remains open beyond the negative derivability
result: no no-go, nonobstruction theorem, or inhabited alternative is proved.
Accordingly this artifact states the complete specification and authors a
**failure-capable full-gate candidate family**. It selects no route, no map
member, no field/gauge/frame representative, and does not assert that the
family is inhabited. The
minimal `F^2`-compatible branch has no route to a harmonic flat-holonomy line;
an alleged nonzero harmonic route would require an additional, separately
typed Wilson/holonomy seam and is not silently included in `Loc`.

The physical-J2 equation

```text
pi_Mx,N(Loc_N(Kernbar_N(Q_N(L_T,N)))) = 1          (FALSE-ANCHOR)
```

is an equation of record for an unbuilt composite. It is not used as a
premise, definition, normalization proof, or existence theorem below.

## 0. Preflight, authority ledger, and fences

### 0.1 Access and no-clobber

The three STEP-0 locations were accessible before substantive work:

```text
cleanroom = accessible
archive workspace = accessible
supervision = accessible
```

The questions-settled register was read at current head `Q-495`; its SHA-256
at preflight was
`33218ce933e388b98e4a6fb55e432245995b29caaee5a7f17da6682ac33d01c9`.
Neither the commissioned filename nor its seal existed in the cleanroom or
archive workspace. The no-clobber check was repeated immediately before the
write.

### 0.2 Verified authorities

Every load-bearing file below was verified against its sealed sidecar before
use.

| Authority | Verified SHA-256 | Content used |
|---|---|---|
| `STAGE8_TASK5_EQ6_TWO_LEMMAS_LANE3_V001.md` | `48616c239ccdd777d1ac7cf5a049f324b98ca6abdba84966c5dc98151c461de4` | `(M5)` and the reciprocal-loop harmonic pairing |
| `STAGE8_TASK5_EQ6_SEED_ADJUDICATION_DARIO_V001.md` | `e287b0573d2a84c439eacef00199b9113db7f5d2b437c2a4da09101ce6bdb03c` | false-anchor warning, `Kernbar` scope, `F^2`/holonomy headwind |
| `STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md` | `474bf721517f77b240e2a215325a86227d4e0fd4934e89ba910d955627b5ab60` | identity scope, missing `(L3)/(L8)`, linearity debt |
| `STAGE8_TASK5_EQ6_PROJECTOR_CERT_V003_LANE1_V001.md` | `0bef9a003abb586666535689e98d7af29e7fc75b81e06fe439397ce2b8ef9100` | finite `d`, `delta`, `P_H`, and rank-preserving intertwiners |
| `STAGE8_TASK5_EQ6_PHYSICAL_J2_LANE2_V001.md` | `3fd4b924ec1a70f3e730bcf99f406d697c53937b26467d2ae6d5aa8c1bb6822c` | physical factor types and the exact stop after `Loc^shadow` |
| `DOR_020_A1_WHERE_CLAUSES_AMENDMENT_2026-08-04.md` | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | path/current and bundle/field transport law |
| `DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md` | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | metric, Riesz typing, no implicit cross-sector conversion |
| `DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04.md` | `5beccc617f4c5f5f76aba777c664f3dd4393f72257fc850950e2246109a7a424` | contact-reducing law after Hodge entry |
| `DOR_020_A6_J2_SCOPED_PROJECTED_LAW_2026-08-05.md` | `202234ac77136592fdc24a96838909f1dad7e8f6a79fdab81342c5f12d6e82cd` | old-image scope; independent full `Loc/pi_Mx` remains pending |
| `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual Q-408 current and bilocal-kernel realization |
| `STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md` | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50` | physical local-symbol contract and future route menu |
| `STAGE8_TASK5_EQ6_WHERE_CLAUSES_LANE2_V005.md` | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | exact A1 support/current law and no-selection boundary |
| `STAGE8_TASK5_EQ6_WHERE_CLAUSES_FINAL_CHECK_LANE1_V001.md` | `b4c901f512379251b55d31aae6914d1fce9af6280c8f65081afeabfd4e75f219` | flat-holonomy falsifier and no-Hodge boundary |
| `STAGE8_TASK4B_CARRIER_METRIC_AND_UNITS_PROPOSAL_LANE2_V005.md` | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | lawful conversion set and no implicit C/K map |
| `STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001.md` | `70dde1c0cb776abec4a61cac118d4edf1f4c75a1185a2e984dc341961d4b643c` | local `F^2` symbol stop and underdetermined reading fiber |

### 0.3 Result tags and fences

```text
[PROVABLE]      = displayed consequence of verified sealed stock.
[PART-PROVABLE] = displayed consequence on its exact stated subscope.
[YOURS]         = candidate/specification authored here; not adopted stock.

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
comparison_to_measured_constants = none
```

## 1. B1 — complete specification before construction

This section specifies the object first. No construction verdict is used in
the specification.

### 1.1 Source and the `L_T` scoping repair

[PROVABLE] The profile presentation at stage `N` has the typed source

```text
E_N
 := R L_T,N direct-sum R Rhat_K,N
       direct-sum S_(1,sa)(K_N),

Q_N:E_N -> O_prof,N:=E_N/Rel_N.                  (S1)
```

Define only for bookkeeping

```text
E_N^cyc
 := {0} direct-sum R Rhat_K,N
          direct-sum S_(1,sa)(K_N),

O_N^cyc:=Q_N(E_N^cyc).                           (S2)
```

[PROVABLE] The sealed Q-408 formula constructs a reader-free path-bilocal
kernel on `(S2)`. It does not define its formula on the separate `L_T,N`
summand. Thus the prior string `Kernbar_N Q_N(L_T,N)` is not presently a
term.

Write that exact scoped map as

```text
Kernbar_N^cyc:O_N^cyc -> Ker_N^Q408.             (S2a)
```

[YOURS — required source contract] A physical `Loc` package must first build
one named topological ambient kernel space `D_N^ker` containing the image of
`(S2a)`, a continuous injective natural embedding

```text
iota_N^Q408:Ker_N^Q408 -> D_N^ker,               (S2b)
```

and one continuous linear raw realization

```text
Krawhat_N:E_N -> D_N^ker                          (S3)
```

The local Maxwell operator must have a separately typed Schwartz-kernel
realization, with the bundle density and dual-density conventions fixed:

```text
M_N^loc
 :=delta_(g,N) d:
   Omega^1(M_N;ad P_N)->Omega^1(M_N;ad P_N),
M_N^loc in Op_N^(loc,2),

Sch_N:Op_N^(loc,2)->D_N^ker,
Sch_N is continuous and injective,

f_*:Op_N^(loc,2)->Op_M^(loc,2),
eta_f^ker:D_N^ker->D_M^ker,

eta_f^ker Sch_N(A)
 =Sch_M(f_* A)
for every certified rank-preserving f:N->M,

eta_id^ker=id_Dker,
eta_gf^ker=eta_g^ker eta_f^ker,
eta_f^ker(D_N^Loc) subset D_M^Loc,
eta_f^ker|_(D_N^Loc)=etahat_f,

kappa_T,N
 :=Krawhat_N(L_T,N)
  =Sch_N(M_N^loc).                               (S4)
```

and with the old Q-408 branch reproduced exactly:

```text
Krawhat_N(0,b,C)
 = iota_N^Q408 Kernbar_N^cyc(Q_N(0,b,C)).        (S5)
```

It must prove quotient descent, not assume it:

```text
Rel_N subset ker(Krawhat_N).                     (S6)
```

Only after `(S6)` may one define

```text
D_N^Loc:=Krawhat_N(E_N) subset D_N^ker,

D_N^Loc carries the quotient topology transported from
 E_N/ker(Krawhat_N),

the inclusion D_N^Loc -> D_N^ker is a topological embedding,
so that quotient and D_N^ker-subspace topologies coincide,

Kernbarhat_N:O_prof,N -> D_N^Loc,
Kernbarhat_N(Q_N z):=Krawhat_N(z).              (S7)
```

The exact source decomposition available from `(S4)`--`(S7)` is

```text
D_N^Loc
 = span{kappa_T,N}
   +iota_N^Q408(Kernbar_N^cyc(O_N^cyc)).        (S8)
```

The plus sign in `(S8)` is not declared a direct sum. Mixed relations may
identify the two displayed pieces; a direct-sum sign is licensed only after
their intersection is computed.

`kappa_T,N` must be an actual surface/bundle kernel or a diagonal-supported
distribution in the same named topology as the Q-408 image. A formal tag
adjoined solely to make `(S4)` parse fails the specification.

### 1.2 Physical target, Hodge target, and the one permitted naming convention

[YOURS — required target contract] The physical target must be a named
topological vector space

```text
Sym_N^loc
 := Sigma_N^(2),Ward / Ctc_N^sym,                (S9)
```

where `Sigma_N^(2),Ward` is the declared closed Ward-transverse principal-
symbol class of homogeneous order two and `Ctc_N^sym` is its declared
**closed** contact/boundary symbol ideal. Operators, Schwartz kernels, and principal
symbols are not identified. Their two typed symbol maps are

```text
sigma_2^op:Op_N^(loc,2) -> Sigma_N^(2),Ward,

sigma_2^ker:Sch_N(Op_N^(loc,2)) -> Sigma_N^(2),Ward,

sigma_2^ker(Sch_N(A)):=sigma_2^op(A).            (S9a)
```

The quotient must carry a proved closed splitting

```text
Sym_N^loc=Mx_N^sym direct-sum Sym_N^perp         (S10)
```

The split supplies the projection

```text
Proj_Mx,N^sym:Sym_N^loc -> Mx_N^sym.             (S11)
```

A scalar coordinate is not licensed by `(S10)` alone.

The profile generator, its kernel, its local symbol, and its possible Hodge
image are four different objects:

```text
L_T,N       in E_N,
kappa_T,N   in D_N^Loc,
s_T,N^sym   in Sym_N^loc,
s_T,N^C     in C_N^k.                            (S12)
```

No line below identifies the four types.

[PROVABLE] The projector certificate acts on a different carrier:

```text
d_N^r:C_N^r->C_N^(r+1),
delta_N^(r+1):C_N^(r+1)->C_N^r,
P_H,N:C_N^k->C_N^k.                              (S13)
```

Therefore a physical package must separately build a reader-free linear
Hodge realization

```text
iota_N^H:Sym_N^loc -> C_N^k.                    (S14)
```

The two maps required by the previous artifacts are then typed definitively
as

```text
Loc_N^phys:D_N^Loc -> Sym_N^loc,
Loc_N^C:=iota_N^H compose Loc_N^phys
        :D_N^Loc -> C_N^k.                       (S15)
```

The physical Maxwell normalization is a construction obligation, not an
available theorem. For a future independently built member it must be proved
inside the local-symbol carrier as

```text
s_Max,N^unit
 :=[sigma_2^op(M_N^loc)]_Ctc_sym,
s_Max,N^unit != 0,

Mx_N^sym:=span{s_Max,N^unit},
p_Mx,N^sym:Sym_N^loc->R,
p_Mx,N^sym(s_Max,N^unit)=1,
p_Mx,N^sym(a s_Max,N^unit+r)=a
 for r in Sym_N^perp,

s_T,N^sym:=Loc_N^phys(kappa_T,N),
s_T,N^sym=s_Max,N^unit,
s_T,N^C:=iota_N^H(s_T,N^sym).                   (S15a)
```

`(S15a)` is not inferred from `(FALSE-ANCHOR)`, from an algebraic reader, or
from `p_loc(L_T)=1` on a separately typed interface. It is a void-capable
comparison between an independently fixed unit Maxwell symbol and a
constructed localization image. It also does
not imply

```text
P_H,N iota_N^H(s_T,N^sym) != 0.                  (S15b)
```

The latter is the distinct harmonic seed condition.

In this artifact, bare `Loc_N` means `Loc_N^phys`. Every occurrence of the
cochain-valued map is written `Loc_N^C`. Thus:

```text
physical J2's Loc reading = Loc_N^phys;
projector-cert P_H Loc reading = P_H,N Loc_N^C;
Loc-naturality (L3)/(L8) reading = Loc_N^C;
seed (M5) reading = Loc_N^C.                     (S16)
```

The two prior `pi_Mx` readings are also kept distinct. The physical-symbol
coordinate is

```text
p_Mx,N^sym:Sym_N^loc -> R,                       (S16a)
```

whereas the projector certificate's bundled Hodge map is

```text
Pi_N^Hdg
 :=pi_N^resp compose P_H,N compose Loc_N^C
   :D_N^Loc -> Bot_N.                            (S16b)
```

Physical J2 would use

```text
p_Mx,N^sym compose Loc_N^phys:D_N^Loc -> R.      (S16c)
```

Because `Bot_N` and `R` are different displayed codomains, direct comparison
of `(S16b)` and `(S16c)` is presently unformed. A later comparison face must
first build a typed seam and only then prove

```text
beta_N:Bot_N->R,

beta_N Pi_N^Hdg
 =p_Mx,N^sym Loc_N^phys.                        (S16d)
```

Neither `beta_N` nor `(S16d)` is part of the sealed stock, and neither may be
used to construct its two sides.

The sealed `sigma_N^fin` remains a component of the datum
`C3_N^fin=(Ward_N^fin,sigma_N^fin)`; it is not applied to a kernel. Shadow
recovery requires newly typed maps

```text
sh_N:Sym_N^loc -> Shadow_N^fin,

Sigmahat_N^fin
 :=sh_N compose Loc_N^phys compose iota_N^Q408
   :Ker_N^Q408 -> Shadow_N^fin,

Ev_N^C3:
 C3_N^fin x Ker_N^Q408 x Test_N^fin -> R.        (S17)
```

The comparison to the sealed datum is only the component/test equality

```text
<Sigmahat_N^fin(K),tau>_Shadow
 =Ev_N^C3(C3_N^fin,K,tau)
for K in Ker_N^Q408 and tau in Test_N^fin.       (S17a)
```

Building `Ev_N^C3` and proving `(S17a)` are obligations. No expression
`sigma_N^fin(K)` is used.

### 1.3 Complete map-level properties

[YOURS — full B1 contract] A member of the `Loc` solution set must display
all of the following.

1. **Linearity and topology**

   ```text
   Loc_N^phys(aK+bK')
    =a Loc_N^phys(K)+b Loc_N^phys(K'),           (S18)

   iota_N^H(as+bs')
    =a iota_N^H(s)+b iota_N^H(s'),               (S19)
   ```

   with continuity in the named source and target topologies.

2. **Locality and Ward/contact typing**

   ```text
   Loc_N^phys(D_N^Loc) subset Sym_N^loc,

   WardData_N is a named topological vector space,
   BndWard_N is a closed linear subspace of WardData_N,

   Wardtilde_N:Sigma_N^(2),Ward -> WardData_N
    is linear and continuous,
   Wardtilde_N(Ctc_N^sym) subset BndWard_N,

   Ward_N^loc([s]_Ctc_sym)
    :=[Wardtilde_N(s)]_BndWard,

   Ward_N^loc(Loc_N^phys K)=0
    in WardData_N/BndWard_N.                    (S20)
   ```

   Formation of the diagonal/principal symbol must pass its wavefront or
   pseudodifferential-domain criterion before `(S20)` is asserted.

3. **Field symbol and long-wavelength limit**. Build a field-symbol map in a
   named topology and prove the limit exists:

   ```text
   sigma_field,N:Sym_N^loc -> FieldSym_N,

   LW_2,N:D_N^Loc -> FieldSym_N
    is linear and continuous in the named topology,

   LW_2,N(K)(x,q)
    :=lim_(epsilon->0+) epsilon^(-2)
       sigma_field,N(Loc_N^phys K)(x,epsilon q). (S20a)
   ```

   The Maxwell/contact separation must then be displayed on that same
   topology:

   ```text
   FieldSym_N
    =R sigma_Maxwell,N direct-sum FieldSym_N^perp,

   sigma_Maxwell,N:=sigma_field,N(s_Max,N^unit),

   p_Mx,N^field:FieldSym_N->R,
   p_Mx,N^field(a sigma_Maxwell,N+r)=a
    for r in FieldSym_N^perp,
   sigma_field,N(Sym_N^perp) subset FieldSym_N^perp,

   LW_2,N(K)
    =p_Mx,N^field(LW_2,N(K)) sigma_Maxwell,N
      +sigma_perp,N(K),

   sigma_perp,N(K) in FieldSym_N^perp,
   p_Mx,N^field(LW_2,N(K))
    =p_Mx,N^sym(Loc_N^phys K),
   p_Mx,N^sym(s_Max,N^unit)=1,

   LW_2,N(kappa_T,N)=sigma_Maxwell,N,
   sigma_perp,N(kappa_T,N)=0.                   (S20b)
   ```

   The limit, its topology, and its restriction/reality/Ward squares are
   construction obligations; no finite shadow is promoted to `(S20a)`.

4. **Kernel and record-visibility certificates**. Name

   ```text
   K_Loc,N:=ker(Loc_N^phys),
   K_LW,N:=ker(LW_2,N),
   K_Mx,N:=ker(p_Mx,N^sym compose Loc_N^phys),

   V_Q396,N^prof subset O_prof,N,
   V_Q396,N^ker
    :=Kernbarhat_N(V_Q396,N^prof) subset D_N^Loc,

   V_rec,N subset D_N^Loc,
   K_phys,N^licensed subset D_N^Loc.             (S20c)
   ```

   `K_phys,N^licensed` must be named before `Loc` from an independent
   physical-null theorem. If no such theorem exists, the only licensed
   value is `{0}`.

   Compute, rather than suppress,

   ```text
   K_Loc,N intersection V_Q396,N^ker,
   K_LW,N intersection V_Q396,N^ker,
   K_Mx,N intersection V_Q396,N^ker.            (S20d)
   ```

   On the declared record-visible subspace, the independent licensed kernel
   must agree with the actual localization kernel:

   ```text
   K_Loc,N intersection V_rec,N
    =K_phys,N^licensed intersection V_rec,N.    (S20e)
   ```

   Any nonzero record-visible intersection in `(S20d)` requires an
   independent physical-kernel theorem; it may not be called contact by
   nomenclature alone.

5. **Quotient and old-shadow fidelity**: `(S6)`, `(S7)`, `(S17)`, and
   `(S17a)`.

6. **Contact/Hodge compatibility**

   ```text
   [E_C,N,Delta_N^Hdg]=0,

   iotatilde_N^H:Sigma_N^(2),Ward -> C_N^k,
   Ctc_N^sym subset ker(iotatilde_N^H),

   iota_N^H([s]_Ctc_sym):=iotatilde_N^H(s),
   iota_N^H([s]_Ctc_sym)
    =(1-E_C,N)iota_N^H([s]_Ctc_sym).            (S21)
   ```

   The kernel clause includes the full contact-plus-boundary ideal and is
   the descent proof. The final projection equation alone would not prove
   quotient well-definedness. No inference says A5 constructs either
   `iotatilde_N^H` or `iota_N^H`.

7. **Rank-preserving transport**. For every certified rank-preserving arrow
   `f:N->M`, build

   ```text
   etahat_f:D_N^Loc -> D_M^Loc,
   j_f^Sym:Sym_N^loc -> Sym_M^loc,               (S22)

   etahat_id=id_DLoc,
   etahat_gf=etahat_g etahat_f,

   j_id^Sym=id_Sym,
   j_gf^Sym=j_g^Sym j_f^Sym.                    (S22a)
   ```

   and prove, including the `L_T` branch,

   ```text
   etahat_f iota_N^Q408
    =iota_M^Q408 eta_f^K,

   etahat_f Kernbarhat_N
    =Kernbarhat_M j_f^prof,                      (S23)

   Loc_M^phys etahat_f
    =j_f^Sym Loc_N^phys,                         (S24)

   iota_M^H j_f^Sym
    =j_f^C iota_N^H.                             (S25)
   ```

   Equations `(S24)` and `(S25)` imply the cochain square; they do not follow
   from `d/delta/P_H` naturality.

8. **Cycle-creating scope**. For a cycle-creating arrow `a:N->M`, type the
   target old-image subspaces and downward restrictions

   ```text
   D_M^old subset D_M^Loc,
   Sym_M^old subset Sym_M^loc,
   iota_M^H(Sym_M^old) subset C_M^(k,old),

   rho_a^D:D_M^old -> D_N^Loc,
   rho_a^Sym:Sym_M^old -> Sym_N^loc,
   rho_a^C:C_M^(k,old) -> C_N^k.                (S25a)
   ```

   The only required equalities are the downward old-image squares

   ```text
   rho_a^Sym Loc_M^phys|_(D_M^old)
    =Loc_N^phys rho_a^D,

   rho_a^C iota_M^H|_(Sym_M^old)
    =iota_N^H rho_a^Sym.                         (S25b)
   ```

   No canonical upward lift of the target's new-cycle complement is added.

9. **Covariance and provenance**: support, A1 bundle typing, gauge/reality,
   R4 units, restriction, zero extension, batching, common-refinement, and
   DoR-008 finite shadows must commute through the following displayed
   schema (with each named transport separately typed):

   ```text
   j_f^Shadow:Shadow_N^fin->Shadow_M^fin,
   j_f^Field:FieldSym_N->FieldSym_M,

   R_N^D:D_N^Loc->D_Nbar^Loc,
   R_N^Sym:Sym_N^loc->Sym_Nbar^loc,

   U_f^D:D_N^Loc->D_M^Loc,
   U_f^Sym:Sym_N^loc->Sym_M^loc,

   z_f^D:D_N^Loc->D_M^Loc,
   z_f^Sym:Sym_N^loc->Sym_M^loc,

   sh_M j_f^Sym
    =j_f^Shadow sh_N,

   sigma_field,M j_f^Sym
    =j_f^Field sigma_field,N,

   LW_2,M etahat_f
    =j_f^Field LW_2,N,                           (S25c)

   Loc_Nbar^phys R_N^D
    =R_N^Sym Loc_N^phys,

   Loc_M^phys U_f^D
    =U_f^Sym Loc_N^phys,                         (S25d)

   Loc_M^phys z_f^D
    =z_f^Sym Loc_N^phys,

   Supp(Loc_N^phys K)
    subset Delta_N^*(Supp K),                   (S25e)

   Loc_(N1,N2)^phys(K1,K2)
    =(Loc_N1^phys K1,Loc_N2^phys K2),           (S25f)

   etahat_g1 etahat_f1
    =etahat_g2 etahat_f2,

   j_g1^Sym j_f1^Sym
    =j_g2^Sym j_f2^Sym                          (S25g)
   ```

   on every admitted common-refinement diamond
   `g1 f1=g2 f2`. `(S25f)` is an ordered product, not a scalar contraction.
   The shadow square `(S17a)` and `(S25c)` must reproduce the sealed finite
   data; the symbols `R`, `U`, and `z` denote the already typed
   reality/orientation, unit, and zero-extension transports, respectively.

10. **Reader freedom**. The allowed dependency branch is

   ```text
   A1 paths/currents/bundle + DoR-019 metric
    + Q-408 kernel + A5 contact/Hodge data
    -> Kernbarhat -> Loc^phys -> iota^H -> Loc^C. (S26)
   ```

   The prohibited reverse branch is

   ```text
   algebraic reader/(chi,T)/desired coefficient/FALSE-ANCHOR
    -/-> Kernbarhat, Loc^phys, iota^H, or p_Mx^sym. (S27)
   ```

11. **No seed axiom**. Neither

   ```text
   P_H,N Loc_N^C(kappa_T,N) != 0                 (S28)
   ```

   nor any normalization of `(S28)` is a property of `Loc`. `(S28)` is the
   later structural end-test condition, not a construction clause. The
   local-symbol obligation `(S15a)` does not promote `(S28)`.

```text
LOC_SPECIFICATION_B1 = COMPLETE
FALSE_ANCHOR_CONSUMED = false
```

## 2. B2 — determination from sealed stock

### 2.1 The derivation reaches a bilocal kernel and stops

[PROVABLE] The A1/Q-408 branch builds

```text
c in K_N
 -> J_c in D'(M_N;T*M_N),                       (D1)

H:K_N->K_N^*
 -> Kern_N^fin(H)
      in D'(M_N x M_N;T*M_N box-times T*M_N).   (D2)
```

The second line remains an arbitrary path-bilocal distribution. A
pseudodifferential Schwartz kernel does not require contraction with a
selected field to possess a principal symbol. What is missing is instead the
displayed structural chain

```text
Kern_N^fin(H)
 in a properly supported conormal/Schwartz-kernel operator class
 -> fiber Fourier transform in a named density/trivialization calculus
 -> sigma_2^ker(Kern_N^fin(H))
      in Sigma_N^(2),Ward
 -> [sigma_2^ker(Kern_N^fin(H))]_Ctc_sym
      in Sym_N^loc.                              (D2a)
```

A1 proves support and path/current transport, but it proves neither the
conormal wavefront condition, the operator/density identification, nor the
principal-symbol and contact-quotient arrows in `(D2a)`.

[PROVABLE] DoR-019's same-sector Riesz operation gives at most

```text
R_K,N^(-1) H in End(K_N).                       (D3)
```

It does not yield

```text
R_K,N^(-1) H in C_N^k.                          (D4-false)
```

The lawful C/K mixing set contains only the already named R4 mixed blocks;
an unnamed conversion `(D4-false)` fails the no-implicit-cross-sector
certificate.

[PROVABLE] A5 starts with a vector already in `C_N^k` and proves contact
reduction there. Consequently the sequence

```text
D'(M_N x M_N)
 -/-> Sym_N^loc
 -/-> C_N^k                                     (D5)
```

is not completed by A5. A6 records the same boundary by retaining full
new-cycle `Loc/pi_Mx` as an independent post-scope object.

Therefore

```text
A1 + DoR-019 + A5 + A6
 does not derive Kernbarhat_N, Loc_N^phys, or iota_N^H. (D6)
```

### 2.2 Exact conditional freedom; neither derivation nor impossibility

[PART-PROVABLE] Fix admissible source, Hodge, and target **families over all
stages and arrows**, and suppose one compatible localization family

```text
L^0:=(Loc_N^(phys,0))_N                          (D7)
```

exists with those structures frozen. Every other localization in that same
fiber has the conditional form

```text
Loc_N^(phys,1)=Loc_N^(phys,0)+A_N,               (D8)
```

where the exact correction set is defined without abbreviating affine gates:

```text
A_Fiber(L^0)
 :={ (A_N)_N :
      every A_N:D_N^Loc->Sym_N^loc is continuous,
        linear, and reader-free;
      sh_N compose A_N=0;
      A_N(kappa_T,N)=0;
      replacing Loc_N^(phys,0) by
        Loc_N^(phys,0)+A_N and then recomputing every
        Loc-derived shadow, LW_2, sigma_perp, and kernel datum
        satisfies every membership gate (S18)--(S27)
        on every stage, arrow, and diamond }.    (D9)
```

At a rank-one loop, any *existing* harmonic component of such a correction
has the form

```text
P_H,N iota_N^H A_N(K)=a_N(K)c_N,                (D10)
```

and would shift the corresponding pairing by

```text
<iota_N^H(Loc_N^(phys,0)(K)+A_N(K)),c_N>
 =<iota_N^H Loc_N^(phys,0)(K),c_N>
   +a_N(K)<c_N,c_N>.                            (D11)
```

Equations `(D8)`--`(D11)` classify the localization fiber conditional on
inhabitance. They do not assert that a nonzero `A`, or even `L^0`, exists.
The other data vary in family-level fibers:

```text
Ext
 :={ (D_N^ker,iota_N^Q408,Sch_N,Krawhat_N)_N
      satisfying (S2b)--(S8) and every source
      transport/common-refinement equation },

Hdg
 :={ (iotatilde_N^H,iota_N^H)_N
      satisfying (S14),(S19),(S21),(S25),
      (S25b), and every Hodge diamond equation },

Tar
 :={ Loc-independent target inputs
      (Sigma^(2),Ward,sigma_2^op,sigma_2^ker,Ctc^sym,Sym^loc,
       s_Max^unit,Mx^sym,Proj_Mx^sym,p_Mx^sym,
       FieldSym,sigma_Maxwell,FieldSym^perp,p_Mx^field,
       WardData,BndWard,sh,sigma_field,
       V_Q396^prof,V_rec,K_phys^licensed)
      satisfying their target-side topology, split,
      normalization, reality, unit, and transport laws },

Sol_Loc
 :=Ext fiber-product_Gates Hdg
      fiber-product_Gates Tar
      fiber-product_Gates Product_N Hom(D_N^Loc,Sym_N^loc). (D12)
```

Thus `(D8)`--`(D11)` classify the `Loc` fiber of `Sol_Loc` after the
Loc-independent source, Hodge, and target inputs are fixed and the derived
data are recomputed. `Sol_Loc` is a compatibility fiber product, not the
unconstrained Cartesian product `Ext x Hdg x Tar`.

No sealed theorem proves the solution family empty. Hence `OBSTRUCTED` is
not licensed. No sealed construction supplies a member. Hence `DERIVED` is
not licensed.

### 2.3 [YOURS] Minimal full-gate candidate

The candidate is a family, not a selected map:

```text
M_N^(Hdg,k)
 :=delta_N^(k+1) d_N^k:C_N^k->C_N^k,

P_N^Loc
 :=(M_N^loc,M_N^(Hdg,k),Op_N^(loc,2),
    D_N^ker,iota_N^Q408,Sch_N,Krawhat_N,Kernbarhat_N,
    Sigma_N^(2),Ward,sigma_2^op,sigma_2^ker,
    Ctc_N^sym,Sym_N^loc,
    s_Max,N^unit,Mx_N^sym,Proj_Mx,N^sym,p_Mx,N^sym,
    Loc_N^phys,sh_N,Sigmahat_N^fin,Ev_N^C3,
    Wardtilde_N,WardData_N,BndWard_N,Ward_N^loc,
    sigma_field,N,LW_2,N,
    FieldSym_N,sigma_Maxwell,N,FieldSym_N^perp,
    p_Mx,N^field,sigma_perp,N,
    K_Loc,N,K_LW,N,K_Mx,N,K_phys,N^licensed,
    V_Q396,N^prof,V_Q396,N^ker,V_rec,N,
    iotatilde_N^H,iota_N^H,
    eta_f^ker,etahat_f,j_f^Sym,j_f^Shadow,j_f^Field,
    R_N^D,R_N^Sym,U_f^D,U_f^Sym,z_f^D,z_f^Sym,
    rho_a^D,rho_a^Sym,rho_a^C),

L_F2
 := { (P_N^Loc)_N :
      all membership clauses (S2b)--(S27) hold,
      D_N^Loc subset Sch_N(Op_N^(loc,2)),
      Op_N^(loc,2) is the declared properly supported
        classical pseudodifferential operator class of order <=2,
      Loc_N^phys(K)=[sigma_2^ker(K)]_Ctc_sym,
      kappa_T,N=Sch_N(M_N^loc) as typed in (S4),
      iota_N^H Loc_N^phys(kappa_T,N)
        in closure(ran(M_N^(Hdg,k))) }.          (C1)
```

`(S28)` is expressly not a membership clause of `(C1)`; it is the later
nonzero seed condition. Including `(S28)` would contradict the candidate's
`F^2` range clause and would silently author the end test.

Here the candidate's localization formula is displayed:

```text
Loc_N^phys(K):=[sigma_2^ker(K)]_Ctc_sym.         (C2)
```

For the continuum Maxwell operator its order-two symbol is

```text
sigma_2^op(M_N^loc)(x,q)
 = |q|_g^2 Id-q tensor q^sharp.                  (C3)
```

`(C2)` is reader-free and linear on its declared operator class:

```text
Loc_N^phys(aK+bK')
 =[sigma_2^ker(aK+bK')]_Ctc_sym
 =[a sigma_2^ker(K)+b sigma_2^ker(K')]_Ctc_sym
 =a Loc_N^phys(K)+b Loc_N^phys(K').             (C4)
```

This branch is minimal in the following exact sense: it adds no smoothing
scale, trace normalization, Cauchy slice, cycle generator, gauge/frame
representative, target coefficient, harmonic counterterm, or reader. The
principal symbol is taken only on a domain where it exists. Lower-order
operator/quantization differences lie in `ker(sigma_2^op)` and vanish before
the quotient. An order-two principal-symbol difference is absorbed only if
it is proved to lie in `Ctc_N^sym`.

The stock does not prove any Q-408 kernel lies in the domain in `(C1)`, does
not construct `kappa_T,N`, and does not build `iota_N^H`. Thus `(C1)` is an
authorable full-gate candidate and may be empty.

### 2.4 Genuine alternatives and exact prices

No alternative row is adopted or supplied with a member. Candidate `(C1)`
is the minimal proposal being gated, not an inhabited selection.

| Route | Exact added structure | Genuine alternative | Void/falsifier |
|---|---|---|---|
| refinement/microlocal derivation | refinement-density theorem, completed-current topology, diagonal-local limit, order-two symbol, Ward/contact split | derive localization from records rather than an external field | limit absent, off-diagonal support persists, filtration dependence, relation or Ward failure |
| external-field localization family | retain every A1/A2-compatible covariant localization and every compatible `Krawhat` extension | use the admitted external bundle/field family without selecting a member | family empty, selected gauge/frame/background, output-sensitive member rule |
| spectral Cauchy route | Cauchy Hilbert space, generator/domain, boundary condition, covariant low-spectrum scaling | spectral rather than microlocal localization | hidden foliation, target-dependent spectrum, domain or contact failure |
| operator-space repair | keep `Loc` in local order-two operator symbols and project onto the Maxwell operator line there | do not vectorize `F^2` into a harmonic one-form | requires a reviewed J7/J2 typing amendment; void if no natural operator split |
| A6 post-scope/reject | add no map and retain full J2 as conditional | preserve present law while awaiting an independent construction | no physical J2 or coefficient-face inhabitance follows |

The following are not admissible alternatives: finite normalized trace,
Calkin-at-finite-rank, `R_K=L_T`, “contact equals Maxwell,” reader-defined
`pi_Mx`, or a harmonic correction selected to make `(S28)` true.

### 2.5 Full-gate void conditions

The candidate family `(C1)` is void if any one of the following witnesses
occurs.

1. `Sch_N` or `kappa_T,N` cannot be realized with the fixed bundle/density
   convention in the common kernel topology, the two topologies in `(S7)`
   fail to coincide, or the embedding `(S2b)` fails continuity, injectivity,
   or naturality.
2. `Krawhat_N(Rel_N)` contains a nonzero element.
3. A Q-408 kernel is not properly supported or lies outside the symbol
   domain; equivalently its required diagonal pullback fails the wavefront
   transversality test.
4. `Sym_N^loc`, `Ctc_N^sym`, or the closed split `(S10)` does not exist, or the
   independent local-symbol calculation fails `(S15a)`.
5. No reader-free `iota_N^H` satisfies `(S21)` and `(S25)`.
6. Either nonidentity base square `(S23)`--`(S25)` fails on the reciprocal
   generator or the `q=2` refinement generator.
7. The shadow construction `(S17)` or datum comparison `(S17a)`, Ward/contact
   law, units, reality, support, restriction, batching, or common-refinement
   square fails.
8. A contact quotient deletes a record-visible cycle kernel.
9. A cycle-creating arrow is assigned an unlicensed canonical upward lift.
10. Any construction input references the algebraic reader, `chi`, reader
    `T`, a desired coefficient, or `(FALSE-ANCHOR)`.
11. A hidden smoothing scale, measure, contour, boundary condition, frame,
    Cauchy slice, gauge representative, or cross-sector unit is required.
12. Any `L in L_F2` has
    `P_H,N iota_N^H Loc_N^phys(kappa_T,N) != 0`. Such a witness voids
    `L_F2`; a separately declared and falsifiable `Hol_N` seam belongs to a
    different candidate family.
13. The field symbol or limit `(S20a)` does not exist in the named topology,
    changes under a certified restriction/realization, or fails `(S20b)`.
14. Any kernel or Q-396 intersection in `(S20c)`--`(S20d)` remains
    undisclosed, or record-visible injectivity `(S20e)` fails without an
    independent physical-kernel theorem.

```text
LOC_DERIVED_FROM_SEALED_STOCK = false
LOC_IMPOSSIBILITY_PROVED = false
FULL_GATE_FAMILY_NONEMPTY = not proved
LOC_DETERMINATION = FULL_GATE_CANDIDATE_SPECIFIED /
  INHABITANCE_AND_NONOBSTRUCTION_UNDETERMINED
```

## 3. B3 — immediate consequence board

Because `(C1)` has no member of record, this section separates conditional
display from current status.

### 3.1 Naturality base cases and induction

[PART-PROVABLE — formal identity law conditional on a typed map member]
Current sealed stock supplies no physical `Loc` member and certifies no
nonidentity square. For `K in D_N^Loc`, if the
identity maps exist with their required types, every intermediate equality
is

```text
Loc_N^C etahat_id,N(K)
 =Loc_N^C(id_D,N(K))
 =Loc_N^C(K)
 =id_C,N(Loc_N^C(K))
 =j_id,N^C Loc_N^C(K).                           (N1)
```

[PART-PROVABLE — conditional on a member of `(C1)`] Let `u:N_RL->M_RL`
be the reciprocal-loop rank-preserving generator. Equations `(S24)` and
`(S25)` give the complete cochain display

```text
Loc_MRL^C etahat_u
 =iota_MRL^H Loc_MRL^phys etahat_u
 =iota_MRL^H j_u^Sym Loc_NRL^phys
 =j_u^C iota_NRL^H Loc_NRL^phys
 =j_u^C Loc_NRL^C.                               (N2)
```

Let `r_nm:N_n->N_m` be the sealed nontrivial `q=2` refinement generator.
The second base display is

```text
Loc_Nm^C etahat_rnm
 =iota_Nm^H Loc_Nm^phys etahat_rnm
 =iota_Nm^H j_rnm^Sym Loc_Nn^phys
 =j_rnm^C iota_Nn^H Loc_Nn^phys
 =j_rnm^C Loc_Nn^C.                              (N3)
```

For certified `f:N->M` and `g:M->L`, the composition display is

```text
Loc_L^C etahat_gf
 =Loc_L^C etahat_g etahat_f
 =j_g^C Loc_M^C etahat_f
 =j_g^C j_f^C Loc_N^C
 =j_gf^C Loc_N^C.                                (N4)
```

For a word `F_k=f_k...f_1`, assume

```text
Loc_Nk^C etahat_Fk=j_Fk^C Loc_N0^C.             (N5)
```

Then one further certified generator gives

```text
Loc_N(k+1)^C etahat_F(k+1)
 =Loc_N(k+1)^C etahat_f(k+1) etahat_Fk
 =j_f(k+1)^C Loc_Nk^C etahat_Fk
 =j_f(k+1)^C j_Fk^C Loc_N0^C
 =j_F(k+1)^C Loc_N0^C.                          (N6)
```

Thus a future member with both base squares extends to their generated
subcategory. Since neither base square has a sealed member today, `(N2)` and
`(N3)` are conditional displays. The only surviving scope is the **formal
identity-law scope conditional on a physical map member**; there is no
current certified physical `Loc` scope because no member exists.

### 3.2 `(M5)` subject and seed condition

[PART-PROVABLE — conditional on `(S3)`--`(S15)`] At the named reciprocal
loop stage, define in dependency order

```text
kappa_T,RL
 :=Kernbarhat_RL(Q_RL(L_T,RL)),

s_T,RL^C
 :=Loc_RL^C(kappa_T,RL),

m_T,RL
 :=P_H,RL(s_T,RL^C),

q_T,RL
 :=<s_T,RL^C,c_RL>_RL.                           (N7)
```

The end-test condition would then be the displayed statement

```text
q_T,RL != 0.                                    (N8)
```

No member of `(C1)` is bound, so `(N7)` is not a sealed record-level term and
`(N8)` is not evaluated. The current seed subject therefore remains
undefined. In particular, neither `(FALSE-ANCHOR)` nor `p_loc(L_T)=1` is
used to assign `(N8)`.

### 3.3 J2 premise board

| Premise | Status after this artifact | Exact reason |
|---|---|---|
| `(F1)` quotient descent/transport | **SUBSCOPE** | full only on the sealed linear-isomorphism subcategory; generic reciprocal and general `Ref_path` descent/identification remain open |
| extended `Kernbarhat` descent | **CANDIDATE ONLY** | `(S3)`--`(S7)` specify it; no member proves the `L_T` branch |
| `(F2)` kernel transport | **PROVEN only on old represented Q-408 scope; candidate on `L_T`** | `(S23)` is an uninstantiated full-gate clause |
| `(F3)` `Loc` naturality | **FORMAL IDENTITIES ONLY, conditional on a typed member** | `(N1)` is tautological after a map exists; `(N2)` and `(N3)` are conditional uninstantiated base squares |
| `(F4)` Hodge projector naturality | **PROVEN on certified rank-preserving scope** | independent `d/delta/P_H` theorem |
| map-level linearity | **CANDIDATE ONLY** | `(C4)` applies only if the physical symbol domain is inhabited |
| scalar `pi_Mx`/normalization | **NOT BUILT** | `p_Mx^sym` and the Hodge coordinate are separately typed; no reader-free equality between them is proved |
| equality with an algebraic reader | **NOT PROVED** | requires the independent coefficient equalizer; it cannot define `Loc` or `pi_Mx` |

The general rank-preserving J2 chain is therefore not premise-complete.

### 3.4 Coefficient-face inhabitance basis

Conditional on `(N7)`, the address predicate is well typed as

```text
N in A_RP^+  iff  P_H,N Loc_N^C(kappa_T,N) != 0. (N9)
```

No stage satisfies `(N9)` as a fact of record in this artifact. Thus the
coefficient face gains a full-gate address but no inhabitance basis. The
reciprocal-loop stage cannot be cited as an inhabitant unless a future
reader-free construction forms `(N7)` and separately establishes `(N8)`.

```text
J2_RP_PREMISE_COMPLETE = false
A_RP_PLUS_INHABITANT_OF_RECORD = none
COEFFICIENT_FACE_BASIS = conditional only at this point
```

## 4. B4 — battery

### 4.1 F_PLDEC and false-anchor regression

The construction order used here is

```text
verified geometry/current/kernel stock
 -> B1 source and target types
 -> derivability test
 -> full-gate candidate family
 -> conditional consequences
 -> structural F^2/holonomy test.                (B1)
```

The algebraic reader branch is downstream and disconnected:

```text
reader/(chi,T)/desired coefficient/FALSE-ANCHOR
 -/-> every construction arrow in (B1).         (B2)
```

The false anchor appears only as a named prohibited premise. No equality in
this artifact is derived from it.

```text
F_PLDEC = PASS
FALSE_ANCHOR_USED_AS_THEOREM = false
READER_USED_TO_BUILD_LOC = false
```

### 4.2 `F^2` and flat holonomy

[PROVABLE] For every admitted `v in C_N^k` and every harmonic
`c in im(P_H,N) subset C_N^k`,

```text
d_N^k c=0,
delta_N^k c=0.                                  (B3)
```

With `M_N^(Hdg,k)=delta_N^(k+1)d_N^k`, adjointness gives the fully
displayed, degree-correct chain

```text
<M_N^(Hdg,k)v,c>_(C_N^k)
 =<delta_N^(k+1)d_N^k v,c>_(C_N^k)
 =<d_N^k v,d_N^k c>_(C_N^(k+1))
 =<d_N^k v,0>_(C_N^(k+1))
 =0.                                             (B4)
```

Therefore

```text
closure(ran(M_N^(Hdg,k))) perpendicular im(P_H,N),
P_H,N(closure(ran(M_N^(Hdg,k))))={0}.           (B5)
```

[YOURS — exact scope] For a candidate member `L in L_F2`, define only the
member-relative route

```text
HRoute_N(L)
 :=P_H,N iota_N^H Loc_N^phys(kappa_T,N).        (B5a)
```

Candidate clause `(C1)` and `(B5)` give, for every member without selecting
one,

```text
for every L in L_F2,
HRoute_N(L)
 in P_H,N(closure(ran(M_N^(Hdg,k))))
 ={0},
hence HRoute_N(L)=0.                             (B5b)
```

Thus every member of the minimal `F^2` candidate has no route from its
`L_T` image to the harmonic line. The family may be empty; `(B5b)` is not a
stock-wide existence or zero theorem.

For every `L in L_F2` and harmonic generator `c_N`, the corresponding
candidate pairing is therefore

```text
q_T,N(L)
 :=<iota_N^H Loc_N^phys(kappa_T,N),c_N>_N
 =<P_H,N iota_N^H Loc_N^phys(kappa_T,N),c_N>_N
 =<HRoute_N(L),c_N>_N
 =<0,c_N>_N
 =0.                                             (B5c)
```

Consequently no member of `(C1)` supplies an `A_RP^+` inhabitant. This is a
universal property of candidate membership, not an execution of the
stock-wide seed: no `L` is bound, and the record-level term `(N7)` remains
undefined.

```text
L_F2_A_RP_PLUS_SUPPORT = empty for every candidate member
COEFFICIENT_FACE_BASIS = conditional outside L_F2; absent on L_F2
```

The stock-wide term in `(N7)` remains unformed, so `(B4)` is not an execution
of the seed end test. It is a universal operator identity applied to the
declared candidate class. If a future proposal adds a nonzero harmonic term,
it must display a new map

```text
Hol_N:D_N^Loc -> im(P_H,N)                       (B6)
```

with its own provenance, units, covariance, restriction, and falsifiers.
It would define a distinct enlarged package

```text
Sym_N^ext:=Sym_N^loc direct-sum im(P_H,N),

Loc_N^(phys,ext)(K)
 :=(Loc_N^phys(K),Hol_N(K)),

iota_N^(H,ext)(s,h)
 :=iota_N^H(s)+h,

Loc_N^(C,ext)
 :=iota_N^(H,ext) compose Loc_N^(phys,ext)
  =Loc_N^C+Hol_N.                                (B7)
```

This is a different candidate family, not a redefinition of `(S15)`. Without
independently constructing `(B6)` and every enlarged-target gate, `(B7)` is a
hidden holonomy counterterm, not localization of the local Maxwell `F^2`
symbol.

The A1 flat-holonomy attack reinforces the distinction: equal curvature and
equal characteristic class do not identify connections with inequivalent
flat holonomy. Curvature/F² data therefore cannot be relabeled as a Wilson
period.

```text
HOLONOMY_ROUTE_SCOPE = minimal F^2-compatible full-gate candidate (C1)
HOLONOMY_ROUTE_QUANTIFIER = for every member of L_F2; L_F2 may be empty
STOCK_WIDE_HOLONOMY_TERM = unformed
END_TEST_EXECUTED = false
```

### 4.3 Anti-tuning ledger

| Attack | Control displayed above | Result |
|---|---|---|
| define `Loc` from a desired reader value | dependency prohibitions `(S26)`, `(S27)`, `(B2)` | clean |
| use physical-J2 normalization as existence | `(FALSE-ANCHOR)` named and quarantined | clean |
| identify profile `L_T`, kernel, symbol, and cochain | four types separated in `(S12)` | clean |
| hide `L_T` outside `Kernbar`'s domain | explicit extension and relation descent `(S3)`--`(S8)` | clean |
| equate `sigma_fin` with a cochain map | shadow recovery only in `(S17)` | clean |
| infer `Loc` from Riesz or A5 | stopped arrows `(D3)`--`(D6)` | clean |
| assert candidate inhabitance | nonemptiness explicitly not proved | clean |
| tune a harmonic component | prohibited unless independent `(B6)` is built | clean |
| use a hidden scale or Cauchy slice | candidate minimality and void 11 | clean |
| run seed/end test or numeric evaluation | `(N8)` displayed but not evaluated; fences unchanged | clean |
| bind a membership member | no member selected; all candidate consequences scoped conditionally | clean |

### 4.4 Self verb audit

| Verb/status used | Displayed witness | Audit |
|---|---|---|
| `SPECIFICATION ... COMPLETE` | source `(S1)`--`(S8)`, target `(S9)`--`(S17a)`, physical/limit/kernel/transport properties `(S18)`--`(S28)` | clean |
| `not derived` | explicit type stops `(D1)`--`(D6)` | clean |
| `obstruction not established` | no stock emptiness theorem; conditional fibers `(D7)`--`(D12)` | clean; no nonobstruction or existence claimed |
| `AUTHORABLE_CANDIDATE` | candidate family and formula `(C1)`--`(C4)`, alternatives, fourteen voids | clean; no member selected |
| `IDENTITY_ONLY` | formal conditional identity display `(N1)`; nonidentity displays `(N2)`, `(N3)` explicitly conditional | clean; no physical member claimed |
| `STILL_UNDEFINED` | missing member for source extension and `Loc`; `(N7)` conditional only | clean |
| `structurally_absent` | universal adjoint chain `(B3)`--`(B5)` on exact candidate scope | clean; no stock-wide zero claimed |
| `J2 incomplete` | premise table and missing independent maps | clean |
| `F_PLDEC PASS` | dependency separation `(B1)`, `(B2)` | clean |

```text
MACHINERY-APPEAL = true
MACHINERY-APPEAL_WITNESS = sealed stock supplies no inhabitant or emptiness theorem for the full-gate Loc family
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
comparison_to_measured_constants = none
```

LOC_SPEC = stated
LOC = AUTHORABLE_CANDIDATE (+full membership gate `(S2b)`–`(S27)` specified and seed `(S28)` excluded; inhabitance and nonobstruction undetermined; minimal `F^2` family `(C1)` may be empty; no member selected)
SEED_CONDITION = STILL_UNDEFINED
NATURALITY_BASE_CASES = IDENTITY_ONLY (+formal identity conditional on a typed member; no physical member)
HOLONOMY_ROUTE = structurally_absent (+for every member of candidate `(C1)` only; `(C1)` may be empty; stock-wide route unformed)
VERB_AUDIT_SELF = CLEAN
