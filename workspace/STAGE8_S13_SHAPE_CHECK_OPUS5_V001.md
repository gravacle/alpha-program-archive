# STAGE8_S13_SHAPE_CHECK_OPUS5_V001

ROLE: BLIND ADVERSARIAL CHECKER (OPUS5-CHECK-S13). TASK: REFUTE the subject's verdict that the
S13 threshold is LOCK-SHAPED by exhibiting a FIBER-SHAPED (dimensionful, R-carrying,
eigenvalue-scale) threshold that is RECORD-FURNISHABLE surface-natively. Skepticism cuts toward
finding Finish A still live. STRUCTURAL / TYPING ONLY.

GATES HELD: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
JOINT_ANCHOR_DERIVED = false. NOTHING COMPUTED — no value, threshold, eigenvalue value, kappa,
scale, or alpha evaluated, bounded, or estimated; every numeric string below is a sealed-text
quotation or a symbol; every scale symbolic. No measured-constant comparison. No register,
tracker, continuation, plan, road, or ledger file read. No git action. Testimony given zero
weight — every typing re-derived from the cited sources, not from the subject's certifications.

## SEALS VERIFIED AT PATH (shasum -a 256, before use)

```text
SUBJECT  workspace/STAGE8_S13_THRESHOLD_SHAPE_FABLE_V001.md
         ef977b167202e95e7ad453d8d03caea0fe5fcae2a8117aa1c49a9354f84ee6d6   MATCHES-TASKED-SEAL
ANCHOR   alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md
         20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df   MATCHES-TASKED-SEAL
CENSUS   workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md
         04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d   MATCHES-TASKED-SEAL
R34      workspace/R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md
         14b807227f5594789b2cab042fe3633640f51921aacf94c72536a17137e72b45   MATCHES-SUBJECT-TABLE
                                                                            + sidecar-consistent
```

Additional named files read for enumeration/attack (workspace, named-file reads only):
DBR760 = STAGE8_7A_DBR_SPECTRUM_DARIO_V001.md; DBR789 = STAGE8_7A_DBR_BETA_SECTORS_V3_DARIO_V001.md;
UCC = BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md; CSP =
BID_CHIRAL_SOURCE_RECORD_INCIDENCE_PARENT_V001.md; GK =
STAGE8_GAMMA_K_NONRETURN_CHARGED_SPECTRUM_OBS06_TEST_V001.md. These are consumed for their
R-occurrence structure only; DBR relay artifacts carried at relay-custody level, not upgraded.

---

## 0. THE PIVOT THE WHOLE VERDICT TURNS ON — RE-ESTABLISHED FROM SOURCE, NOT TESTIMONY

The beta action is not free-standing; it is DEFINED. CENSUS §0 / V002 map: `R = beta c Delta tau`,
so `R -> beta R` at fixed `Delta tau`, with `T_R and the branch data unchanged`. DBR789 §1.1
(:98) states it without ambiguity: **"`beta` is defined as the parameter of the common radius
rescaling"** `(R_T, R_Q, r_flux) -> beta (R_T, R_Q, r_flux)`, and "independent radius scalings are
the two `beta`-invariant ratios." Consequence, re-derived: **beta scales the fiber radii ONLY;
the record interval `T_R` is beta-INVARIANT.** Hence the occurrence criterion is not a heuristic
but exact: a derived statement moves under beta iff the fiber radius `R` occurs in it; a statement
whose only length is `T_R` (or a ratio of record intervals, or an action `pi hbar`) is
beta-blind. Lock-shaped ≡ scale set against `T_R` (beta-invariant); fiber-shaped ≡ scale set
against `R` (beta-carrying). This is the axis every attack below is tested on.

## 1. ATTACK ON CLAIM 1 — RECORD-CANNOT-FURNISH-FIBER-SHAPED [subject §2, §3, §5; span :52-72, :88-227, :308-315]

REFUTATION SOUGHT: any surface-native route to a dimensionful, R-carrying threshold that does NOT
pass through an adopted radius or through amplitude/weight structure the record lacks.

Structural closure, from the ANCHOR's primitive inventory (re-read, not taken on testimony):
- ANCHOR I.1-I.4: dynamics indexed by connection/holonomy histories alone; no native metric
  argument in the ratified finite object; holonomy characters unit modulus, `g_ij = exp(i
  theta_ij) in U(1)`; "No fiber metric; a radius is a metric datum, not a surface datum"
  (Register :224). The surface's only dimensionful primitives are the record interval `T_R` and
  its derived companions (`|Delta S_record| = pi hbar`, an action; `m_* T_R = pi`) — ALL
  beta-invariant, none carrying `R` (ANCHOR II.9, CENSUS §1.3).
- The fiber radius `R` is not in the inventory to be consumed. Therefore any surface-derived
  dimensionful quantity is locked against `T_R` (beta-invariant) ⇒ lock-shaped. To be
  fiber-shaped it must be locked against `R`, which requires `R` to occur in a surface-derived
  statement — excluded by I.4. The subject's §2 corollary is thus not an over-reach; it is forced
  by I.4 read together with §0's exact occurrence criterion.
- The amplitude channel is independently closed. A fiber-shaped threshold is a dimensionful
  eigenvalue-scale — DBR760 AL3, re-read: "every eigenvalue scales as an inverse proper length, so
  `D_BR^2 ∝ 1/R^2`" — i.e. a converting MAGNITUDE, not a unit-modulus phase. ANCHOR II.6 (Q-290,
  TYPE-R, exact mechanism): the write structure carries unit-modulus (phase) weights, not
  amplitude/magnitude; "the physical signature needs weight/amplitude structure the records
  provably do not carry." A record-native converting magnitude is refuted with an exact mechanism.

EVERY CANDIDATE ROUTE I COULD CONSTRUCT COLLAPSES TO ONE OF TWO SINKS, both non-surface-native:
1. Route via a D_BR eigenvalue `(n_Q+a_Q)^2/R_Q^2` (the one concrete fiber face). Its `R`-content
   is ADOPTED: DBR789 (:98-121) — the radii are the defining parameters of beta, "NONE IS
   SELECTED"; DBR760 §1.1/§2.3 — three radii, spin structures, bundle class, odd profile, index
   all "unselected," defining principle ABSENT archive-side, `theorem_proof_reachable_now = false
   | TYPE-U`. Its normalizing step IS the rail conversion to `L_BR`'s proper-time consumer (ANCHOR
   III.11, the internal/external conversion booked a rail import). Its threshold map is underived.
   This route passes THROUGH an adopted radius — excluded by the refutation target's own terms.
2. Route via a record eigenvalue given a length by division by `R`. Requires `R` in a surface
   statement — excluded by I.4; and the resulting magnitude is Q-290-refuted.

No third route exists in the primitive inventory. A fiber-shaped S13 remains WRITABLE, but only
ON THE RAIL (adopt skeleton, adopt radii, adopt the transfer), and rail beta-sensitivity types
the rail, not the surface — the `K_KK ~ beta^2` class. **CLAIM 1 CONFIRMED.**

## 2. ATTACK ON CLAIM 2 — EVERY-BUILT-EXEMPLAR-IS-LOCK-SHAPED [subject §4; span :129-179]

Enumeration re-checked at path for a missed BUILT fiber-shaped (R-carrying) exemplar:

- E1 (UCC, sealed): grep of the source returns `tau_R = pi/sqrt(2)` (:209), `tau_first(B_ch) =
  pi/sqrt(2) = tau_R` (:216); ZERO occurrences of any fiber radius, `R_Q`, `1/R`, or the
  conversion beta. R-FREE ⇒ lock-shaped. CONFIRMED at source.
- E2 (CSP): joint spectrum `{-sqrt(2),-sqrt(2),0,0,+sqrt(2),+sqrt(2)}` (:157-160), first opening
  `tau_R = pi/sqrt(2)` (:166); the incidence family is exhausted to UNIT magnitudes (:122-133).
  The token "beta" here is a PHASE ANGLE in the incidence matrix `C_(alpha,beta) = [[...,],
  [exp(i beta),0]]` (:60-62) and the "fiber" tokens are U(1) endpoint rephasings (:65, :82) — a
  NAMING COLLISION, not the internal/external conversion beta and not a fiber metric. No fiber
  radius occurs. R-FREE ⇒ lock-shaped. CONFIRMED at source (collision explicitly cleared).
- E3 (R34): treated in §3 below. R-FREE ⇒ lock-shaped.
- E4 (`m_* T_R = pi`, ANCHOR II.9 / CENSUS §1.3): "contains no `R`, trivially beta-invariant."
  R-FREE ⇒ lock-shaped. CONFIRMED at source.
- GK (charged-spectrum nonreturn test, checked for a missed exemplar): consumes exactly R34's
  Hamiltonian spectrum (:267-268) and keeps the charged sector SCOPED —
  `interacting_gauge_infraparticle_spectrum_derived = false` (:322),
  `same_spectrum_theorem_derived = false` (:340). Not a distinct built exemplar; no R introduced.
- The only fiber-shaped FACE (D_BR) is UNBUILT (DBR760: `SPECTRUM = PARTIAL`,
  `theorem_proof_reachable_now = false | TYPE-U`) and its `R`-content adopted. Not a built exemplar.

Four built exemplars, all R-free; the sole R-carrying object is unbuilt-and-adopted. Zero built
fiber-shaped exemplars. **CLAIM 2 CONFIRMED.**

## 3. ATTACK ON CLAIM 3 + R34 R-FREEDOM — RE-DERIVED, NOT TAKEN [subject §4-E3; span :153-167, :299-306]

R34's sealed forms, re-derived for a hidden `R`:

```text
H(p) = alpha_D . p + mu S tensor c_partial ,   S = -i gamma^0 gamma^5
spec(c_partial) = {-sqrt(2), 0, +sqrt(2)}          (executable-verified in R34)
H_lambda(p)^2 = |p|^2 + mu^2 lambda^2
band edge / threshold:  p_lambda(E) = sqrt(E^2 - mu^2 lambda^2) = 0  =>  E = mu |lambda|
```

The threshold condition consumes exactly two things: the source-mass symbol `mu` and the derived
record PURE NUMBER `lambda in {0, sqrt(2)}`. The fiber radius `R` occurs NOWHERE in `H`, in the
band `E_{+/-sqrt(2)} = sqrt(|p|^2 + 2 mu^2)`, in the density `rho_(lambda,sigma)`, or in the
threshold. The continuum carrier `L2(R^3; C^4 tensor C^3)` supplies the momentum `p` (integrated
over) but no `R`: the threshold is the `p = 0` band edge, so `p` does not enter the threshold
value either. R34_R_FREE holds LITERALLY.

Can the pure-number spectrum `{-sqrt(2),0,+sqrt(2)}` acquire GENUINE `R`-dependence surface-
natively (Attack 3)? No. `c_partial` is an incidence operator built from surface primitives
(unit-modulus holonomy characters; CSP's unit-incidence family), dimensionless by construction —
to carry `R` it would have to be divided by a fiber length, i.e. `R` in a surface statement (§1,
excluded by I.4). To become a DIMENSIONFUL threshold it needs `mu`; and `mu`, if surface-derived,
lands in the interval-lock class (`mu x T_R` = pure number, beta-invariant), because `T_R` is the
only surface length and it is beta-invariant. The only way to make `mu ~ 1/R` (fiber-shaped) is to
furnish `mu` from a D_BR eigenvalue — a RAIL determination through an adopted radius (§1 sink 1),
which the subject carries as the non-identification `mu = m_*` and does not assert. The eigenvalue
DATA is real; its `R`-carrying dimensionful promotion is not surface-furnishable. **CLAIM 3
CONFIRMED; R34 R-FREE CONFIRMED.**

## 4. ATTACK ON CLAIM 4 — FENCE AND IMPORT [subject §7, §8, FINAL LINES]

No value was computed by the subject and none is imported. Every numeric string in the subject
(`pi/sqrt(2)`, `sqrt(2)`, `{-sqrt(2),0,+sqrt(2)}`, `m_* T_R = pi`, `(n_Q+a_Q)^2/R_Q^2`) is a
sealed-text quotation of a named source, consumed for occurrence structure only; verified against
ANCHOR, CENSUS, R34, DBR760 at path. The subject does NOT smuggle a metric by treating R34's
continuum carrier as surface-native: it carries R34's own scope discipline
(`complete_parameter_free_Q_spec_frozen = false`) and consumes only R34's threshold STATEMENT for
its R-occurrence. No register/tracker/plan/road/continuation/ledger read. This checker likewise
computed nothing. **CLAIM 4 (fence + import) CONFIRMED CLEAN — both subject and checker.**

## 5. THE ONE HONEST RESIDUAL, AND WHY IT DOES NOT REOPEN FINISH A

The strongest push a skeptic has: S16 (ANCHOR IV) is the ONLY junction that "could be both DERIVED
and SCALE-SENSITIVE," and IV.13 says it "must transport a cell quantity to a continuum coupling" —
so S16 is DEFINITIONALLY rail-touching, and a transport-carrying S16 might carry beta through the
internal/external conversion. The subject boxes this correctly as a disjunction, and both disjuncts
kill the Finish-A LEVER (which requires the CONJUNCTION derived AND scale-sensitive):
- (a) If S13/S16 is surface-native/derived, its threshold foot is lock-shaped (§1-§3) ⇒
  beta-invariant ⇒ not scale-sensitive.
- (b) If S16 cannot be completed without rail imports, it is not "derived-as-specified"; its
  beta-sensitivity is rail-typed (III.11), not surface-native ⇒ not derived-as-specified.
Neither disjunct yields "derived AND scale-sensitive." One correction to the subject's phrasing,
carried not repaired: disjunct (b) is strictly "Finish-A lever dead / determination possibly
UNRESOLVED," not positively "Finish B"; the subject writes "Finish B side" for (b), which slightly
overstates the positive claim. It does NOT affect the tasked verdict, whose content is exactly
"Finish-A-lever dead," and that holds under both disjuncts.

---

## VERDICT LINES

```text
FIBER_SHAPED_ROUTE_FOUND = NO (lock-shaped holds).  No surface-native, R-carrying threshold is
    record-furnishable: beta is DEFINED as the common fiber-radius rescaling (DBR789 :98), so the
    fiber radius R is the sole beta-carrier; R is not a surface primitive (ANCHOR I.4, Register
    :224); the only surface length T_R is beta-invariant; a dimensionful eigenvalue-scale is a
    converting magnitude, Q-290 TYPE-R refuted (ANCHOR II.6).  Every constructible route sinks to
    an adopted D_BR radius (rail) or to R-in-a-surface-statement (excluded) — neither surface-native.

ALL_BUILT_EXEMPLARS_LOCK_SHAPED = CONFIRMED.  E1 UCC (tau_first = pi/sqrt(2) = tau_R, no R,
    verified at source), E2 CSP (spec {-sqrt(2),0,+sqrt(2)}, unit-incidence, the "beta"/"fiber"
    tokens are a phase/U(1)-rephasing naming collision, no fiber radius), E3 R34 (E = mu|lambda|,
    no R), E4 m_* T_R = pi (no R) — four built, all R-free; GK consumes R34's R-free spectrum and
    is explicitly scoped; the only R-carrying face (D_BR) is UNBUILT and adopted.  Zero built
    fiber-shaped exemplars.

R34_R_FREE = CONFIRMED.  Re-derived independently: p_lambda(E) = sqrt(E^2 - mu^2 lambda^2) = 0 =>
    E = mu|lambda|; consumes only the source-mass symbol mu and the record pure number
    lambda in {0, sqrt(2)}; R occurs nowhere in H, its bands, its density, or its threshold; the
    continuum momentum p enters only away from the p=0 threshold edge.  No hidden R-dependence.

SUBJECT_FENCE_AND_IMPORT_CLEAN = CONFIRMED.  No value computed; every numeric a sealed-text
    quotation; no metric/continuum machinery imported; R34's continuum carrier consumed for
    R-occurrence only under R34's own carried scope; no barred file read.  Checker computed nothing.

VERDICT = LOCK-SHAPED-CONFIRMED (Finish B; Finish-A-lever dead).  The threshold-foot shape is
    lock-shaped for a DERIVED S13; the fiber-shaped alternative is not record-furnishable
    surface-natively and is writable ON THE RAIL ONLY (adopted moduli), which types the rail not
    the surface.  The Finish-A lever (S16 both DERIVED and SCALE-SENSITIVE) is unreachable under
    both disjuncts: derived => lock-shaped/beta-invariant; scale-sensitive => rail-typed/not
    derived-as-specified.  Single carried correction: disjunct (b) is strictly "lever dead /
    possibly undetermined," not positively Finish B; immaterial to the tasked verdict.
```

FENCE_RESPECTED = CERTIFIED (no value, threshold, eigenvalue value, kappa, scale, or alpha
    evaluated, bounded, or estimated; every numeric a sealed-text quotation; no measured-value
    comparison). IMPORTED_NOTHING = CERTIFIED (no metric/continuum theorem used as ground; no
    barred register/tracker/plan/road/continuation/ledger read; all sources verified at path;
    testimony given zero weight, every typing re-derived).

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false ;
JOINT_ANCHOR_DERIVED = false
