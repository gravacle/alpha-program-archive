# STAGE 8 TASK 5 / R4 - COVARIANT COMPLETED LOCAL-SYMBOL MAP BUILD - LANE 1 V001

Date: 2026-08-04
Task: PASTE 485 / Task 5 / R4
Lane: Codex Lane 1
Custody: construction, derivation first; authored alternatives disclosed and none selected

## Lead result

```text
LM1 = BLOCKED_WITH_MAXIMAL_SUBPACKAGE_BUILT |
  BUILT: the completed conserved-current carrier-to-field-source seam and
  the universal unitized trace-class topology containing L_T, Rhat_K, and
  every rank-one profile;
  BLOCKED: no ratified map sends that completed source-field class to a
  spacetime-local kernel/cotangent symbol class

LM2 = BLOCKED |
  the algebraic normalized-reader dual and its exact Ward/remainder
  decomposition are built, but no spectral/long-wavelength realization
  proves that the scalar coordinate is the physical local Maxwell F^2
  coefficient rather than a carrier coefficient

R2_FAMILY = ENUMERATED_ALGEBRAICALLY__PHYSICAL_SUBFAMILY_BLOCKED |
  every bounded normalized real covariant reader on the built profile class
  has coordinates (chi_K,T), modulo the exact physical-relation annihilator;
  the physically local members are exactly those admitting the missing
  localization/spectral certificate

CHI_K = chi_K |
  p_(chi,T)[Rhat_K]=chi_K,
  p_(chi,T)[H_x]
    =f(s)chi_K+2f_1(s)<x,T x>_K,
  s=||x||_K^2

RECIPROCAL_LOOP_COORDINATE_DIMENSION = 1 |
  p[H_(r e)]=[f(r^2)+2r^2 f_1(r^2)]chi_K

S8A_EXCHANGE_CARRIER_COORDINATE_DIMENSION = 2 |
  coordinates (chi_K,tau_cross) on the declared exchange-invariant
  rank-two profile span;
  on the two exchanged axis profiles the tau_cross term vanishes and
  p[H_(r e_j)]=[f(r^2)+r^2 f_1(r^2)]chi_K

NEW_EXACT_RESIDUE =
  COMPLETED_CONSERVED_CURRENT_TO_LOCAL_FIELD_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS
```

The attempted build does not identify a carrier quadratic form with a
spacetime principal symbol.  That substitution was already refuted in
Q-400, and re-running it here produces the same category error.  The
ratified metric and DoR-015 current family do, however, construct a genuine
completed source-field seam.  On that seam the rank-one profile class has a
canonical Banach presentation, and all possible bounded readers can be
classified without selecting one.

The remaining obstruction is narrower than Q-400's original statement.  It
is not an unspecified topology and not an unspecified coefficient.  It is
one map with a failure-capable contract: a covariant realization of the
completed conserved-current Hilbert class as local field kernels (or an
equivalent local cotangent/spectral class), followed by a proved
long-wavelength Ward/contact decomposition.  Deriving that map from the
record tower would consume the still-open stitching/refinement package R12.
Declaring it from the external field would be authored physics.  Neither is
performed here.

```text
ALPHA_COMPUTED = false
PROOF_AUTHORIZED = false
KAPPA_RECORD_COMPUTED = false
NUMERIC_EVALUATION = false
REGISTERED_VERDICT_WRITTEN = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight, authorities, and register sweep

### 0.1 Locked process and register

`alpha_supervision/LOCKED_PROCESS.md` was read in full before the build.  It
verified at SHA-256
`e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2`.

The questions-settled register was at Q-406 and verified at preflight at
SHA-256
`dbbf83140afa9ae47a54e0a719b9dba6108ca4ed376ded5834b61d3e4a933ca3`.

```text
DOES_THE_OBJECT_EXIST = no_as_a_physical_local_symbol_map |
  yes_as_the_Q406_construction_obligation

IS_THE_VERSION_CURRENT = yes | through Q-406

ARE_ITS_INPUTS_PRESENT = yes_for_the_completed_profile_topology_and_dual |
  no_for_the_local_field_kernel_realization_and_spectral_limit

PREFLIGHT = PASS_WITH_TYPED_STOP
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| Q-406 residue ledger | `c35ef84636ec1501bd99858538988711d0c717ac98671e6162abb0a0e5978c74` | R4 and LM-1/LM-2 route |
| Q-400 coefficient/symbol result | `70dde1c0cb776abec4a61cac118d4edf1f4c75a1185a2e984dc341961d4b643c` | exact prior block, profile formulas, reader fiber |
| metric V005 / DoR-019 object | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | current embedding, carrier metrics, Riesz maps, R4 units |
| square V004 / DoR-017 object | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | generated R5 Hessian class, restriction/covariance cube |
| DoR-016/V004 network law | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | CTP reality, ordered batching, no joint contraction |
| field signature V005 / DoR-015 object | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | conserved-current source family and field/background carrier |
| Q-396 representative witness | `f0f8b09b1aa6a16c0ed0dffedbd275aba9e647fb5841fc8ce06ce9e6a8b00857` | actual rank-one completed response profiles |

Every named sidecar verified before the corresponding content was used.

### 0.3 Provenance correction

The prompt associates the rank-one profiles with DoR-016/V004.  Direct
inspection gives a narrower provenance:

```text
DoR-016/V004 supplies:
  (T_+,T_-), R_CTP=T_-^dagger T_+, CTP reality, per-system towers,
  ordered-pair batching, and no joint contraction;

Q-396 supplies:
  H_x=f(s)R_K+2f_1(s)x^flat tensor x^flat.
```

The profile input is present and sealed, so preflight does not fail.  Its
load-bearing source is Q-396, while DoR-016 supplies the CTP/batching
certificates.  `R_CTP`, the independent bilocal probe `R`, the Riesz map
`R_K`, and the rank-one profile `H_x` are not aliases.

### 0.4 Register sweep

The register and corpus were swept for `local symbol`, `Maxwell symbol`,
`p_loc`, `R_K`, `Riesz`, `rank-one`, `carrier to field`, `spectral`,
`long wavelength`, `Ward`, `contact`, `kernel`, `current realization`, and
`stitching`.  Bearing rows:

```text
Q-247  separation is class-relative;
Q-279  finite retarded blocks are exactly zero and p-free;
Q-288  every completion and topology must be named;
Q-309  finite kernel/mixing blocks remain zero with probes;
Q-315  source and physical tangent carriers may not be identified silently;
Q-365  generated-Hessian restriction/covariance cube;
Q-384  carrier metric and unit seam, not local symbol physics;
Q-396  nonzero completed rank-one response family with zero finite shadows;
Q-397  p_loc consumes the completed operator, not a finite shadow;
Q-398  S8-A pushforward reduces to chi_K on the exchange orbit;
Q-400  carrier symbol built, physical local Maxwell symbol blocked;
Q-403  memberwise abstract reader completion built;
Q-404  no magnitude bound on chi_K;
Q-406  R4 classified constructible and ordered before R2/R3 use.
```

No later row supplies a local field kernel, a pseudodifferential/spectral
operator class, a long-wavelength limit, or a Ward/contact theorem for the
completed conserved-current image.

---

## 1. Types and symbol separations

For one admitted realization `G`, write:

```text
K_G        completed physical cycle Hilbert carrier;
g_K,G      ratified real positive carrier metric;
R_K,G      Riesz map K_G -> K_G^*;
I_K,G      completed conserved-current embedding into J_cyc,G;
J_cyc,G    closure of the DoR-015 A4 conserved-current field-source family;
L_T,G      normalized local transverse Maxwell kernel;
O_R5,G     completed physical retarded response class;
j_R4,G     R4-unit-dressed placement of generated K/K Hessians into O_R5,G;
Rhat_K,G   j_R4,G(R_K,G);
P_x        x^flat tensor x^flat, x^flat=R_K,G x;
H_x        f(s)Rhat_K,G+2f_1(s)j_R4,G(P_x), s=||x||_K^2;
p_loc,G    completed normalized local reader, if physically instantiated.
```

The following distinctions are binding:

```text
K_scalar != K_G;
R_CTP != bilocal source R != R_K;
carrier quadratic symbol != spacetime cotangent principal symbol;
field-source current != local field kernel;
contact support != Maxwell F^2 coefficient;
finite restriction zero != completed reader kernel.
```

---

## 2. A1 / LM-1 - completed topology and carrier-to-field seam

### 2.1 Derived completed current seam

At every finite stage, DoR-015 and metric V005 define

```text
I_K,G(c)=u_c,
u_c(a)=sum_e c_e integral_(gamma_e)a,
c in ker(B_G^T).                                  (LM1-1)
```

Metric V005 proves fullness:

```text
ker(I_K,G)={0},
||c||_K=||I_K,G c||_A4.                          (LM1-2)
```

The W3 rank-preserving inclusions are isometric.  Completion therefore gives
an isometry

```text
I_K,G:K_G -> J_cyc,G:=closure(I_K,G K_fin).       (LM1-3)
```

By definition of `J_cyc,G`, `(LM1-3)` is onto that completed cyclic current
subspace.  It selects no cycle basis or current representative.

For `H:K_G->K_G^*`, define its field-source bilinear pushforward by

```text
Push_G(H)(I_K,G x,I_K,G y):=H(x,y).              (LM1-4)
```

Because `I_K,G` is an isometric isomorphism onto `J_cyc,G`, `(LM1-4)` is
well-defined, faithful, and norm preserving on every Schatten class on which
it is used.  The R4 action-unit torsor dresses `(LM1-4)` into the generated
retarded K/K block; no representative of the unit torsor is selected.

```text
CARRIER_TO_COMPLETED_FIELD_SOURCE_SEAM = BUILT / TYPE-P
CARRIER_TO_LOCAL_SPACETIME_KERNEL_SEAM = NOT_BUILT / TYPE-U
                                                        (LM1-5)
```

The second line is not implied by the first.  A continuous bilinear form on
`J_cyc,G` need not have a local kernel, wavefront class, principal symbol, or
long-wavelength limit.

### 2.2 Minimal profile Banach class

The Q-396 profiles contain one Riesz/identity direction and trace-class
rank-one perturbations.  The minimal stable presentation is the real
unitized trace-class space

```text
A_prof,G
 := R R_K,G direct-sum_1 S_1,sa(K_G),

||(b,C)||_prof:=|b|+||C||_1.                    (LM1-6)
```

`P_x` is rank one and trace class, with

```text
||P_x||_1=||x||_K^2=s.                           (LM1-7)
```

Thus every profile

```text
h_x:=f(s)R_K,G+2f_1(s)P_x                       (LM1-8)
```

belongs to `A_prof,G`.  No value of `s`, `p`, `nu`, or any rank is chosen.

The local-reader presentation must also contain the independently normalized
Maxwell line.  Define

```text
E_G:=R L_T,G direct-sum_1 A_prof,G,

Q_G(a,b,C)
 :=a L_T,G+j_R4,G(b R_K,G+C) in O_R5,G.          (LM1-9)
```

Let

```text
Rel_G:=closure(ker Q_G) in E_G,
O_prof,G:=E_G/Rel_G,
||[z]||_loc:=inf_(r in Rel_G)||z+r||_E.          (LM1-10)
```

`O_prof,G` is Banach because it is a quotient of a Banach space by a closed
subspace.  `Rel_G` retains every actual relation between `L_T`, `Rhat_K`, and
the trace-class directions.  In particular, the construction does not
assume `Rhat_K=L_T`, does not assume they are independent, and remains valid
when a finite carrier makes `R_K` itself trace class.

This quotient topology is target independent.  The `l1` choice is forced by
the existing trace-class source topology and by the requirement that both
the scalar Riesz line and rank-one perturbations be continuously visible.
Using a graph norm of `p_loc` is unnecessary and would be circular.

```text
COMPLETED_PROFILE_LOCAL_TOPOLOGY = BUILT / TYPE-P
TOPOLOGY = quotient_of(R direct-sum_1 R direct-sum_1 S_1,sa)
WEAK_STAR_USED = false
BIDUAL_USED = false
DISTRIBUTIONAL_COMPLETION_USED = false
                                                        (LM1-11)
```

### 2.3 Covariance and reality

For an admitted isometry `U:K_G->K_G'`, define

```text
alpha_U(a,b,C):=(a,b,U C U^*).                  (LM1-12)
```

Trace norm and the two scalar norms are invariant.  The R5 covariance cube
gives

```text
Q_G' alpha_U=Alpha_U Q_G,                        (LM1-13)
```

so `alpha_U(Rel_G)=Rel_G'` and `(LM1-12)` descends isometrically to the
quotients.  Reality/orientation reversal uses the admitted antiunitary
extension and sends `C` to `Theta C Theta^(-1)`; the self-adjoint real slice
is preserved.  No orientation or frame is selected.

### 2.4 Restriction and zero-extension

For a W3 rank-preserving isometric inclusion `j_NM:K_N->K_M`, the lawful
contravariant profile restriction is

```text
rho_MN(a,b,C):=(a,b,j_NM^* C j_NM).             (LM1-14)
```

Trace-norm compression is contractive, and

```text
j_NM^* R_K,M j_NM=R_K,N,
j_NM^* P_(j_NM x) j_NM=P_x.                     (LM1-15)
```

The generated-Hessian square therefore sends relations to relations and
induces a contractive map on `O_prof`.  Identity zero-extension is the same
square.  For cycle-creating extensions, no upward quotient map is claimed;
the new carrier receives its own profile algebra and restricts
contravariantly.  This is exactly the standing Z7 boundary.

### 2.5 Batching

DoR-016 keeps a network as an ordered pair and forbids an unlicensed joint
contraction.  Accordingly

```text
O_prof,(G1,G2)=O_prof,G1 x O_prof,G2             (LM1-16)
```

with product topology and componentwise readers.  No scalar sum, product,
trace, or common normalization is added.  On independently certified
isometric direct sums, trace-class components use the usual block direct
sum; this does not alter ordered-pair semantics.

### 2.6 LM-1 verdict

The topology and completed source-field seam are built.  The demanded
physical word `local` requires one further map:

```text
Loc_G:J_cyc,G x J_cyc,G
      -> Kernel_loc(M_G,P_G;CTP,units,boundary), (LM1-17)
```

or an equivalent map from `O_prof,G` to a local cotangent/spectral class.
It must be injective on record-visible profile content modulo a disclosed
kernel and natural under `(LM1-12)`--`(LM1-16)`.  No authority defines it.

```text
LM1_PROFILE_TOPOLOGY = BUILT
LM1_COMPLETED_CURRENT_SEAM = BUILT
LM1_LOCAL_SPACETIME_REALIZATION = BLOCKED / TYPE-U
LM1 = BLOCKED_WITH_MAXIMAL_SUBPACKAGE_BUILT
```

---

## 3. A2 / LM-2 - dual classification, Ward/contact split, and stop

### 3.1 Exact continuous dual

Before quotienting by `Rel_G`, the real continuous dual of `E_G` is

```text
E_G^*=R direct-sum_infinity R direct-sum_infinity B_sa(K_G).
                                                        (LM2-1)
```

The trace pairing is

```text
ell_(lambda,chi,T)(a,b,C)
 =lambda a+chi b+Tr(TC).                         (LM2-2)
```

Normalization on `L_T` fixes `lambda=1`.  A tuple defines a reader on the
physical quotient iff it annihilates every exact relation:

```text
a+chi b+Tr(TC)=0
for every (a,b,C) in Rel_G.                      (LM2-3)
```

Consequently the complete algebraic reader fiber is

```text
R2_alg(G):={
  (chi,T) in R x B_sa(K_G):
  (LM2-3),
  U T U^*=T on each admitted stabilizer,
  family covariance/reality/restriction/batching equations
}.                                               (LM2-4)
```

For transports between realizations,

```text
chi_G'=chi_G,
T_G'=U T_G U^*.                                  (LM2-5)
```

For a family rather than a fixed realization, `(LM2-4)` means a natural
section of these affine fibers.  No section is selected.

At a finite `d`-dimensional carrier, put

```text
m_G:=dim Sym^2(K_G)^(Aut_G),
r_G:=rank of the independent relation constraints in (LM2-3).
```

Then

```text
dim R2_alg(G)=1+m_G-r_G,                         (LM2-6)
```

provided the normalized affine fiber is nonempty.  On a completed
infinite-dimensional carrier, `(LM2-4)` is the corresponding real Banach
affine space and may be infinite dimensional.  This is an exact dimension
formula, not an assumed finite count.

### 3.2 p_loc on every rank-one profile

For `x in K_G`,

```text
Tr(TP_x)=<x,T x>_K.                              (LM2-7)
```

Therefore every algebraic family member acts by

```text
p_(chi,T)[Rhat_K]=chi,
p_(chi,T)[j_R4(P_x)]=<x,T x>_K,

p_(chi,T)[H_x]
 =f(s)chi+2f_1(s)<x,T x>_K,
s=||x||_K^2.                                     (LM2-8)
```

Equation `(LM2-8)` is the maximal completed rank-one computation.  It is
linear, real, unit-correct, and covariant.  It assigns no value to `chi` or
`T`.

### 3.3 Reciprocal-loop fiber

On a one-dimensional carrier, write `x=r e`.  The exact operator relation is

```text
P_x=r^2 R_K.                                     (LM2-9)
```

Thus `Rel_G` forces the rank-one coordinate to be `r^2 chi`, and

```text
p[H_(r e)]
 =[f(r^2)+2r^2 f_1(r^2)]chi.                    (LM2-10)
```

The normalized reader restriction to the reciprocal profile span has one
free real coordinate:

```text
dim R2_alg(loop)|profile=1 | coordinate chi.     (LM2-11)
```

### 3.4 S8-A exchange fiber

Use an orthonormal pair only to compute the invariant description.  Let the
declared exchange send `e_1<->e_2`.  Every real self-adjoint reader tensor
invariant under that exchange has matrix

```text
T=[[chi/2,tau_cross],
   [tau_cross,chi/2]].                            (LM2-12)
```

The diagonal coefficient is `chi/2` because

```text
R_K=P_(e_1)+P_(e_2)                              (LM2-13)
```

on the rank-two carrier.  Therefore the declared exchange-invariant profile
fiber has coordinates

```text
(chi,tau_cross),
dim=2.                                           (LM2-14)
```

Additional admitted stabilizers may impose an additional linear relation;
`(LM2-14)` is exact for the sealed exchange carrier and is never promoted to
all realization groups.

For `x=x_1 e_1+x_2 e_2`,

```text
p[H_x]
 =[f(s)+s f_1(s)]chi
  +4f_1(s)tau_cross x_1 x_2,
s=x_1^2+x_2^2.                                   (LM2-15)
```

On either exchange-axis member `x=r e_j`, the cross term vanishes:

```text
p[H_(r e_j)]
 =[f(r^2)+r^2 f_1(r^2)]chi.                     (LM2-16)
```

This reproduces the Q-398 exchange-orbit result and exposes, rather than
deletes, the perpendicular reader coordinate at general S8-A points.

### 3.5 Algebraic Ward/remainder decomposition

Every tuple `(chi,T)` gives a continuous normalized scalar map on
`O_prof,G`.  Algebraically one may write

```text
H=p_(chi,T)(H)L_T+H_rem,
H_rem:=H-p_(chi,T)(H)L_T,
p_(chi,T)(H_rem)=0.                              (LM2-17)
```

The exact kernels are:

```text
ker p_(chi,T)
 ={[a,b,C]:a+chi b+Tr(TC)=0},                   (LM2-18)

ker p_(chi,T) on rank-one profiles
 ={x:f(s)chi+2f_1(s)<x,T x>=0}.                 (LM2-19)
```

These are genuine kernel certificates for the algebraic reader.  They do
not prove that `H_rem` is a physical contact/nonlocal/higher term.  Naming
it `contact` before a field-local support/spectral theorem would be the
same error as naming `(S2-4)` in Q-400 a spacetime symbol.

### 3.6 Physical spectral/long-wavelength contract

To upgrade `(LM2-17)` to the required physical Ward/contact split, a map
member must supply:

```text
KERNEL_REALIZATION:
  Loc_G from (LM1-17), on a named local kernel or pseudodifferential class;

FIELD_SYMBOL:
  sigma_field(Loc_G H)(x,q) with a named topology and reality law;

LONG_WAVELENGTH_LIMIT:
  LW_2(H)(x,q)
   :=lim_(epsilon->0+) epsilon^(-2)
       sigma_field(Loc_G H)(x,epsilon q),
  with existence and topology stated;

WARD:
  q^mu LW_2(H)_(mu nu)=0 modulo the declared boundary/contact term;

MAXWELL_SPLIT:
  LW_2(H)=p_loc(H) sigma_Maxwell+sigma_perp(H),
  with a proved direct sum and p_loc(L_T)=1;

CONTACT_BOUNDARY:
  the diagonal/contact and boundary forms separated without deleting a
  record-visible cycle profile;

NATURALITY:
  covariance, reality, W3 restrictions, zero-extension, batching,
  quotient, units, and DoR-008 squares;

KERNELS:
  ker Loc_G, ker LW_2, ker p_loc, and their intersections with the Q-396
  profile span explicitly proved.                              (LM2-20)
```

No named authority supplies `KERNEL_REALIZATION` or `FIELD_SYMBOL`.
Therefore the limit and the physical Ward/contact split cannot be executed.

Define the physically admissible subfamily without pretending to know it:

```text
R2_phys(G)
 :={(chi,T) in R2_alg(G):there exists a certificate (LM2-20)
                         whose coefficient map is p_(chi,T)}.   (LM2-21)
```

`R2_phys` is a failure-capable, physically typed family.  Its coordinate
ambient space is explicit, but current stock proves neither its membership
test nor its dimension.

```text
ALGEBRAIC_SYMBOL_FAMILY = BUILT
ALGEBRAIC_WARD_REMAINDER = BUILT
PHYSICAL_LONG_WAVELENGTH_SYMBOL = BLOCKED / TYPE-U
PHYSICAL_WARD_CONTACT_SPLIT = BLOCKED / TYPE-U
LM2 = BLOCKED
```

---

## 4. A3 - deliverable and reading-rule family

### 4.1 Exact family statement

The build physically narrows the old untyped reader interface to the chain

```text
R2_phys subset R2_alg
          subset affine natural dual of O_prof.                 (A3-1)
```

The outer family is now explicitly enumerable by `(chi,T)` and the relation,
covariance, reality, restriction, batching, and unit equations.  The inner
physical family waits only on `(LM2-20)`.

```text
R2_ALGEBRAIC_FAMILY = ENUMERATED
R2_ALGEBRAIC_COORDINATES = (chi_K,T) modulo Rel and naturality
R2_PHYSICAL_FAMILY = BLOCKED_ON_LM2-20
R3_IS_INDEPENDENT_OBJECT = false
R3_COORDINATE = chi_K
                                                        (A3-2)
```

### 4.2 Chi and profile table

| Carrier/profile | Exact scalar image |
|---|---|
| `L_T` | `1` |
| `Rhat_K` | `chi_K` |
| `j_R4(P_x)` | `<x,T x>_K` |
| `H_x` | `f(s)chi_K+2f_1(s)<x,T x>_K` |
| reciprocal `H_(r e)` | `[f(r^2)+2r^2f_1(r^2)]chi_K` |
| S8-A axis `H_(r e_j)` | `[f(r^2)+r^2f_1(r^2)]chi_K` |
| S8-A general `H_x` | `[f(s)+s f_1(s)]chi_K+4f_1(s)tau_cross x_1x_2` |

All entries are symbolic.  The table computes what any eventual physical
member does once its coordinates are known; it does not select them.

### 4.3 Does R4 collapse R2?

No.  The built carrier/source topology alone leaves the affine dual
`(LM2-4)`.  Distinct tuples obey all currently executable algebraic
certificates and can differ on the Q-396 completed directions while sharing
every sealed finite retarded shadow.  A future `(LM2-20)` may reduce or even
collapse that family, but this has not been proved.

```text
R4_COLLAPSES_R2 = false_as_a_current_theorem
CHI_K_FORCED = false
CHI_K_ZERO_FORCED = false
CHI_K_ONE_FORCED = false
```

---

## 5. Authored choice table at the physical stop

No row is selected, adopted, or recommended.

| Candidate | Added structure | Carrier-structural justification | Alternative | Void condition |
|---|---|---|---|---|
| path-current kernel realization | realize `u_c` as local field kernels along adopted paths, then complete in a named kernel topology | starts from the actual A4 current embedding | retain only abstract currents; refinement-density realization | distributional completion unflagged, failed covariance/restriction, record-visible cycle killed |
| pseudodifferential Ward class | properly supported local operator class with principal/long-wave symbol and contact ideal | directly implements `(LM2-20)` | microlocal distribution class; spectral family on Cauchy data | imported contour/measure, no proof rank-one profiles belong, failed reality/Ward |
| refinement-derived localization | obtain local kernels and the long-wave limit from record refinement/common-refinement data | keeps the infinite emergent from finite records | authored external localization family | consumes unbuilt R12 or depends on a filtration member |
| external-field localization family | retain every covariant localization compatible with A1/A2 and no-selection | uses the adopted field rather than inventing a second background | derive from R12; reject | family empty, output-sensitive member rule, hidden frame/Cauchy-slice choice |
| carrier Calkin/trace symbol | treat finite-rank directions as a contact ideal and the Riesz line as the bulk symbol | minimal abstract operator quotient | full `(chi,T)` family | finite-dimensional carriers make identity compact; S8-A relation fails unless chi collapses; not a spacetime symbol |
| normalized finite trace | `Tr(H)/dim K_N` | basis-free at one stage | singular completed reader; full affine family | zero-extension/refinement changes normalization; stage dependence |

The last two candidates were run as hostile controls and fail as physical
solutions.  The first four are genuine future construction routes with
explicit prices.  Selecting one would add physics or unfinished mathematics
and therefore requires a new reviewed artifact.

### 5.1 Dependency correction

The Q-406 order remains lawful only with this refinement:

```text
R4a completed profile topology/current seam        BUILT here;
R4b local kernel/spectral realization              OPEN;
R4b derived from records                           consumes R12;
R4b authored from external field                   requires its own gate;
R2/R3 physical enumeration                         follows after R4b.
                                                        (A3-3)
```

This is not a new free scalar.  It is the missing map which decides which
points of the already-recorded R2 affine fiber are physically local.

---

## 6. A4 - falsifiers, regressions, and anti-tuning

### 6.1 Falsifier table

| Check | Execution | Verdict |
|---|---|---|
| finite Q-243/Q-279/Q-309 retarded zeros | no finite `p_loc,N` is invented; completed profiles restrict to the sealed zero | PASS |
| one-edge/tree | `K_G=0`; `A_prof` has no nonzero cycle profile | PASS |
| reciprocal loop | relation `P_x=r^2R_K` gives `(LM2-10)` | PASS |
| S8-A exchange | invariant tensor `(LM2-12)` gives axis result `(LM2-16)` and retains `tau_cross` off axis | PASS |
| pendant/tree quotient | `I_K` starts after Gate-4 quotient; no pendant content reappears | PASS |
| Q-396 fiber | nonzero completed profiles remain in `O_prof`; no finite silence promoted to kernel | PASS |
| covariance | conjugation by admitted isometries preserves trace norm, relations, and reader coordinates | PASS |
| reality | self-adjoint real slice and antiunitary transport preserved | PASS |
| R4 units | `j_R4` is the only action-unit dressing; no bare C/K conversion | PASS |
| W3 restriction | compression `(LM1-14)` is contractive and reproduces `(LM1-15)` | PASS |
| cycle creation | no upward naturality; new cycles get new-stage profiles | PASS |
| batching | ordered product, componentwise reader, no joint contraction | PASS |
| normalized trace hostile control | dimension normalization changes under refinement | KILLED |
| Calkin hostile control | collapses at finite-rank carriers and is not a spacetime symbol | KILLED |
| metric-principal-symbol substitution | has no local cotangent variables or Ward theorem | KILLED |

### 6.2 Failure-capable future tests

Any future LM-2 artifact fails if:

```text
F1 Loc_G does not map the Q-396 profile class into its declared local kernel
   or pseudodifferential domain;
F2 the long-wavelength limit changes under an admitted realization,
   orientation, frame, or restriction;
F3 the Ward remainder is not transverse or a boundary/contact term is hidden;
F4 an element with nonzero record-visible cycle content is deleted without a
   proved physical kernel theorem;
F5 L_T is not normalized to one in the same symbol topology;
F6 a cycle-creating extension is treated as a canonical upward map;
F7 batching performs an unratified joint scalar contraction;
F8 a weak-star, bidual, distributional, contour, measure, or boundary step is
   invoked without its named door;
F9 chi_K or T is chosen after inspecting the modulus or fixed-point result;
F10 the finite zero is used to set a completed coordinate to zero.
```

### 6.3 Fresh attack - finite-stage trace naturality

Suppose one tries to force a unique reader at each finite `d`-dimensional
carrier by full orthogonal invariance.  It must have the form

```text
ell_d(C)=c_d Tr(C).
```

Normalizing the identity gives `c_d=chi/d`.  Under a zero-extension from
dimension `d` to `m>d`, a fixed old finite-rank `C` would satisfy

```text
ell_m(j C j^*)=(chi/m)Tr(C)
!= (chi/d)Tr(C)=ell_d(C)                         (A4-1)
```

unless `chi=0` or `d=m`.  Thus normalized finite trace is not a natural
family-wide local reader.  A singular completed reader can evade `(A4-1)`
by annihilating finite-rank directions, but choosing that reader is exactly
an R2 member choice and still does not produce a spacetime Maxwell symbol.

### 6.4 Anti-tuning ledger

```text
1. The A4 current embedding and trace-class topology were fixed before any
   reader coordinate was inspected.
2. Rel_G retains all physical relations; no independence or equality of
   L_T and Rhat_K was assumed.
3. The complete affine dual was derived before reciprocal/S8-A formulas.
4. Neither annihilating nor detecting readers were selected.
5. No topology was chosen to force q<1 or a fixed-point conclusion.
6. p, nu, ranks, chi_K, T, tau_cross, and all relation ranks remain symbolic.
7. No alpha, K_*, root, response value, or measured constant was evaluated.
8. The DoR-016 provenance correction was recorded before using the profiles.
```

---

## 7. Final board

```text
LM1_COMPLETED_CURRENT_SEAM = BUILT
LM1_PROFILE_BANACH_TOPOLOGY = BUILT
LM1_LOCAL_SPACETIME_REALIZATION = BLOCKED
LM1 = BLOCKED_WITH_MAXIMAL_SUBPACKAGE_BUILT

LM2_ALGEBRAIC_DUAL = BUILT
LM2_ALGEBRAIC_KERNELS = BUILT
LM2_LONG_WAVELENGTH_FIELD_SYMBOL = BLOCKED
LM2_PHYSICAL_WARD_CONTACT_SPLIT = BLOCKED
LM2 = BLOCKED

R2_ALGEBRAIC_FAMILY = ENUMERATED
R2_GENERAL_COORDINATES = (chi_K,T) modulo Rel_G and naturality
R2_GENERAL_DIMENSION = 1+m_G-r_G at finite stage; Banach-affine in completion
R2_RECIPROCAL_PROFILE_DIMENSION = 1
R2_S8A_EXCHANGE_PROFILE_DIMENSION = 2 before extra stabilizer relations
R2_PHYSICAL_FAMILY = BLOCKED_ON_LM2-20

CHI_K = p_loc[Rhat_K] = chi_K
P_LOC_RANK_ONE
 =f(||x||_K^2)chi_K+2f_1(||x||_K^2)<x,T x>_K

R4_STATUS = PARTIALLY_CONSTRUCTED_WITH_EXACT_NEW_SUBOBJECT
NEW_SUBOBJECT =
  COMPLETED_CONSERVED_CURRENT_TO_LOCAL_FIELD_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS

MEMBER_SELECTED = none
RANK_OR_RATIO_SELECTED = none
ORIENTATION_OR_FRAME_SELECTED = none
FILTRATION_OR_LOCALIZATION_SELECTED = none
NUMERIC_EVALUATION = false
REGISTER_ACTION_TAKEN = false
PLAN_OR_TRACKER_ACTION_TAKEN = false
GIT_COMMIT_OR_PUSH_ACTION_TAKEN = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The next lawful action is a review of this split.  If the local-kernel seam
is required to descend from records, R12 must move before completion of R4.
If the external-field family is allowed to supply it, that family must be
authored and gated without selecting a localization member.  Until one route
closes, `(chi_K,T)` is an exact algebraic coordinate system, not a physical
Maxwell coefficient assignment.

