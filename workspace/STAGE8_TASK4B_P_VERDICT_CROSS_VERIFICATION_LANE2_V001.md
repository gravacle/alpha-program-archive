# STAGE 8 TASK 4B — p-VERDICT COMPUTATION CROSS-VERIFICATION — LANE 2 V001

Date: 2026-08-03  
Task: PASTE 470 / Task 4b  
Lane: CODEX LANE 2  
Custody: adversarial cross-verifier; no registration authority

```text
REGISTER_HEAD_AT_PREFLIGHT = Q-391
PREFLIGHT = PASS

LEAD_RESULT = COMPUTATION_CONFIRMED

COMPUTATION = CONFIRMED
P38_DECISION = NEEDS |
  EVALUABLE_COMPLETED_BASE_STATIONARY_P_JET_AND_RETEXTRACT_KERNEL_INSTANCE

P38_CRITERION = EXACT_AND_MEMBER_FREE
P38_DOMAIN = RATIFIED_COMPLETED_CARRIER
P38_EXECUTABLE_FROM_CURRENT_COMPONENT_DATA = false | TYPE-U

FINITE_STAGE_DECISION = insufficient |
  finite active shadows are exactly zero but are not completed stationary points

REGISTERED_P_VERDICT_WRITTEN = false
P_VERDICT_DECLARED_BY_THIS_ARTIFACT = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The Lane 1 computation is algebraically correct.  Shape K adds only the
cycle/cycle Hessian term, so the completed response splits exactly into a
base term and an affine, p-free-coefficient `nu` term.  All possible p
dependence is localized to the total p derivative of the base stationary
Schur block in `(P3-8)`.

The forward decidability sweep does **not** promote that exact criterion to
an executable decision.  DoR-019 supplies the completed carriers, metrics,
Riesz maps, and unit routing.  DoR-017 R5 supplies the covariant
inverse/Schur/retarded architecture for a supplied base action and supplied
Hessian blocks.  Neither decision instantiates `Gamma_base` off the finite
active section or the image of its stationary p-jet under the completed
retarded extraction.  The finite zero theorem cannot fill this gap because
the finite active reference is not the completed stationary locus.

---

## 0. Preflight, process, and authority ledger

### 0.1 Preflight

The locked process was read before writing.  The live questions-settled
register and its sidecar verified at head `Q-391`.  The target artifact was
hash-verified before it was read.

```text
DOES_THE_OBJECT_EXIST = yes |
  the Lane 1 symbolic dependence computation exists

IS_THE_VERSION_CURRENT = yes |
  Q-391 is the register head and DoR-016/017/018/019 are in force

ARE_ITS_INPUTS_PRESENT = yes |
  enough for cross-verifying the symbolic computation and typing the
  forward decision boundary

PREFLIGHT = PASS
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| p-verdict computation under review | `27790d53b018a84f5f02e97f68e885de0ebb332735307ae0fa433322a6053189` | P1 and P3-5 through P3-14 |
| DoR-016 decision | `b4157df6f327e261f40389d5a3011a0aef66ee0f198d8ebba8b1b9303142d708` | ordered doubled network law |
| DoR-017 decision | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | N member and R1–R5 square |
| DoR-018 decision | `57532849d0766741a365d001596275ef4f2ca612063ef6d05031292711a0614d` | Shape K and its stationary jets |
| DoR-019 decision | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | completed C/K carriers, metrics, Riesz maps, units |
| germ V003 | `231bbd1d77c39a45249149b47f9bb543e7a748c67b477dfde1f245efecd4aa13` | Shape K formula, DP certificates, free-input ledger |
| carrier metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | ratified norm and no-implicit-unit structure |
| square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | stationary Hessian cube and Schur interfaces |
| stationary response computation | `be570c182ef875b557395b62c382ee875420ac0462e2efb5774e9600f794b27a` | confirmed structural response expression |
| stationary response cross-verification | `cac131d949a917576e30332b2f4ec76ca7db57d6d4683e91cfeac658994c499b` | prior component-evaluation boundary |

### 0.3 Register sweep for K3 and K7

The following settled entries were checked, with particular attention to
whether any entry instantiates the completed base stationary p-jet:

```text
Q-243   finite ordered-retarded block is zero and p-free;
Q-279   exact finite probe-on base patterns;
Q-309   finite source-kernel mixing zeros;
Q-313   source-to-cycle factorization and finite zero bridge;
Q-315   physical restriction squares;
Q-365   R5 Hessian restriction cube and retarded covariance;
Q-367   DoR-017 ratification of the square and N member;
Q-368   exact stationary response architecture; components remained TYPE-U;
Q-369   independent confirmation of the stationary-jet boundary;
Q-372-Q-384  carrier-metric gate and DoR-019 completed-carrier adoption;
Q-385-Q-390  Shape K germ review, repair, and DoR-018 ratification;
Q-391   Lane 1 conditional p-dependence computation.
```

The sweep found no ratified executable formula for `Gamma_base` off the
finite active section, no solved completed base stationary family, and no
certificate deciding whether the base Schur p-derivative image lies in the
kernel of `RetExtract`.

### 0.4 Load-bearing symbol distinctions

```text
p_i          = symbolic source-sector weight, not a selected ratio value;
nu           = independent symbolic Shape K scale;
k            = physical cycle argument, not K_* and not a root;
K_cycle      = carrier sector, not kappa_record;
RetExtract   = structural retarded map, not a numerical response value;
finite zero  = restriction target, not a completed stationary evaluation;
criterion    = an exact iff statement, not an executed decision;
R5 field     = an admitted/extensional package field, not automatically an
               executable component formula.
```

---

## 1. Verdict table K1–K7

| Item | Verdict | Cross-verification finding |
|---|---|---|
| K1 substitution | **PASS** | Shape K has zero C and mixed jets and the stated `D_K^2 phi_K`; the completed block substitution is correct |
| K2 exact split | **PASS** | stationarity, complement inversion, Schur reduction, and linear retarded extraction introduce no hidden `nu` cross-term |
| K3 classification | **PASS** | `(P3-8)` is the exact total-derivative criterion; `CONDITIONALLY_DEPENDENT` is the strongest presently ratified statement |
| K4 rank-port audit | **PASS** | the ordered ranks enter this chain only through symbolic `p`; no later operation has an independent rank port |
| K5 `nu` statement | **PASS** | dependence is affine with p-free coefficient `RetExtract[T_K]`; whether that coefficient vanishes remains separately typed |
| K6 falsifier suite | **PASS** | finite shadows, probes, zero theorem, covariance, reality, batching, restriction, and standing regressions survive |
| K7 decidability sweep | **NEEDS / TYPE-U** | the carrier exists, but the executable completed base stationary p-jet and the relevant retarded-kernel/image certificate do not |

No K1–K6 defect was found.  K7 is not a defect in the Lane 1 computation:
that artifact correctly stopped at a conditional classification.  It is the
typed boundary for the next deciding computation.

---

## 2. K1 — Shape K substitution recomputed

On the ratified DoR-019 cycle carrier set

```text
s(k)       := ||k||_K^2,
k^flat     := R_K k,
phi_K(k)   := nu f(s(k)).                         (K1-1)
```

For the adopted flat radial profile, with `f_1` and `f_2` denoting the first
two scalar derivatives on the nonzero branch and their smooth flat
extensions at zero,

```text
D_C phi_K      = 0,
D_C^2 phi_K    = 0,
D_C D_K phi_K  = 0,
D_K D_C phi_K  = 0,

D_K^2 phi_K
  = nu T_K(k),

T_K(k)
  := 2 f_1(s) R_K
     +4 f_2(s) k^flat tensor k^flat.             (K1-2)
```

The derivative follows directly from

```text
D_K s[h]       = 2 <k,h>_K,
D_K^2 s[h,l]   = 2 <h,l>_K,

D_K^2(f o s)[h,l]
  = 2 f_1(s)<h,l>_K
    +4 f_2(s)<k,h>_K<k,l>_K.                    (K1-3)
```

This verifies the coefficient and the Riesz placement.  The corrected unit
routing of DoR-019 is respected: every covector occurrence enters through
`R_K`, and no implicit C-to-K unit conversion appears.

Write the base stationary blocks, evaluated at the base complement critical
family `c_star(p,k)`, as

```text
A := H_CC^base,
B := H_CK^base,
C := H_KC^base,
D := H_KK^base.                                  (K1-4)
```

Shape K therefore gives exactly

```text
H_CC^K = A,
H_CK^K = B,
H_KC^K = C,
H_KK^K = D + nu T_K.                             (K1-5)
```

There is no missing block and no response-facing assumption in this
substitution.

```text
K1 = PASS
```

---

## 3. K2 — exact affine split, including the cross-term attack

### 3.1 Stationary-family independence

The complement stationarity equation is

```text
D_C Gamma_base(p;c,k)+D_C phi_K(k)=0.
```

By `(K1-2)`, this reduces identically to

```text
D_C Gamma_base(p;c_star(p,k),k)=0.               (K2-1)
```

Thus Shape K does not shift `c_star`.  This closes the first possible hidden
cross-term port.

### 3.2 Inverse and Schur algebra

On the ratified R5 reducing complement,

```text
Inv_CC^K = A^(-1),                               (K2-2)

Schur_K
  = (D+nu T_K)-C A^(-1) B
  = Schur_base + nu T_K,

Schur_base := D-C A^(-1)B.                      (K2-3)
```

No Neumann-series or inverse expansion in `nu` occurs: the only inverted
block is `A`, and Shape K contributes zero to it.  Likewise, both mixed
blocks are base-only.  This closes all algebraic places where terms such as
`nu^2`, `nu partial_p A`, or a `nu`-dependent stationary pullback could have
appeared.

R5's retarded extraction is additive and homogeneous on the admitted Schur
class.  Therefore

```text
Response_K(p,nu;k)
  = RetExtract[Schur_base(p;k)]
    +nu RetExtract[T_K(k)]

  = Response_base(p;k)+nu Response_flat(k),      (K2-4)

Response_flat(k):=RetExtract[T_K(k)].            (K2-5)
```

The equality is exact, not perturbative.

### 3.3 Fresh cross-term attack

The strongest remaining cross-term candidate would be p- or `nu`-dependence
of the retarded map itself.  The R5 map is part of the member-independent
stationary architecture and transports covariantly with the Hessian cube;
DoR-018 changes only the N-member germ.  No decision supplies a
member-indexed deformation of `RetExtract`.  Introducing one would be a new
unratified field and would violate the R5 member ledger.  The attack fails.

```text
K2 = PASS
HIDDEN_NU_CROSS_TERM = none
```

---

## 4. K3 — p derivative, `(P3-8)`, and maximal classification

All derivatives below are total p derivatives at fixed physical cycle
argument `k`.  For any base block `X(p;c_star(p,k),k)`, define

```text
dot X
  := partial_(p_i) X
     +(D_C X)[partial_(p_i)c_star].               (K3-1)
```

Differentiating `(K2-1)` on the reducing complement gives

```text
partial_(p_i)c_star
  = -A^(-1) partial_(p_i)[D_C Gamma_base].        (K3-2)
```

The inverse derivative is

```text
dot(A^(-1))=-A^(-1)(dot A)A^(-1).                (K3-3)
```

Hence direct differentiation of `(K2-3)` yields

```text
dot Schur_base
  = dot D
    -dot C A^(-1)B
    +C A^(-1)(dot A)A^(-1)B
    -C A^(-1)(dot B).                            (K3-4)
```

At fixed `k`, with `nu` an independent symbolic input and the DoR-019 metric
p-blind,

```text
partial_(p_i)[nu T_K(k)]=0.                      (K3-5)
```

Therefore

```text
partial_(p_i) Response_K
  = RetExtract[dot Schur_base].                  (K3-6)
```

On each connected physical p-domain, the exact classification is

```text
Response_K is p_i-free
  iff RetExtract[
        dot D
        -dot C A^(-1)B
        +C A^(-1)(dot A)A^(-1)B
        -C A^(-1)(dot B)
      ]=0 for every i and throughout the family; (K3-7 / P3-8)

Response_K is p-dependent
  iff that expression is nonzero for at least one i at an admitted point.
                                                        (K3-8 / P3-9)
```

This confirms the Lane 1 criterion exactly.  Nonconstant finite base inputs
do not prove survival after Schur reduction and retarded extraction.  The
finite retarded zero does not prove completed cancellation.  No authority
swept in §0.3 supplies either implication.

```text
K3 = PASS
MAXIMAL_RATIFIED_CLASSIFICATION = CONDITIONALLY_DEPENDENT
P_DEPENDENT = NO_VERDICT / TYPE-U
P_FREE = NO_VERDICT / TYPE-U
```

The two typed negatives above are mutually exclusive future outcomes, not
two physical claims.

---

## 5. K4 — ordered-rank port audit

The ratified source germ carries

```text
p_i = r_ch,i/(r_0,i+r_ch,i)                      (K4-1)
```

symbolically.  DoR-014's gauge theorem identifies same-rank presentations
without changing the germ outputs.  The chain was re-audited operation by
operation:

| Operation | Rank input found | Finding |
|---|---|---|
| DoR-016 doubled network law | `p_i` only | no separate ordered-rank slot |
| finite base restrictions | functions of `p_i` | no independent rank datum |
| completed `Gamma_base` interface | inherited base p port | no declared rank port |
| DoR-019 C/K metrics and Riesz maps | record/carrier geometry | no source-sector rank input |
| Shape K | `nu`, `k`, and the K metric | no source-sector rank input |
| complement inverse | A block only | inherits p through A; no new rank slot |
| Schur operation | supplied blocks | algebra cannot inject a rank datum |
| `RetExtract` | supplied Schur block | no independent rank slot |

Thus

```text
Response_K(r_0,r_ch;nu;k)
  = R_hat(p(r_0,r_ch);nu;k),

RANK_PAIR_DEPENDENCE_BEYOND_P = none_on_ratified_chain. (K4-2)
```

No ranks or ratios were selected or evaluated.

```text
K4 = PASS
```

---

## 6. K5 — symbolic `nu` statement

Equation `(K2-4)` proves

```text
partial_nu Response_K = RetExtract[T_K(k)],
partial_nu^2 Response_K = 0.                     (K5-1)
```

The coefficient contains `k`, the ratified K metric/Riesz structure, the
adopted radial profile, and the member-independent retarded map.  It contains
no p or ordered-rank port.  Consequently

```text
NU_DEPENDENCE = affine;
NU_COEFFICIENT_P_FREE = true;
NU_NONTRIVIAL
  iff RetExtract[T_K(k)] != 0.                   (K5-2)
```

The final iff is a separately typed vanishing question.  Neither the Lane 1
artifact nor this verification evaluates that coefficient.

```text
K5 = PASS
RETEXTRACT_TK_VANISHING = NO_VERDICT / TYPE-U
```

---

## 7. K6 — falsifier and regression suite

### 7.1 Finite shadows with probes

Shape K is all-orders flat on the finite active section.  Thus on every
finite restriction, including the admitted local-Log probes,

```text
rho_H,N(D_C phi_K)     = 0,
rho_H,N(D_C^2 phi_K)   = 0,
rho_H,N(D_C D_K phi_K) = 0,
rho_H,N(D_K D_C phi_K) = 0,
rho_H,N(D_K^2 phi_K)   = 0.                     (K6-1)
```

The restricted R5 Schur expression is therefore the base finite expression,
and the sealed Keldysh extraction reproduces

```text
FINITE_ACTIVE_REFERENCE_RETARDED_BLOCK = 0 |
  p-free | nu-free | every finite stage | probes included. (K6-2)
```

This matches Q-243 and Q-279 exactly.

### 7.2 Certificate rerun

| Check | Rerun | Verdict |
|---|---|---|
| covariance | `R_K`, `k^flat`, and `T_K` transport isometrically; R5 Schur/retarded cube commutes | **PASS** |
| reality | radial scalar is real and even; the ratified semilinear retarded law is preserved | **PASS** |
| batching | direct-sum carrier metric gives sectorwise Shape K Hessians and blockwise R5 assembly | **PASS** |
| restriction | `(K6-1)` and the R5 restriction cube reproduce the finite base block | **PASS** |
| rank-two exchange | norm and Riesz structure are invariant; no cycle member selected | **PASS** |
| pendant/tree quotient | K carrier has already descended through the faithful quotient; no tree direction is restored | **PASS** |
| cycle-creating stage | restriction is claimed only in the ratified downward/cube scope; no unlawful upward identity extension | **PASS** |
| zero degeneration | `nu -> 0` gives the Z-member/base response continuously and without changing carrier units | **PASS** |
| hidden unit | every C/K mixed term passes through the R4/DoR-019 seam | **PASS** |
| hidden selection | no rank, ratio, orientation, frame, filtration, cycle basis, or realization member selected | **PASS** |

### 7.3 Adversarial probe attack

A probe might make a finite noise block p-dependent, but the ordered
retarded extraction still has no common-source leg in the exact Q-279 table.
The probe therefore does not create a finite counterexample to `(K6-2)`.
It also does not decide the completed stationary pullback, because no theorem
identifies that completed evaluation with the finite active one.

```text
K6 = PASS
FINITE_TO_COMPLETED_ZERO_PROMOTION = forbidden / TYPE-R as an inference
```

---

## 8. K7 — `(P3-8)` decidability sweep

### 8.1 What is ratified now

DoR-019 removes the former carrier-geometry obstruction.  The following
objects now exist:

```text
C_prop and K_cycle completed carriers;
positive metrics and corrected Riesz maps;
the R4 unit seam and no-implicit-cross-sector-unit certificate;
the R5 Hessian restriction/covariance cube;
the reducing-complement inverse and Schur operation as lawful operations;
the structural RetExtract map on the admitted Schur class;
the Shape K member and all its jets.
```

This is sufficient to **state** `(P3-8)` on the correct completed carrier and
to prove that it is member-free.

### 8.2 What the deciding computation consumes

For every source-sector coordinate `p_i`, an execution of `(P3-8)` consumes

```text
1. c_star(p,k), or an equivalent executable base critical-family oracle;
2. A, B, C, D on that completed critical family;
3. partial_(p_i)[D_C Gamma_base], hence partial_(p_i)c_star;
4. dot A, dot B, dot C, dot D, including the stationary pullback;
5. the image X_i := dot Schur_base formed by (K3-4);
6. an executable RetExtract action on X_i, or equivalently a proved
   characterization of ker(RetExtract) on the span/image of all X_i;
7. a family-wide zero certificate X_i in ker(RetExtract) for every i,
   or one covariant nonzero witness.
```

Items 1–4 are not generated by the ratified stack.  Square V004 R3 states
that `Gamma_base,N^bot` is an independently supplied member of the finite
jet fiber.  Its R5 tuple contains `Gamma_base` and the Hessian block names as
proposal/adopted fields, but R5 transports and evaluates supplied fields; it
does not provide their off-section component formulas.  The confirmed
stationary response artifact names `explicit Gamma_base off the finite
active section` as part of the missing evaluation record.  DoR-018 fills
the member-jet part of that earlier record, not the base-action part.

Item 6 is likewise present only as structural retarded extraction plus its
covariance law.  No swept authority gives its completed kernel on the base
Schur p-derivative image.  A direct evaluation of each `RetExtract[X_i]`
would be equivalent and would also close the item.

### 8.3 Minimal stages do not decide

At the reciprocal loop, S8-A, and every finite stage, the active-reference
retarded block is exactly zero.  But the finite functional has no stationary
point of the required completed kind, and the completed action contains
coupled terms absent from the finite active shadow.  Therefore

```text
finite zero for every N
  does not imply
completed RetExtract[dot Schur_base]=0.           (K7-1)
```

No density, continuity, or tail-annihilation theorem in Q-315, R5, or
DoR-019 upgrades `(K7-1)` to an equality.  Minimal-stage execution can rerun
the falsifier but cannot decide the completed criterion.

### 8.4 Exact missing object

The narrow missing object is

```text
EVALUABLE_COMPLETED_BASE_STATIONARY_P_JET_AND_RETEXTRACT_KERNEL_INSTANCE := (
  Gamma_base on the completed R5 domain;
  its covariant critical family c_star(p,k);
  A,B,C,D and their total p-jets on that family;
  X_i=dot Schur_base for every i;
  ker(RetExtract)|_(span/image{X_i}), or direct RetExtract[X_i] data;
  restriction, reality, batching, covariance, and unit certificates
).
```

This is an **instantiation/certificate of the already-ratified base/R5
interfaces**, not another carrier metric and not another Shape K member.
Once it exists, the deciding operation is finite in logical length:

```text
form each X_i;
test X_i in ker(RetExtract) for all i;
apply (K3-7)/(K3-8).
```

The present artifact does not run that operation.

```text
K7 = NEEDS / TYPE-U

P38_DECISION = NEEDS |
  EVALUABLE_COMPLETED_BASE_STATIONARY_P_JET_AND_RETEXTRACT_KERNEL_INSTANCE

P38_EXACT_CRITERION_EXISTS = true
P38_CARRIER_EXISTS = true
P38_ALL_EXECUTABLE_COMPONENTS_RATIFIED = false
P38_MINIMAL_STAGE_SUFFICES = false
MACHINERY_APPEAL = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 9. Six-account and tag audit

| Operation | Domain | Image | Kernel / loss | Restriction square | Tail / completion | Tag |
|---|---|---|---|---|---|---|
| Shape K Hessian | ratified K carrier | K-covector block | finite active flat section | R5/DoR-019 cube | completed K carrier, no new tail | MEMBER-SENSITIVE |
| complement inverse | reducing C block | inverse C block | outside reducing domain excluded | R5 inverse square | completed domain ratified | MEMBER-INDEPENDENT for Shape K |
| Schur reduction | four R5 blocks | K/K Schur block | complement eliminated lawfully | R5 Schur square | no cycle inversion | MEMBER-SENSITIVE TOTAL |
| p derivative | completed base critical family | base Schur p-jet `X_i` | none declared | finite shadow only, not decision-complete | executable base instance missing | MEMBER-INDEPENDENT BASE |
| retarded extraction | admitted Schur class | physical retarded response class | relevant completed kernel uninstantiated | finite extraction exact | structural map exists | MEMBER-INDEPENDENT STRUCTURE |
| completed response | completed stationary family | response class | conditional by `(P3-8)` | finite zero exact | value/verdict not executed | MEMBER-SENSITIVE TOTAL |

No untagged member-sensitive or member-independent claim was found.

---

## 10. Final board

```text
K1_SUBSTITUTION = PASS
K2_EXACT_SPLIT = PASS
K3_CLASSIFICATION = PASS
K4_RANK_PORT = PASS
K5_NU_STATEMENT = PASS
K6_FALSIFIERS = PASS
K7_DECIDABILITY = NEEDS / TYPE-U

COMPUTATION = CONFIRMED
P38_DECISION = NEEDS |
  EVALUABLE_COMPLETED_BASE_STATIONARY_P_JET_AND_RETEXTRACT_KERNEL_INSTANCE

CONDITIONALLY_DEPENDENT_IS_MAXIMAL_CURRENT_STATEMENT = true
REGISTERED_P_VERDICT_WRITTEN = false
NO_P_VERDICT_DECLARED = true

ALPHA_EVALUATED = false
K_STAR_OR_ROOT_EVALUATED = false
NUMERIC_RESPONSE_VALUE_EVALUATED = false
RANK_OR_RATIO_EVALUATED = false
MEASURED_CONSTANT_COMPARISON = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No register, plan, tracker, git, commit, or push action was performed.
