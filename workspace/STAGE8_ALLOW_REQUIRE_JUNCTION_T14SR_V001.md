# STAGE 8 — THE ALLOW/REQUIRE JUNCTION OF THE SEALED SURFACE (T14SR) V001

DATE: 2026-08-15
COMMISSION: T14SR (JUNCTION-BUILD). OBJECT: the boundary between what the record FORCES
(requires) and what it PERMITS (allows), displayed as an object of record.
STATUS: DETERMINATION ONLY — display of sealed content and derived classification of displays.
No adoption, no authored physics, no value, no number. Nothing here discharges any absence.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: /Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md — ABSENT at start of commission (probed before
any write; ls returned "No such file or directory").

---

## §0 — GROUND CONSUMED, SEALS VERIFIED

All seals verified sha256(file) == sidecar digest, in the primary root
`/Users/bgm/MB Work/alpha-program-archive/workspace`:

```text
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md            (vacancy display)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md      (its audit)
OK  STAGE8_IDEAL_EXTENT_S9AD_V001.md                  (extent display)
OK  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md            (its audit)
OK  STAGE8_5D_REREAD_T13SR_V001.md                    (freedom exhibits; D1-D9)
OK  STAGE8_5D_REREAD_T13SR_AUDIT_V001.md              (its audit)
OK  STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md           (authored entry)
OK  STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001.md     (its check)
OK  DEMAND_READING_DECISION_OF_RECORD_V001.md         (principal decision, 2026-08-11)
OK  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md (typing correction, 2026-07-29)
OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md    (the three-shapes reading)
```

Wall pairs cited by the ground were seal-verified as consumed; each is listed at its point of
use below with its verification result.

## §0.1 — SWEEP DECLARATION (CUTOFFS)

```text
SWEEP 1 (filename): both roots, pattern (allow|require|demand|descent.?audit), case-
  insensitive; register/tracker/road/plan/continuation names excluded unread; cutoff = first
  120 names per root. Result: 40 artifact names (primary); root 2 a proper subset.
SWEEP 2 (content): both roots, pattern (three,? not one|THREE, not one|three (require )?
  shapes|shapes are three), same exclusions; cutoff = first 60 hits per root. Result: 6 files
  (primary), 3 (root 2).
LOCATED by SWEEP 2: STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md line 54 — the
  unique-require-forces + three-shapes reading. Seal verified. §1 consumes it at bytes.
ADJACENT, not the target: DEMAND_READING_DECISION_OF_RECORD_V001.md (the B03 one-object
  receiver-reading decision — a demand COUNT ruling, not the forcing-shape taxonomy);
  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md (typing split: the allow/require
  constraint is CELL_CONSTRAINT_ONLY; minimality sites carry their own labels). Both consumed
  where cited below.
ROOT 2 (/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003): mirror copies only; no artifact unique to
  root 2 was needed. "Q-..." register objects: EXPECTED-UNLOCATABLE, not searched beyond the
  declared patterns.
```

---
## §1 — THE SEALED DEMAND-READING, LOCATED AND CONSUMED AT ITS OWN BYTES

ARTIFACT: `STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md` (2026-07-30, lane
adjudication, SEAL OK). Its typed status is quoted faithfully: "The corrected standard in §2
is offered under Q-52 discipline — `derived = false`, specified to be tested and adopted or
refused by the principal." It is sealed on the surface; it is a lane adjudication, not a
principal adoption. This commission DISPLAYS it and derives classifications from it; it does
not upgrade its status.

THE UNIQUE-REQUIRE-FORCES READING (its §0, lines 24-25):

> "THE FIRST SENTENCE IS RIGHT. THE SECOND IS FALSE:
>  *** A REQUIRE-SHAPED CONDITION WITH DERIVED EXISTENCE AND UNIQUENESS FORCES A NUMBER. ***"

THE THREE REQUIRE SHAPES (its §1 POINT 1, lines 54-57 — consumed at bytes, NOT as quoted in
citing artifacts):

> "Forcing has ONE criterion (the admissible set is a point) and THREE shapes: a single require
>  with a unique root; EXACT JOINT DETERMINATION (n conditions, n freedoms — the marker/C_R
>  case, which the reviewer's 'two ways' silently folds in); and overdetermined joint
>  determination (surplus conditions)."

```text
SHAPE-1  SINGLE REQUIRE, UNIQUE ROOT          one condition; admissible set is a point.
SHAPE-2  EXACT JOINT DETERMINATION            n conditions, n freedoms; require distributed
                                              over legs (its §3: grade rides on weakest leg).
SHAPE-3  OVERDETERMINED JOINT DETERMINATION   surplus conditions; strongest anti-fit grade.
```

THE JUNCTION TAXONOMY IT SEALS (its §3 four-case map, lines 134-148, compressed):

```text
ALLOW + RANGE             FORCES? NO       (any selection is a choice)
ALLOW + UNIQUE SOLUTION   WITHIN THE CLASS (number rides on the class adoption)
REQUIRE + UNIQUE SOLUTION YES              (via §2 certificates)
OVERDETERMINED            YES              (with structural evidence)
```

ADJACENT SEALED READINGS CONSUMED (both SEAL OK):

- `DEMAND_READING_DECISION_OF_RECORD_V001.md` (2026-08-11, RATIFIED — PRINCIPAL DECISION):
  the B03 residual demand is ONE OBJECT (receiver reading governs) — "a common-origin Θ_hist
  delivery satisfying the sealed S1 receiver's typing, realized by any sealed route." This is
  a demand-COUNT ruling on one port, not the forcing-shape taxonomy; the two set-aside
  as-written items carry a revival falsifier ("if any sealed record demand leg is later shown,
  at bytes, to require either ... the as-written obligation revives").
- `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md` (2026-07-29): the allow/require
  CONSTRAINT is typed `CELL_CONSTRAINT_ONLY` (`allow_require_standalone_selector_status =
  CELL_CONSTRAINT_ONLY`); local minimality/saturation adoptions named after allow/require are
  DIFFERENTLY TYPED objects — five pure minimality sites, three misattributions, two missed
  termini. Consequence for this commission: "allow-side" below never means "licensed by the
  allow/require constraint"; it means NOT FORCED — the constraint itself is a cell constraint,
  not a selector.

---
## §2 — THE REQUIRE-SIDE INVENTORY (deliverable 1)

Every forced content of record on the sealed surface visible in the consumed ground, each with
its forcing chain's SHAPE (the §1 taxonomy) displayed at the bytes that carry it. Audit status
of the carriers: AJ pair CONFIRMED-WITH-CORRECTIONS (VACANCY-CLOSED stands, convergent verdicts
by independent routes); extent audit CONFIRMED-WITH-CORRECTIONS; 5d audit
CONFIRMED-WITH-CORRECTIONS (VERDICT FREE stands); JD check CHECK-PASS-WITH-CORRECTIONS (no
self-kill fires).

| # | Forced content of record | Forcing chain, at bytes | SHAPE |
|---|---|---|---|
| R-1 | WINDING/QUANTIZATION: winding labels are integer — "integer-quantized by single-valuedness (CAS P3a/P3b: fractional winding leaves an exact residual −2)" (AJ §5); exp(2πin) = 1 at integer n, residual −2 at n = 1/2 (AJ CAS P3a/P3b, PASS) | one condition (single-valued closure) whose admissible set is the integer lattice ℤ — a CLASS, not a point; the same closure kills continuum winding output ("an integer label, not a continuum response magnitude") | SHAPE-1 form (single require); terminates at a class — see §4 site J-1 |
| R-2 | CHARGE STRUCTURE: U(1) characters χ_n with cocycle/transition covariance (both characters transform with the same g^n — AJ §6.1, CAS P1); the displayed conjugate pair closes magnitude-free, χ_{+1}χ_{−1} = 1 (CAS N4); every native composite unit-modulus (CAS N1–N3); on-record amplitude uniquely the trivial constant G = 1 (CAS T1) | cocycle consistency + single-valuedness jointly pin the character lattice and its conjugation; the modulus theorem is an induction over the displayed operations (base + step + closure) | SHAPE-2 (joint determination of the structure); the G = 1 corollary is SHAPE-1 (single require, unique root — an actual forced value: the trivial amplitude) |
| R-3 | THE CROSSING'S FORM OBLIGATIONS: the (JD-1)/(JD-2)/(JD-3)/(JD-3a)/(JD-3c) target form — "The target form is FORCED; nothing in it is authored here" (JD §1.3, quoting fdf20bd4 §3.5) — plus the obligation sentence: orientation covariance, presentation independence, continuity, contact-kernel | a sealed demand fixing the FORM of any lawful supplier: several displayed clauses jointly pin the shape and its four proof obligations; the admissible set is the class of compliant suppliers, not a point | SHAPE-2 on the form (n clauses, n form-freedoms); terminates at the supplier class — §4 site J-3 |
| R-4 | THE EXACT SQUARES (wall, summed quantifier): "tr H_A^Sigma = 0 and \|\|H_A^Sigma\|\|_2^2 = 0 EXACT (two independent cancellation mechanisms); HS enclosure carrier-free via the DISCHARGED B-L2*" (5d §3); the fourth summed quantity's shortfall is a LOGARITHM, and the log is GENUINE at the census grade (display pair, fired branch) | two independent mechanisms deliver the same exact zeros — surplus conditions agreeing; decided of record at the summed quantifier ONLY ("per-composite untouched") | SHAPE-3 (overdetermined; the record's one two-mechanism agreement); terminates at the quantifier — §4 site J-4 |
| R-5 | EXTENT, pure-cycle slice: "CLOSED EXACTLY (X-1): the a=0 slice of ker(qtilde_N) = the (R4) line span{(0,1,−R_K,N)}, dim 1" — bound ABOVE by (B2-7) faithfulness through the [PROVABLE] (S2a) formation, met BELOW by the displayed (R4) generator (extent §4.1) | two conditions meeting exactly (upper bound + lower witness), one freedom — the slice is a point of the lattice of subspaces | SHAPE-2 (exact joint determination) |
| R-6 | EXTENT, menu: "dim ker(qtilde_N) is in {1,2} — no third extent exists" (X-2, exact linear algebra at dim E_N = 3); zero-value mixing EXCLUDED of record (X-3: value = 0 would force L_T,N = 0 against the sealed typing) | rank-nullity on the displayed carriers forces a TWO-POINT admissible set; the X-3 exclusion is faithfulness + the sealed typing jointly | menu: SHAPE-1 form terminating at a two-point set (ONE BIT — §4 site J-2); X-3: SHAPE-2 |
| R-7 | ALPHA-FORCING LADDER, derived rungs of record (5d §3): r-2 "B1-DERIVED: kappa_n → +infinity, full-family"; MO-3 "DERIVED p = −3: no coincidence vanishing — that route CLOSED"; T4SR "one-vertex trace-parity lemma DERIVED"; clock exponent "PINNED alpha-k in [1, 3/2]" | each a single displayed derivation with its own unique conclusion at its own quantifier; the exponent pin forces an INTERVAL, not a point | SHAPE-1 each (single require, unique root/conclusion); the pin terminates at an interval — §4 site J-5 |
| R-8 | CARRIER VANISHINGS (5d §4.3): working class "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic, member-independent, kernel-independent. Unconditional at that stratum"; b_1 = 1 cell "mirror collapse phi_f == 0 (F = 0 mandated) under the entered Attach TYPE-P premise" | the first is a kinematic identity (unconditional at its stratum); the second is forced RELATIVE to a carried TYPE-P premise and voids with it | SHAPE-1 (identity); the b_1 = 1 chain carries a named premise — a require riding an entered leg |
| R-9 | CHANNEL RIDER (5d §5.1): "a surviving cross-block forces the CHANNEL (interference present, irreducible rank-one, no sector-respecting basis removes it), NOT the winding magnitude \|n\| — n enters the derived form exactly once, as the global n^2 prefactor of an n-blind form" | conditional forcing: IF the cross-block survives, the channel is forced; the magnitude is expressly NOT — the require's own text draws the junction | SHAPE-1, conditional antecedent; terminates at the channel/magnitude split — §4 site J-1 |
| R-10 | THE NINE FORCED-FAILURE DISPLAYS D1–D9 (5d §4.1): each alpha-forcing link displayed NOT displayable at forced grade — D1 no derived carrier (ENTERED is not DERIVED); D2 domain extension entered-only; D3 typing rule UNWRITTEN; D4 gluing performed-not-licensed; D5 Attach TYPE-P undischarged; D6 mu symbolic-only, Act-5 content PENDING; D7 witnesses live / ladder open; D8 port stopped TYPE-R/TYPE-U; D9 realization datum not supplied | nine would-be require-chains, each terminating BEFORE forced grade at a named site; the FORCED sentence needs all nine (a SHAPE-2 conjunction), and zero are displayable | the FORCED branch is SHAPE-2 with 0/9 legs landed; each Di is a termination display — §4 consumes all nine |

Boundary note (per §1's minimality-split consumption): the five pure minimality sites (d = 4
lower-bound selection; C_R = 1 boundary point; orthogonalization saturation; tau = 1 floor;
spin-1/2 lowest class) are NOT require-side rows — each is a derived BOUND (require) followed
by an ADOPTED boundary-point selection (permission exercised); the derived-bound halves belong
to the require side, the selections to §3. The mixed site (one vectorlike pair + unit charge)
carries its unit-charge component "inherited from a different carrier/holonomy input" — the
charge-structure require R-2 reaches it; the pair-count half is minimality-shaped (allow-side).

---
## §3 — THE ALLOW-SIDE INVENTORY (deliverable 2)

Every permission of record in the consumed ground, each with its two-model witness or vacancy
display — the conservativity pattern: models agreeing on every displayed clause, differing on
the quantity. "Permission of record" = a quantity the sealed surface leaves unfixed, displayed
as such at bytes; per §1, never a licence from the allow/require cell constraint.

| # | Permission of record | Witness or vacancy display, at bytes |
|---|---|---|
| A-1 | THE MAGNITUDE PROFILE (the amplitude junction): any phase→magnitude response profile | TWO-MODEL WITNESS, executed: f₁ = (2 − w − conj w)/4 = sin²(Δ/2) and f₂ = f₁² satisfy every displayed clause simultaneously (AJ CAS M8/M9) and differ as functions (M3, exact residual 1/4) — "two models of every displayed clause exist that differ on the junction"; VACANCY-CLOSED; "the magnitude map is ALLOW-SIDE BY THEOREM ... its lawful provenance class UNINHABITED"; entry route Act-5 AUTHOR, "content PENDING ... no value exists" (5d NP-7) |
| A-2 | THE EXTENT BIT: Rhat_K,N ∈ R·L_T,N or not (E-1 vs E-2) | TWO-MODEL WITNESS of record: both extents are lawful over the displayed clauses ((LM1-10) "does not assume Rhat_K = L_T, does not assume they are independent"; (S8) "Mixed relations may identify the two displayed pieces"); the bit is the sealed question R_K_EQUALS_L_T = NO_VERDICT/TYPE-U, "UNDECIDABLE-TODAY + positive two-model certificate" (S9AD row), twice-determined (T1SR) |
| A-3 | THE WINDING MAGNITUDE \|n\| | VACANCY DISPLAY: "WINDING_FORCED = NOT-FORCED for \|n\| absent an external n-blind comparand, both sealed candidates unavailable"; "this docket supplies NO external n-blind comparand" (5d §5.1) — no displayed clause pins \|n\|; any integer inhabits every displayed clause |
| A-4 | mu (magnitude and sign) | VACANCY DISPLAY: "mu != 0 pinned SYMBOLICALLY ONLY, authored physics by principal act, sealed non-derivable; magnitude and sign OPEN (NO_VERDICT); principal value-selection BARRED" (NP-4) — the pin itself is an authored act, not a derivation |
| A-5 | THE COUPLING CONTENT (alpha's magnitude payload) | VACANCY + ACT: "no new require reached the magnitude; the record did not supply the coupling content on this ground"; Act 5 RESOLVED (a) AUTHOR — "the magnitude enters as an exercised permission, visibly, by the principal's hand, never as smuggled force" (NP-7); at fire time "no value exists" |
| A-6 | lambda_JD (the cross-sector unit) | PERMISSION EXERCISED, value withheld: "declared EXPLICIT, SYMBOLIC, UNASSIGNED"; explicitness itself OBLIGATORY (DoR-019 rule as carried), the datum free; anti-tuning standing row (SK-10); "any assignment, rescaling, or normalization ... is a tuning surface of record" (JD T2-5) |
| A-7 | THE AUTHORED CORE of the JD candidate (gr_N/FS_N^(0); FieldSym_N + sigma_field,N; sect_N; M_1/flat_g/Pi_iR; cell_N; rho_N; the per-cell rule; CONF-1) | MULTI-MODEL DISPLAY on the candidate's own face: "distinct admissible sections are DISTINCT CANDIDATES; no canonical section is claimed" (T2-3); the reference-covector alternative "displayed as rejected" (CL-4); the representative-route alternative displayed (CL-2) — the record's type slots admit many suppliers; only TYPE slots are of record (SK-3) |
| A-8 | THE MIXING COEFFICIENT a_K (live only in model E-2) | VACANCY DISPLAY: "coefficient a_K != 0 undisplayed" (extent X-2/E-2); never valued; its exhibition WITH exact value is the named closure object's (b) branch |
| A-9 | THE ORDERED RANK PAIR (r_0, r_ch) | VACANCY DISPLAY: "stays NO_VERDICT of record, value selection barred" (NP-2, rank-blindness respected) |
| A-10 | alpha' (summed-S2') | VACANCY DISPLAY: "alpha' A SYMBOL" — adopted spec content decides "a QUANTIFIER/TEST, not a verdict, a value, or a flag" (NP-5); two certification witnesses LIVE |
| A-11 | THE CLOCK EXPONENT within its pin | INTERVAL PERMISSION: "exponent PINNED alpha-k in [1, 3/2]" (5d §3, clock ceiling row, C12 witness pair) — the pin is require-side (R-7); the location WITHIN the interval is undecided at bytes (UNDECIDABLE-TODAY, forced at displayed-hypothesis grade) |
| A-12 | THE FIVE MINIMALITY SELECTIONS (d = 4 point; C_R = 1 boundary point; saturation; tau = 1 floor; spin-1/2 lowest class) | ADOPTION DISPLAYS at bytes (minimality split §"Five Genuine Pure Minimality Sites"): each is "adopted," "frozen branch convention," or "supplies the selected boundary point" — a derived bound followed by an unforced selection; "not a theorem" said in so many words at tau = 1 |
| A-13 | THE RUNNING TARGET | VACANCY DISPLAY: "no sealed running target exists ... of record 'would be a NEW PRINCIPLE,' a principal/spec-author act" (5d §4.2); the flow exhibited, the anchor absent |

CONSERVATIVITY PATTERN NOTE: A-1 is the executed instance (both models CAS-checked against
every displayed clause). A-2 is the record's second live instance (its two-model certificate
sealed in the bit pairs). A-3/A-4/A-5/A-8/A-9/A-10/A-13 are vacancy displays: the ground
carries no clause mentioning the quantity, so model freedom is textual (no clause to differ
on). A-6/A-7/A-12 are permissions EXERCISED — authored acts displayed as authored, the
junction's allow side made visible by hand rather than by model pair.

---
## §3.5 — CAS RE-VERIFICATION (fresh venv; exact symbolic; displayed bytes only)

Environment: fresh venv under the session scratchpad, sympy 1.14.0; no evalf, no N(), no float;
integers/rationals appearing are algebra residuals of identity checks on ground-symbolic or toy
objects, never physical values. The battery re-verifies at this commission's own hands the
displayed identities its classification leans on. Output, verbatim:

```text
T14SR CAS -- exact symbolic, zero numerics (sympy)
[J1a] PASS -- integer winding closes single-valuedly (R-1)
[J1b] PASS -- fractional winding exact residual -2 (R-1)
[J2a] PASS -- transition covariance: both characters carry the same g^n (R-2)
[J2b] PASS -- conjugate pair closes magnitude-free (R-2)
[J2c] PASS -- on-record amplitude forced to the trivial constant G = 1 (R-2/SHAPE-1)
[J3a] PASS -- f1 == sin^2(Delta/2) exactly (A-1 witness)
[J3b] PASS -- f1 != f2; exact separation residual 1/4 at the toy argument (A-1 witness)
[J4a] PASS -- E-1 (independence): dim ker = 1 exactly (R-6)
[J4b] PASS -- E-2 (mixing): dim ker = 2 exactly (R-6)
[J4c] PASS -- dim ker in {1,2}; no third extent (R-6)
[J4d] PASS -- zero-value mixing forces a = 0 against a != 0: antecedent empty (R-6/X-3)
TOTAL: 11 checks; FAIL count = 0
```

(J3a/J3b re-run the sealed TOY witness pair exactly as quarantined in the AJ build; toy status
carried — see TOY_SEPARATION.)

---

## §4 — THE JUNCTION ITSELF (deliverable 3): TERMINATION SITES, THEN THEIR CLASSIFICATION

For each require-chain of §2: the exact point, at bytes, where forcing ends and permission
begins.

| Site | Require-chain (from §2) | TERMINATION — where forcing ends, at bytes | What begins there (from §3) |
|---|---|---|---|
| J-1 | R-1 winding + R-9 channel rider | forcing ends at the INTEGER CLASS and (conditionally) the CHANNEL: "integer-quantized by single-valuedness"; "forces the CHANNEL ... NOT the winding magnitude \|n\| — n enters ... exactly once, as the global n^2 prefactor of an n-blind form"; the forcing would continue only through "an external n-blind comparand," and "both sealed candidates unavailable" | A-3: the choice of \|n\| |
| J-2 | R-5/R-6 extent chain | forcing ends at the TWO-POINT MENU: "dim ker ... in {1,2} — no third extent exists"; the chain stops because "O_R5,N is a PRIOR completed physical carrier ... the C-sector is consumed INSIDE the Schur complement and no block/direct-sum separation survives into O_R5,N"; "(LM1-10) states the quotient 'does not assume they are independent' in so many words" | A-2: the bit (E-1 vs E-2); A-8: a_K if E-2 |
| J-3 | R-3 crossing form obligations | forcing ends at the FORM + FOUR OBLIGATIONS: "The target form is FORCED; nothing in it is authored here" — and immediately past it, "NO SEALED ARROW OF THIS TYPE EXISTS (ba9430c1 K1.3; SW-3). This is the sector crossing itself — the type mismatch both independent routes died on. Everything about it below is AUTHORED" (JD §2) | A-6/A-7: lambda_JD and the authored core (rows 5–13 of the JD §6 table) |
| J-4 | R-4 exact squares (wall) | forcing ends at the SUMMED QUANTIFIER: "decided-by-cancellation at the summed quantifier; per-composite untouched"; the boundary-closure route FAILS-AT "F2 (locus: THE DIAGONAL, not the boundary)" with "all four wall quantities ... UNDECIDED" per-composite; the fourth summed quantity ends at its GENUINE log | per-composite openness; the log's coupling bearing lens-fenced (permission to read it as coupling REFUSED of record) |
| J-5 | R-7 ladder rungs | forcing ends rung by rung at NAMED BLOCKERS: MO-4 "blocked on a super-n^{3/2} clock"; clock ceiling "PINNED alpha-k in [1, 3/2]" (interval, not point); overlap law "the needed full-range asymptotic is THE genuinely NEW analytic input, displayed as blocker" | A-11: location within the pin; r-3 open |
| J-6 | R-8 carrier vanishings → the magnitude | forcing ends at the DISPLAYED EQUATION SET'S EDGE: "the ground's displayed equation set contains no clause linking any response magnitude to any phase datum (CAS M8)"; the Gleason-style rescue dies at the carrier: "fibers of dimension 1 ... The ground's own carrier is below the forcing threshold" (AJ §6.3.2) | A-1: the profile; A-5: the coupling content (Act-5 AUTHOR) |
| J-7 | R-10, the nine links D1–D9 | each link's own termination, at its byte: D1/D2 at ENTERED-not-DERIVED; D3 at "UNWRITTEN of record; the verdict TURNS on it"; D4 at "performed-not-licensed"; D5 at TYPE-P carried; D6 at "no value exists" (authored permission PENDING); D7 at live witnesses + unbuilt cluster kernel; D8 at "stopped of record — TYPE-R/TYPE-U"; D9 at "the realization datum is not supplied" | A-4, A-5, A-9, A-10, A-13 variously — the FREE verdict's whole allow side |
| J-8 | Minimality-site bound halves (§2 boundary note) | each derived bound ends AT ITS OWN BOUNDARY POINT: "a strict untrapped inequality leaves a half-line; the marginal public-closure rule supplies the selected boundary point"; "the lower bound is derived, but saturation is adopted"; "a derived lower bound is followed by an adopted lower-bound selection"; "the unit floor is a frozen branch convention, not a theorem"; "the sign selects half-integer classes; primitive minimality selects the lowest class" | A-12: the five selections |

### §4.1 CLASSIFICATION OF THE TERMINATION SITES — derived from the displays above

Sorting the eight sites by what their own bytes say stops the forcing:

```text
CARRIER-EDGE (the chain stops because the record's carrier ends or cannot
separate): J-2 ("prior completed physical carrier ... no block/direct-sum
  separation survives"); J-3 ("no sealed arrow of this type exists ... the
  sector crossing itself"); J-6 ("no clause in which a response magnitude and
  a phase-sector variable co-occur"; "carrier ... below the forcing
  threshold"); J-1 (continuation requires an EXTERNAL comparand — an object
  the carrier does not hold: "both sealed candidates unavailable").
QUANTIFIER-CROSSING (the chain holds at one quantifier and stops at the next):
  J-4 ("summed quantifier; per-composite untouched" — the same quantities,
  decided at Sigma, undecided per-composite); J-5 (each rung holds at its
  displayed-hypothesis grade and stops at the next rung's quantifier — the
  full-range asymptotic, the super-n^{3/2} clock); J-7's D7 leg (summed-level
  B-L2* discharged, per-composite and certification quantifiers open).
BOUNDARY/CLOSURE-ADJACENT (the chain derives a bound and stops at the bound's
  own boundary point): J-8, all five sites in so many words ("half-line ...
  selected boundary point"; "saturation ... adopted"; "floor ... frozen
  branch convention"; "lowest class").
MIXED-STATUS SITES: J-7 minus its D7 leg is not one structural type — D1/D2
  terminate at a GRADE boundary (ENTERED vs DERIVED), D3/D4 at ABSENT
  instruments (a rule unwritten, a licence not issued), D5/D6 at carried/
  pending AUTHORED acts, D8 at a typed STOP, D9 at an unsupplied datum.
```

DERIVED CLASSIFICATION: the termination sites are NOT scattered, and they are also not one
type. They fall into THREE STRUCTURAL CLUSTERS plus one mixed family: (i) every DEEP-STRUCTURE
termination — the sites where a forcing chain dies against the sealed surface itself (J-1, J-2,
J-3, J-6) — is CARRIER-EDGE: in each case the chain's own bytes locate the stop at the edge of
what the carrier holds or can separate, not at a missing lemma; (ii) every ANALYTIC-LADDER
termination (J-4, J-5, D7) is QUANTIFIER-CROSSING: the forcing survives at its own quantifier
and stops exactly at the crossing to the next; (iii) every SELECTION termination (J-8) is
BOUNDARY/CLOSURE-ADJACENT: a derived bound, then an adopted boundary point. The residual family
(J-7's D1–D6, D8, D9) terminates at INSTRUMENT-STATUS boundaries (entered/unwritten/unlicensed/
pending/stopped/unsupplied) — a fourth, process-typed cluster the record itself names link by
link. Correlation with the §1 shapes, displayed not asserted: the SHAPE-3 instance (R-4) sits
at a quantifier-crossing; the pure SHAPE-1 chains (R-1, R-2's corollary) terminate at
carrier-edges or at forced trivial values; the SHAPE-2 conjunction (R-10) terminates at
instrument-status boundaries. One byte of the record says the boundary type out loud in the
negative: the wall's F2 ruling — the fatal locus is "THE DIAGONAL, not the boundary, which
boundary-supported structure provably does not reach" — certifying that at J-4 the stop is NOT
boundary-adjacent, which is exactly why it classifies as quantifier-crossing here.

---
## §5 — THE META-QUESTION (deliverable 4): IS THE LINE'S LOCATION ITSELF FORCED?

Both directions attempted at bytes, per commission. "The line" = the assignment, per quantity,
of require-side vs allow-side status displayed in §2/§3.

### §5.1 Toward JUNCTION-RIGID — where the attempt SUCCEEDS, and its exact scope

AT THE MAGNITUDE SITE (J-6), the line's location IS model-independent, by the record's own
theorem: Theorem II quantifies over ALL exit operations and ALL profiles at once — "derivability
from the ground implies truth in every model of the ground," and CAS M8/M9 exhibit that the
displayed clause set constrains no profile; therefore EVERY model of the displayed clauses
places the magnitude allow-side. No model moves this boundary inward (forces a profile), and
none moves it outward (un-forces the winding class or the charge structure: J1a/J2a–J2c hold in
every model — they are consequences of the displayed clauses themselves). Scope of this
rigidity, exact: it is the AJ closure's own residue (§8.2) — conditional on the C12
categorical-scope premise for the withheld-content and refinement-stage components (the
conservativity core itself rests only on the displayed equation set), plus the finite-sealed-
text enumeration warrant. RIGID-AT-J-6, with the named residue.

### §5.2 Toward JUNCTION-CONTINGENT — where the attempt also SUCCEEDS: the meta-level
two-model witness, at bytes

AT THE EXTENT SITE (J-2), the record itself carries two models agreeing on every displayed
clause and differing on where a permission begins:

```text
MODEL E-1 (independence): every displayed clause holds ((LM1-10), (S8), the
  displayed generators, L_T,N != 0); ker dim 1; PURE-CYCLE-COMPLETE; "per
  round 3's biconditional ... the fiber INHABITED (modulo TOP-1/TOP-2 and
  the dim M_N cell)" — the B-1 permission OPENS here.
MODEL E-2 (mixing, some a_K != 0): the same displayed clauses hold; ker dim 2;
  the extra relation "kills BOTH families (NG-3' ...)" on the dim M_N >= 2
  cell — the SAME quantity is not a permission but a forced kill.
AGREEMENT ON REQUIRES: both models satisfy every §2 row that touches them
  (R-5's slice closure holds in both — J4a/J4b: the a=0 slice is the same
  dim-1 line in both; R-6's menu IS the model pair; X-3 holds in both).
DISAGREEMENT ON THE LINE: whether B-1 inhabitance sits allow-side (E-1) or
  a kill sits require-side (E-2) — the junction's location at J-2 differs
  between models of the same displayed clause set.
STATUS OF THE BIT: "UNDECIDABLE-TODAY + positive two-model certificate"
  (S9AD), independently re-determined (T1SR) — the witness is SEALED, twice.
```

This is exactly the commission's JUNCTION-CONTINGENT pattern, and it is not constructed here —
it is cited from the sealed extent display and the two bit pairs.

### §5.3 VERDICT

```text
VERDICT = JUNCTION-CONTINGENT — with a rigid sub-region, both at bytes:
  (i) the line's location is PROVABLY MODEL-INDEPENDENT at the magnitude
      site J-6 (rigid, conditional on the AJ residue: C12 + finite-text
      warrant) — no model of the displayed clauses moves the magnitude to
      the require side or the quantization class to the allow side;
  (ii) the line's location is PROVABLY MODEL-DEPENDENT at the extent site
      J-2: a sealed meta-level two-model witness exists (E-1/E-2, positive
      certificate, twice-determined) — two models agreeing on all displayed
      requires place the B-1 permission's onset differently;
  (iii) at the program level the line is FREE-not-closed: the 5d verdict is
      FREE with its conversion map NAMED and unexercised ("this verdict
      FORCES NOTHING SHUT") — the line's global location awaits F-i/F-ii/
      F-iii (toward a FREE closure) or the nine links (toward FORCED).
Because at least one sealed two-model witness moves the boundary between
models agreeing on every displayed require, the junction AS A WHOLE is not
rigid: JUNCTION-CONTINGENT.

WHAT DECIDES THE CONTINGENT SITE (named by the record, not by this
commission): the extent artifact's MISSING CLOSURE OBJECT — "one sealed
R_K_EQUALS_L_T determination at one anchor stage per isometry class," either
an (S1-5)-shaped independence certificate (=> E-1) or the exhibited
proportionality with exact a_K != 0 (=> E-2). Sealing either collapses the
witness pair and renders J-2 rigid; the program-level line additionally has
its own named deciders (F-i composition bar / F-ii the T1 typing written /
F-iii the DoR-013/014 falsifier; or all nine D-links at their grades).
NO NEW OBJECT is invented here; every decider named is the record's own.
```

---
## §6 — THE DOOR MAP (deliverable 5): the three require shapes against the record's named
absences — DISPLAY ONLY, no new derivation claimed

The four named doors, at their bytes: D-SUM the summability/cluster kernel ("the Gate-5
cluster kernel does not exist of record"; RUNS's anchor "the sealed running target ... plus the
unbuilt Gate-5 cluster kernel it would run on"); D-STITCH the stitching rule (D3 "the
cell-local write-typing rule is UNWRITTEN of record; the verdict TURNS on it (T1 kills, T0/T2
pass)" + D4 "performed-not-licensed"); D-BRIDGE the parent-node bridge (the form-to-HS bridge
"SPLIT THREE WAYS, NONE FULL"; the minimality split's missed terminus "the
no-independent-row-renormalization bridge attached to the source-mass depth ratio site");
D-ENTER sealed carriers entering (the AJ supply route "only genuinely new booked surface
structure — a change of ground"; K_dd/domain-extension at ENTERED awaiting DERIVED; an external
comparand entering sealed).

| Allow-side quantity | Which shape could still arrive, through which door — at the record's own bytes |
|---|---|
| A-1 profile / A-5 coupling content | D-ENTER ONLY, and jointly: "the new structure must do more than provide an exit operation ... it must provide the FORCING EQUATION: a booked clause in which a response magnitude and phase-sector data co-occur with enough rigidity to select the profile" (AJ §8.3) — a SHAPE-1 arrival carried inside new structure; SHAPE-3 only as its upgrade ("overdetermination adds ... corroboration"). D-SUM/D-STITCH/D-BRIDGE do not reach it: every surface-native route is closed by theorem |
| A-2 the extent bit / A-8 a_K | NONE OF THE FOUR NAMED DOORS. Its decider is its own named object — "one sealed R_K_EQUALS_L_T determination at one anchor stage per isometry class" ((S1-5)-shaped certificate or exhibited a_K) — a SHAPE-1 arrival (one certificate, one bit) on the EXISTING carrier; displayed for completeness, outside the commission's four |
| A-3 \|n\| | D-ENTER: "an external n-blind comparand" entering sealed (both prior candidates unavailable — one fork-gated, one UNAUTHORIZED); arrival would be SHAPE-2 (comparand + the derived n^2 prefactor jointly determining \|n\|) |
| A-4 mu (magnitude/sign) | D-ENTER: only a derivation on new structure could replace the authored symbolic pin (the ratified chain is "sealed non-derivable" on this ground) — SHAPE-1 if it arrives; the DoR-013/014 falsifier (F-iii) is a VOIDING door, not a forcing one |
| A-9 (r_0, r_ch) | D-ENTER at DERIVED grade: D1's own demand is "a DERIVED connected physical b_1 >= 2 filled-face carrier" — carrier + domain extension landing together is SHAPE-2; rank data stay barred while the entry is ENTERED |
| A-10 alpha' certification | D-SUM: the summed-S2' witnesses (SCAD uniformity/remainder) live at the summed quantifier; their discharge is quantifier work of SHAPE-2 kind (uniformity + remainder jointly); the adoption itself remains a spec act |
| A-11 exponent within [1, 3/2] | its rung's OWN named blocker first ("the needed full-range asymptotic is THE genuinely NEW analytic input"), then D-SUM (the r-3 race terminates in D7's post-limit kernel link) — SHAPE-1 per rung (each blocker one condition), SHAPE-2 across the ladder |
| A-12 the five minimality selections | D-BRIDGE for the depth-ratio site (its missed terminus is a bridge; a DERIVED bridge would be SHAPE-1 — as postulate it stays allow-side); the others have no named door among the four: a derivation replacing an adoption would enter at D-ENTER, SHAPE-1 each |
| A-6 lambda_JD / A-7 authored core | D-ENTER via SK-6's own words: "a future target-independent derivation selects the arrow — this candidate dissolves into the derivation and the authored core vanishes from the premise set" — SHAPE-1; R9-JII J1/J3 police the unit "as a FALSIFIER, never a constructor" |
| A-13 the running target | NO SHAPE ARRIVES THROUGH ANY DOOR: creating it "would be a NEW PRINCIPLE," a principal/spec-author act — an exercised permission by definition; D-SUM supplies only what it would RUN ON |
| the FORCED program verdict itself | all four doors at once, per the conversion map: "all nine links landed at their own grades" — D-ENTER (D1/D2), D-STITCH (D3/D4), D-SUM (D7), plus D5/D6/D8/D9's own instruments — the SHAPE-2 conjunction of §2 R-10 |

DISPLAY DISCIPLINE: every cell above cites the door's or quantity's own sealed words; no cell
asserts that any arrival WILL occur, is likely, or is authorized; no door is opened here.

---
## §7 — CHOICE LEDGER (commission T14SR; every unforced choice, classified)

```text
CH-1  IDENTIFYING THE SEALED DEMAND-READING with the forcing-boundary
      adjudication (its lines 24-25, 54-57): FORCED-BY-SWEEP — the declared
      content sweep for the commission's own description ("a unique require
      forces too"; "THREE shapes") lands on exactly one sealed artifact
      carrying both statements as its own content; the other five hits are
      displayed and disposed in §0.1 (different objects: a demand-count
      ruling; span conventions; a proof-conjunction count; thread shapes).
      Its LANE-ADJUDICATION status is displayed, not upgraded (§1).
CH-2  READING "the exact squares" as the wall's summed exact quantities
      (tr H_A^Sigma = 0, ||H_A^Sigma||_2^2 = 0): PREMISE(named) — they are
      the only contents in the consumed ground that are both EXACT and
      squares decided of record; the alternative reading (the S23/S24/S25
      commutative squares) is covered anyway at R-3/F-3 as form obligations.
      IMMATERIAL to the classification: under either reading J-4's
      termination bytes are the same artifacts'.
CH-3  INVENTORY GRANULARITY (10 require rows, 13 allow rows, 8 sites):
      PREMISE(named), presentation-grade; every cell is a consumed
      artifact's own sealed language; a finer split changes no verdict.
CH-4  CLASSIFICATION VOCABULARY = the commission's own three type names
      plus one residual family: FORCED in substance — the three named types
      are the commission's; the fourth (instrument-status) is not asserted
      as a new structural kind but as the record's own per-link naming
      (D1-D9), kept separate precisely so the three-type claim is not
      overstated.
CH-5  META-VERDICT FORM (site-partitioned verdict with a single overall
      sentence): FORCED by the displays — both directions succeed at
      different sites at bytes; any single-word verdict without the
      partition would misdisplay one of the two proofs. The overall word
      follows the commission's own definition (one sealed witness moving
      the line suffices for CONTINGENT).
CH-6  CAS BATTERY SCOPE (11 re-verification checks, no new content):
      PREMISE(named); the classification is textual/structural and stands
      without CAS; the battery re-executes displayed identities only.
CH-7  AUDIT CONSUMPTION at verdict-line grade (scoped greps) after full
      V001 reads, seals verified 8/8 + 3 located: PREMISE(named) — the
      commission's ground clause names the pairs; the audits' verdicts are
      consumed as their own one-line displays (§2 header).
CH-8  ROOT-2 NON-CONSUMPTION beyond the sweep (mirror subset): FORCED in
      substance — every named artifact resolved in the primary root at
      verified seals; root 2's sweep hits are a proper subset (§0.1).
ZERO entries in class OPEN.
```

## §8 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The only toy objects touched are the AJ pair's own quarantined model
  profiles f1, f2, re-executed at J3a/J3b EXACTLY as the sealed build
  quarantines them: meta-level counterexamples about the derivability
  relation, cited here solely as the A-1 two-model witness. The J4 battery
  rows use generic placeholder matrices to re-verify the extent artifact's
  own rank-nullity display (a_K symbolic, never valued). No toy value,
  profile, or model is asserted as surface content, candidate, or physics;
  none leaks into any inventory row, site classification, verdict, or flag.
  Every load-bearing object in §2-§6 is an ACTUAL sealed carrier or an
  actual sealed artifact's own verdict line.
```

## §9 — FLAG BLOCK

```text
COMMISSION = T14SR (JUNCTION-BUILD; the allow/require junction of the sealed
  surface as an object of record; display + derived classification only).
SEALS = 8/8 named-ground MATCH + 3 located (demand-reading, minimality
  split, forcing-boundary adjudication) MATCH; audits consumed at their own
  verdict lines (CONFIRMED-WITH-CORRECTIONS x3, CHECK-PASS-WITH-CORRECTIONS);
  MISMATCHES = 0.
DEMAND_READING = LOCATED + CONSUMED AT ITS OWN BYTES — the forcing-boundary
  adjudication: forcing has ONE criterion (admissible set a point) and
  THREE shapes (single require w/ unique root; exact joint determination;
  overdetermined); "a require-shaped condition with derived existence and
  uniqueness forces a number"; lane-adjudication status displayed verbatim,
  not upgraded. The one-object receiver-reading decision and the
  CELL_CONSTRAINT_ONLY typing split consumed as adjacent sealed readings.
REQUIRE_SIDE = 10 rows displayed at bytes with shapes (winding/quantization
  SHAPE-1-form to class; charge structure SHAPE-2 with the G = 1 SHAPE-1
  corollary; crossing form obligations SHAPE-2 on the form; the exact
  squares SHAPE-3 (two independent mechanisms, summed quantifier); extent
  slice SHAPE-2; extent menu to a two-point set; ladder rungs SHAPE-1 each;
  carrier vanishings; channel rider; the nine D1-D9 failure displays).
ALLOW_SIDE = 13 rows, each with two-model witness (A-1 executed CAS-grade;
  A-2 sealed positive certificate, twice-determined) or vacancy display
  (A-3, A-4, A-5, A-8, A-9, A-10, A-13) or exercised-permission display
  (A-6, A-7, A-12) or interval permission (A-11).
JUNCTION_SITES = 8 termination sites displayed at bytes; CLASSIFICATION
  DERIVED: three structural clusters — CARRIER-EDGE (J-1, J-2, J-3, J-6:
  every deep-structure termination), QUANTIFIER-CROSSING (J-4, J-5, D7:
  every analytic-ladder termination), BOUNDARY/CLOSURE-ADJACENT (J-8: every
  selection termination) — plus the instrument-status family (D1-D6, D8,
  D9, the record's own link-by-link naming). NOT scattered; not one type.
META_VERDICT = JUNCTION-CONTINGENT — with a rigid sub-region, both proven
  at bytes: RIGID at the magnitude site J-6 (two-model theorem quantifies
  over all models; conditional on the AJ residue C12 + finite-text
  warrant); CONTINGENT at the extent site J-2 (sealed meta-level two-model
  witness E-1/E-2: models agreeing on every displayed clause, differing on
  where the B-1 permission begins; positive certificate, twice-determined);
  program level FREE-not-closed with its conversion map named. DECIDERS,
  all the record's own: the R_K_EQUALS_L_T closure object (per isometry
  class) for J-2; F-i/F-ii/F-iii or the nine links for the program line.
DOOR_MAP = 11 allow-side rows crossed against the four named doors (D-SUM
  cluster kernel; D-STITCH typing rule/gluing licence; D-BRIDGE bridge
  family; D-ENTER new booked structure): magnitude reachable through
  D-ENTER ONLY and only jointly (exit operation + booked forcing clause);
  the extent bit through NONE of the four (its own named certificate); |n|
  through D-ENTER as SHAPE-2 (comparand + n^2 prefactor); the running
  target through NO door (a NEW PRINCIPLE, authored by definition).
  Display only; no arrival asserted; no door opened.
SWEEP_CUTOFF = DECLARED (§0.1: two sweeps, patterns + exclusions + caps;
  primary root + root-2 subset check; no register/tracker/road/plan/
  continuation file read; "Q-..." EXPECTED-UNLOCATABLE, cited only as
  carried inside sealed artifacts).
CAS = fresh venv, sympy 1.14.0, 11/11 PASS, exact symbolic only, no float,
  no value frozen; re-verification of displayed bytes only.
CHOICE_LEDGER = CH-1..CH-8 (§7): 4 FORCED/FORCED-in-substance, 4
  PREMISE(named); ZERO OPEN. TOY_SEPARATION = clean (§8).
DETERMINATION_ONLY = held: no adoption, no authored physics, no value, no
  number as a value of anything, no member bound, no gate/flag/witness of
  any consumed artifact moved; nothing here discharges any named absence.
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT
  before first write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = the junction is now an object of record: ten requires with their
  shapes, thirteen permissions with their witnesses or vacancies, eight
  termination sites falling into three derived clusters plus the record's
  own instrument-status family — and the line itself is JUNCTION-CONTINGENT:
  rigid exactly where the conservativity theorem holds the magnitude
  allow-side in every model, movable exactly where the sealed two-model
  witness E-1/E-2 stands, with every decider already named by the record
  it would decide.
```
