# STAGE 8 — MO-3 / THE R-L2b SHARPENED TARGET: THE COINCIDENCE EXPONENT p OF THE UNSANDWICHED DIFFERENCE KERNEL, DERIVED AT KERNEL GRADE FROM THE SEALED DUHAMEL/PROPAGATOR STRUCTURE

## BLIND BUILDER — CODENAME MO3-BUILD — COMMISSION S9AD — [CLAIMED]

Date: 2026-08-14
Role: BLIND builder (MO3-BUILD). Commission: the R-L2b SHARPENED TARGET =
r-3's MO-3 = the wall's F1 escape — the coincidence-vanishing order p of the
UNSANDWICHED difference kernel V(a) - V(0) as r = |x - y| -> 0 (threshold
p > 3/2 for CXC Hilbert-Schmidt), by the one route the corpus names but never
runs: derive the kernel's short-distance expansion DIRECTLY from the sealed
Duhamel/propagator structure, exactly, order by order.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ONLY — Clifford/band algebra, exact Fourier and
radial identities, exact-rational support witnesses; CAS (sympy 1.14.0, fresh
venv) verifying 28 exact identities, script and output reproduced verbatim
(§10); no floats as ground; no value frozen (the derived exponent is an exact
statement, not a numeric fit); no physical magnitude computed, bounded,
estimated, or evaluated. No scale, imported GR, or faithfulness premise used
as authority; scale-bearing sealed text read to fix structure and to consume
adjudicated results at their sealed grade. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-..." tokens
EXPECTED-UNLOCATABLE, not chased. No git action. ONE output (this artifact)
plus its seal sidecar at the commission-distinct path, probed ABSENT before
write. Every enumeration sentence claims only its own displayed sweep.

---

## 0. VERDICT IN ONE LINE

**DERIVED: p = -3. The unsandwiched difference kernel does NOT vanish at
coincidence at any order — it DIVERGES there at the same degree -3 as the sea
covariance itself, plus an exact CONTACT (delta) term. For the sealed
two-time cell-S-matrix object, at first order in a_Delta (which decides p; §8),
record-stratum zero (which the sealed record strata cannot cancel; §8), the
kernel's coincidence expansion at spatial base point x is EXACT:**

```text
[V(a) - V(0)](x, x - r) = i a_Delta [ c(x) delta^3(r) + Omega(x, r_hat) |r|^-3 (PV) ]
                          + O(|r|^-2) + C^infinity,

c(x)          = (1/8 pi) int_{S^2} dOmega_khat  k_hat_x [ (beta_+ - beta_-) I
                + (beta_+ + beta_-) alpha.k_hat ],
Omega(x,.)    = FT of the mean-free part of  a_0(x, k_hat)
                = i a_Delta k_hat_x [ beta_+ P_+(k_hat) - beta_- P_-(k_hat) ],
beta_s(x,k_hat) = int_0^1 b_D(t, x + s t k_hat) dt      (null-ray averages of the
                                                         sealed diamond profile),
```

**with BOTH leading coefficients certified nonzero at every spatial base point
0 < |x| < 1 (exact positivity of the ray averages; §3.5). In the sealed
grammar |X_diff(r)|^2 ~ r^{2p} this is p = -3 EXACTLY for the function part,
with the contact term sitting outside the power grammar and making the failure
strictly worse. The threshold p > 3/2 is REFUTED for the sealed object — not
missed by half a power: missed by 4.5 powers plus a delta. The F1 escape
closes NEGATIVELY: no certified sub-volume trace-norm rate for Delta_n(a) can
come from coincidence vanishing, because there is no coincidence vanishing.
The sandwiched-vs-unsandwiched grading gap of 29084cd2 is CLOSED by direct
derivation on both sides — no conversion step was performed: the unsandwiched
jet is derived here from the sealed Duhamel structure, and sandwiching it
reproduces the sealed two-time survivor exactly (§4.1); both gradings carry
kernel-coincidence degree -3. The equal-time "subtraction buys one order"
(p = 1) is confirmed to be an artifact of the refuted surrogate object class:
on the sealed object, subtraction removes the a-independent kernel EXACTLY
(that is what it buys) and supplies ZERO coincidence vanishing — the
intraband sector's time phase is non-oscillatory, so the cell-time
integration, the b_D all-orders edge vanishing, and the Gevrey decay are all
consumed and all sector-blind to the survivor.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session and matched against its
`.seal.sha256` sidecar (and the commission's pinned prefixes). First 8 hex.

```text
COMMISSION-PINNED SEALED STATE (consumed at audited grade):
29084cd2  STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md            OK (p undetermined of record; its failure modes = my fence list, §7)
52f2490b  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md                  OK (uniform constant refuted; b_D/J structure at CHECK 1)
4971e273  STAGE8_REQUIRE_BUILD_CANCELLATION_V001.md                      OK (equal-time-class one-power softening)
5801681a  STAGE8_CANC_PANEL_CLASSIFICATION_V001.md                       OK (RUNS-vs-CLOSES horn open; kernel-level full cancellation excluded)
564b7040  STAGE8_CANC_PANEL_SYMHUNT_V001.md                              OK (composition parity — surrogate class)
d1807b96  STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md OK (the sharpened target; threshold arithmetic; "delta times a bump")

THE SEALED DUHAMEL/PROPAGATOR STRUCTURE (the commissioned route's ground):
18af3dee  STAGE8_TWO_TIME_REANALYSIS_V001.md                             OK (the exact first-order two-time term; symbol-grade survivor)
6a2aa0fd  STAGE8_TWO_TIME_CHECK_V001.md                                  OK (cross-lineage CONFIRMED; pointwise-in-t sandwich isolation)
46846730  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md                    OK (E1: V, Delta, S1/S2, C1, C6, b_D Gevrey clause, V(0) = I)
789338ad  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md OK (PA: h_0, h_lambda, J, b_D = exp(16 - 1/s), frozen pairs)
5e49d209  STAGE8_R_RECORD_L_FORM_FABLE_V001.md                           OK (FORM: D3 branch structure)

FRAME / TARGET CONTEXT:
0f3082ca  STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md   OK (ECO: sealed C(r); pair-integral grammar)
2ede02ae  STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md           OK (RFA: r -> 0 internal to the diamond; aggregation arithmetic)
bebc0f08  STAGE8_R3_JOINT_RATE_S9AD_V001.md                              OK (r-3: MO-3 named; P-2 Duhamel pattern at operator grade)
72c95d42  STAGE8_R3_JOINT_RATE_S9AD_AUDIT_V001.md                        OK
```

15/15 MATCH. Key spans read at the bytes this session: E1 :465-470, :508-515,
:650-665, :686-700, :750-756, :1054-1060, :1105-1118; PA :75-145, :178-186;
plus the full texts of the six commission-pinned artifacts and the two-time
pair. No register/tracker/plan/road/ledger/lens/THE_HANDOFF file read.

---

## 2. THE OBJECT AND THE SEALED STRUCTURE IT RESTS ON — AT THE BYTES

```text
THE OBJECT. p is defined on the UNSANDWICHED difference V(a) - V(0), as the
  coincidence order entering |X_diff(r)|^2 ~ r^{2p} inside the pair integral
  int_{|r|<1} d^3r |C(r)|^2 |X_diff(r)|^2, with |C(r)|^2 ~ r^-6 carried
  separately (29084cd2 §1, verbatim consumed). Threshold: p > 3/2
  (d1807b96 Ground 1: 4 pi int r^{2p-4} dr converges at 0 iff p > 3/2).
  The sealed object is TWO-TIME: "MUST be a TWO-TIME (cell-S-matrix) object;
  the equal-time version is FALSE by C6" (E1 :696-698). This derivation is on
  the two-time object throughout.
THE SEALED GENERATOR (PA :75-127, :178-186; bytes read):
  h_lambda(t;a) = h_0 + lambda v(t) M(t) tensor S_n + a J(t),  h_0 = sum_j p_j tensor alpha_j
  J(t) = -(Q b_D(t,x) Q) tensor alpha_x
  b_D(t,x) = exp(16 - 1/s(t,x)),  s = s_- s_+,  s_- = t^2 - |x|^2,  s_+ = (1-t)^2 - |x|^2,
    support the open unit causal diamond, C-infinity, vanishing to all orders
    at boundary and tips, b_D(1/2,0) = 1, ||b_D||_inf = 1
  u_lambda(a) = the one-particle propagator generated by h_lambda(t;a) (PA :181-182)
  V_{mu lambda}(a) = u_mu(a_-)^dagger u_lambda(a_+) (E1 :653-654); V(0) = I on the
    surviving sector (E1 :754); frozen pairs a_Delta = 18/100 and 9/100, both
    nonzero exact rationals (PA :140-141).
THE EXACT FIRST-ORDER TWO-TIME TERM — SEALED AND CROSS-LINEAGE CONFIRMED
  (18af3dee §3; 6a2aa0fd §2, EXPANSION_FIDELITY = CONFIRMED):
  T_1 = -i int_0^1 W_mu(t,0)^dag [ a_+ G_{mu lambda}(t) J(t) - a_- J(t) G_{mu lambda}(t) ] W_lambda(t,0) dt
  record-stratum zero (pair-independent):
  T_1^{(0)} = + i a_Delta int_0^1 e^{i h_0 t} b_D(t,x) alpha_x e^{-i h_0 t} dt.
THE COMMISSIONED STEP, NEW HERE: the KERNEL of T_1^{(0)} and its exact r -> 0
  expansion. The corpus holds the symbol-grade statement (degree-0 intraband
  survivor, sealed); "conversion_step_exists_in_corpus = false" (29084cd2).
  No conversion is performed: the kernel is derived directly.
```

---

## 3. THE DERIVATION — EXACT, ORDER BY ORDER IN r

### 3.1 Conventions and the exact amplitude

FT pair: `f_hat(q) = int d^3u e^{-iq.u} f(u)`, inversion with `(2 pi)^-3`.
For an operator T, the exact left-quantized amplitude is
`a(x,k) := e^{-ik.x} (T e_k)(x)`, `e_k(u) = e^{ik.u}`, and by Fourier
inversion the kernel is EXACTLY

```text
K(x, x - r) = (2 pi)^-3 int d^3k  e^{ik.r} a(x,k).
```

(Self-check: multiplication by f gives a = f(x), K = f(x) delta^3(r) — exact.)
Acting with T_1^{(0)} on a plane wave and Fourier-transforming the profile:

```text
a(x,k) = i a_Delta int_0^1 dt (2 pi)^-3 int d^3q  e^{iq.x}
          e^{i alpha.(k+q) t}  b_hat_D(t; q)  alpha_x  e^{-i alpha.k t}      (EXACT)
```

with `b_hat_D(t;q)` the spatial FT of the sealed profile. Every subsequent
step manipulates this exact expression.

### 3.2 Band split and the two sectors

With `w = |k|`, `M = alpha.k_hat`, `P_pm = (I pm M)/2` (`P_- = C(k)`, sealed
E1 C1), and `e^{-i alpha.k t} = e^{-iwt} P_+ + e^{+iwt} P_-` (CAS A1/A2/A6),
the t-integrand splits into four band blocks with time phases
`e^{i(s'|k+q| - s|k|)t}`:

```text
INTERBAND (s' = -s): phase frequency |k+q| + |k| -> infinity at fixed q
  (CAS C2). b_D Gevrey with all derivatives vanishing at boundary and tips
  (PA :100-113; E1 :1056-1058, :1111-1113) makes its spacetime transform decay
  faster than any power; the q-integral is dominated; the interband sector of
  a(x,k) is O(|k|^{-N}) for every N — it contributes a C^infinity kernel near
  r = 0: bounded, NO singularity and NO vanishing constraint. (This is the
  sealed cell-time-integration mechanism WORKING, on its own sector — exactly
  as adjudicated at symbol grade, 18af3dee §4.3 / 6a2aa0fd §3.)
INTRABAND (s' = s): phase frequency s(|k+q| - |k|) -> s k_hat.q, BOUNDED at
  fixed q (CAS C1; first correction O(1/|k|), CAS C3). The time phase is
  non-oscillatory in the large-|k| limit; the cell-time integration supplies
  NO decay. The sealed massless on-shell marginality, now at kernel grade.
```

### 3.3 The leading intraband amplitude — closed form

In the intraband blocks, at large `|k|` and fixed `q`:
`P_s(k+q) alpha_x P_s(k) -> s k_hat_x P_s(k_hat)` (CAS A3/A4; corrections
O(|q|/|k|)), and the phase `e^{i s t k_hat.q}` combines with `e^{iq.x}` so the
q-integral is EXACT Fourier inversion at the transported point (CAS D5):

```text
(2 pi)^-3 int d^3q e^{iq.(x + s t k_hat)} b_hat_D(t;q) = b_D(t, x + s t k_hat).
```

Hence, defining the null-ray averages of the sealed profile

```text
beta_s(x, k_hat) := int_0^1 b_D(t, x + s t k_hat) dt ,        s in {+,-},
```

the amplitude obeys `a(x,k) = a_0(x,k_hat) + a_{-1}(x,k) + rho(x,k)` with

```text
a_0(x,k_hat) = i a_Delta k_hat_x [ beta_+(x,k_hat) P_+(k_hat) - beta_-(x,k_hat) P_-(k_hat) ]
             = (i a_Delta / 2) k_hat_x [ (beta_+ - beta_-) I + (beta_+ + beta_-) alpha.k_hat ]
```

(the two forms equal — CAS B1), `a_{-1}` of symbol class S^-1 (each listed
correction is O(1/|k|) with symbol-type derivative gains: the phase-argument
correction O(|q|^2/|k|) against the Schwartz q-decay, and the projector
correction O(|q|/|k|)), and `rho` the interband remnant plus the bounded
small-|k| region (compactly supported bounded). `beta_s` is smooth in
(x, k_hat) (b_D C-infinity), bounded by 1, real, >= 0, and
`beta_+(x, k_hat) = beta_-(x, -k_hat)`, so `Sigma := beta_+ + beta_-` is even
and `beta_+ - beta_-` odd under `k_hat -> -k_hat`; `a_0` is even in k_hat.

### 3.4 The kernel conversion — delta plus Calderon-Zygmund, exactly

The three pieces convert as follows (the derived step):

```text
(i)  a_0 (degree-0 homogeneous in k, smooth in k_hat): split a_0 = mean + mean-free
     on S^2. The angular MEAN converts to the CONTACT term:
        FT[mean] = c(x) delta^3(r),   c(x) = (4 pi)^-1 int dOmega a_0(x,k_hat)
     (delta coefficient = angular mean EXACTLY: pairing the mean-free part
     against a Gaussian gives zero, and a homogeneous degree -3 distribution
     supported at the origin can only be a multiple of delta — §3.6).
     The MEAN-FREE part converts to a PV kernel homogeneous of degree -3:
        FT[mean-free](r) = Omega(x, r_hat) |r|^-3,   int dOmega_rhat Omega = 0,
     nonzero iff the mean-free part is nonzero (§3.6). Exact worked instance
     (TOY-SEPARATED, §9): for k_hat-independent Sigma-bar,
        FT[ k_hat_x k_hat_j ] = (1/3) delta_xj delta^3(r)
                                + (delta_xj - 3 r_hat_x r_hat_j) / (4 pi |r|^3),
     assembled from FT[1/|k|^2] = 1/(4 pi r) (CAS D1),
     -d_x d_j (1/(4 pi r)) = (delta_xj - 3 r_hat_x r_hat_j)/(4 pi r^3) off
     origin (CAS D3a/D3b), Laplacian trace -> delta (CAS D3c), and the
     angular means <k_hat_x^2> = 1/3, <k_hat_x> = <k_hat_x k_hat_y> = 0
     (CAS D4a-c). The cutoff to |k| >= 1 changes the kernel by a bounded
     real-analytic function — no change at any singular order.
(ii) a_{-1} (class S^-1): kernel O(|r|^-2) at the diagonal — one full order
     below the leading terms. Standard symbol-to-kernel degree bookkeeping,
     the same class the sealed record already uses.
(iii) rho: C^infinity kernel near the diagonal (interband O(|k|^{-infinity})
     after the q-integration; bounded compact small-k part).
```

Therefore, EXACTLY:

```text
K(x, x-r) = i a_Delta [ c(x) delta^3(r) + Omega(x, r_hat) |r|^-3 (PV) ] + O(|r|^-2) + C^infinity
```

with `c(x)` and `Omega(x, r_hat)` the closed forms of §0.

### 3.5 Nonvanishing of both leading coefficients — exact, no evaluation

```text
RAY WITNESS (general): for every spatial x with 0 < |x| < 1, take k_hat = -x_hat
  and t = |x|: the transported point is (t, u) = (|x|, 0), where
  s(t, 0) = t^2 (1-t)^2 > 0 exactly (CAS E1) — the ray from (0, x) passes
  through the diamond's INTERIOR spine, so b_D > 0 there and, by continuity,
  on an open (t, k_hat) neighborhood. Hence beta_+(x, -x_hat) > 0 and
  Sigma(x, .) > 0 on an open set of directions. EXACT-RATIONAL INSTANCE
  (CAS E2/E3): x = (1/4)e_x, k_hat = -e_x, t = 3/10 gives s_- = 7/80,
  s_+ = 39/80, s = 273/6400 > 0, b_D = e^{16 - 6400/273} > 0 — no float.
CONVERSELY: for |x| >= 1 no null ray from (0,x) reaches the open diamond
  (|x + t k_hat| >= |x| - t >= 1 - t >= min(t, 1-t), equality only on the
  boundary where b_D = 0), and at the single point x = 0 every ray rides the
  lower light cone (|t k_hat| = t, s_- = 0): beta == 0. The leading jet is
  supported exactly on 0 < |x| < 1; its vanishing at x = 0 and outside the
  causal shadow is the correct two-time geometry, not a rescue.
CONTACT TERM c(x) != 0 on 0 < |x| < 1: the alpha_x-component of c(x) is
  (1/8 pi) int dOmega k_hat_x^2 Sigma(x, k_hat) > 0 — a nonnegative integrand,
  strictly positive on an open set. Exact positivity; nothing evaluated.
CZ TERM Omega(x,.) != 0 on 0 < |x| < 1: if the mean-free part of a_0
  vanished, every component of a_0 would be constant in k_hat; in particular
  k_hat_x^2 Sigma(x,k_hat) = C_x. On the great circle k_hat_x = 0 (CAS F1)
  this forces C_x = 0, hence Sigma == 0 by continuity — contradicting the ray
  witness. So the mean-free part is nonzero, and by §3.6 Omega is nonzero.
```

### 3.6 The two distributional lemmas, stated with their proofs

```text
LEMMA 1 (delta coefficient = angular mean). For g(k_hat) mean-free, smooth:
  <FT[g], Gaussian> = <g, FT[Gaussian]> = (int dOmega g) x (radial integral) = 0,
  while <c delta^3, Gaussian> = c x (positive). So FT[mean-free] has no delta
  component, and FT[mean] = mean x delta^3 trivially.
LEMMA 2 (mean-free nonvanishing transports). FT is injective on tempered
  distributions; FT[g] is homogeneous of degree -3 (exact FT scaling); a
  degree -3 homogeneous distribution supported at the origin is a multiple of
  delta^3 (origin-supported distributions are delta-derivative sums; only
  delta^3 itself has degree -3); by Lemma 1 that multiple is 0 for mean-free
  g != 0, contradicting injectivity if FT[g] were origin-supported. Hence
  FT[g] is nonzero away from r = 0: Omega != 0. Smoothness of Omega in r_hat
  follows from g smooth (standard CZ regularity; used only qualitatively).
```

### 3.7 The result

```text
p = -3.  |X_diff(r)|^2 ~ |Omega|^2 r^-6 = r^{2p} with p = -3 EXACTLY (function
part), plus the contact term i a_Delta c(x) delta^3(r) outside the power
grammar. The coincidence-vanishing order asked for by the sharpened target
(d1807b96 §4: "does the difference kernel vanish at coincidence like |r|^p
with p > 3/2?") is answered: IT DOES NOT VANISH AT ALL.
```

---

## 4. CONSISTENCY WITH THE SEALED RECORD — EVERY CONTACT POINT

### 4.1 The sandwich recovers the sealed two-time survivor exactly

`P_-(k_hat) a_0 P_-(k_hat) = - i a_Delta k_hat_x beta_-(x,k_hat) P_-(k_hat)`
(CAS B2) — VERBATIM the sealed survivor
`sigma(C T_1^{(0)} C) = -i a_Delta k_hat_x b_tilde(x,k_hat) C(k)` of
18af3dee/6a2aa0fd, with the transport sign now DERIVED (sea band s = -:
`b_tilde = beta_- = int_0^1 b_D(t, x - t k_hat) dt`), where the sealed pair
had noted it as conventional. The sandwiched kernel is therefore ALSO
delta + CZ at degree -3 (FT of a degree-0 symbol; its contact coefficient's
alpha-sector carries the strictly positive weight k_hat_x^2 beta_-). BOTH
GRADINGS AGREE IN COINCIDENCE DEGREE: sandwiching selects beta_- and flips a
sign; it does not change the degree. The grading gap 29084cd2 flagged is
closed from both ends by derivation, with no conversion step ever performed.

### 4.2 The remaining sealed contact points

```text
S1 (E1 :693-694): tr[C(k) alpha_x] = -2 k_hat_x re-verified (CAS B3); the
  trace-level oddness rescue and its non-transfer to the squared HS integrand
  are exactly as sealed — reproduced, not disturbed.
52f2490b LINK 6 (||X||_2 = infinity of record): now MECHANIZED — the limit
  object's HS divergence is the delta + CZ coincidence jet derived here.
d1807b96 §2 ("J's kernel is a delta times a bump ... hence ||X||_2 =
  INFINITY"): the "hence" — quick in the sealed text — is now a derivation:
  time-averaging the conjugated delta spreads ONLY the interband sector; the
  intraband sector retains an exact contact term with the ray-averaged bump
  as coefficient.
4971e273 / 5801681a / 564b7040 (subtraction buys one order; composition
  parity buys another, SANDWICHED, surrogate class): both mechanisms are
  object-class-bound. The surrogate's middle factor was the phase-dressed
  COVARIANCE (kernel degree -3 before subtraction, and the phase difference
  vanishes first-order at coincidence). The sealed middle factor is a
  difference of UNITARIES: V(0) cancels EXACTLY (identity on the surviving
  sector, E1 :754) — the subtraction's entire purchase — and the first-order
  Duhamel kernel carries no phase-difference prefactor at all. The TT pair
  established the class substitution at symbol grade (sealed, CONFIRMED);
  this artifact establishes what it means for p: the one-power softening was
  the surrogate's, never the object's.
29084cd2 §2 ("time integration: at most p = 1 is a ceiling on a method"):
  refined at kernel grade — on the sealed object time integration supplies
  NOTHING at coincidence on the intraband sector (non-oscillatory phase);
  the honest ceiling of that method on this object is VACUOUS, and the
  object's own p is -3.
29084cd2 §4 (region-level route at p = 0 not excluded): UNTOUCHED in logic;
  its INPUT changes (p = -3, not 0 or 1). The connected/cumulant
  (Gamma_K-log) route is a different functional and is not decided here.
WARD/COMM mechanism inventory (five sourced closures, ceilings 0 and 1):
  consistent — those were ceilings on candidate VANISHING mechanisms; the
  object possesses no vanishing for them to supply.
```

---

## 5. WHAT p = -3 SETTLES — THE COMMISSION'S EQUIVALENT FORMS

```text
THE THRESHOLD: p > 3/2 REFUTED. Pair-integral grammar (sealed): the
  integrand exponent is 2p - 4 = -10 (CAS F3); int_eps^1 r^-10 dr diverges,
  rate eps^-9 exact (CAS F2b), against the convergent-iff 2p - 3 > 0 (CAS
  F2a). And the contact term sits outside the grammar entirely: the sealed
  factorized model |C|^2 |X_diff|^2 presumed X_diff a function near r = 0;
  the derived jet contains delta^3(r) — the premise fails in the worst
  direction. CXC Hilbert-Schmidt does NOT follow from any coincidence
  behavior of the unsandwiched kernel; consistently, the sealed sandwiched
  survivor (degree-0 symbol) gives carrier-volume HS scaling of record.
THE |C|_4-SCALING EXPONENT alpha: NOT COMPUTED (fence). What is settled: the
  kernel-level mechanism that would have set alpha = 1 through the sealed
  RFA arithmetic (bounded difference kernel) is refuted at its root — the
  difference kernel is not bounded, not vanishing, and not one power short:
  it is divergent at degree -3 with a contact term. The RFA kernel-to-alpha
  dichotomy remains self-disclaimed power counting (RFA :86-88, :137-140)
  and is not run here in either direction.
THE SUB-VOLUME TRACE-NORM RATE FOR Delta_n(a) (= r-3's MO-3 = the wall's F1
  escape): CLOSED NEGATIVELY AT THIS ROUTE. The route "certify p > 3/2, get
  CXC HS, get the sub-volume rate" is dead at its first link. MO-3 as a
  NAMED OBJECT remains open only through routes that do not pass through
  coincidence vanishing of the unsandwiched kernel (per-pair cancellation at
  assembly, weight-summed re-quantification onto the record strata — both of
  record, neither touched here).
```

---

## 6. SLIVER VALIDITY AND THE EXACT QUANTIFIER

```text
QUANTIFIER of the derived statement: for the sealed two-time object
  V_{mu lambda}(a) - V_{mu lambda}(0), per record-color pair on the surviving
  sector (stratum zero is pair-independent), per CTP pair on the closed pair
  polydisc with a_Delta != 0 (both sealed frozen pairs qualify: a_Delta =
  18/100, 9/100 exact), at first order in a_Delta, record-stratum zero, at
  every spatial base point 0 < |x| < 1 of the unit cell; the exceptional set
  (x = 0 and |x| >= 1) is derived, not assumed.
SLIVER VALIDITY: r -> 0 is internal to whatever diamond one is in (RFA :33,
  of record). The derived jet is a per-cell interior statement; under the
  charter every diamond is isotropic with one scale, and coincidence DEGREES
  are invariant under isotropic dilation (coefficients rescale, orders do
  not). No aspect-ratio direction enters the derivation, so the failure
  p = -3 <= 3/2 holds uniformly over the chartered decomposition of any
  atom, sliver directions included. The per-cell pair-integral divergence
  (eps^-9, symbolic) is aspect-ratio-blind for the same reason.
```

---

## 7. THE FENCE LIST — 29084cd2's FAILURE MODES, MAPPED ONE BY ONE

```text
FM-1 SYMBOL COLLISION ON "X" (its Instance 9): avoided by construction — the
  object is pinned UNSANDWICHED V(a) - V(0) in every display; the sandwiched
  object appears only in §4.1, labeled, as a consistency recovery.
FM-2 THE GRADING CONVERSION ("converting degree -3 sandwiched into p = 0
  requires a grading step the corpus never performs"): NOT PERFORMED. No
  sandwiched fact was converted into p. p was derived on the unsandwiched
  kernel directly; the sandwich was then derived FROM it (legitimate
  direction), reproducing the sealed survivor.
FM-3 THE TWO-TIME AXIS ("the sealed object is two-time; the equal-time
  version is FALSE by C6"): the derivation lives on the sealed two-time
  Duhamel object from line one; the equal-time surrogate enters only as the
  adjudicated contrast (§4.2).
FM-4 SEPARATION-VS-COINCIDENCE CONFUSION: only r -> 0 is used; no
  large-separation statement is imported; the amplitude decay axis (R^-1) is
  never touched.
FM-5 CEILING-OVER-A-SET MISREAD ("at most p = 1" is not the object's p):
  respected — no mechanism ceiling was used as a value; the object's p is
  derived and lands below every ceiling in the inventory.
FM-6 p IS ONE EXPONENT, NOT A SPECTRUM: one p, defined as the sealed loci
  define it, derived once; order-by-order in a is handled by separability
  (§8), not by per-order exponents.
```

---

## 8. SCOPE — WHAT DECIDES p, AND WHAT THIS ARTIFACT DOES NOT DO

```text
FIRST ORDER DECIDES. V(a) - V(0) is analytic on the closed pair polydisc
  (sealed quantifier, E1 :687-689); its a_Delta-linear coefficient's
  coincidence jet is nonzero (derived). Higher-order-in-a terms are separate
  monomial orders and cannot cancel the linear jet identically over a
  continuum of pairs; at the sealed frozen pairs a_Delta != 0 exactly. The
  coincidence order of the full difference is that of its lowest nonvanishing
  a-order: p = -3.
RECORD STRATA (>= 1 sharp M(t) insertion): ADDITIVE separate terms, sealed
  sharp (E1 :465-470), outside the smooth class, power-law-tailed at the
  record-ball loci. Cancelling the smooth stratum-zero jet would require a
  record-built SMOOTH matching jet at every interior x — nothing sealed
  supplies one, and the panel-grade strengthening note is of record
  (6a2aa0fd SCOPE (ii)); C4 saturation runs the other way (E1 :343-350).
  If a record stratum is itself MORE singular, p only decreases: the
  threshold verdict is stable in both directions.
GALERKIN Q: transparent at the leading grade in the carrier interior — the
  same declared idealization the sealed TT pair carries, typed
  carrier-sensitive there. The continuum jet derived here is the
  carrier-uniform shape; on any fixed carrier the delta/CZ structure is
  smeared at the carrier's UV scale, which IS the sealed carrier-volume HS
  scaling shape (E1 C2). Nothing diverges on a fixed carrier; the
  carrier-UNIFORM demand is what p was for, and p is now derived.
BASE-POINT CONVENTION: left quantization (amplitude at x). Right or midpoint
  conventions shift coefficients at one degree down (O(r^-2)), never the
  leading orders or their nonvanishing.
NOT DONE HERE (fences): no alpha, no kappa_record, no coupling, no physical
  magnitude; no RUNS-vs-CLOSES adjudication of the program horn (the
  weight-summed and assembly re-quantifications of record are untouched); no
  region-level (cumulant) statement; no statement about the record-vertex
  strata's own rates (B-L2* stands where it stood).
```

---

## 9. CHOICE LEDGER, TOY SEPARATION, SWEEP CUTOFF

```text
CHOICE LEDGER (every discretionary choice, with its blast radius):
 CL-1 FT sign/normalization conventions — pure bookkeeping; self-check
      (multiplication -> f(x) delta^3(r)) displayed; no effect on p.
 CL-2 Left (Kohn-Nirenberg) quantization, exact by Fourier inversion —
      convention shifts live at O(r^-2); no effect on p.
 CL-3 Stratum-zero focus with panel-grade citation for the record strata —
      the additive/non-cancelling shape is consumed at its sealed grade,
      not re-derived; flagged in §8.
 CL-4 Q transparency in the carrier interior — the sealed TT pair's own
      declared idealization, same scope, same typing.
 CL-5 Homogeneous-extension regularization for FT of degree-0 symbols —
      standard tempered-distribution extension; the delta coefficient is
      shown intrinsic (Lemma 1), so no regularization ambiguity reaches any
      stated coefficient.
 CL-6 The |k| >= 1 cutoff placement — changes the kernel by a real-analytic
      function; displayed in §3.4(i).
TOY SEPARATION: the k_hat-independent-Sigma display in §3.4(i) is a TOY used
  only to exhibit the exact ell = 2 FT constants (CAS D1/D3/D4). The sealed-
  object claims rest on the general Lemmas 1-2 and the ray-witness
  positivity, never on the toy.
SWEEP CUTOFF (declared): corpus roots swept = workspace/ and
  cleanroom_output/ of the archive. Searches run: sidecar-hash lookups for
  the six commission digests; content greps for "b_D", "h_lambda",
  "16-1/s|16 - 1/s|e^{16", "coincidence", "p > 3/2|p>3/2", "MO-3", "P-2";
  artifacts opened in full or at named spans are exactly those in §1's seal
  table plus STAGE8_RATE_GAP_LINKAGE_S9AD_V001 (verdict section, for the
  gap-identity frame) and STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001 (F1 grep,
  for the escape's shape). CUTOFF: no full-corpus read; no fenced-class file
  opened; "Q-..." register tokens not chased (EXPECTED-UNLOCATABLE per
  commission). Enumeration claims are limited to these sweeps.
PROCESS NOTE OF RECORD: the CAS battery's first run reported D3a/D3b False —
  THIS builder's expected sign for -d_x d_j (1/(4 pi r)) was wrong (the
  correct off-origin value is (delta_xj - 3 r_hat_x r_hat_j)/(4 pi r^3)).
  The identity was re-derived by hand, the expected value corrected, and the
  corrected battery passes 28/28. The sign propagates ONLY into the
  orientation of the toy display's CZ tensor; no conclusion, coefficient
  formula, or nonvanishing claim depends on it.
```

---

## 10. THE SCRIPT (exact symbolic verification; reproduced in full) AND ITS OUTPUT

Run with sympy 1.14.0 in a fresh venv. Exact symbolic only; nothing numeric.

```python
# MO3-BUILD: exact symbolic verification battery for the kernel-grade coincidence
# expansion of the UNSANDWICHED difference V(a) - V(0) (first order, stratum zero).
# EXACT SYMBOLIC ONLY: polynomial/trig/Clifford identities, exact Fourier/radial
# integrals with symbolic (Abel) regularization, exact rational support witnesses.
# NOTHING numeric: no value of n, kappa_record, alpha (coupling), any physical
# norm, scale, or spectrum is computed, bounded, estimated, or evaluated.
import sympy as sp

I2 = sp.eye(2); Z2 = sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])

def alpha(s):  # Dirac alpha_j, chiral-block form [[0,s],[s,0]]  (PA :79 h_0 structure)
    return sp.Matrix(sp.BlockMatrix([[Z2, s], [s, Z2]]).as_explicit())

ax, ay, az = alpha(sx), alpha(sy), alpha(sz)
I4 = sp.eye(4)

kx, ky, kz, t = sp.symbols('kx ky kz t', real=True)
w = sp.symbols('w', positive=True)          # w = |k|, imposed by substitution w^2 -> kx^2+ky^2+kz^2
n2 = kx**2 + ky**2 + kz**2
K  = kx*ax + ky*ay + kz*az                  # symbol of h_0 = alpha.p
M  = K/w                                    # alpha.k_hat
Pm = (I4 - M)/2                             # sea band projector C(k)   (E1 C1)
Pp = (I4 + M)/2

def Z(Mx):
    Mx = sp.expand(Mx)
    for _ in range(3):
        Mx = sp.expand(Mx.subs(w**2, n2))
    Mx = sp.simplify(sp.expand(sp.expand_trig(Mx)))
    for _ in range(3):
        Mx = sp.expand(Mx.subs(w**2, n2))
    Mx = sp.simplify(Mx)
    return Mx == sp.zeros(*Mx.shape)

def Zs(e):
    e = sp.expand(sp.expand_trig(sp.expand(e)))
    for _ in range(3):
        e = sp.expand(e.subs(w**2, n2))
    return sp.simplify(e) == 0

R = []
def chk(name, ok):
    R.append((name, ok)); print(("PASS " if ok else "FAIL ") + name)

# ---- Block A: band algebra of the sealed vertex (kernel-grade reuse of the confirmed identities)
chk("A1  K^2 = |k|^2 I",                       Z(K*K - n2*I4))
chk("A2  P_-^2 = P_-, P_+^2 = P_+, P_+P_- = 0", Z(Pm*Pm - Pm) and Z(Pp*Pp - Pp) and Z(Pp*Pm))
chk("A3  P_- ax P_- = -(kx/w) P_-",            Z(Pm*ax*Pm + (kx/w)*Pm))
chk("A4  P_+ ax P_+ = +(kx/w) P_+",            Z(Pp*ax*Pp - (kx/w)*Pp))
apar  = (kx/w)*M
aperp = ax - apar
chk("A5  interband typing: P_- aperp P_- = 0 = P_+ aperp P_+", Z(Pm*aperp*Pm) and Z(Pp*aperp*Pp))
chk("A6  e^{iKt} = cos(wt)I + i sin(wt)M is unitary",
    Z((sp.cos(w*t)*I4 + sp.I*sp.sin(w*t)*M)*(sp.cos(w*t)*I4 - sp.I*sp.sin(w*t)*M) - I4))

# ---- Block B: the exact amplitude assembly a_0 and its sandwich reduction
bp, bm, aD = sp.symbols('beta_p beta_m a_Delta', real=True)   # beta_+/-(x,k_hat) as commuting scalars; a_Delta symbolic
a0_band = sp.I*aD*(kx/w)*(bp*Pp - bm*Pm)                      # a_0 = i a_D k_hat_x [beta_+ P_+ - beta_- P_-]
a0_split = (sp.I*aD/2)*(kx/w)*((bp - bm)*I4 + (bp + bm)*M)    # split into I and alpha.k_hat parts
chk("B1  a_0 band form = (i a_D/2) k_hat_x [(b+-b-)I + (b++b-) alpha.k_hat]", Z(a0_band - a0_split))
chk("B2  sandwich reduction P_- a_0 P_- = -i a_D k_hat_x beta_- P_-  (matches sealed TT survivor 18af3dee/6a2aa0fd)",
    Z(Pm*a0_band*Pm + sp.I*aD*(kx/w)*bm*Pm))
chk("B3  trace consistency tr[C(k) ax] = -2 kx/w  (sealed S1 identity, E1 :693-694)",
    Zs((Pm*ax).trace() + 2*kx/w))

# ---- Block C: on-shell kinematics (intraband bounded, interband divergent)
Ksym, qx, qy, qz = sp.symbols('Ksym qx qy qz', positive=True)
om_intra = sp.sqrt((Ksym+qz)**2 + qx**2 + qy**2) - Ksym
om_inter = sp.sqrt((Ksym+qz)**2 + qx**2 + qy**2) + Ksym
chk("C1  |k+q|-|k| -> k_hat.q (bounded intraband transfer)", sp.simplify(sp.limit(om_intra, Ksym, sp.oo) - qz) == 0)
chk("C2  |k+q|+|k| -> infinity (interband transfer diverges)", sp.limit(om_inter, Ksym, sp.oo) == sp.oo)
# first correction is O(1/|k|): series
ser = sp.series(om_intra, Ksym, sp.oo, 2).removeO()
chk("C3  intraband transfer = k_hat.q + O(1/|k|) (series witness)", sp.simplify(sp.limit((om_intra - qz)*Ksym, Ksym, sp.oo) - (qx**2+qy**2)/2) == 0)

# ---- Block D: exact Fourier identities for the kernel conversion (the derived step)
r_, u_, eps_ = sp.symbols('r u epsilon', positive=True)
# D1: (1/(2pi)^3) int d^3k e^{ik.r}/|k|^2 = 1/(4 pi r): radial reduction gives (1/(2 pi^2 r)) int_0^oo sin(u)/u du
int_sinc = sp.integrate(sp.sin(u_)/u_, (u_, 0, sp.oo))
chk("D1  int_0^oo sin(u)/u du = pi/2  =>  FT[1/|k|^2] = 1/(4 pi r)", sp.simplify(int_sinc - sp.pi/2) == 0)
# D2: (1/(2pi)^3) int d^3k e^{ik.r}/|k| = 1/(2 pi^2 r^2): radial reduction gives (1/(2 pi^2 r^2)) * int_0^oo sin(u) du (Abel)
int_abel = sp.limit(sp.integrate(sp.sin(u_)*sp.exp(-eps_*u_), (u_, 0, sp.oo)), eps_, 0)
chk("D2  Abel int_0^oo sin(u) du = 1  =>  FT[1/|k|] = 1/(2 pi^2 r^2)", sp.simplify(int_abel - 1) == 0)
# D3: -d_x d_j (1/(4 pi r)) off-origin = (delta_xj - 3 rhat_x rhat_j)/(4 pi r^3)
X, Y, Zc = sp.symbols('X Y Zc', real=True)
rr = sp.sqrt(X**2 + Y**2 + Zc**2)
Hxx = -sp.diff(1/(4*sp.pi*rr), X, X)
Hxy = -sp.diff(1/(4*sp.pi*rr), X, Y)
chk("D3a -d_x d_x (1/(4 pi r)) = (1 - 3 X^2/r^2)/(4 pi r^3)", sp.simplify(Hxx - (1 - 3*X**2/rr**2)/(4*sp.pi*rr**3)) == 0)
chk("D3b -d_x d_y (1/(4 pi r)) = -3 XY/(4 pi r^5)", sp.simplify(Hxy + 3*X*Y/(4*sp.pi*rr**5)) == 0)
chk("D3c Laplacian(1/r) = 0 off origin (delta content is distributional)",
    sp.simplify(sp.diff(1/rr, X, X) + sp.diff(1/rr, Y, Y) + sp.diff(1/rr, Zc, Zc)) == 0)
# D4: angular means on S^2 (the delta coefficient = angular mean; ell=2 projection)
th, ph = sp.symbols('theta phi', positive=True)
khx = sp.sin(th)*sp.cos(ph); khy = sp.sin(th)*sp.sin(ph); khz = sp.cos(th)
mean = lambda f: sp.integrate(sp.integrate(f*sp.sin(th), (th, 0, sp.pi)), (ph, 0, 2*sp.pi))/(4*sp.pi)
chk("D4a <k_hat_x>_S2 = 0",            sp.simplify(mean(khx)) == 0)
chk("D4b <k_hat_x^2>_S2 = 1/3",        sp.simplify(mean(khx**2) - sp.Rational(1,3)) == 0)
chk("D4c <k_hat_x k_hat_y>_S2 = 0",    sp.simplify(mean(khx*khy)) == 0)
# D5: Fourier inversion (the beta delta-evaluation), exact on a symbolic Gaussian (1D witness of the 3D identity)
q_, v_ = sp.symbols('q v', real=True)
bhat = sp.integrate(sp.exp(-u_**2)*sp.exp(-sp.I*q_*u_), (u_, -sp.oo, sp.oo)).rewrite(sp.exp)
binv = sp.integrate(bhat*sp.exp(sp.I*q_*v_), (q_, -sp.oo, sp.oo))/(2*sp.pi)
chk("D5  Fourier inversion exact on Gaussian (beta_s(x,k_hat) = int_0^1 b_D(t, x + s t k_hat) dt legitimated)",
    sp.simplify(binv - sp.exp(-v_**2)) == 0)

# ---- Block E: support/positivity witnesses (exact rationals; structural, no evaluation)
ts, xs = sp.symbols('t_s x_s', positive=True)
s_minus = ts**2; s_plus = (1 - ts)**2          # at spatial point u = 0: s_-(t,0) = t^2, s_+(t,0) = (1-t)^2
chk("E1  center-ray witness: s(t,0) = t^2 (1-t)^2 > 0 on 0<t<1 (symbolic factorization)",
    sp.simplify(s_minus*s_plus - (ts*(1-ts))**2) == 0)
# exact rational instance: x = (1/4,0,0), k_hat = (-1,0,0), t = 3/10 -> u = x + t k_hat = (-1/20,0,0)
tq = sp.Rational(3,10); uq = sp.Rational(-1,20)
sm = tq**2 - uq**2; spl = (1-tq)**2 - uq**2
chk("E2  rational ray witness: s_- = 7/80 > 0 and s_+ = 39/80 > 0 at (t,u) = (3/10, -1/20 e_x)",
    sm == sp.Rational(7,80) and spl == sp.Rational(39,80) and sm > 0 and spl > 0)
chk("E3  witness product s = s_- s_+ = 273/6400 > 0  =>  b_D = e^{16 - 6400/273} > 0 EXACTLY (no float)",
    sp.simplify(sm*spl - sp.Rational(273,6400)) == 0)

# ---- Block F: the nonvanishing lemma's algebraic core and the threshold bookkeeping
# F1: if k_hat_x^2 * Sigma = C_x (const) with Sigma bounded, then C_x = 0: witness that k_hat_x^2 has zeros on S^2
chk("F1  k_hat_x^2 vanishes on the great circle phi = pi/2 (so bounded Sigma forces C_x = 0)",
    sp.simplify(khx.subs(ph, sp.pi/2)) == 0)
# F2: threshold bookkeeping (symbolic degree statement, same class as sealed ECO/RFA precedents):
p_ = sp.symbols('p', real=True)
Ipow = sp.integrate(r_**(2*p_ - 4), (r_, eps_, 1))
chk("F2a pair-integral exponent: int_eps^1 r^{2p-4} dr converges as eps->0 iff 2p-3 > 0 (p > 3/2)",
    sp.simplify(sp.limit(Ipow.subs(p_, 2), eps_, 0) - 1) == 0)   # convergent instance p=2
div_at_m3 = sp.limit(sp.integrate(r_**(-10), (r_, eps_, 1)), eps_, 0)
chk("F2b at p = -3 the same integral diverges (rate eps^-9): divergence exact", div_at_m3 == sp.oo)
# F3: coincidence-jet bookkeeping: at p = -3 the pair-integrand exponent is 2p-4 = -10 (threshold needs > -1)
chk("F3  exponent bookkeeping: 2p - 4 at p = -3 equals -10 (threshold requires 2p - 4 > -1)",
    sp.simplify(sp.expand(2*(-3) - 4)) == -10)

print()
ok = all(x for _, x in R)
print("BATTERY:", ("ALL %d PASS" % len(R)) if ok else "FAILURES PRESENT")
```

Output, verbatim (corrected run; first-run D3 sign defect disclosed in §9):

```text
PASS A1  K^2 = |k|^2 I
PASS A2  P_-^2 = P_-, P_+^2 = P_+, P_+P_- = 0
PASS A3  P_- ax P_- = -(kx/w) P_-
PASS A4  P_+ ax P_+ = +(kx/w) P_+
PASS A5  interband typing: P_- aperp P_- = 0 = P_+ aperp P_+
PASS A6  e^{iKt} = cos(wt)I + i sin(wt)M is unitary
PASS B1  a_0 band form = (i a_D/2) k_hat_x [(b+-b-)I + (b++b-) alpha.k_hat]
PASS B2  sandwich reduction P_- a_0 P_- = -i a_D k_hat_x beta_- P_-  (matches sealed TT survivor 18af3dee/6a2aa0fd)
PASS B3  trace consistency tr[C(k) ax] = -2 kx/w  (sealed S1 identity, E1 :693-694)
PASS C1  |k+q|-|k| -> k_hat.q (bounded intraband transfer)
PASS C2  |k+q|+|k| -> infinity (interband transfer diverges)
PASS C3  intraband transfer = k_hat.q + O(1/|k|) (series witness)
PASS D1  int_0^oo sin(u)/u du = pi/2  =>  FT[1/|k|^2] = 1/(4 pi r)
PASS D2  Abel int_0^oo sin(u) du = 1  =>  FT[1/|k|] = 1/(2 pi^2 r^2)
PASS D3a -d_x d_x (1/(4 pi r)) = (1 - 3 X^2/r^2)/(4 pi r^3)
PASS D3b -d_x d_y (1/(4 pi r)) = -3 XY/(4 pi r^5)
PASS D3c Laplacian(1/r) = 0 off origin (delta content is distributional)
PASS D4a <k_hat_x>_S2 = 0
PASS D4b <k_hat_x^2>_S2 = 1/3
PASS D4c <k_hat_x k_hat_y>_S2 = 0
PASS D5  Fourier inversion exact on Gaussian (beta_s(x,k_hat) = int_0^1 b_D(t, x + s t k_hat) dt legitimated)
PASS E1  center-ray witness: s(t,0) = t^2 (1-t)^2 > 0 on 0<t<1 (symbolic factorization)
PASS E2  rational ray witness: s_- = 7/80 > 0 and s_+ = 39/80 > 0 at (t,u) = (3/10, -1/20 e_x)
PASS E3  witness product s = s_- s_+ = 273/6400 > 0  =>  b_D = e^{16 - 6400/273} > 0 EXACTLY (no float)
PASS F1  k_hat_x^2 vanishes on the great circle phi = pi/2 (so bounded Sigma forces C_x = 0)
PASS F2a pair-integral exponent: int_eps^1 r^{2p-4} dr converges as eps->0 iff 2p-3 > 0 (p > 3/2)
PASS F2b at p = -3 the same integral diverges (rate eps^-9): divergence exact
PASS F3  exponent bookkeeping: 2p - 4 at p = -3 equals -10 (threshold requires 2p - 4 > -1)

BATTERY: ALL 28 PASS
```

---

## 11. FLAG BLOCK

```text
P_EXPONENT = DERIVED(p = -3 EXACTLY, function part, in the sealed grammar
  |X_diff(r)|^2 ~ r^{2p}; PLUS an exact contact term i a_Delta c(x) delta^3(r)
  outside the power grammar. Quantifier: the sealed two-time object
  V_{mu lambda}(a) - V_{mu lambda}(0), per record-color pair on the surviving
  sector, per CTP pair on the closed pair polydisc with a_Delta != 0 (both
  sealed frozen pairs qualify, a_Delta = 18/100 and 9/100 exact), first order
  in a_Delta (which decides p by polydisc separability), record-stratum zero
  (which the sealed sharp strata cannot cancel — additive, outside the smooth
  class, 6a2aa0fd SCOPE (ii) of record; more-singular strata only lower p),
  Galerkin Q transparent in the carrier interior (the sealed TT pair's own
  declared idealization), at every spatial base point 0 < |x| < 1; the
  exceptional set x = 0 and |x| >= 1 derived. Coefficients closed-form:
  c(x) = (1/8 pi) int dOmega k_hat_x [(beta_+ - beta_-) I + (beta_+ + beta_-)
  alpha.k_hat]; Omega = FT of the mean-free part of i a_Delta k_hat_x
  [beta_+ P_+ - beta_- P_-]; beta_s(x,k_hat) = int_0^1 b_D(t, x + s t k_hat) dt;
  both certified nonzero by exact ray positivity (s(t,0) = t^2(1-t)^2;
  rational witness 273/6400) and the great-circle constancy contradiction.)

THRESHOLD = REFUTED(p > 3/2 fails: 2p - 4 = -10 against the convergence line
  -1; per-cell pair-integral divergence rate eps^-9 exact symbolic; and the
  contact term breaks the factorized grammar's function-kernel premise in the
  worst direction. The F1 escape / r-3 MO-3 / R-L2b sharpened target CLOSES
  NEGATIVELY on the coincidence-vanishing route: no certified sub-volume
  trace-norm rate for Delta_n(a) can be sourced from coincidence vanishing of
  the unsandwiched kernel, because there is none. Routes NOT through this
  mechanism (assembly cancellation, weight-summed re-quantification onto the
  record strata, region-level cumulant objects) are untouched and remain
  open of record.)

GRADING_GAP = CLOSED_BY_DERIVATION(29084cd2's sandwiched-vs-unsandwiched gap:
  no conversion step performed in either direction; the unsandwiched jet is
  derived directly from the sealed Duhamel structure, and sandwiching it
  reproduces the sealed two-time survivor -i a_Delta k_hat_x beta_- C(k)
  EXACTLY (CAS B2), with the transport sign now derived (sea band s = -).
  Both gradings carry kernel-coincidence degree -3: the sandwich selects
  beta_- and flips a sign; it does not change the degree. All six 29084cd2
  failure modes mapped and avoided one by one (§7).)

EQUAL_TIME_ONE_POWER = OBJECT_CLASS_ARTIFACT(the CANC "subtraction buys one
  order" (4971e273, panel-adjudicated) and the SYMHUNT composition parity
  (564b7040) are TRUE of the phase-dressed-covariance surrogate and DO NOT
  TRANSFER: on the sealed object the subtraction's entire purchase is the
  EXACT removal of the a-independent kernel (V(0) = I on the surviving
  sector), and the first-order Duhamel kernel carries no phase-difference
  prefactor. The b_D all-orders edge vanishing and the Gevrey decay are fully
  consumed and act only on the interband sector (faster than any power); the
  intraband time phase is non-oscillatory and receives nothing — the sealed
  massless on-shell marginality (18af3dee/6a2aa0fd, CONFIRMED) realized at
  kernel grade. Consistency contacts: S1 trace re-verified (CAS B3);
  52f2490b LINK 6 and d1807b96 §2's "delta times a bump => ||X||_2 =
  infinity" now MECHANIZED as the derived contact + CZ jet.)

MACHINERY_INVOKED = yes(exact symbolic CAS only — sympy 1.14.0, fresh venv,
  28-check battery reproduced verbatim with output in §10; one first-run sign
  defect (D3 expected value) disclosed and corrected in §9; shasum -a 256,
  file reads, and greps otherwise. NOTHING numeric: no value of n,
  kappa_record, alpha (coupling), any physical norm, scale, length, or
  spectrum computed, bounded, estimated, or evaluated; the derived exponent
  and all displayed rationals (18/100, 9/100, 7/80, 39/80, 273/6400) are
  exact structural statements, not measurements.)

FORBIDDEN_IMPORTS = none(no scale, no imported GR, no faithfulness premise as
  authority; scale-bearing sealed text consumed at its adjudicated grade to
  fix structure and for consistency contacts; every positive step is exact
  algebra on the sealed generator/profile forms. No register/tracker/plan/
  road/ledger/lens/THE_HANDOFF file read; "Q-..." tokens not chased; sweep
  cutoff declared (§9); 15/15 seals recomputed at path before reliance; no
  git action; output path probed ABSENT before write.)

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
ALL_RESULTS = CLAIMED until the adversarial panel.
MO3_P_EXPONENT_RESULT = COMPLETE AT COMMISSION SCOPE.
```
