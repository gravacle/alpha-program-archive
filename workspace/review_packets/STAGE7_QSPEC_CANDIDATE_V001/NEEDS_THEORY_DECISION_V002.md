# Needs Theory Decision v002

Date: 2026-07-24

This file continues the sealed `NEEDS_THEORY_DECISION.md` lineage without
editing that prior artifact. Forks 1 through 7 remain exactly as recorded
there.

## Fork 8: Durable-record architecture

Classification:

```text
HYPOTHESIS
```

### Question

What physical completion turns the finite reversible BID write into a durable
public record?

### Admissible mechanisms

The finite-system no-go leaves:

```text
an exact superselection or central sector;
an invariant post-write pointer algebra;
an infinite causal/environmental limit with outgoing sectors;
or a derived open-system limit from a larger unitary theory.
```

The current candidate is the Causal Direct-Limit Record Hypothesis. Its first
architecture audit includes a causal half-line, a three-branch tree, a
three-dimensional spatial lattice, a Lorentz-covariant causal-complex
continuum, and an effective continuum environment.

### Existing selector candidate

The ordinary branch discloses `3+1` Lorentzian covariance, locality, causal
cell composition, and a future boundary normal. The blind adjudication must
decide whether these inputs:

1. force one Lorentz-covariant causal-complex class;
2. demote the other architectures to regulators of that class;
3. or leave multiple physically inequivalent covariant completions.

No outcome is assumed here.

### Required work

1. Run the covariance selector before any decay result is inspected.
2. Compute the root-return decay law for all five sealed architectures.
3. Attempt the direct-limit existence proof for the surviving covariant
   class.
4. Carry cycle 7's `(T, delta)` thresholded durability quantifiers.
5. Derive an outgoing recoverable record sector, not merely root-amplitude
   loss.

### Closure condition

Fork 8 closes only if one physical architecture or one response-equivalent
class is forced, its direct limit and durable outgoing sector are derived,
and no free spectral density, topology, branching, regulator, or damping
normalization survives.

### Current status

```text
fork_8_registered = true
fork_8_classification = HYPOTHESIS
covariance_selector_executed = false
five_architecture_decay_lane_executed = false
covariant_direct_limit_existence_proved = false
outgoing_record_recoverability_proved = false
fork_8_closed = false
alpha_computed = false
```
