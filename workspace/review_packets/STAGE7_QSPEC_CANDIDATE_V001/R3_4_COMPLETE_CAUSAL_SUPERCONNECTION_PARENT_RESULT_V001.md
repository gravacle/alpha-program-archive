# R3.4 Complete Causal Superconnection Parent Result v001

Date: 2026-07-24

## Verdict

```text
FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN
```

The sealed first-order superconnection defines one finite causal
source-record parent with:

```text
the derived intrinsic diamond envelope;
one shared charged source;
distinct record factors;
all square-generated descendants;
a common self-adjoint domain;
a unique finite propagator;
finite compact-support Moller maps;
and exact reduced-state persistence of completed records.
```

The finite construction does not yet prove convergence of the complete
source-inclusive state or Moller operator under an infinite future
exhaustion. It is therefore not promoted to the strongest predeclared
complete-parent verdict.

## Intrinsic envelope

The uniform four-volume measure on the unit causal diamond pushes forward to

```text
w(s)=32 min(s,1-s)^3.
```

The exact audit gives:

```text
integral_0^1 w(s) ds = 1;
mean(s)               = 1/2.
```

No pulse parameter or fitted interior profile was introduced.

## One first-order operator and its descendants

The Clifford and cellular identities are reproduced to machine zero:

```text
maximum Clifford error                  = 0
incidence-spin Hermiticity error         = 0
incidence-spin square error              = 0
kinetic/incidence anticommutator error   = 0
record-incidence Hermiticity error       = 0
```

For the finite Galerkin regression, direct squaring of the parent agrees
with the declared generated decomposition:

```text
full square identity error               = 0
overlap-descendant norm                  = 16
derivative-support descendant norm       = 9.797958971132712
independent descendant coefficient used = false
```

Thus the overlap and closure-face terms are generated by the parent. They
were not added as separate dynamics.

## Finite causal evolution

The primary midpoint propagator converges at second order:

```text
||U_24-U_48|| / ||U_48-U_96|| = 4.001003471175759.
```

The first record is not assumed to be a universal exact finite-wavepacket
endpoint. In this disclosed regression its pointer probability is:

```text
after first cell  = 0.9252683055811919;
after second cell = 0.9252683055812020.
```

What is exact is persistence after closure:

```text
later-cell reduced-record error = 1.07e-14;
free-tail reduced-record error  = 3.41e-16.
```

The finite Moller unitary error is `4.26e-13`. Reversing the two causally
overlapping cells changes the propagator by `8.8750` in Frobenius norm.

The incoming pullback of the first pointer lies a distance `4.0662` from the
bare record-only algebra while remaining a projector to `1.74e-13`. This
reproduces the earlier correction: the incoming representation is a
source-dressed monomorphism, not a bare record endomorphism.

An independently coded RK4 state evolution used 2,400 steps per cell and
reproduced both pointer probabilities within the preregistered tolerance.
Its reduced-record restriction error is `1.15e-14`.

## Free asymptotic tail

In the declared first flat source-free branch,

```text
h_0(p)=alpha_D dot p;
h_0(p)^2=|p|^2 I.
```

The three-dimensional Clifford regression has zero dispersion error. On
`L2(R^3;C^4)`, Fourier multiplication and coarea give an absolutely
continuous `L1` spectral density for every admitted source wavepacket.
There is no source point spectrum. The Riemann-Lebesgue theorem therefore
gives thresholded source-root nonreturn.

The completed record-label degeneracy is different: `h_0` acts as the
identity on its record factor, making that label an invariant public sector.
Source return decay and record persistence are not conflated.

## Why the strongest verdict is withheld

For every finite set of causal cells, compact support makes the Moller maps
finite propagator expressions. The output record states are restriction
compatible and define a quasi-local record state.

For the full source-inclusive algebra, however, later cells continue to act
on the shared source. The current gate has not proved:

```text
convergence of source-inclusive finite states;
an infinite-future Moller limit;
or regulator independence of that full limit.
```

Those are mathematical obligations, not wording caveats. They are the next
continuum-completion gate.

## Status

```text
finite_causal_source_record_parent_derived = true
generated_descendant_identity_derived = true
finite_compact_support_Moller_derived = true
quasilocal_output_record_state_derived = true
source_dressed_incoming_record_monomorphism_derived = true
free_tail_source_spectrum_absolutely_continuous = true
thresholded_source_return_derived = true
exact_completed_record_reduced_state_persistence_derived = true

source_inclusive_state_projective_limit_derived = false
infinite_future_Moller_limit_derived = false
continuum_regulator_independence_derived = false
complete_parent_to_outgoing_GNS_map_derived = false
complete_causal_source_record_parent_flat_branch = false

absolute_record_interval_derived = false
curved_nonstationary_parent_derived = false
interacting_gauge_infraparticle_spectrum_derived = false
complete_parameter_free_Q_spec_frozen = false
physical_Thomson_stiffness_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
