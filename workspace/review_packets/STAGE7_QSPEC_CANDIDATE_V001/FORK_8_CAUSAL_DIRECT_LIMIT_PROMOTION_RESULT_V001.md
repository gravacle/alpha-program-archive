# Fork-8 Causal Direct-Limit Promotion Result v001

Date: 2026-07-24

## Verdict

```text
FORK_8_PROMOTED_TO_SCOPED_CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE
```

The original hypothesis has earned promotion for the durability and
outgoing-record problem in the ordinary `3+1` flat-asymptotic branch. All
seven conditions in its original promotion bar are now discharged within
that scope.

This result does not promote a global infinite-future source-scattering
theory, a curved-spacetime extension, a response normalization, or alpha.

## P1. Physical architecture

The blind five-architecture audit classified:

```text
A4 Lorentz-covariant causal-cell continuum = physical class;
A1 half-line, A2 tree, A3 spatial lattice = regulators/reductions;
A5 effective environment continuum        = downstream description.
```

No target value or decay match made that selection. It follows from the
disclosed `3+1` Lorentz covariance and causal composition.

The later global measure theorem fixes normalized four-volume as the unique
intrinsic positive absolutely-continuous measure on each flat primitive
diamond in its declared class.

A unique combinatorial triangulation is neither claimed nor needed. For the
durability question, two cofinal physical exhaustions are response-equivalent
because:

1. each completed record observable has finite cell support;
2. after its causal buffer, later primitive incidences act as the identity
   on its completed record factors;
3. its output state and dressed map therefore stabilize exactly; and
4. any two cofinal exhaustions eventually contain that same finite causal
   support.

This is response equivalence for the outgoing record net. It is not a claim
that every regulator has the same microscopic spectral measure or coupling
response.

## P2. Direct limit and generators

The finite record algebras and embeddings define:

```text
R_infinity=inductive_limit tensor_(j=1)^N M_3(C).
```

Their output states are exactly compatible, giving one quasi-local state and
its GNS representation. The completed-record dynamics is the strongly
continuous identity automorphism group, whose self-adjoint generator is
zero on that algebra.

The same parent supplies the source tail:

```text
h_0(p)=alpha_D dot p
```

on the ordinary flat source-free branch. The fixed Dirac operator is
self-adjoint on its standard domain, has purely absolutely-continuous source
spectrum for the declared finite-energy `L2` class, and has no source point
spectrum.

The source and record results are not merged into an unproved global
infinite-future Moller unitary.

## P3. Thresholded durability

For every admitted finite-energy `L2` source root, the spectral theorem and
coarea representation give an integrable spectral density. The
Riemann-Lebesgue theorem gives:

```text
A_source(t) -> 0.
```

By the definition of a limit, for every `delta>0` there is finite `T_delta`
such that:

```text
sup_(t>=T_delta)|A_source(t)|^2<delta.
```

Independently, Causal Incidence Support makes every completed record
restriction invariant under later cells. Source-root nonreturn and public
record persistence are therefore both derived and remain distinct.

## P4. Recoverable public records

The completed records have:

```text
a compatible quasi-local output state;
a GNS representation;
a coherent source-dressed incoming record net;
and central pointer averages M_N.
```

For every fixed-support observable `O`,

```text
||[M_N,O]|| <= 2m||O||/N -> 0.
```

The limiting expectations `+1` and `-1` separate the public labels. The
construction therefore provides recoverability, not only disappearance of
source-root amplitude.

## P5. No separately adjustable spectral law

The following objects descend from one sealed parent chain:

```text
intrinsic cell measure;
causal envelope;
first-order parent and square-generated descendants;
incoming quasifree state class;
free source-tail generator;
completed-record output state;
and dressed outgoing representation.
```

No independent density, topology, branching number, damping rate, regulator
weight, tail generator, or response normalization is added.

For a specified incoming state `psi`, the measure

```text
mu_psi(B)=<psi,P_h0(B)psi>
```

is computed from the fixed `h_0` and physical boundary data. Different
incoming states can have different measures. This result does not claim one
state-independent spectral profile, and these state profiles are not
authorized as coupling selectors.

## P6. New architecture-sensitive consequences

Two consequences were derived that the hypothesis did not assume:

1. the finite registration-counting ray cannot converge to a nonzero bounded
   Hilbert covector; its direct-limit home is instead a compatible
   quasi-local state and central sequence;
2. the incoming representation of an outgoing record is generally a
   coherent source-dressed monomorphism, not a bare record-only endomorphism.

Neither consequence was selected by numerical agreement.

## P7. Complete reporting

All five original architectures retain their sealed classifications. The
three regulators share a `t^-3` probability-decay class but not one complete
spectral measure. The physical continuum calculation and the effective
continuum remain separately identified. No winner or ranking was generated
from alpha, an endpoint, or a desired exponent.

## Promoted principle

Within the stated scope:

```text
A durable public record is the compatible outgoing sector of a
future-directed Lorentz-covariant causal exhaustion governed by the same
finite parent. Its durability consists jointly of thresholded source
nonreturn and exact completed-record persistence; its public content is the
recoverable quasi-local record state and central sequence.
```

This is a derived scoped principle, not a newly adopted numerical premise.

## Mandatory boundaries

```text
fork_8_closed = true
hypothesis_promoted_to_principle = true

global_source_inclusive_state_limit_derived = false
global_infinite_future_source_Moller_unitary_derived = false
curved_nonstationary_extension_derived = false
interacting_gauge_infraparticle_spectrum_derived = false
absolute_charged_response_normalization_derived = false
complete_parameter_free_Q_spec_frozen = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
