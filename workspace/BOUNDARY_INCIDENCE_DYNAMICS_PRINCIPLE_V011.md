# Boundary Incidence Dynamics Principle v011

Date: 2026-07-23

## Status

This document is a V011 repair specification in progress. It is not sealed,
no result gate has been executed, and no coupling has been computed. It is
target-value-free but historically target-aware.

Boundary Incidence Dynamics (BID) is a proposed Level-1 microscopic law. It
tests whether primitive record formation is governed by one natural
boundary operator rather than separately normalized sector actions. No
electromagnetic target value, mass, cosmological endpoint, earlier
charged-branch coefficient, or target-selected operator may adjudicate a
BID gate.

The unsealed v001-v009 drafts are superseded. Their reviews exposed:

```text
a rooted-star and equal-weight assumption hidden in the inputs;
an incomplete weighted family;
an undefined naturality condition and Hilbert metric;
an unresolved joint comparison group;
an untyped F^2/Pauli competitor class;
an underdefined loop prediction;
an incompatibility between the decorated category and its universal edge;
an incoherent object-by-object Hilbert-form family;
an incompletely specified gauge law for local augmentation;
an incompletely specified gauge law for local augmentation;
a word-order filtration that was not yet mathematically fixed;
a root-only category that did not contain every elementary-cell test map;
a Hilbert functor that stopped at degree one while the effective audit used
degree two;
an incorrect extension of flat local-system language to curved filled cells;
an unresolved Lorentzian/Euclidean contraction and functional selector;
undefined edge and face coefficient fibers;
a face curvature that was not a traceable endomorphism;
an unproved identification between the cellular adjoint and the covariant
shift;
an attempt to read a Pauli coefficient from a scalar response functional;
equal endpoint weight hidden inside a preselected augmentation;
an incomplete orientation-reversal law for the full competitor family;
an incidence filtration in which nominally order-zero transports collapsed
the curvature order;
a nonunique lift from face flux to connection tangent;
a selected covariance rule not yet derived from ray geometry;
one seal flag used both before and after the postseal parent comparison;
equal weights relocated into a same-pair reversal requirement;
an undefined reversed augmentation family;
an augmentation ideal without a universal algebra or completion;
an implicit projective state-orbit map;
conflation of the global action character, vertex gauge action, and ray
quotient;
and an impossible equality between a positive fidelity metric and the
indefinite Lorentz bivector form;
an incompletely defined category composition and decorated-morphism law;
missing maps among endpoint, chain, tangent, and face carriers;
an objectwise but not natural public-collapse map;
an absolute normalization incorrectly attributed to colimit uniqueness;
a free-group representation applied to partial finite-complex shifts;
an oppositely oriented universal plaquette word;
componentwise gauge stabilizers treated as one constant;
a flux lift stated outside its physical image;
an imported Fubini-Study metric described with an invalid passive-symmetry
uniqueness argument;
and a seal graph that did not distinguish preregistration specification from
executed result gates;
an abstract endpoint carrier without canonical maps into the object fibers;
a represented logarithm whose analytic and formal domains were conflated;
an exact-order test stated in the universal rather than represented
filtration;
an unfrozen response complex, background, root embedding, local tensor
extraction, Hodge map, and Hamiltonian-to-action map;
a dimensionless response made to depend unnecessarily on a future
dimensionful interval selector;
an external holdout requirement without an outcome-masked selection
protocol;
and a seal dependency graph described in prose but not encoded as literal
fail-closed dependencies.
```

V010 subsequently failed three complete hostile reviews. Its full immutable
snapshot, analytic zero-stiffness counterexample, and consolidated blocker
set are recorded in `BID_FULL_STACK_REVIEW_LEDGER_V003.md`. V011 treats every
one of those blockers as a regression failure rather than waiting for a
reviewer to rediscover it.

Every review of this revision must use the complete matrix
`BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md`. Its hash will be frozen only
when the V011 specification is complete.
A repair to any failed row creates a new principle version and requires all
matrix rows to be rerun. A delta-only review cannot seal BID.

The persistent failed-snapshot and repair ledger is
`BID_FULL_STACK_REVIEW_LEDGER_V003.md`. Its hash will be frozen with V011.

This revision seeks only `BID_v011_specification_sealed`: confirmation that
the premises, competitors, maps, tests, stop rules, and dependency graph are
complete enough to execute without discretionary choices. It does not seek a
gate-result, core-result, coupling, alpha, or proof seal. Unexecuted result
rows remain `PENDING-BY-DESIGN`; they are not counted as passes.

## Pinned inherited authorities

| Authority | Limited role | SHA-256 |
|---|---|---|
| `FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V001.md` | Declares the target-independent Level-1 record-action program, common `Q_spec`, and no independently adjustable bare `F^2`; it does not derive BID or alpha | `9894228202a4f2d53f8ef2eec3273401b773e0828e0ef04b40db1a03dae1138a` |
| `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` | Gives a conditional balanced two-path action bridge and its exact half-turn/FS budget; it is not authority for the V011 general finite-Hilbert premise, physical amplitude, alpha, or the full BID operator | `fc3e44f0ce78955c3c3ecbce57a901ca5f2770728b051052cc3ea638bcf3acdf` |
| `../primitive_comparison_group_provenance_gate_v001.md` | Historical fork among finite and continuous comparison models | `baa5f8150de019ed36fa9f946f8fc798aa5f272f1aefb3ea4be0ce66e7e09483` |
| `../minimal_public_carrier_principle_v001.md` | Historical target-aware minimal-carrier argument | `43f295a776ea1a789a1bb24d01fe3c068700e1c5cc823056a8f8e9a5d9a2f436` |
| `../primitive_action_character_carrier_completion_v001.md` | Conditional action-character completion; not independent proof | `9b2c0b93fb4bd2fbb34a8e9f5adb578b9b29422ea7adc8849ff425ad4139e23a` |
| `../primitive_complete_boundary_transition_functional_principle_v001.md` | Defines the complete normalized amplitude and transverse-Hessian target; does not select a determinant | `698051f21310c029f6e3b52aa49b3e129b94240214c48ffa75be6be00ca5e0a6` |
| `../primitive_boundary_native_record_CAR_functional_principle_v001.md` | Declared CAR product branch; admitted as a competitor, not assumed as the BID generator | `099440a4ca77d022ffb7a1977834c83155c9b53e3958fb56efa926e16c5ba0b7` |
| `../primitive_physical_charged_dirac_trace_completion_v001.md` | Conditional Dirac parity-even trace identity; does not fix Maxwell stiffness | `fe9f4e157e9beda7e8351575add717d28e10c49f17dcb0a0fc8dc50a7e2cabee` |
| `../primitive_inclusive_record_spectral_kernel_principle_v001.md` | Pre-existing unitary Duhamel/covariance response rule; physical inputs remain open | `b8c03857602e6f2bc5d07b0b30d2a335d721e49699c5951f9486f3b2a7bf4c32` |

The historical parent used only after the BID result is sealed is:

| Comparison artifact | Scope | SHA-256 |
|---|---|---|
| `../results/primitive_same_cell_discrete_parent_action_v001.json` | Historical structural parent; explicitly does not select alpha | `3f5991ef3ca62bc2166b73f6ae61c69a22648639af16021f0db458c49f81a262` |

The parent supplies no BID premise and may not be opened by the BID result
script. A separate comparison map must be preregistered before any parent
score is produced.

## Three distinct objects

The derivation must keep separate:

1. the **joint comparison group** represented by the action character;
2. the **endpoint carrier** of first public record alternatives; and
3. the **graded chain carrier** on which the incidence operator acts.

A finite-dimensional Hilbert carrier can carry a continuous group
representation. Carrier dimension does not determine whether the comparison
group is finite or continuous.

The connecting maps are explicit. For a first-opening object `K_open` with
root vertex `r`, endpoint vertices `p_h`, and object-fiber Hermitian lines
`L_r,L_pM,L_pQ,L_pG`, let

```text
E_open(K_open;L)
  = L_r direct-sum
    direct-sum_(h in {M,Q,G}) L_p_h
```

be the endpoint-alternative carrier. The direct-sum decomposition defines the
canonical summand injection

```text
iota_open:E_open(K_open;L)->C_0(K_open;L).
```

It is the identity on each displayed object fiber and zero on all other
vertex summands. No basis vector or metric normalization is chosen here. The
unresolved preparation is the projective root ray `P(L_r)`. Only after Gate 3
derives the counting Hermitian forms may `iota_open` be called an isometry and
may a unit representative of that ray be used. Applying the
handle-forgetful functor removes the endpoint labels after the injection; it
does not identify the four alternatives.

The complete cell-counting Hilbert carrier is

```text
H_cell(K)=C_0(K;L) direct-sum C_1(K;L) direct-sum C_2(K),
```

while the primitive odd incidence operator acts on the invariant subcarrier

```text
H_BID(K)=C_0(K;L) direct-sum C_1(K;L).
```

`C_2` supplies the counting metric and generated face-response carrier; it is
not silently included in the primitive `B_D` orbit. Accordingly, the
projective state orbit used below lies in `P(H_BID)`.

Finally, the real face-flux cochains and complex face carrier are connected by
the canonical real-form map

```text
c_2:A_R^2(K)->C_2(K),
c_2(sum_f a_f f)=sum_f a_f |f>,

A_R^2(K) tensor_R C ~= C_2(K).
```

No identification is made between connection tangents `A_R^1`, chain states
`C_1`, or endpoint alternatives; every later use must display its map.

## Common total-action comparison premise

Let the three inherited ordinary action handles contribute dimensionless
phases

```text
theta = (theta_M, theta_Q, theta_G) in R^3.
```

At the unresolved primitive surface their public phase is the phase of the
sum in the common action:

```text
ell(theta) = theta_M + theta_Q + theta_G.
```

Define

```text
K_sum = {theta in R^3 : ell(theta) is in 2 pi Z},
G_joint = R^3 / K_sum ~= U(1),
rho_joint([theta]) = exp(i ell(theta)).
```

`rho_joint` is strongly continuous and faithful on `G_joint`. This
**Common Total-Action Character Premise** is explicit new target-aware
physical content. It says the primitive surface records one total action
phase; it does not say the three downstream handles are the same physical
interaction.

Three appearances of `U(1)` are related but are not identified:

1. `G_joint` is the additive-action comparison group.
2. The vertex lines form an associated Hermitian line bundle
   `L=P x_(rho_joint) C` for a principal `G_joint` bundle `P`; `U_e` is
   parallel transport in this associated line bundle. Vertex gauge maps are
   changes of local section of `P`.
3. Projective state space quotients the common central scalar action on the
   complete Hilbert state. This removes a phase shared by every component.

Thus `rho_joint` converts action comparisons into relative phases on the
associated vertex fibers. Its component acting as one common scalar on the
complete state is precisely the component removed by the projective
quotient. The comparison group, local gauge-coordinate action, and ray
quotient are connected by this associated-bundle construction; they are not
three interchangeable assumptions.

Under this premise, `Z4` can be a finite subgroup sample and both `Z4` and
`Z2 x Z2` can be noncontinuous coarse-grainings or finite readout models.
Neither is an exact faithful continuous replacement for the connected group
`G_joint`; `Z2 x Z2` is not a subgroup or continuous group quotient of
`U(1)`. The theorem must prove this classification. It may not infer it from
a tree spectrum.

## First-opening topology premise

The ordinary branch inherits three independent public action handles and one
unresolved pre-record alternative. BID adds the following target-aware
first-opening premise:

```text
each independent primitive handle produces one distinct first public
endpoint alternative;
all primitive first-opening arrows begin at the unresolved root;
and no endpoint, arrow, loop, or composite cell exists at first order
without an additional public distinction or a composition.
```

This premise fixes the minimal first-opening `1`-complex to the rooted star
`K_(1,r)` with `r=3`. Consequently,

```text
dim C_0 = 4,
dim C_1 = 3,
dim(C_0 direct-sum C_1) = 7.
```

Equivalently, the first-opening inventory contains three public endpoint
vertices plus one unresolved root vertex. Those dimensions are a disclosed
target-aware postulate consequence. They are not a new BID prediction, a
holdout, or evidence for alpha.

## Record-complex categories and connection-preserving morphisms

Three categories are required; first-opening status and handle labels are not
informal annotations.

An object of `BareRec_2` is

```text
(K,r,L,U),
```

where `K` is a finite oriented regular CW complex of dimension at most two,
each connected component carries zero or one distinguished root `r`, `L`
assigns a one-dimensional Hermitian fiber to every vertex, and `U` is the
discrete unitary connection below. The first-opening object has exactly one
root. Rootless objects are retained so every elementary cell has a test
inclusion.

Each vertex `v` carries a one-dimensional Hermitian fiber `L_v`. Each
oriented edge `e:s->t` carries a unitary transport

```text
U_e:L_s->L_t,       U_(bar e)=U_e^(-1).
```

No flatness around a filled two-cell is imposed. In dimension one this is the
usual rank-one local-system data; in dimension two it is deliberately called
a discrete unitary connection, because nontrivial face holonomy must remain
admissible.

Choose one orientation representative for each unoriented edge and face.
The coefficient carriers are fixed by:

```text
C_0(K;L) = direct-sum_(v in K_0) L_v,

C_1(K;L) = direct-sum_(e in K_1^+) L_(s(e)),

C_2(K)   = direct-sum_(f in K_2^+) C |f>.
```

An edge coefficient is therefore stored in its source fiber. Orientation
reversal is not a second degree of freedom:

```text
x_(bar e) = -U_e x_e,
|bar f> = -|f>.
```

The face carrier is a gauge-neutral counting carrier. It records an oriented
public two-cell; it is not a flat continuation of the vertex line bundle.

For vertex gauge maps `h_v in U(1)`,

```text
psi_v -> h_v psi_v,
x_e -> h_(s(e)) x_e,
U_e -> h_(t(e)) U_e h_(s(e))^(-1),
z_f |f> -> z_f |f>.
```

A `BareRec_2` morphism is a pair

```text
(i, eta): (K,L_K) -> (K',L_K')
```

where:

```text
i is an injective cellular map preserving degree, incidence, orientation,
face attaching maps, and every root marked in the domain;
eta={eta_v} is a family of unitary fiber maps satisfying
eta_t U_e = U_(i(e)) eta_s on every domain edge.
```

The identity is `(id_K,{id_(L_v)})`. For composable morphisms

```text
(i,eta):(K,L)->(K',L'),
(j,zeta):(K',L')->(K'',L''),
```

composition is

```text
(j,zeta) compose (i,eta)
  = (j compose i,
     {zeta_(i(v)) compose eta_v}_v).
```

The transport intertwining and every domain-root image are preserved under
this composition; associativity and both identity laws follow from ordinary
map composition. These data complete the definition of `BareRec_2`.

Its induced chain maps are:

```text
J_0 psi_v = eta_v psi_v,
J_1 x_e = eta_(s(e)) x_e,
J_2(z_f |f>) = z_f |i(f)>.
```

An object of `OpenRec_2` is

```text
(K,r,L,U,FO),
```

where `(K,r,L,U)` is a `BareRec_2` object and `FO` is a designated subset of
the chosen oriented edge representatives `K_1^+`. Every `e in FO` begins at
the root of its connected component. An `OpenRec_2` morphism is a
`BareRec_2` morphism satisfying

```text
i^(-1)(FO_target)=FO_source.
```

Thus it both preserves and reflects first-opening status on its domain.
Identity and composition preserve this equation.

An object of `DecRec_2` is

```text
(K,r,L,U,FO,lambda),
lambda:FO->{M,Q,G}.
```

A `DecRec_2` morphism is an `OpenRec_2` morphism satisfying

```text
lambda_target(i(e))=lambda_source(e),  e in FO_source.
```

Identities and composition are inherited and remain label preserving. There
are two distinct forgetful functors:

```text
U_label:DecRec_2 -> OpenRec_2,
U_open:OpenRec_2 -> BareRec_2.
```

`U_label` removes only `lambda` and retains the first-opening subset.
`U_open` then removes first-opening status. BID's primitive magnitude
functor is required to factor through `U_label`, not through `U_open`; it may
distinguish a first-opening edge from a later composite edge but not the
`M/Q/G` name on that edge. This **Handle-Blind Primitive Magnitude
Hypothesis** is explicit new target-aware content: a handle may change
transported action data, but not the primitive magnitude assigned to edges
with the same opening status.

For each oriented `p`-cell `c` of `K`, with `p=0,1,2`, let `bar(c)` denote
its closed-cell object with the induced attaching data. If `c` is
first-opening, `bar(c)` inherits its source root, first-opening designation,
and label; otherwise no root or first-opening decoration is added.
There is a canonical connection-preserving cellular inclusion

```text
j_c:bar(c)->K.
```

On degree `p`, the induced map sends the unique top-cell generator of
`bar(c)` to `c`. A positive-dimensional `bar(c)` includes its attaching
boundary; "elementary cell" never means an isolated non-CW open cell. These
are the test maps used by the Hilbert gate. Their existence is no longer
inferred from a root-preserving category.

The universal edge object is the abstract transport diagram

```text
E_U = (L_s --U--> L_t),
```

with two vertices, one oriented edge, and a unitary line isomorphism
`U:L_s->L_t`. Choosing unit frames represents `U` by a scalar `u in U(1)`,
but that scalar is not part of the invariant definition. Every primitive
decorated edge defines a morphism

```text
(i_e,eta_e):E_U -> U_open(U_label(K)).
```

The fiber isomorphism `eta_e` is part of the morphism; its induced `J_0` and
`J_1` are the restrictions of the displayed chain maps.

## Coherent Hilbert functor and its competitor class

Before applying BID, the audit admits every coherent assignment of
positive-definite Hermitian forms

```text
M_p(K) on the degree-p coefficient carriers defined above, p=0,1,2,
```

including nondiagonal forms. "Coherent" means the assignment includes a
linear map on every `(i,eta)` and respects identity and composition. It is
not a collection of unrelated forms chosen object by object.

BID proposes the **Elementary Record Hilbertization Hypothesis**. The
Hilbert functor

```text
H_p:BareRec_2 -> Hilb,       p=0,1,2,
```

acts on `(i,eta)` by the induced `J_p(i,eta)` and satisfies:

1. `C_0`, `C_1`, and `C_2` are pairwise orthogonal grading sectors;
2. distinct elementary cells of the same degree are orthogonal;
3. disjoint union maps to orthogonal Hilbert direct sum;
4. every closed-cell map `j_c` is an isometry on its top-cell generator;
5. the unique top-cell generator of every `bar(c)` has norm one in
   dimensionless record-counting units; and
6. every bare incidence-preserving cell relabeling is unitary.

These conditions include roots, undecorated/decorated edges after applying
`U`, and two-cells through their elementary-cell maps. A root-specific norm,
a handle-specific edge norm, and a face-specific counting weight are not left
free. If consistent, they force:

```text
M_0(K) = I,
M_1(K) = I,
M_2(K) = I
```

in the elementary cell bases. This is a new physical hypothesis, not a
result inherited from the action character. Gate 3 must classify coherent
Hilbert functors starting from all positive-definite forms and prove whether
one unitary equivalence class survives.

Allowed coordinate equivalences are natural unitary transformations of
`H_0 direct-sum H_1 direct-sum H_2` preserving degree, incidence, root, and normalized
elementary alternatives. A compensating positive basis/measure rescaling is
not an equivalence; it changes the physical Hilbert operator and remains in
the competitor audit. The sole nonunitary exception is rescaling the
public-closure **constraint covector** by a nonzero scalar, which leaves its
kernel unchanged and is never used to normalize a Hilbert operator.

## Complete local first-incidence family

On an oriented edge `e:s->t`, for `x in L_s`, the complete
incidence-supported, character-covariant local column is

```text
D_(a,b),e x
  = i_t(a_e U_e x) - i_s(b_e x),

a_e and b_e complex numbers, not both zero.
```

The audit may not assume `a_e=b_e`.

Orientation is bookkeeping, not an additional physical handle. For the
reversed representative define

```text
R_1,e(x) = -U_e x in L_t.
```

Do not reuse the same coefficient pair by assumption. Give the reversed
representative an independent pair `(a_bar_e,b_bar_e)` and impose only that
the resulting column descend to the same underlying unoriented cell:

```text
D_(a_bar,b_bar),bar(e) R_1,e = D_(a,b),e.
```

Direct comparison of the source and target components gives

```text
a_bar_e = b_e,
b_bar_e = a_e.
```

This is the complete orientation-extension law. It is involutive and does
not imply `a_e=b_e`.

For `y=i_s(y_s)+i_t(y_t)` in `L_s direct-sum L_t`, admit the complete
nonzero gauge-covariant edge-local augmentation family into the target fiber:

```text
epsilon_(c,d),e(y)
  = c_e U_e y_s + d_e y_t in L_t,

c_e and d_e complex numbers, not both zero.
```

The physical public collapse is not selected from this family. BID adopts
the target-value-free **Operational Public-Closure Principle**:

1. a public readout must identify a source value with that same value
   transported to the target;
2. any compatible public readout must factor uniquely through the primitive
   closure, so no hidden endpoint quotient is inserted;
3. closure must be natural under connection-preserving fiber maps; and
4. closure of a composed path must agree with sequential closure.

These operational requirements characterize the categorical colimit of the
transport diagram. They are physical premises submitted to review, not
consequences of category theory or evidence for alpha. For
`L_s --U_e--> L_t`, the colimit in the underlying category of
finite-dimensional complex vector spaces and linear maps is `L_t` with
canonical cocone maps

```text
j_s = U_e:L_s->L_t,
j_t = id_(L_t):L_t->L_t.
```

Therefore a canonical representative of the map induced from the Hilbert
coproduct
`L_s direct-sum L_t` is

```text
epsilon_e(y_s,y_t) = U_e y_s + y_t.
```

This is the **Canonical Public-Collapse Ray**. In the hostile family, the
cocone equation requires

```text
c_e U_e = d_e U_e,
```

so `c_e=d_e`. A common nonzero scalar `lambda` describes the same closure
constraint because `ker(lambda epsilon_e)=ker(epsilon_e)`. The physical
object used by closed-boundary accounting is therefore the covector ray
`[epsilon_e]=[U_e,id]`, not an absolutely normalized transition amplitude.
The displayed `j_t=id` is the canonical terminal-object representative; no
observable magnitude is attributed to that convention. Absolute transition
magnitude is fixed only later by the one-record norm of `D`.

The collapse ray is coherent. For every connection-preserving morphism,

```text
eta_t epsilon_e
  = epsilon_(i(e)) (eta_s direct-sum eta_t).
```

For the endpoint-swap map

```text
S_e(y_s,y_t)=(y_t,y_s),
```

the reverse-edge collapse lands in `L_s` and obeys

```text
U_e epsilon_(bar e) S_e = epsilon_e.
```

Thus it is natural and independent of the chosen edge orientation.
Equal endpoint ratio is a consequence of the disclosed operational-colimit
principle, not a target value. It is not represented as an independently
discovered fact.

Sequential composition is also explicit. For

```text
L_s --U--> L_t --V--> L_u,
```

the chain-diagram colimit map is

```text
epsilon_(V,U)^(3)(y_s,y_t,y_u)
  = V U y_s+V y_t+y_u.
```

Taking the colimit of the first arrow and then the second gives

```text
epsilon_V(epsilon_U(y_s,y_t),y_u)
  = V(U y_s+y_t)+y_u
  = epsilon_(V,U)^(3)(y_s,y_t,y_u).
```

This is the required path-composition coherence; it does not identify the
three input records or omit the intermediate endpoint.

BID then adopts **Local Closed-Boundary Accounting**:

```text
epsilon_e(D_(a,b),e x)
  = (a_e-b_e) U_e x = 0
```

for every primitive edge and every `x`. Under a vertex gauge transformation,

```text
D_(a,b),e x -> (h_s direct-sum h_t) D_(a,b),e x,
epsilon_e(y) -> h_t epsilon_e(y).
```

Thus the zero equation is coordinate-free and gauge covariant.
This is an edge-local covector only; no global augmentation is assumed, so
nontrivial loop holonomy remains admissible.

The order of inference is frozen:

1. orientation-choice invariance derives the reversed pair from the general
   `(a,b)` and independent `(a_bar,b_bar)` families;
2. the operational colimit property is tested against every nonzero `(c,d)`;
3. naturality, orientation coherence, and path-composition coherence are
   checked for the surviving collapse ray;
4. local closed-boundary accounting is applied to that derived collapse;
5. naturality and cell relabeling test edge/handle dependence; and
6. one-record normalization is applied last.

Thus equal endpoint ratio is not built into the hostile augmentation family;
it must follow conditionally from the declared operational-colimit principle
before closure is evaluated.
Cases with `a=0`, `b=0`, `c=0`, or `d=0` remain admitted until the displayed
tests reject them, if they do.

The differential naturality condition is fixed as

```text
D_K J_1(i_e,eta_e) = J_0(i_e,eta_e) D_(E_U)
```

for every primitive one-edge morphism `(i_e,eta_e):E_U->U(K)`.

Any two abstract unitary line diagrams `E_U` and `E_V` are isomorphic:
after choosing any unitary `eta_s:L_s->L'_s`, the equation

```text
eta_t U = V eta_s
```

fixes a unitary `eta_t`. Differential naturality over these isomorphisms,
cell relabelings, and the handle-forgetful functor must therefore eliminate
edge-, transport-, and handle-dependent coefficients. After closure gives
`a=b`, one-record normalization gives `|a|=1`. The surviving common phase is
removed by the natural degree-one unitary transformation multiplying every
edge generator by that phase. Gate 4 must verify each step rather than
recording this paragraph as a passed result.

Full operator intertwining

```text
B_K J = J B_E
```

is not assumed; it is generally false at a shared vertex because the adjoint
sees other incident edges. After the Hilbert functor is fixed, compression
naturality

```text
J^dagger B_K J = B_E
```

is an audit consequence, not a premise.

With the declared Hilbert forms, define

```text
D^sharp = M_1^(-1) D^dagger M_0,

B_D = [[0, D],
       [D^sharp, 0]]
```

on

```text
H_K = C_0(K;L_rho) direct-sum C_1(K;L_rho).
```

In the identity Hilbert forms, the edge component of the adjoint is fixed:

```text
(D_(a,b)^sharp psi)_e
  = conjugate(a_e) U_e^dagger psi_t
    - conjugate(b_e) psi_s
  in L_s.
```

The dimensionless one-record normalization is

```text
norm(D_E e)^2_(M_0) / norm(e)^2_(M_1) = 2.
```

The dimensionful conversion to a physical generator remains downstream of
the independently derived causal record interval.

## Mandatory hostile counterfamilies

The uniqueness gate must include, at minimum:

```text
D_(a,b),e x = i_t(a U_e x) - i_s(b x);

D_x,e z
  = i_t(sqrt(2-x) U_e z) - i_s(sqrt(x) z),
0 < x < 2;

arbitrary positive-definite M_0 and M_1;

epsilon_(c,d),e(y)=c U_e y_s+d y_t for all nonzero (c,d);

independent edge and handle magnitudes;

and all common or relative complex phases not removed by allowed gauge or
unitary equivalence.
```

`D_x` satisfies the displayed norm for every `x`. It may be rejected only by
the preregistered closed-boundary and naturality conditions. If a continuum
survives those conditions, BID uniqueness fails.

## Proposed unique primitive operator

If the Hilbert, closed-boundary, and naturality gates all pass, the candidate
operator is

```text
partial_rho,e x = i_t(U_e x) - i_s(x),

B_rho = [[0, partial_rho],
         [partial_rho^dagger, 0]].
```

The theorem must establish this form from the complete competitor class.
Writing it down is not a proof.

Before one-record normalization, the physical quotient must contain exactly
one positive ray. After normalization, it must contain exactly one point.
Any inequivalent continuous or discrete family is a failure.

## Typed primitive/effective word filtration and degree-two geometry

For the primitive-addition audit, use the cellular chains through degree two,
the discrete unitary connection, and the ordinary complex Dirac spinor module

```text
S = C^4,
{gamma^mu,gamma^nu} = 2 eta^(mu nu) I,
eta = diag(+1,-1,-1,-1),
sigma^(mu nu) = (i/2)[gamma^mu,gamma^nu],
Tr_S(I) = 4.
```

The `3+1` spacetime dimension, Lorentz signature, spin structure, CPT
framework, and resulting four-component complex Dirac module are disclosed
ordinary-branch inputs. BID does not derive them, and the number four in
this spinor trace is not counted as target-blind evidence for alpha.

For the filtration audit, use the finite translation-complete test object

```text
K_L = the oriented 2-skeleton of (Z/LZ)^4,    L>=3,
```

with one Hermitian line at each vertex and arbitrary periodic unitary link
transports. `K_L` is a finite two-dimensional CW complex with four edge
directions and every elementary plaquette; the higher-dimensional cells of
the four-torus are not included. Let

```text
U_mu(x):L_x->L_(x+mu)
```

be its sealed periodic edge transport. On
`H_0(K_L)=direct-sum_x L_x`, define the global unitary shifts

```text
(T_mu psi)_x = U_mu(x)^dagger psi_(x+mu),
nabla_mu = T_mu - I
```

with periodic indices. They are everywhere-defined unitary endomorphisms with
globally defined inverses. On a nonperiodic finite complex the same formula
may be compressed to interior cells, but no free-group representation is
claimed for that compression. Because `C_1` stores an edge coefficient in
its source fiber, the identity-metric adjoint supplies the directional
vertex-to-edge difference without another identification. For the candidate
`a=b=1`,

```text
(D^sharp psi)_(e_mu(x)) = (nabla_mu psi)_x.
```

This equality is an exact consequence of the source-fiber convention, not a
separately normalized derivative.

For an oriented elementary two-cell `f_(mu nu)(x)`, freeze the boundary word

```text
partial_2 f_(mu nu)(x)
  = e_mu(x) + e_nu(x+mu) - e_mu(x+nu) - e_nu(x).
```

Its positive and negative boundary-path transports are

```text
U_+(x) = U_nu(x+mu) U_mu(x),
U_-(x) = U_mu(x+nu) U_nu(x).
```

The directional commutator is a map from the opposite-corner fiber back to
the base fiber:

```text
C_(mu nu)
  = [nabla_mu,nabla_nu]
  = T_mu T_nu - T_nu T_mu,

C_(mu nu) psi_(x+mu+nu)
  = [U_+(x)^dagger - U_-(x)^dagger]
    psi_(x+mu+nu).
```

The oriented base-point holonomy and its traceable curvature are then

```text
W_(mu nu)(x) = U_-(x)^(-1) U_+(x):L_x->L_x,

F_BR(mu,nu)(x)
  = -i Log_0 W_(mu nu)(x):L_x->L_x.
```

`Log_0` is the analytic logarithm in the identity neighborhood, with
eigenphase in `(-pi,pi)`, used for the local quadratic response at zero
field. Reversing the face orientation sends `F_BR(mu,nu)` to
`-F_BR(mu,nu)`. Under a vertex gauge transformation it is conjugated by
`h_x`; for `U(1)` it is invariant.

The filtration is defined first in a universal algebra, not inside a
target representation. Let

```text
P_univ = C[F_4]
```

be the complex group `*`-algebra of the free group on four formal unitary
comparison shifts `T_0,...,T_3`, with

```text
T_mu^* = T_mu^(-1).
```

The algebra contains no connection value or target coefficient. Its
augmentation homomorphism is

```text
varepsilon:P_univ->C,
varepsilon(T_mu)=varepsilon(T_mu^(-1))=1.
```

Define

```text
I_inc = ker(varepsilon),
delta_mu = T_mu-I in I_inc,
P_hat = inverse-limit_n P_univ/I_inc^n.
```

`P_hat` is the formal `I_inc`-adic completion. The four global unitaries on
`H_0(K_L)` define a genuine `*`-representation

```text
pi_U:P_univ->B(H_0(K_L)),
pi_U(delta_mu) = nabla_mu.
```

No continuity or extension of `pi_U` to all of `P_hat` is assumed. Formal
associated-graded statements are proved at finite order in `P_univ/I_inc^n`.
The formal `I_inc`-adic logarithm and the represented physical logarithm are
different constructions and may not be substituted for one another.

Scalar contractions, fixed cell projections, Clifford matrices, and the
identity have degree zero. The formal transport is `T_mu=I+delta_mu`, so it
belongs to `1+I_inc` and cannot be treated as a homogeneous order-zero
generator. Define

```text
F_(>=n) = I_inc^n,
F_(>=m) F_(>=n) subset F_(>=m+n).
```

Let the formal oriented plaquette word use the frozen path order:

```text
W_univ = T_nu T_mu T_nu^(-1) T_mu^(-1).
```

Because augmentation sends every shift to one,

```text
W_univ-I is in I_inc^2,
W_univ-I
  = delta_nu delta_mu-delta_mu delta_nu mod I_inc^3.
```

The formal series `Log(W_univ)` therefore lies in `I_inc^2` in `P_hat`.
Its universal degree-two symbol is the class in `I_inc^2/I_inc^3`. For the
physical representation define the decreasing represented filtration

```text
F_pi^n = pi_U(I_inc^n),
gr_pi^n = F_pi^n/F_pi^(n+1).
```

Equivalently,

```text
gr_pi^n
  ~= I_inc^n /
     [I_inc^(n+1)+(I_inc^n intersect ker(pi_U))].
```

The represented exact order is the least `n` for which the represented symbol
in `gr_pi^n` is nonzero. Membership in the universal ideal alone establishes
only a lower bound; a representation may kill the degree-two symbol.

The word order is frozen so that

```text
M_W,(mu nu) = direct-sum_x W_(mu nu)(x),
pi_U(W_univ) = M_W,(mu nu).
```

The opposite group commutator represents `M_W,(mu nu)^dagger`. Restricting
the global identity to the base-point fiber gives the local holonomy
`W_(mu nu)(x)`. After applying `pi_U`, the corresponding local pullback
identities are

```text
U_-^(-1) C_(mu nu)^dagger = W_(mu nu)-I,

C_(mu nu) U_- = W_(mu nu)^dagger-I.
```

The physical principal logarithm is defined by holomorphic functional
calculus on the finite normal operator `M_W,(mu nu)` when its spectrum avoids
`-1`, using the branch with eigenphases in `(-pi,pi)`:

```text
Log_pr M_W,(mu nu)
  = direct-sum_x Log_0 W_(mu nu)(x).
```

The Taylor series `Log[I+(M_W-I)]` may be used only on the stricter domain
`norm(M_W-I)<1`; it is not asserted to converge on the entire principal-log
domain. Therefore represented `M_W-I` and `Log_pr M_W`, whenever the latter
is defined, inherit the filtration lower bound through local holomorphic
functional calculus. Exact order two is reported only after a nonzero class
in `gr_pi^2` is computed. Flat branches and representations that kill that
class may begin at higher order or vanish.

Raise indices only with

```text
eta = diag(+1,-1,-1,-1),
F_BR^(mu nu) = eta^(mu rho) eta^(nu sigma) F_BR(rho,sigma).
```

The Lorentzian generated structures are

```text
slash_nabla = gamma^mu nabla_mu,

slash_nabla^2
  = nabla_mu nabla^mu
    - (i/2) sigma^(mu nu)[nabla_mu,nabla_nu],

P_Pauli,kin(F_BR)
  = -(1/2) sigma^(mu nu) F_BR(mu,nu)
    at leading associated-graded order,

P_Maxwell^L(F_BR)
  = -(1/4) sum_(mu,nu)
      Tr_internal[F_BR(mu,nu) F_BR^(mu nu)].
```

Here `Tr_internal(I)=1` for the primitive `U(1)` character. With
`W=exp(i F_BR)`,

```text
C_(mu nu) U_- = -i F_BR(mu,nu) + O(I_inc^4).
```

The displayed `-1/2` is therefore the signed leading coefficient under the
frozen face orientation and source-fiber derivative convention. Orientation
or unit-charge reversal flips the corresponding signed field convention; the
invariant kinematic magnitude is `1/2`. It is not an anomalous magnetic
moment and is not extracted from a scalar effective action.

The BID gate is Lorentzian and unitary. It performs no Wick rotation and
imports no Euclidean determinant. Any later Euclidean many-record completion
requires a separate preregistration and cannot retroactively alter this gate.
The counting two-cell measure is the one induced by `M_2=I` if Gate 3
passes. In the associated incidence filtration:

```text
the BID incidence difference has order at least 1;
the local curvature has order at least 2;
the kinematic Pauli term has order at least 2;
the quadratic Maxwell invariant has order at least 4.
```

An `exact` order label additionally requires the corresponding
associated-graded symbol to be nonzero. No exact-order claim follows from
ideal membership alone.

BID's **Single-Operator Completeness Postulate** says only the unique `F_1`
incidence law is independently primitive. Therefore independent `F_2`
coefficients are excluded by the BID postulate, not proved absent by
restating that postulate.

The theorem obligation is stronger and downstream: construct the generated
local curvature and the unitary record response from the sealed `F_1`
operator. If an additional primitive response coefficient is required,
Single-Operator Completeness fails as a physical hypothesis.

### Physical-amplitude, record-interval, and extensive-response subgate

V010 failed here. V011 therefore admits rather than suppresses the two
complete normalization competitors

```text
U_tau(A)=exp(-i tau B(A)),  tau>0,
Gamma_c(A)=-c log|A(A)|,    c>0.
```

Neither `tau=1` nor `c=1` follows from Fubini-Study geometry. A specification
that omits either family fails A23-A24.

V011 retains the following **Finite Pure-State Kinematics Premise** only as a
standard kinematic input:

```text
For every finite Gate-3 Hilbert carrier, pure preparations are rays,
transition probabilities obey the Born overlap, and infinitesimal ray
separation is the Fubini-Study metric

ds_FS^2=<d psi|d psi>-|<psi|d psi>|^2.
```

For a normalized physical amplitude `A(A)` with `A(0)=1`,

```text
-log|A(A)|=(1/2) ds_FS^2+O(A^3)
```

is a local identity. It checks a derived Hessian; it does not establish that
this Hessian is the complete electromagnetic action or fix an independent
power `A^c`.

#### Target-free record interval

The response uses the odd periodic sequence

```text
K_resp(L)=K_L,  L=2m+1,  m=1,2,...,
```

where `K_L` is the oriented two-skeleton of `(Z/LZ)^4` with periodic boundary
conditions and origin `0`. The baseline connection is the gauge class
`[U_0]` for which every plaquette holonomy and all four noncontractible
fundamental Wilson loops are the identity.

Choose `L_r=L_0`. Trivial holonomy gives the path-independent,
gauge-covariant embedding

```text
J_r,L:L_r->C_0(K_L;L),
(J_r,L z)_x=U_(0->x)z.
```

The preparation domain is, by first-opening semantics,

```text
P_root(L)=P(im J_r,L).
```

The unresolved alternative exists in the vertex/root sector before the
incidence operator generates edge support. Constant edge-direction rays are
therefore admitted as invariant states of `H_BID` but are not primitive
preparations. Gate A24 must test this domain restriction as a physical
consequence of first opening; merely observing that it makes the ray unique
is not a pass. If the restriction fails, preparation is nonunique.

After Gate 3 derives the metric, write the unique unit representative as

```text
r_L=J_r,L z/norm(J_r,L z).
```

Let

```text
B_L(A)=[[0,D_L(A)],[D_L(A)^dagger,0]],
U_L(A;tau)=exp(-i tau B_L(A)),
```

with `U_e(A)=exp(iA_e)U_0,e`. V011 adopts the **Durable-Record Interval
Principle**:

```text
tau_R(L) is the least positive tau for which the first-opening output
alternatives generated from P_root(L) satisfy the declared public
closure/orthogonality record criterion.
```

The criterion must be written as a finite projector/Gram-matrix equation
using only the first-opening carrier, the derived Hilbert metric, the
public-collapse map, and the allow/require record condition. It must be
gauge invariant and Lorentz-scalar in the continuum map. Pass requires:

```text
existence of tau_R(L);
uniqueness of the least positive solution;
nondegeneracy under every admitted preparation and branch automorphism;
a certified L->infinity limit tau_R;
and no alpha, endpoint, mass, or response value in the equation.
```

If no unique `tau_R` survives, absolute response normalization fails. The
phrase "one complete primitive transition" cannot replace this calculation.

The target-free closed-cell calculation is frozen in
`BID_FIRST_OPENING_INTERVAL_DERIVATION_V001.md`. Conditional on Gate 4
deriving unit incidence magnitude, the handle-conditioned operator has
spectrum `{-sqrt(2),0,+sqrt(2)}` and gives

```text
tau_R=pi/sqrt(2),
U_h(tau_R)|r>=|p_h>.
```

The three handle-conditioned outputs are mutually orthogonal. The same
derivation fixes the local causal-domain shape to
`J^+(p) intersect J^-(q)` and its flat-branch four-volume coefficient
`pi T_R^4/24`. This is an exact first-orthogonality result, not yet a
durability result; if complete `Q_spec` does not preserve or redundantly
encode the endpoint labels, A24 still fails.

The root survival amplitude is not the response amplitude. At `tau_R` it is
exactly zero and its logarithm is singular. The primitive completed-record
amplitude is instead derived, before response evaluation, in
`BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md`. For branch `h`,

```text
a_h(A) I_(L_r)
  = U_h^dagger P_(p_h) W_h(A;tau_R) i_r,
Z_h(A)=a_h(A)/a_h(0),
Z_h(0)=1.
```

The endpoint projector follows from the declared completed public alternative;
parallel transport returns the endpoint map to the root line, where it is one
canonical scalar. This selection is semantic, not a repair chosen because the
survival amplitude vanishes. The remaining connected theory must prove a
volume-uniform zero-free neighborhood for the derived amplitude.

#### Monoidal many-record response

The V010 normalized direct-sum global ray is a mandatory negative control.
For `n=(1,0,0,0)` and `v=e_1` it gives

```text
R_L(a,a)=L^-4,
||b_L||^2=4 sin^2(pi/L),
kappa_L=1/[4 L^4 sin^2(pi/L)] -> 0.
```

This route is terminated. Multiplying it afterward by `L^2`, `L^4`, a cell
count, or a cell volume is a fail.

V011 instead requires the record theory to be a symmetric monoidal theory on
disjoint causal cells. For disjoint cells `c`:

```text
H_(disjoint c)=tensor_c H_c,
r_(disjoint c)=tensor_c r_c,
U_(disjoint c)=tensor_c U_c,
A_(disjoint c)=product_c A_c,
Gamma_(disjoint c)=-log|A_(disjoint c)|=sum_c Gamma_c.
```

Thus the Hessian of `Gamma` is additive without inserting an extensive
factor. For a connected cellulation the complete amplitude must additionally
admit a linked-cluster expansion

```text
Gamma_K=sum_(connected clusters C subset K) gamma_C,
```

with a target-free locality bound that makes

```text
gamma_density=lim_(K exhausts R^4) Gamma_K/number_of_4_cells(K)
```

exist and makes boundary corrections subextensive. Exact tensor
factorization is required only for disjoint independent components; shared
boundaries are handled by the connected cluster terms rather than falsely
declared independent.

The exact disjoint proof and the connected-cluster closure conditions are
frozen in `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md`. The disjoint theorem
is proved; the connected thermodynamic limit remains a Gate-5 obligation.

For a connection tangent `a`, the local generated operator is

```text
V_L(a)=d/ds B_L(sa)|_(s=0),

G_L(a;tau_R)=integral_0^(tau_R)
  exp(iB_L(0)t)V_L(a)exp(-iB_L(0)t) dt.
```

The primitive record susceptibility is the intensive Hessian

```text
R_record,L(a,b)
  =d^2/ds dt [Gamma_K(sa+tb)/N_4(K)]|_(s=t=0),
```

evaluated after the thermodynamic/linked-cluster limit. The Duhamel covariance
computed from `G_L` must reproduce this Hessian. Fubini-Study is a consistency
check on each normalized local output ray, not an independently selectable
action multiplier.

At this stage the output is named only `kappa_record`. The complete charged
amplitude later fixes `c=1` by

```text
Gamma_Q[A]=-i Log(Z_Q[A]/Z_Q[0])
```

with the branch fixed continuously at `A=0`. Replacing the computed
amplitude by `(Z_Q/Z_Q[0])^c` changes the physical amplitude and is not an
equivalence. Until `Z_Q` is constructed from complete `Q_spec`, the
conversion from `kappa_record` to a physical action coefficient remains
unauthorized.

#### Tangent complex and unique flux lift

Connection tangents, chain states, and face records are distinct. Define

```text
A_R^0(K_L)=R^(K_0),
A_R^1(K_L)=R^(K_1^+),
A_R^2(K_L)=R^(K_2^+),

(d_0 lambda)_e=lambda_t-lambda_s,
(d_1 a)_f=sum_(e in boundary f) incidence(f,e)a_e,
d_1 d_0=0.
```

The unit action character and Gate-3 metric give the counting products. For
components `K^(j)`,

```text
ker(d_0)=span{constant function on K^(j)}_j,
P_h=I-d_0(d_0^dagger d_0)^+d_0^dagger,
F_phys=im(d_1),
Q_flux=P_h d_1^dagger(d_1 P_h d_1^dagger)^+|_(F_phys).
```

For every `xi in F_phys`, `Q_flux xi` is the unique horizontal,
minimum-norm lift orthogonal to
`ker(d_1) intersect im(d_0)^perp`. It obeys
`d_1 Q_flux xi=xi`. Individual unit faces outside `F_phys` are never assigned
a lift. Complexification is allowed only for Fourier analysis and must return
the same real bilinear form from the paired sine/cosine modes.

The generated face response is

```text
mathcal_K_L(xi,zeta)
  = R_record,L(Q_flux xi,Q_flux zeta),  xi,zeta in F_phys.
```

#### Tetrad-derived scale and anisotropy map

The record interval `tau_R` is derived above; it is not discarded merely
because a four-dimensional gauge coefficient is dimensionless. Spatial and
temporal cell scales may cancel only after the exact geometric map is shown.

For an orthogonal physical cell with edge lengths `ell_mu`, volume

```text
V_cell=product_mu ell_mu,
```

and face flux

```text
xi_(mu nu)=ell_mu ell_nu F_(mu nu)+higher-order terms,
```

the tetrad/Hodge-induced positive face metric in the coordinate-flux basis is

```text
<xi,xi>_(2,ell)
  =sum_(mu<nu) [V_cell/(ell_mu^2 ell_nu^2)] xi_(mu nu)^2.
```

Therefore each cell contributes

```text
V_cell sum_(mu<nu) F_(mu nu)^2
```

in the local orthonormal frame. For a general coframe `theta^a=e^a_mu dx^mu`,
the same map is defined by pulling the bivector through
`wedge^2(e^(-1))` and multiplying by `|det e|`. The diagonal formula above
is a mandatory exact check.

Gate 3's identity face metric is the counting metric in the orthonormalized
face basis. The coordinate-flux metric is its pullback by the displayed
tetrad map; it is not a new adjustable face weight. The inverse weight
`ell_mu^2 ell_nu^2/V_cell`, an inserted compensator, or any residual
shape-dependent scalar fails A27.

The local coefficient must also be cellulation independent. Freeze an
admissible class `C_ref` of oriented, shape-regular periodic regular-CW
cellulations with:

```text
cubical bisection;
oriented simplicial/barycentric subdivision;
and common refinements preserving the same smooth coframe and connection.
```

The response map must commute with pullback to a common refinement, and the
intensive quadratic coefficient must be invariant under each elementary
refinement up to a boundary term whose ratio to four-volume tends to zero.
One hypercubic sequence is a regression fixture, not proof of universality.
Failure to prove this naturality either blocks the general claim or restricts
the result explicitly to that cellulation.

#### Frozen local tensor extraction

The local Maxwell tensor may not be guessed from a global covariance matrix.
First prove that `[U_0]` and `r_L` make `mathcal_K_L` translation invariant.
The authoritative finite calculation is real. Let `N=L^4`. For every
nonzero sign-representative momentum `n` on odd `L`, define the exactly
normalized scalar modes

```text
phi_c,n(x)=sqrt(2/N) cos(2 pi n dot x/L),
phi_s,n(x)=sqrt(2/N) sin(2 pi n dot x/L).
```

For each raw polarization `v`, put `v_hat=v/norm(v)` and define

```text
a_c,(n,v),mu(x)=v_hat_mu phi_c,n(x),
a_s,(n,v),mu(x)=v_hat_mu phi_s,n(x),

xi_c=d_1 P_h a_c,
xi_s=d_1 P_h a_s.
```

Both scalar modes and every polarization have norm one. The paired responses

```text
y_c=mathcal_K_L(xi_c,xi_c),
y_s=mathcal_K_L(xi_s,xi_s)
```

must agree exactly or by a preregistered certified enclosure. Complex
exponentials may be used only in an independent reconstruction of the same
real pair.

For design bookkeeping define the raw discrete symbol using the unnormalized
`v`:

```text
b_L(n,v)_(mu nu)
  =[exp(2 pi i n_mu/L)-1]v_nu
   -[exp(2 pi i n_nu/L)-1]v_mu,

y_raw(n,v)=norm(v)^2 (y_c+y_s)/2.
```

The finite Hermitian design row has diagonal entries `|b_i|^2` and
off-diagonal entries `2 Re(conjugate(b_i)b_j)`. Every factor of `2`, `N`,
and `norm(v)` is part of the sealed specification.

Candidate momenta are all
`n in {-1,0,1}^4\{0}`, identifying `n~-n` by requiring the first nonzero
component to be positive, ordered by `(norm(n)^2,n)` lexicographically.
Candidate polarizations, in order, are `e_j` and then `e_j+e_k` for `j<k`;
zero bivectors are skipped. In the ordered local bivector basis

```text
(01,02,03,23,31,12),
```

form the rational continuum design row from
`b_infinity(n,v)=n wedge v`:

```text
d(n,v)=vech_sym[b_infinity b_infinity^T],
```

with off-diagonal entries doubled. Every decomposable exact mode obeys the
Pluecker identity

```text
b_infinity wedge b_infinity=0,
```

so these rows cannot detect the one parity-odd topological quadratic
`F wedge F`, proportional to `E dot B`. The complete candidate set must be
proved to have exact rank 20 with a one-dimensional nullspace spanned by that
topological form.

In the ordered basis `(01,02,03,23,31,12)`, define the symmetric tensor
`T_top` by

```text
(T_top)_(0,3)=(T_top)_(3,0)=1/2,
(T_top)_(1,4)=(T_top)_(4,1)=1/2,
(T_top)_(2,5)=(T_top)_(5,2)=1/2,
all other entries=0.
```

Then

```text
b^T T_top b=b_01 b_23+b_02 b_31+b_03 b_12,
||T_top||_F^2=3/2.
```

The canonical lift from the rank-20 quotient is the unique representative
satisfying

```text
<M_L,T_top>_F=0.
```

This is the ordinary parity/CPT-even section. It is frozen before response
evaluation; it replaces the ambiguous instruction to "insert a zero
coefficient."

Starting from the empty set, retain a continuum row iff it increases exact
rational rank, and stop at rank 20. Every remaining row is a frozen
regression row. The same normalized real-mode rows must retain rank 20 at
finite odd `L`.

For each odd `L`, solve the fixed 20-equation Hermitian system for the
rank-20 class, then impose the exact Frobenius section to obtain `M_L`.
Verification-row residuals divided by `norm(b_L)^2` must tend to zero. Pass
requires an analytic limit or certified convergent enclosures

```text
M_record=lim_(odd L->infinity) M_L
```

that are independent of deleting any one verification row and invariant
under the sealed refinement class.

Finite rows do not prove locality. The reconstructed symbol must satisfy,
uniformly for all directions in a full neighborhood `0<|p|<p_0`,

```text
K_hat(p)=Q_(M_record)(p)+Rem(p),
||Rem(p)|| <= C |p|^(2+delta)
```

for sealed `p_0,C,delta>0`, uniformly along the refinement sequence.
An exact finite-range analytic-symbol theorem may replace this bound. A
finite set of rays, direction-dependent limit, nonanalyticity, divergence,
or path dependence fails locality and cannot be relabeled as another scale.
This stage yields a primitive record tensor, not yet the Thomson response.

#### Lorentzian Hodge and Maxwell test

Freeze the ordinary-branch inputs

```text
eta=diag(+1,-1,-1,-1),  epsilon_0123=+1,
(*F)_(mu nu)=(1/2)epsilon_(mu nu rho sigma)F^(rho sigma).
```

The resulting matrix `J_star` in the ordered bivector basis is generated from
that formula and must satisfy `J_star^2=-I_6`. It acts on the reconstructed
local six-dimensional tensor space; no claim is made that it preserves the
entire finite global subspace `F_phys`.

Rotation and parity/CPT tests must first reduce `M_record` to

```text
H_trial=(kappa_record,E/2)|E|^2+(kappa_record,B/2)|B|^2
```

with vanishing cross and anisotropy blocks. They do not equate the two
coefficients. Only the predeclared exact test

```text
[M_record,J_star]=0
```

may yield
`kappa_record,E=kappa_record,B=kappa_record>=0`. If any equality is only numerical,
it requires a certified enclosure excluding every nonzero value; an
after-the-fact tolerance is forbidden. The positive record susceptibility is
compared only with this positive tensor, never directly with the indefinite
Lorentz bivector contraction. Passing this test establishes Maxwell-form
kinematics for the primitive response; it does not yet identify
`kappa_record` with the physical electromagnetic action coefficient.

#### Downstream complete charged specification and Thomson map

The primitive gate ends with `kappa_record`. It does not output
`kappa_Thomson`, `e`, or alpha.

A distinct sealed object `Q_spec` must define the complete physical charged
transition amplitude. It must contain, with no measured alpha:

```text
the charged boundary carrier and unit action-character current;
all charged source sectors and their statistics;
the gauge, constraint, ghost if required, and public edge sectors;
the preparation and durable-record projectors;
the microscopic measure and regulator;
the regulator-removal and locality theorem;
the Ward identity and transverse physical quotient;
the derived charged spectrum and every threshold entering the response;
decoupling and matching rules;
and the zero-momentum Thomson prescription.
```

Measured masses or threshold locations may be used for a phenomenological
consistency calculation, but not for a first-principles alpha claim. For the
end-to-end claim sought here, every load-bearing threshold must be derived
from the same sealed theory or proved irrelevant to the final coefficient.

The normalized physical amplitude is

```text
Z_Q[A]/Z_Q[0],
Gamma_Q[A]=-i Log(Z_Q[A]/Z_Q[0]),
```

with the logarithm branch fixed continuously at `A=0`. The complete
transverse quadratic response is defined by

```text
Gamma_Q^(2)[A]
 = (1/2) integral d^4q/(2 pi)^4
   A_mu(-q) (q^2 eta^(mu nu)-q^mu q^nu)
   kappa_Q(q^2) A_nu(q).
```

Pass requires the Ward identity, gauge-parameter independence on the
physical quotient, regulator independence, threshold matching, and a finite
path-independent limit

```text
kappa_Thomson=lim_(q^2->0) kappa_Q(q^2).
```

The old family `Gamma_c=-c log|A|` is resolved only here: `Gamma_Q` is
computed from the physical amplitude itself. Replacing `Z_Q/Z_Q[0]` by its
`c`th power changes that amplitude and describes `c` copies or a different
theory. If a multiplicity remains, it must be derived from `Q_spec`; it may
not be selected by the desired coupling.

The primitive `kappa_record` may constrain or appear inside this
construction, but equality

```text
kappa_record=kappa_Thomson
```

is not assumed. It must follow from the complete amplitude or fail.
Determinants, heat kernels, Wick rotations, regulators, and counterterms are
forbidden as primitive selectors but may appear here only when derived by
`Q_spec`. Regulator removal must leave no independently adjustable finite
`F^2` counterterm; otherwise the absolute coupling is not predicted.

Only after `kappa_Thomson` is derived may the unit-character convention be
applied:

```text
S_EM=-(kappa_Thomson/4) integral d^4x F_(mu nu)F^(mu nu),
kappa_Thomson=1/e^2,
alpha(0)=e^2/(4 pi)=1/(4 pi kappa_Thomson).
```

The canonical phase-space/Legendre construction is then an independent
consistency check on the resulting Lorentzian action. It cannot supply a
missing normalization.

## Exact gates

### Gate 1: joint comparison group

Verify that `rho_joint` is well-defined, strongly continuous, and faithful
on `G_joint`, and prove that finite `V4` and `Z4` models are noncontinuous
coarse-grainings, finite readouts, or, for `Z4`, a subgroup sample rather
than exact faithful continuous replacements.

### Gate 2: first-opening topology accounting

Verify that the explicit first-opening premise gives the rooted star and
the `4+3=7` graded dimension. Record this only as a premise consequence.

### Gate 3: Hilbert functor classification

Start from general positive-definite forms and classify those satisfying the
six Elementary Record Hilbertization conditions through degrees zero, one,
and two. Pass requires one unitary equivalence class and must explicitly
derive `M_0=M_1=M_2=I`; merely restating the isometry hypothesis is not a
completed gate.

### Gate 4: differential uniqueness

Start from `D_(a,b)` and all mandatory hostile counterfamilies. Apply only:

```text
orientation-choice invariance with an independent reversed coefficient pair;
the full epsilon_(c,d) hostile augmentation family;
the operational public-closure axioms and colimit/cocone universal property;
local closed-boundary accounting;
differential naturality;
character covariance;
orientation reversal;
and one-record normalization.
```

Pass requires exactly one normalized differential equivalence class and one
public-collapse covector ray. The audit must derive, conditional on the
declared operational principle, the orientation extension
`(a_bar,b_bar)`, the natural orientation-coherent collapse ray, equal
endpoint ratio, and the surviving differential phase/normalization class.
No absolute magnitude is assigned to the constraint covector.

### Gate 5: primitive/effective classification

Classify the enlarged typed competitors by the frozen incidence-word
filtration and the displayed Pauli/Maxwell projections.
Report separately:

```text
excluded from primitive status by the BID postulate;
mathematically incompatible with the unique F_1 operator;
and generated downstream with a computed coefficient.
```

No "excluded by theorem" flag may be produced merely from the completeness
postulate. The response subgate must run before any quadratic response is
reported. Pass requires:

```text
the base-point curvature to be generated from the sealed F_1 operator;
the augmentation-ideal order claims to be proved algebraically;
the Clifford square to return the frozen signed leading coefficient
K_Pauli,kin=-1/2, with invariant magnitude 1/2;
the complete tau>0 interval family to be admitted and the least positive
durable-record interval to be derived without target access;
the root-generated preparation domain to follow from first-opening semantics
and every translation-invariant competitor to be enumerated;
the V010 direct-sum response to reproduce its analytic zero-stiffness failure;
the many-record amplitude to be monoidal on disjoint cells and to possess a
local linked-cluster thermodynamic density on connected cellulations;
the complete c>0 action-multiplier family to remain visible until Q_spec;
the Fubini-Study pullback to check, but not select, the derived local Hessian;
the no-extra-flux lift to be unique on `F_phys=im(d_1)` and representative
independent;
the tetrad-induced anisotropic face measure and refinement naturality to be
proved with no shape-dependent scalar;
the normalized real sine/cosine Fourier map, exact Hermitian rows, rank-20
quotient, explicit T_top, and Frobenius-orthogonal section to reconstruct one
local tensor;
the full-neighborhood analytic locality bound and cellulation-independent
thermodynamic limit to pass;
the predeclared Hodge commutator, rotation, parity/CPT, and anisotropy tests
to return a primitive Maxwell-form record tensor;
and the core output to contain only kappa_record, with every physical
Thomson, e, alpha, and proof flag false.
```

This gate authorizes only the primitive record susceptibility. It does not
authorize a determinant-derived coefficient, a physical electromagnetic
action coefficient, or alpha.

### Gate 6: fail-closed evaluator and downstream completion

The dependency graph is computed by a separate evaluator. Manuscript status
strings and a script's own `PASS` label have no authority. Every report uses
a fixed schema containing:

```text
schema version;
immutable bundle hash;
stage and reviewer role;
independent process/reviewer identifier;
verdict and complete blocker list;
full-matrix attestation;
no-edit attestation;
artifact hashes;
and creation timestamp.
```

Three distinct roles and process identifiers are mandatory at every review
stage. The evaluator verifies every hash and computes this acyclic graph:

```text
SPEC-SEAL
  requires immutable V011 specification artifacts, passed A01-A29 and A35,
  and three unanimous content-addressed full-stack specification reports.

CORE-RESULT-SEAL
  requires SPEC-SEAL, executed Gates 1-5, the composition-loop result,
  three new unanimous core reports, and an independent core reconstruction.
  Its strongest numerical output is kappa_record.

PARENT-COMPARISON
  requires CORE-RESULT-SEAL and a predeclared comparison map.

HOLDOUT-UNIVERSE-SEAL
  requires the exhaustive registry snapshot, eligibility implementation,
  independent salted outcome commitments, and external-beacon rule.

QSPEC-SPEC-SEAL
  requires a complete target-free Q_spec and three unanimous reviews.

PREDICTION-MAP-SEAL
  requires HOLDOUT-UNIVERSE-SEAL and immutable code mapping future sealed
  theory output to every eligible observable before any alpha value exists.

THOMSON-RESULT-SEAL
  requires CORE-RESULT-SEAL, QSPEC-SPEC-SEAL, executed complete charged
  amplitude, Ward/regulator/threshold tests, and independent reconstruction.

ALPHA-RESULT-SEAL
  requires THOMSON-RESULT-SEAL, PARENT-COMPARISON,
  HOLDOUT-UNIVERSE-SEAL, and PREDICTION-MAP-SEAL.

HOLDOUT-RESULT-SEAL
  requires ALPHA-RESULT-SEAL, the future randomness beacon, a sealed
  prediction value, custodian unmasking, and passed commitment/contamination
  checks.

FINAL-CLAIM-SEAL
  requires HOLDOUT-RESULT-SEAL, A33 independent end-to-end reconstruction,
  and three new unanimous final-claim reports.

proof_authorized if and only if FINAL-CLAIM-SEAL is true.
```

Any failed or malformed dependency makes every descendant false. No stage
may inherit a review from an earlier stage. After
`CORE-RESULT-SEAL`, a separate process may open the pinned historical parent.
Agreement supplies provenance; disagreement corrects the parent. No BID datum
may change. The comparison receives
`BID_parent_comparison_completed`; it is not represented by either the
specification or core-result seal.

## Fully specified composition-loop prediction

This is a new forward mathematical consequence, not an empirical holdout.

Use the unfilled oriented `1`-skeleton `K_square` with no `2`-cell and:

```text
vertices:
  v_00, v_10, v_01, v_11

edges:
  e_a0: v_00 -> v_10
  e_0b: v_00 -> v_01
  e_ab: v_10 -> v_11
  e_ba: v_01 -> v_11
```

The two forward paths are:

```text
v_00 --e_a0--> v_10 --e_ab--> v_11
v_00 --e_0b--> v_01 --e_ba--> v_11.
```

Let the ordered loop holonomy be

```text
u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi).
```

Use the fixed gauge:

```text
u_a0 = 1,
u_ab = 1,
u_0b = 1,
u_ba = exp(-i Phi).
```

For every edge,

```text
partial_rho,e x = u_e x v_target - x v_source
```

Use the Hilbert forms produced by Gate 3. No filled `2`-cell is included in
the trace carrier. Define

```text
B_square(Phi)
  = [[0, partial_rho],
     [partial_rho^dagger, 0]]
```

on the full eight-dimensional space

```text
C_0(K_square) direct-sum C_1(K_square).
```

Freeze the ordered bases:

```text
C_0: (v_00, v_10, v_01, v_11),
C_1: (e_a0, e_0b, e_ab, e_ba).
```

In those bases the exact incidence matrix is:

```text
D_square(Phi) =
[[-1, -1,  0,              0],
 [ 1,  0, -1,              0],
 [ 0,  1,  0,             -1],
 [ 0,  0,  1,    exp(-i Phi)]].
```

The exact block matrix and characteristic-polynomial convention are:

```text
B_square(Phi) =
  [[0_(4x4), D_square(Phi)],
   [D_square(Phi)^dagger, 0_(4x4)]],

p_Phi(z) = det(z I_8 - B_square(Phi)).
```

The preregistered evaluation point is:

```text
Phi = pi.
```

The result must report:

```text
the exact characteristic polynomial;
the eight real eigenvalues in nondecreasing order with multiplicity;
the exact kernel multiplicity;
Tr[B_square^2] over the full eight-dimensional carrier;
Tr[B_square^4] over the full eight-dimensional carrier;
and R_square = Tr[B_square^4] / Tr[B_square^2]^2.
```

Zero means an exact algebraic zero. The symbolic result is authoritative; a
separately coded 100-decimal numerical diagonalization is only a check.

No result may modify BID. This output was not found under its exact name in
the historical lineage before declaration. It remains target-aware because
the broader program knows its desired structures.

## External holdout requirement

Historical target awareness cannot be erased. A32 therefore uses an external
registry, a future randomness beacon, and an outcome-masked custodian rather
than an author's claim of retrospective blindness.

Before `SPEC-SEAL`, the protocol must freeze the following A32 holdout
fields. The freeze values in this subsection were ratified by the principal on
2026-07-28 in `A32_FREEZE_V002_RATIFIED_2026-07-28.md`
(`32dbfc33b4f07407903ec014627ea64de57b5b1a6dc017dd27c6504729c3a327`),
with the thirteen mechanical items ratified as written in
`A32_FREEZE_DRAFT_V000_2026-07-28.md`
(`13faf0bc9a455590bd99d1a40587d798bc558e87aa1d1bc6dcf6778731138123`):

```text
Registry / observable family:
  NIST CODATA recommended-values table, 2022 adjustment, as one fixed-vintage
  machine-readable artifact. The collector retrieves exactly one of:
    physics.nist.gov/cuu/Constants/Table/allascii.txt
  or a versioned canonical JSON form only if the collector verifies that JSON as
  the canonical NIST artifact. The collector retrieves once and hashes the full
  raw bytes. T_cutoff is the 2022 adjustment vintage.

Eligibility exclusions:
  LINEAGE-ONLY exclusions: the alpha datum and deterministic re-expressions of
  the alpha datum, meaning values computed from the alpha measurement rather
  than independently measured. There is no correlation ceiling, no proximity
  exclusion, and no human carve-out after registry collection. The observable
  family is every machine-enumerated candidate satisfying the sealed eligibility
  conditions and computability under the frozen prediction-map interface. An
  empty eligible set fails closed.

Mechanical collector / commitment fields:
  1. Query and pagination: full endpoint path, query parameters, page-size rule,
     sort rule, retry rule, rate-limit behaviour, termination condition, failure
     behaviour; no manual addition or removal.
  2. Cutoff rule: one frozen cutoff, here the 2022 adjustment vintage; include
     records within that vintage, and machine-exclude records whose timestamp or
     version is unparseable when a timestamp/version field is supplied.
  3. Response schema: registry record ID, observable definition text, value or
     masked-outcome handle, units, uncertainty/covariance metadata,
     kinematic/domain metadata, source identifier, timestamp/version, provenance
     URL.
  4. Canonical field mapping: registry fields map to typed canonical JSON keys;
     a missing required key is machine-excluded, never human-repaired.
  5. Canonical JSON: RFC-8785, UTF-8, sorted keys, normalized number/string
     handling.
  6. Canonical observable ID:
     canonical_id = SHA256(RFC8785(canonical_source_record)), where the record
     includes registry source ID, record ID, observable definition, units,
     domain, and source identifier, and not the observed value.
  7. Deterministic deduplication: cluster key = normalized source identifier
     plus normalized observable definition plus units plus domain. Keep the
     lexicographically lowest canonical_id; emit all suppressed IDs with
     reasons.
  8. Collector output: raw page bytes, SHA256(raw_page_bytes) per page,
     canonical candidate JSONL, exclusion JSONL, duplicate report, collector
     version hash, collection transcript.
  9. Custodian commitments: independent custodian, one 256-bit salt per
     candidate from auditable external randomness or hardware entropy. Payload =
     RFC-8785 JSON of canonical outcome value, uncertainty, covariance, units,
     source identifier. Commitment =
     SHA256(salt || RFC8785(canonical_outcome_payload)). Custodian attests that
     no payload was disclosed.
  10. Selection rule: after PREDICTION-MAP-SEAL, with B the canonical beacon
      value and H_spec the final spec hash, sort eligible IDs by
      SHA256(H_spec || B || UTF8(canonical_id)) and select the lowest.
  11. Contamination audit: search all accessible repository history,
      attachments, logs, prompts, tickets, browser-history exports where
      available, and lane artifacts for the selected canonical_id, source
      identifier, observable definition, and outcome payload before unmasking.
      Any prior outcome access fails A32.
  12. Prediction-map interface: immutable
      (sealed_theory_output_record, canonical_candidate_record) ->
      prediction_payload, carrying predicted value, units, theory uncertainty,
      covariance if vector-valued, propagation trace, comparator outputs,
      structure-sensitivity statistic, and pass/fail statistic template. Values
      wait downstream; the interface freezes before evaluation.
  13. Uncertainty treatment: theory uncertainty from sealed prediction payloads
      only; comparator uncertainty from preregistered comparator payloads only;
      measurement uncertainty/covariance from the custodian commitment payload,
      uneditable after commitment. Vector observables use Mahalanobis distance
      with combined covariance; singular covariance fails eligibility unless a
      projection rule was frozen beforehand.

External randomness beacon:
  Primary is drand League of Entropy mainnet. The round number is computed
  deterministically from the frozen genesis time and period as the first round
  strictly after PREDICTION-MAP-SEAL; the value is that round's randomness hex,
  parsed verbatim. Fallback, with no discretion, is the NIST Randomness Beacon
  v2 pulse at the first whole UTC minute after the same seal instant if the
  drand round is unretrievable from all frozen mirrors for 24 hours. If both
  are unavailable for 72 hours, A32 fails closed. The collector records exact
  chain hash, genesis, period, and mirror list at freeze execution.

Same-alpha comparator class and calibration:
  Comparators are published standard-theory QED/SM expressions cited before
  candidate-universe construction. They are evaluated with the identical alpha
  input under the frozen convention
  alpha(0) = 1/(4 pi kappa_Thomson), with perturbative order frozen per formula
  at preregistration. No candidate-specific fitted parameter, refit, channel
  coefficient, or post-selection is allowed; BID and comparator use identical
  candidate metadata. A candidate with no published standard expression is
  ineligible for that candidate.

Distinctness threshold:
  D >= 5, with
  D = |mu_BID - mu_comp| / sqrt(sigma_BID^2 + sigma_comp^2 + sigma_meas^2)
  after unit conversion and covariance projection. Singular covariance fails
  eligibility unless a projection rule was frozen beforehand. D is never
  renegotiated after any set size, prediction, or value is seen. An empty
  eligible set fails closed.
```

The registry collector must exhaust every page and publish the raw response
hashes plus a machine-generated inclusion/exclusion report. A hand-curated
candidate list fails A32. This A32 freeze makes the freeze-field component
attainable; it does not claim `SPEC-SEAL`, `HOLDOUT-UNIVERSE-SEAL`,
`PREDICTION-MAP-SEAL`, `ALPHA-RESULT-SEAL`, or any ladder-grade flag.

For every machine-enumerated candidate, an independent custodian publishes a
content-addressed manifest containing:

```text
a canonical observable identifier and definition;
units, kinematic domain, and public source identifier;
eligibility metadata needed to construct the prediction;
a SHA256(salt || canonical_outcome) commitment to the observed value,
uncertainty, and covariance, using an independently generated 256-bit salt;
and an attestation that no outcome value was disclosed to the derivation
team.
```

An eligible candidate must:

```text
be absent as an outcome or target from the complete historical lineage;
be computable from the sealed BID output with no new fitted parameter,
channel-specific coefficient, or post-selection;
possess a public independently measured value and uncertainty;
not share the alpha datum or a deterministic re-expression of it;
and be structure-sensitive: BID and at least one preregistered comparator
calibrated to the same alpha must make distinct predictions.
```

Before alpha is evaluated, immutable code must produce a prediction for every
eligible canonical ID from a future theory-output record. Only then may alpha
be computed. Target selection uses the first public value emitted after
`PREDICTION-MAP-SEAL` by the predeclared external randomness beacon. Let
`H_spec` be the hash of the final V011 specification bundle and `B` the
canonical beacon value. Sort eligible IDs by

```text
SHA256(H_spec || B || UTF8(canonical_id))
```

and select the lowest. No human choice among candidates is allowed. The
selected numerical prediction, units, uncertainty propagation, and
pass/fail statistic are sealed while the observed outcome remains masked.
Only then may the custodian reveal the selected outcome and salt.

A contamination audit must search the full accessible lineage and access logs
for the selected outcome before unmasking. Any prior access, an empty eligible
set, a changed universe, a failed commitment, beacon substitution, duplicate
ambiguity, a comparator with identical predictions, or a prediction-map edit
after unmasking returns `A32_FAIL`. The internal composition-loop prediction
is not an empirical holdout and cannot substitute for A32.

## Stop rules

BID remains open or fails if:

```text
the authority hashes drift;
the joint group or first-opening premise is silently changed;
first-opening status or its handle label is not part of the categorical
object and morphism data;
a non-counting Hilbert form survives;
an elementary degree-two cell lacks a canonical test inclusion;
the face connection is silently replaced by a flat local system;
the augmentation family is restricted before the full `(c,d)` hostile test;
equal endpoint ratio is asserted without the disclosed operational closure
principle, colimit-ray derivation, naturality, and orientation coherence;
an absolute magnitude is attributed to the public-collapse constraint ray;
orientation reversal leaves an unfrozen coefficient map;
the D_x continuum survives;
full B intertwining is substituted for differential naturality;
an independent primitive magnitude survives;
an F^2 or Pauli term is declared theorem-excluded only because completeness
postulated it away;
a nominally order-zero transport collapses the universal augmentation-ideal
order, or exact order is claimed without a nonzero associated-graded symbol;
a covariance rule is cited without the explicit projective-orbit pullback;
a response complex, trivial-holonomy background, root embedding, generator,
record interval, or preparation domain is chosen after a response is seen;
the first-opening semantics do not uniquely justify the root-generated
preparation domain;
the least positive durable-record interval fails to exist, is degenerate, or
uses a target;
the direct-sum global-ray response is revived, or its zero stiffness is
multiplied afterward by an extensive factor;
disjoint independent record amplitudes fail monoidal composition, or the
connected theory lacks an extensive linked-cluster density;
the c>0 amplitude-power family is suppressed before complete Q_spec fixes the
physical amplitude;
a nonunique zero-flux tangent can be added to the face lift;
the tetrad-derived anisotropic face measure fails, an inverse/ad hoc weight
is inserted, or refinement naturality fails;
the finite real sine/cosine modes, polarization norms, Hermitian rows, or
volume factors are not exact;
the frozen rank-20 extraction has a nullspace larger than the proved
topological direction, lacks the explicit T_top Frobenius section, lacks a
certified odd-L limit, or fails a preregistered verification row;
locality is inferred only from finitely many rays rather than a uniform
full-neighborhood analytic bound;
the positive face response fails the source-free Maxwell energy test;
the local Hodge matrix fails J_star^2=-I, the response fails the predeclared
Hodge commutator, or electric/magnetic equality is imposed afterward;
the positive record response is equated directly with an indefinite
Lorentzian metric or with the physical Thomson coefficient;
a CAR determinant, heat kernel, Wick rotation, finite counterterm, or
Euclidean generator enters the primitive unitary-response gate;
complete Q_spec omits a charged source/statistics/gauge/edge/measure,
Ward-identity, regulator, spectrum, threshold, matching, or Thomson-limit
obligation;
measured masses or thresholds enter a first-principles alpha result;
an independent finite F^2 counterterm survives regulator removal;
the equality kappa_record=kappa_Thomson is assumed rather than derived;
the unresolved root state, tangent map, gauge quotient, face-flux
normalization, Fourier design, or action convention is supplied after seeing
a response value;
an audit combines reports from different content hashes, trusts a
self-declared status, lacks the independent report schema/evaluator, or uses
an unqualified seal status;
an executed core or final claim lacks its required content-addressed formal,
physics/operator, red-team, or independent-reconstruction report;
the historical parent is opened before CORE-RESULT-SEAL;
the loop complex, gauge, phase, trace carrier, or output list changes;
the holdout source/query/cutoff is unset, the candidate universe is manually
curated, canonical serialization/deduplication is ambiguous, the future
beacon is substituted, or the selected observable is not structure-sensitive;
the external holdout candidate universe, selector, prediction map, or
contamination audit is changed after an outcome is seen;
or alpha is evaluated before THOMSON-RESULT-SEAL, PARENT-COMPARISON,
HOLDOUT-UNIVERSE-SEAL, and PREDICTION-MAP-SEAL.
```

No residual patch or target comparison may repair a failed gate.

## Scope

If Gates 1-5 pass, BID fixes the primitive incidence form and computes an
extensive dimensionless record susceptibility `kappa_record` within the
declared ordinary first-opening branch. That is not alpha. An end-to-end
alpha result additionally requires the complete `Q_spec` amplitude, derived
thresholds, Ward-consistent Thomson limit, fixed action normalization,
outcome-blind structure-sensitive holdout, and independent reconstruction.
This working specification is not that completed result: all computation and
claim flags remain false.

The completed branch would not by itself determine:

```text
why charged access exists;
the full physical charged spectrum unless Q_spec derives it;
the complete durable environment;
or the full matter spectrum.
```

## Authorization state

```text
BID_v011_forward_repair_in_progress = true
BID_v011_specification_sealed = false
historical_target_blindness_established = false
V010_immutable_failure_bundle_frozen = true
V010_direct_sum_response_terminated = true
V010_zero_stiffness_regression_frozen = true
V010_known_blockers_encoded_as_V011_requirements = true
common_total_action_character_premise_declared = true
comparison_gauge_ray_U1_roles_related_but_not_identified = true
first_opening_star_premise_declared = true
seven_dimensional_carrier_counts_as_new_evidence = false
first_opening_subset_and_label_map_typed = true
bare_open_decorated_forgetful_functors_typed = true
general_a_b_incidence_family_frozen = true
general_positive_Hilbert_forms_frozen_as_competitors = true
D_x_counterfamily_mandatory = true
differential_naturality_frozen = true
full_B_intertwining_assumed = false
Hilbert_elementary_cell_isometry_hypothesis_declared = true
Hilbert_degree_two_included = true
nonflat_face_connection_admitted = true
endpoint_chain_tangent_face_maps_frozen = true
periodic_translation_complete_test_object_frozen = true
universal_path_star_algebra_and_I_adic_completion_frozen = true
exact_filtration_order_requires_nonzero_symbol = true
Lorentzian_contraction_frozen = true
Euclidean_Wick_map_used = false
augmentation_is_edge_local_not_global = true
general_c_d_augmentation_family_frozen = true
orientation_choice_invariance_equation_frozen = true
independent_reversed_coefficient_pair_frozen = true
operational_public_closure_principle_declared = true
canonical_public_collapse_ray_declared = true
public_collapse_naturality_and_orientation_tests_frozen = true
equal_endpoint_ratio_inserted_into_hostile_family = false
equal_endpoint_ratio_gate_passed = false
unit_character_phase_fixed = true
transition_magnitudes_fixed = false
single_operator_completeness_postulate_declared = true
independent_primitive_F2_axiomatically_disallowed = true
independent_primitive_Pauli_axiomatically_disallowed = true
primitive_F2_theorem_excluded = false
primitive_Pauli_theorem_excluded = false
generated_F2_coefficient_computed = false
generated_Pauli_coefficient_computed = false
kinematic_Pauli_coefficient_computed = false
finite_pure_state_kinematics_premise_declared = true
finite_pure_state_kinematics_counts_as_alpha_evidence = false
complete_action_multiplier_family_admitted = true
complete_record_interval_family_admitted = true
durable_record_interval_principle_declared = true
durable_record_interval_derived = false
deterministic_periodic_response_sequence_frozen = true
trivial_holonomy_background_and_root_embedding_frozen = true
root_generated_preparation_domain_declared = true
preparation_uniqueness_proved = false
dimensionless_BID_generator_frozen = true
monoidal_disjoint_record_composition_declared = true
monoidal_disjoint_record_composition_proved = false
root_survival_log_response_rejected = true
primitive_public_transition_amplitude_derived = true
primitive_transition_baseline_nonzero = true
volume_uniform_zero_free_neighborhood_proved = false
connected_linked_cluster_density_proved = false
primitive_action_multiplier_fixed = false
tetrad_anisotropy_map_frozen = true
cellulation_refinement_class_frozen = true
cellulation_independence_proved = false
normalized_real_Fourier_map_frozen = true
topological_Frobenius_section_frozen = true
uniform_locality_test_frozen = true
uniform_locality_test_passed = false
rank_20_plus_parity_local_tensor_extraction_frozen = true
Lorentzian_Hodge_operator_frozen = true
primitive_record_stiffness_computed = false
complete_Q_spec_declared_as_required = true
complete_Q_spec_sealed = false
physical_charged_amplitude_computed = false
Ward_identity_and_threshold_matching_passed = false
physical_Thomson_stiffness_computed = false
alpha_Thomson_convention_map_frozen = true
outcome_blind_holdout_protocol_frozen = true
holdout_registry_source_query_cutoff_frozen = false
holdout_randomness_beacon_frozen = false
structure_sensitive_holdout_available = false
independent_seal_evaluator_implemented = false
stage_dependency_graph_acyclic = true
projective_orbit_map_frozen = true
projective_orbit_pullback_computed = false
componentwise_gauge_stabilizer_quotient_frozen = true
physical_flux_subspace_frozen = true
unique_flux_lift_proved = false
positive_response_and_Lorentz_action_separated = true
source_free_Maxwell_energy_response_proved = false
Lorentz_scalar_face_response_proved = false
unitary_response_inputs_complete = false
unitary_response_provenance_gate_passed = false
joint_group_gate_passed = false
first_opening_accounting_gate_passed = false
Hilbert_functor_gate_passed = false
differential_uniqueness_gate_passed = false
primitive_effective_gate_passed = false
composition_loop_prediction_sealed = false
formal_spec_review_report_sealed = false
physics_operator_spec_review_report_sealed = false
independent_red_team_spec_review_report_sealed = false
formal_core_result_review_report_sealed = false
physics_operator_core_result_review_report_sealed = false
independent_red_team_core_result_review_report_sealed = false
independent_core_reconstruction_sealed = false
BID_core_result_sealed = false
parent_reproduced_or_corrected = false
BID_parent_comparison_completed = false
holdout_universe_sealed = false
Qspec_specification_sealed = false
prediction_map_sealed = false
Thomson_result_sealed = false
alpha_result_sealed = false
holdout_result_sealed = false
BID_final_claim_sealed = false
external_unused_holdout_preregistered = false
independent_end_to_end_reconstruction_sealed = false
formal_final_claim_review_report_sealed = false
physics_operator_final_claim_review_report_sealed = false
independent_red_team_final_claim_review_report_sealed = false
spectral_evaluation_authorized = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
