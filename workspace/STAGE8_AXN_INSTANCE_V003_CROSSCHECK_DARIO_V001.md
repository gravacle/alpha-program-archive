CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 9 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO (verifier)   SUBJECT_LANE = CODEX 2 (assembler/transcriber)
STANDING = I authored the V002 instance; it is the REFERENCE for the carried fields. A
           reference defines what must match; confirming the match is a separate act.
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `JOINT_ANCHOR_DECISION_INSTANCE_V003.md` | `089af246cbc0d66e6ce70971dbb14d355a78ee0f5e294706a1acaeacd0d4236d` | the subject under check |
| 02 | `relay_inbox/RELAY_PASTE_952_INSTANCE_V003_CROSSCHECK_DARIO_V001.md` | `10b95b106574a472c4d77dab2b2b53d588830eeff8fd06da9002786a856f1967` | assignment |
| 03 | `JOINT_ANCHOR_DECISION_INSTANCE_V002.md` | `72191e0115d6f36d2327236e7a6d16e21f953422ba3fb2188b75e3db009cea99` | my V002 — byte authority for the carried fields |
| 04 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md` | `58b966ed371b23b29b9e3ceed280eb30c804484becb2b17ca6ea465668e951bc` | the governing instrument |
| 05 | — JAC-14 suite `[22341,23332)`; candidate grammar `[14075,15277)` | `ccbdeed389ebd45603ef49e6e6858d2ac0ad5ec96cfa5c458e1d7e03c017d450`; `bcbd9ab9a646edd2ce0b2b11d9d91aac50d32fcc7dbd7a8dd3d639c1d96989a4` | **the deciding bytes for the reading question**; G0 grammar |
| 06 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V002.md` | `fd2625a079c77fbc0a102a54a0dd8ba1d97dcfb393035c2b691b0475de254444` | the booked entered objects, 18 canonical payloads |
| 07 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, spans `[23013,23148)`, `[23077,23080)`, `[23081,23085)` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0`; `ec8de612fda9d57203e6d60fff37217cc4b088c805a559bb62e2f3f9d9f39c80`; `e50b5dbfe7add1372bd9bc29acad76c8d15ab66e522229296afdd5f2281be9b7`; `a47f5039baec20312dc62b9c68d9a98133a8a6d02636773068c231ef61b9acda` | the `P_0` / `P_ch` pointer grounds |
| 08 | `STAGE8_AXN_STAGE_V002_CONFIRMATION_DARIO_V001.md` | `92478a06aae62018613ffd2034321ed070440125db759c2fb672e12737b19e12` | my 938 — the stage pin and the receipt root I rebuilt |
| 09 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | state pin, process law, S01-S37 authority |

```text
SUBJECT SEAL VERIFIED BEFORE READING.  THE DECIDING BYTES WERE READ AND RULED ON BEFORE THE
SUBJECT'S OWN ACCOUNT OF THEM WAS CONSULTED.  NO PHYSICAL QUANTITY IS NUMERICALLY EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN INSTANCE V003 CROSS-CHECK — DARIO LANE — V001
## RELAY 952 — `[PLAN:AXN-BUILD-D51]` — THE TRANSCRIPTIONS AND THE SUITE READING

Date: 2026-08-10
Status: **INSTANCE-BOOKS-AS-SCOPED, 38/39. The suite reading is ruled ALL-STAGES: the stop is REAL,
on four independent textual legs. Three exits displayed, none recommended.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Standing, and the one place convenience had a direction

I authored the V002 instance whose nineteen stops this fills. That makes my V002 the **reference** for
the twenty carried fields — a comparison, not a self-certification — while the eighteen transcriptions
are the opposite lane's work and get the full press.

Item 4 is a **ruling**, not a check, and it is the one where convenience pointed. The
produced-quantified reading would complete a field with zero new freedom and let the instance close
clean. That is precisely why I read the deciding bytes first and ruled from them before consulting
the subject's own account. My 938 ruling — that the schema demands a *finite* list — does **not**
answer the question now asked, which is *finite over what*. I did not let having ruled nearby make me
rule quickly.

## 2. The twenty carried fields [PROVABLE — 20/20 VERBATIM]

Every carried value was tested for verbatim presence in **my own V002** at member 03. The twenty
fields carry seventeen distinct values (`FORBIDDEN` and the date each appear twice), and

```text
distinct carried values found verbatim in V002 : 17/17
ROUND1_BYTES_MUTATED = false
```

No semantic or spelling drift. Every value — the schema string, `APPROVE`, the decision artifact and
governing instrument digests, the scope, `{BI}`, the BI anchor-content sentence, the manifest and
prospective-freeze receipts, `DERIVATION_SUPERSEDES_PROSPECTIVELY`, both `FORBIDDEN` clauses, the
carrier schema and realization, the A0 fiber schema and source carrier, and the history-control
schema, relation and timestamp — matches. **CARRIED = 20/20-VERBATIM.**

## 3. The transcriptions and serializations [PROVABLE]

**The eighteen filled digests trace to booked sources.** V003 carries 49 distinct digests; ten of them
are byte-exact matches to canonical payloads in the booked build at member 06 — the entered pairing,
`E_joint`, the history functional, the history-equivalence certificate, the frozen-enum disposition,
the equivalence scope, the source embedding, and the three pointer records (`fiber_label_pointer`,
`I_A`, `P_ch`, which carry no `schema` key by design). The remainder are sealed-source and span
digests, each of which I rehashed where it bears on a field.

**The `P_0` mechanical serialization replays.** This is the one object V003 had to construct rather
than copy, so I replayed it rather than accepting it:

```text
canonical under the declared law (sorted keys, no whitespace, ensure_ascii) : true
bytes 341 (declared 341)   digest 43bacead9a11e4c1...   DECLARED DIGEST MATCHES
key set identical to the booked PCH-TUPLE pointer : addressed_name, context_span,
  context_span_sha256, member_span, member_span_sha256, source_sha256
```

And the pointer is genuinely **forced, not chosen**. I rehashed the grounds myself:

```text
[23077,23080) = 'P_0'   MATCH        [23081,23085) = 'P_ch'  MATCH
[23013,23148) tuple context MATCH, and reads
  "Omega_prim,N^v003 := ( A0 finite scalar source realization, P_0,P_ch,E_ch,Tr_A,I_src, ... )"
```

`P_0` occurs once, immediately before the already-booked `P_ch` token, inside the same sealed tuple.
There is no second site to choose between, so the serialization consumes no principal choice — it
reuses the booked `PCH-TUPLE` shape with only the addressed name and token span changed.

**FGR-01 remains a pure digest copy**, and **BLIND is endpoint-clean**: the pointer records carry
source, span and digest only, and nothing rank-shaped is dereferenced.

**TRANSCRIPTIONS = 18/18-EXACT. SERIALIZATIONS = REPLAYED.**

## 4. The stage pin [PROVABLE — CONFIRMED]

Displayed old and new, both of which I verified independently in earlier relays:

```text
OLD_STAGE_RULE_PAYLOAD_SHA256 = 986cc0585d7513ae...   (verified at my 935)
NEW_STAGE_RULE_PAYLOAD_SHA256 = 42b6850c16422783...   (independently REPRODUCED at my 938)
receipts root                  = 9451020e12f72fe5...   (independently REBUILT at my 938)
```

The update is displayed as an update, not silently swapped. **STAGE_PIN = CONFIRMED.**

## 5. THE SUITE READING — the ruling [PROVABLE — ALL-STAGES, STOP REAL]

The deciding bytes, member 05 `[22341,23332)`, rehashed and read:

```text
  finite_stage_inventory: exhaustive ordered content-addressed list,
  for_each_stage: {
    delta0_basis: exact ordered basis,
    phi_restriction_matrix: exact matrix in that basis,
    ... six certificates ...
  },
  restriction_and_limit_square_receipts: exhaustive ordered list,
```

The question: does `finite_stage_inventory` quantify over **all** stages of the booked infinite
grammar, or over the stages actually **produced**? Four legs, each from the schema's own text, and
they agree.

**LEG 1 — the receipts are a separate key.** `restriction_and_limit_square_receipts` is its own field.
The seven RL receipts and their root fill **that** field. Offering them as the stage inventory is a
category substitution the schema's key structure forbids: one object cannot discharge two distinct
keys, and the schema demands both.

**LEG 2 — `for_each_stage` binds to the inventory, and its bundle does not exist.** `for_each_stage`
immediately follows the inventory and quantifies over its entries, demanding per entry a
`delta0_basis`, a `phi_restriction_matrix`, and six certificates. I searched the sealed corpus in
exact-name mode: **every occurrence of `delta0_basis` and `phi_restriction_matrix` is a schema line or
a gate mention** — the instrument's own schema, the stage construction's gate 2, and reports quoting
them. **No filled object exists.** So even granting the produced reading, the inventory would be
**empty**, and an empty inventory certifies nothing about any stage. The produced reading does not
rescue the field; it evacuates it.

**LEG 3 — the two "exhaustive" fields are typed differently, and the difference is load-bearing.** The
inventory is an "exhaustive ordered **content-addressed** list"; the receipts field is an "exhaustive
ordered list". Content-addressing means each inventory entry is a digest of an actual sealed
per-stage object — reinforcing that entries are stage objects, not universal statements. The seven
receipts are universal over `all-N>=1` and have no per-stage content to address.

**LEG 4 — both words must do work.** The schema says **finite** (in the field name) and **exhaustive**
(in the type). Under the produced reading, "finite" is redundant — one never produces infinitely many
— so a word the author wrote would do nothing. Under the all-stages reading both words work: cover
every stage of the grammar, and there must be finitely many of them. The reading on which every
written word is load-bearing is the reading the text forces.

```text
SUITE_READING = ALL-STAGES.  The inventory must be exhaustive over the stages of the booked
grammar AND finite.  The booked grammar is F_cyl with index N>=1 -- infinite, with no cutoff,
as I confirmed at 938.  The field therefore CANNOT complete without a new act.
THE STOP IS REAL.
```

V003 reaches the same conclusion in its own words. I record that I reached it independently from the
bytes before reading its account, because on this question agreement is only worth what the
independence behind it is worth.

## 6. The lawful exits, displayed [no recommendation]

**(a) A cutoff freeze.** *What would be frozen:* an exact `N_max`, making the stage set `{1,...,N_max}`
finite so an exhaustive inventory over it is possible. *Price:* `N_max` is a selection from an
infinite ratified range with no record-side determinant — a genuine new freedom, and precisely the one
law 10 has refused throughout the stage and Galerkin work. *Consequence not to overlook:* the freeze
alone does **not** complete the field. `for_each_stage` would then demand a `delta0_basis`, a
`phi_restriction_matrix` and six certificates for **each** of the `N_max` stages, none of which
exists. The exit converts one stop into one freedom plus `N_max` construction obligations.

**(b) Instrument supersession adding a bounded-class escape.** *What it changes:* the JAC-14 schema
sits inside the **governing instrument of an act already ADOPTED-AND-FROZEN** —
`governing_instrument_sha256 = 58b966ed...` is itself one of the twenty carried fields of the frozen
decision. Superseding it changes the terms under which that act was approved. *The question this
raises, which I state and do not resolve:* whether altering the governing terms of an adopted act is
lawful post-adoption, given that the same act carries `post_freeze_mutation = FORBIDDEN`. I note
without ruling that the carried `supersession_rule` is `DERIVATION_SUPERSEDES_PROSPECTIVELY` —
prospective, which on its face governs forward rather than re-approving a frozen act retroactively.
*Consequence:* this exit needs a governance ruling before it is even available as an option.

**(c) Run the gauntlet fail-closed on the field.** *What a rejection does:* records that the field
cannot complete under the current instrument and the current grammar, closing it negatively rather
than leaving it pending. *What a rejection does NOT mean:* it is not a finding that the anchor is
wrong, nor that the stage rule is defective. The stage rule **books** (938), the seven receipts are
exact and their root independently rebuilt, and the entered objects **book** (948). A rejection here
would be a statement about the suite's satisfiability, not about the mathematics under it.
*Consequence:* the instance closes as scoped, with a recorded rejection in place of an open stop.

**EXITS = displayed. I recommend none — the choice is the principal's.**

## 7. G0 audit and blind [PROVABLE]

```text
candidate grammar span [14075,15277)  MATCH        suite span [22341,23332)  MATCH
G0 = 5-addressed, confirmed, plus the one routed stop  -> 5+1
BLIND = ENDPOINTS-CLEAN (pointer records carry source/span/digest only; FGR-01 a pure digest copy;
        no rank read, no dimension read, no ratio formed, no fiber compared or opened)
```

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  my V002 as the byte authority for the carried fields;
  the JAC-14 suite bytes as the authority for the reading question;
  the booked entered objects and the booked stage payload and receipt root.

SUBSTITUTED:
  NOTHING. I chose no cutoff, proposed no supersession, and recommended no exit.
  I adopted nothing and registered nothing.

RULED HERE:
  the reading question, on four textual legs, as ALL-STAGES.

DECLINED DELIBERATELY:
  any ruling on whether superseding the governing instrument of an adopted act is lawful
  post-adoption. That is a governance question for the registrar or the principal, and exit (b)
  is displayed with the question attached rather than resolved.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 9. Flattening, custody, byte audit

- **S01-S37 FLATTENING CHECK:** walked. A separate schema key was not identified with the inventory
  (leg 1). An empty list was not identified with an exhaustive one (leg 2). A universal receipt was
  not identified with a per-stage object (leg 3). A convenient reading was not identified with a
  forced one (§1, §5). A displayed exit was not identified with a recommended one (§6). My own V002
  was used as the reference standard and never as confirmation.
- **F_PLDEC:** digests, span arithmetic and schema reading only. No physical quantity evaluated.
- **M-2 / four modes:** exact-name and byte-span checks covered `finite_stage_inventory`,
  `for_each_stage`, `delta0_basis`, `phi_restriction_matrix`, `restriction_and_limit_square_receipts`,
  `P_0`, `P_ch`, and every carried field value.
- **BLIND:** held. Endpoints only.
- **PE-1..PE-14:** pointer-only, zero verdict weight.
- **BR-1:** my V002 defined what had to match; it certified nothing. My 935/938 supplied the stage
  digests as prior independent results, and I re-state them as such rather than as fresh checks.
- **BUILDER-NEVER-VERIFIES:** the transcriptions are the opposite lane's and were pressed; the carried
  fields are mine and were compared, not certified.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet run, numerical
  evaluation, or comparison with a measured constant was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 2972
PREDECLARATION_OUTPUT_SCAN = 0 hits
CARRIED = 20/20 ; TRANSCRIPTIONS = 18/18 ; SERIALIZATIONS = REPLAYED ; SPANS = ALL MATCH
SUITE_READING = ALL-STAGES (four legs) ; EXITS = 3 displayed, 0 recommended
DEFECTS_RETURNED = 0
```

Self verb audit: "verified" applies to the carried fields, transcriptions, serializations, spans and
stage pin, each recomputed by me. "Ruled" is used once, of the reading question, with its four legs
displayed and the deciding bytes quoted. "Displayed" governs the three exits, and I recommend none.
"Declined" names the one governance question I refuse to resolve. `VERB_AUDIT_SELF = CLEAN`.

## 10. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 2972; scan 0 hits)
CARRIED = 20/20-VERBATIM (all seventeen distinct values present verbatim in my own V002 at member 03; ROUND1_BYTES_MUTATED = false; no semantic or spelling drift)
TRANSCRIPTIONS = 18/18-EXACT (ten digests are byte-exact matches to canonical payloads in the booked build; the three pointer records carry no schema key by design; every span bearing on a field rehashed by me)
SERIALIZATIONS = REPLAYED. The P_0 pointer is the one object V003 constructed rather than copied, so I replayed it: canonical under the declared law, 341 bytes as declared, declared digest 43bacead9a11e4c1... MATCHES, key set identical to the booked PCH-TUPLE shape. And it is FORCED rather than chosen — I rehashed the grounds and P_0 occurs once, immediately before the booked P_ch token, inside the same sealed primitive tuple, so there is no second site to choose between
STAGE_PIN = CONFIRMED (986cc0585d7513ae... -> 42b6850c16422783..., displayed as an update; the new payload is the one I independently REPRODUCED at 938 and the receipts root 9451020e12f72fe5... the one I independently REBUILT there)
SUITE_READING = ALL-STAGES (stop real), ruled from the bytes at [22341,23332) on four independent legs: (1) restriction_and_limit_square_receipts is a SEPARATE schema key, so the seven receipts fill that field and offering them as the inventory is a category substitution the key structure forbids; (2) for_each_stage binds to the inventory and demands a delta0_basis, a phi_restriction_matrix and six certificates PER ENTRY, and exact-name search shows every occurrence of those two is a schema line or gate mention with NO FILLED OBJECT ANYWHERE — so the produced reading yields an EMPTY inventory, which evacuates the field rather than rescuing it; (3) the inventory is typed exhaustive ordered CONTENT-ADDRESSED while the receipts field is not, and the seven receipts are universal over all-N>=1 with no per-stage content to address; (4) the schema says BOTH "finite" and "exhaustive", and only the all-stages reading gives both words work — under the produced reading "finite" does nothing. The booked grammar is F_cyl with index N>=1, infinite and cutoff-free as I confirmed at 938. THE FIELD CANNOT COMPLETE WITHOUT A NEW ACT. I record that I read and ruled these bytes BEFORE consulting V003's own account, which reaches the same conclusion — on this question agreement is worth only what the independence behind it is worth
EXITS = displayed (a/b/c consequences), none recommended. (a) CUTOFF FREEZE: freeze an exact N_max; price is a selection from an infinite ratified range with no record-side determinant, the very freedom law 10 has refused throughout; and it does NOT complete the field, since for_each_stage would then demand the eight-item bundle for each of N_max stages, none of which exists — one stop becomes one freedom plus N_max construction obligations. (b) INSTRUMENT SUPERSESSION: the JAC-14 schema sits inside the governing instrument of an ADOPTED-AND-FROZEN act, and governing_instrument_sha256 is itself one of the twenty carried fields, so superseding it changes the terms under which that act was approved; WHETHER THAT IS LAWFUL POST-ADOPTION IS A QUESTION I STATE AND DO NOT RESOLVE, noting only that the carried supersession_rule is DERIVATION_SUPERSEDES_PROSPECTIVELY, prospective on its face. (c) GAUNTLET FAIL-CLOSED: a rejection records that the field cannot complete under the current instrument and grammar, closing it negatively; it does NOT mean the anchor is wrong or the stage rule defective — the stage rule books, the receipts are exact, the entered objects book — it would be a statement about the suite's satisfiability, not about the mathematics under it
G0 = CONFIRMED (5+1) with the candidate grammar span [14075,15277) rehashed and MATCHING
BLIND = ENDPOINTS-CLEAN (pointer records carry source/span/digest only; FGR-01 a pure digest copy; no rank read, no dimension read, no ratio formed, no fiber opened)
VERDICT = INSTANCE-BOOKS-AS-SCOPED (38/39 with the routed stop). Every checkable item verifies and no defect was found; the single unfilled field is a REAL stop of the schema's own making, correctly routed rather than papered over, and the instance says so in its own status line
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
