# Stage 8 First-Opening-Root Principle Recovery Index v001

Date: 2026-07-29

Status: APPEND_ONLY_RECOVERY_INDEX. This artifact brings a root principle into
the working set as recovered evidence and a recovered frame with a falsifier. It
does not restore the retired route built on that principle and does not present
the first-opening root as currently computable.

## Recovered Principle

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_same_cell_opening_normalization_principle_v001.md`

SHA-256:

```text
1c573812b0e658408424aa9ae147564b80ab6243d80c69e498d0892e52e594fb
```

Lines 5-9 define the physical statement: the allow/require threshold acts on
one complete Lorentz-invariant record cell, so the theory does not attach an
independently adjustable opening coefficient to an already computed source
Hessian. The source cost and public record gain are accumulated over the same
stationary physical interval, configuration, and dimensionless action
convention.

Lines 27-35 define the first-opening operator

```text
L_open(K) = H_cell(K) - R_BR(K)
```

and state that there is no separate coefficient to fit or identify with the
coupling; the Maxwell stiffness `K` enters only through the complete microscopic
action, its stationary saddle, and the generated unitary record dynamics.

Lines 39-47 make the selection a first positive root with a lowest-eigenvalue
crossing, one-dimensional kernel, transversality in `K`, and positivity before
the crossing. Lines 49-51 require every admitted saddle, boundary, continuum,
and regulator family to yield the same root. Lines 53-58 give the physical sign
reason and the falsifier: if a microscopic derivation produces a different
relative sign or an additional independent coefficient, this branch is falsified
rather than repaired.

## Critical Scope Limit

The route built on this principle is retired. The current source for that limit
is:

`/Users/bgm/MB Work/alpha_supervision/BOHM_ROUTE_RUNNABILITY_WORKFLOW_RECOVERED_2026-07-28.md`

which records at lines 96-101 that the step-order gate requiring `L_open` was
superseded and that the successor explicitly forbids `R_BR` from re-entering
under a renamed energy, noise, fidelity, or Hessian term. The quoted active
gate is:

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_symbolic_first_proof_gate_v003.md`

lines 5-10, which state that v001 required the retired positive-cost expression
`L_open=(T/hbar)H_energy-R_BR`; the active route instead requires the complete
zero-bare 2PI Dyson kernel, and v001/v002 may not steer the active route.

The same recovery workflow records at lines 136-143 that `R_BR` does not descend
from `PRIMITIVE_ADDITIVE_RECORD_FIDELITY_ACTION_V002`; the association is a
cross-tree name resemblance, and `R_BR` has its own unpaid premise burden.

## Working-Set Status

The principle is recovered as evidence for the program's earlier stiffness
frame: stiffness was posed as an allow/require onset condition with an explicit
falsifier. It is not recovered as an executable path. Whether a first-opening
root can be formed from one microscopic CTP functional is open, and the retired
`L_open` subtraction may not be reused by renaming its terms.

```text
first_opening_root_principle_recovered_to_working_set = true
first_opening_root_route_restored = false
L_open_route_retired = true
first_opening_root_computable_from_current_working_set = false
alpha_computed = false
proof_authorized = false
```
