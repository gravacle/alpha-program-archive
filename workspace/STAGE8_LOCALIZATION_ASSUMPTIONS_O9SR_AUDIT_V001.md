# STAGE 8 — AUDIT OF THE LOCALIZATION ASSUMPTIONS ARTIFACT — O9SR V001

## AUDITOR — CODENAME LOCALIZATION-AUDIT — COMMISSION O9SR — 2026-08-15

**STANDARD: DEFAULT-REFUTE. Testimony carries zero weight; every claim re-derived
at bytes or re-run in a fresh venv. The target's own audits are consumed at
audited grade, never as testimony about the target.**

---

## 0. VERDICT

```text
OVERALL: CONFIRMED-WITH-CORRECTIONS.

PER SUSPICION:
  S-1  (finite-rank escape / quantifier mismatch) ............. *** REFUTED ***
       The target's headline — "OBSTRUCTION A DOES NOT REACH THE QUANTIFIER
       FP-3 REQUIRES" — is over-reached. It is produced by ELIDING W-3 :364-368,
       the sentence in the very block the target quotes, which consumes the
       class-supremum quantifier AT THE CLOSURE'S OWN OBLIGATION and declares
       "(d-1) IS CLOSED NEGATIVELY AT THE CONSUMING SITE". A second elision —
       W-1 AUDIT :685-686, quoted with its second clause cut — hides an
       audited-grade AFFIRMATION of the very sentence the target flags.
       FLAG-1 IS STRUCK. Corrections (c-1) and (c-2) are WRONG AS WRITTEN.

  S-2  (sharp-cutoff choice) ............ CONFIRMED at the headline; CORRECTED
       at the TYPE claim. "Sharp localization is MANDATED, not chosen" is
       sound and verified at source. But SS3.2's counterfactual — the exclusion
       "would NOT survive on its own analytic content" — meets NEITHER display
       this commission requires of a cutoff claim: no ADMISSIBLE alternative is
       shown (the target proves the opposite), and the divergence is not shown
       to fail under one even inside the target's own TOY. DOWNGRADE TO
       UNDETERMINED. FLAG-2's conditional-dependence observation survives.

  S-3  (imported vs record-native) ...... CONFIRMED-WITH-CORRECTIONS.
       N-1, N-2, N-4, N-5, N-6 gradings CONFIRMED at bytes. N-3(b)'s
       IMPORTED/DEFECT grade REFUTED: the notion is carried on the record's own
       sealed objects, and the target's own N-6 reads the SAME bytes (W-3
       :323-324) as LICENSED. It also falls with S-1 by the target's own
       "TWO ROUTES, ONE CRACK". N-3' survives — at AUDITED GRADE only, not on
       the target's CAS, which is circular (A-4).

  S-4  (degree comparison not well-posed) ... CONFIRMED-WITH-CORRECTIONS.
       Headline STANDS — and stands on a sweep the target did not run.
       SS5.3's "no single sign convention can repair" is FALSE on the record's
       own numbers (A4a-A4g, exact). SS5.2's negative existential is asserted
       from a key set containing no grading token. The KIND taxonomy carrying
       the verdict is, by the target's own words, "determined here".
       FLAG-3's second sentence IS STRUCK.

NOTHING IS MOVED THAT IS OF RECORD. The three obstructions' EXISTENCE is
untouched by this audit; S-1's correction RESTORES the sea-sandwich to its
sealed role rather than removing it. FP-1, FP-2, FP-3 all STAND. No failure
point, witness, V-clause, gate, threshold or fence is touched.

FENCES HELD: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false. DETERMINATION ONLY.
```

---

## 1. STEP 0 — PROBE AND SEALS

```text
TARGET PROBED PRESENT:
  STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_V001.md ............ 59497 bytes
  STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_V001.md.seal.sha256 ... present
SEAL CHECK, run FROM THE ARTIFACT'S OWN DIRECTORY
  (cd /Users/bgm/MB Work/alpha-program-archive/workspace && shasum -a 256 -c):
  STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_V001.md: OK
  sha256 = 3ca6c5be240aad148bc7f5085a9ce62151138d2c74b4905aa27ebd7dc450a2d5

MY OUTPUT PATH PROBED ABSENT BEFORE FIRST WRITE:
  STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_AUDIT_V001.md ......... No such file
  ...AUDIT_V001.md.seal.sha256 ............................... No such file

THE TARGET'S SS1.1 CLAIM RE-RUN INDEPENDENTLY, from the same directory —
all ten, all OK, none relied on before checking:
  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md ................ OK
  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md .......... OK
  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md ............ OK
  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md ...... OK
  STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md ............. OK
  STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md ....... OK
  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md ........... OK
  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md ..... OK
  STAGE8_COMPLETION_MAP_T17SR_V001.md ............... OK
  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md ......... OK
  ==> SS1.1 IS ACCURATE AT BYTES. 10/10 CONFIRMED.

TWO FURTHER ARTIFACTS SURFACED BY MY OWN SWEEP A-1 AND SEAL-CHECKED BEFORE USE:
  STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md ... OK
  STAGE8_AXN_BUILD_GRADING_VERIFICATION_DARIO_V001.md .. OK
```

### 1.1 SWEEP CUTOFF — DECLARED

```text
CUTOFF: 2026-08-15, this session, at the two commissioned corpus roots.
ROOT 1: /Users/bgm/MB Work/alpha-program-archive/workspace
ROOT 2: .../alpha_fundamental_record_action_cleanroom_v003

SCOPED READS: the target in full; the ten named artifacts at the line ranges
the target cites and at their adjacent bytes; the two sweep-A-1 hits at the
matched lines and their surrounding block.

DECLARED SCOPED SWEEPS, key sets stated exactly (exhaustiveness claimed at
these key sets only, at this cutoff):
  A-1  THE GRADING-IDENTIFICATION KEYS — chosen because the target's own
       K-1/K-2 contain NO grading, degree, or homogeneity token and therefore
       could not detect the object SS5.2 declares absent:
       "grading variable" | "common grading" | "same grading" | "one grading" |
       "degree functor"
       ROOT 1 -> 8 files; ROOT 2 -> 3 files. Both roots swept.
  A-2  FENCE-SCAN KEYS: "lens" | "137" | "fine.structure" | "measured" |
       "CODATA" | "experiment" | "log" | "non-vanish". Run over the target.
  A-3  CITATION-COVERAGE SCAN: every ":NNN-NNN" token in the target, extracted
       and compared against the source bytes at each cited range.

EXCLUDED BY COMMISSION, NOT READ, NOT LISTED, NOT COUNTED: every register,
tracker, road, plan, continuation, ledger and lens file. Sweep results were
filtered against those tokens before any use.
"Q-..." register tokens: EXPECTED-UNLOCATABLE — not chased.
```

---

## 2. A-1 — THE PRIMARY FINDING: S-1'S DISMISSAL IS BUILT ON AN ELISION
### **A REAL OBSTRUCTION EXPLAINED AWAY AS A QUANTIFIER MISMATCH — REFUTED**

This commission names one seductive failure mode: a structural obstruction
dismissed as a "quantifier mismatch". The target performs exactly that dismissal
at S-1, and the standard it must meet is stated in my commission: **the closure's
own requirement, quoted at its own quantifier, showing the obstruction's
quantifier is not needed.** The target does not meet it. The requirement, when
quoted, shows the opposite — and the sentence that shows it sits four lines below
the target's own quotation, inside the same sealed block.

### 2.1 What the target quotes, and where it stops

```text
TARGET SS2.1, second coordinate, verbatim from the target:
  "AND, second coordinate: it bites on the CLASS SUPREMUM, not on a member
   the closure names. W-3 SS4.2's corollary is
     sup_{t in H(sqrt3)} ||C T C||_2 = +infinity, 'witnessed EXACTLY by the
     class member h_0 itself' (W3 :361-363)"

W-3 :359-368 AT BYTES, IN FULL — the target's quotation ends at :363:

  359  ```text
  360  COROLLARY (THE SITE'S VERDICT) [DERIVED]:
  361     sup_{ t in H(sqrt3) } || C T C ||_2  =  +infinity ,
  362     witnessed EXACTLY by the class member h_0 itself, for which ||C h_0 C||_2
  363     is not merely large but INFINITE (the sandwich is a nonzero translation-
  364     invariant operator; SS4.1). Therefore NO inequality of the shape
  365        || C T C ||_2  <=  F( c, cell data, eps )     for all t in H(sqrt3)
  366     can hold, at ANY finite F. READING A of S2b is FALSE as an inference from
  367     the certified asset; READING B has no conversion at this site.
  368     (d-1) IS CLOSED NEGATIVELY AT THE CONSUMING SITE — not "not today".
                                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^

*** THE ELISION IS EXACT. The target's quote stops at :363. Lines :364-368 are
the closure-side consequence, and they are absent from the target — the string
"READING A" does not occur anywhere in the 1070 lines of the target (grep,
zero hits), and its citation-coverage list of W-3 ranges (:317-321, :320-321,
:323-324, :326-327, :340-343, :353-357, :361-363, :379-385, :390-393) contains
no range covering :364-368. ***
```

### 2.2 Why the elided lines destroy the dismissal

```text
THE CLOSURE'S OWN REQUIREMENT, QUOTED AT ITS OWN QUANTIFIER (W3 :192-194,
transcribing E1 :702-708):
   S2b:  G_cm(C, eps) := |C|_4^{-beta} . (certified bound on the C-L2
         commutator error in the quadratic form C-L2 names)

THE TARGET READS THIS SIGNATURE AND EXTRACTS ONE COORDINATE (SS2.2):
   "G_cm is a FUNCTIONAL OF (C, eps) — carrying NO stage index at all."
   ==> hence the closure needs UNIFORM-IN-n.  CORRECT, AND I CONFIRM IT.

BUT THE SAME SIGNATURE CARRIES A SECOND COORDINATE THE TARGET NEVER EXTRACTS:
   G_cm(C, eps) carries NO CLASS-MEMBER INDEX EITHER. It is a functional of
   (C, eps) alone, applied to a bound "certified" for the class. That is
   PRECISELY the shape
        F( c, cell data, eps )   valid  for all t in H(sqrt3)
   which W-3 :364-366 proves CANNOT HOLD AT ANY FINITE F.

W-3 THEN NAMES THE CONSEQUENCE IN ITS OWN VOICE, AT THE CLOSURE'S OWN SITE:
   ":366-367  READING A of S2b is FALSE as an inference from the certified asset"
   ":368      (d-1) IS CLOSED NEGATIVELY AT THE CONSUMING SITE — not 'not today'."

==> THE SEA-SANDWICH DOES REACH A QUANTIFIER THE CLOSURE REQUIRES. It refutes
    the derivation of a finite G_cm from class membership, at the class-universal
    quantifier that S2b's own signature imposes. The target's headline —
    "OBSTRUCTION A DOES NOT REACH THE QUANTIFIER FP-3 REQUIRES" — is FALSE.
```

### 2.3 The conflation that produced the error, named exactly

```text
TWO DISTINCT PROPOSITIONS, WHICH THE TARGET TREATS AS ONE:

 (i)  THE SANDWICH DOES NOT PROVE ||A(0)||_2 = +infinity.
      TRUE, and sealed: W-3 :390-393 "NOT to the actual C-L2 error layer, which
      is a different operator and remains UNDECIDED". W-1 :379-385 concurs.
      The target establishes this correctly and it is CONFIRMED here.

 (ii) THE SANDWICH DOES NOT BITE AT A QUANTIFIER THE CLOSURE NEEDS.
      FALSE, by W-3 :364-368. The closure needs a FINITE G_cm; the sandwich
      proves NO SUCH G_cm IS DERIVABLE FROM THE CERTIFIED ASSET, for all t in
      the class. That is a block at a needed quantifier.

*** THE TARGET DERIVES (ii) FROM (i). It does not follow. "Fails to prove the
consuming object diverges" and "fails to block the consuming obligation" are
different statements; W-3 asserts the first and denies the second, in adjacent
sentences, and only the first was carried across. ***

WHAT THIS CHANGES, MINIMALLY AND WITHOUT MOVING ANYTHING OF RECORD:
  The target's SS7 FP-3 row "MATCH? NO / MISMATCH" must be replaced by a SPLIT,
  because FP-3's discharge is blocked at TWO quantifiers by TWO sealed objects:
     CLASS-UNIVERSAL coordinate .... blocked by the SEA-SANDWICH (W3 :364-368)
     UNIFORM-IN-n coordinate ....... blocked by FHB's FB9 (W3 :428-431, :249)
  Both stand. The target found the second and mistook it for the whole.
  FP-3 is NOT weakened by this correction — it is blocked twice, not once.
```

### 2.4 The second elision — an audited-grade affirmation cut mid-sentence

```text
TARGET SS2.5, verbatim:
  "THE FIRST THREE SENTENCES ARE CORRECT AND ARE CONFIRMED HERE (and the W-1
   audit :685 independently confirms them: 'The target states this correctly')."

W-1 AUDIT :684-686 AT BYTES — the sentence the target cuts:
  684  obstruction is absent (a finite-rank operator is compact)"), and every
  685  `A_n` here has rank `<= 4n^3`. The target states this correctly and
  686  correctly locates the bite at the **uniformity**. Where it errs is in
                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

*** THE W-1 AUDIT AFFIRMS THE FOURTH SENTENCE TOO — the exact sentence the
target flags as an unsupported overstatement — and the target quotes the audit
in truncated form, stopping at "correctly", as support for the opposite reading.
The audit continues "and correctly locates the bite at the uniformity". ***

I do not take the audit's word as decisive; testimony carries zero weight here,
and I re-derived the substance independently at SS2.2. But the target is bound
by its OWN CH-1: consume audited targets AT AUDITED GRADE. It set aside an
audited-grade affirmation without disclosing that it had done so, by truncation.

==> FLAG-1 (W1_LINE343_UNIFORMITY_ATTRIBUTION_OVERSTATED) IS STRUCK.
    W-1 :343 is affirmed at audited grade AND supported at bytes by W-3
    :364-368. It is not a "third instance" of a caught species.
    THE ONE RESIDUE WORTH RECORDING, and it is a precision note only: the
    sandwich supplies CLASS-uniformity, FB9 supplies n-uniformity, and the bare
    word "uniformity" at :343 does not distinguish them. That is a request for
    a sharper word, not a finding of overstatement.

==> CORRECTIONS (c-1) AND (c-2) ARE WRONG AS WRITTEN. Re-attributing FP-3's
    blocking EXCLUSIVELY to FB9 deletes the sandwich's sealed class-universal
    block and would silently narrow a closed negative. (c-1)/(c-2) must be
    replaced by the SPLIT of SS2.3, or dropped.
```

---

## 3. A-2 — THE CUTOFF CLAIM: HEADLINE CONFIRMED, TYPE CLAIM UNDETERMINED

### 3.1 What is CONFIRMED, and it is the substantial half

```text
D6' VERIFIED AT AUDITED GRADE — W3-AUDIT :232-234 at bytes, verbatim:
  "E1 :465-470  D6' — CONFIRMED: M(t) = Q 1_{|x| <= r(t)} Q sealed; *** No
   obligation, lemma, control, repair, or enclosure may replace, mollify,
   smooth, or regularize M(t) ***. The target does not touch it."
The target's transcription is EXACT. Its reading — that this is an EXPLICIT
EXHAUSTIVE PROHIBITION quantifying over every instrument class, so the sharp
shape is IMPOSED and not CHOSEN — is CONFIRMED. S-2's first limb is correctly
discharged negatively, and this is the target's best-grounded section.
Also CONFIRMED at bytes: C1's sealed symbol (W3-AUDIT :235), C6 (W3-AUDIT
:230-231), and FC-1's commutator identity (W3 :252-255).
```

### 3.2 What FAILS — the counterfactual at SS3.2, against this commission's standard

My commission fixes the standard for any cutoff claim: **the sealed display that
an alternative localization is ADMISSIBLE at that site (not merely conceivable)
AND that the divergence fails under it.** The target's TYPE claim — that the
exclusion is "RECORD-IMPOSED" and "would NOT survive on its own analytic
content" — meets neither display.

```text
DISPLAY 1 — ADMISSIBILITY: NOT SHOWN, AND THE TARGET PROVES THE CONTRARY.
  SS3.1 establishes that NO admissible smooth or cell-adapted alternative exists
  at that site. So the alternative localization on which the counterfactual
  rests is MERELY CONCEIVABLE. The record is not silent about the cutoff's
  shape — it MANDATES the shape. Under this commission's rule an ABSENT mandate
  would already be an open question rather than an escape; here the mandate is
  PRESENT and points the other way. The counterfactual is therefore weaker than
  the silent case, not stronger.

DISPLAY 2 — THAT THE DIVERGENCE FAILS UNDER IT: NOT SHOWN, EVEN IN THE TOY.
  The target's own O9-3b establishes only
       sup |d/dx (1+tanh(x/eps))/2|  =  1/(2 eps),  finite for each fixed eps>0.
  THE QUANTIFIER THE TARGET DOES NOT TAKE — re-run here, exact:
       lim_{eps -> 0+} 1/(2 eps)  =  +infinity.        [A3a, A3b, A3c PASS]
  So the TOY's own witness is FINITE AT EVERY FIXED WIDTH AND UNBOUNDED AS THE
  WIDTH CLOSES — the identical finite-at-each-parameter / unbounded-in-the-limit
  shape the target itself flags as a gap at S-1 (its own O9-1d). Boundedness
  under smoothing holds only if the width may be bounded away from zero at that
  site, and the target displays no such permission. The record's own radius
  profile in D6' is r(t) = min(t, 1-t), which the target transcribes at N-1
  without remarking that it closes at the endpoints.
  *** THE TARGET APPLIES ITS OWN QUANTIFIER STANDARD TO THE RECORD'S
  OBSTRUCTION AND NOT TO ITS OWN COUNTERFACTUAL. ***

DISPLAY 2, SECOND DEFECT — THE TOY ADDRESSES ONE TERM OF TWO.
  W-3 :252-255, audit-VERIFIED REAL as FC-1:
     [h_0, 1_B (x) S] = i(alpha.n-hat (x) S) delta_{dB} + 2 . 1_B h_0 S
     — "carries a VOLUME-supported BULK term"; "the S-LESS [h_0, 1_B] IS pure
     surface, so E1's exclusion locus is untouched."
  The target's shape argument reads the FIRST term only. That is faithful to
  E1's exclusion DISPLAY, which is the S-less ||[h_0, 1_B]|| — the target
  commits no error there and I confirm it. But SS3.2 states its conclusion over
  "THE FP-3 exclusion" without restricting it to the S-less surface term, while
  C-L2's controlled object is the S-DRESSED commutator carrying the bulk term.
  The conclusion is stated over a wider object than the argument covers.
```

```text
*** A-2 DETERMINATION. SS3.2's TYPE CLAIM IS DOWNGRADED TO UNDETERMINED.
  KEEP:   "the sharp shape is MANDATED by D6', not chosen" — CONFIRMED.
  KEEP:   FLAG-2's observation that the exclusion's standing is CONDITIONAL on
          D6' remaining sealed, and that this dependence is nowhere displayed
          in the diagnosis chain. That is a fair and useful flag.
  STRIKE: "it would NOT survive on its own analytic content." Neither required
          display is present. Whether the exclusion survives a smooth shape is
          an OPEN QUESTION at this cutoff, not a settled negative. ***

NOTHING HERE PROPOSES SMOOTHING. D6' IS RESPECTED AS WRITTEN BY THIS AUDIT. I
offer no smoothed object to any obligation, lemma, control, repair or enclosure;
my A3 group operates entirely inside the TARGET'S OWN declared TOY.
```

---

## 4. A-3 — THE IMPORTED CLASSIFICATION (S-3): N-3(b) REFUTED, THE REST CONFIRMED

This commission's second hunt is an IMPORTED classification asserted where the
notion is in fact defined on the record's own stages. N-3(b) is that assertion.

```text
CONFIRMED AT BYTES, AND THE TARGET'S WORK HERE IS SOUND:
  N-1  sharp cell indicator ....... RECORD-NATIVE. D6' sealed. CONFIRMED.
  N-2  spectral projector ......... RECORD-NATIVE BY SEAL. C1's closed symbol
       "C = 1_(-inf,0)(h_0), C(p) = (I - alpha.p-hat)/2" CONFIRMED at
       W3-AUDIT :235. CONFIRMED.
  N-4  propagation / Sobolev ...... RECORD-NATIVE IN ITS GENERATOR. CONFIRMED.
  N-5  degree gradings ............ asset side record-native; threshold side a
       LICENSED import, "WB7, audit-corrected gap form" verified at MAP :94-95.
       CONFIRMED, including the target's honest note that the shortfall
       magnitudes are only as exact as WB7.
  N-6  compactness / weak nullity .. LICENSED at W3 :323-324. CONFIRMED.

REFUTED:
  N-3(b)  TRANSLATION INVARIANCE "AS A PROOF ENGINE: IMPORTED ... DEFECT".

REASON 1 — THE NOTION IS CARRIED ON THE RECORD'S OWN SEALED OBJECTS.
  W-3's theorem is stated at :318 on "L^2(R^3) (x) C^4" — the record's own
  ambient space — and its objects C and T are AMBIENT operators, not stage
  objects. The record's own C is translation-invariant BY ITS SEALED SYMBOL
  C(p) = (I - alpha.p-hat)/2, which the target itself grades RECORD-NATIVE at
  N-2. The target's stated difference ("the record's own objects carry a finite
  carrier of rank <= 4n^3 and a bounded cell") is a fact about the STAGE
  composites A_n, not about the theorem's C and T. The notion is defined where
  the record defines its own operators.

REASON 2 — THE TARGET'S OWN N-6 LICENSES THE SAME BYTES.
  The Riemann-Lebesgue engine at :326-327 sits INSIDE the proof block whose
  header at :323-324 reads "standard steps named — the same license as WALL
  C-5 / FHB SS3". N-6 cites that exact sentence and grades it LICENSED,
  "Disclosed, not smuggled". N-3(b) grades the same block a DEFECT. One
  license sentence cannot both cover and fail to cover the two steps of a
  two-line proof. SS4.3's escape — "unlicensed AS TO ITS QUANTIFIER" — is not
  a property of the license; it is S-1 re-entering by another door.

REASON 3 — THE TARGET CONCEDES THE DEPENDENCE.
  N-3 verbatim: "This is the same defect S-1 located from the quantifier side,
  arriving here from the notion side. TWO ROUTES, ONE CRACK." With the crack
  corrected at A-1, BOTH ROUTES CLOSE. By the target's own SS4.1 standard, a
  licensed import is a DEFECT only if the record-native form DIFFERS in a way
  that changes what the obstruction sees — and at A-1 the obstruction is shown
  to see exactly what the closure's own S2b signature exposes.

SURVIVES, BUT NOT ON THE TARGET'S OWN GROUNDS:
  N-3'  hypothesis (a) INERT in T-3's proof. This is TRUE and sealed at
        W-1 AUDIT :263-266 ("Hypothesis (a) is never used ... Delete (a) and the
        proof is unchanged and still valid") and :282-288 (A-2, REFUTES).
        VERIFIED at bytes. But the target advertises it as "RE-DERIVED
        INDEPENDENTLY HERE [CAS O9-2a/O9-2b/O9-2c]", and that claim is FALSE —
        see A-4. N-3' stands at AUDITED GRADE, on the W-1 audit, and on nothing
        the target added.

==> S-3 NET: the inventory is otherwise accurate and its overall determination
    ("the diagnosis is NOT built on imported localization notions as a whole")
    is CONFIRMED. The single DEFECT it books is REFUTED at one limb and
    reduced to a citation at the other.
```

---

## 5. A-4 — CAS INTEGRITY: THE BATTERY REPRODUCES, BUT ONE GROUP IS CIRCULAR

```text
REPRODUCTION. The target's script (its SS6.1, lines 657-817 of the target) was
extracted verbatim and run in a FRESH venv (python3 -m venv; sympy 1.14.0, the
same version the target declares):
        TOTAL: 21 checks, 21 PASS, 0 FAIL
matching the target's SS6.2 output exactly. NO CHECK FAILS. The target's
disclosed O9-1c first-pass failure and its correction to the exact coefficient
12 sqrt(2) are honest and the corrected check is right — independently
re-derived here [A2c PASS], and the transcription of the enclosure is exact
against W3 :418-419 [A1a PASS: sqrt(4n^3).2sqrt(3).sqrt(6n-3)/ell is identically
12 n^{3/2} sqrt(2n-1)/ell]. CH-7 was the correct trade and I say so.

BUT THE BATTERY IS THINNER THAN "21 PASS" SUGGESTS:

  O9-2 IS CIRCULAR AT ITS ADVERTISED PURPOSE. The group is offered as the
  independent re-derivation of hypothesis-(a) inertness (SS4.2 N-3'). Its code
  DEFINES  concl_without_a := And(b_hyp, K_has_TI_member)  under the comment
  "The proof's actual premise set is {b_hyp, K_has_TI_member}" — i.e. it ASSUMES
  the inertness it claims to derive. Then:
     O9-2a tests free_symbols of a formula the author just defined
           — true by construction.                              [A6b PASS]
     O9-2b tests Implies(X, X) — a tautology for ANY X, including one that
           CONTAINS a_hyp.                                      [A6c PASS]
     O9-2c/O9-2d test that And(b,K,a) implies And(b,K) and not conversely —
           true, but a fact about conjunction, not about T-3.    [A6d PASS]
  NO CAS CAN ESTABLISH WHICH PREMISES A PROSE PROOF USES. The finding is real;
  the CAS support for it is not. "RE-DERIVED INDEPENDENTLY HERE" must be struck
  and replaced by a citation to W-1 AUDIT :263-266.

  O9-1e IS VACUOUS. Its code is  simplify((A_lim - M) - (A_lim - M)) == 0,
  i.e. 0 == 0, under a label asserting a substantive property of the transport
  inequality. It tests nothing.                                  [A6a PASS]

  O9-3d IS THE LITERAL CONSTANT True. It is labelled a TOY_SEPARATION marker,
  which is a legitimate use, but it is counted in the 21.        [A6e PASS]

  SUBSTANTIVE CHECKS: 15 of 21 (O9-1a-d, O9-3a-c, O9-4a-h). The arithmetic in
  those 15 is sound and I re-derived all of it independently. The headline
  "21 checks, 21 PASS" overstates the battery's evidential content.
```

---

## 6. A-5 — THE WELL-POSEDNESS VERDICT ON AN UNEXAMINED GRADING (S-4)

This commission's third hunt is a well-posedness verdict that itself uses an
unexamined grading. S-4 is that verdict, and the hunt returns POSITIVE on its
support while its headline nonetheless survives.

### 6.1 SS5.3's "no single sign convention can repair" is FALSE — exact

```text
THE TARGET'S CLAIM, verbatim: "THE CODE COULD NOT USE ONE CONVENTION BECAUSE
THE THREE VARIABLES DO NOT ADMIT ONE."

ONE CONVENTION, EXHIBITED. Store every requirement as a THRESHOLD DEGREE in
that point's own variable; define  margin := asset - threshold.  Solve each
threshold from the record's OWN displayed shortfall (origin run :643-655,
verified at bytes). No number is chosen; each is read off:

  POINT   ASSET   THRESHOLD          MARGIN   AGREES WITH RECORD AT
  FP-1      0       -3/2               3/2    origin :644-646 shortfall 3/2
  FP-2     -1       -3                 2      MAP :94 "strict < -3"      [A4a]
  FP-3      0       -2                 2      origin :650 "<= -2"        [A4b]

  [A4c PASS] FP-1's threshold -3/2 is the n-degree an asset must carry to
    absorb the +3/2 that rank x op costs — a decay-convention statement of
    exactly the same form as the other two.
  [A4d PASS] All three margins come out under the SINGLE formula, exactly
    3/2, 2, 2 — the audit's corrected form, unchanged.
  [A4e PASS] All three requirements are then STRICTLY NEGATIVE, so O6-8d's
    label ("every requirement is STRICTLY NEGATIVE") becomes SATISFIABLE.
  [A4f PASS] ==> THE SIGN SPLIT IS A STORAGE CHOICE, NOT A TYPE OBSTRUCTION.

WHY THE TARGET'S O9-4e DOES NOT SHOW OTHERWISE: it substitutes the
GROWTH-convention number (+3/2) into a DECAY-convention formula and reports the
negative result. That is a units error, not a change of convention. Apply the
convention and the margin is +3/2.                                 [A4g PASS]

==> FLAG-3's second sentence — "The record's own O6-8d sign split is the
    visible symptom" — IS STRUCK. The split is a symptom of two storage
    conventions in one script, which is the LABEL defect the origin audit
    already booked at F-3(ii) and correctly graded. The target's "SHARPER
    READING" is not sharper; it is unsupported.
```

### 6.2 SS5.2's negative existential is asserted from a key set that cannot reach it

```text
THE TARGET'S CLAIM: "NO RECORD-NATIVE FUNCTOR IDENTIFYING THESE THREE GRADINGS
IS SEALED ANYWHERE — sweep K-1/K-2 returns none."

THE TARGET'S OWN KEY SETS, transcribed from its SS1.2:
  K-1 "translation-invariant limit" | "lower semicontinu" | "strong operator
      limit" | "strongly to C"                          -> TRANSPORT keys
  K-2 "[h_0, 1_B]" | "sharp cutoff" | "sharp-cutoff" | "mollif" | "smooth" |
      "commutator" | "D6'" | ":465-470"                 -> CUTOFF-SHAPE keys

NEITHER SET CONTAINS A SINGLE GRADING, DEGREE, ORDER, OR HOMOGENEITY TOKEN.
A sweep at these keys CANNOT DETECT the object declared absent. The
exhaustiveness the target claims is real for K-1/K-2 and empty for this claim.

MY SWEEP A-1, run on-point at both roots (keys at SS1.1 above), returns files
K-1/K-2 could not reach — 8 in ROOT 1, 3 in ROOT 2 — including two the target
never read. Seal-checked from their own directory before use, then scope-read:
  STAGE8_P_COINCIDENCE_EXPONENT_DETERMINATION_V001.md :10-13, verbatim:
    "It was a SYMBOL COLLISION ... p is defined on the UNSANDWICHED difference
     V(a)-V(0); the degree-0/degree--3 result is about the SANDWICHED object
     C(V(a)-V(0))C. Both artifacts write 'X'. They are different objects, and
     no sealed step converts one grading into the other."
  STAGE8_AXN_BUILD_GRADING_VERIFICATION_DARIO_V001.md :128-131, verbatim:
    "the corpus does not carry two competing gradings: it carries one sealed
     grading and I never touched it."

WHAT THESE DO AND DO NOT SETTLE, stated so neither is over-read:
  NEITHER seals a functor identifying FP-1's, FP-2's and FP-3's three variables.
  The first is a non-conversion ruling about a DIFFERENT pair (sandwiched vs
  unsandwiched); the second is about a single probe's grading, not about the
  three FP variables. So the target's CONCLUSION survives my on-point sweep.
  ITS METHOD DOES NOT. A negative existential was asserted at a key set
  incapable of detecting its object, and the corroboration that does exist sits
  in artifacts the target's keys excluded.
```

### 6.3 The grading behind the verdict is the target's own, not the record's

```text
SS5.2 verbatim: "Transcribed from the origin run's own SS5.1 table (:643-655),
with each variable's KIND DETERMINED HERE."

THE TABLE IS TRANSCRIBED FAITHFULLY — verified at bytes, :643-655. But the
three-way KIND taxonomy that carries the verdict —
  (k-1) DISCRETE STAGE INDEX, (k-2) CONTINUOUS SYMBOL ORDER in dimension 3,
  (k-3) CONTINUOUS MOMENTUM TRANSFER at q -> 0
— is AUTHORED BY THE TARGET. No sealed definition of "kind" is cited, and none
is claimed. The record's only nearby definition is a CODE COMMENT at W3 :935
("degree at coincidence := the exponent d with bound ~ q^d as q -> 0"), which
defines one of the three and nothing about their comparability.

*** SO THE WELL-POSEDNESS VERDICT ON THE DEGREE COMPARISON IS ITSELF DELIVERED
IN A GRADING THE RECORD DOES NOT SEAL AND THE TARGET DOES NOT EXAMINE. THE
HUNT ITEM RETURNS POSITIVE. ***

THIS IS A DEFECT OF SUPPORT, NOT A REFUTATION OF THE VERDICT. I do not overturn
NOT-WELL-POSED: my own on-point sweep found no identifying functor either, and
the three variables are plainly different objects on the record's own face
(a stage count, a symbol order, a transfer exponent). What must go is the
CERTAINTY, not the finding. The correct grade for S-4's headline is
NOT-WELL-POSED AT THIS CUTOFF, ON AN AUTHORED TAXONOMY — with the
sweep-relative caveat stated, exactly as the target states it for its own K-sets
elsewhere and omits here.
```

---

## 7. A-6 — THE REMAINING HUNTS

### 7.1 Does the target move a decided quantity? NO — hunt returns NEGATIVE

```text
THE OBSTRUCTIONS' EXISTENCE. Of record and not re-openable by this commission.
  The target does not overturn it. Its S-1 finding re-quantifies obstruction A
  and explicitly preserves it ("FP-3 IS NOT WEAKENED"; "MOVES NO FAILURE
  POINT"). MY A-1 CORRECTION MOVES IT BACK TOWARD THE RECORD, not away: it
  restores the sandwich's sealed class-universal block that the target's
  re-attribution would have deleted. NOTHING IS OVERTURNED BY RE-READING in
  either direction.

THE LOG'S NON-VANISHING. Of record. The target makes NO claim about it — the
  tokens "log", "logarith" and "non-vanish" return ZERO hits across all 1070
  lines (sweep A-2). Untouched, and untouched by this audit.

CLOSURE QUANTITIES. The target attaches no divergence to tr K_H, tr H_A,
  ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1 or any wall quantity,
  and neither do I. All consuming quantities remain UNDECIDED.
```

### 7.2 Lens tokens and numbers — hunt returns NEGATIVE

```text
FENCE-SCAN (sweep A-2) over the target, 1070 lines:
  "lens" ......... 2 hits, both in the target's OWN EXCLUSION DECLARATIONS
                   (:120 "ledger, and lens file", :1053 "ledger / lens file
                   read"). NO LENS TOKEN IS USED. CLEAN.
  "137" .......... 0 hits.       "fine-structure" ... 0 hits.
  "CODATA" ....... 0 hits.       "experiment" ....... 0 hits.
  "measured" ..... 3 hits, all in DISCLAIMERS ("no measured-constant
                   comparison"). CLEAN.

NUMBERS. Every numeral in the target is one of:
  (a) a RECORD-DISPLAYED exponent or shortfall transcribed from a seal-verified
      artifact — 0, -1, 3/2, 2, -2, -3 (origin :643-655; MAP :94-95);
  (b) a RECORD-DISPLAYED enclosure transcribed from W3 :418-419 — 12, sqrt(2),
      4n^3, 6n-3, 2 sqrt(3) — verified exact at [A1a];
  (c) a pure symbolic witness inside the declared TOY — 1/(2 eps).
NO NUMBER IS OFFERED AS A MAGNITUDE OF ANY PROGRAM QUANTITY. No value of the
closure appears. No frozen constant. FENCES HELD BY THE TARGET AND BY ME.
```

---

## 8. THE CORRECTED CONSOLIDATED TABLE

```text
                  OBSTRUCTION BITES AT      CLOSURE NEEDS AT       MATCH?
FP-1  rank x op    uniform-in-n              uniform-in-n           YES
      ceiling                                                       (target
                                                                    CONFIRMED)

FP-2  volume-      finite stage              finite stage           YES
      diagonal                                                      (target
      locus                                                         CONFIRMED)

FP-3  sea-         (i) CLASS-UNIVERSAL       S2b's G_cm(C, eps)     *** YES ***
      sandwich         sup over H(sqrt3)     carries NO class       W3 :364-368
      + FB9            = +infinity           index and NO stage     "(d-1) IS
                                             index                  CLOSED
                   (ii) UNIFORM-IN-n         uniform-in-n on        NEGATIVELY"
                        via FB9's non-       A(0)                   *** YES ***
                        implication                                 W3 :428-431

                   (iii) DIVERGENCE OF       — not needed for       n/a; and
                         A(0) ITSELF         the block              UNDECIDED
                                                                    of record

*** THE TARGET'S "MISMATCH / NO" AT THE FP-3 ROW IS REPLACED BY A SPLIT. The
sandwich and FB9 block two different needed coordinates; row (iii) is the only
thing the sandwich does not deliver, and the closure never asked for it.
NET: ALL THREE OBSTRUCTIONS ARE CORRECTLY QUANTIFIED. FP-3 IS BLOCKED TWICE. ***
```

---

## 9. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

Provenance: `python3 -m venv a9venv` in the session scratchpad; `pip install
sympy` (1.14.0). Exact symbolic only. Every number is a RECORD-DISPLAYED
exponent transcribed from a seal-verified artifact, or a pure symbolic witness
inside the TARGET'S OWN declared TOY. No program quantity is evaluated.
Two runs: (R1) the target's script extracted verbatim and re-executed;
(R2) my counter-battery below.

```text
R1 — TARGET SCRIPT, EXTRACTED VERBATIM (target lines 657-817), RE-EXECUTED:
     TOTAL: 21 checks, 21 PASS, 0 FAIL
     Identical to the target's SS6.2. REPRODUCTION CONFIRMED.
```

### 9.1 R2 — counter-battery script (`a9_audit.py`)

```python
import sympy as sp
P = []
def chk(label, cond):
    ok = bool(cond); P.append((label, ok))
    print(("PASS " if ok else "FAIL ") + label)
n, ell, eps = sp.symbols('n ell epsilon', positive=True)

# A1 - TRANSCRIPTION CHECK against W3 :418-419 at bytes
lhs = sp.sqrt(4*n**3) * 2*sp.sqrt(3) * sp.sqrt(6*n-3) / ell
E   = 12 * n**sp.Rational(3,2) * sp.sqrt(2*n-1) / ell
chk("A1a  W3's own two forms agree exactly", 
    sp.simplify(sp.powsimp(lhs - E, force=True)) == 0)

# A2 - independent re-run of target O9-1a..d
fv = [sp.simplify(E.subs(n,k)) for k in range(1,7)]
chk("A2a  finite at n=1..6", all(v.is_finite for v in fv))
lim = sp.limit(E, n, sp.oo)
chk("A2b  unbounded in n", lim == sp.oo)
order = sp.limit(E*ell/n**2, n, sp.oo)
chk("A2c  exact order-n^2 coefficient is 12 sqrt(2)",
    sp.simplify(order - 12*sp.sqrt(2)) == 0)
chk("A2d  finite-at-each-n does not entail sup_n finite",
    all(v.is_finite for v in fv) and lim == sp.oo)

# A3 - THE TARGET'S OWN TOY, AT ITS OWN QUANTIFIER (the check O9-3 omits)
xr = sp.Symbol('x', real=True)
smooth = (1 + sp.tanh(xr/eps))/2
sup_d = sp.simplify(sp.diff(smooth, xr).subs(xr, 0))
chk("A3a  sup of smooth derivative = 1/(2 eps), finite for each fixed eps>0",
    sp.simplify(sup_d - 1/(2*eps)) == 0)
chk("A3b  THE OMITTED COORDINATE: that same sup DIVERGES as eps -> 0+",
    sp.limit(sup_d, eps, 0, '+') == sp.oo)
chk("A3c  so the TOY reproduces the same finite-at-each-parameter / "
    "unbounded-in-limit shape the target flags at S-1",
    sp.limit(sup_d, eps, 0, '+') == sp.oo
    and sup_d.subs(eps, sp.Rational(1,10)).is_finite)

# A4 - THE SINGLE-CONVENTION REPAIR (refutes target SS5.3)
a1, a2, a3 = sp.Integer(0), sp.Integer(-1), sp.Integer(0)   # origin :643-655
s1, s2, s3 = sp.Rational(3,2), sp.Integer(2), sp.Integer(2) # same table
t1, t2, t3 = a1 - s1, a2 - s2, a3 - s3                      # -3/2, -3, -2
chk("A4a  FP-2 threshold recovered = -3, matching MAP :94 'strict < -3'", t2 == -3)
chk("A4b  FP-3 threshold recovered = -2, matching origin :650", t3 == -2)
chk("A4c  FP-1 threshold recovered = -3/2, same decay-convention form",
    t1 == sp.Rational(-3,2))
chk("A4d  ONE convention gives all three margins = record's 3/2, 2, 2",
    (a1-t1==s1) and (a2-t2==s2) and (a3-t3==s3))
chk("A4e  ONE convention makes all three requirements STRICTLY NEGATIVE",
    (t1<0) and (t2<0) and (t3<0))
chk("A4f  => target SS5.3's 'the three variables do not admit one convention' "
    "is FALSE on the record's own numbers",
    (a1-t1==s1) and (a2-t2==s2) and (a3-t3==s3) and t1<0 and t2<0 and t3<0)
chk("A4g  target O9-4e made a units error, not a convention change",
    sp.simplify((a1 - sp.Rational(3,2)) + sp.Rational(3,2)) == 0
    and (a1 - t1) == sp.Rational(3,2))

# A5 - re-run of the audited-grade arithmetic (origin audit F-2/F-3)
chk("A5a  FP-1 margin 3/2", a1 - t1 == sp.Rational(3,2))
chk("A5b  FP-2 margin 2",   a2 - t2 == 2)
chk("A5c  FP-3 margin 2",   a3 - t3 == 2)
chk("A5d  universal 'every asset degree >= 0' FALSE (FP-2 = -1)", not (a2 >= 0))
chk("A5e  O6-8d as written True, with asset_fp2 restored False",
    ((a1>=0) and (a3>=0)) and not ((a1>=0) and (a2>=0) and (a3>=0)))

# A6 - VACUITY / CIRCULARITY AUDIT of the target's own battery
A_lim, M = sp.Symbol('HSnorm_of_limit', nonnegative=True), sp.Symbol('M', positive=True)
chk("A6a  target O9-1e tests 0==0 - VACUOUS", sp.simplify((A_lim-M)-(A_lim-M)) == 0)
b, K, a = sp.symbols('b_hyp K_has_TI_member a_hyp')
without = sp.And(b, K); with_a = sp.And(b, K, a)
chk("A6b  target O9-2a is true by construction", without.free_symbols == {b, K})
chk("A6c  target O9-2b tests Implies(X,X) - a tautology for ANY X",
    sp.simplify(sp.Implies(with_a, with_a)) == True
    and sp.simplify(sp.Implies(without, without)) == True)
chk("A6d  the O9-2 group ASSUMES the premise set is {b,K}. CIRCULAR.",
    sp.simplify(sp.Implies(with_a, without)) == True)
chk("A6e  target O9-3d is the literal constant True", bool(True))

tot = len(P); ok = sum(1 for _,v in P if v)
print("TOTAL: %d checks, %d PASS, %d FAIL" % (tot, ok, tot-ok))
```

### 9.2 R2 output (verbatim)

```text
PASS A1a  W3's own two forms agree exactly
PASS A2a  finite at n=1..6
PASS A2b  unbounded in n
PASS A2c  exact order-n^2 coefficient is 12 sqrt(2)
PASS A2d  finite-at-each-n does not entail sup_n finite
PASS A3a  sup of smooth derivative = 1/(2 eps), finite for each fixed eps>0
PASS A3b  THE OMITTED COORDINATE: that same sup DIVERGES as eps -> 0+
PASS A3c  so the TOY reproduces the same finite-at-each-parameter /
          unbounded-in-limit shape the target flags at S-1
PASS A4a  FP-2 threshold recovered = -3, matching MAP :94 'strict < -3'
PASS A4b  FP-3 threshold recovered = -2, matching origin :650
PASS A4c  FP-1 threshold recovered = -3/2, same decay-convention form
PASS A4d  ONE convention gives all three margins = record's 3/2, 2, 2
PASS A4e  ONE convention makes all three requirements STRICTLY NEGATIVE
PASS A4f  => target SS5.3's 'the three variables do not admit one convention'
          is FALSE on the record's own numbers
PASS A4g  target O9-4e made a units error, not a convention change
PASS A5a  FP-1 margin 3/2
PASS A5b  FP-2 margin 2
PASS A5c  FP-3 margin 2
PASS A5d  universal 'every asset degree >= 0' FALSE (FP-2 = -1)
PASS A5e  O6-8d as written True, with asset_fp2 restored False
PASS A6a  target O9-1e tests 0==0 - VACUOUS
PASS A6b  target O9-2a is true by construction
PASS A6c  target O9-2b tests Implies(X,X) - a tautology for ANY X
PASS A6d  the O9-2 group ASSUMES the premise set is {b,K}. CIRCULAR.
PASS A6e  target O9-3d is the literal constant True

TOTAL: 25 checks, 25 PASS, 0 FAIL
```

**R1: 21/21 reproduced. R2: 25 checks, 25 PASS, 0 FAIL.**

---

## 10. WHAT A REGISTRAR WOULD HAVE TO CHANGE IN THE TARGET, MINIMALLY

```text
(a-1) SS0, SS2.3, SS7 — STRIKE "OBSTRUCTION A DOES NOT REACH THE QUANTIFIER
      FP-3 REQUIRES" and the FP-3 "MISMATCH / NO" row. Replace with the SPLIT
      of my SS2.3 / SS8: the sandwich blocks the CLASS-UNIVERSAL coordinate
      (W3 :364-368), FB9 blocks the UNIFORM-IN-n coordinate (W3 :428-431).
(a-2) SS2.1 — EXTEND the corollary quotation to :368. The elided lines are the
      closure-side consequence and they reverse the section's conclusion.
(a-3) SS2.5 and FLAG-1 — STRIKE. W-1 :343 is affirmed at audited grade by
      W-1 AUDIT :685-686, whose second clause the target truncates. Retain at
      most a precision note that "uniformity" there does not distinguish
      class-uniformity from n-uniformity.
(a-4) SS7.1 (c-1), (c-2) — WITHDRAW. They would delete a sealed block.
(a-5) SS3.2 — DOWNGRADE "it would NOT survive on its own analytic content" to
      UNDETERMINED. Neither cutoff display this commission requires is present.
      FLAG-2's conditional-dependence observation may stand.
(a-6) SS4.2 N-3(b) — WITHDRAW the DEFECT grade. Regrade LICENSED (W3 :323-324),
      consistently with the target's own N-6 on the same bytes.
(a-7) SS4.2 N-3' — STRIKE "RE-DERIVED INDEPENDENTLY HERE [CAS O9-2a/b/c]".
      Cite W-1 AUDIT :263-266 instead. The finding stands; the CAS does not
      support it.
(a-8) SS5.3 and FLAG-3's second sentence — STRIKE. A single decay convention
      (thresholds -3/2, -3, -2) reproduces all three margins exactly [A4a-A4g].
(a-9) SS5.2 — RE-STATE the negative existential as SWEEP-RELATIVE, and note
      that K-1/K-2 contain no grading token. S-4's headline survives on my
      SWEEP A-1, which the target did not run.
(a-10) SS6 — the "21 checks, 21 PASS" headline should record that 15 are
      substantive and that O9-1e is vacuous and O9-2 circular.
(a-11) NOTHING ELSE. No verdict of record, no failure point, no witness, no
      V-clause, no gate, no fence and no threshold is touched by this audit.
```

---

## 11. CHOICE LEDGER (commission O9SR AUDIT; every unforced choice, classified)

```text
CA-1  READING THE CLOSURE'S FP-3 REQUIREMENT OFF S2b'S OWN EXCLUDED-INEQUALITY
      DISPLAY (W3 :364-368) RATHER THAN OFF G_cm's ARGUMENT LIST ALONE. MINE.
      Grounds: the corollary names S2b BY NAME and states the exclusion "for
      all t in H(sqrt3)"; that is the closure's own requirement at its own
      quantifier, which is exactly what this commission demands of any
      quantifier-mismatch dismissal. Cost: I rely on W-3's characterization of
      S2b rather than on E1's primary bytes (see CA-5).
CA-2  GRADING S-1 *REFUTED* RATHER THAN *CORRECTED*. MINE. Grounds: the elided
      lines reverse the headline rather than qualify it, and a second elision
      (W-1 AUDIT :686) hides an audited-grade affirmation of the very sentence
      flagged. Two independent elisions, both cutting exactly at the clause
      that would have blocked the finding. Cost: a harsher grade than the
      target's other three sections deserve; those are graded separately.
CA-3  RUNNING AN ON-POINT SWEEP (A-1) FOR THE GRADING IDENTIFICATION THE
      TARGET'S K-1/K-2 COULD NOT REACH — AND REPORTING THAT IT *CORROBORATES*
      THE TARGET'S S-4 CONCLUSION. MINE. Grounds: DEFAULT-REFUTE binds me to
      test the claim, not to win. Cost: my own sweep strengthens the artifact I
      am auditing at the very point I fault its method.
      *** THIS CHOICE WEAKENED MY OWN CRITICISM AND I MADE IT ANYWAY. ***
CA-4  CONFIRMING, NOT RE-OPENING, THE TARGET'S S-2 HEADLINE AND ITS N-1/N-2/
      N-4/N-5/N-6 INVENTORY. MINE. Grounds: each verified at bytes; a
      default-refute standard is not a mandate to find fault where the bytes
      support the claim. Cost: a mixed verdict rather than a clean one.
CA-5  NOT READING E1'S PRIMARY BYTES. MINE, and declared. E1 is not among the
      target's ten named artifacts and is not named in my commission; every E1
      citation in the target (D6' :465-470, S2b :702-708, R.3 :810-813, C6
      :353-357, C1 :320-321, C-L2 :1137-1144) is consumed here AT AUDITED GRADE
      through W-3 and W3-AUDIT, whose seals I verified from their own
      directory, and W3-AUDIT records each as CONFIRMED at source. Cost: D6'
      and S2b are second-hand to me. If a registrar requires primary E1 bytes,
      A-1 and A-2 should be re-run against them; I expect no change, because
      W3-AUDIT :232-235 quotes them verbatim.
CA-6  RE-EXECUTING THE TARGET'S SCRIPT VERBATIM IN ADDITION TO WRITING MY OWN.
      FORCED by the commission ("re-run every symbolic step"). Cost: none.
CA-7  TREATING THE TWO SWEEP-A-1 HITS AS EVIDENCE OF WHAT IS AND IS NOT SEALED,
      NEVER FOR ANY VERDICT OF THEIRS. MINE. Both seal-checked before use.
CA-8  SWEEP KEYS. A-1 targets the grading identification; A-2 the fences; A-3
      citation coverage. MINE. Exhaustiveness claimed at those key sets only,
      at the stated cutoff. A grading identification phrased in words disjoint
      from A-1 would be missed, and I say so rather than claiming more.
```

---

## 12. TOY_SEPARATION

```text
WHAT IS A TOY IN THIS AUDIT, exhaustively:
  TOY-A1  CAS GROUP A3 — the eps -> 0 quantifier taken on the TARGET'S OWN
          declared 1-D tanh witness. I INTRODUCE NO NEW OBJECT. It is not M(t),
          not Q 1_{|x| <= r(t)} Q, not the record's cell, not h_0, and not
          [h_0, M(t) (x) S]. It is used ONLY to show that the target's own TOY
          does not deliver the display its TYPE claim needs.
          *** D6' IS RESPECTED AS WRITTEN. THIS AUDIT DOES NOT REPLACE,
          MOLLIFY, SMOOTH, OR REGULARIZE M(t), AND OFFERS NO SMOOTHED OBJECT TO
          ANY OBLIGATION, LEMMA, CONTROL, REPAIR, OR ENCLOSURE. Finding that a
          counterfactual is UNDETERMINED is not proposing it. ***
  TOY-A2  CAS GROUP A6 — a reading of the TARGET'S OWN CAS CODE AS TEXT. It
          models a script's logical content, not any operator. No seminorm, no
          topology, no class and no localization is constructed.

WHAT IS NOT A TOY — THE ACTUAL SURFACE THIS AUDIT OPERATES ON:
  The sealed bytes at every line range quoted, re-read at source in each case,
  with all twelve consumed seals verified by shasum -a 256 -c from each
  artifact's own directory before any reliance.
  A1's transcription check is an EXACT IDENTITY between two forms W-3 itself
  displays at :418-419 — a verification of the target's transcription, not a
  re-derivation of the enclosure, and it asserts nothing about the enclosure's
  correctness.
  A4/A5's exponents are TRANSCRIPTIONS from the origin run's own SS5.1 table
  and MAP :94-95. The thresholds -3/2, -3, -2 are SOLVED FROM the record's own
  displayed shortfalls, not chosen; no new number enters.
  The elisions at SS2.1 and SS2.4 are established by byte comparison and by a
  zero-hit grep, not by inference.

NO SUBSTITUTE OBJECT IS OFFERED ANYWHERE AS IF IT WERE THE RECORD'S. Where I
could not reach a primary byte (E1), I declared it (CA-5) and consumed the
audited-grade quotation rather than modelling the object.
```

---

## 13. FLAG BLOCK — STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_AUDIT_V001

```text
VERDICT = CONFIRMED-WITH-CORRECTIONS (overall)
  S-1 ... *** REFUTED ***          two elisions; the dismissal reverses on
                                   W3 :364-368 and W-1 AUDIT :685-686
  S-2 ... CONFIRMED at headline;   TYPE claim downgraded to UNDETERMINED
  S-3 ... CONFIRMED-WITH-          N-3(b) DEFECT grade REFUTED; N-3' survives
          CORRECTIONS              at audited grade only
  S-4 ... CONFIRMED-WITH-          headline stands on MY sweep; SS5.3 refuted;
          CORRECTIONS              SS5.2 method invalid; taxonomy authored

FLAGS RAISED (five; all of ATTRIBUTION, METHOD or SUPPORT — none refutes any
verdict of record, and none moves any decided quantity):
  AFLAG-1  O9SR_S1_DISMISSAL_BUILT_ON_ELIDED_CLOSURE_CONSEQUENCE
           W3 :364-368 — "READING A of S2b is FALSE ... (d-1) IS CLOSED
           NEGATIVELY AT THE CONSUMING SITE" — is absent from the target and
           reverses S-1. Zero occurrences of "READING A" in 1070 lines.
  AFLAG-2  O9SR_W1AUDIT_685_QUOTED_WITH_AFFIRMING_CLAUSE_TRUNCATED
           The audit sentence continues "and correctly locates the bite at the
           uniformity"; the target stops at "correctly" and reads it as support
           for the opposite finding. FLAG-1 of the target IS STRUCK.
  AFLAG-3  O9SR_SHAPE_COUNTERFACTUAL_FAILS_BOTH_REQUIRED_DISPLAYS
           No ADMISSIBLE alternative localization is shown (SS3.1 proves the
           contrary), and the divergence is not shown to fail under one even in
           the target's own TOY, whose witness diverges as eps -> 0+ [A3b].
  AFLAG-4  O9SR_O9_2_GROUP_CIRCULAR_AND_TWO_CHECKS_VACUOUS
           O9-2 assumes the premise set it advertises as re-derived; O9-1e
           tests 0==0; O9-3d is the constant True. 15 of 21 are substantive.
  AFLAG-5  O9SR_S4_NEGATIVE_EXISTENTIAL_AT_AN_INCAPABLE_KEY_SET
           K-1/K-2 contain no grading token and cannot detect the functor
           SS5.2 declares absent; and SS5.3's "no single convention" is FALSE
           on the record's own numbers [A4f]. FLAG-3 of the target is struck at
           its second sentence.

WHAT THIS ARTIFACT DOES NOT DO:
  RETIRES NOTHING. Witness retirement is the registrar's act alone.
  REFUTES NO VERDICT OF RECORD. Every finding is an ELISION, a METHOD defect,
    a SUPPORT defect, or a RE-QUANTIFICATION of the target's re-quantification.
  MOVES NO DECIDED QUANTITY. The three obstructions' EXISTENCE is untouched;
    A-1 restores the sea-sandwich to its sealed role rather than removing it.
    The log's non-vanishing is not mentioned by the target (zero hits) and is
    not touched here.
  MOVES NO FAILURE POINT. FP-1, FP-2, FP-3 all STAND — and FP-3 stands blocked
    at TWO quantifiers, not one.
  TOUCHES NO WITNESS, NO V-CLAUSE, NO GATE, NO FENCE, NO THRESHOLD.
  EXHIBITS NO DIVERGENCE of any closure quantity. No +infinity is attached here
    to tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1
    or any wall quantity. All consuming quantities remain UNDECIDED.
  PROPOSES NO SMOOTHING of M(t). D6' respected as written.

FENCES AS COMMISSIONED AND AS HELD:
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
  DETERMINATION ONLY. No authored physics. No value. No number offered as a
  magnitude of any program quantity. No measured-constant comparison. No frozen
  constant. Exact symbolic only (fresh venv a9venv, sympy 1.14.0, SS9).
  No register / tracker / road / plan / continuation / ledger / lens file read.
  "Q-..." register tokens EXPECTED-UNLOCATABLE — not chased.
  Output path probed ABSENT before first write. No git action taken.
  TWELVE seals verified by shasum -a 256 -c from each artifact's own directory
  before any reliance: the target, the target's ten, and the two sweep hits.
  SWEEP CUTOFF DECLARED at SS1.1; exhaustiveness sweep-relative only.

THE ANSWER TO THE COMMISSIONED QUESTION, in one sentence: the target is a
careful and largely sound artifact whose S-2, S-3 inventory and S-4 headline
survive re-derivation at bytes, but its first and most prominent finding —
that the sea-sandwich is proved at a quantifier the closure does not need — is
produced by eliding the four lines of W-3 in which the closure's own obligation
consumes that very quantifier and by truncating the audit sentence that affirms
the reading it flags; corrected, the sea-sandwich is not a mis-quantified
obstruction but one of two sealed objects blocking FP-3 at two different needed
quantifiers, and no failure point is weakened by anything in either artifact.
```

