# STAGE 8 — C-L2 QUADRATIC-FORM CERTIFICATION AND THE G_cm INPUT

## BLIND BUILDER — CODENAME CL2-BUILD — [SEALED]

Date: 2026-08-14
Commission: road item 5, elevated — certify C-L2's quadratic form (the sole
permitted route for the record-kink commutator [error] layer) and the G_cm
input, from sealed stock, or refuse with exact blockers. C-L2 is also the
wall's F3 blocker.

Gates, LIVE THROUGHOUT: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv,
script + output verbatim §7); no floats as ground, no measured constants, no
value frozen. No file matching register|road_|ledger|lens|plan|tracker|
THE_HANDOFF opened. "Q-..." tokens EXPECTED-UNLOCATABLE, noted not chased.
No git; nothing registered/committed/pushed; no existing file edited. Output
path probed ABSENT before write.

---

## 0. VERDICT IN ONE LINE

**C-L2 = PARTIAL: the quadratic form is NAMED and the commutator's form bound
is CERTIFIED EXACTLY — |⟨psi, [h_0, M(t)⊗S] psi⟩| <= 2·sqrt(3)·||h_0 psi||·
||psi|| on D(h_0), every t, every admitted cell, relative form bound ZERO
w.r.t. h_0², via the exact anticommutator flip [h_0, M⊗S] = Σ_j {p_j, M}⊗α_jS
(sealed CL-ERROR J3 ground, independently re-derived, CAS 19/19) — and the
operator-route exclusion is displayed exactly WHY (witness family with bounded
L² mass and divergent sphere trace; the form does not extend to L², so no
bounded operator represents it — the of-record ||[h_0, 1_B]|| = +infinity
:1139-1141 made mechanism-explicit); BUT the clause's "Supplies S2b" is NOT
discharged: R.3's consuming site is HS-typed (||A(0)||_2 <= |C|_4^beta G_cm),
and the form-to-HS distance is blocked at three named of-record points
(B-L2* stratum-resummation FAILS-AT, 6e81ae92/2699af25; R-L2b beta a symbol,
SCAD_HS_SCALING_EXPONENT_UNDERIVED; the wall's F1 rank×op grade ceiling).
G_cm = NOT-DERIVABLE-TODAY at its consuming type, blockers named and swept.
WALL F3: NOT unblocked — per the sealed conditional and no stronger; what
tightens tonight is WHAT the modulo-layer is (its generator is now
form-certified with relative bound zero), not its discharge. The witness
E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS — retirement is the
registrar's; this artifact retires nothing.**

---

## 1. SEAL TABLE — VERIFIED AT PATH BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/` unless noted.
Every digest recomputed from bytes at path THIS session (shasum -a 256); every
sidecar read and matched. None unverifiable.

```text
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md                  MATCH (tasked 46846730) [E1]
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md                         MATCH (tasked a903716c) [RL0]
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                                 MATCH (tasked 685afac8)
80db260fa1561d76296d5f54e1e52397b79009b8a0d12bc060c140818c38fdf7  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                    MATCH (tasked 80db260f) [WALL]
46938251aeaa9af3541633691ecdd9f5939508e70bffbc43034b589c4945fa63  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md              MATCH (tasked 46938251)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (E1's pin :82) [PA]
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md                        MATCH (sidecar) [CL-ERROR — found by sweep, §1.1]
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                               MATCH (sidecar) [BL2STAR]
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                                 MATCH (sidecar) [BL2STAR-CHK]
```

Pin chased: PA is E1's own sealed definition carrier for
`(h_0, M(t), v(t), b_D, J, S, w_lambda)` (E1 :82, SEAL_MATCH row reproduced).
`tau_R = pi/sqrt2` derivation (b786db3a) is NO_SEAL_FILE carve-out B of record
(E1 :88) — consumed only through E1's own sealed displays, not opened as ground.

### 1.1 Sweep-displayed exhaustiveness (both corpus roots, minus fenced classes)

```text
$ cd "/Users/bgm/MB Work" && grep -rlE "G_cm|RECORD_KINK_COMMUTATOR" \
    alpha-program-archive alpha_supervision | \
    grep -viE "register|road_|ledger|lens|plan|tracker|THE_HANDOFF" | sort
  -> 43 paths listed; total hits including fenced classes = 47
  -> 4 fenced-class paths NOT OPENED (pattern register|road_|ledger|lens|plan|tracker|THE_HANDOFF)
CLASSED, all 43:
  CONSUMED AT BYTES (9): E1, RL0, RL0-CHK, WALL, WALL-AUDIT, PA, CL-ERROR,
    BL2STAR, BL2STAR-CHK (seal table above).
  SUPERVISION-LANE, NOT SEALED DEFINITION CARRIERS, NOT OPENED (9 + 9 mirror
    duplicates): BOHM_* (5), CONTINUATION_STATE.md, ROUTE_MEMO_2026-07-26,
    R_L4A_N_CLAUSE_V002_{PREPARED,ADOPTED} — supervision commentary; the
    definition of record for G_cm lives in E1 S2b (:702-708) and its RL0
    restatement (:360-375), both consumed. Identical basenames appear under
    both roots (alpha_supervision and alpha-program-archive/supervision):
    mirror copies, classed once.
  PRIOR-LINEAGE WORKSPACE ARTIFACTS, CONSUMPTION-SITE MENTIONS ONLY (16):
    CHAIN_*, PUSHBACK_RESOLUTION_SWEEP, STAGE8_F5_*, STAGE8_R1_*, STAGE8_R3C_*,
    STAGE8_RL1_RL2_*, STAGE8_RL2B_*, STAGE8_SUMMED_S2_*, STAGE8_T7_E1_..._V001
    (superseded by V002 of record), STAGE8_T7_REFINEMENT_*, STAGE8_X_TO_KAPPA_*,
    STAGE8_ZERO_FREE_* — none is a definition carrier for [h_0, M(t)⊗S] or
    G_cm; none certifies the C-L2 form (verified by the sweep's own hit
    context: all reference the witness or the S2b slot, none contains a
    certification display).
EXHAUSTIVENESS CLAIM, exact scope: no non-fenced file in either root
containing the tokens G_cm or RECORD_KINK_COMMUTATOR is unclassed above.
```

---

## 2. SUB-TASK (1) — THE OBJECTS AT BYTES

### 2.1 What C-L2 demands, at its own clause (E1 :1137-1144, verbatim)

```text
C-L2  COMMUTATOR ERROR — *** THE WHOLE CONTENT OF THIS ROUTE, AND (per §R.6)
      OF SCAD'S SURVIVING CONSTANTS. *** Control [h_0, M(t) ⊗ S] in a
      QUADRATIC-FORM / Besov-type norm. Norm-based control is unavailable
      (||[h_0, 1_B]|| = +infinity for first-order h_0 against a sharp
      indicator), so the certification must be in a quadratic form and the
      artifact must state WHICH form and WHY the operator-norm route is
      excluded. Supplies S2b.
      Witness: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED
```

"Certified" at this clause therefore = three obligations: (o1) STATE WHICH
quadratic form; (o2) CONTROL the commutator in it, exactly; (o3) state WHY the
operator-norm route is excluded. Plus a consumption interface: (o4) "Supplies
S2b". The clause's own naming act is assigned to the artifact ("the artifact
must state WHICH form") — the naming below is that act, not an import.

### 2.2 What the [error] layer contains (sealed, exact)

C-L1 (E1 :1131-1136): `u_lambda = (I - P) + phase_lambda · P + [error]`.
The sealed CL-ERROR decomposition (3b9730e0, §2.2, exact at the sealed-display
level) resolves the weight-summed error layer:

```text
U(0) = P + E ,   E = E_prof + E_int :
E_prof = (g - 1)·1_ball ⊗ I = -sin²(8 pi |x|⁴)·1_ball ⊗ I     (C-L1 profile
         deficit; degree-0 intraband; Hermitian, k-independent, exact)
E_int  = Σ_lambda w_lambda (-i lambda) ∫_0^1 Omega_lambda(1,t) v(t) D(t)
         Omega_lambda^eq(t,0) dt ,                      (C-L2/S2b content)
D(t)   = ∫_0^t e^{i h_0 s} i[h_0, M(t) ⊗ S] e^{-i h_0 s} ds .   (exact Duhamel)
```

Every E_int term is ONE C-L2 insertion dressed by norm-1 propagators; the
C-L2 generator is exactly `[h_0, M(t) ⊗ S]`, whose sealed exact kernel display
(CL-ERROR J3) is the anticommutator flip re-derived and CAS-verified in §3.

### 2.3 G_cm's definition of record and exact position

Definition (E1 S2b :702-708, verbatim core):

```text
S2b. G_cm(C, eps) := |C|_4^{-beta} · (certified bound on the C-L2 commutator
       error [h_0, M(t) ⊗ S] in the quadratic form C-L2 names)
      [... beta is derived with alpha under R-L2b.
       Witness: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED.]
```

Position (E1 R.3 :803-812; RL0 §3.2 :358-370, exponent-explicit):

```text
x(C,eps) = |C|_4 · ( G_tr + rho_res · G_hs · ( G_cm + G_hs ) )
consuming derivation (E1 :810-813): ||A_s||_2 <= ||A(0)||_2 + ||Delta||_2
   <= |C|_4^{beta} G_cm + |C|_4^{alpha} G_hs   — A(0) IS the C-L2 error,
   and the consuming site is HILBERT-SCHMIDT-typed.
Of-record unvalued items at that site (RL0 :377-391): u-1 alpha/beta SYMBOLS
   (R-L2b open); u-2 rho_res = H-R (may never default); u-3 G_cm form
   uncertified (the witness); u-4 per-pair carrier sup +infinity of record
   (52f2490b) — x exists only as the fixed-n family x_n.
```

TYPE NOTE, displayed honestly (no clause repair attempted): S2b's parenthetical
consumes "the certified bound ... in the quadratic form C-L2 names" (form-
typed), while R.3's derivation display consumes `||A(0)||_2` (HS-typed). Both
readings are carried in §5; under either, x stays indeterminate tonight
(u-1/u-2/u-4 stand regardless of tonight's result).

### 2.4 The wall's F3, at bytes (WALL :414-419)

```text
F3 THE ERROR-TERM STEP — the closure's exact identities hold modulo
   the C-L1 [error]; its only permitted certification (C-L2 quadratic
   form) is UNCERTIFIED of record with the operator-norm route
   excluded (||[h_0, 1_B]|| = +infinity); the odd sector's would-be
   single-composite majorant lives in exactly that layer.
```

F3's consuming site: a single-composite majorant of the odd sector's stay-
strata content — the four wall quantities are TRACE/HS-typed (tr K_H, tr H_A,
||K_H||_2², ||H_A||_2²), and the wall's own F1 is the proved rank×op ceiling
on converting op/form-grade control to that type (WALL :408-413, WB6 = r-3 D10
re-pinned).

---

## 3. SUB-TASK (2) — THE C-L2 ATTEMPT

### 3.1 (o1) THE FORM, NAMED

**The h_0-relative quadratic form.** For psi in Q := D(h_0) = H¹(R³) ⊗ C⁴
(the operator domain of the massless Dirac h_0 = Σ_j p_j ⊗ α_j; C1's own
object), define

```text
q_t(psi) := ⟨psi, [h_0, M(t) ⊗ S] psi⟩ .
```

Besov-type variant, displayed for the clause's "/ Besov-type" branch: by the
boundary-trace display (§3.4, mechanism (ii)) q_t extends to the sphere-trace
pairing i∮_{∂B_r(t)} psi†(α·n̂ ⊗ S)psi dS, controlled on the Besov space
B^{1/2}_{2,1}(R³) (the sharp trace domain). The certification below is stated
and closed on Q = D(h_0); the Besov display is named, not needed.

### 3.2 (o2) THE CERTIFIED CONTROL — exact, with the derivation displayed

**Step 1 — the anticommutator flip (sealed CL-ERROR J3 ground, re-derived
here; CAS B1c + B2).** With M any bounded Hermitian spatial operator (in
particular the sealed sharp M(t) = Q 1_{|x|<=r(t)} Q, NEVER smoothed, and its
continuum reading 1_{B_r(t)}), using only {S, α_j} = 0, [M, α_j] = [M, S] = 0,
[p_j, S] = 0:

```text
[h_0, M ⊗ S] = Σ_j (p_j M ⊗ α_j S − M p_j ⊗ S α_j) = Σ_j {p_j, M} ⊗ α_j S .
```

The degree-(+1) unbounded commutator is EXACTLY a sum of anticommutators of
one momentum against the bounded vertex — one derivative, placeable on the
state. (S = −i slash(n) γ⁵ at the timelike record axis; S² = I of record
FORCES the timelike reading — the spacelike alternative squares to −I,
CAS B1b/B1f.)

**Step 2 — the form estimate.** For psi, phi in D(h_0), each term pairs one
derivative to a state (p_j Hermitian, M Hermitian, ||M|| <= 1,
||α_j S|| = 1 exactly — CAS B1e):

```text
⟨phi, {p_j, M} ⊗ α_j S psi⟩ = ⟨p_j phi, M ⊗ α_j S psi⟩ + ⟨M phi, α_j S ⊗ p_j psi⟩
|⟨phi, [h_0, M ⊗ S] psi⟩|  <=  Σ_j ( ||p_j phi||·||psi|| + ||phi||·||p_j psi|| )   (BILINEAR, primitive)
|q_t(psi)|                 <=  2 Σ_j ||p_j psi|| · ||psi||                          (QUADRATIC, primitive)
```

**Step 3 — consolidation on the h_0 scale (continuum-exact).** By
Cauchy-Schwarz on R³ (CAS B5) and ||h_0 psi||² = Σ_j ||p_j psi||² (from
{α_i, α_j} = 2δ_ij, CAS B1d, and [p_i, p_j] = 0 in the continuum):

```text
|q_t(psi)| <= 2·sqrt(3)·||h_0 psi||·||psi||        for all psi in D(h_0), all t in [0,1]
```

**Step 4 — the eps-split: relative form bound ZERO (CAS B4).** For every
eps > 0:

```text
|q_t(psi)| <= eps·⟨psi, h_0² psi⟩ + (3/eps)·⟨psi, psi⟩ .
```

[h_0, M(t)⊗S] is infinitesimally form-bounded with respect to h_0². This is
the exact sense in which "quadratic-form bounds survive where operator norms
diverge": the commutator admits NO bound as an operator (§3.4) and a ZERO
relative bound as a form.

**Uniformities, displayed at the quantifier:**

```text
(u-a) t-UNIFORM: the bound is r(t)-independent — valid at every cell time
      including the tips r -> 0.
(u-b) CELL- AND SCALE-UNIFORM: the constant 2 sqrt(3) is a pure number; the
      estimate uses only ||M|| <= 1 and holds for the indicator of ANY
      measurable region — every admitted cell of the D3 refinement, every
      radius, no cellulation datum consumed (R.1 alphabet respected: the
      constant is a functional of NOTHING but the Clifford algebra).
(u-c) CARRIER-BLIND AT THE PRIMITIVE STEP: Steps 1-2 hold verbatim at every
      sealed Galerkin carrier (p_j -> p_{j,n,ell}, M -> Q 1_B Q; the flip
      needs only the spinor relations, which the carrier tensor structure
      H_(n,ell) ⊗ C⁴ preserves): |q_t| <= 2 Σ_j ||p_{j,n,ell} psi||·||psi||
      at every (n, ell). The sqrt(3)-consolidation (Step 3) uses
      [p_i, p_j] = 0 and is continuum-exact; at finite carrier the primitive
      display stands (compressed momenta need not commute) — stated, not
      papered over.
(u-d) M NEVER SMOOTHED: every step keeps the sealed sharp M(t) as written
      (D6' :465-470 respected); the mechanism is derivative placement on the
      STATE, not regularization of the vertex.
```

**Time-integrated and weight-summed displays (R.1-alphabet constants, all
exact; CAS B6a/B6c/B6d):**

```text
∫_0^1 v(t) |q_t(psi)| dt        <= 2 sqrt(3) · tau_R · ||h_0 psi||·||psi||        (∫ v = tau_R exactly)
D(t) BILINEAR (one dressed C-L2 insertion, free dressing commutes with h_0):
  |⟨phi, D(t) psi⟩|             <= t · sqrt(3) · ( ||h_0 phi||·||psi|| + ||phi||·||h_0 psi|| )
weight sum over the sealed w_lambda: Σ_lambda |w_lambda||lambda| = sqrt(2)/2 ,
  so the one-insertion weighted, v-integrated form-layer constant is
  2 sqrt(3) · tau_R · sqrt(2)/2 = pi·sqrt(3)   EXACTLY (tau_R = pi/sqrt2).
```

These constants are functionals of (tau_R, |w_lambda|) only — squarely inside
the R.1 alphabet (:679-687). No float, no measured constant, no frozen value.

### 3.3 (o2 continued) What the certified control DOES and DOES NOT cover in E_int

DOES: the C-L2 generator [h_0, M(t)⊗S] (the clause's named object) — fully;
and the single FREE-dressed insertion D(t) at bilinear form grade (above),
because e^{-i h_0 s} commutes with h_0.

DOES NOT (stated exactly): the full E_int bilinear form on H¹ — the
INTERACTION dressings Omega_lambda(1,t) do not commute with h_0, and
commuting h_0 through them is again a C-L2-layer object (a Gronwall at form
grade would consume ||[h_0, M(s)⊗S] Omega psi||, an OPERATOR-grade quantity —
exactly what is excluded). An H¹-propagation estimate for the sharp-vertex
dynamics is the missing lemma; it is NOT attempted tonight and NOT named
anywhere in sealed stock as discharged. This is a residual INSIDE the form
grade, named here so the registrar sees the exact edge of tonight's layer.

### 3.4 (o3) WHY the operator-norm route is excluded — of record, and made exact

OF RECORD: `||[h_0, 1_B]|| = +infinity` (E1 :1139-1141); locus and kernel made
exact by the sealed CL-ERROR J3 display (momentum kernel
chi_hat_{r(t)}(q)·(α·(k+k'))·S, degree +1 in |k|).

MECHANISM, displayed tonight (two exact exhibits):

```text
(i)  THE FORM DOES NOT EXTEND TO L² — so no bounded operator represents it.
     Witness family (radial profile, exact; CAS B8a-c):
        f_eps(rho) = ((rho − r)² + eps²)^(−1/8) on an annulus of half-width a:
        L² mass  <= (r+a)²·4·sqrt(a)  UNIFORMLY in eps   (comparison
                    (s²+eps²)^{−1/4} <= |s|^{−1/2}, ∫_{−a}^{a}|s|^{−1/2} = 4 sqrt a)
        sphere-trace value f_eps(r)² = eps^{−1/2} → +infinity .
     On a spherical cap with spinor chosen in the +1 eigenspace of the
     Hermitian unitary (α·n̂₀ ⊗ S), the boundary-trace pairing (ii) grows
     without bound along the family while the L² norm stays bounded: the
     form is unbounded on L², a fortiori ||[h_0, 1_B]||_op = +infinity.
(ii) THE BOUNDARY-TRACE DISPLAY (what the form IS, distributionally):
     ∇1_{B_r} = −n̂·delta_{∂B_r}  gives  [h_0, 1_B ⊗ S] = i(α·n̂ ⊗ S)·delta_{∂B_r},
     hence q(psi) = i ∮_{∂B_r} psi†(α·n̂ ⊗ S)psi dS — a SURFACE pairing: finite
     exactly when the state has a square-integrable sphere trace (H^{1/2+}/
     Besov B^{1/2}_{2,1}; guaranteed on D(h_0) = H¹), never for generic L².
     1-D structure check exact (CAS B7, TOY_SEPARATION §9).
```

So the exclusion is not a technical failure but a type fact: the commutator is
a surface distribution; operators act on states, forms pair them — only the
pairing can spend the state's half-derivative on the trace. The certification
is therefore in the quadratic form, as the clause mandates, and the certified
domain is the sealed h_0's own H¹.

### 3.5 (o4) "Supplies S2b" — NOT discharged; the exact distance, each step of record

What R.3 consumes is HS-typed: `||A(0)||_2 <= |C|_4^{beta} G_cm` (E1 :810-813)
with A(0) = C·(error layer)·C on the surviving sector. From tonight's certified
form layer to that consumable:

```text
(d-1) FORM -> HS GRADE CONVERSION: the wall's F1 (WALL :408-413) is the PROVED
      rank×op ceiling — op/form-grade control times carrier rank cannot close
      a carrier-uniform trace/HS estimate (WB6 = r-3 D10 re-pinned). A
      sub-volume HS rate is verbatim MO-3, open of record.
(d-2) THE TWO-TIME HS ESTIMATE ITSELF: B-L2* — FAILS-AT of record at the
      stratum-resummation step at the sealed saturation clause (6e81ae92 :32,
      check CONFIRMED 2699af25 :23): the per-stratum coincidence-power
      mechanism is exact, and C4's saturation is exactly the statement that
      the gain does not survive the stratum sum at full tau_R. The equal-time
      shortcut is FALSE of record (C6: ||[C, 1_B]||_2 = +infinity, E1
      :353-357; the boundary-commutator class is divergent, kappa_n → +infinity,
      WALL CS-3 :231-233).
(d-3) THE SCALING EXPONENT beta: a SYMBOL until R-L2b closes (E1 :826-832,
      witness SCAD_HS_SCALING_EXPONENT_UNDERIVED; RL0 u-1 :377-380).
```

No sealed authority bridges (d-1)-(d-3); the sweep (§1.1) shows no non-fenced
carrier containing such a bridge. (o4) therefore FAILS-AT (d-1, d-2, d-3),
all previously named, none new — consistent with §O.X of record ("IR-C ...
reduce to it through C-L2", E1 :925-937).

---

## 4. SUB-TASK (3) — THE G_cm ATTEMPT

**Verdict: NOT-DERIVABLE-TODAY at its consuming type.** G_cm's definition has
two factors; each is blocked by a standing of-record item, and the blockers
are exactly the §3.5 distance plus one:

```text
(b-1) |C|_4^{-beta}: beta is a symbol (R-L2b open; u-1). No exponent, no
      G_cm — the object is not yet well-typed as a cell functional.
(b-2) the "certified bound" factor at R.3's HS-typed site: blocked at
      (d-1)+(d-2) — B-L2* resummation FAILS-AT of record; equal-time FALSE
      (C6/CS-3); form-to-HS conversion capped by F1/MO-3.
(b-3) EVEN A CERTIFIED G_cm DOES NOT MAKE x DERIVABLE: the consuming chain
      still carries u-2 (rho_res = H-R, may never default) and u-4 (per-pair
      carrier sup +infinity of record, 52f2490b) — stated so the value of
      this target is not overread in either direction.
```

**What IS delivered on G_cm tonight (exact, alphabet-compliant):** under S2b's
own form-typed parenthetical reading, the ingredient "certified bound on the
C-L2 commutator error ... in the quadratic form C-L2 names" now EXISTS at the
generator level: the t-uniform, cell-uniform form bound 2·sqrt(3)·||h_0 psi||·
||psi|| (§3.2), with weighted time-integrated constant pi·sqrt(3) exact. It is
delivered AS A FORM-GRADE OBJECT AWAITING ITS CONSUMER: it cannot be inserted
into R.3's HS-typed slot, and no |C|_4-power can be attached to it until
R-L2b exists. Cross-check consumed without freezing: the entered PA cell has
|C|_4 = (4 pi/3)∫r³ = pi/24 exact (CAS B6b; RL0 flag block agreement).

---

## 5. SUB-TASK (4) — VERDICTS, AT THEIR EXACT QUANTIFIERS

```text
C-L2 = PARTIAL(
  CERTIFIED — (o1) form NAMED: the h_0-relative quadratic form on Q = D(h_0)
    = H¹(R³)⊗C⁴ (Besov B^{1/2}_{2,1} trace variant displayed);
  (o2) CONTROL CERTIFIED: for all psi in D(h_0), all t in [0,1], every
    admitted cell of the D3 class, every sealed carrier at the primitive step:
      |⟨psi, [h_0, M(t)⊗S] psi⟩| <= 2 sqrt(3) ||h_0 psi|| ||psi|| ,
      |q_t(psi)| <= eps⟨psi, h_0² psi⟩ + (3/eps)⟨psi,psi⟩  for every eps > 0
    (relative form bound ZERO), exact constants only, sharp M(t) untouched;
  (o3) EXCLUSION DISPLAYED: of record + mechanism-exact (surface-distribution
    type; L² witness family; the form does not extend to L²);
  NOT CERTIFIED — (o4) the S2b supply at its HS-typed consuming site:
    FAILS-AT (d-1) F1/MO-3 grade ceiling, (d-2) B-L2* stratum-resummation
    of record + C6/CS-3 equal-time FALSE, (d-3) R-L2b beta symbol;
    plus the named form-grade residual (§3.3): H¹-propagation through the
    interaction dressings, unattempted.
)
G_cm = NOT-DERIVABLE-TODAY( blockers b-1, b-2, b-3 — all standing of-record
  items, swept §1.1; the form-typed ingredient newly exists (§4) but has no
  sealed consumer at its grade ).
```

**What tonight's PARTIAL unblocks — per the sealed conditionals and NO
STRONGER:**

```text
WALL F3: NOT UNBLOCKED. F3's consuming site is a single-composite majorant of
  the odd sector at the stay strata in TRACE/HS grade; tonight's layer is
  form-grade; the conversion is the wall's own F1 ceiling. What changes if
  registered: F3's premise "its only permitted certification (C-L2 quadratic
  form) is UNCERTIFIED of record" becomes uncertified AT THE SUPPLY LAYER
  ONLY — the closure identities at the stay strata now hold modulo an error
  layer whose GENERATOR is form-certified with relative bound zero. A
  tightening of what the modulo-layer is; not a discharge of it.
R-L0 THRESHOLD CONSUMPTION: NOT UNBLOCKED. x remains indeterminate on
  u-1/u-2/u-4 independently of tonight (RL0 :377-391 stands verbatim).
NOTHING ELSE: no gate moves, no fence moves, no threshold is approached; no
  conditional beyond the two above is touched by this result.
```

---

## 6. SUB-TASK (5) — CONSISTENCY, WITNESS MECHANICS, V-CLAUSES

```text
WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS. Its clause
  covers (o1)-(o4); tonight (o1)-(o3) are certified and (o4) is not. Whether
  the witness is retired, split (form layer vs supply layer), or left standing
  is the REGISTRAR'S decision exclusively; this artifact retires NOTHING.
  SCAD_HS_SCALING_EXPONENT_UNDERIVED — untouched, standing (consumed as
  blocker b-1/d-3). E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — untouched,
  standing (consumed as blocker d-2). No witness created: every failure point
  tonight already carries its own named witness of record.
V-CLAUSES: untouched. No verdict here asserts n >= 2 certified, no E1
  certification, no threshold satisfaction, no alpha-adjacent claim.
FENCES: sharp M(t) unmodified (D6'); no equal-time HS object resurrected (C6
  respected as FALSE; the equal-time facts are cited only as exclusions); no
  baseline normed (S3 stays deleted; nothing here touches G_bl); no
  Phase-1 (K_sea, T_R, b_0) consumed; kappa_bal display not cited; no
  coarsest-cell/cellulation datum consumed (the certified constant is a pure
  number); "Q-..." register tokens EXPECTED-UNLOCATABLE, noted not chased.
NO STRONGER READING: nothing in this artifact bounds E_int, E_prof, A(0),
  x, eta_1, or any wall quantity. The certified object is the form of the
  COMMUTATOR GENERATOR and the free-dressed single insertion D(t), exactly
  as displayed, nothing more.
```

---

## 7. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

### 7.1 Script (`cl2_battery.py`, run with a fresh `python3 -m venv`)

```python
# STAGE8 CL2/GCM CERTIFICATION — CAS BATTERY (exact symbolic only; sympy)
# Every check exact. No floats. No measured constants. PASS/FAIL printed per item.
import sympy as sp

PASS = []
def ok(name, cond):
    PASS.append((name, bool(cond)))
    print(("PASS " if cond else "FAIL ") + name)

I2 = sp.eye(2); Z2 = sp.zeros(2, 2)
# Dirac representation gammas (signature +---), exact integer entries
s1 = sp.Matrix([[0, 1], [1, 0]]); s2 = sp.Matrix([[0, -sp.I], [sp.I, 0]]); s3 = sp.Matrix([[1, 0], [0, -1]])
def blk(A, B, C, D):
    return sp.Matrix(sp.BlockMatrix([[A, B], [C, D]]))
g0 = blk(I2, Z2, Z2, -I2)
g = [blk(Z2, s, -s, Z2) for s in (s1, s2, s3)]  # gamma^1,2,3
g5 = sp.I * g0 * g[0] * g[1] * g[2]
alpha = [g0 * gj for gj in g]                    # alpha_j = gamma^0 gamma^j
# S = -i slash(n) gamma^5 at the timelike record axis n = (1,0,0,0): slash(n) = gamma^0
S = -sp.I * g0 * g5

# J-B1: Clifford facts the certification stands on
ok("B1a S = -i g0 g5 is Hermitian", sp.simplify(S - S.H) == sp.zeros(4, 4))
ok("B1b S^2 = I (E1 :1133 'S^2 = I' reproduced)", sp.simplify(S * S - sp.eye(4)) == sp.zeros(4, 4))
ok("B1c {S, alpha_j} = 0 for j = 1,2,3 (root of the anticommutator flip; CL-ERROR J3 ground)",
   all(sp.simplify(S * a + a * S) == sp.zeros(4, 4) for a in alpha))
ok("B1d {alpha_i, alpha_j} = 2 delta_ij I (h_0^2 = |p|^2 I ground)",
   all(sp.simplify(alpha[i] * alpha[j] + alpha[j] * alpha[i]
       - (2 * sp.eye(4) if i == j else sp.zeros(4, 4))) == sp.zeros(4, 4)
       for i in range(3) for j in range(3)))
ok("B1e ||alpha_j S|| = 1: (alpha_j S)(alpha_j S)^H = I",
   all(sp.simplify((a * S) * (a * S).H - sp.eye(4)) == sp.zeros(4, 4) for a in alpha))

# B1f: the reading n = timelike record axis is FORCED by S^2 = I of record:
# the spacelike alternative S' = -i slash(n) g5 with n spatial gives S'^2 = -I.
S_spacelike = -sp.I * (-g[2]) * g5   # slash(n) = -gamma^3 for n = (0,0,0,1), signature +---
ok("B1f spacelike-axis alternative has S'^2 = -I: S^2 = I of record SELECTS the timelike axis",
   sp.simplify(S_spacelike * S_spacelike + sp.eye(4)) == sp.zeros(4, 4))

# J-B2: the commutator -> anticommutator flip, entrywise over noncommutative spatial symbols.
# h_0 = sum_j p_j (x) alpha_j ; record vertex M (x) S, M any spatial operator (commutes with
# spinor matrices). Entry (a,b) of [h_0, M(x)S] vs sum_j {p_j, M} (x) (alpha_j S)_(ab).
p = [sp.Symbol(f'p{j}', commutative=False) for j in range(3)]
M = sp.Symbol('M', commutative=False)
flip_ok = True
for a_ in range(4):
    for b_ in range(4):
        lhs = sum((alpha[j] * S)[a_, b_] * (p[j] * M) - (S * alpha[j])[a_, b_] * (M * p[j]) for j in range(3))
        rhs = sum((alpha[j] * S)[a_, b_] * (p[j] * M + M * p[j]) for j in range(3))
        if sp.expand(lhs - rhs) != 0:
            flip_ok = False
ok("B2 [h_0, M(x)S] = sum_j {p_j, M} (x) alpha_j S  (uses only B1c; valid for ANY spatial M, any carrier)", flip_ok)

# J-B4/B5: the elementary inequalities consolidating the form bound
a_, b_, e_ = sp.symbols('a b epsilon', positive=True)
x_, y_, z_ = sp.symbols('x y z', real=True)
ok("B4 eps a^2 + 3 b^2/eps - 2 sqrt(3) a b = (sqrt(eps) a - sqrt(3) b/sqrt(eps))^2 >= 0",
   sp.simplify(e_ * a_**2 + 3 * b_**2 / e_ - 2 * sp.sqrt(3) * a_ * b_
               - (sp.sqrt(e_) * a_ - sp.sqrt(3) * b_ / sp.sqrt(e_))**2) == 0)
ok("B5 (x+y+z)^2 <= 3(x^2+y^2+z^2)  [Cauchy-Schwarz on R^3]",
   sp.simplify(3 * (x_**2 + y_**2 + z_**2) - (x_ + y_ + z_)**2
               - ((x_ - y_)**2 + (y_ - z_)**2 + (z_ - x_)**2)) == 0)

# J-B6: exact sealed-profile integrals and weight sums (PA A1/A2; E1 D6'; C4 weights)
t = sp.Symbol('t', positive=True)
r_t = sp.Min(t, 1 - t)
tau_R = sp.pi / sp.sqrt(2)
i1 = sp.integrate(32 * r_t**3, (t, 0, 1))
ok("B6a int_0^1 32 min(t,1-t)^3 dt = 1  (int v = tau_R exactly)", sp.simplify(i1 - 1) == 0)
i2 = sp.integrate(sp.Rational(4, 3) * sp.pi * r_t**3, (t, 0, 1))
ok("B6b |C|_4 = (4 pi/3) int r^3 dt = pi/24  (entered-cell cross-check, RL0 cert flag block)",
   sp.simplify(i2 - sp.pi / 24) == 0)
w = {sp.sqrt(2): sp.Rational(-1, 4), 0: sp.Rational(1, 2), -sp.sqrt(2): sp.Rational(-1, 4)}
ok("B6c sum_lambda |w_lambda| = 1  (C4 tightness reproduced)",
   sp.simplify(sum(sp.Abs(v) for v in w.values()) - 1) == 0)
ok("B6d sum_lambda |w_lambda||lambda| = sqrt(2)/2  (weight-summed E_int form-layer constant)",
   sp.simplify(sum(sp.Abs(v) * sp.Abs(k) for k, v in w.items()) - sp.sqrt(2) / 2) == 0)
ok("B6e sum_lambda w_lambda = 0 (m_0 = 0) and sum w lambda = 0 (m_1 = 0)",
   sp.simplify(sum(w.values())) == 0 and sp.simplify(sum(k * v for k, v in w.items())) == 0)

# J-B7 (TOY_SEPARATION - structure check only, 1-D analogue): the distributional
# boundary form equals the anticommutator form by exact FTC. h = -i s1 d/dx,
# vertex 1_[-r,r] (x) s3, {s1,s3} = 0. Test spinor psi = (exp(-x^2), x exp(-x^2)).
xx = sp.Symbol('x', real=True); rr = sp.Symbol('r', positive=True)
psi = sp.Matrix([sp.exp(-xx**2), xx * sp.exp(-xx**2)])
dpsi = psi.diff(xx)
V = s1 * s3
# anticommutator-form value: -i int_{-r}^{r} d/dx (psi^H V psi) dx
integrand = sp.simplify((dpsi.H * V * psi + psi.H * V * dpsi)[0, 0])
lhs_form = -sp.I * sp.integrate(integrand, (xx, -rr, rr))
bdry = sp.simplify((psi.H * V * psi)[0, 0])
rhs_form = -sp.I * (bdry.subs(xx, rr) - bdry.subs(xx, -rr))
ok("B7 TOY(1-D): quadratic form of [h, 1_B (x) S] = boundary-trace form exactly (FTC)",
   sp.simplify(lhs_form - rhs_form) == 0)

# J-B8: WHY the operator route is excluded, made exact — a witness family with
# bounded L^2 mass and divergent sphere trace: f_eps(rho) = ((rho-r)^2 + eps^2)^(-1/8).
s_, aa, ee = sp.symbols('s a varepsilon', positive=True)
ok("B8a comparison (s^2 + eps^2)^(-1/4) <= |s|^(-1/2)  (since s^2 + eps^2 >= s^2)",
   sp.simplify((s_**2 + ee**2) - s_**2) == ee**2)
ok("B8b eps-uniform L^2 mass bound: int_{-a}^{a} |s|^(-1/2) ds = 4 sqrt(a)  (finite, exact)",
   sp.simplify(2 * sp.integrate(s_**sp.Rational(-1, 2), (s_, 0, aa)) - 4 * sp.sqrt(aa)) == 0)
trace_val = (ee**2)**sp.Rational(-1, 4)
ok("B8c sphere-trace value f_eps(r)^2 = eps^(-1/2) -> +infinity as eps -> 0",
   sp.limit(trace_val, ee, 0, '+') == sp.oo)

# J-B9: E_prof cross-check against the sealed CL-ERROR display (consistency, not a new ground):
# Theta(x) = int_0^1 v(t) 1_{|x|<=r(t)} dt = tau_R (1 - 16 |x|^4) on the maximal ball.
rho = sp.Symbol('rho', positive=True)
Theta = sp.integrate(tau_R * 32 * r_t**3 * sp.Piecewise((1, rho <= r_t), (0, True)), (t, 0, 1))
Theta_closed = tau_R * (1 - 16 * rho**4)
ok("B9 Theta(rho) = tau_R (1 - 16 rho^4) on 0 < rho < 1/2  (CL-ERROR J8 reproduced)",
   sp.simplify((Theta - Theta_closed).rewrite(sp.Piecewise).subs(rho, sp.Rational(1, 4))) == 0
   and sp.simplify(Theta.subs(rho, sp.Rational(1, 3)) - Theta_closed.subs(rho, sp.Rational(1, 3))) == 0
   and sp.simplify(Theta.subs(rho, sp.Rational(1, 5)) - Theta_closed.subs(rho, sp.Rational(1, 5))) == 0)

print()
n_pass = sum(1 for _, c in PASS if c)
print(f"BATTERY: {n_pass}/{len(PASS)} PASS")
```

### 7.2 Output (verbatim)

```text
PASS B1a S = -i g0 g5 is Hermitian
PASS B1b S^2 = I (E1 :1133 'S^2 = I' reproduced)
PASS B1c {S, alpha_j} = 0 for j = 1,2,3 (root of the anticommutator flip; CL-ERROR J3 ground)
PASS B1d {alpha_i, alpha_j} = 2 delta_ij I (h_0^2 = |p|^2 I ground)
PASS B1e ||alpha_j S|| = 1: (alpha_j S)(alpha_j S)^H = I
PASS B1f spacelike-axis alternative has S'^2 = -I: S^2 = I of record SELECTS the timelike axis
PASS B2 [h_0, M(x)S] = sum_j {p_j, M} (x) alpha_j S  (uses only B1c; valid for ANY spatial M, any carrier)
PASS B4 eps a^2 + 3 b^2/eps - 2 sqrt(3) a b = (sqrt(eps) a - sqrt(3) b/sqrt(eps))^2 >= 0
PASS B5 (x+y+z)^2 <= 3(x^2+y^2+z^2)  [Cauchy-Schwarz on R^3]
PASS B6a int_0^1 32 min(t,1-t)^3 dt = 1  (int v = tau_R exactly)
PASS B6b |C|_4 = (4 pi/3) int r^3 dt = pi/24  (entered-cell cross-check, RL0 cert flag block)
PASS B6c sum_lambda |w_lambda| = 1  (C4 tightness reproduced)
PASS B6d sum_lambda |w_lambda||lambda| = sqrt(2)/2  (weight-summed E_int form-layer constant)
PASS B6e sum_lambda w_lambda = 0 (m_0 = 0) and sum w lambda = 0 (m_1 = 0)
PASS B7 TOY(1-D): quadratic form of [h, 1_B (x) S] = boundary-trace form exactly (FTC)
PASS B8a comparison (s^2 + eps^2)^(-1/4) <= |s|^(-1/2)  (since s^2 + eps^2 >= s^2)
PASS B8b eps-uniform L^2 mass bound: int_{-a}^{a} |s|^(-1/2) ds = 4 sqrt(a)  (finite, exact)
PASS B8c sphere-trace value f_eps(r)^2 = eps^(-1/2) -> +infinity as eps -> 0
PASS B9 Theta(rho) = tau_R (1 - 16 rho^4) on 0 < rho < 1/2  (CL-ERROR J8 reproduced)

BATTERY: 19/19 PASS
```

---

## 8. CHOICE LEDGER

```text
ch-1 RECORD-AXIS READING: n in S = -i slash(n) gamma^5 read TIMELIKE.
     Ground: S^2 = I of record (E1 :1133) FORCES it — the spacelike
     alternative squares to -I (CAS B1b/B1f). Tag: PROVABLE.
ch-2 NAMING THE FORM: the h_0-relative quadratic form on D(h_0), Besov trace
     variant displayed. Ground: the clause assigns the naming act to the
     artifact ("must state WHICH form", :1142); content of the named form is
     then certified. Tag: YOURS (naming), PROVABLE (content).
ch-3 DUAL-LEVEL STATEMENT (continuum consolidated + carrier primitive):
     chosen so the certification is carrier-blind where R.1 demands and
     honest where compressed momenta stop commuting. Tag: PROVABLE (both
     displays), YOURS (the choice to carry both).
ch-4 STOCK BEYOND THE SEED LIST: CL-ERROR (3b9730e0), BL2STAR (6e81ae92),
     BL2STAR-CHK (2699af25) consumed — surfaced by the mandated sweep,
     sidecar-verified, commission-relevant (the exact E-decomposition and the
     of-record B-L2* verdict). Tag: YOURS (inclusion), grounds displayed.
ch-5 S2b TYPE AMBIGUITY carried, not repaired: S2b's parenthetical is
     form-typed, R.3's consuming display is HS-typed. Both readings reported
     (§2.3, §5); no clause repair attempted — the spec author/registrar owns
     it. Tag: YOURS (to flag), the ambiguity itself displayed at bytes.
ch-6 WITNESS FAMILY exponent -1/8 in §3.4(i): any exponent in (-1/4, 0)
     works; -1/8 chosen for exact CAS display. Tag: PROVABLE (the conclusion
     is exponent-independent within the class).
ch-7 NOT ATTEMPTED tonight, named: H^1-propagation through the interaction
     dressings (the form-grade residual, §3.3). Ground: it is a new lemma,
     not a certification of a sealed display; overreaching it would blur the
     exact edge of the certified layer. Tag: YOURS.
```

## 9. TOY_SEPARATION

```text
T-1 B7 (1-D two-band analogue, sigma_1/sigma_3): structure check ONLY of the
    boundary-trace = anticommutator-form FTC identity. NO 3-D conclusion
    rests on it; the 3-D certification stands on §3.2 Steps 1-4, whose
    grounds are B1c/B1d/B1e/B2/B4/B5 (exact, 3-D, carrier-relevant).
T-2 B9 rational sample points (rho = 1/4, 1/3, 1/5) inside the piecewise
    reduction: a CONSISTENCY REPRODUCTION of the sealed CL-ERROR J8 display,
    not a ground of any verdict here.
T-3 No numeric evaluation anywhere: every constant in this artifact is exact
    symbolic (2 sqrt 3, 3/eps, sqrt2/2, tau_R = pi/sqrt2, pi sqrt3, pi/24).
```

---

```text
FLAG BLOCK — STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
CL2_FORM_NAMED = true            (h_0-relative quadratic form on D(h_0); Besov B^{1/2}_{2,1} trace variant displayed)
CL2_COMMUTATOR_FORM_BOUND_CERTIFIED = true
    (|<psi,[h_0, M(t) (x) S]psi>| <= 2 sqrt(3) ||h_0 psi|| ||psi||; relative
     form bound ZERO w.r.t. h_0^2; t-, cell-, scale-uniform; carrier-blind at
     the primitive step; sharp M(t) untouched; CAS 19/19 PASS)
CL2_OPERATOR_ROUTE_EXCLUSION_DISPLAYED = true   (of record :1139-1141 + mechanism-exact: surface distribution; L^2 witness family)
CL2_S2B_SUPPLY_DISCHARGED = false   (FAILS-AT: F1/MO-3 grade ceiling; B-L2* stratum-resummation of record 6e81ae92/2699af25; R-L2b beta symbol; + named form-grade residual = H^1 propagation through interaction dressings)
CL2_VERDICT = PARTIAL
GCM_VERDICT = NOT_DERIVABLE_TODAY   (b-1 beta symbol/R-L2b; b-2 HS-typed bound blocked at B-L2* + C6/CS-3 + F1; b-3 consumption additionally indeterminate on u-2/u-4 of record)
WALL_F3_UNBLOCKED = false   (per the sealed conditional and NO STRONGER: F3's consuming site is trace/HS-typed at the stay strata; tonight's layer is form-grade; the conversion is F1. Registered, it tightens WHAT the modulo-layer is — generator form-certified, relative bound zero — not its discharge)
RL0_THRESHOLD_CONSUMPTION_UNBLOCKED = false   (u-1/u-2/u-4 stand verbatim)
E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED = STANDS   (retirement/split is the registrar's; this artifact retires nothing)
witnesses_retired_here = none ; witnesses_created_here = none
V_clauses_touched = none ; gates_moved = none ; fences_moved = none
new_numbers_frozen = none (all constants exact symbolic)
seeds_verified = 46846730, a903716c, 685afac8, 80db260f, 46938251, 789338ad, 3b9730e0, 6e81ae92, 2699af25 (all at path, this session)
sweep = both roots, 47 hits, 4 fenced-class not opened, 43 classed (§1.1)
CAS = sympy 1.14.0, fresh venv, 19/19 PASS, script + output verbatim §7
output = ONE artifact + seal sidecar at the commission-distinct path, probed ABSENT before write
```
