# Stage-8 T7 Majorant-Arm Re-Scope — Principal Decision V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_PRINCIPAL_DECISION — SUPERSEDES A FROZEN VERDICT-TABLE ROW
```

Brian's decision, taken as principal precisely because the escalation's
own rule forbade settling it by spec amendment ("this must NOT be settled
by amending a spec"). The sealed majorant spec text
(STAGE8_T7_CONNECTED_LINKED_CLUSTER_MAJORANT_DERIVATION_SPEC_V001,
818083a5…) is NOT edited; this artifact supersedes its verdict-table row
for the DERIVED arm.

## Q1 answered: NO — the DERIVED arm does not require O7

Independently audited by the external reviewer lane from three angles
(input trace, quantifier scope, certification status), each with
adversarial refutation; all three returned ROUTE_Q_INDEPENDENT and all
three survived. Findings of record:

```text
- The executed M3 consumes NO Route-T output: zero citations of
  TT1/TT2/TT3, the transfer operator, the spectral gap, or the
  intertwiner anywhere in the executed O4-O6 text.
- Every load-bearing M3 input traces to sealed Phase-A objects
  (789338ad, 52401eef, 0df721a1, 451550c3, b786db3a), Lemma 0, the
  frozen KP threshold, or Route-Q-internal machinery.
- THE DEPENDENCY ARROW RUNS THE OTHER WAY: per amendment M-10, Route T's
  TT2-E3 consumes Route Q's epsilon_star. Route T depends on Route Q,
  never the reverse.
- The cellulation-blind constants are blind BY CONSTRUCTION (sups over
  all admitted cells), not computed on the pinned skeleton. The only
  skeleton-computed number (TT2-P0's 3/8) lives in O3 and is not
  consumed by M3.
- O7 is typed as a TRANSPORTER of O3 certificates; the executed M3 uses
  no O3 certificate. O7 has no consumer in Route Q.
```

## The re-scope

```text
The T7III_MAJORANT_DERIVED arm is RE-SCOPED to the Route-Q predicate:
    M1  +  M2-certified  +  M3-activated over the full D3 quantifier,
WITHOUT the O7 condition.
```

### Accompaniment (a) — the change is WIDER than removing O7, stated explicitly

```text
The re-scope ALSO REMOVES O3's TT1-TT3 SKELETON CERTIFICATES from the
arm's predicate. The DERIVED arm no longer requires any Route-T
artifact: not the anchored transfer operator (TT1), not the spectral-
isolation certificate (TT2), not the clustering conversion (TT3), and
not the refinement intertwiner (O7). This is recorded here so the
breadth of the change is visible and cannot be read as an O7-only
adjustment.
```

### Accompaniment (b) — F-8 is not waived; it is now the gate

```text
The re-scoped arm remains fully subject to the sealed spec's F-8
obligations: fresh-context re-derivation of the M3 schema from the
sealed text, and hostile pre-execution review, BEFORE anything seals as
derived. The reviewer's audit noted that the Route-Q independence
finding currently rests on DRAFT artifacts authored by the claiming
lane; F-8 discharges exactly that exposure and is the operative gate on
the re-scoped arm.
```

## What is retired, and what stands untouched

```text
RETIRED: the O7 escalation's forced choice — pin the cellulation family
  to a finite list vs carry T7(iii) underived. NEITHER option is
  selected, because the dilemma does not exist.
STANDS UNTOUCHED, as sealed results about ROUTE T: the O7 obstruction
  record and its exact witnesses (record-tier tensor powers (3/8)^k vs
  3/8; every refined cell inserting an O(1) full record cycle with the
  exact phase e^{+-i pi} = -1); the frozen prediction P2's landing; the
  O3 ordering blocks. O7's refutation kills Route T's transport
  MECHANISM only.
```

## Consequence for the front

Route Q carries the full quantifier alone ONLY contingent on an E1
architecture that does not yet exist: per the IR memo's C2, the Phase-1
K_sea functional is DIVERGENT as defined and must be REPLACED, not
certified. E1 is therefore the front, and the E1 successor program is
separately authorized.

## Protected status

```text
majorant_derived_arm_rescoped = true
O7_escalation_retired = true
refinement_intertwiner_derived = false   (unchanged; now not required)
routeT_skeleton_gap_certified = false    (unchanged; now not required)
F8_rederivation_and_hostile_review_discharged = false   (the gate)
connected_linked_cluster_density_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
