# STAGE 8 — REQUIRE-G1 COLD RE-DERIVATION: DOES THE POST-LIMIT KERNEL C ADMIT A CONNECTION-ONLY, SCALE-FREE EXISTENCE CERTIFICATION?
## BLIND COLD RE-DERIVATION — REQUIRE-G1-COLD — CROSS-LINEAGE — [CLAIMED]

Date: 2026-08-13
Codename: REQUIRE-G1-COLD
Role: BLIND COLD re-deriver. Objects + QUESTION only. No other party's answer
read. The two forbidden files (STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md,
STAGE8_REQUIRE_G1_CHECK_V001.md) were NOT opened; their existence was probed
only to route around them. No register/tracker/plan/road/ledger/lens read.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

FENCES HELD THROUGHOUT: connection-only, scale-free, symbolic. No value of
n / kappa / alpha. NO scale imported as authority (no ell_P, K_KK, c^2,
metric, G_4, Lorentzian diamond, sea-covariance value, |D|_4 volume, spatial
Schatten norm value). NO imported GR. NO faithfulness premise. Where the only
available certification route uses a scale, that is REPORTED AS THE FINDING
and NOT ENDORSED (per the tasking's explicit instruction).

---

## 0. THE QUESTION, RESTATED

`R_record,L = n^2 · Phi^T C Phi`, with `C` the post-thermodynamic-limit
"Gate-5" connected cluster covariance kernel of the transported per-cell write
generators (a connected-cumulant / Duhamel covariance). Whether `R_record,L`
factorizes turns on `C` existing as a well-defined intensive post-limit object.

ASKED: does the post-limit kernel `C` admit a CONNECTION-ONLY, SCALE-FREE
existence certification (its intensive thermodynamic limit exists) — or is its
existence unavoidably a scale/volume/metric statement?

Three sub-questions, answered independently below:
1. What does certifying C's post-limit existence REQUIRE — a decay/summability
   rate of the connected covariance in cell-separation? Connection-only, or
   inherently metric/volume?
2. Is there a connection-only cluster-summability / connected-graph bound that
   certifies C, or is the only available route scale-bearing?
3. Verdict.

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH, MARKED connection-only vs scale-bearing

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Full digests
recomputed by `shasum -a 256` before reading; each matched the tasked/at-path
seal.

| Tag | Source | Seal (verified MATCH) | Certification content: connection-only / scale-bearing |
|---|---|---|---|
| S0 | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d209…658f37` | TYPES C connection-only (symmetric/PSD/intensive/additive/cluster-summable/n-independent) but names existence as the UNBUILT Gate-5 obligation G1. Provides NO certification. |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d49…f108a` | The bedrock. States the intensive-limit obligation with a "target-free locality bound" and density `Gamma_K / number_of_4_cells(K)` — a COUNT (connection-only) normalization, stated as an OBLIGATION, not certified. |
| T7 | `STAGE8_T7_CONNECTED_LINKED_CLUSTER_MAJORANT_DERIVATION_SPEC_V001.md` | `818083a5…84e3` | SCALE-BEARING. The only worked certification route. Activity majorant is ACTION-DENSITY, per unit 4-volume: `sum_(gamma∋C,|gamma|=n)|Phi_gamma| <= |C|_4 · eta^n`; constants depend on "sea-kernel decay data" and `tau_R`; per-cell (combinatorial) normalization explicitly refuted (NC6). |
| EX | `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md` | `0f3082ca…7505` | SCALE-BEARING. Connected extensivity ≡ C's intensive existence, one of R-L2b's four discharges. Control norm is spatial Schatten-2 `∫∫ d^3x d^3y`; covariance realized as degree-−3 metric kernel `1/(2π^2|r|^3)`; finiteness "purchased by cancellation", exponent set on `|D|_4`. |
| FR | `STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md` | `2ede02ae…2840` | SCALE-BEARING. Certification reduced to exponent `alpha` of the 4-volume `|D|_4^alpha`; D5 requires `alpha >= 1`; sliver failure `A^(3(1-alpha))` in aspect ratio; isotropic diamond `|D|_3~L^3`, `|D|_4~L^4`. |
| DA | `STAGE8_RL2B_DIAGONAL_ATTACK_RESULT_V001.md` | `07ea1496…540c` | SCALE-BEARING. Derives `P alpha_x P = -n̂_x P`: sea sandwich leaves degree-0 symbol → degree-−3 kernel in metric `r` → not Hilbert-Schmidt in the continuum. |
| CR | `STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md` | `d1807b96…8f86` | Kills the nearest connection-graph route: single commutator gives only `p=1<3/2`; `C[C,Y]C=0` identically (commutators control off-diagonal; the object is diagonal); HS-ideal bound vacuous (`||X||_2=∞`). |
| UR | `STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md` | `52f2490b…6241` | SCALE-BEARING. `||X||_2=∞` because C1 modulus is exactly `1/(2π^2|r|^3)`, degree −3 on R^3 not HS ⇒ no uniform Schatten-2 bound exists. |
| TR | `STAGE8_RL2B_CONVERGENCE_HYPOTHESIS_RESOLUTION_AND_F5_TRILEMMA_V001.md` | `8270721c…3b0bc` | The trilemma: continuum (refuted), finite-carrier-only (carrier-indexed, uniformity clause violated), deny-continuum (running kappa_record = new principle). No horn yields an intensive scale-free C. |
| FF | `STAGE8_IS_RL2B_FAILURE_FORCED_BY_F5_DETERMINATION_V001.md` | `83750234…b242` | Separates the carrier-index clause from the cellulation-geometric-datum clause; records that the per-cell demand is sealed in T7 D5, and the subtracted-response scaling in `|C|_4` is NOT DERIVED in either direction. |
| PC | `STAGE8_PARENT_PREPARATION_CLUSTERING_DETERMINATION_V001.md` | `58f89435…d672` | Connection-honest. Whether the prepared state clusters is UNDETERMINED (no preparation selected). Explicitly DECLINES to identify the sea `r^-3` with preparation clustering, and DECLINES to infer decay from masslessness. |

FORBIDDEN, NOT OPENED: `STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md` (present at
path), `STAGE8_REQUIRE_G1_CHECK_V001.md`. Neither read. No contamination.

---

## 2. WHAT "C EXISTS, INTENSIVE, POST-LIMIT" MEANS — POSED CONNECTION-ONLY FIRST [PROVABLE from S0, V011]

`C_jk` is the intensive post-limit connected covariance of the transported
per-cell write generators. Its existence AS AN INTENSIVE OBJECT is two demands,
both from the sealed obligation (V011's linked-cluster clause; S0 §D5):

```text
(E-density)  the connected-cluster density limit exists:
             lim (Gamma_K / #cells_or_volume(K))  converges;
(E-bound)    boundary corrections are subextensive, and the differentiated
             (a-derivative) connected series converges — so the pullback
             n^2 Phi^T C Phi is the actual Hessian, not a formal one.
```

Posed connection-only, the natural certifier is the ABSTRACT POLYMER / cluster
route: index clusters by a COMBINATORIAL cell-adjacency graph, bound per-cell
activities in OPERATOR NORM, and invoke a Kotecky-Preiss / connected-graph
summability criterion. That route is scale-free BY CONSTRUCTION — it needs only
a per-polymer activity bound and a combinatorial a-priori weight; no metric, no
volume, no length. IF such a bound held, C would be CONNECTION-ONLY-CERTIFIABLE.

The cold question is therefore sharp: **does the sealed realization of these
objects admit that connection-only certifier — or does every certifier that
survives the sealed structure carry a scale?** Sections 3-5 answer.

---

## 3. SUB-QUESTION 1 — THE REQUIRED RATE IS REFINEMENT-FORCED TO A 4-VOLUME [PROVABLE from T7 §D5/NC6, V011]

Two independent facts show the summability normalization CANNOT be kept
connection-only for this object.

**3.1 The count-normalization V011 wrote down is not refinement-stable.**
V011 states the density as `Gamma_K / number_of_4_cells(K)` — a combinatorial
CELL COUNT, which is connection-only. But the certification must be UNIFORM over
the sealed refinement families (a pinned skeleton, cubical bisection A,
barycentric subdivision B, and ALL common refinements). Refining one cell into
many multiplies the cell COUNT while leaving the physical connected activity
unchanged. So a per-cell (count) activity bound `|Phi_gamma| <= eta^n` is NOT
invariant under refinement: the same physical cluster is recounted, and the
majorant mis-aggregates. This is exactly the sealed "refinement-stability trap":
the activities MUST be stated per unit 4-volume,

```text
sum_(gamma∋C, |gamma|=n) |Phi_gamma(a)|  <=  |C|_4 · eta^n ,   |C|_4 = cell 4-volume,
```

and the executable control NC6 is precisely the demonstration that a per-cell
(non-density) normalization FAILS uniformity under one common refinement. The
connection-only normalization is therefore not merely absent — it is REFUTED for
the sealed refinement class. The refinement-stable normalization is a 4-VOLUME.
(This is where V011's connection-only aspiration is broken by the refinement
families; the metric enters as the only refinement-invariant weight.)

**3.2 The connected covariance's "decay" is a power in a metric separation.**
The connected covariance whose summability is at issue is realized by the sea
two-point kernel; its off-diagonal modulus is exactly homogeneous degree −3,
`~ 1/|r|^3`, in a spatial SEPARATION `|r|`. "Decay of the connected covariance
in cell-separation" is thus a power law in a METRIC DISTANCE, and the object is
controlled in the spatial Schatten-2 (Hilbert-Schmidt) norm `∫∫ d^3x d^3y`. The
certifying quantity is the exponent `alpha` of the 4-volume, `|D|_4^alpha`, and
the density requirement (3.1) is exactly `alpha >= 1`. Both the separation
variable and the certifying exponent are metric/volume.

**Answer to sub-question 1.** Certifying C's post-limit existence requires (i) a
refinement-stable activity/summability normalization and (ii) a decay of the
connected covariance in cell-separation. In the sealed realization NEITHER is
connection-only: (i) refinement-stability forces the per-4-volume weight `|C|_4`
(the per-cell combinatorial weight is refuted, NC6); (ii) the covariance decay
is a power in a metric `|r|`, certified in a spatial Schatten-2 norm by a
4-volume exponent `alpha >= 1`. **The required rate is a SCALE/VOLUME/METRIC
statement, not a connection-only one.**

---

## 4. SUB-QUESTION 2 — EVERY CONNECTION-ONLY BOUND IS CLOSED BY ALGEBRA, NOT BY A FENCE [PROVABLE from CR, UR, DA]

A connection-only certifier would be an OPERATOR-NORM / combinatorial bound on
the per-cell activities. Each such candidate is closed — by algebra or by
refinement-instability, never by an imported scale or a fence:

```text
(a) Combinatorial per-cell normalization (count-weighted activities).
    REFUTED by refinement-instability (§3.1; NC6). Not summable uniformly over
    the refinement families.

(b) Operator-norm / HS-ideal bound  ||C X C||_2 <= ||X||_2  (C a projection).
    TRUE BUT VACUOUS: the per-cell operator X = C(V(a)-V(0))C has ||X||_2 = ∞,
    because V(a)-V(0) is built from J = -Q b_D Q ⊗ alpha_x whose kernel is a
    (delta × bump) — infinite HS norm. There is no finite per-cell operator
    quantity to sum. (CR "obvious rescue is vacuous".)

(c) Commutator / connected-graph route: write the difference as a commutator
    with C and read decay off ||[C,·]||_2. CLOSED ON TWO INDEPENDENT GROUNDS:
      - QUANTITATIVE: finiteness needs coincidence-vanishing |r|^p with p>3/2; a
        single commutator gives only p=1 → r^-2 → divergent. (CR Ground 1.)
      - STRUCTURAL & DECISIVE: C^2=C ⇒ C[C,Y]C = 0 identically. Commutators
        control the OFF-diagonal block C X (1-C); the certified object is the
        DIAGONAL block C X C. Putting the object "in commutator form" makes it
        VANISH, not become finite. (CR Ground 2.)

(d) Continuum leading symbol is DERIVABLY non-summable. P alpha_x P = -n̂_x P
    (exact) ⇒ the sea sandwich leaves a degree-0 symbol ⇒ a degree −3 kernel in
    metric r ⇒ NOT Hilbert-Schmidt. So the scale-bearing uniform Schatten-2
    bound itself FAILS in the continuum: ||X_n||_2 → ∞ as the carrier exhausts
    (conditional on Galerkin convergence, written as a sealed Trotter-Kato
    requirement, items 3-5 undischarged). (DA §3-4; UR; TR H1.)
```

So there is no connection-only cluster-summability bound available: the
operator-side per-cell quantities are infinite, the combinatorial normalization
is refinement-refuted, and the connection-graph (commutator) route both
under-gains and vanishes on the required block. The ONLY worked route is the
scale-bearing one (per-4-volume action-density majorant + spatial Schatten-2
exponent, T7/EX/FR) — and even that route is refuted in the continuum and
survives ONLY at finite carrier, where uniformity/intensivity is lost.

**The clustering premise a connection-only bound would need is itself
unavailable.** An abstract combinatorial cluster bound presupposes UNIFORM
(typically exponential) clustering of the prepared state. PC records that
whether the sealed parent prepares ANY clustering state is UNDETERMINED (no
preparation is selected; open three-way fork), that identifying the sea `r^-3`
with preparation clustering was DECLINED, and that inferring decay from
masslessness was DECLINED. The only realized correlation is a power law in a
metric distance — precisely the non-exponential, metric object a scale-free
combinatorial bound does NOT get. Connection-only summability has no premise to
stand on.

**Answer to sub-question 2.** No connection-only cluster-summability /
connected-graph bound is available; every candidate is closed by algebra or by
refinement-instability. The only available route is scale-bearing, and it is
refuted-in-continuum / finite-carrier-only.

---

## 5. THE EXACT MISSING CONNECTION-ONLY SUB-ESTIMATE, AND WHETHER A SCALE-FREE VERSION IS POSSIBLE [YOURS, assembled from §§3-4]

Named precisely so it is not rediscovered:

```text
MISSING CONNECTION-ONLY SUB-ESTIMATE (would suffice, if it existed):
  a refinement-INVARIANT, per-cell (non-4-volume) OPERATOR-NORM activity bound
      |Phi_gamma(a)| <= eta^{|gamma|}
  on a purely COMBINATORIAL cell-adjacency connectivity, together with an
  operator-norm (not spatial-Schatten-2) decay of the connected write-generator
  covariance in COMBINATORIAL cell-separation — equivalently, UNIFORM (not
  power-law) clustering of the prepared state. This is the abstract-polymer /
  Kotecky-Preiss certifier of §2, which is scale-free by construction.
```

Whether a scale-free version is even POSSIBLE — cold judgment, for the sealed
realization: **NO.** Three structural obstructions, each intrinsically
metric/volume, each without a connection-only substitute in the sealed corpus:

```text
O-I   Refinement families force a refinement-invariant weight, and the only
      refinement-invariant weight of the cells is the 4-VOLUME |C|_4; the
      combinatorial cell count is refinement-unstable (NC6). SCALE ENTERS AS
      THE ONLY REFINEMENT-INVARIANT NORMALIZATION.
O-II  The connected covariance is CARRIED BY the sea two-point kernel, a
      degree −3 Calderon-Zygmund object: L^2-bounded but NOT Hilbert-Schmidt.
      Its per-cell operator/HS quantities are INFINITE; finiteness exists only
      as a cancellation controlled by the metric structure, whose rate is a
      4-volume exponent. THE METRIC ENTERS THE COVARIANCE OBJECT ITSELF.
O-III The clustering a combinatorial bound needs (uniform/exponential) is not
      established and is not expected: a massless/gapless sea gives power-law
      (metric) correlations, and preparation-selection is an open fork. THE
      COMBINATORIAL PREMISE HAS NO REALIZATION.
```

A scale-free certification would require the connected covariance to admit an
operator-norm / combinatorial decay rather than being carried by the metric sea
kernel `1/|r|^3`, and the activities to admit a refinement-invariant per-cell
(non-volume) normalization. Neither is sealed; the sealed structure actively
refutes both. Hence a scale-free version is not possible ON THE SEALED
REALIZATION — the entanglement is structural, not a matter of a missing clever
estimate on the same objects.

---

## 6. HONEST RESIDUAL — SCALE-ENTANGLED, AND STILL UNCERTIFIED [stated, not flattened]

Two things must not be flattened together:

```text
(1) The CERTIFICATION TYPE is scale-entangled: insofar as C's intensive
    existence is certifiable at all by the sealed machinery, the certification
    is a scale/volume/metric statement (per-4-volume normalization, metric
    covariance decay, spatial Schatten-2 control, 4-volume exponent).
(2) It is ALSO currently UNCERTIFIED: the scale-bearing route is refuted in the
    continuum (leading symbol survives; not HS) and survives only at finite
    carrier — where C is no longer intensive but a RUNNING (RG-type)
    kappa_record, which the corpus flags as a NEW PRINCIPLE, principal-only,
    NOT PURSUED. So this is "scale-entangled AND uncertified", never
    "scale-entangled AND certified".
```

This is why the verdict is SCALE-ENTANGLED with the OPEN-GAP diagnostic carried
INSIDE it (§5): the exact connection-only object is named, and it is judged
impossible for the sealed realization — not merely unbuilt.

WHAT THIS DOES NOT SAY: it does not import a scale as authority (every scale
above is reported as the corpus's own only route and left un-endorsed); it does
not assume faithfulness; it imports no GR; it does not compute or bound n,
kappa, alpha, any volume, norm, or exponent; the exponent `alpha`, the weight
`eta`, `|C|_4`, `|r|`, and `tau_R` appear only as symbols inside quoted sealed
structure. It does not decide the materiality fork or the factorization fork.

---

## 7. FINAL FLAG BLOCK

```text
C_EXISTENCE_NEEDS = SCALE_VOLUME_RATE(
  why: certifying C's intensive post-limit existence requires (i) a
  refinement-INVARIANT activity/summability normalization and (ii) a decay of
  the connected covariance in cell-separation. In the sealed realization both
  are metric/volume: (i) refinement-stability over the sealed families forces
  the per-4-volume action-density weight |C|_4 (the connection-only per-cell
  COUNT weight is refuted by NC6); (ii) the covariance is the degree −3 sea
  kernel ~1/|r|^3 in a metric separation, controlled in the spatial Schatten-2
  norm, with the certifying quantity the 4-volume exponent alpha in |D|_4^alpha,
  and the density requirement alpha >= 1.
  span: the intensive-limit + subextensive-boundary + differentiated-series
  certification of C (V011 Gate-5 / S0 G1), i.e. connected extensivity — one of
  R-L2b's four discharges.)

CONNECTION_ONLY_CERT_AVAILABLE = NO(only scale-bearing route):
  the abstract-polymer / operator-norm certifier is unavailable because every
  connection-only candidate is closed by algebra or refinement-instability —
  combinatorial per-cell normalization REFUTED (NC6); HS-ideal / operator-norm
  bound VACUOUS (per-cell ||X||_2 = ∞); commutator (connection-graph) route
  under-gains (p=1<3/2) AND vanishes on the certified diagonal block
  (C[C,Y]C=0). The clustering premise it needs (uniform/exponential) is
  undetermined and, for a gapless massless sea, not expected (power-law/metric).
  The only worked route is the per-4-volume action-density majorant plus spatial
  Schatten-2 exponent, which is itself REFUTED-IN-CONTINUUM (leading symbol
  survives: P alpha_x P = -n̂_x P ⇒ degree −3 ⇒ not HS) and FINITE-CARRIER-ONLY
  (⇒ running kappa_record, not an intensive object).

VERDICT = SCALE-ENTANGLED(C's intensive existence, insofar as it is certifiable
  by the sealed machinery at all, is inseparable from a scale/volume/metric
  statement: a 4-volume normalization forced by refinement-stability, a
  metric-kernel covariance decay, spatial Schatten-2 control, and a single
  certifying 4-volume exponent. A connection-only version is not merely missing
  but actively refuted for the realized (degree −3 kernel, infinite refinement
  families, unselected preparation) object.
  OPEN-GAP DIAGNOSTIC CARRIED INSIDE: exact missing connection-only sub-estimate
  = a refinement-invariant per-cell OPERATOR-NORM activity bound
  |Phi_gamma| <= eta^{|gamma|} on a combinatorial cell-adjacency connectivity,
  with an operator-norm (not spatial-Schatten-2) covariance decay in
  combinatorial cell-separation ≡ uniform clustering of the prepared state.
  Whether a scale-free version is even possible: NO on the sealed realization —
  obstructions O-I (refinement forces the 4-volume weight), O-II (the covariance
  is carried by the metric degree −3 sea kernel, non-HS per cell), O-III (the
  combinatorial clustering premise is unselected/undetermined and metric where
  realized). HONEST RESIDUAL: scale-entangled AND still UNCERTIFIED — the
  scale-bearing route is refuted-in-continuum / finite-carrier-only; the surviving
  finite-carrier branch is a running (RG) kappa_record, a new principle, not an
  intensive scale-free C.)

FORBIDDEN_IMPORTS = none. No scale, metric, volume, spatial Schatten norm, GR,
  KK ansatz, or faithfulness premise was consumed as authority. Every scale
  above (|C|_4, |D|_4, |r|, spatial Schatten-2, tau_R, alpha, eta) appears ONLY
  as a symbol inside the corpus's own quoted certification route, REPORTED AS
  THE FINDING that the only available route is scale-bearing, and explicitly NOT
  ENDORSED — per the tasking's instruction to report rather than adopt such a
  route. No value of n, kappa, alpha, any norm, exponent, or volume computed,
  bounded, estimated, or compared.

CONTAMINATED = no. The two forbidden files
  (STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md, STAGE8_REQUIRE_G1_CHECK_V001.md)
  were not opened; no register/tracker/plan/road/ledger/lens read; all source
  seals verified at path before reading; no git action; output name probed
  ABSENT before write.

alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until an adversarial cross-lineage check.
```
