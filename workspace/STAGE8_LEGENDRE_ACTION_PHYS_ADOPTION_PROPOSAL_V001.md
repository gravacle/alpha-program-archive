# LEGENDRE_ACTION_PHYS V001 — COMPLETED PHYSICAL LEGENDRE PAIRING AND ACTION MAP PROPOSAL

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-016 RESERVED)**

Date: 2026-08-03  
Lane: CODEX LANE 1  
Task: PASTE 401 / Task 4a / C39 action-race authored arm  
Register head used: Q-318  
Plan head: C39  
Status: **SIX-FIELD PROPOSAL COMPLETE FOR ADVERSARIAL REVIEW; NO PHYSICAL ACTION IS INSTALLED**

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

---

## 0. Lead determination

**The proposal has exactly six independent authored fields. It does not author
the source pairing, source Legendre map, or their coefficients: Q-318 derives
those uniquely.**

The frozen physical proposal is a family-wide **constrained flat-fiber
Legendre extension**. For every ratified field/background family member `b`, it
keeps the record-visible transverse fiber as physical content. Its completed
projection is the projective limit of the exact finite block projections, so
no complement or retraction member is selected. Each ratified family member
pulls the derived one-dimensional Legendre action back from the active base:

```text
0 -> K_b -> Y_C,b --pi_b-> Im(Leg_W,b) -> 0,

Gamma_b(Abar,G) :=
  gamma_parallel,b(pi_b(kappa_b(Abar,G))).
```

The corresponding physical level fiber is not quotiented. Distinct
record-visible cycle configurations
remain distinct points even though this proposed action is flat between them.
The full physical domain is therefore not the inadmissible scalar quotient
`E_src/ker(lambda)` killed by Q-318.

This is authorship, not derivation:

```text
CONSTRAINED_FLAT_FIBER_RULE_FORCED_BY_SEALED_STOCK = false | TYPE-U |
  would-build: principal ratification of proposed field R2
```

The choice is frozen for two target-independent reasons:

1. quotienting the cycle-current subspace is refuted because it deletes a nonzero record-visible
   cycle current;
2. after R1's proposed restriction certificate is assumed, a nonzero
   finite-visible transverse term fails the exact zero finite retarded shadows,
   while a finite-invisible tail term violates the standing finite-authority
   battery.

Those exclusions do **not** prove the zero transverse action. They leave the
flat constrained rule as the least authored extension, which is why it remains
on the proposal table.

The other five authored fields are the physical tangent realization, the
measure/delta pairing, the interacting contour, the boundary/contact
variational form, and the common graph domain/closure. No value, response,
root, coupling, or target-facing verdict is evaluated here.

```text
INDEPENDENT_PROPOSED_FIELD_COUNT = 6
PROPOSED_FIELDS = (R1,R2,R3,R4,R5,R6)

COMPLETED_PHYSICAL_ACTION_EXISTS = false | TYPE-U |
  would-build: ratify DoR-016 after adversarial review

PHYSICAL_CRITICAL_LOCUS_EXISTS = false | TYPE-U |
  would-build: ratification followed by the declared locus execution

STATIONARY_PHYSICAL_BLOCKS_EXIST = false | TYPE-U |
  would-build: ratification followed by the declared block execution

P_APPEARS_IN_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT
P_CANCELS_FROM_PHYSICAL_CYCLE_RESPONSE = NO_VERDICT
```

---

## 1. Preflight, currency, roots, and authorities

### 1.1 Currency correction

The relay was commissioned at Q-317. At send-time preflight, the independent
derivation arm had already entered Q-318. This proposal consumes Q-318 as a
constraint and removes everything that Q-318 derived from its authored table.
It does not coordinate outcomes with that arm.

```text
RELAY_PREFLIGHT_HEAD = Q-317
LIVE_HEAD_AT_FREEZE = Q-318
VERSION_DIFFERENCE_REPORTED = true
Q318_DERIVED_CONTENT_REAUTHORED = false | TYPE-R |
  test: R1-R6 contain none of P_src, Leg_W, their coefficients, W convention,
        N, or the same-correlator coefficient
```

### 1.2 Roots entered

```text
ROOTS_ENTERED = (
  /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
    alpha_fundamental_record_action_cleanroom_v003,
  /Users/bgm/MB Work/alpha_supervision,
  /Users/bgm/MB Work/alpha-program-archive/workspace
)

a32_holdout/custodian_private/ = NOT ENTERED | TYPE-S
```

No private-holdout path was listed, opened, or searched.

### 1.3 Frozen authorities

| Authority | SHA-256 | Load-bearing use |
|---|---|---|
| `LOCKED_PROCESS.md` | `f3bd9a62e172ba58667840e7717eb7ab8cd74be9fbe8d190802794ee5c3377ba` | fences, typing, custody |
| `QUESTIONS_SETTLED_REGISTER_V001.md` | `0f49c8ec6c29c22021532fae7f565428eb4154913a9c8495aa71ac914398f3c6` | live Q-318 head at freeze |
| `TASK_LIST_HERE_TO_ALPHA_2026-08-01_V002.md` | `6329ad6c8628dcb842da40ee0a9a184f893773ac3e03b1f2b2cf9b7829b6f399` | C39 commission and DoR-016 reservation |
| `RELAY_PASTE_401_DRAFT_THE_ACTION_V001.md` | `30ff4c79a16a42d3d4adf22f9259ce8149c9694615fbb7f79973c748908fbad6` | authored-arm contract and battery |
| `STAGE8_TASK4A_COMPLETED_RECORD_GEOMETRY_GEOMETRIC_LOCUS_CANONICAL_FUNCTIONAL_TEST_V001.md` | `cd29b21bfab2781dc047151d76821a9e977c5eb1f3e86bfddf7e2882d9c847a0` | `REC_GEOM_DIAG_399`, missing action arrow |
| `STAGE8_TASK4A_COMPLETED_PHYSICAL_LEGENDRE_PAIRING_AND_ACTION_MAP_DERIVATION_ATTEMPT_V001.md` | `d220a4dc9f5d49674cb88c6b08272e3304795117121c2b3a742152120bc1cfb7` | Q-318 source derivation, kernel obstruction, exact six-residue reduction |
| `STAGE8_TASK4A_P2_PHYSICAL_SOURCE_TOPOLOGY_AND_DIFFERENTIAL_CALCULUS_CONSTRUCTION_V002.md` | `40b2af34443e051fffdc7bf2ec7025c811a98c501a82d19164596ee6f37f00c1` | source topology, finite core, restrictions |
| `STAGE8_TASK4A_P4_PHYSICAL_LOG_GERM_ON_P2_CALCULUS_CONSTRUCTION_V001.md` | `b4c77ea948a02f3736fc824976f9ebd6381deff35f5c339a646739ea159725c5` | anchored `W` and derivative tower |
| `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V007.md` | `bd33e54c27ddb8ed5224637d7888a071026db33a1a17c7127a27ffa647b69896` | `N=i Id`, source realization, family discipline |
| `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V005.md` | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | `X_phys`, cycle currents, gauge/path quotient, six adopted structures |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | physical pairing, stationary blocks, Schur consumer contract |
| `primitive_record_cell_selection_principle_v004.md` | `13d227ceb2198d96b0e4e2fef57b874cea71cb755320508fbf4c6d64a00c507e` | formal 2PI source coefficients and first-order identities |
| `STAGE8_TASK4A_SOURCE_TO_OUTPUT_NORMALIZATION_N_UNIQUENESS_INSTALL_AND_FULL_DOOR_D_REEXECUTION_V001.md` | `00414b0d5617e44b12e9f7faef4484803c858992be6ed1951df38c22719b2696` | installed normalization and same-correlator identity |
| `STAGE8_TASK4A_PHYSICAL_INVERSE_SCHUR_COMPLETED_DOMAIN_AND_P_VERDICT_ATTEMPT_V001.md` | `c09783785546a8d6273b2fd104f3aeea0751e83c337ee4e8ac7677d9df87f3d0` | kernel witness and refusal of naive inverse |

### 1.4 Acts not performed

```text
source-pairing coefficient altered                    false | TYPE-S
W convention altered                                 false | TYPE-S
N or same-correlator coefficient altered             false | TYPE-S
record-visible kernel quotiented                     false | TYPE-S
anchor/torsor/orientation/rank/background selected   false | TYPE-S
new smooth manifold or metric imported               false | TYPE-S
alpha-facing target used to choose a field           false | TYPE-S
response/coupling/root/value evaluated               false | TYPE-S
register/plan/tracker/git/commit/push performed       false | TYPE-S
```

---

## 2. The derived base that this proposal may not change

Q-318 derives, on the P2 source carrier,

```text
P_src((J,R),(A,C)) = A(J) + (1/2) C(R),
Leg_W(s)           = (D_J W(s), 2 D_R W(s)),
W(s)               = F(lambda(s)),
lambda(J,R)        = L(J) - (1/2)Q(R).
```

It also proves

```text
D W(s)   = F'(lambda(s)) lambda,
D^2 W(s) = F''(lambda(s)) lambda tensor lambda.
```

The active image is one-dimensional and `Leg_W` is constant on every affine
`ker(lambda)` coset. The following are consequently immutable inputs:

| Item | Standing in this artifact | Proposal treatment |
|---|---|---|
| coefficient of `A(J)` | DERIVED INPUT | reproduced |
| coefficient of `C(R)` | DERIVED INPUT | reproduced |
| `W=-i hbar Log_0 Z` | RATIFIED/DERIVED INPUT | reproduced |
| `N=i Id` on active image | DERIVED INPUT | reproduced |
| same-correlator coefficient `i/hbar` | SEALED INPUT | reproduced |
| Q-276 sign | DERIVED INPUT | reproduced |
| CTP order, branch metric, reality covariance | RATIFIED INPUT | reproduced |
| connected subtraction on active line | DERIVED INPUT | reproduced |

The diagnostic scalar quotient has a Legendre chart, but it is not the
physical carrier. The nonzero cycle current `u_square` lies in the deleted
source kernel and is record-visible on `X_phys`.

```text
SCALAR_QUOTIENT_USED_AS_ACTIVE_BASE_COORDINATE = true | DERIVED_INPUT
SCALAR_QUOTIENT_USED_AS_PHYSICAL_CARRIER = false | TYPE-R |
  test: every Y_phys family member retains the full level fibers of pi_b o kappa_b
```

The distinction is load-bearing: an action may be constant along physical
directions without identifying those physical points.

---

## 3. Frozen carrier and proposal family

### 3.0 One declared input, six generated fields

The proposal's sole parent object is the assembled completed record geometry

```text
Omega_action := REC_GEOM_DIAG_399.
```

Each authored field is output by a frozen rule

```text
r_i : Omega_action -> R_i(Omega_action),  i=1,...,6,
```

specified in Section 4. `Omega_action` does not contain a physical action,
measure, contour, boundary form, or graph domain as a field. Those outputs are
therefore not recovered by projection. The proposed completed action is the
declared composition of the six `r_i` outputs with the Q-318 derived base.

### 3.1 Physical configuration extension

For every member `b` of the already-ratified no-selection family, start with
the adopted physical tangent carrier

```text
Y_C,b    := T_Abar,b direct-sum SymBil_cont(T_Abar,b),  coordinates (Abar,C),
Y_phys,b := T_Abar,b direct-sum SymBil_cont(T_Abar,b),  coordinates (Abar,G).
```

where `T_Abar,b` is the Gate-4/path-visibility quotient tangent on `X_phys`
completed in the retained A4 norm, and `SymBil_cont` is the adopted A6
continuous symmetric bilocal class. Door D and `N=i Id` embed the generated
active line `I_b:=Im(Leg_W,b)` into this carrier.

R1 also freezes, as one tangent-realization package, the executable maps

```text
iota_J,b : T_Abar,b -> J_src,b^vee,
  iota_J,b(Abar)[J] := <J,Abar>_b,

iota_R,b : SymBil_cont(T_Abar,b) -> R_src,b^vee,
  iota_R,b(G)[R] := <R,G>_b,

Prod_b : T_Abar,b x T_Abar,b -> SymBil_cont(T_Abar,b),
  Prod_b(A1,A2) := Sym(A1 tensor A2),

Conn_b(Abar,C) := C - Prod_b(Abar,Abar),

rho_A,N := adjoint of the ratified isometric finite inclusion,
rho_G,N := symmetric-bilocal adjoint induced by rho_A,N tensor rho_A,N.
```

The scalar brackets in `iota_J,b` and `iota_R,b` are R3's proposed physical
pairings; R1 and R3 are therefore jointly required but remain independent
premises. `Prod_b` has coefficient one. It cannot absorb the derived `1/2`
source coefficient or any additional normalization. The maps must commute
with the quotient, U1 reality covariance, and every W3 restriction.

The raw/connected chart conversion is

```text
kappa_b : Y_phys,b -> Y_C,b,
kappa_b(Abar,G) := (Abar, G + Prod_b(Abar,Abar)),

kappa_b^(-1)(Abar,C) := (Abar, Conn_b(Abar,C)).
```

Let `T_cyc,b` be the physical cycle-phase tangent distribution supplied by
the adopted A5 coordinates `Phi_c`; its cotangent is separated by the complete
conserved current family `u_c=d Phi_c`, `c in ker(B_N^T)`. Thus `u_c` remains
a source/cotangent object and is not misidentified with an `(Abar,G)` tangent.
At every finite stage, Q-243/Q-279 provide the exact active/noise versus
retarded cycle-phase block decomposition. R1 **proposes** the associated block projection

```text
pi_b,N : Y_C,b,N -> I_b,N
```

onto the active/noise line, with the retarded cycle-phase block in its kernel.
The W3 adjoints give the commuting relation

```text
rho_I,NM o pi_b,M = pi_b,N o rho_C,NM,   N <= M.
```

R1 authors the physical raw-dual carrier as the corresponding projective
completion

```text
Y_C,b := projective-limit_N Y_C,b,N,
I_b   := projective-limit_N I_b,N,
```

in the R6 graph topology. The projective universal property then defines the
single map

```text
pi_b((y_N)_N) := (pi_b,N(y_N))_N.
```

This is an executable instance, not a schema and not a selected complement.
It is continuous in R6's projective graph topology by construction. Any later
consumer requiring one norm-bounded realization must prove a uniform bound
from the finite maps; failure is an R1/R6 void test, not permission to change
topology.
Gauge/path quotient and U1 reality covariance hold componentwise and hence on
the limit.

The resulting proposed raw-dual exact sequence is

```text
0 -> K_b --i_b-> Y_C,b --pi_b-> I_b -> 0,

K_b := ker(pi_b).
```

`K_b` includes the raw-dual image of the record-visible cycle sector. The
corresponding physical flat distribution at `y in Y_phys,b` is

```text
K_phys,b(y) := ker(D(pi_b o kappa_b)_y).
```

No projection, complement, basis, cycle, anchor, torsor, rank, orientation,
or background member is selected.

The physical variables retain their `(Abar,G)` typing through the tangent
bundle described in R1. `I_b` is merely the generated active base. `Y_phys,b`
is the physical carrier.

### 3.2 The proposed action rule

R3-R5 realize the Q-318 source formula physically, without changing it:

```text
Z_phys,b[J,R] := nu_phys,b(
  R4-ordered law insertion with sources (J,R) and R5 endpoint glue
),

W_phys,b[J,R] := -i hbar Log_0 Z_phys,b[J,R],

P_phys,b((J,R),(A,C))
  := <J,A>_b + (1/2)<R,C>_b,

gamma_parallel,b(Leg_W(s))
  := W_phys,b(s) - P_phys,b(s,Leg_W(s)).
```

The last line is well-defined on the active local chart because Q-318 proves
that both terms depend only on `lambda(s)`. R3's cylinder restriction forces
`W_phys,b` to reproduce the installed `W` on every finite core. R4 and R5 add
only the completed contour and variational realization; they do not change
the displayed coefficients.

The construction is a completed **local action germ** on the ratified
`Log_0` chart. It does not author a global logarithm or analytic continuation.
A consumer requiring a global action outside that chart voids the proposal or
requires a separately gated field.

The proposal is

```text
Gamma_phys,b := gamma_parallel,b o pi_b o kappa_b.
```

Therefore

```text
D Gamma_phys,b =
  (D kappa_b)^* (D pi_b)^* D gamma_parallel,b,

D^2 Gamma_phys,b =
  (D kappa_b)^* (D pi_b)^* D^2 gamma_parallel,b
    (D pi_b)(D kappa_b)
  + D gamma_parallel,b[(D pi_b)(D^2 kappa_b)].
```

on the common graph domain proposed in R6. The projection is affine-linear on
each chart, so no unlisted second-derivative term of `pi` is inserted. The
displayed `D^2 kappa_b` term is the connected-product contribution and is not
dropped. It vanishes on the stationary locus only because
`D gamma_parallel,b=0` there.

For `k in K_phys,b(y)`,

```text
D Gamma_phys,b[k] = 0,

y in Crit(Gamma_phys,b) implies
  D^2 Gamma_phys,b[k, .] = 0.
```

This is a physical flat direction, not gauge and not deletion.

### 3.3 Constrained inverse rule

The proposal does not pretend that a rank-deficient Hessian has a two-sided
inverse. It authors the following constrained rule:

```text
Inv_constr(H_b) := inverse of H_b on I_b where it exists;
K_phys,b        := retained physical modulus, outside the inverse's domain;
Schur_constr     := Schur operation on the active exact-sequence base only.
```

No zero extension is called a full inverse. Any consumer that requires a
two-sided inverse on `K_phys,b` voids this proposal.

```text
NAIVE_FULL_HESSIAN_INVERSE_CLAIMED = false | TYPE-R |
CONSTRAINED_PARTIAL_LEGENDRE_RULE = PROPOSED_NOT_ADOPTED
```

---

## 4. Choice table — exactly six independent authored fields

### R1. Physical `(Abar,G)` tangent realization

**Proposed field.** For each ratified family member `b`, take `Y_phys,b` to be
the explicit adopted `T_Abar direct-sum SymBil_cont(T_Abar)` carrier, take its
raw-dual chart projective completion `Y_C,b`, and construct `pi_b` as the
projective limit of the exact finite active-line projections. `Abar` is the
linear tangent component and `G` the symmetric connected-bilocal tangent
component. The finite restrictions are the W3 adjoints of the ratified
isometric inclusions.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | the completed `Abar/G` classes, source/response intertwiners, connected product/subtraction, physical restrictions, and projective completed projection |
| Alternative 1 | identify source duals directly with physical fields — rejected by Q-318/Q-315 carrier mismatch |
| Alternative 2 | use only the scalar quotient — refuted because it deletes `u_square` |
| Alternative 3 | select a complement of `ker(lambda)` — rejected by no-selection and noncanonicity |
| Minimality | uses the finite projective system's universal map and adds no chosen split, basis, or member |
| Void condition | any finite restriction fails, any record-visible vector is identified or removed from `Y_phys`, or the `(Abar,G)` typing is not preserved |

```text
R1_PHYSICAL_TANGENT_REALIZATION = PROPOSED_NOT_ADOPTED
```

### R2. Transverse cycle action / constrained Legendre rule

**Proposed field.** Pull back the derived active action along
`pi_b o kappa_b` and keep the entire physical level fiber with tangent
`K_phys,b` as flat moduli.
Inversion and Schur operations are constrained to the active base; they never
quotient the fiber. Results remain family-wide over the ratified geometry,
anchor, torsor, orientation, and rank families.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | the rule that the ungenerated transverse physical action is zero and the lawful constrained inverse prescription |
| Alternative 1 | quotient the transverse fiber — TYPE-R, deletes record-visible content |
| Alternative 2 | add a nonzero finite-visible quadratic form — fails the Q-243/Q-279 restriction battery conditional on R1 |
| Alternative 3 | add a finite-invisible tail form — rejected by finite authority and unflagged-completion bar |
| Alternative 4 | select a pseudoinverse/regularizer — rejected as an extra unbounded choice |
| Minimality | adds no coefficient, scale, transverse metric, or selected cycle |
| Void condition | a sealed finite shadow has nonzero transverse Hessian; a consumer lawfully requires a two-sided inverse on the physical fiber; or a ratified common-origin term supplies nonzero transverse action |

The family is frozen without reference to any later parameter or target. Its
later response consequence is not used as a premise.

```text
R2_CONSTRAINED_FLAT_FIBER_RULE = PROPOSED_NOT_ADOPTED
```

### R3. Physical scalar pairing and measure/delta structure

**Proposed field.** R3 contains two different contractions and does not
identify them.

First, on each already-ratified geometric family member, use its adopted
volume density together with the ratified CTP branch metric to define the
local source-field pairing

```text
<J,Abar>_b := integral_C dmu_b J_I Abar^I,

<R,G>_b := integral_(C x C) dmu_b(x)dmu_b(y)
           R_IJ(x,y) G^IJ(x,y),

delta_b := identity kernel for this pairing on the finite core.
```

The branch sign is the ratified branch metric; the bilocal factor remains the
derived `1/2` in `P_src`. No independent density multiplier is admitted. The
compound-index order is the ratified U1 order.

Second, for every ratified origin-family member, let `nu_N` be the exact
finite operational CTP linear functional generated by the ratified law, that
family member's ready state, and the inclusive effect. No member is selected.
R3 authors
the previously absent marginal-consistency clause

```text
nu_M(Cyl_M(iota_NM f)) = nu_N(Cyl_N(f)),   N <= M,
sup_N ||nu_N|| < infinity,
```

and defines `nu_phys` as the unique continuous extension of the resulting
cylinder functional to the R6 graph closure. `nu_phys` is generally complex
and oscillatory; it is not asserted to be a positive probability measure.
The local density `dmu_b` and the history functional `nu_phys` are not
synonyms and neither is Haar measure on `Q_N`.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | the local physical pairing/delta plus the marginal-consistent oscillatory history functional and its extension rule |
| Alternative 1 | Haar on `Q_N` — sealed-refuted as descended physical measure |
| Alternative 2 | arbitrary positive history measure — rejected because CTP weights need not be positive and it adds unratified content |
| Alternative 3 | arbitrary local density multiplier — rejected because it adds a free local normalization |
| Alternative 4 | post-output state- or parameter-weighted measure — rejected by common-origin, freeze, and target-independence |
| Minimality | local pairing uses only adopted density/branch data; history functional is the continuous cylinder extension and adds no scalar coefficient |
| Void condition | marginal consistency or uniform boundedness fails, cylinder core is not dense, W3 adjoints fail, a finite coefficient changes, or the extension depends on a selected state/anchor/rank member |

```text
R3_PHYSICAL_MEASURE_DELTA_PAIRING = PROPOSED_NOT_ADOPTED
```

### R4. Interacting contour and boundary-value prescription

**Proposed field.** Use the ratified `E_post` orientation to define the
forward/opposite-backward CTP contour family `C_epsilon`, with the sign of the
deformation fixed by `E_post`. The boundary value is the `epsilon -> 0+`
limit in the R6 common graph topology. Difference source, probe source, and
connection history remain three distinct zero operations.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | the interacting boundary-value contour and named convergence topology |
| Alternative 1 | opposite orientation — TYPE-R against ratified `E_post` and the refuted exchanged root |
| Alternative 2 | Euclidean analytic continuation — rejected; no sealed continuation map licenses it |
| Alternative 3 | algebraic CTP with no boundary-value rule — insufficient for the completed action domain |
| Minimality | fixes only the orientation already ratified and one graph-limit prescription; no contour scale or member |
| Void condition | limit fails on the common core, violates U1 reality covariance, merges any two of the three zeros, or changes a finite restriction |

```text
R4_INTERACTING_CONTOUR = PROPOSED_NOT_ADOPTED
```

### R5. Boundary/contact variational form

**Proposed field.** The completed boundary form is the closure of the exact
finite sequential glue. Contact terms are only those generated by varying the
same frozen action and pairing. No independent post-output contact,
counterterm, parent term, curvature term, or distributed source term is added.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | the physical closure of finite glue and the no-independent-contact variational rule |
| Alternative 1 | independently adjustable local contacts — rejected by normalization and no-supplementation batteries |
| Alternative 2 | impose boundary-zero by convention — rejected because it can delete ratified endpoint data |
| Alternative 3 | choose a parent/curvature/distributed contact class — rejected as additional unratified physics |
| Minimality | closes existing glue and derives contacts from the same action; adds no separate term |
| Void condition | variation is not differentiable, finite endpoint data are lost, an extra contact is needed after outputs are read, or same-correlator identity changes |

```text
R5_BOUNDARY_CONTACT_FORM = PROPOSED_NOT_ADOPTED
```

### R6. Common graph domain and closure

**Proposed field.** Let `D_fin` be the ratified finite physical core. Before
any output is read, freeze the **finite** projective graph-seminorm family

```text
p_N(v) := ||rho_N v||_P2,N
        + ||D Gamma_N (rho_N v)||_N
        + ||D^2 Gamma_N (rho_N v)||_N
        + ||Cont_N (rho_N v)||_N,

tau_graph := projective locally-convex topology generated by {p_N}_N.
```

`Gamma_N` and `Cont_N` here are the exact finite action/contour operations,
not completed outputs. The proposed R3-R5 finite operations are included on
their declared common core. `D_graph` is the completion of `D_fin` in
`tau_graph` modulo vectors annihilated by every `p_N`, followed by the
already-ratified physical gauge/path quotient. Reality acts covariantly.

This removes a potential circularity: R6 does not define its topology using
the completed `Gamma_phys`. R1's `pi_b` is continuous by the universal
property of this projective topology. The completed R3-R5/Gamma operations
exist only if their finite graphs are closable on `D_graph`; failure voids the
relevant row.

| Question | Frozen answer |
|---|---|
| Adds beyond ratified data | one common invariant completion domain for action, contour, boundary form, and derivatives |
| Alternative 1 | maximal operator domains field by field — rejected because intersections need not be common or natural |
| Alternative 2 | weak-star/bidual completion — rejected by tail and finite-authority hazards |
| Alternative 3 | assume essential self-adjointness — rejected; it must be proved after ratification where applicable |
| Minimality | closure of the existing finite core in the operations actually consumed; no extra vectors |
| Void condition | core not dense, restrictions fail to be adjoints, quotient/reality invariance fails, or the completion creates finite-invisible response content |

```text
R6_COMMON_GRAPH_DOMAIN = PROPOSED_NOT_ADOPTED
```

### 4.7 Independence count

R1 fixes the physical tangent carrier; R2 fixes action content on its
transverse fiber; R3 fixes scalar contraction; R4 fixes temporal boundary
values; R5 fixes variational boundary/contact content; R6 fixes the common
closure domain. None is a consequence of another without an additional
premise.

```text
RAW_TABLE_ROW_COUNT = 6
DEPENDENCE_COLLAPSES = 0
HONEST_INDEPENDENT_PROPOSED_COUNT = 6
```

---

## 5. Consistency obligations — pre-ratification certificates

Every certificate in this section certifies the **proposal's syntax and
conditional consequences**. It does not install the proposed fields.

### C1. Exact finite restriction reproduction

On every finite stage, R1 restrictions are the W3 adjoints, R3 cylinder
pairings use the finite weights, R4 reduces to the sealed algebraic CTP order,
R5 reduces to exact finite sequential glue, and R6 contains `D_fin` as its
dense core. R2 contributes no transverse finite Hessian. Thus the restriction
of the proposal is the already-derived source germ and reproduces:

```text
Q-243: finite retarded block zero and parameter-free,
Q-279: exact probes-on retarded/noise sector pattern,
Q-276: installed sign,
Door D: same-correlator coefficient and normalization.
```

```text
C1_FINITE_RESTRICTION_CERTIFICATE = PASS_CONDITIONAL_ON_PROPOSAL
```

The certificate is falsifiable: any nonzero transverse finite Hessian or any
changed coefficient voids the proposal.

### C2. First-order derivative relations

Because `Gamma_phys` is a pullback of the derived active Legendre graph, its
active first variation is exactly the Q-318 first-order relation. Kernel
variations vanish by R2. R3 does not alter the derived `1` and `1/2`
coefficients.

```text
C2_FIRST_ORDER_RELATIONS = PASS_CONDITIONAL_ON_PROPOSAL
```

### C3. Nonstationary finite shadows

The completed proposed critical family is not declared to be the critical
set of any finite truncation. Restriction maps send completed locus data to
the honest Q-279 finite data, which remain nonstationary as Q-252 requires.
Restriction and taking `Crit` are not asserted to commute.

The Q-318 critical point is critical in the active **source-dual Legendre
coordinate**. Q-252's nonstationarity is a derivative with respect to the
finite **connection-history coordinate**. R1 does not identify those
coordinates; its restriction square carries one to the other through the
declared source-field map. Thus this certificate does not transport a
criticality identity across unlike derivatives.

```text
CRIT_COMMUTES_WITH_FINITE_RESTRICTION = NO_VERDICT |
  pre-ratification: no physical locus exists to test the square
Q279_NONSTATIONARY_SHADOW_PRESERVED = PASS_CONDITIONAL_ON_PROPOSAL
```

### C4. E_post, incidence, T_cyl, and X_phys compatibility

R1 is defined over `X_phys`; its physical flat distribution contains the
cycle-phase tangents separated by the ratified cycle-current family. R3 uses
the adopted physical family without selecting a geometry. R4
uses `E_post` for orientation. R5 descends finite incidence glue. R6 completes
the `T_cyl`/P2 finite core without changing the quotient. No new `(M,g)` is
introduced.

```text
C4_GEOMETRIC_COMPATIBILITY = PASS_CONDITIONAL_ON_PROPOSAL
```

### C5. Two-sector and kernel preservation

The active base carries the derived rank-one source content. Every `K_b`
contains the complete record-visible cycle sector. The projective exact
sequence preserves both; R2 makes the off-diagonal and kernel-kernel action
blocks zero without calling the kernel gauge.

```text
RECORD_VISIBLE_KERNEL_DELETED = false | TYPE-R |
KERNEL_DECLARED_GAUGE = false | TYPE-R |
C5_TWO_SECTOR_CERTIFICATE = PASS_CONDITIONAL_ON_PROPOSAL
```

### C6. Reality covariance

The source pairing is the derived one; R3 uses the ratified branch metric;
R4's forward/backward orientation is conjugated by the ratified involution;
R5 and R6 close invariant finite data. `Gamma_Theta(b)` is the conjugate
covariant action on the transformed family member, not an invariant scalar on
an untransformed member.

```text
REALITY_USED_AS_INVARIANCE = false | TYPE-R
C6_REALITY_COVARIANCE = PASS_CONDITIONAL_ON_PROPOSAL
```

### C7. Normalization ownership

```text
W_OWNS_MINUS_I_HBAR = unchanged
N_OWNS_I_ON_ACTIVE_OUTPUT = unchanged
IDENTITY_OWNS_I_OVER_HBAR = unchanged
SOURCE_PAIRING_OWNS_ONE_AND_ONE_HALF = unchanged
PROPOSED_FIELDS_OWN_NO_EXTRA_SCALAR_NORMALIZATION = true
```

```text
C7_NORMALIZATION_CERTIFICATE = PASS_CONDITIONAL_ON_PROPOSAL
```

### C8. Family discipline and finite authority

All six rules act over the complete ratified families. None evaluates a rank
ratio or chooses an anchor, torsor, orientation, cycle, frame, or background.
R2 forbids a finite-invisible tail action; R6 forbids a completion-created
response. Thus no alpha-facing consumer is granted non-finite physics by this
proposal.

```text
C8_NO_SELECTION_AND_FINITE_AUTHORITY = PASS_CONDITIONAL_ON_PROPOSAL
```

### C9. Common-input and no-supplementation certificate

Every R1-R6 rule has the same declared input `REC_GEOM_DIAG_399`. The rules,
alternatives, and void conditions are frozen in this artifact before any
action, locus, block, or response output is read. No state, density,
counterterm, contour member, domain vector, or retraction is accepted after
output.

```text
C9_COMMON_INPUT_NO_SUPPLEMENTATION = PASS_CONDITIONAL_ON_PROPOSAL
```

---

## 6. Proposed locus and stationary-block execution after ratification

This section defines the executable program. It does not execute it.

### 6.1 Locus theorem to run

Let `y0,b` be the source-free critical point of the Q-318 active local dual
chart. If R1-R6 are ratified and their domains pass, then

```text
Crit(Gamma_phys,b) = (pi_b o kappa_b)^(-1)({y0,b}).
```

The critical set is a full physical level fiber with tangent
`K_phys,b`. The locus is the complete family over `b`; it is gauge-covariant
and contains no selected member. This is
the proposed replacement for a
separate background-formation map.

```text
PROPOSED_LOCUS_FORM = ACTIVE_CRITICAL_LEVEL_FIBER_OVER_K_PHYS_FAMILY
LOCUS_NONEMPTY_AFTER_RATIFICATION = TO_BE_EXECUTED
```

### 6.2 Stationary blocks to run

On the proposed locus, the action Hessian has the block form

```text
H_phys,b = [ H_parallel,b   0 ]
           [      0         0 ]
```

relative to the exact-sequence base/fiber structure, without selecting a
splitting in the carrier. The `(AA,AG,GA,GG)` presentation is obtained by R1's
typed source-field maps. R2 directs inversion/Schur only on the active block.

After ratification the execution must:

1. prove `H_parallel,b` has the required active inverse on the locus;
2. construct the four typed stationary blocks;
3. verify uniformity over the full background and no-selection families;
4. run the finite restriction squares;
5. only then trace any symbolic parameter through the physical response.

No cancellation or survival result is claimed in this proposal.

### 6.3 Consumer interfaces

| Consumer | Receives after ratification and execution |
|---|---|
| geometric-locus program | `Crit(Gamma_phys)` as a family, not a chosen background |
| physical inverse/Schur | R2 constrained active inverse plus explicit retained kernel |
| retarded response | stationary `(AA,AG,GA,GG)` blocks on R6 domain |
| finite falsifier | exact Q-243/Q-279 restrictions from C1 |
| target-facing verdict | only the post-ratification symbolic dependence trace; no value |

---

## 7. Door and completion ledger

| Door | Class-formation or operation | Topology/domain | Kernel/image | Restriction square | Tail action | Proposal result |
|---|---|---|---|---|---|---|
| A | R1 projective physical realization | adopted physical tangent topology | kernel `K_b`; image `I_b` | W3 adjoints | no tail added | PASS conditional |
| B | R2 pullback action family | R6 graph domain | stationary Hessian kernel contains `K_phys,b`; active image only | exact zero transverse finite restriction | tail action forbidden | PASS conditional |
| C | R3 physical pairing | cylinder/projective pairing topology | pairing nulls only ratified quotient nulls | finite delta/pairing reproduced | no bidual scalarization | PASS conditional |
| D | source-to-action/field image | R1-R3 carrier | active image plus retained physical fiber | Door-D coefficients unchanged | no unaccounted output tail | PASS conditional |
| E | R4 contour boundary value | R6 graph topology | boundary-value kernel reported at execution | finite algebraic CTP restriction | no separate tail prescription | PASS conditional |
| F | R6 projective class formation | P2 graph completion | zero graph seminorm only, then ratified quotient | finite core dense; W3 adjoints | completion-created response voids | PASS conditional |
| G | R5 boundary closure | R6 common graph domain | only finite-glue nulls | exact sequential glue | no post-output contact | PASS conditional |
| H | constrained Schur operation | active-base domain only | `K_phys,b` retained outside inverse | finite active block reproduced | no tail inverse | PASS conditional |

The door ledger contains no claim that the proposed limits exist before
ratification and execution. A failed door voids the corresponding proposal
field and therefore DoR-016 as a package.

---

## 8. Mandatory self-kill passes

### 8.1 Hidden-adoption sweep

The definitions were swept for unlisted choices. The following candidates
were found and removed:

1. a chosen complement to `ker(lambda)`;
2. a selected transverse metric or quadratic coefficient;
3. an arbitrary scalar density multiplier;
4. a separate contact/counterterm family;
5. assumed essential self-adjointness;
6. a weak-star or bidual completion;
7. a chosen background or cycle.

The remaining authored content is exactly R1-R6.

```text
HIDDEN_EIGHTH_FIELD_FOUND = false | TYPE-R |
  test: every non-derived clause maps to exactly one of R1-R6
```

### 8.2 Smuggled `(M,g)` sweep

R3 uses the geometric family already adopted by DoR-015. It neither selects a
member nor adds a new manifold, metric, smooth structure, or external field.
Its new content is the physical history pairing, disclosed as R3.

```text
NEW_M_G_IMPORTED = false | TYPE-R
```

### 8.3 B14 anti-bundling test

Define a bundled candidate by placing an arbitrary action or state inside
`Y_phys` and returning it by projection. Such a candidate can change the
output while leaving the declared source exact sequence unchanged. It fails
this proposal because:

```text
R1 contains carrier/intertwiner data only and no action output;
R2 fixes the complete transverse action rule before outputs;
R3-R6 fix every contraction/limit/boundary/domain operation before outputs;
no output field is an input field of Y_phys.
```

Thus the proposed action is generated by the declared composition of the
derived base with R1-R6; it is not a bag containing its output.

```text
B14_BUNDLING_COUNTERMODEL_PASSES = false | TYPE-R |
  test: arbitrary projected action violates R2 and the six-field freeze
```

### 8.4 Affine-witness test on measure and boundary freedom

Consider

```text
dmu -> dmu + delta_mu,
nu  -> nu  + delta_nu,
B    -> B + C,
```

where `delta_mu`, `delta_nu`, or `C` vanishes on every inspected finite
output. Those deformations are the action analogue of the affine state witnesses that
killed earlier drafts. They are not admitted:

1. R3 fixes the local family rule to the already-adopted objectwise density
   and branch metric, with no additive density slot;
2. R3 fixes the history object as the unique continuous extension from a
   dense cylinder core, so `delta_nu` zero on that core is zero everywhere;
3. R5 permits only closure of finite glue and variation-generated contacts;
4. R6 voids completion-created finite-invisible response content;
5. finite authority rejects an undetectable target-facing tail.

```text
AFFINE_MEASURE_WITNESS_ADMITTED = false | TYPE-R
AFFINE_BOUNDARY_WITNESS_ADMITTED = false | TYPE-R
```

### 8.5 Circular-certificate test

No field is certified by the output it is supposed to produce:

| Field | Independent certificate |
|---|---|
| R1 | exact sequence, W3 restrictions, kernel retention |
| R2 | finite shadows, finite authority, no deletion |
| R3 | cylinder marginal/restriction and coefficient reproduction |
| R4 | E_post orientation, graph-limit existence, reality covariance |
| R5 | exact finite glue and no post-output supplementation |
| R6 | density, invariance, restriction adjoints, no new tail |

The future parameter verdict is not a certificate for any row.

### 8.6 Answer-defined and target-tuning sweep

The proposal family was frozen before any response dependence was traced. No
field refers to a desired cancellation, survival, coupling, root, or measured
quantity. R2's zero transverse term follows proposal minimality plus the
finite-authority battery, not a desired target output. Because that reasoning
still does not make R2 a theorem, it remains disclosed as authorship.

```text
TARGET_AWARE_CONSTRAINT_USED = false | TYPE-R
```

### 8.7 State-valued freedom sweep

R3 measure, R4 contour, R5 boundary form, and R6 domain are forbidden to
select or add a state member, charge weighting, anchor member, or rank value.
R3's finite functional depends functorially on each already-ratified origin
family member, and the completed action may consume that ratified
state-derived source data through `W`; none of the six authored fields selects
or redefines it.

```text
NEW_STATE_SLOT_CREATED = false | TYPE-R
```

### 8.8 Strongest surviving weakness

The weakest field is R2. A nonzero common-origin transverse action discovered
later would void the flat-fiber rule. Conversely, a consumer that genuinely
requires a two-sided inverse on the record-visible fiber would refute the
constrained rule rather than authorize quotienting the fiber.

```text
PROPOSAL_SURVIVES_SELF_KILL_PASS = true | PROPOSED_NOT_ADOPTED
WEAKEST_FIELD = R2_CONSTRAINED_FLAT_FIBER_RULE
```

---

## 9. Adjudication-ready choice table

| Field | Bare proposed choice | What ratification adds | Primary void condition |
|---|---|---|---|
| R1 | unsplit physical tangent exact sequence | source-to-`(Abar,G)` physical realization | restriction or kernel-retention failure |
| R2 | active pullback, flat physical fiber, constrained inverse | action content on the Legendre kernel | nonzero sealed transverse action or need for full inverse |
| R3 | adopted-density CTP pairing, no extra multiplier | measure/delta contraction | marginal, coefficient, or family failure |
| R4 | `E_post`-oriented graph-limit CTP contour | interacting boundary-value prescription | limit/reality/three-zero failure |
| R5 | finite-glue closure, variation-generated contacts only | boundary/contact variational form | differentiability or no-supplementation failure |
| R6 | minimal common graph closure of finite core | common completed domain | density, invariance, W3, or tail failure |

The six choices are proposed as one indivisible package. A failed row returns
the package for repair; it does not license silently selecting an alternative.

```text
PROPOSAL_PACKAGE_NAME = LEGENDRE_ACTION_PHYS_V001
PROPOSAL_PACKAGE_SIZE = 6
RATIFICATION_SLOT = DoR-016_RESERVED
PACKAGE_INSTALLED = false | TYPE-U
```

---

## 10. Final determination

The action-race derivation arm removed the source pairing and Legendre map from
the authored burden. The honest proposal is therefore a six-field physical
extension, not a seven-component reconstruction and not a re-adoption of
proved normalizations.

Its decisive physical premise is explicit: **record-visible transverse
directions remain distinct physical moduli, while the proposed action is flat
along them and the Legendre/Schur operation is constrained to the active
base.** This is the least extension that neither deletes the cycle sector nor
adds finite-invisible physics. It is still authorship and awaits the gate.

```text
PROPOSAL_COMPLETE_FOR_ADVERSARIAL_REVIEW = true
PROPOSAL_RATIFIED = false | TYPE-U

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-016 RESERVED)**
