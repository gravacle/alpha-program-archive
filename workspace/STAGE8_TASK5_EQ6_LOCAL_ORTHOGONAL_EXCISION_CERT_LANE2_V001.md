# STAGE 8 TASK 5 / EQ6 — LOCAL ORTHOGONAL EXCISION CERT — LANE 2 V001

Date: 2026-08-04  
Lane: Codex Lane 2  
Task: 5 / EQ6 / joint cycle-creating admission  
Custody: builder; hostile check required

## Lead result

```text
EXCISION_CERT = BUILT |
  failure-capable proof object, defect calculus, covariance, composition,
  bundle discipline, and actual-diamond law are constructed

PHYSICAL_EXCISION_CERT_FAMILY = STOPPED_AT(
  NONEMPTY_COVARIANT_ZERO_DEFECT_SECTION_ON_FIXED_DOR019_Q408_DATA)

D_DISJOINT =
  CONDITIONAL_PASS under two coherent certificate terms;
  ACTUAL_PHYSICAL_ADMISSION remains TYPE-U at
  (OLD_FID defects, orthogonal-excision defect, support-excision defects)

D_CONTACT =
  CORRECTED_REQUIREMENTS:
    OLD_FID + EXCLUSIVE_REGION_RNL + LR;
  CONDITIONAL_PASS under two coherent certificate terms;
  ACTUAL_PHYSICAL_ADMISSION remains TYPE-U at the same fixed-data defects

DIAMONDS =
  subdivision = PASS / ADMITTED;
  disjoint cycle creation = CONDITIONAL_PASS / PHYSICAL_TERM_OPEN;
  contact cycle creation = CONDITIONAL_PASS / PHYSICAL_TERM_OPEN

ALL_RANK = PARTIAL / TYPE-U |
  the certificate class is built but no nonempty coherent physical section
  over every positive-source primitive orbit is proved

JOINT_EQ6 = PARTIAL / TYPE-U |
  no downstream fiber consumes a merely conditional cycle-creating arrow

DIRECT_SUM_MODEL = CERTIFIED_MODEL_TERM / TYPE-P
DIRECT_SUM_MODEL_IS_PHYSICAL_INHABITANT = false
JOINT_UNSATISFIABLE = false / TYPE-R
MACHINERY_APPEAL = false
```

The named certificate is now a real mathematical object rather than a
phrase: it has data, zero-defect equations, a decision procedure on every
finite candidate, covariance laws, composition, and a common-refinement
law.  The direct-sum compatibility model inhabits that object.  What the
ratified stack still does not provide is an actual retained surface family
whose fixed DoR-019 metric and Q-408 analysis maps give certificate terms
on every positive-source cycle-creating leg.  The distinction is
load-bearing: compatibility is proved; physical family-level inhabitance is
not.

---

## 0. Preflight, authorities, and register sweep

### 0.1 Three-line preflight

```text
DOES THE OBJECT EXIST?  The certificate class does not exist before this
                        artifact; the compatible direct-sum model and its
                        required typing do exist at Q-442.
IS THE VERSION CURRENT? YES, through register head Q-442.
ARE ITS INPUTS PRESENT? YES: the Q-442 check, Q-441 diamond build, adopted
                        W1/W4 clauses, fixed DoR-019 carrier geometry,
                        Q-408 analysis/support maps, DoR-020+A1.
PREFLIGHT = PASS
```

`LOCKED_PROCESS.md` was read in full before task action.  The Q-442 check,
the Q-441 build, and the settled register were seal-verified before use.

| Authority | SHA-256 | Use |
|---|---|---|
| locked process | `d537e294c03b3fc50fa49844f5b166bdcee1d64fe1513b9201047f602cd1518f` | custody; fences; surface/rails split |
| register at Q-442 | `c1b0090a17d5897a6de0a0803be6670225e748e4d368bbff13296c54498d8136` | current head |
| Q-442 hostile check | `6cd40961b3fc7862e5f6e92904b49d595115ba612d74b97a7e27df096f18d445` | direct-sum model; certificate typing; contact correction |
| Q-441 all-rank build | `dec994976774bf598e79cd496f6b424d777d2321df2b41c0393219a3247c3ad6` | actual diamonds and fixed-data defects |
| verified first-orbit member | `c0cc95112bf8c213b0e2c4095a06c4e4658fa3950641b9a21fa51950a9f818fa` | admitted zero-source member |
| adopted where-clauses | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | W1/W4 membership law |
| pass-1 witness hunt | `452f1bb87aeb7f7bfe4ab4556134cf723ab6df697f93c1a59c33671146cb0083` | six-fiber subpackages |
| metric V005 / DoR-019 object | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | fixed A4 current form, Riesz maps, W3 scope, units |
| DoR-020 decision | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | conditional package and `[EQ6]` |
| DoR-020-A1 | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | adopted bundle-typed where-laws |

### 0.2 Register sweep

```text
Q-408  finite current/kernel realization, analysis maps, and local supports;
Q-418  actual rank-preserving subdivision with P=id;
Q-422  DoR-020 is conditional on one joint [EQ6] witness;
Q-427  actual surface geometry must not be replaced by rails;
Q-430  an algebraically commuting Riesz mixer can leak across supports;
Q-432  relative no-leakage replaces global orthogonality;
Q-438  DoR-020-A1 adopts W1/W4 as possibly-empty membership laws;
Q-439  the zero-source first-cycle member exists;
Q-440  positive-source members and actual diamonds are the all-rank need;
Q-441  three actual diamonds were run; both cycle-creating legs stopped;
Q-442  joint inconsistency is refuted; the missing object is the
       LOCAL_ORTHOGONAL_EXCISION_CERT, and contact retains OLD_FID plus
       exclusive-region RNL in addition to LR.
```

No settled result proves that the fixed physical family contains a
cycle-rank-increasing member with local orthogonal excision.  No settled
result licenses replacing its metric by the direct-sum model.

### 0.3 Bearing symbol distinctions

```text
calB                      retained physical surface/background;
B_G                       graph incidence operator;
P_bundle -> calB          U(1) bundle;
P_r                       canonical Q-408 test transport;
E_r^geom                  geometry-defined old-current excision projector;
E_r;O,W^orth              support-indexed fixed-metric orthogonal projector;
R_K,G                     fixed DoR-019 Riesz map;
Abar_G                    Q-408 analysis isomorphism;
Phi_G=R_K,G^(-1)Abar_G    current/test intertwiner;
F_r                       actual support-region map;
F_conn                    U(1) connection curvature;
c_1(P_bundle)             first Chern class;
c_U,c_V                   graph-cycle currents;
direct-sum model term      consistency witness for the certificate class;
physical certificate term proof on an actual retained fixed-data member.
```

No path, rank, orientation, frame, metric, bundle gauge, refinement, or
computation member is bound.

---

## 1. H1 — construction of `LOCAL_ORTHOGONAL_EXCISION_CERT`

### 1.1 Input contract: actual geometry first

Let `r:G->G'` be an actual cycle-creating W1/W4 candidate under the adopted
law.  It supplies before any response-facing test:

```text
Gamma_r       actual old/new path correspondence;
S_r:K_G->K_G' induced injective conserved-current map;
F_r           actual support-region map;
tilde_f_r     smooth equivariant U(1)-bundle lift;
Cof_r,Dens_r  full-rank coframe and positive-density relations;
eta_conn,r    connection pullback;
eta_curv,r    curvature pullback;
c_1 source = f_r^* c_1 target.                 (H1-1)
```

The fixed physical carrier then supplies

```text
(K_G,R_K,G,Abar_G,Phi_G,Tbar_G(O)),
(K_G',R_K,G',Abar_G',Phi_G',Tbar_G'(O')).       (H1-2)
```

Nothing in the certificate may change `(H1-1)` or `(H1-2)`.

The actual path/current correspondence determines an old image
`S_r K_G`.  When the actual new-cell relation supplies a complementary
new-current sector, define the geometry excision map by

```text
E_r^geom:K_G'->K_G',
(E_r^geom)^2=E_r^geom,
image(E_r^geom)=S_r K_G,
E_r^geom S_r=S_r,                               (H1-3)
```

with `ker(E_r^geom)` generated by the actually new cycle directions.  This
is not inferred merely from dimension.  If the actual path/current data do
not define `(H1-3)` without choosing a cycle basis or deleting a visible
current, the candidate has no excision datum and is rejected.

For the two concrete Q-441 diamonds, `(H1-3)` is geometry-defined:

```text
E_U^geom(x c_U+y c_V)=x c_U,
E_V^geom(x c_U+y c_V)=y c_V.                   (H1-4)
```

The formulas are carried covariantly under exchanging `U,V`; neither
direction is preferred family-wide.

### 1.2 The support-indexed fixed-metric orthogonal projector

Global orthogonality is forbidden by Q-432.  The certificate therefore
forms a projector only on each RNL-relevant local pair.  For an old local
region `O` and a disjoint or contact-exclusive new region `W`, set

```text
A_r(O):=S_r Phi_G(Tbar_G(O)),
N_r(W):=ker(E_r^geom) intersect K_G'(W),
H_r(O,W):=A_r(O)+N_r(W).                        (H1-5)
```

The actual support relation makes the sum in `(H1-5)` direct on an
admissible exclusive pair.  Let

```text
E_r;O,W^geom(a+n)=a
for a in A_r(O), n in N_r(W).                  (H1-6)
```

Because the fixed form is positive, its orthogonal projector onto
`A_r(O)` inside `H_r(O,W)` exists uniquely:

```text
E_r;O,W^orth
 :=j_A (j_A^* R_K,G' j_A)^(-1)
       j_A^* R_K,G' |_H,                       (H1-7)
```

where `j_A:A_r(O)->H_r(O,W)`.  Equation `(H1-7)` is diagnostic, not a
repair.  The local-orthogonal mechanism is the failure-capable equality

```text
E_r;O,W^geom=E_r;O,W^orth                      (H1-8)
```

for every RNL-relevant local pair, and says nothing about arbitrary
pre-existing target sectors.  Thus the Q-432 rank-preserving `P=id`
witness remains admissible.

When `A_r(O)=0`, both local projectors are the unique zero map on the new
local sector; no inverse on a nonzero source space is invoked.

For the rank-two Gram matrix on a minimal disjoint pair

```text
[g_K,G']_(c_U,c_V)
 =[[alpha,zeta],[conj(zeta),delta]],            (H1-9)
```

`E_U;O,W^orth(c_V)=(conj(zeta)/alpha)c_U`, whereas
`E_U;O,W^geom(c_V)=0`.  Thus `(H1-8)` holds on that local pair exactly when
`zeta=0`.  The Q-430 mixer is rejected without rotating `S_r` or changing
the fixed metric.

### 1.3 The defect calculus

For each fixed candidate, construct four finite failure operators.

**Fidelity defect**

```text
Def_fid(r):=S_r^* R_K,G' S_r-R_K,G.             (H1-10)
```

`Def_fid=0` is exactly `OLD_FID`.

**Orthogonal-excision defect**

```text
Def_orth(r;O,W)
 :=E_r;O,W^geom-E_r;O,W^orth.                  (H1-11)
```

`Def_orth=0` says the actual local support excision is the fixed-metric
orthogonal excision on the relevant pair.  Equivalently,

```text
g_K,G'(S_r Phi_G(a),n)=0
for a in Tbar_G(O) and n in N_r(W).             (H1-12)
```

**Local leakage defects**

For an old local region `O` and a target new-cycle region `W` satisfying
the ordinary RNL antecedent `F_r(O) intersect W=empty`, set

```text
Def_leak(r;O,W)
 :=i_W^* R_K,G' S_r Phi_G i_O.                 (H1-13)
```

Here `i_O` and `i_W` are the ratified local-sector inclusions.  The zero of
every `(H1-13)` is precisely RNL on the transported analysis image.  For an
endpoint-contact primitive with contact locus `C_r`, the mandatory family
includes every pair of exclusive regions

```text
O subset old_support\C_r,
W subset new_support\C_r,
F_r(O) intersect W=empty.                      (H1-14)
```

Contact-containing pairs may make the antecedent false; they do not remove
the exclusive family `(H1-14)`.

**Support-excision defects**

The canonical test transport is fixed:

```text
P_r:=Phi_G'^(-1) S_r Phi_G.                    (H1-15)
```

Let `q_G',F_r(O)` be the quotient by the target local test subspace.  Define

```text
Def_supp(r;O)
 :=q_G',F_r(O) P_r i_O.                        (H1-16)
```

Every `(H1-16)` vanishes exactly when

```text
P_r(Tbar_G(O)) subset Tbar_G'(F_r(O)),          (H1-17)
```

which is LR.  `Def_orth=0` does not by itself imply `(H1-17)`; the Q-408
analysis map can remain nonlocal even for a diagonal Gram matrix.

All four defects are finite symbolic operators on built carriers.  A finite
stage has finitely many distinct path/cell support strata; regions inducing
the same local inclusion or quotient operator give the same check.  The
resulting finite support-stratum family can be evaluated without selecting
a coefficient or a member.

### 1.4 Certificate definition

Define the proof object

```text
LOCAL_ORTHOGONAL_EXCISION_CERT(r) :=
  data:
    actual E_r^geom from the W1 support/current relation;

  proofs:
    LOE1  Def_fid(r)=0;
    LOE2  Def_orth(r;O,W)=0 for every RNL-relevant local pair;
    LOE3  Def_leak(r;O,W)=0 for every ordinary disjoint pair and every
          contact-exclusive pair (H1-14);
    LOE4  Def_supp(r;O)=0 for every actual old local region;
    LOE5  W1 incidence/current/support and all W4 bundle equations (H1-1);
    LOE6  covariance, reality, units, restriction, composition, and actual
          common-refinement coherence.                         (H1-18)
```

The apparent redundancy between LOE2 and LOE3 is deliberate.  LOE2 tests
the geometry-defined excision on each relevant local pair; LOE3 is the
same required zero written directly in the ratified RNL typing, including
contact-exclusive regions.  Neither asserts global orthogonality.  Either
presentation can catch a domain or scope error the other does not expose.

The certificate is failure-capable:

```text
Cert_LOE(r)=
  PASS(proof record) if every defect and bundle equation vanishes;
  FAIL(named defect,witness) otherwise.          (H1-19)
```

It does not assert that every candidate passes.

This certificate is a sufficient construction route, not a new necessary
axiom for every possible P4 member.  A candidate lacking a geometry-defined
excision datum may still be tested directly against OLD_FID, RNL, and LR;
it simply does not inhabit this named certificate subfamily.  Thus the
construction does not strengthen DoR-020-A1 by stealth.

```text
EXCISION_CERT_CLASS = BUILT / TYPE-P
EXCISION_CERT_DECISION_PROCEDURE = BUILT_ON_EACH_FINITE_CANDIDATE
EXCISION_CERT_INHABITANCE_ASSERTED = false
```

### 1.5 Covariance, reality, metric fidelity, and units

For an admitted realization automorphism or covariant carrier map
`(U_G,U_G')`, every datum transforms by conjugation:

```text
S_r |-> U_G' S_r U_G^(-1),
R_K,G |-> (U_G^(-1))^* R_K,G U_G^(-1),
E_r^geom |-> U_G' E_r^geom U_G'^(-1),
Phi_G |-> U_G Phi_G V_G^(-1).                  (H1-20)
```

Substitution into `(H1-7)` on each transported local pair shows

```text
E_r;O,W^orth |->
  U_G' E_r;O,W^orth U_G'^(-1).                 (H1-21)
```

Consequently `Def_fid`, `Def_orth`, `Def_leak`, and `Def_supp` transform by
conjugation or the corresponding domain/codomain equivalence.  Their zero
loci are invariant.  Orientation reversal changes the signs of current
representatives on both sides; reality conjugates the Hermitian pairings;
all zero equations survive.  No orientation or basis is chosen.

`R_K` carries its already ratified `U_K^-2` Riesz class.  Equations
`(H1-7)`–`(H1-16)` compose only matching K/test dualities and introduce no
new conversion unit.  The certificate neither rescales `R_K` nor fixes any
action scale.

### 1.6 Bundle discipline

The certificate never factorizes or replaces the U(1) data.  LOE5 requires
the same actual smooth full-rank bundle square as W4:

```text
pi' tilde_f_r=f_r pi,
P_source isomorphic_to f_r^*P_target,
c_1(P_source)=f_r^*c_1(P_target),
eta_conn,r(A_target)=A_source,
eta_curv,r(F_target)=F_source,
eta_curv,r Curv=Curv eta_conn,r,                (H1-22)
```

with positive density, coframe rank, E_post, reality, and unit
compatibility.  Current excision is therefore a certificate on the
physical cycle carrier, not a split of the bundle or a gauge choice.

### 1.7 Composition theorem

Let `r:G->G'` and `s:G'->G''` carry certificate terms.  Write

```text
L_s: image(E_s^geom)=S_s K_G' -> K_G'
```

for the inverse of `S_s` on its image.  The geometry-defined composite
excision is

```text
E_sr^geom:=S_s E_r^geom L_s E_s^geom.          (H1-23)
```

It is an idempotent onto `S_s S_r K_G`; no complement basis is chosen.
The fidelity defect composes exactly:

```text
Def_fid(sr)
 =S_r^* Def_fid(s) S_r+Def_fid(r)=0.           (H1-24)
```

For an actual old local stratum and the inherited/fresh new strata, write

```text
A_sr(O)       =S_sS_rPhi_G(Tbar_G(O)),
N_inherited   =S_s N_r(W_r),
N_fresh       =N_s(W_s).                       (H1-25)
```

LOE2/LOE3 for `r`, transported by the OLD_FID isometry of `s`, makes
`A_sr(O)` orthogonal to `N_inherited`.  LOE2/LOE3 for `s` applies to every
local source-analysis stratum, including the inherited one supplied by
LOE4, and makes both transported source strata orthogonal to `N_fresh` on
the corresponding disjoint/exclusive pairs.  Thus the local restriction
of `(H1-23)` agrees with the fixed-metric orthogonal projector on each
composite RNL-relevant support pair.  No global old/new orthogonal sum is
asserted.

The canonical test maps compose without a new premise:

```text
P_s P_r
 =Phi_G''^(-1)S_sPhi_G' Phi_G'^(-1)S_rPhi_G
 =Phi_G''^(-1)S_sS_rPhi_G
 =P_sr.                                        (H1-26)
```

Applying the two LR inclusions successively gives

```text
P_sr(Tbar_G(O))
 subset Tbar_G''(F_s(F_r(O))).                 (H1-27)
```

Bundle composition follows from pullback functoriality and curvature
naturality.  Thus certificate terms are closed under composition.

### 1.8 Actual common-refinement theorem

For an actual diamond

```text
G --r--> G_1 --s--> H
|                    ^
u                    |v
v                    |
G_2 -----------------+
```

assume the four legs carry certificate terms and the actual W1/W4 diamond
has

```text
S_sS_r=S_vS_u,
F_sF_r=F_vF_u,
eta_s eta_r=eta_v eta_u.                       (H1-28)
```

The two composite canonical test maps are equal by `(H1-26)`.  The
geometry-defined composite old image and its support are the same on both
routes; uniqueness in `(H1-7)` on each local pair makes the local
orthogonal projector the same.  Equations `(H1-24)`–`(H1-27)` therefore give identical composite
certificate terms.  Hence the certificate commutes on actual diamonds.

If the two routes do not determine the same actual old image/support split,
`(H1-28)` fails and the diamond is rejected rather than repaired
categorically.

```text
CERT_COMPOSITION = PROVED
CERT_ACTUAL_DIAMOND_COHERENCE = PROVED
GENERIC_BATCHING_ISOMETRY_USED = false
```

### 1.9 Direct-sum compatibility term and its scope

On the Q-442 model

```text
K_G'=S_rK_G direct-sum N_r,
R_K,G'=S_(r,* )R_K,G direct-sum R_N,
Tbar_G'=P_rTbar_G direct-sum Tbar_N,
Phi_G'=Phi_G direct-sum Phi_N,                  (H1-29)
```

take `E_r^geom(x,n)=(x,0)`.  On every relevant local pair, `(H1-7)` gives
`E_r;O,W^orth=E_r;O,W^geom`; every defect `(H1-10)`–`(H1-16)` is zero, and identity
bundle pullbacks satisfy LOE5.  This is a complete certificate term.

It proves

```text
Cert_LOE is consistent and inhabited in the compatibility model.
```

It does not prove

```text
some actual retained DoR-019/Q-408 surface member realizes (H1-29).
```

That scope is exactly Q-442's ruling.

---

## 2. H2 — corrected contact diamond

### 2.1 Actual contact geometry

Retain the Q-441 endpoint-contact diamond: old and new cycle corridors have
distinct interiors and meet only at the recorded endpoint `q`.  Its raw W1
path/current maps and W4 bundle pullbacks commute exactly.  Both positive-
source legs have rank one and the target has rank two.

The contact locus changes only which RNL antecedents are active.  It does
not change the source/target current forms and therefore cannot discharge
OLD_FID.

### 2.2 Corrected requirement set

For a positive-source contact leg `r`, the exact checks are:

```text
1  OLD_FID:
   Def_fid(r)=0;

2  EXCLUSIVE_REGION_RNL:
   Def_leak(r;O,W)=0 for every O,W in (H1-14);

3  LR:
   Def_supp(r;O)=0 for every actual old local region. (H2-1)
```

Thus “OLD_FID + exclusive-region RNL” are the two requirements omitted by
the Q-441 LR-only statement; LR remains independently load-bearing.  A
contact-containing pair for which `F_r(O) intersect W` is nonempty is not
an RNL obligation, but no such pair removes any exclusive instance.

### 2.3 Re-execution

Write the fixed target Gram form as

```text
[[alpha,zeta],[conj(zeta),delta]].              (H2-2)
```

On the two positive-source legs, the finite defects are

```text
Def_fid,U = alpha-alpha_0,
Def_fid,V = delta-delta_0,

Def_leak,U(O_ex,W_ex) = theta_U zeta,
Def_leak,V(O_ex,W_ex) = theta_V conj(zeta),     (H2-3)
```

for nonzero exclusive-region analysis tests.  The support defects are the
quotient maps `(H1-16)`.  Therefore:

```text
contact certificate terms exist exactly if
  alpha=alpha_0,
  delta=delta_0,
  zeta=0 on every exclusive local block,
  all Def_supp maps vanish,
  and the W4 equations commute.                 (H2-4)
```

Under two coherent certificate terms, H1.8 proves the complete contact
diamond passes.  On the program's actual fixed family, `(H2-4)` remains
unproved.  No LR-only classification remains.

```text
D_CONTACT_REQUIREMENTS = OLD_FID + EXCLUSIVE_REGION_RNL + LR
D_CONTACT_UNDER_CERT = PASS / TYPE-P
D_CONTACT_PHYSICAL_TERM = OPEN / TYPE-U
```

---

## 3. H3 — both cycle-creating diamonds rerun

### 3.1 Disjoint diamond

For the actual disjoint `U,V` diamond, the geometry projectors are `(H1-4)`.
The finite certificate table is:

| Leg | `Def_fid` | `Def_orth` / RNL | `Def_supp` / LR | W4 |
|---|---|---|---|---|
| `G_U->G_UV` | `alpha-alpha_0` | `zeta` | `q_U P_U i_U` | zero defects |
| `G_V->G_UV` | `delta-delta_0` | `conj(zeta)` | `q_V P_V i_V` | zero defects |

Here the quotient notation abbreviates `(H1-16)` with the corresponding
mapped corridor.  The direct-sum model sets every displayed defect to zero;
the ratified physical family does not.

With two coherent certificate terms, the actual path/current, test, and
bundle maps commute by H1.8.  Without those terms, the outer `0=0` base
square still cannot certify either positive-source side.

```text
D_DISJOINT_UNDER_CERT = PASS / TYPE-P
D_DISJOINT_FIXED_PHYSICAL_DATA =
  STOPPED_AT(Def_fid=0, Def_orth=0, Def_supp=0) / TYPE-U
```

### 3.2 Contact diamond

The contact rerun uses the complete table `(H2-1)` and the explicit
exclusive defects `(H2-3)`.  Under coherent certificate terms it passes by
the same composition theorem.  On fixed physical data it stops at
`(H2-4)`.

```text
D_CONTACT_UNDER_CERT = PASS / TYPE-P
D_CONTACT_FIXED_PHYSICAL_DATA =
  STOPPED_AT(OLD_FID,exclusive_RNL,LR_zero_defects) / TYPE-U
```

### 3.3 Rank-preserving control

On the admitted Q-418 subdivision diamond,

```text
S=id,
N_r(W)=0 on every would-be new local sector,
E_r;O,W^geom=E_r;O,W^orth=id on A_r(O),
Def_fid=Def_orth=Def_leak=Def_supp=0,
P=id.                                           (H3-1)
```

Thus the new certificate admits the `P=id` control and does not reintroduce
the Q-432 global-orthogonality overreach.  In particular, it imposes no
condition on off-diagonal pairings between unrelated pre-existing cycles.

### 3.4 Joint result

```text
CERTIFICATE_CLASS = BUILT
MODEL_TERM = BUILT
PHYSICAL_ALLRANK_SECTION = UNBUILT

ACTUAL_DIAMONDS_EXECUTED = 3
UNCONDITIONAL_PHYSICAL_PASSES = 1
CONDITIONAL_PASSES_WITH_CERT_TERMS = 2
```

The exact remaining object is not another certificate definition.  It is
the inhabitance statement

```text
Gamma_cov(Cert_LOE over actual positive-source primitives) != empty,
with common-refinement coherence.               (H3-2)
```

The direct-sum model proves that `(H3-2)` is not structurally contradictory;
it does not prove `(H3-2)` for the retained physical family.

---

## 4. H4 — all-rank cascade and `[EQ6]`

### 4.1 All-rank statement

Define

```text
F_Q408^LOE :=
  the covariant family of actual W1/W4 primitive members equipped with
  coherent LOCAL_ORTHOGONAL_EXCISION_CERT terms. (H4-1)
```

H1 proves that `(H4-1)` is closed under composition and actual common
refinement whenever it is inhabited.  The Q-439 first-cycle family and
Q-418 subdivision family provide its zero-source/rank-preserving boundary.
No positive-source physical term is yet proved.

```text
ALL_RANK_CLOSURE_THEOREM = CONDITIONAL_ON_NONEMPTY(F_Q408^LOE)
ALL_RANK = PARTIAL / TYPE-U
EXACT_STOP = NONEMPTY_COHERENT_PHYSICAL_SECTION_(H3-2)
LAW_REVISION_NEEDED = false
NEW_CLAUSE_NEEDED = false
```

### 4.2 Fiber ledger

| Fiber | Now built | Exact remaining need | Standing |
|---|---|---|---|
| `B_Q408_REFINEMENT` | verified first-cycle family; subdivision orbits; raw positive-source W1/W4 members; `Cert_LOE` class, model term, and composition theorem | nonempty coherent physical section `(H3-2)` on fixed DoR-019/Q-408 data | **PARTIAL / TYPE-U** |
| `B_R1_NATURAL` | pass-1 old-arrow subpackage | action/Hessian/reducing-domain naturality on arrows admitted by `(H3-2)` | **PARTIAL / TYPE-U** |
| `C1` / P4 core | finite/first-orbit kernel core | completed response-natural kernel on the admitted all-rank category | **PARTIAL / TYPE-U** |
| completed faithfulness | pass-1 orbitwise bounds | faithful completion on the admitted all-rank category | **PARTIAL / TYPE-U** |
| `B_C2_RESPONSE_BOUNDARY` | finite/old-arrow boundary cocycle | response-boundary naturality on the admitted all-rank category | **PARTIAL / TYPE-U** |
| `C3` / stationary return | conditional interface | simultaneous equalizer after all prior rows inhabit jointly | **PARTIAL / TYPE-U** |

### 4.3 Cascade

```text
Cert_LOE class + model term
  -> proves joint consistency and gives an exact admission checker
  -> does not itself supply a physical positive-source term
  -> B_Q408 remains partial
  -> B_R1/C1/faithfulness/C2 receive no new admitted physical arrow
  -> C3 and JOINT_EQ6 remain partial.            (H4-2)
```

If `(H3-2)` is later proved, H1.7–H1.8 immediately close the all-rank
composition/diamond part of `B_Q408`; the dependency order then remains:

```text
1  physical inhabitance of F_Q408^LOE;
2  B_R1 naturality on that category;
3  C1 + completed faithfulness + C2 in one topology;
4  C3 and one simultaneous J1-J15 equalizer term. (H4-3)
```

```text
JOINT_EQ6 = PARTIAL / TYPE-U
MEMBER_BOUND = false
FIXED_POINT_LICENSED = false
```

---

## 5. H5 — falsifiers, regressions, and anti-tuning

### 5.1 Nine inherited regressions

| Regression | Execution | Result |
|---|---|---|
| abstract-kernel substitution | all test/support defects use fixed Q-408 `Abar`, `Phi`, and local quotients | **PASS** |
| circular map | actual `S_r,E_r^geom` precede local diagnostic `E_r;O,W^orth` and `P_r` | **PASS** |
| false nonemptiness | certificate class/model term separated from physical section `(H3-2)` | **PASS** |
| cycle-creating restriction | target new cycles stay visible; no target current is quotiented away | **PASS** |
| covariance-orbit overclaim | zero loci are carried as full orbits; covariance creates no term | **PASS** |
| all-stage skeleton overclaim | composition is conditional; all-rank inhabitance remains TYPE-U | **PASS** |
| Q-430 sector mixer | local `E_U;O,W^orth(c_V)!=E_U;O,W^geom(c_V)` when `zeta!=0` | **PASS — REJECTED** |
| Q-432 `P=id` overreach | subdivision has no new local sector, `P=id`, and passes without constraining unrelated old sectors | **PASS — ADMITTED** |
| clause nonemptiness overreach | DoR-020-A1 is used only as a membership law | **PASS** |

### 5.2 Additional hostile attacks

| Attack | Computation | Result |
|---|---|---|
| direct-sum laundering | `(H1-29)` is labeled model term, never physical inhabitant | **REJECTED** |
| nonlocal orthogonal projection | `(H1-7)` is diagnostic; equality with local geometric excision is required only on RNL-relevant pairs | **REJECTED** |
| Gram-Schmidt repair | rotating `S_r` changes the actual current and its support | **REJECTED** |
| metric replacement | no coefficient is set; fixed `R_K` is tested | **REJECTED** |
| contact LR-only shortcut | `(H2-1)` reinstates OLD_FID and every exclusive RNL pair | **REJECTED** |
| RNL-implies-LR shortcut | `Def_supp` remains an independent zero operator | **REJECTED** |
| outer `0=0` laundering | positive-source leg terms remain required | **REJECTED** |
| bundle factorization | current excision never splits or trivializes the U(1) bundle | **REJECTED** |
| componentwise-to-joint leap | coherent physical section `(H3-2)` remains explicit | **REJECTED** |

### 5.3 Fresh attack — orthogonal projector without geometric excision

Every positive finite metric gives the local projector `(H1-7)`.  If that
fact alone counted as local orthogonal excision, every Q-430 mixer would
pass by definition.  But for nonzero `zeta`, `(H1-7)` sends the new physical
current partly into the old analysis sector while `(H1-6)` excises it.  The
equality `(H1-8)` fails on the disjoint pair.  Hence the certificate cannot
be fabricated from Hilbert-space orthogonality alone; it requires agreement
with actual surface support.  No condition is imposed on unrelated
pre-existing sector pairs.

```text
FRESH_ATTACK = PASS /
  ABSTRACT_ORTHOGONAL_PROJECTION_IS_NOT_PHYSICAL_EXCISION
```

### 5.4 Surface geometry versus rails

| Layer | Actual geometric content | Rail/certificate content | Status |
|---|---|---|---|
| W1/W4 primitives | actual paths, conserved currents, support, U(1) bundle and fields | incidence/pullback squares | **BUILT** |
| geometric excision | old/new physical current split from actual new-cell support | idempotent equations | **BUILT ON THE TWO MINIMAL DIAMONDS** |
| metric agreement | fixed A4/Riesz pairing respects the relevant local split and old norm | zero defects `(H1-10),(H1-11)` | **OPEN ON PHYSICAL FAMILY** |
| analysis support | fixed Q-408 `Phi` and inverse preserve local excision | zero defects `(H1-13),(H1-16)` | **OPEN ON PHYSICAL FAMILY** |
| composition/diamonds | nested actual support and bundle pullback | equations `(H1-23)`–`(H1-28)` | **PROVED CONDITIONALLY** |

The remaining stop is not a rail artifact: it asks whether fixed physical
metric/analysis data respect actual surface excision.  It is also not a
new-physics clause request: the where-laws already say which members are
admissible.  It is the witness burden under those laws.

### 5.5 Anti-tuning ledger

```text
1  Verify Q-442 and retain its direct-sum model only as compatibility data.
2  Fix actual W1/W4 surface and bundle candidates.
3  Derive E^geom from the actual path/current split.
4  Derive each local E_r;O,W^orth, P_r, and all defects from fixed
   DoR-019/Q-408 data.
5  Define the certificate as the zero-defect proof record.
6  Prove covariance, units, bundle discipline, composition, and diamonds.
7  Rerun disjoint and contact diamonds without changing any input.
8  Only then state physical inhabitance, all-rank, and EQ6 status.
```

No response, threshold, fixed point, end test, alpha consequence, or
measured constant entered the construction.  No metric, support,
orientation, frame, bundle gauge, rank, path, refinement, or continuum
member was selected for a desired downstream effect.

---

## 6. Final board

```text
EXCISION_CERT = BUILT
EXCISION_CERT_MODEL_TERM = BUILT
EXCISION_CERT_PHYSICAL_FAMILY =
  STOPPED_AT(NONEMPTY_COVARIANT_ZERO_DEFECT_SECTION)

D_SUBDIVISION = PASS / ADMITTED
D_DISJOINT = CONDITIONAL_PASS / PHYSICAL_TERM_OPEN
D_CONTACT = CONDITIONAL_PASS / PHYSICAL_TERM_OPEN
D_CONTACT_REQUIREMENTS = OLD_FID + EXCLUSIVE_REGION_RNL + LR

ALL_RANK = PARTIAL / TYPE-U
JOINT_EQ6 = PARTIAL / TYPE-U
JOINT_UNSATISFIABLE = false / TYPE-R

EXACT_REMAINING_OBJECT =
  Gamma_cov(Cert_LOE over actual positive-source primitives) != empty,
  coherent on every actual common-refinement diamond

LAW_REVISION_NEEDED = false
NEW_CLAUSE_NEEDED = false
MEMBER_BINDING = false
FIXED_POINT_EXECUTION = false
END_TEST = false
NUMERIC_EVALUATION = false
MACHINERY_APPEAL = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
