CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 2645
PREDECLARATION_REGION = EMPTY (closure opens at byte 0)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 8 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2
ROLE_THIS_RELAY = OPPOSITE-LANE VERIFIER
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_951_SDN_CROSSCHECK_CODEX2_V001.md` | `f9dbd62819dbe7443e54b0c968ebcc859a593f669ebb9ec1ad17c90d1fe5b658` | assignment |
| 02 | `STAGE8_AXN_SDN_SLICING_INSTANCE_DARIO_V001.md` | `5b111f8c73ab74d5eb292402a6e0cd713421fe8422282f3f4ce6ced85fdbc8c3` | subject |
| 03 | `LINE6_SITTING_DECISION_OF_RECORD_V001.md` | `00b77a0887bb84ea2a87384b78d52a775c7e882ee99f054dac075c56697018b3` | principal's SD-N entry |
| 04 | `STAGE8_AXN_SLICING_DECISION_INSTRUMENT_CODEX2_V001.md` | `08a13df40f8f08deb2727e5313162c5362c2d03f0a19db0c3855cb7de451bd8b` | governing route schema and price |
| 05 | `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md` | `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9` | sealed parent |
| 06 | member 05: branch `[3519,4287)`; carrier `[4287,5711)`; parent `[5711,6867)`; descend `[6867,7879)` | `1c7e13ae6b9605f152a19f944e014d627ee2675b8ce8d4ced0a3364bd58d411c`; `87e404a0a6d8a6dec443b7bdafb00b2c1ed60d115e781cb1b04443e6d1fa5c78`; `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3`; `827cf361f052d36c62e7fc6ea57e61c04cf8c18fc552dcde18bd7ee5e5ef8e3a` | spin/global-hyperbolic ground, `Sigma`, `S_n`, `h_0`, `h_K`, `D_K`, `C_K` |
| 07 | `STAGE8_AXN_SUPPLEMENTS_DARIO_V001.md` | `2a829a35eb5fb6cf0b8dc1ca8c4c07848684d1958a787cadb070f4dcc0df8ba9` | prior localisation, used only as an attack target and source pointer |
| 08 | `STAGE8_AXN_GALERKIN_CROSSCHECK_DARIO_V001.md`; `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `7dce7e71c21bba61157433bd63de6491aa66543a654665f1b0f4f70e0203b5b6`; `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | sharp-profile result and process fences |

```text
SUBJECT AND BOTH GOVERNING ARTIFACTS WERE HASHED AND THEIR ADJACENT SEALS VERIFIED BEFORE SEMANTIC READ.
THE OPPOSITE-LANE RESULT IS INDEPENDENT: NO CONCLUSION OF THE BUILDER IS USED AS A PREMISE.
NO PHYSICAL QUANTITY IS NUMERICALLY EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN SD-N SLICING INSTANCE — OPPOSITE-LANE CROSS-CHECK — CODEX 2 — V001

Date: 2026-08-10  
Status: **THE NAMING, WRITE ALGEBRA, FORMULA-ABSENCE, TRANSPORT CONSTRUCTION, AND SHARP-PROFILE DISCLOSURE CHECK. THE CONSTITUTION IS FORCED AT THE INVARIANT OPERATOR LEVEL, SO IT NEEDS A SEALED DEFINITION RATHER THAN ANOTHER PRINCIPAL CHOICE. THE INSTANCE DOES NOT BOOK AS WRITTEN: ITS FIELD-8 DOMAIN ASSERTS AN UNJUSTIFIED UNIFORM TUBE AND IDENTIFIES FOCAL-FREENESS WITH INJECTIVITY.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. `Sigma_0`: naming replay

The parent branch `[3519,4287)` seals a globally hyperbolic **spin** spacetime. The carrier bytes
`[4287,5711)` then say:

```text
On a Cauchy surface `Sigma`, let
K_Sigma=L2(Sigma,S tensor L^q).
```

The subject's `Sigma_0 := Sigma` therefore points to the already-declared base of the sealed carrier.
It does not select a member from the class of Cauchy surfaces. The distinction matters: global
hyperbolicity supplies other Cauchy surfaces, but the carrier already names this `Sigma`. The spin
ground also supplies the structure needed for the later spin lift. **Naming, not choosing, is
confirmed.**

## 2. Write half: byte-grounded algebra replay

The descend span `[6867,7879)` gives exactly

```text
D_K = i gamma^mu nabla_mu + i gamma^5 C_K(x),
C_K(x) = sum_c v_c(x) M_c(x) iota_c(c_c).
```

The parent span `[5711,6867)` gives the two record spellings

```text
S_n = -i slash(n) gamma^5,
h_K(t) = h_0[g,a] + sum_c v_c(t) M_c(t) tensor S_n tensor iota_c(c_c).
```

The instrument independently displays `S_n=(-slash(n))(i gamma^5)`. Replaying without importing
the builder's conclusion:

```text
(-slash(n))(i gamma^5 C_K)
 = ((-slash(n))(i gamma^5)) C_K
 = (-i slash(n) gamma^5) C_K
 = S_n C_K
 = sum_c v_c(t) M_c(t) tensor S_n tensor iota_c(c_c).
```

The first reassociation is lawful because the scalar/multiplication/record factors in `C_K` act on
non-spin factors and commute with the Clifford matrices. The spelling equality is literal scalar
algebra:

```text
(-slash(n))(i gamma^5) = -(i) slash(n) gamma^5 = -i slash(n) gamma^5.
```

No sign, factor, receiver, or record insertion is chosen in this replay. The endpoint is the parent's
own write term. **The write half stands exactly.**

## 3. Independent four-mode hunt for `h_0[g,a]`

The hunt ran in both directions: it looked for the exact receiver and also for an unpacking hidden
under different vocabulary. Administrative relay text and the subject were not allowed to prove the
subject.

| Mode | Queries / normalization | Result at the relevant receiver |
|---|---|---|
| M1 exact bytes | `h_0[g,a]`, formula-assignment forms, and every exact occurrence | The parent defines it only by the phrase *"the Dirac Hamiltonian obtained from the same spin-plus-U(1) connection"*. Later exact-name hits restate or quote that phrase and the `h_K` sum. No operator RHS exists. |
| M2 normalized names | `h0`, `h_0`, `H_D`, `Dirac Hamiltonian`, hyphen/underscore and case variants | `H_D(q,A)` is named as a stationary self-adjoint Hamiltonian and used through projectors, not defined by a formula. No normalized hit binds a formula to the parent receiver. |
| M3 split vocabulary | `3+1`, `ADM`, `lapse`, `shift`, `normal/tangential/spatial Dirac`, time-derivative forms together with `gamma`, `Dirac`, `h_0`, or `H_D` | The slicing instrument and audits name the missing mapping. They do not display its RHS. The entered SD-N values occur only in the subject/decision layer, not in a pre-existing unpacking of `h_0[g,a]`. |
| M4 frame vocabulary | `tetrad`, `vierbein`, `coframe`, `spin connection`, `gamma^0 gamma^j`, `slash(n) gamma` | The only same-parent explicit factor is `S_n`, which is the write receiver just replayed. No tetrad/spin-connection formula defines `h_0[g,a]`. |

The broad search produced superficially formula-shaped false positives; receiver typing kills each:

| Candidate | Why it is not the missing formula |
|---|---|
| `COMPLETE_QSPEC_EXACT_SPIN2_SUPPORT_BRIDGE_SPEC_V001.md`: `h_0=-i D_0 tensor alpha_x` | A different exact-spin-2 support-bridge carrier; it has neither the parent receiver's `[g,a]` arguments nor its spin-plus-`U(1)` connection. |
| `STAGE8_H1_CHAIN_VERIFICATION_RETURN_V001.md`: `h_0=sum_j p_j tensor alpha_j` | A massless flat Hermite carrier used for a separate chain check, not the curved/gauged parent operator. |
| public-layer formulas `H_0=D_(Sigma,A)^2+...` | Uppercase, second-order squared/public-layer objects, not the first-order `h_0[g,a]`. |
| stationary `H_D(q,A)` | Named but never formula-defined in its source. |

**The formula-absence finding survives all four modes.**

## 4. Constitution ruling: forced operator, missing sealed binding

The absence of an old formula does not by itself prove that a fresh principal freedom remains. Pressing
the entered data gives the following uniqueness chain:

1. `Sigma_0`, its future unit normal `n`, `N=1`, `beta=0`, and normal proper time fix the normal/tangent
   splitting and its parameterization.
2. The entered signature and `K_ij=(1/2) partial_s h_ij` convention fix the signs and coefficient of
   the extrinsic term; the sealed metric `g` and connection `a` fix all covariant-derivative terms.
3. Orthogonal projection uniquely decomposes `i gamma^mu nabla_mu` into its `nabla_n` term and its
   tangential Dirac part. Left multiplication by the already-forced `-slash(n)` uniquely isolates the
   evolution coefficient under `slash(n)^2=-1`.
4. Parallel transport along each normal geodesic fixes the fiber identification. Changing the
   initially chosen orthonormal/spin frame only conjugates the matrix presentation by the associated
   spin-frame change; it does not produce a different geometric operator.
5. Consequently there is no surviving lapse, shift, sign, connection, normal, or frame freedom with
   which to make a second inequivalent Dirac Hamiltonian while retaining the entered SD-N data and
   the parent's *"obtained from the same connection"* clause.

Thus the invariant differential slice is **forced**. What is missing is not a principal decision but a
sealed receiver definition (and its displayed 3+1 proof) binding the existing name `h_0[g,a]` to that
unique operator. Until those bytes exist, equality with the opaque symbol is not a theorem, and the
subject correctly refuses to manufacture one. The lawful next move is mechanical formalization and
seal of the forced definition; it is not another sitting entry.

This also narrows the subject's §5.3 claim: the write component lands, but the total equality
`Slice(D_K)=h_K` and its lifted equality cannot be booked until that forced definition is sealed.

## 5. Scope press: no-caustic absence confirmed, proposed tube not confirmed

### 5.1 Absence result

The independent exact/normalized search for `focal point`, `conjugate point`, `normal injectivity
radius`, `Gaussian normal`, `caustic`, `normal cut locus`, and neighbourhood spelling variants found
no pre-subject no-caustic theorem. Relevant hits are the instrument stating the burden, its checks,
the subject, and register/phrasing records. Global hyperbolicity does not supply normal-exponential
injectivity. The choice of a bounded rather than global claim is therefore honest.

### 5.2 The field-8 formula overclaims

The subject nevertheless writes

```text
N(Sigma_0)=exp_perp(Sigma_0 x (-s_*,s_*)),
```

with one global `s_*`, calls this the maximal normal neighbourhood, and says diffeomorphism there is
*"i.e. strictly inside the first focal locus"*. Two structural defects remain even with `s_*`
symbolic:

- the sealed ground does not make `Sigma_0` compact and supplies no positive uniform normal
  injectivity radius. A normal tubular neighbourhood may require point-dependent endpoints; a single
  positive-width product `Sigma_0 x (-s_*,s_*)` does not follow;
- absence of focal/conjugate points gives local nonsingularity of the exponential differential, not
  global one-to-one-ness. Distinct normal geodesics can meet at the normal cut locus before a focal
  point. Hence *"diffeomorphism — i.e. before the first focal locus"* is not an equivalence.

The bounded statement that the available ground does support is:

```text
U_normal := the maximal connected open neighbourhood of the zero section in N Sigma_0
            on which exp_perp is one-to-one and a local diffeomorphism;
domain   := exp_perp(U_normal).
```

The fiber intervals may depend on the base point. This definition invokes neither a global width nor
a numerical evaluation. On this domain Gaussian normal coordinates, `N=1`, and `beta=0` hold. Because
the subject does not state this domain and asserts the stronger product/focal equivalence, field 8 is
**not complete as written**.

## 6. Frame and spin-frame transport

On the corrected normal-injectivity domain, every point has exactly one normal geodesic from
`Sigma_0`. The parallel-transport ODE therefore gives one frame value at each point. Metric
compatibility preserves orthonormality; `nabla_n n=0` preserves `e_0=n`; and
`partial_s g(e_i,n)=0` keeps the transported spatial legs tangent to the slices. The spin connection
lifts the same pathwise transport after an initial spin-frame lift. Clifford compatibility makes the
transported gamma matrices covariantly constant.

This construction is well-defined on that domain and fails precisely where normal-path uniqueness
fails. Accordingly **transport is confirmed conditional on the corrected domain**, not as independent
support for the subject's uniform-tube claim.

## 7. Sharp profile and `S_2`

The parent bytes say each spatial support is the corresponding diamond section and that the
opening/closure-face term may be distributional for the sharp causal support. The confirmed Galerkin
cross-check records the sharp-cell `S_2` obstruction and treats smoothing as a new selector. The
subject states `PROFILE=SHARP`, inherits the `S_2` result, and expressly authors no profile,
thickness, scale, or limiting rule. **The disclosure is exact and does not silently smooth the
parent.**

## 8. Result and routing

| Item | Cross-check result | Consequence |
|---|---|---|
| `Sigma_0` | confirmed named | no freedom consumed |
| write half | replayed exact | `SD-04` stands |
| `h_0[g,a]` formula | absent | no old RHS exists against which to prove equality |
| constitution | forced at invariant operator level | seal the forced definition/proof; no new principal entry |
| bounded scope | correction required | uniform product tube and focal/injectivity equivalence do not stand |
| transport | confirmed on corrected domain | no independent transport defect |
| profile | sharp/S2 disclosure confirmed | smoothing remains deferred |

The builder's stated `8/9` tally is therefore not confirmed. The differential half remains stopped
pending the forced name-binding, and field 8 is defective as written. The instance **fails this
cross-check** rather than booking through a silent domain repair.

## 9. FREEDOMS-CONSUMED, flattening, and fences

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the principal's SD-N route entry and Galerkin deferral;
  the sealed Sigma, n, g, a, D_K, C_K, h_K, S_n, and sharp parent profile;
  all process fences.

DERIVED/VERIFIED HERE:
  Sigma_0 is a naming of the carrier's Sigma;
  the exact write identity and equality of the two S_n spellings;
  formula absence under four search modes;
  uniqueness of the invariant differential slice under the entered SD-N data;
  the scope countercheck and the conditional frame-transport result.

AUTHORED OR SUBSTITUTED:
  NOTHING. The corrected normal-injectivity domain is a verifier finding and required repair,
  not silently written into the subject. No h_0 formula, smoothing profile, Galerkin family,
  physical value, member, or anchor was selected.

STOPPED/ROUTED:
  forced h_0[g,a] definition and displayed 3+1 binding -> mechanical formalization/seal;
  field-8 scope -> builder correction to the normal-injectivity domain.
```

- **FLATTENING CHECK:** naming was not flattened into choosing; invariant uniqueness was not flattened
  into an already-sealed formula; focal-freeness was not flattened into injectivity; symbolic `s_*`
  was not flattened into existence of a uniform positive width; conditional transport was not used
  to prove its own domain; sharp inheritance was not flattened into smoothing; carrier landing was
  not flattened into total equality.
- **BLIND:** held. No rank, ratio, fiber, or comparative outcome was consulted.
- **PE-1..PE-14:** pointer-only, zero verdict weight.
- **F_PLDEC:** symbolic differential geometry and operator algebra only; no physical quantity was
  numerically evaluated and no measured constant was consulted.
- **CHAIN:** no member binding, fixed-point execution, end test, proof authorization, anchor act, or
  gauntlet was invoked.

Self verb audit: `replayed` is confined to the displayed write algebra; `forced` is supported by the
five-step uniqueness argument and does not claim the absent formula is already sealed; `fails` is
confined to the subject's scope assertion and resulting instance verdict; `confirmed` transport is
explicitly conditional on the corrected injectivity domain. `VERB_AUDIT_SELF = CLEAN`.

## 10. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 2645; scan 0 hits)
SIGMA0 = CONFIRMED-NAMED
WRITE = REPLAYED-EXACT
H0_FORMULA = CONFIRMED-ABSENT
CONSTITUTION = FORCED (uniqueness shown; the next object is a sealed mechanical definition/binding, not another principal entry)
SCOPE = CORRECTED (the uniform Sigma_0 x (-s_*,s_*) tube and focal-locus equivalence are unsupported; use the normal-injectivity domain with point-dependent fiber intervals allowed)
TRANSPORT = CONFIRMED ON THE CORRECTED DOMAIN
PROFILE = DISCLOSED-CONFIRMED
VERDICT = FAILS (field 8 overclaims its domain; the forced h_0 name-binding also remains unsealed, so the stated 8/9 tally does not book)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
