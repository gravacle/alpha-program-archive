# STAGE 8 — THE 5d RE-READ — INDEPENDENT AUDIT — COMMISSION T13SR

## 5d RE-READ AUDITOR — DEFAULT-REFUTE — 2026-08-15

Role: blind-lane auditor of STAGE8_5D_REREAD_T13SR_V001.md (the re-read).
Protocol executed in order: STEP 1 independent pre-verdict from the docket
WITHOUT reading the re-read; STEP 2 seal verification of the claimed
artifact; STEP 3 byte adjudication, default-refute, every link re-walked,
every consumption rule checked, menu compliance checked, disagreements
adjudicated at bytes.

**PRE-VERDICT FIXED BEFORE FIRST READ:**
sha256(T13SR_AUDITOR_PREVERDICT.md) =
26dc21b42c434aa97ac2637fb3a8958663cdcd5c36d1a9c76ddff86131543ceb
(written to the session scratchpad and hashed BEFORE the first byte of the
re-read was read; content: determination FREE, grounds sketch G1-G6 from
the docket only). The re-read artifact's seal was verified AFTER this hash
was fixed and BEFORE its first read.

Fences held throughout: alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false. No numeric evaluation, no floats, no
measured-constant comparison — symbolic/byte comparison only. No CAS. No
git. Output: THIS artifact + its seal sidecar, both probed ABSENT before
write (exit 1 on ls, logged this session). No register/tracker/road/plan
file read. The registrar-only symmetric-audit instrument: seal verified as
listed, NEVER OPENED.

SWEEP CUTOFF, DECLARED: the 66-file blind docket at its verified bytes +
the re-read build artifact + its seal sidecar + the registrar-only
prereg's seal line (hash only, file unopened) — nothing else consumed,
nothing sealed after 2026-08-15 08:39 CDT (the re-read sidecar's mtime)
sought or read. Adjudication universe closed at that boundary.

---

## §0 — SEAL VERIFICATION (before reliance)

1. THE DOCKET: all 67 commission-listed seals (66 blind-docket files + the
   registrar-only prereg) re-verified THIS session by `shasum -a 256 -c`
   at path, full digest, from a check list built verbatim from the
   commission's pointer/digest lines: **67/67 OK, zero mismatches, none
   unverifiable.** My independent composition count of the blind docket:
   (a) 2 ; (b) 50 ; (c) 6 ; (d) 7 ; (e) 1 = 66 — MATCHES the re-read's
   §0 count and composition exactly.
2. THE CLAIMED ARTIFACT: STAGE8_5D_REREAD_T13SR_V001.md recomputed at
   path = 27cb73268d5ec2cdaf6e05e39fbee960bf30d6e3f36c8e329c465d9189e30f9d
   — MATCHES its .seal.sha256 sidecar byte-for-byte. NOT BLOCKED.

---

## §1 — WHAT WAS RE-WALKED (method)

Read whole before adjudication: the standing read pair (build read in
full; check's verdict/correction spans), the close assembly V002, the
close sealing instrument, the U2 closure, the Act-5 instrument, the
adopted R-L4a-N clause (header + N.4 in full), the summed-S2' entry
addendum, and the re-read itself in full. Read at flag-block/verdict-span
grade with targeted byte pulls: the wall summed re-quantification, the
log-free rate (T4SR), the degree-3 census (T6SR), the degree-3 display
pair (T8SR), MO-4, the earned trace, the T7 amplitude adjudication, the
amplitude junction, the RA27 response legs, the r-2 determination + its
audit, R3, MO-2 + audit, MO-3, MO-4 audit, MO-4-R, clock ceiling, sign
crossing, overlap law + audit, half-plane, C-L2/G_cm, OBL-D transport,
form-to-HS bridge, refuting branch, both R_K/L_T bits, both scoring
passes + re-score audit. Every §3 sweep row the audit cites below was
checked against the named artifact's own verdict bytes. Targeted greps
(scope: the 66 docket files) for: "K_dd", "domain extension",
"comparand", lens tokens in the re-read's grounds, forced-undecidability
language, tallies.

---

## §2 — PER-CLAIM ADJUDICATION (default-refute; verdicts per claim)

C-1 §0 seal claim (66/66 OK, composition) — **CONFIRMED** (my §0.1).
C-2 §1 standing-read summary (NO-UNCONDITIONAL verdict, V1-V5 map,
    branch statuses, comparand rider) — **CONFIRMED** at f3165db1's §4-§6
    and flag block, reproduced faithfully; check verdict READ_VERDICT =
    SOUND with ONE non-converting U-D8 correction — **CONFIRMED** at
    f13974b2 :201-215, :269-284.
C-3 NP-1 (Attach TYPE-P carried, never discharged) — **CONFIRMED** as to
    status and use (D5 content correct; earned-trace ae2202e0 flag:
    magnitude routes NOT-DERIVABLE-TODAY, values vacancy-barred; nothing
    in the re-read treats the premise as discharged). CORRECTION noted
    (n-1, §5): the quoted rider "TYPE-P on DoR-008/009/013/014 + A0 +
    the entry's three open conditions; voids on the neutrality/
    restriction falsifier" sits at the standing read's D6 (mu's chain)
    row, not its D5 (Attach) row; at the docket bytes the referent of
    "the entry" is unexpanded, so the re-read's folding of the rider
    under the Attach heading is one available reading — duplicative-
    conservative (adds riders to NP-1, drops none; NP-4 carries the mu
    rider correctly and separately). Non-converting.
C-4 NP-2 (K_dd admission + domain extension consumed at ENTERED
    strength, FV envelope LIVE) — **VOID(named-premise reproduction) AS
    DISPLAYED, correction applied; non-converting.** The finding, exact:
    the admission instrument is CITED but NOT DISPLAYED — no digest, no
    quoted bytes, no docket pointer — and it is NOT LOCATABLE within the
    66-file docket: my grep sweep finds "K_dd" and "domain extension"
    inside the docket ONLY in the standing read pair, where K_dd is
    "candidate-not-of-record" and the extension is a NEEDED supply
    (D2), not an entered one. The re-read asserts entered status of
    record ("stands at ENTERED strength", FV "LIVE") on an instrument
    below/outside its displayed reach, WITHOUT the reach-limit display
    it correctly gave at NP-1. Under the commission's reproduction rule
    (any cited-but-undisplayed premise VOIDS), NP-2's entered-status
    display is VOID at the docket bytes. ADJUDICATED CONSEQUENCE,
    applied and checked: K_dd + the extension revert to the standing
    read's own byte grade (candidate-not-of-record; supply absent).
    EFFECT ON THE VERDICT: NONE — strictly conservative. Every use of
    NP-2 (§4.1 D1/D2 "ENTERED is not DERIVED, non-converting"; §4.3
    "a named door, not a forcing"; CH-2) argued non-conversion FROM the
    stronger status; at candidate grade the same conclusions hold a
    fortiori, and D1/D2 stand FARTHER from FORCED. The undisplayed
    premise smuggled no force toward the verdict; it granted unearned
    strength only to the branch that lost. If the ratified 5d
    instrument (3df78596 — not in this docket, not readable by this
    lane) itself names the admission as a consumption, the display
    defect reduces to a missing reach-limit sentence; the void of the
    DISPLAY stands either way; the verdict is untouched either way.
C-5 NP-3 (R-L4a-N inside N.4) — **CONFIRMED**: the N.4 quotation is
    verbatim against 72d5343e with both elisions MARKED by ellipses at
    exactly the two elided spans; witness-standing logic checked: the
    clause retires the three R-L4 witnesses only when r-2 AND r-3 are
    sealed and checked; r-2 landed (B1-DERIVED; audit CONFIRMED-WITH-
    CORRECTIONS, corrections cosmetic, none verdict-chain), r-3 open
    (R3 flag: NOT-DERIVABLE-TODAY) — witnesses STAND at fire time. ✔
C-6 NP-4 (mu chain symbolic-only, authored, rank pair NO_VERDICT, value
    selection barred) — **CONFIRMED** at the standing read's D6/flag;
    nowhere consumed above symbolic strength in the re-read (checked
    every mu mention).
C-7 NP-5 (summed-S2' adopted; alpha' a symbol; two witnesses live;
    "decides a QUANTIFIER/TEST, not a verdict, a value, or a flag") —
    **CONFIRMED** verbatim at cbfbb74c including both witness names and
    the SCAD_COLOR_SUM_LOG_MISUSE non-retirement.
C-8 NP-6 (U2 closure, self-kill, "confirms nothing") — **CONFIRMED**
    verbatim at 46aee087.
C-9 NP-7 (close landed, tally 2/0/12; "no new require reached the
    magnitude; the record did not supply the coupling content"; ALLOW-
    SIDE BY THEOREM; provenance class UNINHABITED; Act-5 AUTHOR quotes)
    — **CONFIRMED** verbatim at d287f177 / 8c9e7d47 §2-§4 / 96bd68e5.
C-10 §3 sweep table (33 rows) — **CONFIRMED-WITH-CORRECTIONS**: every
    spot-checked row matches its artifact's own verdict bytes — R1/
    clause; r-2 B1-DERIVED (kappa_n -> +infinity, full-family); R3
    NOT-DERIVABLE-TODAY; MO-2 + audit CONFIRMED-WITH-CORRECTIONS; MO-3
    P_EXPONENT = DERIVED(p = -3 EXACTLY); MO-4 PARTIAL + audit "FORCED:
    no product-level cancellation"; MO-4-R "UNDECIDABLE-TODAY —
    undecidability FORCED at the displayed-hypothesis grade"; clock
    ceiling UNDECIDABLE-TODAY FORCED (C12 pair) with alpha-k in
    [1, 3/2] of record; sign crossing UNDECIDABLE-TODAY one level
    deeper (polar resolved, two models identical spectrum); overlap law
    UNDECIDABLE-TODAY at O-1-S-B + audit CONFIRMED; wall boundary
    FAILS-AT F1/F2/F3 with op-grade enclosure ||.||_op <= 2 delivered;
    summed re-quant flags TRHA/HSHA = 0 exact, HSKH enclosure with
    "B-L2* discharged" in its own bytes, TRKH FAILS-AT log; T4SR
    UNDECIDABLE-TODAY reduced to the census; T6SR CENSUS-INCOMPLETE
    with branch table armed; T8SR COMPLETE-LOG-GENUINE, rate REFUTED;
    half-plane NOT-DERIVABLE-TODAY; C-L2 G_cm NOT-DERIVABLE-TODAY at
    consuming type; OBL-D VERDICT = BUILT (r_F^* package); S2' trio
    ADOPTED; RA27 PARTIAL (eta_resp + naming not sealed-available);
    bridge SPLIT THREE WAYS NONE FULL, R.3 FAILS-AT(compression type);
    refuting branch OB1_RL2B = REFUTED-WITH-IT both grades; R_K/L_T
    UNDECIDABLE-TODAY twice; earned trace PARTIAL, vacancy-confirmed;
    T7 BLOCKED, flag = false STANDS; junction VACANCY-CLOSED by derived
    partition; scoring 0/0/15; re-score 2/0/12 zero disagreements, R2/
    R4 on displayed authored provenance. CORRECTIONS n-2, n-3 (§5).
C-11 §3 lost-discharge pre-empt — **CONFIRMED-WITH-CORRECTION n-3**:
    the governing sentence ("no sealed discharge of any D1-D9 link at
    its own required grade exists in this docket") is TRUE at the
    bytes; the subsidiary enumeration ("the only DISCHARGED items are
    B-L2*-summed and the S2' entry") misses the OBL-D consequence
    line's "SEALED-KINEMATIC HALF DISCHARGED" token — a clause-half,
    not a D-link at required grade, displayed anyway in the same
    table's own row as BUILT/partial. Non-converting.
C-12 §4.1 FORCED nine-for-nine — **CONFIRMED**, each link re-walked
    against the §3-verified bytes (D1-D2 with C-4's correction: farther
    from forced; D3 no docket artifact writes the typing rule — sweep
    concurs; D4 no license appears; D5 per C-3; D6 per C-6 + "no value
    exists" at 96bd68e5; D7 witnesses live + r-3 open + G_cm blocked +
    no cluster-kernel artifact exists in the docket; D8 stopped, U-D8
    correction non-converting per C-2; D9 RA27 PARTIAL at bytes).
    Comparand-rider bar carried. No expectation language present
    (scanned; the section states displayed state only).
C-13 §4.2 RUNS — **CONFIRMED**: no docket artifact creates a sealed
    running target (sweep + my grep concur); the S2' entry is a
    quantifier/test with alpha' a symbol; the close carries the genuine
    log "solely as a decided register fact"; the kernel is nowhere
    typed non-intensive of record (the log is a decided fact about ONE
    summed wall trace at census grade — its coupling bearing is the
    close's lens-fenced reading, kept out of grounds). Anchors named.
C-14 §4.3 + §6 FREE grounds G-1..G-5 — **CONFIRMED** at the bytes:
    V1's vanishing and its exact reopening condition (dim H > 0,
    supplied by nothing on the sweep — checked); V2's mirror collapse
    under carried TYPE-P; the third object per C-4 (correction applied,
    conclusion unchanged and strengthened); the junction VACANCY-CLOSED
    NET reproduced faithfully (15148ab5); ALLOW-SIDE BY THEOREM +
    uninhabited provenance class (8c9e7d47 §3); the close's checked §4
    display; Act-5 AUTHOR with content PENDING, "no value exists". The
    verdict's strength boundary is displayed (fire-time verdict, NOT
    the standing read's F-closure — F-i/F-ii/F-iii open of record) and
    the conversion map is named all three ways. **The verdict FREE
    follows from the displayed bytes.**
C-15 §5.1 comparand rider — **CONFIRMED**; the grep-scoped claim
    ("comparand" only in the standing read pair within the docket)
    INDEPENDENTLY REPRODUCED by this audit's own grep over the 66.
C-16 §5.2 founding-question engagement — **CONFIRMED** (boundary-
    closure F1/F2/F3 with the diagonal locus per 80db260f; summed
    dispositions per C-10; faithfulness-never-authority per the
    standing read's method fence and the builds' FORBIDDEN_IMPORTS
    blocks; nothing silently closed).
C-17 §7 choice ledger / toy separation / lens discipline —
    **CONFIRMED**: CH-1 elimination structure matches §4 as verified;
    CH-2 per C-4 (the classification FORCED survives the correction —
    entered-or-less never converts); lens tokens appear ONLY at G-5's
    zero-assertion, the §7 discipline block (named to display
    exclusion), and the flag — ZERO lens citations in grounds, grep-
    verified.
C-18 "five FORCED-undecidable at displayed-hypothesis grade" —
    **CONFIRMED as consistent**: five sweep rows carry forced/
    certificate-grade undecidability in their own artifacts' bytes
    (MO-4-R; clock ceiling C12 pair; sign crossing two-model deeper;
    R_K/L_T S9AD positive two-model certificate; R_K/L_T T1SR
    certificate-carried menu) — the count is supportable; the five are
    not named in the re-read (presentation nit, subsumed in n-2).

---

## §3 — CONSUMPTION-RULE CHECKLIST (each rule, checked at bytes)

R-1 TYPE-P not discharged — **HELD** (C-3; earned-trace consumed at
    PARTIAL/vacancy; no discharge language anywhere in the re-read).
R-2 ENTERED never DERIVED — **HELD IN EVERY CONSUMPTION** (S2' entry,
    U2 seal, ratified premise via re-score row "authored provenance
    displayed", N-clause adoption, Act-5 word); the one defect is
    NP-2's PROVENANCE DISPLAY (C-4), where the rule's direction was
    nonetheless obeyed (never converted to derived).
R-3 Rank-blindness — **HELD** ((r_0, r_ch) displayed as NO_VERDICT and
    untouched; no rank datum consumed; "rank-one" appearances are the
    sealed FORM's channel language, not the rank pair).
R-4 N.4 boundaries — **HELD** (C-5; nothing rests on the naming beyond
    N.4; kappa_n content taken only from r-2's own derivation).
R-5 Named-premise reproduction — **ONE VIOLATION, NP-2 (C-4), VOID as
    displayed, correction applied, non-converting.** NP-1/NP-3..NP-7
    reproduced faithfully (NP-1 with attribution correction n-1).
R-6 Comparand rider — **HELD AND ENGAGED** (C-15; §5.1 addressed |n|
    directly; the verdict rests on no channel-forcing).
R-7 Founding-question engagement — **HELD** (C-16).
R-8 Zero lens citations in grounds — **HELD** (C-17).

## §4 — VERDICT MENU COMPLIANCE

Verdict = FREE. THE SWEEP DISPLAYED — **COMPLIANT**: the §3 full
established-work sweep (every docket determination, own verdict, own
quantifier), the §4.3 carrier sweep (V1/V2/third object, each at its
displayed quantifier, reopening condition checked against the sweep),
and the §4.2 running-target sweep. FORCED riders n/a (verdict not
FORCED); §4.1 nonetheless uses no expectation language. RUNS riders n/a;
anchors named anyway. NO-VERDICT-YET excluded per the ratified
instrument's menu as carried by the commission — consistent with this
audit's own commission; not independently readable at docket bytes
(instrument 3df78596 outside the docket), noted, not chased.

---

## §5 — FINDINGS LEDGER (complete)

F-1 (MATERIAL-BUT-NON-CONVERTING; the one rule violation): NP-2 cited-
  but-undisplayed admission — VOID as displayed; correction applied
  (candidate-grade consumption); verdict unaffected in direction and
  STRENGTHENED in margin. Detail at C-4. Repair owed by the record, not
  by this audit: either display the admission instrument (digest +
  entry terms) in any successor re-read, or carry the object at
  candidate grade with the reach-limit sentence NP-1 already models.
n-1 (correction): NP-1 folds the standing read's D6 rider under the
  Attach heading; ambiguous referent at the bytes; duplicative-
  conservative. Detail at C-3.
n-2 (correction): §3 r-2 row compresses the audit verdict to "audit
  CONFIRMED"; the audit's own line is CONFIRMED-WITH-CORRECTIONS
  (cosmetic, none verdict-chain). Same-class compressions: "THREE OF
  FOUR DECIDED" (the third is a DERIVED ENCLOSURE — a bound, not a
  value; the row's own text displays the distinction); the five
  forced-undecidable rows unnamed (C-18).
n-3 (correction): the lost-discharge enumeration misses OBL-D's
  "SEALED-KINEMATIC HALF DISCHARGED" clause-half token; the governing
  no-D-link-discharged sentence is TRUE. Detail at C-11.
Nothing else survived default-refute hunting: every quotation checked
landed verbatim or verbatim-with-marked-elision; every verdict line
matched its artifact; both grep-scoped sweeps reproduced.

## §6 — PRE-VERDICT COMPARISON (hash above; adjudication of differences)

My pre-fixed determination: **FREE**, grounds G1-G6 (carriers vanish;
vacancy theorem + exercised authored permission; FORCED debts standing
with comparand rider; RUNS target absent with lens fence; modality
fire-time-not-closure). The re-read: **FREE**, grounds materially
identical, same modality, same conversion map. **ZERO verdict-level
disagreements; zero grounds-level disagreements.** One framing
difference adjudicated at bytes: my pre-verdict held K_dd at
candidate-not-of-record (the docket bytes); the re-read consumed an
entered admission (NP-2). The bytes side with the pre-verdict;
adjudication at C-4; outcome identical either way — the difference is
void-of-display, not of substance.

## §7 — VERDICT OF THIS AUDIT

**OVERALL: CONFIRMED-WITH-CORRECTIONS.** The re-read's verdict of
record — **FREE at the 5d fire-time state** — is CONFIRMED at the
docket bytes, independently pre-derived under the fixed-hash protocol,
and survives default-refute adjudication of every forcing, freedom, and
flow link. One consumption-rule violation found and named (F-1, NP-2,
named-premise reproduction), VOID as displayed, corrected in place,
strictly non-converting — it weakens no ground and moves no branch.
Three minor corrections (n-1, n-2, n-3), all non-converting. No claim
REFUTED. The verdict, its grounds G-1..G-5, its strength boundary, and
its conversion map stand as written. ALL_RESULTS = CLAIMED until the
ratified symmetric audit (the registrar's instrument, unopened here)
runs; this lane routes nothing.

---

## FLAG BLOCK

```text
COMMISSION = T13SR-AUDIT (5d re-read auditor; default-refute; pre-fixed
  independent determination protocol executed in order).
PRE_VERDICT_HASH = 26dc21b42c434aa97ac2637fb3a8958663cdcd5c36d1a9c76ddff86131543ceb
  (FIXED BEFORE FIRST READ of the re-read; content: FREE + grounds).
SEALS = 67/67 OK at path this session (66 blind-docket + the registrar-
  only prereg hash line, file unopened); the audited artifact
  27cb7326... MATCHES its sidecar; composition recount 2/50/6/7/1 = 66
  matches the re-read's own §0.
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the re-read's VERDICT_OF_
  RECORD = FREE stands at the docket bytes; per-claim: C-1..C-18 —
  fifteen CONFIRMED, C-10/C-11 CONFIRMED-WITH-CORRECTIONS, C-4 = NP-2
  VOID(named-premise reproduction) as displayed with correction applied
  and ZERO effect on the verdict (conservative direction: the voided
  entered-status only overcredited the losing FORCED branch; at the
  bytes' own candidate grade every conclusion holds a fortiori). )
RULE_CHECKS = TYPE-P-not-discharged HELD ; ENTERED-never-DERIVED HELD
  in every consumption (one provenance-display defect = F-1) ;
  rank-blindness HELD ; N.4 HELD (verbatim, elisions marked) ;
  named-premise reproduction ONE VIOLATION (F-1, non-converting) ;
  comparand rider HELD+ENGAGED (grep independently reproduced) ;
  founding-question ENGAGED ; lens citations in grounds ZERO
  (grep-verified).
MENU_COMPLIANCE = FREE-with-sweep-displayed COMPLIANT ; no expectation
  language in the FORCED section ; RUNS anchors named though unowed.
FINDINGS = F-1 (VOID-as-displayed, corrected, non-converting) + n-1 +
  n-2 + n-3 (corrections, non-converting). NOTHING REFUTED.
PRE_VS_POST = verdicts IDENTICAL (FREE/FREE); zero disagreements at
  verdict or grounds level; the one framing difference (NP-2)
  adjudicated at bytes in the pre-verdict's favor, outcome unchanged.
SWEEP_CUTOFF = DECLARED( the 66-file docket + the re-read pair +
  prereg hash line; nothing later sought; cutoff at the re-read
  sidecar's 2026-08-15 08:39 CDT state ).
FORBIDDEN_IMPORTS = none( no numeric evaluation, no floats, no
  measured-constant comparison, symbolic/byte only; no CAS; no scale/
  GR/faithfulness authority; no lens ground; no git; outputs probed
  ABSENT before write ).
MACHINERY_INVOKED = no( shasum -a 256 -c at path, file reads, greps,
  incremental writes only ).
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false — ALL REMAIN; no flag flipped, no witness
retired, no gate moved by this artifact; witness/consumption actions
are the registrar's alone.
ALL_RESULTS = CLAIMED until the ratified symmetric audit; the
registrar routes; this lane routes nothing.
T13SR_AUDIT_RESULT = SEALED.
```
