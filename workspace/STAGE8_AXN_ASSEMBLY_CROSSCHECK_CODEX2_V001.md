# STAGE 8 / [PLAN:AXN-BUILD-B2] — FIRST-ASSEMBLY CROSS-CHECK
## CODEX 2 LANE — V001

Relay 874. The relay sidecar and CODEX 2 lane guard matched; pickup ACK was
written before task work. `PROGRAM_STATE_BRIEF_V005.md` was seal-verified and
read first. Subject:
`STAGE8_AXN_ASSEMBLY_FINITE_DARIO_V001.md` =
`5e9d4303c6223fb4c0547a6843512518731b3009cfa5801f8069e1e98f6fb585`,
seal verified before reading.

GATES: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No member was bound; no fixed point, end
test, smooth import, electromagnetic identification, common-cell formation,
junction evaluation, physical numerical evaluation, measured-constant
comparison, register write, plan write, tracker write, git action, or chain
invocation occurred. PE-1 through PE-13 remained pointer-only and unopened.

**CLAIM STATUS: all headline items and verdicts below are CLAIMED.** Finite
matrix norms are structural diagnostics only.

---

## 0. Lead

**The three composition refusals held.** The two-layer display does not write
a generic U1 joint; the missing pairing stops before an integral; and no
gravity term is inserted.

**The provenance ledger needs a scope correction but not a wholesale
rewrite.** T2/T3 remain antecedents, T4/T5 are derived projector images, T6
is derived only on the packet-parent branch, and T7 remains a target with its
non-identity. The action spine has derived indexing/support **schema**, not a
derived `L_c` inhabitant or evaluable summand. Also, the cited 821 endpoint
order-independence is a pure-charge operator result and cannot be pulled back
through the explicitly missing U1 joint; formal sum order-independence is
ordinary addition and needs no such citation.

**`NF-3 = CONDITIONAL`, not forced of record.** The stability premise is not
sealed. Fixed parity projectors give a census for arbitrary perturbations;
they do not require the perturbation itself to be odd. Oddness follows only
after adding both of these presently absent premises:

```text
U1(delta L_c) lands solely in delta W_c, and
the admissible path is required to stay tangent to the adopted record-odd branch
with h held fixed.
```

The earlier direction artifact expressly declined exactly this pullback
because U1 does not exist. Consequently 849's count remains **two independent
forced direction conditions** (`D1`, `D3`); a third parity condition may be
added only conditionally after the missing carrier/path and U1 are supplied.

**NG-2 is decided at the packet/global branch.** The global descent writes
one coefficient-free cell sum and the parent specification states “No new
cell coefficient is admitted.” Independent per-cell `lambda_spin,c` is
therefore excluded there. A single shared residual scalar may remain at the
local generic-branch level; its value is not filled here.

The twelve-lack list mixes roots with their leaves, omits the SC4 carrier
frontier and SC5 aggregate, and contains two items that no longer stand
(mixing-action covariance and per-cell/global scalar freedom). A closed
root-level fifteen-item successor is displayed in §6.

---

## 1. Closure-first declarations and sealed spans

### 1.1 NF-3 closure

Before testing the claimed absence or forcing, define:

```text
C_NF3 := unique union of
  870 demand typing; 871 uniformity theorem; 849 direction relation;
  867 branch-hypothesis forcing; calculus V2; Exit-B;
  the two local grading sources; and
  all fifteen authorities pinned by the R3_4 parent specification.

|C_NF3| = 21 unique sealed files after overlap removal.
```

All sidecars and the parent specification's 15 authority hashes matched.
M-2 covered exact, spacing/case, hyphen/underscore, and
scope/self-reference/semantic-receiver modes. The search families included
`variation/perturbation/tangent/path` crossed with
`grading/parity/odd/even/census/projector/block/stable/preserve`, plus
`delta L`, `delta W`, and `record odd` variants.

The only affirmative receiver rule is the opposite of NF-3's claimed route:
849 states that packet operator structure and receiver constraints may not
be pulled back to directions without U1.

### 1.2 NG-2 closure

```text
C_NG2 := the R3_4 parent specification plus its fifteen pinned authorities.
|C_NG2| = 16 unique sealed files.
```

This closure contains the local grading/zero-form classification, global
boundary descent, unique controlled coupling, and complete parent. The
decision rests on affirmative bytes, not an absence.

### 1.3 Frontier closure

The lack check uses the sealed current frontier and every correction that
moves one of its leaves:

| source | SHA-256 | role |
|---|---|---|
| `STAGE8_AXN_BUILD_ROUND2_FRONTIER_MAP_CODEX2_V001.md` | `5e163d85c49361d315dd8de1d61fc9b50629eb5aee4305fb5465808082331df0` | closed SC4/SC5 root convention |
| `STAGE8_AXN_BUILD_BOUNDARY_DATA_DARIO_V001.md` | `887b0107021b6edd365fe187d04ca833adb63a1c0d6993811805333500e85b63` | boundary generator/correspondence/global-to-cell split |
| `STAGE8_AXN_BUILD_U3_FOUR_FIELD_MATERIAL_DARIO_V001.md` | `d3668b908777ea76f561ea839217e432b9cff88ae231c717ead5ffa1cb6df02a` | S1–S4 leaf census |
| `STAGE8_AXN_BUILD_FOUR_FIELD_CROSSCHECK_CODEX2_V001.md` | `c9e5b5d53d3512a2cddeaa2305b7a4823d801c4c0d276f6c05e3c81f61f04211` | S4 partial correction |
| `STAGE8_AXN_BUILD_S4_FIT_ATTACK_CODEX2_V001.md` | `c1562a6ef1d97cd9354fc7731e0a6731df57b1a1f8ed2aa4e864a795f10ad723` | free-tail partial result |
| `STAGE8_AXN_BUILD_S4_FIT_ATTACK_CROSSCHECK_DARIO_V001.md` | `5c3ef817b643930b9c4ef033b9c720df4796629fa286174a524ce3610975af91` | free-tail confirmation |
| `STAGE8_7A_ORIENTATION_UNITARY_CODEX2_V001.md` | `831c0dd826c40a4e156e5d6104ca87cd08116ba2d30af1f7b1d36f31c2c72ed2` | admitted-action orientation carrier |

### 1.4 Load-bearing spans

| content | source | span | span SHA-256 |
|---|---|---:|---|
| operator constraints declined as directions | 849 = `a94e9ae1ec80a3a507811df8b2e55fd100e3d43f5c8da5124aa2f040654a7e49` | `[2284,3626)` | `cc2d2c4e669e8fa980eac42da2a899e52b93257ea5b2e861c614dd7a7d841141` |
| D1/D2/D3 and missing ambient carrier | same | `[3626,7059)` | `0a4dda18c7b3c49037432d63f22814a79a9557d96c3ea0eebc3f62a05e1f6209` |
| seven adopted branch hypotheses | 867 = `4465cf71e04656f9d45e9aad02908bd6de1762898f8942c61e9d7d08129ba25e` | `[4171,7263)` | `32099ec566554ace18d20308288e6170b3b01deec8cdc5511a72d62e4f3ed4e8` |
| finite parity projectors | 861 = `fb1c127336bd63889c010369a179a44fe0aef01ae916244eedddc1ad937f7524` | `[6813,10630)` | `dff7821218f84debb2ed6e4819e10a40669ea7bd81f6e3a72e4d55f8b09c5dc5` |
| `chi_P=0` and its packet scope | round-1 cross-check = `886cd9a36b66f3581413790ac617be722e3bfb7922ac40342493371c552ae53e` | `[16521,17723)` | `6fad2419d4fcd65af0b6016ff6bf5f0c45cc84e31a6a6f08602127d26ad5e5cf` |
| parent cell coefficient rule | parent spec = `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9` | `[4287,4905)` | `a4953cb66daa6dacf588351447c24defea88068681cbd586e7dd8452a1207f6a` |
| global coefficient-free cell sum | global descent = `949181d78aca143ebef06eacc4ab1018d43cdf79962ee8c78c350f654a7555dd` | `[1705,4006)` | `acb6dc65873a47529d54b6e2b592872e6345c601ca49d4d6e2cfe26dd0cac0a1` |
| SC4 five open roots plus conditional supplier | frontier map | `[2565,4594)` | `88473aced62f5a150a2c667bef0c827a3f5563ce7fb32fdadba9419c30ddc408` |
| SC5 five open roots | frontier map | `[4594,6405)` | `73cc6d7a94ebcb5a9b5ae9b52af46e37934c089824e45634a2046e0e6bd8cb62` |
| boundary non-identity and correspondence | boundary data | `[4212,5824)` | `d8172a0bc2abfbaa482f1179062b3616b76a28c307f044709b493856ea090cc9` |
| S1–S4 exact requirements | four-field census | `[1700,3949)` | `93809d037588b43ae081ce8cbdd8533ba2895143058a0bff6d061952de9b2810` |

---

## 2. Task A: composition discipline

### 2.1 Three refusals

| refusal | finding |
|---|---|
| no formula spans Layer A to Layer B | **HELD.** The displayed U1 arrow is marked required/absent; no equality identifies `L_c` with `D_c` or `W_c`. |
| no integral sign before S1 | **HELD.** The pairing is a named gap and the action display uses quotation marks around `=`. No measure, base family, or descended functional is installed. |
| no gravity term | **HELD.** The assembly carries 857's `NEITHER-FORCED` typing and does not insert `S_grav`, a metric Euler term, or a measure. |

### 2.2 Seven-slot provenance

| slot | 873 tag | cross-check |
|---|---|---|
| T1 action-spine schema | DERIVED | **CORRECTED IN SCOPE.** Exit-B `[3039,3812)` genuinely derives one index per primitive incidence and `support(L_c) subset Omega_c`, at restricted/non-exhaustive scope. It does not derive an `L_c` inhabitant, cell pairing, or evaluable action summand. Count T1 as `DERIVED-SCHEMA / EVALUATION-GAPPED`, not an installed derived term. |
| T2 free/kinetic | ANTECEDENT | **CONFIRMED.** H1/H5/H6 are choices/disclosures. No derivation leak. |
| T3 write/incidence | ANTECEDENT | **CONFIRMED.** H3/H4 are adopted branch premises and H7 derives the quadrature only inside their selected line. The scalar remains symbolic. |
| T4 even projector image | DERIVED | **CONFIRMED.** The sealed grading gives `P_even(D^2)=h^2+W^2`; no finer even splitter is supplied. |
| T5 odd projector image | DERIVED | **CONFIRMED.** `P_odd(D^2)=hW+Wh`. |
| T6 independent Pauli coefficient | DERIVED-AT-SCOPE | **CONFIRMED WITH SCOPE TRAVEL.** Exact vertex comparison gives `chi_P=0` only on the sealed packet-parent branch. Generated square-level `sigma.F` and the absence of a global Pauli-exclusion theorem remain distinct. |
| T7 boundary form | TARGET | **CONFIRMED.** It belongs to the complete Boundary-Resolved generator, not B0; the B0 correspondence is false/TYPE-U, and no global-to-cell descent is supplied. |

One additional provenance correction: 821's exact commutator and endpoint
order result is a pure-charge operator-side result. It cannot derive
Layer-A order independence without U1. The formal sum itself is independent
of enumeration by commutativity/associativity of addition; dynamic endpoint
independence remains at 821's pure-charge scope.

The `4 DERIVED / 2 ANTECEDENT / 1 TARGET` tally is therefore acceptable only
under 873's convention that T1 denotes a derived **schema**. It is not seven
inhabited terms.

---

## 3. Task B: attack NF-3

### 3.1 No sealed stability requirement

The complete `C_NF3` probe found no sentence requiring the parity-block
identification to remain fixed under every admissible perturbation. More
decisively, 849 says:

```text
The packet's operator structure and receiver constraints are receiver-side
tests. Pulling them back to directions requires U1, which does not exist.
```

NF-3 performs that barred pullback: it takes the parity of the operator-side
write block and assigns it to the action-side `delta L_c` while the assembly
simultaneously declares the map between those layers absent.

H3's adopted Boundary Superconnection Principle does select record-odd
primitive operator terms. That supports the conditional statement
“a path required to remain inside this branch must preserve its odd operator
slot.” It does not type an action density or furnish the tangent map.

### 3.2 The census survives mixing without NF-3

For a general perturbation of the typed operator write

```text
delta D = A + B,
G A G = +A,
G B G = -B.
```

With `D=h+W`, the first variation splits automatically:

```text
P_even(delta(D^2)) = {h,A}+{W,B},
P_odd (delta(D^2)) = {h,B}+{W,A}.
```

Thus the projector census remains well defined for an even, odd, or mixed
perturbation. What changes is which pieces contribute to each block. The
special formulas in 873 (`delta h=0`, `delta W=delta L`) follow only after
assuming the missing U1/tangent rule.

An anchor-side structural witness makes the independence visible. On the
same first-cell mask and the same own-new-record factor, take

```text
E = diag(1,-1,0)       (record even),
c = c_partial          (record odd),
X_mix = mask_0 tensor I_spin tensor ((E+c) tensor I_3).
```

The source mask is the sealed finite-cell support carrier and the operator
acts on no earlier record factor, so it has the D1/D3 support/target shape.
The exact finite diagnostics are:

```text
||[G,X_even]|| = 0,
||{G,X_odd}||  = 0,
||[G,X_mix]||  = 19.595917942265,
||{G,X_mix}||  = 13.856406460551.
```

It is genuinely parity-mixing. The even part is one of the explicitly live
outside-branch even-detuning competitors, so this witness refutes derivation
of oddness from D1, D3, or the census. Because `A_c` and U1 are absent, it
may not be promoted to an inhabited `delta L_c` direction inside the adopted
branch. That is why the correct verdict is **CONDITIONAL**, not an
unqualified refutation of a branch-tangent statement.

### 3.3 Count consequence

The count of forced directions remains:

```text
D1 support preservation;
D3 own-new-record-factor targeting;
D2 redundant under the fixed-incidence path premise.
```

Parity becomes a third condition only after a future carrier/path and U1
declare that admissible action variations map solely into the record-odd
operator slot. It is not a third condition of record now.

### 3.4 NF-1 and NF-2

**NF-1 is confirmed at its correct receiver.** As an identity of projector
maps on the whole operator space,

```text
a P_even + b P_odd = id
```

forces `a=b=1` by evaluating on nonzero even and odd operators. At a single
degenerate subject a coefficient multiplying a zero image is unobservable;
the universal reconstruction law still has unit weights.

**NF-2 is confirmed with wording narrowed.** There is no sealed canonical
splitter of the even projector image, so no independent **census-level**
weights for `h^2` and `W^2` may be introduced. The block is algebraically
splittable; “unsplittable” means no sealed canonical receiver exists. This
does not forbid coefficients already contained upstream in `h` or `W`.

---

## 4. Task C(1): NG-2

The local classification states `gamma^5` “up to one real scalar.” But the
global sealed construction then writes

```text
h_K = sum_c P_c tensor gamma^5 tensor iota_c(c_c)
```

and says its isolated-cell restriction is **exactly**
`gamma^5 tensor c_c`. The complete-parent carrier block adds the affirmative
rule:

```text
No new cell coefficient is admitted.
```

Therefore the multi-cell packet/global branch does decide the issue:
independent per-cell `lambda_spin,c` is barred. If the local residual is
retained in a more generic branch, it is one shared scalar, not a freely
varying cell family. Its value remains symbolic unless a separately sealed
normalization fixes it. Extending the conclusion beyond the sealed finite
global branch would require the same global identity-extension/naturality
law on the enlarged carrier; no such enlargement is made here.

`NG2 = DECIDED (global/shared at packet scope; independent per-cell values
excluded)`.

---

## 5. Task C(3): the H1 posed form

The five named 873 blockers are real and correctly typed at their immediate
receivers:

| receiver | finding |
|---|---|
| S1 | measure/base-family/descended pairing remains gated |
| S2 | interacting epsilon-indexed contour remains gated |
| S3 | joint physical preparation/gluing/boundary package remains gated |
| S4 | free-tail operator/domain is partial; complete physical endpoint/common-domain package remains gated |
| U1 | generic action-to-operator map remains absent/packet-scoped |

But the stationarity statement is not yet receiver-bound and “poseable” is
too strong. Its quantifier “for every admissible direction” lacks SC4-1
carrier, SC4-2 membership, SC4-3 path/tangent structure, SC4-4 total U1
closure, and SC4-6 completeness. D1/D3 are only the conditional lawful
predicate after a path carrier exists. NF-3 cannot supply that carrier and is
itself conditional.

The faithful present statement is a **template**:

```text
IF a same-incidence A_c membership/path package and total U1 are supplied,
IF the path is declared tangent to the adopted record-odd operator branch,
AND IF S1 supplies the pairing,
THEN pose delta S_rec[p]/delta L_c = 0 along D1/D3-preserving paths,
subject also to the S2/S3/S4 completion receivers.
```

No derivative or equation is executed. `H1_POSED = CORRECTED` from
“poseable” to “typed conditional template; not yet poseable on an inhabited
direction domain.”

---

## 6. Task C(2): lack ledger amended at one counting level

The twelve-item list mixes top-level roots with their lower leaves. For
example, S1–S4 are leaves of SC5-D, while the B0/correspondence/global-to-cell
items are SC5-A/B/E roots. It also omits SC5-C and five open SC4 roots.

Use this closed root-level successor:

| # | root lack | status |
|---:|---|---|
| 1 | SC4-1 declared `A_c` carrier | FREEDOM |
| 2 | SC4-2 `L_c in A_c` membership | ABSENCE |
| 3 | SC4-3 inhabited direction/path/tangent structure | FREEDOM |
| 4 | SC4-4 total class-wide U1 closure | ABSENCE |
| 5 | SC4-6 primitive-scope completeness | ABSENCE |
| 6 | SC5-A inhabited complete B0 role | ABSENCE |
| 7 | SC5-B B0-to-Boundary-Resolved correspondence | ABSENCE |
| 8 | SC5-C complete aggregate `DESCEND_B0` maps/certificates | ABSENCE |
| 9 | SC5-D complete U3 four-field package | ABSENCE; leaves S1–S4 remain separately displayed, with S4 partial only |
| 10 | SC5-E global-boundary-form to per-cell closure-face descent | ABSENCE |
| 11 | gravity term/type decision (`Delta_Gamma`) | NEITHER-FORCED / not startable |
| 12 | one shared residual `lambda_spin` value at generic-branch scope | SYMBOLIC / UNFILLED |
| 13 | `K_R` point on its positive ray | SYMBOLIC / UNFILLED |
| 14 | infinite-incidence grading/domain implementation or class bound | ABSENCE |
| 15 | calculus-U3 same-parent exhaustiveness | ABSENCE |

Corrections to 873's list:

- remove L10: the admitted sealed action's relabeling and orientation
  generators are grading-homogeneous; a generic mixing matrix is not an
  action member;
- remove the per-cell/global half of L7: NG-2 is decided at packet/global
  scope; retain only the one shared scalar value where the generic local
  branch leaves it open;
- add SC4-1/2/3/4/6 and SC5-C;
- count S1–S4 as disclosed leaves of SC5-D rather than additional roots;
- count the generator/correspondence/global-to-cell chain as SC5-A/B/E,
  avoiding duplication.

This is fifteen root-level lacks, not a “twelve lacks against seven terms”
ratio assembled from mixed levels.

---

## 7. Task C(4): citation hygiene

The free-tail realization is Q-773; Dario's confirming cross-check is Q-775.
Q-779 is the assembly itself. The cited sealed artifacts are the correct
free-tail pair and the S4 conclusion is accurately carried as
`FREE_TAIL_PARTIAL_ONLY`; only the displayed register number is wrong.

`CITATION = display-level`.

---

## 8. FREEDOMS-CONSUMED

```text
CARRIED UNCHANGED:
  Exit-B's restricted/non-exhaustive scope;
  D1 and D3 as conditional lawful predicates, D2 redundant only under its
    fixed-incidence path premise;
  all seven branch hypotheses as antecedents, with outside-branch competitors live;
  T4/T5 projector census and the absence of a finer even splitter;
  chi_P=0 only on the packet-parent branch;
  T7 as target, its generator non-identity, and the absent correspondence/descent;
  S1-S4 at their corrected current scopes, including free-tail partial only;
  gravity NEITHER-FORCED; global lambda value and K_R point unfilled;
  finite-incidence stop; calculus-U3 exhaustiveness absent.

DERIVED HERE:
  NF-3's conditional typing and the general mixed-variation projector formulas;
  a D1/D3-shaped parity-mixing operator witness;
  NF-1 and narrowed NF-2 checks;
  NG-2's packet/global decision from affirmative coefficient bytes;
  the conditional-template correction to H1;
  the closed root-level fifteen-lack successor;
  the display-only citation correction.

SELECTED HERE: NOTHING.
  No L_c member, carrier, direction, path, U1 map, measure, contour, boundary
  package, endpoint operator, gravity term, scalar value, K_R point, or
  infinite tensor is selected, installed, or preferred.

NOT DONE AND DISCLOSED:
  no parity-mixing operator witness is promoted to an inhabited action-side
  delta L_c; doing so requires the absent A_c and U1;
  no H1 variation is executed;
  no generic-branch scalar value is fixed;
  no frontier root is discharged.

SCALING WEIGHTS: none consumed.  SUBSTITUTED: none.
```

---

## 9. Flattening check and battery

**FLATTENING CHECK — CLEAN.** Rejected identifications:

1. support/target shape `=>` an inhabited direction;
2. operator-side odd write `=>` action-side odd `delta L_c` without U1;
3. projector stability `=>` perturbation homogeneity;
4. branch-tangent condition `=>` unconditional direction law;
5. formal action sum `=>` evaluated action or installed `L_c`;
6. pure-charge endpoint commutation `=>` generic Layer-A order theorem;
7. no even census splitter `=>` algebraic impossibility of splitting the block;
8. local one-real-scalar statement `=>` independent per-cell scalars after global descent;
9. S1–S4 leaves `=>` four roots in addition to SC5-D;
10. mixing algebraic counterexample `=>` admitted action member;
11. free-tail partial operator `=>` complete S4;
12. Q-779 assembly entry `=>` Q-773 free-tail entry.

`F_PLDEC = PASS`; `ANTI_TUNING = PASS`; `M-2 = PASS` in all four
modes. All diagnostics are finite algebraic operator checks. No headline is
promoted beyond CLAIMED and no standing gate moves.

DISCIPLINE = HELD (three refusals confirmed)
TERMS = CORRECTED (seven-slot provenance retained, but T1 is a derived restricted schema rather than an inhabited/evaluable term; 821 order independence stays pure-charge operator-side and cannot cross the absent U1 joint)
NF3 = CONDITIONAL (requires a supplied A_c/path plus U1 mapping delta L_c solely into the record-odd delta W slot with h fixed; census stability alone does not force oddness)
NF1_NF2 = CONFIRMED (NF1 at universal reconstruction-map scope; NF2 means no canonical census-level h^2/W^2 coefficients, not algebraic unsplittability)
NG2 = DECIDED (packet/global bytes require one coefficient-free cell sum and admit no new cell coefficient; independent per-cell lambda_spin values excluded)
LACKS = AMENDED (15 closed root-level items: SC4-1/2/3/4/6; SC5-A/B/C/D/E; gravity; shared lambda_spin value; K_R point; infinite-incidence implementation/bound; calculus-U3 exhaustiveness; L10 mixing and per-cell/global NG2 removed)
H1_POSED = CORRECTED (five S1/S2/S3/S4/U1 receivers are real, but SC4 carrier/membership/path/closure/completeness are also required; current object is a conditional template, not a poseable inhabited variation)
CITATION = display-level (free-tail Q-773, confirmation Q-775; Q-779 is the assembly)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
