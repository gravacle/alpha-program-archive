# Stage-8 T7 Codex Battery Tier-2 Returns V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY. ONE INDEPENDENTLY CONFIRMED ITEM (T8) + FOUR DERIVATIONS WITH
FOUR PRECISELY NAMED MISSING SEALED OBJECTS.
THE FOUR GAPS ARE RECORDED AS OBLIGATIONS, NOT AS FAILURES — per the
principal: "a precisely named missing sealed object is the most useful
thing this exercise produces."
NO BATTERY VERDICT IS ISSUED HERE; the evaluator alone issues it.
```

Derived by the INDEPENDENT-SYSTEM lane (Codex, different model family)
from SEALED SPEC TEXT ONLY.

## T8 — CONFIRMED CLEAN

```text
The full c > 0 family remains VISIBLE AND UNDECIDED through this gate. FS
geometry does NOT fix c = 1; only complete Q_spec can, and
complete_Q_spec_sealed = false.
CREDITED as independently confirmed. This is the correct outcome for a
no-go-preserving gate: the gate does not secretly select the coupling.
```

## T2 — exact universal algebra COMPUTED, with the represented class NOT certified

```text
DERIVED, with an explicit witness:
  T_mu = 1 + delta_mu
  W_univ - I = Log(W_univ) = delta_nu delta_mu - delta_mu delta_nu
               mod I_inc^3
  nonzero universal gr^2 witness: coefficient +1 on word (nu, mu),
                                  -1 on word (mu, nu)
All order claims follow: incidence >= 1, curvature >= 2, kinematic Pauli
>= 2, quadratic Maxwell >= 4. Order-zero-transport collapse is EXCLUDED
because T_mu is not homogeneous of degree zero.

*** THE GAP, AND IT IS THIS PROGRAM'S RECURRING CLASS. EXACT REPRESENTED
ORDER 2 IS NOT CERTIFIED. V011 itself says universal membership is only a
LOWER BOUND, and flat/commuting representations can kill the represented
class.
MISSING SEALED OBJECT (obligation, named):
    a sealed REPRESENTED nonzero gr_pi^2 witness.
T2 MAY NOT BE RECORDED AS DISCHARGED ON THE UNIVERSAL COMPUTATION ALONE. ***

THE PATTERN, NAMED SO IT IS RECOGNISED NEXT TIME: universal-vs-represented
is the same shape as OPERATOR-vs-SCALAR (the gamma fork, where a numerical
cancellation at 1e-13 was refused as a theorem) and OBJECT-vs-BOUND (the
Q2 tripwire). In all three, a computation in the larger/looser category is
mistaken for a result in the smaller/actual one. Three instances is a
class, and this lane treats it as a standing tripwire rather than three
coincidences.
```

## T1 — formula DERIVED, value UNDETERMINED

```text
Derived from sealed F_1, and machine-checkable rather than hand-supplied:
  W_(mu nu) = U_-^-1 U_+
  F_BR      = -i Log_0 W_(mu nu)
MISSING SEALED OBJECT (obligation, named):
    a sealed REPRESENTED link/holonomy object for the basepoint.
Until that exists the formula has no value to report. Recorded as
DERIVED-BUT-UNVALUED, which is a different and better state than open.
```

## T9 — identity DERIVED, BLOCKED_BY_ORDERING

```text
Born overlap identity:
  |A|^2 = 1 - ds_FS^2 + O(A^3)   hence   -log|A| = (1/2) ds_FS^2 + O(A^3)
It CHECKS the derived local Hessian and DOES NOT SELECT the action
multiplier — the correct and expected scope.
MISSING SEALED OBJECT (obligation, named):
    the T7 Hessian / normalized output-ray bundle with certified enclosure.
BLOCKED_BY_ORDERING, expected; it clears when T7 does. Not a failure and
not a finding.
```

## T11 — local face measure DERIVED, refinement naturality NOT proved

```text
DERIVED:
  <xi, xi>_(2,ell) = sum_(mu<nu) V_cell / (ell_mu^2 ell_nu^2) xi_(mu nu)^2
  xi_(mu nu) = ell_mu ell_nu F_(mu nu)
  each cell contributes  V_cell sum F^2
  general coframe = pullback by wedge^2(e^-1) times |det e|
NO inverse weight, NO ad hoc weight, NO residual shape scalar — which is
the substantive content: the measure is forced, not chosen.
MISSING SEALED OBJECTS (obligation, named, two of them):
    the sealed RESPONSE-MAP PULLBACK on common refinements; and
    BOUNDARY-SUBEXTENSIVE INVARIANCE over C_ref.
V011 flags cellulation_independence_proved = false.
```

### The principal's question: is T11's gap the same obligation as Q6?

Asked directly, and answered directly rather than left as a gesture.

```text
THIS LANE'S ASSESSMENT: NO — NOT THE SAME OBLIGATION. BUT PLAUSIBLY A
PREREQUISITE OF IT, WHICH IS A SHARPER AND MORE USEFUL RELATION.

WHAT THEY SHARE, and it is not nothing:
  the SAME QUANTIFIER — "all common refinements", the ratified
  unrestricted D3 reading; and
  the SAME FAILURE MODE — refinement-dependence, where a quantity that
  looks fine on a fixed cellulation degrades under subdivision.
WHY THEY ARE NOT THE SAME OBLIGATION:
  T11's gap is FUNCTORIALITY OF A MEASURE — does the response-map pullback
  commute with refinement, and is the boundary term subextensive?
  Q6's recast form is a COUNTING BOUND — is there a weighted,
  subdivision-insensitive bound on the number of anchored connected
  polymers? One is about a map's naturality, the other about a cardinality.
  Discharging either would not discharge the other.
WHY THE RELATION IS A DEPENDENCY, AND THIS IS THE USEFUL PART:
  Q6's RECAST form is explicitly the WEIGHTED, subdivision-insensitive
  count, and the weight it must use is the 4-VOLUME weight. A weighted
  count is only well-posed across refinements if the WEIGHT ITSELF is
  refinement-natural. T11 is precisely the derivation of that weight, and
  its open item is precisely whether the weight is natural under common
  refinements.
  SO: T11's naturality gap is plausibly a PREREQUISITE of Q6's recast
  form, not a synonym for it. If T11's naturality fails, the recast Q6 is
  not merely unproved — it may be ill-posed, because "weighted" would not
  survive the quantifier it is quantified over.
STATUS OF THIS ASSESSMENT: it is THIS LANE'S JUDGEMENT, formed from the
sealed statements of both items, and it is NOT certified. It is routed for
independent confirmation rather than adopted. It is recorded because the
principal asked to be told if the two are the same, and the honest answer
is "not the same, but one may sit under the other" — which changes the
ORDER of work if confirmed: T11's naturality would come BEFORE any attempt
at the recast Q6, and Q6 reading B should not be commissioned until it is
settled.
NOTE ALSO: this makes THREE items now circling the same quantifier — Q6
recast, A-L5 (the D3-uniform anchored sum), and T11's naturality. Two
hostile lanes have already been asked whether A-L5 and Q6 are one
obligation and returned NOT_A_FINDING. The right question for the next
pass is not pairwise identity but whether ALL THREE share a single missing
sealed object: a refinement-natural measure on D3.
```

## Protected status

```text
T8_independently_confirmed = true
T8_c_family_undecided_through_gate = true
complete_Q_spec_sealed = false
T2_universal_algebra_derived = true
T2_represented_order2_certified = false        (OBLIGATION: sealed
                                                represented nonzero gr_pi^2
                                                witness)
T2_discharged = false
T1_formula_derived = true
T1_value_determined = false                    (OBLIGATION: sealed
                                                represented link/holonomy
                                                object for the basepoint)
T9_identity_derived = true
T9_status = BLOCKED_BY_ORDERING                (OBLIGATION: T7 Hessian /
                                                normalized output-ray
                                                bundle with certified
                                                enclosure)
T9_selects_action_multiplier = false
T11_local_measure_derived = true
T11_refinement_naturality_proved = false       (OBLIGATIONS: sealed
                                                response-map pullback on
                                                common refinements;
                                                boundary-subextensive
                                                invariance over C_ref)
cellulation_independence_proved = false
T11_gap_same_obligation_as_Q6 = false          (assessed: PREREQUISITE,
                                                not synonym; UNCERTIFIED)
three_items_share_the_D3_quantifier = Q6_recast, A_L5, T11_naturality
universal_vs_represented_tripwire_recorded = true
battery_verdict_issued = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
