# Stage 8 Task 4d Stitching Theorem and Germ-Independent Half v001

Date: 2026-08-02

Lane: CODEX LANE 1

Task: 4d

Status: RESULT / THEOREM FAMILY STATED / GERM-INDEPENDENT HALF PROVED / NO ADOPTION

Register head consulted at start and before writing: Q-257

## 0. Lead determination

**A DIRECTED NORM-COMPLETION EXISTS IN THE RATIFIED CLASSES, BUT IT IS NOT
THE DISCRETE-TO-CONTINUUM STITCHING THEOREM REQUIRED BY DoR-007.** It is a
kinematic cylindrical completion of sequential character labels:

```text
Lambda_N = Z^N,
Lambda = colim_N Lambda_N = direct-sum_(j>=1) Z e_j,
A_N = C*(Lambda_N),
A_F = C*(Lambda),
B_N = A_N,+ tensor_min A_N,-^op,
B = A_F,+ tensor_min A_F,-^op.
```

The finite cylinder algebras have norm-dense union in `A_F` and `B`; the
canonical finite retractions are natural and separate the full norm C-star,
standard-module, and left-multiplier classes. No norm-class moving tail is
created by the limit.

But the ratified source states on its face that its index category introduces
**no geometric refinement relation**, and that its labels contain no edge,
path, smooth base, metric, or measure. The proposed refinement/path functor was
deliberately removed before ratification. The later forcing protocol then
found 1,088 surviving `K_square` support filtrations and no complete
finite-incidence realization functor. Therefore:

```text
KINEMATIC_CYLINDRICAL_COMPLETION_EXISTS = true | TYPE-P |
  premises: DoR-008 and the imported mathematics listed in Section 1.3

KINEMATIC_CYLINDRICAL_COMPLETION_IS_GEOMETRIC_REFINEMENT_LIMIT = false | TYPE-R |
  test: compare the ratified index, morphisms, generators, and preserved
        relations with BareRec_2 and C_ref; no cell, degree, boundary,
        incidence, common-refinement, or response map is carried

DOR007_NAMED_EQUIVALENCE_THEOREM_DISCHARGED = false | TYPE-U |
  would-build: the geometric-refinement realization, topology, response
               naturality, boundary-subextensivity, and physical-interface
               package in Section 8

CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U |
  would-build: Section 8 fields 1-5 and execute the theorem family T_ref/T_phys
```

The correct new class is:

```text
KINEMATIC_CYLINDRICAL_COMPLETION :=
  a norm-complete algebraic limit whose finite stages are sequential label
  cylinders, but which carries no certified geometric refinement semantics.
```

This class is neither a failed continuum nor an imported smooth continuum. It
is a real derived-given-DoR-008 completion with a lower scope ceiling. Calling
it the Task-4d continuum would erase exactly the missing theorem.

The germ-independent result is therefore a split:

```text
T_CYL  = PROVED in the ratified norm/module classes;
T_REF  = STATED AS A FAMILY, not frozen and not proved;
T_PHYS = INTERFACE-CONDITIONAL, not discharged.

TASK4D_GERM_INDEPENDENT_COMPLETION_CORE = 5_of_5_proved
TASK4D_PUBLIC_CONTINUUM_CONSUMERS_DISCHARGED = 0_of_5 | TYPE-U |
  would-build: T_ref followed by T_phys
```

The stitching cap does **not** lift. The result narrows the cap to one package:
the target-independent geometric-refinement realization and response-
naturality package, with the already named boundary and physical-interface
obligations attached.

## 1. Preflight, scope, premises, and authorities

### 1.1 Preflight

```text
DOES_THE_OBLIGATION_EXIST = true
  DoR-007:8-10 and the governing plan:214-218 name it.

DOES_A_STITCHING_THEOREM_EXIST = false | TYPE-U |
  would-build: Section 8 package

IS_VERSION_CURRENT = true_through_Q_257
  Q-257 supplies the newest finite-core/class-transport theorem and does not
  supply geometric refinement semantics.

ARE_GERM_INDEPENDENT_INPUTS_PRESENT = true
  DoR-008's ratified completion, Q-245/Q-247/Q-257 finite-core machinery,
  Gate-4's discrete connection, and V011's finite cell data exist.

ARE_GEOMETRIC_STITCHING_INPUTS_PRESENT = false | TYPE-U |
  would-build: a target-independent map from finite record complexes and
               refinements into the ratified completion, plus the topology
               and response natural transformations in Section 8
```

No new physical premise is introduced. Every positive conclusion using the
ratified completion is:

```text
TYPE-P | premises: DoR-008 and declared imported mathematics.
```

The adopted spacetime `(M,g)` is not used to supply a smooth domain, a
refinement category, or the target continuum. No germ field is authored.

### 1.2 Scope

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
```

Excluded:

```text
.git; binary/media payloads; archive mirrors as independent authorities;
superseded variants except where lineage was necessary; the germ draft held
by the other lane; and a32_holdout/custodian_private (not entered, listed,
searched, opened, or read).
```

Bounded, case-insensitive searches used:

```text
stitching | discrete-to-continuum | refinement | common refinement |
cellulation independence | response pullback | boundary subextensive |
directed limit | colimit | cylindrical density | zero-extension |
realization functor | smooth connection | physical public EM | Ward |
Thomson | moving tail
```

### 1.3 Authorities

| Authority | SHA-256 | Load-bearing content |
|---|---|---|
| `DECISION_OF_RECORD_007_SMOOTH_FORK_DERIVE_THE_LIMIT_2026-08-01_V001.md` | `17dc023e297cfd6960a36623af2aa8efd57210574076590ef10b579b72c1808b` | derive the refinement-limit equivalence; do not adopt `(M,g)` |
| `TASK_LIST_HERE_TO_ALPHA_2026-08-01_V002.md` | `9f549ef3f34f74f7fe17ae2f7d3331a0313830582b856b48c9d6c8576bf4ed5b` | Task 4d and downstream gate |
| `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | BareRec_2, discrete connection, face curvature, per-cell density, C_ref obligation |
| `RESULT_STITCHING_RULE_DECOMPOSED_2026-07-30.md` | `578eb5e9d7076b053ab31f4e26c99f9677e911f41a6daaea760275810eeec2a0` | measure/result split and two coherence obligations |
| `STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md` | `76916244bdbcac7c2a6d4afae40f35127540d1d9e4cfc86fb72318506671161f` | ratified sequential index, labels, norm C-star/CTP completion, explicit exclusions |
| `STAGE8_TASK4A_FINITE_RESTRICTION_SEPARATION_AND_BIDUAL_TAIL_DETERMINATION_V001.md` | `60b5b4c5788eca2be2d9f11d67983b2e7a5823066cdabf9a734f7a59aae0ecd1` | retractions, separation, topology split, moving-tail counterexample |
| `STAGE8_TASK4A_P7_FINITE_CORE_SEPARATION_T5_COMMUTING_SQUARE_CERTIFICATE_V001.md` | `07205bf5e1888bd39a97d4e86543852d5e9b88b103e5c0b429c76bd77290d6be` | 6/6 finite core and conditional class transport |
| `STAGE8_TASK2F_C0_008_FINITE_COMPLEX_RESTRICTION_PACKAGE_BUILD_AND_ARM_EXECUTION_ATTEMPT_V001.md` | `5515517ca2d1b48dd439fe97f2972292620132605767c4dd5de1007f67152c5a` | exact sequential/cellular mismatch and would-build package |
| `STAGE8_TASK2F_FINITE_INCIDENCE_REALIZATION_FUNCTOR_FORCING_PROTOCOL_RESULT_V001.md` | `a4d8b9c44fd0705ba97fd49d1e0c8373c28e12e2c3acea9409b60217b274a0f8` | 1,088 support survivors; full functor unbuilt |
| `STAGE8_DOWNSTREAM_SMOOTH_CONNECTION_REQUIREMENT_AUDIT_V001.md` | `46bfeb7d1c05a70661caa473cb47700efce70d074507563de8af576f26e7c207` | exact downstream public/continuum consumers |
| `STAGE8_PRPS_SMOOTH_PHRASING_IMPORT_ADJUDICATION_V001.md` | `e09349f5b71a790b40700669227268e7a4615750c9d03d164daa373bb40af3df` | discrete/smooth type distinction |
| `STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md` | `52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241` | refuted uniformity route and unstated bearing on extensivity |

Imported mathematics, declared rather than promoted to physics:

1. functoriality of group C-star algebras for the explicit abelian system;
2. Gelfand duality for discrete abelian groups;
3. Stone-Weierstrass density of finite-coordinate cylinder functions on a
   compact product;
4. functoriality of the minimal tensor product in the commutative/nuclear
   setting; and
5. definiteness of Hilbert C-star module inner products and faithfulness of
   left multiplication on the standard module.

## 2. What Task 4d actually asks

DoR-007:8-10 states:

> The smooth-required subset ... is to be met by a DERIVED
> DISCRETE-TO-CONTINUUM EQUIVALENCE THEOREM -- the stitching rule as a theorem
> over refinements.

DoR-007:21-22 adds that base smoothness exists only as a refinement limit and
that the limit's cellulation independence is the theorem's content. The
governing plan:214-218 places that theorem at Task 4d and says it gates the
public/alpha-facing response chain.

Those authorities fix the theorem's role, but not a unique mathematical
statement. In particular, they do not fix:

1. the category of geometric refinements independently of a supplied smooth
   coframe/connection;
2. the functor from Gate-4 finite record data to the limiting carrier;
3. the topology of convergence;
4. the complete response object transported along refinement; or
5. the equivalence maps to each public/continuum consumer.

```text
SEALED_TEXT_FIXES_UNIQUE_STITCHING_THEOREM_STATEMENT = false | TYPE-R |
  test: enumerate the theorem fields above and inspect DoR-007, the plan,
        V011:1397-1412, and the downstream consumer audit; fields 1-5 are not
        jointly supplied
```

The lawful response is therefore a theorem family, not a convenient freeze.

## 3. The theorem family

### 3.1 `T_cyl`: kinematic cylindrical completion

For the ratified thin category

```text
Ob(I_rec)=N_{>=1},
Hom(N,M)={j_NM} iff N<=M,
```

with zero-extension `j_NM^Lambda:Z^N->Z^M`, define

```text
Lambda = colim_(I_rec) Lambda_N,
A_N=C*(Lambda_N),
A_F=C*(Lambda),
B_N=A_N,+ tensor_min A_N,-^op,
B=A_F,+ tensor_min A_F,-^op.
```

`T_cyl` asserts, in C-star norm and Hilbert-module norm:

1. the algebraic colimit is the finite-support group
   `direct-sum_(j>=1) Z e_j`;
2. the union of finite-coordinate cylinder subalgebras is norm-dense in `A_F`
   and `B`;
3. the canonical truncation retractions are natural for `N<=M`;
4. the retractions separate the full norm C-star, standard-module, and
   represented left-multiplier classes; and
5. no nonzero norm-class element is invisible to every finite stage.

This theorem is proved in Section 5.

### 3.2 `T_ref`: geometric refinement stitching

Let `Ref` be a not-yet-instantiated category whose objects are admissible
finite oriented record complexes carrying the Gate-4 data and whose morphisms
are certified elementary refinements/common-refinement maps. A complete
candidate theorem must supply:

```text
D_ref : Ref -> PhysClass,
eta_K : Gate4Data(K) -> D_ref(K),
P_KR  : D_ref(K) -> D_ref(R) for each refinement K->R,
Resp_K: D_ref(K) -> Response_K,
```

and prove:

```text
P_KR eta_K = eta_R pullback_KR,
Resp_R P_KR = pullback_KR^Resp Resp_K,
```

with the intensive response invariant under common refinement up to a boundary
term whose ratio to four-volume vanishes. The resulting compatible response
must be independent of the cofinal refinement family in one frozen topology.

The candidate topology family left open by sealed text is:

```text
tau_norm    = C-star/module norm on the ratified completion;
tau_cyl     = projective convergence of every finite restriction;
tau_density = local-response/action-density convergence over C_ref.
```

No authority proves these equivalent or selects one. `tau_cyl` alone admits a
bidual tail; `tau_norm` excludes that tail but lacks geometric realization;
`tau_density` is named operationally by V011 but not defined as a complete
topological carrier. No member is frozen here.

### 3.3 `T_phys`: public-response equivalence

For any `T_ref` survivor, `T_phys` must additionally prove that the limit data
deliver, with compatible restriction/refinement diagrams:

1. the public electromagnetic connection rather than only vertex-edge
   incidence transport;
2. a local spacetime curvature/Maxwell tensor rather than only formal face
   holonomy;
3. the completed connection-history response and retarded CTP Hessian;
4. continuum Ward/transversality; and
5. the microscopic compact-flux-to-Thomson matching map.

These are the exact named consumers at
`STAGE8_DOWNSTREAM_SMOOTH_CONNECTION_REQUIREMENT_AUDIT_V001.md:199-217`.
The final normalization identity adds no independent smoothness burden; its
burden is upstream in selecting and matching the physical response.

## 4. Discrete input and claim partition

### 4.1 The discrete object actually available

V011:281-304 defines a `BareRec_2` object `(K,r,L,U)` with finite oriented
regular-CW complex `K`, one-dimensional vertex fibers, and unitary edge
transport. V011:864-904 defines face holonomy and
`F_BR=-i Log_0 W`. Gate 4 then returns the unit-weight covariant incidence
differential modulo vertex gauge, with loop holonomy as the residual freedom.

That object is not the ratified field-label cylinder. The two sides are:

| Gate/refinement side | Ratified completion side |
|---|---|
| finite CW complexes, cells, degrees, boundary, incidence, face holonomy | integer character labels, sequential stage, zero-extension, group C-star algebra |
| geometric/cellular embeddings and common refinements | the thin order category `N<=M` |
| distinguished incidence differential | arbitrary finite-support source-map grammar |

No shared word identifies them.

### 4.2 Partition

| Claim | Partition | Standing | Reason |
|---|---|---|---|
| `Lambda_N` directed system and algebraic colimit | PROVABLE-NOW | proved | explicit zero-extension |
| norm C-star/CTP inductive completion | PROVABLE-NOW | proved | finite cylinder functions are norm-dense |
| canonical retractions and `N<=M` naturality | PROVABLE-NOW | proved | frozen coordinate truncations |
| norm/module/left-multiplier separation | PROVABLE-NOW | proved | Q-247/Q-257 |
| Gate-4 discrete connection and face holonomy | PROVABLE-NOW | previously derived in finite scope | V011 and Gate 4 |
| per-cell `V_cell sum F^2` and coframe pullback | INTERFACE-CONDITIONAL | derived given supplied coframe | V011:1373-1395 does not derive the coframe |
| finite-complex/refinement category independent of smooth input | BLOCKED | `TYPE-U` | no instantiated source category satisfying DoR-007 |
| realization functor from Gate-4 complexes into the ratified classes | BLOCKED | `TYPE-U` | deliberately absent; 1,088 support survivors |
| complete source incidence operator and intertwiner | BLOCKED | `TYPE-U` | Q-246 six-component package |
| unique convergence topology | BLOCKED | `TYPE-U` | three inequivalent candidate roles remain |
| response pullback on common refinements | BLOCKED | `TYPE-U` | named, never built |
| boundary-subextensive invariance | BLOCKED | `NO_VERDICT` | R-L2b route refuted; bearing on this clause unstated |
| cellulation independence | BLOCKED | `TYPE-U` | V011:2286 is false |
| public/continuum consumer equivalence | GERM-INTERFACE | conditional | requires P2-P6 and the geometric package |

The P7 pattern reaches the first four rows on the completion side. It does not
transport them into the geometric rows because its canonical maps are
sequential-cylinder retractions, not finite-complex restrictions.

## 5. Proof of `T_cyl`

### 5.1 Algebraic directed limit

The ratified maps are

```text
j_NM^Lambda(n_1,...,n_N)=(n_1,...,n_N,0,...,0).
```

They are injective, preserve identity and composition, and every element in
their union has finite support. Hence the group colimit is exactly

```text
Lambda = direct-sum_(j>=1) Z e_j.
```

No representative choice remains.

```text
T_CYL_GROUP_COLIMIT = true | TYPE-P | premises: DoR-008
```

### 5.2 Norm C-star completion

By Gelfand duality,

```text
A_N = C(X_N),       X_N=U(1)^N,
A_F = C(X),         X=product_(j>=1) U(1).
```

The algebra embedding `A_N->A_M` pulls a function back along projection onto
the first `N` coordinates. Thus its image consists of continuous functions
depending on those coordinates only. The union over `N` is a unital,
self-adjoint subalgebra of `C(X)` and separates points: two different points
of `X` differ in some finite coordinate. Stone-Weierstrass therefore gives

```text
closure_norm(union_N A_N) = A_F.
```

The same argument on `Y=X_+ x X_-` gives

```text
closure_norm(union_N B_N) = B.
```

This proves an honest norm-inductive completion. It does not add a measure or
a spacetime topology.

```text
T_CYL_NORM_COMPLETION = true | TYPE-P |
  premises: DoR-008, Gelfand duality, Stone-Weierstrass
```

### 5.3 Retractions, density, and separation

The ordered coordinates define

```text
p_N:Lambda->Lambda_N,
p_N(sum_j n_j e_j)=(n_1,...,n_N),
p_N=p_NM compose p_M.
```

Functoriality gives retractions

```text
r_N^B:B->B_N,
q_N:E_F->(B_N)_(B_N),
Pi_N:L(B)->L(B_N)
```

with exact `N<=M` naturality. The embedded finite spectra that set all tail
coordinates to the identity have dense union in `Y`. Therefore a continuous
coefficient killed by every `r_N^B` vanishes on a dense set and is zero.
Definiteness of the standard-module inner product and faithfulness of left
multiplication then give

```text
intersection_N ker(r_N^B)={0},
intersection_N ker(q_N)={0},
intersection_N ker(Pi_N)={0}.
```

This reuses Q-247/Q-257 without changing its scope.

```text
T_CYL_RETRACTION_NATURALITY = true | TYPE-P | premises: DoR-008
T_CYL_NORM_CLASS_SEPARATION = true | TYPE-P | premises: DoR-008
```

### 5.4 Moving-tail kill-pass

Q-247 proves the category split:

```text
norm C-star / standard module / left multipliers: separation holds;
bidual weak-star class: separation fails by z_tail B**.
```

A moving-tail sequence does not converge in the C-star norm to a nonzero
restriction-invisible element. A weak-star bidual limit can. Therefore the
limit proved here stays in the ratified norm/module classes. It is not silently
enlarged to the bidual.

```text
T_CYL_NORM_MOVING_TAIL_EXISTS = false | TYPE-R |
  test: Q-247 norm separation on the full ratified classes

T_CYL_BIDUAL_TAIL_EXCLUDED_BY_RATIFIED_CLASS = true | TYPE-P |
  premise: Task 4d uses only the ratified norm/module completion at this step
```

### 5.5 Scope ceiling of the proof

The proof has established a completion of the **label algebra** and its module,
not a limit of the Gate-4 cellular connection. It contains no theorem assigning
a cell, edge, boundary, incidence operator, coframe, local derivative, or
response to a character coordinate.

```text
T_CYL_IS_FIRST_DERIVED_PHYSICAL_CONTINUUM = false | TYPE-R |
  test: the proved limit's domain and morphisms contain none of the fields
        required by T_ref or T_phys
```

## 6. Why the norm completion does not discharge `T_ref`

### 6.1 The exclusion is explicit

The ratified presentation:214-224 says `I_rec` is exactly the sequential
`N<=M` index and "introduces no geometric refinement relation." Lines 244-253
say its labels contain no oriented edge set, path matrix, smooth base, spatial
metric, or measure. Lines 271-273 say the refinement/path functor is absent and
would have been an eighth adoption.

This is not a defect in DoR-008. The narrow adoption did what it said. It is a
scope boundary that Task 4d must cross by theorem rather than vocabulary.

### 6.2 The map is not forced by the finite constraints

The realization forcing protocol proves that the largest executable
`K_square` support slice contains exactly 1,088 linear-extension filtrations.
Base case, `N<=M` naturality, orientation/boundary compatibility, and
unit-weight compatibility kill none. Even quotienting by all square graph
automorphisms leaves at least 136 orbits.

More importantly, the support maps still lack the finite algebra map,
representation, source incidence operator, and intertwiner. Therefore:

```text
GEOMETRIC_REALIZATION_UNIQUENESS = NO_VERDICT |
  prerequisite: the complete realization family is not instantiated

SUPPORT_MAP_UNIQUENESS = false | TYPE-R |
  test: complete covered K_square support family has 1088 survivors

SEQUENTIAL_NATURALITY_SELECTS_GEOMETRIC_REFINEMENT = false | TYPE-R |
  test: all 1088 survivors satisfy it
```

### 6.3 `C_ref` is an obligation, not the derivation

V011:1397-1412 freezes shape-regular cellulations and common refinements that
preserve the **same smooth coframe and connection**, then demands response
pullback commutation and a vanishing boundary/four-volume ratio.

That is a valid target interface, but it cannot by itself derive the smooth
limit from Gate-4 data: the smooth coframe and connection already appear in
the admissibility clause. Using that clause as the source of the smooth object
would be circular under DoR-007.

```text
C_REF_AS_WRITTEN_DERIVES_SMOOTH_DATA_FROM_GATE4 = false | TYPE-R |
  test: the class definition takes the same smooth coframe and connection as
        input before the refinement conclusion is posed

C_REF_USABLE_AS_TARGET_INTERFACE = true
```

### 6.4 The older wall remains, now more precisely located

The four failed smooth-bridge attempts are not rerun. The new limit direction
hits their common wall at a better typed point: there is no outcome-blind
functor transporting the finite cellular connection into the ratified
sequential completion. The new completion machinery proves what happens
**after** such a map lands in the norm class; it does not construct the map.

## 7. The remaining two coherence conditions

The prior stitching decomposition remains correct with one status correction
made explicit.

### 7.1 Measure-side content

V011:1373-1395 gives, in a supplied coframe,

```text
<xi,xi>_(2,ell)
  = sum_(mu<nu) V_cell/(ell_mu^2 ell_nu^2) xi_(mu nu)^2,
xi_(mu nu)=ell_mu ell_nu F_(mu nu),
```

so each cell contributes `V_cell sum F^2`; the general coframe law is the
pullback by `wedge^2(e^-1)` times `|det e|`. No extra face weight remains.

This proves the local density transformation given the coframe. It does not
derive the coframe or the refinement bridge.

### 7.2 Response pullback

The response-map pullback on common refinements remains unbuilt. The
ratified completion has no physical response object or finite-complex
restriction maps, and P7's `r_N^B/q_N/Pi_N` cannot be name-matched to them.

```text
RESPONSE_PULLBACK_ON_COMMON_REFINEMENTS = false | TYPE-U |
  would-build: `Resp_K`, geometric restriction maps, the common-refinement
               square, and its topology/domain preservation certificate
```

### 7.3 Boundary subextensivity

The boundary-subextensive clause is named, but its prior route does not close.
R-L2b's written uniform Hilbert-Schmidt obligation was refuted. Its own result
states that the campaign's binding did not specify what the refuting branch
implies for connected extensivity. Therefore neither "proved" nor "refuted"
may be transported to the stitching clause.

```text
BOUNDARY_SUBEXTENSIVITY_FOR_T4D = NO_VERDICT |
  prerequisite: a replacement failure-capable boundary estimate or a ruling
                connecting the R-L2b refuting branch to this exact clause

RL2B_REFUTATION_AUTOMATICALLY_REFUTES_T4D_BOUNDARY_CLAUSE = false | TYPE-R |
  test: the R-L2b result expressly records that bearing as unstated
```

## 8. Exact would-build package

The next object is one package, not an instruction to import a manifold:

```text
TASK4D_GEOMETRIC_REFINEMENT_STITCHING_PACKAGE := (
  1. Ref, an instantiated finite-record-complex/refinement category whose
     definition does not presuppose the smooth target it must derive;

  2. J_ref, a target-independent realization functor/natural transformation
     from Gate-4 complexes, connection, degree, boundary, and source incidence
     into one declared completion class;

  3. tau_ref, one frozen topology with proofs that all structure maps and
     limits use that topology and do not acquire a bidual moving tail;

  4. eta_conn and eta_curv, maps transporting edge connection and face
     holonomy/curvature through refinement, with common-refinement coherence;

  5. eta_resp, the response natural transformation and its commuting pullback
     square on every common refinement;

  6. BoundaryCert, a failure-capable boundary-subextensivity theorem for the
     exact response class, or a principal ruling replacing the refuted R-L2b
     route;

  7. PhysInterface, the P2-P6 topology/calculus, quotient/measure/contour/
     boundary/domain, response class, restriction, and T5 preservation data;

  8. PublicEquiv, certificates delivering the public EM connection, local
     Maxwell tensor, continuum Ward/transversality, and Thomson matching.
).
```

The earliest missing field is `J_ref`: without it, the already proved norm
limit has no geometric input and no refinement semantics. But `J_ref` cannot
be treated as a lone support ordering. Q-246/Q-217 show that it must carry the
algebra, representation, source-incidence, degree, and intertwining data, not
only a map of labels to cells.

```text
TASK4D_NEXT_RELAY_SIZED_TARGET = J_REF_FINITE_COMPLEX_REALIZATION_PACKAGE |
  scope: fields 1-4 above, with the complete outcome-blind candidate family
         declared before any finite response output is inspected

J_REF_DERIVABLE_FROM_CURRENT_RATIFIED_DATA = NO_VERDICT |
  prerequisite: the complete family is not instantiated; support uniqueness
                alone is already refuted
```

## 9. Conditional composition theorem

The germ-independent work can still be loaded in advance.

### Theorem

Assume a future package instantiates fields 1-8 of Section 8 and proves:

1. `J_ref` is functorial and lands in the ratified norm/module class;
2. every structure and response map commutes with common-refinement pullback;
3. `BoundaryCert` makes the intensive difference vanish on cofinal
   refinements;
4. `PhysInterface` satisfies the P7 class-transport predicates; and
5. `PublicEquiv` intertwines the limiting response with each named public
   consumer.

Then:

1. the norm-directed limit from `T_cyl` receives the geometric diagram;
2. common-refinement coherence makes the induced limit independent of finite
   representatives;
3. boundary subextensivity makes the intensive response independent of the
   cofinal cellulation family;
4. P7 separation rules out nonzero norm-class content invisible to every
   finite restriction; and
5. the public consumer diagrams commute.

Therefore `T_ref` and `T_phys` discharge by composition, with no new
finite-core or separation theorem.

### Proof

Functoriality supplies a cocone into the norm limit proved in Section 5.
Common-refinement commutation identifies any two finite representatives after
pullback to a shared refinement. `BoundaryCert` sends the residual intensive
difference to zero in the one frozen topology. P7's class-transport theorem
then promotes finite agreement to agreement in the completed norm/module
class. Finally `PublicEquiv` transports that unique class to the public
connection, curvature, Ward, and Thomson consumers. Each step uses one named
interface predicate; no germ body is authored by the proof. QED.

```text
TASK4D_CONDITIONAL_COMPOSITION_THEOREM = true | TYPE-P |
  premises: fields 1-8 and predicates 1-5 above

TASK4D_COMPOSITION_REQUIRES_NEW_FINITE_CORE_THEOREM = false | TYPE-R |
  test: Section 5 and Q-257 already prove the finite-core and class-separation
        steps used by the composition

TASK4D_COMPOSITION_PREMISES_INSTANTIATED_NOW = false | TYPE-U |
  would-build: Section 8 package
```

## 10. Kill-passes

### 10.1 No smooth domain imported

`(M,g)` supplies no domain, topology, coframe, refinement category, connection,
or measure in this artifact. V011's smooth-input `C_ref` is used only as a
target interface and is not called a derivation.

### 10.2 No continuum rule imported by convention

The only complete limit proved is the ratified norm completion. No lattice
spacing limit, smooth atlas, exterior derivative, distributional completion,
or continuum field theory is supplied from ordinary practice.

### 10.3 No topology switch

Every positive limit claim is in C-star norm or Hilbert-module norm. The
projective/cylindrical and density candidates remain candidates. The bidual
weak-star tail is reported and excluded from the proved norm theorem, not
silently identified with it.

### 10.4 No old bridge attempt rerun

No patch cover, nerve, PRPS localization, or target-specific support map is
reconstructed. Their common missing functor is identified from current
machinery.

### 10.5 No target-aware realization

No `K_square` ordering, finite response polynomial, or downstream public
coefficient is used to select `J_ref`. The 1,088-member result is evidence of
unforced support, not a menu from which this lane chooses.

## 11. Final flags

```text
T4D_THEOREM_FAMILY_STATED = true
T_CYL_PROVED = true | TYPE-P | premises: DoR-008
TASK4D_GERM_INDEPENDENT_COMPLETION_CORE = 5_of_5_proved
TASK4D_PUBLIC_CONTINUUM_CONSUMERS_DISCHARGED = 0_of_5 | TYPE-U |
  would-build: T_ref followed by T_phys
T_CYL_TOPOLOGY = C_STAR_NORM_AND_HILBERT_MODULE_NORM
T_CYL_FINITE_CYLINDER_UNION_NORM_DENSE = true | TYPE-P
T_CYL_CANONICAL_RETRACTIONS_NATURAL = true | TYPE-P
T_CYL_NORM_CLASS_SEPARATION = true | TYPE-P
T_CYL_BIDUAL_PROMOTION_MADE = false | TYPE-S |
  scope: all constructions and proofs in this artifact

T_REF_PROVED = false | TYPE-U |
  would-build: TASK4D_GEOMETRIC_REFINEMENT_STITCHING_PACKAGE fields 1-6
T_PHYS_PROVED = false | TYPE-U |
  would-build: that package fields 1-8
GEOMETRIC_REALIZATION_FUNCTOR_INSTANTIATED = false | TYPE-U |
  would-build: Section 8 fields 1-4
RESPONSE_REFINEMENT_NATURALITY_PROVED = false | TYPE-U |
  would-build: Section 8 field 5
BOUNDARY_SUBEXTENSIVITY_PROVED = NO_VERDICT
CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U |
  would-build: execute T_ref after fields 1-6 exist
PUBLIC_EM_EQUIVALENCE_PROVED = false | TYPE-U |
  would-build: execute T_phys after fields 1-8 exist

DOR007_KINEMATIC_COMPLETION_PREREQUISITE_AVAILABLE = true | TYPE-P |
  premises: DoR-008
DOR007_REFINEMENT_EQUIVALENCE_DISCHARGED = false | TYPE-U |
  would-build: Section 8 package
STITCHING_CAP_LIFTED = false | TYPE-U |
  would-build: T_ref and T_phys

NEW_PHYSICAL_PREMISE_ADOPTED = false | TYPE-S |
  scope: this artifact
SMOOTH_DOMAIN_IMPORTED_FROM_M_G = false | TYPE-S |
  scope: this artifact
TARGET_OUTPUT_USED_TO_SELECT_LIMIT = false | TYPE-S |
  scope: this artifact
TOPOLOGY_CHANGED_MID_PROOF = false | TYPE-R |
  test: each proof step names norm/module or bidual weak-star separately
FENCE_BLOCKED_STRUCTURAL_RESULT = false | TYPE-S |
  scope: this artifact

alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
```

No physical value, coupling, scale, root, or measured comparison was computed.
No theorem was promoted beyond the class on which it was proved.
