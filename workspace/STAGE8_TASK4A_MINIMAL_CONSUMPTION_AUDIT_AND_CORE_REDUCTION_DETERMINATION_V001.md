# Stage 8 Task 4a Minimal-Consumption Audit and Core-Reduction Determination V001

Date: 2026-08-02  
Task: PASTE 362 / Task 4a  
Lane: CODEX LANE 2  
Status: CORE-ONLY PHYSICAL-VERDICT REDUCTION REFUTED; MINIMAL VERDICT CERTIFICATES IDENTIFIED

Premise-dependent positives are marked:

```text
TYPE-P | premises: DoR-008, DoR-009, DoR-013, DoR-014
```

where the finite-to-retarded transport statement is used, DoR-011 is also named.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 0. Lead determination

**The physical `p_ch` cancellation/survival verdict is not decidable on the
built finite core alone.** The proposed core reduction is refuted by two
independent, already-sealed failure modes:

1. a completed response may differ by a source-dependent restriction-invisible
   tail while all finite source germs, derivatives, and mixed retarded blocks
   remain unchanged; and
2. the completed response is evaluated on an uninstantiated stationary
   background whose `p_ch` dependence is not fixed by the finite zero-source
   law or by the partly forced CTP surface.

The exact finite statement **is** decidable and has already run:

```text
FINITE_CORE_MIXED_RETARDED_P_CH_TRACE_DECIDABLE = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013, DoR-014, DoR-011

FINITE_CORE_MIXED_RETARDED_BLOCK_CONTAINS_P_CH = false | TYPE-P |
  premises: same | test: Q-243 exact Keldysh rotation and ordered projection

CORE_ONLY_REDUCTION_TO_PHYSICAL_P_CH_VERDICT = false | TYPE-R |
  test: Q-250 source-dependent tail countermodel plus Q-252's unbuilt
        stationary-background map preserve or bypass the finite verdict

PHYSICAL_P_CH_CANCELLATION_OR_SURVIVAL = NO_VERDICT
```

The four missing P3 fields are therefore **not moot**. None is currently mere
domain formality. Each occurs in the package-preservation and extraction
certificates required to transport the finite zero into the physical response.
But the symbol-only verdict does not require computing their full numerical
content. It requires a weaker, exact certificate from each field proving that
completion, restriction, inversion, boundary-value extraction, and closure do
not introduce or hide `p_ch` dependence.

Those four certificates are necessary on the object-side route, but they are
not sufficient. The minimal deciding subset also requires:

```text
P5  a class-typed RetHess_phys and injective class embedding;
P6  physical restriction maps and the commuting restriction/extraction square;
BG  the common-origin stationary-background map or a theorem making the
    verdict uniform over every admitted background;
OUT the selected output's p-dependence consumption/factorization signature.
```

Thus the four fields move neither wholly to a later value-only chain nor wholly
out of the deciding chain. Their **full realizations** gate physical values;
their **symbol-preservation certificates** remain part of the minimal verdict
chain. The background and output-consumption objects remain independent
blockers after those certificates.

## 1. Preflight, scope, and authorities

### 1.1 Currency

This audit is current through register head Q-278. It consumes:

- P2 V002, the current sign-corrected source topology and calculus;
- P4, the anchored `Log_0` germ and `W=-i hbar Log_0 Z` convention;
- the Q-278 law-side P3 subpackage;
- Q-243's exact finite Keldysh chain;
- Q-250's tail-quotient countermodel;
- Q-251's six-output consumption audit;
- Q-252's background-channel determination; and
- Q-257's conditional P7 class-transport and T5 theorems.

Relay 361 was observed only as an in-flight/unregistered finite-reference task.
No result from it is assumed:

```text
RELAY_361_RESULT_CONSUMED = false | TYPE-S |
  scope: registered/sealed authorities available through Q-278 |
  reason: no registered returned artifact was available to this audit
```

### 1.2 Roots entered

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
```

The archive workspace was not treated as an independent source when a
cleanroom authority existed. The following were not entered or performed:

```text
a32_holdout/custodian_private/                    NOT ENTERED
alpha/kappa/coupling/root/scale evaluation        NOT PERFORMED
physical eigenvalue or spectral evaluation        NOT PERFORMED
comparison to measured constants                  NOT PERFORMED
register/plan/tracker/git/commit/push              NOT TOUCHED
```

### 1.3 Load-bearing authorities

| Authority | Load-bearing content |
|---|---|
| `STAGE8_TASK4A_P2_PHYSICAL_SOURCE_TOPOLOGY_AND_DIFFERENTIAL_CALCULUS_CONSTRUCTION_V002.md:16-57,95-165,167-244` | P2 source Banach topology, dense finite core, derivatives, and explicit refusal to type `RetHess_phys` |
| `STAGE8_TASK4A_P4_PHYSICAL_LOG_GERM_ON_P2_CALCULUS_CONSTRUCTION_V001.md:23-92,135-208` | anchored `Log_0`, physical `W`, finite mixed-retarded zero, and incomplete physical provenance |
| `STAGE8_TASK4A_FINITE_HESSIAN_TO_DELTAPHI_TRANSPORT_MAP_CONSTRUCTION_AND_P_CH_TRACE_V001.md:193-255,360-440` | exact finite rotation and the four physical promotion inputs; no direct finite-Hessian-to-`DeltaPhi` signature |
| `STAGE8_TASK4A_RESPONSE_CLASS_FORCING_AND_TAIL_OUTPUT_INERTNESS_DETERMINATION_V001.md:278-419,421-532` | source-dependent tail countermodel, quotient determinacy, and object/output closure alternatives |
| `STAGE8_TASK4A_ALPHA_FACING_OUTPUT_TAIL_ANNIHILATION_THEOREM_DETERMINATION_V001.md:411-447,496-527` | six complete outputs have unspecified consumption; finite visible quotients are not complete response outputs |
| `STAGE8_TASK4A_BACKGROUND_CHANNEL_STATIONARY_EVALUATION_POINT_DETERMINATION_V001.md:270-343,393-430,470-496` | C1 is not an evaluation rule; physical surface partly fixed; background instance and its `p_ch` dependence unbuilt |
| `STAGE8_TASK4A_P7_FINITE_CORE_SEPARATION_T5_COMMUTING_SQUARE_CERTIFICATE_V001.md:391-455,457-588` | conditional class transport, package preservation, extraction naturality, and physical restriction requirements |
| `STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md:17-77,144-292,294-368,374-420` | exact law-side subpackage and the four narrowed P3 residues |

## 2. The minimal computation, written as its own chain

Let `[A]` denote an admitted DoR-014 rank class, carried symbolically and never
instantiated. The p-entry map already established is:

```text
d_state -> p_[A] -> Z_inc -> Log_0 Z_inc -> source derivatives.
```

The candidate physical verdict chain is:

```text
V1  P4 anchored functional
      W_[A] = -i hbar Log_0 Z_[A]

V2  P2 source calculus
      D W_[A], D^2 W_[A], and mixed common/difference derivatives

V3  finite Keldysh/retarded extraction
      H_N^(delta,c) from the exact rotated finite Hessian

V4  physical promotion
      raw G -> inverse/contour Hessian -> RetHess_phys,
      with physical restrictions rho_G,N and rho_H,N

V5  physical evaluation
      (Abar_*,G_*(Abar_*)) at J=R=0 and A_delta=0

V6  selected response/output consumption
      RetHess_phys -> selected complete response object -> DeltaPhi or the
      exact alpha-facing consumer whose `p_ch` dependence is being asked

V7  structural verdict
      does the resulting physical expression contain dependence on p_[A]?
```

`V7` asks only presence or absence of the symbol. It does not request a value.
That weaker output removes the need to evaluate the physical object, but it
does not remove the need to prove that the arrows carrying the symbol are
well-defined and dependence-preserving.

## 3. Per-step consumption table

| Step | Built status | Four-field consumption | Verdict |
|---|---|---|---|
| V1: anchored physical `W` on the source germ | P4 source-analytic body exists on P2; its full physical provenance is incomplete (`P4:72-91`) | none to form the analytic germ; P3 fields remain in the full physical tuple | `RUNS-ON-BUILT | TYPE-P` |
| V2: first/second/mixed source derivatives | P2 V002 supplies complex Fréchet calculus and restriction naturality (`P2:95-165`) | none for analytic differentiation on the dense/norm source core | `RUNS-ON-BUILT | TYPE-P` |
| V3: finite Keldysh rotation and ordered mixed block | Q-243 executes exactly; the `(delta,c)` block is zero | none at finite stage; sealed finite branch convention suffices | `RUNS-ON-BUILT | TYPE-P` |
| V4a: analytic derivatives -> physical raw `G` | only the source-analytic operands exist; the raw physical correlator does not | measure, contour, boundary/contact, and domains enter the contraction and physical operator signature | `NEEDS ALL FOUR ESSENTIALLY | TYPE-U` |
| V4b: raw `G` -> inverse/retarded Hessian | P5/P6 uninstantiated | measure fixes contraction/inverse; contour fixes ordered boundary value; boundary/contact fixes inverse problem; domains fix closures and inverses | `NEEDS ALL FOUR ESSENTIALLY PLUS P5/P6 | TYPE-U` |
| V4c: finite-to-physical transport | P7 theorem conditional, not executed | all four occur explicitly in P7 C4; contour/measure/domain conventions recur in C5 | `NEEDS FOUR FIELD CERTIFICATES PLUS T1-T2/C3-C5 | TYPE-U` |
| V5: evaluation on the physical saddle | surface partly fixed; point unbuilt | the common-origin background map declares the physical quotient, contour, measure, contacts, boundary/domains and stationary class | `NEEDS COMPLETED PACKAGE AND BG MAP | TYPE-U` |
| V6: output consumption | six complete outputs have no sealed tail factorization or tail pairing | full field values are not logically required merely to declare a dependence signature, but a selected output map/factorization certificate is absent | `NEEDS OUT SIGNATURE | TYPE-U` |
| V7: final physical presence/absence verdict | finite premise exists; physical antecedents do not | inherits V4-V6 | `NO_VERDICT` |

The table proves a three-way split:

1. V1-V3 are exact built-core mathematics.
2. V4-V5 are object construction/promotion and consume the four fields
   essentially, not decoratively.
3. V6 needs less than a numerical evaluation but more than a name: it needs a
   declared response consumption or a factorization through a proved quotient.

## 4. The core-reduction proposal and its refutation

### 4.1 Proposed reduction

The strongest lawful version of the proposed reduction is:

> If the finite mixed-retarded block has no `p_ch` dependence on a dense core,
> and completion/extraction commute with that core, then the physical response
> has no `p_ch` dependence.

The antecedent splits into two claims. The first is proved; the second is not.

```text
FINITE_MIXED_RETARDED_BLOCK_P_CH_FREE = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013, DoR-014, DoR-011

COMPLETION_AND_EXTRACTION_COMMUTE_WITH_FINITE_CORE = false | TYPE-U |
  would-build: P3 four-field preservation certificates, P5 class embedding,
               P6 restrictions, and P7 C3-C5 execution
```

### 4.2 Tail countermodel

Q-250 proves that if the completed class admits a nonzero
`t in Tail_R`, then

```text
H_t(s)=H(s)+f(s)t
```

has the same finite source germs and every finite derivative as `H`, while its
complete derivative may differ. Every finite restriction annihilates `t`.
Choosing `f` to depend symbolically on `p_[A]` gives two completed candidates
with identical V1-V3 data and different complete `p_[A]` dependence.

This is a countermodel to **derivation from the current finite data**, not a
claim that a physical tail is admitted. The physical embedding remains
`NO_VERDICT`. It is nevertheless sufficient to refute the inference that the
finite result alone forces the physical verdict.

```text
FINITE_DATA_FORCE_PHYSICAL_P_CH_ABSENCE = false | TYPE-R |
  test: H and H+f(p_[A])t agree under every finite restriction

PHYSICAL_P_CH_TAIL_EMBEDDING_EXISTS = NO_VERDICT |
  prerequisite: RetHess_phys, its support/reality/Ward/domain class, and a
                physical tail embedding
```

### 4.3 Background counterchannel

Independently of the tail, Q-252 fixes only:

```text
A_delta=0, R=0, J=0,
G=G_*(Abar), A_c=Abar.
```

It does not fix `(Abar_*,G_*)`. C1 is an operator reduction at zero connection,
not a response evaluation rule. The finite relative-phase functional has no
interior stationary point, so it supplies no finite `a_*(p)` that could replace
the missing physical saddle.

Consequently, even `Tail_R=0` would not by itself settle whether evaluating a
completed `p_ch`-dependent or `p_ch`-independent operator on the physical saddle
reintroduces state dependence.

```text
BACKGROUND_CHANNEL_REMOVED_BY_FINITE_CORE_ZERO = false | TYPE-R |
  test: the physical saddle map is a distinct arrow and is uninstantiated

PHYSICAL_STATIONARY_BACKGROUND_P_CH_DEPENDENCE = NO_VERDICT
```

### 4.4 Reduction theorem verdict

The proposed theorem is therefore false on the current admitted envelope:

```text
THEOREM_PROPOSAL:
  finite-core p_ch absence => physical p_ch absence

VERDICT:
  false | TYPE-R

COUNTERCHANNELS:
  restriction-invisible source-dependent completion;
  untyped physical stationary background.
```

This refutes the reduction, not the physical cancellation itself.

## 5. What the four missing fields minimally need to supply

The symbol-only verdict can consume **certificates** rather than evaluated
field values. For each field, the weakest identified certificate is:

| Missing P3 field | Minimal verdict certificate | Why it can fail | Current standing |
|---|---|---|---|
| branch-joint measure / complex history functional | marginal consistency; p-neutral descent; contraction and restriction commute | a measure may weight appended histories or charge sectors in a p-dependent way even when cylinder pullbacks agree | `TYPE-U` |
| interacting contour / `i epsilon` | an E_post-compatible analytic family with unique p-neutral boundary-value rule; restriction and retarded extraction commute with its limit | endpoint orientation does not determine pole displacement, boundary value, or regulator limit | `TYPE-U` |
| complete joint boundary/contact package | restrictions preserve the same preparation, contacts, edge terms, and boundary conditions; no p-dependent term enters only at completion | two completions sharing an interior core can differ by boundary/contact data | `TYPE-U` |
| unbounded endpoint operators/domains | named closable operators with a common core; unique p-neutral closure or proof that every admitted extension gives the same dependence verdict | distinct self-adjoint/closed extensions can share a core yet differ globally | `TYPE-U` |

These are not declarations that the certificates exist. They are the exact
weaker objects that would make full field evaluation unnecessary for the
presence/absence question.

```text
FULL_NUMERICAL_REALIZATION_OF_FOUR_FIELDS_REQUIRED_FOR_SYMBOL_VERDICT = false |
  TYPE-R |
  test: a dependence-preserving naturality/factorization certificate suffices

FOUR_FIELD_SYMBOL_PRESERVATION_CERTIFICATES_CURRENTLY_BUILT = false |
  TYPE-U |
  would-build: the four certificates in the table

FOUR_FIELDS_ARE_ONLY_DOMAIN_FORMALITIES = false | TYPE-R |
  test: each can change contraction, analytic boundary value, global boundary
        term, or closure while preserving the finite algebraic core
```

## 6. The other indispensable pieces

### 6.1 P5/P6 class and restriction package

P2 proves its source tail zero. It expressly does not prove the response tail
zero. P7's class-transport lemma needs:

```text
T1  injective embedding RetHess_phys -> L_B(E_F);
T2  iota_R,N compose rho_H,N = Pi_N compose iota_R.
```

Only after T1-T2 and the P3-P6 package-preservation/naturality conditions are
instantiated does the theorem give `Tail_R=0` and a commuting extraction square.

```text
P2_SOURCE_TAIL_ZERO_DISCHARGES_PHYSICAL_RESPONSE_TAIL = false | TYPE-R |
  test: distinct source and response carriers; P7 T1-T2 remain uninstantiated
```

### 6.2 Background map

The minimal background object remains:

```text
COMMON_ORIGIN_STATIONARY_BACKGROUND_MAP:
  (completed Z_inc/Log_0, state, effects, dynamics, physical source domain)
    -> (Abar_*,G_*(Abar_*)) at J=R=0
    -> H_R[G_*(Abar_*)] at A_delta=0.
```

For the symbol verdict, a full solution may be replaced by a theorem that the
verdict is uniform over every admitted stationary background. Neither object
exists.

### 6.3 Output consumption

Q-251 finds six complete response outputs with unspecified tail consumption.
For `DeltaPhi`, the response-to-phase arrow is absent. The symbol-only audit
does not need a numerical `DeltaPhi`; it needs one of:

```text
OUT-A  a selected complete map whose formula exposes its p-dependence;
OUT-B  a theorem that the selected output factors through the physical finite
       restriction quotient;
OUT-C  a theorem that the output is constant on p-varying response fibers.
```

No sealed object supplies any of OUT-A through OUT-C.

```text
FULL_DELTAPHI_VALUE_REQUIRED_FOR_P_CH_PRESENCE_TEST = false | TYPE-R |
  test: a certified p-dependence signature or quotient factorization suffices

DELTAPHI_P_CH_DEPENDENCE_SIGNATURE_EXISTS = false | TYPE-U |
  would-build: OUT-A, OUT-B, or OUT-C on the selected physical consumer
```

## 7. Verdict-chain versus value-chain

### 7.1 Minimal verdict-chain

The shortest presently justified chain is:

```text
BUILT:
  P4 anchored W
  -> P2 derivatives
  -> Q-243 finite Keldysh rotation
  -> exact finite mixed-retarded p_ch absence.

STILL REQUIRED:
  four P3 symbol-preservation certificates
  -> P5 RetHess_phys class
  -> P6 physical restrictions and extraction square
  -> P7 class transport or an output-side tail factorization
  -> stationary-background map or background-uniformity theorem
  -> selected output p-dependence signature
  -> physical cancellation/survival verdict.
```

The 361 finite nonzero-`R` reference, if it returns and passes its own gate,
would be an additional finite control for the bottom leg. It cannot by itself
close the completion, background, or output arrows, and it is not consumed here.

### 7.2 Value-chain

Any physical value chain requires the complete field objects, not only their
symbol-preservation certificates:

```text
actual history measure or oscillatory functional;
actual interacting contour/boundary-value prescription;
actual joint boundary/contact package;
actual unbounded operators and domains;
physical raw G, inverses, RetHess, restrictions, background, and output maps.
```

No value is computed in this artifact. The structural verdict and any future
value remain explicitly separated.

```text
P_CH_SYMBOL_VERDICT_NEEDS_LESS_THAN_FULL_VALUE_CHAIN = true | TYPE-P |
  premises: dependence-signature/factorization criterion

P_CH_SYMBOL_VERDICT_DECIDABLE_NOW = false | TYPE-U |
  would-build: the minimal verdict-chain receipts above

PHYSICAL_VALUE_CHAIN_READY = false | TYPE-U |
  would-build: complete P3-P6, background, and selected output realizations
```

## 8. Kill-passes

### 8.1 No weakened consumer

The finite mixed block is not renamed `RetHess_phys`, raw `G`, `DeltaPhi`, or a
physical output. Their signatures and carrier changes remain explicit.

### 8.2 No continuity by density alone

P2's dense finite source core determines continuous functions in its own norm
class. It does not determine an untyped completed response. Q-250's tail
countermodel is retained exactly at this boundary.

### 8.3 No domain-formality downgrade

Measure, contour, boundary/contact, and unbounded domains can each change a
physical operator without changing the finite analytic formula. They are not
called formal merely because the requested verdict is symbolic.

### 8.4 No background conflation

`A_delta=0`, zero connection history, zero probe, and zero common background
remain distinct. C1 is not used to select a saddle.

### 8.5 No output-tail inference

The finite response-visible quotient `p_ch` is a finite source-state object,
not an alpha-facing complete response functional. No identity transports its
finite visibility to `DeltaPhi`.

### 8.6 No in-flight result transport

Relay 361 is neither assumed successful nor treated as sealed. Its possible
future output is named only as a control that would remain inside the finite
bottom leg.

## 9. Final flag block

```text
FINITE_CORE_P_CH_TRACE_RUNS = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013, DoR-014, DoR-011
FINITE_CORE_MIXED_RETARDED_P_CH_ABSENCE = true | TYPE-P |
  premises: same

CORE_ONLY_PHYSICAL_P_CH_REDUCTION_THEOREM = false | TYPE-R |
  test: Q-250 tail countermodel and Q-252 background counterchannel

FOUR_P3_FIELDS_MOOT_FOR_PHYSICAL_P_CH_VERDICT = false | TYPE-R
FOUR_P3_FIELDS_FULL_VALUES_REQUIRED_FOR_SYMBOL_ONLY_VERDICT = false | TYPE-R
FOUR_P3_FIELD_SYMBOL_PRESERVATION_CERTIFICATES_BUILT = false | TYPE-U

P5_P6_CLASS_RESTRICTION_PACKAGE_BUILT = false | TYPE-U
P7_OBJECT_SIDE_DISCHARGED = false | TYPE-U
P7_OUTPUT_SIDE_DISCHARGED = false | TYPE-U
PHYSICAL_BACKGROUND_CHANNEL_CLOSED = false | TYPE-U
SELECTED_OUTPUT_P_CH_CONSUMPTION_TYPED = false | TYPE-U

PHYSICAL_P_CH_CANCELLATION = NO_VERDICT
PHYSICAL_P_CH_SURVIVAL = NO_VERDICT

VERDICT_CHAIN_READY = false | TYPE-U
VALUE_CHAIN_READY = false | TYPE-U

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The correct scheduling consequence is narrow. Do not commission full physical
values merely to answer the symbolic question. First build the four
symbol-preservation certificates, P5/P6 transport receipts, the background
uniformity/selection object, and the selected output consumption signature.
The exact finite zero is a valid premise of that proof; it is not the proof's
physical conclusion.
