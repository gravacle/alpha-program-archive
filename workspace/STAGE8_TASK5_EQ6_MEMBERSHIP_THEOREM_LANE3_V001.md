# STAGE 8 TASK 5 / EQ6 — THE MEMBERSHIP THEOREM: `F_actual` CERTIFIED ON ONE TUPLE — LANE 3 V001

Date: 2026-08-05  
Lane: Codex Lane 3  
Task: PASTE 559 / Task 5 / EQ6  
Custody: membership build; review of record remains cross-family

## Lead result

[PROVABLE]

```text
REGISTER_HEAD = Q-486
AXIOM_GUARD = FiniteCoherent_020^V002 = FC1 through FC11 plus FC13
FC12 = STRUCK (M16/N7)

F_ACTUAL_COMPONENT_COUNT = 13
F_ACTUAL_INDEX = I_F
F_ACTUAL_IS_ONE_TUPLE = true

FC1_FC11_FC13_JOINT = PASS
FC10_AMENDED_J1_J15 = PASS
FC11_COMPLETION_V2 = PASS
JOINT_FAILURE_FOUND = none

COMPLETED_OBJECT_IN_F_ACTUAL = none
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
MACHINERY_APPEAL = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

[PROVABLE] Claim-tag convention: every prose paragraph and displayed block inherits the nearest explicit `[PROVABLE]`, `[PART-PROVABLE]`, or `[YOURS]` tag. Every table row has its own tag. `PROVABLE` means derived from the sealed stock and the ratified rows; `PART-PROVABLE` marks a conditional implication whose premise is not adopted or a deliberately retained post-scope statement; `YOURS` marks authored adopted content rather than a derivation from the earlier stock.

## 0. Preflight, custody, and authorities

### 0.1 Access, no-clobber, and register

| Claim | Tag | Result |
|---|---|---|
| The cleanroom, archive workspace, and supervision directory are readable. | PROVABLE | All three mandatory access commands returned entries. |
| The requested output name was absent before construction. | PROVABLE | `STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V001.md` did not exist. |
| The register head is Q-486. | PROVABLE | Q-486 is the last substantive register row before the register's usage footer. |
| Q-486 finding 1 is honored. | PROVABLE | Every assembly citation below names the confirmed `...LANE1_V003.md` / `...LANE1_V004.md` chain, never the killed `...LANE2_V001.md` look-alike. |
| Q-486 finding 2 is honored. | PROVABLE | Every object, arrow, overlap, reader, J2/J7, and counterexample domain named below is displayed as a set, type, or source/codomain declaration. |

### 0.2 Commissioned archive hashes

[PROVABLE] Every commissioned artifact was hashed in the archive workspace before it was read. The full verified values are:

| Alias | Archive object | Verified SHA-256 | Tag |
|---|---|---|---|
| `H_AX` | `STAGE8_TASK5_EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001.md` | `a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b` | PROVABLE |
| `H_ASM3` | `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md` | `a46beae26048a0d093de339363fb8597d362a0101d103cfc06305edabac53f4c` | PROVABLE |
| `H_ASM4` | `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md` | `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` | PROVABLE |
| `H_R3` | `STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001.md` | `1470ae6c0d91698c10ee31a0e3073c9c70e5713d8cbaa7ff43239547ff4bef27` | PROVABLE |
| `H_R4` | `STAGE8_TASK5_EQ6_STAGE1_V004_REVIEW_LANE2_V001.md` | `2709ee3c0e7434d90b79c90522abc78e6aef18f7432b8bba5643632718185bff` | PROVABLE |
| `H_J2` | `STAGE8_TASK5_EQ6_J2_BUILD_LANE2_V001.md` | `7c52c1a8d4e1158097cb6e6ea8cdbb921ff45c78764ae38e1a19c958445748f6` | PROVABLE |
| `H_J2R` | `STAGE8_TASK5_EQ6_J2_REVIEW_LANE1_V001.md` | `afc14d32b0ddcf816b71292074b73e7dc11a90a28aafb0aceee7ae5d52625ca5` | PROVABLE |
| `H_DIA` | `STAGE8_TASK5_EQ6_PARTIAL_JOINT_DIAMONDS_LANE2_V001.md` | `5539b53ddeb208638af314f34e018591e1e6bd93403906e6a5edd46bc34e4766` | PROVABLE |
| `H_DIAR` | `STAGE8_TASK5_EQ6_DIAMONDS_REVIEW_LANE1_V001.md` | `fa3ab255829aa7768a6f9fe35f800c1a25117627e42d22313eb5e6d28b4abd41` | PROVABLE |
| `H_PROJ` | `STAGE8_TASK5_EQ6_PROJECTOR_CERT_V003_LANE1_V001.md` | `0bef9a003abb586666535689e98d7af29e7fc75b81e06fe439397ce2b8ef9100` | PROVABLE |

### 0.3 Ratified amended rows

| Alias | Decision file | Verified SHA-256 | Claim tag and operative content |
|---|---|---|---|
| `H_A3C1` | `DOR_020_A3_J4_RELATIONAL_INCREMENT_GROUPOID_2026-08-04.md` including corrigendum A3-c1 | `579becc33694aa5ee8bde64b793c4c67973f0b5be098fd8ea94434f260327049` | PROVABLE: J4 is the relational increment groupoid with the forced simultaneous action. |
| `H_A4` | `DOR_020_A4_J12_CONTACT_GLUE_STRICT_PUSHOUT_2026-08-04.md` | `5fd95472bd6f0507a371779505fe91e2c3c4657ee3afc664ca563a3743d668a4` | YOURS: J12 is the authored strict signed pushout. |
| `H_A5` | `DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04.md` | `5beccc617f4c5f5f76aba777c664f3dd4393f72257fc850950e2246109a7a424` | YOURS: actual contact is Hodge-Laplacian reducing. |
| `H_A6` | `DOR_020_A6_J2_SCOPED_PROJECTED_LAW_2026-08-05.md` | `202234ac77136592fdc24a96838909f1dad7e8f6a79fdab81342c5f12d6e82cd` | PROVABLE: cycle-creating J2 is the scoped old-image square; the full new-cycle form is post-scope. |

### 0.4 Sealed component stock

[PROVABLE] The following source bytes were independently checked against the sealed hashes recorded by `H_AX` and are used for component-level provenance:

| Alias | Sealed source | Verified SHA-256 | Tag |
|---|---|---|---|
| `H_FIN` | `STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md` | `452f1bb87aeb7f7bfe4ab4556134cf723ab6df697f93c1a59c33671146cb0083` | PROVABLE |
| `H_Q408` | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | PROVABLE |
| `H_SEC` | `STAGE8_TASK5_EQ6_SECTION_DEEPER_CONSTRUCTION_LANE1_V001.md` | `a78c2450985429f6c9e152eb52ab6f133b013e1a1b1d695e2cc5241617b4bdb4` | PROVABLE |
| `H_FLIPR` | `STAGE8_TASK5_EQ6_FLIP_SECTION_CHECK_AND_SCOPED_WITNESS_LANE2_V001.md` | `0e61e6ebe8a7d69390c60f0fa1490c46a866c95e1f4a00ad6ffb7646bec62ee7` | PROVABLE |
| `H_SCOPE` | `STAGE8_TASK5_EQ6_SCOPED_EQUALIZER_COMPLETION_LANE1_V001.md` | `3b721be750f21e78d410f118aadd1235b4c6a86c1ae874cb591136db34ac017d` | PROVABLE |
| `H_FIVE` | `STAGE8_TASK5_EQ6_FIVE_GENERATORS_ON_IFLIP_LANE2_V001.md` | `3ae580d05886bfa0df23442a3d7eaf5a4500c0280229cc615f6998f29a93be8e` | PROVABLE |
| `H_EXC` | `STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md` | `d61a550a33bf1215c35f4d6f27cd2ec5d644b93a05e16d08fe3d43ded3416817` | PROVABLE |

## 1. M1 — exact name and exhibition of `F_actual`

### 1.1 The index category `I_F`, with every arrow domain displayed

[PROVABLE] Define the object set by sealed occurrence, including full covariance orbits rather than chosen representatives:

```text
Obj(I_F)
 := { N : N occurs as an active finite stage in the Q-408, Ref_path,
          flip, or built common-refinement constructions of
          H_Q408, H_SEC, H_FLIPR, H_ASM3, H_ASM4, or H_DIA }
    closed under admitted relabeling, reality, frame-torsor,
    bundle-gauge, and orientation images.
```

[PROVABLE] The morphism generators are the following explicitly displayed classes:

```text
A_iso
 := { f:N->M | N,M in Obj(I_F), f is an admitted actual-surface
                isomorphism, relabeling, reality, orientation, frame,
                or bundle-gauge arrow };

A_RP
 := { f:N->M | N,M in Obj(I_F), f is a rank-preserving W3/DoR-008
                restriction or a rank-preserving Ref_path subdivision };

A_CC
 := { f:N->M | N,M in Obj(I_F), f is a sealed flippable or other
                covered cycle-creating attachment, with the proved
                old-image restriction r_f^Bot:Bot_M^resp->Bot_N^resp };

A_CR
 := { k:X->Y | k is a built disjoint/contact common-refinement leg
                for arrows in A_RP union A_CC };

Mor(I_F)
 := the finite closure of A_iso union A_RP union A_CC union A_CR
    under identities and the sealed admissible compositions.
```

[PROVABLE] The relations of `I_F` are identity and associativity; the proved `Ref_path` subdivision relations; the commuting flip-stabilizer relations; and the actual common-refinement relations `g∘a=h∘f` displayed in Section 3.2. This is the full live finite category used by the confirmed chain; no hard arrow is deleted by intersecting component scopes.

[PROVABLE] For each `f:N->M` in `A_CC`, display the old-image/harmonic J2 domain as

```text
D_f^old
 := { x in Dom(Loc_N) :
        eta_f(x) is in Dom(Loc_M),
        Loc_N(x) and Loc_M(eta_f(x)) lie in the licensed P_H old-image sector }.
```

[PROVABLE] On this domain, both sides of the A6 square have the identical type

```text
r_f^Bot o pi_Mx,M o Loc_M o eta_f : D_f^old -> Bot_N^resp,
pi_Mx,N o Loc_N                   : D_f^old -> Bot_N^resp.
```

[PROVABLE] The target-only factor is displayed, but is not a tuple member:

```text
K_f^new
 := { y in the target cycle/Maxwell carrier at M : r_f^Bot(y)=0 }
    modulo the licensed old-image factor.

K_f^new STATUS = A6_POST_SCOPE_CONDITION_ONLY;
K_f^new in Components(F_actual) = false.
```

[PROVABLE] Thus the two arrow classes relevant to J2 are exactly `A_RP` with full J2 and `A_CC` with the old-image square on `D_f^old`. A full independently constructed `Loc/pi_Mx` pair on `K_f^new` is retained as a post-scope test and is not smuggled into `I_F`'s finite antecedent data.

### 1.2 The one thirteen-component tuple

[PROVABLE] On the single category just displayed, name

```text
F_actual := (
  Adm_fin,
  Kern_Q408^fin,
  C1_core^fin,
  Faith_fin,
  C2_fin,
  C3_fin,
  R2_alg^fin,
  A_fin,
  Where_A1^fin,
  rho_fin,
  j_fin,
  Diamond_fin,
  Scope_component
).
```

[PROVABLE] Every component and its full domain is displayed in this ledger. Repeated hashes are intentional: several coordinates are built jointly in the same sealed source.

| # | Component | Displayed object/arrow domain and content | Sealed source hash(es) | Tag |
|---:|---|---|---|---|
| 1 | `Adm_fin` | For every `N in Obj(I_F)`, the full retained R1 action/Hessian family; for every `f:N->M in Mor(I_F)`, `Rel_f={(I_N,I_M,v_f): I_M=I_N∘rho_f+v_f}` with the A3-c1 simultaneous action. | `H_FIN=452f1bb8…`; `H_ASM3=a46beae2…`; repair `H_ASM4=9bf34e27…` | PROVABLE |
| 2 | `Kern_Q408^fin` | For every `N in Obj(I_F)`, the actual physical current/kernel map and cycle carrier; for every `f in Mor(I_F)`, the built actual restriction/refinement leg, never an abstract isomorphic substitute. | `H_Q408=ccb94dfa…`; carried jointly by `H_ASM3=a46beae2…` | PROVABLE |
| 3 | `C1_core^fin` | For every `N in Obj(I_F)`, the finite Hausdorff seminorm/initial-topology core and attained finite bounds; for every `f in A_iso union A_RP union A_CC union A_CR`, the bounded finite package leg supplied by the one-functor assembly. | `H_FIN=452f1bb8…`; `H_ASM3=a46beae2…`; `H_ASM4=9bf34e27…` | PROVABLE |
| 4 | `Faith_fin` | For every `N in Obj(I_F)`, the finite kernel-separation/quotient-horn certificate; for every `f in Mor(I_F)`, its orbitwise transfer without deleting a record-visible cycle. | `H_FIN=452f1bb8…`; `H_Q408=ccb94dfa…`; `H_ASM3=a46beae2…` | PROVABLE |
| 5 | `C2_fin` | For every `N in Obj(I_F)`, `R_N^phys=(R_N^rep⊕I_N^contact)/{(c,-c):c in C_N}`; for every `f:N->M in Mor(I_F)`, `Eta_f[r,b]=[Eta_f^rep(r),eta_f^boundary(b)]`. | `H_ASM3=a46beae2…`; A4 `H_A4=5fd95472…` | PROVABLE from YOURS A4 |
| 6 | `C3_fin` | For every `N in Obj(I_F)`, finite Ward and Hodge-Maxwell symbol data including finite `d_N,delta_N,Delta_N^Hdg,P_H,N`; for `f in A_RP`, full finite naturality; for `f in A_CC`, only the displayed old-image projected comparison. | `H_Q408=ccb94dfa…`; `H_PROJ=0bef9a00…`; A5 `H_A5=5beccc61…` | PROVABLE from YOURS A5 |
| 7 | `R2_alg^fin` | Domain `D_reader,N:={(chi_N,T_N): p_(chi,T)[Q_N(a,b,C)]=a+chi_N·b+Tr(T_N C) is the normalized finite relation-annihilating algebraic reader}` for each `N`; all members retained and no physical comparison map defined from the reader. | `H_AX=a681c784…`; factor check `H_J2=7c52c1a…` | PROVABLE |
| 8 | `A_fin` | For every `N in Obj(I_F)`, the `h=0` anchored Shape-K finite action family; for every `f in Mor(I_F)`, the same retained relational family coordinate as component 1, with no selected action member. | `H_FIN=452f1bb8…`; `H_ASM4=9bf34e27…` | PROVABLE |
| 9 | `Where_A1^fin` | For every `N in Obj(I_F)`, the actual path/current support, density/coframe, U(1) bundle, and connection members; for each `f in Mor(I_F)`, the built pullback/full-rank/characteristic-class-compatible leg. | `H_SEC=a78c2450…`; `H_FLIPR=0e61e6eb…`; `H_ASM3=a46beae2…` | PROVABLE |
| 10 | `rho_fin` | For every `f:N->M in Mor(I_F)`, all proved downward/restriction legs, including `rho_f`, `rho_f^pkg`, and `r_f^Bot:Bot_M^resp->Bot_N^resp`; no upward new-cycle quotient. | `H_ASM3=a46beae2…`; `H_R3=1470ae6c…`; `H_ASM4=9bf34e27…` | PROVABLE |
| 11 | `j_fin` | For every `f:N->M in Mor(I_F)`, all proved forward current/profile/bundle transports `j_prof,f`, `j_f^H`, `S_f^J`, and `Eta_f` on their explicitly typed finite package coordinates. | `H_Q408=ccb94dfa…`; `H_ASM3=a46beae2…`; `H_ASM4=9bf34e27…` | PROVABLE |
| 12 | `Diamond_fin` | Domain `D_square` of every built common-refinement square in `I_F`, as displayed in Section 3.2; it contains the five finite faces plus the corrected J2/J7 faces and contact cocycle. | `H_DIA=5539b53d…`; correction `H_DIAR=fa3ab255…`; closure `H_J2=7c52c1a…`; review `H_J2R=afc14d32…` | PROVABLE |
| 13 | `Scope_component` | The exact per-coordinate object/arrow domains in this table, especially `A_RP`, `A_CC`, `D_f^old`, and the declaration `K_f^new notin Components(F_actual)`. | `H_AX=a681c784…`; `H_ASM4=9bf34e27…`; A6 `H_A6=202234ac…` | PROVABLE |

[PROVABLE] This thirteen-coordinate expansion is the V002 exhibition tuple after the built repairs. It compresses, without changing witnesses, to the confirmed mixed-variance package

```text
F_actual(N) = (Act_N, P_N, Bot_N, Aux_N),
F_actual(f) = (Rel_f, Eta_f, BotStruct(f;r_f^Bot), Aux_f),

Bot_N = (Gate1-4_N, Q243_N, Q279_N, Q309_N,
         Q408_N, ActionBottom_N, WardBottom_N,
         ReaderBottom_N, WhereBottom_N, C1FaithBottom_N).
```

[PROVABLE] The same `N`, the same `f`, the same component values, and the same package bottom occur in every subsequent FC and J-row check. No row is witnessed on a substitute tuple.

### 1.3 Circularity boundary at the exhibition stage

[PROVABLE] The forbidden completed-output set is explicitly

```text
C_forbidden
 := { I_bar, Q408_bar, C1_bar, Faith_bar, C2_bar, C3_bar,
      ell_bar, A_bar, GRef_bar, Dens_bar, Eta_bar, Bound_bar,
      rho_bar, Hodge_Maxwell_bar, Cof_bar,
      c_J1,...,c_J15, W in Comp_020(F_actual) }.

Components(F_actual) intersect C_forbidden = empty.
```

[PROVABLE] The `P_H,N` in component 6 is a finite-stage projector from `H_PROJ`, not the completed `Hodge_Maxwell_bar`; `R2_alg^fin` is the finite algebraic reader family, not `ell_bar`; and the strict finite pushout `P_N` is not a completed response boundary. Hence no completed object occurs anywhere in `F_actual`.

## 2. M2 — joint certification of FC1–FC11 and FC13

### 2.1 The guard of record

[PROVABLE] From `H_AX`, the V002 guard is exactly

```text
FiniteCoherent_020^V002(F)
 := FC1(F) and FC2(F) and FC3(F) and FC4(F) and FC5(F)
    and FC6(F) and FC7(F) and FC8(F) and FC9(F) and FC10(F)
    and FC11(F) and FC13(F).

FC12 is absent from the conjunction.
```

### 2.2 FC ledger on the one tuple

| FC | Joint discharge on the displayed `F_actual` | Source | Tag / verdict |
|---|---|---|---|
| FC1 actuality | Components 2 and 9 use the physical Q-408 map, actual paths/currents, adopted surface, bundle, and connection objects at every `N in Obj(I_F)`; no abstract kernel appears. | `H_Q408`, `H_SEC`, `H_FLIPR`, `H_AX` | PROVABLE — PASS |
| FC2 finite bottom | The single ten-entry `Bot_N` above reproduces Gate 1–4, Q-243, Q-279, Q-309, Q-408, action, Ward, reader, WHERE, and C1/Faith shadows; the response coordinate obeys the exact J15 mate and other coordinates keep their sealed variances. | `H_ASM3` §§2,5,7; `H_R3` H3/H5; carried by `H_ASM4` | PROVABLE — PASS |
| FC3 functoriality | On every `f,g,gf in Mor(I_F)`, the one mixed-variance package has identity and composition. The repaired relation equality, pushout composition, bottom contravariance, and common-refinement equations are displayed below. | `H_ASM4` §§2–4; `H_R4` R1–R3; `H_DIA`/`H_DIAR` | PROVABLE — PASS |
| FC4 contravariant honesty | For `f in A_CC`, the only bottom comparison is `r_f^Bot:Bot_M^resp->Bot_N^resp`; target-only `K_f^new` survives at `M` and no map `Bot_N^resp->Bot_M^resp` is installed. | `H_ASM3` §5; `H_R3` H3; `H_A6` | PROVABLE — PASS |
| FC5 covariance | The corrected full-family action `v_f'=v_f+psi_M-psi_N∘rho_f` cancels intermediate changes; full relabeling, reality, orientation, batching, frame, bundle-gauge, and W3 orbits are retained with no selected representative. | `H_ASM4` §2; `H_R4` R1/R3 | PROVABLE — PASS |
| FC6 surface support | On every `f in Mor(I_F)`, `OLD_FID`, RNL, LR, and local orthogonal excision hold on the component domains in Section 1.2; the pushout identifies only the actual overlap and cannot mix disjoint new-cycle sectors. | `H_ASM3` §§4,8; `H_EXC`; `H_DIA` D1 | PROVABLE — PASS |
| FC7 bundle discipline | On every `f:N->M in Mor(I_F)`, the U(1) lift, pullback-bundle isomorphism, smooth full-rank condition, characteristic-class restriction, and density/coframe units are the `Where_A1^fin` coordinate. | `H_SEC`, `H_FLIPR`, `H_ASM3` Aux | PROVABLE — PASS |
| FC8 analytic core | For every `N in Obj(I_F)`, `C1_core^fin(N)` is finite/Hausdorff on the physical quotient; every declared map is bounded on its displayed finite domain and uses the adopted `tau_ker`/DoR-019/R4 unit seam, with no global uniform bound assumed. | `H_FIN`, `H_ASM3` §§5.4,7 | PROVABLE — PASS |
| FC9 finite-kernel honesty | `Kern_Q408^fin` supplies the faithful/quotient horn; the A4 pushout injections are injective, so neither a visible represented bulk cycle nor a target-only new cycle is deleted. | `H_Q408`; `H_ASM3` §§4.4,6.2 | PROVABLE — PASS |
| FC10 joint compatibility | The amended finite J1–J15 rows all hold on this same thirteen-coordinate tuple. The fifteen-row walk is Section 2.4. | `H_ASM3`, `H_ASM4`, `H_PROJ`, `H_J2`, `H_J2R`, `H_A3C1`–`H_A6` | PROVABLE — PASS |
| FC11 equalizer coherence | The five confirmed finite faces on every built common-refinement domain plus the now-built corrected J2/J7 faces satisfy `COMPLETION_THEOREM_V2`; the full six-generator finite fiber-product term, including contact cocycle, follows. | `H_DIA`, `H_DIAR`, `H_J2` K3/K3b, `H_J2R` Z4/Z5 | PROVABLE — PASS |
| FC12 | Struck by V002; no return-domain predicate is evaluated as part of membership. | `H_AX` Part A | PROVABLE — STRUCK / NOT IN GUARD |
| FC13 target blindness | Every component source and every map precedes completed response, threshold, fixed point, end test, numeric result, or measured constant; Section 5.1 reruns the construction-order ledger. | all sources above | PROVABLE — PASS |

### 2.3 Joint functor and bottom checks actually run

[PROVABLE] For `f:N->M`, `g:M->L` in `Mor(I_F)`, the action coordinate satisfies

```text
v_gf = v_f o rho_g + v_g,
Rel_g o Rel_f = Rel_gf,
v_f' = v_f + psi_M - psi_N o rho_f,
v_f' o rho_g + v_g' = v_gf'.
```

[PROVABLE] The response and bottom coordinates on the same arrows satisfy

```text
Eta_gf = Eta_g o Eta_f,
r_gf^Bot = r_f^Bot o r_g^Bot,
r_f^Bot o pi_M^resp o Eta_f = pi_N^resp.
```

[PROVABLE] Therefore the exact joint package equations are

```text
F_actual(id_N) = id_(F_actual(N)),
F_actual(gf)   = F_actual(g) o F_actual(f)
                 in the declared mixed-variance category FinPkg_020,

rho_N^pkg o F_actual(f) = Bot(f) o rho_M^pkg
coordinatewise on the single Bot tuple.
```

[PROVABLE] These equations use the repaired J4 implementation from `H_ASM4/H_R4` and the J12/J15 faces confirmed in `H_R3`. They do not use the killed `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md` pointer that appears in `H_DIA`.

### 2.4 FC10: all fifteen amended rows on this tuple

| Row | Explicit domain and equality/law on `F_actual` | Source | Tag / verdict |
|---|---|---|---|
| J1 | Domain `D_reader,N` displayed in component 7 for every `N in Obj(I_F)`; the normalized finite algebraic reader family exists and all `(chi_N,T_N)` members are retained. | `H_AX`; `H_J2` K2 | PROVABLE — PASS |
| J2 | Domain `D_J2^RP:={(f,x):f in A_RP, x in Dom(Q_N)}` carries the full composite `reader_f=pi_Mx,F∘Loc_F∘Kernbar_F∘Q_F`. Domain `D_J2^CC:={(f,x):f in A_CC, x in D_f^old}` carries exactly the A6 square. `K_f^new` is post-scope and not a tuple member. | `H_PROJ`; `H_A6`; `H_J2` K1/K2; `H_J2R` Z1/Z2 | PROVABLE — PASS |
| J3 | Domain `D_reader,N` and every `f in Mor(I_F)`; covariance, reality, restriction, and full-family retention hold for the algebraic reader coordinate. | `H_AX`; `H_ASM3` Aux; `H_ASM4` | PROVABLE — PASS |
| J4 | Domain `Rel_f` for every `f:N->M in Mor(I_F)`; `I_M=I_N∘rho_f+v_f`, `v_gf=v_f∘rho_g+v_g`, and simultaneous action `v_f'=v_f+psi_M-psi_N∘rho_f`. | `H_A3C1`; `H_ASM4` §§2–3; `H_R4` R1/R2 | PROVABLE — PASS |
| J5 | Domain `{(N,I_N):N in Obj(I_F), I_N in A_fin(N)}`; `h=0` anchors every retained finite Shape-K action member. | `H_FIN`; `H_AX`; carried by `H_ASM3` Aux | PROVABLE — PASS |
| J6 | Same domain as J5; zero addition preserves the Shape-K scope and chooses no member. | `H_FIN`; `H_AX` | PROVABLE — PASS |
| J7 | Domain `D_J7^CC:=D_J2^CC`; the corrected comparison face is the same A6 map equality `r_f^Bot∘pi_Mx,M∘Loc_M∘eta_f=pi_Mx,N∘Loc_N`. The full new-cycle `rho∘Delta` content is a post-scope condition. | `H_DIAR` U3/U4; `H_J2` K3/K3b; `H_J2R` Z4 | PROVABLE — PASS |
| J8 | Domain every mixed-sector finite expression in components 5–7; only the declared R4 unit seam is used, with no cross-sector unit conversion. | `H_ASM3` §§4.3,6.2 | PROVABLE — PASS |
| J9 | Domain every ordered batching arrow in `A_iso subset Mor(I_F)`; batching is covariant and order-preserving componentwise. | `H_AX`; `H_ASM3` Aux | PROVABLE — PASS |
| J10 | Domain all composable pairs `(f,g)` with `f:N->M`, `g:M->L` in `Mor(I_F)`; the one package satisfies the identity/composition equations in Section 2.3. | `H_ASM4`; `H_R4`; `H_DIA/H_DIAR` | PROVABLE — PASS |
| J11 | Domain `Where_A1^fin(N)` for every `N` and its arrow transports; actual current/density normalization and U(1) bundle/coframe units are preserved. | `H_SEC`; `H_FLIPR`; `H_ASM3` Aux | PROVABLE — PASS |
| J12 | Domain `P_N=(R_N^rep⊕I_N^contact)/Delta_N`, `Delta_N={(c,-c):c in C_N}`; the authored A4 overlap law makes `Eta_f[r,b]=[Eta_f^rep(r),eta_f^boundary(b)]` the unique strict-pushout map. | `H_A4`; `H_ASM3` §4; `H_R3` H2 | PROVABLE from YOURS A4 — PASS |
| J13 | Domain every `[r,b] in P_N` and composable `f,g in Mor(I_F)`; composition, beta cocycle, restriction, reality, unit, Ward, support, OLD_FID, RNL, LR, and subextensivity descend through the same pushout. | `H_ASM3` §§4.2–4.3; `H_DIA` D1 | PROVABLE — PASS |
| J14 | Domain every `N in Obj(I_F)` and the displayed finite/orbitwise component domains; finite bounds and finite faithfulness hold without claiming the completed horn or a global bound. | `H_FIN`; `H_Q408`; `H_ASM3` §§5.4,7 | PROVABLE — PASS |
| J15 | Domain every `x in P_N` and `f:N->M in Mor(I_F)`; the contravariant mate is `r_f^Bot∘pi_M^resp∘Eta_f(x)=pi_N^resp(x)`. No covariant bottom lift exists. | `H_ASM3` §5; `H_R3` H3; carried unchanged by `H_ASM4` | PROVABLE — PASS |

[PROVABLE] The walk uses the amended rows as law: A3-c1 for J4, A4 for J12, A6 for J2/J7, and the contravariant mate for J15. A5 supplies the finite contact-reducing Hodge projector used in the J2 factor chain. No row is borrowed from another tuple.

### 2.5 FC11: overlap domains, corrected contract, and completion

[PROVABLE] A built common-refinement diamond has the displayed domain

```text
      a
  n -------> b
  |          |
f |          | g
  v          v
  c -------> d
      h

n,b,c,d in Obj(I_F),
a,f,g,h in Mor(I_F),
g o a = h o f.

D_square := F_actual(n).
```

[PROVABLE] If a cycle-creating leg occurs, the shared scoped J2/J7 domain is explicitly

```text
D_square^old
 := D_a^old
    intersect eta_a^(-1)(D_g^old)
    intersect D_f^old
    intersect eta_f^(-1)(D_h^old),
```

with terms omitted from the intersection when their leg is not in `A_CC`. Every corrected J2/J7 route is therefore a map `D_square^old -> Bot_n^resp`; no equality is asserted on a target-only new-cycle factor.

[PROVABLE] The five confirmed face equations on `D_square` are

```text
F_actual(g) F_actual(a) = F_actual(ga)
                         = F_actual(hf)
                         = F_actual(h) F_actual(f),

Rel_g Rel_a = Rel_ga = Rel_hf = Rel_h Rel_f,
Eta_g Eta_a = Eta_ga = Eta_hf = Eta_h Eta_f,
r_a^Bot r_g^Bot = r_f^Bot r_h^Bot,
rho_n^pkg F_actual(a/f route) = Bot(a/f route) rho_(b/c)^pkg,

beta_ga = beta_g + Eta_g(beta_a)
        = beta_h + Eta_h(beta_f)
        = beta_hf
```

where the last line is the contact cocycle through the A4 pushout. These are the finite functor, package-bottom, J4, J12, and J15 faces of `H_DIA`, independently recomputed in `H_DIAR` and confirmed at Q-486.

[PROVABLE] Contract re-check: `H_DIAR` corrected the original D2 over-demand to

```text
r_k^Bot o pi_Mx,target(k) o Loc_target(k) o eta_k
  = pi_Mx,source(k) o Loc_source(k)
```

for each cycle-creating leg `k` on `D_k^old`. `H_A6` states that identical equation. `H_J2` K3 uses that identical equation as the J7 face, and K3b supplies the J2 face as full on `A_RP` and scoped on `A_CC`. Thus source, codomain, factors, variance, and scope all match; no completion object or hidden lift is needed.

[PROVABLE] Applying `COMPLETION_THEOREM_V2` from `H_J2` K3b to the five confirmed faces of `H_DIA`, with the now-built J2 and J7 faces, yields the full FC11 fiber-product term on `D_square` and `D_square^old`. `H_J2R` Z4 finds no hidden simultaneity condition beyond precisely this scope match.

[PROVABLE] The citation correction is substantive and explicit: the five-face input is consumed through confirmed `H_ASM3=a46beae2…` plus repaired `H_ASM4=9bf34e27…` and their reviews `H_R3/H_R4`. The killed `...JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md` is not an authority for this proof.

## 3. M3 — the membership theorem

### 3.1 Exact statement

[PROVABLE]

```text
THEOREM (F_actual membership).

For the single thirteen-component tuple F_actual over I_F displayed in
Section 1, with the amended A3-c1/A4/A5/A6 rows operative,

FiniteCoherent_020^V002(F_actual).
```

### 3.2 Proof

[PROVABLE] FC1, FC6, FC7, FC8, and FC9 hold componentwise on their displayed domains and jointly because those components are coordinates of the same `F_actual(N)` and `F_actual(f)`. FC2 supplies one package-wide bottom. FC3 supplies one mixed-variance functor. FC4 supplies only lawful downward old-image restrictions. FC5 supplies the whole corrected covariance family. Section 2.4 proves all amended J1–J15 rows on that same tuple, hence FC10. Section 2.5 applies the corrected-contract completion theorem to the same faces on the same common-refinement domains, hence FC11. The construction order and the absence of completed objects give FC13. FC12 is struck and is absent from the V002 conjunction. Therefore every conjunct in the exact guard of Section 2.1 is true, proving the theorem.

### 3.3 What the theorem licenses

[PROVABLE] The proposed V002 axiom has the exact implication

```text
for every F,
  FiniteCoherent_020^V002(F)
    -> NonemptyCovariantFamily(
         { W in Comp_020(F) :
             Res_fin(W)=F
             and Eq_J1-J15(W)
             and SixGeneratorScope(W) }).
```

[PART-PROVABLE] Instantiating the universally quantified `F` with the now-qualified `F_actual` is valid. If DoR-020-A2 adopts this axiom, its consequent supplies a nonempty proof-carrying completed covariant family whose exact finite reduct is this one tuple. This would discharge Step 1 non-vacuously for the live tower. Adoption has not occurred here, so no completed member is asserted, selected, or bound by this artifact.

[PART-PROVABLE] The post-scope new-cycle `Loc/pi_Mx` construction retained by A6 is not a missing antecedent component. If independently constructed later, it becomes a falsifier/test of the retained full J2 condition; it does not block the membership theorem under the amended row.

## 4. M4 — honesty clause and joint-failure audit

[PROVABLE] The stopping rule was applied condition-by-condition and overlap-by-overlap. The resulting audit is:

| Joint site | Explicit test domain | Recomputed result | Tag |
|---|---|---|---|
| rank-preserving composition / `Ref_path` | `{(f,g,x):f,g,gf in A_RP, x in F_actual(source(f))}` | functor, relation, response, bottom, full J2, and contact routes agree | PROVABLE — PASS |
| disjoint cycle-creating flips | `{(a,f,g,h,x):a,f,g,h in A_CC, ga=hf, x in D_square^old}` | commuting flip faces, scoped J2/J7, old-image bottom, and zero/contact defects agree | PROVABLE — PASS |
| contact common refinement | `{(a,f,g,h,x):ga=hf, x in D_square, contact overlap C_source != empty}` | A4 pushout identifies only the common class; beta/J12/J15 routes agree | PROVABLE from YOURS A4 — PASS |
| mixed rank-preserving / cycle-creating square | `{(a,f,g,h,x):ga=hf, legs in A_RP union A_CC, x in D_square^old}` | full J2 is used only on rank-preserving legs and the identical A6 square on cycle-creating legs; codomains match at `Bot_n^resp` | PROVABLE — PASS |
| full six-generator completion | `D_square` for finite faces and `D_square^old` for scoped cycle faces | five confirmed faces plus built J2/J7 satisfy `COMPLETION_THEOREM_V2` | PROVABLE — PASS |

[PROVABLE]

```text
ANY_FC_FAILURE = none
ANY_DIAMOND_OVERLAP_FAILURE = none
CONTRACT_MISMATCH = none after A6 correction
JOINT_FAILURE_FOUND = none
```

[PROVABLE] Because no first-order joint failure was found, the honesty clause does not trigger a failed-membership stop.

## 5. M5 — battery

### 5.1 Anti-tuning ledger rerun

| Order | Construction or check | Downstream datum consulted? | Tag / verdict |
|---:|---|---|---|
| 1 | Freeze DoR-008, DoR-019, DoR-020+A1, amended A3-c1/A4/A5/A6, J1–J15, and counterexamples. | none | PROVABLE — PASS |
| 2 | Freeze `Obj(I_F)`, `Mor(I_F)`, actual Q-408 carriers, surface/bundle anchors, and all covariance orbits. | none | PROVABLE — PASS |
| 3 | Assemble the thirteen finite coordinates and one package bottom. | none | PROVABLE — PASS |
| 4 | Build J4 relation, A4 pushout, J15 mate, finite projector factors, A6 J2/J7, and common-refinement faces. | no response, threshold, fixed point, end test, numeric value, or measured constant | PROVABLE — PASS |
| 5 | Define and prove `FiniteCoherent_020^V002(F_actual)`. | no completed output `W` | PROVABLE — PASS |
| 6 | Only after membership, state the axiom-on-adoption consequence. | no member selected or executed | PROVABLE — PASS |

[PROVABLE]

```text
ANTI_TUNING = PASS
TARGET_OUTPUT_USED_TO_DEFINE_COMPONENT = none
RESPONSE_OR_THRESHOLD_USED_TO_CHOOSE_MAP = none
```

### 5.2 Circularity falsifier rerun

[PROVABLE] The search domain is the exact component list `Components(F_actual)={the thirteen names in Section 1.2}` and the forbidden domain is `C_forbidden` from Section 1.3. Their intersection is empty. In particular:

```text
pi_Mx is sourced from finite Hodge/projector stock, not defined from reader ell;
Loc is sourced before the reader comparison;
Diamond_fin is finite and invokes no W in Comp_020(F_actual);
no completed carrier, cofinal element, completed response boundary,
completed Hodge-Maxwell object, or completed J certificate occurs in F_actual.
```

[PROVABLE] Therefore the circular `F_PLDEC` repair and the broader completed-object circularity falsifier both return PASS.

### 5.3 Nine geometric regressions on the assembled tuple

| # | Regression and displayed test domain | Joint result | Tag / verdict |
|---:|---|---|---|
| 1 | `D_new:={(f,y):f in A_CC, y in K_f^new, y!=0}` | `y` remains nonzero at the target while `r_f^Bot(y)=0`; no physical target cycle is deleted. | PROVABLE — PASS |
| 2 | `D_orbit:={(N,x,u):N in Obj(I_F), x in F_actual(N), u an admitted relabeling/reality/orientation/frame/bundle automorphism}` | the full orbit `u·x` is retained; no representative or member is chosen. | PROVABLE — PASS |
| 3 | `D_skel:={(f,g,x):f,g,gf in Mor(I_F), x in F_actual(source(f))}` | identity and composition hold for the one finite skeleton; it is not mislabeled a completed realization. | PROVABLE — PASS |
| 4 | `D_RP:={(f,H):f in A_RP, H in Dom(Kernbar_source(f))}` | `v_f=0` on the identity-extension subcase and `Eta_f^rep Kernbar_N(H)=Kernbar_M(j_prof,f H)`; exact old restriction is reproduced. | PROVABLE — PASS |
| 5 | `D_no_up:={f:f in A_CC}` | only `r_f^Bot:Bot_M^resp->Bot_N^resp` exists; no upward response quotient/lift is created. | PROVABLE — PASS |
| 6 | `D_prof:={(f,H):f in Mor(I_F), H in Dom(Kernbar_source(f))}` | represented transport is defined by the physical `Kernbar` equation, never by an adjoint-compression stand-in; target kernel survives. | PROVABLE — PASS |
| 7 | `D_mix:={(S1,S2,P_mix):H=H(S1) direct_sum H(S2), 1_(S2)P_mix1_(S1)!=0}` | the old-to-new sector mixer fails RNL/LR and FC6. | PROVABLE — PASS |
| 8 | `D_id:={(f,P):f in A_RP, P=id, relative no-leakage holds}` | `P=id` is admitted; inherited cross terms are not banned merely by form. | PROVABLE — PASS |
| 9 | `D_bundle:={(f,L_N,L_M):f:N->M in Mor(I_F), L_N,L_M are the actual U(1) bundles in Where_A1^fin}` | pullback isomorphism, smooth full rank, unit class, connection, density/coframe, and characteristic-class restrictions commute; mismatches are rejected by FC7. | PROVABLE — PASS |

### 5.4 Three permanent counterexamples against the V002 guard

[PROVABLE] Disconnected-domain test domain and map:

```text
D_Q448 := {-1,1},
b:D_Q448->D_Q448,
b(K)=K^3/2-3K/2,
b(-1)=1,
b(1)=-1,
b'(-1)=b'(1)=0,
Fix(b|D_Q448)=empty.
```

[PROVABLE] V002 has no FC12 conjunct and `F_actual` carries no stationary-return domain. Therefore the completed-existence guard is silent on `D_Q448`; the disconnected branch is excluded only when the independent `C_ret` certificate is attempted at Steps 3–4. The guard itself does not exclude it.

[PROVABLE] Sector-mixer test domain:

```text
H_mix := H(S1) direct_sum H(S2),
P_mix:H_mix->H_mix,
1_(S2) P_mix 1_(S1) != 0.
```

[PROVABLE] Even if a rail square commutes, the displayed nonzero cross-sector block violates RNL/local range on the physical surface; hence the mixer fails FC6 and is not a member.

[PROVABLE] Abstract-kernel test domain:

```text
K_abs:H_abs->R_abs,
K_abs is abstractly isomorphic to a finite Q-408 kernel,
K_abs is not the extensional physical Kern_Q408^fin on the ratified
incidence/cycle/refinement surface and carries no actual Q-408 Ward anchors.
```

[PROVABLE] `K_abs` fails FC1 actuality even if its abstract dimensions and categorical diagrams match. It cannot replace component 2.

[PROVABLE]

```text
DISCONNECTED_DOMAIN = GUARD_SILENT / C_RET_STEPS_3-4_ONLY
SECTOR_MIXER = EXCLUDED_BY_FC6
ABSTRACT_KERNEL = EXCLUDED_BY_FC1
BATTERY = PASS
```

## 6. Fence and stopping board

[PROVABLE]

```text
AXIOM_V002 = PROPOSED_NOT_ADOPTED
FINITE_MEMBERSHIP_THEOREM = PROVEN
COMPLETED_FAMILY_ASSERTED = false
MEMBER_SELECTION = none
FIXED_POINT_EXECUTION = none
END_TEST = none
NUMERIC_EVALUATION = none
MEASURED_CONSTANT_COMPARISON = none
FENCE_BLOCKED_STRUCTURAL_RESULT = false
MACHINERY_APPEAL = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

F_ACTUAL = NAMED (+13 components)
MEMBERSHIP = PROVEN (+FiniteCoherent_020^V002 on the one tuple; +no failing condition or overlap witness)
STEP1_DISCHARGE_READY = yes_pending_axiom_adoption
