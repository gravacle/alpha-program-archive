# STAGE 8 — AUDIT OF THE OBSTRUCTION-ORIGIN TRACE — O6SR AUDIT V001

## AUDITOR — CODENAME ORIGIN-AUDIT — COMMISSION O6SR — 2026-08-15

DEFAULT-REFUTE. TESTIMONY ZERO WEIGHT — every claim relied on below was
re-derived at bytes or re-run in a fresh CAS. DETERMINATION ONLY. No authored
physics, no value, no number, and no measured-constant comparison is produced
for any program quantity. FENCES HELD: `alpha_computed = false`;
`proof_authorized = false`; `kappa_record_computed = false`.

---

## 0. STEP 0 — PROBE AND SEAL, BEFORE ANY RELIANCE

```text
TARGET PRESENT:  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md          92311 bytes
SIDECAR PRESENT: STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md.seal.sha256
VERIFY, RUN FROM THE ARTIFACT'S OWN DIRECTORY:
  shasum -a 256 -c STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md.seal.sha256
  => STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md: OK
  digest a04c446aa1406fb89cb732fcf8fc6d777e3414cf0fa1d6761702dedd5dd7d5a3
OUTPUT PATH PROBED ABSENT BEFORE ANY WRITE: confirmed absent.
```

---

## 1. VERDICT

```text
OVERALL: **CONFIRMED-WITH-CORRECTIONS.**

PER DELIVERABLE:
  D1  each failure point's blocking mechanism, isolated        CONFIRMED
  D2  the origin trace, counterfactuals executed               CONFIRMED-WITH-CORRECTIONS
  D3  the convergence test / the shared origin named           CONFIRMED-WITH-CORRECTIONS
  D4  the consequence reading                                  CONFIRMED-WITH-CORRECTIONS
  (aux) the SS4.1 token census                                 CONFIRMED-WITH-CORRECTIONS
  (aux) fences / TOY_SEPARATION / CHOICE LEDGER                CONFIRMED

THE COMMISSIONED QUESTION IS ANSWERED, AND THE ANSWER SURVIVES: the
boundary-closure's binding obstructions DO NOT come from gauge/EM structure.
That negative rests on ground this audit re-derived independently and could not
break (SS4 below).

THE CORRECTIONS RUN IN BOTH DIRECTIONS. One STRENGTHENS the negative: the
target's single GAUGE-ORIGINATED attribution (W-2's H-3) is NOT SUPPORTED AT
BYTES and must be withdrawn or re-typed (F-1). Two WEAKEN the positive: the
shared origin as NAMED is FALSE on the target's own displayed table, and the
CAS check that would have caught it omits the failing term (F-2, F-3).
NOTHING HERE IS REFUTED OUTRIGHT. No verdict of the target is reversed.
```

---

## 2. GROUND RE-VERIFIED INDEPENDENTLY; SWEEP CUTOFF DECLARED

Every seal below was verified with `shasum -a 256 -c` **run from the artifact's
own directory**, and every digest recomputed this session. All fourteen carriers
the target relies on returned `OK`, and all fourteen digests AGREE byte-for-byte
with the target's SS1 table. No carrier was unverifiable; no digest was
mis-transcribed.

```text
VERIFIED OK + DIGEST AGREES WITH TARGET SS1 (14 of 14):
  W3 241262ff | W3-A d05f22be | W2 eacd810c | W2-A d95f4285 | MAP afc3a79c
  MAP-A 4504cd14 | MAXSYM 70dde1c0 | KERCALC ccb94dfa | WALL 80db260f
  CERT e6838ffc | FHB 9a295498 | CL-ERROR 3b9730e0 | BL2* 6e81ae92
  RL2B-R 99da8628
NOTE, IN THE TARGET'S FAVOUR: the target digested the six "further carriers"
  but did not claim to verify their sidecars. This audit verified all six
  sidecars as well; all OK. The target's SS1 is conservative, not overstated.
```

```text
SWEEP CUTOFF: 2026-08-15, this session. Roots swept:
  (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace          (primary)
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
SWEEP TERMS, DECLARED AND SCOPED: the target's own seven gauge/EM tokens
  ("Ward" | "gauge" | "Maxwell" | "U(1)" | "electromagnet" | "photon" |
  "connection") re-counted at word boundary as OCCURRENCES (not lines) over the
  ten carriers of the target's SS4.1 table PLUS the two the table omits (W2,
  W2-A); plus "spinor" and "rank <= 4" in WALL for the FP-1 typing question;
  plus "lens" over the target itself. Reads beyond the named sections were
  confined to the carriers those terms returned.
EXCLUDED BY COMMISSION, NOT READ: every register / tracker / road / plan /
  continuation / ledger / lens file. "Q-..." EXPECTED-UNLOCATABLE; none resolved.
THIS SWEEP'S NEGATIVES ARE BOUNDED BY THESE TERMS AND SAY SO.
```

---

## 3. FINDINGS, HARDEST FIRST

### F-1 — THE ONE GAUGE-ORIGINATED ATTRIBUTION IS NOT SUPPORTED AT BYTES

**CLAIM AUDITED (target SS3.2 H-3, SS4.5, SS5.4, SS8, FLAG BLOCK):** W-2's H-3
section-gauge halt is **GAUGE-ORIGINATED** — "THE ONLY GAUGE-ORIGINATED BLOCKER
IN THE ENTIRE INVENTORY", "the one blocker in the entire inventory that a
non-gauge content class would not have."

**THE STANDARD APPLIED (commission O6SR audit):** every gauge-originated
attribution must carry an explicit display that the blocking property FAILS for
a non-gauge content class; an attribution resting only on the blocker appearing
in a gauge-carrying artifact is killed.

```text
WHAT THE BYTES ACTUALLY SUPPLY — W-2, re-read at source (eacd810c, OK):
  W2 :442-451  "H-3 THE GAUGE HALT (residual, non-binding but displayed) ...
     the value CE-1 produces is gauge-dependent on an authored datum: `sect_N`
     is 'an authored gauge of the construction: distinct admissible sections
     are DISTINCT CANDIDATES; no canonical section is claimed' (premise SS3.3)"
  W-2's OWN TOKEN COUNTS, re-counted this session at word boundary:
     gauge = 9, Ward = 1, Maxwell = 0, U(1) = 0, electromagnet = 0,
     photon = 0, connection = 0.
  ALL NINE "gauge" HITS ARE THE SAME OBJECT: "THE GAUGE HALT" (:442),
     "gauge-dependent" (:444), "an authored gauge of the construction" (:444),
     "H-3 gauge residual" (:564, :848), "SECTION-GAUGE HALT" (:721, :740),
     "GAUGE HALT" (:607). NOT ONE names a gauge GROUP, a gauge TRANSFORMATION,
     a connection, a U(1) phase, a Maxwell symbol, or a Ward identity.
  THE ONE "Ward" IS THE NAMED OBJECT "the Ward symbol `s`" (:292) and the row
     label WARD_SYMBOL_MAP_JII (:114) — the CARRIERS-NEEDED / absence side.

WHAT THE BLOCKING PROPERTY IS, AT BYTES: the NON-CANONICITY OF AN UNFORCED
  CHOICE AMONG ADMISSIBLE REPRESENTATIVES. That is the whole content of
  "distinct admissible sections are DISTINCT CANDIDATES; no canonical section
  is claimed."

WHY THE ATTRIBUTION FAILS THE STANDARD:
  (a) THE COUNTERFACTUAL OFFERED IS ANALYTIC, NOT A DISPLAY. The target's
      counterfactual reads, in full: "for a content class carrying no gauge
      freedom — no section to choose — H-3 WOULD SIMPLY NOT EXIST." That is
      true by the naming alone: an object with no section has no
      section-choice. It does not test whether the BLOCKING PROPERTY
      (non-canonicity of an unforced choice) fails for a non-gauge class.
  (b) ITS CAS BACKING IS A LITERAL `True`. O6-7f's condition is the constant
      `True` (target SS9.1). It verifies nothing. O6-7e verifies only an
      EQUALITY (see F-4).
  (c) THE PROPERTY DEMONSTRABLY DOES NOT FAIL FOR NON-GAUGE CONTENT — BY THE
      TARGET'S OWN LEDGER. H-2 is also a non-canonicity ("OVERLAPPING, not
      IDENTICAL ... forming the common cell would BE new authorship") and the
      target classifies H-2 **GENERIC**. The two halts differ in the WORD
      their carrier uses for the unforced choice, not in the property.
  (d) WHAT REMAINS IS EXACTLY WHAT THE STANDARD KILLS: the attribution rests
      on the token "gauge" occurring inside a carrier that is gauge-carrying
      on other grounds (the Ward symbol map). The target's own supporting
      inference — SS4.5(iii), that a line integral of a 1-form with a
      contact-kernel obligation is "connection-like data" — carries its own
      hedge ("-like") and is nowhere a record typing.
  (e) THE SCOPE SENTENCE THAT WOULD RESCUE IT IS TESTIMONY. The target reports
      the commission as scoping "contact/Hodge structure" INTO gauge/EM
      (SS4 preamble). The commission is not a sealed artifact in either root
      and carries zero weight here. On bytes alone the rescue is unavailable.

CORRECTION REQUIRED: H-3's origin must be re-typed **NON-CANONICITY (CHOICE)**,
  with the displayed fact that ITS CARRIER NAMES THE CHOICE "an authored gauge"
  — carried as the carrier's word, not as a derived gauge origin. The honest
  line: NO BLOCKER IN THE INVENTORY IS DISPLAYED GAUGE-ORIGINATED AT BYTES.

DIRECTION OF THE CORRECTION: it STRENGTHENS the target's answer to the
  commissioned question — with H-3 re-typed, the inventory is 9 GENERIC / 0
  gauge-originated, and the verdict word moves from MIXED to GENERIC. The
  target ALREADY DISPLAYS the neighbouring alternative at CH-4 ("GENERIC at
  every binding blocker; one non-binding gauge-originated blocker of record")
  and invites the registrar to take it. This audit's correction goes one step
  further than CH-4 offers: not merely non-binding, but not displayed
  gauge-originated at all.
```

### F-2 — THE SHARED ORIGIN AS NAMED IS FALSE ON THE TARGET'S OWN TABLE

**CLAIM AUDITED (target SS0, SS5.3, SS8, FLAG BLOCK `SHARED_ORIGIN_NAMED`):**
"**EVERY CERTIFIED ASSET OF THE CLOSURE HAS DEGREE >= 0 IN THE LOCALIZATION
VARIABLE ITS OWN OBSTRUCTION IS GRADED IN**, WHILE EVERY OBSTRUCTION'S
REQUIREMENT IS STRICTLY ON THE LOCALIZED SIDE."

```text
THE TARGET'S OWN SS5.1 TABLE, TRANSCRIBED VERBATIM:
   POINT   ASSET'S DEGREE        REQUIREMENT          SHORTFALL
   FP-1     0                    beat n^{3/2}          3/2
   FP-2    -1 ("degree -1 at     strict < -3           2
             the stay strata")
   FP-3     0                    <= -2 per stratum     2

THE ROW THAT REFUTES THE UNIVERSAL: FP-2's ASSET DEGREE IS **-1**, WHICH IS NOT
   >= 0. The sentence "every certified asset ... has degree >= 0" is FALSE on
   one of its own three rows — and FP-2 is not a marginal row: it is the point
   the target itself calls "where the record's actual gauge/EM content lives,
   so it is the sharpest test available."
   [AUDIT CAS A2a, PASS: asset_fp2 = -1 and NOT (asset_fp2 >= 0).]

AND THE GLOSS FAILS WITH IT. SS5.3 reads the property as "the assets are
   homogeneous / uniform / invariant precisely where the obstructions are
   graded" and SS8 as "LOCALIZATION-BLIND". A degree -1 symbol is NOT invariant,
   NOT uniform, NOT constant, and NOT blind to the grading variable — it decays
   in it. It merely decays TOO SLOWLY (-1 against a strict < -3).

WHAT SURVIVES, AND IT IS STRICTLY WEAKER: at each of the three points the
   certified asset's displayed degree FAILS THAT POINT'S OWN DISPLAYED
   THRESHOLD BY A POSITIVE MARGIN. That is true, it is arithmetic on record
   numbers, and this audit re-derived all three margins (A1a/A1b/A1c, PASS).
   But it is much closer to "all three fail" than SS5.3's own defence admits.
   SS5.3 argues "WHY THIS IS AN ORIGIN AND NOT A RESTATEMENT: ... each one's
   blocking property is the SPECIFIC WAY its asset is localization-blind."
   With FP-2's asset NOT localization-blind, that defence loses its middle
   instance, and the tripartite reading (INVARIANCE / UNIFORMITY / WRONG
   STRATUM) is left holding two group-and-count instances plus one
   insufficient-decay instance, which is a different kind of thing.

CORRECTION REQUIRED: the named shared origin must be restated without the false
   universal. A form the bytes DO carry: **"AT EACH FAILURE POINT THE CERTIFIED
   ASSET'S DISPLAYED DEGREE IN THAT POINT'S OWN GRADING VARIABLE FALLS SHORT OF
   THAT POINT'S OWN DISPLAYED THRESHOLD BY A POSITIVE MARGIN (3/2, 2, 2)."**
   The word "every", the bound ">= 0", and the gloss "localization-blind" must
   go, or FP-2 must be excluded from the convergence and said to be excluded.
```

### F-3 — THE CAS CHECK THAT WOULD HAVE CAUGHT F-2 OMITS THE FAILING TERM

```text
O6-8d, CONDITION TRANSCRIBED VERBATIM FROM TARGET SS9.1:
    (asset_deg_fp1 >= 0) and (asset_deg_fp3 >= 0) and
    (need_fp1 > 0) and (need_fp2 < 0) and (need_fp3 < 0)
O6-8d, LABEL TRANSCRIBED VERBATIM:
    "IN ALL THREE the asset's degree in the obstruction's own grading variable
     is >= 0 ... while every requirement is STRICTLY NEGATIVE"

TWO UNIVERSALS ASSERTED IN THE LABEL; NEITHER IS TESTED, AND BOTH ARE FALSE:
  (i)  "IN ALL THREE ... >= 0" — the conjunction tests FP-1 and FP-3 ONLY.
       `asset_deg_fp2` is defined four lines above as sp.Integer(-1) and then
       NEVER APPEARS in the conjunction. Restore it and the check FAILS.
       [A2b PASS: the check as written evaluates True. A2c PASS: with the
        omitted term restored it evaluates False.]
  (ii) "every requirement is STRICTLY NEGATIVE" — but the conjunction's own
       first requirement term is `need_fp1 > 0`, and need_fp1 = 3/2. The label
       asserts strictly negative; the code tests strictly POSITIVE, for the
       same row, in the same line. [A2d PASS.]

THIS IS THE ONE CHECK IN THE 49 THAT CARRIES THE ARTIFACT'S HEADLINE. It is
  reported as PASS, and it does pass — because the term that would fail was not
  included. NOTHING SUGGESTS CONCEALMENT: `asset_deg_fp2 = -1` is printed in
  SS5.1's table in the open, and SS4.5 states the -1 again. The defect is that
  a PASS was read as verifying a sentence the code does not express.

CORRECTION REQUIRED: O6-8d must be re-run with the FP-2 asset term present and
  the label rewritten to what the arithmetic supports (three positive margins,
  not one sign condition). Until then the "49/49 PASS" line must not be cited
  as verifying the shared-origin sentence.
```

### F-4 — THREE SMALLER OVER-READINGS, EACH CORRECTED IN PLACE

```text
F-4a  "THE BLOCKER IS DESTROYED" (SS4.3 counterfactual B; repeated SS5.3 "is
   DESTROYED by switching real gauge structure on"). WHAT O6-1d3/d4 ACTUALLY
   ESTABLISH: that a non-constant A(x) makes the symbol position-dependent, so
   hypotheses (h-1)/(h-2) FAIL and W-3's theorem DOES NOT APPLY. Denying a
   SUFFICIENT condition does not deny its conclusion; compactness of the
   sandwich under a local potential is not decided anywhere in this artifact or
   its ground. [A6a/A6b PASS re-derive the hypothesis failure; A6c PASS
   exhibits the propositional gap: (H => B) and NOT H is satisfiable with B
   true.] THE TARGET STATES IT CORRECTLY TWICE — SS0 ("DESTROYS the blocker's
   HYPOTHESES") and SS8 ("BREAKS hypotheses (h-1)/(h-2)") — and incorrectly
   twice. CORRECTION: keep the SS0/SS8 wording everywhere; strike "the blocker
   is destroyed". The directional finding SURVIVES on the correct wording: real
   gauge structure removes the blocker's stated hypotheses rather than
   supplying them, so it is not displayed as the blocker's origin.

F-4b  "TWO ADMISSIBLE SECTIONS DIFFER BY A NONZERO RESIDUAL Integral(delta_sect)"
   (SS4.5, SS8, citing O6-7e). O6-7e's condition establishes only an EQUALITY:
   the difference of the two per-cell values EQUALS Integral(delta_sect). It
   does not decide vanishing. COUNTEREXAMPLE WITNESS, EXACT: delta_sect(u) =
   u - 1/2 gives Integral = 0 exactly [A7b PASS]. W-2's own byte is the weaker
   and correct one — the two expressions "are not identically equal" (:449-451).
   CORRECTION: carry W-2's wording; drop "nonzero residual".

F-4c  FP-1's COUNTERFACTUAL PRESUMES A TYPING WALL DOES NOT MAKE. O6-5 sets the
   carrier rank to d.n^3 "at spinor dimension d ... (the record's case is
   d = 4)" and reads d = 1 as "a scalar content class with no spinor index".
   WALL's byte is "rank <= 4n^3 on ran C_n (of record)" (:302) and WALL
   contains ZERO occurrences of "spinor" (re-counted this session). The 4 is
   never typed as an internal dimension anywhere in WALL. CORRECTION: the d=1
   reading is a construction, not a licensed counterfactual — but the FP-1
   verdict does NOT depend on it and stands on the fully licensed ground the
   target also states: the divergence is carried by n^3 against an n-free
   target and is independent of ANY constant prefactor, whatever its
   provenance. [O6-5c/O6-5d carry this; both re-derived.]
   ALSO NOTED: WALL's displayed budgets are |tr| <= 4n^3 . 2 and ||.||_2^2 <=
   4n^3 . 4 (:305) — degree 3, not 3/2. The 3/2 the target uses is the HS-NORM
   branch (sqrt of the HS^2 budget), and MAP does display "HS threshold 3/2"
   (:95) and "the exact power n^{3/2}" (:365). The row is licensed; it is one
   of the two branches F1 covers, and the target does not say so.
```

### F-5 — THE TOKEN CENSUS IS WRONG IN TWO PLACES; NEITHER CHANGES A VERDICT

```text
RE-COUNTED THIS SESSION, WORD-BOUNDARY, OCCURRENCES NOT LINES:

  (i) THE COMPLETION MAP PAIR IS NOT ZERO ON `connection`.
      TARGET SS4.1 TABLE: MAP = 0, MAP-A = 0 across all seven tokens.
      TARGET SS0: "the COMPLETION MAP pair contains zero of all seven."
      BYTES: MAP has **6** word-boundary occurrences of `connection`
        (:39, :153, :156, :160, :165, :593) and MAP-A has **3** (:95, :98,
        :204). All nine are the compound "connection-only", typing a
        cluster-summability kernel (MAP :153 "THE CONNECTION-ONLY
        CLUSTER-SUMMABILITY KERNEL (the Gate-5 kernel C's certifier)";
        :156 "connection-only (symmetric/PSD/intensive/additive/
        cluster-summable/n-independent)").
      ASSESSMENT: the SENSE is not a gauge connection — it is a kernel typing,
        and MAP lists it as an ABSENCE (A-1, "MISSING CONNECTION-ONLY
        SUB-ESTIMATE"). So the census's CONCLUSION is unharmed. But the
        DISPLAYED NUMBER IS WRONG, and a reader re-running the declared sweep
        finds nine hits where the table promises none.
      CORRECTION: report 6 and 3, with the sense displayed as a kernel typing
        and not a connection 1-form.

  (ii) SS0 CONTRADICTS THE TARGET'S OWN TABLE ON WALL.
      SS0: "the entire FP-3 chain — WALL, CERT, FHB, CL-ERROR, BL2*, RL2B-R,
        and the W-3 pair — contains ZERO word-boundary occurrences of `Ward`,
        `gauge`, ..."
      SS4.1's TABLE, same artifact: WALL row reads `2*` under Ward, footnoted
        as the two WARD_SYMBOL_MAP_JII row labels. CONFIRMED AT BYTES: WALL
        contains exactly 2 occurrences of WARD_SYMBOL_MAP_JII and 0 of `Ward`
        standing alone.
      CORRECTION: SS0's "ZERO" must carry SS4.1's own exception. The footnote
        is right; the one-line verdict overstates it.

  (iii) VERIFIED CORRECT, AND WORTH SAYING: every other cell of the SS4.1 table
      re-counted to 0 exactly as displayed, in BOTH directions — CERT, FHB,
      CL-ERROR, BL2*, RL2B-R and the W-3 pair carry zero of all seven tokens,
      and MAXSYM/KERCALC carry zero of h_0 / C-L2 / G_cm / B-L2 / Clifford.
      THE "TWO LANES DO NOT TOUCH AT BYTES" FINDING STANDS.

  (iv) SCOPE NOTE, NOT AN ERROR: the SS4.1 table omits W-2 and W-2-A, the two
      carriers that DO carry gauge tokens (9 and 5). The target does not hide
      this — SS4.5 is built on exactly that content. But the table's caption
      ("the entire FP-3 chain and the map") should say that FP-2's discharger
      is excluded from it by construction.
```

### F-6 — THE "49 CHECKS, 49 PASS" LINE OVERSTATES WHAT THE BATTERY TESTS

```text
CONDITIONS TRANSCRIBED FROM TARGET SS9.1. At least NINE of the 49 conditions
  are a literal `True` or an X - X == 0 tautology and test nothing:
    O6-2b, O6-3b, O6-7b, O6-7d, O6-7f, O6-7g, O6-8e   (literal `True`)
    O6-2c   ((s0 + t1.mu^d) - (s0 + t1.mu^d) == 0 — true for any expression)
    O6-4b   (order(2) == 2, with order defined as the identity function)
  [A8a PASS, A8b PASS.] THREE MORE ARE DEFINITIONAL BOOKKEEPING RATHER THAN
  DERIVATION: O6-4a (charged = order(1+1) = 2 by the same identity function),
  O6-4c (1 + 1 > 1), O6-6d (3 - 3 == 0).
CHECK-DISCIPLINE DEFECT, SEPARATE: O6-1b1's condition accepts
  `sp.ask(sp.Q.nonnegative(m_def)) in (True, None)` — it PASSES on an UNDECIDED
  oracle return [A8c PASS]. The underlying fact (|k| >= 0) is not in doubt; the
  gate is.
CONSEQUENCE: the battery's REAL content is O6-1a (Clifford, exact), O6-1b2/1c
  (the vacuity edge and the scalar counterfactual), O6-1d3/d4/d5 (the symbol's
  position dependence), O6-2a, O6-3a, O6-5a-d, O6-6a-c, O6-7a/c/e, and O6-8a-c.
  THIS AUDIT RE-DERIVED THE LOAD-BEARING ONES INDEPENDENTLY AND THEY HOLD
  (SS5). The correction is to the COUNT and to what a PASS is taken to mean,
  not to the mathematics.
```

---

## 4. WHAT THIS AUDIT COULD NOT BREAK — CONFIRMED, RE-DERIVED

```text
C-1  THE RECORD-SITE CLIFFORD GROUND IS EXACT. A = alpha.k-hat with A^2 = I_4;
     P_- = (I-A)/2 Hermitian idempotent with tr P_- = 2; C h_0 C = -|k| P_-
     exactly; HS density tr(m^dag m) = 2|k|^2 != 0, so hypothesis (h-4) holds
     at the record's own site. Re-derived from an exact angular unit vector,
     no radicals, no numerics. [A4a-A4d PASS.] The target's O6-1a is sound.

C-2  THE DECIDING COUNTERFACTUAL (A/A') IS SOUND AND IS THE ONE THAT CARRIES
     THE VERDICT. On L^2(R^3) (x) C^1 with the scalar INDEFINITE symbol
     m(k) = k_3 — no Clifford, no spinor index, no U(1) — the negative set is
     the nonempty open half-space, C = 1_{k_3<0} is a nonzero translation-
     invariant idempotent multiplier, C m C is nonzero on it, and the symbol's
     modulus exceeds 1 on a set of infinite measure. W-3's sealed two-line
     theorem (W3 :331 "Hence C T C is not compact") applies verbatim.
     With the DEFINITE symbol |k| the negative set is EMPTY, C = 0, and the
     theorem is vacuous. [A5a-A5d PASS.]
     THE CONSEQUENCE STANDS AS THE TARGET STATES IT: the escape from the
     sea-sandwich obstruction is DEFINITENESS OF THE GENERATOR, not absence of
     gauge structure. FP-3's blocking property is GENERIC.

C-3  THE HYPOTHESIS SPLIT IS FAITHFUL TO W-3's BYTES. (h-1)/(h-2)/(h-3) are
     constant-coefficient/class facts carrying no internal index; only (h-4)
     reads the content, and what it reads is spectral indefiniteness, supplied
     at the record's site by Clifford (spinor) structure. W-3's own readings
     are quoted correctly: "the obstruction is a property of C, not of the
     member" (W3 :42) and the escape clause "an input that is NOT
     translation-invariant — i.e. one that consumes the sharp vertex's own
     SPATIAL LOCALIZATION" (W3 :386-387, :701-702). Both verified at bytes.
     THE LOAD-BEARING NOTE AT (h-3) IS ALSO RIGHT: the witness member is h_0,
     not the C-L2 generator, so the spin matrix S never enters the blocking
     mechanism — which is why ABS-4 is correctly displayed as MOOT rather than
     answered.

C-4  THE GAUGE LANE'S OWN DETERMINATIONS ARE QUOTED FAITHFULLY AND DO RUN
     AGAINST A GAUGE ORIGIN. KERCALC's transversality-holds-for-every-member
     findings and its SS3.2 "NO ALGEBRAIC WARD IDENTITY TURNS THAT SUPPORT INTO
     THE DIAGONAL", and MAXSYM SS4.6's insufficiency of contact support, are
     present at bytes in the sealed carriers and say what the target says they
     say. SS4.6's use of them as CORROBORATION OF TYPE — explicitly not as a
     shared mechanism — is the correct weight, and ABS-2 states the seam's
     absence honestly.

C-5  THE HONEST NEGATIVES ARE HONEST. ABS-1 (no gauge typing at the failure
     points, and the branch therefore refused on counterfactuals rather than on
     the census), ABS-2, ABS-3 ("longitudinal" NOT typed; the (d-2) trace
     therefore rested on the index-free degree fact instead), ABS-4, the
     refused HS-sign reconciliation (CH-6), and CH-8's own statement that the
     census is corroboration and not proof — all are correct, and all are
     places where the target declined an available inflation. CH-4 displays the
     alternative aggregate verdict for the registrar rather than burying it.

C-6  NOTHING MOVED THAT MUST NOT MOVE. Independently checked against the
     target's bytes: no witness created or retired; FP-1/FP-2/FP-3 all stand;
     MAP SS3.4/SS3.5 and the minimal completion set {W-1, W-2, W-3} untouched;
     no gate, fence, quantifier or V-clause moved; no divergence of any closure
     quantity exhibited; no route selected and no object proposed.
```

---

## 5. FENCE-SCAN OF THE TARGET

```text
alpha_computed          = false.  No value of alpha, no fine-structure token,
                          no "137", no CODATA, no measured/experimental value
                          appears anywhere in the target. Swept, zero hits.
proof_authorized        = false.  Every determination is one-sided; SS8's NOT
                          REFUTED clause carries it explicitly and correctly.
kappa_record_computed   = false.  `kappa_T,RL` appears only as the UNFORMED
                          datum of W-2's H-1. No kappa value is produced.
MEASURED-CONSTANT CMP   = none. Zero hits.
FLOATS                  = none as premise. Every decimal in the target is a
                          section number or the sympy version 1.14.0.
NUMBERS PRODUCED        = none that is a value of a program quantity. The
                          numerals present are (i) identities re-derived here
                          (tr P_- = 2; HS density 2|k|^2), (ii) quoted record
                          degrees (-1, -3, 0, -2, 3/2, 4n^3, 2 sqrt(3)), and
                          (iii) differences between quoted degrees (3/2, 2, 2).
                          Category (iii) is lawful as an exponent difference —
                          but see F-2/F-3 on how those three were assembled.
LENS TOKEN              = one occurrence, at SS1.1 :162, inside the EXCLUSION
                          declaration itself ("every register / tracker / road
                          / plan / continuation / ledger / lens file" — NOT
                          READ). No lens object is consumed, quoted, or relied
                          on. CLEAN.
REGISTER/TRACKER/ROAD/PLAN/CONTINUATION: none consumed by the target; none
                          consumed by this audit.
TOY_SEPARATION (target SS11): SOUND, and this audit tested it. The scalar
                          classes, the placeholder potential, and the O6-7 set
                          and integral models are used ONLY subtractively — to
                          remove a structure and ask whether a blocker survives
                          — and no quantity computed from any of them is
                          carried into a record object. The single +infinity
                          produced from a constructed object (O6-1c4) is
                          quarantined in SS8 by name. NO TOY IS SUBSTITUTED FOR
                          THE ACTUAL SURFACE ANYWHERE.
CHOICE LEDGER (target SS10): NINE choices declared, and the three that matter
                          most (CH-4 the aggregate word, CH-5 the origin's
                          name, CH-8 the census's weight) are exactly the three
                          this audit found load-bearing. The ledger is
                          accurate. IT IS ALSO INCOMPLETE IN ONE PLACE: the
                          identification of WALL's "4" in 4n^3 as a spinor
                          dimension (F-4c) is an unforced choice and is not
                          ledgered.
```

---

## 6. AUDIT CAS BATTERY — FRESH VENV, EXACT SYMBOLIC (sympy 1.14.0)

Fresh venv `o6auditvenv`, sympy 1.14.0 — same version as the target's, so no
version difference can explain a divergence. 27 checks, 27 PASS, 0 FAIL. Every
check either RE-DERIVES a target claim from scratch or EXHIBITS the gap between
a target check's label and what that check tests. No float is a premise; no
program quantity receives a value.

```text
A1  THE CONVERGENCE ARITHMETIC RE-RUN
PASS A1a  O6-8a reproduced: need_fp1 - asset_fp1 = 3/2
PASS A1b  O6-8b reproduced: asset_fp2 - need_fp2 = 2
PASS A1c  O6-8c reproduced: asset_fp3 - need_fp3 = 2

A2  THE HEADLINE UNIVERSAL IS FALSE ON THE TARGET'S OWN TABLE
PASS A2a  FP-2's displayed asset degree is -1, i.e. NOT >= 0 => "every asset
          has degree >= 0" is FALSE on the target's own row
PASS A2b  O6-8d AS WRITTEN evaluates True — because the FP-2 asset term is
          ABSENT from the conjunction
PASS A2c  O6-8d WITH the term its own label asserts ("IN ALL THREE") RESTORED:
          FAILS
PASS A2d  the label's second universal is false too: need_fp1 = 3/2 > 0, NOT
          strictly negative

A3  THE THREE SHORTFALLS ARE NOT ONE PATTERN: ORIENTATION IS NOT UNIFORM
PASS A3a  under one uniform orientation (asset - need) the rows are
          [-3/2, 2, 2] — not the reported [3/2, 2, 2]
PASS A3b  under the other (need - asset) they are [3/2, -2, -2] — also not it
PASS A3c  the reported triple MIXES the two orientations: FP-1 uses
          need-asset, FP-2/FP-3 use asset-need

A4  THE RECORD-SITE CLIFFORD GROUND, RE-DERIVED INDEPENDENTLY
PASS A4a  A = alpha.khat satisfies A^2 = I_4
PASS A4b  P_- = (I-A)/2 is a Hermitian projection with tr = 2
PASS A4c  C h_0 C = -|k| P_- exactly
PASS A4d  HS density tr(m^dag m) = 2|k|^2 != 0 — (h-4) holds at the record site

A5  THE SCALAR COUNTERFACTUAL (A / A'), RE-DERIVED
PASS A5a  DEFINITE symbol |k|: {m<0} EMPTY => C = 0 => theorem VACUOUS
PASS A5b  INDEFINITE symbol k3: {m<0} = (-oo,0) NONEMPTY => C != 0
PASS A5c  C m C = k3 on {k3<0}, nonzero there => (h-4) holds with NO Clifford,
          NO spinor index, NO U(1)
PASS A5d  |symbol| >= 1 on {k3 < -1}, a set of infinite measure

A6  COUNTERFACTUAL B: WHAT THE CHECK ESTABLISHES vs WHAT THE TEXT CLAIMS
PASS A6a  a non-constant U(1) potential breaks translation invariance
PASS A6b  h_A's symbol is position-dependent => NOT a multiplier => (h-1)/(h-2)
          FAIL  [this is ALL O6-1d3/d4 establish]
PASS A6c  PROPOSITIONAL GAP EXHIBITED: (H => B) and (NOT H) does not entail
          (NOT B) — a model with H false and B true satisfies both premises
PASS A6d  therefore "THE BLOCKER IS DESTROYED" is not licensed; compactness
          under a local potential is UNDECIDED here

A7  O6-7e: THE H-3 RESIDUAL IS NOT SHOWN NONZERO
PASS A7a  the check's content is an EQUALITY, deciding nothing about vanishing
PASS A7b  COUNTEREXAMPLE WITNESS: delta_sect(u) = u - 1/2 gives residual
          EXACTLY 0 => "a NONZERO residual" is not licensed by O6-7e

A8  VACUOUS-CHECK CENSUS INSIDE THE TARGET'S BATTERY
PASS A8a  at least nine of the 49 target checks assert a literal True or an
          X-X==0 tautology: O6-2b, O6-2c, O6-3b, O6-4b, O6-7b, O6-7d, O6-7f,
          O6-7g, O6-8e
PASS A8b  O6-2c transcribed verbatim is (X) - (X) == 0, true for any X
PASS A8c  O6-1b1 accepts sp.ask(...) in (True, None) — it passes on an
          UNDECIDED oracle return

AUDIT CHECKS: 27   PASS: 27   FAIL: 0
```

---

## 7. CHOICE LEDGER (this audit; every unforced choice, classified)

```text
AC-1  THE STANDARD FOR "GAUGE-ORIGINATED". This audit demanded, per its
      commission, an explicit display that the blocking property FAILS for a
      non-gauge content class, and killed attributions resting only on the
      blocker appearing in a gauge-carrying artifact. UNFORCED as a threshold
      — a looser reader could accept a carrier's own word "gauge" as the
      typing, and would then keep the target's MIXED verdict intact. BOTH
      READINGS ARE DISPLAYED HERE (F-1) so the registrar may take either.
AC-2  THE COUNTEREXAMPLE FUNCTION IN A7b. delta_sect(u) = u - 1/2 was chosen to
      witness a vanishing residual. UNFORCED — any function with zero mean on
      the cell serves. CLASS: generic placeholder, INSTRUMENT of a refutation
      only. It instantiates no record object and proposes none.
AC-3  THE PROPOSITIONAL DISPLAY IN A6c. The gap between "hypothesis fails" and
      "conclusion fails" was exhibited by satisfiability rather than by
      argument. UNFORCED; any equivalent display of the same logical fact
      serves. It asserts NOTHING about whether the sandwich is compact under a
      local potential — that remains UNDECIDED and is left undecided.
AC-4  WHAT I DID NOT RE-DERIVE. O6-2a, O6-3a, O6-4, O6-6 and O6-7a/c were read
      and found sound but were not independently re-run, because none of them
      is contested by any finding here and each is elementary on its face.
      DECLARED so the audit's coverage is not overstated.
AC-5  SCOPE OF READING. W-2, W-3, MAP, WALL, BL2* and the two Maxwell-lane
      carriers were read only at the sections the findings turn on, plus the
      declared token sweeps. CERT, FHB, CL-ERROR and RL2B-R were consumed only
      through the commissioned artifacts' quotations, with their seals and
      digests independently verified. No register / tracker / road / plan /
      continuation / ledger / lens file was opened.
AC-6  VERDICT WORD. CONFIRMED-WITH-CORRECTIONS was chosen over CONFIRMED
      because two displayed claims are false as written (F-2, F-3) and one
      attribution is unsupported (F-1); and over REFUTED because the
      commissioned QUESTION is correctly answered, the deciding counterfactual
      is sound and was re-derived, and no verdict of the target is reversed by
      any finding. The corrections are to claims, counts, and one attribution
      — not to the determination.
```

---

## 8. TOY_SEPARATION (this audit)

```text
ACTUAL SURFACE — the thing itself:
  - The target artifact's own bytes, verified at seal, and the fourteen sealed
    carriers it consumes, each verified from its own directory.
  - The findings F-1..F-6, each anchored to a quoted byte or to a condition
    transcribed verbatim from the target's own script.
  - The confirmations C-1..C-6, each re-derived rather than accepted.

INSTRUMENT, NOT SURFACE — constructed here solely to test a claim:
  - delta_sect(u) = u - 1/2 (A7b), a zero-mean witness.
  - The propositional model in A6c.
  - The transcribed degree constants in A1/A2/A3, which are the TARGET'S OWN
    displayed record degrees re-entered to re-run its arithmetic.
  NONE models the closure. NONE stands in for, approximates, or bounds any
  record object. NO quantity computed from any of them is offered as a value.

WHY THIS IS NOT A TOY: nothing constructed here has its behaviour attributed
  back to a record object. The instruments appear only in the refuting
  direction — to show that a stated claim does not follow — which attributes
  nothing. The one place this audit re-derives record-site mathematics (A4,
  the Clifford ground) uses the RECORD'S OWN object, not a stand-in.
```

---

## 9. FLAG BLOCK — STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001

```text
COMMISSION            = O6SR AUDIT | ORIGIN-AUDIT | 2026-08-15 | DEFAULT-REFUTE
TARGET                = STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md
TARGET_SEAL           = OK from the artifact's own directory
                        a04c446aa1406fb89cb732fcf8fc6d777e3414cf0fa1d6761702dedd5dd7d5a3
VERDICT_OVERALL       = CONFIRMED-WITH-CORRECTIONS
VERDICT_D1            = CONFIRMED (blocking mechanisms isolated correctly)
VERDICT_D2            = CONFIRMED-WITH-CORRECTIONS (F-1, F-4a, F-4b, F-4c)
VERDICT_D3            = CONFIRMED-WITH-CORRECTIONS (F-2, F-3)
VERDICT_D4            = CONFIRMED-WITH-CORRECTIONS (inherits F-2's restatement)
VERDICT_CENSUS        = CONFIRMED-WITH-CORRECTIONS (F-5)
VERDICT_FENCES        = CONFIRMED (all three held; no lens object consumed)
CORRECTIONS_REQUIRED  = 6( F-1 withdraw/re-type the sole GAUGE-ORIGINATED
                        attribution; F-2 restate the shared origin without the
                        false universal ">= 0"; F-3 re-run O6-8d with the FP-2
                        asset term present and rewrite its label; F-4a strike
                        "the blocker is destroyed", keep "breaks the
                        hypotheses"; F-4b strike "nonzero residual", keep W-2's
                        "not identically equal"; F-5 correct the MAP/MAP-A
                        `connection` counts to 6 and 3 and qualify SS0's
                        "ZERO" per the artifact's own WALL footnote )
SURVIVES_UNBROKEN     = the commissioned answer( the binding obstructions are
                        NOT gauge-originated ); the deciding counterfactual A/A'
                        ( escape is DEFINITENESS, not gauge-neutrality );
                        the Clifford ground exact; the hypothesis split;
                        "the two lanes do not touch at bytes"; the four exact
                        absences; the one-sidedness of every determination
NET_EFFECT_ON_ANSWER  = STRENGTHENED, not weakened: with F-1 applied the
                        inventory carries NO blocker displayed gauge-originated
                        at bytes. The POSITIVE naming of the shared origin is
                        what needs rework, not the negative finding.
SEALS_VERIFIED        = 15( target + 14 consumed carriers ), all OK from own
                        directory; all 14 digests agree with the target's SS1
SWEEP_CUTOFF          = 2026-08-15, this session; terms declared SS2
CAS                   = fresh venv o6auditvenv, sympy 1.14.0, 27 checks,
                        27 PASS, 0 FAIL; exact symbolic only
FENCES                = alpha_computed = false | proof_authorized = false |
                        kappa_record_computed = false
AUTHORED_PHYSICS      = none | VALUE_PRODUCED = none | NUMBER_PRODUCED = none
MEASURED_CONSTANT_CMP = none
FP1_FP2_FP3           = ALL STAND, unmoved
MAP_SS3_4_AND_3_5     = UNTOUCHED; {W-1,W-2,W-3} still the unique minimal set
WITNESSES             = E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS |
                        SCAD_HS_SCALING_EXPONENT_UNDERIVED STANDS |
                        E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER STANDS
WITNESS_CREATED       = none | WITNESS_RETIRED = none (registrar's act alone)
V_CLAUSES             = untouched | GATES = untouched | QUANTIFIERS = untouched
NOT_REFUTED           = carried in full; no divergence of any closure quantity
                        is exhibited by this audit; all consuming quantities
                        remain UNDECIDED
ROUTE_SELECTED        = none | OBJECT_PROPOSED = none
GIT                   = not used
```
