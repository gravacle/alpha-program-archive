CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep) -- CLAIMED, not clean (Q-921)
VERDICT_BEARING_SET = exactly the 23 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path at seal time (Q-913 standard).
LANE = DARIO   ROLE_THIS_RELAY = B03, THE SELECTOR -- DERIVE OR STOP.  CHOOSING IS NOT ON THIS MENU
  AND IS NOT ATTEMPTED, HINTED, OR PREPARED FOR.
DIGEST_RULE = every file digest and every byte offset below is COMPUTED by member 23 and quoted from
  its output.  No digest and no offset in this artifact is typed by hand.
SPAN_CONVENTION = DECLARED, NOT IMPROVISED (Q-927/931/935).  Three shapes are used and each span
  below is tagged with the one that produced it:
    ROW    -- from the row's leading pipe THROUGH its terminating newline.  This is B01/B02's
              convention, inherited, and it is re-verified here rather than assumed: the two FIXED
              audit-row spans S01/S02 reproduce the digests the handover carried.
    ANCHOR -- from a unique start anchor to (or through) a named end anchor, offsets computed.
              The generator REFUSES if the start anchor occurs more or less than once.
    FIXED  -- a literal [a,b) used ONLY to re-verify a span an upstream sealed artifact already
              pinned.  A mismatch is a refusal, not a footnote.  10 of the 38 spans are FIXED and
              all 10 MATCH their upstream pins.
SUBJECT_READ_RULE = member 13 is read ONLY WITH member 14 (overlay V002) and member 08 (DoR-013),
  per Q-931/Q-935.  status(member 13) := RATIFIED_FAMILY_LEVEL_BY_DOR_013, no-member clause carried.
  Member 13's own header is stale at twenty-six pinned surfaces and is NOT governing.
REGISTER_SNAPSHOT = member 07 is live-append; its digest is its value at my seal time and is later
  than the snapshots members 09, 11 and 17 pin.  No appended register byte carries verdict weight
  here beyond the four rulings quoted in section 1.3 at their own headings.
GENERATOR_REFUSAL = member 23 emits NOTHING unless every declared input opens, every anchor is
  present and unique, and every FIXED span reproduces its upstream digest (Q-920/Q-924).  It refused
  once during this relay, on a start anchor that wrapped a line; the anchor was corrected and rerun.
ALL_RESULTS = CLAIMED until the opposite-lane check.
CLOSURE_END_BYTE = 00007480
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1020_B03_BUILD_DARIO_V001.md` | `46d8e6e4abe6bb53f193dabc7e5c5994aa55a9c893eed914ed490a1efaf715c3` | **the assignment** |
| 02 | `relay_inbox/RELAY_PASTE_1023_FRESH_SESSION_BOOTSTRAP_DARIO_V003.md` | `1c25d0b0964cc63c79b96713983e16e4ef59bad367e92fb919270d47540f7718` | the session contract; the V002 overlay pin that governs where the assignment cites V001 |
| 03 | `relay_outbox/1020_DEFERRED.md` | `5a12fa9443c2721849d5331d74023e5cd1abe809eaf006fcb93cfc670d3b0ddb` | the handover — **re-verified from scratch, never trusted forward** |
| 04 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 05 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law; the seven alignment laws |
| 06 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 07 | `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `fe55632f1c469757e9b87bae544ec5bbc27071032b3843e8519a2cebfda2192c` | the register, live-append; Q-900, Q-909, Q-926, Q-936 |
| 08 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | **the ratified origin; the no-member clause; the authored-physics disclosure** |
| 09 | `workspace/STAGE8_DESC_AUDIT_CODEX2_V002.md` | `078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d` | **the supply map; row 18; the B03 row; the stop rule** |
| 10 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | the demand; CLOSED at V008 (Q-932) |
| 11 | `workspace/STAGE8_DESC_B02_DARIO_V001.md` | `80fc23ed9687b4ccde1a51e4455e0c1878837c535cd4c5594e839c43f0743d1a` | **the trace stop this relay would have released; G2** |
| 12 | `workspace/STAGE8_DESC_B01_DARIO_V003.md` | `41ea9708aa51bdecc3ab3b485763bb2a69b001c4a549a3c1bee4a0094945849e` | the inventory; rows enter at their exact sourced status (Q-922) |
| 13 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | the ratified byte object — READ ONLY, and only with 14 and 08 |
| 14 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | **the overlay of record; 26 pins; the read rule** |
| 15 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V001.md` | `17726437898697cbed3cc730a465a37fd00f5026eed89bed112f41d1d238c539` | V001, superseded append-only, still on the books |
| 16 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **THE CONTINUUM PROOF, and the record's own falsifier list** |
| 17 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | **the placement square; SM-1..SM-8; the product-extension theorem** |
| 18 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | **the port, its nine fields, and its two supply conditions** |
| 19 | `workspace/STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | the field-by-field inhabitance audit |
| 20 | `workspace/STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md` | `35f581dee0683add232367f953ec7807e89bb5eac47e34f4ad5d781a87c95be0` | **Family C; the orbit-collapsing-anchor result; purification** |
| 21 | `workspace/STAGE8_AXN_PINNING_CHECK_CODEX2_V001.md` | `e6e078ef323a7bd66adf45887587798aa7880d2c8008cbe82845151829d0830f` | **the opposite lane's independent verification of the non-uniqueness theorem** |
| 22 | `workspace/STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab` | **G4 — the one rule of record that determines `omega_phys`** |
| 23 | `workspace/build_b03_pins_v001.py` | `a829a9e6925baa620f856ee21f4d39c2d7773a301ecd69153edfc4adff11327f` | the pin generator, sealed alongside; **its declared inputs are exactly members 01–23** |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.  NO COMPARISON TO A MEASURED CONSTANT.
NO SELECTOR EXERCISED, FROZEN, RANKED, OR PROPOSED FOR FREEZING.  NO CANDIDATE STATE NAMED.
NO DOWNSTREAM OBJECT OPENED.  NOTHING ADOPTED.  NOTHING REGISTERED.  omega_phys UNTOUCHED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — B03 BUILD — DARIO LANE — V001
## RELAY 1020 — `[PLAN:DESC-23]` — THE SELECTOR: THE RECORD DOES NOT FORCE `omega_phys`

Date: 2026-08-11
Status: **STOP: SELECTOR-UNFROZEN. The record proves a continuum of possible states at the branch
receiver, twice, by two independent routes, one of them already verified by the opposite lane.
Thirty-eight record-side constraints were consumed and every one is dispositioned: five cut nothing
and the record types each `TYPE-R`; eight state the producer's obligations without constraining its
output; two are supply conditions rather than collapse conditions; five force a unique state at a
different algebra; four are derived machinery that consume the free coordinate instead of producing
it; eight bind the descent object rather than the branch state; one — and only one — actually
determines `omega_phys`, and it determines it from an object that does not exist. The collapsing
property is NAMED-ABSENT: the record states it in its own words, as its own falsifier, and the one
place the record owns a property of that exact shape it discloses as AUTHORED PHYSICS, CONFIRMED
NON-DERIVABLE.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 1. The row, the question, and what would have counted [PROVABLE — quoted]

### 1.1 The row

Member 09 at `[32547,32848)` (ROW), span SHA-256
`b232586ff2599b937b0ee1bca8e7005011fa8a3aa18ef284b663e859d3285474`:

```text
| `B03` | `omega_phys` obtained by a sealed record-native derivation or prospectively principal-frozen without output inspection, with common-origin provenance | SELECTOR-or-derived path | opens the StatePort; its local provenance object is narrower than B15's full suite, so no cycle is introduced |
```

The row names two lawful paths and this relay may walk exactly one of them. The prospective freeze
is the principal's act. **The derivation is mine to attempt, and this artifact is the report of that
attempt.**

Member 09's classification row for the same object, `[26859,27077)` (ROW), span SHA-256
`43c7de164f96e46a45e1f96787efccacf00524970c8e94b1f08bd4a7b162e2b4`:

```text
| 18 | `omega_phys:B->C` at the open StatePort | **SELECTOR** | current record says choose or derive and proves a continuum of possible states; absent a native derivation, only prospective principal freeze is lawful |
```

**That row is the audit's summary, not a proof.** It asserts a continuum. Section 3 does not cite it
for that; it goes to the artifacts that prove it and pins them.

### 1.2 The stop rule that fires

Member 09 at `[30551,30735)` (ANCHOR), span SHA-256
`7d21ef48b2e11ee5227e548e1b10f9276ea36263e3653d61d875eec606ccff93`:

```text
3. If a SELECTOR is reached, require either a sealed record-native derivation or one prospective
   principal freeze made before output inspection. Otherwise STOP as SELECTOR-UNFROZEN.
```

### 1.3 The four rulings of record that scope this relay

Read from member 07 at their own headings, and stated here because a relay that walked past them
would be re-litigating settled questions:

- **Q-900** adjudicates the port open rather than fixed: *"a specified-not-instantiated port with a
  choose-or-derive line is the record typing the slot OPEN, not fixing it"*, and names the reopener's
  real wall as the common-origin descent requirement.
- **Q-909** records the section's load-bearing sentence: the basis stands for construction through
  B02; **B03 halts the run** absent a record-native derivation or one prospective principal freeze.
- **Q-926** overturns B02's one forcing claim (`Params_B0` OPEN-RULED, B02 forces nothing) and rules
  that **the selector stop leaves B03 as the only lawful release protocol.**
- **Q-936** closes every documentary thread and records that **the one live item in the descent
  section is B03.**

Q-909's phrase *"B03 is a principal act"* is about the **freeze** branch. It does not foreclose the
derivation branch, and the assignment expressly sends me to attempt it. I read the two together as:
the freeze is not mine; the attempt is.

### 1.4 What a derivation had to produce

Member 18 at `[10824,11934)` (FIXED), span SHA-256
`4313fb92fd78f6b26a5eba216e7ae7540f6b0698dc82eddd98236ceee5d7cf1b` — the port's own fields:

```text
StatePort_U2_008 := (
  omega_phys : B -> C,
  H_omega := completion of E_C0 / N_omega,
  pi_omega : A_C0 -> B(H_omega),
  rho_pre in trace-class(H_omega),
  rho_pre >= 0,
  Tr_omega(rho_pre)=1,
  state_domain,
  state_descent_map,
  state_provenance_certificate
).
```

Member 18 at `[11681,11929)` (FIXED), span SHA-256
`37a1e1585bdf144b18f0d14fa6c67118591b9f24b15716aee6bc0f0dcbf64e4d`:

```text
U2_STATE_PORT_SPECIFIED = true | TYPE-P | premises: DoR-008
U2_STATE_PORT_INSTANTIATED = false | TYPE-U |
  would-build: choose or derive omega_phys without output inspection, construct
               H_omega and rho_pre, and certify common origin
```

Member 17 at `[16822,17244)` (FIXED), span SHA-256
`7b1ee5198923d54eb03d3f0942acfa8a8dfed7d96fa7edbdf2b20658da06ee47` — the placement square:

```text
Omega_C0(a) := Tr(rho_pre pi_omega(a)),
omega_phys(b) = Omega_C0(i_B(b))
              = Tr(rho_pre pi_omega(i_B(b))).
```

with the same span's own sentence that **no such equation has both sides instantiated.**

**So the derivation's job was exact, and it is worth stating before reporting that it failed.** Not
"find a state." Exhibit a record-side condition that collapses the space of admissible `omega_phys`
to one, where the condition is itself forced by the record and cited at bytes — and then build
`H_omega` and `rho_pre` from it and certify common origin. Member 09's own words for the obstruction
are that the record **proves a continuum**. Section 3 establishes that this is true and what exactly
it means; section 4 reports every constraint I consumed trying to collapse it; section 6 names the
property that would.

---

## 2. Method, and the instrument I did not use [PROVABLE]

The handover (member 03) warns that a lexical sweep for `continuum` returns 1,143 unrelated contexts
because the corpus uses the word pervasively in a spacetime/continuum-limit sense. **I ran no such
sweep and no verdict here rests on one.** The continuum was reached through the receiver instead:

```text
RECEIVER-FIRST HUNT
  1. fix the receiver: omega_phys : B -> C, i.e. a member of State(B)
  2. enumerate every sealed artifact that names BOTH omega_phys AND the branch algebra
     (State(B) / A_F_CTP / C(Y))                                        -> 13 files
  3. read each for a CONSTRAINT ON State(B), not for the word
  4. classify: does it cut the family, state an obligation, or bind a different object?
```

The thirteen are members 11, 16, 17, 18, 19, 20, 21, 22 of this closure, plus
`STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md` and `V003.md` (superseded by member 22),
`STAGE8_AXN_ORIGIN_MORPHISM_HUNT_CODEX2_V001.md`, `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md`,
and `STAGE8_TASK2D_MULTIAXIAL_STATE_TRANSITION_ENVELOPE_SPECIFICATION_V001.md`. The last three carry
no constraint on `State(B)` that members 16/17/20/22 do not carry at their own bytes, and they are
therefore **declined as closure members rather than admitted and left unused** — a consequential
census, per member 09's own standard, not a lexical one.

**This is a bounded instrument and I state its boundary.** It finds constraints in artifacts that
name the branch state. A constraint that bears on `State(B)` while naming neither `omega_phys` nor
the branch algebra would not appear. I have no candidate for such an artifact and I looked; I record
that as the residual in section 12 rather than as a finding.

---

## 3. THE CONTINUUM, AS THE RECORD PROVES IT, AT BYTES [PROVABLE]

This section is diagnosis-display item (a). It is deliberately built from **two independent proofs**
at two different receivers, because a single proof would make the display rest on one artifact's
correctness.

### 3.1 First proof — the exhaustive census at the branch algebra

Member 16 at `[6680,8157)` (FIXED), span SHA-256
`ba3c5741ded604a938483fab8cc9da1f793de1aabf18aa341cfbf4c237b0935f`. Three steps, each quoted:

**(i) The algebra is ratified, commutative, and concrete.**

```text
Lambda = direct-sum_(j>=1) Z e_j,
A_F := C*(Lambda),
B := A_F_CTP := A_F,+ tensor_min (A_F,-)^op,
X := Hom(Lambda,U(1)),  Y := X_+ x X_-,
B isomorphic to C(Y).
```

with the same span's own sentence: *"`Lambda` is countable, so `X` is the countable product of
circles and `Y` is a compact metrizable space."*

**(ii) The census is exhaustive, not exemplary.** By Riesz–Markov,

```text
F_raw := {omega:B->C | omega bounded, linear, positive}  <->  finite positive regular Borel measures on Y,
F_1   := {omega in F_raw | omega(1_B)=1}                 <->  probability measures on Y.
```

and the span states the discipline in its own words: *"This is exhaustive for bounded positive
`B->C` maps. It is not a lower-bound census based only on named examples."*

**(iii) The cardinality is exactly the continuum.** *"Point masses give at least one distinct state
for every point of `Y`; a probability measure on compact metrizable `Y` is determined by its values
on a countable generating family of Borel sets, giving at most continuum many."*

Member 16 grounds this on three imported mathematical theorems, declared as such at its §1.2 and
**not** as physical premises: the group C\*-algebra of a discrete abelian group is the continuous
function algebra of its compact character group; the minimal tensor product of commutative
C\*-algebras is the function algebra on the product spectrum; and Riesz–Markov. Their hypotheses are
supplied by the ratified presentation, so the census inherits `TYPE-P | premises: DoR-008`.

**(iv) The separation witness is explicit.** Member 16 at `[9107,9616)` (ANCHOR), span SHA-256
`021179837ab74b20f4211d1914ecbe6f4f7e68b75b754fea9118ef24a7ef3065`: for `t in [0,1]`,
`omega_t := (1-t) omega_H + t omega_epsilon` is positive and normalized by convexity, and on a
plus-branch generator `b_lambda` one has `omega_t(b_lambda)=t`, so distinct `t` give distinct
functionals. The span states its own scope: *"an algebraic separation witness, not evaluation of a
physical observable or response."*

**I carry `omega_H` and `omega_epsilon` strictly as the record's witnesses and name neither as a
candidate.** Member 16's own §7 forbids the promotion in terms: *"Mere preference for Haar,
trivial-character evaluation, a product state, or any convex mixture does not satisfy that
condition."*

The headline result, member 16 at `[1243,2752)` (ANCHOR), span SHA-256
`79f9b89b8472431b151bd765335f461c40f76bbcbceff152ee4d32a0bfe033ca`:

```text
UNIQUE_SCALARIZATION_FORCED = false | TYPE-R |
  test: the explicit family omega_t=(1-t)omega_H+t omega_epsilon,
        0<=t<=1, consists of pairwise inequivalent normalized positive
        functionals and every member passes the executable U1, one-cell, and
        inclusive-identity constraints

CONSTRUCTION_VERDICT = CONTINUUM_RESIDUAL_FAMILY__STATE_PORT_REMAINS_UNBUILT
```

**`TYPE-R` is a refutation, not an absence.** The record does not merely lack a forcing argument; it
has refuted the claim that the executable constraints force one — and refuted it constructively,
with a witness family, at the exact receiver.

### 3.2 Second proof — the product-extension theorem at the joint algebra

Independent of the census, and at a different receiver. Member 17 at `[15554,15965)` (FIXED), span
SHA-256 `e9dafa5963128305d212f83b8c6617bfa47293f70bf91f5d7c0729fdb6828532`: on the sealed two-factor
subalgebra `R_inf tensor_min B`, the record state `omega_out` induces the slice map
`E_out := omega_out tensor id_B`, a conditional expectation onto the history factor; and **every**
`omega_B in State(B)` produces a product state `Omega_(out,B) := omega_out tensor omega_B` with the
same record marginal. Therefore *"the record marginal alone admits a continuum of such extensions.
This is an algebraic non-uniqueness theorem at the current receiver, not a count of physical
survivors."*

**This theorem is not mine and not one lane's.** Member 21 — the opposite lane — independently
re-derives it at `[15053,15989)` (ANCHOR), span SHA-256
`15d9c3f70000a3dc4365bf2cb16cc8addfaf612b2fe711f848a829a3690067c5`, pins the same span digit for
digit, and rules:

```text
NONUNIQUENESS_THEOREM = VERIFIED
VOID_TARGET = the unbound history-functional coordinate / continuum selection
SELECTION_MADE_HERE = false
```

with its own reading of the consequence: the theorem *"proves that the record marginal does not
determine the history functional... it does **not** select the restriction or turn `omega_phys` into
a dependent coordinate."*

Member 20 states the same result a third time as a row of its own no-go table, `[20270,20415)`
(ROW), span SHA-256 `8fb75a2e05a3b399faa47bb1bb13cdd9340777fb7eeada4658f483597269f6d3`:
*"record marginal plus slice | `omega_out tensor omega_B` for every `omega_B in State(B)` | a
marginal does not force the other factor's state."*

### 3.3 What the two proofs jointly establish, stated exactly

```text
THE CONTINUUM, AS THE RECORD PROVES IT

  (1) The admissible family at the branch receiver is exactly the probability measures on Y,
      by an EXHAUSTIVE census (Riesz-Markov), not a census of named examples.        [member 16]
  (2) Its cardinality is EXACTLY the continuum, upper and lower bound both proved.   [member 16]
  (3) A pairwise-inequivalent witness family is exhibited and separated.             [member 16]
  (4) Uniqueness is REFUTED (TYPE-R) against the executable constraint set.          [member 16]
  (5) Independently, at the joint receiver, the record marginal admits a continuum
      of extensions -- an algebraic non-uniqueness theorem.                          [member 17]
  (6) (5) is independently VERIFIED by the opposite lane and re-stated by a third
      artifact as a row of its own no-go table.                              [members 21, 20]
```

### 3.4 What the continuum proof is NOT — a flattening guard I owe the successor

The handover flags a specific trap and it is a real one. Member 19 at `[8902,10649)` (FIXED), span
SHA-256 `20b0b65035f4dbf75ae72f2c62983f881a71cb8bca69fe568e9d9eaa83c7e77f`, opens its inhabitance
audit with:

```text
| `omega_phys:B->C` | `B` is isomorphic to `C(Y)` and its entire state simplex exists | **UNBOUND**: no origin map lands in `State(B)` |
```

**That row is a receiver-side statement and it is NOT the continuum proof.** It says the target
exists and nothing lands in it. Read as the proof it would be a flattening in both directions: it
would credit the record with proving a *size* it does not prove there, and it would let a successor
think the obstruction is "no map yet" — a shortfall of construction — when the obstruction proved at
§3.1–§3.2 is that the record's own constraints **do not narrow the target**, which construction alone
does not fix. I pin the row for what it is: the inhabitance result, which is why the port has zero
inhabitants, and which is a different finding from the non-uniqueness theorem.

Member 19's closing sentence for that table, quoted because it is the honest scope: *"the advance
since the old port text is real but scoped: a source-sector state and its common-origin subtrace now
exist; the joint physical state port still does not."*

---

## 4. EVERY RECORD-SIDE CONSTRAINT CONSUMED [PROVABLE]

This section is diagnosis-display item (b). **Thirty-eight constraints, each with its ground at
bytes and its disposition.** The display's whole function is exhaustiveness, so the failure mode to
guard against is a list that reads complete while missing entries — which is why section 2 states
the hunt's boundary and section 12 states its residual.

Dispositions use five words and they are not interchangeable:

```text
CUTS-NOTHING       the constraint is executable, every member of the family passes it,
                   and the record types the non-collapse (usually TYPE-R)
OBLIGATION         it constrains the PRODUCER of the state, not the state produced;
                   it can reject a bad construction but cannot narrow the target
WRONG-ALGEBRA      it forces a unique state, but at a different algebra than B
CONSUMES-THE-FREEDOM  it takes omega_B as input; running it requires the thing sought
NOT-BEARING        it binds a different object in the section; consumed and dispositioned
                   so that no successor mistakes it for the obstruction
```

### 4.1 Group A — the five executable constraints, and the sixth that cannot run

Member 16's own survivor ledger, `[17673,18170)` (ANCHOR), span SHA-256
`a452cec80591d83758dc911e9ee9838271d953eadc61a5aaba4d93b534d97a00`, quoted whole because it is the
single most load-bearing table in this artifact:

```text
| Stage | Survivor class | Size/status |
|---|---|---|
| Raw positivity | finite positive measures on `Y` | continuum |
| Inclusive normalization | probability measures on `Y` | continuum |
| U1 reality | invariant probability measures on `Y` | continuum |
| One-cell C0-scope restriction | same class; only `omega(1)=1` is consumed | continuum |
| Weak `rho_pre` type-only existence | no further cut | continuum |
| Concrete physical `rho_pre` plus common origin | unbuilt test | `NO_VERDICT` |
```

| # | Constraint | Ground | Disposition |
|---:|---|---|---|
| C01 | **Raw positivity** — `omega` bounded, linear, positive | 16 `[6680,8157)` | **CUTS-NOTHING.** It is what makes the census well-posed; by Riesz–Markov it *defines* the family as the finite positive measures. A constraint that constitutes the family cannot narrow it |
| C02 | **Inclusive-identity normalization** — `omega(1_B)=1` | 16 `[16045,16242)`, `4fc152fdd560339523652d871920fe4813fcaec0b250a528341e20b3c536c6b6` | **CUTS-NOTHING.** *"removes the zero functional and one positive scaling degree"* and leaves the probability simplex. `INCLUSIVE_NORMALIZATION_SELECTS_UNIQUE_OMEGA = false \| TYPE-R` |
| C03 | **U1 reality / branch-CTP consistency** — `omega(Theta_F(b)) = conj(omega(b))` | 16 `[10834,11067)`, `30218e8d826a1f5a7a0c47e064a8a20e6a14788f5c6cdd70556c20d104c4b87b` | **CUTS-NOTHING.** Every `omega_t` is `Theta_F`-fixed and `b_lambda` still separates. `U1_EQUIVALENCE_COLLAPSES_CONTINUUM_FAMILY = false \| TYPE-R`; the survivor class is the invariant probability measures — still a continuum |
| C04 | **One-cell C0-scope restriction** — `j_SR,1(a)=a tensor 1_B1` | 16 `[14670,15066)`, `49ff6a56bdc94d37782a075c62371301e5467ebfaecfde5eaa584d715e006f4b` | **CUTS-NOTHING.** *"the restriction consumes omega only through omega(1)=1"* — it re-imposes C02 and nothing more. `TYPE-R` |
| C05 | **`rho_pre` type-only existence** — positive, trace-class, unit trace | 16 `[12963,13486)`, `845eb340de3e648fd3df991af1cccdbe3f4e963bb0d28cd8a4d6fe49a4e2ee13` | **CUTS-NOTHING.** *"each normalized scalar realization admits normalized positive trace-class densities, while the role supplies no further discriminator."* `TYPE-R`. Every candidate has such a density, so having one discriminates nobody |
| C06 | **Concrete physical `rho_pre` + common origin** — the full placement reading | 16 `[12963,13486)`; 17 `[16822,17244)` | **CANNOT RUN.** `RHO_PRE_PHYSICAL_PLACEMENT_TEST_EXECUTED = false \| TYPE-C`; the physical subfamily is `NO_VERDICT`. **This is the only constraint in the record that could in principle be failure-capable against the continuum, and it is unexecutable** — which is exactly why the answer is a stop and not a refutation |

**C06 is the hinge of the whole display and I state its consequence in the record's own guard.**
Member 16 §5: *"Calling that subfamily empty would convert missing construction into a physical
refutation, which DoR-006 forbids."* The same discipline forbids calling it a singleton. C06 is
`NO_VERDICT` in both directions.

### 4.2 Group B — the map constraint system, SM-1..SM-8

Member 17 at `[17926,19822)` (ANCHOR), span SHA-256
`231fc71a621a4d77009a863636fe88bfae5846c5a79c1c2e1456ed2f0171c1c7`.

| # | Constraint | Disposition |
|---:|---|---|
| C07 | `SM-1` typed source/codomain — the producer starts from one declared common-origin input class and lands in `State(A_C0)` or the port | **OBLIGATION.** Types the arrow; admits every member of the family as a possible value of a conforming arrow |
| C08 | `SM-2` joint positivity and normalization — `Omega_C0>=0`, `Omega_C0(1)=1` | **OBLIGATION**, and it is C01+C02 one level up. Its own failure-capable receiver is *"a non-state cannot feed `res_B`"* — it rejects non-states, not states |
| C09 | `SM-3` record marginal — `Omega_C0 compose i_R` reproduces the admitted `M_3` record state | **OBLIGATION.** And this is precisely the hypothesis of the non-uniqueness theorem at §3.2: fixing the record marginal is exactly what leaves a continuum of extensions. **The constraint most likely to be mistaken for a collapse is the one proved not to be one** |
| C10 | `SM-4` history binding — `omega_phys = Omega_C0 compose i_B`, *"a construction output, not a later preference"* | **CONSUMES-THE-FREEDOM.** It defines `omega_phys` in terms of `Omega_C0`. It converts the question "which `omega_B`?" into "which `Omega_C0`?" without answering either. See C34 |
| C11 | `SM-5` finite/quotient compatibility — agreement with `q_N`, the sequential embeddings, U1 reality, branch conventions | **CUTS-NOTHING beyond C03.** Its U1 leg is C03, already `TYPE-R`; `q_N` is a quotient of doubled histories and member 17 §2.3 states it *"do[es] not turn a record state into a history state"* |
| C12 | `SM-6` common primitive provenance — carrier, law, record state, history state, domains, effects all descend from one frozen trace | **OBLIGATION**, and the strongest one. It is the record's one standing *require*. It rejects a product-state assembly by declaration — but rejecting bad provenance does not single out a good state, and the trace it quantifies over is itself unbuilt (member 11's G2) |
| C13 | `SM-7` target independence / no supplementation — criteria fixed before K4–K7 or downstream outputs are read | **OBLIGATION**, and it is a bar on *me*, not on the family. It is the constraint this relay is most at risk of violating and section 7 certifies compliance |
| C14 | `SM-8` coverage statement — the producer either enumerates the admitted family with a completeness certificate or states its bounded working class | **OBLIGATION — and the one that forbids the shortcut.** Its own failure-capable receiver reads *"prevents one successful member from being called forced."* A derivation that produced one state would still owe SM-8, and SM-8 is what makes "I found one" different from "the record forces one" |

Member 17's own summary of the group, carried: *"`SM-3` and `SM-4` are the missing commuting square.
`SM-6` and `SM-7` distinguish a lawful common-origin producer from the always-available product
extension `omega_out tensor omega_B`."*

### 4.3 Group C — the port's two supply conditions

| # | Constraint | Ground | Disposition |
|---:|---|---|---|
| C15 | **No output inspection** | 18 `[11681,11929)`; 9 §6 | **NOT A COLLAPSE CONDITION.** It is a condition on *how* a value may be supplied, not on *which*. It rules a procedure lawful or unlawful and is silent about the family |
| C16 | **Common-origin certificate** | 18 `[11681,11929)`; 10 cond. 12 `[24132,24240)`, `5fb6489657a190f3a4eb67a0b532c11dac032d7651203f359ed453caef71358f` | **OBLIGATION.** Same shape as C12 at the port's own bytes. Q-900 names it the reopener's *"real wall"* — a wall in front of a supplied value, not a rule that picks one |

**Q-900's reading is the governing one and it is worth quoting against my own temptation:** the port
*"left `omega_phys` an open supply with two conditions attached."* Two conditions attached to a
supply are not a derivation of what is supplied.

### 4.4 Group D — the ratified origin, and the uniqueness it does prove

This is the group a lane most wants to be the answer, because the record really does force a unique
state here — at the wrong algebra.

Member 08 at `[355,992)` (FIXED), span SHA-256
`bc83e53db7e46f27b570b1492ebd9ea53f168c344e1b06d1b82b45bb36a00750`:

```text
- THE ANCHORED GENERATIVE FAMILY — all three convergent anchor classes (bistochasticity,
  finite-trace detailed balance, irreducible symmetry covariance) adopted AS A FAMILY.
  *** NO MEMBER IS SELECTED, EVER. *** The proven p_ch-neutrality certificate (Q-265,
  independently verified Q-266) is the license: every member forces the same normalized-
  identity invariant state and the same symbolic sector-dimension-ratio form for p_ch.
- The generative maps: d_state (rho_S), d_ready (the single forced plus-root — the
  exchanged root is REFUTED, Q-264, not chosen against), d_law (reproducing the ratified
  DoR-009 law exactly).
```

| # | Constraint | Disposition |
|---:|---|---|
| C17 | **DoR-013's family-level adoption and the no-member clause** | **NOT A COLLAPSE CONDITION — and a closed one.** It forbids ever selecting an anchor member. It removes a route rather than narrowing `State(B)`; member 11 §7 already ruled this a *closed* selector, not a live one, and reopening it would be re-litigating a decision of record |
| C18 | **The p_ch-neutrality certificate** — every member forces the same normalized-identity invariant state | **WRONG-ALGEBRA, and this is the sharpest entry in the ledger.** It is a genuine, proven, independently verified uniqueness result — *"uniqueness collapses the invariant-state image"* (member 20 `[20416,20954)`). Its receiver is `State(A_src)`. Member 19's `rho_pre` row types the transport failure exactly: DoR-013 generates `rho_S,[A]=I_A/Tr_A(I_A)` **on the finite source sector**, and the result at the joint receiver is **WRONG CODOMAIN** |
| C19 | **`d_state` (`rho_S`)** | **WRONG-ALGEBRA.** Member 19: *"`d_state:Omega_prim,N->Dens_1^+(H_A)` is ratified — **WRONG CODOMAIN**: it is not `d_state_Omega:Omega->StatePort_U2_008` or `d_joint:Omega->State(A_C0)`."* The ratified map produces what would be a *restriction* of the object needed, not the object |
| C20 | **`d_ready`** — the single forced plus-root, the exchanged root REFUTED | **NOT-BEARING on `State(B)`.** A forced ray in the ready sector; the record forces it and it does not touch the branch functional |
| C21 | **`d_law`** — reproducing DoR-009 exactly | **NOT-BEARING on `State(B)`.** It fixes the transition law, not the pre-state's branch restriction |

**The positivity/unit-trace row of member 19 makes the scope failure explicit in one line**, and it
is the line I would put in front of any successor who thinks the anchor settles this: *"positivity
and unit trace | independently proved for the source density at fixed A0 | **PASS ONLY AT
SOURCE-SECTOR SCOPE**; not a joint-state certificate."*

### 4.5 Group E — the derived algebraic machinery

| # | Constraint | Ground | Disposition |
|---:|---|---|---|
| C22 | **`res_B` exactness** — `res_B(Omega_C0) := Omega_C0 compose i_B` preserves positivity and normalization | 17 §3.2 | **CONSUMES-THE-FREEDOM.** Member 17: *"the sealed corpus supplies no `Omega_C0 in State(A_C0)`."* An exact arrow with no input is not a derivation of its output |
| C23 | **The slice conditional expectation `E_out`** | 20 `[17758,18031)`, `bbc23b0cefcbc8056ad935044a1558b9e9ef633deb7e4f266f17634470dd34ac` | **CONSUMES-THE-FREEDOM, proved.** *"This direction **consumes** `omega_B`. It cannot produce one."* And `E_out(1_R tensor b)=b` returns the observable, not a scalar; to get a scalar one must compose with an `omega_B` — *"which imports exactly the member being sought"* |
| C24 | **Purification / dilation of the ratified density** | 20 `[18033,19428)`, `95cab6ad2c256f6901b00ef41b59a8fbfb57998a3bfef6b44f045b04ec743630` | **CUTS-NOTHING.** No identification of the ancilla `K` with the physical record/history factors, no `B` marginal, no record marginal, no certificate — and *"unitary freedom on `K`... is exactly why purification alone does not determine the physical joint state"* |
| C25 | **Abstract state-extension existence** | 20, same span | **CUTS-NOTHING.** *"would establish only that some extension exists... would not provide a sealed, finite, target-independent producer choosing one extension, and it would not discharge `SM-3` through `SM-8`"* |

### 4.6 Group F — the demand map's conditions that bind this object

Member 10, CLOSED at V008 per Q-932. These are demand-side and I consume them as such.

| # | Condition | Ground | Disposition |
|---:|---|---|---|
| C26 | **2** — an actual common-origin construction trace, concrete not existential | 10 §3 A | **OBLIGATION**, and unbuilt: member 11's trace stops at this very selector (G2). The dependency is genuinely circular at the section's level and section 5 says so |
| C27 | **7** — state properties: positive, trace-class where applicable, normalized | 10 `[23605,23719)`, `fde0455196a69d779f381eb60f1f9c6388234ec2b188840531a579d3656b5751` | **CUTS-NOTHING.** This is C01+C02+C05 restated at the certificate's receiver; the same `TYPE-R`s dispose of it |
| C28 | **12** — provenance: shared primitive inputs and construction record, no post hoc import | 10 `[24132,24240)` | **OBLIGATION.** = C16 at the certificate's receiver |
| C29 | **15** — the temporal bar: one trace supplies state, effects, domains, dynamics before any response output is inspected | 10 §3 D | **NOT A COLLAPSE CONDITION.** A bar on ordering. Member 10 §1.1 is explicit that this is the *timing* bar and nothing more |
| C30 | **16** — the compositional bar: no ad hoc cross-row assembly, from common-origin provenance | 10 §3 D | **OBLIGATION.** Member 10 §1.1: *"A datum bagged independently before output may pass literal timing while still failing A03/A05 common derivational provenance; both predicates are required."* It rejects assembly; it does not select |
| C31 | **17** — no undeclared external supplement after a descendant output is inspected | 10 §3 D | **NOT A COLLAPSE CONDITION.** A closure bar on the build |
| C32 | **18** — K7: base and weighted family descend from the frozen joint trace; a status flag cannot substitute | 10 §3 E | **NOT-BEARING, and deliberately not opened.** K7 is downstream. What it would *need* `omega_phys` to be is exactly what section 7 certifies I did not consult |
| C33 | **35** — the StatePort placement binding: the supplied `omega_phys` bound to the actual representation and density by a declared relation with its common-origin certificate | 10 `[27780,27998)`, `00d7b4c8a84fea121d9d19745f1ff3ee44e327fd5a09c17c66da145ef96130cc` | **CONSUMES-THE-FREEDOM.** The word is *supplied*. Condition 35 governs what happens to a value once there is one |

### 4.7 Group G — the one rule of record that actually determines `omega_phys`

**There is exactly one, and finding it is the substantive result of this relay.** Member 22 at
`[38169,39014)` (FIXED), span SHA-256
`ae1f79d9b7f24d2e0899f1116e38db3b51f72f0d89cc9983f8a00399f0076cee`:

```text
### G4 — `res_B` construction [CLAIMED]

Only after eight SM passes, compute structurally

    omega_hist := res_B(Omega_C0) := Omega_C0 compose i_B.

Verify positivity, normalization, declared representation, and equality to the `JAC-07` history
marginal. The slice `E_out` is not a substitute and no independent `omega_B` may enter.
```

| # | Constraint | Disposition |
|---:|---|---|
| C34 | **G4's restriction pinning** — `omega_hist := Omega_C0 compose i_B`, and *"no independent `omega_B` may enter"* | **DETERMINES `omega_phys` — CONDITIONALLY, AND THE CONDITION IS UNMET.** Three separate reasons it does not close this relay, below |

**(i) It is conditional on an object that does not exist.** The rule opens *"Only after eight SM
passes"* and computes from `Omega_C0`. Member 17 §3.2: the corpus supplies no `Omega_C0 in
State(A_C0)`. G4 does not collapse the continuum; **it relocates it** — from a free `omega_B` to a
free `Omega_C0`, whose own `B`-marginal ranges over the same simplex by §3.2's theorem.

**(ii) The registrar has already ruled it not record-forced.** This is the exact question Q-900
adjudicated, and it went against the necessity claim: the restriction route was *"lawful and
natural"* for the instrument as constituted, but a choose-or-derive port is *"the record typing the
slot OPEN, not fixing it."* Member 21 records the same result as a status line at `[18881,19075)`
(ANCHOR), span SHA-256 `d53d4554564e5aa42adcfeeccf1c1f08a1c3860d1b108efb87805704888f81e1`:

```text
SECTION_CURRENT_INSTRUMENT = remains closed by its own governing restriction route
SECTION_WALL_TO_WALL_RECORD_NECESSITY = not established
COUNTERFACTUAL = live, principal-visible, not exercised
```

**(iii) Its scope is one instrument, not the record.** Member 21 §6 types it *"Level 0 — current
instruments... exact restriction is governing for the present instrument."* An instrument-scoped
determination is not a record-native derivation, and treating it as one is precisely the
instrument-versus-receiver conflation member 09's stop-rule clause 6 was amended to prevent.

**I record this as the relay's central finding and I do not repair it in place**: the one rule of
record that determines `omega_phys` determines it from an unbuilt object, was ruled instrument-
authored rather than record-forced, and is scoped to a single instrument. All three are already on
the record; assembling them against B03's exact question is what this relay adds.

### 4.8 Group H — constraints that bind a different object

Consumed and dispositioned so that no successor mistakes any of them for the obstruction.

| # | Constraint | Disposition |
|---:|---|---|
| C35 | **MD-3 descent non-degeneracy** — `DESCEND_B0` faithful on the tested quotient | **NOT-BEARING.** It tests a descent map's faithfulness, and it is *"an acceptance condition over the tested family, not a global uniqueness theorem"* (member 10 §2.5). It also returns `NO_VERDICT` on an empty probe set, which *"is not evidence against `B0`. It is also not a pass."* |
| C36 | **A01–A05** — the inhabited B0 candidate, inventory, constructor, filled signature, provenance certificate | **NOT-BEARING, and upstream-stopped.** Member 11 stops the candidate at A01 against a standing decision of record. Even satisfied, these constrain the origin object, not the branch state |
| C37 | **A14–A17** — the closing certificate machinery, no-post-output supplementation as an executable test, the equivalence/naturality rule, the T1–T6 ledger | **NOT-BEARING.** Certificate and replay machinery over a package; A16 carries its own express non-demand, *"unique-origin reconstruction remains unnecessary"* |
| C38 | **The four-field stop** — measure, contour, boundary, endpoint domains, all `TYPE-U` | **NOT-BEARING, and stated because it is the most attractive wrong answer.** These four gate `U3`, `B06`–`B10`, and K7's measure input. They sit **downstream** of the state port. A successor who reads the section as "blocked on the four fields" will build four packages and arrive back at this same unfrozen selector |

### 4.9 The tally

```text
CONSTRAINTS_CONSUMED = 38, EACH DISPOSITIONED

  CUTS-NOTHING            9   C01 C02 C03 C04 C05 C11 C24 C25 C27
  OBLIGATION             11   C07 C08 C09 C12 C13 C14 C16 C26 C28 C30 C36*
  WRONG-ALGEBRA           3   C18 C19 (and C36 is not counted twice; see note)
  CONSUMES-THE-FREEDOM    4   C10 C22 C23 C33
  NOT-A-COLLAPSE-COND     4   C15 C17 C29 C31
  NOT-BEARING             6   C20 C21 C32 C35 C37 C38
  CANNOT-RUN              1   C06
  DETERMINES-CONDITIONALLY 1  C34

  NOTE ON THE TALLY: C36 is dispositioned NOT-BEARING and is listed once, under NOT-BEARING.
  The OBLIGATION row's count is 10, not 11, and the asterisked C36 is struck from it.  I leave
  the correction visible rather than silently re-typing the row, because an arithmetic slip
  inside an exhaustiveness display is exactly the defect this display exists to make visible.
  CORRECTED COUNTS: CUTS-NOTHING 9 | OBLIGATION 10 | WRONG-ALGEBRA 2 | CONSUMES-THE-FREEDOM 4 |
  NOT-A-COLLAPSE-CONDITION 4 | NOT-BEARING 7 | CANNOT-RUN 1 | DETERMINES-CONDITIONALLY 1.
  SUM = 9+10+2+4+4+7+1+1 = 38.

  COLLAPSING CONSTRAINTS FOUND = 0
  CONSTRAINTS THAT WOULD COLLAPSE IF THEIR RECEIVER WERE B = 2  (C18, C19 -- both at A_src)
  CONSTRAINTS THAT DETERMINE omega_phys FROM AN UNBUILT OBJECT = 1  (C34)
  CONSTRAINTS FAILURE-CAPABLE AGAINST THE CONTINUUM BUT UNEXECUTABLE = 1  (C06)
```

---

## 5. The attack I owe this stop [Q-98 discipline]

Q-98 rules the vacuity/name-match defect structural to this corpus and records that what catches bad
constructions is the adversarial countermodel. So I attacked my own stop at its strongest point.

**Attack.** *"You dispositioned C18/C19 WRONG-ALGEBRA and C09 OBLIGATION separately. Combine them.
The anchor forces a unique invariant state `rho_S` at `A_src`; `SM-3` forces the record marginal
`omega_out` at `R_inf`. `A_C0 = A_src tensor R_inf tensor B`. Two of three marginals are then forced
by the record. Surely the third is pinned, and `omega_phys` is derived."*

**Result — the attack fails, and it fails for a reason worth pinning rather than by my say-so.** Two
prescribed marginals on a tensor product do not determine the third factor's state: for any
`omega_B in State(B)`, the product state `rho_S tensor omega_out tensor omega_B` reproduces both
prescribed marginals and varies over the whole simplex in its third. That is §3.2's theorem with one
more factor, and the record reaches the same conclusion from its own side twice: member 20 §3.2
states that the full `A_C0` slice *"would additionally require a source-record state"*, and member
20 §3.3 states that purifying the DoR-013 density supplies no `B` marginal, no record marginal, and
no certificate, with unitary freedom on the ancilla *"exactly why purification alone does not
determine the physical joint state."*

**One caveat, declared rather than buried.** Member 17 types the joint algebra with *graded* minimal
tensor products (`A_SR = A_src graded-tensor_min R_inf`, `A_C0 = A_SR graded-tensor_min B`). My
three-factor product-state construction is stated for the tensor decomposition as member 17 displays
it; I have **not** discharged the graded/`Z_2` structure's effect on product-state existence, and I
do not claim to have. **The verdict does not rest on it**: the record's own two-factor theorem
(§3.2, opposite-lane verified) and member 20's two paragraphs reach the same disposition without my
construction. I record the attack and its caveat because an attack whose caveat is hidden is not an
attack.

**A second attack, shorter.** *"C34 determines it. Use G4."* Answered at §4.7 in three ways, of which
(ii) is dispositive and is not mine: **Q-900 already adjudicated exactly this and ruled the pinning
instrument-authored, not record-forced.** A lane that revived it would be re-arguing a settled
registrar ruling from the losing side.

---

## 6. PROPERTY P [PROVABLE — named, not invented]

This section is diagnosis-display item (c). **P is quoted from the record, not composed by me.**

### 6.1 P, stated precisely

Member 16's falsifier list, `[18972,19937)` (ANCHOR), span SHA-256
`a8e6fff44449e239a6e4231dd3365694c36302a964b751764ad5abf95f0d3a2d`, item 2, verbatim:

```text
2. proves a failure-capable physical condition, fixed before response output, whose
   admissible U1-invariant probability-measure set is a singleton;
```

and the same artifact's statement of its own residual ask, `[18260,18656)` (ANCHOR), span SHA-256
`349938c5c41f11f5e5c9c45aa42fb1a803b1dd014e51f7ad0aa0f1279b808954`, verbatim:

```text
The honest residual ask is not a choice between two candidates. It is a rule or
common-origin construction that reduces the U1-invariant probability-measure
simplex to one `omega_phys`, together with the corresponding concrete density,
domain, descent map, and provenance certificate.
```

So, stated as one property:

```text
PROPERTY P

  There exists a record-side condition Phi on states of B such that

    (P1) Phi is FAILURE-CAPABLE       -- some member of State(B) demonstrably fails it;
    (P2) Phi is FIXED BEFORE OUTPUT   -- frozen prior to any response/K4-K7/downstream reading;
    (P3) Phi is RECORD-FORCED         -- derived from the record's own premises, not authored
                                         for this purpose and not selected for its consequences;
    (P4) { omega in State(B) : omega U1-invariant and Phi(omega) } IS A SINGLETON;
    (P5) the singleton comes with its concrete density, state domain, descent map, and
         common-origin provenance certificate.

  P4 alone is the collapse.  P3 is what makes it a DERIVATION rather than a freeze.
  P1/P2/P5 are what make it lawful at this port.
```

**The record already tells us what such a property looks like when it works**, and this is the most
useful thing in this section. Member 20 at `[20416,20954)` (ANCHOR), span SHA-256
`70ba6600c3912de0a1c9b2d686e3aafe5637e32107117b64d9d489f5a488ada0`:

> *"The successful counterexample is different. `C_BI` adds unitality plus input-faithfulness to the
> already trace-preserving, mixing channel family. Unitality makes the normalized carrier identity a
> fixed state; uniqueness collapses the invariant-state image; input-faithfulness excludes even the
> replacement channel to that same state. A depolarizing family proves nonemptiness. This refutes
> universal impossibility while showing exactly why CP/Kraus/Stinespring richness **without an
> orbit-collapsing anchor** cannot do the requested work."*

**`P` is an orbit-collapsing anchor at the branch receiver.** The record has exactly one object of
that shape, it works, and it lives at `A_src`.

### 6.2 Where in the record P would have to live

Three loci, each named by the record itself, in ascending order of how much they would have to change:

1. **The target-independent admissibility manifest.** Member 09 row 23, `[27757,27987)` (ROW), span
   SHA-256 `9c90611ef0f3f6be385d3218c60ac13781552d12a0d6f5ff199bde0aaf425f4b`, types it `INPUT` with
   the reason *"must be frozen before downstream output; current law does not choose its complete
   member set; WATCH at build time for underdetermination that would require retyping."* **This is
   the record naming, in advance, the exact slot where a member-set-choosing rule would sit and
   recording that current law does not supply one.**
2. **`SM-8`'s coverage statement** (C14) — the place a producer must either certify completeness of
   the admitted family or declare a bounded working class. P4 is precisely what would let the first
   branch be taken.
3. **The anchor instrument's condition set** — where `C_BI`'s unitality-plus-input-faithfulness sits
   for the source sector, and where the branch-receiver counterpart would have to sit.

### 6.3 What is there instead

**At locus 1:** `NO_VERDICT`. Member 16 §5: `PHYSICAL_COMMON_ORIGIN_SUBFAMILY_COVERAGE_PROVED =
false | TYPE-U`, would-build *"concrete StatePort_U2_008 and its target-independent admissibility
manifest"* — the manifest that would host P is itself gated behind the port P would open.

**At locus 2:** an obligation with no discharger. `SM-8` is stated; no producer exists to carry it.

**At locus 3 — and this is the finding that decides section 6.** The record does own an
orbit-collapsing anchor, and it discloses its own provenance. Member 08 at `[1929,2350)` (ANCHOR),
span SHA-256 `7606251d28161747ed5dc33407baffafadc90b34881148dafb6ca302a73da723`:

```text
## HONESTY OF RECORD
A0 and the anchor are AUTHORED PHYSICS — confirmed non-derivable from the existing
transition structure (the commuting nonunital counterexample, independently verified).
The program adopts them knowingly, through the full gate: battery 11/3/0, two prior
versions killed by cross-lane review, convergence independently proven.
```

**The one property of P's shape that the record possesses was confirmed NON-DERIVABLE and adopted as
authored physics through a principal gate.** That is not a defect in the record; member 08 records it
as an honesty item and paid for it with a full battery and two killed versions. But it is decisive
for *this* relay's question, and I state the consequence in the narrowest form I can defend:

```text
P AT THE BRANCH RECEIVER = NAMED-ABSENT

  The record names P (member 16 §7 item 2; member 16 §6's residual ask).
  The record names the three loci where P would have to live (section 6.2).
  All three are empty of P.
  The nearest object of P's exact shape -- the source-sector anchor -- is disclosed by its own
  decision of record as AUTHORED, CONFIRMED NON-DERIVABLE, and adopted through a principal act.

  I do not conclude that P cannot exist.  I conclude that it is not in the record, that the
  record says where it would be, and that the record's own precedent for a property of this
  shape is a PRINCIPAL ACT rather than a derivation.
```

### 6.4 What P is not — four guards

- **P is not a preference.** Member 16 §7 forecloses this in terms: *"Mere preference for Haar,
  trivial-character evaluation, a product state, or any convex mixture does not satisfy that
  condition."*
- **P is not the common-origin certificate.** C16/C12 are provenance obligations on a supplied value.
  P is a condition whose *solution set* is a singleton. A certificate cannot narrow a family.
- **P is not `SM-3`.** Fixing the record marginal is the hypothesis of the theorem that proves
  non-uniqueness (§3.2). Any successor who proposes the record marginal as the collapse is proposing
  the refuted case.
- **P is not "build the missing objects."** C06 is the only failure-capable constraint against the
  continuum and it is `NO_VERDICT` in both directions; building `Omega_C0` without P yields, by
  §3.2, a `B`-marginal that ranges over the whole simplex. **Construction alone does not collapse a
  continuum; only a condition does.**

---

## 7. NO OUTPUT INSPECTION — certified, and disclosed [PROVABLE — law 10]

The assignment defines this so it cannot be crossed by accident, and I certify against its exact
words rather than paraphrasing them.

```text
NOT CONSULTED, AT ANY POINT IN THIS RELAY:
  what the joint trace, B10's aggregate, kappa_record, alpha, K7's verdict, or any battery
  would need omega_phys to be.  No such artifact was opened for that purpose or any other.
NOT DONE:
  no member of the continuum was ranked, filtered, scored, preferred, or approached;
  no candidate state was named and then grounds sought for it.  The direction of every step
  in section 4 is grounds -> disposition, never candidate -> justification.
```

**Two disclosures, because a certification with an undisclosed near-edge is worth less than one
without.**

1. **I read member 22's `[38169,39014)` span, which contains G4 *and* G5.** G5 lists K1–K3
   preconditions *"on the actual `omega_hist` descendant"* — test names and construction order, no
   value, no output, no verdict. I read it because it is inside the span G4 occupies and excising it
   would have been a silent convention. It supplied nothing to any disposition here, and I disclose
   it rather than let a checker discover an unmentioned K-row in my closure.
2. **I read register entries Q-900, Q-909, Q-926 and Q-936.** These are rulings about the section's
   *process and status* — what is settled, what is open, whose act B03 is. None reports a downstream
   numeric result and none states what `omega_phys` should be. Q-909's *"B03 is a principal act"*
   was read and is addressed at §1.3 rather than quietly used or quietly ignored.

```text
OUTPUT_INSPECTION = NONE-CERTIFIED (+2 disclosures, neither load-bearing)
```

---

## 8. What this relay did not do [PROVABLE]

Stated affirmatively so nobody has to infer it from silence.

- **No freeze is proposed and none is hinted.** The choose branch is Brian's act after diagnosis and
  conversation. Nothing in this artifact is a recommendation toward it, and section 6.3's last
  paragraph is a finding about the record's precedent, **not** an argument that a freeze is now due.
- **No candidate state was named.** `omega_H`, `omega_epsilon` and `omega_t` are carried strictly as
  member 16's separation witnesses, with member 16's own bar on their promotion quoted at §3.1.
- **No ranking is hinted.** No member of the continuum is described as more plausible, more natural,
  more physical, or closer than any other. The family is displayed and left flat.
- **`H_omega`, `pi_omega`, `rho_pre`, `Omega_C0`, `d_joint`, `d_state_Omega`** — none constructed,
  sketched, staged, or named as a candidate.
- **No stop was lifted.** B02's three stops, B01's obligation, the four-field stop, and the demand
  map's closure all stand exactly as they stood.
- **No correction was repaired in place.** §4.7's central finding is reported; supersession is the
  registrar's act.
- **PE-1..PE-17 pointer-only.** No expectation text was opened or assigned verdict weight. Q-926's
  note that PE-17 carries zero verdict weight until B03 is honoured: this stop is **not** a verdict
  and **not** a request for a principal choice.

---

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the B03 row, the selector row, and the descent stop rule at their bytes; the port's nine fields,
  its two supply conditions and its would-build; the placement square and MISSING_COMPOSITION;
  member 16's exhaustive census, its four TYPE-R results, its survivor ledger, its coverage
  boundary and its falsifier list; member 17's non-uniqueness theorem, SM-1..SM-8, and its
  FRAGMENTS verdict; member 21's independent verification and its wall-to-wall ruling; member 20's
  Family-C no-go table, its slice/purification paragraphs and its orbit-collapsing-anchor result;
  member 19's field-by-field inhabitance audit; DoR-013's family-level adoption, no-member clause,
  p_ch-neutrality license, three maps, and HONESTY OF RECORD disclosure; member 22's G4; member
  11's G2 and its three stops; member 10's binding conditions; member 14's read rule; Q-900,
  Q-909, Q-922, Q-926, Q-932, Q-936; Q-98's countermodel discipline; all charter fences.

DERIVED HERE:
  the receiver-first hunt and its declared boundary (section 2);
  the assembly of two INDEPENDENT continuum proofs at two receivers, with the flattening guard
    separating the non-uniqueness theorem from member 19's receiver-side inhabitance row;
  the thirty-eight-constraint ledger, its five-word disposition vocabulary, and each row's
    disposition against its own ground;
  the identification of C34/G4 as the ONE rule of record that determines omega_phys, and the
    three independent reasons it does not close this relay -- of which the dispositive one is
    Q-900's existing ruling, not a new argument of mine;
  the WRONG-ALGEBRA typing of C18/C19: the record's real, proven uniqueness result sits at
    State(A_src) and member 19 types its transport failure WRONG CODOMAIN;
  the two-marginal attack of section 5 and its declared graded-tensor caveat;
  the location of P at three named loci and the finding that the record's nearest object of P's
    shape is disclosed as authored and confirmed non-derivable.

SELECTED HERE:
  nothing.  No state, member, measure, functional, candidate, density, representation, domain,
  descent map, certificate, manifest, producer, anchor member, instrument amendment, freeze, or
  selector value is chosen, supplied, ranked, recommended, preferred, or proposed for freezing.
  No basis row is reordered.  No stop is lifted.  No gap is bridged.  No status moves.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  No rank, ratio, fiber, or
  physical quantity was read, formed, or evaluated.  Every scale symbolic.
```

---

## 10. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.**
  - A **receiver-side existence statement** was not flattened into the **continuum proof** — §3.4
    exists solely to keep member 19's row and member 16/17's theorems apart, and the handover named
    this as the trap before I reached it.
  - A **uniqueness result at `A_src`** was not flattened into a uniqueness result at `B` — C18/C19,
    the ledger's sharpest rows, and member 19's own **WRONG CODOMAIN** typing.
  - An **instrument-scoped determination** was not flattened into a **record-native derivation** —
    C34, and member 21's `SECTION_WALL_TO_WALL_RECORD_NECESSITY = not established`.
  - **Missing construction** was not flattened into **physical refutation**, in either direction:
    C06 is `NO_VERDICT`, not zero and not one, per member 16's DoR-006 guard.
  - An **obligation on a producer** was not flattened into a **constraint on the produced state** —
    the whole of the OBLIGATION column.
  - A **separation witness** was not flattened into a **candidate** — §3.1, §8.
  - A **stop** was not flattened into a **verdict**: per PE-17 this triggers a both-lane diagnosis
    pass, and the working prior is ERROR BEFORE NEW PHYSICS.
  - A **`TYPE-R` refutation** was not flattened into an **impossibility theorem**: member 16's
    `TYPE-R` is scoped to the executable constraint set and refutes *forcing*, not existence.
  - A **span convention** was not stretched silently over shapes it does not fit — three shapes
    declared in the closure and each span tagged.
- **VACUITY CHECK, run explicitly (Q-98).** Nothing here is reported as passing. The stop is not a
  name-match: it is grounded in two theorems with witnesses, one of them opposite-lane verified, and
  it was attacked twice in section 5 with one attack's caveat left visible.
- **BUILDER-NEVER-VERIFIES:** everything CLAIMED until the Codex 2 check. I did not re-adjudicate
  member 21's rulings, member 20's exclusions, Q-900's adjudication, or Q-926's `Params_B0`
  correction; I consumed them.
- **B01 ROWS AT SOURCED STATUS (Q-922):** no B01 row is promoted or re-typed here. `Params_B0` is
  carried **OPEN** per Q-926, not FORCED per B02's superseded claim.
- **MEMBER 12 READ RULE (Q-931/Q-935):** member 13 was read only with member 14 and DoR-013;
  `status(member 13) := RATIFIED_FAMILY_LEVEL_BY_DOR_013` with the no-member clause carried;
  member 14's status predicate is pinned at `[15736,15834)`, span SHA-256
  `a9675e42c2352e692b84f5a6ad238b29da1296cc1c969ae8ca5a9eacf49396a5`. The assignment cites the read
  rule through overlay V001; per member 02's pin, **V002 governs**, and it carries V001's rule
  byte-identical.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **CUSTODY:** archive-side only. Two files written in `workspace/`, both sealed. No register, plan,
  tracker, git, commit, or push action. Nothing adopted, nothing registered.
- **NO SUBAGENT DELEGATION.** Every read, hash, and disposition in this artifact was performed in
  this session directly.
- **BYTE AUDIT:** all 23 members rehashed at their full archive-root paths at seal time by member 23.
  All 38 spans located and computed by member 23; **10 FIXED spans re-verify upstream pins and
  10/10 MATCH** — the two audit rows against the handover, the U2 status block and placement square
  against the assignment, the StatePort spec, the census span, the non-uniqueness theorem against
  member 21's pin, the inhabitance audit, DoR-013's clause against member 11's and member 14's pins,
  and G4. Closure declared at byte 0 with its end computed on bytes as a fixed point under a
  fixed-width field; 27-token pre-closure scan over authored prose, 0 hits.
- **RESIDUE, per Q-921 — and this is the one place my first draft was wrong, caught before sealing.**
  The removed-class grep over the vocabulary Q-913/Q-917/Q-930 retired was run over authored prose
  (fenced blocks excluded) before sealing. **My draft asserted zero hits. IT RETURNED EIGHT.** Every
  one of them fell in two loci that were *my own enumeration of the retired class names* — in this
  bullet and in §11, where I had listed the classes in order to say I was not asserting them. That
  is exactly relay 1002's self-catch, which Q-921 carries: **a report that names what it swept can
  reintroduce it.** I reworded both loci rather than rule my own fresh text lawful; ruling it was not
  my call to make, and V008 was corrected on that precise point. **After the rewording the grep
  returns FIVE RAW HITS, and I report them as raw hits rather than as zero**, because collapsing the
  distinction is what the V007 gate refused to do. **The scan's scope is DECLARED: authored prose,
  fenced blocks excluded.** All five are forms of the verb *reproduce* carrying a **live bounded
  receiver**, and I list them exhaustively rather than characterise them: (1) member 17's `SM-3`
  wording at C09, *"reproduces the admitted `M_3` record state"*; (2) DoR-013's `d_law` wording at
  C21; (3) the two-marginal construction at §5; and (4)–(5) **the two occurrences inside this bullet
  itself** — the naming of the verb one line above, and the C09 quotation in item (1). I count my own
  sentences rather than exempt them, which is the whole lesson of this paragraph and is why the
  number is five rather than three. **THREE FURTHER OCCURRENCES SIT INSIDE FENCED BLOCKS** and are
  outside the declared scan scope by that convention: two in the closure declaration (the span-shape
  statement and the generator's refusal rule) and one inside the DoR-013 quotation at §4.4. I
  enumerate them so that a checker running an unscoped pattern set finds **eight** and finds nothing
  I did not disclose. None of the eight is the retired *process-history* class, which claims a build
  re-runs to identical bytes; this document makes no such claim and carries no historical subsection
  for that vocabulary. **The whole result is
  CLAIMED, not clean** — the hand that wrote the prose ran the check, and the gate settles on the
  opposite lane, whose pattern set is not authored by me.
- **GENERATOR CUSTODY (Q-920/Q-924):** member 23's declared inputs are exactly members 01–23 at full
  paths; it opens nothing else — no directory walk, no environment, no network. It refuses to emit
  any table if an input is unreadable, an anchor is absent **or ambiguous**, or a FIXED span misses
  its upstream digest. **It refused once during this relay** — S27's start anchor spanned a line
  break — and emitted nothing until the anchor was corrected. The refusal is reported because a
  generator that never refused is a generator whose refusal path is untested.

---

## 11. The certificate

Per Q-913, Q-917, Q-921 and Q-930, terminal: **the certificate is the digest table.** Members 01–23
at their full archive-root paths, and the thirty-eight span digests quoted in place with their
computed offsets and their declared shapes. The four claim classes those questions retired are named
nowhere in this document and none of them is asserted here — no comparison-arithmetic claim, no
byte-carriage claim, no removal claim, no membership-delta claim. Spans are published **with digests
on both sides**, which is the standard that replaced them. **There is nothing below the digests.**

---

## 12. Final lines

```text
CLOSURE = declared-first (byte position, scan)
     byte 0; closure end 7480 under a fixed-width field; 27-token pre-closure scan over authored
     prose, 0 hits, CLAIMED per Q-921.
OUTCOME = STOP-SELECTOR-UNFROZEN
     Stop-rule clause 3 fires: no sealed record-native derivation exists and a prospective principal
     freeze is not this lane's act.  PER PE-17 THIS IS NOT A VERDICT AND NOT A REQUEST FOR A
     PRINCIPAL CHOICE: it triggers a both-lane diagnosis pass, working prior ERROR BEFORE NEW
     PHYSICS -- the maps, the reading, or the framing is suspect before the record is.
CONTINUUM = DISPLAYED-AT-BYTES
     TWO INDEPENDENT PROOFS AT TWO RECEIVERS.  (1) Member 16: B iso C(Y) with Y compact metrizable;
     Riesz-Markov gives an EXHAUSTIVE census, normalized positive functionals <-> probability
     measures on Y; cardinality EXACTLY the continuum, both bounds proved; the separation witness
     omega_t with omega_t(b_lambda)=t; UNIQUE_SCALARIZATION_FORCED = false | TYPE-R -- a
     constructive REFUTATION of forcing, not an absence of argument.  (2) Member 17 [15554,15965)
     e9dafa59...: for every omega_B in State(B), omega_out tensor omega_B has the same record
     marginal -- an algebraic non-uniqueness theorem, INDEPENDENTLY VERIFIED by member 21 and
     re-stated as a row of member 20's own no-go table.  NOT THE PROOF, AND PINNED AS SUCH:
     member 19's row "B is isomorphic to C(Y) and its entire state simplex exists | UNBOUND" is a
     RECEIVER-SIDE inhabitance statement; conflating it with the theorems would credit the record
     with proving a size it does not prove there and would retype the obstruction as a shortfall
     of construction.
CONSTRAINTS_CONSUMED = 38-EACH-DISPOSITIONED
     9 CUTS-NOTHING (five of them TYPE-R in the record's own words) | 10 OBLIGATION on the producer
     rather than the produced state | 2 WRONG-ALGEBRA | 4 CONSUMES-THE-FREEDOM | 4 NOT-A-COLLAPSE-
     CONDITION | 7 NOT-BEARING | 1 CANNOT-RUN | 1 DETERMINES-CONDITIONALLY.  COLLAPSING CONSTRAINTS
     FOUND = 0.  One tally slip is corrected in the open at §4.9 rather than silently re-typed.
     THE CENTRAL FINDING: C34.  Member 22's G4 [38169,39014) ae1f79d9... is the ONE rule of record
     that determines omega_phys -- "no independent omega_B may enter" -- and it does not close this
     relay for three independent reasons: (i) it computes from Omega_C0, which member 17 says the
     corpus does not supply, so it RELOCATES the freedom rather than collapsing it; (ii) Q-900
     already adjudicated this exact pinning INSTRUMENT-AUTHORED, NOT RECORD-FORCED, and member 21
     records SECTION_WALL_TO_WALL_RECORD_NECESSITY = not established; (iii) its scope is one
     instrument, and instrument-scope is not record-scope.  Reported, NOT repaired in place.
     THE SHARPEST NEAR-MISS: C18/C19.  The record DOES force a unique state -- the anchor family's
     p_ch-neutrality certificate forces the same normalized-identity invariant state for every
     member -- at State(A_src).  Member 19 types the transport WRONG CODOMAIN and types the source
     positivity/unit-trace pass "PASS ONLY AT SOURCE-SECTOR SCOPE; not a joint-state certificate."
     ATTACKED (Q-98): combining the forced source marginal with SM-3's forced record marginal still
     leaves the third factor free; caveat on the graded tensor structure DECLARED, and the verdict
     does not rest on my construction.
COLLAPSE_PROPERTY = NAMED-ABSENT
     P is the record's own falsifier, quoted not composed: "a failure-capable physical condition,
     fixed before response output, whose admissible U1-invariant probability-measure set is a
     singleton" (member 16 [18972,19937) a8e6fff4...), with the residual ask at [18260,18656)
     349938c5... naming the density, domain, descent map and certificate that must come with it.
     Its shape has a working precedent: member 20's C_BI -- unitality plus input-faithfulness, an
     ORBIT-COLLAPSING ANCHOR.  WHERE P WOULD HAVE TO LIVE: the target-independent admissibility
     manifest (member 09 row 23, whose own reason reads "current law does not choose its complete
     member set"); SM-8's coverage statement; the anchor instrument's condition set.  WHAT IS THERE
     INSTEAD: at the manifest, PHYSICAL_COMMON_ORIGIN_SUBFAMILY_COVERAGE_PROVED = false | TYPE-U,
     gated behind the very port P would open; at SM-8, an obligation with no discharger; at the
     anchor, THE DECISIVE ITEM -- DoR-013's own HONESTY OF RECORD [1929,2350) 7606251d... discloses
     A0 and the anchor as "AUTHORED PHYSICS -- confirmed non-derivable from the existing transition
     structure," adopted knowingly through a full gate.  THE RECORD'S ONE OBJECT OF P'S SHAPE WAS
     NOT DERIVED.  I do not conclude P cannot exist; I conclude it is not in the record, the record
     says where it would be, and the record's own precedent for it is a principal act.
OUTPUT_INSPECTION = NONE-CERTIFIED
     No downstream object opened; no member ranked or filtered by downstream fitness; no candidate
     named then grounded.  Two near-edges DISCLOSED rather than left for a checker to find: G5 sits
     inside the G4 span I read (test names and order, no value, load-bearing nowhere here), and four
     register rulings were read as process/status, none reporting a downstream result.
COMMON_ORIGIN = N/A
     No state was supplied, so no common-origin certificate was owed, attempted, or asserted.
CHAIN_INVOKED = false
     The inbox holds no further paste addressed to the DARIO lane behind this one; 1021 is DONE and
     1022 is Codex 2's.
VERB_AUDIT_SELF = CLEAN
     "Derived" occurs only in the FREEDOMS block for the reading work actually performed, in sealed
     status names, and in quotations.  No selector was selected, no member approached, no route
     chosen, no downstream result evaluated, and no stop lifted.
```

All findings CLAIMED until the opposite-lane check.

**The step to press first is section 2's hunt boundary, and I want to name it precisely because it is
the one place this display could be complete-looking and incomplete.** The ledger is exhaustive over
constraints found by a receiver-first hunt that enumerated artifacts naming both `omega_phys` and the
branch algebra. A constraint bearing on `State(B)` that names neither — a condition stated purely in
terms of `Omega_C0`, or of the U3 quotient, or of a finite cylinder algebra, that happens to
restrict the branch marginal — would not have appeared. I have no candidate for such a constraint
and I looked for one; I record it as the residual rather than manufacture a doubt I cannot
instantiate. **My own 1015 overlay pinned ten surfaces and read as complete while sixteen were
missing, and the lesson I took from it was that the count is only ever as good as the declared
family it ranges over.** This ledger's declared family is "artifacts naming the branch state," it is
disclosed here rather than left implicit, and **testing it is the check's cheapest high-value move.**

**The second thing to press is narrower and is mine.** Section 5's two-marginal attack carries a
declared caveat about the graded tensor structure that I did not discharge. The verdict does not
depend on it. But if a lane wants one clean thing to verify rather than thirty-eight, that
construction is the one piece of mathematics in this artifact that is mine rather than the record's.
