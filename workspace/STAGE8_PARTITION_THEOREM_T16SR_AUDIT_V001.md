# STAGE 8 — PARTITION THEOREM (T16SR) — ADVERSARIAL AUDIT (PARTITION-AUDIT) V001

DATE: 2026-08-15
COMMISSION: PARTITION-AUDIT of T16SR. POSTURE: DEFAULT-REFUTE; testimony zero weight;
every claim re-derived at bytes; every symbolic step re-run in a fresh venv.
STATUS: DETERMINATION ONLY — no adoption, no authored physics, no value, no number as a
value of anything.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: this file's path probed ABSENT before first write (ls: "No such file
or directory", both .md and sidecar).

---

## §A0 — STEP 0: THE CLAIMED ARTIFACT

```text
PROBE: STAGE8_PARTITION_THEOREM_T16SR_V001.md            PRESENT (36231 bytes)
       STAGE8_PARTITION_THEOREM_T16SR_V001.md.seal.sha256 PRESENT (105 bytes)
shasum -a 256 -c: OK  (5e12af0b28d378fb20437d1416017771449cae853fdc0960dac6c7efd3b0c895)
=> STEP 0 PASS. Artifact read in full after verification.
```

## §A1 — GROUND RE-CONSUMED INDEPENDENTLY; ALL SEALS RE-VERIFIED AT MY OWN HANDS

```text
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md         e1c2ac80  (survey pair, half 1)
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md   0057b134  (survey pair, half 2)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md              15148ab5  (vacancy pair, half 1)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md        217ac664  (vacancy pair, half 2)
OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md 05c832ad (demand-reading)
OK  supervision/SURFACE_DEFINITION_OF_RECORD_V003.md    c80c09a2  (displayed-clause carrier;
    full digest c80c09a2078fc5fb40b6695943f29262306c4e2d6dddb6563e6a28d42b747181 ==
    T16SR's recorded digest; sidecar verified BEFORE reading)
MISMATCHES = 0. Every §0 short-digest in the claimed artifact matches my own hash run.
Surface file resolves to the ARCHIVE root's supervision/ (not the cleanroom's) — same
digest T16SR recorded; path ambiguity noted as F-5 below (finding, correction-grade).
```

Reads executed this commission: T16SR artifact IN FULL; T14SR build + audit IN FULL; AJ build
+ audit IN FULL; forcing-boundary adjudication at its cited lines (1-75 incl. lines 24-25 and
54-57 at bytes); surface §I-§II IN FULL plus §VII as carried in the sealed file (consumed only
to adjudicate T16SR's corroboration-only claims, supplying no premise below).

## §A2 — GROUND CROSS-CHECKS (the consumed artifacts against T16SR's §0/§1/§5 claims)

```text
G-1  T14SR audit verdict line: "OVERALL = CONFIRMED-WITH-CORRECTIONS"; D4 meta-verdict
     CONFIRMED; findings F-1..F-5 all correction-grade, none verdict-bearing — T16SR §0's
     carriage ACCURATE at bytes.
G-2  AJ audit verdict line: CONFIRMED-WITH-CORRECTIONS; C-1/C-2/C-3 "none load-bearing";
     "Convergent verdicts by independent routes" — T16SR §0 ACCURATE at bytes.
G-3  AJ M8 list ("the three lock equations, the character map, the transition/cocycle law,
     the conjugate-pair closure, the orbit action") == T16SR's Σ1-Σ7 clause-for-clause. MATCH.
G-4  AJ Theorem II wording: "extend the displayed ground structure by the response profiles";
     "Derivability from the ground implies truth in every model of the ground" — both verbatim
     at AJ §6.2. T16SR §2's model notion is the AJ's own proof form. MATCH.
G-5  AJ §8.2: C12 content as T16SR §5.1 describes; "Theorem II's conservativity core does NOT
     depend on C12 — it rests only on the displayed equation set" verbatim; finite-text warrant
     marked [DERIVED]. T14SR §5.1 carries the parenthesis T16SR quotes. ALL MATCH.
G-6  AJ §8.3 supply-route sentence ("a booked clause in which a response magnitude and
     phase-sector data co-occur") verbatim as T16SR §6 quotes it. MATCH.
G-7  AJ audit X7 sentence quoted in T16SR §3.5 — VERBATIM MATCH (AJ audit C-1). X11 =
     junction-free expansion models every displayed clause — T16SR's third model carries it
     faithfully. C-2's fiber-local re-scope of Gleason kill 2 — T16SR S13(c3) carries it at
     the corrected scope. ALL MATCH.
G-8  T14SR A-X-B ("every displayed clause used holds at SYMBOLIC integer n: no clause pins
     |n|") — T16SR's class-vs-member boundary carries it accurately; A-X-A is J-2 material
     and T16SR cites it nowhere in the proof (grep-confirmed: sole mention is §6's fence).
G-9  Demand-reading lines 24-25 and 54-57 re-read at bytes; T16SR consumes the file as
     ground only and never mis-cites it; its (I)-claim forces a CLASS, not a number —
     consistent with the sealed forcing taxonomy (admissible set = ℤ, not a point; the T14SR
     R-1 "terminates at a class" carriage).
G-10 Surface bytes vs T16SR §1: Σ1 (SII.9), Σ2 (SII.9), Σ3 (SII.7 — the SII.9 co-cite is
     loose: τ_R is not among SII.9's four listed locks), Σ4/Σ5 (SI.3), Σ7 (SII.7) all present
     as displayed equations; Σ6 NOT displayed anywhere in §I-§II — it is a consequence of the
     character reading of Σ4's definitional pair, and T16SR itself displays this at CH-5.
     D-1..D-6 dispositions match the surface bytes (SI.1 bans; SII.6 sentence; SII.7
     invariance naming; SII.8 booking; withheld-form list; L2-type declarations).
G-11 The strict displayed-equation set of the WHOLE sealed file exceeds Σ: §VII.16 displays
     the transition action δ → δ+2θ and the invariant H_R·T_R/ℏ; §VII.17 displays the
     d₀-square certificate; §VII.18 displays J_FS,rel ≥ πℏ/2 and a K-coefficient form. T16SR's
     Σ is scoped to the GROUND (§I-§II + displayed locks — the AJ's M8 object), which is the
     correct scope of every consumed theorem; but §5.2's phrase "the COMPLETE displayed
     equation set of the sealed surface" overreaches at bytes if "sealed surface" is read as
     the sealed FILE. See F-3.
```

## §A3 — THE SYMBOLIC STEPS, RE-RUN TWICE (fresh venv; exact symbolic; zero numerics)

### §A3.1 — Byte re-run of the build's own battery

The build displays only its script's sha256 (84e277da...), not the script. The script was
LOCATED on disk this commission (scratchpad process file `t16sr_battery.py`), hashed at my own
hands to exactly `84e277da5bae0ed8d2a4aa...b26c` == the displayed digest, read in full at
bytes, and re-run in a fresh venv (sympy 1.14.0):

```text
RE-RUN OUTPUT vs the artifact's embedded output block: diff = IDENTICAL (22/22 PASS).
Script scans: no evalf, no N(), no float literal (only the fence comment matches);
exact rationals (1/2, 1/3, 1/4, -2) are declared algebra residuals. CONFIRMED.
```

### §A3.2 — Independent battery (my own formalization; the build's script NOT copied)

Fresh venv, sympy 1.14.0; script `pa_t16sr_audit_battery.py`, sha256
`e0eece26fa13fc30f2c0d001487ef271082a52c8b6caf1cde18023ef679f3fe4`; differences of
formalization: τ_R kept a SYMBOL constrained by Σ3; transition action exercised in BOTH
readings (sector-blind z→gz as Σ5 displays; sector-covariant g^n as the AJ P1/C-A carries);
class membership checked as true membership in the solved set; the build's reported PT3d
domain artifact reproduced. Output, verbatim:

```text
PARTITION-AUDIT T16SR INDEPENDENT CAS -- exact symbolic, zero numerics (sympy)
[A-PT0a] PASS -- FULL three-lock system (tau_R symbolic) consistent; unique solution over {m_*, |DS|, tau_R} with T_R, hbar free: Sigma non-vacuous, one free scale direction
[A-PT0b] PASS -- orbit coherence: T_R -> lam T_R carries the solved structure to a solved structure (m_* co-moves as pi/T_R must; tau_R and |DS| orbit-fixed): the ground models Sigma and the orbit maps models to models
[A-PT1a] PASS -- TRUE Sigma inventory = the build's claimed set PLUS tau_R (Sigma-3's own symbol): the build's 'exact' inventory omits tau_R -- finding F-2, exhibited mechanically
[A-PT1b] PASS -- the omission is real: tau_R absent from the build's displayed inventory
[A-PT2a] PASS -- corrected inventory still carries NO junction/response symbol: tau_R is lock-side; S7/S8's vocabulary-disjointness argument is UNAFFECTED by F-2 (substance intact). [Formalization-relative half: that the surface's D-3/D-4 SENTENCES contain no phase-sector symbol is a text fact, verified by inspection in the audit body]
[A-PT2b] PASS -- NEITHER the profile symbol NOR the toy argument Delta occurs in ANY of the seven clauses (both halves checked -- completing the build's PT5h, whose displayed check omitted the Delta-half on the phase laws)
[A-PT3a] PASS -- closure at SYMBOLIC integer n: exp(2*pi*i*n) == 1, all n at once
[A-PT3b] PASS -- n = 1/2 fails exactly (residual -2); n = 1/3 fails (nonzero residual)
[A-PT3c] PASS -- label set of the closure equation is EXACTLY Integers over C, and over R via the real closure component (cos form; equivalent over R since cos = 1 forces sin = 0)
[A-PT3d] PASS -- the build's REPORTED domain artifact reproduced: solveset of the complex exponential directly over R returns {0}, provably not the solution set -- the build's SS4 repair note is accurate at bytes and its repaired forms are the sound ones
[A-PT3e] PASS -- the definitional pair {+1,-1} inhabits the SOLVED class (true membership check, replacing the build's set-vs-itself PT3e form); a non-integer is NOT a member (discrete label set, not a continuum -- the (III)(c1) discreteness at CAS grade); conjugate closure magnitude-free
[A-PT4] PASS -- drive datum invariant under BOTH readings of the transition law -- sector-blind (Sigma-5 as displayed: z -> g z) AND sector-covariant (g^n, the AJ P1/C-A carried reading) -- and unit-modulus at symbolic n: the build's PT4/PT5e/PT6 conclusions are READING-ROBUST; its 'Sigma-5's covariance' label (F-1c) is immaterial
[A-PT5a] PASS -- f1 == sin^2(Delta/2) exactly; both profiles real (magnitude-valued)
[A-PT5b] PASS -- f1 != f2 as functions; exact separation residual 1/4 at the toy argument
[A-PT5c] PASS -- both profiles orbit-fixed (beta-invariant), checked against the corrected inventory including tau_R
[A-PT5d] PASS -- both profiles attain 0 and 1 exactly (normalize exactly; endpoints are algebra residuals of the toy objects)
[A-PT5e] PASS -- no logarithm anywhere in the toy construction (SI.1 honored)
[A-PT7a] PASS -- every native composite unit-modulus at symbolic n (SI.3 + SII.6 displayed sentence)
[A-PT7b] PASS -- G positive (J-mag's own R>=0 typing): G == 1 uniquely; G merely real-nonzero: {-1, +1} -- the corollary's uniqueness consumes the magnitude typing, displayed
TOTAL: 19 checks; FAIL count = 0
```

Every algebraic step of the build's §3 proof and §4 battery is thereby re-derived at an
independent formulation, most at equal or stronger grade (membership, both-readings
robustness, full-system locks, completed Delta-half). The textual/logical steps (S2, S5, S8,
S11-S13) were walked by hand against the surface and AJ bytes (§A2, §A4, §A5).

## §A4 — FENCE-SCAN AND NUMBER AUDIT (at bytes)

```text
FENCE-SCAN of the claimed artifact: "alpha" hits = the two fence-declaration
  lines + the archive path string — ALL lawful, no alpha value, computation,
  or comparison anywhere. "lens" hits = 3, ALL inside the §6 NO-LENS scope
  declaration and the flag block's "no lens" (naming-to-fence only; no lens
  reading named, used, or motivated; conforms to the discipline the T14SR
  audit's F-2 asked for — lens tokens confined to discipline blocks).
  FORBIDDEN TOKENS (137|thomson|K_KK|planck|ell_P|electron|QED|4pi|sphere|
  carleman|S16|measured|fine-structure): ZERO hits. FLOAT PATTERN: every hit
  is a §-reference or the sympy version string; no numeric float. evalf/N():
  only the fence-declaration sentence. Script (recovered, hash-matched):
  same scans CLEAN.
NUMBER AUDIT ("any number" hunt): every number in the artifact traced —
  2π, ±1, 1/2, 1/3, 1/4, −2 (declared algebra residuals of identity checks;
  same objects the sealed AJ battery displays); G = 1 (the sealed AJ T1
  triviality corollary re-derived, carried with provenance — the same
  license the T14SR audit adjudicated PASS); dim-1 (sealed AJ §6.3.2,
  quoted); 22/19 checks, 167 lines, five artifacts, section numbers, dates,
  digest prefixes (structural/process counts). NO number appears as a value
  of anything; none authored; no measured-constant comparison. CLEAN.
SCOPE-CREEP HUNT (extent site J-2): the build's body and battery contain NO
  extent computation, no ker/dim work, no A-X-A consumption (grep + read:
  sole mentions are §6's out-of-scope fence and the §0 reading note). The
  survey's JUNCTION-CONTINGENT overall verdict and J-2's decided-of-record
  movability are untouched at bytes. CLEAN.
RESIDUE-ABSORPTION HUNT: §5 displays both residues and disposes each
  explicitly (R-A non-consumption table — walked step-by-step against §3 at
  bytes this commission, accurate: no step of (I)/(II)/(III) consumes
  SII.6's scope, and the ONE consumer (COR-2) is outside the core with its
  premise named; R-B re-performed enumeration — re-performed AGAIN here,
  §A2 G-10/G-11, concordant). What C12 still conditions is displayed with
  its discharging object named; the AJ's record status is untouched. NO
  silent absorption found. The one DAG gap is F-1 (an interpretive premise,
  named below), not a residue.
```



## §A5 — FINDINGS (all correction-grade; none verdict-bearing)

```text
F-1  THE UNNAMED INTERPRETIVE LAYER (the commissioned unnamed-clause /
     quantifier-gap hunt, landed at correction grade). Three components:
     (a) S3 ("exact algebra") consumes the character FUNCTIONAL FORM
         χ_x(h) = exp(ixh) for general labels x — the identification of
         "sector/winding label" with the exponent of the continuous U(1)
         character family. That form is not among Σ1–Σ7 as enumerated and
         is not a named logical step; it is carried by SI.3's "Holonomy
         characters" heading under standard denotation, by the sealed AJ's
         own displayed script (chi = exp(I*n*h), commented "(SI.3)"), and
         by the sealed corroboration T16SR itself demoted to
         corroboration-only (§VII.15).
     (b) S2's warrant token is mis-cited: "it is Σ4's '∈ U(1)' read as
         displayed" — at surface bytes the "∈ U(1)" token attaches to
         g_ij in the TRANSITION half (T16SR's own Σ5 line carries it; its
         own Σ4 line displays "∈ {+1,−1}", not "∈ U(1)"). The U(1)-domain
         typing of χ_n rides the "characters"/holonomy vocabulary, not
         that token.
     (c) PT4/PT5e/PT6 implement the sector-covariant transition g^n and
         label it "Σ5's covariance"; Σ5 at bytes is sector-blind
         (z_j = g_ij z_i). The g^n law is the AJ P1 / C-A CARRIED reading.
         Proven immaterial here: A-PT4 shows every consumed invariance
         holds under BOTH readings.
     CONSEQUENCE: the hygiene headline "every proof step passes through a
     named clause of §1 or a displayed logical step" fails at exactly this
     layer, and the CHOICE LEDGER lacks the corresponding entry. The
     require-half's "in EVERY model of Σ" is honest over the model class
     in which the character notation carries its standard (continuous,
     exponential) denotation — the sealed tower's own uniform reading, and
     arguably what §2's model notion already implies (χ is absent from
     §1's symbol inventory, hence fixed notation, not free vocabulary) —
     but the clauses AS ENUMERATED, with χ read as free unit-modulus
     vocabulary, admit wild-character structures over which claim (I)'s
     wording loses its referent ("label admissible under the closure"
     presupposes the exponential family). (II), (III)(b), (III)(c2),
     (III)(c3) are denotation-robust (vocabulary disjointness only).
     FIX (one sentence): name the reading as a premise — "Σ4-READING
     (named): χ_n are the continuous U(1) characters, labels the
     exponents; carried by SI.3's own vocabulary, the AJ's sealed
     implementation, and §VII.15" — or add the CH-entry. No verdict
     changes over the repaired statement.
F-2  PT1's DISPLAYED CLAIM "Sigma symbol inventory exact:
     {m_star,T_R,H_R,hbar,dS_abs,lambda} ∪ {h,n,theta}" OMITS τ_R — Σ3's
     own displayed symbol ("τ_R = π/√2" at SII.7). Cause visible at script
     bytes: Σ3 formalized with tau_R pre-substituted (= π/√2), collapsing
     the clause to 0 == 0; the same artifact makes PT0b's "tau_R
     orbit-fixed" a derivative-of-a-constant vacuity. Exhibited
     mechanically (A-PT1a/b); non-vacuous forms supplied (A-PT0a/b);
     substance of S6/S7/S8 UNAFFECTED (τ_R is lock-side vocabulary —
     A-PT2a). FIX: add τ_R to the displayed inventory; "exact" then true.
     (Σ1's "SII.9"-co-cite for Σ3 is loose the same way: τ_R is SII.7's,
     not among SII.9's four listed locks.)
F-3  §5.2's SENTENCE "Σ1–Σ7 is the COMPLETE displayed equation set of the
     sealed surface": overreach at bytes if "the sealed surface" is read
     as the sealed FILE — §VII displays further equations (δ → δ+2θ;
     H_R·T_R/ℏ; the d₀-square certificate; J_FS,rel ≥ πℏ/2; the
     K-coefficient form), none in Σ. The correct scope — the GROUND,
     §I–§II + displayed locks, the AJ M8 object — is what §0.1 declares
     and §6 binds. Also §1's Σ6 row: the conjugate closure is NOT a
     displayed equation of §I–§II at bytes; it is a one-step consequence
     under the F-1 reading — T16SR itself displays this at CH-5
     (follows AJ M8 verbatim; immaterial either way: it adds no
     vocabulary and holds in every model). FIX: scope the §5.2 sentence
     to "the sealed surface's ground (§I–§II + displayed locks)".
F-4  NEAR-TAUTOLOGICAL CAS DISPLAYS graded as mechanical completions:
     PT2b's check is disjointness of script-INVENTED response-name
     symbols from phase symbols (true by construction; the byte-grade
     fact — the surface's D-3/D-4 sentences carry no phase-sector
     symbol — rests on the inspection §1 names, verified again here);
     PT3e's first conjunct compares {1,−1} with itself (membership
     content vacuous as coded; true membership supplied at A-PT3e);
     PT5h's displayed sentence promises the Delta-half on the phase laws
     that its code omits (completed at A-PT2b). Every affected CLAIM is
     TRUE at bytes under the completed checks. FIX: display the checks
     at their actual grade, or adopt the completed forms.
F-5  PROVENANCE DISPLAYS: (i) the §0 ground row
     "supervision/SURFACE_DEFINITION_OF_RECORD_V003.md" names no root;
     the path resolves ONLY in the ARCHIVE root (the cleanroom root's
     listing has no supervision/SURFACE_DEFINITION...; the sealed AJ's
     own citation uses a third name, alpha_supervision/). The digest
     c80c09a2 pins the object uniquely, so no substantive doubt. (ii) The
     battery script is not embedded in the artifact (hash only, contra
     the AJ's embed practice); it was recovered on disk at the displayed
     hash and re-run byte-identically this commission — had the
     scratchpad been purged, the hash would be unverifiable. FIX: state
     the root; embed or archive hashed scripts.
```

## §A6 — VERDICT, PER CLAIM AND OVERALL (the headline adjudicated at the bytes)

```text
(I)   REQUIRE-HALF (class forced, exactly ℤ, every model)
        = CONFIRMED-WITH-CORRECTIONS (F-1: the model-class quantifier is
          honest over the standard-denotation reading, which must be
          named; all algebra re-derived twice, incl. the class solved to
          exactly ℤ over C and over R, membership proper, symbolic-n).
(II)  ALLOW-HALF (magnitude free, every model, uniform witness)
        = CONFIRMED (denotation-robust; vocabulary disjointness verified
          on the CORRECTED inventory; toys re-derived exactly as
          quarantined; invariance lemma sound; uniformity over the class
          rides S8, which needs only vocabulary disjointness).
(III) EXCLUSIVITY = CONFIRMED-WITH-CORRECTIONS ((a) inherits F-1's class
          scope; (b) confirmed via S8; (c1) discreteness now at CAS
          membership grade; (c2) co-occurrence absence re-verified at
          surface bytes; (c3) kills carried at the AJ audit's corrected
          scope, accurately).
COR-1/COR-2 = CONFIRMED (named-premise discipline held; COR-2's
          positivity premise made explicit at A-PT7b: G real-nonzero
          gives {−1,+1}, the R≥0 magnitude typing selects 1).
§3.5 L2 BOUNDARY = CONFIRMED (X7 quoted verbatim; carriage accurate;
          the Σ-relative reduct caveat honest).
§5.1 R-A (C12) DISCHARGE = CONFIRMED — the premise-DAG audit is accurate
          at bytes (zero steps of (I)/(II)/(III) consume SII.6's scope;
          the F-1 layer is SI.3-side, so the discharge survives the
          finding); the surviving-conditionality display and its
          discharging object match AJ §8.2/§8.3 verbatim.
§5.2 R-B (FINITE-TEXT) DISCHARGE = CONFIRMED-WITH-CORRECTIONS (F-3
          scope phrase; the enumeration itself re-performed AGAIN this
          commission, three-plus-one-way concordant: my inspection = the
          build's §1 = AJ M8 = AJ-audit X5).
§5.3 "UNCONDITIONAL over its stated scope; no unproven premise in its
     DAG" = CONFIRMED-WITH-CORRECTIONS: the DAG carries, beyond Σ, the
          F-1 interpretive premise — definitional, sealed-carried, not a
          physics premise, but to be NAMED for the sentence to hold as
          displayed.
§6 SCOPE / FENCES = CONFIRMED (current-ground-only honest; J-2 fence
          held at bytes — no extent work anywhere; no lens use; the
          survey's overall verdict and the AJ's record status untouched;
          DETERMINATION ONLY held).
§4 CAS = CONFIRMED-WITH-CORRECTIONS (F-2, F-4; script recovered at
          displayed hash, byte re-run IDENTICAL 22/22; repair note
          verified at bytes — the {0} artifact reproduced; 19/19
          independent battery).
§8/§9 LEDGER + TOY_SEPARATION = CONFIRMED-WITH-CORRECTIONS (ledger
          complete but for the F-1 entry; CH-5 already displays the Σ6
          choice honestly; toys quarantined exactly as sealed).

OVERALL = CONFIRMED-WITH-CORRECTIONS.
  THE HEADLINE, adjudicated at the bytes: on the sealed surface's seven
  displayed ground clauses, READ AS THE SEALED TOWER ITSELF UNIFORMLY
  READS THEM (continuous U(1) characters, labels the exponents — the
  premise F-1 requires be named), the partition theorem STANDS: every
  model quantizes the class to exactly ℤ, no model pins any response
  magnitude, no model places them otherwise, both AJ residues sit off
  the theorem's premise DAG (one proven never consumed, one executed at
  bytes — and re-executed here), the two-model witness is uniform, and
  the survey's J-6 rigidity is thereby carried by the clauses alone at
  its stated scope. Five findings, all correction-grade, none moving
  any claim, quantifier, residue disposition, or fence.
```

## §A7 — SWEEP CUTOFF (declared)

```text
SWEEP_CUTOFF = the claimed artifact + the five commissioned ground
  artifacts + the sealed surface, all at seals re-verified by my own
  shasum runs (7/7 MATCH, §A0/§A1) + the recovered battery script at its
  displayed hash + filename listings (filenames only) of both corpus
  roots and the session scratchpad to resolve named paths and locate the
  script + the declared fence-scan greps of the claimed artifact and
  script + two fresh-venv battery runs. The forcing-boundary file read
  at its cited line ranges; the surface read in full (its §III–§VII
  consumed ONLY to adjudicate the corroboration-only and completeness
  claims, supplying no premise to any re-derivation). NO register/
  tracker/road/plan/continuation file read; "Q-..." tokens
  EXPECTED-UNLOCATABLE, treated as opaque labels carried inside sealed
  artifacts. No git. My exhaustion is by PROOF-WALK over the claimed
  artifact's own finite step list (S1–S13, PT0–PT7, §5's two tables),
  each step checked at bytes; the cutoff bounds file access, not the
  adjudication.
```

## §A8 — CHOICE LEDGER (commission PARTITION-AUDIT; every unforced choice)

```text
ACH-1  INDEPENDENT FORMALIZATION with τ_R symbolic and both transition
       readings: FORCED in substance by DEFAULT-REFUTE — refuting or
       confirming PT1's "exact" claim requires the uncollapsed Σ3, and
       adjudicating F-1(c)'s materiality requires both readings.
ACH-2  BYTE RE-RUN of the recovered script IN ADDITION to the
       independent battery: FORCED in substance — the artifact displays
       the script's hash; the hash-matched file existed on disk as a
       scratchpad process file (not a fenced class); recovery + re-run
       is the strongest available fidelity check and the T14SR audit's
       ACH-2 names re-formulation-only as the WEAKER posture.
ACH-3  CONSUMING surface §III–§VII (inside the sealed file) to
       adjudicate the corroboration-only and G-11 completeness claims:
       PREMISE(named) — the same necessity the build and the AJ each
       declared; no §III–§VII byte supplies a premise to any
       re-derivation here.
ACH-4  ADJUDICATING the wild-character corner CORRECTION-grade, not
       REFUTED: FORCED in substance by three independent displays — the
       claim's own wording presupposes the exponential family ("label
       admissible under Σ4's single-valued closure"); §2's model notion
       fixes everything outside §1's inventory (χ absent from it); the
       consumed sealed tower (AJ script bytes, P1/P3, §VII.15, C-A)
       uniformly carries the denotation. A REFUTED would require reading
       Σ against the tower that defines it.
ACH-5  FINDINGS SEVERITY (five corrections, none verdict-bearing):
       FORCED by the displays — every finding's underlying claim was
       independently verified TRUE under the completed check or named
       premise; no claim, residue, or fence moves under any of F-1..F-5.
ZERO entries in class OPEN.
```

## §A9 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The only toy objects touched are the AJ pair's own quarantined model
  profiles f₁, f₂, re-derived at A-PT5 exactly as the sealed build and
  the claimed artifact quarantine them: meta-level exhibits about the
  derivability relation, entering only as the two-model witness content.
  No toy value, profile, or model is asserted as surface content,
  candidate, or physics; none leaks into any finding, verdict, or flag.
  Every load-bearing object here is a sealed clause read at verified
  seal, a recovered hash-matched script, or an exact-algebra identity.
```

## §A10 — FLAG BLOCK

```text
COMMISSION = PARTITION-AUDIT (T16SR; DEFAULT-REFUTE; testimony zero
  weight; every claim re-derived at bytes; every symbolic step re-run).
STEP_0 = claimed artifact present; seal 5e12af0b... shasum -c OK; output
  path probed ABSENT before first write.
SEALS = 7/7 re-verified at my own hands (claimed artifact + five ground
  artifacts + surface at c80c09a2, full digest matching the build's
  recorded digest and the AJ's); every §0 short-digest in the build
  matches my hash runs; MISMATCHES = 0.
GROUND = same ground consumed independently: T14SR pair IN FULL (audit
  OVERALL CONFIRMED-WITH-CORRECTIONS, D4 CONFIRMED, at bytes); AJ pair
  IN FULL (audit CONFIRMED-WITH-CORRECTIONS; C-1/C-2/C-3, X5/X7/X11 all
  re-located at bytes); demand-reading at its cited lines; surface
  §I-§II re-enumerated clause-by-clause (G-10/G-11).
CAS = build script RECOVERED at its displayed sha256 (84e277da...),
  re-run fresh-venv BYTE-IDENTICAL (22/22); independent battery 19/19
  PASS (sha256 e0eece26...), exact symbolic, zero numerics, both
  transition readings, τ_R symbolic, true membership, domain artifact
  {0} reproduced (repair note verified).
HUNTS = unnamed clause: FOUND at the interpretive layer (F-1, correction
  grade, repair one named premise); quantifier-gap: SAME layer, honest
  over the tower's uniform reading, (II)/(III-b,c) denotation-robust;
  silent residue absorption: NONE (both discharges verified; R-A's
  non-consumption table accurate step-by-step); extent-site scope creep:
  NONE (J-2 fence held; no extent work); lens token: NONE outside
  discipline blocks; number: NONE authored (all residuals/structural/
  sealed-carried; no measured-constant comparison).
FINDINGS = F-1..F-5, ALL correction-grade, NONE verdict-bearing (§A5).
VERDICT = CONFIRMED-WITH-CORRECTIONS — per claim: (I) CwC (F-1 naming),
  (II) CONFIRMED, (III) CwC, COR-1/2 CONFIRMED, §5.1 R-A discharge
  CONFIRMED, §5.2 R-B discharge CwC (F-3), §5.3 unconditional-sentence
  CwC (F-1 premise to be named), §6 fences CONFIRMED, §4 CAS CwC
  (F-2/F-4). The PARTITION-THEOREM-DERIVED headline STANDS at the bytes
  over Σ under the sealed tower's own uniform character reading, with
  that reading to be named as a premise; nothing else moves.
DETERMINATION_ONLY = held by this audit: no adoption, no authored
  physics, no value, no number authored, no gate/flag/witness/status of
  any consumed artifact moved; nothing here discharges any named absence
  of the program.
SWEEP_CUTOFF = DECLARED (§A7). CHOICE_LEDGER = ACH-1..ACH-5, ZERO OPEN
  (§A8). TOY_SEPARATION = clean (§A9).
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT
  before first write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = the partition theorem survives DEFAULT-REFUTE at every step that
  is algebra and at every step that is bookkeeping; its one genuine gap
  is a missing NAME, not a missing truth: the character-denotation
  reading every consumed sealed layer already carries must be displayed
  as a premise of the require-half, and with that sentence added the
  rigid sub-region's standalone grade — class require-side, magnitude
  allow-side, no model otherwise, both residues off the DAG — holds
  exactly as far as the sealed ground reaches, and not one clause
  farther.
```
