# STAGE 8 — AUDIT OF THE T7 AMPLITUDE ADJUDICATION EXECUTION — S9AD AUDIT V001

## INDEPENDENT ADVERSARIAL AUDITOR — CODENAME PLT-AUDIT — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15T00:26–00:37Z)
Commission: independent adversarial audit of STAGE8_T7_AMPLITUDE_ADJUDICATION_
S9AD_V001.md (b6b6b340, sidecar MATCH at path this session). Auditor NOT told
the outcome; default REFUTE. The triage of record (6a839b51) and its audit
(f7f068a2) verified at path and consumed at audited grade; triage §5 defines
the commission family. Fences live throughout: alpha_computed = false ;
proof_authorized = false ; kappa_record_computed = false. GOV-F untouched;
scoring is never declaration; nothing here moves any score. THE HELD
CONSTRUCTION (506bfe2a/5fa46838) not performed, advanced, presupposed, or
probed. Bytes-level + exact-symbolic only; every float cited below is a
sealed-text quotation, never ground; no value frozen. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-..." tokens
EXPECTED-UNLOCATABLE, none chased. No git; no existing file edited; ONE output
(this artifact) + seal sidecar at commission-distinct paths, both probed
ABSENT at session start (00:26Z) before any work.

SWEEP CUTOFF DECLARED: 2026-08-15T00:37Z UTC. Every "none exists"/"zero hits"
quantifier below ranges over the sweeps displayed in §6, closed at that time,
over the two corpus roots minus fenced name classes.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — the build's verdict ACTUAL_PARENT_RECORD_
AMPLITUDE_BLOCKED is the verdict my independently pre-fixed adjudication of
the spec's A-clauses predicted (fixed and hashed 87068804fe83b6c1... at
00:32:45Z, BEFORE the build was opened); the verdict token is on the spec's
own two-token A6 menu; every load-bearing citation I attacked — 14 hashes,
9 verbatim clause quotes, the withdrawal chain, the two-scheme carry, the
rest-normal endpoint identity, the ER-fork non-selection, the ETT decider-
stack semantics — verified at bytes with zero misquotes; the false-PASS
hazard did not occur (no A5.1 vocabulary bridge anywhere: the build refuses
exactly the bridge objects my pre-fixed checklist barred); the false-BLOCKED
hazard is excluded by my own counter-battery (no sealed discharge of ANY of
the three PASS legs exists — sweeps §6); TWO corrections, neither
verdict-bearing: (C-1) one wrong digest label in a custody annotation — the
build's §1 calls the restriction spec "5039541c-family", but 5039541c is the
digest of STAGE8_T7_COMPLETED_EFFECT_ZERO_BASELINE_SPEC_V001.md and the
restriction spec's true digest is 3d598a19; the custody CLAIM itself (prior
return 7aafba86 full-hash pinned inside the restriction spec, line 23, and
the finite-Fock spec, line 25) is TRUE at bytes; (C-2) the build's SW-1
"ZERO hits" is true only pre-publication — post-publication the corpus
contains exactly one hit, the build's own sweep display line, which a future
sweeper must disposition.**

---

## 1. INDEPENDENCE PROTOCOL, DISPLAYED

Worked the spec's A-clauses myself from the spec (7d5041a4, old-convention
sidecar verified at path) and its ten pinned authorities (10/10 hash-verified
at path by me, independently of the build's identical claim), plus the
standing stock my own sweep surfaced. Answers fixed at:

```text
sha256(PLT_AUDIT_FIXED_ANSWERS.md) =
87068804fe83b6c19668488a1c8f9a0565ae874a7f150874f12a4b1ffbb5db93
fixed 2026-08-15T00:32:45Z; build opened after 00:32:45Z
```

PRE-FIXED PREDICTION: ACTUAL_PARENT_RECORD_AMPLITUDE_BLOCKED, deciding legs
(a) A3 unique-scalarization not established, (b) A5.1 exact one-handle
recovery absent of record (actual-parent displays 0.8524... and 0.5344...,
quotations, vs pinned a_h(0) = 1), (c) nonzero baseline not at exact-symbolic
grade — plus a nine-point attack checklist (H1–H9) for bridge/laundering
moves. BUILD VERDICT: identical token, identical two deciding PASS-leg
failures.

Disclosed exposures and divergence, honestly:

1. Before fixing, a corpus sweep surfaced ONE grep line showing the token
   "finite_actual_parent_record_amplitude_derived = true" inside the build.
   Context unknown at fix time; recorded inside the fixed file itself. It
   proved to be the build's own SW-1 sweep-display string, not a
   declaration. My prediction was fixed as BLOCKED notwithstanding.
2. DIVERGENCE, resolved FOR the build: my fixed A3 ground treated the
   regulator-restriction discharge (3a6ff617) as standing; the build cites
   STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md (a1258dcf,
   full-hash pinned inside SEALED 789338ad — pin verified at 789338ad line
   26; sidecar of 789338ad MATCH) which WITHDREW that discharge on an exact
   spectral ground my pre-fix sweep had missed. I then verified the
   correction at its own bytes (§2). The build's A3 account is the
   better-informed one; both routes end at the same mandatory trigger.

## 2. THE ATTACK AT BYTES — EVERY LOAD-BEARING CLAIM RE-VERIFIED

Seals/hashes verified by me at path this session (zero mismatches):

```text
b6b6b340  build, sidecar MATCH          7d5041a4  spec, old-convention sidecar MATCH
6a839b51  triage, MATCH                 f7f068a2  triage audit, MATCH
ae2202e0  earn-the-trace, MATCH         a4c2b7df  ETT audit, MATCH (CONFIRMED-WITH-CORRECTIONS)
10/10 spec-pinned authorities exact     7aafba86  prior return = finite-Fock spec pin AND
                                                  restriction-spec line-23 pin (custody TRUE)
a1258dcf  correction = 789338ad pin     789338ad  sidecar MATCH
235246ab  mixed-covariance = pin        a79939ad  pure-convergence = pin
80aa4e17  Hermite baseline SPEC (RESULT file ABSENT — build SW-8 confirmed)
a087e90a  ER-fork result, sidecar MATCH 1868656d  endpoint result = R3_4 spec pin
3d598a19  restriction spec, sidecar MATCH (NOT 5039541c — correction C-1)
32251007  exec-tree seal = restriction-spec line-24 pin
57853213 / 4059eff5 / 808bfd54  corroborative files match build's CL-3 grading
907a274a  finite-Fock result, custody via execution-tree seal (I located the
          pin independently before opening the build)
```

Verbatim clause checks (build's quote → sealed source, all MATCH at bytes):
"is withdrawn" + "That family does not exist" + "purely continuous spectrum"
+ "generalized-eigenvalue samples" + the four correction flags + "without
selecting between them by output" + C_n^(mix)/C_n^(pure) formulas + "the
general Klich determinant, not a Slater-compression determinant" + "The
continuum response is admissible only if both schemes converge to the same
normalized result" + "C5_regulator_independence = OPEN" + "numerical
convergence alone cannot promote" → a1258dcf. "Q_2 C_4^(mix) Q_2 =
C_2^(mix)" exact Gamma closed forms → 235246ab. "pure_state_sequence_nested
= false" → a79939ad. "only on the rest-normal momentum ray" + "p=0 gives
omega_m T_R=pi; the record endpoint is exact" + nonzero-p commensurate
requirement + universal_exact_finite_wavepacket_write_derived = false →
1868656d. "This gate selected nothing" + ER_A_selected = false +
ER_B_selected = false + envelope_realization_derived = false → a087e90a.
"PASS moves horn (b) ... BLOCKED per its A6 moves horn (a) for the amplitude
layer" → ae2202e0 §6 decider stack item (1), verbatim — the build's §4 fork
consequence is a faithful application of sealed semantics, not an overstep.
v(t) = (pi/sqrt(2)) 32 r(t)^3 → 789338ad (displayed block at lines 86–88;
the build's "line 88" points into that block). Floats 0.8524... / 0.5344...
/ 0.7149... → quotations of 7aafba86 / 907a274a bytes, confirmed.

## 3. CLAUSE-FIDELITY AND HAZARD ADJUDICATION

A6 menu fidelity: verdict token exactly on the spec's two-token menu; no
third token invented; per-clause statuses are display, not verdict tokens.
FALSE-PASS (the fitting hazard): did not occur — and the build independently
bars every bridge object my pre-fixed checklist named (Z-ratio, D3 closure
C(zI)=z, Gram chain 1,-1/2,1/4, conditional (-1/2)^(N-1) identity, ETT
W-to-V identification). No vocabulary bridge to A5.1 anywhere in the build.
FALSE-BLOCKED (the lazy-parking hazard): excluded by counter-battery. PASS
needs three legs at once; my sweeps (§6) found: no scheme-agreement theorem
(C5 nowhere closed), no packet-concentration/finite-volume approximation
theorem for the amplitude (the only "approximation theorem" hits quote the
demand, none supply it), no derived commensurate exact-transfer cohort
("commensurate" corpus hits: the endpoint result itself and an unrelated
Gate-1 U(1) census line), no rest-normal-admitting state carrier, and zero
corpus movement of the verdict flag. B1 and B3 block independently; either
alone forces BLOCKED. The blocker set is real, current, and correctly
migrated from the prior return (movement §3 of the build verified against
7aafba86's closure condition at bytes: arm 1 stops at B1/B2, arm 2 at B3).
Fence-scan of the build: zero hits for any coupling/value token; no numerics
run; held construction untouched (its digest not even probed — honored by
distance, stricter than required).

## 4. CORRECTIONS AND OBSERVATIONS

```text
C-1 (correction, custody annotation): build §1, prior-return row — "restriction
    spec 5039541c-family" is a wrong digest label; 5039541c belongs to
    STAGE8_T7_COMPLETED_EFFECT_ZERO_BASELINE_SPEC_V001.md (per exec-tree seal
    stage8_execution/t7_primitive_connected_lift/); the restriction spec is
    3d598a19. The pinned-custody claim itself is TRUE (verified at both pin
    sites). No downstream statement rests on the wrong label.
C-2 (correction, sweep semantics): SW-1's "ZERO hits" is a pre-publication
    truth; the published build itself now contains the token once (its own
    sweep display). Future sweeps must disposition that self-hit.
O-1 (observation): A1's "ideal product used NOWHERE in the adjudicated
    lineage" is true for the lineage as the build defines it; the pinned
    retest authority (21b782b5) does display projector-chain objects, and the
    build consumes it only as contrast — lawful, but the lineage boundary is
    doing work there.
O-2 (observation): B4 (ER fork) is conservative over-inclusion relative to
    A1's import-unchanged bytes (the QSPEC discloses ER-A for the sealed
    parent); verdict-neutral since B1 and B3 decide independently, and the
    build itself grades A1 "IMPORT SATISFIED" rather than blocked-at-A1.
O-3 (observation): CL-4's identification (pinned amplitude's exact carrier =
    rest-normal configuration) is a named premise; I attacked it and found it
    byte-grounded (6e01e5ba unit-incidence exactness + 1868656d rest-normal-
    only + 7aafba86's "ideal root-line value 1"); no alternative sealed
    reading found (my sweep concurs with the build's SW-5).
```

## 5. WHAT THIS AUDIT ESTABLISHES, PLAINLY

The standing T7 instrument, executed today, lawfully returns BLOCKED, and
the verdict flag finite_actual_parent_record_amplitude_derived = false
stands corpus-wide. Per the sealed ETT decider semantics this moves fork
horn (a) — machinery-fit — for the amplitude layer only; the phase-side
landing and the fork's other deciders are untouched. The blocker set for
the principal's table is exactly B1–B5 as the build states it, with B1
(two-scheme scalarization, agreement OPEN) and B3 (no normalizable
rest-normal carrier + both recovery routes absent) each independently
decisive. Nothing here moves a score or performs the held construction.

## 6. AUDITOR'S SWEEPS DISPLAYED (cutoff 2026-08-15T00:37Z)

```text
AS-1  "finite_actual_parent_record_amplitude_derived = true", workspace *.md,
      fenced names excluded: 1 hit = the build's own SW-1 display line (C-2);
      zero declarations. Flag unmoved.
AS-2  "ACTUAL_PARENT_RECORD_AMPLITUDE" citers: 10 files enumerated (the 9 the
      build dispositions + this audit's target), all read or headlined.
AS-3  scheme-agreement hunt ("C5_regulator_independence = DERIVED|CLOSED",
      "schemes agree", "scheme_agreement"): ZERO hits, both roots.
AS-4  "approximation theorem|packet.concentration": every hit quotes the
      demand (correction, prior return, build, AXN family — different
      family per triage S-1); none supplies the theorem.
AS-5  "commensurate": endpoint result + Gate-1 census (unrelated U(1)
      context) + build; no derived exact-transfer cohort.
AS-6  Hermite-Galerkin baseline RESULT: file ABSENT (spec 80aa4e17 + seal
      only) — the genuine-carrier gate is unexecuted to result.
AS-7  ER selection movement: ER_A_selected = false / ER_B_selected = false at
      the sealed gate; no later selection found.
AS-8  every hash row in §2 measured by shasum -a 256 at path this session.
```

## CHOICE LEDGER

```text
CL-A  CORPUS BOUNDARY — PREMISE(named): the two roots minus fenced name
      classes; sweeps AS-1..AS-8 closed at the declared cutoff; a sealed
      object outside this boundary at a PASS-leg's own form could move the
      grade of §3's false-BLOCKED exclusion, nothing else.
CL-B  CONSUMPTION AT SEALED GRADE — PREMISE(named): the spectral
      nonexistence argument, the exact mixed nesting, the vacuum-block type
      obstruction, and the endpoint identity are consumed at their sealed
      carriers' bytes (as the build also does), not re-derived; each is
      displayed exact-symbolically at its carrier.
CL-C  AUDITED-GRADE CONSUMPTION — PREMISE(named): triage at f7f068a2's grade;
      ETT at a4c2b7df's grade (CONFIRMED-WITH-CORRECTIONS); neither re-run.
CL-D  PRE-FIX EXPOSURE — DISCLOSED: one grep token line from the build seen
      before answer-fixing (§1.1); prediction fixed independently of its
      context and the context proved non-declarative.
CL-E  B3-SYNTHESIS CHECK — my own verification: "no normalizable rest-normal
      state" follows exact-symbolically from sealed "no source point
      spectrum" (345d447e) + rest-normal-only exactness (1868656d); the
      build's flag that this synthesis is its own, links sealed prior, is
      accurate. PREMISE(named) only in that I verified the inference, not a
      sealed restatement of it.
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  No load-bearing audit step rests on an unproven convenience: the verdict
  comparison rides on a pre-fixed hashed prediction plus byte-level
  re-verification of sealed content; every float touched is a quotation;
  the two instances (three-site, momentum-block) enter only as the sealed
  correction types them; no numerics were run by this audit.
```

## FLAG BLOCK

```text
AUDIT = CONFIRMED-WITH-CORRECTIONS (build b6b6b340; verdict token, clause
  statuses A1..A6, blocker set B1-B5, movement account, and fork consequence
  all verified at bytes; corrections C-1 custody-label digest, C-2 SW-1
  post-publication semantics; observations O-1..O-3 verdict-neutral)
INDEPENDENCE = answers fixed + hashed 87068804fe83b6c1... at 00:32:45Z before
  build opened; predicted verdict = build verdict = ACTUAL_PARENT_RECORD_
  AMPLITUDE_BLOCKED; one pre-fix token exposure disclosed (CL-D); one ground
  divergence resolved FOR the build at sealed bytes (a1258dcf withdrawal)
FALSE_PASS = did not occur (no A5.1 vocabulary bridge; all barred bridge
  objects refused by the build itself)
FALSE_BLOCKED = excluded at sweep grade (no sealed discharge of any PASS leg:
  AS-3, AS-4, AS-5, AS-6; B1 and B3 independently decisive)
SPEC_FIDELITY = menu honored (two tokens only); mandatory A3 triggers
  correctly applied; closure-condition bar honored and live
SEALS = every hash in §2 measured at path this session; zero mismatches;
  pinned-custody chains independently re-walked (7aafba86, a1258dcf,
  235246ab, a79939ad, 907a274a, 32251007)
SWEEP_CUTOFF = 2026-08-15T00:37Z ; sweeps AS-1..AS-8 displayed at §6
HELD_CONSTRUCTION = untouched (not performed, advanced, presupposed, or
  probed) ; GOV_F = untouched ; SCORING = unmoved ; DECLARATION = not-made
NO_GIT = honored ; NO_EXISTING_FILE_EDITED = honored ; ONE_OUTPUT+SIDECAR =
  honored at commission-distinct paths, probed ABSENT at 00:26Z
CHOICE_LEDGER = CL-A..CL-E, zero OPEN ; TOY_SEPARATION = clean
FENCES_AT_CLOSE = alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false ; no value frozen ; every numeral above is a
  sealed-text quotation or a verified hash
PLT_T7_AMPLITUDE_ADJUDICATION_AUDIT = SEALED.
```

---

One output file; seal sidecar alongside. PLT-AUDIT ends.
