# STAGE 8 — DEFAULT-REFUTE AUDIT OF STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001

## AUDIT LANE — DEFAULT VERDICT REFUTED — 2026-08-15

**CLASSIFICATION ONLY.** I adopt, recommend, prefer, and argue for NEITHER the
PRODUCER reading nor the CONSTRAINT reading. Where the bytes do not decide, the
row reads INDETERMINATE-AT-BYTES and says so. Nothing here is proposed, authored,
repaired, or reclassified as corpus content.

Fences declared and honoured throughout: `alpha_computed = false`;
`proof_authorized = false`; `kappa_record_computed = false`. No numeric value of
any coupling, scale, root, eigenvalue, or constant was computed; no comparison to
any measured value was made. Fence tokens appear below only inside quotation or
in this line and the FLAG BLOCK.

**TESTIMONY CARRIES ZERO WEIGHT.** Every load-bearing statement of the build
report and of the target artifact was re-derived at the bytes by me.

---

## §0 — SEALS AND TARGET INTEGRITY

Verified with `shasum -a 256 -c` run from `/Users/bgm/MB Work/alpha-program-archive/workspace`.

```text
TARGET
  STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md.sha256        OK
  STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md.seal.sha256   OK
  sha256 = 81934f06889acf5325db0dba6f9bf55aa76cf9a9ab2e07d2f4768bc8c0577fad
           — MATCHES the build report's claimed digest exactly.

THE BUILD'S DECLARED GROUND (its §0 list of 28): 28/28 OK, zero mismatches.
  Re-run by me, file by file, from the artifact directory.

THE BUILD'S UNDECLARED GROUND — 10 FURTHER SEALED FILES IT CITES AND RELIES ON
  BUT DOES NOT LIST AT §0.  Each verified by me, each OK:
    PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md   OK
    SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md                           OK
    STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md                                  OK
    STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md                                  OK
    STAGE8_UNIT_VALUE_ADOPTION_INDEPENDENCE_AUDIT_EINSTEIN_V001.md           OK
    STAGE8_DESC_P0_EXECUTION_CHECK_CODEX2_V001.md                            OK
    STAGE8_SECTION_5_3_UNIQUENESS_GATE_PASSABILITY_DETERMINATION_V001.md     OK
    STAGE8_FINITE_INCIDENCE_REALIZATION_FUNCTOR_ADOPTION_PROPOSAL_V001.md    OK
    STAGE8_P5_COMMON_ORIGIN_DESCENT_PIECE1_ORIGIN_TRACE_ATTEMPT_V001.md      OK
    STAGE8_MICROSCOPIC_GENERATOR_TOWER_FIXING_PROPERTY_V001.md               OK  (mine)
  Plus, located and read by me and not by the build:
    STAGE8_ACTION_FORM_CLOSURE_THEOREM_SPEC_AND_BUILD_STOP_V001.md           OK

UNSEALED, CONFIRMED: PRIMITIVE_RECORD_CARRIER_AND_KINEMATICS_V001.md has no
  sidecar of either kind.  The build's FLAG-1 declaration of this is accurate.

AUDIT SEAL TALLY: 39/39 OK (28 declared + 10 undeclared + 1 located by me),
  plus 1 declared-unsealed.  ZERO MISMATCHES ANYWHERE.
```

The build report's headline "SEALS: 28/28 OK (+1 declared no-sidecar)" is true of
the 28 it lists and understates the ground it actually consumed by ten sealed
files. No soundness break follows — all ten verify — but the tally as reported is
not the tally of the consumption. See **COR-M**.

---

## §0.1 — MY SWEEP DECLARATION AND CUTOFFS

ROOT-1 = `/Users/bgm/MB Work/alpha-program-archive/workspace`.
ROOT-2 = `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`.

Every sweep was basename-filtered against
`(REGISTER|TRACKER|THE_PLAN|ROAD|HANDOFF|DECISION_SHEET|ADDENDUM|alpha_supervision|CONTINUATION|OBSERVATIONS)`
before results were read.

```text
A-1  CONTACT TEST, RE-RUN INDEPENDENTLY.  Pattern: the build's own S-6 pattern
     (U_write|record-write|REVERSIBLE_RECORD_WRITE|GENERATIVE_VS_FORMATION|
     ROOT_PHYSICAL_ONTOLOGY|antecedent carrier|Omega_c|causal_incidence|
     record-forming incidence|write-and-persist|record onset|CAUSAL_DIRECT_LIMIT|
     ONSET_SATURATION), and separately (G_closure|record-forming generator|
     PAIRED_RETURN), counted against the eight artifacts the build calls the
     closure lane.  RESULT: 0/8 and 0/8, CONFIRMED EXACTLY.
     CUTOFF: the eight named artifacts.

A-2  PRODUCTION-VERB SWEEP, RE-RUN.  Pattern: (yields|produces|creates|
     constructs|brings into|makes there be|comes into (being|existence)|
     generates), case-insensitive, against the eight closure-lane artifacts
     AND against the three further artifacts CH-2 places inside the machinery.
     CUTOFF: all eleven files, every matching line displayed and read.

A-3  MACHINERY-SCOPE TEST.  I read in full, at bytes, the two gate artifacts and
     the forcing-notion artifact that CH-2 names as machinery and that S-6/§4.1
     did not sweep: BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md (198
     lines, read whole), SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md
     (172 lines, read whole), STAGE8_FORCING_NOTION_O12SR_V001.md (§1, §3.2,
     §3.3 read at bytes).
     CUTOFF: the three files; the first two read to their last line.

A-4  AXIS-VOCABULARY HUNT (mine, not the build's).  Pattern: (unbuilt|not a
     selector|CELL_CONSTRAINT_ONLY|work not yet done|content not yet entered|
     derived existence|uninhabited type slot), against all eleven machinery
     artifacts.  This is the pattern the build's S-3 could not have matched.
     CUTOFF: the eleven files; every hit read in context.

A-5  GENERATION-RULE SWEEP, RE-RUN.  Pattern: (generat(ing|ion) rule|generation
     rules|production map|production rule|generating rule), case-insensitive,
     ROOT-1 *.md.  RESULT: 31 files including the target artifact itself, i.e.
     30 corpus files — the build's reported count for ROOT-1 CONFIRMED.
     CUTOFF: I opened the six files carrying hits that the build's §2.3 does not
     dispose of, to test its disposal claim.

A-6  QUOTATION RE-VERIFICATION.  Every quotation in §1-§4 of the target that
     carries a grade was re-read at its cited file and, where the citation was
     wrong, located by content search.  CUTOFF: all graded quotations.

NOTHING ELSE OPENED.  No git action.  No register, tracker, plan, road, handoff,
decision sheet, addendum, or supervision file was opened, read, grepped, or
quoted at any point.
```

---

## §1 — D1: SIGNATURE CLASSIFICATIONS (Q1) — **REFUTED**

### §1.1 WHAT VERIFIES

The nine closure-stock rows CS-1…CS-9 verify **verbatim** against
`STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` :116-179, including the build's
inclusion of the clauses that cut against its own rows (CS-2's *"offered as
obligations R-L4a/b, never as results"*; CS-3's *"it is a divergence source, not
a budget"*; CS-8's *"saturation ADOPTED not derived"*). The scope-limit sentence
the build quotes whole at :112-113 is at :112-113 exactly. The MODAL grades on
CS-1, CS-2, CS-6, CS-7, CS-9 are supported by their own defining text: an
equality chain whose right-hand side is an input; a collapse display; a
criterion; a grading; a ruling.

The thirteen failure-point rules F1-a…FS-a verify verbatim against
`STAGE8_CERTIFICATION_RULES_O8SR_V001.md` §2 (with citation drift, **COR-L**).
The build's self-cutting inclusions are real: F1-b's row carries O8SR's own
*"NOTHING is excluded by a clause here"* paragraph whole; F2-c is marked
SECOND-HAND and its elision is flagged rather than completed; F3-d is marked
non-probative in the build's own words.

Row arithmetic in §1.6 is internally consistent at row granularity:
9+13+5+6+6 = 39; 8+12+5+5 = 30 MODAL; 2 MIXED; 6 NAMED-ONLY; 1 INDETERMINATE;
30+0+2+6+1 = 39. MODAL(measurement) = 3+2 = 5. The CH-1 counterfactual
(25/5/2/6/1) also sums to 39.

The six NAMED-ONLY rows N-1…N-6 verify at their sources as **named and
undefined**, which is the grade's content:

- **N-1** `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` :18-24 names "the complete
  microscopic parent" as the assigning agent; the falsifier at :77-79 reads whole
  *"Failure to derive a unique causal cell, a complete parent, or a durable
  outgoing sector blocks downstream promotion. It is not repaired by assuming
  durability."* ✔ (line citation wrong, **COR-L**).
- **N-2** `STAGE8_UNIT_VALUE_ADOPTION_INDEPENDENCE_AUDIT_EINSTEIN_V001.md` :49
  *"(TYPE-U — the durable-record generator is unbuilt)"* ✔ and :212 ✔.
- **N-3** `STAGE8_OBJ0_..._BUILD_STOP_V001.md` :1587-1589 *"**Not fixed by sealed
  text:** a physical category, internal object type, arity, domain, codomain,
  support/core, representation, and analytic conditions."* ✔ — codomain
  expressly not fixed; NAMED-ONLY is the right grade.
- **N-4** all five sources verify at bytes (OBJ0 :183; DESC_P0 :254;
  SECTION_5_3 :367; FINITE_INCIDENCE :33; P5_COMMON_ORIGIN :294). ✔
- **N-5** `STAGE8_INGREDIENT_CENSUS_O17SR_V001.md` :645 heading ✔;
  `STAGE8_STRATIFICATION_O27SR_V001.md` :566 ✔.
- **N-6** `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md`
  :27-40 verifies verbatim ✔; `SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md`
  :24-26 verifies verbatim ✔.

No GENERATIVE row was graded, so there is no GENERATIVE grade to re-check — but
that absence is exactly what breaks below.

### §1.2 **COR-B — "ZERO GENERATIVE ROWS IN THE MACHINERY" IS FALSE AT BYTES**

The target asserts at :276:

> `*** ZERO GENERATIVE ROWS IN THE CLOSURE / FORCING / ALLOW-REQUIRE MACHINERY. ***`

and at :853-857 that CS-5's imported bump function is *"the single row whose
codomain is an object."*

The build's own classification rule, fixed at :152-154 before application, reads:

> `MODAL  ... It returns no object that was not already among its inputs.`
> `GENERATIVE   the defining text's output is an object not among its inputs.`

The build's own scope choice CH-2 at :1211-1215 fixes the machinery as, verbatim:

> "the WBC closure stock CS-1..CS-9; the O8SR failure-point rule inventory; the
> T14SR allow/require inventories and junction; **the onset gate and the
> flux-conditioned write gate**; the forcing criterion and its shapes; plus every
> item those name and never define."

The flux-conditioned write gate is `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md`
— the build's own AR-5 row, cited by it at :231. **That artifact's own bytes
exhibit three operators, none of which appears anywhere in the target artifact:**

At :80-86, quoted whole with the two sentences that follow it and cut against it:

> "The ready-subspace unitary is
>
> ```text
> U_write
>   = (I-Q_Sigma) tensor I_R
>     + Q_Sigma tensor (-iY_R).
> ```
>
> This is conditional on the inherited/adopted rules above. It is not a
> complete microscopic action or a durable record instrument."

At :67-74, quoted whole including the qualifier:

> "Endpoint rephasing rotates the equatorial pair `(v_x,v_y)`. Choosing `Y_R` as
> representative, the conditional integrated record-changing holonomy is
>
> ```text
> K_write,rel = (pi/2) Q_Sigma tensor Y_R
> ```
>
> up to endpoint rephasing and orientation reversal."

At :93-99, the family `U_chi = (I-Q_Sigma) tensor I_R + exp(-i chi) Q_Sigma tensor (-iY_R)`.

The onset gate is `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` — the
build's AR-1 row, cited by it at :227. **That artifact's own bytes exhibit two
further operators and one defined generator**, none of which appears in the target:

At :119-124, quoted whole:

> "The exact symmetric branch representatives are
>
> ```text
> U_0 = (I+iY)/sqrt(2) = exp(+i pi Y/4),
> U_1 = (I-iY)/sqrt(2) = exp(-i pi Y/4).
> ```"

At :57-63, quoted whole:

> "The relative generator in the common `U_0` frame is
>
> ```text
> H_W
>   = i hbar [partial_tau W] W^dagger
>   = U_0^dagger (H_1-H_0) U_0.
> ```"

`U_write`, `K_write,rel`, `U_chi`, `U_0`, `U_1` and `H_W` are each **an object not
among the inputs of their own defining text** — the inputs are `Q_Sigma`, `I_R`,
`Y_R`, `Y`, `H_0`, `H_1`, `W`. Under the build's own rule at :153 that is the
GENERATIVE case, and the contrast with its own CS-1 grade is exact: CS-1 was
graded MODAL because, in the build's words at :178, *"The right-hand side `P` is
among the inputs."* Here the left-hand sides are not.

**AT BYTES, THE MACHINERY AS THE BUILD ITSELF DEFINES IT CONTAINS AT LEAST SIX
OBJECT-CODOMAIN DEFINITIONS THAT THE INVENTORY DOES NOT CONTAIN AS ROWS.** Whether
each should finally read GENERATIVE or MIXED is not settled here — each carries a
disclaimer at bytes (flux gate :88-89 *"It is not a complete microscopic action or
a durable record instrument"*; :163 `complete_physical_write_operator_derived =
false`; onset gate :137-139 *"the geometric budget does not identify a source mass
or pointer coefficient"*) — and I decline to assign the replacement grades, which
would be authorship. What is settled is that **the tally line "GENERATIVE 0" is
not supported at bytes and the inventory is incomplete by at least six items.**

CORRECTED TALLY: the Q1 inventory is **39 rows covering an incomplete item set**;
the GENERATIVE count is **INDETERMINATE-AT-BYTES pending the grading of the six
exhibited operators at SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:67-99 and
BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:57-124**, and is not 0 with
support.

DECIDING BYTES: `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:83-86`.

### §1.3 **COR-C — FC-1'S DEFINING TEXT IS QUOTED WITHOUT ITS OWN CARRIED LEAD**

FC-1 is the row the build calls, at :237, *"**MODAL — decisively, and by explicit
definition.**"* Its quoted defining text is the three-shapes sentence, taken from
`STAGE8_FORCING_NOTION_O12SR_V001.md` :116-119 (cited by the build as :117-121).

Two of the build's own eight closure-lane artifacts carry that same sentence
**paired with a lead sentence from the same source's §0**, and the build displays
neither carrier's pairing.

`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md` :73-84, quoted whole:

> "THE UNIQUE-REQUIRE-FORCES READING (its §0, lines 24-25):
>
> > "THE FIRST SENTENCE IS RIGHT. THE SECOND IS FALSE:
> >  *** A REQUIRE-SHAPED CONDITION WITH DERIVED EXISTENCE AND UNIQUENESS FORCES A NUMBER. ***"
>
> THE THREE REQUIRE SHAPES (its §1 POINT 1, lines 54-57 — consumed at bytes, NOT
> as quoted in citing artifacts):
>
> > "Forcing has ONE criterion (the admissible set is a point) and THREE shapes: ..."

`STAGE8_COMPLETION_MAP_T17SR_V001.md` :407-412 carries the same pairing, quoted
whole:

> "...at its own bytes: "Forcing has ONE criterion (the admissible set is a point)
> and THREE shapes: a single require with a unique root; EXACT JOINT DETERMINATION
> (n conditions, n freedoms ...); and overdetermined joint determination (surplus
> conditions)" — with the lead: "A REQUIRE-SHAPED CONDITION WITH DERIVED EXISTENCE
> AND UNIQUENESS FORCES A NUMBER," graded "conditional derivation,
> singly-attested," pairs an upgrade never a gate; and the four-case map: "REQUIRE
> + UNIQUE SOLUTION — YES.""

And `STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md` :95-105 lifts the criterion
with both halves and states the consequence at :104-105, quoted whole:

> "Two halves, and BOTH are needed: EXISTENCE **and** UNIQUENESS. §2 turns on the
> fact that the record displays one half for each discharger and not the other."

The dropped clause is the one that bears on the existence half — the very half the
build declares undecidable at FC-4 (:240, :285-288, :1132-1136). T14SR :78-79
states in terms that the three-shapes sentence is to be *"consumed at bytes, NOT
as quoted in citing artifacts"*; the build consumed it as quoted in a citing
artifact (O12SR) and lost the lead. FC-1's grade may or may not survive the
restoration — I do not re-grade it — but **"MODAL — decisively, and by explicit
definition" is asserted on a truncated definition.**

DECIDING BYTES: `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:76`.

### §1.4 D1 GRADE

**REFUTED.** The NAMED-ONLY rows all verify, the closure-stock quotations are
clean, and the row arithmetic is sound — but the headline finding the whole
artifact turns on ("ZERO GENERATIVE ROWS") is false at bytes inside the build's
own declared machinery scope (COR-B), and the single row graded "decisively" MODAL
is graded on a truncated definition (COR-C).

---

## §2 — D2: CANDIDATE-GENERATOR CENSUS (Q2) — **CONFIRMED-WITH-CORRECTIONS**

### §2.1 WHAT VERIFIES, INDEPENDENTLY

**THE CENSUS IS NOT EMPTY.** Confirmed at the census's own bytes and at every
primary the build claims to have re-read:

- `STAGE8_INGREDIENT_CENSUS_O17SR_V001.md` :355 heading `### 2.1 WHAT HAS A
  **CELL** ON ITS OUTPUT SIDE?` ✔ and :433 `### 2.2 WHAT HAS A **RECORD** ON ITS
  OUTPUT SIDE?` ✔; :435 *"**PRESENT, AND MORE THAN ONE.** Unlike the cell, the
  record is genuinely yielded."* ✔.
- **G-CELL-1** `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` :18-24 verifies
  verbatim ✔; the census typing at :374-375 verifies verbatim ✔.
- **G-REC-1** `PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md` :18-24 and
  :41-45 verify verbatim ✔, **including** the two clauses the build extended its
  quote to include — *"It does not uniquely determine the unitary on the unused
  input subspace"* (:24) and *"This candidate is not an active Level-1 postulate
  and is not part of the alpha proof path unless an upstream record-dynamics
  principle independently selects it."* (:10-12) ✔. This extension is a genuine
  quotation repair, not a claimed one.
- **G-REC-2** `STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md`
  :33-35 and :26-28 verify verbatim ✔.
- **G-REC-3** is marked SECOND-HAND by the build at FLAG-2 and is second-hand in
  fact; the census's typing at :460-461 verifies verbatim ✔.
- The structural-reading quotation at :463-465 verifies **verbatim and whole**,
  including the half that cuts against a flat no-producers reading ✔.
- U-10's primary at `STAGE8_C1_COMMON_CELL_POSED_CODEX2_V001.md` :352 and :402
  verifies at the exact cited lines ✔.
- The contact test **0 of 8** is CONFIRMED EXACTLY by my own re-run (sweep A-1),
  on both the formation-layer pattern and the `G_closure` pattern.
- The generation-rule sweep count: my re-run of the build's S-2 pattern over
  ROOT-1 `*.md` returns **30 corpus files**, matching the build's report ✔.
- The **5 families / 10 individual rules** count is arithmetically correct
  (6+1+1+1+1 = 10) and each family verifies at its cited bytes ✔. The AXN
  correction is real: `STAGE8_AXN_BUILD_DESCENDANT_CALCULUS_DARIO_V001.md` :52-53
  is G2 and **is** the CIS cell-assignment clause, so the family carries a
  cell-yielding rule at bytes ✔. G3/G4/G5/G6 verify verbatim, including the
  scope note at :63-65 that limits G4 ✔.
- I hunted independently for a sixth generation-rule family with a record or cell
  codomain across the six S-2 files the build does not dispose of. **I found
  none**; every further hit is an *owed* or *absent* generation rule, consistent
  with the build's N-4 NAMED-ONLY treatment.

### §2.2 **COR-G — THE "CLOSURE'S ARGUMENT TYPE" IS FIXED FROM ONE SITE AND APPLIED TO ALL**

§2.0 fixes, at :328-330:

> "**THE CLOSURE'S ARGUMENT TYPE, FIXED: quadratic forms and operators on
> `Q = D(h_0) = H^1(R^3) ⊗ C^4`, consumed at trace/HS grade.** Not a record. Not a
> cell. Not a record-candidate or cell-candidate."

Both quoted sides are real: the `H(c)` set-builder verifies at
`STAGE8_CERTIFICATION_RULES_O8SR_V001.md` :264-266 ✔ and independently at
`STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md` :512-513 ✔; the consuming inequality
verifies at O8SR :272-273 ✔. So the TYPE-MISMATCH grades **do** display both
types, and the brief's unsupported-grade test is met on its face.

But both quoted sides come from **FP-3's admission clause and FP-3's consuming
site only**. The machinery as CH-2 defines it is wider, and the wider half takes a
different argument at its own bytes. `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md`
:99-101, quoted whole:

> "The allow/require boundary is adopted to select first admissible record onset
> through a shortest **relative** projective path on the unique physical record
> cell."

Its argument is *a record cell*. `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md`
:35-43 takes *a charged record handle* and a flux sector. Neither argument type is
displayed anywhere in §2.0, and both are of the kind §2.0 declares absent ("Not a
record. Not a cell."). The consequence is that the blanket grade at :576 —

> `*** FEEDS-CLOSURE: ZERO ROWS. ***`

— is established against **one** consuming type, not against the machinery's
argument types as a set. I do not assert that any census row does feed the
machinery; whether a "Lorentz-covariant causal cell `Omega_c`" and "the unique
physical record cell" are the same type is **INDETERMINATE-AT-BYTES** and I decline
to decide it. What is decided is that the FEEDS-CLOSURE = 0 grade rests on an
argument type displayed from one site and generalized without display.

DECIDING BYTES: `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99-101`
against `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:328-330`.

### §2.3 D2 GRADE

**CONFIRMED-WITH-CORRECTIONS.** The census result (NOT EMPTY; four record/cell
producers; the contingency sentence does not fire), the four hits, the 0-of-8
contact test, the 30-file sweep count, and the 5-family / 10-rule recount all
survive re-derivation at bytes. Corrections: **COR-G** (argument type generalized
from one site), **COR-L** (citation drift, including two swapped ranges inside the
census rows themselves).

---

## §3 — D3: FAILURE RESTATEMENT (Q3) — **CONFIRMED-WITH-CORRECTIONS**

### §3.1 EVERY OPERATIVE SENTENCE RE-READ IN FULL

I read each failure point's operative sentence from its own source, to the end of
the sentence, and compared character by character.

- **FP-1** `STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` :402-407. The build's quote
  is **complete and verbatim**, including the clause that cuts hardest against its
  own grade: *"the r-3 pair PROVED the same-power arithmetic cannot close."* ✔
- **FP-2** WBC :408-413. **Complete and verbatim**, including *"rules boundary
  smoothing insufficient."* ✔
- **FP-3** WBC :414-419. **Complete and verbatim**, including *"Witness
  E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS."* ✔
- **FP-S** `STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md` :384-397. **Complete and
  verbatim**, including the closing *"NOT REFUTED: no divergence of tr K^Sigma_n is
  exhibited; the quantity is UNDECIDED with a quantified, logarithmic shortfall."* ✔
- The set {FP-1, FP-2, FP-3} + FP-S and the "no fourth" claim: the FP-n labelling
  verifies at O8SR §2.1/2.2/2.3 ✔ and FP-S at O8SR :295-302 ✔.

**The single defect D3 is most likely to carry — a fragment quoted as (P) or (C)
where the whole sentence reads otherwise — DID NOT OCCUR at any of the four
operative sentences.** Each is quoted whole. This is the target artifact's
strongest section.

The supporting quotations also verify verbatim, and two of them are load-bearing:
- O8SR *"NOTHING is excluded by a clause here ... It does not say, and nowhere does
  the record say, that no other conversion exists"* — verbatim at :184-187 ✔
  (cited :180-183, **COR-L**).
- `STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md` :438-440 *"Setting `cell_N(e) =
  gamma_RL` for a named record cell `e` would BE the identification — it forms the
  common cell, advances R9-JII past PENDING, and is new authorship. REFUSED here."*
  — verbatim ✔, at the exact cited lines.
- `STAGE8_CONDITION_STATEMENT_O22SR_V001.md` :192-196 verifies **verbatim and at
  the exact cited span**, including the preceding sentence *"Neither file asserts
  the antecedent."* that FLAG-5(g) says was added. That repair is real ✔.

### §3.2 **COR-K — FP-1's GRADE DERIVATION NEVER DISPOSES OF THE IMPOSSIBILITY INSIDE ITS OWN QUOTED SENTENCE**

The build quotes *"the r-3 pair PROVED the same-power arithmetic cannot close"* at
:623-625 and then never returns to it. Its derivation of the (C) grade
(:629-666) addresses the requirement clause, the O8SR inventory disclaimer, the
sufficient condition, and the "no member supplies it" phrase — and is silent on the
proved impossibility.

The corpus's own adjudication of FP-1, in the same artifact the build quotes four
times, is not (C)-flat. `STAGE8_CERTIFICATION_RULES_O8SR_V001.md` :505:

> "### 4.2 FP-1 — **MIXED**, and the two halves are cleanly separable"

:508-513, quoted whole:

> "THE DISPLAYED IMPOSSIBILITY (F1-b): re-derived here exactly.
>   rank x op delivers 4n^3 . k against an n-FREE target; against any same-power
>   carrier object (2n^3) the ratio is 2k — n-free, constant, never o(1)
>   [CAS R4a/R4b].  THIS IS REAL AND EXACT.
> ITS QUANTIFIER: **the rank x op ROUTE.**  It says that ONE conversion cannot
>   close.  It says nothing about the existence of a sub-volume trace/HS rate."

:532-538, quoted whole:

> "CLASSIFICATION [DERIVED]:
>   ON THE ROUTE           = MATHEMATICAL (impossibility displayed, exact).
>   AT THE FAILURE POINT   = RULE-IMPOSED-BY-ABSENCE. No mathematics excludes
>     the required object; a SUFFICIENT CONDITION for it is exhibited of record.
>     What blocks is (a) that no member supplies it — an ABSENCE — and (b) the
>     exclusivity word in F1-b (class (iii)) as hardened downstream.
>   NET: **MIXED.**"

and §4.6 at :641-645, quoted whole:

> "IS THE BOUNDARY-CLOSURE BLOCKED BY MATHEMATICS, OR BY ITS OWN ADMISSION AND
> CERTIFICATION RULES?  **BY BOTH, IN A DEFINITE PROPORTION — AND THE
> MATHEMATICS BLOCKS LESS THAN THE RECORD'S SUMMARY LANGUAGE SUGGESTS.**
> Every displayed impossibility in the inventory is REAL.  Not one of them
> ranges over objects of the required kind."

**What survives, and what does not.** O8SR's MIXED is on the mathematical /
rule-imposed axis, which is not identically the (P)/(C) axis, and O8SR's own
quantifier note (:512-513) and its §4.6 second sentence both limit the
impossibility away from objects of the required kind. So the build's **(C) grade
at FP-1 survives**. What does not survive is the tally sentence at :814-815:

> `NO FAILURE POINT IN THE CORPUS IS GRADED (P).  NO FAILURE POINT IS AMBIGUOUS`
> `BETWEEN (P) AND (C)`

asserted without ever displaying that the corpus's own classification of FP-1 is
`**MIXED**, and the two halves are cleanly separable`, and without displaying the
half labelled *"MATHEMATICAL (impossibility displayed, exact)"* or §4.6's *"Every
displayed impossibility in the inventory is REAL."* Those are the
favourable-to-the-other-side clauses of the disposal, and they are absent from the
artifact. Under this program's standing discipline that is a reportable omission,
whatever the grade.

DECIDING BYTES: `STAGE8_CERTIFICATION_RULES_O8SR_V001.md:505` and `:538`.

### §3.3 A SECOND, SMALLER CUT AT FP-3

The build cites `STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md` **:523-527** and quotes
only through :524. The cited-but-unquoted remainder of that block reads, at
:525-528:

> "A sign in {+1,-1}, a membership in ker, a cycle-space dimension: none of these
> is a term of the relation "<=" as the display uses it, because none of them is a
> magnitude in the units the left side carries. X9b/X9e.  THE DEMAND'S OWN GRAMMAR
> EXCLUDES THE TYPE BEFORE ANY RULE DOES."

The build's §3.3 concludes from the quoted half that *"Nothing in it says an X
cannot be made"* (:745). The unquoted half of the range it cited says the demand's
grammar excludes a type before any rule does. It is a type exclusion, not a
producer exclusion, so the (C) grade again survives — but the cited range was
carried past the quotation and the material inside it cuts.

DECIDING BYTES: `STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md:528`.

### §3.4 D3 GRADE

**CONFIRMED-WITH-CORRECTIONS.** All four operative sentences are quoted whole and
verify verbatim; no early closure occurred at any of them; the four grades
(C/C/C, and NEITHER-STATED + (C) for FP-S) each survive independent re-derivation.
Corrections: **COR-K** (the corpus's own MIXED classification of FP-1 and its
§4.6 both undisplayed, against a tally sentence asserting no ambiguity anywhere),
**COR-L** (citation drift).

---

## §4 — D4: THE DISCRIMINATION TEST (Q4) — **REFUTED**

This is where the heaviest effort went. I tested both halves of the build's split
verdict: I tried to break the §4.5(1) null by hunting independently inside the
machinery, and I tested whether the §4.5(2) hit is genuinely discriminating.

### §4.1 THE §4.5(2) HIT — TESTED, AND IT SURVIVES ONLY WITH A FOURTH LIMIT THE BUILD DID NOT DISPLAY (**COR-E**)

The `G_closure` clause verifies **verbatim** at
`PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md` :37-40
(cited by the build as :36-39), in a section headed `## Complete closure object`
at :27. Its three displayed limits are real:

- **L1** the verb is deontic — confirmed, "must act on" (:29), "must generate" (:37).
- **L2** `G_closure`'s codomain — see below, **overstated**.
- **L3** zero contact with the boundary-closure — **CONFIRMED EXACTLY** by my own
  re-run: `(G_closure|record-forming generator|PAIRED_RETURN)` returns 0 in each of
  the eight closure-lane artifacts, 0 for 8.

**THE FOURTH LIMIT, AT BYTES IN THE SAME ARTIFACT, NOT DISPLAYED ANYWHERE IN THE
TARGET.** The same file states its own discharge condition for the very obligation
the build reads as generative. :124-136, quoted whole:

> "## Durability discipline
>
> The source-odd bilinear does not by itself write a record. Durability must be
> proved from the complete `G_closure` by deriving:
>
> ```text
> the recorded source observable;
> source-conditioned distinguishable record states;
> stable pointer sectors;
> a persistence interval;
> recoverability or redundancy;
> causal and gauge-compatible cell stitching.
> ```"

The obligation whose consequent list is *"source-conditioned distinguishable record
states, stable pointer sectors, persistence, and recoverability or redundancy"*
(:37-39) is discharged, at the same artifact's own bytes ninety lines later, by
**proof and derivation** — *"must be proved from the complete `G_closure` by
deriving"* — over the same list. That is on the axis, it is in the same file, and
it is a limit of the same kind as L1/L2/L3. It is absent from §4.3, from §4.5(2),
and from §10.

Two further undisplayed clauses of the same artifact cut the same way:
:76 *"It does not say that the source bilinear equals the complete closure map."*
and :97-99 *"Equivalently, its coefficient is one complex scalar `phi_R`. This is a
classification of the declared local bilinear class. It is not a proof that the
complete closure generator is unique."*

**AND L2 IS OVERSTATED.** The build asserts at :979-981 that `G_closure`'s
*"CODOMAIN is stated NOWHERE in either root under sweep S-5."* At :127-136 the
artifact lists six things to be derived **from** `G_closure`; at :44-47 it states
that *"the local chiral-odd scalar component of `G_closure` is supplied by the same
boundary closure field"*; and at :49-74 it exhibits that component as `a_R`, `b_R`,
`phi_R = a_R + i b_R` inside a displayed action. Whether any of that constitutes a
codomain is contestable and I do not decide it — but "stated NOWHERE" is asserted
without disposing of :127-136, which the build never quotes.

DECIDING BYTES: `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md:126-127`.

### §4.2 **COR-A — THE §4.5(1) NULL WAS PRODUCED BY A SWEEP NARROWER THAN THE BUILD'S OWN DEFINITION OF THE MACHINERY**

§4.5(1) declares, at :1111-1130, that the hunts were run against "the 39 rows of
§1" and, at :833-835, that they were run "against the eight closure-lane
artifacts individually" (the L3 sweep is counted the same way at :999-1002).

CH-2 at :1211-1215 defines the machinery to include **three artifacts that are not
among those eight**: the onset gate, the flux-conditioned write gate, and the
forcing criterion (whose rows FC-1…FC-6 are all sourced to
`STAGE8_FORCING_NOTION_O12SR_V001.md`). The eight are WBC, WSR, O8SR, O6SR, O10SR,
T17SR, T14SR, O11SR. **The machinery as the build defines it spans at least eleven
artifacts; the sweep that produced the null covered eight.**

The three excluded artifacts are not empty on the axis. Section §1.2 above displays
six object-codomain definitions in the two gates. The forcing-notion artifact is
treated at §4.4 below.

The build's own §4.5(1) bullets are therefore each falsified as stated:

```text
BULLET 3 ("no codomain inside the machinery is an object")
  FALSIFIED by SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:83-86 and
  BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:122-123 — see COR-B.

BULLET 2 ("no clause disclaims production for the machinery as such ...
  zero hits inside the machinery")
  FALSIFIED by STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:110-116 — see §4.3.

BULLET 4 ("the closure never remarks on its own status ... none inside the
  machinery")
  FALSIFIED by STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md:586-592 and
  :690-697, both inside the build's own eight — see §4.3.
```

DECIDING BYTES: `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:1214` against `:494-503`.

### §4.3 **COR-D — DISCRIMINATION-AXIS TEXT INSIDE THE BUILD'S OWN EIGHT, NOT LOCATED**

Three spans, all in artifacts the build swept and graded as yielding nothing.

**(i) A CORPUS-NATIVE TYPE TOKEN FOR THE ALLOW/REQUIRE CONSTRAINT.**
`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md` :110-116, quoted whole:

> "- `STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md` (2026-07-29): the
> allow/require CONSTRAINT is typed `CELL_CONSTRAINT_ONLY`
> (`allow_require_standalone_selector_status = CELL_CONSTRAINT_ONLY`); local
> minimality/saturation adoptions named after allow/require are DIFFERENTLY TYPED
> objects — five pure minimality sites, three misattributions, two missed termini.
> Consequence for this commission: "allow-side" below never means "licensed by the
> allow/require constraint"; it means NOT FORCED — the constraint itself is a cell
> constraint, not a selector."

carried a second time at `STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md` :573-575,
quoted whole:

> "The allow/require constraint is typed CELL_CONSTRAINT_ONLY of record and is not a
> selector at all (T14SR §1, the minimality-split correction).  A certification rule
> discarding r-information is not the partition doing anything."

This is a sealed status token typing a named piece of the machinery. It is exactly
the shape of the build's own hunt (b) — a clause disclaiming a production/selection
role for a specific object — and by the build's own §4.2 standard, under which
DIS-1/DIS-2/DIS-3 count as cutting "object by object" against the PRODUCER reading,
this is a fourth such clause **sited inside the machinery**. The build's S-3 pattern
(:109-113) contains no term that could match `CELL_CONSTRAINT_ONLY` or "not a
selector", so the miss is a pattern defect, not a reading defect.

**(ii) THE CORPUS STATES THE DISCRIMINATOR EXPLICITLY, INSIDE THE MACHINERY.**
`STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md` §5 :586-592, quoted whole:

> "**THE TEST, STATED BEFORE IT IS APPLIED.** An UNBUILT REQUIRE-SIDE object is one
> whose **existence is derived** but whose **construction has not been performed**.
> It has a determinate subject and a displayed chain terminating with the admissible
> set a point; only the work is missing. A PERMISSION is one whose existence is not
> derived at all: the record fixes a type and leaves the inhabitant to authorship.
> **The discriminator is therefore DERIVED EXISTENCE, and its subject.** (Uniqueness
> does not discriminate: §2.1 showed all three have displayed sole-ness while none
> has derived existence.)"

and its §5.4 residue at :690-697, quoted whole including the clauses that cut in
both directions:

> "THEREFORE, EXACTLY: the three dischargers are UNINHABITED TYPE SLOTS — neither
> forced by the record nor excluded by it.  "Allow-side" here is a statement about
> the record's FORCING RELATION.  It is NOT a proof that no such object can exist,
> and it supplies NO comfort about the closure's prospects in either direction.
> THE FALSE COMFORT THE COMMISSION FEARS WOULD BE THIS SENTENCE: "they are
> permissions, so the obstacle is not real."  That sentence does not follow and is
> refused here.  The obstacle is exactly as large as it was; what §2 changes is its
> TYPE — from work not yet done to content not yet entered."

*"from work not yet done to content not yet entered"* is the commission's own
distinction — an unconstructed object as a gap, versus an unconstructed object as
what the theory looks like — stated in the corpus's own words, inside the
machinery, and reached as a derived result 3/3. It is not in the target artifact.

**(iii) THE EXISTENCE/UNIQUENESS SPLIT, WHICH IS THE FC-4 QUESTION.**
O11SR §2.1 :168-195 is a section headed *"THE PIVOT, BEFORE THE THREE ROWS:
UNIQUENESS IS DISPLAYED, EXISTENCE IS NOT"*, whose operative paragraph at :190-195
reads whole:

> "**THE SOLE-NESS IS A PROPERTY OF THE DEMAND, NOT OF THE OBJECT.** "Only sealed
> conversion", "only displayed replacement", "only permitted certification" quantify
> over the record's displayed ROUTES; they say which object WOULD discharge, not
> that the object exists. The completion map itself keeps the mood: a landing "at
> derived grade with derived existence-and-uniqueness **WOULD** instantiate SHAPE-1
> (single require, unique root) per leg" (§4.1) — conditional, and its antecedent is
> precisely what is missing. [CAS C3.]"

The build declares at :1132-1136 that FC-4 is *"The one row that SHOULD have decided
it"* and that it does not. At bytes there is a second, closer site — inside the
build's own eight — that separates existence from uniqueness by name, states which
of the two discriminates, and applies it. **Whether these spans finally decide the
commission's binary is INDETERMINATE-AT-BYTES and I do not decide it. What is
decided is that "NO DISCRIMINATING TEXT WAS FOUND" was not established: the hunts
that produced the null did not reach text of exactly the kind they sought, in files
they claim to have swept.**

DECIDING BYTES: `STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md:590`.

### §4.4 **COR-H — HUNT (d)'S "FOUR PLACES" IS AN UNDERCOUNT, AND TWO OF THE MISSES ARE IN A BLOCK THE BUILD QUOTED FROM**

`STAGE8_FORCING_NOTION_O12SR_V001.md` §3.3 :500-520 carries **four** nearest
approaches. The build displays NEAREST-1 (as FC-5) and NEAREST-3 (as REM-3) and
does not display NEAREST-2 or NEAREST-4, both of which are production-versus-
derivation remarks. :506-511, quoted whole:

> "NEAREST-2  "its lawful provenance class UNINHABITED" (AJ, carried at 5d §4.3
>   and T14SR A-1). A quantifier over PROVENANCES, returning empty — the closest
>   SHAPE in the corpus to "across every lawful production". But "provenance"
>   here means DERIVATION ROUTE ON THIS GROUND, not production of a record; the
>   class is uninhabited because no derivation exists, not because no production
>   does."

:517-520, quoted whole:

> "NEAREST-4  A-7's supplier multiplicity: "distinct admissible sections are
>   DISTINCT CANDIDATES; no canonical section is claimed". Alternatives are
>   exhibited over SUPPLIERS of a type slot — producers, not models. It is a
>   candidate-face display, not a notion, and it is used on exactly one row."

**And there is an internal contradiction in how O12SR is scoped.** §1.4 counts six
rows (FC-1…FC-6) sourced to O12SR as *machinery* rows, and CH-2 names "the forcing
criterion and its shapes" as machinery. §4.5(1)'s fourth bullet then says the
located remarks are *"in the census, the common-origin typing, **the forcing-notion
audit**, and AJ — none inside the machinery."* The forcing-notion audit is O12SR.
The same artifact is inside the machinery in §1 and outside it in §4.5(1), and the
difference is what allows the null to stand.

DECIDING BYTES: `STAGE8_FORCING_NOTION_O12SR_V001.md:508-511` against
`STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:1128-1130`.

### §4.5 **COR-F — THE REPORTED SWEEP YIELD IS NOT THE ACTUAL SWEEP YIELD**

§4.1 states at :835-836: *"The entire yield of that sweep is three lines"*, and
§4.5(1) repeats at :1126-1127: *"the entire production-verb yield of the eight
closure-lane artifacts is three lines, all with a bound or a property on the
codomain."*

I re-ran the build's own pattern over its own eight files. The yield is **fourteen
matching lines**, not three:

```text
WBC   :271, :588
O8SR  :269, :364, :1162
O6SR  :43, :53, :280, :477, :730, :1094, :1299, :1462
T17SR :187
```

The build displays three clauses (WBC :271; O8SR :1162; O8SR :269/:364) and does not
display the other eleven. I read all eleven. **The build's substantive claim about
their codomains happens to hold** — O6SR :730 *"The gauge structure present in the
record produces exactly one blocker, and it is the one that changes nothing"* has a
blocker on the codomain; T17SR :187 has a pinned rate; the rest are gauge
neither-creates-nor-removes clauses and a CAS comment. So the finding survives. But
the enumeration on which the reader is asked to check a null is not the enumeration,
and the assurance at :1180-1182 — *"NOTHING WAS MANUFACTURED. The §4.5(1) null is a
null: I ran hunts (a), (b) and (c) against the closure lane individually and file by
file, and the yield was three magnitude-codomain lines and nothing else"* — is false
in its second half.

DECIDING BYTES: `STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md:730`.

### §4.6 **COR-J — THE S-6 CUTOFF'S AUTHORITY IS MISATTRIBUTED**

§0.1 S-6 at :134-136 and §8 at :1422-1424 justify the eight-file scope as *"the
eight artifacts; that is the whole closure lane as T17SR §1 names it."*

`STAGE8_COMPLETION_MAP_T17SR_V001.md` §1 :71-77 names no such set. Its own words,
:73-74, quoted whole:

> "The boundary-closure of record is the WBC pair (80db260f, audit 46938251, audit
> verdict CONFIRMED-WITH-CORRECTIONS: "the build's FAILS-AT" stands)."

`grep -i "closure lane"` returns **zero** hits in T17SR. The eight-artifact set is
the build's own construction — which is lawful as a declared scope choice, and CH-2
declares a scope — but the attribution of that scope to a corpus artifact is not at
bytes, and the 0-of-8 count that carries L3 and the whole §4.5 split inherits the
misattribution.

DECIDING BYTES: `STAGE8_COMPLETION_MAP_T17SR_V001.md:73-74`.

### §4.7 ONE FURTHER ON-AXIS ARTIFACT NEITHER ROOT-SWEEP DISPOSED OF

Sweep S-2's own pattern surfaces
`STAGE8_ACTION_FORM_CLOSURE_THEOREM_SPEC_AND_BUILD_STOP_V001.md` (hits at :63,
:240, :466). §0.1 S-2 declares *"Every non-O13SR hit line was read. DISPOSED IN FULL
AT §2."* That artifact is disposed of nowhere in the target — it is not named once.
Its §2 is headed `## 2. Bounding Or Generating` and reads, at :240-241 and
:254-258, quoted whole:

> "Section 5.3 needs exhaustiveness. A generation rule is the clean sufficient
> form because it gives the audit its member space constructibly."

> "A loose analytic statement such as "no more than this kind of term" does not
> suffice unless it supplies the executable cover and the no-outside proof. In
> this setting, "bounding" and "generating" differ syntactically but converge
> operationally: Section 5.3 can audit only an exhaustively generated class or a
> bounded class with an exhaustive decision procedure."

with frozen tokens at :266-268:

> "exhaustive_generation_rule_suffices_for_Section_5_3 = true
>
> exhaustive_bound_with_no_outside_decision_suffices_for_Section_5_3 = true"

It is outside the machinery, so it does not move §4.5(1) by itself. It is recorded
because it is a corpus-native section on the commission's exact axis, surfaced by
the build's own declared sweep, and declared disposed when it was not. The same
sweep's `STAGE8_MICROSCOPIC_GENERATOR_TOWER_FIXING_PROPERTY_V001.md` :81-83 carries
the same rule at second hand.

### §4.8 D4 GRADE

**REFUTED.**

```text
§4.5(2) — the DISCRIMINATING-TEXT-FOUND half:  the clause verifies verbatim, its
  L3 count verifies exactly, and it is displayed with limits rather than promoted.
  It stands, WITH A FOURTH LIMIT ADDED (COR-E) and L2 corrected from "stated
  NOWHERE" to "not disposed of at :127-136".

§4.5(1) — the OBSERVATIONALLY-EQUIVALENT-AT-BYTES half:  *** DOES NOT STAND. ***
  Not because the opposite is shown, but because the null was never established:
  the sweep set was three artifacts narrower than the build's own definition of
  the machinery (COR-A); three axis-bearing spans inside artifacts it did sweep
  were not located (COR-D); the same artifact is scoped inside the machinery in
  §1 and outside it in §4.5(1) (COR-H); and the enumeration offered to the reader
  as the null's evidence is not the actual enumeration (COR-F).

  CORRECTED STATE OF Q4 WITHIN THE MACHINERY: *** INDETERMINATE-AT-BYTES. ***
  I do not assert that discriminating text exists there; I assert that the
  located spans were not tested, that at least six object-codomain definitions
  inside the machinery were never graded, and that by the build's OWN standard at
  §4.5(2) — where object-codomain signatures and object-by-object disclaimers are
  both counted as cutting — the spans at COR-B and COR-D would have to be weighed
  before any equivalence verdict is available.  No such weighing is on the record.
```

---

## §5 — D5: ADVOCACY AND ADOPTION BAR — **CONFIRMED**

I scanned all 1592 lines of the target for sentences that adopt, recommend, prefer,
or argue for either reading, and for sentences that re-read a catalogued failure.

```text
PATTERNS RUN: (I (recommend|prefer|adopt|conclude that the|argue)|should be read
  as|the (correct|right) reading|is the better reading|therefore the
  (producer|constraint) reading|in favour of|favours the).
RESULT: ZERO HITS.

MANUAL READ of every verdict sentence in §3.5, §4.5, §5 (CHOICE LEDGER) and §10.
FOUND: NO VIOLATION.
```

Three constructions were examined closely and each clears the bar:

1. `:1138-1141` *"CONSEQUENCE, STATED FLATLY AND WITHOUT ADVOCACY: for this
   machinery the reframe buys nothing."* — evaluative in tone, but symmetric: it
   prefers neither reading and asserts only that no text separates them. Not
   advocacy. (Its **support** fails at COR-A/D/F; that is a D4 defect, not a D5 one.)
2. `:702-717` the FP-2 block, headed *"THE (P) READING IS CONTRADICTED HERE AT
   BYTES"* — this is a grade on a failure point, which is Q3's commissioned work,
   and it is derived from a displayed quotation rather than asserted.
3. `:915-918` DIS-4's *"Reading them as evidence for the CONSTRAINT reading would be
   a category error, and I decline it"* and `:1100-1104` REM-4's declining of an
   absence — both are refusals to use available material, i.e. the opposite of
   advocacy, and both are recorded at CH-7 with the temptation named.

The commission's method note on absence is honoured in two places (§2.6(ii),
§4.4 REM-4) with the temptation recorded, as CH-7 claims.

**D5 GRADE: CONFIRMED.** No sentence of the target adopts, recommends, prefers, or
argues for either reading, and no catalogued failure is re-read.

---

## §6 — CORRECTIONS, IN SEVERITY ORDER

```text
COR-A  THE §4.5(1) NULL WAS PRODUCED OVER A SWEEP SET NARROWER THAN THE BUILD'S
       OWN MACHINERY.  CH-2 names eleven-plus artifacts; the hunts covered eight.
       The three uncovered are the onset gate, the flux-conditioned write gate,
       and the forcing-notion artifact — all named in CH-2 and all sourcing §1
       rows (AR-1, AR-5, FC-1..FC-6).
       DECIDES: STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:1214 vs :494-503.

COR-B  "ZERO GENERATIVE ROWS IN THE MACHINERY" IS FALSE AT BYTES.  Six exhibited
       operators sit in two artifacts CH-2 places inside the machinery, each an
       object not among its own defining text's inputs, i.e. GENERATIVE under the
       build's own rule at :153: U_write, K_write,rel, U_chi (flux gate);
       U_0, U_1, H_W (onset gate).  None is a row of the Q1 inventory.
       DECIDES: SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md:83-86.
       CORRECTED TALLY: 39 rows over an item set incomplete by at least six;
         GENERATIVE = INDETERMINATE-AT-BYTES, not 0-with-support.

COR-C  FC-1's DEFINING TEXT IS TRUNCATED.  The forcing criterion is carried in
       the corpus paired with its own lead — "A REQUIRE-SHAPED CONDITION WITH
       DERIVED EXISTENCE AND UNIQUENESS FORCES A NUMBER" — in two of the build's
       own eight artifacts, and one of them states in terms that the criterion is
       to be consumed at bytes and NOT as quoted in citing artifacts.  The build
       consumed the citing artifact and graded the row "MODAL — decisively, and
       by explicit definition."
       DECIDES: STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:76 (also :78-79;
         STAGE8_COMPLETION_MAP_T17SR_V001.md:410-411).

COR-D  DISCRIMINATION-AXIS TEXT INSIDE THE BUILD'S OWN EIGHT, NOT LOCATED.
       (i)  T14SR:110-116 — the allow/require CONSTRAINT typed
            `allow_require_standalone_selector_status = CELL_CONSTRAINT_ONLY`,
            "a cell constraint, not a selector"; repeated O11SR:573-575.
       (ii) O11SR:586-592 — "The discriminator is therefore DERIVED EXISTENCE,
            and its subject."
       (iii)O11SR:690-697 — "from work not yet done to content not yet entered."
       (iv) O11SR:190-195 — sole-ness is a property of the demand, not the object.
       DECIDES: STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md:590.

COR-E  THE G_closure DISCRIMINATOR CARRIES A FOURTH LIMIT AT BYTES, UNDISPLAYED,
       AND L2 IS OVERSTATED.  The same artifact states the obligation's own
       discharge condition as proof-and-derivation over the same consequent list,
       and carries two further non-uniqueness/non-identity clauses.
       DECIDES: PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE
         _V002.md:126-127 (with :127-136, :76, :97-99).

COR-F  THE REPORTED PRODUCTION-VERB YIELD IS NOT THE ACTUAL YIELD.  Claimed
       "three lines"; the build's own pattern over the build's own eight returns
       fourteen.  Eleven were not displayed.  The substantive codomain finding
       survives my reading of all eleven; the assurance at :1180-1182 does not.
       DECIDES: STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md:730.

COR-G  THE "CLOSURE'S ARGUMENT TYPE" IS DISPLAYED FROM ONE SITE AND APPLIED TO
       ALL.  §2.0 fixes it from FP-3's admission clause and FP-3's consuming
       inequality, then grades every census row TYPE-MISMATCH and FEEDS-CLOSURE
       = 0 against it.  The onset gate's argument is "the unique physical record
       cell" and the flux gate's is a charged record handle; neither is displayed.
       DECIDES: BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md:99-101 vs
         STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:328-330.

COR-H  HUNT (d)'s COUNT AND SCOPE.  O12SR §3.3 carries four nearest approaches;
       two are displayed and two — both explicit production-vs-derivation remarks
       — are not.  And O12SR is scoped INSIDE the machinery at §1.4/CH-2 and
       OUTSIDE it at §4.5(1) bullet 4.
       DECIDES: STAGE8_FORCING_NOTION_O12SR_V001.md:508-511 vs
         STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:1128-1130.

COR-I  ITEM-GRANULARITY TALLY ARITHMETIC IS WRONG.  ":274 the NAMED-ONLY count is
       21 and the grand total is 52."  The enumeration is N-1(1) + N-2(1) +
       N-3(1) + N-4(5) + N-5(10) + N-6(1) = 19, and 33 non-NAMED-ONLY rows + 19
       = 52.  21 is inconsistent with the artifact's own 52.
       DECIDES: STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:274.
       CORRECTED TALLY: NAMED-ONLY at item granularity = 19 (not 21);
         grand total at item granularity = 52 (UNCHANGED).
       NOTE: this survived the mechanical recount FLAG-5(f) reports.

COR-J  THE S-6 CUTOFF'S AUTHORITY IS MISATTRIBUTED.  "the whole closure lane as
       T17SR §1 names it" — T17SR §1 names the WBC pair; "closure lane" occurs
       nowhere in T17SR.
       DECIDES: STAGE8_COMPLETION_MAP_T17SR_V001.md:73-74.

COR-K  FP-1's DISPOSAL LEAVES THE IMPOSSIBILITY IN ITS OWN QUOTED SENTENCE
       UNADDRESSED, AND OMITS THE CORPUS'S OWN CLASSIFICATION OF FP-1.  O8SR
       heads its FP-1 section "**MIXED**, and the two halves are cleanly
       separable", grades "ON THE ROUTE = MATHEMATICAL (impossibility displayed,
       exact) ... NET: **MIXED**", and states at §4.6 "Every displayed
       impossibility in the inventory is REAL."  None of that is displayed,
       against a tally sentence asserting NO failure point is ambiguous.
       The (C) grade itself SURVIVES on O8SR:512-513 and :644-645.
       DECIDES: STAGE8_CERTIFICATION_RULES_O8SR_V001.md:505 and :538.

COR-L  LINE-CITATION DRIFT, PERVASIVE; FIVE CITATIONS POINT AT DIFFERENT CONTENT.
       Text verifies verbatim in every case I checked; the cited bytes do not.
         CIS `complete_causal_parent_derived = false` is at :107.  Cited :111
           (twice) and :110-111.  Line :111 holds `alpha_computed = false`.
         O10SR "NOT ONE OF THESE IS A TYPE EXCLUSION ..." is at :571-572.
           Cited :576-580.
         O8SR "EXCLUDED, and by what clause: NOTHING is excluded ..." is at
           :184-187.  Cited :180-183 (three times).
         O8SR F3-b `H(c) :=` is at :262-266.  Cited :254-260 and :255-259.
         O17SR HIT 3 is at :460-461, cited :463-465; the STRUCTURAL READING is at
           :463-465, cited :461-465 — the two ranges are swapped.
       Smaller drift (1-5 lines) at F1-c, F2-c, F2-d, F3-c, F3-d, F3-e, FC-1,
       FC-2, FC-3, FC-4, G1, G6, REM-4, and the WBC verdict block (:397-448 for
       a block ending :445).
       CONSEQUENCE: FLAG-5(g)'s mechanical re-verification normalized and searched
       TEXT; it did not check LINE NUMBERS, which is why this survived it.
       DECIDES: CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md:107 vs :111.

COR-M  THE DECLARED SEAL TALLY DOES NOT COVER THE CONSUMPTION.  28 declared; ten
       further sealed artifacts are cited and relied on, including the one
       carrying the §4.5(2) discriminator and the one carrying the FP-2 primary.
       All ten verify OK on my re-run, so nothing is unsound.
       DECIDES: STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:34.

COR-N  INTERNAL CROSS-REFERENCE DEFECTS.  §0 :31 sends the no-sidecar declaration
       to FLAG-2; it is FLAG-1.  §2.1 :350 sends G-REC-3 to FLAG-5; it is FLAG-2.
       FLAG-5's sub-items run (a)(b)(c)(d)(f)(e)(g).
       DECIDES: STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md:31 vs :1451.
```

### CORRECTED TALLIES, STATED EXPLICITLY

```text
Q1 ROWS               39, unchanged as a row count.
Q1 ITEM SET           INCOMPLETE by at least six (COR-B).
Q1 GENERATIVE         INDETERMINATE-AT-BYTES (was: 0).
Q1 NAMED-ONLY, item granularity   19 (was: 21).  COR-I.
Q1 grand total, item granularity  52 (UNCHANGED).
Q2 CENSUS             NOT EMPTY — CONFIRMED.  Four record/cell producers —
                      CONFIRMED.  Contact 0 of 8 — CONFIRMED EXACTLY.
                      FEEDS-CLOSURE 0 — supported against ONE displayed argument
                      type only (COR-G).
Q2 GENERATION RULES   5 families / 10 individual rules — CONFIRMED at bytes.
Q3 GRADES             FP-1 (C), FP-2 (C), FP-3 (C), FP-S NEITHER-STATED + (C) —
                      ALL FOUR SURVIVE.  The tally sentence "none is ambiguous"
                      is asserted without displaying the corpus's own MIXED
                      classification of FP-1 (COR-K).
Q4 WITHIN MACHINERY   INDETERMINATE-AT-BYTES (was: OBSERVATIONALLY EQUIVALENT).
Q4 OUTSIDE MACHINERY  DISCRIMINATING-TEXT-FOUND — STANDS, with a fourth limit
                      added and L2 corrected (COR-E).
Q4 CORPUS BOOKINGS    "The forced binary is refuted" (:10) and the NO_VERDICT
                      token (:70-73) — both verify verbatim; STAND.
SEAL TALLY            39/39 OK + 1 declared-unsealed (was reported 28/28 + 1).
```

---

## §7 — OVERALL GRADE

```text
D1  SIGNATURE CLASSIFICATIONS      *** REFUTED ***
D2  CANDIDATE-GENERATOR CENSUS     CONFIRMED-WITH-CORRECTIONS
D3  FAILURE RESTATEMENT            CONFIRMED-WITH-CORRECTIONS
D4  DISCRIMINATION TEST            *** REFUTED ***
D5  ADVOCACY AND ADOPTION BAR      CONFIRMED

OVERALL: *** REFUTED. ***

WHAT SURVIVES, SO THE REFUTATION IS NOT READ WIDER THAN IT IS.  The Q2 answer
survives whole: the census is not empty, the four producers are real at their
primaries, the contact test is 0 of 8 on my own re-run, and the generation-rule
recount is right at bytes.  The Q3 answer survives: all four operative sentences
are quoted WHOLE and verify verbatim, and every grade re-derives.  The §4.5(2)
half of Q4 survives with a fourth limit added.  D5 is clean.

WHAT DOES NOT SURVIVE is the pair of headline findings the artifact is built to
carry: "ZERO GENERATIVE ROWS IN THE MACHINERY" and "OBSERVATIONALLY EQUIVALENT AT
BYTES WITHIN THE MACHINERY."  Both fail for the same structural reason, and it is
worth naming once: the artifact fixes the machinery's scope at CH-2 to include the
two gates and the forcing criterion, and then runs the sweeps that produce both
findings over a set that excludes all three.  The excluded set contains six
exhibited operators and, in the artifacts that WERE swept, four spans of on-axis
text.  Neither headline was refuted by me in the sense of being shown false in
substance; both were shown UNESTABLISHED at the bytes offered for them, and one
("ZERO GENERATIVE") is false as literally stated under the build's own rule.

I do not supply the missing grades, do not weigh the located spans against each
other, and take no position on PRODUCER versus CONSTRAINT.  Within the machinery
the state of Q4 on the record after this audit is INDETERMINATE-AT-BYTES.
```

---

## §8 — CHOICE LEDGER (every interpretive choice I made)

```text
CH-1  WHAT COUNTS AS "THE MACHINERY" FOR TESTING THE BUILD.
      ALTERNATIVES: (a) take the build's eight closure-lane artifacts as the
      machinery and judge the null against those only; (b) take CH-2's own words
      and judge the null against the eleven-plus artifacts CH-2 names; (c) impose
      a scope of my own.
      CHOSEN: (b) — the build's own CH-2 text, verbatim at :1211-1215.
      WHY: (c) would be authorship and would let me manufacture a gap.  (a) would
      let the build define the machinery one way in §1 and test it another way in
      §4 without the difference being visible — which is exactly the defect.
      Using the build's own declared scope makes the finding checkable against a
      sentence the build wrote.
      COST, DISPLAYED: under (a), COR-A shrinks to "the build's scope wording is
      inconsistent" and COR-B/COR-G survive only for AR-1/AR-5's rows rather than
      for the machinery.  COR-D and COR-H are unaffected by this choice: they are
      inside the eight either way.
      STATUS: CLOSED.

CH-2  WHETHER TO ASSIGN REPLACEMENT GRADES TO THE SIX EXHIBITED OPERATORS.
      ALTERNATIVES: (a) grade them GENERATIVE and restate the tally;
      (b) grade them MIXED on the strength of their own disclaimers;
      (c) grade none and report the tally as INDETERMINATE-AT-BYTES.
      CHOSEN: (c).
      WHY: (a) and (b) are both classifications the build did not make, on rows
      the build did not open; making them here would be me writing the inventory
      rather than auditing it, and each of the six carries a disclaimer at bytes
      that a fair grading would have to weigh.  What the audit can say without
      authorship is that GENERATIVE = 0 is not supported.
      STATUS: CLOSED.

CH-3  WHETHER THE LOCATED SPANS AT COR-D "DISCRIMINATE".
      ALTERNATIVES: (a) declare them discriminating and flip §4.5(1) to
      DISCRIMINATING-TEXT-FOUND; (b) declare them non-discriminating and let the
      null stand; (c) declare the null unestablished and the question
      INDETERMINATE-AT-BYTES.
      CHOSEN: (c).
      WHY: (a) would decide, on the audit's own authority, a question the
      commission bars me from deciding by preference — and CELL_CONSTRAINT_ONLY
      types one named object, not the machinery as such, exactly as the build's
      own DIS-1..DIS-3 do.  (b) would be adopting the build's verdict on evidence
      the build did not have.  (c) is what the bytes support: text of the kind
      the hunts sought exists in files the hunts claim to have covered, and was
      never weighed.
      STATUS: CLOSED.

CH-4  WHETHER FP-1's (C) GRADE FALLS WITH COR-K.
      ALTERNATIVES: (a) refute the grade, on "PROVED the same-power arithmetic
      cannot close" plus O8SR's "NET: MIXED"; (b) confirm the grade and report the
      omission separately.
      CHOSEN: (b).
      WHY: O8SR's MIXED is on the mathematical/rule-imposed axis, and O8SR itself
      limits the impossibility's quantifier at :512-513 ("It says that ONE
      conversion cannot close") and at :644-645 ("Not one of them ranges over
      objects of the required kind").  The (C) grade re-derives.  What fails is
      the completeness of the disposal and the "none is ambiguous" tally sentence.
      STATUS: CLOSED.

CH-5  HOW TO TREAT CITATION DRIFT.
      ALTERNATIVES: (a) treat a wrong line number as a quotation defect and
      refute the row; (b) verify the text by content search and report the drift
      as a single grouped correction.
      CHOSEN: (b), as COR-L.
      WHY: every drifted quotation I checked verifies verbatim somewhere in the
      cited file, so the FINDINGS do not fall; but a reader cannot re-check a row
      from a wrong byte, and one drifted citation lands on a fence token.  A
      grouped correction with the five worst named is the honest disposition.
      STATUS: CLOSED.

CH-6  WHETHER TO COUNT MY OWN CONTACT-TEST AGREEMENT AS EVIDENCE FOR THE BUILD.
      ALTERNATIVES: (a) treat 0/8 as corroboration of §4.5's split;
      (b) treat it as corroboration of the count only.
      CHOSEN: (b).
      WHY: the count is exact and I confirm it exactly.  What it corroborates is
      that eight named files do not contain those tokens — not that those eight
      are the machinery (COR-J) nor that nothing else in the machinery does.
      STATUS: CLOSED.

CH-7  TREATMENT OF ABSENCE (the commission's method note).
      I located two absences that were available to me as evidence — the eleven
      undisplayed production-verb lines all having non-object codomains, and the
      zero result of my own hunt for a sixth generation-rule family — and gave
      NEITHER any weight for either reading.  Recorded because I was tempted to
      use the first as support for the build's null.
      STATUS: CLOSED.

CH-8  NO ADOPTION.  I state no preference between the readings, make no
      recommendation, and re-read no catalogued failure.  Where the bytes do not
      decide, the row reads INDETERMINATE-AT-BYTES.
      ALTERNATIVES: none lawful.
      STATUS: CLOSED.

ZERO OPEN CHOICES AT SEAL.
```

---

## §9 — TOY_SEPARATION

```text
I CONSTRUCTED NOTHING FOR ILLUSTRATION.

No toy object, no example, no schematic, no worked instance, no substitute
operator, no candidate grade for any corpus row, no model pair, and no numeric
value was built by me at any point in this audit.  There is nothing to separate.

TWO ITEMS THAT MIGHT BE MISTAKEN FOR CONSTRUCTIONS, NAMED SO THEY ARE NOT:
  (1) The phrase "the eleven machinery artifacts", used in my sweep declarations.
      This is not a corpus set and not a set I adopt.  It is the union of the
      build's own eight closure-lane artifacts with the three further artifacts
      CH-2 names, formed only to run the scope test at COR-A, and it is named
      rather than assumed.  No corpus artifact asserts it.
  (2) The corrected tallies at §6.  These are arithmetic on the BUILD'S OWN
      labels and its own enumeration N-1..N-6; they introduce no new label and
      no new row.  Removing them leaves the build's rows intact.

NO FINDING IN THIS AUDIT RESTS ON ANY OBJECT I MADE.  Every correction is
anchored to a file:line in the corpus or in the target artifact.
```

---

## §10 — IMPORT AUDIT (every notion used but not corpus-defined)

```text
IMP-1  "CODOMAIN" / "OUTPUT TYPE" / "INPUTS OF A DEFINING TEXT".
       SOURCE: elementary function typing — but NOT AN IMPORT HERE: the corpus
       carries the same notion natively at STAGE8_INGREDIENT_CENSUS_O17SR_V001.md
       :32-37 ("RULE takes something, yields something ... CONSTRAINT takes
       something, yields a truth value"), and COR-B is stated entirely inside the
       BUILD'S OWN rule at :152-154.
       SURVIVES WITHOUT THE IMPORT: YES.

IMP-2  "MODAL" / "GENERATIVE" as such.
       SOURCE: THE COMMISSION, via the target artifact.  I use them only to test
       the target against its own declared rule, never to assert a grade.
       The corpus's own word "GENERATIVE" means derivational provenance and is a
       DIFFERENT notion (STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001
       :36-38); the target flags this collision at IMP-2 and I inherit the flag.
       SURVIVES WITHOUT THE IMPORT: YES — COR-B can be stated as "six defining
       texts whose yield is an object not among their inputs are absent from an
       inventory that claims to be exhaustive over the machinery."

IMP-3  "PRODUCER READING" / "CONSTRAINT READING".
       SOURCE: THE COMMISSION.  I use neither as a premise and adopt neither.
       NOTE ON THE TARGET'S IMP-3 CLAIM ("Neither phrase occurs in either root"):
       the two-word phrases do not occur, but "producer" does — e.g.
       STAGE8_ACTION_FORM_CLOSURE_THEOREM_SPEC_AND_BUILD_STOP_V001.md:275 ("I1.
       Complete Action / Producer Universe"), :291 ("producer-action universe"),
       :296 ("the producer-algebra dependency"), and O27SR:566 ("asset SLOTS with
       no producer", which the target quotes itself).  The target's claim is true
       as stated about the phrases and would be false about the word.
       SURVIVES WITHOUT THE IMPORT: YES.

IMP-4  "DISCRIMINATE" (true under one reading, false under the other).
       SOURCE: THE COMMISSION.  Used only to name the test the target ran; I
       apply it to no span on my own authority — see CH-3.
       SURVIVES WITHOUT THE IMPORT: YES.

IMP-5  "SCOPE" / "SWEEP SET" arithmetic (eight versus eleven).
       SOURCE: ordinary counting over declared file lists.  Not corpus-defined
       and not corpus-dependent.
       SURVIVES WITHOUT THE IMPORT: YES — it is a count, and every member of both
       sets is named in the target artifact itself.

NO CONTINUUM LIMIT, NO KK REDUCTION, NO METRIC, NO VARIATIONAL PRINCIPLE, AND NO
CLASSICAL-GRAVITY READING WAS USED AS A PREMISE ANYWHERE IN THIS AUDIT.  Where
quoted corpus text contains such notions (the FS/Mandelstam-Tamm theorem at the
onset gate; `sqrt(-g)` inside the quoted G_closure action display), they appear
only inside quotation and carry no inferential weight in any grade I assigned.
No deontic/indicative mood judgement carries weight in any correction: COR-A,
COR-B, COR-D, COR-F, COR-I, COR-J and COR-L are counts and byte locations.
```

---

## §11 — SWEEP CUTOFFS

Declared in full at **§0.1** with each pattern written out (A-1 … A-6). Restated
here in one line each:

```text
A-1  contact test re-run, both patterns, against the build's eight.  STOPPED at
     the eight.  RESULT 0/8 and 0/8 — the build's count CONFIRMED EXACTLY.
A-2  production-verb sweep re-run over eight + three.  STOPPED after every
     matching line in all eleven was displayed and read.  RESULT 14 lines in the
     eight (build reported 3) — COR-F.
A-3  machinery-scope test: the two gates read to their last line (198 and 172
     lines); O12SR §1/§3.2/§3.3 read at bytes.  STOPPED at the three files.
A-4  axis-vocabulary hunt over the eleven.  STOPPED at the eleven; every hit read
     in context — COR-D.
A-5  generation-rule sweep re-run, ROOT-1 *.md.  STOPPED at the file list (30
     corpus files, CONFIRMING the build); I opened the six files the target's
     §2.3 does not dispose of — COR-F's sibling finding at §4.7.
A-6  quotation re-verification of every graded quotation in §1-§4 of the target,
     by content search where the citation was wrong.  STOPPED when every graded
     quotation had been located — COR-L.

NOT SWEPT, AND DECLARED AS NOT SWEPT:
  - Any file whose basename matches the register bar.  Zero were opened.
  - ROOT-2 was NOT swept by me.  Every artifact this audit relies on resolved in
    ROOT-1 at a verified seal, and no correction depends on a ROOT-2 byte.  The
    build's ROOT-2 counts (30 files at S-2; zero FP-4/FP-5/FP-0 hits) are
    therefore NOT independently confirmed by me and are reported as unaudited.
  - The E1, CERT, W-3, AJ and forcing-boundary-adjudication primaries, which are
    not files in ROOT-1; every target row depending on them is marked
    SECOND-HAND-QUOTE by the build (CH-3 lists thirteen) and I inherit that mark
    without upgrading it.  I did NOT verify any second-hand primary.
  - `.py`, `.json`, `.txt`.  No correction depends on them.
```

---

## §12 — FLAG BLOCK

```text
FENCE LINE.  alpha_computed = false · proof_authorized = false ·
  kappa_record_computed = false.  HONOURED.  No numeric value of any coupling,
  scale, root, eigenvalue, or constant was computed by me; no comparison to any
  measured value was made; no CAS was run.  Every fence token above appears
  inside quotation, in the header, or in this line.

SEAL TALLY.  39/39 OK, verified with `shasum -a 256 -c` run from the artifacts'
  own directory before any reliance, plus 1 file confirmed to carry no sidecar.
  Zero mismatches.  Full list at §0.  The target's own digest matches its
  sidecars and matches the build report's claimed digest.

REGISTER-BAR INCIDENTS: *** ZERO. ***  Every sweep I ran was basename-filtered
  before results were read.  No QUESTIONS_SETTLED_REGISTER / QUESTIONSSETTLED_
  REGISTER, EXECUTION_TRACKER, THE_PLAN, ROAD_REMAINING, THE_HANDOFF,
  OBSERVATIONS_REGISTER, *DECISION_SHEET*, *_ADDENDUM*, or alpha_supervision/
  file was opened, read, grepped, listed, or quoted.  I note that the target
  artifact discloses a register basename inside its own FLAG-3 incident report;
  I did not open, seek, or read that file, and I quote no line of it.

FLAG-A  ROOT-2 UNAUDITED.  Declared at §11.  The build's ROOT-2 sweep results are
  carried as CLAIMED and are not confirmed by me.  No correction depends on them.

FLAG-B  SECOND-HAND PRIMARIES UNVERIFIED.  Thirteen target rows rest on primaries
  outside ROOT-1 (E1, CERT, W-3, AJ, the forcing-boundary adjudication).  The
  build marks them; I did not reach them either.  COR-C is NOT one of these: it
  is decided by two ROOT-1 artifacts (T14SR:76, T17SR:410-411) carrying the lead
  the target dropped.

FLAG-C  I DID NOT RE-RUN THE CELL/RECORD OUTPUT-SIDE SWEEP.  The census's claim
  that exactly one clause in the corpus has a cell on its output side is
  inherited from O17SR's own sweep S-C; the build re-read the named primaries but
  did not re-run that sweep either, and neither did I.  Q2's "four producers"
  therefore rests on O17SR's sweep at both levels.  Declared, not hidden.

FLAG-D  DEFECTS FOUND IN MY OWN DRAFT DURING SELF-CHECK, corrected in place:
  (a) I FIRST WROTE COR-B AS A FLAT "GENERATIVE 5" CORRECTION with a replacement
      tally.  Re-reading the two gate artifacts to their last lines found the
      disclaimers at flux gate :88-89 and :163 and onset gate :137-139, which a
      fair grading would have to weigh; I withdrew the replacement grades and
      restated the correction as INDETERMINATE-AT-BYTES (CH-2).  The two findings
      are not the same finding.
  (b) I FIRST GRADED D3 REFUTED on "PROVED the same-power arithmetic cannot
      close."  Reading O8SR :512-513 and :644-645 at bytes shows the corpus
      limits that impossibility's quantifier to one route and states that no
      impossibility ranges over objects of the required kind.  The (C) grade
      re-derives; D3 corrected to CONFIRMED-WITH-CORRECTIONS (CH-4).
  (c) I FIRST COUNTED THE ITEM-GRANULARITY NAMED-ONLY FIGURE AS "the build says
      21, I say 19" without checking which of 19 and 21 is consistent with the
      build's own grand total.  33 + 19 = 52 and 33 + 21 = 54; the build states
      52.  The correction now names WHICH of the two paired figures is wrong.
  (d) MY FIRST CONTACT-TEST RE-RUN used a case-sensitive pattern and would have
      reported a false agreement.  Re-run case-insensitively; 0/8 stands.
  (e) I FIRST WROTE THAT THE BUILD'S "three lines" CLAIM WAS FALSE AND LEFT IT
      THERE.  That would have been an absence used as evidence.  I read all
      eleven undisplayed lines and report that the build's SUBSTANTIVE codomain
      claim survives them (CH-7), so the correction is about the enumeration and
      the assurance, not about the codomains.
  (f) QUOTATION RE-CHECK: every span I quote above was re-read against bytes
      after drafting.  Two were EXTENDED to include a continuation that cuts
      against my own correction: the flux-gate U_write quote (+ "It is not a
      complete microscopic action or a durable record instrument") and the O11SR
      §5.4 residue (+ "It is NOT a proof that no such object can exist, and it
      supplies NO comfort about the closure's prospects in either direction").
      No elision remains inside any quoted span in this artifact.

NO GIT ACTION TAKEN.  No register, tracker, plan, or road file touched, written,
  or read.  Nothing adopted, proposed, recommended, or argued for.
```

**END OF ARTIFACT — STAGE8_CONSTRAIN_OR_CREATE_O36SR_AUDIT_V001**
