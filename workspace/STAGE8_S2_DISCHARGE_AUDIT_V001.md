# STAGE 8 — BLIND CROSS-LINEAGE AUDIT OF THE S2'/R-L2b^Sigma DISCHARGE ATTEMPT (STAGE8_S2_DISCHARGE_V001)

## BLIND INDEPENDENT AUDITOR — CODENAME S2-DISCHARGE-AUDIT — [SEALED]

Date: 2026-08-14
Role: BLIND independent auditor, cross-lineage, default posture skeptical
re-derivation: nothing confirmed below that was not re-derived by this
auditor from the sealed bytes and its own CAS. Under test:
STAGE8_S2_DISCHARGE_V001.md, sha256
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53 —
recomputed at path THIS session, MATCHES the tasked digest; the tasked flag
block matches the artifact's §8 verbatim.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — one independently WRITTEN CAS
battery (sympy 1.14.0, fresh venv `audvenv`), 18 checks, reproduced in full
in §7 with verbatim output; the artifact's own §7 script ALSO run verbatim
(22/22 PASS, output byte-matches its §7 transcript). NOTHING numeric
evaluated; every constant symbolic or exact rational/surd; L formal
throughout. No scale, imported GR, or faithfulness authority. M(t) sharp;
equal-time FALSE by C6. No register/tracker/plan/road/ledger/lens file
read. Output name probed before write: ABSENT. No git action; not
registered, not committed, not pushed.

---

## 0. VERDICT IN ONE LINE

**All five audits land CONFIRMED. The identification Delta^{Sigma,all}_n =
X_n re-derives exactly (bilinearity of the all-pairs weight sum — my A1);
the |C|_4 bookkeeping re-runs to THE SAME POWER, alpha' = 1/4 on the
similarity class, by my own independent chain (|C_L|_4 = pi L^4/24 by
direct integration; tau_R-invariance forcing v_L = v_1(./L)/L; per-slab
matrix-exponent EQUALITY (not similarity) for the source contraction,
tested on TWO independent slabs; Dyson-order L-ledger L^n·L^{-(n-j)} = L^j
— one factor L per J insertion, exactly E1 :829-830's named mechanism;
termwise polydisc split L^m <= L for m >= 1). The forced alpha = beta = 1/2
is cited to its forcing (RL4RL0 §3.2, CHK X1/X2), re-solved by me (unique),
and used correctly — as the per-pair bookkeeping value TESTED and found
non-transferring, which is the adopted clause's own explicitly reserved
outcome. The alpha' = 1/2 negative re-derives as the exact conditional
(divergence (24/pi)^{1/2} d_1/L unless the assembled linear response is
HS-null; no lower bound claimed — r-A3 honest). The quantifier daylight is
REAL AND AT THE ADOPTED BYTES (S2CAND §4.1's "uniformly over the D3
refinement quantifier" vs S2'-O + the §4.1 bracket deferring the cell sup
to R-L0 and taking the carrier sup in trilemma form); the discharge
conflates nothing — it separates the three sups correctly and covers both
readings, leaving the non-congruent remainder exactly where R-L0 stands.
Tasks B and C re-derive to the same FAILS-AT with all walls and both exact
witnesses verified (the star-shaped insufficiency witness is EXACT: +-i
endpoints, zero-free stars, c-combination 0; half-plane separation
sufficiency EXACT; the licensed consequence is verbatim the sealed
conditional E1 :884-885, no stronger). V-1 does NOT fire — my own
re-derivation DELIVERS the summed estimate at the amendment's own
quantifier at alpha' = 1/4; the failure of one candidate VALUE of a symbol
the amendment refuses to assert (NF-1) is not a refutation of the clause,
and V-4 has no purchase because E1 :817-824 itself downgrades the 1/2 to
conditional bookkeeping. Provenance CLEAN: 14/14 seals recomputed by me by
full digest, all matching sidecars, the artifact's §1 table, and the tasked
prefixes; every load-bearing span re-read at bytes. Injection: none
found.**

---

## 1. SEALS AND PROVENANCE, VERIFIED INDEPENDENTLY AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest below recomputed BY THIS AUDITOR from bytes at path this session
(shasum -a 256, full digest); each compared against (i) its `.seal.sha256`
sidecar, (ii) the artifact-under-test's §1 table, (iii) the tasked prefix.
ALL THREE MATCH FOR ALL 15 FILES (the 14 sources + the artifact under
test). NONE UNVERIFIABLE.

```text
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md                  MATCH (under test; tasked digest)
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md           MATCH (S2CAND)
c234600005c83b2373ce1d3a33d680eacc3444964c5f0eb8f5421c546f30bb7b  STAGE8_SUMMED_S2_CANDIDATE_CHECK_V001.md     MATCH (S2CAND-CHK)
cbfbb74c59f5a88742524dc41958514b4c38418d025f85154f6e1d3594ee1a43  STAGE8_SUMMED_S2_ENTRY_ADDENDUM_V001.md      MATCH (ENTRY — adoption of record, read in full)
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md         MATCH (RL4RL0)
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                 MATCH (RL4RL0-CHK; X1/X2 confirm the forcing)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (REM-CHK; NET_VERDICT = CONFIRMED at its bytes)
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH (ZF)
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH (ZF-CHK)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH (R2)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH (R2-CHK)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (RL2B-REFUTED; flag block read in full)
```

Load-bearing spans re-read at bytes by this auditor: E1 :417-426 (D3
frozen unrestricted quantifier — verbatim); :652-666 (R.0 object
V_{mu lambda}(a) = u_mu(a_-)^dag u_lambda(a_+); the Carleman display,
valid ONLY on the surviving sector); :679-689 (R.1 preamble: functionals
of the frozen tuple only, cell sup deferred to R-L0, S1 "certified outward
enclosure" required); :691-708 (S1/S2/S2b; alpha NOT asserted); :720-727
(S4 H-R, "May NEVER default to 1 or to any other value (F'-3)"); :803-833
(R.3; the exponent-explicit x; the :817-824 BOOKKEEPING DOWNGRADE — "holds
for ANY exponent pair summing to 1 and asserts nothing unless the HS norms
are FINITE AT THOSE EXPONENTS"; R-L2b at :826-832 with the mechanism at
:829-830 — "cell time extent from the Duhamel bound, and the Gevrey b_D");
:835-848 (colour-sum closure, sum_surv c = 1, kappa_bal, R.3.a/b, sector
hygiene); :851-894 (R-L0 grounds (i)/(ii); :856 "for EVERY admitted cell
of D3"; :864 "||b_D^{(c)}||_inf = 1 EXACTLY"; :865 "v(t) scale-covariant;
the diamond profile scale-free"; :867-872 the tau_R hazard; R-L0b at
:877-885 with the conditional "If R-L0b closes, R-L0 is not needed for the
n = 1 leg and that must be stated"). PA :75-128 (r(t) = min(t,1-t); sharp
M(t); v(t) = (pi/sqrt2) 32 r(t)^3; b_D with b_D(1/2,0) = 1;
J = -(Q b_D Q) (x) alpha_x; the a J term LINEAR in the generator). S2CAND
§3.3/§4.1/§4.3/§4.4/§5.3/§5.4 (S2'-O; the certification with alpha' a
symbol; R.3'(a)-(e) with the (c) BAR verbatim; D-i..D-iv; V-1..V-6; NF-1
"alpha' is a SYMBOL (not 1/2, not anything)"). S2CAND-CHK P-5 (its own
independent re-derivation of the identification — bilinearity to
U(a_-)^dag U(a_+)). ENTRY (adoption in force; falsifiers in force; two
witnesses LIVE; f-1/f-2 adopted). REM §2.1-§3.5, §5 (X; the trilemma
bound; the compression line; the dominated a-series; grade e-i/e-ii with
named overturn conditions). ZF §4.1(b)(c), §4.2, §4.3 o-1/o-2/o-3.
52f2490b §0 clause + §5 flags (RL2b_uniformity_refuted = true;
X_HS_norm_infinite = true).

---

## 2. AUDIT 1 — THE IDENTIFICATION AND THE |C|_4 BOOKKEEPING (tested hardest)

### 2.1 The identification, re-derived

From the sealed algebra alone: V_{mu lambda}(a) = u_mu(a_-)^dag
u_lambda(a_+) (E1 :654) and U(a) = sum_l w_l u_l(a) with REAL rational
weights (1/2, -1/4, -1/4) give, by bilinearity (my CAS A1, fully symbolic
2x2 matrices):

```text
  sum_{mu,lambda} w_mu^* w_lambda u_mu(a_-)^dag u_lambda(a_+)
      = U(a_-)^dag U(a_+)   EXACTLY.
```

Hence Delta^{Sigma,all}(a) = C(U(a_-)^dag U(a_+) - U(0)^dag U(0))C, whose
a-expansion is REM's X = a_+ C U(0)^dag F C + a_- C F~^dag U(0) C + O(a^2)
with REM §3.5's dominated series carrying every polydisc order. This is
the same identification S2CAND-CHK P-5 re-derived independently; the
discharge consumes it AT REM's CARRIED GRADE (identity-grade core, e-i/
e-ii estimate steps with named overturn conditions), correctly not
upgraded. One convention noted, NOT a finding: the sealed trilemma form of
record (REM §2.1, CONFIRMED 89641f76) equates carrier-uniformity with
HS-finiteness of the uncompressed X via ||Pi X Pi||_2 <= ||X||_2 — the
relation between that sealed form and the n-indexed truncated dynamics is
REM's sealed convention, consumed at grade here as there; the discharge
additionally names its residue honestly (r-A2). IDENTIFICATION =
CONFIRMED.

### 2.2 The bookkeeping, re-run independently — MATCH at alpha' = 1/4

My own chain, written from scratch and CAS-pinned (§7 A2-A8):

```text
(a) |C_1|_4 = pi/24 and int_0^1 32 r_1^3 = 1 (entered-cell data of record)
    re-derived by direct integration [A2]; |C_L|_4 = pi L^4/24 EXACT by
    direct integration of the dilate [A3]: ONE power of L IS
    (24 |C|_4 / pi)^{1/4} — the 1/4 is forced by the 4-volume's QUARTIC
    scaling, nothing else.
(b) tau_R-invariance (the sealed hazard, E1 :867-872) + the sealed v-form
    (v proportional to r^3, PA :86-88) force v_L = tau_R (32/L^4) r_L^3:
    int_0^L v_L = tau_R EXACT and v_L(L s) = v_1(s)/L on BOTH branches
    [A4] — the dressing scales like h_0 = |k| (degree 1 [A6]); the record
    phases are undamped, exactly as the hazard states.
(c) THE SOURCE CONTRACTION, tested harder than the artifact's own pin:
    per-slab the exponent matrices are EQUAL — (H_k/L + a J_k)(L T_k) =
    (H_k + (L a) J_k) T_k for two INDEPENDENT symbolic slabs [A5i] — so
    the slab unitaries are IDENTICAL and the ordered product matches
    slab-by-slab; at the Dyson level the order-n simplex carries L^n and
    each free/dressing factor 1/L, each J insertion 1: net L^j at a^j
    [A5ii, with the order-2 measure instance exact A5iii]. Hence
    V^{(L)}(a) = U_L V^{(1)}(L a) U_L^dag at ALL a-orders, weight-blind —
    ONE time-extent power per Duhamel source insertion with b_D capped at
    1 of record (E1 :864): EXACTLY the mechanism E1 :829-830 names.
(d) HS unitary invariance makes STEP 6 an EQUALITY; the polydisc split is
    termwise-exact: L(sum K_m eps^m) - sum K_m (L eps)^m =
    sum K_m eps^m L(1 - L^{m-1}), every term >= 0 for m >= 1, 0 < L <= 1
    [A7] — one FULL power, no more (the m = 0 term absent because the
    subtracted object vanishes identically at a = 0).
(e) THE EXPONENT: |C_L|_4^{-1/4} (L M) = (24/pi)^{1/4} M — L-FREE [A8i].
    BOOKKEEPING_RERUN = MATCH: alpha' = 1/4 on the similarity class,
    G_hs^Sigma = (24/pi)^{1/4} M_dom(eps), frozen-tuple, carrier-free.
(f) THE NEGATIVE AT 1/2, re-derived: |C_L|_4^{-1/2} (L d_1) =
    (24/pi)^{1/2} d_1 / L -> infinity as L -> 0+ for d_1 > 0, and = 0
    only at d_1 = 0 [A8ii]: alpha' = 1/2 is deliverable on the class ONLY
    IF the assembled linear response is HS-null — the artifact's exact
    conditional, with no lower bound claimed (r-A3 honest; REM's nonzero
    V_2 (x) V_2 leading display cited as structural contra-indication
    only, correctly).
```

**Scope honesty, verified (where a fake discharge would hide):** the
similarity-class scoping is load-bearing and honestly carried. (i) The
"diamond profile scale-free" reading (E1 :865) is consumed as
shape-congruence in normalized coordinates; a cell whose profile is not
the congruent dilate is simply NOT in the class and falls to r-A1 — the
R-L0 axis, named. (ii) L <= 1 is required twice (the termwise split; the
contracted argument L·eps staying INSIDE the closed polydisc where REM's
domination holds) and is exactly the refinement direction; cells outside
that scope are again r-A1's. (iii) The carrier n'-bookkeeping premise is
named and dischargeable by the one sealed compression line (r-A2). No
hidden leak of L into M_dom found: the covariance is an equality, tau_R
and ||b_D||_inf are scale-invariant of record, and the polydisc radius is
frozen.

### 2.3 The forced-exponent usage, audited

The forcing re-solved by me [A9]: {1 - alpha - beta = 0, 1 - 2 alpha = 0}
has the UNIQUE solution alpha = beta = 1/2 — matching RL4RL0 §3.2 and its
check's X1/X2 (re-read at bytes). The discharge cites it correctly AS the
per-pair x-bookkeeping cancellation condition, carries E1 :817-824's own
downgrade (conditional bookkeeping, not a fact), and uses it as the tested
candidate value of alpha' per the adopted clause's own caution ("may not
be assumed"). It does NOT substitute 1/2 anywhere as a premise. Usage
CORRECT.

---

## 3. AUDIT 2 — THE QUANTIFIER DAYLIGHT

Re-examined at the sealed bytes. The three sups separate exactly as the
artifact says: (q-i) the carrier n-family — S2'-O's own certifying
quantifier ("claims about the n-INDEXED FAMILY WITH n-UNIFORM CONSTANTS"),
delivered by the sealed compression line with no cell content; (q-ii) the
cell axis — D3's FROZEN UNRESTRICTED family (E1 :417-426 verbatim; :856
"for EVERY admitted cell"), NOT delivered by the discharged bound (ZF-CHK
correction, carried in the adopted text itself at S2CAND §4.4 D-ii —
verified at bytes); (q-iii) the colour axis — replaced by the sum inside
(adopted). The daylight is genuinely IN the adopted text: S2CAND §4.1
demands G_hs^Sigma "finite UNIFORMLY over the D3 refinement quantifier"
(the phrase whose E1-lineage referent, :826-832, is the CELL family) while
the same §4.1 bracket defers the cell sup to R-L0 and takes the carrier
sup "as in the trilemma form". The discharge conflates nothing; it names
both readings and covers both — carrier axis of record, cell axis newly on
the similarity class, remainder exactly R-L0's. "Under NEITHER reading is
alpha' = 1/2 delivered" — verified: under the carrier-only reading the
|C|_4 powers have no content to deliver (a fixed cell makes every alpha'
trivially rescalable, and the clause demands the mechanism); under the
cell reading 1/2 is contradicted on the class unless the linear response
is HS-null (§2.2(f)). DAYLIGHT_AUDIT = CONFIRMED.

---

## 4. AUDIT 3 — TASK B, RE-DERIVED

The decomposition is an exact identity given the sealed display: with
g_p = Log rho_p = tr[Delta_p] - seg_p (E1 :663-666, surviving sector only
— the discharge respects the validity restriction) and sum_surv c = 1
(re-derived: 5 surviving pairs, N_surv = 1/2, c = (1/2, 1/8 x 4), all
positive [A10]), sum_surv c_p (rho_p - 1 - tr Delta_p) =
sum_surv c_p (-seg_p + (e^{g_p} - 1 - g_p)) [A11]. The tail bound is
coefficientwise exact: n(n-1) - 2 = (n-2)(n+1) >= 0 for n >= 2, checked
n = 2..12 [A12]. The walls:

```text
W-1 CONFIRMED. The segment integrand is per-pair bilinear
    (resolvent_p x Delta_p); no linearity moves the sum inside. The
    artifact's scalar witness (3/4 != 2/3) is of the correct bilinear
    shape; my independent witnesses re-confirm with the LITERAL map
    f(A) = (1+A)^{-1}A — scalars 5/8 != 2/3 and a non-commuting 2x2
    matrix pair [A13]. (Auditor's note, on my own harness: my first
    matrix witness — equal-diagonal upper-triangulars — coincidentally
    SATISFIED equality; witness choice matters, and the corrected
    witnesses settle it. The claim survives its hardest test.)
    R.3'(b)'s trace linearity as the one per-pair-free passage: verified
    at the adopted bytes.
W-2 CONFIRMED. Every sealed-basis majorant of either term factors through
    per-pair HS norms (the |tr[BXY]| <= ||B||_op||X||_2||Y||_2 route is
    E1's own R.3 derivation line); all five c >= 1/8 > 0 makes the
    c-weighted majorant dominate (1/8) max_p [A10] — positivity BLOCKS
    majorant-level cancellation — and the per-pair carrier sups are
    +infinity of record (52f2490b flags verified in the full flag block;
    ZF §4.1(c)). Infinity times L = infinity: the covariance contraction
    cannot rescue a divergence that is n-wise at fixed cell. The flag
    block's "every majorant route" is the body's "every standard
    majorant" — the gap between them is exactly the named residue ("a new
    summed-level identity ... on no sealed artifact"), so nothing is
    overclaimed in net.
W-3 CONFIRMED. The segment consumes rho_res = H-R; F'-3 no-default
    verified at E1 :725-727. The per-pair BAR verified verbatim at S2CAND
    §4.3(c).
```

FAILS-AT is the honest class: the majorants diverge, the OBJECT is
undecided — this is not a refutation of the estimate and the artifact
correctly does not claim one (no V-1 trigger from here). Fixed-n
n-dependence per ZF o-1: verified. TASKB_AUDIT = CONFIRMED.

---

## 5. AUDIT 4 — TASK C, RE-DERIVED (the region claim verified exactly)

The insufficiency witness re-derived exactly [A14]:
1 + sqrt2 e^{+3 i pi/4} = +i and 1 + sqrt2 e^{-3 i pi/4} = -i EXACTLY;
the two segments [1, +i] and [1, -i] form a region star-shaped about 1;
zero-free EXACTLY, with |1 + s(rho - 1)|^2 = (1-s)^2 + s^2 =
1/2 + 2(s - 1/2)^2 >= 1/2 > 0; and the derived c-combination
(1/2)(+i) + 4(1/8)(-i) = 0 EXACTLY. So five surviving rho's inside a
fixed zero-free star about 1 CAN give Z_hat = 0 — the Q-1048 condition as
written at RL4RL0 §3.5 ("localized in a fixed zero-free star-shaped
region about 1") is INSUFFICIENT for Log Z_hat. The correction is real
and is NOT a bytes-contradiction: RL4RL0's own parenthetical example
("e.g. a half-plane Re rho > 0") is convex and remains fine — the
correction sharpens the stated CONDITION to its sufficient normal form.
Half-plane separation sufficiency re-derived exactly with the rotation
carried [A15]: Re(e^{i theta} rho_p) >= delta for all five, all c > 0,
sum c = 1, forces Re(e^{i theta} Z_hat) >= delta, hence |Z_hat| >= delta
— exponentiation-free, no absolute x bound: the R-L0b shape.

The FAILS-AT grounds verified: the localization consumes the linear
display's BOTH terms — the trace term needs S1's certified outward
enclosure (E1 :687, uncertified); the segment consumes H-R (no-default)
and per-pair HS majorants whose carrier sups are +infinity of record; no
fixed n-uniform region is derivable on the discharged basis, and fixed-n
regions are n-dependent (o-1). THE LICENSED CONSEQUENCE audited against
the sealed conditional: E1 :884-885 licenses exactly "If R-L0b closes,
R-L0 is not needed for the n = 1 leg and that must be stated"; the
artifact states exactly that, conditionally only, and adds the honest
inputs a closure would consume. NO STRONGER — verified. TASKC_AUDIT =
CONFIRMED.

---

## 6. AUDITS 4b/5 — V-1 WATCH, NET, PROVENANCE, INJECTION

```text
V-1 WATCH: my own re-derivation DELIVERS the summed estimate at the
  amendment's own quantifier (n-indexed family, n-uniform constants, per
  admitted cell, cell sup deferred to R-L0) at the derived alpha' = 1/4
  on the all-pairs instance. Nothing in my work refutes it. What fails is
  one candidate VALUE (1/2) of a symbol the amendment expressly does not
  assert (NF-1 verbatim: "alpha' is a SYMBOL (not 1/2, not anything)").
  V-1 = NOT-FIRED. V-2: no summed->per-pair bridge exhibited or used.
  V-3: REM/ZF consumed at sealed grade; overturn conditions carried, not
  triggered. V-4: no bytes contradiction — E1 :817-824 itself downgrades
  the 1/2 to conditional bookkeeping ("holds for ANY exponent pair
  summing to 1 and asserts nothing unless..."); a derived 1/4 contradicts
  no sealed FORCED result. V-5: the kill-before-the-norm is the
  discharged chain's own deciding step (REM §3.2, of record). V-6: the
  artifact's honesty markings check out span by span.
NET AUDIT: gate-list membership verbatim-matches ENTRY item 5 (R-L4a/b +
  R-L0(/R-L0b) + C-L2 + OBL-D, H-R/H-B0 inside). Interior moves accurate:
  witness n-1 content delivered subject to check with retirement
  correctly deferred to the check/registrar; n-2 stands with R.3'(c) the
  one live analytic wall (given n-1); n-3 R-L0 unmoved as obligation with
  ground (i) answered positively ONLY on the summed axis and similarity
  class (consistent with RL4RL0 §3.3(c)'s V(0) = I baseline-cancellation
  reading, verified); n-4 R-L0b open with the corrected condition; n-5
  untouched. No flag flips; per-pair record untouched; B-L2* unaltered;
  no FIXED/RUNNING verdict moved. The downstream note is correct: the
  forced 1/2 governs the per-pair bookkeeping only.
PROVENANCE: CLEAN. 15/15 digests recomputed by full digest at path by
  this auditor; all match sidecars, the artifact's §1 table, and tasked
  prefixes; the tasked flag block is byte-identical to the sealed §8;
  ENTRY's adoption is in force with falsifiers live and both new
  witnesses live; REM-CHK's CONFIRMED and RL4RL0-CHK's X1/X2 verified at
  their bytes; every span the discharge leans on re-read and found as
  cited (including the exact line placement of :829-830, :856, :864,
  :865, :884-885).
INJECTION: none. No directive content addressed to auditors, registrars,
  or downstream lanes found in the artifact or in any consumed span; the
  artifact claims no act beyond its authority (no retirement, no entry,
  no flag, no registration); seals were verified from bytes, never from
  claimed substrings.
FENCES: held by the artifact and by this audit. Exact symbolic only; the
  only orderings used are exact; L formal, never instantiated; M(t)
  sharp; equal-time FALSE by C6; no physical quantity computed, bounded
  numerically, or evaluated; alpha' = 1/4 is a derived exponent of the
  bookkeeping variable, CLAIMED pending check, and no number is frozen by
  the artifact or by this audit.
```

---

## 7. THE AUDITOR'S OWN CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

Independently written; sympy 1.14.0, fresh venv `audvenv`. The artifact's
§7 script was ALSO extracted and run verbatim first: its output reproduces
its §7 transcript byte-for-byte, 22/22 PASS. Then this battery (three of
my own harness bugs were fixed en route — a sympy transform signature, a
nonnegative-vs-positive symbol in a limit, and one degenerate witness
choice documented in §4 W-1; no artifact claim was touched by any of
them):

```python
# S2-DISCHARGE-AUDIT independent CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0,
# fresh venv audvenv). Written independently by the auditor from its own
# re-derivation; NOT copied from the artifact's script. Nothing numeric evaluated;
# every constant symbolic or exact rational/surd.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

L, t, T, eps = sp.symbols('L t T epsilon', positive=True)
tauR = sp.pi/sp.sqrt(2)

# A1 — THE IDENTIFICATION: bilinearity of the all-pairs weight sum.
# sum_{mu,lambda} w_mu^* w_lambda u_mu^dag u_lambda = U^dag U with U = sum w_l u_l,
# checked on fully symbolic 2x2 matrices (weights real of record).
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
us = [sp.Matrix(2, 2, lambda i, j, k=k: sp.Symbol(f'u{k}_{i}{j}')) for k in range(3)]
vs = [sp.Matrix(2, 2, lambda i, j, k=k: sp.Symbol(f'v{k}_{i}{j}')) for k in range(3)]
lhs = sp.zeros(2, 2)
for m in range(3):
    for l in range(3):
        lhs += w_v[m]*w_v[l]*us[m].T*vs[l]
U_ = sum((w*u for w, u in zip(w_v, us)), sp.zeros(2, 2))
V_ = sum((w*v for w, v in zip(w_v, vs)), sp.zeros(2, 2))
ok("A1 all-pairs sum w_mu w_lambda u_mu^T v_lambda = (sum w u)^T (sum w v) EXACT (bilinearity)",
   sp.expand(lhs - U_.T*V_) == sp.zeros(2, 2))

# A2 — the entered cell's data re-derived: |C_1|_4 = pi/24 and int_0^1 32 r^3 = 1
r1 = sp.Min(t, 1 - t)
V4_1 = sp.integrate(R(4, 3)*sp.pi*sp.Piecewise((t, t < R(1, 2)), (1 - t, True))**3, (t, 0, 1))
I32 = sp.integrate(32*sp.Piecewise((t, t < R(1, 2)), (1 - t, True))**3, (t, 0, 1))
ok("A2 entered cell: |C_1|_4 = pi/24 EXACT ; int_0^1 32 r_1^3 dt = 1 (so int v_1 = tau_R)",
   sp.simplify(V4_1 - sp.pi/24) == 0 and sp.simplify(I32 - 1) == 0)

# A3 — scale-L cell: |C_L|_4 = pi L^4 / 24 by direct integration; hence L = (24|C|_4/pi)^{1/4}
rL = sp.Piecewise((t, t < L/2), (L - t, True))
V4_L = sp.integrate(R(4, 3)*sp.pi*rL**3, (t, 0, L))
ok("A3 |C_L|_4 = pi L^4/24 EXACT (direct integration) ; one power of L <-> |C|_4^{1/4}",
   sp.simplify(V4_L - sp.pi*L**4/24) == 0
   and sp.simplify((24*(sp.pi*L**4/24)/sp.pi)**R(1, 4) - L) == 0)

# A4 — tau_R invariance forces v_L = v_1(./L)/L: with v_L = tau_R (32/L^4) r_L^3,
# int_0^L v_L = tau_R and v_L(L s) = v_1(s)/L pointwise on both branches
s_ = sp.symbols('s', positive=True)
vL_int = sp.integrate(tauR*(32/L**4)*rL**3, (t, 0, L))
v1 = lambda x: tauR*32*sp.Piecewise((x, x < R(1, 2)), (1 - x, True))**3
vL_at_Ls = tauR*(32/L**4)*sp.Piecewise((L*s_, s_ < R(1, 2)), (L - L*s_, True))**3
ok("A4 int_0^L v_L = tau_R EXACT ; v_L(L s) = v_1(s)/L on both branches",
   sp.simplify(vL_int - tauR) == 0
   and sp.simplify(vL_at_Ls - v1(s_)/L) == 0)

# A5 — THE SOURCE CONTRACTION, tested harder than the artifact's single pin:
# (i) per-slab EXPONENT EQUALITY as matrices (so the unitaries are IDENTICAL,
#     not merely similar) for TWO independent slabs; the ordered product then
#     matches slab-by-slab: V^{(L)} = V^{(1)}(La) for piecewise-constant h.
a_ = sp.Symbol('a')
H1 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'h1_{i}{j}'))
H2 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'h2_{i}{j}'))
J1 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'j1_{i}{j}'))
J2 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'j2_{i}{j}'))
T1, T2 = sp.symbols('T1 T2', positive=True)
ok("A5i two-slab exponents: (Hk/L + a Jk)(L Tk) = (Hk + (L a) Jk) Tk for k = 1,2 (matrix EXACT)",
   sp.expand((H1/L + a_*J1)*L*T1 - (H1 + L*a_*J1)*T1) == sp.zeros(2, 2)
   and sp.expand((H2/L + a_*J2)*L*T2 - (H2 + L*a_*J2)*T2) == sp.zeros(2, 2))
# (ii) Dyson-order L-bookkeeping: order-n term has measure L^n; each free/dressing
#      factor carries 1/L, each J insertion carries 1: net L^j at a^j — the
#      polydisc contraction a -> L a, at EVERY order.
n_, j_ = sp.symbols('n j', positive=True, integer=True)
ok("A5ii Dyson bookkeeping: L^n * L^{-(n-j)} = L^j EXACT (one factor L per J insertion)",
   sp.simplify(L**n_ * L**(-(n_ - j_)) - L**j_) == 0)
# (iii) order-2 measure instance: substitution t = L*tau on the simplex
f, g = sp.Function('f'), sp.Function('g')
t1, t2 = sp.symbols('t1 t2', positive=True)
tau1, tau2 = sp.symbols('tau1 tau2', positive=True)
def simplex2(pp, qq):
    lhs2 = sp.integrate(sp.integrate((t2/L)**pp*(t1/L)**qq, (t1, 0, t2)), (t2, 0, L))
    rhs2 = L**2*sp.integrate(sp.integrate(tau2**pp*tau1**qq, (tau1, 0, tau2)), (tau2, 0, 1))
    return sp.simplify(sp.powsimp(lhs2 - rhs2, force=True)) == 0
ok("A5iii order-2 simplex substitution t = L tau: measure factor L^2 EXACT "
   "(monomial instances (p,q) = (3,5), (1/2, 7/3))",
   simplex2(sp.Integer(3), sp.Integer(5)) and simplex2(R(1, 2), R(7, 3)))

# A6 — homogeneity: |k/L| = |k|/L ; band direction k-hat invariant
kx, ky, kz = sp.symbols('kx ky kz', real=True)
nk = sp.sqrt(kx**2 + ky**2 + kz**2)
nkL = sp.sqrt((kx/L)**2 + (ky/L)**2 + (kz/L)**2)
ok("A6 h_0 = |k| degree 1 under dilation ; projector direction degree 0",
   sp.simplify(nkL - nk/L) == 0 and sp.simplify((ky/L)/nkL - ky/nk) == 0)

# A7 — the power split, coefficientwise on a truncated dominated series with
# symbolic nonnegative coefficients: sum_{m=1}^{6} K_m (L eps)^m <= L sum K_m eps^m
# because L^m <= L for m >= 1, 0 < L <= 1 (each coefficient of the difference
# carries a factor K_m eps^m (L - L^m) >= 0).
Ks = sp.symbols('K1:7', nonnegative=True)
diff = sum(K*(L*eps)**m for K, m in zip(Ks, range(1, 7)))
maj = L*sum(K*eps**m for K, m in zip(Ks, range(1, 7)))
gap = sp.expand(maj - diff)
ok("A7 L * sum K_m eps^m - sum K_m (L eps)^m = sum K_m eps^m L(1 - L^{m-1}) termwise, m >= 1",
   gap == sp.expand(sum(K*eps**m*L*(1 - L**(m - 1)) for K, m in zip(Ks, range(1, 7)))))

# A8 — the exponent verdict: alpha' = 1/4 uniform, alpha' = 1/2 divergent unless d_1 = 0
M_ = sp.Symbol('M', positive=True)
d1 = sp.Symbol('d1', positive=True)
C4L = sp.pi*L**4/24
ok("A8i |C_L|_4^{-1/4} * (L M) = (24/pi)^{1/4} M — L-FREE (n-uniform, cell-uniform on class)",
   sp.simplify(C4L**R(-1, 4)*(L*M_) - (24/sp.pi)**R(1, 4)*M_) == 0)
lead = C4L**R(-1, 2)*(L*d1)     # ||Delta^{(1)}(La)||_2 = L d1 + O(L^2), d1 = linear-response norm
ok("A8ii |C_L|_4^{-1/2} * (L d1) = (24/pi)^{1/2} d1 / L -> oo as L -> 0+ (d1 > 0); = 0 at d1 = 0",
   sp.simplify(lead - (24/sp.pi)**R(1, 2)*d1/L) == 0
   and sp.limit((24/sp.pi)**R(1, 2)*d1/L, L, 0, '+') == sp.oo
   and sp.simplify(lead.subs(d1, 0)) == 0)

# A9 — the per-pair forcing re-solved independently: powers of x's exponent-explicit
# form cancel iff 1 - alpha - beta = 0 and 1 - 2 alpha = 0; unique solution 1/2, 1/2
al_, be_ = sp.symbols('alpha beta')
sols = sp.solve([sp.Eq(1 - al_ - be_, 0), sp.Eq(1 - 2*al_, 0)], [al_, be_], dict=True)
ok("A9 forced exponents: unique solution alpha = beta = 1/2 (re-solved)",
   sols == [{al_: R(1, 2), be_: R(1, 2)}])

# A10 — surviving weights re-derived: phases (+,-,-), 5 surviving pairs,
# N_surv = 1/2, c = (1/2, 1/8 x 4), all > 0, sum = 1; and min c = 1/8 gives
# sum c_p q_p >= (1/8) q_j for each j (nonneg q)
ph = [1, -1, -1]
pairs = [(i, j) for i in range(3) for j in range(3)]
surv = [(i, j) for (i, j) in pairs if ph[i] == ph[j]]
Ns = sum(w_v[i]*w_v[j] for i, j in surv)
cw = {p: w_v[p[0]]*w_v[p[1]]/Ns for p in surv}
qs = sp.symbols('q0:5', nonnegative=True)
cvals = [R(1, 2), R(1, 8), R(1, 8), R(1, 8), R(1, 8)]
dom = all(all(c >= 0 for c in sp.Poly(sum(ci*qi for ci, qi in zip(cvals, qs)) - R(1, 8)*qj, *qs).coeffs())
          for qj in qs)
ok("A10 census: 5 surviving; N_surv = 1/2; c = (1/2, 1/8 x 4) all > 0, sum 1; sum c q >= q_j/8",
   len(surv) == 5 and Ns == R(1, 2) and sorted(cw.values()) == sorted(cvals)
   and sum(cw.values()) == 1 and all(v > 0 for v in cw.values()) and dom)

# A11 — Task B decomposition is an exact identity given the Carleman display:
# with g_p := Log rho_p and seg_p := tr Delta_p - g_p (the segment integral),
# sum c_p (rho_p - 1 - tr Delta_p) = sum c_p (-seg_p + (e^{g_p} - 1 - g_p))
cs2 = sp.symbols('c0:5', positive=True)
gs2 = sp.symbols('g0:5')
trs = sp.symbols('r0:5')
lhsB = sum(c*(sp.exp(g) - 1 - r) for c, g, r in zip(cs2, gs2, trs))
rhsB = sum(c*(-(r - g) + (sp.exp(g) - 1 - g)) for c, g, r in zip(cs2, gs2, trs))
ok("A11 segment decomposition: exact identity (per-pair Carleman + trace linearity)",
   sp.simplify(lhsB - rhsB) == 0)

# A12 — tail bound coefficientwise: 1/n! <= 1/(2 (n-2)!) iff (n-2)(n+1) >= 0, n >= 2;
# verified exactly for n = 2..12 and factored symbolically
nn = sp.symbols('nn', integer=True)
ok("A12 |e^z - 1 - z| <= |z|^2 e^{|z|}/2 coefficientwise: n(n-1) - 2 = (n-2)(n+1); n=2..12 checked",
   sp.expand(nn*(nn - 1) - 2 - (nn - 2)*(nn + 1)) == 0
   and all(sp.factorial(n) >= 2*sp.factorial(n - 2) for n in range(2, 13)))

# A13 — W-1 witness, literal map f(A) = (1+A)^{-1}A, scalars AND non-commuting
# matrices, grouped derived weights (1/2, 1/2):
fA = lambda Am: (sp.eye(2) + Am).inv()*Am if hasattr(Am, 'inv') else Am/(1 + Am)
lhs_s = R(1, 2)*fA(sp.Integer(1)) + R(1, 2)*fA(sp.Integer(3))     # 1/2*1/2 + 1/2*3/4 = 5/8
rhs_s = fA(R(1, 2)*1 + R(1, 2)*3)                                  # f(2) = 2/3
N0 = sp.Matrix([[0, 1], [0, 0]])
N1 = sp.Matrix([[0, 0], [1, 0]])
lhsW = R(1, 2)*fA(N0) + R(1, 2)*fA(N1)
rhsW = fA(R(1, 2)*N0 + R(1, 2)*N1)
ok("A13 resolvent pair-diagonality: scalar 5/8 != 2/3; non-commuting matrix witness differs too",
   lhs_s == R(5, 8) and rhs_s == R(2, 3) and lhs_s != rhs_s
   and sp.simplify(lhsW - rhsW) != sp.zeros(2, 2))

# A14 — Task C insufficiency witness, re-derived: rho = 1 + sqrt2 e^{±3 i pi/4} = ±i;
# segments from 1 are star-shaped about 1; zero-free with |seg|^2 = (1-s)^2 + s^2 >= 1/2;
# and the derived c-combination is 0 EXACTLY
r_p = 1 + sp.sqrt(2)*sp.exp(3*sp.I*sp.pi/4)
r_m = 1 + sp.sqrt(2)*sp.exp(-3*sp.I*sp.pi/4)
seg_pt = 1 + s_*(r_p - 1)
mod2 = sp.expand_complex(seg_pt*sp.conjugate(seg_pt))
ok("A14 witness: rho_pm = ±i EXACT; |1 + s(rho-1)|^2 = (1-s)^2 + s^2 >= 1/2 > 0; "
   "(1/2)(+i) + 4(1/8)(-i) = 0 EXACT",
   sp.expand_complex(r_p - sp.I) == 0 and sp.expand_complex(r_m + sp.I) == 0
   and sp.simplify(mod2 - ((1 - s_)**2 + s_**2)) == 0
   and sp.simplify(sp.expand((1 - s_)**2 + s_**2 - R(1, 2)) - 2*(s_ - R(1, 2))**2) == 0
   and sp.expand_complex(R(1, 2)*r_p + 4*R(1, 8)*r_m) == 0)

# A15 — half-plane separation sufficiency, with the rotation e^{i theta} carried:
# Re(e^{i th} rho_p) >= delta for all p => Re(e^{i th} Z) >= delta => |Z| >= delta
th_ = sp.Symbol('theta', real=True)
xs = sp.symbols('x0:5', real=True)
ys = sp.symbols('y0:5', real=True)
rhos = [x + sp.I*y for x, y in zip(xs, ys)]
Z = sum(c*r for c, r in zip(cvals, rhos))
ReZrot = sp.re(sp.expand_complex(sp.exp(sp.I*th_)*Z))
lin = sp.expand(ReZrot - sum(c*sp.re(sp.expand_complex(sp.exp(sp.I*th_)*r))
                             for c, r in zip(cvals, rhos)))
xr, yi = sp.symbols('xr yi', real=True)
ok("A15 Re(e^{i th} Z) = sum c_p Re(e^{i th} rho_p) EXACT (all c > 0, sum 1); |z| >= Re z",
   lin == 0 and sum(cvals) == 1
   and sp.expand((xr**2 + yi**2) - xr**2) == yi**2)

print("AUDIT-BATTERY-DONE")
```

Output, verbatim (18/18 PASS):

```text
A1 all-pairs sum w_mu w_lambda u_mu^T v_lambda = (sum w u)^T (sum w v) EXACT (bilinearity): PASS
A2 entered cell: |C_1|_4 = pi/24 EXACT ; int_0^1 32 r_1^3 dt = 1 (so int v_1 = tau_R): PASS
A3 |C_L|_4 = pi L^4/24 EXACT (direct integration) ; one power of L <-> |C|_4^{1/4}: PASS
A4 int_0^L v_L = tau_R EXACT ; v_L(L s) = v_1(s)/L on both branches: PASS
A5i two-slab exponents: (Hk/L + a Jk)(L Tk) = (Hk + (L a) Jk) Tk for k = 1,2 (matrix EXACT): PASS
A5ii Dyson bookkeeping: L^n * L^{-(n-j)} = L^j EXACT (one factor L per J insertion): PASS
A5iii order-2 simplex substitution t = L tau: measure factor L^2 EXACT (monomial instances (p,q) = (3,5), (1/2, 7/3)): PASS
A6 h_0 = |k| degree 1 under dilation ; projector direction degree 0: PASS
A7 L * sum K_m eps^m - sum K_m (L eps)^m = sum K_m eps^m L(1 - L^{m-1}) termwise, m >= 1: PASS
A8i |C_L|_4^{-1/4} * (L M) = (24/pi)^{1/4} M — L-FREE (n-uniform, cell-uniform on class): PASS
A8ii |C_L|_4^{-1/2} * (L d1) = (24/pi)^{1/2} d1 / L -> oo as L -> 0+ (d1 > 0); = 0 at d1 = 0: PASS
A9 forced exponents: unique solution alpha = beta = 1/2 (re-solved): PASS
A10 census: 5 surviving; N_surv = 1/2; c = (1/2, 1/8 x 4) all > 0, sum 1; sum c q >= q_j/8: PASS
A11 segment decomposition: exact identity (per-pair Carleman + trace linearity): PASS
A12 |e^z - 1 - z| <= |z|^2 e^{|z|}/2 coefficientwise: n(n-1) - 2 = (n-2)(n+1); n=2..12 checked: PASS
A13 resolvent pair-diagonality: scalar 5/8 != 2/3; non-commuting matrix witness differs too: PASS
A14 witness: rho_pm = ±i EXACT; |1 + s(rho-1)|^2 = (1-s)^2 + s^2 >= 1/2 > 0; (1/2)(+i) + 4(1/8)(-i) = 0 EXACT: PASS
A15 Re(e^{i th} Z) = sum c_p Re(e^{i th} rho_p) EXACT (all c > 0, sum 1); |z| >= Re z: PASS
AUDIT-BATTERY-DONE
```

---

## 8. FLAG BLOCK

```text
IDENTIFICATION = CONFIRMED(
  Delta^{Sigma,all}_n = X_n re-derived independently: bilinearity of the
  all-pairs weight sum gives sum w_mu^* w_lambda u_mu(a_-)^dag
  u_lambda(a_+) = U(a_-)^dag U(a_+) exactly [A1, real weights of record];
  the a-expansion is REM's X with the dominated series carrying all
  polydisc orders; matches S2CAND-CHK P-5's independent derivation;
  consumed at REM's carried grade, correctly not upgraded; the sealed
  trilemma-form convention (carrier sup via ||Pi X Pi||_2 <= ||X||_2) is
  REM's of record, consumed at grade, with the discharge's r-A2 naming
  the index bookkeeping honestly.)

BOOKKEEPING_RERUN = MATCH(
  alpha' = 1/4 on the similarity class — THE SAME POWER, re-derived by an
  independently written chain: |C_L|_4 = pi L^4/24 by direct integration
  so one power of L IS (24|C|_4/pi)^{1/4} [A3]; v_L = v_1(./L)/L forced
  by tau_R invariance, both branches [A4]; degree-1/degree-0 homogeneity
  [A6]; source contraction V^{(L)}(a) = U_L V^{(1)}(La) U_L^dag at all
  a-orders — per-slab matrix-exponent EQUALITY on two independent slabs
  [A5i] + Dyson L-ledger one factor L per J insertion [A5ii/iii], exactly
  E1 :829-830's named mechanism; HS unitary invariance an equality;
  termwise polydisc split absorbing exactly ONE power [A7];
  |C_L|^{-1/4}(L M) = (24/pi)^{1/4} M L-FREE [A8i]. The negative at 1/2
  re-derived: (24/pi)^{1/2} d_1/L divergent unless the linear response is
  HS-null [A8ii] — the exact conditional, r-A3 honest. The forcing
  alpha = beta = 1/2 re-solved unique [A9], cited to RL4RL0 §3.2/CHK
  X1-X2 correctly, used only as the tested per-pair candidate per the
  adopted clause's own caution. Scope honesty verified: the
  similarity-class restriction is load-bearing (profile congruence; the
  L <= 1 refinement direction keeping La inside the closed polydisc) and
  every out-of-scope cell lands in the named r-A1 = R-L0 axis. No hidden
  L-leak into M_dom found.)

DAYLIGHT_AUDIT = CONFIRMED(
  the three sups separate at the bytes exactly as reported; the daylight
  is genuinely in the adopted text (S2CAND §4.1's "uniformly over the D3
  refinement quantifier" carrying the E1 :826-832 cell-family discipline
  vs S2'-O + the §4.1 bracket deferring the cell sup to R-L0 and taking
  the carrier sup in trilemma form); NO conflation in the discharge —
  both readings covered, ZF-CHK's correction honored via S2CAND §4.4
  D-ii, the non-congruent remainder assigned to R-L0 unchanged; "under
  neither reading is alpha' = 1/2 delivered" verified (carrier-only
  reading delivers no power content; cell reading contradicts 1/2 on the
  class unless HS-null).)

TASKB_AUDIT = CONFIRMED(
  decomposition re-derived as an exact identity [A11] on the sealed
  Carleman display (surviving sector only, respected); tail bound
  coefficientwise exact [A12]; W-1 confirmed with the artifact's witness
  of the correct bilinear shape AND my independent literal-map witnesses
  (scalar 5/8 != 2/3; non-commuting matrices) [A13]; W-2 confirmed (all
  c >= 1/8 > 0 [A10]; per-pair carrier sups +infinity of record 52f2490b/
  ZF §4.1(c); contraction cannot rescue an n-wise divergence at fixed
  cell); W-3 confirmed (H-R no-default F'-3 at E1 :725-727); the per-pair
  BAR verified verbatim at S2CAND §4.3(c); FAILS-AT the honest class —
  majorants diverge, object undecided, correctly NOT claimed as a
  refutation; flag-block phrase "every majorant route" = body's "every
  standard majorant", the gap exactly the named residue, nothing
  overclaimed in net.)

TASKC_AUDIT = CONFIRMED(
  the star-shaped insufficiency witness verified EXACTLY [A14]: endpoints
  +-i, star-shaped about 1, zero-free with |seg|^2 = 1/2 + 2(s-1/2)^2 >=
  1/2, c-combination (1/2)(+i) + 4(1/8)(-i) = 0; the correction is real
  and contradicts no sealed bytes (RL4RL0 §3.5's half-plane EXAMPLE was
  convex and stands; the stated CONDITION was insufficient); half-plane
  separation sufficiency exact with the rotation carried [A15]; FAILS-AT
  grounds verified (S1 enclosure uncertified per E1 :687; H-R; per-pair
  HS carrier-divergent of record; fixed-n regions n-dependent per o-1);
  the licensed consequence is verbatim the sealed conditional E1
  :884-885, stated conditionally only, NO STRONGER.)

V1_STATUS = NOT-FIRED(
  this auditor's own re-derivation DELIVERS the summed estimate at the
  amendment's own quantifier at the derived alpha' = 1/4; the failure of
  the candidate value 1/2 on the cell axis is not a refutation of a
  clause whose exponent is an unasserted symbol (NF-1 verbatim); V-2
  through V-6 checked severally — none engages; V-4 in particular has no
  purchase because E1 :817-824 itself downgrades the 1/2 to conditional
  bookkeeping.)

NET_VERDICT = CONFIRMED(MEMBERSHIP-UNCHANGED-INTERIOR-MOVES —
  the gate list verbatim-matches ENTRY item 5 (R-L4a/b + R-L0(/R-L0b) +
  C-L2 + OBL-D, H-R/H-B0 inside); interior moves accurate as stated
  (witness n-1 content delivered SUBJECT TO CHECK, retirement correctly
  deferred; n-2 stands with R.3'(c) the one live analytic wall; n-3 R-L0
  unmoved with ground (i) answered positively only on the summed axis and
  similarity class, consistent with RL4RL0 §3.3(c); n-4 corrected
  condition; n-5 untouched); no flag flips; per-pair record untouched;
  B-L2* unaltered; no FIXED/RUNNING verdict moved; the downstream note
  correct — the forced 1/2 governs per-pair bookkeeping only.)

PROVENANCE = CLEAN(
  15/15 digests (artifact under test + 14 sources) recomputed by full
  digest at path by this auditor; all match sidecars, the artifact's §1
  table, and tasked prefixes; the tasked flag block byte-matches the
  sealed §8; every load-bearing span re-read at bytes and found as cited;
  ENTRY adoption in force, falsifiers live, both new witnesses live;
  REM-CHK CONFIRMED and RL4RL0-CHK X1/X2 verified at their bytes; the
  artifact's §7 script re-run verbatim reproduces its transcript 22/22.)

INJECTION = none(
  no directive content addressed to auditors/registrars/lanes in the
  artifact or any consumed span; the artifact claims no act beyond its
  authority — no retirement, no entry, no flag, no registration; seals
  verified from bytes, never from substring presence.)

MACHINERY_USED_BY_ME = yes(CAS) — sympy 1.14.0, fresh venv audvenv: the
  artifact's §7 script run verbatim (22/22 PASS, byte-matching output),
  plus an independently WRITTEN 18-check battery (§7, 18/18 PASS, three
  auditor-harness bugs fixed en route and disclosed); shasum/file reads
  otherwise; NOTHING numeric evaluated.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
S2_DISCHARGE_AUDIT_RESULT = SEALED.
```
