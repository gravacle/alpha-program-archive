# STAGE 8 — O54SR AUDIT: DEFAULT-REFUTE AUDIT OF `STAGE8_COMPLEX_PRIMARY_O54SR_V001.md`

Lane: DEFAULT-REFUTE AUDIT. Default verdict REFUTED. Adversarial by mandate.
Date: 2026-08-16
Target: `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_COMPLEX_PRIMARY_O54SR_V001.md`
Target sha256: `57dab825480c1a82432ae1f423369f8f915ab14477d33523762725dd5f0902e7`

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No authoring. No advocacy. No adoption. Nothing computed; no magnitude approached.
Every claim below was re-derived at bytes. The build's text was treated as CLAIMED only.

---

## 0. HEADLINE

```text
D1  Primary or decomposition-of (Q1), heaviest  = REFUTED
D2  What is entered (Q2)                        = CONFIRMED-WITH-CORRECTIONS
D3  The split (Q3)                              = CONFIRMED-WITH-CORRECTIONS
D4  Dynamics (Q4)                               = CONFIRMED-WITH-CORRECTIONS
D5  The discriminator (Q5)                      = CONFIRMED-WITH-CORRECTIONS
D6  Imports and bars                            = REFUTED

OVERALL                                         = REFUTED

INDEPENDENT Q1 GRADE (this lane)                = INDETERMINATE-AT-BYTES
```

The build's Q1 rests on a census of "three complex-valued sites" in which the record's own
complex is the only one without decomposition-of grammar. That census is incomplete at
bytes. Three sealed corpus artifacts use the phrase **"the record complex proper"** and
gloss it as **`C_ref`** — a class frozen inside `BID V011` itself, of *"oriented,
shape-regular periodic regular-CW **cellulations**"* whose common refinements *"preserv[e]
the same smooth coframe and connection."* The build cited the very line-span that carries
that freeze and quoted only its last sentence.

Both grammars are in the corpus, both attach to the record, and no sealed sentence
identifies or separates them. That is an indeterminate, not a PRIMARY.

---

## 1. D1 — PRIMARY OR DECOMPOSITION-OF. **REFUTED.**

### 1.1 What the build got right, verified at bytes

Re-read at the cited lines, byte-identical, adverse clauses included:

- `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md :281-292` — verified. `:281` reads
  *"An object of `BareRec_2` is"*; `:287-292` reads *"where `K` is a finite oriented regular
  CW complex of dimension at most two, each connected component carries zero or one
  distinguished root `r`, `L` assigns a one-dimensional Hermitian fiber to every vertex, and
  `U` is the discrete unitary connection below. The first-opening object has exactly one
  root. Rootless objects are retained so every elementary cell has a test inclusion."*
  **No genitive in that sentence. The build's reading of this one sentence is correct.**
- `V011 :826-835` — verified whole, including *"the higher-dimensional cells of the
  four-torus are not included."*
- `STAGE8_BASE_GEOMETRY_O48SR_V001.md :629`, `:632-635`, `:620-623`, `:637`, `:934` —
  all verified verbatim at the cited lines, including *"and nothing else"* and
  *"The corpus does not identify these objects."*
- `STAGE8_TASK4D_…_V001.md :19-26` — verified whole.
- `STAGE8_TASK5_EQ6_Q408_…_LANE2_V001.md :123`, `:226`, `:231`, `:244` — verified.
- `BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md :12-19, :21-28, :30-44, :71-72,
  :74-79` — verified whole; the built refinement law is graph→graph, no continuum at either
  end. **This part of the build survives.**
- `triangulation of` / `discretization of` — independently re-swept over both roots plus the
  28 rulings: **0 hits** outside the target artifact itself. I widened the pattern to bare
  `discretiz` (24 hits, 12 unique, all opened): lattice free-tail discretization, time
  discretizations, and numerical replay. **None is a discretization of a continuum into `K`.**
  The build's nil return is real and survives a broader sweep than it ran.

### 1.2 THE SITE THE BUILD MISSED — `C_ref`, and the corpus's own phrase for it

**`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md :1397-1412`, quoted whole:**

> "The local coefficient must also be cellulation independent. Freeze an
> admissible class `C_ref` of oriented, shape-regular periodic regular-CW
> cellulations with:
>
> ```text
> cubical bisection;
> oriented simplicial/barycentric subdivision;
> and common refinements preserving the same smooth coframe and connection.
> ```
>
> The response map must commute with pullback to a common refinement, and the
> intensive quadratic coefficient must be invariant under each elementary
> refinement up to a boundary term whose ratio to four-volume tends to zero.
> One hypercubic sequence is a regression fixture, not proof of universality.
> Failure to prove this naturality either blocks the general claim or restricts
> the result explicitly to that cellulation."

The build cited **exactly this span** (`O54SR :128-130`: *"V011 itself adds at `:1397-1412`
… that 'One hypercubic sequence is a regression fixture, not proof of universality.'"*) and
reproduced only the span's **penultimate sentence**, omitting the freeze that occupies
`:1397-1405`. The freeze is booked by V011's own status block:

`V011 :2285-2286`, whole:

> "```text
> cellulation_refinement_class_frozen = true
> cellulation_independence_proved = false
> ```"

`C_ref` is therefore not a passing remark: it is a **frozen** class of *cellulations*, in
the same file and the same authority as `BareRec_2`, whose refinements are required to
**preserve a smooth coframe and connection** — an object the cellulations sit on, not one
they carry.

**The corpus binds `C_ref` to the record complex by name.** Sweep `record complex proper`,
both roots plus the 28 rulings: 3 hits, all opened, all sealed OK.

`STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md :409-416` (**seal OK**), quoted whole:

> "The five carriers are the sealed WORKING-CLASS INSTANCE: the single oriented
> unit 4-cell and its licensed refinements — the same instance the whole lineage
> binds to (738/795/807/D0SQ/AND-CLASS). They are a TOY relative to: (t1) the
> record complex proper — C_ref is a frozen AUDIT class with no sealed incidence
> realization (POSED trap T1), and no multi-cell physical complex exists (O11/
> O12/O-D1 gap); (t2) full D3 — "all common refinements", star-refined atoms of
> unbounded facet count (9a0c2045); (t3) loop-carrying carriers (H^1 > 0), where
> the harmonic block is nonzero and the AND-class question REOPENS of record."

`STAGE8_OBLD_TRANSPORT_STEP_S9AD_AUDIT_V001.md :275-279` (**seal OK**), whole:

> "Everything this audit re-derived lives on the sealed WORKING-CLASS INSTANCE:
> the single oriented unit 4-cell and its five licensed refinement instances plus
> executed composites. It is a TOY relative to (t1) the record complex proper
> (C_ref = frozen AUDIT class, no sealed incidence realization; no multi-cell
> physical complex exists), (t2) full D3 ("all common refinements", star-refined
> atoms of unbounded facet count, F-2 bars finite pinning) …"

and `STAGE8_RA27_RESPONSE_LEGS_S9AD_V001.md :411` carries the same gloss.

**Read exactly:** the corpus's own name for the object this commission is auditing is *"the
record complex proper"*, and in all three occurrences the corpus glosses that name as
**`C_ref`** — a class of cellulations over a smooth coframe. The build's Q1 asserted at
`O54SR :62-67` that *"There is no genitive. Nothing is named that `K` decomposes"* and at
`:47` that the decomposition-bearing sites are *"expressly **not** the record."* Both are
false of `C_ref`.

### 1.3 THE SECOND MISSED SITE — the sealed working-class instance is itself a subdivision

`STAGE8_AND_CLASS_INDEPENDENCE_V001.md` (**seal OK**) `:330-348`, quoted whole:

> "BINDING.  The verdict binds to the sealed working-class instance: the unit-4-cell
>   record complex and its licensed refinements (A0/A1/A2 and composites; the five
>   complexes of §3.2) — the same instance every artifact of this lineage binds to
>   (738's energy solve, 795's coframe, 807's variety, the D0-SQUARE pair), and the
>   instance on which the tasked admissible space (the 1887-dim variety + the 32-dim
>   freedom) is DEFINED of record.
>
> THE EXACT REOPENING CONDITION, stated rather than hidden.  dim H = dim ker(d_1) -
>   rank(d_0) is precisely the first-cohomology dimension of the sealed 2-skeleton.  On
>   ANY sealed record complex whose 2-skeleton has ker(d_1) = im(d_0) — in particular
>   every licensed subdivision of a single 4-cell, and every cellulation of a
>   contractible record region — phi_H ≡ 0 identically and this verdict carries over
>   verbatim.  A sealed record complex with dim H > 0 (nontrivial first cohomology)
>   would give phi_H a nonzero-dimensional range and REOPEN the AND-class question at
>   that level.  NO such complex is sealed of record: the multi-cell/exhaustion side is
>   UNBUILT (charter O-D1; port anchoring O12), and C_ref is a frozen AUDIT class with
>   no sealed incidence realization (trap T1).  This is a scope boundary, not a caveat
>   on the decision: the question as posed — over the sealed admissible space — is
>   DECIDED, not conditional."

Three phrases here are decomposition-of grammar attached to the words *record complex* and
*record region*, in a sealed artifact, none of them disowned:

```text
"the unit-4-cell record complex and its licensed refinements"      :330-331
"every licensed subdivision of a single 4-cell"                    :340
"every cellulation of a contractible record region"                :340-341
```

This is structurally the same shape the build graded DECOMPOSITION-OF at SITE 2 (`K_L` =
2-skeleton of a four-torus) — a 2-skeleton read off a higher-dimensional cell — except that
here the corpus calls the object *the record complex* and calls the instance *sealed*, not a
test fixture. The build's SITE census does not contain it.

### 1.4 THE THIRD MISS — the `Ref` contract, truncated before its adverse sentences

The build quoted `STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md :246-257` and
stopped. The contract continues. `:245-264`, quoted whole:

> "The R4 specialization requires
>
> ```text
> J_ref : Ref -> CompletedCurrentKernelClass,      (D2-1)
> ```
>
> where an object of `Ref` carries at least:
>
> ```text
> finite oriented record complex;
> Gate-4 boundary/incidence and degree;
> connection and source incidence;
> algebra and representation;
> coframe/volume and current-density normalization;
> ```
>
> and every refinement arrow carries compatible intertwiners, restriction,
> current/curvature transport, and response pullback. **Every pair representing
> the same region must admit a common refinement. No choice of cellulation,
> filtration, frame, or realization member may enter the output.**"

The build wrote at `O54SR :262-263`: *"The one decomposition-flavoured phrase in that table
is `refinement = a certified map of the same represented physical region`. It is a **gloss on
an unbuilt category**, and 'represented physical region' is not tied to `K` anywhere."*

**That is false at bytes.** In the same contract block, the object that carries a *"finite
oriented record complex"* is the object subject to *"Every pair representing the same region
must admit a common refinement"* and *"No choice of cellulation … may enter the output."*
The tie is one blank line away from the text the build quoted, in the sentences it cut.

The corroborating tie is explicit elsewhere. `STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md`
(**seal OK**) `:663-684`, quoted whole:

> "For one physical-region address
>
> ```text
> a = (Omega, theta=e dx, A, boundary/support data),
> ```
>
> Here `theta` and `A` are comparison-address fields carried by V011's demand.
> They neither define `Ref/J_ref` nor select its arrows, and no smooth background
> is imported as the record's source.
>
> The future source category would have to contain objects `K in C_ref(a)` and
> elementary arrows preserving that same address:
>
> ```text
> r : K -> R,
> r' : K' -> R,
>
> K --r--> R <--r'-- K'.
> ```
>
> The future cospan would have to be a common refinement of two cellulations of
> the same addressed region, not merely a combinatorial subdivision symbol."

`K in C_ref(a)` — the record symbol, ranging over a cellulation class indexed by a
**physical-region address** containing a region `Omega`, a coframe and a connection. The
adverse clause is carried: *"no smooth background is imported as the record's source"*, and
the same file books the object at `:602` as **"undetermined / TYPE-U"**.

The same file at `:560-569` glosses V011's own inventory, whole:

> "[PROVABLE] The physical raw material named by V011 is:
>
> ```text
> finite oriented regular-CW cellulations K;
> oriented cells, incidence, boundary, degree, and face flux;
> a coframe theta^a=e^a_mu dx^mu;
> a connection A and its face/curvature data;
> cell four-volume and bivector pullback;
> the stagewise response mathcal_K_K on F_phys(K).
> ```"

**"cellulations K"** — the record symbol under the decomposition-of noun, attributed to V011.

### 1.5 THE FOURTH MISS — `K` in an exhaustion of `R^4`

`V011 :1260-1273`, quoted whole:

> "Thus the Hessian of `Gamma` is additive without inserting an extensive
> factor. For a connected cellulation the complete amplitude must additionally
> admit a linked-cluster expansion
>
> ```text
> Gamma_K=sum_(connected clusters C subset K) gamma_C,
> ```
>
> with a target-free locality bound that makes
>
> ```text
> gamma_density=lim_(K exhausts R^4) Gamma_K/number_of_4_cells(K)
> ```
>
> exist and makes boundary corrections subextensive."

`lim_(K exhausts R^4)` and `number_of_4_cells(K)`. The symbol `K` is placed in a family
exhausting a continuum and is credited with 4-cells — in the file whose `BareRec_2` clause
caps `K` at dimension two. The build's Q5 §5.1(ii) reported *"NONE IN THAT DIRECTION"* for
continuum limits and never displayed this line. Its **direction** claim survives (this is a
limit *of* the discrete, not a cut *from* a continuum) but the line is squarely inside the
Q1 evidence set and was not put on the page.

### 1.6 The build's decisive negative is scoped narrower than it was used

`TASK4D :22-26` reads, whole and in context: *"Moreover, **the 1,088 objects** are
linear-extension support filtrations of one fixed unfilled `K_square`, not full `J_ref`
realizations, not cellulations of a continuum region, and not the charter's decompositions
of an admitted atom into causal diamonds. No sealed map identifies these three choice
spaces."*

The subject is **the 1,088 support filtrations of one fixed `K_square`**. The build rendered
it at `O54SR :316-317` as *"the record-side objects are 'not cellulations of a continuum
region', in the corpus's own words"* — generalizing a sentence about 1,088 filtration
objects into a statement about the record's complex. The same paragraph's own residue runs
the other way, `:38-41`, whole:

> "R2  geometric realization -> decomposition-independent completed response
>     missing: connection/curvature transport, response naturality,
>              common-refinement coherence, boundary subextensivity, and the
>              public physical interface."

The corpus's roadmap for these very objects terminates in a **decomposition-independence**
requirement — which presupposes decompositions to be independent of.

### 1.7 A minor typing the corpus does not supply

The build placed its SITE 3 (`r_G:|G| -> X_o`) as *"BASE-2, not BASE-3"* (`O54SR :160`).
`O48SR :628` types BASE-2 as *"the `Ref_a` finite cell carrier (A0/A1/A2 and composites)"*.
`X_o` and the PL refinement category are not named in that row, and no quoted sentence
identifies them. The placement is the build's, not the corpus's.

### 1.8 MY INDEPENDENT Q1 GRADE

## **INDETERMINATE-AT-BYTES**

```text
PRIMARY-SHAPED GROUND (real, verified)
  V011 :281-292  BareRec_2 object declaration; no genitive; nothing named that K
                 decomposes; the tuple's other slots are carried ON K
  O48SR :629     "roots, incidence, orientation — and nothing else"
  REFINEMENT QUOTIENT :12-19,:30-44   the one BUILT refinement law is graph->graph
  triangulation of / discretization of  = 0 hits, both roots + 28 rulings

DECOMPOSITION-SHAPED GROUND AT THE RECORD (real, verified, unreported by the build)
  V011 :1397-1405   C_ref FROZEN: "oriented, shape-regular periodic regular-CW
                    cellulations" with "common refinements preserving the same
                    smooth coframe and connection"; V011 :2285 frozen = true
  OBLD S9AD :412 / OBLD S9AD AUDIT :277 / RA27_RESPONSE_LEGS :411
                    "the record complex proper" == C_ref, three sealed artifacts
  AND_CLASS :330-341 "the unit-4-cell record complex and its licensed refinements";
                    "every cellulation of a contractible record region"
  TASK6 :560-569    V011's raw material glossed "finite oriented regular-CW
                    cellulations K"
  TASK6 :673        "objects K in C_ref(a)", a = (Omega, theta, A, boundary data)
  JREF :251-264     Ref objects carry "finite oriented record complex" AND
                    "Every pair representing the same region must admit a common
                    refinement. No choice of cellulation ... may enter the output."
  V011 :1271        gamma_density = lim_(K exhausts R^4) Gamma_K/number_of_4_cells(K)

WHAT THE CORPUS REFUSES (also real)
  CENSUS :282       C_ref: "Selected audit class; no theorem derives it as the
                    physical record complex."
  OBLD S9AD :412-413 C_ref has "no sealed incidence realization"
  TASK6 :602        addressed physical Ref/J_ref = "undetermined / TYPE-U"
  TASK6 :670-671    "no smooth background is imported as the record's source"
  V011 :2286        cellulation_independence_proved = false
  TASK4D :63        CELLULATION_INDEPENDENCE_PROVED = false | TYPE-U
  O48SR FL-1 :934   "The corpus does not identify these objects."

DECIDING: no sealed sentence identifies BareRec_2's K with C_ref, and none separates
them either. The corpus names the decomposition-shaped object "the record complex
proper" and simultaneously books it unrealized. The bytes carry both grammars for the
same referent and decide neither.

Q1 = INDETERMINATE-AT-BYTES
BOTH-AT-DIFFERENT-SITES = NOT SUSTAINED — the two grammars are not at different
  sites; they meet at the phrase "the record complex proper" (OBLD S9AD :412).
```

### D1 GRADE: **REFUTED.**

---

## 2. D2 — WHAT IS ENTERED. **CONFIRMED-WITH-CORRECTIONS.**

### 2.1 The decider verifies exactly

`STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md :340-342`
(**seal OK** — see §6), re-read at the line, quoted whole with its numbering:

> "2. The general `BareRec_2` category permits any finite oriented regular CW
>    complex of dimension at most two.  A permissive object class does not select
>    a member."

Verified byte-identical. The build cited it as `:341-342` and `:340-342` in different
places; the sentence begins at `:340`. **The which-complex / there-is-a-complex distinction
is genuinely in the corpus's own words, at this line. D2's core survives.**

`V011 :502-505` (E-6), `V011 :262-274` (§2.2), `O48SR :732` (E-4/E-5), `CENSUS :353`
(E-3) — all verified verbatim at the cited lines.

### 2.2 CORRECTION — the itemization omits an entered class-of-cellulations

E-1…E-7 do not include the one thing V011 books as **frozen**:

```text
E-8 (missing)  A frozen refinement class of CELLULATIONS
               ENTERED — V011 :1397-1405 "Freeze an admissible class C_ref ..."
               booked V011 :2285  cellulation_refinement_class_frozen = true
```

This matters for the build's own inference. `O54SR :436-437` concludes: *"Because 'which
complex' is not entered, **the entered thing is not a stipulated decomposition.**"* The
premise is sound; the conclusion does not follow while a *class of cellulations* is
separately entered and frozen. What is entered is (i) an object class that selects no
member, **and** (ii) a frozen cellulation-refinement class over a smooth coframe. The second
is decomposition-shaped and is entered by the imperative **"Freeze"**.

### 2.3 CORRECTION — locator error, twice

The build cites `K_L_forced_by_record_composition = false | TYPE-R` at `CENSUS :355-357`
(`O54SR :122-123`) and at `CENSUS :355-356` (`O54SR :400`). At bytes, `:355-357` reads:

> "  exclusions: section 3.2 |
>   query: section 3.3 |
>   file_list: section 3.4"

— the continuation of a **different** booking. The `K_L` booking is at `:359-360`:

> "K_L_forced_by_record_composition = false | TYPE-R |
>   test: V011's quantified BareRec_2 object class and general composition law do
>         not imply the separately instructed periodic test object"

The booking exists and says what the build said. The locator is wrong in both citations.

### D2 GRADE: **CONFIRMED-WITH-CORRECTIONS.**

---

## 3. D3 — THE SPLIT. **CONFIRMED-WITH-CORRECTIONS.**

### 3.1 The grade survives, and the build's quotation correction was right on its own terms

`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` (**seal OK**) `:21-29`, whole:

> "## Common closed dilation
>
> Let the source, designated record subsystem, and required environment form
> one closed Hilbert space. From one common ready state `|r>`, the two
> source-conditioned closed-dilation unitaries are
>
> ```text
> U_0(tau), U_1(tau).
> ```"

Verified: **no product symbol in the gate's bytes**, imperative `Let`, adjective
*"designated"*. `:92-95`, `:97-108`, `:164`, `:182-183`, `:188` all verified verbatim,
including *"Saturation is adopted, not derived."* `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md :165`
verified: `complete_source_record_environment_operator_derived = false`.

**CHOSEN is the right grade and I reach it independently** — and it is now *better* grounded
than the build had it, because a second, sealed, imperative source exists (§3.2).

### 3.2 CORRECTION — the uniqueness claim is false at bytes, and the tensor split IS in a sealed corpus principle

The build asserts twice:

- `O54SR :504` (X-1): *"**Sweep result: the strings "designated record subsystem" and
  "required environment" occur in the corpus at exactly one file — this gate — and nowhere is
  either defined, derived, or tied to a cell of `K`.**"*
- `O54SR :960` (FL-4): *"The strings … occur at exactly one location in both roots —
  `ONSET GATE V003 :23`, `:93`. There is no second definition, no derivation…"*

Independent sweep, both roots plus the 28 rulings, exclusion array applied, self-exclusion
counted separately:

```text
"designated record subsystem"  kept = 15 lines | self-excluded = 7 | barred filtered = 0
"required environment"         kept = 14 lines | self-excluded = 7 | barred filtered = 0
```

Distinct workspace bearers, all opened: `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003`,
`STAGE8_ENVIRONMENT_FACTOR_O31SR_V001`, `STAGE8_THE_JOIN_O42SR_V001`,
`STAGE8_GAUGE_AS_CONTENT_O34SR_V001`, `STAGE8_GAUGE_AS_CONTENT_O34SR_AUDIT_V001`,
`STAGE8_SPONTANEOUS_FORMATION_O47SR_V001`, `STAGE8_RECORDS_PLURAL_O30SR_V001`,
`STAGE8_JOIN_FRONTIER_O51SR_V001`, plus the cleanroom mirror. **Eight files, not one.**

More seriously, the build's §3.2 told the reader the `⊗` is *"O47SR's typing"* and inferred
*"a three-way split asserted in prose is even less derived than one asserted in symbols."*
The symbolic three-factor statement **is** in the corpus, in a sealed primitive principle.

`PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md` (**seal OK**,
older convention `<stem>.seal.sha256`) `:27-35`, quoted whole:

> "## Complete closure object
>
> The complete record-forming generator must act on the source, primitive
> record carrier, and all required boundary/environment degrees:
>
> ```text
> G_closure acts on
>   H_source tensor H_record tensor H_boundary/environment.
> ```"

The verb is **"must act on"** — a requirement, not a derivation. **The CHOSEN grade is
unaffected and is strengthened.** But the build's premise for its own "quotation-integrity
correction" was that no corpus-native symbolic split exists. It does.

### 3.3 CORRECTION — an entire prior commission on Q3's object was not consulted

`STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md` (**seal OK**, 1396 lines) is a commissioned
determination on exactly the third factor. It is cited nowhere in the build. Its findings
bear directly on X-1:

`O31SR :275-320`, the role-type census over **282 Sense-A lines**, and its finding, quoted
whole:

> "*** THE SHAPE OF THE CENSUS IS THE FINDING. The two largest classes are
>     INDEX-IN-A-FLAG-NAME and MENTION-IN-A-DEBT-LIST — together roughly three
>     quarters of all Sense-A lines. The factor is overwhelmingly named as part
>     of the NAME OF SOMETHING THAT DOES NOT EXIST (`complete_source_record_
>     environment_operator_derived = false`) or as an ITEM IN A LIST OF WHAT IS
>     OWED. It is named as a subject of a positive assertion in no sealed
>     sentence located by any sweep. ***"

and `:329-346`, whole:

> "### §2.1 ITS TYPE — FOUR INCOMPATIBLE TYPINGS, ALL SEALED
>
> ```text
> TYPING 1  A HILBERT SPACE.  "H_source tensor H_record tensor
>           H_boundary/environment."   PRIMITIVE_..._PAIRED_RETURN_V002 :34
> TYPING 2  A SET OF DEGREES OF FREEDOM.  "all required boundary/environment
>           degrees"                     ibid. :30
>           "the physical edge/environment degrees required by durable closure"
>                                        FULL_CARRIER_..._GATE_V001 :145
> TYPING 3  A SECTOR.  "another boundary/environment sector"
>                                        R3_4_..._PULLBACK_SPEC_V001 :87
> TYPING 4  A TERM IN AN ACTION.  "+B_boundary/environment(g,A,record)."
>                                        BID_GLOBAL_CAR_..._V001 :142  UNSEALED
> ```
>
> A Hilbert space, a set of degrees, a sector, and a functional are four
> different kinds of object. No sealed sentence relates them. The corpus uses
> the four interchangeably."

This is consistent with CHOSEN and would have sharpened it. Its absence is a coverage
failure, not a grade failure. **FL-4 as written must be withdrawn.**

### 3.4 X-2…X-8 spot-checked

`V011 :328-335` (vertex gauge, indexed by `K`'s vertices) and `V011 :309-315` (degree
carriers, direct sums over `K_0`, `K_1^+`, `K_2^+`) verified verbatim — **CONSTITUTIVE is
correct for X-2 and X-3.** Write-gate `:153`, `:155` verified. Onset `:92-95`, `:97-108`,
`:182-183`, `:188` verified. X-8's ABSENT survives my own sweep.

### D3 GRADE: **CONFIRMED-WITH-CORRECTIONS.**

---

## 4. D4 — DYNAMICS. **CONFIRMED-WITH-CORRECTIONS.**

### 4.1 Every grade re-derived; the nil returns hold on a wider net

`PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` (**seal OK**)
`:39-41`, whole:

> "The complete microscopic source-plus-record generator must be a unitary,
> phase-complete dilation. Open-system channels and durable record instruments
> are obtained only after tracing or conditioning downstream degrees."

`:43-58` (excluded primitive mutations, including *"independent Lindblad/dephasing rates"*)
and `:62-75` (*"The rule chooses a physical hierarchy"*) verified whole.

`PRIMITIVE_BINARY_CLOSURE_GENERATOR_GATE_V001.md :80-82`, whole:

> "The fundamental evolution remains unitary. A dephasing or Lindblad rate is
> not a primitive coefficient; decoherence and durable amplification must be
> computed from downstream degrees under the transport-only principle."

D-1…D-12 each opened at its defining text: onset `:21-29`, `:41-45`, `:57-63`, `:115-124`;
write gate `:80-86`, `:93-99`, `:103-107`; `V011 :294-299`. **All grades verified.**

Independent modified-dynamics sweep, both roots plus the 28 rulings, exclusion array,
self-exclusion counted separately:

```text
pattern                                    kept   self-excl   barred filtered
GRW                                          0        —             0
continuous spontaneous localization           0        —             0
stochastic Schrod                             0        —             0
nonlinear Schrod                              0        —             0
modified Schrod                               0        —             0
collapse term                                 0        —             0
spontaneous collapse                          0        —             0
objective collapse                            0        —             0
Diosi | Penrose collapse | CSL model          0        —             0     [added by me]
wave-function collapse                        0        —             0     [added by me]
stochastic (unravel|noise|term)               0        —             0     [added by me]
non-linear (evolution|dynamics|term)          0        —             0     [added by me]
Lindblad                                      6        9             0
```

**All twelve nil returns hold, including four patterns the build did not run.** The six
Lindblad lines were opened; all exclusionary. **UNMODIFIED is the right verdict and I reach
it independently.**

### 4.2 CORRECTION — the evolution census is incomplete, and "0 INDETERMINATE" is overstated

Two evolution objects are absent from D-1…D-12:

**`G_closure`**, the corpus's own *"complete record-forming generator"*
(`PRIMITIVE_..._PAIRED_RETURN_V002 :29-35`, quoted at §3.2). It is not typed unitary at that
site. `STAGE8_JOIN_FRONTIER_O51SR_AUDIT_V001.md :719` books it, whole:

> "  G_closure     domain displayed, codomain untyped ................ 1"

A generator whose **codomain is untyped** cannot be graded UNITARY at bytes. It is plausibly
covered by D-8's principle if it is *"the complete microscopic source-plus-record
generator"*, but **the corpus does not state that identification.** Under the build's own
rubric this is a thirteenth object grading **INDETERMINATE**, which contradicts
`O54SR :591` `INDETERMINATE = 0 objects`.

**The Kraus family.** `COMPLETE_QSPEC_RELATIVE_HISTORY_TRANSFER_MAP_SPEC_V001.md :100-113`,
whole:

> "On the existing two-cell finite parent:
>
> 1. build each cell's three Kraus operators directly from its discretized
>    source-record unitary;
> 2. verify `sum_q K_q dagger K_q=I` for each diagonal branch;
> …
> This numerical check verifies the implementation of the theorem. The theorem
> itself is the finite-dimensional partial-trace identity and does not depend
> on the chosen test angle."

Built *from* a unitary, and the theorem is a partial-trace identity — **NON-UNITARY-AS-REDUCTION**,
consistent with the build's typing but not counted by it.

Neither omission changes UNMODIFIED. Both falsify the completeness claim.

### D4 GRADE: **CONFIRMED-WITH-CORRECTIONS.**

---

## 5. D5 — THE DISCRIMINATOR. **CONFIRMED-WITH-CORRECTIONS.**

### 5.1 The two reported near-misses were handled correctly

`DECISION_OF_RECORD_007_SMOOTH_FORK_DERIVE_THE_LIMIT_2026-08-01_V001.md` opened **whole**
(26 lines, **seal OK**). `:8-10`, `:19-20`, `:21-24` verified verbatim. The build's three
grounds for refusing it are sound: it is an assigned obligation (*"is to be met by"*,
*"becomes a NAMED OBLIGATION"*), its decisive word *"cellulation-independence"* presupposes
alternative cellulations, and its subject is *"the record surface"*/*"base"*, not `K`.

`BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md :21-28`, `:43-44`, `:71-72` verified
whole. The refusal is sound: the equivalence class is generated *from* the complexes, so the
sentence is true under both readings.

`DISCRIMINATOR_MANUFACTURED = false` — I find no manufactured discriminator. The
twice-booked prior negative verifies: `STAGE8_COMMON_ORIGIN_..._TYPING_V001.md :71-80` and
`:11-18` (**seal OK**), `O36SR :1044-1060`, `O47SR :1782-1787`, `:1805`, `:1825-1830` — all
verbatim at the cited lines.

### 5.2 CORRECTION — a third and stronger near-miss was not reported

`V011 :1404`, inside the `C_ref` freeze:

> "and common refinements preserving the same smooth coframe and connection."

Test it the way the commission requires. Under DECOMPOSITION-OF, the clause is well-typed:
alternative cellulations of a smooth carrier share its coframe and connection, and refinement
preserves them. Under PRIMARY — the complex is the record, its cells constitutive, nothing
standing behind it — there is no prior smooth coframe for a refinement to *preserve*. This is
closer to a discriminator than either near-miss the build reported.

The corpus flags the same thing itself. `STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md`
(**seal OK**) `:158-162`, quoted whole:

> "[PROVABLE] G3's admissibility clause names *"the same **smooth** coframe and
> connection."* DoR-007 grounds base-smoothness as existing *"only as a refinement
> limit"* whose *"cellulation-independence is the theorem's content."* So G3 consults,
> as an input to the refinement relation, an object that the refinement theorem is
> supposed to produce."

**I still refuse it as a discriminator, and I state why.** Under PRIMARY the clause is not
*false* — it is ill-typed/vacuous, which is a different failure mode; and `CENSUS :282`
disowns the identification (*"Selected audit class; no theorem derives it as the physical
record complex"*). **`Q5 = OBSERVATIONALLY-EQUIVALENT-AT-BYTES` survives** — but only after
three near-misses are reported and refused, not two. `NEAR_MISSES_REPORTED = 2` is wrong.

### 5.3 CORRECTION — the "corpus never asserts the positive claim" absolute is refuted

`O54SR :366-368` and FL-1 (`:957`): *"The corpus never asserts that nothing stands behind
`K`. **PRIMARY here is a residue, not a claim**"* and *"The corpus nowhere makes the positive
assertion."*

Two sealed positive assertions of the architecture exist and the build quoted one of them:

`DoR-007 :21-22` (a permitted ruling), whole:

> "3. Grounding: the record surface is continuous fibers over a discrete base (OBS-12);
>    base-smoothness exists only as a refinement limit; the limit's cellulation-independence
>    is the theorem's content."

`BARE_SURFACE_I2_DETERMINATION_V001.md :36` (**seal OK** — see §6), the relevant clause,
whole:

> "connection/holonomy histories as sole index of the dynamics, no native metric anywhere,
> no logarithm taken (§I.1); **continuous fibers over a discrete base** (§I.2); …"

*"Continuous fibers over a discrete base"* is a positive structural assertion that the base
is discrete and smoothness derived. It is not the sentence *"nothing stands behind `K`"* —
the build's §5.2 count 3 correctly notes the subject is the record surface, not `K` — but the
**absolute** form of FL-1 does not survive.

### D5 GRADE: **CONFIRMED-WITH-CORRECTIONS.**

---

## 6. D6 — IMPORTS AND BARS. **REFUTED.**

### 6.1 The commission's named hazard was handled well

The build's I-1 declaration is the right instinct and I confirm it: no CW-complex theorem was
imported to force `K` into a filtration of a space. I re-checked every Q1 ground and found no
place where standard discrete-geometry theory does the work. **On the hazard the commission
actually named, the build is clean.**

Import spot-check on the remaining rows: I-3's *"A **closed dilation** is unitary by
construction"* (`O54SR :550`) is dilation theory, but the corpus itself writes *"the two
source-conditioned closed-dilation **unitaries**"* at `ONSET V003 :24-25`, so no grade
depends on it. I-5 verified: `ONSET V003 :67` reads *"Import the standard Fubini-Study/…"*
and `:180` books `relative_orthogonalization_bound_imported = true`.

**One undeclared import, load-bearing.** `O54SR :258-259`: *"the corpus's built extension
operation explicitly does **not** shrink a mesh, **which is the operation a
continuum-discretization reading would require.**"* The corpus states `zero_ext = append
identity coordinates; no mesh shrinkage` (`JREF :132`). What a continuum-discretization
reading *requires* is the build's own premise, from outside the corpus, and it carries the
§1.1(b) argument. It appears in no IMPORT AUDIT row.

### 6.2 THE BARS — three false corpus-wide absence claims

The bar violated here is not import; it is the reverse. A default-refute standard treats an
asserted absence as a claim needing bytes. Three fail:

```text
CLAIM (O54SR :45-47)  "The corpus contains three complex-valued sites."
AT BYTES              At least five. C_ref (V011 :1397-1405, frozen), and the
                      unit-4-cell record complex with its licensed refinements
                      (AND_CLASS :330-331) are absent from the census.

CLAIM (O54SR :504, :960)  the two split strings occur "at exactly one file" /
                      "exactly one location in both roots"
AT BYTES              8 distinct workspace files + the cleanroom mirror; 15 and 14
                      kept lines respectively.

CLAIM (O54SR :262-263)  "'represented physical region' is not tied to K anywhere"
AT BYTES              JREF :251-264 ties them in one contract block; TASK6 :673
                      writes "objects K in C_ref(a)" with a a physical-region address.
```

### 6.3 THE SEAL BLOCK CARRIES A FACTUAL ERROR

`O54SR :929-931` declares:

> "`BARE_SURFACE_I2_DETERMINATION_V001.md` and
> `STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md` were likewise
> probed under both conventions and are unsealed."

**Both are sealed and both verify.** Run from each artifact's own directory:

```text
BARE_SURFACE_I2_DETERMINATION_V001.md.seal.sha256                       -> OK
  9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c
STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md.seal.sha256 -> OK
  bf75f3929d739eccd2c0b495f3ec99e3db7564fbc2cdbf78eb503e63d4ee7082
```

The census is the build's **Q2 decider**. Declaring one's own decisive source unsealed when
it is sealed is an error of record in the direction of understating footing, but it is an
error, and it means the sidecar probe reported in §9 was not run for those two files.

### 6.4 QUOTATION INTEGRITY

- **Span cited, span not opened.** `O54SR :128-130` cites `V011 :1397-1412` and reproduces
  only its penultimate sentence. `:1397-1405` is the `C_ref` freeze. This is the single most
  adverse text for the build's own heaviest grade.
- **Contract truncated before its adverse sentences.** `JREF :246-257` quoted; `:261-264`
  cut (§1.4).
- **Subject widened.** `TASK4D :22-26` is about the 1,088 filtration objects; rendered as
  *"the record-side objects"* (§1.6).
- **Locator wrong, twice.** `CENSUS :355-357` / `:355-356` for a booking at `:359-360` (§2.3).
- **Unattributed reuse.** `O54SR :785` opens *"What would decide it, stated so a future lane
  is not tempted to invent one:"* — verbatim the predecessor's sentence at
  `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md :1832`, presented as this lane's own prose.
  Low severity; recorded for completeness.
- **Verified sound:** every other quoted span I re-read — V011 `:281-292`, `:294-299`,
  `:309-315`, `:328-335`, `:502-505`, `:262-274`, `:826-835`; O48SR `:620-623`, `:628-637`,
  `:732`, `:934`; TASK4D `:19-26`, `:50-69`; Q408 `:123`, `:226-245`; onset gate and write
  gate throughout; REFINEMENT QUOTIENT throughout; BARE_SURFACE `:36`, `:110`, `:112`;
  DoR-007 whole; COMMON_ORIGIN, O36SR, O47SR — **byte-identical at the cited lines, with
  adverse clauses carried.** The build's quotation discipline is good where it quotes; the
  failures are of *selection and completeness*, not of fabrication. Nothing was misquoted.

### 6.5 AUTHORING / ADVOCACY / FENCES

```text
authored          = false   no new object, law, map or theorem is constructed
advocated         = PARTIAL — see below
adopted           = false
alpha_computed    = false   verified: no magnitude approached anywhere in the target
proof_authorized  = false   verified
kappa_record_computed = false   verified
```

**Advocacy, PARTIAL.** The build's C-3 explicitly refused to grade Q1 flat PRIMARY on the
ground that suppressing decomposition sites *"would be advocacy"* — correct discipline,
correctly reasoned. But `O54SR :361` writes
`NET FOR THE RECORD'S OWN COMPLEX = PRIMARY, established negatively` on a census that omits
the two sites where the corpus attaches decomposition-of grammar to *the record complex*.
The guard was set in the right place and then failed on the other side: the residue-argument
is only as strong as the enumeration of refusals, and the enumeration is short by two.

### 6.6 REGISTER BAR

Exclusion globs held as an ARRAY and applied per pattern; per-pattern leak counter run;
**this artifact and the target both self-excluded.**

```text
EXCL = ( '*REGISTER*' '*TRACKER*' 'THE_PLAN*' 'ROAD_REMAINING*' 'THE_HANDOFF*'
         'OBSERVATIONS_REGISTER*' '*DECISION_SHEET*' '*FINISH_B*'
         'STAGE8_COMPLEX_PRIMARY_O54SR_V001.md'
         'STAGE8_COMPLEX_PRIMARY_O54SR_AUDIT_V001.md' )
```

`QUESTIONSSETTLED_REGISTER_V001.md` — barred, never opened, and filtered by the array on
every pattern that touched it. **Leak counter into any finding: 0.** Supervision was read
only through the two permitted ruling globs.

### D6 GRADE: **REFUTED.**

---

## 7. CHOICE LEDGER

| # | The choice | Taken | Alternative | What forced it |
|---|---|---|---|---|
| A-1 | Whether the `BareRec_2` declaration alone settles Q1 | No | Yes — grade PRIMARY with the build | `V011 :1397-1405` is in the same file and the same authority and is decomposition-shaped; `OBLD S9AD :412` names `C_ref` *"the record complex proper"*. One declaration does not outrank the file's own frozen class. |
| A-2 | Whether `C_ref` being disowned makes it a fourth *separate* site | No — it meets the record at the phrase | Grade it a fourth disowned site and keep PRIMARY | The corpus's own words are *"the record complex proper — C_ref"* (`OBLD S9AD :412`; `AUDIT :277`; `RA27_RESPONSE_LEGS :411`). A site the corpus calls *the record complex proper* is not a different site from the record's complex. |
| A-3 | Whether to grade Q1 DECOMPOSITION-OF outright | No — **INDETERMINATE-AT-BYTES** | Flip the build | The primary-shaped ground is real and verified (`V011 :281-292`, `O48SR :629`, graph→graph refinement, nil discretization sweep). Flipping would suppress it exactly as the build suppressed the other half. The commission says a clean indeterminate is a real result here. |
| A-4 | Whether `V011 :1271` (`K exhausts R^4`) counts as a continuum-into-complex refinement | No | Yes | It is a limit *of* the discrete family. Reported as unreported Q1 evidence, not as a direction reversal. |
| A-5 | Whether the missed O31SR commission flips Q3 | No | Yes | Its findings (four incompatible typings, nothing built acts on the factor) point the same way as CHOSEN. Graded a coverage failure, not a grade failure. |
| A-6 | Whether `G_closure`'s untyped codomain flips Q4 | No | Yes | Nothing modifies unitarity anywhere; twelve nil patterns hold. Graded as a completeness correction to `INDETERMINATE = 0`. |
| A-7 | Whether the third near-miss (`V011 :1404`) is a discriminator | No | Accept it and flip Q5 | Under PRIMARY the clause is ill-typed, not false, and `CENSUS :282` disowns the identification. Accepting it would manufacture the discriminator the commission barred. |
| A-8 | Whether to open `SURFACE_DEFINITION_OF_RECORD_V001/V002/V003.md` | No | Open them | They sit in `supervision/` and are not among the 28 permitted rulings. Verified present there (all three, all with sidecars) and verified **absent from both corpus roots**. The build's FL-8/C-11 declaration is accurate and I hold the same bar. |
| A-9 | Whether the build's `TASK4D :22-26` over-read changes that quotation's own accuracy | No | Call it a misquote | The span is quoted byte-perfect; only its *subject* was widened in the surrounding prose. Graded as scope, not as quotation. |
| A-10 | Whether to treat the two "unsealed" misstatements as fatal to §9 | No — recorded as COR-E | Void the seal block | The ten seals the build did claim all verify OK independently (§8). The error is additive, not corrupting. |

---

## 8. SEALS

`shasum -a 256 -c` run from each artifact's own directory. **Both sidecar conventions probed
for every file before any unsealed call.**

| Artifact | Convention | Result |
|---|---|---|
| `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` | `.md.seal.sha256` | **OK** |
| `SOURCE_FLUX_CONDITIONED_RECORD_WRITE_GATE_V003.md` | `.md.seal.sha256` | **OK** |
| `PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_BASE_GEOMETRY_O48SR_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_COMMON_ORIGIN_GENERATIVE_VS_FORMATION_TYPING_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md` | `.md.seal.sha256` | **OK** *(build said unsealed)* |
| `BARE_SURFACE_I2_DETERMINATION_V001.md` | `.md.seal.sha256` | **OK** *(build said unsealed)* |
| `STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_T7_CREF_VS_D3_FACTUAL_DETERMINATION_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_AND_CLASS_INDEPENDENCE_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_OBLD_TRANSPORT_STEP_S9AD_AUDIT_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_B1B_SUPPORT_QUESTION_DARIO_V001.md` | `.md.seal.sha256` | **OK** |
| `STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md` | `.md.seal.sha256` | **OK** |
| `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md` | bare `<stem>.seal.sha256` | **OK** |
| `DECISION_OF_RECORD_007_…_V001.md` | `.md.seal.sha256` | **OK** |
| `DOR_020_CONTINUUM_PACKAGE_…_2026-08-04.md` | `.md.seal.sha256` | **OK** |

## **SEALS: 24/24 OK.**

### 8.1 UNSEALED SOURCES — confirmed, and correctly declared by the build

```text
BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md          UNSEALED (both conventions, both roots)
BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md    UNSEALED (both conventions, both roots)
PRIMITIVE_BINARY_CLOSURE_GENERATOR_GATE_V001.md        UNSEALED (both conventions, both roots)
```

The build's FL-2 is correct and its two integrity anchors reproduce independently:

```text
workspace/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md               20a3a17d…  2348 lines
cleanroom/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md               20a3a17d…  2348 lines
workspace/mirrored_sources/corpus_members/…V011.md                    aa7c6d49…  2251 lines
workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/…V011.md         aa7c6d49…  2251 lines
cleanroom/review_packets/STAGE7_QSPEC_CANDIDATE_V001/…V011.md         aa7c6d49…  2251 lines
```

**Variant divergence re-derived independently: it begins at line 1953** and is confined to an
A32 freeze/registry passage. `diff` over lines 1-1952 is empty. Every line this audit quotes
from V011 — `:287`, `:502`, `:829`, `:1271`, `:1397-1405` — is byte-identical at the same
line number in both variants; the `C_ref` bookings sit at `:2285-2286` in `20a3a17d…` and
`:2188-2189` in `aa7c6d49…` with identical text. **All D1 grounds are variant-independent.**

---

## 9. IMPORT AUDIT

| # | Import | Where it touched this audit | Survives without it? |
|---|---|---|---|
| J-1 | **CW-complex theory** (skeleta, cellulations, subdivision, realization) | **NOT USED AS A PREMISE.** I read *cellulation*, *2-skeleton*, *subdivision*, *refinement* as **words the corpus writes about its own objects**, and took no theorem from any of them. In particular I did **not** infer from "CW complex" that `K` must be a filtration of a space — that inference is the commission's named hazard and would have forced DECOMPOSITION-OF on an import. | **YES, and the finding requires it.** Every D1 ground is a quoted corpus sentence. The indeterminate rests on the corpus writing *both* grammars about the same referent, not on what those words mean in mathematics. |
| J-2 | **Tensor factorization** of a composite Hilbert space | §3, reading `H_source tensor H_record tensor H_boundary/environment` | **YES.** The CHOSEN grade rests on the verbs *"must act on"* and *"Let"*, and on `K`'s inventory. |
| J-3 | **Unitarity / dilation / partial trace** | §4 | **YES.** The corpus supplies its own words: *"closed-dilation unitaries"*, *"The fundamental evolution remains unitary"*, *"after tracing or conditioning downstream degrees"*, *"the finite-dimensional partial-trace identity"*. |
| J-4 | **Well-typedness vs falsity** (the distinction used to refuse near-miss 3) | §5.2 | **PARTIALLY.** Without it, `V011 :1404` would grade INDETERMINATE rather than NOT-A-DISCRIMINATOR — **and Q5's verdict would not change**, since an indeterminate near-miss is still not a discriminator. |
| J-5 | **`shasum`/content-addressing** | §8 | **YES.** Mechanical verification only. |

**Net: no import is load-bearing for any grade.** J-1 is the one that would have changed a
verdict in the direction the commission warned about, and it was refused — the same refusal
the build made, and I confirm it.

---

## 10. SWEEP CUTOFFS

Roots: `alpha-program-archive/workspace` (1,967 `.md`), `…/alpha_fundamental_record_action_cleanroom_v003`
(2,312 `.md`), plus the 28 rulings via the two permitted globs only. `.proof_deps` excluded.

| Pattern | Kept | Self-excluded | Barred filtered | Disposition |
|---|---|---|---|---|
| `triangulation of` | **0** | 2 | 0 | Nil confirms the build |
| `discretization of` | **0** | 2 | 0 | Nil confirms the build |
| `discretiz` *(widened by me)* | 24 (12 unique) | 4 | 0 | **ALL OPENED**; none is a continuum→`K` discretization |
| `cellulations? of` | 30 | 5 | 1 | **ALL 30 OPENED** — this is where the build's misses live |
| `record complex proper` | **3** | 0 | 0 | **ALL 3 OPENED**; all sealed OK; decisive for D1 |
| `unit.4.cell\|unit 4-cell` | 4 | 0 | 0 | **ALL 4 OPENED** |
| `C_ref` | 707 | 0 | 20 | Narrowed to defining/typing sites; V011, CENSUS, TASK6, RA27-2, T7-CREF, OBLD, AND_CLASS opened |
| `regular CW complex` | 47 | 6 | 0 | 47 + 6 self = 53, reconciling the build's count exactly |
| `designated record subsystem` | **15** | 7 | 0 | **ALL OPENED** — refutes "exactly one file" |
| `required environment` | **14** | 7 | 0 | **ALL OPENED** — refutes "exactly one file" |
| `G_closure` | 30 | 0 | 0 | Opened the typing sites |
| `Lindblad` | 6 | 9 | 0 | **ALL 6 OPENED**; all exclusionary |
| `GRW`, `continuous spontaneous localization`, `stochastic Schrod`, `nonlinear Schrod`, `modified Schrod`, `collapse term`, `spontaneous collapse`, `objective collapse`, `Diosi\|Penrose collapse\|CSL model`, `wave-function collapse`, `stochastic (unravel\|noise\|term)`, `non-linear (evolution\|dynamics\|term)` | **0 each** | — | 0 | Twelve nil returns; four patterns are mine, not the build's |
| `continuum limit` | 114 | 1 | 1 | Sampled by genitive; not exhausted — declared |
| `refinement limit` | 43 | 3 | 8 | Sampled; DoR-007 and RA27-2 opened |
| `exhausts` | 135 | 0 | 2 | Narrowed to `K exhausts`; `V011 :1271` opened |

**Leak counter into any finding: 0.** Every barred-filename hit was removed by the array
before inspection; the self-exclusion columns are the target artifact and this one.

### OPEN WHAT YOU COUNT — declared

**Opened whole:** `DECISION_OF_RECORD_007_…` (26), `DOR_020_CONTINUUM_PACKAGE_…` (45),
`BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md` (95).

**Opened at the spans quoted and re-verified line-by-line:** `BID V011` (`:250-345`,
`:495-515`, `:815-845`, `:1250-1275`, `:1385-1425`, `:2280-2292`, and the same spans in the
`aa7c6d49…` variant), `STAGE8_BASE_GEOMETRY_O48SR_V001.md` (`:616-645`, `:726-736`,
`:930-938`), `STAGE8_TASK4D_…` (`:1-75`), `STAGE8_TASK5_EQ6_Q408_…` (`:1-99`, `:100-280`),
`STAGE8_TASK5_JREF_…` (`:115-160`, `:230-280`), `STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md`
(`:560-610`, `:660-700`), `STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md` (`:1-180`),
`STAGE8_AND_CLASS_INDEPENDENCE_V001.md` (`:320-360`), `STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md`
(`:404-418`), `…_AUDIT_V001.md` (`:270-284`), `STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md`
(`:1-60`, `:240-370`), `CENSUS` (`:14-30`, `:275-290`, `:330-372`), `BARE_SURFACE_I2` (`:30-40`,
`:106-116`), `ONSET GATE V003` (`:18-32`, `:38-70`, `:88-130`, `:148-198`),
`SOURCE_FLUX…WRITE_GATE_V003` (`:140-172`), `PRIMITIVE_TRANSPORT_ONLY_…` (`:35-80`),
`PRIMITIVE_BINARY_CLOSURE_…` (`:70-95`), `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_…_V002`
(`:24-42`), `COMPLETE_QSPEC_RELATIVE_HISTORY_TRANSFER_MAP_SPEC_V001.md` (`:95-115`),
`STAGE8_COMMON_ORIGIN_…` (`:8-20`, `:68-82`), `O36SR` (`:1044-1062`), `O47SR` (`:270-280`,
`:1780-1790`, `:1803-1832`), `STAGE8_B1B_SUPPORT_QUESTION_DARIO_V001.md` (`:205-215`),
`STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md` (`:124-135`),
`STAGE8_STRATIFICATION_O27SR_AUDIT_V001.md` (`:528-536`),
`STAGE8_GAMMA_K_QUASIFREE_ROOT_ROUTE_ATTEMPT_V001.md` (`:145-152`),
`STAGE8_JOIN_FRONTIER_O51SR_AUDIT_V001.md` (`:719`).

**Counted but NOT opened, declared:** the 707 `C_ref` lines, 114 `continuum limit`, 135
`exhausts` and 43 `refinement limit` hits were narrowed by genitive/signature rather than read
exhaustively. The build's 86 `non-unitary` and 102 `CPTP` counts were **not re-derived** — Q4's
grade does not rest on them and neither does mine.

**The 28 rulings.** Both permitted globs enumerated and counted independently:
`DECISION_OF_RECORD_NNN_…` = **14** (003, 004, 005, 006, 007, 008, 009, 010, 011, 013,
014×3, 015); `DOR_NNN_…` = **14** (016, 017, 018, 019, 020_A1…A9, 020_CONTINUUM). Total **28**.
**`DECISION_OF_RECORD_012` verified absent by enumeration, not assumed.** Note the string
`D012` occurring in `STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md :64` is an unrelated local
alias for `STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md` and is **not** a reference to a missing
ruling. Titles of all 28 read; **DoR-007 and DOR-020 opened whole**, as the two bearing on
continuum and refinement. DOR-020 ratifies *"THE CONTINUUM PACKAGE"* conditionally, naming
`B_Q408_REFINEMENT` and *"the PL refinement core"* among its six joint generators — the build
declares having opened it and does not mention that its SITE 3 is one of that package's
ratified generators.

---

## 11. FLAG BLOCK

| # | Flag | Content |
|---|---|---|
| **G-1** | **THE CORPUS'S OWN NAME FOR THE AUDITED OBJECT IS GLOSSED AS A CELLULATION CLASS.** Three sealed artifacts write *"the record complex proper"* and gloss it `C_ref` (`OBLD S9AD :412`; `OBLD S9AD AUDIT :277`; `RA27_RESPONSE_LEGS :411`). `C_ref` is frozen in `V011 :1397-1405` as *"oriented, shape-regular periodic regular-CW **cellulations**"* whose common refinements *"preserv[e] the same smooth coframe and connection."* **This is the load-bearing structural fact of this audit** and it is absent from the build. |
| **G-2** | **THE BUILD CITED THE DECISIVE SPAN AND QUOTED PAST ITS HEAD.** `O54SR :128-130` cites `V011 :1397-1412` and reproduces only the penultimate sentence. The `C_ref` freeze occupies `:1397-1405` of the span it cited. |
| **G-3** | **BOTH GRAMMARS, ONE REFERENT, NO SEALED IDENTIFICATION EITHER WAY.** `V011 :281-292` is primary-shaped with no genitive; `V011 :1397-1405` is decomposition-shaped and frozen; `V011 :2285-2286` books the class frozen and its independence unproved. No sealed sentence identifies `BareRec_2`'s `K` with `C_ref`, and none separates them. **That is what an indeterminate looks like when both halves are real.** |
| **G-4** | **THE `Ref` CONTRACT TIES REGION-REPRESENTATION TO A RECORD COMPLEX IN ONE BLOCK.** `JREF :251-264`: objects carrying *"finite oriented record complex"* are subject to *"Every pair representing the same region must admit a common refinement. No choice of cellulation … may enter the output."* The build's *"not tied to `K` anywhere"* fails one blank line below the text it quoted. |
| **G-5** | **THREE FALSE CORPUS-WIDE ABSENCE CLAIMS.** "three complex-valued sites"; "exactly one file" for the split strings (8 files at bytes); "'represented physical region' is not tied to `K` anywhere". A default-refute standard treats an asserted absence as a claim; all three fail. |
| **G-6** | **TWO SEALED SOURCES DECLARED UNSEALED — ONE OF THEM THE Q2 DECIDER.** `CENSUS` and `BARE_SURFACE_I2` both carry `.md.seal.sha256` and both verify. The sidecar probe reported in `O54SR §9` was not run for them. |
| **G-7** | **WHAT SURVIVES INTACT.** Q3's CHOSEN, Q4's UNMODIFIED and Q5's null are all reachable independently and I reach them. Q2's decider verifies verbatim at `CENSUS :340-342`. The nil discretization sweep survives a wider net than the build ran. The named import hazard was correctly refused. **The build's failure is enumeration, not fabrication: nothing it quoted was misquoted.** |
| **G-8** | **FOR THE NEXT LANE — WHAT WOULD DECIDE Q1.** A sealed sentence that either (a) identifies `BareRec_2`'s `K` with a member of `C_ref` and exhibits the incidence realization `OBLD S9AD :412` books as absent, or (b) states that `C_ref` is not the record's object, in the corpus's voice rather than by the refusal-residue route. Neither exists in either root. Until one does, the honest grade is INDETERMINATE-AT-BYTES, and a future lane should not read this audit as licensing DECOMPOSITION-OF either. |
| **G-9** | **SCOPE DECLARATION, INHERITED AND HELD.** `SURFACE_DEFINITION_OF_RECORD_V001/V002/V003.md` sit in `supervision/`, are not among the 28 permitted rulings, and are verified **absent from both corpus roots**. **Not opened as authority**, in either lane. |

---

## 12. CORRECTIONS, SEVERITY ORDER

| ID | Correction | Deciding file:line |
|---|---|---|
| **COR-A** | Q1's site census omits `C_ref`, which three sealed artifacts gloss as **the record complex proper**. The grade `record's own complex = PRIMARY` is not supportable on the enumeration given. | `STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md:412` |
| **COR-B** | `V011 :1397-1412` was cited but only its penultimate sentence quoted; `:1397-1405` freezes a class of *cellulations* over a smooth coframe, booked `cellulation_refinement_class_frozen = true`. | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1397` |
| **COR-C** | *"'represented physical region' is not tied to `K` anywhere"* is false: the `Ref` contract block was truncated before the two sentences that tie them. | `STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md:263` |
| **COR-D** | *"the strings … occur in the corpus at exactly one file"* (X-1, FL-4) is false; 8 workspace files plus the cleanroom mirror. FL-4 must be withdrawn. | `STAGE8_ENVIRONMENT_FACTOR_O31SR_V001.md:254` |
| **COR-E** | The symbolic three-factor split **is** in a sealed corpus principle; the §3.2 "quotation-integrity correction" rests on the false premise that it is not. | `PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md:34` |
| **COR-F** | `CENSUS` and `BARE_SURFACE_I2` declared unsealed; both are sealed and verify. The census is the build's own Q2 decider. | `STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md.seal.sha256` |
| **COR-G** | A second unreported decomposition site at the record: *"the unit-4-cell record complex and its licensed refinements"*, *"every cellulation of a contractible record region"*. | `STAGE8_AND_CLASS_INDEPENDENCE_V001.md:330` |
| **COR-H** | `TASK4D :22-26`'s subject is the 1,088 filtration objects; rendered as *"the record-side objects"*. Scope widened, quotation itself accurate. | `STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md:22` |
| **COR-I** | Q5 reports two near-misses; a third and stronger one exists at `V011 :1404`, flagged of record by RA27-2. Still refused — `NEAR_MISSES_REPORTED = 2` is wrong, the verdict is not. | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1404` |
| **COR-J** | Q4's `INDETERMINATE = 0` is overstated: `G_closure` has *"domain displayed, codomain untyped"* and is not in D-1…D-12. UNMODIFIED unaffected. | `STAGE8_JOIN_FRONTIER_O51SR_AUDIT_V001.md:719` |
| **COR-K** | FL-1's absolute — *"The corpus never asserts that nothing stands behind `K`"* — is refuted in its absolute form by *"continuous fibers over a discrete base"*. | `BARE_SURFACE_I2_DETERMINATION_V001.md:36` |
| **COR-L** | Locator error, twice: the `K_L` booking is at `:359-360`, not `:355-357` / `:355-356`. | `STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md:359` |
| **COR-M** | Q2's itemization E-1…E-7 omits an entered, frozen class of cellulations (E-8), weakening the inference at `O54SR :436-437`. | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2285` |
| **COR-N** | SITE 3 typed *"BASE-2"* without corpus warrant; `O48SR :628` types BASE-2 as the `Ref_a` finite cell carrier, not `X_o`. | `STAGE8_BASE_GEOMETRY_O48SR_V001.md:628` |
| **COR-O** | Undeclared load-bearing import: *"which is the operation a continuum-discretization reading would require"* appears in no IMPORT AUDIT row. | `STAGE8_COMPLEX_PRIMARY_O54SR_V001.md:259` |
| **COR-P** | Unattributed reuse of the predecessor's sentence *"What would decide it, stated so a future lane is not tempted to invent one:"*. Low severity. | `STAGE8_SPONTANEOUS_FORMATION_O47SR_V001.md:1832` |

---

## 13. GATES

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
authored = false
advocated = false
adopted = false
discriminator_manufactured = false
imports_load_bearing = false
register_bar_leaks = 0
self_excluded = true
```
