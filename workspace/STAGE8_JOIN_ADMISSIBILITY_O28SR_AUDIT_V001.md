# STAGE8 — JOIN ADMISSIBILITY AUDIT (O28SR)

COMMISSION O28SR AUDIT · 2026-08-15 · DEFAULT-REFUTE · DETERMINATION ONLY
TESTIMONY ZERO WEIGHT — EVERY FINDING RE-DERIVED AT BYTES

---

## 0. OVERALL VERDICT, FIRST

```text
*** CONFIRMED-WITH-CORRECTIONS ***

THE HEADLINE DETERMINATION SURVIVES AT BYTES, AND I RE-DERIVED IT RATHER THAN
ACCEPTING IT.  GATE 1 DOES FAIL, ON CLAUSE (b), FOR THE REASON GIVEN.  THE BUILD
DID NOT COMMIT THE ERROR THE COMMISSION NAMED HARDEST (A): IT REFUSED THE
EQUATION OF THE TWO INVARIANTS RATHER THAN ASSUMING IT, AND ITS REFUSAL IS
CORRECT AT THE BYTES OF BOTH SOURCES.  THE CONSEQUENCE IS NOT OVERSTATED; NEITHER
IS ITS NEGATION ASSERTED.  NOTHING WAS PROPOSED, ADOPTED, CONSTRUCTED, OR
REPAIRED.

EIGHT CORRECTIONS FOLLOW.  ONE IS SUBSTANTIVE (X-1: THE ESCAPE SURVEY RESTS ON A
PREMISE THAT THE BUILD'S OWN COMMISSIONED GROUND MARKS *** REFUTED ***, AND MISSES
THE THREE RULES THE COMMISSION NAMED).  ONE IS A LOGICAL OVER-CLAIM (X-2).  THE
REST ARE CITATION AND QUOTATION-FIDELITY DEFECTS.  NONE OF THEM REVERSES THE
DETERMINATION, AND I SAY SO PLAINLY RATHER THAN INFLATING THEM.
```

---

## 1. STEP 0 — PROBE AND SEAL VERIFICATION

```text
OUTPUT PATH PROBED ABSENT FIRST
  STAGE8_JOIN_ADMISSIBILITY_O28SR_AUDIT_V001.md   ABSENT at probe.  Proceeded.

TARGET SEAL, verified FROM THE ARTIFACT'S OWN DIRECTORY
  cd /Users/bgm/MB Work/alpha-program-archive/workspace
  shasum -a 256 -c STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md.seal.sha256
  -> STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md: OK
  e656377219e3c3d24621303bbfed0139573f9c6b145d5c5614c932e5900a0dcc
```

**EVERY SEAL THE TARGET DECLARES, RE-VERIFIED BY ME, SAME DIRECTORY:**

```text
STAGE8_INDEX_VARYING_SETS_O20SR_V001.md                  OK
STAGE8_INDEX_VARYING_SETS_O20SR_AUDIT_V001.md            OK
STAGE8_GLUING_CANDIDATE_O23SR_V001.md                    OK
STAGE8_GLUING_CANDIDATE_O23SR_AUDIT_V001.md              OK
STAGE8_INGREDIENT_CENSUS_O17SR_V001.md                   OK
STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md             OK
STAGE8_TYPING_RULE_CANDIDATE_V001.md                     OK
STAGE8_GLUED_TOPOLOGY_HUNT_V001.md                       OK

AND ONE THE TARGET CONSUMES AT LINE PRECISION BUT DOES NOT DECLARE (see X-4):
CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md               OK   (verified by me)
```

---

## 2. SWEEP DECLARATION AND CUTOFF (THIS AUDIT)

```text
SWEEP CUTOFF          2026-08-15, all reads completed before this artifact sealed.
CORPUS ROOTS          /Users/bgm/MB Work/alpha-program-archive/workspace (primary)
                      /Users/bgm/Documents/New project/gravity_emergence_evidence_
                        program/alpha_fundamental_record_action_cleanroom_v003
READS                 SCOPED.  Cited spans opened at bytes in both primary sources
                      and in the commissioned determinations and their audits.
BARRED AND NOT READ   register / tracker / road / plan / continuation files.
                      None opened at any point.
"Q-..." ITEMS         EXPECTED-UNLOCATABLE.  None sought, none consumed.
GIT                   No git action of any kind.
```

**DECLARED SCOPED SWEEPS.** Five, all lawful, all within the named roots:

```text
S1  grep the target for program-quantity tokens, lens tokens, and constructive
    verbs (hunts F and G).
S2  grep the census and the census AUDIT for refinement / stitching / subdivision
    rules — the commission's hunt (D) leg.  THIS LEG DECIDED CORRECTION X-1.
S3  grep O23SR for every phrase the target attributes to it, to locate each
    quotation's own span before reading it in full (hunt E).
S4  grep the hunt for its table header, its fence block, and its outcomes ledger.
S5  find, across both corpus roots, every copy of
    CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md, to test one line citation.
```

---

## 3. HUNT (A) — THE TWO INVARIANTS. RE-DERIVED, NOT ACCEPTED.

This was the commission's hardest hunt and the chain's first link. I demanded each
invariant's own definition at its own bytes, with its own object and convention,
and I refused to take either from the target's summary.

### 3.1 The family's index, at its own bytes

`STAGE8_TYPING_RULE_CANDIDATE_V001.md` `:152-161`. I read the span in full and
compared it character-by-character with the target's quotation.

```text
DEFINITION AT BYTES  ":155  exactly the cycle space  Z_1(C_j; Q) =
                     ker(bdry)|_{C_j},  of dimension  b_1(C_j) for connected C_j."
OBJECT               C_j — ":134-135  each a connected admitted subcomplex of K."
VERTEX CONVENTION    FIXED BY LEMMA 1 IN ITS OWN WORDS, ":153  the boundary of an
                     edge of C_j lands on vertices of C_j."  The vertices are
                     CONSTITUENTS of C_j.  NOT ASSUMED — READ.
COEFFICIENTS         Q, named in the symbol itself.
SIDE                 HOMOLOGY — a kernel of a boundary map, not a cohomology.
```

**QUOTATION CHECK.** The target's `:152-161` block is EXACT and COMPLETE TO SPAN
END. No elision, no substitution. This is the load-bearing quotation of the whole
build and it is clean.

### 3.2 The joiner's invariant, at its own bytes

`STAGE8_GLUED_TOPOLOGY_HUNT_V001.md` `:262-267`, row G-9. Read in full.

```text
DEFINITION AT BYTES  ":266  As a graph: 2 vertices, one reciprocal edge pair =
                     a tree; H^1 = 1 - 2 + 1 = 0."
OBJECT               THE TWO-NODE NETWORK GRAPH.  ":264-265  a TWO-NODE network,
                     Adj_2 = [[0,1],[1,0]]."  The row types its own object with
                     the words "As a graph".
VERTEX CONVENTION    THE VERTICES ARE THE GLUED CELLS.  Three sites, all read:
                     (i)  hunt :267, the row's OWN final clause — "Even if
                          adopted, it glues two cells into a contractible object."
                          Two nodes; two cells; one object.
                     (ii) O23SR :209/:213 — "INPUT SIDE  TWO NODES on matched
                          carriers ... The input items are CELLS under the
                          source's own identification."
                     (iii) O23SR :218 + :221 — "TWO CELLS JOINED BY ONE
                          RECIPROCAL EDGE PAIR" / "As a glued structure:
                          2 vertices, 1 reciprocal edge pair."
SIDE                 COHOMOLOGY.  The hunt's method line :287 — "H^1 =
                     dim ker(d_1) - dim im(d_0)".
```

**QUOTATION CHECK.** The target's `:262-267` block is EXACT and COMPLETE TO
SENTENCE END, including the final clause. Clean.

### 3.3 VERDICT ON HUNT (A)

```text
*** THE BUILD DID NOT EQUATE THE TWO INVARIANTS ON A SHARED NAME. ***

It demanded each definition at its own bytes and found them NOT commensurable.
That refusal is CORRECT, and I re-derived it independently from both sources
rather than adopting it.  The corpus really does compute a cohomology of a graph
whose points are cells, and really does index the admissible-set family by a
cycle-space dimension whose points are the 0-cells inside one cell.  Sameness was
NOT assumed by the build; non-sameness was SHOWN, and it is shown again here.

GATE 1 FAILS ON CLAUSE (b).  *** CONFIRMED. ***

CLAUSE (a) — same homological quantity — is also correct as the target has it,
and I checked the point it did not display: the hunt's own table at :294 carries
an F column, and BOTH rows read F = 0 (K_square :295, NET :316).  With no 2-cells
on either object, dim H^1 = dim Z_1 = E - V + components exactly, so the
homology/cohomology difference genuinely does not bite.  The target's import is
sound and is better warranted at bytes than the target itself showed.

CLAUSE (c) — same conventions — correct, and the recorded silence is real: the
G-9 row names no coefficient ring; the hunt's fence :463-465 supplies exact
rational instruments for the artifact.
```

---

## 4. EVERY HOMOLOGICAL COMPUTATION RE-RUN INDEPENDENTLY

Fresh venv, sympy, exact rationals, no floats. I built the boundary matrices from
each object's own sealed combinatorial display and took exact rational ranks. I did
NOT check the target's stated formula against itself; I recomputed from V/E data.

```text
NET two-node network   (nodes = the two glued CELLS; hunt :264-266)
   V = 2   E = 1   F = 0
   rank(d_1) exact              = 1
   dim Z_1 = E - rank(d_1)      = 0
   components = V - rank(d_1)   = 1
   E - V + components           = 0
   H^1 = dim ker(delta_1) - dim im(delta_0) = 0
   AGREE (Z_1 == E-V+c == H^1)  = True

K_square   (vertices = 0-CELLS INSIDE one cell; hunt :226 vertex/edge list,
            ":14  4 vertices / 4 edges / NO 2-cell")
   V = 4   E = 4   F = 0
   rank(d_1) exact              = 3
   dim Z_1 = E - rank(d_1)      = 1
   components = V - rank(d_1)   = 1
   E - V + components           = 1
   H^1 = dim ker(delta_1) - dim im(delta_0) = 1
   AGREE (Z_1 == E-V+c == H^1)  = True

CROSS-CHECK AGAINST THE HUNT'S OWN TABLE (:294-295, :316), not against the target:
   K_square  rank d_0 = 3   (hunt's column)   my transpose rank = 3   MATCH
   K_square  H^1 = 1        (hunt's column)   mine = 1               MATCH
   NET       H^1 = 0        (hunt's column)   mine = 0               MATCH
```

```text
*** BOTH DISPLAYS IN THE TARGET'S §3.4 ARE CONFIRMED, INDEPENDENTLY DERIVED. ***
The target's numbers are right, its agreement flags are right, and its claim that
neither source misapplies the invariant in its own frame is right.  The decisive
contrast — K_square carrying cycle rank 1 as a cell and contributing cycle rank 0
as a node of a network — is arithmetic I reproduced myself.

I computed NOTHING the corpus does not display.  No glued complex with cell
interiors was constructed, exactly as the target's C-4 declined to construct one.
```

---

## 5. HUNT (B) — THE DOMAIN GATE. NOT WAVED THROUGH, BUT OVER-CLOSED.

I read R-CLW's own domain statement rather than the target's rendering of it.

```text
READ AT BYTES   :313-332 (§6 D1-D4) — the target quotes all four IN FULL and
                VERBATIM.  I compared every line.  EXACT.  No elision.
                :376+ (§8 N1-N6) — N2, N3, N4 quoted by the target; N2 and N3 are
                EXACT and COMPLETE at §4.2 of the target.

THE DOMAIN, RE-DERIVED  D1: carriers are connected multi-cell record complexes K
                "with a declared admitted-cell family {C_j}, each C_j a connected
                admitted subcomplex."  D3: "R-CLW is conditional on an admission it
                does not perform."  N2: "admission is unsealed of record and stays
                so."  N3: "under this rule the ports are inert ... and chain
                nothing."

GATE 2 FAILS.  *** CONFIRMED — but on Restriction 2 and N3, not on Restriction 1
alone.  See correction X-2. ***
```

The target did NOT wave the domain gate through. It went to the family's own
definition, quoted it in full, and showed the joined object is not shown to be in
it. That is the right move and it is correctly executed. The defect is narrower:
one stated sufficiency does not hold.

---

## 6. CORRECTIONS

### X-1 *** THE ESCAPE SURVEY RESTS ON A PREMISE ITS OWN GROUND MARKS REFUTED, AND MISSES THE THREE RULES THE COMMISSION NAMED. *** (hunt D — SUBSTANTIVE)

The commission directed: *a refinement rule applied after joining is the obvious
candidate and the census names three such rules, so verify what the build did with
them.* I ran that leg independently. **The build did nothing with them. They are
absent from the artifact entirely.**

```text
WHAT THE TARGET SAYS, §7 E-2:
  "SEALED OBJECT   CIS (census §2.1), the corpus's only rule with a cell on its
   output side."

WHAT THE TARGET'S OWN COMMISSIONED GROUND SAYS.
STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md — listed by the target at its §2 as
COMMISSIONED GROUND, seal verified OK by the target and again by me:

  :83   "## 2. FINDING A-1 — FRAME LEAKAGE, KILL GRADE.
         THE REFINEMENT GENERATORS YIELD CELLS."
  :98-99 "Read at their own bytes, three of the five are cell-yielding rules."
  :153-156  "CONSEQUENCE FOR THE COMMISSION'S MOST IMPORTANT ANSWER. §2.1's
         headline — '**EXACTLY ONE CLAUSE IN THE CORPUS**' — and FLAG
         CELL_ON_OUTPUT_SIDE = 1 are false at bytes."
  :570  verdict table:
         "§2.1  what has a CELL on its output side       *** REFUTED ***"

THE THREE RULES, AT THE AUDIT'S OWN BYTES (:125-139), each typed by structure:
  A1     TAKES the parent complex K.  YIELDS a refined complex — "cubical
         bisection, 16 subcubes".   "TYPE  RULE.  CELLS STAND ON ITS OUTPUT SIDE."
  A2-F   TAKES the parent complex K.  YIELDS a refined complex whose cells are
         "Freudenthal chains of Boolean vertices".  Same TYPE line.
  A2-B   TAKES the parent complex K.  YIELDS a refined complex whose cells are
         "barycentric chains of nonempty 4-cube faces".  Same TYPE line.
```

**WHAT I FOUND IN THE TARGET.** A scoped grep of the target for `bisection`,
`barycentric`, `Freudenthal`, `subdivis`, `A2-F`, `A2-B`, `D0_SQUARE`,
`CELL_ON_OUTPUT`, `generator` returns **nothing**. The target's E-1 addresses only
`T_ref`, and E-1's own scope note correctly excludes `T_ref` from the class anyway
("joins the finite to the completed carrier, NOT CELL TO CELL"). So the escape
class the commission named — **a refinement rule applied after joining** — has
exactly one row in the survey, and that row disclaims being in the class.

```text
THE DEFECT, STATED EXACTLY AND NOT INFLATED:
(1) E-2's uniqueness premise ("the corpus's only rule with a cell on its output
    side") is FALSE AT BYTES against ground the build itself verified and listed.
(2) The refinement-generator class is UNSURVEYED.  No row was run for it.
(3) The survey's claim to have covered the class is therefore not established.

WHAT I DO NOT CLAIM, AND WHY:
I do NOT assert that an escape exists.  The census audit types these generators as
complex -> complex (":158-159  The refinement generators take a complex and yield
a complex; they do not construct a cell from anything more primitive than a
cell"), and the survey's bottom line ADMITS = NONE is not shown by me to be
disturbed.  But it was not shown by the build either.  A survey that reaches the
right bottom line through an unsurveyed class and a refuted premise is CORRECTED,
not vindicated.  Running the missing row is a construction act this commission
bars me from, and I do not perform it.
```

### X-2 RESTRICTION 1 DOES NOT "ALONE SUFFICE". (hunt B/C — LOGICAL OVER-CLAIM)

```text
TARGET, F-4:  "Restriction 1 (level) IS specific to the joined object, and it
              alone suffices."

AT BYTES, R-CLW D1 (:314-316) — which the target quotes IN FULL and then does not
weigh — carries its own degenerate clause:
    "On the one-cell degenerate case (K itself a single admitted cell) R-CLW
     reduces to the sealed containment law — no new content."

So the K level and the C_j level are NOT disjoint types in the rule's own words:
a K can BE an admitted cell.  And nothing in R-CLW bars a multi-cell subcomplex
from admission — admission is an unsupplied act outside the rule (D3, N2), not a
predicate the rule tests.  "A carrier is what a cell sits INSIDE, not an instance
of one" is therefore too absolute for the rule that was quoted.

CONSEQUENCE  GATE 2 STILL FAILS — on Restriction 2 (admission unsealed "and stays
so", N2) together with N3's express disclaimer of any cell-to-cell chaining.
Those two are affirmative and decisive.  The level argument is corroborating, not
sufficient.  The target's own F-4 has the sufficiency backwards: it says
Restriction 2 "defeats domain membership for EVERY object" and Restriction 1
"alone suffices"; at bytes it is Restriction 2 that closes the domain and
Restriction 1 that does not close it by itself.
```

### X-3 FOUR UNMARKED ELISIONS FROM INSIDE QUOTED SPANS. (hunt E)

The commission warned that six builds today elided material from inside quoted
spans, and that both of this build's primary sources were themselves the subjects
of such elisions. I checked **every** quotation in the target to its span end.
Most are clean; four are not. **All four are non-adverse — none conceals material
that would weaken the determination, and two drop material that strengthens it.**

```text
E-a  §3.2, hunt :463-465.
     TARGET      "exact mod-p ranks with rational sandwiches, Fraction-exact
                 RREF.  No floats."
     AT BYTES    "exact mod-p ranks with rational sandwiches, Fraction-exact
                 RREF, integer cocycle/pairing certificates.  No floats."
     ELIDED      ", integer cocycle/pairing certificates" — UNMARKED, and a
                 comma was replaced by a period to close the sentence early.

E-b  §3.2, hunt :468-470.
     TARGET      "every topology claim above rests on a sealed COMBINATORIAL
                 presentation (... the Adj_2 display)"   [quotation ends]
     AT BYTES    the sentence continues: "..., never on a continuum picture."
     ELIDED      the sentence tail, UNMARKED and with no terminal ellipsis.
     MITIGATION  The target renders this fence correctly at its §10, citing the
                 wider span :467-471.  The defect is local to §3.2.

E-c  §7 E-3, O23SR :967-968.
     TARGET      "a cycle needs both the rule's excluded self-edge and a scope
                 past two nodes."
     AT BYTES    "... and a scope past two nodes, and O11, O12, and the
                 connected-composition obligation are all undischarged."
     ELIDED      the tail, UNMARKED, period substituted.  The dropped clause
                 names MORE obstacles — it strengthens the build's own no-escape
                 finding, which is why the elision costs the build nothing and is
                 recorded as a fidelity defect rather than a substantive one.

E-d  §7 E-4, R-CLW N3.
     ELIDED      the cross-reference tags "(§9)" and "(S5 §2.3a)", UNMARKED.
     MITIGATION  The target gives N3 IN FULL and VERBATIM at its §4.2.
```

```text
CLEAN, CHECKED TO SPAN END, NO DEFECT:
  typing rule :152-161 (Lemmas 1-2, the build's load-bearing quotation) — EXACT
  typing rule :313-332 (D1-D4, quoted in full)                          — EXACT
  typing rule §8 N2, N3 at target §4.2, N4 at target §7 E-5             — EXACT
  hunt :262-267 (row G-9, to its final clause)                          — EXACT
  hunt :316, :441-442, :446-448, :449-453, :372-377                     — EXACT
  O23SR :111-114, :162-164, :218-221, :281-283, :333-334, :969          — EXACT
  census :616-622 (T_ref), :637-638, CIS TYPED-BY-STRUCTURE block       — EXACT
  MARKED ellipses at §3.1, §3.3, §6.2, §7 E-1 — all legitimate, content preserved.
```

### X-4 A LINE CITATION THAT IS WRONG IN EVERY COPY, AND AN UNDECLARED CONSUMED SEAL. (hunt E/G)

```text
TARGET, §7 E-2:  "complete_causal_parent_derived = false
                  (CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md :110)"

AT BYTES  The flag is at :107.  :110 is a DIFFERENT flag —
          "coupling_evaluation_authorized = false".
          I checked all four copies across both corpus roots (sweep S5):
            workspace/CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md          :107
            workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/...      :107
            cleanroom_v003/CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md     :107
            cleanroom_v003/review_packets/STAGE7_QSPEC_CANDIDATE_V001/... :107
          The locator is wrong everywhere.  The QUOTED FLAG TEXT IS GENUINE.

SECOND HALF  The target consumes this file at line precision but does not list it
             in its §2 SEALS VERIFIED block.  The census that carries the same
             flag supplies no line number, so the locator did not come from the
             relay.  I verified the seal myself: OK.  Nothing turns on the file
             beyond E-2's (C) verdict, which survives on the flag's real text.
```

### X-5 "THE SAME LINE" IS NOT THE SAME LINE. (hunt A)

```text
TARGET, §3.3 SITE 3:  "The SAME LINE writes 'two cells' and '2 vertices' of one
                      object."
AT BYTES  O23SR :218  "OUTPUT SIDE TWO CELLS JOINED BY ONE RECIPROCAL EDGE PAIR."
          O23SR :221  "As a glued structure: 2 vertices, 1 reciprocal edge pair."
          Same §3.1 OUTPUT SIDE entry; THREE LINES APART.
THE IDENTIFICATION STANDS — the entry does write both of one object, and SITE 2
(:209/:213) states node = cell outright.  The byte-proximity claim does not.
```

### X-6 "ONE CELL" FOR K_SQUARE IS CITED TO THE SOURCE THAT DOES NOT CARRY IT. (hunt A)

```text
TARGET, §3.1  calls K_square "four vertices for ONE cell", citing hunt :226 and
              :14; §12 says "the same K_square carries b_1 = 1 as a cell".

AT BYTES the hunt calls K_square a CARRIER, and its G-6 row ends (:235-236):
    "It is NOT sealed as the physical record complex and is a 1-complex of gauge
     carriers, not a complex of record 4-cells."
The hunt supplies the vertex/edge list and the number; it does not supply "cell".

THE WARRANT EXISTS — IN THE OTHER PRIMARY SOURCE THE BUILD HAD OPEN.
STAGE8_TYPING_RULE_CANDIDATE_V001.md :165, certificate K-1:
    "On the entered cell (K_square, b_1 = 1) the admissible set is Q·gamma_j"
That sentence, and only that sentence, licenses "cell".  The target never cites
K-1.  The characterization is TRUE at bytes; the citation given for it is not the
one that carries it, and the hunt's own qualifying clause is not displayed.
```

### X-7 A MISCITED FILENAME. (minor)

```text
TARGET, §9:  NET "sealed in STAGE8_TASK4A_...CODEX_LANE2_V001.md"
AT PATH   STAGE8_TASK4A_ACTION_COMPARISON_SQUARE_ADOPTION_PROPOSAL_LANE2_V001.md
          (V001/V002/V003 present).  There is no CODEX segment in the name; the
          segment before _LANE2 is PROPOSAL.
```

### X-8 EMPHASIS CAPITALIZATION INSERTED INSIDE QUOTED SPANS, UNMARKED. (hunt E, practice)

```text
At many sites the target upper-cases words inside quotation marks without notice —
e.g. "lands on VERTICES OF C_j" (:153 reads "vertices of C_j"); "AUDIT/PREDICTION
STRATUM ONLY" (:442 reads "audit/prediction stratum only"); "ALL IN ONE FROZEN
TOPOLOGY" (census :622 reads "all in ONE FROZEN TOPOLOGY"); "the UNBUILT O11"
(:449 reads "the unbuilt O11"); "a CONSTRUCTION gap" (:377 reads "a construction
gap"); "NOT CELL TO CELL" (census :637-638 reads "not cell to cell").
NO WORD IS ALTERED, ADDED, OR DROPPED.  The quoted BYTES are.  Recorded because a
reader collating these quotations against the sources will meet the difference.
```

---

## 7. WHAT I HUNTED HARD AND DID NOT FIND

DEFAULT-REFUTE obliges me to report the hunts that came back clean as plainly as
the ones that did not.

### 7.1 HUNT (C) — THE CONSEQUENCE. NOT OVERSTATED.

```text
THE DISPLAYED CLAIM IS WEAKER THAN BOTH SOURCES JOINTLY SUPPORT, NOT STRONGER.
The build declines deliverable three outright and states BOTH non-establishments
symmetrically (§5): not that the joiner lands in the empty sector, and not that it
does not.  It writes "Anyone reading this artifact as a clearance of the joiner is
misreading it."  That is the correct shape for a stopped chain and I could not
find a sentence anywhere in the artifact that exceeds it.

ONE MICRO-LOOSENING, RECORDED AND NOT DIGNIFIED AS A CORRECTION:
the §0 headline says the two invariants are "computed with the same convention"
flat, where §3.5 clause (c) properly records a silence (the G-9 row names no
coefficient ring).  The body governs and the body is right.
```

### 7.2 HUNT (C) — THE STATUS QUALIFIER. CONFIRMED AT BYTES.

```text
STRUCTURAL half   Verified.  The concessive is read correctly: O23SR :111-114 —
                  "Grants the adoption gate ARGUENDO ... It neutralises adoption
                  as the thing that would decide the matter."  O23SR :969 —
                  "ADOPTION CLOSES NONE OF THEM."  Adj_2's cycle rank is a fact
                  about a sealed document, and I recomputed it (§4).
HYPOTHETICAL half Verified.  Hunt :446 — "NOTHING SEALED EXISTS: a physical-
                  stratum glued multi-cell record complex."  O23SR :315-320 shows
                  PROPOSED_NOT_ADOPTED at :3 of V001, V002, V003 AND V004 — the
                  target's "all four sealed versions" is exact.
THE THIRD THING   The build's refusal to file the failed consequence under either
                  heading — "it is UNSUPPORTED ... Adoption is not what stands
                  between the corpus and that claim, and neither is non-adoption"
                  — is the correct disposition and is well made.
```

### 7.3 HUNT (D) — NO ESCAPE WAS INVENTED.

```text
The commission warned equally against ASSERTING an escape from the surface merely
not forbidding it.  The build does the opposite, repeatedly and explicitly:
  E-3   "*** NEITHER FORECLOSED NOR TAKEN IS NOT AN ESCAPE. ***"
  E-4   it raises the strongest counter-text AGAINST ITSELF (the hunt's "NOT
        forbidden ... NAMED constructor", :372-377 and :449-453) and answers it —
        "NAMING IS NOT SUPPLYING, and 'not forbidden' is the definition of
        silence, not of permission."
  C-5   declines ESCAPE-EXISTS on exactly this ground.
I verified O23SR :282-283 ("It is neither foreclosed nor taken.") and the hunt's
:449-453 at bytes.  The build's readings are exact.  NO INVENTED ESCAPE.
The defect in this deliverable is the OPPOSITE one, X-1: a class not surveyed.
```

### 7.4 HUNT (F) — NO PROPOSAL, ADOPTION, JOINER, OR REPAIR.

```text
Scoped grep for constructive verbs returns nothing.  The build:
  - proposes nothing; adopts nothing; admits no cell; promotes no carrier;
  - constructs no joiner — C-4 explicitly DECLINES to compute b_1 of a glued
    complex with cell interiors present, naming it "the sharpest temptation";
  - repairs nothing — F-1 flags a real scale tension in the hunt's own row
    (quarry stated as a glued COMPLEX at :372-377 vs H^1 computed on the NETWORK)
    and expressly refuses to recompute it or draw any consequence from it.
The CAS ran only on displays the corpus carries: Adj_2 (hunt :265) and K_square's
own vertex/edge list (hunt :226).  I confirm no invented complex was computed.
```

### 7.5 HUNT (G) — NO LENS TOKEN, NO PROGRAM NUMBER.

```text
Scoped grep for alpha / kappa / coupling / fine-structure / Newton / measured
constants: the only hits are the FENCE LINES THEMSELVES (alpha_computed = false,
kappa_record_computed = false) and one occurrence of "coupling" at :160 meaning
"inter-cell coupling" — a combinatorial edge, not a physical coupling.
Scoped grep for lens / register / tracker / road / plan / continuation: the only
hits are the target's own BARRED-AND-NOT-READ declarations plus one ordinary-
English use of "continuation" at :288.  NO LENS TOKEN.
Every number in the artifact is a vertex count, an edge count, a rank, a cycle
rank, or a line number.  NO NUMBER OF PROGRAM IMPORT.  NO MEASURED-CONSTANT
COMPARISON.  FENCES HELD.
```

### 7.6 RELAYED CITATIONS — CHECKED, GENUINE, BUT NOT MARKED AS RELAYS

```text
The target's §7 E-3 presents NET V001 :340-345, :347 and NET V002 :142 / §3 choice
table under the heading "SEALED TEXT", with bare locators, though these files are
not in its verified-seal list.  I traced each: all are carried at bytes by O23SR
(:250, :254, :277, :280-281, :333-334), whose seal the target DID verify, and
O23SR's own provenance block (:901-906) records the spans.  So the content is
sound and the practice is a relay.  Recorded as a transparency gap, not a defect:
nothing was consumed unsealed, but a reader cannot tell the relay from a direct
read without doing what I just did.
```

---

## 8. CHOICE LEDGER

```text
A-1  RE-DERIVED BOTH INVARIANTS FROM THE PRIMARY SOURCES, NOT FROM THE TARGET.
     TAKEN.  Testimony carries zero weight, so I opened typing rule :152-161 and
     hunt :262-267 myself before reading the target's account of either.
     LIVE ALTERNATIVE  Check the target's quotations for fidelity only.
     WHY DECLINED  Fidelity checking cannot catch an invariant correctly quoted
     and wrongly typed.  Hunt (A) demanded the definitions, not the quotes.

A-2  RAN THE HUNT-(D) REFINEMENT LEG AGAINST THE CENSUS *AUDIT*, NOT ONLY THE
     CENSUS.  TAKEN, and it is what found X-1.  The commission said the census
     names three such rules; the census names one (T_ref).  The three are in the
     census's AUDIT, in a KILL-GRADE finding, in a section the audit marks
     REFUTED — and that audit is on the target's own verified-ground list.
     LIVE ALTERNATIVE  Grep the census alone, find only T_ref, and conclude the
     commission's "three" was EXPECTED-UNLOCATABLE.
     WHY DECLINED  It would have missed the finding entirely.

A-3  DID NOT RUN THE MISSING ESCAPE ROW MYSELF.
     DECLINED — and this was the sharpest temptation in this audit.  Typing the
     three refinement generators against a joined object would answer whether
     X-1 changes the survey's bottom line.  That is authoring a row the build
     did not author.  DETERMINATION ONLY was the fence and I kept it.  I display
     the gap; I do not fill it, and I do not claim to know what filling it yields.

A-4  DID NOT INFLATE THE FOUR UNMARKED ELISIONS.
     TAKEN.  Six builds today elided material from quoted spans, so the commission
     was right to demand the check — and the check came back with four real
     instances that are ALL NON-ADVERSE, two of which drop text that would have
     helped the build.  Reporting them as a fidelity defect rather than as
     evidence of a suppressed counter-case is what the bytes support.
     LIVE ALTERNATIVE  Weight them as pattern evidence and downgrade the verdict.
     WHY DECLINED  It would be a finding about six other builds, not this one.

A-5  CONFIRMED GATE 1 RATHER THAN HUNTING A FAILURE IN IT.
     TAKEN.  DEFAULT-REFUTE is a posture toward evidence, not a quota.  I tried
     the equation from both sides — including whether C_j's possible 2-cells
     break the shared formula (they do not: the family's index is Z_1 = ker(bdry)
     by :155, and the hunt's own F column reads 0 on both objects) — and the
     build's non-sameness finding survived every attempt.  I record it CONFIRMED.

A-6  CHECKED THE BUILD'S POSITIVE CONCESSIONS, NOT ONLY ITS NEGATIVE FINDINGS.
     TAKEN.  Clauses (a) and (c) of gate 1 are SATISFIED in the target — these
     are the sentences that make its failure sharper, so they are the ones an
     audit is likeliest to skip.  Both hold, and (a) holds on a byte the target
     did not display (the F = 0 column).
```

---

## 9. TOY_SEPARATION

```text
IS ANYTHING IN THIS AUDIT A TOY?   *** NO. ***

WHAT I COMPUTED ON:
  Adj_2 = [[0,1],[1,0]]            — the joiner's OWN sealed display, hunt :265.
  K_square, 4 vertices / 4 edges   — the record's OWN vertex/edge list, hunt :226,
                                     with "NO 2-cell" at :14 and F = 0 at :295.
  NO INVENTED GRAPH.  NO MODEL.  NO ILLUSTRATIVE EXAMPLE.  Had the audit needed a
  complex the corpus does not display — in particular the joined object with cell
  interiors present, which is what would settle F-1 and X-1 — I stopped instead.

WHAT I DETERMINED ON:
  The actual sealed artifacts of the record, each seal verified at its own
  directory before reliance, plus one the target consumed without declaring
  (X-4), which I verified myself.

NO SURFACE WAS SIMULATED.  No physical quantity, no stratum crossed, no adoption,
no admission, no program value.  This audit touches only what two sealed documents
say and the arithmetic they themselves display.
```

---

## 10. IMPORT AUDIT

```text
IMPORTED FROM OUTSIDE THE CORPUS — ONE ITEM, THE SAME ONE THE TARGET DECLARED:
  For a finite graph over a field, dim H^1 = dim H_1 = dim Z_1 = E - V + comps
  (rank-nullity on the boundary map d_1).
  USED FOR   Re-deriving each source's own displayed number from its own displayed
             combinatorial data, and confirming the homology/cohomology difference
             changes no value.
  NOT USED FOR  Deciding anything about the corpus's content.  Every finding above
             rests on quoted sealed bytes.
  WHY SAFE   Both sources already use it — hunt :316 writes "E - V + components",
             typing rule :155-156 writes "of dimension b_1(C_j)" for
             ker(bdry)|_{C_j} — and the hunt's own F column reads 0 on both
             objects, so no 2-cell correction arises on either.

EXPRESSLY NOT IMPORTED:
  - Continuum contractibility (the GC-barred route; hunt fence :467-471).
  - Any physical quantity, scale, coupling, or measured constant.
  - Any relation between strata; any admission; any adoption; any gluing license.
  - Any typing of the three refinement generators against a joined object.  That
    is the row X-1 says is missing, and I did not supply it.
  - Any content from register / tracker / road / plan / continuation files.
    None was read.
```

---

## 11. FLAG BLOCK

```text
FENCES, CARRIED UNCHANGED
  alpha_computed              = false
  proof_authorized            = false
  kappa_record_computed       = false
  No value.  No number as a program quantity.  No measured-constant comparison.
  Every number here is a vertex count, edge count, matrix rank, cycle rank, or a
  line number.

ACTS NOT TAKEN
  Nothing proposed.  Nothing authored.  Nothing adopted.  No joiner constructed.
  No cell admitted.  No carrier promoted.  No stratum crossed.  NOTHING REPAIRED —
  in particular X-1's missing escape row and X-2's over-claim are DISPLAYED and
  left standing, and the target's own F-1 is left exactly where it put it.

AF-1  THE TARGET'S F-1 IS A REAL TENSION AND IT IS CORRECTLY LEFT UNREPAIRED.
      The hunt's quarry at :372-373 is "a glued record complex with H^1 > 0";
      G-9's row computes H^1 of the two-node NETWORK.  Whether those coincide for
      G-9's yield turns on the joined cells' own cycle ranks, which NET does not
      display.  I confirm the tension exists at bytes, confirm the build drew no
      consequence from it anywhere, and draw none myself.  UNRESOLVED AT BYTES.

AF-2  X-1 AND AF-1 ARE THE SAME SCALE QUESTION WEARING TWO HATS, AND BOTH ARE
      UNRESOLVED FOR THE SAME REASON: no sealed text displays a joined object
      with its cells' interiors present.  I record the connection and stop.

AF-3  EXPECTED-UNLOCATABLE ITEMS.  "Q-..." — none sought, none consumed, none
      required by any finding above.

AF-4  SCOPE OF X-1.  X-1 is a defect in the SURVEY'S COVERAGE AND PREMISE, not a
      demonstration that an escape exists.  A reader who takes X-1 as reopening
      the joiner's admissibility has over-read it; the consequence never stood
      (target §5), so nothing is in need of relief either way.

AF-5  ONE SEAL CONSUMED UNDECLARED (X-4).  Verified OK by me.  No finding of the
      target's rests on any unsealed byte.
```

---

## 12. VERDICT, PER DELIVERABLE AND OVERALL

```text
DELIVERABLE                                            VERDICT
§0    headline                                         CONFIRMED-WITH-CORRECTIONS
      (states "same convention" flat where §3.5(c) properly records a silence)
§1    sweep declaration and cutoff                     CONFIRMED-WITH-CORRECTIONS
      (X-4 second half: one consumed file undeclared; §7.6 relays unmarked)
§2    seals verified                                   CONFIRMED-WITH-CORRECTIONS
      (all eight re-verified OK by me from the artifact's own directory; one
       further consumed file omitted from the list)
§3    GATE ONE — the two invariants                    *** CONFIRMED ***
      (re-derived from both primary sources; X-5 and X-6 are citation defects
       that do not touch the finding)
§3.4  the exact symbolic check                         *** CONFIRMED ***
      (both displays independently re-derived, fresh venv, exact rationals; also
       cross-checked against the hunt's own table columns)
§4    GATE TWO — the domain gate                       CONFIRMED-WITH-CORRECTIONS
      (gate 2 does fail; X-2 — Restriction 1 does not alone suffice)
§5    deliverable three — does not fire                *** CONFIRMED ***
§6    deliverable four — the status qualifier          *** CONFIRMED ***
§7    deliverable five — the escape survey             CONFIRMED-WITH-CORRECTIONS
      (X-1 — refuted uniqueness premise; the refinement-generator class unsurveyed)
§8    choice ledger                                    CONFIRMED
§9    TOY_SEPARATION                                   CONFIRMED-WITH-CORRECTIONS
      (X-7 — miscited filename)
§10   import audit                                     CONFIRMED
§11   flag block                                       CONFIRMED
§12   verdict                                          CONFIRMED-WITH-CORRECTIONS
      (carries §4's over-claim and §7's premise forward)

QUOTATION INTEGRITY, CHECKED TO SPAN END THROUGHOUT
                                                       CONFIRMED-WITH-CORRECTIONS
      (X-3: four unmarked intra-span elisions, ALL NON-ADVERSE; X-8: unmarked
       emphasis capitalization inside quoted spans)

FENCES (alpha_computed, proof_authorized, kappa_record_computed; no value, no
program number, no measured-constant comparison, no git, no barred file)
                                                       *** CONFIRMED ***
ACTS BARRED (proposal, adoption, constructed joiner, repair)
                                                       *** CONFIRMED — NONE TAKEN ***
```

```text
*** OVERALL: CONFIRMED-WITH-CORRECTIONS ***

THE DETERMINATION STANDS.  The two invariants are not commensurable; gate 1 fails
on clause (b); the chain stops; the consequence is neither displayed nor asserted
nor negated; gate 2 fails independently; the status qualifier separates correctly;
no escape is displayed.  I re-derived every one of those at bytes and every
homological number in a fresh venv, and none of them moved.

The build's central discipline is sound in the two places it was most likely to
fail.  It refused to equate two invariants that share a name, a formula, and a
field but not an argument — hunt (A), and it got it right.  And it refused to read
the corpus's awareness of a move as the corpus's provision of one — hunt (D)'s
second half, and it got that right too.

What it did not do is survey the class the commission named.  X-1 is the finding
of this audit: E-2's uniqueness premise is marked *** REFUTED *** by the census
audit the build itself verified and listed as ground, and the three cell-yielding
refinement generators that refute it are absent from the artifact.  The survey
reaches ADMITS = NONE, and I do not show that wrong — but the build did not show
it right, and a survey is a claim about coverage.

X-2 corrects a sufficiency that R-CLW's own degenerate clause denies.  X-3 through
X-8 are citation and quotation-fidelity defects; every one of them is non-adverse,
and I decline to let their number stand in for weight they do not have.
```

```text
DETERMINATION ONLY.  Displayed, not constructed.  Nothing proposed, authored,
adopted, or repaired.  alpha_computed = false; proof_authorized = false;
kappa_record_computed = false.
```

*** END OF AUDIT — STAGE8_JOIN_ADMISSIBILITY_O28SR_AUDIT_V001 ***
