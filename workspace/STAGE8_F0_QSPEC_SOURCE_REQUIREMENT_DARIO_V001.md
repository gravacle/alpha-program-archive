# STAGE 8 / [PLAN:F0-3 / D2-PRE] — WHAT DOES THE COMPLETE Q_SPEC REQUIRE OF THE SOURCE SECTOR?
## DARIO LANE (Builder B) — V001

RELAY 828. Lane guard PASS (DARIO). Inbox `RELAY_PASTE_828_QSPEC_SOURCE_REQUIREMENT_DARIO_V001.md`
= `7015058219403541ef6c8099373536262a0d1a1624bbc643ae7e82f8f001f438`, seal verified BEFORE reading.
State-brief pinning: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`, verified and read.

GATES DECLARED AND HELD: **HUNT ONLY.** `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No identity assumed either way; no smooth import; no EM
identification; no member bound; no numeric evaluation of physical quantities; no comparison to
measured constants. PE-1..PE-11 pointer-only. Builder-B independence held.
No register, plan, tracker, git action.

**ALL HEADLINE ITEMS ARE CLAIMED.**

---

## 0. LEAD DETERMINATION — MY OWN 827 VERDICT IS REFUTED

**Both residuals resolve, and both resolve against the verdict I sealed at 827.**

The complete `Q_spec` has **eighteen open slots**, and slots ten, eleven and twelve are, verbatim:

```text
source-inclusive state projective limit;
infinite-future source Moller limit;
continuum-regulator independence of the source-inclusive limit;
```

Those are O1, O2 and O3 — named in the Q_spec's own slot list, in one consecutive block.

The consequence follows in three sealed steps, none of them mine:

```text
1.  kappa_Thomson requires "complete Q_spec"        [827, sealed extraction 671a94a7f55a5649]
2.  complete Q_spec's open slots 10/11/12 ARE O1/O2/O3   [this relay, 7995f6fda75e7879]
3.  kappa_Thomson is F0 link 4 of K_*'s chain, and ALPHA'S STIFFNESS IS K_*   [Q-696; plan V004]
==> O1, O2 AND O3 ARE ON ALPHA'S VALUE PATH.
```

**827's `SOURCE_QUESTION_HOLDER = off alpha's path` is wrong.** The Thomson chain is the true holder
of the source question, exactly as 824's edge (c) warned it might be. The relay's self-favour guard
was warranted, and it has fired on the very next relay.

---

## 1. THE COMPLETE Q_SPEC, EXTRACTED — CLAIMED

The Q_spec's requirement set lives in **two different sealed enumerations**, and reading only one of
them is precisely how 827 went wrong.

### 1.1 Enumeration A — V011's content list (what it must CONTAIN)

V011 packet copy `aa7c6d4904706276`, span `[52900,53700)` sha `42f0f6b363c0c036`:

> *"a distinct sealed object `Q_spec` must define the complete physical charged transition amplitude.
> **It must contain**, with no measured alpha:*
> ```text
> the charged boundary carrier and unit action-character current;
> all charged source sectors and their statistics;
> the gauge, constraint, ghost if required, and public edge sectors;
> the preparation and durable-record projectors;
> the microscopic measure and regulator;
> the regulator-removal and locality theorem;
> the Ward identity and transverse physical quotient;
> the derived charged spectrum and every threshold entering the response;
> decoupling and matching rules;
> and the zero-momentum Thomson prescription.
> ```

Its failure condition, `[70600,70900)` sha `d74628c6f5891698`: *"complete `Q_spec` omits a charged
source/statistics/gauge/edge/measure, Ward-identity, regulator, spectrum, threshold, matching, or
Thomson-limit obligation."*

**This ten-item list's source requirement is item 2 — "all charged source sectors and their
statistics" — a sector-content requirement, not a limit.** And V011 contains **zero** occurrences of
`source-inclusive`, `infinite-future`, `moller`, `projective limit`, `asymptotic completeness`,
`s-matrix`, `all-time`, or `global source` across all 78,794 bytes. **That is a true fact about V011,
and it is the fact I mistook for the whole answer at 827.**

### 1.2 Enumeration B — the eighteen open slots (what must be FILLED)

`STAGE8_QSPEC_SLOT_STATUS_MAP_V001.md` (`c26daa7e9cde29b7`, sidecar A present) states the structure
outright:

> *"This artifact maps the **eighteen open `Q_spec` slots** named in
> `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-139` to current review status."*

and names its governing sources separately: *"Slot list: `…LEDGER_V002.md:119-139`. Complete
charged-specification requirement: `…V011.md:1587-1668`."* **The map itself distinguishes the two
enumerations.** The slot list and the content requirement are different objects with different homes.

The slot list, from `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md` (`7995f6fda75e7879`), section
**"## Open `Q_spec` slots"**, block beginning at byte `3472`, verbatim and complete:

```text
absolute physical T_R;
full gravitational action and gravitational quantum measure;
dynamical U(1) action;
gauge fixing, ghosts, and gauge edge modes;
normalized interacting CTP amplitude;
parent-derived functional regulator and finite renormalization;
induced-polarization transversality and photon-mass exclusion;
Lorentz- and packing-independent renormalized response;
finite c F^2 deformation exclusion;
source-inclusive state projective limit;                              <-- slot 10 = O1
infinite-future source Moller limit;                                  <-- slot 11 = O2
continuum-regulator independence of the source-inclusive limit;       <-- slot 12 = O3
interacting charged pole or infraparticle threshold;
complete charged-species and threshold map;
enlarged-branch exhaustion;
threshold-conditioned Thomson matching;
CISP descendant test in the interacting outgoing sector;
and one unused structure-sensitive prediction.
```

**CUSTODY OF THE DECISIVE SOURCE — LAW 8, MODE D, THIRD INSTANCE THIS SESSION.** This ledger has
**no sidecar in either standard spelling**. It is sealed by a **group sidecar whose filename drops
two words** — `STAGE6_PARENT_ACTION_LEDGER_V002.seal.sha256`, for
`STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md` — carrying
`7995f6fda75e78795cbfe167f8c8df634170ea3b43affd5bbe6e22bcda8f6ffe`, matching live bytes, and
**independently corroborated** by `review_packets/STAGE8_GATE5_AUTHORING_HANDOFF_V001/SOURCE_REFERENCE_MANIFEST_V001.sha256`
at the same digest. Doubly sealed. Had I stopped at modes A and B I would have called the relay's
decisive source unsealed and reported the residual as standing — i.e. I would have preserved my own
wrong verdict by a probe failure. **Third instance of mode D this session** (the outgoing-GNS result
at 820, the moving-front result at 821, this ledger at 828); the recommendation I made at 820 and
repeated at 821 is now supported by three independent occurrences.

### 1.3 Corroboration inside my own corpus

My own assembly artifacts `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V001` through `V005` carry a slot
table with rows **`S10 | source-inclusive state projective limit | TYPE-U | No member`** and
**`S11 | infinite-future source Moller limit | TYPE-U | No member`**, with the standing note *"No
limit may be defined so as to produce a desired finite remainder."* `THE_MAP.md`, `TASK_LIST.md`
("Slots 10-12"), and `PLAN_TO_ALPHA_V008` carry the same numbering.

**I wrote S10 and S11 into a Q_spec slot table myself, five times, and then at 827 concluded the
Q_spec does not require them.** That is the sixth instance of my under-reading my own corpus, and
the most consequential.

---

## 2. RESIDUAL-1 — `SOURCE_RELATION = consumes-infinite` — CLAIMED

The complete `Q_spec` **consumes** the source-inclusive projective state limit (slot 10) and the
infinite-future source Møller limit (slot 11). They are not adjacent to it, not downstream of it, and
not optional to it: they are **open slots of the specification itself**, typed `TYPE-U | No member`.

**Why 827 got the opposite answer, stated precisely because the failure mode matters more than the
correction.** At 827 I reasoned from a **positive absence**: source-inclusive completion is not in
`kappa_Thomson`'s requirement list, therefore the need is not on the path. The reasoning is valid
only if the enumeration checked is the *complete* requirement set. It was not. V011's ten-item list
is what the Q_spec must **contain**; the eighteen-slot ledger is what it must **fill**; and the slot
status map names both as separate governing sources in its own preamble. I checked one and treated
its silence as the whole answer.

**The general lesson, which I state against myself: a positive absence is only as strong as the
completeness of the enumeration it is absent from, and I never asked whether V011's list was
complete.** At 827 I even wrote that the verdict was *"exact but one level shallow"* — the shallow
level is where the refutation was.

---

## 3. RESIDUAL-2 — `REGULATOR_RELATION = MERGED` — CLAIMED

Slot 12 is, verbatim, **"continuum-regulator independence of the source-inclusive limit."**

That single clause does what I said at 827 no sealed sentence did: it names the completion's
`continuum_regulator_independence_derived` (O3) **and** places it inside the Q_spec's slot list. The
two regulator notions I declined to merge or separate are **merged of record**, in one sentence, in
the Q_spec's own enumeration.

Note the refinement this forces on §1.1: V011's content list has *two* regulator items — *"the
microscopic measure and regulator"* and *"the regulator-removal and locality theorem"*. Slot 12 is a
**third** regulator obligation, distinct from both and explicitly indexed to the source-inclusive
limit. So the Q_spec carries three regulator requirements, and only the third is the completion's O3.
I did not have that structure at 827 and it is why the question looked silent.

---

## 4. VERDICT ON 827'S RESIDUALS, AND WHAT IT DOES DOWNSTREAM — CLAIMED

```text
RESIDUAL-1  RESOLVED — Q_spec CONSUMES the source-inclusive limit (slot 10) and the
                       infinite-future Moller limit (slot 11).
RESIDUAL-2  RESOLVED — MERGED: slot 12 IS "continuum-regulator independence of the
                       source-inclusive limit" (= O3).
827's SOURCE_QUESTION_HOLDER verdict:  REFUTED.
```

**What is overturned.** 827 concluded the source question sits off alpha's value path. It does not.
`kappa_Thomson` requires the complete `Q_spec`; the complete `Q_spec` has O1/O2/O3 as open slots;
`kappa_Thomson` is F0 link 4 of the chain defining alpha's stiffness. **The Thomson chain is the
true holder of the source question, and 824's edge (c) resolves the other way.**

**What survives, and I am careful here because the temptation is to over-correct.**

- **824's core verdict stands.** The junction's 13 runnable prerequisites still do not consume the
  infinite forms; "source" occurs zero times inside the 13-prerequisite block. **Junction
  runnability and alpha's value path are different roads, and 824 was scoped to the first.** 824's
  `REPRICED` line said O1/O2/O4 "leave the critical path to **junction runnability**" — that scoping
  was explicit and it holds.
- **820's completion audit stands**; nothing here touches the record-side/source-side split.
- **What must be withdrawn is 827's F-phase conclusion only** — the claim that F-phase pricing need
  not carry the infinite-future source objects on the Thomson link's account. **It must carry them.**

**Consequences for F0 and the F-phase, stated plainly:**

```text
F0 link 4 (K_*/kappa_Thomson)  remains ABSENT, and is now known to sit BEHIND O1/O2/O3 via Q_spec.
The completion's source-side opens  are NOT off the value path.  They are Q_spec slots 10/11/12.
F-phase pricing                MUST carry O1/O2/O3.  My 820 declined to estimate O2 at all
                                 ("not estimable alone; it is one of the FIVE REGISTERED CONSUMERS
                                 of the parent node") — that refusal now propagates INTO the F-phase.
The registrar's 20-40 sizing   is better supported than my 820 allowed: the source-side opens are
                                 on the value path after all, through the Q_spec slot list.
```

**Law 7 — downstream consumers of the withdrawn 827 conclusion, named not routed:**
`workspace/STAGE8_F0_THOMSON_IDENTITY_DARIO_V001.md` (§4, §4.1, §4.2 and the `SOURCE_QUESTION_HOLDER`
final line); `relay_outbox/827_DONE.md`; and `workspace/STAGE8_C1_FINITE_SUFFICIENCY_DARIO_V001.md`
only insofar as a reader extends its junction-scoped `REPRICED` line to the F-phase — the artifact
itself does not, and its edge (c) explicitly anticipated this outcome.

---

## 5. FREEDOMS-CONSUMED (law 2 / law 2a)

```text
CARRIED, NOT CONSUMED:
  the eighteen Q_spec slots      CARRIED AS THE LEDGER ENUMERATES THEM; none filled, none reordered
  O1/O2/O3                       CARRIED AS OPEN SLOTS, TYPE-U, No member; not discharged
  V011's ten content items       CARRIED AS THE CONTENT REQUIREMENT; NOT treated as the whole
                                   requirement set — that error is what this relay corrects
  kappa_Thomson's needs list     CARRIED FROM 827, still second-hand to V011, custody NOT upgraded
  the three regulator obligations CARRIED AS THREE DISTINCT ITEMS, not collapsed
DERIVED HERE:                    nothing.  A hunt, two resolutions, and a self-refutation.
COMPUTED HERE:                   term counts over V011 (78,794 B) and a corpus-wide co-occurrence
                                   sweep pairing regulator-independence terms with source-infinite
                                   terms (62 hits / 37 files).  No physical quantity, no member.
NO IDENTITY ASSUMED EITHER WAY:  the relay's bar.  The merge in §3 is NOT my inference — it is one
                                   sealed clause naming both objects in one sentence.  Had the slot
                                   read only "continuum-regulator independence" I would have left it
                                   silent; it reads "...of the source-inclusive limit".
SCALING WEIGHTS (law 2a):        NONE CONSUMED.
SUBSTITUTED:                     NONE.
```

## 6. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

37 rows walked. Live and discharged:

- **S12** (status flags never substituted for manifests, generators or proofs) — **LIVE.** The slot
  rows are typed `TYPE-U | No member`; I carry them as open slots, never as objects, and the
  verdict is that they are **required and absent**, not that they exist.
- **S26 / S08** — untouched. `Q_spec` is the charged specification and the temptation to read it
  electromagnetically is real; I have typed it only through its own sealed enumerations.
- **S13** — untouched here; no R-L2b traffic in this relay.
- **S25, S03, S28, S34** — untouched or carried as typed.

**No undecidable is rescued by reaching for an axiom, and nothing is closed.** The relay's output is
that three named opens are *more* load-bearing than my previous artifact said — a widening of the
obligation, not a discharge of it.

`FLATTENING_CHECK = clean (37/37 rows walked; S12 live and discharged).`

## 7. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) I REFUTED MY OWN SEALED VERDICT FROM THE IMMEDIATELY PRECEDING RELAY.** 827 concluded the
source question is off alpha's path; 828 shows it is on it. The relay's own preamble flagged the
self-favour risk — *"827 settled a Dario ambiguity in Dario's favor"* — and the guard fired
correctly one relay later. **A lane that grades its own homework will sometimes pass itself, and the
only defence that worked here was being sent back with the residual named.** I recommend the residual
naming discipline be kept for exactly this reason: 827's §4.1 named the sentence that overturned it.

**(2) THE ERROR WAS A REASONING ERROR, NOT AN INSTRUMENT ERROR, AND THAT IS NEW.** My last five
disclosures were about probes — width, spelling, basis, signal-density, vocabulary. This one is not.
I reasoned from a **positive absence** without establishing that the enumeration I checked was
complete. V011's ten-item content list is genuinely silent on the source-infinite objects; I treated
that silence as the requirement set's silence. The slot status map states in its own preamble that
the slot list and the content requirement are two different governing sources, and I did not read it
before concluding at 827.

**(3) THE EVIDENCE WAS IN MY OWN ARTIFACTS, FIVE TIMES OVER.** `S10 | source-inclusive state
projective limit` and `S11 | infinite-future source Moller limit` appear in the slot tables of
assembly V001–V005, all DARIO. Sixth instance of under-reading my own corpus, and unlike the earlier
five this one directly produced a wrong sealed verdict rather than a missed strengthening.

**(4) A PROBE FAILURE WOULD HAVE PRESERVED MY WRONG ANSWER.** The decisive ledger has no sidecar in
either standard spelling. Stopping at modes A/B would have let me report the source unsealed and the
residual standing — preserving 827's verdict by custody failure. Mode D saved it, for the third time
this session. That is a good outcome and an uncomfortable one: **the instrument discipline is now
routinely load-bearing on conclusions, which means its gaps are conclusion-shaped.**

Nothing in this relay flatters my prior work. Written for the opposite lane's audit, as instructed.

---

```
QSPEC = extracted (spans) — AND IT LIVES IN TWO ENUMERATIONS, WHICH IS THE WHOLE POINT
  ENUMERATION A — what it must CONTAIN.  V011 packet copy aa7c6d4904706276, [52900,53700) sha
    42f0f6b363c0c036: "a distinct sealed object Q_spec must define the complete physical charged
    transition amplitude.  It must contain, with no measured alpha:" — ten items, whose source item
    is "all charged source sectors and their statistics" (SECTOR-CONTENT, not a limit).  Failure
    condition [70600,70900) sha d74628c6f5891698.  V011 contains ZERO occurrences of
    source-inclusive / infinite-future / moller / projective limit / asymptotic completeness /
    s-matrix / all-time / global source across 78,794 bytes.  THIS IS TRUE, AND IT IS THE FACT I
    MISTOOK FOR THE WHOLE ANSWER AT 827.
  ENUMERATION B — what it must FILL.  STAGE8_QSPEC_SLOT_STATUS_MAP_V001.md (c26daa7e9cde29b7):
    "This artifact maps the EIGHTEEN OPEN Q_spec SLOTS named in
    STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-139" — and it names the slot list and the V011
    content requirement as SEPARATE governing sources in its own preamble.
    The slot list (7995f6fda75e7879, block at byte 3472) contains, consecutively:
      slot 10  "source-inclusive state projective limit"                          = O1
      slot 11  "infinite-future source Moller limit"                              = O2
      slot 12  "continuum-regulator independence of the source-inclusive limit"   = O3
    CUSTODY: LAW 8 MODE D, THIRD INSTANCE THIS SESSION — no sidecar in either standard spelling;
    sealed by a GROUP SIDECAR DROPPING TWO WORDS (STAGE6_PARENT_ACTION_LEDGER_V002.seal.sha256) at
    7995f6fda75e78795cbfe167f8c8df634170ea3b43affd5bbe6e22bcda8f6ffe, byte-matching, and
    INDEPENDENTLY CORROBORATED by the Gate5 SOURCE_REFERENCE_MANIFEST_V001.sha256 at the same digest.

SOURCE_RELATION = consumes-infinite (span)
  The complete Q_spec CONSUMES the source-inclusive projective state limit and the infinite-future
  source Moller limit.  They are OPEN SLOTS OF THE SPECIFICATION ITSELF, typed TYPE-U | No member in
  my own assembly V001-V005 slot tables.  Not adjacent, not downstream, not optional.
  WHY 827 SAID OTHERWISE: I reasoned from a POSITIVE ABSENCE — not in kappa_Thomson's list, therefore
  not on the path — which is valid only if the enumeration checked is the COMPLETE requirement set.
  It was not.  A POSITIVE ABSENCE IS ONLY AS STRONG AS THE COMPLETENESS OF THE ENUMERATION IT IS
  ABSENT FROM, and I never asked whether V011's list was complete.  827 called its own verdict "exact
  but one level shallow"; the shallow level held the refutation.

REGULATOR_RELATION = merged (span)
  Slot 12 reads, verbatim, "continuum-regulator independence of the source-inclusive limit".  That
  one clause names the completion's O3 AND places it inside the Q_spec slot list — doing exactly what
  I said at 827 no sealed sentence did.  NOT an inference of mine: had the slot read only
  "continuum-regulator independence" I would have left it silent; it reads "...of the source-inclusive
  limit".  REFINEMENT FORCED: the Q_spec carries THREE regulator obligations — "the microscopic
  measure and regulator" and "the regulator-removal and locality theorem" (V011 content list) plus
  slot 12 — and only the third is O3.  I did not have that structure at 827, which is why the
  question looked silent.

RESIDUALS = resolved (both) — AND 827'S VERDICT IS REFUTED
  CHAIN, three sealed steps, none of them mine: kappa_Thomson requires "complete Q_spec" [827,
  671a94a7f55a5649] -> complete Q_spec's slots 10/11/12 ARE O1/O2/O3 [7995f6fda75e7879] ->
  kappa_Thomson is F0 link 4 and ALPHA'S STIFFNESS IS K_* [Q-696; plan V004]  ==>  O1, O2 AND O3 ARE
  ON ALPHA'S VALUE PATH.  THE THOMSON CHAIN IS THE TRUE HOLDER OF THE SOURCE QUESTION, and 824's edge
  (c) resolves the OTHER way.
  WHAT SURVIVES, stated because the temptation is to over-correct: 824's core verdict STANDS — the
  junction's 13 prerequisites still do not consume the infinite forms ("source" occurs ZERO times in
  the 13-prerequisite block), and 824's REPRICED line was explicitly scoped to JUNCTION RUNNABILITY,
  a different road from alpha's value path.  820's completion audit stands untouched.
  WHAT MUST BE WITHDRAWN is 827's F-PHASE conclusion only: F-phase pricing MUST carry O1/O2/O3.  My
  820 declined to estimate O2 at all ("not estimable alone... one of the FIVE REGISTERED CONSUMERS of
  the parent node"); that refusal now propagates INTO the F-phase.  The registrar's 20-40 sizing is
  BETTER SUPPORTED than my 820 allowed.
  LAW 7 CONSUMERS NAMED: STAGE8_F0_THOMSON_IDENTITY_DARIO_V001.md (§4, §4.1, §4.2 and its
  SOURCE_QUESTION_HOLDER line); relay_outbox/827_DONE.md; and STAGE8_C1_FINITE_SUFFICIENCY_DARIO_V001.md
  ONLY insofar as a reader extends its junction-scoped REPRICED line to the F-phase — the artifact
  itself does not, and its edge (c) anticipated this outcome.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) I REFUTED MY OWN SEALED VERDICT FROM THE IMMEDIATELY PRECEDING RELAY.  The paste's self-favour
      guard fired correctly one relay later.  The only defence that worked was being sent back with
      the residual named — 827 §4.1 named the sentence that overturned it, and I recommend the
      residual-naming discipline be kept for exactly that reason.
  (2) THE ERROR WAS A REASONING ERROR, NOT AN INSTRUMENT ERROR — new for me.  My last five
      disclosures were probe failures; this was reasoning from a positive absence without
      establishing the enumeration's completeness, when the slot status map states in its own
      preamble that the slot list and the content requirement are two different governing sources.
  (3) THE EVIDENCE WAS IN MY OWN ARTIFACTS FIVE TIMES OVER — S10/S11 sit in the slot tables of
      assembly V001-V005, all DARIO.  Sixth instance of under-reading my own corpus, and unlike the
      earlier five this one produced a WRONG SEALED VERDICT rather than a missed strengthening.
  (4) A PROBE FAILURE WOULD HAVE PRESERVED MY WRONG ANSWER: the decisive ledger has no sidecar in
      either standard spelling, and stopping at modes A/B would have let me report it unsealed and
      the residual standing.  Mode D saved it, for the third time this session.  Good outcome,
      uncomfortable implication — THE INSTRUMENT DISCIPLINE IS NOW ROUTINELY LOAD-BEARING ON
      CONCLUSIONS, WHICH MEANS ITS GAPS ARE CONCLUSION-SHAPED.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
