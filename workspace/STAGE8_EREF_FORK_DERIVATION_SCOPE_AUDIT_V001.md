# CODEX 2 — E_ref fork derivation scope

`alpha_computed = false; proof_authorized = false; kappa_record_computed = false.`

The sealed Stage-10 brief states the surviving fork as `E_ref ∈ {E_* ≡ m_*, μ = m_*/√2}` and identifies it as the matching question (`45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md:8-18`). The ratio result records the same fork and says only derived Stage-10 response/matching may decide it (`21_DIMENSIONLESS_RATIO_RESULT_V001.md:20-39,55-68`). The candidates differ by the reference rate used in the public-response denominator. The marginality-coincidence route is unavailable.

Downstream, `rho = (3/16) g_N^2/(m_* E_ref)` consumes E_ref (`21_DIMENSIONLESS_RATIO_RESULT_V001.md:9-17`); seat occupancy leaves exactly this fork (`26_SEAT_OCCUPANCY_RESULT_V001.md:56-70`). The Stage-10 public-response normalization is the named deciding consumer (`45_STAGE10_HANDOFF_GEOMETRIC_BRIEF_V001.md:34-45`). Continuous interval dependence cancels, but E_ref itself remains consequential.

Candidate grounds were checked without choosing: first-principles quasilocal energy (`NO_VERDICT | TYPE-U`, response not built); positivity/monotonicity/limit (`NO_VERDICT | TYPE-S`, no one-sided sealed constraint); consistency with sealed results (`NO_VERDICT | TYPE-S`, both survive); marginal `C_R=1` (`NO_VERDICT | TYPE-R`, coincidence route refuted and no alternative selector); record-cell boundary conditions (`NO_VERDICT | TYPE-U`, Stage 10 response/matching unexecuted).

`E_ref_derived = false | TYPE-U | would-build: Stage-10 physical response/matching with pre-frozen predictions and Ward/causal tests.`
`E_ref_choice_made = false | TYPE-C | constraint: fork must be derived, never selected.`

No git, commit, push, gate, or deploy action was performed.
