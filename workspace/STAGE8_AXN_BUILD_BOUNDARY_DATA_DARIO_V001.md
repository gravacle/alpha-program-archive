# STAGE 8 / [PLAN:AXN-BUILD-A13] — THE BOUNDARY DATA FROM B0/C0
## DARIO LANE (Builder B) — V001

RELAY 846. Lane guard PASS (DARIO). Inbox `a6b6c58194a37328…`, seal verified BEFORE reading.
State brief `e26f0d16055f3e83…` verified and read. Charter `c0ad6decf156ef06`, seal OK, binds whole.

**GOVERNING SUBJECT (builder-never-verifies):** Codex 2's 845,
`STAGE8_AXN_BUILD_EXITB_CROSSCHECK_CODEX2_V001.md` = `de99bf8962f08ee2`, `.md.seal` OK, verified
BEFORE reading, digest matches the paste's pin. **Its corrected form of my 844 scheme governs.**

GATES: `alpha_computed = false`; `proof_authorized = false`; `kappa_record_computed = false`.
**DERIVE-NEVER-SELECT; STOP-ON-FREEDOM ABSOLUTE. NOTHING SELECTED.** No smooth import; no EM
identification; no member binding. PE-1..PE-11 pointer-only. **ALL HEADLINE ITEMS CLAIMED.**

---

## 1. CORRECTIONS CARRIED FIRST

**(1a) MY SECOND CIRCULARITY IS BROKEN — adopted.** 845 resolves the SC5 citation at the BID source
directly (the byte-level resolution I asked a verifier to do) and finds the support *"Stage-3-specific"*.
The dependency graph settles it: **the complete action and the U3 boundary data are sibling
descendants of B0, not the same object.** `SAME_OBJECT = distinct`. So my 844 principal finding —
that both loops close through the complete action — **is wrong as a graph claim.** SC5 is **stopped,
not circular.**

**(1b) `P1` IS REFUTED AT ITS ADVERTISED CONSEQUENCE — NOT CONSUMED.** The square expansion and the
support *upper bound* survive; the conclusions do not. Overlap support is an upper bound, **nonzero
products are not forced** (orthogonal-fiber counterexample), and **closure-face support rides a
separate derivative term.** My "C5's overlap subset is automatically satisfied" is withdrawn.
**P1 appears nowhere below as an input.**

**(1c) `SC4_SUPPLIER = NONE-CONFIRMED` — my SC4 stop is upheld** on a 2,030-file / 621-candidate
enumeration, with partial other-carrier regularity noted as not a complete `X_prim` supplier.

**(1d) `B0C0_PATH = LICENSED` but *"still TYPE-U/unbuilt; named supplier is not discharge."***
That is this relay's starting point.

---

## 2. THE LICENSED ROUTE, AND WHERE IT STOPS

The sealed dependency graph (`STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md`
= `ec848d293d0d8241`, `.md.seal` OK — digest matches 845's pin):

```text
B0  COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR
  +--> C0  narrow carrier/algebra extension
  +--> U1  physical branch/source typing on C0
  +--> U2  action/evolution + state/effects + action contact rules on C0
  +--> U3  quotient + descended measure + boundary/edge/gluing and endpoint operator domains
           + predeclared contour prescription on C0
manifest:  C0|B0 ;  U1|B0,C0 ;  U2|B0,C0 ;  U3|B0,C0
common-origin: "The state, action, carrier, sources, quotient, measure, and domains must all
                descend from B0"
```

**The route to `complete_CTP_boundary_edge_data` therefore runs B0 → C0 → U3. Walking it link by
link, it is blocked at three sealed TYPE-U flags — two upstream, one at the far end.**

### 2.1 LINK 0 — B0's construction witness: **ABSENT**

`[2367,2907)` sha `69ad26da99b2479b`:

> `COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR_derived = false | TYPE-U | would-build: produce the single
> complete microscopic source-record-field boundary operator/dynamics from an explicitly frozen
> primitive-input inventory and construction rule, without receiving any descendant or discharge flag
> as an input`
>
> `B0_CONSTRUCTION_WITNESS_derived = false | TYPE-U | would-build: state and derive the
> target-independent primitive input inventory and construction rule that produce the package B0
> object; **the present bounded corpus names no exact prer[equisite]**`

**B0 is a ROLE, and it is uninhabited — and the corpus names no exact prerequisite for inhabiting
it.** The relay's own framing ("B0 is a ROLE with a bar") is exact. **The build cannot begin at link
0**, and the would-build's bar is severe: the construction rule may not receive *"any descendant or
discharge flag as an input"* — which forbids reconstructing B0 backwards from the very boundary data
it is supposed to produce.

### 2.2 LINK 3 — the far end: a NON-IDENTITY that 845's route summary does not carry

This is the relay's own question — *what the route yields and what it lacks* — and it is where I add
something. **The condition that would actually serve as SC5's prescription does not live on B0.**

`[31771,32311)` sha `a7e4dde61c9802cf`, quoting `primitive_causal_record_cell_domain_principle_v004.md:25-39`:

> *"The complete Boundary-Resolved generator must still prove microcausal support of the history
> difference and **make the global Dirac boundary form vanish under the CTP preparation/gluing
> variations**."*

and immediately, `[32029,32569)` sha `d4b495520a3d89e4`:

> *"That source **does not equate** its complete Boundary-Resolved generator with package B0. The
> causal/boundary conditions are obligations on the named generator; their bearing on B0 **requires a
> separately derived correspondence**."*
>
> `B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false | TYPE-U | would-build:
> independently type both objects and derive a direction-bearing map …`

**So even a fully built B0 would not deliver SC5's prescription**, because the boundary-vanishing
condition is an obligation on a *different* named generator and the correspondence is absent.

**Attribution, stated precisely:** the flag is the record's, not my discovery. **What is mine is the
routing observation** — that this non-identity sits *on the SC5 path*, at its far end, and that
845's route summary (*"licensed but uninhabited"*) locates the blockage only at B0. The path has
**two** independent absences, not one.

### 2.3 WHAT THE ROUTE DOES YIELD

Not nothing. **The route names the SHAPE of the prescription SC5 needs.** SC5 asked: at `Omega_c`'s
closure face, is the variation fixed, or free with a boundary term? The record's answer-type is
displayed above: **the global Dirac boundary form VANISHES under the preparation/gluing variations.**

```text
YIELD:   a TARGET SPECIFICATION for SC5 — the boundary contribution vanishes under the CTP
         preparation/gluing variations, on the complete Boundary-Resolved generator.
NOT:     the prescription itself, and not for X_prim's cells.
LACKS:   (i) an inhabited B0 (link 0, two TYPE-U flags, no named prerequisite);
         (ii) the B0 <-> Boundary-Resolved-generator correspondence (link 3, TYPE-U);
         (iii) any statement descending the condition to per-cell Omega_c closure faces —
               the condition as stated is GLOBAL ("the global Dirac boundary form"), and X_prim's
               requirement is PER-CELL.  No sealed descent from the global form to cell faces exists,
               and I do not supply one.
```

**(iii) is a third lack and it is mine to report:** even with (i) and (ii) discharged, a *global*
boundary-form condition is not yet a *per-cell* closure-face prescription. Bridging global to
per-cell is a further derivation, unnamed of record.

---

## 3. SC5 RE-TYPED — a refinement of my own 844

At 844 I typed SC5 as a **freedom** and stopped on it under the charter's stop-on-freedom law. On the
corrected graph that typing is wrong, and the correction matters for what round two does:

```text
844:   SC5 = A FREEDOM        (something I must not fill)
846:   SC5 = BLOCKED ON ABSENT OBJECTS  (something I cannot fill)
```

**A freedom and an absence both stop a lane, for opposite reasons.** A freedom is closed by a ruling
or by a derivation that forces it; an absence is closed only by building the missing object. SC5 has
a named shape, a licensed path, and three sealed TYPE-U flags on that path — it is an **absence**,
and no ruling can discharge it.

**SC4 remains a genuine freedom** (845: `NONE-CONFIRMED` supplier), so the two stops in the scheme are
now of *different kinds*. That distinction is the clearest thing this relay produces.

---

## 4. SC4 CANDIDATE — the tempting one, and why I decline it

The relay asks: *"If B0's certificates themselves force a regularity/function class in passing,
DISPLAY it as a candidate SC4 supplier with provenance."*

**The candidate exists and it fails.** U3's own content includes **"endpoint operator domains"** —
a function-class object, forced to descend from B0 by the common-origin constraint. That is exactly
the shape of an SC4 supplier.

**It is not one.** SC4 needs the function space of **integrands** — the domain of the variation.
U3's endpoint operator domains are **operator** domains — the receiver side. **They are objects on
opposite sides of the map.**

**I flag this as the same conflation that produced four of my six over-reaches at 840**, where the
round-1 cross-check found that every one of them *"bound an OUTPUT condition to the ACTION."* U3's
operator domains are output-side; SC4 is input-side. Adopting them would repeat that error exactly,
one relay after 845 upheld my SC4 stop.

`SC4_CANDIDATE = NONE.` The near-miss is displayed rather than suppressed, because it is the obvious
next thing a reader would reach for.

---

## 5. FREEDOMS-CONSUMED · FLATTENING CHECK

```text
CARRIED, NOT CONSUMED: B0 as a ROLE with its bar (frozen primitive-input inventory, construction rule,
  no descendant/discharge flag as input) — NOT inhabited, NOT reconstructed backwards; C0 and U3 as
  sealed; the boundary-form condition CARRIED ON ITS OWN GENERATOR, not transferred to B0; the
  corrected 845 verdicts whole.
NOT CONSUMED, EXPRESSLY: P1 — refuted at its advertised consequence and used nowhere.
STOPPED / BLOCKED, NOT FILLED: SC4 (a FREEDOM — no function class chosen); SC5 (an ABSENCE — no
  boundary prescription written, no global-to-per-cell descent supplied, no B0-correspondence
  asserted).
DECLINED: U3's endpoint operator domains as an SC4 supplier (input/output conflation — the 840 error).
DERIVED HERE: nothing.  A route walk with three blockers pinned and one re-typing.
SCALING WEIGHTS (law 2a): NONE CONSUMED — no weight, normalization or measure is fixed anywhere.
SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37 rows walked.**
**S03 / the void condition — LIVE at three places and declined at all three:** writing a per-cell
boundary prescription; asserting the B0↔generator correspondence; descending the global boundary form
to cell faces. Each would let the construction proceed and none is forced.
**S12** — the three TYPE-U flags are carried as statuses **naming absent objects**, never as the
objects. **S26 / S08** — the Dirac boundary form is carried as the record's condition on its own
generator; no smooth or EM reading attached. **S19 / S24** — nothing imported to bridge global to
per-cell. **T5** — untouched; no action substituted.
`FLATTENING_CHECK = clean (37/37; S03 and the void condition live at three declined moves).`

## 6. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) MY 844 PRINCIPAL FINDING IS WRONG AND I ADOPT THE CORRECTION WITHOUT DEFENCE.** I claimed both
loops close through the complete action. They do not: the action and the U3 boundary data are
**sibling descendants of B0**. The error was mine and it was the headline of that relay.

**(2) THE VERIFIER DID THE CITATION WORK I FLAGGED AND DECLINED TO DO.** At 844 I noted the SC5
citation was second-hand and said *"a verifier should resolve it against the BID gate directly."*
845 did, and the resolution went against my reading. **Flagging a weak citation is worth less than
resolving it**, and this is the second time this session that an item I flagged rather than fixed
turned out to carry the error (the `w(s)` line at 840 being the first).

**(3) MY ONE DERIVED POSITIVE FROM 844 IS REFUTED.** P1's algebra survives; its consequence does not.
I stated it at true size and flagged it for attack, which is the right handling — but the round it
came from now has **no surviving positive content**, and I record that rather than letting the
algebra's survival stand in for the claim's.

**(4) WHAT I ADD HERE IS SMALL AND I WILL NOT INFLATE IT.** Three things: the routing observation
that link 3's non-identity sits on the SC5 path (the flag is the record's, the placement is mine);
the third lack, that a *global* boundary form is not a *per-cell* prescription; and the
freedom-versus-absence re-typing of SC5. **None is a construction, and the relay asked for one.**

---

```
BOUNDARY_DATA = BLOCKED (obstruction displayed) — three sealed TYPE-U links on the licensed route
  LINK 0  COMPLETE_MICROSCOPIC_BOUNDARY_OPERATOR_derived = false | TYPE-U, and
          B0_CONSTRUCTION_WITNESS_derived = false | TYPE-U, whose would-build records that
          "the present bounded corpus names no exact prerequisite".  B0 IS A ROLE AND IT IS
          UNINHABITED, and its bar forbids reconstructing it backwards — the construction rule may
          not receive "any descendant or discharge flag as an input".  THE BUILD CANNOT BEGIN.
          [ec848d293d0d8241, [2367,2907) sha 69ad26da99b2479b]
  LINK 3  B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false | TYPE-U.  THE CONDITION
          THAT WOULD SERVE AS SC5's PRESCRIPTION DOES NOT LIVE ON B0: "The complete Boundary-Resolved
          generator must ... make the global Dirac boundary form vanish under the CTP
          preparation/gluing variations", and the source "does not equate its complete
          Boundary-Resolved generator with package B0 ... their bearing on B0 requires a separately
          derived correspondence."  SO EVEN A FULLY BUILT B0 WOULD NOT DELIVER THE PRESCRIPTION.
          [[31771,32311) sha a7e4dde61c9802cf ; [32029,32569) sha d4b495520a3d89e4]
          ATTRIBUTION: the flag is the RECORD'S; the ROUTING OBSERVATION — that this non-identity
          sits ON the SC5 path at its far end, where 845's summary locates the blockage only at B0 —
          IS MINE.  The path has TWO independent absences, not one.
  WHAT THE ROUTE YIELDS: not nothing — A TARGET SPECIFICATION for SC5.  The record names the SHAPE of
          the prescription: the boundary contribution VANISHES under the CTP preparation/gluing
          variations.  SC5 asked whether the variation is fixed at the closure face; the record's
          answer-type is displayed, on the wrong generator and at the wrong scale.
  THIRD LACK, MINE TO REPORT: the condition as stated is GLOBAL ("the global Dirac boundary form")
          and X_prim's requirement is PER-CELL.  No sealed descent from the global form to cell
          closure faces exists.  Even with links 0 and 3 discharged, a further derivation is needed
          and it is unnamed of record.  I DO NOT SUPPLY IT.

SC5 = STILL-STOPPED — AND RE-TYPED, which is this relay's clearest product
  844 typed SC5 A FREEDOM (something I must not fill).  On the corrected graph it is BLOCKED ON
  ABSENT OBJECTS (something I cannot fill).  A FREEDOM IS CLOSED BY A RULING OR A FORCING DERIVATION;
  AN ABSENCE IS CLOSED ONLY BY BUILDING THE MISSING OBJECT — so NO RULING CAN DISCHARGE SC5.
  SC4 remains a genuine freedom (845: NONE-CONFIRMED).  THE SCHEME'S TWO STOPS ARE NOW OF DIFFERENT
  KINDS, and that governs what round two can even attempt on each.

SC4_CANDIDATE = NONE (near-miss displayed, not suppressed)
  The candidate exists: U3's content includes "endpoint operator domains", a function-class object
  forced to descend from B0 by the common-origin constraint — exactly the shape of an SC4 supplier.
  IT FAILS: SC4 needs the function space of INTEGRANDS (the variation's domain); U3's are OPERATOR
  domains (the receiver side).  OPPOSITE SIDES OF THE MAP.  Adopting them would repeat EXACTLY the
  conflation the round-1 cross-check found in four of my six 840 over-reaches — each "bound an OUTPUT
  condition to the ACTION" — one relay after 845 upheld my SC4 stop.  DECLINED AND DISPLAYED.

H1_SYSTEM = STILL-GATED, on SC4 and SC5, now of different kinds
  SC4: a freedom with NO SUPPLIER (845's 2,030-file / 621-candidate enumeration).
  SC5: an ABSENCE with a LICENSED BUT UNINHABITED PATH carrying three TYPE-U flags, plus an unnamed
       global-to-per-cell descent.
  NOT POSEABLE-MOD-SC4: 846 does not close SC5, so the H1 system remains gated on BOTH.  What moved
  is the TYPE of the SC5 gate and the count of its blockers — from "one named supplier" to "two
  independent absences plus a scale gap".

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) MY 844 PRINCIPAL FINDING IS WRONG AND I ADOPT THE CORRECTION WITHOUT DEFENCE — the action and
      the U3 boundary data are SIBLING DESCENDANTS of B0, not the same object; the second circularity
      was the headline of that relay and it does not hold.
  (2) THE VERIFIER DID THE CITATION WORK I FLAGGED AND DECLINED TO DO.  At 844 I wrote that the SC5
      citation was second-hand and "a verifier should resolve it against the BID gate directly"; 845
      did, and the resolution went AGAINST my reading.  FLAGGING A WEAK CITATION IS WORTH LESS THAN
      RESOLVING IT — the second time this session an item I flagged rather than fixed carried the
      error, after the w(s) line at 840.
  (3) MY ONE DERIVED POSITIVE FROM 844 IS REFUTED.  P1's algebra survives; its consequence does not.
      That round now has NO SURVIVING POSITIVE CONTENT, and I record that rather than letting the
      algebra's survival stand in for the claim's.
  (4) WHAT I ADD HERE IS SMALL AND I WILL NOT INFLATE IT — the routing of link 3's non-identity onto
      the SC5 path, the global-versus-per-cell third lack, and the freedom-versus-absence re-typing.
      NONE IS A CONSTRUCTION, AND THE RELAY ASKED FOR ONE.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
