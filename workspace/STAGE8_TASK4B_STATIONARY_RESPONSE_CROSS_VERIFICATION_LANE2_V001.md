# STAGE8 TASK 4B: STATIONARY RESPONSE CROSS-VERIFICATION — LANE 2 V001

Date: 2026-08-03  
Task: PASTE 450 / Task 4b / adversarial cross-verification  
Lane: CODEX LANE 2  
Register head at preflight and send-time recheck: Q-368  
Custody: cross-verifier; no registration authority

```text
LEAD_RESULT = STATIONARY_RESPONSE_STRUCTURE_CONFIRMED

RESPONSE_STRUCTURE = CONFIRMED
JET_BOUNDARY = CONFIRMED_TYPE_U

FINITE_ACTIVE_REFERENCE_RETARDED_BLOCK = 0 |
  p-free | nu-free | every finite stage | every admitted local-Log probe

KNOWN_EXPLICIT_P_ENTRY = base finite restrictions through omega and kappa
N_MEMBER_P_OR_RANK_DEPENDENCE = TYPE-U | no invariance or dependence law
LOCALIZED_NOT_DECIDED = CONFIRMED_WITH_SCOPE_PRECISION

N_MEMBER_STATIONARY_JET_GENERATOR_FOUND = false
VOID_ON_DOWNSTREAM_FAILURE_TRIGGERED = false
UNTAGGED_DOWNSTREAM_RESULT = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The reviewed artifact correctly computes the maximal exact response as an
operator expression.  It does not compute a response value and does not
decide the program's p question.  The missing object is genuinely the
executable stationary jet of the already-ratified N member, not another
transport or response architecture.

---

## 0. Preflight and authority ledger

`LOCKED_PROCESS.md` was read in full.  The live questions-settled register
and its local sidecar verified, and the head was Q-368.  The target artifact
was hash-verified before it was read.

| Authority | Verified SHA-256 | Audit use |
|---|---|---|
| stationary response computation under review | `be570c182ef875b557395b62c382ee875420ac0462e2efb5774e9600f794b27a` | W1–W5 claims |
| DoR-017 decision | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | installed N member and three-layer test |
| action-comparison square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | R1–R5, Hessian cube, Schur covariance |
| derive standard | `a9b733c711a692d5eedad8ae6acb5e2829c357c2c6aa3870c1aca2c570604136` | forced quotient pullback and Q-408 placement |
| descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | physical cycle quotient |
| DoR-016 law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled CTP trace and ordered tower pair |
| Q-279 finite probe table | `c7624d88aedaa0659755b5c566121b24c2ad0c29a0060daada43a31b22a6ddfb` | direct finite recomputation |
| Q-326 stationary-family determination | `30532ecb2c08d21e28af05f1228b9c29264be99731f63c8cf5b30273bb51e7b8` | off-section counterattack |
| divergence/action draft | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | generator sweep |

```text
DOES_THE_OBJECT_EXIST = yes | ratified R5 structure and N member
IS_THE_VERSION_CURRENT = yes | Q-368
ARE_ITS_INPUTS_PRESENT = yes for structural assembly and finite checks |
  no for exact stationary-jet evaluation
PREFLIGHT = PASS
```

### 0.1 Load-bearing distinctions

```text
N-member       != finite stage N;
active reference != completed stationary point;
J_delta/R mixed block != ordered-retarded J_delta/J_c block;
symbolic nu    != the member direction or its Hessian;
abstract Gen(delta_div)=phi != an executable formula for stationary jets;
member-sensitive value != member-independent operation/certificate.
```

---

## 1. Verdict table X1–X7

| Item | Verdict | Finding |
|---|---|---|
| X1 stationary assembly | **PASS** | four total blocks, complement inverse, Schur reduction, and retarded extraction are exactly the ratified R5 construction; covariance, reality, batching, and restriction rerun cleanly |
| X2 finite stages | **PASS** | reciprocal-loop, S8-A, and general-stage shadows reproduce Q-243/Q-279 exactly |
| X3 zero theorem | **PASS** | no admitted local-Log probe or finite stage creates a common-source leg; member jets vanish on the active section |
| X4 p/nu map | **PASS WITH SCOPE PRECISION** | the only explicit p port is the base; possible member p/rank dependence is TYPE-U and, if present, propagates through the stationary solve/inverse/Schur exactly as the artifact records |
| X5 instantiation boundary | **PASS** | no ratified authority supplies an executable `delta_div`, `Gen`, `phi_m`, or stationary jet; abstract existence is not component evaluation |
| X6 tag ledger | **PASS** | every stationary/response claim maps to an explicit member-sensitive or member-independent ledger class; no orphan result found |
| X7 fresh attack | **PASS BY BOUNDARY** | support-rank jumps along the scaling orbit can obstruct a common inverse, but the artifact neither varies the ratified member nor claims response homogeneity/continuity in `nu` |

---

## 2. X1 — stationary system assembly recomputed

Let `m` be the N member selected by DoR-017.  The ratified action and its
complement stationarity equation are

```text
Gamma_m(p;y)=Gamma_base(p;y)+phi_m(y),

D_C Gamma_base(p;y_star,m)+D_C phi_m(y_star,m)=0.  (X1-1)
```

Define `F_AB^m=D_A D_B phi_m` at `y_star,m`.  Direct differentiation gives

```text
H_AB^m=H_AB^base(p)+F_AB^m,
AB in {CC,CK,KC,KK}.                               (X1-2)
```

On the R5 reducing complement—not on a cycle direction—the installed inverse
and Schur block are therefore

```text
I_CC^m=(H_CC^base+F_CC^m)^(-1)|_(C_red,m),

S_m=H_KK^base+F_KK^m
   -(H_KC^base+F_KC^m) I_CC^m
     (H_CK^base+F_CK^m),                          (X1-3)

Response_m=RetExtract_m(S_m).                     (X1-4)
```

This is exactly W1-3 through W1-6.  No commutation, cancellation, or scalar
evaluation has been added.

### 2.1 Downstream certificates rerun

For every admitted signed realization arrow `alpha:G->G'`, V004 proves

```text
H_AB,(G')^m alpha_B=alpha_H,A H_AB,G^m,

rho_H,N,(G') alpha_H,A
 =alpha_H,A,N rho_H,N,G,

Schur_(G')^m alpha_K=alpha_H,K Schur_G^m.          (X1-5)
```

Uniqueness of the two-sided inverse on the transported reducing support
gives the inverse line.  Substitution gives the Schur line.  The sealed
Keldysh rotation then gives retarded covariance.  Under orientation reversal
the same equations are semilinear: cycle indices carry their signs and block
coefficients are conjugated.  Thus covariance and reality pass.

DoR-016 batching remains per-system and E_post ordered.  The audited
calculation never forms a joint network amplitude or cross-system inverse,
so batching passes.  Finally, V004's completed Hessian cube and reducing
inverse theorem give

```text
rho_H,N(S_m)
 =H_KK,N^m-H_KC,N^m (H_CC,N^m)^(-1) H_CK,N^m,     (X1-6)
```

on the finite reducing domain.  Restriction passes.  These arguments operate
on the installed N member; no response entry is evaluated.

```text
X1_COVARIANCE = PASS
X1_REALITY = PASS
X1_BATCHING = PASS
X1_RESTRICTION = PASS
X1_VOID_CANDIDATE = false
```

---

## 3. X2 — finite stages recomputed

### 3.1 Direct derivative calculation

At finite stage `N`, write

```text
Xi=i n ell_N(J_delta)-(1/2)Q_N(R),
Z=(1-p)+p exp(Xi),
omega=p exp(Xi)/Z,
kappa=omega(1-omega).
```

Because `Xi` is linear,

```text
D^2 W[h_1,h_2]
 =-i hbar kappa D Xi[h_1]D Xi[h_2].               (X2-1)
```

At the Q-279 probe reference `J=0`, `Q_N(R_eta)=eta`, put

```text
u=exp(-eta/2),
omega=p u/(1-p+p u),
kappa=p(1-p)u/(1-p+p u)^2.                        (X2-2)
```

Since

```text
D_(J_c)Xi=0,
D_(J_delta)Xi=i n ell_N,
D_R Xi=-(1/2)Q_N,
```

equation `(X2-1)` gives exactly

```text
D^2_(delta,delta)W=i hbar kappa ell_N tensor ell_N,
D^2_(delta,R)W=-(hbar n/2)kappa ell_N tensor Q_N,
D^2_(R,R)W=-(i hbar/4)kappa Q_N tensor Q_N,

D^2_(c,c)W=D^2_(c,delta)W=D^2_(delta,c)W
 =D^2_(c,R)W=D^2_(R,c)W=0.                       (X2-3)
```

This independently reproduces W2-1/W2-2 and the complete Q-279 table.  At
`R=0`, `u=1`, so `omega=p`, `kappa=p(1-p)` and Q-243 is recovered exactly.

### 3.2 Reciprocal-loop stage

DoR-016 supplies, for each system `i`,

```text
A_(i,k)^B=(1-p_i)+p_i product_(t=1)^k Z_(i,t)^CTP,

Tower_AB,k^B=((F_(1,k)^B,A_(1,k)^B),
              (F_(2,k)^B,A_(2,k)^B)).             (X2-4)
```

The network object is the ordered pair, not the product of its entries.
Applying `(X2-1)` per system, with the tier covectors summed into the stage
covector, gives the finite ordered-retarded pair `(0,0)`.  Each zero occurs
before `omega_i` or `kappa_i` can contribute.  The N-member restriction is
all-orders flat on the active section, so its active jet also contributes
zero.  W2-3 is exact; W2-4 correctly remains the unevaluated completed
operator expression.

### 3.3 S8-A and the general stage

For the S8-A exchange,

```text
c_1<->c_2,
c_3->-c_3,
S_sigma=[[0,1,0],[1,0,0],[0,0,-1]].
```

The zero retarded block maps to itself.  The nonzero noise/probe blocks in
`(X2-3)` transform by the same signed covector action, and reality reversal
adds coefficientwise conjugation.  No basis is selected.  This is precisely
the rank-two covariance check of V004.

For arbitrary finite stage, only `ell_N` and `Q_N` change.  The identity
`D_(J_c)Xi=0` remains exact, so every common-source block remains zero.
Identity extension adds unit character factors and extends the covectors by
zero; batching preserves their E_post order.  Hence the general-stage claim
is proved, not extrapolated from the two examples.

```text
X2_RECIPROCAL_LOOP = PASS
X2_S8A = PASS
X2_GENERAL_STAGE = PASS
X2_Q243_RESTRICTION = PASS
X2_Q279_RESTRICTION = PASS
```

---

## 4. X3 — adversarial attack on the finite zero theorem

Three possible routes to a nonzero finite active-reference retarded block
were tested.

1. **Change stage or batching.** This only changes `ell_N`; it never creates
   a `J_c` derivative of `Xi`.
2. **Switch on the bilocal probe.** This changes `omega`, `kappa`, and the
   `J_delta/R` block.  It does not turn `R` into `J_c`; `(X2-3)` remains zero
   throughout the admitted local Log domain.
3. **Insert the N member.** Its finite restriction is all-orders flat on the
   active section, so every first and second active jet is zero independently
   of the member's normalization.

No stage/probe counterexample exists in the licensed domain.  A Log-branch
singularity would make the derivative undefined, not nonzero, and lies
outside Q-279's admitted local Log domain.

```text
FINITE_ACTIVE_REFERENCE_RETARDED_BLOCK = 0
FINITE_ACTIVE_REFERENCE_RETARDED_BLOCK_P_CONTENT = none
FINITE_ACTIVE_REFERENCE_RETARDED_BLOCK_NU_CONTENT = none
X3 = PASS
```

This theorem does not move the completed stationary point onto the active
section and therefore does not imply a completed response zero.

---

## 5. X4 — p/nu appearance map audited in both directions

### 5.1 p localization

The only **proved explicit** p entry is the base finite family:

```text
p -> omega -> kappa -> base first/noise/probe restrictions
  -> completed base labels -> stationary equation and base blocks. (X4-1)
```

The N-member interface does not declare either p dependence or p
independence for `delta_div`, `Depth`, `Accum`, `Gen`, `phi_m`, or `nu`.
Therefore it would be wrong to promote `(X4-1)` into the theorem “the member
is p-independent.”  The reviewed artifact does not do so: its W3 table marks
the member's p/rank status unspecified and the completed response unresolved.

Conversely, more is already decidable than a completely unlocalized label:
the finite active member jets are uniformly zero, and the explicit base
route is known exactly.  Thus the uncertainty is localized to the
off-section member jet/stationary record.  If that record contains p, its
effect then spreads through `y_star,m`, `I_CC^m`, `S_m`, and the response.
W3 lists every one of those nonlinear consumers, so no hidden spread was
missed.

```text
P_EXPLICIT_ENTRY_LOCALIZED = true | base restrictions
P_MEMBER_INDEPENDENCE = NO_VERDICT / TYPE-U
P_MEMBER_DEPENDENCE = NO_VERDICT / TYPE-U
P_NONLINEAR_PROPAGATION_IF_PRESENT = fully listed
LOCALIZED_NOT_DECIDED = CONFIRMED_WITH_SCOPE_PRECISION
```

### 5.2 nu homogeneity

Along the disclosed scaling orbit,

```text
phi -> lambda phi,
D^2 phi -> lambda D^2 phi,
nu -> lambda nu.                                  (X4-2)
```

This proves degree-one homogeneity only for the direct member jets.  It does
not reconstruct their direction from `nu`, and it does not linearize the
inverse, stationary solve, Schur block, or moving evaluation point.  The
artifact states exactly this boundary.  No response proportionality to
`nu` is claimed.

```text
NU_DIRECT_JET_HOMOGENEITY = PASS
NU_DETERMINES_MEMBER_DIRECTION = false | TYPE-R as an inference
NU_DETERMINES_RESPONSE = NO_VERDICT / TYPE-U
X4 = PASS_WITH_SCOPE_PRECISION
```

---

## 6. X5 — instantiation-boundary sweep

| Candidate source of the stationary jets | What it actually supplies | Generator verdict |
|---|---|---|
| DoR-017 decision | selects the certified nonzero tuple and keeps `nu` symbolic | no executable `delta_div`, `Gen`, or jet formula |
| square V004 R1 | signature `Gen_m(delta_div,m)=phi_m`, covariance, flatness, units, normalizer | abstract role realization, not values of `D phi_m` or `D^2 phi_m` |
| square V004 R2–R5 | topology, restrictions, block placement, stationary/Schur interfaces | transports/evaluates a supplied jet; does not generate one |
| derive standard Q-358 | canonical `D_G^*` and five-item residue classification | explicitly leaves divergence member and stationary package as residues |
| divergence/action draft | failed bridge plus would-build `GeneratorAndUniquenessCertificate` | expressly says `phi_div` is not constructible from its sealed stock |
| descent V003 | prefix-family to physical-cycle quotient | information transport only, no action coefficient or stationary jet |
| DoR-016 network law | doubled trace and ordered per-system towers | no physical action-member generator |
| `Norm(phi_m)=nu` | one homogeneous scalar label | cannot reconstruct a function or its stationary Hessian |

The ratified member therefore exists extensionally as an authoritative
object but is not intensionally executable at the stationary point.  Formal
expressions such as `D^2 Gen_m(delta_div,m)` only rename the missing
operation; without an explicit `delta_div` and executable `Gen`, they do not
evaluate a component.

```text
N_MEMBER_RATIFIED = true
N_MEMBER_STATIONARY_JET_GENERATOR_FOUND = false
EXACT_COMPONENT_EVALUATION = TYPE-U
MISSING_OBJECT = N_MEMBER_STATIONARY_JET_RECORD
X5 = PASS
```

---

## 7. X6 — member-sensitivity tag audit

Every result in the reviewed computation maps to one of the following
explicit ledger classes:

| Result family | Required tag | Coverage in reviewed artifact |
|---|---|---|
| prefix traces, `D_G`, `D_G^*`, Keldysh/retarded operation | MEMBER-INDEPENDENT STRUCTURE | W1.4 and W3 |
| `Gamma_base`, Q-243/Q-279 finite base tables | MEMBER-INDEPENDENT | W1.4, W2, W3 |
| `phi_m`, its off-section derivatives | MEMBER-SENSITIVE | W1.4, W3 |
| active-section member-jet value zero | MEMBER-INDEPENDENT ZERO over the admitted flat class | W1.4, W2, W3 |
| stationary equation, `Crit_m`, `y_star,m`, `G_star,m` | MEMBER-SENSITIVE | W1.1/W1.4 |
| total Hessian blocks, reducing support, inverse instance | MEMBER-SENSITIVE | W1.2–W1.4 |
| Schur and response values | MEMBER-SENSITIVE | W1.3/W1.4/W3 |
| inverse/Schur/RetExtract operation and covariance/reality/batching/restriction certificates | MEMBER-INDEPENDENT STRUCTURE/CERTIFICATE | W1.3–W1.5 |
| finite zero theorem and permanent restriction regressions | MEMBER-INDEPENDENT result over the admitted class | W1.4 plus W2/W4 |
| exact boundary and TYPE-U evaluation standing | typing result, not a member-sensitive response value | W5 |

The reciprocal-loop pair, S8-A covariance, source-kernel zeros, Q-243/Q-279
restrictions, and all W4 regressions fall under the finite-result or
certificate rows above.  No downstream result lacks a ledger tag.  The
artifact also keeps the N/Z difference visible: replacing N by Z removes
`phi_m` and changes every object tagged member-sensitive.

```text
MEMBER_SENSITIVITY_TAGS_AUDITED = all
UNTAGGED_DOWNSTREAM_RESULT = false
N_Z_DIFFERENCE_VISIBLE = true
X6 = PASS
```

---

## 8. X7 — fresh attack: scaling-orbit support-rank jump

**Attack.**  Use the homogeneous counterfamily `phi_lambda=lambda phi_m`.
Although the direct member jets scale linearly, the total complement block

```text
H_CC(lambda)=H_CC^base+lambda F_CC^m
```

can change kernel or reducing-support rank at exceptional symbolic values of
`lambda`.  Then a single inverse domain need not persist across the full
scaling orbit.  If the reviewed artifact inferred response homogeneity or
continuity from `(W3-1)`, its stationary structure would fail.

**Result.**  The attack does not land.  DoR-017 adopts one certified nonzero
member, not the whole scaling orbit as a common stationary family.  The
artifact uses scaling only to show that `nu` does not fix the response.  It
explicitly denies degree-one behavior for the inverse, stationary solve,
Schur block, and response.  For the installed member, the R5 inverse acts
only on its own declared reducing support.  No cross-`lambda` inverse or
continuity statement is consumed.

```text
FRESH_ATTACK = scaling_orbit_support_rank_jump
COMMON_INVERSE_ACROSS_SCALING_ORBIT_CLAIMED = false
ATTACK_RESULT = PASS_BY_BOUNDARY
X7 = PASS
```

---

## 9. Final determination

```text
X1_STATIONARY_ASSEMBLY = PASS
X2_FINITE_STAGES = PASS
X3_ZERO_THEOREM = PASS
X4_P_NU_APPEARANCE_MAP = PASS_WITH_SCOPE_PRECISION
X5_INSTANTIATION_BOUNDARY = PASS
X6_TAG_LEDGER = PASS
X7_FRESH_ATTACK = PASS_BY_BOUNDARY

RESPONSE_STRUCTURE = CONFIRMED
JET_BOUNDARY = CONFIRMED_TYPE_U

P_VERDICT_COMPUTED = false
RESPONSE_VALUE_COMPUTED = false
VOID_CANDIDATE = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No alpha, `K_*`, root, numerical response value, rank ratio, or measured
constant was evaluated.  No p verdict was issued.  No register, plan,
tracker, git, commit, or push action was performed.
