# STAGE 8 — REFUTING-BRANCH ADJUDICATION — ADVERSARIAL AUDIT V001

## AUDITOR — CODENAME RFB-AUDIT — COMMISSION S9AD

Date: 2026-08-14 (UTC 2026-08-15). Role: adversarial auditor, NOT told the
outcome, default REFUTE; this verdict governs. Target:
STAGE8_REFUTING_BRANCH_S9AD_V001.md (seal 48ecdabe... VERIFIED at path
against sidecar, 45,588 bytes, before any content reliance; content opened
only AFTER the pre-disposition file below was hashed).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. No git action. ONE output (this artifact)
+ seal sidecar at the commission-distinct path, both probed ABSENT at
2026-08-15T04:25:45Z before write.

---

## 0. VERDICT

**CONFIRMED-WITH-CORRECTIONS.**

**All four dispositions stand under attack, and none overreads the
one-directional binding. Obligation 1's REFUTED-WITH-IT carries its OWN
two-grade chain at bytes (52f2490b: no admissible uniform M; 9fdc3d1c/549362d4:
p = -3, coincidence-vanishing certification dead at the polydisc-sup
quantifier) — no binding converse is used anywhere. Obligation 2 (T11 response
half) takes RE-POSES, not REFUTED-WITH-IT — correct: every sealed pass-route is
verified dead at bytes while naturality itself stays not-proved/not-refuted
(6ca465a6 N4/N7; dcf29eea :211-222; 5fbae703 :140). Obligation 3's split —
weight side UNTOUCHED / response side route-dead per-pair / SURVIVES-VIA the
summed quantifier with the bridge BARRED and residues r-A1/D-i displayed — is
byte-grounded (0f3082ca :90-91, §5; e5b0879b :38, :285-286, :309-362; cbfbb74c
item 5). Obligation 4 is NOT-REFUTED-WITH-IT with the truth-value transfer
blocked at bytes (3de0502c NS-1..NS-5; 58f89435 recorded-DECLINED via 3de0502c
:409-410; fdc09c07 via NS-5) — exactly the binding's own refuting-branch
sentence, which is attribution, not falsity. Every consumption of p = -3 in the
build is at the audited polydisc-sup grade (549362d4 C1/CONSUMER_NOTE); the
pointwise form is used nowhere — checked line by line. NO 5d verdict content:
the FORCED/FREE/RUNS sweep returns only fence text, non-support clauses,
sealed-label quotes ("the forced per-pair 1/2" is e5b0879b :38's own label),
and choice-ledger provenance tags; the T-A "antecedent REACHED" sentence is
consequence-mapping of the sealed conditional 3de0502c :523-526 (verified
verbatim), route-scoped, with the consequent's RFA-grade content correctly NOT
consumed and the non-support clause carried at the point of maximum risk;
Q-1089 is neither consumed nor extended. The §6 charter display is verbatim
against 0f3082ca §4 :130-146 (compared word-for-word at bytes) and STAGES
NOTHING. 19/19 seals recomputed and matched; every load-bearing quoted span
re-read at path; the CAS battery re-ran verbatim in a fresh venv, 11/11 PASS,
output BYTE-IDENTICAL. Three corrections below; none disturbs any
disposition.**

---

## 1. INDEPENDENCE PROTOCOL — DISPOSITIONS FIXED BEFORE OPENING

```text
Pre-disposition file: scratchpad RFB_AUDIT_PREDISPOSITIONS.md
  sha256 = 328ad0e2c572a4d4058fa8323b5f94a7088fea31d16bf49a379ca1753226160c
  fixed 2026-08-15T04:19:08Z, BEFORE the build was opened (its seal verified
  by sidecar recomputation only; no content read until after this timestamp).
Worked from the sealed corpus alone: the binding at 0f3082ca :86-100; the two
  refutation grades at 52f2490b and 9fdc3d1c+549362d4 (audited C1 grade); the
  sharpened target at d1807b96; RUNS-vs-CLOSES UNDETERMINED at 5801681a.
MY FOUR PRE-FIXED DISPOSITIONS vs THE BUILD'S:
  OB1: MATCH (refuted as written, own chain, both grades; successor = new
       pose; MO-3 only via non-coincidence routes).
  OB2: MATCH (not refuted; undischarged/re-poses; no byte chain to falsity).
  OB3: MATCH ON CONTENT, build finer-grained: I pre-fixed
       SURVIVES-AS-OPEN-UNDISCHARGED + weight side untouched; the build adds
       the route-scoped per-pair REFUTED-WITH-IT and the summed
       SURVIVES-VIA. VERIFIED at bytes and ACCEPTED: the per-pair route
       death is own-chain (0f3082ca §5 :171-176 "alpha IS SET BY THE
       CANCELLATION RATE" + p = -3 at sup), and the summed delivery with
       barred bridge + named residues is of record (e5b0879b; V-2 checked).
  OB4: MATCH (not refuted; unresolved at the named failure; transfer
       blocked; contingency stays the principal's).
My kill conditions K1-K6: NONE fires. K1 (binding converse) — clear on all
  four. K2 (quantifier overclaim) — clear; CL-4 enforced throughout. K3 (5d
  lean / Q-1089 extension) — clear. K4 (charter staging) — clear (but see
  C2). K5 (seals/bytes/floats/frozen value) — clear. K6 (weight-side
  disturbance / extensivity-false / unnamed survivor route) — clear.
```

## 2. WHAT I VERIFIED MYSELF (auditor's own work, not the build's)

```text
SEALS: 19/19 of the build's §1 table recomputed at path THIS session — all
  match sidecars and the build's 8-hex prefixes. Build seal 48ecdabe matches
  its sidecar.
BYTES (each span re-read at path this session): 0f3082ca :86-100 (binding),
  :130-146 (§4 contingency, compared word-for-word against the build's §6
  display — VERBATIM), :171-179 (§5 cancellation-rate), :209 (extensivity
  flag REQUIRED_NOT_DERIVED_NOT_CONTRADICTED); 52f2490b :150-166 (§4 four
  discharges + the named gap) + flag :196 (UNSTATED_IN_CORPUS); 9fdc3d1c §0,
  :355-362, :375, :396, :416-419, :738-746; 549362d4 §0, C1-C3, :238-241
  (CONSUMER_NOTE — the build's consumption rule is its verbatim content);
  d1807b96 full; f6fe4072 full; 3de0502c :38-43, :122-145, :181-186,
  :219-304 (NS-1..NS-5; Chain 1 with 0a10c030 :348-349), :245-246 (the
  2ede02ae FULLY-CANCELS display — so that citation IS carried second-hand
  by a table-listed sealed artifact), :409-410, :515-540 (T-A conditional
  verbatim); 2ede02ae :118-131 at bytes directly (matches; hash recomputed);
  6ca465a6 N4/N6/N7; dcf29eea :205-225; 5fbae703 :140; 430f0971 §7.3 flags;
  bebc0f08 :340-346, :478-487, :813-815, s-6 (:395); 80db260f §0 ("the F1
  stop IS MO-3"; MO-4 absence); e5b0879b :38-72, :238, :264-331, :361-362,
  :392-425 (W-1..W-3, witness); cbfbb74c :34, :48-50.
CAS: battery extracted verbatim from §8, run in a FRESH venv (rfbauditvenv,
  sympy 1.14.0): 11/11 PASS, output diffed against the build's §8 display —
  BYTE-IDENTICAL. Exact symbolic only; no floats as ground; no value frozen.
SWEEPS OF THE BUILD: FORCED/FREE/RUNS occurrence-by-occurrence (all benign,
  §0); float sweep (no numeric ground); "p = -3" corpus carrier sweep
  (matches the build's claim: MO3 pair + build only); refuting-branch
  carrier sweep (build's claim FALSIFIED as "exactly" — correction C1).
```

## 3. THE FIVE COMMISSIONED ATTACKS — RESULTS

```text
A1 BINDING-CONVERSE OVERREAD: NONE FOUND. OB4 and OB2 explicitly decline
   REFUTED-WITH-IT; OB1's is the branch's own subject (own chain, both
   grades); OB3's is route-scoped with its own chain and inline scope. The
   build's §3.4 blocking of the truth-value transfer is STRONGER than my
   pre-fixed quantifier argument and is byte-grounded (NS-1..NS-5,
   recorded-DECLINED, one-way A-B).
A2 SURVIVES-AT-WRONG-QUANTIFIER: NONE FOUND. OB3's SURVIVES-VIA names the
   summed quantifier, the similarity-class scope, the R-L0/D-i residues,
   and the V-2 barred bridge in the same breath (e5b0879b :316 itself ties
   the D3 quantifier to the non-congruent remainder — verified). OB4's
   three routes carry their quantifiers inline and none is claimed to
   close. OB1's re-pose demands the successor state its quantifier.
A3 5D-VERDICT LEAKAGE: NONE FOUND (sweep displayed §2; T-A adjudication is
   consequence-mapping of a verified sealed conditional, scoped to the
   coincidence route, consequent's RFA-grade content not consumed; T-B
   correctly NOT FIRED — extensivity is unresolved, not failed).
A4 CHARTER-DISPLAY STAGING: NOTHING STAGED. The §6 carrier quote is
   verbatim at bytes. One header-scope overstatement on c-4 — correction
   C2; it stages nothing either way.
A5 SEALS/BYTES/SWEEPS: 19/19 seals OK; quoted spans verified; CAS
   byte-identical; ONE sweep-exhaustiveness sentence falsified (C1); one
   audit-table omission (C3); CL-5's disclosed filename-level deviation
   ACCEPTED (every T11 statement independently sourced and verified here).
```

## 4. CORRECTIONS (the "WITH-CORRECTIONS")

```text
C1 (sweep exhaustiveness — the recurring slip genus). §9's "The
   refuting-branch carrier sweep found exactly: 52f2490b, f6fe4072,
   3de0502c, 430f0971" is FALSIFIED at bytes: two further carriers exist —
   STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md :157 (flags the
   same binding gap "REMAINS UNSTATED in sealed" — CORROBORATES the build's
   premise) and STAGE8_RATE_GAP_LINKAGE_S9AD_AUDIT_V001.md :174 (a
   verification line). "Exactly" must be weakened to the displayed search
   set. No disposition is disturbed; the missed substantive carrier argues
   FOR the build's premise, not against it.
C2 (§6 c-4 header scope). The header "WHAT THE NEW PRINCIPLE CHARTER WOULD
   HAVE TO CONTAIN — DESCRIPTIVELY, AS THE RECORD STATES IT" overstates for
   c-4: the F1-F5 terms attach at bytes to the HESSIAN-FIRST fallback
   (0f3082ca §3), and no sealed sentence states the running-kappa_record
   charter must contain that pattern. c-4's own text discloses the
   attachment ("attaching to THAT fallback"), so nothing is staged; but
   under that header c-4 reads as charter content of record. Read c-1..c-3
   as carrier content, c-4 as neighbor-fallback display only.
C3 (grade bookkeeping). 3de0502c is consumed heavily "at audited grade" but
   its audit artifact is absent from the §1 seal table (bebc0f08's audit
   72c95d42 IS listed). I verified the linkage audit's relevant content at
   bytes myself (:174; its NS verifications); table omission only, no
   content defect.
None of C1-C3 touches any of the four dispositions, the consumer map, the
r-3/MO-2+MO-4 interaction statement, the summed-adoption V-1..V-6 check, or
the trigger split. The verdict is CONFIRMED-WITH-CORRECTIONS, not CONFIRMED,
solely on C1-C3.
```

## 5. CHOICE LEDGER, TOY_SEPARATION, SWEEP CUTOFF (auditor's own)

```text
CHOICE LEDGER:
 A-1 Re-ran the build's §8 battery VERBATIM in a fresh venv rather than
     rewriting it — blast radius: none (byte-identical output required and
     got).
 A-2 Accepted the build's OB3 refinement over my coarser pre-fixed D3 ONLY
     after independent byte-verification of e5b0879b/cbfbb74c/0f3082ca §5 —
     the acceptance is verification-purchased, not deference (the pre-fixed
     content is a strict subset of the build's; no contradiction).
 A-3 Treated 549362d4's C1/CONSUMER_NOTE as the binding consumption grade
     for p = -3 (it is the sealed audit of the producing build) — same
     grade the build assigned (its CL-4).
 A-4 Did not attempt an independent adjudication of whether the corpus flag
     extensivity_binding_on_refuting_branch should flip: the build
     correctly leaves that to the registrar; out of my commission.
 A-5 Second-hand citations (0a10c030, df4514a5, 58f89435, fdc09c07,
     5cdd5daf, 2ede02ae, 29084cd2, 218be86d) were checked FOR PRESENCE IN
     the table-listed sealed carriers that quote them; 2ede02ae was
     additionally opened directly at bytes (hash recomputed 2ede02ae) as
     the one case where specific content ("FULLY CANCELS") was
     load-bearing — it is also carried verbatim at 3de0502c :245-246, so
     the build's table discipline holds there.
TOY_SEPARATION: CLEAN — no toy family or model instance used by me; every
  audit claim is a byte re-read of sealed artifacts at path, a hash
  recomputation, or the verbatim CAS re-run.
SWEEP CUTOFF (declared): ROOTS swept = alpha-program-archive/{workspace
  (content-swept via targeted greps + full listing), relay_inbox/outbox
  (listings), supervision (listing + one bounded grep)} and
  alpha_supervision (listing). Searches: "REFUTING|S9AD|CHARTER|T11|
  response half|connected extensivity|RUNS|p = -3|refuting branch|FULLY|
  NS-|label", sidecar-hash recomputations for all 19 + 2ede02ae + the two
  C1 carriers, and the byte-span reads of §2. NOT swept: full corpus;
  cleanroom_output. FENCED CLASSES: no register/road_/ledger/lens/plan/
  tracker file opened for verdict content; "Q-..." tokens
  EXPECTED-UNLOCATABLE, not chased. DISCLOSURE (pre-fix, on the record in
  the hashed pre-disposition file): during orientation and BEFORE fixing
  dispositions I read CONTINUATION_STATE.md and THE_HANDOFF_2026-08-15
  (supervision instruments); both were written BEFORE the build landed
  (22:21 vs 23:13 file times) and contain no refuting-branch adjudication
  outcome; independence with respect to the build's content is intact; the
  commission itself supplied the only Q-1089 fact used (registered
  RUNS-support exists; not extended here). CUTOFF: sweeps after
  2026-08-15T04:19:08Z (the pre-fix hash time) were verification-targeted
  only; enumeration claims are limited to these displayed sweeps.
```

## 6. FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS(target =
  STAGE8_REFUTING_BRANCH_S9AD_V001.md, seal 48ecdabe verified at path;
  corrections C1 (sweep "exactly" falsified — two additional
  refuting-branch carriers, one corroborating), C2 (§6 c-4 header scope —
  F1-F5 attach to the Hessian-first fallback, not the running-kappa_record
  charter), C3 (3de0502c's audit absent from the seal table; content
  verified here). ALL FOUR DISPOSITIONS CONFIRMED AS LABELED; consumer map
  confirmed at the checked spans; no binding-converse overread; no
  quantifier overclaim (polydisc-sup only, everywhere); no 5d verdict
  content; charter display verbatim and unstaged; CAS 11/11 byte-identical.)
INDEPENDENCE = PRE-FIXED(sha256 328ad0e2..., 2026-08-15T04:19:08Z, before
  target opened; four dispositions matched — OB3 at finer grain,
  verification-accepted; kill list K1-K6 all clear.)
MACHINERY_INVOKED = yes(shasum -a 256; byte-span reads; fresh-venv CAS
  re-run sympy 1.14.0, 11/11 PASS byte-identical. NOTHING numeric as
  ground: no value of alpha (coupling), kappa_record, n, or any physical
  magnitude computed, bounded, estimated, or evaluated.)
VERDICT_CONTENT_FOR_5D = NONE(this audit maps nothing to FORCED/FREE/RUNS;
  the registered RUNS-support of Q-1089 is not consumed, not extended;
  RUNS-vs-CLOSES (5801681a) stands UNDETERMINED of record.)
FORBIDDEN_IMPORTS = none(no scale, imported GR, or faithfulness premise as
  authority; fenced-class opens for verdict content: none; orientation
  disclosure carried in §5 and in the hashed pre-fix; no git; output +
  sidecar probed ABSENT before write; sweep cutoff declared.)

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
production_authorized = false
```
