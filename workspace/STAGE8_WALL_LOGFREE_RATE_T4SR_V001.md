# STAGE 8 — THE WALL'S LAST SUMMED-LEVEL NUMBER: ONE LOG-FREE RATE FOR THE DEGREE-3 DIAGONAL CLASS OF THE SUMMED ASSEMBLY — T4SR V001

## BLIND BUILDER — CODENAME RATE-BUILD — COMMISSION T4SR — [SEALED]

Date: 2026-08-15 (session CDT early)
Role: BLIND builder (RATE-BUILD). Commission T4SR: certify ONE LOG-FREE
RATE for the degree-3 diagonal class of the summed assembly — the wall's
single remaining summed-level number (tr K^Sigma_n, the even trace at the
summed quantifier; the other three of the wall's four are decided of
record). Verdict menu, none preferred: RATE-CERTIFIED (the log-free rate
displayed with its proof whole — the log ELIMINATED, not estimated away)
/ UNDECIDABLE-TODAY (the blockers exact, swept; the NEW sealed object
that would decide, named). Routes named of record, each attempted and
displayed: assembly cancellation; region cumulants; the Gram transfer
(the profile-split factorization, derivation seal-verified before
consuming).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.
DETERMINATION ONLY — no adoption, no authored physics, no member binding.

Fences held: EXACT SYMBOLIC ONLY — one CAS battery (sympy 1.14.0, fresh
venv `t4srvenv` under the session scratchpad), reproduced verbatim in §7
with output verbatim (8/8 PASS on a single run; NO pre-final
corrections); every constant symbolic or an exact rational/surd; NOTHING
numeric evaluated (Lambda, ell, n, M, eps, C_G, c_G FORMAL throughout —
the record's standing KF1 license); no floats as ground; no measured
constant; no value frozen. Scoped reads of the FIVE named artifacts only;
no file matching register|road_|ledger|lens|plan|tracker|THE_HANDOFF
opened (fenced classes and all other files matched by FILENAME ONLY in
the sweep, never opened). "Q-..." tokens EXPECTED-UNLOCATABLE per the
tasking; noted, never chased, nothing defaulted. No git action; no
existing file edited; ONE output (this artifact) plus its seal sidecar at
the commission-distinct path, probed ABSENT at session start AND
re-probed ABSENT immediately before write. Every enumeration sentence
below claims only its own displayed sweep. SWEEP CUTOFF DECLARED (§6).
SCOPE GUARD held: the per-composite quantifier is UNTOUCHED of record —
all work at the SUMMED quantifier only; V-2 not crossed in either
direction (§5). Marks: DERIVED / CLAIMED / GROUNDED(record cited at
grade) / CONDITIONAL(premise named) span by span.

Homonym discipline, inherited and declared: n = the CARRIER index of the
family of record; k = the momentum variable inside |k| and khat
(contextual, inherited from the sealed bytes); o, c = the REM strata
indices (oscillation order, coincidence count) as quoted at bytes in the
primary ground.

---

## 0. VERDICT IN ONE LINE

**UNDECIDABLE-TODAY — no log-free rate for the degree-3 diagonal class of the summed assembly is certifiable on the five named sealed artifacts, and the blocker is now ONE exactly-named missing object, reduced to a single premise by a new identity-grade deliverable: THE ONE-VERTEX TRACE-PARITY LEMMA (DERIVED, CAS T1-T3): the summed assembly's [a^1] slot carries EXACTLY ONE J vertex — one alpha_x — per stratum (U(0) is J-free, F carries one J, at the sealed displays), the sealed generator's ONLY other spinor letters are alpha.khat (h_0, band projectors) and S (the chiral vertex; spatial profiles are scalars tensor 1_spinor of record), and EVERY 4x4 Dirac trace tr[alpha_x W] over words W in {alpha.khat, alpha.khat', S} is ODD-or-zero under the joint inversion (khat, khat') -> (-khat, -khat') — verified exhaustively at the displayed sweep (all 63 single-momentum words to length 5, all 121 two-momentum words to length 4): the spinor factor of EVERY diagonal stratum of tr K^Sigma at EVERY degree is joint-odd, so the commissioned log-free rate holds IFF every degree-3 SCALAR amplitude is khat-EVEN — one exact angular kill per stratum (T4), landing the trace count at degree <= -4 = carrier integral 1 - 1/Lambda, BOUNDED, the log ELIMINATED (T5). THAT PREMISE IS UNDECIDABLE ON TODAY'S SEALED BASIS, three blockers exact: (B-1) the degree-3 angular census is ABSENT — the o = 3 segment-corrected subsidiaries' angular symbols are displayed NOWHERE in the five named artifacts (only the V_2 subsidiary is quoted at bytes: -i V_2(x)/(2|k|), angular-scalar), and the primary audit's own AC-4 marks stratum-by-stratum parity at degree -3 as a claim made by NEITHER build NOR audit; (B-2) the c-bearing degree-3 classes carry the sealed khat-DEPENDENT ray averages (the delta + CZ jet coefficients beta_s(x, khat), at bytes via the Gram-transfer artifact), whose displayed traced combination khat_x(b_+ - b_-) is EVEN under the joint swap (khat -> -khat, b_+ <-> b_-) — parity supplies NO kill there (CAS T6, the o-2-OBS grammar re-instantiated: odd amplitude x odd trace = even integrand, kill refused), and NO first-angular-moment certificate for the ray averages exists in the sealed scope; (B-3) every trace-side budget reaching the summed object is POLYNOMIAL and therefore estimates rather than eliminates — of record 8n^3 (rank x op) and 12 sqrt2 n^{7/2}/ell (FHB); NEW HERE, delivered meanwhile at their own grades, the two best caps yet, BOTH at the same sub-volume power and BOTH refused as certification: the GRAM TRANSFER assembles at the summed quantifier by the direct chain ||K^Sigma_n||_1 <= 2||F||_1 <= 2 G_n <= 2 C_G n^{3/2} (the profile split applied INSIDE the summed F; unitaries + l1-tight weights + Bochner triangle + ||J(t)||_1 = 4 g_n(t), CAS T7; CONDITIONAL(CL-A) at the MO4 artifact's CLAIMED grade), hence |tr K^Sigma_n| <= 2 C_G n^{3/2} — and the split's budget is SHARP of record (c_G n^{3/2} <= G_n): no improvement in n exists inside the Gram route, and c_G n^{3/2}/log Lambda -> oo — the transfer's own floor infinitely exceeds the log scale, the route can NEVER certify log-free; and the RANK x HS cap |tr K^Sigma_n| <= sqrt(4n^3) ||K^Sigma_n||_2 <= 2 n^{3/2} M/eps (Lagrange/Cauchy-Schwarz + the sealed carrier-free HS enclosure, CAS T8; grade inherited) — same power, independent route, same refusal. REGION CUMULANTS: EMPTY — zero occurrences of any cumulant or region-cumulant object in the five named artifacts (sweep displayed §6); constructing one is authored physics, fenced. THE NEW SEALED OBJECT THAT WOULD DECIDE, named exactly (§4): THE DEGREE-3 ANGULAR CENSUS of the summed assembly — a sealed display, one order deeper than the record's V_2 (x) V_2 display, of EVERY degree-3 diagonal stratum (the o = 3 segment-corrected subsidiaries AND the c-bearing coincidence classes) with each scalar amplitude's joint khat-parity and exact first angular moments: the ALL-EVEN branch combines with the one-vertex lemma delivered here to yield the log-free rate MECHANICALLY (class trace = 0 exactly; count at degree <= -4; carrier integral 1 - 1/Lambda) — RATE-CERTIFIED; any stratum with a nonzero odd moment makes the log GENUINE there — the rate refuted; EITHER branch decides the wall's last summed number. NOT REFUTED: no divergence of tr K^Sigma_n is exhibited; the quantity stays UNDECIDED with its logarithmic shortfall quantified of record. The per-composite wall, T_n, o-2-OBS, B-L2*, MO-2/MO-3, and every sealed identity: UNTOUCHED at their own quantifiers; V-2 not crossed. CAS 8/8 PASS, single run.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest via
`shasum -a 256 -c` against its `.seal.sha256` sidecar. 5/5 MATCH (OK);
NONE UNVERIFIABLE. Scoped reads: these five files and NO others.

```text
STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md        OK  [WSR — primary: K^Sigma defined; three of four decided; tr K^Sigma FAILS-AT log; the residue = THIS commission; WS1-WS9]
STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md  OK  [WSR-AUDIT — CONFIRMED-WITH-CORRECTIONS; COR-1; A3 spinor-trace kill; AC-4 (no degree -3 parity claim); a-2 (MO4 adjacent, unconsumed)]
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md      OK  [WBC — ground: op-grade enclosure <= 2; rank <= 4n^3; F1 = rank x op stop; CS-1..CS-9 typing]
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md OK [WBC-AUDIT — CONFIRMED-WITH-CORRECTIONS; COR-1 sign fix; F1/F3 each sufficient]
STAGE8_MO4_CANCELLATION_S9AD_V001.md           OK  [MO4 — the Gram transfer: profile split J = -[(Q b^{1/2}) (x) 1][(b^{1/2} Q) (x) alpha_x]; g_n, G_n; c_G n^{3/2} <= G_n <= C_G n^{3/2} (CL-A); jet amplitude khat_x[(beta_+ - beta_-) I + (beta_+ + beta_-) alpha.khat] at bytes; CLAIMED grade, no audit in scope]
```

Load-bearing spans consumed (all read IN FULL this session): WSR §0, §2
(K^Sigma := [a^1] X_n(a,a) = C(U(0)^dag F + F^dag U(0))C; U(0) = sum_l
w_l u_l(0); F = -i int_0^1 sum_l w_l W_l(1,t') J(t') W_l(t',0) dt'; d-1
all-pairs factorization; d-2), §3-§5 (the two odd kills; the HS
enclosure ||K^Sigma_n||_2 <= M/eps; t-1..t-3 the degree -2 start, the
V_2 (x) V_2 leading stratum, the angular kill, the landing AT degree -3
= log Lambda; s-1..s-4; §5.3 the residue naming = this commission), §7,
§10-§12 (C-3, T-1/T-2 the spinor-type license; the flag block);
WSR-AUDIT §0, §3 (t-spans: "degree -3 strata exist (o = 3, c = 0;
c-bearing routes)"; A3 tr[alpha_x C(k)] = -2 khat_x), §7 (COR-1; N-2),
§9 a-2 (MO4 sealed 00:05:12, adjacent, "neither occupies this commission
nor decides tr K_H^Sigma"), §10 AC-4 ("no stronger claim (e.g.,
stratum-by-stratum parity at degree -3) is made by me or by the build");
WBC §0, §2 (CS-1..CS-9), §3.3 (the op chain; rank <= 4n^3; F1), §7-§10;
WBC-AUDIT §0, §5 (COR-1 corrected splitting sign; COR-4 F1/F3 each
sufficient), §7; MO4 §0, §2, §3.3 (the jet's leading amplitude
"k_hat_x [(beta_+ - beta_-) I + (beta_+ + beta_-) alpha.k_hat]" at
bytes; oddness kills traces never trace norms), §4.1-§4.3 (the profile
split; g_n(s) = tr_spatial(Q_n b_D(s) Q_n); G_n = 4 int_0^1 g_n; the
two-sided c_G n^{3/2} <= G_n <= C_G n^{3/2} given CL-A; "the split is
SHARP"), §4.6, §8 CH-8 (the Gram side compresses by Q_n — scheme-blind),
§10. "Q-..." tokens inside the sealed stock: EXPECTED-UNLOCATABLE; noted,
never chased.

---

## 2. THE COMMISSIONED NUMBER AND THE DEGREE-3 CLASS, AT BYTES

```text
(q-1) THE NUMBER: tr K^Sigma_n, per (n, cell) — the wall's single
      remaining summed-level quantity. Of record (WSR, audited):
      tr H_A^Sigma = 0 and ||H_A^Sigma||_2^2 = 0 identically;
      ||K_H^Sigma||_2^2 <= M^2/eps^2 n-uniform carrier-free; the sign
      ledger leaves the one nonnegative number (tr K^Sigma_n)^2.
(q-2) THE STATE OF RECORD: the leading (only) degree -2 diagonal stratum
      (V_2 (x) V_2, o = 2, c = 0) contributes ZERO to the trace (the
      angular kill: its diagonal carries exactly ONE khat_x, first
      moment zero); every surviving diagonal contribution has degree
      <= -3; AT degree exactly -3 the carrier integral is log Lambda;
      the trace threshold (< -3) is STRICT. The degree-3 diagonal class
      = the assembled strata at diagonal symbol degree exactly -3: of
      record these EXIST — (o = 3, c = 0) segment-corrected strata AND
      c-bearing (coincidence) routes (WSR-AUDIT §3, at bytes).
(q-3) "LOG-FREE RATE", operative meaning (the commission's own bar):
      a certificate that the degree-3 class's trace contribution is
      BOUNDED — the log Lambda ELIMINATED by exact structure (a kill or
      an exact radial power), NOT estimated from above; a polynomial
      majorant is an estimate, not an elimination (the record already
      types its polynomial budgets as blockers, WSR s-3; the sealed
      threshold is strict). RATE-CERTIFIED would land the summed trace
      count strictly below -3: carrier integral 1 - 1/Lambda, bounded
      (CAS T5) — enclosing the wall's last summed number at the
      symbol-count grade of the record's own KF1 license.
```

---

## 3. THE THREE ROUTES OF RECORD, ATTEMPTED IN ORDER — EACH DISPLAYED

### 3.1 Route A — ASSEMBLY CANCELLATION: reduced to ONE premise by the one-vertex lemma; the premise undecidable in scope

THE DELIVERABLE [DERIVED — exact 4x4 Dirac algebra of the record (not a
toy); alphabet and lengths a displayed sweep; CAS T1-T3]:

```text
(a-1) ONE VERTEX, at the sealed displays: the [a^1] slot K^Sigma =
      C(U(0)^dag F + F^dag U(0))C carries EXACTLY ONE J vertex — hence
      exactly one alpha_x — in every stratum: F = -i int_0^1 sum_l w_l
      W_l(1,t') J(t') W_l(t',0) dt' carries ONE J at bytes; U(0) =
      sum_l w_l u_l(0) carries NONE (u_l(0) is the a = 0 propagator; J
      enters only with a). GROUNDED(WSR §2.2 displays at bytes).
(a-2) THE SPINOR ALPHABET IS CLOSED, at the sealed type ledger: the
      generator's spinor content is h_0 -> alpha.k, the band/sea
      projectors -> (1 -+ alpha.khat)/2 (constant coefficients), the
      chiral vertex -> S = -i gamma^0 gamma^5; every spatial profile
      (v(t), M(t), b_D, Q_n, P, C_n) is a scalar/projector tensor
      1_spinor of record (WSR T-2 at bytes; beta enters no dynamics).
      So every diagonal stratum's pointwise spinor factor is
      tr[alpha_x W], W a word in {alpha.khat, alpha.khat', S} — at the
      record's own pointwise-symbol (KF1/KF2) grade.
(a-3) THE LEMMA (CAS T2/T3, exhaustive at the displayed sweep): for
      EVERY word W over {alpha.khat, S} of length <= 5 (63 words) and
      EVERY word over {alpha.khat, alpha.khat', S} of length <= 4 (121
      words), tr[alpha_x W] is ODD-or-zero under the JOINT inversion of
      all momenta. Exemplar: tr[alpha_x alpha.khat] = 4 khat_x. So the
      spinor factor of EVERY diagonal stratum of tr K^Sigma — at every
      degree, single-momentum or transfer-bearing — is JOINT-ODD.
(a-4) THE REDUCTION: the class's angular integral is, stratum by
      stratum, int (joint-odd spinor factor) x (scalar amplitude)
      dOmega. If the amplitude is khat-EVEN the stratum dies EXACTLY
      (T4: first moment against every even monomial to the displayed
      degree = 0); the count lands at degree <= -4 = 1 - 1/Lambda,
      BOUNDED (T5): the log ELIMINATED. The commissioned rate is
      therefore equivalent, at the record's symbol grade, to ONE
      premise: EVERY degree-3 scalar amplitude is khat-even.
(a-5) THE ASSEMBLY'S OWN CANCELLATION RESOURCES ARE SPENT: the two
      summed kills of record (transpose-closure Hermiticity; per-factor
      insertion-parity evenness) act on the COUPLING grading — they
      delivered H_A^Sigma = 0 and are orthogonal to the khat grading;
      the involution GRADES (not a kill), the stencil weight is a
      square (no sign resource) — all of record. The ONLY cancellation
      axis left to this route is angular parity: (a-4)'s premise.
```

THE BLOCKER [exact, swept]:

```text
(a-6) THE PREMISE IS UNDECIDABLE ON THE FIVE NAMED ARTIFACTS:
      (i)  the o = 3 segment-corrected subsidiaries' angular symbols
           are displayed NOWHERE in the sealed scope — the only
           subsidiary quoted at bytes is the U-side V_2: -i V_2(x)/
           (2|k|), angular-scalar; nothing pins V_3/segment-corrected
           angular character;
      (ii) the primary audit EXPLICITLY declines the claim: AC-4 — "no
           stronger claim (e.g., stratum-by-stratum parity at degree
           -3) is made by me or by the build" — the record's own line
           marking this exact premise as unclaimed;
      (iii) the c-bearing degree-3 classes REFUSE parity structurally:
           their coefficients are the sealed khat-DEPENDENT null-ray
           averages beta_s(x, khat) (the delta + CZ jet, at bytes via
           MO4 §2/§3.3), and the jet's own displayed traced combination
           khat_x(b_+ - b_-) is EVEN under the joint swap khat -> -khat,
           b_+ <-> b_- (CAS T6) — the o-2-OBS grammar re-instantiated
           at the summed degree-3 class: odd amplitude x odd trace =
           even integrand, kill refused; and NO first-angular-moment
           certificate for the ray averages exists anywhere in scope
           (T6's exhibit: a khat-odd amplitude component defeats the
           kill with moment 8pi/3 != 0).
      Deriving the missing displays would require opening artifacts
      outside the named five (the REM/BL2STAR/R2 stock) — out of scope
      — or authoring the strata — fenced. ROUTE A: reduced, displayed,
      NOT certifiable today.
```

### 3.2 Route B — REGION CUMULANTS: EMPTY on the sealed scope

```text
(b-1) SWEEP (displayed, §6): the token "cumulant" occurs ZERO times in
      all five named artifacts; no region-decomposition object for the
      summed trace exists in the sealed scope (the WBC's closure typing
      CS-1..CS-9 contains none; the WSR's e_2 collapse relates (tr)^2
      to ||.||_2^2 — the trace enters SQUARED, no linear extraction;
      the FHB surface channel enters the ground only as a name).
(b-2) Constructing a region-cumulant decomposition with certified
      cross-region cancellation would be NEW physics — fenced
      (proof_authorized = false; determination only). ROUTE B: REFUSED
      EMPTY — nothing to consume, nothing consumed.
```

### 3.3 Route C — THE GRAM TRANSFER: assembles at the summed quantifier; delivers the best cap yet; REFUSED as certification at its own sealed sharpness

THE TRANSFER, consumed at its verified seal [chain: named classical
steps + sealed displays; CONDITIONAL(CL-A) at MO4's CLAIMED grade]:

```text
(c-1) THE DIRECT SUMMED CHAIN (no polydisc, no per-pair passage): the
      profile split (MO4 §4.1, seal-verified) applied INSIDE the summed
      F-factor:
        J(t) = -[(Q_n b_D(t)^{1/2}) (x) 1_4][(b_D(t)^{1/2} Q_n) (x) alpha_x]
        ||J(t)||_1 = ||Q_n b_D(t) Q_n||_1 . ||alpha_x||_1 = 4 g_n(t)
          [tensor multiplicativity; PSD trace-norm = trace; ||alpha_x||_1
           = 4 — all pinned CAS T7; g_n(t) := tr_spatial(Q_n b_D(t) Q_n),
           the MO4 Gram mass, Q_n-compressed (scheme-blind, MO4 CH-8)]
        ||F||_1 <= int_0^1 sum_l |w_l| ||W_l J W_l||_1 dt
                = int_0^1 ||J(t)||_1 dt = 4 int_0^1 g_n = G_n
          [Bochner triangle; unitary invariance; l1-tight weights
           sum_l |w_l| = 1 of record]
        ||K^Sigma_n||_1 <= 2 ||U(0)||_op ||F||_1 <= 2 G_n <= 2 C_G n^{3/2}
          [||U(0)||_op <= 1, ||C_n||_op <= 1 of record; CL-A ceiling at
           MO4's grade, n >= n_c, constants symbolic]
        ==> |tr K^Sigma_n| <= 2 C_G n^{3/2}   per (n, cell).
      DELIVERED MEANWHILE: the first trace-norm budget for the summed
      composite, sub-volume (n^{3/2} vs the 8n^3 of record) — a real
      asset for the registrar, NOT the commissioned rate.
(c-2) THE REFUSAL, at the route's own sealed display: the commissioned
      object is a LOG-free certificate; 2 C_G n^{3/2} is POLYNOMIAL —
      it majorizes the class from above by a budget that infinitely
      exceeds the log scale (2 C_G n^{3/2}/log Lambda -> oo, T7): it
      ESTIMATES, the commission bars estimation. And the refusal is
      FINAL for this route, not provisional: the split's budget is
      SHARP of record (MO4 §4.3: c_G n^{3/2} <= G_n, "no better n-power
      can come out of this split") and the FLOOR itself outruns the log
      (c_G n^{3/2}/log Lambda -> oo, T7) — the Gram route can never
      certify a log-free rate; its remaining refinement axis (the
      resolvent-weighted factor, MO-4-R) concerns R_n, which does not
      appear in the baseline-free K^Sigma. Consistent with the primary
      audit's own ruling (a-2): MO4 "neither occupies this commission
      nor decides tr K_H^Sigma". ROUTE C: consumed, displayed, REFUSED
      as certification.
(c-3) THE INDEPENDENT CAP AT THE SAME POWER (subsidiary display, grade
      inherited from the sealed HS enclosure): |tr T| <= sqrt(rank T)
      ||T||_2 (Lagrange/Cauchy-Schwarz, T8) with rank <= 4n^3 on ran
      C_n (of record) and ||K^Sigma_n||_2 <= M/eps (of record, grade
      inherited: REM e-i/e-ii; ray class CONDITIONAL(P-1)):
        |tr K^Sigma_n| <= 2 n^{3/2} M/eps.
      TWO independent routes cap the wall's number at the SAME
      sub-volume power n^{3/2}; NEITHER is log-free; both -> oo (T8).
      The concurrence sharpens the diagnosis: today's sealed assets
      reach exactly n^{3/2} at trace grade and no further.
```

---

## 4. VERDICT — AND THE NEW SEALED OBJECT THAT WOULD DECIDE

```text
T4SR VERDICT: UNDECIDABLE-TODAY.

BLOCKERS, exact (each swept, §6):
  B-1 THE CENSUS ABSENCE: no display of the o = 3 segment-corrected
      subsidiaries' angular symbols exists in the five named artifacts;
      the primary audit's AC-4 marks degree-3 stratum-by-stratum parity
      as claimed by no one. (Route A's premise, side (i).)
  B-2 THE c-CLASS REFUSAL: the coincidence-bearing degree-3 amplitudes
      are the sealed khat-dependent ray averages; parity supplies no
      kill (khat_x(b_+ - b_-) joint-EVEN, T6); no moment certificate
      sealed. (Route A's premise, side (iii) — and the direction the
      per-composite record's o-2-OBS grammar points.)
  B-3 EVERY TRACE BUDGET POLYNOMIAL: 8n^3 and 12 sqrt2 n^{7/2}/ell of
      record; 2 C_G n^{3/2} and 2 n^{3/2} M/eps delivered here — all
      -> oo; polynomial majorants estimate, never eliminate; the Gram
      split's floor is sharp of record and itself outruns the log:
      Route C is refused finally, not provisionally.
  B-4 NO CUMULANT OBJECT: Route B is empty on the sealed scope;
      authoring one is fenced.

NOT REFUTED: no divergence of tr K^Sigma_n is exhibited here or
anywhere of record; the number stays UNDECIDED with its logarithmic
shortfall quantified (WSR §5.3, audited).

THE NEW SEALED OBJECT THAT WOULD DECIDE — named exactly:

  THE DEGREE-3 ANGULAR CENSUS of the summed assembly: a sealed display,
  at the grade of the record's own V_2 (x) V_2 leading-stratum display
  but ONE ORDER DEEPER, enumerating EVERY diagonal stratum of K^Sigma
  at degree exactly -3 — (i) the (o = 3, c = 0) segment-corrected
  subsidiaries with their angular symbols, and (ii) the c-bearing
  coincidence classes with their ray-average coefficients — together
  with each scalar amplitude's joint khat-parity and its EXACT first
  angular moments against khat_x.

  DECIDES IN BOTH DIRECTIONS, mechanically, on the lemma delivered
  here: ALL amplitudes khat-even ==> every degree-3 stratum's angular
  integral vanishes EXACTLY (odd trace x even amplitude, T4), the
  summed trace count lands at degree <= -4 = carrier integral
  1 - 1/Lambda BOUNDED (T5): RATE-CERTIFIED, the log eliminated, the
  wall's last summed number enclosed at the record's symbol-count
  grade. ANY stratum with a nonzero odd moment ==> the log is GENUINE
  at that stratum: the rate is REFUTED and the number's logarithmic
  growth is exhibited. Either branch closes this commission.

  (Successor note: the census is a display-and-moment computation over
  already-sealed structure — REM's stratification one degree past its
  §3.3 display plus the jet's ray-average moments — not new physics;
  it is exactly the shape of work the record's KF1/KF2 license covers.)
```

---

## 5. SCOPE GUARD AND QUANTIFIER HONESTY

```text
(g-1) SUMMED ONLY: every claim above is OF the summed object K^Sigma at
      its own quantifier. No per-composite or per-pair quantity is
      quoted, moved, or decided; the wall's four per-composite
      quantities and T_n stand UNDECIDED at their own quantifier
      exactly as of record; V-2 is not crossed in either direction
      (the one Gram chain used is DIRECT on the summed factor — no
      per-pair extraction, no summed-to-per-pair transport).
(g-2) o-2-OBS UNTOUCHED AND CONSISTENT: the lemma does not contradict
      the per-composite even-integrand refusal — the o-2-OBS integrand
      khat_x(khat_x - khat'_x) is EVEN precisely as odd-amplitude x
      odd-trace (T4's refusal display); the lemma concerns the spinor
      FACTOR alone and assigns the per-composite refusal to its
      amplitude side — the record's own grammar, re-instantiated, not
      revised.
(g-3) NOTHING MOVED: B-L2*, the adopted clause, alpha' = 1/4, MO-2,
      MO-3/p = -3, MO4's PARTIAL and its residual MO-4-R, the WBC
      op-grade enclosure, o-2-OBS, KF1/KF2 — all consumed (where
      consumed) at their own quantifiers and sealed grades; no flag of
      the record flipped; no witness action (witness status is the
      registrar's); no falsifier engaged.
(g-4) GRADE HONESTY: the lemma is exact finite algebra (identity
      grade) with its alphabet/length sweep displayed; the reduction
      (a-4) runs at the record's pointwise-symbol KF1/KF2 grade — the
      same license the sealed kill consumptions carry; the Gram chain
      is CONDITIONAL(CL-A) at MO4's CLAIMED (un-audited in scope)
      grade and NOTHING in the verdict rests on it (it feeds only the
      delivered-meanwhile budget and blocker B-3, which stands of
      record without it); the rank x HS cap inherits the HS
      enclosure's sealed grade (REM e-i/e-ii; ray class
      CONDITIONAL(P-1)).
```

---

## 6. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 02:58:05 -0500**. Corpus roots
(`workspace` primary; cleanroom root untouched — no artifact of this
commission's ground lives there) swept by FILENAME ONLY except the five
named artifacts (token greps inside those five only); fenced classes
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF) and ALL other
files matched by filename only, never opened. Artifacts sealed after
the cutoff are UNSWEPT and unclassed here.

```text
sw-1 ROUTE OCCUPATION (filename sweep): no workspace filename matches
     logfree|log_free|T4SR|cumulant; the CENSUS-named files are older,
     different-commission artifacts (family/carrier censuses, names
     only, never opened). NO artifact occupies this commission; the
     output path was ABSENT at session start and at the pre-write
     re-probe.
sw-2 TOKEN SWEEP INSIDE THE FIVE (displayed): "cumulant" — 0 hits in
     all five (Route B's emptiness); "log-free" — only the WSR's own
     residue naming (this commission's source, §5.3/§12) ; the
     degree-3 census facts confirmed at WSR-AUDIT :152 ("degree -3
     strata exist (o = 3, c = 0; c-bearing routes)") and :561 (AC-4).
sw-3 ADJACENCY AT CUTOFF: the newest workspace artifacts by mtime are
     RK_LT_BIT lane files (a different commission by name; not
     opened). MO4's adjacency to this commission is already
     adjudicated of record (WSR-AUDIT a-2: adjacent, does not occupy,
     does not decide tr K_H^Sigma) — consistent with §3.3's refusal.
sw-4 The enumerations of §3 claim exhaustiveness ONLY over the five
     named seals at the spans read and the displayed CAS sweeps
     (alphabet/lengths as stated).
```

---

## 7. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `t4srvenv` under the session scratchpad;
nothing written to the workspace but this artifact and its seal. Exact
symbolic / exact rational/surd only; nothing numeric evaluated. ONE
battery, 8 checks, 8/8 PASS on a single run; NO pre-final corrections.

```python
# RATE-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv t4srvenv).
# Commission T4SR: one log-free rate for the degree-3 diagonal class of the summed
# assembly. Every constant symbolic or an exact rational/surd. Nothing numeric
# evaluated (Lambda, ell, n, M, eps, C_G, c_G FORMAL). Groups:
#   T1 the Dirac ground (the sealed spinor bytes the lemma runs on);
#   T2 the ONE-VERTEX TRACE-PARITY LEMMA, single momentum (words over {alpha.khat, S},
#      lengths 0..5, all 63 words): every tr[alpha_x W] is ODD-or-zero under khat -> -khat;
#   T3 the lemma, two momenta (words over {alpha.khat, alpha.khat', S}, lengths 0..4,
#      all 121 words): every tr[alpha_x W] is ODD-or-zero under the JOINT inversion;
#   T4 the angular kill against EVEN scalar amplitudes (exact moments) and its REFUSAL
#      against odd amplitudes (the o-2-OBS grammar: odd amplitude x odd trace = even);
#   T5 the threshold arithmetic: degree -3 = log Lambda; one more kill lands -4 =
#      1 - 1/Lambda, LOG-FREE — the conditional certification's landing site;
#   T6 the c-class jet-amplitude parity: khat_x(b+ - b-) is EVEN under the joint swap
#      (khat -> -khat, b+ <-> b-) — the parity kill is structurally refused there —
#      and the moment exhibit int khat_x(f(khat) - f(-khat)) dOmega = 8pi/3 != 0 at f = khat_x;
#   T7 the Gram-transfer mechanics at the summed object: tensor trace-norm
#      multiplicativity, ||alpha_x||_1 = 4, PSD trace-norm = trace, and the refusal
#      ratio n^{3/2}/log(sqrt(2n)/ell) -> oo (a polynomial budget never decides a log);
#   T8 the rank x HS cap: (sum sigma)^2 <= r sum sigma^2 (Lagrange), sqrt(4n^3) =
#      2 n^{3/2}, and its own -> oo refusal — two independent caps at the SAME power.
import sympy as sp
from sympy import Rational as R
from sympy import I as Im
from itertools import product as iproduct

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== T1 — the Dirac ground (sealed bytes: WS6/A3 grammar) =====
s0 = sp.eye(2); sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -Im], [Im, 0]]); sz = sp.Matrix([[1, 0], [0, -1]])
Z2 = sp.zeros(2, 2)
blk = lambda a, b, c, d: sp.Matrix(sp.BlockMatrix([[a, b], [c, d]]))
beta = blk(s0, Z2, Z2, -s0)
al = [blk(Z2, s, s, Z2) for s in (sx, sy, sz)]
g5 = blk(Z2, s0, s0, Z2)
S_op = sp.expand(-Im*beta*g5)
Zq = lambda M: sp.expand(M) == sp.zeros(*M.shape)
anti_ok = all(Zq(al[i]*al[j] + al[j]*al[i] - 2*sp.KroneckerDelta(i, j)*sp.eye(4))
              for i in range(3) for j in range(3))
ok("T1 DIRAC GROUND: {alpha_i, alpha_j} = 2 delta_ij, S = -i gamma^0 gamma^5 Hermitian,"
   " S^2 = I, {S, alpha_j} = 0 for every j — the sealed spinor bytes (WS6 grammar);"
   " the lemma's alphabet {alpha.khat, alpha.khat', S} + ONE vertex alpha_x is closed"
   " under these relations",
   anti_ok and Zq(sp.expand(S_op - S_op.H)) and Zq(sp.expand(S_op*S_op - sp.eye(4)))
   and all(Zq(sp.expand(S_op*a + a*S_op)) for a in al))

# ===== T2 — THE ONE-VERTEX TRACE-PARITY LEMMA, single momentum =====
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
kxp, kyp, kzp = sp.symbols('khp_x khp_y khp_z', real=True)
A1 = kx*al[0] + ky*al[1] + kz*al[2]
inv1 = {kx: -kx, ky: -ky, kz: -kz}
odd_all, nonzero_ct, exemplar = True, 0, None
for L in range(6):
    for word in iproduct((A1, S_op), repeat=L):
        Mw = al[0]
        for f in word:
            Mw = Mw*f
        tr = sp.expand(Mw.trace())
        odd_all = odd_all and sp.expand(tr + tr.subs(inv1, simultaneous=True)) == 0
        if tr != 0:
            nonzero_ct += 1
            if exemplar is None:
                exemplar = tr
ok("T2 THE LEMMA (single momentum): for EVERY word W over {alpha.khat, S} of length"
   " <= 5 (all 63 words), tr[alpha_x W] is ODD under khat -> -khat (zero traces"
   " trivially; nonzero traces all odd-degree — exemplar tr[alpha_x alpha.khat] ="
   " 4 khat_x); with exactly ONE vertex alpha_x the spinor trace of every"
   " single-momentum diagonal stratum is first-moment-pure: the angular kill fires"
   " IFF the accompanying SCALAR amplitude is khat-EVEN",
   odd_all and nonzero_ct > 0 and sp.expand(exemplar - 4*kx) == 0)

# ===== T3 — the lemma, two momenta (interband/transfer structure) =====
A2 = kxp*al[0] + kyp*al[1] + kzp*al[2]
inv2 = {kx: -kx, ky: -ky, kz: -kz, kxp: -kxp, kyp: -kyp, kzp: -kzp}
odd_all2, nonzero_ct2 = True, 0
for L in range(5):
    for word in iproduct((A1, A2, S_op), repeat=L):
        Mw = al[0]
        for f in word:
            Mw = Mw*f
        tr = sp.expand(Mw.trace())
        odd_all2 = odd_all2 and sp.expand(tr + tr.subs(inv2, simultaneous=True)) == 0
        if tr != 0:
            nonzero_ct2 += 1
ok("T3 THE LEMMA (two momenta): for EVERY word W over {alpha.khat, alpha.khat', S}"
   " of length <= 4 (all 121 words), tr[alpha_x W] is ODD under the JOINT inversion"
   " (khat, khat') -> (-khat, -khat') — the one-vertex reduction survives interband/"
   "transfer structure at the displayed sweep: the entire summed-trace integrand's"
   " spinor factor is joint-odd; ONLY the scalar amplitudes can break the kill",
   odd_all2 and nonzero_ct2 > 0)

# ===== T4 — the kill against EVEN amplitudes; the refusal against ODD ones =====
th, ph = sp.symbols('theta phi', real=True)
sub = {kx: sp.sin(th)*sp.cos(ph), ky: sp.sin(th)*sp.sin(ph), kz: sp.cos(th)}
def ang(e):
    return sp.integrate(sp.integrate(e.subs(sub)*sp.sin(th), (ph, 0, 2*sp.pi)),
                        (th, 0, sp.pi))
kv = [kx, ky, kz]
even_monos = [sp.Integer(1)] + [kv[i]*kv[j] for i in range(3) for j in range(i, 3)]
kills = all(sp.simplify(ang(kx*m)) == 0 for m in even_monos)
refusal = sp.simplify(ang(kx*kx) - R(4, 3)*sp.pi) == 0
ok("T4 KILL vs REFUSAL (exact moments): int_S2 khat_x m(khat) dOmega = 0 for EVERY"
   " even monomial m of degree <= 2 (7 monomials displayed) — odd trace x EVEN"
   " amplitude dies EXACTLY; and int_S2 khat_x . khat_x dOmega = 4pi/3 != 0 — odd"
   " trace x ODD amplitude SURVIVES (the o-2-OBS grammar: the per-composite even"
   " integrand khat_x(khat_x - khat'_x) is odd-amplitude x odd-trace, kill refused)",
   kills and refusal)

# ===== T5 — threshold arithmetic (KF1 license, Lambda FORMAL) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
I4 = sp.integrate(kk**(2 - 4), (kk, 1, Lam))
ok("T5 THRESHOLDS (exact): the degree-3 diagonal class's carrier integral is"
   " int_1^Lam k^2 k^-3 dk = log Lambda (divergent, formal limit oo); ONE more"
   " exact angular kill (all degree-3 amplitudes khat-even) lands the count at"
   " degree -4: int k^2 k^-4 = 1 - 1/Lambda -> 1, BOUNDED — the log ELIMINATED,"
   " carrier-free: the census's all-even branch IS the log-free rate",
   sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo
   and sp.simplify(I4 - (1 - 1/Lam)) == 0 and sp.limit(I4, Lam, sp.oo) == 1)

# ===== T6 — the c-class jet-amplitude parity (the sealed ray-average shape) =====
bp, bm = sp.symbols('b_plus b_minus', real=True)
amp = kx*(bp - bm)
swapped = amp.subs({kx: -kx, ky: -ky, kz: -kz, bp: bm, bm: bp}, simultaneous=True)
f_odd = kx   # ray-average exhibit: f(khat) with odd part khat_x
moment = sp.simplify(ang(kx*(f_odd - f_odd.subs(inv1, simultaneous=True))))
ok("T6 THE c-CLASS SHAPE: the sealed jet amplitude combination khat_x (b_+ - b_-)"
   " is EVEN under the joint swap (khat -> -khat, b_+ <-> b_-) — parity supplies"
   " NO kill on the coincidence-bearing degree-3 class; and the first-moment"
   " exhibit int khat_x (f(khat) - f(-khat)) dOmega = 8pi/3 != 0 at f = khat_x:"
   " a khat-odd ray-average component defeats the kill — only a sealed moment"
   " census can decide",
   sp.expand(swapped - amp) == 0 and sp.simplify(moment - R(8, 3)*sp.pi) == 0)

# ===== T7 — the Gram transfer at the summed object, and its refusal =====
def kron(A, B):
    m, n = A.shape; p, q = B.shape
    return sp.Matrix(m*p, n*q, lambda i, j: A[i//p, j//q]*B[i % p, j % q])
a7, b7 = sp.symbols('a7 b7', positive=True)
Mten = kron(sp.diag(a7, b7), sx)
sv2 = (Mten.H*Mten).eigenvals()
tn_ten = sp.expand(sum(sp.sqrt(v)*mult for v, mult in sv2.items()))
alx1 = sp.expand(sum(sp.sqrt(v)*m for v, m in (al[0].H*al[0]).eigenvals().items()))
v7 = sp.Matrix([1, 2, 2]); P7 = v7*v7.T
tnP = sp.expand(sum(sp.sqrt(v)*m for v, m in (P7.T*P7).eigenvals().items()))
n_, ell_, CG_, cG_ = sp.symbols('n ell C_G c_G', positive=True)
ratio = (2*CG_*n_**R(3, 2))/sp.log(sp.sqrt(2*n_)/ell_)
floor_ratio = (cG_*n_**R(3, 2))/sp.log(sp.sqrt(2*n_)/ell_)
ok("T7 GRAM TRANSFER AT THE SUMMED OBJECT: ||A tensor B||_1 = ||A||_1 ||B||_1"
   " (exact instance: ||diag(a,b) tensor sigma_x||_1 = 2(a+b)); ||alpha_x||_1 = 4;"
   " ||PSD||_1 = tr (instance 9) — hence ||J(t)||_1 = 4 g_n(t) and the direct"
   " summed chain ||K^Sigma_n||_1 <= 2 G_n (unitaries + l1-tight weights + Bochner"
   " triangle, NO polydisc); REFUSAL: 2 C_G n^{3/2} / log(sqrt(2n)/ell) -> oo AND"
   " the SHARP floor c_G n^{3/2} / log -> oo — the split's budget infinitely"
   " exceeds the log scale in BOTH directions: it estimates, it cannot eliminate",
   sp.simplify(tn_ten - 2*(a7 + b7)) == 0 and alx1 == 4 and tnP == 9
   and sp.limit(ratio, n_, sp.oo) == sp.oo and sp.limit(floor_ratio, n_, sp.oo) == sp.oo)

# ===== T8 — the rank x HS cap at the same power, and its refusal =====
s1, s2, s3 = sp.symbols('sigma1 sigma2 sigma3', nonnegative=True)
lagr = sp.expand(3*(s1**2 + s2**2 + s3**2) - (s1 + s2 + s3)**2
                 - ((s1 - s2)**2 + (s1 - s3)**2 + (s2 - s3)**2)/1)
Meps = sp.Symbol('M_over_eps', positive=True)
cap = sp.sqrt(4*n_**3)*Meps
ok("T8 RANK x HS CAP: (sum_{i<=r} sigma_i)^2 <= r sum sigma_i^2 (Lagrange identity"
   " at r = 3, exact), so |tr T| <= sqrt(rank T) ||T||_2; with rank <= 4n^3 and the"
   " sealed HS enclosure ||K^Sigma_n||_2 <= M/eps: |tr K^Sigma_n| <= 2 n^{3/2} M/eps"
   " (sqrt(4n^3) = 2 n^{3/2} exact) — the SAME sub-volume power as the Gram cap by"
   " an independent route; REFUSAL: 2 n^{3/2} (M/eps) -> oo, polynomial, not"
   " log-free — neither cap certifies the commissioned rate",
   lagr == 0 and sp.simplify(cap - 2*n_**R(3, 2)*Meps) == 0
   and sp.limit(cap, n_, sp.oo) == sp.oo)

print("T4SR-BATTERY-DONE")
```

Output, verbatim (8/8 PASS, single run):

```text
T1 DIRAC GROUND: {alpha_i, alpha_j} = 2 delta_ij, S = -i gamma^0 gamma^5 Hermitian, S^2 = I, {S, alpha_j} = 0 for every j — the sealed spinor bytes (WS6 grammar); the lemma's alphabet {alpha.khat, alpha.khat', S} + ONE vertex alpha_x is closed under these relations: PASS
T2 THE LEMMA (single momentum): for EVERY word W over {alpha.khat, S} of length <= 5 (all 63 words), tr[alpha_x W] is ODD under khat -> -khat (zero traces trivially; nonzero traces all odd-degree — exemplar tr[alpha_x alpha.khat] = 4 khat_x); with exactly ONE vertex alpha_x the spinor trace of every single-momentum diagonal stratum is first-moment-pure: the angular kill fires IFF the accompanying SCALAR amplitude is khat-EVEN: PASS
T3 THE LEMMA (two momenta): for EVERY word W over {alpha.khat, alpha.khat', S} of length <= 4 (all 121 words), tr[alpha_x W] is ODD under the JOINT inversion (khat, khat') -> (-khat, -khat') — the one-vertex reduction survives interband/transfer structure at the displayed sweep: the entire summed-trace integrand's spinor factor is joint-odd; ONLY the scalar amplitudes can break the kill: PASS
T4 KILL vs REFUSAL (exact moments): int_S2 khat_x m(khat) dOmega = 0 for EVERY even monomial m of degree <= 2 (7 monomials displayed) — odd trace x EVEN amplitude dies EXACTLY; and int_S2 khat_x . khat_x dOmega = 4pi/3 != 0 — odd trace x ODD amplitude SURVIVES (the o-2-OBS grammar: the per-composite even integrand khat_x(khat_x - khat'_x) is odd-amplitude x odd-trace, kill refused): PASS
T5 THRESHOLDS (exact): the degree-3 diagonal class's carrier integral is int_1^Lam k^2 k^-3 dk = log Lambda (divergent, formal limit oo); ONE more exact angular kill (all degree-3 amplitudes khat-even) lands the count at degree -4: int k^2 k^-4 = 1 - 1/Lambda -> 1, BOUNDED — the log ELIMINATED, carrier-free: the census's all-even branch IS the log-free rate: PASS
T6 THE c-CLASS SHAPE: the sealed jet amplitude combination khat_x (b_+ - b_-) is EVEN under the joint swap (khat -> -khat, b_+ <-> b_-) — parity supplies NO kill on the coincidence-bearing degree-3 class; and the first-moment exhibit int khat_x (f(khat) - f(-khat)) dOmega = 8pi/3 != 0 at f = khat_x: a khat-odd ray-average component defeats the kill — only a sealed moment census can decide: PASS
T7 GRAM TRANSFER AT THE SUMMED OBJECT: ||A tensor B||_1 = ||A||_1 ||B||_1 (exact instance: ||diag(a,b) tensor sigma_x||_1 = 2(a+b)); ||alpha_x||_1 = 4; ||PSD||_1 = tr (instance 9) — hence ||J(t)||_1 = 4 g_n(t) and the direct summed chain ||K^Sigma_n||_1 <= 2 G_n (unitaries + l1-tight weights + Bochner triangle, NO polydisc); REFUSAL: 2 C_G n^{3/2} / log(sqrt(2n)/ell) -> oo AND the SHARP floor c_G n^{3/2} / log -> oo — the split's budget infinitely exceeds the log scale in BOTH directions: it estimates, it cannot eliminate: PASS
T8 RANK x HS CAP: (sum_{i<=r} sigma_i)^2 <= r sum sigma_i^2 (Lagrange identity at r = 3, exact), so |tr T| <= sqrt(rank T) ||T||_2; with rank <= 4n^3 and the sealed HS enclosure ||K^Sigma_n||_2 <= M/eps: |tr K^Sigma_n| <= 2 n^{3/2} M/eps (sqrt(4n^3) = 2 n^{3/2} exact) — the SAME sub-volume power as the Gram cap by an independent route; REFUSAL: 2 n^{3/2} (M/eps) -> oo, polynomial, not log-free — neither cap certifies the commissioned rate: PASS
T4SR-BATTERY-DONE
```

---

## 8. CHOICE LEDGER (commission T4SR)

```text
C-1 VERDICT CLASS [YOURS]: UNDECIDABLE-TODAY over RATE-CERTIFIED.
    Ground: the one missing premise (the degree-3 amplitude parity
    census) is displayed in none of the five named artifacts, is
    EXPLICITLY unclaimed of record (WSR-AUDIT AC-4), and carries
    positive contrary evidence at the c-class (the sealed ray-average
    shape, T6); deriving it would need out-of-scope reads (the
    REM/BL2STAR/R2 stock) or authored strata (fenced). A conditional
    certificate is not the commissioned certificate.
C-2 "LOG-FREE" READING [YOURS, disclosed §2 q-3]: bounded carrier
    integral for the class — the log ELIMINATED — per the commission's
    own bar ("not estimated away") and the record's typing of
    polynomial budgets as blockers (WSR s-3). Consequence: the n^{3/2}
    caps of §3.3 are refusals, not certifications, even though they
    are the best trace budgets yet.
C-3 GRAM-TRANSFER GRADE [YOURS, disclosed]: MO4 is consumed at its
    verified seal but CLAIMED grade (no audit sidecar in scope; the
    primary audit's a-2 marks it adjacent/unconsumed there). Its
    content feeds ONLY the delivered-meanwhile budget (c-1) and
    blocker B-3's sharpness clause; the verdict stands without it
    (B-1/B-2/B-4 suffice; B-3's polynomial-budget core is of record).
C-4 LEMMA SWEEP GRADE [YOURS, displayed]: T2/T3 claim exhaustiveness
    over the displayed alphabet and lengths only (63 + 121 words); the
    alphabet-closure claim (a-2) is GROUNDED at the record's own type
    ledger (WSR T-2; the sealed generator displays), not proved beyond
    it. The reduction (a-4) runs at the record's pointwise-symbol
    KF1/KF2 grade — the same license the sealed angular-kill
    consumptions carry (WSR-AUDIT AC-4/AT-2 grammar).
C-5 ROUTE ORDER [YOURS]: assembly cancellation first (the only route
    that could ELIMINATE), region cumulants second (emptiness cheap to
    display), Gram transfer last (its refusal needs the first two's
    framing). All three attempted and displayed per the commission.
C-6 THE DECIDING OBJECT'S SHAPE [YOURS]: named as a CENSUS (display +
    exact moments over already-sealed structure) rather than a new
    estimate, so that either branch of its outcome decides the
    commission mechanically on the lemma delivered here — the
    successor's work is determination-shaped, not physics-shaped.
C-7 HOMONYM SPLIT [inherited, disclosed in header]: n carrier index;
    k momentum; (o, c) the REM strata indices as quoted.
```

---

## 9. TOY_SEPARATION

```text
T-1 T1-T3 use the EXACT 4x4 Dirac algebra of the record (not a toy:
    the sealed spinor structure itself); the word alphabet is a
    slot-model of the sealed symbol classes at the record's own type
    ledger (spatial profiles = scalars tensor 1_spinor; h_0 and band
    projectors = alpha.khat class; the chiral vertex = S; ONE J vertex
    = alpha_x). No matrix-grade result is carried to the carrier
    quantifier; every carrier-axis statement cites sealed carrier-axis
    text (KF1 thresholds, T5) or is a named refusal.
T-2 T4/T5/T6 are exact continuum angular/radial integrals at the
    record's KF1/KF2 effective-degree license; Lambda is FORMAL, never
    valued.
T-3 T7/T8's exact-rational/symbolic instances (the Kronecker trace-norm
    instance; the PSD instance; the Lagrange identity at r = 3) are
    identity-grade exhibits of displayed general facts (tensor
    multiplicativity; trace-norm of positives; Cauchy-Schwarz on
    singular values) — the same license the sealed MO4 battery used
    (its V3/V4/V8). No instance is promoted to a family claim beyond
    its displayed general proof.
T-4 No testbed evaluation appears; nothing witness-family-grade is
    promoted; no spectral datum of the actual family is valued.
```

---

## 10. FLAG BLOCK

```text
LOGFREE_RATE_VERDICT = UNDECIDABLE-TODAY( no log-free rate for the
  degree-3 diagonal class of the summed assembly is certifiable on the
  five named seals; the commission is REDUCED to one exactly-named
  missing premise by the one-vertex trace-parity lemma (DERIVED, CAS
  T1-T3: one J vertex per stratum at bytes; every tr[alpha_x W] over
  the sealed spinor alphabet joint-ODD at the displayed sweep), so the
  rate holds IFF every degree-3 scalar amplitude is khat-even — and
  that premise is undecidable today: B-1 the degree-3 angular census
  ABSENT (o = 3 subsidiaries' angular symbols displayed nowhere in
  scope; WSR-AUDIT AC-4 marks the parity claim made by no one); B-2
  the c-bearing classes carry the sealed khat-dependent ray averages —
  khat_x(b_+ - b_-) joint-EVEN (T6), the o-2-OBS grammar, no moment
  certificate sealed; B-3 every trace budget polynomial (8n^3, 12
  sqrt2 n^{7/2}/ell of record; 2 C_G n^{3/2} and 2 n^{3/2} M/eps
  delivered here) — estimates, never eliminations, and the Gram
  split's floor is sharp of record (its own floor outruns the log);
  B-4 no cumulant object exists in scope. NOT REFUTED: no divergence
  of tr K^Sigma_n exhibited; the number UNDECIDED with its log
  shortfall quantified of record. )

ROUTES_ATTEMPTED = THREE( assembly cancellation: REDUCED to the parity
  premise by the one-vertex lemma — not certifiable, census absent
  (§3.1); region cumulants: EMPTY on the sealed scope, zero cumulant
  tokens in the five artifacts, authoring fenced (§3.2); Gram
  transfer: ASSEMBLES at the summed quantifier — ||K^Sigma_n||_1 <=
  2 G_n <= 2 C_G n^{3/2} direct, no polydisc, no per-pair passage —
  and is REFUSED as certification at its own sealed sharpness
  (c_G n^{3/2} <= G_n; floor/log -> oo, T7): final, not provisional
  (§3.3). )

DECIDING_OBJECT = DEGREE3_ANGULAR_CENSUS( a sealed display, one order
  past the record's V_2 (x) V_2 display: every degree-3 diagonal
  stratum of K^Sigma — the (o = 3, c = 0) segment-corrected
  subsidiaries AND the c-bearing coincidence classes — with each
  scalar amplitude's joint khat-parity and exact first angular moments
  against khat_x. Decides BOTH ways on the lemma delivered here:
  all-even => class trace = 0 exactly, count at degree <= -4, carrier
  integral 1 - 1/Lambda BOUNDED: RATE-CERTIFIED, the wall's last
  summed number enclosed; any nonzero odd moment => the log is genuine
  there: the rate refuted, the growth exhibited. Census-shaped
  (display + moments over sealed structure), not new physics. )

DELIVERED_MEANWHILE = TWO( (i) the ONE-VERTEX TRACE-PARITY LEMMA,
  identity grade, alphabet/length sweep displayed — the reduction
  instrument any successor census consumes mechanically; (ii) the
  first trace-norm budget for the summed composite: ||K^Sigma_n||_1 <=
  2 G_n <= 2 C_G n^{3/2} (CONDITIONAL CL-A at MO4's CLAIMED grade; T7)
  with |tr K^Sigma_n| <= 2 C_G n^{3/2}, alongside the independent
  rank x HS cap |tr K^Sigma_n| <= 2 n^{3/2} M/eps (grade inherited;
  T8) — two independent caps at the SAME sub-volume power n^{3/2},
  neither log-free, both displayed as refusals. )

PER_COMPOSITE_WALL = UNTOUCHED( summed quantifier only; no
  per-composite or per-pair quantity quoted, moved, or decided; V-2
  not crossed in either direction (the Gram chain is direct on the
  summed factor); o-2-OBS consistent and untouched — its even
  integrand is odd-amplitude x odd-trace in the lemma's grammar (T4);
  T_n, B-L2*, alpha' = 1/4, MO-2, MO-3/p = -3, MO4's PARTIAL and
  MO-4-R, the WBC enclosure, and every sealed identity consumed (where
  consumed) at their own quantifiers; no flag of the record flipped;
  no witness action — witness status is the registrar's. )

SEALS_VERIFIED = 5/5( STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md +
  _AUDIT_V001.md; STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md +
  _AUDIT_V001.md; STAGE8_MO4_CANCELLATION_S9AD_V001.md — every digest
  recomputed from bytes at path this session by full digest via
  shasum -a 256 -c against sidecars; all OK; none unverifiable; every
  load-bearing span read at path; scoped reads of these five ONLY. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant
  symbolic or an exact rational/surd; NOTHING numeric evaluated
  (Lambda, ell, n, M, eps, C_G, c_G formal); no float ground; no
  measured constant; no value frozen; alpha never computed or
  approached; kappa_record nowhere; no per-pair route traversed; H-R
  nowhere defaulted; no fenced-class or out-of-scope file opened
  (filename matches only); "Q-..." tokens noted, never chased; no
  flag flipped; no witness action; no git; ONE output + sidecar at
  the commission-distinct path, probed ABSENT at start and re-probed
  ABSENT immediately before write. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv t4srvenv under
  the session scratchpad, ONE battery, 8 checks, 8/8 PASS on a single
  run, NO pre-final corrections, script and output reproduced verbatim
  in §7; shasum/file reads/filename sweeps otherwise; NOTHING numeric
  evaluated.

SWEEP_CUTOFF = 2026-08-15 02:58:05 -0500 (workspace root; fenced
  classes and all non-named files filename-matched only, never opened;
  token greps inside the five named artifacts only; artifacts sealed
  after the cutoff UNSWEPT).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
T4SR_LOGFREE_RATE_RESULT = SEALED.
```
