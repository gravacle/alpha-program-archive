# STAGE 8 — R-L4a/b AND R-L0(/R-L0b): TWO NAMED CERTIFICATIONS ATTEMPTED ON THE DISCHARGED BASIS

## BLIND ANALYST — CODENAME RL4-RL0 — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (RL4-RL0). Two named certifications attempted from the
discharged basis and the exact closed forms now on paper: (A) R-L4a/R-L4b,
the sector-defining obligations (E1 :766-790); (B) R-L0, the cell-scale
normalization (E1 :854-875), with R-L0b (E1 :877-885) located and shaped.
Honest outcomes equal citizens: CERTIFIED / PARTIAL / FAILS-AT / REDUCED /
NOT-DERIVABLE with the act named. The MAY-FAIL on R-L0 is treated as real;
a failure would be reported as a finding.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — one CAS script (sympy 1.14.0,
fresh venv `rl4venv`), reproduced in full in §5 with output verbatim;
NOTHING numeric evaluated; every constant symbolic or an exact
rational/surd; the three exact-rational matrix trials in A3a use exact
rational arithmetic only (seeded, reproducible). No physical quantity
computed, bounded numerically, or evaluated. No scale, imported GR, or
faithfulness authority. M(t) sharp; equal-time FALSE by C6 (the omega = 0
slice is consumed only as the sealed bookkeeping intermediate of record,
R2 §4). No register/tracker/plan/road/ledger/lens file read. Output name
probed before write: ABSENT. No git action; not registered, not pushed.

---

## 0. VERDICT IN ONE LINE

**R-L4a/b: PARTIAL. The zero-free chain's exact census (5/4 pairs,
S_± = ±1/2, N_surv(0) = 1/2, c = (1/2, 1/8 x 4), kappa_bal = 1 — re-derived
here independently, §5 A1) discharges the R-L4 BOOKKEEPING clause's content
and the kappa_bal consequence (:794-800), but NOT R-L4a/b themselves — the
dependency runs the other way (the census is conditional ON the exclusion).
What IS newly certifiable: the unregularized half of R-L4a ("D IS
UNDEFINED") at identity grade from the sealed divergent trace; and the
sharpening that the clause's own displayed regularized route (det_2 · e^{tr})
ALSO does not exist uncompressed, because ||CPC||_2 = infinity of record —
so the only regularization available on the discharged basis is the carrier
compression C_n, whose naming is the spec author's act. At fixed n the exact
eigenvalue form det_n(0) = prod(1 - 2 s_i) is on paper, with an exact
dichotomy: divergent commutator HS mass ||[C_n,P]||_2^2 forces det_n(0) -> 0
(sufficient), while bounded mass plus uniform 1/2-avoidance forces det_n(0)
BOUNDED AWAY from 0 (R-L4b then FALSE at that regularization, and the 5/4
census would need 9-pair re-derivation). Neither branch is sealed. EXACT
RESIDUE: the regularization-naming act + the commutator-mass input + (for
R-L4b) a joint baseline/source rate on the polydisc. R-L0: NOT-DERIVABLE
as stated — the inequality cannot even be STATED symbolically on the
discharged basis, because x's ingredients contain four unvalued items of
record (alpha, beta underived — R-L2b; rho_res = H-R undischarged, forbidden
to default; G_cm's form uncertified — C-L2 witness; and the per-pair carrier
sup of the S2 ingredient +infinity of record, 52f2490b). The quantifier at
R-L0's sealed bytes is EVERY admitted cell of D3 — the unrestricted
common-refinement class, an unbounded family — NOT the entered cell. What
IS derived exactly: the threshold algebra (kappa_bal = 1 forces
X_* <= W(1/2) < 1/2, Lambert W, unique root, CAS-exact) and the sharpened
MAY-FAIL: the one scale-invariant log-majorant constant on the discharged
basis is sqrt2 · tau_R = pi exactly, and pi e^pi > 1/2 SYMBOLICALLY — so if
the record cycle enters x undamped at any refinement depth (the sealed O7
hazard branch), R-L0's inequality FAILS at that depth; ground (i)'s
compensating weight (= R-L2b) is the only live route. No fence line was
reached: the blocker is missing derivations, not forbidden numerics, so
REQUIRES-EVALUATION is NOT the verdict. R-L0b located (:877-885): its
footing is STRENGTHENED by the derived census — all five c are POSITIVE, so
the surviving colour sum is a true CONVEX combination and the clause's
"signed-weight convexity" upgrades to genuine convexity; its residue is
per-pair phase localization from the linear estimate alone.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session; all match sidecars and
tasked prefixes. None unverifiable.

```text
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH (tasked 26f9314b)
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH (tasked aed551e3)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM; tasked 2e4e5163)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (tasked 89641f76)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH (R2; tasked a7969f0c)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH (tasked a4f2e1b7)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA; tasked 789338ad)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (per-pair refutation; cited by ZF o-1, verified here)
```

Sealed content read at path and relied on below: E1 :655-666 (Carleman
displays; Log rho VALID ONLY ON THE SURVIVING SECTOR, where
det(1+A(0)) != 0), :679-689 (per-cell S_i; "the sup over CELLS is deferred
to R-L0"), :691-736 (S1/S2/S2b/S4/S5; S3 DELETED with grounds — ||CPC||_2
radius-independent and INFINITE by C6), :745-762 (R.2 sector algebra and
weight bookkeeping), :766-790 (R-L4a/b/R-L4, quoted in full in §2.1),
:794-800 (kappa_bal from surviving weights), :803-833 (R.3 majorant x and
R-L2b), :835-848 (colour-sum closure R.3.a/R.3.b), :851-894 (R.4: R-L0 with
grounds and hazard; R-L0b; eta_1 display), :417-426 (D3 FROZEN QUANTIFIER
READING: unrestricted, universally quantified), :1008-1029 (O.R register
lines); PA :63-142 (Phase A carrier n = 2, ell in {1, sqrt2}; r(t) =
min(t,1-t); M(t) = Q 1_{|x|<=r(t)} Q; v(t) = (pi/sqrt2) 32 r(t)^3; b_D
Gevrey diamond with b_D(1/2,0) = 1; frozen polydisc pairs (a_+,a_-)); R2
:78-98, :158-268, :330 (tau_R = pi/sqrt2 frozen; Dyson domination
(|lambda| Theta)^n/n! using only |lambda| <= sqrt2, Theta <= tau_R; the
omega = 0 slice closed forms); REM :352-383 (dominated a-series on the
closed polydisc; ||Pi X Pi||_2 <= ||X||_2), flag block (all-orders
dominator e^{sqrt2 tau_R} symbolic); ZF §3-§6 + o-1..o-3 (the census
derivation, the reduction, the obstructions); ZF-CHECK §0-§1 (CONFIRMED
with the landing-set correction: summed-S2' + R-L4a/b + R-L0(/R-L0b) +
C-L2/G_cm); 52f2490b flag block (RL2b_uniformity_refuted = true; no
admissible M).

---

## 2. TASK A — R-L4a/b, THE SECTOR-DEFINING OBLIGATIONS

### 2.1 The sealed clauses, quoted in full (E1 :766-790)

```text
:766  R-L4a BASELINE DETERMINANT: EXISTENCE OR NON-EXISTENCE (replaces v001's
:767        invalid limit; B-4). Establish, via the Carleman factorization
:768        det = det_2 · e^{tr} AND WITH THE REGULARIZATION EXPLICITLY NAMED,
:769        whether the opposite-phase baseline determinant EXISTS. AT THE POINT
:770        WHERE C6's HYPOTHESIS HOLDS, THE OBJECT `D = det_{ran C}(1 - 2CPC)`
:771        DOES NOT EXIST AS A FREDHOLM DETERMINANT — tr(CPC) = +infinity — so the
:772        correct unregularized statement is "D IS UNDEFINED", NOT "D = 0".
:773        *** THE INEQUALITY D^2 <= exp(-2||[C,P]||_2^2) IS STRICKEN AS A GROUND
:774        FOR D = 0. *** It is a finite-dimensional identity/inequality (200 exact
:775        trials, no violation; the exact commutator identity
:776        ||[C,P]||_2^2 = 2 sum_i sigma_i(1-sigma_i) on 50 exact trials) and may
:777        be REPORTED as such; it may not be evaluated at an infinite right-hand
:778        side, and no artifact may infer a value of D from it.
:779        The conclusion may well be RECOVERABLE by the regularized route
:780        (det = det_2 · e^{tr} with tr = -infinity ⇒ det = 0). THAT IS A
:781        DIFFERENT AND HONEST ARGUMENT AND IT IS THE ONE R-L4b MUST MAKE.
:782        Witness: E1_BASELINE_DETERMINANT_EXISTENCE_UNCERTIFIED.
:783  R-L4b OPPOSITE-PHASE SECTOR VANISHES IDENTICALLY, as displayed above, on the
:784        closed pair polydisc, with the regularization of R-L4a.
:785        Witness: E1_OPPOSITE_PHASE_SECTOR_VANISHING_UNCERTIFIED.
:786  R-L4  BASELINE BOOKKEEPING, REPORTED PER STATE: the weight algebra above
:787        (independently confirmed), the finite-trial identity and inequality as
:788        REPORTED items only. MUST be reported as a CANDIDATE partial statement
:789        about the single-cell baseline, NEVER as a discharge of H-B.
:790        Witness: E1_BASELINE_COLLAPSE_UNCERTIFIED.
```

The "as displayed above" of R-L4b is the R.2 display (:759-762): "THE
SECTOR VANISHES IDENTICALLY on the closed pair polydisc — det(1 + A(a)) = 0
for ALL a, not only at a = 0 — so it drops out of numerator AND denominator,
and it is EXCLUDED from every sum, sup and segment rather than being
assigned a value." Context frozen by the same spec: V(0) - 1 =
(phase_mu phase_lambda - 1)P, so the opposite-sector baseline operator is
A(0) = -2CPC (:746-748, re-derived §5 A2a); tr(CPC) = +infinity is C2's own
divergence (:757-758); and — from the S3 deletion grounds, same sealed spec
— ||CPC||_2 is radius-independent by dilation covariance and INFINITE by C6
(:713-715).

### 2.2 What the exact results now on paper actually give (the map)

```text
GIVEN (zero-free chain, CONFIRMED by its check; re-derived independently
here, §5 A1a-A2a, 8/8):
  m-1  m_0 = 0; S_+ = 1/2, S_- = -1/2 (phases (+1,-1,-1) on
       lambda = (0,+sqrt2,-sqrt2), w = (1/2,-1/4,-1/4)).
  m-2  census: 9 pairs = 5 surviving + 4 opposite; surviving weight total
       1/2 signed AND absolute (= S_+^2 + S_-^2); opposite total -1/2
       signed (= 2 S_+ S_-), 1/2 absolute.
  m-3  at the collapse level, CONDITIONAL ON R-L4a/b: N_surv(0) = 1/2;
       c = (1/2, 1/8 x 4), ALL POSITIVE; sum c = 1; sum |c| = kappa_bal = 1;
       Z_hat(0) = 1 exactly.
  m-4  the nonvanishing region |Z_hat| >= 1 - kappa_bal x e^x > 0 on
       {kappa_bal x e^x < 1} (perturbation-of-1, F'-14-compliant).
DIRECTION OF DEPENDENCY (the decisive point): m-3 and m-4 CONSUME the
exclusion — the surviving sum is defined by R-L4b's "EXCLUDED from every
sum". The census cannot discharge the obligation that defines it. ZF says
this itself (o-3); it is confirmed here by exhibiting the failure mode: if
the opposite-sector determinants took a common nonzero limit value d != 0,
the all-pairs baseline would be 1/2 + (-1/2) d != 1/2 — at d = 1 it is
|m_0|^2 = 0 and NO normalization exists. R-L4b is load-bearing for the
whole normalized architecture, not a bookkeeping nicety.
```

Therefore, of the three sealed clauses:

```text
R-L4  (the BOOKKEEPING clause, :786-790): its content — the weight algebra
      independently confirmed, the finite-dimensional identity and
      inequality as REPORTED items — is now on paper TWICE over (ZF Z10,
      and §5 A1/A3/A4 here) and is REPORTED here per the clause's own
      terms: a CANDIDATE partial statement about the single-cell baseline,
      NEVER a discharge of H-B. The reporting duty the clause states is
      met; the witness E1_BASELINE_COLLAPSE_UNCERTIFIED attaches to the
      collapse itself, which stays conditional on R-L4a/b and therefore
      STANDS.
R-L4a (:766-782): PARTIALLY certifiable — see §2.3.
R-L4b (:783-785): NOT certifiable from the discharged basis — see §2.4.
```

### 2.3 R-L4a attempted: what closes, what is the exact residue

**(a) The unregularized half, CERTIFIED (identity-grade, conditional only
on sealed inputs).** CPC = (PC)^dag (PC) >= 0 (P = P^2 = P^dag, C = C^2 =
C^dag). A nonnegative operator is trace class iff its trace is finite;
the sealed input is tr(CPC) = +infinity (:757-758, C2's divergence). Hence
2CPC is not trace class and D = det_{ran C}(1 - 2CPC) DOES NOT EXIST as a
Fredholm determinant: "D IS UNDEFINED" is certified, exactly as the clause
states it. No regularization is consumed by this half; no value of D is
inferred; the stricken inequality is not used.

**(b) The clause's own displayed regularized route CANNOT BE RUN
uncompressed — a sharpening.** The route offered at :779-781
(det = det_2 · e^{tr}, tr = -infinity => det = 0) needs det_2(1 - 2CPC),
which exists only for Hilbert-Schmidt perturbations. But the SAME sealed
spec's S3-deletion grounds state ||CPC||_2 is radius-independent and
INFINITE by C6 (:713-715). So uncompressed, BOTH factors of the Carleman
factorization are undefined — e^{tr} = "e^{-infinity}" AND det_2 diverges
at its hypothesis. The regularized recovery cannot be an uncompressed-level
argument at all: it must be run on a regularized FAMILY, and the only such
family on the discharged basis is the carrier compression C_n (the
n-indexed family of record; the per-pair carrier limit does not exist of
record — ZF §4.1(c), 52f2490b). NAMING that regularization (or another) is
what the clause itself reserves ("WITH THE REGULARIZATION EXPLICITLY
NAMED") and is the spec author's adoption act, consistent with ZF o-3
("they need the named regularization act at the divergent trace"). A lane
cannot supply it; this artifact does not.

**(c) The fixed-n exact structure, derived (the honest content of the
regularized route).** At fixed carrier n, A_n(0) = -2 C_n P C_n is finite
rank; by the R-L1 block-triangular identity (:657-659),

```text
  det(1 + A_n(0)) = det_{ran C_n}(1 - 2 C_n P C_n) = prod_i (1 - 2 s_i),
  s_i = eigenvalues of C_n P C_n on ran C_n, s_i in [0, 1].
```

Exact per-factor facts (§5 A3a/A4a/A4b, CAS): (1-2s)^2 = 1 - 4 s(1-s);
1 - u <= e^{-u} on u >= 0; and the projection commutator identity
||[C_n,P]||_2^2 = 2 tr(C_nPC_n) - 2 tr((C_nPC_n)^2) = 2 sum_i s_i(1-s_i)
(three exact-rational trials here, matching the clause's own 50). Hence at
EVERY fixed n the finite-dimensional inequality

```text
  |det_n(0)|^2 = prod_i (1 - 4 s_i(1-s_i)) <= exp(-2 ||[C_n,P]||_2^2)
```

holds — REPORTED here exactly as the clause permits: a finite-dimensional
inequality, never evaluated at an infinite right-hand side, and NO value
of D inferred from it.

**(d) The exact dichotomy at the compression regularization (the residue
made precise).** Write kappa_n := ||[C_n,P]||_2^2 = 2 sum_i s_i(1-s_i)
(the commutator HS mass).

```text
BRANCH 1 (vanishing): kappa_n -> infinity  =>  det_n(0) -> 0
   (sufficient, by (c); this is the honest regularized recovery of
   "det = 0" that R-L4b would consume).
BRANCH 2 (non-vanishing): sup_n kappa_n = 2M < infinity AND uniform
   1/2-avoidance (exists delta > 0 with |s_i - 1/2| >= delta for all i, n)
   =>  |det_n(0)|^2 >= (4 delta^2)^{M/U} > 0 uniformly, U := 1/4 - delta^2
   (chord bound from concavity of log(1-4u), §5 A5a/A5b: on u in [0,U],
   log(1-4u) >= (u/U) log(4 delta^2); sum over i and use sum u_i <= M).
   On this branch det_n(0) is BOUNDED AWAY from zero: R-L4b is FALSE at
   the compression regularization, and §2.2's failure mode is live.
NEITHER BRANCH IS SEALED. The sealed divergence tr(C_nPC_n) -> infinity
does NOT decide between them: trace mass carried by eigenvalues near 1
contributes factors of modulus near 1 (det NOT -> 0; sign oscillates as
(-1)^{#{s_i > 1/2}}); trace mass carried by eigenvalues near 0 or spread
across (0,1) drives kappa_n -> infinity (det -> 0). Both are consistent
with every sealed input. The commutator mass kappa_n under the named
regularization is a NEW estimate obligation, on no discharged artifact.
```

**R-L4a VERDICT: PARTIAL.** Certified: the unregularized non-existence
half ("D IS UNDEFINED"), at identity grade from sealed inputs; plus the
sharpening (b) that the uncompressed regularized display is also empty,
which narrows the admissible regularizations to named families (carrier
compression the only one of record). Exact residue: (r-1) the
regularization-naming act — the spec author's, not a lane's; (r-2) the
behavior of kappa_n = ||[C_n,P]||_2^2 under that regularization — the
dichotomy of (d) is exact and unsealed on both branches. Witness
E1_BASELINE_DETERMINANT_EXISTENCE_UNCERTIFIED STANDS (the clause demands
the existence-or-nonexistence determination WITH the regularization named;
(r-1) is not a lane's to give).

### 2.4 R-L4b attempted: NOT-DERIVABLE, the act named

At any FIXED n, det(1 + A_n(a)) is entire in (a_+, a_-) and equals
prod(1 - 2 s_i) != 0 at a = 0 unless some s_i = 1/2 exactly: identical
vanishing on the closed pair polydisc is IMPOSSIBLE at fixed n except on a
spectral coincidence no sealed input supplies. R-L4b can therefore only be
a statement about the regularized LIMIT — i.e., it sits strictly behind
R-L4a's residue (r-1)/(r-2). For the a-dependence: where 1 + A_n(0) is
invertible,

```text
  det(1 + A_n(a)) = det(1 + A_n(0)) · det(1 + R_n Delta_n(a)),
  R_n = (1 + A_n(0))^{-1},   |det(1 + R_n Delta_n(a))| <= e^{||R_n Delta_n(a)||_1},
```

so identical-in-a vanishing in the limit needs det_n(0) -> 0 (Branch 1)
AND the joint rate ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|) uniformly on
the closed pair polydisc. The resolvent factor R_n is exactly the
opposite-sector blow-up S4 records as written (:720-727: the segment
starts at 1 - 2CPC; the unrestricted sup is +infinity as written); the
joint rate couples the baseline degeneration to the source response and
appears on NO discharged artifact. **R-L4b VERDICT: NOT-DERIVABLE (acts:
the R-L4a regularization naming + the kappa_n divergence input + the joint
baseline/source rate on the polydisc). MAY-FAIL is live and now exact: on
Branch 2 R-L4b is FALSE at the compression regularization, and with it the
5/4 census, N_surv(0) = 1/2, and kappa_bal = 1 all require re-derivation
over the full 9-pair sum (§2.2's failure mode).** Witness
E1_OPPOSITE_PHASE_SECTOR_VANISHING_UNCERTIFIED STANDS.

---

## 3. TASK B — R-L0, THE CELL-SCALE NORMALIZATION (MAY-FAIL)

### 3.1 The sealed clause and its quantifier, located at their bytes

R-L0's operative demand (E1 :854-858): "Exhibit X_*(eps), a functional of
(||b_D||, tau_R, sea-kernel decay data, |w_lambda|) ONLY, with x(C,eps) =
|C|_4 g(C,eps) <= X_*(eps) for EVERY admitted cell of D3, and with
kappa_bal · X_*(eps) · e^{X_*(eps)} <= 1/2 certified (the 1/2 is the
parent's frozen E1 threshold, re-used; NO new number)." Two admissible
grounds only (:859-866): (i) SCALE COVARIANCE (g carries a compensating
negative scaling weight so |C|_4 g is scale-invariant); (ii) UNIFORM
SMALLNESS in the cell scale, per cell. The hazard, sealed (:867-872):
"tau_R is scale-INVARIANT — every refined cell inserts a FULL record cycle
at every refinement depth, at exact phase e^{+-i pi} = -1 ... it is
therefore NOT a priori true that g carries the compensating weight, and
R-L0 MAY FAIL. NAMED WITNESS: E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED."
Forbidden substitutions (:873-875): any coarsest-cell 4-volume, V_max,
skeleton datum, or cellulation-family quantity for X_*.

**THE QUANTIFIER (the tasked question, answered at the bytes):** R-L0
quantifies over "EVERY admitted cell of D3" (:856); the sup over cells is
exactly the sup DEFERRED at :687-689 ("the sup over CELLS is deferred to
R-L0"); and D3 is the FROZEN QUANTIFIER READING (:417-426): "ANY
cellulation refining BOTH parents — UNRESTRICTED, UNIVERSALLY QUANTIFIED.
It is NOT the minimal overlay." So the class is the UNBOUNDED
common-refinement cell family — NOT the single entered cell. The entered
cell of the PA executor (the "b_1 = 1" designation of the tasking maps to
this cell and appears nowhere in the sealed sources; its sealed data are
b_D(1/2,0) = 1, ||b_D||_inf = 1) is the unit causal diamond 0 < t < 1,
|x| < r(t) = min(t,1-t), with v(t) = (pi/sqrt2) 32 r(t)^3 (PA :86-88),
int_0^1 v = tau_R = pi/sqrt2 EXACTLY, and |C|_4 = pi/24 EXACTLY (§5
B1a/B2a). The working class = every admitted cell of every unrestricted
D3 refinement, each carrying its own diamond profile with ||b_D^{(c)}||_inf
= 1 exactly (E1 :864) and each inserting the SAME full record cycle
tau_R = pi/sqrt2 (the hazard).

### 3.2 x's exact symbolic form — and why it does not close

The sealed form (R.3, :806-808 with S1/S2/S2b/S4 at :691-727):

```text
  x(C, eps) = |C|_4 · g(C, eps),
  g = G_tr + rho_res · G_hs · (G_cm + G_hs),
  G_tr = |C|_4^{-1} |tr_{ran C}[C(V(a) - V(0))C]| ,
  G_hs = |C|_4^{-alpha} ||C(V(a) - V(0))C||_2 ,
  G_cm = |C|_4^{-beta} (certified bound on the C-L2 commutator error),
  rho_res = sup_{s in [0,1]} ||(1 + A_s(a))^{-1}||_op   (surviving sector).
Equivalently, exponent-explicit:
  x = |tr[C Delta C]| + rho_res ( ||C Delta C||_2 · Gcm_num · |C|_4^{1-alpha-beta}
                                 + ||C Delta C||_2^2 · |C|_4^{1-2 alpha} ),
with the |C|_4 powers cancelling IFF the R-L2b exponents balance
(alpha + beta = 1, alpha = 1/2) — which is BOOKKEEPING CONDITIONAL ON
R-L2b of record (:817-824), not a fact.
```

Four ingredients are UNVALUED on the discharged basis, each with a sealed
status that forbids filling it here:

```text
u-1  alpha, beta: SYMBOLS until R-L2b closes (":698-700: v001 wrote 1/2
     without derivation; R-L2b must DERIVE it. Until R-L2b closes, alpha
     is a symbol, not 1/2"). R-L2b is OPEN (witness
     SCAD_HS_SCALING_EXPONENT_UNDERIVED).
u-2  rho_res: NAMED HYPOTHESIS H-R, "NOT discharged here. May NEVER
     default to 1 or to any other value (F'-3)" (:725-727).
u-3  G_cm: the C-L2 commutator error's certified bound — form itself
     uncertified (witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED,
     :708).
u-4  the S2 ingredient per-pair carrier sup: +infinity OF RECORD
     (52f2490b, verified at path: RL2b_uniformity_refuted = true, no
     admissible M; ZF o-1: at fixed carrier n the chain closes with x_n
     finite but n-DEPENDENT). The input x therefore exists only as the
     fixed-n family x_n unless the summed-S2' act (the spec author's,
     AQ §2.3(iii) of record) replaces the per-pair input.
```

**CONSEQUENCE (the tasked question answered): x is NOT a cell-independent
exact constant for the sealed class — it is not even a DETERMINATE
symbolic expression.** There is no exact symbolic form of x to check
against the threshold, for the entered cell or any other. And the class
quantified over is the unbounded D3 family (§3.1), on which the cell-sup
of x_n is exactly the R-L2b scaling question: if the true scaling exponent
falls below the bookkept one, the sup DIVERGES under refinement (:819-824
exhibits alpha = 0 doing exactly that for the deleted S3).

### 3.3 What IS derivable exactly: the threshold algebra, and the sharpened MAY-FAIL

**(a) The threshold, exact.** With kappa_bal = 1 (derived, conditional on
R-L4b — §2.2 m-3), R-L0's certification display is X_* e^{X_*} <= 1/2.
u e^u is strictly increasing on u >= 0 ((1+u)e^u > 0, §5 B4a); the CAS
exact solve gives the unique root of u e^u = 1/2 as u = W(1/2) (Lambert W,
§5 B4b); and W(1/2) < 1/2 because (1/2)e^{1/2} > 1/2 (§5 B4c). So R-L0
demands, exactly:

```text
  X_*(eps) <= W(1/2) < 1/2 .
```

**(b) The one scale-invariant constant on the discharged basis, and its
exact failure of the threshold.** The discharged Dyson domination (R2
:175-181, :330) is (|lambda| Theta)^n / n! uniformly in omega, using ONLY
|lambda| <= sqrt2 and Theta <= tau_R; the all-orders dominator is
e^{sqrt2 tau_R} symbolic (REM flag block). The scale-invariant log-scale
of the full record cycle is therefore

```text
  sqrt2 · tau_R = sqrt2 · pi/sqrt2 = pi   EXACTLY (§5 B3a),
and   pi · e^{pi} > 1/2   SYMBOLICALLY (§5 B4d: pi > 1/2 and e^{pi} > 1).
```

FENCE REASONING, stated explicitly: these are orderings of exact symbolic
mathematical constants (pi, e, W(1/2)) internal to the sealed threshold
display — structural arithmetic on the derivation's own constants, not
computation, bounding, or evaluation of any physical observable; no
decimal value was produced or used; each comparison is decided by an exact
monotonicity chain (e^u > 1 for u > 0; pi > 1/2; u e^u increasing). This
does not cross the evaluation fence. Had the check required a VALUE of x,
it would have been reported REQUIRES-EVALUATION; it never got that far —
the blocker is §3.2 (missing derivations), which is upstream of any
evaluation question.

**(c) The MAY-FAIL, sharpened to an exact conditional.** The sealed hazard
(:867-872) is that every admitted cell at every refinement depth inserts
the FULL record cycle (tau_R scale-invariant, exact phase -1). The exact
consequence of (a)+(b): IF the record cycle enters the source-response
majorant x undamped at any admitted depth — i.e., if x inherits the
scale-invariant log-scale pi (or any constant >= 1/2) on some admitted
cell — then kappa_bal x e^x > 1/2 THERE and R-L0's inequality FAILS at
that cell. Ground (ii) (uniform smallness) is therefore DEAD at the
record-cycle scale; ground (i) (the compensating negative scaling weight,
whose derivation is exactly R-L2b's named mechanism — cell time extent
from the Duhamel bound and the Gevrey b_D — :826-831) is the ONLY live
ground, exactly as the hazard's "structural root of O7" framing predicts.
What keeps R-L0 alive: x measures V(a) - V(0), and on the surviving sector
V(0) = I exactly — the record cycle cancels at baseline and re-enters only
through the source difference, which carries the polydisc radius eps and
the Gevrey vertex. Whether that damping delivers the compensating weight
cell-uniformly over unrestricted D3 is precisely R-L2b + the cell-sup —
underived, and refuted per-pair at the carrier axis (u-4).

### 3.4 R-L0 verdict

```text
RL0 = NOT-DERIVABLE. The act(s), named exactly:
  a-1  R-L2b: derive alpha (and beta) with the mechanism exhibited
       (SCAD_HS_SCALING_EXPONENT_UNDERIVED) — ground (i)'s substance;
  a-2  discharge or bound H-R (rho_res) on the surviving sector;
  a-3  certify the C-L2 commutator form feeding G_cm
       (E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED);
  a-4  the summed-S2' spec act replacing the per-pair carrier input
       REFUTED at 52f2490b (the spec author's act, AQ §2.3(iii) of record).
NOT REQUIRES-EVALUATION: no numeric evaluation was reached or needed; the
inequality has no symbolic instance to evaluate (§3.2).
NOT FAILS (yet): no admitted cell exhibits a derived x violating the
threshold — but the failure branch is now EXACT (§3.3(c)): undamped
record-cycle entry at any depth fails the inequality symbolically. The
MAY-FAIL is live, sharpened, and sits entirely on a-1.
DELIVERED EXACTLY MEANWHILE: the quantifier pinned at its bytes (D3
unrestricted, :856 + :687-689 + :417-426); the threshold algebra
X_* <= W(1/2) < 1/2; the entered cell's exact data (|C|_4 = pi/24,
int v = tau_R = pi/sqrt2, ||b_D||_inf = 1); the exact failure conditional.
Witness E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED STANDS.
```

### 3.5 R-L0b, located and shaped (E1 :877-885, register :1010)

The sealed alternative, in full: "R-L0b EXPONENTIATION-FREE COLOUR-SUM
CLOSURE (new; B-5). The requirement for an absolute bound on x arises
SOLELY from the colour-sum step (the |rho - 1| <= x e^x exponentiation and
the 1/(1 - kappa x e^x) denominator); THE LINEAR CARLEMAN ESTIMATE ALONE
ALREADY YIELDS ACTION-DENSITY FORM. Bound Log(sum_surviving c rho) WITHOUT
an absolute bound on x — e.g. by a signed-weight convexity argument on the
surviving sector. If R-L0b closes, R-L0 is not needed for the n = 1 leg
and that must be stated. Witness: SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND."

Its shape, with the derived census applied:

```text
NEW FOOTING (derived, conditional on R-L4b): ALL FIVE c ARE POSITIVE
  (c = (1/2, 1/8 x 4), §5 A1e) with sum c = 1 — so Z_hat(a) =
  sum_surv c rho(a) is a TRUE CONVEX COMBINATION of the five per-pair
  rho's. The clause's "signed-weight convexity argument" upgrades to
  genuine convexity: no sign cancellation is needed on the surviving
  sector. Immediate consequences: Z_hat(a) lies in the convex hull of
  {rho_{mu lambda}(a)}; |Z_hat - 1| <= max_surv |rho - 1| (no kappa_bal
  slack is even consumed, since kappa_bal = 1).
THE RESIDUE (what R-L0b would take instead of R-L0): control of Log of a
  convex combination without |rho - 1| <= x e^x requires the five rho's
  localized in a fixed zero-free star-shaped region about 1 (e.g. a
  half-plane Re rho > 0) ON the closed pair polydisc, supplied by the
  LINEAR Carleman estimate alone (Log rho = tr[Delta] - int_0^1
  tr[(1+A_s)^{-1} A_s Delta] ds, :663-666) — i.e., per-pair PHASE
  localization not routed through an absolute x bound. That localization
  is not on any discharged artifact; it consumes rho_res (H-R) through
  the same segment. R-L0b remains OPEN; it inherits u-2 (H-R) and the
  surviving-sector definition (R-L4b) but NOT u-1/u-4's uniformity axis —
  which is exactly why it is the named alternative for the n = 1 leg.
RL0B VERDICT: located; NOT closed here; the convexity footing is now
  derived rather than hypothesized; the remaining act is the per-pair
  phase-localization estimate (plus H-R), with the explicit obligation, if
  it closes, to state that R-L0 is not needed for n = 1.
```

---

## 4. NET — WHAT CLOSES, WHAT REMAINS

```text
CLOSED-AS-REPORTED HERE: R-L4's bookkeeping clause content (census, weight
  algebra, finite-dimensional identity/inequality as REPORTED items;
  candidate partial statement; H-B untouched).
CERTIFIED HERE: the unregularized half of R-L4a ("D IS UNDEFINED",
  identity-grade from sealed tr(CPC) = +infinity); the sharpening that
  det_2 also fails uncompressed (||CPC||_2 = infinity of record), so the
  clause's displayed regularized route is empty until a regularization is
  NAMED; the fixed-n eigenvalue form and the exact vanishing dichotomy
  (kappa_n -> infinity sufficient for det_n -> 0; bounded kappa_n +
  1/2-avoidance forces non-vanishing); R-L0's threshold algebra
  (X_* <= W(1/2) < 1/2) and the exact MAY-FAIL conditional
  (undamped record-cycle scale pi fails the threshold symbolically).
STANDS UNCERTIFIED (witnesses intact): R-L4a as a whole (the naming act +
  the kappa_n input); R-L4b (NOT-DERIVABLE; live failure branch that would
  re-open the 5/4 census); R-L0 (NOT-DERIVABLE; acts a-1..a-4; MAY-FAIL
  sharpened onto R-L2b); R-L0b (open; convexity footing improved).
NO GATE MOVES; NO FLAG FLIPS; NO NEW OBSTRUCTION; NO NEW ACT CREATED —
  every residue lands on already-named acts (the R-L4a regularization
  naming, R-L2b, H-R, C-L2/G_cm, summed-S2') plus one already-implied
  estimate input now made exact (kappa_n = ||[C_n,P]||_2^2 behavior),
  which is the commutator-mass content ALREADY quoted inside R-L4a's own
  clause (:775-776) — named there, quantified here.
CONSISTENT with ZF/ZF-CHECK's landing set: summed-S2' + R-L4a/b +
  R-L0(/R-L0b) + C-L2/G_cm — this artifact certifies the certifiable
  interior of the two named members and leaves the set's membership
  unchanged.
```

---

## 5. THE CAS SCRIPT (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `rl4venv`. Exact symbolic / exact rational only.

```python
# RL4-RL0 CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv rl4venv).
# Every constant symbolic or exact rational/surd. Nothing numeric evaluated.
# Random trials use EXACT RATIONAL matrices (seeded, reproducible).
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

# ---------- TASK A — R-L4a/b support ----------
# A1  Census / weight algebra (E1 :745-762, re-derived from the sealed weights)
w = {0: R(1,2), 1: R(-1,4), 2: R(-1,4)}          # lambda = (0, +sqrt2, -sqrt2)
ph = {0: 1, 1: -1, 2: -1}                          # phases (+1,-1,-1)
m0 = sum(w.values())
Sp_ = sum(w[l] for l in w if ph[l] == +1)
Sm_ = sum(w[l] for l in w if ph[l] == -1)
pairs = [(m, l) for m in w for l in w]
surv = [(m, l) for (m, l) in pairs if ph[m] == ph[l]]
opp  = [(m, l) for (m, l) in pairs if ph[m] != ph[l]]
ok("A1a m_0 = 0 ; S_+ = 1/2 ; S_- = -1/2                              ",
   m0 == 0 and Sp_ == R(1,2) and Sm_ == R(-1,2))
ok("A1b census 9 pairs = 5 surviving + 4 opposite                     ",
   len(pairs) == 9 and len(surv) == 5 and len(opp) == 4)
same_signed = sum(w[m]*w[l] for (m, l) in surv)
same_abs    = sum(abs(w[m]*w[l]) for (m, l) in surv)
opp_signed  = sum(w[m]*w[l] for (m, l) in opp)
opp_abs     = sum(abs(w[m]*w[l]) for (m, l) in opp)
ok("A1c surviving totals: signed = abs = 1/2 = S_+^2 + S_-^2          ",
   same_signed == R(1,2) and same_abs == R(1,2) and Sp_**2 + Sm_**2 == R(1,2))
ok("A1d opposite totals: signed = -1/2 = 2 S_+ S_- ; abs = 1/2        ",
   opp_signed == R(-1,2) and 2*Sp_*Sm_ == R(-1,2) and opp_abs == R(1,2))
Nsurv = same_signed                                # collapse level: det(1+A(0)) = 1 same-phase
c = {p: w[p[0]]*w[p[1]]/Nsurv for p in surv}
ok("A1e N_surv(0) = 1/2 ; c = (1/2, 1/8 x 4), ALL > 0                 ",
   Nsurv == R(1,2) and c[(0,0)] == R(1,2)
   and sorted(c.values()) == [R(1,8)]*4 + [R(1,2)] and all(v > 0 for v in c.values()))
ok("A1f sum c = 1 ; sum |c| = kappa_bal = 1                           ",
   sum(c.values()) == 1 and sum(abs(v) for v in c.values()) == 1)

# A2  Sector algebra: V(0) - 1 = (ph_mu ph_lambda - 1) P  (E1 :746-748)
Pp = sp.MatrixSymbol('P', 3, 3)
ok("A2a same-phase factor (ph ph - 1) = 0 ; opposite = -2             ",
   all((ph[m]*ph[l] - 1) == 0 for (m, l) in surv)
   and all((ph[m]*ph[l] - 1) == -2 for (m, l) in opp))

# A3  Projection commutator identity ||[C,P]||_F^2 = 2 tr(CPC) - 2 tr((CPC)^2)
#     = 2 sum s_i(1 - s_i)  — EXACT RATIONAL trials (orthogonal projections).
import random
random.seed(20260814)
def rand_proj(n, k):
    while True:
        B = sp.Matrix(n, k, lambda i, j: R(random.randint(-3, 3), random.randint(1, 3)))
        G = B.T * B
        if G.det() != 0:
            return B * G.inv() * B.T
trials_ok = True
for (n, k1, k2) in [(4, 2, 2), (5, 2, 3), (6, 3, 2)]:
    C = rand_proj(n, k1); P = rand_proj(n, k2)
    lhs = sp.trace((C*P - P*C).T * (C*P - P*C))            # ||[C,P]||_F^2 (real symmetric)
    rhs = 2*sp.trace(C*P*C) - 2*sp.trace((C*P*C)**2)       # = 2 sum s_i(1-s_i)
    trials_ok = trials_ok and sp.simplify(lhs - rhs) == 0
ok("A3a ||[C,P]||_F^2 = 2 tr(CPC) - 2 tr((CPC)^2), 3 exact trials     ", trials_ok)

# A4  Per-factor identities behind |det|^2 <= exp(-2||[C,P]||_2^2) at FIXED n
s, u, U = sp.symbols('s u U', real=True)
ok("A4a (1 - 2s)^2 = 1 - 4 s(1-s)  (exact identity)                   ",
   sp.expand((1 - 2*s)**2 - (1 - 4*s*(1 - s))) == 0)
f = sp.exp(-u) - (1 - u)                                   # f(0)=0, f' = 1 - e^{-u} >= 0 on u>=0
# ground for f' >= 0 on u >= 0: (e^u)' = e^u with e^0 = 1, so every Taylor
# coefficient of e^u - 1 is 1/n! > 0 (n >= 1) => e^u >= 1 => e^{-u} <= 1 on u >= 0.
ok("A4b f(0) = 0 ; f' = 1 - e^{-u} ; (e^u)' = e^u, e^0 = 1 (coeffwise)",
   f.subs(u, 0) == 0 and sp.simplify(sp.diff(f, u) - (1 - sp.exp(-u))) == 0
   and sp.simplify(sp.diff(sp.exp(u), u) - sp.exp(u)) == 0 and sp.exp(0) == 1)
# hence prod(1-4u_i) <= exp(-4 sum u_i): per-factor 1-4u <= e^{-4u}, product monotone.

# A5  Concavity for the bounded-mass LOWER bound (the R-L4b failure branch)
g5 = sp.log(1 - 4*u)
ok("A5a d2/du2 log(1-4u) = -16/(1-4u)^2 < 0 on 0 <= u < 1/4           ",
   sp.simplify(sp.diff(g5, u, 2) + 16/(1 - 4*u)**2) == 0)
# chord (concavity): log(1-4u) >= (u/U) log(1-4U) on [0,U], U < 1/4; endpoints exact:
ok("A5b chord equality at u = 0 and u = U                             ",
   g5.subs(u, 0) == 0 and sp.simplify(g5.subs(u, U) - (U/U)*sp.log(1-4*U)) == 0)

# ---------- TASK B — R-L0 support ----------
t, rho = sp.symbols('t rho', real=True, nonnegative=True)
tau_R = sp.pi/sp.sqrt(2)

# B1  int_0^1 32 min(t,1-t)^3 dt = 1  =>  int v = tau_R = pi/sqrt2 EXACT (PA :86-88)
I1 = sp.integrate(32*t**3, (t, 0, R(1,2))) + sp.integrate(32*(1-t)**3, (t, R(1,2), 1))
ok("B1a int_0^1 32 r(t)^3 dt = 1 ; int v = tau_R = pi/sqrt2           ",
   I1 == 1 and sp.simplify(tau_R*I1 - sp.pi/sp.sqrt(2)) == 0)

# B2  |C|_4 of the entered unit diamond (spatial ball radius r(t) in R^3):
V4 = sp.integrate(R(4,3)*sp.pi*t**3, (t, 0, R(1,2))) \
   + sp.integrate(R(4,3)*sp.pi*(1-t)**3, (t, R(1,2), 1))
ok("B2a |C|_4(entered cell) = pi/24 EXACT                             ",
   sp.simplify(V4 - sp.pi/24) == 0)

# B3  Theta = tau_R (1 - 16 rho^4) on rho in [0,1/2]: range [0, tau_R];
#     sup |lambda| Theta = sqrt2 * tau_R = pi EXACT (R2 :180, :330)
Theta = tau_R*(1 - 16*rho**4)
ok("B3a Theta(0) = tau_R ; Theta(1/2) = 0 ; sqrt2*tau_R = pi EXACT    ",
   sp.simplify(Theta.subs(rho, 0) - tau_R) == 0 and Theta.subs(rho, R(1,2)) == 0
   and sp.simplify(sp.sqrt(2)*tau_R - sp.pi) == 0)

# B4  Threshold algebra of R-L0: u e^u strictly increasing on u >= 0;
#     root of u e^u = 1/2 is W(1/2) < 1/2; and pi e^pi > 1/2 (record-cycle scale FAILS)
ok("B4a d/du (u e^u) = (1+u) e^u > 0 for u >= 0 (strict increase)     ",
   sp.simplify(sp.diff(u*sp.exp(u), u) - (1 + u)*sp.exp(u)) == 0
   and sp.ask(sp.Q.positive((1 + u)*sp.exp(u)), sp.Q.nonnegative(u)))
Wh = sp.LambertW(R(1,2))
ok("B4b CAS exact solve: {u : u e^u = 1/2} = {LambertW(1/2)}          ",
   sp.solve(u*sp.exp(u) - R(1,2), u) == [Wh])
# (1/2) e^{1/2} > 1/2  <=>  e^{1/2} > 1  <=>  1/2 > 0 : so root W(1/2) < 1/2 by B4a
ok("B4c e^{1/2} > 1 (so (1/2)e^{1/2} > 1/2, hence W(1/2) < 1/2)       ",
   bool(sp.exp(R(1,2)) > 1))
ok("B4d pi > 1/2 and e^pi > 1  =>  pi e^pi > 1/2 (symbolic ordering)  ",
   bool(sp.pi > R(1,2)) and bool(sp.exp(sp.pi) > 1))

# B5  kappa_bal = 1 carried from A1f: the R-L0 display reads X_* e^{X_*} <= 1/2.
ok("B5a with kappa_bal = 1 the threshold is X_* <= W(1/2), unique root ",
   sum(abs(v) for v in c.values()) == 1)

print("DONE")
```

Output, verbatim (20/20 PASS):

```text
A1a m_0 = 0 ; S_+ = 1/2 ; S_- = -1/2                              : PASS
A1b census 9 pairs = 5 surviving + 4 opposite                     : PASS
A1c surviving totals: signed = abs = 1/2 = S_+^2 + S_-^2          : PASS
A1d opposite totals: signed = -1/2 = 2 S_+ S_- ; abs = 1/2        : PASS
A1e N_surv(0) = 1/2 ; c = (1/2, 1/8 x 4), ALL > 0                 : PASS
A1f sum c = 1 ; sum |c| = kappa_bal = 1                           : PASS
A2a same-phase factor (ph ph - 1) = 0 ; opposite = -2             : PASS
A3a ||[C,P]||_F^2 = 2 tr(CPC) - 2 tr((CPC)^2), 3 exact trials     : PASS
A4a (1 - 2s)^2 = 1 - 4 s(1-s)  (exact identity)                   : PASS
A4b f(0) = 0 ; f' = 1 - e^{-u} ; (e^u)' = e^u, e^0 = 1 (coeffwise): PASS
A5a d2/du2 log(1-4u) = -16/(1-4u)^2 < 0 on 0 <= u < 1/4           : PASS
A5b chord equality at u = 0 and u = U                             : PASS
B1a int_0^1 32 r(t)^3 dt = 1 ; int v = tau_R = pi/sqrt2           : PASS
B2a |C|_4(entered cell) = pi/24 EXACT                             : PASS
B3a Theta(0) = tau_R ; Theta(1/2) = 0 ; sqrt2*tau_R = pi EXACT    : PASS
B4a d/du (u e^u) = (1+u) e^u > 0 for u >= 0 (strict increase)     : PASS
B4b CAS exact solve: {u : u e^u = 1/2} = {LambertW(1/2)}          : PASS
B4c e^{1/2} > 1 (so (1/2)e^{1/2} > 1/2, hence W(1/2) < 1/2)       : PASS
B4d pi > 1/2 and e^pi > 1  =>  pi e^pi > 1/2 (symbolic ordering)  : PASS
B5a with kappa_bal = 1 the threshold is X_* <= W(1/2), unique root : PASS
DONE
```

Note on A3a: three exact-rational trials here against the clause's own 50
(:775-776); the identity is also an operator algebra fact for orthogonal
projections (cyclic trace rewriting: ||[C,P]||_2^2 = 2 tr(CP) - 2 tr((CP)^2)
with tr(CP) = tr(CPC) = sum s_i and tr((CP)^2) = tr((CPC)^2) = sum s_i^2).
Note on B4d: sympy's exact relational decision; grounded independently by
the stated monotonicity chain (pi > 1/2, e^{pi} > e^0 = 1).

---

## 6. FLAG BLOCK

```text
RL4AB_CLAUSES = E1 :766-790, quoted in full in §2.1. Operative cores:
  R-L4a (:766-782): "Establish, via the Carleman factorization det = det_2
    · e^{tr} AND WITH THE REGULARIZATION EXPLICITLY NAMED, whether the
    opposite-phase baseline determinant EXISTS ... the correct
    unregularized statement is 'D IS UNDEFINED', NOT 'D = 0' ... THE
    INEQUALITY D^2 <= exp(-2||[C,P]||_2^2) IS STRICKEN AS A GROUND FOR
    D = 0 ... [regularized recovery] IS THE ONE R-L4b MUST MAKE."
    Witness E1_BASELINE_DETERMINANT_EXISTENCE_UNCERTIFIED.
  R-L4b (:783-785): "OPPOSITE-PHASE SECTOR VANISHES IDENTICALLY ... on the
    closed pair polydisc, with the regularization of R-L4a." (display
    :759-762: det(1+A(a)) = 0 for ALL a; EXCLUDED from every sum, sup,
    segment). Witness E1_OPPOSITE_PHASE_SECTOR_VANISHING_UNCERTIFIED.
  R-L4 (:786-790): "BASELINE BOOKKEEPING, REPORTED PER STATE ... CANDIDATE
    partial statement ... NEVER as a discharge of H-B." Witness
    E1_BASELINE_COLLAPSE_UNCERTIFIED.
RL4AB = PARTIAL(certified: R-L4's bookkeeping content reported per its own
  terms (census 5/4, S_± = ±1/2, totals ±1/2, N_surv(0) = 1/2,
  c = (1/2, 1/8 x 4) all positive, kappa_bal = 1 — conditional on the
  exclusion); the unregularized half of R-L4a ("D IS UNDEFINED",
  identity-grade from sealed tr(CPC) = +infinity); the sharpening that
  det_2 also fails uncompressed (||CPC||_2 = infinity of record, E1
  :713-715) so the displayed regularized route is empty until a
  regularization is NAMED; the fixed-n form det_n(0) = prod(1 - 2 s_i)
  with the exact dichotomy — kappa_n := ||[C_n,P]||_2^2 -> infinity
  suffices for det_n(0) -> 0, bounded kappa_n + uniform 1/2-avoidance
  forces |det_n(0)| >= (4 delta^2)^{M/(2U)} > 0.
  EXACT RESIDUE: (r-1) the regularization-naming act (spec author's, per
  the clause and ZF o-3); (r-2) the behavior of kappa_n under it — unsealed
  on both branches; (r-3) for R-L4b additionally the joint rate
  ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|) on the polydisc. R-L4b itself
  NOT-DERIVABLE(the acts r-1..r-3); on the bounded-mass branch R-L4b is
  FALSE at the compression regularization and the 5/4 census re-opens.
  All three witnesses STAND.)
RL0_QUANTIFIER = "for EVERY admitted cell of D3" at E1 :856; the cell-sup
  deferred at :687-689; D3 = the FROZEN unrestricted common-refinement
  quantifier at :417-426 ("ANY cellulation refining BOTH parents —
  UNRESTRICTED, UNIVERSALLY QUANTIFIED; NOT the minimal overlay"). An
  UNBOUNDED family — not the entered cell. (The tasking's "b_1 = 1"
  designation is not in the sealed sources; it maps to the entered PA
  unit-diamond cell, b_D(1/2,0) = 1, ||b_D||_inf = 1, |C|_4 = pi/24
  exact, int v = tau_R = pi/sqrt2 exact.)
X_FORM = x(C,eps) = |C|_4 (G_tr + rho_res G_hs (G_cm + G_hs)) [E1
  :806-808], S1/S2/S2b/S4 at :691-727; exponent-explicit: x =
  |tr[C Delta C]| + rho_res(||C Delta C||_2 Gcm_num |C|_4^{1-alpha-beta} +
  ||C Delta C||_2^2 |C|_4^{1-2 alpha}), powers cancel IFF R-L2b's
  exponents balance (conditional bookkeeping, :817-824). CELL-DEPENDENCE:
  NOT a cell-independent constant and NOT a determinate symbolic
  expression — four unvalued items of record: alpha/beta symbols (R-L2b
  open), rho_res = H-R (may never default), G_cm form uncertified (C-L2
  witness), and the per-pair carrier sup of the S2 ingredient +infinity
  OF RECORD (52f2490b) — x exists only as the fixed-n family x_n, whose
  D3 cell-sup is exactly the R-L2b scaling question.
RL0 = NOT-DERIVABLE(acts: R-L2b (alpha/beta with mechanism); H-R
  discharge/bound; C-L2/G_cm certification; summed-S2' replacement of the
  refuted per-pair input — all already-named). NOT REQUIRES-EVALUATION:
  no numeric evaluation was reached or needed; the inequality has no
  symbolic instance to check. NOT FAILS yet — but the failure branch is
  EXACT: kappa_bal = 1 forces X_* <= W(1/2) < 1/2 (Lambert W, unique
  root, CAS-exact), the one scale-invariant log-majorant constant on the
  discharged basis is sqrt2 tau_R = pi exactly, and pi e^pi > 1/2
  SYMBOLICALLY — undamped record-cycle entry at any admitted depth fails
  the inequality (ground (ii) dead at that scale; ground (i) = R-L2b the
  only live ground; the sealed O7-root hazard made exact). Witness
  E1_CELL_SCALE_NORMALIZATION_UNCERTIFIED STANDS.
RL0B = located at E1 :877-885 (register :1010): exponentiation-free
  colour-sum closure — bound Log(sum_surv c rho) WITHOUT an absolute x
  bound (the need arises solely from |rho-1| <= x e^x and the
  1/(1 - kappa x e^x) denominator; the linear Carleman estimate already
  yields action-density form). NEW FOOTING derived: all five c POSITIVE,
  sum c = 1 — a TRUE convex combination (conditional on R-L4b); "signed-
  weight convexity" upgrades to genuine convexity. RESIDUE: per-pair
  phase localization (rho's in a fixed star-shaped zero-free region about
  1 on the polydisc) from the linear estimate alone, plus H-R; if closed,
  must state R-L0 not needed for the n = 1 leg. OPEN.
NET = R-L4 bookkeeping reported per its own terms; R-L4a's unregularized
  half certified + regularized route narrowed to named-family form with
  the kappa_n dichotomy exact; R-L4b and R-L0 NOT-DERIVABLE with all acts
  named and both MAY-FAIL branches now exact conditionals; R-L0b's
  convexity footing derived. No gate moves, no flag flips, no new
  obstruction, no new act — residues land on already-named acts plus the
  kappa_n input already quoted inside R-L4a's clause text. Landing set of
  record (ZF-CHECK) unchanged: summed-S2' + R-L4a/b + R-L0(/R-L0b) +
  C-L2/G_cm.
SEALS_VERIFIED = 26f9314b (ZF), aed551e3 (ZF-CHK), 46846730 (E1),
  2e4e5163 (REM), 89641f76 (REM-CHK), a7969f0c (R2), a4f2e1b7 (R2-CHK),
  789338ad (PA), 52f2490b (per-pair refutation) — all recomputed from
  bytes at path this session; none unverifiable.
FORBIDDEN_IMPORTS = none (no scale, no GR, no faithfulness; no
  register/tracker/plan/road/ledger/lens read; M(t) sharp; equal-time
  FALSE by C6; no coarsest-cell/V_max/skeleton datum substituted for X_*;
  the stricken D^2-display used only as the fixed-n REPORTED item its own
  clause permits, never evaluated at an infinite right side, no value of
  D inferred).
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, exact symbolic/rational only,
  script + output verbatim in §5 (20/20 PASS).
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
