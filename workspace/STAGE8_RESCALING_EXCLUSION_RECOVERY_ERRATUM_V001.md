# Stage 8 Rescaling Exclusion Recovery Erratum v001

Date: 2026-07-29

Status: APPEND_ONLY_RECOVERY_ERRATUM. This artifact restores a deleted
requirement to the working set as RECOVERED AND UNDISCHARGED. It does not edit
or supersede `primitive_record_cell_selection_principle_v003.md`, does not
re-adopt the v001 route, and does not authorize any response or coupling
evaluation.

## Source

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v001.md`

SHA-256:

```text
b58985b509d68371f3a47cfba503fa697d4513257d798ef3b5ca0739f8a2643e
```

The deleted section is lines 95-109, headed `## Rescaling exclusion`. It states
that the selector closes the old normalization gap only if no
target-independent one-parameter family

```text
A_* -> lambda A_*,
K_R -> K_R / lambda^2
```

or corresponding clock, source, measure, or cell deformation preserves every
upstream record principle while changing `K_R,*`. It also states that compact
unit winding blocks a bare rescaling of `A_*`, but does not by itself fix the
cell, duration, matter action, or field-energy integral.

The same file links the requirement to allow/require at lines 3-9: the principle
"strengthens the earlier allow/require rule"; that earlier rule fixed whether a
support could become a durable public record but did not fix an absolute action
coefficient.

## Successor Check

`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v003.md`

SHA-256:

```text
3ea70741b15a2842e1103a1153ff397e7354ebef429258e889ba08db92b39982
```

Version 003 says at lines 5-9 that it supersedes
`primitive_record_cell_selection_principle_v002.md` for the active post-cleanroom
route, because v002 placed a local Maxwell term with coefficient `K` inside the
microscopic action; in v003 `K` labels a local surrogate for an exact induced
connection response and is not a microscopic input. Version 003 records
`v002_microscopic_K_action_superseded = true` at line 174.

I found no v003 note deliberately superseding the v001 rescaling-exclusion
requirement, no account of the deleted v001 section, and no current working-set
citation that carries the v001 section forward. The requirement was therefore
DROPPED SILENTLY relative to the working set, not deliberately discharged or
deliberately superseded.

## Recovered Requirement

Before any absolute microscopic stiffness or downstream charged response may be
claimed from a selected primitive cell, the program must defeat target-independent
rescaling or deformation families that preserve every upstream record principle
while changing the absolute local response coefficient. The defeat must include
the possible clock, source, measure, field-energy, cell-shape, and action-partition
variants named in v001, not only the bare `A_*` rescaling.

This recovered requirement is UNDISCHARGED. Restoring it to the record does not
select a route, does not make the v001 selector active, and does not compute any
physical output.

```text
rescaling_exclusion_recovered_to_working_set = true
rescaling_exclusion_discharged = false
v003_deliberate_supersession_of_v001_rescaling_exclusion_found = false
v001_rescaling_exclusion_dropped_silently_relative_to_working_set = true
alpha_computed = false
proof_authorized = false
```
