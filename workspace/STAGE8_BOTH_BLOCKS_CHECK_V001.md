# STAGE 8 / CROSS-BLOCK / BOTH-BLOCKS-CHECK — BLIND ADVERSARIAL VERIFICATION OF THE BOTH-BLOCKS DETERMINATION

Lane: FABLE blind adversarial verifier, codename BOTH-BLOCKS-CHECK, cross-lineage.
DEFAULT = REFUTE. Date: 2026-08-13.
UNDER TEST: `STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md`, sha256
`92dba8d33b09c5cd2a9ff5dab122f2cf2d3420d521cdf422d210d988f1ab6ee9` — RECOMPUTED AT
PATH THIS SESSION, MATCH, before any content was read.

VERDICT UP FRONT: **BB_VERDICT = SOUND.** All four determinations survive attack.
The K_3 blocks and all three typing runs reproduce EXACTLY under a third
independent instrument; the scope reading is confirmed at the bytes against a
whole-file sweep; the composition hunt is confirmed with one REINFORCING span the
determination did not cite (GC :124-131, :229 — noted in §3.4, strengthens, does
not change); the NET is exact as scoped. The determination's own in-artifact
script was extracted verbatim, run, and its output is diff-identical.

---

## 0. Preflight, seals, and fences

Output name probed before any write: `STAGE8_BOTH_BLOCKS_CHECK_V001.md` ABSENT
(artifact and sidecar). No register, tracker, plan, road, ledger, or lens file
read. Every digest below recomputed from bytes at path by `shasum -a 256` /
hashlib THIS session, BEFORE reliance.

```text
SEALS VERIFIED (all recomputed at path, all MATCH):
92dba8d33b09c5cd2a9ff5dab122f2cf2d3420d521cdf422d210d988f1ab6ee9  STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md  (under test)
a3bedc7ef092ebd932d254331dc10053aa123334a013b95fb1e15019bdfea016  STAGE8_B1_CELL_INSTANCE_V001.md
86b6134d9ae9ad646da2c67f8d9477677effc8aee69824c0b27bce48cfb13b37  STAGE8_B1_CELL_CHECK_V001.md
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md
5154c203ee237c4dee1b02176f8fa64c84b3ec97e561b720304f1a58219ad29b  STAGE8_ATTACH_CANDIDATE_V007_CHECK_CODEX2_V001.md
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md
a22ef820c5e665cbf9a5c941d24be57efb06c9c921ee579e5291d0cdc65feaa3  STAGE8_GLUED_TOPOLOGY_CHECK_V001.md
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md

V011 BYTE SPANS, sha256 of span recomputed independently (file length 84987):
[29318,30557)  7d81c132...  MATCH  K_L presentation ("For the filtration audit, use the finite
                                   translation-complete test object" — V011 line 826 confirmed)
[30559,30843)  96e164af...  MATCH  frozen plaquette boundary word (line 864 confirmed)
[44530,45446)  9bbd9525...  MATCH  tangent complex A^0/A^1/A^2; F_phys = im(d_1) (lines 1322-1355)
[58559,58704)  7103582e...  MATCH  Gate-5 pass line (line 1740)
[80185,80240)  a6cc611f...  MATCH  "periodic_translation_complete_test_object_frozen = true"
                                   (V011 line 2241 confirmed by grep)
SPAN-DRIFT CLAIM INDEPENDENTLY CONFIRMED: the hunt (:91) cites a6cc611f at V011
[73992,74047); MY rehash of those bytes gives 90123379... (content "nged;\nfirst-
opening status or its handle label is not p") — NOT a6cc611f. The determination's
corrected offset [80185,80240) carries the exact 55-byte flag content and hashes to
a6cc611f... EXACTLY. The determination's SPAN-DRIFT note is accurate; reliance on
content, not offset, was the right call. Nothing else moved.
UNVERIFIABLE: none.
```

FENCES HELD: exact structural algebra only (integer/rational); NO physical
quantity computed, bounded, or evaluated; no scale, no imported GR, no
faithfulness authority; no continuum content consumed (the Gate-5 line read for
obligation TYPE only); register/tracker/plan/road/ledger/lens untouched. K_3
handled as an AUDIT-stratum object throughout. My script: Python 3 stdlib only,
reproduced verbatim in §5 with verbatim output in §6.

---

## 1. ATTACK 1 — THE SCOPE READING, re-read independently at the bytes

### 1.1 The whole-file sweep the determination did not print

`grep -n "filled"` over ALL of V011 (84,987 bytes) returns EXACTLY FOUR hits:

```text
:35    "an incorrect extension of flat local-system language to curved filled cells"
       (in the corrected-defect list — curved filled cells are FIRST-CLASS objects
        whose earlier mistreatment was itself typed a defect)
:301   "No flatness around a filled two-cell is imposed. ... nontrivial face
        holonomy must remain admissible"        (explicit ADMISSION, general cells)
:1840  "Use the unfilled oriented 1-skeleton K_square with no 2-cell and:"
:1881  "Use the Hilbert forms produced by Gate 3. No filled 2-cell is included in
        the trace carrier."
```

There is NO fifth span. No sealed sentence in V011 extends the exclusion beyond
"the trace carrier". Supplementary sweeps: "trace carrier" occurs ONLY at :1882
(the operative sentence) and :2184 (the void-trigger list, where it is one frozen
component of the composition-loop prediction alongside "the loop complex, gauge,
phase, ... or output list"); "two-cell/2-cell" occurrences (:301, :326, :492,
:864, :1070, :1840, :1881) contain no ban.

### 1.2 The placement, verified

Read at :1830-1895: the operative sentence sits inside the section "## Fully
specified composition-loop prediction" ("This is a new forward mathematical
consequence, not an empirical holdout"), between the fixed gauge display and the
definition of `B_square(Phi)` "on the full eight-dimensional space
`C_0(K_square) direct-sum C_1(K_square)`". The definite article binds to THAT
carrier. Confirmed exactly as the determination reads it.

### 1.3 The other horn, attacked

Could the UNIVERSAL reading stand anyway? No — it contradicts sealed text three
ways, all verified at bytes this session:
(i) V011 :301-303 deliberately admits nontrivial face holonomy on filled
two-cells in the general cell category; (ii) V011 :1322-1355 (span 9bbd9525)
defines the ratified response functional ON `F_phys = im(d_1)` — the flux sector
is EMPTY without filled faces, so a universal F = 0 would void a ratified sealed
object; (iii) the AND result table (38bbb9fc :192-199) seals the working-class
parent K census 16/32/24 with rank d_1 = 17 — twenty-four filled faces OF RECORD.
Also verified: every restatement of the mandate in the b1 lineage (instance :172,
:391, :488, :756; check :87-88, :293) carries "in the trace carrier" attached —
the source set nowhere quotes the sentence unmoored from its carrier.

### 1.4 Genuine-ambiguity horn

Rejected too: :2184 types "trace carrier" as a component of ONE prediction, and
the addendum's operative-domain clause (:33-34, verified verbatim: "OPERATIVE
DOMAIN: connected admitted b₁ = 1 cells (trees inert ...; b₁ > 1 out of scope)")
types domain in b_1 terms and says nothing about faces-in-general. The V007 chain
(:162-164, verified verbatim: "connected admitted cells with b_1 = 1 — the
specified square sector, member 10 :1840-1852") resolves per the b1 instance's
note N-1 (:79-88, resolution "confirmed at bytes" both ways) to the V011
:1840-1851 K_square display. The clean-division table (trace carrier F = 0 vs
response carrier F > 0) is exact. The §3.5 canonicity remark is verified in §4
below (H^1 = b_1 − rank d_1, so at b_1 = 1 any Q-active face kills the readable
class — arithmetic checked).

**SCOPE_READING = CONFIRMED (TRACE-CARRIER-SPECIFIC).** No sealed span supports
the universal horn; the specific horn is positively supported at :1840, :1881-1882,
:2184, and negatively forced by :301-303, :1322-1355, and AND :192-199.

---

## 2. ATTACK 2 — COMPOSITION, hunted independently

Searched for any sealed BAR on a both-features complex: none exists. The one
candidate sentence is :1881-1882, disposed of in §1. K_L IS a sealed
both-features complex: presentation span 7d81c132 verified ("the oriented
2-skeleton of (Z/LZ)^4, L >= 3", every elementary plaquette, frozen boundary word
96e164af), frozen flag a6cc611f verified at corrected offset, role typed at hunt
:238-251 ("the finite translation-complete TEST OBJECT ... AUDIT object; nothing
seals it as the physical record carrier"), H^1 = 4 certified at hunt :298-317 and
reproduced by me in §4 with a third instrument.

Searched for any sealed physical-stratum LICENSE: none exists. Verified at bytes:
- hunt G-1 :165-173: composition of record is the COPRODUCT, disjoint-only;
  "The CONNECTED case is not a composition law of record at all — it is an OPEN
  OBLIGATION"; `connected_linked_cluster_density_proved = false` confirmed
  standing at V011 :2282.
- hunt G-4 :193-204: ports are endpoint slots; "their identification with 0-cells
  of the complex is not displayed"; "NOTHING licenses identifying any out-port
  with any in-port ... and nothing forbids it"; "exactly O11 + O12 of POSED, both
  UNBUILT."
- V011 :1740 (span 7103582e): the Gate-5 pass line is obligation-typed language
  pointing at connected cellulations.
- AND :344-346: "the multi-cell/exhaustion side is UNBUILT (charter O-D1; port
  anchoring O12)"; hunt :324-326: "no sealed map sends either audit object onto
  the working class or vice versa."
- glued check :288-291: "NONE_SEALED at the physical write-carrier stratum
  SURVIVES on the strict reading."
- AND :337-348: the reopening clause is prospective ("A sealed record complex
  with dim H > 0 ... would ... REOPEN the AND-class question at that level. NO
  such complex is sealed of record") — a license SHAPE, not a bar, and consistent
  with K_L's audit-stratum placement (K_L is not sealed as a RECORD complex).

MISSED-SPAN HUNT RESULT: nothing that changes the verdict. ONE reinforcing span
the determination did not cite, recorded in §3.4.

**COMPOSITION_HUNT = CONFIRMED (UNBUILT-UNSPECIFIED; absence, not prohibition).**

---

## 3. ATTACK 3 — K_3 REBUILT, BLOCKS RECOMPUTED, WITNESSES RE-VERIFIED

### 3.1 Instrument independence

Three instruments now agree on the block dimensions, pairwise independent:
the hunt used Fraction RREF; the determination used exact certificate + three-
prime sandwiches; I used EXACT FRACTION-FREE INTEGER GAUSSIAN ELIMINATION OVER Q
(gcd-normalized rows, no mod-p anywhere, no RREF) on matrices I built myself from
the sealed law at the verified spans. Result (script §5, output §6):

```text
rank_Q d_0 = 80   rank_Q d_1 = 240   =>   gauge 80 / flux 240 / harmonic 4
sum 80 + 4 + 240 = 324 = E exact;  d_1 d_0 = 0 entrywise over Z;
census V/E/F = 81/324/486 = sealed L^4/4L^4/6L^4 at L = 3;
c_0..c_3 in ker d_1, perp im(d_0), Gram = 81*I_4  =>  exact orthogonal basis of H.
```

**K3_RERUN = MATCH** (against both the determination and the hunt's census row
:298-301).

### 3.2 Witnesses, against MY matrices

```text
T0  ell = c_0 + w, w = d_1^T delta_{f_01(0)}:  bdry.ell = 0 VERIFIED; ell != 0
    VERIFIED; <w, c_mu> = 0 all mu VERIFIED (so w perp H against the exact basis);
    c_0 perp im(d_1^T) by <c_0, d_1^T y> = <d_1 c_0, y> = 0 with d_1 c_0 = 0
    VERIFIED  =>  P_H(ell) = c_0 != 0 and P_f(ell) = w != 0.  BOTH BLOCKS NONZERO.
T1  restricted null space on f_01(0)'s 4 support edges recomputed by my own
    Fraction elimination: nullity 1 VERIFIED, basis vector = the boundary word w
    VERIFIED (entrywise equality after integer scaling and sign);  w in im(d_1^T)
    and w perp H  =>  phi_H == 0 on the whole on-cell admissible set.  AND FAILS.
T2  lam = coordinate 3-circuit cochain:  bdry.lam = 0 VERIFIED; <lam, c_mu> =
    (3,0,0,0) VERIFIED; P_H(lam) = (3/81)c_0 = c_0/27 != 0 exact; pf = lam - c_0/27
    satisfies pf perp H VERIFIED, bdry.pf = 0 VERIFIED (perp im(d_0)), d_1 pf =
    d_1 lam VERIFIED with EXACTLY 18 nonzero entries VERIFIED  =>  pf in
    im(d_1^T) by FORM D1's split (verified of record: FORM :134-142, A^1 =
    im(d_0) ⊕ H ⊕ im(d_1^dagger), H := ker(d_1) ∩ im(d_0)^perp, counting metric)
    and pf != 0.  BOTH BLOCKS NONZERO.
```

**WITNESS = CONFIRMED** (every claimed property re-derived from my own build;
nothing fails).

### 3.3 The typing choices, attacked

The claim "the sealed text supplies NO cell/cell-local typing on K_3" — verified:
G3 Q6 (:130-135) fixes `ell_j` gauge-invariant, cell-local, conserved-typed,
total-nonzero, and "does NOT fix the block-split"; GC (:82-91) proves conserved =
gauge-invariant = the ONE condition ⊥ im(d_0) and "imposes NOTHING on the split".
Cell-locality of record is CONTAINMENT-ONLY (GC :153, :203: "support(L_c) ⊆
Omega_c"), defined on the working-class carrier; K_3 is an audit object with no
sealed cell decomposition. All three typings are explicit, minimal, flagged;
none is presented as forced; the headline verdict is TYPING-DEPENDENT, exactly as
the fence demands. T2's "cell" = connected b_1 = 1 subcomplex matches the entered
operative domain's shape (addendum :33-34) — a flagged analogy, not an imported
authority. T1 reproduces the working-class mechanism. No sealed basis for a
choice was ignored; no choice was silently privileged.

### 3.4 A reinforcing span the determination did not cite

GC :124-131 and :229 (verified at bytes): the strongest available forcing line —
"cell-local ⟹ contractible ⟹ boundary ∈ im(d_1^dagger) ⟹ phi_H = 0" — "REQUIRES
importing" what the record "does NOT seal": that a cell-local cycle bounds
(H_1(Omega_c) = 0 is NOT sealed). This is the sealed record's own statement that
plaquette-locality-style collapse (T1) is not forced; it independently supports
the determination's refusal to privilege T1. Reinforces; changes nothing.

**TYPING_CHOICES = SOUND.**

---

## 4. ATTACK 4 — THE NET, checked for over- and under-claim

The structural identity behind N-1 is verified by my own numbers: on a connected
complex every d_1 row is a cycle (d_1 d_0 = 0, verified entrywise), so
H^1 = b_1(1-skeleton) − rank d_1. Instantiated and checked: K_square 1 − 0 = 1;
single plaquette 1 − 1 = 0; K_3: 244 − 240 = 4 (my §6 output, C6). Both blocks
nonzero ⟺ H^1 ≥ 1 AND rank d_1 ≥ 1 ⟹ b_1 ≥ 2 — EXACT, so N-1's "b_1 >= 2
forced" is right, and the disjoint coproduct cannot produce it (H^1 additivity,
hunt G-1). N-2 verified verbatim at V007 :170-171 ("DISPOSITION OF b_1 > 1: OUT
OF SCOPE ... would require a lawful cycle selector, which this artifact does not
supply and may not freeze"). N-3 is exactly the §3.3 finding. Over-claim check:
§4 explicitly keeps NONE_SEALED standing, keeps the three carried Attach
conditions (addendum :43-47, verified present), and places the K_3 result at the
audit stratum making "nothing physical". Under-claim check: nothing sealed
stands between the record and the carrier beyond N-1/N-2/N-3 that I could find —
the carried Attach conditions burden the ENTRY generally, not this carrier
specifically, and §4 already carries them.

**NET_STATEMENT = EXACT.**

---

## 5. MY SCRIPT, VERBATIM (`k3_check_v001.py`, Python 3 stdlib only)

```python
#!/usr/bin/env python3
# k3_check_v001.py — BOTH-BLOCKS-CHECK: independent rebuild of K_3 from the sealed
# law (V011 20a3a17d spans [29318,30557), [30559,30843), [44530,45446)), verified at
# bytes this session. INSTRUMENT INDEPENDENCE: ranks computed by EXACT fraction-free
# integer Gaussian elimination over Q (gcd-normalized, no mod-p, no sandwich) —
# a different exact method from both the hunt (Fraction RREF) and the determination
# (certificate + three-prime sandwich). Witnesses re-verified against THESE matrices.
# Exact integer/rational arithmetic only. Python 3 stdlib only. No physical quantity.
from fractions import Fraction
from itertools import product as iproduct
from math import gcd

L = 3
DIRS = range(4)
PAIRS = [(0, 1), (0, 2), (0, 3), (1, 2), (1, 3), (2, 3)]

VERTS = list(iproduct(range(L), repeat=4))
vidx = {x: i for i, x in enumerate(VERTS)}
def shift(x, mu):
    y = list(x); y[mu] = (y[mu] + 1) % L; return tuple(y)

EDGES = [(mu, x) for mu in DIRS for x in VERTS]
eidx = {e: i for i, e in enumerate(EDGES)}
FACES = [(mu, nu, x) for (mu, nu) in PAIRS for x in VERTS]
fidx = {f: i for i, f in enumerate(FACES)}
nV, nE, nF = len(VERTS), len(EDGES), len(FACES)
assert (nV, nE, nF) == (81, 324, 486), "census"
print("C0 census V,E,F =", nV, nE, nF, "= sealed L^4/4L^4/6L^4 at L=3")

# d_0 rows (one per edge): lambda_t - lambda_s
d0 = []
for (mu, x) in EDGES:
    row = [0]*nV; row[vidx[shift(x, mu)]] += 1; row[vidx[x]] -= 1
    d0.append(row)
# d_1 rows (one per face): frozen boundary word +e_mu(x) +e_nu(x+mu) -e_mu(x+nu) -e_nu(x)
d1 = []
for (mu, nu, x) in FACES:
    row = [0]*nE
    row[eidx[(mu, x)]] += 1
    row[eidx[(nu, shift(x, mu))]] += 1
    row[eidx[(mu, shift(x, nu))]] -= 1
    row[eidx[(nu, x)]] -= 1
    d1.append(row)

# C1: d_1 d_0 = 0 entrywise over Z
for fr in d1:
    for j in range(nV):
        assert sum(fr[e]*d0[e][j] for e in range(nE) if fr[e]) == 0
print("C1 d_1 d_0 = 0 entrywise over Z")

# exact rank over Q by fraction-free integer elimination with gcd normalization
def rank_exact(rows_in):
    rows = [r[:] for r in rows_in if any(r)]
    ncols = len(rows_in[0])
    rank, prow = 0, 0
    for col in range(ncols):
        piv = None
        for i in range(prow, len(rows)):
            if rows[i][col]:
                piv = i; break
        if piv is None:
            continue
        rows[prow], rows[piv] = rows[piv], rows[prow]
        pv = rows[prow][col]
        for i in range(prow+1, len(rows)):
            b = rows[i][col]
            if b:
                ri = rows[i]; pr = rows[prow]
                nr = [pv*a - b*c for a, c in zip(ri, pr)]
                g = 0
                for v in nr:
                    if v:
                        g = gcd(g, abs(v))
                        if g == 1: break
                rows[i] = [v//g for v in nr] if g > 1 else nr
        prow += 1; rank += 1
        if prow == len(rows): break
    return rank

r0 = rank_exact(d0)
r1 = rank_exact(d1)
print("C2 rank_Q d_0 =", r0, "(exact elimination over Q; no mod-p)")
print("C3 rank_Q d_1 =", r1, "(exact elimination over Q; no mod-p)")
assert r0 == 80 and r1 == 240
GAUGE, FLUX = r0, r1
HARM = (nE - FLUX) - GAUGE
assert GAUGE + HARM + FLUX == nE and HARM == 4
print("C4 blocks gauge/flux/harmonic =", GAUGE, "/", FLUX, "/", HARM, "; sum =", nE, "= E")

# direction cocycles c_mu; H-basis certificate against MY matrices
c = []
for mu in DIRS:
    v = [0]*nE
    for x in VERTS:
        v[eidx[(mu, x)]] = 1
    c.append(v)
def dot(u, v): return sum(a*b for a, b in zip(u, v))
def bdry(u):
    out = [u[0]*0]*nV
    for j in range(nE):
        if u[j]:
            for k in range(nV):
                if d0[j][k]:
                    out[k] = out[k] + d0[j][k]*u[j]
    return out
def d1_apply(u):
    return [sum(fr[e]*u[e] for e in range(nE) if fr[e]) for fr in d1]
for mu in DIRS:
    assert all(v == 0 for v in d1_apply(c[mu])), "d_1 c_mu = 0"
    assert all(v == 0 for v in bdry(c[mu])), "bdry c_mu = 0"
gram = [[dot(c[m], c[n]) for n in DIRS] for m in DIRS]
assert gram == [[81 if m == n else 0 for n in DIRS] for m in DIRS]
print("C5 c_0..c_3 in ker d_1, perp im(d_0), Gram = 81*I_4; dim H = 4 => exact orthogonal basis of H")

# T0 witness: ell = c_0 + w, w = d_1^T delta_{f_01(0)}
f_star = fidx[(0, 1, (0, 0, 0, 0))]
w = d1[f_star][:]
ell = [a + b for a, b in zip(c[0], w)]
assert all(v == 0 for v in bdry(ell)), "conserved"
assert any(v != 0 for v in ell), "total-nonzero"
assert all(dot(w, c[mu]) == 0 for mu in DIRS), "w perp H"
assert any(v != 0 for v in w) and any(v != 0 for v in c[0])
# c_0 perp im(d_1^T): <c_0, d_1^T y> = <d_1 c_0, y> = 0 (C5). So P_H ell = c_0, P_f ell = w.
print("T0 witness VERIFIED: bdry.ell=0; P_H(ell)=c_0 != 0; P_f(ell)=w != 0 (both blocks nonzero)")

# T1: on-cell (single plaquette f_01(0)) conserved cochains — my own nullspace run
sup = sorted(e for e in range(nE) if d1[f_star][e])
assert len(sup) == 4
# nullspace of bdry restricted to these 4 columns, Fraction elimination
A = [[Fraction(d0[e][v]) for e in sup] for v in range(nV)]
# transpose orientation: rows = vertices, cols = 4 support edges
r, piv = 0, []
for cc in range(4):
    pr = next((i for i in range(r, nV) if A[i][cc] != 0), None)
    if pr is None: continue
    A[r], A[pr] = A[pr], A[r]
    pv = A[r][cc]; A[r] = [x/pv for x in A[r]]
    for i in range(nV):
        if i != r and A[i][cc] != 0:
            fq = A[i][cc]; A[i] = [a - fq*b for a, b in zip(A[i], A[r])]
    piv.append(cc); r += 1
assert r == 3, "nullity on 4 support columns = 1"
free = [cc for cc in range(4) if cc not in piv][0]
vec = [Fraction(0)]*4; vec[free] = Fraction(1)
for ri, pc in enumerate(piv): vec[pc] = -A[ri][free]
den = 1
for x in vec: den = den*x.denominator//gcd(den, x.denominator)
nvec = [int(x*den) for x in vec]
full = [0]*nE
for k, e in enumerate(sup): full[e] = nvec[k]
if dot(full, w) < 0: full = [-v for v in full]
assert full == w, "the on-cell conserved line IS the boundary word"
assert all(dot(w, c[mu]) == 0 for mu in DIRS)
print("T1 VERIFIED: on-cell conserved set = k*w exactly (nullity 1); w perp H => phi_H == 0; AND fails")

# T2 witness: lam = coordinate 3-circuit in direction 0 through the origin
x0 = (0, 0, 0, 0); x1 = shift(x0, 0); x2 = shift(x1, 0)
lam = [0]*nE
for x in (x0, x1, x2): lam[eidx[(0, x)]] = 1
assert all(v == 0 for v in bdry(lam)), "conserved"
assert [dot(lam, c[mu]) for mu in DIRS] == [3, 0, 0, 0]
pH = [Fraction(3, 81)*v for v in c[0]]                    # P_H lam via exact orthogonal basis
pf = [Fraction(a) - b for a, b in zip(lam, pH)]
assert any(v != 0 for v in pH)
assert all(dot(pf, [Fraction(v) for v in c[mu]]) == 0 for mu in DIRS), "pf perp H"
assert all(v == 0 for v in bdry(pf)), "pf perp im(d_0)"
d1lam = d1_apply(lam)
nz = sum(1 for v in d1lam if v != 0)
assert nz == 18, "d_1 lam nonzero entries"
assert d1_apply(pf) == [Fraction(v) for v in d1lam], "d_1 pf = d_1 lam"
print("T2 witness VERIFIED: bdry.lam=0; <lam,c_0>=3; P_H(lam)=c_0/27 != 0;")
print("   pf = lam - c_0/27 perp H, perp im(d_0) => pf in im(d_1^T) (FORM D1 split); d_1 lam has", nz, "nonzero entries => pf != 0")

# structural proposition 3.5: H^1 = b_1(1-skeleton) - rank d_1 on connected complexes
b1_skel = nE - nV + 1
assert HARM == b1_skel - FLUX and b1_skel == 244
print("C6 structural: b_1(1-skel) =", b1_skel, "; H^1 =", b1_skel, "-", FLUX, "=", HARM,
      "; both blocks nonzero forces b_1 >= 2")
print("ALL CHECKS PASSED")
```

## 6. MY SCRIPT'S OUTPUT, VERBATIM (`python3 k3_check_v001.py`, exit code 0)

```text
C0 census V,E,F = 81 324 486 = sealed L^4/4L^4/6L^4 at L=3
C1 d_1 d_0 = 0 entrywise over Z
C2 rank_Q d_0 = 80 (exact elimination over Q; no mod-p)
C3 rank_Q d_1 = 240 (exact elimination over Q; no mod-p)
C4 blocks gauge/flux/harmonic = 80 / 240 / 4 ; sum = 324 = E
C5 c_0..c_3 in ker d_1, perp im(d_0), Gram = 81*I_4; dim H = 4 => exact orthogonal basis of H
T0 witness VERIFIED: bdry.ell=0; P_H(ell)=c_0 != 0; P_f(ell)=w != 0 (both blocks nonzero)
T1 VERIFIED: on-cell conserved set = k*w exactly (nullity 1); w perp H => phi_H == 0; AND fails
T2 witness VERIFIED: bdry.lam=0; <lam,c_0>=3; P_H(lam)=c_0/27 != 0;
   pf = lam - c_0/27 perp H, perp im(d_0) => pf in im(d_1^T) (FORM D1 split); d_1 lam has 18 nonzero entries => pf != 0
C6 structural: b_1(1-skel) = 244 ; H^1 = 244 - 240 = 4 ; both blocks nonzero forces b_1 >= 2
ALL CHECKS PASSED
```

## 7. The determination's own script, reproduced

The §5 code block of the artifact under test was extracted verbatim
(11,842 bytes), run under Python 3, exit code 0; its stdout is DIFF-IDENTICAL to
the artifact's §6 claimed output (byte-for-byte, `diff` empty). The
determination's SCRIPT_REPRODUCED = yes claim is confirmed by execution, not
trust.

## 8. Provenance and injection

PROVENANCE: CLEAN. The determination correctly types its §1/§4 Attach-leaning
claims TYPE-P on the entered addendum (whose ENTERED-APPEND-ONLY status,
authority DoR digests, trigger succession, and three carried conditions were
read at the sealed bytes) and correctly exempts the K_3 computation (leans only
on V011's sealed law + method files). All 14 file seals and 5 byte spans
recomputed before reliance; the one anomaly (a6cc611f offset drift) was disclosed
by the determination and independently confirmed here at the bytes — content
exact, offset stale in the hunt, nothing else moved.
INJECTION: none — directive-pattern scan over the determination and all ten
source files returned zero hits; no sealed text instructs a reader to skip
verification or alter behavior.

---

## FLAG BLOCK

```text
SCOPE_READING = CONFIRMED(TRACE-CARRIER-SPECIFIC horn — V011 20a3a17d :1840,
  :1881-1882 "No filled 2-cell is included in the trace carrier" bound by definite
  article to B_square's carrier C_0(K_square) ⊕ C_1(K_square) inside the
  composition-loop prediction; :2184 types trace carrier as one frozen component of
  that prediction; whole-file sweep: "filled" occurs at exactly 4 spans in V011
  (:35, :301, :1840, :1881), none universal; the universal horn contradicts sealed
  text three ways (:301-303 admission, :1322-1355 F_phys = im(d_1) response carrier,
  AND 38bbb9fc :192-199 cube-cell F = 24 rank d_1 = 17 of record); addendum
  420ab02f :33-34 types the operative domain in b_1 only; V007 :162-164 resolves to
  the K_square display per b1-instance note N-1, confirmed at bytes.)
COMPOSITION_HUNT = CONFIRMED(no sealed bar found — K_L sealed both-features at the
  AUDIT stratum, spans 7d81c132/96e164af/a6cc611f all rehashed; no physical-stratum
  license found — disjoint-only (hunt :165-173), ports unanchored O11+O12 (hunt
  :193-204), Gate-5 obligation (V011 :1740, 7103582e),
  connected_linked_cluster_density_proved = false standing (V011 :2282), NONE_SEALED
  (glued check :288-291), AND reopening clause prospective (:337-348).  One
  REINFORCING span the determination did not cite: GC 8ed95b5a :124-131, :229 — the
  record does NOT seal that a cell-local cycle bounds, so the T1-style collapse line
  requires an unsealed import; strengthens, changes nothing.)
K3_RERUN = MATCH(V=81, E=324, F=486; d_1 d_0 = 0 entrywise over Z; gauge 80 / flux
  240 / harmonic 4 by a THIRD independent instrument — exact fraction-free integer
  elimination over Q, no mod-p, no RREF — agreeing with both the determination's
  certificate+sandwich and the hunt's Fraction RREF; c_0..c_3 an exact orthogonal
  basis of H, Gram 81*I_4; sum 80+4+240 = 324 = E; structural identity H^1 =
  b_1(1-skel) - rank d_1 = 244 - 240 = 4 verified.)
WITNESS = CONFIRMED(T0 ell = c_0 + d_1^T delta_f01(0): conserved, total-nonzero,
  w ⊥ H, c_0 ⊥ im(d_1^T), both projections nonzero — all re-verified against my own
  matrices.  T1: restricted nullity 1 with basis = the boundary word, recomputed
  independently; phi_H ≡ 0 on-cell.  T2 lam = the 3-circuit cochain: bdry.lam = 0,
  <lam,c_0> = 3, P_H(lam) = c_0/27 exact, pf = lam - c_0/27 ⊥ H and ⊥ im(d_0) with
  d_1 pf = d_1 lam at exactly 18 nonzero entries.  Nothing fails.)
TYPING_CHOICES = SOUND(sealed text supplies no cell/cell-local typing on K_3 —
  verified at G3 3e35ffe2 :130-135 and GC 8ed95b5a :82-91, :153 (containment-only);
  all three typings explicit, minimal, flagged; headline verdict correctly
  TYPING-DEPENDENT; no choice presented as forced; no sealed basis ignored; GC
  :124-131/:229 independently confirms T1 is not forced of record.)
NET_STATEMENT = EXACT(b_1 >= 2 forcing verified arithmetically (H^1 = b_1 - rank
  d_1; both blocks nonzero => b_1 >= 2); N-2 verbatim at V007 :170-171; N-3 = the
  verified typing gap; no over-claim — NONE_SEALED kept, three carried Attach
  conditions kept (addendum :43-47), K_3 placed at audit stratum; no under-claim
  found — nothing sealed stands between the record and the carrier beyond
  N-1/N-2/N-3.)
BB_VERDICT = SOUND(all four determinations survive adversarial re-derivation;
  the determination's own script extracted and run: exit 0, output diff-identical;
  its seal table and span table fully re-verified including the disclosed a6cc611f
  span drift, confirmed at bytes at both offsets.)
PROVENANCE = CLEAN(14 file seals + 5 byte spans recomputed at path before reliance;
  TYPE-P typing of the Attach-leaning claims correct and the K_3 exemption correct;
  the one offset anomaly disclosed by the determination and independently confirmed.)
INJECTION = none
MACHINERY_USED_BY_ME = no
alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS of the determination remain CLAIMED-until-registered; this check
CONFIRMS them at its stratum.
```
