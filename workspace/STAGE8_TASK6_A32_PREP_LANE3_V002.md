# STAGE 8 / TASK 6 — A32 PREPARATION V002

Date: 2026-08-06  
Lane: Codex Lane 3 (SOL, high effort)  
Task: PASTE 601 / Task 6, Step 1  
Custody: bounded builder repair; Dario confirms; the principal alone ratifies

## Lead determination

```text
REGISTER_HEAD = Q-534
BASE_PREP = STAGE8_TASK6_A32_PREP_LANE3_V001.md / 0f5976a2...
REVIEW = STAGE8_TASK6_A32_PREP_REVIEW_DARIO_V001.md / d13bcf1f...

A32 = RATIFIED_PROCEDURE_NOT_ATTAINED
A32_SOURCE_CENSUS = 16_LOAD_BEARING_SOURCES
A32_LIVE_TOP_LEVEL_OBLIGATIONS = 20

SATISFIABLE_AS_INHERITED = 6
NEEDS_A_TASK_6_STEP = 6
IN_TENSION = 8

INSTRUMENT = REVISED_DRAFT_NOT_RATIFIED
A32_PRE_EVALUATION_READY = false
GATE_LIFTED_BY_THIS_ARTIFACT = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The V001 substantive conclusion survives. The bounded repair changes the source
census, the seal ancestry used by the ladder and `(M5a)`, the register citation
method, and the operative typing of the heterogeneous tensions. It does not add
a twenty-first obligation or move any of the twenty rows between categories.

The four states remain distinct:

```text
A32_FREEZE_RATIFIED
  != A32_PRE_EVALUATION_READY
  != A32_RESULT_PASSED
  != FINAL_CLAIM_AUTHORIZED.                              (L0)
```

## 0. Preflight, delta boundary, and fences

### 0.1 Preflight

The following checks passed before drafting:

```text
QUESTIONS_SETTLED_REGISTER terminal entry                  = Q-534

STAGE8_TASK6_A32_PREP_REVIEW_DARIO_V001.md SHA-256        =
  d13bcf1fdadc5f70fd6e2d1a854a8d5b103c5756197f8fb039bad3ce08a126fa
review sidecar                                               = verified

PREREGISTRATION_ENCODING_PRECEDENCE_PRINCIPAL_DECISION SHA-256 =
  70c4080eae018bd644a3f0694557f1c0e854d621aa61097c775737887fec528f
decision sidecar                                             = verified

authoritative V011 JSON SHA-256                            =
  13cf1e178a9fdced88590998984ec04e84ed83c0681b68dccd11b4e37d6afacd
JSON authority pin                                           = verified

required V002 output absent in cleanroom                   = true
required V002 output absent in archive workspace           = true
```

The questions register is a living append-only file. Consistent with Q-534's
LOCKED_PROCESS rule, this artifact cites that register by stable Q-entry and
does not assert a whole-file digest.

### 0.2 Bounded-delta boundary

V001's verified findings are carried except at these named points:

```text
D1  source census:                 15 -> 16;
D2  source 10 pin:                 whole-file hash -> Q-entry citation;
D3  graph authority:               prose stage_dag -> machine stage_dependencies;
D4  full ladder:                   10 -> 11 nodes;
D5  (M5a):                         ruled parents and non-seal conjuncts explicit;
D6  tension typing:                M9 / P3 / P4 separated from custody tensions;
D7  operative instrument:          D3-D6 inserted inside the ratification text.
```

The review passed the twenty-obligation count, the `6/6/8` classification, the
V011 byte-version disposition, and the four required carries already inside the
instrument. Those conclusions are redisplayed here at their corrected ancestry.

### 0.3 Tags and protected operations

```text
[PROVABLE]       displayed consequence of verified sealed authority;
[PART-PROVABLE]  conditional statement with its unmet premise displayed;
[YOURS]          proposed binding text, not an adopted rule;
[IN-TENSION]     a live obligation lacking its required witness;
[TYPE-U]         typed object with no sealed member of record.
```

No member is bound. No fixed point is executed. No end test is run. No value,
magnitude, uncertainty, distinctness statistic, coupling, or measured constant
is evaluated or compared.

## 1. C1 — census 16 and the authoritative ladder

### 1.1 The sixteen-source load-bearing census

Rows 1–9 and 11–16 were recomputed at the displayed digest and consumed only
through their verified seal attachment. Row 10 is deliberately different: it
is a living register and is pinned by entry, not by a transient file hash.

| # | Source and pin | Seal / citation mode | Load-bearing content |
|---:|---|---|---|
| 1 | `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md` — `7995f6fda75e78795cbfe167f8c8df634170ea3b43affd5bbe6e22bcda8f6ffe` | sealed manifest row | The open `Q_spec` list requires one unused structure-sensitive prediction. |
| 2 | `BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md` — `78f6bb08b7ae89d700cf84a19ebf8e62fa489a4ec6762429ac46d027538cbfe3` | pinned by sealed Gate-5 authority | A32 is a literal A34 dependency; the exhaustive registry, future beacon, pre-unmask prediction, and fail-closed conditions bind. |
| 3 | sealed packet `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` — `aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a` | packet manifest + Gate-5 pin | Base seal rules, selected-output sequence, and `proof_authorized` only at the final seal. |
| 4 | `A32_FREEZE_V002_RATIFIED_2026-07-28.md` — `32dbfc33b4f07407903ec014627ea64de57b5b1a6dc017dd27c6504729c3a327` | adjacent sidecar | The freeze is ratified and attainable, not attained; V000 mechanics incorporated; commit-before-execute order retained. |
| 5 | `AUTHORITY_CHAIN_PRECEDENCE_PRINCIPAL_DECISION_2026-07-29.md` — `85bacbee7c9b6ef9f4e65e5eb898bd5273440d600fadd342c5556c2075f5dc8e` | adjacent sidecar | The cleanroom chain, V011, and ratified A32 govern procedure; a final claim remains behind passed A32. |
| 6 | `STAGE8_AUTHORITY_CHAIN_PRECEDENCE_BINDING_V001.md` — `61c6de58a8690852fbae5d95cfbe21cd377e80b5ceeb3c52852e007755c60150` | adjacent sidecar | External evidence enters by path and hash and does not import parent premises. |
| 7 | `A32_FREEZE_DRAFT_V000_2026-07-28.md` — `13faf0bc9a455590bd99d1a40587d798bc558e87aa1d1bc6dcf6778731138123` | exact ranges incorporated by source 4 | Thirteen mechanical items and six principal items, only at their incorporated ranges. |
| 8 | `A32_MASKING_DISPOSITION_PRINCIPAL_DECISION_2026-07-30.md` — `d7153b91039974af15ab88fa6698e0573a0113fa826aa4f4ba9651b2277467bc` | adjacent sidecar | Q-25's limitation is permanent and travels with every A32 headline; vector use requires collector revision and recommitment before prediction. |
| 9 | `SLOT18_ADMISSIBILITY_PRINCIPAL_DECISION_2026-07-30.md` — `a132f4b2421610c7df4e9a8746286999b31672f1f2d805588ed3f1ad81ad6259` | adjacent sidecar | Complete-lineage `unused`; F-U1/F-U2; Q-28 precision clause and F-C3. |
| 10 | `QUESTIONS_SETTLED_REGISTER_V001.md` — **LIVE; BY ENTRY ONLY** | Q-05, Q-25, Q-27, Q-28, Q-521, Q-522, Q-532, Q-534 | Q-05 sequencing; Q-25 masking; Q-27 unused; Q-28 precision; Q-521 J-II TYPE-U; Q-522 sector/type and units; Q-532 escrow; Q-534 this repair. No whole-file hash is asserted. |
| 11 | `STAGE8_SLOT18_Q23_Q28_GOVERNING_REGISTRATION_V001.md` — `84966722ba7d7430a66f360d8f5ca3edde19f5b5035a26c7eb374f88e477424f` | adjacent sidecar | Current family screen empty, eligibility unexecuted, permanent qualification and scalar-only collector, ladder seals false. |
| 12 | `STAGE8_SLOT18_COMPARATOR_PREREGISTRATION_V001.md` — `2dbd3d1e3780f8ac96ada796fc070f085cc770ba9a4336d8702fcf929232485b` | adjacent sidecar | Same-alpha comparator contract, fixed order and units; five families lack a formula payload and the sixth is refuted by Q-28. |
| 13 | `STAGE8_SLOT18_Q34_NATIVENESS_AND_HOLONOMY_BRIDGE_RESULT_V001.md` — `8abb40899bda75d90a36c17448ced180df311e55aa50d10f66fa01c1f024d095` | adjacent sidecar | Deterministic alpha re-expression is ineligible; physical observable, A32 record, comparator, and uncertainty bridges remain absent. |
| 14 | `STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md` — `3a1a9d985d518937821cdd2af6c6a33fd4878969eaddae44b036ab0d29a37b0a` | adjacent sidecar | Public five-field record and output hashes are evidence, not an eligible universe; response-schema and uncertainty mismatches remain. |
| 15 | `STAGE8_PREREGISTRATION_ENCODING_PRECEDENCE_AND_A32_PREPARED_TEMPLATE_AUTHORITY_RECORD_V001.md` — `bec512930fceffe99ca8acff398e68df453c94c490df2db2487c6b59ecd2a178` | adjacent sidecar | V001 template is prepared, not ratified, not sealed, and not ladder authority. It is not a substitute for source 16. |
| 16 | `PREREGISTRATION_ENCODING_PRECEDENCE_PRINCIPAL_DECISION_2026-07-29.md` — `70c4080eae018bd644a3f0694557f1c0e854d621aa61097c775737887fec528f` | adjacent sidecar; ruled payload read directly | `:9-19` identifies both encodings and the evaluator; `:25-32` names four structural differences; `:36-50` makes `stage_dependencies` authoritative while preserving every `stage_dag` non-seal precondition; `:59-63` creates no seal and authorizes no computation. |

The machine encoding governed by row 16 was read directly at
`provenance/boundary_incidence_dynamics_preregistration_v011.json`, SHA-256
`13cf1e178a9fdced88590998984ec04e84ed83c0681b68dccd11b4e37d6afacd`,
JSON pointer `review_stage_semantics.stage_dependencies`. Its bytes are pinned by
the sealed Gate-5 specification authority. The JSON is row 16's directly-read
ruled payload, not a seventeenth independent principal source.

The derivative dependency map remains excluded: it adds no independent
obligation or authority. Historical Q-23/Q-24 states remain superseded by Q-27
and Q-25 respectively. Q-533's flattened tension summary is not consumed;
Q-534 expressly corrects it.

### 1.2 Source 16 — ruling extracted without summary substitution

[PROVABLE] The verified decision distinguishes:

```text
stage_dependencies = 11-node seal-to-seal graph consumed by evaluator;
stage_dag          = 10-node descriptive graph carrying mixed non-seal terms.
```

It rules the first authoritative for node set, edges, and fail-closed descendant
computation, while every non-seal term in the second continues to bind. The
sequence constraint is explicit:

> “This act cannot loosen any gate.”

The act ranks encodings already present. It edits no immutable bytes, discharges
no A-matrix row, creates no seal, supplies no missing subject, and authorizes no
computation.

### 1.3 The authoritative 11-node graph, read from the JSON

[PROVABLE] Direct parsing of `stage_dependencies` gives, in evaluator-consumed
insertion order:

```text
SPEC-SEAL
  <- []

CORE-RESULT-SEAL
  <- [SPEC-SEAL]

PARENT-COMPARISON
  <- [CORE-RESULT-SEAL]

HOLDOUT-UNIVERSE-SEAL
  <- [SPEC-SEAL]

QSPEC-SPEC-SEAL
  <- [SPEC-SEAL]

PREDICTION-MAP-SEAL
  <- [HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL]

THOMSON-RESULT-SEAL
  <- [CORE-RESULT-SEAL, QSPEC-SPEC-SEAL]

ALPHA-RESULT-SEAL
  <- [THOMSON-RESULT-SEAL,
      PARENT-COMPARISON,
      HOLDOUT-UNIVERSE-SEAL,
      PREDICTION-MAP-SEAL]

HOLDOUT-RESULT-SEAL
  <- [ALPHA-RESULT-SEAL]

END-TO-END-RECONSTRUCTION-SEAL
  <- [ALPHA-RESULT-SEAL, HOLDOUT-RESULT-SEAL]

FINAL-CLAIM-SEAL
  <- [END-TO-END-RECONSTRUCTION-SEAL, HOLDOUT-RESULT-SEAL]. (G1)
```

The corrected ladder is therefore:

```text
SPEC-SEAL
  -> {CORE-RESULT-SEAL, HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL};

CORE-RESULT-SEAL
  -> PARENT-COMPARISON;

{HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL}
  -> PREDICTION-MAP-SEAL;

{CORE-RESULT-SEAL, QSPEC-SPEC-SEAL}
  -> THOMSON-RESULT-SEAL;

{THOMSON-RESULT-SEAL, PARENT-COMPARISON,
 HOLDOUT-UNIVERSE-SEAL, PREDICTION-MAP-SEAL}
  -> ALPHA-RESULT-SEAL
  -> HOLDOUT-RESULT-SEAL;

{ALPHA-RESULT-SEAL, HOLDOUT-RESULT-SEAL}
  -> END-TO-END-RECONSTRUCTION-SEAL;

{END-TO-END-RECONSTRUCTION-SEAL, HOLDOUT-RESULT-SEAL}
  -> FINAL-CLAIM-SEAL.                                    (G2)
```

Compared with the demoted structural reading, `(G1)` adds the eleventh node,
adds `SPEC-SEAL` as parent of both holdout and Qspec, and adds `QSPEC-SPEC-SEAL`
as the second parent of the prediction-map seal. The four immediate parents of
`ALPHA-RESULT-SEAL` are unchanged.

### 1.4 The preserved non-seal conjuncts

[PROVABLE] The descriptive encoding supplies 24 non-seal terms. They are bound
to their matching authoritative nodes as follows:

| Authoritative node | Non-seal conjuncts that still bind |
|---|---|
| `SPEC-SEAL` | immutable V011 specification artifacts; passed A01–A29 and A35; three unanimous specification reports |
| `CORE-RESULT-SEAL` | executed Gates 1–5; composition-loop result; three unanimous core reports; independent core reconstruction |
| `PARENT-COMPARISON` | predeclared comparison map |
| `HOLDOUT-UNIVERSE-SEAL` | exhaustive registry snapshot; eligibility implementation; the descriptive `independent salted commitments` term under Q-25's accepted-with-permanent-limitation disposition; external randomness-beacon rule |
| `QSPEC-SPEC-SEAL` | complete target-free `Q_spec`; three unanimous Qspec reviews |
| `PREDICTION-MAP-SEAL` | immutable prediction code before alpha |
| `THOMSON-RESULT-SEAL` | complete charged amplitude; Ward/regulator/threshold passes; independent reconstruction |
| `ALPHA-RESULT-SEAL` | no additional descriptive conjunct beyond its four seal parents |
| `HOLDOUT-RESULT-SEAL` | future beacon; sealed numerical prediction; custodian unmask; commitment and contamination passes |
| `END-TO-END-RECONSTRUCTION-SEAL` | no descriptive counterpart; its two seal parents govern |
| `FINAL-CLAIM-SEAL` | A33 independent end-to-end reconstruction; three unanimous final-claim reports |

The merged evaluator rule is displayed, not inherited by abbreviation:

```text
Certified(node) :=
  SealTokenVerified(node)
  and every PreservedNonSealConjunct(node) certified
  and for every p in stage_dependencies[node], Certified(p). (G3)

Failure(node) -> Failure(each fail-closed descendant of node). (G4)
```

Thus a child token cannot stand in for an unnamed or uncertified parent. Ranking
the encodings does not discard A-matrix, Gate, composition-loop, beacon, Qspec,
report, or A33 obligations.

The holdout term is retained rather than deleted, but Q-25 controls what may be
certified about the current arrangement:

```text
Disposition("independent salted commitments") :=
  cryptographic_salted_commitments_verified
  and permanent_independence_limitation_carried
  and process_independence_not_claimed
  and independent_attestation_not_claimed.                 (G4a)
```

This is not a weakening by silent reinterpretation. It is the later principal
disposition of that exact current arrangement: the cryptographic binding stands,
while independent custody and attestation may not be claimed.

### 1.5 The rebuilt pre/post-evaluation boundary

The pre-evaluation A32 rail is the ruled subgraph

```text
SPEC-SEAL
  -> {HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL}
  -> PREDICTION-MAP-SEAL,                                 (G5)
```

where the braces denote sibling children of `SPEC-SEAL`, both required before
the prediction-map seal. Their applicable non-seal conjuncts in §1.4 also bind.

`END-TO-END-RECONSTRUCTION-SEAL` is displayed in the full ladder but is not put
inside the pre-evaluation subgate: it depends on `ALPHA-RESULT-SEAL` and
`HOLDOUT-RESULT-SEAL`, so requiring it before evaluation would be circular.

The alpha stop remains stronger than `(G5)`:

```text
ALPHA-RESULT-SEAL may be formed only from
  THOMSON-RESULT-SEAL
  + PARENT-COMPARISON
  + HOLDOUT-UNIVERSE-SEAL
  + PREDICTION-MAP-SEAL.                                  (G6)
```

No predicate in this artifact makes any token in `(G1)` true.

### 1.6 Obligation-count and mapping delta

Source 16 contributes a stricter authorization/dependency overlay. It does not
add content to the ratified enumeration

```text
13 incorporated mechanical items
 + 6 incorporated principal items
 + 1 Q-28 comparator-precision item
 = 20 top-level A32 obligations.                           (G7)
```

The category partition remains:

```text
SATISFIABLE-AS-INHERITED = {M1,M2,M5,M7,M8,P1}          = 6;
NEEDS-A-TASK-6-STEP      = {M10,M11,P2,P5,P6,A20}       = 6;
IN-TENSION               = {M3,M4,M6,M9,M12,M13,P3,P4}  = 8. (G8)
```

The re-derivations forced by the graph are:

| Row | V002 dependency statement | Category effect |
|---|---|---|
| M10 | Future-beacon selection remains after `PREDICTION-MAP-SEAL`; that seal now expressly requires both holdout and Qspec children of `SPEC-SEAL`. | none; still NEEDS |
| M12 | The all-ID interface traverses `SPEC -> {HOLDOUT,QSPEC} -> PREDICTION-MAP`; no shorthand child token certifies it. | none; still IN-TENSION |
| P2 | The beacon rule is a preserved non-seal conjunct of holdout, whose `SPEC-SEAL` parent is explicit. | none; still NEEDS |
| P6 | Exhaustive enumeration/eligibility is a holdout conjunct after `SPEC-SEAL`; Qspec independently remains required before the prediction map. | none; still NEEDS |
| M9 | It is a permanent disclosed limitation, not something graph completion can discharge. | none; still IN-TENSION |
| P3 | A physical comparator for `LOCAL-SHADOW` is an observable/type debt, not a seal-parent omission. | none; still IN-TENSION |
| P4 | The exact domain bridge is the Q-521/Q-534 J-II type-crossing of record. | none; still IN-TENSION |

## 2. C2 — `(M5a)` with ruled parents and sequence

The payload whose type `(M5a-V002)` must preserve is displayed before the
predicate, so the evaluator never certifies an undefined shorthand:

```text
A32CorePayload_i :=
  (canonical_id_i,
   predicted_output_i,
   units_i,
   theory_uncertainty_i,
   covariance_i_if_applicable,
   propagation_trace_i,
   comparator_output_i,
   structure_sensitivity_i,
   pass_fail_template_i);

Task6TypedPayload_i :=
  (A32CorePayload_i,
   output_type_i = LOCAL-SHADOW,
   route_i = LOCAL,
   trial_i = ONE-SIDED_ON_CURRENT_FINITE_CHI_LATTICE,
   period_native_i = false,
   A7_branch_i in {ZERO, IDENTITY},
   CARRY_1_labels_i,
   cross_sector_unit_trace_i,
   open_premises_and_falsifiers_i).                       (M5p)
```

No field in `(M5p)` promotes the local shadow to a period-native or Thomson
quantity. Both A7 labels remain indices, not a selected or merged branch.

### 2.1 Complete evaluator-certified predicate

[YOURS — binding draft; no seal asserted] The Step-1 completion predicate is:

```text
A32_PRE_EVALUATION_READY :=
  instrument_ratified

  and schema_and_canonical_ID_tensions_resolved
  and Q25_current_scalar_commitment_disposition_carried
  and executable_eligibility_and_comparator_interfaces_frozen

  and immutable_V011_specification_artifacts
  and passed_A01_A29_and_A35
  and three_unanimous_specification_reports
  and evaluator_certified(SPEC-SEAL <- [])

  and exhaustive_registry_snapshot
  and eligibility_implementation
  and evaluator_certified(
        stage_dag_term("independent salted commitments")
          at Q25_ACCEPTED_WITH_PERMANENT_LIMITATION)
  and cryptographic_salted_commitments_verified
  and process_independence_not_claimed
  and independent_attestation_not_claimed
  and permanent_Q25_headline_qualification_carried
  and external_randomness_beacon_rule
  and evaluator_certified(
        HOLDOUT-UNIVERSE-SEAL <- [SPEC-SEAL])

  and complete_target_free_Q_spec
  and three_unanimous_Qspec_reviews
  and evaluator_certified(
        QSPEC-SPEC-SEAL <- [SPEC-SEAL])

  and immutable_all_eligible_ID_prediction_map_code_before_alpha
  and evaluator_certified(
        PREDICTION-MAP-SEAL
          <- [HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL])

  and every_type_in_Task6TypedPayload_preserved.           (M5a-V002)
```

The evaluator certifies every conjunct individually under `(G3)`. A named
descendant does not import an unnamed parent by shorthand. In sequence:

```text
SPEC-SEAL
  -> {HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL}
  -> PREDICTION-MAP-SEAL.                                  (M5b)
```

The sibling nodes in braces may be processed in either order, but both follow
`SPEC-SEAL` and both precede `PREDICTION-MAP-SEAL`. The ruling's binding phrase
travels with the sequence: **“This act cannot loosen any gate.”**

The remaining non-seal conjuncts in §1.4 stay binding at their later nodes; they
are not imported early into `(M5a-V002)` and are not discarded. In particular,
the downstream end-to-end node is not a readiness conjunct.

### 2.2 Current truth status and scope

This artifact drafts `(M5a-V002)` but supplies none of its missing certificates:

```text
instrument_ratified                                  = false;
SPEC-SEAL                                             = false_of_record;
HOLDOUT-UNIVERSE-SEAL                                 = false_of_record;
QSPEC-SPEC-SEAL                                       = false_of_record;
PREDICTION-MAP-SEAL                                   = false_of_record;
A32_PRE_EVALUATION_READY                              = false. (M5c)
```

Even future truth of `(M5a-V002)` would be necessary for the Step-2 conditional
scope DoR, not sufficient for alpha evaluation. The full parents in `(G6)` and
their own non-seal conjuncts remain required.

`(M5a-V002)` requires M9's permanent qualification to be carried; it does not
require or permit M9 to be restated as independent custody proved. It also does
not construct the P3 comparator or the P4/J-II type crossing. Those distinctions
make the conjunct list complete without falsely turning readiness into closure
of the heterogeneous tension board.

## 3. The twenty-row mapping at corrected ancestry

`SATISFIABLE-AS-INHERITED` means the inherited stock can meet a row without a
new Task-6 mathematical construction; it does not make a ladder seal true.
`NEEDS-A-TASK-6-STEP` identifies a later governed operation. `IN-TENSION` names
a missing witness, permanent qualification, or type/observable gap.

| Row | Live obligation | Verdict | V002 application |
|---|---|---|---|
| M1 | exact query/pagination/retry/rate/failure rule; no manual additions | **SATISFIABLE-AS-INHERITED** | Public collector/transcript stock exhibits a deterministic machine path; no universe seal is inferred. |
| M2 | frozen cutoff/vintage; unparseable supplied version excluded | **SATISFIABLE-AS-INHERITED** | CODATA 2022 fixed vintage remains ratified; schema exclusions remain separately at M4. |
| M3 | full response schema | **IN-TENSION** | The public five-field record does not exhibit the required response schema; repair belongs before any seal in `(M5a-V002)`. |
| M4 | canonical field map; missing required key machine-excluded | **IN-TENSION** | Missing required fields lack a sealed map/default rule, and the public exclusion file is empty. |
| M5 | RFC-8785 canonical JSON | **SATISFIABLE-AS-INHERITED** | Existing all-string records serialize deterministically; any schema repair must rerun canonicalization. |
| M6 | canonical ID over the required value-free source record | **IN-TENSION** | Current IDs hash the public subset, not the required record; no sealed exact-equivalence theorem exists. |
| M7 | deterministic dedup with complete suppression report | **SATISFIABLE-AS-INHERITED** | Collector stock carries deterministic clusters and reports. |
| M8 | raw bytes/hashes, canonical/exclusion/duplicate outputs, collector hash, transcript | **SATISFIABLE-AS-INHERITED** | Stock artifacts exist; existence is not eligibility or sealing. |
| M9 | independent salted commitments and no-disclosure attestation | **IN-TENSION — PERMANENT CARRIAGE** | Q-25 accepts cryptographic concealment but records that process independence and independent attestation were never established. It travels with every headline and is not cleared by `(M5a-V002)`. |
| M10 | future-beacon selection only after prediction-map seal | **NEEDS-A-TASK-6-STEP** | Selector and metadata must freeze; the map seal carries both ruled parents. No beacon is read here. |
| M11 | full-lineage contamination audit before unmask | **NEEDS-A-TASK-6-STEP** | Q-27 fixes scope; later audit precedes unmask; F-U1/F-U2 remain armed. |
| M12 | immutable all-eligible-ID map and typed payload before evaluation | **IN-TENSION** | No map or seal exists. The repair must traverse `(M5b)` and preserve all Task-5 types, branches, CARRY-1 rows, falsifiers, and units. |
| M13 | theory/comparator/measurement uncertainty and frozen covariance | **IN-TENSION** | Theory sensitivity cannot substitute for the absent preselection measurement-metadata bridge; scalar-only qualification remains. |
| P1 | fixed public registry/API source and observable-universe basis | **SATISFIABLE-AS-INHERITED** | Fixed-vintage NIST CODATA 2022 source remains ratified; all records still face eligibility. |
| P2 | drand primary/NIST fallback; frozen timing/parser/mirrors; fail closed | **NEEDS-A-TASK-6-STEP** | Beacon rule is a holdout non-seal conjunct after `SPEC-SEAL`; retrieval waits for the governed post-map-seal instant. |
| P3 | published same-alpha QED/SM comparator, citable before universe construction | **IN-TENSION — TYPE/OBSERVABLE** | Five families lack formula payloads; the sixth is refuted by Q-28; no published comparator is proven to consume `LOCAL-SHADOW` faithfully. |
| P4 | identical physical alpha input/metadata; no refit/coefficient/post-selection | **IN-TENSION — J-II TYPE-CROSSING** | The exact bridge from `LOCAL-SHADOW` to the physical alpha-adjacent carrier is the same Q-521/Q-534 J-II object of record. It is TYPE-U, not supplied by A32. |
| P5 | fixed symbolic distinctness rule `D>=5`; empty fails | **NEEDS-A-TASK-6-STEP** | Later typed predictions and uncertainties may be tested only after sealing; an out-of-lattice seed is not relabeled a negative. |
| P6 | all machine-enumerated eligible IDs; no human subfamily | **NEEDS-A-TASK-6-STEP** | Exhaustive registry/eligibility follows `SPEC-SEAL`; complete target-free Qspec is a separate sibling prerequisite; current eligibility remains unexecuted. |
| A20 | Q-28 comparator-precision clause before beacon; no measured central value | **NEEDS-A-TASK-6-STEP** | Freeze an executable pre-beacon clause or F-C3 fails closed; no fitting or softening is allowed. |

The arithmetic is unchanged and displayed:

```text
SATISFIABLE-AS-INHERITED = {M1,M2,M5,M7,M8,P1}          = 6;
NEEDS-A-TASK-6-STEP      = {M10,M11,P2,P5,P6,A20}       = 6;
IN-TENSION               = {M3,M4,M6,M9,M12,M13,P3,P4}  = 8;
TOTAL                    = 6 + 6 + 8                     = 20. (M6)
```

## 4. C3 — revised principal ratification instrument

The following is the complete operative draft. It binds existing rules to the
certified Task-5 subject and authors no physics law, candidate, comparator,
number, branch selector, threshold, seal, or evaluation.

> ### TASK-6 A32 APPLICATION, GRAPH, AND TYPE-BINDING INSTRUMENT — V002
>
> **DRAFT — NOT RATIFIED — NO GATE LIFT**
>
> I ratify only the binding below of the already-ratified A32 procedure to the
> Task-5-certified computation. This act does not declare A32 attained; create
> `SPEC-SEAL`, `CORE-RESULT-SEAL`, `PARENT-COMPARISON`,
> `HOLDOUT-UNIVERSE-SEAL`, `QSPEC-SPEC-SEAL`, `PREDICTION-MAP-SEAL`,
> `THOMSON-RESULT-SEAL`, `ALPHA-RESULT-SEAL`, `HOLDOUT-RESULT-SEAL`,
> `END-TO-END-RECONSTRUCTION-SEAL`, or `FINAL-CLAIM-SEAL`; authorize numerical
> evaluation; bind a member; execute a fixed point or end test; compute a
> number; compare with a measured central value; or change a protected flag.
>
> **AUTHORITY AND LIVING-REGISTER CITATIONS.** Graph structure is read directly
> from `review_stage_semantics.stage_dependencies` in the sealed V011 machine
> encoding. The sealed principal precedence act rules that encoding
> authoritative for node set, edges, and fail-closed descendants;
> `stage_dag` remains binding for every non-seal precondition it states. In the
> ruling's words, **“This act cannot loosen any gate.”** Citations to
> `QUESTIONS_SETTLED_REGISTER_V001.md` are by entry—Q-05, Q-25, Q-27, Q-28,
> Q-521, Q-522, Q-532, and Q-534—never by a whole-file hash.
>
> **SUBJECT.** The only admitted subject is the certified V004 local chain with
> every displayed gate and certificate inherited. Its product carries:
>
> ```text
> COMPUTATION_SCOPE = LOCAL
> PRODUCT_TYPE = LOCAL-SHADOW
> TRIAL = ONE-SIDED_ON_CURRENT_FINITE_CHI_LATTICE
> PERIOD_NATIVE = false
> ```
>
> The product may be evaluated only under a later, separate evaluation DoR and
> after every applicable pre-evaluation A32 condition below is true. It may not
> be renamed a global period, holonomy charge, period-native Maxwell quantity,
> `kappa_Thomson`, physical alpha, or a two-sided trial.
>
> **FOUR REQUIRED CARRIES.** Every A32 record and headline retains:
>
> 1. the exact `LOCAL / LOCAL-SHADOW` type;
> 2. the one-sided rider: a nonzero seed may confirm inside the licensed local
>    lattice, while seed zero is a pole/out-of-lattice condition and is not a
>    negative verdict;
> 3. Q-25's permanent disclosure that cryptographic concealment is accepted but
>    process independence and independent attestation were never established;
>    this language travels with every A32 headline and `FINAL-CLAIM`; and
> 4. the scalar-only caveat: any vector or multicomponent extension requires
>    collector revision **and recommitment before any prediction exists**.
>
> **TYPE PRESERVATION.** The sealed theory-output record, immutable A32
> prediction map, and every payload preserve without deletion or weakening:
> the four carries above; the period cure only as `CARRIED-CONDITIONAL`,
> requiring a formed period route and true `d^per` modulus certificate; all
> seven CARRY-1 rows at their sealed verbs; both A7 branches, ZERO and IDENTITY,
> computed and reported per branch if later computed, with neither selected,
> averaged, merged, nor dropped; every open premise and falsifier; and every
> cross-sector-unit factorization in the propagation trace, with no conversion
> silently set to one.
>
> No aggregate branch verdict is created. If the prediction-map schema cannot
> carry both branches, the gate remains unresolved; no branch may be chosen. If
> a branch exits the licensed lattice, the payload records `OUT_OF_LATTICE` or
> noncomputable and invents no value.
>
> **PRE-EVALUATION A32 CONDITIONS.** Before numerical evaluation is authorized,
> the evaluator certifies this ancestor subgraph in ruled order. A descendant
> token is insufficient unless every displayed parent and applicable non-seal
> conjunct is certified:
>
> ```text
> SPEC-SEAL                         <- []
> HOLDOUT-UNIVERSE-SEAL             <- [SPEC-SEAL]
> QSPEC-SPEC-SEAL                   <- [SPEC-SEAL]
> PREDICTION-MAP-SEAL               <- [HOLDOUT-UNIVERSE-SEAL,
>                                       QSPEC-SPEC-SEAL]
> ```
>
> `HOLDOUT-UNIVERSE-SEAL` and `QSPEC-SPEC-SEAL` are sibling descendants of
> `SPEC-SEAL`; neither substitutes for the other, and `PREDICTION-MAP-SEAL`
> requires both. The evaluator certifies immutable V011 artifacts, passed
> A01–A29 and A35, and three unanimous specification reports at `SPEC-SEAL`;
> exhaustive registry snapshot and eligibility implementation at the holdout
> seal; the descriptive `independent salted commitments` term there only through
> Q-25's accepted-with-permanent-limitation disposition—cryptographic salted
> commitments verified, the permanent independence limitation carried, and no
> process independence or independent attestation asserted; and the external
> randomness-beacon rule at the same holdout seal;
> complete target-free `Q_spec` and three unanimous Qspec reviews at the Qspec
> seal; and immutable prediction code before alpha at the prediction-map seal.
>
> The V002 freeze and incorporated thirteen mechanics remain fixed. Schema,
> serialization, canonical IDs, deduplication, reports, commitments,
> eligibility implementation, all-ID coverage, and all typing above meet their
> live clauses. Complete-lineage `unused`, computability without fit or a new
> coefficient, independent-value and non-alpha requirements, structure
> sensitivity, and Q-28 comparator precision apply before beacon selection.
> Every eligible candidate has a preregistered same-alpha comparator or is
> ineligible; an empty eligible set fails closed; no measured central value is
> consulted.
>
> The remaining seven authoritative graph nodes retain their ruled parents and
> descriptive non-seal conditions at their later stages:
>
> ```text
> CORE-RESULT-SEAL               <- [SPEC-SEAL]
> PARENT-COMPARISON              <- [CORE-RESULT-SEAL]
> THOMSON-RESULT-SEAL            <- [CORE-RESULT-SEAL, QSPEC-SPEC-SEAL]
> ALPHA-RESULT-SEAL              <- [THOMSON-RESULT-SEAL, PARENT-COMPARISON,
>                                    HOLDOUT-UNIVERSE-SEAL,
>                                    PREDICTION-MAP-SEAL]
> HOLDOUT-RESULT-SEAL            <- [ALPHA-RESULT-SEAL]
> END-TO-END-RECONSTRUCTION-SEAL <- [ALPHA-RESULT-SEAL,
>                                    HOLDOUT-RESULT-SEAL]
> FINAL-CLAIM-SEAL               <- [END-TO-END-RECONSTRUCTION-SEAL,
>                                    HOLDOUT-RESULT-SEAL]
> ```
>
> Executed Gates 1–5, the composition-loop result, predeclared comparison map,
> complete charged amplitude, Ward/regulator/threshold passes, independent
> reconstructions, future-beacon and governed-unmask conditions,
> commitment/contamination passes, A33 reconstruction, and applicable unanimous
> report counts are not discarded or imported early; each remains binding at
> its named node. `END-TO-END-RECONSTRUCTION-SEAL` is downstream, not a Step-1
> readiness conjunct and not something this instrument creates.
>
> **HETEROGENEOUS TENSION TYPING.** `M3/M4/M6/M12/M13` are
> implementation/custody tensions and require sealed witnesses or fail closed.
> `M9` is not a discharge target: by Q-25, process independence and independent
> attestation were never established; that permanent limitation travels with
> every A32 headline and `FINAL-CLAIM`. `P3` and `P4` are type/observable gaps,
> not custody gaps. `P3` lacks a published physical comparator proven to consume
> `LOCAL-SHADOW` faithfully. `P4` lacks the exact domain bridge from
> `LOCAL-SHADOW` to the physical alpha-adjacent carrier; by Q-521 and Q-534 this
> is the same J-II type-crossing obstruction of record, not a new A32
> implementation item under another name. Neither `(M5a-V002)` nor any current
> Task-6 custody step supplies it. Unless a sealed lawful bridge is later
> supplied, the affected candidate is ineligible and the protocol fails closed.
> `(M5a-V002)` certifies pre-evaluation rails; it does not erase or resolve M9,
> P3, or P4 and does not attain A32.
>
> **THE EVALUATOR PREDICATE.** The evaluator certifies every conjunct of
> `(M5a-V002)` as displayed in the accompanying preparation, in the order
>
> ```text
> SPEC-SEAL
>   -> {HOLDOUT-UNIVERSE-SEAL, QSPEC-SPEC-SEAL}
>   -> PREDICTION-MAP-SEAL.
> ```
>
> No descendant seal stands in for an unnamed parent. If the predicate is
> false, the board stops before the evaluation DoR and before any numerical
> execution.
>
> **MASKING QUALIFICATION.** Every A32 headline and any `FINAL-CLAIM` states:
> cryptographic concealment is accepted, but process independence between
> collector and custodian was never established and no independent attestation
> exists. This is permanent carriage, not a condition `(M5a-V002)` resolves.
>
> The present collector is confined to the scalar fixed-column universe. Any
> vector or multicomponent extension requires collector revision and
> recommitment before any prediction exists.
>
> **ORDER.** This draft is Task-6 Step 1a. Before Step 2, a separate Step-1
> completion act must ratify this instrument, construct the pre-evaluation A32
> rails, and evaluator-certify every conjunct of `(M5a-V002)` in the parent
> order displayed above. If false, the board stops before the evaluation DoR.
> Only after it is true may a separate principal evaluation DoR state the
> conditional `LOCAL / LOCAL-SHADOW` scope. Member binding, fixed-point
> execution, end test, sensitivity, assembly, result sealing, governed beacon,
> selected-prediction sealing, custodian unmasking, and comparison retain their
> escrow/V011 order. Task-6 Step 8 seals a result; it cannot retroactively create
> the prerequisite prediction-map seal. A32 alone authorizes none of these acts.
> `proof_authorized` remains equivalent only to `FINAL-CLAIM-SEAL`.
>
> **FAIL CLOSED.** All inherited F0–F7, A9-era falsifiers, A4/A5 voids,
> counterexample regressions, F-U1/F-U2/F-C3, and A32 failures remain armed.
> Prior outcome access; an empty eligible set; registry drift; failed
> commitment; beacon substitution; duplicate ambiguity; an identical
> comparator prediction; post-unmask editing; type erasure; undeclared
> cross-sector conversion; branch/route/member selection; a missing or merely
> inherited parent seal; an uncertified applicable `stage_dag` conjunct;
> promotion of M9 to proved independence; relabeling P3/P4 as custody-only; or
> treating the Q-521/Q-534 J-II crossing as supplied by A32 fails the applicable
> gate.
>
> A wrong result for one future bound member refutes that instance. It does not
> refute the whole family without a separate rigidity theorem.
>
> ```text
> alpha_computed = false
> proof_authorized = false
> kappa_record_computed = false
> ```

### 4.1 Attainment-by-preparation refusal check

The operative first paragraph enumerates all 11 authoritative nodes and denies
creation of each. It separately denies ratification, readiness, evaluation,
member binding, fixed-point execution, end testing, a number, comparison, and
flag movement. Therefore:

```text
instrument_drafted -> not instrument_ratified;
instrument_drafted -> not A32_PRE_EVALUATION_READY;
instrument_drafted -> not any_seal_in_(G1);
instrument_drafted -> not numerical_authorization.          (I1)
```

## 5. Surface anchor and R9 quantification

### 5.1 Named actual objects

| Actual object | Anchor result |
|---|---|
| V001 prep `0f5976a2...` | **PASS AS BASE** — only the named review delta is changed. |
| Dario review `d13bcf1f...` | **PASS** — missed source, M5a inheritance, living pin, and heterogeneous typing consumed exactly. |
| principal precedence act `70c4080e...` | **PASS** — authoritative/scoping verb used only at its ruled strength. |
| V011 machine JSON `13cf1e17...` | **PASS** — all 11 nodes read directly; no summary substituted. |
| sealed packet V011 `aa7c6d49...` | **PASS** — base procedural text retained at its sealed byte version. |
| ratified A32 V002 `32dbfc33...` | **PASS** — freeze is ratified, no ladder seal inferred. |
| Task-5-certified local chain | **PASS AT LOCAL-SHADOW TYPE** — no period-native or Thomson interpretation added. |
| Q-521 J-II object | **PASS AS TYPE-U RECORD** — same type-crossing named; no member claimed. |
| A7 branch family | **PASS AS CARRIED FAMILY** — ZERO and IDENTITY retained, neither selected. |

### 5.2 Geometry / rails split

```text
GEOMETRY / PHYSICS SUBJECT
  local reader product;
  LOCAL-SHADOW type;
  one-sided seed rider;
  seven CARRY-1 rows;
  A7 branch index;
  J-II TYPE-U crossing;
  explicit cross-sector units;

RAILS / A32 PROCEDURE
  ruled 11-node graph;
  preserved non-seal conjuncts;
  registry and canonical records;
  commitments and qualification;
  eligibility, comparator, prediction-map code;
  beacon, contamination, unmasking;
  end-to-end and final authorization.                      (SA0)
```

The rails cannot construct J-II or promote a local shadow. The physics subject
cannot waive a parent seal or non-seal rail.

### 5.3 R9-lesson quantification check

The instrument quantifies only over:

```text
each machine-enumerated eligible canonical ID;
each certified A7 branch carried by the named subject;
each later lawfully bound member within its actual family scope. (SA1)
```

It does not quantify over an orientation representative, a phantom common
member, an unformed period route, or a class A8/A9 did not name. It requires no
equality between branches. A route comparison exists only for a common lawful
member. The repaired non-degenerate R9 quantification is unchanged.

`SURFACE_ANCHOR = present`.

## 6. Dependency re-audit

Every argument touched by the seven delta clauses was re-walked:

| Changed clause | Dependent arguments re-audited | Result |
|---|---|---|
| source 16 admitted | census, authority chain, graph verbs | Census becomes 16; direct JSON supplies graph; no seal/value follows. |
| source 10 by entry | every register citation | Q-05/Q-25/Q-27/Q-28/Q-521/Q-522/Q-532/Q-534 used; no living hash pin remains. |
| `stage_dependencies` authoritative | full ladder, fail-closed descendants, authorization | 11 nodes displayed; end-to-end inserted before final. |
| `stage_dag` descriptive | non-seal preconditions | All 24 retained at matching nodes; none discarded or imported prematurely. |
| `(M5a-V002)` explicit ancestry | Step-1 completion, M10/M12/P2/P6 | Both sibling seals follow SPEC and precede map; every applicable precondition named. |
| downstream end-to-end node | Step 8, A33, final authorization | Kept downstream; no pre-evaluation circularity; `proof_authorized` still only final. |
| M9 permanent | mapping, instrument, headline, final claim | Never relabeled dischargeable or independent-custody proved. |
| P3 type/observable | mapping, fail-closed rule | No comparator is invented by schema work. |
| P4 = J-II crossing | mapping, instrument, cross-sector row | Same Q-521/Q-534 TYPE-U object named; no member or silent unit supplied. |
| four required carries | payload, map, headline, order | Local-shadow, rider, Q-25, and scalar/vector rule remain inside operative text. |
| attainment refusal | lead, instrument, final board | All 11 seals and every protected operation remain false/unperformed. |

No dependent formula changes. No candidate becomes eligible. No branch is
collapsed. No seal, flag, or evaluator result changes.

`DEPENDENCY_REAUDIT = DISPLAYED`.

## 7. Battery

### 7.1 F_PLDEC

```text
graph nodes, parent edges, source census, registry, schema,
commitments, beacon rule, comparator rule, and threshold
  are fixed without reading ell, p_loc[Rhat_K], q_loop^loc,
  a seed value, a branch value, or any desired number;

the local reader enters only as a future typed payload subject;
no A32 rule is inferred from reader normalization;
no period, Thomson, or J-II identity is inferred from the reader. (B0)
```

`F_PLDEC = PASS`.

### 7.2 Anti-tuning ledger

| Possible tuning surface | Fixed response |
|---|---|
| graph choice | Machine encoding is ruled authoritative before this task; no edge chosen from a result. |
| non-seal preconditions | All 24 descriptive conjuncts retained; none dropped to make readiness easier. |
| parent shorthand | Disallowed; each parent and applicable conjunct is evaluator-certified. |
| registry/source/cutoff | Ratified source/vintage retained. |
| schema and canonical ID | Missing fields remain tensions; no post-result default. |
| threshold | Symbolic `D>=5` remains fixed. |
| empty screen | Fails closed; no eligibility relaxation. |
| comparator | Exact published identity/order/domain required; no nearest analogue or refit. |
| `LOCAL-SHADOW` | Propagated into every payload; not stripped for eligibility. |
| one-sided seed | Out-of-lattice is not converted to a favorable negative. |
| A7 branches | Both carried and later computed per branch; no selection or merge. |
| M9 | Permanent limitation carried; not promoted to proved independence. |
| P3/P4 | Type/observable and J-II gaps remain; not laundered as custody tasks. |
| member/route/address | None selected. |
| uncertainty | Frozen sources/traces required; no post-result resizing. |
| measured central value | Not consulted. |
| cross-sector unit | Every actual conversion declared in advance; no invisible factor equal to one. |
| failed gate | No residual patch or desired comparison repairs it. |

`ANTI_TUNING = CLEAN`.

### 7.3 Cross-sector-unit row

Any future supplier crossing sectors must exhibit

```text
U_cross : Carrier_source -> Carrier_target,
domain(U_cross), codomain(U_cross), units(U_cross),
covariance(U_cross), source_hash(U_cross).                 (B1)
```

If no cross-sector arrow is consumed, the payload states
`CROSS_SECTOR_UNIT = not_applicable`. If an arrow is consumed and `(B1)` is
absent, assembly and sealing stop. This artifact declares no value for
`U_cross`; the J-II object remains TYPE-U.

### 7.4 Permanent fail-closed regressions

The following attacks remain armed:

```text
authority or source-hash drift;
living-register whole-file pin substituted for an entry citation;
demoted prose graph used for structure;
missing or merely inherited parent seal;
uncertified applicable non-seal conjunct;
END-TO-END node omitted from final ancestry;
schema or canonical-ID mismatch;
Q-25 limitation removed from a headline;
vector candidate without collector revision and recommitment;
F-U1 / F-U2 / F-C3;
empty eligible set;
universe or prediction-map drift;
prior outcome access;
commitment failure or beacon substitution;
duplicate ambiguity or identical comparator prediction;
type erasure or undeclared cross-sector conversion;
M9 promoted to proved independence;
P3/P4 relabeled custody-only;
J-II treated as supplied by A32;
branch, route, address, or member selection;
post-unmask edit;
numerical execution before its gate.                       (B2)
```

No regression in `(B2)` is scored using a desired numerical consequence.

### 7.5 Self verb audit

| Verb | Display | Audit |
|---|---|---|
| `located` | 16-row census | clean |
| `verified` | displayed hashes/attachments and direct JSON parse | clean |
| `authoritative` | used only for `stage_dependencies` under source 16 | clean |
| `descriptive` / `binding` | used only for preserved `stage_dag` non-seal terms | clean |
| `ratified` | A32 V002 and sealed principal acts only | clean |
| `carried` | Task-5 certification, A7, CARRY-1, and Q-25 | clean |
| `permanent` | Q-25 limitation only | clean |
| `same J-II object` | Q-521 and Q-534 entry-level identity | clean |
| `consolidated` | `(G1)`, §1.4, and `(G7)` | clean |
| `re-derived` | changed-row table in §1.6 | clean |
| `evaluator-certified` | required future conjunct, never claimed complete | clean |
| `revised` | complete operative V002 instrument | clean |
| `attained` | used only under negation | clean |
| `sealed` / `authorized` | historical authority or future conditional; no current seal claimed | clean |
| `computed` / `evaluated` / `compared` | used only under explicit negation or future condition | clean |

Every final-board verb is backed by a display above. No unbuilt map is called
built, no TYPE-U family inhabited, no local shadow physical, no child seal a
parent, and no preparation an attainment.

## Final board

```text
SOURCE_CENSUS = 16
AUTHORITATIVE_GRAPH = stage_dependencies / 11 nodes
DESCRIPTIVE_NONSEAL_TERMS = 24 retained

SPEC-SEAL = false_of_record
HOLDOUT-UNIVERSE-SEAL = false_of_record
QSPEC-SPEC-SEAL = false_of_record
PREDICTION-MAP-SEAL = false_of_record
A32_PRE_EVALUATION_READY = false
A32_RESULT = UNATTAINED

LIVE_OBLIGATIONS = 20
SATISFIABLE_AS_INHERITED = 6
NEEDS_A_TASK_6_STEP = 6
IN_TENSION = 8

M9 = permanent_carried_limitation
P3 = type_observable_gap
P4 = JII_type_crossing_TYPE_U

LOCAL_CHAIN = executable_of_record_but_not_executed_here
PRODUCT_TYPE = LOCAL-SHADOW
TRIAL = one_sided_on_current_finite_chi_lattice
A7_BRANCHES = both_carried_no_selection
CARRY_1 = exact
CROSS_SECTOR_UNIT = declared_if_consumed

MEMBER_BINDING = none
FIXED_POINT_EXECUTION = none
END_TEST = none
NUMERIC_EVALUATION = none
MEASURED_CONSTANT_COMPARISON = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

CENSUS = 16
M5A = RULED_PARENTS_NAMED (+sequence bound)
INSTRUMENT = REVISED
REGISTER_CITATIONS = by_entry
VERB_AUDIT_SELF = CLEAN
