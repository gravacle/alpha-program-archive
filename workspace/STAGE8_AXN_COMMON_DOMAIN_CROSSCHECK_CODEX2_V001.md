CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits for this artifact and its sidecar
VERDICT_BEARING_SET = exactly the 11 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2   ROLE_THIS_RELAY = VERIFIER   SUBJECT_LANE = DARIO
SUBJECT_KIND = claimed relative-domain theorem
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_947_COMMON_DOMAIN_CROSSCHECK_CODEX2_V001.md` | `cc8cc2612b0469765be01abb2fa2397d4b03060bc8fa4b991a10bd49f1b2a7e5` | assignment |
| 02 | `STAGE8_AXN_COMMON_DOMAIN_DARIO_V001.md` | `870f10d8d1b8b56bc0597f77ac2ff41ad8b8b23e98410db026d646f17a793d99`; number route `[4396,6807)` `96fcb388d592e044c733e28f3f4411ab20ba57b7c83df721bc2175009324c1e0`; Sobolev step `[6807,8580)` `bded3d9142173706e5fb129300f712dbab29b69ae6a0658b9fb4b3c06f37c602`; second-quantized step `[8580,11215)` `043a5842905deef6dd62097902d4205bd2581c51cc7856fc8e2aee7a99e2e15b`; characterization `[11215,13021)` `d890b63cdf744cafefec83c1be31cd7d737dd3c20f41aa2faa949da77ac25584` | subject under proof-grade check |
| 03 | `STAGE8_AXN_CHAIN_INSTANCE_CROSSCHECK_CODEX2_V001.md` | `107aa7509570fb12010a6f8ef482f7737527570b1741e77a33f78ff1532b7261`; missing-domain finding `[11931,12922)` `11672e9d1518f19450ef295ad281676904a282ccf4e013a44f189a40fa606393` | 926 receiver and countercontrol |
| 04 | `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md` | `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9`; parent `[5711,6867)` `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3` | `h_0`, bounded cell multiplier, finite-`K` parent, and lift receiver; nested custody through member 02's sealed table |
| 05 | `STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md` | `a1258dcf40732f0e3fce358a68ffdbd34bc347d70283550440582a1129bec510`; continuum carrier/masslessness `[334,824)` `9b6eaa618e7d6f92bd00c0af9d8df53edfe59d71ea1151c561fc79ad7866c251` | `L2(R^3;C^4)`, free massless Dirac multiplier, purely continuous spectrum; nested custody through member 02 |
| 06 | `STAGE8_H1_CHAIN_VERIFICATION_RETURN_V001.md` | `4faffdfc03144542e39ddd22e8c4f34cd992fa9d2f8a7d332c4a6890735c89f1`; localizer and `h_0` `[6314,7167)` `5d99ff2a46522cffb28d211e477ada028274abaa16734874a60cfbf5cfee5279` | bounded-ball localizer, three-dimensional carrier, and `sigma(h_0)=R` no-gap statement |
| 07 | `STAGE8_AXN_SPEC_ACTS_DARIO_V001.md` | `241ccf880266a895f5955173c8c87e7d180d8c6ba6dffefd10f257267454eca1`; trace-class criterion `[6959,8913)` `b8b7426e7bddfd1eeaa59b2e5b42ba62ae7e8de442e572375102eda56fe929ae` | Q-829 fermionic-lift criterion |
| 08 | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V001.md` | `9cfafde1774e78b61b65d44858faf73f177c2ec75f323911473bae06a805ce7b`; Lipschitz-localizer control `[12088,12620)` `2beb8ea2d086f1d4eba50c43d764c713dfeed227058e4fc35e0290b14b1bcdc4` | law-9b cutoff candidate, classified as spatial smoothing in the unrelated sea-kernel divergence |
| 09 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 10 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process, correction, custody, and claim-status law |
| 11 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01-S37 flattening guard |

```text
ALL WHOLE-FILE PINS AND SIDECARS AVAILABLE AT THIS CLOSURE VERIFIED BEFORE READING.
MEMBERS 04 AND 05 HAVE NO ADJACENT CLEANROOM SIDECAR; THEIR EXACT BYTES REHASH TO THE DIGESTS
PINNED INSIDE THE SEALED SUBJECT, AND ONLY THAT NESTED CUSTODY IS USED.
NO PHYSICAL QUANTITY IS NUMERICALLY EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN COMMON-DOMAIN THEOREM CROSS-CHECK — CODEX 2 — V001
## RELAY 947 — `[PLAN:AXN-BUILD-C46]` — PROOF-GRADE ADVERSARIAL CHECK

Date: 2026-08-10  
Status: **THE RELATIVE-DOMAIN THEOREM BOOKS, WITH ONE PROOF-RATIONALE CORRECTION. The
off-diagonal inequality is valid by positive tensor-product order, not by the subject's false claim
that all four same/different-factor operators commute. The optional signed-Dirac Kato-Rellich
strengthening remains excluded; it is not needed for the domain inclusion.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Exact receiver and standard

The subject must answer 926's countercontrol: one-particle boundedness of `M_c` does not make
`dGamma(M_c)` bounded, and without a positive gap the number operator is not controlled by the
massless lift. The claimed replacement is the precise graph-domain inclusion

```text
Dom(dGamma(|h_0|)) subset Dom(dGamma(M_c)),
```

at fixed time and finite cell set. That is the receiver checked below. I do not weaken it to a form
domain, identify `dGamma(h_0)` with `dGamma(|h_0|)`, or treat a synthetic finite-dimensional control
as proof.

## 2. Section 3 — the Sobolev step [PROVABLE — CONFIRMED]

Let `H=|p|` on `L2(R^3;C^4)` and let `M` be the nonnegative scalar multiplication operator supported
in the bounded cell `Omega`. For scalar `u`, the critical homogeneous Sobolev embedding gives

```text
||u||_3 <= C_S || |p|^(1/2) u||_2.
```

Hoelder with `3/2` and `3` then gives, on the form domain of `H`,

```text
<u,Mu> <= ||M||_inf int_Omega |u|^2
        <= ||M||_inf |Omega|^(1/3) ||u||_3^2
        <= ||M||_inf C_S^2 |Omega|^(1/3) <u,Hu>.
```

Thus

```text
M <=_form a H,       a=||M||_inf C_S^2 |Omega|^(1/3).
```

### 2.1 Spinor-valued carrier

The four-component case introduces no hidden scalarization and no factor of four. With
`|psi|^2=sum_alpha |psi_alpha|^2`, Minkowski in `L^(3/2)` and the scalar embedding give

```text
||psi||_(L^3(C^4))^2
 = ||sum_alpha |psi_alpha|^2||_(L^(3/2))
 <= sum_alpha ||psi_alpha||_3^2
 <= C_S^2 sum_alpha <psi_alpha,|p|psi_alpha>
 = C_S^2 <psi,(|p| tensor I_4)psi>.
```

Member 05 seals exactly `L2(R^3;C^4)` and the free massless Dirac multiplier, so
`|h_0|=|p| tensor I_4`. Member 06 seals the multiplier as the bounded-ball localizer. The embedding
therefore applies componentwise to the sealed Dirac structure.

### 2.2 Direction and noncommutation

`M` and `H` need not commute. The displayed relation is a **quadratic-form order**, not the norm
claim `||M psi||<=a||H psi||` and not an operator-product comparison. This direction is exactly what
section 4 can consume after it is carried through positive tensor products. No illicit squaring of
`M<=_form aH` occurs.

### 2.3 Independent scaling derivation

Write a candidate volume factor as `|Omega|^beta`. Under
`psi_lambda(x)=lambda^(3/2)psi(lambda x)` and `Omega -> Omega/lambda`, the localized mass is
invariant, while

```text
<psi_lambda,H psi_lambda> = lambda <psi,H psi>,
|Omega/lambda|^beta = lambda^(-3 beta)|Omega|^beta.
```

The right side scales as `lambda^(1-3 beta)`. Equality of scaling for all positive `lambda` forces
`1-3 beta=0`, hence `beta=1/3`. This is independent of the Hoelder calculation and confirms the
subject's exponent.

## 3. Section 4 — fermionic square and off-diagonal term

### 3.1 Antisymmetrization [PROVABLE — CONFIRMED]

On the full `n`-fold tensor product define `M_i` and `H_i` on factor `i`. The sums
`sum_i M_i`, `sum_i M_i^2`, and `sum_(i!=j)M_iM_j` commute with every permutation representation,
so the antisymmetric subspace is invariant under each **sum**. Individual `M_i` need not preserve
that subspace; the proof never requires that. Expanding on the full tensor product and then
restricting gives

```text
dGamma(M)^2 = dGamma(M^2) + sum_(i!=j) M_i M_j
```

on every fermionic sector and hence on the finite-particle core.

### 3.2 The subject's commutation sentence is false; the bound survives [CORRECTED RATIONALE]

The subject says all four operators in a fixed ordered-pair comparison commute. They do not:
`M_i` generally fails to commute with `H_i`, and similarly on factor `j`. The required inequality
does not need that claim. Positive tensor-product order supplies it. After reordering the two active
factors,

```text
a^2 H tensor H - M tensor M
 = (aH-M) tensor aH + M tensor (aH-M) >=_form 0.
```

Therefore, for each `i!=j`,

```text
M_i M_j <=_form a^2 H_i H_j.
```

Summing, then restricting the permutation-invariant forms to the antisymmetric sector, gives

```text
sum_(i!=j) M_iM_j
 <=_form a^2 sum_(i!=j) H_iH_j
  = a^2[dGamma(H)^2-dGamma(H^2)]
 <= a^2 dGamma(H)^2.
```

The diagonal term is also lawful: functional calculus for the bounded positive multiplier gives
`M^2<=||M||M`, followed by the form bound `M<=_form aH`, hence

```text
dGamma(M^2) <=_form a||M|| dGamma(H).
```

Adding the two terms yields exactly

```text
||dGamma(M)psi||^2
 <= a^2||dGamma(H)psi||^2 + a||M||<psi,dGamma(H)psi>.
```

This is a quadratic-form proof of the graph estimate; it never assumes `M` commutes with `H`.

## 4. Kato coefficient and domain extension [PROVABLE — CONFIRMED AT THE CLAIMED RECEIVER]

Put `X=||dGamma(H)psi||`, `Y=||psi||`, and `c=a||M||`. Cauchy-Schwarz followed by Young gives

```text
c<psi,dGamma(H)psi> <= cXY <= (c eps/2)X^2 + (c/(2 eps))Y^2.
```

Thus, for every `eps>0`,

```text
||dGamma(M)psi||
 <= sqrt(a^2+c eps/2) ||dGamma(H)psi||
  + sqrt(c/(2 eps)) ||psi||.
```

The graph coefficient tends to `a` as `eps->0`, while the additive coefficient is

```text
b_eps=sqrt(a||M||/(2 eps)) = O(eps^(-1/2)),
```

and diverges. That growth is the price of approaching the infimal relative bound; it is not hidden.

The estimate is first proved on the finite-particle core, which is a core for `dGamma(H)`.
Approximating in the `dGamma(H)` graph norm makes `dGamma(M)psi_k` Cauchy; closedness of the
self-adjoint `dGamma(M)` gives

```text
Dom(dGamma(H)) subset Dom(dGamma(M)),       H=|h_0|.
```

This is the claimed theorem. For the finite-`K` parent, multiplication by the sealed bounded
spin/record factors and taking a finite sum only multiplies/adds symbolic graph constants; it does
not change the domain argument.

**Scope correction to the optional strengthening.** The estimate is relative to
`dGamma(|h_0|)`, not automatically to the signed `dGamma(h_0)`. Therefore even a future proof of
`a<1` would directly invoke Kato-Rellich for `dGamma(|h_0|)+dGamma(M)`; applying it to the signed sum
would additionally require a positive-energy restriction or a graph comparison not displayed here.
The subject's main domain theorem does not use that optional statement. Separately,
`h_0+M` is self-adjoint on `Dom(h_0)` by one-particle bounded perturbation, so its second
quantization is self-adjoint on its own natural domain.

## 5. Infrared dissolution

### 5.1 Slater witness [PROVABLE — CONFIRMED]

Members 05 and 06 seal a free massless Dirac operator with purely continuous spectrum and
`sigma(h_0)=R`. For every `delta>0`, the spectral subspace of `|h_0|` below `delta` is
infinite-dimensional. Choose `n` orthonormal vectors there and form their Slater determinant
`Psi_n`. Then

```text
||N Psi_n||=n,
||dGamma(h_0)Psi_n|| <= sum_i ||h_0 phi_i|| <= n delta.
```

Any graph estimate `||N psi||<=A||dGamma(h_0)psi||+B||psi||` would give
`n<=A n delta+B`; choosing `delta<1/(2A)` and then `n>2B` contradicts it. The subject's particular
`1/(4A), n>4B` choice is a stronger numerical separation. Pauli exclusion does not rescue the
estimate because the low-energy spectral subspace is infinite-dimensional.

### 5.2 What actually rescues the direct route

For a normalized band-limited spinor, Fourier Cauchy-Schwarz gives
`||psi||_inf^2<=C delta^3`; hence

```text
int_Omega |psi|^2 <= C |Omega| delta^3.
```

So the subject's localization heuristic is correct up to the harmless Fourier-normalization
constant. **It is motivation only.** No line in sections 3-4 consumes that estimate. The proof-grade
rescue is the Sobolev-Hoelder quadratic-form bound of section 2 above. The theorem therefore does
not lean on an unproved motivational sentence.

## 6. Characterization and law-9b replay

The search surface was the cleanroom's top-level Markdown artifact set, excluding the subject being
checked, mutable indexes/status files, relay I/O, run/evidence replicas, and this output. Exact-name,
normalized-name, fixed-string, and semantic-receiver modes were run. Candidate text received no
weight until checked at a content-addressed member above.

| Search | Census and adjudication | Result |
|---|---|---|
| positive gap for this `h_0` | exact `h_0`/gap search has one authoritative hit, member 06, and it says `sigma(h_0)=R` with **no gap**; member 05 independently seals free masslessness and purely continuous spectrum; other spectral-gap hits concern different operators/receivers | **NO GAP POSSIBLE FOR THIS RECEIVER** |
| infrared cutoff/regulator for this `h_0` | exact `IR/infrared + cutoff/regulator` returns zero pre-subject artifacts; normalized search returns the V001/V002 successor pair's repeated **Lipschitz spatial-localizer cutoff**, member 08's control, which remains divergent and is not a momentum-space infrared regulator for `h_0` | **NO IR CUTOFF OF RECORD** |
| particle-number restriction/bound | exact phrases `particle-number bound/restriction`, `number cutoff`, `N-bounded`, and fixed-string `N <= ... dGamma` return zero grants; the number-operator construction and 926's particle-number-growth countercontrol are occurrences, not bounds, and 926 expressly denies a lower bound controlling `N` | **NO NUMBER BOUND** |

The three positive hypotheses are also sealed at bytes:

- **bounded cell:** member 06 gives `M_c(t)` as multiplication by a bounded-ball indicator; member 04
  places each spatial section inside its causal cell and the finite-`K` cell term is bounded;
- **masslessness:** members 05 and 06 name the free massless Dirac operator and `sigma(h_0)=R`;
- **dimension three:** members 05 and 06 give `L2(R^3;C^4)` and the three-dimensional momentum
  multiplier.

Thus the theorem's actual hypotheses are present, while the three tempting rescue assumptions are
absent and unnecessary.

## 7. Independent finite-sector replay

The control was rebuilt independently, not copied. With deterministic seed `947031`, one-particle
dimension 5, `a_control=1.3`, four positive random `H`, and
`M=a H^(1/2) X H^(1/2)` for a positive contraction `X`, each pair satisfies `0<=M<=aH` while
`[M,H]` is generically nonzero. The exterior-power isometries were constructed explicitly for
fermionic sectors `n=2,3`; 200 random normalized states per pair and sector give 1,600 states.

```text
pair 1: min eig(offdiag margin) = 2.374e+00 ; min eig(full margin) = 5.960e+01
pair 2: min eig(offdiag margin) = 4.612e+00 ; min eig(full margin) = 8.755e+01
pair 3: min eig(offdiag margin) = 8.590e-01 ; min eig(full margin) = 2.853e+01
pair 4: min eig(offdiag margin) = 4.857e+00 ; min eig(full margin) = 8.183e+01
states = 1600 ; violations = 0
```

The displayed margins are the worst over `n=2,3` for each pair. This is a dimensionless
mathematical control only and carries no physical verdict weight.

## 8. Held-backs and verdict

- `a<1` was **not evaluated**. `|Omega_c|`, `||M_c||_inf`, and `C_S` remain symbols. The synthetic
  `a_control=1.3` belongs only to the dimensionless control and is not the physical `a`.
- Optimality of `a` remains unclaimed.
- Uniformity in `t` remains unclaimed; it would require the two symbolic suprema the subject names.
- Self-adjointness of the signed sum **on** `Dom(dGamma(|h_0|))` is not promoted from the optional
  Kato sentence; the exact signed/absolute scope distinction is recorded in section 4.

These held-backs do not enter the graph-domain inclusion. The theorem therefore books at its exact
receiver, and line 6's common absolute-energy-domain/affiliation remainder closes. The Galerkin
selector and slicing datum remain separate; nothing here enters either.

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  M_c(t), the bounded cell support, the free massless h_0, and the Q-829 trace-class criterion;
  all constants a, ||M_c||_inf, C_S, |Omega_c|, eps, and b_eps remain symbolic.

CONDITIONED-ON:
  the sealed nonnegative spatial-section multiplier;
  the standard critical Sobolev embedding and the finite-particle-core theorem.

SUBSTITUTED:
  NOTHING. No gap, infrared cutoff, number restriction, profile, cell size, basis, or selector was
  introduced. The synthetic finite-sector matrices are controls, not physical instances.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 10. Flattening, custody, and self audit

- **S01-S37 FLATTENING CHECK:** walked. A form order was not identified with an operator norm
  inequality. Same-sector tensor placement was not identified with pairwise commutation. A
  motivational band-limit estimate was not identified with the proof. `dGamma(h_0)` was not
  identified with `dGamma(|h_0|)`. A synthetic check was not identified with a theorem.
- **F_PLDEC:** symbolic analysis plus a dimensionless finite-matrix control. No physical quantity
  was numerically evaluated and no measured constant was consulted.
- **M-2 / four modes:** exact, normalized, fixed-string, and semantic-receiver searches covered the
  three law-9b classes, `M_c`, `|h_0|`, `dGamma`, `N`, Sobolev/Hoelder, antisymmetric/exterior-power,
  Kato, and all hyphen/underscore/spacing variants.
- **BLIND:** held. No rank, ratio, or fiber comparison was opened or formed.
- **PE-1..PE-13:** pointer-only and zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** observed. This lane verifies the opposite lane's theorem.
- **CORRECTION PROPAGATION:** the false pairwise-commutation rationale is replaced everywhere in
  this report by tensor-product order; the optional signed Kato scope is narrowed and not booked.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet, physical
  numerical evaluation, or measured-constant comparison was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 3894
PREDECLARATION_OUTPUT_SCAN = 0 hits
SOBOLEV_REDERIVED = true
OFFDIAGONAL_RATIONALE_CORRECTIONS = 1
NUMERIC_CONTROL_STATES = 1600
NUMERIC_CONTROL_VIOLATIONS = 0
BOOKINGS_MADE_BY_THIS_LANE = 1 theorem at the exact receiver
CHAIN_INVOKED = false
```

Self verb audit: “confirmed” is used only for independently re-derived form/domain statements.
“Corrected” applies to the subject's false pairwise-commutation rationale and the optional signed
Kato scope; neither correction changes the main inequality. “Books” applies only to the exact
relative-domain theorem after proof-grade replay. The two principal decisions remain untouched.
`VERB_AUDIT_SELF = CLEAN (+1 subject rationale corrected; optional strengthening narrowed)`.

## 11. Final lines

```text
CLOSURE = declared-first (byte range [0,3894), scan 0 hits)
SOBOLEV = CONFIRMED (spinor+form direction)
EXPONENT = FORCED-CONFIRMED
OFFDIAG = CONFIRMED (antisymmetrization survives; pairwise-commutation rationale corrected to tensor-product order)
KATO = CONFIRMED (relative to dGamma(|h_0|); coefficient -> a, b_eps = O(eps^(-1/2)))
IR_DISSOLUTION = PROOF-GRADE (Slater witness confirmed; O(|Omega_c| delta^3) is motivation only; Sobolev form bound does the proof)
LAW9B = CONFIRMED x3
HELDBACKS = HONEST (a<1 unevaluated; optimality and uniform-in-t excluded; signed-base Kato not promoted)
NUMERIC = REPLAYED (0 violations / 1600 states)
VERDICT = THEOREM-BOOKS (line 6's third remainder closes at the common absolute-energy-domain receiver)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+1 rationale correction; optional strengthening narrowed)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
