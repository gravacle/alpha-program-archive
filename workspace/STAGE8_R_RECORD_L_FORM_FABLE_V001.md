# STAGE 8 — THE FORM OF R_record,L ON CONNECTION TANGENTS: DERIVATION, FACTORIZATION, AND THE WINDING
## BLIND RECORD-NATIVE CONSTRUCTION — FABLE-RFORM — [CLAIMED]

Date: 2026-08-13
Role: TYPING/FORM ONLY. The FORM of the record susceptibility `R_record,L` is derived
from the record's own sealed content as far as it derives; its factorization across
the charged/EM and exterior/response tangent sectors is decided or the exact missing
piece named; whether the cross-sector structure forces the winding index `n` is
decided structurally. No value of `R_record,L`, `n`, `kappa`, `alpha`, any scale,
length, coefficient, or constant is computed, bounded, estimated, or compared. Every
numeric string below is a sealed-text quotation or an exact structural integer.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

Forbidden imports, held throughout: NO SCALE (no ell_P, fiber radius, metric length,
K_KK, c^2); NO FAITHFULNESS AS A PREMISE; NO IMPORTED GR (no KK ansatz, no
Einstein-Hilbert); NO INTERPRETIVE INPUT (no physical reading of the factorization).

---

## 0. VERDICT IN ONE LINE

**The FORM derives to a three-factor pullback — `R_record,L = n^2 · Phi^T C Phi` on
difference-branch connection tangents, with `Phi` the per-cell linear-write
differentials (exactly one functional per cell, from the one-character-power write)
and `C` the intensive post-limit Duhamel/cluster kernel — closing everything except
three named objects: the Gate-5 connected linked-cluster kernel `C`, the
scalarization/state-port pair, and the write chains' support realization. The
factorization across the two tangent sectors is UNDETERMINED with a decided
dichotomy inside it (rank-one at finite N: either a degenerate one-sector form or an
irreducible rank-one cross term — no third case, no basis removes it), pending
exactly the unsealed support/current-density realization of the write chains. The
winding is NOT-FORCED on either branch: `n` enters the derived form exactly once, as
the global quadratic prefactor `n^2` of an n-blind form, and every structural
predicate available of record is invariant under that scaling.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. The three tasked
members verified against the tasked digests; the rest verified at path this relay
(first 8 hex shown; full digests recomputed by `shasum -a 256` before reading).

| # | Source | Seal | Role |
|---:|---|---|---|
| T1 | `STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md` | `e6fae142...ca6ee` MATCHES-TASKED | locates R_record,L, the SPEC GAP, the linear write, the dim-32 freedom, the factorization fork |
| T2 | `STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md` | `660e0c14...5df1f` MATCHES-TASKED | the face-response objects at their V011 spans; the materiality criterion and its two SPEC GAPS |
| T3 | `STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` | `1d11f150...ffa75` MATCHES-TASKED | connection-only interior; the one-argument import-block at `g` |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d49` SEALED-OK | THE BEDROCK: Gamma_K, the monoidal/linked-cluster structure, V_L, G_L, R_record,L, the Duhamel constraint, the tangent complex, Q_flux, the energy, C_ref (the sealed packet member, NOT the same-named top-level copy — T2 §0.2's pin hazard honored) |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffce` SEALED-OK | the exact ratified finite-N influence functional: `F_N = P_0 + Z_N P_ch`; the zero cross-history entries; the record sandwich leaves a source operator |
| M06 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2` SEALED-OK | scalarization unbuilt: `CANONICAL_SCALAR_F_N_FROM_RECORD_SANDWICH_ALONE = false | TYPE-R`; `RHO_PRE_PLACED = false | TYPE-U`; `U2_STATE_PORT_INSTANTIATED = false | TYPE-U` |
| FR | `STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md` | `4d072e76` SEALED-OK | the dim-32 physical, non-gauge assignment freedom; support and current density NOT SUPPLIED |
| CV | `STAGE8_7A_COUNTEREXAMPLE_CONTEST_DARIO_V001.md` | `c854c8b2` SEALED-OK | the non-coboundary certificate; the forced energy on the equal-energy set |
| W | `STAGE8_BARE_SURFACE_ALPHA_DETERMINATION_FABLE_V001.md` | `82e81f6e` SEALED-OK | the one-character-power write; `chi_n(holonomy)`; unit-modulus characters; the relative holonomy of the doubled history; the n = 0 elimination (carried); the faithfulness result (BARRED, not consumed) |

No register, tracker, plan, road, ledger, or lens file read. No git action. Output
name probed before write: ABSENT.

---

## 2. THE SEALED BEDROCK — QUOTED, NOT PARAPHRASED

**B1 — the object (V011).**

```text
R_record,L(a,b)
  =d^2/ds dt [Gamma_K(sa+tb)/N_4(K)]|_(s=t=0),
```

"evaluated after the thermodynamic/linked-cluster limit."

**B2 — the action it is the Hessian of (V011).** `Gamma_(disjoint c) =
-log|A_(disjoint c)| = sum_c Gamma_c` — the MODULUS action, monoidal over disjoint
cells: "the Hessian of Gamma is additive without inserting an extensive factor."
For connected cellulations: "the complete amplitude must additionally admit a
linked-cluster expansion Gamma_K = sum_(connected clusters C subset K) gamma_C,
with a target-free locality bound that makes gamma_density = lim Gamma_K /
number_of_4_cells(K) exist and makes boundary corrections subextensive." Of the
frozen derivation: "The disjoint theorem is proved; the connected thermodynamic
limit remains a Gate-5 obligation."

**B3 — the linear response objects (V011).** `V_L(a) = d/ds B_L(sa)|_(s=0)`;
`G_L(a;tau_R) = integral_0^(tau_R) exp(iB_L(0)t) V_L(a) exp(-iB_L(0)t) dt`; and the
sealed constraint on the form: **"The Duhamel covariance computed from `G_L` must
reproduce this Hessian."**

**B4 — the tangent complex (V011).** `A^0 = R^(K_0)`, `A^1 = R^(K_1^+)`,
`A^2 = R^(K_2^+)`; `(d_0 lambda)_e = lambda_t - lambda_s`; `(d_1 a)_f =
sum_(e in boundary f) incidence(f,e) a_e`; **`d_1 d_0 = 0`**;
`P_h = I - d_0(d_0^dagger d_0)^+ d_0^dagger`; `F_phys = im(d_1)`;
`Q_flux = P_h d_1^dagger (d_1 P_h d_1^dagger)^+|_(F_phys)`, the unique horizontal
minimum-norm lift; `mathcal_K_L(xi,zeta) = R_record,L(Q_flux xi, Q_flux zeta)`.

**B5 — the ratified connection dependence (M03, T3).** The record's ratified
interacting object is "indexed by CONNECTION / HOLONOMY HISTORIES ALONE — no metric
argument anywhere" (T3, member-03 span). Its exact finite-N law (M03 §2–3):
`D_(n,j)[a_j] = diag(1, z_j^(n)[a_j], 1)`; `W_(1,j)^(n) = D_(n,j) S_j`;
`U_N^(n)[a] = P_0 tensor I + P_ch tensor W_N^(n)[a]`;
`W_N^(n)[a]|R_N> = (product_j z_j^(n)[a_j]) |P_N>`;
`F_N[a_+,a_-] = P_0 + Z_N[a_+,a_-] P_ch` with
`Z_N = product_j conjugate(z_j[a_(-,j)]) z_j[a_(+,j)]`; the cross-history entries
`I_N(0,ch) = I_N(ch,0) = 0` "because `<R_N|P_N> = 0`"; and
`RECORD_SANDWICH_LEAVES_SOURCE_OPERATOR = true`.

**B6 — the write's character structure (W).** `z_(±,j)^(n) = chi_n(h_j[a_±])` —
"unit-modulus holonomy characters"; `Z_N^(n)[a_+,a_-] = chi_n(relative holonomy of
the doubled history)`; the exact write carries "exactly one character power"
(`D_n[a] S |r> = z_n[a] |p_Q>`); the trivial character `n = 0` "has zero variation
under every external history and fails the surface's own charge/flux-access
requirement" (carried at checked strength).

**B7 — the physical tangent freedom (FR, CV).** The refined edge assignment "is
free on a 32-dimensional set at fixed energy, and the difference is not a
coboundary — physical, not gauge" (FR; CV's exact-elimination certificate:
`d_0 lambda = x_alt - x_F` inconsistent). RA27-3's `support` and `current density`
fields: **NOT SUPPLIED** (FR board).

**B8 — scalarization unbuilt (M06).** `CANONICAL_SCALAR_F_N_FROM_RECORD_SANDWICH_
ALONE = false | TYPE-R`; `RHO_PRE_PLACED = false | TYPE-U`;
`U2_STATE_PORT_INSTANTIATED = false | TYPE-U`. The record sandwich leaves a source
OPERATOR; no canonical scalar exists from the sandwich alone.

**B9 — the phase object and its bar (V011).** `Gamma_Q[A] = -i Log(Z_Q[A]/Z_Q[0])`
with `c = 1` fixed by the complete charged amplitude; "Until `Z_Q` is constructed
from complete `Q_spec`, the conversion from `kappa_record` to a physical action
coefficient remains unauthorized."

---

## 3. THE DERIVATION — WHAT GENUINELY DERIVES, STEP BY STEP

### D1. The tangent kinematics: the record's own three-block split [PROVABLE from B4]

From `d_1 d_0 = 0` alone: `ker(d_1) supseteq im(d_0)`, hence orthogonally (in the
sealed Gate-3 counting metric)

```text
A^1 = im(d_0)  ⊕  H  ⊕  im(d_1^dagger),
H := ker(d_1) ∩ im(d_0)^perp        (the flux-free physical block),
im(d_1^dagger) = ker(d_1)^perp      (the flux-carrying physical block).
```

**And the exterior/response carrier is exactly the third block.** Because
`d_1 d_0 = 0` gives `d_0^dagger d_1^dagger = 0`, every `d_1^dagger x` is already
horizontal, so `P_h d_1^dagger = d_1^dagger` and

```text
Q_flux = d_1^dagger (d_1 d_1^dagger)^+|_(F_phys),   im(Q_flux) = im(d_1^dagger).
```

This is a one-line consequence of the sealed complex, consistent with V011's own
orthogonality clause (the lift orthogonal to `ker(d_1) ∩ im(d_0)^perp`). The sealed
dim-32 assignment freedom (B7) is physical and non-gauge of record (its difference
is not a coboundary), i.e. it lives in the physical complement of `im(d_0)`; the
exact structural integer 32 is quoted, not derived here.

### D2. The connection dependence of the ratified action is the write, and n factors out [PROVABLE from B5, B6]

On the ratified object the connection enters ONLY through the per-cell characters
`z_j^(n)[a_j] = chi_n(h_j[a_j])` (B5: no other `a`-dependence exists anywhere in the
law). Characters of the (abelian) holonomy give per-cell phase functionals LINEAR in
the tangent: writing `h_j[a] = exp(i <ell_j, a>)` with `ell_j` the cell-j history
chain (an incidence datum of the write, NOT sealed as an object — see D6),

```text
d/ds z_j^(n)[s a]|_(s=0) = i n <ell_j, a> · z_j^(n)[0],    chi_n(identity) = 1.
```

**Two exact consequences.** (i) The linear-response operator is `V_L(a) =
n · sum_j <ell_j, a> X_j` with `X_j` FIXED n-independent operator directions (the
per-cell write generators): the winding multiplies the response uniformly and
appears nowhere else — the base objects `B_L(0)`, `X_j`, and the base state are
n-BLIND because every character is trivial at the identity. (ii) Exactly one
functional per cell: the "exactly one character power" clause (B6) means the write's
differential is the single family `Phi = (<ell_j, ·>)_j` — no second charged
functional exists per cell.

### D3. The channel is intrinsically two-branch [PROVABLE from B5]

From the sealed finite-N identities:

```text
one branch:   <R_N| W_N^(n)[a] |R_N> = (product_j z_j)<R_N|P_N> = 0   identically —
              the ready-to-ready single-branch amplitude through the charged sector
              VANISHES for every a and every n (the write writes: W|R> ⊥ |R>).
two branches: <R_N| W_N[a_-]^dagger W_N[a_+] |R_N> = Z_N[a_+,a_-]  (sealed eq. (3)),
diagonal:     a_+ = a_-  ⇒  Z_N = product_j |z_j|^2 = 1  ⇒  F_N = P_0 + P_ch = I.
```

So the connection response of the modulus action `Gamma = -log|A|` (B2) through the
charged sector (a) does not exist on a single branch at all, (b) exists on the
doubled contour, and (c) is FLAT on the branch diagonal. **Derived: `R_record,L`'s
connection argument is carried by the difference-branch (relative) combination
`a_Delta = a_+ - a_-` of the two-branch CTP algebra; the doubled algebra is not
optional for this object.** The write functional on the doubled tangents reads only
`phi_j[(a_+,a_-)] = <ell_j, a_Delta>`; under the licensed common-gauge moves it is
invariant exactly (abelian relative holonomy — W's C4 as carried).

### D4. The scalarized finite-N Hessian, exact and conditional [PROVABLE mechanics on B5; CONDITIONAL on B8]

The record sandwich leaves the source operator `F_N = P_0 + Z_N P_ch` (B5). Any
scalarization — the unbuilt state-port pair (B8) — produces sector weights
`w_0 = <E, P_0 rho>`, `w_ch = <E, P_ch rho>` and the exact scalar

```text
A_N[a_+,a_-] = w_0 + w_ch · chi_n(h_rel) = w_0 + w_ch · exp(i n Phi_tot[a_Delta]),
Phi_tot[a] := sum_j <ell_j, a>,
Gamma_N = -log|A_N| = -(1/2) log( |w_0|^2 + |w_ch|^2
                                  + 2|w_0||w_ch| cos(n Phi_tot + theta) ).
```

Its exact mixed second variation at the base point is

```text
Hess Gamma_N (a,b) = n^2 · mu(w) · Phi_tot[a] Phi_tot[b],
```

with `mu(w)` a symbolic rational expression in the sector weights and their relative
phase — NOT evaluated here; its one structural property is displayed:
**`mu(w) ≡ 0 iff w_0 w_ch = 0`.** Three exact structural facts:

```text
(i)   the finite-N charged Hessian is RANK ONE: n^2 mu · Phi_tot ⊗ Phi_tot;
(ii)  it is an INTERFERENCE term: on the neutral sector alone the amplitude is
      constant, on the charged sector alone |Z_N| = 1 makes the modulus action
      IDENTICALLY ZERO — the entire connection response of -log|A| lives in the
      neutral×charged source-sector cross term;
(iii) it scales as n^2 and as nothing else in n.
```

### D5. The post-limit form and the Duhamel constraint — the closure and its exact gap [PROVABLE typing on B1–B3; the kernel UNSEALED]

The sealed definition (B1) is intensive and post-limit. The sealed constraint (B3)
says the Hessian must be reproduced by the Duhamel covariance of the transported
response operators `G_L(a)`; with D2's `V_L(a) = n sum_j <ell_j,a> X_j` and `G_L`
linear in `V_L`, the covariance pulls back through the write differentials:

```text
R_record,L(a,b) = n^2 · sum_(j,k) C_jk <ell_j, a> <ell_k, b>
               =: n^2 · (Phi^T C Phi)(a,b)      on difference-branch tangents,
```

where `C_jk` is the intensive post-limit Duhamel kernel of the transported per-cell
write generators in the base record state. Everything about `C` that is TYPED of
record: symmetric and positive-semidefinite (the sealed word is "covariance", and a
Duhamel covariance of self-adjoint perturbations is one — typed, not proved here);
intensive (per `N_4`); additive over disjoint cells with no extensive factor (B2's
monoidal theorem); cluster-summable with subextensive boundary corrections (B2's
sealed locality requirement on the connected expansion); n-INDEPENDENT (D2: the
base dynamics at zero connection is n-blind, `chi_n(1) = 1` for every n).

**The exact gap, and it is sharp.** The finite-N scalarized form (D4) is
`n^2 mu · Phi_tot ⊗ Phi_tot` — an ALL-PAIRS, separation-independent, rank-one
kernel (`C_jk^(finite,coherent) = mu` for all j,k). That kernel is NOT
cluster-summable: it violates the sealed locality/intensivity requirement of B2.
So the coherent finite-N object cannot itself be the post-limit form; the
thermodynamic/linked-cluster limit must replace the all-pairs weight by a
cluster-decaying `C_jk` — and V011 seals exactly this as the open obligation ("the
connected thermodynamic limit remains a Gate-5 obligation"). Likewise the sign of
the finite-N `mu` is not structurally fixed, while the post-limit kernel is typed
PSD; the reconciliation is part of the same limit burden. **The FORM therefore
closes to `n^2 Phi^T C Phi` with every factor typed, and does not close further;
the exact missing pieces are named in D6.**

### D6. The three named missing pieces

```text
G1  the kernel C — the Gate-5 connected linked-cluster limit of the doubled record
    functional (sealed as an obligation, unsealed as an object; B2). It is the SPEC
    GAP of record (T2 §3.3 Path A) resolved one level down: the gap is not "any
    form" but exactly "which cluster kernel".
G2  the scalarization/state-port pair (w_0, w_ch) — sealed unbuilt (B8: TYPE-R for
    the sandwich alone, TYPE-U for the port). Without it Gamma_K is not a scalar
    and the finite-N anchor of D4 stays conditional.
G3  the write chains ell_j — the support/current-density realization of the per-cell
    write (which edges the per-cell holonomy reads). RA27-3's `support` and
    `current density` fields are NOT SUPPLIED of record (B7). This datum decides
    the carrier's decomposition against D1's three blocks and is the deciding
    object of section 4.
```

---

## 4. FACTORIZATION ACROSS THE TWO SECTORS

### 4.1 The sectors, posed on the derived kinematics

The two sectors of the tasking, realized record-natively on D1's derived split of
the physical tangents `P_h A^1 = H ⊕ im(d_1^dagger)`:

```text
EXTERIOR/RESPONSE sector:  im(Q_flux) = im(d_1^dagger)  — derived (D1): the face
    response mathcal_K_L reads R_record,L exactly and only on this block.
CHARGED/EM sector:         the write directions — the Riesz representatives
    ell_j^sharp of the write differentials Phi; gauge-invariant on the difference
    branch (D3); their decomposition across H ⊕ im(d_1^dagger) is datum G3.
The dim-32 physical freedom (B7) is the sealed inhabitant of the non-gauge
complement — the arena over which any cross-sector coupling acts.
```

### 4.2 The decided dichotomy [PROVABLE on the derived form]

On the derived form the whole of `R_record,L` is the pullback `n^2 Phi^T C Phi`;
its carrier is `span{ell_j^sharp}`. Write `phi_f` and `phi_H` for the
`im(d_1^dagger)`- and `H`-components of the write functionals. The sector-block
display is

```text
R_record,L = n^2 [  Phi_f^T C Phi_f    Phi_f^T C Phi_H  ]
                 [  Phi_H^T C Phi_f    Phi_H^T C Phi_H  ]     over [flux | H],
```

and at finite N (rank one, D4) this is `n^2 mu (phi_f + phi_H) ⊗ (phi_f + phi_H)`.
Two exact algebraic facts force a dichotomy with no third case:

```text
(a) a rank-one bilinear form is NEVER a direct sum of two nonzero forms
    (a direct sum of nonzero blocks has rank at least 2);
(b) a nonzero cross block is removable by NO basis that respects the sector split:
    sector-preserving changes act by block-diagonal congruence, sending the cross
    block M to S^T M T with S, T invertible — nonvanishing is preserved.
```

Therefore, at the finite-N anchor: **either** the total write direction lies
entirely in one sector (`phi_H = 0` or `phi_f = 0`), and the form FACTORIZES only
degenerately — `R = R_sector ⊕ 0`, the other sector's response identically zero —
**or** it straddles both, and the form carries the irreducible rank-one cross term
`n^2 mu · phi_f ⊗ phi_H`, which is DOES-NOT-FACTORIZE in exactly the tasked sense.
Post-limit, the same dichotomy is controlled by whether `Phi_f^T C Phi_H` vanishes.

### 4.3 What is and is not sealed about the deciding datum

The write functional is nonzero for every admitted `n != 0` (the sealed n = 0
elimination runs on zero variation; nonzero variation is `Phi != 0` — carried, B6).
The sealed phrase "charge/flux-access requirement" points the write's reading at
the flux block, but the tangent-block statement `phi_f != 0` is not proven at that
strength here, and `phi_H != 0` — the write chains' component in the dim-32
flux-free physical block — is exactly datum G3, NOT SUPPLIED of record. No sealed
object forces it either way; the sealed materiality fork (T2 §3.4) is logically
independent of this block question (it concerns `Phi_f^T C Phi_f` against
`c_L G_(2,ell)` on the flux block alone, not the cross block).

### 4.4 The verdict

```text
FACTORIZES = UNDETERMINED — with the decided dichotomy inside: on the derived form
  the question is BINARY and EXHAUSTIVE (degenerate one-sector factorization XOR an
  irreducible rank-one cross term n^2 mu phi_f ⊗ phi_H that no sector-respecting
  basis removes; a rank-one form admits no nontrivial direct-sum split). THE EXACT
  MISSING PIECE: the sealed support/current-density realization of the per-cell
  write chains (RA27-3 fields, NOT SUPPLIED) — specifically whether the write
  direction has nonzero components in BOTH the flux block im(d_1^dagger) and the
  dim-32 flux-free physical block H — together with, at post-limit strength, the
  Gate-5 kernel C on the cross pairing.
```

---

## 5. THE WINDING DETERMINATION

### 5.1 The mechanism question, answered on the derived form [PROVABLE]

On the derived form the winding enters EXACTLY ONCE: as the global prefactor `n^2`
of the n-blind form `Phi^T C Phi` (D2: uniform one-character-power write; the base
generator, the write operator directions, the base state, and hence `C` are n-blind
because `chi_n(identity) = 1` for every n; `Phi` carries no n). Consequences, each
exact:

```text
(1) SCALING BLINDNESS. Every structural predicate available of record on
    R_record,L alone — positive-semidefiniteness, rank, kernel, carrier,
    cluster-summability, additivity, refinement-naturality of the response map,
    commutation with pullback to a common refinement, and the presence or absence
    of the cross-sector term — is invariant under the global positive scaling
    n^2 -> n'^2. If any such predicate holds for one nonzero n it holds for all.
(2) THE FREEDOM IS n-BLIND. The dim-32 assignment freedom acts on base data
    (assignments, chains, kernels) that carry no n; quantifying any admissibility
    condition over the freedom cannot introduce n-sensitivity.
(3) THE CROSS TERM CANNOT SELECT n. The cross-sector term, when present, is
    n^2 mu phi_f ⊗ phi_H — its existence, its irreducibility, and its kernel are
    properties of (mu, phi_f, phi_H, C), all n-blind; n multiplies the whole.
(4) SIGN NEVER VISIBLE. R_record,L(n) = R_record,L(-n) exactly: the quadratic
    response can at most ever reach |n|, never the sign.
(5) n = 0 REPRODUCED. At n = 0 the derived form vanishes identically — the
    zero-variation elimination of the trivial character (B6) is re-derived by the
    form itself, independently of the carried result. The derived form admits
    every n != 0 at identical structure.
```

### 5.2 What forcing would require, and where it is sealed shut today [PROVABLE]

Forcing `|n|` through this object requires an EQUATION between the n^2-scaled
channel and an n-blind comparand — a second determination that does not scale as
n^2 (the quadratic-against-linear crossing named in T1 §6.1). The derived form
exhibits the n^2 side exactly; the record's two candidate comparands are both
unavailable of record: the energy form `c_L <.,.>_(2,ell)` is gated by the
undecided materiality fork with both settlement paths blocked on SPEC GAPS (T2
§3.3–3.4), and the conversion of `kappa_record` to a physical action coefficient is
sealed UNAUTHORIZED until `Z_Q` is constructed from complete `Q_spec` (B9). So the
forcing, if it exists, lives in an unbuilt external crossing, not in the
cross-sector term.

### 5.3 The verdict

```text
WINDING_FORCED = NOT-FORCED, on EITHER branch of the factorization fork: the
  derived form fixes only the ray { n^2 · Phi^T C Phi : n in Z, n != 0 }; the
  winding is a global quadratic prefactor of an n-blind form, every record-internal
  structural predicate is ray-invariant, the dim-32 freedom moves only n-blind
  data, and the sign is invisible in principle. The one channel that could reach
  |n| is an equation against an n-blind comparand, and both sealed candidates are
  respectively fork-blocked and sealed-unauthorized. This verdict NEEDS no
  faithfulness, consumes no scale, and — stated for the panel — is the
  anti-overclaim reading: a FORCED verdict here would have required exhibiting an
  n-blind channel INSIDE R_record,L, and D2 proves there is none on the ratified
  content.
```

---

## 6. WHAT THIS DOES NOT SAY, AND THE OVERCLAIM AUDIT

```text
IT DOES NOT say R_record,L is fully built: the kernel C is the Gate-5 obligation,
   the scalarization is TYPE-R/TYPE-U unbuilt, and the write chains are unsealed —
   the form closes to n^2 Phi^T C Phi and no further.
IT DOES NOT decide the materiality fork (FACTORS-through-the-energy), which is a
   statement about the flux-block component Phi_f^T C Phi_f, logically independent
   of the sector-split question decided here as a dichotomy.
IT DOES NOT count the faithfulness-fed |n| = 1 result — BARRED and not consumed;
   NOT-FORCED here is the opposite-direction finding and does not contradict it
   (that result runs on a premise this construction is forbidden).
IT DOES NOT import GR, a metric, a KK ansatz, or any quasilocal object: the
   derivation runs entirely on the ratified connection-only object (T3's
   import-block at g is honored — no g is supplied anywhere).
IT DOES NOT use any scale: tau_R and the counting metric appear only inside quoted
   sealed definitions; no length, radius, or c^2 is consumed; 32, 24, the matrix
   entries, and all displayed integers are sealed-text quotations or exact
   structural integers.
IT DOES NOT evaluate mu(w), C, any spectrum, or any response value; mu's one used
   property (≡ 0 iff w_0 w_ch = 0) is structural.
IT DOES NOT interpret: no physical reading of the factorization, the interference
   structure, or the two-branch necessity is stated or used.
YOURS assemblies, marked: the three-block split display and im(Q_flux) =
   im(d_1^dagger) (D1, one line from sealed d_1 d_0 = 0); the operator-level
   factor-out of n (D2, from the sealed character law); the two-branch necessity
   reading of the sealed zero entries (D3); the scalarized Hessian mechanics (D4,
   conditional and flagged); the cluster-summability tension between the coherent
   finite-N form and B2's sealed locality requirement (D5); the rank-one dichotomy
   and congruence argument (4.2); the ray/scaling argument (5.1). Each runs on
   quoted sealed identities; none consumes an unsealed value.
FLATTENING CHECK: "the form derives" is not flattened into "the object is built";
   UNDETERMINED (factorization) is not flattened into NO by the attractiveness of
   the interference cross term — the deciding support datum is named instead;
   NOT-FORCED is not flattened into "n is meaningless" — n = 0 stays excluded and
   the external crossing stays genuinely open; the finite-N anchor's conditionality
   on scalarization is stated wherever used.
```

---

## 7. FINAL FLAG BLOCK

```text
R_FORM_DERIVED = CLOSES TO A THREE-FACTOR PULLBACK, by named surface objects:
  R_record,L(a,b) = n^2 · sum_(j,k) C_jk <ell_j,a><ell_k,b>  on difference-branch
  connection tangents — with, DERIVED: the three-block tangent split A^1 = im(d_0)
  ⊕ H ⊕ im(d_1^dagger) and im(Q_flux) = im(d_1^dagger) (from sealed d_1 d_0 = 0);
  the write differentials Phi as the ONLY connection dependence of the ratified
  object (exactly one character power per cell); the uniform factor-out of the
  winding (V_L proportional to n; base objects n-blind since chi_n(identity) = 1);
  the two-branch necessity (single-branch charged amplitude identically zero;
  branch diagonal flat; the channel carried by a_Delta); the finite-N scalarized
  anchor Hess = n^2 mu Phi_tot ⊗ Phi_tot, rank one, an interference term that
  vanishes iff either source-sector weight vanishes; and the post-limit typing of C
  (symmetric, PSD, intensive, additive, cluster-summable, n-independent) forced by
  the sealed Duhamel-reproduction and locality clauses. EXACT MISSING PIECES, named:
  (G1) the Gate-5 connected linked-cluster kernel C — the coherent finite-N kernel
  is all-pairs and NOT cluster-summable, so the sealed intensivity requirement
  makes the limit's clustering the one unbuilt load-bearing object; (G2) the
  scalarization/state-port pair (sealed TYPE-R/TYPE-U); (G3) the write chains'
  support/current-density realization (RA27-3 fields NOT SUPPLIED).

FACTORIZES = UNDETERMINED(the sealed support/current-density realization of the
  per-cell write chains — whether the write direction has nonzero components in
  BOTH im(d_1^dagger) and the dim-32 flux-free physical block H — plus, at
  post-limit strength, the Gate-5 kernel C on the cross pairing. DECIDED INSIDE
  THE UNDETERMINED: on the derived form the question is a BINARY dichotomy —
  either a degenerate one-sector factorization (R = R_sector ⊕ 0, the other
  sector's response identically zero) or an irreducible rank-one cross term
  n^2 mu phi_f ⊗ phi_H that no sector-respecting basis removes (block-diagonal
  congruence preserves a nonzero cross block; a rank-one form is never a direct
  sum of two nonzero forms). No third case exists.)

WINDING_FORCED = NOT-FORCED(on either factorization branch: n enters the derived
  form exactly once, as the global quadratic prefactor n^2 of an n-blind form —
  the write is uniformly one character power, and the base generator, write
  operators, base state, kernel C, and chains Phi all carry no n because every
  character is trivial at the identity. Every record-internal structural predicate
  — PSD, rank, carrier, naturality over the dim-32 freedom, cross-term presence —
  is invariant under the global scaling, so it holds for all nonzero n if for any;
  the freedom moves only n-blind data; the sign is invisible in principle
  (R(n) = R(-n)); and n = 0 is re-derived as the identically-zero response,
  matching the sealed elimination. Forcing |n| requires equating the n^2 channel
  to an n-blind external comparand, and both sealed candidates are unavailable:
  the energy-form comparand is gated by the undecided materiality fork with both
  settlement paths SPEC-GAPPED, and the kappa_record-to-physical conversion is
  sealed UNAUTHORIZED until Z_Q from complete Q_spec.)

GR_IMPORTED = no. No metric, KK ansatz, Einstein-Hilbert, or quasilocal object is
  consumed; the derivation runs on the ratified connection-only object and T3's
  one-argument import-block at g is left standing.

FAITHFULNESS_ASSUMED = no. The faithfulness-fed |n| = 1 result is BARRED and not
  consumed; NOT-FORCED is derived without any injectivity premise.

SCALE_USED = no. No ell_P, fiber radius, metric length, K_KK, or c^2; tau_R and
  all dimensionful-looking strings appear only inside quoted sealed definitions;
  every displayed integer is a sealed-text quotation or exact structural integer.

INTERPRETIVE_INPUT_USED = no. No physical reading of factorization, interference,
  or branch structure is assumed or asserted; verdicts are structural only.

IMPLICATION = STRUCTURAL(the magnitude-sensitive channel is a RAY, not a point:
  the record's own content fixes R_record,L up to the n-blind factor Phi^T C Phi,
  and no record-internal predicate can select n from it. The factorization fork
  compresses to ONE unsealed datum — the write chains' block decomposition (G3) —
  and the form's full closure to ONE unbuilt object — the Gate-5 cluster kernel
  (G1) with the scalarization port (G2) as its consumer. Any winding
  determination through this object must come from an external n-blind comparand;
  the two sealed candidates are fork-blocked and sealed-unauthorized
  respectively, and building either is upstream of any n-forcing claim.)

FENCE_RESPECTED = CERTIFIED. Typing/form only; no value of R_record,L, n, kappa,
  alpha, any scale, coefficient, spectrum, or constant computed, bounded,
  estimated, or compared; mu(w) and C left symbolic; no register/tracker/plan/
  road/ledger/lens read; seals verified at path before reading (the V011 packet
  member aa7c6d49, not the same-named top-level copy); the forbidden imports held
  and audited; no git action; output name probed ABSENT before write.

alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel check.
```
