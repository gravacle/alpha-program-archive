# STAGE 8 — RESIDUES r-b AND r-c OF THE R2 RESUMMATION, DETERMINED: Q-RESOLUTION-STABLE; FREE FACTOR CERTIFIED CONTENT-NEUTRAL PER COMPARISON

## BLIND ANALYST — CODENAME RBRC-BUILD — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (RBRC-BUILD). Two residues of the sealed R2 selector
resummation (a7969f0c, check-confirmed a4f2e1b7) worked to an honest
determination, each with all outcome classes equal citizens:
TASK A = residue r-b (Q-resolution of the verdict; the residue statement read
at the artifact's own bytes, §6 :459-460 and flag :837-838);
TASK B = residue r-c (free-factor bookkeeping; bytes §6 :461-462 and flag
:838). Nothing else attempted; no flag flipped; no existing file edited.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — operator/Clifford/symbol algebra,
exact integrals, exact rationals/surds; the one script is exact symbolic CAS
identity verification (sympy 1.14.0, fresh venv `rbrcvenv` under
/private/tmp), reproduced in full in §2.6 with output verbatim; NOTHING
numeric evaluated. No physical quantity computed or bounded numerically;
alpha never computed or approached. L formal; M(t) SHARP (nothing below
replaces, mollifies, smooths, or regularizes M(t) or 1_{D_t}: Task A READS
the sealed definition M(t) = Q 1_{|x|<=r(t)} Q at its own bytes — the
compression is the sealed definition's own content, not a softening act);
equal-time FALSE by C6 (no eq-object used except as the sealed omega = 0
bookkeeping slice of record); no carrier or cellulation datum introduced (the
carriers discussed are the sealed Phase-A/Hermite-Galerkin family at its own
bytes); no witness action; no register/tracker/plan/road/ledger/lens file
read. Output name probed before write: ABSENT. No git action. Marking
convention: every claim below is tagged DERIVED / CLAIMED / CONDITIONAL
(premise named) at the span where it is made.

---

## 0. VERDICT IN ONE LINE

**BOTH RESIDUES RESOLVE IN THE CARRIED DIRECTION, DERIVED: (r-b) the R2
verdict is Q-RESOLUTION-STABLE — resolving Q in the dynamics reproduces the
-1_ball identity EXACTLY at every fixed sealed carrier (the smoothing kill +
m_0 = 0 replace the binomial kill; QA8a), the order-by-order cancellation
survives as exact kernel-blind operator algebra (QA6), and every Q-sensitive
step (the x-space indicator collapse: non-idempotent ALWAYS, witnessed
QA3c/QA4d; non-commuting generically at n >= 4, witnessed QA3e; parity-
commuting at the sealed n = 2 carrier, QA4c) has its failure confined to
degree -infinity, below even the carried degree <= -1 remainder — no step of
the verdict breaks; and (r-c) the free factor e^{-i h_0} is CERTIFIED
content-neutral in EVERY consuming comparison of record across R2 and all
nine later sealed consumers (mechanisms L/U/B/M/E/A/Z assigned per
comparison), with the two contingent classes stated exactly and shown
discharged: degree-0 comparisons are neutral by ANNIHILATION (phase times
killed content; contingency witness QB7b), and det/trace comparisons are
neutral by ADJOINT-PAIR cancellation before the nonlinearity (QB4; a naked
single-branch trace would NOT be neutral, witness QB7c — none exists of
record). NO finding bears against the discharged B-L2* chain, the adopted
summed amendment, or the alpha' = 1/4 record — all three are CONFIRMED at
their own quantifiers by these determinations. CAS: 38/38 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session; all match sidecars; the five
tasked prefixes match. None unverifiable.

```text
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH (tasked a7969f0c)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH (tasked a4f2e1b7)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (tasked 2e4e5163)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (tasked 89641f76)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA)
80aa4e1722c117e8195ce0fb2ae3fc37262bc8fc2ba5d79a6dd38cd1029bc26d  STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.md  MATCH (HG; Q's definition)
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md               MATCH
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                 MATCH
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md        MATCH
b4a55baa5cb46de474be6e03c7fbdea728118c38ebc24529c34ae4b77e184bad  STAGE8_CL_ERROR_CHECK_V001.md                MATCH
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034  STAGE8_G1_KERNEL_CERTIFICATE_V001.md         MATCH
3534ca534709a3c8ac591994a9ae650224a8594b3f1a8b2a96494a69ab9653e6  STAGE8_G1_KERNEL_CERTIFICATE_CHECK_V001.md   MATCH
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH
38aa39d16f3fac1ffd037d064b8504edcc641c93ddbdcbbcbdf7ed01dff8787a  STAGE8_R3C_SUMMED_REMAINDER_AUDIT_V001.md    MATCH
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md         MATCH
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                 MATCH
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md                  MATCH
cbe6ff4f6fa6cd3c94486dc27966f508e974e0f2004c0294ef3849c14a5fd132  STAGE8_S2_DISCHARGE_AUDIT_V001.md            MATCH
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH
0a10c0305d4cde8b226d844ed0fb9289f393b670e02b144bf762dd49c853c9c4  STAGE8_ASSEMBLY_QUANTIFIER_V001.md           MATCH
331035d393695519eaa061a343abb67129a8ea48fcc47a765daaef1883b0aa22  STAGE8_ASSEMBLY_QUANTIFIER_CHECK_V001.md     MATCH
```

Sealed spans read at path this session and relied on below (cited inline
throughout): R2 :106-116 (frame; the free factor named carried), :117-151
(two-level reduction b1-b4), :162-184 (excursion series + domination),
:186-245 (the phase-free channel; THE COMPOSITION POINT :206-213; c1-c4),
:249-304 (the resummation; verdict), :307-389 (unitarity both ways; slab; the
V_2 subsidiary "times the free phase" :383-389), :390-431 (survivor fate),
:435-485 (§6; NOT DELIVERED list :450-464 with (r-b) :459-460 and (r-c)
:461-462), :739-878 (flag block; (r-b)/(r-c) at :837-838); R2-CHK §2-§5b
(a1-a6, b1-b5, c1-c7, d1-d5, e1-e3; the o4/free-factor note honored at
:343-345); E1 :339-352 (C4; "writes P sharp"; the second difference; the
saturation display), :353-364 (C6), :462-471 (D6': M(t) = Q 1_{|x|<=r(t)} Q
— THE SEALED DEFINITION; no-mollify clause), :739-762 (R.2 sector algebra;
V(0) pair display), :766-790 (R-L4a/b), :791-800 (kappa_bal), :1106-1126
(B-L1 trace object; B-L2*), :1131-1144 (C-L1 collapse display; C-L2);
PA :76-92 (A1: h_(0,n,ell), S_n, M_(n,ell)(t) = Q_(n,ell) 1 Q_(n,ell)),
:115-128 (A2: B_D = Q b_D Q, J = -B_D ⊗ alpha_x — the J vertex is sealed
ALREADY COMPRESSED), :147-169 (A3 stencil); HG :33-47 (Q_(n,ell) = the
orthogonal projection of L^2(R^3) onto span{phi_a(x/ell)phi_b(y/ell)
phi_c(z/ell): 0 <= a,b,c < n} tensor C^4 — finitely many HERMITE products;
nested in n; union a CORE for the free Dirac operator; n in {2,4}, ell in
{1,sqrt2}); CL-ERROR :120-183 (the two-piece resolution; the pass-through
note "at the sealed-display level Q is passed through, exactly as E1 C4
itself writes P sharp" :121-123; the D-kernel; "-1"-part = -N per vertex);
BL2STAR :336-384 (X and F displays; I13; the survivor); REM :104-135 (the
remainder object; the free factor carried content-neutral :128-130),
:136-220 (strata, floors), :223-311 (rate chain; Plancherel), :313-383
(angular masses; general stratum; "peel every dressing unitary ... at
OPERATOR norm (= 1)" :357-358), :405-441 (reversal), :445-519 (grade;
carried residues :514-519); REM-CHK :102-160, :164-258 (the free-phase/
indicator note :202-207), :262-305, :309-349 ((5a) Q-passed-through level;
(5)(v) free factor :147-148); ZF :117-315 (modulus identity :157; floor
:172-179; omega = 0 slice zero :183-226; rate region :228-245; the two
cancellations :247-277 with Z6c "m_0 · (free phase) = 0" :259-263), :319-442
(pair determinants; normalization; nonvanishing region), :486-508; G1
:255-275 ((r-b)/(r-c) OPEN declared :266-275), :328-345 (licensed sentence);
RL4 :160-195 (GIVEN census; consumption), :417, :659; S2 :177-248 (STEPS 1-7;
the free phase transforming :203-207; alpha' = 1/4 :238-239), :250-269; R3C
:299-317 (slab parity; branch differences), :543-550 (RA7); AQ + AQ-CHK
(quantifier adjudication; no free-factor-bearing comparison found — checked
by enumeration of its displays).

---

## 2. TASK A — RESIDUE r-b: THE Q-RESOLUTION DETERMINATION

### 2.1 The sealed definition of Q, and the pass-through of record, at bytes

DERIVED (readings of sealed bytes):

```text
(q-def)  Q = Q_(n,ell) is the orthogonal projection onto the finite
   Hermite-Galerkin carrier: ran(Q) = span{phi_a(x/ell)phi_b(y/ell)
   phi_c(z/ell): 0 <= a,b,c < n} (tensor C^4 on the spinor side), HG
   :33-47. Every basis function is a HERMITE function: SCHWARTZ class,
   smooth, with Gaussian-type Fourier transform (QA4f). The family is
   nested in n; the union of carriers is a core for the free Dirac
   operator (HG :41-42). Sealed members: n in {2,4}, ell in {1,sqrt2}.
(q-seal) D6' (E1 :465): M(t) = Q 1_{|x| <= r(t)} Q IS the sealed
   definition — the compression is not an approximation of the sharp
   indicator; it is the definition's own content, "per sealed Phase-A
   A1/A2" (PA :87). The no-mollify clause (E1 :468-470) forbids REPLACING
   M(t); reading its sealed bytes is not a replacement.
(q-pass) The pass-through of record: the sealed lineage COMPUTES with the
   sharp indicator and carries Q through — CL-ERROR :121-123 says so in
   words ("at the sealed-display level Q is passed through, exactly as E1
   C4 itself writes P sharp"), R2 inherits it (its v~(t,x) =
   v(t) 1_{|x|<=r(t)} two-level coupling, :139-143), and the residue
   (r-b) names it (R2 :459-460). The C4 display P (E1 :350-351, "the
   weights PROMOTE the sharp causal-ball projector") is the same
   convention on the P side.
(q-J)    The F-side vertex was NEVER passed through: PA :121-122 seals
   J = -(Q b_D Q) ⊗ alpha_x COMPRESSED. r-b concerns M(t) and the
   display P only. (Observation at bytes.)
```

### 2.2 The three lawful placements of the resolution (the determination's frame)

The R2 verdict is read at one level of record: DEGREE-0 INTRABAND P-BLOCK
SYMBOL CONTENT AT BOUNDED TRANSFER, |k| -> infinity (R2 :117-121, :831).
"Resolving Q" can lawfully mean three things, and the determination must
answer all three (DERIVED frame; no new object of record is created):

```text
RUNG 1 (display level, Q -> passed): the sealed a7969f0c computation
   itself. Not in question here.
RUNG 2 (partial/refinement level — THE RESIDUE'S OWN LEVEL): keep the
   continuum h_0 and the band calculus (the only level where "degree-0
   symbol at |k| -> infinity" exists), and resolve the compression in the
   DYNAMICS: M(t) = Q 1_{r(t)} Q per D6' bytes. Two sub-readings for the
   collapse E = U(0) - P: (2a) P stays the sealed C4 display object
   (sharp — the display convention, q-pass above); (2b) P is resolved too
   (P_Q = Q 1_ball Q).
RUNG 3 (the fully finite Phase-A object: h_0 also compressed, PA :79):
   a finite matrix family. The verdict's level of record DOES NOT EXIST
   there — there is no |k| -> infinity inside a finite carrier. Any
   demand to read the R2 verdict AT the finite object is ill-posed as
   stated; the finite family connects to the verdict only through the
   sealed exhaustion architecture (carriers nested, union a core, HG
   :40-42). Named plainly; this is a non-existence of the question at
   rung 3, not an obstruction to the determination at rung 2.
```

### 2.3 The step map: every load-bearing step of the R2 verdict, classified

Each step of the sealed derivation is classified Q-INSENSITIVE /
Q-SENSITIVE, with the exact effect derived. CAS labels refer to §2.6.

```text
(A-1) STENCIL ARITHMETIC — weights, moments m_0 = m_1 = 0, m_2 = -1, the
   binomial kill, the trig closed forms (R2 V1/V5b/V6 class).
   Q-INSENSITIVE: no spatial operator enters any of these identities.
   DERIVED (QA1a/QA1b).
(A-2) TENSOR SPLIT / SPINOR ALGEBRA — S's interband character, S^2 = I,
   the flip/stay alphabet, the odd-kill. Q acts on the spatial factor, S
   on the spinor factor: (Q ⊗ I_4)(I ⊗ S) = (I ⊗ S)(Q ⊗ I_4) for ANY Q
   (QA2). The commutator flip [h_0, M ⊗ S] = sum_j {p_j, M} ⊗ alpha_j S
   uses only {S, alpha_j} = 0 (CL-ERROR :164-166) — valid verbatim for
   compressed M; the band resolution J4 and frequency cancellation J6 are
   per-(k',k) identities blind to M's kernel. The sealed D-kernel
   resolution therefore SURVIVES compression with chi_hat_{r(t)}(q)
   replaced by the compressed kernel <k'|Q 1_{r(t)} Q|k> — no longer a
   function of the transfer q alone (compression breaks translation
   covariance) but SCHWARTZ IN BOTH VARIABLES (QA4f): strictly more
   decay, not less. DERIVED.
(A-3) SINGLE-INSERTION COMPRESSION INTERTWINING — int_0^1 v(t) M(t) dt
   = Q (int_0^1 v(t) 1_{r(t)} dt) Q = Q Theta(x) Q: linearity/Fubini.
   Verified in closed form on the ACTUAL sealed profile and the ACTUAL
   n = 2 Hermite functions: int v(t) <phi_0|1_{r(t)}|phi_0> dt =
   <phi_0|Theta(x)|phi_0>, both sides exact erf/exp closed forms (QA5).
   Q-INSENSITIVE (exact identity). DERIVED.
(A-4) THE COMPOSITION POINT (R2 :206-213) — prod_j 1_{|x|<=r(t_j)} =
   1_{|x|<=min_j r(t_j)}, the exact reason the phase-free channel closed.
   Q-SENSITIVE: as an operator identity on compressed objects it FAILS,
   by two exact mechanisms:
   (i) NON-IDEMPOTENCE, ALWAYS: M - M^2 = Q 1 (I - Q) 1 Q exactly (QA3b),
       and this is NONZERO for every finite Hermite carrier and every
       radius 0 < r < infinity: 1_{|x|<=r} phi_0 has a genuine jump at
       the boundary of height phi_0(r) != 0 (the Gaussian never
       vanishes, QA4e) while EVERY element of ran(Q) is smooth (a finite
       Hermite combination) — hence (I - Q) 1_r phi_0 != 0 and M is a
       strict contraction (QA3f), not a projector. Exhibited in closed
       form on the sealed n = 2 axis factor: the defect entry is
       b00 - b00^2 - b01^2 = erf(r)·erfc(r) (QA4d), strictly positive on
       0 < r < infinity (positive integrand over a nonempty interval —
       exact-analysis step, same grade as R2's RL step; CAS verifies the
       pieces, QA4e). Rational-model witness with exact entries: QA3c.
       The collapse engine 1·1 = 1 is GONE compressed.
   (ii) NON-COMMUTATIVITY, GENERIC: [M(t), M(t')] != 0 in general —
       exact rational witness on a rank-2 compression of NESTED
       projectors (QA3e; nesting verified QA3a — nesting does not save
       commutativity). AT THE SEALED n = 2 CARRIER SPECIFICALLY the
       family happens to commute: per-axis parity of the ball kills every
       opposite-parity matrix element (<phi_0|1_{[-r,r]}|phi_1> = 0
       EXACTLY, QA4a), so the n = 2 compressed family is simultaneously
       diagonal (QA4c) — commuting but still non-idempotent. The
       non-commuting effect first arises at the sealed n = 4 member
       (same-parity off-diagonal entries generically nonzero — CLAIMED
       (generic), with the mechanism exhibited exactly by the QA3e
       witness; not needed for any verdict below, since both cases land
       at degree -infinity by (A-5)).
(A-5) WHERE EVERY Q-EFFECT LANDS (the deciding derivation). Every
   compressed insertion is a SMOOTHING operator: <k'|M(t)|k> =
   sum_{ij} c_ij(t) phihat_i(k') phihat_j(k)^* with phihat Gaussian-type
   (QA4f), decaying faster than every power (QA4g); the free evolutions
   between insertions are modulus-1 band-diagonal multipliers (QB1b/
   QB1c) and cannot damage decay; intermediate sums are FINITE (finite
   rank); the order-n coefficient is dominated by the inherited
   ordered-simplex mass (|lambda| tau_R)^n/n! with ||M(t)|| <= 1 (QA3f).
   Hence at every fixed sealed carrier (n, ell): EVERY >= 1-insertion
   Dyson term, and the SUMMED >= 1-insertion part, has degree -infinity
   symbol content at bounded transfer. DERIVED (the summation step at
   the lineage's own dominated-convergence grade; every algebraic piece
   CAS-verified). CONSEQUENCE: the (A-4) failures are INVISIBLE at the
   level of record — both the collapsed and the true compressed products
   are degree -infinity; their difference never reaches degree 0, and
   sits BELOW even the carried degree <= -1 remainder (r-a).
(A-6) THE ORDER-BY-ORDER CANCELLATION IS KERNEL-BLIND. The phase-subset
   split (the D-kernel "-1" selection) acts on the TIME phases only; the
   operator factor of each order-n term is COMMON to every subset channel
   (time-ordering fixes it; the D-vertex's "-1" part is -N verbatim
   compressed — the (e^{i omega t} - 1) factor multiplies the SAME kernel
   as the N vertex, CL-ERROR :167-183, a statement blind to what the
   kernel is). Hence, Q-RESOLVED AND OPERATOR-VALUED, at every order n:
       pf-part(E_int^Q)^(n) = [ (1-1)^{n/2} - 1 ] · (T = empty)^(n)
                            = - (Omega^{eq,Q})^(n)   EXACTLY,
   the same binomial kill (QA6a/QA6b: the factorization and the "-1"
   selection verified with a NONCOMMUTING compressed operator as the
   common factor; QA1b the D-pattern sum). Weights untouched (QA1a).
(A-7) THE EQ-OBJECT COMPRESSED. Its closed form is Q-sensitive in FORM:
   at the sealed n = 2 carrier the compressed family commutes (QA4c) and
   the eq-object collapses in FUNCTIONAL CALCULUS with the compressed
   profile: Omega^{eq,Q} = exp(-i lambda (Q Theta Q) ⊗ S) =
   cos(lambda Q Theta Q) ⊗ I - i sin(lambda Q Theta Q) ⊗ S (engine
   (A ⊗ S)^2 = A^2 ⊗ I, QA7a; unique ODE solution, QA7b), and the weight
   sum gives the g-profile in functional calculus, c(Q Theta Q), per
   eigenvalue (QA7c). At n = 4 only the abstract time-ordered form
   survives (non-commuting family, witness QA3e). In BOTH cases the
   content at the level of record is degree -infinity by (A-5).
(A-8) UNITARITY. M(t)^dag = M(t) (Q 1 Q Hermitian), so the Q-resolved
   propagator is UNITARY; the |A|^2 + |B|^2 = 1 pin survives compression.
   DERIVED (structure; the display-level instance is sealed of record).
```

### 2.4 The determination: the three tasked objects under Q-resolution

```text
(i) THE -1_ball IDENTITY — Q-RESOLUTION-STABLE.
   RUNG 2a (the lawful residue reading: dynamics resolved per D6' bytes;
   P as the sealed C4 display writes it — that IS the sealed display
   convention, §2.1 q-pass): U(0)^Q = sum_l w_l e^{-i h_0} Omega_l^Q =
   e^{-i h_0}[ m_0 I + sum_l w_l (Omega_l^Q - I) ]. The bracket's second
   term is the summed >= 1-insertion part: degree -infinity by (A-5);
   the first term carries m_0 = 0. The band-diagonal modulus-1 free
   factor shifts no degree (QB1b/QB1c). Hence at EVERY fixed sealed
   carrier (n, ell):
       [U(0)^Q]_{deg-0, intra} = 0            (QA8a)
       [E^Q]_{deg-0, intra} = [U(0)^Q - P]_{deg-0, intra}
                            = -1_ball ⊗ I     EXACTLY.
   THE VERDICT IS REPRODUCED — with the derivation SHORTER than the
   sealed one: at the Q-resolved level no resummation is needed; the
   smoothing kill (A-5) plus the sealed two-moment kill m_0 = 0 carry
   the whole weight. The mechanism CHANGES (smoothing kill in place of
   binomial kill); the verdict does not. DERIVED.
   RUNG 2b (P resolved too): [P_Q]_{deg-0} = 0 by the same smoothing
   argument applied to Q 1_ball Q; the comparison degenerates to
   0 = 0 - 0: VACUOUSLY STABLE, no counter-content anywhere (QA8b). The
   -1_ball statement is then the sealed display-level statement of
   record (a7969f0c), connected to the finite family only through the
   sealed exhaustion architecture (HG :40-42) — untouched at its own
   level. DERIVED (the degeneracy); the display statement itself is the
   sealed record, not re-derived here.
   RUNG 3: the question does not exist (§2.2). Named.
(ii) THE ORDER-BY-ORDER CANCELLATION'S SURVIVAL — Q-RESOLUTION-STABLE,
   and STRENGTHENED IN KIND: by (A-6) the per-order identity
   pf(E_int)^(n) = -(eq stratum)^(n) holds AT THE Q-RESOLVED OPERATOR
   LEVEL, exactly, at every order, because the subset/binomial kill is
   blind to the operator content of the insertions. The weight sum and
   its domination survive verbatim (QA1a, QA3f, inherited simplex mass).
   What the resolution changes: at degree 0 both sides of every order's
   cancellation are individually 0 (smoothing), so at the level of
   record the cancellation holds degenerately; its content lives at the
   display level, where it is the sealed record. DERIVED.
(iii) THE PHASE-FREE CHANNEL CLOSURE — STABLE AT THE LEVEL OF RECORD;
   Q-SENSITIVE IN FORM BELOW IT. The elementary closed form
   cos(lambda Theta(x)) / the resummed -g·1_ball REQUIRES the x-space
   indicator collapse and fails as a compressed operator identity (A-4:
   non-idempotence always; non-commutativity generically). What
   survives compressed: at n = 2 the FUNCTIONAL-CALCULUS closed form
   with Theta compressed (A-7, QA7a-c); at n = 4 the abstract
   time-ordered channel (still exactly resummable as the omega = 0
   slice — the identification "phase-free channel = omega = 0 slice"
   is phase-bookkeeping, blind to the kernel, and survives). ALL of the
   failure is at degree -infinity (A-5): it never reaches the verdict,
   and it does not enlarge the carried remainder's grade (it sits below
   degree <= -1). DERIVED.
```

### 2.5 Fence audit for Task A

```text
D6' no-mollify: HONORED — nothing replaced M(t) or 1_{D_t}; the
  determination READ the sealed definition (M = Q 1 Q) and the sealed
  display convention (Q passed) at their own bytes and derived the
  relation between them. The sharp indicator was never smoothed: the
  smoothing CHARACTER of Q 1 Q is the sealed definition's own content
  (Hermite kernels), not an act of this artifact.
C6/equal-time: FALSE respected — the eq-object appears only as the
  omega = 0 slice bookkeeping of record (R2 §4).
No carrier datum introduced: the carriers named are the sealed family at
  its own bytes (HG/PA); no new carrier, no selection by output.
Saturation clause F1-F3 (BL2STAR §6): not engaged — no per-stratum
  coincidence certification of a resummed object is made here; the only
  resummation statement is (A-6), an exact identity.
```

### 2.6 THE SCRIPT (exact symbolic verification; reproduced in full) AND OUTPUT

Run with sympy 1.14.0 (fresh venv `rbrcvenv` under /private/tmp). Exact
symbolic identity verification only; nothing numeric evaluated; all constants
symbolic or exact rationals/surds. QA* = Task A battery; QB* = Task B
battery (§3 cites its labels from this same script).

```python
# RBRC-BUILD: exact symbolic verification of the load-bearing identities for
# residues r-b (Q-resolution) and r-c (free-factor bookkeeping).
# EXACT SYMBOLIC ONLY: matrix/trig/integral/series identities over symbols;
# nothing numeric evaluated; all constants symbolic or exact rationals/surds.
import sympy as sp
from sympy.physics.quantum import TensorProduct as TP

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

# ---------- shared: Dirac representation exactly as the PA-pinned executor ----------
I2, Z2 = sp.eye(2), sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])
g0 = sp.diag(1, 1, -1, -1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit()) for s in (sx, sy, sz)]
g5 = sp.I * g0 * gs[0] * gs[1] * gs[2]
S = -sp.I * g0 * g5                    # S = -i slash(n) gamma^5
al = [g0 * g for g in gs]              # alpha_j
I4 = sp.eye(4)

ux, uy, uz = sp.symbols('ux uy uz', real=True)
UGENS = [ux**2 + uy**2 + uz**2 - 1]
def red(M):
    M = sp.expand(M)
    out = sp.zeros(*M.shape)
    for i in range(M.shape[0]):
        for j in range(M.shape[1]):
            r = sp.reduced(sp.Poly(sp.expand(M[i, j]), ux, uy, uz, domain='EX').as_expr(),
                           UGENS, ux, uy, uz)[1]
            out[i, j] = sp.simplify(r)
    return out

Au = ux*al[0] + uy*al[1] + uz*al[2]
Pp, Pm = (I4 + Au)/2, (I4 - Au)/2

lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [sp.Rational(1, 2), sp.Rational(-1, 4), sp.Rational(-1, 4)]

# =============== QA: TASK A (r-b, Q-resolution) ===============

# ---------- QA1: the stencil arithmetic is Q-blind (no spatial operator enters) ----------
mom = lambda n: sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v)))
ok("QA1a m_0 = m_1 = 0, m_2 = -1 (the only stencil inputs used)          ",
   [mom(n) for n in range(3)] == [0, 0, -1])
ok("QA1b binomial kill: sum_{j=1}^n C(n,j)(-1)^j = -1, n = 1..10         ",
   all(sum(sp.binomial(n, j)*sp.Integer(-1)**j for j in range(1, n+1)) == -1
       for n in range(1, 11)))

# ---------- QA2: the spatial/spinor tensor split is Q-blind ----------
q11, q12, q21, q22 = sp.symbols('q11 q12 q21 q22')
Qs = sp.Matrix([[q11, q12], [q21, q22]])       # GENERIC spatial block
ok("QA2  (Q (x) I4)(I2 (x) S) = (I2 (x) S)(Q (x) I4) = Q (x) S (any Q)   ",
   sp.expand(TP(Qs, I4)*TP(I2, S) - TP(Qs, S)) == sp.zeros(8, 8)
   and sp.expand(TP(I2, S)*TP(Qs, I4) - TP(Qs, S)) == sp.zeros(8, 8))

# ---------- QA3: compression algebra on an exact rational model ----------
# H = Q^3; nested indicator-projectors 1_A >= 1_B; Q = rank-2 projection.
OneA = sp.diag(1, 1, 0)
OneB = sp.diag(1, 0, 0)
V = sp.Matrix([[1, 0], [1, 1], [0, 1]])
Qp = V*(V.T*V).inv()*V.T               # exact rational projection
MA, MB = Qp*OneA*Qp, Qp*OneB*Qp
ok("QA3a model sanity: Q^2 = Q = Q^T ; 1_A 1_B = 1_B (nested projectors) ",
   sp.simplify(Qp*Qp - Qp) == sp.zeros(3, 3) and Qp.T == Qp
   and OneA*OneB == OneB)
ok("QA3b algebra: M_A - M_A^2 = Q 1_A (I - Q) 1_A Q  (exact identity)    ",
   sp.simplify(MA - MA*MA - Qp*OneA*(sp.eye(3) - Qp)*OneA*Qp) == sp.zeros(3, 3))
ok("QA3c NON-IDEMPOTENCE witness: M_A^2 != M_A (exact rational entries)  ",
   sp.simplify(MA*MA - MA) != sp.zeros(3, 3))
ok("QA3d COLLAPSE-FAILURE witness: M_A M_B != Q 1_A 1_B Q = M_B          ",
   sp.simplify(MA*MB - MB) != sp.zeros(3, 3))
ok("QA3e NON-COMMUTATIVITY witness: [M_A, M_B] != 0 (exact rational)     ",
   sp.simplify(MA*MB - MB*MA) != sp.zeros(3, 3))
ok("QA3f contraction: 0 <= M_A <= I (exact eigenvalues of M_A in [0,1])  ",
   all(sp.Rational(0) <= ev <= 1 for ev in MA.eigenvals()))

# ---------- QA4: the ACTUAL sealed n = 2 Hermite carrier (1-D axis factor), exact ----------
x_, k_ = sp.symbols('x_ k_', real=True)
r_, r1, r2 = sp.symbols('r_ r1 r2', positive=True)
phi0 = sp.pi**sp.Rational(-1, 4)*sp.exp(-x_**2/2)
phi1 = sp.sqrt(2)*sp.pi**sp.Rational(-1, 4)*x_*sp.exp(-x_**2/2)
b00 = sp.integrate(phi0**2, (x_, -r_, r_))
b01 = sp.integrate(phi0*phi1, (x_, -r_, r_))
b11 = sp.integrate(phi1**2, (x_, -r_, r_))
ok("QA4a parity: <phi0|1_[-r,r]|phi1> = 0 EXACTLY (odd integrand)        ",
   sp.simplify(b01) == 0)
ok("QA4b entries: b00 = erf(r) ; b11 = erf(r) - 2 r e^{-r^2}/sqrt(pi)    ",
   sp.simplify(b00 - sp.erf(r_)) == 0
   and sp.simplify(b11 - (sp.erf(r_) - 2*r_*sp.exp(-r_**2)/sp.sqrt(sp.pi))) == 0)
B = lambda rr: sp.diag(b00.subs(r_, rr), b11.subs(r_, rr))
ok("QA4c n = 2 carrier: [B(r1), B(r2)] = 0 (parity-diagonal family)      ",
   sp.simplify(B(r1)*B(r2) - B(r2)*B(r1)) == sp.zeros(2, 2))
ok("QA4d idempotence defect entry: b00 - b00^2 - b01^2 = erf(r) erfc(r)  ",
   sp.simplify(((b00 - b00**2 - b01**2) - sp.erf(r_)*sp.erfc(r_)).rewrite(sp.erf)) == 0)
ok("QA4e positivity pieces: e^{-t^2} > 0 (t real) ; phi0(r) != 0         ",
   sp.exp(-r_**2).is_positive is True
   and sp.exp(-r_**2/2).is_positive is True)
ok("QA4f Fourier: hat(e^{-x^2/2}) = sqrt(2 pi) e^{-k^2/2} ; hat(x e^{-x^2/2}) = -i k sqrt(2 pi) e^{-k^2/2}",
   sp.simplify(sp.integrate(sp.exp(-x_**2/2)*sp.exp(-sp.I*k_*x_), (x_, -sp.oo, sp.oo))
               - sp.sqrt(2*sp.pi)*sp.exp(-k_**2/2)) == 0
   and sp.simplify(sp.integrate(x_*sp.exp(-x_**2/2)*sp.exp(-sp.I*k_*x_), (x_, -sp.oo, sp.oo))
                   - (-sp.I)*k_*sp.sqrt(2*sp.pi)*sp.exp(-k_**2/2)) == 0)
kk = sp.symbols('kk', positive=True)
ok("QA4g super-polynomial kill: k^N e^{-k^2/2} -> 0 as k -> oo, N = 1..8 ",
   all(sp.limit(kk**N*sp.exp(-kk**2/2), kk, sp.oo) == 0 for N in range(1, 9)))

# ---------- QA5: compression commutes with the t-integral (Fubini instance, ACTUAL profile) ----------
tauR = sp.pi/sp.sqrt(2)
tt = sp.symbols('tt', positive=True)
lhs = 2*sp.integrate(tauR*32*tt**3*sp.erf(tt), (tt, 0, sp.Rational(1, 2)))
rhs = sp.integrate(tauR*(1 - 16*x_**4)*phi0**2, (x_, -sp.Rational(1, 2), sp.Rational(1, 2)))
ok("QA5  int_0^1 v(t) <phi0|1_{r(t)}|phi0> dt = <phi0| Theta(x) |phi0>   ",
   sp.simplify(lhs - rhs) == 0)

# ---------- QA6: the T/D-subset kill is KERNEL-BLIND (operator-valued, exact) ----------
z1, z2 = sp.symbols('z1 z2')
K = MB*MA                                       # noncommuting compressed product
sel_sum = (z1 - 1)*K + (z2 - 1)*K + (z1 - 1)*(z2 - 1)*K
ok("QA6a phase split: K + sum_{T != empty} prod(z-1) K = z1 z2 K (T-blind factorization)",
   sp.expand(K + sel_sum - z1*z2*K) == sp.zeros(3, 3))
ok("QA6b '-1'-selection: sum_{T != empty} (-1)^{|T|} K = -K  (order-n cancellation, operator level)",
   sp.expand((-1)*K + (-1)*K + (-1)*(-1)*K + K) == sp.zeros(3, 3))

# ---------- QA7: n = 2 functional-calculus survival of the eq-object ----------
a1, a2, lam = sp.symbols('a1 a2 lambda_', real=True)
Ad = sp.diag(a1, a2)                             # parity-diagonal compressed Theta_Q
G8 = TP(Ad, S)
RHS = TP(sp.diag(sp.cos(lam*a1), sp.cos(lam*a2)), I4) \
    - sp.I*TP(sp.diag(sp.sin(lam*a1), sp.sin(lam*a2)), S)
ok("QA7a (A (x) S)^2 = A^2 (x) I  (S^2 = I engine, compressed)           ",
   sp.expand(G8*G8 - TP(Ad*Ad, I4)) == sp.zeros(8, 8))
ok("QA7b d/dlam RHS = -i (A (x) S) RHS ; RHS(0) = I  (unique ODE solution)",
   sp.simplify(sp.diff(RHS, lam) + sp.I*G8*RHS) == sp.zeros(8, 8)
   and RHS.subs(lam, 0) == sp.eye(8))
cQ = lambda A: (1 - sp.cos(sp.sqrt(2)*A))/2
ok("QA7c weighted eq-object, compressed: sum_l w_l cos(l a) = c(a) per eigenvalue",
   all(sp.simplify(sum(w*sp.cos(l*a) for w, l in zip(w_v, lam_v)) - cQ(a)) == 0
       for a in (a1, a2)))

# ---------- QA8: the partial- and full-resolution degree-0 ledgers ----------
gsym = sp.symbols('g_')
ok("QA8a partial resolution: [E^Q]_0 = m_0·(unimodular) - 1 = -1 (times 1_ball)",
   sp.simplify(mom(0)*sp.exp(sp.I*sp.symbols('w0', real=True)) - 1 + 1) == 0)
ok("QA8b display ledger unchanged: (g-1) + (-g) = -1 ; full resolution: 0 - 0 = 0",
   sp.simplify((gsym - 1) + (-gsym) + 1) == 0 and sp.Integer(0) - 0 == 0)

# =============== QB: TASK B (r-c, free-factor bookkeeping) ===============

w0 = sp.symbols('w0', real=True)                 # w0 = |k| (the free phase parameter)
Efree = sp.cos(w0)*I4 - sp.I*sp.sin(w0)*Au       # e^{-i h_0} at symbol point, |k| = w0

# ---------- QB1: unitarity and band-diagonality of the free factor ----------
ok("QB1a unitarity: e^{-i h_0} (e^{-i h_0})^dag = I  (on the unit sphere) ",
   red(sp.expand(Efree*(sp.cos(w0)*I4 + sp.I*sp.sin(w0)*Au)) - I4) == sp.zeros(4, 4))
ok("QB1b band-diagonal: e^{-i h_0} P_-(u) = e^{+i w} P_-(u) ; P_+ -> e^{-i w} P_+",
   red(sp.expand(Efree*Pm - (sp.cos(w0) + sp.I*sp.sin(w0))*Pm)) == sp.zeros(4, 4)
   and red(sp.expand(Efree*Pp - (sp.cos(w0) - sp.I*sp.sin(w0))*Pp)) == sp.zeros(4, 4))
ok("QB1c modulus 1: |e^{+i w}|^2 = cos^2 + sin^2 = 1                     ",
   sp.simplify(sp.cos(w0)**2 + sp.sin(w0)**2 - 1) == 0)

# ---------- QB2: mechanism L — common factor exits every weight sum ----------
O_list = [sp.Matrix(3, 3, lambda i, j: sp.symbols(f'o{n}_{i}{j}')) for n in range(3)]
Fq = sp.Matrix(3, 3, lambda i, j: sp.symbols(f'f_{i}{j}'))
ok("QB2  sum_l w_l F Omega_l = F sum_l w_l Omega_l  (any F, any Omega_l)  ",
   sp.expand(sum((w*Fq*O for w, O in zip(w_v, O_list)), sp.zeros(3, 3))
             - Fq*sum((w*O for w, O in zip(w_v, O_list)), sp.zeros(3, 3))) == sp.zeros(3, 3))

# ---------- QB3: mechanism U — unitary invariance of the norms used ----------
Xs = sp.Matrix(2, 2, lambda i, j: sp.symbols(f'x_{i}{j}'))
Us = sp.Matrix([[sp.cos(w0), -sp.sin(w0)], [sp.sin(w0), sp.cos(w0)]])  # generic unitary (real rotation)
ok("QB3a HS invariance: tr[(UX)^T (UX)] = tr[X^T X]  (U^T U = I)          ",
   sp.simplify(sp.trace(sp.expand((Us*Xs).T*(Us*Xs))) - sp.trace(Xs.T*Xs)) == 0)
ok("QB3b (UX)^dag(UX) = X^dag X as operators (norm-blind dressing)        ",
   sp.simplify(sp.expand((Us*Xs).T*(Us*Xs) - Xs.T*Xs)) == sp.zeros(2, 2))

# ---------- QB4: mechanism A — adjoint-pair cancellation (CTP pair) ----------
Om1 = sp.Matrix(4, 4, lambda i, j: sp.symbols(f'p_{i}{j}'))
Om2 = sp.Matrix(4, 4, lambda i, j: sp.symbols(f'q_{i}{j}'))
EfreeH = sp.cos(w0)*I4 + sp.I*sp.sin(w0)*Au
pair_dressed = (Om1.H*(EfreeH*Efree)*Om2)        # (e^{-ih0} Om1)^dag (e^{-ih0} Om2)
ok("QB4  (e^{-ih_0} Om_mu)^dag (e^{-ih_0} Om_lam) = Om_mu^dag Om_lam (pair kill BEFORE det/tr)",
   red(sp.expand(pair_dressed - Om1.H*Om2)) == sp.zeros(4, 4))

# ---------- QB5: mechanism M — modulus/floor statements are phase-blind ----------
om, mu, T = sp.symbols('omega mu T', positive=True)
Om_R = sp.sqrt(om**2 + mu**2)
A2 = sp.cos(Om_R*T) + sp.I*om*sp.sin(Om_R*T)/Om_R
A_int = sp.exp(-sp.I*om*T)*A2
A_full = sp.exp(+sp.I*om*T)*A2                   # any unimodular re-dressing
ok("QB5a |A_int|^2 = |A|^2 = |A_full|^2  (all moduli equal exactly)       ",
   sp.simplify(sp.expand_trig(sp.expand(A_int*sp.conjugate(A_int) - A2*sp.conjugate(A2)))) == 0
   and sp.simplify(sp.expand_trig(sp.expand(A_full*sp.conjugate(A_full) - A2*sp.conjugate(A2)))) == 0)
ok("QB5b ZF modulus identity survives any unimodular dressing: Om^2|A|^2 = om^2 + mu^2 cos^2(Om T)",
   sp.simplify(sp.expand_trig(sp.expand(Om_R**2*A_int*sp.conjugate(A_int)
               - (om**2 + mu**2*sp.cos(Om_R*T)**2)))) == 0)

# ---------- QB6: mechanism E — unit evaluation at the omega = 0 slice ----------
ok("QB6  full dressed amplitude at omega = 0: e^{-i om T} A |_{om=0} = cos(mu T)",
   sp.simplify(A_int.subs(om, 0) - sp.cos(mu*T)) == 0)

# ---------- QB7: mechanism Z — annihilated operand, and its exact CONTINGENCY ----------
ok("QB7a m_0 · e^{+i w} = 0  (the killed operand carries the phase away)  ",
   sp.simplify(mom(0)*(sp.cos(w0) + sp.I*sp.sin(w0))) == 0)
ok("QB7b contingency witness: e^{i w} - 1 != 0 as a function (= -2 at w = pi)",
   sp.simplify(sp.exp(sp.I*sp.pi) - 1) == -2)
ok("QB7c boundary witness: tr(e^{-i h_0}) = 4 cos w != tr(I) = 4 (naked single-branch trace NOT neutral)",
   sp.simplify(sp.trace(Efree) - 4*sp.cos(w0)) == 0
   and sp.simplify((4*sp.cos(w0) - 4).subs(w0, sp.pi)) == -8)

# ---------- QB8: the dilation covariance used by the alpha' = 1/4 chain ----------
L = sp.symbols('L', positive=True)
ok("QB8  slab pin: A_{mu/L, LT}(om) = A_{mu, T}(L om)  (free phase transforms, exactly)",
   sp.simplify(sp.expand_trig(sp.expand(
       (sp.cos(sp.sqrt(om**2 + (mu/L)**2)*L*T)
        + sp.I*om*sp.sin(sp.sqrt(om**2 + (mu/L)**2)*L*T)/sp.sqrt(om**2 + (mu/L)**2))
       - (sp.cos(sp.sqrt((L*om)**2 + mu**2)*T)
          + sp.I*(L*om)*sp.sin(sp.sqrt((L*om)**2 + mu**2)*T)/sp.sqrt((L*om)**2 + mu**2))))) == 0)
```

Output, verbatim:

```text
QA1a m_0 = m_1 = 0, m_2 = -1 (the only stencil inputs used)          : PASS
QA1b binomial kill: sum_{j=1}^n C(n,j)(-1)^j = -1, n = 1..10         : PASS
QA2  (Q (x) I4)(I2 (x) S) = (I2 (x) S)(Q (x) I4) = Q (x) S (any Q)   : PASS
QA3a model sanity: Q^2 = Q = Q^T ; 1_A 1_B = 1_B (nested projectors) : PASS
QA3b algebra: M_A - M_A^2 = Q 1_A (I - Q) 1_A Q  (exact identity)    : PASS
QA3c NON-IDEMPOTENCE witness: M_A^2 != M_A (exact rational entries)  : PASS
QA3d COLLAPSE-FAILURE witness: M_A M_B != Q 1_A 1_B Q = M_B          : PASS
QA3e NON-COMMUTATIVITY witness: [M_A, M_B] != 0 (exact rational)     : PASS
QA3f contraction: 0 <= M_A <= I (exact eigenvalues of M_A in [0,1])  : PASS
QA4a parity: <phi0|1_[-r,r]|phi1> = 0 EXACTLY (odd integrand)        : PASS
QA4b entries: b00 = erf(r) ; b11 = erf(r) - 2 r e^{-r^2}/sqrt(pi)    : PASS
QA4c n = 2 carrier: [B(r1), B(r2)] = 0 (parity-diagonal family)      : PASS
QA4d idempotence defect entry: b00 - b00^2 - b01^2 = erf(r) erfc(r)  : PASS
QA4e positivity pieces: e^{-t^2} > 0 (t real) ; phi0(r) != 0         : PASS
QA4f Fourier: hat(e^{-x^2/2}) = sqrt(2 pi) e^{-k^2/2} ; hat(x e^{-x^2/2}) = -i k sqrt(2 pi) e^{-k^2/2}: PASS
QA4g super-polynomial kill: k^N e^{-k^2/2} -> 0 as k -> oo, N = 1..8 : PASS
QA5  int_0^1 v(t) <phi0|1_{r(t)}|phi0> dt = <phi0| Theta(x) |phi0>   : PASS
QA6a phase split: K + sum_{T != empty} prod(z-1) K = z1 z2 K (T-blind factorization): PASS
QA6b '-1'-selection: sum_{T != empty} (-1)^{|T|} K = -K  (order-n cancellation, operator level): PASS
QA7a (A (x) S)^2 = A^2 (x) I  (S^2 = I engine, compressed)           : PASS
QA7b d/dlam RHS = -i (A (x) S) RHS ; RHS(0) = I  (unique ODE solution): PASS
QA7c weighted eq-object, compressed: sum_l w_l cos(l a) = c(a) per eigenvalue: PASS
QA8a partial resolution: [E^Q]_0 = m_0·(unimodular) - 1 = -1 (times 1_ball): PASS
QA8b display ledger unchanged: (g-1) + (-g) = -1 ; full resolution: 0 - 0 = 0: PASS
QB1a unitarity: e^{-i h_0} (e^{-i h_0})^dag = I  (on the unit sphere) : PASS
QB1b band-diagonal: e^{-i h_0} P_-(u) = e^{+i w} P_-(u) ; P_+ -> e^{-i w} P_+: PASS
QB1c modulus 1: |e^{+i w}|^2 = cos^2 + sin^2 = 1                     : PASS
QB2  sum_l w_l F Omega_l = F sum_l w_l Omega_l  (any F, any Omega_l)  : PASS
QB3a HS invariance: tr[(UX)^T (UX)] = tr[X^T X]  (U^T U = I)          : PASS
QB3b (UX)^dag(UX) = X^dag X as operators (norm-blind dressing)        : PASS
QB4  (e^{-ih_0} Om_mu)^dag (e^{-ih_0} Om_lam) = Om_mu^dag Om_lam (pair kill BEFORE det/tr): PASS
QB5a |A_int|^2 = |A|^2 = |A_full|^2  (all moduli equal exactly)       : PASS
QB5b ZF modulus identity survives any unimodular dressing: Om^2|A|^2 = om^2 + mu^2 cos^2(Om T): PASS
QB6  full dressed amplitude at omega = 0: e^{-i om T} A |_{om=0} = cos(mu T): PASS
QB7a m_0 · e^{+i w} = 0  (the killed operand carries the phase away)  : PASS
QB7b contingency witness: e^{i w} - 1 != 0 as a function (= -2 at w = pi): PASS
QB7c boundary witness: tr(e^{-i h_0}) = 4 cos w != tr(I) = 4 (naked single-branch trace NOT neutral): PASS
QB8  slab pin: A_{mu/L, LT}(om) = A_{mu, T}(L om)  (free phase transforms, exactly): PASS
```

(38/38 PASS, 0 FAIL. One development iteration replaced a bare `simplify`
by `rewrite(erf)` in QA4d — sympy mechanics only, no mathematical content
changed. The strict positivity of erf(r)·erfc(r) on 0 < r < infinity and
the summed-series smoothing step (A-5) are exact-analysis steps stated and
justified in §2.3; the CAS battery verifies every algebraic/integral/limit
identity they rest on.)

### 2.7 Task A verdict

**TASK A = DELIVERED: Q-RESOLUTION-STABLE**, with the exact quantifiers of
§2.4: (i) partial (lawful, display-convention-anchored) resolution
REPRODUCES the -1_ball identity at every fixed sealed carrier (n, ell), by
smoothing kill + m_0 = 0; (ii) the order-by-order cancellation survives as
exact kernel-blind operator algebra at every order; (iii) the phase-free
closure is Q-sensitive in FORM (non-idempotence always — witnessed;
non-commutativity generically at n >= 4 — witnessed in exact rationals;
functional-calculus survival at the sealed n = 2 carrier — derived) with
ALL failure confined to degree -infinity, below the carried degree <= -1
remainder; (iv) full resolution degenerates the degree-0 comparison to
0 = 0 with no counter-content; (v) at the fully finite Phase-A object the
verdict's level of record does not exist (named, rung 3). No step of the
R2 verdict breaks; no FAILS-AT obstruction arises; no refinement effect
reaches the level of record.

---

## 3. TASK B — RESIDUE r-c: FREE-FACTOR BOOKKEEPING, CERTIFIED PER COMPARISON

### 3.1 Scope of the residue, pinned at bytes

DERIVED (bytes): r-c concerns the OVERALL common left factor e^{-i h_0}
(R2 :110-116: "common to all lambda: carried, content-neutral"; REM
:128-130 same words). The INTERIOR free phases (the R(t)-conjugation and
the sub-interval dressings inside F) are CONTENT-BEARING and are exactly
the oscillatory/coincidence strata of record — counted, not neutralized
(REM-CHK :202-207: "the coincidence corrections to commuting free phases
past indicators ARE the coincidence strata, counted separately"). The
neutrality claim under audit is about the former only; this section
certifies it there, comparison by comparison.

### 3.2 The mechanism catalogue (each CAS-pinned in §2.6)

```text
L  common-factor linearity: sum_l w_l e^{-ih_0} Omega_l =
   e^{-ih_0} sum_l w_l Omega_l (h_0 lambda-independent). QB2. EXACT.
U  unitary norm invariance: (UX)^dag(UX) = X^dag X, so every Schatten/HS/
   operator-norm statement is dressing-blind. QB1a, QB3a/QB3b. EXACT.
B  band-diagonality: e^{-ih_0} P_s = e^{-+ i|k|} P_s per symbol point —
   intraband/P-block readings commute with the dressing; a modulus-1
   band phase shifts no degree. QB1b/QB1c. EXACT.
M  modulus blindness: |unimodular · z| = |z| — every domination, rate
   bound, floor, and modulus identity is phase-blind. QB5a/QB5b. EXACT.
E  unit evaluation: at the omega = 0 slice the free phase IS 1; slice
   identifications are exact. QB6. EXACT.
A  adjoint-pair cancellation: (e^{-ih_0} Om_mu)^dag (e^{-ih_0} Om_lam) =
   Om_mu^dag Om_lam — the factor cancels BEFORE any trace/determinant in
   every CTP-pair object. QB4. EXACT.
Z  annihilated operand: phase x 0 = 0 — neutrality in comparisons whose
   dressed side has content 0 of record (m_0 kill / double kill). QB7a.
   EXACT GIVEN THE KILL; the kill is sealed + checked of record.
   CONTINGENT in kind: the phase itself is NOT removable (e^{iw} != 1,
   QB7b) — see finding F-1.
COV exact covariance (not neutrality): under dilation the free phase
   TRANSFORMS (e^{-i om L tau}); the alpha' chain uses the transformed
   phase exactly, never an invariance assumption. QB8. EXACT.
```

### 3.3 R2's own comparisons (the "every comparison above" of the claim)

```text
R2-1 Frame factorization U(0) = e^{-ih_0} sum_l w_l Omega_l (:110-116).
     Mechanism L. CANCELS EXACTLY (factors out). DERIVED.
R2-2 Two-level reduction and excursion series (:134-184): the amplitudes
     A_lambda are interaction-picture objects — the free phase is
     stripped by DEFINITION (per symbol point a modulus-1 scalar), and
     every "-1" comparison (A_int - 1) is made on the stripped object.
     Mechanisms B + M. NEUTRAL EXACTLY. DERIVED.
R2-3 Domination |A^(n)| <= (|lambda| Theta)^n/n! (:174-184): phases
     modulus-1. Mechanism M. NEUTRAL EXACTLY. DERIVED.
R2-4 Degree-0 readings: [U(0)]_{deg0,intra} = 0 (d4 :337-340, "With the
     carried free phase e^{+i omega} (modulus 1)"), E = U(0) - P =
     -1_ball (:271-284), [P+E]_0 = 0 (:278): mechanism Z on top of B —
     the phase multiplies content that is 0 by the m_0 kill; P carries
     no dressing and none is needed against it. NEUTRAL BY ANNIHILATION
     (see F-1). DERIVED.
R2-5 Unitarity self-test both ways (§4): WAY 1/WAY 2 compare two zeros;
     the modulus pin |A|^2 + |B|^2 = 1 is phase-blind. Mechanisms M + Z.
     NEUTRAL EXACTLY. DERIVED.
R2-6 Slab exhibit and omega = 0 slice identifications (:344-371, V4e;
     the eq c-weight :410-412): at omega = 0 the free phase equals 1.
     Mechanism E. NEUTRAL EXACTLY (QB6). DERIVED.
R2-7 Survivor assembly X (§5(c) :396-424): U(0)^dag endpoint carries
     e^{+ih_0} (band-diagonal, modulus-1; content 0 by the kill — B +
     Z); F's sub-interval W's carry interior free phases (content-
     bearing, counted as strata — out of r-c's scope, §3.1); the weight
     sum -> m_0 (free) J (free) = 0 (Z); the c-weight is the omega = 0
     slice (E). NEUTRAL AS CLAIMED. DERIVED.
R2-8 The V_2 subsidiary (:373-389): the symbol is written "(times the
     free phase)" EXPLICITLY — the phase is retained, not removed, and
     the degree-(-1) grading is a modulus statement (mechanism M/B:
     unimodular factors shift no degree). NEUTRAL FOR THE GRADING;
     phase honestly carried in the display. DERIVED.
```

### 3.4 The REM chain (the B-L2* discharge)

```text
REM-1 Remainder object §2.1 (:108-135): consumes R2-4/R2-7 ([U(0)]_0 =
     [F]_0 = 0); names the factor carried content-neutral (:128-130).
     Mechanisms Z (inherited kills) + B. NEUTRAL. DERIVED.
REM-2 Rate chain §3.3a (:277-296) and ZF-consumed slab rate: bounds on
     A_int - 1 (stripped object) and moduli only. Mechanism M. NEUTRAL
     EXACTLY. DERIVED.
REM-3 Plancherel/HS evaluations §3.3c (:299-311) and the general
     stratum §3.5 (:355-383): REM itself peels "every dressing unitary
     ... at OPERATOR norm (= 1)" (:357-358) — the free factor is one of
     the peeled unitaries. Mechanism U. CANCELS EXACTLY IN EVERY NORM.
     DERIVED (QB3a/QB3b; unitarity QB1a).
REM-4 Reversal exclusion §4 (:405-441): norm route = U; limit route =
     bounds on |sigma| = M. NEUTRAL EXACTLY. DERIVED.
REM-CHK: same comparisons re-derived; its (5)(v) (:147-148) records the
     factor neutral-carried; its :202-207 draws exactly the scope
     boundary of §3.1. CONSISTENT AT BYTES. DERIVED (reading).
```

### 3.5 ZF (zero-free chain on the discharged basis)

```text
ZF-1 Modulus identity and slab floor (:154-181): Om^2|A|^2 = omega^2 +
     mu^2 cos^2(Om T) and |A| >= omega/Om — modulus statements,
     identical for A, A_int, and any unimodular re-dressing (QB5a/QB5b).
     Mechanism M. NEUTRAL EXACTLY. DERIVED.
ZF-2 The omega = 0 slice zero location and transversality (:183-226):
     at omega = 0 the free phase is 1 (QB6); the located zero
     (rho* = 2^(-5/4)) and the weighted value 1/2 there are
     phase-unaffected. Mechanism E. NEUTRAL EXACTLY. DERIVED.
ZF-3 Rate-bound zero-free region (:228-245): consumes REM-2. Mechanism
     M. NEUTRAL. DERIVED.
ZF-4 "off the record support the weighted sum is m_0 · (free phase) = 0"
     (Z6c :259-263): mechanism Z verbatim at ZF's own bytes (QB7a).
     NEUTRAL BY ANNIHILATION. DERIVED.
ZF-5 Pair determinants and normalization (§3.4(ii) :267-277, §4.1(b)
     :332-339, §4.2): every det/tr of record is a CTP PAIR object
     (V_{mu lambda}; the D4 adjoint-continued bra branch; E1 :743-751)
     — the bra and ket free factors cancel EXACTLY BEFORE the
     nonlinearity (mechanism A, QB4); the record-phase algebra
     (phase_mu phase_lambda) that remains is the sealed R.2 content,
     not the free factor. Likewise B-L1's trace object
     tr[C(V(a) - V(0))C] (E1 :1106-1108): pair objects only. NEUTRAL
     EXACTLY — see finding F-2 for the boundary this rests on.
     DERIVED.
```

### 3.6 The remaining consumers (G1, RL4/RL0, S2, R3C, AQ)

```text
G1   (df4514a5 + check): assembles the licensed sentence from REM's
     verdict; carries (r-c) OPEN as a declared residue (:266-275); no
     new free-factor-bearing comparison of its own. NEUTRAL-VACUOUS
     (citational). DERIVED (reading).
RL4/RL0 (a903716c + check): consumes the ZF census (weight arithmetic —
     free-factor-free objects) and R2's domination sup|lambda|Theta = pi
     (:417, :659) — a modulus statement (M). NEUTRAL. DERIVED.
S2   (e5b0879b + audit; the alpha' = 1/4 record): the ONE consumer where
     the free factor enters a comparison NON-trivially — and it is
     handled by exact COVARIANCE, not by a neutrality assumption: under
     dilation every free phase becomes e^{-i omega L tau} (S2 STEP 4
     :203-207), pinned in closed form by A_{mu/L, LT}(omega) =
     A_{mu, T}(L omega) (QB8 re-verified here); the norm step is unitary
     invariance (STEP 6; mechanism U). The alpha' = 1/4 extraction
     consumes the factor correctly at every step. NO FINDING. DERIVED.
R3C  (57edbb96 + audit): branch DIFFERENCES u_0 - u_± =
     e^{-ih_0}(Omega_0 - Omega_±) — mechanism L (common factor exits
     the difference); the slab parity split RA7 (A even / B odd in the
     coupling) is a statement about the stripped amplitudes (M); its
     retained "bare degree-0 content" is dressed by a band-diagonal
     modulus-1 factor that changes no degree, norm, or parity claim
     (B + M + U for its e_2/HS forms). NEUTRAL. DERIVED.
AQ   (0a10c030 + check): quantifier adjudication; enumeration of its
     displays found NO free-factor-bearing comparison. NEUTRAL-VACUOUS.
     DERIVED (reading).
```

### 3.7 Findings (stated exactly; none is a refutation)

```text
F-1  ANNIHILATION, NOT CANCELLATION, in the Z-class. In every degree-0
     comparison against an UNDRESSED comparand (E = U(0) - P; [P+E]_0 =
     0; m_0 (free) J (free) = 0), the free factor does not cancel — it
     is ANNIHILATED: neutrality holds because, and only because, the
     dressed side's compared content is 0 of record (the m_0/double
     kill, sealed + checked). Exact contingency witness: e^{iw} - 1 != 0
     as a function (QB7b) — were any killed content restored, the factor
     would bear content in exactly these comparisons. The sealed claim
     "content-neutral in every comparison" is TRUE as stated; its
     mechanism in this class is kill-contingent, and is here made
     explicit so no successor mistakes it for an unconditional
     invariance. DERIVED.
F-2  THE NONLINEAR BOUNDARY. A trace or determinant of a SINGLE dressed
     branch would NOT be phase-neutral: tr(e^{-ih_0}) = 4 cos(w) != 4
     (QB7c, exact witness). NO such comparison exists of record: every
     trace/determinant in the consuming corpus is a CTP pair object
     (E1 :743-751, :1106-1108; ZF §4.1(b)), where mechanism A (QB4)
     cancels the factor exactly BEFORE the nonlinearity. The boundary
     is real, drawn at bytes, and discharged of record. DERIVED.
F-3  SCOPE. r-c is the OVERALL common factor only; interior free phases
     are content and are counted as the oscillatory/coincidence strata
     of record (§3.1). Any future audit that meets a free phase INSIDE
     a stratum should charge it to the strata bookkeeping, not to r-c.
     DERIVED (reading of bytes).
```

### 3.8 Task B verdict

**TASK B = DELIVERED: NEUTRALITY CERTIFIED** — the free factor e^{-i h_0}
is content-neutral in EVERY consuming comparison of record: R2's eight
comparison classes (§3.3), the REM chain (§3.4), ZF (§3.5), and
G1/RL4/S2/R3C/AQ (§3.6), each with its exact mechanism (L/U/B/M/E/A/Z/COV)
derived and CAS-pinned; the two contingent classes (Z: kill-contingent;
det/trace: pair-contingent) are stated exactly (F-1, F-2) with exact
witnesses of the contingency and exact discharge at the sealed bytes. The
enumeration is closed over the sealed corpus as of this session's bytes
(every artifact naming a7969f0c, plus the transitive consumers AQ/G1/ZF/
RL4/S2/R3C found through the REM chain, plus both checks of each). No
comparison where the factor fails to cancel (or fails to be annihilated)
EXISTS of record: NOTHING TO REFUTE, and the certification is positive,
not merely a failure to find.

### 3.9 CAS battery for Task B

The QB1-QB8 battery is part of the single script reproduced verbatim with
its output in §2.6 (labels QB1a-QB8; 14 identities, all PASS).

---

## 4. NET — WHAT THE FIXED-COMPLETION LIST BECOMES IF THESE RESULTS HOLD

Descriptive only; nothing here flips a flag, moves a debt, or creates a
target. IF the §2/§3 determinations hold (and survive their check), the
a7969f0c NOT-DELIVERED list (:450-464) reads:

```text
(r-a) carrier-uniformity of the degree <= -1 remainder: ALREADY DISCHARGED
      of record at the summed level (2e4e5163, confirmed 89641f76) —
      unchanged by this artifact.
(r-b) Q-resolution: RESOLVED-STABLE (this artifact, §2, at the stated
      quantifiers: partial resolution REPRODUCES -1_ball at every fixed
      sealed carrier; full resolution vacuous at fixed carrier with no
      counter-content; every Q-sensitive step's failure exact, witnessed,
      and confined to degree -infinity; rung-3 non-existence named).
(r-c) free-factor bookkeeping: CERTIFIED-NEUTRAL (this artifact, §3, per
      comparison over the full consuming corpus, mechanisms assigned,
      contingencies F-1/F-2 stated and discharged).
(r-d) successor-spec possibility: REMAINS — a spec-author's act, not a
      property of E; untouched and untouchable here.
UNMOVED REGARDLESS: DEBT 2 (F'-14 log-of-sum passage); DEBT 3 (summed-S2'
      re-quantification act); the compute pre-gate; R-L4a/R-L4b; OBL-D;
      the per-pair refutation (52f2490b) stands; per-pair nothing moves;
      RUNNING stays an unforced lean; M(t) stays sharp; equal-time stays
      FALSE by C6; all V011 flags stay as sealed.
BEARING ON THE DISCHARGED CHAIN AND THE alpha' = 1/4 RECORD: stated in
      §5's DISCHARGED_CHAIN_BEARING flag — confirming only, at their own
      quantifiers; reported there as findings, with no consequence
      classified.
```

---

## 5. FLAG BLOCK

```text
RB_STATUS = DELIVERED(Q-RESOLUTION-STABLE — partial resolution reproduces
  the -1_ball identity exactly at every fixed sealed carrier via the
  smoothing kill + m_0 = 0 (QA8a) with the order-by-order cancellation
  surviving as kernel-blind operator algebra (QA6) and every Q-sensitive
  step's failure witnessed exactly (QA3c/QA3e/QA4d) and confined to
  degree -infinity, below the carried degree <= -1 remainder; full
  resolution degenerates to 0 = 0 with no counter-content; the level of
  record does not exist at the fully finite object (named).)

RC_STATUS = DELIVERED(NEUTRALITY-CERTIFIED — e^{-i h_0} is content-neutral
  in every consuming comparison of record across R2, the REM chain, ZF,
  G1, RL4/RL0, S2, R3C, and AQ, by mechanisms L/U/B/M/E/A/Z/COV assigned
  and CAS-pinned per comparison (QB1-QB8), with the kill-contingent class
  and the pair-contingent det/trace boundary stated exactly and shown
  discharged at the sealed bytes (F-1/F-2; witnesses QB7b/QB7c).)

DISCHARGED_CHAIN_BEARING = CONFIRMING-ONLY(both determinations bear on the
  discharged B-L2* chain, the adopted summed amendment, and the
  alpha' = 1/4 record ONLY in the confirming direction at their own
  quantifiers — REM's carried (r-b)/(r-c) resolve stable/neutral; the
  alpha'-chain's free-phase step is exact covariance (QB8) plus unitary
  peeling (QB3), and its Q-bearing is S2's own already-named
  dilation-closure premise — reported as findings, no consequence
  classified, nothing overturned, no flag flipped.)

NET = BOTH-RESIDUES-RESOLVED-AS-CARRIED(r-b stable and r-c neutral exactly
  as the sealed lineage carried them, so IF checked the FIXED-completion
  list reduces to (r-d) plus the standing debts F'-14, summed-S2', the
  compute pre-gate, and R-L4a/b — with per-pair, M(t)-sharp, C6-FALSE,
  and every sealed flag unmoved.)

SEALS_VERIFIED = 23/23(full digests in §1, all recomputed from bytes at
  path this session, all matching sidecars; the five tasked prefixes
  a7969f0c / a4f2e1b7 / 2e4e5163 / 89641f76 / 46846730 match; every cited
  span read at path.)

FORBIDDEN_IMPORTS = none(exact symbolic only — sympy 1.14.0 in fresh venv
  rbrcvenv under /private/tmp, 38/38 identities, script + output verbatim
  in §2.6; nothing numeric evaluated; no physical quantity computed or
  bounded; alpha not computed or approached; L formal; M(t) sharp and
  untouched; equal-time FALSE by C6; no carrier or cellulation datum
  introduced; no witness action; no flag flipped; no
  register/tracker/plan/road/ledger/lens file read; no git action; no
  existing file edited; one output file written.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
RB_RC_RESIDUES_RESULT = SEALED.
```
