# STAGE 8 — THE BIT: Rhat_K,N in R L_T,N INSIDE O_R5,N — BOTH DIRECTIONS ATTEMPTED — V001

## BLIND BUILDER — CODENAME RKB-BUILD — COMMISSION T1SR — [SEALED]

Date: 2026-08-15 (session UTC). Commission: THE BIT — the single open fact
the member-existence question now reduces to (reduction chain of record:
the extent display's X-1..X-4): whether Rhat_K,N lies in R L_T,N inside
O_R5,N — E-1 (ker(qtilde_N) = the pure-cycle (R4) line alone; NO mixing;
the ExtSrc fiber INHABITED via W-EX3, modulo TOP-1/TOP-2 and the dim M_N
cell) vs E-2 (one mixing line with a_K != 0; the fiber dead at every
dim M_N >= 2 stage per NG-3'). Commissioned 2026-08-15 by the successor
registrar, tag T1SR. FRESH COMMISSION: the prior commission S9AD for this
same bit (run wf_31447492) was recorded dead at session cutover with zero
bytes landed; this artifact is NOT a continuation and consumes nothing
from any RK_LT-class file (one such file EXISTS at the dead S9AD path —
disclosed at §2/D-1 at metadata grade, content UNCONSUMED by commission
fence; it is NOT at this commission's output path, so the STOP condition
does not fire).

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION ONLY — no member
bound, no adoption, no authored physics, no route/branch/address/
representative selected, no score/flag/gate moved; the sealed
R_K_EQUALS_L_T flags are READ, not moved. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF|continuation opened;
"Q-…" tokens EXPECTED-UNLOCATABLE (cited only as carried inside sealed
artifacts). No git; no existing file edited; output + sidecar probed
ABSENT at session start and re-probed ABSENT at 2026-08-15T07:04:24Z
immediately before this write; ONE output file plus its seal sidecar.
Exact symbolic only: the battery ran in a FRESH venv (sympy 1.14.0;
16/16 PASS; exact integers/rationals/symbols; no float anywhere; no
value frozen; source displayed VERBATIM in Appendix A — the standing
C-3 repair honored).

**SWEEP CUTOFF: 2026-08-15T07:03Z** (SW-T1..SW-T6 below; anything sealed
after this cutoff is outside this artifact). Exhaustiveness is
sweep-displayed only; nothing absolute is claimed.

---

## 0. VERDICT

```text
VERDICT = UNDECIDABLE-TODAY.

Neither direction closes on the sealed record. Every candidate separator
toward E-1 and every candidate identity chain toward E-2 terminates at a
NAMED sealed absence (§4-§5), and the terminal absences are exactly the
flags' own unproved list (70dde1c0 §8; 041498bb §3.4 + §5.3) PLUS two
deeper absences the flags do not list:

  MB-1  L_T,N HAS NO PRESENTATION ON THE SEALED CARRIERS. It is typed
        ("the uniquely normalized transverse Maxwell kernel in O_R5,G",
        70dde1c0 §2), normalized (p_loc[L_T]=1, ratified lineage), and
        nonzero at typing grade — but its K/K realization/embedding is
        itself listed as REQUIRED FUTURE DATA ((R4-11) item 2: "the
        embedding of L_T in O_R5 on every physical CTP block"). Every
        E-2 identity chain must terminate at an L_T-side presentation
        that does not exist.
  MB-2  NO ADMITTED EVALUATOR EXISTS. The only normalized covariant
        functional the record names is p_loc, and even its EXISTENCE is
        underived (P_LOC_EXISTENCE_DERIVED = false / TYPE-U, 041498bb
        §6.3); sigma_loc is NOT_BUILT ((R2-3); LM-1/LM-2 doors); the
        trace surrogate is excluded ((S1-9)/(R4-10)). Every E-1
        separator must evaluate a functional that does not exist.

So the record is missing THREE grades of object at once: the values
(chi_K both ways unproved), the evaluator (MB-2), and the operand's own
presentation (MB-1). That is WHY the bit is open, said exactly.

WHAT THIS ARTIFACT ADDS (new, exact, battery-verified, Appendix A):

  D-ASYM (decision asymmetry; LEM-A/LEM-B, §4.3):
    LEM-A  ONE admitted covariant functional ell with displayed values
           ell[L_T,N]=1, ell[Rhat_K,N]=0 CERTIFIES E-1 outright (given
           the audited X-1 corollary Rhat_K,N != 0). The extent
           display's branch-(a) "covariant functional pair" is
           OVER-STRONG: a single functional suffices (T2).
    LEM-B  NO finite set of admitted functional VALUES can certify E-2.
           Two exact models (T3a/T3b) realize IDENTICAL functional data
           (ell[L_T]=1, ell[Rhat_K]=c != 0) with E-2 true in one and
           E-1 true in the other. E-2-by-functionals requires a
           point-separating (total) family on the closure of
           span{L_T,Rhat_K} — a TOP-2-grade certificate, undisplayed
           (round 3 SW-5) — or presentation-grade data (MB-1).
    COROLLARY (shape-level audit criterion for the registrar): any
           future artifact claiming E-2 from one reader value is
           REFUTABLE ON SHAPE; chi_K displayed = 0 (by a DERIVED
           functional) decides E-1; chi_K displayed != 0 alone decides
           NOTHING (it only fixes a_K conditionally on E-2).
  D-FIBER (reader-fiber collapse, downstream-equivalent display; T9/T10):
    at a rank-1 anchor stage the bit is EQUIVALENT, at algebraic reader
    grade, to: the normalized algebraic reader fiber (LM2-4) is a LINE
    (E-1: chi free, dim 1+m-r = 1) vs a POINT (E-2: chi FORCED = a_K,
    tau = a_K/rho, dim 0). E-2 would collapse READING_RULE_FIBER_Q396's
    visible coordinate chi_K to the forced value a_K; E-1 leaves it
    free. Downstream only — readers are constrained BY Rel_N and cannot
    fix it — but it says exactly what each branch does to the sealed
    P_LOC_RK = UNDERDETERMINED flag.
  D-RANK (the cheapest kill shape found; T11): Rhat_K,N is a sealed
    ISOMORPHISM (DoR-019 fullness, (S2-6)). Hence E-2 transports
    invertibility: a_K != 0 and Rhat_K = a_K L_T force L_T,N invertible
    on the carrier realization. CONTRAPOSITIVE: one sealed byte typing
    L_T,N SINGULAR (any kernel direction on its K/K realization) would
    decide E-1 in one line. No such byte exists (SW-T4: the workspace
    has NO L_T singularity/invertibility/rank byte at all) — and the
    byte lives behind MB-1. The "transverse" in L_T's name is a
    FIELD-side word; reading it as carrier-side singularity would be
    the LM-2 category error (70dde1c0 attack 7). Named, not consumed.
  COMPLEMENT PLACEMENT ("R_K lies wholly in the nonlocal/higher-
    derivative complement", 041498bb §5.3): REFUSED-TODAY as UNPOSABLE
    — the complement is not a sealed object. No declared direct sum
    (R2-4), no sigma_loc (R2-3), and the separation is OBLIGATED TO BE
    DERIVED spectrally/at long wavelength (ratified lineage clause,
    (S1-1) line 7). Membership in an undeclared complement is not a
    well-posed sealed question; neither proved nor refuted (§4.2 S-ii).

MISSING CLOSURE OBJECTS, exactly (any ONE decides; per isometry class,
at one anchor stage — X-4 de-duplication carried):
  D-A (decides E-1; cheapest): one DERIVED admitted covariant
      functional on O_R5,N with displayed pair (value 1 on L_T,N,
      value 0 on Rhat_K,N) — the (S1-5) line-2 shape; LEM-A closes it.
  D-B (decides BOTH ways): the sealed presentation/embedding byte for
      L_T,N on the K/K carrier class ((R4-11) item 2 made sealed) +
      the exact comparison computation against the displayed Rhat_K,N;
      also delivers D-RANK's one-line kill if L_T,N is singular.
  D-C (decides E-2, functional route): a derived functional family
      WITH a totality/separation certificate on the closure of
      span{L_T,N, Rhat_K,N} (TOP-2-grade) and the displayed common
      ratio a_K != 0.

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED.
PIN-2-H: every display below is IDENTICAL under R-A and R-B (no step
touches (JD-3)'s words).
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
COMMISSION STOCK (read in full)
cfa2fb97  STAGE8_IDEAL_EXTENT_S9AD_V001.md                     MATCH
eda2ba74  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md               MATCH
          (CONFIRMED-WITH-CORRECTIONS; C-1 citation transposition
          honored below: the does-not-prove list is at 041498bb §5.3)
70dde1c0  STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001.md
          MATCH (§2 types (T2-1)-(T2-3); (S1-1)-(S1-5); (S1-9)/(S1-10);
          (S2-6); (S2-13)-(S2-15); §6.2 attacks; LM-1/LM-2 doors; §8
          flag R_K_EQUALS_L_T = NO_VERDICT / TYPE-U at bytes)
041498bb  STAGE8_TASK4B_CONSUMPTION_VERIFICATION_AND_PUSHFORWARD_SWEEP_
          LANE1_V001.md                                        MATCH
          (§0.5 lineage; (R1-1)/(R1-3); (R2-1)-(R2-4); (R3-4); (R4-3)/
          (R4-8)/(R4-10)/(R4-11); §3.4 flag; §5.3 does-not-prove list;
          §6.3 P_LOC_EXISTENCE_DERIVED = false — all at bytes)
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md                    MATCH
          (anchor stages; arm-1 "one value" <=> no-mixing; NG-3/NG-3';
          §6 biconditional; TOP-1/TOP-2; kappa_T,N witness coordinate)
a7255f38  STAGE8_EXTSRC_ROUND3_S9AD_AUDIT_V001.md              MATCH
          (hash verified; round-3 pair consumed at audited grade)
CHASED FORMATION-CHAIN CARRIERS (verified before their bytes were used)
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md              MATCH
          ((S1)/(S2)/(Q5); (R1)-(R10); §4.4 at bytes)
b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md         MATCH
          ((S1)-(S8) incl. (S2a) :136-144, (S4), (S8) :218-228; §2.4
          route bar :941-943 at bytes)
bae34116  STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md MATCH
          ((LM1-6)-(LM1-17); (LM2-1)-(LM2-19) at bytes)
be570c18  STAGE8_TASK4B_STATIONARY_RESPONSE_COMPUTATION_LANE1_V001.md
          MATCH ((W1-3)-(W1-5) at bytes)
6b7f8f98  STAGE8_TASK4B_RETEXTRACT_KERNEL_CERTIFICATE_LANE1_V001.md
          MATCH (:140-155 "S := an admitted K/K Schur form K->K^*")
474bf721  STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md        MATCH
          (hash at path; (Q13) S_nm isometry consumed at audited grade
          via the extent pair, which walked :577-581)
fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md           MATCH
          (hash at path; u typing byte consumed at audited grade via
          the extent pair: rank-preserving only, no isometry byte)
MISMATCHES = 0. KER (B2-7) faithfulness and the 13d227ce/d386bb74
p_loc-lineage clauses consumed at audited grade exactly as quoted
inside the seal-verified stock above. The held construction 506bfe2a
untouched, unopened, unpresupposed.
```

## 2. SWEEPS RUN (cutoff 2026-08-15T07:03Z; roots per commission = archive workspace + cleanroom v003, minus fenced filename classes; case-insensitive fence filters — the CL-8 lesson applied)

```text
SW-T1  output-path uniqueness: STAGE8_RK_LT_BIT_T1SR_V001.md + sidecar
       probed ABSENT at session start and re-probed ABSENT at
       2026-08-15T07:04:24Z. First-use path.
SW-T2  "R_K_EQUALS_L_T|R_K=L_T|proportional to L_T|Rhat_K=a_K" both
       roots -> the two flag sites (70dde1c0, 041498bb), the route bar
       (b53d9e93), the extent pair (quoting them), one adjudication
       artifact QUOTING (S1-5) and its "No authority proves any line"
       sentence (SEED_ADJUDICATION, match lines :155/:293 — quotation
       grade, no new determination), and the fenced S9AD-path file
       (D-1). Cleanroom root: mirrors of the same three sites. NO
       sealed determination anywhere; the flags stand.
SW-T3  "sigma_loc" both roots -> 9 files; ZERO constructed-grade lines
       (every hit is NOT_BUILT/unbuilt/future/missing/required class).
       The (R2-3) arrow is absent of record.
SW-T4  L_T operator-rank hunt: "L_T … singular|invertible|has kernel|
       rank-deficient|non-injective" (both orders) -> ZERO hits in the
       workspace. No sealed byte types L_T,N's carrier rank; D-RANK's
       kill byte does not exist today. L_T presentation hunt (":=|
       realization|embedding|presentation|matrix") -> no defining site;
       the only embedding language is (R4-11) item 2's REQUIRED row and
       the [YOURS] contract (S4) — specifications, not sealed maps.
SW-T5  "O_R5 … one-dimensional" -> ZERO hits: no sealed byte declares
       the class one-dimensional (guards the §4.4 refusal; concurs with
       041498bb §3.1 "not declared one-dimensional" at bytes).
SW-T6  landings after the extent audit's seal (05:54:23Z): EXACTLY ONE
       file in the workspace — the fenced S9AD-path file (D-1). Nothing
       else post-dates the audited extent pair in either root.

D-1  DISCLOSURE (prominent, metadata grade only): STAGE8_RK_LT_BIT_
     S9AD_V001.md (41,273 bytes, mtime 2026-08-15T06:50:51Z) + sidecar
     (96 bytes, 06:51:11Z) EXIST at the dead S9AD output path. The
     commission's premise "ZERO bytes landed" is CONTRADICTED AT PATH.
     Its mtimes POSTDATE the extent pair's cutoffs (05:30Z/05:48Z) —
     so the extent pair's own sweeps remain sound — and PREDATE this
     commission's sweeps by ~12 minutes. Per the T1SR fence its content
     (including its sidecar) was NOT opened, read, hashed, or consumed;
     it contributes NOTHING below. It is not at this commission's
     output path; the STOP condition does not fire. REGISTRAR ACTION
     SUGGESTED: quarantine or adjudicate that file's standing
     explicitly; its existence-at-path should not remain implicit.
```

---

## 3. TASK 1 — THE TWO OBJECTS AT THEIR DEFINING BYTES

### 3.1 Rhat_K,N — full bottom-up formation chain (every link sealed)

```text
K_N      := the record-visible completed cycle carrier (Gate-4/path-
            visible physical cycle quotient; 70dde1c0 §2, §4.2).
g_K,N    := the ratified positive carrier metric (DoR-019).
R_K,N    : K_N -> K_N^*, (R_K,N x)(v) = g_K,N(x,v) — the Riesz map.
            FULLNESS: ker(I_K,N) = 0 => R_K,N is an ISOMORPHISM
            ((S2-6) at bytes; also (R3-4) "R_K is an isomorphism").
UNITS    : K_N[U_K] --R_K,N[U_K^(-2)]--> K_N^*[U_K^(-1)]  (T2-1).
            Bare R_K,N is a same-sector carrier map, NOT an action
            kernel.
DRESSING : j_R4,N : U_action tensor Hom(K_N,K_N^*) -> O_R5,N,
            Rhat_K,N := j_R4,N(u_action tensor R_K,N)      (T2-2),
            u_action the unit CLASS, never a representative.
NONZERO  : Rhat_K,N != 0 — audited corollary of the slice closure X-1
            (extent pair, consumed at audited grade).
REACHABILITY (sealed identities land ON Rhat_K,N exactly):
            rank 1:  P_x = s R_K,N            ((S3-5)/(LM2-9); T7)
            rank 2:  P_x1 + P_x2 = r^2 R_K,N  ((R4-3)/(LM2-13); T6,
                     choice-invariant, generic-SPD verified)
            — the whole displayed completed-fiber direction at a
            rank-1 anchor stage is a MULTIPLE of Rhat_K,N.
```

### 3.2 L_T,N — top-down typing; NO presentation (the asymmetry)

```text
TYPE     : "L_T,G := the uniquely normalized transverse Maxwell kernel
            in O_R5,G" (70dde1c0 §2) — a NAME + a MEMBERSHIP.
NORMALIZE: p_loc,G[L_T,G] = 1; iota_loc(b) = b L_T,G; Pi_loc =
            iota_loc compose p_loc ((S1-1), ratified lineage 13d227ce
            as carried); "H_R[G_K] = K L_T + declared higher/nonlocal
            structures" ((R1-1) as carried at 041498bb §2.1) — a
            DECLARED decomposition shape on the candidate saddle, not
            a computed identity on the sealed carriers.
NONZERO  : L_T,N != 0 at SEALED-TYPING grade (the extent pair's CL-3:
            typing + (S1-1) + BIND row; grade noted, not upgraded).
HOME     : O_R5,N := the completed physical retarded action-kernel
            class — K/K Schur forms after RetExtract: Schur_m = H_KK -
            H_KC Inv_CC H_CK; Response_m = RetExtract_m(Schur_m)
            ((W1-4)/(W1-5); 6b7f8f98 "S := an admitted K/K Schur form
            K->K^*"). The C-sector is consumed INSIDE Inv_CC: no block
            separation survives into O_R5,N. The class is NOT declared
            one-dimensional (041498bb §3.1 at bytes; SW-T5) and its
            topology exists-of-record but is uncertified (TOP-1/TOP-2,
            round 3 §4.3-4.4).
PRESENTATION: ABSENT. (R4-11) item 2 lists "the embedding of L_T in
            O_R5 on every physical CTP block" as REQUIRED data of the
            future certificate. The one defining-looking equation,
            kappa_T,N := Krawhat_N(L_T,N) = Sch_N(M_N^loc) ((S4)),
            sits INSIDE b53d9e93's [YOURS — required source contract]
            block — a specification an unbuilt Loc package must meet,
            not a sealed inhabited map — and lands in D_N^ker, not in
            O_R5,N. Round 3 §4.1 instantiates kappa_T,N =
            Sch_N(M_N^loc) only as a WITNESS COORDINATE at the
            existence quantifier (unadopted, conditional). SW-T4: no
            other presentation site exists in either root.
```

### 3.3 The carrier presentation both sit in (where the bit lives)

```text
E_N  := R L_T,N (+) R Rhat_K,N (+) S_(1,sa)(K_N)     ((S1) d662889b;
        (LM1-6)/(LM1-9): E_G = R L_T,G (+)_1 A_prof,G)
qtilde_N(a,b,C) := a L_T,N + j_R4,N(b R_K,N + C)      ((Q5)/(LM1-9))
Rel_N := closure(ker qtilde_N) = ker(qtilde_N) at the census;
O_prof,N := E_N/Rel_N                                  ((LM1-10))
(LM1-10) at bytes: "Rel_G retains every actual relation between L_T,
Rhat_K, and the trace-class directions. In particular, the construction
does not assume Rhat_K = L_T, does not assume they are independent."
(S8) at bytes: "The plus sign in (S8) is not declared a direct sum.
Mixed relations may identify the two displayed pieces."
Anchor stages (round 3 §4 / B-3 census, audited): N_RL, M_RL, N_n, N_m,
all cycle rank 1; dim E_N = 3, coordinates (a,b,c), R_K,N spans
S_(1,sa)(K_N), qtilde_N(a,b,c) = a L_T,N + (b+c) Rhat_K,N.
THE BIT: Rhat_K,N in R L_T,N inside O_R5,N — i.e. whether the sealed
FACT-kernel of qtilde_N contains a mixing line. Menu closed at X-2:
E-1 dim ker = 1 (pure-cycle line only) XOR E-2 dim ker = 2 (one mixing
line (-a_K,1,0), a_K != 0, cycle-side value -(1/a_K) Kern(R_K,N) != 0).
Battery T1a-T1d/T12 reproduce the menu and the forced coefficient
exactly (own run, independent).
```

The formation asymmetry is the bit's anatomy: **Rhat_K,N is PRESENTED
(bottom-up chain to displayed formulas); L_T,N is TYPED (top-down name,
normalization, membership — presentation absent, MB-1).** Every test of
proportionality or independence must cross the gap between a presented
object and a typed object, and the sealed record contains no arrow
across it (the (R2-3)/(S2-14) arrow absent; the LM-2 seam NOT_BUILT).

---

## 4. TASK 2 — THE ATTEMPT, BOTH DIRECTIONS

### 4.1 Ground rules the attempt inherits (at bytes)

```text
(i)   Assuming R_K = L_T is BARRED as a route: "The following are not
      admissible alternatives: finite normalized trace, Calkin-at-
      finite-rank, R_K=L_T, 'contact equals Maxwell,' reader-defined
      pi_Mx, or a harmonic correction selected to make (S28) true"
      (b53d9e93 §2.4 :941-943). A fence on assuming — either way.
(ii)  The countermodel family is live: p_chi(a L_T + b Rhat_K) = a +
      b chi for EVERY covariance/reality-compatible chi ((S1-4);
      (R2-2)) — the algebraic clauses force NO value.
(iii) Both objects share: sector (ordered difference/common retarded
      block, (S2-13)/(R4-1) — Rhat_K is NOT killed by RetExtract, not
      noise-only), unit class (qtilde_N sealed well-typed), reality
      compatibility, covariance under every admitted transport, and
      zero finite shadows of record (the sealed finite retarded blocks
      are exactly zero — Q-279/Q-309 as carried at 70dde1c0 §1; no
      sealed byte displays any L_T,N finite shadow distinct from
      zero). NOTHING in the shared data can separate them, and
      NOTHING in it can identify them.
```

### 4.2 Toward E-1 — the separator hunt (each candidate, its exact blocker)

```text
S-i    p_loc itself (value separator).  ell = p_loc, need chi_K = 0
       displayed. BLOCKED: chi_K unproved BOTH ways (the flags); worse,
       p_loc's EXISTENCE is underived (041498bb §6.3) — there is no
       admitted evaluator to display a value of. [MB-2]
S-ii   Locality grading / complement placement ("R_K lies wholly in
       the nonlocal/higher-derivative complement" — the 041498bb §5.3
       hunt the commission names). REFUSED-TODAY AS UNPOSABLE: the
       complement is NOT a sealed object — no declared direct sum
       O_R5 = span{L_T} (+) O_nonlocal/higher ((R2-4) at :316-320
       "no declared direct sum"), no sigma_loc arrow ((R2-3)), and the
       ratified lineage OBLIGATES the separation to be DERIVED
       spectrally/at long wavelength ((S1-1) line 7; d386bb74 as
       carried). Membership in an undeclared complement is not a
       well-posed sealed question. NEITHER proved NOR refuted; the
       proposition acquires truth conditions only when (R2-4) exists.
       Contact support cannot substitute (attack 5: "contact support
       does not separate F^2 from other local/higher tensor
       structures" — and it would cut BOTH ways).
S-iii  sigma_loc kernel ((S1-5) line 2's shape). WOULD CLOSE E-1 by
       LEM-A. BLOCKED: sigma_loc NOT_BUILT — LM-1 door standing
       "P_LOC_R5_COVARIANT_LOCAL_SYMBOL_MAP = NOT_BUILT / TYPE-U";
       its build owes the (S2-15)/(R4-11) interface list (topology on
       O_R5, field-symbol realization, embedding of the cycle bilocal,
       Ward/contact split, all squares, provenance). SW-T3: zero
       constructed-grade sigma_loc lines in either root. Authoring it
       here is fenced (proof_authorized = false) and barred (§4.1(i)).
S-iv   Trace pairing Tr(R_K^(-1)H)/Tr(R_K^(-1)L_T) ((S1-10)/(R4-10)).
       Note it is a DETECTOR (nonzero on Rhat_K), not a separator —
       and it is EXCLUDED: "not an admissible construction from the
       ratified stock" — missing operator ideal, trace, nonzero
       denominator, restriction/batching, Maxwell identification
       ((S1-9); 041498bb §5.5 "would silently author the missing
       map"). Also its L_T-side value needs MB-1. Dead twice over.
S-v    Covariance. BLOCKED IN PRINCIPLE: covariance is orbit-EQUALITY,
       not value ((S1-7); 041498bb §3.3 "A covariance theorem may
       identify equal values on orbit members; it does not make that
       common value zero"); the (S1-8) inference "invariant => zero"
       is defeated at bytes; BOTH objects are invariant. At rank-1
       anchor stages the admitted stabilizer acts trivially on
       quadratics — no leverage at all.
S-vi   Reality/parity grading. BLOCKED: both reality-compatible ((S2)
       checks; (R4-7) preserved under reversal); no sealed grading
       distinguishes them.
S-vii  Sector/Keldysh typing. BLOCKED: same block (§4.1(iii));
       "WITNESS_IS_FORCED_IN_KERNEL_P_LOC_BY_SECTOR = false / TYPE-R".
S-viii Units. BLOCKED: the sealed well-typedness of qtilde_N puts both
       summands in ONE space; no unit mismatch exists to exploit
       ((T2-1)/(T2-2) close the seam).
S-ix   Finite shadows / DoR-008. BLOCKED: both restrict to zero of
       record (§4.1(iii)); "DoR-008 … cannot select between
       assignments which the finite consumer never evaluates"
       ((S1-11)/§3.5; (R2-6)). Restriction data separates nothing.
S-x    Operator rank (D-RANK). THE ONE SEPARATOR SHAPE THAT WOULD BE
       CHEAP: Rhat_K,N is a sealed isomorphism; L_T,N singular would
       force E-1 in one line (T11). BLOCKED: no sealed byte types
       L_T,N's carrier rank (SW-T4: zero hits); "transverse" is a
       field-symbol-side word and the carrier-to-field seam is
       NOT_BUILT (LM-2; reading it carrier-side is attack 7's exact
       category error). The kill byte lives behind MB-1.
S-xi   Provenance. The provenances ARE sealed-distinct (DoR-019
       carrier geometry vs the Maxwell F^2 lineage; attack 2: "they
       live in different provenances and no local field-symbol arrow
       relates them"). BLOCKED AS A SEPARATOR: provenance is a
       formation-history predicate, not a linear functional; (LM1-10)
       DECLINES the inference in so many words ("does not assume they
       are independent"). Distinct formation does not display distinct
       directions.
E-1 ATTEMPT RESULT: NO sealed separator exists. Every candidate
terminates at MB-1, MB-2, or a byte that explicitly declines the
inference. E-1 NOT DERIVED.
```

### 4.3 Toward E-2 — the identity-chain hunt (each candidate, its exact blocker)

```text
C-i    The rank-1 Hom-collapse shortcut (the tempting one, displayed
       to be killed): "dim K_N = 1, so Hom(K_N,K_N^*) is 1-dim, so
       everything is proportional." REFUSED: O_R5,N is the completed
       retarded action-KERNEL class, NOT Hom(K_N,K_N^*) — its domain
       "explicitly includes nonlocal and higher-derivative
       structures, so it is not declared one-dimensional" (041498bb
       §3.1 at bytes; SW-T5 confirms no contrary declaration).
       Collapsing the kernel class onto its carrier Hom fiber is the
       same category error as attack 7 (carrier symbol read as
       spacetime symbol), run in reverse.
C-ii   Via the declared saddle decomposition (R1-1) H_R[G_K] = K L_T +
       declared higher/nonlocal structures. BLOCKED: a DECLARED shape
       on the candidate normalized saddle carrying the unknown
       coefficient K — no Rhat_K term, no sealed computation of the
       decomposition on any carrier, and the declared "higher/
       nonlocal" remainder is exactly the undeclared complement
       (S-ii). No chain link exists.
C-iii  Via the Schur/RetExtract chain (R1-3). BLOCKED: Response_m =
       RetExtract_m(H_KK - H_KC Inv_CC H_CK) is member-sensitive
       ((W1-3)); the ONLY displayed completed-fiber identities land on
       Rhat_K,N (§3.1 REACHABILITY), never on L_T,N. The chain reaches
       one side of the would-be identity only.
C-iv   Via (S4) kappa_T,N := Krawhat_N(L_T,N) = Sch_N(M_N^loc).
       BLOCKED: a [YOURS] required-contract line (specification of an
       unbuilt Loc package), landing in D_N^ker not O_R5,N; round 3's
       instantiation is an existence-quantifier witness coordinate,
       unadopted. No sealed inhabited map; no chain.
C-v    By functional values (any number). BLOCKED IN PRINCIPLE by
       LEM-B (T3a/T3b): identical admitted-value data (1, c != 0) is
       realized by an E-2 model AND an E-1 model — so no finite set
       of displayed functional values certifies proportionality
       without a TOTALITY certificate (a point-separating family on
       the closure of span{L_T,Rhat_K}: T4). Totality is TOP-2-grade
       — undisplayed of record (round 3 SW-5; the BANACH row is a
       requirement, not a certificate). So the functional route to
       E-2 consumes the SAME undisplayed topology bits B-1's arms
       already owe.
E-2 ATTEMPT RESULT: every identity chain terminates at MB-1 (no L_T
presentation to equate against) or at the LEM-B totality bar. a_K has
no display; E-2 NOT DERIVED. (Had any chain closed, the display would
have been: mixing line (-a_K,1,0); cycle-side value -(1/a_K)
Kern(R_K,N) != 0 exact (X-3); kill = NG-3' on the dim M_N >= 2 cell
only — shape held ready, never inhabited.)
```

---

## 5. TASK 3 — THE (S1-5) CLOSURE SHAPES, LINE BY LINE

Label reading (ledgered at CL-7): "(S1-5)" is the sealed three-line
clause at 70dde1c0 :244-252, closed by "No authority proves any line of
(S1-5)."; the commission's "S1-5 closure shapes" are those three lines.

```text
LINE 1  "Rhat_K = a_K L_T + R_perp with p_loc(R_perp) = 0".
  What it would close: R_perp = 0 certified => E-2 (with a_K's
  display); R_perp != 0 certified in O_prof => E-1 (the extent
  display's branch-(a) first shape).
  ATTEMPT: the SHAPE is algebraically VACUOUS for arbitrary readers —
  for EVERY fiber member (chi,T), Rhat_K = chi L_T + R_rem with
  p_(chi,T)(R_rem) = 0 holds tautologically ((LM2-17); battery T8) —
  and bae34116 :571-573 says so at bytes ("They do not prove that
  H_rem is a physical contact/nonlocal/higher term").
  EXACT BLOCKER: as a CLOSURE the line needs (a) a DERIVED p_loc
  member (existence underived — MB-2) and (b) the R_perp = 0 / != 0
  certificate — which IS the bit. Circular as a route; only its
  ratified form closes. UNPROVED, correctly flagged.
LINE 2  "Rhat_K lies in ker(sigma_loc)".
  What it would close: E-1 OUTRIGHT — sigma_loc(L_T)=1,
  sigma_loc(Rhat_K)=0, Rhat_K != 0 sealed => not proportional
  (LEM-A/T2). The strongest cheap closure shape on the menu; = D-A.
  ATTEMPT: cannot be run — the map does not exist.
  EXACT BLOCKER: sigma_loc NOT_BUILT ((R2-3) "The corpus has no map";
  LM-1 door TYPE-U; (S2-14) arrow + (S2-15) interface list absent;
  SW-T3 zero constructed lines). Building it is the named future
  work; doing so here is fenced and barred (§4.1(i)). UNPROVED.
LINE 3  "O_R5/span{L_T} has zero admitted covariant dual".
  What it would close: kills the countermodel family (S1-4) — p_loc
  unique-IF-existing; chi_K unique. FOR THE BIT its force is
  READING-DEPENDENT on the absent topology: WITH a TOP-2-grade
  separation theorem, zero quotient dual would collapse O_R5,N onto
  span{L_T,N} (E-2 for every element) — colliding with the declared
  nonlocal/higher domain typing (041498bb §3.1), so the line is
  either false or the domain typing collapses; WITHOUT TOP-2 the line
  decides NOTHING about the bit (no separation theorem converts
  dual-vanishing into membership; the extent pair's audited practice
  imports no Hahn-Banach onto O_R5,N and neither does this artifact).
  EXACT BLOCKER: no named dual class exists to quantify over — the
  topology obligation (Q-288 as carried; TOP-1/TOP-2 undisplayed,
  round 3 SW-4/SW-5) — the line is NOT STATABLE as a finite check
  today. UNPROVED, and the least-plausible closure of the three.
CROSS-MAP to the extent display's branch (a): its "certified
decomposition with R_perp != 0" = LINE 1's E-1 half; its "covariant
functional pair separating" is OVER-STRONG relative to LINE 2 — LEM-A
shows ONE functional suffices (D-ASYM). Consistent; sharpened.
```

---

## 6. TASK 4 — VERDICT, BLOCKER MAP, AND WHAT WOULD DECIDE IT

```text
VERDICT = UNDECIDABLE-TODAY (menu: E-1-DERIVED | E-2-DERIVED |
UNDECIDABLE-TODAY). Neither E-1 nor E-2 is derivable from the sealed
record at this cutoff; a_K has no display; no verdict of any consumed
artifact moves.

BLOCKER MAP onto the flags' own unproved list (041498bb §5.3, the
audit's C-1 citation honored; 70dde1c0 §8):
  flags' item                      terminal blocker(s) here
  "p_loc(R_K)=0"   unproved   <->  S-i/S-iii: no admitted evaluator
                                   (MB-2); sigma_loc unbuilt
  "p_loc(R_K)!=0"  unproved   <->  same absences, other sign; and by
                                   LEM-B a nonzero value alone would
                                   not even decide the bit
  "R_K=L_T"        unproved   <->  C-i..C-v: no identity chain; MB-1
                                   (no L_T presentation); LEM-B
                                   totality bar; assuming it BARRED
                                   (b53d9e93 §2.4)
  "R_K wholly in the nonlocal/
   higher-derivative complement"
                   unproved   <->  S-ii: REFUSED-TODAY as UNPOSABLE —
                                   the complement is not a sealed
                                   object ((R2-4) absent; separation
                                   obligated-to-be-derived)
  PLUS (not on the flags' list): MB-1 — the operand L_T,N itself has
  no sealed presentation ((R4-11) item 2 = required future data); and
  MB-2 — the evaluator's existence is underived (041498bb §6.3).
  Three grades of absence: values, evaluator, operand presentation.

THE NEW SEALED OBJECT THAT WOULD DECIDE IT (any one; at ONE anchor
stage per isometry class — X-4's collapse carried: N_n/N_m one bit of
record via the sealed S_nm isometry + the a-coordinate-preserving
ideal covariance (LM1-12)/(LM1-13); N_RL/M_RL conditional on u's
undisplayed isometry typing byte, fdf20bd4 :1257-1258):
  D-A  => E-1: one derived admitted covariant functional with
        displayed values (1 on L_T,N, 0 on Rhat_K,N) — (S1-5) line 2
        shape; LEM-A closes; round 3's biconditional then gives B-1
        INHABITED modulo TOP-1/TOP-2 and the dim M_N cell.
  D-B  => EITHER: the L_T,N presentation/embedding byte on the K/K
        class + exact comparison against the displayed Rhat_K,N;
        decisive both ways; if it shows L_T,N singular, E-1 follows
        in one line (D-RANK/T11).
  D-C  => E-2: a derived functional family + totality certificate
        (TOP-2-grade) + displayed common ratio a_K != 0; then the
        mixing relation (-a_K,1,0) with cycle-side value -(1/a_K)
        Kern(R_K,N) != 0 exact, killing BOTH families per NG-3' on
        the dim M_N >= 2 cell only (at undisplayed dim M_N the kill
        does not close; round 3 §5.2).
SHAPE-LEVEL AUDIT CRITERIA (for the registrar, from D-ASYM):
  - a closure artifact claiming E-2 from ONE reader value: REFUTABLE
    ON SHAPE (LEM-B countermodel pair, battery T3);
  - a closure artifact claiming E-1 must exhibit an ADMITTED derived
    functional, not an algebraic (LM2-4) fiber member (the fiber is
    a family, not a derivation; T8's vacuity);
  - chi_K displayed = 0 by a derived functional => E-1; chi_K
    displayed != 0 => the bit REMAINS OPEN (only a_K's conditional
    value moves).
WHAT THIS CHANGED ABOUT THE GAP: nothing moved on the sealed flags —
but the closure work now has exact PRICES: E-1 is one-functional
cheap (D-A) or one-presentation-byte cheap (D-B); E-2 is NEVER
functional-value cheap (LEM-B) — it costs presentation-grade (D-B) or
totality-grade (D-C) new structure, the latter overlapping the TOP-2
bit B-1 already owes. The decision surface is asymmetric, and the
asymmetry is now displayed and battery-verified.
```

---

## 7. CONSISTENCY

Nothing here binds a member, adopts anything, authors physics, selects
a route/branch/address/representative, or touches the held
construction. The E-1/E-2 displays are conditional shapes over the
sealed carriers; a_K is never valued; chi is never valued; no
candidate functional is authored; the sealed flags R_K_EQUALS_L_T,
P_LOC_RK, LOCAL_MAXWELL_SYMBOL, IDEAL_EXTENT are read and left exactly
where the record holds them. B-1 remains UNDECIDABLE-STILL of record;
NG-3/NG-3' remain exactly round 3's; SK-2 does not fire; ACT-3 is not
forced; the D-1 menu is unchanged; R9 and A8 remain UNEXECUTABLE/
PENDING. No score, flag, criterion, or gate moves.

## 8. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: roots per commission = the archive workspace +
      the cleanroom v003 root (NARROWER than the extent pair's five
      roots — commission-fixed), minus fenced filename classes
      (register|road_|ledger|lens|plan|tracker|THE_HANDOFF|
      continuation — none opened; filters case-insensitive). Sweeps
      SW-T1..SW-T6, cutoff 2026-08-15T07:03Z. A wider sweep changes
      this determination only by exhibiting a sealed R_K_EQUALS_L_T
      determination, a derived sigma_loc/p_loc member, an L_T,N
      presentation byte, a totality certificate, an exhibited mixing
      relation, or an isometry typing for u. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the extent pair (X-1..X-4 and their
      corollaries, incl. Rhat_K,N != 0) and the round-3 pair (anchor
      stages, NG-3', biconditional, TOP-1/TOP-2) consumed at audited
      grade; KER (B2-7), LOC-NAT (Q13), 13d227ce/d386bb74 lineage
      clauses consumed as quoted inside seal-verified stock; every
      chased carrier seal-verified at path before reliance (§1).
      PREMISE(named).
CL-3  TYPING-GRADE ACCEPTANCE: L_T,N != 0 at sealed-typing grade
      (extent CL-3 precedent, audit-passed) — spent ONLY where the
      sealed record already spends it (X-3's zero-value exclusion,
      reproduced at T5; the E-2 branch's forced-coefficient display)
      and NEVER as evidence on the bit. PREMISE(named).
CL-4  ANCHOR-STAGE READING: four rank-1 stages from round 3 §4/B-3
      census at audited grade; rank-1 display field-reading-invariant
      (dim S_(1,sa) = 1 both readings); no field pin adopted.
      Blockers cited are class-level (stage-uniform), so the
      determination is identical at every anchor stage; X-4
      de-duplicates future closure work to <= 2 instances.
      PREMISE(named).
CL-5  EXTERNAL-MATH IMPORTS: finite-dimensional linear algebra only
      (kernels on dim-3 domains, 2x2 determinants, Gram pairs) —
      exact, battery-verified. NO topology, NO wavefront, NO
      Hahn-Banach import onto O_R5,N anywhere: LEM-B's totality
      requirement is displayed as a SHAPE (verified on finite-dim
      models), never applied as a theorem on the completed class;
      §5 LINE 3's TOP-2 conditional is displayed, not imported.
      PREMISE(named).
CL-6  THE S9AD-PATH FILE (D-1): existence disclosed at metadata grade
      (basename, sizes, mtimes); content, sidecar, and hash NOT
      consumed, per the T1SR fence "do not search for or consume any
      RK_LT artifact". It surfaced as a BASENAME inside commissioned
      sweeps (not searched for); the commission's zero-bytes premise
      is contradicted at path and flagged to the registrar. Nothing
      below depends on that file either way. PREMISE(named).
CL-7  LABEL READING: "S1-5 closure shapes" read as the three lines of
      the sealed clause (S1-5) at 70dde1c0 :244-252 (the reading the
      extent pair already used: "every closure shape (S1-5)"); walked
      line-by-line at §5. PREMISE(named).
CL-8  DISCLOSURE: sweep listings displayed fenced-class BASENAMES;
      no fenced content opened, read, or used; the SEED_ADJUDICATION
      hit consumed at match-line (quotation) grade only. The battery
      venv is fresh (scratchpad; sympy 1.14.0); all claims are
      exact-symbolic and version-insensitive. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The battery's model targets (R^1 and R^2 abstract codomains, a
  generic symmetric 2x2 G, 1-dim carrier symbols rho/r/t, symbols
  a_K/chi/tau/lambda/c_c never evaluated) are verification
  instruments ONLY: they check that the displayed kernels, the forced
  coefficient, the LEM-A certificate, the LEM-B countermodel pair,
  the totality condition, the reader-fiber dimensions, and the
  invertibility transport are FORCED by the stated hypotheses. The
  LEM-B pair in particular DECIDES NOTHING about the sealed carriers
  — it refutes a proof SHAPE (single-value certification of E-2),
  which is a statement about certificates, not about O_R5,N. No toy
  value, stage, address, dimension, or number enters any conclusion,
  the verdict, or the missing-object list. No float; no value frozen;
  a_K and chi_K never valued.
```

## FLAG BLOCK

```text
THE_BIT = UNDECIDABLE-TODAY ; Rhat_K,N in R L_T,N inside O_R5,N has
  NO derivation either way at cutoff 2026-08-15T07:03Z
R_K_EQUALS_L_T = NO_VERDICT / TYPE-U — UNTOUCHED (70dde1c0 §8;
  041498bb §3.4); both directions still unproved at bytes (§5.3 list)
E1_ATTEMPT = NOT DERIVED ; separator hunt S-i..S-xi each blocked at a
  named sealed absence: no admitted evaluator (MB-2), no locality
  grading ((R2-3)/(R2-4) absent), trace surrogate excluded, covariance
  orbit-equality only, shared sector/units/reality/shadows, no L_T
  rank byte (SW-T4), provenance-distinctness declined as separator
  ((LM1-10))
E2_ATTEMPT = NOT DERIVED ; identity chains C-i..C-v each blocked: the
  rank-1 Hom-collapse REFUSED (class not declared one-dimensional,
  041498bb §3.1), (R1-1) declared-shape only, Schur chain reaches
  Rhat_K only, (S4) is a [YOURS] contract line, functional-value route
  barred in principle by LEM-B ; a_K NOT displayed
COMPLEMENT_PLACEMENT = REFUSED-TODAY as UNPOSABLE ; "R_K wholly in
  the nonlocal/higher-derivative complement" has no truth conditions
  until (R2-4)/(S2-14) exist — neither proved nor refuted
S1_5_WALK = line 1 CIRCULAR-AS-ROUTE (vacuous per (LM2-17)/T8; needs
  MB-2 + the bit itself) ; line 2 = D-A shape, blocked at sigma_loc
  NOT_BUILT ; line 3 NOT STATABLE today (no named dual class; force
  reading-dependent on absent TOP-2) ; all three stand UNPROVED
  exactly as flagged
DECISION_ASYMMETRY = displayed + battery-verified: E-1 is
  one-functional certifiable (LEM-A; extent branch-(a) "pair"
  sharpened to ONE) ; E-2 is NEVER functional-value certifiable
  (LEM-B) — costs presentation-grade (D-B) or totality-grade (D-C)
  structure ; chi_K=0 derived => E-1 ; chi_K!=0 displayed => bit open
READER_FIBER_EQUIVALENT = at rank-1 anchors, E-1 <=> fiber line
  (chi free) ; E-2 <=> fiber point (chi FORCED = a_K, tau = a_K/rho)
  (T9/T10) ; downstream display only — readers cannot fix Rel_N
MISSING_CLOSURE_OBJECTS = D-A (=> E-1, cheapest) ; D-B (=> either,
  the L_T,N presentation byte, (R4-11) item 2) ; D-C (=> E-2,
  functional family + TOP-2-grade totality + displayed a_K) ; one
  per isometry class suffices (X-4 carried; u-pair conditional)
S9AD_PATH_FILE = EXISTS at the dead path (41,273 B + sidecar,
  06:50-06:51Z — postdates the extent pair's cutoffs, predates mine);
  content UNCONSUMED by commission fence; zero-bytes premise
  CONTRADICTED AT PATH; registrar adjudication suggested (D-1)
PIN2H_HANDLING = every display identical under R-A and R-B
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
SEALS = commission 5/5 + chased 7/7 MATCH ; MISMATCHES = 0 ; output
  path first-use, probed ABSENT twice (07:04:24Z final)
SWEEPS = SW-T1..SW-T6 ; CUTOFF 2026-08-15T07:03Z ; exhaustiveness
  sweep-displayed only ; the ONE post-extent landing is the fenced
  S9AD-path file ; no competing T1SR artifact
BATTERY = fresh venv, sympy 1.14.0, 16/16 PASS, exact symbolic only,
  no float, no value frozen, source VERBATIM in Appendix A (C-3
  standing repair honored) ; NUMERIC_EVALUATION = exact integers/
  rationals/symbols only ; MEASURED_CONSTANT_COMPARISON = none
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no functional authored, no common cell, no
  reader, no fixed point, no end test) ; MEMBER_BOUND = false ;
  CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a)
R9 = UNEXECUTABLE/PENDING ; A8 = UNEXECUTABLE/PENDING
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the bit stands exactly where the flags hold it — but its
  closure now has a priced, asymmetric decision surface: one derived
  functional value (zero) or one presentation byte decides E-1; E-2
  cannot be bought with functional values at all and must pay for
  the L_T presentation or a totality certificate. Three grades of
  absence (values, evaluator, operand presentation) are named at
  bytes, mapped onto the flags' own unproved list, and handed to the
  registrar with shape-level audit criteria for any future closure
  artifact. Nothing moved; the gates are where the principal left
  them.
```

---

## APPENDIX A — BATTERY SOURCE (VERBATIM) AND OUTPUT

Fresh venv: `python3 -m venv venv; pip install sympy` (sympy 1.14.0),
scratchpad-local, commission-distinct. Run: `venv/bin/python
rkb_battery.py`. Output: `16/16 PASS` (each check line printed PASS;
no float; no value frozen).

```python
# RKB-BUILD battery — commission T1SR — exact symbolic only (sympy).
# Model targets are verification instruments ONLY (TOY_SEPARATION): they check
# that displayed kernels/certificates are FORCED by stated hypotheses. No toy
# value enters any conclusion about the sealed carriers. No float. No value frozen.
import sympy as sp

PASS = []
def check(name, cond):
    assert cond, name
    PASS.append(name)

a, b, c, aK, chi, tau, rho, r, t, lam, cc = sp.symbols(
    'a b c a_K chi tau rho r t lambda c_c', real=True)

# ---- T1/T12: two-entry extent menu at a rank-1 anchor stage (dim E_N = 3) ----
# Independent model (E-1): codomain R^2, LT=(1,0), Rhat=(0,1).
LT1, Rh1 = sp.Matrix([1, 0]), sp.Matrix([0, 1])
q1 = a*LT1 + (b + c)*Rh1
sol1 = sp.solve([sp.Eq(q1[0], 0), sp.Eq(q1[1], 0)], [a, b, c], dict=True)[0]
# kernel: a=0, b=-c -> span{(0,1,-1)}: dim 1
check("T1a E-1 kernel = span{(0,1,-R_K-dir)} dim 1",
      sol1[a] == 0 and sol1[b] == -c)
# Dependent model (E-2): codomain R^1, Rhat = a_K*LT, a_K != 0 symbolic.
q2 = a*1 + (b + c)*aK
sol2 = sp.solve(sp.Eq(q2, 0), a)[0]           # a = -a_K(b+c): dim 2
check("T1b E-2 kernel dim 2", sp.simplify(sol2 + aK*(b + c)) == 0)
v_mix = {a: -aK, b: 1, c: 0}                  # the mixing line (-a_K,1,0)
check("T1c mixing line (-a_K,1,0) in E-2 kernel", q2.subs(v_mix) == 0)
check("T1d mixing line NOT in E-1 kernel",
      sp.simplify((a*LT1 + (b+c)*Rh1).subs(v_mix)) != sp.Matrix([0, 0]))
# forced coefficient: kernel element with a!=0 forces Rhat=-a/(b+c) LT; b+c=0 => LT=0
bc = sp.symbols('bc', real=True, nonzero=True)
check("T12 rank-nullity menu {1,2}: forced a_K = -a/(b+c)",
      sp.solve(sp.Eq(a + bc*aK, 0), aK)[0] == -a/bc)

# ---- T2 (LEM-A): ONE functional certifies E-1 ----
# ell(LT)=1, ell(Rhat)=0, Rhat!=0 sealed  =>  Rhat = lam*LT impossible:
lam_forced = sp.solve(sp.Eq(0, lam*1), lam)[0]   # apply ell to Rhat=lam LT
check("T2 LEM-A: ell(L_T)=1, ell(Rhat)=0 forces lam=0 => contradicts Rhat!=0",
      lam_forced == 0)

# ---- T3 (LEM-B): one functional with NONZERO value decides NOTHING ----
# Same functional data (ell(LT), ell(Rhat)) = (1, c_c), c_c != 0, both ways:
# M1 (E-2 true): V=R^1, LT=1, Rhat=c_c, ell=id.
check("T3a M1 proportional (E-2) with ell-data (1,c_c)",
      sp.simplify(cc - cc*1) == 0)
# M2 (E-1 true): V=R^2, LT=(1,0), Rhat=(c_c,1), ell=proj_1.
M2 = sp.Matrix([[1, cc], [0, 1]])
check("T3b M2 independent (E-1) with SAME ell-data (1,c_c): det != 0",
      sp.simplify(M2.det()) == 1)

# ---- T4: totality — E-2-by-functionals needs a point-separating family ----
# In M2 the pair (proj_1, proj_2) separates: Gram det = 1 != 0 certifies E-1.
# In M1 (1-dim) EVERY functional is a multiple of ell: all 2x2 dets vanish.
m1_pair_det = sp.det(sp.Matrix([[1, cc], [lam*1, lam*cc]]))
check("T4 in M1 every functional pair has det 0 (no false E-1); "
      "separating pair in M2 has det 1", sp.simplify(m1_pair_det) == 0)
# Common-ratio + separating family forces E-2: if ell_i(Rhat)=a_K ell_i(LT) for a
# separating family, then Rhat - a_K LT is annihilated by all => = 0. In M2 the
# family {proj_1, proj_2} has NO common ratio (c_c/1 vs 1/0) — consistent.

# ---- T5 (X-3 reproduction): zero-value mixing excluded at typing grade ----
# rank 1: C = c*R_K; value = Kern((b+c)/a * R_K); faithfulness: value=0 <=> b+c=0
# then qtilde = a*LT = 0 with a != 0 => LT = 0 against sealed typing.
az = sp.symbols('az', real=True, nonzero=True)
LTsym = sp.symbols('LTsym', real=True)
sol5 = sp.solve([sp.Eq(b + c, 0), sp.Eq(az*LTsym + (b + c)*aK, 0)],
                [b, LTsym], dict=True)[0]
check("T5 zero cycle-side value forces L_T=0 (excluded by typing)",
      sol5[LTsym] == 0)

# ---- T6: orbit identity sum_j (G u_j)(G u_j)^T = G, choice-invariant ----
g11, g12, g22 = sp.symbols('g11 g12 g22', real=True, positive=True)
G = sp.Matrix([[g11, g12], [g12, g22]])       # generic symmetric (SPD assumed)
e1, e2 = sp.Matrix([1, 0]), sp.Matrix([0, 1])
def gdot(x, y): return (x.T*G*y)[0, 0]
u1 = e1/sp.sqrt(gdot(e1, e1))
w2 = e2 - gdot(u1, e2)*u1
u2 = w2/sp.sqrt(gdot(w2, w2))
S = (G*u1)*(G*u1).T + (G*u2)*(G*u2).T
check("T6 (R4-3)/(LM2-13): P_x1+P_x2 = r^2 R_K choice-invariant (r=1 frame)",
      sp.simplify(S - G) == sp.zeros(2, 2))

# ---- T7: rank-1 fiber collapse P_x = s R_K  ((S3-5)/(LM2-9)) ----
Px = (rho*r)*(rho*r)          # x^flat tensor x^flat at 1-dim, g=rho, x=r
s = rho*r**2                  # s = g(x,x)
check("T7 P_x = s R_K at rank 1", sp.simplify(Px - s*rho) == 0)

# ---- T8: (LM2-17) tautology — line-1 SHAPE holds for EVERY reader ----
# reader p(a,b,c) = a + chi*b + tau*rho*c ; R_rem := Rhat - chi*LT = (-chi,1,0)
p = lambda A, B, Cc: A + chi*B + tau*rho*Cc
check("T8 p(R_rem)=0 for EVERY (chi,tau): decomposition shape is vacuous",
      sp.simplify(p(-chi, 1, 0)) == 0)

# ---- T9/T10: reader-fiber collapse — THE BIT downstream-equivalent ----
# E-1 relations {(0,1,-1)}: constraint chi = tau*rho — dim 1 (tau free).
sol9a = sp.solve(sp.Eq(p(0, 1, -1), 0), chi)
check("T9a E-1 fiber: chi = tau*rho, dim 1", sol9a == [tau*rho])
# E-2 adds (-a_K,1,0): chi = a_K AND tau = a_K/rho — dim 0 (unique reader).
sol9b = sp.solve([sp.Eq(p(0, 1, -1), 0), sp.Eq(p(-aK, 1, 0), 0)],
                 [chi, tau], dict=True)[0]
check("T10 E-2 fiber: chi FORCED = a_K, tau = a_K/rho, dim 0",
      sol9b[chi] == aK and sp.simplify(sol9b[tau] - aK/rho) == 0)

# ---- T11: invertibility transport — a singular L_T byte would force E-1 ----
LTm = sp.Matrix([[g11, g12], [g12, g22]])     # if Rhat = a_K*LTm invertible:
det_rel = sp.simplify(sp.det(aK*LTm) - aK**2*sp.det(LTm))
check("T11 det(Rhat)=a_K^2 det(L_T): Rhat iso + E-2 => L_T iso; "
      "contrapositive: L_T singular => E-1", det_rel == 0)

print(f"{len(PASS)}/16 PASS")
for nm in PASS:
    print("  PASS:", nm)
```

```text
OUTPUT (verbatim):
16/16 PASS
  PASS: T1a E-1 kernel = span{(0,1,-R_K-dir)} dim 1
  PASS: T1b E-2 kernel dim 2
  PASS: T1c mixing line (-a_K,1,0) in E-2 kernel
  PASS: T1d mixing line NOT in E-1 kernel
  PASS: T12 rank-nullity menu {1,2}: forced a_K = -a/(b+c)
  PASS: T2 LEM-A: ell(L_T)=1, ell(Rhat)=0 forces lam=0 => contradicts Rhat!=0
  PASS: T3a M1 proportional (E-2) with ell-data (1,c_c)
  PASS: T3b M2 independent (E-1) with SAME ell-data (1,c_c): det != 0
  PASS: T4 in M1 every functional pair has det 0 (no false E-1); separating pair in M2 has det 1
  PASS: T5 zero cycle-side value forces L_T=0 (excluded by typing)
  PASS: T6 (R4-3)/(LM2-13): P_x1+P_x2 = r^2 R_K choice-invariant (r=1 frame)
  PASS: T7 P_x = s R_K at rank 1
  PASS: T8 p(R_rem)=0 for EVERY (chi,tau): decomposition shape is vacuous
  PASS: T9a E-1 fiber: chi = tau*rho, dim 1
  PASS: T10 E-2 fiber: chi FORCED = a_K, tau = a_K/rho, dim 0
  PASS: T11 det(Rhat)=a_K^2 det(L_T): Rhat iso + E-2 => L_T iso; contrapositive: L_T singular => E-1
```
