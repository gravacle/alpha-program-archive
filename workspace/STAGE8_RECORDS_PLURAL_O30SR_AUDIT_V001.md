# STAGE 8 — RECORDS-AUDIT OF THE PLURAL SWEEP
# COMMISSION O30SR — DEFAULT-REFUTE — 2026-08-15

DETERMINATION ONLY. Nothing proposed, authored, adopted. No relation among
records constructed. No missing side supplied. Testimony given zero weight;
every claim re-derived at bytes.
FENCES HELD THROUGHOUT: alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false.

---

## §0 — STEP 0, TARGET AND SEALS

```text
TARGET   STAGE8_RECORDS_PLURAL_O30SR_V001.md          57488 bytes
SEAL     shasum -a 256 -c, run FROM THE ARTIFACT'S OWN DIRECTORY:
         STAGE8_RECORDS_PLURAL_O30SR_V001.md: OK
OUTPUT   STAGE8_RECORDS_PLURAL_O30SR_AUDIT_V001.md — probed ABSENT before write.
```

### §0.1 SEALS VERIFIED THIS AUDIT (own directory, both sidecar conventions probed)

```text
OK       STAGE8_RECORDS_PLURAL_O30SR_V001.md
OK       COMPLETE_QSPEC_OPEN_RECORD_BLOCK_TRANSFER_MAP_SPEC_V001.md
OK       PRIMITIVE_SOURCE_RECORD_PAIRED_RETURN_IDENTIFICATION_PRINCIPLE_V002.md
OK       STAGE8_GLUING_CANDIDATE_O23SR_V001.md
OK       STAGE8_REQUIRE_BOUNDARY_CHECK_V001.md
OK       R3_4_SHARED_SOURCE_CAUSAL_PARENT_SPEC_V001.md
NO SEAL  BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md
NO SEAL  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md
NO SEAL  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V001,V002,V008,V009,V010,V011
         (build's claim of no sidecar on V011: CONFIRMED; extended by me to all six)
```

### §0.2 CUTOFF AND ROOTS, RE-MEASURED

```text
ROOT1  /Users/bgm/MB Work/alpha-program-archive/workspace
       6864 files now / 1922 *.md  (build recorded 6863 at its cutoff; delta =
       objects written after it, incl. its own two)
ROOT2  .../alpha_fundamental_record_action_cleanroom_v003
       7805 files / 2308 *.md — EXACT match to the build.
CUTOFF Build states newest object STAGE8_REFINEMENT_AFTER_JOIN_O29SR_V001.md at
       "Aug 15 17:10"; `stat` reports 17:11. One-minute discrepancy, immaterial.
AUDIT SWEEP CUTOFF  2026-08-15 17:25 (mtime of the target artifact).
```

### §0.3 EVERY AUDIT SWEEP PATTERN, DECLARED VERBATIM

Run with `/usr/bin/grep -InE` over an explicit file list per root, include set
`*.md *.json *.txt *.py`, basename exclusion `(REGISTER|TRACKER|ROAD|PLAN|
CONTINUATION)` applied at the path filter. No register/tracker/road/plan/
continuation file was opened.

```text
A1  \brecords\b
A2  \b(two|three|four|five|many|several|multiple|all|both|other|distinct|
    different|separate|numerous|various|these|those|pair of|set of|family of|
    collection of|list of|sequence of|ensemble of)( +[a-z-]+)? +records\b   (= build P2b)
A3  (many|multi|two|inter|record|cross|[0-9]+|N)[-_ ]record | record[-_]pair
    | per[-_]record                                                        (= build P3)
A4  (neighbou?r|adjacen|abut|contiguous|next to|side by side|touching|
    incident to|common face|shared face|shared edge|shared vertex|shared cell|
    shares? a boundary|share the boundary|interface between)
A5  (each record|every record|any two record|either record|both record|
    per pair|record_[ij1-9]|R_[ij] +(and|tensor)|records? +i +and +j|
    the other record|another record|second record|first record|
    neighbou?ring record|record complex|record lattice|record network|
    record graph|record chain|record tree|record poset)
A6  (\b(two|three|four|five|several|many|multiple|distinct|different|separate|
    all|both|pair of|set of|family of|index set of|ensemble of|sequence of|
    list of)( +[a-z-]+){0,2} +records?\b | many[-_ ]record | multi[-_]record |
    two[-_ ]record | inter[-_]record | record[-_]record | cross[-_]record |
    record[-_]pair | records? +\{ | \{ *R_[0-9ijn] | per[-_]record)
    — then INTERSECTED with the set of files carrying a seal sidecar.
A7  (other side|far side|opposite side|across the boundary|separates? the|
    divides? the|partitions? the|on either side|both sides of|two sides of|
    inside and outside|interior and exterior)
A8  (outside the (record )?cell|exterior of the (record )?cell|beyond the cell|
    complement of the (ball|cell|diamond)|the cell.s exterior|
    external to the (record|cell))                                         (= build B3)
A9  (exterior|outside|complement)[^.]{0,40}(of|to)[^.]{0,30}(cell|record|ball|
    diamond)[^.]{0,60}(is|are|consists|contains|carries|holds)
A10 (without loss of generality|\bWLOG\b|for simplicity|simplifying assumption|
    idealis|idealiz)                                                       (= build D3)
A11 differs in the two-record sector          (exact-string attribution test)
A12 (K_square|K_L\b) ; flux datum ; NOTHING SEALED EXISTS ; inter-cell incidence
A13 build-internal: lens ; (137|0\.00729|7\.29|1/137|6\.674|G_N *=|alpha *= *[0-9]) ;
    (I propose|I adopt|I construct|we propose|we adopt|is hereby|is adopted|
    shall be) ; same (object|shape)
```

### §0.4 A DEFECT IN MY OWN FIRST SWEEP, DISCLOSED

My first A4 run returned 0 on both roots. The zero was an artefact of MY
harness: the corpus paths contain a space (`/Users/bgm/MB Work/...`) and my
unquoted file-list expansion split them, so grep read no files. Detected by
control, corrected to a NUL-delimited list, and recorded here rather than
repaired silently — the same discipline this audit demands of the build.

---

## §1 — HUNT (A): A PLURALITY MISSED?  *** NO. VERDICT NOT INVERTED. ***

I ran A2-A6 uncapped over both roots with wider patterns than the build's,
including plural forms, family and index-set constructions, and the full
adjacency/neighbouring vocabulary the build never swept (A4). A6 returned 740
files, of which 386 carry a seal sidecar; I walked the sealed set against the
build's G-list. Candidates that looked like inversions and what they are at
their own sentence:

```text
CAND-1  CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001.md:42-43
        "Their distinct limiting expectations separate the two asymptotic public
         record sectors."
        NOT AN INVERSION. Two SECTORS (the +/-1 public labels — cf.
        FORK_8_CAUSAL_DIRECT_LIMIT_PROMOTION_RESULT_V001:116 "The limiting
        expectations `+1` and `-1` separate the public labels") of ONE record.
        Excluded by the build's own §1.3 subpart rule, correctly.

CAND-2  BID_FIRST_OPENING_GRAPH_REFINEMENT_QUOTIENT_V001.md:12
        "Consider a connected oriented source-record graph with:" — followed at
        :13-15 by "one unresolved root; one public first-record endpoint; no
        other public vertex labels".
        NOT AN INVERSION. A graph with exactly ONE public record endpoint; the
        vertices are transitions, not records. Also UNSEALED (probed both
        conventions). A plurality of vertices, not of records.

CAND-3  BID_GLOBAL_CAR_RECORD_COMPOSITION_DERIVATION_V001.md:86,92
        "Give every record operator trivial fermion grading." / "it forbids
        copying the same source modes independently into every record factor."
        GENUINE plural record factors, but UNSEALED (no sidecar either
        convention). Adds a third file to the build's "at least TWO more"
        unsealed class — the build's hedge holds.

CAND-4  CHAIN_CHOICE_AUDIT_DRAFT_AUDIT_V001.md:242 "NO object of record on
        either side" — the bookkeeping idiom "of record". Sense-B. Not a plural.
```

*** NO SEALED SENSE-A MULTI-RECORD STATEMENT WAS MISSED THAT INVERTS THE
    VERDICT. Deliverable 1's finding — the corpus holds more than one record,
    in sealed bytes — is CONFIRMED, and my sweeps independently STRENGTHEN it:
    see §1.1. ***

### §1.1 SEALED PLURAL MATERIAL THE BUILD DID NOT DISPLAY (strengthens D1)

```text
R3_4_CAUSAL_SHARED_SOURCE_MOLLER_DURABILITY_SPEC_V001.md:45 (seal OK)
   "distinct record factors `R_j`, use the ordinary local Hamiltonian"
   An INDEXED FAMILY of distinct record factors, sealed. Not in the build's list.

R3_4_SHARED_SOURCE_CAUSAL_PARENT_SPEC_V001.md (seal OK), required-ingredient
   list :12-13, verbatim:
     "overlapping source-incidence projectors;
      distinct even record factors;"
   and its obligation 4, :100:
     "Prove for every earlier record observable `O_j` and every later primitive"
   An EARLIER/LATER ordering across records, indexed by j. Sealed.
```

These do not change the build's verdict; they make it stronger than the build
stated. Displayed only — nothing is constructed from them here.

---

## §2 — HUNT (B): A PLURALITY MANUFACTURED?  ONE CONTESTED OF SIX.

Each claimed hit checked against its own sentence at bytes.

```text
G-1  CONFIRMED GENUINE.  COMPLETE_QSPEC_OPEN_RECORD_BLOCK_TRANSFER_MAP_SPEC_V001
     (seal OK).  ":16-17" at bytes: "At event boundary `e`, let `A_e` be the
     ordered set of records that have opened and have not passed their last
     incident unitary."  Verbatim exact.  An ordered index set with per-record
     factors H_r.  Not stages, not versions, not subparts.
G-2  CONFIRMED GENUINE.  :124-125 verbatim exact.  Quantifier over records with
     an indexed family of carriers.
G-3  CONFIRMED GENUINE.  Three inputs, non-identified.  The build's care in
     using the SEALED quoter rather than the unsealed V011 is correct: no
     BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE file at any version carries a
     sidecar (I probed V001,V002,V008,V009,V010,V011 in both roots).
G-4  CONFIRMED GENUINE.  :285 verbatim exact, quoted to the sentence's period.
G-6  CONFIRMED GENUINE as a two-record occupation sector (but see COR-2 for the
     sentence the build misattributes to it).
G-7, G-8  CONFIRMED UNSEALED.  Correctly quoted with status displayed.

G-5  *** CONTESTED — THE ONE POSSIBLE MANUFACTURE. ***
     STAGE8_PARENT_NORMALIZATION_..._EINSTEIN_V001.md:243 (seal OK), at bytes:
       "it is a **weighted least-squares estimator over a family of records**,
        recovered *"operationally and emergently"*."
     and :244 verbatim: "**An estimator measures; it does not derive.**"
     The build's provenance note is HONEST and accurate — it discloses the
     external source and the negative verdict.  But the SENSE is contestable:
     an estimator that recovers `G_N` "operationally and emergently" ranges over
     MEASUREMENT records, which is the build's own Sense-B, not the physical
     durable public record.  The build classes it Sense-A.
     BEARING: the headline count SIX is better stated FIVE FIRM + ONE CONTESTED.
     The build already hedges ("the weakest of the six and the verdict does not
     rest on it"), so nothing downstream breaks; but the flag
     `sealed_statements_with_two_or_more_records_count = 6` is soft by one.
```

The build's §1.3 exclusions (two-record-CELL as subparts; V001/V002/V003 chains
as document versions; the chronological construction as stages) were re-checked
and are correctly applied.

---

## §3 — HUNT (D): THE IDEALISATION CALL — BUILD SURVIVES INTACT

```text
A10 (= build D3) ROOT1 : 62 lines.  *** EXACT REPRODUCTION of the build's 62. ***
A10 ROOT2 : 14 lines.
```

I then read every A10 line that also carries record- or cell-count vocabulary.
They concern the EQUAL-TIME idealisation, the MULTIPLICATION-OPERATOR
idealisation, and "an idealization THE RECORD DOES [not require]" — where "the
record" is the bookkeeping sense. None attaches idealisation to how many
records are in play.

*** THE BUILD'S §4.1 ABSENCE CLAIM SURVIVES MY OWN SEARCH. *** The declaration
hunted for is not merely unlocated — it is absent, and the build's positive
finding (many-record scope declared, one-record level declared a RESTRICTION) is
independently CONFIRMED at S-1..S-5's bytes. Hunt (D) finds no defect.

---

## §4 — HUNT (E): CONSEQUENCE OVERREACH — NONE FOUND

Every §5 row carries an explicit "DISPLAYED, NOT CLAIMED" or "DISPLAYED AS AN
EXACT NON-CONTACT" marker. No row asserts a failure point or demand dissolved,
weakened, relocated, or discharged. §11 states "NOTHING IS DISSOLVED." The W-2
row is the closest approach — it observes that the missing bridge and the
corpus's one unsealed adjacency object have the same SHAPE — and it disclaims
identity in terms: "THIS COMMISSION DOES NOT CLAIM THEY ARE THE SAME OBJECT."
That disclaimer is adequate. Hunt (E) is clean.

---

## §5 — HUNT (G) AND (H): CLEAN

```text
Lens tokens in the build                          : 0 occurrences.
Numerals of program import                        : none.  The only regex hit was
  the line-range "124-137", not a constant.  No value, no measured-constant
  comparison, no alpha-like numeral anywhere in the artifact.
Proposal / adoption / construction verbs          : none.
Constructed relation among records                : none.  The single
  "same object" occurrence is the disclaimer at :1054.
```

---

## §6 — THE CORRECTIONS

### COR-1 — §1.2's DISCLOSED-DEFECT MECHANISM IS ITSELF WRONG

The build writes, of its withdrawn P2: "Under BSD `grep -E` on this platform an
empty alternative **silently** kills the branch, and P2 returned 0 on both
roots."

At bytes on this platform, an empty alternative does not fail silently. Both
available engines reject the pattern loudly:

```text
/usr/bin/grep -rInE '<P2 as declared>' <ROOT1> --include='*.md'
  -> grep: empty (sub)expression        (no match output at all)
ugrep (the shell's `grep`)
  -> ugrep: error: error at position 42 ... \___empty (sub)expression
```

A zero is recorded only if stderr is discarded (`2>/dev/null | wc -l`). The
build's CONCLUSION is correct and its withdrawal of P2 is the right call — the
zero was an artefact of its pattern, not a property of the corpus. But the
mechanism it narrates did not occur, and a build that discloses a defect must
get the defect right.

### COR-2 — AN UNSEALED SENTENCE PRESENTED AS SEALED  *** HUNT (F) ***

Build §2, R-6 STATUS, verbatim: `Its own sealed sentence, G-6: "differs in the
two-record sector."`

Exact-string sweep A11 over both roots: the string "differs in the two-record
sector" occurs in exactly one file —

```text
BID_MANY_RECORD_PARENT_IDENTIFIABILITY_GATE_V001.md:67
  "and differs in the two-record sector."
```

— which is the file the build itself declares carries NO seal sidecar (its G-7
and R-7). The string occurs in NEITHER of G-6's sealed carriers. What those two
sealed files actually say is:

```text
STAGE8_T7_PRIMITIVE_CONNECTED_SCALARIZATION_DICHOTOMY_SPEC_V001.md:52 (seal OK)
  "two-record action is not."
STAGE8_T7_BLOCKER_RETURN_TO_FABLE_V001.md:62 (seal OK)
  "vacuum/one-record restrictions, different two-record dynamics."
```

An unsealed sentence has been labelled "its own sealed sentence" and attributed
to a sealed hit, and the mislabel runs in the build's favour. R-6's SUBSTANCE
survives on the genuinely sealed quotes above; the attribution does not.

### COR-3 — A REFUTED SENTENCE DISPLAYED WITHOUT ITS STATUS  *** HUNT (C) ***

Build §3.5 closes by offering "one adjacent statement of what the exterior IS",
quoting STAGE8_REQUIRE_BUILD_BOUNDARY_SPLIT_V001.md (seal OK) :95-98. The
quotation is verbatim-exact at :97-98. But the corpus does not leave that
sentence standing. STAGE8_REQUIRE_BOUNDARY_CHECK_V001.md — **seal OK, verified
from its own directory** — names that exact sentence and kills it, :95-97
verbatim:

```text
"The load-bearing hidden premise — build §2 :97-99, "the exterior/boundary of
 the cell IS a flux datum" — is false: the exterior determination is
 R_record,L(Q_flux·,Q_flux·), whose VALUE consumes the background assignment
 (S1 §2.4 flag 2), not merely the flux."
```

and at :99 verbatim: `**Verdict on CLAIM 1: REFUTED.**`

The build displays this quotation with no seal status and no refuted status,
breaking the very discipline it announces at §1.4 ("Seal status matters more
here than anywhere else"). Its §3 finding — the second side is named everywhere
and constructed nowhere — does not depend on this sentence and survives. The
sentence should not have been offered as corroboration.

### COR-4 — QUOTATIONS THAT STOP INSIDE THEIR OWN CITED SPAN  *** HUNT (F) ***

Four instances. In each the cited span extends past where the quotation stops.

```text
(a) §3.2.  Cited ":44-47", introduced as "quoted whole to the end of its
    sentence".  The build stops after sentence one.  :46-47 continues, verbatim:
      "No independent source-mass field or post-closure mass insertion exists."
(b) §2 R-3.  The build renders:
      "K = disjoint_union_i K_i, H(K) = tensor_i H(K_i)" — composition is
       defined of record ONLY for disjoint components.  A disjoint union glues
       nothing."
    STAGE8_GLUED_TOPOLOGY_HUNT_V001.md:172 (seal OK) at bytes does not end
    there.  It reads:
      "A disjoint union glues nothing: H^1(disjoint union) = direct sum of the
       parts'..."
    The build closed with a period where the source has a COLON introducing the
    cohomological content that JUSTIFIES "glues nothing".  The rendering also
    carries an unbalanced trailing quotation mark.
(c) §1.4 G-2.  Cited ":127-130"; the build quotes the H, r and U lines and
    omits the fourth line of the span, "A_(disjoint c)=product_c A_c,".
(d) §3.5 ANSWER 2.  Cited ":172-173"; the quotation stops at "the complete
    interface of the one cell", where the source sentence continues ", and
    these four are its entire derived subset."
```

None of these four reverses a finding. (b) is the most serious: material was
dropped from inside a quoted span and the span was closed as a sentence.

### COR-5 — AN ABSENCE STATED WIDER THAN THE BYTES ALLOW  *** HUNT (C) ***

Build §2.1 row: `gluing of records   ABSENT.`
Build §11 verdict: "Gluing of records into a complex is absent, exactly as the
CENSUS records."

The CENSUS sentence the build quotes is faithful, and it is STRATUM-QUALIFIED.
STAGE8_INGREDIENT_CENSUS_O17SR_V001.md:494 (seal OK), verbatim:

```text
"NOTHING SEALED EXISTS:    a physical-stratum glued multi-cell record complex;"
```

But STAGE8_GLUING_CANDIDATE_O23SR_V001.md — **seal OK** — carries, at :157-159,
verbatim and whole:

```text
"a sealed multi-cell glued record complex EXISTS — two of them, K_square and K_L,
 both inside V011, both loop-carrying — but both are sealed at the AUDIT/PREDICTION
 stratum."
```

So: qualified by stratum, the build is right. Unqualified — as its §2.1 row
label and its §11 verdict clause both state it — it is FALSE at bytes, and the
build never displays K_square or K_L at all. The same defect touches §5.1,
which states flatly that "The cell register has none of these" while two
loop-carrying glued cell complexes are sealed at the prediction stratum.
DISPLAYED ONLY. I construct nothing from K_square or K_L and do not relate them
to anything.

### COR-6 — "EXACTLY ONCE, UNSEALED" IS REFUTED AT BYTES  *** HUNT (A)/(C) ***

Build §2 finding: the adjacency/shared-boundary relation "appears exactly ONCE,
in an UNSEALED file, in the imperative mood, under a flag that reads false."
Build flag: `adjacency_or_shared_boundary_object_SEALED = false`.

R3_4_SHARED_SOURCE_CAUSAL_PARENT_SPEC_V001.md — **seal OK** — obligation 3,
:98-99, quoted whole to the end of its sentence:

```text
"Show that adjacent-cell order is physically load-bearing and fixed by causal
 order, while spacelike/disjoint cells commute."
```

Further sealed adjacency vocabulary, STAGE8_GLUING_CANDIDATE_O23SR_V001.md
(seal OK) :76, :221, :239 — "the adjacency is", "2 vertices, 1 reciprocal edge
pair", "An adjacency matrix and a glued pair".

Adjacency is therefore named in SEALED bytes, not once and not only unsealed.
The build's DEEPER point survives and is in fact reinforced: every occurrence,
sealed and unsealed alike, is in the imperative — DEMANDED, never HELD, never
constructed. But "exactly ONCE, in an UNSEALED file" and the flag
`adjacency_or_shared_boundary_object_SEALED = false` are wrong at bytes.

### COR-7 — TWO OF THE THREE "SECOND SIDES" ARE READINGS, NOT SENTENCES

Hunt (C) demands the sentence that says what lies beyond the boundary.

```text
ANSWER 1  Ground = "G_closure acts on H_source tensor H_record tensor
          H_boundary/environment." (verified verbatim, :33-34).  This is a LIST
          OF TENSOR FACTORS THE GENERATOR ACTS ON.  No sentence in that file
          says the record's boundary is a boundary BETWEEN the record and the
          environment.  The identification is the build's reading.
ANSWER 2  Ground = "The boundary is the record cell's junction — the interface
          between the cell's internal ... and its external ... description."
          (verified at :127).  *** THIS ONE IS A SENTENCE THAT SAYS SO. ***
ANSWER 3  Ground = a PROJECTOR DEFINITION, "P = multiplication by
          1_(|x| <= r(t))" (verified at :156-157).  That the second side is the
          region outside the ball "and what sits there is C — the sea, P_-" is
          the build's reading of a projector, not a quoted statement.
```

The build displays its reasoning openly in each case, so this is overreach in
the verdict language rather than fabrication. But §3.3's "*** YES ***" and
§11's "the boundary's other side is not missing" rest on inference for two of
three answers, and the build should have displayed that only ANSWER 2 is
carried by a sentence. To that extent `side_supplied_by_this_commission = false`
is overstated. I supply no side and reconcile nothing.

### COR-8 — MINOR

```text
(a) G-1's line cites run ~2 low: ":14-17" for text at :16-17, ":19-21" for :21,
    ":43-46" for :41-44.  Content exact in every case.
(b) Cutoff mtime given as 17:10; `stat` reports 17:11.
(c) §4.1 quotes STAGE8_LOCALIZATION_ASSUMPTIONS_O9SR_V001 ending mid-sentence
    at "not an idealization adopted for" — truncation is visually evident, so
    this is disclosed rather than concealed.
(d) Headline counts P2b 31/28 and P3 330/394 did not reproduce exactly for me
    (36/39 and 367/394 on a strict-uppercase exclusion filter).  P1 and D3
    reproduced.  The divergence is filter-boundary noise, not a finding: no
    Sense-A hit turns on it.
```

---

## §7 — CHOICE LEDGER

```text
D-1  FORCED.  Verified every consumed seal with `shasum -a 256 -c` from the
     artifact's own directory, including seals I first inferred from sidecar
     presence during the A6 intersection — I went back and checked each
     explicitly rather than relying on the inference.
D-2  UNFORCED, DISCLOSED.  I recorded my own false-zero sweep (§0.4) instead of
     silently rerunning it.  Same reason the build gave for its P2 disclosure.
D-3  UNFORCED.  I used /usr/bin/grep (BSD) as the authority rather than the
     shell's `grep`, which is a ugrep wrapper on this machine.  Named because
     engine choice is exactly what produced COR-1.  Both engines were tested on
     the empty-alternative question and agree.
D-4  UNFORCED.  My first exclusion filter was case-insensitive and over-excluded
     ("planar", "broad", "analytic_continuation" — 12 files in ROOT2).  I
     rebuilt it strict and re-ran.  The discarded first counts are not reported
     as findings anywhere above.
D-5  FORCED.  I display K_square/K_L and the sealed adjacency sentence as
     located bytes only.  I do not construct a relation among records, do not
     join anything, and do not supply a second side.
D-6  UNFORCED.  I graded G-5 CONTESTED rather than striking it.  Striking it
     would have been a sense-judgement of mine substituted for the build's; the
     honest form is to display the contest and leave the count soft.
D-7  FORCED.  No register/tracker/road/plan/continuation file opened.  The
     exclusion is applied at the path filter, before any read.
D-8  FORCED.  "Q-..." tokens encountered in consumed text recorded
     EXPECTED-UNLOCATABLE, not chased, nothing defaulted.
D-9  UNFORCED.  I did not open BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE at any
     version as ground.  I probed only for sidecars, to test the build's
     unsealed claim, and extended the probe to all six versions.
```

## §8 — TOY_SEPARATION

```text
NOTHING IN THIS AUDIT IS A TOY, MODEL, STAND-IN, OR ILLUSTRATION.  This artifact
contains no constructed object.  It contains sweeps, counts, seal results, and
quotations checked against bytes.
THE PLACE A TOY COULD HAVE CREPT IN, NAMED:  COR-5 and COR-6 display objects the
build omitted — K_square, K_L, and a sealed adjacency obligation.  It would be
one short step to relate them, or to offer them as the missing joiner.  THEY ARE
NOT SO OFFERED.  They are displayed as located bytes with their stratum and
their mood (imperative) shown, and nothing is built on them.
THE ACTUAL SURFACE, NOT A TOY:  every claim above is a property of files on disk
at the declared cutoff, re-checkable by re-running the declared A-patterns.
```

## §9 — IMPORT AUDIT

```text
IMPORTED FROM OUTSIDE THE TWO ROOTS   : NOTHING.
EXTERNAL LITERATURE CONSULTED         : NONE.
MATHEMATICAL FACT ASSERTED BY ME      : NONE.  Every operator identity, tensor
  decomposition, and symbol above is a quotation from a corpus artifact at cited
  bytes.  This audit computes nothing and derives nothing.
TOOLS USED                            : /usr/bin/grep, ugrep, find, sed, stat,
  ls, wc, sort, xargs, shasum -a 256 -c.  No CAS.  No numeric evaluation.
VOCABULARY IMPORTED INTO THE CORPUS   : NONE ADOPTED.  "COR-n", "CAND-n", "A-n"
  are this artifact's internal display labels only.
FILES WRITTEN                         : exactly two — this artifact and its seal
  sidecar.  No existing file edited.  No git operation of any kind.
```

## §10 — FLAG BLOCK — STAGE8_RECORDS_PLURAL_O30SR_AUDIT_V001

```text
alpha_computed                                        = false
proof_authorized                                      = false
kappa_record_computed                                 = false
value_computed_of_any_kind                            = false
number_used_as_program_quantity                       = false
measured_constant_compared                            = false
git_operation_performed                               = false
existing_file_edited                                  = false
register_tracker_road_plan_continuation_file_opened   = false
relation_constructed_by_this_audit                    = false
side_supplied_by_this_audit                           = false

target_seal_verified_from_own_directory               = true
every_consumed_seal_verified                          = true
own_sweep_defect_disclosed                            = true   (§0.4)

plurality_missed_by_build                             = false  (hunt A: none)
plurality_manufactured_by_build                       = partial (G-5 contested)
sealed_multi_record_statements_firm                   = 5      (+1 contested)
build_idealisation_absence_claim_survives             = true   (D3 = 62, exact)
build_consequence_overreach_found                     = false
build_proposal_or_construction_found                  = false
lens_token_in_build                                   = false
program_numeral_in_build                              = false

quotation_defects_found                               = 5      (COR-2, COR-4 a-d)
unsealed_sentence_presented_as_sealed                 = true   (COR-2)
refuted_sentence_displayed_without_status             = true   (COR-3)
absence_claim_wider_than_bytes                        = true   (COR-5)
adjacency_named_in_sealed_bytes                       = true   (COR-6, refutes
                                                         build's "exactly once,
                                                         unsealed" and its flag)
adjacency_ever_HELD_rather_than_DEMANDED              = false  (build's deeper
                                                         point survives)
boundary_second_side_carried_by_a_sentence            = 1 of 3 (COR-7)
boundary_second_side_is_ever_another_record           = false  (CONFIRMED)
```

---

## §11 — VERDICT

```text
PER DELIVERABLE
D1  THE PLURAL SWEEP              CONFIRMED.  Strengthened: §1.1 adds sealed
                                  plural material the build did not display.
                                  Count soft by one (G-5 contested).
D2  RELATIONS AMONG RECORDS       CONFIRMED-WITH-CORRECTIONS.  Substance holds;
                                  COR-2 (unsealed sentence labelled sealed),
                                  COR-5 (gluing absence overstated), COR-6
                                  ("exactly once, unsealed" refuted at bytes).
D3  THE BOUNDARY'S OTHER SIDE     CONFIRMED-WITH-CORRECTIONS.  Core finding —
                                  named everywhere, constructed nowhere, three
                                  non-agreeing readings, none of them another
                                  record — survives my own B1/B3/A7/A9 sweeps.
                                  COR-3 (refuted sentence shown without status)
                                  and COR-7 (two of three sides are readings).
D4  THE IDEALISATION              CONFIRMED.  D3 count reproduced exactly at 62;
                                  the absence claim survived my own search.
D5  THE CONSEQUENCE               CONFIRMED.  No dissolution, no relocation.

OVERALL   *** CONFIRMED-WITH-CORRECTIONS ***

Nothing inverts the verdict.  I hunted a missed plurality hardest, with wider
patterns than the build's and with the adjacency vocabulary the build never
swept, and the corpus does not yield a sealed Sense-A multi-record statement
that changes the result — it yields more of them, which is the build's own
direction.  The corpus holds more than one record in sealed bytes; the
boundary's second side is named and never constructed; no second side is ever
another record; there is no single-record idealisation to find.

What the build owes is quotation discipline and the width of its absences.  It
labelled an unsealed sentence sealed, in its own favour.  It offered as
corroboration a sentence that a sealed artifact in the same root declares
REFUTED, without displaying that status.  It stated "gluing absent" and
"adjacency named exactly once, unsealed" more widely than its own quoted ground
allows, and both are false at bytes once the stratum qualifier and the sealed
R3_4 obligation are put back.  And four quotations stop inside their own cited
spans, one of them closing a colon as a period.

The build's central achievement stands: it went looking for an absence, found a
presence, and reported the presence against its own commission's premise.  That
is the right failure mode to have.  The corrections above are the price of the
speed it was written at, not a defect in its finding.

FENCES AT CLOSE:  alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false.
```

*** END OF STAGE8_RECORDS_PLURAL_O30SR_AUDIT_V001 ***
