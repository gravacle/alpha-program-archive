# STAGE 8 / 7A / STEP 8 — RA27-3's FRONTIER: THE FIVE TRANSPORT FIELDS, FIELD BY FIELD

Lane: DARIO (Builder B, independent verifier). Relay 751.
Governing: the booking 750 `57c55221…`; the working class; 749's corollary — the per-cell
tetrad `e = diag(ell)` is the coframe-shaped object refinement **changes**.

## Lead determination

**The coframe lead is half right, and the half that fails is the half that matters.**

On `A1` — cubical bisection — the tetrad's transformation **is** the transport RA27-3
demands: each child carries `diag(ell_mu/2)`, canonically, from its own extents, grounded in
a sealed executed result. That much lands.

On `A2` it does not, and the reason is a quantifier I have now been caught by twice on this
thread. `V011`'s tetrad display opens *"**For an orthogonal physical cell with edge lengths
`ell_mu`**"*. `A2`'s children are **order-simplices** — `MAJ`'s sealed construction says so:
*"the oriented order-simplex (Freudenthal) subdivision (24 simplices)"*. A simplex is not an
orthogonal cell with four edge lengths, so the diagonal display does not reach `A2`'s
children at all. `V011`'s general clause supplies **the map given a coframe**
(`wedge^2(e^-1)`, `|det e|`) — it does not supply **a coframe for a simplex**. So over the
working class, which contains `A2` and every mixed composite, the coframe transport is
**partial and the mismatch is named**.

**The four remaining fields, and one of them is a concrete dividend of 750's restriction:**

- **connection — UNDERDETERMINED, free datum named exactly.** A connection on a record
  complex is edge data; transporting it along a refinement is assigning values to refined
  edges; 738 established by exact arithmetic I reproduced independently that this assignment
  is free on a **32-dimensional** set at fixed energy, and that the difference is **not a
  coboundary** — physical, not gauge.
- **degree — the restriction removes the known obstruction.** Sealed record closes raw
  degree: *"Q6's raw-degree form is FALSE on full D3 (**explicit star-refinement
  construction**)."* The counterexample is a **fan/star subdivision** — *"fan-subdivide one
  cell into n pieces meeting at an interior point"* — and **star refinement is not in the
  working class.** The obstruction is out of scope; no positive transport replaces it.
- **support — UNDERDETERMINED**, structurally immediate and unsealed.
- **current density — UNSUPPLIED**, three corpus mentions, two of them D012's own scoping.

**AC3 closes as a gap, and the composite question dissolves for the third time.**
`subdivision chain map` returns **zero** corpus-wide. Composition of chain maps is a chain
map, so composites are free again — the gap is at the generator level. And the object exists
**by name** in the corpus — `J_ref^cell = functorial cellular subdivision on incidence/degree
chains` — but inside **`Ref_PL`**, a different index, and inside a proposal.

`RA27_3 = partial (remainder named)`.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively over the whole archive: **absent**, artifact and
sidecar both. Nothing overwritten.

### 0.2 Sources verified before use

```text
750    workspace/STAGE8_7A_RESTRICTED_BOOKING_DARIO_V001.md      57c55221…  SEALED-OK
749    workspace/STAGE8_7A_FLAT_CHAMBER_DARIO_V001.md            3e89b5ea…
742    workspace/STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md      863bc606…
738    workspace/STAGE8_7A_COUNTEREXAMPLE_CONTEST_DARIO_V001.md  c854c8b2…
D012   workspace/STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md      74bbb7aa…
V011   workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/
         BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md           aa7c6d49…  packet-manifest sealed
MAJ    workspace/stage8_execution/work/MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md  08b91543…
X2     workspace/STAGE8_7A_BRANCH_F_CROSSCHECK_CODEX2_V001.md    97587de8…
D3RAT  workspace/STAGE8_T7_D3_FREEZE_RATIFICATION_AND_PREDICTION_WEIGHTING_RULE_V001.md c373adba…
F8     supervision/F8_SECOND_HALF_REVIEW_SEALED_TRANSCRIPT_V001.md  3cacd4b7…
PL     workspace/STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V003.md  ee69fd1c…
R33    workspace/R3_3_INTRINSIC_CELL_MEASURE_DERIVATION_RESULT_V001.md  e60aec3c…
4d     workspace/STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md  430f0971…
```

Seal checks run from each file's own directory. All `V011` offsets against `aa7c6d49…`, never
the unsealed same-named top-level file.

**Searched space for every negative claim below:** recursive glob `./workspace/**/*.md` +
`./supervision/**/*.md` — **1,976 files** — `*_DARIO_V001.md` excluded so no probe
corroborates itself from my own artifacts, `evaluator_build_A/` code excluded.

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities.  No comparison to measured constants.  No common cell formed.
No junction map evaluated.  No response evaluated.  No smooth data derived.
Nothing adopted.  No register, plan, tracker, or git action.
```

---

## 1. AC1 — COFRAME: DERIVED ON `A1`, MISMATCH ON `A2`

### 1.1 The demand

[PROVABLE] `D012` `74bbb7aa…[44665,45007)`: the full `J_ref` realization carrying *"incidence,
degree, coframe, connection, volume, support, and current density"*, with *"natural transports
on **every** RA27-2 generator and composite"*, modality **derivation**.

**"Every generator"** is the operative phrase. A transport on one generator is not the field.

### 1.2 On `A1` — derived

[PROVABLE] `V011` `aa7c6d49…[45718,46068)`: *"For an orthogonal physical cell with edge
lengths `ell_mu`, volume `V_cell = product_mu ell_mu`"*, face flux
`xi_(mu nu) = ell_mu ell_nu F_(mu nu) + higher-order terms`, face metric
`sum_(mu<nu) [V_cell/(ell_mu^2 ell_nu^2)] xi_(mu nu)^2`.

[PROVABLE] `MAJ` `08b91543…[19632,19996)`: family-A member = one bisection of the unit 4-cube,
**16 subcubes**, `|C|_4 = 1/16`.

[YOURS] **The transport.** A cubical bisection carries an orthogonal cell with tetrad
`e = diag(ell_0,…,ell_3)` to 16 orthogonal children each with `e' = diag(ell_0/2,…,ell_3/2)`.
The child's tetrad is computed **from the child's own extents**; nothing is chosen. Composites
of `A1` iterate: after `n` bisections, `diag(ell/2^n)`.

[PROVABLE] **And it is grounded, not merely natural-looking.** `R33` `e60aec3c…` — a sealed
STAGE7 packet member bound to row A27, verdict
`INHERITED_REFINEMENT_NATURALITY_FORCES_UNIFORM_MEASURE` — states *"a subregion promoted to an
elementary cell must be evaluated by that child's intrinsic cell measure. It may not retain a
weighting profile defined by an arbitrarily chosen parent."* Assigning the child its own
`diag(ell/2)` is exactly that; retaining the parent's is exactly what `R33` forbids.

**Verdict on `A1`: the coframe transport is DERIVED.**

### 1.3 On `A2` — the mismatch

[PROVABLE] The tetrad display's quantifier is *"**For an orthogonal physical cell with edge
lengths `ell_mu`**"* — `aa7c6d49…[45718,…)`, first clause.

[PROVABLE] `A2`'s children are **order-simplices**: `MAJ` `08b91543…[19632,19996)` — *"the
oriented order-simplex (Freudenthal) subdivision (**24 simplices**, `|C|_4 = 1/24` each)"*.

[PROVABLE] **An order-simplex is not an orthogonal cell with four edge lengths.** A 4-simplex
has five vertices and ten edges; it has no assignment of four `ell_mu` making `V_cell =
product_mu ell_mu`. The quantifier excludes it, and `MAJ`'s own count confirms the geometry:
`1/24`, not a product of four extents.

[PROVABLE] **What `V011` supplies instead, and why it is not enough.**
`aa7c6d49…[46074,46387)`: *"For a general coframe `theta^a=e^a_mu dx^mu`, the same map is
defined by pulling the bivector through `wedge^2(e^(-1))` and multiplying by `|det e|`."*
That clause is the **consumer** of a coframe — it says what to do *given* `e`. It does not
construct `e` for a simplex. `X2` `97587de8…[10899,11140)` independently fixes the same
object one step downstream: *"let `D_(ell')` denote the tetrad/coframe integration map … `(D_(ell')
F)_(mu nu) = ell'_mu ell'_nu F_(mu nu)`"* — again in the **orthogonal display**, and `X2`'s own
bearings note says this *"fixes `D_(ell')`, but not a refined edge preimage."*

[YOURS] **The mismatch, stated against the demand text.** RA27-3 demands transports on
**every** generator. The sealed coframe datum is defined only on orthogonal cells; `A2` leaves
that class on its first application; therefore no sealed coframe value exists on `A2`'s
children, and a transport cannot be exhibited along `A2` or along any composite containing
one.

[YOURS] **What would close it, named as a spec gap rather than authored here.** A coframe for
an order-simplex is constructible — the barycentric gradient frame, which I built and inverted
exactly at 738 for the energy computation. **It is not sealed anywhere**, and constructing it
as the coframe field would be authoring the object RA27-3 demands be derived, on my own
authority, in the lane barred from verifying its own constructions. **SPEC GAP: a sealed
coframe on simplicial cells.**

```text
COFRAME = mismatch named — DERIVED on A1 (child's own extents, grounded in R33's
    executed result); NOT SUPPLIED on A2, because V011's tetrad display is
    quantified "For an orthogonal physical cell" and A2's children are simplices.
```

---

## 2. AC2 — THE OTHER FOUR FIELDS

### 2.1 CONNECTION — **UNDERDETERMINED**, free datum named

[PROVABLE] A connection on the record complex is **edge data** — `V011`'s baseline connection
is the gauge class `[U_0]` of edge transports, and the cochain structure `(d_0 lambda)_e =
lambda_t − lambda_s`, `(d_1 a)_f = sum_(e in boundary f) incidence(f,e) a_e` is carried on
edges (`aa7c6d49…[44595,44690)`). Transporting a connection along a refinement therefore means
**assigning values to the refined edges**.

[PROVABLE] That assignment is **not determined**. 738 `c854c8b2…`, confirmed by my own
independent reconstruction — 24 simplices, 16 vertices, 65 edges, 32 fixed, **33 free**, rank
33, `E_min = 191/252`, `E(x_alt) = E(x_F) = 1`, 22 of 33 edges differing — establishes that on
`A2`'s move the refined edge assignment is free on a **32-dimensional** set at fixed energy.

[PROVABLE] **And the freedom is physical, not gauge.** I solved `d_0 lambda = x_alt − x_F`
over all 65 edges and 16 vertices by exact elimination: **inconsistent**. The difference is not
a coboundary.

[YOURS] **Verdict.** The connection field's transport over the working class is
**UNDERDETERMINED**, and the free datum is exactly nameable: **the refined edge assignment**,
of dimension **32** on `A2`'s unit-4-cube instance. What *is* forced is the energy — `Σ F²`
per cell, exactly, on the whole equal-energy set — which is the volume field, not this one.

**Scope flag carried:** on `A1` the analogous free space is **untested**. 738 §3 records that
the natural reading *"the cubical assignment is forced"* has not been earned by either lane —
both arguments quantify over a one-parameter equal-share family. So `A1`'s status is *not
established as forced*, and I do not report it as such.

### 2.2 DEGREE — **UNDERDETERMINED, with the known obstruction removed by the restriction**

[PROVABLE] The sealed record **closes** the raw-degree route. `D3RAT` `c373adba…[821,1151)`,
a principal ratification:

```text
Q6's raw-degree form is FALSE on full D3 (explicit star-refinement
construction). RAW-DEGREE KP IS CLOSED. Reading A is FORCED. Q6 is
RE-CAST (weighted, subdivision-insensitive counting)…
```

[PROVABLE] **The counterexample is a fan/star subdivision.** `F8` `3cacd4b7…[9519,9668)`:
*"fan-subdivide one cell into n pieces meeting at an interior point, Δ ≥ n − 1."*

[PROVABLE] **Star and fan subdivisions are not in the working class.** The working class is
`A0 + A1 + A2 + finite composites` (750 §1.1). Corpus-wide over the searched space, `stellar`
returns **0**; the single `fan-subdiv` occurrence is the counterexample sentence itself.

[YOURS] **So the restriction of 750 buys RA27-3 something concrete, and this is the first
place it pays.** The sealed obstruction to the degree field is a construction that the working
class excludes. Degree is therefore **not obstructed over the working class** — but no
positive transport is sealed in its place. **UNDERDETERMINED, obstruction out of scope.**

[YOURS] I do not claim degree is bounded under `A1`/`A2` iteration. That is very likely and I
did not derive it, and asserting it would be the kind of plausible-looking step this thread has
already punished twice.

### 2.3 SUPPORT — **UNDERDETERMINED** (structurally immediate, unsealed)

[YOURS] The structural statement is one line: a refinement partitions each cell, so the support
of a pulled-back cochain is the union of the children of the cells in the original support; it
is monotone and composes. **Nothing in the searched space seals it** — the `support`
occurrences are overwhelmingly support-*locality* conditions on operators, not a transport law
for the field. Not obstructed, not supplied. Naming it rather than writing the line into the
record, because a one-line construction of mine is still a construction.

### 2.4 CURRENT DENSITY — **UNSUPPLIED**

[PROVABLE] `current density` occurs in **three** files across the searched space, two of them
D012's own scoping documents (Lane 2 V003 and Lane 3 V002 of the A25/A27 scoping) — i.e. the
demand restating itself — and one Maxwell-symbol certificate. **No transport law exists.** It
is the most downstream of the seven fields and nothing upstream of it is complete.

---

## 3. AC3 — THE COCHAIN-INCIDENCE HALF: GAP NAMED

### 3.1 What is being asked

Naturality of `incidence(f,e)` under subdivision: for a refinement `g: K → R`, a map carrying
the incidence structure of `K` to that of `R` compatibly with `d_1` — i.e. a chain/cochain map
intertwining the differentials, natural on generators and composites.

### 3.2 The composite step is free — for the third time on this thread

[YOURS] If each generator carries such a map, so does every finite composite: a composition of
chain maps is a chain map, and functoriality on composites follows by induction on composite
length, exactly the shape used at 742 §3 for four-volume additivity. **The composite question
is not the gap** — this is the third relay in which it dissolves and the third time the real
question turns out to sit at the generator level.

### 3.3 The generator-level object is unsealed

[PROVABLE] `subdivision chain map` — and `chain map … subdivision`, `subdivision operator`,
`sd_*` — return **ZERO** across the 1,976-file searched space. The object is not sealed for
`A1` or for `A2`.

[PROVABLE] **It exists by name elsewhere.** `PL` `ee69fd1c…[38100,38420)`:

```text
Ref_PL objects = order-complex/edgewise subdivisions sd_n(G);
…
J_ref^cell     = functorial cellular subdivision on incidence/degree chains.
```

**That is precisely the object AC3 wants** — and it is stated for **`Ref_PL`**, which 742 §1.9
classified as a **different index** (the Q-408 continuum root, `Ref_0 != full Ref_PL/P4
generator category`), inside a **proposal**. A pointer, not a supply.

### 3.4 The hazard, named because it is mine

[YOURS] The bookkeeping this map requires is **transverse multiplicity** — how a refined
2-cell meets refined 1-cells across sub-faces, not only in-plane. That is exactly where my 731
trichotomy failed: I summed over in-plane sub-faces only and was short by exactly the
transverse multiplicity `m`, and it survived my first pass because the single case I computed
by hand had `m = 1`. Any lane taking this gap should treat that as the live failure mode.

```text
INCIDENCE = gap named — the generator-level subdivision chain map is unsealed
    (zero corpus-wide); the composite step is free; the object is named for a
    DIFFERENT index (Ref_PL) in a proposal; transverse multiplicity is the hazard.
```

---

## 4. THE BOARD

| # | Field | Verdict over the working class | Basis |
|---|---|---|---|
| 1 | **incidence** — four-volume half | **SUPPLIED** | 742 §3; generator hypothesis sealed on full D3 |
| 1b | **incidence** — cochain half | **GAP NAMED** | §3 |
| 2 | **degree** | **UNDERDETERMINED** — obstruction **out of scope** | §2.2 |
| 3 | **coframe** | **PARTIAL** — derived on `A1`, not supplied on `A2` | §1 |
| 4 | **connection** | **UNDERDETERMINED** — free datum: the refined edge assignment, dim **32** on `A2`; `A1` untested | §2.1 |
| 5 | **volume** | **SUPPLIED** | 739 §2.2; `volume_weight_natural_on_full_D3 = true`, exact |
| 6 | **support** | **UNDERDETERMINED** — structurally immediate, unsealed | §2.3 |
| 7 | **current density** | **UNSUPPLIED** | §2.4 |

```text
RA27_3 = partial.  Two of seven supplied (volume; incidence's four-volume half).
    One partial (coframe, on A1 only).  Three underdetermined (degree, connection,
    support).  One unsupplied (current density).  One half-field gap (cochain
    incidence).  Nothing obstructed of record over the working class.
```

[YOURS] **The shape of what remains is worth stating.** Nothing on this board is *obstructed*
any more — 750's restriction removed the one sealed obstruction (degree), and the excluded
constituent that blocked coframe/connection is out of scope rather than in the way. What
remains is **three unsealed constructions** — a coframe on simplicial cells, a refined edge
assignment rule, a subdivision chain map — each of which is standard mathematics and none of
which this lane may author. **RA27-3's frontier is now a build list, not an obstruction list.**
That is a different kind of open than the one this thread has been carrying since 727.

---

## 5. GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | RA27-3's seven fields; "every generator and composite"; modality derivation | `74bbb7aa…[44665,45007)` | PROVABLE |
| 2 | The tetrad display's orthogonal-cell quantifier | `aa7c6d49…[45718,46068)` | PROVABLE |
| 3 | `A2`'s children are 24 order-simplices | `08b91543…[19632,19996)` | PROVABLE |
| 4 | The general-coframe clause is a consumer, not a constructor | `aa7c6d49…[46074,46387)` | PROVABLE |
| 5 | `D_(ell')` fixed in the orthogonal display; "not a refined edge preimage" | `97587de8…[10899,11140)` | PROVABLE |
| 6 | `A1`'s 16 subcubes | `08b91543…[19632,19996)` | PROVABLE |
| 7 | The child's own intrinsic measure is forced | `e60aec3c…` | PROVABLE |
| 8 | The connection is edge data; the cochain structure | `aa7c6d49…[44595,44690)` | PROVABLE |
| 9 | 32-dimensional free assignment; non-coboundary | `c854c8b2…` | PROVABLE |
| 10 | Raw degree closed by a star-refinement construction | `c373adba…[821,1151)` | PROVABLE |
| 11 | The counterexample is a fan subdivision | `3cacd4b7…[9519,9668)` | PROVABLE |
| 12 | `stellar` = 0, `fan-subdiv` = 1 (the counterexample itself) | §2.2 census | PROVABLE |
| 13 | `subdivision chain map` = 0 corpus-wide | §3.3 census | PROVABLE |
| 14 | `J_ref^cell` named for `Ref_PL`, in a proposal | `ee69fd1c…[38100,38420)` | PROVABLE |
| 15 | The `A1` coframe transport | §1.2 | YOURS |
| 16 | The `A2` mismatch against the demand text | §1.3 | YOURS |
| 17 | The composite-closure step for chain maps | §3.2 | YOURS |

**Pin check: 17/17**, every span re-read from bytes this relay.

### 5.2 Jurisdiction check

**On returning a partial where a booking was reachable.** *What was the rule written to
protect?* RA27-3's *"every generator and composite"* protects against a field transported on
the easy generator and declared natural. That is exactly the risk the coframe lead ran, and
the quantifier caught it. *Does the outcome space distinguish false from cannot-see?* Yes —
§4 returns four distinct verdicts across seven fields, not one shrug. *Would it permit the
evidence to appear if the theory is right?* Yes: each of the three named gaps is a single
sealed construction away, and §4 says which.

**On the VOID CONDITION.** Nothing chosen from a desired coefficient. §1's mismatch and §2.1's
underdetermination both run against the direction that would book RA27-3 today.

**On the authoring line.** Three times here I could have written the missing construction —
the simplicial coframe (I have the gradients from 738), the support law, the chain map. Each
is refused and named as a spec gap. I verify; I do not author the objects I would then be
verifying.

**On R9 / R9-JII.** Untouched.

### 5.3 Self verb audit — **CLEAN, with three disclosures**

1. **The relay's lead was half right and I report the failing half first.** The coframe
   transport does not cover `A2`, and the commission's framing (*"the COFRAME field's transport
   may already be derived"*) would have been comfortable to accept whole. A producer-supplied
   lead may accuse; it may not exculpate.
2. **The defect that killed the lead is a quantifier inside a span I have cited repeatedly.**
   *"For an orthogonal physical cell"* opens `[45718,46068)`, which I have cited at 731, 733,
   738, 749 and 750 — for the measure, never for its restriction. Sixth occurrence on this
   thread of a qualifier inside already-displayed text deciding a relay.
3. **I declined three available constructions** (§5.2). Each would have moved a field from
   underdetermined to supplied on my own authority, and each is named as a gap instead.

---

```text
COFRAME = mismatch named.  DERIVED on A1 — each child carries diag(ell_mu/2) from
    its own extents, canonical, grounded in R33's executed verdict
    INHERITED_REFINEMENT_NATURALITY_FORCES_UNIFORM_MEASURE.  NOT SUPPLIED on A2:
    V011's tetrad display is quantified "For an orthogonal physical cell with edge
    lengths ell_mu" (aa7c6d49...[45718,46068)) and A2's children are ORDER-SIMPLICES
    (MAJ 08b91543...[19632,19996): 24 simplices at 1/24).  V011's general clause
    supplies the map GIVEN a coframe, not a coframe FOR a simplex.  Since RA27-3
    demands transports on EVERY generator and composite, the field is partial.
    SPEC GAP: a sealed coframe on simplicial cells.
FIELDS = connection UNDERDETERMINED, free datum named exactly — the refined edge
    assignment, dimension 32 on A2's instance, with x_alt - x_F NOT a coboundary
    (physical, not gauge); A1's free space UNTESTED and not reported as forced.
    degree UNDERDETERMINED WITH ITS OBSTRUCTION OUT OF SCOPE — raw degree is closed
    on full D3 by an explicit STAR-REFINEMENT construction (c373adba...[821,1151);
    F8 3cacd4b7...[9519,9668): "fan-subdivide one cell into n pieces"), and star/fan
    refinement is NOT in the working class (stellar = 0 corpus-wide).  This is the
    first concrete dividend of 750's restriction.
    support UNDERDETERMINED (structurally immediate, unsealed).
    current density UNSUPPLIED (three corpus mentions, two of them the demand
    restating itself).
INCIDENCE = gap named.  "subdivision chain map" returns ZERO across 1,976 files.
    The composite step is FREE (composition of chain maps is a chain map) — third
    relay running in which the composite question dissolves and the gap proves to
    sit at the generator level.  The object is named for a DIFFERENT index:
    ee69fd1c...[38100,38420) "J_ref^cell = functorial cellular subdivision on
    incidence/degree chains" in Ref_PL, inside a proposal.  Hazard named:
    transverse multiplicity, where my own 731 trichotomy failed.
RA27_3 = partial (remainder named).  Two of seven supplied, one partial, three
    underdetermined, one unsupplied, one half-field gap.  NOTHING OBSTRUCTED of
    record over the working class — what remains is three unsealed constructions
    (a simplicial coframe; a refined edge assignment rule; a subdivision chain map),
    each standard mathematics, none of which this lane may author.  RA27-3's
    frontier is now a BUILD LIST, not an obstruction list.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+3 disclosures at section 5.3, including that the
    commission's lead was half right and the failing half is reported first)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
