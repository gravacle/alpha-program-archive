# STAGE 8 — AUDIT OF THE O-1-S-B OVERLAP-LAW ARTIFACT (T11SR V001) — DEFAULT-REFUTE, RE-DERIVED AT BYTES — OVERLAP-AUDIT V001

## AUDITOR — CODENAME OVERLAP-AUDIT — COMMISSION T11SR — 2026-08-15

Role: independent auditor, DEFAULT-REFUTE; builder testimony weighted
ZERO — every load-bearing claim re-derived at bytes or re-run in the
auditor's own fresh environment. THE CLAIMED ARTIFACT:
`STAGE8_OVERLAP_LAW_T11SR_V001.md` + seal (sha256
8af48b58ff92cd0a1604f3be2b89eb92652d49b9ed21bf7503cd3e4974548d08,
verified at path by `shasum -a 256 -c`, OK, BEFORE any read).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. DETERMINATION ONLY — no adoption, no
authored physics; the registrar consumes. Fences held: exact symbolic
only (auditor CAS: sympy 1.14.0, fresh venv `auditvenv_t11sr` under
the session scratchpad); no floats; no numeric evaluation; no
measured-constant comparison; no git action; no fenced-class file
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF|continuation)
opened at content level (filename-level listing and match-count
probes only); scoped reads of named artifacts only; "Q-..." tokens
EXPECTED-UNLOCATABLE by design (none encountered beyond the ground's
own notations); ONE output (this artifact) + seal sidecar, path
probed ABSENT at session start AND re-probed immediately before
write.

SWEEP CUTOFF, DECLARED: the auditor's corpus sweep (§7) was executed
against the tree state as of **2026-08-15 07:56:35 CDT** (later than
the builder's 07:41:01 cutoff; the target artifact itself, sealed
07:46, is the only new carrier found — as expected).

---

## 0. VERDICT IN ONE LINE

**CONFIRMED — overall and per claim.** The headline
(UNDECIDABLE-TODAY for O-1-S-B as commissioned: neither the ceiling
A_n <= C_B'' n^{3/2 - gamma_B} with explicit gamma_B > 0 nor the
anti-localization floor A_n >= c n^{3/2} is derivable on the sealed
stock at the cutoff) is CONFIRMED at the bytes. All four partials
(OP-1 direction-commutator identity; OP-2 member valuation at n = 2;
OP-3 hyperoctahedral collapse; OP-4 radial trim + momentum-weight
stall) re-derived independently by the auditor and CONFIRMED exact at
their stated grades (OP-4's trim CONDITIONAL(CL-C), its M-ceiling
CONDITIONAL(CL-A), exactly as displayed). The route refusals
(RT-1..RT-6), the import ruling (§7), the decider menu (§8), the
sweep (§9), the consistency ledger (§10), the CAS battery (§11 —
hash-matched, re-run twice in the auditor's own fresh venv, 9/9 PASS,
byte-identical to each other and to the displayed output), the choice
ledger + chain choice audit (§12), toy separation (§13), and the flag
block (§14) all CONFIRMED. The commissioned hunts all come back
NOT FOUND: no classical citation is consumed beyond its displayed
statement or outside the T7SR discipline (CL-D is genuinely
display-only, zero blast radius); no localization law is assumed
anywhere (the needed law is displayed as the decider, consumed
nowhere); no gamma_B exists to degrade (none is claimed; the
NOT-POLYLOG-ONLY line is exact); no witness is built at all, so no
F-3-pattern padding leak is possible (the audited repairs are
consumed at audited grade only; OP-2 is a fixed-member display with
no n-indexing and grounds no rate); every displayed constant is
n-uniform and symbolic; no hidden numerics (fence-scan clean; the
battery is fully exact-symbolic). Two observation-grade notes (§5
o-1, o-2), neither a correction: nothing moves. Nothing fires; O-1,
O-1-S, O-1-S-B, MO-4-R all stay open; no flag of any prior artifact
flips; the registrar consumes.**

---
## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`.
Verified by the auditor independently, full digest against sidecar,
each BEFORE consumption. 7/7 MATCH:

```text
A-0 STAGE8_OVERLAP_LAW_T11SR_V001.md              OK (the target)
A-1 STAGE8_SIGN_CROSSING_T10SR_V001.md            OK
A-2 STAGE8_SIGN_CROSSING_T10SR_AUDIT_V001.md      OK
A-3 STAGE8_CLOCK_CEILING_T7SR_V001.md             OK
A-4 STAGE8_CLOCK_CEILING_T7SR_AUDIT_V001.md       OK
A-5 STAGE8_MO4R_GRAM_MASS_T3SR_V001.md            OK
A-6 STAGE8_MO4R_GRAM_MASS_T3SR_AUDIT_V001.md      OK
```

The same six ground files the builder consumed, consumed here
independently at bytes (the auditor's quotes below are the auditor's
own reads, not the builder's).

---

## 2. GROUND CONSUMPTION — THE BUILDER'S QUOTES ADJUDICATED AT BYTES

Every load-bearing quote in the target's §1/§2 was re-located and
re-read by the auditor in the sealed ground. Findings:

```text
Q-1 T-1 SP-1 (grid, eigenvectors, tridiagonal): MATCH. G_n = (zeros
    of phi_n)^3/ell, |G_n| = n^3, joint eigenvalues simple in the
    spatial factor; psi_p = products of 1D eigenvectors; 1D
    coefficient bytes i^k phi_k(z_j)/sqrt(lambda_n(z_j)); sealed
    tridiagonal off-diagonals -i sqrt((k+1)/2)/ell; D-conjugation to
    the real Jacobi matrix J_x with a_k = sqrt((k+1)/2). (T-1 §3.)
Q-2 T-1 SP-2 (the count law): MATCH — and the accounting detail the
    trim leans on is AT THE BYTES: the per-axis counts multiply to
    the CUBE (1 + 2 E ell sqrt(2n+1)/pi)^3 for GRID POINTS, and
    "each grid point carries 4 states", giving N_n(E) <= 4 (cube)
    for EIGENVALUE count. The target's OP-4 chain "4 #{p : |p| <= T}
    <= 4 (1 + 2 T ell sqrt(2n+1)/pi)^3" therefore consumes the
    sealed spatial (cube) count EXACTLY — no factor slips. Grade
    CONDITIONAL(CL-C) travels, as displayed. (T-1 §4.2.)
Q-3 T-1 SP-3 + CO-2: MATCH. S_n^2 = J_n^2 + (1/2)||Q[P,sgn]Q||_2^2
    - ||Q^perp P Z||_2^2, correction in [0, 4], = 0 for n even; grid
    form X_n + ||Q^perp P Z||^2 = 4 sum_{p != p'} |B_pp'|^2 (1 -
    cos); B tensor 1_4 = matrix of Q_n P Q_n, so tr B = tr(P Q_n)/4
    (CO-2 displays 2 tr B = tr(P Q_n)/2 — same byte). The target's
    §2 kernel-column pin and its OP-4 use of tr B~ <= tr(P Q_n)/4
    are both EXACT at these bytes. (T-1 §5.)
Q-4 T-1 §8 (the O-1-S-B spec): MATCH. Ceiling => O-1-S at gamma' =
    min(gamma_B, 1/12) through SP-3 + sealed P-2, => O-1 through
    sealed P-3, => MO-4-R DECIDED-o(1) at rate; floor => S_n^2 >=
    4 c n^{3/2} - 4 => CEILING-FAILS-AT-3/2. The "-4" in the
    target's OV8 floor transfer is the SEALED display's own -4 (the
    one-signed kernel correction), consumed verbatim. The "genuinely
    NEW analytic input" naming (full-range PR at the zeros, Szego
    §8.22.9 regime, OR any equivalent) is at T-1 §8 verbatim.
Q-5 T-2 F-1 (the convention): MATCH. A_n over NONZERO pairs; exact
    excess = 4 A_n + 4 sum_{p != 0} |B_p0|^2, second term in [0, 4]
    (sum_p |B_p0|^2 = (B^2)_00 <= 1); +4 folds into constants
    (AT-9). The target's CH-2 "FORCED" classification is accurate;
    T-2's CONSUMPTION_BOUNDARY says verbatim "with F-1's convention
    pinned and F-5's reading guard".
Q-6 T-2 F-2/F-3/F-4 + AT-5/AT-6/AT-7: MATCH. The F-3 padding leak
    (toys' J^2 = 45/49 per copy exits sealed P-2 under padding) and
    its exact repair (idempotent-B toy AT-6a: J_toy = 0, separation
    2/copy; n-indexed radius rho_n = 2 sqrt(6n)/ell above the record
    band via Gershgorin ||J_n||_op <= sqrt(2n); count-law
    verification via classical pi < 22/7, AT-7); the all-parallel
    witness AT-6b (excess = 0 EXACTLY at record-shaped listed data)
    carrying the floor-side refusal (F-4). The target consumes
    exactly these, at audited grade, as RT-5/RT-6 — no witness
    rebuilt, no repair re-derived. The AT-7 bytes also already carry
    the Gershgorin sqrt(2n) display the target's OP-4 re-uses
    ("DERIVED, no new citation" — accurate both places).
Q-7 T-2 F-5 + CL-3/CL-4 (via G-6): MATCH. Grid-abstract reading
    guard ("angular geometry KNOWN, moduli MISSING"); CL-A
    fixed-compact ONLY ("no growing windows, no turning-point or
    Airy regime"); CL-4: Widom/Sobolev covers scaled
    pseudodifferential compressions, NOT Galerkin spectral
    projections. The target's X-4/X-6 and CO-A consume these
    verbatim; OP-1's "sharpening" (route refusal -> object
    identification) adds no new classical consumption.
Q-8 T-3 P-1/P-2 + §4.1: MATCH. kappa_n <= tr(P Q_n) + 4 <= C_B
    n^{3/2} + 4 with C_B = 131072 sqrt2 r^3/(3 pi^2 ell^3) (given
    CL-A); J_n^2 <= C_Q n^{17/12} = C_Q n^{3/2 - 1/12}, gamma_Q =
    1/12, n >= N_Q (given CL-A + CL-B); CL-B displayed with named
    theorem, named source (Szego §8.91 (8.91.3)), displayed
    statement (sup_R |phi_m| <= C_s m^{-1/12}), and the registrar
    downgrade path with VERDICT UNCHANGED — the four-clause
    discipline the target's §7 restates is at these bytes (plus T-4
    G-h: "FAITHFUL + DISCIPLINE RESPECTED", and o-2/o-3/AB5/c-A as
    the target lists them).
Q-9 T-5 (the reduction + R7) and T-6 c-1: MATCH. F_n(eps) <= 2
    kappa_n/((1 - 4 eps^2) c_G n^{3/2}); R7 "the fraction is the
    localization HALF of MO-4-R"; c-1 at its exact statement: a
    polylog-only gain decides o(1) rate-free but NOT O-1 as
    specified (POWER gamma demanded). The target's NOT-POLYLOG-ONLY
    line and X-2 consume c-1 exactly.
```

NO QUOTE FAILURE FOUND. Every sealed display the target leans on
says what the target says it says, at bytes.

---

## 3. THE PARTIALS, RE-DERIVED INDEPENDENTLY (testimony zero)

### 3.1 OP-1 — the direction-commutator identity: CONFIRMED (exact)

```text
AR-1 (auditor's own derivation, general n). W_j diagonal in the SP-1
  basis with entries u_p^(j) = p_j/|p| on G_n*: ([W_j, B~])_pp' =
  (u_p^(j) - u_p'^(j)) B~_pp' entrywise (diagonal matrix commutator
  — pure algebra). Summing j: sum_j |u_p - u_p'|^2 = |u_p|^2 +
  |u_p'|^2 - 2 u_p.u_p' = 2(1 - cos angle(p, p')) (unit vectors).
  Hence sum_j ||[W_j, B~]||_2^2 = 2 sum_{p != p'} |B~_pp'|^2 (1 -
  cos) = 2 A_n (diagonal terms vanish; kernel column excluded by the
  pinned F-1 convention — which is EXACTLY what makes W_j lawful,
  CO-C adjudicated sound). Trace form: for Hermitian W, B,
  ||[W, B]||_2^2 = -tr([W, B]^2) = 2 tr(W^2 B^2) - 2 tr(WBWB);
  summing with sum_j W_j^2 = 1 on G_n* halves to A_n = tr B~^2 -
  sum_j tr(W_j B~ W_j B~). IDENTICAL to the display.
AR-2 (the Galerkin form). With W_j = (Q-Z) W_j (Q-Z) (carrier-
  supported) and B~ = (Q-Z) P (Q-Z): [W_j, B~] = W_j P (Q-Z) -
  (Q-Z) P W_j = (Q-Z)[W_j, P](Q-Z) — auditor-expanded, EXACT. So
  A_n = (1/2) sum_j ||(Q_n - Z_n)[W_j, P](Q_n - Z_n)||_2^2.
AR-3 (CO-B re-derived). |tr(WBWB)| = |<BW, WB>_HS| <= ||WB||_2
  ||BW||_2 = tr(W^2 B^2) (HS Cauchy-Schwarz + cyclicity), so 0 <=
  A_n <= 2 tr B~^2 <= 2 tr B~ — the sealed CO-2 bulk reproduced; NO
  sub-3/2 gain claimed or obtainable from norms alone. CONFIRMED.
AR-4 (object class). The audited CL-4 byte says Widom-class covers
  scaled pseudodifferential compressions, not Galerkin spectral
  projections; AR-2 shows A_n IS a Galerkin-compressed commutator
  mass of the 0-order direction symbol. CO-A's "sharpening" is a
  re-labeling of audited ground, no new consumption. CONFIRMED.
```

### 3.2 OP-2 — the member valuation at n = 2: CONFIRMED (exact; scope rail held)

```text
AR-5 (eigenvector bytes). At n = 2 the sealed coefficient bytes
  i^k phi_k(z_j)/sqrt(lambda_2(z_j)) at z_j = s/sqrt2 give
  (1, i s)/sqrt2 exactly (phi_1(s/sqrt2) = s phi_0(s/sqrt2);
  lambda_2 = phi_0^2 + phi_1^2), i.e. chi_s = [phi_0(x/ell) + s i
  phi_1(x/ell)]/sqrt(2 ell), normalized (auditor-checked). The
  displayed product formula follows by direct expansion; re-run in
  the auditor's venv (OV2), all four sign pairs EXACT.
AR-6 (parity kill — the display's argument, auditor-verified). Each
  factor's odd term i(s_i'-s_i) sqrt2 x_i/ell is odd in ITS OWN
  coordinate only; any cross term containing at least one odd factor
  carries an odd power of that coordinate and dies on the
  ball-symmetric integral. Survivors = product of even parts:
  B_pp' = (8 ell^3 pi^{3/2})^{-1} Int_ball e^{-|x|^2/ell^2}
  prod_i (1 + sigma_i 2 x_i^2/ell^2) dx EXACTLY — real, flip-count
  dependent. AIRTIGHT.
AR-7 (moments + unitarity limit, auditor's own closed-form check).
  Sphere averages 1/3, 1/15, 1/105 (re-run OV3, exact). As r -> oo
  each bracket slot tends to pi^{3/2} ell^3, so b_m -> (1/8)
  prod_i (1 + sigma_i) = {1 if m = 0; 0 else} — the auditor's
  independent factorization of the displayed limit. EXACT.
AR-8 (positivity + A_2). On the sealed freeze (r <= 1/2, ell in
  {1, sqrt2}): x_i^2 <= 1/4 <= ell^2/4, factor >= 1/2, b_m >=
  c I_0/8 > 0. Pair table (24, 24, 8) at weights (2/3, 4/3, 2):
  A_2 = 16 b_1^2 + 32 b_2^2 + 16 b_3^2 >= 64 (c I_0/8)^2 = c^2
  I_0^2 > 0. Auditor re-derived AND re-run (OV5/OV6). EXACT.
AR-9 (scope). §4.2 SCOPE displays fixed-member/no-rate/consumed-by-
  no-verdict-leg; the auditor traced every verdict leg (§4 below):
  OP-2 indeed appears in none. The honesty rail (member positivity
  is NOT a floor) is HELD in every display touching it. CONFIRMED.
```

### 3.3 OP-3 — the hyperoctahedral collapse: CONFIRMED (exact, all n)

```text
AR-10. Pi p Pi = -p and Pi q_n Pi = q_n (parity of phi_k) give
  Pi (q_n p q_n) Pi = -(q_n p q_n); 1D spectrum simple (sealed SP-1
  STEP 1), so parity maps the eigenvector at z to a PHASE times the
  eigenvector at -z; permutations permute identical tensor factors;
  the ball and Q_n are invariant. Hence V_g psi_p = phase psi_{gp}
  and |B_{gp, gp'}| = |B_pp'|, angles preserved: A_n collapses to
  B_3-orbit sums (|B_3| = 48). Member pin re-run (OV6: chi_s(-x) =
  chi_{-s}(x) exact). The auditor finds the general proof COMPLETE
  as displayed (simplicity + invariance are both sealed bytes).
```

### 3.4 OP-4 — the radial trim + the stall: CONFIRMED (exact at its stated conditional grades)

```text
AR-11 (the trim chain, auditor-expanded). Pairs with min radius <=
  T: weight <= 2; union over which endpoint is low <= 2 x
  sum_{p low} (B~^2)_pp (row sums of B~^2); (B~^2)_pp <= B~_pp <= 1
  (0 <= B~ <= 1); #{p low} <= the sealed SP-2 spatial cube (Q-2
  above): A_n(low, T) <= 4 (1 + 2 T ell sqrt(2n+1)/pi)^3 —
  CONDITIONAL(CL-C) exactly as displayed, and n-UNIFORM: at T =
  n^{-a}, a in (0, 1/2], the bound is <= 4 (1 + 2 ell sqrt3/pi)^3
  n^{3(1/2 - a)} for n >= 1 (auditor's explicit constant via
  sqrt(2n+1) <= sqrt(3n) and n^{1/2-a} >= 1) — sub-3/2 for every
  a > 0. The p = 0 singularity quarantine is DERIVED, not assumed.
AR-12 (high pairs). 1 - cos <= |p - p'|^2/(2 |p||p'|) <= |p -
  p'|^2/(2 T^2): AM-GM difference (|p| - |p'|)^2/2 >= 0, auditor
  re-derived + re-run (OV7). ([pi_j, B~])_pp' = (p_j - p'_j) B~_pp'
  at the sealed joint-eigenbasis bytes: A_n(high, T) <= M_n/(2 T^2).
AR-13 (M-ceiling, CONDITIONAL(CL-A) via the sealed P-1 display
  only). Gershgorin on the sealed tridiagonal: max row sum <=
  a_{n-3} + a_{n-2} <= 2 a_{n-1} = sqrt(2n), so ||pi_j||_op <=
  sqrt(2n)/ell — DERIVED at sealed bytes (and already displayed in
  the audited AT-7); ||[pi_j, B~]||_2 <= 2 ||pi_j||_op ||B~||_2;
  tr B~^2 <= tr B~ <= tr(P Q_n)/4 <= (C_B/4) n^{3/2} (Q-3/Q-8):
  M_n <= 24 n tr B~/ell^2 <= (6 C_B/ell^2) n^{5/2}. EXACT.
AR-14 (the stall, auditor re-derived + re-run). min_T [c_1 T^3
  n^{3/2} + c_2 n^beta/T^2] is at T* = (2 c_2/(3 c_1))^{1/5}
  n^{(beta - 3/2)/5} with exponent (3 beta + 3)/5: 21/10 at beta =
  5/2 (the only UNCONDITIONAL bracket), 9/5 at the beta = 2
  anticipation (consumed nowhere — verified: no display consumes
  it), and sub-3/2 IFF beta < 3/2 — i.e. only if the momentum mass
  is already sub-3/2, which the auditor confirms is strictly
  stronger at the dominant radii (the 2n/ell^2 radial factor). The
  REFUSAL of the momentum-weight route at its own arithmetic is
  CONFIRMED; the trim survives as a derived quarantine display.
```

---

## 4. THE VERDICT CHAIN, THE ROUTES, AND THE IMPORT RULING — ADJUDICATED

```text
V-1 THE CEILING SIDE. Rests on: (i) the audited W12' forcing WITH
  the audit's own J-preserving repair (AT-6a/AT-7) — consumed at
  audited grade, covering the resolved-byte thresholded class
  INCLUDING sealed P-2 and the count law (Q-6); (ii) the moduli
  absence (RT-1): CL-A fixed-compact (audited CL-3), CL-B
  phase-destroying sup, zero full-range-PR carriers (sweep, §7
  below); (iii) the stall (AR-14) and the trivial-bracket return
  (AR-3). The auditor finds NO derivable ceiling on the sealed
  stock and NO step in the target that claims one. CONFIRMED.
V-2 THE FLOOR SIDE. Rests on: the audited all-parallel witness
  (AT-6b, class-level, record-shaped listed data with n-indexing
  handled at audited grade — the F-3-pattern hunt comes back clean
  BECAUSE the target builds no witness of its own); sweep absence
  of any anti-localization carrier; OP-2's positivity correctly
  quarantined as rate-free (AR-9). CONFIRMED.
V-3 ROUTES RT-1..RT-6: each refusal re-checked. RT-1 (moduli
  unvalued; the coefficients live at |z_j| up to sqrt(2n+1), outside
  every fixed compact — consistent with audited CL-3 bytes); RT-2
  (no Widom import; object displayed as decider instead — audited
  CL-4 bytes); RT-3 (= AR-14); RT-4 (polynomial moments cannot reach
  |pi|^{-1} without a radial localization law — REFUSED AS BLOCKER
  TO DISPLAY, the honesty rail held verbatim); RT-5/RT-6 (audited
  grade, Q-6). ALL CONFIRMED at their own displays.
V-4 THE IMPORT RULING (§7). Clause (iv) of the T7SR discipline
  (verdict-independence with a downgrade path leaving the verdict
  unchanged — Q-8 bytes) CANNOT be satisfied by a decision-grade
  import: if the import decides, ruling it out flips the verdict —
  a definitional soundness the auditor endorses. Clause (i) fails
  independently: the needed statement (n-uniform explicit remainders
  across elliptic + turning + exponential regions, all k <= n, at
  the zeros) is an ASSEMBLED package — consistent with the audited
  o-2 byte that Szego's displayed statements are region-separated
  with unquantified remainders. The CONSEQUENCE line ("no
  8.22.9-grade statement is cited, consumed, or silently used") was
  HUNTED: the batteries consume ladder algebra, parity, Gaussian
  moments, finite linear algebra, and Gauss-quadrature-at-members
  ONLY (auditor re-read of all 9 checks); no asymptotic statement
  appears anywhere in the target. CONFIRMED.
V-5 THE DECIDER MENU (§8). O-1-S-B-PR and O-1-S-B-W are equivalent
  guises BY OP-1 (AR-1/AR-2 — the W-guise ceiling IS the A_n
  ceiling, verbatim); yield chains pinned at the sealed displays
  (Q-4; 3/2 - 17/12 = 1/12; +4 fold; floor transfer with the sealed
  -4). The n log n anticipation is displayed and consumed nowhere
  (verified by trace). CONFIRMED.
V-6 CONSISTENCY LEDGER (§10 X-1..X-10): every contact point
  re-checked at the sealed bytes (Q-1..Q-9); X-10's residual claims
  verified by direct scan (H-R/rho_n appear only in the fence
  sentences themselves; no closure claimed anywhere). CONFIRMED.
```

---

## 5. THE COMMISSIONED HUNTS — RESULTS (all NOT FOUND) + OBSERVATIONS

```text
H-1 A CLASSICAL CITATION CONSUMED BEYOND ITS DISPLAYED STATEMENT OR
    WITHOUT THE CL DISCIPLINE: NOT FOUND. CL-A/CL-B/CL-C enter ONLY
    through sealed displays (P-1's tr(P Q_n) bracket; P-2; SP-2
    count law), grades traveling exactly with the displays that
    carry them (AR-11/AR-13). CL-D (Gauss-Hermite quadrature /
    Christoffel numbers, Szego §3.4 + §15.3 — classical in the
    strictest sense) is DISPLAY-ONLY: the auditor traced every
    verdict leg and every inequality — CL-D appears in NONE; its
    blast radius is the alias "Christoffel profile" for the missing
    law, exactly as CH-1 declares; the displayed statement
    lambda_n(z_j) = e^{-z_j^2}/w_j is member-pinned exactly by the
    auditor's own OV9 re-run. Discipline shape: all four clauses
    displayed (named theorem, named source, displayed statement,
    total verdict-independence) — the G-h-respecting shape.
H-2 A LOCALIZATION LAW ASSUMED RATHER THAN DERIVED: NOT FOUND. The
    only localization-flavored displays are DERIVED: the trim
    (AR-11, from the sealed count law, CONDITIONAL(CL-C)) and the
    quarantine it implies. RT-4 REFUSES the bridge that would need
    an assumed radial law, displaying it as blocker — the honesty
    rail held verbatim. The needed angular law is displayed as the
    DECIDER in two guises, consumed nowhere.
H-3 GAMMA_B DEGRADING TO POLYLOG: NOT FOUND. No gamma_B is claimed
    (verdict UNDECIDABLE-TODAY); NOT-POLYLOG-ONLY is accurate in
    the strong sense: no sub-3/2 gain OF ANY SIZE is derived (AR-3
    returns the exact-3/2 bulk); c-1 consumed at its exact
    statement (Q-9).
H-4 A FLOOR WITNESS WITH THE F-3 PADDING-LEAK PATTERN (n-INDEXING):
    NOT FOUND — vacuously AND substantively: the target constructs
    ZERO witnesses (verified by read: no model built anywhere); the
    audited witnesses enter at audited grade only, where F-3's leak
    is already repaired (Q-6); OP-2 is a fixed-member family-byte
    valuation with no n-indexed padding and no rate claim (AR-9).
H-5 CONSTANTS NOT n-UNIFORM: NOT FOUND. Every displayed inequality
    carries explicit symbolic n-uniform constants; the auditor
    exhibited the one constant the target leaves implicit (the trim
    at T = n^{-a}: C(a, ell) <= 4 (1 + 2 ell sqrt3/pi)^3 for n >=
    1 — AR-11), confirming n-uniformity rather than assuming it.
    Thresholds displayed where they exist (N_Q; n^{3/2} >= 2/c).
H-6 HIDDEN NUMERICS: NOT FOUND. Fence-scan of the full target at
    bytes: "alpha" hits are the three gate declarations + ONE
    "p.alpha" (the sealed Dirac-matrix notation inside a ground
    quote) + the two root path names — no fine-structure content,
    no 137, no measured constant, no decimal literals, no
    evalf/Float/N( ) anywhere; the battery is exact-symbolic
    end-to-end (Rational/sqrt/pi/exp/erf/limits). CLEAN.

OBSERVATIONS (observation-grade; NO repair required; verdict and
displays stand):
o-1 (sw-4 enumeration compression). The "Gauss-Hermite quadrature"
    key also hits, beyond the files the builder's lane-level line
    names, the same T7 lane's execution/work and script files
    (CONTROL4_V3_PRIMARY_NOTES.md, T07_control4_v3_blind_commit_
    v001.json, three t7 scripts) and STAGE8_T7_EXACT_HERMITE_MIXED_
    COVARIANCE files. All are the SAME numerics-of-another-lane
    homonym class the builder typed out; auditor count-probes:
    B_pp = 0, kappa_n = 0, sgn( = 0, angular-localization = 0 in
    every one (one lowercase "angular" token in the MIXED_COVARIANCE
    result at count level — not the deciding token). The sw-4
    FINDING (no independent carrier of the deciding object) HOLDS.
o-2 (FLAG BLOCK stall phrasing). "(3 beta + 3)/5 > 3/2 for every
    lawful beta >= 3/2" is true only because the lawful set is
    {5/2} (plus the unconsumed beta = 2 anticipation), both
    strictly above 3/2; at the boundary beta = 3/2 the exponent
    EQUALS 3/2. The governing exact form ("sub-3/2 iff beta <
    3/2") is what §5.2/OV7 display and what the chain consumes —
    no display is false at its stated quantifier.
```

---

## 6. THE CAS BATTERY — INDEPENDENTLY RE-RUN

```text
C-1 EXTRACTION + HASH: the §11 python block extracted at bytes by
    the auditor hashes to sha256 672f5ea7d13956a7384a27bf0ee66ee461
    729dddf12f9e90264ae4f184a23fc7 — EXACTLY the value the target
    declares for its cleaned battery. (The disclosed pre-final edit
    — two inert `if False` scaffolding lines removed after a first
    9/9 PASS — is priced by the disclosure; the battery OF RECORD
    is the displayed one, and that is what the auditor verified.)
C-2 FRESH-VENV RE-RUN: auditor's own venv `auditvenv_t11sr` (sympy
    1.14.0, fresh), TWO runs: byte-identical to each other (diff
    empty) AND byte-identical to the displayed output block (diff
    empty). 9/9 PASS. (The single grep hit for "FAIL" in the output
    is the substring of "CEILING-FAILS-AT-3/2" inside OV8's PASS
    line — no failed check.)
C-3 CONTENT REVIEW (all 9): OV1 proves the identity at a fully
    symbolic 3x3 Hermitian instance with exact rational units — a
    CONSISTENCY PIN, correctly labeled ("checks of steps whose
    operator-theoretic content is derived in the artifact"); the
    general-n content is the displayed derivation, which the
    auditor re-derived independently (AR-1). OV2-OV6 member
    valuations at the sealed bytes; OV7 majorizations + symbolic
    optimization (exponent extraction via exact log-ratio limit);
    OV8 arithmetic + member Gershgorin exhibit of the displayed
    general row-sum fact; OV9 the CL-D member pin. NOTHING numeric;
    every check exact; no check consumes an asymptotic statement.
```

---

## 7. THE AUDITOR'S SWEEP (independent; cutoff in header)

```text
ROOTS: workspace (primary) + cleanroom v003 (both probed; cleanroom
exists). Fenced name classes excluded from every opening;
filename-level listing and match-count probes only.
KEYS RE-RUN: "T11SR"; "OVERLAP_LAW"; "O-1-S-B"; "8.22.9";
"full-range Plancherel"; "anti-localization"; "direction operator";
"B_pp"; "Christoffel"; "Gauss-Hermite quadrature"; "W12".
FINDINGS (auditor's own, at 07:56:35 CDT):
 asw-1 T11SR / OVERLAP_LAW carriers: the target + its seal ONLY
       (the builder's occupation claim at ITS cutoff stands; the
       target is the sole post-cutoff addition). This audit's own
       output path was probed ABSENT at session start and re-probed
       immediately before write.
 asw-2 O-1-S-B / 8.22.9 / full-range-Plancherel / anti-localization
       / B_pp carriers: EXACTLY the T10SR pair + the target. No
       third carrier in either root. sw-2/sw-3 REPRODUCED at a
       later cutoff.
 asw-3 "direction operator": the target only — OP-1's naming
       confirmed NEW.
 asw-4 Homonyms at count level (never opened): "Christoffel" =
       FORM_TO_HS_BRIDGE pair + MO2 build + P1_DILATION audit +
       vendored sympy diffgeom/tensor — all zero on B_pp / sgn( /
       angular-localization (auditor counts); "Gauss-Hermite
       quadrature" = the T7 Hermite-numerics lane (md + work +
       scripts; see o-1) + vendored quadrature.py — all zero on
       the deciding tokens; "W12" = the T10SR pair + three AXN
       grammar artifacts + one vendored sympy test — AXN files all
       zero on the deciding tokens. sw-4 REPRODUCED (with the o-1
       enumeration note).
```

Each sentence claims only this displayed sweep at the declared
cutoff.

---

## 8. AUDITOR'S CHOICE LEDGER (unforced choices of THIS audit)

```text
ACH-1 CL-D SOURCE-POINTER ADJUDICATION: accepted at classical-
      notoriety grade (Gauss/Jacobi/Christoffel quadrature
      exactness and w_j = 1/K_n^{pol}(z_j, z_j) are classical in
      the strictest sense; the displayed STATEMENT is what binds,
      and it is member-pinned by the auditor's own OV9 re-run).
      IMMATERIAL: CL-D is display-only with zero blast radius
      either way (H-1).
ACH-2 VERIFICATION ROUTE FOR THE IDENTITY CORES: hand re-derivation
      (AR-1..AR-14) PLUS re-running the builder's battery in the
      auditor's own fresh venv, rather than authoring a second
      independent battery. IMMATERIAL: both routes are exact; the
      hand derivations are the general-n content and stand alone.
ACH-3 SWEEP KEY SET: the builder's keys re-run verbatim plus
      refined deciding-token count-probes (B_pp / kappa_n / sgn( /
      angular-localization) on every homonym file. IMMATERIAL:
      absences corroborate; nothing consumes a sweep positive.
CHAIN CHOICE AUDIT (this audit's): the CONFIRMED verdict rests
      only on byte-reads of sealed ground, the auditor's own exact
      re-derivations, and the auditor's own venv re-runs — none of
      ACH-1..ACH-3 enters it (alternative choices at each yield
      the same adjudications).
```

---

## 9. TOY_SEPARATION (auditor self-assessment)

```text
CLEAN. The auditor built NO model and NO witness; every check is
either a byte-read of sealed artifacts, an exact symbolic hand
derivation on the record family's own sealed bytes, or an exact
symbolic CAS run (fresh venv, sympy 1.14.0) of the displayed
battery. No toy is consumed as family ground anywhere in this
audit; the member n = 2 objects handled are the RECORD family's own
bytes at fixed n (grounding no asymptotic claim, exactly as the
target's §4.2 SCOPE displays). No value frozen: A_n, B_pp', S_n,
J_n, kappa_n, M_n, C_B'', gamma_B, C_Q, C_B, C_*, c, c'', T and all
thresholds remain symbols or displayed exact closed forms
throughout this audit.
```

---

## 10. FLAG BLOCK

```text
OVERLAP_LAW_AUDIT = CONFIRMED( overall and per claim: headline
  UNDECIDABLE-TODAY for O-1-S-B (both directions) CONFIRMED at
  bytes; OP-1/OP-2/OP-3/OP-4 CONFIRMED exact at their stated
  grades (trim CONDITIONAL(CL-C); M-ceiling CONDITIONAL(CL-A),
  both traveling correctly); RT-1..RT-6 refusals CONFIRMED; the
  import ruling CONFIRMED; the decider menu (O-1-S-B-PR |
  O-1-S-B-W, equivalent by OP-1) and its pinned yield chains
  CONFIRMED at the sealed displays; sweep REPRODUCED at a later
  cutoff; CAS battery hash-matched and re-run 9/9 PASS twice
  byte-identical in the auditor's own fresh venv. ZERO corrections.
  Two observations (o-1 sw-4 enumeration compression; o-2 FLAG
  BLOCK boundary phrasing at beta = 3/2), neither touching any
  verdict, display, or constant. )
HUNTS = ALL-NOT-FOUND( H-1 citation discipline (CL-D display-only,
  zero blast radius); H-2 no assumed localization law (rail held);
  H-3 no gamma_B, no polylog degrade (c-1 exact); H-4 no witness
  built, no F-3 padding-leak possible, n-indexing clean at audited
  grade; H-5 constants n-uniform (auditor exhibited the implicit
  trim constant); H-6 no hidden numerics (fence-scan clean at
  bytes). )
CONSUMPTION_BOUNDARY = NOTHING-FIRED( O-1, O-1-S, O-1-S-B, MO-4-R
  all stay open; no flag of any prior artifact flips; the target's
  flags stand AS SEALED with zero corrections; the audited
  F-1..F-5 repairs remain the pinned conventions; whether to
  commission either decider guise, O-2, or anything else is the
  registrar's call; the registrar consumes. )
SEALS_VERIFIED = 7/7( target + six ground files, full-digest
  sidecar checks at path before any reliance, §1; "Q-..." tokens
  EXPECTED-UNLOCATABLE, none encountered beyond the ground's own
  notations. )
SWEEP_CUTOFF = 2026-08-15 07:56:35 CDT( §7; asw-1..asw-4; the
  target itself is the only carrier newer than the builder's
  cutoff. )
FORBIDDEN_IMPORTS = none( exact symbolic only (auditor CAS: sympy
  1.14.0, fresh venv auditvenv_t11sr under the session scratchpad);
  no floats; no numeric evaluation; no measured-constant
  comparison; no git action; no fenced-class file opened at content
  level; no existing file edited; ONE output (this artifact) + its
  seal sidecar, path probed ABSENT at session start and re-probed
  immediately before write. )
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
DETERMINATION ONLY; the registrar consumes.
OVERLAP_LAW_AUDIT_RESULT = SEALED.
```
