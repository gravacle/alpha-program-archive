# STAGE 8 — AXN — CROSS-CHECK OF THE JOINT-ANCHOR ADOPTION INSTRUMENT (917)
## DARIO LANE — RELAY 918 — `[PLAN:AXN-BUILD-C16]`

## 0. Preflight

Relay 918 verified before reading at
`d5a1e7c03d3093edd9d393d43b2312a020a0f5af93326e14b1c577b1e6ac6a49`. Lane guard read DARIO; the
pickup ACK was written before content access. `PROGRAM_STATE_BRIEF_V005.md` verified and read before
task work at `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`. The subject verified
against its own sidecar before reading at the digest the relay states. The requested output name and
its sidecar were clear at pickup.

---

## 1. Law-9b closure — declared first, exact members, full digests

This is the first substantive content in this artifact. Every determination below is taken **at a
named receiver inside this closure**. The relay's six tasks are each run **in both directions** —
whether a bad anchor could pass, and whether a lawful one could be rejected — and §7 reports the
second direction under its own heading, so its result stands visible on its own.

```text
C_918 = {
 1  RELAY_PASTE_918_INSTRUMENT_CROSSCHECK_DARIO_V001.md
      d5a1e7c03d3093edd9d393d43b2312a020a0f5af93326e14b1c577b1e6ac6a49
 2  supervision/PROGRAM_STATE_BRIEF_V005.md
      e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c
 3  STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V001.md                        [SUBJECT — 917]
      c792ff4bb02d8406c6e02864218cbad0b14d3badddae01762d5d8d2214c12060
 4  STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md  [the authored-anchor template]
      1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0
 5  STAGE8_GEN_OMEGA_NONCIRCULAR_GENERATIVITY_NO_GO_ATTEMPT_V001.md    [the seven-item benchmark]
      7000963e769362d87e17e36f06903e63aaa8caaeb284ee9c9f3676febe4f4163
 6  STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_V003_ADVERSARIAL_REVIEW_DETERMINATION_V001.md
      ae1f60b049f21073c7513f8133712d17b9abf4dfb8c46ccc6ea894fc2283c7eb
 7  STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md                        [K4 fiber test and its controls]
      32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842
 8  STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md   [B ≅ C(Y); Y's structure]
      d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98
 9  STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md                            [915 benchmark/hinge display]
      35f581dee0683add232367f953ec7807e89bb5eac47e34f4ad5d781a87c95be0
10  supervision/QUESTIONS_SETTLED_REGISTER_V001.md                     [live-append; see §2.3]
      9aeb427e9e499fcc0593ae55826d05a48148b0c10d1d91f7a8735f7f4a5dad30
}
```

**Name probe.** `STAGE8_AXN_INSTRUMENT_CROSSCHECK_DARIO_V001.md` and its sidecar: the workspace
listing at pickup returned a clear name for both. This artifact creates the first instance.

---

## 2. Gates, standing, and three preliminary facts

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

Every headline determination here is **CLAIMED**. This report adopts nothing, registers nothing,
fills no principal entry, and **takes no position on whether the principal should approve or
decline**. Its output is whether the instrument is sound, not what should be done with it. No member
was bound; no fixed point was executed; no end test was run; no physical quantity was numerically
evaluated; no measured constant was consulted. PE-1 through PE-13 remained pointer-only.

### 2.1 The failure mode here is two-sided, and I did not import my usual asymmetry

At 901 and 913 I declared in advance that I would press closures harder than openings, because a
wrongly-closed route costs a path the program never revisits. **That asymmetry does not hold here and
I did not carry it over.** An instrument that under-constrains launders authorship as derivation; one
that over-constrains converts an adoptable fork into a dead end. Both are fatal, and because the act
is a principal act taken once, neither is recoverable by a re-run. §3–§6 report the under-constraint
direction; **§7 reports the over-constraint direction separately, and it comes back clean.**

### 2.2 BR-1

My 913 surfaced member 5 as a sealed document absent from both core-pair closures. **It is now
closure member 08 of the subject.** That is recorded as a fact about the subject's closure, not as
support for anything here: member 5's benchmark was re-read **at its own bytes** in §3, never against
my 913 description of it. A verifier who checks a document against his own earlier summary of the
source is checking nothing.

### 2.3 One benign pin discrepancy, disclosed

917 pins the settled register at `d64778161dbfc741…`; it now hashes `9aeb427e9e499fcc…`. 917
declares this file a *"sealed live snapshot… seal-time live append tolerated."* **Both cited register
spans still match at their byte offsets** (§3.1), which is only possible if the appends landed after
those offsets. The discrepancy is expected and benign, and the spans — not the whole-file digest —
are what carry the load.

---

## 3. Byte verification

### 3.1 Every cited span recomputed

917 grounds its claims in 27 half-open spans with digests. All were recomputed here from the sealed
source bytes.

```text
27 of 27 spans MATCH.
```

Including both live-register spans (`Q-818` at `[1669464,1670877)`, `Q-821` at `[1675467,1678215)`),
the three no-go spans, the four V003 spans, and every S1/SM/K-system span. Nothing was transcribed.

### 3.2 Zero advocacy and empty entry — confirmed at bytes

A scan of the full instrument for advocacy vocabulary (`recommend`, `should approve`, `should
decline`, `suggest`, `advisable`, `urge`, `prefer`) returns **two hits**, and neither is advocacy:

```text
line 474  "The instrument recommends neither disposition."          <- anti-advocacy
line 148  "...already encodes a preferred measure..."               <- about a MEASURE, not a disposition
```

Eight `[PRINCIPAL ENTRY]` slots appear as type declarations with no values; `ANCHOR_TAG_CLASS_ENTRY`
and `ANCHOR_CONTENT_ENTRY` carry bracketed placeholders; §2 states *"No default is supplied for
`disposition`, carrier, tag class, or tag content"*; §12 records `PRINCIPAL_ENTRIES_FILLED = 0`.

```text
ADVOCACY = ZERO-CONFIRMED.   EMPTY-ENTRY = CONFIRMED.
```

---

## 4. Template fidelity — checked against V003's sealed conditions

Member 4's sealed tag block, read at its own bytes inside the `anchor-certs` span:

```text
G1_v003 := { P_src normal, CPTP, charge covariant;  P_src E_ch = E_ch P_src;
             one normalized mixing invariant state rho_P;
             ker(P_src|Delta_0^ss) = {0};  Cert_IF(P_src) an exact finite certificate;
             a frozen anchor tag A in {BI,DB,SYM} with its exact Cert_A(P_src) }
Delta_0^ss := { Delta in T_1(H_src^A) : Tr_A(Delta)=0 and E_ch(Delta)=Delta }
ANCHOR_BI  : P_src(I_src) = I_src.
ANCHOR_DB  : P_src equals its adjoint for the finite Hilbert-Schmidt pairing <X,Y>_Tr.
ANCHOR_SYM : a finite unitary action G frozen; Comm(G) = C I_src; P_src is G-covariant.
```

Against 917 §4.1–§4.2, clause by clause:

| V003 clause | 917 joint clause | verdict |
|---|---|---|
| normal, CPTP, charge covariant, `E`-commuting | identical, with `E_joint` | **faithful** |
| one normalized mixing invariant state | identical, `Omega_Phi` | **faithful** |
| `ker(P_src\|Delta_0^ss) = {0}` | `ker(Phi_joint\|Delta_0^joint) = {0}`, with `Delta_0^joint` defined by the same two conditions | **faithful** |
| `Cert_IF` exact finite certificate | `Cert_IF_joint` "exact replayable certificate" | **faithful in name; under-specified in content — §5** |
| `BI : P_src(I_src)=I_src` | `BI : Phi_joint(I_C0)=I_C0` plus trace-preserving/input-faithful | **faithful** (V003 carries IF as a separate G1 clause; 917 folds it into the row — same content) |
| `DB : adjoint for the HS pairing` | `DB : Phi_joint = Phi_joint^dagger` for the **already entered** pairing, **no free reference density** | **faithful + STRENGTHENED** — the added clause closes V003's own reverse-A2 hole |
| `SYM : finite unitary action, scalar commutant, covariance` | identical, plus **content-addressed** | **faithful + STRENGTHENED** |
| — | "every finite/cylindrical restriction and limit square commutes" | **ADDED, and justified**: the joint carrier has a stage/limit structure the source carrier lacked |

**The one structural difference, examined.** V003 freezes *"a frozen anchor tag A in {BI,DB,SYM}"* —
one tag. 917 has the principal enter a *nonempty subset*, with §4.1 requiring each candidate to carry
"one tag A frozen in `ANCHOR_TAG_CLASS_ENTRY`" and §4.2 requiring that *"every admitted tag must prove
the same joint state."* This is a **faithful generalisation that is strictly harder**: a larger
entered class adds cross-tag singleton obligations (JPO-02, JPO-06), and entering `{BI}` alone reduces
exactly to V003's discipline. **No widening.** The proofs 917 states for each row — DB reducing to BI
via trace preservation, SYM via fixed-state uniqueness plus scalar commutant — reproduce V003's own
anchor-theorem arguments.

```text
TEMPLATE = FAITHFUL
```

---

## 5. The certificates — a fourteenth is missing

### 5.1 The sweep, run over the sources' vocabulary rather than over 917's list

Enumerating 917's own thirteen against itself would be self-certifying. The sweep was therefore run
over the **certificate vocabulary of member 4 at its bytes**, which instantiates:

```text
Cert_anchor_IF(P_src) := ( B_ss,
                           exact matrix M_P of P_src restricted to Delta_0^ss,
                           exact factorization/inverse certificate for M_P,
                           CPTP/charge/superselection certificates,
                           finite fixed-space and mixing certificate,
                           exactly one anchor certificate: BI / DB / SYM )
```

and prices it separately as adoption item **A3 — "exact finite certificate format"**, whose stated
purpose is that it *"makes every datum finite-visible and pre-output."*

JAC-01…JAC-13 carry V003's `CO1`–`CO4` (as JAC-02/03/09/10/12), the nonempty/failure-capable family
(JAC-04), the reverse-A2 guard (JAC-05), and the joint-specific additions (JAC-06/07/08/11/13).
**They do not carry A3.** 917 requires `Cert_IF_joint` and `Cert_A_joint` to be *"exact and
replayable"* and never says what makes them so: no basis, no exact matrix on `Delta_0^joint`, no
factorization/inverse certificate, no fixed-space/mixing certificate. JAC-01 does not cover this —
it is the **carrier's** type certificate, not the **channel's** replay format.

```text
MISSING FOURTEENTH — proposed:

  JAC-14  EXACT CERTIFICATE FORMAT.  Every Cert_IF_joint and Cert_A_joint must be
          instantiated as a finite-visible, pre-output object: declared basis of
          Delta_0^joint; the exact matrix of Phi_joint restricted to it; an exact
          factorization/inverse certificate; CPTP/charge/superselection certificates;
          and the finite fixed-space and mixing certificate.
          Receiving test: reject any certificate that cannot be replayed from displayed
          finite data.
          Source: member 4's Cert_anchor_IF instantiation and its A3 pricing, at bytes.
```

**This is the under-constraint direction.** Without a format, "exact and replayable" is a promise
rather than a testable object, and a weak certificate could satisfy JAC-04's demand for "exact
witnesses" while being unreplayable.

```text
CERTIFICATES = AMENDED (+1: JAC-14)
```

### 5.2 Two further under-constraints

**(i) JAC-07's source marginal is indexed to a fiber the entry never pins.** JAC-07 requires the
source and record restrictions to *"reproduce sealed inputs."* The sealed source input is
`rho_S,[A] = I_A/Tr_A(I_A)`, defined **per admitted A0 rank fiber `[A]`**. A full-text scan of 917
returns **zero** occurrences of `rho_S` and no A0 fiber index; `JointCarrierEntry.v001` has twelve
fields and none of them is the fiber. So JAC-07 admits two readings and the instrument selects
neither: *reproduce at one `[A]`* — which requires the fiber to be entered, and it is not among the
entry fields — or *reproduce at every admitted `[A]`* — which is likely unsatisfiable, since distinct
fibers carry distinct states. **The same clause is under-specified on one reading and unsatisfiable on
the other, and it sits at the exact point where a rank ratio could enter.** One clause fixes it:
state whether `declared_joint_algebra`'s "content-addressed realization of sealed `A_C0`" pins the A0
realization, and if it does not, add the field.

**(ii) JAC-13 gates an automatic action on an unspecified certificate.** Automatic supersession
applies *"only to a sealed record-native derivation with the same receiver and a passed
equivalence/naturality certificate."* That certificate's content appears nowhere: `equivalence_scope`
in `JointCarrierEntry` and `JPO-10` both govern presentations **within** the adopted act, not a
**superseding** derivation's equivalence to it. **Automatic plus undefined is under-constraint**: a
weak equivalence claim could trigger silent replacement, which is precisely what JAC-13's own
failure-capable test says it must reject.

---

## 6. Obligations, gauntlet, and ledger

### 6.1 The eleven proof obligations — COMPLETE, and the proves-not-defines discipline holds

The load-bearing check is that the anchor theorem **proves** the state rather than **defining** it.
Member 4's discipline is *"The anchor theorem then proves, rather than defines, `d_state = …`"*.
917 preregisters:

> `Omega_anchor,C0` is proved at J4–J6 from the J0–J3 frozen carrier and anchor, **not supplied as a
> coordinate and not selected after any acceptance output.**

**Faithful.** And the J-order enforces it structurally: J2 verifies `Cert_IF_joint` and the tag
certificate, J3 closes the candidate set and exposes nothing, and only J4 solves — matching V003's
`T2 verify Cert_anchor_IF BEFORE solving` / `T3 solve` order exactly. JPO-06's singleton-image
statement reproduces V003's `StateImage` pattern. The eleven cover carrier, anchor theorem, mixing
uniqueness, input-faithfulness, nonemptiness, singleton image, stage/limit, three marginals,
common-origin, representation honesty, and coverage; no receiver in the sources is left without an
obligation.

```text
OBLIGATIONS = COMPLETE-11
```

### 6.2 The G0–G8 gauntlet — SOUND, with one tightening

Ordered and fail-closed; G2 is genuinely failure-capable (*"Perturb identity/unitality,
input-faithfulness, the DB adjoint equation, the SYM commutant/covariance equation, restriction maps,
and freeze order; each advertised control must bite"*); G8's mutation guard bars all four of the
obvious escapes, including counting repeated negative runs as a derivation.

**Blindness is properly enforced.** J3 exposes no downstream result; J8 exposes `Omega_C0` only after
J0–J7; JAC-12 requires *"timestamps, input-root hashes, and unread-output receipt"*; G6 states that a
failing K4 stops the run and *"the anchor is not revised, narrowed, or swapped after seeing that
result."*

**Tightening.** G6 publishes the two K4 controls by name and verdict. Publishing them is lawful and
correct — they are prior sealed results and acceptance criteria must precede the output — but JAC-03's
target-independence test is a *"fixed-string and semantic scan over the complete frozen manifest"* for
**downstream vocabulary**, and whether the control names count as downstream vocabulary is unstated.
**Naming the control classes in JAC-03's scan list closes that.**

### 6.3 The one-shot disclosure gap — the finding I would most want the principal to have

Three sealed facts, each verified at bytes, that 917 states separately and never connects:

1. **§4.2 (BI row):** the anchor theorem's conclusion is that *"the normalized joint identity/tracial
   functional is invariant… mixing uniqueness makes it the only joint state."* So **the history
   marginal is whatever measure the entered pairing/trace encodes.**
2. **§3 (reverse-A2):** 917 already knows this — *"If the pairing/trace on the commutative history
   factor already encodes a preferred measure, that fact must be stated as authored input."* JAC-05
   tests for it. **This is the instrument's single best feature: it names its own worst failure mode
   and puts a certificate on it.**
3. **G6 and member 7 at bytes:** the product-Haar control **fails K4**, because
   `integral_U(1) r dH(r) = 0` removes the charged character under fiber integration. And member 8
   fixes the carrier: *"`Lambda` is countable, so `X` is the countable product of circles"* — so `Y`
   is a compact abelian group, whose canonical normalized invariant measure **is product Haar.**

Chaining them: **the most natural pairing/trace on the history factor is the one G6 records as
failing.** The instrument would catch it correctly at G6 and stop, and G8 would forbid the swap —
the machinery works. But §8.5 makes that failure permanent (`JOINT_ANCHOR_INPUT` remains
`ADOPTED-AND-FROZEN`, `0/5`, `0/4`, and a non-equivalent replacement needs a **new** principal act),
and the instrument nowhere warns that the canonical carrier choice leads there.

**This leaks nothing.** G6 already publishes the controls; the connection to the carrier entry is what
is missing, and a principal cannot enter competently on a one-shot act without it.

### 6.4 The ledger — HONEST, with a declared scope limit

`§8.1` zeroes everything before the decision; `§8.3` mandates the exact `ADOPTED-AND-FROZEN` block
with `JOINT_ANCHOR_DERIVED = false` in **every** downstream `FREEDOMS-CONSUMED`; `§8.4` carries
`2/5` direct, `3/4` unblocked, `0/4` passed and states in terms that *"'Unblocked' means the dependent
construction/test may start… It does not mean its own receiver passes"*; `§8.5` keeps a **failed**
anchor on the books rather than discarding it, and zeroes both tallies. The unblocked-versus-passed
distinction is exactly the inflation this ledger could have committed and does not.

**Scope limit, declared rather than glossed:** I verified the tallies are carried consistently and
without inflation, and that they match the assignment's figures. **I could not independently verify
the denominators** — which five core lines, which four dependent lines — from the register at the
cited spans; those are the assignment's figures and 917 says so (*"The assignment's program-level
tally is carried without inflation"*). A registrar wanting the denominators audited should route that
separately.

```text
GAUNTLET = SOUND (one tightening)      LEDGER = HONEST (denominators not independently verified)
```

---

## 7. The over-constraint direction — reported separately, and clean

Every certificate and obligation was also tested for whether a **lawful** anchor could fail it.

| candidate over-constraint | finding |
|---|---|
| JAC-04 demands a passing **and** a failing witness | satisfiable: member 5's `C_BI` theorem supplies a nonempty passing witness and its replacement-channel countermodels supply failing ones. Must be re-established jointly — G1 says so in terms — but the shape exists in the record |
| JAC-11 demands family completeness | **disjunctive**: complete enumeration **or** a bounded class with a reopening trigger. The disjunction is what keeps it satisfiable; a bare completeness demand would have been over-constraining |
| JPO-06 singleton across **all** admitted tags | escapable: the principal may enter `{BI}` alone, which is V003's own discipline. A larger class is the principal's choice to make it harder |
| JPO-02 all tags fix the same functional | same escape; and member 6 supplies an independent proof that BI/DB/SYM converge at a fixed carrier |
| JAC-12's freeze receipts | mechanically satisfiable (timestamps, input-root hashes, unread-output receipt) |
| G2's perturbation battery | a failure-capability test, not an additional burden on a lawful anchor |

```text
OVER-CONSTRAINT: none found. No certificate or obligation is unsatisfiable by a lawful anchor.
```

The two places over-constraint was most likely — a completeness demand and a cross-tag singleton
demand — are both properly escaped, and the escapes are the sources' own discipline rather than
loopholes. **This direction comes back clean, and I report it as prominently as the findings.**

---

## 8. Typed controls (Q-797 discipline)

| control | type | source index |
|---|---|---|
| 27/27 span recomputation | **TRANSCRIBES** — establishes byte fidelity; decides no substance | 917 §1 |
| V003 tag-block comparison, §4 | **EXPLAINS** — shows the joint transposition is the source's own conditions, and where it is stricter | member 4 `anchor-certs` |
| certificate sweep over member 4's vocabulary, §5.1 | **ELIMINATES** — removes "the thirteen are complete" by exhibiting A3's format requirement outside them | member 4 |
| `rho_S` zero-occurrence scan, §5.2(i) | **ELIMINATES** — removes the reading that JAC-07 is well-defined as written | 917 full text |
| Haar/carrier chain, §6.3 | **EXPLAINS** — connects three clauses 917 states separately; introduces no new information, since G6 already publishes the controls | 917 §3/§4.2/G6; members 7, 8 |
| over-constraint battery, §7 | **ELIMINATES** — removes the over-constraint hypothesis, the failure direction I was required to test and did not find | 917 §5–§7; members 5, 6 |

---

## 9. FREEDOMS-CONSUMED (law 2a)

```text
CARRIED-AS-PARAMETER:
  917's verdicts and schemas as CLAIMED, at their declared closure;
  V003's authored anchor template and freeze discipline, at bytes;
  the no-go's seven-item benchmark and C_BI theorem, at bytes;
  the K4 fiber test and its two sealed controls;
  B ≅ C(Y) with Y a countable product of circles;
  the assignment's 2/5 and 3/4 tallies, unverified as to denominators;
  Q-818 / Q-821 as the registrar's, untouched.

CONDITIONED-ON:
  every fidelity verdict on the byte intervals 917 itself cites;
  section 6.3 on the entered pairing/trace being a product trace whose history
    factor carries the canonical invariant measure -- the natural entry, NOT the
    only admissible one, and section 3 of the subject expressly contemplates others;
  the JAC-14 proposal on member 4's A3 being the right source pattern for a JOINT
    certificate format, which is an analogy from the source carrier and is displayed
    as such rather than asserted.

SUBSTITUTED:
  NOTHING. No disposition, carrier, pairing, trace, tag class, tag, certificate,
  state, marginal, fiber, measure, or principal entry was chosen or filled. No verdict
  of the subject was moved. JAC-14 is PROPOSED to the producing lane, not installed.
  I took no position on whether the principal should approve or decline.

SCALING WEIGHTS:
  NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 10. Flattening check, verb audit, byte audit

**FLATTENING CHECK — S01–S37 walked, clean.** A matching span digest was not identified with a
correct claim about that span's content; §4–§6 check content separately. A faithful transposition was
not identified with an identical one — the strengthenings and the added limit-square clause are named.
A larger entered tag class was not identified with a weaker discipline; it is stricter. "Exact and
replayable" was not identified with a specified format. A published control was not identified with a
blindness breach, and the §6.3 connection was shown to introduce no information G6 does not already
publish. A one-shot disclosure gap was not identified with an unsound instrument — the machinery
catches the failure; the principal is under-informed before entering it. An under-specified clause was
not identified with an unsatisfiable one: §5.2(i) is reported as both-readings-fail, and §7 reports
the over-constraint direction as clean. My own 913 was not used as evidence about its own subject.

**BUILDER-NEVER-VERIFIES:** held in the clean direction. The subject is the opposite lane's. No
artifact of mine is load-bearing; member 5 was re-read at its own bytes rather than against my 913
description of it.

**SELF VERB AUDIT.** "Verified" is used for the 27 span recomputations and the byte-level content
checks. "Faithful" is used only where a 917 clause was compared to a sealed source clause at bytes.
"Missing" is used once, for JAC-14, and is sourced to a displayed source pattern rather than to my
judgement of what a manifest ought to contain. "Under-specified" is used where a clause admits more
than one reading, and both readings are displayed. "Clean" is used for the over-constraint direction,
which was tested rather than assumed. No adoption is recommended, opposed, or implied.
`VERB_AUDIT_SELF = CLEAN`.

**BYTE-POSITION SELF-AUDIT.** Measured on the sealed bytes of this file: §1's heading begins at byte
**629**, its closure fence opens at byte **1087**, and the exact member list ends at byte
**2694**. A token scan of bytes `[0,1087)` for absence-shaped forms — `no `, `not `, `none`,
`never`, `absent`, `missing`, `without`, `lack`, `fail`, `gap`, ` open`, `unresolved`, `underived`,
`false`, `cannot`, `zero`, `stop`, `block`, `wrong`, `refus`, `unswept`, `unsealed` — returns **zero
hits**.

---

## 11. Final lines

```text
CLOSURE = declared-first (byte position: sec-1 heading 629, closure fence 1087, members end 2694; pre-closure absence-token scan over 22 forms = 0 hits)
TEMPLATE = FAITHFUL (checked clause-by-clause against V003's sealed G1/BI/DB/SYM block at bytes; two lawful STRENGTHENINGS — DB's "no free reference density" closing the reverse-A2 hole, SYM's content-addressing — and one justified ADDITION, the limit-square commutation the joint carrier's stage structure requires. The subset-versus-single-tag difference is a faithful generalisation that is strictly HARDER, with {BI} reducing exactly to V003's discipline.)
CERTIFICATES = AMENDED (+1). JAC-14 EXACT CERTIFICATE FORMAT is missing: 917 requires Cert_IF_joint and Cert_A_joint to be "exact and replayable" and never specifies what makes them so, while V003 instantiates precisely that at bytes (basis of Delta_0, exact matrix, factorization/inverse certificate, CPTP/charge/superselection certificates, finite fixed-space and mixing certificate) and prices it separately as adoption item A3, "makes every datum finite-visible and pre-output". JAC-01 does not cover it — that is the carrier's type certificate, not the channel's replay format. Sweep run over the SOURCES' certificate vocabulary, not over 917's own list, which would have been self-certifying.
OBLIGATIONS = COMPLETE-11 (the proves-not-defines discipline is explicit and faithful — "proved at J4-J6 … not supplied as a coordinate and not selected after any acceptance output"; the J-order puts the certificate verification and candidate close BEFORE the solve, matching V003's T2/T3 exactly; JPO-06 reproduces the singleton-image pattern)
GAUNTLET = SOUND (ordered, fail-closed, genuinely failure-capable at G2, mutation guard sound at G8, and blindness properly enforced — J3 exposes nothing, JAC-12 requires an unread-output receipt, G6 forbids revising the anchor after seeing K4). ONE TIGHTENING: JAC-03's target-independence scan is for "downstream vocabulary" and does not say whether the G6 control names count; name the control classes in its scan list.
LEDGER = HONEST (unblocked-versus-passed distinguished in terms; a FAILED anchor stays ADOPTED-AND-FROZEN on the books rather than being discarded; both tallies zeroed on failure and before the decision; the ADOPTED-AND-FROZEN block with JOINT_ANCHOR_DERIVED=false mandated into every downstream freedoms block). SCOPE LIMIT DECLARED: I verified the tallies are carried without inflation and match the assignment, but could NOT independently verify the DENOMINATORS — which five core lines, which four dependent — from the register at the cited spans.
ADVOCACY = ZERO-CONFIRMED at bytes (two hits in the whole advocacy vocabulary; one is the anti-advocacy sentence itself, the other is "a preferred measure" and concerns a pairing, not a disposition). EMPTY-ENTRY CONFIRMED: eight [PRINCIPAL ENTRY] slots unfilled, no defaults, PRINCIPAL_ENTRIES_FILLED = 0.
OVER-CONSTRAINT = NONE FOUND, tested and reported separately (section 7): no certificate or obligation is unsatisfiable by a lawful anchor. The two places it was most likely — JAC-11's completeness demand and JPO-06's cross-tag singleton — are both properly escaped, by a disjunction and by the {BI}-only entry path, and both escapes are the sources' own discipline rather than loopholes.
VERDICT = RETURNED-FOR-REVISION (four items, none fatal to the design, all on the under-constrain or under-inform side of a ONE-SHOT act): (1) ADD JAC-14, the exact certificate format; (2) RESOLVE JAC-07's A0-fiber indexing — `rho_S` occurs ZERO times in 917 and JointCarrierEntry has no fiber field, so the clause is under-specified on one reading and unsatisfiable on the other, at the exact point where a rank ratio could enter; (3) SPECIFY JAC-13's equivalence/naturality certificate, which currently gates an AUTOMATIC supersession with an undefined object; (4) CONNECT the entered history-factor pairing/trace to the G6 control classes — the anchor theorem's output IS the trace-induced measure, Y is a countable product of circles so the canonical trace is product Haar, and G6 records product Haar as FAILING K4; the instrument catches this correctly but never warns that the natural entry leads there, while section 8.5 makes the failure permanent. This leaks nothing, since G6 already publishes the controls. THE INSTRUMENT IS WELL BUILT — its reverse-A2 clause names its own worst failure mode and puts a certificate on it, and the over-constraint direction is clean — but on an irreversible one-shot principal act, "well built with four fixable gaps" is not "ready".
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
