# Boundary Record-Onset Saturation and Action Gate v001

Date: 2026-07-23

## Purpose

This gate makes the adopted allow/require threshold quantitative without
using a mass, electromagnetic coupling, cosmological endpoint, or previous
alpha construction.

It separates:

```text
the universal action required to reach an orthogonal record;
the additional physical identifications needed to turn that action into a
source coefficient or an electromagnetic response.
```

## Proven geometric bound

Let `|R_0>` be the ready record state and `|R_1>` a conditionally generated
record state in the complete closed source-record-environment Hilbert space.
For unitary evolution generated by a possibly time-dependent Hamiltonian,
the Fubini-Study path-length bound is

```text
arccos |<R_0|R_1>|
  <= integral_cell d tau Delta H(tau)/hbar.
```

Distinct perfectly recoverable alternatives require orthogonal conditional
record states. Therefore

```text
<R_0|R_1> = 0

implies

integral_cell d tau Delta H(tau) >= pi hbar/2.
```

This is the standard Mandelstam-Tamm/geometric orthogonalization bound. It
applies to the complete closed dilation; it does not turn a reduced
nonunitary record channel into a fundamental nonunitary law.

## Adopted Gravacle onset rule

The allow/require boundary is adopted to mean the **first admissible durable
record onset**. At this boundary:

```text
the conditional records first become orthogonal;
the primitive path is a shortest projective geodesic;
the physical record interval and support are the unique causal cell required
by the Fundamental Boundary Record Action Principle.
```

Consequently the onset cell saturates the bound:

```text
A_write
  := integral_cell d tau Delta H(tau)
   = pi hbar/2.
```

This saturation is a new target-value-free, target-aware Level-1 Gravacle
principle. It is not derived from ordinary quantum mechanics: ordinary
quantum mechanics supplies the lower bound, while Gravacle identifies the
physical record threshold with equality. Historical target blindness is not
claimed, so a later genuinely unused prediction remains mandatory.

## Exact two-state realization

On the primitive record factor, a constant geodesic write generator may be
represented by

```text
H_write = E_write Y,
E_write tau_* / hbar = pi/2.
```

Then

```text
exp[-i H_write tau_*/hbar] |0> = |1>
```

up to an irrelevant overall phase. Any equatorial generator related by a
record-endpoint rephasing gives the same geodesic. The phase choice is not a
new magnitude.

The post-closure contrast operator is

```text
C_contrast = P_1-P_0.
```

It commutes with the endpoint projectors, while the write generator does not.
They are different operator directions:

```text
Tr(Y C_contrast)=0.
```

Therefore the orthogonalization bound fixes the integrated **write**
strength. It does not by itself identify either source coefficient
`kappa_I`, `kappa_Z`, or their pseudoscalar analog with `E_write`.

## What is fixed

```text
minimum integrated action for an orthogonal conditional record = pi hbar/2;
under the adopted onset rule, the primitive write cell saturates that value;
for a constant affine-speed realization,
  E_write = pi hbar/(2 tau_*).
```

## What remains open

```text
an explicit derivation of the numerical proper interval tau_* and cell
support from the causal boundary geometry;
the complete source-record-environment Hamiltonian;
the physical durability and stitching map;
the relation, if any, between E_write and kappa_I or kappa_Z;
the interacting source pole and residue;
the electromagnetic spectral response and alpha.
```

The route fails if a later step silently identifies `E_write` with a source
mass or pointer coefficient without deriving that equality from the complete
action.

## Exact next gate

Construct the complete one-cell action and test whether a single
parameter-free operator norm forces the write, source-odd, and post-closure
pointer blocks together. If their relative coefficients remain deformable,
the onset-saturation principle does not close the source scale.

## Status

```text
orthogonal_record_action_lower_bound_derived = true
allow_require_onset_saturation_adopted_Level_1 = true
historical_target_blindness_established = false
integrated_write_action_fixed = true
constant_geodesic_write_energy_in_terms_of_tau_derived = true
unique_causal_record_interval_numerically_derived = false
complete_source_record_environment_action_derived = false
physical_durability_derived = false
write_energy_identified_with_pointer_coefficient = false
write_energy_identified_with_source_mass = false
record_generated_source_mass_derived = false
spectral_evaluation_authorized = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
