# STAGE 8 — NDIST: THE ADVERSARIAL HUNT FOR A DERIVED |n|-DISTINGUISHER
## FABLE-LINEAGE ADVERSARIAL REVIEW — FABLE-NDIST — [CLAIMED]

Date: 2026-08-13
Role: ADVERSARIAL DETERMINATION / TYPING ONLY. One target: the sibling hinge
claim that a winding of `|n| > 1` is a faithful record of the quotient
`U(1)_rel / Z_|n| ≅ U(1)`, INDISTINGUISHABLE from `|n| = 1` on every derived
object. The hunt is for one derived, surface-native object `O` with
`O(|n|=1) != O(|n|>1)`. No value of `n`, `K`, `X`, `kappa_record`,
`kappa_Thomson`, `alpha`, any amplitude, coupling, scale, coefficient, or
constant is computed, bounded, estimated, or compared. Every numeric string
below is a sealed-text quotation or an exact structural integer.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held: BEDROCK ONLY — a distinguisher counts only if surface-native; it
is FALSE if it bottoms out on the internal/external conversion (rail import),
the faithfulness premise itself (circular), an imported scale/GR, or an
unbuilt/order-blocked object. No register, tracker, plan, road, ledger, or
lens file read. No git action. Output name probed before write: ABSENT.

---

## 0. VERDICT IN ONE LINE

**The distinguisher exists. The hinge claim is FALSE of record: the pair
(derived stabilizer action, ratified write) at winding `n` carries a derived
`Z_|n|` torsion fingerprint — the subgroup `ker(chi_n) ⊂ U(1)_rel` of
carrier symmetries that are PHYSICALLY ACTIVE by the sealed kinematics (the
null kernel is "exactly `U(1)_diag`"; the doubled-history orbit invariant is
COMPLETE) yet INVISIBLE to the write. It is trivial at `|n| = 1` and of order
`|n|` at `|n| > 1`. It is built from exactly two sealed objects — the
n-blindly derived stabilizer and the ratified one-character-power write — and
consumes no conversion junction, no scale, no GR, no unbuilt slot, and no
faithfulness premise. The quotient-degeneracy defense fails because "which
group is physically recorded" is not underdetermined of record: three
independent seals pin it canonically, and the sibling's own artifact consumes
that derivation (its N9) while its §4.4(c) requires it to be absent. The two
halves of the sibling artifact contradict each other, and the distinguisher
lives in the gap.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every member
rehashed by `shasum -a 256` at its path before reading; tasked members
verified against the tasked full digests (first 8 hex shown).

| # | Source | Seal | Role |
|---:|---|---|---|
| SUBJ | `STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md` | `f19bf3d7` MATCHES-TASKED (sidecar verified) | the hinge under attack: §4.4(c) quotient degeneracy; N9; the two-arms-one-object synthesis |
| W | `STAGE8_BARE_SURFACE_ALPHA_DETERMINATION_FABLE_V001.md` | `82e81f6e` MATCHES-TASKED (sidecar verified) | the winding `n_ch`, `chi_n`, the character lattice, the one-cell write, §5(a) |
| SF | `STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` | `1d11f150` MATCHES-TASKED | connection-only ground; the record's interacting object indexed by holonomy histories alone |
| T2 | `STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md` | `68cce523` sidecar verified | D3 one-write route (a statement about RESPONSES); composite windings in `nZ`; the NO-FAITHFULNESS fence this construction also holds |
| S1 | `STAGE1_PREMISE_DISPOSITION_V001.md` | `254128f7` rehashed at path (no sidecar; matches SUBJ's recorded rehash) | the kernel ladder; "faithfulness AFTER the response-null quotient"; the five-input list |
| P1 | `PRIMITIVE_RELATIVE_PHASE_CONNECTION_V001.md` | `45f6015c` rehashed at path (no sidecar) | `U(1)_diag` "changes no projective state or public record statistic"; the quotient map's kernel "exactly `U(1)_diag`" |
| P2 | `PRIMITIVE_RELATIVE_PHASE_STABILIZER_V002.md` | `42691711` rehashed at path (no sidecar; its lines 49-61 are quoted at seal by the citing corpus) | `Stab(P_0,P_1) = U(1) x U(1)`; the EFFECTIVE ACTIVE stabilizer `= Stab/U(1)_diag ≅ U(1)_rel`; `relative_character_lattice = Z` |
| U3 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U3_DOR008_DOR009_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `b0118d89` sidecar verified | THE COMPLETENESS SEAL: `Q_N := X_N/(G_N/U(1)_diag)` "canonically isomorphic to `U(1)^N`"; the trivial-acting stabilizer is "constant vertex transformations" only; the orbit invariant `q_N` is COMPLETE (TYPE-P, DoR-008/DoR-009) |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffce` sidecar verified | the ratified law `z = chi_n(h)`, `F_N = P_0 + Z_N P_ch`, exact at every finite `N` |

SUBJ and W are CLAIMED artifacts, engaged adversarially. TYPE-P statuses
(DoR-008/DoR-009) are carried, never upgraded. `STAGE8_TASK2D_...` and the
emergent-boundary artifact are consumed only as quoted inside SUBJ/W/T2 at
their verified seals.

---

## 2. THE CLAIM UNDER ATTACK, QUOTED AT FULL STRENGTH

SUBJ §4.4(c): for `|n| > 1`, `chi_n` "is a FAITHFUL character of the quotient
`U(1)_rel / Z_|n|`, which is again isomorphic to `U(1)`. Every ratified
response functional reads the stabilizer only through `chi_n` … so the
record's own dynamics cannot distinguish 'a winding-n record of the full
stabilizer' from 'a winding-1 record of the quotient stabilizer' — the two
readings agree on every derived object. Enforcing W's norm against the FULL
stabilizer therefore requires an n-blind access to the stabilizer that does
NOT pass through the write … EXACTLY the neutral comparand determination (1)
finds absent."

The claim quantifies over "every derived object" — not every derived
RESPONSE. The refutation below exhibits a derived object on which the two
readings do not agree. The comparison is run across the same hypothetical
winding family `{chi_n, n != 0}` the hinge itself quantifies over.

---

## 3. THE DISTINGUISHER — ONE OBJECT, THREE DERIVED FACES

### 3.1 The two sealed constituents [both bedrock; neither is the premise]

```text
(K1) THE STABILIZER, DERIVED CONCRETELY AND n-BLINDLY (P1, P2, U3).
     Stab(P_0,P_1) = U(1) x U(1) on the inherited endpoint rays; the null
     quotient removes EXACTLY U(1)_diag — the sealed nullity criterion is
     carrier-kinematic ("changes no projective state or public record
     statistic", P1) and the quotient map's "kernel is exactly U(1)_diag"
     (P1, P2). The result is the EFFECTIVE ACTIVE stabilizer U(1)_rel (P2 —
     "active" is the sealed word), with character lattice Z (P2). At the
     doubled-history level the same removal gives the physical orbit space
     Q_N = X_N/(G_N/U(1)_diag), "canonically isomorphic to U(1)^N", with the
     orbit invariant q_N (the per-cell relative holonomies) COMPLETE: "two
     pairs lie in the same common-gauge orbit exactly when their q_N values
     agree" (U3, TYPE-P DoR-008/DoR-009). Provenance is upstream of and not
     built from the write — certified by the SUBJECT'S OWN N9: "genuinely
     n-BLIND and genuinely NON-CHARGED in provenance."
(K2) THE WRITE, AT ITS RATIFIED FORM (M03, W). Per cell, exactly one
     character power: z = chi_n(h); Z_N = chi_n(relative holonomy of the
     doubled history); F_N = P_0 + Z_N P_ch, exact at every finite N. Held
     SYMBOLICALLY in n — the same hypothesis space as the hinge.
```

### 3.2 The object

```text
O(n) := the subgroup of the derived effective stabilizer whose action on the
        derived physical history space is nontrivial OF RECORD while its
        induced action on the ratified influence functional is trivial

      = { g in U(1)_rel : chi_n(g) = 1 }  =  ker(chi_n)  ≅  Z_|n|.

O(|n| = 1) = {identity}:  every physically active stabilizer element shifts
                          the write (chi_1 injective — S1's sealed kernel
                          ladder: "the identity alone for n=+1 or n=-1").
O(|n| = m > 1) = Z_m:     a nontrivial finite group. Each element g (an
                          |n|-th root of unity in U(1)_rel) is a non-identity
                          element of the ACTIVE quotient — of record NOT
                          null, since the null kernel is exactly U(1)_diag —
                          and shifting any one-cell doubled history by g
                          changes the COMPLETE orbit invariant q_N (a
                          different physical orbit, U3) while chi_n(g) = 1
                          leaves Z_N, F_N, and every derived response
                          functional identical (T2 D3: all responses pass
                          through the one write).

|O(n)| = |n|   — exact structural group arithmetic, the same class of fact
                 as S1's sealed kernel ladder. Nothing evaluated.
```

`O(1) != O(m)` for every `m > 1`. The Boolean face — "there exists a
physically active, response-invisible stabilizer element" — is FALSE at
`|n| = 1` and TRUE at `|n| > 1`.

### 3.3 The same fingerprint in two more derived spellings

```text
FACE 2 — FIBER MULTIPLICITY (orbit/fixed-point structure). On the derived
  one-cell physical orbit space Q_1 ≅ U(1) (U3: canonical, invariant
  complete), the ratified response r -> chi_n(r) has fiber cardinality |n|
  over every value: exactly |n| of-record-DISTINCT physical one-cell doubled
  histories share each response value. Count = 1 iff |n| = 1. This is a
  derived multiplicity/degeneracy count; U3's completeness theorem — not any
  norm — certifies the fiber members as physically distinct.
FACE 3 — LATTICE INDEX (composite-sector structure). The write-generated
  winding sublattice is nZ (T2 D3: every composite sector's winding is an
  integer multiple of the one write index). The AMBIENT lattice is the
  derived character lattice Z of the derived stabilizer (P2:
  relative_character_lattice = Z — sealed independently of the write).
  The index [Z : nZ] = |n| is a derived integer: 1 iff |n| = 1.
```

All three faces are the one `Z_|n|` torsion fingerprint of the quotient
`U(1)_rel / Z_|n|`, left exactly where the tasking predicted a mod-`|n|`
invariant could live.

---

## 4. WHY THE QUOTIENT-DEGENERACY DEFENSE FAILS OF RECORD

The hinge needs "which group is physically recorded" to be underdetermined —
re-identifiable as `U(1)_rel/Z_|n|`. Of record it is determined, three
independent ways:

```text
(a) THE NULL KERNEL IS SEALED AS EXACT. P1/P2: the removed direction is
    "exactly U(1)_diag", by a criterion (projective/record-statistic nullity
    on the carrier) that is write-independent. Reading the recorded group as
    the quotient requires enlarging the null set by Z_|n| — but those
    elements FAIL the sealed nullity criterion (they are non-identity
    elements of the sealed ACTIVE quotient). The re-identification is not a
    reading of the record; it is a contradiction of it.
(b) THE ORDER OF OPERATIONS IS SEALED. S1: faithfulness enters "AFTER the
    response-null quotient" — the quotient is taken and CLOSED (yielding
    U(1)_rel) before the character question is posed. The degeneracy defense
    re-runs a second, larger quotient AFTER the write, with
    response-invisibility as its criterion. No sealed source licenses a
    second quotient; and a nullity criterion of "whatever the write cannot
    see" would make faithfulness tautologically true at EVERY winding — the
    defense does not discover indistinguishability, it manufactures it.
(c) THE HISTORY SPACE IS SEALED CANONICAL AND ITS INVARIANT COMPLETE. U3:
    Q_N "canonically isomorphic to U(1)^N"; the only trivially-acting
    transformations are the constant vertex ones; "two pairs lie in the same
    common-gauge orbit EXACTLY when their q_N values agree." Histories
    differing by a kernel element are distinct physical orbits BY SEALED
    THEOREM. Declaring them "the same history" (the winding-1-of-the-quotient
    reading) is coarser than the derived equivalence and contradicts the
    completeness clause.
```

And the internal contradiction: SUBJ's §4.4(c) rests on the absence of "an
n-blind access to the stabilizer that does not pass through the write," while
SUBJ's own N9 books exactly such an access — `U(1)_rel` "derived from the
carrier's own kinematics … upstream of and not built from the write, hence
genuinely n-BLIND." Arm (A) established the absence of an n-blind
RESPONSE-VALUED COMPARAND for the equation `n^2 K = X`; §4.4(c) silently
widened that into the absence of any n-blind access to the stabilizer. The
conflation is the flaw. Distinguishing `|n|` does not require a response
equation; it requires exactly what N9 grants plus the ratified write form —
and the compound of two derived objects is derived.

Isomorphism does not rescue the defense: derived objects live on the
CONCRETE sealed structure, not the isomorphism class. The pairs
`(U(1)_rel acting on the carrier, chi_1)` and `(U(1)_rel acting on the
carrier, chi_n)` are non-isomorphic AS PAIRS — the order of the active-but-
invisible subgroup (`1` vs `|n|`) is an invariant of the pair — and the
quotient group has no derived action on the carrier at all (its elements are
cosets, not carrier operators; distinct coset representatives act
projectively differently, of record).

---

## 5. FENCE AUDIT — WHY `O` IS NOT A RAIL, NOT CIRCULAR, NOT AN IMPORT

```text
NOT A RAIL IMPORT.     No internal/external conversion junction is consumed:
                       both constituents live on the same side (the carrier
                       kinematics and the ratified functional — SF's
                       connection-only ground). No scale, metric, ell_P,
                       K_KK, KK ansatz, or GR object appears.
NOT THE FAITHFULNESS   Faithfulness ASSERTS O is trivial. Here O is
PREMISE (NOT CIRCULAR). CONSTRUCTED, per n, from (K1)+(K2), and comes out
                       nontrivial at |n|>1. The construction runs unchanged
                       under T2's explicit fence "NO FAITHFULNESS /
                       INJECTIVITY AS A PREMISE". Nothing is assumed about
                       kernels; the kernel is exhibited.
NOT UNBUILT /          One cell suffices (the one-cell write is sealed; U3's
ORDER-BLOCKED.         orbit theorem is instantiated TYPE-P). No Q_spec slot,
                       no Gate-5 object, no linked-cluster limit, and no
                       localization premise is consumed — the stabilizer
                       result is pointwise (P2) and the distinguisher is
                       pointwise. P2's open localization gate is untouched.
TYPING ONLY.           |O| = |n| and [Z : nZ] = |n| are exact structural
                       integers; chi_n(g) = 1 on |n|-th roots of unity is a
                       group identity. No physical value computed, bounded,
                       or compared.
```

Standing: the constituents carry TYPE-P (DoR-008/DoR-009) where their sources
do; the distinguisher inherits exactly that standing and no more — the same
standing on which the hinge claim itself operates.

---

## 6. WHAT THE FIND DOES TO THE SIBLING'S VERDICT CHAIN

```text
(1) The hinge sentence — "indistinguishable on every derived object" — is
    FALSE as quantified. O is a derived object; O(1) != O(n>1).
(2) SUBJ's §4.4(c) was its own "decisively" flagged leg: the argument that
    W §5(a)'s record-completeness norm CANNOT self-enforce because the two
    readings agree everywhere. With O derived, the norm has a derived
    discharge object: at |n| > 1 the record's own sealed content (the
    COMPLETE orbit invariant, U3) certifies physically distinct source
    histories — W §5(a)'s "kernel cosets … precisely those source histories
    the record would fail to record" — that every derived response
    conflates; at |n| = 1 that set is empty of record. The norm's referent
    is no longer normative talk: it is a sealed nonemptiness asymmetry.
(3) SUBJ's two-arms-one-object synthesis ("the two arms fail on the SAME
    single missing object") collapses: the missing object of arm (A) is a
    response-valued comparand (still missing — see §7), but arm (B) never
    needed one. The n-blind stabilizer access exists (N9, P1/P2, U3).
(4) Therefore FAITHFULNESS_STATUS = IMPOSED is unsupported at its decisive
    step, and the |n|-freedom conclusion (Finish B) loses its hinge. The
    winding magnitude is distinguishable of record; the sealed |n| = 1
    result's faithfulness input discharges against derived content (the
    active/invisible subgroup must be trivial for the record to record what
    its own kinematics certifies as distinct); Finish A reopens.
```

---

## 7. WHAT THIS DOES NOT SAY, AND THE OVERCLAIM AUDIT

```text
IT DOES NOT supply arm (A)'s comparand. No n-blind response-valued X with a
   derived equation n^2 K = X is exhibited; T2's DOES-NOT-FORCE for THAT
   equation stands untouched. The distinguisher is kinematic-vs-response,
   not a response crossing; it pins nothing about K, K_Q, kappa, or alpha.
IT DOES NOT evaluate anything. n, K, X, kappa_record, kappa_Thomson, alpha:
   SUBJECTS throughout. Displayed integers (1, |n|, k, indices) are exact
   structural integers; quoted strings are sealed text.
IT DOES NOT deny T2 D3/D4. All RESPONSES do pass through the one write and
   do cancel n in same-order crossings. O is not a response functional.
IT DOES NOT import the boundary, a scale, or GR; SF's import-block at g is
   left standing; no conversion junction is used or needed.
IT DOES NOT claim the panel must book faithfulness FORCED in one step. What
   is settled here: the indistinguishability hinge is false, the quotient
   re-identification contradicts three seals, and W §5(a)'s discharge
   object exists derived. The residual panel question — whether "the record
   separates what its own complete orbit invariant separates" is consumed
   as the ratified record-of-the-connection typing or booked as the norm's
   final step — is named, not smoothed over. On either booking, the
   sibling's IMPOSED verdict rested on a false universal.
IT DOES NOT touch the sign. ker(chi_n) = ker(chi_-n): O distinguishes
   magnitudes only, matching the tasked target |n| and the sealed
   sign-invisibility.
YOURS assemblies, marked: the compound object O (from sealed K1 + K2); the
   three-face identification; the exactness reading of the null kernel
   (quoted verbatim from P1/P2); the completeness reading of q_N (quoted
   verbatim from U3); the conflation diagnosis of §4 (from SUBJ's own N9
   vs §4.4(c)); the pair-invariant remark. None consumes a value, a scale,
   a conversion, or a faithfulness premise.
FLATTENING CHECK: "distinguisher exists" is not flattened into "alpha
   moves" (nothing numeric moves); "hinge false" is not flattened into
   "SUBJ worthless" (its arm (A) survey and T2's cancellation stand);
   the TYPE-P standing of the constituents is not flattened into
   unconditional; the residual booking question is stated, not hidden.
```

---

## 8. FINAL FLAG BLOCK

```text
DISTINGUISHER_FOUND = YES(O(n) = ker(chi_n) ⊂ U(1)_rel — the subgroup of the
  derived effective ACTIVE stabilizer that acts nontrivially on the derived
  physical history space (non-null of record: the sealed null kernel is
  exactly U(1)_diag, P1/P2; kernel-shifted histories change the COMPLETE
  orbit invariant q_N, U3) while acting trivially on the ratified influence
  functional (chi_n(g) = 1). O(|n|=1) = {identity}; O(|n|=m>1) = Z_m;
  |O| = |n|. Equivalent derived faces: the write's fiber cardinality |n|
  over the derived complete one-cell orbit invariant; the index
  [Z : nZ] = |n| of the realized winding sublattice in the derived character
  lattice. NOT A RAIL: no internal/external conversion, scale, or GR object
  consumed — both constituents are surface-side (carrier kinematics + the
  ratified write). NOT CIRCULAR: faithfulness asserts O trivial; here O is
  constructed without any injectivity premise, under T2's own
  no-faithfulness fence. NOT UNBUILT: one sealed cell suffices; pointwise;
  no Q_spec slot, junction, or localization premise consumed.)

CANDIDATES_TRIED =
  1. n-blind neutral response comparand X (n^2 K = X): does not derive
     (SUBJ arm A; unchallenged) — any junction route is a FALSE
     distinguisher (rail import, T1 U2 as carried). FAILED.
  2. Z_Q / Thomson-limit comparand: X = n^2 K_Q through the identical
     write; cancels exactly (T2 D4). FAILED.
  3. Cross-order response ratios (order-m scales as n^m): the n-content is
     a ratio of n-blind kernels — extracting it is a VALUE evaluation.
     FALSE distinguisher (typing fence).
  4. Composite-sector fusion/selection rules read as responses only: the
     realized windings nZ re-index as the full character lattice of the
     quotient — degenerate at response level. FAILED as responses;
     succeeds only as FACE 3 (index against the DERIVED ambient lattice Z,
     which the quotient reading cannot re-identify of record).
  5. Boundary legs — conservation datum Q_Sigma, exterior geometry,
     coefficient-ratio mechanism: PROVEN winding-blind (T1 as carried in
     SUBJ §4.3). FAILED.
  6. Neutral amplitude constancy / derived ACTION conversion / energy-form
     crossing: response-free / sealed not-made / underived coefficient
     absorbs n^2. FAILED.
  7. Anomaly-pair integrality (S1 Disposition 2): its input list includes
     "the primitive faithful U(1)_rel character" — faithfulness-fed and
     Level-1 adopted content. FALSE distinguisher (circular + import).
  8. Charge/flux-access elimination above n = 0: kills exactly the
     zero-variation response and nothing else. FAILED.
  9. Structural predicates on the derived response form (PSD, rank,
     diagonal flatness, Hermiticity, contractivity, monoidality,
     naturality): n-uniform of record (T2 D2/D4(ii); RF 5.1(1) as carried).
     FAILED.
  10. The response-null-yet-physically-active subgroup / fiber
      multiplicity / lattice index: FOUND (Section 3).

MOD_N_FINGERPRINT = FOUND(Z_|n| ≅ ker(chi_n) ⊂ U(1)_rel: the derived torsion
  subgroup of physically active, write-invisible carrier symmetries left by
  the quotient U(1)_rel/Z_|n| — order |n|, trivial exactly at |n| = 1; with
  its two derived restatements, fiber multiplicity |n| over the complete
  orbit invariant and lattice index [Z : nZ] = |n|.)

VERDICT = DISTINGUISHER-EXISTS(|n| forced; Finish A reopens — the hinge's
  universal ("indistinguishable on every derived object") is refuted by a
  derived object; the quotient re-identification contradicts three seals
  (null kernel exactly U(1)_diag; faithfulness ordered AFTER the one sealed
  response-null quotient; the doubled-history orbit invariant COMPLETE and
  canonical); the n-blind stabilizer access §4.4(c) declared absent is
  present of record and consumed by the sibling's own N9; and W §5(a)'s
  completeness requirement thereby discharges against derived content —
  kernel cosets are of-record-distinct physical histories the |n|>1 record
  provably fails to record, a set empty exactly at |n| = 1. The residual
  booking step for the panel is named in Section 7 and does not restore the
  hinge.)

GR_IMPORTED = no. SCALE_USED = no. INTERPRETIVE_INPUT_USED = no.
FAITHFULNESS_ASSUMED = no (constructed under T2's no-faithfulness fence;
  the premise's content is the OUTPUT trivial-O condition, never an input).
NOTHING_EVALUATED = CERTIFIED. No value of n, K, K_Q, X, kappa_record,
  kappa_Thomson, alpha, any amplitude, coupling, scale, coefficient, or
  constant was computed, bounded, estimated, or compared, at any step.
FENCE_RESPECTED = CERTIFIED. Bedrock only; typing only; seals verified at
  path before reading (tasked digests matched in full; sidecars verified
  where present; S1/P1/P2 rehashed at path); no register/tracker/plan/road/
  ledger/lens read; no git action; output name probed ABSENT before write.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false ; coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel check.
```
