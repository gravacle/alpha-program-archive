# STAGE 8 TASK 5 / EQ6 — THE DIAMOND FAMILY AND THE CONSUMER ARROWS, EXPLICIT — LANE 3 V001

Date: 2026-08-05  
Lane: Codex Lane 3 (Sol, high effort)  
Task: PASTE 564 V002 / register head Q-491  
Custody: manifest construction and obstruction report; review returns to Dario

## Lead result

The sealed corpus supports a complete walk of the ten finite-bottom
**coordinate families**, but it does not support the requested complete
geometric consumer-arrow manifest.  Two native arrow systems remain
unbridged:

```text
I_rec:
  the DoR-008 sequential zero-extension category;

I_F^surf:
  the Q-408 / Ref_path / flip actual-surface refinement category.   (L1)
```

The joint assembly supplies an opaque typed coordinate `BotStruct(f)` for
each already-admitted surface arrow `f`; it does not exhibit which concrete
native arrow each consumer requires, nor a realization functor carrying the
native sequential/Gate arrows to actual Q-408 stages with cycle rank and
`m_T`.  Treating the ten coordinates as ten geometric arrows, or simply
declaring that every consumer needs every surface generator, would repeat the
scope error in a different type.

The geometric diamond stock has the same boundary.  Three orbit schemas are
certified:

```text
D_RsubRsub       two certified `Ref_path`/common-parameter subdivisions;
D_CC^disjoint    two compatible flippable cycle additions;
D_CC^contact     two endpoint-contact flippable cycle additions.   (L2)
```

The mixed subdivision/cycle schema is named but jointly unbuilt.  The
asymmetric B2.6 schema has only its near legs; its far legs, target rank, and
six package overlap maps are absent.  The non-subdivision part of `RR` and
five further critical-pair sorts have neither a blanket square nor a proved
incompatibility.  The full family is infinite,
and the cited stock does not prove it finitely presentable.

Accordingly this artifact gives:

1. the complete ten-row native registry walk and the maximal six-schema
   incidence description;
2. the complete eleven-row critical-pair **status manifest**;
3. the exact coefficient-face scope on the certified diamond closure;
4. the exact machinery needed to turn the partial manifests into the
   requested full ones.

The census verdict itself remains decisive: the two certified
cycle-creating schemas already contain O1 and O2 positions, so the
coefficient-face obstruction is nonvacuous even though the global manifest
cannot be completed from current stock.

```text
REGISTER_HEAD = Q-491

CONSUMER_COORDINATE_ROWS_WALKED = 10
NAMED_SUBCONSUMERS_WALKED = 17
MAXIMAL_BASE_ARROW_SCHEMAS = 6

CRITICAL_PAIR_SORTS = 10
DISPLAY_ROWS_AFTER_CC_SPLIT = 11
CERTIFIED_GEOMETRIC_SCHEMAS = 3

MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
MEASURED_CONSTANT_COMPARISON = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 0. Preflight, custody, and authority

### 0.1 Access, no-clobber, and register

The Lane 3 bootstrap access checks passed for the cleanroom, archive
workspace, and supervision directory.  Before construction, neither the
local output, its seal, nor the archive mirror existed.  The supervision
register was read through its tail; Q-491 is the last substantive head.

Q-491 is used exactly at its declared scope:

```text
rank-preserving subdivision:
  FC6 defects vanish identically;

zero-source first-cycle primitive:
  the FC6 source test is vacuous;

positive-source flippable primitive:
  the zero defects are forced by stabilizer parity;

positive-source asymmetric primitive:
  the physical zero-defect section remains open,
  and this is the same object as guard B2.6 / clause (vi).       (P0)
```

No FC6 amendment is adopted here.

### 0.2 Hash-verified load-bearing inputs

| Input | SHA-256 | Use |
|---|---|---|
| guard V002 | `a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b` | exact clauses (i)--(vi), D-MIX, B2.6 |
| prior Lane 3 census | `676863b5ac68108a87dc6b5cf070908bdb15ad45d44a0f80edd473fe2c6ee245` | three prototypes, O1/O2, missing manifests |
| joint assembly V003 | `a46beae26048a0d093de339363fb8597d362a0101d103cfc06305edabac53f4c` | ten-entry `Bot_N`, typed `BotStruct(f)` coordinates |
| joint assembly V004 | `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` | corrected package composition |
| membership build | `0d00c9cfdb7187d45bb7892a5f71131b2cf1e0d5b17f871bdde3a770de2c0ae6` | prior four arrow classes and D1 boundary |
| cross-family review | `9caef0f7fa23b7f77fd2dfa27e003b706b3572419480e6333e8b851cfcfe2906` | D1 and repair item 4 |
| FC6 determination | `d032309d1c27a4dc882a6a6621c5374d8637a0578fc25ad0a9b1e6394545b009` | exact flippable/asymmetric split |
| J7/J2/typing repair | `9c74ed3dbe748450e525102128b0367ce21c0e5029929eab33a111ca9802dc3c` | `A_RP+`, O1, O2 |
| all-rank W1/W4 source | `dec994976774bf598e79cd496f6b424d777d2321df2b41c0393219a3247c3ad6` | the three executed prototypes |
| deeper flip construction | `a78c2450985429f6c9e152eb52ab6f133b013e1a1b1d695e2cc5241617b4bdb4` | flippable same-carrier family |
| flip cross-check | `0e61e6ebe8a7d69390c60f0fa1490c46a866c95e1f4a00ad6ffb7646bec62ee7` | admitted disjoint/contact flip squares |
| Q-408 primitive exhibit | `cd03cc87554b5ec0372e7b3b62662fac12e523f0ce9f63b08200aea9c2bd3e8c` | physical subdivision/common overlay on declared refinement scope |
| Q-243 finite transport | `70185aa842bc500724719c65bd66b5f07005e2214b97be0d35e07fd029d5c68c` | finite scalar shadow |
| Q-279 finite probe | `c7624d88aedaa0659755b5c566121b24c2ad0c29a0060daada43a31b22a6ddfb` | native bilocal zero-extension |
| Q-309 kernel sector | `a4c916a7cfa82c2130c82d8947c869f118e224959d7824bba45695711b4919c3` | old-kernel/mixing-zero restriction |
| Q-408 kernel calculus | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual surface path/current/kernel carrier |
| field sequential presentation | `76916244bdbcac7c2a6d4afae40f35127540d1d9e4cfc86fb72318506671161f` | exact `I_rec` typing |
| finite/SRE bridge determination | `9721dc049a79c0c9b9069ade6436ab93e3ff1266cfb25da753f39143c45794c5` | absence of realization bridge |

### 0.3 Status convention

`PROVABLE` marks a direct sealed consequence.  `PARTIAL_GENERATED` means a
finite schema/status description whose certified instances are displayed.
`UNBUILT` means required data are absent, not unequal.  `MACHINERY-APPEAL`
names the absent structural object and does not import it.

## 1. Symbolic stage and face classifier

For an actual finite **surface** stage `X`, write

```text
b(X) := beta_1(X),
H_X  := im(P_H,X),
m_T,X
 := P_H,X Loc_X Kernbar_X Q_X(L_T,X).              (S1)
```

The structural classifier is

```text
b(X)=0
  => H_X={0}
  => m_T,X=0                                      => ZERO;

b(X)>0
  => H_X may be nonzero, but the pairing
     <Loc_X Kernbar_X Q_X(L_T,X),c>_X
     is not proved nonzero                         => UNDETERMINED;

m_T,X!=0 with an independently sealed seed         => NONZERO. (S2)
```

No cycle-carrying stage in this manifest is promoted to `NONZERO` merely by
cycle content.

Define

```text
Z := {X : m_T,X=0},
P := {X : m_T,X!=0},
U := {X : b(X)>0 and sealed stock decides neither branch}.    (S3)
```

For a rank-preserving actual surface arrow `r:X->Y`, define

```text
A_RP+ := {r : X,Y in P};
A_RP0 := {r : X in Z or Y in Z};
A_RP? := the remaining cyclic, branch-undetermined RP arrows. (S4)
```

For a surface arrow `e:X->Y` with `Delta b(e):=b(Y)-b(X)>0`, define two
tagged coefficient positions:

```text
Old(e) := the licensed coefficient-bearing old harmonic image,
          present only when that image is exhibited nonempty;

New(e) := the target-only new harmonic sector,
          nonempty for a genuine cycle-rank increase.          (S5)
```

This definition also covers a composite arrow with `Delta b(e)>1`; no
rank-one factorization is assumed.

## 2. P1 — the finite-consumer registry and the exact manifest obstruction

### 2.1 The six maximal base-arrow schemas

The guard and Q-491 expose the following maximal incidence schema.  It is a
schema census, not yet a complete per-consumer endpoint manifest.

| Schema | Displayed arrow form | Source | Geometric coefficient class when typed |
|---:|---|---|---|
| `G1` | `sigma_(X,gamma):X -> gamma X` | guard (i) identities/surface isomorphisms | rank preserving; orbit action |
| `G2` | `w:X -> Y`, an unqualified W3/DoR-008 restriction | guard (ii) | `A_RP+`, `A_RP0`, or `A_RP?` if rank preserving; `Old/New` if rank increasing; native endpoints not all exhibited |
| `G3` | `s_p:X -> X[p]`, actual `Ref_path` subdivision | guard (iii) | rank preserving, with RP branch tag |
| `G4` | `c:X -> X_c`, flippable cycle attachment | guard (iv) | `A_CC` old image when nonempty plus target-only `New(c)` |
| `G5` | `k:X_e -> X_(e,e')`, a built common-refinement leg | guard (v), including isomorphism images | class determined by actual rank change; a leg label is not itself a rank proof |
| `G6` | `a_xi:G -> G_a`, asymmetric positive-source primitive orbit | guard (vi), B2.6, Q-491 | nonempty old image plus target-only new cycle; cyclic `m_T` remains `UNDETERMINED` |

`G6` is the one explicit clause-(vi) geometric witness.  The sealed corpus
does not prove that `G1`--`G6` extensionally exhaust each consumer's native
needed arrows.

### 2.2 The native sequential category is not a surface category

The DoR-008 field presentation displays

```text
Ob(I_rec)=N_(>=1),
Hom_Irec(N,M)={j_NM} exactly when N<=M,
j_NN=id_N,
j_ML j_NM=j_NL.                                  (C1)
```

Its zero-extension is

```text
j_NM^Lambda(n_1,...,n_N)
 =(n_1,...,n_N,0,...,0).                          (C2)
```

The same source says `(C1)` introduces no geometric refinement relation.
The bridge determination independently records that there is no sealed
functor carrying these stages/arrows to finite incidence/Q-408 stages with a
cellular differential and physical realization.  Therefore `b`, `H`, and
`m_T` are undefined on bare `N in I_rec`.

The joint assembly supplies the later package statement

```text
for every already-admitted surface arrow f:X->Y,
F_003(f)=(Rel_f,Eta_f,BotStruct(f;r_f^Bot),Aux_f),              (C3)

Bot_X=(Gate1-4_X,Q243_X,Q279_X,Q309_X,Q408_X,
       ActionBottom_X,WardBottom_X,ReaderBottom_X,
       WhereBottom_X,C1FaithBottom_X).                          (C4)
```

`(C3)` proves that every coordinate has a typed leg once `f` is admitted.
It does not display a realization functor from `(C1)` to the surface
category, does not identify the concrete native endpoints demanded by every
coordinate, and does not prove the converse claim that every surface
generator is needed by every consumer.

### 2.3 Complete registry walk

| # | Sealed consumer row | Native arrow/comparison actually displayed | Surface-arrow class obtainable from sealed stock | Remaining endpoint debt |
|---:|---|---|---|---|
| 1 | Gate 1, Gate 2, Gate 3, Gate 4 | sealed fixed finite restrictions/zero-extension comparisons; four coordinate entries in `(C4)` | none family-wide | no manifest sending every Gate source/target to an actual surface `X->Y` |
| 2 | Q-243 | the exact finite scalar shadow; its sequential restriction is represented by `j_NM`/zero extension on its native carrier | rank-preserving only after an actual surface lift | native-to-surface endpoints absent |
| 3 | Q-279 | `iota_NM` appends identity holonomies and zero bilocal components; the scalar/bilocal equality is exact | rank-preserving only after an actual surface lift | native-to-surface endpoints absent |
| 4 | Q-309 | exact old-kernel/mixing-zero restriction on its displayed exercise | `A_CC` old image for that exercise | no family-wide base-arrow list or target-only surface lift |
| 5 | Q-408 | actual `f:X->Y` on physical path/current/kernel stages; response old-image map `r_f^Bot:Bot_Y^resp->Bot_X^resp` | RP for `Ref_path`; old/new for actual cycle creation | consumer-wide equality with native Gate/DoR arrows not exhibited |
| 6 | action, Hessian | `Rel_f` and finite active-jet bottom entries over an already-admitted `f` | inherits the admitted `f` class | no independent clause-(vi) endpoint enumeration; `a_xi` R1 naturality absent |
| 7 | Ward, C3 | induced Ward/pushout restriction over an already-admitted `f` | full on RP, old-image projected on rank increase | no independent native endpoint enumeration |
| 8 | reader | sealed algebraic `ReaderBottom(f)` over an already-admitted `f` | inherits `f`; physical scalar J2 is separate | no independent native endpoint enumeration |
| 9 | WHERE, bundle | actual support restriction and pullback bundle transport over an admitted surface arrow | inherits `f` | native consumer demand set not listed |
| 10 | C1, Faith | finite norm/bound/separation legs over an admitted surface arrow | inherits `f` | native consumer demand set not listed |

The compound rows expand to

```text
Gate1, Gate2, Gate3, Gate4,
Q243, Q279, Q309, Q408,
Action, Hessian,
Ward, C3,
Reader,
WHERE, Bundle,
C1, Faith.                                         (C5)
```

Thus the sealed registry count is exact:

```text
CONSUMER_COORDINATE_ROWS_WALKED = 10,
NAMED_SUBCONSUMERS_WALKED = 17.                    (C6)
```

But the requested geometric manifest set

```text
A_cons^surf
 := {(C,e:X->Y,class(e),b(X),b(Y),m_T status at X,Y,
       every actual common-refinement partner of e)}           (C7)
```

cannot be filled from `(C1)`--`(C4)`: rows 1--4 lack a sealed geometric
realization, and rows 6--10 name typed package coordinates rather than
consumer-specific base-arrow demand sets.

### 2.4 B2.6 asymmetric-primitive rerun

The one definite consumer-indexed geometric arrow is retained:

```text
a_xi:G -> G_a,
b(G)>0,
b(G_a)=b(G)+1,
a_xi has actual Q-408 data and a finite bottom,
a_xi has no proved R1 naturality or six-component overlap maps. (C8)
```

Against a compatible flippable attachment `c:G->G_c`, the guard gives only
the attack template

```text
                    c
              G ----------> G_c
              |              |
         a_xi |              | a_(xi|c)  UNBUILT
              v              v
              G_a --------> G_(ac)       UNBUILT
                   c_|a
                                                        (C9)
```

The asymmetric decoration breaks the path-exchange stabilizer in the attack.
The sealed stock does not exhibit `G_(ac)`, the exact class of both far legs,
their common composite equality, or the six package overlap maps.  The attack
therefore has the same verdict after the registry walk:

```text
ASYMMETRIC_PRIMITIVE_RETAINED = yes,
R1_NATURALITY_ON_a_xi = absent,
FC6_PHYSICAL_SECTION_ON_a_xi = open,
ASYMMETRIC_COMMON_REFINEMENT = UNBUILT.             (C10)
```

No hard arrow is deleted; no unbuilt far leg is fabricated.

### 2.5 P1 verdict

The maximal source-backed result is

```text
CONSUMER_NATIVE_REGISTRY = COMPLETE(10 rows,17 names),
CONSUMER_BASE_SCHEMA = PARTIAL_GENERATED(G1--G6),
CONSUMER_GEOMETRIC_ARROW_MANIFEST = OBSTRUCTED.      (C11)
```

The exact obstruction is

```text
MACHINERY_APPEAL_P1
 := FINITE_INCIDENCE_REALIZATION_FUNCTOR
    + PER_CONSUMER_ENDPOINT_AND_VARIANCE_MANIFEST
    + ASYMMETRIC_FAR_LEG_DATA.                       (C12)
```

## 3. P2 — common-refinement critical-pair manifest

### 3.1 Primitive sorts and why the family is not known finitely presentable

Modulo admitted surface isomorphisms, the maximal source-backed primitive
sorts are

```text
R := rank-preserving actual surface restriction/subdivision;
R_sub := the certified `Ref_path`/common-parameter subdivision subclass of R;
C := flippable cycle-creating actual surface attachment;
X := asymmetric consumer primitive a_xi;
Z := native sequential zero-extension j_NM in I_rec.            (D1)
```

`A_CR` is a provenance tag on a far/common-refinement leg, not another
primitive sort.  A target-only new cycle is a position, not an arrow.

The ten unordered critical-pair sorts are

```text
RR, RC, RX, RZ, CC, CX, CZ, XX, XZ, ZZ.             (D2)
```

Splitting `CC` into the two admitted overlap modes gives eleven display rows.
The carrier/cycle-rank parameters are unbounded, so the individual family is
infinite.  No cited theorem proves that the unresolved rows are finitely
generated or proves them incompatible.  Hence no finite presentation of the
full `I_F` is claimed.

### 3.2 Complete eleven-row status manifest

| Row | Critical-pair sort | Maximal sealed status | Four-stage/class status |
|---:|---|---|---|
| 1 | `RR` | the `R_sub R_sub` subdivision subfamily is built; other `RR` pairs have no blanket square theorem | four `R_sub` legs for the certified cells in `(D3)`; remaining pairs UNINSTANTIATED |
| 2 | `RC` | guard D-MIX: Q-408/path/bundle routes stated; joint square unbuilt | schematic ranks only; displayed in `(D7)` |
| 3 | `RX` | no square and no incompatibility theorem | UNINSTANTIATED |
| 4 | `RZ` | no surface realization bridge | MAXWELL-UNTYPED |
| 5 | `CC-disjoint` | actual flippable common refinement built | four `C` legs; displayed in `(D4)` |
| 6 | `CC-contact` | actual endpoint-contact flippable common refinement built | four `C` legs; displayed in `(D4)` |
| 7 | `CX` | B2.6 attack template only | far legs/target unbuilt; displayed in `(D8)` |
| 8 | `CZ` | no surface realization bridge | MAXWELL-UNTYPED |
| 9 | `XX` | no square and no incompatibility theorem | UNINSTANTIATED |
| 10 | `XZ` | no surface realization bridge | MAXWELL-UNTYPED |
| 11 | `ZZ` | thin-category unique-path equality | internally built but nongometric; displayed in `(D9)` |

### 3.3 Certified geometric schema `D_RsubRsub`

```text
                    r:R_sub
              X ----------> X_r
              |              |
      s:R_sub |              | s_r:R_sub/A_CR
              v              v
              X_s --------> X_rs
                    r_s:R_sub/A_CR

s_r r = r_s s,
(b(X),b(X_r),b(X_s),b(X_rs))=(b,b,b,b).             (D3)
```

| Rank stratum | Four stage statuses | Face status |
|---|---|---|
| `b=0` | `ZERO,ZERO,ZERO,ZERO` | four stage O1 positions and four RP O1 positions |
| `b>0` without a seed | all four `UNDETERMINED` | branch-undetermined; formable iff all four legs are `A_RP+` |
| independently seeded nonzero component | all four `NONZERO` | all four legs `A_RP+`; full face formable |

The census prototype `D-sub` is the certified `b=1` instance.  Its stage
status is `UNDETERMINED`, not `NONZERO`.

### 3.4 Certified geometric schemas `D_CC^disjoint` and `D_CC^contact`

For `kappa in {disjoint,contact}`:

```text
                    u:C
              X ----------> X_u
              |              |
            v |              | v_u:C/A_CR
              v              v
              X_v --------> X_uv
                    u_v:C/A_CR

v_u u = u_v v,
(b(X),b(X_u),b(X_v),b(X_uv))=(b,b+1,b+1,b+2).       (D4)
```

For `disjoint`, the active interiors are disjoint.  For `contact`, their
interiors are disjoint and their closures meet only at the recorded endpoint;
exclusive-region tests remain active.

| Source rank | Four stage statuses | Old/new coefficient positions |
|---:|---|---|
| `b=0` | source `ZERO`; other three `UNDETERMINED` | `New` on all four legs; `Old` nonempty on the two upper legs only |
| `b>0` | all four `UNDETERMINED` absent a seed | `New` and nonempty `Old` on all four legs |

The census prototypes are

```text
D-disj    = D_CC^disjoint at b=0,
D-contact = D_CC^contact at b=0.                   (D5)
```

Their definite unformable positions are

```text
b=0 source stage                         -> O1;
four target-only new-cycle positions     -> O2-new;
two positive-source upper old images     -> O2-old. (D6)
```

### 3.5 Named but unbuilt `D_RC`

The guard's maximal schematic typing is

```text
                    r:R
              X ----------> X_r
              |              |
            c |              | c_r:C/A_CR  UNBUILT jointly
              v              v
              X_c --------> X_rc           UNBUILT jointly
                    r_c:R/A_CR

candidate ranks=(b,b,b+1,b+1).                     (D7)
```

The physical path/current and bundle routes are stated to agree.  The cited
stock does not exhibit one joint four-stage package square with the common
`rho`, C1, response, J2, and J15 data.  Therefore `(D7)` is a demanded schema,
not a certified actual diamond in the count.

If later built, its two cycle-increasing legs would carry O2-new, and O2-old
when their sources have nonempty old harmonic images.  That conditional
classification is not counted as an actual position here.

### 3.6 B2.6 `D_CX` attack template

```text
                    c:C
              G ----------> G_c
              |              |
         a_xi |              | a_(xi|c)  UNBUILT
              v              v
              G_a --------> G_(ac)       UNBUILT
                   c_|a
                                                        (D8)
```

The near asymmetric witness is positive-source and hence has cyclic source
and target; both `m_T` statuses are `UNDETERMINED`.  The target `G_(ac)`, its
rank, and both far-leg classes are not sealed.  No O1/O2 position is promoted
from this template into the certified census.

### 3.7 Internal nongometric `D_ZZ`

For `N<=M_1,M_2<=L` in `I_rec`:

```text
                  j_(N,M1)
              N ----------> M_1
              |              |
     j_(N,M2) |              | j_(M1,L)
              v              v
              M_2 --------> L
                  j_(M2,L)

j_(M1,L) j_(N,M1)=j_(N,L)=j_(M2,L) j_(N,M2).       (D9)
```

This is a unique-path equality in a thin sequential category.  It is not a
geometric common refinement.  Without the bridge in `(C12)`, `b`, `H`,
`m_T`, `A_RP`, and `A_CC` are undefined on its four objects/arrows.

### 3.8 Certified closure and completeness boundary

The exact certified geometric closure is

```text
D_cert
 := Cl_(covariance orbit, identity, transpose,
         whiskering by certified R_sub/C arrows,
         finite pasting)
       {D_RsubRsub,D_CC^disjoint,D_CC^contact}.      (D10)
```

Common parameter subdivision generates the certified `R_sub R_sub`
relations.  No blanket commuting-square claim is made for other `RR` pairs.
On `I_flip`,
finite compatible additions compose by actual local union; adjacent
transpositions are generated by the two certified `CC` cells.  This proves
the closure only on the declared certified fragment.

The full induction stops at the first `RC`, `CX`, or `Z`/surface interchange.
The following data are absent:

```text
MACHINERY_APPEAL_P2
 := MIXED_JOINT_SQUARE
    + ASYMMETRIC_COMMON_REFINEMENT_DATA
    + FINITE_INCIDENCE_REALIZATION_FUNCTOR
    + EXTENSIONAL_COMMON_REFINEMENT_EXHAUSTION.      (D11)
```

Consequently

```text
DIAMOND_MANIFEST_CERTIFIED = PARTIAL_GENERATED(3 schemas),
FULL_I_F_DIAMOND_MANIFEST = UNDETERMINED.            (D12)
```

## 4. P3 — coefficient-face scope table

### 4.1 Atomic positions on the certified family

For `D in D_cert`, let `V(D)` be its four stages and `E(D)` its four displayed
boundary arrows.  Pasted/composite boundaries are classified by `Delta b`,
not by an assumed rank-one factorization.  Define

```text
Stage(D,X) for every X in V(D);

RP(D,e)    for every e in E(D) with Delta b(e)=0;

Old(D,e)   for every e in E(D) with Delta b(e)>0
              whose licensed old coefficient image is exhibited nonempty;

New(D,e)   for every e in E(D) with Delta b(e)>0;

Route(D)   for the two equal composites.                         (F1)
```

The D2 contract demands every applicable position in `(F1)`; it supplies no
acyclic-stage or new-cycle exemption.

### 4.2 Exact unformable set on `D_cert`

Define

```text
U_O1^stage
 := {Stage(D,X) : D in D_cert, X in V(D), X in Z};

U_O1^RP
 := {RP(D,e) : D in D_cert, Delta b(e)=0,
                  e has an m_T=0 endpoint};

U_O2^old
 := {Old(D,e) : D in D_cert, Delta b(e)>0,
                  Old(e) is exhibited nonempty};

U_O2^new
 := {New(D,e) : D in D_cert, Delta b(e)>0}.          (F2)
```

O1 is the unformed scalar normalization

```text
lambda_X(h)=<0,h>_X/<0,0>_X.                       (F3)
```

O2-old is the scalar/response-bottom codomain gap on a nonempty old image;
O2-new is the absence of a scalar route on a target-only new-cycle sector.
No sealed `Bot^resp -> R` closes either position.

Let `cl_route` add `Route(D)` whenever a constituent position is
unformable.  Then the exact certified set is

```text
UNFORMABLE_SET_cert
 := cl_route(U_O1^stage union U_O1^RP
             union U_O2^old union U_O2^new).        (F4)
```

The obstruction tags may overlap.  “Unformable” means from sealed stock under
the coefficient contract, not under every future extension.

### 4.3 Branch-undetermined set

Keep separate

```text
B_stage
 := {Stage(D,X) : D in D_cert, X in U};

B_RP
 := {RP(D,e) : D in D_cert, e in A_RP?};

B_route
 := {Route(D) : D in D_cert,
                    every boundary has Delta b=0,
                    no vertex lies in Z,
                    some vertex lies in U}.          (F5)
```

These positions are formable on a future nonzero branch and O1-unformable on
the zero branch.  They are not entered in `(F4)` merely because the branch is
unsettled.

### 4.4 Exact formable support on `D_cert`

```text
FORM_stage
 := {Stage(D,X) : D in D_cert, X in P};

FORM_arrow
 := {RP(D,e) : D in D_cert, e in A_RP+};

FORM_route
 := {Route(D) : D in D_cert,
                    every boundary arrow lies in A_RP+};

FORMABLE_SUPPORT_cert
 := FORM_stage union FORM_arrow union FORM_route.    (F6)
```

The exact whole-diamond support is

```text
D_form^cert
 := {D in D_cert : every boundary arrow lies in A_RP+}.         (F7)
```

Only the `D_RsubRsub` portion can enter `(F7)`.  No cited representative, including
`D-sub`, is unconditionally proved to lie there.  This does not prove the set
empty.

### 4.5 Certified schema census

| Certified schema | Definite ZERO stages | Cyclic-stage status | Definite O1 | Definite O2-old | Definite O2-new | Whole route |
|---|---|---|---|---|---|---|
| `D_RsubRsub`, `b=0` | all four | none | four stages + four RP faces | none | none | unformable O1 |
| `D_RsubRsub`, `b>0` | none | all four `UNDETERMINED` absent a seed | none unconditionally | none | none | branch-undetermined; formable iff all legs `A_RP+` |
| `D_CC^disjoint/contact`, `b=0` | source | other three `UNDETERMINED` | source stage | two upper legs | all four legs | unformable O1/O2 |
| `D_CC^disjoint/contact`, `b>0` | none | all four `UNDETERMINED` absent a seed | none unconditionally | all four legs | all four legs | unformable O2 |

Thus the exact actual verdict does not await the missing global manifest:

```text
D-disj and D-contact contain demanded positions in UNFORMABLE_SET_cert;
therefore CENSUS = DEMANDED_UNFORMABLE.             (F8)
```

For the unresolved `RC`, `CX`, and bridge-dependent rows, `(F2)`--`(F7)` give
the exact **conditional classifier** once their stages/arrows are exhibited.
Their positions are not counted as certified actual positions now.

## 5. P4 — completeness boundary, anti-tuning, and verb audit

### 5.1 What is complete and what is not

| Object | Exact status | Completeness witness or obstruction |
|---|---|---|
| native bottom-coordinate registry | complete | all ten rows and seventeen names in `(C5)`--`(C6)` |
| maximal base-arrow incidence schema | partial-generated | `G1`--`G6`; per-consumer geometric endpoints absent `(C7)`--`(C12)` |
| certified geometric diamond schemas | partial-generated | three schemas `(D3)`--`(D6)` and closure `(D10)` |
| critical-pair status manifest | complete | all ten unordered sorts / eleven split rows in Section 3.2 |
| full actual `I_F` diamond family | undetermined | exact missing data `(D11)` |
| coefficient scope on `D_cert` | exact | obstruction `(F2)`--`(F4)`, support `(F6)`--`(F7)` |
| full-`I_F` enumerated scope | undetermined | depends on the missing full manifest; conditional classifier supplied |

### 5.2 Anti-tuning ledger

| Forbidden input/action | Use | Result |
|---|---|---|
| numerical value or magnitude of `m_T` | none | clean |
| inference `cycle present => m_T!=0` | rejected in `(S2)` and every stage table | clean |
| response, threshold, fixed-point, or end-test consequence | none | clean |
| member, frame, orientation, basis, or attachment binding | none | clean |
| measured-constant comparison | none | clean |
| tuning `Loc`, `P_H`, metric, or decoration | none | clean |
| deletion of hard consumer arrow | none; `a_xi` is retained in `(C8)`--`(C10)` | clean |
| native `I_rec` renamed as a surface category | none; type boundary `(C1)`--`(C4)` retained | clean |
| unbuilt far leg promoted to actual | none; `(C9)`/`(D8)` remain `UNBUILT` | clean |
| A6 bottom substituted for scalar J7 | none; O2-old/new retained | clean |
| infinite family silently truncated | none; certified schemas and unresolved critical pairs counted separately | clean |

### 5.3 Self status-verb audit

| Verb/status | Display supporting it | Audit |
|---|---|---|
| `COMPLETE` for native registry | ten rows and seventeen names `(C5)`--`(C6)` | exact only at native coordinate level |
| `PARTIAL_GENERATED` for consumer base schema | six schemas plus obstruction `(C7)`--`(C12)` | no geometric endpoint completion claimed |
| `COMPLETE` for critical-pair status manifest | ten sorts `(D2)`, eleven rows Section 3.2 | status enumeration, not actual-diamond certification |
| `PARTIAL_GENERATED` for diamonds | three certified schemas and closure `(D10)` | no mixed/asymmetric/bridge cell promoted |
| `ZERO` | acyclic implication `(S2)` | structural equality only |
| `NONZERO` | used only under an independently sealed seed | no cycle-content inference |
| `UNDETERMINED` | missing pairing witness `(S2)` | branch not promoted |
| `DEMANDED` | contract positions `(F1)` | no exemption added |
| `UNFORMABLE` | O1 `(F3)` and O2 sets `(F2)` | exact on certified positions |
| `FORMABLE` | `(F6)`--`(F7)` | only nonzero RP stages/arrows/routes |
| `MACHINERY-APPEAL` | exact objects `(C12)` and `(D11)` | confined to unavailable global manifests |
| `CLEAN` | anti-tuning ledger and this table | no verb outruns its display |

## 6. Final board

```text
CLAUSE_VI_ASYMMETRIC_WITNESS = RETAINED
CLAUSE_VI_FULL_GEOMETRIC_EXHIBITION = OBSTRUCTED

CONSUMER_NATIVE_REGISTRY = COMPLETE(+10 rows,+17 names)
CONSUMER_GEOMETRIC_ARROW_MANIFEST = PARTIAL_GENERATED(+6 schemas)

CRITICAL_PAIR_STATUS_ROWS = COMPLETE(+11)
CERTIFIED_GEOMETRIC_DIAMONDS = PARTIAL_GENERATED(+3 schemas)
FULL_I_F_DIAMOND_MANIFEST = UNDETERMINED

CENSUS = DEMANDED_UNFORMABLE
  (+D-disj/D-contact source O1;
   +all four new-cycle legs O2-new;
   +two positive-source upper old images O2-old)

MACHINERY_APPEAL
 = FINITE_INCIDENCE_REALIZATION_FUNCTOR
   + PER_CONSUMER_ENDPOINT_AND_VARIANCE_MANIFEST
   + MIXED_JOINT_SQUARE
   + ASYMMETRIC_COMMON_REFINEMENT_DATA
   + EXTENSIONAL_COMMON_REFINEMENT_EXHAUSTION

MEMBERSHIP_THEOREM = NOT_ASSERTED
AXIOM_ADOPTION = not_performed
MEMBER_SELECTION = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The commissioned `COMPLETE` / full `GENERATED` final statuses are not
mathematically available from sealed stock.  The following final lines state
the strongest displayed result rather than silently upgrading the missing
objects.

CONSUMER_MANIFEST = PARTIAL_GENERATED (+10 native coordinate rows; +17 named subconsumers; +6 maximal base schemas; +geometric endpoint/variance bridge MACHINERY-APPEAL)
DIAMOND_MANIFEST = PARTIAL_GENERATED (+3 certified geometric schemas; +unresolved non-subdivision RR subfamily; +1 named-unbuilt mixed schema; +1 attack-only asymmetric schema; +1 nongometric sequential square; +5 uninstantiated pair sorts)
UNFORMABLE_SET = exact_on_D_cert (+cl_route(U_O1-stage union U_O1-RP union U_O2-old union U_O2-new); full-I_F enumeration UNDETERMINED)
FORMABLE_SUPPORT = exact_on_D_cert (+FORM_stage union FORM_arrow union FORM_route; full-I_F enumeration UNDETERMINED)
VERB_AUDIT_SELF = CLEAN
