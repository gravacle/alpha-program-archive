# STAGE 8 — P-1 DILATION CLOSURE: INDEPENDENT AUDIT OF 631356a3 (P1-BUILD)

## INDEPENDENT AUDITOR — CODENAME P1-AUDIT — [SEALED]

Date: 2026-08-14
Role: independent auditor, briefed to REFUTE. Default stance: the build is
wrong until each claim survives re-derivation by routes the build did not
use. Everything below worked ONLY from sealed artifacts verified at path;
own CAS battery (sympy 1.14.0, fresh venv `p1auditvenv` under /private/tmp
scratch), exact symbolic only, nothing numeric evaluated, L formal
throughout. No register/tracker/plan/road/ledger/lens file read. No git
action. ONE file written (this artifact) plus its seal.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — the build's PARTIAL-DELIVERED determination survives independent re-derivation by routes it did not use (structural/generating-kernel dilation identity for ALL degrees at once, projector-kernel covariance, full even-ladder Parseval escape, solution-set intersection, bijection-ray algebra: 15/15 PASS, fresh venv, exact symbolic only); both tasked attacks FAIL — no counterexample exists to the declared-reading family closure, and the frozen-execution counterexample survives attack ((n, sqrt2) is genuinely a member at PA/BASELINE bytes and its dilation image at oscillator length 2 is genuinely outside, contained in NO fixed-ell member at ANY truncation, with exact quantified membership defect); the consuming-step identification is correct at the chain's bytes (Route III's class-reduction inequality consumes FAMILY-level closure only; member-wise closure fails for every L != 1 and is consumed nowhere); the dissolution of P-1 into the residue's ell-ray quantifier width is faithful to ATTEMPT :366-370 and contradicts no sealed byte; ONE correction, named exactly: the build's §1 annotation "no sidecar exists" for BASELINE is FALSE (a matching standalone sidecar exists at path) — the digest verification itself is unaffected; V-1..V-6 severally NOT ENGAGED, the delivered alpha' = 1/4 record and Q-1050's residue statement unmoved.**

---

## 1. SEALS INDEPENDENTLY VERIFIED AT PATH (shasum -a 256, full digests recomputed THIS session)

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest below recomputed by me from bytes at path; matched against sidecars
where sidecars exist, and against PA's sealed frozen-authority table
(:26-27) for the two inherited authorities. NONE UNVERIFIABLE.

```text
631356a36ed93cd61eab1b0177bbbe456eb1fcfc51ef9b5eca4cdc388910016b  STAGE8_P1_DILATION_CLOSURE_V001.md  MATCH (UNDER AUDIT; = tasked digest; sidecar present and MATCH)
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH (sidecar; ATTEMPT — P-1 named §2.5 :355-363; Route III :334-353; consequence :366-370; i-2 :941-946; flag (iv) :984-985)
38aa39d16f3fac1ffd037d064b8504edcc641c93ddbdcbbcbdf7ed01dff8787a  STAGE8_R3C_SUMMED_REMAINDER_AUDIT_V001.md  MATCH (sidecar; AUDIT — §2.4 :143-167 P-1 real CONFIRMED; :160-165 no det compression monotonicity sealed)
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md  MATCH (sidecar; DISCHARGE — §2.3 STEPS 1-7 :179-236; STEP 5 premise :208-219; class scope :183-185; r-A2 :319-325; §3 q-i/q-ii :341-367)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (sidecar; PA — A1 carrier :48-74; construction :75-128; frozen polydisc :137-142; frozen-authority table :26-27)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (sidecar; E1 — D3 :417-426; R.1 :679-689 incl. :684-686; R-L0/R-L0b :851-894 incl. :864-865)
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md  MATCH (sidecar; S2CAND — S2'-O :181-213; R-L2b^Sigma :236-246; V-1..V-6 :417-444)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md  MATCH (sidecar; REM — :380-382 projector-blind compression line; :450 family sup)
80aa4e1722c117e8195ce0fb2ae3fc37262bc8fc2ba5d79a6dd38cd1029bc26d  STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.md  MATCH TWICE: vs PA frozen-authority table :27 AND vs the standalone sidecar STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.seal.sha256, WHICH EXISTS (correction to the build, §3.2 item c-1) — family declaration :32-44; Freeze :46-53; :55-56
a1258dcf40732f0e3fce358a68ffdbd34bc347d70283550440582a1129bec510  STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md  MATCH (vs PA frozen-authority table :26; no standalone sidecar exists — build's claim TRUE for this row) — defining class :43-51; :53
```

Every span the build cited was re-read by me at its location and found as
cited (one sub-line imprecision noted, §3.2 item c-2). My own corpus sweep
(independent of the build's): every workspace artifact matching
"oscillator length", "D3/Hermite", "ell in {", "closed under dilation",
"dilation closure" was located and its context examined — the executed
values n = 2 (with {2,4} at BASELINE), ell in {1, sqrt2} appear ONLY in
execution/citation context (CONTROL4, ENVELOPE, RL4RL0, N48, G7, E1 F'-5
fence at :1695); NO artifact pins the family's ell-extent as a
definitional matter, and NO artifact other than DISCHARGE/ATTEMPT/the
build addresses the closure question. The build's corpus-sweep claim is
CONFIRMED.

---

## 2. THE ADJUDICATION

### 2.1 What the build had to get right, and how I attacked it

The build's central determinations, each attacked by a route the build did
not use (battery labels in brackets; script and output verbatim in §2.5):

```text
b-1  THE DILATION IMAGE IS EXACT AND DEGREE-INVARIANT:
     U_L u_k(.;ell) = u_k(.; L ell), so U_L H_(n,ell) = H_(n, L ell) and
     U_L^dag Q_(n,ell) U_L = Q_(n, ell/L); the covariance's index map is
     (n, ell) -> (n, ell/L), truncation level n INVARIANT.
     Build's route: degree-by-degree simplify (k = 0..3), Gram + mixing
     integrals. MY ROUTES: (i) the STRUCTURAL mechanism — u_k(x;l) =
     l^(-1/2) F_k(x/l) with F_k the unit-length profile [A1, verified
     k = 0..5], from which the identity follows for EVERY k by one line
     of surd algebra (1/sqrt(L)/sqrt(ell) = 1/sqrt(L ell), pinned in A1);
     (ii) the Hermite GENERATING KERNEL in closed form — no series, no
     degree instances: L^(-1/2) G(t, x/L, ell) = G(t, x, L ell) exactly
     [A2], which carries every degree simultaneously as formal
     t-coefficients; (iii) the CHRISTOFFEL-DARBOUX PROJECTOR KERNEL,
     basis-free: L K_n(Lx, Ly; ell) = K_n(x, y; ell/L) for n = 1, 2, 3
     symbolically [B] — this IS U_L^dag Q_(n,ell) U_L = Q_(n, ell/L) at
     kernel level (conjugation kernel rule [U_L^dag A U_L](x,y) =
     L K(Lx,Ly), derived in the script comment). The 3D index cube
     {0 <= a,b,c < n} is preserved componentwise (per-axis action,
     L^(-3/2) = (L^(-1/2))^3); the spinor factor C^4 is untouched
     (spatial unitary). CONFIRMED — and the build's d-2 compression
     display re-checked by operator algebra: inserting U_L^dag U_L,
     Q_(n,ell) V^(L)(a) Q_(n,ell) = U_L[Q_(n,ell/L) V^(1)(La)
     Q_(n,ell/L)]U_L^dag needs exactly U_L Q_(n,ell/L) U_L^dag =
     Q_(n,ell), the ell -> L ell instance of [B]. On L <= 1 (DISCHARGE's
     sealed class scope :183-185): ell/L >= ell. CONFIRMED.
b-2  BOTH STATE SCHEMES RIDE THE MAP. Build's route: one component
     identity + reduce_inequalities. MY ROUTE: the FULL 4x4 massless
     Dirac band projector P_-(k) = (I - alpha.k/|k|)/2 as a matrix:
     P_-^2 = P_-, H(k)P_- = -|k| P_-, tr P_- = 2 [G1], and
     P_-(k/L) = P_-(k) as a 4x4 MATRIX identity at symbolic k, L [G2]
     (C^mix = Q P_- Q conjugates to Q' P_- Q'); for C^pure =
     1_(-inf,0)(Q h_0 Q): a generic symmetric matrix A and A/L have THE
     SAME eigenvectors with eigenvalues scaled by 1/L > 0 [G3] — the
     negative-spectral subspace is identical, so the indicator is blind
     to the positive factor 1/L. CONFIRMED.
b-3  det/tr CONJUGATION INVARIANCE (Route III's display R^(L)_(n,ell)(a)
     = R^(1)_(n,ell/L)(La)): re-pinned on a FULLY SYMBOLIC generic 2x2
     with exact adjugate inverse [H] (record: RA8a/CA5a on unitary/3x3).
     CONFIRMED.
b-4  THE EXPONENT ARITHMETIC: |C_L|_4 = pi L^4/24 re-derived by TWO
     routes the build did not use — the t <-> L-t symmetry route and the
     pure L^4 scaling route (t = L tau substitution against the
     unit-cell value pi/24) [F1]; L^2 = (24/pi)^(1/2)|C|_4^(1/2) and the
     identity (L eps)^m = L^2 L^(m-2) eps^m [F1]; the collapse
     inequality's sign content L^(m-2) <= 1 for the INTEGER Dyson orders
     m >= 2 the display quantifies, by the GEOMETRIC FACTORIZATION
     1 - L^r = (1-L) sum_(i<r) L^i with both Piecewise branches handled
     and each factor's sign pinned [F2] (plus the real-m exponent route:
     L^(m-2) = exp(-(m-2)log(1+w)) under the exact bijection
     L = 1/(1+w), exponent sign pinned nonnegative). CONFIRMED.
```

### 2.2 The DELIVERED horn attacked — hunting a counterexample to declared-reading closure

Task: exhibit (n, ell, L) with the image H_(n, ell/L) OUTSIDE the
ell-parameterized realization (s-2, ell a range-unrestricted parameter at
BASELINE :34-44 / PA :50-61). Candidate failure modes, each closed
exactly:

```text
a-1  DEGREE MIXING under U_L: REFUTED by [A1]/[A2]/[B] — the identity is
     degree-wise exact with matched-basis matrix the identity; the image
     of the n-cube span is the n-cube span at L ell. No mixing exists.
a-2  NORMALIZATION/ORTHOGONALITY LOSS: REFUTED — U_L unitary (S2g of
     record); [B]'s kernel identity preserves the projector property.
a-3  SPINOR-FACTOR DISTORTION: REFUTED — the dilation is spatial;
     C^4 untouched; [G1]/[G2] pin the band projector's blindness.
a-4  DEFINING-CLASS (s-1, PROVENANCE :46-51) FAILURE AT ell/L: REFUTED —
     at ANY fixed oscillator length ell' > 0 the family {H_(m,ell')}_m
     is finite rank, nested, Q_(m,ell') -> I strongly (Hermite density
     at fixed length), and the core/compression conditions transfer
     under the fixed unitary conjugation: a core for h_0 maps to a core
     for U_L^dag h_0 U_L = h_0/L, which shares h_0's domain and an
     equivalent graph norm (constants max(1, 1/L) formal). The build's
     §2.2 parenthetical is correct as stated.
NO COUNTEREXAMPLE EXISTS. Family closure at the declared bytes, with the
ray criterion (ell-extent an upward multiplicative ray), is CONFIRMED:
my route for the criterion is the BIJECTION s = 1/L of (0,1] onto
[1, oo) (no calculus): {ell/L : L in (0,1]} = ell·[1, oo) [E], and ray
closure of [c, oo) by the exact decomposition es - c = (e-c)s + c(s-1),
each term nonnegative [E]. (Build's route: derivative sign + limit,
P1g.) CONFIRMED.
```

### 2.3 The REFUTED horn attacked — is the counterexample genuine at the bytes?

Task: (i) is the exhibited object genuinely in the family at the frozen
reading's bytes? (ii) is its dilation image genuinely outside?

```text
x-1  MEMBERSHIP AT THE BYTES: the escaping member carries ell = sqrt2 —
     executed and retained at PA :67, :72-73 ("Both `ell` values are
     executed and retained") and BASELINE :50. GENUINE.
x-2  IMAGE OUTSIDE, INDEX LEVEL: my route is per-member SOLUTION-SET
     INTERSECTION (build's was pairwise-consistency enumeration):
     {L : 1/L in E} = {1, 1/sqrt2} and {L : sqrt2/L in E} = {sqrt2, 1};
     intersection = {1} EXACTLY [D]. The executed set is closed under NO
     nontrivial class dilation; at L = 1/sqrt2 the second member's image
     is sqrt2/(1/sqrt2) = 2, and {2} ∩ {1, sqrt2} = ∅ (exact surds) [D].
     CONFIRMED.
x-3  IMAGE OUTSIDE, SUBSPACE LEVEL — MY STRONGER ROUTE (the build used
     non-polynomiality of the exp series, P1e): the FULL EVEN-LADDER
     EXPANSION. c_j := <u_2j(.;ell), u_0(.;ellp)> has the exact closed
     form sqrt(s)·sqrt((2j)!)/(2^j j!)·(-q)^j, s = 2 ell ellp/(ell^2 +
     ellp^2), q = (ell^2 - ellp^2)/(ell^2 + ellp^2), verified j = 0..3
     by exact integration [C1]; Parseval closes EXACTLY at the summed
     level: s·sum_j binom(2j,j)(q^2/4)^j = s/sqrt(1-q^2) = 1, the
     central-binomial branch condition q^2 < 1 pinned by the exact
     identity 1 - q^2 = s^2 with s^2 > 0 [C2]; q = 0 iff ellp = ell
     (unique positive root) [C3]. HENCE: for ellp != ell EVERY even
     ladder coefficient is nonzero, so u_0(.;ellp) lies in NO finite
     truncation H_(m,ell) — with the membership defect QUANTIFIED
     exactly: 1 - c_0^2 = q^2/(1 + sqrt(1-q^2)) > 0 and 1 - c_0^2 -
     c_1^2 = 1 - s(1 + q^2/2) > 0 [C4] (every finite partial sum falls
     short of Parseval's 1). Instances for the exhibited escape: (ell,
     ellp) = (1, 2): q = -3/5; (sqrt2, 2): q = -1/3 — both nonzero by
     [C3]'s zero-set. The dilated member escapes EVERY fixed-ell member
     at EVERY truncation level, over BOTH executed ells. CONFIRMED, and
     STRENGTHENED (the build's claim is the qualitative corollary).
x-4  Note: the build's P1e closed-form guess for the ladder coefficient
     is not in its artifact (it used a different, valid route); my [C1]
     found the integral's own sign is (-q)^j — recorded here because an
     exact-surd audit must match the integral, not a convention. No
     bearing on any build claim (only q^2 and nonvanishing are consumed).
THE COUNTEREXAMPLE SURVIVES BOTH ATTACKS. The frozen-execution horn's
REFUTED verdict is CONFIRMED.
```

### 2.4 The quantifier audit (tasked item 2) — which closure the chain consumes, at the exact step

```text
q-1  THE BUILD DISTINGUISHED member-closure from family-closure at its
     c-0 vs c-1/c-2, and identified the consuming step as Route III's
     class-reduction inequality (*) — the inclusion image_L(F) ⊆ F'
     inside ATTEMPT §2.5's consequence clause. CHECKED AGAINST THE
     CHAIN'S BYTES MYSELF: ATTEMPT :338-345 derives R^(L)_n(a) =
     R^(1)_n'(La) "at fixed carrier n" with n' the moved index —
     well-defined UNCONDITIONALLY (the dilated projector exists, [B]);
     the premise is CONSUMED exactly when the hypothetical unit-cell
     bound Q^Sigma_1 (":366-370 IF ... ever exists") is APPLIED at n' —
     which requires n' inside Q^Sigma_1's certification range. That is
     the build's (*), faithful. DISCHARGE :217-219 names the premise at
     STEP 5 in family terms ("the D3/Hermite-Galerkin carrier family is
     closed under dilation"); member-wise closure appears NOWHERE in the
     chain (no step conjugates a member onto itself), and it is FALSE
     for every L != 1 (my [C3]/[C4]: 1 - c_0^2 > 0 at ellp = ell/L,
     q = (L^2-1)/(L^2+1) != 0 — same content as the build's P1d by a
     different object). CONFIRMED.
q-2  FIRST ORDER CONSUMES NO MEMBERSHIP: DISCHARGE r-A2 :319-325 +
     REM :380-382 re-read at bytes — the compression line ||Pi X Pi||_2
     <= ||X||_2 holds for ANY orthogonal projector (projector-blind);
     the first-order chain runs STEP 6's unitary-invariance EQUALITY
     into REM's carrier-free domination of the uncompressed object.
     P-1 is real ONLY at determinant level (AUDIT :143-167, :160-165:
     no det compression monotonicity sealed — re-read, as cited).
     CONFIRMED.
q-3  THE n-AXIS SETTLEMENT: "their union is a core" (BASELINE :43-44)
     is FALSE for any finite n-set (a finite union of finite-rank
     subspaces is a proper closed subspace — never dense, never a core),
     so the family's n-extent is unbounded by the declaration's own
     sentence; independently forced by the defining class's "Q_n -> I
     strongly" (PROVENANCE :47, impossible for a finite n-family) and by
     every consuming quantifier (REM :450 sup over the family; S2CAND
     :204-205 "n-INDEXED FAMILY"; PA :63 "the first nontrivial member").
     TRIPLE-GROUNDED; the build's n-half settlement is CONFIRMED, and
     with it the LOCALIZATION: the one unpinned byte is the ell-freeze's
     scope (family vs execution) — exactly r-A2's "spec-reading no
     artifact pins", now localized. My independent corpus sweep (§1)
     found no pinning byte. CONFIRMED.
q-4  THE DISSOLUTION (build e-3): the reduction needs precisely F' ⊇
     ∪_{L in (0,1]} image_L(F) = the ell-axis ray closure of F ([E]:
     for frozen F this is n × ([1,oo) ∪ [sqrt2,oo)) = n × [1,oo); for
     declared F it is F itself). Under the declared reading P-1 HOLDS
     DERIVED and the reduction rests on derived ground; under the frozen
     reading the standalone closure statement is FALSE yet the reduction
     re-lands on Q^Sigma_1 certified over the ray range — every ray
     member a genuine Galerkin carrier (§2.2 a-4). EITHER WAY the
     operative residue obligation is the certification's quantifier
     width — reading-independent. E1 :684-686's discipline (constants
     carry no n, no ell) already forces the certified CONSTANT'S shape;
     the ray widens only the STATEMENT'S range (consistent also with
     E1 :1695's F'-5 fence barring carrier-indexed bounds). A residue
     certified only at the executed two-member ell-set cannot feed (*)
     — the first nontrivial refinement already exits the set ([D]).
     FAITHFUL AND CONFIRMED. This does NOT contradict the sealed AUDIT's
     "P-1 real": the build preserves that finding (no r-A2-style
     discharge is claimed; the second-order reduction still cannot run
     today — Q^Sigma_1 does not exist); it narrows what the premise IS
     at the consuming step. The sealed ATTEMPT's phrasing ("a unit-cell
     n-uniform bound") is SHARPENED, not contradicted: the build makes
     the ell-quantifier width explicit (its n-1/n-4), which the
     ATTEMPT's text left implicit in "under P-1".
q-5  L's SCOPE: the build's "L sweeps a continuum" (§2.1) is dictum —
     the determination needs only the DISCHARGE's own sealed scope
     L in (0,1] (:183-185, re-read) over which the requirement
     quantifies, and failure at EVERY L != 1 ([D]). No defect.
```

### 2.5 THE AUDIT CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `p1auditvenv` (under /private/tmp scratch;
nothing written to the workspace but this artifact and its seal). Exact
symbolic / exact rational/surd only; nothing numeric evaluated; L formal.

```python
# P1-AUDIT independent CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv p1auditvenv).
# Routes deliberately DIFFERENT from P1-BUILD's battery:
#  A: structural scaling factorization (all-k mechanism) + closed-form Gaussian kernel identity
#     [build: degree-by-degree simplify, k=0..3]
#  B: Christoffel-Darboux KERNEL covariance of the projector (basis-free)
#     [build: basis-vector image + Gram/mixing integrals]
#  C: full even-ladder expansion of the foreign ground state with EXACT Parseval closure
#     (every even coefficient nonzero -> in NO finite truncation; partial sums < 1)
#     [build: single overlap + non-polynomiality of exp series]
#  D: per-member solution-set INTERSECTION for the executed-set closure arithmetic
#     [build: pairwise-consistency enumeration]
#  E: bijection algebra s = 1/L for the ray (no calculus); ray-closure by exact inequality
#     [build: derivative sign + limit]
#  F: |C_L|_4 by the t <-> L-t symmetry route AND by pure L^4 scaling; exponent re-pins
#     [build: Piecewise profile integral]
#  G: full 4x4 massless Dirac band projector matrix identities; generic-matrix spectral
#     indicator scale-blindness by eigenvector invariance
#     [build: single-component identity + reduce_inequalities]
#  H: det/tr conjugation invariance on a fully symbolic 2x2 with generic invertible S
#     [record: RA8a/CA5a on unitary/3x3]
# Every constant symbolic or an exact rational/surd; NOTHING numeric evaluated.
import sympy as sp
from sympy import Rational as R

results = []
def ok(name, cond):
    results.append(bool(cond))
    print(f"{name}: {'PASS' if cond else 'FAIL'}")

x, y, t = sp.symbols('x y t', real=True)
L, ell, ellp, l_, e_, c_, s_ = sp.symbols('L ell ellp l_ e_ c_ s_', positive=True)

def u(k, xx, l):
    # normalized 1D Hermite function, oscillator length l (BASELINE :34-41 / PA :50-57 axis factor)
    return (sp.hermite(k, xx/l) * sp.exp(-xx**2/(2*l**2))
            / (sp.pi**R(1, 4) * sp.sqrt(2**k * sp.factorial(k)) * sp.sqrt(l)))

# ===== A1 — STRUCTURAL FACTORIZATION: u_k(x;l) = l^(-1/2) F_k(x/l), F_k the UNIT-length profile =====
# This exhibits the MECHANISM valid for every k: u_k depends on (x,l) only through x/l, times l^(-1/2).
# Given it, U_L u_k(.;ell) = u_k(.;L ell) for ALL k is one line of surd algebra:
#   L^(-1/2) u_k(x/L; ell) = L^(-1/2) ell^(-1/2) F_k(x/(L ell)) = (L ell)^(-1/2) F_k(x/(L ell)) = u_k(x; L ell).
xi = sp.symbols('xi', real=True)
F = lambda k, z: u(k, z, 1).subs(x, z)  # unit-length profile F_k
ok("A1 u_k(x;l) = l^(-1/2) F_k(x/l) structurally, k = 0..5 (the all-k mechanism)   ",
   all(sp.simplify(u(k, x, l_) - F(k, xi).subs(xi, x/l_)/sp.sqrt(l_)) == 0 for k in range(6))
   and sp.simplify(1/sp.sqrt(L)/sp.sqrt(ell) - 1/sp.sqrt(L*ell)) == 0)

# ===== A2 — THE GAUSSIAN GENERATING KERNEL RIDES THE SAME IDENTITY (closed form, no series) =====
# G(t,x,l) := e^(2(x/l)t - t^2) e^(-x^2/(2 l^2)) / (pi^(1/4) sqrt(l))  [Hermite generating kernel]
# L^(-1/2) G(t, x/L, ell) = G(t, x, L ell) EXACTLY — the identity at the generating object.
Gk = lambda tt, xx, l: sp.exp(2*(xx/l)*tt - tt**2) * sp.exp(-xx**2/(2*l**2)) / (sp.pi**R(1,4)*sp.sqrt(l))
ok("A2 L^(-1/2) G(t, x/L, ell) = G(t, x, L ell) — generating kernel, closed form    ",
   sp.simplify(Gk(t, x/L, ell)/sp.sqrt(L) - Gk(t, x, L*ell)) == 0)

# ===== B — CHRISTOFFEL-DARBOUX KERNEL COVARIANCE OF THE PROJECTOR (basis-free) =====
# K_n(x,y;l) = sum_(k<n) u_k(x;l) u_k(y;l);  conjugation kernel rule: [U_L^dag A U_L](x,y) = L K(Lx, Ly).
# Claim: L K_n(Lx, Ly; ell) = K_n(x, y; ell/L) — i.e. U_L^dag Q_(n,ell) U_L = Q_(n, ell/L) AT KERNEL LEVEL.
K = lambda n, xx, yy, l: sum(u(k, xx, l)*u(k, yy, l) for k in range(n))
ok("B  L K_n(Lx,Ly;ell) = K_n(x,y;ell/L), n = 1,2,3 (projector kernel covariance)   ",
   all(sp.simplify(L*K(n, L*x, L*y, ell) - K(n, x, y, ell/L)) == 0 for n in (1, 2, 3)))

# ===== C — THE FOREIGN GROUND STATE'S FULL EVEN-LADDER EXPANSION + EXACT PARSEVAL =====
# c_j := <u_(2j)(.;ell), u_0(.;ellp)>. Closed form: c_j = sqrt(s) * sqrt((2j)!)/(2^j j!) * (-q)^j,
# s = 2 ell ellp/(ell^2+ellp^2), q = (ell^2-ellp^2)/(ell^2+ellp^2). Verified j = 0..3 by exact integration.
# (The sign (-q)^j is the integral's own; only q^2 enters Parseval and nonvanishing.)
sQ = 2*ell*ellp/(ell**2 + ellp**2)
qQ = (ell**2 - ellp**2)/(ell**2 + ellp**2)
cj = lambda j: sp.sqrt(sQ) * sp.sqrt(sp.factorial(2*j))/(2**j*sp.factorial(j)) * (-qQ)**j
ints = [sp.integrate(u(2*j, x, ell)*u(0, x, ellp), (x, -sp.oo, sp.oo)) for j in range(4)]
ok("C1 <u_2j(.;ell), u_0(.;ellp)> = sqrt(s) sqrt((2j)!)/(2^j j!) (-q)^j exactly, j=0..3",
   all(sp.simplify(ints[j] - cj(j)) == 0 for j in range(4)))
# Parseval closes EXACTLY: sum_j c_j^2 = s * sum_j binom(2j,j) (q^2/4)^j = s / sqrt(1-q^2) = 1.
# sympy returns the central-binomial sum as a Piecewise; its convergence branch condition
# z <= 1/4, z != 1/4 at z = q^2/4 is PINNED EXACTLY by 1 - q^2 = s^2 with s^2 > 0 (positive symbols).
jj = sp.symbols('jj', integer=True, nonnegative=True)
z = sp.symbols('z', positive=True)
S_generic = sp.Sum(sp.binomial(2*jj, jj) * z**jj, (jj, 0, sp.oo)).doit()
branch_expr, branch_cond = S_generic.args[0]
ok("C2 Parseval EXACT: branch 1/sqrt(1-4z); condition z = q^2/4 < 1/4 pinned by 1-q^2 = s^2 > 0;"
   " s * 1/sqrt(1-q^2) = 1 (all symbolic)",
   sp.simplify(branch_expr - 1/sp.sqrt(1 - 4*z)) == 0
   and branch_cond == sp.And(sp.Le(z, R(1, 4)), sp.Ne(z, R(1, 4)))
   and sp.simplify(1 - qQ**2 - sQ**2) == 0
   and (sQ**2).is_positive is True
   and sp.simplify(sQ * branch_expr.subs(z, qQ**2/4) - 1) == 0)
# Every even coefficient nonzero iff q != 0 iff ellp != ell; q = 0 has the UNIQUE positive root ellp = ell.
pos_roots = [r_ for r_ in sp.solve(sp.Eq(qQ, 0), ellp) if r_.is_positive]
ok("C3 q = 0 iff ellp = ell (unique positive root); at ellp = ell/L: q = (L^2-1)/(L^2+1), zero iff L = 1",
   len(pos_roots) == 1 and sp.simplify(pos_roots[0] - ell) == 0
   and sp.simplify(qQ.subs(ellp, ell/L) - (L**2 - 1)/(L**2 + 1)) == 0
   and sp.solveset(sp.Eq((L**2 - 1)/(L**2 + 1), 0), L, domain=sp.S.Reals) == sp.FiniteSet(-1, 1))
# CONSEQUENCE (exact, quantified): tr(Q_(m,ell) Q_(1,ellp)) = sum over included even levels of c_j^2 < 1
# for EVERY finite m when ellp != ell — the foreign ground state is in NO finite truncation H_(m,ell).
# Truncation defects pinned exactly: 1 - c_0^2 = 1 - s = q^2/(1 + sqrt(1-q^2)) — STRICTLY POSITIVE iff
# q != 0 (numerator q^2 > 0, denominator > 0); 1 - c_0^2 - c_1^2 = 1 - s(1 + q^2/2) likewise.
defect1 = sp.simplify(1 - cj(0)**2)
defect2 = sp.simplify(1 - cj(0)**2 - cj(1)**2)
ok("C4 truncation defects EXACT: 1 - c_0^2 = 1 - s = q^2/(1+sqrt(1-q^2)); 1 - c_0^2 - c_1^2 = 1 - s(1+q^2/2)",
   sp.simplify(defect1 - (1 - sp.sqrt(1 - qQ**2))) == 0
   and sp.simplify(defect2 - (1 - sp.sqrt(1 - qQ**2)*(1 + qQ**2/2))) == 0
   and sp.simplify((1 - sp.sqrt(1 - qQ**2)) - qQ**2/(1 + sp.sqrt(1 - qQ**2))) == 0)

# ===== D — EXECUTED-SET CLOSURE BY SOLUTION-SET INTERSECTION (exact surds) =====
# {L : 1/L in E} = {1, 1/sqrt2}; {L : sqrt2/L in E} = {sqrt2, 1}; intersection = {1}.
Eset = sp.FiniteSet(1, sp.sqrt(2))
sol1 = sp.FiniteSet(*[sp.simplify(1/a) for a in Eset])           # L with 1/L in E
sol2 = sp.FiniteSet(*[sp.simplify(sp.sqrt(2)/a) for a in Eset])  # L with sqrt2/L in E
ok("D  {L: 1/L in E} inter {L: sqrt2/L in E} = {1} exactly; exit witness sqrt2/(1/sqrt2) = 2 notin E    ",
   sol1.intersect(sol2) == sp.FiniteSet(1)
   and sp.simplify(sp.sqrt(2)/(1/sp.sqrt(2)) - 2) == 0
   and sp.FiniteSet(2).intersect(Eset) == sp.EmptySet)

# ===== E — THE RAY BY BIJECTION ALGEBRA (no calculus) =====
# s = 1/L maps (0,1] one-to-one onto [1,oo); ell/L = ell*s. Ray closure of [c,oo):
# e >= c, s >= 1  =>  e*s - c = (e-c)*s + c*(s-1) >= 0 — an exact algebraic decomposition, each term >= 0.
es_minus_c = e_*s_ - c_
decomp = (e_ - c_)*s_ + c_*(s_ - 1)
ok("E  e*s - c = (e-c)*s + c*(s-1) EXACT; s = 1/L bijects (0,1] onto [1,oo): L = 1/s, 1/(1/s) = s      ",
   sp.simplify(es_minus_c - decomp) == 0
   and sp.simplify(1/(1/s_) - s_) == 0
   and sp.simplify((e_/L).subs(L, 1/s_) - e_*s_) == 0)

# ===== F — |C_L|_4 BY SYMMETRY AND BY PURE SCALING; THE EXPONENT RE-PINS =====
# Symmetry route: r_L(t) = min(t, L-t) is symmetric under t <-> L-t; |C_L|_4 = 2 int_0^(L/2) (4pi/3) t^3 dt.
V_sym = 2*sp.integrate(R(4,3)*sp.pi*t**3, (t, 0, L/2))
# Scaling route: substitute t = L*tau in the unit-cell integral: |C_L|_4 = L^4 |C_1|_4, |C_1|_4 = pi/24.
tau = sp.symbols('tau', real=True)
V_unit = 2*sp.integrate(R(4,3)*sp.pi*tau**3, (tau, 0, R(1,2)))
m_, eps_ = sp.symbols('m_ eps_', positive=True)
ok("F1 |C_L|_4 = pi L^4/24 (symmetry route) = L^4 * pi/24 (scaling route); L^2 = (24/pi)^(1/2)|C|_4^(1/2);"
   " (L eps)^m = L^2 L^(m-2) eps^m identically",
   sp.simplify(V_sym - sp.pi*L**4/24) == 0
   and sp.simplify(V_unit - sp.pi/24) == 0
   and sp.simplify(L**4*V_unit - V_sym) == 0
   and sp.simplify((24/sp.pi)**R(1,2)*(sp.pi*L**4/24)**R(1,2) - L**2) == 0
   and sp.simplify((L*eps_)**m_ - L**2*L**(m_-2)*eps_**m_) == 0)
# F2 — L^(m-2) <= 1 for the INTEGER Dyson orders m >= 2 the collapse display quantifies (m = 2 + r,
# r >= 0 integer), by the GEOMETRIC FACTORIZATION 1 - L^r = (1-L) * sum_(i<r) L^i — both Piecewise
# branches handled exactly (generic L != 1; and the L = 1 branch where both sides vanish identically);
# each factor's sign is a pinned exact fact: L^i > 0 (positive base), 1 - L >= 0 on the class direction.
rr = sp.symbols('rr', integer=True, positive=True)
ii = sp.symbols('ii', integer=True, nonnegative=True)
geo = sp.Sum(L**ii, (ii, 0, rr - 1)).doit()
D_geo = sp.simplify((1 - L)*geo - (1 - L**rr))
branch_vals = [sp.simplify(e) for (e, c) in D_geo.args if not e.has(sp.nan)] if isinstance(D_geo, sp.Piecewise) else [D_geo]
at_one = sp.Sum(sp.Integer(1)**ii, (ii, 0, rr - 1)).doit()
ok("F2 (1-L)*sum_(i<r) L^i = 1 - L^r exactly (generic branch); at L = 1: sum = r and both sides 0;"
   " L^i > 0 pinned; real-m exponent route: L^(m-2) = exp(-(m-2)log(1+w)) under L = 1/(1+w), exponent sign pinned",
   all(v == 0 for v in branch_vals)
   and sp.simplify(at_one - rr) == 0
   and sp.simplify((1 - 1)*rr - (1 - sp.Integer(1)**rr)) == 0
   and (L**ii).is_positive is True
   and (lambda w_, r_: sp.simplify((1/(1 + w_))**r_ - sp.exp(-r_*sp.log(1 + w_))) == 0
        and (r_*sp.log(1 + w_)).is_nonnegative is True)(*sp.symbols('w_ r_', nonnegative=True)))

# ===== G — THE 4x4 MASSLESS DIRAC BAND PROJECTOR: FULL MATRIX IDENTITIES =====
kx, ky, kz = sp.symbols('kx ky kz', real=True, positive=None)
sig1 = sp.Matrix([[0, 1], [1, 0]]); sig2 = sp.Matrix([[0, -sp.I], [sp.I, 0]]); sig3 = sp.Matrix([[1, 0], [0, -1]])
Z2 = sp.zeros(2, 2)
alph = [sp.Matrix(sp.BlockMatrix([[Z2, s], [s, Z2]])) for s in (sig1, sig2, sig3)]
Hk = kx*alph[0] + ky*alph[1] + kz*alph[2]
nk = sp.sqrt(kx**2 + ky**2 + kz**2)
Pm = (sp.eye(4) - Hk/nk)/2
HkL = (kx/L)*alph[0] + (ky/L)*alph[1] + (kz/L)*alph[2]
nkL = sp.sqrt((kx/L)**2 + (ky/L)**2 + (kz/L)**2)
PmL = (sp.eye(4) - HkL/nkL)/2
ok("G1 P_-(k)^2 = P_-(k); H(k) P_-(k) = -|k| P_-(k); tr P_- = 2 — 4x4 matrix-level, symbolic k          ",
   sp.simplify(Pm*Pm - Pm) == sp.zeros(4, 4)
   and sp.simplify(Hk*Pm + nk*Pm) == sp.zeros(4, 4)
   and sp.simplify(sp.trace(Pm) - 2) == 0)
ok("G2 P_-(k/L) = P_-(k) — degree-0 homogeneity of the FULL 4x4 projector (L > 0 symbolic)             ",
   sp.simplify(PmL - Pm) == sp.zeros(4, 4))
# Pure-scheme indicator: for symmetric A with distinct eigenvalues, eigenvectors of A/L = eigenvectors of A,
# eigenvalues scale by 1/L > 0: the negative-spectral subspace is IDENTICAL. Generic symbolic 2x2:
a11, a12, a22 = sp.symbols('a11 a12 a22', real=True)
A2 = sp.Matrix([[a11, a12], [a12, a22]])
evA = A2.eigenvects(); evAL = (A2/L).eigenvects()
pairs_match = all(
    any(sp.simplify(lamL - lam/L) == 0 and
        all(sp.simplify(sp.Matrix(vL[0]).normalized()[i]**2 - sp.Matrix(v[0]).normalized()[i]**2) == 0
            for i in range(2))
        for (lam, m, v) in evA)
    for (lamL, mL, vL) in evAL)
ok("G3 eig(A/L) = eig(A)/L with THE SAME eigenvectors (generic symmetric 2x2, symbolic)                ",
   pairs_match)

# ===== H — det/tr CONJUGATION INVARIANCE, FULLY SYMBOLIC GENERIC 2x2 =====
b11, b12, b21, b22, p11, p12, p21, p22 = sp.symbols('b11 b12 b21 b22 p11 p12 p21 p22')
B = sp.Matrix([[b11, b12], [b21, b22]]); P = sp.Matrix([[p11, p12], [p21, p22]])
Pinv = P.adjugate()/P.det()   # exact symbolic inverse via adjugate — det(P) symbolic nonzero
BC = P*B*Pinv
ok("H  det(P B P^-1) = det B and tr(P B P^-1) = tr B — generic symbolic 2x2 (adjugate route)           ",
   sp.simplify(BC.det() - B.det()) == 0 and sp.simplify(sp.trace(BC) - sp.trace(B)) == 0)

print(f"P1-AUDIT-BATTERY: {sum(results)}/{len(results)} PASS")
```

Output, verbatim (15/15 PASS):

```text
A1 u_k(x;l) = l^(-1/2) F_k(x/l) structurally, k = 0..5 (the all-k mechanism)   : PASS
A2 L^(-1/2) G(t, x/L, ell) = G(t, x, L ell) — generating kernel, closed form    : PASS
B  L K_n(Lx,Ly;ell) = K_n(x,y;ell/L), n = 1,2,3 (projector kernel covariance)   : PASS
C1 <u_2j(.;ell), u_0(.;ellp)> = sqrt(s) sqrt((2j)!)/(2^j j!) (-q)^j exactly, j=0..3: PASS
C2 Parseval EXACT: branch 1/sqrt(1-4z); condition z = q^2/4 < 1/4 pinned by 1-q^2 = s^2 > 0; s * 1/sqrt(1-q^2) = 1 (all symbolic): PASS
C3 q = 0 iff ellp = ell (unique positive root); at ellp = ell/L: q = (L^2-1)/(L^2+1), zero iff L = 1: PASS
C4 truncation defects EXACT: 1 - c_0^2 = 1 - s = q^2/(1+sqrt(1-q^2)); 1 - c_0^2 - c_1^2 = 1 - s(1+q^2/2): PASS
D  {L: 1/L in E} inter {L: sqrt2/L in E} = {1} exactly; exit witness sqrt2/(1/sqrt2) = 2 notin E    : PASS
E  e*s - c = (e-c)*s + c*(s-1) EXACT; s = 1/L bijects (0,1] onto [1,oo): L = 1/s, 1/(1/s) = s      : PASS
F1 |C_L|_4 = pi L^4/24 (symmetry route) = L^4 * pi/24 (scaling route); L^2 = (24/pi)^(1/2)|C|_4^(1/2); (L eps)^m = L^2 L^(m-2) eps^m identically: PASS
F2 (1-L)*sum_(i<r) L^i = 1 - L^r exactly (generic branch); at L = 1: sum = r and both sides 0; L^i > 0 pinned; real-m exponent route: L^(m-2) = exp(-(m-2)log(1+w)) under L = 1/(1+w), exponent sign pinned: PASS
G1 P_-(k)^2 = P_-(k); H(k) P_-(k) = -|k| P_-(k); tr P_- = 2 — 4x4 matrix-level, symbolic k          : PASS
G2 P_-(k/L) = P_-(k) — degree-0 homogeneity of the FULL 4x4 projector (L > 0 symbolic)             : PASS
G3 eig(A/L) = eig(A)/L with THE SAME eigenvectors (generic symmetric 2x2, symbolic)                : PASS
H  det(P B P^-1) = det B and tr(P B P^-1) = tr B — generic symbolic 2x2 (adjugate route)           : PASS
P1-AUDIT-BATTERY: 15/15 PASS
```

REPRODUCTION CHECK of the build's own battery: the ```python block of
631356a3 §2.5 was extracted byte-for-byte from the sealed artifact at
path and executed unmodified in the same fresh venv (sympy 1.14.0). Its
output is BYTE-IDENTICAL to the output the build prints in §2.5,
including the final `P1-BUILD-DONE` line: 10/10 PASS. The build's
enumeration logic in P1f was additionally re-read and hand-checked (the
four (a,b) pairs; only (1, sqrt2) satisfies the consistency condition
b = sqrt2·a; L = 1).

---

## 3. V-WATCH, PROVENANCE, INJECTION, FENCES

### 3.1 Bearing audit (tasked item 3) — each stated explicitly

```text
V-1 (refutation at the amendment's own quantifier, S2CAND :417-423):
    NOT ENGAGED. Neither the build nor this audit exhibits any refutation
    of sup_n ||Delta^Sigma_n(a)||_2 <= |C|_4^alpha' G_hs^Sigma at the
    amended quantifier. The refuted horn concerns the carrier INDEX
    SET's closure under one reading of the ell-freeze's scope — not the
    estimate. The first-order chain never consumed membership (q-2).
V-2 (one-way bridge shown two-way, :424-428): NOT ENGAGED — no
    summed -> per-pair derivation is created or used by build or audit.
V-3 (anchor overturn, :429-435): NOT ENGAGED — REM is consumed at its
    sealed grade; its compression line was re-read at bytes and holds
    for any orthogonal projector; no overturn condition is touched.
V-4 (bytes-contradiction, :436-438): NOT ENGAGED — the frozen-reading
    refutation contradicts NO sealed clause: no sealed byte asserts
    family closure at that reading (that scope is exactly r-A2's
    unpinned spec-reading, DISCHARGE :319-325); the build's finding is
    consistent with the sealed AUDIT's "P-1 real" (q-4).
V-5 (C4-kill failure, :439-443): NOT ENGAGED — no stratum claim is
    formed; the kill's operation inside Delta^Sigma is untouched.
V-6 (marking fraud, :444): NOT ENGAGED — the build's markings are
    honest (CLAIMED until checked; witness declared STANDING; no flag
    flips; conditionality of the dissolution stated at every use).
ADOPTED AMENDMENT'S ESTIMATE AT ITS OWN QUANTIFIER: UNMOVED — severally
    none of V-1..V-6 engages, per above.
DELIVERED alpha' = 1/4 RECORD (DISCHARGE §2.3, similarity class):
    UNMOVED — first order consumed no membership under EITHER reading
    of the ell-freeze (REM :380-382 projector-blind; r-A2's own point,
    confirmed at bytes by this audit).
Q-1050 RESIDUE STATEMENT: analytic content UNMOVED — Q^Sigma_1 remains
    uncertified; witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
    STANDS untouched by build and audit alike. IF the build's result is
    adopted, residue item (iv) (ATTEMPT :984-985) narrows to the exact
    quantifier-width condition (ray-wide in ell, n-uniform) — a
    registrar's act, correctly left unexecuted by the build (its §3 is
    descriptive, "no verdict moved"). The reduction itself still cannot
    run today: Q^Sigma_1 does not exist.
```

### 3.2 Provenance findings (tasked item 4)

```text
c-1  CORRECTION (the one correction of this audit): the build's §1 and
     SEALS_VERIFIED flag state BASELINE (80aa4e17) carries "no sidecar"
     / "no standalone sidecar exists". FALSE AT PATH: the standalone
     sidecar STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.seal.sha256
     EXISTS (named without the `.md` infix) and contains exactly
     80aa4e1722c117e8195ce0fb2ae3fc37262bc8fc2ba5d79a6dd38cd1029bc26d,
     matching my recomputation and PA's table. The build's DIGEST
     verification is unaffected (its PA-table route is valid and the
     digest is right); the annotation is wrong. For PROVENANCE
     (a1258dcf) the build's claim is TRUE: no standalone sidecar exists;
     PA-table route confirmed.
c-2  NOTE (not a correction): the build cites the DISCHARGE class scope
     as ":183-186"; the sentence lies at :183-185 (:186 begins the
     CAS-pinning remark). Sub-line imprecision; content as cited.
c-3  All other cited spans — ATTEMPT :334-370, :941-946, :984-985;
     AUDIT :143-167; DISCHARGE :179-236, :319-325, :341-367; PA :48-74,
     :75-128, :137-142, table :26-27; E1 :417-426, :679-689, :851-894;
     BASELINE :32-44, :46-53, :55-56; PROVENANCE :43-51, :53; S2CAND
     :181-213, :236-246; REM :380-382, :450 — re-read at their
     locations THIS session and found as cited. The build's quotation
     of E1 :684-686, PA :72-73, BASELINE :43-44, :55-56 is verbatim-
     faithful.
```

### 3.3 Injection scan (tasked item 5)

CLEAN. The build contains no directive content addressed to auditors,
registrars, or downstream lanes. Systematic scan for imperative/authority
phrasing found "registrar" only inside authority DISCLAIMERS ("witness
status is the registrar's"; "retirement/entry the registrar's") — the
build claims no act beyond its authority, performs no witness action, no
flag flip, no retirement, and its §3 is explicitly descriptive. This
audit likewise directs no one.

### 3.4 Fences (tasked item 6)

```text
BUILD'S FENCES, CHECKED AT ITS BYTES: exact symbolic only — its battery
  contains no float, no evalf, no numpy, no scientific notation (grep
  swept); L formal throughout (the case points L = 1 and 1/L = sqrt2
  arise as formal solutions of the quantifier's own equations — no
  physical scale fixed); alpha never computed or approached (the 1/4,
  1/2 appear only as the sealed record's own |C|_4-exponents, re-pinned
  symbolically); M(t) sharp untouched (STEP 1 consumed as sealed); no
  equal-time object formed; NO carrier or cellulation datum introduced
  (ell/L is the covariance's own image; the executed values cited are
  sealed data at PA/BASELINE; the ray is a DERIVED criterion — no new
  frozen range, no member added to any sealed family); no flag flips;
  no witness action; one file + seal (sidecar exists and matches).
  HELD.
AUDIT'S FENCES, HELD: exact symbolic only (fresh venv p1auditvenv,
  sympy 1.14.0; every constant symbolic or an exact rational/surd;
  nothing numeric evaluated); L formal; alpha never approached; no
  carrier or cellulation datum introduced (my [C]-instances q = -3/5,
  -1/3 are exact rationals derived from sealed executed values and the
  covariance's own image — no new member admitted anywhere); M(t)
  untouched; no equal-time object; no flag flips; no witness action;
  no register/tracker/plan/road/ledger/lens file read; no git action;
  ONE file written (this artifact) plus its seal.
```

---

## 4. FLAG BLOCK

```text
P1_VERDICT = CONFIRMED-WITH-CORRECTIONS( the build's PARTIAL-DELIVERED
  determination — dilation image exact and degree-invariant with index
  map (n, ell) -> (n, ell/L), family closure DERIVED at the declared
  bytes by the ray criterion, REFUTED by exact counterexample at the
  frozen-execution reading, the unpinned byte localized to the
  ell-freeze's scope, and the consuming step needing FAMILY-level
  closure only so the standalone premise dissolves into the residue's
  ell-ray quantifier width — survives independent re-derivation by
  routes the build did not use (structural all-k factorization,
  generating kernel, projector-kernel covariance, even-ladder Parseval
  escape with quantified defects, solution-set intersection, bijection
  ray algebra: 15/15 PASS), both tasked attacks failing; the one
  correction is provenance-note-level, not determination-level. )

V1_STATUS = NOT-ENGAGED( no refutation of the summed estimate at the
  amendment's own quantifier is exhibited by build or audit — the
  refuted horn lives at the carrier index-set level, and V-2..V-6
  are severally not engaged either; the delivered alpha' = 1/4
  similarity-class record and Q-1050's residue statement stand unmoved,
  with witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDING. )

NET_VERDICT = CONFIRMED-WITH-CORRECTIONS( one correction, named exactly:
  the build's §1/SEALS_VERIFIED annotation that BASELINE (80aa4e17)
  carries no standalone sidecar is FALSE at path — the sidecar
  STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.seal.sha256 exists and
  matches — the digest verification itself unaffected; plus one
  non-correction note, the ":183-186" sub-line imprecision for a
  sentence at :183-185; every determination, quantifier identification,
  counterexample, dissolution step, and bearing claim of the build is
  CONFIRMED at the bytes and at my own independent CAS. )

PROVENANCE = VERIFIED( 10/10 full digests recomputed from bytes at path
  this session — the build itself plus all nine artifacts it relies on —
  matched against sidecars, tasked digests, and PA's sealed
  frozen-authority table; every cited span re-read at its location and
  found as cited except the two §3.2 items, one corrected, one noted;
  independent corpus sweep found no artifact pinning the family's
  ell-extent and none beyond DISCHARGE/ATTEMPT addressing closure. )

INJECTION = CLEAN( no directive content addressed to auditors,
  registrars, or downstream in the build — authority disclaimers only;
  the build performs no act beyond its authority; this audit directs
  no one and performs none either. )

FENCES = HELD( by the build at its bytes — exact symbolic only, nothing
  numeric, L formal, alpha never approached, M(t) sharp untouched, no
  equal-time object, no carrier or cellulation datum introduced, no
  flag flips, no witness action, one file plus matching seal — and by
  this audit — fresh venv, exact rationals/surds only, no register or
  tracker or plan or road or ledger or lens file read, no git, ONE file
  written (this artifact) plus its seal. )

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
P1_DILATION_CLOSURE_AUDIT_RESULT = SEALED.
```
