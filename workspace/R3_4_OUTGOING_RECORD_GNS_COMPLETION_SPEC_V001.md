# R3.4 Outgoing Record-GNS Completion Specification v001

Date: 2026-07-24

## Status

Forward-sealed continuum-completion specification. It is written after the
finite parent correctly returned:

```text
FINITE_CAUSAL_PARENT_DERIVED_CONTINUUM_COMPLETION_OPEN.
```

This gate tests the exact outgoing record limit. It does not try to force a
global source-inclusive Moller unitary for infinitely many future
interactions.

No alpha, measured coupling, mass, endpoint, or desired decay exponent may
enter.

## Pinned authorities

| File | Role | SHA-256 |
|---|---|---|
| `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md` | Finite parent, generated descendants, record persistence, and continuum gap | `345d447eaf6d730caa6fa655b92a7b0cd93a68b5f86e0c8929cf969f40aeb7cb` |
| `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_V001.seal.sha256` | Sealed finite-parent bundle | `e771a28a9c1e1c9f410a00dc2c407be3338fcdee06d31f50cbc8563d63e41029` |
| `R3_4_DRESSED_OUTGOING_RECORD_RANGE_RESULT_V001.md` | Correct dressed incoming range | `3240d935ef558948e09dd6a70092a3f3592747b5e5c99ea559d64adc2a5c303e` |
| `CAUSAL_DIRECT_LIMIT_COVECTOR_RAY_LIFT_RESULT_V001.md` | Quasi-local state and central record sequence | `7c81c625c41adae66e4c72f4b4e351507760bca74e68f555053fac87ae70c859` |
| `CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001.md` | Recoverable redundant output labels | `3359960fb411eff8ac0360a8c052bfc4d00a6281bd151c390fa3addd3603d05a` |
| `PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md` | Same-parent state/dynamics and locally normal outgoing limit | `532b0f0eac4ac749ba3e24954db356f7ca0f98c4f730030075c463efa3158efb` |
| `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` | Future cells cannot reuse completed record factors | `b0c636f3b2b00f0694ad001cb32a3a84c5d4fc09c25c57fd4fdb8885e8206b30` |

## Finite outgoing systems

For the first `N` completed cells, let

```text
R_N=tensor_(j=1)^N M_3(C)
```

be the full finite record algebra, with the public pointer algebra as its
diagonal subalgebra. The canonical embedding is

```text
iota_NM(A)=A tensor I_(M-N).
```

Let `W_N` be the finite propagator supplied by the sealed parent, starting
from the same incoming source state and ready records. Define:

```text
omega_N(A)
 =omega_in[W_N^*(I_source tensor A)W_N];

Phi_N(A)
 =W_N^*(I_source tensor A)W_N.
```

`omega_N` is the output record state. `Phi_N` is the incoming
source-dressed representation of that record algebra.

## Theorem obligations

### T1. Exact output-state compatibility

For `M>N`, factor the parent evolution as

```text
W_M=V_(M,N)(W_N tensor I_new),
```

where `V_(M,N)` acts on the shared source and new record factors but as the
identity on the first `N` completed record factors. Partial-trace invariance
must give:

```text
omega_M(iota_NM(A))=omega_N(A)
```

for every `A in R_N`.

This is a full algebra statement, not only a pointer-probability check.

### T2. Quasi-local outgoing state and GNS representation

Compatibility must define one state `omega_out` on

```text
R_infinity=inductive_limit(R_N).
```

The standard GNS construction then supplies

```text
(pi_out,H_out,Omega_out).
```

Because every completed record observable commutes with all later parent
terms, the outgoing record evolution is:

```text
alpha_out,t(A)=A.
```

This identity automorphism group is strongly continuous and is generated by
zero on the completed-record algebra. The nontrivial source tail remains in
the source sector and is not erased.

### T3. Coherent dressed incoming net

The maps `Phi_N` are injective star-homomorphisms. Define the dressed
embedding by:

```text
j_NM=Phi_M o iota_NM o Phi_N^(-1)
```

on `Phi_N(R_N)`.

The gate must prove:

```text
j_ML o j_NM=j_NL;
omega_in o Phi_N=omega_N;
```

and therefore that the dressed incoming record net is isomorphic to the
bare outgoing quasi-local record algebra.

This construction must not claim that `Phi_N(A)` converges as an operator in
one fixed finite-source Hilbert space. Coherent embeddings, not a
nonexistent fixed-space limit, are the required object.

### T4. Recoverability

The public pointer averages

```text
M_N=(1/N)sum_(j=1)^N Z_j
```

must remain a central sequence:

```text
||[M_N,O]||<=2m||O||/N
```

for `O` supported on at most `m` cells. Their limiting expectations must
separate the public output labels.

### T5. Source-sector boundary

The gate must state separately:

```text
global source-inclusive projective state limit: not derived here;
global infinite-future source Moller unitary: not derived here;
finite-energy source tail spectrum: inherited from the complete finite
parent and its free asymptotic generator.
```

The absence of a global source Moller unitary does not invalidate the
outgoing record GNS construction. Conversely, the record construction may
not be relabeled as a complete source-inclusive scattering theorem.

## Executable obligations

The primary audit must:

1. verify every pinned hash and the specification seal;
2. construct at least three sequential shared-source record writes;
3. test state restriction on a complete matrix-unit basis, not one pointer;
4. test star, product, unit, and injectivity for each `Phi_N`;
5. test dressed-embedding coherence for `N=1,2,3`;
6. reproduce the central-sequence commutator bound on nontrivial local
   observables;
7. retain a source-observable negative control showing that full
   source-inclusive restriction compatibility is not being claimed.

An independent verifier must use a separately coded state-evolution route
and test at least the matrix-unit state restrictions and the source negative
control.

## Predeclared verdicts

```text
if T1-T4 pass while T5 remains explicitly scoped:
  OUTGOING_RECORD_GNS_AND_DRESSED_NET_DERIVED

if only finite record restrictions pass:
  FINITE_RECORD_COMPATIBILITY_ONLY

if record-state compatibility fails:
  OUTGOING_RECORD_GNS_BLOCKED
```

## Fixed status

```text
global_source_inclusive_state_limit_derived = false
global_infinite_future_source_Moller_unitary_derived = false
complete_source_inclusive_GNS_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
