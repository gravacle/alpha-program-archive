# STAGE 8 — PREMISE P-1: THE D3/HERMITE-GALERKIN CARRIER FAMILY'S CLOSURE UNDER DILATION, SETTLED AT THE BYTES

## BLIND ANALYST — CODENAME P1-BUILD — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (P1-BUILD). One question, Q-1050's REOPEN item (named at
57edbb96 §2.5): IS the sealed carrier family closed under the dilation the
covariance chain of record performs? Honest outcome classes equal citizens:
DELIVERED / REFUTED / PARTIAL-DELIVERED / FAILS-AT. Everything below worked
ONLY from sealed artifacts verified at path; the family-closure question is
DERIVED about the sealed family — no carrier or cellulation datum is
introduced into it.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — one CAS battery (sympy 1.14.0,
fresh venv `p1venv` under /private/tmp scratch), reproduced in full in §2.5
with output verbatim (10/10 PASS); NOTHING numeric evaluated; every
constant symbolic or an exact rational/surd; the dilation parameter L is a
FORMAL symbol, never instantiated (the case points of the index arithmetic
— L = 1; the solved exit at 1/L = sqrt2 — are formal solutions of the
quantifier's own equations, no physical scale fixed). No physical quantity
computed, bounded numerically, or evaluated. alpha never computed or
approached. M(t) sharp (untouched — the dilation's branchwise-exact action
on the sharp indicator is the sealed chain's STEP 1, consumed as sealed).
Equal-time FALSE by C6 (no equal-time object formed). No flag flips; no
witness action (witness status is the registrar's). No register/tracker/
plan/road/ledger/lens file read. Output name probed before write: ABSENT.
No git action. ONE file written (this artifact) plus its seal.

---

## 0. VERDICT IN ONE LINE

**P-1 = PARTIAL-DELIVERED: the dilation's action on the sealed Hermite-Galerkin carrier is EXACT and degree-invariant — U_L H_(n,ell) = H_(n, L ell), so the covariance chain's index move n -> n' is (n, ell) -> (n, ell/L) with the truncation level n INVARIANT and the entire premise living on the ell-axis; FAMILY closure is DERIVED, exactly, at the family's declared bytes (the ell-parameterized realization — criterion: ell-extent an upward multiplicative ray), and REFUTED by exact counterexample at the frozen-execution reading (ell in {1, sqrt2}: closed only at L = 1, the dilated member escaping every fixed-ell member at every truncation level); the one byte the record leaves unpinned is the ell-freeze's scope (family vs execution — r-A2's reading gap, CONFIRMED and LOCALIZED); and the exact consuming step (Route III's class-reduction inequality) needs FAMILY-level closure only — never member-wise closure, which fails for every L != 1 and is consumed nowhere — so the standalone premise P-1 DISSOLVES, reading-independently, into a quantifier-width condition on the already-open unit-cell residue: Q^Sigma_1 certified over an ell-upward-ray carrier range, a shape E1's own no-n/no-ell constant discipline already demands; under the declared reading that ray IS the family and P-1 holds derived.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by full digest; matched
against sidecars and tasked prefixes. Two inherited authorities carry no
standalone sidecar and are verified against the SEALED frozen-authority
table of PA (789338ad, itself sidecar-verified) — noted per row. NONE
UNVERIFIABLE.

```text
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH (ATTEMPT; tasked 57edbb96 — P-1 named §2.5; Route III; i-2)
38aa39d16f3fac1ffd037d064b8504edcc641c93ddbdcbbcbdf7ed01dff8787a  STAGE8_R3C_SUMMED_REMAINDER_AUDIT_V001.md    MATCH (AUDIT; tasked 38aa39d1 — §2.4 P-1 CONFIRMED real; no det compression line sealed)
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md                  MATCH (DISCHARGE; tasked e5b0879b — §2.3 STEPS 1-6; STEP 5 premise; §2.6 r-A2; §3 q-i/q-ii)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA; tasked 789338ad — A1 carrier :48-74; :75-128; frozen authorities table)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730 — D3 :417-426; R.1 :679-689; R-L0 :851-894)
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md           MATCH (S2CAND; sidecar — S2'-O :181-213; R-L2b^Sigma :236-246)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM; sidecar — :380-382 the projector-blind compression line; :450)
80aa4e1722c117e8195ce0fb2ae3fc37262bc8fc2ba5d79a6dd38cd1029bc26d  STAGE8_T7_HERMITE_GALERKIN_BASELINE_SPEC_V001.md  MATCH vs PA frozen-authority table (no sidecar exists) — the family declaration :32-56
a1258dcf40732f0e3fce358a68ffdbd34bc347d70283550440582a1129bec510  STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md  MATCH vs PA frozen-authority table (no sidecar exists) — the defining class :43-53
```

Sealed spans read at path this session and relied on: E1 :417-426 (D3
frozen unrestricted quantifier), :679-689 (R.1 order of quantification;
:684-686 "no Hermite carrier index n, no ell, no truncation level, no
cellulation-family index, NO CELLULATION GEOMETRIC DATUM"), :851-894
(R-L0/R-L0b; :864-865); PA :48-74 (A1: the carrier family, the Phase-A
member, "Both `ell` values are executed and retained"), :75-128 (carrier
construction; r(t); M(t); v(t); b_D), :137-142 (frozen polydisc values),
frozen-authority table (baseline + provenance digests); BASELINE :32-44
(the Galerkin-family declaration; "nested in n and their union is a core
for the free Dirac operator"), :46-53 (Freeze block), :55-56 ("The second
scale is a regulator-scheme stress test, not an adjustable physical
length"); PROVENANCE :43-53 (the genuine-Galerkin defining class; "Exact
commutation with h_0 is neither required nor possible"); DISCHARGE §2.3
(:179-236 the covariance chain; STEP 5 :209-219 with the premise
parenthetical; STEP 6 :220-227), §2.6 r-A2 (:319-325), §3 (:341-367 the
three sups); ATTEMPT §2.5 (:334-370 Route III; P-1 :355-363; the
consequence clause :366-370), §4 i-2 (:941-946), §5 flag (iv) (:984-985);
AUDIT §2.4 (:143-167 — P-1 real CONFIRMED; "no sealed line supplies"
determinant compression monotonicity :160-165); S2CAND :181-213 (S2'-O;
"per carrier index n of the D3/Hermite-Galerkin refinement family"
:182-183; "the n-INDEXED FAMILY WITH n-UNIFORM CONSTANTS" :204-205),
:236-246 (R-L2b^Sigma; "no carrier index, no cellulation datum" :243);
REM :380-382 ("every refinement compression obeys ||Pi X Pi||_2 <=
||X||_2"), :450 ("sup over the D3/Hermite-Galerkin refinement family").
Corpus sweep for additional pins: every workspace byte matching
"oscillator length" or "D3/Hermite" was located and its context examined
(G3/AXN/T7-numerics artifacts: the phrase used generically or as import
fences; no artifact pins the family's ell-extent beyond the spans above).

---

## 2. THE DETERMINATION

### 2.1 The two quantifiers at their bytes (the tasked item (1))

**D3's frozen quantifier ranges over CELLULATIONS, not carriers** (E1
:417-426, at bytes): "All common refinements of members of A and B" means
ANY cellulation refining BOTH parents — UNRESTRICTED, UNIVERSALLY
QUANTIFIED. In the record's phrase "D3/Hermite-Galerkin carrier family"
(REM :450; S2CAND :182-183 "per carrier index n of the D3/Hermite-Galerkin
refinement family"; DISCHARGE q-i) the "/" is apposition: the carrier
family OF the D3 architecture. The two quantifiers meet in P-1 exactly
here: D3's unrestricted CELL quantifier supplies the dilation scales — on
the similarity class of the entered cell, L formal in (0,1] (the
DISCHARGE's own sealed scope, :183-186, consumed as sealed) — and the
CARRIER family must absorb the index move those scales induce. Because the
cell quantifier is unrestricted, L sweeps a continuum; that is what makes
the ell-arithmetic below decisive. [DERIVED at the cited bytes]

**The carrier family sits at three strata at its bytes:**

```text
s-1  THE DEFINING CLASS (PROVENANCE :43-51): "Q_n is finite rank and
     nested; Q_n -> I strongly; Q_n h_0 Q_n converges to h_0 on a common
     core; Q_n M_c(t) Q_n converges strongly to M_c(t); and the finite
     propagators converge strongly, uniformly on compact times."
     — n-indexed, ell-FREE. ("Exact commutation with h_0 is neither
     required nor possible", :53.)
s-2  THE DECLARED REALIZATION (BASELINE :34-44; PA :50-61): "Use
     normalized ... Hermite functions with oscillator length ell";
     Q_(n,ell)L2(R3) = span{phi_a(x/ell)phi_b(y/ell)phi_c(z/ell):
     0<=a,b,c<n}; spinor factor tensor C^4; "The spaces are nested in n
     and their union is a core for the free Dirac operator" (BASELINE
     :43-44); "nested in n, converge strongly to the continuum carrier"
     (PA :59-61). ell enters as a NAMED PARAMETER of the declaration,
     range-unrestricted at these bytes.
s-3  THE EXECUTION FREEZES (BASELINE :46-53; PA :63-74): baseline
     "Freeze: n in {2,4}; ell in {1,sqrt(2)}; T_R=1 ...; tau_R=pi/sqrt(2)";
     Phase A "uses the first nontrivial member" n = 2, ell in {1,sqrt2},
     "Both `ell` values are executed and retained. Neither may be selected
     by its output" (PA :72-73); "The second scale is a regulator-scheme
     stress test, not an adjustable physical length" (BASELINE :55-56).
```

**DERIVED, at the bytes — the freeze's n-half is EXECUTION-scoped, not
family-scoped:** "their union is a core" (BASELINE :43-44) is FALSE for
any finite set of n — a finite union of finite-rank subspaces is a proper
closed subspace, never dense, never a core — so the family's n-extent is
unbounded BY THE DECLARATION'S OWN SENTENCE; and every consuming artifact
quantifies n unboundedly (REM's trilemma sup_n :380-382; the per-pair
carrier limit's nonexistence and carrier-divergence, of record via
DISCHARGE/ATTEMPT citations of ZF §4.1(c) and 52f2490b; PA :63 "the first
nontrivial member"). Block membership decides nothing about scope — the
same freeze block carries tau_R = pi/sqrt2, permanent of record — each
item's scope is fixed by its consumption. **For the ell-half no consuming
byte pins the extent either way:** E1's single carrier-ell byte (:684-686)
is a DISCIPLINE ("no Hermite carrier index n, no ell, no truncation
level" — constants may not be functionals of either axis), and S2CAND's
certifying quantifier ("the n-INDEXED FAMILY WITH n-UNIFORM CONSTANTS",
:204-205, carrying that discipline at :243) names the family without an
ell-extent. r-A2's finding — membership "is a spec-reading no artifact
pins" (DISCHARGE :319-325) — is CONFIRMED at the bytes and now LOCALIZED:
the unpinned byte is exactly the ell-freeze's scope (family vs execution),
and ONLY that; the n-axis is settled. [DERIVED]

### 2.2 The dilation's image — exact (the tasked item (2))

With (U_L f)(x) = L^{-3/2} f(x/L) the chain's STEP-3 spatial unitary
(DISCHARGE S2g of record; the C^4 spinor factor untouched) and u_k(.; ell)
the normalized 1D Hermite axis factor at oscillator length ell:

```text
d-1  U_L u_k(.; ell) = u_k(.; L ell) — SAME degree k, EXACTLY [P1a];
     3D products factorize axis-wise (L^{-3/2} = (L^{-1/2})^3) [P1a3];
     orthonormal bases map to orthonormal bases [P1b] with matched-basis
     matrix the IDENTITY — no degree mixing [P1c]. Hence, spans being
     unitary-covariant:
         U_L H_(n,ell) = H_(n, L ell)   and   U_L^dag Q_(n,ell) U_L = Q_(n, ell/L)
     as EXACT subspace/projector identities. Nestedness in n is preserved
     (conjugation preserves inclusions; level n's basis is the first-n
     slice of level n+1's).
d-2  THE INDEX MAP OF THE COVARIANCE. Compressing STEP 5's identity
     V^{(L)}(a) = U_L V^{(1)}(La) U_L^dag with Q_(n,ell):
         Q_(n,ell) V^{(L)}(a) Q_(n,ell)
           = U_L [ Q_(n, ell/L) V^{(1)}(La) Q_(n, ell/L) ] U_L^dag .
     The same map under the Galerkin (generator-compressed) construction:
     U_L^dag [Q h^{(L)}(t) Q] U_L = Q' [U_L^dag h^{(L)}(t) U_L] Q' with
     Q' = Q_(n, ell/L), STEPS 1-5 carrying the generator to the unit cell
     at La, and time-ordering commuting with a fixed conjugation. EITHER
     construction:  n' = (n, ell/L) — TRUNCATION LEVEL INVARIANT,
     oscillator length divided by L. On the class direction L <= 1:
     ell/L >= ell (the dilated carrier is WIDER).
d-3  BOTH SEALED STATE SCHEMES RIDE THE SAME MAP: C^(mix) = Q P_- Q
     conjugates to Q' P_- Q' (P_- degree-0 homogeneous — conjugation-blind
     [P1i]); C^(pure) = 1_(-inf,0)(Q h_0 Q) conjugates to
     1_(-inf,0)(Q' (h_0/L) Q') = 1_(-inf,0)(Q' h_0 Q') (the negative-
     spectral indicator is blind to the positive factor 1/L [P1i]).
d-4  det/tr are conjugation-invariant (RA8a/CA5a of record), so Route
     III's display carries the index map EXPLICITLY:
         R^{(L)}_(n,ell)(a) = R^{(1)}_(n, ell/L)(La) .
```

**So the dilation produces, from the member H_(n,ell), EXACTLY the member
H_(n, ell/L) of the ell-parameterized Hermite realization — never anything
outside that realization — and the image is itself a genuine Galerkin
family per the defining class s-1** (finite rank, nested, strong
convergence, and the core/compression conditions transfer under the
unitary: a core for h_0 dilates to a core for h_0/L, which shares h_0's
domain and graph topology up to the constant). [DERIVED; grade: d-1/d-3/d-4
identity-grade (CAS-pinned instances + unitary covariance); d-2 carries
STEP 5's own sealed grade — identity-grade given the sealed constructions
— and adds only the projector identity d-1]

### 2.3 Closure — the derivation and the exact counterexample (the tasked item (3))

```text
c-0  MEMBER-WISE invariance (U_L H_(n,ell) = H_(n,ell)) FAILS for every
     L != 1: the per-axis ground-state overlap is sqrt(2L/(1+L^2)), and
     1 - 2L/(1+L^2) = (1-L)^2/(1+L^2) EXACTLY [P1d] — unity iff L = 1.
     Consumed NOWHERE in the chain (§2.4). [DERIVED]
c-1  FAMILY closure at the DECLARED bytes (s-2, ell a parameter):
     DERIVED. The index map (n, ell) -> (n, ell/L) stays inside the
     realization for every formal L > 0. On the class direction the orbit
     {ell/L : L in (0,1]} = [ell, infinity) exactly [P1g]. GENERAL
     CRITERION, exact: the family with ell-extent E is closed under the
     class dilations IFF E · [1, infinity) is contained in E (E an upward
     multiplicative ray); (0, infinity) and every [c, infinity) qualify
     [P1g]. The declared realization (ell range-unrestricted) closes.
c-2  FAMILY closure at the FROZEN-EXECUTION reading (E = {1, sqrt2},
     n unbounded — the strongest coherent frozen-ell reading; the purely
     executed n-reading {2,4} is already incoherent with BASELINE :43-44,
     §2.1): REFUTED, exact counterexample. {1/L, sqrt2/L} inside
     {1, sqrt2} FORCES (1/L, sqrt2/L) = (1, sqrt2), i.e. L = 1 [P1f]: the
     executed set is closed under NO nontrivial class dilation — the case
     1/L = sqrt2 exits at the other index, sqrt2/L = 2, and 2 is not in
     {1, sqrt2} (exact surds). STRONGER, at subspace level: for
     ell' = ell/L off the frozen pair, H_(n, ell') is not a member, equals
     no member, and is CONTAINED IN no member at ANY truncation — writing
     its ground state as p(x) e^{-x^2/(2 ell^2)} forces p = c exp(-c2 x^2)
     with c2 != 0 and EVERY even series coefficient (-c2)^m/m! nonzero
     [P1e]: no polynomial of any degree; so H_(n, ell') is not inside
     union_m H_(m, ell) for ell' != ell. And even containment would
     consume nothing at determinant level: no determinant compression
     monotonicity exists, of record (AUDIT :160-165). [DERIVED]
c-3  HONESTY NOTE — the refuted horn strands NOTHING sealed. The
     first-order delivery never consumed membership: REM :380-382's
     compression line ||Pi X Pi||_2 <= ||X||_2 is PROJECTOR-BLIND — it
     bounds the compression onto H_(n, ell/L) whether or not that space
     is "in the family". The alpha' = 1/4 similarity-class result and
     every first-order estimate stand unmoved under EITHER reading
     (r-A2's own point, confirmed at REM's bytes). [DERIVED]
```

### 2.4 The exact consuming step — which closure the chain needs

```text
e-1  FIRST ORDER CONSUMES NO MEMBERSHIP AT ALL. STEP 6/7's domination
     runs through the uncompressed norm; the premise was named "only for
     the n' index bookkeeping" and discharged by the projector-blind
     compression line (DISCHARGE r-A2 :319-325; REM :380-382). P-1 is a
     REAL premise ONLY at determinant level (ATTEMPT :355-363; AUDIT
     :143-167 CONFIRMED): R_n is a scalar, not a norm; det of a
     compression is not dominated by det of the full object.
e-2  THE SECOND-ORDER CONSUMING STEP is the class-reduction passage of
     Route III (ATTEMPT :366-370; §4 i-2 :941-946): from
     R^{(L)}_(n,ell)(a) = R^{(1)}_(n, ell/L)(La) to "the cell axis of
     R.3'(c) REDUCES to the unit cell":
       sup_{(n,ell) in F} |R^{(L)}_(n,ell)(a)|
         = sup_{(n,ell) in F} |R^{(1)}_(n, ell/L)(La)|
         <= sup_{(n',ell') in F'} |R^{(1)}_(n',ell')(La)|      (*)
         <= (24/pi)^{1/2} |C|_4^{1/2}-weighted Q^Sigma_1-shape
            at the collapse level [(L eps)^m <= L^2 eps^m, m >= 2;
            L^2 = (24/pi)^{1/2}|C|_4^{1/2}, re-pinned P1h].
     THE MEMBERSHIP CONSUMPTION IS EXACTLY INEQUALITY (*): image_L(F)
     inside F', with F the carrier range at the admitted cell and F' the
     range over which the (still-open) unit-cell bound Q^Sigma_1 is
     certified. It consumes FAMILY-LEVEL closure — a quantifier-range
     inclusion — and NEVER member-wise closure (c-0's failure is
     harmless: no step conjugates a member onto itself).
e-3  THE ABSORPTION (reading-independent, exact). "Closure of the family"
     is (*) in the special case F' = F. In general the reduction needs
     precisely: F' contains union over class scales L in (0,1] of
     image_L(F) — the ell-axis ray closure of F [P1g]. Under the DECLARED
     reading (F the full realization) that union IS F: closure DERIVED
     (c-1), P-1 holds, the reduction rests on derived ground. Under the
     FROZEN-EXECUTION reading, family closure is REFUTED (c-2) — but the
     reduction is not thereby lost: it re-lands on the requirement that
     Q^Sigma_1 be certified over the ray range {(n, ell') : n >= 1,
     ell' in [1, infinity)} (the union of [1,inf) and [sqrt2,inf), P1g),
     every member of which is a genuine Galerkin carrier (§2.2). EITHER
     WAY the standalone membership premise DISSOLVES into a
     quantifier-width condition on the ALREADY-OPEN residue: certify
     Q^Sigma_1 over an ell-upward-ray carrier range. No separate closure
     premise survives. And E1 :684-686's discipline (admissible constants
     carry no n, no ell) already demands exactly the constant shape a
     ray-quantified certification uses — the ray widens the STATEMENT'S
     range, adds nothing to the CONSTANT'S shape. A residue certified
     ONLY at the executed two-member ell-set can NOT feed (*) (c-2: the
     first nontrivial refinement already exits the set).
e-4  WHAT WOULD RE-OPEN THE QUESTION: only a future byte freezing the
     family's ell-extent to a non-ray set AND barring the residue from
     ray quantification. No sealed byte does either. [DERIVED]
```

### 2.5 THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `p1venv` (under /private/tmp scratch; nothing
written to the workspace but this artifact and its seal). Exact symbolic /
exact rational/surd only; nothing numeric evaluated.

```python
# P1-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv p1venv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated.
# Groups: P1a-P1c the dilation image of the sealed Hermite carrier (exact);
# P1d-P1e member-level non-invariance and escape (exact witnesses);
# P1f-P1g the index-set closure arithmetic (executed set vs ray);
# P1h class-weight re-pins (of record); P1i state-scheme conjugation pins.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

x, y, z, mu_ = sp.symbols('x y z mu_', real=True)
L, ell, ellp, e_, c2 = sp.symbols('L ell ellp e_ c2', positive=True)

def u(k, xx, l):
    # normalized 1D Hermite function with oscillator length l (baseline spec A-family axis factor)
    return (sp.hermite(k, xx/l) * sp.exp(-xx**2/(2*l**2))
            / (sp.pi**R(1, 4) * sp.sqrt(2**k * sp.factorial(k)) * sp.sqrt(l)))

# ===== P1a — THE DILATION IDENTITY, DEGREE-WISE EXACT (k = 0,1,2,3) =====
# (U_L f)(x) = L^{-1/2} f(x/L):  U_L u_k(.; ell) = u_k(.; L ell) — same degree k.
ok("P1a U_L u_k(.;ell) = u_k(.;L ell) exactly, k = 0..3 (degree INVARIANT)     ",
   all(sp.simplify(u(k, x, L*ell) - u(k, x/L, ell)/sp.sqrt(L)) == 0 for k in range(4)))

# 3D product instance (a,b,c) = (0,1,2): L^{-3/2} factorizes axis-wise
ok("P1a3 U_L[u_0(x)u_1(y)u_2(z)](.;ell) = product at L ell (3D, one instance) ",
   sp.simplify(u(0, x, L*ell)*u(1, y, L*ell)*u(2, z, L*ell)
               - u(0, x/L, ell)*u(1, y/L, ell)*u(2, z/L, ell)/(sp.sqrt(L)**3)) == 0)

# ===== P1b — ORTHONORMALITY AT SYMBOLIC ell (Gram = identity; j,k = 0..2) =====
gram = [[sp.integrate(u(j, x, ell)*u(k, x, ell), (x, -sp.oo, sp.oo)) for k in range(3)]
        for j in range(3)]
ok("P1b Gram_{jk}(ell) = delta_{jk} at symbolic ell (normalized family)        ",
   all(sp.simplify(gram[j][k] - (1 if j == k else 0)) == 0
       for j in range(3) for k in range(3)))

# ===== P1c — NO DEGREE MIXING ACROSS THE DILATION (matched-basis matrix = identity) =====
mix = [[sp.integrate(u(j, x, L*ell)*u(k, x/L, ell)/sp.sqrt(L), (x, -sp.oo, sp.oo))
        for k in range(3)] for j in range(3)]
ok("P1c <u_j(.;L ell), U_L u_k(.;ell)> = delta_{jk} (truncation level carried) ",
   all(sp.simplify(mix[j][k] - (1 if j == k else 0)) == 0
       for j in range(3) for k in range(3)))

# ===== P1d — MEMBER-WISE INVARIANCE FAILS FOR EVERY L != 1 (exact witness) =====
ov = sp.integrate(u(0, x, ell)*u(0, x, ellp), (x, -sp.oo, sp.oo))
ov_closed = sp.sqrt(2*ell*ellp/(ell**2 + ellp**2))
ov_L = ov_closed.subs(ellp, ell/L)
ok("P1d <u_0(.;ell), u_0(.;ellp)> = sqrt(2 ell ellp/(ell^2+ellp^2)) exact;"
   " at ellp = ell/L it is sqrt(2L/(1+L^2)); 1 - 2L/(1+L^2) = (1-L)^2/(1+L^2)",
   sp.simplify(ov - ov_closed) == 0
   and sp.simplify(ov_L - sp.sqrt(2*L/(1 + L**2))) == 0
   and sp.simplify(1 - 2*L/(1 + L**2) - (1 - L)**2/(1 + L**2)) == 0
   and sp.solveset(sp.Eq((1 - L)**2, 0), L, domain=sp.S.Reals) == {1})

# ===== P1e — ESCAPE FROM THE WHOLE FIXED-ell UNION (exact) =====
# u_0(.;ellp) = p(x) e^{-x^2/(2 ell^2)} forces p = const * exp(-c2 x^2),
# c2 = 1/(2 ellp^2) - 1/(2 ell^2); every even series coefficient of exp(-c2 x^2)
# is (-c2)^m/m! != 0 when c2 != 0 — no polynomial of ANY degree matches.
coeffs = sp.Poly(sp.series(sp.exp(-c2*x**2), x, 0, 10).removeO(), x).all_coeffs()[::-1]
pos_roots = [r_ for r_ in sp.solve(sp.Eq(1/(2*ellp**2) - 1/(2*ell**2), 0), ellp)
             if r_.is_positive]
ok("P1e [x^{2m}] exp(-c2 x^2) = (-c2)^m/m!, m = 0..4, each nonzero for c2 != 0;"
   " and c2 = 0 iff ellp = ell on the positive domain (unique positive root)",
   all(sp.simplify(coeffs[2*m] - (-c2)**m/sp.factorial(m)) == 0 for m in range(5))
   and all(((-c2)**m/sp.factorial(m)).is_nonzero is True for m in range(5))
   and len(pos_roots) == 1 and sp.simplify(pos_roots[0] - ell) == 0)

# ===== P1f — THE EXECUTED-SET READING IS NOT CLOSED (exact index arithmetic) =====
# E = {1, sqrt2}; need {1/L, sqrt2/L} subset E. Enumerate (1/L, sqrt2/L) = (a,b) in E^2:
Eset = [sp.Integer(1), sp.sqrt(2)]
closure_scales = []
for a in Eset:
    for b in Eset:
        # 1/L = a and sqrt2/L = b force b = sqrt2 * a; then L = 1/a.
        if sp.simplify(b - sp.sqrt(2)*a) == 0:
            closure_scales.append(sp.simplify(1/a))
ok("P1f {1/L, sqrt2/L} subset {1,sqrt2} forces (1/L,sqrt2/L)=(1,sqrt2), L = 1 ONLY;"
   " the case 1/L = sqrt2 exits: sqrt2/L = 2, and 2 notin {1,sqrt2} (exact surds)",
   closure_scales == [1]
   and sp.simplify(sp.sqrt(2)*sp.sqrt(2) - 2) == 0
   and all(sp.simplify(2 - v) != 0 for v in Eset))

# ===== P1g — THE RAY CHARACTERIZATION (exact) =====
# {e/L : L in (0,1]} = [e, oo): e/L monotone (d/dL = -e/L^2 < 0), value e at L = 1, -> oo;
# rays are closed: e >= c and 0 < L <= 1 give e/L - e = e(1-L)/L >= 0, so e/L >= e >= c.
ok("P1g d/dL(e/L) = -e/L^2 (exact, negative); e/L|_{L=1} = e; lim_{L->0+} e/L = oo;"
   " e/L - e = e(1-L)/L (exact factorization, >= 0 on 0 < L <= 1)",
   sp.simplify(sp.diff(e_/L, L) + e_/L**2) == 0
   and (e_/L**2).is_positive is True
   and sp.simplify((e_/L).subs(L, 1) - e_) == 0
   and sp.limit(e_/L, L, 0, '+') == sp.oo
   and sp.simplify(e_/L - e_ - e_*(1 - L)/L) == 0)

# ===== P1h — CLASS-WEIGHT RE-PINS (of record: RA8c/RA8d, CA5b, S2b) =====
t = sp.symbols('t', real=True)
r_L = sp.Piecewise((t, t <= L/2), (L - t, True))
V4L = sp.integrate(R(4, 3)*sp.pi*r_L**3, (t, 0, L))
m_, eps_ = sp.symbols('m_ eps_', positive=True)
ok("P1h |C_L|_4 = pi L^4/24 EXACT; L = (24|C|_4/pi)^{1/4}; L^2 = (24/pi)^{1/2}|C|_4^{1/2};"
   " (L eps)^m = L^2 L^{m-2} eps^m; d/dm L^m = L^m log L",
   sp.simplify(V4L - sp.pi*L**4/24) == 0
   and sp.simplify((24*(sp.pi*L**4/24)/sp.pi)**R(1, 4) - L) == 0
   and sp.simplify((24/sp.pi)**R(1, 2)*(sp.pi*L**4/24)**R(1, 2) - L**2) == 0
   and sp.simplify((L*eps_)**m_ - L**2*L**(m_ - 2)*eps_**m_) == 0
   and sp.simplify(sp.diff(L**m_, m_) - L**m_*sp.log(L)) == 0)

# ===== P1i — STATE-SCHEME CONJUGATION PINS =====
kx, ky, kz = sp.symbols('kx ky kz', real=True)
nk = sp.sqrt(kx**2 + ky**2 + kz**2)
nkL = sp.sqrt((kx/L)**2 + (ky/L)**2 + (kz/L)**2)
ineq = sp.reduce_inequalities(mu_/L < 0, mu_)
ok("P1i band projector degree-0: (kx/L)/|k/L| = kx/|k| (P_- conjugation-blind);"
   " negative-spectral indicator scaling-blind: mu/L < 0 iff mu < 0 (L > 0)",
   sp.simplify((kx/L)/nkL - kx/nk) == 0
   and ineq == sp.reduce_inequalities(mu_ < 0, mu_))

print("P1-BUILD-DONE")
```

Output, verbatim (10/10 PASS):

```text
P1a U_L u_k(.;ell) = u_k(.;L ell) exactly, k = 0..3 (degree INVARIANT)     : PASS
P1a3 U_L[u_0(x)u_1(y)u_2(z)](.;ell) = product at L ell (3D, one instance) : PASS
P1b Gram_{jk}(ell) = delta_{jk} at symbolic ell (normalized family)        : PASS
P1c <u_j(.;L ell), U_L u_k(.;ell)> = delta_{jk} (truncation level carried) : PASS
P1d <u_0(.;ell), u_0(.;ellp)> = sqrt(2 ell ellp/(ell^2+ellp^2)) exact; at ellp = ell/L it is sqrt(2L/(1+L^2)); 1 - 2L/(1+L^2) = (1-L)^2/(1+L^2): PASS
P1e [x^{2m}] exp(-c2 x^2) = (-c2)^m/m!, m = 0..4, each nonzero for c2 != 0; and c2 = 0 iff ellp = ell on the positive domain (unique positive root): PASS
P1f {1/L, sqrt2/L} subset {1,sqrt2} forces (1/L,sqrt2/L)=(1,sqrt2), L = 1 ONLY; the case 1/L = sqrt2 exits: sqrt2/L = 2, and 2 notin {1,sqrt2} (exact surds): PASS
P1g d/dL(e/L) = -e/L^2 (exact, negative); e/L|_{L=1} = e; lim_{L->0+} e/L = oo; e/L - e = e(1-L)/L (exact factorization, >= 0 on 0 < L <= 1): PASS
P1h |C_L|_4 = pi L^4/24 EXACT; L = (24|C|_4/pi)^{1/4}; L^2 = (24/pi)^{1/2}|C|_4^{1/2}; (L eps)^m = L^2 L^{m-2} eps^m; d/dm L^m = L^m log L: PASS
P1i band projector degree-0: (kx/L)/|k/L| = kx/|k| (P_- conjugation-blind); negative-spectral indicator scaling-blind: mu/L < 0 iff mu < 0 (L > 0): PASS
P1-BUILD-DONE
```

---

## 3. NET — WHAT Q-1050'S CONDITIONAL BECOMES IF THIS RESULT HOLDS (descriptive only; no verdict moved)

```text
IF the determination of §2 holds (ALL_RESULTS = CLAIMED until checked):
n-1  The class reduction of record (ATTEMPT i-2: "the cell axis of
     R.3'(c) REDUCES to the unit cell ... CONDITIONAL on premise P-1")
     READS, exactly: the |C|_4^{1/2} collapse-level reduction (and the
     |C|_4^{1/4} general floor) is CONDITIONAL on the unit-cell residue
     Q^Sigma_1 being certified AT THE RAY QUANTIFIER — all n, all ell in
     an upward multiplicative ray (under the declared family reading, the
     family itself; minimally [1, infinity) for the executed starting
     carriers) — and on NOTHING ELSE from P-1: the standalone membership
     premise is dissolved (DERIVED at the quantifier the consuming step
     needs; REFUTED only at a reading whose failure the reduction routes
     around, §2.4 e-3).
n-2  The ATTEMPT's residue list (i) surviving-instance square, (ii) the
     ten-term pairwise-difference e_2 form, (iii) H-R + C-L2 for the
     tail, (iv) P-1 for the class reduction — becomes (i), (ii), (iii)
     UNCHANGED, with (iv) REPLACED by the exact quantifier condition on
     the future certification of (i)/(ii): ray-wide in ell, n-uniform.
     One independent unknown fewer; no new obligation added — E1
     :684-686's discipline already forces the certified constant to be
     (n, ell)-blind, so the ray widens the statement's range only.
n-3  NOTHING ELSE MOVES. The adopted amendment and its delivered
     alpha' = 1/4 estimate are untouched at their own quantifier (first
     order never consumed P-1 — the compression line is projector-blind,
     REM :380-382). Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
     STANDS — this artifact narrows the reduction's PREMISE, not the
     residue's analytic content. R-L0's non-congruent-cell axis (r-A1)
     is untouched — congruence, not carrier, is its axis. Gate-list
     membership unchanged (R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D; H-R/
     H-B0 inside). Per-pair record (52f2490b) untouched. No verdict on
     FIXED/RUNNING advanced or retarded.
n-4  FOR ANY FUTURE DISCHARGE ATTEMPT AT THE RESIDUE, the consumable
     target is now exact: certify (i)/(ii) n-uniformly at the unit cell
     with the carrier quantifier "all n >= 1, all ell in the ray" —
     a certification at the executed two-member ell-set alone cannot
     feed the class reduction (§2.3 c-2, §2.4 e-3).
```

---

## 4. FLAG BLOCK

```text
P1_STATUS = PARTIAL-DELIVERED(
  closure DERIVED exactly for the carrier family at its declared bytes —
  the dilation image is U_L H_(n,ell) = H_(n, L ell) degree-invariantly,
  the covariance's index map is (n, ell) -> (n, ell/L) with truncation
  level n INVARIANT, so the premise lives entirely on the ell-axis and
  the declared ell-parameterized realization closes under every class
  dilation (criterion, exact: ell-extent an upward multiplicative ray);
  closure REFUTED by exact counterexample at the frozen-execution reading
  ell in {1, sqrt2} — closed only at L = 1, the dilated member escaping
  every fixed-ell member at every truncation level (P1e/P1f); the single
  unpinned byte is the ell-freeze's scope (family vs execution — r-A2's
  reading gap CONFIRMED and LOCALIZED, the n-half settled by BASELINE
  :43-44's core sentence); and the consuming step never needs that byte
  resolved — the premise dissolves into the residue's quantifier width. )

CONSUMING_STEP = ROUTE-III-CLASS-REDUCTION-MEMBERSHIP-INEQUALITY(
  the passage sup_{(n,ell) in F} |R^{(L)}(a)| = sup |R^{(1)}_{(n,ell/L)}
  (La)| <= sup_{F'} |R^{(1)}(La)| inside ATTEMPT §2.5's consequence
  clause — the inclusion image_L(F) inside F' — which consumes
  FAMILY-level closure (a quantifier-range inclusion) and NEVER
  member-wise closure (false for every L != 1, consumed nowhere); at
  first order the analogous step consumed no membership at all (REM
  :380-382 projector-blind), which is exactly why P-1 exists only at
  determinant level (AUDIT :143-167: no det compression monotonicity
  sealed). )

ADOPTED_ESTIMATE_BEARING = NONE(
  every delivered sealed estimate is untouched: the adopted operator
  clause S2'/R-L2b^Sigma and the alpha' = 1/4 similarity-class result
  never consumed P-1 (the compression line is projector-blind), this
  artifact's findings live at the carrier index-set level, no falsifier
  V-1..V-6 engages, and the refuted horn strands nothing sealed. )

NET = P1-DISSOLVED-INTO-RESIDUE-QUANTIFIER(
  descriptive only: IF this result holds, Q-1050's |C|_4^{1/2} class
  reduction becomes conditional on the already-open unit-cell residue
  Q^Sigma_1 certified at the ray quantifier (all n, all ell in an upward
  ray — under the declared reading, the family itself) and on nothing
  else from P-1 — one independent premise fewer, the constant shape
  already forced by E1 :684-686; witness
  SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS; gate-list membership,
  R-L0's non-congruent-cell axis, the per-pair record, and FIXED/RUNNING
  all unmoved; no flag flips; retirement/entry the registrar's. )

SEALS_VERIFIED = 9/9(
  57edbb96 ATTEMPT; 38aa39d1 AUDIT; e5b0879b DISCHARGE; 789338ad PA;
  46846730 E1; ce59b480 S2CAND; 2e4e5163 REM — all vs sidecars and tasked
  prefixes; 80aa4e17 BASELINE and a1258dcf PROVENANCE vs PA's sealed
  frozen-authority table (no standalone sidecars exist) — all recomputed
  from bytes at path this session by full digest; none unverifiable. )

FORBIDDEN_IMPORTS = none(
  no scale authority (L formal, never instantiated; the index
  arithmetic's case points are formal solutions of the quantifier's own
  equations); no imported GR; no faithfulness authority; no physical
  quantity computed, bounded numerically, or evaluated — every constant
  symbolic or an exact rational/surd; alpha never computed or approached;
  M(t) sharp (untouched; STEP 1 consumed as sealed); equal-time FALSE by
  C6 (no equal-time object formed); NO CARRIER OR CELLULATION DATUM
  INTRODUCED — no member added to any family, ell/L is the covariance's
  own image, the executed values cited are sealed data, and the ray is a
  DERIVED criterion, not a new frozen range; no register/tracker/plan/
  road/ledger/lens file read; no number frozen. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv p1venv under
  /private/tmp scratch, 10 exact identity/enumeration groups P1a-P1i over
  symbols, script and output reproduced verbatim in §2.5 (10/10 PASS);
  shasum/file reads otherwise; NOTHING numeric evaluated.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
P1_DILATION_CLOSURE_RESULT = SEALED.
```
