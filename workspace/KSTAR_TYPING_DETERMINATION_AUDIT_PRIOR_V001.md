# K*-TYPING DETERMINATION — AUDIT V001 (2026-08-14)

Auditor: KST-AUDIT (independent; briefed stance: REFUTE — the build is wrong until each claim
survives re-derivation at the bytes). Subject: `KSTAR_TYPING_DETERMINATION_V001.md`, recomputed
at path = `2b12dc4066794d49a012e81aca54ca5055d8dbfdfcf83c3eff5aead4c2a88d1c` = the tasked digest,
sidecar MATCH. Instrument: ACTUAL_SURFACE_GUARD_V001 (`2baa4c31…`), all four principal
calibration clauses governing.

INDEPENDENCE PROTOCOL, kept: question (2) was re-derived from the defining condition's own
ingredients — and the question (1) trace hunt and question (3) referent checks were run — with
the answers fixed in working notes BEFORE the subject file was opened at all. The subject was
then read once, in full, and attacked at its cited bytes; every digest it claims was recomputed
at path; its search boundary (its Choice Ledger C-1) was deliberately stepped outside.

Fences live throughout: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false. No numeric evaluation, no closed form of K*, no evaluation of
alpha_micro, no root, response, scale, or coupling evaluated on either side of this audit;
every numeral below is a sealed-text quotation, a digest, an occurrence count, or a line
number. No register, road, ledger, lens, plan, or tracker file opened (disclosure at Ledger
A-5). One output file + seal; no other writes; no git.

---

## 0. VERDICT (one line)

CONFIRMED-WITH-CORRECTIONS — all three of the build's verdicts survive independent re-derivation from the defining condition's ingredients and a deliberately wider hunt (NOT-TIED at question 1; TYPED-MAGNITUDE / APPARATUS-ONLY-as-STATUS with the I2 vacancy engaged at question 2; IDENTITY-at-§IV plus HOMONYM-at-§II.9 at question 3, extended by this audit in the reverse direction: no other K-family object — K_R, k_R, K_KK, K_n, K_square, kappa_record, kappa_Thomson — can claim §IV's referent), with three corrections named, every one running FAVORABLE to the build's conclusions: (C1) the onset gate V003 is NOT unsealed — its sidecar exists under the old naming convention (`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.seal.sha256`, no `.md`), prefix cc113a16 MATCH, mtime 2026-07-23 predating the build, so the build's §2.3 corroboration and half of its C-6 custody premise upgrade from flagged to sealed custody; (C2) the build's §2.2 quantifier "every J_FS-carrying non-fenced file in both roots" enumerates four files but the true carrier set is TWELVE — a quantifier slippage in the guard's own failure-mode-1 sense, and internally inconsistent since the build's own §2.1 cites a fifth carrier — yet this audit's full twelve-carrier re-scan finds zero K_*/K_star/C_record tokens in every carrier (the one co-carrying file, DERIVED_RESULTS_INVENTORY, holds J_FS at :174 and K_* at :192 in unrelated entries; the only binding texts anywhere remain the FABLE F1/OPUS5 glosses the build adjudicated), so the corrected quantifier STRENGTHENS the absence; (C3) the cited `primitive_record_cell_selection_principle_v002.md` is not merely TRACE-INCOMPLETE-as-a-file — stepping outside the build's boundary it EXISTS in the origin program tree at `/Users/bgm/Documents/Documents - Brian’s MacBook Pro/New project/gravity_emergence_evidence_program/`, SHA-256 `a3c7349bf7b1dbc4f87bd449effa7d82040ab8967e3c5c79f5dd89705fd6a839`, digest-pinned EXACTLY by its own sealed provenance manifest (`primitive_record_cell_selection_manifest_v002.json`, sealed_date 2026-07-20, timestamp files alongside), its :95-97/:104 carry the defining condition VERBATIM as both of the build's display carriers quote it (the build's C-3 premise is hereby verified at source), and its own text supplies decisive bytes the build never saw — K is defined as the coefficient of `(K/4) integral sqrt(|g|) F_munu F^munu d^4x` on cell data `X_K = [Omega_K, g_K, Delta tau_K, A_K, Psi_K]` gated by the `D_BR(K; X_K)` spectral opening conditions, i.e. a metric-weighted, amplitude-squared MAGNITUDE functional (TYPED-MAGNITUDE confirmed at the definition's source), and the selector itself rules "The precise map from the spectrum to durable-record closure must be derived from the record theory" (the tie is an underived demand AT THE DEFINITION'S OWN HOME — NOT-TIED confirmed at source), with zero J_FS/Fubini/onset-bound tokens in v002, v003, or v004.

---

## 1. PROVENANCE — every digest recomputed at path

### 1.1 Subject and tasked authorities

| Document | Recomputed SHA-256 (prefix; full where tasked) | Status |
|---|---|---|
| `workspace/KSTAR_TYPING_DETERMINATION_V001.md` | `2b12dc4066794d49a012e81aca54ca5055d8dbfdfcf83c3eff5aead4c2a88d1c` | = tasked digest; sidecar MATCH |
| `alpha_supervision/ALPHA_STIFFNESS_IS_K_STAR_PRINCIPAL_DECISION_2026-07-30.md` | `754506191e1c9129101ada17255e07db55e48619b291c1190d3af011ec1e8126` | = tasked prefix 75450619 |
| `alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` | `20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df` | = tasked prefix 20ee87c0 |
| `alpha_supervision/ACTUAL_SURFACE_GUARD_V001.md` | `2baa4c31fbe566516c3f61dcac351889e914368bd33b8c96ff40a161788c4fd5` | verified (no tasked prefix) |
| `workspace/BARE_SURFACE_I2_DETERMINATION_V001.md` | `9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c` | = tasked prefix 95235195; = sidecar |
| `workspace/BARE_SURFACE_I2_DETERMINATION_AUDIT_V001.md` | `97ae605d065b7b614ed2927602403cd72550507b26bf71fb0f7299be4c545d73` | = tasked prefix 97ae605d; = sidecar |
| `workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` | `04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d` | = sidecar; the §II.8-9 source |

### 1.2 The build's §1.3 authority stock — all recomputed

`STAGE8_F0_KSTAR_DEFINING_CHAIN_CODEX2_V001.md` 95c003ca = sidecar MATCH ·
`STAGE8_F0_COMPUTAND_CHAIN_V001.md` 05eeab53 = sidecar MATCH ·
`STAGE8_KAPPA_RECORD_KSTAR_PAIR_TEST_UNDER_Q61_V001.md` 5d86c9c1 = sidecar MATCH and =
CODEX2's pinned D10 digest (cross-attestation verified) ·
`STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md` 45fa8f6e = sidecar MATCH (and = the digest
OPUS5 quotes at its :15) · `STAGE8_S16_BETA_CHECK_OPUS5_V001.md` 9c9e120e — NO sidecar,
build's flag CONFIRMED · `alpha_supervision/DERIVED_RESULTS_INVENTORY_2026-08-01_V001.md`
f12a95b4 = sidecar MATCH · `CHAIN_RELEVANCE_MAP_V001.md` eae6cf88 = sidecar MATCH ·
`STAGE8_SLOT16_REPOSED_SCOPE_AND_FK1_AUDIT_V001.md` df0d1c74 = sidecar MATCH ·
`alpha_supervision/RESULT_TWO_STIFFNESS_OBJECTS_2026-07-30.md` f3eb0c84 = sidecar MATCH = the
digest the decision quotes at its :7 ·
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md` cc113a16 — **SIDECAR EXISTS AND
MATCHES** (correction C1: old naming convention `…V003.seal.sha256` without `.md`; file mtime
2026-07-23, i.e. sealed three weeks before the build; the build's "NO SIDECAR anywhere in the
archive" is false at path).

Span pins recomputed byte-exact against CODEX2's table: D07 =
`STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md[17597,20485)` sha
`775e7191fd4e83ee5116dd4d0cb937f83679b902c983a313d6fd99a2bf7c19ae` MATCH (the C_record
definition span); D13 = `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V005.md[29145,30285)` sha
`92f6a9d5e6bcbaeb652199f058ece7091d1c1a461990f86d29789d58b01091af` MATCH (the S16 re-posed
obligation span). Carrier files verified: `STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md` 2d63dfad,
`STAGE8_C_RECORD_LIVE_DEFINITION_CURRENCY_AUDIT_V001.md` 885265bb — both = CODEX2's pins.

### 1.3 Outside the build's boundary (audit-directed step-out)

`primitive_record_cell_selection_principle_v002.md` LOCATED at the origin tree (path in §0),
SHA-256 `a3c7349bf7b1dbc4f87bd449effa7d82040ab8967e3c5c79f5dd89705fd6a839`; its provenance
manifest `primitive_record_cell_selection_manifest_v002.json` pins that exact digest
(sealed_date 2026-07-20; `.tsr`/`.tsq` timestamp files alongside; manifest flags
`operator_files_present_at_seal=false`, `K_star_computed_at_seal=false`,
`alpha_computed_at_seal=false`). Read-only; lines :95-97 (`C_record(K_*) = 0`,
`d C_record / dK at K_* != 0`, `K_* > 0`) and :104 (`alpha_micro = 1 / (4 pi K_*)`) verified
VERBATIM as the decision cites and as both display carriers quote. The origin tree's own J_FS
carriers are byte-mirrors of archive files already swept (plus a register copy, not opened);
v002/v003/v004 of the selection principle contain ZERO J_FS/Fubini/onset-bound tokens.
`a32_holdout/` not entered. No file outside `MB Work` was modified.

### 1.4 The build's line-cites — spot-verified at bytes, zero misquotes found

Decision :11-13, :15-16, :23-25, :30-32, :46-47, :54-55, :67-69 ✓; two-stiffness :16-19
(defining condition displayed), :52, :55-58, :60-64 ✓; surface: `K_*` occurs ONLY at :63 and
:68 ✓ (grep, whole file); the hunt: ZERO `K_*`/`K_star` occurrences ✓, onset budget at
:83-85, junction split at :110-118 ✓; F0 chain :278-279 (TYPE-U flags), :303-308 (`K e_HL^2 =
1`; generic-DERIVED vs physical-ADOPTED), :493-497 (residual required-not-built, F-RP1 FIRED)
✓; pair test :125-134 ("not as write strength"; "surrogate coordinate for an induced
connection response") ✓; inventory :174-175 (saturation ADOPTED), :192 (`alpha_micro = 1/(4
pi K_*)` ITSELF ADOPTED, Q-140 N11) ✓; slot16 audit :20-22 ("conditioned on the interacting
pole-versus-infraparticle outcome, not conditioned on the onset root") ✓; onset gate :88-90,
:163, :172-173 ✓; FABLE :124-125 and :283 ✓; OPUS5 :15, :21-23 (five tasked files; un-tasked
sources NOT opened — the blind-spot mechanism confirmed), :93 ✓; relevance map item 28
:409-424, U-11 :471-473, item 27's I2-constraint parallel ✓; I2 audit :9
CONFIRMED-WITH-CORRECTIONS with no verdict-chain correction ✓; I2 mechanism lines (CAS B2.9,
B3.1 "A strength has nothing on-record to act on"; §3.5 "structural, not informational") ✓.

---

## 2. QUESTION (1) — THE TRACE, adjudicated: **NOT-TIED CONFIRMED** (correction C2 applied)

### 2.1 The audit's independent hunt (run before the build was opened)

Protocol: from the surface side, the §II.9 lock's content and lineage were established first
(hunt :83-85, :110-114 — onset budget `J_FS,rel >= pi hbar/2`, derived; saturation adopted;
the fourth derived beta-blind junction; its derivation carrier
`PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` derives `J_FS = pi hbar/2` and
`Delta S_record = 2 J_FS` inside the imported C^2 representation and DISCLAIMS, in its own
"Not established" list, physical durability and the finite electromagnetic response — the
exact territory where K* lives). From the K* side, CODEX2's sixteen fixing spans were taken
as the defining census (its 1,787-file multi-form sweep) and independently spot-verified
(§1.2). The two lineages share not one span, not one token. Independent verdict: NOT-TIED —
formed before the build's §2 was read, and convergent with it.

### 2.2 Correction C2 — the carrier enumeration, re-quantified

The build's §2.2 first bullet claims a scan of "every J_FS-carrying non-fenced file in both
roots" and lists FOUR files. The audit's enumeration (both roots, `*.md`, fenced classes
excluded, the subject and this audit excluded): **twelve carriers** — workspace:
`BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md`,
`PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md`,
`STAGE8_AXN_GRAMMAR_CROSSCHECK_DARIO_V001.md`, `STAGE8_BETA_SURFACE_DERIVATION_CHECK_OPUS5_V001.md`,
`STAGE8_BOUNDARY_FORCING_CHARACTERIZATION_FABLE_V001.md`,
`STAGE8_LOAD_BEARING_HOLONOMY_DERIVABILITY_DETERMINATION_V001.md`,
`STAGE8_ONSET_SATURATION_STEP3_FORCE_CHECK_V001.md`,
`STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md`; supervision:
`AXIOM_SET_MAP_2026-07-29.md`, `BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md`,
`DERIVED_RESULTS_INVENTORY_2026-08-01_V001.md`,
`RELAY_PASTE_255_DISCHARGE_STEP3_ONSET_SATURATION_V001.md`. The build's own §2.1 cites
DERIVED_RESULTS_INVENTORY's J_FS lines — a fifth carrier its §2.2 "every" does not cover:
quantifier slippage (guard failure mode 1), in a determination issued under that guard.

**Result of the corrected scan — the conclusion is unchanged and STRENGTHENED:** every one of
the twelve carriers scans ZERO for `K_*`, `K_star`, and `C_record` (the four the build listed
additionally scan zero for "stiffness", verified), with two typed exceptions, neither a tie:
DERIVED_RESULTS_INVENTORY carries J_FS at :174 and `K_*` at :192 in two unrelated entries
(the :192 entry being the ADOPTED-status booking of the alpha_micro formula — evidence FOR
the build's §3, not a tie); and the FABLE/OPUS5 pair carry the glosses adjudicated at §2.3
below. Bonus corroboration found in the corrected sweep: BOHM sweep :154 — the corpus's
one-sided inequalities (`J_FS,rel >= pi hbar/2` among them) yield magnitudes "only by
adopting saturation", and "the corpus's one exact equality (the one-cell kernel) … carries
no stiffness."

### 2.3 The contrary text (FABLE F1 / OPUS5) — the build's adjudication re-verified, then sharpened

All four of the build's §2.4 points verified at bytes: (1) F1's cited spans contain zero `K_*`
tokens — surface §II.8-9 has none (the file's only two occurrences are :63/:68 in §IV), the
hunt has none anywhere ✓; (2) the identification contradicts the definition of record — K* is
the root of `C_record` in the K-indexed stiffness family, and the §II.9 lock is an inequality
in `J_FS,rel` in which K does not occur (now verifiable at the definition's SOURCE bytes,
§1.3) ✓; (3) OPUS5's own scope statement (:21-23) confirms it had no definitional authority
for K* in scope — the blind-spot confirmation mechanism is as the build says ✓; (4) the
consequence for the FABLE artifact's F1 foot is as stated ✓. AUDIT SHARPENING, from the
source bytes the build lacked: the defining gate consumes the `D_BR(K; X_K)` spectral
counting (v002 :61-87) and metric cell data `g_K` — and "the complete normalized D_BR
spectrum" is one of the census's THREE beta-SENSITIVE junction classes (UNBUILT; hunt
:116-118, surface §III.12). So F1's beta-blind typing of the cell foot is wrong twice over:
its cited ground contains no K*, and the definitional route it bypassed runs through the
census's own conversion-carrying junction classes. The build's OPEN ruling on
beta-consumption (§3.2) is the correct quantifier; F1's shortcut is not.

### 2.4 The gap statement (§2.5) — confirmed and double-anchored

The build's named tie-creating derivation (appointed executable `C_record(K)` + a booked
theorem landing its onset content on the FS budget, deriving saturation if used) is exactly
right, and now carries a second anchor the build could not cite: the definition's own source
sentence — "The precise map from the spectrum to durable-record closure must be derived from
the record theory. The phrases 'first record' and 'allow/require' are not numerical equations
by themselves" (v002, durable-record selector section). The demand-not-derivation status of
the tie is written INTO the defining condition's home file.

**TRACE_VERDICT: NOT-TIED — CONFIRMED**, on a corrected and wider quantifier than the build
claimed, with the absence now attested at the definition's source, in the full twelve-carrier
sweep, in the census's sixteen spans, and in the origin tree.

---

## 3. QUESTION (2) — THE TYPING, adjudicated: **CONFIRMED** (TYPED-MAGNITUDE; APPARATUS-ONLY as STATUS; vacancy ENGAGED)

### 3.1 The audit's independent derivation (fixed before the build was read)

From the defining condition's ingredients at their source bytes (v002): K is the coefficient
of `(K/4) integral_Omega sqrt(|g|) F_munu F^munu d^4x` — a metric-weighted, amplitude-squared
(magnitude) functional; the stationary cell data is `X_K = [Omega_K, g_K, Delta tau_K, A_K,
Psi_K]` — carrying a metric as CELL DATA; the opening gate is the `D_BR(K; X_K)` spectrum and
its counting function; `C_record` is the closure residual of that joint problem (no
executable formula appointed — CODEX2 link 9 ABSENT). Typed against the surface of record:
(i) the metric ingredient — the surface has NO native metric argument (§I.1) and the
internal/external conversion is a RAIL import, struck as a surface object (§III.11; §I.4's
principle: a radius — a metric datum — is not a surface datum); (ii) the magnitude ingredient
— the surface's write algebra has no magnitude coordinate (§II.6; I2 at audited grade: CAS
B2.9, B3.1 — "A strength has nothing on-record to act on: it either acts trivially or exits
the record algebra"; vacancy "structural, not informational", refinement-stable); (iii) the
spectral ingredient — the D_BR spectrum is UNBUILT and census-typed beta-sensitive-if-built
(§III.12). No ingredient is a §I-§II clause or displayed lock; no booked derivation lands any
ingredient on booked surface content. Independent verdict: K* is horn (b) — a magnitude-typed
induced-response apparatus quantity; the I2 vacancy engages for any magnitude-side landing;
APPARATUS-ONLY as of today, a STATUS under the CALIBRATION clause, primary exit = a booked
phase/action landing, which is identically the question-(1) theorem. Convergent with the
build in every respect.

### 3.2 The build's route, checked

The build reached the same verdict WITHOUT the source file, from the display carriers and the
sealed typing stock ("stiffness is a magnitude" two-stiffness :52; impedance `Z_F = 1/kappa_F`
decision :23-25; `K e_HL^2 = 1` F0 chain :303-308; "not as write strength … induced connection
response" pair test :125-134; apparatus data classes CODEX2 AS4). Every cite verified (§1.4).
The build's honest scoping of "magnitude-valued" (response-strength-typed, NOT
carries-units) matches the I2 result's sense exactly. Its refusal to occurrence-type
beta-consumption in a non-existent formula (OPEN, named) is the correct quantifier where
FABLE F1 overreached — and is now sharpened by §2.3's two visible conversion carriers. Its
three-exit CALIBRATION framing, its non-indication of exit (2) (K* is the decision's
alpha-stiffness of record; standing adjudicated, not worth), and its engagement parallel to
the relevance map's item 27 are all faithful to the instruments.

### 3.3 The pushback fork — held open here too

Both horns re-stated and both survive audit: (a) MACHINERY-FIT — structural and strong (I2
audited + four-verifier census + the source functional's own metric/magnitude ingredients);
(b) SURFACE-DESCRIPTION-FIT — live and unbooked (the surface displays the onset bound
name-only; saturation adopted; the booked absence records what has been derived; the corpus's
own U-11 names the onset bound K*'s "plausible eventual consumer"). Deciders as the build
states: the appointed executable `C_record(K)` plus the §2.5/§2.4 theorem — landing = horn
(b) and a partial surface trace; provable apparatus-only onset content = horn (a). This audit
decides neither; collapsing either way would violate the fork clause.

**TYPING_VERDICT: CONFIRMED** — TYPED-MAGNITUDE at the definition's source bytes;
APPARATUS-ONLY as of today, as STATUS not verdict; vacancy ENGAGED for magnitude-side
landings; exits and fork exactly as the build states.

---

## 4. QUESTION (3) — THE REFERENT, adjudicated: **CONFIRMED**, and extended in the reverse direction

### 4.1 Identity at §IV — re-verified, now closed to source

The build's byte-chain (surface §IV.13's every load-bearing phrase = the decision's own
bytes: "(principal act …)", `K_* -> kappa_Thomson` :15-16, "transport a cell quantity to a
continuum coupling" :46-47, "the relocated hard problem" :54-55, "the onset root" :11) —
verified phrase by phrase. The audit closes the chain one link deeper than the build could:
surface §IV `K_*` → decision `K_*` → v002 :95-97/:104 AT SOURCE, digest-pinned by the sealed
2026-07-20 manifest. IDENTITY — now anchored end to end, with the build's C-3 display-carrier
premise verified byte-faithful against the source it substituted for.

### 4.2 Homonym at §II.9 — confirmed

The "onset ROOT" (root of `C_record` in the K-indexed stiffness family; microscopic-action
side; magnitude-typed; underived) and the "onset BOUND" (`J_FS,rel >= pi hbar/2`; FS
relative-action budget; internal phase/action geometry; derived bound, adopted saturation,
beta-invariant lock) are DIFFERENT OBJECTS sharing one token. Lineages disjoint (§2); K does
not occur in the bound; J_FS does not occur in the root's defining stock nor in v002/v003/
v004. The sole merging text is FABLE F1 (echoed OPUS5), adjudicated a hole at §2.3.

### 4.3 The reverse homonym sweep (audit addition — "homonym risk both ways")

Could §IV's `K_*` be some OTHER K-object? The K-family swept: **K_R** (v001's direct-division
rearrangement `K_R = 4(Phi_* - Gamma_rest,*)/I_F,*` — RETIRED by v002's own "Correction to
v001" section, which replaces it with the joint saddle-and-closure problem; also the retired
standalone `K_R` threshold selector, two-stiffness :52-53) — cannot be §IV's referent: §IV
cites the principal act, which cites v002, which supersedes v001; **k_R** (the branch floor
in the Gamma_K conditional branch, CODEX2 D03) — a floor datum, not a root; **K_KK** (the
rail junction coefficient, changes by beta^2, ADOPTED ansatz — surface §III.12's first
junction, explicitly NOT derived and not an onset object); **K_n** (a wall-side object of the
recent phase, unrelated grammar); **K_square** (a testbed instance, guard failure-mode-1
vocabulary); **kappa_record** (intensive Hessian — separately typed, Q-62/D02, "never
related", pair test); **kappa_Thomson** (the TARGET of S16's transport, not its cell foot).
None satisfies §IV's three byte-anchors simultaneously (principal-act citation; `K_* ->
kappa_Thomson` direction; "the onset root" naming). §IV's referent is unique and is the
decision's K*.

**REFERENT_VERDICT: CONFIRMED** — IDENTITY at §IV (now source-anchored), HOMONYM at §II.9,
and no reverse-direction claimant.

---

## 5. FLAG BLOCK

```text
TYPING_VERDICT = CONFIRMED — the build's TYPED-MAGNITUDE / APPARATUS-ONLY-as-STATUS verdict is
    re-derived independently from the defining condition's ingredients AT SOURCE (v002, located
    outside the build's boundary, digest-pinned by its own sealed manifest): K is the
    coefficient of a metric-weighted amplitude-squared functional ((K/4) int sqrt|g| F^2) on
    cell data carrying g_K, gated by the D_BR(K;X_K) spectrum — metric ingredient = rail
    (SURF SI.1/SIII.11), magnitude ingredient = the booked-absent SII.6 slot (I2 at audited
    grade: no magnitude coordinate; strength acts trivially or exits), spectral ingredient =
    census-flagged UNBUILT beta-sensitive class (SIII.12) — no ingredient surface-native, no
    booked trace, residual unappointed (CODEX2 link 9 ABSENT, F-RP1 FIRED), physical selection
    TYPE-U, assembly formula ADOPTED (Q-140 N11); vacancy ENGAGED for any magnitude-side
    landing; STATUS per the CALIBRATION clause with exit (1) = the question-1 theorem, exit
    (3) = the held 5fa46838 construction (principal-gated); beta-consumption as-defined OPEN
    confirmed (and sharpened: two census conversion carriers are visible in the defining data).
TRACE_VERDICT = CONFIRMED (NOT-TIED) — no booked derivation ties K* to the SII.9 onset-bound
    lock (J_FS,rel >= pi hbar/2) anywhere reachable: re-verified in the build's evidence, in
    the audit's corrected TWELVE-carrier J_FS sweep (build's SII.2 said four — correction C2;
    zero K_*/K_star/C_record tokens in every carrier), in CODEX2's sixteen fixing spans (no
    onset-bound span), in the lock's derivation carrier (which disclaims durability/response),
    and — beyond the build's boundary — at the definition's source: v002 itself rules the
    spectrum-to-durable-closure map "must be derived from the record theory", and v002/v003/
    v004 carry zero J_FS tokens; the sole contrary texts (FABLE F1 :124-125, OPUS5 :93) are
    re-confirmed as an unsupported homonym-binding whose cited spans contain zero K_*
    occurrences and whose checker had no definitional authority in scope; the tie-creating
    theorem is exactly as the build's S2.5 states, now double-anchored.
REFERENT_VERDICT = CONFIRMED (IDENTITY + HOMONYM, extended) — SURF SIV.13's "K_*" is the
    principal decision's K* by phrase-level byte-identity, and the audit closes the chain to
    the source bytes (SIV -> decision -> v002 :95-97/:104 verbatim, manifest-pinned); SII.9's
    "onset bound" is a DIFFERENT object wearing the same token (disjoint lineages, zero
    co-occurrence, no booked relation); reverse sweep: no other K-family object (K_R retired
    by v002's own correction section; k_R floor; K_KK rail; K_n; K_square; kappa_record;
    kappa_Thomson) satisfies SIV's three byte-anchors — the referent is unique.
VALUE_FENCE = CLEAN BOTH SIDES — the build: every numeral is a sealed-text quotation, no
    numeric evaluation, no closed form, no alpha_micro evaluation, no root/response/scale/
    coupling work (scanned line-by-line; confirmed); this audit: digests, byte-span hashes,
    occurrence counts, and line numbers only; no CAS, no symbolic identity, no physical
    number computed or compared on either side; pushback-fork discipline kept by the build
    (S3.4) and by this audit (S3.3) — both horns stated, neither collapsed.
NET_VERDICT = CONFIRMED-WITH-CORRECTIONS — all three verdicts and the SPLIT structure stand;
    corrections, all favorable: C1 the onset gate V003 sidecar EXISTS (old naming convention,
    cc113a16 MATCH, sealed 2026-07-23) — the build's S2.3 corroboration upgrades from flagged
    to sealed custody and its C-6 premise is half-dissolved (OPUS5's missing sidecar remains
    true); C2 the S2.2 "every J_FS-carrying non-fenced file" quantifier was FOUR of TWELVE
    (guard failure-mode-1 slippage, internally inconsistent with the build's own S2.1
    inventory cite) — corrected scan strengthens NOT-TIED; C3 the v002 principle file is
    LOCATED outside the build's boundary, digest-pinned (a3c7349b...), defining bytes verified
    verbatim — the build's C-3 premise upgrades from PREMISE(named) to VERIFIED-AT-SOURCE and
    its "absent from the reachable corpus" narrows to "absent from the archive roots"; plus
    one sharpening: F1's beta-blind shortcut fails twice over (no K* in its cited ground; the
    bypassed definitional route runs through census conversion carriers). No finding of the
    audit weakens any build conclusion; several strengthen them.
PROVENANCE = VERIFIED — subject digest = tasked digest at path (2b12dc40...); all five tasked
    authorities at their tasked prefixes; all ten S1.3 stock digests recomputed, nine sidecar
    MATCH, OPUS5 no-sidecar confirmed, V003 sidecar found (C1); CODEX2's D07/D13 span SHAs
    recomputed byte-exact; v002 + manifest verified at the origin tree; every load-bearing
    build line-cite spot-verified with zero misquotes (S1.4).
INJECTION = CLEAN — the subject and all consumed authorities scanned for embedded directives,
    imperative steering, hidden comments, and escape sequences: none found; non-ASCII content
    is typographic only; the register-titled copy in the origin tree was not opened; one
    corpus-wide grep printed two register lines to this audit's terminal before the exclusion
    filter was tightened — disclosed at Ledger A-5, content duplicative of the sealed I2
    artifact, nothing register-only is load-bearing anywhere in this audit.
FENCES = alpha_computed = false; proof_authorized = false; kappa_record_computed = false —
    live at open and close on both sides; no fence approached, no premise entered, no
    register/road/ledger/lens/plan/tracker opened, a32_holdout not entered, no file modified
    anywhere except this audit artifact and its seal.
AUDIT_CHOICE_LEDGER =
    A-1 boundary step-out — PREMISE(named): the Documents origin tree was opened READ-ONLY,
        deliberately outside the build's C-1 boundary, per the audit tasking; only the
        selection-principle files, their provenance manifests, and a J_FS filename/token
        sweep were consumed there; the register copy there was not opened.
    A-2 THE_HANDOFF_2026-08-14_V002.md read in full as the tasking instrument of record (it
        names this pair's questions; it is not in the fenced class) — PREMISE(named); the
        build excluded it as plan-adjacent (stricter practice, its C-2); only the task
        definition and agent-discipline clauses were consumed; no queue/state content is
        load-bearing here.
    A-3 independence ordering — IMMATERIAL(kept by protocol): the question-2 derivation and
        the question-1/-3 answers were fixed in working notes before the subject file was
        opened; the subject was read after, once, in full.
    A-4 SII.9 "onset bound" = the hunt's "onset inequality" = "onset budget J_FS,rel >= pi
        hbar/2" — IMMATERIAL(derived independently, convergent with build C-4): the surface's
        SII.8-9 cites the hunt as source; the hunt's fourth derived junction is its displayed
        budget; the bridge gate, inventory :174-175, and I2 Chain D carry the same single
        object.
    A-5 fenced-class handling — PREMISE(named): one early corpus-wide grep matched two lines
        inside the supervision register before the exclusion filter was added; the file was
        never opened; the matched content duplicates the sealed I2 artifact consumed at path;
        all subsequent sweeps excluded register/road/ledger/lens/plan/tracker by filename.
    A-6 custody grading — PREMISE(named): OPUS5 (9c9e120e) remains sidecar-less and is
        consumed only where the build consumed it (flagged, corroborative); the V003 gate is
        RE-GRADED sealed custody per C1.
    TOY_SEPARATION = clean — build side: re-derived clean after C1-C3 (no load-bearing step
        rests on an unproven convenience; the one quantifier slippage, C2, was load-bearing
        for a bullet's wording, not for the verdict, and is corrected here with the
        conclusion strengthened); audit side: every step is byte-reading at recomputed
        digests; the one convenience (filename-based fenced-class filter, A-5) is named with
        its margin.
```

---

Fences at close: alpha_computed = false; proof_authorized = false; kappa_record_computed =
false. No value of K*, kappa_record, kappa_Thomson, beta, J_FS, or alpha was computed,
bounded, estimated, or compared, on either side; every numeral in this audit is a sealed-text
quotation, a digest, an occurrence count, or a line number. One output file; seal alongside.
KST-AUDIT ends.
