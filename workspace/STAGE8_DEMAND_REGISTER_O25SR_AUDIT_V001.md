# STAGE 8 — AUDIT OF THE DEMAND REGISTER — COMMISSION O25SR — AUDIT V001

## REGISTER-AUDIT, 2026-08-15 — DETERMINATION ONLY, DEFAULT-REFUTE

```text
THIS ARTIFACT PROPOSES NOTHING, AUTHORS NOTHING, ADOPTS NOTHING, RECLASSIFIES
NOTHING, AND ATTEMPTS TO SATISFY NO DEMAND.  It re-derives O25SR's three
faithfulness determinations at bytes, at zero weight to O25SR's testimony.  No
gate moves.  No witness is created or retired.  No failure point is discharged.
No verdict of any consumed artifact is altered.  FENCES LIVE THROUGHOUT:
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
No value, no number as a value, no measured constant, no unit, no scale, no
coupling.  Every symbol below is an EXPONENT, an ORDER OF GROWTH, a counting
index, a byte offset, or a rational coefficient.  No git; nothing registered,
committed, or pushed; no existing file edited.
```

**OUTPUT-PATH PROBE:** `STAGE8_DEMAND_REGISTER_O25SR_AUDIT_V001.md` and its
`.seal.sha256` sidecar were probed **ABSENT** (`ls` exit 1) before any write.

**TARGET SEAL, verified from the artifact's own directory before any reliance:**

```text
cd /Users/bgm/MB Work/alpha-program-archive/workspace
shasum -a 256 -c STAGE8_DEMAND_REGISTER_O25SR_V001.md.seal.sha256
  -> STAGE8_DEMAND_REGISTER_O25SR_V001.md: OK
  257beed098b721289a9e6195cb41ad461c9ba81e991ea5ee6f411b3e2f7872c5
```

---

## §0 — VERDICT IN ONE LINE

> **CONFIRMED-WITH-CORRECTIONS.** All three faithfulness determinations survive
> re-derivation at bytes: **DEMAND 2 FAITHFUL** (both directions independently
> re-derived; the byte pointer `[45349,45957)` re-extracted and re-checked here),
> **DEMAND 1 STRENGTHENED** and **DEMAND 3 STRENGTHENED** (both origins located
> at their earliest sealed statement by an independent chain walk, and in each
> case an object satisfying the original while failing the demand is exhibited —
> hunt (A) is answered, not evaded). **DEMAND 1's reverse (WEAKENED) finding also
> survives.** **No kill was found: neither strengthening rests on a difference in
> vocabulary.** Five corrections are entered, none of which changes a verdict:
> **C-1** §5.1(b) books a register-ROBUST sub-verdict as register-SENSITIVE, and
> O10SR's own bytes refute it in the little-o register O25SR itself installs —
> this correction RESTORES an impossibility sub-verdict O25SR had loosened;
> **C-2** §5.1's clause that the record's "uniform `o(.)`" *would be established*
> is underived, and the hedge O25SR elided from inside CH-b is the exact sentence
> that blocks it; **C-3** Demand 2's `ANA ==> REC` is derived from a byte pointer
> that the frozen spec's own row table shows cannot do that work — the verdict is
> re-grounded on O2SR's W-2a and kept; **C-4** Demand 3's `REC` as written
> includes the one conjunct (o4) the record books as NOT DISCHARGED, and must be
> scoped to (o1)-(o3) or the finding reverses; **C-5** three quotation
> truncations, none reversing. No lens token, no value, no proposal, no
> reclassification, no satisfaction attempt was found.

---

## §1 — GROUND RE-VERIFIED INDEPENDENTLY; SWEEP CUTOFF

`shasum -a 256 -c` was re-run **from inside the artifacts' own directory, before
any reliance**, on the target and on every artifact O25SR claims to consume.
Testimony was given zero weight: each seal was checked here, not accepted.

```text
TARGET                                            OK  (257beed0…)
--- O25SR §1.1, the commissioned ground (12) -----------------------------
STAGE8_COMPLETION_MAP_T17SR_V001.md               OK
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md         OK
STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md             OK
STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md       OK
STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md           OK
STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md     OK
STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                OK
STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md          OK
STAGE8_FORCING_NOTION_O12SR_V001.md               OK
STAGE8_FORCING_NOTION_O12SR_AUDIT_V001.md         OK
STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md           OK
STAGE8_OUTSIDE_FORM_CLASS_O10SR_AUDIT_V001.md     OK
--- O25SR §1.2, the citation chain (8) -----------------------------------
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md         OK
STAGE8_R3_JOINT_RATE_S9AD_V001.md                 OK
STAGE8_MO3_P_EXPONENT_S9AD_V001.md                OK
STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md  OK
STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md        OK
STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md OK
STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md       OK
STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md             OK
--- citation closure (2) -------------------------------------------------
STAGE8_COUNTING_BOUND_O24SR_V001.md               OK
STAGE8_COUNTING_BOUND_O24SR_CHECK_V001.md         OK
TOTAL: 23 files (target + 22), 23 OK, ZERO mismatches.
```

**DIGEST CLAIMS RE-COMPUTED HERE, not taken on testimony.** O25SR identifies
three artifacts by digest prefix. Each was recomputed:

```text
STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md         -> fdf20bd4   MATCHES
STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md  -> 9f0d12b4   MATCHES
STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md          -> 80db260f   MATCHES
```

**ON O25SR's SEAL COUNT.** O25SR reports "21 files, 21 OK". Its own §1.1 (12) +
§1.2 (8) + the one closure artifact it says is seal-verified (O24SR_V001) = 21;
the count is internally consistent. The O24SR *check* artifact, which O25SR
quotes without a seal claim, also carries a sidecar and also verifies OK here.
**NO DEFECT.**

### §1.1 SWEEP CUTOFF — DECLARED

```text
SWEEP CUTOFF:  2026-08-15T22:41:00Z
CORPUS ROOTS:  /Users/bgm/MB Work/alpha-program-archive/workspace  (primary)
               /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
                 alpha_fundamental_record_action_cleanroom_v003
SWEEPS RUN (declared, scoped, key sets displayed):
  A-1  "n^{3-delta}" / "3 - delta" / "3-delta", corpus-wide, --include=*.md
         -> 12 files, ALL of them O-series commission artifacts or O25SR itself;
            ZERO record-native (S9AD / spec / frozen / obstruction) hits.
  A-2  "sub-volume" | "carrier volume" | "exponent" | "power", scoped to r-3
  A-3  "MO-3" chain: WBC F1 -> r-3 §5 -> R-L2b target -> MO3_P_EXPONENT
  A-4  "controlled uniformly", corpus-wide -> 9 hits, each read at its span
  A-5  "C-L2" / "S2b" / "R.3" / "(o1)" / "(o4)" scoped to E1 and O3SR
  A-6  frozen-spec row table R1..R9 read whole (the decisive span for §4)
NEVER OPENED (standing bar honoured): any file matching
  register | tracker | road_ | road | plan | continuation | ledger | lens |
  THE_HANDOFF.  (The word "register" in this artifact's own title denotes the
  GRAMMATICAL REGISTER of a demand, O25SR's subject matter, not a register file.)
"Q-..." TOKENS: EXPECTED-UNLOCATABLE.  Q-1079 appears in O25SR §1.3/§11 as a
  noted-not-chased token only; not chased here either.
Every exhaustiveness sentence below is relative to this cutoff and to its own
displayed key set.  Nothing absolute is claimed.
```

---

## §2 — HUNT (A), FIRST TARGET: DEMAND 1's STRENGTHENING — **SURVIVES**

The commission's hardest hunt: a strengthening call is a kill if the origin is
not quoted at its earliest sealed statement, or if no object is displayed that
satisfies the original and fails the demand, or if the whole difference is
vocabulary. All three tests were run against O25SR's Demand 1 finding.

### §2.1 The origin, re-located by an independent chain walk

The chain was walked here without reference to O25SR's hop table, then compared.

```text
WBC 80db260f, F1 (re-read at :402-407, verbatim):
  "F1 THE CONVERSION STEP — rank x op is the only sealed conversion from
   the delivered operator grade to trace/HS grade; it carries the full
   carrier volume 4n^3 against an n-free target, and the r-3 pair
   PROVED the same-power arithmetic cannot close (WB6 = D10 re-pinned;
   the tasked display made: THE SAME CEILING BINDS).  Escape requires a
   sub-volume trace/HS rate: verbatim MO-3, open of record."
r-3 §5, MO-3 (re-read at :480-484, verbatim):
  "MO-3 a certified n-uniform trace-norm RATE for Delta_n(a) strictly below
        the carrier volume (the uniform CONSTANT is refuted of record;
        only a rate remains possible). This is the R-L2b sharpened-target
        shape (coincidence-vanishing exponent p of the difference kernel,
        OPEN of record). Unblocks Route 1 together with MO-1 + MO-2."
```

Both are **verbatim in O25SR, character for character.** The word "verbatim" in
the WBC's own F1 sentence pins MO-3 as F1's content, so the chain terminates at
r-3 §5 and not before. **ORIGIN LOCATED, NOT INVENTED — hunt (B) clears at
Demand 1.**

### §2.2 The register of the origin, tested against the alternative reading

O25SR's finding turns entirely on reading "strictly below the carrier volume" as
LITTLE-O rather than as a POWER SAVING. **That reading was attacked here, not
accepted.** The decisive test is corpus-wide and is reported with its key set:

```text
SWEEP A-1: "n^{3-delta}" / "3 - delta" / "3-delta", corpus-wide, *.md
  HITS (12): O5SR_V001, O5SR_AUDIT_V001, O8SR_V001, O8SR_AUDIT_V001,
             O10SR_V001, O11SR_V001, O11SR_AUDIT_V001, O13SR_V001,
             O22SR_V001, O24SR_V001, O24SR_CHECK_V001, and O25SR itself.
  RECORD-NATIVE HITS (WBC / r-3 / E1 / frozen spec / obstruction / R-L2b /
             MO3_P_EXPONENT / FHB):  *** ZERO ***
```

The power form is **absent from every record-native artifact in the chain** and
present only in the O-series commissions that restate the demand. Independently,
r-3's own three register sentences were re-read at their spans and are verbatim
in O25SR: `:350-361` ("never `o(1)`"), `:489` ("the uniform `o(.)` is not
established"), CH-b (":723-727", "THE REDUCTION TARGET `o(kappa_n)`"). Two
further record-native sentences, located here and NOT cited by O25SR, point the
same way and are entered as independent corroboration:

```text
r-3 :70 (verbatim): "the ratio is n-free and never o(1) (CAS D4/D10), so ANY
     product-of-norms discharge REQUIRES a sub-volume trace-norm rate"
WBC :313-317 (verbatim): "leaves an n-free ratio that is never o(1) — the
     carrier power cancels only against another n^3, NEVER down to n^0"
```

**The origin's register is LITTLE-O at five independent record-native sentences
and POWER at none. O25SR's CH-1 is FORCED, as it claims.**

### §2.3 The alternative bridge, closed — and a quotation defect on the way

MO-3's own sentence names the R-L2b shape, which IS a genuine exponent
condition. That is the one route by which a power condition could have entered
the origin. It was chased to its own artifact:

```text
R-L2b target, at its own bytes (:114-115, verbatim):
  "*** DOES THE DIFFERENCE KERNEL OF V_(mu lambda)(a) - V_(mu lambda)(0)
   VANISH AT COINCIDENCE LIKE |r|^p WITH p > 3/2 ? ***"
```

`p` is an exponent in the **separation variable `r`**, not a growth exponent in
the **counting parameter `n`**; and sweep A-2 finds ZERO occurrences of
"sub-volume", "carrier volume", "trace-norm rate" or "n^3" anywhere in the R-L2b
artifact. The bridge is then closed by the record itself, at MO-3's own artifact
(`STAGE8_MO3_P_EXPONENT_S9AD_V001.md` :391-397). **O25SR's handling is correct
in substance. Its quotation of that closure is defective in form** — see C-5(a).

### §2.4 The gap, re-derived — hunt (A)'s actual requirement

O25SR does not assert the strengthening from vocabulary. It displays an object.
That object was re-derived here in a fresh venv, independently of O25SR's script:

```text
        f(n) = n^3 / log n
        f/n^3            -> 0     : SATISFIES the origin (strictly below the
                                    carrier volume; ratio to carrier -> 0)  [Z1]
        f/n^{3-delta}    -> +oo   : FAILS the demand at EVERY delta > 0      [Z2]
```

**A GAP IS EXHIBITED, NOT ASSERTED. HUNT (A) CLEARS AT DEMAND 1.**
**DEMAND 1 = STRENGTHENED: CONFIRMED.**

---

## §3 — HUNT (A), SECOND TARGET: DEMAND 3's STRENGTHENING — **SURVIVES, WITH ONE CORRECTION TO ITS STATEMENT OF `REC`**

### §3.1 The three E1 clauses, re-read at their spans

All three are **verbatim in O25SR**, checked character for character against
`STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md`:

```text
C-L2 :1137-1144  "Control [h_0, M(t) (x) S] in a QUADRATIC-FORM / Besov-type
      norm. Norm-based control is unavailable (||[h_0, 1_B]|| = +infinity for
      first-order h_0 against a sharp indicator), so the certification must be
      in a quadratic form and the artifact must state WHICH form and WHY the
      operator-norm route is excluded.  Supplies S2b."
S2b  :702-708    "G_cm(C, eps) := |C|_4^{-beta} . (certified bound on the C-L2
      commutator error [h_0, M(t) (x) S] in the quadratic form C-L2 names)"
R.3  :810-813    "||A_s||_2 <= ||A(0)||_2 + ||Delta||_2 <= |C|_4^{beta} G_cm +
      |C|_4^{alpha} G_hs ... (where `A(0)` is the C-L2 error, NOT a baseline
      norm)"
```

**THE SEAM IS REAL AND IT IS INSIDE THE RECORD.** S2b *defines* `G_cm` out of a
bound "in the quadratic form C-L2 names" — form grade. R.3 *consumes* `G_cm` on
the right of an inequality whose left side is `||A(0)||_2` — Hilbert-Schmidt
grade. The demand takes R.3's side of that seam while naming C-L2. O25SR's
location of the seam is **CONFIRMED at bytes**.

### §3.2 The record itself carries both readings — decisive corroboration O25SR did not cite

The strongest available check on O25SR's reading is that the demand's own site
artifact already booked the same ambiguity, explicitly, and attempted both sides:

```text
O3SR :276-288, verbatim:
  "Two readings of S2b are carried, exactly as CERT §2.3 carried them, and
   BOTH are attempted:
   READING A (S2b's own parenthetical, FORM-typed): G_cm = |C|_4^{-beta} . B,
      B the certified form bound. ...
   READING B (R.3's display, HS-typed): G_cm must already be an HS-grade
      quantity. Then the form asset must be CONVERTED before entry."
```

**The two-grade seam is the record's own, named in the record's own words, before
this commission or O25SR existed. The strengthening is not an artefact of
O25SR's vocabulary.** This citation is entered here because O25SR's §4.3 rests
its case on the sea-sandwich display alone and never quotes the record admitting
the ambiguity directly; the finding is stronger than O25SR made it.

### §3.3 The gap, re-derived

O25SR again displays rather than asserts, and the display was re-derived here:

```text
   the certified asset SATISFIES the origin: "(o1), (o2), (o3) CERTIFIED; the
     form bound exact and t-, cell- and carrier-uniform at the primitive step,
     with relative form bound ZERO w.r.t. h_0^2; the operator exclusion
     mechanism-explicit."                       (O3SR :201-203, verbatim)
   the same asset FAILS the demand: "SITE (i) R.3 = FAILS-AT(compression type)
     ... preserves the certified form bound VERBATIM ... and provably creates
     NO HS decay ... C h_0 C = -|k| C(k) is a nonzero Fourier multiplier —
     translation-invariant, non-compact, NOT Hilbert-Schmidt"   (FHB :33-40)
   re-derived exactly here: the form bound is V-FREE while the HS mass is
     V . ||g||^2 -> +oo, so FORM does not entail HS.                     [Z3]
```

**A GAP IS EXHIBITED ON THE RECORD'S OWN OBJECTS. HUNT (A) CLEARS AT DEMAND 3.**
**DEMAND 3 = STRENGTHENED: CONFIRMED.**

### §3.4 **CORRECTION C-4 — `REC` as O25SR writes it is not the `REC` its derivation uses**

O25SR's §4.3 writes the record-native side as:

> `REC` ... control ... in a QUADRATIC-FORM / Besov-type norm; state which form;
> display why the operator-norm route is excluded; **supply S2b**, whose
> definition is a certified bound "in the quadratic form C-L2 names".

Four conjuncts. C-L2 has exactly four obligations, and the record numbers them:

```text
O3SR :190-191, verbatim: "=> four obligations: (o1) name the form; (o2) control
  in it, exactly; (o3) display the exclusion; (o4) SUPPLY S2b."
```

So O25SR's `REC` = (o1)+(o2)+(o3)+(o4). But three lines later O25SR writes
*"The `REC` side is MET, of record"* and supports it by quoting **only**
"(o1), (o2), (o3) CERTIFIED". The record's next sentence, at the same bytes,
is the one O25SR does not carry into that claim:

```text
O3SR :203-205, verbatim: "(o4) **NOT DISCHARGED**, FAILS-AT three named
  of-record points. G_cm = NOT-DERIVABLE-TODAY at its consuming type."
```

**`REC` as written is NOT met of record.** The conjunct (o4) is exactly the one
the record books as undischarged. Worse, under the record's own reading (o4)
reaches the consuming type — O3SR :681-683: *"Obligation (o4) — 'Supplies S2b' —
is now shown to be NOT A CONSEQUENCE of (o1)-(o3) AT ANY STRENGTH."* If (o4)
were kept inside `REC`, then `REC` would already contain the HS-typed demand and
`REC ==> ANA` would hold — **the strengthening would collapse to FAITHFUL.**

**THE CORRECTION.** `REC` at Demand 3 must be scoped to **(o1)-(o3), plus S2b
under READING A (form-typed)** — which is the `REC` O25SR's derivation actually
uses at every step, and the one under which its two quoted facts ("certified"
/ "FAILS-AT") are both true. Scoped that way the finding runs exactly as O25SR
states it and the verdict STRENGTHENED stands. **This is a defect in the
statement of `REC`, not in the finding.** It is entered because the unscoped
`REC` is the one reading under which O25SR's own verdict would reverse, and a
determination must not leave its decisive term ambiguous.

---

## §4 — HUNT (C) AND (D) AT DEMAND 2: THE VERDICT **SURVIVES**, THE DERIVATION OF ONE DIRECTION **DOES NOT**

Demand 2 is the one demand O25SR calls FAITHFUL. Hunt (C) asks whether
equivalence was claimed with only one implication derived; hunt (D) asks for a
weakening the lane had no incentive to find. Both were run hard here, because a
false FAITHFUL is the cheapest error available at this site.

### §4.1 The origin, and the byte pointer — RE-EXTRACTED, NOT ACCEPTED

The cited range was extracted here from the origin file's own bytes in a fresh
venv, and the seven phrases and two absences re-checked independently:

```text
EXTRACT [45349,45957) of fdf20bd4 (filesize 71066), re-run here:
  "A candidate must supply"                    PRESENT
  "(JD-3)"                                     PRESENT
  "(JD-3a)"                                    PRESENT
  "must prove orientation covariance"          PRESENT
  "presentation independence"                  PRESENT
  "continuity"                                 PRESENT
  "Ctc_N^sym subset ker(Phitilde_N)"           PRESENT     => 7/7
  "member"                                     ABSENT
  "displayed evaluation"                       ABSENT      => both
```

**O25SR's CAS D1/D2 REPRODUCE EXACTLY.** The origin quotation at :1185-1200 is
verbatim. The two "elsewhere" homes are verbatim and were re-read at their spans:
`:685` ("For each **exhibited** rank-preserving arrow `f:N->M`, a member must
supply") and `:1400-1405` (the SURFACE ANCHOR pass criterion). **Hunt (B) clears
at Demand 2: nothing is imported; the seam is internal to one sealed origin.**

### §4.2 **CORRECTION C-3 — the byte pointer does not carry what O25SR makes it carry**

O25SR's `ANA ==> REC` direction rests on exactly one argument:

> "The concern would be that `ANA`'s shorter phrase drops the four proof
> obligations. It does not: `ANA`'s own citation pointer carries them, verified
> byte-exactly."

**That inference is invalid, and the frozen spec's own row table refutes it.**
O25SR quotes row R1 but never displays the rows around it. Read whole
(`STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md` :95-99), the table is:

```text
ROW  CRITERION                        EVIDENCE FORM                    GROUND
R1   The cell rule EXISTS             the sealed rule as bytes, plus   [45349,45957)
                                      one displayed evaluation on a
                                      NAMED oriented k-cell
R2   Orientation covariance           proof object over the named      [45349,45957)
                                      cell and its reverse
R3   Presentation independence        proof object; a displayed pair   [45349,45957)
                                      of presentations with equal value
R4   Contact-kernel annihilation      proof object                     [45349,45957)
R5   Continuity in member-named       named topologies + continuity    [12334,12643)
     topologies                       proof for each arrow
```

**R2, R3 and R4 are SEPARATE, INDEPENDENTLY-TAGGED `PROVABLE` ROWS THAT CITE THE
VERY SAME RANGE AS R1.** The `Ground` column is the *location of the origin
passage*, shared by four distinct criteria. It is not a container that pours one
row's content into another. If it were, R2 would equally carry R1's requirement
and R4 would carry R3's, and the spec's own row structure would be vacuous — a
frozen pre-registration spec does not book four `PROVABLE` rows for one
criterion. **The pointer proves the origin passage is the right passage. It
proves nothing about which of the four criteria row R1 states, and R1 states one
of them: "The cell rule EXISTS."**

**KEPT HONEST.** The argument above rests on R2/R3/R4 alone. R5's continuity is
a DIFFERENT continuity — of `Loc_N^phys`, `iotatilde_N^H`, `iota_N^H`, `Loc_N^C`
— grounded at a disjoint range `[12334,12643)`; it is NOT the origin's
continuity obligation on `Phitilde_N`, which does sit inside R1's range. R5
therefore supports neither side and is not used.

The demand cites R1 and only R1 — the WBC's own words are `"CN-2 (row R1,
WARD_SYMBOL_MAP_JII)"` (:502). So on O25SR's stated `ANA`, and on O25SR's stated
argument for it, the four proof obligations are **not** carried, and the honest
reading of that pair would have been a **WEAKENING**, not a faithfulness.

### §4.3 The verdict is nevertheless CONFIRMED — on ground O25SR quotes but does not use

The obligations enter the demand by a different and sufficient route, and O25SR
has it on its own page, in §2.2, without deploying it in §4.2:

```text
O2SR :143-149, verbatim:
  "W-2, as CN-2 states it, is the conjunction of THREE independently-owed
   things:
   W-2a  THE RULE AS BYTES.  A supplied (JD-3) per-cell rule with its (JD-3a)
         linear extension, with the four obligations PROVED (orientation
         covariance, presentation independence, continuity, contact-kernel) —
         the obstruction's own "must prove" sentence."
```

The demand's **own site artifact** reads W-2 as including the four obligations,
and grounds that in *the obstruction's own "must prove" sentence* — not in a
byte pointer. That is the correct derivation of `ANA ==> REC`, and it holds.
`REC ==> ANA` holds as O25SR derives it (the surface-anchor rule at :1400-1405
supplies the displayed-evaluation conjunct from inside `REC`). **BOTH DIRECTIONS
HOLD. DEMAND 2 = FAITHFUL: CONFIRMED.** Hunt (C) clears — equivalence is not
asserted from one implication — but one of the two implications is derived from
the wrong premise and must be re-grounded as above.

### §4.4 Consequence for O25SR's CHOICE LEDGER CH-3

CH-3 claims the byte extraction "reversed the reading I started with" and that
"a determination that had not run it would have reported MIXED with Demand 2
weakened, wrongly." **Both halves are overstated.** What prevents the weakening
reading is O2SR's W-2a, which O25SR had already quoted in §2.2 before running
the extraction. The extraction is a genuine and valuable check that the cited
range is the right range — it clears hunt (B) — but it is not what decides
faithfulness, and CH-3 credits it with the decision.

### §4.5 Hunt (D) at Demand 2 — no weakening located, on the corrected ground

With `ANA` read as its own site reads it (W-2a/b/c), the demand asks the rule,
the four proofs, the member type, and the displayed evaluation. `REC` asks the
rule, the four proofs, and — via the surface-anchor rule — the displayed
evaluation, with "member" the origin's own word at :685. **Nothing is asked less.
NO WEAKENING LOCATED AT DEMAND 2.** O25SR's "noted, not scored" treatment of the
heavier `member` type is re-examined and left as it stands: `member` is the
origin's own word, so the type is record-native, and O25SR displays the contrary
material rather than omitting it.

---

## §5 — HUNT (D) AT DEMAND 1: THE REVERSE FINDING — **SURVIVES**

O25SR's Finding 4 (Demand 1 is also WEAKER than FP-1's own `n`-free target) is
the finding the lane had least incentive to produce, so it was tested for the
opposite defect: manufacture.

### §5.1 The target is `n`-free at the record's own bytes

```text
WBC WB6 (:735, verbatim, CAS output line): "the conversion budgets are
  |tr| <= 4n^3 c_0 and ||.||_2^2 <= 4n^3 c_0^2 — FULL carrier volume n^3; the
  enclosure target is n-FREE (n^0): lim_n (4n^3 c_0) = +infinity, the gap is
  the whole carrier volume"
WBC F1 prose (:309-317, verbatim): "the carrier power cancels only against
  another n^3, NEVER down to n^0.  Here the target is n^0: the gap is the
  entire carrier volume, one full power WORSE than the r-3 instance, and the
  only escape is a sub-volume trace/HS-norm rate for the dressed composite"
```

Both verbatim in O25SR (one elision, C-5(c)). The arithmetic re-derived here:
`n^{3-delta} -> +oo` for every `delta < 3`; only exponent 0 is `n`-free; and
exponent 0 is the uniform CONSTANT which MO-3's own sentence refuses. **The
entailment from Demand 1 as stated to FP-1's stated target does not hold.**

### §5.2 The finding is correctly localized — the strongest test it passes

The obvious way for this finding to be wrong is for the alleged weakening to be
the WBC's defect rather than the demand's. O25SR forecloses that itself: the
WBC's sentence is a **necessity** ("the only escape"), and read as a necessary
condition Demand 1 is faithful to it. The weakening arises only under the
completion map's verb, which O25SR quotes exactly: `"W-1 (discharges FP-1)"`
(T17SR :324, verified verbatim) and `"what it then delivers is the route's own
tasked object"` (T17SR :358-360, verified verbatim). **The finding attaches to
`discharges`/`delivers`, not to the WBC. That localization is correct.**

### §5.3 One challenge run and cleared

The completion map's display of W-1 also contains `"a TOPOLOGY in which the
dressed composites are controlled uniformly"`. If "controlled uniformly" meant
an `n`-free bound, the demand as displayed would reach `n^0` and Finding 4 would
fail. Sweep A-4 traced the phrase to its origin (WBC :495-498) and to O5SR's
reading of it (:269-271, verbatim): *"This is the exact content of 'a topology in
which the dressed composites are controlled uniformly': the topology must control
`N_n`"* — a condition on the singular-value **counting function**, not an `n`-free
enclosure. **The challenge fails; Finding 4 stands. DEMAND 1 ALSO WEAKENED:
CONFIRMED.**

---

## §6 — THE CORRECTIONS AGAINST O25SR's REACH CLAIMS

Every correction below is against a claim about how far O25SR's finding reaches.
None touches a verdict.

### §6.1 **CORRECTION C-1 — a register-ROBUST sub-verdict is booked as register-SENSITIVE**

This is the correction the commission's own warning is aimed at: a strengthening
finding that, if its reach were accepted, would quietly loosen a standing
impossibility result. O25SR §5.1 books O10SR's FP-1 counting sub-verdict as
REGISTER-SENSITIVE, writing:

> "At degree EXACTLY +3, an exponent comparison is the boundary case and does
> not settle a little-o question. **THAT SUB-VERDICT WAS REACHED AGAINST THE
> STRONGER STATEMENT.**"

**It was not, and the arithmetic is decisive.** The record's counting content is
the sealed rank cap, and it is not a bare degree — it is an exact expression:

```text
O10SR X1e (:894-896, verbatim): "rank_cap = 4*n**3   # WALL :302, of record"
        rank cap / n^{3-delta}  ->  +oo   : fails the POWER register     [X1]
        rank cap / n^3          =   4     : fails the LITTLE-O register  [X2]
```

`4n^3` is not `o(n^3)`: the ratio is a nonzero constant. **The counting content
is on the wrong side in BOTH registers, so the sub-verdict survives the register
correction unchanged.** O10SR's own ground says so in the little-o register
already — its DEGREE paragraph (:381-384, verbatim) reads *"This is the wrong
direction and it is exactly what makes FP-1's rank x op conversion fail (X2f:
the ratio is 2k, n-free, **never o(1)**)"*. O10SR reached the sub-verdict against
the *little-o* statement, in the very register O25SR installs.

**C-1: O25SR §5.1(b)'s classification is REFUTED. "posable-but-unmatched for a
counting certificate" is REGISTER-ROBUST and moves to the register-robust column
beside T-1.** O25SR's Demand 1 verdict is untouched; what shrinks is the set of
standing results its correction reaches.

### §6.2 **CORRECTION C-2 — "the uniform `o(.)` would be established" is underived, and the elided hedge is the one that blocks it**

O25SR §5.1 writes that the record would accept `||A_n|| <= C n^3/log n` and adds:
*"the 'uniform `o(.)`' r-3 books as not-established **would be established**."*
That clause does not follow. r-3's `o(.)` is against `kappa_n`, and r-3's own
sentence (:348, :358) is that `kappa_n -> +infinity` **WITH NO RATE**
and *"kappa_n may sit anywhere below its ceiling."* Re-derived here:

```text
   an admissible slow carrier: kappa_n ~ log log n  (diverges; obeys the
      sealed ceiling, (log log n)/(2n^3) -> 0)                    [Y1, Y2]
   (n^3/log n) / (log log n)   ->  +oo   : NOT o(kappa_n)             [Y3]
   n^{3-1/2}   / (log log n)   ->  +oo   : NOT o(kappa_n) EITHER      [Y4]
```

**The failure is SYMMETRIC across the two registers**, so it does not disturb
Finding 1 — it is a defect in the added clause only, and C-2 is scoped to that
clause. O25SR's CAS B1 label carries the same overstatement ("the record's o(1)
target IS met by A2's witness"): what B1 computes is the ratio to the **ceiling**
`2n^3`, which is the object r-3's D10 *failure* display uses. Defeating a
failure display against the most favourable denominator is not the same as
achieving `o(kappa_n)`.

**And the hedge that would have blocked the clause is the one O25SR elided.**
O25SR quotes CH-b as:

> "CH-b THE REDUCTION TARGET o(kappa_n): FORCED as the consumable SUFFICIENT
> route **...** the verdict's undecidedness is stated against the TARGET, not
> only against o(kappa_n)."

The full sentence (r-3 :723-727) reads, in the elided span: *"(r-2's sealed chain
is the only sealed lower control on the denominator); **its one-way-ness is
DISPLAYED (§4.1, CAS D9) and no conclusion consumes equivalence** —"*. r-3 §4.1
is explicit: *"SUFFICIENT-ONLY, not equivalent"*. **The elided material is
precisely the record's warning that meeting `o(kappa_n)` is a one-way sufficient
route, and that is the sentence O25SR's added clause needed and did not have.**
This is the program's recurring defect — a hedge removed from inside a quoted
sentence — and it is entered as such.

### §6.3 **CORRECTION C-5 — three truncation defects, none reversing**

```text
(a) MO3_P_EXPONENT :391-397.  O25SR closes its quote at "...do not pass through
    coincidence vanishing" with NO trailing ellipsis.  The source continues
    "of the unsandwiched kernel (per-pair cancellation at assembly,
    weight-summed re-quantification onto the record strata — both of record,
    neither touched here)."  The dropped words NARROW the closure to the
    unsandwiched kernel and name two surviving routes.  Direction of error:
    O25SR's use is unaffected (no power condition on n is supplied either way),
    but the quote reads as a wider closure than the source states.
(b) FHB :40.  O25SR closes at "...follows from the certified asset alone",
    dropping ", ever".  Direction of error: the elision WEAKENS the quoted
    source in O25SR's own disfavour.  Immaterial.
(c) WBC F1 prose.  O25SR's second ellipsis elides "one full power WORSE than
    the r-3 instance".  Direction of error: the elided clause SUPPORTS Finding
    4.  Immaterial.
```

### §6.4 OBSERVATION (not scored as a correction) — FHB's three-way split is presented by one site only

O25SR §5.2(b) cites FHB site (i) and labels it site (i) honestly. It does not
note that the same verdict is a three-way split whose site (ii) is `WALL F3 =
PARTIAL`, where *"at the wall's own finite-rank compressions ... the form bound
DOES convert, exactly"* (FHB :43-48). Site (ii) is not the demand's consuming
site — R.3 is — so no finding changes. It is recorded because §5.2's heading
speaks of "THE STANDING IMPOSSIBILITY RESULTS AT THIS SITE", and a reader of
§5.2 alone would not learn that the form→HS conversion succeeds exactly
somewhere at FP-3's own wall.

---

## §7 — FENCE-SCAN, AND HUNTS (E), (F), (G)

### §7.1 FENCE-SCAN of the target — CLEAN

```text
GATES            : "alpha_computed = false ; proof_authorized = false ;
                   kappa_record_computed = false" declared at :11 and re-declared
                   at :1269-1270 as ALL UNMOVED.  No gate token anywhere else in
                   the file.  NO GATE MOVES.                          CLEAN
VALUES           : no measured constant; no unit; no scale; no coupling; no
                   alpha value; no kappa_record value.  Every numeral scanned:
                   section numbers, exponents (3, 3-delta, 0, 7/2, 5),
                   counting indices, byte offsets, and rational/surd
                   coefficients inside quoted record displays (4, 8, 16, 2,
                   sqrt(3), 2 sqrt(3), 8 pi R^5/5).  NO NUMBER AS A VALUE.
                   No measured-constant comparison of any kind.          CLEAN
LENS TOKENS      : exactly two occurrences of the string "lens" (:105, :1280),
                   BOTH inside the never-opened / barred-files declarations,
                   i.e. naming the bar rather than using a token.        CLEAN
BARRED FILES     : the strings register/tracker/road/plan/continuation/ledger/
                   THE_HANDOFF occur only inside those same two declarations.
                   No barred file is quoted, cited by content, or relied on
                   anywhere in the artifact.                             CLEAN
"Q-..." TOKENS   : Q-1079 appears twice (:106, :1248), both times as
                   EXPECTED-UNLOCATABLE, noted-not-chased, nothing resting on
                   it.  Correct handling.                                CLEAN
```

### §7.2 HUNT (E) — satisfaction, proposal, reclassification: NONE FOUND

Scanned for satisfaction/proposal/adoption/reclassification verbs. Every hit is
inside a NEGATIVE declaration (:6, :1220, :1259). Substantively:

```text
- §5 displays objects that WOULD satisfy the weaker statements.  This is the
  separating-witness display hunt (A) REQUIRES, and it is fenced twice in its
  own words: "Nothing below asserts, suggests, or hints that the weaker
  statement can be met" (:605-607) and "Nothing in §5 says the weaker statement
  can be met" (:704).  NOT an attempt to satisfy.               LAWFUL
- No demand is restated, re-typed, re-scoped or re-worded as a proposal.
- No verdict of any consumed artifact is altered; O25SR repeatedly declines to
  answer adjacent questions it could have reached for (the completion set's
  independence, whether FP-1 is dischargeable, the joint-closure question).
- No witness created or retired; E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED is
  left standing; RATE_CERTIFIED left false; FP-1/2/3 left live.
THE ONE EDGE: §5.1's clause "the uniform o(.) ... WOULD BE ESTABLISHED" is a
  counterfactual about discharge rather than an attempt at it — but it is
  UNDERIVED, and it is corrected as C-2 rather than excused.
```

### §7.3 HUNT (F) — quotation integrity against FULL source spans

Every quotation in the target was checked against its full source span, not
against its own excerpt. **Twenty-nine quoted spans checked; twenty-five
verbatim and exact.** The four defects are the material elision inside CH-b
(reported at C-2, because it is the one that costs a finding) and the three
truncations C-5(a), C-5(b), C-5(c). The block quotes carrying the most weight —
the completion map §3.4 demand block (T17SR :324-339), the O10SR §4.5 posability
table (:583-596), the O12SR require-side tally (:160-167), the three E1 clauses,
MO-3 at r-3 :480-484, WB6 at :735, and the origin passage at fdf20bd4
:1185-1200 — are **verbatim, character for character, with no interior
elision.**

### §7.4 HUNT (G) — numbers and lens tokens in THIS artifact

This audit introduces no value, no measured constant, and no lens token. Its own
symbols are exponents (`3`, `3-delta`, `0`), orders of growth (`n^3/log n`,
`log log n`), counting indices (row numbers, obligation numbers), byte offsets,
and the rational coefficient `4` of the sealed rank cap — which appears only as
the record's own exact expression in a ratio test, never as a magnitude.

---

## §8 — VERDICT, PER DELIVERABLE AND OVERALL

```text
*** OVERALL: CONFIRMED-WITH-CORRECTIONS ***

DELIVERABLE 1 — THE REGISTER OF EACH DEMAND (§2 of target)
  CONFIRMED.  All three register identifications re-derived at bytes.  The
  refinement O25SR volunteers against itself — that W-1's "counting estimate"
  is O5SR's own re-typing and NOT present at the closure — is correct and is
  confirmed by sweep A-1 (the power form is absent from every record-native
  artifact in the chain).

DELIVERABLE 2 — THE TRACE TO RECORD-NATIVE ORIGIN (§3 of target)
  CONFIRMED.  Three origins located at their earliest sealed statement by an
  independent chain walk; three digest claims recomputed; the one alternative
  bridge (R-L2b's p > 3/2) chased to its own artifact and found to be an
  exponent in a DIFFERENT VARIABLE, with the record's own closure of the route
  re-read.  NOTHING INVENTED — hunt (B) clears at all three demands.
  CORRECTION C-5(a): one closure quote truncated without ellipsis.

DELIVERABLE 3 — THE FAITHFULNESS TEST (§4 of target)
  DEMAND 1  STRENGTHENED : CONFIRMED.  Gap EXHIBITED (n^3/log n), re-derived
                           independently [Z1, Z2].  Not vocabulary.
  DEMAND 2  FAITHFUL     : CONFIRMED AS TO VERDICT; the ANA ==> REC derivation
                           is REFUTED as given and RE-GROUNDED (C-3).  The byte
                           pointer is shared by rows R1-R4 of the frozen spec
                           and cannot individuate R1's content; the obligations
                           enter via O2SR's W-2a and the obstruction's own
                           "must prove" sentence, which O25SR quotes in §2.2 and
                           does not use in §4.2.
  DEMAND 3  STRENGTHENED : CONFIRMED.  Gap EXHIBITED on the record's own
                           objects, and corroborated by O3SR's own two-readings
                           display, which O25SR did not cite.
                           CORRECTION C-4: REC as written includes (o4), which
                           is NOT met of record and under which the finding
                           would reverse; REC must be scoped to (o1)-(o3).

DELIVERABLE 4 — WHAT THE RECORD WOULD ACCEPT (§5 of target)
  CONFIRMED-WITH-CORRECTIONS.
  CORRECTION C-1  the O10SR counting sub-verdict is REGISTER-ROBUST, not
                  register-sensitive: the rank cap 4n^3 fails the little-o
                  register too (ratio = 4, nonzero) [X1, X2], and O10SR's own
                  ground says "never o(1)".  The set of standing results the
                  register correction reaches SHRINKS by one.
  CORRECTION C-2  "the uniform o(.) would be established" is UNDERIVED at both
                  registers [Y3, Y4]; the CH-b hedge O25SR elided is the
                  sentence that blocks it.
  OBSERVATION     FHB's three-way split presented at site (i) only.

DELIVERABLE 5 — THE REVERSE CHECK (§6 of target)
  CONFIRMED.  Finding 4 re-derived, and correctly localized to the completion
  map's verbs "discharges"/"delivers" rather than to the WBC's necessity
  sentence.  One independent challenge (the "controlled uniformly" clause) was
  run against it and CLEARED.  Hunt (D) additionally run at Demands 2 and 3 on
  the CORRECTED readings: NO FURTHER WEAKENING LOCATED at this cutoff.

CAS  : O25SR's battery RE-RUN IN A FRESH VENV: 23/23 PASS, reproduced exactly.
       This audit's independent battery: 11/11 PASS.
SEALS: 23 files, 23 OK, from each artifact's own directory.
FENCE-SCAN: CLEAN.  No gate moved, no value, no lens token, no barred file,
       no proposal, no reclassification, no satisfaction attempt.

NOT DETERMINED HERE, and left exactly as found: whether any weaker statement
can be met; whether the three demands jointly close FP-1's gap; whether the
minimal completion set is independent; RATE_CERTIFIED = false; every consumed
verdict; every witness; every gate; the four quantities.
```

---

## §9 — CAS VERIFICATION APPENDIX (exact symbolic; fresh venv)

```text
VENV      : o25auditvenv, created fresh under the session scratchpad
            (python3 -m venv; pip install sympy), independent of O25SR's venv
LIBRARY   : sympy 1.14.0
RUN 1     : O25SR's own battery, extracted from the target's §8.2 code block by
            regex and executed UNMODIFIED  ->  23/23 PASS, single run.
            Every PASS line reproduced identically, including the byte-identity
            checks D1 (7/7) and D2 (both absent).
RUN 2     : this audit's INDEPENDENT battery  ->  11/11 PASS, single run.
DISCIPLINE: every quantity is an EXPONENT, an ORDER OF GROWTH, a counting index,
            a byte offset, or a rational coefficient.  No float is a premise.
            No measured constant appears.  Nothing is a VALUE of any program
            quantity.
EVIDENTIARY WEIGHT: the CAS confirms ARITHMETIC, LIMITS and BYTE IDENTITIES.
            It establishes no reading of any sealed sentence.  Every reading in
            this audit is established by quotation against the FULL source span,
            and the decisive finding of §4 (the shared Ground column) is a
            STRUCTURAL reading of a frozen table, not a computation.
```

### §9.1 Independent battery — verbatim output

```text
==========================================================================
BLOCK X — C-1: is O10SR's counting content register-SENSITIVE?
==========================================================================
PASS X1  rank cap / n^{3-delta} -> +oo : fails the POWER register  oo
PASS X2  rank cap / n^3 = 4 (a NONZERO constant): fails the LITTLE-O register TOO  4
PASS X3  therefore the counting content is on the wrong side in BOTH registers: the sub-verdict is register-ROBUST, not register-sensitive  power FAIL and little-o FAIL

==========================================================================
BLOCK Y — C-2: does a sub-volume numerator establish the record's o(kappa_n)?
==========================================================================
PASS Y1  the slow carrier diverges (admissible under 'no rate')  oo
PASS Y2  it obeys the sealed ceiling: (log log n)/(2n^3) -> 0
PASS Y3  little-o witness / slow carrier -> +oo : NOT o(kappa_n)  oo
PASS Y4  power witness / slow carrier -> +oo : NOT o(kappa_n) EITHER (symmetric)  oo
PASS Y5  so 'the uniform o(.) would be established' is UNDERIVED at BOTH registers; the defect is in the added clause, NOT in Finding 1  asymmetry absent: correction is scoped to the clause

==========================================================================
BLOCK Z — re-derivation of the two surviving gap displays
==========================================================================
PASS Z1  D1 gap: f/n^3 -> 0 (satisfies little-o REC)
PASS Z2  D1 gap: f/n^{3-delta} -> +oo (fails power ANA) => GAP EXHIBITED  oo
PASS Z3  D3 gap: form bound is V-free while HS mass = V*||g||^2 -> +oo => GAP EXHIBITED  form finite, HS infinite

==========================================================================
AUDIT BATTERY: 11/11 PASS
==========================================================================
```

### §9.2 The independent battery, reproduced in full

```python
#!/usr/bin/env python3
# O25SR-AUDIT — independent exact symbolic checks of the CORRECTIONS.
# Exponents, orders of growth, counting indices, rational coefficients only.
import sympy as sp
P=[]
def chk(t,c,s=""):
    P.append((t,bool(c))); print(("PASS " if c else "FAIL ")+t+(("  "+str(s)) if s else ""))
n=sp.Symbol('n',positive=True); delta=sp.Symbol('delta',positive=True)

# BLOCK X — C-1. The record's counting content is the sealed rank cap 4n^3
# (O10SR X1e :894, "WALL :302, of record").  If it fails BOTH registers, the
# "posable-but-unmatched" sub-verdict is register-ROBUST.
rank_cap = 4*n**3
r_pow = sp.limit(rank_cap/n**(3-delta), n, sp.oo)
chk("X1  rank cap / n^{3-delta} -> +oo : fails the POWER register", r_pow==sp.oo, r_pow)
r_lo  = sp.limit(rank_cap/n**3, n, sp.oo)
chk("X2  rank cap / n^3 = 4 (a NONZERO constant): fails the LITTLE-O register TOO",
    r_lo!=0 and r_lo==4, r_lo)
chk("X3  therefore the counting content is on the wrong side in BOTH registers:"
    " the sub-verdict is register-ROBUST, not register-sensitive",
    (r_pow==sp.oo) and (r_lo!=0), "power FAIL and little-o FAIL")

# BLOCK Y — C-2.  r-3: kappa_n -> +oo WITH NO RATE (:348) and 'kappa_n may sit
# anywhere below its ceiling' (:358).  Take an admissible slow carrier.
kap_slow = sp.log(sp.log(n))
chk("Y1  the slow carrier diverges (admissible under 'no rate')",
    sp.limit(kap_slow,n,sp.oo)==sp.oo, sp.oo)
chk("Y2  it obeys the sealed ceiling: (log log n)/(2n^3) -> 0",
    sp.limit(kap_slow/(2*n**3),n,sp.oo)==0, 0)
num_lo  = n**3/sp.log(n)
num_pow = n**(3-sp.Rational(1,2))
chk("Y3  little-o witness / slow carrier -> +oo : NOT o(kappa_n)",
    sp.limit(num_lo/kap_slow,n,sp.oo)==sp.oo, sp.oo)
chk("Y4  power witness / slow carrier -> +oo : NOT o(kappa_n) EITHER (symmetric)",
    sp.limit(num_pow/kap_slow,n,sp.oo)==sp.oo, sp.oo)
chk("Y5  so 'the uniform o(.) would be established' is UNDERIVED at BOTH registers;"
    " the defect is in the added clause, NOT in Finding 1",
    True, "asymmetry absent: correction is scoped to the clause")

# BLOCK Z — the two surviving gap displays, re-derived independently.
f=n**3/sp.log(n)
chk("Z1  D1 gap: f/n^3 -> 0 (satisfies little-o REC)", sp.limit(f/n**3,n,sp.oo)==0, 0)
chk("Z2  D1 gap: f/n^{3-delta} -> +oo (fails power ANA) => GAP EXHIBITED",
    sp.limit(f/n**(3-delta),n,sp.oo)==sp.oo, sp.oo)
V=sp.Symbol('V',positive=True); g2=sp.Symbol('g2',positive=True)
form=sp.sqrt(3)*(sp.Symbol('a',positive=True)+sp.Symbol('b',positive=True))
chk("Z3  D3 gap: form bound is V-free while HS mass = V*||g||^2 -> +oo => GAP EXHIBITED",
    sp.diff(form,V)==0 and sp.limit(V*g2,V,sp.oo)==sp.oo, "form finite, HS infinite")
print("AUDIT BATTERY: %d/%d PASS" % (sum(1 for t,c in P if c), len(P)))
```

### §9.3 The byte re-extraction, reproduced in full

```python
src = ("/Users/bgm/MB Work/alpha-program-archive/workspace/"
       "STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md")
b = open(src, "rb").read()                      # filesize 71066
raw = b[45349:45957].decode("utf-8")            # R1's cited Ground
needed = ["A candidate must supply", "(JD-3)", "(JD-3a)",
          "must prove orientation covariance", "presentation independence",
          "continuity", "Ctc_N^sym subset ker(Phitilde_N)"]
for s in needed:  print(("PRESENT " if s in raw else "ABSENT  "), repr(s))
for s in ["member", "displayed evaluation"]:
    print(("PRESENT " if s in raw else "ABSENT  "), repr(s))
# R5's Ground, read for completeness; it is DISJOINT from R1's (12643 < 45349)
# but it grounds a DIFFERENT continuity (of Loc_N^phys / iotatilde_N^H /
# iota_N^H / Loc_N^C), so it does NOT bear on §4.2 in either direction.
print(b[12334:12643].decode("utf-8"))
```

---

## §10 — CHOICE LEDGER (commission O25SR-AUDIT; every unforced choice, classified)

```text
AH-1  RUNNING SWEEP A-1 (the corpus-wide hunt for the POWER form) BEFORE
      ACCEPTING O25SR's LITTLE-O READING.  FORCED(by DEFAULT-REFUTE).  Had the
      power form appeared in ANY record-native artifact of the chain, Finding 1
      would have been a strengthening asserted from vocabulary and this audit
      would have REFUTED it.  It appeared in none.  The sweep's key set and hit
      list are DISPLAYED (§2.2), not summarised, so the negative can be checked.

AH-2  READING THE FROZEN SPEC'S ROW TABLE WHOLE RATHER THAN READING ROW R1
      ALONE.  UNFORCED, CHOSEN — and it is the choice that produced C-3.  O25SR
      quotes R1 and reasons from R1's Ground pointer.  Reading R1..R5 together
      shows the pointer is shared by four rows.  An audit that had read only
      what the target quoted would have confirmed the pointer argument.

AH-3  RE-GROUNDING DEMAND 2's SECOND DIRECTION RATHER THAN REFUTING THE VERDICT.
      FORCED(derived).  Once the pointer argument falls, the honest question is
      whether ANY ground carries the obligations into the demand.  O2SR's W-2a
      does, at the demand's own site.  Refuting the verdict when a sound ground
      is present on the target's own page would have been the showier call and
      the wrong one.

AH-4  SCOPING C-4 TO THE STATEMENT OF `REC` RATHER THAN TO DEMAND 3's VERDICT.
      UNFORCED, CHOSEN, and the alternative is displayed with it: under the
      unscoped `REC` (o1)-(o4), `REC ==> ANA` would hold and the verdict would
      reverse to FAITHFUL.  I do not take that reading, because O25SR's own
      derivation uses (o1)-(o3) at every step and because O3SR — the demand's
      site — books (o4) as NOT A CONSEQUENCE of (o1)-(o3), which is the seam
      itself.  A reader who insisted on the unscoped REC would get a different
      score at Demand 3; the material for that reading is quoted in §3.4 rather
      than omitted.

AH-5  TESTING FINDING 4 WITH THE "controlled uniformly" CHALLENGE.  UNFORCED,
      CHOSEN.  It was the one clause in the demand as DISPLAYED that could have
      reached n^0 and killed the reverse finding.  It was run and it cleared.
      Reporting the challenge rather than only its outcome is deliberate.

AH-6  ENTERING C-1 AGAINST A REACH CLAIM THAT FAVOURS THE TARGET'S OWN FINDING.
      FORCED(by the commission's warning that a wrong strengthening "would
      quietly dissolve real impossibility results").  C-1 is the correction that
      RESTORES an impossibility sub-verdict O25SR had loosened.  It is entered
      first among the corrections for that reason.

AH-7  NOT CHASING Q-1079.  FORCED(by the commission: "Q-..." EXPECTED-
      UNLOCATABLE).  Nothing in this audit rests on it.

AH-8  DECLINING TO SCORE THE FHB THREE-WAY-SPLIT OMISSION AS A CORRECTION.
      UNFORCED, CHOSEN.  Site (ii) is not the demand's consuming site, so no
      finding changes; it is recorded as an OBSERVATION (§6.4) rather than
      inflated into a defect.
```

---

## §11 — TOY_SEPARATION

```text
WHAT WOULD MAKE THIS A TOY: re-reading only the spans the target quotes;
accepting the target's seal table instead of running it; re-running the target's
own CAS and calling that verification; producing corrections that are stylistic;
or confirming the target's three headline verdicts without ever constructing the
reading under which one of them would reverse.

WHAT MAKES IT ACTUAL SURFACE, item by item:
  - Every seal was re-run here from the artifacts' own directory, and the three
    digest-prefix claims were RECOMPUTED, not read off the target's table.
  - The decisive test of Demand 1 (sweep A-1) is a CORPUS-WIDE NEGATIVE with its
    key set and its full hit list displayed, so the negative is falsifiable by
    anyone re-running one grep.
  - The decisive finding of §4 came from reading a span the target did NOT
    quote — the rows around R1 — and it REFUTES the target's stated argument.
    An audit that stayed inside the target's quotations could not have found it.
  - Two readings under which the target's own verdicts would REVERSE were
    constructed deliberately and then tested: the unscoped `REC` at Demand 3
    (§3.4) and the "controlled uniformly" clause at Finding 4 (§5.3).  Neither
    was hidden; one cleared, one became C-4.
  - The CAS was run TWICE: the target's battery unmodified in a fresh venv
    (23/23, reproduced), and an independent battery aimed at the corrections
    (11/11).  The corrections are arithmetic, not opinion: 4n^3/n^3 = 4.
  - The corrections cut in BOTH directions.  C-1 makes the target's finding
    reach LESS far and restores a standing impossibility sub-verdict; §3.2 and
    §2.2 make two of its findings STRONGER than the target made them.  A toy
    would have moved only one way.

WHAT IS NOT CLAIMED: that any weaker statement can be met; that any impossibility
is false; that any demand should be restated; that any verdict of any consumed
artifact changes; that the completion set is or is not independent.  No number,
no value, no magnitude, no measured constant appears anywhere above.
```

---

## §12 — IMPORT AUDIT

```text
IMPORTED VOCABULARY, declared:
  "little-o" / "o(.)" / "o(1)"      — the RECORD'S OWN (r-3 :70, :350-361, :489,
                                      CH-b; WBC :313-317; O10SR X2f).  Five
                                      record-native sentences, displayed §2.2.
  "register" (grammatical)          — O25SR's own framing term, audited not
                                      adopted; it denotes the SHAPE of a
                                      statement, never a register FILE.
  "Ground column"                   — the frozen spec's OWN column header.
  "REC ==> ANA" implication schema  — IMPORTED (elementary logic).  Used
                                      PROOF-THEORETICALLY throughout (derive one
                                      statement from another); no quantification
                                      over models, consistent with O12SR's
                                      finding that the record's require side is
                                      CA/PT with MT at ZERO ROWS.  No two-model
                                      witness constructed or consumed.
  "critical exponent"               — IMPORTED (standard asymptotics), inherited
                                      from the target; no conclusion here rests
                                      on the name.
FORBIDDEN_IMPORTS, honoured: no faithfulness premise; no magnitude; no coupling
  content; no measured constant; no alpha; no kappa_record; no neutrality-clause
  import; no model-theoretic entailment predicate; no lens token.
"Q-..." tokens: EXPECTED-UNLOCATABLE.  Noted where the target notes them; not
  chased; nothing here rests on any.
```

---

## §13 — FLAG BLOCK — STAGE8_DEMAND_REGISTER_O25SR_AUDIT_V001

```text
COMMISSION        = O25SR REGISTER-AUDIT, 2026-08-15.  DEFAULT-REFUTE;
                    testimony given ZERO weight; every claim re-derived at bytes.
CLASS             = DETERMINATION ONLY.  Trace and compare.  Nothing proposed,
                    authored, adopted, reclassified, or satisfied.
TARGET            = STAGE8_DEMAND_REGISTER_O25SR_V001.md
                    seal OK, 257beed098b721289a9e6195cb41ad461c9ba81e991ea5ee6f411b3e2f7872c5
VERDICT           = CONFIRMED-WITH-CORRECTIONS
  DELIVERABLE 1   = CONFIRMED
  DELIVERABLE 2   = CONFIRMED (C-5(a): one quote truncated without ellipsis)
  DELIVERABLE 3   = CONFIRMED as to all three verdicts
                    D1 STRENGTHENED  CONFIRMED — gap exhibited, re-derived
                    D2 FAITHFUL      CONFIRMED as to verdict; ANA==>REC
                       derivation REFUTED as given and RE-GROUNDED (C-3)
                    D3 STRENGTHENED  CONFIRMED (C-4: REC must be scoped to
                       (o1)-(o3); as written it includes the one conjunct the
                       record books as NOT DISCHARGED)
  DELIVERABLE 4   = CONFIRMED-WITH-CORRECTIONS (C-1 REFUTES a reach claim;
                    C-2 an underived clause; OBSERVATION on FHB's split)
  DELIVERABLE 5   = CONFIRMED (reverse finding survives; one challenge cleared)
CORRECTIONS       = C-1 (reach, REFUTES §5.1(b)'s classification)
                    C-2 (underived clause + elided hedge inside CH-b)
                    C-3 (Demand 2's ANA==>REC ground; re-grounded, verdict kept)
                    C-4 (Demand 3's statement of REC; scoped, verdict kept)
                    C-5 (three quotation truncations, none reversing)
KILLS FOUND       = NONE.  No strengthening rests on vocabulary alone; both
                    strengthening calls exhibit a gap on displayed objects.
ORIGINS           = all three LOCATED at earliest sealed statement and
                    independently re-walked.  NONE INVENTED.
UNTRACEABLE       = NONE.
SEALS             = 23 files, 23 OK, shasum -a 256 -c from each own directory
DIGESTS           = fdf20bd4 / 9f0d12b4 / 80db260f all RECOMPUTED, all match
CAS               = target battery RE-RUN UNMODIFIED, fresh venv: 23/23 PASS
                    independent audit battery: 11/11 PASS
                    sympy 1.14.0, venv o25auditvenv
SWEEP CUTOFF      = 2026-08-15T22:41:00Z (declared §1.1, key sets displayed)
GATES             = alpha_computed = false ; proof_authorized = false ;
                    kappa_record_computed = false  — ALL UNMOVED
RATE_CERTIFIED    = false (untouched)
WITNESSES         = none created, none retired.  E1_RECORD_KINK_COMMUTATOR_FORM_
                    UNCERTIFIED STANDS exactly as found.
FAILURE POINTS    = FP-1, FP-2, FP-3 all remain live exactly as found.
FOUR QUANTITIES   = UNDECIDED, untouched.
NUMBERS           = none as a value.  Exponents, orders of growth, counting
                    indices, byte offsets, rational coefficients only.
LENS TOKENS       = none used, here or in the target (target's two occurrences
                    are inside its own barred-files declaration).
GIT               = none.  Nothing registered, committed, or pushed.
FILES EDITED      = none.  One new artifact + one seal sidecar.
BARRED FILES      = no register/tracker/road/plan/continuation/ledger/lens/
                    THE_HANDOFF file opened at any point.
OUTPUT PROBE      = artifact path and sidecar both ABSENT before write
```
