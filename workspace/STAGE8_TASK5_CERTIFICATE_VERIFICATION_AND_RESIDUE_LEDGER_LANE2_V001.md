# STAGE 8 TASK 5 — RETURN-CERTIFICATE VERIFICATION AND CONSOLIDATED RESIDUE LEDGER — LANE 2 V001

Date: 2026-08-04  
Task: PASTE 484 / Task 5  
Lane: Codex Lane 2  
Custody: adversarial cross-verification and residue consolidation only

## Lead result

```text
THRESHOLD_THEOREM = CONFIRMED |
  on an inhabited return certificate, on the reciprocal-loop regular branch,
  and with 0 < A_loop < infinity so that the displayed derivative supremum is
  the true modulus,

    strict contraction
      iff |p_loc[Rhat_K]| < A_loop^(-1).

  The theorem is not an iff for fixed-point existence, does not extend to
  S8-A without the perpendicular reader coordinates, and proves no physical
  inequality because neither A_loop nor the reader coefficient is fixed.

CERTIFICATE_INTERFACE = CONFIRMED |
  every inhabitant carries the independent domain, stationary graph,
  regularity estimates, bounded reader, full residual, and RETURN proof;
  therefore Lipschitz boundedness and self-map closure follow.  No inhabitant
  is supplied by the ratified stack.

INHABITANT_MAP_1 = FIBER_GRADE |
  SCALAR_K_TO_COMPLETED_ACTION_AND_FULL_RESIDUAL_MAP is new physical action
  dependence.  Q-396's admissible flat action directions produce a lawful
  non-singleton target-blind family of such arrows with identical sealed
  finite shadows.  Nothing ratified selects its K dependence.

INHABITANT_MAP_2 = CONSTRUCTIBLE_WITH_BRANCH_BOUNDARY |
  once Map 1 is supplied, the full stationary solution relation is defined by
  the already-declared residual and can be attacked by implicit, monotone, or
  compactness machinery.  A single-valued solution map needs either proved
  uniqueness/consumer-uniformity or a separately disclosed branch rule.

RESIDUE_COUNT = 14 objects |
  6 ratification-grade, 5 constructible, 3 derivable-open.
  Several rows are dependent coordinates or downstream outputs, not fourteen
  independent physical constants; those dependencies are stated in the ledger.
```

No value of `p`, `nu`, either rank, a response, a fixed point, a protected
quantity, or a measured constant is evaluated here.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
```

---

## 0. Preflight, seals, and register sweep

### 0.1 Preflight

The locked process was read in full from the archive supervision location.
The submitted artifact and its sidecar were verified before the artifact was
read.

| Object | SHA-256 / result |
|---|---|
| `supervision/LOCKED_PROCESS.md` | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` |
| `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `bd99a5a2d66b68af9a678cb3aac4856c4b714ff6b9b11fa8924eb99095ee86cd` |
| artifact under review | `b569a89e661ad92b744213bfc7cd65985908bc509b8dd9de77bcae3a2bdb4bad` — exact match |
| submitted sidecar check | `STAGE8_TASK5_RETURN_CERTIFICATE_BUILD_LANE1_V001.md: OK` |

The register's last numbered entry is Q-405.

```text
DOES_THE_OBJECT_EXIST = yes_as_a_typed_certificate_and_conditional_theorem |
                        no_as_an_inhabited_physical_certificate
IS_THE_VERSION_CURRENT = yes | through Q-405
ARE_ITS_INPUTS_PRESENT = yes_for_verification_and_interface_typing |
                         no_for_the_two_physical_maps_and_return inhabitant
PREFLIGHT = PASS_WITH_TYPED_STOP
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| return-certificate build | `b569a89e661ad92b744213bfc7cd65985908bc509b8dd9de77bcae3a2bdb4bad` | object under review |
| Q-404 typing verification | `02786457ea34eaab16b709a373ae44ce1f8f16c59ecede2eb60720f8ca1aea01` | exact certificate interface and modulus scope |
| Q-403 scalar carrier build | `aebe708d2e7ba4b67e828976bd01eae2d5eec04afbb6a28f3f77f9dc8003fc97` | ambient Maxwell line, memberwise reader, rank-one factorization |
| Q-401 conditional theorem | `88854f08966c15e6afbcb300c6151f59a169e2725c3d5a8643b653abfe3ddcb3` | Banach premises and sensitivity boundary |
| Q-396 representative theorem | `f0f8b09b1aa6a16c0ed0dffedbd275aba9e647fb5841fc8ce06ce9e6a8b00857` | nontrivial admissible completion/action fiber |
| Q-395 seam audit | `337769f4a122512de5c79a9fe4f936c4edac2170bfe3bc471484d4ff85724a34` | finite/completed split and Task-5 value path |
| Q-397 consumption derivation | `cacd317391759c1b1c6925c671be33cf4e91b7cccf4c3a529669dea6c18400fc` | reading-rule interface |
| Q-400 coefficient/symbol result | `70dde1c0cb776abec4a61cac118d4edf1f4c75a1185a2e984dc341961d4b643c` | `chi_K` fiber and LM-1/LM-2 map |
| p-verdict computation | `27790d53b018a84f5f02e97f68e885de0ebb332735307ae0fa433322a6053189` | completed response dependencies |
| metric V005 / DoR-019 object | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | completed carriers, Riesz maps, R4-only unit seam |

### 0.3 Questions-settled sweep

The Q-391–Q-405 seam sequence was read as one chain.  Additional searches
covered `stationary return`, `full residual`, `scalar K to action`,
`stationary pullback`, `reader`, `local symbol`, `completion representative`,
`nu`, `integer pair`, `A32`, `stitching`, `O-D3`, and the Task-6 plan.

| Entry | Settled content consumed here |
|---|---|
| Q-391 | exact `Response_base(p)+nu Response_flat` split; no rank port beyond `p` |
| Q-392 | computation confirmed; executable completed base family absent |
| Q-393 | exact RetExtract kernel; types do not decide Keldysh support |
| Q-394 | completion does not choose an action representative |
| Q-395 | finite response canonical/p-free; completed family required by the value path; no identification theorem |
| Q-396 | admissible completed action/response fiber is nontrivial and physical |
| Q-397 | completed reception fixed; reading semantics underdetermined |
| Q-398 | S8-A reduction to `p_loc[R_K]`; full rank needs local symbols |
| Q-399 | no contraction, washout, monotonicity, or asymptotic theorem |
| Q-400 | reading rule and `p_loc[R_K]` remain freedoms; covariant local-symbol map unbuilt |
| Q-401 | exact conditional Banach theorem; scalar `K` is not `K_cycle` |
| Q-402 | premise audit |
| Q-403 | complete ambient scalar line; memberwise reader; exact modulus factorization |
| Q-404 | factorization confirmed; return interface constructible; no coefficient bound |
| Q-405 | conditional threshold and two missing maps |

The older bearing results were also checked: Q-252 supplies only a conditional
critical-set theorem after a pullback is independently given; Q-281 supplies a
seed, not a completed stationary solution; Q-314/Q-315 name the missing
completed pullback/solution map; Q-365 proves the Hessian covariance cube only
on generated Hessians; Q-384 supplies geometry, not scalar-parameterized
dynamics.  Task 4d's Q-258/Q-261 result leaves the geometric stitching/O-D3
obligation open and explicitly decoupled from the present fixed-point chain,
but the governing plan requires it to be checked again before the public
Task-6 result.

No later entry supplies either inhabitant map, a physical domain, a common
reader topology, a finite `A_loop`, a magnitude bound on the reading
coefficient, or the threshold inequality.

### 0.4 Symbol separations

```text
K_scalar       dimensionless variable in K=B_ind(K)
K_cycle        DoR-019 physical cycle Hilbert carrier
Rhat_K         R4-unit-dressed K-sector Riesz direction
ell            one retained completed local reader
chi_K          ell[Rhat_K]=p_loc[Rhat_K]
I              one completed action/response representative
S_I            scalar-parameterized stationary solution map, if built
H_I            RetExtract(Schur_I at S_I(K))
A_loop         sup_D |a_loop(K,p,nu,I)| on a regular reciprocal branch
nu             symbolic N-member scale, not a reader normalization
(r_0,r_ch)     ordered positive integer input, never a selected numerical pair here
```

---

## 1. Verdict table Z1–Z5

| Item | Verdict | Exact finding |
|---|---|---|
| Z1 certificate interface and conditional theorem | **PASS_WITH_SCOPE** | an inhabitant is proof-carrying; it supplies finite regularity bounds and an independently proved RETURN inclusion, so Lipschitz boundedness and closure follow; existence of an inhabitant remains `TYPE-U` |
| Z2 threshold theorem | **PASS_WITH_SCOPE** | the reciprocal-loop `iff` is exact for the true modulus with `0<A_loop<infinity`; edge cases, S8-A, and existence are correctly excluded; no authority proves the inequality |
| Z3 inhabitant maps | **PASS_WITH_RETYPE** | Map 1 is **FIBER-GRADE**; Map 2 is **CONSTRUCTIBLE_WITH_BRANCH_BOUNDARY**; neither is derivable from the current ratified stack |
| Z4 consolidated residue ledger | **PASS / BUILT HERE** | fourteen dependent objects are classified; six ratification-grade, five constructible, three derivable-open |
| Z5 falsifiers and fresh attack | **PASS** | circular domain, graph-norm, finite-zero promotion, disconnected-domain threshold, hidden branch selection, and scalar/cycle type-confusion attacks all handled |

---

## 2. Z1 — certificate interface and conditional theorem

### 2.1 Minimal proof-carrying interface

For one admitted completion/reader pair `(I,ell)`, an inhabitant contains:

```text
D          nonempty closed subset of K_amb=Scalar_dimless^real;
S_I        D -> completed full stationary solution class;
H_I        K |-> RetExtract(Schur_I at S_I(K));
O_phys     one named Banach response home, or a proved uniformly equivalent family;
ell        bounded O_phys -> Scalar_dimless, with norm M_ell;
L_H        finite Lipschitz bound for H_I on D;
RETURN     an independent proof that ell[H_I(D)] is a subset of D and that
           the returned points again admit the full stationary solution;
FULL       every source, metric, Gauss, record, boundary, contact, glue, and
           R_comp equation;
CERTS      covariance, reality, batching, W3/rho_H,N, DoR-008 restrictions,
           units, quotient, zero-extension, and the permanent regressions.
                                                        (Z1-1)
```

The interface must carry the proof of `RETURN`; defining `D` as the set on
which return already holds is rejected as circular.

### 2.2 Boundedness on every inhabitant

For `K,K' in D`, boundedness of the reader and regularity of the response give

```text
|ell[H_I(K)]-ell[H_I(K')]| 
 <= M_ell ||H_I(K)-H_I(K')||_O
 <= M_ell L_H |K-K'|.                              (Z1-2)
```

Thus the scalar composite is Lipschitz with

```text
Lip(B_(I,ell)) <= M_ell L_H.                       (Z1-3)
```

On the Q-403 memberwise coefficient-plus-kernel completion, `M_ell=1`.
This is boundedness in the analytic theorem's required Lipschitz sense.  A
globally bounded range on an unbounded `D` does not follow merely from
Lipschitz continuity and is not needed by Banach; if such a range estimate is
claimed later, it additionally needs one finite base value and a bounded
domain or an independent a-priori estimate.

### 2.3 Closure on every inhabitant

The certificate's `RETURN` component proves, rather than defines,

```text
B_(I,ell)(D) subset D,                             (Z1-4)
```

and includes existence of `S_I(B(K))` with the same full residual.  Therefore
closure genuinely follows from an inhabitant.  Carrier completeness, reader
normalization, physical reality, and finite zero restrictions alone do not
prove `(Z1-4)`.

### 2.4 Conditional Banach theorem

If `(Z1-1)` is inhabited and the true scalar modulus satisfies `q<1`, then
`D`, being closed in the complete scalar line, is complete; `(Z1-4)` makes
`B` a self-map; and Banach gives exactly one fixed point in `D`.  This is a
valid conditional theorem.  Its converse is false: unique fixed points need
not arise from contractions.

```text
Z1_INTERFACE = CONFIRMED
Z1_BOUNDEDNESS_IMPLICATION = CONFIRMED
Z1_CLOSURE_IMPLICATION = CONFIRMED
Z1_CERTIFICATE_INHABITED = false / TYPE-U
Z1_UNCONDITIONAL_EXISTENCE = false / TYPE-U
```

---

## 3. Z2 — threshold theorem recomputed

### 3.1 Reciprocal-loop factorization

On the reciprocal loop, `dim K_cycle=1`.  Hence every differentiated
retarded response is uniquely

```text
J_loop(K)=partial_K H_I(K)
         =a_loop(K,p,nu,I) Rhat_K.                 (Z2-1)
```

For a fixed reader along the branch,

```text
partial_K B_(I,ell)(K)
 =ell[J_loop(K)]
 =a_loop(K,p,nu,I) chi_K(I,ell),

chi_K(I,ell)=ell[Rhat_K]=p_loc[Rhat_K].           (Z2-2)
```

On a convex/interval branch where the stationary solve and R5 operations are
`C1` (or absolutely continuous with the corresponding essential supremum),
the derivative supremum is the true difference-quotient modulus:

```text
q_loop=|chi_K| A_loop,
A_loop(p,nu,I;D)=sup_(K in D)|a_loop(K,p,nu,I)|.  (Z2-3)
```

The theorem under review correctly makes the true-modulus hypothesis.  A
disconnected or singleton domain may have a smaller difference-quotient
modulus than a derivative supremum sampled on unused directions; such a
domain is outside an unqualified use of `(Z2-3)`.  This is the strongest
fresh scope attack and it does not kill the theorem as stated.

### 3.2 Exact cases

For finite `chi_K`:

```text
A_loop=0:
  q_loop=0; no inverse threshold is needed.

0<A_loop<infinity:
  q_loop<1
    iff |chi_K| A_loop<1
    iff |p_loc[Rhat_K]|<A_loop^(-1).              (Z2-4)

A_loop=infinity, chi_K!=0:
  q_loop=infinity; the contraction route fails.

A_loop=infinity, chi_K=0:
  q_loop=0 pointwise; this is not the undefined product 0*infinity.
                                                        (Z2-5)
```

Equation `(Z2-4)` is an iff for **strict contraction**, not an iff for
existence or uniqueness by arbitrary fixed-point methods.

### 3.3 Definition and dependence of `A_loop`

`A_loop` is defined only after a nonempty physical domain and regular
stationary branch exist.  Its complete symbolic dependence is

```text
A_loop=A_loop(p,nu,I;D),                           (Z2-6)
```

where:

- `p` enters through base stationary blocks;
- `nu` enters through Shape-K content and any movement of the stationary
  branch;
- `I` is the completed action/response representative;
- `D` and the regularity/continuation package determine the supremum's domain;
- the fixed reciprocal reader enters separately through `chi_K`, not through
  `A_loop`.

At S8-A the derivative decomposes as

```text
J_S8A=a_0 Rhat_K+J_perp,
q_S8A=sup_D |a_0 chi_K+ell[J_perp]|,              (Z2-7)
```

so no threshold in `|chi_K|` alone is lawful there without additional bounds
on the covariant perpendicular reader coordinates.

### 3.4 Inequality sweep

The following ratified clauses were tested and none supplies `(Z2-4)`'s
physical inequality:

| Clause | What it fixes | Why it does not prove the inequality |
|---|---|---|
| `ell[L_T]=1` normalization | one Maxwell reference direction | no theorem identifies `Rhat_K` with `L_T` |
| DoR-019 units/Riesz maps | legal domain, dual units, and R4 conversion | no reader magnitude or operator norm bound |
| covariance/reality | orbit and conjugation behavior | no magnitude |
| DoR-008 finite zeros | every sealed finite shadow | no completed scalar reader and no bound on a finite-invisible branch |
| Q-403 memberwise norm | `||ell||=1` in its own reader-dependent completion | makes `||Rhat_K||` reader-dependent; no common physical bound |
| trace/tower amplitudes | bounded finite data | no passage through stationary solve, inverse, Schur, RetExtract, and local reader |

Neither `A_loop`'s finiteness nor a strict bound on `chi_K` is ratified.

```text
Z2_THRESHOLD = CONFIRMED
Z2_INEQUALITY_PHYSICALLY_PROVED = false / TYPE-U
Z2_S8A_ONE_FACTOR_THRESHOLD = false / TYPE-R
```

---

## 4. Z3 — the two inhabitant maps typed

### 4.1 Map 1: scalar `K` to completed action and full residual

The required type is

```text
A_I : K_scalar in D
      -> Gamma_(K,I) in Act_phys^017,

F_I(K,G)
  := full Euler/Legendre, source, metric, Gauss, record, boundary, contact,
     glue, and R_comp residual generated by Gamma_(K,I).           (Z3-1)
```

No ratified arrow has this type.  In particular, `K_scalar` is not
`K_cycle`, `R_K`, a source port, or a parameter already present in DoR-017's
member.  R5 starts with a supplied action/stationary package; it does not
parameterize that package by the fixed-point scalar.

The failure is not merely lack of labor.  Let `I_0` be any admissible Q-396
completion and let `Psi` be one of Q-396's nonzero, covariant, reality- and
restriction-compatible flat action directions.  For any target-blind,
dimensionless regular scalar function `h` of the correct base convention,

```text
A_I^(0)(K)=Gamma_(I_0),
A_I^(h)(K)=Gamma_(I_0)+h(K) Psi.                  (Z3-2)
```

Both arrows preserve the same sealed finite action jets because every finite
restriction of `Psi` is flat/zero in the falsifier's active data.  Covariance,
reality, batching, quotient, and units are inherited when `h` is a scalar;
the full action support family was fixed before any threshold consequence.
The arrows differ at the completed level.  Current authorities neither force
`h=0`, force one nonzero `h`, nor identify all such arrows as gauge.

`(Z3-2)` is a family witness, not an adopted choice and not a claim that every
member already satisfies global return.  It proves the scalar-to-action arrow
contains genuine completed-level physics which must be retained as a family
or ruled through a gate.

```text
SCALAR_K_TO_COMPLETED_ACTION_AND_FULL_RESIDUAL_MAP = FIBER-GRADE
MAP1_DERIVABLE_FROM_RATIFIED_STACK = false / TYPE-R
MAP1_MEMBER_SELECTED = none
MAP1_PREPARED_FIBER = target-blind admissible h(K)*Q396-action directions
```

### 4.2 Map 2: completed stationary pullback and solution map

After one Map-1 member/family is supplied, the physical solution relation is
not another freely invented equation:

```text
Sol_I(K):={G in X_G : F_I(K,G)=0 and every full certificate passes}.
                                                        (Z3-3)
```

This relation is constructional.  A local executable route consumes:

```text
one base pair (K_0,G_0) with F_I(K_0,G_0)=0;
Frechet differentiability of F_I;
D_G F_I(K_0,G_0) a bounded isomorphism on the named completed domains;
R1-COV, rho_H,N, reality, restriction, batching, units, and full residual;
continuation without inverse-domain loss over the independent D.   (Z3-4)
```

Implicit-function machinery would then construct a local `S_I`.  Monotone or
compactness machinery could construct the full solution relation under their
own coercivity/order/compactness premises.  None of `(Z3-4)` is presently
instantiated, but no new residual law need be authored after Map 1.

The boundary is nonuniqueness.  If `Sol_I(K)` contains more than one member,
the lawful output is the whole covariant family.  A single-valued `S_I` may be
used only after one of:

```text
stationary uniqueness;
consumer-uniformity of H_I over Sol_I(K);
or a separately ratified branch/member rule.             (Z3-5)
```

The first two are constructible theorems; the third would be fiber-grade
physics.  Nothing here selects it.

```text
COMMON_ORIGIN_COMPLETED_STATIONARY_PULLBACK_AND_SOLUTION_MAP = CONSTRUCTIBLE
MAP2_CONSTRUCTION_ROUTE = full solution relation + local/global continuation
MAP2_SINGLE_VALUED_WITHOUT_UNIQUENESS = false / TYPE-R
MAP2_BRANCH_SELECTION = none
```

### 4.3 Relationship between the two maps

Map 2 cannot be constructed before Map 1 because `F_I(K,G)` is otherwise
undefined.  Map 1 alone does not prove nonempty solutions.  Neither the
DoR-019 completion convention nor Q-396's existence of action representatives
supplies a stationary point.  This dependency is why the two maps must not be
merged into an anonymous “background” datum.

---

## 5. Z4 — consolidated residue ledger

### 5.1 Classification convention

```text
freedom-for-ratification:
  a genuine physical family/input remains and current authority does not
  select a member; a future derivation may still collapse it, but no such
  derivation is presently available;

constructible:
  its interface and lawful build route are known; it is not a free physical
  coefficient unless the construction later exposes a nonunique branch;

derivable-open:
  no authored datum is presently required; the result is a symbolic theorem,
  check, or computation waiting on earlier ledger rows.
```

The count below is a count of steering objects, not independent constants.
Rows R2/R3 depend on R4; R8 is an output of R5–R7; and R11 is an output of
the return package plus R8.

### 5.2 Centerpiece ledger

| # | Remaining object | Type | What it gates | Prepared fiber or route |
|---:|---|---|---|---|
| R1 | Q-396 completed action/response representative | **freedom-for-ratification** | which completed R5 operator and base action enter Task 5 | `Adm_base` with the explicit covariant flat `Psi_(a,b,d)` witness; retain the family or ratify a covariant section |
| R2 | Q-397 completed `p_loc` reading rule | **freedom-for-ratification** | scalarization of the completed response and all fixed-point sensitivity | `READING_RULE_FIBER_Q396`: normalized, covariant, real, unit-correct, restriction/batching-compatible readers; depends on R4 for physical instantiation |
| R3 | `chi_K=p_loc[Rhat_K]` (Q-398/Q-400) | **freedom-for-ratification** | reciprocal-loop modulus and the S8-A exchange-orbit pushforward | coordinate of R2, not an extra independent freedom; normalization fixes `p_loc[L_T]`, not this coordinate |
| R4 | covariant completed local-symbol map, including the carrier-to-field seam | **constructible** | makes R2 physically enumerable and computes `p_loc` on rank-one profiles | LM-1/LM-2: topology, spectral/long-wavelength symbol, Ward/contact split, R4 units, covariance/reality/restriction/batching/kernel certificates |
| R5 | scalar-`K` to completed action/full-residual map | **freedom-for-ratification** | defines the stationary problem as a function of the fixed-point scalar | family `(Z3-2)`: target-blind scalar functions multiplying Q-396 admissible action directions; no member selected |
| R6 | completed stationary pullback/solution family | **constructible** | supplies `G_(K,I)`, the Schur blocks, and the physical `B_ind` branch | solve `(Z3-3)` from R5; implicit/monotone/compactness route; retain full family unless uniqueness or uniformity is proved |
| R7 | nonempty physical scalar domain, common operator home, global continuation/return/regularity certificate | **constructible** | boundedness, closure, family-wide sensitivity, and application of a fixed-point theorem | independently specify `D`; prove common topology/readers, full residual, return, finite restrictions, and uniform regularity; no graph-norm or target-defined domain |
| R8 | `A_loop`, full modulus, and threshold decision | **derivable-open** | whether the contraction route yields existence/uniqueness | compute from R6/R7 and R2/R3; reciprocal threshold `(Z2-4)`, S8-A includes perpendicular coordinates; noncontractive route remains optional |
| R9 | numerical `nu` scale | **freedom-for-ratification** | instantiates the ratified N-member contribution and the later response/evaluation | DoR-017/018 keep `nu` symbolic; requires an independently sealed scale at the A32/final-evaluation gate; no value here |
| R10 | ordered integer pair `(r_0,r_ch)` | **freedom-for-ratification** | fixes the declared origin input `p=r_ch/(r_0+r_ch)` wherever p survives | DoR-014 declares the ordered positive-integer input and its gauge theorem; no pair or ratio evaluated; this is an already-declared input, not a new authored field |
| R11 | fixed-point solution and freedom-sensitivity classification | **derivable-open** | completes Task 5: existence/uniqueness, `p` dependence, and whether R1–R3 reach the fixed point | after R6–R8, apply Banach or a proved alternative and equations for parameter derivatives; compute symbolically only after authorization |
| R12 | DoR-007 geometric stitching/cellulation-independence package (Task 4d/O-D3) | **constructible** | public discrete-to-continuum equivalence checked again at Task 6 | build the `J_ref` realization/refinement and response-naturality/common-refinement package; Q-261 leaves it open and decoupled from R5–R11 |
| R13 | Task-6 structural authorization bundle | **derivable-open** | permission to evaluate the protected endpoint | execute Ward, Gauss, Einstein, causality, positivity, and limits; then `dC/dK!=0`, positivity, no-other-root, and D2/D3 checks on the solved object; no check run here |
| R14 | A32 pre-evaluation seal and deterministic evaluation/result-seal machinery | **constructible** | the publicly committed, mechanically reproducible sealed endpoint | A32 freeze is ratified, but prediction-map/universe seal and evaluator execution remain future acts; registry/holdout bytes stay untouched here |

### 5.3 Count and dependency precision

```text
RATIFICATION_GRADE = 6 objects | R1, R2, R3, R5, R9, R10
CONSTRUCTIBLE       = 5 objects | R4, R6, R7, R12, R14
DERIVABLE_OPEN      = 3 objects | R8, R11, R13
TOTAL               = 14 steering objects
```

This does **not** assert six independent continuous constants:

- R3 is a coordinate of R2;
- R5 is a section/path through completed action data whose constant slices
  include R1;
- R9 and R10 are already-declared symbolic inputs awaiting instantiation, not
  hidden additions;
- R8, R11, and R13 are downstream determinations, not physical choices.

No additional `lambda` row survives: DoR-018 ratified Shape K, while the
Shape-CK `lambda` family remains banked behind a future gate and is not on the
live route.  No separate rank ratio row is added because it is determined by
R10.  No completed representative is silently identified with the canonical
finite zero: Q-396 refutes that identification.

### 5.4 What the principal can steer now

The shortest Task-5 route is not to choose a threshold value.  Its lawful
dependency order is:

```text
R4 -> physically instantiate the reader family R2 and its coordinate R3;
R5 -> declare/derive how scalar K enters the completed action;
R6 -> construct the no-selection stationary solution family;
R7 -> prove a nonempty returned domain and regularity;
R8 -> decide the exact modulus and select no coefficient by outcome;
R11 -> solve/classify the fixed point if a fixed-point route closes.
                                                        (Z4-1)
```

R1 is already proved a genuine physical fiber and must be carried throughout.
R9/R10 remain symbolic until their own lawful instantiation.  R12–R14 are
post-solution obligations on the route to the sealed endpoint.

---

## 6. Z5 — falsifiers and fresh attacks

### 6.1 Falsifier table

| Attack | Execution | Verdict |
|---|---|---|
| circular physical domain | defining `D={K:S_I(K) exists and B(K) in D}` presupposes return | **REJECTED**; R7 requires an independently specified domain and proof |
| graph-norm tautology | choosing a graph norm after seeing `B` can force boundedness and alter the true modulus | **REJECTED**; common home/norm precedes the estimate |
| finite-zero promotion | Q-243/Q-279/Q-309 zeros do not set the completed response or reader to zero | **REJECTED** by Q-396 witness |
| reader normalization leak | `ell[L_T]=1` does not imply `ell[Rhat_K]=1` or any bound | **REJECTED** by Q-400 typing |
| reciprocal formula at S8-A | perpendicular covariant directions survive | **REJECTED** by `(Z2-7)` |
| `0*infinity` ambiguity | pointwise `chi_K=0` is separated from the nonzero-coefficient case | **PASS** piecewise theorem |
| scalar/cycle relabeling | treating `K_scalar` as a coordinate on `K_cycle` would fake Map 1 | **REJECTED** by units and carrier types |
| hidden stationary member | a single `S_I` without uniqueness/uniformity selects a branch | **REJECTED**; full relation retained in R6 |
| target-tuned threshold | choosing R2/R3/R5/R7 after reading whether `q<1` | **VOID** by order `(Z4-1)` |
| disconnected-domain derivative attack (fresh) | derivative supremum need not equal the minimal difference quotient on an arbitrary disconnected `D` | **SURVIVES AS SCOPE**: theorem requires the true modulus on a regular interval/AC branch |
| constant-action-map attack (fresh) | `A_I(K)=Gamma_I` is not uniquely forced; Q-396 directions generate equally finite-compatible nonconstant arrows | **CONFIRMS MAP1 FIBER-GRADE** |
| implicit-function shortcut (fresh) | no `F_I`, base solution, or invertible full derivative is ratified | **REJECTED**; Map 2 remains constructible, not derived |

### 6.2 Minimal-stage and standing regressions

| Regression | Result |
|---|---|
| one edge / connected tree | `K_cycle=0`; no cycle reader coefficient or scalar fixed-point map inferred — **PASS** |
| reciprocal loop | exact rank-one algebra and piecewise threshold — **PASS_WITH_UNINHABITED_BRANCH** |
| reciprocal loop with probes | retarded finite block remains exactly zero; noise/dephasing data do not become `B_ind` — **PASS** |
| S8-A exchange | covariant perpendicular directions retained; no basis member selected — **PASS** |
| pendant/tree quotient | quotient-null content not restored by any map family — **PASS** |
| reality/batching/W3/`rho_H,N` | carried as required certificates, not assumed for ungenerated Hessians — **PASS_WITH_SCOPE** |
| Q-396 completion fiber | finite-invisible nonzero completed directions retained — **PASS** |

### 6.3 Anti-tuning and fence ledger

```text
reader_member_selected = none
completion_member_selected = none
scalar_to_action_member_selected = none
stationary_branch_selected = none
rank_pair_selected = none
nu_value_selected = none
domain_or_norm_chosen_from_threshold = false
alpha_or_protected_value_evaluated = false
measured_constant_compared = false
holdout_private_area_entered = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 7. Final board

```text
Z1_CERTIFICATE_INTERFACE = CONFIRMED
Z1_INHABITANT = NOT_BUILT / TYPE-U
Z1_BOUNDEDNESS_AND_CLOSURE_ON_ANY_INHABITANT = CONFIRMED

Z2_RECIPROCAL_THRESHOLD = CONFIRMED
Z2_THRESHOLD_SCOPE = true modulus; regular branch; 0<A_loop<infinity
Z2_PHYSICAL_INEQUALITY = NOT_PROVED / TYPE-U
Z2_S8A_REDUCTION_TO_ONE_FACTOR = REFUTED

Z3_MAP1 = FIBER-GRADE
Z3_MAP2 = CONSTRUCTIBLE_WITH_BRANCH_BOUNDARY
Z3_MAP_MEMBER_SELECTED = none

COMPLETED_FAMILY_FREEDOMS_RETAINED = yes
P_SYMBOLIC = true
NU_SYMBOLIC = true
RANKS_SYMBOLIC = true

THRESHOLD_THEOREM = CONFIRMED
RESIDUE_COUNT = 14 objects (6 ratification-grade, 5 constructible, 3 derivable-open)

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

