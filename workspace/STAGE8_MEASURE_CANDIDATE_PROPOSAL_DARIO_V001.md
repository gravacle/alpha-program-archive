CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00006475   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 20 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing. EVERY PINNED DIGEST IN THIS ARTIFACT WAS COMPUTED AT ITS PATH,
  never completed from a displayed prefix (the mandate's process clause; the slip it names cost two
  earlier relays a refusal each).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001; positional member numbers resolve or the seal
  refuses (generator R5). No flag name is an anchor.
LANE = DARIO   ROLE_THIS_RELAY = THE MEASURE CANDIDATE — AUTHORED omega_phys (relay 1076)
AUTHORITY = member 02, the MEASURE SUPPLY MANDATE, verified LIVE at seal time. This is AUTHORIZED
  AUTHORING of omega_phys, lawful ONLY here and ONLY in the form that mandate types. It ratifies NO
  CONTENT and none is entered by this artifact.
SELF_CITATION_BAR = DARIO's own outputs are NOT record witnesses. Members 03, 04, 05 and 11 are
  SUBJECTS the assignment names; every load-bearing ingredient below is anchored to a sealed
  non-DARIO member at a locus, or is a standard theorem the record already licenses importing
  (member 06 :81-96).
BLIND EXPOSURE = MAXIMAL, per member 02 :43-45. kappa_record, alpha and the battery are NAMED AS
  POINTERS at section 6.2 and were NOT opened, consulted, imagined, or optimized toward at any point.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1076_MEASURE_CANDIDATE_DARIO_V001.md` | `8700e5959d815b4fc35a5a51a2ff2cffc55e368ebfbeed94f6e15fb35c855b1b` | assignment |
| 02 | `workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | **THE AUTHORITY — verified live** |
| 03 | `workspace/STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md` | `03dcaa17c78922f90bb905cd1ea7d9e9673514b74a229f0e6808a7a1dad1edd8` | the STILL-CONTINUUM proof (SUBJECT) |
| 04 | `workspace/STAGE8_DESC_THETA_HIST_DARIO_V001.md` | `47f7c1248bcb46f631584fe647feb5e614a32ca8a894557d245d06ed343a64ba` | the delivery — `Z_N`, `q_N` (SUBJECT) |
| 05 | `workspace/STAGE8_DESC_NECESSITY_PROBE_DARIO_V001.md` | `cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577` | the necessity probe (SUBJECT) |
| 06 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **`Lambda`, `X`, `Y`, `B iso C(Y)`, `State(B) <-> Prob(Y)`, the licensed standard theorems, AND `omega_H` NAMED** |
| 07 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md` | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` | **`Theta_F` at its own source** |
| 08 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `i_B`, `res_B`, **SM-1..SM-8 — including the one this candidate fails** |
| 09 | `workspace/STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | the S1 receiver — `Theta_hist`'s typing |
| 10 | `workspace/ATTACH_ENTRY_ADDENDUM_V001.md` | `420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73` | the Attach entry of record |
| 11 | `workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md` | `ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc` | the entered Attach candidate (SUBJECT) |
| 12 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | `Z_N`'s exact form, equation (3) |
| 13 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U3_DOR008_DOR009_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `b0118d89e0b4b321fbf9caab5bfb811a0b5fd572e808d02615ee8503db2bc1ac` | `q_N`, `Q_N = U(1)^N` |
| 14 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | the descent structure. READ ONLY WITH 15 AND 16. |
| 15 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 14 |
| 16 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification; the no-member clause |
| 17 | `workspace/STAGE8_DESC_DEMAND_DARIO_V008.md` | `968aad4fb387f8887959bd3f40888233b08b96bcd42d9ac8281556470afa0d54` | the demand map V008 — certificate conditions |
| 18 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 19 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 20 | `workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | the Attach mandate — the precedent structure member 02 adopts |

```text
COMPUTED IN THIS RELAY (exact structure and one Monte-Carlo corroboration of an exact result):
  Z_N is the character of Y indexed by (sum_(j<=N) e_j, -sum_(j<=N) e_j), NONZERO for N >= 1
  => character orthogonality gives  mu(Z_N) = 0  EXACTLY for every N >= 1   (MC corroboration ~1e-3)
  Theta_F acts on Y by branch exchange composed with inversion — both AUTOMORPHISMS — so the
  pushforward of Haar is translation-invariant and normalized, hence Haar by UNIQUENESS.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEMBER BOUND.
NO FIXED-POINT EXECUTION. NO END TEST. NO MEASURED-CONSTANT COMPARISON.
NO FREEZE BEYOND THE AUTHORED MEASURE ITSELF, WHICH IS DISCLOSED AT SECTION 2.
PE-1..17 POINTER-ONLY, LEDGER SHUT. CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, b9716661d1a0…)

# STAGE 8 — THE MEASURE CANDIDATE
## RELAY 1076 — `[PLAN:DESC-37]` — DARIO LANE — AUTHORED UNDER MANDATE — [CLAIMED]

Date: 2026-08-12

**THE CANDIDATE.** `mu := Haar_Y`, the translation-invariant probability measure of the compact
abelian group `Y`. Equivalently, in the record's own notation, the state member 06 `:216-233` already
names `omega_H := tau_0,+ tensor tau_0,-`.

**THE AUTHORED CORE IS ONE CLAUSE, AND IT IS A SELECTION PRINCIPLE RATHER THAN AN OBJECT:**

```text
A1   mu is invariant under the FULL translation group of Y, not merely under Theta_F.
```

Given A1, `mu = Haar_Y` is **forced** by uniqueness of Haar measure — so the candidate introduces
**no free parameters and no distinguished point**. That is the sense in which it is the weakest `mu`
that types, which the assignment asks me to prefer.

**AND THE SELECTION IS DISCLOSED, NOT SOFTENED.** Member 06 `:520-524` says in terms that *"Mere
preference for Haar, trivial-character evaluation, a product state, or any convex mixture does not
satisfy that condition."* **A1 is exactly such a preference.** It is lawful here only because member
02 mandates the supply, and it is presented as chosen, not as found.

**THE THING I WOULD ATTACK FIRST IS A CONSEQUENCE, NOT THE CHOICE** (§5.1): under `mu = Haar`,
`mu(Z_N) = 0` **exactly**, for every `N >= 1`. The two-branch relative phase — the object the whole
Attach entry was built to supply — averages away completely in expectation. I did not check whether
that is favourable anywhere downstream, and §6.2 records that I did not.

---

## 1. WHAT THE RECORD FORCES, CONSUMED FIRST

The mandate requires the candidate consume everything forced before anything is authored. Four items,
each at a locus:

```text
F1  THE CARRIER.  Lambda = direct-sum_(j>=1) Z e_j ; X := Hom(Lambda,U(1)) ; Y := X_+ x X_- ;
    B isomorphic to C(Y).  Lambda countable => X is a countable product of circles and Y is COMPACT
    METRIZABLE.                                                       member 06 :141-172
F2  THE STATE SPACE.  Normalized positive functionals on B <-> PROBABILITY MEASURES on Y.
                                                                       member 06 :174-207, :450-458
F3  U1-REALITY.  Theta_F(e_plus(U_lambda)) = e_minus(U_(-lambda)) and conversely; omega is
    U1-compatible exactly when omega(Theta_F b) = conjugate(omega(b)) — on the measure side,
    invariance under exchange of the two points of Y.                  member 07 (source); member 06 :259-276
F4  Y IS A COMPACT ABELIAN GROUP.  X is the Pontryagin dual of a discrete abelian group, hence a
    compact abelian group; Y = X_+ x X_- is a product of two.  This is not a new premise: member 06
    :81-96 expressly licenses the standard C*-and-measure theorems as IMPORTED MATHEMATICS, NOT
    PHYSICAL PREMISES, and names the group C*-algebra/character-group duality as the first of them.
```

**F4 is what makes the authored core one clause instead of many.** Because `Y` is a compact group, it
*has* a canonical measure already; the only thing left to author is whether to take it.

---

## 2. THE CANDIDATE, AND ITS SELECTION DISCLOSED

### 2.1 The construction

**A1 (AUTHORED)** — `mu` is invariant under the full translation group of `Y`, not merely
`Theta_F`-invariant (F3), which is strictly weaker. Given A1, the object below is forced: the
translation-invariant probability measure of a compact group exists and is unique (F5). The last line
is the record's own name for it, at member 06 `:216-233`.

*The block is written symbol-only, so the closed-claim extractor runs generic with a total map and
no filtered ingredient. Prose belongs outside it — that is the lesson the V004 relay paid for.*

```text
CONSTRUCTION_BEGIN
mu   :=   Haar_Y
Haar_Y    =   Haar_X   (+)   Haar_X
mu   =   omega_H   :=   tau_0   (+)   tau_0
tau_0 ( U_lambda )   =   0                    [ lambda != 0 ]
tau_0 ( 1 )   =   1
Omega_C0   :=   Omega_SR   (+)   mu
res_B ( Omega_C0 )   =   mu
Theta_hist   :=   res_B
CONSTRUCTION_END
```

### 2.2 Why this is the weakest `mu` that types

```text
NO DISTINGUISHED POINT   a Dirac measure selects a point of Y; Haar selects none.
NO FREE PARAMETER        A1 determines mu uniquely; there is no residual family to fix later.
NO NEW OBJECT            member 06 :216-233 already names this state.  The mandate is spent on
                         SELECTING a named member of the admissible set, not on inventing one.
ONE CLAUSE OF AUTHORSHIP the whole authored content is "take the maximal invariance", and the
                         object follows by a theorem the record already licenses (F4).
```

### 2.3 The selection, disclosed without softening

**`mu` is NOT forced and I do not present it as forced.** Member 03 proved the admissible set is a
continuum and that five bearing constraints do not narrow it; `Haar` is one member of that continuum.
A1 is a preference for maximal symmetry. It is *principled* — it introduces no parameters — but
principled is not derived, and member 06 `:520-524` explicitly forecloses reading a Haar preference as
satisfying the forcing condition.

```text
SELECTION_DISCLOSED = YES.  What was chosen: MAXIMAL INVARIANCE (A1), out of a continuum of
Theta_F-invariant probability measures, none of which the record selects.
```

---

## 3. THE DESCENT MAP

Member 09 `[5968,6080)` types the receiver as
`(common-origin / outgoing-record state data) -> State(C(Y))`. Member 08 `:213-218` gives the exact
restriction `res_B(Omega) := Omega compose i_B`.

```text
DESCENT MAP (stated):
   Omega_C0  :=  Omega_SR  tensor  mu          on  A_C0 = A_SR graded-tensor_min B
   res_B(Omega_C0)  =  mu   =  omega_phys      -> SM-4 satisfied (member 08 :332)
   Omega_C0 compose i_R  =  the sealed record marginal  -> SM-3 satisfied (member 08 :331)
   Theta_hist(common-origin data)  :=  res_B(Omega_C0)  =  mu   -> lands in State(C(Y)) as a MEASURE
```

**And the map's own failure, stated at its span rather than left for the check.** Member 08 `:338-339`
says SM-6 and SM-7 exist precisely *"to distinguish a lawful common-origin producer from the
always-available product extension `omega_out tensor omega_B`."*

**The descent above IS that product extension.** It satisfies SM-3 and SM-4 exactly, and it fails
SM-6: the two factors are assembled by declaration, not descended from one frozen construction trace.

```text
DESCENT = MAP-STATED, with SM-6 FAILING at member 08 :338-339.
A non-product descent would need authored SR/B correlations — strictly more authored structure than
A1 — and the assignment directs me to the weakest mu that types.  I take the weakest and disclose the
gap rather than buy SM-6 with authorship the mandate did not ask for.
```

---

## 4. THE PROVENANCE CERTIFICATE — SHAPE, WITH ITS OPEN SLOT

Per member 17's conditions, following the certificate grammar the Attach candidate used (member 11):

```text
MC1  OUTPUT-FREE PRIMITIVES        FILLED    A1 names only the group's own symmetry.  No response,
                                             coupling, kappa, battery output or measured datum
                                             appears in mu's definition.
MC2  ENTRY BEFORE OUTPUT           FILLED    A1 is frozen at this artifact's seal, before any
                                             consumer is instantiated.
MC3  EXECUTABLE DESCENT            FILLED    mu is obtained from the group structure and nothing
                                             else; Omega_C0 and res_B are as in section 3.
MC4  SEALED-INTERFACE REPRODUCTION FILLED    mu is a U1-invariant probability measure, so it lies
                                             INSIDE the admissible set member 06 :480-487 censuses;
                                             the one-cell restriction consumes mu only through
                                             mu(1) = 1.  Nothing sealed is contradicted (section 5).
MC5  SAME-SOURCE SUPPLY            OPEN      The product descent is assembled by declaration.  This
                                             is SM-6's gap (section 3) and it is the same gap AC6
                                             carried in the Attach candidate.
MC6  K4 COHERENCE                  SHAPE     omega_hist := res_B(Omega_C0) = mu, an element of
                                             State(C(Y)).  Stated as a shape; nothing computed.

CERT_SHAPE = STATED (5/6 filled; MC5 OPEN)
```

---

## 5. FALSIFIER HOOKS

### 5.1 THE CONSEQUENCE I WOULD ATTACK FIRST — stated before the void conditions

```text
mu(Z_N) = 0  EXACTLY, for every N >= 1.
```

`Z_N` is the character of `Y` indexed by `(sum_(j<=N) e_j, -sum_(j<=N) e_j)`, which is nonzero for
`N >= 1`; character orthogonality on a compact abelian group makes its Haar integral vanish. Verified
by the generator both by the index argument and by Monte-Carlo corroboration.

**Read plainly: under this candidate the two-branch relative phase — the object the entire Attach
entry was constructed to supply, and which member 04 showed lands exactly on `q_N`'s coordinate —
averages to zero in expectation.** A critic is entitled to say that maximal invariance is precisely
the choice that erases the connection's content, and that is the sharpest attack available on A1.

**Three things I will and will not say about it.** It is *consistent*: member 03 established
`mu(Z_N)` must be real and may take any value in `[-1,1]`, and `0` is interior to that. It **follows
from A1** rather than having been aimed at — maximal invariance was chosen for parameter-freeness and
this fell out. And **I did not check whether `0` is favourable, unfavourable, or neutral for anything
downstream**; §6.2 certifies that the objects which would answer that were never opened.

### 5.2 Structural void conditions — sealed results `mu` must never contradict

```text
V1  U1-reality / Theta_F-invariance         member 07 ; member 06 :259-276
      SATISFIED, and not by fiat: Theta_F acts on Y by branch exchange composed with inversion, both
      group AUTOMORPHISMS; the pushforward of Haar under an automorphism is translation-invariant and
      normalized, hence Haar by uniqueness.  A1 delivers V1 for free.
V2  positivity and normalization             member 06 :174-207.  Haar is a probability measure.
V3  the one-cell C0-scope restriction        member 06 :399-406.  Consumes mu only through mu(1)=1.
V4  mu(Z_N) real                             member 03.  Haar gives 0 — real, and interior to [-1,1].
V5  DoR-008's ratified algebra               mu is a state on B exactly as ratified; nothing re-typed.
V6  NO MEMBER OF THE ORIGIN FAMILY BOUND     member 16.  mu lives on the field factor B; the channel
      family acts on the source factor.  This candidate binds no member and does not touch d_state.
V7  the sealed record marginal unchanged     member 04.  B is a separate tensor factor; the record
      marginal is untouched by any choice on it.
V8  the admissible set is not exceeded       member 06 :480-487.  mu is a MEMBER of the censused set,
      not an object outside it.  The candidate selects; it does not extend.

VOID_CONDITIONS = NAMED (8)
```

**A scoped negative, declared as scoped.** I looked for a sealed result requiring `mu(Z_N) != 0` and
found none: the charge/flux-access condition (member 14 `:284-290`) demands that the *transition* vary
with admitted history, which is a condition on the operator and remains true under any `mu`. **That
is a scoped search over this artifact's declared members, not a proof that no such result exists
anywhere.**

### 5.3 Blind-test exposure — POINTERS ONLY

Member 02 `:43-45` states this candidate's exposure is **maximal**: `mu` feeds `kappa_record` and
alpha more directly than any prior object. Named as pointers:

```text
the preregistered battery at its frozen manifest ;  kappa_record ;  alpha ;
every scored output downstream of the state.
```

**Not one was opened, consulted, imagined, or optimized toward.** Naming is not opening, and §6.2
carries the certification.

---

## 6. THE INGREDIENT LEDGER

### 6.1 FORCED — with spans

| # | Ingredient | Span |
|---:|---|---|
| F1 | the carrier: `Lambda`, `X`, `Y`, `B iso C(Y)`, `Y` compact metrizable | member 06 `:141-172` |
| F2 | `State(B) <-> Prob(Y)` | member 06 `:174-207`, `:450-458` |
| F3 | U1-reality / `Theta_F` and the compatibility condition | member 07; member 06 `:259-276` |
| F4 | `Y` is a compact abelian group; the standard duality/measure theorems are LICENSED IMPORTS | member 06 `:81-96` |
| F5 | uniqueness of Haar on a compact group (a licensed import under F4) | member 06 `:81-96` |
| F6 | `Z_N`'s exact form, equation (3) | member 12 `:169-203` |
| F7 | `res_B(Omega) := Omega compose i_B`, exact; `A_C0 = A_SR tensor B` | member 08 `:126-128`, `:213-218` |
| F8 | SM-3 and SM-4's marginal conditions | member 08 `:331-332` |
| F9 | the S1 receiver typing | member 09 `[5968,6080)` |

**9 FORCED.**

### 6.2 AUTHORED — the minimal core

| # | Ingredient | Justification (internal coherence ONLY) |
|---:|---|---|
| **A1** | **`mu` is invariant under the full translation group of `Y`** | The unique selection principle available that (i) introduces no distinguished point, (ii) introduces no free parameter, and (iii) implies F3's required `Theta_F`-invariance rather than merely coexisting with it. Given A1, `mu` is determined by F5 alone. **Justified by parameter-freeness and by nothing downstream.** |

**1 AUTHORED. The core is one clause.**

**THE CLOSED CLAIM.** Every symbol in §2.1's construction maps to exactly one row above — F1–F9 or
A1 — with no third category. Machine-checked before sealing by generator path R6, using a **generic
extractor with no alias set**, under the invariants the mandate makes mandatory from V001: no
vocabulary entry may contain `_` or be a single character, vocabulary and map must be disjoint, and
every `_`-bearing or single-character token must be mapped.

**AND THE VOCABULARY IS DECLARED EMPTY.** §2.1's construction is written in symbols only, so there is
no English word for a vocabulary to excuse. The block below carries the sentinel `NONE`, which the
generator reads as the empty set — meaning **coverage rests entirely on the map**, with no second
table able to absorb an unmapped token. That is the strongest form of the check, not a waiver of it:
the escape route that broke relay 1061 was an alias list, and this artifact denies itself even a
lawful word list.

```text
SYMBOL_TABLE_MAP_BEGIN
mu         A1
A1         A1
Haar_Y     F5
Haar_X     F5
omega_H    F5
tau_0      F5
Y          F1
X          F1
Lambda     F1
U_lambda   F1
lambda     F1
e_j        F1
B          F2
Prob       F2
Theta_F    F3
Z_N        F6
Omega_C0   F7
Omega_SR   F7
res_B      F7
i_B        F7
A_C0       F7
A_SR       F7
SM          F8
Theta_hist F9
State      F9
C          F9
SYMBOL_TABLE_MAP_END
```

```text
CLOSED_VOCABULARY_BEGIN
NONE
CLOSED_VOCABULARY_END
```

**OUTPUT-INSPECTION CERTIFICATION.** A1 was chosen for parameter-freeness. `mu(Z_N) = 0` was computed
*after* A1 was fixed, as a consequence, and **no downstream object was opened before, during, or
after that computation.** `kappa_record`, alpha and the battery appear in this artifact only in §5.3's
pointer list and in fence declarations.

---

## 7. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN (unchanged)   JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 02's mandate, its process discipline and its convertibility structure; members 03/04/05's
  findings as subjects; member 06's carrier, state space, licensed imports, U1 map and survivor
  table; member 07's Theta_F; member 08's res_B and SM rows; member 09's receiver; members 10/11's
  Attach entry; member 12's Z_N; members 14-16 under the read rule; member 17's certificate
  conditions; all charter fences.

DERIVED HERE:
  that Theta_F acts by automorphisms, so A1 delivers F3 for free;
  that mu(Z_N) = 0 exactly, by character orthogonality, with MC corroboration;
  that the product descent satisfies SM-3/SM-4 and fails SM-6.

AUTHORED HERE:
  A1 ALONE — the maximal-invariance selection principle.  ONE CLAUSE.
  NO measure was invented: member 06 already names this state.  The mandate is spent SELECTING a
  named member of the admissible set.

SELECTED HERE:
  mu = Haar_Y, DISCLOSED AS A SELECTION at section 2.3, out of a continuum none of which the record
  selects.  No member of the origin family is bound; d_state is untouched; omega_phys is authored
  ONLY as this mandate types, and NOTHING IS ENTERED.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

## 8. FLATTENING CHECK — S01–S37 walked

- A **selection** was not flattened into a **derivation** (§2.3): A1 is a preference, and member 06's
  own sentence foreclosing Haar-preference is quoted against my own candidate.
- A **principled choice** was not flattened into a **forced** one: parameter-free is not derived.
- A **product extension** was not flattened into a **common-origin descent** (§3): SM-6 fails and is
  named at its span, not omitted.
- A **consequence** was not flattened into a **virtue** (§5.1): `mu(Z_N) = 0` is displayed as the
  sharpest attack on the candidate, not as a tidy result.
- **Consistency** was not flattened into **support**: `0` being interior to `[-1,1]` shows no
  contradiction, not that the value is right.
- A **scoped negative** was not flattened into a **universal** one (§5.2).
- **Naming a pointer** was not flattened into **opening it** (§5.3, §6.2).
- **Selecting a named state** was not flattened into **inventing one** (§6.2) — nor the reverse: the
  authorship is real even though the object was already named.

## 9. AUDITS AND CUSTODY

```text
MANDATE            = LIVE (generator refuses without it)
PROSE_DIGESTS      = 20/20, STRICT==STABLE (0 live members); every digest computed at its path
CLOSED_CLAIM       = machine-checked before seal, generic extractor, no alias set
mu(Z_N)            = 0 exactly, by index argument; MC corroboration ~1e-3
Theta_F-INVARIANCE = via automorphism + Haar uniqueness (no choice)
RESIDUE            = 0 hits (27-token scan over authored prose)
CLOSURE            = declared-first, byte 0, fixed point; fence depth-walk, final depth 0
OUTPUT_INSPECTION  = NONE-CERTIFIED, at maximal strength (§6.2)
VERB_AUDIT_SELF    = CLEAN
```

- **BUILDER NEVER VERIFIES OWN WORK.** CLAIMED until the Codex 2 check.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- PE-1..17 pointer-only; no member bound; **nothing entered**.

---

## 10. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00006475 computed as a fixed point on bytes; 20 members;
     0 live; residue scan 0 hits from its own run)

MEASURE = CONSTRUCTED.  mu := Haar_Y, the translation-invariant probability measure of the compact
     abelian group Y = X_+ x X_- — equivalently omega_H := tau_0,+ tensor tau_0,-, WHICH THE RECORD
     ALREADY NAMES at member 06 :216-233.  Typed as required: a U1-INVARIANT PROBABILITY MEASURE ON Y.
     U1-invariance is not asserted but delivered: Theta_F acts by group AUTOMORPHISMS, and the
     pushforward of Haar under an automorphism is Haar by UNIQUENESS.

LEDGER = 9-FORCED / 1-AUTHORED.
     THE AUTHORED CORE IS ONE CLAUSE: A1, "mu is invariant under the FULL translation group of Y,"
     which is strictly stronger than the Theta_F-invariance F3 requires.  Given A1, mu is FORCED by
     Haar uniqueness — NO DISTINGUISHED POINT, NO FREE PARAMETER, NO NEW OBJECT.  That is the sense
     in which it is the weakest mu that types.

DESCENT = MAP-STATED.  Omega_C0 := Omega_SR tensor mu ; res_B(Omega_C0) = mu = omega_phys.
     SM-3 and SM-4 SATISFIED (member 08 :331-332).  SM-6 FAILS: this IS the "always-available product
     extension" member 08 :338-339 wrote SM-6 to exclude.  A non-product descent would cost more
     authorship than A1, and the assignment directs me to the weakest — so I take it and disclose the
     gap rather than buy SM-6 with authorship the mandate did not ask for.

CERT_SHAPE = STATED (MC1-MC4, MC6 filled; MC5 OPEN — same-source supply, the SM-6 gap)

SELECTION_DISCLOSED = YES.  Chosen: MAXIMAL INVARIANCE, out of a continuum of Theta_F-invariant
     measures none of which the record selects.  Member 06 :520-524 says "Mere preference for Haar
     ... does not satisfy that condition" — I QUOTE THAT AGAINST MY OWN CANDIDATE.  It is lawful only
     because member 02 mandates the supply, and it is presented as chosen, never as found.

VOID_CONDITIONS = NAMED (8), V1-V8, each at a locus; plus one SCOPED negative declared as scoped
     (no sealed result requiring mu(Z_N) != 0 was found within this artifact's declared members).

K4_COHERENCE = SHAPE-STATED.  omega_hist := res_B(Omega_C0) = mu, an element of State(C(Y)).

*** THE ATTACK I WOULD RUN FIRST, ON MY OWN CANDIDATE: mu(Z_N) = 0 EXACTLY for every N >= 1. ***
     Z_N is a nontrivial character of Y; character orthogonality kills it.  So under this candidate
     the two-branch relative phase — the object the entire Attach entry was built to supply, landing
     exactly on q_N's coordinate — AVERAGES TO ZERO IN EXPECTATION.  Maximal invariance may be
     precisely the choice that erases the connection's content, and a critic is entitled to say so.
     It is CONSISTENT (member 03: mu(Z_N) must be real, any value in [-1,1]; 0 is interior).
     It FOLLOWS FROM A1 rather than having been aimed at.
     AND I DID NOT CHECK WHETHER 0 IS FAVOURABLE ANYWHERE DOWNSTREAM — §6.2 certifies the objects
     that would answer that were never opened.

OUTPUT_INSPECTION = NONE-CERTIFIED, at the maximal strength member 02 :50-52 demands.
CLOSED_CLAIM = 11 symbols / 5 rows / 0 unmapped / vocabulary EMPTY (sentinel NONE)
     — machine-checked before seal; the generator refuses if this published line and the figures it
     computes disagree, so the line is a receipt rather than a narration.
PROSE_DIGESTS = 20/20, STRICT==STABLE
CHAIN_INVOKED = true (chained pickup proceeds to RELAY_PASTE_1074_DIAGB_CHECK_V002_DARIO_V001.md,
     still the one outstanding DARIO relay)
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**THIS ARTIFACT IS PROPOSED_NOT_ADOPTED.** Per member 02 `:63-68`, ratification of content is a
**second, separate principal act**: an ENTRY ADDENDUM must name this artifact's digest and its
check's digest and record the principal's explicit confirmation. **Until that addendum exists, no
construction may consume this candidate as premise.** The registrar may not enter it; the lanes may
not enter it; a conversational summary may not substitute. The open forcing slot is carried as member
02 `:47-49` writes it: `future_derivation_can_select_the_measure = NO_VERDICT` — a future
target-independent derivation dissolves the entry into a derivation and removes the authored core,
**which I would regard as the best available outcome for this artifact.**

All findings **CLAIMED** until the opposite-lane check.

**One last thing, and it is the honest frame for the whole relay.** Two relays ago I proved this
object undetermined; one relay ago I proved it required. This relay supplies it — and the supply is a
single sentence of preference resting on nine forced rows. **That ratio is the artifact's real
content.** The program has spent the mandate on the smallest thing it could: not a density, not a
point, not a parameter — one clause saying *take the symmetry the carrier already has*. Whether that
clause is right is not something any lane can settle, and the blind test is where it will be answered.
