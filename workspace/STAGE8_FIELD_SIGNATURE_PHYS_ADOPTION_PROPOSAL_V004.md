# FIELD_SIGNATURE_PHYS V004 — FRAME-TANGENT CURRENTS, PROJECTIVE DOOR F, AND BOUNDED RERUNS

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-015 RESERVED)**

**Status:** `V004_COMPLETE_FOR_FINAL_ADVERSARIAL_REVIEW`

**Register head at start:** Q-294, register SHA-256
`1e0ccd83afee6be4651884b5c1fc50f4153a6db1442f6a060877a754ad1a834c`.

**Scope:** the two bounded repairs ordered by the Q-294 final review of V003:
the endpoint-frame tangent determination and the missing projective-limit
class-formation door. V003 survives except where this artifact explicitly
replaces A3/A4/A5/A6, S5/S6, Door F/V/A/B/C, and the corresponding choice-table
and audit statements.

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

---

## 1. Authorities, custody, and roots entered

### 1.1 Frozen authorities

| Authority | SHA-256 | Use |
|---|---|---|
| `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V003.md` | `a5a8420da4878f735553b1cc7870d2e722b0bb039328621070bcb29fc066aaa2` | preserved proposal and the exact framed carrier |
| `STAGE8_FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_REVIEW_DETERMINATION_V001.md` | `6e191e562988180f4a7051ca3601d051cce7bcc7689d9f68cfd1afeffe3274f4` | Q-294 counterexample, repair alternatives, Door-F contract, and rerun list |
| `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V002.md` | `deaa86ee58edb9f841ae3f7bae8ccf9b1cf659328b99fb60cd290a348641e1ad` | six-field baseline and inherited seams |
| `STAGE8_TASK4A_RECORD_SURFACE_TO_PHYSICAL_FIELD_SIGNATURE_DERIVATION_AND_BETA_GAP_ATTACK_V001.md` | `65e4dd6a6e2926c9c100edb162f57352311e73438f23dd19509dda0403e2e4f6` | derived record-shadow components and authored residue |
| `STAGE8_TASK4A_P5_MAXIMAL_TRANSPORT_CHAIN_AND_DEPENDENCE_ACCOUNTING_PACKAGE_V002.md` | `31a738ec17696ea01e1cb6a6ee7a37a29e6c0ca24d6fb8cc050d06aed32a583f` | Q-288 mandatory class-formation fields (`:572-629`) |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | physical quotient, endpoint intertwiner, and raw-G contracts (`:641-688`) |
| `STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md` | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4` | `T_cyl`, finite retractions, and zero-tail argument (`:213-243`) |
| `32_GATE4_DIFFERENTIAL_UNIQUENESS_RESULT_V001.md` | `a0d8b3f71632bd56cc3646fa59e84a2c2776539fadf04be733c7a1eaa997bdbb` | vertex rephasing removes tree phases; loop holonomy alone survives (`:9-31`) |
| `STAGE8_PRPS_GATE4_LOCALIZATION_BRIDGE_ATTEMPT_V001.md` | `f651817ffbfd52caa57be7e63437e2e0682f421c911611151389df8f14579cf7` | exact limit on transporting Gate-4 vertex gauge to all smooth endpoint-frame observables (`:218-232`) |

### 1.2 Roots entered

```text
ROOTS_ENTERED = (
  /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
    alpha_fundamental_record_action_cleanroom_v003,
  /Users/bgm/MB Work/alpha-program-archive/cleanroom_output,
  /Users/bgm/MB Work/alpha-program-archive/workspace,
  /Users/bgm/MB Work/alpha_supervision
)

EXCLUSIONS = (
  a32_holdout/custodian_private,
  every response/value/root/rank-member/background-member evaluation,
  every measure/contour/boundary/domain construction,
  every register/plan/tracker/git/commit/push act
)
```

No occurrence in `a32_holdout/custodian_private/` was opened, listed, or
searched.

### 1.3 Standing split

The inherited record shadow remains:

```text
D1 = rooted signed local write | TYPE-P | premises: DoR-013;
D2 = finite locality, E_post, faithful character covariance, and N<=M
     zero-extension | TYPE-P | premises: DoR-008/009/013;
D3 = finite CTP branch, metric, ordering, and reality grammar
     | TYPE-P | premises: DoR-008;
D4 = separated T_cyl norm completion with natural finite retractions and
     zero common-restriction tail | TYPE-P | premises: DoR-008;
Gate4 = unit-weight covariant incidence modulo vertex rephasing
     | DERIVED_OVER_ENUMERATED_FAMILY.
```

The external field carrier A1-A6 remains authored proposal content. No
authored positive below is marked `TYPE-P`; mathematical conclusions from a
proposed carrier are explicitly `PROPOSAL-CONDITIONAL`.

---

## 2. Lead determination: endpoint-frame tangents are record-visible on the declared carrier

### 2.1 The test the relay ordered

V003 defines, for an edge `e:s->t`,

```text
PT_A(gamma_e)(p_s)=p_t h_e(A,p),
H_N([A,p])=(h_e(A,p))_(e in E_N),
```

and explicitly makes `H_N` the finite record-visibility coordinate
(`V003:282-339`). Under

```text
p_v -> p_v g_v^(-1),
h_e -> g_t h_e g_s^(-1).
```

Fix `A`, fix every frame except `p_t`, and take

```text
p_t(epsilon)=p_t exp(-i epsilon).
```

Then V003's own law gives

```text
h_e(epsilon)=exp(i epsilon)h_e(0),
dH_e/d epsilon|0=i h_e(0) != 0.
```

This is the Q-294 counterexample (`...V003_FINAL_ADVERSARIAL_REVIEW...:88-136`).
It is also the relay's demanded finite-record test: on the carrier V003
declared, a finite realized record coordinate distinguishes the two frame
choices at fixed `A`.

### 2.2 Why Gate 4 cannot turn that answer into boundary gauge here

Gate 4 proves per-vertex rephasing equivalence for its **incidence** data:
tree phases are removable and loop holonomy survives
(`32_GATE4_DIFFERENTIAL_UNIQUENESS_RESULT_V001.md:17-25`). But the sealed
localization-bridge attempt expressly states that the corpus does not prove
that all smooth endpoint-frame changes are exhausted by those vertex-gauge
maps on all comparison observables
(`STAGE8_PRPS_GATE4_LOCALIZATION_BRIDGE_ATTEMPT_V001.md:218-232`).

Therefore declaring the Q-294 tangent gauge-null would use the unbuilt
endpoint-frame-to-vertex-gauge functor as though it already existed. That is
barred by no-unproved-identity transport.

```text
FINITE_DECLARED_RECORD_COORDINATE_DISTINGUISHES_FRAME_CHANGE_AT_FIXED_A = true |
  PROPOSAL-CONDITIONAL THEOREM |
  witness: dH_e/d epsilon=i h_e !=0

ENDPOINT_FRAME_TO_GATE4_VERTEX_GAUGE_EXHAUSTION_PROVED = false | TYPE-U |
  would-build: the sealed bridge's endpoint-frame-to-vertex-gauge functor,
               including exhaustion on all comparison observables

FRAME_TANGENT_STATUS_V004 = RECORD_VISIBLE_AUGMENTED_SOURCE |
  DERIVED_SELECTION_MECHANISM: the declared finite-record observability test

FRAME_TANGENT_BOUNDARY_GAUGE_ROUTE_AVAILABLE_NOW = false | TYPE-U |
  would-build: the same exhaustion functor plus a Ward generator, invariant
               common domain, and completed quotient/restriction square
```

This is not a claim that an endpoint coordinate is an invariant of a future
completed Ward quotient. It is the narrower, current statement: the proposal's
actual finite record map sees it, and the sealed corpus has not licensed its
deletion. The later functor, if proved, would void the augmented-source field
and require a boundary-gauge successor proposal.

### 2.3 Physical meaning in record terms

At fixed parallel transport, an endpoint-frame tangent changes the phase in
which a finite record reports the relation between its source and target line
coordinates. The record-visible datum is therefore the **endpoint comparison
phase**, with two contributions:

1. connection transport along the record path; and
2. the target-minus-source frame coboundary.

The Q-294 witness changes the second while holding the first fixed. V003 kept
that phase in `H_N`; V004 consequently teaches the source family to see both
parts instead of deleting the second.

---

## 3. The exact bounded repair of A3-A6

### 3.1 A1 and A2 are unchanged

V004 preserves V003 A1 and A2-R1 through R11 verbatim. In particular:

```text
X_N=(Conn(P_M) x Fr_N(P,iota))/Gauge_c(P_M),
Fr_N=product_(v in V_N) (P_M)_(iota_N(v)),
```

the complete frame family remains, no member is selected, and the frame
restrictions forget only later vertices.

```text
A1_V004_STATUS = UNCHANGED | MECHANICAL_COMPARISON_WITH_V003_SECTION_5_1
A2_V004_STATUS = UNCHANGED | MECHANICAL_COMPARISON_WITH_V003_SECTION_5_2
FRAME_MEMBER_SELECTED = false | TYPE-S |
  roots: complete Fr_N family and every construction below |
  exclusions: no evaluated frame tuple |
  fences: family discipline |
  query: any distinguished p or global section
```

### 3.2 A3_v004: compatible framed presentations, now formed through Door F

For `M>=N`, define

```text
r_MN:X_M -> X_N
```

by retaining the same connection, restricting to the first `N` realized paths,
and forgetting frames at vertices beyond `V_N`. A2-R6/R8 gives
`r_LN=r_MN r_LM`. V004 does **not** form the compatible family silently:

```text
X_inf^fr := inverse_limit_N (X_N,r_MN)
          = {(x_N)_N in product_N X_N : r_MN(x_M)=x_N for M>=N},
```

with Door F in Section 8.

Let

```text
H=(H_N)_N:X_inf^fr -> inverse_limit_N U(1)^(E_N).
```

The repaired visibility relation remains V003's record relation:

```text
x ~vis y iff H_N(pi_N x)=H_N(pi_N y) for every N,
Conn_fr,vis^004 := X_inf^fr/~vis = image(H).
```

Because `H_N` includes its frame-dependent endpoint comparison coordinates,
this quotient retains the Q-294 frame direction. It removes only the common
kernel of the complete finite coordinate family.

```text
A3_V004_FRAMED_VISIBILITY_PROPOSED = true [PROPOSAL FIELD]
A3_PROJECTIVE_FORMATION_ACCOUNTED_BY_DOOR_F = true |
  PROPOSAL-CONDITIONAL MATHEMATICS
VISIBLE_FINITE_H_COORDINATE_DELETED = false | TYPE-R |
  test: inequality in any H_N prevents equivalence
RESIDUAL_SMOOTH_WARD_DESCENT_BUILT = false | TYPE-U |
  would-build: endpoint-frame/Gate4 exhaustion functor, Ward generator,
               invariant common domain, and full restriction square
```

### 3.3 A4_v004: augmented frame-tangent source currents

Write a tangent to the complete finite frame presentation as

```text
v=(a,theta),
a = connection tangent,
theta=(theta_v)_(v in V_N) = infinitesimal endpoint-frame phases.
```

For every oriented edge `e:s->t`, define

```text
u_e^conn(a) = integral_(gamma_e) a,
b_e(theta)  = theta_t-theta_s,
u_e^aug(v)  = u_e^conn(a)+b_e(theta).
```

The sign is fixed by V003's convention `p_v->p_v g_v^(-1)` and the Q-294
counterexample. Equivalently, on a local logarithm chart,

```text
u_e^aug = -i h_e^(-1) d h_e = d phi_e.
```

Thus the augmented current is not an added dynamics term. It is the exact
logarithmic differential of the already-declared finite record coordinate.

Define the physical visible tangent and source map by

```text
T_vis,x^004 = T_x X_inf^fr / intersection_N ker(dH_N|x),
iota_src^aug(c)=sum_e c_e u_e^aug,
J_a^mu(x)=sum_e c_(a,e) (u_e^aug)^mu(x),
J_phys^004=completion_of_finite_span{u_e^aug} in the proposed A4 norm.
```

The displayed spacetime kernel is still pairing notation, not an edge-to-point
bijection. A global constant frame shift lies in every `b_e` kernel and is
already null in `T_vis^004`. Every nonconstant frame direction seen by some
finite `H_N` is seen by the corresponding `u_e^aug`.

**Separation proof.** If `u_e^aug([v])=0` for every realized edge, then every
component of `dH_N(v)` is zero at every finite `N`. Hence
`v` lies in the quotient kernel and `[v]=0` in `T_vis,x^004`. Conversely, the
Q-294 tangent has `a=0` and `b_e(theta)` nonzero, so it is now detected.

```text
A4_AUGMENTED_SOURCE_PROPOSED = true [PROPOSAL FIELD]
A4_CURRENTS_SEPARATE_REPAIRED_VISIBLE_TANGENT = true |
  PROPOSAL-CONDITIONAL THEOREM
Q294_FRAME_TANGENT_DETECTED_BY_A4_V004 = true |
  PROPOSAL-CONDITIONAL THEOREM |
  witness: u_e^aug(0,theta)=theta_t-theta_s !=0
A4_CONNECTION_ONLY_CURRENT_IS_COMPLETE_ON_FULL_FRAMED_TANGENT = false | TYPE-R |
  test: Q-294 fixed-A endpoint-frame tangent
A4_FRAME_NORM_FORCED_BY_INCIDENCE = false | TYPE-U |
  would-build: an independent source-normalization theorem over the complete
               proposed augmented family
```

### 3.4 A5_v004: exact phase differential

The bounded algebra remains the V003 family-wide algebra generated by

```text
f(H_N([A,p])), f in C(U(1)^(E_N)).
```

In any retained nonzero `Log_0` chart, with reference held fixed,

```text
phi_e([A,p];[A_0,p_0])
  = -i Log_0(h_e(A,p)h_e(A_0,p_0)^(-1)).
```

Differentiating on the actual full framed tangent now gives

```text
d phi_e(v)=u_e^conn(a)+theta_t-theta_s=u_e^aug(v).
```

For the Q-294 counterexample this reads `d phi_e=theta_t-theta_s`, not zero.
For fixed frames it reduces exactly to V003's connection-current identity.
All reference charts and all frame tuples remain; none is selected.

```text
A5_PHASE_DIFFERENTIAL_EQUALS_A4_V004_CURRENT = true |
  PROPOSAL-CONDITIONAL THEOREM
A5_CONNECTION_ONLY_IDENTITY_ON_FULL_FRAME_FAMILY = false | TYPE-R |
  test: Q-294 gives d phi_e != u_e^conn
A5_SCALAR_PHYSICAL_FUNCTIONAL_BUILT = false | TYPE-U |
  would-build: scalarization, measure/contour package, connected subtraction,
               and physical restriction certificate
```

### 3.5 A6_v004: source domain repaired, physical image still open

Replace V003's source argument by the repaired augmented domain:

```text
E_J,phys^004=J_phys^004,
E_R,phys^004=S_1,sym(H_CTP^004),
Bil_phys^004=Bil_cont(J_phys^004 x J_phys^004;C),
RawGClass^004=Bil_phys^004 intersect D3-real symmetric forms.
```

This class is separated on the finite augmented source core. It includes the
connection and endpoint-frame coboundary sectors visible to `H_N`; it includes
no measure, distributional dual, scalar state, dynamics, or raw-G image.

```text
A6_PHYSICAL_SOURCE_DOMAIN_STATEMENT = PASS_AS_PROPOSAL |
  domain: completed finite span of u_e^aug on T_vis^004
A6_ABSTRACT_BILOC_CLASS_SEPARATED = true |
  PROPOSAL-CONDITIONAL MATHEMATICS
A6_RAW_G_PHYSICAL_IMAGE_EXISTS = false | TYPE-U |
  would-build: scalar functional, physical differentiation, connected
               subtraction, lift, restriction square, and Door-D image theorem
```

---

## 4. Endpoint intertwiner and family-wide scalarization regression

For each endpoint frame tuple, the V003 associated-line intertwiner remains

```text
I_(v,p):C_chi -> L_v,
I_(v,p)(z)=[p_v,z],

PT_A(gamma_e) I_(s,p)
  = I_(t,p) multiplication_by(h_e(A,p)).
```

Changing frames changes both intertwiners and the scalar coordinate
equivariantly, so the square still commutes. No member is chosen.

Conditional on A2-R9, each finite `H_N` remains continuous and onto. Pullback
therefore gives an injective isometry

```text
C(U(1)^(E_N)) -> C_b(X_N), f -> f compose H_N.
```

The maps commute with `r_MN`. Door F supplies the previously missing
compatible-family passage, and D4 universality supplies

```text
C_Emb_v004:T_cyl -> Cyl_fr,vis^004.
```

The augmented differential changes none of these bounded maps.

```text
R6_ENDPOINT_INTERTWINER_FAMILY = SURVIVED
C_EMB_V004_REQUIRES_SELECTED_FRAME = false | TYPE-R |
  test: pullback is defined on the complete X_N family
TORSOR_FAMILY_SCALARIZATION = SURVIVED_FAMILY_WIDE
```

---

## 5. Choice table and the frame-tangent price

| Field | V004 proposal | Alternatives considered | Why this member | Void condition |
|---|---|---|---|---|
| A1 | V003 full globally hyperbolic oriented Lorentzian U(1)-bundle family | nonsmooth causal locale; fixed member; Euclidean family | unchanged external carrier family | family empty or raw-G signature mismatch |
| A2 | V003 complete causal/support realization plus full endpoint-frame family | selected section; selected frames; nonscalar groupoid only | retains all frames and chooses none | R1-R10 conflict or family empty |
| A3 | framed finite transports, Door-F projective family, exact all-finite-coordinate visibility quotient | unquotiented connection; loop-only quotient; boundary-gauge quotient | retains exactly what declared finite records distinguish | a finite record coordinate is deleted or a sealed consumer needs common-kernel data |
| A4 | augmented currents `u_e^aug=integral a+theta_t-theta_s` on the full framed tangent | fixed-frame fiberwise derivatives; Ward-first quotient; connection currents only | relay criterion selects it because `H_N` sees frame variation and the Ward exhaustion theorem is TYPE-U | endpoint-frame/Gate4 exhaustion functor proves these directions gauge-null, or continuity/R10 fails |
| A5 | bounded framed holonomy cylinders plus local phases with `d phi_e=u_e^aug` | global affine trivialization; bounded cylinders only | exact differential of existing record coordinate; no new dynamics | chart transition violates D3 or the differential identity fails |
| A6 | continuous bilinear class on repaired augmented source domain | Hilbert-Schmidt; distributional dual; unrestricted algebraic bilinear | minimal separated class; no physical image claimed | finite augmented core fails separation or hidden completion invoked |

The endpoint-frame status is now explicit. It is not a seventh authored
top-level object: it fixes how the already-authored A3 framed visibility data
feeds A4. Its **selection mechanism** is derived from the declared finite
record test, while the A3/A4 carrier remains proposed.

```text
INDEPENDENT_AUTHORED_STRUCTURE_COUNT = 6 | PROPOSAL-LEVEL COUNT
FRAME_TANGENT_CHOICE_DISCLOSED = true | MECHANICAL_AUDIT
FRAME_TANGENT_ROUTE_FROZEN = AUGMENTED_SOURCE_ONLY | MECHANICAL_AUDIT
HIDDEN_SEVENTH_AUTHORED_STRUCTURE_FOUND = false | TYPE-S |
  roots: A1-A6 and generated Door-F/A4 consequences |
  exclusions: no downstream analytic object |
  fences: no principal ratification |
  query: every independent existence clause beyond A1-A6
```

---

## 6. Required seam reruns

### 6.1 S5 — finite labels to augmented physical source kernels

The repaired map is

```text
(a,c) -> J_a^mu(x)=sum_e c_(a,e)(u_e^aug)^mu(x).
```

Finite recovery reads the coefficient of each `u_e^aug`. The family separates
`T_vis^004` by Section 3.3. The map remains a supported current/kernel map,
not an edge-to-one-point bijection.

```text
S5_SOURCE_KERNEL_AND_VISIBLE_SEPARATION = PASS_AS_PROPOSAL |
  premises: A2-R10, A3_v004, A4_v004
S5_EDGE_LABEL_IS_ONE_SPACETIME_POINT = false | TYPE-R |
  test: augmented edge current has path and endpoint support, not one point
```

### 6.2 S6 — zero-extension on the repaired scope

For `M>=N`, A2-R6/R8 preserves the old paths and frames. Therefore

```text
r_MN^* u_e^aug = u_e^aug for e in E_N,
```

and later edge currents are appended without changing earlier coefficients.
Thus the finite coefficient injections commute with the A4 map on the full
repaired visible source span.

```text
S6_ZERO_EXTENSION_TO_AUGMENTED_SOURCE_RIGGING = PASS_AS_PROPOSAL
S6_SCOPE = finite augmented edge-current span and its declared A4 norm completion
S6_FULL_RAW_G_OR_ANALYTIC_DOMAIN = TYPE-S |
  scope: S6 ends at the augmented source rigging; no measure, contour,
         unbounded domain, or raw-G image is formed
```

### 6.3 Other seams regression

```text
S1_ORIENTATION_TO_CAUSAL_PATH = SURVIVED_CONDITIONALLY
S2_INCIDENCE_TO_SUPPORT = SURVIVED_CONDITIONALLY
S3_FRAMED_TRANSPORT = SURVIVED_CONDITIONALLY_FAMILY_WIDE
S4_CTP_REALITY = SURVIVED_CONDITIONALLY
S7_TCYL_PULLBACK = SURVIVED_CONDITIONALLY_THROUGH_DOOR_F
```

No unchanged seam was reopened beyond verifying that its input/output types
still match the repaired A3-A6 interfaces.

---

## 7. Door D0 recheck

Let `r_N i_N=id`, each `r_N` contractive, and finite cylinders dense in the D4
norm. If every `r_N x=0`, choose `y=i_N y_N` with `||x-y||<epsilon`. Then

```text
||y||=||r_N y||=||r_N(y-x)||<=||y-x||<epsilon,
||x||<2 epsilon.
```

Hence `x=0`. The completion is separated and has zero common finite-restriction
tail. No weak-star, bidual, distributional, or nonseparating operation occurs.

```text
CLASS_FORMATION_DOOR_D0 := (
  input_class=finite cylinder union,
  input_topology=D4 finite cylinder norm,
  input_restrictions=contractive r_N with r_N i_N=id,
  formation_or_completion_operation=norm completion,
  output_class=T_cyl,
  output_topology=C-star/module norm,
  output_restrictions=continuous extensions of r_N,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output=intersection_N ker(r_N)={0},
  created_tail_image=zero by the epsilon proof above,
  class_separation_proved=true,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true by norm completion,
  restriction_square=PASS,
  Q279_full_tuple_reproduced=TYPE-U |
    would-build: physical raw-G image and full restriction tuple,
  common_origin_provenance=TYPE-P | premises: DoR-008 for D4 only,
  target_independence=PASS,
  kernel=zero common-restriction kernel,
  image=dense finite cylinder image,
  sector_transfers=none,
  Tail_R_action=NOT_REACHED,
  door_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL
).
```

---

## 8. Door F — finite framed system to compatible projective carrier

This is the class formation V003 omitted (`Q-294 review:327-369`).

### 8.1 Construction and projection theorem

Give every finite `X_N` its quotient topology from
`Conn(P_M) x Fr_N` modulo diagonal `Gauge_c`. The transition `r_MN` is
continuous because it is induced by the continuous frame-forgetting and
finite-path restriction map. Define `X_inf^fr` as the exact equalizer inside
the product, with the subspace/projective topology generated by the
projections `pi_N`.

Two compatible families with the same every finite projection are identical
componentwise. Therefore the common finite-projection kernel is zero.

Conditional on A2 nonemptiness and R6/R8, each admitted finite presentation
extends along the directed system: retain the same global connection and
choose arbitrary points only in the newly appearing nonempty principal
fibers. This proves finite-projection surjectivity over the **whole** frame
family without choosing a distinguished coherent section. Different
extensions remain different family members.

### 8.2 Complete Q-288 door block

```text
CLASS_FORMATION_DOOR_F := (
  input_class=directed family {X_N=(Conn(P_M) x Fr_N)/Gauge_c},
  input_topology=finite quotient topology on every X_N,
  input_restrictions=continuous r_MN retaining A and old paths while forgetting later frames,

  formation_or_completion_operation=exact inverse/projective limit equalizer,
  output_class=X_inf^fr={(x_N):r_MN(x_M)=x_N},
  output_topology=subspace topology from product_N X_N, equivalently projective initial topology,
  output_restrictions=finite projections pi_N:X_inf^fr->X_N,

  topology_changed=true,
  every_limit_named=true,
  limit_topology=projective/subspace-product topology,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,

  Tail_output=intersection_N ker(pi_N)={0} in the separated-family sense,
  created_tail_image=zero -- equality of every finite projection is equality of families,
  class_separation_proved=true componentwise,
  quotient_exactness_proved=NOT_APPLICABLE -- this door is an equalizer, not a quotient,
  closure_uniqueness_proved=true by the inverse-limit universal property; no closure adjoined,

  projection_surjectivity=PASS_AS_PROPOSAL |
    premises: A2 family nonempty and R6/R8 extendability,
  restriction_square=PASS_AS_PROPOSAL |
    equation: pi_N = r_MN compose pi_M,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: physical raw-G image and complete Q279 restriction tuple,
  common_origin_provenance=false | TYPE-U |
    would-build: ratified A1/A2/A3 physical descent witness,
  target_independence=PASS,

  kernel=zero common finite-projection kernel,
  image=the compatibility equalizer inside product_N X_N,
  sector_transfers=connection and endpoint-frame sectors assembled componentwise; none mixed,
  Tail_R_action=NOT_REACHED | NO_VERDICT,

  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and physical provenance
).
```

Door F can add compatible infinite presentations, but it cannot add an object
invisible to every finite projection. This is the exact distinction between
an inverse-limit object and a created tail.

```text
DOOR_F_TAIL_SAFE = true | PROPOSAL-CONDITIONAL THEOREM
DOOR_F_CREATES_TAIL_R_CONTENT = NO_VERDICT |
  prerequisite: Tail_R and the physical response lift are not constructed
```

---

## 9. Door V — compatible carrier to declared record-visible quotient

```text
CLASS_FORMATION_DOOR_V := (
  input_class=X_inf^fr from Door F,
  input_topology=Door-F projective topology,
  input_restrictions=pi_N followed by H_N,
  formation_or_completion_operation=exact quotient by equality of all finite H_N,
  output_class=Conn_fr,vis^004 identified with image(H),
  output_topology=subspace projective finite-coordinate topology,
  output_restrictions=finite H_N coordinates,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=NOT_APPLICABLE -- no closure adjoined,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} relative to all finite H_N by construction,
  created_tail_image=zero -- the quotient deletes the common kernel,
  class_separation_proved=true by the induced injection into image(H),
  quotient_exactness_proved=true by definition,
  closure_uniqueness_proved=NOT_APPLICABLE,
  restriction_square=PASS_AS_PROPOSAL through Door F and A2-R6/R8,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: raw-G physical image and complete restriction tuple,
  common_origin_provenance=false | TYPE-U |
    would-build: ratified A1/A2/A3 descent witness,
  target_independence=PASS,
  kernel=intersection_N ker(H_N compose pi_N),
  image=image(H),
  sector_transfers=connection/frame tangent contributions combine only through dH,
  Tail_R_action=NOT_REACHED | NO_VERDICT,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and provenance
).
```

The frame tangent is **not** in Door V's kernel when the Q-294 witness makes
`dH_e` nonzero. This is consistent with the augmented-source determination.

---

## 10. Door A composition through Door F

The finite pullback squares are

```text
C(U(1)^(E_N)) --H_N^*--> C_b(X_N)
       |                         |
       v                         v
C(U(1)^(E_M)) --H_M^*--> C_b(X_M),
```

with the left arrow the D4 zero-extension/cylinder inclusion and the right
arrow pullback by `r_MN`. Their commutativity is the naturality identity
`H_N r_MN = restriction_E,N H_M`. Door F assembles the right-hand finite
family; D4 assembles the left. Therefore `C_Emb_v004` is the unique compatible
norm map. No frame member is evaluated.

```text
CLASS_FORMATION_DOOR_A := (
  input_class=T_cyl separated norm C-star/module cylinder class,
  input_topology=C-star/module norm,
  input_restrictions=D4 finite retractions,
  formation_or_completion_operation=family-wide compatible holonomy pullback through Door F,
  output_class=bounded framed visibility cylinders on Conn_fr,vis^004,+ x Conn_fr,vis^004,-,
  output_topology=sup norm,
  output_restrictions=finite H_N pullbacks,
  topology_changed=false,
  every_limit_named=true,
  limit_topology=norm/projective compatibility through Door F,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on A2-R9 and Door F,
  created_tail_image=zero by injective isometry and Door-F separation,
  class_separation_proved=true conditional on A2-R9,
  quotient_exactness_proved=true through Door V,
  closure_uniqueness_proved=true by D4 and inverse-limit universal properties,
  restriction_square=PASS_AS_PROPOSAL by the displayed naturality square,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: raw-G image and complete physical restriction tuple,
  common_origin_provenance=false | TYPE-U |
    would-build: ratified source-record-field descent witness,
  target_independence=PASS,
  kernel=zero conditional on finite H_N surjectivity,
  image=compatible bounded framed holonomy cylinders,
  sector_transfers=none; frame covariance retained in H_N coordinates,
  Tail_R_action=NOT_REACHED | NO_VERDICT,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015, full Ward descent, and provenance
).

DOOR_A_COMPOSITION_THROUGH_DOOR_F = PASS_AS_PROPOSAL
```

---

## 11. Doors B and C on the repaired physical-source domain

### 11.1 Door B

Let `J_fin^aug=span_fin{u_e^aug}`. The A4 norm is defined so this finite span
is dense in `H_1^004`. If an output is killed by every finite coefficient
projection, the same epsilon/contractivity argument as D0 makes it zero.

```text
CLASS_FORMATION_DOOR_B := (
  input_class=J_fin^aug,
  input_topology=declared A4 finite frame norm,
  input_restrictions=finite coefficient maps rho_J,N,
  formation_or_completion_operation=Hilbert norm completion,
  output_class=H_1^004 and doubled H_CTP^004,
  output_topology=Hilbert norm,
  output_restrictions=continuous finite coefficient maps,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=Hilbert norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} by dense finite augmented span,
  created_tail_image=zero by norm density,
  class_separation_proved=true,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true by Hilbert completion,
  restriction_square=PASS_AS_PROPOSAL through S6,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: physical raw-G image and full tuple run,
  common_origin_provenance=false | TYPE-U |
    would-build: ratified A4 descent and common-origin witness,
  target_independence=PASS,
  kernel=zero common finite-coordinate kernel,
  image=dense completed augmented source span,
  sector_transfers=connection and frame-coboundary contributions combined within each u_e^aug,
  Tail_R_action=NOT_REACHED | NO_VERDICT,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and provenance
).
```

### 11.2 Door C

Finite-rank symmetric operators on `H_CTP^004` are trace-norm dense in the
declared trace-class source. The finite corner restrictions commute with S6.

```text
CLASS_FORMATION_DOOR_C := (
  input_class=finite-rank symmetric operators on H_CTP^004,
  input_topology=trace norm,
  input_restrictions=finite matrix corners,
  formation_or_completion_operation=trace-norm completion,
  output_class=S_1,sym(H_CTP^004),
  output_topology=trace norm,
  output_restrictions=continuous finite corners,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=trace norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} by finite-rank density,
  created_tail_image=zero,
  class_separation_proved=true,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true by trace-norm completion,
  restriction_square=PASS_AS_PROPOSAL through finite corners,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: physical raw-G image and full tuple run,
  common_origin_provenance=false | TYPE-U |
    would-build: ratified A4/A6 common-origin descent,
  target_independence=PASS,
  kernel=zero common finite-corner kernel,
  image=trace-class symmetric source carrier,
  sector_transfers=none beyond declared CTP doubling,
  Tail_R_action=NOT_REACHED | NO_VERDICT,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and provenance
).
```

```text
DOORS_B_C_PHYSICAL_SOURCE_APPLICATION = PASS_AS_PROPOSAL_ON_AUGMENTED_DOMAIN
DOORS_B_C_PHYSICAL_PROVENANCE_BUILT = false | TYPE-U |
  would-build: ratified A4/A6 descent and common-origin witness
```

---

## 12. Door D and the physical-image boundary

The continuous bilinear class on the separated augmented source domain is
well defined. No physical raw-G element has been placed in it.

```text
CLASS_FORMATION_DOOR_D := (
  input_class=finite continuous bilinear forms on J_fin^aug x J_fin^aug,
  input_topology=bounded-bilinear norm induced by A4,
  input_restrictions=finite source corners,
  formation_or_completion_operation=continuous-bilinear class declaration; no physical image map executed,
  output_class=RawGClass^004,
  output_topology=bounded-bilinear norm,
  output_restrictions=finite bilinear corners,
  topology_changed=false,
  every_limit_named=true,
  limit_topology=NOT_APPLICABLE for the absent physical image,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} for the abstract finite-core-separated class,
  created_tail_image=zero for the class declaration; physical image NO_VERDICT,
  class_separation_proved=true abstractly,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=NOT_APPLICABLE,
  restriction_square=false | TYPE-U |
    would-build: scalar physical functional, derivatives, connected subtraction, and raw-G lift,
  Q279_full_tuple_reproduced=false | TYPE-U |
    would-build: the same physical image and restriction execution,
  common_origin_provenance=false | TYPE-U |
    would-build: physical raw-G descent witness,
  target_independence=PASS,
  kernel=NO_VERDICT for absent physical image map,
  image=NO_VERDICT for absent physical image map,
  sector_transfers=NO_VERDICT,
  Tail_R_action=NO_VERDICT,
  door_verdict=UNDECIDED_TYPE_U
).

DOOR_D_ABSTRACT_CLASS_SEPARATED = true | PROPOSAL-CONDITIONAL MATHEMATICS
DOOR_D_PHYSICAL_IMAGE = NO_VERDICT |
  prerequisite: scalar physical functional, completed physical quotient,
                raw-G lift, restrictions, and common-origin certificate
```

---

## 13. Bounded rerun matrix

| Required Q-294 rerun | V004 result | Exact basis |
|---|---|---|
| A4 separation | `PASS_AS_PROPOSAL` | `u_e^aug=-i h_e^-1 dh_e`; common kernel is exactly the visible tangent quotient |
| A5 phase differential | `PASS_AS_PROPOSAL` | `d phi_e=u_e^conn+theta_t-theta_s=u_e^aug` |
| A6 physical-source domain | `PASS_AS_PROPOSAL` for the abstract class | source arguments are the completed augmented finite span; physical raw-G image remains TYPE-U |
| S5 | `PASS_AS_PROPOSAL` | finite labels map to augmented edge-current kernels and separate the repaired quotient |
| S6 | `PASS_AS_PROPOSAL` on the declared augmented span | frame restriction and zero-extension preserve old edge coboundaries |
| Door B physical-source application | `PASS_AS_PROPOSAL` | Hilbert completion of dense augmented finite span, zero tail |
| Door C physical-source application | `PASS_AS_PROPOSAL` | trace-norm completion of finite corners, zero tail |
| Door A through Door F | `PASS_AS_PROPOSAL` | finite pullback naturality + inverse-limit universal property + D4 norm universality |
| choice table completeness | `PASS` | endpoint-frame status now a separate explicit A4 row with alternatives and void condition |
| unflagged-class-formation scan | `PASS` | D0, F, V, A, B, C, D each has a full Q-288 block |

The reruns do not imply A1-A6 exist physically. They establish consistency of
the proposal conditional on its six authored structures and inherited
premises.

---

## 14. Regression and kill-passes

### 14.1 Six-count and family discipline

```text
V004_INDEPENDENT_AUTHORED_STRUCTURE_COUNT = 6 | PROPOSAL-LEVEL COUNT
V004_SELECTS_FRAME_MEMBER = false | TYPE-S |
  roots: A2 full family, A3, A4, A5, C_Emb, and every door |
  exclusions: no evaluated representative |
  fences: family discipline |
  query: selected frame/member/section
```

Augmenting the source does not add a carrier: it differentiates the already
authored `H_N` on the tangent V003 already declared.

### 14.2 Quotient separation and record-visible witness

The induced total map

```text
Hbar:Conn_fr,vis^004 -> image(H)
```

is injective by definition. A fixed-frame connection mutation with nonzero
path integral survives. The Q-294 fixed-connection frame mutation also
survives and is now detected by `u_e^aug`.

```text
REPAIRED_QUOTIENT_SEPARATED = true | PROPOSAL-CONDITIONAL MATHEMATICS
RECORD_VISIBLE_CONNECTION_WITNESS_PRESERVED = true |
  PROPOSAL-CONDITIONAL MATHEMATICS
RECORD_VISIBLE_FRAME_WITNESS_PRESERVED = true |
  PROPOSAL-CONDITIONAL MATHEMATICS
```

### 14.3 D0 and Door-F zero-tail

Both zero-tail proofs were rerun. D0 uses norm density and contractive finite
retractions. Door F uses componentwise equality in the exact inverse limit.
Neither invokes a forbidden completion.

### 14.4 Unproved bridge not smuggled

V004 does not identify smooth endpoint-frame redundancy with Gate-4 vertex
gauge. The sealed bridge leaves that identity `TYPE-U`. The full physical Ward
operator and domain remain `TYPE-U`.

### 14.5 No target tuning or forbidden operation

No A1-A6 membership predicate reads a response output, `p_ch`, a target
residual, or a measured constant. No measure, contour, unbounded domain,
stationary background, physical raw-G image, response, root, or coupling is
constructed or evaluated.

```text
TARGET_AWARE_MEMBERSHIP_PREDICATE_FOUND = false | TYPE-S |
  roots: A1-A6, choice table, seams, and Doors D0/F/V/A/B/C/D |
  exclusions: downstream boundary statements |
  fences: no response/value evaluation |
  query: p_ch; alpha; stiffness; response value; residual value; measured target

WEAK_STAR_BIDUAL_DISTRIBUTIONAL_OR_NONSEPARATING_STEP_FOUND = false | TYPE-S |
  roots: every class-forming operation in V004 |
  exclusions: operations named only as unbuilt alternatives |
  fences: Q-288 door schema |
  query: weak-star; bidual; distributional completion; nonseparating extension

UNFLAGGED_CLASS_FORMATION_STEP_FOUND = false | TYPE-S |
  roots: every quotient, inverse limit, pullback, norm completion, trace
         completion, and bilinear class declaration in V004 |
  exclusions: no operation named only as TYPE-U |
  fences: Q-288 mandatory schema |
  query: all class-forming arrows against Doors D0/F/V/A/B/C/D
```

---

## 15. Preserved limits and final proposal verdict

V004 still does not supply:

```text
PHYSICAL_SCALAR_FUNCTIONAL_BUILT = false | TYPE-U |
  would-build: scalarization on the proposed algebra with state/measure provenance
MEASURE_OPERATION_BUILT = false | TYPE-U |
  would-build: invariant descended measure and convolution pairing
CONTOUR_OPERATION_BUILT = false | TYPE-U |
  would-build: interacting prescription and named boundary-value topology
BOUNDARY_CONTACT_OPERATION_BUILT = false | TYPE-U |
  would-build: physical contact terms, boundary form, and gluing theorem
DOMAIN_CLOSURE_OPERATION_BUILT = false | TYPE-U |
  would-build: common unbounded domains and endpoint compatibility
WARD_DESCENT_BUILT = false | TYPE-U |
  would-build: endpoint-frame/Gate4 exhaustion functor, Ward generator,
               invariant common domain, and completed quotient theorem
RAW_G_PHYSICAL_IMAGE_BUILT = false | TYPE-U |
  would-build: physical functional, derivatives, connected subtraction,
               lift, restriction square, and Door-D image theorem
STATIONARY_BACKGROUND_BUILT = false | TYPE-U |
  would-build: STAT_BG_LIFT_FIBER, 2PI blocks, and stationary selector
```

Final block:

```text
FRAME_TANGENT_DETERMINATION = RECORD_VISIBLE_AUGMENTED_SOURCE
FRAME_TANGENT_DETERMINATION_MECHANISM = finite declared record observability
FRAME_TANGENT_BOUNDARY_GAUGE_EXHAUSTION = false | TYPE-U

A4_SEPARATION_RERUN = PASS_AS_PROPOSAL
A5_PHASE_DIFFERENTIAL_RERUN = PASS_AS_PROPOSAL
A6_SOURCE_DOMAIN_RERUN = PASS_AS_PROPOSAL
S5_RERUN = PASS_AS_PROPOSAL
S6_RERUN = PASS_AS_PROPOSAL_ON_DECLARED_SCOPE
DOOR_D0 = CLOSED_SEPARATED_ZERO_CREATED_TAIL
DOOR_F = PROPOSAL_CONDITIONAL_CLOSED_SEPARATED_ZERO_CREATED_TAIL
DOOR_V = PROPOSAL_CONDITIONAL_CLOSED_SEPARATED_ZERO_CREATED_TAIL
DOOR_A = CONDITIONAL_PASS_THROUGH_DOOR_F
DOORS_B_C = CONDITIONAL_PASS_ON_AUGMENTED_SOURCE
DOOR_D_PHYSICAL_IMAGE = NO_VERDICT
CHOICE_TABLE_COMPLETE = true | MECHANICAL_AUDIT
UNFLAGGED_CLASS_FORMATION_STEP_FOUND = false | TYPE-S

OVERALL_PROPOSAL_STATUS = V004_COMPLETE_FOR_FINAL_ADVERSARIAL_REVIEW
DOR_015_PACKAGE_ISSUED = false | TYPE-C |
  constraint: principal ratification and independent final review have not occurred |
  release: V004 survives final adversarial review and principal issues DoR-015
```

At send time the register must be rechecked against Q-294. Any later bearing
ruling supersedes the final custody block before sealing.

```text
REGISTER_HEAD_AT_START = Q-294
REGISTER_HEAD_AT_SEND_TIME = Q-294
REGISTER_SHA256_AT_FINAL_CHECK =
  1e0ccd83afee6be4651884b5c1fc50f4153a6db1442f6a060877a754ad1a834c
LATER_BEARING_RULING_CONSUMED = false | TYPE-S |
  roots: alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md through Q-294 |
  exclusions: no unregistered V004 or DoR-015 draft |
  fences: no cross-lane coordination |
  query: Q-295; FIELD_SIGNATURE_PHYS V004; DoR-015

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

Custody: seal this V004 proposal, verify the sidecar, mirror artifact and
sidecar to `alpha-program-archive/workspace/`, report hashes and exact paths,
and stop. No register, plan, tracker, git, commit, push, gate, or deploy action
is performed by this lane.
