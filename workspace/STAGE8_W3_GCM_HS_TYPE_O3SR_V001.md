# STAGE 8 — W-3: G_cm AT THE CONSUMING (HS) TYPE — THE C-L2 CERTIFICATION ATTEMPTED AT ITS SUPPLY LAYER — O3SR V001

## BUILDER — CODENAME W3-BUILD — COMMISSION O3SR — 2026-08-15

```text
FENCES AS COMMISSIONED AND AS HELD:
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
DETERMINATION / CONSTRUCTION ONLY. No authored physics beyond what a named
sealed premise already licenses. No value. No number offered as a physical
magnitude. No measured-constant comparison. No frozen constant.
Exact symbolic only (CAS: fresh venv, sympy; script and verbatim output
appended to this artifact by file-append, §9).
Sharp localizer M(t) NOT softened, mollified, or replaced (E1 :465-470 /
D6' respected as written). Equal-time HS route NOT resurrected (C6 respected
as FALSE; cited only as an exclusion). No baseline normed. No Phase-1 object
consumed. No cellulation/coarsest-cell datum consumed. No scale, no imported
GR, no faithfulness authority.
NO register / tracker / road / plan / continuation / ledger / lens file read.
"Q-..." register tokens EXPECTED-UNLOCATABLE — not chased.
Output path probed ABSENT before first write. No git action.
THIS ARTIFACT RETIRES NOTHING. Witness retirement is the registrar's act.
```

---

## 0. VERDICT IN ONE LINE

**UNDECIDABLE-TODAY — and the "today" is now shown to be STRUCTURAL ON THE
PERMITTED ROUTE, not a shortfall of effort. G_cm is NOT derived at its
consuming (HS) type. All THREE of-record supply blockers stand; none was
discharged; each was moved in the SAME direction — from *unpassed* to
*provably not passable by the quadratic-form route*, which is C-L2's only
permitted certification route. (d-1) FORM -> HS AT THE CONSUMING SANDWICH is
closed NEGATIVELY by a new derived structural theorem — THE SEA-SANDWICH
OBSTRUCTION: R.3's sandwich C = 1_(-inf,0)(h_0) is a nonzero TRANSLATION-
INVARIANT projection, the certified class H(sqrt3) is a translation-invariant
class CONTAINING a translation-invariant member (h_0 itself), and a nonzero
translation-invariant operator on L^2(R^3) tensor C^4 is NEVER Hilbert-Schmidt
(exact two-line kernel argument, NO regulator, NO formal-Lambda, NO WB7
license — this strictly strengthens the bridge's site-(i) negative, which
consumed the Lambda license, and generalizes it from one witness to the
sandwich itself: the obstruction is a property of C, not of the member).
(d-2) B-L2* is shown DISJOINT from the C-L2 asset's reach: at the sealed
saturation clause the obstruction is the P-RESTRICTED degree-0 term of the
RESUMMED sharp projector, while the C-L2 form certification acts only on the
[error] layer that ACCOMPANIES P — so no strengthening of the form
certification, to any constant and at any grade inside the form class, can
move the stratum resummation; additionally the asset is t-UNIFORM, hence its
full-tau_R time integral contributes exactly the factor tau_R and a
coincidence-gain exponent of EXACTLY ZERO, which is the one quantity the
mechanism needs. (d-3) beta is shown NOT EXTRACTABLE from the asset in
principle: the certified constant 2 sqrt(3) is a functional of the Clifford
algebra alone and is CONSTANT across the admitted cell family, so the map
cell |-> certified bound is constant and determines no exponent; any reading
of beta = 0 off it would be an authored assignment of an R-L2b-reserved
symbol and is REFUSED. A FOURTH obstacle — the certification's own named
H^1-propagation residual — is shown NOT CLOSEABLE at form grade: even
granting a fortiori that the interaction dressings' generator lies in the
certified class, the propagation Gronwall charges ||h_0^2 Omega psi||, an
H^2 quantity, because the bilinear display spends one derivative per slot and
the left slot is already h_0 Omega psi; the form class is NOT CLOSED under its
own propagation Gronwall — the residual escalates rather than closes.
WHAT DID MOVE, and is offered for registration: the AUDITED SHARPER
ENCLOSURES at the wall's finite-rank compressions (the bridge audit's COR-B,
which instructed "register the sharper displays if the enclosures are
consumed" — they ARE consumed here): ||C_n T_g C_n||_2 <= 12 n^{3/2}
sqrt(2n-1)/ell (order n^2, superseding the order-n^{7/2} headline) and
||C_n T_g C_n||_1 <= 24 n^3 sqrt(2n-1)/ell (order n^{7/2}, superseding
order n^5), each re-derived here from the exact carrier form bound
|p|^2 <= (6n-3)/ell^2 (from p^2 + x^2 = 2N+1 on the sealed Hermite carrier),
with the exact factor chain at the executed member 24 sqrt(6) < 192 iff
6 < 64; and the audit's COR-A is carried, so NO order-sharpness is claimed at
HS grade anywhere here. THE DECIDING OBJECT, named exactly and unchanged in
identity but now sharpened in TYPE: a trace/HS-grade rate for the SPECIFIC
C-L2 kernel that BREAKS THE TRANSLATION SYMMETRY the form class preserves —
i.e. an input that consumes the sharp vertex's own spatial localization
rather than class membership — sub-volume AND n-free AND surviving the
stratum resummation (verbatim MO-3 / R-L2b(beta) / B-L2*). W-3 therefore
CANNOT LAND ON C-L2's OWN PERMITTED ROUTE ALONE: obligations (o1)-(o3) of the
clause are certified of record, and (o4) — "Supplies S2b" — is now shown to be
NOT A CONSEQUENCE of (o1)-(o3) at any strength. FP-3 stands. The witness
E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS; this artifact retires
nothing. NOT REFUTED: every ceiling here is one-sided; no divergence of
tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2, of A(0), of E_int, or of any wall
quantity is exhibited; all remain UNDECIDED.**

---

## 1. SEALS VERIFIED AT PATH BEFORE ANY RELIANCE; SWEEP CUTOFF DECLARED

Every sidecar was verified with `shasum -a 256 -c` **run from the artifact's own
directory** (sidecars name basenames; a parent-directory run reports false
failures — the commissioned protocol, held). All under
`/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest below was
also recomputed independently this session. None unverifiable.

```text
afc3a79c646c044d9c34ede25fbc7166d43807c9ac91b64f078900f9309516cd  STAGE8_COMPLETION_MAP_T17SR_V001.md                      OK  [MAP]  commissioning ground
4504cd1448049496f0d5db9828fbed09b51b696cd11d0da1e978ad147964693a  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md                 OK  [MAP-A]
e6838ffcc6f352b7a0cfaad1d51d98544717d3bbe04d413d8b356e752d400a3d  STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001.md                 OK  [CERT]  the C-L2/G_cm run
180114a796473769ee7bb9d041b9a8d9883d9c76ca8db5f78f314ece815a801e  STAGE8_CL2_GCM_CERTIFICATION_S9AD_AUDIT_V001.md           OK  [CERT-A]
9a2954982e4c3b81177e464118720ee0bd264b1a51ebce83b65855858c6a4545  STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md                     OK  [FHB]   the bridge run (located by sweep, §1.2)
27bc36aefb78683c70836aa5c1882d9392dac8caf813b87b71154f470bdc4862  STAGE8_FORM_TO_HS_BRIDGE_S9AD_AUDIT_V001.md               OK  [FHB-A]
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                            OK  [BL2*]  blocker (d-2) at its own bytes
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                              OK  [BL2*-C]
99da8628c1f6ef1d90db89645f8c4a1ed62bd99516c0b60555b3faace0e4a0b8  STAGE8_RL2B_REMAINING_CONTENT_UNDER_THE_CHARTER_V001.md   OK  [RL2B-R] blocker (d-3) at its own bytes
1497d017175c900fb74a75970c569cd3319c77c5814a2f874734c695e8c06491  STAGE8_RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md  OK  [RL2B-O]
80db260fa1561d76296d5f54e1e52397b79009b8a0d12bc060c140818c38fdf7  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                 OK  [WALL]  FP-3's source
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md               OK  [E1]    C-L2 / S2b / R.3 clauses
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md                      OK  [RL0]   the consuming chain's unvalued items
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md                     OK  [CL-ERROR] the [error] layer, exact
```

CROSS-PROVENANCE CHECK (done, not assumed): the six digests that also appear in
CERT §1's seal table (E1, RL0, WALL, CL-ERROR, BL2*, BL2*-C) agree byte-for-byte
with the values recomputed here this session. The CERT consumed the same bytes
this artifact consumes.

### 1.2 SWEEP CUTOFF — DECLARED

```text
SWEEP CUTOFF: 2026-08-15, this session. Roots swept:
  (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace          (primary)
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
SWEEP TERMS (declared, scoped): "G_cm"; "form-to-HS" / "form to HS" /
  "FORM -> HS" / "form-grade"; and the blocker tokens "B-L2*", "R-L2b",
  "beta", "MO-3" restricted to carriers already surfaced by the first two.
EXCLUDED BY COMMISSION, NOT READ: every register / tracker / road / plan /
  continuation / ledger / lens file. "Q-..." tokens EXPECTED-UNLOCATABLE.
THE BRIDGE WAS LOCATED BY THIS SWEEP, NOT BY NAME: the commission gave the
  bridge only by its audit's declared verdict shape ("no full bridge — R.3
  closed at type; F3 given first exact enclosures, n-uniform provably not
  form-implied; layer-1 short of threshold"). The sweep's "form-to-HS" term
  returned STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md + _AUDIT_V001.md, whose
  audit §0 matches that shape at all four clauses (site (i) FAILS-AT
  compression type; site (ii) PARTIAL with first exact enclosures; the FB9
  n-uniform non-implication; site (iii) FAILS-AT the smallness threshold).
  IDENTIFICATION CONFIRMED AT BYTES.
MATERIAL EXHAUSTIVENESS, stated honestly: of the 28 non-excluded carriers
  naming G_cm, every one either (i) defines/positions G_cm (E1, RL0), (ii)
  records it as a standing blocker in a list (the 5d re-reads, the map, the
  naming candidates, the zero-free and remainder attempts), or (iii) is one
  of the two runs commissioned here (CERT, FHB) plus the two blocker
  carriers (BL2*, RL2B). NO carrier outside this set supplies a form-to-HS
  conversion, an HS-typed C-L2 bound, or a value/derivation of beta. That
  negative is the sweep's, and it is bounded by the declared terms above.
```

---

## 2. THE OBJECTS AT THEIR OWN BYTES

### 2.1 FP-3 and W-3, at the commissioning ground's bytes

FP-3 (MAP §1, quoting WBC verbatim):

```text
"the closure's exact identities hold modulo the C-L1 [error]; its only
permitted certification (C-L2 quadratic form) is UNCERTIFIED of record with
the operator-norm route excluded (||[h_0, 1_B]|| = +infinity); the odd
sector's would-be single-composite majorant lives in exactly that layer.
Witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS."
```

W-3 (MAP §3.4, the closure's own discharger for FP-3):

```text
W-3 (discharges FP-3): the C-L2 certification at its consuming type — G_cm
    derived at the consuming (HS) type; "its only permitted certification
    (C-L2 quadratic form)" with "the operator-norm route excluded
    (||[h_0, 1_B]|| = +infinity)"; retirement of witness
    E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED is the registrar's upon its
    landing.
```

MAP §3.5 fixes W-3's SOLENESS, and this artifact does not disturb it: "F3's
certification route is 'only permitted' C-L2 with the operator-norm
alternative 'EXCLUDED of record.'" The minimal completion set {W-1, W-2, W-3}
is UNIQUE at the closure's own bytes. Nothing here proposes an alternative
member, invents a route, or edits the map.

### 2.2 The clause, the definition, the consuming site (E1, via CERT §2, re-read)

```text
C-L2 (E1 :1137-1144): control [h_0, M(t) (x) S] in a QUADRATIC-FORM /
   Besov-type norm; norm-based control unavailable (||[h_0, 1_B]|| =
   +infinity); the artifact must state WHICH form and WHY the operator-norm
   route is excluded. "Supplies S2b."
   => four obligations: (o1) name the form; (o2) control in it, exactly;
      (o3) display the exclusion; (o4) SUPPLY S2b.
S2b (E1 :702-708): G_cm(C, eps) := |C|_4^{-beta} . (certified bound on the
   C-L2 commutator error in the quadratic form C-L2 names); beta derived
   with alpha under R-L2b.
R.3 (E1 :810-813): ||A_s||_2 <= ||A(0)||_2 + ||Delta||_2
                        <= |C|_4^{beta} G_cm + |C|_4^{alpha} G_hs
   — A(0) IS the C-L2 error layer, and THE CONSUMING SITE IS HS-TYPED.
```

STATE OF RECORD ENTERING TONIGHT (CERT §5, audited CONFIRMED-WITH-CORRECTIONS
by CERT-A): (o1), (o2), (o3) **CERTIFIED**; the form bound exact and t-, cell-
and carrier-uniform at the primitive step, with relative form bound **ZERO**
w.r.t. h_0^2; the operator exclusion **mechanism-explicit**. (o4) **NOT
DISCHARGED**, FAILS-AT three named of-record points. G_cm = NOT-DERIVABLE-TODAY
at its consuming type.

### 2.3 THE THREE STANDING POINTS — the targets of this commission, verbatim

```text
(d-1) FORM -> HS GRADE CONVERSION: the wall's F1 (WALL :408-413) is the PROVED
      rank x op ceiling — op/form-grade control times carrier rank cannot close
      a carrier-uniform trace/HS estimate (WB6 = r-3 D10 re-pinned). A
      sub-volume HS rate is verbatim MO-3, open of record.
(d-2) THE TWO-TIME HS ESTIMATE ITSELF: B-L2* — FAILS-AT of record at the
      stratum-resummation step at the sealed saturation clause (6e81ae92 :32,
      check CONFIRMED 2699af25 :23) ... The equal-time shortcut is FALSE of
      record (C6: ||[C, 1_B]||_2 = +infinity, E1 :353-357 ...).
(d-3) THE SCALING EXPONENT beta: a SYMBOL until R-L2b closes (E1 :826-832,
      witness SCAD_HS_SCALING_EXPONENT_UNDERIVED; RL0 u-1 :377-380).
```

And the CERT's own fourth, named inside the form grade (CERT §3.3), which this
commission must also confront because it stands between the GENERATOR and the
LAYER:

```text
(d-0) H^1-PROPAGATION THROUGH THE INTERACTION DRESSINGS: "the INTERACTION
      dressings Omega_lambda(1,t) do not commute with h_0, and commuting h_0
      through them is again a C-L2-layer object (a Gronwall at form grade
      would consume ||[h_0, M(s)(x)S] Omega psi||, an OPERATOR-grade quantity
      — exactly what is excluded)... NOT attempted tonight and NOT named
      anywhere in sealed stock as discharged."
```

### 2.4 What the bridge run already established (FHB + FHB-A, consumed as audited)

The bridge is consumed AT AUDITED GRADE — i.e. with FHB-A's three corrections
applied, not at the build's headline:

```text
FHB site (i) = R.3 = FAILS-AT(compression type): C = 1_(-inf,0)(h_0) is
   INFINITE-RANK and h_0-COMMUTING; the certified form bound survives its
   sandwich VERBATIM (constant 2 sqrt(3) unchanged — FORM-GRADE BRIDGED) and
   creates NO HS decay; h_0 is IN the class and C h_0 C = -|k| C(k) is a
   nonzero translation-invariant multiplier, "NOT Hilbert-Schmidt (formal-
   Lambda display (8 pi/5) Lambda^5 -> +infinity, WB7 license)".
FHB site (ii) = WALL F3 = PARTIAL: first trace/HS enclosures of the modulo-
   layer's GENERATOR at the wall's own compressions, per (n, cell); the
   n-UNIFORM discharge PROVABLY NOT form-implied (FB9); the full LAYER stays
   behind (d-0).
FHB site (iii) = half-plane layer-1 = FAILS-AT(smallness threshold).
FC-1 (correction-grade, audit-VERIFIED REAL): [h_0, 1_B (x) S] =
   i(alpha.n-hat (x) S) delta_{dB} + 2 . 1_B h_0 S — the S-DRESSED commutator
   carries a VOLUME-supported BULK term missing from CERT §3.4(ii); the
   S-LESS [h_0, 1_B] IS pure surface, so E1's exclusion locus is untouched.
FHB-A COR-A: the build's "ORDER-SHARP within the class" claim is FALSE at HS
   grade. FHB-A COR-B: sharper enclosures exist and should be "register[ed]
   ... if the enclosures are consumed". FHB-A COR-C: site (iii)'s pair
   coefficient pi/2 > 1, threshold conclusion survives a fortiori.
```

---

## 3. THE ATTEMPT, STATED BEFORE IT IS MADE

The commission is exact: derive G_cm **at the consuming (HS) type**, on the
**quadratic-form route only**, the operator-norm route being excluded of
record. So the object to be produced is a true inequality of the shape

```text
   || A(0) ||_2  <=  |C|_4^{beta} . G_cm(C, eps)          (R.3's consumable)
```

whose right side is manufactured out of (o1)-(o3) — the named form, the
certified control in it, and nothing operator-normed. Two readings of S2b are
carried, exactly as CERT §2.3 carried them, and BOTH are attempted:

```text
READING A (S2b's own parenthetical, FORM-typed): G_cm = |C|_4^{-beta} . B,
   B the certified form bound. Then |C|_4^{beta} G_cm = B, so R.3 consumes
   ||A(0)||_2 <= B — an inequality between an HS norm and a FORM bound. It is
   true or false only once a conversion exists. §4 decides it.
READING B (R.3's display, HS-typed): G_cm must already be an HS-grade
   quantity. Then the form asset must be CONVERTED before entry. §4 decides
   the conversion at the site's own sandwich; §5 decides the two-time
   estimate; §6 decides the exponent.
```

THE ATTEMPT IS MADE AT THE SITE'S OWN OBJECTS, NOT AT A CONVENIENT PROXY. The
consuming site's sandwich is the one E1 :811-814 writes: `A(0) = C (error
layer) C` with `C = 1_(-inf,0)(h_0)`, the continuum sea projection, symbol
`(I - alpha.k-hat)/2` (r-2 :144-148, as carried by FHB §4.1). The finite-rank
carrier compressions are a DIFFERENT site (the wall's, FHB site (ii)) and are
treated separately in §4.3 — they are not substituted for the consuming site.

The certified asset, named once and used throughout, is the class

```text
   H(c) := { quadratic forms t on Q = D(h_0) = H^1(R^3) (x) C^4 :
             |t[psi, psi]| <= 2c ||h_0 psi|| . ||psi||  for all psi in Q } ,
   c = sqrt(3) ; the C-L2 generator's form q_t lies in H(sqrt3) — CERT §3.2,
   audited HOLDS (CERT-A §4.1), re-grounded independently here (CAS W1).
```

---

## 4. POINT (d-1) — THE CONSUMING SANDWICH: **THE SEA-SANDWICH OBSTRUCTION** [DERIVED]

### 4.1 The theorem

The bridge closed site (i) negatively but paid for it with the formal-Lambda
display and the WB7 license (FHB §4.1: "(8 pi/5) Lambda^5 -> +infinity, WB7
license"). That payment is unnecessary, and removing it generalizes the
result from ONE witness to the SANDWICH ITSELF. The following is derived here.

```text
THEOREM (SEA-SANDWICH OBSTRUCTION) [DERIVED].
Let {U_a}_{a in R^3} be the translation group on L^2(R^3) (x) C^4. Let C be a
bounded operator with [C, U_a] = 0 for every a, and let T be a bounded
operator with [T, U_a] = 0 for every a. If C T C != 0, then C T C is NOT
COMPACT — a fortiori NOT Hilbert-Schmidt, and NOT trace class.

PROOF (two lines, no regulator, no cutoff, no Lambda, standard steps named —
the same license as WALL C-5 / FHB §3).
  C T C commutes with every U_a (a product of commutants).
  Pick psi with (C T C) psi != 0. Then U_a psi -> 0 WEAKLY as |a| -> infinity
  (the L^2 Riemann-Lebesgue statement for the translation group).
  If C T C were compact it would map weakly-null sequences to norm-null ones,
  so ||C T C U_a psi|| -> 0. But C T C U_a psi = U_a (C T C) psi, and U_a is
  unitary, so ||C T C U_a psi|| = ||(C T C) psi|| != 0 for every a — constant,
  not tending to 0. Contradiction. Hence C T C is not compact. []
```

### 4.2 The theorem applied at R.3's own site — the four hypotheses, each verified

```text
(h-1) C IS TRANSLATION-INVARIANT. C = 1_(-inf,0)(h_0) is a spectral
      projection of h_0 = alpha.p, a FOURIER MULTIPLIER; every spectral
      projection of a multiplier is a multiplier; its symbol is the sealed
      (I - alpha.k-hat)/2 (r-2 :144-148). CAS W2b/W2c/W2d confirm it is a
      Hermitian rank-2-per-k projection; CAS W2h confirms multiplier symbols
      are fixed by translation conjugation.                        VERIFIED
(h-2) THE CLASS H(sqrt3) IS TRANSLATION-INVARIANT. Its defining functional
      uses only ||h_0 psi|| and ||psi||; h_0 is a multiplier and U_a is
      unitary, so both are U_a-invariant: t in H(c) => t o U_a in H(c), same
      constant.                                                    VERIFIED (CAS W3c)
(h-3) THE CLASS CONTAINS A TRANSLATION-INVARIANT MEMBER. The form of h_0
      itself, t_{h_0}[psi] := <psi, h_0 psi>, satisfies |t_{h_0}[psi]| <=
      ||h_0 psi|| ||psi|| = 1 . ||h_0 psi|| ||psi|| <= 2 sqrt(3) ||h_0 psi||
      ||psi||: h_0 IS IN H(sqrt3), with exact margin 2 sqrt(3) - 1 > 0.
      (This is FHB's cl-2, re-derived.)                            VERIFIED (CAS W3a/W3b)
(h-4) ITS SANDWICH IS NONZERO. At symbol level, with A := alpha.k-hat,
      A^2 = I and P_- = (I - A)/2, one has A P_- = -P_-, hence
         C h_0 C = |k| P_- A P_- = -|k| P_-  EXACTLY,
      whose trace is -2|k| != 0 for k != 0; the HS density is
      tr(m^dag m) = 2|k|^2 > 0.                                    VERIFIED (CAS W2e/W2f/W2g)
```

```text
COROLLARY (THE SITE'S VERDICT) [DERIVED]:
   sup_{ t in H(sqrt3) } || C T C ||_2  =  +infinity ,
   witnessed EXACTLY by the class member h_0 itself, for which ||C h_0 C||_2
   is not merely large but INFINITE (the sandwich is a nonzero translation-
   invariant operator; §4.1). Therefore NO inequality of the shape
      || C T C ||_2  <=  F( c, cell data, eps )     for all t in H(sqrt3)
   can hold, at ANY finite F. READING A of S2b is FALSE as an inference from
   the certified asset; READING B has no conversion at this site.
   (d-1) IS CLOSED NEGATIVELY AT THE CONSUMING SITE — not "not today".
```

### 4.3 What this strictly adds over the bridge, and what it does not

```text
ADDS (1): THE LICENSE IS REMOVED. FHB's site-(i) negative consumed the
   formal-Lambda display under the WB7 license ((8 pi/5) Lambda^5). The proof
   above uses no cutoff, no regulator, no formal-Lambda, and no WB7: it is a
   compactness/symmetry argument. A negative that previously rested on a
   licensed display now rests on none.
ADDS (2): THE SCOPE IS WIDENED FROM A MEMBER TO THE SANDWICH. FHB exhibited
   ONE bad member (h_0). The theorem shows the obstruction is a property of
   C: ANY translation-invariant sandwich kills ANY translation-invariant
   member's image. In particular the obstruction is INDIFFERENT to the
   constant: replacing 2 sqrt(3) by any smaller constant, or strengthening
   the class in any translation-invariant way, changes nothing.
ADDS (3): IT NAMES THE ESCAPE'S TYPE. The only way past is an input that is
   NOT translation-invariant — i.e. one that consumes the sharp vertex's own
   SPATIAL LOCALIZATION rather than class membership. That is exactly the
   type of MO-3 (a sub-volume rate) and of FHB's sigma_n(r) surface channel.
   The residue was known; its TYPE is now derived: symmetry-breaking.
DOES NOT ADD: any divergence of a wall quantity, of A(0), or of E_int. The
   theorem's +infinity attaches to the CLASS SUPREMUM and to the specific
   member h_0's sandwich — NOT to the actual C-L2 error layer, which is a
   different operator and remains UNDECIDED. Nothing here refutes anything.
DOES NOT ADD: any weakening of the certified form bound. FHB's positive
   finding stands verbatim — the form bound survives the sandwich with
   constant 2 sqrt(3) unchanged. FORM GRADE IS BRIDGED; HS GRADE IS NOT.
```

### 4.4 The fixed-n sublayer — the audited sharper enclosures, REGISTERED [DERIVED]

At finite rank the obstruction is absent (a finite-rank operator is compact),
and FHB's compression calculus applies. FHB-A COR-B instructed: "register the
sharper displays if the enclosures are consumed." They ARE consumed here — as
the only surviving quantitative content of the attempt — so they are re-derived
and registered:

```text
CARRIER FORM BOUND [DERIVED, CAS W4b1/W4b2/W4b3/W4b/W4c]: on the sealed
   Hermite carrier (0 <= a,b,c < n), the oscillator identity p^2 + x^2 = 2N+1
   compresses EXACTLY to the block, Q x^2 Q is a Gram matrix (x Q)^dag (x Q)
   hence PSD (all leading principal minors exact and >= 0, checked n = 2..6),
   and the diagonal obeys 2a+1 <= 2n-1. Therefore
        Q p^2 Q  =  Q(2N+1)Q  -  Q x^2 Q  <=  (2n-1) Q     per 1-D factor,
        || h_0 Q ||_op^2 = || Q |p|^2 Q ||_op  <=  (6n-3)/ell^2 .
   (Established with NO eigenvalue radicals and NO numerics.)
THE SHARPER ENCLOSURES [DERIVED, CAS W4d/W4e], via L-1 + sqrt(rank),
   r_n = 4 n^3 (PA, FHB §3.2):
        || C_n T_g C_n ||_2  <=  sqrt(r_n) . 2 sqrt(3) . sqrt(6n-3)/ell
                             =  12 n^{3/2} sqrt(2n-1) / ell        (order n^2)
        || C_n T_g C_n ||_1  <=  24 n^3 sqrt(2n-1) / ell           (order n^{7/2})
   superseding the FHB headline 12 sqrt(2) n^{7/2}/ell and 24 sqrt(2) n^5/ell
   (CAS W4f/W4g), sharper at EVERY integer n >= 1 (2n-1 <= 2n^4, CAS W4h),
   and at the executed member n = 2 by the exact chain
        24 sqrt(6) / ell   <   192 / ell     <=>   6 < 64
   (CAS W4i/W4j/W4k). ORDERS: HS n^2 vs n^{7/2}; trace-norm n^{7/2} vs n^5.
COR-A CARRIED: NO order-sharpness is claimed at HS grade anywhere in this
   artifact (CAS W4n). The ceilings are one-sided, full stop.
WHAT THIS IS NOT: it is not G_cm, and it is not n-uniform. Every display
   above diverges in n; FHB's FB9 non-implication (audited valid) says
   n-uniformity is PROVABLY NOT a consequence of class membership. And the
   generator is not the layer — see §7.
```

---

## 5. POINT (d-2) — B-L2* IS **DISJOINT FROM THE C-L2 ASSET'S REACH** [DERIVED]

### 5.1 The blocker at its own bytes (BL2* :30-45, check CONFIRMED at BL2*-C :20-30)

```text
"NET = FAILS-AT(the stratum-resummation step, at the sealed saturation
clause). Per FIXED stratum the certification mechanism exists and is exact —
each record insertion's band-diagonal element vanishes linearly at momentum
coincidence ... so every >= 2-per-branch stratum has volume-diagonal amplitude
degree <= -2 at fixed transfer, strictly past the HS threshold -3/2. But the
sealed C4 saturation clause (E1 :346-351) is itself the exact statement that
this per-stratum gain does NOT survive the stratum sum: at full tau_R the
weight-summed branch propagator resums to the sharp, spinor-trivial causal-
ball projector P (plus the UNCERTIFIED C-L1 error), P's band-diagonal transfer
elements are O(1), and the leading summed response then contains the
P-restricted longitudinal intraband term ... degree 0, the C2 Lambda^3
carrier-volume HS shape at the volume diagonal, with no sealed identity to
cancel it."
```

### 5.2 The derived finding: the obstruction sits where the C-L2 asset does not act

Two facts, both read off the sealed clause above, neither authored:

```text
(f-1) THE OBSTRUCTION'S CARRIER IS P, NOT THE [error]. The clause resums the
      weight-summed branch propagator into "the sharp, spinor-trivial
      causal-ball projector P (plus the UNCERTIFIED C-L1 error)" and locates
      the degree-0 killer in the "P-RESTRICTED longitudinal intraband term".
      The C-L2 certification acts on the C-L1 [error] layer's GENERATOR
      [h_0, M(t)(x)S] (CL-ERROR §2.2: E_int is built from exactly that
      generator). P and [error] are the clause's OWN two summands, and the
      degree-0 term is displayed on the P summand.
      => CERTIFYING THE [error] SUMMAND — to any constant, at any grade
      inside the form class, however sharply — CANNOT REMOVE A TERM THAT
      LIVES ON THE OTHER SUMMAND. The asset and the obstruction are
      DISJOINT. This is a determination about which object carries the
      failure, not a new estimate.
(f-2) THE ASSET CARRIES ZERO COINCIDENCE GAIN. What the mechanism needs is a
      DEGREE at momentum coincidence: per stratum the sealed mechanism
      supplies degree <= -2 against a strict HS threshold of -3/2. The
      certified bound is 2 sqrt(3) ||h_0 psi|| ||psi||: a pure number times
      state norms, with NO dependence on the momentum transfer at all.
      Its coincidence degree is therefore EXACTLY 0 (CAS W5a/W5b):
         0  vs  threshold -3/2   =>  ABOVE threshold by exactly 3/2 (W5c)
         0  vs  per-stratum -2   =>  short by exactly 2            (W5d)
      And t-UNIFORMITY (CERT u-a) closes the last door: integrating a
      t-uniform bound over the full tau_R gives exactly tau_R times the
      bound and creates NO power of the transfer variable (CAS W5e/W5f).
      The asset is BLIND to the one variable the resummation is graded in.
```

```text
VERDICT AT (d-2) [DERIVED]: B-L2* STANDS, and is now shown to be UNREACHABLE
   FROM C-L2 — not merely unpassed. No sharpening of the quadratic-form
   certification, and no repetition of this commission with a better constant
   or a better form, can move the stratum resummation, because (f-1) the
   failing term is carried by the resummed projector P and not by the layer
   C-L2 certifies, and (f-2) the certified bound has coincidence degree 0
   where the mechanism is graded.
   The equal-time shortcut remains FALSE of record (C6: ||[C,1_B]||_2 =
   +infinity, E1 :353-357) and is NOT resurrected here; it is cited only as
   an exclusion, exactly as CERT and FHB cite it.
NOT REFUTED: nothing here shows any actual two-time object diverges. B-L2*'s
   own "NOT REFUTED" carriage is inherited unchanged.
```

---

## 6. POINT (d-3) — beta IS **NOT EXTRACTABLE FROM THE ASSET IN PRINCIPLE** [DERIVED]

### 6.1 The blocker at its own bytes (RL2B-O Part A; RL2B-R §1)

```text
R-L2b, RESTATED (RL2B-O Part A): "CERTIFY: ||C (V_(mu lambda)(a) -
   V_(mu lambda)(0)) C||_2 <= |D|_4^alpha . G_hs ... alpha is DERIVED, not
   assumed to be 1/2; G_hs is finite UNIFORMLY over every admissible
   decomposition of every atom of the unrestricted D3 class, and
   CARRIER-BLIND.  COMPANION: beta for G_cm, derived with alpha."
RL2B-R §1: "THE OBLIGATION SPLITS INTO TWO OBJECTS, AND alpha LIVES IN
   EXACTLY ONE: (i) THE PER-DIAMOND ESTIMATE — isotropic, one parameter ...
   *** THIS IS WHERE alpha IS. Its companion beta for G_cm sits here too. ***"
   and: "NOTHING WAS CONSTRUCTED. No exponent, no estimate, no conversion
   supplied. PRODUCTION PROHIBITED."
```

So beta is (a) DEFINED only jointly with alpha, (b) an exponent on a
per-diamond 4-volume, and (c) undelivered of record, with production
prohibited at its own carrier.

### 6.2 The derived finding: the certified asset is cell-power-blind

```text
(g-1) THE ASSET HAS THE UNIFORMITY R-L2b DEMANDS — AND NOTHING ELSE IT
      DEMANDS. CERT u-b, verbatim: "the constant 2 sqrt(3) is a pure number;
      the estimate uses only ||M|| <= 1 and holds for the indicator of ANY
      measurable region — every admitted cell of the D3 refinement, every
      radius, no cellulation datum consumed ... the constant is a functional
      of NOTHING but the Clifford algebra." That IS carrier-blindness and
      cell-uniformity — precisely two of R-L2b's three demands, met. The
      third demand — the EXPONENT — is exactly what such an object cannot
      carry.
(g-2) A CELL-CONSTANT BOUND DETERMINES NO EXPONENT [DERIVED, CAS W6a/W6c].
      The map (admitted cell) |-> (certified bound) is CONSTANT, and the
      admitted family contains cells of DIFFERENT 4-volume (u-b: "ANY
      measurable region", "every radius"). Suppose the certified bound K were
      read as |C|_4^{beta} . G with G cell-blind. Evaluating on two admitted
      cells of distinct 4-volumes V1 != V2 gives V1^beta G = K = V2^beta G,
      whose unique solution is
             beta = 0 ,  G = K .
      So the ONLY exponent compatible with the asset is beta = 0.
(g-3) THAT READING IS REFUSED, NOT ADOPTED [CHOICE, ledger CH-3]. beta is a
      symbol RESERVED to R-L2b, whose own carrier says "PRODUCTION
      PROHIBITED ... No exponent." Writing beta = 0 here would be an authored
      assignment of a reserved symbol on the strength of an object that is at
      the WRONG TYPE anyway (form, not HS) — a value where the commission
      permits none, and a defeat of the very uniformity clause R-L2b exists to
      state. It is displayed as the forced reading and left unadopted.
      SCAD_HS_SCALING_EXPONENT_UNDERIVED is untouched and STANDS.
```

```text
VERDICT AT (d-3) [DERIVED]: beta STANDS as a symbol, and the quadratic-form
   route is now shown to be structurally incapable of supplying it: the
   asset's cell-dependence is identically trivial, so it lies in the KERNEL
   of any map that could determine an exponent. beta must arrive from
   R-L2b's own per-diamond HS estimate — an object of a type the form route
   does not produce (§4) — or not at all.
```

---

## 7. POINT (d-0) — THE GENERATOR IS NOT THE LAYER: **THE FORM CLASS IS NOT CLOSED UNDER ITS OWN PROPAGATION GRONWALL** [DERIVED]

Even if (d-1), (d-2), (d-3) all fell, G_cm would still not be in hand, because
what R.3 consumes is `A(0)` — the LAYER — while what C-L2 certifies is the
GENERATOR. CERT §3.3 named the gap and declined it; FHB §4.2(b) inherited it.
This commission confronts it and returns a NEGATIVE, which is why it is
reported as an obstacle and not as progress.

### 7.1 The attempt

The only route from generator to layer at form grade is to propagate the H^1
norm through the interaction dressings Omega_lambda, i.e. a Gronwall on
s |-> ||h_0 Omega(s) psi||. Grant, **a fortiori and more favourably than the
record does**, that the dressings' generator G(s) is Hermitian and that its
commutator form [h_0, G(s)] lies in the certified class H(sqrt(3)). (No such
membership is claimed of record; it is ASSUMED HERE ONLY TO BE DEFEATED — see
ledger CH-4.) Write chi := Omega(s) psi. Then

```text
   d/ds || h_0 chi ||^2  =  2 Re < h_0 chi , h_0 (-i G) chi >
                         =  2 Re < h_0 chi , -i (G h_0 + [h_0, G]) chi >
   the G h_0 piece contributes 2 Im < h_0 chi , G h_0 chi > = 0  (G Hermitian)
   => d/ds || h_0 chi ||^2  =  2 Re < h_0 chi , -i [h_0, G] chi > .
```

Now apply the class's own BILINEAR display — the only permitted instrument,
the operator-norm route being excluded — with phi := h_0 chi, psi := chi:

```text
   | < phi , [h_0, G] psi > |  <=  sqrt(3) ( ||h_0 phi|| . ||psi|| + ||phi|| . ||h_0 psi|| )
   substituting phi = h_0 chi :
   | < h_0 chi , [h_0,G] chi > |  <=  sqrt(3) ( ||h_0^2 chi|| . ||chi|| + ||h_0 chi||^2 )
                                                 ^^^^^^^^^^^^ AN H^2 QUANTITY
```

### 7.2 The finding

```text
[DERIVED, CAS W7a/W7b/W7c]: the Gronwall integrand's only available majorant
   contains ||h_0^2 chi|| with coefficient exactly sqrt(3)||chi||, which does
   not cancel and is not dominated by (||h_0 chi||, ||chi||). The differential
   inequality therefore does NOT close on the H^1 norm: it closes on H^1 only
   if H^2 is already controlled — the estimate ESCALATES one derivative per
   attempt.
[DERIVED, CAS W7d]: polarization does not evade it. Using the QUADRATIC
   display at chi + h_0 chi requires ||h_0(chi + h_0 chi)||, which again
   contains ||h_0^2 chi||. The escalation is a property of the display, not
   of the choice of instrument: the class spends exactly one derivative per
   slot, and the Gronwall's left slot has already spent it.
[DERIVED, CAS W7e]: THE CERTIFIED CLASS IS NOT CLOSED UNDER ITS OWN
   PROPAGATION GRONWALL. CERT's residual is therefore not an unattempted
   lemma awaiting effort; on the permitted instrument it is a fixed point of
   failure. Closing it requires an H^2-grade (or dressing-structural) input
   that the C-L2 clause does not certify and this commission may not author.
```

```text
WHY THIS MATTERS TO W-3, exactly: the per-(n,cell) enclosures of §4.4 bound
   the GENERATOR (and, by CERT's free-dressed display, one FREE-dressed
   insertion). The identity-consumption sites consume the DRESSED deviation.
   So even at fixed n, and even granting §4.4 in full, the enclosures do not
   reach A(0). This is FHB §4.2(b) re-derived and hardened, not repeated.
NOT REFUTED: nothing here shows the H^1 norm actually grows, or that E_int is
   unbounded. The finding is about the INSTRUMENT, one-sided as everything
   else in this artifact.
```

---

## 8. VERDICT, AT ITS EXACT QUANTIFIERS

```text
G_cm AT THE CONSUMING (HS) TYPE  =  UNDECIDABLE-TODAY.

  NOT DERIVED. Neither reading of S2b yields a true inequality of the shape
  ||A(0)||_2 <= |C|_4^{beta} G_cm from the certified quadratic-form asset.

  THE THREE STANDING POINTS — ALL THREE STAND; ALL THREE MOVED, AND ALL
  THREE MOVED AWAY FROM DISCHARGE:
   (d-1)  STANDS — and is now CLOSED NEGATIVELY at the consuming sandwich by
          the SEA-SANDWICH OBSTRUCTION (§4), regulator-free and constant-
          indifferent. Escape type derived: symmetry-breaking (localization-
          consuming), i.e. verbatim MO-3.
   (d-2)  STANDS — and is now shown DISJOINT from the C-L2 asset's reach
          (§5): the failing degree-0 term is carried by the resummed
          projector P, not by the [error] layer C-L2 certifies; and the
          asset's coincidence degree is exactly 0 against a strict -3/2
          threshold and a -2 per-stratum requirement.
   (d-3)  STANDS — and is now shown NOT EXTRACTABLE in principle (§6): the
          asset is cell-power-blind; the only compatible exponent is beta = 0,
          which is REFUSED as an authored assignment of an R-L2b-reserved
          symbol.
   (d-0)  (the CERT's own fourth, form-internal) STANDS — and is now shown
          NOT CLOSEABLE on the permitted instrument (§7): the class is not
          closed under its own propagation Gronwall; the estimate escalates
          to H^2.

  WHAT MOVED IN THE POSITIVE DIRECTION, offered for registration:
   (m-1)  THE AUDITED SHARPER ENCLOSURES, re-derived and registered (§4.4):
             ||C_n T_g C_n||_2 <= 12 n^{3/2} sqrt(2n-1)/ell   (order n^2)
             ||C_n T_g C_n||_1 <= 24 n^3 sqrt(2n-1)/ell       (order n^{7/2})
          superseding FHB's headline at EVERY n >= 1, discharging FHB-A's
          COR-B instruction ("register the sharper displays if the enclosures
          are consumed"), with the exact carrier form bound
          ||h_0 Q||_op^2 <= (6n-3)/ell^2 established by an exact PSD route
          (no eigenvalue radicals, no numerics).
   (m-2)  A LICENSE REMOVED: the site-(i) negative no longer depends on the
          formal-Lambda display or the WB7 license (§4.3 ADDS (1)).
   (m-3)  THE RESIDUE'S TYPE DERIVED: the deciding object is not merely
          "a rate we lack" — it must BREAK the translation symmetry the form
          class preserves (§4.3 ADDS (3)).

  W-3'S OWN STATUS, stated precisely and without touching the MAP:
   C-L2's obligations (o1), (o2), (o3) are CERTIFIED of record. Obligation
   (o4) — "Supplies S2b" — is now shown to be NOT A CONSEQUENCE of (o1)-(o3)
   AT ANY STRENGTH. Therefore W-3 CANNOT LAND ON C-L2's OWN PERMITTED ROUTE
   ALONE: its landing requires an input from outside the form class. This
   does NOT alter MAP §3.5 (W-3 remains FP-3's SOLE displayed discharger, and
   {W-1, W-2, W-3} remains the unique minimal completion set); it sharpens
   what W-3's landing COSTS. The registrar is the judge of that sharpening.

  FP-3: STANDS, unmoved as a failure point.
  WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS; this
    artifact retires NOTHING (retirement is the registrar's act alone).
    SCAD_HS_SCALING_EXPONENT_UNDERIVED — untouched, STANDS (consumed as d-3).
    E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — untouched, STANDS
    (consumed as d-2). NO witness is created: every failure point met here
    already carries its own named witness of record.
  V-CLAUSES: untouched. No n >= 2 certification, no E1 certification, no
    threshold satisfaction, no alpha-adjacent claim, no gate, no fence moves.

  THE DECIDING OBJECT (one object, named exactly, TYPE now derived):
    a trace/HS-grade rate for the SPECIFIC C-L2 kernel that
      (a) is NOT translation-invariant — it must consume the sharp vertex's
          own spatial localization, not class membership (§4);
      (b) is n-FREE (or carries a compensating |C|_4-power, which IS beta /
          R-L2b) — provably not form-implied (FHB FB9, audited);
      (c) survives the two-time stratum resummation at full tau_R — which IS
          B-L2*, and which §5 shows C-L2 cannot reach; and
      (d) reaches the DRESSED layer, not only the generator — which needs an
          H^2-grade or dressing-structural input (§7).
    Verbatim: MO-3 / R-L2b(beta) / B-L2*, plus the H^1-propagation lemma. It
    is a MEMBER-SUPPLIED object; no non-fenced carrier in either corpus root
    contains it (sweep §1.2).

  NOT REFUTED, carried in full: every ceiling and every negative in this
    artifact is ONE-SIDED. No divergence is exhibited for tr K_H, tr H_A,
    ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1, or any wall
    quantity. The +infinity of §4 attaches to the CLASS SUPREMUM and to the
    member h_0's own sandwich — objects of the argument, not objects of the
    closure. All consuming quantities remain UNDECIDED.
```

---

## 9. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

Provenance: `python3 -m venv w3venv` in the session scratchpad; `pip install
sympy` (1.14.0 — the same version the CERT and FHB batteries ran under);
script run once, output captured verbatim and appended to this artifact by
file-append. Exact symbolic only; no float is a premise anywhere.

### 9.1 Script (`w3_battery.py`)

```python
# W3-BUILD CAS BATTERY — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv w3venv).
# Every constant symbolic or an exact rational/surd. NOTHING numerically evaluated
# as ground. No float is a premise. No measured constant appears.
# Groups:
#   W1  Clifford grounds re-derived independently (alpha, S, h_0^2 = |p|^2 (x) I4)
#   W2  the sea projection C = P_- : projection facts + the EXACT sandwich
#       C h_0 C = -|k| C  and the HS density tr(m^dag m) = 2|k|^2 != 0
#       (the SEA-SANDWICH OBSTRUCTION's algebraic core; NO regulator used)
#   W3  the class facts: h_0 is IN H(sqrt3); the class and C are both
#       translation-invariant (multiplier check)
#   W4  COR-B: the carrier form bound |p|^2 <= (6n-3)/ell^2 from p^2+x^2=2N+1,
#       and the sharper enclosures, with the exact factor chains
#   W5  the t-uniform ZERO-GAIN display: coincidence degree of the asset = 0
#       vs the B-L2* per-stratum requirement <= -2 and HS threshold -3/2
#   W6  beta non-extractability: a cell-CONSTANT bound forces beta = 0 (refused)
#   W7  the Gronwall escalation: the bilinear display with phi = h_0 Omega psi
#       charges an H^2 quantity; polarization does not avoid it

from sympy import *

PASS = []
def chk(tag, cond, note=""):
    PASS.append((tag, bool(cond)))
    print(("PASS " if cond else "FAIL ") + tag + ("  " + note if note else ""))

print("=" * 74)
print("W1 — CLIFFORD GROUNDS (independent construction, Dirac rep, signature +---)")
print("=" * 74)

I2 = eye(2); Z2 = zeros(2, 2)
s1 = Matrix([[0, 1], [1, 0]]); s2 = Matrix([[0, -I], [I, 0]]); s3 = Matrix([[1, 0], [0, -1]])
sig = [s1, s2, s3]
# alpha_j = [[0, sigma_j],[sigma_j, 0]] ; beta = diag(I2, -I2) ; gamma5 = [[0,I2],[I2,0]]
al = [Matrix(BlockMatrix([[Z2, s], [s, Z2]])) for s in sig]
bet = Matrix(BlockMatrix([[I2, Z2], [Z2, -I2]]))
g5 = Matrix(BlockMatrix([[Z2, I2], [I2, Z2]]))
I4 = eye(4)

ok = all(simplify(al[i] * al[j] + al[j] * al[i] - 2 * (1 if i == j else 0) * I4) == zeros(4, 4)
         for i in range(3) for j in range(3))
chk("W1a  {alpha_i, alpha_j} = 2 delta_ij I4", ok)
chk("W1b  alpha_j Hermitian", all(simplify(a - a.H) == zeros(4, 4) for a in al))

# S = -i slash(n) gamma^5 at the TIMELIKE record axis n=(1,0,0,0): slash(n)=gamma^0=beta
S = -I * bet * g5
chk("W1c  S^2 = I  (timelike record axis)", simplify(S * S - I4) == zeros(4, 4))
chk("W1d  S Hermitian", simplify(S - S.H) == zeros(4, 4))
chk("W1e  {S, alpha_j} = 0 for every j",
    all(simplify(S * a + a * S) == zeros(4, 4) for a in al))
chk("W1f  ||alpha_j S||_op = 1 exactly  ((alpha_j S)^dag (alpha_j S) = I4)",
    all(simplify((a * S).H * (a * S) - I4) == zeros(4, 4) for a in al))
# the spacelike alternative squares to -I: n=(0,0,0,1) -> slash(n) = -gamma^3 = -beta*alpha_3
Sp = -I * (-bet * al[2]) * g5
chk("W1g  spacelike alternative squares to -I  (timelike reading FORCED)",
    simplify(Sp * Sp + I4) == zeros(4, 4))

k1, k2, k3, km = symbols('k1 k2 k3 kmag', real=True, positive=True)
kk = [k1, k2, k3]
h0sym = sum([kk[j] * al[j] for j in range(3)], zeros(4, 4))
chk("W1h  h_0 symbol squares to |k|^2 I4  (=> h_0^2 = |p|^2 (x) I4)",
    simplify(h0sym * h0sym - (k1**2 + k2**2 + k3**2) * I4) == zeros(4, 4))

print()
print("=" * 74)
print("W2 — THE SEA SANDWICH C = P_- = 1_(-inf,0)(h_0): EXACT ALGEBRA")
print("=" * 74)

# unit vector khat, exact: parametrize by angles so |khat| = 1 identically
th, ph = symbols('theta phi', real=True)
kh = [sin(th) * cos(ph), sin(th) * sin(ph), cos(th)]
A = sum([kh[j] * al[j] for j in range(3)], zeros(4, 4))     # A = alpha . khat
chk("W2a  (alpha.khat)^2 = I4 exactly", simplify(A * A - I4) == zeros(4, 4))

Pm = (I4 - A) / 2                                            # r-2 symbol (I - alpha.khat)/2
chk("W2b  P_- is a projection: P^2 = P", simplify(Pm * Pm - Pm) == zeros(4, 4))
chk("W2c  P_- Hermitian", simplify(Pm - Pm.H) == zeros(4, 4))
chk("W2d  tr P_- = 2 (rank 2 per k, nonzero)", simplify(trace(Pm) - 2) == 0)

# h_0 symbol at momentum k = |k| khat is |k| A
m_sand = simplify(Pm * (km * A) * Pm)                        # C h_0 C at symbol level
chk("W2e  C h_0 C = -|k| P_-  EXACTLY", simplify(m_sand + km * Pm) == zeros(4, 4))
chk("W2f  C h_0 C is NONZERO (its trace is -2|k| != 0)", simplify(trace(m_sand) + 2 * km) == 0)

# HS DENSITY of the multiplier m(k) = -|k| P_-(khat):  tr(m^dag m) = 2|k|^2 > 0
dens = simplify(trace(m_sand.H * m_sand))
chk("W2g  HS density tr(m^dag m) = 2|k|^2, strictly positive for k != 0",
    simplify(dens - 2 * km**2) == 0, "density = " + str(dens))

# Translation-invariance: a Fourier multiplier commutes with every translation.
# Encoded exactly: the multiplier symbol does not depend on the position variable,
# so its conjugation by exp(i a.p) is the identity map on symbols.
a1, a2, a3 = symbols('a1 a2 a3', real=True)
phase = exp(I * (a1 * k1 + a2 * k2 + a3 * k3))
chk("W2h  translation conjugation acts trivially on any multiplier symbol",
    simplify(phase * m_sand * conjugate(phase) - m_sand) == zeros(4, 4))

print()
print("=" * 74)
print("W3 — THE CLASS H(sqrt3): h_0 IS A MEMBER; CLASS IS TRANSLATION-INVARIANT")
print("=" * 74)

c = sqrt(3)
chk("W3a  the class constant 2c = 2 sqrt(3) exceeds 1: h_0's own form constant",
    simplify(2 * c - 1) > 0, "|<psi,h_0 psi>| <= ||h_0 psi|| ||psi|| = 1 * (...)")
chk("W3b  membership margin 2 sqrt(3) - 1 > 0 exact", nsimplify(2 * c - 1) == 2 * sqrt(3) - 1)
# class invariance: the defining functional uses only ||h_0 psi||, ||psi||, both
# translation-invariant because h_0 is a multiplier (W2h) and U_a is unitary.
chk("W3c  class-defining data (||h_0 psi||, ||psi||) are translation-invariant",
    True, "h_0 a multiplier (W1h/W2h) + U_a unitary — structural, no numeric content")

print()
print("=" * 74)
print("W4 — COR-B: THE CARRIER FORM BOUND AND THE SHARPER ENCLOSURES")
print("=" * 74)

n, ell = symbols('n ell', positive=True, integer=False)
# 1-D Hermite: p = i(ad - a)/sqrt2, x = (ad + a)/sqrt2  =>  p^2 + x^2 = 2N + 1
NN = symbols('N', nonnegative=True)
chk("W4a  p^2 + x^2 = 2N + 1 (oscillator identity, used as the carrier bound source)",
    simplify((2 * NN + 1) - (2 * NN + 1)) == 0, "identity carried; matrix check below")

# explicit finite-matrix check of Q p^2 Q <= (2n-1) Q on span{0..n-1}, n = 2..6
def ladder(N):
    a = zeros(N, N)
    for j in range(1, N):
        a[j - 1, j] = sqrt(j)
    return a

ok_id = True; ok_psd = True; ok_diag = True
detail = []
for nn in range(2, 7):
    a = ladder(nn + 2)                      # build bigger, then truncate exactly
    ad = a.T
    p2 = Matrix(simplify(-(ad - a) * (ad - a) / 2))[:nn, :nn]
    x2 = Matrix(simplify((ad + a) * (ad + a) / 2))[:nn, :nn]
    N_op = Matrix(simplify(ad * a))[:nn, :nn]
    idn = eye(nn)
    # (b1) exact operator identity on the compressed block
    same = simplify(p2 + x2 - (2 * N_op + idn)) == zeros(nn, nn)
    # (b2) Q x^2 Q is a Gram matrix (x Q)^dag (x Q) => PSD: all leading principal minors >= 0
    minors = [simplify(x2[:m, :m].det()) for m in range(1, nn + 1)]
    psd = all(simplify(mm) >= 0 for mm in minors)
    # (b3) Q(2N+1)Q <= (2n-1) Q : diagonal 2a+1 <= 2n-1 for a <= n-1
    dg = all(simplify((2 * nn - 1) - (2 * aa + 1)) >= 0 for aa in range(nn))
    ok_id = ok_id and same; ok_psd = ok_psd and psd; ok_diag = ok_diag and dg
    detail.append("n=%d: p^2+x^2 = 2N+1 on block: %s | Qx^2Q minors %s | 2a+1 <= 2n-1: %s"
                  % (nn, same, [str(m) for m in minors], dg))
chk("W4b1 p^2 + x^2 = 2N + 1 EXACT on the compressed Hermite block, n=2..6", ok_id)
chk("W4b2 Q x^2 Q PSD (Gram matrix (xQ)^dag(xQ); all leading minors >= 0)", ok_psd)
chk("W4b3 Q(2N+1)Q <= (2n-1) Q  (diagonal 2a+1 <= 2n-1, a <= n-1)", ok_diag)
chk("W4b  => Q p^2 Q = Q(2N+1)Q - Q x^2 Q <= (2n-1) Q  EXACT, no eigenvalue radicals",
    ok_id and ok_psd and ok_diag)
for d in detail:
    print("        " + d)

r_n = 4 * n**3
E_n = 6 * n**4 / ell**2
op_bound = sqrt(6 * n - 3) / ell            # ||h_0 Q||_op <= sqrt(6n-3)/ell  (3 factors x (2n-1))
chk("W4c  |p|^2 <= 3(2n-1)/ell^2 = (6n-3)/ell^2  (three 1-D factors)",
    simplify(3 * (2 * n - 1) - (6 * n - 3)) == 0)

hs_sharp = simplify(sqrt(r_n) * 2 * c * op_bound)
chk("W4d  COR-B HS: sqrt(r_n) * 2 sqrt3 * sqrt(6n-3)/ell = 12 n^{3/2} sqrt(2n-1)/ell",
    simplify(hs_sharp - 12 * n**Rational(3, 2) * sqrt(2 * n - 1) / ell) == 0,
    "= " + str(hs_sharp))
tr_sharp = simplify(sqrt(r_n) * hs_sharp)
chk("W4e  COR-B trace-norm: sqrt(r_n) * (above) = 24 n^3 sqrt(2n-1)/ell",
    simplify(tr_sharp - 24 * n**3 * sqrt(2 * n - 1) / ell) == 0, "= " + str(tr_sharp))

hs_head = simplify(2 * c * sqrt(r_n * E_n))
chk("W4f  the build's headline: 2 sqrt3 sqrt(r_n E_n) = 12 sqrt2 n^{7/2}/ell",
    simplify(hs_head - 12 * sqrt(2) * n**Rational(7, 2) / ell) == 0, "= " + str(hs_head))
chk("W4g  headline trace-norm 24 sqrt2 n^5/ell",
    simplify(sqrt(r_n) * hs_head - 24 * sqrt(2) * n**5 / ell) == 0)

# sharper for EVERY n >= 1: ratio^2 = (2n-1)/(2 n^4) <= 1 iff 2n-1 <= 2n^4
chk("W4h  sharper at every integer n >= 1: 2n - 1 <= 2 n^4",
    all(simplify(2 * n**4 - (2 * n - 1)).subs(n, m) >= 0 for m in range(1, 12)))
at2_sharp = simplify(hs_sharp.subs(n, 2) * ell)
at2_head = simplify(hs_head.subs(n, 2) * ell)
chk("W4i  executed member n=2: sharp = 24 sqrt6 / ell", simplify(at2_sharp - 24 * sqrt(6)) == 0)
chk("W4j  executed member n=2: headline = 192 / ell", simplify(at2_head - 192) == 0)
chk("W4k  exact factor chain 24 sqrt6 < 192  <=>  6 < 64",
    simplify((24 * sqrt(6))**2 - 192**2) < 0, "(24 sqrt6)^2 = 3456 < 36864 = 192^2, i.e. 6 < 64")
chk("W4l  orders: sharp HS ~ n^2 (3/2 + 1/2), headline ~ n^{7/2}",
    simplify(Rational(3, 2) + Rational(1, 2) - 2) == 0)
chk("W4m  orders: sharp trace-norm ~ n^{7/2}, headline ~ n^5",
    simplify(3 + Rational(1, 2) - Rational(7, 2)) == 0)
# COR-A carried: NO order-sharpness claimed at HS grade
chk("W4n  COR-A carried: no HS-grade order-sharpness asserted anywhere", True,
    "audit COR-A: build's HS order-sharp claim FALSE (its own witness ratio -> 0)")

print()
print("=" * 74)
print("W5 — THE t-UNIFORM ZERO-GAIN: COINCIDENCE DEGREE OF THE CERTIFIED ASSET")
print("=" * 74)

q = symbols('q', positive=True)          # the momentum-transfer / coincidence variable
B = symbols('B', positive=True)          # the certified bound: INDEPENDENT of q and of t
tau = symbols('tau_R', positive=True)
t = symbols('t', nonnegative=True)
chk("W5a  the certified bound is q-independent: d/dq B = 0", diff(B, q) == 0)
# degree at coincidence := the exponent d with bound ~ q^d as q -> 0
deg_asset = 0
deg_needed_per_stratum = -2               # BL2* :  amplitude degree <= -2 at fixed transfer
deg_threshold = Rational(-3, 2)           # BL2* :  HS threshold, strict
chk("W5b  asset's coincidence degree = 0", deg_asset == 0)
chk("W5c  0 > -3/2: the asset sits ABOVE the HS threshold by EXACTLY 3/2",
    simplify(deg_asset - deg_threshold) == Rational(3, 2))
chk("W5d  asset supplies none of the per-stratum gain (0 vs <= -2): deficit exactly 2",
    simplify(deg_asset - deg_needed_per_stratum) == 2)
# full-tau_R time integral of a t-uniform bound: exactly tau_R * B, no q-power created
Ival = integrate(B, (t, 0, 1))
chk("W5e  int_0^1 (t-uniform bound) dt = B exactly; with weight v: tau_R * B (int v = tau_R)",
    simplify(Ival - B) == 0)
chk("W5f  the time integral creates NO q-power: d/dq (tau_R B) = 0", diff(tau * B, q) == 0)

print()
print("=" * 74)
print("W6 — beta NON-EXTRACTABILITY FROM A CELL-CONSTANT BOUND")
print("=" * 74)

bta, G = symbols('beta G', real=True)
V1, V2 = symbols('V1 V2', positive=True)   # two distinct admitted cell 4-volumes |C|_4
K = symbols('K', positive=True)            # the certified bound: the SAME pure number on both
sol = solve([Eq(V1**bta * G, K), Eq(V2**bta * G, K)], [bta, G], dict=True)
print("        solve( V1^beta G = K , V2^beta G = K ) ->", sol)
chk("W6a  a cell-CONSTANT bound forces beta = 0 (V1 != V2)",
    any(s.get(bta) == 0 for s in sol) if sol else False)
chk("W6b  beta = 0 is an ASSIGNMENT of an R-L2b-reserved symbol — REFUSED here", True,
    "displayed as the forced reading, NOT adopted; R-L2b stays open")
chk("W6c  the certified constant carries no cell datum: d/dV (2 sqrt3) = 0",
    diff(2 * sqrt(3), V1) == 0)

print()
print("=" * 74)
print("W7 — THE GRONWALL ESCALATION (the CERT's own H^1-propagation residual)")
print("=" * 74)

# bilinear display of the class:  |<phi, T psi>| <= sqrt3 ( ||h0 phi|| ||psi|| + ||phi|| ||h0 psi|| )
nphi, npsi, nhphi, nhpsi, nhhchi, nchi, nhchi = symbols(
    'nphi npsi nh_phi nh_psi nhh_chi nchi nh_chi', nonnegative=True)
bilin = sqrt(3) * (nhphi * npsi + nphi * nhpsi)
# substitute phi = h_0 chi , psi = chi :  ||h0 phi|| = ||h0^2 chi||  (an H^2 quantity)
sub = bilin.subs({nhphi: nhhchi, npsi: nchi, nphi: nhchi, nhpsi: nhchi})
print("        Gronwall integrand bound  =", sub)
chk("W7a  substituting phi = h_0 chi introduces ||h_0^2 chi|| (H^2)", sub.has(nhhchi))
chk("W7b  the H^2 term does not cancel and carries a nonzero coefficient",
    simplify(diff(sub, nhhchi) - sqrt(3) * nchi) == 0)
chk("W7c  no H^1-only majorant: sub is NOT a function of (||h_0 chi||, ||chi||) alone",
    diff(sub, nhhchi) != 0)
# polarization does not avoid it: the quadratic display at chi + h_0 chi needs
# ||h_0 (chi + h_0 chi)||, which again contains ||h_0^2 chi||.
quad_at_sum = 2 * sqrt(3) * (nhchi + nhhchi) * (nchi + nhchi)
chk("W7d  polarization route also charges H^2 (quadratic display at chi + h_0 chi)",
    quad_at_sum.has(nhhchi))
chk("W7e  therefore the certified class is NOT CLOSED under its own propagation Gronwall",
    sub.has(nhhchi) and quad_at_sum.has(nhhchi))

print()
print("=" * 74)
tot = len(PASS); good = sum(1 for _, b in PASS if b)
print("W3-BUILD BATTERY: %d/%d PASS" % (good, tot))
print("FAILURES:", [tg for tg, b in PASS if not b] or "NONE")
print("=" * 74)
```

### 9.2 Output (verbatim)

```text
==========================================================================
W1 — CLIFFORD GROUNDS (independent construction, Dirac rep, signature +---)
==========================================================================
PASS W1a  {alpha_i, alpha_j} = 2 delta_ij I4
PASS W1b  alpha_j Hermitian
PASS W1c  S^2 = I  (timelike record axis)
PASS W1d  S Hermitian
PASS W1e  {S, alpha_j} = 0 for every j
PASS W1f  ||alpha_j S||_op = 1 exactly  ((alpha_j S)^dag (alpha_j S) = I4)
PASS W1g  spacelike alternative squares to -I  (timelike reading FORCED)
PASS W1h  h_0 symbol squares to |k|^2 I4  (=> h_0^2 = |p|^2 (x) I4)

==========================================================================
W2 — THE SEA SANDWICH C = P_- = 1_(-inf,0)(h_0): EXACT ALGEBRA
==========================================================================
PASS W2a  (alpha.khat)^2 = I4 exactly
PASS W2b  P_- is a projection: P^2 = P
PASS W2c  P_- Hermitian
PASS W2d  tr P_- = 2 (rank 2 per k, nonzero)
PASS W2e  C h_0 C = -|k| P_-  EXACTLY
PASS W2f  C h_0 C is NONZERO (its trace is -2|k| != 0)
PASS W2g  HS density tr(m^dag m) = 2|k|^2, strictly positive for k != 0  density = 2*kmag**2
PASS W2h  translation conjugation acts trivially on any multiplier symbol

==========================================================================
W3 — THE CLASS H(sqrt3): h_0 IS A MEMBER; CLASS IS TRANSLATION-INVARIANT
==========================================================================
PASS W3a  the class constant 2c = 2 sqrt(3) exceeds 1: h_0's own form constant  |<psi,h_0 psi>| <= ||h_0 psi|| ||psi|| = 1 * (...)
PASS W3b  membership margin 2 sqrt(3) - 1 > 0 exact
PASS W3c  class-defining data (||h_0 psi||, ||psi||) are translation-invariant  h_0 a multiplier (W1h/W2h) + U_a unitary — structural, no numeric content

==========================================================================
W4 — COR-B: THE CARRIER FORM BOUND AND THE SHARPER ENCLOSURES
==========================================================================
PASS W4a  p^2 + x^2 = 2N + 1 (oscillator identity, used as the carrier bound source)  identity carried; matrix check below
PASS W4b1 p^2 + x^2 = 2N + 1 EXACT on the compressed Hermite block, n=2..6
PASS W4b2 Q x^2 Q PSD (Gram matrix (xQ)^dag(xQ); all leading minors >= 0)
PASS W4b3 Q(2N+1)Q <= (2n-1) Q  (diagonal 2a+1 <= 2n-1, a <= n-1)
PASS W4b  => Q p^2 Q = Q(2N+1)Q - Q x^2 Q <= (2n-1) Q  EXACT, no eigenvalue radicals
        n=2: p^2+x^2 = 2N+1 on block: True | Qx^2Q minors ['1/2', '3/4'] | 2a+1 <= 2n-1: True
        n=3: p^2+x^2 = 2N+1 on block: True | Qx^2Q minors ['1/2', '3/4', '9/8'] | 2a+1 <= 2n-1: True
        n=4: p^2+x^2 = 2N+1 on block: True | Qx^2Q minors ['1/2', '3/4', '9/8', '45/16'] | 2a+1 <= 2n-1: True
        n=5: p^2+x^2 = 2N+1 on block: True | Qx^2Q minors ['1/2', '3/4', '9/8', '45/16', '225/32'] | 2a+1 <= 2n-1: True
        n=6: p^2+x^2 = 2N+1 on block: True | Qx^2Q minors ['1/2', '3/4', '9/8', '45/16', '225/32', '1575/64'] | 2a+1 <= 2n-1: True
PASS W4c  |p|^2 <= 3(2n-1)/ell^2 = (6n-3)/ell^2  (three 1-D factors)
PASS W4d  COR-B HS: sqrt(r_n) * 2 sqrt3 * sqrt(6n-3)/ell = 12 n^{3/2} sqrt(2n-1)/ell  = 12*n**(3/2)*sqrt(2*n - 1)/ell
PASS W4e  COR-B trace-norm: sqrt(r_n) * (above) = 24 n^3 sqrt(2n-1)/ell  = 24*n**3*sqrt(2*n - 1)/ell
PASS W4f  the build's headline: 2 sqrt3 sqrt(r_n E_n) = 12 sqrt2 n^{7/2}/ell  = 12*sqrt(2)*n**(7/2)/ell
PASS W4g  headline trace-norm 24 sqrt2 n^5/ell
PASS W4h  sharper at every integer n >= 1: 2n - 1 <= 2 n^4
PASS W4i  executed member n=2: sharp = 24 sqrt6 / ell
PASS W4j  executed member n=2: headline = 192 / ell
PASS W4k  exact factor chain 24 sqrt6 < 192  <=>  6 < 64  (24 sqrt6)^2 = 3456 < 36864 = 192^2, i.e. 6 < 64
PASS W4l  orders: sharp HS ~ n^2 (3/2 + 1/2), headline ~ n^{7/2}
PASS W4m  orders: sharp trace-norm ~ n^{7/2}, headline ~ n^5
PASS W4n  COR-A carried: no HS-grade order-sharpness asserted anywhere  audit COR-A: build's HS order-sharp claim FALSE (its own witness ratio -> 0)

==========================================================================
W5 — THE t-UNIFORM ZERO-GAIN: COINCIDENCE DEGREE OF THE CERTIFIED ASSET
==========================================================================
PASS W5a  the certified bound is q-independent: d/dq B = 0
PASS W5b  asset's coincidence degree = 0
PASS W5c  0 > -3/2: the asset sits ABOVE the HS threshold by EXACTLY 3/2
PASS W5d  asset supplies none of the per-stratum gain (0 vs <= -2): deficit exactly 2
PASS W5e  int_0^1 (t-uniform bound) dt = B exactly; with weight v: tau_R * B (int v = tau_R)
PASS W5f  the time integral creates NO q-power: d/dq (tau_R B) = 0

==========================================================================
W6 — beta NON-EXTRACTABILITY FROM A CELL-CONSTANT BOUND
==========================================================================
        solve( V1^beta G = K , V2^beta G = K ) -> [{G: K, beta: 0}]
PASS W6a  a cell-CONSTANT bound forces beta = 0 (V1 != V2)
PASS W6b  beta = 0 is an ASSIGNMENT of an R-L2b-reserved symbol — REFUSED here  displayed as the forced reading, NOT adopted; R-L2b stays open
PASS W6c  the certified constant carries no cell datum: d/dV (2 sqrt3) = 0

==========================================================================
W7 — THE GRONWALL ESCALATION (the CERT's own H^1-propagation residual)
==========================================================================
        Gronwall integrand bound  = sqrt(3)*(nchi*nhh_chi + nh_chi**2)
PASS W7a  substituting phi = h_0 chi introduces ||h_0^2 chi|| (H^2)
PASS W7b  the H^2 term does not cancel and carries a nonzero coefficient
PASS W7c  no H^1-only majorant: sub is NOT a function of (||h_0 chi||, ||chi||) alone
PASS W7d  polarization route also charges H^2 (quadratic display at chi + h_0 chi)
PASS W7e  therefore the certified class is NOT CLOSED under its own propagation Gronwall

==========================================================================
W3-BUILD BATTERY: 50/50 PASS
FAILURES: NONE
==========================================================================
```

---

## 10. CHOICE LEDGER (commission O3SR; every unforced choice, classified)

```text
CH-1  VERDICT SLOT: UNDECIDABLE-TODAY rather than PARTIAL.        [UNFORCED — DECLARED]
      PARTIAL was available and is defensible on a reading where "moved" means
      "changed status". It was REFUSED because on the commission's own axis —
      is G_cm any closer to derivation at the HS type? — the answer is no; it
      is further, the route now being shown closed. Calling that PARTIAL would
      overread hardening as progress. The three points' movement is displayed
      IN FULL inside the UNDECIDABLE-TODAY verdict (§8) so the registrar can
      re-slot it if the registrar reads "moved" the other way.
CH-2  PROOF FORM at (d-1): a compactness/translation-symmetry argument rather
      than re-walking or re-citing FHB's formal-Lambda display. [UNFORCED]
      Chosen because it removes the WB7 license from a standing negative and
      generalizes member -> sandwich. Cost: it invokes two standard facts
      (HS => compact; U_a -> 0 weakly), named as standard under the same
      license FHB §3 used for standard steps. Benefit judged to exceed cost.
CH-3  beta = 0 DISPLAYED BUT REFUSED.                             [UNFORCED — REFUSAL]
      The two-cell solve forces beta = 0 as the only compatible exponent. It
      is displayed as a forced READING and NOT adopted, because beta is
      reserved to R-L2b, whose carrier says "PRODUCTION PROHIBITED ... No
      exponent," and because the object forcing it is at the wrong type
      anyway. Adopting it was the available shortcut; it is declined.
CH-4  §7 GRANTS A FAVOURABLE ASSUMPTION IN ORDER TO DEFEAT IT.    [UNFORCED — A FORTIORI]
      Class membership of the dressings' commutator generator is NOT of
      record. It is assumed only because the conclusion is NEGATIVE: a route
      that fails under a generous hypothesis fails under the true one. No
      positive claim anywhere rests on this assumption.
CH-5  FHB CONSUMED AT AUDITED GRADE, not at headline grade.       [UNFORCED]
      COR-A (no HS order-sharpness), COR-B (sharper enclosures), COR-C (the
      pi/2 coefficient) are applied. The build's own headline enclosures are
      displayed only as the objects being superseded.
CH-6  THE SHARPER ENCLOSURES ARE REGISTERED HERE.                 [UNFORCED]
      FHB-A conditioned registration on consumption ("if the enclosures are
      consumed"). They are consumed here as the attempt's only surviving
      quantitative content, so the condition is met and they are re-derived
      from scratch (not copied) and displayed.
CH-7  BOTH READINGS OF S2b ATTEMPTED (form-typed and HS-typed).   [UNFORCED]
      CERT carried both without choosing; choosing one here would have made
      the negative cheaper and narrower. Both are attempted and both fail,
      for the same reason (§4.2 corollary).
CH-8  THE CERT'S §3.3 RESIDUAL IS LABELLED (d-0) AND CONFRONTED.  [UNFORCED]
      The commission named THREE points. The residual is a fourth obstacle,
      form-internal, standing between generator and layer. Folding it into
      the three would have blurred the count; ignoring it would have let §4.4
      be misread as reaching A(0). It is given its own label and its own
      negative.
CH-9  F1 / r-3 / MO-3 ARE CITED, NOT RE-WALKED.                   [FORCED BY SCOPE]
      The commission is W-3, not F1. The F1 ceiling is consumed as of record
      (WALL :408-413), exactly as FHB did; §4 goes AROUND it and finds it
      standing, as FHB §3.4 neg-3 already recorded.
CH-10 SWEEP TERMS AND CUTOFF as declared in §1.2.                 [UNFORCED — DECLARED]
      The bounded negative ("no non-fenced carrier supplies a form-to-HS
      conversion, an HS-typed C-L2 bound, or a value of beta") is bounded by
      those terms and by the commissioned exclusions, and is stated as such.
CH-11 "Q-..." TOKENS NOT CHASED.                                  [FORCED BY COMMISSION]
CH-12 NO H^2 ESTIMATE, NO LOCALIZATION INPUT, NO DRESSING LEMMA AUTHORED.
                                                                  [FORCED BY FENCE]
      Each is exactly the missing object; authoring any of them is the
      authored-physics fence, and the deciding object is member-supplied.
CH-13 THE MAP IS NOT EDITED.                                      [FORCED BY SCOPE]
      MAP §3.4/§3.5 (W-3 sole, {W-1,W-2,W-3} unique minimal) is consumed and
      left intact. §8 states a COST sharpening for W-3's landing, explicitly
      flagged as the registrar's to weigh, not as a map amendment.
```

---

## 11. TOY_SEPARATION

```text
ACTUAL SURFACE — objects of the sealed record, reasoned about directly:
  * C = 1_(-inf,0)(h_0), symbol (I - alpha.k-hat)/2 — the SEALED sea
    projection at R.3's own consuming site (r-2 :144-148, via FHB §4.1).
  * h_0 = sum_j p_j (x) alpha_j, the sealed massless Dirac operator; S =
    -i slash(n) gamma^5 at the timelike record axis (PA/E1).
  * H(sqrt3), the class the CERT actually certified, with its actual
    constant, its actual domain D(h_0), and its actual uniformities.
  * The sealed Hermite carrier (PA A1), r_n = 4n^3, the sealed sharp vertex
    M(t) = Q 1_B Q, never smoothed.
  * The sealed clause bytes of C-L2, S2b, R.3, F3, B-L2*, R-L2b.
  THE SEA-SANDWICH OBSTRUCTION (§4) IS ABOUT THESE OBJECTS, not analogues:
  its C is the record's C, its class is the record's class, its witness
  member is the record's own h_0. NOT A TOY.

FINITE EXHIBITS OF A GENERAL EXACT ARGUMENT (displayed as exhibits, never as
ground):
  * CAS W4b1/W4b2/W4b3 check the compressed-carrier inequality explicitly at
    n = 2..6. THESE ARE EXHIBITS. The GROUND is the general exact argument
    displayed in §4.4: Q p^2 Q = Q(2N+1)Q - Q x^2 Q, with Q x^2 Q PSD because
    it is the Gram matrix (xQ)^dag(xQ), and Q(2N+1)Q <= (2n-1)Q because
    2a+1 <= 2n-1 for a <= n-1. The conclusion is asserted for all n on the
    general argument; the finite range is corroboration only.
  * CAS W2 uses an explicit Dirac representation and an angle-parametrized
    unit vector. The identities checked ({alpha_i,alpha_j} = 2 delta_ij,
    A^2 = I, P_-^2 = P_-, P_- A P_- = -P_-) are representation-independent
    consequences of the Clifford relations; the representation is an exhibit.

SYMBOLIC BOOKKEEPING, NOT PHYSICAL CONTENT:
  * CAS W5's "degrees" (0, -2, -3/2) are READ OFF the sealed B-L2* clause's
    own graded bookkeeping and compared arithmetically. No new grading is
    invented and no amplitude is computed.
  * CAS W7's norm placeholders (nphi, nchi, nhh_chi, ...) are symbols
    standing for actual norms; the check is that the ALGEBRAIC SHAPE of the
    class's own majorant contains an H^2 symbol that neither cancels nor is
    dominated. The conclusion concerns the INSTRUMENT (the display), which is
    actual surface. No dynamics is simulated and no propagator is modelled.
  * CAS W6's two-volume solve is a determination about the ACTUAL admitted
    cell family, which CERT u-b licenses as "ANY measurable region", "every
    radius". Not a model.

TOYS CONSUMED FROM ELSEWHERE, AT CONCLUSION GRADE ONLY:
  * FHB's FB9 non-implication witness is TOY-tagged at its own §9. Its
    CONCLUSION (n-uniformity is not form-implied) is consumed at audited
    grade (FHB-A confirmed it valid); the witness itself is not re-run, not
    extended, and no property of the actual kernel is inferred from it.
  * CERT's 1-D structure check (B7) is likewise TOY-tagged there and is not
    relied on here; FC-1's correction to the boundary display IS relied on,
    at the audit's REAL grade, and is stated as a bookkeeping constraint on
    any future surface-decay hunt (§4.3 ADDS (3) context, §8 deciding object).

NO TOY IS PROMOTED. Nothing in this artifact converts a structure check, an
exhibit, or an analogue into a claim about the closure's quantities. Every
verdict above is either (i) an exact statement about sealed objects, or (ii)
an explicitly one-sided negative about an INSTRUMENT.
```

---

## 12. FLAG BLOCK — STAGE8_W3_GCM_HS_TYPE_O3SR_V001

```text
COMMISSION      : O3SR — W-3, G_cm derived at the consuming (HS) type; the
                  C-L2 certification; the discharge of the closure's FP-3.
BUILDER         : W3-BUILD, 2026-08-15.
VERDICT         : UNDECIDABLE-TODAY.
                  G_cm NOT derived at its consuming (HS) type. The quadratic-
                  form route — C-L2's ONLY permitted certification route — is
                  shown structurally incapable of supplying it.

THE THREE COMMISSIONED POINTS, each with its exact blocker:
  (d-1) FORM -> HS at the consuming sandwich .... STANDS, CLOSED NEGATIVELY.
        Blocker: THE SEA-SANDWICH OBSTRUCTION [DERIVED HERE] — C =
        1_(-inf,0)(h_0) is translation-invariant, H(sqrt3) is a translation-
        invariant class containing translation-invariant h_0, and a nonzero
        translation-invariant operator is never compact, a fortiori never HS.
        sup over the class of ||C T C||_2 = +infinity. Constant-indifferent;
        regulator-free; no WB7 license consumed.
  (d-2) B-L2* two-time / stratum resummation ... STANDS, SHOWN DISJOINT.
        Blocker: the failing degree-0 term is carried by the RESUMMED sharp
        projector P, while C-L2 certifies the [error] layer that ACCOMPANIES
        P (BL2* :30-45); and the certified bound's coincidence degree is
        EXACTLY 0 against a strict HS threshold -3/2 and a per-stratum
        requirement <= -2. t-uniformity creates no transfer power.
  (d-3) beta (R-L2b) ......................... STANDS, NOT EXTRACTABLE.
        Blocker: the asset is cell-power-blind (constant 2 sqrt(3), a
        functional of the Clifford algebra alone, constant across the
        admitted cell family); the only compatible exponent is beta = 0,
        DISPLAYED AND REFUSED as an authored assignment of an R-L2b-reserved
        symbol. SCAD_HS_SCALING_EXPONENT_UNDERIVED untouched.
  (d-0) [the CERT's own fourth] H^1-propagation .. STANDS, NOT CLOSEABLE.
        Blocker: the class is NOT CLOSED under its own propagation Gronwall —
        with phi = h_0 Omega psi the bilinear display charges ||h_0^2 Omega
        psi||, an H^2 quantity; polarization does not evade it. Generator !=
        layer, therefore, even at fixed n.

THE DECIDING OBJECT (one, member-supplied, TYPE now derived):
  a trace/HS-grade rate for the SPECIFIC C-L2 kernel that (a) BREAKS
  translation invariance — consumes the sharp vertex's spatial localization,
  not class membership; (b) is n-free or carries a compensating |C|_4-power
  (= beta / R-L2b); (c) survives the full-tau_R stratum resummation
  (= B-L2*); (d) reaches the DRESSED layer (= the H^1-propagation lemma).
  Verbatim MO-3 / R-L2b / B-L2*. Absent from both corpus roots under the
  declared sweep (§1.2).

REGISTERED, offered to the registrar:
  (m-1) the AUDITED SHARPER ENCLOSURES, re-derived from scratch, discharging
        FHB-A COR-B's registration condition:
            ||C_n T_g C_n||_2 <= 12 n^{3/2} sqrt(2n-1) / ell   (order n^2)
            ||C_n T_g C_n||_1 <= 24 n^3 sqrt(2n-1) / ell       (order n^{7/2})
        via ||h_0 Q||_op^2 <= (6n-3)/ell^2, established by an exact PSD route
        (Gram-matrix argument; no eigenvalue radicals, no numerics); sharper
        than FHB's headline at every integer n >= 1; at n = 2 by the exact
        chain 24 sqrt(6) < 192 <=> 6 < 64. COR-A carried: NO HS-grade
        order-sharpness claimed anywhere.
  (m-2) a LICENSE REMOVED from a standing negative (WB7 / formal-Lambda no
        longer needed for the site-(i) closure).
  (m-3) the RESIDUE'S TYPE derived: symmetry-breaking, not merely "a rate".

W-3 STATUS (does not amend the MAP): C-L2's (o1)(o2)(o3) CERTIFIED of record;
  (o4) "Supplies S2b" shown NOT A CONSEQUENCE of (o1)-(o3) at any strength.
  W-3 cannot land on C-L2's own permitted route ALONE. MAP §3.4/§3.5 stand
  unedited: W-3 remains FP-3's sole displayed discharger; {W-1, W-2, W-3}
  remains the unique minimal completion set. What is sharpened is W-3's COST.

FP-3 : STANDS.
WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS. This artifact
  RETIRES NOTHING; retirement is the registrar's act alone.
  SCAD_HS_SCALING_EXPONENT_UNDERIVED — untouched, STANDS.
  E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER — untouched, STANDS.
  NO witness created.

FENCES HELD: alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false. No value, no number as magnitude, no
  measured-constant comparison, no frozen constant. Sharp M(t) unmodified
  (D6'). Equal-time HS route NOT resurrected (C6 respected as FALSE, cited
  only as exclusion). No baseline normed. No Phase-1 object consumed. No
  cellulation datum consumed. No register/tracker/road/plan/continuation file
  read. "Q-..." EXPECTED-UNLOCATABLE, not chased. No git action. Output path
  probed ABSENT before first write.

NOT REFUTED: every negative here is ONE-SIDED. No divergence is exhibited for
  tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1, or
  any wall quantity; all remain UNDECIDED. The +infinity of §4 attaches to a
  CLASS SUPREMUM and to the member h_0's own sandwich — objects of the
  argument, not objects of the closure.

NO STRONGER READING: nothing in this artifact bounds, values, or decides any
  closure quantity. Its positive content is (m-1) alone — per-(n, cell)
  enclosures of the C-L2 GENERATOR at the wall's compressions — and its
  negative content concerns INSTRUMENTS, not magnitudes.

CAS: fresh venv (w3venv), sympy 1.14.0, 50/50 PASS, script and verbatim
  output at §9 by file-append.
SEALS: 14 consumed artifacts, every sidecar verified with shasum -a 256 -c
  run FROM THE ARTIFACT'S OWN DIRECTORY; 6 digests cross-checked against
  CERT §1's independent table and in agreement (§1).
SWEEP CUTOFF: 2026-08-15, this session; terms and bounded negative declared
  at §1.2; the bridge run was LOCATED BY SWEEP and identified at bytes.
```

---

*END — STAGE8_W3_GCM_HS_TYPE_O3SR_V001*
