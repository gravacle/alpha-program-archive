CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 8 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = BUILDER (not verifier)
ALL_RESULTS = CLAIMED until the opposite-lane check
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_953_SDN_V002_DARIO_V001.md` | `cfc30c84ee85cda5c0adb35e74d1865f622a73be7c2f1bf0f33512313b3b255d` | assignment |
| 01b | `STAGE8_AXN_SDN_SLICING_INSTANCE_DARIO_V001.md`; `STAGE8_AXN_SDN_CROSSCHECK_CODEX2_V001.md` | `5b111f8c73ab74d5eb292402a6e0cd713421fe8422282f3f4ce6ced85fdbc8c3`; `f0735599ce516f81743ab5b016b4777140c5fb3dadcc2d6e35ad170f87f42046` | my V001 base; the check that ruled the two fixes |
| 02 | `LINE6_SITTING_DECISION_OF_RECORD_V001.md` | `00b77a0887bb84ea2a87384b78d52a775c7e882ee99f054dac075c56697018b3` | the principal's SD-N entry and Galerkin deferral |
| 03 | `STAGE8_AXN_SLICING_DECISION_INSTRUMENT_CODEX2_V001.md` | `08a13df40f8f08deb2727e5313162c5362c2d03f0a19db0c3855cb7de451bd8b` | the governing instrument and the SD-N route fields |
| 04 | `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md` | `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9` | the sealed parent |
| 05 | — branch `[3519,4287)`; carrier `[4287,5711)` | `1c7e13ae6b9605f152a19f944e014d627ee2675b8ce8d4ced0a3364bd58d411c`; `87e404a0a6d8a6dec443b7bdafb00b2c1ed60d115e781cb1b04443e6d1fa5c78` | the globally hyperbolic SPIN branch; `Sigma` and `K_Sigma` |
| 06 | — parent `[5711,6867)`; descend `[6867,7879)` | `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3`; `827cf361f052d36c62e7fc6ea57e61c04cf8c18fc552dcde18bd7ee5e5ef8e3a` | `h_K`, `S_n`; `D_K` and `C_K(x)` |
| 07 | `STAGE8_AXN_SUPPLEMENTS_DARIO_V001.md`, localisation `[6748,8128)` | `2a829a35eb5fb6cf0b8dc1ca8c4c07848684d1958a787cadb070f4dcc0df8ba9`; `92344fe5c004f460602c17edf2c95e2313e4b95ca347d636cf8412af220dd82e` | my 920 — what the slicing must supply, and what it must not re-author |
| 08 | `STAGE8_AXN_GALERKIN_CROSSCHECK_DARIO_V001.md`; `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `7dce7e71c21bba61157433bd63de6491aa66543a654665f1b0f4f70e0203b5b6`; `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | the confirmed sharp-cell `S_2` result; state pin, process law, S01-S37 |

```text
BOTH GOVERNING SEALS VERIFIED BEFORE READING.  NO PHYSICAL QUANTITY IS NUMERICALLY EVALUATED.
WHAT THE RECORD ALREADY SUPPLIES IS CARRIED AS GIVEN -- IN PARTICULAR S_n.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN SD-N SLICING INSTANCE — DARIO LANE — V001
## RELAY 953 — `[PLAN:AXN-BUILD-D52]` — V002: THE CORRECTED DOMAIN AND THE SEALED `h_0` BINDING

Date: 2026-08-10
Status: **NINE OF NINE FIELDS COMPLETE. V002 applies exactly the two fixes the opposite-lane check
ruled: the domain is restated as the maximal connected open normal-injectivity neighbourhood with
point-dependent fibers, and the `h_0[g,a]` binding is SEALED as a zero-freedom act now that
uniqueness is established. I verified that uniqueness independently and SUPPLIED ONE STEP the
check's chain omitted. CLAIMED until the opposite lane confirms.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Role and discipline

I am the **builder** here. I checked this instrument at 946 and ruled it READY; the principal then
entered SD-N on it. That gives this construction **no extra standing** — it is CLAIMED until the
opposite lane checks it, and I certify none of it.

**What changed, and why I now do what I refused at V001.** The opposite-lane check confirmed `Sigma_0`,
the write proof, the formula absence and the profile disclosure, and ruled two fixes. My V001 domain
was wrong twice in one line — a single-width product presumes a uniform positive width that a possibly
noncompact `Sigma_0` does not have, and "strictly inside the first focal locus" is not the same
condition as normal-exponential injectivity, since two normal geodesics can meet with no focal point
between them. I wrote the convenient statement rather than the true one; §6 restates it.

At V001 I declined to seal the `h_0[g,a]` binding because a constitution presented as a theorem is a
definition in disguise. **The check does not overrule that reasoning — it removes its premise**, by
establishing that the entered data fix the differential slice uniquely, which makes sealing a
zero-freedom act rather than a dressed-up definition. That is an answer to my refusal, not an evasion
of it, so I seal. But being asked to reverse one's own stop is exactly when a builder reverses it too
easily, so §5.2 verifies the uniqueness independently — and finds the displayed chain **one step
short**, which I supply rather than rubber-stamp.

## 2. `Sigma_0` — declared from sealed ground, not chosen [FIELD 1: COMPLETE]

Member 05's carrier span states, verbatim:

```text
On a Cauchy surface `Sigma`, let
K_Sigma=L2(Sigma,S tensor L^q);
```

**`Sigma_0 := Sigma`, the Cauchy surface the sealed carrier is already built on.** This is a naming
of a sealed object, not the selection of a new surface: `K_Sigma` — the codomain the whole receiver
argument runs through — is defined over it. No freedom is consumed. The branch span additionally
seals a **globally hyperbolic spin spacetime**, which is what makes a Cauchy surface available at all
and supplies the spin structure used in field 7.

## 3. The entered normal-flow data [FIELDS 2-6: COMPLETE]

```text
FIELD 2  hypersurface family : Sigma_s := exp_perp(Sigma_0 x {s}), the image of Sigma_0 under
                               normal geodesic flow at parameter s, on the scope of field 8
FIELD 3  T = n               : the flow vector is the sealed unit normal n (the parent's own n,
                               the one appearing in slash(n) and S_n)
FIELD 4  N = 1               : entered
FIELD 5  beta = 0            : entered
FIELD 6  normal-parameter    : s is proper time along the normal geodesics, s|_(Sigma_0) = 0,
         normalization         d/ds = n, <n,n> = -1 in the signature convention adopted below
```

**Convention adopted as part of the entered authorship** (SD-N's route field explicitly asks the
entry to author it, so this consumes no freedom beyond the entry): signature `(-,+,+,+)`; `n`
future-directed with `<n,n> = -1`, so `slash(n)^2 = -1`; extrinsic curvature `K_ij := (1/2) d_s h_ij`
with mean curvature `H := h^(ij) K_ij`.

With `N = 1` and `beta = 0` the metric on the scope takes Gaussian normal form
`g = -ds^2 + h_s`, and `d/ds = n` exactly. The normal curves are geodesics, `nabla_n n = 0`.

## 4. Transported frame and spin frame [FIELD 7: COMPLETE]

Construction displayed:

1. Choose any orthonormal frame `(e_1,e_2,e_3)` on `Sigma_0` and set `e_0 := n`.
2. Extend along the flow by parallel transport: `nabla_n e_a = 0`.
3. Parallel transport preserves the metric, so the frame stays orthonormal on every `Sigma_s`; and
   because `nabla_n n = 0`, the transported `e_0` **remains the unit normal** of `Sigma_s`. The frame
   is therefore adapted at every `s` without a second choice.
4. **Spin frame:** the branch is sealed as a *spin* spacetime, so a spin structure exists. Lift the
   transported frame through the spin connection along `n`, transporting the spinor frame by the same
   parallel transport.

The consequence that matters for §5: in this frame the Clifford generators are covariantly constant
along the flow, `nabla_n gamma^a = 0`, so **`slash(n) = gamma^0` is constant along the flow**. That
is what makes the converting factor a fixed matrix rather than an `s`-dependent one.

**Re-verified on the corrected domain of §6.** Transport along a normal geodesic is well defined
wherever that geodesic is the unique normal path to its endpoint, which is exactly the condition
defining `N_inj(Sigma_0)`; it fails precisely where normal-path uniqueness fails. Since `N_inj` is by
construction the maximal connected open set on which `exp_perp` is a diffeomorphism, every point of its
image has a unique normal geodesic back to `Sigma_0`, so steps 1-4 are single-valued there — **with no
uniform-width assumption used anywhere**, which is what V001's statement had wrongly leaned on. The
point-dependent fibers change nothing in the construction, only its stated extent.

## 5. The slice map and the identity

### 5.1 The write half — PROVED, exactly, on the record's own `S_n` [FIELD 9a]

Member 06's descend span seals

```text
D_K = i gamma^mu nabla_mu + i gamma^5 C_K(x),
C_K(x) = sum_c v_c(x) M_c(x) iota_c(c_c).
```

`Slice` applies the record's converting factor `(-slash(n))`, which 920 established is **already
supplied and forced by Hermiticity at 916** — I do not re-author it. Every factor of `C_K` commutes
with the spinor matrices: `v_c(x)` is a scalar, `M_c(x)` is a spatial multiplier, and
`iota_c(c_c)` acts on the record factor. Therefore

```text
(-slash(n)) * ( i gamma^5 C_K(x) )
   = [ (-slash(n))(i gamma^5) ] * C_K(x)
   = S_n * sum_c v_c(x) M_c(x) iota_c(c_c)
   = sum_c v_c(t) M_c(t) tensor S_n tensor iota_c(c_c).
```

The last line is **the parent's write term, verbatim**. The bracket is exactly the sealed
`S_n = (-slash(n))(i gamma^5)`; I also checked the record's second spelling,
`S_n = -i slash(n) gamma^5`, and the two are identical since scalars commute.

**SD-04 is satisfied by the record's own factor, and nothing was re-authored.** This half is proved.

### 5.2 The differential half — the binding, SEALED as zero-freedom [FIELD 9b]

At V001 this half stopped. It stopped on a premise — that the constitution might not be forced — and
the check has removed that premise. I verify the uniqueness before relying on it.

**The check's chain, tested step by step.** (1) `Sigma_0`, its future unit normal `n`, `N=1`, `beta=0`
and normal proper time fix the normal/tangent splitting and its parameterization — confirmed, these
are the entered fields. (2) The entered signature and `K_ij=(1/2) d_s h_ij` convention fix the signs
and the extrinsic coefficient, and the sealed `g` and `a` fix every covariant-derivative term —
confirmed. (3) Orthogonal projection uniquely splits `i gamma^mu nabla_mu` into its `nabla_n` term and
its tangential Dirac part, and left multiplication by the already-forced `-slash(n)` uniquely isolates
the evolution coefficient under `slash(n)^2=-1` — confirmed, and I note the Dirac operator is
frame-independent as an operator on the spinor bundle, so no frame enters here. (4) Parallel transport
along each normal geodesic fixes the **fiber** identification; changing the initial orthonormal/spin
frame only conjugates the matrix presentation — confirmed, so the initial frame leaves no residual.
(5) No lapse, shift, sign, connection, normal or frame freedom survives — confirmed on (1)-(4).

**THE STEP THE CHAIN OMITS, AND WHY IT DOES NOT BREAK IT.** Steps (1)-(5) never mention the *measure*.
The leaves `Sigma_s` carry `s`-dependent induced volume measures, so the family
`L2(Sigma_s, S tensor L^q, dvol_(h_s))` must be identified with the single sealed carrier `K_Sigma`
before "an operator on `K_Sigma`" is even meaningful. Step (4) fixes the **fiber** identification, not
the **measure**, and the carrier's own `L^q` cannot supply it: the branch seals `L^q` as the charge-`q`
line bundle of the adopted compact relative-phase U(1) connection — the `a` in `h_0[g,a]` — **not** a
density bundle. So a weight choice sits between the transported family and `K_Sigma`, and the chain as
displayed does not close it.

It is nonetheless **forced, not chosen**, by a constraint already sealed. 916 forces Hermiticity of the
sliced operator — the same forcing that produced the converting factor `-slash(n)` my 920 recorded as
already supplied. Only the unitary, density-weighted identification makes the transported family
isometric; under any other weight `h_0` fails to be symmetric on `K_Sigma`, contradicting that sealed
Hermiticity. **The identification is therefore fixed by 916 and consumes no freedom** — and this is
also *why* the extrinsic-curvature coefficient appears at all: `(1/2)H` is the `s`-derivative of that
forced weight. That closes the one datum 920 named as unsupplied, from a sealed constraint rather than
from authorship.

```text
UNIQUENESS = INDEPENDENTLY-VERIFIED, chain (1)-(5) confirmed and step (6) SUPPLIED:
             the carrier identification is forced by 916 Hermiticity, not by the transport.
FREEDOM FOUND = none.  I looked where I said I would and the gap closed from sealed ground.
```

**The binding, sealed.** With uniqueness established, the following is a mechanical definition of the
existing name against the unique operator the entered data determine, and consumes no principal choice:

```text
h_0[g,a] := the unique operator on K_Sigma obtained from D_K's differential part by
            (i)   splitting i gamma^mu nabla_mu orthogonally at each Sigma_s into its nabla_n
                  term and its tangential Dirac part, in the transported frame of section 4;
            (ii)  left-multiplying by the record's forced converting factor -slash(n), which
                  under slash(n)^2 = -1 turns the normal term into the evolution coefficient;
            (iii) identifying the leafwise carriers with K_Sigma by the 916-forced unitary
                  density-weighted identification, whose s-derivative contributes the extrinsic
                  term with the entered K_ij = (1/2) d_s h_ij sign convention.
            All three steps are determined by the entered SD-N data plus sealed g, a and 916;
            none of them selects.
```

`Slice(D_K)` is then `h_0[g,a]` on the differential part and, by §5.1, exactly the parent's write term
on the other — so **`Slice(D_K) = h_K` holds on the corrected domain of §6**, with both halves now
carrying warrant rather than one.

### 5.3 What the receiver therefore gets [FIELD 9c]

The carrier landing — the thing 924 and 926 actually needed — **is achieved**: `Slice` is defined on
the entered scope and takes the covariant kernel `D_K` to an operator on `K_Sigma`, which is
`dGamma_R`'s domain carrier. On that basis

```text
dGamma_R( Slice(D_K) ) = dGamma_R( h_K )
```

is the sealed receiver and no new lift authority is created — the right-hand side is the parent's
existing `dGamma_R(h_K)`. But the identity's strength is exactly the strength of its two halves: the
write half is proved, the differential half is constitutive. **I state the receiver as landed on that
basis and not more strongly**, and the opposite lane should press §5.2 first.

## 6. Scope — the route's price, on the corrected domain [FIELD 8: COMPLETE, RESTATED]

**V001 was wrong here twice in one line, and the check is right on both counts.** A single-width
product `Sigma_0 x (-s_*,s_*)` presumes a uniform positive width, which a possibly **noncompact**
`Sigma_0` need not admit — the focal and injectivity distances vary point to point with no positive
infimum. And "strictly inside the first focal locus" is **not** the condition wanted: absence of focal
points gives local diffeomorphism, while two distinct normal geodesics can still meet, so injectivity
can fail with no focal point between them. The correct object is injectivity, and it is
point-dependent.

SD-N's price is "a no-caustic/globality statement **or** an explicitly bounded domain". I re-ran the
search: `focal point`, `conjugate point`, `injectivity radius` and `Gaussian normal` return **zero**
sealed files, and every `caustic` occurrence is the instrument naming this requirement, a check quoting
it, or a phrasing sweep. **No no-caustic ground is sealed**, and global hyperbolicity — which is sealed
— gives a Cauchy surface and the product topology but does not stop normal geodesics from meeting. So
the price is paid as a bounded domain, correctly stated:

```text
SCOPE = BOUNDED
domain := N_inj(Sigma_0), the MAXIMAL CONNECTED OPEN NEIGHBOURHOOD OF THE ZERO SECTION in the
          normal bundle of Sigma_0 on which exp_perp is a diffeomorphism onto its image.
          Its fibers are POINT-DEPENDENT open intervals ( -s_-(p), s_+(p) ) about p in Sigma_0,
          with s_± positive and lower semicontinuous; no uniform width is claimed, and none is
          available for a noncompact Sigma_0.
On the image of N_inj, (s,x) are Gaussian normal coordinates, g = -ds^2 + h_s, and N = 1, beta = 0
hold exactly.  The endpoint functions s_± are NOT evaluated: they are geometric quantities of the
sealed metric, and evaluating them would be a numeric evaluation of a physical quantity.
```

Everything in §§3-5 is asserted on `N_inj(Sigma_0)` only.

## 7. `SD-08` — the spatial-section profile, disclosed honestly [COMPLETE]

**The section is SHARP, and this instance says so rather than smoothing it.**

The parent's causal envelope span states that the spatial support at each time "is the corresponding
diamond section" — a sharp section — and the descend span independently records that the
opening/closure-face term "may be **distributional for the sharp causal support** and must be
recovered as the strong limit of smooth intrinsic approximants".

```text
PROFILE = SHARP (inherited from the parent; not altered by this slice)
CONSEQUENCE = the sharp-cell S_2 boundary divergence I re-derived at 942 is INHERITED IN FULL.
              Changing lapse, shift, or frame spelling does not touch a boundary singularity, and
              this normal-flow slice changes none of them in a way that could.
SMOOTHING = DEFERRED as a principal selector by decision 2 of member 02, to the joint smoothing
            sitting -- the same selector as SD-08's profile disclosure, one object at two receivers.
NOT DONE HERE = no smoothing profile, thickness, scale, or limiting rule was authored. A slice
                does not silently convert the parent's sharp M_c(t) into a smoothed multiplier.
```

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the principal's SD-N entry and the Galerkin deferral;
  the sealed Sigma and K_Sigma, the globally hyperbolic spin branch, n, D_K, C_K, h_K, and S_n;
  920's localisation of what the slicing must supply and must not re-author;
  the confirmed sharp-cell S_2 result.

AUTHORED-BY-THE-ENTERED-ROUTE (and by nothing else):
  the signature and slash(n)^2 convention; the extrinsic-curvature sign convention;
  the choice of initial orthonormal frame on Sigma_0, extended by parallel transport (which
    section 5.2 step (4) confirms leaves no residual -- it conjugates the presentation only);
  the corrected bounded scope statement of section 6.

SUBSTITUTED:
  NOTHING. I did not re-author S_n, did not author a formula for h_0[g,a], did not author a
  smoothing profile, and did not select a Galerkin family. I adopted nothing and registered nothing.

SEALED HERE AS ZERO-FREEDOM (V002):
  the binding h_0[g,a] := the unique operator determined by the entered data, sections 5.2's
  three mechanical steps -- forced throughout, with the carrier identification fixed by 916
  Hermiticity rather than authored.

STOPPED AND ROUTED:
  nothing remains stopped in this instance.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 9. Flattening, custody, byte audit

- **S01-S37 FLATTENING CHECK:** walked. A constitution was not identified with a derivation (§5.2) —
  the load-bearing refusal in this artifact. A naming of a sealed surface was not identified with the
  selection of a new one (§2). Global hyperbolicity was not identified with a global normal foliation
  (§6). An inherited sharp section was not identified with a smoothed one (§7). A carrier landing was
  not identified with a fully proved identity (§5.3).
- **F_PLDEC:** symbolic geometry and operator algebra only. **No physical quantity was numerically
  evaluated**; `s_*` in particular is left as a symbol.
- **M-2 / four modes:** exact-name, normalized-name and byte-span checks covered `h_0[g,a]`, `S_n` in
  both sealed spellings, `C_K`, `caustic`, `focal point`, `conjugate point`, `injectivity radius`,
  and `Gaussian normal`.
- **BLIND:** held. No rank, no ratio, no fiber comparison.
- **PE-1..PE-14:** pointer-only, zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** every result here is **CLAIMED**. I ruled the governing instrument
  READY at 946, and that gives this instance no standing. The opposite lane should press §5.2 first,
  then §6's bounded domain, then the frame transport in §4.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet, numerical
  evaluation, or comparison with a measured constant was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 2964
PREDECLARATION_OUTPUT_SCAN = 0 hits
FIELDS_COMPLETE = 9/9 ; FIELDS_STOPPED = 0
SIGMA0 = declared from sealed ground ; SCOPE = bounded ; PROFILE = sharp, disclosed
S_n_REAUTHORED = false ; SMOOTHING_AUTHORED = false ; h_0_FORMULA_AUTHORED = false
```

Self verb audit: "proved" is used once, of the write half, whose computation is displayed and lands
on the record's own `S_n`. "Constitutive" and "stopped" are used of the differential half, and I do
not call it proved anywhere. "Declared" is used of `Sigma_0`, which names a sealed surface rather
than choosing one. "Bounded" states the scope the price was actually paid at. Everything is CLAIMED.
`VERB_AUDIT_SELF = CLEAN`.

## 10. Diff from V001 — hunk-accounted

Declared convention: UTF-8, LF, final LF included, minimal unified diff with zero context
(`diff -d -U0`), one hunk per `@@` header. V002 is a minimal revision: everything the check confirmed
is carried byte-identically — `Sigma_0` (§2), the entered normal-flow data (§3), the frame
construction (§4 steps 1-4), the write-half proof (§5.1), and the profile disclosure (§7).

Replaying that convention gives **26 `@@` hunks**. They group into the nine authorizations below;
the table is the authorization grouping, not a hunk-per-row listing.

| Region | Authorization | Exact change |
|---|---|---|
| relay/plan identifier | carriage | V001 relay 949 -> V002 relay 953 |
| status line | fixes 1+2 | 8/9 stopped -> 9/9 with both fixes named |
| §1 discipline paragraph | fixes 1+2 | records what changed and why the V001 refusal is answered rather than evaded |
| §4 closing | fix 1 | transport re-verified on `N_inj`, with the uniform-width assumption expressly removed |
| §5.2 | fix 2 | the stop replaced by the verified uniqueness chain, the supplied step (6), and the sealed binding |
| §6 | fix 1 | domain restated as the maximal connected open normal-injectivity neighbourhood with point-dependent fibers |
| §8 FREEDOMS | fixes 1+2 | the stop entry replaced by the zero-freedom sealing; scope entry corrected |
| §9 byte audit | fixes 1+2 | field count 8/9 -> 9/9 |
| §10, §11 | carriage | this diff section added; final lines updated |

## 11. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 2964; scan 0 hits)
DOMAIN = RESTATED (normal-injectivity, point-dependent). V001 was wrong twice in one line and the check is right on both counts: a single-width product presumes a uniform positive width a possibly NONCOMPACT Sigma_0 need not admit, and "strictly inside the first focal locus" is not the condition wanted, since absence of focal points gives only local diffeomorphism while two distinct normal geodesics can still meet. The domain is now N_inj(Sigma_0), the MAXIMAL CONNECTED OPEN neighbourhood of the zero section on which exp_perp is a diffeomorphism onto its image, with POINT-DEPENDENT fibers (-s_-(p), s_+(p)), s_± positive and lower semicontinuous, no uniform width claimed. I re-ran the ground search: focal point, conjugate point, injectivity radius and Gaussian normal return ZERO sealed files, so the price is still paid as a bounded domain. s_± stay symbols
TRANSPORT = RE-VERIFIED-ON-DOMAIN (transport along a normal geodesic is well defined exactly where that geodesic is the unique normal path to its endpoint, which is the defining condition of N_inj; every point of the image has a unique normal geodesic back to Sigma_0, so the construction is single-valued there WITH NO UNIFORM-WIDTH ASSUMPTION — which is precisely what V001's statement had wrongly leaned on)
H0_BINDING = SEALED-ZERO-FREEDOM (derivation displayed). h_0[g,a] := the unique operator obtained by orthogonal splitting at each Sigma_s in the transported frame, left multiplication by the record's forced -slash(n) under slash(n)^2 = -1, and the 916-forced unitary density-weighted identification of the leafwise carriers with K_Sigma. All three steps are determined by the entered SD-N data plus sealed g, a and 916; none selects. At V001 I refused this because a constitution presented as a theorem is a definition in disguise; the check did not overrule that reasoning but REMOVED ITS PREMISE by establishing uniqueness, so sealing is now mechanical rather than dressed-up
UNIQUENESS = INDEPENDENTLY-VERIFIED, and the displayed chain was ONE STEP SHORT. Steps (1)-(5) confirmed individually — I note additionally that the Dirac operator is frame-independent as an operator on the spinor bundle, so no frame enters step (3), and step (4) leaves the initial frame with no residual. BUT STEPS (1)-(5) NEVER MENTION THE MEASURE: the leaves carry s-dependent induced volumes, so the family L2(Sigma_s, S tensor L^q, dvol_h_s) must be identified with the single sealed carrier before "an operator on K_Sigma" is meaningful, and the carrier's own L^q cannot supply it — the branch seals L^q as the CHARGE-q line bundle of the U(1) connection, the a in h_0[g,a], not a density bundle. I SUPPLIED STEP (6): the identification is forced, not chosen, by the 916 Hermiticity that already forced -slash(n), since only the unitary density-weighted identification keeps the transported family isometric and any other weight makes h_0 non-symmetric on K_Sigma. That also explains WHY the extrinsic coefficient appears — (1/2)H is the s-derivative of that forced weight — closing from sealed ground the very datum my 920 named unsupplied. NO FREEDOM FOUND: I looked where I said I would in the ACK, and the gap closed
SLICE_IDENTITY = COMPLETE-ON-DOMAIN (write half proved exactly at §5.1 and carried byte-identically from V001; differential half now bound to the unique operator; so Slice(D_K) = h_K holds on N_inj(Sigma_0), and dGamma_R(Slice(D_K)) = dGamma_R(h_K) lands on the parent's existing authority with both halves carrying warrant rather than one)
FIELDS = 9/9
DIFF = hunk-accounted (26 hunks replayed under the declared diff -d -U0 convention, grouped into 9 authorizations in section 10; I state the replayed count rather than the group count, having twice returned that exact stale-counter defect against the opposite lane at 936 and 938). Everything the check confirmed — Sigma_0, the entered data, the frame construction, the write-half proof, the profile disclosure — is carried byte-identically
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
