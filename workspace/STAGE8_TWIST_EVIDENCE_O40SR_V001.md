# TWIST-EVIDENCE DETERMINATION — COMMISSION O40SR — READ AND CLASSIFIED AT BYTES

COMMISSION O40SR — BUILD LANE OF A COMMISSIONED PAIR — 2026-08-15
READ AND CLASSIFY ONLY. NOTHING IS DERIVED, EXTENDED, ADOPTED, ADVOCATED,
RECOMMENDED, OR SUPPLIED. NO MISSING OBJECT, PRINCIPLE, OR CLAUSE IS AUTHORED
ANYWHERE BELOW — NOT IN A TOY, A FOOTNOTE, OR AN ASIDE. GAPS ARE DESCRIBED,
NEVER FILLED.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

**THE HARD BAR, RESTATED BEFORE THE FIRST FINDING.** The member under test —
DoR-020-A4, J12-G STRICT PUSHOUT — is a RATIFIED AUTHORED PRINCIPAL ACT. This
artifact does not void it, does not declare it void, does not recommend voiding
it, does not argue for reopening it, and does not characterise the evidence as
sufficient or insufficient for any act. It reports **evidence status at bytes**
and stops. Voiding is the registrar's on a finding; the ruling is the
principal's. No missing object is authored here.

OUTPUT-PATH PROBE, BEFORE ANY WRITE:
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TWIST_EVIDENCE_O40SR_V001.md`
— ABSENT (`ls` exit 1, "No such file or directory"); sidecars
`...V001.md.sha256` / `...V001.md.seal.sha256` — ABSENT. Probed at commission
start, before the first byte was written.

CORPUS (the only readable surface; both roots swept, plus the DoR series):
```text
ROOT1 = /Users/bgm/MB Work/alpha-program-archive/workspace
ROOT2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
DOR   = /Users/bgm/MB Work/alpha-program-archive/supervision/DOR_*.md
        (expressly permitted and required; nothing else in that directory
         was opened)
```

---

## §0 — IMPORT AUDIT, STATED FIRST

```text
*** THE WORDS "HOLONOMY", "MONODROMY", "TWIST", "COCYCLE", "TRANSITION
    FUNCTION", "PUSHOUT", "AUTOMORPHISM", "PHASE", "CHERN CLASS" AND
    "COUNTERMODEL" ARE USED BELOW ONLY TO *DESCRIBE* WHAT THE CORPUS DISPLAYS.
    NO THEOREM ABOUT ANY OF THEM IS IMPORTED, AND NOTHING IS CONCLUDED FROM A
    NAME. ***
```

| Import | Source | Where used | Does the finding survive without it? |
|---|---|---|---|
| "holonomy", "monodromy" as descriptive shape names | standard differential geometry, outside this corpus | §3 (Q3) sweep vocabulary and the definition of what would count as a twist | **YES.** The commission supplied these as search vocabulary. Every grade rests on quoted corpus bytes. The corpus itself writes `contact-holonomy` of its own accord at `DETERMINATION_MAP:357`, so at the one site that matters the *name* is record-native. |
| "twist" as a shape name | commission vocabulary, and record-native | §3 | **YES.** The corpus uses `twist`, `contact twist`, `CONTACT_TWIST_SEEN`, and `Tw_f` on its own; see the sweep counts in §8. |
| "pushout", "signed pushout", "automorphism" | standard category/algebra usage | §§1–4 | **YES.** All three are record-native tokens quoted from the ruling and its basis. |
| "U(1)-valued transition function", "triple-overlap cocycle condition", "charge", "phase" | commission vocabulary for the Q5 reach test | §5 | **YES.** Used only as search strings and as descriptions of displayed corpus equations. `g_ij = exp(i theta_ij) in U(1)` and `g_ij g_jk g_ki = 1` are the corpus's own displayed bytes, quoted whole. |
| "symbol collision" | record-native — the determination map has a section titled "Symbol collisions bearing on this question" (§1.3) | §§3, 5 | **YES.** |

NO OTHER IMPORT IS MADE. In particular: no theorem about holonomy, no
classification of flat bundles, no vanishing or non-vanishing criterion, no
cohomological interpretation, no consequence of any composition law, and no
statement about what a twist "would" imply physically is imported or applied.
Where the corpus states a law, this artifact reports the law and stops.

---

## §0.1 — THE UNIVERSE, ESTABLISHED AND DEFENDED

A prior build tested six artifacts while declaring it had tested "every
artifact carrying the object", when the corpus had twenty. The universe here is
therefore constructed by sweep, stated in full, and defended.

**Root sizes.** ROOT1 = 3676 entries; ROOT2 = 3133 entries.

**Universe construction, in three widening passes.**

```text
PASS A  token `I_contact`
        -> 30 file instances, 16 distinct basenames.
        REJECTED AS THE UNIVERSE: too narrow. It misses
        STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003/V004 and
        STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001 — the three artifacts
        that actually run the twist detector — and it misses
        STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001, which is
        the ruling's own cited source for "the exact contact stock and its
        open physical term" and which uses different notation entirely.

PASS B  any case-insensitive `contact`
        -> 686 file instances, 383 distinct basenames.
        REJECTED AS THE UNIVERSE: too broad. `contact` is used in this corpus
        for at least four distinct objects (see TOY_SEPARATION / symbol
        collisions, §7).

PASS C  the J12 contact-glue object itself:
        I_contact | eta_f^boundary | Eta_f^rep | J12-G | J12_CONTACT_GLUE |
        R_N^phys | contact glue | contact-glue
        -> 40 file instances, 21 distinct basenames.  ADOPTED, then extended.
```

**ADOPTED UNIVERSE = PASS C + two named extensions = 44 file instances,
23 distinct basenames.** The extensions are
`STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md` (the ruling's
cited contact-stock source, 2 instances) and
`STAGE8_7A_HANDOFF_PACKAGE_V001.md` (2 instances), which carries the void-5
ledger row. The object under test, `DOR_020_A4_...md`, is read whole and
counted separately.

The 23 basenames:

```text
STAGE8_7A_HANDOFF_PACKAGE_V001
STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001
STAGE8_DISCREPANCY_COCYCLE_O38SR_AUDIT_V001      (ROOT1 only)
STAGE8_DISCREPANCY_COCYCLE_O38SR_V001            (ROOT1 only)
STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V001
STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002
STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V003
STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001
STAGE8_TASK5_EQ6_CERT_V003_REVIEW_LANE2_V001
STAGE8_TASK5_EQ6_DETERMINATION_MAP_LANE1_V001
STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003
STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004
STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002
STAGE8_TASK5_EQ6_LOC_FAMILY_V003_AND_EC_LANE3_V001
STAGE8_TASK5_EQ6_MAXWELL_HODGE_PROJECTOR_CERT_LANE2_V001
STAGE8_TASK5_EQ6_MEMBERSHIP_REVIEW_DARIO_V001
STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V001
STAGE8_TASK5_EQ6_PROJECTOR_CERT_V003_LANE1_V001
STAGE8_TASK5_EQ6_PROJECTOR_LOCALITY_REDUCING_CERT_V002_LANE1_V001
STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001
STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001
STAGE8_TASK5_MAXWELL_SYMBOL_CERTIFICATES_LANE1_V001
STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001
```

Twenty-one of the twenty-three are mirrored in both roots; the two
`O38SR` artifacts exist in ROOT1 only. **All 21 mirrored pairs were compared by
digest: 0 divergent pairs — the mirrors are byte-identical, so a finding read
from one root holds at the other.**

**Artifacts outside the adopted universe that were nevertheless consumed** —
because the sweeps in §8 put them on the twist/holonomy/reach question and the
headline had to be reconciled against them:
`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001`,
`STAGE8_COMPLETION_AND_FORCING_O37SR_V001`,
`STAGE8_EM_PARTICIPATION_O4SR_V001`,
`STAGE8_FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_REVIEW_DETERMINATION_V001`,
`STAGE8_SOURCE_COUPLED_RECORD_TRANSITION_LAW_ADOPTION_PROPOSAL_V002`,
`STAGE8_TASK5_EQ6_PASS2_CHECK_AND_ALLRANK_TYPING_LANE1_V001`,
`STAGE8_TASK5_HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001`,
`STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V004`,
`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001`,
and the DoR rulings `DOR_020_A4` (the object) and `DOR_020_A5`.

---

## §0.2 — SEAL LEDGER

Every consumed artifact was verified with `shasum -a 256 -c` **run from that
artifact's own directory**.

```text
ADOPTED UNIVERSE (44 instances):      44 / 44 digests confirmed
  - 40 verified directly by `shasum -a 256 -c <name>.md.seal.sha256` -> OK
  - 2  (STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md, both
       roots): the `.seal.sha256` sidecar is MALFORMED — it separates digest
       from filename with ONE space where `shasum -c` requires TWO, so
       `shasum -c` returns "no properly formatted SHA checksum lines found".
       The RECORDED digest and the file's ACTUAL digest are identical
       (`9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514`)
       in BOTH roots, and the sidecar verifies OK once the separator is
       normalized. This is a sidecar FORMATTING defect, not a content
       mismatch. Reported, not repaired: this lane took no write action on
       any corpus file.
  - 2  (STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md, both roots): no
       `.seal.sha256` sidecar exists; a plain `.sha256` sidecar exists and
       verifies OK from the artifact's own directory in both roots.

OUT-OF-UNIVERSE CONSUMED:
  DOR_020_A4_J12_CONTACT_GLUE_STRICT_PUSHOUT_2026-08-04.md          OK
  DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04.md               OK
  DOR_020_A2_COMPLETED_EXISTENCE_ADOPTED_2026-08-06.md              OK
  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md                       OK
  STAGE8_EM_PARTICIPATION_O4SR_V001.md                              OK
  STAGE8_FIELD_SIGNATURE_PHYS_V003_..._DETERMINATION_V001.md        OK
  STAGE8_SOURCE_COUPLED_RECORD_TRANSITION_LAW_ADOPTION_PROPOSAL_V002.md OK
  STAGE8_TASK5_EQ6_PASS2_CHECK_AND_ALLRANK_TYPING_LANE1_V001.md     OK
  STAGE8_TASK5_HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001.md          OK
  STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V004.md    OK
  LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md          NO SIDECAR OF ANY KIND
                                                  (see FLAG BLOCK)

NO MISMATCH WAS FOUND ANYWHERE. Two sidecar hygiene defects and one wholly
unsealed artifact are reported in the FLAG BLOCK.
```

**Basis-citation check.** DoR-020-A4 cites its basis as
`STAGE8_TASK5_EQ6_DETERMINATION_MAP_LANE1_V001.md` (`76ee3c69…`). Recomputed
at bytes: `76ee3c695b1c0c02986a13ff64d6db93f76e39c6861b40273bd31aed1c3a2eb0`.
**MATCHES.** The ruling's own digest is
`5fd95472bd6f0507a371779505fe91e2c3c4657ee3afc664ca563a3743d668a4`, which is
the digest `STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001.md:58` cites for
"DoR-020-A4 J12 strict signed pushout". The chain is intact.

---

## §1 — Q1: WHAT IS CONTACT DATA, AND WHAT DOES "ACTUAL" MEAN HERE?

### §1.1 The defining text for the contact objects

**`I_contact` — the properties clause.** The J1–J15 constraint system,
`STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md`
(`d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4`, the same
digest the determination map cites as "J1-J15 constraint system" and the V003
review cites as "continuum constraint arm"), `:445-463`, quoted WHOLE:

> ### J13. Boundary/contact coherence
>
> Let `beta_R` be the boundary/contact discrepancy along a refinement.  Path
> independence forces the cocycle law
>
> ```text
> beta_(S compose R)=beta_S+Eta_S(beta_R),            (J13-1)
> ```
>
> up to the declared sign convention.  `Bound` must prove in the one topology
>
> ```text
> beta_R / Vol_R -> 0 on every admitted cofinal family,
> I_contact is closed, invariant, restriction-natural, and contains no
> record-visible bulk cycle class.                    (J13-2)
> ```
>
> The quotient/direct-sum member and its signs must be stated.  Calling every
> off-diagonal path kernel `contact` fails `(J13-2)`.

Note the last sentence, which cuts against any loose reading: naming something
`contact` does not make it contact.

**`C_N` — the intersection.** The determination map, `:286-308`, §3.1, quoted
WHOLE including its closing clause:

> ### 3.1 The exact missing equation
>
> Let
>
> ```text
> C_N:=R_N^rep intersect I_contact,N.                 (D2-1)
> ```
>
> The represented map is derived:
>
> ```text
> Eta_f^rep(Kernbar_N H)=Kernbar_M(j_f H).            (D2-2)
> ```
>
> A full response map on the stated quotient/direct-sum carrier exists only if
> the contact transport obeys
>
> ```text
> eta_f^boundary|_(C_N)=Eta_f^rep|_(C_N)              (D2-3)
> ```
>
> after the declared signs and quotient maps. This is the overlap equality
> isolated as `(Y2-4)`.

**`I_contact,N` — the response/symbol subspace, typed and distinguished.**
`STAGE8_TASK5_EQ6_PROJECTOR_LOCALITY_REDUCING_CERT_V002_LANE1_V001.md:132`:

> `I_contact,N    contact response/symbol subspace, not the A3 action I_N;`

**`I_contact` — the split it is required to produce, and the section heading
that types it.** `STAGE8_TASK5_MAXWELL_SYMBOL_CERTIFICATES_LANE1_V001.md:480-489`,
quoted WHOLE **including its heading and the `or` line**:

> ### 4.2 Contact split - not built
>
> A physical contact split requires a closed invariant ideal `I_contact` and a
> failure-capable direct sum or quotient statement such as
>
> ```text
> Ker_tau = Ker_phys direct-sum I_contact
> or
> Ker_phys = Ker_tau / I_contact.                  (C3-3)
> ```

The heading is the corpus's own type for this object: **"Contact split - not
built"**. It is quoted here because it cuts directly on Q2.

**The contact/Ward stock — what it proves, and on what condition.** The
determination map `:309-324`, §3.2, quoted WHOLE:

> ### 3.2 What the sealed contact/Ward stock proves
>
> The stock proves, conditionally on an actual physical contact term:
>
> ```text
> I_contact is closed, invariant, restriction-natural, and bulk-cycle safe;
> beta_(gf)=beta_g+Eta_g(beta_f);
> beta_f/Vol_f -> 0 on admitted cofinal families;
> OLD_FID + exclusive-region RNL + LR + Ward/bundle compatibility;
> contact and disjoint diamonds compose when a coherent physical section exists.
>                                                                (D2-4)
> ```
>
> These facts are **PROVABLE**. The local-excision artifact expressly leaves
> the positive-source physical contact term and coherent all-rank section
> unproved. None of `(D2-4)` identifies the two maps on `C_N`.

**Contact supports / the contact diamond geometry.** The excision certificate,
`STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md:551-560`:

> ### 2.1 Actual contact geometry
>
> Retain the Q-441 endpoint-contact diamond: old and new cycle corridors have
> distinct interiors and meet only at the recorded endpoint `q`.  Its raw W1
> path/current maps and W4 bundle pullbacks commute exactly.  Both positive-
> source legs have rank one and the target has rank two.
>
> The contact locus changes only which RNL antecedents are active.  It does
> not change the source/target current forms and therefore cannot discharge
> OLD_FID.

### §1.2 The ACTUALITY STANDARD the corpus sets

The word ACTUAL is load-bearing in void condition 5, and the corpus sets its
standard in five places. They agree.

**(A) The claim-tag definition — actuality is INHABITANCE, not consistency.**
Determination map `:67-74`, §1.2, quoted WHOLE:

> ### 1.2 Claim tags
>
> ```text
> PROVABLE      = forced by the sealed/ratified stock cited above;
> PART-PROVABLE = the form or consistency is proved, but actual physical
>                 inhabitance is not;
> YOURS         = a genuine law/member choice requiring the principal's gate.
> ```

**(B) The excision certificate states the distinction and calls it
load-bearing.** `..._LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md:47-55`,
quoted WHOLE:

> The named certificate is now a real mathematical object rather than a
> phrase: it has data, zero-defect equations, a decision procedure on every
> finite candidate, covariance laws, composition, and a common-refinement
> law.  The direct-sum compatibility model inhabits that object.  What the
> ratified stack still does not provide is an actual retained surface family
> whose fixed DoR-019 metric and Q-408 analysis maps give certificate terms
> on every positive-source cycle-creating leg.  The distinction is
> load-bearing: compatibility is proved; physical family-level inhabitance is
> not.

The same artifact's symbol table, `:128-129`, states the contrast as a
definition:

> ```text
> direct-sum model term      consistency witness for the certificate class;
> physical certificate term  proof on an actual retained fixed-data member.
> ```

*(Byte-check note: at `:128-129` the two lines are
`direct-sum model term      consistency witness for the certificate class;`
and `physical certificate term proof on an actual retained fixed-data member.`
— the second line carries no column padding between "term" and "proof" in the
source. The words are reproduced exactly; only the alignment differs.)*

**(C) THE OPERATIVE THREE-PART TEST — the corpus applies "actual" to a
candidate contact datum and rejects it.** This is the clearest statement of
the standard anywhere in the corpus.
`STAGE8_TASK5_EQ6_CERT_V003_REVIEW_LANE2_V001.md:120-131`, quoted WHOLE
including the conclusion clause:

> The key check is whether this data is an actual record-surface contact witness in the
> adopted scope. It is **not**:
>
> 1. It supplies only a positive finite Hilbert pair and a closed-form `d^0` choice.
> 2. It omits the actual surface transport data required for an actual `[EQ6]` contact
>    realization on the finite live tower (path current data, admissible transport,
>    and the `Loc/σ` realization stack under `A1`-scope).
> 3. It therefore is a **pre-A5 countermodel family** (showing logical non-implying),
>    not a direct admitted object of the current surface coherence class.
>
> So A5 void-1 is **not** triggered by this model on the actual scope; `DoR-020-A5`
> remains in force.

**(D) The closing ledger states the same test in one clause.**
`STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md:142`, the F18 row, note column:

> The prior abstract countermodel was not actual record-surface data.

**(E) The excision certificate's input contract puts actuality first.**
`..._LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md:139-143`:

> ### 1.1 Input contract: actual geometry first
>
> Let `r:G->G'` be an actual cycle-creating W1/W4 candidate under the adopted

### §1.3 THE Q1 ANSWER — the standard, stated

**CONTACT DATA** in this corpus is: the closed, invariant,
restriction-natural, bulk-cycle-safe contact response/symbol subspace
`I_contact,N` (J13-2); its intersection with the represented response carrier,
`C_N := R_N^rep ∩ I_contact,N` (D2-1); and the two transports whose behaviour
on `C_N` is at issue — the derived `Eta_f^rep` and the boundary transport
`eta_f^boundary`, which the corpus's own symbol table calls **"the unbuilt
contact transport"** (`DETERMINATION_MAP:87`).

**THE ACTUALITY STANDARD THE CORPUS SETS.** A datum counts as ACTUAL when, and
only when, it is a **record-surface witness in the adopted scope** — which the
corpus decomposes into three requirements, all three necessary:

```text
A1  IT IS AN OBJECT OF THE ADMITTED SURFACE CLASS, not a freely posed
    algebraic structure.  A "positive finite Hilbert pair and a closed-form
    d^0 choice" is expressly declared NOT to satisfy this (CERT_V003_REVIEW
    :123).  A model that is internally lawful is still not actual.

A2  IT CARRIES THE ACTUAL SURFACE TRANSPORT DATA — named exactly as
    "path current data, admissible transport, and the Loc/sigma realization
    stack under A1-scope" (CERT_V003_REVIEW :124-126) — on the finite live
    tower, with the fixed DoR-019 metric and the Q-408 analysis maps
    (EXCISION_CERT :51-53).

A3  IT IS A DIRECT ADMITTED OBJECT OF THE CURRENT SURFACE COHERENCE CLASS,
    not a countermodel family whose purpose is to show logical
    non-implication (CERT_V003_REVIEW :127-128).
```

Equivalently, in the corpus's own tag vocabulary: **consistency is not
actuality.** `PART-PROVABLE` is precisely the tag for "the form or consistency
is proved, but actual physical inhabitance is not". A twist exhibited inside a
consistency model is, by the corpus's own standard, **not** an actual contact
datum — and the corpus says so about the twist specifically (§3 below).

---

## §2 — Q2: DOES ACTUAL CONTACT DATA EXIST?

### GRADE: **SPECIFIED-NOT-CONSTRUCTED.**

The conditions under which contact data would exist are stated exactly, in
several independent places, as **equations a future construction must satisfy**.
No construction is cited anywhere in either root. The corpus states this of
itself, repeatedly and without hedging.

**The prior finding that a contact term is "recorded unproved" — VERIFIED AT
BYTES, not inherited.** Four independent sites, each quoted whole:

**(i) The determination map's symbol table, `:86-89`, quoted WHOLE:**

> ```text
> Eta_rep       = the derived represented-response forward map;
> eta_boundary  = the unbuilt contact transport;
>                 equality on their overlap is exactly D2's issue;
> ```

The word is **unbuilt**, in the corpus's own symbol table, in the ruling's own
cited basis.

**(ii) The determination map's §3.2 closing sentences, already quoted whole in
§1.1:** "The local-excision artifact expressly leaves the positive-source
physical contact term and coherent all-rank section **unproved**."

**(iii) The excision certificate — the artifact just named — confirms it in
its own bytes.** `:604-620`, quoted WHOLE:

> ```text
> contact certificate terms exist exactly if
>   alpha=alpha_0,
>   delta=delta_0,
>   zeta=0 on every exclusive local block,
>   all Def_supp maps vanish,
>   and the W4 equations commute.                 (H2-4)
> ```
>
> Under two coherent certificate terms, H1.8 proves the complete contact
> diamond passes.  On the program's actual fixed family, `(H2-4)` remains
> unproved.  No LR-only classification remains.
>
> ```text
> D_CONTACT_REQUIREMENTS = OLD_FID + EXCLUSIVE_REGION_RNL + LR
> D_CONTACT_UNDER_CERT = PASS / TYPE-P
> D_CONTACT_PHYSICAL_TERM = OPEN / TYPE-U
> ```

`(H2-4)` is the SPECIFICATION. "On the program's actual fixed family, `(H2-4)`
remains unproved" is the NON-CONSTRUCTION. Both are in the same span.

**(iv) The determination map's D2 result block, `:403-408`, quoted WHOLE:**

> ```text
> D2_RESULT = AUTHORABLE
> CONTACT_OVERLAP_EQUALITY_DERIVABLE = false | PROVABLE
> STRICT_GLUE_CONSISTENT = true | PART-PROVABLE
> ACTUAL_PHYSICAL_GLUE_INHABITED = OPEN
> ```

**Corroborating sites, all at bytes.**

- `DETERMINATION_MAP:573-575`: "Nothing proves the joint physical contact
  gluing inhabited. Nothing proves the entire `[EQ6]` fiber inhabited. Those
  remain construction/witness burdens, not contradictions."
- `DETERMINATION_MAP:536-537`, the forward build list, item 3: "after a
  principal J12 gluing ruling, **construct** the actual physical contact
  transport satisfying the chosen overlap law and all J13 certificates" — the
  construction is listed as a FUTURE obligation, after the ruling.
- `EXCISION_CERT:15-16`: `PHYSICAL_EXCISION_CERT_FAMILY = STOPPED_AT(
  NONEMPTY_COVARIANT_ZERO_DEFECT_SECTION_ON_FIXED_DOR019_Q408_DATA)`.
- `EXCISION_CERT:41-42`: `DIRECT_SUM_MODEL = CERTIFIED_MODEL_TERM / TYPE-P`
  and `DIRECT_SUM_MODEL_IS_PHYSICAL_INHABITANT = false`.
- `MEMBERSHIP_REVIEW_DARIO_V001:251-253`: `D_DISJOINT = CONDITIONAL_PASS /
  PHYSICAL_TERM_OPEN`; `D_CONTACT  = CONDITIONAL_PASS / PHYSICAL_TERM_OPEN`;
  `ALL_RANK   = PARTIAL / TYPE-U`.
- `MEMBERSHIP_REVIEW_DARIO_V001:259-260`: "The certificate's **physical family
  term is open on both the disjoint and the contact diamond**, its all-rank
  status is PARTIAL/TYPE-U, and its exact remaining object is a *nonemptiness*
  obligation."
- `LOC_FAMILY_V003_AND_EC_LANE3_V001:430-432` poses, as an open question `(E0)`,
  "whether `c_RL` lies in the actual ambient `I_contact,RL^k` or in
  `(I_contact,RL^k)^perp`?" — i.e. even membership of a candidate vector in the
  actual contact subspace is undecided at bytes.

**The clause that cuts against the grade, quoted rather than suppressed.** The
V003 review states, of the A4 pushout algebra,
`MEMBERSHIP_REVIEW_DARIO_V001:264-268`, whole:

> The A4 no-mixing algebra the build relies on is separately fine: for
> `P_N = (R^rep ⊕ I^contact)/{(c,-c) : c in C_N}` with `C_N = R_N^rep ∩ I_contact,N` an actual
> intersection, the quotient identifies only the overlap class. That statement is true. It is a
> statement about the **quotient carrier**, and it does not by itself discharge OLD_FID/RNL/LR,
> which are conditions on the support behaviour of the analysis maps.

The phrase "an actual intersection" is the strongest pro-existence clause in
the corpus. It is immediately scoped by its own next sentence to "a statement
about the **quotient carrier**" — i.e. it says the set-theoretic intersection
is genuine (which is what makes the pushout injections injective), not that a
constructed contact datum inhabits it. It does not meet A2 of the actuality
standard, and the same artifact three paragraphs earlier types the physical
contact term OPEN.

**Also weighed and not sufficient.** `PASS2_CHECK_AND_ALLRANK_TYPING:108`
records a **built** member — "E1 built member, geometry first | **PASS** |
actual two-path cycle, nonzero conserved current, identity U(1) bundle/field
transport, and all geometric regressions pass". This is an actual built
surface member, and it is the nearest thing in the corpus to actual data. It
carries **no contact datum**: the same artifact's contact section, `:475-483`,
is a *contact-only leakage attack* concluding "neither the identity field horn
nor recorded contact closes the gap", and the built member is a
cycle/current/bundle object, not a contact-overlap object. It does not supply
`I_contact,N`, `C_N`, or `eta_f^boundary`.

**Q2 SUMMARY.** The conditions are stated — `(H2-4)`, `(J13-2)`, `(D2-3)`. The
object is unbuilt: `eta_boundary = the unbuilt contact transport`;
`(H2-4) remains unproved`; `D_CONTACT_PHYSICAL_TERM = OPEN / TYPE-U`;
`ACTUAL_PHYSICAL_GLUE_INHABITED = OPEN`. **SPECIFIED-NOT-CONSTRUCTED.**

---

## §3 — Q3: DOES ANY OF IT EXHIBIT A NONTRIVIAL TWIST? (the commissioned question)

### GRADE: **NO-ACTUAL-DATA-TO-TEST.**

There is no actual contact datum on which the question can be posed. That is
the complete answer, and the corpus states it in its own words at three
independent sites. A twist IS exhibited in the corpus — twice — and both
exhibits are, by the corpus's own explicit classification, models and not
actual data.

### §3.1 What was swept, and what was found

Sweep vocabulary and hit counts are in §8. The record-native twist vocabulary
is: `twist` (315 hits / 66 files), `contact twist` in either order (24 / 10),
`CONTACT_TWIST_SEEN` (10 / 6), `Tw_f` (14 / 4), `contact-holonomy` (2 / 2),
`contact automorphism` (6 / 4), `monodrom` (51 / 18). Structurally equivalent
forms were swept too: an overlap identification differing from the identity
(`u != id`, `u(c_f)!=c_f`), a nonvanishing transition datum on a contact
overlap (`Tw_f != 0`), and phase/holonomy around a closed cycle
(`single-valuedness` 66 / 26, `triple-overlap` 62 / 21, `g_ij` 166 / 37).

**Of the 315 `twist` hits, the overwhelming majority are a different object.**
The corpus's dominant `twist` senses are: the *parity-twisted trace* and
*twisted CD columns* of the Galerkin/sign-crossing work
(`GALERKIN_COMMUTATOR_T12SR`, `SIGN_CROSSING_T10SR`, `OVERLAP_LAW_T11SR`); the
*untwisted S² Dirac spectrum* and *flux twist* of the 7A/DBR work; and the
*twisted incidence matrix* of the orientation determination. None of these is a
contact holonomy. This is recorded in TOY_SEPARATION (§7) as a symbol
collision, not folded into the finding.

### §3.2 EXHIBIT ONE — the declined countermodel `u`, in the ruling's own basis

The determination map, §3.3, `:326-350`, quoted WHOLE — every clause,
including the two that cut hardest against reading this as evidence of an
actual twist:

> ### 3.3 Independent non-derivability test
>
> The missing implication can be tested without choosing a physical member.
> Take a one-arrow model with a one-dimensional closed contact ideal `C`, put
> `C=R^rep intersect I_contact`, and let the Ward differential vanish on `C`.
> Let
>
> ```text
> Eta^rep|_C=id_C,
> eta^boundary|_C=u,                                  (D2-5)
> ```
>
> where `u` is any reality/unit-compatible contact automorphism. On a one-arrow
> category the cocycle is automatic. On a chain choose a multiplicative natural
> family `u_(gf)=u_g u_f`; the boundary discrepancy then satisfies the same
> composition law. A fixed contact element is subextensive against growing
> volume, and no bulk cycle is introduced.
>
> Both `u=id` and `u!=id` satisfy the sealed properties `(D2-4)`. Only the first
> satisfies `(D2-3)`. Therefore the sealed stock does not derive the overlap
> equality. This non-derivability is **PROVABLE**.
>
> The `u=id` model also proves there is no formal inconsistency in strict
> gluing. Actual physical inhabitance remains unproved. Hence J12 is not
> OBSTRUCTED; it is AUTHORABLE.

**Is this datum ACTUAL by Q1's standard? NO, on all three limbs.**

```text
A1  It is introduced as "a one-arrow model" — the corpus's own word, first
    line of the span.  It fails A1 exactly as the A5 countermodel did.
A2  It carries no path current data, no admissible transport, no Loc/sigma
    realization stack, no DoR-019 metric, no Q-408 analysis maps.  `u` is
    quantified as "any reality/unit-compatible contact automorphism" — a
    quantifier over a class, not an exhibited datum.
A3  Its stated purpose is "the missing implication can be tested without
    choosing a physical member" and its stated conclusion is "the sealed
    stock does not derive the overlap equality".  It is a
    logical-non-implication device: exactly the "countermodel family
    (showing logical non-implying)" that CERT_V003_REVIEW:127-128 rules is
    not actual.
```

Its own span says so twice: "without choosing a physical member" and "Actual
physical inhabitance remains unproved."

This same `u` reappears in the choice table at `DETERMINATION_MAP:357` as the
declined alternative, and in the ruling's own "Alternatives declined" line.

### §3.3 EXHIBIT TWO — the descent-failure model in the V003 review

`STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001.md:272-282`, quoted WHOLE:

> Their images differ by
>
> ```text
> (a c_f,-a c_f) in Delta_M,                           (H2-4)
> ```
>
> so they are equal in `P_M`.  Hence response depends only on the pushout class.
> If the boundary leg carried a twist `u(c_f)!=c_f`, (H2-4) would be replaced by
> `(a c_f,-a u(c_f))`, which is not generally in `Delta_M`; the map would fail
> to descend.  Thus V003 has not silently absorbed a contact twist.  Its
> `Tw_f=0` detector is exactly failure-capable.

This is an explicitly counterfactual construction ("If the boundary leg
carried a twist"), on the worked carrier `R_N^rep = span{r,c}`,
`I_N^contact = span{c,b}`, `C_N = span{c} != {0}` introduced at `:258-262` as
"3.2 Independent nontrivial-overlap computation". **Not actual by A1 and A2.**

### §3.4 THE CORPUS'S OWN DETECTOR, ITS OWN SWEEP, AND ITS OWN VERDICT

The corpus built a failure-capable twist detector and ran it. `Tw_f` is its
name for the overlap defect, and `Tw_f != 0` is exactly a nontrivial contact
twist. `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:431-450`,
quoted WHOLE — including the final clause, which cuts against any reading of
this as a negative theorem:

> ### 4.5 Contact-twist detector and five permanent void regressions
>
> Define the overlap defect for an admitted arrow
>
> ```text
> Tw_f := eta_f^boundary |_C_N - Eta_f^rep |_C_N.               (J12-5)
> ```
>
> The adopted member has `Tw_f=0`.  The present corpus sweep found no actual
> contact datum exhibiting `Tw_f != 0`.  The sweep included the determination
> map, both adoption decisions, the pass-2 contact/excision builds, and both
> prior stage-1 assemblies/reviews; the only nontrivial twist present is the
> declined countermodel, not an exhibited actual contact datum.  This is reported
> as
>
> ```text
> CONTACT_TWIST_SEEN = none,
> ```
>
> not as a theorem that future physical contact data cannot produce one.

And the adversarial review of that artifact, independently,
`..._STAGE1_V003_REVIEW_LANE2_V001.md:294-302`, quoted WHOLE:

> 5. A future actual `Tw_f!=0` immediately voids the member.  The only nonzero
>    twist in the checked stock is A4's declined countermodel, not actual data.
>
> ```text
> H2 = PASS
> SIGNED_PUSHOUT = CONFORMING
> NONTRIVIAL_OVERLAP = WELL_DEFINED
> CONTACT_TWIST_ABSORBED = false
> ```

And its verdict line, `:500`: `CONTACT_TWIST_SEEN = none in actual checked stock`.
And `..._JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md:257`: `CONTACT_TWIST_SEEN = none`.

### §3.5 WHY THE GRADE IS "NO-ACTUAL-DATA-TO-TEST" AND NOT
### "TWIST-ABSENT-ON-EXISTING-DATA"

This distinction is the whole commission, and the corpus draws it itself.

`TWIST-ABSENT-ON-EXISTING-DATA` would assert that actual contact data exists
and was found untwisted. That is **not** what the bytes say. Q2 establishes
that the contact transport is `unbuilt`, `(H2-4)` is `unproved`, and
`ACTUAL_PHYSICAL_GLUE_INHABITED = OPEN`. `Tw_f := eta_f^boundary|_C_N −
Eta_f^rep|_C_N` cannot be evaluated on actual data because
**`eta_f^boundary` — one of its two terms — is the object the corpus calls
unbuilt.** The detector is well-defined and failure-capable; its input set is
empty.

The corpus itself is careful about this. Its report is `CONTACT_TWIST_SEEN =
none`, and the artifact that reports it appends, in the same span, that this is
reported "not as a theorem that future physical contact data cannot produce
one". The review restates the same scope: "none **in actual checked stock**".

**Q3 ANSWER, stated exactly as the commission asked for it if it were the
honest one: there is no actual contact datum in either root on which the
question of a nontrivial contact twist can be posed.** Two twists are
exhibited in the corpus; both are, by the corpus's own explicit
classification, models — one a declined countermodel in the ruling's own
basis, one a counterfactual in an adversarial review — and neither is actual
data by the standard the corpus itself sets in §1.2 (C).

---

## §4 — Q4: THE OTHER FOUR VOID CONDITIONS

The ledger position first, so it is not mistaken for this lane's grade.
`STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md:138-142` records all five A4
voids with status **STANDING** and falsifier type **PROVABLE**; the 7A handoff
package `:307-315` carries the same five rows forward to owners 7A-8 / 7A-13.
"STANDING" is the corpus's word for a live, uncontradicted, failure-capable
regression. This lane grades independently at bytes.

### VOID 1 — "unequal images on an overlap element": **NOT-TRIGGERED**

The equality is imposed, not derived, and nothing at bytes contradicts it on
any object the corpus admits. `..._STAGE1_V003_REVIEW_LANE2_V001.md:251-252`:

> The equality is imposed only on the actual common subspace `C_N`; no equality
> is imposed on exclusive represented or exclusive contact directions.

The independent recomputation at `:254-278` (quoted in part in §3.3) confirms
the pushout class is well defined and returns `H2 = PASS`,
`NONTRIVIAL_OVERLAP = WELL_DEFINED`. The permanent regression at
`..._JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:456`, void 1 row, reports:
"compare the represented and boundary images on every actual overlap class |
equal by the adopted datum".

**Grade: NOT-TRIGGERED.** With the standing caveat, visible in that very row,
that "equal by the adopted datum" is equality by adoption on a carrier whose
boundary leg is unbuilt — the same emptiness as Q3. No unequal image is
exhibited anywhere in either root.

### VOID 2 — "gluing deletes a `D_G^*`-visible bulk cycle": **NOT-TRIGGERED**

This is the one void with a positive proof at bytes rather than an empty test.
`..._JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:425-429`, quoted WHOLE:

> injective.  Similarly the contact injection is injective.  The pushout deletes
> only the duplicate presentation of the common class, not either physical leg.
>
> In particular, a `D_G^*`-visible bulk cycle cannot be deleted by contact gluing.
> Deleting one would trigger A4 void condition 2.

Independently recomputed at `..._STAGE1_V003_REVIEW_LANE2_V001.md:287-288`:
"Both canonical injections are injective: `[r,0]=0` forces `r=0`, and `[0,b]=0`
forces `b=0`; no represented bulk cycle is deleted." And independently again at
`MEMBERSHIP_REVIEW_DARIO_V001:279-281`: "**The A4 pushout injections are
injective, and injective for the right reason** — because `C_N` is the genuine
intersection. Had `C_N` been an abstract object with non-injective legs, a
cycle could have been deleted. **PASS.**"

**The clause cutting against, quoted whole** —
`MEMBERSHIP_REVIEW_DARIO_V001:283-286`:

> The row's second clause ("neither a visible represented bulk cycle **nor a target-only new
> cycle** is deleted") is broader than injectivity delivers — injectivity of the pushout legs
> speaks to represented/contact content, not to `K_f^new`. The build's own regression #1 covers
> the target-only case separately, so this is a NOTE on the row's phrasing, not a failure.

**Grade: NOT-TRIGGERED**, and this is the strongest of the five: it rests on an
injectivity argument that is checkable and was checked by three lanes, not on
an empty input set.

### VOID 3 — "failure of beta cocycle / restriction / reality / units / Ward / OLD_FID / RNL / LR / subextensivity": **UNTESTABLE-AS-STATED**, on the subextensivity limb

The nine limbs do not have one status. Eight of them are reported PASS on the
pushout by the assemblies and confirmed by review; the ninth — subextensivity —
cannot be tested at bytes, and this lane tested that claim itself rather than
inheriting it.

**What the assemblies report.**
`..._JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:458`, the void-3 regression
row: "rerun beta, restriction, reality, units, Ward, OLD_FID, RNL, LR, and
subextensivity | pass on the pushout".

**The premise the review immediately attaches, quoted WHOLE** —
`..._STAGE1_V003_REVIEW_LANE2_V001.md:289-291`:

> 3. Composition, reality, units, Ward, restriction, support, OLD_FID, RNL, LR,
>    and subextensivity descend only when their two legs agree on `C_N`; V003
>    states and uses that premise rather than deriving it from the quotient.

So the eight-limb pass is conditional on the same overlap agreement the member
authors, on a carrier whose boundary leg is unbuilt.

**THE SUBEXTENSIVITY LIMB, TESTED INDEPENDENTLY AT BYTES BY THIS LANE.**
A separate finding in this program holds the estimates behind the
subextensivity law to be unlocatable. Tested, not inherited:

1. The law is stated as an OBLIGATION, not a result. `CONSTRAINT_ARM:454-457`:
   "`Bound` **must prove** in the one topology / `beta_R / Vol_R -> 0 on every
   admitted cofinal family`". The modal is `must prove`.
2. Restated as a clause, not a theorem, in the adoption proposals:
   `ADOPTION_PROPOSAL_LANE2_V002:449` and `_V003:443` carry
   `beta_R/Vol_R -> 0 in tau_ker on every admitted cofinal family;` and
   `_V004:166-167` carries it as the labelled clause `(F-J13b)`.
3. The determination map's `(D2-4)` carries the different-notation relative
   `beta_f/Vol_f -> 0 on admitted cofinal families;` — and that whole block is
   prefixed "The stock proves, **conditionally on an actual physical contact
   term**".
4. **The corpus states the quantity is undefined.**
   `STAGE8_TASK5_HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001.md:223-231`, seal
   verified OK, quoted WHOLE:

> The generated contact ideal is `{0}`, so no visible cycle is deleted.  This
> is neither circular nor a blanket declaration about future new-edge
> discrepancies.  The expression `beta/Vol` remains undefined until a positive
> P4 volume exists; the hunt correctly leaves subextensivity conditional.
>
> ```text
> REF_PATH_KERNEL_TRANSPORT = CONFIRMED
> REF_PATH_ZERO_COCYCLE = CONFIRMED
> REF_PATH_SUBEXTENSIVITY = conditional_on_P4_density
> ```

5. **Exhaustive sweep for a supplying estimate.** Every occurrence of
   `beta_R/Vol_R`, `beta_f/Vol_f`, and `beta/Vol` in either root under the
   register bar was enumerated (11 + 13 + counted sites; see §8). Every one is
   either (a) a statement of the clause/obligation, (b) the "remains undefined"
   sentence or a quotation of it, or (c) a prior commission's sweep row about
   (a) and (b). **No site supplies a bound, a rate, a constant, a convergence
   proof, or a cofinal-family estimate.** The independent verification of the
   separate finding therefore returns: **the estimates behind the
   subextensivity law are not locatable in either root.** This lane reproduces
   that result at bytes.

**Grade for VOID 3: UNTESTABLE-AS-STATED.** What is missing, named exactly:
(a) a positive P4 volume, without which `beta/Vol` "remains undefined" by the
corpus's own sentence; (b) any estimate, bound, or rate for `beta_R/Vol_R` on
an admitted cofinal family; (c) an actual contact term, on which the whole
`(D2-4)` block — including the beta cocycle limb — is expressly conditional.
The other eight limbs are reported PASS but on a premise stated rather than
derived. Nothing at bytes exhibits a failure of any limb. Nothing at bytes
permits the subextensivity limb to be run.

### VOID 4 — "a contact automorphism selected from a response, threshold, fixed-point, end-test, or numerical consequence": **NOT-TRIGGERED**

`..._JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:459`, void-4 regression row:
"inspect the provenance of every overlap automorphism | none selected; the
strict equality is the adopted datum".

Independently, `..._STAGE1_V003_REVIEW_LANE2_V001.md:292-293`: "4. The overlap
identification is the adopted A4 member, not an automorphism selected from a
later consumer."

**Provenance checked directly at bytes by this lane.** The `contact
automorphism` token occurs 6 times in 4 file instances across both roots (S-7) — that
is 2 distinct basenames, and both are the determination map's `u` construction
(`:331` "where `u` is any reality/unit-compatible contact automorphism") and
its choice-table row (`:357`). No occurrence anywhere is a *selection*. The
`u_f` symbol, case-sensitive and delimiter-bounded, occurs at exactly 3 lines
in 2 distinct basenames in the corpus (§5.1) plus once in the ruling — none of
them a selection either.

Corroborating the anti-tuning custody at the ledger level:
`CLOSING_LEDGERS:141` routes F16 to "7A-8 anti-tuning custody; 7A-13".

Fence-relevant note: the four fences named in the void — response, threshold,
fixed point, end test — are exactly what the determination map's own header
block declares not executed (`MEMBER_BOUND = false`, `FIXED_POINT_EXECUTED =
false`, `END_TEST_EXECUTED = false`, `NUMERIC_EVALUATION = false`), and the
same block appears in the V003 review at `:15-21`.

**Grade: NOT-TRIGGERED.** This is the one void of the four that is positively
discharged by provenance rather than by an empty input set: the adopted
identification is the strict equality itself, whose provenance is the
principal's ruling, and no automorphism was chosen from any consumer at all.

### §4.1 The four, tabulated

| Void | Content | Grade | Basis |
|---|---|---|---|
| 1 | unequal images on an overlap element | **NOT-TRIGGERED** | equality imposed on `C_N`, recomputed PASS by two lanes; no unequal image exhibited anywhere. Test set is empty in the same way Q3's is. |
| 2 | gluing deletes a `D_G^*`-visible bulk cycle | **NOT-TRIGGERED** | positive injectivity proof, checked by three lanes; the strongest of the four. One phrasing NOTE (target-only new cycles) recorded above. |
| 3 | beta cocycle / restriction / reality / units / Ward / OLD_FID / RNL / LR / **subextensivity** | **UNTESTABLE-AS-STATED** | eight limbs PASS on a stated-not-derived premise; the subextensivity limb has no locatable estimate and `beta/Vol` is stated undefined pending a positive P4 volume. |
| 4 | contact automorphism selected from a response/threshold/fixed-point/end-test/numerical consequence | **NOT-TRIGGERED** | provenance inspected at bytes: no automorphism selected at all; the adopted datum is the strict equality. |

---

## §5 — Q5: THE DECLINED MEMBER `u_f`, AND ITS REACH

### §5.1 IS `u_f` DEFINED ANYWHERE? — **NO. It exists only as the name of a declined alternative, plus the model quantifier that introduced it.**

**Exhaustive enumeration.** The token `u_f`, case-sensitive and bounded by
non-word characters, occurs across both roots under the register and
self-exclusion bars at exactly **3 distinct lines in 2 distinct basenames**
(8 delimited matches at S-10, because each basename is mirrored):

```text
STAGE8_TASK5_EQ6_DETERMINATION_MAP_LANE1_V001.md      2 lines
STAGE8_FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_
  REVIEW_DETERMINATION_V001.md                        1 line
```

plus **1 line** in `DOR_020_A4_...md` (the ruling itself). Nowhere else.

**The two determination-map lines.** `:333` — inside the one-arrow model —
"On a chain choose a multiplicative natural family `u_(gf)=u_g u_f`; the
boundary discrepancy then satisfies the same composition law." And `:357`, the
choice table row, quoted WHOLE:

> | twisted gluing | choose a covariant reality/unit-compatible automorphism family `u_f` on `C_N` and glue by `eta_boundary=u_f Eta_rep` | adds a contact-holonomy/member fiber | **YOURS** |

That row is the **entirety** of the corpus's specification of `u_f`. It is a
*quantifier over a class* — "choose a covariant reality/unit-compatible
automorphism family" — with a gluing formula attached. There is no
construction, no exhibited member, no existence claim, no properties theorem,
no domain/codomain typing beyond "on `C_N`", and no site anywhere in either
root that uses `u_f` after this row.

The ruling then declines it, `DOR_020_A4:18`, quoted with its parenthetical
whole: "Twisted gluing (a contact-holonomy member u_f — new physical fiber; no
current evidence of twist)".

**The third line is a SYMBOL COLLISION, and must not be counted.**
`STAGE8_FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_REVIEW_DETERMINATION_V001.md:111`
reads `u_f(v)=integral_(gamma_f) 0=0`. Context `:106-115` shows this `u_f` is an
**A4 current** — a linear functional on visible tangents indexed by an edge `f`
— in a refutation of a field-signature claim ("Thus every A4 current
annihilates a nonzero visible tangent"). That artifact's "A4" is a current
label, not DoR-020-A4. It is a different object with the same name.

**A near-collision, also excluded.** The uppercase `U_f` occurs 54 times in
both roots (S-12); it is a **unit transport** carrier map
(`U_f^D:D_N^Loc->D_M^Loc`, `U_f^Sym:Sym_N^loc->Sym_M^loc`, e.g.
`STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md:785-786`, listed at `:1004` as
"`REQUIRED` unit transport"). Not the contact-holonomy member.

**`contact-holonomy` as a token occurs 2 times in the corpus** — both are the
two mirrored copies of `DETERMINATION_MAP:357` — **plus once in the ruling.**
It is never defined, only named.

**DEFINITION STATUS: NAMED-ONLY. `u_f` is defined nowhere in the corpus. It
exists as (a) a quantifier over an automorphism class in a declined choice-table
row, (b) a composition law `u_(gf)=u_g u_f` asserted of that class inside a
one-arrow model, and (c) the name of the alternative the ruling declined.**

### §5.2 REACH TEST (i) — U(1)-valued transition functions with a triple-overlap cocycle condition: **UNRELATED**

**The object exists in the corpus, in full.**
`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:27-43`, quoted WHOLE:

> Two lifts represent the same local ray exactly when, on an overlap,
>
> ```text
> z_j = g_ij z_i,
> g_ij = exp(i theta_ij) in U(1).
> ```
>
> On triple overlaps, equality of the represented ray requires
>
> ```text
> g_ij g_jk g_ki = 1.
> ```
>
> The local lifts and transition functions therefore define a complex line
> bundle, equivalently a principal `U(1)` comparison bundle. This `U(1)` is a
> local representative redundancy of the adopted projective record field. It
> is not obtained by mistaking a passive basis change for a new physical force.

That is precisely the family of U(1)-valued transition functions with a
triple-overlap cocycle condition. It is record-native and displayed.

**The relation to contact holonomy or `u_f` does not exist at bytes.**

```text
- LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md contains ZERO occurrences of any of:
  contact, u_f, J12, C_N, I_contact, glue, pushout.  Checked directly.
- Files carrying `triple-overlap`: 21 instances / 15 distinct basenames.
  Files carrying a contact-technical term: 190 instances / 106 basenames.
  Their intersection is THREE basenames, and all three are prior COMMISSION
  artifacts that swept both vocabularies as search strings —
  STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001,
  STAGE8_DISCREPANCY_COCYCLE_O38SR_V001 and its AUDIT.  A sweep row listing
  two tokens is not the corpus relating two objects.
- Files carrying (`U(1)-valued` OR `transition function`) AND the J12
  contact-glue object: TWO — the same O38SR pair.
- The fourth `g_ij`-and-contact file, STAGE8_EM_PARTICIPATION_O4SR_V001, was
  opened: its `g_ij` hits are the Sigma5 transition law (`:124`, `:453`) and
  its contact hits are `PHYSICAL_WARD_CONTACT_HODGE_SPLIT = NOT_BUILT` (`:182`,
  `:669`) — a different object, in a different section, with no relating
  sentence between them.
```

**Grade: UNRELATED.** The corpus displays both structures and never connects
them.

**A separate U(1) bundle also exists inside the contact work, and it is also
not related to a contact holonomy.** The excision certificate's W4 layer carries
`P_bundle -> calB  U(1) bundle`, `F_conn  U(1) connection curvature`, and
`c_1(P_bundle)  first Chern class` (`:117`, `:125-126`), with the bundle
discipline at `:404-420`. That discipline is a *pullback/isomorphism* square —
`P_source isomorphic_to f_r^*P_target`, `c_1(P_source)=f_r^*c_1(P_target)` —
and the certificate states at `:406` "The certificate never factorizes or
replaces the U(1) data" and at `:419-420` "Current excision is therefore a
certificate on the physical cycle carrier, not a split of the bundle or a gauge
choice." No transition-function cocycle on a contact overlap appears, and no
contact holonomy is defined from it.

### §5.3 REACH TEST (ii) — a require-site turning on single-valuedness around a closed cycle: **UNRELATED**

**Require-sites of exactly that shape exist.**
`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:130` carries require-row R-1:
"WINDING/QUANTIZATION: winding labels are integer — "integer-quantized by
single-valuedness (CAS P3a/P3b: fractional winding leaves an exact residual
−2)" (AJ §5) … one condition (single-valued closure) whose admissible set is
the integer lattice ℤ — a CLASS, not a point". `STAGE8_COMPLETION_AND_FORCING_
O37SR_V001.md:1146` names the same row's content as "R-1  single-valuedness on
a closed cycle".

**Not related to contact.**

```text
- Files carrying `single-valuedness`: 26.  Their intersection with the J12
  contact-glue universe is exactly ONE basename: the determination map.
- The determination map's TWO single-valued occurrences are NOT about a closed
  cycle.  They are :356 "gives one-valued `Eta`" (one image per element, the
  pushout's descent property) and :401 "A lane cannot silently treat
  single-valuedness as already adopted."  Both concern single-valuedness OF A
  MAP ON A QUOTIENT, not monodromy-freeness around a loop.  This is a
  genuine near-miss and this lane declines the identification: they are
  different properties that share a word.
- T14SR's own contact mention is at :132, require-row R-3, where
  "contact-kernel" appears as the fourth of four proof obligations on a
  crossing's target form.  It is a different row from R-1/R-2, a different
  object, and no sentence relates them.
```

**Grade: UNRELATED.**

### §5.4 REACH TEST (iii) — charge-type or phase structure: **UNRELATED**

**Charge and phase structure exist in the corpus, richly.**
`T14SR:131`, require-row R-2: "CHARGE STRUCTURE: U(1) characters χ_n with
cocycle/transition covariance (both characters transform with the same g^n …)
… cocycle consistency + single-valuedness jointly pin the character lattice and
its conjugation". `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:81-92` carries a
holonomy and character section with `W_n(gamma) = exp(i n integral_gamma a)`,
and `:107` names "the charged source/record current" as still-to-derive.

**Not related to contact holonomy or `u_f` at bytes.** The proximity sweep
(±6 lines, every `contact` occurrence in ROOT1, against
`holonom|monodrom|U(1)|triple-overlap|transition function|winding|chern|
single-valuedness`) returned the complete set of near-neighbours. Every hit
resolves to one of: a census/ledger row listing both tokens without relating
them; the excision certificate's W4 pullback discipline (§5.2); an unrelated
"contact term" in the Duhamel/link-holonomy spec; or the one substantive site
treated next.

**THE CLOSEST STRUCTURAL ANALOGUE IN THE CORPUS, REPORTED IN FULL BECAUSE IT
CUTS BOTH WAYS.**
`STAGE8_SOURCE_COUPLED_RECORD_TRANSITION_LAW_ADOPTION_PROPOSAL_V002.md:29-32`
declares, under "DERIVED, NOT A CHOICE", quoted WHOLE:

> ```text
>   narrow no-contact theorem:
>   with the post endpoint-charge package fixed and only open-chain holonomies
>   as input, an extra one-sided multiplicative contact is the identity.
> ```

and proves it across `:100-150` (§2.1 Left contact at `:100`, §2.2 Right contact and open chain at `:135`), concluding at `:141-145`, quoted WHOLE:

> On a sequential open chain, independent vertex gauge transformations put all
> open-link holonomy assignments in one gauge orbit. Without a loop or an
> additional parent datum there is no nonconstant gauge-invariant contact
> scalar. Tensoring the one-cell result therefore adds no new contact at finite
> `N`.

with the result block at `:149-160` including
`NONTRIVIAL_ONE_SIDED_CONTACT_FROM_DECLARED_OPEN_HOLONOMY_EXISTS = false`
and the scope limit at `:162-165`: "It does not quantify over a different
endpoint-charge package or over new parent, curvature, closed-loop, state,
effect, metric, continuum, or contact data."

**This is a contact-shaped multiplicative datum `C(z)`, built from holonomy,
proved to be the identity, with the loop case expressly outside scope.** It is
the nearest thing in the corpus to relating a contact to a holonomy and a
charge package. **It is nevertheless a different object from the J12 contact,
and this lane declines the identification:** `C(z)` is a multiplicative factor
on the record transition law `W_post(z)`, and that artifact contains no
`I_contact`, no `C_N`, no `eta_f^boundary`, no pushout, and no reference to
J12 or DoR-020-A4. The corpus nowhere states that the two "contacts" are the
same object, and nowhere states that they are different. Reporting it as a
relation would be an import; suppressing it would be a clipped sweep.

**Grade: UNRELATED**, with the near-analogue above recorded of record.

### §5.5 Q5 tabulated

| Item | Grade | Basis |
|---|---|---|
| `u_f` defined in the corpus? | **NAMED-ONLY — not defined** | 3 delimited lines in 2 basenames + 1 in the ruling; two of those are a symbol collision or a model quantifier; the choice-table row is the entire specification |
| Reach (i) U(1)-valued transition functions with triple-overlap cocycle | **UNRELATED** | object exists whole at `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001:27-43`; that artifact has zero contact/u_f/J12/C_N tokens; the only co-carrying files are prior commissions' sweep rows |
| Reach (ii) require-site on single-valuedness around a closed cycle | **UNRELATED** | require-sites exist (T14SR R-1); their intersection with the contact universe is the determination map alone, whose "single-valued" is a map property on a quotient, not monodromy around a loop |
| Reach (iii) charge-type or phase structure | **UNRELATED** | charge/phase structure exists (T14SR R-2; record bundle §Holonomy and character); no relating sentence to contact holonomy or `u_f` anywhere; the nearest analogue (`SOURCE_COUPLED..._V002` narrow no-contact theorem) is a different object |

**THE Q5 FINDING, STATED PLAINLY: the corpus relates none of the three.** The
structures the commission named all exist in this corpus, some of them in full
and displayed form. The contact holonomy `u_f` is connected to none of them by
any sentence in either root.

---

## §6 — CHOICE LEDGER

| # | Choice made | The road not taken | Why | Status |
|---|---|---|---|---|
| CH-1 | Adopt PASS-C (the J12 contact-glue object) as the universe, then extend it by two named artifacts, giving 44 instances / 23 basenames | Use PASS-A (`I_contact`, 30/16) or PASS-B (any `contact`, 686/383) | PASS-A demonstrably misses the three artifacts that run the twist detector and the ruling's own cited contact-stock source; PASS-B admits at least four distinct objects called "contact" | **CLOSED** — universe stated in full at §0.1 and defended |
| CH-2 | Grade Q3 **NO-ACTUAL-DATA-TO-TEST**, not **TWIST-ABSENT-ON-EXISTING-DATA** | Report the corpus's own `CONTACT_TWIST_SEEN = none` as an absence finding on existing data | `Tw_f := eta_f^boundary\|_C_N − Eta_f^rep\|_C_N` cannot be evaluated when the corpus calls `eta_boundary` "the unbuilt contact transport"; the detector's input set is empty, which is a different fact from a negative result | **CLOSED** |
| CH-3 | Grade Q2 **SPECIFIED-NOT-CONSTRUCTED**, not **ABSENT** | Grade ABSENT on the strength of "unbuilt"/"unproved"/"OPEN" | The conditions ARE stated, exactly and quotably — `(H2-4)`, `(J13-2)`, `(D2-3)`. A specification exists; only the object does not. ABSENT would misdescribe the record | **CLOSED** |
| CH-4 | Grade void 3 **UNTESTABLE-AS-STATED** rather than NOT-TRIGGERED | Follow the assemblies' "pass on the pushout" row and grade NOT-TRIGGERED | The row is true as reported, but the review attaches the premise "V003 states and uses that premise rather than deriving it", and the subextensivity limb has no locatable estimate at all. Grading NOT-TRIGGERED would report a pass on a limb that cannot be run | **CLOSED** |
| CH-5 | Verify the subextensivity-estimate finding independently by exhaustive `beta/Vol` enumeration | Inherit the prior finding's INDETERMINATE-AT-BYTES row | The commission required testing it; a catalogued negative may not be re-read as authority | **CLOSED** — reproduced at bytes from primary sources (`CONSTRAINT_ARM:457`, `HUNT_CROSS_CHECK:223-231`, `ADOPTION_PROPOSAL_V004:166`) |
| CH-6 | Count the `u_f` in `FIELD_SIGNATURE_PHYS_V003...` as a symbol collision, excluded | Count 3 basenames as carrying `u_f`, inflating its reach | Its context (`:106-115`) types it as an A4 *current*, a functional on visible tangents; that artifact's "A4" is a current label, not DoR-020-A4 | **CLOSED** |
| CH-7 | Report the `SOURCE_COUPLED..._V002` narrow no-contact theorem in full while grading reach (iii) UNRELATED | Either (a) count it as a RELATED site, or (b) omit it | (a) would import an identification the corpus never makes between two objects both called "contact"; (b) would clip the sweep. Reported and separated | **CLOSED** |
| CH-8 | Report the malformed V004 seal sidecar as a formatting defect and continue, rather than STOP | Treat "no properly formatted SHA checksum lines found" as a seal failure and stop the commission | The recorded digest equals the file's actual digest in both roots; the defect is a one-space-instead-of-two separator. No content mismatch exists. STOP is reserved for mismatch | **CLOSED** — flagged in §9 |
| CH-9 | On re-check, restore the two clauses this lane had clipped from the MAXWELL_SYMBOL quotation, and add its section heading | Let the shortened quotation stand | The clipped span dropped "failure-capable direct sum or quotient statement such as", the `or` line, and — worst — the heading "### 4.2 Contact split - not built", which cuts directly on Q2. Clipping a clause that supports the finding is still clipping | **CLOSED** — see §1.1 and SC-2 |
| CH-10 | Re-run every `timeout`-wrapped sweep after discovering `timeout` is absent on this machine | Let the empty results stand | Four sweeps had silently returned nothing (exit 127), and one of them would have caused this artifact to report `U_f` as nonexistent when it occurs 54 times | **CLOSED** — see SC-1 |
| CH-11 | Add a seventh `--exclude` naming this artifact and re-run all 54 sweep rows from scratch | Keep the counts taken after the draft hit disk | This artifact is written into ROOT1 and was being counted by its own sweeps (`Tw_f` 14→30, `contact-holonomy` 2→11). Reporting a lane's own prose back as corpus evidence would invert the Q5 finding | **CLOSED** — see SC-5; every §8 number is post-fix |
| CH-12 | Count the proximity sweep rather than reading it off a `head`-truncated display | Report the 24/12 figures the truncated listing showed | 24/12 was a display artifact; the measured figures are 108 site-lines / 42 files, and the sharper same-line test exhausts at 12 and is enumerated in full | **CLOSED** — see SC-6 |

---

## §7 — TOY_SEPARATION

**THE ACTUAL SURFACE, NOT A TOY.** Every quoted sentence in this artifact is
from a file in the corpus at the line range stated. Every such file is sealed
and was verified by `shasum -a 256 -c` from its own directory, **with one
stated exception: `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` carries no sidecar
of any kind** (defect D-3); its digest is recorded in §9 so the §5.2 quotation
remains checkable, and the grade that quotation supports does not rest on it.
No object is constructed here. No model is built here. No missing object is
authored here — not in a toy, a footnote, or an aside.

**THE TOYS THAT ARE IN THE CORPUS, NAMED AND SEPARATED.** This commission's
central question turns on exactly this separation, so it is stated in full.

| Corpus construction | What it is | What it is NOT | Corpus's own word for it |
|---|---|---|---|
| The one-arrow `u` model, `DETERMINATION_MAP:326-350` | a consistency/non-derivability device with `Eta^rep\|_C=id_C`, `eta^boundary\|_C=u` | actual contact data; an exhibited twist; a member | "a one-arrow **model**"; "without choosing a physical member" |
| The `u(c_f)!=c_f` descent counterfactual, `STAGE1_V003_REVIEW:279-282` | a failure-capability demonstration for the `Tw_f=0` detector | actual contact data | "**If** the boundary leg carried a twist" |
| The `span{r,c}`/`span{c,b}` overlap computation, `STAGE1_V003_REVIEW:256-278` | an independent recomputation of pushout descent | an actual `C_N` | "Independent nontrivial-overlap computation" |
| The `A_epsilon` Hodge countermodel, `PROJECTOR_LOCALITY_REDUCING_CERT_V002:367-411` | proof that A4 does not imply contact-reducing | actual contact data (expressly ruled so) | "**countermodel**"; "**pre-A5 countermodel family**" |
| The direct-sum model, `EXCISION_CERT:41-42` | consistency witness for the certificate class | a physical inhabitant (expressly) | `DIRECT_SUM_MODEL_IS_PHYSICAL_INHABITANT = false` |

**SYMBOL COLLISIONS — four objects called "contact", two called `u_f`/`U_f`,
and one "twist" that is many.** The determination map itself keeps a section
titled "Symbol collisions bearing on this question" (`:76-93`), so this
discipline is record-native.

```text
"contact", sense 1  the J12/J13 contact response/symbol subspace
                    I_contact,N and its overlap C_N.  THE OBJECT UNDER TEST.
"contact", sense 2  the endpoint-contact DIAMOND GEOMETRY of the excision
                    certificate — "old and new cycle corridors ... meet only
                    at the recorded endpoint q".  A geometry, not an ideal.
"contact", sense 3  the one-sided multiplicative factor C(z) of the
                    source-coupled record transition law.  A different object;
                    see §5.4.
"contact", sense 4  "contact term" in the Duhamel/interchange spec's sense
                    (a second-history-derivative contribution).  Unrelated.

"u_f"               (a) the declined contact-holonomy member (named only);
                    (b) an A4 CURRENT, a functional on visible tangents,
                        in the field-signature review.
"U_f"               a REQUIRED unit transport carrier map U_f^D / U_f^Sym.
                    54 occurrences.  Unrelated to (a).

"twist"             dominated in this corpus by (i) the parity-TWISTED TRACE
                    and TWISTED CD COLUMNS of the Galerkin/sign-crossing work,
                    (ii) the UNTWISTED S^2 Dirac spectrum / flux twist of the
                    7A DBR work, (iii) the TWISTED INCIDENCE matrix of the
                    orientation determination.  None is a contact holonomy.
                    Only 24 of the 315 `twist` hits sit near `contact` at all.
```

---

## §8 — SWEEP CUTOFFS

Every pattern below was RUN — not listed, not estimated, not read off a
truncated display. LEAK = the number of returned paths whose basename matches
`REGISTER|TRACKER|THE_PLAN|ROAD_REMAINING|THE_HANDOFF|DECISION_SHEET`; it is
computed per row from that row's own result set, not asserted.

**TWO EXCLUSIONS, BOTH VERIFIED TO FIRE.**

```text
E-1  THE REGISTER BAR, passed as a shell ARRAY of six separate --exclude
     elements (never as one string), because a prior lane's single-string
     multi-glob silently stopped working.
     TEST: enumerate all *.md in both roots WITH the array and count barred
     basenames -> 0.  Same enumeration WITHOUT it -> 31.  The bar fires.

E-2  SELF-EXCLUSION.  This artifact is written INTO ROOT1, so from the moment
     the first draft hit disk every sweep was counting THIS FILE.  Discovered
     on the pre-seal recount (see SC-5).  A seventh --exclude element,
     'STAGE8_TWIST_EVIDENCE_O40SR_V001.md', was added and every row below was
     RE-RUN from scratch under it.
     TEST: grep for 'TWIST_EVIDENCE_O40SR' across both roots under the array
     -> 0 hits.  The self-exclusion fires.
```

**METHOD, STATED ONCE SO THE ROWS ARE COMPARABLE.** HITS = `grep -rIhioE`
(case-insensitive) unless the row is marked CASE-SENS, in which case it is
`grep -rIhoE`. FILES = the matching `-l` count under the same case discipline.
Both roots, `--include='*.md'`, both exclusion sets. FILES counts **file
instances across both roots**, so a mirrored artifact counts twice.

| # | Pattern | Hits | Files | LEAK |
|---|---|---|---|---|
| S-1 | `twist` | 315 | 68 | 0 |
| S-2 | `contact.twist` OR `twist.{0,12}contact` | 24 | 12 | 0 |
| S-3 | `CONTACT_TWIST_SEEN` — CASE-SENS | 10 | 6 | 0 |
| S-4 | `Tw_f` — CASE-SENS | 14 | 4 | 0 |
| S-5 | `contact-holonomy` — CASE-SENS | 2 | 2 | 0 |
| S-6 | `contact holonomy` (space) | 0 | 0 | 0 |
| S-7 | `contact automorphism` — CASE-SENS | 6 | 4 | 0 |
| S-8 | `monodrom` | 51 | 18 | 0 |
| S-9 | `holonom` | 3274 | 799 | 0 |
| S-10 | `u_f` delimited — CASE-SENS | 8 | 4 | 0 |
| S-11 | `u_f` delimited — case-INsensitive | 44 | 12 | 0 |
| S-12 | `U_f` — CASE-SENS | 54 | 16 | 0 |
| S-13 | `I_contact` / `I_N^contact` / `I^contact` — CASE-SENS | 168 | 36 | 0 |
| S-14 | `C_N` | 2905 | 422 | 0 |
| S-15 | `contact term` | 135 | 70 | 0 |
| S-16 | `contact/Ward` | 32 | 17 | 0 |
| S-17 | `contact support` | 32 | 26 | 0 |
| S-18 | `R_N^rep` | 79 | 23 | 0 |
| S-19 | `R_N^phys` | 19 | 11 | 0 |
| S-20 | `Eta_f` / `eta_f` | 785 | 135 | 0 |
| S-21 | `ACTUAL_PHYSICAL_GLUE_INHABITED` — CASE-SENS | 2 | 2 | 0 |
| S-22 | `subextensiv` | 373 | 168 | 0 |
| S-23 | `sub-extensiv` | 0 | 0 | 0 |
| S-24 | `beta_f/Vol` | 9 | 4 | 0 |
| S-25 | `beta_R ?/ ?Vol` | 11 | 9 | 0 |
| S-26 | `Vol_f` | 13 | 4 | 0 |
| S-27 | `Vol_R` | 20 | 11 | 0 |
| S-28 | `cofinal` | 497 | 138 | 0 |
| S-29 | `triple-overlap` | 42 | 17 | 0 |
| S-30 | `triple overlap` (space) | 20 | 11 | 0 |
| S-31 | `triple.overlap` (either) | 62 | 21 | 0 |
| S-32 | `g_ij` | 166 | 37 | 0 |
| S-33 | `U(1)-valued` | 17 | 13 | 0 |
| S-34 | `transition function` | 94 | 37 | 0 |
| S-35 | `cocycle condition` | 8 | 7 | 0 |
| S-36 | `single-valued` | 152 | 72 | 0 |
| S-37 | `single-valuedness` | 66 | 26 | 0 |
| S-38 | `single valued` (space) | 0 | 0 | 0 |
| S-39 | `one-valued` | 2 | 2 | 0 |
| S-40 | `multivalued` / `multi-valued` | 0 | 0 | 0 |
| S-41 | `closed cycle` | 13 | 9 | 0 |
| S-42 | `around a cycle` | 0 | 0 | 0 |
| S-43 | `around a closed` | 2 | 2 | 0 |
| S-44 | `winding` | 855 | 196 | 0 |
| S-45 | `first Chern` | 30 | 14 | 0 |
| S-46 | `c_1` | 1127 | 269 | 0 |
| S-47 | `charge` | 14820 | 2305 | 0 |
| S-48 | `phase` | 8350 | 1253 | 0 |
| S-49 | `quantization condition` | 14 | 6 | 0 |
| S-50 | `flux` | 4360 | 557 | 0 |
| S-51a | PROXIMITY, ROOT1: every `contact` within +/-6 lines of `holonom\|monodrom\|U(1)\|triple.overlap\|transition function\|winding\|chern\|single-valuedness` | 108 site-lines | 42 files | 0 |
| S-51b | SAME-LINE co-occurrence, ROOT1: `contact` AND one of the same eight tokens on ONE line | 12 lines | — | 0 |
| S-52 | universe: any `contact` | 3955 | 686 | 0 |
| S-53 | universe: contact-technical terms (21-way alternation) | 719 | 190 | 0 |
| S-54 | universe: J12 contact-glue object (PASS C) | 273 | 40 | 0 |

**S-51b, ENUMERATED IN FULL — because it is the sharpest reach test and it is
small enough to exhaust.** All 12 same-line co-occurrences in ROOT1 resolve as:

```text
 7 lines  the SOURCE_COUPLED narrow no-contact theorem and its two
          determinations (:93, :111, :157, :450, :489, :507, :558) — the
          different-object near-analogue reported in full at §5.4.
 1 line   DETERMINATION_MAP:357 — the choice-table row that NAMES
          "contact-holonomy".  The naming site, not a relating site.
 1 line   O37SR:1868 — a census line listing "U(1) charge-type + cocycle;
          orientation/presentation/contact-kernel;" as SEPARATE require-rows.
 1 line   PRE_ROOT_HIGHER_DERIVATIVE_EQUIVALENCE_THEOREM:120 — a preservation
          list ("...contact terms, Ward identities, incidence data,
          holonomy...").  A conjunction of preserved items, not a relation.
 1 line   a Loc^phys row naming "contact quotient" and "hidden
          harmonic/holonomy counterterm" as two separate failure modes.
 1 line   OBSTRUCTION_ORIGIN_O6SR:772 — "use U(1)/contact/Hodge structure to
          fix the locus", inside a block headed "DISPLAY ONLY — no route is
          selected, recommended, costed, or authored here", listing repairs
          under a reading the same artifact reports already refuted.

NOT ONE of the twelve relates a contact holonomy to U(1)-valued transition
functions, to single-valuedness around a closed cycle, or to a charge/phase
structure.  This is the byte-level basis for the three UNRELATED grades.
```

**HEADLINE RECONCILIATION AGAINST THESE COUNTS, as required.**

- **S-1 vs S-2.** 315 `twist` hits; only 24 sit anywhere near `contact`. The
  headline does not claim the corpus is twist-free — it claims **no actual
  contact datum exists on which a contact twist can be tested**. The ~291
  non-contact `twist` hits are the parity-twisted trace, the untwisted Dirac
  spectrum, and the twisted incidence matrix; they are separated in §7 and are
  evidence for nothing here.
- **S-3 / S-4.** The corpus's own detector is defined at 4 file instances and
  reports `none` at 6. §3.4 quotes both whole. The headline does **not** adopt
  `CONTACT_TWIST_SEEN = none` as its grade — that phrasing would assert data
  was checked and found clean. The grade is that the detector's input set is
  empty, which S-4's own definition (`Tw_f := eta_f^boundary|_C_N −
  Eta_f^rep|_C_N`) plus Q2's `unbuilt` finding forces.
- **S-5 / S-6.** `contact-holonomy` = 2 (the two mirrored copies of one line),
  `contact holonomy` with a space = 0. Two hits, one sentence. Consistent with
  Q5's NAMED-ONLY.
- **S-10 vs S-11 vs S-12.** The load-bearing figure for Q5.1 is the
  case-sensitive S-10: 8 delimited matches = **3 distinct lines in 2 distinct
  basenames** (each mirrored). The case-insensitive inflation to 44 (S-11) is
  entirely the unrelated uppercase `U_f` unit transport, independently counted
  at 54 in S-12. Reconciled and separated; had this not been checked, Q5 would
  have overstated `u_f`'s reach by an order of magnitude.
- **S-8 / S-9.** 51 `monodrom` and 3274 `holonom` do not contradict Q5. The
  corpus's holonomy is overwhelmingly graph/cycle holonomy
  (`H([h_e,h_a])=h_e h_a^(-1)`) and open-link/open-chain holonomy. S-51b
  exhausts the intersection with `contact` at 12 lines and none relates them.
- **S-22 vs S-24/S-25/S-26/S-27.** Subextensivity is discussed at 373 hits
  across 168 file instances; the ratio it is about (`beta/Vol` in all three
  notations) occurs at 9 + 11 hits, and every one of those is a clause, an
  obligation, the "remains undefined" sentence, or a quotation of one of
  those. **Discussed constantly, estimated nowhere.** This asymmetry is the
  §4 void-3 finding and it is visible in the sweep table itself.
- **S-29/S-31/S-32/S-33/S-34.** The U(1)/transition-function/triple-overlap
  machinery is real and substantial (62 + 166 + 17 + 94 hits). Its intersection
  with the contact-glue universe is three basenames, all prior commissions'
  sweep rows. This is why reach (i) is UNRELATED rather than CANNOT-DETERMINE:
  the structures are both present and plainly unconnected, not merely
  unfindable.
- **S-47 / S-48.** `charge` at 14820 and `phase` at 8350 are the two largest
  counts in the table and were **used as evidence for nothing**. Reach (iii)
  was decided on S-51a/S-51b plus direct reading of the named artifacts, never
  on a raw count. A large count is not a relation.
- **S-52 / S-53 / S-54.** 686 / 190 / 40 file instances. The adopted universe
  is S-54 plus two named extensions = 44. §0.1 shows both rejected passes and
  says why each fails, so the universe claim is checkable rather than asserted.
- **One drift recorded rather than smoothed.** An early pass of S-9 returned
  3274 hits and a later pass 3272 under nominally identical flags; the
  definitive re-run under both exclusion sets returns 3274, reproducibly, by
  two methods. The 2-count discrepancy is unexplained. It bears on no finding
  — `holonom` is a context count and is cited for nothing — but it is recorded
  rather than quietly rounded.

## §9 — FLAG BLOCK

**FENCE LINE.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false

No numeric value of any coupling, scale, root, eigenvalue, norm, or constant
was computed anywhere in this artifact.  No comparison to any measured value
was made.  No member was bound; no fixed point, end test, threshold, or
numerical evaluation was executed.  Every number appearing above is a COUNT
(of hits, files, or lines) or a SHA-256 digest.
```

**SEAL TALLY.**

```text
ADOPTED UNIVERSE              44 / 44 digests confirmed
  verified by `shasum -a 256 -c` directly           40 / 44  OK
  digest confirmed, sidecar malformed                2 / 44  (see D-1)
  digest confirmed via plain .sha256 sidecar         2 / 44  (see D-2)
OUT-OF-UNIVERSE CONSUMED      10 / 11 verified OK; 1 unsealed (see D-3)
MIRROR COMPARISON             21 / 21 mirrored pairs byte-identical
                              (0 divergent)
BASIS-CITATION CHECK          DoR-020-A4's cited basis digest `76ee3c69…`
                              recomputed and MATCHES
MISMATCHES FOUND              0.  No STOP condition arose.
```

**DEFECTS FOUND IN THE RECORD (not in this draft).**

```text
D-1  STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md.seal.sha256
     is MALFORMED in BOTH roots: one space separates digest from filename
     where `shasum -c` requires two, so `shasum -a 256 -c` reports "no
     properly formatted SHA checksum lines found" rather than OK.  The
     recorded digest EQUALS the file's actual digest in both roots --
     9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514 --
     on one unbroken line here to avoid any transcription ambiguity.  Content
     is intact; the sidecar cannot be machine-verified as written.  REPORTED,
     NOT REPAIRED — this lane took no write action on any corpus file.

D-2  STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md has NO `.seal.sha256`
     sidecar in either root.  A plain `.sha256` sidecar exists and verifies
     OK from the artifact's own directory in both roots.  Naming
     irregularity, not a content problem.

D-3  LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md — the artifact carrying the
     corpus's U(1)-valued transition functions and the triple-overlap cocycle
     condition, and therefore a load-bearing citation for Q5 reach (i) — has
     NO SIDECAR OF ANY KIND in ROOT1.  Its digest is
     b96bef557c150015cf9c0c523f63f4eb53ffe45051b01db9d8b86fe109805645,
     recorded here so the quotation in §5.2 is checkable, but it is an
     UNSEALED source and §5.2 is flagged accordingly.  The Q5 (i) grade does
     not depend on it: UNRELATED rests on the absence of any relating
     sentence, which the sealed contact artifacts establish on their own side.
```

**REGISTER-BAR INCIDENTS: NONE.**

```text
Barred: QUESTIONS_SETTLED_REGISTER*, QUESTIONSSETTLED_REGISTER*,
EXECUTION_TRACKER*, THE_PLAN*, ROAD_REMAINING*, THE_HANDOFF*,
OBSERVATIONS_REGISTER*, *DECISION_SHEET*, and any name containing REGISTER or
TRACKER.  Passed as a shell ARRAY of --exclude flags -- six elements for the
register bar, plus a seventh excluding this artifact itself (see E-2/SC-5) --
never as a single string.  Firing verified twice, at commission start and
again at the pre-seal recount: 0 barred basenames returned with the array,
31 without it.  LEAK counter = 0 on every one of the 54 sweep rows, computed
per row from that row's own result set.  No barred file was opened, quoted,
or counted, and no barred basename appears anywhere in this artifact.

NOTE ON A PERMITTED FILE THAT LOOKS BARRED: STAGE8_7A_HANDOFF_PACKAGE_V001.md
is a WORKSPACE artifact, not `THE_HANDOFF*`.  The bar is on files whose name
BEGINS `THE_HANDOFF`; this one does not, and the exclusion array (which uses
the anchored glob `THE_HANDOFF*`) correctly does not exclude it.  Checked
deliberately rather than assumed.

SUPERVISION DIRECTORY: only DOR_* files were opened — DOR_020_A4 (the object,
read whole and first), DOR_020_A5, DOR_020_A2.  Nothing else in
/Users/bgm/MB Work/alpha-program-archive/supervision/ was read.  The prior
commission's error of reporting a DoR ABSENT FROM BOTH ROOTS is not repeated:
DOR_020_A4 is present, sealed, and verified.
```

**DEFECTS FOUND IN THIS LANE'S OWN DRAFT ON SELF-CHECK.**

```text
SC-1  SWEEP INFRASTRUCTURE FAILURE, CAUGHT AND CORRECTED.  Four sweeps were
      wrapped in `timeout`, which does NOT EXIST on this machine (`which
      timeout` -> "timeout not found").  Each wrapped command failed with
      exit 127 and printed NOTHING, which read as a legitimate zero result.
      One of them would have made this artifact state that `U_f` does not
      occur in the corpus; it occurs 54 times.  All four were re-run
      unwrapped.  This is the same class of defect the commission warned
      about (a silently-stopped exclusion) in a different disguise, and it is
      recorded rather than quietly fixed.

SC-2  CLIPPED QUOTATION — the exact defect this commission warned about, found
      in this lane's own draft.  A first pass quoted
      MAXWELL_SYMBOL_CERTIFICATES_LANE1_V001 as ":482-488" and dropped THREE
      things: the clause "failure-capable direct sum or quotient statement
      such as", the `or` line between the two displayed equations, and the
      section heading at :480, "### 4.2 Contact split - not built".  The
      heading is load-bearing for Q2 and the clip removed a span that
      SUPPORTS the finding — clipping is not made acceptable by cutting in
      one's own favour.  Caught on the pre-seal re-read against bytes.
      Restored WHOLE at §1.1, with the span re-cited as :480-489.  The same
      first pass also mistranscribed :487 `Ker_phys` as `Ken_phys`; the
      restored quotation carries the correct byte.

SC-3  UNIVERSE UNDER-SCOPING, CAUGHT BEFORE SEALING.  The first universe
      (PASS A, `I_contact`, 16 basenames) omitted the three artifacts that
      actually run the twist detector and the ruling's own cited contact-stock
      source.  Had it stood, Q3 would have been graded without ever seeing
      `Tw_f`, `CONTACT_TWIST_SEEN`, or `(H2-4)`.  Widened to PASS C + 2 and
      documented at §0.1 with both rejected passes shown.

SC-4  COLUMN-ALIGNMENT NOTE.  Two quoted code-block lines
      (EXCISION_CERT:128-129) carry column padding in the source that this
      artifact's rendering does not reproduce identically.  The WORDS are
      exact; the note at §1.2(B) states this rather than leaving it implicit.

SC-5  SWEEP SELF-CONTAMINATION — the most serious defect in this draft, found
      on the pre-seal recount.  This artifact is written INTO ROOT1.  From the
      moment the first draft hit disk, every re-run sweep was counting THIS
      FILE, which by then contained `Tw_f`, `contact-holonomy`,
      `CONTACT_TWIST_SEEN`, `u_f` and every other pattern in the table.
      Symptom: `Tw_f` returned 14 hits / 4 files before the write and
      30 hits / 5 files after it; `contact-holonomy` went 2 -> 11.  Had this
      gone unnoticed, the sweep table would have reported this lane's own
      prose back as corpus evidence, and §5's NAMED-ONLY finding would have
      been contradicted by its own artifact.  A seventh --exclude element
      naming this file was added, its firing was verified (0 hits for
      'TWIST_EVIDENCE_O40SR' under the array), and ALL 54 sweep rows were
      RE-RUN from scratch.  Every number in §8 is post-fix.

SC-6  A TRUNCATED DISPLAY WAS ABOUT TO BE REPORTED AS A COUNT.  The proximity
      sweep S-51 was first read off a screen listing that had been piped
      through `head -80`, and the draft recorded "24 sites, 12 files".  The
      actual figures, counted rather than eyeballed, are 108 site-lines across
      42 files (S-51a).  The finding is unchanged — the sharper same-line test
      S-51b exhausts at 12 lines and all 12 are enumerated in §8 — but the
      original number was a display artifact, not a measurement, and reporting
      it would have been exactly the failure this commission names ("SWEEPS
      ARE RUN, NOT LISTED").

SC-7  MIXED CASE-SENSITIVITY IN THE FIRST SWEEP HARNESS.  The first sweep
      helper computed HITS case-insensitively but FILES case-sensitively, so
      several early rows paired a case-insensitive hit count with a
      case-sensitive file count (e.g. `twist` 315 / 66 instead of 315 / 68).
      The definitive harness in §8 applies one case discipline per row and
      marks it.

NO OTHER DEFECT FOUND ON SELF-CHECK.  Every quotation above was re-read
against bytes after drafting; four line-range citations were corrected in that
pass (DETERMINATION_MAP item 3, LOC_FAMILY (E0), and two SOURCE_COUPLED
spans).
```

**WHAT THIS ARTIFACT DID NOT DO.** It did not void, recommend voiding, or
argue for reopening DoR-020-A4. It did not characterise the evidence as
sufficient or insufficient for any act. It did not author the missing contact
term, the missing subextensivity estimate, or the member `u_f`. It did not
re-read a catalogued negative as authority — where a prior commission's
finding bore on a question here (the subextensivity estimates), the finding was
re-derived from primary sources at bytes. It took no git action. It wrote to
exactly one path.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
