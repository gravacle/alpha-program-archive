# STAGE 8 — REMAINDER-UNIFORMITY CHECKED: THE DOUBLE-KILL DEGREE BOOKKEEPING, THE JOINT ESTIMATE, THE ANGULAR-ZERO ACCOUNTING, AND THE REVERSAL EXCLUSION ALL SURVIVE INDEPENDENT RE-DERIVATION

## BLIND ADVERSARIAL VERIFIER — CODENAME REMAINDER-CHECK — [SEALED]

Date: 2026-08-14
Role: BLIND adversarial verifier (REMAINDER-CHECK), cross-lineage, DEFAULT =
REFUTE. Under test: STAGE8_REMAINDER_UNIFORMITY_V001.md (sha256 2e4e5163…,
verified at path). All five tasked attacks pressed; every load-bearing
identity re-derived by my own method, wider than the build's own battery.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked (this check
included).

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — my one script is exact symbolic
CAS identity verification (sympy 1.14.0, fresh venv `rmchkvenv`), reproduced
in full in §7 with verbatim output; NOTHING numeric evaluated; every constant
symbolic or an exact rational/surd. No physical quantity computed, bounded
numerically, or evaluated. No scale, imported GR, or faithfulness authority.
M(t) stays SHARP; equal-time stays FALSE by C6. No register/tracker/plan/
road/ledger/lens file read. Output name probed before write: ABSENT. No git
action.

---

## 0. VERDICT IN ONE LINE

**NET_VERDICT = CONFIRMED(PROVED at exactly the licensed strength — B-L2*
DISCHARGED at the summed level). Every attack failed to refute: my own
enumeration (n <= 12, wider than the build's n <= 8) reproduces the parity
floors exactly (per-factor intra 2 / inter 3; assembled 4/6; NO (c=1, o=0)
class anywhere — the one configuration that would have been log-divergent);
my own re-derivations confirm the double m_0 kill as the second power (with
the negative control run: a nonzero [F]_0 leaves u1 f0/omega and linear
carrier growth — the refutation shape, which does NOT obtain); the angular
zero genuinely supplies NO |k|-power (angular average 2/3, a constant) and
the joint mass is genuinely Theta(1/omega) — HALF the pointwise power, both
directions verified; the reversal exclusion is sound and my hunt for a
missed coincidence-strata resummation found none (the J5 chain has no
x-multiplicative structure and no "-1"-split analogue; any ladder resummation
converges absolutely INSIDE the build's own norm argument). Two findings,
both non-load-bearing: (f1) a dimensional display slip in §3.3a's inline rate
constant; (f2) five transcription-level divergences between the relay copy of
the flag block and the sealed bytes (sealed bytes authoritative, hash MATCH).
The build's 31/31 CAS battery re-run from its verbatim script: output
IDENTICAL. My own independent battery: 48/48 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session. None unverifiable.

```text
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md    MATCH (tasked sha256; sidecar MATCH)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md          MATCH (tasked a7969f0c; sidecar MATCH)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md    MATCH (tasked a4f2e1b7; sidecar MATCH)
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md         MATCH (tasked 6e81ae92; sidecar MATCH)
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md           MATCH (tasked 2699af25; sidecar MATCH)
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md  MATCH (tasked 3b9730e0; sidecar MATCH)
b4a55baa5cb46de474be6e03c7fbdea728118c38ebc24529c34ae4b77e184bad  STAGE8_CL_ERROR_CHECK_V001.md          MATCH (tasked b4a55baa; sidecar MATCH)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA; tasked 789338ad)
```

Cited spans READ AT PATH this session and verified to carry what the build
cites: E1 :339-352 (C4 stencil, m_0 = m_1 = 0, m_{2j} = -2^{j-1}, l1-tight
saturation, "the weights PROMOTE the sharp causal-ball projector"), :353-364
(C6 two-time-only + volume-diagonal locus refinement), :465-470 (sharp M(t),
v(t) = tau_R 32 r(t)^3, no-mollify clause), :826-832 (R-L2b: G_hs finite
uniformly over the D3 refinement quantifier — the trilemma target form),
:1106-1126 (B-L2/B-L2* typing; "only repair would be to soften M(t)"),
:1131-1144 (C-L1 operator collapse; C-L2 quadratic-form demand); R2-RESUM
§2.2-2.4 (two-level intraband reduction; excursion domination
(|lambda|Theta)^n/n!; the phase-free channel's exact x-space multiplicative
composition; chi_hat's wall never engages there), §3 (E's degree-0 intraband
content = -1_ball; [P+E]_0 = 0), §4/slab exhibit (V4a-V4h; the
omega-interpolating family; A_int - 1 decomposition of record), §5(c) (the
survivor CANCELS; [U(0)]_0 = 0 both ways; [F]_0 = 0 via the m_0 kill on
sub-intervals; sin.sin channel needs two dying interband hops), §5(d)+§6
(r2 RELOCATED to this remainder; (r-a) needed in the POSITIVE direction;
residues (r-b)/(r-c)/(r-d); debts 2 and 3 remain regardless; the V_2
subsidiary -i V_2(x)/(2|k|), V_2 = (8 pi^2/7)(1-128|x|^7); "E's remainder is
genuinely degree -1, not smaller" — PER FACTOR); R2-RESUM-CHECK (d1)-(d5)
survivor-fate audit CONFIRMED incl. (d2) F-route ("weight sum -> m_0 (free)
J (free) = 0"), (e1) r2 supersession UPHELD; BL2STAR §2.1 (thresholds BOTH
sides: position p < 3/2; momentum 2a > 3; the C2 Lambda^3 shape; X display
and second-difference Kraus structure), §3 (a1)-(a3), §4.1-4.4 (J5; the
per-stratum degree certification with the licensed one-IBP-per-segment and
the mixed-transfer-regime trade |k|^{-1-gamma} <= |k|^{-1}; the X = a_+ C
U(0)^dag F C + a_- C F~^dag U(0) C display; the saturation reversal), §5
(chi_hat closed form, L^2 not L^1, tails clear, factor-wise bounds do not
exist), §6 (F1-F3) + BL2STAR-CHECK (STRATUM_FIDELITY / THRESHOLD / DEGREE
CONFIRMED; r2 real); CL-ERROR §2.2 (E = E_prof + E_int; the exact D-kernel
(e^{i(s'|k'|-s|k|)t} - 1) chi_hat_{r(t)}(q) P_{s'}(k')P_{-s}(k) S; J3-J6),
§3.2 (E_int: every route costs a power); PA :75-128 (A1/A2, S, M(t), v(t),
b_D Gevrey, J = -(Q b_D Q) (x) alpha_x), :367-384 (Strang).

---

## 2. ATTACK 1 — THE DEGREE BOOKKEEPING, RE-DERIVED INDEPENDENTLY. CONFIRMED.

My own stratum model, built from the sealed vertex algebra only (§7 C2, all
re-verified by my own matrix constructions — spherical parametrization, a
different reduction route than the build's polynomial ideal):

```text
(1) VERTEX EXHAUSTIVENESS. Per S-insertion the band action is exhaustive:
    FLIP (S P_s = P_{-s} S, full O(1): P_+(u) S P_-(u) = S P_-(u), my C2d) or
    STAY (label preserved across the momentum transfer: P_s(k') S P_s(k) =
    P_s(k') P_{-s}(k) S = -(s/2) P_s(k')(alpha.(k_hat-k_hat')) S — the J5
    coincidence factor, my C2c both signs; at equal momentum the stay is
    EXACTLY zero, my C2b). The J vertex (alpha_x-type): flip or stay, both
    O(1), NO coincidence factor (S J S = -J route, my C2e). No third option
    exists in the algebra: the enumeration's alphabet is complete.
(2) WEIGHT PARITY. The stencil is symmetric (w_+ = w_-): it kills EVERY odd
    function of lambda (my C1b, three independent odd families), so per
    factor only n EVEN S-insertion strata survive; n = 0 strata are
    m_0-killed IDENTICALLY (lambda-independent operator times m_0 = 0) —
    including F's bare-J stratum and every O(a^2) analogue (the kill is
    a-order-independent: the weight sum per branch factor multiplies
    whatever lambda-independent chain remains).
(3) MY ENUMERATION (own code, n <= 12 vs the build's n <= 8, jf in {0,1}
    for F): floors per factor intra min(2o+c) = 2, inter min = 3 (both
    factors); NO (c = 1, o = 0) class exists anywhere (my C4c); EVERY o = 0
    class is pure-stay intraband with c even >= 2 (my C4d — stronger than
    the build states); floors stable under nmax extension (my C4f).
(4) ASSEMBLED ROUTES. The sea-sea compression with the exact junction
    resolution C U^dag (C + C_+) F C forces EQUAL block types on the two
    factors (through C: intra x intra; through C_+: inter x inter) — mixed
    intra x inter routes CANNOT occur for the sea-sea element (two-band
    completeness P_+ + P_- = I at fixed momentum, my C2f). Assembled floors:
    intra x intra = 4, inter x inter = 6, overall min = 4 (my C4e). The
    marginality arithmetic is exactly as the build states: 2o+c = 3 would be
    log-divergent (my C5c) — the (c=1, o=0) exclusion is load-bearing and
    HOLDS; 2o+c = 4 converges with a full power to spare (my C5d).
(5) MISSED DEGREE-0 ROUTE HUNT (the tasked refutation): (i) n = 0 strata —
    m_0-killed, all a-orders; (ii) each factor's degree-0 intraband content —
    zero OF RECORD (sealed + independently checked; mechanism re-verified,
    my C1); (iii) degree-0 interband route into the sea-sea element — none:
    every interband block has o >= 1 AND c >= 1 (degree <= -2 per factor);
    (iv) assembled degree-(-1) — the product ledger [X]_{-1} =
    [U^dag]_{-1}·0 + 0·[F]_{-1} = 0 (my C5e), with the NEGATIVE CONTROL run:
    were [F]_0 = f0 != 0, the product would retain u1 f0/omega — degree -1 —
    and HS^2 would grow LINEARLY in the carrier (my C5f + C5a): the
    refutation shape exists and does NOT obtain; (v) the free factor
    e^{-i h_0} — modulus-1 band-diagonal, content-neutral, carried (r-c).
    NO MISSED ROUTE FOUND.
(6) THE LAWFUL SHARPENING: the inherited per-factor "degree <= -1, V_2-form
    leading" (R2-RESUM subsidiary, read at path: "E's remainder is genuinely
    degree -1, not smaller" — a PER-FACTOR statement) is consistent with the
    assembled degree <= -2: the assembly is a PRODUCT of two such factors.
    Sign ledger checked: U(0)-side -i V_2/(2|k|) daggers to +i; F-side +i
    via the m_2-form Sigma w(1 - i l^2 y) = +i y (my C1e); V_2-additivity
    across the F-junction holds for ANY split point (my C7c, stronger than
    the build's display). No contradiction with any sealed display.
```

**BOOKKEEPING = CONFIRMED.**

---

## 3. ATTACK 2 — THE JOINT ESTIMATE, RE-RUN BY MY OWN METHOD. MATCH.

### 3.1 The honest difficulty and the second power

Re-derived (my C5a/C5b): a degree-(-1) intraband amplitude gives HS^2 =
4 pi C^2 (Lambda - k_0) — LINEAR in the carrier; degree 0 gives the C2
Lambda^3 shape. Each factor ALONE fails carrier-uniformity: the tasked
honest possibility ("one power of escape is NOT enough on the momentum
side") is REAL and the build exhibits it before discharging it. The second
power is the partner factor's own escape — the double m_0 kill (my C5e),
NOT the angular zero, NOT transfer decay. Verified against the sealed
inheritance: [U(0)]_0 = 0 (R2-RESUM §3/§4, both ways, check-confirmed);
[F]_0 = 0 (R2-RESUM §5(c), check (d2)); both read at path.

### 3.2 The rate chain, re-derived by a different route

My C6: the slab closed form verified as the UNIQUE solution of the ODE
(H_2^2 = Om^2 I engine, dU/dT = -i H_2 U, U(0) = I — not the build's route);
sea-sea A = cos(Om T) + i omega sin(Om T)/Om; interband B = -i mu sin(Om T)/
Om with |B| <= mu/omega (the per-lambda interband degree -1, weight-killed
at even lambda-order — consistent with the floors, not in tension); exact
unitarity |A|^2 + |B|^2 = 1; the A_int - 1 decomposition; Om - omega =
mu^2/(Om + omega) <= mu^2/(2 omega); |e^{iz} - 1| = 2|sin(z/2)| <= |z|; and
the second-term bound mu^2/(Om(Om+omega)) <= mu^2/(2 omega^2) (my C6g).
Assembled: |A_int - 1| <= (mu^2/omega)(T/2 + 1/(2 omega)) — on omega >= k_0
a C/omega rate with symbolic constant; the small-omega regime is covered by
the trivial unitarity-pinned l1 bound (<= 2). The weighted sum uses
sum|w_l| l^2 = 1 (my C1c). FINDING (f1), non-load-bearing: the build's
§3.3a INLINE display writes "|A_int - 1| <= (mu^2/omega)(T/2 + 1/2)" —
dimensionally a slip (the "+1/2" needs "/(2 omega)"); its §6 R5 chain and
comment carry the correct form, and the object actually used downstream
(C_X min(1, omega^{-2}) with the k_0 split) is unaffected.

### 3.3 The transfer evaluation

My C10a: with |sigma| <= C_X min(1, omega^{-2}), the k-integral splits at
k_0 into (4 pi/3) C_X^2 k_0^3 + 4 pi C_X^2/k_0 — both exact, symbolic,
carrier-free; the Plancherel conversion factor (2 pi)^3 Vol(ball) is a
symbolic convention constant. The x-parameterization of the LEADING stratum
is the sealed decomposition's own structure (the coincidence corrections to
commuting free phases past indicators ARE the coincidence strata, counted
separately — the D-kernel resolution of record, CL-ERROR §2.2 read at
path): the chi_hat L^2-not-L^1 wall never engages for this stratum, exactly
as the phase-free precedent (R2-RESUM §2.4 + check (e1), read at path).

### 3.4 The angular zero, verified exactly (the tasked question)

My C3, component-level (a different verification route than the build's
symbol-level R3c): |k_hat' - k_hat|^2 = 2|k x q|^2/[|k||k'|(|k||k'| + k.k')]
holds as an identity of components (my C3a); it vanishes EXACTLY at forward
scattering and attains the cap 4 at antiparallel (my C3b); the small-transfer
form is Q^2 sin^2(psi)/K^2 + O(Q^3) (my C3c); and the angular average
<sin^2 psi> = 2/3 — A CONSTANT: the angular zero supplies NO power of |k|
(my C3d). The capped joint mass re-computed (my C8): M1 = 64 pi^3 r/(45
omega^2), M2 = 256 pi^3 r^2(2 omega - 1/r)/omega^2 <= 512 pi^3 r^2/omega,
M3 = 512 pi^3 r^2/omega — total <= 64 pi^3 r/(45 omega^2) + 1024 pi^3 r^2/
omega, matching the flag block EXACTLY; and the NEGATIVE CONTROL (my C8d):
the uncapped tail integral diverges LINEARLY — the naive full pointwise
power is genuinely unavailable, so the joint mass is genuinely Theta(1/
omega): the half-power accounting is TIGHT in both directions. VERDICT ON
THE TASKED HYPOTHESIS: the build's CORRECTION is right — alpha.(k_hat -
k_hat') does NOT supply the claimed power at forward scattering (its
angular average is a bounded weight); each coincidence insertion is worth
|k|^{-1} pointwise but only |k|^{-1/2} in joint L^2; the missing power at
the leading stratum is the PRODUCT structure; the angular/radial trade is
what keeps the coincidence strata past threshold given the floors.

### 3.5 The general stratum and the carrier-uniformity failure hunt

The induction's decay arithmetic re-verified: HS^2 integrand ~ k^2 ·
k^{-(2o+c)}, convergent iff 2o+c > 3; every existing assembled stratum has
2o+c >= 4 (my C4e) — one full power to spare, including the (c = 2, o = 0)
pure-stay classes (partner floor 2). The order-coefficient sum: my C9a
re-derives sum x^n n^2 2^n/n! = 2x(2x+1)e^{2x} by the y(y+1)e^y route —
entire; and my C9b: the growing moments -m_{2j} = (sqrt2)^{2j}/2 are
EXACTLY the |lambda|^n mass already inside the dominated exponential — the
C4-reversal mechanism has no independent object. THE HUNT for the C2 shape
re-entering: (a) degree-0 content — excluded exactly (§2); (b) assembled
degree-(-1) — excluded by the double kill; (c) a marginal 2o+c = 3 stratum
— excluded by the (c=1, o=0) non-existence (my C4c); (d) n-nonuniform
constants in the stratum sum — the declared estimate-grade steps (e-i)/
(e-ii), whose combinatorial content (channel count 2^n, <= 2 IBPs with
redistribution <= n^2, unitaries/C peeled at norm 1, unitarity-pinned l1
bounds) sums in closed form (my C9a) and whose inherited component (the
mixed-transfer-regime trade |k|^{gamma-1-2gamma} <= |k|^{-1}) is the sealed
BL2STAR §4.3 certification, check-CONFIRMED, re-read at path. NO
CONFIGURATION FOUND that re-enters the C2 shape. The F1-F3 fence audit
(§3.6 of the build) is correct: F2 forbids certifying the resummed object
by per-stratum COINCIDENCE orders against a conditionally-structured sum;
an ABSOLUTELY convergent sum of norm bounds is a different object —
||Sigma|| <= Sigma ||.|| unconditionally — and the degree-0 content is
handled by the exact cancellation of record, not by decay claims.

**JOINT_RERUN = MATCH. ANGULAR_ZERO = CONFIRMED(|k|^{-1/2} per insertion
jointly — half the pointwise power; no |k|-power from the angular average).**

---

## 4. ATTACK 3 — THE REVERSAL AUDIT. CONFIRMED: EXCLUDED.

The build's two exclusion routes verified, then my own hunt for the missed
resummation:

```text
(4a) NORM route: sound GIVEN §3 — an absolutely convergent stratum sum in
     HS norm is rearrangement-immune by construction; the limit object
     inherits ||X||_2 <= M carrier-free; degree-0 intraband content forces
     HS^2 ~ carrier momentum volume (my C5b): contradiction. The C4-reversal
     precedent (BL2STAR §4.4, re-read at path) ran through a CONDITIONALLY
     structured sum (per-stratum coincidence orders with n-dependent degree
     against growing moments); here the moments' growth is inside the
     dominated exponential (my C9b) and every per-stratum bound is an
     absolute norm bound. No object for the mechanism.
(4b) LIMIT route: independent — the summed intraband symbol's pointwise
     bound C(x) min(1, omega^{-2}) (dominated exchange, licensed by the
     inherited termwise domination) makes the large-omega limit — which IS
     the degree-0 content — zero.
(4c) MY HUNT (the phase-free trick one level down): the phase-free
     resummation needed (i) an exact x-space multiplicative composition
     (nested indicators multiply) and (ii) a "-1"-part split with a
     binomial kill. The coincidence strata have NEITHER: the J5 factor
     -(s/2) P_s(alpha.(k_hat - k_hat')) is a momentum-angular (Riesz-type)
     factor composing by q-convolution — its x-realization is a singular
     integral, not a multiplication; alpha-matrices at different transfer
     directions do not commute; and there is no analogous exact split of
     the stay factor (the D-kernel's (e^{i omega t} - 1) split lives in the
     TIME phase and is already exhausted by the sealed resolution). The one
     candidate I could construct — resumming the pure-stay ladder
     Sigma_c (stay-chain)^c at fixed k — converges ABSOLUTELY in HS by the
     capped per-insertion mass C/omega itself: any such resummation lands
     INSIDE the build's norm argument, not outside it. NO missed
     resummation found; and none could evade (4a) without first breaking a
     per-stratum bound (§2/§3: none broke).
(4d) Cross-checks as tasked: the rate chain IS the slab unitarity
     decomposition (my C6, own route); the moment identities re-verified
     (my C1: m_0 double kill, m_2 = -1 forms, the n = 2 ledger jet
     -iy + y^2).
```

**REVERSAL_AUDIT = CONFIRMED(EXCLUDED — both routes sound; no missed
coincidence-strata resummation exists to exhibit, and absolute convergence
forecloses the class).**

---

## 5. ATTACK 4 — VERDICT STRENGTH: EXACTLY LICENSED, DEBTS CARRIED, NOTHING ROUNDED.

```text
(5a) THE CLAIM vs THE LICENSE. The sealed require-form (R-L2b, E1 :826-832
     read at path) is the trilemma form sup_n ||X_n||_2 <= M over the D3
     refinement quantifier. Delivered: HS bound on the uncompressed X +
     the compression contraction ||Pi X Pi||_2 <= ||X||_2 — at the
     lineage's own Q-passed-through level, with (r-b) Q-resolution
     EXPLICITLY carried in NET, alongside (r-c) and (r-d). Correctly
     graded, nothing silently absorbed.
(5b) THE GRADE DECLARATION is honest and complete: identity-grade claims
     all CAS-verified (31/31 re-run IDENTICAL; my independent 48/48);
     estimate-grade steps (e-i) all-orders Volterra-IBP rate and (e-ii)
     composed-transfer Cauchy-Schwarz + mixed-regime trade are DECLARED,
     each pinned by an exact closed-form instance (the slab; the
     M-integrals), and each is the sealed lineage's own currency (BL2STAR's
     sealed per-stratum certification used exactly this grade and was
     check-CONFIRMED). Neither is deferred; neither is hidden.
(5c) DEBTS AND RESIDUES carried unmoved and correctly worded against the
     sealed sources (re-read at path): DEBT 2 (F'-14) untouched; DEBT 3
     (summed-S2' re-quantification: "no sealed display places the colour
     sum inside a Schatten-2 norm" — BL2STAR §2.2, verbatim consistent);
     the COMPUTE PRE-GATE downstream; (r-b)/(r-c)/(r-d) as sealed.
     Per-pair nothing moves; M(t) sharp everywhere (every IBP in a time
     variable, jumps retained — no softening found anywhere in the build);
     equal-time FALSE by C6 respected (all objects two-time).
(5d) OVERTURN CONDITIONS named in §5 of the build are the right ones and
     none obtains: [U(0)]_0 = 0 and [F]_0 = 0 stand (sealed + checked +
     mechanism re-verified); the enumeration is exhaustive over the sealed
     vertex algebra (my §2(1)); no composition route evades both Plancherel
     and the capped-mass Cauchy-Schwarz (my §3.5 hunt).
(5e) WHY NOT THE OTHER VERDICTS — checked: not FAILS-AT (the §3.1 failure
     shape is real but attaches per factor; the assembled object is the
     product — exhibited, then discharged); not REFORMULATED (the tasked
     object itself is delivered); not UNDECIDABLE-AT (no missing sub-object
     within B-L2*; the two estimate-grade steps are performed and graded).
```

**NET_VERDICT = CONFIRMED(PROVED — B-L2* DISCHARGED at the summed level at
exactly the licensed strength, debts 2/3 + compute pre-gate + residues
(r-b)/(r-c)/(r-d) carried unmoved).**

---

## 6. ATTACK 5 — PROVENANCE + INJECTION

```text
PROVENANCE: CLEAN. The artifact at path hashes to the tasked sha256
  (2e4e5163…) and matches its own sidecar; all 8 cited seals recomputed
  from bytes and matching sidecars and tasked prefixes; every span the
  build cites was re-read at path and carries what is cited; the build's
  §6 script re-run in a fresh venv reproduces its §6 verbatim output
  IDENTICALLY (31/31). FINDING (f2), non-semantic: the RELAY COPY of the
  flag block (the rm_flag_block presented to this check) differs from the
  sealed §7 bytes at 5 transcription-level points — "JOINTLY." for
  "JOINTLY:"; a line-wrap space inside the R3c identity; and the elision
  of the FORBIDDEN_IMPORTS parenthetical "(pi, r, tau_R, omega, Lambda
  never instantiated" with its closing paren and one comma. No semantic
  change; the sealed bytes are the authority and were checked directly.
FENCES: no numeric evaluation anywhere in the build's script (re-run
  confirms: symbolic throughout); scale/GR/faithfulness appear only inside
  the fence declarations themselves; the carrier radius appears only in
  the §3.1 exhibited failure shape, as declared; no register/tracker/plan/
  road/ledger/lens citation anywhere in the artifact.
INJECTION: none — no instruction-like content, no directives to a reader,
  no attempt to steer a verifier; scanned.
GATES: alpha_computed = false; kappa_record_computed = false;
  proof_authorized = false — asserted by the build, respected by this
  check; nothing here computes, bounds, or evaluates any physical
  quantity.
```

---

## 7. MY SCRIPT (exact symbolic verification; reproduced in full) AND OUTPUT

Run with sympy 1.14.0 (fresh venv `rmchkvenv`). Independent derivations:
own Dirac construction with spherical-parametrized projectors (C2), own
component-level angular identity (C3), own enumeration to n <= 12 (C4),
negative controls the build did not run (C5f, C8d), own ODE-uniqueness slab
route (C6), own symmetric-integral profile route (C7). The build's own
script was ALSO extracted verbatim from §6 and re-run: output byte-identical
to the artifact's §6 "Output, verbatim" block (31/31 PASS).

```python
# REMAINDER-CHECK: independent adversarial CAS battery (exact symbolic only).
# Own derivations, wider enumeration, negative controls. Nothing numeric evaluated;
# all constants symbolic or exact rationals/surds.
import sympy as sp

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

# ================= C1: weights / moments / odd-kill (independent) =================
lam = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w   = [sp.Rational(1,2), sp.Rational(-1,4), sp.Rational(-1,4)]
mom = lambda n: sp.nsimplify(sum(wi*li**n for wi,li in zip(w,lam)))
ok("C1a moments m_0..m_6 = 0,0,-1,0,-2,0,-4 (m_2j = -2^(j-1))",
   [mom(n) for n in range(7)] == [0,0,-1,0,-2,0,-4])
th = sp.symbols('th', real=True)
# odd-kill on genuinely odd analytic families (sinh, l^4 sinh, l cosh):
ok("C1b odd-kill: stencil symmetric (w[+s2]=w[-s2]) kills every odd f",
   w[1]==w[2]
   and sp.simplify(sum(wi*sp.sinh(li*th) for wi,li in zip(w,lam)))==0
   and sp.simplify(sum(wi*li**4*sp.sinh(li*th) for wi,li in zip(w,lam)))==0
   and sp.simplify(sum(wi*li*sp.cosh(li*th) for wi,li in zip(w,lam)))==0)
ok("C1c l1 mass = 1; sum|w|l^2 = 1; |lambda|max = sqrt2",
   sum(abs(x) for x in w)==1 and sp.nsimplify(sum(abs(wi)*li**2 for wi,li in zip(w,lam)))==1
   and max(abs(l) for l in lam)==sp.sqrt(2))
y = sp.symbols('y', real=True)
lvl = sum(wi*sp.exp(sp.I*li**2*y) for wi,li in zip(w,lam))
ok("C1d level-repulsion weight sum = -i e^{iy} sin y ; jet -iy + y^2",
   sp.simplify(sp.expand_complex(lvl + sp.I*sp.exp(sp.I*y)*sp.sin(y)))==0 and
   sp.simplify(sp.series(-sp.I*sp.exp(sp.I*y)*sp.sin(y), y, 0, 3).removeO() - (-sp.I*y + y**2))==0)
ok("C1e m_2-form (F leading weight): sum w (1 - i l^2 y) = +i y",
   sp.simplify(sum(wi*(1 - sp.I*li**2*y) for wi,li in zip(w,lam)) - sp.I*y)==0)

# ================= C2: band algebra, own construction =================
# My own Dirac rep (standard): same PA pinning.
I2, Z2 = sp.eye(2), sp.zeros(2,2)
sx = sp.Matrix([[0,1],[1,0]]); sy = sp.Matrix([[0,-sp.I],[sp.I,0]]); sz = sp.Matrix([[1,0],[0,-1]])
g0 = sp.diag(1,1,-1,-1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2,s],[-s,Z2]]).as_explicit()) for s in (sx,sy,sz)]
g5 = sp.I*g0*gs[0]*gs[1]*gs[2]
S  = -sp.I*g0*g5
al = [g0*g for g in gs]
I4 = sp.eye(4)
ok("C2a S^2 = I, S = S^dag, {S, alpha_j} = 0",
   S*S==I4 and sp.simplify(S.H-S)==sp.zeros(4,4)
   and all(sp.expand(S*a+a*S)==sp.zeros(4,4) for a in al))
# unit vectors via spherical parametrization (no polynomial reduction needed):
t1,p1,t2,p2 = sp.symbols('t1 p1 t2 p2', real=True)
def unit(t,p): return [sp.sin(t)*sp.cos(p), sp.sin(t)*sp.sin(p), sp.cos(t)]
u = unit(t1,p1); v = unit(t2,p2)
Au = u[0]*al[0]+u[1]*al[1]+u[2]*al[2]
Av = v[0]*al[0]+v[1]*al[1]+v[2]*al[2]
Pp_u, Pm_u = (I4+Au)/2, (I4-Au)/2
Pp_v, Pm_v = (I4+Av)/2, (I4-Av)/2
simp = lambda M: sp.simplify(sp.trigsimp(sp.expand(M)))
ok("C2b flip law S P_s(u) = P_{-s}(u) S ; equal-momentum stay = 0",
   simp(S*Pp_u - Pm_u*S)==sp.zeros(4,4) and simp(Pp_u*S*Pp_u)==sp.zeros(4,4)
   and simp(Pm_u*S*Pm_u)==sp.zeros(4,4))
Duv = (u[0]-v[0])*al[0]+(u[1]-v[1])*al[1]+(u[2]-v[2])*al[2]
ok("C2c J5 stay factor: P_s(v)P_{-s}(u) = -(s/2) P_s(v)(alpha.(u-v)), both signs",
   simp(Pp_v*Pm_u + sp.Rational(1,2)*Pp_v*Duv)==sp.zeros(4,4)
   and simp(Pm_v*Pp_u - sp.Rational(1,2)*Pm_v*Duv)==sp.zeros(4,4))
ok("C2d flip is full O(1): P_+(u) S P_-(u) = S P_-(u)",
   simp(Pp_u*S*Pm_u - S*Pm_u)==sp.zeros(4,4))
ok("C2e S J S = -J for J ~ alpha_x (anticommutation route)",
   sp.expand(S*al[0]*S + al[0])==sp.zeros(4,4))
ok("C2f band completeness at fixed momentum: P_+ + P_- = I",
   sp.expand(Pp_u+Pm_u)==I4)

# ================= C3: the angular identity (own derivation, components) =================
kx,ky,kz,qx,qy,qz = sp.symbols('kx ky kz qx qy qz', real=True)
k = sp.Matrix([kx,ky,kz]); q = sp.Matrix([qx,qy,qz]); kp = k+q
nk  = sp.sqrt((k.T*k)[0]); nkp = sp.sqrt((kp.T*kp)[0])
khat = k/nk; kphat = kp/nkp
lhs = sp.expand(((kphat-khat).T*(kphat-khat))[0])
cr  = k.cross(q)
rhs = 2*(cr.T*cr)[0] / (nk*nkp*(nk*nkp + (k.T*kp)[0]))
# equality of the two expressions as functions of components: clear denominators.
num = sp.simplify(sp.together(lhs - rhs))
ok("C3a |khat'-khat|^2 == 2|k x q|^2/(|k||k'|(|k||k'|+k.k'))  [component-level]",
   sp.simplify(sp.radsimp(num))==0)
# forward scattering: q parallel k -> zero (positive symbols from the start),
# and the antiparallel cap 4 attained:
K_,Q_ = sp.symbols('K_ Q_', positive=True)
kf = sp.Matrix([0,0,K_]); qf = sp.Matrix([0,0,Q_]); kpf = kf+qf
nkf = sp.sqrt((kf.T*kf)[0]); nkpf = sp.sqrt((kpf.T*kpf)[0])
fw = sp.simplify((((kpf/nkpf - kf/nkf).T*(kpf/nkpf - kf/nkf))[0]))
kp2 = sp.Matrix([0,0,-K_])
ap = sp.simplify((((kp2/K_ - kf/K_).T*(kp2/K_ - kf/K_))[0]))
ok("C3b forward scattering: factor vanishes exactly at q || k; antiparallel cap = 4",
   sp.simplify(fw)==0 and sp.simplify(ap-4)==0)
# global cap: |khat'-khat|^2 <= 4 trivially; check the small-angle form:
# with k=(0,0,K), q=(Q sin(psi),0,Q cos(psi)): leading term Q^2 sin^2 psi/K^2
psi = sp.symbols('psi', real=True)
sub2 = {kx:0,ky:0,kz:K_,qx:Q_*sp.sin(psi),qy:0,qz:Q_*sp.cos(psi)}
ser = sp.series(sp.simplify(lhs.subs(sub2)), Q_, 0, 3).removeO()
ok("C3c small-transfer form: |khat'-khat|^2 = Q^2 sin^2(psi)/K^2 + O(Q^3)",
   sp.simplify(ser - Q_**2*sp.sin(psi)**2/K_**2)==0)
# angular average of sin^2 psi over the sphere = 2/3: A CONSTANT — no |k| power.
avg = sp.integrate(sp.sin(psi)**2 * sp.sin(psi), (psi,0,sp.pi))/sp.integrate(sp.sin(psi),(psi,0,sp.pi))
ok("C3d angular average <sin^2 psi> = 2/3 (constant: angular zero gives NO k-power)",
   avg==sp.Rational(2,3))

# ================= C4: parity-floor enumeration, own code, n <= 12, both factors ====
# Model: a branch-factor chain with n S-insertions (lambda-grading n) and, for the
# F-factor, one J vertex with jflip in {0,1}. Weight functional kills n odd (C1b).
# Each S-insertion: FLIP (O(1), C2d) or STAY (one J5 coincidence power, C2c).
# J vertex: flip or stay, both O(1), NO coincidence factor (smooth vertex).
# Block type = parity of total flips (even = intra/sea-sea under sea compression,
# odd = interband). Plus-band segments >= ceil(total_flips/2); each segment >= one
# oscillatory power (one licensed IBP). m_0 kill: n = 0 strata die identically
# (weight sum multiplies a lambda-independent operator by m_0 = 0), INCLUDING the
# bare-J stratum of F.
def classes(has_J, nmax):
    out = []
    for n in range(0, nmax+1):
        if n % 2 == 1:      # weight-killed (C1b)
            continue
        for flips in range(0, n+1):
            stays = n - flips
            for jf in ([0,1] if has_J else [0]):
                if n == 0:  # m_0-killed (with or without J)
                    continue
                tot = flips + jf
                btype = 'intra' if tot % 2 == 0 else 'inter'
                o_min = (tot + 1)//2      # ceil
                out.append((btype, o_min, stays, n, flips, jf))
    return out
def floor_of(cl, b): return min(2*o + c for (bt,o,c,*_ ) in cl if bt==b)
clU = classes(False, 12); clF = classes(True, 12)
ok("C4a U floors n<=12: intra min(2o+c) = 2, inter = 3",
   floor_of(clU,'intra')==2 and floor_of(clU,'inter')==3)
ok("C4b F floors n<=12: intra min(2o+c) = 2, inter = 3",
   floor_of(clF,'intra')==2 and floor_of(clF,'inter')==3)
# the load-bearing exclusion: NO (c = 1, o = 0) class in EITHER factor
ok("C4c NO (c=1, o=0) class exists in either factor (n <= 12)",
   not any(o==0 and c==1 for (bt,o,c,*_) in clU+clF))
# in fact o = 0 forces flips = jf = 0, i.e. pure-stay intra with c = n even >= 2:
ok("C4d every o=0 class is pure-stay intra with c even >= 2",
   all((bt=='intra' and c>=2 and c%2==0) for (bt,o,c,*_) in clU+clF if o==0))
# assembled X floors: sea-sea compression => junction resolution C + C_plus forces
# EQUAL block types on the two factors (intra*intra via C, inter*inter via C_+).
# Mixed intra*inter routes CANNOT occur for the sea-sea element (band parity).
asm_intra = floor_of(clU,'intra') + floor_of(clF,'intra')
asm_inter = floor_of(clU,'inter') + floor_of(clF,'inter')
ok("C4e assembled floors: intra*intra = 4 ; inter*inter = 6 ; overall min = 4",
   asm_intra==4 and asm_inter==6 and min(asm_intra,asm_inter)==4)
# stability check: floors already attained at n <= 4; nmax extension changes nothing
clU8 = classes(False, 8); clF8 = classes(True, 8)
ok("C4f floors stable under nmax 8 -> 12 (attained at small n; larger n only adds cost)",
   floor_of(clU8,'intra')==floor_of(clU,'intra') and floor_of(clF8,'inter')==floor_of(clF,'inter'))

# ================= C5: threshold arithmetic + negative controls =================
Kv, k0, Lam, C_ = sp.symbols('K k0 Lambda C_', positive=True)
# honest difficulty (degree -1 amplitude): linear carrier growth
one_pow = sp.integrate(4*sp.pi*Kv**2*C_**2/Kv**2, (Kv, k0, Lam))
ok("C5a degree -1 alone: HS^2 = 4 pi C^2 (Lambda - k0) — LINEAR in carrier",
   sp.simplify(one_pow - 4*sp.pi*C_**2*(Lam-k0))==0)
# degree 0: cubic carrier growth (the C2 shape)
zero_pow = sp.integrate(4*sp.pi*Kv**2*C_**2, (Kv, k0, Lam))
ok("C5b degree 0: HS^2 = (4 pi/3) C^2 (Lambda^3 - k0^3) — the C2 Lambda^3 shape",
   sp.simplify(zero_pow - sp.Rational(4,3)*sp.pi*C_**2*(Lam**3-k0**3))==0)
# the marginal case 2o+c = 3: LOG divergent — the exclusion of (c=1,o=0) is load-bearing
marg = sp.integrate(4*sp.pi*Kv**2*C_**2/Kv**3, (Kv, k0, Lam))
ok("C5c 2o+c = 3 would be log-divergent: 4 pi C^2 log(Lambda/k0)",
   sp.simplify(marg - 4*sp.pi*C_**2*sp.log(Lam/k0))==0)
# 2o+c = 4: converges with a full power to spare
four = sp.integrate(4*sp.pi*Kv**2*C_**2/Kv**4, (Kv, k0, sp.oo))
ok("C5d 2o+c = 4: tail = 4 pi C^2/k0 — finite, carrier-free",
   sp.simplify(four - 4*sp.pi*C_**2/k0)==0)
# double-kill ledger + NEGATIVE CONTROL: a nonzero [F]_0 would resurrect degree -1
u1,u2,f0,f1,f2,om = sp.symbols('u1 u2 f0 f1 f2 omega', positive=False), None, None, None, None, None
u1,u2,f0,f1,f2 = sp.symbols('u1 u2 f0 f1 f2')
om = sp.symbols('omega', positive=True)
prod_killed = sp.expand((u1/om + u2/om**2)*(f1/om + f2/om**2))
ok("C5e double kill: leading term u1 f1/omega^2 (degree -2)",
   sp.limit(prod_killed*om**2, om, sp.oo)==u1*f1)
prod_alive = sp.expand((u1/om + u2/om**2)*(f0 + f1/om))
ok("C5f NEGATIVE CONTROL: [F]_0 = f0 != 0 leaves u1 f0/omega (degree -1 -> linear HS^2)",
   sp.limit(prod_alive*om, om, sp.oo)==u1*f0)

# ================= C6: slab rate chain, own route (unique ODE solution) =================
mu, T = sp.symbols('mu T', positive=True)
Om = sp.sqrt(om**2 + mu**2)
H2 = sp.Matrix([[om, mu],[mu, -om]])   # sigma_3 om + sigma_1 mu; sea = eigenvalue -om -> index 1
ok("C6a-1 H2^2 = Om^2 I (the closed form's engine)",
   sp.simplify(H2*H2 - Om**2*sp.eye(2))==sp.zeros(2,2))
U2 = sp.cos(Om*T)*sp.eye(2) - sp.I*sp.sin(Om*T)*H2/Om
ok("C6a-2 closed form solves dU/dT = -i H2 U, U(0) = I (unique solution)",
   sp.simplify(sp.diff(U2,T) + sp.I*H2*U2)==sp.zeros(2,2) and sp.simplify(U2.subs(T,0))==sp.eye(2))
A_sea, B_sea = U2[1,1], U2[0,1]
ok("C6a-3 sea-sea A = cos(Om T) + i om sin(Om T)/Om",
   sp.simplify(A_sea - (sp.cos(Om*T) + sp.I*om*sp.sin(Om*T)/Om))==0)
ok("C6b unitarity |A|^2 + |B|^2 = 1 exact",
   sp.simplify(sp.expand_complex(A_sea*sp.conjugate(A_sea) + B_sea*sp.conjugate(B_sea)))==1)
ok("C6c interband B = -i mu sin(Om T)/Om : |B| <= mu/Om <= mu/omega (degree -1 per lambda)",
   sp.simplify(B_sea + sp.I*mu*sp.sin(Om*T)/Om)==0)
A_int = sp.exp(-sp.I*om*T)*A_sea
decomp = (sp.exp(sp.I*(Om-om)*T)-1) - sp.I*sp.exp(-sp.I*om*T)*sp.sin(Om*T)*mu**2/(Om*(Om+om))
ok("C6d A_int - 1 decomposition (own check)",
   sp.simplify(sp.expand(sp.expand_complex(A_int - 1 - decomp)))==0)
ok("C6e Om - om = mu^2/(Om + om) ; and <= mu^2/(2 omega)",
   sp.simplify(Om - om - mu**2/(Om+om))==0 and
   sp.simplify(mu**2/(2*om) - mu**2/(Om+om) - mu**2*(Om-om)/(2*om*(Om+om)))==0)
z = sp.symbols('z', real=True)
ok("C6f |e^{iz}-1| = 2|sin(z/2)| <= |z|",
   sp.simplify(sp.expand_complex((sp.exp(sp.I*z)-1)*(sp.exp(-sp.I*z)-1)) - 4*sp.sin(z/2)**2)==0
   and sp.simplify(sp.diff(z - sp.sin(z), z) - (1-sp.cos(z)))==0)
# assembled rate: |A_int - 1| <= (Om-om)T + mu^2/(Om(Om+om)) <= mu^2 T/(2 om) + mu^2/(2 om^2)
# = (mu^2/om)(T/2 + 1/(2 om)). NOTE: the artifact's SECTION 3.3a inline display writes
# "(T/2 + 1/2)" — dimensionally a slip; its section 6 chain carries the correct
# (T/2 + 1/(2 om)). On omega >= k_0 this is <= (mu^2/om)(T/2 + 1/(2 k_0)): C/omega, symbolic.
ok("C6g second term bound: mu^2/(Om(Om+om)) <= mu^2/(2 om^2)  [Om >= om]",
   sp.simplify(mu**2/(2*om**2) - mu**2/(Om*(Om+om))
               - mu**2*(Om*(Om+om) - 2*om**2)/(2*om**2*Om*(Om+om)))==0
   and sp.simplify(sp.factor(Om*(Om+om) - 2*om**2 - (mu**2 + om*(Om-om))))==0)

# ================= C7: profile arithmetic, own derivations =================
rho = sp.symbols('rho', nonnegative=True)
tt = sp.symbols('tt', real=True)
tauR = sp.pi/sp.sqrt(2)
v_of = lambda t: tauR*32*sp.Min(t,1-t)**3
# V_2 via symmetry: 2 * int_rho^{1/2} (tauR 32 t^3)^2 dt
V2own = 2*sp.integrate((tauR*32*tt**3)**2, (tt, rho, sp.Rational(1,2)))
ok("C7a V_2(rho) = (8 pi^2/7)(1 - 128 rho^7)  [own symmetric route]",
   sp.simplify(V2own - sp.Rational(8,7)*sp.pi**2*(1-128*rho**7))==0)
ok("C7b Theta(0) = int_0^1 v = tau_R = pi/sqrt2 (l1 anchor)",
   sp.simplify(sp.integrate(tauR*32*tt**3,(tt,0,sp.Rational(1,2)))*2 - tauR)==0)
# junction additivity is integral additivity over [0,t'] u [t',1]: exact for ANY t'
tp = sp.symbols('tp', positive=True)
V2a = sp.integrate((tauR*32*tt**3)**2,(tt,rho,tp))
V2b = sp.integrate((tauR*32*tt**3)**2,(tt,tp,sp.Rational(1,2))) + \
      sp.integrate((tauR*32*(1-tt)**3)**2,(tt,sp.Rational(1,2),1-rho))
ok("C7c V_2 additivity across the F-junction (any split point)",
   sp.simplify(V2a+V2b-V2own)==0)
# chi_hat own derivation (radial Fourier transform of the ball indicator)
r_, q_ = sp.symbols('r q', positive=True)
x_ = sp.symbols('x', positive=True)
chi = sp.integrate(4*sp.pi*x_**2*sp.sin(q_*x_)/(q_*x_), (x_, 0, r_))
ok("C7d chi_hat_r(q) = 4 pi (sin qr - qr cos qr)/q^3  [own]",
   sp.simplify(chi - 4*sp.pi*(sp.sin(q_*r_)-q_*r_*sp.cos(q_*r_))/q_**3)==0)
# envelopes: |sin z - z cos z| <= z^3/3 (via monotone difference) and <= 1 + z
ok("C7e envelope 1: z^3/3 - (sin z - z cos z) has derivative z(z - sin z) >= 0, value 0 at 0",
   sp.simplify(sp.diff(z**3/3 - (sp.sin(z)-z*sp.cos(z)), z) - z*(z-sp.sin(z)))==0
   and (z**3/3 - (sp.sin(z)-z*sp.cos(z))).subs(z,0)==0)

# ================= C8: the capped joint mass, own splits =================
# M(om) = int d^3q |chi_hat|^2 min(q^2/om^2, 4); envelopes from C7e; om r >= 1 regime.
M1 = sp.integrate(4*sp.pi*q_**2*(4*sp.pi*r_**3/3)**2*(q_**2/om**2), (q_, 0, 1/r_))
M2 = sp.integrate(4*sp.pi*q_**2*(8*sp.pi*r_/q_**2)**2*(q_**2/om**2), (q_, 1/r_, 2*om))
M3 = sp.integrate(4*sp.pi*q_**2*(8*sp.pi*r_/q_**2)**2*4, (q_, 2*om, sp.oo))
ok("C8a M1 = 64 pi^3 r/(45 om^2)", sp.simplify(M1 - sp.Rational(64,45)*sp.pi**3*r_/om**2)==0)
ok("C8b M2 = 256 pi^3 r^2 (2 om - 1/r)/om^2 <= 512 pi^3 r^2/om",
   sp.simplify(M2 - 256*sp.pi**3*r_**2*(2*om-1/r_)/om**2)==0
   and sp.simplify(512*sp.pi**3*r_**2/om - 256*sp.pi**3*r_**2*(2*om-1/r_)/om**2
                   - 256*sp.pi**3*r_**2/(om**2*r_))==0)
ok("C8c M3 = 512 pi^3 r^2/om", sp.simplify(M3 - 512*sp.pi**3*r_**2/om)==0)
# => M(om) <= 64 pi^3 r/(45 om^2) + 1024 pi^3 r^2/om : the flag-block display. CONFIRMED.
# NEGATIVE CONTROL: uncapped mass diverges LINEARLY (the naive full power is NOT there)
Qc = sp.symbols('Qc', positive=True)
uncapped_tail = sp.integrate(4*sp.pi*q_**2*(8*sp.pi*r_/q_**2)**2*(q_**2/om**2), (q_, 1/r_, Qc))
ok("C8d uncapped tail = 256 pi^3 r^2 (Qc - 1/r)/om^2 : linear divergence as Qc -> oo",
   sp.simplify(uncapped_tail - 256*sp.pi**3*r_**2*(Qc-1/r_)/om**2)==0)
# the half-power ledger: M ~ 1/om in HS^2 = |k|^{-1/2} per amplitude; pointwise the
# factor is q/om ~ |k|^{-1} — the coincidence factor is worth HALF its pointwise power
# jointly. CONFIRMED by C8a-C8d shape: no piece decays faster than 1/om overall, and
# the 1/om piece is genuinely present (M3 exact), so the accounting is TIGHT.

# ================= C9: coefficient sums / domination =================
n_ = sp.symbols('n', integer=True, nonnegative=True)
xdom = sp.symbols('x', positive=True)
s_ = sp.summation(xdom**n_ * n_**2 * 2**n_ / sp.factorial(n_), (n_, 0, sp.oo))
ok("C9a sum x^n n^2 2^n/n! = 2x(2x+1) e^{2x} (entire) [own: n^2 y^n/n! = y(y+1)e^y]",
   sp.simplify(s_ - 2*xdom*(2*xdom+1)*sp.exp(2*xdom))==0)
s2_ = sp.summation(xdom**n_ / sp.factorial(n_), (n_, 0, sp.oo))
ok("C9b bare domination sums to e^x; growing moments 2^{j-1} = (sqrt2)^{2j}/2 sit inside",
   sp.simplify(s2_ - sp.exp(xdom))==0
   and all(sp.nsimplify(-mom(2*j)) == sp.nsimplify(sp.sqrt(2)**(2*j)/2) for j in range(1,4)))

# ================= C10: Plancherel display =================
# HS^2 of an x-parameterized symbol: int d^3k (2 pi)^3 ||sigma(., k)||_L2(ball)^2
# (Fourier convention factor symbolic). With |sigma| <= C_X min(1, om^{-2}):
CX = sp.symbols('C_X', positive=True)
Vb = sp.symbols('Vol_ball', positive=True)
low = sp.integrate(4*sp.pi*Kv**2*CX**2, (Kv, 0, k0))
high = sp.integrate(4*sp.pi*Kv**2*CX**2/Kv**4, (Kv, k0, sp.oo))
ok("C10a HS^2 <= (2pi)^3 Vol(ball) C_X^2 [(4pi/3)k0^3 + 4pi/k0] — finite, carrier-free",
   sp.simplify(low - sp.Rational(4,3)*sp.pi*CX**2*k0**3)==0
   and sp.simplify(high - 4*sp.pi*CX**2/k0)==0)

print("done")
```

Output, verbatim:

```text
C1a moments m_0..m_6 = 0,0,-1,0,-2,0,-4 (m_2j = -2^(j-1)): PASS
C1b odd-kill: stencil symmetric (w[+s2]=w[-s2]) kills every odd f: PASS
C1c l1 mass = 1; sum|w|l^2 = 1; |lambda|max = sqrt2: PASS
C1d level-repulsion weight sum = -i e^{iy} sin y ; jet -iy + y^2: PASS
C1e m_2-form (F leading weight): sum w (1 - i l^2 y) = +i y: PASS
C2a S^2 = I, S = S^dag, {S, alpha_j} = 0: PASS
C2b flip law S P_s(u) = P_{-s}(u) S ; equal-momentum stay = 0: PASS
C2c J5 stay factor: P_s(v)P_{-s}(u) = -(s/2) P_s(v)(alpha.(u-v)), both signs: PASS
C2d flip is full O(1): P_+(u) S P_-(u) = S P_-(u): PASS
C2e S J S = -J for J ~ alpha_x (anticommutation route): PASS
C2f band completeness at fixed momentum: P_+ + P_- = I: PASS
C3a |khat'-khat|^2 == 2|k x q|^2/(|k||k'|(|k||k'|+k.k'))  [component-level]: PASS
C3b forward scattering: factor vanishes exactly at q || k; antiparallel cap = 4: PASS
C3c small-transfer form: |khat'-khat|^2 = Q^2 sin^2(psi)/K^2 + O(Q^3): PASS
C3d angular average <sin^2 psi> = 2/3 (constant: angular zero gives NO k-power): PASS
C4a U floors n<=12: intra min(2o+c) = 2, inter = 3: PASS
C4b F floors n<=12: intra min(2o+c) = 2, inter = 3: PASS
C4c NO (c=1, o=0) class exists in either factor (n <= 12): PASS
C4d every o=0 class is pure-stay intra with c even >= 2: PASS
C4e assembled floors: intra*intra = 4 ; inter*inter = 6 ; overall min = 4: PASS
C4f floors stable under nmax 8 -> 12 (attained at small n; larger n only adds cost): PASS
C5a degree -1 alone: HS^2 = 4 pi C^2 (Lambda - k0) — LINEAR in carrier: PASS
C5b degree 0: HS^2 = (4 pi/3) C^2 (Lambda^3 - k0^3) — the C2 Lambda^3 shape: PASS
C5c 2o+c = 3 would be log-divergent: 4 pi C^2 log(Lambda/k0): PASS
C5d 2o+c = 4: tail = 4 pi C^2/k0 — finite, carrier-free: PASS
C5e double kill: leading term u1 f1/omega^2 (degree -2): PASS
C5f NEGATIVE CONTROL: [F]_0 = f0 != 0 leaves u1 f0/omega (degree -1 -> linear HS^2): PASS
C6a-1 H2^2 = Om^2 I (the closed form's engine): PASS
C6a-2 closed form solves dU/dT = -i H2 U, U(0) = I (unique solution): PASS
C6a-3 sea-sea A = cos(Om T) + i om sin(Om T)/Om: PASS
C6b unitarity |A|^2 + |B|^2 = 1 exact: PASS
C6c interband B = -i mu sin(Om T)/Om : |B| <= mu/Om <= mu/omega (degree -1 per lambda): PASS
C6d A_int - 1 decomposition (own check): PASS
C6e Om - om = mu^2/(Om + om) ; and <= mu^2/(2 omega): PASS
C6f |e^{iz}-1| = 2|sin(z/2)| <= |z|: PASS
C6g second term bound: mu^2/(Om(Om+om)) <= mu^2/(2 om^2)  [Om >= om]: PASS
C7a V_2(rho) = (8 pi^2/7)(1 - 128 rho^7)  [own symmetric route]: PASS
C7b Theta(0) = int_0^1 v = tau_R = pi/sqrt2 (l1 anchor): PASS
C7c V_2 additivity across the F-junction (any split point): PASS
C7d chi_hat_r(q) = 4 pi (sin qr - qr cos qr)/q^3  [own]: PASS
C7e envelope 1: z^3/3 - (sin z - z cos z) has derivative z(z - sin z) >= 0, value 0 at 0: PASS
C8a M1 = 64 pi^3 r/(45 om^2): PASS
C8b M2 = 256 pi^3 r^2 (2 om - 1/r)/om^2 <= 512 pi^3 r^2/om: PASS
C8c M3 = 512 pi^3 r^2/om: PASS
C8d uncapped tail = 256 pi^3 r^2 (Qc - 1/r)/om^2 : linear divergence as Qc -> oo: PASS
C9a sum x^n n^2 2^n/n! = 2x(2x+1) e^{2x} (entire) [own: n^2 y^n/n! = y(y+1)e^y]: PASS
C9b bare domination sums to e^x; growing moments 2^{j-1} = (sqrt2)^{2j}/2 sit inside: PASS
C10a HS^2 <= (2pi)^3 Vol(ball) C_X^2 [(4pi/3)k0^3 + 4pi/k0] — finite, carrier-free: PASS
done
```

(48/48 PASS. The four FAILs of my battery's first run were my own test bugs
— an even test function offered as odd, sympy matrix-exp/Abs simplification
forms — each diagnosed, corrected, and re-verified; the corrected checks are
the ones above. Nothing in the build failed at any point.)

---

## 8. FLAG BLOCK

```text
BOOKKEEPING = CONFIRMED(
  independent enumeration n <= 12 (build: n <= 8), both factors, jf in
  {0,1}: per-factor floors intra 2 / inter 3; assembled intra x intra 4,
  inter x inter 6, overall 4; NO (c = 1, o = 0) class anywhere; every
  o = 0 class is pure-stay intra with c even >= 2; band parity closes the
  mixed intra x inter routes for the sea-sea element; vertex alphabet
  exhaustive (flip/stay/J re-derived by my own matrix constructions); n = 0
  m_0-kill a-order-independent; NO missed degree-0 or degree-(-1) route —
  negative control run: [F]_0 = f0 != 0 would leave u1 f0/omega and LINEAR
  carrier growth, and does not obtain; the per-factor degree-(-1) V_2-form
  vs assembled degree-(-2) sharpening is lawful (sign ledger checked,
  V_2-additivity for ANY split point).)

JOINT_RERUN = MATCH(
  honest difficulty re-derived (degree -1 alone: HS^2 = 4 pi C^2 (Lambda -
  k_0), linear; degree 0: the C2 Lambda^3 shape); double m_0 kill supplies
  the second power ([X]_{-1} = 0, product ledger); rate chain re-derived by
  my own ODE-uniqueness route (H_2^2 = Om^2 I; A, B, unitarity, A_int - 1
  decomposition, Om - om = mu^2/(Om + om), second term <= mu^2/(2 om^2));
  transfer evaluation re-computed: k_0-split gives (4 pi/3) C_X^2 k_0^3 +
  4 pi C_X^2/k_0, finite, symbolic, carrier-free; threshold arithmetic
  2o + c = 3 log-divergent / = 4 finite with a power to spare; general-
  stratum coefficients sum to 2x(2x+1)e^{2x} (entire), growing moments
  -m_{2j} = (sqrt2)^{2j}/2 inside the domination; carrier-uniformity
  failure hunt: NO configuration re-enters the C2 shape. One finding, non-
  load-bearing (f1): §3.3a's INLINE rate display "(T/2 + 1/2)" is a
  dimensional slip for "(T/2 + 1/(2 omega))" — the §6 R5 chain is correct
  and the downstream object C_X min(1, omega^{-2}) is unaffected.)

ANGULAR_ZERO = CONFIRMED(
  the power: |k|^{-1/2} per coincidence insertion in joint L^2 — HALF the
  pointwise |k|^{-1} — and the angular zero itself supplies NO power of
  |k| (angular average <sin^2 psi> = 2/3, a bounded constant weight; the
  power is the RADIAL q/omega-vs-tail trade). Verified exactly, component
  level: the identity |k_hat'-k_hat|^2 = 2|k x q|^2/[|k||k'|(|k||k'| +
  k.k')]; zero at forward, cap 4 attained at antiparallel; capped mass
  M(omega) <= 64 pi^3 r/(45 omega^2) + 1024 pi^3 r^2/omega re-computed
  exactly; TIGHTNESS both directions: uncapped tail diverges linearly (the
  full pointwise power is genuinely unavailable), M3 piece genuinely
  Theta(1/omega). The build's CORRECTION of the tasked hypothesis is
  right: the missing power at the leading stratum is the PRODUCT
  structure, not the angular zero.)

REVERSAL_AUDIT = CONFIRMED(
  EXCLUDED, both routes sound: NORM (absolute HS convergence is
  rearrangement-immune; degree-0 intraband content incompatible with the
  carrier-free bound; the C4-reversal mechanism has no object — its
  growing moments are the dominated exponential's own coefficients) and
  LIMIT (dominated exchange; degree-0 content IS the large-omega limit =
  0). My hunt for the phase-free trick one level down found NOTHING: the
  J5 stay factor is Riesz-type (q-convolution, non-commuting alpha
  directions, no x-multiplicative structure) and admits no "-1"-split
  binomial kill; the pure-stay ladder resummation converges absolutely
  INSIDE the build's own norm argument. No reversal exists to exhibit.)

NET_VERDICT = CONFIRMED(
  PROVED at exactly the licensed strength: B-L2* DISCHARGED at the summed
  level — carrier-uniform HS control of the remainder (trilemma form
  against the sealed R-L2b require, read at path) + reversal exclusion —
  with the proof grade declared honestly: identity-grade steps 31/31
  re-run IDENTICAL from the build's verbatim script + 48/48 on my
  independent battery; estimate-grade steps (e-i)/(e-ii) declared, pinned
  by exact instances, in the sealed lineage's own currency (BL2STAR §4.3's
  check-confirmed grade). Debts carried unmoved and correctly worded:
  DEBT 2 (F'-14), DEBT 3 (summed-S2' spec act), the COMPUTE PRE-GATE,
  residues (r-b) Q-resolution, (r-c) free factor, (r-d) successor-spec.
  Per-pair nothing moves; M(t) sharp throughout; equal-time stays FALSE by
  C6. Not FAILS-AT / REFORMULATED / UNDECIDABLE-AT — each alternative
  checked and rejected for the stated reasons. Findings f1 (display slip)
  and f2 (relay transcription variance) are non-load-bearing and change
  nothing.)

PROVENANCE = CLEAN(
  artifact hash = tasked sha256, sidecar MATCH; 8/8 seals recomputed from
  bytes, all matching sidecars and tasked prefixes; every cited span
  re-read at path and carries what is cited; the build's script re-run
  reproduces its verbatim output; NOTE (f2): the relay copy of the flag
  block differs from the sealed bytes at 5 transcription-level points
  (punctuation, a wrap space, one elided FORBIDDEN_IMPORTS parenthetical)
  — non-semantic; sealed bytes are the authority and were checked
  directly.)

INJECTION = none(
  no instruction-like content anywhere in the artifact; fence vocabulary
  appears only inside the fence declarations; no numeric evaluation in
  the script; no register/tracker/plan/road/ledger/lens citation.)

MACHINERY_USED_BY_ME = yes(
  CAS — sympy 1.14.0, fresh venv rmchkvenv: 48 independent exact symbolic
  checks C1-C10 (own derivation routes, enumeration to n <= 12, negative
  controls C5f/C8d) reproduced verbatim in §7; plus the build's §6 script
  extracted and re-run byte-identically (31/31); shasum/file reads
  otherwise; NOTHING numeric evaluated.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
REMAINDER_UNIFORMITY_CHECK = SEALED.
```
