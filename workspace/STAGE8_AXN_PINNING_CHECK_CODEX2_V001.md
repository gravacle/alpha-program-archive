CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00003979
PREDECLARATION_REGION = EMPTY (closure opens at byte 0)
PREDECLARATION_OUTPUT_SCAN = 0 hits (empty byte interval; 27-token verdict sweep)
VERDICT_BEARING_SET = exactly the content-addressed members below plus the declared sealed-root hunt surface
CORPUS_HUNT_SURFACE = 1093 root-level Markdown artifacts having a .seal.sha256 sidecar at the search cut
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2
ROLE_THIS_RELAY = OPPOSITE-LANE CHECK ONLY
DIGEST_RULE = every pinned digest below was recomputed from local sealed bytes before reliance
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_984_PINNING_CHECK_CODEX2_V001.md` | `dff536d12cf1cfee00dc69babfd4563ad963daa116f0c70d32520899a2e3740d` | assignment |
| 02 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 03 | `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | process and flattening law |
| 04 | `QUESTIONS_SETTLED_REGISTER_V001.md` | `7b0f492aa5ee2e2e64fa8f5776c1f4da5dc1b242151244ce348fcb48b0b54001` | live-append register snapshot used for Q-814, Q-818, and Q-898 |
| 05 | `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md` | `4710453efd3fdb96e029aa1991a7e57b34f49e71b0d48184475bd98fb0f5ec15` | opposite-lane subject |
| 06 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | `StatePort_U2_008` source |
| 07 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | explicit placement-equation absence |
| 08 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `i_B`, `res_B`, missing square, `E_out`, non-uniqueness, SM-4 |
| 09 | `STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | independent-coordinate field audit and missing producer shape |
| 10 | `STAGE8_LAW_READY_STATE_SEALED_COMMON_ORIGIN_DETERMINATION_V001.md` | `6745b3c5d08d1c1df89dbcbce301ce66315bf640c26ff41332f77e456f477c7a` | supplementation certificates |
| 11 | `STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md` | `8e9a09c104f4b6352263591037b2e0bb9a82b659aa1b6276cdd48117f872acec` | direction firewall |
| 12 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md`; `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1`; `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab` | level-0 route and governing acceptance |
| 13 | `STAGE8_AXN_BASE_FAMILY_INVERSE_CODEX2_V001.md`; `STAGE8_AXN_K4_PROVENANCE_DARIO_V001.md`; `STAGE8_AXN_K4_PROVENANCE_CHECK_CODEX2_V001.md` | `e857302bd35c01dd7eb30062a48f2ec1937fc02f87a802aa7efd447e222465b2`; `bb503ebe33b499e9146fbe691b84001f10072cac7c44a16d383ce803e6e0fda4`; `a4384f6bf116f9572f1e6c4eb89261b7d701e945d16db00579c5276fea74cb9e` | comparison case for the `DERIVED HERE` reading rule |
| 14 | `STAGE8_AXN_HOTHER_FEASIBILITY_DARIO_V001.md`; `STAGE8_AXN_HOTHER_CHECK_CODEX2_V001.md` | `7899ea69ac9fde4e34725ec424305f002194cc2c6d1fc7f2eb7068f5d8d948e1`; `7f93e6246882cb409c483945603587277df84dcfddad997ea8fb410461df94c0` | self-narrowing and output-class theorem |

```text
BLIND HELD. EVERY SCALE REMAINS SYMBOLIC.
NO PHYSICAL QUANTITY WAS NUMERICALLY EVALUATED. NO MEASURED CONSTANT WAS CONSULTED.
PE-1..PE-15 ARE POINTERS ONLY AND CARRY ZERO VERDICT WEIGHT.
NO AMENDMENT, CHALLENGE, ENTRY, MEMBER, FUNCTIONAL, OR MEASURE IS AUTHORED OR SELECTED HERE.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN INDUCTION-PINNING CHECK — CODEX 2 LANE — V001
## RELAY 984 — `[PLAN:AXN-BUILD-C83]` — THE SIGNATURE-NECESSITY ARGUMENT

Date: 2026-08-11  
Status: **WEAKENS.** The subject's decisive inference does not follow from the sealed signature. The
port contains `omega_phys : B -> C` as a separate, unbound tuple coordinate beside `pi_omega` and
`rho_pre`; no equation in the port binds that coordinate to the density. The scalarization result
says the proposed equality is not instantiated, and the state-algebra map says the physical
placement square is missing. The restriction route is governing for the current instrument, but its
provenance remains authored at that instrument rather than forced by the earlier port signature.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

---

## 1. Pin and byte replay

The state brief and the opposite-lane subject both matched their relay pins before reading. Every
source in the subject's forcing chain also rehashed exactly. The load-bearing spans replayed as
follows:

| Source and half-open byte span | Recomputed span SHA-256 | Result |
|---|---|---|
| U2 partial assembly `[10824,11928)` | `63f743bfcc1df70bccb8cf1c0e3041c7a9202009c944c33adbfe8ae75da3535e` | full state-port declaration and non-instantiation text match |
| U2 `rho_pre` role `[7290,7829)` | `f9fb7a84ce4e3b954e5444baedc6703d9cc8ec52fe04f41e1b3b6d915b3d5372` | one positive normalized trace-class density role matches |
| TASK2D placement block `[11354,13537)` | `ad81601717e80b8872c367e95af2fce50129ed10e2601455a6b6356cf2f786f7` | equation is displayed as missing, not imposed |
| state map `res_B` `[13916,14477)` | `684efbe4784bcf745711c2a99acdea43ce1ae78d0395bd60e22155d20cf5742a` | exact restriction exists if a joint state exists |
| state map non-uniqueness `[15554,15965)` | `e9dafa5963128305d212f83b8c6617bfa47293f70bf91f5d7c0729fdb6828532` | continuum theorem matches |
| state map composition `[16298,16972)` | `67b224f03a4250db1f9e93b9c92dd2a7566768db91b8a74c7f1a1d9240c99c39` | candidate composition is displayed |
| state map SM-4 `[18649,18864)` | `1f49a5ae2bf1d776778dbe21408909917537e12d3146397df1678e81a48b9deb` | future construction output bars later attachment |
| direction firewall `[7027,8499)` | `e6926428b9c66c0a5e78f51daae4903cd3be2cebbd17bda0c9ce05880529e8b6` | record-state route is wrong-carrier and not transported |
| `OriginLawState_N` `[18627,20232)` | `f18549f66fb0babb9fec4f79aa514b6da85f97607e9d025cde517e45c9c8bbc2` | both named certificates occur |
| entry propagation `[21900,22459)` | `891a21c7eac0ca5d78a7d18a4b6f4a233bf0e6a0a8bcad909c139fd3d94421a8` | current instrument explicitly chooses exact restriction |
| anchor G4/G5 `[38169,39014)` | `ae1f79d9b7f24d2e0899f1116e38db3b51f72f0d89cc9983f8a00399f0076cee` | governing run bars independent `omega_B` |

The hashes establish custody. They do not replace the typing test below.

---

## 2. The signature: the reported tuple differs at the decisive predicate

### 2.1 What is verified

The exact tuple-only bytes `[10944,11210)`, SHA-256
`e3c24f6e0e8f41349126b0ea16dff16bdd6a7335611946b7f8b64c25f19534c4`, contain:

```text
StatePort_U2_008 := (
  omega_phys : B -> C,
  H_omega := completion of E_C0 / N_omega,
  pi_omega : A_C0 -> B(H_omega),
  rho_pre in trace-class(H_omega),
  rho_pre >= 0,
  Tr_omega(rho_pre)=1,
  state_domain,
  state_descent_map,
  state_provenance_certificate
).
```

Therefore these reported facts are exact:

- one explicitly named density coordinate, `rho_pre`;
- one explicitly named representation coordinate, `pi_omega`, with joint-algebra domain `A_C0`;
- one explicitly named history functional coordinate, `omega_phys : B -> C`;
- no named automorphism, conditional expectation, limit, or second normalization component;
- the booked typed arrow from `B` into `A_C0` is `i_B`. The state-map source `[10550,11209)`,
  SHA-256 `f3a376bab6831abfb4a85e2fb91eff37f4b6280ed5be6b5ccf1bd0d683498cb5`, derives the
  canonical inclusion `i_B(b)=1_A_SR tensor b` and no second booked `B -> A_C0` arrow surfaced.

### 2.2 What differs

The signature does **not** define `omega_phys` from `rho_pre` and `pi_omega`. It lists the functional
as its own coordinate. Neither a dependent type nor a predicate of the form

```text
omega_phys(b) = Tr(rho_pre pi_omega(i_B(b)))
```

occurs in the tuple. The unnamed `state_descent_map` and `state_provenance_certificate` coordinates
do not state that equality either. A closed tuple is a closed field inventory; it is not, merely by
co-location, an equality between fields.

The earlier TASK2D bytes make this non-equivalence explicit. At `[11736,12108)`, SHA-256
`78bbe760cf362bc941735d89e3f0be9eba9748350c204e12777e5305ee4e61db`, the artifact lists
`omega_phys`, `pi_omega`, and `rho_pre` separately, then says the sealed role does not provide an
equation of the displayed form **with both sides instantiated**.

The state-algebra map is sharper still. At `[16822,17244)`, SHA-256
`7b1ee5198923d54eb03d3f0942acfa8a8dfed7d96fa7edbdf2b20658da06ee47`, it says:

```text
These forms agree only when the physical placement square is filled:

Omega_C0(a) := Tr(rho_pre pi_omega(a)),
omega_phys(b) = Omega_C0(i_B(b))
              = Tr(rho_pre pi_omega(i_B(b))).

The scalarization result explicitly says that no such equation has both sides instantiated.
```

Thus `i_B` makes the restriction **well typed** once the joint state is supplied; it does not make
the restriction the signature's unique inhabitance rule. SM-4 later makes the equality a required
**construction output**. That is an authored acceptance rule at level 1/instrument scope, not an
equality forced by the older level-2 port bytes.

```text
SIGNATURE_CARDINALITIES = VERIFIED
NO_INDEPENDENT_HISTORY_SLOT = FALSE
COMPOSITION_WELL_TYPED_THROUGH_i_B = VERIFIED
COMPOSITION_FORCED_BY_PORT = FALSE
```

---

## 3. The invited corpus hunt — FOUND

### 3.1 Register first

The register was searched before the corpus hunt:

- Q-814 says `res_B` exists but the joint input is absent; record data leave a continuum and the
  physical selection must come from the origin side.
- Q-818 says the missing object is a target-independent common-origin joint state / StatePort
  inhabitant with both marginals and certificates.
- Q-898 records the subject's claimed forced reading and explicitly leaves this relay the press:
  find an accepting consumer or the section closes.

No earlier registered answer supplies the missing equality inside `StatePort_U2_008`.

### 3.2 Four M-2 modes

The search space was the cleanroom root's 1093 seal-sidecar-backed Markdown artifacts at the cut;
relay inbox/outbox self-reporting was excluded from positive candidate classification.

| Mode | Query discipline | Result |
|---|---|---|
| regex-metacharacter | escaped `State(B)`, arrows, parentheses, `omega_phys`, `omega_B`, and `induced_history_functional_sha256` | 26 candidate files for the protected exact tokens |
| line-wrap/whitespace | multiline search with whitespace admitted between `omega`, functional name, joint state/density/pairing terms | 12 candidate files |
| self-reference/context | every candidate read as operative receiver, historical report, rejected control, or mere mention | self-reports and rejection tables removed; operative signatures retained |
| hyphen/space/underscore | joint normalization of `history-functional`, `history functional`, `history_functional`, plus state-port variants | 85 candidate files; no hidden binding repaired the port |

### 3.3 The accepting receiver

The accepting receiver is the alleged forcing consumer itself:

```text
StatePort_U2_008.omega_phys : B -> C
StatePort_U2_008.rho_pre    : trace-class(H_omega)
StatePort_U2_008.pi_omega   : A_C0 -> B(H_omega)
```

Those are sibling fields, with no equality receiver. This is exactly the relay's test class: a
signature having a history-functional slot beside joint-state machinery. The port accepts the
history functional as a field to be supplied; it does not compute that field from `rho_pre`.

The later field audit at `STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md:[8902,10649)`, SHA-256
`20b0b65035f4dbf75ae72f2c62983f881a71cb8bca69fe568e9d9eaa83c7e77f`, reads the port in exactly
that typed way: `omega_phys` is **UNBOUND**, `rho_pre` is independently **WRONG CODOMAIN**, and
`state_descent_map` is independently **WRONG CODOMAIN**. Its missing-producer block
`[13534,13989)`, SHA-256
`309af304080cac6596ff52b07b9d6f1f35a1d9c13bd46a33829e3ed5eb401551`, adds the B-marginal
equality as part of the object still to be built. This same-lane artifact is corroboration only; the
opposite-independent decisive bytes are the port, TASK2D, and state-algebra-map spans above.

Other apparent candidates do not change the finding:

- the current entry instrument and anchor accept an `induced_history_functional_sha256`, but their
  propagation and G4 bind it to the exact restriction; they are the authored current route, not a
  second route;
- the entered P-HS/H-HAAR object authors a joint pairing and then proves its normalized restriction;
  it is not an independently accepted history member;
- the Legendre-action history-functional text belongs to a proposal killed at the gate (Q-321/Q-322),
  not a standing StatePort receiver;
- the older complete relative-history CTP functional is a different source-record amplitude receiver,
  not a `State(A_C0) -> State(B)` induction signature.

The hunt therefore finds a live typed counterexample without promoting any rejected proposal: the
port's own unbound coordinate.

---

## 4. The four import closures

All four named barriers are present at the cited bytes:

1. **Post-output supplementation / automorphism.** `OriginLawState_N:[18627,20232)` contains
   `Cert_target_independence` and `Cert_no_post_output_supplementation`; SM-4 at
   `[18649,18864)` rejects later attachment of an independently chosen `omega_phys`.
2. **Conditional expectation.** The state map `[14955,15966)`, SHA-256
   `dad215a43519a04c604ba507346aac1dfc0e8d17c7a027ebe16a1c032cd1beb3`, proves
   `E_out(1_R tensor b)=b`; scalarization still consumes `omega_B` rather than producing it.
3. **Record/history direction.** The firewall `[7027,8499)` distinguishes the outgoing-record
   inductive state from a doubled-history measure and says it is not transported into the history
   measure slot.
4. **Alternate normalization.** The port fixes `Tr_omega(rho_pre)=1`, while `res_B` preserves
   normalization because `i_B(1_B)=1_A_C0` at `[13916,14477)`.

These closures block the four named **imports**. They do not fill the already-declared
`omega_phys` coordinate, and they do not insert the absent equality into the port. In particular,
no-post-output supplementation says how a completed producer must behave; it does not prove which
functional an incomplete signature has already computed.

```text
IMPORT_1 = CLOSED-AT-BYTES
IMPORT_2 = CLOSED-AT-BYTES
IMPORT_3 = CLOSED-AT-BYTES
IMPORT_4 = CLOSED-AT-BYTES
UNIQUE_SATISFIER_FOLLOWS = false
```

---

## 5. Non-uniqueness: verified, and it identifies the live freedom

The theorem at `[15554,15965)`, SHA-256
`e9dafa5963128305d212f83b8c6617bfa47293f70bf91f5d7c0729fdb6828532`, is exact: for every
`omega_B in State(B)`, the product state `omega_out tensor omega_B` has the same record marginal;
the sealed history-state simplex is a continuum.

That theorem proves that the record marginal does not determine the history functional. It is the
void ground: choosing a member would consume a genuine free selection. It therefore supports the
subject's refusal to let a lane silently choose `omega_B`, but it does **not** select the restriction
or turn `omega_phys` into a dependent coordinate. The lawful consequence is a principal-visible
authorship point, not forced provenance.

```text
NONUNIQUENESS_THEOREM = VERIFIED
VOID_TARGET = the unbound history-functional coordinate / continuum selection
SELECTION_MADE_HERE = false
```

---

## 6. The three levels and the `DERIVED HERE` rule

The subject's levels are displayed accurately as locations:

| Level | Bytes | What the level actually establishes |
|---|---|---|
| 0 — current instruments | entry `[21900,22459)` and anchor G4/G5 `[38169,39014)` | exact restriction is governing for the present instrument; an independent `omega_B` is barred |
| 1 — state-algebra map | `res_B` `[13916,14477)`, FREEDOMS `[22701,23387)` SHA-256 `e02320e9055b3ab3e9f241bf621f1b64a57721c8d166b557ebb5501ead4e624c` | `i_B` and `res_B` are derived as algebraic maps; the input state and placement square remain missing |
| 2 — state-port specification | `[10824,11928)` | fields required of an inhabitant; no equation relates the sibling fields |

The durable reading rule itself survives this check. Three `DERIVED HERE` uses were checked:

1. base-family inverse `[12579,13006)`, SHA-256
   `1eeff2ec6f2f9a3825ec6e616ab097527d589355e82a286e022ea4c6a9d1f8be`, locates the weighted-
   projectivity derivation; its provenance was established separately by the upstream P3 consumer;
2. state-algebra map `[22701,23387)` locates the derivation of `res_B`; it does not itself prove a
   joint state or induction provenance;
3. the subject's own FREEDOMS block locates its unique-satisfier argument; the label does not
   authenticate that argument against the port bytes.

Thus three for three, `DERIVED HERE` is a locus marker rather than a provenance verdict. Applying
that rule consistently is exactly why the subject's third use remains checkable and fails here.

---

## 7. Self-narrowing and section consequence

The subject's §5 self-narrowing is accurate:

- the K4 theorem classifies positive output families independently of how an induction produced
  them: `lambda_N = mu tensor delta_1^(N-1)` at the current `N >= 1` indexing;
- the theorem alone does not empty H-OTHER, because non-Dirac first-circle `mu` can carry
  `NEITHER_EQUIVALENCE_PROVED` and pass K4;
- emptiness in the present instrument additionally uses faithfulness inherited through the exact
  restriction route, so the pinning is load-bearing for both instrument-specific impossibilities;
- adding a stage-0 trivial group would expose the first circle to the same appended-coordinate
  argument and collapse the free-`mu` coordinate, but this is an accurate counterfactual scope
  refinement, not a current stage-0 execution.

What changes is the provenance status of that load-bearing pinning. The current instrument remains
exactly pinned to restriction and the already-sealed impossibility theorems remain valid **of that
instrument**. What does not stand is the stronger wall-to-wall claim that the earlier port signature
forced the pin. The counterfactual receiver that does not inherit restriction-faithfulness is live
as a governance/authorship possibility. No such receiver is authored here.

```text
SECTION_CURRENT_INSTRUMENT = remains closed by its own governing restriction route
SECTION_WALL_TO_WALL_RECORD_NECESSITY = not established
COUNTERFACTUAL = live, principal-visible, not exercised
```

---

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  StatePort_U2_008 and its exact field inventory;
  rho_pre's sealed role;
  i_B and res_B as typed algebra maps;
  TASK2D's explicit uninstantiated-equation finding;
  the OriginLawState_N certificate names;
  SM-4, the direction firewall, trace normalization, and the non-uniqueness theorem;
  the current instrument's exact restriction route;
  the K4 output-class theorem and the already-sealed instrument-specific impossibilities;
  all gates.

DERIVED HERE:
  the field-dependency audit distinguishing tuple co-location from a binding equation;
  the four-mode accepting-consumer hunt;
  the classification of the port's omega_phys field as an unbound sibling coordinate;
  the separation of import closures from field binding;
  the WEAKENS consequence and its scope.

SELECTED HERE:
  nothing. No omega_phys, omega_B, Omega_C0, rho_pre, state_descent_map, branch, receiver,
  amendment, member, measure, or control is selected or supplied.

SCALING WEIGHTS:
  none consumed, fixed, evaluated, compared, or substituted. p_[A], r_0, and r_ch were not read or
  formed into a ratio.
```

---

## 9. Flattening, blind, and custody audit

- **S01–S37 flattening:** clean. Co-location was not flattened into equality; a typed inclusion was
  not flattened into a state-selection theorem; missing inhabitance was not flattened into
  impossibility; non-uniqueness was not flattened into a chosen member; instrument-specific closure
  was not flattened into record-wide necessity.
- **F_PLDEC:** held. All scale, rank, charge-weight, and measure symbols remain symbolic.
- **BLIND:** held. No rank, ratio, fiber content, or physical value was inspected.
- **PE-1..PE-15:** pointer-only and zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** the checked subject is Dario-lane. Same-lane antecedents are used only
  as corroborating pointers after the decisive opposite-independent source bytes had already
  established the field split.
- **Custody:** cleanroom-only. No register, plan, tracker, git, commit, push, chain, member binding,
  fixed-point execution, end test, or physical numeric evaluation occurred.

---

## 10. Final lines

```text
CLOSURE = declared-first (byte position, scan)
SIGNATURE = DIFFERS (omega_phys is a separate unbound functional coordinate; no tuple equality binds it to rho_pre/pi_omega)
HUNT = FOUND (StatePort_U2_008 [10944,11210); TASK2D [11736,12108); state map [16822,17244); weakens)
IMPORTS = 4/4-CLOSED-AT-BYTES
NONUNIQUENESS = VERIFIED (the void ground)
READING_RULE = VERIFIED-3/3
SELF_NARROWING = ACCURATE
VERDICT = WEAKENS (counterfactual live)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
