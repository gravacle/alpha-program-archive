# Stage 8 C_R Falsifier Design and Floor Source/Value Reconciliation v001

Date: 2026-07-30

## 0. Controlling result

**A physical falsifier for `C_R = 1` can be designed.** The cleanly typed
candidate asks a target-independent complete source-record-gravity action to
select the first durable, outwardly recoverable public record configuration
without receiving marginality as an input. A first configuration strictly
inside the recoverable set, or a complete derivation that excludes marginal
first closure while selecting a non-marginal first closure, would count
against `C_R = 1`. Equality would make the premise survive that test; an
incomplete construction would be inconclusive.

This is a **candidate test design**, not a frozen falsifier and not an executed
test. Freezing it is a principal act. Therefore:

```text
C_R_EQUALS_1_STATUS = ADOPTED_LEVEL_1_UNTESTED
C_R_PHYSICAL_FALSIFIER_DESIGNABLE = true
C_R_FALSIFIER_CANDIDATE_FROZEN = false
C_R_TEST_EXECUTED = false
C_R_TEST_RESULT = NOT_RUN
C_R_IS_DEFINITION_BY_UNFALSIFIABILITY = false
NEW_PREMISE_BEYOND_CURRENT_STACK_INTRODUCED = false
```

The released CAR primary source confirms that the proper-time unit value and
zero independent floor stiffness are **two premise classes**:

```text
PROPER_TIME_LOWER_ENDPOINT_TAU_EQUALS_1 =
  UNTESTED_COMPETITOR_OPEN
ZERO_INDEPENDENT_FLOOR_STIFFNESS_K_BARE_EQUALS_0 =
  TESTED_PENDING
FLOOR_UNIT_VALUE_AND_K_BARE_ZERO_SAME_PREMISE_CLASS = false
```

The later governing ruling Q-49 already preregistered that distinction.
The source read confirms it. The ratified lower bound therefore remains:

```text
VALUE_PATH_PREMISE_CLASSES_IN_BOUNDED_INVENTORY = 26
VALUE_PATH_UNTESTED_LOWER_BOUND = 17
VALUE_PATH_TESTED_IN_BOUNDED_INVENTORY = 9
LOWER_BOUND_CHANGED = false
```

No TESTED premise is reported proved. No premise is adopted, withdrawn,
retired, repaired, or promoted here.

## 1. Governing authority

### 1.1 Ratified counting unit

`/Users/bgm/MB Work/alpha_supervision/LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md:12-34`
ratifies the prior counting convention:

```text
COUNTING_UNIT = ONE_STATUS_AND_TEST_EQUIVALENT_PREMISE_CLASS
CLAUSES_WITH_THE_SAME_SEALED_STATUS_AND_THE_SAME_TEST_MAY_SHARE_ONE_CLASS = true
CLAUSES_WITH_DIFFERENT_EPISTEMIC_STATUS_SPLIT = true
MIRRORS_DUPLICATE_FLAGS_AND_DOWNSTREAM_CONSEQUENCES_COUNTED_AGAIN = false
ledger_scope_decision_needed = false
```

The same decision states:

```text
THE COUNT REMAINS A LOWER BOUND AND MUST ALWAYS BE REPORTED AS ONE.
...
17 of 26 UNTESTED (Q-45) is now a ratified lower bound, not a provisional one
```

Accordingly, this artifact does not call 26 an exhaustive corpus-wide premise
count.

### 1.2 CAR-source read/cite boundary

The same decision at `:38-61` permits only status reconciliation:

```text
CAR_SOURCE_READ = PERMITTED
CAR_SOURCE_CITE = PERMITTED
CAR_SOURCE_IMPORT_INTO_CONSTRUCTION = FORBIDDEN
CAR_SOURCE_USE_AS_PREMISE_OR_LICENCE = FORBIDDEN
SCOPE = establishing what the source SAYS, for classification and reconciliation only
```

It further says:

```text
No content of that source may enter a construction, act as a premise, license
a step, or supply a value. A lane that reads it may report what it says and
nothing more.
```

The CAR source is therefore quoted in Section 7 only to establish the
historical status and separation of its clauses.

### 1.3 Later authority Q-49

`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2044-2067`
rules:

```text
TWO. THE Q-45 STALENESS FLAG WAS WRONG ON THE MERITS, AND Q-46's PARTIAL
WITHDRAWAL GAVE THE WRONG REASON.
```

It distinguishes:

```text
Row 2, K_bare = 0 ... TESTED — PENDING.
...
Row 7, the proper-time floor: the premise is tau = 1, the UNIT VALUE
...
UNTESTED — COMPETITOR OPEN.
```

Q-49 then gives the controlling source/value distinction:

```text
Q-18 IDENTIFIED THE FLOOR'S SOURCE, NOT ITS VALUE.
...
It does not say the value is 1.
...
the two rows can carry different test statuses without contradiction
```

And it corrects the earlier causal diagnosis:

```text
the fence is not the cause. The cause is that source and value are different
claims.
```

The fence release remains a separate governance repair. This artifact does
not attribute the two-status distinction to the fence.

## 2. Roots, exclusions, and bounded method

The roots were:

```text
C   = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
      alpha_fundamental_record_action_cleanroom_v003
S   = /Users/bgm/MB Work/alpha_supervision
A_W = /Users/bgm/MB Work/alpha-program-archive/workspace
A_S = /Users/bgm/MB Work/alpha-program-archive/supervision
P   = /Users/bgm/Documents/New project/gravity_emergence_evidence_program
```

`A_W` and `A_S` were used only to identify mirrors. `P` excluded its nested
`C` subtree.

Every search excluded before descent:

```text
all a32_holdout and custodian_private subtrees;
.git;
dependency and cache trees, including .proof_deps and .python_deps variants;
node_modules;
review_packets;
stage8_execution;
and, in P, papers, raw, extracted, data, runtime_snapshots, and nested C.
```

The Lane-1 `Gamma_K`, non-return, and `OBS-06` filename families were excluded
from the evidence set and from all writes. No Lane-1 construction result is
used below.

Searches covered `*.md`, `*.json`, `*.py`, and `*.csv`. File lists were
NUL-delimited before conversion for display. No path list was piped into a
second search and no `xargs` was used. This output-under-construction was
excluded from every negative and file-list search. The superseded first
Departure-2 tracker was excluded after its current V002 successor was read;
the successor is accounted for explicitly below.

The exact superseded exclusion is the filename formed by concatenating:

```text
DEPARTURE_2_RESCALING_EXCLUSION_FOUR_CHANNEL_AUDIT_ + V001.md
```

## 3. What `C_R = 1` asserts as physics

### 3.1 The configuration

`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:29-47` identifies the
configuration:

```text
R_R = c T_R / 2.
```

and says:

```text
R_R is the maximum areal radius of the causal diamond with tip-to-tip
proper-time separation T_R.
```

On that maximal spherical section it defines the physical admissibility
condition:

```text
C_R = 2 G E_R / (c^4 R_R) <= 1.
```

and states:

```text
C_R > 1 is trapped and cannot be the outwardly recoverable primitive
comparison cell of this branch.
```

Thus the object is the primitive causal record cell in the declared ordinary,
isotropic `3+1` branch; marginality is with respect to the trapped/outwardly
recoverable boundary of its maximal spherical section.

### 3.2 The claim

The same source at `:49-60` states:

```text
Marginal public-closure rule (adopted Level-1 Gravacle rule)
```

and:

```text
The allow/require boundary is the first physically admissible public record
cell. The least positive T_R is attained at the boundary of the recoverable
set:

C_R = 1.
```

It identifies the role of the equality:

```text
This fourth input is the selector. Without it, public recoverability gives
a half-line of allowed durations and no absolute record scale.
```

At `:62-67` it preserves the epistemic status:

```text
It is the geometric meaning of the adopted "first admissible boundary" rule
...
A reviewer may reject that rule
```

`/Users/bgm/MB Work/alpha_supervision/RESULT_MODULUS_RADIUS_VS_RECORD_CELL_AND_THE_UNATTEMPTED_BRIDGE_2026-07-29.md:13-21`
gives the governing physical gloss:

```text
C_R = 1 IS, IN CONTENT, A MARGINAL SELF-GRAVITATION CONDITION. It places the
cell's areal radius at the gravitational radius of its own energy content,
and it is the corpus's ONLY absolute-scale selector.
```

The premise therefore asserts that the declared branch's first physically
admissible public primitive record cell occurs at the marginal
self-gravitating boundary, not at an interior member of the recoverable
family.

### 3.3 The present result is conditional

`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:33-48` says the product
identification requires the same microscopic theory to establish, among other
things:

```text
the CTP branch-energy difference equals the complete gravitating cell energy
after one fixed reference subtraction;
...
the energy is the one used by the chosen gravitational closure condition.
```

It then states:

```text
The relative action marker |Delta S_record|=pi hbar establishes none of these
identifications by itself.
```

The causal-cell source flags at
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:154-170` include:

```text
marginal_public_closure_rule_adopted_Level_1 = true
absolute_record_interval_derived_in_declared_branch = false
primitive_record_radius_derived_in_declared_branch = false
physical_durability_derived = false
```

Those flags keep the physical claim adopted and incomplete.

## 4. What is not a physical falsifier

### 4.1 The existing producer

`scripts/derive_bid_minimal_public_causal_cell_v001.py:2-5` says:

```text
This program verifies algebra and deformation sensitivity. It does not prove
the physical marginal-publicity premise
```

Its function signature at `:19-23` receives:

```text
scale_ratios(
    action_marker,
    causal_radius_fraction,
    compactness_threshold,
)
```

The result at `results/bid_minimal_public_causal_cell_v001.json:55-70` records:

```text
"compactness_threshold_chi_star": "1",
"marginal_public_closure_rule_status": "ADOPTED_LEVEL_1",
...
"physical_premise_proved_by_script": false
```

An executable that receives the threshold as an input cannot return physical
evidence against that input.

### 4.2 The Departure-2 forcedness audit

The current tracker
`/Users/bgm/MB Work/alpha_supervision/DEPARTURE_2_RESCALING_EXCLUSION_FOUR_CHANNEL_AUDIT_V002.md:3-14`
says it:

```text
SUCCEEDS
...
this is the same tracker, not a second plan document.
```

It reports:

```text
the audit cannot be run at all yet — it is acceptance check 5 of 5 on an
operator that was never built.
...
The audit is not discharged; it is re-scoped and re-sequenced.
```

The governing register makes the typing explicit at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:1951-1959`:

```text
THE DEPARTURE-2 FORCEDNESS AUDIT IS NOT A TEST OF C_R = 1.
...
Running it leaves C_R = 1 UNTESTED whatever it returns.
```

It is therefore not a test of whether the first physical cell is marginal.

## 5. Assessment of the three candidate falsifier forms

| Candidate form | Can it be stated in the bounded roots? | Genuine physical falsifier? | Current disposition |
|---|---|---|---|
| Target-independent derivation yields non-marginal first closure | **YES** | **YES** | Candidate design below; not frozen or executed |
| Upstream-preserving admissible counterconfiguration | **YES, with firstness and durability required** | **YES**, if it independently occupies the first durable/public role | None identified in the bounded direct-family inspection in Section 9.1 |
| Internal inconsistency with a separately sealed principle | **Only as a logical schema** | **NO concrete current test** | No independently sealed contradictory principle identified |

### 5.1 Target-independent derivation

The required derivation object is already typed at
`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:169-185`:

```text
Stage 3 closes only when one target-independent complete source-record-
gravity action:
```

followed by requirements to:

```text
derive the CTP Hamilton-Jacobi energy;
...
derive, rather than assume, the marginal closure condition; and
yield one isolated stable positive T_R/t_P.
```

The gate concludes:

```text
Until then the causal-cell formulas are exact conditional algebra, not an
absolute scale derivation.
```

`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:59-79` independently
requires the scale identification to follow from:

```text
the coupled gravity-source-record saddle or another target-independent
stationary condition
```

and requires:

```text
one isolated positive stable solution, before alpha or any endpoint is
evaluated.
```

Those sealed interfaces are enough to state a non-circular, failure-capable
candidate without adding a new physical premise.

### 5.2 Upstream-preserving counterconfiguration

The first three inputs are separately typed at
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:14-47`: the record-action
marker/interface, causal-diamond support, and public recoverability condition.
The fourth input at `:49-60` is the marginal selector.

A counterconfiguration must preserve the first three and independently
establish that a strictly submarginal configuration is the **first durable
public** record. Mere existence in the recoverable family does not suffice.
The source already says at `:134-142`:

```text
replacing C_R=1 by only C_R<=1 leaves
...
a half-line
...
and does not derive a unique cell.
```

It also says at `:144-152`:

```text
Until that interface is closed, the result is a conditional candidate
...
It also does not yet establish physical durability
```

Therefore an upstream-preserving first/durable counterconfiguration would be
evidence against the premise, while a freely inserted interior member of the
already acknowledged half-line would not. No executed physical
counterconfiguration was identified in the bounded direct-family inspection
reported in Section 9.1.

### 5.3 Internal inconsistency

A concrete inconsistency test would require a separately sealed principle,
independent of the marginal selector, that requires strict submarginality for
the first public record or otherwise forbids marginal closure.

The direct source does not supply that antagonist. It states `C_R <= 1` at
`BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md:38-47`, so equality remains
inside its declared recoverable set.

The split correction also does not supply it.
`STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md:31-41` separates:

```text
The allow/require constraint
```

from:

```text
local equality, least-member, or single-normalization moves
```

and `:64-65` describes this site:

```text
a strict untrapped inequality leaves a half-line; the marginal public-closure
rule supplies the selected boundary point.
```

The bounded negative and its exact file list are in Section 9. No current
separately sealed conflicting principle was identified. The inconsistency
candidate is therefore only a schema today, not a complete Q-37 test.

## 6. Candidate physical falsifier — designed, not frozen

The following is a design submitted for principal assessment. It is not an
adoption or a frozen gate:

```text
CANDIDATE_LABEL = F_CR1_FIRST_CLOSURE_MARGINALITY
AUTHORITY = CANDIDATE_ONLY_NOT_FROZEN

PRECONDITIONS:
  one target-independent complete source-record-gravity action;
  the action supplies the energy used by gravitational recoverability;
  fixed reference subtraction and no-spectator control;
  causal-diamond support and public recoverability retained;
  physical durability/publicity and firstness derived independently;
  marginality is not supplied as an input or hidden in "admissible".

REFUTE_C_R_EQUALS_1 IF:
  the completed construction selects a unique first durable/public,
  outwardly recoverable configuration strictly inside the recoverable set;
  OR it excludes marginal first closure while independently selecting a
  non-marginal first closure.

SURVIVE_THIS_TEST IF:
  the completed construction independently selects the marginal boundary.

INCONCLUSIVE IF:
  no complete action, no independently established first durable/public
  closure, or no isolated physical selection is obtained.

NO_REPAIR:
  no retuning of the action marker, support relation, energy convention,
  compactness threshold, or admissibility definition after the outcome.
```

The preconditions are the existing interface requirements quoted from
`BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md:169-185`; the isolated,
target-independent selection requirement is quoted from
`BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md:59-79`.

The criterion can return evidence against marginal self-gravitation. Therefore
the premise is not forced to be a definition by lack of any possible
falsifier. It remains UNTESTED because this candidate has not been frozen or
run.

## 7. CAR primary-source reconciliation

### 7.1 The unit lower-endpoint value

The read-only historical source is:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
primitive_boundary_native_record_CAR_functional_principle_v001.md
```

At `:30-41` it says:

```text
The same normalized Boundary Superconnection and record scale k_R define
both the source pole and the dimensionless proper-time coordinate
tau=s k_R^2. This minimal matching branch adopts the convention that the
first admitted boundary interval begins at

tau=1.
```

It immediately preserves a competitor:

```text
A different positive floor multiplier remains mathematically possible. The
choice tau=1 is a frozen target-independent branch convention, not a theorem
forced by the existence of one dimensionful scale.
```

V011 independently preserves the full competitor family at
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1094-1105`:

```text
V011 therefore admits rather than suppresses the two complete normalization
competitors
...
U_tau(A)=exp(-i tau B(A)), tau>0
...
Neither tau=1 nor c=1 follows from Fubini-Study geometry.
```

No test in these sources selects the unit member. The proper-time
lower-endpoint value remains `UNTESTED — COMPETITOR OPEN`.

### 7.2 Zero independent stiffness at that endpoint

The CAR source separately says at `:50-55`:

```text
The boundary-native Wilsonian matching-stage coefficient of this branch is
defined directly by the transverse q -> 0 second variation of the same
boundary-native functional. The branch adopts zero independent finite local
F^2 matching as its renormalization condition; symmetry alone does not force
that value.
```

The current induced-only source,
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_induced_only_boundary_action_principle_v001.md:5-23`,
states:

```text
Before durable public record formation there is no independent bare metric or
gauge stiffness.
```

and:

```text
The lower proper-time boundary is the first durable record scale;
Gamma_BR,k_R=0 states that no separate public stiffness is installed before
the record branch opens.

No independent local counterterm may be selected to repair a coupling.
```

FBRAP at
`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:121-141` says:

```text
K_bare[Q_spec, regulator, causal-cell scale] = 0.
```

and:

```text
This is an adopted compositeness condition
```

with the failure condition:

```text
The route fails to compute the coupling if the completed specification admits
an arbitrary finite deformation
```

Thus this clause has a failure-capable finite-deformation gate and remains
`TESTED — PENDING`.

### 7.3 Why the clauses are two premise classes

The primary source places the unit lower-endpoint convention at `:30-41` and
zero finite local matching at `:50-55`. It explicitly allows a different
positive floor multiplier while separately imposing zero independent local
matching.

They can fail independently:

```text
moving the positive floor multiplier does not itself install a finite F^2
term;
admitting an independent finite F^2 term does not itself move the proper-time
lower endpoint.
```

Their statuses and tests differ, so the ratified rule at
`LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md:18-29`
requires them to remain separate classes.

Q-49 at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2050-2067`
states the same distinction and says:

```text
Q-18 IDENTIFIED THE FLOOR'S SOURCE, NOT ITS VALUE.
```

The primary-source read confirms Q-49. It does not trigger the Q-49 reopen
condition at `:2075-2079`:

```text
REOPENS IF: lane 2's primary-text reading of the CAR-named source contradicts
row 7's status string, or a test selecting the unit tau member is found.
```

Neither event occurred.

### 7.4 Governance disposition

Q-49 at `:2064-2067` rules:

```text
I read "same content" as "same premise" and flagged a conflict that is not
there.
...
the fence is not the cause. The cause is that source and value are different
claims.
```

Therefore:

```text
Q45_STALENESS_FLAG = REFUTED_BY_SOURCE_VALUE_DISTINCTION
Q46_FENCE_CAUSAL_DIAGNOSIS = SUPERSEDED_BY_Q49
LANE2_V001_CLASSIFICATION_CORRECT = true
CAR_FENCE_RELEASE_REMAINS_SEPARATELY_IN_FORCE = true
```

Q-49 at `:2069-2073` separately leaves open whether `tau=1` is a second
scale-carrying unit adoption. This artifact does not answer that open
question.

## 8. Count reconciliation

The two rows remain:

| Premise class | Status | Failure-capable test |
|---|---|---|
| Induced-only zero floor boundary value / no independent public stiffness, `K_bare=0` | **TESTED — PENDING** | Completed specification admits an arbitrary finite deformation; Slot 18 is the separate premise-earning route |
| Proper-time lower-endpoint unit value, `tau=1` | **UNTESTED — COMPETITOR OPEN** | None frozen; the complete positive-`tau` family remains admitted |

No merge occurs:

```text
RATIFIED_LOWER_BOUND = 17_UNTESTED_OF_26
TESTED_CLASSES_IN_BOUNDED_INVENTORY = 9
COUNT_RESTATED_NOT_CHANGED = true
```

`C_R = 1` remains among the 17 UNTESTED classes until a principal freezes a
failure-capable criterion.

## 9. Complete bounded file lists

### 9.1 Direct `C_R` testability family

The direct family query covered:

```text
C_R = 1
C_R=1
marginal public-closure
marginal_public_closure
```

The complete `C` direct-hit list, excluding this output-under-construction,
was:

```text
BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
STAGE8_G3_SLOT18_MODULUS_PROCESS_DEFECTS_REGISTER_RECORD_V001.md
STAGE8_LANE_STATUS.md
STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md
results/bid_minimal_public_causal_cell_v001.json
scripts/derive_bid_minimal_public_causal_cell_v001.py
```

The complete `S` direct-hit list was:

```text
AXIOM_SET_MAP_2026-07-29.md
BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md
BOHM_CTP_ABSOLUTE_RESPONSE_ROUTE_SWEEP_2026-07-28.md
BOHM_GR_EM_RELATION_REFUTED_2026-07-29.md
BOHM_RATIO_ROUTE_ADJUDICATION_RESULTS_2026-07-28.md
BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md
CONTINUATION_STATE.md
LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md
QUESTIONS_SETTLED_REGISTER_V001.md
RATIO_ROUTE_CROSS_CITATION_MAP_2026-07-28.md
RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md
RELAY_PASTE_126_ACTION_FORM_2026-07-29.md
RELAY_PASTE_129_GOVERNING_CHAIN_REGISTRATION_AND_CROSS_SECTOR_SPEC_2026-07-30.md
RELAY_PASTE_149_PREMISE_TEST_CLASSIFICATION_2026-07-30.md
RELAY_PASTE_154_CR_FALSIFIER_DESIGN_2026-07-30.md
RELAY_PASTE_156_EINSTEIN_ENUMERATION_AUDIT_2026-07-30.md
RESULT_MODULUS_RADIUS_VS_RECORD_CELL_AND_THE_UNATTEMPTED_BRIDGE_2026-07-29.md
THEORY_CANDIDATE_001_COUPLING_LOCATION_BY_EXHAUSTION_V001.md
```

The complete archive-mirror direct-hit lists were:

```text
A_W:
  A_W/BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  A_W/STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  A_W/STAGE8_G3_SLOT18_MODULUS_PROCESS_DEFECTS_REGISTER_RECORD_V001.md
  A_W/STAGE8_LANE_STATUS.md
  A_W/STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md
  A_W/results/bid_minimal_public_causal_cell_v001.json
  A_W/scripts/derive_bid_minimal_public_causal_cell_v001.py

A_S:
  A_S/AXIOM_SET_MAP_2026-07-29.md
  A_S/BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md
  A_S/BOHM_CTP_ABSOLUTE_RESPONSE_ROUTE_SWEEP_2026-07-28.md
  A_S/BOHM_GR_EM_RELATION_REFUTED_2026-07-29.md
  A_S/BOHM_RATIO_ROUTE_ADJUDICATION_RESULTS_2026-07-28.md
  A_S/BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md
  A_S/CONTINUATION_STATE.md
  A_S/LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md
  A_S/QUESTIONS_SETTLED_REGISTER_V001.md
  A_S/RATIO_ROUTE_CROSS_CITATION_MAP_2026-07-28.md
  A_S/RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md
  A_S/RELAY_PASTE_126_ACTION_FORM_2026-07-29.md
  A_S/RELAY_PASTE_129_GOVERNING_CHAIN_REGISTRATION_AND_CROSS_SECTOR_SPEC_2026-07-30.md
  A_S/RELAY_PASTE_149_PREMISE_TEST_CLASSIFICATION_2026-07-30.md
  A_S/RELAY_PASTE_154_CR_FALSIFIER_DESIGN_2026-07-30.md
  A_S/RELAY_PASTE_156_EINSTEIN_ENUMERATION_AUDIT_2026-07-30.md
  A_S/RESULT_MODULUS_RADIUS_VS_RECORD_CELL_AND_THE_UNATTEMPTED_BRIDGE_2026-07-29.md
  A_S/THEORY_CANDIDATE_001_COUPLING_LOCATION_BY_EXHAUSTION_V001.md
```

`P`, with nested `C` excluded, returned no direct hit.

Three additional `C` files supply the typed derivation/test interface quoted
above but do not match the four literal direct-family strings. The current
Departure-2 V002 successor is also included because it supersedes the
excluded first tracker:

```text
C:
  BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md
  BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md
  STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md
A_W:
  A_W/BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md
  A_W/BID_CTP_HAMILTON_JACOBI_SCALE_BRIDGE_GATE_V001.md
  A_W/STAGE8_GRAVITY_EVIDENCE_ADMISSION_AND_FIVE_STRATA_BINDING_V001.md
S:
  DEPARTURE_2_RESCALING_EXCLUSION_FOUR_CHANNEL_AUDIT_V002.md
A_S:
  A_S/DEPARTURE_2_RESCALING_EXCLUSION_FOUR_CHANNEL_AUDIT_V002.md
```

### 9.2 Bounded negative for a present inconsistency antagonist

The source-family list was intersected with the null-safe file list for:

```text
strict_untrapped
strict untrapped
strictly untrapped
C_R < 1
C_R<1
C_R <= 1
C_R<=1
cannot equal 1
incompatible with marginal
contradicts marginal
marginal closure impossible
```

The complete intersection was:

```text
C:
  BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md

S:
  AXIOM_SET_MAP_2026-07-29.md
  BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md
  BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md
  RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md
  RESULT_MODULUS_RADIUS_VS_RECORD_CELL_AND_THE_UNATTEMPTED_BRIDGE_2026-07-29.md

A_W:
  A_W/BID_MINIMAL_PUBLIC_CAUSAL_CELL_DERIVATION_V001.md
  A_W/STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  A_W/STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md

A_S:
  A_S/AXIOM_SET_MAP_2026-07-29.md
  A_S/BOHM_CONSISTENCY_CONDITIONS_SWEEP_2026-07-28.md
  A_S/BOHM_TWO_STEPS_WORKFLOW_RESULTS_2026-07-28.md
  A_S/RECOVERY_STIFFNESS_AND_ALLOW_REQUIRE_2026-07-29.md
  A_S/RESULT_MODULUS_RADIUS_VS_RECORD_CELL_AND_THE_UNATTEMPTED_BRIDGE_2026-07-29.md

P:
  []
```

The direct source says `C_R <= 1` and the split correction says the strict
inequality leaves a half-line whose boundary is supplied by the adopted rule.
None of the files states a separately sealed principle that forbids the
marginal equality. This is the bounded basis for classifying the
internal-inconsistency proposal as an uninstantiated schema.

### 9.3 Floor/source-value family

The direct governing/source list was:

```text
P:
  primitive_boundary_native_record_CAR_functional_principle_v001.md
  alpha_induced_only_boundary_action_principle_v001.md

C:
  FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md
  STAGE8_OPERATOR_FLOOR_BOUNDARY_CHAIN_CANDIDATE_DERIVATION_V001.md
  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md
  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md

S:
  RESULT_FLOOR_BOUNDARY_VALUE_SETTLED_2026-07-30.md
  QUESTIONS_SETTLED_REGISTER_V001.md
  LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md
  AXIOM_SET_MAP_2026-07-29.md

A_W:
  A_W/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md
  A_W/STAGE8_OPERATOR_FLOOR_BOUNDARY_CHAIN_CANDIDATE_DERIVATION_V001.md
  A_W/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md
  A_W/STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
  A_W/STAGE8_STANDING_PREMISE_TEST_CLASSIFICATION_V001.md

A_S:
  A_S/RESULT_FLOOR_BOUNDARY_VALUE_SETTLED_2026-07-30.md
  A_S/QUESTIONS_SETTLED_REGISTER_V001.md
  A_S/LEDGER_SCOPE_AND_CAR_FENCE_RELEASE_DECISION_2026-07-30_V001.md
  A_S/AXIOM_SET_MAP_2026-07-29.md
```

The CAR primary source itself has SHA-256:

```text
099440a4ca77d022ffb7a1977834c83155c9b53e3958fb56efa926e16c5ba0b7
```

That hash identifies the read-only source; it is not imported or copied.

## 10. Protected and authorization status

```text
artifact_type =
  CLASSIFICATION_RECONCILIATION_AND_FALSIFIER_DESIGN_ONLY

CAR_SOURCE_READ = true
CAR_SOURCE_CITED_FOR_STATUS_ONLY = true
CAR_SOURCE_IMPORTED_INTO_CONSTRUCTION = false
CAR_SOURCE_USED_AS_PREMISE_OR_LICENCE = false

C_R_PHYSICAL_FALSIFIER_DESIGNABLE = true
C_R_FALSIFIER_FROZEN = false
C_R_TEST_EXECUTED = false
C_R_EQUALS_1_STATUS = UNTESTED
NEW_PREMISE_BEYOND_CURRENT_STACK_INTRODUCED = false

FLOOR_AND_K_BARE_SAME_PREMISE_CLASS = false
PROPER_TIME_TAU_EQUALS_1_STATUS = UNTESTED_COMPETITOR_OPEN
K_BARE_EQUALS_0_STATUS = TESTED_PENDING
RATIFIED_LOWER_BOUND = 17_UNTESTED_OF_26

TAU_EQUALS_1_SECOND_SCALE_CARRYING_ADOPTION_RULED = false
PREMISE_ADOPTED_OR_WITHDRAWN_HERE = false
PHYSICAL_TEST_EXECUTED = false
MEASURED_CONSTANT_COMPARISON_PERFORMED = false
PROTECTED_HOLDOUT_CONTENT_ACCESSED = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
kappa_Thomson_computed = false
coupling_computed = false
radius_computed = false
scale_computed = false
root_computed = false
eigenvalue_computed = false
beta_function_computed = false
```
