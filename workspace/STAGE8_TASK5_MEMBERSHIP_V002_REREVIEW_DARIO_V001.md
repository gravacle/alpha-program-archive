# STAGE 8 / TASK 5 / EQ6 — MEMBERSHIP V002: THE LAST RE-REVIEW — DARIO V001

Lane: Dario (Claude Opus 5), cross-family review/adjudication lane
Task: PASTE 597 / Task 5 / Step 6
Authority to review: DoR-020-A8. **THIS ARTIFACT ADOPTS NOTHING, REGISTERS NOTHING, AND RULES NOTHING.**
Custody: the last review of Task 5. The A2 re-adjudication and both closing rulings rest on it.

```text
REGISTER_HEAD = Q-527
MEMBERSHIP_V002 = CONFIRMED (+2 NOTES, neither a kill)
READY_FOR_READJUDICATION = yes
VERB_AUDIT_SELF = CLEAN
D_REPAIRS = 4/4 CLOSED at the standard I set
CARRY_ROWS = 7/7 at sealed type
MEMBER_BOUND = false ; FIXED_POINT_EXECUTED = false ; END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
MACHINERY_APPEAL = none of my own
```

**The one thing the re-adjudication must not miss** is N1 below: V002 books the
failure of `A_extent` at tower-contingent strength, but every premise for its
**permanence** is displayed in V002 itself. This does not weaken the artifact —
it *sharpens* the decision A2 has to make, and it converts that decision from
"defer pending more stock" to "choose between two named options."

## 0. Preflight

| Check | Result |
|---|---|
| Register head Q-527 | verified |
| `STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V002.md` = `9b019ae3b0fc64f5fc01bf7e2493346344f7052a4ef1a18339f416de68b80d95` | **verified before reading** |
| Line count 1,012 as stated | verified |
| My V001 kill review = `9caef0f7fa23b7f77fd2dfa27e003b706b3572419480e6333e8b851cfcfe2906` | verified |
| CARRY-1 source (JII) = `fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70` | verified |
| Output name collision (workspace + cleanroom) | none — clear to write |

V002's own head reads `Q-526` against the paste's `Q-527` — the familiar
one-step drift, not staleness.

**Method disclosure.** I read the lead, §1.1–1.3, §3.2, §4.3–4.4, §5.1–5.4, §6
personally, and ran a parallel audit (11 agents, adversarially verified). Ten of
eleven load-bearing agent claims were refuted on verification. **One survived,
and I re-derived it myself from sealed text before booking it — it is N1.** No
unverified agent claim is carried onto this board.

---

## E1. THE FOUR D-REPAIRS AGAINST MY OWN KILLS — **PASS ×4**

### E1.1 D1 (scope-shrink: clause (vi) absent from `Mor(I_F)`) — **CLOSED**

[PROVABLE] The category is restored at the definitional level. `(I1)` reproduces
the guard's six-clause generation verbatim including clause (vi), and goes
further than my kill required by **de-narrowing clauses (ii) and (v)** — the two
lesser narrowings I recorded but did not make load-bearing:

> *"item (ii) is not narrowed to `A_RP`, and item (v) is not narrowed to an
> `A_RP/A_CC` shorthand."*

I ran the decisive mechanical check myself: **`Mor(I_F)` occurs in all 1,012
lines exactly twice** — at line 105 (the guard definition) and line 317, which is
a *denial* (*"It is not `FC6 PASS for every f in Mor(I_F)`"*). No row in V002
asserts a component over the shrunken category, so the shrink cannot recur.

**On my "closure by exhibition" standard,** the consumer side is exhibited and
the arrow side is not — and V002 says so in the same section rather than
papering over it:

```text
CONSUMER_NATIVE_REGISTRY = COMPLETE (+10 rows, +17 names)
CONSUMER_GEOMETRIC_ARROW_MANIFEST = PARTIAL_GENERATED (+6 schemas)
FULL_GEOMETRIC_CLAUSE_VI_EXHIBITION = OBSTRUCTED
```

That is the honest disposition, and **the kill still closes**, because it closes
by my own second repair route. My review offered two: *display the consumer
arrows inside the four classes, or restore the class and re-run the guard's B2.6
asymmetric-primitive attack against the repaired chain.* V002 takes the second.
§2.2 reruns B2.6 and reports the attack **still succeeding**:
`ASYMMETRIC_PRIMITIVE_RETAINED = yes`, `R1_NATURALITY_ON_a_xi = absent`,
`FC6_PHYSICAL_SECTION_ON_a_xi = open`, `ASYMMETRIC_COMMON_REFINEMENT = UNBUILT`.

The kill was never "the category is wrong" — it was **"the build proves
something weaker than membership requires while reporting membership."** V002
reports no membership. The primitive is retained rather than deleted, and the
map-definition burden is relocated into named, failure-capable premises
(`A_vi`/`A_pkg`). That is exactly the lawful escape from guard B2.6's binary.

*Sub-note (not a defect):* §2.2's rerun is transcribed from the Q-491 manifests
artifact rather than independently re-executed after the Q-493 Loc-naturality
relay. I checked what would have to change: the missing objects are the target
`G_(ac)` and the two far legs — far-cell geometry that no typing or naturality
repair supplies. The transcribed verdict stands.

### E1.2 D2 (FC6 over-claim; J2's `A_RP` half posed, not proved) — **CLOSED**

[PROVABLE] **FC6 half.** The killed universal is retracted *in terms* at line
317, and the replacement is the four-class board `(D2.1)` at exactly the
certificate's scope: `FC6 = PASS_ON_PROVED_SCOPES`,
`FC6_ASYMMETRIC_PHYSICAL_TERM = TYPE-U`. I checked the four rows against my own
FC6 determination clause by clause; the zero-source row carries the disclosure
my determination made mandatory — *"vacuous by empty source, not by a metric
theorem."* The category slip I flagged at §2.4 is also answered: the no-mixing
conjunct is sourced to Q-434 and **not** inferred from the A4 quotient carrier.

[PROVABLE] **J2 half — and this is the part that matters.** My kill was that the
rank-preserving half was *posed, not proved*, with `A_RP^+` never established
nonempty. V002 does not re-pose it. It converts it into a **predicate inside an
antecedent** and then says so explicitly:

> *"The formulas in `(D2.2)` are predicates inside an antecedent. They do not
> produce `L`, do not evaluate the seed, and **do not show that `P(L)` is
> inhabited**."*

and again at §4.4: *"current stock has no formed physical `L`, **no established
inhabitance of `P(L)/A_RP^+(L)`**, and no nonidentity J2 base case."*

I probed this as an attack of my own — whether `A_J2` is a second false package
that (D6.1a) fails to disclose — and found the disclosure already present in
both sections. The substance of my kill is carried as an explicit
non-establishment, which is the correct handling: a premise may name an
uninhabited set; a *theorem* may not.

### E1.3 D3 (J7 discharged by J2's own equation) — **CLOSED**

[PROVABLE] The J7 face is genuinely distinct, not J2 renamed. Its contract is
the `R`-valued rank-one coefficient pair `(D3.1)`–`(D3.2)`, character-matched to
the demand of record in the sealed diamonds artifact, and V002 states in terms
that **no bottom-valued A6 equality is substituted for it**. That was the exact
defect: consuming A6's weaker bottom square *as* the coefficient face. §4.3's
`(O2-old)` row keeps the A6 square explicitly labelled as the weaker comparison
with scalar J7 remaining post-scope — which is the labelling discipline my
adjudication demanded.

`(D3.7)` then reports the result at its true strength:
`COMPLETION_IMPLICATION_ON_D_form^cert = PROVEN`,
`FC11_ACTUAL_FULL_TOWER = CONDITIONAL/CARRIED`, and V002 adds *"it does not
promote J7 to `PASS`."*

### E1.4 D6 (five rows report a stronger verb than their sources carry) — **CLOSED**

[PROVABLE] I re-ran the verb audit myself across **every** row of §5.1 rather
than reading V002's §9.4 self-audit. Result: **no row over-claims.**

| Row | Final tag | Verdict |
|---|---|---|
| FC1, FC4, FC5, FC8, FC13 | `PROVABLE — PASS` | warranted; FC8 carries the honest rider "no global bound" |
| FC2, FC3, FC7 | `PROVABLE on displayed arrows / TYPE-U residue` | **lowered**; clause-(vi) endpoints deferred to `A_vi/A_pkg` |
| FC9 | `PROVABLE on displayed scopes / TYPE-U residue` | **lowered** |
| FC6 | `PART-PROVABLE — PASS ON PROVED SCOPES / TYPE-U residue` | matches the guard's own grading exactly |
| FC10 | `CONDITIONAL / CARRY row CR1` | **no longer PASS** — my D2/D3 jointness kill closed |
| FC11 | `CONDITIONAL / CARRY row CR2` | **no longer PASS**; "the unchanged full-extent row is not discharged" |
| FC12 | `STRUCK / NOT IN GUARD` | correct |

All five formerly over-strong rows are lowered. Six rows now carry an explicit
TYPE-U residue and two are CONDITIONAL. I note without charging it that FC2/FC3
are graded at or below what **my own review** granted them (I credited FC2/FC3
`PASS` on the four-coordinate package); V002 states the same content with the
residue named, which is more precise, not weaker. **The build is harder on
itself than I was.**

---

## E2. THE SEVEN CARRY-1 ROWS — **PASS. 7/7 at sealed type.**

I checked each row against JII §7.4's `(CARRY-1)` text and against the sealed
source in V002's own ledger, testing **both** failure modes.

| CR | Carried | Sealed type | Over? | Under? |
|---|---|---|---|---|
| CR1 | FC10 | conditional on `A_vi/A_pkg/A_J2/A_J7` (+`A_extent` unchanged) | no | no |
| CR2 | FC11 | `(D3.6)` on supported squares only | no | no |
| CR3 | `family(Pkg_N^H)` | TYPE-U, potentially empty | no — exactly CARRY-1's licence | no |
| CR4 | `family(Pkg_N^Hol)` | TYPE-U, potentially empty; *"does not inherit an H-route tail"* | no | no |
| CR5 | A9 interface | `ADOPTED/LICENSING, not inhabitance`; five gates open | no — matches the ledger ceiling verbatim | no |
| CR6 | seed | `END_TEST_STRUCTURAL / unexecuted`; neither `(S28)` nor its negation assumed | no | no |
| CR7 | ExtSrc | `STOPPED / TYPE-U` | no | **no** — it affirms that quotient inverse and scoped covariance *are derived* rather than hedging the whole row |

**Nothing over-carried.** CR3/CR4 keep the families potentially empty; CR5 keeps
adoption separate from inhabitance; CR7 matches the ExtSrc round-2 result's
sealed type. The row count is fenced against drift: §6 states that FC6's
asymmetric physical term is *"a typed source residue … not a membership
antecedent and not one of the seven rows."*

**Nothing under-carried.** I looked specifically for a proven row needlessly
conditionalized and found the opposite discipline at CR7, which names its
derived content inside a STOPPED row.

[PROVABLE] **One item of custody interest.** `(C2)` now carries

```text
A7 : BOTH E_C branches affirmatively retained
```

with `(C1)` displaying both branches and line 762 adding *"Neither branch is
selected or omitted."* This is precisely the **N3 gap I raised against CARRY-1
at Q-521** — that CARRY-1 carried the *prohibition* on branch selection but not
A7's *positive* obligation to retain both, so a guard could lose a branch by
omission without ever selecting. The positive obligation is now in the carriage.
I record the repair as verified; I do not claim causation.

---

## E3. THE VACUITY STATEMENT — **PASS, exact on all four counts**

**1. Is the vacuity disclosed?** Yes, in V002's own word, three times, including
in the machine-readable stop board:

> *"This disclosure is why the full-guard implication below is formally proved
> but **currently vacuous** rather than a live membership certificate."*
> `FULL_GUARD_IMPLICATION = FORMALLY_PROVEN_BUT_CURRENTLY_VACUOUS`

Nothing is hidden. A material implication with a false antecedent is announced
as such.

**2. Is the proof itself vacuous?** **No — and this is the distinction that
matters.** The proof of `(D6.2)` is *relevant*, not *ex falso*. It assumes the
antecedent locally and derives **each conjunct** from a named package: FC1/FC4/
FC5/FC8/FC13 hold unconditionally; `A_vi`/`A_pkg` extend the confirmed package
arrows *"without deleting `G6`"*; `A_J2` and `A_J7` close **two distinct face
rows**; `A_extent` places every FC11 demand inside `D_form^cert(L)`. It would go
through if the antecedent were true. It also refuses a smuggle I checked for
specifically: the FC6 asymmetric physical term *"remains TYPE-U and is not added
to the antecedent."*

**3. Is the falsified extent correctly attributed?** **Yes — to FC11's contract
extent, not to the tuple.** The failure is that `D_CC^disjoint` and
`D_CC^contact` *"have demanded O1/O2 positions outside the formable support"* —
a statement about what the unchanged guard **demands**, not about `F_actual^sc`
being defective. The datum is not impugned anywhere. Each of the six packages is
failure-capable with a named one-element falsifier.

**4. Is the support-qualified candidate correctly typed, and A2's obligation
stated?** Yes. `A_scope = NOT_ADOPTED`; `(D6.2a)`'s consequent is explicitly
*"not `FiniteCoherent_020^V002`"*; §4.3 closes *"no decision of record has
adopted it as a replacement guard."* I searched for a silent substitution of the
support-scoped form for the full form and found none. The freezing obligation is
correctly located as A2's to discharge, not this artifact's:
*"this artifact does not perform that adoption."*

---

## E4. THE FINAL STATEMENT AS A UNIT, AND ONE FRESH ATTACK

### E4.1 What is proven jointly, at its true strength

Walking it once as a unit: **five conjuncts hold unconditionally on the one
scoped datum** (`FC1, FC4, FC5, FC8, FC13`, per §5.4); four hold **on displayed
scopes with a named TYPE-U residue** (`FC2, FC3, FC7, FC9`); `FC6` holds
PART-PROVABLE on its certificate's scopes; `FC10` and `FC11` are **not claimed
proved at all** — they are CONDITIONAL carry rows.

I tested the jointness failure mode the relay names — a conjunction of results
each true on a *different* scope, silently presented as one joint result — and
it does not bite. The five unconditional rows hold on the whole of
`F_actual^sc`, so no per-coordinate scope conjunction arises for them; the rows
where scope genuinely varies are exactly the rows tagged with residues; and the
row where jointness is really at stake, `FC10`, is tagged *"jointly typed"* and
CONDITIONAL rather than proved. **The lead's list is a selection of the
PROVABLE-tagged rows and omits every CONDITIONAL/TYPE-U row** — it is not an
unchecked joint over-claim.

`F_actual^+` adding no new coordinate is stated and fenced: *"the same thirteen
coordinates with the missing lawful arrow actions and equalities supplied … one
extended tuple, not a second choice of component data. Without those premises
only `F_actual^sc` is formed."*

The old uniformity contradiction from my §1.3 is **cured by withdrawal**: V002
makes no per-component `Mor(I_F)` claim anywhere (verified mechanically above).

### N2 — NOTE: `Scope_component` is declared but its value is never displayed

[PART-PROVABLE] §1.2 names the thirteenth coordinate and says *"`Scope_component`
records each coordinate's maximal sealed arrow domain"*, with the correct
anti-filling rider. But **no value is given**, and V002 supplies no replacement
per-coordinate table — it relies on a hash-verified pointer to V001's exhibit.
Since V002's category is strictly larger than V001's, an adjudicator cannot read
the per-coordinate domains off this artifact alone.

I grade this an **exhibition debt, not a contradiction**, and specifically not a
recurrence of my §1.3 kill: the upstream data exists in the guard, which supplies
a maximal-proved-scope column, and V002's rows carry no arrow quantifiers to be
contradicted. Repair: tabulate the thirteen maximal sealed domains. **Not a
kill.**

### N1 — FRESH ATTACK (survived adversarial verification; re-derived by me)

**`A_extent` is not merely false on the current tower. It is unsatisfiable for
every candidate `L`, by geometry — and V002 displays every premise for that
conclusion while booking it at the weaker strength.**

The chain, each link from V002's own displayed text:

```text
D_form^cert(L) := {D in D_cert : every boundary e is in A_RP^+(L)}   (V002 §4.2)
A_RP^+(L)      := {f:X->Y in A_RP : X,Y in P(L)}
P(L)           := {X : m_T,X(L) != 0}
m_T,X(L)       := P_H,X Loc_X^L Kernbar_X Q_X(L_T)                   (D2.2)
acyclic (O1-Z) : H_X={0} ==> P_H,X=0 ==> m_T,X=0                     (§4.3)
```

The step that carries the weight, and which I verified directly: `P_H,X` is the
**leftmost** factor of `m_T,X(L)`. If `P_H,X = 0` then `m_T,X(L) = 0` **for every
`L` whatsoever**, whatever `Loc_X^L` may later turn out to be. So an acyclic
vertex lies outside `P(L)` *universally*, not contingently. My own J7
adjudication already recorded the reason as **"geometric, not evidential"** —
i.e. not a stock deficiency that more work could repair — and recorded that
`D-disj` and `D-contact` **each contain an acyclic source `(O1-Z)`**.

Therefore every boundary edge at that vertex is outside `A_RP^+(L)` for every
`L`; both certified prototypes are outside `D_form^cert(L)` for every `L`; and
since unchanged V002 demands FC11 on them, **`A_extent` is false for every `L`**.
`ANTE_full` is not contingently unsatisfied — it is definitionally unsatisfiable
in any tower containing these certified prototypes.

V002 books this as `A_extent = FALSE_ON_CERTIFIED_D_CC_PROTOTYPES` and
`ANTE_full = FALSE_ON_CURRENT_TOWER`.

**Being fair to the build, three ways:** the statement V002 makes is *true* — it
is merely weaker than its own premises support. Its **remedy is correct**: the
support-qualified guard *"would … remove the full-extent demand"*, which is
precisely the right response to a permanent obstruction rather than a temporary
one. And `FALSE_ON_CURRENT_TOWER` admits a charitable reading ("false, as
established on the current tower") under which it is imprecise rather than
misleading. Under either reading the repair is the same one line.

**Why it is nonetheless the most consequential item in this review.** Read as
tower-contingency, an adjudicator may reasonably **defer** — wait for a richer
tower or more stock. That inference is unavailable: no tower and no stock can
satisfy `A_extent` while the certified prototypes carry acyclic sources. The
correct posture for A2 is therefore not deferral but a **choice between two named
options**:

1. adopt the support-qualified guard (`A_scope`), removing the full-extent
   demand — after which `ANTE_support` still requires `A_vi`, `A_pkg`, `A_J2`
   (whose `P(L)` inhabitance V002 discloses as unestablished) and `A_J7`; or
2. accept permanent incompleteness on the unchanged guard for this datum.

I note that this residual runs in the **opposite direction from my original D6
kill**: that kill was overstatement; this is understatement. Both mislead a
reader of the board, which is why I book it rather than waive it. **It is a
status-verb defect, not a mathematical one, and not a kill.**

### E4.2 Battery

§9.1's three permanent counterexamples are real and permanent; §9.2's
circularity/F_PLDEC regression genuinely excludes the result depending on what
it establishes; §9.3's anti-tuning ledger records no selection, consistent with
`MEMBER_BOUND = false` throughout. §8.3's R9 quantification is carried at the
same extent I confirmed at Q-518 and Q-521.

---

## E5. SURFACE ANCHOR + VERB AUDIT ON MY OWN BOARD

**Surface anchor:** present. §8.1 names objects and required displays; the
residue table §4.3 is the anchor's real work, typing each unformable position
with its *reason* — `NOT FORMED` with a displayed geometric cause for `(O1-Z)`,
`UNDETERMINED` for `(O1-U)`, post-scope for `(O2-old)`/`(O2-new)`, and *"no
verdict"* for unclassified arrows. The distinction between *absence of proof* and
*proof of absence* is drawn correctly on both O1 branches — the failure mode I
flagged in the FC6 determination.

| My verb | Check |
|---|---|
| "PASS ×4" on E1 | PASS = the repair closes the kill **at the standard I set**. It is not a claim that membership holds — V002 asserts no membership and neither do I. |
| "the collapse of D2's `A_RP` half is closed" | Closed as a *typing* matter: the set is now a predicate in an antecedent, with inhabitance explicitly unestablished. No inhabitance is claimed by me or by V002. |
| N1 "unsatisfiable for every `L`" | **PROVABLE**, re-derived by me from `(D2.2)`'s leftmost-factor structure plus the sealed `(O1-Z)` row. It is a claim about `A_extent`, **not** a claim that any family is empty — `(OB-2)`-style non-emptiness remains unproved either way. |
| N1's grading as NOTE | Deliberate. V002's statement is true and its remedy correct; only the strength is understated. I declined to inflate this to a kill. |
| N2 | Graded an exhibition debt and explicitly **not** a recurrence of my §1.3 kill, which I record as cured. |
| "7/7 at sealed type" | Row-by-row against JII §7.4 and the sealed ledger, both directions tested. |
| "CONFIRMED" | Confirms V002's **determination and its display**. Not a ruling, not an adoption, not a finding that `F_actual^+` is a member. |
| Agent reliance | 11 agents ran; 10 of 11 load-bearing claims were refuted on verification. The one survivor (N1) I re-derived personally before booking. No unverified agent claim is on this board. |

Recorded against myself: I mounted an attack that `A_J2` is a second
undisclosed-false package, and **withdrew it** — V002 discloses the
non-establishment of `P(L)`/`A_RP^+(L)` in two separate sections. I also
considered charging FC2/FC3 as under-carried against my own earlier grading and
**declined**, because V002 states the same content with the residue named, which
is more precise rather than weaker.

## Custody carry-forward

`STAGE8_TASK5_AUDIT_V002_REVIEW_DARIO_V001.md` (PASTE 577) remains never
written, sealed, or mirrored — a tool failure at the time, disclosed at Q-517 and
Q-521. That relay's verdict is still not of record. This is the only gap in this
lane's custody chain across Task 5.

---

```text
MEMBERSHIP_V002 = CONFIRMED (+2 NOTES: N1 the failure of A_extent is booked at
  tower-contingent strength (FALSE_ON_CURRENT_TOWER) although V002's own
  displayed definitions make it unsatisfiable for EVERY L -- since P_H,X is the
  leftmost factor of m_T,X(L), an acyclic (O1-Z) vertex lies outside P(L)
  universally, and both certified D_CC prototypes carry such a vertex; the
  statement made is true and the remedy correct, but the strength understates
  the premises, which matters because it invites deferral where none is
  available. N2 Scope_component is declared but never displayed and no
  replacement per-coordinate table is supplied, leaving an exhibition debt
  discharged only by pointer to V001)
READY_FOR_READJUDICATION = yes
VERB_AUDIT_SELF = CLEAN
```

All four of my kills are closed at the standard I set. The seven carry rows sit
at their sealed types in both directions. The vacuity is disclosed in V002's own
word, the implication is proved *relevantly* rather than *ex falso*, and the
falsified extent is attributed to the contract rather than to the datum.

The single correction I press is N1, and it is a correction that **helps** the
re-adjudication: A2 is not waiting on a better tower. It is choosing between
adopting the support-qualified guard and accepting permanent incompleteness on
the unchanged one.
