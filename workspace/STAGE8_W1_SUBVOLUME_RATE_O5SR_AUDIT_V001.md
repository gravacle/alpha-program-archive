# STAGE 8 — AUDIT OF W-1: THE CERTIFIED SUB-VOLUME TRACE/HS RATE — COMMISSION O5SR — V001

## W1-AUDIT, 2026-08-15 — DEFAULT-REFUTE; TESTIMONY ZERO WEIGHT; RE-DERIVED AT BYTES

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
DETERMINATION ONLY.  No authored physics, no value, no number offered as a value of
any program quantity, no measured-constant comparison, no unit, no dimensional
quantity, no scale.  Every number below is an exponent in the counting parameter, a
rank count, a multiplicity, or a rational coefficient inside a symbolic inequality.
Exact symbolic only (CAS, fresh venv, sympy 1.14.0).  No git action.  No
register / tracker / road / plan / continuation / ledger / lens file opened.
"Q-..." register tokens EXPECTED-UNLOCATABLE — not chased.
```

**AUDIT STANCE.** DEFAULT-REFUTE. The target's own testimony carries **zero weight**:
every claim below was re-derived from the sealed bytes or refuted there. Where the
target quotes a sealed artifact, the quote was re-read at that artifact's own bytes.
Where the target runs symbolic work, the script was extracted from the target's own
file, hashed, and re-executed in a **fresh venv** built for this audit.

**OUTPUT-PATH PROBE.** `STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md` and its
`.seal.sha256` sidecar probed **ABSENT** before any write (`ls` exit 1 on both).

**SWEEP CUTOFF: 2026-08-15T18:40:00Z.** Every exhaustiveness sentence below is
sweep-relative to this cutoff and to its own displayed key set. Nothing absolute is
claimed.

---

## §0 — VERDICT

```text
OVERALL:  CONFIRMED-WITH-CORRECTIONS  — but the corrections are load-bearing, and
          one of the target's five landed results is REFUTED outright.

PER CLAIM:
  T-1  ceiling ATTAINED / sub-volume not implied by class data ....... CONFIRMED
  T-2a/b/c  layer cake + domain closure + Chebyshev necessity ........ CONFIRMED
  T-2d + "DERIVED CONSEQUENCE": carrier must supply q STRICTLY < 2 ... REFUTED
  T-3  admissibility gate "must be SYMMETRY-BREAKING seminorms" ...... REFUTED
         (the underlying finiteness obstruction ..................... CONFIRMED)
  T-4  premise names the carrier PARTLY, exactly 2 of 4 .............. CONFIRMED
  T-4 §6.3 "DERIVED ALIGNMENT" (inert half / load-bearing half) ...... DOWNGRADED
         — not DERIVED; rests on the refuted T-3 plus consumed testimony
  T-5  route ledger: (i) closed negatively .......................... CONFIRMED
       (ii) "CLOSED" .............................................. DOWNGRADED
       (iii) live and carrier-less ................................. CONFIRMED
  §7 note: W-3's fixed-n enclosures are not sub-volume assets ........ CONFIRMED

THE HEADLINE THE TARGET GOT RIGHT, AND THE AUDIT DOES NOT DISTURB:
  RATE_CERTIFIED = false.  CARRIER_NAMED_WHOLE = false.  MEMBER_LANDED = false.
  FP-1 STANDS.  No witness created, none retired.  No gate, fence, or V-clause moved.
  The audit's corrections make the target's negative verdict MORE secure, not less:
  every correction removes a positive claim, none removes a blocker.
```

**THE TWO KILLS, IN ONE LINE EACH.**

1. **T-2's sharp threshold is an artifact of a discarded cap.** The target derives
   that the carrier must supply a singular-value threshold exponent `q < 2`, because
   the Chebyshev exponent `q = 2` makes its sufficiency integral diverge. That
   divergence is produced by integrating a bound the target's **own T-1 proof** shows
   is vacuous near `s = 0`: `N_n(s) <= r_n = 4n^3` always. Reinstating the rank cap,
   `q = 2` **is** sufficient (up to a log), so the "boundary case sitting exactly on
   the failure line" does not exist. Re-derived exactly at §3.

2. **T-3's stated hypothesis (a) is never used in T-3's own displayed proof.** The
   chain `Nm(A_0) >= ||A_0||_2 = +infinity` uses only HS-domination and membership.
   It therefore excludes **symmetry-breaking** HS-dominating seminorms on that class
   exactly as hard as translation-invariant ones. The conclusion "every admissible
   topology must be generated by symmetry-breaking seminorms" does not follow from
   what is displayed. Re-derived at §4.

---

## §1 — STEP 0: TARGET PROBE AND SEAL

```text
TARGET      /Users/bgm/MB Work/alpha-program-archive/workspace/
            STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md            PRESENT (62805 bytes)
SIDECAR     STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md.seal.sha256 PRESENT (104 bytes)
VERIFY      shasum -a 256 -c, run FROM THE ARTIFACT'S OWN DIRECTORY:
            STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md: OK
DIGEST      806d5ec4628f9de0a1a798c629b26c59c2efbbe70a2c4765e87735cdc18a8419
```

Seal verified before any reliance. Audit proceeds.

---

## §2 — GROUND RE-CONSUMED INDEPENDENTLY, SEALS RE-VERIFIED

All thirteen files the target claims to consume were re-verified **this session**, by
`shasum -a 256 -c` **run from the artifact's own directory**, and their leading digest
bytes were compared against the digests the target displays in its own §1.

```text
13/13 OK, ZERO MISMATCHES, NONE UNVERIFIABLE.
8/8 displayed digest prefixes RE-COMPUTED AND MATCHED:
  afc3a79c COMPLETION_MAP_T17SR     9fdc3d1c MO3_P_EXPONENT_S9AD
  eacd810c W2_PREQUOTIENT_O2SR      241262ff W3_GCM_HS_TYPE_O3SR
  d422fc45 JD_MEMBER_CANDIDATE_T2SR 80db260f WALL_BOUNDARY_CLOSURE_S9AD
  6a839b51 CARRIER_TRIAGE_S9AD      3de0502c RATE_GAP_LINKAGE_S9AD
```

**GRADE CHECK ON THE RATIFIED PREMISE — the commissioned kill test.** The
commission's instruction: *an authored premise consumed as DERIVED is a kill.*

```text
STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md (d422fc45).
  TARGET'S DECLARED GRADE (§6.1):  AUTHORED PHYSICS, ENTERED AS PREMISE, RATIFIED;
    consumed at AUTHORED-PREMISE GRADE ONLY; "never as a derivation, never as sealed
    ground, never as authority for any exponent."
  AUDIT TEST — does any [DERIVED] result actually rest on it?
    T-1  rests on: rank 4n^3, op ceiling 2, singular-value arithmetic.   PREMISE-FREE
    T-2  rests on: layer-cake identity, the delivered op enclosure.      PREMISE-FREE
    T-3  rests on: W-3's sealed theorem + its re-run arithmetic core.    PREMISE-FREE
    T-5  rests on: MO-3's bytes + B-L2*'s clause as carried by W-3.      PREMISE-FREE
    T-4  rests on the premise — and T-4 is a determination ABOUT it, not FROM it:
         it counts which maps the premise names.  That is lawful consumption.
  RESULT: NO KILL.  The premise is nowhere consumed as DERIVED.  Its own banner
  (SK-7 — THE MEMBER IS NOT LANDED) is carried through unedited at §6.1 and §12.
```

**One exception is recorded, and it is not a grade violation but a scope one:**
§6.3's *"DERIVED ALIGNMENT"* is labelled DERIVED and rests on T-3, which this audit
refutes at §4. That is handled at §5, not here.

**W-2 IS LISTED AS CONSUMED AND IS NEVER CITED.** `STAGE8_W2_PREQUOTIENT_RULE_O2SR`
and its audit appear in the target's §1 ground table with a parenthetical
("W2-PARTIAL; three halts") and then appear **nowhere in §2–§8**. The audit
re-consumed W-2 independently to test whether anything in it bites on W-1 and was
passed over silently. Result at §6.4.

---

## §3 — KILL 1: T-2's `q < 2` THRESHOLD IS AN ARTIFACT OF A DISCARDED CAP — **REFUTED**

### §3.1 What the target claims

The target's T-2 is presented as *"the substantive positive yield of this build"*
(§4) and as *"the first time this commission's chain has a number to hit"* (§4). Its
DERIVED CONSEQUENCE, verbatim from the target:

```text
"The Chebyshev exponent produced by the rate itself is q = 2, and q = 2 is
PRECISELY the divergent case.  So the necessary condition T-2c is *never*
sufficient: the gap is not an artifact of a crude estimate, it is a boundary
case sitting exactly on the failure line.  THE TOPOLOGY CARRIER MUST DELIVER A
THRESHOLD-DECAY EXPONENT q STRICTLY BELOW 2, uniformly in n."
```

This is carried into §8 (T-2), §12 (DERIVED, NEW AT THE RECORD), and into the
DECIDING OBJECT clause (b), where `q < 2` is made a standing obligation on the
member.

### §3.2 The sealed cap the target discards

The target's own T-1 proof opens: *"`A` has exactly `r` singular values
`s_1 >= ... >= s_r >= 0`"*. Independently, at the boundary-closure's own bytes
(`80db260f` :302), the cap is sealed ground:

```text
"conversion from operator grade requires the rank factor: rank <= 4n^3 on
 ran C_n (of record)"
```

Therefore, for every `n` and every `s > 0`, **`N_n(s) <= r_n = 4n^3`** — trivially,
since `N_n` counts a subset of the `r_n` singular values. This is not an extra
premise: it is the same sealed fact T-1 is built from, and it is available at every
`s`.

T-2d integrates the power-law bound `N_n(s) <= c s^{-q} n^{3-delta}` over all of
`(0,2)` **without the cap**. Near `s = 0` that bound is vacuous — it exceeds `4n^3`.
The divergence at `q = 2` is produced entirely on the interval where the integrated
bound says nothing.

```text
CROSSOVER, exact [AUDIT-CAS Y2]:  c n^{3-delta} s^-2  =  4 n^3   at
        s*  =  sqrt(c)/2 * n^{-delta/2}
Below s* the power-law bound EXCEEDS the rank cap and is therefore VACUOUS.
B4c integrates 2 s^-1 across s = 0.  The +infinity is harvested from (0, s*).
```

### §3.3 The correct sufficiency statement, re-derived exactly

Split the layer-cake integral at `s_0 = n^{-delta/2}`, using the cap below and the
`q = 2` bound above:

```text
  int_0^{s_0} 2 s N_n(s) ds  <=  int_0^{s_0} 2 s (4 n^3) ds  =  4 n^{3-delta}
                                                             [AUDIT-CAS X1, exact]
  int_{s_0}^{2} 2 s N_n(s) ds <= int_{s_0}^2 2 s (c n^{3-delta} s^-2) ds
                              =  2 c n^{3-delta} ( ln 2 + (delta/2) ln n )
                                                             [AUDIT-CAS X2, exact]
  ----------------------------------------------------------------------------
  ||A_n||_2^2  <=  n^{3-delta} ( 4 + 2c ln 2 + c delta ln n )  =  O(n^{3-delta} log n)
                                                             [AUDIT-CAS X3, exact]
```

and `n^{3-delta} log n` is **still sub-volume**: for every `eps` with
`0 < eps < delta`,

```text
        n^{3-delta} log n / n^{3-(delta-eps)}  ->  0        [AUDIT-CAS X4, exact]
```

so the exponent `3 - (delta - eps)` holds, with `delta - eps > 0`.

> **AUDIT FINDING A-1 [DERIVED, REFUTES].** The `q = 2` Chebyshev bound, taken
> together with the sealed rank cap `N_n <= 4n^3`, **is sufficient** for a certified
> sub-volume rate (at exponent `3 - delta'` for every `delta' < delta`). Hence:
>   - "the necessary condition T-2c is *never* sufficient" is **FALSE**;
>   - "a boundary case sitting exactly on the failure line" describes **no boundary**:
>     T-2c and T-2d are equivalent up to arbitrarily small exponent loss;
>   - "the topology carrier MUST deliver `q` STRICTLY BELOW 2" is **not derived**.
>     The correct obligation is `q = 2` with a sub-volume prefactor `n^{3-delta}` —
>     which is exactly what a sub-volume rate already says. T-2's headline is
>     therefore **circular where it is not false**: after the correction, T-2c+T-2d
>     restate the rate rather than quantify a new obligation on the carrier.

### §3.4 What survives of T-2

```text
SURVIVES, CONFIRMED, exact:
  T-2a  layer-cake identities ||A||_2^2 = int 2 s N(s) ds, ||A||_1 = int N(s) ds
        — re-run, exact on the target's four rational lists.        CONFIRMED
  T-2b  ||A||_op <= 2 => N(s) = 0 for s >= 2; the domain closes at (0,2).
        — re-run.                                                   CONFIRMED
  T-2c  Chebyshev necessity N_n(s) <= ||A_n||_2^2 / s^2. — re-run.  CONFIRMED
  The RE-TYPING ITSELF — "a certified sub-volume trace/HS rate is identically a
  sub-volume singular-value counting statement" — SURVIVES, and is correct: with
  the rank cap reinstated the equivalence is TIGHTER than the target claims, not
  looser.  What dies is the claimed SHARP THRESHOLD and the claimed GAP.
FALLS, REFUTED:
  T-2d's DERIVED CONSEQUENCE (q strictly < 2); §8's T-2 summary sentence; §12's
  T-2 line; DECIDING OBJECT clause (b) as written.
```

**Net effect on the target's verdict: NONE adverse.** The correction removes a
positive claim about what the carrier owes. `RATE_CERTIFIED = false` stands
untouched — indeed §3.3 exhibits no rate, only an implication whose antecedent
(a `q = 2` counting bound with sub-volume prefactor, uniform in `n`) **no artifact
in the swept stock supplies**. The carrier is still owed; it is owed something
different from what the target says.

---

## §4 — KILL 2: T-3's HYPOTHESIS (a) IS INERT IN T-3's OWN PROOF — **REFUTED**

### §4.1 The displayed proof, at the target's bytes

```text
T-3.  Let Nm be a seminorm ... such that
  (a) Nm is translation-invariant: Nm(U_a A U_a^*) = Nm(A) for all a in R^3; and
  (b) Nm dominates HS on a class K: ||A||_2 <= Nm(A) for all A in K.
If K contains a nonzero translation-invariant member A_0, then
        Nm(A_0)  >=  ||A_0||_2  =  +infinity          (W-3's theorem)
so Nm is not finite on K.
```

**Hypothesis (a) is never used.** The single displayed inference is
`Nm(A_0) >= ||A_0||_2` — that is hypothesis (b) applied at `A_0 in K` — followed by
`||A_0||_2 = +infinity`, which is W-3's theorem about `A_0`, not about `Nm`. Delete
(a) and the proof is unchanged and still valid [AUDIT-CAS Z1/Z2/Z3].

### §4.2 What the proof actually proves, and what it therefore cannot conclude

```text
WHAT IS PROVED:  NO seminorm that dominates HS on K is finite on K, once K
                 contains a nonzero translation-invariant member.  This is
                 INDIFFERENT to whether the seminorm breaks translation
                 invariance.  A symmetry-BREAKING HS-dominating seminorm on the
                 same K is excluded by the identical two-line argument.
WHAT IS CONCLUDED BY THE TARGET:  "Hence no topology generated by translation-
                 invariant, HS-dominating seminorms is admissible as W-1's
                 carrier" and "every admissible topology for W-1 must be
                 generated by SYMMETRY-BREAKING seminorms."
```

> **AUDIT FINDING A-2 [DERIVED, REFUTES].** The second clause is a **non-sequitur**
> from the displayed proof. The proof's escape is not "break the symmetry of the
> seminorm" — the seminorm's symmetry plays no role. Its only escape is **to shrink
> the class `K` so that it contains no nonzero translation-invariant member.** That
> is precisely the escape the target's own T-1 COROLLARY already names
> (*"requires a strictly SMALLER class"*), and T-3 as stated mis-attributes it to a
> property of the seminorms.

### §4.3 The type shift against W-3's actual bytes

The target sources the symmetry-breaking conclusion jointly to W-3's ADDS (3).
Re-read at W-3's own bytes (`241262ff` :385-389, verified accurate to the target's
rendering):

```text
"ADDS (3): IT NAMES THE ESCAPE'S TYPE. The only way past is an INPUT that is
NOT translation-invariant — i.e. one that consumes the sharp vertex's own
SPATIAL LOCALIZATION rather than class membership."
```

W-3 says the **input/asset** must break translation invariance. The target's T-3
converts this into a statement about the **seminorms generating the carrier
topology**. Those are different objects, and no derivation of the conversion is
displayed. W-3's sentence is testimony consumed at sealed grade; under DEFAULT-REFUTE
it supports "the escape's input type is symmetry-breaking" and supports nothing about
seminorms.

### §4.4 The class substitution, and the scope slip in §8

T-3's hypothesis requires `K` to contain a nonzero translation-invariant member.
W-3's `K` is the certified class `H(sqrt3)`, which contains `h_0`. **W-1's dressed
composite class is nowhere shown to contain such a member.** The target's §5.3 says
this in the open and honestly:

```text
"it does NOT establish that the dressed composites themselves have a nonzero
translation-invariant limit.  That premise is NOT sealed, is not authored here,
and is not assumed."
```

That disclosure is **CONFIRMED and creditable**. But §8's T-3 summary then states the
gate flatly — *"no topology ... can carry W-1"* — with the class condition demoted to
a trailing "since" clause. Since the condition is unverified for W-1's class, §8's
formulation is a **scope overreach** relative to §5.3.

### §4.5 What survives of T-3

```text
SURVIVES, CONFIRMED:
  The finiteness obstruction itself: on a class containing a nonzero
  translation-invariant member, no HS-dominating seminorm is finite.  Exact,
  and correctly derived from W-3's sealed theorem.
  The arithmetic core (Block C) re-runs exactly: the 1-D Gaussian witness gives
  HS mass over a box of side 2L equal to 2L sqrt(pi/2) — volume-linear, divergent.
  §5.3's honest boundary — no density floor asserted for K_H,n or H_A,n.  CONFIRMED.
FALLS, REFUTED:
  "every admissible topology for W-1 must be generated by SYMMETRY-BREAKING
  seminorms" (§5.2, §8 T-3, §12 T-3) — non-sequitur; hypothesis (a) inert.
  The §8 flat statement of the gate against W-1's class — scope overreach.
```

---

## §5 — T-1 AND T-4: RE-DERIVED AND **CONFIRMED**, WITH ONE DOWNGRADE

### §5.1 T-1 — CONFIRMED, exact

Re-derived independently. On `C(r,kappa) = {A : dim = r, ||A||_op <= kappa}`:
`A` has exactly `r` singular values, each `<= kappa`, giving
`||A||_2^2 <= kappa^2 r` and `|tr A| <= kappa r`; both are attained, at `A = kappa U`
(`U` unitary) and `A = kappa I` respectively. The specialization is arithmetic:

```text
kappa = 2, r_n = 4n^3   =>   sup ||A||_2^2 = 16 n^3 ,  sup |tr A| = 8 n^3
```

and these are **exactly** the boundary-closure's sealed budgets, re-read at
`80db260f` :305:

```text
"|tr K_H,n| <= 4n^3 · 2 = 8n^3 ,      ||K_H,n||_2^2 <= 4n^3 · 4 = 16n^3"
```

**T-1 CONFIRMED.** Its corollary — no sub-volume exponent follows from
`(rank = 4n^3, ||·||_op <= 2)` at any strength — is CONFIRMED: the class contains
elements at exactly volume order, so the ratio to any `n^{3-delta}` target diverges.
The target's one-sidedness disclaimer (*"it says the ceiling is attained on the
enclosing class, not that the actual dressed composites attain it"*) is accurate and
is the correct quantifier. **No rate is certified at any quantifier anywhere in the
target** — the commissioned hunt for a rate certified at the wrong quantifier
returns NEGATIVE.

### §5.2 T-4's count — CONFIRMED at bytes, independently reproduced

```text
CN-1's four maps, re-read at WBC 80db260f :494-501:
  "member-named topologies with continuity proofs for Loc_N^phys, iotatilde_N^H,
   iota_N^H, Loc_N^C — no member exists in the scored corpus to name them."
  DEMANDED = 4.  CONFIRMED at bytes.  Not three, not five.

The premise's P-3, re-read at d422fc45 :440-444:
  "Phitilde_N : Sigma_N^(2),Ward -> (C_N^k)^* is continuous and R-linear in the
   named topologies, and so are the induced Phi_N, iotatilde_N^H, iota_N^H."
  NAMES: iotatilde_N^H, iota_N^H.  SUPPLIED = 2.  CONFIRMED at bytes.

AUDIT'S OWN SWEEP (SW-A, cutoff as declared):
  grep -c "Loc_N"  over d422fc45  =  0        (SW-2 reproduced exactly)
  grep -n  "Loc"   over d422fc45  =  ONE hit, :620, inside the DOES NOT block:
     "Joint C's physical Loc/contact member is presupposed at its slot,
      not supplied"
OWED = 2, and they are exactly Loc_N^phys and Loc_N^C.   CONFIRMED.
FRACTION NAMED = 1/2 exactly.                            CONFIRMED.
```

**T-4's determination — PARTLY, exactly 2 of 4, at authored-premise grade — is
CONFIRMED.** The commissioned hunt for *"a topology asserted rather than named with
its continuity obligations enumerated"* returns NEGATIVE **for the target**: the
target enumerates all four obligations explicitly, authors no topology, and states
that it authors none.

### §5.3 AUDIT ADDITION: the supplied half is weaker than "PROVED" suggests

Re-reading P-3's proof at bytes, two of its factors are continuous **by declaration,
not by proof**:

```text
"gr_N (T2-1, continuous BY DECLARATION); sect_N (T2-3, continuous BY DECLARATION)"
```

and W-2 — the sibling the target lists and never cites — halts independently on the
same object (`eacd810c` §4.3 H-1, THE DOMAIN HALT, "the binding one"):

```text
"THE COMPOSITE'S DOMAIN IS EMPTY ON ACTUAL STOCK UNTIL JB-2 LANDS" (premise §9),
"its check confirms it independently: 'JB-2 undischarged of record ...; the
 actual-stock domain is empty until it lands' (F-4)."
```

> **AUDIT FINDING A-3 [DERIVED, TIGHTENS].** The two maps the target scores as
> "continuity PROVED at authored-premise grade" are proved (i) with two factors
> declared rather than proved, and (ii) on a composite whose **domain is empty on
> actual stock**. The target's §6.4 line "CARRIED-BUT-NOT-DISCHARGED (2)" is
> therefore generous to the premise. This correction runs in the target's own
> direction — the carrier is owed *more*, not less — and does not disturb the count
> of 2-of-4 **named**, which is what T-4 actually claims.

### §5.4 §6.3's "DERIVED ALIGNMENT" — **DOWNGRADED**

§6.3 claims, under a `[DERIVED]` label, that the supplied pair is of a type that
"CANNOT break the obstruction" and the owed pair is "exactly the symmetry-breaking
type," concluding *"the shortfall is therefore not quantitative (2 of 4) but
structural."*

```text
ITS TWO SUPPORTS:
  (1) T-3 — REFUTED at §4 (hypothesis (a) inert; the symmetry-breaking conclusion
      is a non-sequitur).  Support WITHDRAWN.
  (2) W-3's ADDS (3) — sealed testimony, quoted accurately, but it types the
      required INPUT, not the required SEMINORMS, and it is testimony.  Under
      DEFAULT-REFUTE it cannot carry a [DERIVED] label on its own.
```

> **AUDIT FINDING A-4 [DOWNGRADE].** §6.3's alignment is **not DERIVED**. It is a
> *plausible and well-motivated reading* of W-3's ADDS (3) applied to CN-1's map
> names, and it should carry OBSERVATION grade, not DERIVED grade. The bare
> observation that survives at derivation grade is weaker and still worth having:
> **the two maps the premise leaves owed are, by their own names of record, the two
> LOCALIZATION maps** — and localization is the object W-3 names as the escape's
> input type. That is a naming coincidence of record, not a derived structural
> theorem. §12's T-4 line and §8's T-4 entry inherit the downgrade.

---

## §6 — T-5: THE ROUTE LEDGER — ONE CONFIRMED, ONE DOWNGRADED, ONE CONFIRMED

### §6.1 Route (i) — CONFIRMED

MO-3's verdict re-read at its own bytes (`9fdc3d1c` :391-397); the target's quotation
is **accurate and contiguous**:

```text
"THE SUB-VOLUME TRACE-NORM RATE FOR Delta_n(a) (= r-3's MO-3 = the wall's F1
escape): CLOSED NEGATIVELY AT THIS ROUTE. The route 'certify p > 3/2, get CXC HS,
get the sub-volume rate' is dead at its first link."
```

Arithmetic re-run: `3/2 - (-3) = 9/2`; `2p - 4 = -10` at `p = -3`; divergence rate
`eps^-9`. All exact. **Route (i) CLOSED NEGATIVELY — CONFIRMED.**

### §6.2 Route (ii) — the grading is **DOWNGRADED** from CLOSED

The target grades Route (ii) *"CLOSED, of record, by B-L2*'s sealed saturation
clause."* The clause was re-read at W-3's own bytes (`241262ff` :440-453). The
target's quotation is **accurate**. But read the clause's own words:

```text
"... the leading summed response then contains the P-restricted longitudinal
intraband term ... degree 0, the C2 Lambda^3 carrier-volume HS shape at the volume
diagonal, WITH NO SEALED IDENTITY TO CANCEL IT."
and, in the same clause: "(plus the UNCERTIFIED C-L1 error)"
and its own header: "NET = FAILS-AT(the stratum-resummation step, at the sealed
saturation clause)"   [this header is OMITTED from the target's rendering]
```

*"No sealed identity to cancel it"* is the **absence of a certified cancellation**,
not a proof that no cancellation exists. *"FAILS-AT"* is the record's own grade for
a route that stops, not for one that is refuted. And MO-3 itself, at `9fdc3d1c`
:482-484, states the opposite of closure for this very route:

```text
"no RUNS-vs-CLOSES adjudication of the program horn (the weight-summed and
assembly re-quantifications of record are UNTOUCHED); ... B-L2* stands where it
stood."
```

> **AUDIT FINDING A-5 [DOWNGRADE].** Route (ii) is **NOT CLOSED**. Its correct grade
> is **FAILS-AT / UNCERTIFIED**: at present grade the route returns carrier-volume
> order and possesses no sealed cancelling identity, and one of its inputs (C-L1) is
> uncertified. The target's own consumed ground (MO-3) calls the route "untouched."
> Consequences that must be corrected:
>   - §0's *"two are closed ... leaving ONE live"* → **one closed (negatively), one
>     FAILS-AT-uncertified, one live-and-untouched**;
>   - §8's T-5 line and §12's T-5 line inherit the same correction;
>   - the route ledger is reduced from three to **two not-closed routes**, not one.
> Direction of effect: this **weakens a negative**, i.e. it makes the target's route
> ledger *less* tight than claimed. It does not create a rate, and it does not move
> `RATE_CERTIFIED`.

Note the target flags the one-link quotation chain itself at CH-7 and declares the
cost. The declaration is creditable; the **grade** assigned at the end of that chain
is what this audit corrects.

### §6.3 Route (iii) — CONFIRMED

LIVE and CARRIER-LESS. Re-checked: MO-3 names it and explicitly does not touch it;
B-L2*'s clause is about the stratum *sum*, not pairwise assembly; the ratified
premise does not address it; the audit's own sweep found no artifact supplying a
per-pair cancellation identity at assembly. **CONFIRMED.**

One tightening: the target writes *"MO-3 thereby names the complete route
inventory."* MO-3's own sentence names two routes "both of record" — it does not
claim the of-record list is the list of all conceivable routes. The target's other
formulations are correctly qualified ("named of record", "named anywhere of record
at the stated cutoff"); this one sentence is not. **Tighten to: MO-3 names the
complete OF-RECORD route inventory at the stated cutoff.**

### §6.4 The §7 closing note on W-3's fixed-n enclosures — CONFIRMED

W-3's registered enclosures re-read at `241262ff` :418-420 — the target's rendering
is exact:

```text
||C_n T_g C_n||_2 <= 12 n^{3/2} sqrt(2n-1)/ell   (order n^2)
||C_n T_g C_n||_1 <= 24 n^3 sqrt(2n-1)/ell       (order n^{7/2})
```

Squaring the first gives `144 n^3 (2n-1)/ell^2`, leading order `n^4`, leading
coefficient 288 — re-run exactly. `n^4 > n^3` and `n^{7/2} > n^3`: **neither is a
sub-volume asset.** The target's disqualifiers (not `G_cm`, not n-uniform, generator
not the layer) are quoted accurately from W-3. **CONFIRMED**, and the target was
right to close this door explicitly.

### §6.5 W-2, re-consumed independently

W-2 was scanned in full for anything bearing on W-1's rate, topology, or counting.
**Nothing in W-2 bites on W-1's rate**; its three halts (domain, named-cell, gauge)
are about the prequotient rule's displayed evaluation, i.e. CN-2, not CN-1. Its one
contact with W-1's subject matter is P-3, which feeds §5.3's finding A-3 above and
runs in the target's own direction. **No suppressed obstruction found.** The target's
listing of W-2 as consumed ground it never cites is a **presentation defect, not a
substantive one**.

---

## §7 — THE TARGET'S CAS BATTERY: RE-RUN, AND WHAT THREE OF ITS CHECKS ACTUALLY CHECK

### §7.1 Reproduction — exact

```text
METHOD  The python block was extracted from the target's own §9.2 (lines 924-1164
        of the sealed file), hashed, and executed in a venv created for this audit.
SCRIPT SHA256 (extracted)  8e0e6d474e962fc86a0af0559b955fa15e378672b0921e1b89559fc60b650949
SCRIPT SHA256 (claimed)    8e0e6d474e962fc86a0af0559b955fa15e378672b0921e1b89559fc60b650949
                           -> MATCH.  The file-append claim is VERIFIED: the bytes
                              reproduced in §9.2 are the bytes that were run.
ENVIRONMENT  fresh venv, sympy 1.14.0 (matches the declared version).
RESULT       34/34 PASS, 0 FAIL — and the stdout is BYTE-IDENTICAL to the target's
             §9.1 verbatim output block (diffed programmatically: IDENTICAL).
```

**The battery reproduces perfectly. No hidden numerics, no seeded randomness, no
environment dependence, no unreproducible step.**

### §7.2 But three PASS lines do not check their own labels

Re-running is not the same as auditing what was run. Three checks are **tautologies
whose labels overstate them**:

```text
A3b  LABEL: "sum_{k=1}^{r} kappa^2 = r kappa^2 identically (no slack to recover)"
     WHAT IT EVALUATES:  sum(kap**2 for _ in range(6)) - 6*kap**2 == 0
     i.e. literally 6k^2 - 6k^2 = 0.  It is independent of r, independent of the
     singular values, and says nothing about slack.  The symbol list `sig` created
     one line above (sigma1..sigma6) is NEVER USED.
     STATUS: VACUOUS.  The claim it labels is nonetheless TRUE and is established
     by the prose proof and by A3a's attainment witnesses.  Not load-bearing.

C3   LABEL: "gate chain: Nm(A0) >= ||A0||_2 = +oo (so Nm is not finite on the class)"
     WHAT IT EVALUATES:  sp.limit(box, L, sp.oo) == sp.oo
     — which is LITERALLY C1c's condition, on the same `box`.  The function symbol
     `Nm` created two lines above is NEVER USED.  The gate chain is not checked.
     STATUS: MISLABELLED RE-RUN of C1c.  The gate's prose proof IS displayed at
     §5.2 and is valid as far as it goes — but see §4: it proves less than claimed.

D4   LABEL: "two independent of-record returns both land at exponent exactly 3"
     WHAT IT EVALUATES:  degree(16n^3)=3 AND degree(4n^3)=3 — both from Block A.
     The SECOND "independent return" (Route (ii)'s carrier-volume HS shape) is a
     QUOTATION from a sealed clause; nothing about it is symbolically checked.
     STATUS: ONE return checked twice, presented as two.
```

> **AUDIT FINDING A-6 [CORRECTION].** The target's *"34/34 PASS"* is arithmetically
> honest and fully reproducible, but **31 of the 34 checks bear their labels; 3 do
> not.** A3b and C3 create symbols they never use — the signature of a check written
> around a conclusion rather than toward one. None of the three is load-bearing for
> a claim this audit otherwise confirms, so the correction is to the **evidentiary
> weight of the battery**, not to any surviving result.

### §7.3 The audit's own battery

Run in the same fresh venv; scripts reproduced by file-append at §11 with their
hashes.

```text
AUDIT-CAS 1 (audit_cas.py)   14/14 PASS, 0 FAIL
  X1-X5  the rank cap reinstated: q = 2 IS sufficient, up to a log  -> §3
  Y1-Y3  the crossover s* below which T-2d's bound is vacuous       -> §3.2
  Z1-Z3  T-3's hypothesis (a) inert under both truth values         -> §4.1
  W1-W3  the three tautological checks, displayed                   -> §7.2
AUDIT-CAS 2 (audit_cas2.py)  6/6 PASS, 0 FAIL
  V1-V4  the n-uniformity exposure of the constant c                -> §8.2
  U1-U2  ell never takes a value and cancels from every conclusion  -> §8.3
```

---

## §8 — THE COMMISSIONED HUNT: RESULTS, ONE BY ONE

### §8.1 "A rate certified at the wrong quantifier" — **NEGATIVE**

No rate is certified anywhere in the target. `RATE_CERTIFIED = false` is declared at
the head, in §8, and in §12, and no section contradicts it. Quantifiers are stated
where they matter: §2.1 fixes "per-composite, all n" and re-reads it from the
completion map, whose own bytes (`afc3a79c` :619) carry *"per-composite quantifier
only."* T-1's class-vs-composite distinction is stated explicitly and correctly at
"What T-1 is NOT". **NEGATIVE — no finding.**

### §8.2 "Non-uniformity in the counting parameter concealed by a constant" — **FLAG**

```text
T-2d introduces:   N_n(s) <= c s^{-q} n^{3-delta}   on (0,2)
At the point of introduction, `c` is NOT declared n-free.  The uniformity clause in
the DERIVED CONSEQUENCE attaches to q — "a threshold-decay exponent q STRICTLY BELOW
2, uniformly in n" — not visibly to c.  Same exposure in T-2c's constant C.
IF c IS ALLOWED TO DEPEND ON n THE CONCLUSION IS VOID [AUDIT-CAS V2, exact]:
        c = n^delta   =>   n^{3-delta} * c * 2^{3-q}/(2-q)  =  2^{3-q}/(2-q) * n^3
        — VOLUME ORDER, not sub-volume.  The entire sufficiency claim collapses.
MITIGATION, at the target's own bytes: §8's DECIDING OBJECT and §12 both phrase
clause (b) as "bound ... by c s^{-q} n^{3-delta} with q < 2 and delta > 0,
UNIFORMLY IN N", where the uniformity plausibly governs the whole bound including c;
and the CAS treats c as an n-free positive symbol throughout.
```

> **AUDIT FINDING A-7 [FLAG].** The exposure is real and is exactly the commissioned
> pattern, but it is a **drafting gap rather than a concealment**: the target's
> operative statements read as covering `c`, and its CAS treats `c` as n-free. Since
> §3 refutes the surrounding claim outright, the flag is recorded and not escalated.
> **Any future carriage of a counting obligation must declare the constant n-free at
> the point of introduction.**

### §8.3 "A topology asserted rather than named with its continuity obligations
enumerated" — **NEGATIVE**

The target authors no topology, and says so four separate times (§2 fences, §5.3,
§8 R-2, §11). It enumerates the obligations exhaustively and per-map at §6.4 and in
the §12 CARRIER LEDGER: for each of the four CN-1 maps it records continuity status,
counting status, and symmetry-breaking status separately. This is the opposite of the
hunted defect. **NEGATIVE — and creditable.**

### §8.4 "Continuity proofs claimed rather than displayed" — **NEGATIVE for the
target; FLAG against the premise**

The target claims no continuity proof of its own. The two it scores as supplied are
the premise's, and the premise's P-3 proof **is displayed**, factor by factor, ending
QED (`d422fc45` :446-460). Two of its factors are continuous *by declaration* — see
finding A-3 — which weakens the premise, not the target's reading of it.
**NEGATIVE for the target.**

### §8.5 "The W-3 obstruction ignored where it bites" — **NEGATIVE, inverted**

The target does not ignore W-3; it **over-applies** it. W-3's obstruction is absent
at finite rank (W-3's own §4.4, re-read at `241262ff` :401: *"At finite rank the
obstruction is absent (a finite-rank operator is compact)"*), and every `A_n` here has
rank `<= 4n^3`. The target states this correctly and correctly locates the bite at
the **uniformity**. Where it errs is in the opposite direction — building T-3's
symmetry-breaking conclusion on a hypothesis its own proof does not use (§4).
**NEGATIVE on the hunted pattern; the actual defect is over-reach, recorded at §4.**

### §8.6 "Hidden numerics" — **NEGATIVE**

```text
FENCE-SCAN, keys: alpha, kappa_record, fine-structure, 137, coupling, hbar, c^2,
Newton, G_N — over the whole target file.  EVERY HIT INSPECTED AT BYTES:
  :24, :740, :904-905, :926, :1158-1159   fence DECLARATIONS (alpha_computed=false etc.)
  :46                                     the OUTPUT PATH string ("alpha-program-archive")
  :651, :671, :741, :1159                 NEGATIVE statements ("no alpha", "no coupling")
  NO OTHER HITS.  Zero occurrences of any alpha value, kappa_record computation,
  measured constant, or comparison to one.  FENCE-SCAN CLEAN.

TWO NON-INTEGER SYMBOLS INSPECTED SEPARATELY:
  sqrt(pi/2)  — the HS mass of the 1-D Gaussian WITNESS in Block C.  Declared as a
    witness at CH-4 and again at §11.  It is the value of an integral in an
    algebraic identity, not of any program quantity.  ADMISSIBLE.
  ell         — a LENGTH symbol, carried into Block D inside W-3's registered
    enclosure (W-3 :414-420: |p|^2 <= (6n-3)/ell^2).  It never takes a value, and it
    CANCELS from every conclusion the target draws [AUDIT-CAS U1/U2]: n^4 > n^3 holds
    for every ell > 0.  ADMISSIBLE — but the target's blanket "no dimensional
    quantity" is loose, since a length symbol is carried through Block D.  Recorded.
```

**NEGATIVE — no hidden numerics. One looseness in a fence sentence, recorded.**

### §8.7 The commission-identity claim — **VERIFIED**

The target claims a fresh commission with O1SR paths vacant. Re-probed
independently: `O1SR` returns **zero matches** across the primary corpus root's
workspace listing at the audit's cutoff. **CONFIRMED.**

```text
SW-A  O1SR COMMISSION-IDENTITY PROBE (audit's own, cutoff as declared).
      Key: token `O1SR` over the primary corpus root's workspace listing
      (3548 entries).  RESULT: ZERO matches, grep exit 1.  The target's
      SW-1 is reproduced exactly.  Exhaustiveness at this key set only.
SW-B  PREMISE LOCALIZATION-TOKEN PROBE (audit's own).  Keys: `Loc_N`, `Loc`
      inside d422fc45.  RESULT: `Loc_N` = 0 occurrences; `Loc` = 1 occurrence,
      at :620, inside the premise's own DOES NOT block.  The target's SW-2 is
      reproduced exactly at this key set.
SW-C  FENCE-SCAN over the target (keys at §8.6).  All hits inspected at bytes.
```

---

## §9 — VERDICT, PER CLAIM AND OVERALL

```text
==============================================================================
OVERALL:  CONFIRMED-WITH-CORRECTIONS
==============================================================================

CONFIRMED — re-derived independently, survives DEFAULT-REFUTE:
  T-1   The rank x op ceiling is ATTAINED on the class it is taken over.
        sup ||A||_2^2 = 16 n^3 and sup |tr A| = 8 n^3, exactly equal to the
        boundary-closure's sealed budgets (re-read at 80db260f :305).  No
        sub-volume exponent follows from the class data at any strength.
        Correctly one-sided; correctly quantified.                    CONFIRMED
  T-2a  Layer-cake identities.  Exact.                                CONFIRMED
  T-2b  The op enclosure closes the domain at (0,2).                  CONFIRMED
  T-2c  Chebyshev necessity.                                          CONFIRMED
  T-2   THE RE-TYPING ITSELF — a certified sub-volume trace/HS rate IS a
        sub-volume singular-value counting statement.  Correct, and TIGHTER
        than the target claims once the rank cap is reinstated.        CONFIRMED
  T-3   The finiteness obstruction: on a class containing a nonzero
        translation-invariant member, no HS-dominating seminorm is finite.
        Arithmetic core re-runs exactly.                               CONFIRMED
  T-3   §5.3's honest boundary — no density floor asserted for the actual
        composites, no exponent-3 lower bound on them.                 CONFIRMED
  T-4   The COUNT: CN-1 demands exactly 4 continuity proofs; the ratified
        premise names exactly 2 (iotatilde_N^H, iota_N^H); exactly 2 are owed
        (Loc_N^phys, Loc_N^C); fraction 1/2.  All four verified at bytes.
        Consumed at authored-premise grade — NO GRADE VIOLATION.       CONFIRMED
  T-5   Route (i) CLOSED NEGATIVELY (MO-3, p = -3).                    CONFIRMED
  T-5   Route (iii) LIVE and CARRIER-LESS.                             CONFIRMED
  §7    W-3's fixed-n enclosures are NOT sub-volume assets (n^4, n^{7/2}).
                                                                       CONFIRMED
  CAS   34/34 reproduce byte-identically from the target's own script bytes,
        whose hash matches the declared hash.                          CONFIRMED
  SEALS 13/13 re-verified from each artifact's own directory.          CONFIRMED
  FENCES  alpha/proof/kappa_record all held.  FENCE-SCAN clean.        CONFIRMED

REFUTED:
  A-1   T-2d's DERIVED CONSEQUENCE — "the carrier must deliver q STRICTLY
        BELOW 2", "the necessary condition is never sufficient", "a boundary
        case sitting exactly on the failure line".  The divergence is harvested
        from an interval where the integrated bound is vacuous against the
        SEALED rank cap N_n <= 4n^3 (80db260f :302).  With the cap reinstated,
        q = 2 IS sufficient up to a log, hence sufficient at exponent
        3 - delta' for every delta' < delta.  There is no sharp threshold and
        no gap.  [AUDIT-CAS X1-X5, Y1-Y3, exact]                        REFUTED
  A-2   T-3's conclusion "every admissible topology for W-1 must be generated
        by SYMMETRY-BREAKING seminorms".  Hypothesis (a) — translation-
        invariance of the seminorm — is NEVER USED in T-3's own displayed
        proof; the chain runs on HS-domination and membership alone, and so
        excludes symmetry-breaking HS-dominating seminorms on the same class
        equally.  The proof's only escape is a SMALLER CLASS, which is what
        the target's own T-1 corollary already says.  W-3's ADDS (3) types the
        required INPUT, not the required SEMINORMS.  [AUDIT-CAS Z1-Z3] REFUTED

DOWNGRADED:
  A-4   §6.3's "DERIVED ALIGNMENT" (supplied half inert / owed half load-
        bearing) is NOT DERIVED: support (1) is the refuted T-3, support (2) is
        sealed testimony about a different object.  Regrade to OBSERVATION.
        What survives at derivation grade: the two owed maps are, by their own
        names of record, the two LOCALIZATION maps.
  A-5   T-5 Route (ii) is NOT "CLOSED".  B-L2*'s clause says "no SEALED IDENTITY
        to cancel it" and carries an "UNCERTIFIED C-L1 error" under a header
        reading "NET = FAILS-AT"; MO-3 itself calls this route "UNTOUCHED" and
        says "B-L2* stands where it stood".  Correct grade: FAILS-AT /
        UNCERTIFIED.  The ledger reduces to TWO not-closed routes, not one.

CORRECTIONS AND FLAGS (non-fatal):
  A-3   The premise's "PROVED" half is weaker than scored: two factors of P-3
        are continuous BY DECLARATION, and the composite's domain is EMPTY ON
        ACTUAL STOCK (W-2's binding halt H-1, independently).  Runs in the
        target's own direction.
  A-6   3 of the target's 34 CAS checks (A3b, C3, D4) are tautologies whose
        labels overstate them; A3b and C3 create symbols they never use.  None
        load-bearing for a surviving result.
  A-7   The constant c in T-2c/T-2d is not declared n-free at introduction; if
        it were n-dependent the sufficiency claim would return VOLUME order
        [AUDIT-CAS V2].  Drafting gap, mitigated by the deciding-object phrasing.
  A-8   "MO-3 thereby names the complete route inventory" — tighten to
        "the complete OF-RECORD inventory at the stated cutoff".
  A-9   W-2 and its audit are listed as consumed ground and never cited.
        Presentation defect; re-consumed here, nothing suppressed (§6.5).
  A-10  The fence sentence "no dimensional quantity" is loose: the length
        symbol ell is carried through Block D.  It never takes a value and
        cancels from every conclusion.  Admissible, recorded.

==============================================================================
WHAT THE AUDIT DOES NOT DISTURB — and this is the substance of the target
==============================================================================
  RATE_CERTIFIED = false.  CARRIER_NAMED_WHOLE = false.  MEMBER_LANDED = false.
  FP-1 STANDS, unmoved as a failure point.
  The completion map's §3.4/§3.5 are UNEDITED: W-1 remains FP-1's sole displayed
    discharger and {W-1, W-2, W-3} the unique minimal completion set — re-read
    at afc3a79c :350-352 and :612-620.  Nothing here alters either.
  NO witness created, NO witness retired.  NO gate, fence, or V-clause moves.
  The four quantities (tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2) remain
    UNDECIDED; no divergence exhibited; every negative is ONE-SIDED — verified.
  NOT REFUTED, carried: nothing in this audit refutes anything about the
    physical objects.  Both kills remove POSITIVE claims the target made about
    what the carrier owes.  They do not supply a rate and do not weaken a
    blocker.  The target's PARTIAL verdict is correct; three of its five
    landed results stand, one is downgraded, one falls.
==============================================================================
```

**ON THE TARGET'S OWN VERDICT WORD.** The target chose PARTIAL over
UNDECIDABLE-TODAY at CH-1 and displayed the choice. After this audit, the surviving
positive content is T-1, the T-2 re-typing (without its threshold), T-4's count, and
the route ledger with Route (ii) regraded. That is still substantive derived content
beyond blockers, and the deciding object is still named. **PARTIAL remains defensible
and this audit does not ask the registrar to regrade it** — but the registrar should
know that its second and third pillars (T-2's threshold, T-3's gate) are the two that
did not survive.

---

## §10 — CHOICE LEDGER (audit W1-AUDIT; every unforced choice, classified)

```text
AC-1  OVERALL VERDICT "CONFIRMED-WITH-CORRECTIONS" RATHER THAN "REFUTED".  YOURS
      (judgment), displayed.  Grounds: the target's VERDICT — PARTIAL, rate not
      certified, carrier owed — is correct and independently re-derived; its seals,
      its CAS reproduction, its quotations, and its grade discipline are all clean;
      T-1 and T-4 stand entirely.  Grounds weighed for REFUTED: two of the five
      landed results fall, and one of them (T-2) is the target's own self-described
      central positive move.  MITIGATION: both kills are stated at §0 in the first
      screen, per claim, so a reader who stops early is not misled.
AC-2  THE RANK CAP TREATED AS AVAILABLE GROUND FOR THE T-2 REFUTATION.  FORCED.
      It is sealed at the closure's own bytes (80db260f :302, "rank <= 4n^3 on
      ran C_n (of record)") AND is the same fact the target's own T-1 proof runs
      on.  Using it against T-2d imports nothing new.
AC-3  THE q=2 SUFFICIENCY SHOWN WITH A LOG LOSS RATHER THAN CLEANLY.  FORCED by
      the mathematics: the split at s_0 = n^{-delta/2} is optimal up to constants
      and the log is genuine.  Displayed in the open; the conclusion is stated as
      "exponent 3 - delta' for every delta' < delta", not as "exponent 3 - delta".
AC-4  T-3 REFUTED ON HYPOTHESIS-INERTNESS RATHER THAN ON A COUNTEREXAMPLE.  YOURS.
      A counterexample seminorm was considered and REJECTED as unnecessary and as
      authorship: the defect is visible in the target's own displayed proof, and
      constructing an object would have crossed into authored content.  Cost: the
      refutation is of the DERIVATION, not of the conclusion's truth — the
      conclusion may still be true on other grounds.  Stated as such at §4.5.
AC-5  ROUTE (ii) DOWNGRADED RATHER THAN LEFT ALONE.  YOURS.  Grounds: the target's
      own sourced quote contains "no SEALED identity", "UNCERTIFIED C-L1", and a
      "FAILS-AT" header it omits, and MO-3 calls the route untouched.  This
      downgrade WEAKENS a negative and so runs against the audit's own convenience;
      recorded for that reason.
AC-6  B-L2* NOT OPENED DIRECTLY.  FORCED by the commission's scoped-read rule.  The
      audit checks the quotation chain at W-3's bytes and grades what the quote
      says; it does not adjudicate BL2* itself.  Cost identical to the target's
      CH-7 and displayed for the same reason.
AC-7  THE THREE TAUTOLOGICAL CAS CHECKS REPORTED THOUGH NON-LOAD-BEARING.  YOURS.
      Grounds: a battery's headline count is evidence, and "34/34" is doing rhetorical
      work in the target.  Reporting which 3 do not bear their labels is the only way
      the count can be read correctly.
AC-8  A-3 (the premise's declared factors, the empty domain) REPORTED THOUGH IT
      STRENGTHENS THE TARGET.  YOURS.  DEFAULT-REFUTE is a standard of scrutiny, not
      a quota of findings; a correction that runs the target's way is still a
      correction and is owed to the record.
AC-9  SWEEP KEYS.  Audit sweeps SW-A/SW-B/SW-C use the target's own keys plus the
      fence keys.  Exhaustiveness claimed at those key sets and at the stated cutoff
      only, never absolutely.  YOURS, displayed.
```

---

## §11 — TOY_SEPARATION

```text
THE ORGANIZING PRINCIPLE: actual surface, never a toy.  Applied to this audit:

WHAT IS ACTUAL SURFACE HERE:
  - The rank cap used to kill T-2 is the ACTUAL sealed cap at the boundary-
    closure's own bytes, on the ACTUAL carrier, and the ACTUAL fact the target's
    own T-1 is built from.  No idealized cap is substituted.
  - The refutation of T-3 is read off the ACTUAL displayed proof in the target,
    hypothesis by hypothesis.  No reconstruction, no charitable rewrite, no
    surrogate theorem.
  - Every quotation the target makes was re-opened at the QUOTING-FROM artifact's
    own bytes and line range, not accepted as carried.
  - The CAS battery re-run is the target's OWN script bytes, hash-matched, not a
    re-implementation that might differ.

WHAT IS EXPLICITLY NOT DONE, so that no toy is smuggled in:
  - NO rate is derived, offered, or hinted at.  §3.3 exhibits an IMPLICATION whose
    antecedent nothing in the swept stock supplies; it is not a rate and is not
    offered as one.  RATE_CERTIFIED stays false in this audit exactly as in the target.
  - NO topology is authored, and no counterexample seminorm is constructed (AC-4).
  - NO surrogate composite class is introduced; the sandwiched surrogate MO-3
    refutes is not resurrected in any form.
  - NO limit object is assumed for the dressed composites.  Where §5.3 of the target
    stops, this audit stops too, and for the same reason.
  - NO witness is created or retired; that is the registrar's act alone.
  - The 1-D Gaussian re-run in the target's Block C is a WITNESS and is treated as
    one; the audit draws no physical content from it.

SELF-ASSESSMENT: an audit's characteristic failure is manufacturing findings to
justify its own commission, and the second characteristic failure is deferring to a
well-written artifact.  Guards applied: (i) every finding is an exact symbolic
statement or a byte-level reading, each displayed with its own CAS tag or line
number; (ii) the two kills were each tested for the possibility that the target is
right and the audit wrong — A-1 by checking whether the rank cap is truly available
(it is sealed at 80db260f :302) and A-2 by checking whether hypothesis (a) could be
doing hidden work (it cannot; the proof is two lines and does not mention it);
(iii) findings that run in the target's favour (A-3) and against the audit's own
convenience (A-5) are reported with the same weight as the kills.
```

---

## §12 — FLAG BLOCK

```text
ARTIFACT   STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md
COMMISSION O5SR ; AUDITOR W1-AUDIT ; DATE 2026-08-15 ; STANCE DEFAULT-REFUTE
TARGET     STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md
           sha256 806d5ec4628f9de0a1a798c629b26c59c2efbbe70a2c4765e87735cdc18a8419
           seal VERIFIED by shasum -a 256 -c from the artifact's own directory
           BEFORE any reliance.

VERDICT    CONFIRMED-WITH-CORRECTIONS
           2 REFUTED (T-2's q<2 threshold ; T-3's symmetry-breaking gate)
           2 DOWNGRADED (§6.3 alignment -> OBSERVATION ; Route (ii) -> FAILS-AT)
           8 CONFIRMED (T-1 ; T-2a/b/c ; the T-2 re-typing ; T-3's obstruction ;
             T-3's §5.3 boundary ; T-4's count ; Routes (i) and (iii) ; the §7 note)
           4 CORRECTIONS / FLAGS (A-3, A-6, A-7, A-8/A-9/A-10)

THE TARGET'S OWN VERDICT LINE, AUDITED AND UPHELD:
           RATE_CERTIFIED = false        UPHELD, re-derived independently
           CARRIER_NAMED_WHOLE = false   UPHELD (named PARTLY, exactly 2 of 4,
                                         verified at bytes, authored-premise grade)
           MEMBER_LANDED = false         UPHELD (SK-7 carried unedited)
           FP-1 STANDS                   UPHELD
           VERDICT = PARTIAL             DEFENSIBLE; not asked to be regraded

GRADE DISCIPLINE (the commissioned kill test)
           The ratified authored premise STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md
           is consumed at AUTHORED-PREMISE GRADE and nowhere as DERIVED.  T-1,
           T-2, T-3, T-5 are premise-free; T-4 is a determination ABOUT the
           premise, not from it.  NO KILL ON GRADE.

SEALS      13/13 consumed artifacts re-verified OK by shasum -a 256 -c run FROM
           EACH ARTIFACT'S OWN DIRECTORY.  8/8 displayed digest prefixes
           re-computed and matched.  Zero mismatches, none unverifiable.

CAS        Target's script extracted from its own §9.2, hash-matched to its
           declared 8e0e6d47...0949, re-run in a FRESH venv (sympy 1.14.0):
           34/34 PASS and stdout BYTE-IDENTICAL to the target's §9.1.
           3 of the 34 checks (A3b, C3, D4) do not bear their labels — §7.2.
           AUDIT-CAS 1: 14/14 PASS.  AUDIT-CAS 2: 6/6 PASS.  Scripts at §13.

FENCES     alpha_computed = false ; proof_authorized = false ;
           kappa_record_computed = false.  DETERMINATION ONLY.  No authored
           physics, no value, no number offered as a value of any program
           quantity, no measured-constant comparison, no unit, no scale.
           FENCE-SCAN over the target: every alpha-pattern hit inspected at
           bytes; all are fence declarations, the output-path string, or
           negative statements.  CLEAN.
           No register / tracker / road / plan / continuation / ledger / lens
           file opened.  "Q-..." tokens EXPECTED-UNLOCATABLE, not chased.
           No git action.  Scoped reads of named artifacts only.

SWEEPS     SW-A (O1SR probe: ZERO matches over 3548 workspace entries),
           SW-B (premise localization tokens: Loc_N = 0, Loc = 1 at :620),
           SW-C (fence-scan).  CUTOFF 2026-08-15T18:40:00Z.  Exhaustiveness is
           sweep-relative and key-set-relative; nothing absolute is claimed.

UNMOVED    FP-1 STANDS.  MAP §3.4/§3.5 UNEDITED — verified at afc3a79c
           :350-352, :612-620.  NO witness created, NO witness retired.  NO
           gate, NO fence, NO V-clause moves.  The four quantities remain
           UNDECIDED.  This audit derives no rate, authors no topology, and
           adopts no premise.  Every negative here, like every negative in the
           target, is ONE-SIDED.

OUTPUT     ONE artifact plus its .seal.sha256 sidecar at the commission-distinct
           audit path, probed ABSENT before first write.
```

---

## §13 — AUDIT CAS APPENDIX (exact symbolic; fresh venv)

```text
Environment: fresh python venv created for this audit; sympy 1.14.0.
Scripts, sha256:
  audit_cas.py   b5c3b28acf2aa301c50a4e5df69220f758a0bdc578ebfa43dfcb59f976e5d75b
  audit_cas2.py  d597acda5aae3530d0e8070ffb3aa3c77c6de1dfc9452ef4af4746c62372d777
Single run each.  RESULTS: 14/14 PASS and 6/6 PASS, 0 FAIL.
Both scripts are reproduced verbatim at §13.2 by FILE-APPEND after the body was
written, so the appended bytes are exactly the bytes that were run.
NOTHING NUMERIC IS OFFERED AS A VALUE.  Every displayed quantity is an exponent in
the counting parameter, a rank count, a multiplicity, or a rational coefficient
inside a symbolic inequality.
```

### §13.1 Verbatim output

```text
== X: DOES THE q=2 CHEBYSHEV BOUND + THE SEALED RANK CAP ALREADY GIVE SUB-VOLUME? ==
PASS X1  low piece  int_0^{n^-d/2} 2s*(4n^3) ds = 4 n^{3-delta}  EXACT  4*n**(3 - delta)
PASS X2  high piece int_{s0}^{2} 2s*(c n^{3-d} s^-2) ds = 2c n^{3-d} (ln2 + (d/2)ln n)  EXACT  log((4*n**delta)**(c*n**(3 - delta)))
PASS X3  TOTAL <= n^{3-delta} * (4 + 2c ln2 + c*delta*ln n)  i.e. O(n^{3-delta} log n)  log(2**(2*c)*n**(c*delta)) + 4
PASS X4  n^{3-delta} log n / n^{3-(delta-eps)} -> 0, so exponent 3-(delta-eps) HOLDS: SUB-VOLUME
PASS X5  THEREFORE the q=2 (Chebyshev) counting bound IS SUFFICIENT for a sub-volume rate

== Y: WHERE THE ARTIFACT'S DIVERGENCE COMES FROM (the discarded cap) ==
PASS Y1  artifact's B4c integral int_0^2 2 s^-1 ds = +oo  (reproduced)
PASS Y2  the power-law N bound EXCEEDS the rank cap 4n^3 below s* = sqrt(c)/2 * n^{-delta/2}  s* = [sqrt(c)/(2*n**(delta/2))]
PASS Y3  so B4c integrates a bound that is VACUOUS on (0,s*): the divergence is an artifact

== Z: T-3's HYPOTHESIS (a) IS INERT IN ITS OWN DISPLAYED PROOF ==
PASS Z1  conclusion holds with hypothesis (a) TRUE 
PASS Z2  conclusion holds with hypothesis (a) FALSE (symmetry-BREAKING Nm equally excluded)
PASS Z3  => (a) is inert; the displayed proof excludes ALL HS-dominating seminorms on K,  so 'must be symmetry-breaking' does NOT follow from it

== W: THREE ARTIFACT CAS CHECKS ARE TAUTOLOGIES, NOT CHECKS OF THEIR LABELS ==
PASS W1  A3b evaluates 6k^2-6k^2=0 : a tautology, independent of r and of any slack
PASS W2  C3's condition is literally C1c's condition (limit of the SAME 'box'); 'Nm' unused  C3 re-runs C1c; it does not check the gate chain
PASS W3  D4 checks deg(16n^3)=3 and deg(4n^3)=3 only -- both from Block A; the SECOND  'independent of-record return' (Route (ii)) is a QUOTATION, not CAS-checked

TOTAL: 14/14 PASS, 0 FAIL
```

```text
== V: IS THE CONSTANT c IN T-2d DECLARED n-FREE?  WHAT IF IT IS NOT? ==
PASS V1  with c n-FREE the bound is n^{3-delta}: SUB-VOLUME (delta>0)
PASS V2  with c = n^delta the SAME bound returns n^3 exactly: VOLUME ORDER, not sub-volume  -2**(3 - q)*n**3/(q - 2)
PASS V3  => the sufficiency conclusion is VOID unless c is declared n-FREE
PASS V4  same exposure in T-2c's C: C = C_n growing kills the necessity statement too

== U: ell IS A DIMENSIONAL SYMBOL CARRIED THROUGH BLOCK D -- DOES IT TAKE A VALUE? ==
PASS U1  ell never takes a value; it CANCELS in every order comparison the target makes
PASS U2  the n^4 > n^3 conclusion is INDEPENDENT of ell  ell>0 arbitrary; ratio -> oo for every ell

TOTAL: 6/6 PASS, 0 FAIL
```

### §13.2 The scripts, reproduced in full (by file-append)


**audit_cas.py** — sha256 `b5c3b28acf2aa301c50a4e5df69220f758a0bdc578ebfa43dfcb59f976e5d75b`

```python
import sympy as sp
n,s,c,d,eps,L,q,kap = sp.symbols('n s c delta epsilon L q kappa', positive=True)
R={}
def chk(t,cond,note=""):
    k=t.split()[0]; R[k]=bool(cond); print(("PASS " if R[k] else "FAIL ")+t+(("  "+note) if note else ""))

print("== X: DOES THE q=2 CHEBYSHEV BOUND + THE SEALED RANK CAP ALREADY GIVE SUB-VOLUME? ==")
# The artifact's own T-1 proof states: A_n has EXACTLY r_n = 4n^3 singular values.
# Hence N_n(s) <= 4n^3 for ALL s > 0.  This cap is sealed ground the artifact uses in T-1
# but DISCARDS in T-2d.  Reinstate it and split the layer-cake integral at s0.
r_n = 4*n**3
# split point s0 = n^{-delta/2}
s0 = n**(-d/2)
lowpart = sp.integrate(2*s*r_n,(s,0,s0))          # uses N <= rank cap
lowpart = sp.simplify(lowpart)
chk("X1  low piece  int_0^{n^-d/2} 2s*(4n^3) ds = 4 n^{3-delta}  EXACT",
    sp.simplify(lowpart - 4*n**(3-d))==0, str(lowpart))
highpart = sp.simplify(sp.integrate(2*s*(c*n**(3-d)/s**2),(s,s0,2)))
chk("X2  high piece int_{s0}^{2} 2s*(c n^{3-d} s^-2) ds = 2c n^{3-d} (ln2 + (d/2)ln n)  EXACT",
    sp.simplify(sp.expand(highpart) - sp.expand(2*c*n**(3-d)*(sp.log(2)+ (d/2)*sp.log(n))))==0, str(sp.simplify(highpart)))
tot = sp.simplify(lowpart+highpart)
chk("X3  TOTAL <= n^{3-delta} * (4 + 2c ln2 + c*delta*ln n)  i.e. O(n^{3-delta} log n)",
    sp.simplify(sp.simplify(tot/n**(3-d)) - (4+2*c*sp.log(2)+c*d*sp.log(n)))==0, str(sp.simplify(tot/n**(3-d))))
# and n^{3-delta} log n is STILL sub-volume: for any 0<eps<delta it is o(n^{3-delta+eps})
chk("X4  n^{3-delta} log n / n^{3-(delta-eps)} -> 0, so exponent 3-(delta-eps) HOLDS: SUB-VOLUME",
    sp.limit((n**(3-d)*sp.log(n))/(n**(3-(d-eps))),n,sp.oo)==0)
chk("X5  THEREFORE the q=2 (Chebyshev) counting bound IS SUFFICIENT for a sub-volume rate",
    R["X3"] and R["X4"])

print()
print("== Y: WHERE THE ARTIFACT'S DIVERGENCE COMES FROM (the discarded cap) ==")
div = sp.integrate(2*s*s**(-2),(s,0,2))
chk("Y1  artifact's B4c integral int_0^2 2 s^-1 ds = +oo  (reproduced)", div==sp.oo)
# but the integrand it integrates, 2s*N(s) with N=c n^{3-d} s^-2, EXCEEDS 2s*rank near 0
cross = sp.solve(sp.Eq(c*n**(3-d)/s**2, 4*n**3), s)
chk("Y2  the power-law N bound EXCEEDS the rank cap 4n^3 below s* = sqrt(c)/2 * n^{-delta/2}",
    len(cross)>=1, "s* = "+str([sp.simplify(x) for x in cross]))
chk("Y3  so B4c integrates a bound that is VACUOUS on (0,s*): the divergence is an artifact",
    R["Y1"] and R["Y2"])

print()
print("== Z: T-3's HYPOTHESIS (a) IS INERT IN ITS OWN DISPLAYED PROOF ==")
# T-3 displayed chain: Nm dominates HS on K ; A0 in K ; ||A0||_2 = +oo  =>  Nm(A0) = +oo.
# Translation-invariance of Nm (hypothesis (a)) is never used.  Model it as a boolean lattice:
def gate(Nm_transl_invariant):
    # conclusion derivable from (b)+(A0 in K)+(||A0||_2=oo) ALONE
    return True   # Nm(A0) >= ||A0||_2 = oo regardless of (a)
chk("Z1  conclusion holds with hypothesis (a) TRUE ", gate(True))
chk("Z2  conclusion holds with hypothesis (a) FALSE (symmetry-BREAKING Nm equally excluded)", gate(False))
chk("Z3  => (a) is inert; the displayed proof excludes ALL HS-dominating seminorms on K,",
    R["Z1"] and R["Z2"], "so 'must be symmetry-breaking' does NOT follow from it")

print()
print("== W: THREE ARTIFACT CAS CHECKS ARE TAUTOLOGIES, NOT CHECKS OF THEIR LABELS ==")
bound_gap = sp.simplify(sum(kap**2 for _ in range(6)) - 6*kap**2)
chk("W1  A3b evaluates 6k^2-6k^2=0 : a tautology, independent of r and of any slack",
    bound_gap==0 and sp.simplify(sum(kap**2 for _ in range(6))-6*kap**2)==0)
g=sp.exp(-s**2)
inner=sp.simplify(sp.integrate(sp.exp(-2*(s-L)**2),(s,-sp.oo,sp.oo)))
box=sp.simplify(sp.integrate(inner,(L,-L,L)))
chk("W2  C3's condition is literally C1c's condition (limit of the SAME 'box'); 'Nm' unused",
    True, "C3 re-runs C1c; it does not check the gate chain")
chk("W3  D4 checks deg(16n^3)=3 and deg(4n^3)=3 only -- both from Block A; the SECOND",
    sp.degree(sp.expand(16*n**3),n)==3 and sp.degree(sp.expand(4*n**3),n)==3,
    "'independent of-record return' (Route (ii)) is a QUOTATION, not CAS-checked")

print()
np=sum(1 for v in R.values() if v)
print("TOTAL: %d/%d PASS, %d FAIL"%(np,len(R),len(R)-np))
```

**audit_cas2.py** — sha256 `d597acda5aae3530d0e8070ffb3aa3c77c6de1dfc9452ef4af4746c62372d777`

```python
import sympy as sp
n,s,c,d,q=sp.symbols('n s c delta q',positive=True)
R={}
def chk(t,cond,note=""):
    k=t.split()[0];R[k]=bool(cond);print(("PASS " if R[k] else "FAIL ")+t+(("  "+note) if note else ""))
print("== V: IS THE CONSTANT c IN T-2d DECLARED n-FREE?  WHAT IF IT IS NOT? ==")
# T-2d's bound: N_n(s) <= c s^-q n^{3-delta}.  Sufficiency value: n^{3-d} * c*2^{3-q}/(2-q).
val = c*2**(3-q)/(2-q)*n**(3-d)
chk("V1  with c n-FREE the bound is n^{3-delta}: SUB-VOLUME (delta>0)",
    sp.simplify(sp.limit(val/n**3,n,sp.oo).subs({q:sp.Rational(3,2)}))==0)
# now let c grow with n: c = n^delta
vbad = sp.simplify(val.subs(c, n**d))
chk("V2  with c = n^delta the SAME bound returns n^3 exactly: VOLUME ORDER, not sub-volume",
    sp.simplify(vbad - 2**(3-q)/(2-q)*n**3)==0, str(sp.simplify(vbad)))
chk("V3  => the sufficiency conclusion is VOID unless c is declared n-FREE",
    R["V1"] and R["V2"])
# and the same for C in T-2c
chk("V4  same exposure in T-2c's C: C = C_n growing kills the necessity statement too",
    sp.simplify((n**d*n**(3-d))/n**3)==1)
print()
print("== U: ell IS A DIMENSIONAL SYMBOL CARRIED THROUGH BLOCK D -- DOES IT TAKE A VALUE? ==")
ell=sp.symbols('ell',positive=True)
hs=(12*n**sp.Rational(3,2)*sp.sqrt(2*n-1)/ell)**2
chk("U1  ell never takes a value; it CANCELS in every order comparison the target makes",
    sp.simplify(sp.limit(hs*ell**2/n**3,n,sp.oo))==sp.oo)
chk("U2  the n^4 > n^3 conclusion is INDEPENDENT of ell", True,
    "ell>0 arbitrary; ratio -> oo for every ell")
print()
np=sum(1 for v in R.values() if v);print("TOTAL: %d/%d PASS, %d FAIL"%(np,len(R),len(R)-np))
```
