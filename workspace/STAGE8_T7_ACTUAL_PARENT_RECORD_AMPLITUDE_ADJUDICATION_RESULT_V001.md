# Stage-8 T7 Actual-Parent Record-Amplitude Adjudication Result v001

Date: 2026-07-24

## Verdict

```text
ACTUAL_PARENT_RECORD_AMPLITUDE_BLOCKED
```

The calculation uses the finite Lorentzian source-record parent itself, not
the rejected ideal projector chain. Projection onto every two-record public
outcome produces a full-rank operator on the 12-dimensional source
regulator. In particular, the completed `|p,p>` Kraus operator is strongly
non-scalar:

```text
source-operator Frobenius norm = 2.158029616704532
distance from scalar identity = 2.151758052952419
```

The disclosed nonzero-momentum quasifree state does not determine a state on
this same finite regulator. Its source Hamiltonian has:

```text
4 negative modes;
4 zero modes;
4 positive modes.
```

The sealed lineage supplies no zero-mode covariance, and the completed
record operator acts nontrivially on that zero-mode sector
(`norm = 0.7236003117991379`). The ambiguity therefore does not cancel.

The actual finite-packet one-cell amplitude for the disclosed regression
vector is

```text
0.8524349274215997
```

rather than the ideal root-line value `1`. This is consistent with the
earlier Lorentzian result that a generic finite wavepacket is not a universal
exact endpoint. It may not be repaired by replacing the parent with the
ideal root line.

No determinant shortcut was used: projection on a record outcome has not
been proved to leave a single second-quantized one-particle contraction.

## Closure condition

The next admissible result must derive a regulator-compatible incoming
state, including its zero-mode treatment, from the same parent and prove
that the normalized completed-record response is independent of that
regulator choice. Equivalently, it may construct the continuum amplitude
directly and prove a finite-volume approximation theorem. A final source
ray, normalized source trace, or Gaussian determinant may not be inserted
after this failure.

All T7 thermodynamic obligations and all protected statuses remain false.
