# Stage-8 T7(iv) Completed-Chain Conditioned Duhamel Identity and Conditional Interchange Schema Specification v001

Date: 2026-07-25 (night)
Proposed canonical filename:
`STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_IDENTITY_AND_INTERCHANGE_SCHEMA_SPEC_V001.md`
Root: `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`

## Status

```text
SPECIFICATION_SEALED_BEFORE_EXECUTION
(hostile pre-execution review is mandatory before any execution)
```

This specification implements pieces 1 and 2 ONLY of the item-5 decomposition
recorded in `ROUTE_MEMO_2026-07-25_duhamel_intensive_hessian.md`, re-typed
onto the completed chain as mandated by the sealed L2 typing freeze
(`STAGE8_T7_L2_INTENSIVE_HESSIAN_TYPING_FREEZE_V001.md`, fence F2.3). It
derives a finite-K conditioned Duhamel identity on the completed chain and
seals a conditional Vitali-Cauchy interchange theorem schema whose two
hypotheses are NAMED UNDERIVED INPUTS. It does not discharge T7(ii) or
T7(iii), does not instantiate on the actual parent, does not compute
kappa_record, a coupling, or alpha, and does not touch any exhaustive-chain
sealed result. Exact arithmetic is used wherever the claim is decidable;
certified enclosures otherwise; no measured constant is consulted or used
anywhere in this specification or its execution.

## Frozen authorities

The executor must verify every hash before any other action. Any mismatch is
an atomic `AUTHORITY_MISMATCH_BLOCKED`. No authority may be replaced by an
unhashed equivalent. Route memos and supervision records live at
`/Users/bgm/MB Work/alpha_supervision/`; all other artifacts at the canonical
cleanroom root.

| Role | Artifact | SHA-256 |
|---|---|---|
| Item-5 decomposition (pieces/ordering/controls) | `ROUTE_MEMO_2026-07-25_duhamel_intensive_hessian.md` | `8130ae5d11a582ab79bb19a624dc8e128a5ceddc349d3bd27585b1c975f98cff` |
| BINDING typing freeze (fences F2.1-F2.5) | `STAGE8_T7_L2_INTENSIVE_HESSIAN_TYPING_FREEZE_V001.md` | `cdbacdaf5efcd3a70a6ffa456bc790753e0ff36609c40c67b9bb606b79624746` |
| Typing-freeze grounds memo | `ANALYSIS_MEMO_2026-07-25_L2_typing_freeze.md` | `f2848ce7ca14d2e7765991fe9bad226197dc105f7cdd9a47728230b6690490f1` |
| Sealed O5 operator Duhamel tangent (Q = Q_comp) | `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md` | `2f2aa7f7397b70616fa5c9e8ed628ca1d1e819bb698133a169c6d544086b3cde` |
| D1-D6 exact finite derivations | `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_EXACT_DERIVATION_V001.md` | `a9875788301d8434113f77e3b5726b49d70d8609fbcfcc72c9fede76a1249e4a` |
| Sealed finite anchors (epsilon-halving 8.19415e-8 / 6.69468e-8; non-scalar witness) | `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_RESULT_V001.md` | `76f5505e3aa1fdc11102f782ed8ee40e49787bb38e3a2524e17b92fd8de46740` |
| Route-2 architecture amendment (A2 exhaustive closure; A5 no-promotion list) | `STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md` | `8a7f52ffa2500d20ad834b11e3762ed114ee1a201f2fec18bcb119e3c7ead860` |
| Phase-A completed-chain construction (A2 generator; A3 pointer weights; A4 K_pointer/R_pointer) | `STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md` | `789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3` |
| Battery authority (T6, T7(i)-(iv), T9, NC5) | `STAGE8_GATE5_KAPPA_RECORD_THEOREM_BATTERY_SPEC_AUTHORITY_FABLE_V002.md` | `ddb36cbfe4edfc2a0520e9ae58063295214c64afed5cad5a64e9f311826358f5` |
| kappa_record semantics (1217-1315) and V010 negative control | `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a` |
| Completed-amplitude selection derivation | `BID_PUBLIC_RECORD_TRANSITION_AMPLITUDE_DERIVATION_V001.md` | `6e01e5bac60b0f9efdb6f964e2b2ea7727ca46e4cfd66153e8ef70baf240a6eb` |
| Non-clustering witness cos(N tau_R A); contact-term obligation | `STAGE8_T7_CONNECTED_ANALYTIC_CLOSURE_RESULT_V001.md` | `f891d3afc58e695529d8d569b5d2ba4b853e30c9cfa4296817cb17b60f38b054` |
| Completed/exhaustive substitution fence | `STAGE8_T7_CRITICAL_PATH_SCOPE_CORRECTION_V001.md` | `9410ee80ff89beed4e133f75fcdb952d059f3386df12069793b5d60895d15486` |
| Relay coordinates and sigma-witness curvatures | `STAGE8_T7_RELAYED_FAMILY_RESOLUTION_RESULT_V001.md` | `52401eefc3ff84e22f04bbc329a619b830058661db7f969d2d43d167d97d893d` |
| Relay necessity along the exhaustion | `STAGE8_T7_OPEN_EXHAUSTION_ATTACHMENT_AND_RELAY_NECESSITY_RESULT_V001.md` | `0df721a170f4f4a1ec630775a3ae47b0d793c82e100b326e681030389eaf0694` |
| Exhaustive companion scalar closure | `COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md` | `273e1473a1a8bf0be0467634411cec1b7daeee0c9f24c330fad5d288d191dcbb` |
| Route-1 falsifier binding | `STAGE8_ROUTE1_SPECIAL_CASE_CONSISTENCY_BINDING_V001.md` | `460e87522884e703968025081cceccc0153af3cda27410c397fc2a09a0b367e3` |
| Enclosure/positivity/intersection conventions to re-type | `COMPLETE_QSPEC_FINITE_HOLONOMY_DUHAMEL_CROSSCHECK_PROTOCOL_V001.md` | `3d86dc4f15cc51bd3a3848b71c295734c6cee565cffc5817b7ddcfcedd30126f` |
| Independent-verifier conventions (bit-mask CAR, unsplit RK4) | `COMPLETE_QSPEC_FINITE_HOLONOMY_INDEPENDENT_VERIFIER_PROTOCOL_V001.md` | `f5302ee851bc8c76e4abf91ca7fdbddaca9569202b556db4ef59f0235c592ebe` |
| Exact sigma curvature witness (1/4, 1/4, 1/2, 0) | `STAGE8_T7_RESPONSE_CLOSURE_SELECTION_DERIVATION_RESULT_V001.md` | `e12fffcc0f94d6896bc5607898db6b3a1ea343aeaf557b1f6f70e58db061fab6` |
| C2 envelope clause 3(d); parity lemmas; L1/L2 flags | `ROUTE_MEMO_2026-07-25_gamma_record_level_insensitivity.md` | `da6d8cc7bd4736b079e8262cc9ea15edfb6a574d038bc790eaad1432e8515875` |
| Item-3 interface to name identically | `ROUTE_MEMO_2026-07-25_linked_cluster_majorant.md` | `3e37eaa4d9183d171eb9e4b0dd0ad799a3503cd364c022858ac402396d04834e` |
| Principal disposition (5.1-5.4) | `DISPOSITION_2026-07-25_gamma_killtest_beta.md` | `143d4b494b5ce5c24e105ac8ddbaa3f7655803ce53b4803d72c1bb4c076ec638` |

## S0 - Inherited binding fences

The following are standing fences of this specification, inherited verbatim
in force from the typing freeze and the scope correction. Violation of any
one is an atomic BLOCKED.

1. F2.1 — the exhaustive chain's zero-free results may NEVER be cited
   against, or substituted for, the completed chain's T7(ii) obligation or
   the (H1) hypothesis below.
2. F2.2 — the completed chain's Hessian may NEVER be checked against the
   exhaustive three-way `H_CTP/g_FS/g_Duhamel` agreement as if it were the
   same number. That agreement is internal to the exhaustive chain. Section
   S3 derives the exact reason: the completed chain's diagonal is not flat,
   and the attenuation identity acquires an explicit correction term.
3. F2.3 — the route memo's piece-1 wording (unitality-anchored
   `Z[A,A] = 1`; `g_D = <eta|eta> - |<psi|eta>|^2`) is NOT inherited. The
   completed object has no unitality anchor. The replacements frozen here
   are the conditioned-covariance normal form of S3 and the named baseline
   hypothesis B0 of S2.
4. F2.4 — the additive relation and its Hessian-mixing formula are an
   obligation of THIS specification (lemma L-ADD, section S4).
5. F2.5 — the periodic zero-free lineage's object is the all-outcome
   closure; no audit of this execution may cite it as a T7(ii)/B0/(H1)
   discharge.
6. Per-state quantifier discipline: every scalar claim in this
   specification is quantified per pinned state. No claim is promoted to
   "for all states"; the two Phase-A pinned states are evaluated and
   reported separately; no cross-state averaging is permitted.
7. Substitution fences in both directions (critical-path scope correction;
   O6 of the sealed O5 spec): the all-outcome sum may not be substituted for
   the completed component, and conversely.
8. No measured constants; no target reads; exact arithmetic where decidable,
   certified enclosures otherwise; intersection-of-enclosures is the only
   numerical pass criterion (S7).

## S1 - Frozen objects, ordering, and state evaluation

### S1.1 Carriers and kernels

Let `K` denote a member of the relayed causally-sequential exhaustion. On the
abstract amended objects (sealed O1-O3 of `2f2aa7f7...`):

```text
V_K[A] = W_K[A] i_r : H_S -> H_S tensor H_R;
Q = I_S tensor Q_comp,  Q = Q^dagger = Q^2;

R_comp,K[A_+,A_-] = V_K[A_-]^dagger Q V_K[A_+];
R_all,K [A_+,A_-] = V_K[A_-]^dagger V_K[A_+];
R_rest,K[A_+,A_-] = V_K[A_-]^dagger (I - Q) V_K[A_+];
R_all,K = R_comp,K + R_rest,K            (exact; D2 completeness).
```

On the Phase-A completed chain (`789338ad...` A3/A4) the completed kernel is
realized by the pointer-weighted history sums

```text
K_pointer(a) = sum_lambda w_lambda Gamma(u_lambda(a));
R_pointer(a_+,a_-) = K_pointer(a_-)^dagger K_pointer(a_+)
 = sum_(mu,lambda) w_mu^* w_lambda
     Gamma(u_mu(a_-)^dagger u_lambda(a_+)),
```

using functoriality `Gamma(u)^dagger Gamma(v) = Gamma(u^dagger v)`. This is
the freeze's Phase-A A4 pointer lineage; no other completed realization is
admitted by this gate.

### S1.2 State evaluation (per pinned state)

Let `rho` be a pinned state (density operator; the pure case is
`rho = |psi><psi|`). Define the Hilbert-Schmidt vectors

```text
X_K(a) = Q V_K[A^(a)] rho^(1/2)   in HS(H_S, H_S tensor H_R),
```

so that, exactly, using `Q = Q^2 = Q^dagger`:

```text
z_c,K(a_+,a_-) := omega( R_comp,K[A_+,A_-] )
               = Tr( rho^(1/2) V_K[A_-]^dagger Q V_K[A_+] rho^(1/2) )
               = < X_K(a_-), X_K(a_+) >_HS.
```

For pure `rho` this is `< Q Psi(a_-), Q Psi(a_+) >` with `Psi(a)=V_K[A^(a)]psi`.
The completed-sector state evaluation is therefore a Gram form. Analogously

```text
z_all,K(a_+,a_-) = omega( R_all,K ),   z_r,K = omega( R_rest,K ),
z_all,K = z_c,K + z_r,K                 (exact, state-evaluated).
```

### S1.3 Ordering, pinned explicitly

The mixed-CTP ordering is pinned as follows and may not be varied:

```text
z_c,K(a_+,a_-) = omega( V_K[A_-]^dagger Q V_K[A_+] ):
  the PLUS history propagator stands on the KET (right, forward) branch;
  the MINUS history stands on the BRA (left) branch under the adjoint.
```

This is the (-,+) Wightman-ordered completed two-point functional. No
time-ordering symbol is inserted between the two branch tangents; the mixed
second derivative below is the (+/-) Wightman-ordered covariance of the
Duhamel tangent. Hermitian symmetry holds exactly for real histories:

```text
z_c,K(a_+,a_-)^* = z_c,K(a_-,a_+)        (O4 adjoint exchange, state-evaluated).
```

The T9/FS attenuation form is a COROLLARY (FK-2/FK-3 of S3), not the
primary form. Structural departure from the exhaustive chain, recorded as a
frozen definition: the diagonal

```text
q_K(a) := z_c,K(a,a) = ||X_K(a)||^2   (completed probability at history a)
```

is NOT identically 1 and is NOT flat. `Z_all,K[A,A] = 1` (unitality) holds
only for the exhaustive companion. The completed log branch is anchored by
the T7(i) normalization

```text
zhat_c,K := z_c,K / q_K(0),   zhat_c,K(0,0) = 1,
```

which requires the named baseline hypothesis B0 below — the anchor is a
hypothesis, never a structural identity, on this chain.

## S2 - Named hypotheses and hypothesis-lemmas

### S2.1 B0 (baseline non-vanishing; the finite restriction of T7(ii))

```text
B0(K, rho):  q_K(0) = z_c,K(0,0) != 0.
```

B0 is T7(ii) where it always lived (freeze F2.3). At fixed finite `K` and
pinned state it is decidable (exact arithmetic) or certifiable (enclosure
excluding zero); the executor must so verify it for every fixture (S7).
B0 may NEVER be promoted to the volume-uniform statement: that promotion IS
(H1), a named underived input of S5. A failed or uncertifiable B0 is
`BASELINE_ZERO_BLOCKED` for that fixture.

### S2.2 C1 (hypothesis-lemma: branch generators affine in the branch tangent at baseline)

**Statement.** For every branch label `lambda` and every admitted history
direction `a`, the one-particle generator family is affine in the history
parameter at baseline:

```text
h_lambda(t; s) = h_lambda(t; 0) + s J(t),
d^2 h_lambda(t; s) / ds^2 = 0,
```

with `J(t)` independent of `s` and of `lambda`.

**Proof obligation (on the sealed finite objects; expected DERIVE).** For
the Phase-A generator this is TRUE by inspection of the sealed A2 frozen
form

```text
h_(lambda,n,ell)(t;a) = h_(0,n,ell) + lambda v(t) M_(n,ell)(t) tensor S_n
                        + a J_(n,ell)(t),
J_(n,ell)(t) = -B_(D,n,ell)(t) tensor alpha_x,
```

(cite `789338ad...` A2). The executor must verify `d^2 h/da^2 = 0`
symbolically/exactly on the frozen Phase-A generator.

**Recorded falsification channel (non-blocking here, binding on
successors).** C1 is NOT automatic for V011 link holonomies: a link-holonomy
generator depends on the connection through `exp(i integral A)`-type factors
and is not affine, so its second history derivative contributes a contact
term. This is a genuine falsification channel for the V011 link-holonomy
wording and must be re-examined by any successor that changes the connection
realization. The sealed three-site fixture generator
`h_source(theta) = -i D_theta tensor alpha_x` with phases `exp(+-i theta/3)`
is itself NON-affine; this spec exploits that fact as the contact-term
negative control (S6.3) rather than treating it as a C1 failure — C1 is
quantified over the completed Phase-A chain, and the fixture's non-affine
curvature term must be included exactly wherever the fixture is used.

**Role.** The mixed identity FK-1 and the FS identity FK-3 need only
norm-`C^1` regularity (the sealed O5 hypothesis). C1 is load-bearing for
FK-2's second-order terms (it kills the generator-curvature contact term)
and for the interchange schema's thermodynamic tangent (the same `J` at
every history — the linearized tangent is exact).

C1 may individually block: `C1_AFFINE_TANGENT_LEMMA_BLOCKED` with the
violating generator as named witness.

### S2.3 C2 (hypothesis-lemma: history-independence of boundary data along the relayed exhaustion)

**Statement.** Along the relayed causally-sequential exhaustion, the
following are all independent of the connection history `A`:

```text
(a) the ready injections i_r^(K);
(b) the final PVMs {P_xi^(K)} and hence Q_comp^(K);
(c) the relay isometries binding member K to its successor;
(d) [envelope clause, per the gamma memo's condition 3(d)]:
    (a)-(c) hold uniformly over the declared ER envelope-profile class —
    envelope-independence is part of the hypothesis, stated, not assumed.
```

**Proof obligation (on the sealed finite objects; expected DERIVE).** The
sealed O5 gate already freezes (a) and (b) as history-independent boundary
data and blocks if either varies ("its derivative must be added and this
specification blocks", `2f2aa7f7...` O5). The executor must verify: on the
Phase-A construction, `i_r` and the qutrit spectral PVM are built from the
`c`-matrix and carry no `A` dependence (exact inspection); on the relayed
family, the relay isometries are built from incidence projectors with no
history argument (cite `52401eef...`, `0df721a1...`); and clause (d) by
inspecting that the envelope profile enters the generator only through the
scalar `v(t)` multiplying `lambda`, never through `i_r`, the PVM, or the
relays (cite `789338ad...` A2; `da6d8cc7...` 3(3)(d)).

**Role.** C2 guarantees the O5 tangent has no boundary-derivative terms, so
the conditioned tangent of S3 is exactly the compressed Duhamel integral,
and the multi-cell composition retains the same form along the exhaustion
(interface to the majorant spec's Lemma 0).

**Recorded falsification channel.** Any future gauge-covariance completion
transports the carrier by `exp(i chi)` and makes the injection
history-dependent in the transformed frame; C2 must then be re-derived, not
assumed. C2 may individually block:
`C2_BOUNDARY_HISTORY_INDEPENDENCE_LEMMA_BLOCKED` with the violating datum as
named witness.

## S3 - Piece 1: the finite-K conditioned identity (frozen definitions and derivation)

All statements in S3 are per member `K`, per pinned state `rho`, under B0
and C2 (FK-2 additionally under C1 or with the contact term explicitly
included). All are finite-dimensional exact algebra on the sealed objects;
their derivations are shown and are themselves execution obligations
(symbolic re-verification plus numeric regression).

### S3.1 Conditioned Duhamel tangent (sealed input)

With `A_s = A + s a`, the sealed O5 operator Duhamel tangent (derived for
`Q = Q_comp`; flag `finite_primitive_operator_Duhamel_tangent_derived =
true`) gives the frozen tangent vector

```text
delta V = -i integral_0^T W(T,t) J_a(t) W(t,0) dt  i_r;
eta := Q (delta V) rho^(1/2)          (conditioned tangent);
X_0 := X_K(0) = Q V_K[0] rho^(1/2)    (conditioned baseline);
q   := q_K(0) = <X_0, X_0>_HS != 0    (hypothesis B0).
```

On the Phase-A chain the compressed tangent is exactly the pointer-weighted
history sum

```text
(I_S tensor <pointer|) delta V = sum_lambda w_lambda d Gamma(u_lambda(s a))/ds |_(s=0),
```

so `eta` and `X_0` are the "pointer-projected tangent and state" of the
freeze, and every scalar below is a ratio of pointer-weighted history sums.

First derivatives of the Gram form, exact:

```text
d_(a_+) z_c |_0 = <X_0, eta>;   d_(a_-) z_c |_0 = <eta, X_0>;
d_(a_+) d_(a_-) z_c |_0 = <eta, eta>.
```

### S3.2 FK-1 — the mixed-CTP conditioned-covariance normal form (primary)

**Statement.** Under B0 and C2:

```text
g_(D,c,K) := d_(a_+) d_(a_-) log z_c,K |_(0,0)
  = <eta,eta>/q - <eta,X_0><X_0,eta>/q^2
  = [ <eta,eta> <X_0,X_0> - |<X_0,eta>|^2 ] / q^2.
```

**Derivation (shown; two lines).** From the Gram derivatives above,

```text
d_+ d_- log z_c = [ z_c (d_+ d_- z_c) - (d_+ z_c)(d_- z_c) ] / z_c^2
   at (0,0):    = <eta,eta>/q - <X_0,eta><eta,X_0>/q^2.
```

The second (ratio) form is the same expression over the common denominator
`q^2`. This is the conditioned-covariance normal form mandated by freeze
fence F2.3: the completed amplitude's mixed log-Hessian equals a ratio-form
covariance built from pointer-weighted history sums.

**Derived structural facts (to be verified, not assumed):**

```text
(P1) g_(D,c,K) is real (hermitian symmetry: the (+,-) coefficient of log z_c
     is self-conjugate).
(P2) g_(D,c,K) >= 0 (Gram/Cauchy-Schwarz on the HS inner product), with
     equality if and only if eta is parallel to X_0. Positivity of the
     MIXED form is structural on this chain; the one-branch weak-value
     forms carry no such guarantee, and no claim is made for them.
(P3) Reduction check: at Q = I_R, q = 1, pure rho, FK-1 reduces exactly to
     the sealed exhaustive form  g_D = <eta|eta> - |<psi|eta>|^2  of the
     crosscheck protocol (3d86dc4f...). The reduction is an executable
     control, not a licence: fences F2.1/F2.2 forbid using it to transport
     exhaustive results to the completed chain.
(P4) Degenerate endpoint: if the completed compression is rank-one (the
     Route-1 comparator), eta is automatically parallel to X_0 and
     g_(D,c) = 0 exactly. Predeclared exact value; see S6.5 and S7.
```

### S3.3 FK-2 — the attenuation corollary with the diagonal correction (the completed-chain H_K = g_D statement)

**Statement.** Under B0, C2 and with the full second-order Duhamel expansion
(under C1 the contact term vanishes; otherwise it must be included):

```text
H_(att,K) := -Re d^2_(a_+) log z_c,K |_(0,0)
           = g_(D,c,K) - (1/2) d^2/da^2 log q_K(a) |_(a=0).
```

Equivalently: the attenuation (log-moduli stencil) Hessian equals the
conditioned Duhamel covariance MINUS half the diagonal log-curvature of the
completed probability. On the exhaustive chain `q(a) == 1` and the
correction vanishes — that, and only that, is why the exhaustive three-way
agreement could equate the stencil with the covariance. On the completed
chain the correction is generically nonzero; omitting it is the predeclared
wrong-form control (S6.3), and fence F2.2 is thereby made mechanical.

**Derivation (shown).** Write the exact second-order Taylor coefficients of
`L := log z_c` at `(0,0)` for real histories:

```text
L(a_+,a_-) = L_0 + alpha a_+ + conj(alpha) a_-
             + (1/2) L_(++) a_+^2 + (1/2) conj(L_(++)) a_-^2
             + g_(D,c,K) a_+ a_-,
```

where hermitian symmetry (`z_c(a_+,a_-)^* = z_c(a_-,a_+)`) forces the
displayed conjugations and the reality of the mixed coefficient. Restrict to
the diagonal:

```text
log q(a) = L(a,a)  =>  (log q)''(0) = 2 Re L_(++) + 2 g_(D,c,K)
        =>  -Re L_(++) = g_(D,c,K) - (1/2)(log q)''(0).
```

Second-derivative content, exact, from the Gram form: with
`zeta := Q (delta^2 V) rho^(1/2)` and the sealed second-order Duhamel
expansion

```text
delta^2 V = 2 (-i)^2 integral_(T>=t>s>=0) W(T,t) J(t) W(t,s) J(s) W(s,0) dt ds  i_r
            + (-i) integral_0^T W(T,t) [d^2 H_s(t)/ds^2] W(t,0) dt  i_r,
```

the second line is the CONTACT TERM; it vanishes exactly under C1 and must
be computed exactly whenever the generator is non-affine (three-site
fixture). Then `d^2_(a_+) z_c|_0 = <X_0, zeta>`, and the identity
`-Re L_(++) = g - (1/2)(log q)''` follows from
`Re[c^2] = 2(Re c)^2 - |c|^2` applied to `c = <X_0, eta>` (the executor must
re-verify this algebra symbolically). The stencil realization

```text
H_(att,K) = lim_(h->0) [ -log|zhat_c(h,0)| - log|zhat_c(-h,0)| ] / h^2
```

is the completed-chain re-typing of the sealed `H_CTP` stencil; note the
normalization `zhat_c` (B0) — the un-normalized stencil diverges, which is
the finite shadow of the missing unitality anchor.

### S3.4 FK-3 — the Fubini-Study corollary on the completed output ray

**Statement.** Under B0 and C2, the FS pullback of the NORMALIZED completed
output ray `Xhat(a) := X(a)/||X(a)||` at baseline equals the conditioned
covariance exactly:

```text
g_FS(completed ray) |_(a=0)
  = <X'(0),X'(0)>/q - |<Xhat(0),X'(0)>|^2/q
  = g_(D,c,K).
```

**Derivation (shown).** Direct computation of the FS metric of `a -> X(a)`
with `X'(0) = eta`, `||X(0)||^2 = q`:
`g_FS = [<eta,eta> q - |<X_0,eta>|^2]/q^2 = g_(D,c,K)`. This realizes
battery T9 on the completed chain exactly as V011 1300-1302 requires — the
FS check anchors on "each normalized local output ray", here the COMPLETED
output ray. The FS of the full (exhaustive) output ray is a different
number (it equals `g_all`); any conflation is an F2.2 violation and must be
caught by the wrong-form control.

### S3.5 What piece 1 claims and does not claim

Pass of S3 derives the finite bundle {FK-1, FK-2, FK-3} per fixture and per
pinned state, conditional on B0, C1 (where invoked), C2. It does NOT derive:
a volume-uniform zero-free neighborhood; a linked-cluster density; any
intensive limit; any unconditional Duhamel/intensive-Hessian equality; any
statement about the actual parent's continuum. The flag
`Duhamel_intensive_Hessian_equality_proved` remains `false` on pass.

## S4 - L-ADD: the additive relation and the q-weighted Hessian-mixing lemma (freeze F2.4 obligation)

**Statement (exact; to be sealed under the typing freeze).** With
`z_all = z_c + z_r` (S1.2), `z_all(0,0) = 1` (exhaustive unitality),
`q := z_c(0,0) in (0,1)`, normalized branches
`zhat_c = z_c/q`, `zhat_r = z_r/(1-q)`, drift coefficients
`alpha_c := d_(a_+) log zhat_c|_0`, `alpha_r := d_(a_+) log zhat_r|_0`, and
mixed log-Hessians `g_c := g_(D,c,K)`, `g_r`, `g_all` defined as in FK-1 for
each functional:

```text
L-ADD:   g_all = q g_c + (1-q) g_r + q(1-q) |alpha_c - alpha_r|^2.
```

**Derivation (shown).** At baseline `z_all = 1`:

```text
d_+ z_all|_0 = q alpha_c + (1-q) alpha_r;
d_+ d_- z_all|_0 = q (g_c + |alpha_c|^2) + (1-q)(g_r + |alpha_r|^2);
g_all = d_+ d_- z_all - (d_+ z_all)(d_- z_all)
      = q g_c + (1-q) g_r
        + [ q|alpha_c|^2 + (1-q)|alpha_r|^2 - |q alpha_c + (1-q) alpha_r|^2 ]
      = q g_c + (1-q) g_r + q(1-q)|alpha_c - alpha_r|^2.
```

**Degenerate endpoint `q = 1` (stated separately; the generic formula
requires `q != 1`).** There `X_r(0) = 0`, so `d_+ z_r|_0 = 0` while
`d_+ d_- z_r|_0 = ||eta_r||^2` need not vanish, giving the exact endpoint
form

```text
g_all = g_c + ||eta_r||^2        (q = 1).
```

**Consequences recorded (the lemma equates nothing).**

```text
(i)  The completed and exhaustive mixed Hessians differ by
     (1-q)(g_r - g_c) + q(1-q)|alpha_c - alpha_r|^2 — unrecorded-sector
     response plus a drift-mismatch term. No sealed identity equates them,
     and this lemma quantifies exactly how they differ.
(ii) Attenuation-level coincidence at the Route-1 endpoint is reproduced
     WITHOUT Hessian equality: on the comparator (exact, decidable)
        g_c = 0;            (log q_c)''(0) = -1/2;
        H_att,c  = 0 + 1/4 = 1/4;
        g_all = 1/4;        (log q_all)'' = 0;   H_att,all = 1/4;
        g_all - g_c = ||eta_r||^2 = |A_-'(0)|^2 = 1/4.
     The two ATTENUATION Hessians coincide (1/4 = 1/4) while the two MIXED
     covariances differ by exactly 1/4. These six rational values are
     mandatory exact-arithmetic anchors (S7.M4) and reproduce the sealed
     sigma-witness curvatures (FS 1/4; linear-amplitude attenuation 1/4;
     endpoint-probability 1/2; inclusive sandwich 0) of e12fffcc.../52401eef...
(iii) Auditability of the substitution fences: any pipeline claiming a
     completed Hessian obtained from an exhaustive computation must violate
     L-ADD unless q = 1; the executor runs L-ADD as a residual check on
     every fixture (S7.M5).
```

## S5 - Piece 2: the conditional interchange theorem schema (Route A, Vitali-Cauchy)

### S5.1 Named underived inputs

The schema consumes exactly two hypotheses. THEY ARE NOT DERIVED HERE, NOT
ASSUMED HERE, AND NOT DISCHARGEABLE HERE. They must arrive as derived
outputs of program items 1-4 through the item-3 interface (S5.4).

```text
(H1) [= battery T7(ii); volume-uniform zero-free polydisc]
     There exist epsilon_* > 0 and delta in (0,1] such that for every
     member K of the relayed exhaustion and every point of the closed
     polydisc P = {(z,w) in C^2 : |z| <= epsilon_*, |w| <= epsilon_*}:
        | Ghat_K(z,w) | >= delta,
     where Ghat_K is the normalized CTP pair-holomorphic extension of
     zhat_c,K (holomorphic in z on the ket branch; the bra branch continued
     through the adjoint so that G_K(z,w) := omega( Ktilde(w) K(z) ) with
     Ktilde(w) := [K_pointer(conj(w))]^dagger is jointly holomorphic and
     restricts to z_c,K on real pairs). (H1) subsumes volume-uniform B0.

(H2) [= battery T7(iii); uniform intensive bound + density convergence]
     With the log branch anchored at Ghat_K(0,0) = 1 (well-defined on the
     simply connected polydisc by (H1)), the intensive logs
        gamma_K := -Log Ghat_K / N_4(K)
     satisfy: (i) sup_K sup_P |gamma_K| <= Gamma_* < infinity; and
     (ii) gamma_K converges pointwise on a subset of P with an accumulation
     point in each variable, to a limit gamma_inf.
```

### S5.2 Schema conclusion (derived here, conditionally)

**Theorem schema (conditional).** Assume (H1) and (H2), plus C1 and C2
uniformly along the exhaustion. Then, by Montel (uniform boundedness on P)
and Vitali-Porter (pointwise convergence on a set with accumulation points),
`gamma_K -> gamma_inf` locally uniformly on the open polydisc; `gamma_inf`
is jointly holomorphic; and by the Cauchy integral representation of
derivatives on a polydisc contour strictly inside P, every mixed partial
derivative converges. In particular the interchange of the intensive limit
with the Hessian is VALID:

```text
lim_K  [ d_+ d_- (-log Ghat_K) / N_4(K) ] |_(0,0)
  =  d_+ d_- (-gamma_inf) |_(0,0),
```

and, via FK-2 transported term-by-term (each of `H_att,K`, `g_(D,c,K)`,
`(log q_K)''` is a second derivative of the same uniformly controlled
family), the intensive attenuation Hessian equals the thermodynamic
conditioned Duhamel covariance minus the intensive diagonal correction:

```text
R_record = lim_K H_(att,K)/N_4(K)-normalized objects
         = lim_K g_(D,c,K)-density  -  (1/2) lim_K (log q_K)''-density,
```

with all three limits existing and the equality exact. The identification
of `lim_K g_(D,c,K)`-density with the V011 `G_L`-computed Duhamel covariance
is IMPORTED through interface I3 (Route B-L5 cluster resummation under the
item-3 majorant) and is NOT proved by this schema.

**Proof obligation.** The executor's formal lane must write the
Vitali-Cauchy argument in full on the stated hypotheses (this is classical
finite-complex-analysis material and must be derived, not cited), verify
the branch-anchoring step uses only (H1) and simple connectivity, and
verify that no step uses clustering, decay, or any property of the actual
parent beyond (H1)/(H2)/C1/C2.

### S5.3 Adoption fence (hard)

Any attempt, anywhere in the execution, to DISCHARGE (H1) or (H2) — by
assumption, by citation of exhaustive-chain zero-freeness (F2.1), by
citation of the periodic zero-free lineage (F2.5), by adoption of
`BUFFERED_EXHAUSTION_CLUSTERING` or ANY clustering principle, or by any
"physically reasonable" interpolation — is

```text
CLUSTERING_ADOPTION_ATTEMPT_BLOCKED
```

and escalates to Brian by name. The GHZ control (S6.1) exists precisely to
prove such hypotheses can genuinely fail.

### S5.4 Item-3 interface I3 (named identically to the majorant memo)

This specification names, and does not consume, the majorant interface:

```text
I3 = { epsilon_*, delta, Gamma_*, eta(epsilon_*) with cluster sums bounded
      by eta^n; per-cell activities in ACTION-DENSITY form; uniform over
      (i) the relayed causal exhaustion, (ii) the declared cellulation
      families, (iii) common refinements, (iv) the per-cell polydisc
      |a| <= epsilon_*, (v) per-cell independent source assignments;
      every cell at full tau_R = pi/sqrt(2);
      LEMMA 0 (relayed multi-cell composition identity) as the majorant
      spec's first numbered lemma }.
```

**Interface typing delta (flagged loudly for the majorant spec and the
hostile lane).** The majorant memo's remark that the diagonal-baseline
problem is "discharged BY TYPE on the exhaustive object" (`R_all(a,a)=I`)
does NOT transport to the completed chain: the completed diagonal is
`q_K(a)`, not 1, and its non-vanishing is exactly (H1)/B0. The majorant
spec must therefore supply (H1)/(H2) FOR THE COMPLETED NORMALIZED
FUNCTIONAL `Ghat_K`; exhaustive-typed versions do not satisfy this
interface. Both specs must cite this paragraph so they name the same
interface with the same typing.

## S6 - Mandatory negative controls (all blocking unless labeled otherwise)

1. **cos(N tau_R A) non-clustering witness — must BLOCK.** Instantiate the
   sealed witness family `Z_N(A) = cos(N tau_R A)` with first zero
   `pi/(2 N tau_R)` (cite `f891d3af...`). The (H1)-checker must return
   `H1_VIOLATED` (no volume-uniform zero-free polydisc exists), and the
   schema must REFUSE — emitting BLOCKED with the named violated hypothesis
   — rather than produce any limit. Producing a limit on this witness is a
   pipeline falsifier. Per the majorant memo, the refusal must name the
   violated structural premise (independent record colors; GHZ is the
   perfectly-correlated-color limit).
2. **V010 zero-stiffness stays failed.** Reproduce
   `kappa_L = 1/[4 L^4 sin^2(pi/L)] -> 0` (V011 lineage; battery T6).
   Failed stays failed; any post-hoc extensive factor is a fail.
3. **Contact-term-omission / wrong-ordering variant — must produce a
   certified mismatch (teeth).** Three predeclared broken variants, each of
   which MUST fail detectably:
   (a) CONTACT OMISSION: on the sealed three-site fixture (non-affine
   generator, `d^2 h/d theta^2 != 0`), compute the diagonal correction
   `(log q)''(0)` and `H_att` via the second-order Duhamel expansion WITH
   and WITHOUT the contact term of S3.3. The omission variant must mismatch
   the independent central-difference stencil by more than `3x` the
   enclosure radius, and the certified enclosure of the observed
   discrepancy must intersect the independently computed enclosure of the
   state-evaluated contact term itself (the mismatch must be the contact
   term, at its expected second order — not noise).
   (b) WRONG ORDERING: swap the pinned Wightman ordering
   (evaluate `omega(V[A_+]^dagger Q V[A_-])` in place of the S1.3 form) on
   the frozen asymmetric history pair `(7/100, -11/100)`; the
   first-derivative phase `Im<X_0,eta>` must flip sign and the FK-2
   cross-check must certify a mismatch.
   (c) WRONG FORM (F2.2 enforcement): apply the exhaustive flat-diagonal
   attenuation stencil (no diagonal correction) to the completed
   functional on the three-site fixture; it must mismatch `g_(D,c)` by
   more than `3x` the enclosure radius whenever the certified
   `(log q)''`-enclosure excludes zero.
4. **Route-1 falsifier re-execution.** Re-execute the sealed O6 pattern
   unchanged (frozen pairs `(7/100,-11/100)`, `(13/100,4/100)`): the
   completed component and the exhaustive kernel must match their sealed
   closed forms; the all-outcome sum may not be substituted for the
   completed component. Additionally verify the S4(ii) exact endpoint
   anchors on the comparator in exact arithmetic.
5. **Degenerate-endpoint control.** On the Route-1 comparator the mixed
   conditioned covariance must be EXACTLY zero (`g_c = 0`, rank-one
   compression, exact arithmetic) while `H_att,c = 1/4` — proving the
   pipeline distinguishes the mixed and attenuation objects and cannot
   silently conflate them.
6. **Per-state and intersection discipline (standing).** Every enclosure is
   computed per pinned state; the only numerical pass criterion anywhere is
   nonempty intersection of independently produced certified enclosures
   (S7); a preferred-lane tiebreak is a fail.

## S7 - Machine-verification obligations on the sealed finite anchors

Frozen fixtures: (F-A) the three-site regulator fixture exactly per the
sealed O7 conventions of `2f2aa7f7...` (3 sites, Dirac 4, two qutrit record
factors, 96 midpoint steps per pulse, integrated write action `pi/sqrt(2)`,
completed subset `{(1,1)}`, frozen histories/pairs, frozen `G_S`, epsilons
`{2^-8, 2^-9, 2^-10}`, IEEE-754 binary64/complex128); (F-B) the Route-1
one-dimensional comparator per O6/D6; (F-C) the Phase-A one-particle-sector
objects per `789338ad...` A2-A5 with BOTH pinned states, used only as
already sealed (no Phase-A production run is required or permitted by this
gate).

```text
M1  Hash-verify every frozen authority and this specification; atomic
    AUTHORITY_MISMATCH_BLOCKED on any mismatch.

M2  O5 epsilon-halving regression (F-A): recompute the plus/minus Duhamel
    derivative comparisons for Q = I_R and Q = Q_comp; the finest relative
    error must be <= 2e-5, strictly decreasing, factor >= 2 per halving;
    and the recomputed finest values must reproduce the sealed independent-
    verifier anchors to all printed significant figures:
        plus/all  = minus/all  = 8.19415e-8
        plus/comp = minus/comp = 6.69468e-8      (76f5505e...).

M3  Conditioned crosscheck, re-typed from 3d86dc4f...: independently coded
    RK4 (unsplit; 200 and 400 steps per cell; bit-mask CAR conventions per
    f5302ee8... where the CAR lift is needed) integrating
        dot psi = -i H_0 psi;  dot eta = -i H_0 eta - i J psi
    then forming the CONDITIONED quantities X_0, eta (completed
    compression), q, g_(D,c) by FK-1. Enclosure radius per the sealed
    convention: radius = |g(400) - g(200)|/3 + 1e-8, applied likewise to
    q, (log q)'', H_att, g_all, g_r, and every L-ADD residual. Pass
    requires, per fixture and per state:
      (i)  the certified q-enclosure excludes 0 with relative margin
           >= 10^3 (division-safety fence; else BASELINE_ZERO_BLOCKED);
      (ii) predeclared positivity: on F-A, lower(g_(D,c) interval) > 0
           (completed member non-scalar, sealed witness); on F-B,
           g_(D,c) = 0 exactly (exact arithmetic, no enclosure);
      (iii) FK-2 intersection: the {H_att} enclosure and the
           {g_(D,c) - (1/2)(log q)''} enclosure intersect;
      (iv) FK-3 intersection: the FS-pullback enclosure of the completed
           output ray intersects the {g_(D,c)} enclosure;
      (v)  every independently produced enclosure pair for the same scalar
           intersects (intersection-of-enclosures only; no tolerance may
           be widened in response to any value).

M4  Exact-arithmetic anchors (F-B; decidable, therefore exact — rational/
    symbolic only, no floats): g_c = 0; (log q_c)'' = -1/2; H_att,c = 1/4;
    g_all = 1/4; H_att,all = 1/4; g_all - g_c = ||eta_r||^2 = 1/4; L-ADD
    endpoint identity; sigma-witness reproduction (1/4, 1/4, 1/2, 0)
    against e12fffcc...

M5  L-ADD on F-A: independent computation of q, g_all, g_c, g_r, alpha_c,
    alpha_r with enclosures; the L-ADD residual enclosure must contain 0.

M6  All S6 controls, each with its predeclared direction of failure.

M7  Formal lane: symbolic re-derivation of FK-1, FK-2, FK-3, L-ADD, the
    C1/C2 verifications of S2, and the full Vitali-Cauchy schema proof of
    S5.2, executed on the sealed definitions with no numerical input.
```

The independent verifier must recompute the decisive identities and
enclosures without importing the executor as a module, per the sealed
verifier-protocol pattern (`f5302ee8...`). Executor and verifier read only
this specification and the frozen authority allowlist, emit exact-theorem
fields separately from numerical-regression fields (every tolerance,
residual, and decision), emit an atomic BLOCKED on any startup exception or
failed check, and attest that no coupling, kappa_record, or alpha target was
read.

## S8 - Verdict rule (predeclared)

Return, jointly, only if S2-S7 all pass:

```text
T7IV_COMPLETED_CHAIN_FINITE_CONDITIONED_IDENTITY_DERIVED
T7IV_CONDITIONAL_INTERCHANGE_SCHEMA_SEALED
LADD_HESSIAN_MIXING_LEMMA_SEALED
```

Otherwise return the applicable named block, each with its witness:

```text
AUTHORITY_MISMATCH_BLOCKED
C1_AFFINE_TANGENT_LEMMA_BLOCKED          (named violating generator)
C2_BOUNDARY_HISTORY_INDEPENDENCE_LEMMA_BLOCKED  (named violating datum)
BASELINE_ZERO_BLOCKED                    (named fixture/state)
IDENTITY_MISMATCH_BLOCKED                (named identity and enclosures)
CONTROL_FAILURE_BLOCKED                  (named control)
CLUSTERING_ADOPTION_ATTEMPT_BLOCKED      (escalates to Brian)
```

The C1/C2 hypothesis-lemmas may individually block without invalidating the
other derivations; a partial result must still report every completed
obligation and every named witness.

## S9 - Frozen predictions (calibration; recorded before execution)

```text
1. FK-1, FK-2, FK-3 and L-ADD DERIVE cleanly — they are short algebra on
   sealed finite objects; predicted verdict components: all three pass.
2. C1 DERIVES on the Phase-A generator (affine by the sealed A2 form); the
   V011 link-holonomy caveat remains an open falsification channel, not a
   block of this gate. C2 DERIVES, including envelope clause (d).
3. The contact-term-omission control S6.3(a) PRODUCES its certified
   mismatch at the expected (second) order: the three-site generator's
   curvature term is nonzero and exactly computable, and the discrepancy
   enclosure will match the contact-term enclosure.
4. The GHZ witness BLOCKS naming (H1); V010 stays failed; the Route-1
   re-execution matches its sealed closed forms; the M4 exact anchors hold
   identically (0, -1/2, 1/4, 1/4, 1/4, 1/4).
5. The M2 regression reproduces 8.19415e-8 / 6.69468e-8 with factor-~4
   halving as sealed.
6. No flag currently false becomes true except the three verdict flags of
   S8; in particular (H1)/(H2) remain underived and
   Duhamel_intensive_Hessian_equality_proved remains false ON PASS.
```

## S10 - Out of scope (no promotion)

Piece 3 of the route memo — instantiation on the actual parent and its
numerics — is explicitly OUT OF SCOPE and parked behind the Phase-A bundle,
the Phase-B scalar, and relay items T7(-1)/(0)/(i) including the residual
`F(S,chi,beta,sigma,I)` fork resolution, per the route memo's disposition
and the amendment's A5 no-promotion list, which this specification inherits
verbatim: nothing here yields a universal primitive scalar amplitude, an
interacting continuum CTP amplitude, a volume-uniform zero-free
neighborhood, a connected linked-cluster density, an unconditional
Duhamel/intensive-Hessian equality, ER-A/ER-B closure, kappa_record, an
electromagnetic coupling, or alpha. The L1 per-cell-to-K multi-cell lemma
(disposition 5.3) is a separate named obligation, not consumed and not
discharged here. The A4(3) erratum of the typing freeze remains PROPOSED
pending Brian's sign-off; this specification is governed by the freeze note
and does not cite the amendment's literal A4(3) for any sector claim.

## Protected status

```text
completed_chain_finite_conditioned_identity_derived = false
conditional_interchange_schema_sealed = false
additive_hessian_mixing_lemma_sealed = false
C1_affine_tangent_lemma_derived = false
C2_boundary_history_independence_lemma_derived = false
volume_uniform_zero_free_neighborhood_proved = false      # (H1): named underived input
connected_linked_cluster_density_proved = false           # (H2): named underived input
Duhamel_intensive_Hessian_equality_proved = false         # remains false even on pass
A4_3_erratum_sealed = false
ER_fork_closed = false
kappa_record_computed = false
physical_Thomson_stiffness_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
