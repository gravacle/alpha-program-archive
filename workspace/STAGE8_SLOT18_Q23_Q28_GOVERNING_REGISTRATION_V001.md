# Stage 8 Slot-18 Q-23/Q-24/Q-25 with Q-27/Q-28 Superseding Registration v001

Date: 2026-07-30

## Controlling result

**MISSING SPECIFICATION.** Q-27 supersedes Q-23's open state by identifying
`unused` with the sealed historical-lineage clause. Q-28 then adds a third
admissibility clause and rules the electron magnetic-anomaly branch—the only
family with a frozen formula payload—the clearest failure of that clause.
Five families still lack a formula-level comparator payload; the sixth is
refuted as a Slot-18 candidate. Zero of the six current families satisfies all
three conditions today. The eligibility stage has not been executed; an empty
eligible set, if that stage returns one, fails closed. This is a present-state
screen, not a hard proof against every future observable. The per-family
states are recorded at
`STAGE8_SLOT18_COMPARATOR_PREREGISTRATION_V001.md:218-233`.

The preceding bounded inventory's then-current verdict block at
`STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md:873-889` states:

```text
RETURN_VERDICT = MISSING SPECIFICATION
reason_1 = UNUSED_PREDICATE_UNDEFINED_IN_BOUNDED_SEALED_GOVERNING_TEXT
...
hard_impossibility_proved = false
prediction_derivable_now = false
```

That Q-23 defect is retained here as history, not as the controlling result.
Q-27 performs the principal identification a construction lane was not
authorized to make. Q-25's masking disposition and permanent qualification
remain controlling and unchanged.

This record registers the later supervision rows into the cleanroom governing
chain. It does not use a relay paste as authority.

## External authorities admitted by path and hash

The cleanroom external-citation rule is
`STAGE8_AUTHORITY_CHAIN_PRECEDENCE_BINDING_V001.md:18-26`:

```text
The cleanroom chain governs all procedure.
...
Any such artifact must be cited by
path and hash on the face of the cleanroom artifact consuming it.
```

The admitted supervision records are:

```text
/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md
SHA-256: 2bf6a21f4d70d63a85e3f11d347a21954143dd7a2b3301cc2ab49a7dde0098bb

/Users/bgm/MB Work/alpha_supervision/A32_MASKING_DISPOSITION_PRINCIPAL_DECISION_2026-07-30.md
SHA-256: d7153b91039974af15ab88fa6698e0573a0113fa826aa4f4ba9651b2277467bc

/Users/bgm/MB Work/alpha_supervision/RESULT_A32_MASKING_AND_UNUSED_UNDEFINED_2026-07-30.md
SHA-256: be40963583b368b65d2be9b1453d3c22a24addc3577adf7f21bd5a0c681cadde

/Users/bgm/MB Work/alpha_supervision/SLOT18_ADMISSIBILITY_PRINCIPAL_DECISION_2026-07-30.md
SHA-256: a132f4b2421610c7df4e9a8746286999b31672f1f2d805588ed3f1ad81ad6259

/Users/bgm/MB Work/alpha_supervision/FINDING_D_TEST_DEGENERATE_ON_PRECISION_OBSERVABLES_2026-07-30.md
SHA-256: 34f9f9658f9aae1632de23ba470e0de8c82eb91666c50e91f2d8e516568d7f4a
```

Lane evidence consumed:

```text
STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md
SHA-256: 3a1a9d985d518937821cdd2af6c6a33fd4878969eaddae44b036ab0d29a37b0a
```

The question-register hash is the observed append-only state containing Q-23
through Q-28. Its precedence rule at `:23-24` is:

```text
APPEND-ONLY. Rows are never edited, only superseded by a later row citing the earlier one.
```

A later register row therefore controls if that file advances.

## Q-23 registered historically; Q-27 supersedes its open state

The sealed obligation itself is
`STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-139`:

```text
Open `Q_spec` slots
...
and one unused structure-sensitive prediction.
```

The sealed operational condition is
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2080-2089`:

```text
be absent as an outcome or target from the complete historical lineage;
...
and be structure-sensitive: BID and at least one preregistered comparator
calibrated to the same alpha must make distinct predictions.
```

The obligation and defect are recorded at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:1004-1018`:

```text
RULING: NO — SLOT 18 IS HALF-POSED. "STRUCTURE-SENSITIVE" IS DEFINED; "UNUSED" IS NOT.
...
nothing equates that with "unused" or supplies a machine-testable predicate.
...
Defining it is a PRINCIPAL ACT
```

The consequence is explicit at the same authority, `:1013-1015`:

```text
slot 18 is the mechanism that would EARN the induced-only axiom that
alpha's conditionality equals. An earning mechanism with an undefined
admissibility condition cannot be relied on to earn anything.
```

Q-23's closure condition was `:1033-1034`:

```text
the principal defines "unused", or rules that the "absent from the complete
historical lineage" condition IS the definition and records that identification.
```

The Q-25 principal decision then preserved that state at
`/Users/bgm/MB Work/alpha_supervision/A32_MASKING_DISPOSITION_PRINCIPAL_DECISION_2026-07-30.md:64-68`:

```text
Does NOT define "unused" — Q-23 remains open
...
Does NOT seal the holdout universe, and does NOT convert A32 from ATTAINABLE to attained.
```

The later question-register row at `:1162-1168` now controls:

```text
RULING: `unused` IS the sealed clause at `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2080-2089` —
"be absent as an outcome or target from the complete historical lineage"
...
Supersedes Q-23's open state.
```

This lane consumes that identification. It does not invent a new predicate or
declare any family admissible.

## Q-24 registered as the historical finding

Q-24's correct historical type is not "violation proved." The register says at
`:1042-1043`:

```text
COMPLIANCE IS NOT ESTABLISHED ON TWO SPECIFIC POINTS. THE HOLDOUT IS NOT SHOWN COMPROMISED.
```

The current cleanroom rule 9 occupies
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2005-2010`:

```text
Custodian commitments: independent custodian, one 256-bit salt per
candidate from auditable external randomness or hardware entropy.
Payload = RFC-8785 JSON of canonical outcome value, uncertainty, covariance, units,
source identifier. Commitment =
SHA256(salt || RFC8785(canonical_outcome_payload)).
Custodian attests that no payload was disclosed.
```

This corrects only the line range: supervision describes rule 9 as
`V011:2008-2011`, but current V011 line 2011 begins selection rule 10.

Q-24 identified:

1. no evidence establishing process independence or an independent
   attestation; and
2. an apparent covariance-field mismatch between rule 9 and the scalar
   collector.

The historical findings are verbatim at the question register `:1050-1066`:

```text
(a) NO INDEPENDENT CUSTODIAN.
...
(c) NEW — THE COMMITTED PAYLOAD OMITS COVARIANCE
...
Two innocent readings
exist — all observables scalar, or covariance carried elsewhere — and neither is recorded.
```

The numeric-looking definition-suffix warning remains separately typed and
unadjudicated. Lane evidence
`STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md:393-405` says:

```text
The candidate text and suffix are not reproduced here. No inference was made
about what the suffix represents. Inspection stopped under the turn fence.
This is a masking-assurance warning ... not a measured-value finding.
```

Q-25's evidence check was source-only. The principal decision says at
`:28-32`:

```text
THE CHECK, RUN 2026-07-30 ON THE COLLECTOR SOURCE ONLY, NO CANDIDATE CONTENT READ
```

A bounded exact-string check over only
`/Users/bgm/MB Work/alpha_supervision/A32_MASKING_DISPOSITION_PRINCIPAL_DECISION_2026-07-30.md`
for `suffix`, `numeric-looking`, and `candidate-definition` returned this hit
file list:

```text
(empty)
```

Root: that one principal-decision file. Exclusions: every other supervision
file, every cleanroom file, every holdout file, and the forbidden custodian
directory. The check supports only the bounded statement that Q-25 does not
dispose of the suffix warning; it does not turn the warning into either a
compromise finding or a cleared finding.

## Q-25 registered as the controlling masking disposition

The question register states at `:1083-1085`:

```text
Q-25. "How was A32's masking disposed?" — SUPERSEDES Q-24's OPEN DISPOSITION
RULING, TWO PARTS. PRINCIPAL ACT, 2026-07-30.
```

### Standing custodian qualification

The controlling principal decision says at `:15-19`:

```text
The commitments are NOT withdrawn and the holdout is NOT re-collected.
THE LIMITATION IS PERMANENT AND
MUST TRAVEL WITH EVERY A32 HEADLINE
...
process independence between
collector and custodian was never established, and no independent attestation exists.
```

The terminal inheritance is explicit at `:57-62`:

```text
The cost of the decision is that the independence limitation is
now permanent and inherited by FINAL-CLAIM.
```

**Standing qualification to reproduce with every A32 headline and
FINAL-CLAIM:** concealment is accepted as cryptographically sound, while
process independence was never established and no independent attestation
exists. This is a permanent qualification, not a footnote.

### Covariance disposition

The controlling decision says at `:35-40`:

```text
THE SOURCE FORMAT IS A FIXED-COLUMN TABLE CARRYING NAME, VALUE AND UNCERTAINTY. IT HAS NO COVARIANCE TO
COMMIT.
...
The commitment binds everything the source supplies.
...
NO RE-COMMITMENT IS REQUIRED.
```

Thus Q-24's covariance concern is closed by evidence for the current
fixed-column source, which supplies no covariance. This record does not infer
that every future eligible candidate must be scalar, and it does not
independently inspect candidate content or the collector.

### Binding forward condition

The controlling decision says at `:43-49`:

```text
THIS COLLECTOR CANNOT COMMIT A MULTI-COMPONENT OBSERVABLE.
...
if the eligible universe is ever extended to vector or multi-component observables,
the collector must be revised and those candidates re-committed BEFORE any prediction exists.
```

The condition is live because
`provenance/bid_stage_subjects_v011.json:22-24` says:

```text
No sealed HOLDOUT-UNIVERSE-SEAL immutable subject exists
...
"status": "UNRESOLVED"
```

No multi-component extension is authorized or performed here.

The Q-25 reopen condition is
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:1110-1111`:

```text
the eligible universe is extended to covariance-bearing candidates, or an external review
rejects the recorded custodian limitation as insufficient for a published claim.
```

## Fence clarification for `D`

The question register records at `:1025-1026`:

```text
and **`D` is NOT a comparison to a measured central value** — measurement enters only through
uncertainty/covariance, so computing `D` does not breach the no-comparison fences.
```

The cleanroom formula is
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2052-2058`; it places the two
theory predictions in the numerator and measurement uncertainty in the
denominator. The source of that uncertainty is fixed separately at
V011:2025-2028:

```text
measurement uncertainty/covariance from the custodian commitment payload,
uneditable after commitment.
```

This record does not evaluate that expression.

## Q-27 registered as the controlling `unused` identification

The later principal decision says at
`/Users/bgm/MB Work/alpha_supervision/SLOT18_ADMISSIBILITY_PRINCIPAL_DECISION_2026-07-30.md:12-27`:

```text
RULED: `unused` IS the condition already sealed at
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2080-2089` — "be absent as an outcome or target from the
complete historical lineage."
...
THE TEST IT LICENSES: a bounded search over the rule-11 scope, returning zero occurrences of the
candidate observable as an outcome or a target. Roots and exclusions stated, file lists reported.
```

The operation is an identification rather than a new definition. The same
authority at `:20-24` says:

```text
Both halves are sealed text that has been in the corpus
throughout; what was missing was the statement that they are the same condition.
```

The falsifiers remain live at `:29-35`: an unbounded or unsearchable rule-11
scope returns `unused` to undefined, while later evidence that a candidate
shaped the construction must be escalated. No candidate-specific lineage test
is run in this record.

## Q-28 registered: clause 3 refutes the only formula-payload branch

The controlling third clause is
`/Users/bgm/MB Work/alpha_supervision/SLOT18_ADMISSIBILITY_PRINCIPAL_DECISION_2026-07-30.md:44-56`:

```text
An observable is admissible only if the comparator's own predictive uncertainty is not so far below
any plausible BID deviation that a `D >= 5` difference would be automatically in conflict with
established measurement.
...
IT IS A CONDITION ON COMPARATOR PRECISION, NEVER ON A MEASURED CENTRAL VALUE.
...
APPLIED AT THE ELIGIBILITY STAGE, BEFORE THE BEACON SELECTS.
```

The same authority applies the clause to the surviving formula branch at
`:75-80`:

```text
Family 6 — the charged magnetic form factor at the electron zero-momentum anomaly — is the one family
holding a comparator payload, and it is the clearest failure of clause 3.
...
look for observables that satisfy all three clauses, not to relax the third.
```

The supporting finding distinguishes the present screen from a universal
impossibility result at
`/Users/bgm/MB Work/alpha_supervision/FINDING_D_TEST_DEGENERATE_ON_PRECISION_OBSERVABLES_2026-07-30.md:80-81`:

```text
- **Not** that the eligible set is provably empty. It is empty of families satisfying all three
conditions today, which is a different and weaker statement.
```

This refutes Family 6 as a current Slot-18 candidate; it does not invite a
replacement formula, relaxed threshold, or candidate repair. Five other
families lack a frozen formula-level comparator payload. The resulting
six-family screen has zero families satisfying all three conditions today.
The eligibility stage has not been executed; this is not an executed empty
eligible set or a corpus-wide impossibility proof.

Q-28 preserves the comparison fence. No measured central value is consulted
here, and no comparator uncertainty, theory uncertainty, or `D` is evaluated.

The controlling falsifier is mandatory. The principal decision says at
`:63-64`:

```text
if clause 3 cannot be applied without consulting a measured central value, it
breaches the comparison fence and must be withdrawn.
```

The question-register reopen condition at `:1220-1226` likewise requires a
breach to be reported rather than softened. F-C3 is not tested in this record.

The later principal authority also preserves Q-25 at `:66-73`:

```text
Repair A32's recorded custodian-independence limitation (Q-25), which stands unchanged.
```

## Seal state is unchanged

`provenance/boundary_incidence_dynamics_preregistration_v011.json:651-658`
remains:

```text
"holdout_universe_sealed": false,
"prediction_map_sealed": false,
"alpha_result_sealed": false,
"holdout_result_sealed": false,
"BID_final_claim_sealed": false,
"external_unused_holdout_preregistered": false
```

Q-25 is a disposition and standing qualification; Q-27 identifies an existing
condition; Q-28 adds an eligibility clause. They flip none of those seals.

## Registration verdict and scope

```text
RETURN_VERDICT = MISSING SPECIFICATION
Q23_registered = true
Q24_registered_as_historical_finding = true
Q25_registered_as_controlling_disposition = true
Q27_registered_as_controlling_identification = true
Q28_registered_as_controlling_third_clause = true
Q23_open_state_superseded = true
unused_identified_with_sealed_lineage_clause = true
new_unused_definition_added = false
electron_anomaly_branch_refuted_by_clause3 = true
current_six_family_all_three_condition_screen_nonempty = false
eligibility_stage_executed = false
empty_eligible_set_declared = false
candidate_admissibility_asserted = false
holdout_universe_sealed = false
prediction_attempted = false
candidate_content_read = false
custodian_private_accessed = false
measured_value_comparison_performed = false
significance_evaluated = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
