CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00006927   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 24 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing (Q-913 standard).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001. Every span cites member + line locus. No flag name
  is used as an anchor.
LANE = DARIO   ROLE_THIS_RELAY = CANDIDATE V002 — SIX REPAIRS (relay 1057)
AUTHORITY = member 02, the ATTACH SUPPLY MANDATE, verified LIVE at seal time. Authoring is lawful
  here in exactly the form that mandate types. It ratifies NO CONTENT and none is entered here.
SUPERSESSION = supersedes STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V001.md APPEND-ONLY. V001 IS
  BYTE-UNTOUCHED at 8c257818…, verified before and after; the generator refuses to emit if it moved.
ALL_RESULTS = CLAIMED until the opposite-lane check.
SELF_CITATION_BAR = DARIO's own prior outputs are not record witnesses. V001 (member 03) is a member
  ONLY as the superseded subject of the repairs, never as evidence for any claim.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1057_CANDIDATE_V002_DARIO_V001.md` | `278f8ca939c84b1ffa70ad3e1a18ef6631a1a8c3d8c7739f652585102633cd77` | assignment |
| 02 | `workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | **THE AUTHORITY — verified live** |
| 03 | `workspace/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V001.md` | `8c257818b55c66aef1842024601c51f3a22599a949db7de4280d7763fc9dcdbc` | **V001 — superseded, byte-untouched, subject of the repairs** |
| 04 | `workspace/STAGE8_ATTACH_CANDIDATE_CHECK_CODEX2_V001.md` | `617e0144c0adfad97ec14ae85fbb76efd9787a63f1c0b77b778c9768c747a528` | **the check — the six deltas and seven overclaims** |
| 05 | `workspace/STAGE8_DESC_OPEN_CONDITIONS_SWEEP_CODEX2_V001.md` | `85514b04912f77e9276aca131b41d62d400a3e555e21492eb43a1df53dd5d588` | the six-check acceptance registry |
| 06 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 07 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 08 | `workspace/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | **THE CELL DYNAMICS: first-opening premise, `BareRec_2`, the composition-loop carrier** |
| 09 | `workspace/STAGE8_HOLONOMY_FIRST_OPENING_TENSION_ADJUDICATION_EINSTEIN_V001.md` | `8fbdc8300b28166564e4ba32e6747bc643135e25d674cc37da706de3afbbf019` | **THE SCOPE DETERMINATION — emptiness is a determination; the carrier appears one composition later** |
| 10 | `workspace/STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md` | `ab156ee764db9d0bd48f54f1b879f1bafcfac08b45520ca6c4fb582e48edf572` | the Attach type and absence adjudication |
| 11 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | `d_law` — the ratified codomain. READ ONLY WITH 12 AND 13. |
| 12 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 11 |
| 13 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification; the no-member clause |
| 14 | `workspace/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | holonomy normalization; the `U(1)_rel` bundle; the cell postulate |
| 15 | `workspace/PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` | `aa2ef0e78a1ad975a04596831eef2ea7587b78c4fa9d979ae8a742308cd7c214` | transport-only entry; `q_primitive=1` |
| 16 | `workspace/STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md` | `c49d31200460e96209375f06a7a655d343767695ef09805f1e396d1814833b6b` | the Q-227 baseline; full cell action; `P_0=I-P_ch` |
| 17 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md` | `2be95d5b58ea000df9a30b717a809374a90693f78a893d87b73a12e4be97fa21` | the computed cell endpoint; the SR representation obstruction |
| 18 | `workspace/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | the base-write ordering facts and their conditionality |
| 19 | `workspace/R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_RESULT_V001.md` | `321c52bc3f5cf8d66b2ca4a7f14811a41e905048ef89415f8c76e22837261c58` | **the O4 tail anchor — the member V001 mis-cited** |
| 20 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_SPEC_V002.md` | `4a7600caa23d0c7a98eeef8a79941c20ca4e28a4f5a2c1cf5c2362e88c7d4721` | the charge projector |
| 21 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `res_B`; the AC6 gap |
| 22 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | **the slot identity: `a` denotes connection/holonomy histories** |
| 23 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | P0–P7 surface content |
| 24 | `workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | the nine fields — coherence display only |

```text
COMPUTED IN THIS RELAY (structural integer computations on sealed cell data; no physical quantity):
  b_1(K_(1,3))   = 0    first-opening complex, member 08 :268-276    (|V|=4, |E|=3, rank d_1 = 3)
  b_1(K_square)  = 1    composition carrier,  member 08 :1840-1852   (|V|=4, |E|=4, rank d_1 = 3)
  [D(z), S] != 0 for z != 1, = 0 at z = 1   — the check's intra-cell finding, reproduced

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEMBER BOUND.
NO FIXED-POINT EXECUTION. NO END TEST. NO MEASURED-CONSTANT COMPARISON. NO FREEZE. NO SELECTOR FROZEN.
PE-1..17 POINTER-ONLY, LEDGER SHUT. omega_phys UNTOUCHED. CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)

# STAGE 8 — THE ATTACH CANDIDATE, V002 — SIX REPAIRS
## RELAY 1057 — `[PLAN:DESC-33]` — DARIO LANE — AUTHORED UNDER MANDATE — [CLAIMED]

Date: 2026-08-12
Status: **SUPERSEDES V001 APPEND-ONLY.** V001 byte-untouched.

**THE HEADLINE V001 CARRIED IS WITHDRAWN.** "The minimal authored core is one ingredient" was false.
The check was right: circuit existence, circuit selection and the comparator embedding do not descend
from the bare record-cell postulate. **The core is seven.** That is the honest number and §6 shows how
it is reached.

**WHAT THE CIRCUIT REPAIR FOUND.** The record does not force a loop in every history — and it does not
have to, because **the record already made this a scope determination and wrote it down.** The
first-opening complex is acyclic *on purpose*; the loop-bearing carrier is specified, named and
mandated **one composition later**. The candidate is therefore domain-restricted to a sector the
record types, not to a sector I chose. And on that sector the circuit is **canonical**: `b_1 = 1`, so
there is exactly one independent cycle and **no selector to freeze** — which is the constraint the
assignment placed on me, satisfied by topology rather than by my restraint.

---

## 0. THE SEVEN CORRECTIONS, ACCEPTED

Member 04 `:231-245` lists them. All are accepted; none is re-argued. Two are mine and substantive:

1. **The intra-cell time-ordering inference was WRONG, not merely unsupported.** V001 §4.4 said the
   cell-local phase leaves the sealed intra-cell result intact. It does not. I reproduced the check's
   computation before writing a line of the repair:

```text
D(z) = diag(1,z,1),  S = [[0,1,0],[1,0,0],[0,0,-1]]

D(z)S = [[0,1,0],[z,0,0],[0,0,-1]]        S D(z) = [[0,z,0],[1,0,0],[0,0,-1]]

|| [D(z),S] || != 0 for z != 1 ;  = 0 exactly at z = 1.
```

   **Cross-cell commutation survives; intra-cell triviality does not.** The claim is withdrawn and
   §4 supplies its replacement.

2. **The one-ingredient headline is false**, withdrawn above and recounted at §6.

Accepted without further comment: the representation bridge is absent (§2); the comparator embedding
is not exact (§3); AC5 is a predicate, not a discharged fact (§7.3); the closed claim omitted `J`, `g`
and three maps (§5); and the O4 anchor was mis-cited (§6.2).

**One finding runs in my favour and I record it without leaning on it.** `SLOT_IDENTITY = SAME`, and
supported *more strongly* than V001 stated: member 22 `:160-165` says in its own words that
`a_+, a_-` **denote connection/holonomy histories** and are *not* the discrete endpoint labels. My
"abstract discrete label" wording was too weak, not too strong. And `LEDGER = 13/13-FORCED-CONFIRMED`
— no forced row was secretly authored, so the mandate's void clause does not fire. F01–F13 carry
into V002 unchanged, with F07 and F10 re-scoped as the check requires (§8.1).

---

## 1. REPAIR (1) — THE CIRCUIT: **DOMAIN-RESTRICTED, ON RECORD-TYPED SECTORS**

*Derive-or-display. I did not choose, and I did not freeze a selector.*

### 1.1 The first-opening sector is acyclic, and that is a determination rather than an accident

Member 08 `:252-259` states the first-opening premise, and the operative clause is the
root-incidence one:

> all primitive first-opening arrows begin at the unresolved root; and no endpoint, arrow, loop, or
> composite cell exists at first order without an additional public distinction or a composition.

Member 08 `:262-270` fixes the consequence: the minimal first-opening `1`-complex is the rooted star
`K_(1,r)` with `r = 3`, giving `dim C_0 = 4`, `dim C_1 = 3`.

**Computed here from those integers, not asserted:**

```text
K_(1,3):  |V| = 4, |E| = 3, rank d_1 = 3  ->  b_1 = |E| - rank d_1 = 0.
```

No independent cycle. On the first-opening carrier `gamma_j` **does not exist**, and V7 fires exactly
as V001 admitted it would.

**And this is the record's own ruling, not a defect I am routing around.** Member 09 `:70-77` rules:

> **EMPTINESS IS A DETERMINATION** — the corpus excluded the carrier at first order **on purpose**,
> in the governing spec, in the same paragraph that forces the star.

with `GROWTH_CARRIER_APPEARS_LATER` carried as a subordinate clause of that ruling.

### 1.2 The loop-bearing sector is specified by the record, one composition later

Member 08 `:1836-1852` — read at bytes, and the section's own first line says
*"This is a new forward mathematical consequence, not an empirical holdout"*, which is why reading its
**carrier structure** is lawful here:

```text
the unfilled oriented 1-skeleton K_square with no 2-cell:
  vertices  v_00, v_10, v_01, v_11
  edges     e_a0: v_00->v_10   e_0b: v_00->v_01   e_ab: v_10->v_11   e_ba: v_01->v_11
```

and member 08 `:1858-1863` gives the **ordered loop holonomy** in the record's own notation:

```text
u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi).
```

**Computed here:**

```text
K_square:  |V| = 4, |E| = 4, rank d_1 = 3  ->  b_1 = 1.
```

**Exactly one independent cycle.** Member 09 `:125-130` states the standing plainly: *"THE CARRIER IS
NOT MISSING. IT APPEARS ONE COMPOSITION LATER, BY DESIGN, AND IT IS ON THE CRITICAL PATH"* — mandated
for CORE-RESULT-SEAL at member 08 `:1790`.

**I read the carrier's topology and its holonomy FORM. I did not open, and do not use, the
prediction's value, direction, sign, or any downstream consequence of it.** The candidate needs a
circuit and a `U(1)` phase on it; that is all that is taken.

### 1.3 The restriction, stated

```text
CIRCUIT = DOMAIN-RESTRICTED, on sectors the record types.

DOMAIN OF THE CANDIDATE:  admitted cells with b_1 >= 1 — the composition sector, whose carrier the
   record specifies at member 08 :1840-1852 and mandates at :1790.
   On K_square, b_1 = 1: the circuit is CANONICAL up to orientation.  THERE IS NO SELECTOR TO FREEZE,
   and I freeze none.  Orientation is fixed by F08's reversal law together with the ratified ordering.

DISPOSITION OF TREE INPUTS (b_1 = 0, incl. first opening):  the candidate is INERT.  Trivial holonomy
   gives chi_n = 1, D_n = I, and U_N[A,J,g] = U_N^0.  This is NOT a failure of the candidate: member
   09 :185-192 rules the configurational holonomy freedom ABSENT there, so a map that returned
   anything else would be manufacturing a phase the record determines does not exist.  V7 fires on
   the tree sector, and firing there is the correct behaviour.
```

### 1.4 What this restriction does NOT establish — the residual, displayed

The check's alternative (2) asked for *a sealed class of loop-bearing cells* plus *proof that the
restricted domain is the physical causal-cell domain*. Neither is available, and I say so rather than
blur it:

```text
R1  THE LOOP-BEARING CLASS IS SPECIFIED AND MANDATED BUT **NOT SEALED**.
      member 08 :2321  composition_loop_prediction_sealed = false
      member 09 :304-305 carries the same as TYPE-U.
R2  NO PROOF EXISTS THAT THE RESTRICTED DOMAIN IS **THE** PHYSICAL CAUSAL-CELL DOMAIN.
      member 14 :103-111 requires an explicit causal-cell construction before any response is
      evaluated, and member 14 :168 records that the unique causal record cell is not derived.
R3  ADMISSIBILITY IS NOT UNIVERSALITY.  member 08 :296-304 permits nontrivial face holonomy in
      BareRec_2; that every physical record cell lies in the b_1 >= 1 sector is NOT a theorem, and
      this artifact does not claim it.
```

**R1–R3 are entry-relevant disclosures, not silent holes.** The restriction is lawful in the sense
the assignment defines — the record types the sectors — and it is incomplete in the sense the check
defined, because the loop-bearing class is not yet sealed. Both halves are true and both are stated.

**What I refused to do.** I could have declared the composition sector "the physical domain" and
called the restriction discharged. Nothing at bytes supports that, and it would have converted a
mandated-but-unsealed carrier into a premise by assertion — which is the exact move the mandate's
honesty clause exists to prevent.

---

## 2. REPAIR (2) — THE REPRESENTATION BRIDGE: **AUTHORED**

The check is right that this was absent. Member 08 `:224-239` says the several `U(1)` appearances are
*related but not identified*; member 14 `:47-51` puts the adopted auxiliary connection on the
`U(1)_rel` bundle. **Equality of group names is not an intertwiner**, and no sealed map supplies one.

I therefore **author it and count it** rather than let the claim overreach:

```text
A4 (AUTHORED, NEW) — THE REPRESENTATION BRIDGE

  iota_rep : U(1)_rel -> G_joint,   a homomorphism of the adopted comparison groups,
  subject to    rho_joint o iota_rep = chi_1   on the cell comparator line,

  i.e. the bridge is required to carry the adopted auxiliary connection's unit character (member 15
  :24-31, q_primitive = 1) onto the associated-line transport rho_joint of member 08 :224-239.

  JUSTIFICATION, internal coherence only: without iota_rep the candidate's theta_j(A) — computed from
  the U(1)_rel connection of member 14 — cannot enter the rho_joint-associated transport that member
  08's incidence structure acts through.  The bridge is the minimal datum making the two adopted
  carriers composable.  It is NOT derived, and member 08 :224-239 is explicit that the record
  declines to identify them.
  VOID CONDITION: if a sealed result later identifies or separates these carriers incompatibly with
  iota_rep, this ingredient and everything resting on it fall.
```

---

## 3. REPAIR (3) — THE COMPARATOR EMBEDDING: **EXACT AS A MAP, AUTHORED AS AN IDENTIFICATION**

The check asked for exact linear maps, basis assignment, intertwining law, and the circuit-selection
domain. The map is now written exactly; the *identification* it encodes remains authored.

```text
A5 (AUTHORED, NEW) — THE COMPARATOR EMBEDDING

  DOMAIN: an admitted cell K_j with b_1(K_j) = 1 (§1.3), with its canonical cycle gamma_j and a
    chosen orientation representative (F08 fixes the reversal law; the ratified ordering fixes which
    representative is used — no freedom is exercised here).
  Let s(j), t(j) be the two comparison endpoints gamma_j traverses, with vertex lines L_(s(j)),
  L_(t(j)) — each a one-dimensional Hermitian fibre (member 08 :291-299).

  Fix unit vectors  e_s in L_(s(j)),  e_t in L_(t(j)).  Define the exact linear map

     E_j : L_(s(j)) direct-sum L_(t(j)) -> span{ |r>, |p_Q> } subset K_cell,
     E_j(e_s) = |r>,      E_j(e_t) = |p_Q>,      E_j extended complex-linearly.

  E_j is a linear isomorphism onto the comparator pair, and is unitary for the Hermitian structures.
  It leaves |e_Q> outside its image — correctly, since the edge state is not a compared alternative
  (member 14 :76-82 states the roles: the record carrier supplies the compared alternatives).

  INTERTWINING LAW (the content, not decoration): with U_(gamma_j) the ordered transport of member 08
  :291-299 around gamma_j, the embedding is required to satisfy

     E_j o ( rho_joint(iota_rep(Hol_(gamma_j)(A))) )  =  D_n[a_j] o E_j     on span{ e_s, e_t },

  which is exactly the statement that transporting first and embedding second equals embedding first
  and applying the ratified cell phase second.  This is what makes A1's assignment well typed rather
  than merely suggestive.

  WHY AUTHORED: the assignment of s(j) to |r> and t(j) to |p_Q> — which endpoint is the root
  alternative and which the successor — is not fixed by any sealed span.  Reversing it conjugates
  D_n and is absorbed by the F08 orientation reversal, so the map is well defined up to that
  reversal, but the choice itself is mine and is tabled, not hidden.
```

---

## 4. REPAIR (4) — THE INTRA-CELL TIME-ORDERING CARRIER: **AUTHORED, and the false claim replaced**

### 4.1 What is withdrawn

V001 §4.4 said the phase is cell-local and therefore both sealed ordering facts survive. **The
intra-cell half is false**, per §0's computation. Member 18 `:16-48` proves intra-cell triviality for
the *unphased* base pulse only; the check's receiver limit on F10 is correct.

### 4.2 What survives, unchanged

**Cross-cell commutation survives untouched.** `D_n[a_j] S_j` and `D_n[a_k] S_k` act on distinct
tensor factors for `j != k`, so the endpoint remains independent of the causal linear extension —
member 18 `:39-48`'s content is unaffected by a factor-local insertion.

### 4.3 The authored carrier, and the order it must use

```text
A6 (AUTHORED, NEW) — THE INTRA-CELL SEQUENTIAL CARRIER

  Realize the cell endpoint as TWO temporally disjoint sub-pulses within the cell's own window:

    stage 1   H_j^(1)(t) = v_j(t) B_j ,          integral v_j dt = tau_R
              -> T exp[-i integral H_j^(1)] = S_j            (member 18 :16-34, unchanged)

    stage 2   H_j^(2)(t) = w_j(t) theta_j(A) Q_cell ,        integral w_j dt = 1
              -> T exp[-i integral H_j^(2)] = exp(i theta_j(A) Q_cell) = D_n[a_j]

    composite endpoint = D_n[a_j] S_j .

  THE ORDER IS NOT MINE.  It is read off the ratified law: member 11 :650-654 writes the cell factor
  as `D_n[a_j] S_j`, and under the standing operator convention the rightmost factor acts first.
  Stage 1 therefore precedes stage 2.  Reversing the stages would give S_j D_n[a_j], which §0 shows
  is a DIFFERENT operator — so the ratified law's own ordering is what fixes the carrier's sequence,
  and F01 carries it.

  PROFILE INDEPENDENCE, PRESERVED IN CORRECTED FORM: each stage has a generator fixed throughout its
  own sub-window, so [H^(i)(t), H^(i)(s)] = 0 within each stage and each stage's time-ordered
  exponential is profile-independent by member 18 :23-34's own argument.  The composite is therefore
  profile-independent GIVEN THE ORDER.  What is NOT recovered is intra-cell order-independence: the
  two stages do not commute, and the candidate does not claim they do.

  WHY AUTHORED, not forced: no sealed span supplies a two-stage intra-cell decomposition, a second
  generator proportional to Q_cell, or the sub-window structure.  This is new physical content.
  VOID CONDITION: if the derived causal parent (member 18 :77-101, still underived) realizes the cell
  write in a form incompatible with a two-stage sequence, A6 falls.
```

### 4.4 Consequence, stated

**The candidate now requires more of the parent than V001 admitted.** The parent must supply not only
a one-use moving front but a *two-stage* front with the ratified order. That is a strictly stronger
demand on an object that member 18 `:77-101` says is not derived. It is recorded here as the price of
repair (4) rather than left for the check to find.

---

## 5. REPAIR (5) — THE CLOSED CLAIM, WITH EVERY SYMBOL TABLED

The five symbols the check found outside both tables now enter **exactly one table each**:

```text
J   -> A7 (AUTHORED)   g   -> A8 (AUTHORED)
representation bridge  -> A4 (AUTHORED, §2)
comparator embedding   -> A5 (AUTHORED, §3)
physical time-ordering -> A6 (AUTHORED, §4)
```

```text
A7 (AUTHORED, NEW) — THE DECLARED J-RESTRICTION
  The candidate's dependence on the source history J is EXACTLY the ratified charge control: J enters
  through P_ch (F04) and through nothing else.  This is a restriction I declare, not a dependence I
  derive; a candidate with genuine source-history dependence beyond the charge sector is not supplied.
  VOID: a sealed result requiring J-dependence beyond the charge sector.

A8 (AUTHORED, NEW) — THE DECLARED g-RESTRICTION
  The candidate's dependence on the geometry g is EXACTLY through the cell complex that carries
  gamma_j: g fixes which cells are admitted and their b_1.  No metric datum enters D_n or theta_j
  other than through the circuit.  Declared, not derived.
  VOID: a sealed causal-cell construction whose g-dependence enters the write directly.
```

### 5.1 The closed claim, restated and re-invited

> **Every ingredient of the construction at §9.1 appears in exactly one of the two tables of §6 —
> the FORCED table (F01–F13) or the AUTHORED table (A1–A8). There is no third category, no
> narrative-only ingredient, and no symbol in the displayed codomain `U_N[A,J,g]` that is absent from
> both.**

**I re-invite the test in the sharper form the check used to break the last one:** take every symbol
appearing in §9.1 — including `J`, `g`, `rho_joint`, the embedding, and the physical evolution — and
find one that is in neither table. The previous claim died to exactly that procedure and the
procedure was right. If it dies again, it dies for a reason I would want to know.

---

## 6. REPAIR (6) — THE RECOUNT, AND THE ANCHOR

### 6.1 The recount

```text
V001 CLAIMED:  "the minimal authored core is ONE ingredient."   ** FALSE — WITHDRAWN **

V002 COUNTS:
  A1  the holonomy identification  a_j := theta_j(A)                     NEW
  A2  the circuit and its domain restriction (§1)                        NEW — the check is right
        that this does NOT descend from the bare cell postulate
  A3  n = +1                                                             already adopted elsewhere;
        no new authored content added to the program's premise set
  A4  the representation bridge iota_rep (§2)                            NEW
  A5  the comparator embedding E_j (§3)                                  NEW
  A6  the intra-cell sequential carrier (§4)                             NEW
  A7  the declared J-restriction (§5)                                    NEW (a scope decision)
  A8  the declared g-restriction (§5)                                    NEW (a scope decision)

CORE_COUNT = 8-AUTHORED, of which 7 are new to the program's premise set.
```

**The headline changes accordingly and says so.** The candidate is not a one-ingredient supply; it is
a seven-ingredient authored core resting on thirteen confirmed forced rows. That is a materially
weaker object than V001 advertised, and the count is the honest measure of it.

### 6.2 The O4 anchor, recited correctly

```text
WRONG (V001 :445-451):  "O4 write/tail join and analyticity — member 11 (R3.4 tail) :36-41"
   In V001's own numbering, member 11 was FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md.
   The anchor pointed at the wrong file.  The open condition was true; the citation was not.

CORRECT (this artifact's numbering):
   O4  write/tail join and analyticity —
       member 19, `workspace/R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_RESULT_V001.md`, :36-41,
       where the gate records that the covariance equation is not forced as the physical write/tail
       joining law and the physical join is not derived.
```

---

## 7. THE REGISTRY, CARRIED

### 7.1 The two FAIL items, addressed

```text
AC-2  representation  ->  ADDRESSED BY A4 (§2), as AUTHORED rather than met.
      V001 claimed the representation requirement met; that claim is WITHDRAWN.  The bridge is now
      supplied as authored content with a void condition.  The SR representation obstruction
      (member 17 :108-128) is untouched by this and remains open as O2.

AC-3  causal parent / time-ordering  ->  ADDRESSED BY A6 (§4), as AUTHORED rather than inferred.
      The false intra-cell inference is withdrawn; the carrier is authored with the ratified order;
      the moving-front antecedent stays open (member 18 :77-101) and the demand on the parent is now
      STRONGER, per §4.4.
```

### 7.2 The two NOT-YET items, stated as awaiting the completed object

```text
AC-4  write/tail analyticity      NOT-YET-TESTABLE — awaits the parent-derived physical join and the
                                  tail test (member 19 :36-41).  No discharge is claimed.
AC-5  connected continuum preparation
                                  NOT-YET-TESTABLE — awaits connected preparation, continuum
                                  ordering, and the completed common-origin object.
```

### 7.3 AC5 and AC6 — open and honest

**AC5 is a predicate, not a discharged fact**, exactly as the check ruled: co-membership of `A` and
`R` in `Q_spec` is naming, not descent, and member 14 `:83-95,168-169` records that the complete
`Q_spec` is not frozen. V001's AC5 wording is corrected to a predicate.

**AC6 stays open.** The marginal binding needs a joint state on the completed object; member 21
`:209-221` shows `res_B` consumes an `Omega_C0 in State(A_C0)` that no construction supplies. It is
a named gap, not an asserted clause.

---

## 8. THE LEDGER

### 8.1 FORCED — F01–F13 carried, with the two re-scopings the check requires

All thirteen were independently re-derived and CONFIRMED at their spans (member 04 `:137-154`). Two
carry the check's scope limits, and they are written into the rows rather than left in a footnote:

```text
F07  loop holonomy is not vertex-gauge-removable; tree phases are.   member 10 :219, :263-266
     SCOPE (check): this does NOT prove a loop exists in the candidate cell.  §1 is where existence
     is handled, and it is handled by domain restriction, not by F07.
F10  time-ordering.   member 18 :16-48
     RECEIVER LIMIT (check): proves intra-cell triviality and inter-cell commutation FOR THE UNPHASED
     BASE PULSE.  The candidate inherits the INTER-cell half only; the intra-cell half does not
     transfer and is replaced by A6.
```

F01–F06, F08, F09, F11–F13 carry unchanged.

### 8.2 AUTHORED — A1–A8, per §6.1

Each carries its justification (internal coherence only) and its void condition in the section that
introduces it. **No authored ingredient is presented as forced.** Where doubt existed — A7 and A8 are
scope decisions rather than physical posits — the label still went to AUTHORED.

---

## 9. THE CANDIDATE, RESTATED

### 9.1 The construction

```text
DOMAIN: admitted cells K_j with b_1(K_j) >= 1 (§1.3); on the record's specified carrier b_1 = 1 and
        the cycle gamma_j is canonical up to the F08 orientation reversal.

  theta_j(A)   := contour-integral_(gamma_j) A                        [A1; A2 supplies gamma_j]
  Hol_(gamma_j)(A) := exp( i theta_j(A) )                              [F05, verbatim rule]
  chi_n(a_j)   := ( rho_joint o iota_rep )( Hol_(gamma_j)(A) )^n       [A4 supplies iota_rep]
  Q_cell       := |p_Q><p_Q|                                           [F02]
  D_n[a_j]     := diag(1, chi_n(a_j), 1) = exp( i n theta_j(A) Q_cell )
  E_j          : comparator embedding, intertwining as in §3           [A5]
  cell endpoint: stage 1 then stage 2, endpoint D_n[a_j] S_j           [A6; order from F01]
  W_N^(n)[A]   := tensor_(j=1)^N ( D_n[a_j] S_j )
  U_N[A,J,g]   := P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[A],  n = +1 [A3]
                  J-dependence exactly F04 [A7];  g-dependence exactly through gamma_j [A8]
```

### 9.2 Zero-source reduction — unchanged and still exact

At `A = 0` the holonomy is `1`, so `chi_n = 1`, `D_n = I`, and `U_N[0,J,g] = U_N^0` (member 16
`:158-169`; member 11 `:761`). **The tree sector reduces to the same object** — which §1.3 explains
is the correct behaviour there rather than a degeneracy.

### 9.3 The five typed requirements, restated honestly

```text
representation        A4 AUTHORED (was claimed met in V001; that claim withdrawn)
carrier embeddings    A5 AUTHORED, map now EXACT with its intertwining law
orientation law       FORCED (F08, F09) — MET, conditional on the §1 domain
time-ordering         inter-cell FORCED (F10, re-scoped); intra-cell A6 AUTHORED
common-origin cert    SHAPE only; AC5 a predicate; AC6 a named gap
```

**Three of the five are met only by authored content, and one is a shape with a gap.** V001 claimed
5/5; that count is withdrawn.

### 9.4 What is still NOT claimed

- **No Lorentzian parent action.** Of the prior refusal's four grounds, **three still stand**
  (member 10 `:370-377`); the candidate addresses the fourth by domain restriction, and §1.4's R1–R3
  say how far that reaches.
- **No uniqueness.** At least four placement classes remain with coverage unproved.
- **The candidate is not established on the physical causal-cell domain** — R2.

---

## 10. FREEDOMS, FLATTENING, AUDITS, CUSTODY

### 10.1 FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN (unchanged)
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 02's mandate and honesty clause; member 04's seven corrections, thirteen confirmations and
  two re-scopings; member 05's registry; member 08's first-opening premise, BareRec_2 and specified
  composition carrier; member 09's scope determination; member 10's Attach type and prior kill;
  members 11/12/13's ratified law under its read rule; member 14's holonomy rule and cell postulate;
  member 15's transport-only rule; members 16-20's cell, endpoint, geometry and projectors;
  member 21's res_B and AC6; member 22's slot identity; members 23/24's receivers; all fences.

DERIVED HERE:
  b_1(K_(1,3)) = 0 and b_1(K_square) = 1 from the members' own integers;
  the reproduction of [D(z),S] != 0 and its vanishing exactly at z = 1;
  the domain restriction and the observation that b_1 = 1 leaves NO selector to freeze;
  the intertwining law that makes A1 well typed;
  the two-stage carrier and the reading that the ratified law fixes its order;
  the recount to 8 authored / 7 new.

AUTHORED HERE:
  A4, A5, A6, A7, A8 — five NEW authored ingredients added in this version, each tabled with its
  justification and void condition.  The core grew from a claimed 1 to an actual 7-new.

SELECTED HERE:
  NOTHING.  No selector Gamma_cell is frozen — b_1 = 1 leaves nothing to select, and on the wider
  admitted class I decline to supply one because the assignment reserves it.  No member of the origin
  family, no state, no scalarization, no dimension, no scale.  NOTHING ADOPTED, RATIFIED OR ENTERED.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

### 10.2 Flattening check — S01–S37 walked

- **Admissibility was not flattened into universality** (§1.4 R3) — the exact error the check named.
- A **scope determination** was not flattened into a **defect**: the tree sector's emptiness is the
  record's ruling, and the candidate's inertness there is correct behaviour (§1.3).
- A **specified carrier** was not flattened into a **sealed** one (§1.4 R1).
- A **domain restriction** was not flattened into a **proof of the physical domain** (§1.4 R2).
- An **authored ingredient** was not flattened into a **forced** one — five new rows went to the
  AUTHORED table rather than into prose.
- **Cell locality** was not flattened into **intra-cell commutation** (§4.1) — withdrawn, not softened.
- A **certificate predicate** was not flattened into a **discharged certificate** (§7.3).
- A **carrier's topology** was not flattened into a **prediction's content** (§1.2): `b_1` and the
  holonomy form were taken; no predicted value was opened.
- A **corrected candidate** was not flattened into an **entered premise** (§11).

### 10.3 Audits

```text
V001            = BYTE-UNTOUCHED (8c257818…, verified before and after; generator refuses otherwise)
MANDATE         = LIVE (generator refuses without it)
PROSE_DIGESTS   = 24/24, STRICT==STABLE (0 live members)
STRUCTURAL COMPUTATIONS = b_1 both sectors, and the [D(z),S] reproduction, recomputed by the generator
RESIDUE         = 0 hits, 27-token scan over authored prose
OUTPUT_INSPECTION = NONE-CERTIFIED.  The circuit determination rests on member 08's first-opening
   premise and specified carrier and on member 09's scope ruling — cell dynamics, upstream in every
   direction.  The battery, kappa_record and alpha are pointers only and were not opened.
```

### 10.4 Custody

- **BUILDER NEVER VERIFIES OWN WORK.** CLAIMED until the Codex 2 check. I executed against the
  check's findings and did not re-adjudicate any of them.
- **V001 read-only**, superseded append-only, still sealed and on the books.
- **No register, plan, tracker, or git action of any kind, read-only included.** Writes confined to
  `workspace/` and `relay_outbox/`.
- PE-1..17 pointer-only, ledger shut; ω_phys untouched; no member bound; no freeze.

---

## 11. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00006927 computed as a fixed point on bytes; 24 members;
     0 live; residue scan 0 hits from its own run)

CIRCUIT = DOMAIN-RESTRICTED (record-typed sectors)
     TREE SECTOR (b_1 = 0, incl. first opening K_(1,3), member 08 :262-270): the candidate is INERT
       and reduces to U_N^0.  Correct behaviour, not failure — member 09 :70-77 rules the emptiness a
       DETERMINATION made on purpose, so returning any phase there would manufacture one.
     LOOP SECTOR (b_1 >= 1): the record SPECIFIES the carrier one composition later (member 08
       :1840-1863, K_square with ordered loop holonomy exp(i Phi)) and MANDATES it (member 08 :1790).
       Computed here: b_1(K_square) = 1 — the circuit is CANONICAL, so THERE IS NO SELECTOR TO FREEZE
       and none is frozen.
     RESIDUAL, DISPLAYED (entry-relevant, not silent): R1 the class is specified and mandated but NOT
       SEALED (member 08 :2321); R2 no proof it is THE physical causal-cell domain (member 14
       :103-111, :168); R3 admissibility is not universality (member 08 :296-304).

BRIDGE = AUTHORED (A4; member 08 :224-239 declines to identify the carriers, so no sealed bridge
     exists; supplied with its intertwining requirement and void condition)
EMBEDDING = EXACT map with intertwining law, AUTHORED identification (A5)
TIME_ORDERING = AUTHORED (A6).  V001's intra-cell inference is WITHDRAWN as FALSE — [D(z),S] != 0 for
     z != 1.  Inter-cell commutation survives (F10, re-scoped).  Order fixed by the ratified law, not
     by me.  CONSEQUENCE: the parent must now supply a TWO-STAGE front — a strictly stronger demand
     on an object that is still underived (member 18 :77-101).

CLOSED_CLAIM = RESTATED-ALL-SYMBOLS-TABLED (J -> A7, g -> A8, bridge -> A4, embedding -> A5,
     physical time-ordering -> A6; no third category; test re-invited in the form that broke the last one)

CORE_COUNT = 8-AUTHORED (7 NEW).  ** HEADLINE UPDATED: V001's "exactly one new ingredient" is FALSE
     and WITHDRAWN. **  The candidate is a seven-new-ingredient authored core on thirteen confirmed
     forced rows — materially weaker than V001 advertised.

O4_ANCHOR = RECITED — member 19, R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_RESULT_V001.md :36-41.
     V001 cited "member 11 (R3.4 tail)"; its member 11 was the Record Action Principle.  Corrected.

REGISTRY = 2 FAIL addressed as AUTHORED (AC-2 -> A4, AC-3 -> A6, with V001's "met" claims withdrawn);
     2 NOT-YET stated as awaiting the completed object; AC5 corrected to a predicate; AC6 OPEN.
REQUIREMENTS = 2/5 FORCED-MET (orientation; inter-cell ordering), 3/5 met only via AUTHORED content,
     certificate a SHAPE with a named gap.  V001's 5/5 is withdrawn.

OUTPUT_INSPECTION = NONE-CERTIFIED
PROSE_DIGESTS = 24/24, STRICT==STABLE
CHAIN_INVOKED = false (chained pickup run after this DONE; relay_inbox holds no paste beyond 1057,
     so no successor exists for either lane.  DARIO holds.)
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**THIS ARTIFACT IS PROPOSED_NOT_ADOPTED.** Ratification of content is a second, separate principal
act (member 02 `:60-66`): an ENTRY addendum must name this artifact's digest and its check's digest.
**Until that addendum exists, no construction may consume this candidate as premise.** The registrar
may not enter it; the lanes may not enter it; a conversational summary may not substitute. Nothing
downstream may treat any line here as available. The mandate's open forcing slot is carried unchanged:
`future_derivation_can_select_the_attach_map = NO_VERDICT`.

All findings **CLAIMED** until the opposite-lane check.

**Where I would attack this version.** Not the circuit ruling — it rests on the record's own scope
determination and two integer computations anyone can redo. **The soft place is now A6, the two-stage
carrier.** It buys back the ordering the check destroyed, but it does so by demanding a *two-stage*
moving front from a parent that has not been derived at all, and it makes the candidate's physical
realizability strictly harder than V001's did. If the derived parent turns out to write the cell in
one stage, A6 falls and the intra-cell ordering problem returns with nothing to replace it.

**Second: A4.** Member 08 `:224-239` does not merely omit the bridge — it says the `U(1)` appearances
are *related but not identified*, and one of them is *precisely the component removed by the
projective quotient*. If `iota_rep` lands the connection's phase in that removed component, the
candidate's phase is gauge-trivial by construction and V7 fires on the loop sector too. **That is the
substitution I would try first**, and I could not close it from the spans I have.
