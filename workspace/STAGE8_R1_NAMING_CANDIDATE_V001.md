# STAGE 8 — THE R-L4a REGULARIZATION-NAMING RESIDUE (r-1): NECESSITY DETERMINED FIRST, THEN THE NAMING CANDIDATE STAGED

## BLIND ANALYST — CODENAME R1-BUILD — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (R1-BUILD). Tasked in the staging pattern of the
tasking (its "Q-1046" token and its "DERIVE-BEFORE-AUTHOR" precedent
token are UNLOCATABLE in the workspace corpus — full grep this session —
and are flagged as tasking-context, NOT defaulted; the discipline they
describe is applied as tasked): FIRST determine from the sealed record
whether the r-1 residue of record — THE REGULARIZATION-NAMING ACT of
R-L4a (E1 :768, "spec-author's, per the clause's own text";
certification a903716c §2.3(b)/(r-1)) — is actually NECESSARY, i.e.
whether a derivation FORCES the choice (lemma, not act) or makes
R-L4b's consumers regularization-INDEPENDENT (act moot); ONLY IF
genuinely necessary, author the naming candidate as adoptable spec
text, PROPOSED-NOT-ADOPTED. Honest outcome classes equal citizens:
UNNECESSARY(derived) / CANDIDATE-STAGED / PARTIAL / FAILS-AT. Nothing
is adopted by this work; adoption is the principal's alone.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — one CAS battery (sympy
1.14.0, fresh venv `r1venv` under /private/tmp scratch), reproduced in
full in §2.6 with output verbatim (15/15 PASS on the final full run);
every constant symbolic or an exact rational/surd; NOTHING numeric
evaluated; no physical quantity computed, bounded numerically, or
evaluated; alpha never computed or approached; L formal; M(t) sharp
(consumed as sealed; nothing mollified — and the candidate itself BARS
mollification, per D6'); equal-time FALSE by C6 (the omega = 0 slice
consumed only as the sealed bookkeeping intermediate of record); no
carrier or cellulation datum introduced (the candidate names the family
OF RECORD; the two witness-family eigenvalue models of §2.3/§2.6 are
abstract model sequences at the record's RB4/CB4 witness-family grade,
never the sealed operators, and certify no bound on them); H-R never
defaulted (rho_res never valued); per-pair route barred and not
traversed (no per-pair majorant chain is invoked anywhere); no
register/road/ledger/lens/plan/tracker file read; no flag flipped; no
witness action (witness status is the registrar's); no value frozen (no
value of D, of kappa_n, of delta, of M). Output name probed before
write: ABSENT. No git action; no existing file edited; ONE file written
(this artifact) plus its seal sidecar. Marks: DERIVED / CLAIMED /
CONDITIONAL(premise named) span by span.

Tooling disclosure, on the record: two harness-form corrections were
made before the final battery run, neither changing any mathematical
claim — (i) a first partial run FAILed its V-family trace check on an
UNEVALUATED Sum object (sympy leaves sum_i 1/(i+2) symbolic; replaced
by the index-shifted sum_{m=3}^{n+2} 1/m with the summand-shift pin —
the record's S4a-lesson pattern), and that run's division-carrying 3x3
factorization check exceeded practical time and was restructured
DIVISION-FREE (adjugate identity + det multiplicativity + scaling);
(ii) `expand` left 9^{-i} - 3^{-2i} unresolved in one NV-family
conjunct; replaced by `simplify`. The final battery then ran ONCE:
15/15 PASS, reproduced verbatim in §2.6.

---

## 0. VERDICT IN ONE LINE

**NECESSITY = NECESSARY (derived to the sealed maximum), then CANDIDATE-STAGED (PROPOSED-NOT-ADOPTED): the r-1 naming is a genuine, load-bearing act on three exact chains — (i) NO NAMING-FREE COMPLETION: uncompressed, BOTH Carleman factors are undefined of record (tr(CPC) = +infinity; ||CPC||_2 = infinity by C6), and the regularized determination is FAMILY-RELATIVE — two exact witness-family models satisfying every sealed eigenvalue-data constraint (s_i in [0,1], trace divergent) realize OPPOSITE outcomes, det_n = 2/((n+1)(n+2)) -> 0 with kappa_n -> infinity (model V, s_i = 1/(i+2)) versus |det_n| >= 3^{-27/16} > 0 with sign (-1)^n and NO limit (model NV, s_i = 1 - 3^{-i}, kappa mass 3/8, avoidance delta = 1/6), so the sealed record alone decides nothing and R-L4a/b are unsatisfiable at their own bytes without a named family; (ii) THE CHOICE IS NOT FORCED: every regularization-shaped byte of E1 is enumerated (:170, :462-470, :766-782, :784, :1024-1025, :1103, :1947-1956) and none closes the admissible class — the clause itself reserves the selection ("WITH THE REGULARIZATION EXPLICITLY NAMED"); what IS derivable is the narrowing: admissibility constraints AR-1..AR-5 (Carleman route; D6' no-mollification; no new frozen number; F'-5 index-blind constants; full-family consumption) plus UNIQUENESS-OF-RECORD — the carrier compression C_n is the only admissible family consuming no datum beyond the record (ZF §4.1(b)(c); 52f2490b; PA carrier) — a narrowing lemma, not a forcing; (iii) THE ACT IS NOT MOOT: R-L4b's consumers are NOT regularization-independent — on the FALSE branch with common opposite-sector value d, N_all(d) = (1-d)/2 (no normalization at d = 1 = |m_0|^2 = 0), c'_00(d) = 1/(2(1-d)), and the R-L0 threshold root moves strictly below W(1/2) at any balance ratio k > 1; independence would require d = 0 across the OPEN admissible class — exactly the unsealed r-2 universally quantified. The candidate (§3) names THE CARRIER COMPRESSION OF RECORD, with domain, AR-conformity, and its exact interaction with the sealed kappa_n dichotomy stated while asserting NOTHING about kappa_n (r-2 unsealed on both branches; the third region undecided per the check's n-1); the FALSE branch is worked as an explicit void/reopen clause (N.5) with falsifiers V-N1..V-N5 in the adopted style; no value frozen, no fence touched, no flag flipped; the Actual-Surface CHOICE LEDGER carries exactly ONE PREMISE(named) — the naming itself, with its immateriality and forcing conditions stated — and four FORCED choices with grounds. CAS battery 15/15 PASS. Adoption is the principal's alone; all three R-L4 witnesses STAND.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

Workspace files under `/Users/bgm/MB Work/alpha-program-archive/workspace/`;
the guard under `/Users/bgm/MB Work/alpha_supervision/`. Every digest
recomputed from bytes at path THIS session by FULL digest; each compared
against its `.seal.sha256` sidecar AND the tasked prefixes. 8/8 MATCH;
NONE UNVERIFIABLE.

```text
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md  MATCH (CERT — the r-1/r-2 residue of record; §2.2 failure mode; §2.3(b)-(d); §2.4 r-3)
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md  MATCH (CERT-CHK — CONFIRMED all five audits; C6 re-derivation; notes n-1..n-3, the non-partition)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1 — the clause bytes; D6'; F'-4/F'-5; R.2/R.3/R.4; PA-3; register)
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH (R3C — consumers stay conditional on R-L4a/b; the collapse-level scoping)
d66a922cfe023284890de8a335c38028efe94fb7a9d31b1f779bb0a0513b95cc  STAGE8_K_CHANNEL_CONTROL_V001.md  MATCH (KCH — its K_n is the cross-branch composite, NOT kappa_n; r-2 untouched there; Q-1054 handling precedent)
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md  MATCH (ZF — §4.1(b)(c) the n-indexed family of record; §4.3 o-1/o-2/o-3)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (per-pair refutation; seal verified; substance consumed via CERT/CERT-CHK's byte-verified citations)
43db1d77b40bb4c9c41dfb6ab72cfe681a5c4c78c0dcc582f7040a7b1948a9c6  ACTUAL_SURFACE_GUARD_V001.md  MATCH (GUARD — THE CRITERION; MANDATORY CHOICE LEDGER; relevance burden)
```

Sealed content read at path this session and consumed: CERT in full
(esp. §2.1-§2.4, §5 A3a-A5b, flag block); CERT-CHK in full (esp. §4.3
i-4/i-5, §5 n-1..n-3, §6 C6/D1-D3/E1-E3); E1 :160-178 (B-4 repair),
:286-345 (enforcement-mechanism reading; C2 at :330-333), :404-458 (D3
:417-426; D5' split), :460-480 (D6' at :462-470), :640-900 (R.0-R.4:
the Carleman display :657-666; S1-S5 :679-737; R.2 :739-762; the
clauses :766-790 verbatim; kappa_bal grounds :794-800; R.3 :803-848;
R.4 :851-894), :995-1035 (register; R-L4a at :1024-1025), :1665-1700
(F'-3..F'-11; F'-5 at :1671-1678), :1944-1960 (PA-3); R3C :1-890
(verdict; §2.1 collapse-level conditionality; §2.7 residue; §3.7; the
kappa_n-absence scan); KCH :1-640 and :870-982 (verdict; §1 seal table;
flag block; the kappa_n-absence scan; the Q-1054 unlocatable-token
handling); ZF :321-360 (§4.1(a)-(c)) and :390-430 (§4.3 o-1..o-3);
GUARD in full. The tasked context tokens "Q-1046" and
"DERIVE-BEFORE-AUTHOR": full grep of the workspace corpus this session
— UNLOCATABLE; flagged as tasking-context, NOT defaulted (the KCH/
Q-1054 handling).

---

## 2. TASK 1 — THE NECESSITY DETERMINATION (WORKED BEFORE ANY AUTHORING)

### 2.1 The question located, and the derivable constraint set on ANY admissible regularization

The r-1 residue of record (CERT §2.3(b)/(r-1), check-confirmed): the
R-L4a clause demands its determination "via the Carleman factorization
det = det_2 · e^{tr} AND WITH THE REGULARIZATION EXPLICITLY NAMED"
(E1 :767-768), and of record the uncompressed level is EMPTY — the
unregularized D IS UNDEFINED (tr(CPC) = +infinity, :770-772, certified
identity-grade), AND the displayed regularized route also fails
uncompressed (||CPC||_2 radius-independent and INFINITE by C6,
:713-715; CERT §2.3(b)) — so the determination can only run on a
regularized FAMILY, and the clause reserves the family's NAMING. The
necessity question: does any sealed derivation FORCE the choice, or
make R-L4b's consumers independent of it?

What IS derivable from the sealed bytes is the admissibility
constraint set — every occurrence of regularization-shaped text in E1
was enumerated this session (:170, :462-470, :766-782, :784,
:1024-1025, :1103, :1947-1956; grep-complete) and yields exactly:

```text
AR-1 ROUTE (E1 :767-768): the determination runs via det = det_2 ·
     e^{tr}; hence every family member must render BOTH factors
     defined — the regularized operator 2 C~ P C~ must be TRACE CLASS
     per member (nonnegative, so trace class iff finite trace; trace
     class => HS => det_2 defined). [CERT §2.3(a)-(b), identity-grade]
AR-2 LOCALIZER INTEGRITY (D6', E1 :462-470): "No obligation, lemma,
     control, repair, or enclosure may replace, mollify, smooth, or
     regularize M(t) or 1_{D_t}." Every mollified-localizer family is
     BARRED. The admissible regularizations act on the OPERATOR/SPACE
     axis, never on the sealed sharp localizers.
AR-3 NO NEW FROZEN NUMBER (F'-4 :1669-1670 "No measured constants
     anywhere"; the exact collapse algebra :746-748 frozen): a
     coupling-deformation family (e.g. -2 -> -2 + epsilon) replaces
     the sealed operator and freezes a new number — not a
     regularization OF the sealed object. BARRED.
AR-4 INDEX HYGIENE (F'-5 :1671-1678; C2 :330-333 "finite only on a
     fixed carrier, which spec-header scoping clause 1 forbids"): no
     certified CONSTANT may carry the family index; the determination
     is consumable only at the family/limit quantifier, never at a
     fixed member.
AR-5 CONSUMER QUANTIFIER (derived, §2.4/N5e): R-L4b's exclusion
     assigns the census ONE value; a subsequential naming leaves the
     consumer two-valued (the NV model's (-1)^n sign is the exact
     exhibit). The naming must bind the FULL family limit.
```

### 2.2 Prong A — is the choice FORCED (lemma, not act)? NO; the exact narrowing derived

None of the enumerated bytes closes the class {families satisfying
AR-1..AR-5} to one member. The clause itself reserves an open
selection (:768; the register repeats it at :1024-1025 "with the
regularization named"); ZF o-3 states the reservation as the record
reads it ("they need the named regularization act at the divergent
trace"); CERT §2.3(b) reads it the same way ("NAMING that
regularization (or another) ... is the spec author's adoption act").
PA-3 (:1947-1956) is an OUTCOME-CLASS prediction (confidence:
moderate), not a derivation — and its displayed route was itself
sharpened by CERT: uncompressed, det_2 fails too.

The derivable maximum is a NARROWING, not a forcing:

```text
UNIQUENESS-OF-RECORD (lemma, derived): among admissible families
consuming ONLY objects of record, the carrier compression C_n is the
unique member. Grounds at bytes: the record's own fixed-n objects ARE
the compressions — A_{mu lambda, n}(a) = C_n(V(a) - 1)C_n (ZF
§4.1(b)); "only the n-indexed family exists" — the per-pair
uncompressed limit is refuted of record (ZF §4.1(c); 52f2490b via
CERT-CHK's byte-verified u-4); the PA carrier is the sealed
construction the family lives on. Any OTHER admissible family must
introduce a construction datum not of record — an introduction the
sealed record nowhere performs and the clause licenses only as the
spec author's naming act.
CONSEQUENCE: the naming act survives as a genuine selection — between
THE family of record and any new-datum family a spec author might
introduce. The precedent class "proposed rule proven derivable, hence
never authored" does NOT match: here the content left open is a
selection over a class the record leaves open, and no derivation can
close an open class. NOT FORCED.
```

### 2.3 Prong B — is there a NAMING-FREE completion of R-L4a/b? NO (family-relativity, exact)

The one escape from Prong A's conclusion would be a derivation
completing R-L4a/b WITHOUT selecting a family — i.e., proving the same
determination outcome under EVERY admissible regularization. That
escape is REFUTED at the sealed constraint set:

```text
(b-1) Uncompressed completion: EMPTY of record (both factors
      undefined; CERT §2.3(a)-(b), check-confirmed). No completion
      exists at the unregularized level — certified.
(b-2) Universal completion: REFUTED by FAMILY-RELATIVITY. The sealed
      record constrains the regularized eigenvalue data exactly by:
      s_i in [0, 1] (projection compression) and tr_n = sum s_i ->
      infinity (C2's divergence, consumed at CERT §2.3(d)). TWO exact
      witness-family models satisfy BOTH constraints and realize
      OPPOSITE determination outcomes [CAS N5/N6/N7]:
        MODEL V  (s_i = 1/(i+2)):   det_n(0) = 2/((n+1)(n+2)) -> 0
                 EXACT closed form; kappa_n = 2(tr_n - sum s_i^2) ->
                 infinity (tr_n = harmonic(n+2) - 3/2 -> oo; sum s_i^2
                 = pi^2/6 - 5/4 finite). Branch-1-type outcome: d = 0.
        MODEL NV (s_i = 1 - 3^{-i}): |det_n(0)| >= 3^{-27/16} > 0 for
                 ALL n (the of-record chord bound instantiated at
                 delta = 1/6, U = 2/9, M = 3/8 — all exact), while
                 tr_n = n - (1 - 3^{-n})/2 -> infinity; every factor
                 1 - 2s_i < 0, so sign det_n = (-1)^n: NO limit exists
                 (exact partial products -1/3, 7/27, -175/729).
                 Branch-2-type outcome: non-vanishing, non-convergent.
      Since the constraint set is satisfied by both models, no
      derivation FROM THE SEALED RECORD ALONE returns a family-free
      truth value for "the regularized determinant vanishes." Model
      grade stated honestly: these are abstract eigenvalue families
      (witness-family grade, the record's RB4/CB4 license); they
      decide nothing about the actual carrier family — deciding THAT
      is exactly the unsealed r-2 — but they PROVE the record's
      constraint set cannot force the answer.
(b-3) Hence R-L4a/b are unsatisfiable at their own bytes without a
      named family: the act-CLASS is NECESSARY. [With (2.2): the act
      is necessary AND its content is a genuine selection.]
```

### 2.4 Prong C — are R-L4b's consumers regularization-INDEPENDENT (act moot)? NO (exact)

R-L4b's consumers of record (CERT §2.2 m-3, all CONDITIONAL on the
exclusion; re-pinned N1): N_surv(0) = 1/2; c = (1/2, 1/8 x 4);
kappa_bal = 1; the R-L0 threshold display kappa_bal X_* e^{X_*} <=
1/2; R-L0b's convexity footing. Independence would mean: these values
survive under every admissible naming. Three exact exhibits refute
derivability:

```text
(c-1) THE PARAMETRIC d-EXHIBIT (the record's own §2.2/C6 failure-mode
      pattern, extended to the weights; d a FORMAL parameter — the
      common opposite-sector determinant value IF the exclusion
      fails): N_all(d) = (1-d)/2 EXACT — the normalization denominator
      moves with d; at d = 1 it is 0 = |m_0|^2 (BY-DESIGN vanishing,
      ZF Z10b): NO normalization exists. The (0,0) weight becomes
      c'_00(d) = 1/(2(1-d)) != 1/2 for every d != 0. Every census
      consumer is a NONCONSTANT function of d. [CAS N2]
(c-2) THE THRESHOLD-CONSUMER MOTION: at balance ratio k, the R-L0
      display's root is W(1/(2k)); u e^u is strictly increasing and W
      is increasing on y > 0, so for ANY k > 1 the admissible root
      sits STRICTLY BELOW the sealed W(1/2): the R-L0 threshold is a
      strict function of the balance data the exclusion fixes.
      [CAS N3; parametric — no balance value asserted]
(c-3) d IS NOT DECIDED BY THE RECORD: the two models of (b-2) realize
      d = 0 and (liminf |det_n| >= 3^{-27/16} with alternating sign)
      within the sealed constraint set — so "d = 0 under every
      admissible naming," the exact content independence needs, is
      underivable; it is the unsealed r-2 (plus r-3 on the polydisc)
      universally quantified over an OPEN class. NOT MOOT.
FENCE NOTE (explicit): no value of D is asserted or inferred anywhere;
d is a formal parameter of a conditional exhibit on the FALSE branch;
the STRICKEN (1+|D|)/|1-D| display is cited nowhere as ground;
kappa_bal OF RECORD is untouched — it remains 1, conditional on the
exclusion, from the surviving weights only (:794-800 honored).
```

### 2.5 The determination

```text
NECESSITY = NECESSARY. The r-1 naming is (i) required for R-L4a/b to
be satisfiable at all (no naming-free completion — §2.3), (ii) a
genuine selection no sealed derivation forces (§2.2, with the
uniqueness-of-record narrowing derived), and (iii) load-bearing for
every consumer of the exclusion (§2.4). Neither UNNECESSARY branch
exists on the sealed record; the honest deliverable is therefore the
candidate, authored under the derived narrowing, with the selection
carried as a NAMED PREMISE and the FALSE branch worked explicitly.
Proceed to §3.
```

### 2.6 THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `r1venv` under /private/tmp scratch; nothing
written to the workspace but this artifact and its seal.

```python
# R1-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv r1venv
# under /private/tmp scratch). Every constant symbolic or an exact rational/surd.
# Nothing numeric evaluated. Groups:
#   N* — the NECESSITY determination (Task 1): record re-pins; the FALSE-branch
#        parametric consumer-dependence exhibit (the certification's own §2.2 /
#        check C6 pattern, extended to the weights — NO value of D inferred, the
#        stricken display nowhere cited as ground); the threshold-consumer
#        motion; the two exact witness-family models (NV and V) showing the
#        sealed eigenvalue-data constraint set realizes BOTH determination
#        outcomes — family-relativity, so no naming-free completion and no
#        derivable regularization-independence.
#   C* — CANDIDATE support (Task 2): the per-factor arithmetic of the sealed
#        dichotomy's two branch bounds; the division-free factorization frame
#        of the r-3 joint-rate input; Branch-1 domination.
# The NV and V families are WITNESS-FAMILY-GRADE abstract eigenvalue models of
# the sealed constraint set (s_i in [0,1], trace divergent) — the record's
# RB4/CB4 license — never the sealed operators, and certify no bound on them.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== N1 — record re-pins: census, weights, conditional footing =====
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
ph = [1, -1, -1]
pairs = [(a, b) for a in range(3) for b in range(3)]
surv = [(a, b) for (a, b) in pairs if ph[a] == ph[b]]
opp = [(a, b) for (a, b) in pairs if ph[a] != ph[b]]
Nsurv = sum(w_v[a]*w_v[b] for (a, b) in surv)
c_w = {p: sp.nsimplify(w_v[p[0]]*w_v[p[1]]/Nsurv) for p in surv}
ok("N1 census re-pin: 9 = 5 + 4; N_surv(0) = 1/2; c = (1/2, 1/8 x 4) all > 0;"
   " sum c = 1; sum |c| = kappa_bal = 1 (ALL conditional on the exclusion)",
   len(pairs) == 9 and len(surv) == 5 and len(opp) == 4 and Nsurv == R(1, 2)
   and sorted(c_w.values()) == [R(1, 8)]*4 + [R(1, 2)]
   and all(v > 0 for v in c_w.values()) and sum(c_w.values()) == 1
   and sum(abs(v) for v in c_w.values()) == 1)

# ===== N2 — consumers are NOT regularization-independent: the parametric =====
# FALSE-branch exhibit (certification §2.2 failure mode; check C6 re-derived).
# d is a FORMAL parameter: the common opposite-sector determinant value IF the
# exclusion fails. NO value of D is asserted or inferred; the stricken display
# is not cited; on the TRUE branch kappa_bal = 1 from surviving weights only.
d = sp.Symbol('d')
N_all = sum(w_v[a]*w_v[b] for (a, b) in surv)*1 \
      + sum(w_v[a]*w_v[b] for (a, b) in opp)*d
c00_of_d = sp.simplify(w_v[0]*w_v[0]/N_all)
ok("N2 N_all(d) = (1-d)/2 EXACT; N_all(0) = 1/2 (exclusion value);"
   " N_all(1) = 0 = |m_0|^2 (NO normalization); c'_00(d) = 1/(2(1-d)) != 1/2"
   " for d != 0: every census consumer is a nonconstant function of d",
   sp.expand(N_all - (1 - d)/2) == 0 and N_all.subs(d, 0) == R(1, 2)
   and N_all.subs(d, 1) == 0
   and sp.simplify(c00_of_d - 1/(2*(1 - d))) == 0
   and sp.simplify(c00_of_d.subs(d, 0) - R(1, 2)) == 0
   and sp.simplify(c00_of_d - R(1, 2)).subs(d, R(1, 3)) != 0)

# ===== N3 — threshold-consumer motion (parametric; nothing valued) =====
# The R-L0 display kappa_bal X_* e^{X_*} <= 1/2 has root W(1/(2k)) at balance
# ratio k; u e^u strictly increasing and W increasing on y > 0, so for k > 1
# the admissible root SHRINKS below the sealed W(1/2): the display MOVES with
# the balance ratio — the exact sense in which R-L0 consumes the exclusion.
u = sp.Symbol('u', real=True)
k = sp.Symbol('k', positive=True)
y = sp.Symbol('y', positive=True)
Wy = sp.LambertW(y)
ok("N3 d/du(u e^u) = (1+u)e^u > 0 on u >= 0; root set of u e^u = 1/(2k) is"
   " {W(1/(2k))}; W'(y) = W/(y(1+W)) > 0; and k > 1 => 1/(2k) < 1/2 exactly"
   " ((k-1)/(2k) > 0): the threshold root moves strictly below W(1/2)",
   sp.simplify(sp.diff(u*sp.exp(u), u) - (1 + u)*sp.exp(u)) == 0
   and sp.ask(sp.Q.positive((1 + u)*sp.exp(u)), sp.Q.nonnegative(u))
   and sp.solve(u*sp.exp(u) - 1/(2*k), u) == [sp.LambertW(1/(2*k))]
   and sp.simplify(sp.diff(Wy, y) - Wy/(y*(1 + Wy))) == 0
   and sp.simplify(R(1, 2) - 1/(2*k) - (k - 1)/(2*k)) == 0)

# ===== N4 — fixed-n existence and the kappa_n identity (theta-family) =====
th = sp.Symbol('theta', real=True)
cth, sth = sp.cos(th), sp.sin(th)
Cm = sp.Matrix([[cth**2, cth*sth], [cth*sth, sth**2]])
Pm = sp.Matrix([[1, 0], [0, 0]])
CPC = Cm*Pm*Cm
detr = sp.simplify(sp.trace(Cm*(sp.eye(2) - 2*Pm)*Cm))
lhs4 = sp.trace((Cm*Pm - Pm*Cm).T*(Cm*Pm - Pm*Cm))
rhs4 = 2*sp.trace(CPC) - 2*sp.trace(CPC*CPC)
ok("N4 fixed-n existence: C, P exact projections; det_{ran C}(1-2CPC) ="
   " 1 - 2cos^2(theta) EXACT (every fixed-n member defined); kappa identity"
   " ||[C,P]||_F^2 = 2tr(CPC) - 2tr((CPC)^2) ALL theta",
   sp.simplify(Cm*Cm - Cm) == sp.zeros(2) and Pm*Pm == Pm
   and sp.simplify(detr - (1 - 2*cth**2)) == 0
   and sp.simplify(lhs4 - rhs4) == 0)

# ===== N5 — WITNESS FAMILY NV (non-vanishing model): s_i = 1 - 3^{-i} =====
i, n, m = sp.symbols('i n m', positive=True, integer=True)
t = sp.Symbol('t', positive=True)
iv = sp.Symbol('iv', positive=True)        # continuous stand-in for the index
s_nv = 1 - 3**(-i)
u_nv = sp.expand(s_nv*(1 - s_nv))          # = 3^{-i} - 9^{-i}
# monotone-chain grounds (B4-style): 3^{-i} strictly decreasing (d/di 3^{-i} =
# -3^{-i} log 3, log 3 > 0), value 1/3 at i = 1 => 3^{-i} <= 1/3 < 1/2 on
# i >= 1; and t(1-t) increasing on t <= 1/3 (d/dt = 1-2t >= 1/3 there).
ok("N5a NV data: u_i = 3^{-i} - 9^{-i} EXACT; d/di 3^{-i} = -3^{-i} log 3"
   " with log 3 > 0 (strictly decreasing; value 1/3 at i = 1 => 3^{-i} <= 1/3"
   " < 1/2 on i >= 1); factor 1 - 2 s_i = 2*3^{-i} - 1 < 0 for all i >= 1"
   " (from 3^{-i} <= 1/3): sign det_n = (-1)^n exactly",
   sp.simplify(u_nv - (3**(-i) - 9**(-i))) == 0
   and sp.simplify(sp.diff(3**(-iv), iv) + 3**(-iv)*sp.log(3)) == 0
   and bool(sp.log(3) > 0)
   and sp.simplify((3**(-i)).subs(i, 1) - R(1, 3)) == 0
   and bool(R(1, 3) < R(1, 2)) and bool(2*R(1, 3) - 1 < 0)
   and sp.simplify(1 - 2*s_nv - (2*3**(-i) - 1)) == 0)
ok("N5b NV 1/2-avoidance and chord domain: |s_i - 1/2| = 1/2 - 3^{-i} >= 1/6"
   " on i >= 1 (monotone chain; equality at i = 1 EXACT); delta = 1/6,"
   " U = 1/4 - delta^2 = 2/9; u = t(1-t) with d/dt = 1 - 2t >= 1/3 > 0 on"
   " t <= 1/3, so u_i <= u_1 = 2/9 = U (domain [0, U] respected, equality"
   " only at i = 1)",
   sp.simplify((R(1, 2) - 3**(-i)).subs(i, 1) - R(1, 6)) == 0
   and sp.simplify(R(1, 4) - R(1, 6)**2 - R(2, 9)) == 0
   and sp.simplify(sp.diff(t*(1 - t), t) - (1 - 2*t)) == 0
   and bool((1 - 2*R(1, 3)) - R(1, 3) == 0)
   and sp.simplify(u_nv.subs(i, 1) - R(2, 9)) == 0
   and sp.simplify(u_nv.subs(i, 2) - R(8, 81)) == 0 and R(8, 81) < R(2, 9))
M_nv = sp.summation(3**(-i) - 9**(-i), (i, 1, sp.oo))
tr_nv = sp.summation(s_nv, (i, 1, n))
ok("N5c NV mass and trace: M = sum u_i = 1/2 - 1/8 = 3/8 EXACT (bounded);"
   " tr_n = sum s_i = n - (1 - 3^{-n})/2 -> oo (the sealed divergence"
   " tr(C_n P C_n) -> oo respected by the model)",
   M_nv == R(3, 8)
   and sp.simplify(tr_nv - (n - (1 - 3**(-n))/2)) == 0
   and sp.limit(n - (1 - 3**(-n))/2, n, sp.oo) == sp.oo)
ok("N5d NV Branch-2 bound instantiated: 4 delta^2 = 1/9; M/U = (3/8)/(2/9) ="
   " 27/16; |det_n|^2 >= (1/9)^{27/16}, i.e. |det_n| >= 3^{-27/16} > 0 for"
   " ALL n (chord bound of record; exponent monotonicity for base < 1)",
   sp.simplify(4*R(1, 6)**2 - R(1, 9)) == 0
   and sp.simplify(R(3, 8)/R(2, 9) - R(27, 16)) == 0
   and sp.simplify((R(1, 9)**R(27, 16))**R(1, 2) - 3**(-R(27, 16))) == 0
   and 3**(-R(27, 16)) > 0)
p1 = 1 - 2*(1 - R(1, 3))
p2 = p1*(1 - 2*(1 - R(1, 9)))
p3 = p2*(1 - 2*(1 - R(1, 27)))
ok("N5e NV exact partial products: det_1 = -1/3, det_2 = 7/27,"
   " det_3 = -175/729 (sign (-1)^n literal; moduli stay above 3^{-27/16})",
   p1 == R(-1, 3) and p2 == R(7, 27) and p3 == R(-175, 729)
   and p1 < 0 and p2 > 0 and p3 < 0)

# ===== N6 — WITNESS FAMILY V (vanishing model): s_i = 1/(i+2) =====
s_v = 1/(i + 2)
det_v = sp.simplify(sp.product(1 - 2*s_v, (i, 1, n)))
ok("N6a V data: 0 < s_i = 1/(i+2) <= 1/3 < 1/2 for i >= 1; det_n ="
   " prod i/(i+2) = 2/((n+1)(n+2)) EXACT closed form; lim det_n = 0"
   " (Branch-1-type outcome realized: d = 0)",
   sp.simplify(s_v.subs(i, 1) - R(1, 3)) == 0
   and sp.simplify(det_v - 2/((n + 1)*(n + 2))) == 0
   and sp.limit(2/((n + 1)*(n + 2)), n, sp.oo) == 0)
# index shift: sum_{i=1}^n 1/(i+2) = sum_{m=3}^{n+2} 1/m (summand pin at
# i = m - 2), evaluated exactly to harmonic(n+2) - 3/2.
jj = sp.Symbol('j', positive=True, integer=True)
tr_v_shift = sp.Sum(1/m, (m, 3, n + 2)).doit()
sum_sq_v = sp.summation(s_v**2, (i, 1, sp.oo))
ok("N6b V trace and commutator mass: summand shift (1/(i+2))|_{i=j-2} = 1/j"
   " EXACT; tr_n = sum_{m=3}^{n+2} 1/m = harmonic(n+2) - 3/2 -> oo (sealed"
   " divergence respected); sum s_i^2 = pi^2/6 - 5/4 FINITE; hence kappa_n ="
   " 2 sum s_i(1-s_i) = 2(tr_n - sum s_i^2) -> oo (Branch-1 hypothesis"
   " realized by the model)",
   sp.simplify((1/(i + 2)).subs(i, jj - 2) - 1/jj) == 0
   and sp.simplify(tr_v_shift - (sp.harmonic(n + 2) - R(3, 2))) == 0
   and sp.limit(sp.harmonic(n + 2), n, sp.oo) == sp.oo
   and sp.simplify(sum_sq_v - (sp.pi**2/6 - R(5, 4))) == 0)

# ===== N7 — family-relativity (the conjunction): no naming-free completion =====
ok("N7 BOTH models satisfy the full sealed eigenvalue-data constraint set"
   " (s_i in [0,1]; trace -> oo) yet realize OPPOSITE determination outcomes"
   " (V: det_n -> 0; NV: |det_n| >= 3^{-27/16} with sign (-1)^n — no limit"
   " exists): the sealed constraints do NOT decide the regularized outcome;"
   " the determination is family-relative; independence is underivable",
   M_nv == R(3, 8) and sp.limit(2/((n + 1)*(n + 2)), n, sp.oo) == 0
   and 3**(-R(27, 16)) > 0
   and sp.limit(sp.harmonic(n + 2), n, sp.oo) == sp.oo)

# ===== C1 — per-factor arithmetic of the sealed dichotomy =====
s, U, dl = sp.symbols('s U delta', real=True)
ok("C1 (1-2s)^2 = 1 - 4s(1-s); s(1-s) = 1/4 - (s-1/2)^2; 1 - 4(1/4 - d^2) ="
   " 4 d^2; midpoint chord identity (1-2U)^2 - (1-4U) = 4U^2 >= 0; concavity"
   " d2/du2 log(1-4u) = -16/(1-4u)^2",
   sp.expand((1 - 2*s)**2 - (1 - 4*s*(1 - s))) == 0
   and sp.expand(s*(1 - s) - (R(1, 4) - (s - R(1, 2))**2)) == 0
   and sp.expand(1 - 4*(R(1, 4) - dl**2) - 4*dl**2) == 0
   and sp.expand((1 - 2*U)**2 - (1 - 4*U) - 4*U**2) == 0
   and sp.simplify(sp.diff(sp.log(1 - 4*u), u, 2) + 16/(1 - 4*u)**2) == 0)

# ===== C2 — Branch-1 support: kappa_n -> oo forces det_n -> 0 =====
h2 = sp.exp(-4*u) - 1 + 4*u
ser = sp.Poly(sp.series(sp.exp(4*u) - 1, u, 0, 6).removeO(), u)
Msym = sp.Symbol('M_s')
ok("C2 1 - 4u <= e^{-4u} on u >= 0 (h(0) = 0, h' = 4(1 - e^{-4u}),"
   " e^{4u} - 1 coefficientwise > 0): |det_n|^2 = prod(1-4u_i) <="
   " exp(-4 sum u_i) = exp(-2 kappa_n), coefficient bookkeeping"
   " 4 sum u_i = 2 (2 sum u_i) = 2 kappa_n exact",
   h2.subs(u, 0) == 0
   and sp.simplify(sp.diff(h2, u) - 4*(1 - sp.exp(-4*u))) == 0
   and all(cf > 0 for cf in ser.coeffs())
   and sp.expand(4*Msym - 2*(2*Msym)) == 0)

# ===== C3 — the r-3 factorization frame (division-free + multiplicativity) =====
A0 = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'a0_{a}{b}'))
AA = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'aa_{a}{b}'))
X3 = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'x_{a}{b}'))
Y3 = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'y_{a}{b}'))
I3 = sp.eye(3)
D0 = (I3 + A0).det()
lhsM = sp.expand((I3 + A0)*(D0*I3 + (I3 + A0).adjugate()*(AA - A0)))
rhsM = sp.expand(D0*(I3 + AA))
c_ = sp.Symbol('c_s')
ok("C3 division-free frame (3x3 fully symbolic): (1+A0)(D0 I + adj(1+A0)"
   "(A - A0)) = D0 (1+A) EXACT; det(XY) = det X det Y EXACT; det(cX) ="
   " c^3 det X EXACT — jointly: where det(1+A0) != 0, det(1+A) = det(1+A0)"
   " det(1 + (1+A0)^{-1}(A - A0)), the frame of the r-3 joint-rate input"
   " ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|)",
   sp.expand(lhsM - rhsM) == sp.zeros(3, 3)
   and sp.expand((X3*Y3).det() - X3.det()*Y3.det()) == 0
   and sp.expand((c_*X3).det() - c_**3*X3.det()) == 0)

print("R1-BATTERY-DONE")
```

Output, verbatim (15/15 PASS):

```text
N1 census re-pin: 9 = 5 + 4; N_surv(0) = 1/2; c = (1/2, 1/8 x 4) all > 0; sum c = 1; sum |c| = kappa_bal = 1 (ALL conditional on the exclusion): PASS
N2 N_all(d) = (1-d)/2 EXACT; N_all(0) = 1/2 (exclusion value); N_all(1) = 0 = |m_0|^2 (NO normalization); c'_00(d) = 1/(2(1-d)) != 1/2 for d != 0: every census consumer is a nonconstant function of d: PASS
N3 d/du(u e^u) = (1+u)e^u > 0 on u >= 0; root set of u e^u = 1/(2k) is {W(1/(2k))}; W'(y) = W/(y(1+W)) > 0; and k > 1 => 1/(2k) < 1/2 exactly ((k-1)/(2k) > 0): the threshold root moves strictly below W(1/2): PASS
N4 fixed-n existence: C, P exact projections; det_{ran C}(1-2CPC) = 1 - 2cos^2(theta) EXACT (every fixed-n member defined); kappa identity ||[C,P]||_F^2 = 2tr(CPC) - 2tr((CPC)^2) ALL theta: PASS
N5a NV data: u_i = 3^{-i} - 9^{-i} EXACT; d/di 3^{-i} = -3^{-i} log 3 with log 3 > 0 (strictly decreasing; value 1/3 at i = 1 => 3^{-i} <= 1/3 < 1/2 on i >= 1); factor 1 - 2 s_i = 2*3^{-i} - 1 < 0 for all i >= 1 (from 3^{-i} <= 1/3): sign det_n = (-1)^n exactly: PASS
N5b NV 1/2-avoidance and chord domain: |s_i - 1/2| = 1/2 - 3^{-i} >= 1/6 on i >= 1 (monotone chain; equality at i = 1 EXACT); delta = 1/6, U = 1/4 - delta^2 = 2/9; u = t(1-t) with d/dt = 1 - 2t >= 1/3 > 0 on t <= 1/3, so u_i <= u_1 = 2/9 = U (domain [0, U] respected, equality only at i = 1): PASS
N5c NV mass and trace: M = sum u_i = 1/2 - 1/8 = 3/8 EXACT (bounded); tr_n = sum s_i = n - (1 - 3^{-n})/2 -> oo (the sealed divergence tr(C_n P C_n) -> oo respected by the model): PASS
N5d NV Branch-2 bound instantiated: 4 delta^2 = 1/9; M/U = (3/8)/(2/9) = 27/16; |det_n|^2 >= (1/9)^{27/16}, i.e. |det_n| >= 3^{-27/16} > 0 for ALL n (chord bound of record; exponent monotonicity for base < 1): PASS
N5e NV exact partial products: det_1 = -1/3, det_2 = 7/27, det_3 = -175/729 (sign (-1)^n literal; moduli stay above 3^{-27/16}): PASS
N6a V data: 0 < s_i = 1/(i+2) <= 1/3 < 1/2 for i >= 1; det_n = prod i/(i+2) = 2/((n+1)(n+2)) EXACT closed form; lim det_n = 0 (Branch-1-type outcome realized: d = 0): PASS
N6b V trace and commutator mass: summand shift (1/(i+2))|_{i=j-2} = 1/j EXACT; tr_n = sum_{m=3}^{n+2} 1/m = harmonic(n+2) - 3/2 -> oo (sealed divergence respected); sum s_i^2 = pi^2/6 - 5/4 FINITE; hence kappa_n = 2 sum s_i(1-s_i) = 2(tr_n - sum s_i^2) -> oo (Branch-1 hypothesis realized by the model): PASS
N7 BOTH models satisfy the full sealed eigenvalue-data constraint set (s_i in [0,1]; trace -> oo) yet realize OPPOSITE determination outcomes (V: det_n -> 0; NV: |det_n| >= 3^{-27/16} with sign (-1)^n — no limit exists): the sealed constraints do NOT decide the regularized outcome; the determination is family-relative; independence is underivable: PASS
C1 (1-2s)^2 = 1 - 4s(1-s); s(1-s) = 1/4 - (s-1/2)^2; 1 - 4(1/4 - d^2) = 4 d^2; midpoint chord identity (1-2U)^2 - (1-4U) = 4U^2 >= 0; concavity d2/du2 log(1-4u) = -16/(1-4u)^2: PASS
C2 1 - 4u <= e^{-4u} on u >= 0 (h(0) = 0, h' = 4(1 - e^{-4u}), e^{4u} - 1 coefficientwise > 0): |det_n|^2 = prod(1-4u_i) <= exp(-4 sum u_i) = exp(-2 kappa_n), coefficient bookkeeping 4 sum u_i = 2 (2 sum u_i) = 2 kappa_n exact: PASS
C3 division-free frame (3x3 fully symbolic): (1+A0)(D0 I + adj(1+A0)(A - A0)) = D0 (1+A) EXACT; det(XY) = det X det Y EXACT; det(cX) = c^3 det X EXACT — jointly: where det(1+A0) != 0, det(1+A) = det(1+A0) det(1 + (1+A0)^{-1}(A - A0)), the frame of the r-3 joint-rate input ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|): PASS
R1-BATTERY-DONE
```

---

## 3. TASK 2 — THE NAMING CANDIDATE (PROPOSED-NOT-ADOPTED)

The following is adoptable spec text for the r-1 naming, authored under
§2's determination and the derived narrowing. It is a CANDIDATE:
nothing below is adopted, valued, or in force; adoption is the
principal's alone. On adoption it would discharge exactly the naming
demand of E1 :768 ("WITH THE REGULARIZATION EXPLICITLY NAMED") and
:784 ("with the regularization of R-L4a") — and ONLY that: all three
R-L4 witnesses stand until the r-2 and r-3 inputs are sealed and
checked.

```text
=====================================================================
CANDIDATE CLAUSE R-L4a-N — THE REGULARIZATION NAMING
STATUS: PROPOSED-NOT-ADOPTED
=====================================================================

N.1  THE NAMING. For R-L4a and R-L4b (E1 :766-790), the regularization
     is NAMED as THE CARRIER COMPRESSION OF RECORD: the n-indexed
     family {C_n} of compressions onto the sealed carrier subspaces —
     the same family in which the record's own fixed-n objects live,
     A_{mu lambda, n}(a) = C_n(V_{mu lambda}(a) - 1)C_n (ZF §4.1(b);
     the PA carrier construction, 789338ad; the n-indexed family of
     record at ZF §4.1(c) and 52f2490b). Regularized baseline objects:
     A_n(0) = -2 C_n P C_n on ran C_n (opposite-phase pairs; the exact
     collapse algebra of E1 :746-748 untouched);
     det_n(0) := det_{ran C_n}(1 - 2 C_n P C_n) = prod_i (1 - 2 s_i),
     s_i the eigenvalues of C_n P C_n on ran C_n (of record, R-L1
     block-triangular identity). The R-L4a determination and the
     R-L4b vanishing are statements about the family {det(1 + A_n(a))}
     as n -> infinity, at the FULL-family quantifier (no subsequence).
     NO new carrier, cellulation, truncation, or numeric datum is
     introduced: the family is the one of record.

N.2  DOMAIN. Per admitted state (M-3), per admitted cell of D3, per
     OPPOSITE-PHASE record-colour pair, on the closed pair polydisc
     (M-2); baseline point a = 0 as displayed at E1 :746-762. The
     surviving sector is untouched (V(0) = I exactly there); this
     naming administers ONLY the opposite-phase sector's Carleman
     route. ADMISSIBILITY CONFORMITY, exhibited: AR-1 — each member is
     finite rank, hence 2 C_n P C_n is trace class per member and both
     Carleman factors are defined (det_n exists at every fixed n; CAS
     N4); AR-2 — the compression acts on the carrier axis only; M(t)
     and 1_{D_t} are consumed SHARP (D6' honored by construction);
     AR-3 — no number is frozen; AR-4 — the index n lives in the NAMED
     FAMILY, never in a certified constant; consumption is at the
     family/limit quantifier only; AR-5 — the full-family limit binds.

N.3  INTERACTION WITH THE SEALED kappa_n DICHOTOMY (consumption
     interface; NO hypothesis below is asserted). With kappa_n :=
     ||[C_n, P]||_2^2 = 2 sum_i s_i(1 - s_i) (of record):
     (B1) a SEALED input "kappa_n -> infinity" discharges the
          regularized vanishing at the baseline point: det_n(0) -> 0
          (|det_n(0)|^2 <= exp(-2 kappa_n), of record) — the honest
          "det = 0 regularized" recovery of E1 :779-781.
     (B2) a SEALED input "sup_n kappa_n = 2M < infinity AND uniform
          1/2-avoidance (exists delta > 0: |s_i - 1/2| >= delta for
          all i, n)" REFUTES R-L4b at this naming:
          |det_n(0)| >= (4 delta^2)^{M/(2U)} > 0, U := 1/4 - delta^2
          (of record). The FALSE branch, N.5, then fires.
     (B3) bounded kappa_n WITHOUT uniform 1/2-avoidance: UNDECIDED of
          record (the check's n-1: the two branches are sufficient
          conditions, NOT a partition); no consumption is licensed.
     R-L4b is DISCHARGED at this naming only by a (B1)-type sealed
     input PLUS the joint-rate input on the polydisc (r-3 of record):
     ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|) uniformly on the closed
     pair polydisc, in the exact factorization frame
     det(1 + A_n(a)) = det(1 + A_n(0)) det(1 + R_n Delta_n(a)) where
     1 + A_n(0) is invertible (CAS C3). NEITHER input is supplied,
     presumed, or valued here.

N.4  WHAT THE NAMING DOES NOT DO. It asserts NOTHING about kappa_n —
     not (B1), not (B2), not (B3): r-2 is unsealed on both branches
     and stays a named estimate obligation on no discharged artifact.
     It freezes no value (no value of D or of any limit; no delta, no
     M). It flips no flag, retires no witness (the registrar's), moves
     no gate. It does not touch the stricken display (E1 :773-778),
     which remains consumable only as its own clause permits. It does
     not decide, weaken, or touch R-L2b, H-R, C-L2/G_cm, summed-S2',
     R-L0, or R-L0b. It introduces no datum beyond the record.

N.5  THE FALSE BRANCH (void/reopen clause; the compression-
     regularization hazard, LIVE of record). This clause FIRES iff a
     sealed artifact, checked, does any of:
       (F-a) certifies the (B2) hypotheses at this naming; or
       (F-b) exhibits liminf_n |det_n(0)| > 0; or
       (F-c) exhibits non-convergence of det_n(0) (e.g. sign
             alternation at non-vanishing modulus — a live shape: the
             sign is (-1)^{#{s_i > 1/2}} of record).
     THEN, all of the following, and nothing else:
     (i)   R-L4b is FALSE AT THIS REGULARIZATION (of record, CERT
           §2.3(d)/§2.4). The exclusion may NOT be consumed.
     (ii)  Every consumer of the exclusion REOPENS to the full 9-pair
           architecture: the 5/4 census, N_surv(0) = 1/2, the weights
           c = (1/2, 1/8 x 4), kappa_bal = 1, the R-L0 threshold
           display's kappa_bal factor, and R-L0b's convexity footing
           (the sealed failure mode, CERT §2.2 / check C6, governs).
     (iii) NO value is thereby assigned to the opposite sector: the
           reopen is to OBLIGATIONS, never to values (E1 :773-778;
           F'-3). The stricken display stays stricken.
     (iv)  This candidate's R-L4b-service interface (the N.3
           consumption of the exclusion) is VOID. The naming itself
           survives only as the R-L4a determination interface, whose
           determination then completes with the honest answer
           NON-VANISHING at this regularization (R-L4a answered;
           R-L4b failed at it). Whether to name a DIFFERENT
           regularization is a new adoption act and returns to the
           principal (V-N4 territory; no lane may make that call).

N.6  FALSIFIERS (adopted V-1 style; refutation of a load-bearing claim
     at its own quantifier VOIDS the candidate):
     V-N1 ADMISSIBILITY: a sealed derivation that some member fails —
          2 C_n P C_n not trace class on ran C_n, or det_n(0)
          undefined at some n — VOIDS the candidate.
     V-N2 UNIQUENESS-OF-RECORD: a sealed regularized family on the
          discharged basis, distinct from the carrier compression and
          antedating this candidate, VOIDS the narrowing premise; the
          naming returns to the principal at the wider class.
     V-N3 THE INTERFACE: refutation, at its own quantifier, of
          det_n(0) = prod(1 - 2 s_i), of the (B1) sufficiency, or of
          the (B2) lower bound, VOIDS the candidate.
     V-N4 DERIVE-BEFORE-AUTHOR SUPERSESSION: a sealed derivation that
          FORCES the regularization choice, or makes R-L4b's
          consumers regularization-independent, VOIDS the candidate
          as MOOT (the lemma supersedes the act; §2 of this artifact
          exhibits why no such derivation exists on the current
          sealed basis).
     V-N5 FALSE-BRANCH INTEGRITY: a sealed derivation that N.5(ii)'s
          reopen list omits a consumer of the exclusion VOIDS the
          candidate until repaired.

N.7  CHOICE LEDGER (Actual-Surface Guard V001; every unforced choice
     in this candidate's chain, classified):
     CH-1 THE NAMING SELECTION ITSELF (carrier compression of record
          vs any admissible new-datum family): PREMISE(named). Every
          claim of this candidate visibly conditions on it ("AT THIS
          REGULARIZATION" throughout). What would prove it
          IMMATERIAL: a sealed derivation that every admissible
          regularization yields the same R-L4b truth value (i.e. the
          unsealed r-2/r-3 content universally quantified over the
          admissible class). What would prove it FORCED: a sealed
          clause closing the admissible class to families of record
          (uniqueness-of-record, §2.2, would then force C_n).
          Neither exists sealed (§2).
     CH-2 FULL-FAMILY QUANTIFIER (no subsequence): FORCED by the
          consumer — the exclusion assigns the census ONE value; a
          subsequential naming leaves it two-valued (the NV model's
          (-1)^n exhibit, CAS N5e). Ground exhibited, §2.1 AR-5.
     CH-3 THE a = 0 + JOINT-RATE DECOMPOSITION of R-L4b: FORCED of
          record (the factorization identity, CAS C3; CERT §2.4).
     CH-4 OBLIGATIONS-REOPEN (not value-assignment) on the FALSE
          branch: FORCED by the sealed strictures (E1 :773-778 no
          value of D; F'-3 no defaults).
     CH-5 ONE REGULARIZATION SERVING BOTH CLAUSES: FORCED at E1 :784
          ("with the regularization of R-L4a").
     MACHINERY/RELEVANCE (guard append): the compression family is
     apparatus consumed from the sealed constructions with a booked
     surface trace (the PA carrier the response functions live on;
     ZF §4.1(b)(c)) — SURFACE-DERIVED, not surface-native; its
     apparatus origin is exactly why CH-1 is a PREMISE and N.5 is
     live, and it may never silently anchor a surface verdict beyond
     the named conditional. TOY_SEPARATION: self-assessment CLAIMED
     clean at the stated quantifiers (every instance-grade exhibit in
     the supporting artifact is marked witness-family grade; no
     instance is quoted wider than its derivation); the RULING is the
     audit's, not this artifact's.

N.8  STATUS AND ADOPTION. PROPOSED-NOT-ADOPTED. Adoption is the
     principal's alone; no lane adoption exists. On adoption: the
     naming becomes the regularization of R-L4a at :768's own demand;
     witnesses E1_BASELINE_DETERMINANT_EXISTENCE_UNCERTIFIED,
     E1_OPPOSITE_PHASE_SECTOR_VANISHING_UNCERTIFIED, and
     E1_BASELINE_COLLAPSE_UNCERTIFIED all STAND until the r-2 and r-3
     inputs are sealed and checked; the falsifiers V-N1..V-N5 are in
     force from adoption.
=====================================================================
```

---

## 4. FLAG BLOCK

```text
NECESSITY = NECESSARY( three exact chains, worked before any
  authoring: (i) NO NAMING-FREE COMPLETION — uncompressed both
  Carleman factors undefined of record (tr(CPC) = +infinity;
  ||CPC||_2 = infinity by C6), and the regularized determination is
  FAMILY-RELATIVE: two witness-family models satisfying every sealed
  eigenvalue-data constraint (s_i in [0,1], trace divergent) realize
  OPPOSITE outcomes — model V (s_i = 1/(i+2)): det_n = 2/((n+1)(n+2))
  -> 0 with kappa_n -> infinity; model NV (s_i = 1 - 3^{-i}):
  |det_n| >= 3^{-27/16} > 0 all n with sign (-1)^n, no limit (delta =
  1/6, U = 2/9, M = 3/8, all exact) — so R-L4a/b are unsatisfiable at
  their own bytes without a named family; (ii) NOT FORCED — every
  regularization-shaped E1 byte enumerated (:170, :462-470, :766-782,
  :784, :1024-1025, :1103, :1947-1956), none closes the admissible
  class; the clause reserves the selection; derived narrowing:
  admissibility AR-1..AR-5 (Carleman route; D6' no-mollification; no
  new frozen number; F'-5 index-blind constants; full-family
  consumption) + UNIQUENESS-OF-RECORD (the carrier compression is the
  only admissible family consuming no new datum: ZF §4.1(b)(c),
  52f2490b, PA) — a narrowing lemma, not a forcing; (iii) NOT MOOT —
  consumers not regularization-independent: N_all(d) = (1-d)/2 with
  N_all(1) = 0 = |m_0|^2 (no normalization), c'_00(d) = 1/(2(1-d)),
  and the R-L0 threshold root W(1/(2k)) < W(1/2) for any balance
  ratio k > 1; independence = the unsealed r-2 (+ r-3) universally
  quantified over an open class — underivable. No value of D
  asserted or inferred; d formal; the stricken display cited nowhere
  as ground; kappa_bal of record untouched (= 1 conditional on the
  exclusion, surviving weights only). )
CANDIDATE_STATUS = STAGED-PROPOSED-NOT-ADOPTED( §3 clause R-L4a-N:
  names THE CARRIER COMPRESSION OF RECORD {C_n} (the family of ZF
  §4.1(b)(c), 52f2490b, PA carrier) for R-L4a AND R-L4b (one
  regularization, :784); domain = opposite-phase sector only, per
  admitted state/cell/pair on the closed pair polydisc, baseline
  a = 0; det_n(0) = prod(1 - 2 s_i) at the full-family quantifier;
  AR-1..AR-5 conformity exhibited (finite rank => trace class per
  member; M(t)/1_{D_t} sharp; no number frozen; index never in a
  certified constant; full-family limit); adoption the principal's
  alone; all three R-L4 witnesses STAND. )
FALSE_BRANCH = WORKED-EXPLICIT( N.5 fires iff a sealed checked
  artifact (F-a) certifies the Branch-2 hypotheses (sup_n kappa_n =
  2M < infinity AND uniform 1/2-avoidance) at this naming, or (F-b)
  exhibits liminf_n |det_n(0)| > 0, or (F-c) exhibits non-convergence
  of det_n(0) (sign (-1)^{#{s_i > 1/2}} live). THEN: R-L4b FALSE AT
  THIS REGULARIZATION; the exclusion unconsumable; reopen list =
  {5/4 census, N_surv(0) = 1/2, c = (1/2, 1/8 x 4), kappa_bal = 1,
  the R-L0 display's kappa_bal factor, R-L0b's convexity footing} —
  reopened to the 9-pair architecture per the sealed failure mode
  (CERT §2.2 / check C6); NO value assigned to the opposite sector
  (obligations, not values; :773-778, F'-3); the candidate's
  R-L4b-service interface VOID; R-L4a then completes with the honest
  answer NON-VANISHING at this naming; renaming is a new adoption
  act returned to the principal. The third region (bounded kappa_n
  without uniform avoidance) consumes NOTHING — undecided of record
  (check n-1: two sufficient branches, not a partition). )
R2_HANDLING = UNSEALED-BOTH-BRANCHES-ASSERTED-NOWHERE( the candidate
  asserts no kappa_n behavior — not Branch 1, not Branch 2, not the
  third region; it defines the consumption interface only: (B1)-type
  sealed input + the r-3 joint rate ||R_n Delta_n(a)||_1 =
  o(-log|det_n(0)|) discharges R-L4b at this naming; (B2)-type sealed
  input refutes it (N.5); third-region input licenses nothing.
  kappa_n = ||[C_n, P]||_2^2 stays a named estimate obligation on no
  discharged artifact, exactly as certified (r-2). )
CHOICE_LEDGER = ONE-PREMISE-FOUR-FORCED( CH-1 the naming selection:
  PREMISE(named) — immaterial iff a sealed derivation gives every
  admissible regularization the same R-L4b truth value (r-2/r-3
  universally quantified); forced iff a sealed clause closes the
  admissible class to families of record (uniqueness-of-record then
  forces C_n); neither exists sealed. CH-2 full-family quantifier:
  FORCED by the consumer (subsequential naming leaves the census
  two-valued — the NV (-1)^n exhibit). CH-3 the a = 0 + joint-rate
  split: FORCED of record (C3 identity; CERT §2.4). CH-4
  obligations-reopen-not-values: FORCED (:773-778; F'-3). CH-5 one
  regularization for both clauses: FORCED (:784). MACHINERY/
  RELEVANCE: the compression family SURFACE-DERIVED (booked trace:
  PA carrier; ZF §4.1(b)(c)); apparatus origin is why CH-1 is a
  PREMISE and N.5 is live. TOY_SEPARATION self-assessment CLAIMED
  clean at stated quantifiers; the ruling is the audit's. )
NET = NECESSITY-DERIVED-CANDIDATE-STAGED-NOTHING-ADOPTED( the
  DERIVE-BEFORE-AUTHOR discipline was run to its sealed maximum
  BEFORE authoring: the act is necessary (no naming-free completion;
  not forced; not moot) and the candidate is staged under the derived
  narrowing with the selection carried as the ledger's one
  PREMISE(named). No gate moves; no flag flips; no witness action; no
  new obstruction; no value frozen; the residue landscape is
  UNCHANGED — r-2 (kappa_n under the naming) and r-3 (the joint
  polydisc rate) remain the named unsealed inputs, R-L2b / H-R /
  C-L2/G_cm / summed-S2' untouched at their own quantifiers; the
  landing set of record (summed-S2' + R-L4a/b + R-L0(/R-L0b) +
  C-L2/G_cm) unchanged. The tasked tokens Q-1046 and
  DERIVE-BEFORE-AUTHOR are UNLOCATABLE in the workspace corpus —
  flagged as tasking-context, not defaulted (the KCH/Q-1054
  handling). ALL_RESULTS = CLAIMED until checked; the candidate is
  PROPOSED-NOT-ADOPTED and adoption is the principal's alone. )
SEALS_VERIFIED = 8/8( a903716c CERT; 685afac8 CERT-CHK; 46846730 E1;
  57edbb96 R3C; d66a922c KCH; 26f9314b ZF; 52f2490b per-pair
  refutation; 43db1d77 GUARD — all recomputed from bytes at path this
  session by full digest; all match sidecars and tasked prefixes;
  none unverifiable. )
FORBIDDEN_IMPORTS = none( exact symbolic only — every constant
  symbolic or an exact rational/surd; nothing numeric evaluated; no
  physical quantity computed or bounded; alpha never approached; L
  formal; M(t) sharp (and the candidate bars mollification per D6');
  equal-time FALSE by C6 respected; no carrier/cellulation datum
  introduced (the named family is OF RECORD; the NV/V models are
  witness-family-grade abstract sequences, never the sealed
  operators); H-R never defaulted; per-pair route barred and not
  traversed; no register/road/ledger/lens/plan/tracker read; no
  value of D inferred; the stricken display consumed nowhere as
  ground; no git action; no existing file edited; ONE file written
  plus its seal. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv r1venv under
  /private/tmp scratch; ONE final full run, 15/15 PASS, script and
  output reproduced verbatim in §2.6; two pre-final harness-form
  corrections disclosed in the header (an unevaluated-Sum form, a
  division-carrying determinant check restructured division-free, an
  expand-vs-simplify power unification); no mathematical claim
  changed by any of them.
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
R1_NAMING_CANDIDATE_RESULT = SEALED.
```
