# BID Primitive Boundary-Superconnection Conditional Inventory v001

Date: 2026-07-23

## Purpose and provenance

This file addresses the parent-action counterexample

```text
minimal Dirac source action
versus
minimal action plus an independently primitive Pauli term.
```

The Boundary Superconnection Principle was adopted in the historically
target-aware alpha program before this classification. The result below is
therefore not retroactively target-blind. Its evidential use still requires a
future frozen unused prediction.

No alpha, mass, endpoint, or residual enters the classification.

## Primitive filtration

The BID incidence filtration distinguishes:

```text
degree 0:
  local source/record zero-forms;

degree 1:
  one primitive oriented boundary incidence or covariant transport;

degree 2:
  a composed two-step return, commutator, loop, or face curvature.
```

The primitive generator is required to contain every allowed degree-zero and
degree-one component exactly once. Degree-two structures are generated by
composition of that operator and are not separately primitive.

This is the load-bearing physical content of the Boundary Superconnection
Principle. It is stronger than choosing the shortest Lagrangian.

## Dirac-type classification

On the disclosed `3+1` spin branch, fix the Clifford principal symbol

```text
sigma_D(xi)=i c(xi).
```

Any two first-order local operators with this symbol differ by a zero-order
endomorphism. Compatibility with the spin and unit-character vector-`U(1)`
connections gives

```text
D_BR
  =i gamma^mu (nabla_spin,mu-i a_mu)
   -Phi_R.
```

The compact connection coefficient is fixed by the primitive faithful
character `|q|=1`; it is not the low-energy electromagnetic coupling.

In the minimal isotropic source-record branch, the admissible algebraic
scalar/pseudoscalar zero-form is

```text
Phi_R=rho exp(i delta gamma^5).
```

Its boundary Riesz representative is

```text
slash(n) Phi_R,
```

as derived independently in the Lorentz source-boundary map file. This
classifies the operator form but does not select `rho` or `delta`.

## Why an independent primitive Pauli term is excluded

A term

```text
ell_* bar(psi) sigma^(mu nu) psi F_(mu nu)
```

depends on the face curvature

```text
F=[nabla_mu,nabla_nu].
```

It is therefore degree two in the BID incidence filtration. Adding it as an
independent component of `D_BR` would count a generated two-incidence return
as a second primitive operator. It violates the declared degree-one
superconnection completeness rule.

This does not say Pauli structure is absent. Squaring the one primitive
operator generates it:

```text
D_BR^2
  =covariant_Laplacian
   +R/4
   +(fixed Clifford contraction of F)
   +i gamma^mu nabla_mu Phi_R
   +Phi_R^2,
```

with the curvature coefficient fixed by the Clifford identity. Effective
Pauli and `F^2` terms may then arise from the complete quantum response, but
their coefficients are outputs rather than independent primitive inputs.

The same filtration excludes independently primitive higher-derivative
source terms. It does not exclude their generated effective descendants.

## What this resolves

Given the adopted primitive degree-one superconnection rule:

```text
the minimal-versus-independent-Pauli parent fork is closed;
the charged connection and finite return are components of one operator;
and generated curvature terms inherit fixed relative Clifford structure.
```

## What remains open

This classification does not yet supply:

```text
the magnitude rho or axial phase delta;
the complete record/environment block;
the causal-cell smearing and continuum domain;
the CTP state and contour;
the measure, regulator, and gauge/ghost quotient;
the global self-adjoint connected operator;
the physical two-point pole;
the induced transverse response;
or alpha.
```

The primitive filtration is an adopted Gravacle principle. A hostile reviewer
may reject that principle; the future unused prediction is required to give it
independent empirical force.

## Status

```text
historical_target_blindness_established = false
primitive_BID_filtration_disclosed = true
primitive_BID_filtration_derived_independently = false
fixed_Clifford_principal_symbol_disclosed = true
incidence_degree_and_differential_order_distinguished = false
complete_zero_order_endomorphism_class_enumerated = false
odd_internal_superconnection_grading_typed = false
displayed_Laplace_type_square_verified = false
Dirac_type_operator_form_classified = false
unit_character_connection_coefficient_fixed = true
minimal_isotropic_zero_form_family_classified = false
rho_selected = false
delta_selected = false
independent_primitive_Pauli_term_filtration_admissible = false
Pauli_structure_generated_by_operator_square = false
independent_primitive_higher_derivative_terms_admissible = false
minimal_versus_independent_Pauli_parent_fork_closed_given_superconnection_principle = false
conditional_degree_one_inventory_only = true
complete_connected_source_record_action_derived = false
complete_Q_spec_sealed = false
physical_Thomson_stiffness_computed = false
alpha_computed = false
proof_authorized = false
```
