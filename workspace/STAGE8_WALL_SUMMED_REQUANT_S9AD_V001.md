# STAGE 8 — THE WALL'S NAMED SURVIVING ROUTE: WEIGHT-SUMMED RE-QUANTIFICATION OF THE FOUR DEFINITE-SIGNED QUANTITIES ONTO THE RECORD STRATA — S9AD V001

## BLIND BUILDER — CODENAME WSR-BUILD — COMMISSION S9AD — [SEALED]

Date: 2026-08-14 (session CDT 2026-08-15 early)
Role: BLIND builder (WSR-BUILD). Commission: the wall's named surviving
route (RFB obligation-1 re-pose, route (b): "weight-summed
re-quantification onto the record strata") — re-quantify the four
definite-signed quantities of the wall of record (tr K_H, tr H_A,
||K_H||_2^2, ||H_A||_2^2; UNDECIDED per (n, cell) with the per-composite
obstruction "real and unpassable on the sealed basis") at the
WEIGHT-SUMMED level: does the zero-sum weight structure (m_0 = m_1 = 0,
exact at full tau_R) + the parity grading + the chiral involution yield,
AT THE SUMMED QUANTIFIER, the enclosure, a definite-sign resolution, or an
exact cancellation that the per-composite level provably refuses? Summed
objects worked exactly; the C4/REM kill mechanics consumed at their sealed
quantifiers; the four quantities adjudicated one by one. Honest outcome
menu, none preferred: DERIVED(per quantity, at the summed quantifier,
exact) / PARTIAL / FAILS-AT / NOT-DERIVABLE-TODAY(blockers named).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ONLY — one CAS battery (sympy 1.14.0, fresh
venv `wsrvenv` under the session scratchpad), reproduced verbatim in §8
with output verbatim (14/14 PASS on the single completed run; ONE
pre-completion harness-form correction disclosed: the battery's first
execution crashed AFTER check WS1 (PASS) on a Python `sum()` start-value
TypeError in the WS2a/WS2b matrix comprehensions — the fix supplied the
`sp.zeros(3,3)` start value, a mechanical harness repair; NO check
condition changed; WS1's result is unchanged across executions); every
constant symbolic or an exact rational/surd; NOTHING numeric evaluated
(the carrier radius Lambda in WS4 is a FORMAL symbol — the record's
standing WB7/KF1 license); no floats as ground; no measured constant; no
value frozen (M, eps symbolic throughout). No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened (fenced classes
matched by filename only, never opened). "Q-..." tokens
EXPECTED-UNLOCATABLE per the tasking; noted, never chased, nothing
defaulted. No git action; nothing registered/committed/pushed; no existing
file edited; ONE output (this artifact) plus its seal sidecar at the
commission-distinct path, probed ABSENT at session start AND re-probed
ABSENT immediately before write. Every enumeration/exhaustiveness sentence
below claims only its own displayed sweep. SWEEP CUTOFF DECLARED (§9).
Marks: DERIVED / CLAIMED / GROUNDED(record cited at grade) /
CONDITIONAL(premise named) span by span.

Homonym discipline, declared up front: n = the CARRIER index of the family
of record (the quantifier of the wall's enclosures); k = the INSERTION
order (the coupling-parity grading; appears only as "order m^k"); the
momentum variable k of the sealed symbol displays appears only inside |k|
and khat and is disambiguated by context — all three usages are inherited
from the sealed bytes.

---

## 0. VERDICT IN ONE LINE

**THE SUMMED QUANTIFIER DECIDES THREE OF THE FOUR AND SHRINKS THE FOURTH'S SHORTFALL FROM TWO-PLUS POWERS TO A LOGARITHM: at the record's own summed object — the equal-source [a^1] composite of the weight-summed assembly, K^Sigma_n := [a^1] X_n(a,a) = C(U(0)^dag F + F^dag U(0))C, the CHECK-VERIFIED all-pairs instance Delta^{Sigma,all}_n of the adopted clause (e5b0879b §2.1 D-NOW-1 at bytes; F~ = F FORCED of record, 798de0dd §2.1) — (1) tr H_A^Sigma = 0 and (2) ||H_A^Sigma||_2^2 = 0 IDENTICALLY, per (n, cell), every n: DERIVED, EXACT CANCELLATION, by TWO independent exact mechanisms the single composite provably lacks — transpose-closure (the equal-source summed assembly pairs G_{mu lambda} with G_{lambda mu}^dag at EQUAL weight, so K^Sigma is HERMITIAN as a polynomial identity, CAS WS2c) and the per-factor odd-kill (each branch factor's weight sum annihilates every odd function of lambda — m_1 = m_3 = 0 exact at full tau_R — so K^Sigma is EVEN in the coupling, CAS WS2d) — the odd sector the per-composite level DOUBLES (KB4 of record, re-pinned WS2f with a displayed nonzero exhibit) is ZERO at the summed quantifier: the m_1 = 0 kill operates AT THE COMPOSITE LEVEL exactly as the precedent pattern promised; (3) ||K_H^Sigma||_2^2 = ||K^Sigma||_2^2 (Hermiticity; HS Pythagoras WS3b) is ENCLOSED n-UNIFORMLY AND CARRIER-FREELY per unit cell: ||K^Sigma_n||_2 <= M/eps with M the sealed carrier-free constant of the DISCHARGED B-L2* (REM: sup_n ||X_n||_2 <= ||X||_2 <= M, the C4/REM DOUBLE degree-0 kill — m_0 = 0 once per factor — consumed AT ITS SEALED QUANTIFIER as the deciding step; the summed object starts at degree -2, past the HS threshold 2p = 4 > 3, WS4) + the audited identification + the EXACT Cauchy slot extraction at radius eps (WS3a): DERIVED(enclosure; grade inherited — REM's identity-grade core with estimate-grade steps e-i/e-ii and their sealed overturn conditions; ray class CONDITIONAL(P-1) as of record) — the enclosure the per-composite level provably refuses (d66a922c + 4ebe6b8f) is DELIVERED at the summed quantifier by direct consumption, no V-2 bridge, no extraction, the allowed direction of the one-way quantifier record (0a10c030); the summed composite also inherits the boundary-closure operator enclosure with the SAME constant, ||K^Sigma_n||_op <= 2 (l1-tight weights, sum|w_l| = 1, the WBC chain transported); (4) tr K_H^Sigma = tr K^Sigma FAILS-AT — BUT AT A LOG-THIN THRESHOLD, NEW: the summed level pays TWO powers the per-composite level cannot (the double m_0 kill lifts the leading stratum from degree -1 to degree -2, of record REM §3.2; AND the S1 odd-spinor-trace leg ENGAGES at the summed leading stratum where the per-composite level provably refuses it — the ONLY degree -2 assembled stratum is the V_2 (x) V_2 class (o = 2, c = 0, REM §3.3; the coincidence classes assemble to degree <= -3 and "there is NO (c = 1, o = 0) class anywhere" IS a weight-summed statement, valid verbatim for THIS object), its diagonal carries EXACTLY ONE longitudinal khat_x (the J vertex's intraband part -khat_x C(k) at bytes; the V_2 factors angular-scalar; the BL2STAR sigma_intra display), an ODD integrand whose first angular moment VANISHES EXACTLY — CAS WS5, against the per-composite even-integrand refusal re-pinned — worth EXACTLY ONE power, KF2 of record), landing the summed trace count AT effective degree -3 = log Lambda by exact threshold arithmetic (WS4): the shortfall is LOGARITHMIC, the threshold (strictly below -3) is strict, and NO sealed asset crosses it — the chiral involution is EXACTLY GENERATOR-ODD (beta h_lambda beta = -h_lambda termwise, S = -i gamma^0 gamma^5 anticommuting with beta, CAS WS6) and therefore GRADES (the sea band mirrors to the positive band under conjugate dynamics modulo MO-2's rank <= 4 kernel correction) — NOT a kill; the stencil weight c(theta) = sin^2(sqrt2 theta/2) >= 0 is a SQUARE (WS7) — no sign resource; the sealed trace-grade budgets are polynomially divergent (FHB 12 sqrt2 n^{7/2}/ell with E_n = 6n^4/ell^2 EXACT; the rank x op ceiling 8n^3, WBC F1 = MO-3); MO-3's p = -3 has NO purchase on the c = 0 leading stratum and no coincidence resurrection exists (REM). EXACT RESIDUE, one number: a certified log-free rate for the degree-3 diagonal class of the summed assembly — MO-3's shape, now LOG-THIN. DELIVERED MEANWHILE, the sign ledger: with H_A^Sigma = 0 the wall's four-quantity difference COLLAPSES at the summed quantifier to the two-quantity Hermitian form Re e_2(K^Sigma) = (1/2)(tr K^Sigma)^2 - (1/2)||K^Sigma||_2^2 (WS8) — the between-sector sign indeterminacy (d66a922c n-2) is GONE, and the NEGATIVE channel is enclosed one-sidedly: e_2(K^Sigma) >= -M^2/(2 eps^2), exact. QUANTIFIER-SHIFT HONESTY, displayed: the summed level decides SUM-THEN-QUANTIFY and provably NOT quantify-then-sum — squares are PARITY-BLIND (the c-weighted linear sum of per-pair odd sectors is ZERO while the c-weighted sum of their SQUARES survives as (1/4)(tr H_A^{(+-)})^2, CAS WS9) — so the per-composite wall, its four UNDECIDED quantities, and the equal-source survivor T_n are UNTOUCHED at their own quantifier (the V-2-barred bridge is not crossed in either direction); nothing here moves the adopted amendment, alpha' = 1/4, B-L2*, or any sealed identity at its own quantifier. CAS 14/14 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest; each
compared against its `.seal.sha256` sidecar AND the tasked prefixes (where
tasked). 17/17 MATCH; NONE UNVERIFIABLE.

```text
d66a922c  STAGE8_K_CHANNEL_CONTROL_V001.md                 MATCH (tasked) [KCH — the wall: the four quantities; KB1-KB4/KC/KD/KF; o-2-OBS; s-i..s-v]
4ebe6b8f  STAGE8_K_CHANNEL_CONTROL_AUDIT_V001.md           MATCH (tasked) [KCH-AUDIT — "real and unpassable on the sealed basis"; the corrected gap form]
9fdc3d1c  STAGE8_MO3_P_EXPONENT_S9AD_V001.md               MATCH (tasked) [MO3 — p = -3 DERIVED; the coincidence escape DEAD]
80db260f  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md        MATCH (tasked) [WBC — op-grade ||K_H||_op, ||H_A||_op <= 2; WB4 splitting; F1 = MO-3; rank x op 8n^3]
9a295498  STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md            MATCH (tasked) [FHB — F3 generator per-(n,cell) enclosures 12 sqrt2 n^{7/2}/ell; E_n = 6n^4/ell^2 EXACT; sub-volume surface channel sigma_n]
6997ff61  STAGE8_MO2_KAPPA_RATE_S9AD_V001.md               MATCH (tasked) [MO2 — the chiral involution beta C_n beta = Q_n - C_n - (rank <= 4); the linear rate]
e5b0879b  STAGE8_S2_DISCHARGE_V001.md                      MATCH (tasked) [DISCHARGE — §2.1 the CHECK-VERIFIED identification Delta^{Sigma,all}_n = X_n; alpha' = 1/4 on the similarity class; W-1..W-3; the BAR]
cbe6ff4f  STAGE8_S2_DISCHARGE_AUDIT_V001.md                MATCH (tasked) [DISCHARGE-AUDIT — CONFIRMED per its own grade]
48ecdabe  STAGE8_REFUTING_BRANCH_S9AD_V001.md              MATCH (tasked) [RFB — obligation-1 RE-POSES as MO-3 restricted to three non-coincidence routes, route (b) = THIS commission; OB-3 response side SURVIVES summed]
798de0dd  STAGE8_SECOND_ORDER_TOTAL_CONTENT_V001.md        MATCH (sidecar) [S2TOT — §2.1 F~ = F FORCED at bytes; the G-array; the master form; the equal-source collapse T_n = (a^2/4) Re e_2(K_n); X-3/X-5]
c944a901  STAGE8_SECOND_ORDER_TOTAL_CONTENT_AUDIT_V001.md  MATCH (sidecar) [S2TOT-AUDIT — the F~ = F forcing CONFIRMED]
2e4e5163  STAGE8_REMAINDER_UNIFORMITY_V001.md              MATCH (sidecar) [REM — §2.1 the X display; §2.2-§2.3 the (o,c) taxonomy + parity floors (weight-summed statements); §3.2 the DOUBLE degree-0 kill; §3.3 the V_2 (x) V_2 leading stratum; §3.5; §5 grade e-i/e-ii]
6e81ae92  STAGE8_BL2STAR_ATTEMPT_V001.md                   MATCH (sidecar) [BL2STAR — :337-360 the X display + F definition; the stencil I13/I14; sigma_intra(X) = -i a_Delta khat_x b~_P C(k)]
a7969f0c  STAGE8_R2_RESUMMATION_V001.md                    MATCH (sidecar) [R2 — SURVIVOR_FATE = CANCELS; the m_0 kill both factors; the V_2 subsidiary -i V_2(x)/(2|k|)]
789338ad  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (sidecar) [PA — :75-128 h_0, S_n = -i slash(n) gamma^5, M(t), v(t), b_D, J = -(Q b_D Q) (x) alpha_x, h_lambda lambda-linear]
0a10c030  STAGE8_ASSEMBLY_QUANTIFIER_V001.md               MATCH (sidecar) [ASSEMBLY-Q — the quantifier of record SPLIT BY OBJECT; the one-way bridge (per-pair feeds summed, never the reverse)]
549362d4  STAGE8_MO3_P_EXPONENT_S9AD_AUDIT_V001.md         MATCH (sidecar) [MO3-AUDIT — CONFIRMED-WITH-CORRECTIONS; consumed at audited grade]
```

Sealed content read at path this session and consumed (spans): d66a922c §0,
§2.2 (KB1-KB4: the swap law, parity localization, the quartet kill vs the
composite doubling), §2.4 (KF1 thresholds; KF2 the odd-trace kill worth
exactly one power; the o-2-OBS even-integrand refusal), §3.1 (KC1 channel
identities), §4 n-1/n-2, §5 flag block; 4ebe6b8f §0 (the obstruction
"real and unpassable"; the corrected 3/2 symbol-degree gap form);
80db260f §0, CS-1..CS-9, §3.3 (the op-grade chain and the rank x op
conversion; WB6 = r-3 D10), §8-§9 (ledger/toy conventions); 9a295498 §0
(site (ii) PARTIAL: |tr|, ||.||_2 <= 2 sqrt3 sqrt(r_n E_n) = 12 sqrt2
n^{7/2}/ell; E_n = 6n^4/ell^2 EXACT (FB5); sigma_{Q,2}(r) closed form
(FB6); FC-1 bulk-term correction carried); 6997ff61 §0 (the involution
facts (i)-(vi); the linear rate; the mixed-scheme remark); e5b0879b §2.1
(the adopted clause; the CHECK-VERIFIED identification "the ALL-PAIRS
instance Delta^{Sigma,all}_n(a) IS the assembled X_n of the discharged
chain — U(0) and F each weight-summed ... all a-orders included"; the
discharged bound sup_n ||X_n||_2 <= ||X||_2 <= M at its declared grade),
§4.1-§4.2 (W-1..W-3; the BAR); cbe6ff4f §0; 48ecdabe §0 (obligation 1
re-posed at the three named non-coincidence routes; obligation 3
SURVIVES-VIA the summed quantifier; the corroborated per-pair/summed
split); 798de0dd §2.1 (F~ = F forced at bytes; the G-array; Delta^{[1]}
display), §0 (X-3 branch-diagonal pairs respond prop. to (a_+ - a_-);
X-5; the equal-source collapse); 2e4e5163 §0, §2.1 (the X display; the
trilemma form ||Pi X Pi||_2 <= ||X||_2), §2.2-§2.3 (the (o,c) floors —
weight-summed statements, hence VERBATIM for this artifact's object;
"ODD n DIES EXACTLY"; "There is NO (c = 1, o = 0) class anywhere"; the
J-vertex longitudinal intraband part -khat_x C(k) with NO coincidence
zero), §3.1-§3.3 (degree-(-1) => HS^2 linear; the DOUBLE degree-0 kill;
the V_2 (x) V_2 leading stratum with x a parameter), §3.5, §5 (grade
e-i/e-ii, overturn conditions); 6e81ae92 :320-360 (the X display; F :=
-i int_0^1 sum_l w_l W_l(1,t') J(t') W_l(t',0) dt'; the stencil identity
and S J S = -J; sigma_intra(X) = -i a_Delta khat_x b~_P C(k)); a7969f0c
§5 (SURVIVOR_FATE = CANCELS; the V_2 subsidiary -i V_2(x)/(2|k|));
789338ad :75-128 (A1/A2 at bytes); 0a10c030 §0 (the SPLIT-BY-OBJECT
ruling; the one-way bridge; F'-14); 9fdc3d1c + 549362d4 §0 (p = -3 at the
polydisc-sup quantifier, audited). "Q-..." tokens inside the sealed stock
(Q-1042, Q-1048, Q-1054, Q-1079, Q-1089): EXPECTED-UNLOCATABLE per the
tasking; noted, never chased, nothing defaulted.

---

## 2. THE SUMMED OBJECTS, EXACTLY

### 2.1 The per-composite wall (the state, cited)

Of record (d66a922c §4 n-1, audited 4ebe6b8f): per (n, cell), the wall is
the n-uniform enclosure of FOUR definite-signed real quantities of the
equal-source cross composite K_n = C(g_{+-} + g_{-+}^dag)C:

```text
  tr K_H ,  tr H_A ,  ||K_H||_2^2 ,  ||H_A||_2^2 ,
  K_H = even-insertion strata (k >= 2), H_A = -i K_A = odd-insertion
  strata (k >= 1), T_n = (a^2/8)[(tr K_H)^2 - (tr H_A)^2 - ||K_H||_2^2
  + ||H_A||_2^2] EXACTLY,
```

all four UNDECIDED, the obstruction "real and unpassable on the sealed
basis": every sealed kill is a WEIGHT-SUM property structurally absent
from the single composite (the odd strata DOUBLE instead, KB4/KF3), the
coincidence escape DEAD (9fdc3d1c p = -3, audited), the sealed budgets
polynomially divergent (rank x op 8n^3, WBC F1 = MO-3).

### 2.2 The summed re-quantification (the object, defined from bytes)

The record owns ONE summed object at this slot, and its identification is
CHECK-VERIFIED of record (e5b0879b §2.1 D-NOW-1): the all-pairs instance
`Delta^{Sigma,all}_n(a)` IS the assembled X_n of the discharged chain,

```text
  X = a_+ C U(0)^dag F C + a_- C F~^dag U(0) C + O(a^2) ,
  U(0) = sum_l w_l u_l(0) ,
  F    = -i int_0^1 sum_l w_l W_l(1,t') J(t') W_l(t',0) dt' ,
  F~ = F  (FORCED at bytes, 798de0dd §2.1: one per-branch family),
```

(REM §2.1 / BL2STAR :337-343 at bytes). THE SUMMED COMPOSITE of this
commission is its equal-source [a^1] slot, per (n, cell):

```text
  K^Sigma_n := [a^1] X_n(a,a) = C( U(0)^dag F + F^dag U(0) )C .
```

Two exact structural facts pin this as the faithful summed
re-quantification of the wall's composite [DERIVED, CAS WS2a/WS2b at the
slot-model grade, §7 T-1]:

```text
(d-1) FACTORIZATION = ALL-PAIRS ASSEMBLY: coefficientwise in the
      coupling, C(U(0)^dag F + F^dag U(0))C = sum_{mu,lambda} w_mu
      w_lambda C(G_{mu lambda} + G_{lambda mu}^dag)C — the summed object
      is EXACTLY the weight-summed family of the wall's per-pair
      composite shapes (G_{mu lambda} = u_mu(0)^dag F_lambda, the
      G-array of record). Onto the record strata: the (o,c) strata
      taxonomy of REM §2.2-§2.3 is stated FOR the weight-summed object —
      it applies to K^Sigma VERBATIM, floors and all.
(d-2) SELF-PERFORMED X-5: the bare stratum enters the assembly with
      total weight m_0^2 = 0 — the summed object with DRESSED g's equals
      the bare-inclusive one identically. The weight sum itself performs
      the dressed-only subtraction; no bare divergence class attaches
      (the same X-5 fact the wall's composite needed by construction).
```

### 2.3 The four quantities at the summed quantifier

```text
  K_H^Sigma := (K^Sigma + K^Sigma dag)/2 ,   H_A^Sigma := -i(K^Sigma - K^Sigma dag)/2 ,
  the four: tr K_H^Sigma , tr H_A^Sigma , ||K_H^Sigma||_2^2 , ||H_A^Sigma||_2^2 ,
  per (n, cell), n-uniformity the tasked grade — SUM FIRST, THEN QUANTIFY.
```

This is the summed quantifier of the precedent record (the C4 double
degree-0 kill paid its power at the summed level; alpha' = 1/4 was
delivered on the similarity class; OB-3's response side survives summed)
and the direction the one-way bridge permits (0a10c030: per-pair content
feeds the summed object, never the reverse).

---

## 3. THE ODD SECTOR: TWO EXACT KILLS — tr H_A^Sigma AND ||H_A^Sigma||_2^2 DECIDED

[DERIVED — unconditional polynomial/matrix identities at the slot-model
grade (§7 T-1), instantiating sealed displays; CAS WS2c/WS2d/WS2f.]

```text
(k-1) TRANSPOSE-CLOSURE (kills the anti-Hermitian sector): the
      equal-source summed assembly pairs G_{mu lambda} with
      G_{lambda mu}^dag at EQUAL weight (w_mu w_lambda = w_lambda w_mu),
      so K^Sigma is HERMITIAN IDENTICALLY — entrywise, as a polynomial
      in the coupling (WS2c). Hence
        H_A^Sigma = 0 ,  tr H_A^Sigma = 0 ,  ||H_A^Sigma||_2^2 = 0 ,
      per (n, cell), EVERY n — not enclosed: ZERO.
(k-2) THE PER-FACTOR ODD-KILL (kills the odd-insertion grading): each
      branch factor's weight sum annihilates every odd function of
      lambda (m_1 = m_3 = 0 — the symmetric-second-difference weights,
      exact at full tau_R; REM §2.3 "ODD n DIES EXACTLY"), so K^Sigma is
      EVEN in the coupling: its odd-insertion strata vanish
      coefficientwise (WS2d). Consistent with (k-1) through the sealed
      parity localization (KB2: odd strata = anti-Hermitian
      combinations); the two mechanisms are INDEPENDENT grounds for the
      same zero.
(k-3) THE CONTRAST (what the per-composite level refuses, re-pinned):
      the single cross composite's odd stratum [m^1]K^{(+-)} =
      C(h - h^dag)C, h = U_1^dag Ph_0 - U_0^dag Ph_1, is ANTI-HERMITIAN
      and generically NONZERO (exhibit displayed in §8 output), and the
      cross difference DOUBLES: [m^1](g_{+-} - g_{-+}) = 2h (KB4
      re-pin, WS2f). The odd sector the summed level annihilates is
      exactly the sector the per-composite level doubles.
```

VERDICTS (quantities 2 and 4 of the wall, at the summed quantifier):

```text
  tr H_A^Sigma      = 0   DERIVED — EXACT CANCELLATION, identity grade.
  ||H_A^Sigma||_2^2 = 0   DERIVED — EXACT CANCELLATION, identity grade
                          (the operator itself vanishes; nothing to enclose).
```

This is the tasked question's core answered affirmatively: the zero-sum
weight structure + the parity grading DO yield, at the summed quantifier,
the exact cancellation the per-composite level provably refuses.

---

## 4. THE EVEN HS QUANTITY: ||K_H^Sigma||_2^2 ENCLOSED n-UNIFORMLY, CARRIER-FREE

The chain, every step sealed or CAS-pinned, in the allowed direction:

```text
(h-1) By (k-1) and HS Pythagoras (WS3b): ||K_H^Sigma||_2^2 =
      ||K^Sigma||_2^2 (the anti-Hermitian share is zero).
(h-2) THE SEALED SUPPLY, at its own quantifier: B-L2* is DISCHARGED at
      the summed level (REM NET, CONFIRMED of record):
      sup_n ||X_n(a)||_2 <= ||X(a)||_2 <= M on the closed pair polydisc
      |a_+-| <= eps, M SYMBOLIC and CARRIER-FREE. THE DECIDING STEP IS
      THE TASKED KILL MECHANICS CONSUMED AT ITS SEALED QUANTIFIER: the
      C4/REM DOUBLE degree-0 kill (m_0 = 0 applied ONCE PER FACTOR —
      [U(0)]_0 = 0 and [F]_0 = 0, both of record) lifts the assembled
      object to degree -2, past the HS threshold 2p = 4 > 3 (WS4; REM
      §3.1-§3.2). The identification Delta^{Sigma,all}_n = X_n is
      CHECK-VERIFIED of record (e5b0879b §2.1).
(h-3) THE SLOT EXTRACTION, exact: X(a,a) is given by the dominated
      a-series on the closed polydisc (REM §3.5, sealed; the equal-source
      diagonal lies inside it), and the [a^1] coefficient is the EXACT
      contour average at radius eps (WS3a, CAS-pinned on the generic
      cubic; the operator-valued step is the same identity plus the
      triangle inequality for the Bochner integral — named standard
      steps, eps symbolic):
        ||K^Sigma_n||_2 = ||[a^1] X_n(a,a)||_2 <= M/eps .
(h-4) VERDICT: ||K_H^Sigma,n||_2^2 <= M^2/eps^2, n-UNIFORM, CARRIER-FREE,
      per unit cell — DERIVED(enclosure; grade INHERITED: REM's
      identity-grade core with estimate-grade steps e-i/e-ii at their
      sealed grade, overturn conditions inherited as sealed; the cell
      axis rides the sealed covariance at the ray quantifier,
      CONDITIONAL(P-1), exactly as of record).
```

No V-2 bridge is crossed: nothing is extracted FROM the summed sup to any
pair; the summed object is quantified AS ITSELF. This is the enclosure the
per-composite level provably refuses (the wall's s-i named B-L2* "NOT
CONSUMABLE" for K_n; for K^Sigma it is the estimate's OWN object).

DELIVERED ALONGSIDE (operator grade, the WBC chain transported): the
weights are l1-TIGHT (sum_l |w_l| = 1, WS1), each u_l(0) unitary, and
||F||_op <= sum_l |w_l| int_0^1 ||J(t)||_op dt <= 1 (b_D <= 1 of record),
so by the same named steps as WBC §3.3:

```text
  ||U(0)||_op <= 1 ,  ||F||_op <= 1 ,  ||K^Sigma_n||_op <= 2  (n-free),
  hence the rank-grade ceilings |tr K^Sigma_n| <= 8n^3 ,
  ||K^Sigma_n||_2^2 <= 16n^3 — dominated by (h-4)'s carrier-free bound
  on the HS side, and still the F1 = MO-3 stop on the trace side (§5).
```

---

## 5. THE EVEN TRACE: tr K_H^Sigma — FAILS-AT, BUT LOG-THIN (THE SHORTFALL COLLAPSES FROM TWO-PLUS POWERS TO A LOGARITHM)

### 5.1 The summed power ledger (what the summed level pays)

```text
(t-1) POWER ONE AND TWO — THE DOUBLE m_0 KILL (of record, consumed at
      its sealed quantifier): both branch factors lose their degree-0
      intraband content exactly, so the assembled K^Sigma starts at
      diagonal degree -2 (REM §3.2: "THE ASSEMBLED REMAINDER STARTS AT
      DEGREE -2"); the per-composite leading strata sat at degree -1
      (o-2-OBS). The summed level's leading stratum is the V_2 (x) V_2
      class (o = 2, c = 0) — REM §3.3 at bytes — and it is the ONLY
      degree -2 assembled stratum: the per-factor floors are
      weight-summed statements applying VERBATIM here (d-1) — a
      coincidence factor forces either c >= 2 per factor (that factor
      degree <= -2, the OTHER factor degree <= -1 post-kill: assembled
      <= -3) or an interband block (floor 2o + c >= 3 per factor:
      assembled <= -3); "There is NO (c = 1, o = 0) class anywhere."
(t-2) POWER THREE — THE ANGULAR KILL ENGAGES (new, the summed
      quantifier's own purchase): the leading stratum's diagonal symbol
      is the product of the two V_2-forms with the J vertex's
      longitudinal intraband part:
        U-side  -i V_2(x)/(2|k|)      (angular-SCALAR, sealed subsidiary)
        F-side  +i V_2-form/(2|k|) x (-khat_x C(k))   (the J vertex's
                intraband part, EXACT at bytes, no coincidence zero)
      — sigma-shape confirmed by BL2STAR's own display sigma_intra(X) =
      -i a_Delta khat_x b~_P C(k): the khat_x factor is STRUCTURAL. The
      degree -2 diagonal integrand therefore carries EXACTLY ONE khat_x
      against angular-scalar factors: ODD under khat -> -khat (WS5), and
      its full angular average VANISHES EXACTLY — int_S2 khat_x dOmega
      = 0, the S1 odd-spinor-trace leg (KF2 of record), worth EXACTLY
      ONE power (second moment 4pi/3 != 0). THE CONTRAST, re-pinned
      (WS5): the per-composite (1,0) single-J5-stay integrand
      khat_x(khat_x - khat'_x) is EVEN under the joint inversion — the
      kill is REFUSED there (o-2-OBS of record). The summed level is
      where this leg finally engages.
(t-3) THE LANDING: the entire degree -2 diagonal stratum contributes
      ZERO to the trace's angular integral; every surviving diagonal
      contribution has degree <= -3; at degree exactly -3 the carrier
      integral is log Lambda (WS4, exact, Lambda FORMAL). The trace
      threshold is STRICT (degree < -3, KF1): NOT crossed.
```

### 5.2 The remaining summed assets, each to its exact end

```text
(s-1) THE CHIRAL INVOLUTION (MO-2, consumed at audited grade + WS6):
      beta is EXACTLY GENERATOR-ODD — beta h_lambda(t;a) beta =
      -h_lambda(t;a) TERMWISE (h_0 massless; beta S beta = -S for
      S = -i gamma^0 gamma^5, CAS; beta J beta = -J; M(t), b_D, Q_n, P
      beta-commuting), and beta C(p) beta = I - C(p) (the sealed sea
      symbol), beta C_n beta = Q_n - C_n - (rank <= 4 correction) (MO-2
      (vi)). Consequence: conjugation maps the summed trace's sea-band
      content to POSITIVE-band content under the CONJUGATE dynamics,
      modulo the rank <= 4 correction — an exact MIRROR between strata.
      It GRADES; it does not kill (the RA4b/CA3 typing, re-confirmed):
      no composite trace kill follows on the swept basis.
(s-2) THE STENCIL WEIGHT (BL2STAR I13/I14, re-pinned WS7): the
      collapse-level F-weight c(theta) = sin^2(sqrt2 theta/2) is a
      SQUARE — formally >= 0, vanishing only quadratically at t' = 1/2:
      the summed F-factor carries NO sign resource for a trace
      cancellation beyond (t-2)'s angular zero.
(s-3) THE SEALED TRACE BUDGETS: FHB's form-to-HS conversion at the
      wall's own compressions gives |tr| <= 2 sqrt3 sqrt(r_n E_n) =
      12 sqrt2 n^{7/2}/ell with E_n = 6n^4/ell^2 EXACT (F3 generator
      class, per (n, cell)) — polynomially DIVERGENT; the rank x op
      ceiling gives 8n^3 (§4) — polynomially DIVERGENT; the r-3 pair
      PROVED rank x op cannot close a carrier-uniform estimate (WBC
      WB6 = D10). No sealed n-uniform trace majorant exists at any
      stratum of the summed object; this enumeration claims only the
      displayed sweep (§9).
(s-4) MO-3's p = -3 (9fdc3d1c, audited): the coincidence-vanishing rate
      has NO purchase on the c = 0 leading stratum, and no resummation
      inside the coincidence strata resurrects degree-0 content (REM
      NET) — the coincidence escape is dead at the summed level too,
      exactly as tasked.
```

### 5.3 Verdict and the exact residue

```text
  tr K_H^Sigma :  FAILS-AT( the log threshold — the summed level pays
    the double m_0 kill (two powers relative to the per-composite
    leading count) AND engages the angular kill (one more, where the
    per-composite integrand refuses it), landing the trace count at
    effective diagonal degree -3 = log Lambda; the threshold (< -3) is
    strict; no swept sealed asset crosses it. NOT-DERIVABLE-TODAY
    blockers, vs the new structure: (i) a certified log-free rate for
    the degree-3 diagonal class of the summed assembly — one radial
    power, or one more angular/cancellation structure, on the o >= 2
    segment-corrected strata — verbatim MO-3's shape, now LOG-THIN;
    (ii) every sealed budget polynomial (s-3); (iii) the involution
    grades, the stencil is positive (s-1/s-2). NOT REFUTED: no
    divergence of tr K^Sigma_n is exhibited; the quantity is UNDECIDED
    with a quantified, logarithmic shortfall. )
```

---

## 6. THE SIGN LEDGER AT THE SUMMED QUANTIFIER (DELIVERED MEANWHILE)

[DERIVED, CAS WS8 — unconditional matrix identities.] With H_A^Sigma = 0:

```text
  Re e_2(K^Sigma) = e_2(K^Sigma) = (1/2)(tr K^Sigma)^2 - (1/2)||K^Sigma||_2^2 ,
```

the wall's FOUR-quantity difference collapses to a TWO-quantity Hermitian
form: the between-sector sign indeterminacy (d66a922c n-2: "the
indeterminacy is between sectors") is GONE at the summed quantifier — only
the within-Hermitian comparison (tr)^2 vs ||.||_2^2 remains. And one side
is now enclosed:

```text
  e_2(K^Sigma) + (1/2)||K^Sigma||_2^2 = (1/2)(tr K^Sigma)^2  >= 0 formally
  (the square of a REAL symbol), hence with §4:
  e_2(K^Sigma_n) >= -(1/2)||K^Sigma_n||_2^2 >= -M^2/(2 eps^2)   (every n)
```

— the NEGATIVE channel of the summed second-order form is carrier-uniformly
enclosed by the HS enclosure alone; the entire remaining openness of the
summed quantifier is the ONE nonnegative number (tr K^Sigma_n)^2. This is
the tasked "definite-sign resolution" disjunct delivered ONE-SIDEDLY:
PARTIAL, exact, one-sided by construction.

---

## 7. QUANTIFIER-SHIFT HONESTY: WHAT THE SUMMED LEVEL DOES NOT DECIDE

[DERIVED, CAS WS9 + sealed bytes.]

```text
(q-1) SUM-THEN-QUANTIFY vs QUANTIFY-THEN-SUM: the summed level decides
      the four quantities OF THE SUMMED OBJECT. It provably does NOT
      decide the c-weighted sums of per-pair quantities: squares are
      PARITY-BLIND — the swap flips the odd sector (H_A^{(-+)} =
      -H_A^{(+-)} entrywise; branch-diagonal composites Hermitian), so
      the c-weighted LINEAR sum of per-pair odd sectors is ZERO while
      the c-weighted sum of SQUARES survives as (1/4)(tr H_A^{(+-)})^2
      (WS9): the zero-sum weights cannot see squared per-pair content.
(q-2) THE WALL STANDS AT ITS OWN QUANTIFIER: the equal-source survivor
      of record is per-composite (T_n = (a^2/4) Re e_2(K_n), the cross
      composite, 798de0dd); transporting any summed decision back to
      K_n's four quantities is the summed-to-per-pair bridge V-2
      refuses and the one-way quantifier record forbids (0a10c030).
      NOT CROSSED, in either direction. The per-composite UNDECIDED
      verdicts of d66a922c/4ebe6b8f are UNTOUCHED.
(q-3) BEARING ON T_n: NONE. Bearing on the adopted amendment, the
      alpha' = 1/4 record, B-L2*, MO-2, MO-3, the p = -3 record, and
      every sealed identity: NONE at their own quantifiers — B-L2* is
      CONSUMED (the allowed direction), not moved; no falsifier V-1..V-6
      engages (V-5's C4-kill-before-the-norm clause is respected — the
      kill IS the consumed deciding step; V-2 not crossed per (q-2)).
```

WHAT THE ROUTE CHANGES (descriptive only, IF these results hold): at the
summed quantifier the wall's four-quantity program is ONE number — the
even trace tr K^Sigma_n — with a LOGARITHMIC shortfall; the other three
are decided (two exactly zero, one enclosed carrier-free). RFB
obligation-1's route (b) is thereby EXECUTED to its exact frontier: what
remains of it is MO-3's log-thin residue (§5.3), named for any successor.

---

## 8. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `wsrvenv` under the session scratchpad; nothing
written to the workspace but this artifact and its seal. Exact symbolic /
exact rational/surd only; nothing numeric evaluated. ONE battery, 14
checks; labels WS1-WS9 cited throughout. The disclosed harness-form
correction (header) preceded the single completed run.

```python
# WSR-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv wsrvenv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated.
# Groups: WS1 census/weights/moments re-pin; WS2 the SUMMED assembly (factorization,
#   self-performed X-5, Hermiticity, insertion-parity evenness, the per-composite
#   contrast: the doubled odd strata); WS3 the [a^1] Cauchy slot extraction (exact
#   integral identity) + the HS parity Pythagoras; WS4 threshold arithmetic (KF1
#   license, carrier radius Lambda FORMAL); WS5 the angular kill's ENGAGEMENT at the
#   summed leading stratum vs its refusal per-composite; WS6 the chiral involution
#   (explicit Dirac algebra; beta h beta = -h termwise; beta C(p) beta = I - C(p));
# WS7 the stencil positivity c(theta) = sin^2(sqrt2 theta/2) re-pin; WS8 the summed
#   four-quantity form and the one-sided sign enclosure; WS9 squares are parity-blind
#   (the quantifier-shift honesty: sum-then-quantify vs quantify-then-sum).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def e2(M):
    return sp.expand(((M.trace())**2 - (M*M).trace())/2)

def hs2(M):
    return sp.expand((M*M.H).trace())

def cmat(pre, n, m=None):
    m = n if m is None else m
    return sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im*sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

def Zq(M):
    return sp.expand(M) == sp.zeros(*M.shape)

def mco(M, var, k):
    return M.applyfunc(lambda e: sp.expand(e).coeff(var, k))

# ===== WS1 — census/weights/moments re-pin (KA1/R1 of record) =====
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
ph = [1, -1, -1]
surv = [(i, j) for i in range(3) for j in range(3) if ph[i]*ph[j] == 1]
N = sum(w_v[i]*w_v[j] for (i, j) in surv)
c_w = {p: sp.nsimplify(w_v[p[0]]*w_v[p[1]]/N) for p in surv}
mom = lambda k: sp.nsimplify(sum(w*l**k for w, l in zip(w_v, lam_v)))
ok("WS1 census/weights/moments: surv = {(0,0)} + quartet, c = (1/2, 1/8 x 4) ALL"
   " POSITIVE (convexity footing), sum c = 1; m_0 = m_1 = m_3 = 0 (the zero-sum"
   " kills), m_2 = -1, m_4 = -2 (the even moments are NONZERO: the even strata"
   " SURVIVE the weight sum); w_+ = w_- (the transpose symmetry of the census)",
   sorted(surv) == [(0, 0), (1, 1), (1, 2), (2, 1), (2, 2)] and N == R(1, 2)
   and c_w[(0, 0)] == R(1, 2) and all(c_w[p] == R(1, 8) for p in surv if p != (0, 0))
   and all(c_w[p] > 0 for p in surv) and sum(c_w.values()) == 1
   and mom(0) == 0 and mom(1) == 0 and mom(2) == -1 and mom(3) == 0 and mom(4) == -2
   and w_v[1] == w_v[2])

# ===== WS2 — THE SUMMED ASSEMBLY: the record's own weight-summed composite =====
# Abstract Dyson-slot model (the KB1 parent form of record): three branches with
# coupling values (0, m, -m), branch-INDEPENDENT slot operators (the lambda-linear
# sealed generator); u_l and F_l the branch factor slot series; C the compression.
m_ = sp.Symbol('m_c', real=True)
NN = 3
U_k = [cmat(f'U{k}', 3) for k in range(NN + 1)]
Ph_k = [cmat(f'Ph{k}', 3) for k in range(NN + 1)]
lamv = [sp.Integer(0), m_, -m_]
u_l = [sum((lamv[l]**k*U_k[k] for k in range(NN + 1)), sp.zeros(3, 3)) for l in range(3)]
F_l = [sum((lamv[l]**k*Ph_k[k] for k in range(NN + 1)), sp.zeros(3, 3)) for l in range(3)]
C3 = sp.diag(1, 1, 0)
G0 = sp.expand(U_k[0].H*Ph_k[0])
# (a) FACTORIZATION: the all-pairs weighted equal-source [a^1] assembly equals the
#     product of the two weight-summed branch factors (U(0)^dag F + F^dag U(0)):
A_pairs = sp.expand(sum((sp.nsimplify(w_v[mu]*w_v[la])*(u_l[mu].H*F_l[la] + F_l[mu].H*u_l[la])
                         for mu in range(3) for la in range(3)), sp.zeros(3, 3)))
Uw = sp.expand(sum((w_v[l]*u_l[l] for l in range(3)), sp.zeros(3, 3)))
Fw = sp.expand(sum((w_v[l]*F_l[l] for l in range(3)), sp.zeros(3, 3)))
A_fact = sp.expand(Uw.H*Fw + Fw.H*Uw)
ok("WS2a FACTORIZATION (all-pairs, coefficientwise in m): sum_{mu,lam} w_mu w_lam"
   " [u_mu^dag F_lam + F_mu^dag u_lam] = U(0)^dag F + F^dag U(0) with U(0) ="
   " sum_l w_l u_l, F = sum_l w_l F_l — the record's own summed assembly (REM §2.1"
   " display at the equal-source [a^1] slot, F~ = F forced of record)",
   Zq(sp.expand(A_pairs - A_fact)))
# (b) SELF-PERFORMED X-5: the bare stratum enters with total weight m_0^2 = 0 —
#     dressed and bare-inclusive summed assemblies COINCIDE:
g_d = {(mu, la): sp.expand(u_l[mu].H*F_l[la] - G0) for mu in range(3) for la in range(3)}
A_dressed = sp.expand(sum((sp.nsimplify(w_v[mu]*w_v[la])*(g_d[(mu, la)] + g_d[(la, mu)].H)
                           for mu in range(3) for la in range(3)), sp.zeros(3, 3)))
ok("WS2b SELF-PERFORMED X-5: the summed assembly with DRESSED g's equals the"
   " bare-inclusive one — the bare stratum carries total weight m_0^2 = 0: the"
   " weight sum ITSELF performs the dressed-only subtraction",
   Zq(sp.expand(A_dressed - A_pairs)))
# (c,d,e) THE TWO KILLS: Hermiticity by transpose-closure; evenness by the odd-kill.
KSig = sp.expand(C3*A_fact*C3)
HASig = sp.expand(-Im*(KSig - KSig.H)/2)
ok("WS2c HERMITICITY BY TRANSPOSE-CLOSURE: K^Sigma = C(U(0)^dag F + F^dag U(0))C"
   " is HERMITIAN IDENTICALLY (as a polynomial in m, entrywise) — the equal-source"
   " summed assembly pairs G_{mu lam} with G_{lam mu}^dag at EQUAL weight; hence"
   " H_A^Sigma := -i(K^Sigma - K^Sigma dag)/2 = 0, tr H_A^Sigma = 0 and"
   " ||H_A^Sigma||_2^2 = 0 IDENTICALLY",
   Zq(sp.expand(KSig - KSig.H)) and Zq(HASig)
   and sp.expand(HASig.trace()) == 0 and sp.expand(hs2(HASig)) == 0)
ok("WS2d INSERTION-PARITY EVENNESS: K^Sigma is EVEN in the coupling — its odd-"
   "insertion strata (orders m^1, m^3) vanish IDENTICALLY (each branch factor's"
   " weight sum kills every odd function of lambda: the m_1 = m_3 = 0 kill applied"
   " PER FACTOR, exact at full tau_R)",
   all(Zq(mco(KSig, m_, k)) for k in (1, 3)))
# (f) THE PER-COMPOSITE CONTRAST: the single cross composite's odd strata are
#     NONZERO and DOUBLED (KB4 of record, re-pinned).
Kpm = sp.expand(C3*(g_d[(1, 2)] + g_d[(2, 1)].H)*C3)
K1 = mco(Kpm, m_, 1)
h1 = sp.expand(U_k[1].H*Ph_k[0] - U_k[0].H*Ph_k[1])
ok("WS2f THE CONTRAST: [m^1] K^{(+-)} = C(h - h^dag)C with h = U_1^dag Ph_0 -"
   " U_0^dag Ph_1 — ANTI-HERMITIAN, generically NONZERO (entry (0,0) displayed"
   " below), and the cross DIFFERENCE doubles: [m^1](g_{+-} - g_{-+}) = 2h — the"
   " odd sector the summed level kills is exactly the sector the per-composite"
   " level DOUBLES (KB4 re-pin); [m^1] K^Sigma = 0 alongside",
   Zq(sp.expand(K1 - C3*(h1 - h1.H)*C3)) and Zq(sp.expand(K1 + K1.H))
   and (not Zq(K1))
   and Zq(sp.expand(mco(sp.expand(g_d[(1, 2)] - g_d[(2, 1)]), m_, 1) - 2*h1))
   and Zq(mco(KSig, m_, 1)))
print("WS2f_EXHIBIT [m^1]K^{(+-)}[0,0] =", sp.simplify(K1[0, 0]))

# ===== WS3 — the [a^1] Cauchy slot extraction + the HS parity Pythagoras =====
a_, eps_, t_ = sp.Symbol('a'), sp.Symbol('epsilon', positive=True), sp.Symbol('t', real=True)
c0, c1, c2, c3 = [sp.Symbol(f'c{k}') for k in range(4)]
f_a = c0 + c1*a_ + c2*a_**2 + c3*a_**3
cauchy = sp.integrate(f_a.subs(a_, eps_*sp.exp(Im*t_))*sp.exp(-Im*t_)/eps_,
                      (t_, 0, 2*sp.pi))/(2*sp.pi)
ok("WS3a THE CAUCHY SLOT IDENTITY (exact symbolic integral, generic cubic,"
   " complex coefficients): c_1 = (1/2pi) int_0^{2pi} f(eps e^{it}) e^{-it}/eps dt"
   " — the [a^1] slot is an EXACT contour average at radius eps; the enclosure"
   " ||[a^1]X||_2 <= M/eps is this identity + the triangle for the Bochner"
   " integral (named standard steps; eps SYMBOLIC)",
   sp.simplify(cauchy - c1) == 0)
M3 = cmat('m3', 3)
ok("WS3b HS PARITY PYTHAGORAS (generic 3x3): ||M||_2^2 = ||M_H||_2^2 +"
   " ||M_A||_2^2 — the Hermitian/anti-Hermitian split is HS-orthogonal; with"
   " H_A^Sigma = 0 the whole summed HS mass is the even sector's",
   sp.expand(hs2(M3) - hs2((M3 + M3.H)/2) - hs2((M3 - M3.H)/2)) == 0)

# ===== WS4 — threshold arithmetic (KF1 license: Lambda a FORMAL symbol) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
tr_int = {p: sp.integrate(kk**(2 - p), (kk, 1, Lam)) for p in (1, 2, 3, 4)}
hs_int = {p: sp.integrate(kk**(2 - 2*p), (kk, 1, Lam)) for p in (1, 2)}
ok("WS4 THRESHOLD ARITHMETIC (exact): trace side int_1^Lam k^2 k^-p dk ="
   " (Lam^2-1)/2 [p=1], Lam-1 [p=2], log Lam [p=3], 1-1/Lam [p=4]; HS side"
   " int k^2 k^-2p dk = Lam-1 [p=1], 1-1/Lam [p=2] — the summed degree -2 start"
   " (the double m_0 kill of record) PASSES the HS threshold (2p = 4 > 3:"
   " bounded) and FAILS the trace threshold (p = 2 < 3: linear); with ONE more"
   " power (the angular kill, WS5) the trace count lands AT p = 3: log Lam —"
   " a LOGARITHMIC shortfall, threshold strict",
   sp.simplify(tr_int[1] - (Lam**2 - 1)/2) == 0
   and sp.simplify(tr_int[2] - (Lam - 1)) == 0
   and sp.simplify(tr_int[3] - sp.log(Lam)) == 0
   and sp.simplify(tr_int[4] - (1 - 1/Lam)) == 0
   and sp.simplify(hs_int[1] - (Lam - 1)) == 0
   and sp.simplify(hs_int[2] - (1 - 1/Lam)) == 0)

# ===== WS5 — the angular kill: ENGAGED summed, REFUSED per-composite =====
th, phv = sp.symbols('theta_a phi_a', real=True)
ang1 = sp.integrate(sp.sin(th)*sp.cos(phv)*sp.sin(th), (phv, 0, 2*sp.pi), (th, 0, sp.pi))
ang2 = sp.integrate((sp.sin(th)*sp.cos(phv))**2*sp.sin(th), (phv, 0, 2*sp.pi), (th, 0, sp.pi))
kx, ky, kz, kxp, kyp, kzp = sp.symbols('kh_x kh_y kh_z khp_x khp_y khp_z', real=True)
V2s = sp.Symbol('V2_x', real=True)   # the V_2(x) scalar (angular-free, sealed subsidiary)
I_sum = V2s**2*kx                    # the summed leading diagonal stratum: ONE khat_x
I_pc = kx*(kx - kxp)                 # the per-composite (1,0) stay stratum integrand shape
inv = {kx: -kx, ky: -ky, kz: -kz, kxp: -kxp, kyp: -kyp, kzp: -kzp}
ok("WS5 THE ANGULAR KILL'S ENGAGEMENT: int_S2 khat_x dOmega = 0 EXACT (first"
   " moment; the S1 odd-spinor-trace leg, KF2 of record), second moment 4pi/3 !="
   " 0 (worth EXACTLY ONE power); the SUMMED leading diagonal stratum (V_2 (x)"
   " V_2, o = 2, c = 0 — the ONLY degree -2 assembled stratum, REM §3.3) carries"
   " ONE longitudinal khat_x against angular-SCALAR V_2 factors: ODD under"
   " inversion — the kill ENGAGES; the per-composite (1,0) single-J5-stay"
   " integrand khat_x(khat_x - khat'_x) is EVEN under joint inversion — the kill"
   " is REFUSED there (o-2-OBS of record, re-pinned)",
   ang1 == 0 and sp.simplify(ang2 - 4*sp.pi/3) == 0
   and sp.expand(I_sum.subs(inv, simultaneous=True) + I_sum) == 0
   and sp.expand(I_pc.subs(inv, simultaneous=True) - I_pc) == 0)

# ===== WS6 — the chiral involution (MO-2's beta), termwise on the generator =====
s1p = sp.Matrix([[0, 1], [1, 0]])
s2p = sp.Matrix([[0, -Im], [Im, 0]])
s3p = sp.Matrix([[1, 0], [0, -1]])
I2, Z2 = sp.eye(2), sp.zeros(2, 2)
blk = lambda A, B, Cc, D: sp.Matrix(sp.BlockMatrix([[A, B], [Cc, D]]))
beta = blk(I2, Z2, Z2, -I2)                       # gamma^0
al = [blk(Z2, s, s, Z2) for s in (s1p, s2p, s3p)] # alpha_j
g5 = blk(Z2, I2, I2, Z2)                          # gamma^5
S_op = sp.expand(-Im*beta*g5)                     # S = -i slash(n) gamma^5, slash(n) = gamma^0
px, py, pz = sp.symbols('p_x p_y p_z', real=True)
sV, sJ = sp.symbols('s_V s_J', real=True)         # lambda v(t) M-slot and -a b_D-slot (real scalars, beta-commuting)
h_gen = sp.expand(px*al[0] + py*al[1] + pz*al[2] + sV*S_op + sJ*al[0])
Cp = sp.expand((sp.eye(4) - (kx*al[0] + ky*al[1] + kz*al[2]))/2)
ok("WS6 THE CHIRAL INVOLUTION IS EXACTLY GENERATOR-ODD: S = -i gamma^0 gamma^5"
   " is Hermitian, S^2 = I, {S, alpha_x} = 0 (the stencil's own ground); beta"
   " anticommutes with every alpha_j AND with S; hence beta h_lambda(t; a) beta"
   " = -h_lambda(t; a) TERMWISE (h_0 massless; the S-vertex; the J vertex —"
   " M(t), b_D, Q_n, P all beta-commuting spatial scalars/projectors); and"
   " beta C(p) beta = I - C(p) for the sealed sea symbol (MO-2's conversion):"
   " the involution GRADES (sea band <-> positive band, conjugate dynamics,"
   " modulo MO-2's rank <= 4 kernel correction) — it is NOT a composite kill",
   sp.expand(S_op - S_op.H) == sp.zeros(4, 4) and sp.expand(S_op*S_op) == sp.eye(4)
   and Zq(sp.expand(S_op*al[0] + al[0]*S_op))
   and all(Zq(sp.expand(beta*A + A*beta)) for A in al)
   and Zq(sp.expand(beta*S_op + S_op*beta))
   and Zq(sp.expand(beta*h_gen*beta + h_gen))
   and Zq(sp.expand(beta*Cp*beta - (sp.eye(4) - Cp))))

# ===== WS7 — the stencil positivity re-pin (BL2STAR I13/I14 of record) =====
th0, th1 = sp.symbols('theta_0 theta_1', real=True)
def expS(c):
    return sp.expand(sp.cos(c)*sp.eye(4) - Im*sp.sin(c)*S_op)   # S^2 = I
Jm = al[0]   # the J vertex's spinor factor (alpha_x type; scalars carried outside)
sten = sp.expand(w_v[0]*Jm
                 + w_v[1]*expS(sp.sqrt(2)*th1)*Jm*expS(sp.sqrt(2)*th0)
                 + w_v[2]*expS(-sp.sqrt(2)*th1)*Jm*expS(-sp.sqrt(2)*th0))
c_form = (1 - sp.cos(sp.sqrt(2)*(th1 - th0)))/2
ok("WS7 THE STENCIL: sum_l w_l e^{-i lam_l th_1 S} J e^{-i lam_l th_0 S} ="
   " c(th_1 - th_0) J with c(th) = (1 - cos(sqrt2 th))/2 = sin^2(sqrt2 th/2)"
   " >= 0 FORMALLY (1 - cos = 2 sin^2, a square): the collapse-level F-weight"
   " is POSITIVE — the summed level's F-factor carries NO sign resource, only"
   " kills (S J S = -J the deciding ground, verified inside)",
   Zq(sp.expand(sten - c_form*Jm).applyfunc(sp.simplify))
   and sp.simplify(c_form - sp.sin(sp.sqrt(2)*(th1 - th0)/2)**2) == 0
   and Zq(sp.expand(S_op*Jm*S_op + Jm)))

# ===== WS8 — the summed four-quantity form + the one-sided sign enclosure =====
H3r = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'hr_{min(i,j)}{max(i,j)}', real=True))
H3i = sp.Matrix(3, 3, lambda i, j: sp.sign(j - i)*sp.Symbol(f'hi_{min(i,j)}{max(i,j)}', real=True))
H3 = H3r + Im*H3i   # generic Hermitian
ok("WS8 THE SUMMED FORM: for Hermitian K^Sigma (H_A^Sigma = 0): Re e_2(K^Sigma)"
   " = e_2(K^Sigma) = (1/2)(tr K^Sigma)^2 - (1/2)||K^Sigma||_2^2 EXACTLY — the"
   " wall's FOUR-quantity difference collapses to a TWO-quantity Hermitian-"
   "sector form at the summed quantifier (the between-sector indeterminacy is"
   " GONE), and e_2(K^Sigma) + (1/2)||K^Sigma||_2^2 = (1/2)(tr K^Sigma)^2, the"
   " square of a REAL symbol: the NEGATIVE channel of the summed e_2 is"
   " enclosed by the HS enclosure ALONE (one-sided, exact)",
   Zq(sp.expand(H3 - H3.H))
   and sp.expand(e2(H3) - ((H3.trace())**2/2 - hs2(H3)/2)) == 0
   and sp.expand((e2(H3) + hs2(H3)/2) - (H3.trace())**2/2) == 0
   and sp.expand(H3.trace() - sp.conjugate(H3.trace())) == 0)

# ===== WS9 — squares are parity-blind (the quantifier-shift honesty) =====
Kmp = sp.expand(C3*(g_d[(2, 1)] + g_d[(1, 2)].H)*C3)
HA_pm = sp.expand(-Im*(Kpm - Kpm.H)/2)
HA_mp = sp.expand(-Im*(Kmp - Kmp.H)/2)
Kpp = sp.expand(C3*(g_d[(1, 1)] + g_d[(1, 1)].H)*C3)
tr_lin = sp.expand(c_w[(1, 2)]*HA_pm.trace() + c_w[(2, 1)]*HA_mp.trace()
                   + c_w[(1, 1)]*sp.expand(-Im*(Kpp - Kpp.H)/2).trace()
                   + c_w[(2, 2)]*sp.expand(-Im*(Kpp - Kpp.H)/2).trace())
ok("WS9 QUANTIFIER-SHIFT HONESTY: the swap flips the odd sector — H_A^{(-+)} ="
   " -H_A^{(+-)} entrywise, and the branch-diagonal composites are Hermitian"
   " (H_A^{(mu mu)} = 0); hence the c-weighted LINEAR sum of per-pair odd"
   " sectors is ZERO (sum-then-quantify: killed), while the c-weighted sum of"
   " SQUARES is (1/4)(tr H_A^{(+-)})^2 — squares are PARITY-BLIND: the zero-sum"
   " weights cannot kill quantify-then-sum objects, and the summed decisions do"
   " NOT transport back per-composite (the V-2-barred bridge)",
   Zq(sp.expand(HA_mp + HA_pm))
   and Zq(sp.expand(Kpp - Kpp.H))
   and sp.expand(tr_lin) == 0
   and sp.expand(c_w[(1, 2)]*(HA_pm.trace())**2 + c_w[(2, 1)]*(HA_mp.trace())**2
                 - R(1, 4)*(HA_pm.trace())**2) == 0)

print("WSR-BATTERY-DONE")
```

Output, verbatim (14/14 PASS, single completed run):

```text
WS1 census/weights/moments: surv = {(0,0)} + quartet, c = (1/2, 1/8 x 4) ALL POSITIVE (convexity footing), sum c = 1; m_0 = m_1 = m_3 = 0 (the zero-sum kills), m_2 = -1, m_4 = -2 (the even moments are NONZERO: the even strata SURVIVE the weight sum); w_+ = w_- (the transpose symmetry of the census): PASS
WS2a FACTORIZATION (all-pairs, coefficientwise in m): sum_{mu,lam} w_mu w_lam [u_mu^dag F_lam + F_mu^dag u_lam] = U(0)^dag F + F^dag U(0) with U(0) = sum_l w_l u_l, F = sum_l w_l F_l — the record's own summed assembly (REM §2.1 display at the equal-source [a^1] slot, F~ = F forced of record): PASS
WS2b SELF-PERFORMED X-5: the summed assembly with DRESSED g's equals the bare-inclusive one — the bare stratum carries total weight m_0^2 = 0: the weight sum ITSELF performs the dressed-only subtraction: PASS
WS2c HERMITICITY BY TRANSPOSE-CLOSURE: K^Sigma = C(U(0)^dag F + F^dag U(0))C is HERMITIAN IDENTICALLY (as a polynomial in m, entrywise) — the equal-source summed assembly pairs G_{mu lam} with G_{lam mu}^dag at EQUAL weight; hence H_A^Sigma := -i(K^Sigma - K^Sigma dag)/2 = 0, tr H_A^Sigma = 0 and ||H_A^Sigma||_2^2 = 0 IDENTICALLY: PASS
WS2d INSERTION-PARITY EVENNESS: K^Sigma is EVEN in the coupling — its odd-insertion strata (orders m^1, m^3) vanish IDENTICALLY (each branch factor's weight sum kills every odd function of lambda: the m_1 = m_3 = 0 kill applied PER FACTOR, exact at full tau_R): PASS
WS2f THE CONTRAST: [m^1] K^{(+-)} = C(h - h^dag)C with h = U_1^dag Ph_0 - U_0^dag Ph_1 — ANTI-HERMITIAN, generically NONZERO (entry (0,0) displayed below), and the cross DIFFERENCE doubles: [m^1](g_{+-} - g_{-+}) = 2h — the odd sector the summed level kills is exactly the sector the per-composite level DOUBLES (KB4 re-pin); [m^1] K^Sigma = 0 alongside: PASS
WS2f_EXHIBIT [m^1]K^{(+-)}[0,0] = 2*I*(-Ph0_00*U1i_00 - Ph0_10*U1i_10 - Ph0_20*U1i_20 + Ph0i_00*U1_00 + Ph0i_10*U1_10 + Ph0i_20*U1_20 + Ph1_00*U0i_00 + Ph1_10*U0i_10 + Ph1_20*U0i_20 - Ph1i_00*U0_00 - Ph1i_10*U0_10 - Ph1i_20*U0_20)
WS3a THE CAUCHY SLOT IDENTITY (exact symbolic integral, generic cubic, complex coefficients): c_1 = (1/2pi) int_0^{2pi} f(eps e^{it}) e^{-it}/eps dt — the [a^1] slot is an EXACT contour average at radius eps; the enclosure ||[a^1]X||_2 <= M/eps is this identity + the triangle for the Bochner integral (named standard steps; eps SYMBOLIC): PASS
WS3b HS PARITY PYTHAGORAS (generic 3x3): ||M||_2^2 = ||M_H||_2^2 + ||M_A||_2^2 — the Hermitian/anti-Hermitian split is HS-orthogonal; with H_A^Sigma = 0 the whole summed HS mass is the even sector's: PASS
WS4 THRESHOLD ARITHMETIC (exact): trace side int_1^Lam k^2 k^-p dk = (Lam^2-1)/2 [p=1], Lam-1 [p=2], log Lam [p=3], 1-1/Lam [p=4]; HS side int k^2 k^-2p dk = Lam-1 [p=1], 1-1/Lam [p=2] — the summed degree -2 start (the double m_0 kill of record) PASSES the HS threshold (2p = 4 > 3: bounded) and FAILS the trace threshold (p = 2 < 3: linear); with ONE more power (the angular kill, WS5) the trace count lands AT p = 3: log Lam — a LOGARITHMIC shortfall, threshold strict: PASS
WS5 THE ANGULAR KILL'S ENGAGEMENT: int_S2 khat_x dOmega = 0 EXACT (first moment; the S1 odd-spinor-trace leg, KF2 of record), second moment 4pi/3 != 0 (worth EXACTLY ONE power); the SUMMED leading diagonal stratum (V_2 (x) V_2, o = 2, c = 0 — the ONLY degree -2 assembled stratum, REM §3.3) carries ONE longitudinal khat_x against angular-SCALAR V_2 factors: ODD under inversion — the kill ENGAGES; the per-composite (1,0) single-J5-stay integrand khat_x(khat_x - khat'_x) is EVEN under joint inversion — the kill is REFUSED there (o-2-OBS of record, re-pinned): PASS
WS6 THE CHIRAL INVOLUTION IS EXACTLY GENERATOR-ODD: S = -i gamma^0 gamma^5 is Hermitian, S^2 = I, {S, alpha_x} = 0 (the stencil's own ground); beta anticommutes with every alpha_j AND with S; hence beta h_lambda(t; a) beta = -h_lambda(t; a) TERMWISE (h_0 massless; the S-vertex; the J vertex — M(t), b_D, Q_n, P all beta-commuting spatial scalars/projectors); and beta C(p) beta = I - C(p) for the sealed sea symbol (MO-2's conversion): the involution GRADES (sea band <-> positive band, conjugate dynamics, modulo MO-2's rank <= 4 kernel correction) — it is NOT a composite kill: PASS
WS7 THE STENCIL: sum_l w_l e^{-i lam_l th_1 S} J e^{-i lam_l th_0 S} = c(th_1 - th_0) J with c(th) = (1 - cos(sqrt2 th))/2 = sin^2(sqrt2 th/2) >= 0 FORMALLY (1 - cos = 2 sin^2, a square): the collapse-level F-weight is POSITIVE — the summed level's F-factor carries NO sign resource, only kills (S J S = -J the deciding ground, verified inside): PASS
WS8 THE SUMMED FORM: for Hermitian K^Sigma (H_A^Sigma = 0): Re e_2(K^Sigma) = e_2(K^Sigma) = (1/2)(tr K^Sigma)^2 - (1/2)||K^Sigma||_2^2 EXACTLY — the wall's FOUR-quantity difference collapses to a TWO-quantity Hermitian-sector form at the summed quantifier (the between-sector indeterminacy is GONE), and e_2(K^Sigma) + (1/2)||K^Sigma||_2^2 = (1/2)(tr K^Sigma)^2, the square of a REAL symbol: the NEGATIVE channel of the summed e_2 is enclosed by the HS enclosure ALONE (one-sided, exact): PASS
WS9 QUANTIFIER-SHIFT HONESTY: the swap flips the odd sector — H_A^{(-+)} = -H_A^{(+-)} entrywise, and the branch-diagonal composites are Hermitian (H_A^{(mu mu)} = 0); hence the c-weighted LINEAR sum of per-pair odd sectors is ZERO (sum-then-quantify: killed), while the c-weighted sum of SQUARES is (1/4)(tr H_A^{(+-)})^2 — squares are PARITY-BLIND: the zero-sum weights cannot kill quantify-then-sum objects, and the summed decisions do NOT transport back per-composite (the V-2-barred bridge): PASS
WSR-BATTERY-DONE
```

---

## 9. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 00:02:23 -0500**. Both corpus roots
(`workspace`, `supervision`) swept by filename and token grep; fenced
classes (register|road_|ledger|lens|plan|tracker|THE_HANDOFF) matched by
FILENAME ONLY, never opened. Artifacts sealed after the cutoff are
UNSWEPT and unclassed here.

```text
sw-1 ROUTE OCCUPATION: token classes "H_A^Sigma | K^Sigma |
     SUMMED_REQUANT | summed re-quantif" — hits (non-fenced): the seed
     stock only (9fdc3d1c names the route; 48ecdabe re-poses it) plus
     0a10c030 (ASSEMBLY-Q — adjudicates the per-pair/summed SPLIT and
     the one-way bridge; performs NO four-quantity re-quantification).
     NO artifact occupies this commission. The one register-root hit is
     a fenced-class filename, not opened.
sw-2 PARALLEL-LANE STATE AT CUTOFF: no workspace artifact newer than the
     seed stock's newest member (STAGE8_MO2_KAPPA_RATE_S9AD_AUDIT) at
     the cutoff instant.
sw-3 The enumerations of §5.2 (s-1..s-4) claim exhaustiveness ONLY over
     the seals of §1 at the spans read — the same displayed-sweep grade
     the wall's own s-i..s-v carried.
```

---

## 10. CHOICE LEDGER

```text
C-1 OBJECT CHOICE [YOURS, grounded]: the summed re-quantification target
    is the RECORD'S OWN summed object — the equal-source [a^1] slot of
    Delta^{Sigma,all}_n = X_n (the CHECK-VERIFIED identification,
    e5b0879b §2.1; ASSEMBLY-Q's ruling that the defined object of record
    is the weight-summed assembly). Alternative (the census-normalized
    survivor sum (1/N) sum_surv c_p K_p): shares BOTH kills (its pair
    set is transpose-closed and +--symmetric) but is NOT the object the
    sealed HS estimate quantifies; not adopted, difference disclosed.
C-2 VERDICT CLASSES [YOURS]: the two odd quantities are DERIVED (exact
    cancellation, identity grade) — not "enclosed": the operator
    vanishes. The even HS quantity is DERIVED(enclosure) rather than
    PARTIAL: every chain step is exact and sealed or CAS-pinned; the
    inherited estimate grade (REM e-i/e-ii) is named, not hidden. The
    even trace is FAILS-AT with NOT-DERIVABLE-TODAY blockers named —
    not PARTIAL: no fraction of the trace is enclosed.
C-3 THE ANGULAR-KILL SPAN [YOURS, graded]: (t-2) is DERIVED at the
    sealed symbol-display grade — it consumes REM/BL2STAR/R2 displays
    at bytes (the khat_x structure, the V_2 subsidiaries, the (o,c)
    floors) plus KF2's exact moments; its scope is the LEADING stratum
    only, and the verdict does NOT rest on it (FAILS-AT stands with or
    without it; it sharpens the shortfall from linear to log).
C-4 SLOT-MODEL GRADE [YOURS, disclosed]: WS2/WS9 run at the abstract
    Dyson-slot level — the same license as the sealed KB1-KB4 blocks
    (d66a922c §2.2, audited); the parent instantiation is the sealed
    lambda-linear generator. See TOY_SEPARATION.
C-5 HOMONYM SPLIT [YOURS, disclosed in header]: n = carrier index, k =
    insertion order, momentum k contextual — inherited overloads.
C-6 F~ = F [PROVABLE, consumed]: forced at bytes (798de0dd §2.1,
    audit-confirmed); the Hermiticity kill (k-1) rests on it and on
    nothing else beyond weight commutativity.
```

---

## 11. TOY_SEPARATION

```text
T-1 WS2/WS9 use the 3x3 abstract Dyson-slot model as identity-schema
    instantiations of exact operator identities (polynomial identities
    in the coupling with generic complex slot operators) — the SAME
    license as the sealed d66a922c KB/KC blocks. No matrix-grade result
    is carried to the carrier quantifier; every carrier-axis statement
    cites sealed carrier-axis text (REM, KF1/WS4) or is a named
    FAILS-AT.
T-2 WS5/WS6/WS7 use the EXACT 4x4 Dirac algebra of the record (not a
    toy: the sealed spinor structure itself); the generator display
    h_gen models the spatial operator slots (p_j, v(t)M(t), b_D) as
    real beta-commuting scalars — exactly their sealed TYPE (spatial
    scalars/projectors tensor 1_spinor); the involution conclusion
    (generator-oddness) is a type-level identity, valid termwise for
    the operator instantiation.
T-3 WS3a's generic cubic is an identity schema for the operator-valued
    coefficient extraction; the operator step is the same contour
    identity plus the Bochner-integral triangle — named standard steps,
    not re-proved.
T-4 No testbed evaluation appears in this artifact; nothing
    witness-family-grade is promoted.
```

---

## 12. FLAG BLOCK

```text
TRHA_SIGMA_STATUS = DERIVED( EXACT CANCELLATION at the summed
  quantifier: tr H_A^Sigma_n = 0 IDENTICALLY per (n, cell), every n —
  transpose-closure Hermiticity (WS2c; F~ = F forced of record) +
  per-factor odd-kill (WS2d; m_1 = m_3 = 0 exact at full tau_R), two
  independent exact mechanisms; the per-composite level DOUBLES this
  sector instead (KB4, re-pinned WS2f with displayed nonzero exhibit). )

HSHA_SIGMA_STATUS = DERIVED( EXACT CANCELLATION at the summed
  quantifier: ||H_A^Sigma_n||_2^2 = 0 IDENTICALLY — the operator itself
  vanishes; nothing left to enclose. )

HSKH_SIGMA_STATUS = DERIVED( ENCLOSURE at the summed quantifier:
  ||K_H^Sigma,n||_2^2 = ||K^Sigma_n||_2^2 <= M^2/eps^2, n-UNIFORM and
  CARRIER-FREE per unit cell — B-L2* discharged (REM: sup_n ||X_n||_2 <=
  M, the C4/REM double degree-0 kill consumed AT ITS SEALED QUANTIFIER
  as the deciding step) + the check-verified identification (e5b0879b
  §2.1) + the exact Cauchy slot extraction (WS3a) + Hermiticity/
  Pythagoras (WS2c/WS3b); GRADE INHERITED: REM's identity-grade core
  with estimate-grade steps e-i/e-ii and their sealed overturn
  conditions; ray class CONDITIONAL(P-1) as of record; also
  ||K^Sigma_n||_op <= 2 n-free (l1-tight weights; the WBC chain
  transported). The enclosure the per-composite level provably refuses
  is DELIVERED at the summed quantifier — the allowed direction, no V-2
  bridge. )

TRKH_SIGMA_STATUS = FAILS-AT( the LOG threshold, at the summed
  quantifier: the double m_0 kill lifts the leading diagonal stratum to
  degree -2 (of record) and the S1 odd-spinor-trace leg ENGAGES at the
  summed leading stratum (the ONLY degree -2 assembled stratum is
  V_2 (x) V_2, c = 0, carrying EXACTLY ONE longitudinal khat_x — odd,
  first angular moment ZERO, WS5 — where the per-composite even
  integrand refuses it, o-2-OBS re-pinned): effective diagonal degree
  -3 = log Lambda (WS4, exact); the threshold (< -3) is strict; NO
  swept sealed asset crosses it — the chiral involution is exactly
  generator-odd and GRADES (mirror modulo MO-2's rank <= 4 correction),
  not a kill (WS6); the stencil weight is a square, no sign resource
  (WS7); the sealed trace budgets are polynomial (FHB 12 sqrt2
  n^{7/2}/ell with E_n = 6n^4/ell^2; rank x op 8n^3 = WBC F1 = MO-3);
  p = -3 has no purchase on the c = 0 stratum and no coincidence
  resurrection exists (REM). NOT-DERIVABLE-TODAY blockers: a certified
  log-free rate for the degree-3 diagonal class of the summed assembly
  — MO-3's shape, now LOG-THIN. NOT REFUTED: no divergence exhibited;
  the quantity UNDECIDED with a quantified logarithmic shortfall. )

SIGN_LEDGER = PARTIAL( one-sided, exact: with H_A^Sigma = 0 the four-
  quantity difference collapses to Re e_2(K^Sigma) = (1/2)(tr K^Sigma)^2
  - (1/2)||K^Sigma||_2^2 (WS8) — the between-sector indeterminacy of
  d66a922c n-2 is GONE at the summed quantifier, and the NEGATIVE
  channel is enclosed: e_2(K^Sigma_n) >= -M^2/(2 eps^2) every n; the
  remaining openness is the single nonnegative number (tr K^Sigma_n)^2. )

PER_COMPOSITE_WALL = UNTOUCHED( the summed level decides SUM-THEN-
  QUANTIFY and provably not quantify-then-sum — squares are parity-blind
  (WS9); the equal-source survivor T_n and the wall's four per-composite
  quantities stand UNDECIDED at their own quantifier; the V-2-barred
  bridge is not crossed in either direction; the one-way quantifier
  record (0a10c030) is respected. )

ADOPTED_ESTIMATE_BEARING = NONE( B-L2*, the adopted S2'/R-L2b^Sigma
  clause, alpha' = 1/4, MO-2, MO-3/p = -3, the op-grade WBC enclosure,
  and every sealed identity are CONSUMED at their own quantifiers and
  MOVED NOWHERE; no falsifier V-1..V-6 engages (V-5's kill-before-the-
  norm clause is the consumed deciding step; V-2 per the previous flag);
  RA4b/CA3's no-moment-kill stands (the involution and the parity
  structure GRADE — the only kills claimed are the two summed-level
  cancellations, which are weight-sum/closure properties exactly as the
  record types them); witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
  and all standing witnesses UNTOUCHED (no witness action; witness
  status is the registrar's). )

SEALS_VERIFIED = 17/17( the nine tasked (d66a922c, 4ebe6b8f, 9fdc3d1c,
  80db260f, 9a295498, 6997ff61, e5b0879b, cbe6ff4f, 48ecdabe) + eight
  additional consumed (798de0dd, c944a901, 2e4e5163, 6e81ae92, a7969f0c,
  789338ad, 0a10c030, 549362d4) — all recomputed from bytes at path this
  session by full digest; all match sidecars and tasked prefixes; none
  unverifiable; every load-bearing span read at path. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant symbolic
  or an exact rational/surd; NOTHING numeric evaluated (Lambda, M, eps
  FORMAL); no float as ground; no measured constant; no value frozen;
  alpha never computed or approached; kappa_record nowhere; no carrier
  or cellulation datum introduced; the per-pair route not traversed
  (per-pair-type objects appear only inside exact identities whose
  assembled form is the summed content of record — the d-1 display);
  H-R nowhere defaulted; no fenced-class file opened; no flag flipped;
  no witness action; no git. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv wsrvenv under
  the session scratchpad, ONE battery, 14 checks, 14/14 PASS on the
  single completed run, script and output reproduced verbatim in §8; one
  pre-completion harness-form correction disclosed in the header (a
  Python sum() start-value TypeError after WS1 on the first execution;
  mechanical fix; no check condition changed); shasum/file reads
  otherwise; NOTHING numeric evaluated.

SWEEP_CUTOFF = 2026-08-15 00:02:23 -0500 (both roots; fenced classes
  filename-matched only, never opened; artifacts sealed after the
  cutoff UNSWEPT).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
WSR_SUMMED_REQUANT_RESULT = SEALED.
```
