# Stage-8 T7 E1 Repair Binding — Supplement: Codex Reconciliation Consumed V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_SUPPLEMENT — FILLS THE R7 RESERVED SLOT
+ CLOSES ONE ARM OF A-L0 AS DEAD
+ ADDS ONE NAMED OBLIGATION
```

Fills the CODEX_RECONCILIATION_ADDENDUM slot reserved by R7 of
STAGE8_T7_E1_SPEC_REPAIR_BINDING_V001. The addendum arrived CORRECTED
via the principal, who verified it against this lane's C3/C5 erratum:
algebra consistent (V - 1 = Y_1 + Y_2 + Y_2 Y_1; Phi_12(eps^2) =
-tr[C Y_2 Y_1 C] + tr[C Y_1 C Y_2] + O(Y^3)); the contamination flagged
under D2 is FULLY WITHDRAWN by Codex; the object-vs-bound distinction is
maintained. The D2 quarantine on the addendum's earlier two-line/R^-6/NC3
sentences is therefore LIFTED for this corrected restatement only; the
withdrawn sentences remain non-citable.

## S1 — A-L0's FIRST ARM IS DEAD (closure recorded, with the witness)

```text
A-L0 as sealed offered two arms:
  arm 1: prove Y_2 Y_1 = 0 as an EXACT IDENTITY on the sealed forms;
  arm 2: bound tr[C Y_2 Y_1 C] with certified separation decay,
         uniform over D3.
ARM 1 IS CLOSED AS DEAD. It cannot succeed without a forbidden
idealization, and the refuting witness is THIS LANE'S OWN: the exact
rational computation sealed in STAGE8_T7_C3_C5_REFUTATION_AND_F8_FLAG_
ERRATUM_V001 gives tr[C Y_2 Y_1 C] != 0 (order-eps^2 one-line term
-1337849531/65078154 against a two-line term
+35484269897501/52286001580836, with "twoline alone" FALSE and
"one-line identically zero" FALSE). A universal zero identity is
refuted by that witness.
A-L0 THEREFORE REDUCES TO ARM 2 ONLY: bound the ACTUAL Dyson-dressed,
RELAY-ORDERED cross term with certified decay in the cell separation,
uniform over the D3 quantifier, AND WITH ENOUGH DECAY BEYOND BARE R^-3
THAT THE ANCHORED SUMS CONVERGE. Nothing weaker discharges it.
```

LAPSE OF THIS LANE, RECORDED: the two-armed A-L0 was sealed one artifact
after the erratum whose witness already killed arm 1. I did not
cross-check my own binding against my own erratum. Codex's independent
reading caught it. No executor was misdirected (no execution occurred),
but the binding would have sent a lane at an impossible arm.

## S2 — New named obligation, beside Q6

```text
E1_ONE_LINE_CONNECTED_CROSS_TERM_UNCERTIFIED
  A named blocker of equal standing to
  Q6_UNIFORM_CELL_ADJACENCY_DEGREE_UNDERIVED.
  Owner: the E1 successor spec v002 (arm 2 of A-L0 is its discharge
  site).
NC3 IS REHABILITATED as the relevant ONE-LINE-SECTOR DETECTOR. Its exact
24 H_K divergence is the correct behaviour of a one-line-only bound, and
the earlier dismissal of NC3 as "a counting artifact saying nothing about
E1" is void (already corrected in the erratum; restated here because NC3
now has a POSITIVE role, not merely a rehabilitated one).
```

## S3 — Codex's re-frozen predictions (to the calibration ledger)

```text
P-C1 Most likely outcome: E1S_BLOCKED, with the one-line cross-term
     obligation AND Q6 as the live blockers.
P-C2 IR-A's n >= 2 leg is DOWNGRADED from likely-survivor to
     open-block candidate.
P-C3 Route-Q / O7 independence: UNCHANGED (now triple-confirmed).
P-C4 SCAD remains right for the n = 1 side but DOES NOT repair the
     n >= 2 cross term.
These are recorded in CALIBRATION_LEDGER.md as an independent-system
prediction set, tracked separately from this lane's own predictions.
```

## S4 — Effect on the E1 successor spec v002 (authoring scope, unchanged otherwise)

```text
- A-L0 is authored with arm 2 only, citing S1's closure.
- E1_ONE_LINE_CONNECTED_CROSS_TERM_UNCERTIFIED is a named obligation
  with its own graceful-block witness.
- NC3 is authored as the one-line-sector detector, with its PASS
  condition being detection (not dismissal).
- The D1 reading committed in the pipeline binding (M2 supplies the
  anchored-sum D5 bound directly; M3 closes as a geometric series) is
  restated, together with its honest consequence: the burden sits on M2,
  which is the open E1 obligation, and Q6 is retained for the
  alternative reading.
- All of R1-R8 otherwise stand.
```

## Protected status

```text
A_L0_arm1_closed_dead = true
E1_one_line_cross_term_uncertified = true   (named blocker, open)
Q6_uniform_cell_adjacency_degree_underived = true   (named blocker, open)
E1_successor_spec_v002_authored = false
connected_linked_cluster_density_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
