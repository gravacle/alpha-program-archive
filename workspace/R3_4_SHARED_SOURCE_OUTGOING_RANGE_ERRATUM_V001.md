# R3.4 Shared-Source Outgoing-Range Erratum v001

Date: 2026-07-24

## Corrected claim

Two sealed v001 result summaries used the phrase
`public-record Moller endomorphism`:

```text
R3_4_SHARED_SOURCE_CAUSAL_PARENT_RESULT_V001.md
R3_4_CAUSAL_SHARED_SOURCE_MOLLER_DURABILITY_RESULT_V001.md
```

That phrase is superseded.

The full matrix-unit range audit in
`R3_4_DRESSED_OUTGOING_RECORD_RANGE_RESULT_V001.md` proves:

```text
the images stabilize;
the map is a unital injective star-homomorphism;
and its range is a source-dressed record algebra inside the full parent.
```

It also proves:

```text
the range is not generally contained in the bare record-only algebra.
```

The correct term is therefore:

```text
stable dressed outgoing-record monomorphism.
```

The causal-order, primitive pointer-persistence, finite Moller, and
bare output-state restriction calculations in the two historical results
remain unchanged.

```text
bare_record_endomorphism_derived = false
stable_dressed_record_monomorphism_derived = true
complete_parent_to_outgoing_GNS_map_derived = false
complete_physical_durability_derived = false
alpha_computed = false
proof_authorized = false
```
