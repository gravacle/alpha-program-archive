# STAGE 8 — RULE-2 CANDIDATE T15SR — OPPOSITE-LANE CHECK
## RULE2-CHECK (commission T15SR, 2026-08-15) — DEFAULT-REFUTE; TESTIMONY ZERO WEIGHT

```text
FENCES, LIVE THROUGHOUT THIS CHECK AND UNMOVED:
  alpha_computed        = false
  proof_authorized      = false
  kappa_record_computed = false
  exact symbolic only; NO numeric evaluation of any physical quantity;
  NO value assigned to any datum; NO measured-constant comparison;
  numerals below are provenance coordinates (digests, spans, line refs,
  dates, record-item counts) or sealed-text quotations ONLY. No git.
```

CHECKED ARTIFACT: STAGE8_RULE2_CANDIDATE_T15SR_V001.md
CANDIDATE DIGEST (recomputed at path by this check, STEP 0):
`cc4af085c13e145865ec9768b84119c346c273b2a50a7b07ac6ee3275c3be021`
Sidecar STAGE8_RULE2_CANDIDATE_T15SR_V001.md.seal.sha256 read; shasum
recomputation MATCHES the sidecar line. STEP 0 PASS.
Check-output path probed ABSENT before any write.

DEFAULT-REFUTE POSTURE: every claim below was treated as false until its
sealed source was independently re-verified at path/span/bytes by this
commission. The candidate's own testimony (its §1 table, its quotes, its
status labels) was given ZERO weight; everything relied on was recomputed
or re-read from the sealed instruments directly.

---

## 1. SEAL RE-VERIFICATION (INDEPENDENT, AT PATH, 2026-08-15)

All sixteen pointer-list instruments recomputed by this check with
`shasum -a 256` at the stated paths: ALL SIXTEEN MATCH the registrar's
pointer list (rows 01-16 of the candidate's §1 table carry the same
digests; the candidate's table is therefore accurate, though this check's
reliance is on its OWN recomputation, not the table).

Special rows, independently confirmed:
- STAGE8_DESC_JOINT_MAP_CHECK_CODEX2_V001.md: sidecar ABSENT at path
  (confirmed by probe); recomputed full digest matches the recorded
  `9b89beeb...` — VERIFIED-BY-RECORDED-DIGEST, exactly as the pointer
  list records. Treated consumable.
- Candidate row 17 (declared scoped-sweep read beyond the pointer list):
  R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md — full digest
  recomputed `10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995`
  MATCHES the candidate's row; span [698,1630) recomputed
  `2a99a925eb1288a8b0aa074963b2beeb4a082cb65d109e6821765a44a4f6c383`
  MATCHES; and this span digest IS pinned by the sealed AXN map
  (a67ed435, its M_3-state row, file line 69) — the candidate's claim
  "equal to the AXN map's pinned span digest" is TRUE at bytes. The
  extra consumption is lawful (declared scoped sweep, pinned by a sealed
  consumed instrument) and honestly declared in the candidate's §13.

Candidate span-digest block (§1), all five recomputed by this check:
```text
1be5f6a7 [22959,24536) -> 3e0b43706ef6e329... MATCH
a33be83c [2622,5894)   -> e017e9da18d54761... MATCH
a67ed435 [17865,20008) -> aa04e0ea2c901d0a... MATCH
2b629122 [10824,11934) -> 4313fb92fd78f6b2... MATCH
10909b5c [698,1630)    -> 2a99a925eb1288a8... MATCH
```

Row 03 (FIVE_C staging sheet): seal recomputed OK by this check; content
NOT opened by this check (staging/plan-adjacent, outside the check lane's
read scope; the registrar's requirement card restates its Act-5 content
neutrally). The candidate declares the same discipline (row 03
"seal verified; content not opened") — CONSISTENT.
Register: NOT opened by this check (registrar-only, per commission).
"Q-..." row IDs in the commissioning instruments: carried as registrar
cross-references, EXPECTED-UNLOCATABLE, not resolved — per commission.

SEAL PHASE VERDICT: PASS — no mismatch anywhere.

---

## 2. BYTE-VERIFICATION TABLE — EVERY LOAD-BEARING CANDIDATE CLAIM RE-READ AT SOURCE

Each row: the candidate's claim, the sealed source re-read by THIS check at
path/span/line, and the finding. DEFAULT-REFUTE: rows were hunted to fail.

| # | Candidate claim | Source re-read | Finding |
|---:|---|---|---|
| V-01 | Slot type F1: one U1-invariant probability measure on Y + state_descent_map + state_provenance_certificate, landing State(C(Y)) | f7fd0c60 §5 (file lines 278-282); b9716661 bytes [1470,2433) | CONFIRMED verbatim at both sources |
| V-02 | Named construction of record: SM-1..SM-8-conformant joint with omega_hist = Omega_C0 o i_B; Theta_hist-conformant descent | f7fd0c60 §5 "WHAT KIND OF OBJECT SELECTS" (lines 293-301) | CONFIRMED verbatim |
| V-03 | Receiver StatePort_U2_008 consumes rho_pre itself as input field | 2b629122 bytes [10824,11934): schema lists `rho_pre in trace-class(H_omega), rho_pre >= 0, Tr_omega(rho_pre)=1` | CONFIRMED |
| V-04 | (F2) canonical null-quotient/GNS completion route "U2 :259-263"; construction-not-choice under no-post-output-supplementation | f7fd0c60 §5 (F2) lines 300-304; 2b629122 lines 255-266 (H_omega := completion of E_C0/N_omega) | CONFIRMED |
| V-05 | D8 / U-D8: G2 port pre-assigned to the measure-supply mandate; derived route closed | f3165db1 lines 177-182 (D8: omega_phys STOPPED both routes) and 297-302 (U-D8: "the ratified measure-supply mandate pre-assigns F1's supply slot") | CONFIRMED |
| V-06 | Commissioning word: AUTHOR, mandate lane, frozen, complete descent + certificate, opposite-lane check, second word naming digests | 96bd68e5 full read; c14d0b5c full read (door-return) | CONFIRMED — candidate instantiates every element of the word's form |
| V-07 | Mandate clauses (honesty / falsifiers / open forcing slot / battery / entry mechanics) | ad9fc14e full read, lines 39-66 | CONFIRMED verbatim; candidate §5, §9.1-9.3, banner instantiate all five |
| V-08 | Dirac lawful only as disclosed authored selection, not favored; not taken | b9716661 [1470,2433) final sentence; f7fd0c60 §5 NON-SELECTORS (line 319) | CONFIRMED; candidate CH-1(i) declines it with disclosure |
| V-09 | P5 five-part package form + four descent maps + eight Cert conditions | a33be83c bytes [2622,5894) | CONFIRMED — candidate §6 tracks the sealed form condition-by-condition, in order |
| V-10 | Tuple members P_0, P_ch, I_src, Tr_A etc.; freeze-before-output; T0-T8 order | 1be5f6a7 bytes [22959,24536) | CONFIRMED — "The tuple contains no rho_S, ready ray, ... response, or target output" |
| V-11 | Anchor THEOREM proves (not defines) d_state = I_src/Tr_A(I_src) — the forced normalized-invariant SOURCE-face form | 1be5f6a7 §7.1 (inside the span) | CONFIRMED — "proves, rather than defines". R2-A's reading (form FORCED on source face, TRANSPORT authored) is accurate |
| V-12 | d_law = ratified E_post law | 1be5f6a7 §7.3 — "exactly the ratified DoR-009 E_post law" | CONFIRMED |
| V-13 | Overlay read rule: tuple consumed only with overlay + DoR-013; no member ever | e85f444c lines 98, 114, 237, 243; f2a7838d ("Ratify at family level", the *** clause) | CONFIRMED — candidate's §1 READ-RULE COMPLIANCE block names both, as required |
| V-14 | SM-1..SM-8 rows as candidate restates them (incl. SM-3 "admitted M_3 outgoing-record state on every finite record subalgebra and its compatible limit"; SM-8 bounded-class + reopening-trigger branch) | a67ed435 bytes [17865,20008) | CONFIRMED — every row restated without distortion; candidate SM-8 uses the table's second lawful branch |
| V-15 | Carrier block: A_SR, B := A_F_CTP iso C(Y), A_C0, i_R, i_B, res_B, "Their ranges commute", X_N --q_N--> Q_N iso U(1)^N, State(B) iso Prob_reg_Borel(Y) | a67ed435 lines 118-165, 214-215 | CONFIRMED verbatim |
| V-16 | M_2/M_3 "a same-algebra binding is required" (the strict-SM-6 open surface) | a67ed435 line 73 | CONFIRMED — candidate C-7(iv) displays it rather than absorbing it |
| V-17 | q_N = complete common-gauge orbit invariant (candidate C-6 gauge argument) | a67ed435 line 74 | CONFIRMED |
| V-18 | rho_pre^SR = d_state tensor Q_R sealed at E3 | 31e69336 §2.2 line 151 | CONFIRMED — "rho_pre := omega_S tensor Q_R ... this is d_state tensor the [ready ray]" |
| V-19 | Master lemma formula (the two-term Lambda_E display) | 31e69336 lines 170-171 | CONFIRMED byte-verbatim; lemma is generic in nu, so instantiating nu := Haar(Q_N) is a lawful specialization, not a new derivation |
| V-20 | Formal moments m1, m2 symbolic never evaluated; inclusive defect kappa_ch(1-kappa_ch) c_0 c_ch (m2 - m1^2); "coherence, not statehood" | 31e69336 lines 191-196, 223 | CONFIRMED — candidate carries all three verbatim, all symbols formal |
| V-21 | E3 absolute theorem: every SM-2-positive conditioned functional has exactly the product mixed term (the F-09(ii) forcing) | 31e69336 §2.3 lines 214-217 | CONFIRMED — F-09's FORCED label is true at bytes |
| V-22 | Collapse theorem: unital sector-controlled Y-diagonal stock cannot move the mixed term (the F-09(i) forcing) | 9b89beeb §1-2 (lines 66-88) | CONFIRMED |
| V-23 | "No sealed selector of the residual non-product term is present" (the beyond-quantifier vacancy R2-B fills by authorship) | 9b89beeb line 250; RULE2_COUPLING_UNCONSTRAINED = CORRECTED (line 310) | CONFIRMED — so R2-B's zero-covariance clause is correctly declared AUTHORED, and F-09 correctly bounded to "within every sealed quantifier" |
| V-24 | SM-6 conditional, sentence quoted in candidate §4 | 9b89beeb lines 237-239 | CONFIRMED — quotation matches at bytes, including "Authorship is not provenance" |
| V-25 | Product joints not intrinsically forbidden; lawful if an executable frozen producer derives them with provenance; choosing product to fill Step 4 consumes a joint-extension choice | 9b89beeb §3 correction paragraph (lines 164-169) | CONFIRMED — the candidate has exactly this sealed-contemplated shape: frozen producer + the joint-extension choice DECLARED as authored core A-02 |
| V-26 | Z_N sealed unit-modulus multiplier (R1/R4); eq (4) F_N = P_0 + Z_N P_ch | 9b89beeb §3 R1/R4; 31e69336 lines 69, 114-115, 202 | CONFIRMED |
| V-27 | The 1082 witness = sector-dependent branch marginals; realizable only (if ever) by a descended nonunital instrument — the live falsifier of R2-B | 9b89beeb §3 final paragraph; 31e69336 line 8 | CONFIRMED — SK-R2-4(f) names the genuine deciding surface |
| V-28 | E3 seven ground items + statuses; audit tally | 31e69336 §2.4 items 1-7 re-read; b707cc8b line 11 ("35/35 audit checks, exact symbolic") | CONFIRMED — candidate's "audited 35/35 at b707cc8b" accurate; its G-1..G-7 track the sealed items |
| V-29 | R3_4 span: omega_out unique on the inductive limit; causal factorization W_M = V_(M,N)(W_N tensor I_new) INSIDE the declared span [698,1630) | 10909b5c bytes [698,1630) re-read | CONFIRMED — the factorization and the restriction-consistency computation are both inside the span; NO sweep breach in candidate C-6 |
| V-30 | SM-3-BIND: R3_4's omega_in definition not consumed; genuinely open | 10909b5c bytes [0,698) probed by THIS check: head is title/verdict; omega_in's definition is not there and not in [698,1630) | CONFIRMED OPEN — the candidate's refusal to identify omega_in with Lambda_SR by label is honest; the deciding bytes exist elsewhere |
| V-31 | "arrows do not constrain states" | b9716661 line 7 | CONFIRMED at bytes |
| V-32 | THETA_HIST = STOPPED, routes closed, opposite-lane ROUTE-CONFIRMED | f7fd0c60 lines 42-47, 146, 257-260 | CONFIRMED |
| V-33 | kappa_ch, mu, (r_0, r_ch) typed in f7fd0c60 §0/§5; coupling_evaluation_authorized = false carried | f7fd0c60 lines 8-15, 31-35, 287, 310, 336 | CONFIRMED — candidate touches none of them; flag carried |
| V-34 | AXN pin of the R3_4 span digest (row-17 lawfulness) | a67ed435 line 69 (M_3-state row) | CONFIRMED — pinned digest equals this check's recomputation |

Mathematical-structure spot-checks performed by this check (exact symbolic,
classical structure only, nothing evaluated):
- Haar(Q_N) on a compact group: existence/uniqueness of the normalized
  translation-invariant state; invariance under inversion and every
  continuous automorphism (uniqueness argument) — the R2-A "whichever sealed
  reading" invariance claim HOLDS under each candidate reading. SOUND.
- C-4 PROVED leg: Haar of a product compact group marginalizes to factor
  Haar along the unital coordinate inclusion f |-> f tensor 1 — exact
  (product-Haar uniqueness + Fubini at structure level). SOUND, and
  correctly CONDITIONAL on the sealed embedding having that form.
- C-2: product of the sealed state Lambda_SR with a state on the
  commutative factor B (sealed "Their ranges commute"; B iso C(Y)
  commutative, trivially graded) is positive and normalized — SOUND at the
  record's own exact-symbolic standard; normalization is unit pairing.
- C-5: the branch pairings nu(.), nu(Z_N .) are the sealed lemma's own
  pairing typing (31e69336 :170-171 pairs nu with Z_N f of record); the
  candidate adds only nu := Haar. The Z_N-on-Q_N byte form remains open and
  is DISPLAYED (§7) — no new gap is introduced by the candidate. SOUND.
- G-6 acyclicity: re-walked the §6.1 R-order dependency graph — (tuple ->
  Lambda_SR), (carrier -> nu_N), ({Lambda_SR, nu_N} -> Omega_C0^R2 ->
  omega_phys^R2); no read-off object enters any state-forming step; nu_N
  is built from the carrier and never reads Z_N, any effect, or any law
  image. ACYCLIC AS DISPLAYED.

---

## 3. REQUIREMENT-CARD WALK — THE NINE OBLIGATIONS, PER-CLAIM VERDICTS

```text
OB-1 SLOT TYPE ................................ MET — CHECK-PASS
     omega_phys^R2 is a U1-invariant probability measure on Y (Haar via the
     sealed State(B) iso Prob_reg_Borel(Y) identification — a genuine regular
     Borel measure, not merely a functional label), delivered WITH
     d_state^R2 (§6.1, executable closed-form) and Cert_R2 (§6.2), landing
     State(C(Y)); rho_pre delivered to StatePort_U2_008 via the schema's own
     canonical completion (V-03, V-04). No response-value selection; the
     rank pair untouched (V-33); no invariant substitutes for rho_pre.

OB-2 PLACE IN THE GOVERNING CHAIN ............. MET — CHECK-PASS
     D8/U-D8 verified at their lines (V-05); mandate lane; Act-5 AUTHOR word
     verified (V-06); derived route closed of record (V-32) and the deferral
     door returned with the record NOT supplying the content (c14d0b5c +
     96bd68e5, read directly).

OB-3 COMMISSIONING FORM ....................... MET — CHECK-PASS
     Authored as proposed physics (banner + §9.1); FROZEN at seal with the
     sidecar as the freeze line (§14, R0 precedes every output); complete
     common-origin descent + certificate CARRIED with statuses displayed
     (§6); this artifact is the commissioned opposite-lane check;
     ratification correctly deferred to a second principal word naming both
     digests; no fence moved anywhere in the candidate (V-33, §5 below).

OB-4 SM-6 LAWFULNESS CONDITIONAL .............. MET-AS-DISPLAYED — CHECK-PASS
     The sealed conditional is quoted accurately (V-24), instantiated in its
     own two limbs (§8), authorship-in-a-frozen-rule with output derived
     (§6.1 R-order), and — decisively for honesty — the candidate STATES the
     conditional is NOT discharged (C-7 strict leg OPEN; two CONDITIONAL
     legs named). No rounding up detected anywhere.

OB-5 THE COMPLETE DESCENT (executable maps) ... MET — CHECK-PASS
     d_state^R2 closed-form on sealed objects; d_effect/d_domain/d_dynamics
     consumed-as-sealed with spans (the E3 instrument, the tuple domains,
     the ratified E_post law: V-12, V-18, V-28); tuple consumed only under
     overlay + DoR-013, family level, no member (V-13).

OB-6 THE CERTIFICATE (eight conditions) ....... DELIVERED-WITH-DISPLAYED-LEGS — CHECK-PASS
     All eight conditions are ADDRESSED with proof-attempts on the
     candidate's own construction. Established exactly at finite N: C-1,
     C-2, C-4, C-5, C-6 (structure level), plus C-8; C-3
     met-by-consumption. Displayed rather than claimed: C-4/C-6 continuum
     leg (sealed embedding byte form, deciding span named), SM-3-BIND
     (omega_in identification, deciding span named, V-30), C-7 strict
     one-trace leg (the sealed M_2/M_3 binding row, V-16). Every displayed
     leg has a matching voiding falsifier in SK-R2-4. NOTHING OPEN IS
     CLAIMED DISCHARGED — verified line by line against §6.2/§6.3.
     The five-part P5 package is present in the sealed form (V-09):
     maps §6.1 / trace §6.1+V-10 / property certificate C-2..C-4 /
     dynamics certificate C-5 / admissibility manifest C-8+§5.3.

OB-7 THE SEVEN GROUND ITEMS ................... MET ON OWN CONSTRUCTION — CHECK-PASS
     G-1..G-7 re-walked against the sealed E3 standard (V-28); the
     state-side additions carry their own exact structure (C-2, G-6
     acyclicity re-derived by this check); consumed items cited, not
     re-claimed; G-7 calculus carried with the sealed dichotomy undeformed
     and every moment formal (V-19, V-20, V-21).

OB-8 MANDATE PATTERN CLAUSES .................. ALL FIVE INSTANTIATED — CHECK-PASS
     Honesty of record: §5 with the forced/authored split VERIFIED TRUE at
     bytes in both directions (§4 below). Falsifiers: SK-R2-3 (blind test,
     blast radius) + SK-R2-4 (structural, at bytes, without awaiting the
     cascade) — both limbs present. Open forcing slot:
     future_derivation_can_select_the_joint = NO_VERDICT with the
     dissolution mechanics (§9.2). Battery overdetermination: clause-3
     carried at full strength (§5.3, C-8). Entry mechanics: banner + §14
     carry PROPOSED_NOT_ADOPTED, the second-act addendum requirement, the
     registrar/lane bars, and the no-summary-substitution rule.

OB-9 STANDING FENCES .......................... HELD — CHECK-PASS
     Flag block complete (candidate §12), including the f7fd0c60-carried
     coupling_evaluation_authorized = false and the ad9fc14e-listed
     JOINT_ANCHOR_DERIVED = false; fence-scan at bytes below (§5) found
     zero violations; PROPOSED_NOT_ADOPTED carried first, last, and in the
     flag block.
```

---

## 4. HUNT RESULTS — EVERY COMMISSIONED HUNT ITEM, WHAT WAS FOUND

```text
H-1 AUTHORED INGREDIENT PRESENTED AS FORCED ......... NONE FOUND.
    Both directions checked at bytes: every F-row (F-01..F-12) re-verified
    at its cited span and IS forced there (V-01..V-33; F-09's two forcing
    theorems re-read verbatim, V-21/V-22, and correctly BOUNDED to the
    sealed quantifiers per V-23). The two A-rows are genuinely unforced of
    record: A-01 (no sealed selector of the history-face state; THETA_HIST
    STOPPED, V-32) and A-02 (no sealed selector of the residual covariance,
    V-23). The candidate's declaration is exact in both directions.
H-2 DESCENT OBLIGATION CLAIMED DISCHARGED
    WITHOUT PROOF AT BYTES ........................... NONE FOUND.
    Every MET carries either an exact structural argument re-checked here
    (§2 spot-checks) or a consumption citation re-verified at span; every
    non-MET is labeled CONDITIONAL/OPEN-LEG/PARTIAL with its deciding span
    NAMED and a falsifier attached. The three undischarged surfaces the
    candidate names (§13) are exactly the three this check found.
H-3 DATUM WITH A VALUE / IMPLICIT UNIT /
    TARGET-SHAPED PROVENANCE ........................ NONE FOUND.
    Symbol table (§3): every magnitude-like symbol explicit, symbolic,
    unassigned, sealed-sourced; no unit anywhere in the artifact; no
    provenance path touches a downstream target (C-8; §5.3).
H-4 DERIVATION CLAIM ................................ NONE FOUND.
    The vacancy display (§9.1) is on the face and accurate to its cited
    instruments (V-31, V-32, V-23); the entry displays itself as EXERCISED
    PERMISSION throughout; "proposes", never "derives", governs the
    authored core; the two places where forcing IS claimed (F-09,
    normalization/reality rows) are true forcings of record (H-1).
H-5 CLAUSE-3 BREACH (output inspection /
    downstream target) .............................. NONE FOUND.
    No battery datum, response value, K4-K7 content, root information, or
    downstream output appears anywhere in the candidate; the blind is
    declared held and nothing in the artifact contradicts the declaration;
    authored content (Haar, product) is structural, target-free, and
    parameter-free (no dial to tune even in principle, §5.3).
H-6 A NUMBER ANYWHERE ............................... NO VIOLATION.
    Full numeral sweep at bytes (§5): every numeral is a provenance
    coordinate (digest hex, byte span, line ref, date, version, record
    count) or a sealed-formula quotation (the unit, 3^N, {+1,-1},
    sum_r E_r = I, m2 - m1^2 as formal symbols). ZERO numerals function as
    an evaluated physical magnitude; no measured constant appears; the
    candidate's own numeral-discipline declaration (§0) is HELD at bytes.
H-7 FREEZE ABSENT ................................... NOT ABSENT — PRESENT (§14 +
    R0 + the sidecar as freeze line; rule frozen before any output exists).
H-8 DISSOLUTION CLAUSE ABSENT ....................... NOT ABSENT — PRESENT (§9.2,
    correctly instantiating the pattern's NO_VERDICT slot, with the SM-8
    reopening trigger wired to it, V-14).
H-9 MEMBER / HISTORY / RANK / RESPONSE SELECTION .... NONE FOUND.
    Haar is full-support (the anti-selection: no member, no history point,
    no subgroup, no cell); the ordered rank pair is never read (V-33); the
    step-voiding rule is carried at SK-R2-5.
```

MINOR NOTES (verdict-inert; no correction required):
- N-1: the §3 symbol row for `nu` attributes the phrase "symbolic and
  unselected" to 31e69336 §2.2; the exact phrase sits in that file's §0
  verdict line (line 8), while §2.2 carries the same content as "NEVER
  evaluated" (line 117). Semantics identical; attribution is loose by one
  section. Wording-level only.
- N-2: R2-A's authored status rests on the record's "U1-invariant" row NOT
  meaning full translation-invariance on Q_N (under which Haar would be the
  unique admissible measure and A-01 would be forced). The candidate's
  §5.2 void-on-misdeclaration paragraph already routes that eventuality to
  DISSOLUTION (the lawful direction under ad9fc14e + §9.2), so either
  reading leaves the candidate lawful. Displayed here for the record.
- N-3: candidate C-2's positivity argument implicitly uses that B is
  commutative/trivially graded (sealed: B iso C(Y)); made explicit in this
  check's spot-check. No gap.

---

## 5. FENCE-SCAN — ALPHA-PATTERN AND NUMERAL HITS INSPECTED AT BYTES

```text
PATTERN alpha ......... hits: fence lines (alpha_computed = false, twice) and
                        path names (alpha-program-archive, alpha_supervision)
                        in the seal table. NO computed alpha, NO alpha value.
PATTERN kappa ......... hits: fence lines (kappa_record_computed = false,
                        twice) and the SYMBOLIC kappa_ch rows (§3 symbol
                        table; §7 defect formula, formal). NO value, NO
                        evaluation, NO classification of kappa_ch.
PATTERN mu ............ hits: §3 row "untouched, not computed, not
                        classified". HELD.
PATTERN measured/CODATA/evaluate ... hits: negation/discipline sentences
                        only. NO comparison anywhere.
NUMERAL SWEEP ......... every digit-run classified: digests, spans, dates,
                        line refs, version tags, section numbers, record
                        counts (16, 35/35), sealed structural constants
                        (1 as unit, 0 as zero, 3^N, {+1,-1}, U(1)^N,
                        M_3, N indices), formal symbol names (m1, m2,
                        c_0, c_ch, r_0, r_ch, P_0, P_ch). ZERO physical
                        values. Omega_C0^R2(1) = 1 is the unit pairing
                        (structural), as declared.
FENCE-SCAN VERDICT .... PASS — no alpha-pattern violation, no numeric
                        breach, no fence moved.
```

---

## 6. THE CANDIDATE'S CHOICE LEDGER, RE-WALKED (CHAIN CHOICE AUDIT POSITION: BEFORE FENCE 2)

```text
CH-1 (history-face form = Haar) ..... alternatives list COMPLETE as far as
     this check can see: Dirac (disclosed-selection-only of record, V-08),
     kernel-supported (converts the sealed CONDITIONED-functional dichotomy
     into a PRE-state support law nothing sealed imposes — this check
     re-confirmed the dichotomy is a consequence map on conditioned
     functionals, 31e69336 §2.3, not a pre-state constraint), density/tilt
     (introduces the dial the no-dial manifest excludes). The stated "why
     Haar" reasons are each TRUE (uniqueness, full support, invariance
     under every candidate U1 reading, form-parity with the FORCED source
     face, V-11). CHOICE HONESTLY DISPLAYED.
CH-2 (product completion) ........... the vacancy it fills is real of
     record (V-23); the forced-inside-quantifiers boundary is exact
     (V-21, V-22); the sealed correction paragraph explicitly contemplates
     this shape as lawful-when-declared (V-25). CHOICE HONESTLY DISPLAYED.
CH-3 (states only) .................. confirmed: the candidate authors no
     effect, carrier, embedding, law, or outcome (V-28, C-3); the
     alternative (author an instrument) is correctly identified as a
     different door than the one the AUTHOR word opened (V-06).
CH-4 (conditional limit reading) .... the honest branch; the unconditional
     alternative would have been an unverified-consumption violation.
CH-5 (SM-3-BIND displayed) .......... the honest branch; the silent
     identification would have been flattening-by-label. This check probed
     the deciding surface (V-30) and confirms it is genuinely undecided at
     the consumed spans.
LEDGER VERDICT: COMPLETE AND HONEST — no hidden choice found; the
CONSUMED-AS-PARAMETER list matches this check's own consumption set.

THIS CHECK'S OWN CHOICES (displayed per the actual-surface guard):
K-1 row 03 (FIVE_C staging sheet) hash-verified but NOT opened —
    plan-adjacent, outside the check lane's read scope; the registrar's
    requirement card restates its content. Alternative (open it): rejected
    as a scope breach risk with zero verification gain.
K-2 R3_4 head bytes [0,698) probed to test SM-3-BIND (V-30) — a scoped
    read of a named row-17 artifact, hunting a voiding contradiction.
    Alternative (not probing): rejected — DEFAULT-REFUTE requires hunting
    the falsifier surface. No further R3_* file opened (not in scope).
K-3 no numeric CAS run: every spot-check (§2) is classical exact
    structure re-derivation in symbols. Alternative (execute a CAS
    battery): rejected — the fences bar nothing symbolic, but no
    generator execution is in this check's commission.
```

---

## 7. TOY_SEPARATION (this check)

```text
NO TOY OBJECT WAS CONSTRUCTED BY THIS CHECK. Every verification above is
against the ACTUAL sealed artifacts at their actual paths, spans, and
lines — no model, no reduced example, no stand-in algebra, no
re-implementation. The mathematical spot-checks (§2) re-derive sealed or
classical structure symbolically on the sealed carriers themselves
(Haar on the sealed Q_N, the sealed A_C0 factors), not on simplified
surrogates. The candidate's own TOY_SEPARATION declaration (§11) was
verified consistent: no toy object appears in the candidate either.
```

---

## 8. FLAG BLOCK (this check)

```text
alpha_computed                 = false   (held throughout)
proof_authorized               = false   (held; nothing new proved into the
                                          record — re-derivations are check
                                          work product, binding nothing)
kappa_record_computed          = false   (held; kappa_ch untouched, formal)
numeric_evaluation             = NONE (exact symbolic only)
value_assigned_to_any_datum    = NONE
measured_constant_comparison   = NONE
output_inspection              = NONE (no downstream output, battery, or
                                       response object opened)
member/rank/response selection = NONE
git                            = NO ACTION
registers/trackers/roads/plans = NOT READ (register untouched; row 03
                                 hash-only; "Q-..." IDs unresolved,
                                 EXPECTED-UNLOCATABLE as commissioned)
candidate_status_after_check   = PROPOSED_NOT_ADOPTED (unchanged — a check
                                 PASS ratifies NOTHING; only the
                                 principal's ENTRY addendum can)
```

---

## 9. SWEEP CUTOFF — DECLARED

```text
READ SET, COMPLETE, THIS CHECK: the candidate + its sidecar; the sixteen
pointer-list instruments (row 03 hash-verified only, content not opened;
register not touched) at the spans/sections cited in §2; the row-17 R3_4
artifact at [698,1630) plus the [0,698) head probe (K-2); nothing else.
SCOPED SWEEPS RUN: (i) workspace directory listing filtered to T15SR
(STEP-0 probe + output-absence probe); (ii) grep-scoped line locations
within the sixteen named instruments; (iii) the candidate full-file
fence-scan. NO OTHER FILE OPENED — no register, tracker, road, plan,
ledger, lens, continuation, relay, or cleanroom file (ROOT_C not
entered; the second corpus root was not needed — every citation resolved
in the workspace root). NOT CONSUMED, left open exactly as the candidate
displays them: the U3 sequential-embedding byte form; R3_4's omega_in
definition bytes; the Z_N-on-Q_N byte form.
CUTOFF TIME: at this artifact's seal, 2026-08-15.
```

---

## 10. VERDICT

```text
PER-CLAIM: every load-bearing candidate claim tested (V-01..V-34) —
           CONFIRMED AT BYTES; zero refuted; zero unverifiable-as-cited.
OB-1..OB-9: CHECK-PASS each (§3).
HUNT: all nine hunt items — NOTHING FOUND (§4); three verdict-inert
      notes N-1..N-3, no correction required.
FENCE-SCAN: PASS (§5).

OVERALL: CHECK-PASS.

WHAT THIS VERDICT IS: the opposite-lane confirmation the mandate's entry
mechanics require — the candidate is internally lawful, honestly declared,
seal-true, fence-clean, and byte-consistent with every sealed instrument
it cites; its authored core is exactly the two declared clauses and
nothing else; its open legs are displayed, named, and falsifier-wired.
WHAT THIS VERDICT IS NOT: it is not ratification, not adoption, not a
discharge of the SM-6 conditional's open legs, and not a licence for any
construction to consume the candidate. The candidate remains
PROPOSED_NOT_ADOPTED until the principal's ENTRY addendum names, together,
the candidate digest and this check's digest.

RATIFICATION-RELEVANT SURFACES FOR THE PRINCIPAL (all already displayed
by the candidate; listed here as the check's independent confirmation of
what remains open):
  S-1 the sealed sequential-embedding byte form (C-4/C-6 continuum leg);
  S-2 R3_4's omega_in identification (SM-3-BIND);
  S-3 the Z_N-on-Q_N byte form (the §7 horn display);
  S-4 the strict SM-6 one-trace leg (the sealed M_2/M_3 binding row).

CANDIDATE DIGEST OF RECORD (named for the entry addendum):
  cc4af085c13e145865ec9768b84119c346c273b2a50a7b07ac6ee3275c3be021
THIS CHECK'S DIGEST: carried by the sidecar
  STAGE8_RULE2_CANDIDATE_T15SR_CHECK_V001.md.seal.sha256 (the freeze line
  of this check; computed at seal, after this file's last byte).

END OF CHECK. CHECK-PASS. THE CANDIDATE REMAINS PROPOSED_NOT_ADOPTED.
```

