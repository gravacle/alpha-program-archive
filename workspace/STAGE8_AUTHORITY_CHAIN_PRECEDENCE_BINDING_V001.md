# Stage 8 Authority Chain Precedence Binding v001

Date: 2026-07-29

## Authority

Principal decision:

```text
/Users/bgm/MB Work/alpha_supervision/AUTHORITY_CHAIN_PRECEDENCE_PRINCIPAL_DECISION_2026-07-29.md
SHA-256: 85bacbee7c9b6ef9f4e65e5eb898bd5273440d600fadd342c5556c2075f5dc8e
```

This record indexes the principal act into the cleanroom chain. It does not merge
the cleanroom and parent chains, decide any parent artifact's correctness, or
authorize any computation.

## Binding

The cleanroom chain governs all procedure. This includes
`CURRENT_AUTHORITY_LEDGER` lineage, V011, the seal ladder, the ratified A32
freeze, R-9, and the F-prime fence block.

Parent-tree material is admissible as evidence and as input only under the
cleanroom's external-citation discipline. Any such artifact must be cited by
path and hash on the face of the cleanroom artifact consuming it.

Parent-chain ordering instructions do not govern and are void where they
conflict with the cleanroom chain. Specifically void are:

```text
alpha-before-Thomson ordering
bare unsubscripted alpha_micro or unsubscripted k-symbol emissions
```

Nothing in the parent tree is retired, refuted, or discarded by this binding.
Evidence admission does not import the parent tree's premises. Any parent-tree
claim called derived in its own chain must still clear F'-1, F'-5, and R-9
before it can become load-bearing here.

## Scope

This artifact:

```text
merges_chains = false
parent_tree_retired = false
parent_premises_imported = false
parent_ordering_governs = false
C_L3_computed = false
kappa_record_computed = false
kappa_Thomson_computed = false
alpha_computed = false
proof_authorized = false
```
