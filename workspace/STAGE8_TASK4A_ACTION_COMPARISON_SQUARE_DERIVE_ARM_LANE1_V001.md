# STAGE8 TASK 4A: ACTION-COMPARISON SQUARE DERIVE ARM - LANE 1 V001

Date: 2026-08-03  
Task: PASTE 440 / Task 4a / square race derivation arm  
Lane: CODEX LANE 1  
Register head at preflight: Q-357  
Custody: derivation arm; no authored action content installed

```text
LEAD_RESULT = PARTIALLY_DERIVED

CONSUMER_TYPING =
  ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY = true | TYPE-P |
  scope: the divergence-determined transverse correction phi_div and its
         action-comparison/2PI square only |
  premises: the draft's own phi_div signature, DoR-015/V005, DoR-016,
            descent V003

UNIVERSAL_PHYSICAL_SCALAR_CONSUMER_CLASS_IS_QUOTIENT_ONLY = false | TYPE-R |
  witness: DoR-015/V005 endpoint-covariant route and its one-edge scalar

SQUARE_FORCED_PART =
  DoR-016 doubled endpoint access
    -> DoR-009 every-prefix traces
    -> V003 prefix-to-cycle descent D_G
    -> canonical pullback D_G^* on quotient action functionals;
  Q-313 T^char agrees on the sealed square and im(j_NM^Q) only;
  formal 2PI dependence on phi_div is fixed by Q-408

AUTHORED_RESIDUE =
  the divergence/action datum and generated quotient action member;
  completed-to-finite physical action restrictions rho_Gamma,N;
  an independently instantiated finite physical action bottom leg;
  the scalar comparison equality and normalization;
  the physical A/G tangent, stationary 2PI, complement inverse, and rho_H,N
  package

ACTION_COMPARISON_SQUARE_COMPLETE = false | TYPE-U
STATIONARY_2PI_DESCENT_COMPLETE = false | TYPE-U
PHYSICAL_P_VERDICT = NO_VERDICT

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The missing consumer certificate is true for the declared transverse action
correction and false as a universal statement about every physical scalar.
The draft itself fixes that scope: `phi_div` is a scalar correction on
`X_phys` or `K_cycle`, its required bridge contains a quotient-compatible
`CycleMap`, and its form battery requires path-visible quotient compatibility.
No endpoint vector, endpoint frame, or open-path transport occurs among the
arguments of `phi_div`.

DoR-016's endpoint-covariant law is not discarded. It is the upstream source
of the exact DoR-009 prefix traces. Once those traces are formed, the action
correction consumes their physical cycle class through `D_G`. Retaining the
raw endpoint tuple as another argument of `phi_div` would enlarge the draft's
declared signature and would be authored content, not derivation.

---

## 0. Preflight and register sweep

### 0.1 Locked process and current register

`LOCKED_PROCESS.md` was read in full and its local sidecar verified. The
questions-settled register sidecar verified. The live head was Q-357 before
substantive derivation.

```text
DOES_THE_OBJECT_EXIST = split |
  consumer-typing question: yes |
  forced comparison subdiagram: yes |
  complete action/2PI square: no | TYPE-U

IS_THE_VERSION_CURRENT = yes | register head Q-357

ARE_ITS_INPUTS_PRESENT = split |
  quotient typing and map layer: yes |
  action member and physical 2PI realization: no | TYPE-U

PREFLIGHT = PASS
```

### 0.2 Hash-verified load-bearing authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| transverse-action draft | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | `phi_div` signature, eight-field bridge, action residue |
| descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | family-natural `D_G`, kernel/image, restrictions, consumer split |
| extension V002 | `eb3675d525af7d1420c4ed033a5e5b94eb7494c1bac1305029b25ac9169567a0` | scoped `T^char`, corrected `j_NM^Q` square |
| DoR-015 / FIELD_SIGNATURE V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical quotient, cycle currents, two scalar routes |
| DoR-016 / network law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled endpoint law, exact trace, prefix towers |
| DoR-016 decision | `b4157df6f327e261f40389d5a3011a0aef66ee0f198d8ebba8b1b9303142d708` | ratified scope and unopened doors |
| Q-313 Map 1 | `ea8f4e9a79a9e4aca4a0b9e9aab470d78cd4d9c78794c20d9e979c0db16fbeab` | scalar character factorization and source/action distinction |
| Q-315 transport attempt | `f886284c632f238bb01d02de2cc64e3f7ed76c0bae4ba14487ac0b2bde7a22e3` | built bounded squares and absent physical restrictions |
| Q-408 stationary-response determination | `30532ecb2c08d21e28af05f1228b9c29264be99731f63c8cf5b30273bb51e7b8` | exact formal `phi` dependence |
| Q-324 off-section determination | `64561aca2a2cf7f2f0decf64278b6745018d6eb8052b8fa037efa5ff36a543a1` | exact scalar comparison condition `(OS-7)` |
| determiner Door-7 cross-review | sidecar PASS | existing-map correction and narrowed residue |

Every listed seal passed before use.

### 0.3 Questions-settled register entries checked

The pre-derivation sweep checked:

```text
Q-313  unique primitive-square Map 1 and source/action typing boundary;
Q-315  built source/raw restrictions; physical action/RetHess restrictions absent;
Q-321  quartic physical cycle action defeats two-jet determination;
Q-322  smooth Flat(S) family survives all source jets;
Q-323  flat germs are finite-visible off section; complement inverse scope;
Q-324  exact source constancy; scalar action comparison OS-7 absent;
Q-325  complement stationary response package named;
Q-326  exact phi dependence in mixed and stationary routes;
Q-327  divergence bridge package and phi_div signature;
Q-343  network-law V004 final review;
Q-344  DoR-016 ratification;
Q-345  finite network accumulation determined, overbroad Door 7 declared;
Q-346  Door 7 corrected to existing Map 1 plus extension plus action square;
Q-347  terminal-scalar extension boundary;
Q-348  every-prefix trace isomorphism;
Q-349  prefix family confirmed to carry rank-two cycle content;
Q-350  extension naturality typing defect localized;
Q-351  extension V002 repair installed;
Q-352  prefix-to-cycle descent D_G constructed;
Q-353  descent core confirmed and map split proved;
Q-354  quotient-only universality overclaimed;
Q-355  universality killed by endpoint-matched witness;
Q-356  V003 bounded restatement;
Q-357  V003 confirmed; map layer closed; consumer certificate commissioned.
```

No register result already constructs the completed action square.

### 0.4 Symbol collisions that control the result

```text
Z_N^CTP      = DoR-016/DoR-009 traced relative character;
(T_+,T_-)    = endpoint-covariant access pair, not a scalar action argument;
R_CTP        = T_-^dagger T_+, an endpoint endomorphism, not bilocal source R;
P_G^fam      = every-prefix trace presentation family, not a chosen filtration;
D_G          = P_G^fam -> Q_G physical cycle quotient;
T_G^char     = terminal-character map on its rank-0/1 scope, not D_G globally;
X_phys^005   = all-finite Gate-4/path-visible quotient, not endpoint data;
phi_div      = scalar physical action correction, not finite Gamma_N=-Log Z_N;
Gamma_fin,N  = finite source functional, not stationary Gamma_2PI;
D_G^*        = pullback of quotient functions, not the cycle-lattice dual map
               also denoted D_G^* in character notation;
rho_Gamma,N  = unbuilt physical action restriction, not rho_raw,N;
rho_H,N      = unbuilt RetHess restriction, not rho_raw,N.
```

---

## 1. A1 - consumer typing from the draft's own condition

### 1.1 Exact declared signature

The transverse-action draft states at lines 33-38:

```text
phi_div : X_phys or K_cycle -> scalar action correction.
```

It sharpens the missing bridge at lines 73-75 to a map from a certified
divergence datum to a scalar functional on `K_cycle`. Its required successor
package at lines 579-600 contains:

```text
CycleMap:
  quotient-compatible map to X_phys or K_cycle;

ActionComparisonSquare:
  source/depth-to-physical-action comparison;

FormCertificates:
  Flat(S), reality, path-visible quotient, finite restrictions.
```

Q-408's exact consumer uses a physical tangent split

```text
Y = C_prop direct-sum K_cycle
```

and evaluates derivatives of `phi` on that physical action carrier. It does
not add endpoint vectors or endpoint morphisms to `Y`.

### 1.2 Ratified carrier typing

DoR-015/V005 defines

```text
X_phys^005 = image((K_N)_N) subset product_N Q_N,
Q_N = U(1)^(E_N)/Gamma_N,
```

and proves that the basis-free conserved cycle currents separate this
quotient. V005 separately permits two kinds of physical scalar:

```text
route 1: closed-cycle products;
route 2: endpoint-covariant transport contracted with matching endpoint data.
```

Route 2 is a scalar on a joint endpoint tuple. It is not a function of
`X_phys^005` or `K_cycle` alone. The V003 one-edge witness proves this: the
cycle quotient is one point while the endpoint-contraction scalar varies.

Therefore the general V005 statement does not force every physical scalar
through `D_G`. Conversely, the draft's narrower signature excludes route 2
from `phi_div` unless endpoint data are added as a new argument.

### 1.3 DoR-016's role

DoR-016 ratifies the ordered pair `(T_+,T_-)` as emitted access data. The
receiver forms

```text
R_CTP = T_-^dagger T_+
```

and consumes it only through the DoR-009 finite trace

```text
Z_N^CTP
 = product_(j=1)^N chi_n(R_CTP,j)
 = product_(j=1)^N conjugate(z_(-,j)^n) z_(+,j)^n.
```

The endpoint route is therefore essential upstream of the trace. DoR-016
does not ratify a second endpoint argument for a transverse action, a joint
network scalar, or a completed physical contraction. Its six doors retain
those extensions as `TYPE-U`.

The exact chain is:

```text
endpoint-covariant access
  -> relative CTP endomorphisms
  -> ratified prefix traces
  -> physical cycle quotient D_G
  -> quotient action-functional slot.
```

The access data are consumed to make the trace. They are not silently carried
past `D_G` as an independent action coordinate.

### 1.4 A1 verdict

```text
ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY = true | TYPE-P |
  exact scope: phi_div and the divergence-determined transverse
               action-comparison/2PI square |
  proof: declared domain X_phys/K_cycle plus path-visible quotient battery

DOR016_ENDPOINT_ROUTE_IS_UPSTREAM_INPUT_TO_TRACE = true | TYPE-P

DOR016_ENDPOINT_ROUTE_IS_AN_INDEPENDENT_PHI_DIV_ARGUMENT = false | TYPE-S |
  roots: draft signature and bridge fields |
  query: endpoint vectors, frames, or T_sigma in phi_div domain

EVERY_PHYSICAL_SCALAR_CONSUMER_IS_QUOTIENT_ONLY = false | TYPE-R |
  witness: V005 route 2 and the one-edge endpoint contraction
```

This closes P-432-3 without reinstating Q-354's overbroad universality claim.

---

## 2. A2 - the forced part of the action-comparison square

### 2.1 Upstream traced-data leg

For every finite receiver stage, write

```text
Tr_pref,N^009 : H_N^CTP -> P_G^fam,
Tr_pref,N^009(h) = (Z_0^CTP,...,Z_N^CTP),
Z_0^CTP=1.
```

DoR-016 makes every prefix available through its per-tier factors. Faithful
characters `n in {+1,-1}` give the triangular inverse

```text
r_j^n = (Z_(j-1)^CTP)^(-1) Z_j^CTP.              (S-1)
```

Because `n in {+1,-1}`, the character is faithful and `n^(-1)=n`; hence the
carrier coordinate itself is recovered by

```text
r_j = ((Z_(j-1)^CTP)^(-1) Z_j^CTP)^n.
```

This is the Q-348/Q-349 prefix-trace isomorphism. No terminal-only inference
is used.

### 2.2 Physical cycle leg

Descent V003 supplies for every finite signed edge-resolved realization

```text
D_G:P_G^fam -> Q_G,
D_G([eta,Z])=q_G(Phi_eta(Z)),

h_(epsilon(j))=r_j^(s_j),

Hol_G(D_G([eta,Z]))(c)
 =product_j r_j^(s_j c_(epsilon(j))).             (S-2)
```

Its exact properties are:

```text
ker(D_G)=Phi_G^(-1)(Gamma_G),
D_G is surjective,
D_G^* is injective on the integral cycle lattice,
D_N P_f = rho_f D_M,
D_G(Theta Z)=Theta D_G(Z).
```

Thus it deletes exactly vertex rephasing and no record-visible cycle.

### 2.3 Forced comparison operation on scalar action functions

Let

```text
Act_N^quot := {f:Q_G->C with the declared action regularity and reality law}.
```

Once A1 is fixed, the only comparison operation supplied without new content
is pullback along `D_G`:

```text
Comp_N^forced := D_G^*,
D_G^*:Act_N^quot -> Fun(P_G^fam,C),
(D_G^* f)(Z)=f(D_G(Z)).                           (S-3)
```

This is not an action member. It says how any already-instantiated quotient
action is compared with prefix data. Since `D_G` is surjective,

```text
D_G^* f=D_G^* g  implies f=g.                     (S-4)
```

Therefore no extra quotient-action ambiguity is introduced by the
comparison pullback itself.

For a declared signed cellular restriction `f:G_N->G_M`, define pullbacks on
function spaces by

```text
rho_f^*(a_N)=a_N compose rho_f,
P_f^*(b_N)=b_N compose P_f.
```

Using `D_N P_f=rho_f D_M`,

```text
D_M^* rho_f^* = P_f^* D_N^*.                     (S-5)
```

Equation `(S-5)` is the family-natural forced face of the action-comparison
square. It is valid for every declared signed cellular restriction. The
upward version is asserted only for cycle-rank-preserving identity extensions,
exactly as V003 requires.

### 2.4 Existing Map 1 occupies its proven scope only

On the sealed primitive square,

```text
T_N^char = Hol_(c_square)^(-1) compose Z_N,
D_(G_square)=T_N^char.                            (S-6)
```

Extension V002 and descent V003 further prove agreement on
`im(j_NM^Q)` under cycle-rank-preserving extension. Thus, for every quotient
action `f` on that scope,

```text
f compose D_G = f compose T_G^char.               (S-7)
```

The equality is not extended off that image. For the cycle-plus-pendant
witness, `D_G` removes `w` while `T_G^char` retains it. `T^char` therefore
serves as the sealed rank-0/1 compatibility leg; it is not substituted for
the full-family physical descent.

### 2.5 Full finite forced diagram

The constructed finite diagram is:

```text
H_N^CTP
  | Tr_pref,N^009
  v
P_G^fam ----------------D_G----------------> Q_G
  |                                          |
  | evaluate D_G^* f                         | f in Act_N^quot
  v                                          v
  C -------------------------identity-------> C
```

Equivalently, the scalar composite for an already-instantiated finite quotient
action `f` is

```text
f compose D_G compose Tr_pref,N^009.          (S-8)
```

On the sealed square, `(S-8)` may be computed through `T_N^char`; at higher
rank it must use the prefix-family descent `D_G`.

### 2.6 What the finite trace does not force

The DoR-009 trace is not itself a transverse action. The pendant regression
makes this exact. Set all cycle characters to one and a pendant character to
`w!=1`. Then

```text
D_G(Z)=identity cycle class,
Z_terminal=w,
T_G^char(Z) has holonomy w.                       (S-9)
```

No function on `Q_G` can reproduce the full terminal trace on both this
history and the history with `w=1`. Therefore the square cannot lawfully use

```text
finite Gamma_N=-Log Z_N = finite physical transverse action
```

as an identity on the complete family. Q-313's source/action distinction is
preserved. The finite trace supplies the data from which `D_G` extracts the
physical cycle class; it does not supply the action value on that class.

### 2.7 Forced formal 2PI placement

For any future completed physical action

```text
Gamma_phi = Gamma_base + phi_div
```

on a typed chart `Y=C_prop direct-sum K_cycle`, Q-408 fixes the algebraic
effect of the correction:

```text
Delta H_CC = D_C^2 phi_div,
Delta H_CK = D_C D_K phi_div,
Delta H_KC = D_K D_C phi_div,
Delta H_KK = D_K^2 phi_div,                       (S-10)

delta c_phi[psi]
 =-[D_C^2 Gamma_phi]^(-1) D_C psi(c_phi,k),       (S-11)
```

where `(S-11)` uses only a separately proved complement inverse. These
formulas are forced conditionally on the action and domain. They neither
construct `phi_div` nor identify source Hessians with action Hessians.

### 2.8 Restriction obligations under DoR-008

Any completed square must provide physical restrictions

```text
rho_Gamma,N : Act_phys -> Act_N^quot,
rho_H,N     : RetHess_phys -> RetHess_N,
```

and prove:

```text
1. rho_Gamma,N is compatible with (S-5), reality, and batching;
2. differentiation commutes with rho_Gamma,N on the declared domain;
3. rho_H,N transports the physical block/Schur construction to an
   independently instantiated finite physical action bottom leg;
4. finite restriction does not silently commute with inversion without a
   reducing/intertwining theorem;
5. every corresponding Q-243/Q-279 shadow is reproduced exactly:
     equal-history identity;
     exact DoR-009 trace upstream;
     zero and p-free finite cycle/source mixed block;
     exact symbolic noise/dephasing block;
     reality, identity extension, and batching.
```

The last list is a falsifier target, not a license to rename the finite source
block as a stationary action Hessian. Q-313 and Q-315 explicitly forbid that
promotion.

### 2.9 A2 verdict

```text
PREFIX_TRACE_LEG = BUILT | TYPE-P
FULL_FAMILY_CYCLE_LEG_D_G = BUILT | TYPE-P
QUOTIENT_ACTION_PULLBACK_D_G_STAR = BUILT | TYPE-P
FAMILY_NATURAL_PULLBACK_SQUARE = PASS | TYPE-P
Q313_TCHAR_COMPATIBILITY = PASS_ON_SEALED_SCOPE | TYPE-P
Q408_FORMAL_2PI_DEPENDENCE = DERIVED_CONDITIONAL | TYPE-P

PHI_DIV_MEMBER = NOT_BUILT / TYPE-U
PHYSICAL_ACTION_RESTRICTIONS = NOT_BUILT / TYPE-U
FINITE_PHYSICAL_ACTION_BOTTOM_LEG = NOT_BUILT / TYPE-U
STATIONARY_2PI_BLOCK_SYSTEM = NOT_BUILT / TYPE-U
PHYSICAL_RETHESS_RESTRICTION = NOT_BUILT / TYPE-U

SQUARE_FORCED_PART = BUILT
ACTION_COMPARISON_SQUARE_COMPLETE = false | TYPE-U
```

---

## 3. A3 - exact authored residue

The derivation leaves no consumer-route choice for `phi_div`. It leaves the
following content, which this arm does not author:

### 3.1 Divergence/action datum

```text
TRANSVERSE_ACTION_DATUM := (
  a certified divergence object or coefficient;
  a typed depth map to the sequential record system;
  a stage-compatible accumulation/subtraction rule;
  a named topology and normalization;
  a generated quotient functional phi_div on X_phys/K_cycle;
  uniqueness or an explicit residual family;
  Flat(S), off-section, reality, rank, finite-visibility, and provenance
  certificates
).
```

The original draft proves this datum is absent. DoR-016 and `D_G` supply its
finite information carrier, not its action content.

### 3.2 Scalar action restriction and comparison package

```text
ACTION_VALUE_COMPARISON_PACKAGE := (
  completed action class Act_phys;
  finite quotient action classes Act_N^quot;
  rho_Gamma,N with separation and naturality;
  an independently instantiated finite physical action bottom leg;
  the scalar equality comparing that bottom leg with D_G^* rho_Gamma,N;
  action units, normalization, branch/domain, and common-origin provenance;
  proof that no full terminal/gauge content is re-imported after D_G
).
```

This is the missing content of Q-324's `(OS-7)` after replacing its formerly
unbuilt cycle map with the now-confirmed `D_G`. The type-correct source-side
argument is the physical prefix quotient represented by `D_G`, not an
arbitrarily selected raw history.

### 3.3 Physical stationary 2PI package

```text
PHYSICAL_2PI_PACKAGE := (
  completed physical Abar and G tangent carriers;
  source-field and bilocal-source/response intertwiners;
  Gamma_base and Gamma_phi on one common domain;
  physical stationary locus and G_*;
  Gamma_AA, Gamma_AG, Gamma_GG, Gamma_GA;
  complement reducing certificate and inverse/prescription;
  Schur and retarded extraction;
  rho_G,N, rho_H,N, differentiation/restriction and inverse/restriction
  theorems;
  measure, contour, boundary/contact, closure, tail, and door accounts
).
```

Neither `D_G^*` nor the formal identities `(S-10)` and `(S-11)` construct
these objects.

### 3.4 Residue verdict

```text
AUTHORED_RESIDUE =
  TRANSVERSE_ACTION_DATUM
  + ACTION_VALUE_COMPARISON_PACKAGE
  + PHYSICAL_2PI_PACKAGE

ENDPOINT_CONTRACTION_MEMBER_OR_ROUTE_SELECTION = not part of this residue |
  reason: upstream DoR-016 access is already ratified; phi_div signature is
          quotient-only

NEW_ACTION_CONTENT_AUTHORED_BY_THIS_ARM = false | TYPE-S
```

---

## 4. A4 - falsifiers and executed finite tests

### 4.1 Consumer-typing falsifier: one-edge endpoint variation

Take the one-edge tree `e:s->t`. Then

```text
C_G={0},
Q_G={*},
D_G(any prefix history)=*.
```

For matching endpoint data, V003's witness gives

```text
S_e(v_t,T_e,v_s)=<v_t,T_e v_s>.
```

The tuples `(1,1,1)` and `(1,1,u)` with `u!=1` have the same `D_G` output and
different `S_e`. Therefore:

```text
any phi_div:X_phys/K_cycle->C has equal values on the two histories;
any proposed phi_div that varies between them has imported route-2 data and
fails the declared signature.
```

```text
FALSIFIER_A1_ONE_EDGE = PASS
QUOTIENT_ONLY_TYPING_REFUTED = false
UNIVERSAL_SCALAR_QUOTIENT_ONLY_CLAIM_REFUTED = true
```

### 4.2 Prefix reconstruction

At `N=2`, let the faithful relative characters be `r_1^n=u` and `r_2^n=v`.
Then

```text
Z_0=1,
Z_1=u,
Z_2=uv,
Z_0^(-1)Z_1=u,
Z_1^(-1)Z_2=v.
```

Thus the every-prefix trace reconstructs both cell characters exactly. The
terminal trace `uv` alone would not. This runs the trace-sufficiency
falsifier on the smallest nontrivial stage.

```text
FALSIFIER_PREFIX_TRIANGULAR_INVERSE = PASS
TERMINAL_ONLY_SUBSTITUTION_USED = false
```

### 4.3 Sealed square compatibility

For `c_square=(1,-1,1,-1)` and the signed presentation used by V003,

```text
Hol_(c_square)(D_G(Z))=product_j r_j=Z_N,
D_(G_square)=Hol_(c_square)^(-1)Z_N=T_N^char.
```

```text
FALSIFIER_Q313_EXISTING_MAP = PASS
RIVAL_MAP1_CONSTRUCTED = false
```

### 4.4 Pendant gauge-content attack

Set every cycle character to one and a pendant character to `w!=1`:

```text
Hol_c(D_G(Z))=1,
Z_terminal=w,
Hol_c(T_G^char(Z))=w.
```

The test confirms:

```text
1. D_G deletes only Gate-4 tree/gauge content;
2. T^char cannot replace D_G off its certified scope;
3. the full finite trace cannot be equated with a quotient-only action value.
```

```text
FALSIFIER_PENDANT = PASS
FULL_TRACE_EQUALS_TRANSVERSE_ACTION = REFUTED | TYPE-R
```

### 4.5 Rank-two cycle survival

At the S8-A stage with edges `(a,b,d)`, V003 gives

```text
c_1=(1,1,0),
c_2=(0,1,1),
c_3=(1,0,-1),

H_(c_1)=Z_2,
H_(c_2)=Z_1^(-1)Z_3,
H_(c_3)=Z_1 Z_2 Z_3^(-1).
```

Under the admitted edge exchange, `H_(c_3)` is inverted, not deleted. Hence
the full prefix descent carries the record-visible rank-two cycle that the
terminal scalar obstruction lost.

```text
FALSIFIER_S8A_C3 = PASS
NO_RECORD_VISIBLE_CYCLE_DELETED = true
```

### 4.6 Identity extension, batching, and reality

Appending an identity cell gives `r_(N+1)=1`, so every old prefix character
and `D_G` class is unchanged on its lawful restriction. Consecutive batching
uses

```text
r_[a,b]=Z_(a-1)^(-1)Z_b
```

and equals the corresponding cochain pullback. Branch exchange/conjugation
sends every `Z_m`, `r_j`, and edge character to its conjugate, hence

```text
D_G(Theta Z)=Theta D_G(Z).
```

Pullback `(S-3)` preserves the action reality law.

```text
FALSIFIER_IDENTITY_EXTENSION = PASS
FALSIFIER_BATCHING = PASS
FALSIFIER_REALITY = PASS
```

### 4.7 Physical 2PI restriction falsifier

The failure-capable target is:

```text
rho_H,N(
  Gamma_AA-Gamma_AG Gamma_GG^(-1) Gamma_GA
)
=
Gamma_AA,N-Gamma_AG,N Gamma_GG,N^(-1) Gamma_GA,N,
```

together with exact reproduction of the Q-243/Q-279 finite pattern. It
cannot be run because neither side's finite physical action block system,
`rho_H,N`, nor the completed physical action exists. The already-built
source/raw zeros are not substituted.

```text
FALSIFIER_PHYSICAL_2PI_RESTRICTION = NOT_EXECUTABLE | TYPE-C |
  constraints: finite physical action bottom leg, completed action,
               stationary blocks, inverse, and rho_H,N absent

DOR008_FALSIFIER_FIRED = false | TYPE-S |
  scope: every executable forced-map check passes; absent physical comparison
         not reported as a pass
```

---

## 5. A5 - selections, symbolic standing, accounts, and fences

### 5.1 No-selection scan

```text
rank or rank ratio selected = false
p evaluated = false
orientation member selected = false
endpoint frame selected = false
filtration member selected = false
cycle basis selected = false
realization member selected = false
chain-map member selected = false
flat/action member selected = false
endpoint contraction selected = false
joint network contraction selected = false
measure, contour, boundary, or inverse selected = false
```

The full signed presentation family and complete cycle lattice are retained.
All occurrences of `p` remain symbolic.

### 5.2 Six-account rows

| Account | Forced content | Residue | Tail/topology verdict |
|---|---|---|---|
| measure | none used by `Tr_pref`, `D_G`, or `D_G^*` | physical action/2PI measure | `TYPE-U`; no measure imported |
| contour | finite CTP order and reality inherited through DoR-016 trace | completed interacting contour | `TYPE-U`; no new boundary limit |
| boundary/contact | signed incidence boundary and cycle quotient | physical action contact/subtraction law | `TYPE-U`; pendant and one-edge tests pass |
| domain closure | finite compact prefix and quotient classes only | completed action and common A/G domain | `TYPE-U`; no weak-star/bidual step |
| stationary Schur | formal `(S-10)` and `(S-11)` | blocks, reducing complement, inverse, extraction | `TYPE-U`; check not executable |
| class formation | quotient function pullback `D_G^*` | `Act_phys`, `phi_div`, finite action image and separation | forced pullback creates no class or tail; residue `TYPE-U` |

### 5.3 Door flags

```text
DOOR_PREFIX_TRACE = CLOSED | finite ratified DoR-009/016 data
DOOR_PREFIX_TO_CYCLE = CLOSED | V003 D_G
DOOR_QUOTIENT_FUNCTION_PULLBACK = CLOSED | D_G^*
DOOR_ENDPOINT_ACTION_ARGUMENT = NOT_OPENED | not in phi_div signature
DOOR_ACTION_MEMBER = OPEN / TYPE-U
DOOR_ACTION_RESTRICTION = OPEN / TYPE-U
DOOR_PHYSICAL_2PI = OPEN / TYPE-U
DOOR_WEAK_STAR = NOT_USED
DOOR_BIDUAL = NOT_USED
DOOR_DISTRIBUTIONAL = NOT_USED
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

No evaluation, root, coupling value, response value, or comparison to a
measured constant was performed.

---

## 6. Final determination

The square race's derivation arm closes one question and constructs one
canonical functional face:

1. The draft's transverse correction is quotient-only by its own declared
   domain and battery.
2. DoR-016 endpoint access remains essential upstream, but only its ratified
   prefix traces enter the transverse comparison.
3. `D_G^*` is the forced, family-natural comparison of quotient action
   functionals with prefix data.
4. `T^char` agrees with this comparison on its sealed scope and is not used
   beyond it.
5. The action member, physical action restrictions, independent finite action
   bottom leg, and stationary 2PI package remain absent.

```text
CONSUMER_TYPING = QUOTIENT_ONLY | TYPE-P | scope: phi_div square
SQUARE_FORCED_PART = BUILT | prefix trace -> D_G -> D_G^*
AUTHORED_RESIDUE =
  TRANSVERSE_ACTION_DATUM
  + ACTION_VALUE_COMPARISON_PACKAGE
  + PHYSICAL_2PI_PACKAGE

ACTION_COMPARISON_SQUARE_COMPLETE = false | TYPE-U
PHYSICAL_P_VERDICT = NO_VERDICT

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
