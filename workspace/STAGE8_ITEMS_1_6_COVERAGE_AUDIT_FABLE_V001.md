# STAGE8_ITEMS_1_6_COVERAGE_AUDIT_FABLE_V001

LANE: FABLE (blind record-native auditor, codename FABLE-COVER-16)
DATE: 2026-08-12
TASK: For road items 1, 2, 3, 5, 6 of the Surface Definition of Record §VI, determine whether
each claim is GENUINELY BOOKED/DERIVABLE from the defined surface or GLOSSED. Item 4 skipped
(struck rail). Item 7 skipped (the open S16 edge, handled elsewhere). All results CLAIMED.

FENCES HELD: nothing computed or evaluated — no number, coupling, kappa, scale, or
fine-structure value; no measured-constant comparison; every scale symbolic. No
QUESTIONS_SETTLED_REGISTER, EXECUTION_TRACKER, CONTINUATION_STATE,
PRINCIPAL_EXPECTATIONS_LEDGER, THE_PLAN, or ROAD_* file was read. Structural/typing work only.
Nothing registered, committed, or pushed.

## 0. ANCHOR AND SOURCES — seals verified before reading

| File | SHA-256 | verified |
|---|---|---|
| `alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` (the anchor) | `20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df` | MATCH |
| `workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` ("04c10c0d") | `04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d` | MATCH |
| `workspace/STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` ("1d11f150") | `1d11f15040f8b85b7e081fccfeddb995c41941c55464d759a2fa91a8feffc775` | MATCH |

Additional named files read (workspace, read-only, per the item-2/item-3 search authorization):
`STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md` (relay 751 — the transport-field board; sidecar
`4d072e76…` present), `STAGE8_B2_GLUING_SCOPE_AND_RUN_CODEX2_V001.md` (relay 808 — the sealed
gluing signature and N_r diagnostic), `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md`
and `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md` (the built finite causal
parent), `R3_4_CAUSAL_TRANSPORTED_WRITE_TAIL_SPEC_V001.md` (join-uniqueness candidate
constraint). Anchor §I–III taken as given, per assignment.

## 1. ITEM 1 — BRANCH-STATE MEASURE (claim: rides on the free scale; allow side)

**Verdict: COVERED.** The claim is genuinely booked; the hidden-requirement hunt came back
negative on the absolute side.

- The free scale is booked: anchor §II.7 (BID Absolute Record-Scale Identifiability Gate,
  `8f9bdd93`): `(T_R -> lambda T_R, H_R -> H_R/lambda)` leaves **all first-opening
  probabilities, orthogonality, endpoint transfer, and projective lengths identical**. So the
  measure's probabilistic content is orbit-invariant and the ONLY scale content in the supply
  is the absolute normalization — which is exactly the free scale. The claim "not independent
  of the scale question" is the correct direction: the normalization IS the free modulus.
- The supply is allow-side of record: `Omega_c` is **assigned by the parent** with support
  containment (CAUSAL_INCIDENCE_SUPPORT `:18-24` as quoted at 04c10c0d §1.2), and its
  **uniqueness is open** (`:77-78`). An open uniqueness is the booked statement that no surface
  requirement selects the supply. The built parent confirms the typing: "any declared
  finite-energy incoming excitation treated as **boundary data rather than a new coupling
  selector**" (R3_4 parent spec, In-state section).
- Hidden-requirement hunt, each candidate checked and each found dimensionless/structural,
  forcing no scale:
  (a) support containment — an admissibility constraint on the supply, not a selector
  (uniqueness open);
  (b) the intra-cell time marginal `w(s) = 32 min(s,1-s)^3` is FORCED (unique uniform intrinsic
  measure; executed in the R3_4 result with "No pulse parameter or fitted interior profile") —
  but this is forced **dimensionless** structure, an instance of anchor §II.10, not a scale
  requirement; the envelope's dimensionful prefactor `tau_R/T_R` rides the same free orbit;
  (c) the internal normalization is **pinned** (derived `ell^2` Hilbertization, unit-modulus
  lift, 04c10c0d §1.1: "the internal normalization is fully pinned and dimensionless; the
  entire conversion freedom sits at the junction") — pinned dimensionless, free absolute:
  exactly the anchor's split;
  (d) an amplitude requirement cannot hide in the supply at all: the surface is unit-modulus
  only (anchor §II.6, Q-290 TYPE-R) — the carrier for a hidden magnitude requirement provably
  does not exist.
- In the booked closure form the branch data enter **dimensionless**: `F(T_R/t_P,
  dimensionless branch data)` (BID `:72` as quoted at 04c10c0d §0).

## 2. ITEM 2 — TRANSPORT FIELDS (claim: connection native; support and current-density FREE, allow)

**Verdict: COVERED — the freedom claim tested TRUE of record, with two precision notes booked
below.** The triple (connection / support / current-density) is three of the seven RA27-3
transport fields (relay 751 board: incidence, degree, coframe, connection, volume, support,
current density).

- **Connection native — booked.** Anchor §I.1 (Q-1005/Q-1006, both-lane): the record's
  dynamics is indexed by connection/holonomy histories alone. Independently at the sealed
  build 1d11f150 §4 / member 03 `[4243,5977)`: the ratified finite influence functional is
  indexed by CONNECTION/HOLONOMY HISTORIES ALONE — no metric argument anywhere. And the
  connection is native as data on the complex: edge data, gauge class `[U_0]`, cochain
  structure carried on edges (V011 `aa7c6d49…[44595,44690)` as pinned at 751 §2.1).
- **Current-density FREE — booked.** 751 §2.4: `current density` = **UNSUPPLIED** — three
  corpus mentions, two of them the demand restating itself; **no transport law exists**;
  nothing upstream complete. Nothing sealed forces one; a supply must come from outside =
  allow. Confirmed downstream: the B2 gluing signature CONSUMES current-density normalization
  and the corresponding receiver components are absent (`s_g, S_g` — relay 808 §3). Genuinely
  free.
- **Support FREE — holds of record.** 751 §2.3: support transport = **UNDERDETERMINED** — "Not
  obstructed, not supplied." No sealed surface constraint forces it; nothing sealed supplies
  it. The placement freedom is likewise booked (`Omega_c` uniqueness open). The B2 receiver
  census records the support components absent (`f_g, F_g`). So "free (allow)" is the correct
  of-record typing: nothing forces, supply is external.
- **Precision note 1 (support).** The freedom is placement/transport-law freedom, NOT profile
  freedom: once a cell is placed, its intra-cell support is forced — the spatial support is
  the diamond section and the envelope is the pushforward of the classified unique uniform
  intrinsic measure ("A residual interior scalar profile, post-evaluation pulse reshaping, or
  independently normalized tail is **forbidden**" — R3_4 parent spec; executed with no pulse
  parameter in the result). And 751 §2.3 itself records the refinement-transport law as
  "structurally immediate" — unsealed, deliberately not authored. Both bounds are
  dimensionless/structural (anchor §II.10 territory), so they bound the freedom without
  forcing a number or a scale. A reader of row 2 should not take "free" to mean the profile
  is choosable.
- **Precision note 2 (connection).** "Native" is booked for the OBJECT; the connection's
  refinement TRANSPORT is of record UNDERDETERMINED with a **proven physical free datum** —
  the refined edge assignment, free on a 32-dimensional set at fixed energy on A2's instance,
  with the difference NOT a coboundary (738, reproduced independently at 751 §2.1; A1
  untested, not reported as forced). This freedom is allow-side and forces nothing, but row
  2's compression does not book it; the one PROVEN transport freedom in the triple sits on
  the connection, not on support or current-density.
- **Hidden-requirement hunt (the definition says free — tested):** the write/tail covariance
  join is UNIQUE with "no relative coefficient" and "without changing either normalization or
  selecting a coupling" (R3_4 write-tail spec) — a structural uniqueness, removes a
  coefficient rather than forcing a supply; the negative controls of the parent spec bar any
  independent primitive `F^2`/Pauli/tail/damping term. No candidate constraint forces support
  or current-density into existence, and none forces a scale. The freedom claim survives.

## 3. ITEM 3 — GLUING OBSTRUCTION (claim: a DERIVED junction => beta-invariant, scale-free)

**Verdict: COVERED** — derivable once its inputs exist, and beta-invariant as-typed.

- **The object.** The gluing obstruction is the RA27-4 diagnostic, sealed by signature at D012
  `[30747,33958)` (hash-pinned at relay 808, span `b6844e3c…`):
  `N_r(d_K) := [(r_F^*)^* Eval_R(eta_resp(r) d_K)]_resp - [Eval_K(d_K)]_resp in Q_resp(K)`,
  with the gluing condition `N_r(d_K) = 0`.
- **Derived-once-inputs-exist — verified.** The formation rule is completely typed by the
  sealed signature: the addressed cospan must carry incidence, degree, connection, coframe,
  volume, current-density normalization, and support, and the carrier chain
  `r_F^*, eta_resp, Eval_K, Eval_R, (r_F^*)^*` is closed. There is no free coefficient and no
  choice in forming it — corroborated by the discipline of relay 808 itself, which REFUSED to
  run precisely because "Returning zero would replace missing response and density carriers by
  emptiness; returning nonzero would author an obstruction." An exact expression that cannot
  even be inflected without violating the signature is derived-by-signature, pending inputs.
- **Beta-invariant — verified directly on the signature, not assumed.** The test is the
  census's own executed form (04c10c0d §0 and C2: if `beta` does not appear in the condition,
  every member of the beta family passes identically): the sealed signature contains **no
  occurrence of the transverse fiber radius and no fiber datum of any kind** — its inputs are
  cochain/response carriers on the 4D addressed cospan, its volume input is the forced
  intrinsic `Vol_4` ("carried as forced; no alternate measure" — 808 §4), and its own custody
  block records "metric = none adopted". Under `R -> beta R` at fixed `Delta tau`, nothing in
  `N_r` moves. Scale-free in the lambda-orbit sense for the same reason: the diagnostic is a
  naturality residual — the same datum through two legs of one square — so its vanishing is a
  scale-free condition.
- **Caution booked.** Anchor §II.8's quantifier is a census over the THIRTEEN record-cell
  interface quantities; the gluing obstruction is not one of the thirteen, so row 3 must not
  be read as covered BY that census. It is covered by the direct as-typed check above — same
  test, run on this object's own sealed signature. Conclusion identical; basis independent.
- **Residual correctly located elsewhere.** Of record the obstruction is UNFORMED
  (`OBSTRUCTION = n-a`; six receiver components absent: `f_g, F_g, s_g, S_g, delta_K', d_g`).
  Those absences are exactly item 2's declared free supplies (support, current,
  density/response) — the definition's own allow side — not a gap in item 3's claim.

## 4. ITEM 5 — BETA-INVARIANCE -> COMMON CELL (claim: the defined property; the common cell is the free-orbit presentation)

**Verdict: COVERED** — the free-orbit presentation is a genuine surface object, and it is not
a rail.

- **The property is booked:** anchor §II.8 = the sealed census (04c10c0d §1.3, four
  adversarial verifiers, degeneracy test executed twice).
- **The common-cell object is surface-made.** The free-orbit presentation is the cell
  presented modulo the booked free scale action, and every constituent is booked: the orbit
  and its freeness (§II.7, BID gate `8f9bdd93`); the entire dimensionless structure forced
  (§II.10 — `tau_R = pi/sqrt(2)`; `causal_diamond_shape_derived = true`; the internal
  normalization fully pinned and dimensionless, 04c10c0d §1.1); and the derived junctions are
  FIXED POINTS of the scale action (§II.8–9), so they descend to the orbit presentation with
  no representative chosen. Nothing beyond booked surface results is needed to present it.
- **Rail check — passes, and the rail is named by the anchor itself.** §III.11: the rail is a
  CHART on the free presentation orbit (the internal/external metric conversion; its selection
  a modulus of an imported fiber). The free-orbit presentation is precisely the object with NO
  chart chosen; the orbit is the surface-side term of §III.11's own sentence. Covered object =
  the orbit; rail = selecting a point on it. Item 5 consumes the former only.
- **Disambiguation booked, adversarially.** The 7A gluing grammar contains a DIFFERENT object
  with a similar name: the "common junction cell" / common-refinement cell of two complexes.
  That object is NOT covered by item 5 and is not formed anywhere of record ("common junction
  cell formed = false", 808; "No common cell formed", 751 gates), and its prerequisite
  same-region relation is of record NOT forced and NOT unique (RA27-2:
  `RA27_2_FORCED_BY_STOCK = false`, `RA27_2_CLOSURE_UNIQUE = false`, one-parameter
  countermodel; the stitching scoping types it as possibly requiring ADOPTION). The anchor's
  row 5 claim is the free-orbit reading and is genuine on that reading; any reading of row 5
  as supplying the gluing grammar's common junction cell would be a gloss and is here flagged
  as NOT claimed.

## 5. ITEM 6 — JUNCTION (claim: the derived junction inventory, 13 interface quantities, all beta-invariant)

**Verdict: COVERED — with the quantifier precision booked.**

- **The inventory is the derived object of record:** the thirteen sealed interface quantities
  census at 04c10c0d §1.3, each member carried as a sealed-text quotation at source, the
  census adversarially verified (all four verifications CONFIRMED; the decisive degeneracy
  test executed independently by attacker and verifier with identical result).
- **All DERIVED members beta-invariant — booked, member by member:** the marker
  `|Delta S_record| = pi hbar` (an action, a fixed point of the scale action, §II.9);
  `m_* T_R = pi` (contains no `R`, trivially beta-invariant — the brief's one cross-geometry
  lock); the balanced-geodesic identities; the onset inequality. Each pairs its two feet into
  a dimensionless, scale-orbit-invariant object (04c10c0d §1.3, first bullet).
- **Quantifier precision — the one place row 6's compression could mislead.** "All
  beta-invariant" binds the DERIVED members only. The inventory of thirteen contains three
  beta-SENSITIVE members and NONE of them is derived: the fiber proper radius `R` in `K_KK`
  (ADOPTED ansatz; `K_KK` changes by `beta^2`), the complete normalized `D_BR` spectrum
  (UNBUILT), the skeleton-to-cell embedding (GAP, "has never been attempted"). Read as "all
  thirteen beta-invariant" the row would be FALSE of record. The anchor's own §II.8 states the
  quantifier correctly ("every junction that is *derived* is beta-invariant"), and its §III.12
  books the three exceptions on the rail side — so the definition as a whole is not glossed;
  row 6's shorthand must be read through §II.8.
- The covered claim consumes no rail: the three adopted/unbuilt/gap members sit on the
  NOT-derived side and are excluded from the "all" by the correct quantifier.

## 6. RAIL CHECK ACROSS ALL FIVE VERDICTS

No COVERED verdict above rests on §III's rail (the internal/external metric conversion) or on
any adopted beta-sensitive member. Near-rail objects were identified and checked as
NOT consumed: the `C_R = 1` selector (ADOPTED Level-1, external-side consultation only — no
item above consumes it); the `K_KK` fiber radius (ADOPTED — inside the item-6 inventory but on
the not-derived side, outside the covered quantifier); the chart on the free orbit (§III.11 —
item 5 is defined by NOT taking it).

## 7. LIMITS OF THIS AUDIT

Claims verified against the named sealed artifacts and the anchor only; register entries
cited by the anchor (e.g. `:5187`, `:224`, `:6093`) were NOT read (fenced) and are trusted as
anchor consolidation, which is the assignment's stated basis (§I–III given). The RA27-3 board
and the B2 gluing scope are lane results marked CLAIMED/verified in their own custody chains;
they were used as sealed sources, seals present, not re-derived. Nothing here evaluates any
quantity; every scale symbolic.

---

ITEM_1 = COVERED (branch-state measure allow-side; only scale content is the absolute
normalization = the booked free orbit, BID `8f9bdd93`; supply constraints all
dimensionless/structural, uniqueness open — no hidden requirement)
ITEM_2 = COVERED (connection native — anchor §I.1 / 1d11f150 member 03; current-density
UNSUPPLIED-free — 751 §2.4; support free of record — 751 §2.3 not-obstructed-not-supplied,
`Omega_c` uniqueness open; precision booked: intra-cell support profile FORCED dimensionless
(R3_3/R3_4), and the connection's refinement transport carries a proven 32-dim physical free
datum — neither forces a scale)
ITEM_3 = COVERED (N_r(d_K) formation sealed-by-signature at D012 `[30747,33958)`, no free
coefficient — derivable once the six absent receiver components exist; beta-invariant
as-typed: no fiber-radius occurrence in the sealed signature, verified directly, not via the
thirteen-quantity census quantifier)
ITEM_5 = COVERED (the free-orbit presentation is surface-made — §II.7 orbit + §II.10 forced
dimensionless structure + §II.8–9 fixed points descend; the rail is the chart on the orbit,
not the orbit; flagged: this does NOT supply the 7A common junction cell, whose same-region
relation is of record not forced, non-unique, adoption-shaped)
ITEM_6 = COVERED (the thirteen-quantity census is the sealed derived object, four verifiers;
all DERIVED members beta-invariant — `|Delta S| = pi hbar`, `m_* T_R = pi`,
balanced-geodesic identities, onset bound; quantifier binds derived members only — three
beta-sensitive members exist and none is derived)
OVERALL = ALL-COVERED
RAIL_CHECK = none — no covered verdict rests on a rail; near-rail objects (`C_R = 1` adoption,
`K_KK` fiber radius, the orbit chart) identified and not consumed
FENCE_RESPECTED = CERTIFIED
SURFACE_ANCHOR = present, seal verified at read time:
`20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df`

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
