# STAGE 8 TASK 5 / R4 — KERNEL REALIZATION AND SYMBOL-CALCULUS CROSS-VERIFICATION — LANE 2 V001

Date: 2026-08-04  
Task: PASTE 486 / Task 5 / R4  
Lane: Codex Lane 2  
Custody: adversarial verification followed by derivation-first construction

## Lead result

```text
PARTIAL_BUILD = CONFIRMED |
  the completed conserved-current seam, minimal profile Banach presentation,
  affine reader dual, rank-one formula, and coordinate counts recompute.
  Precision: the l1 direct-sum norm is a canonical representative of an
  equivalent finite-product Banach topology, not the uniquely forced norm;
  and O_prof is an abstract quotient presentation until a physical kernel
  topology makes its map into O_R5 continuous.  Neither precision changes
  the reader dual or its coordinates.

KERNEL_REALIZATION = OBSTRUCTED |
  BUILT: on every finite/cylindrical stage, conserved cycles map canonically
  to divergence-free distributional path currents, and every profile operator
  maps faithfully to a bilocal distribution kernel with exact Ward,
  covariance, reality, restriction, batching, quotient, and R4-unit rules.

  OBSTRUCTED: no ratified uniform continuity estimate extends that map from
  the A4 Hilbert completion to a named distribution-kernel topology; and the
  available zero-extension system is not a geometric refinement system, so
  it supplies no shrinking mesh, density, common-refinement limit, diagonal
  locality, or physical long-wavelength Maxwell symbol.  The missing clauses
  are exactly the R12 refinement/density and local-symbol calculus interfaces.

FIBER_CONSTRAINT = NO_NEW_PHYSICAL_RELATION |
  the derived cylindrical kernel map is faithful and every cycle kernel is
  Ward-transverse, so it imposes no additional relation, normalization, sign,
  or magnitude bound on (chi_K,T).  The only relations remain Rel_G,
  covariance/reality/restriction/batching, and admitted stabilizers.
  Reciprocal profile dimension remains 1; S8-A remains 2 before additional
  stabilizer relations.  No member is selected.
```

This result builds more than the submitted stop—the finite/cylindrical
current-kernel calculus is now explicit—but it does not relabel a bilocal path
kernel as a local spacetime operator.  The obstruction is physical/mathematical
missing structure, not a fence.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight, seals, and register sweep

### 0.1 Preflight

The locked process was read in full.  The artifact under review and its
sidecar were verified before the artifact was opened.

| Object | SHA-256 / result |
|---|---|
| `supervision/LOCKED_PROCESS.md` | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` |
| `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `412767161453769ff9c435074695f8c58755ff152ac3d8d8c3c41090142312f8` |
| artifact under review | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50` — exact match |
| submitted sidecar | `STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md: OK` |

The register head is Q-407.

```text
DOES_THE_OBJECT_EXIST = yes_for_the_partial_build |
  no_for_the_completed_local_Maxwell_kernel_realization
IS_THE_VERSION_CURRENT = yes | through Q-407
ARE_ITS_INPUTS_PRESENT = yes_for_finite_cylindrical_distribution_kernels |
  no_for_completion_continuity_and_geometric_long_wavelength_locality
PREFLIGHT = PASS_WITH_MAXIMAL_CONSTRUCTION_AND_TYPED_STOP
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| Lane-1 R4 partial build | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50` | B1 object under review |
| Q-406 residue ledger | `c35ef84636ec1501bd99858538988711d0c717ac98671e6162abb0a0e5978c74` | R4 route and dependency order |
| metric V005 / DoR-019 object | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | faithful K metric, Riesz map, completion and units |
| DoR-016/V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | CTP reality and ordered batching |
| square V004 / DoR-017 object | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | generated Hessian and restriction/covariance cube |
| field signature V005 / DoR-015 object | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical backgrounds, causal paths, connection currents and A4 rigging |
| Task-4d stitching half | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4` | exact refinement/density/local-Maxwell residue |
| finite temporal-plaquette result | `082a56a6cac2be75322209626a6086901cad7b5e9900cf1d1d021e99b46b7b0c` | finite nonlocality and required many-cell test |

### 0.3 Register and corpus sweep

Searches covered `conserved current`, `path current`, `current kernel`,
`distribution`, `Schwartz kernel`, `pseudodifferential`, `principal symbol`,
`long wavelength`, `spectral`, `Ward`, `contact`, `Maxwell`, `refinement`,
`cellulation`, and `A4 norm`.

| Entry/source | Bearing result |
|---|---|
| Q-247 | separation is class-relative; no topology may be inferred by name |
| Q-258/Q-261 | `T_cyl` exists, but geometric refinement, `T_ref/T_phys`, and O-D3 remain open |
| Q-279/Q-309 | exact finite active retarded and mixing zeros |
| Q-288 | every completion/topology/class formation is named |
| Q-315 | source/current and physical tangent carriers may not be silently identified |
| Q-365 | `rho_H,N` covariance on generated Hessians |
| Q-384 | K metric/Riesz/completion exists, not a spacetime symbol |
| Q-396 | nonzero completed rank-one profile family with zero active finite shadows |
| Q-397/Q-398 | completed reader path and `(chi_K,T)`-relevant pushforward |
| Q-400 | carrier symbol built; local Maxwell map absent |
| Q-403/Q-404 | reader boundedness is memberwise; no magnitude bound |
| Q-406 | R4 route precedes physical use of R2/R3 |
| Q-407 | profile topology and algebraic reader fiber built; current-to-local-field kernel calculus named |

The DoR-015 ratified A1–A4 package was read at source.  It supplies a full
family of globally hyperbolic oriented Lorentzian U(1) backgrounds, coherent
causal path realizations, smooth connection variations, compactly supported
one-currents `u_j(A)=integral_(gamma_j) A`, and the A4 Hilbert rigging.  It
therefore supplies more than an abstract edge label: it supplies the finite
distributional-current realization used below.

The finite temporal-plaquette result was also checked.  It proves that the
available finite response is nonlocal or temporally inhomogeneous and names
the still-missing connected many-cell, long-wavelength, packing-independent,
spatial-magnetic/Hodge test.  No later artifact executes that test.

### 0.4 Symbol distinctions

```text
K_scalar       fixed-point scalar, not used in this construction
K_G            completed physical cycle Hilbert carrier
u_c            conserved path current, a functional on connection variations
J_c            distributional realization of u_c
R_K            carrier Riesz map
H              carrier/profile bilinear K_G -> K_G^*
Kern_N(H)      finite bilocal distribution kernel on M x M
sigma_cyl      cylindrical distribution-kernel representation
sigma_loc      unbuilt local cotangent/long-wavelength Maxwell symbol
L_T            normalized local Maxwell kernel, not R_K
chi_K          p_loc[Rhat_K]
T              bounded self-adjoint reader tensor on the trace-class profile ideal
```

---

## 1. B1 — cross-verification of the partial build

### 1.1 Completed current seam

At finite stage `G`, metric V005 gives

```text
K_G^fin=ker(B_G^T),
I_K,G(c)=u_c,
g_K,G(c,d)=g_A4,G(u_c,u_d),
ker(I_K,G)=0.                                      (B1-1)
```

Consequently `I_K,G` is an isometry in the forced norm.  W3 supplies
rank-preserving isometric inclusions and adjoint restrictions, so completion
extends it to an isometry

```text
I_K,G:K_G -> J_cyc,G:=closure_A4(I_K,G K_G^fin). (B1-2)
```

It is onto `J_cyc,G` by definition.  Conjugation through this isometry sends
Schatten-class operators on `K_G` to the corresponding Schatten-class
operators/bilinears on `J_cyc,G` and preserves their norms.  This confirms the
submitted seam.  It is still an A4 current/source seam; locality in a
distribution or cotangent topology is a separate question.

### 1.2 Profile Banach topology

The submitted presentation

```text
A_prof,G=R R_K,G direct-sum_1 S_1,sa(K_G),
E_G=R L_T,G direct-sum_1 A_prof,G,
O_prof,G=E_G/closure(ker Q_G)                    (B1-3)
```

is Banach.  `P_x=x^flat tensor x^flat` is rank one and

```text
||P_x||_1=||x||_K^2.                             (B1-4)
```

Thus every `H_x=f(s)Rhat_K+2f_1(s)P_x` belongs to the presentation.

One wording precision is required.  `l1` is not the uniquely forced norm:
on a finite direct sum of the two scalar lines and `S_1`, the `l1`, maximum,
and standard product norms are equivalent and induce the same continuous
dual.  `l1` is a canonical convenient representative compatible with the
trace-class summand.  No physical coordinate or continuous functional changes
under this precision.

There is a second scope precision.  If `ker Q_G` is not already known closed
in `(B1-3)`, quotienting by its closure builds an abstract Banach completion;
the original algebraic map to `O_R5` descends continuously only after the
physical kernel topology supplies that continuity.  The submitted reader
classification on the quotient remains correct; the quotient itself must not
be called a proved embedded local subspace of `O_R5`.

```text
PROFILE_BANACH_PRESENTATION = CONFIRMED
L1_NORM_UNIQUELY_FORCED = false | TYPE-R
EQUIVALENT_PRODUCT_TOPOLOGY_CLASS = CONFIRMED
O_PROF_PHYSICAL_LOCAL_EMBEDDING = NOT_BUILT / TYPE-U
```

### 1.3 Continuous dual and profile formula

The real dual is

```text
E_G^*=R direct-sum_infinity R direct-sum_infinity B_sa(K_G),

ell_(lambda,chi,T)(a,b,C)
 =lambda a+chi b+Tr(TC).                         (B1-5)
```

Normalization fixes `lambda=1`; descent to `O_prof` is exactly annihilation
of `closure(ker Q_G)`.  Therefore the complete algebraic natural reader family
is parameterized by `(chi_K,T)` subject to those relation, covariance,
reality, restriction, batching, and stabilizer equations.

For `P_x=x^flat tensor x^flat`, trace duality gives

```text
Tr(TP_x)=<x,T x>_K.                              (B1-6)
```

Hence

```text
p_(chi,T)[H_x]
 =f(||x||_K^2)chi_K
  +2f_1(||x||_K^2)<x,T x>_K.                    (B1-7)
```

The formula is exact and does not choose a reader.

### 1.4 Reciprocal-loop coordinate count

For normalized `e` in the one-dimensional carrier, `x=r e` and

```text
P_x=r^2 R_K.                                     (B1-8)
```

The exact relation in the quotient identifies the trace-class coordinate
with the Riesz-line coordinate, so

```text
p[H_(r e)]
 =[f(r^2)+2r^2 f_1(r^2)]chi_K.                  (B1-9)

dim(reader restriction to reciprocal profile span)=1. (B1-10)
```

### 1.5 S8-A coordinate count

Choose an orthonormal pair only to calculate the invariant tensor.  Under the
sealed exchange `e_1<->e_2`, a real self-adjoint invariant tensor has equal
diagonal entries.  The relation

```text
R_K=P_(e_1)+P_(e_2)                              (B1-11)
```

forces its trace to be `chi_K`, so

```text
T=[[chi_K/2,tau_cross],
   [tau_cross,chi_K/2]].                          (B1-12)
```

Thus the exchange-invariant profile fiber is two-dimensional before any
additional admitted stabilizer relation:

```text
(chi_K,tau_cross),

p[H_x]
 =[f(s)+s f_1(s)]chi_K
  +4f_1(s)tau_cross x_1 x_2.                    (B1-13)
```

For `x=r e_j`, the cross term vanishes and the Q-398 axis result follows.
The basis is discarded after the calculation; `(B1-12)` is an invariant
tensor statement.  No edge, cycle, orientation, frame, reader, or realization
member is selected.

```text
PARTIAL_BUILD = CONFIRMED
RECIPROCAL_PROFILE_COORDINATES = 1
S8A_EXCHANGE_PROFILE_COORDINATES = 2 before extra stabilizers
READER_MEMBER_SELECTED = none
```

---

## 2. B2 — finite/cylindrical kernel realization constructed

### 2.1 Conserved currents as distributions

Fix one member `B=(M,g,o,t;P_M,iota)` of the ratified no-selection A1/A2
family.  Let

```text
T_B:=Omega_c^1(M;iR)                             (B2-1)
```

be the smooth compactly supported connection-variation test space.  For
`c in K_G^fin`, define the distributional one-current

```text
<J_c,a>
 :=sum_e c_e integral_(gamma_e) a
 =u_c(a),
a in T_B.                                        (B2-2)
```

This is not newly authored: A2 gives the causal paths and A3/A4 give exactly
the current pairing `(B2-2)`.

For a compactly supported scalar test function `varphi`,

```text
<partial J_c,varphi>
 :=<J_c,d varphi>
 =sum_e c_e[varphi(t(e))-varphi(s(e))]
 =<B_G^T c,varphi>
 =0.                                             (B2-3)
```

Thus every cycle current is distributionally conserved and annihilates exact
connection variations.  `(B2-3)` is the finite Ward identity derived directly
from incidence; it uses no gauge representative.

Define the basis-free analysis map

```text
A_G:T_B -> (K_G^fin)^*,
(A_G a)(c):=<J_c,a>.                             (B2-4)
```

Fullness/injectivity of the current family makes its finite transpose
injective, hence `(B2-4)` is onto the finite-dimensional dual.  Equivalently,
A2-R7/A4 supplies enough localized connection variations to realize every
cycle covector.

### 2.2 Profile operator to bilocal distribution kernel

For `H in Hom(K_G^fin,(K_G^fin)^*)`, define

```text
Kern_G^fin(H)[a,b]
 :=H(R_K,G^(-1) A_G a,
     R_K,G^(-1) A_G b),
a,b in T_B.                                      (B2-5)
```

Because `K_G^fin` is finite dimensional and every `J_c` is a continuous
distribution, `(B2-5)` is a continuous bilinear functional on the nuclear
test space.  The Schwartz-kernel correspondence therefore gives a unique
bilocal distribution on `M x M`.  In any temporary cycle basis its form is

```text
Kern_G^fin(H)
 =sum_(a,b) H_tilde_(ab) J_(c_a) tensor J_(c_b), (B2-6)
```

where the coefficient matrix includes the two Riesz inverses in `(B2-5)`.
Equation `(B2-5)`, not `(B2-6)`, is the basis-free definition.

Surjectivity of `A_G` makes the realization faithful:

```text
Kern_G^fin(H)=0 iff H=0.                          (B2-7)
```

Its support is

```text
supp Kern_G^fin(H)
 subset (union_e gamma_e) x (union_f gamma_f).   (B2-8)
```

It is therefore a path-bilocal kernel, not generally a diagonal local kernel.

### 2.3 Exact Ward, covariance, reality, and units

From `(B2-3)` and `(B2-5)`,

```text
Kern_G^fin(H)[d varphi,b]=0,
Kern_G^fin(H)[a,d psi]=0.                        (B2-9)
```

Thus every realized cycle-profile kernel is transverse in both arguments.
This Ward property holds for the whole `(chi_K,T)` family and cannot select a
reader coordinate.

For an admitted background/realization morphism and its current isometry
`U`, pushforward of path currents and conjugation of `H` give

```text
Kern_(G')^fin(U H U^*)
 =(iota x iota)_* Kern_G^fin(H).               (B2-10)
```

Orientation/reality uses the ratified antiunitary/conjugation law and
reverses current signs twice in a bilinear kernel.  The appropriate real/CTP
slice is preserved.  `j_R4` remains the sole action-unit dressing before
`Kern`; no C/K identification or conversion unit is introduced.

### 2.4 Restrictions, zero-extension, quotient, and batching

For a rank-preserving W3 isometry `j_NM:K_N->K_M`, coherent A2 paths give

```text
A_N=j_NM^* A_M,
R_K,N=j_NM^* R_K,M j_NM.                         (B2-11)
```

Consequently compression of a generated operator commutes with restriction
of `(B2-5)` to the old current cylinder:

```text
Kern_N(j_NM^* H_M j_NM)
 =Res_(M->N) Kern_M(H_M).                        (B2-12)
```

Identity zero-extension is the same square.  When a stage creates a new cycle,
the new stage has its own current and kernel class; no canonical upward map
onto the new cycle direction is claimed.  Gate-4 quotienting precedes
`J_c`, so pendant/tree coboundaries are absent.  DoR-016 batching remains an
ordered product with componentwise kernels; no joint scalar contraction is
formed.

### 2.5 Cylindrical symbol calculus

Equations `(B2-2)`–`(B2-12)` define the maximal choice-free calculus:

```text
sigma_cyl,G:
  Hom(K_G^fin,(K_G^fin)^*)
    -> D'(M x M;T^*M boxtimes T^*M),
  H |-> Kern_G^fin(H),                            (B2-13)
```

with:

```text
kernel(sigma_cyl,G)=0;
Ward kernel in either field argument=exact one-forms;
support=path-bilocal support (B2-8);
reality/covariance/restriction/batching=equations above;
R4 units=carried by j_R4 before realization.      (B2-14)
```

This is a genuine kernel realization and symbol calculus on every finite
cylinder.  It is not yet the commissioned **completed local** Maxwell map.

```text
FINITE_CONSERVED_CURRENT_TO_BILOCAL_KERNEL_REALIZATION = BUILT / TYPE-P
FINITE_CYLINDRICAL_WARD_CALCULUS = BUILT / TYPE-P
FINITE_KERNEL_REALIZATION_FAITHFUL = true
```

---

## 3. B2 — exact obstruction at completion and locality

### 3.1 Completion-continuity obstruction

The A4 completion uses the forced current/frame norm.  A completed current is
an equivalence class of A4-Cauchy finite currents.  To extend `(B2-2)` and
`(B2-13)` to a distribution topology, one needs a named locally convex kernel
space `D'_tau` and estimates of the form

```text
p_q(J_c) <= C_q ||c||_K,

p_Q(Kern_G^fin(H)) <= C_Q ||H||_prof             (B2-15)
```

for every defining distribution/kernel seminorm, uniformly along the directed
finite core and over the admitted realization family.

No ratified clause supplies `(B2-15)`.  The A4 orthonormal/frame norm controls
the record coefficient norm; it does not control path length, wavefront set,
derivatives, local concentration, or any distribution seminorm.  The A1/A2
family deliberately retains all coherent realizations rather than a bounded-
geometry member.  Therefore two A4-Cauchy presentations are not proved to have
the same distributional limit, and the finite map cannot be continuously
extended by completion.

```text
COMPLETED_A4_CURRENT_TO_DISTRIBUTION_EMBEDDING = NOT_BUILT / TYPE-U
MISSING_CERTIFICATE = UNIFORM_A4_TO_LOCAL_KERNEL_CONTINUITY_AND_CLOSABILITY
                                                        (B2-16)
```

This is an absence of a required estimate, not a theorem that no such estimate
can ever hold after more structure is supplied.

### 3.2 Diagonal-locality obstruction

A local Maxwell action kernel is supported on the diagonal modulo a named
contact/boundary ideal and has an order-two local cotangent symbol.  By
contrast, `(B2-8)` is generally supported on `gamma_e x gamma_f`, including
off-diagonal pairs.  No algebraic Ward identity turns that support into the
diagonal.

The finite temporal-plaquette result independently confirms this boundary:
the finite response has cross-profile terms and unequal frequency response,
so it is nonlocal or temporally inhomogeneous rather than a local constant
Maxwell form.  Selecting a diagonal entry, averaging profiles, or quotienting
all off-diagonal support would be authored/tuned and would delete
record-visible completed profiles.

To obtain diagonal locality one needs a refinement/density theorem in a named
kernel topology:

```text
mesh/refinement parameter h -> 0;
cell volumes/coframes and current-density normalization;
connected many-cell kernel rather than zero-extension;
common-refinement and packing/cellulation independence;
boundary/contact subextensivity;
convergence to a distribution conormal to the diagonal.      (B2-17)
```

The thin `N<=M` zero-extension system appends identity cells; it does not
shrink cells or define `h`.  Q-258/Q-261's R12 package names exactly the
missing fields in `(B2-17)`.

```text
COMPLETED_PATH_BILOCAL_TO_DIAGONAL_LOCAL_KERNEL_LIMIT = NOT_BUILT / TYPE-U
MISSING_CERTIFICATE = R12_REFINEMENT_DENSITY_AND_DIAGONAL_LOCALITY
                                                        (B2-18)
```

### 3.3 Long-wavelength symbol obstruction

Even after a local kernel exists, the physical coefficient requires one of:

```text
a covariant order-two principal-symbol calculus on a proved local operator
class; or
a named low-spectrum/long-wavelength family with domains and convergence.
                                                        (B2-19)
```

On the retained globally hyperbolic Lorentzian family, no canonical positive
elliptic spectral generator, Cauchy slice, self-adjoint domain, or boundary
condition is selected.  A microlocal route can avoid selecting a spectrum,
but it still requires the conormal-to-diagonal operator class absent in
`(B2-18)`.  The full Maxwell split additionally needs:

```text
local order-two Ward tensor decomposition;
spatial magnetic/Hodge partner to the temporal response;
contact/boundary ideal and direct-sum proof;
normalization p_loc(L_T)=1 in that same topology;
kernel intersections with every Q-396 profile;
covariance, reality, W3, batching and DoR-008 squares.         (B2-20)
```

No authority supplies `(B2-19)`–`(B2-20)`.

```text
PHYSICAL_LONG_WAVELENGTH_MAXWELL_SYMBOL = NOT_BUILT / TYPE-U
PHYSICAL_WARD_CONTACT_HODGE_SPLIT = NOT_BUILT / TYPE-U
```

### 3.4 Exact obstruction and status

The commissioned completed object factors as

```text
K_G completion
  --C_dist--> completed conserved distribution currents
  --Kern--> completed bilocal kernel class
  --RefLoc--> diagonal local operator class
  --sigma_2--> order-two transverse cotangent symbol
  --MaxProj--> local Maxwell coefficient plus proved remainder. (B2-21)
```

The finite-core versions of `C_dist` and `Kern` are built in Section 2.
Every completed arrow in `(B2-21)` is blocked by `(B2-16)`, `(B2-18)`, or
`(B2-20)`.  No single coefficient can repair a missing domain/codomain.

```text
COMPLETED_CONSERVED_CURRENT_TO_LOCAL_FIELD_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS
 = OBSTRUCTED / TYPE-U |
   exact: completion continuity + R12 refinement density/diagonal locality
          + local Ward/contact/Hodge symbol calculus

KERNEL_REALIZATION = OBSTRUCTED
MACHINERY_APPEAL = false
```

---

## 4. Authored/derived continuation table

No row is selected or recommended.

| Route | Added content | Standing | Void condition |
|---|---|---|---|
| derive through R12 | geometric refinement category, coframes/volumes, current-density maps, common-refinement response square, one frozen topology, boundary subextensivity | **derivation route**, unbuilt | filtration member, topology chosen from desired coefficient, failed packing/cellulation independence |
| authored external local-kernel family | a family of continuous embeddings `C_dist`, local operator ideals, and microlocal symbol maps on every A1/A2 member | **authored-through-gate candidate** | selected background/frame/Cauchy slice, empty family, failed covariance/restriction, deleted cycle content |
| spectral Cauchy route | Cauchy-data Hilbert space, positive generator, domain/boundary conditions, low-spectrum scaling | **authored unless derived later** | unannounced foliation/domain, topology switch, no equivalence to covariant microlocal symbol |
| finite trace/Calkin shortcut | normalized trace or compact quotient as “local” coefficient | **REFUTED control** | stage dependence; finite identity compact; no spacetime support/symbol |
| finite temporal average | average diagonals or discard cross-profile response | **REFUTED control** | finite result is nonlocal and provides no canonical average |

The first route consumes ledger R12 before completing R4.  The second and
third add genuine field-local structure and require their own gate.  This
artifact takes none of them.

---

## 5. B3 — reader fiber after the realized kernel

### 5.1 Pulling the algebraic reader through the finite kernel map

Because `sigma_cyl,G` is injective, define on its finite image

```text
p_(chi,T)^cyl(sigma_cyl,G(H)):=p_(chi,T)(H).     (B3-1)
```

This is well-defined and gives exactly

```text
p_(chi,T)^cyl(sigma_cyl(H_x))
 =f(||x||_K^2)chi_K
  +2f_1(||x||_K^2)<x,T x>_K.                    (B3-2)
```

Thus the realized finite kernel reproduces the submitted rank-one formula.
On the reciprocal loop and S8-A, equations `(B1-9)` and `(B1-13)` remain
unchanged.

### 5.2 Does current conservation constrain `(chi_K,T)`?

No.  Ward transversality `(B2-9)` holds for **every** profile operator before
the reader is applied.  It therefore defines the image class, not a hyperplane
in its dual.  Faithfulness `(B2-7)` introduces no new relation among `R_K` and
`P_x`.  Reality, restrictions, batching, units, and stabilizers are already
the equations defining `R2_alg`.

```text
FIBER_CONSTRAINT_FROM_FINITE_KERNEL_REALIZATION = none_new
```

### 5.3 Why locality cannot be used as a zero constraint

It would be invalid to argue that finite path kernels are not diagonal and
therefore set

```text
chi_K=0, T=0.                                    (B3-3)
```

The physical coefficient is defined after the completed refinement/long-wave
map, not by literal finite-stage diagonal support.  Q-396 profiles are
finite-invisible at the active reference yet nonzero in completion; DoR-008
does not reverse that implication.  Conversely, declaring them detected is
equally unsupported.  Only a completed map `(B2-21)` can derive a relation.

No normalization or magnitude bound follows:

```text
p_loc(L_T)=1 does not identify L_T with Rhat_K;
the cylindrical Ward identity is homogeneous;
the memberwise Banach norm is reader-dependent;
the finite kernel map is faithful but supplies no local coefficient norm.
                                                        (B3-4)
```

### 5.4 Exact fiber standing

```text
FIBER_CONSTRAINT = {
  Rel_G annihilation;
  automorphism/stabilizer covariance;
  reality;
  W3 restriction and zero-extension covariance;
  ordered batching;
  R4 units
} | no new physical relation from B2

CHI_K_FORCED = false
CHI_K_ZERO_FORCED = false
CHI_K_ONE_FORCED = false
CHI_K_BOUND_DERIVED = false
T_RELATION_BEYOND_EXISTING_STABILIZERS = none
R2_PHYSICAL_MEMBERSHIP_TEST = BLOCKED_ON_(B2-21)
MEMBER_SELECTED = none
```

---

## 6. B4 — falsifiers, regressions, and anti-tuning

### 6.1 Falsifier table

| Check | Execution | Verdict |
|---|---|---|
| finite active restrictions | Q-396 flat profiles restrict to Q-243/Q-279/Q-309 zero; no finite `p_loc,N` invented | **PASS** |
| finite off-section content | `sigma_cyl` retains it as path-bilocal distributions rather than deleting it | **PASS** |
| one edge / connected tree | `K_G=0`, so no cycle current/profile/kernel | **PASS** |
| reciprocal loop | `P_(re)=r^2R_K`; one reader coordinate | **PASS** |
| S8-A exchange | `(chi_K,tau_cross)` retained; axes and general points distinguished correctly | **PASS** |
| pendant/tree quotient | Gate-4 quotient precedes current realization | **PASS** |
| current Ward test | exact calculation `(B2-3)` and two-leg form `(B2-9)` | **PASS** |
| kernel faithfulness | finite analysis map onto; `(B2-7)` | **PASS** |
| W3 restriction | adjoint compression square `(B2-11)`–`(B2-12)` | **PASS_WITH_RANK-PRESERVING_SCOPE** |
| cycle creation | no upward map; new stage receives its own class | **PASS_BY_SCOPE** |
| reality/covariance | path-current pushforward and operator conjugation commute | **PASS** |
| batching | ordered product; no scalar contraction | **PASS** |
| R4 units | only `j_R4`; no implicit cross-sector conversion | **PASS** |
| completion tail | no distributional limit asserted without `(B2-15)` | **PASS_WITH_OBSTRUCTION** |
| local-support attack | off-diagonal path support explicitly exposed | **PASS_WITH_OBSTRUCTION** |
| finite temporal response | nonlocal/cross-profile result not averaged or renamed | **PASS** |

### 6.2 Fresh hostile attacks

1. **Equivalent-norm attack.**  Replacing `l1` by the max norm changes
   constants but not the finite-product topology or dual.  Therefore the
   submitted claim of unique `l1` force is too strong, but no reader coordinate
   or physical conclusion changes.

2. **Completion-without-distributions attack.**  An A4-orthonormal sequence
   may remain normalized while its path supports concentrate or vary in every
   local distribution seminorm.  With no estimate `(B2-15)`, the completed
   current cannot be declared a distribution.  This is the load-bearing
   obstruction.

3. **Ward-selects-Maxwell attack.**  All conserved-current bilocal kernels are
   transverse.  Transversality alone does not distinguish local Maxwell order
   two from transverse nonlocal/higher response, so it cannot constrain
   `(chi_K,T)`.

4. **Long-wavelength-by-zero-extension attack.**  Appending identity cells
   changes no mesh size and supplies no density.  Treating it as `h->0` would
   reintroduce the dead moving-tail/refinement error.

5. **Contact-quotient attack.**  Quotienting all off-diagonal support would
   delete record-visible cycle profiles before a boundary/contact theorem and
   violates the refusal discipline.

6. **Background-member attack.**  Choosing one `(M,g,iota)` to obtain Fourier
   coordinates, a Cauchy spectrum, or a preferred packing violates the A1/A2
   no-selection family.  A lawful result must be covariant family-wide.

### 6.3 Anti-tuning order

```text
1  Recompute the current seam and full reader dual before inspecting locality.
2  Construct sigma_cyl on the entire finite operator class, not one profile.
3  Prove Ward and faithfulness before applying any reader.
4  Test support and completion continuity before defining a Maxwell projector.
5  Find the R12/refinement obstruction before revisiting (chi_K,T).
6  Only then pull the unchanged reader family through the finite kernel map.
7  Select no localization route and derive no coefficient relation from a
   desired modulus or fixed-point consequence.
```

```text
p_symbolic = true
nu_symbolic = true
chi_K_symbolic = true
T_symbolic = true
rank_pair_selected = none
background_member_selected = none
localization_member_selected = none
numeric_response_evaluated = false
protected_value_evaluated = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 7. Final board

```text
PARTIAL_BUILD = CONFIRMED

COMPLETED_CURRENT_A4_SEAM = CONFIRMED
PROFILE_BANACH_PRESENTATION = CONFIRMED_WITH_EQUIVALENT_NORM_PRECISION
ALGEBRAIC_READER_FAMILY = CONFIRMED
P_CHI_T_ON_HX = CONFIRMED
RECIPROCAL_PROFILE_DIMENSION = 1
S8A_EXCHANGE_PROFILE_DIMENSION = 2 before extra stabilizers

FINITE_CONSERVED_CURRENT_DISTRIBUTION_MAP = BUILT
FINITE_BILOCAL_KERNEL_REALIZATION = BUILT
FINITE_CYLINDRICAL_WARD_SYMBOL_CALCULUS = BUILT
FINITE_KERNEL_MAP_FAITHFUL = true

COMPLETED_A4_TO_DISTRIBUTION_CONTINUITY = NOT_BUILT / TYPE-U
REFINEMENT_DENSITY_DIAGONAL_LOCALITY = NOT_BUILT / TYPE-U
PHYSICAL_LONG_WAVELENGTH_MAXWELL_SYMBOL = NOT_BUILT / TYPE-U
PHYSICAL_WARD_CONTACT_HODGE_SPLIT = NOT_BUILT / TYPE-U

KERNEL_REALIZATION = OBSTRUCTED |
  exact: completion continuity + R12 geometric refinement/locality
         + completed Maxwell symbol calculus

FIBER_CONSTRAINT = no_new_relation_on_(chi_K,T)
CHI_K_BOUND_DERIVED = false
MEMBER_SELECTED = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

