# STAGE 8 — G3 PANEL, OVER-DERIVATION LENS: BLIND ADVERSARIAL VERIFICATION OF STAGE8_G3_REALIZATION_BUILD_V001
## BLIND ADVERSARIAL VERIFIER (G3-PANEL-OVER) — CROSS-LINEAGE, DEFAULT-REFUTE — [SEALED]

Date: 2026-08-13
Role: BLIND, cross-lineage, DEFAULT = REFUTE. Lens: OVER-DERIVATION — attack every
POSITIVE/fixed claim of the build (the realized write R1-R7, the block-split verdict,
the regularity typing) for (a) forbidden imports / unsealed idealizations, (b)
circularity, (c) unreal or non-load-bearing spans, (d) injection from tasking text.
The other panel lens artifact (any STAGE8_G3_PANEL_*) was NOT opened. No
register/tracker/plan/road/ledger/lens file read. No git action; no register, commit,
or push. Everything symbolic; no value of n, kappa, alpha, mu, C, any coefficient,
norm, or exponent computed, bounded, or evaluated.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

---

## 0. VERDICT IN ONE LINE

**BUILD_SOUND. Every FIXED-BY-CONSTRUCTION claim (R1-R7) is genuinely assembled from
sealed spans that were re-read at the bytes; no assembly step consumes the continuum
diamond's metric/contractibility, a scale, faithfulness, or imported GR as authority;
the equal-time idealization that killed a prior build is explicitly NOT taken (the
build quotes the very span that bars it); no step silently supplies the datum it
claims to derive — the split verdict correctly returns NOT_DETERMINED instead of
manufacturing G3; no verdict cites the tasking instead of a sealed span. Two minor,
YOURS-marked, non-overturning imprecisions are recorded (§5).**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256; full digests recomputed BEFORE reading)

TARGET (verified before reading; MATCHES the tasked digest and its own sidecar):

```text
STAGE8_G3_REALIZATION_BUILD_V001.md
  3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  MATCHES-TASKED  OK
```

THE SEVEN SOURCES (each recomputed at path; each MATCHES the build's SEALS_VERIFIED
block, the tasked digests, and its own adjacent `.seal.sha256` sidecar — all seven
sidecars present, all seven matched):

```text
FORM  STAGE8_R_RECORD_L_FORM_FABLE_V001.md
      5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37   OK
GB    STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md
      1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb   OK
GC    STAGE8_REQUIRE_G3_CHECK_V001.md
      8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3   OK
M03   STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md
      2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f   OK
E1    STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md
      468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5   OK (seal begins 46846730, as tasked)
PA    STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md
      789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3   OK
FR    STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md
      4d072e7671113357764b61e11ef1d3d336ce34e307ba07c545ec7cf1a38a6abc   OK
```

No seal failed. The E1 file (materially omitted by a prior build) was READ here in
the cited regions at its verified seal. M03, PA, FORM, GB, GC, FR read in full.
Output name probed before write: ABSENT.

---

## 2. QUOTE-CHECK OF THE THREE MOST LOAD-BEARING SPANS (lens requirement (c))

Each re-read from bytes this session and compared to the build's use of it.

```text
Q-1  M03 :325-354 (underpins R4, the build's boldest claim). VERBATIM MATCH:
     "For one oriented cell, let a gauge change act on the holonomy character by
     z^g=t z s^dagger. The ratified post endpoint representations are G_out(t)=D(t),
     G_in(s)=S D(s) S. Exact multiplication gives
     W_post(z^g)=G_out(t)W_post(z)G_in(s)^dagger." (:325-339) — and
     "conjugate(t z_- s^dagger)(t z_+ s^dagger) = conjugate(z_-)z_+" (:347-349) —
     and "independent transformations of the two open histories carry relative
     endpoint characters and are not silently identified with the common
     transformation" (:352-354). REAL and LOAD-BEARING: the two named endpoint
     representations are the two ports; the abelian common-gauge cancellation is the
     sealed mechanism the build reports; "the two open histories ... endpoint
     characters" grounds the build's OPEN reading.

Q-2  E1 :1111-1116 (underpins R7 and REGULARITY). VERBATIM MATCH:
     "a-VERTEX SECTOR: ... J = -(Q b_D Q) (x) alpha_x with b_D Gevrey, all
     derivatives vanishing at boundary and tips, so its spacetime transform decays
     faster than any power. RECORD VERTEX: M(t) is a SHARP indicator and by C4 the
     pointer weights put it in the LEAD at full tau_R
     (sum_lambda w_lambda u_lambda = P). The locus is the VOLUME DIAGONAL (C6
     refinement)." REAL and LOAD-BEARING: this is E1's own smooth-vs-sharp typing of
     the two vertex sectors, exactly the content of REGULARITY = MIXED.

Q-3  FR :237-242 (underpins S2 and BLOCK_SPLIT = NOT_DETERMINED). VERBATIM MATCH:
     "CURRENT DENSITY — UNSUPPLIED ... No transport law exists. It is the most
     downstream of the seven fields and nothing upstream of it is complete."
     REAL and LOAD-BEARING: the sealed absence of the diamond-to-complex transport
     is exactly what stops the split from being determined.
```

Secondary spans verified at the bytes (all match; none misquoted): M03 :15-22,
:24-41, :121-150, :177-181, :190-198, :205-213, :234-248, :286-293, :342-351,
:364-370, :376-378, :385-399, :516; PA :50-70, :83-88, :99-113, :115-127, :132-135,
:137-141, :143-145; E1 :82, :339-351, :353-366, :398-399, :510-513, :1056-1058;
FORM :164-165, :180-199, :277-281, :293-301, :334-343; GB :97-145, :147-160,
:217-238, :306-340; GC :86-91, :122-148, :252-262, :279-285; FR :27-31, :37-38,
:174-199, :228-242.

---

## 3. ATTACK (a) — FORBIDDEN IMPORT / UNSEALED IDEALIZATION IN ANY ASSEMBLY STEP

### 3.1 The equal-time trap (the exact failure of a prior build) → NOT TAKEN

E1 :510-515 seals: "Y_2 Y_1 = 0 holds only in the strict equal-time
multiplication-operator idealization with disjoint supports; the actual V_i - 1 are
Dyson-dressed by free h_0 propagation over the whole interval. ANY EXECUTION ARTIFACT
THAT ... CITES Y_2 Y_1 = 0 ... BLOCKS." Pressed at the bytes: the build QUOTES the
Dyson-dressing sentence (build §2 Q3, §3 R7, §5), types the vertex TWO-TIME
throughout, and nowhere uses an equal-time simplification, Y_2 Y_1 = 0, or any
disjoint-support product identity. E1's C6 (:353-358) — only two-time /
scattering-type objects can work — is carried, not contradicted. NO IDEALIZATION
CONSUMED.

### 3.2 The diamond's metric/contractibility → BARRED SHORTCUT NOT TAKEN

The one decisive use — smooth density on a contractible diamond => transported chain
bounds => phi_H = 0 (single block) — is exactly the refutation line GC closed as a
forbidden SCALE/GR import (GC :122-148, quoting B1B: Omega_c is "A LORENTZ-COVARIANT
CONTINUUM DIAMOND, NOT A CW OBJECT AT ALL," uniqueness open). Pressed: the build
names this shortcut, marks it BARRED, and does NOT take it (build §4); its split
verdict is NOT_DETERMINED, which is precisely what refusing the import yields.
Independently the build states the correct logical point that regularity of a density
and the homology class of its transported chain are independent data. NO IMPORT.

### 3.3 Scale-bearing PA/E1 content → SUSPECT-ONLY, never certifying

R7's carrier (Hermite/oscillator Galerkin with oscillator length ell, the causal
diamond; PA :50-70, :99-113) is SCALE-bearing, and PA itself seals the fixed-gauge
disclaimer verbatim at :132-134: "This is explicitly a fixed-gauge finite response
diagnostic. Phase A claims no local gauge covariance." Pressed: the build (i) quotes
PA's own disclaimer, (ii) classifies R7 SUSPECT with all three flags (fixed-gauge,
scale-bearing carrier, one probed direction — PA :137-141 confirms the single frozen
profile coefficient), (iii) never promotes any R7 property to a record-native
certificate — the complex-level regularity is declared NOT DETERMINED (S2), and R7 is
excluded from deciding the split (§4). No numeric content of PA/E1 is consumed (the
frozen rationals, thresholds, and quadratures are quoted-context only, and the build
quotes none of them as inputs). SUSPECT USE ONLY — compliant.

### 3.4 Faithfulness / GR → ABSENT

No injectivity premise appears in any of R1-R7, the split verdict, or the regularity
typing; the build's derivations are n-blind and faithfulness-free (consistent with
the carried GB/GC audit, GC §4(c)). No metric g, KK ansatz, Einstein-Hilbert, or
quasilocal object is consumed anywhere. `alpha_x` is verified at PA :79/:122 and
E1 :345 as the Dirac velocity matrix — the build's symbol-collision flag is correct.

---

## 4. ATTACK (b) — CIRCULARITY, AND THE PER-CLAIM AUDIT

Does any step silently supply the datum it claims to derive? Pressed claim by claim
against the sealed bytes:

```text
R1  CELL-FACTORIZED READ — SOUND. W_N = tensor_j W_(1,j)[a_j] with D_(n,j)
    reading a_j only (M03 :128-139, :146-149); C7 zero-extension appends
    conjugate(z[0])z[0] = 1 factors that drop exactly (M03 :385-399). The per-cell
    indexing of histories is the ratified law's own; nothing outside cell j enters
    the cell-j read. Nothing supplied silently.

R2  ONE CHAIN PER CELL, ready->pointer ONLY — SOUND. D = diag(1,z,1) puts the
    character in the pointer slot alone; D S|r> = z|p_Q> (M03 :177-180); the
    explicit one-cell matrix [[0,1,0],[z,0,0],[0,0,-1]] (M03 :234-248) shows the
    pointer->ready entry (1) and the eraser entry (-1) both connection-INDEPENDENT.
    Exact read of sealed matrices; the "exactly one functional per cell" typing is
    carried from FORM (one character power), not re-manufactured.

R3  UNIT-MODULUS PAIRING, FLAT DIAGONAL — SOUND. Unit modulus used in the sealed
    derivation (M03 :190-198); F_N[a,a] = I exactly (M03 :286-293); matches FORM D3
    (:180-199). Exact.

R4  TWO-PORT OPEN-TRANSPORT INCIDENCE — SOUND AS HEDGED. The sealed law displays
    exactly two endpoint representations (Q-1 above). The YOURS step
    d_0^dagger ell_j = e_out - e_in is exact adjoint algebra CONDITIONAL on reading
    t, s as gauge-0-cochain values at the two ports — stated as such, with the
    port-to-0-cell anchoring flagged UNSEALED (S3). The OPEN reading is grounded in
    the sealed words "the two open histories ... endpoint characters" (M03
    :352-354): a single-branch-invariant (closed) chain would make
    W_post(z^g) = W_post(z) and trivialize the sealed covariance law — so the sealed
    content itself forces single-branch gauge-COVARIANCE, not invariance, which is
    the build's mechanism claim. TENSION HANDLED, NOT HIDDEN: the carried GB/GC
    typing "ell_j ⊥ im(d_0)" (GB :150-153; GC :77-85) is literally a zero-boundary
    statement; the build reconciles it as a difference-branch/quotient-level typing
    (build R4 CONSISTENCY paragraph) — which is the only reading under which GB/GC
    and the sealed M03 covariance law are jointly coherent, since the sealed
    invariance is COMMON-gauge only (M03 :342-351) and independent transformations
    are sealed non-invariant (:352-354). R4 refines, and does not contradict, the
    quotient-level inventory; the refinement is span-grounded. NOT circular: the
    port pair pins only the im(d_0) component (exact linear algebra: the gauge-block
    projection is a function of d_0^dagger ell alone), and the build draws no
    physical-block conclusion from it.

R5  TWO-BRANCH SAME-CHAIN CONJUGATED READ — SOUND. Z_N = product
    conjugate(z_-)z_+ (M03 :15-22); single-branch charged amplitude identically
    zero, <R_N|W_N[a]|R_N> = 0 (M03 :205-213, :364-370). Exact.

R6  SECTOR-DIAGONAL COUPLING — SOUND. F_N = P_0 + Z_N P_ch with cross-sector
    entries zero because <R_N|P_N> = 0 (M03 :24-41, :205-213); the quoted sentence
    at :376-378 is verbatim. Exact.

R7  THE ONE CONCRETE VERTEX REALIZATION — SOUND AS CLASSIFIED. Every constitutive
    quote verified (PA :99-113, :115-127, :132-135, :137-141; E1 :342-351, :353-366,
    :510-513, :1111-1116). The SUSPECT classification is PA's own sealed text, not
    the build's invention. Two scope notes, neither overturning: (i) "the one
    concrete realization" is uniqueness OVER THE VERIFIED SOURCE CHAIN (M03 consumes
    h_j abstractly; PA/E1 is the only concrete vertex in the sealed construction
    read) — not a corpus-wide census, and the build's use (classification only)
    does not lean on global uniqueness; (ii) the flag block drops C4's "at full
    tau_R" qualifier on sum_lambda w_lambda u_lambda = P — harmless because E1's own
    frozen definition D2 (:398-399) fixes every cell at full tau_R, and the build's
    §2 Q3 quotes the qualifier in full.

S1-S4  STOPPING POINTS — SOUND, and they are what block circularity. S1: h_j is
    consumed, never displayed as an edge sum (verified against the WHOLE of M03 —
    h_j occurs only inside chi_n(h_j[a_±]) at :16-17, :228-229, :516; FORM :164-165
    concurs verbatim). S2: FR :237-242 verbatim (Q-3). S3: anchoring unsealed —
    nothing in M03 names 0-cells for the ports. S4: PA :137-141 — one frozen profile
    direction. The build STOPS exactly where the seals stop.

BLOCK_SPLIT = NOT_DETERMINED — SOUND. (i) The pinned gauge-block statement is exact
    conditional algebra (R4) and explicitly excluded from the phi_f/phi_H question;
    (ii) block-blindness of R1-R3, R5, R6 checked: none references d_1 or
    discriminates H from im(d_1^dagger); (iii) R7 is quarantined by S2 + S4 + the
    fixed-gauge flag; (iv) the named missing input matches the sealed absences
    (FR :237-242; GB :105-111 exhaustion-vs-subdivision index gap; GB :132-135 no
    Ref_a-indexed receiver, no Ref_a<-Ref_path bridge); (v) the verdict is
    consistent with the panel-confirmed GB flag (:306-340) and GC SOUND (:279-285).
    NO silent supply of G3 anywhere — the build's central negative discipline holds.

REGULARITY = MIXED (vertex) / NOT DETERMINED (complex) — SOUND IN CONTENT. The
    smooth/sharp assignment is E1's own (Q-2); the two-time dressing is sealed
    (:510-513); the complex-level refusal is forced by S2 and is stated. One framing
    imprecision recorded in §5.

RESIDUAL_FREEDOM — SOUND. FR :27-31, :174-199 verified (32-dimensional, fixed
    energy, non-coboundary, physical not gauge; A2 instance — the build's body
    carries "on the tested instance"). Selectors (1)-(3) match the sealed absences;
    non-selectors verified: gauge selects nothing (non-coboundary, FR :187-189), G2
    gates mu only (GB :217-238), response-value selection barred (PA :143-145).
```

---

## 5. MINOR FINDINGS (recorded; neither overturns any flag)

```text
MF-1  "TENSORED WITH" (build §5 and the REGULARITY flag line) misstates the algebra
      by which the smooth and sharp factors combine. At the sealed bytes they are
      separate SUMMANDS of the generator — h_lambda = h_0 + lambda v(t) M(t) (x) S
      + a J(t) (PA :124-127) — i.e. two distinct vertex insertion types composed
      under two-time Dyson dressing (E1 B-L2's "a-VERTEX SECTOR" vs "RECORD
      VERTEX"), not tensor factors of one another. The build marks the framing
      YOURS (§7) and displays the correct sealed structure in its own §2 Q3, and
      the MIXED verdict (smooth connection-read side, sharp record-write side,
      singular structure on the record side, two-time) is exactly E1's sealed
      typing, unchanged by the framing word. NON-OVERTURNING.

MF-2  The flag block twice compresses a sealed qualifier: (i) "weights promoting
      the sharp projector to the lead" without C4's "at full tau_R" (present in the
      build's §2 Q3 and §5 citations; satisfied identically under E1's frozen D2,
      every cell at full tau_R, :398-399); (ii) the BLOCK_SPLIT flag cites
      "GB :122-145" for the exhaustion<->refinement index bridge whose sealed
      statement sits at GB :105-111 (the body's citation, §3 S2 "GB §2.2-2.3
      :97-145", is precise). Citation-precision only. NON-OVERTURNING.
```

---

## 6. ATTACK (d) — INJECTION

Checked every verdict-bearing sentence of the build's §§0, 3-6 and the flag block
against its citations: every positive claim carries an M03/PA/E1 span; every
stopping point carries an M03/FR/GB/FORM span; the carried inventory carries GB/GC
spans; the barred shortcut carries GC's span. No verdict cites the build's tasking
or any brief text as authority. The one brief-shaped sentence ("The E1 file
materially omitted by a prior build is READ here") is a process disclosure, not a
verdict. INJECTION = none.

---

## 7. WHAT WOULD HAVE REFUTED, AND WHY IT DID NOT

```text
REFUTE-1 (idealization): an equal-time or disjoint-support simplification anywhere
  in R7/REGULARITY → the build quotes E1 :510-513 against it and types two-time
  throughout. Not found.
REFUTE-2 (import): diamond contractibility, oscillator length, Vol_4, tau_R-as-
  scale, or sea-covariance consumed as authority in any of R1-R7 or the split →
  the only decisive candidate (contractibility => single-block) is named BARRED and
  not taken; PA/E1 content is classification-only. Not found.
REFUTE-3 (circularity): the build supplying the incidence set, the transport, the
  anchoring, or the spanning family it says are missing → S1-S4 are honored at
  every use; the split returns NOT_DETERMINED; h_j is verified consumed-not-
  displayed across all of M03. Not found.
REFUTE-4 (fake spans): any of the three most load-bearing quotes absent or altered
  at the bytes → all three verbatim (§2), plus 40+ secondary spans checked. Not
  found.
REFUTE-5 (contradiction with the carried panel inventory): R4's nonzero single-
  branch boundary vs GB/GC's ⊥ im(d_0) → the sealed M03 covariance law itself
  forces single-branch non-invariance (:336-354), so the carried typing is only
  coherent at the difference-branch/quotient level — which is exactly the build's
  stated reconciliation. The refinement is grounded, hedged (YOURS + S3), and
  changes no carried verdict. Not a refutation.
```

---

## 8. FLAG BLOCK

```text
FIXED_CLAIMS_AUDIT = R1 SOUND(M03 :128-139,:146-149,:385-399) ; R2 SOUND(M03
  :128-133,:177-180,:234-248 — eraser entry connection-independent at the bytes) ;
  R3 SOUND(M03 :190-198,:286-293; FORM :180-199) ; R4 SOUND(M03 :325-354 verbatim;
  the YOURS adjoint step is exact conditional algebra with the port-to-0-cell
  anchoring flagged UNSEALED; the OPEN reading is grounded in the sealed "two open
  histories ... endpoint characters" :352-354; the tension with GB/GC's literal
  ⊥ im(d_0) typing is handled as quotient-level and is the only jointly coherent
  reading of GB :150-153 with M03 :336-354) ; R5 SOUND(M03 :15-22,:205-213,
  :364-370) ; R6 SOUND(M03 :24-41,:376-378 verbatim) ; R7 SOUND-AS-CLASSIFIED(PA
  :99-113,:115-127,:132-135,:137-141; E1 :342-351,:353-366,:510-513,:1111-1116 all
  verbatim; SUSPECT flags are PA's own sealed text; uniqueness read as
  over-the-source-chain, classification-only use) ; S1-S4 SOUND(S1 verified against
  the whole of M03 — h_j never displayed as an edge sum; S2 = FR :237-242 verbatim) ;
  BLOCK_SPLIT NOT_DETERMINED SOUND(no silent supply of G3; block-blindness of
  R1-R3,R5,R6 checked; missing input matches FR :237-242 + GB :105-111,:132-135;
  consistent with GB flag :306-340 and GC :279-285) ; REGULARITY MIXED/NOT-
  DETERMINED SOUND-IN-CONTENT(E1's own smooth/sharp typing; complex level correctly
  refused via S2; framing imprecision MF-1) ; RESIDUAL_FREEDOM SOUND(FR :27-31,
  :174-199; selectors match sealed absences; non-selectors verified).

IDEALIZATION_OR_IMPORT = none consumed. The equal-time idealization is explicitly
  not taken (E1 :510-513 quoted against it; vertex typed two-time throughout); the
  diamond-contractibility shortcut is named BARRED and not taken (GC :122-148);
  scale-bearing PA/E1 content is SUSPECT-classification only, never certifying; no
  faithfulness, no imported GR anywhere. Minor: the "TENSORED WITH" framing (MF-1)
  is an unsealed structural idealization of how the two vertex factors combine —
  YOURS-marked, non-load-bearing, verdict-preserving.

CIRCULARITY = none. The build derives read-properties of the ratified law and stops
  at S1-S4; it does not supply the incidence set, the transport, the anchoring, or
  the spanning family, and its split verdict returns NOT_DETERMINED rather than
  manufacturing the datum. h_j verified consumed-as-given across all 678 lines of
  M03.

INJECTION = none. Every verdict traced to a sealed span; no tasking text cited as
  authority.

OVER_LENS_VERDICT = BUILD_SOUND. Two minor non-overturning imprecisions recorded
  (MF-1 tensor-framing; MF-2 two flag-block citation compressions); no positive
  claim rests on a forbidden import, an unsealed idealization, a circular supply,
  or a fabricated span.

PROVENANCE = CLEAN(all 8 digests — target + 7 sources — recomputed at path before
  reading; all matched tasked values and all 7 adjacent sidecars; every load-bearing
  span re-read from bytes; the other panel lens artifact NOT opened; no
  register/tracker/plan/road/ledger/lens read).

MACHINERY_USED_BY_ME = no (shasum -a 256 and byte reads only; exact symbolic
  reasoning on sealed forms; no numeric evaluation of any physical quantity, no
  fixed-point execution, no end test, no member binding; no git action; no
  register/commit/push; output name probed ABSENT before write).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
G3_PANEL_OVER_RESULT = SEALED.
```
