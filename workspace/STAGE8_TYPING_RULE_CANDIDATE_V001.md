# PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (mandate pattern of ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)

# STAGE 8 — THE CELL-LOCAL WRITE-TYPING RULE CANDIDATE, V001
## BLIND AUTHOR (TYPING-CANDIDATE) — AUTHORED PROPOSED PHYSICS — [CLAIMED]

Date: 2026-08-13.
ALL_RESULTS = CLAIMED until checked.

## 0. STATUS, ON ITS FACE

**THIS IS AUTHORED PROPOSED PHYSICS.** It is AUTHORED, never ENTERED. Entry is the
principal's act alone, by a separate entry addendum naming this artifact's sealed
digest and its opposite-lane check's digest (the ad9fc14e entry-mechanics pattern).
Until that addendum exists this candidate is PROPOSED_NOT_ADOPTED and no construction
may consume it as premise. The registrar may not enter; the lanes may not enter;
conversational summaries may not substitute for the addendum.

The object supplied: the single unwritten typing the record's sealed text does not
give — what "cell-local" means for the WRITE on a multi-cell complex, i.e. which
chains an admissible per-cell write may read. Of record this typing is ABSENT ("the
sealed text supplies NO definition of 'cell' or 'cell-local' on K_3" — 92dba8d3
§3.3; "cell-local is undefined of record on any multi-cell carrier" — 92dba8d3 N-3
:368-372; both CONFIRMED at bytes, 22f69dd3 §3.3, flag TYPING_CHOICES). Three
typings exist of record as ANALYSES only (T0 unconstrained / T1 plaquette-local /
T2 circuit-local, 92dba8d3 §3.3-3.4). This candidate authors ONE rule, mandate-shaped,
with its grounding, its authored core disclosed, its falsifiers, voids, no-freeze
clauses, its neutral consequence map, and its entry conditions.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
FENCES HELD: exact structural algebra only where math appears; NO physical quantity
       computed, bounded, or evaluated; no scale, no imported GR, no faithfulness
       authority; no register/tracker/plan/road/ledger/lens file read; no git action.
       An authored ingredient presented as forced VOIDS this candidate (the ad9fc14e
       honesty-of-record clause, adopted here as binding on this artifact).
```

---

## 1. PREFLIGHT AND SEALS

Output name probed before any write: `STAGE8_TYPING_RULE_CANDIDATE_V001.md` ABSENT
(artifact and sidecar). Every digest below recomputed from bytes at path by
`shasum -a 256` THIS session, BEFORE reliance.

```text
SEALS VERIFIED (recomputed at path; all MATCH the tasked digests / their sidecars):
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md   (the mandate pattern)
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                      (the entered premise)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md     (the entered artifact)
92dba8d33b09c5cd2a9ff5dab122f2cf2d3420d521cdf422d210d988f1ab6ee9  STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md           (T0/T1/T2 on K_3)
22f69dd3a87a1f71cc79d47606de2f547348a5619ba330cf31d53d013c67350e  STAGE8_BOTH_BLOCKS_CHECK_V001.md                   (BB_VERDICT = SOUND)
f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md               (T0/T1/T2 on K_dd)
bb2e6570e17bb5d7734d306d94ac672df73bb0377b71e252b48e4751b8dc28f5  STAGE8_MIN_CARRIER_CHECK_V001.md                   (MC_VERDICT = SOUND)
ca7fa4572a8b7037b40dd280700827081fec0e369ac0a8f9be1607113d719763  STAGE8_ANCHORING_DERIVATION_V001.md                (the anchoring law + fork)
4a048901d4d2869352b5daf668bc341c0a5a715ef9e6ef232ba5772a6c911625  STAGE8_ANCHORING_CHECK_V001.md                     (AN_VERDICT = CONFIRMED)
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md                    (GC — containment-only)
2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md  (M03)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md                (the write constraints of record)
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md      (V011 — digest matches the one quoted in 92dba8d3/22f69dd3)
UNVERIFIABLE: none. V011 content is consumed only at spans already opened and
confirmed at bytes by the verified sealed checks (92dba8d3 §1, 22f69dd3 §1).
```

---

## 2. THE SEALED RAW MATERIAL, QUOTED AT SPANS

```text
S1  THE RATIFIED LAW'S OWN LOCALITY SHAPE (M03 2cd1ffce).  DoR-009's ratified
    finite-locality row:  W_N^(n)[a] := tensor_(j=1)^N W_(1,j)^(n)[a_j]  (M03
    :135-140); per cell D_(n,j)[a_j] = diag(1, z_j^(n)[a_j], 1) reads a_j ONLY
    (M03 :127-133); zero-extension appends identity holonomies that drop out
    exactly (C7, M03 :387-399).  The j-th write factor depends on cell j's own
    history carrier and on nothing else.  What this FORCES: containment of the
    per-cell read inside the cell's own carrier.  What it does NOT force: which
    chains within the cell, or any bounding/contractibility property.
S2  CONTAINMENT-ONLY, OF RECORD (GC 8ed95b5a).  Cell-locality of record is the
    containment law support(L_c) ⊆ Omega_c and nothing more (GC :153, :203-206,
    flag :253-255); conservation and gauge-invariance are ONE condition,
    ⊥ im(d_0) (GC :82-91); and the one line that would collapse the typing —
    "cell-local ⟹ contractible ⟹ boundary ⟹ phi_H = 0" — REQUIRES importing the
    diamond's contractibility, unsealed on connection-only bedrock and barred
    (GC :122-148; reinforced 22f69dd3 §3.4: the record does NOT seal that a
    cell-local cycle bounds).
S3  THE ENTERED READING'S OWN SHAPE (V007 ccca6bb4; addendum 420ab02f).  The
    entered operative construction:  theta_j := contour-integral over gamma_j of
    A;  D_n = diag(1, z^n, 1) = exp(i n theta_j Q_cell)  (V007 :633-649) — the
    cell's read IS its closed circuit.  OPERATIVE DOMAIN: connected admitted
    b_1 = 1 cells (addendum :33-34; V007 :162-172); trees INERT by determination
    (V007 :168-169); b_1 > 1 OUT OF SCOPE — "choosing among them would be exactly
    the selector I am forbidden to freeze" (V007 :159-160, :171-172).  The entered
    trace cell is K_square with F = 0 — "No filled 2-cell is included in the
    trace carrier" (V011 :1840, :1881-1882, scope TRACE-CARRIER-SPECIFIC per
    92dba8d3 §1, CONFIRMED 22f69dd3 §1).
S4  THE WRITE CHAIN AND THE READ CHAIN ARE ONE OBJECT (G3 build 3e35ffe2).
    "Node G3 is the per-cell write chain ell_j — which edges/cells the per-cell
    holonomy actually reads" (:5-7); R1 cell-factorized read realized by the
    tensor row (:146-151); constraint inventory carried: gauge-invariant =
    conserved (one condition), cell-local, total-nonzero for n != 0, block-split
    NOT fixed (Q6 :130-135); S1 the incidence set is a consumed input, "h_j never
    displayed as an edge sum" (:225-230); the free space = the physical-block
    component of ell_j per cell (§6 :327-336); selectors of record: the
    current-density receiver + index bridge, OR a ratified successor displaying
    h_j as an explicit incidence sum, OR a principal decision (:338-344).
S5  THE ANCHORING LAW AND ITS FORK (ca7fa457, confirmed 4a048901).  The anchoring
    equation (*) d_0^dagger ell_j = e_out - e_in (derived exact, §2.1); under the
    ENTERED reading ell_j = gamma_j is closed, d_0^dagger gamma_j = 0, the ports
    collapse to one common 0-cell, the port action is INERT — DERIVED-FAMILY
    (degenerate), no contradiction with any sealed identity (§2.3a); the OPEN
    (edge) horn requires an UNSEALED nondegeneracy demand (§2.3b, "supplied, not
    sealed"); unconditionally the loop/edge typing of h_j is NOT-DERIVABLE, and
    the named HOLDER is "an authored-physics entry (an Attach-strength successor
    ...) or a principal decision; no lane may select the horn" (§2.3c :308-316;
    AN_VERDICT :514-522).  Kernel identity, exact and horn-independent:
    ker(d_0^dagger)|C_1(K_square) = Q·gamma_j — the circuit spans exactly the
    anchoring-invisible read directions (:347-356).
S6  THE THREE ANALYSES OF RECORD (92dba8d3 §3.3-3.4 on K_3; f1881511 §5 on K_dd;
    both checked SOUND).  T0 UNCONSTRAINED: AND SATISFIABLE.  T1 PLAQUETTE-LOCAL
    ("cell" = one filled 2-cell + closure): AND UNSATISFIABLE — on-cell conserved
    set = k·(boundary word), phi_H ≡ 0.  T2 CIRCUIT-LOCAL ("cell" = connected
    b_1 = 1 subcomplex, the Attach shape): AND SATISFIABLE — exact witnesses with
    both projections nonzero (K_3: phi_H = c_0/27, phi_f = lam − c_0/27,
    92dba8d3 :312-321; K_dd: chi_j = h/4 + (3/4)w, f1881511 :309-322).
    All three are ANALYSES; none is of record as a typing.
```

---

## 3. THE RULE — R-CLW (CIRCUIT-LOCAL WRITE TYPING), STATED EXACTLY

```text
R-CLW.  Let K be a connected multi-cell record complex and let {C_j} be its
ADMITTED CELLS, each a connected admitted subcomplex of K (admission is a separate,
unsupplied act — §8 N2).  "Cell-local", for the write on K, means EXACTLY:

  (a) CONTAINMENT.  The per-cell write chain ell_j (identically: the chain the
      per-cell holonomy reads, S4) has support contained in the 1-cells of its
      own cell:  support(ell_j) ⊆ C_j^(1).  The write of cell j may read no
      1-cell outside C_j.
  (b) NO BOUNDING CLAUSE.  No clause requires ell_j to bound in K or in C_j.
      Cell-locality is containment, not contractibility.
  (c) NO FURTHER CLAUSE.  (a) and (b) exhaust the typing.  In particular
      "cell-local" does NOT mean "supported on one filled 2-cell's boundary"
      (T1's clause), and does NOT mean "unconstrained on K" (T0's non-clause).

JOINTLY with the sealed constraints carried of record (NOT part of the typing,
restated for the operative content): conserved = gauge-invariant = bdry.ell_j = 0
(one condition, S2), and total-nonzero for n != 0 —

  LEMMA 1 (exact).  For a 1-chain ell with support(ell) ⊆ C_j^(1), bdry_K(ell) =
    bdry_{C_j}(ell) (the boundary of an edge of C_j lands on vertices of C_j with
    the same coefficients).  Hence the conserved cell-local set of cell C_j is
    exactly the cycle space  Z_1(C_j; Q) = ker(bdry)|_{C_j},  of dimension
    b_1(C_j) for connected C_j.
  LEMMA 2 (exact).  If b_1(C_j) = 1 the admissible set is the circuit line
    Q·gamma_j(C_j) \ {0}  (total-nonzero); if b_1(C_j) = 0 (tree) it is EMPTY —
    no nonzero conserved cell-local write exists; if b_1(C_j) > 1 it is the
    b_1(C_j)-dimensional cycle space minus zero, and R-CLW SELECTS NOTHING in it
    (§8 N1).

COHERENCE CERTIFICATES (consequences of the rule agreeing with sealed structure;
checks, not motivations):
  K-1  On the entered cell (K_square, b_1 = 1) the admissible set is Q·gamma_j —
       the entered read theta_j = <A, gamma_j> is EXACTLY a cell-local write
       under R-CLW, with the entered normalization the coefficient 1 (S3).
  K-2  On a tree cell the admissible set is empty — reproducing the entered
       disposition "b_1 = 0 ... INERT ... a DETERMINATION made on purpose"
       (V007 :168-169) as a theorem of the typing rather than a stipulation.
  K-3  The admissible chains are closed, so they lie in ker(d_0^dagger) — by S5's
       kernel identity the rule types the write to read EXACTLY the
       anchoring-invisible coordinate of the read space, the coordinate the
       sealed corpus supplies (entered) while leaving the boundary coordinate
       unsupplied.  The anchoring equation (*) is satisfied degenerately, never
       violated; no sealed identity is contradicted (S5, §2.3a).
```

R-CLW is the T2 shape of the sealed analyses (S6) elevated from analysis to a
proposed typing — with the difference that R-CLW is stated as clauses (a)-(c) with
each clause separately grounded or disclosed (§4), not as a witness recipe.

---

## 4. GROUNDED vs AUTHORED — THE LEDGER

Every clause traced. FORCED means: an exact consequence of sealed text at the cited
span. AUTHORED-CONTENT means: beyond what any sealed text forces; part of the
authored core; voids the candidate if presented as forced.

```text
ROW  CLAUSE                                          STATUS      GROUND / DISCLOSURE
L1   Containment at the one-cell stratum: the        FORCED      M03 :127-133, :135-140,
     per-cell write factor reads its own cell's                  :387-399 (the ratified
     history carrier only.                                       tensor row); GC :153,
                                                                 :203-206 (containment-only).
L2   Extension of containment to a MULTI-CELL        AUTHORED-   No sealed definition of
     complex, clause (a): identifying the            CONTENT     cell-local exists on any
     containment region with the subcomplex                      multi-cell carrier
     C_j^(1).                                                    (92dba8d3 §3.3, N-3
                                                                 :368-372; 22f69dd3 §3.3).
                                                                 L2 is the minimal faithful
                                                                 extension of L1's sealed
                                                                 shape; minimality argued
                                                                 §5, not forced.
L3   Clause (b), stated negatively (no bounding      FORCED      GC :122-148: the bounding
     clause can be ADDED to a grounded rule): the    (as an      inference requires an
     record does not seal that a cell-local cycle    exclusion)  unsealed import, barred;
     bounds; any rule carrying a bounding clause                 22f69dd3 §3.4 (the
     carries an underivable premise.                             record's own statement).
L4   The cell shape: "cell" = connected admitted     AUTHORED-   Grounded in the entered
     subcomplex; the b_1 = 1 sector is where the     CONTENT     operative domain (addendum
     rule's admissible set is a line.                (TYPE-P-    :33-34; V007 :162-172) —
                                                     grounded)   but that entry types the
                                                                 ATTACH map's domain, not a
                                                                 general cell concept; the
                                                                 lift to "the cell shape of
                                                                 the write typing" is
                                                                 authored, TYPE-P on the
                                                                 Attach entry chain.
L5   Clause (c), exclusivity (nothing beyond         AUTHORED-   The sealed inventory
     (a)+(b); excludes T0).                          CONTENT     carries cell-local as a
                                                                 REAL constraint on ell_j
                                                                 (G3 Q6 :130-135; GC flag
                                                                 :253-255); a typing that
                                                                 constrains nothing on
                                                                 multi-cell carriers types
                                                                 the sealed word away.
                                                                 Structural case §5; not
                                                                 forced.
L6   Lemmas 1-2 and certificates K-1..K-3.           DERIVED     Exact algebra from (a)+(b)
                                                     (exact)     + the sealed conserved
                                                                 condition (GC :82-91);
                                                                 one-line proofs displayed
                                                                 in §3; nothing consumed
                                                                 beyond L1-L5 and sealed
                                                                 identities.

AUTHORED CORE, complete: L2 (multi-cell containment extension), L4 (the cell
shape), L5 (exclusivity).  Nothing else is authored.  No ingredient of the core is
presented as forced anywhere in this artifact; any such presentation voids the
candidate (§0).
```

---

## 5. THE STRUCTURAL CASE — WHY THIS RULE AND NOT THE ALTERNATIVES

Argued from sealed structure only: the ratified law's own shape (S1), the entered
premise's own reading (S3), the anchoring law's boundary equation (S5). Never from
consequences.

**5.1 Against T0 (unconstrained).** T0 is not a typing of the sealed word; it is
its deletion on exactly the carriers where the word must bind. The ratified law's
own shape is a per-cell tensor factorization (S1) — a locality STRUCTURE sealed at
ratified strength. A typing of "cell-local" on a multi-cell carrier must extend that
structure; T0 vacates it (any K-global chain becomes "cell-local"). The sealed
constraint inventory lists cell-local as a fixed property of ell_j (S4, Q6); under
T0 that row would assert nothing on any multi-cell complex. R-CLW's clause (a) is
the minimal extension under which the sealed row keeps content and M03's factorized
row remains the one-cell instance of the same law.

**5.2 Against T1 (strict plaquette-local).** Three independent structural failures.
(i) T1's operative inference (on-cell chains bound, killing the harmonic sector)
is exactly the line GC seals as requiring an unsealed import — the record does not
seal that a cell-local cycle bounds (S2; GC :122-148; 22f69dd3 §3.4). A rule whose
distinctive clause needs a barred import cannot be the record's typing. (ii) T1's
cell concept ("cell" = one filled 2-cell with its closure) is inconsistent with the
entered premise's own cell: the entered trace cell has F = 0 of record (S3; V011
:1840, :1881-1882) — under T1 the entered cell contains no cell at all, and the
entered read theta_j (supported on four edges lying on no filled face) would be
typed NOT cell-local. A typing that renders the standing entered premise's own read
ill-typed contradicts the entry while it stands. (iii) T1 mistakes a one-carrier
degeneracy for a law: on the working-class carrier the whole complex was one cell
and cell-locality was satisfied trivially (92dba8d3 §3.3); the plaquette shape is
that carrier's census, not a sealed locality law — no sealed locality sentence
mentions faces (S2 is a containment law on Omega_c, not on face boundaries).

**5.3 Against the open/edge-typed variant** (typing the per-cell read as an OPEN
chain, the anchoring fork's other horn). The open horn rests on a nondegeneracy
demand that is supplied, not sealed (S5, §2.3b; the check: "held as a horn
hypothesis, never asserted"). The only sealed realization of a per-cell read is the
entered circuit (S3) — closed. The two horns are mutually exclusive realizations of
one h_j (S5, §2.3c); authoring the open horn would contradict the standing entry's
operative construction. R-CLW follows the standing entry's own reading; and per S5
the fork's HOLDER is exactly "an authored-physics entry ... or a principal
decision" — this candidate is that shaped act, submitted to the principal.

**5.4 ANTI-OUTCOME-SHOPPING DECLARATION.** The rule's motivation chain, complete
and in order: (1) M03's ratified per-cell tensor factorization → clause (a)'s
shape; (2) GC's containment-only ruling and its sealed bar on the bounding import →
clause (b); (3) the entered Attach's own operative construction (circuit read,
b_1 = 1 cell shape, tree disposition, no-selector bar) → clause (a)'s cell concept
and the closed-read shape; (4) the anchoring law's boundary equation, consulted
only for CONSISTENCY (certificate K-3), never for selection. **Nowhere in
(1)-(4) is the AND verdict on K_3 or K_dd, the anchoring VALUE, the G3 outcome, or
any downstream number consulted.** Three checkable facts pin this: (i) the
discrimination against T0 cannot be AND-outcome-driven, because T0 and R-CLW
produce the SAME AND outcome on every exhibited carrier (both SATISFIABLE, S6) —
the AND hinge cannot distinguish them, so it cannot have selected between them;
(ii) the discrimination against T1 rests on GC's sealed ruling and the entered
cell's F = 0 census, both independent of the K_3/K_dd computations and neither
mentioning any verdict; (iii) the consequence map (§9) REPORTS outcomes already
computed in sealed analyses (S6) — it was assembled after clauses (a)-(c) were
fixed from sources (1)-(4), and no clause was revised in response to any entry of
the map. The anchoring value the rule yields is the DEGENERATE family (ports
inert) — reported as-is, not repaired.

---

## 6. OPERATIVE DOMAIN

```text
D1  CARRIERS: connected multi-cell record complexes K with a declared admitted-cell
    family {C_j}, each C_j a connected admitted subcomplex.  On the one-cell
    degenerate case (K itself a single admitted cell) R-CLW reduces to the sealed
    containment law — no new content.
D2  CELLS: the rule TYPES any connected admitted cell (Lemmas 1-2 give its
    admissible set at every b_1); its non-vacuous single-line sector is b_1 = 1,
    the entered shape.  At b_1 > 1 the rule types the SPACE and selects no class
    (§8 N1).  At b_1 = 0 the admissible set is empty (K-2).
D3  ADMISSION IS OUTSIDE THE RULE: which subcomplexes of K are admitted cells is
    an unsupplied, separate act (§8 N2).  R-CLW is conditional on an admission it
    does not perform.  In particular R-CLW does NOT extend the entered Attach
    map's domain: the Attach entry stands at b_1 = 1 (S3) and this rule supplies
    no cycle selector beyond it.
D4  STRATUM: the rule binds the WRITE TYPING only.  It licenses no gluing
    (O11/O12/O-D1 remain unbuilt acts; the min-carrier ledger L-1..L-5 statuses
    are unchanged by this rule — f1881511 §7, ca7fa457 §4), promotes no candidate
    carrier (K_dd, K_3 remain candidate/audit stratum; NONE_SEALED at the
    physical write-carrier stratum stands), and decides nothing post-limit.
```

---

## 7. FALSIFIERS AND VOID CONDITIONS

Mandate-shaped, following the Attach lineage's convertibility structure (ad9fc14e
:45-58; addendum :49-56).

```text
F1  BLIND-TEST FALSIFIER.  The blind test at the cascade's end scores every output
    this typing feeds; a failed blind score voids the entry and everything TYPE-P
    on it (the Q-212 blast-radius pattern).
F2  STRUCTURAL FALSIFIER.  If any sealed forced result is later shown, at bytes,
    to contradict the rule — in particular: a sealed display of a per-cell write
    reading a 1-cell outside its own cell; a sealed forced OPEN single-branch
    read chain of record; a ratified derivation forcing a bounding clause — the
    entry is void without awaiting the cascade.
F3  TRANSPORT FALSIFIER.  If the sealed-absent diamond-to-complex transport is
    ever ratified (the Ref_a-indexed current-density receiver + the
    exhaustion<->refinement index bridge, the two UNSUPPLIED slots of record —
    3e35ffe2 S2, :338-344) and its transported per-cell chain is NOT in the
    cell's cycle space Z_1(C_j; Q), the entry is void.  (If it lands IN the
    cycle space, F5 governs.)
F4  DEPENDENCY VOID.  The circuit-shaped clauses (L4, and K-1/K-3's operative
    content) are TYPE-P on the Attach entry chain (420ab02f, ccca6bb4, ad9fc14e,
    DoR-008/009/013, A0); an Attach-entry void voids them with it.  The
    containment clause (L1/L2) rides M03's chain only and survives an Attach
    void at its own strength.  The three carried Attach conditions (underived
    causal parent; AC-2; the physical-domain proof — addendum :40-47) ride into
    every entered-rule claim unchanged.
F5  THE OPEN FORCING SLOT (the DoR-008 pattern, carried verbatim in shape):
        future_derivation_can_select_the_write_typing = NO_VERDICT
    This candidate does not prove that a future target-independent derivation
    cannot select the multi-cell typing.  If one lands and AGREES, the entry
    dissolves into the derivation and the authored core (L2/L4/L5) vanishes from
    the premise set; if one lands and DISAGREES, F2 has fired.
VOID SCOPE: on any void, every downstream claim consuming the typing (TYPE-P on
    the entry addendum) voids with it; the consequence map's entries revert per
    §9's void column.
```

---

## 8. NO-FREEZE CLAUSES

```text
N1  NO CYCLE SELECTOR.  At b_1(C_j) > 1 the rule types the cycle space and
    freezes NO class selection — the entered bar is carried verbatim ("would
    require a lawful cycle selector, which this artifact does not supply and may
    not freeze", V007 :171-172).  Selection remains with a future derivation or
    principal entry.
N2  NO CELL ADMISSION.  The rule admits no subcomplex as a cell; admission is
    unsealed of record and stays so (ca7fa457 §4 L-2: admission itself unsealed).
N3  NO GLUING LICENSE.  No O11/O12/O-D1 act is licensed; the anchoring law (*)
    and its fork resolution (§9) license no cell-to-cell port chaining — under
    this rule the ports are inert (S5 §2.3a) and chain nothing.
N4  NO CARRIER PROMOTION.  K_dd and K_3 remain candidate/audit-stratum objects;
    NONE_SEALED at the physical write-carrier stratum survives this candidate
    untouched.
N5  NO VALUE.  mu/G2, the post-limit kernel C, the physical-domain proof, and
    every gate are untouched: alpha_computed = false; kappa_record_computed =
    false; proof_authorized = false; coupling_evaluation_authorized = false;
    JOINT_ANCHOR_DERIVED = false.
N6  NO SEALED-TEXT AMENDMENT.  The rule adds a typing where the record has none;
    it re-reads no sealed sentence and amends no ratified law.  If entry occurs,
    the typing binds as an ENTERED PREMISE alongside the record, never as a
    correction to it.
```

---

## 9. THE CONSEQUENCE MAP — NEUTRAL

What the rule DECIDES if entered, stated flatly; every outcome below is either an
exact lemma of §3 or an already-sealed analysis quoted at spans; none was used to
select the rule's content (§5.4).

```text
C1  THE AND VERDICT HINGE (T0/T1/T2 → typed).
    K_3:  AND SATISFIABLE under R-CLW — the sealed T2 analysis becomes the typed
          verdict: witness the coordinate 3-circuit cochain of a b_1 = 1 circuit
          cell, exact split phi_H = c_0/27 != 0, phi_f = lam − c_0/27 != 0
          (92dba8d3 §3.4, checked 22f69dd3 §3.2).
    K_dd: AND SATISFIABLE under R-CLW — witness chi_j (the entered cell's own
          reading cochain), exact split chi_j = h/4 + (3/4)w, both nonzero
          (f1881511 §5, checked bb2e6570).
    The hinge moves from TYPING-DEPENDENT to TYPED-SATISFIABLE on both exhibited
    carriers.  CARRIED UNCHANGED: this settles the typing supply (N-3 of
    92dba8d3 §4) ONLY; the construction N-1 (a derived connected physical
    carrier, b_1 >= 2) and the supply N-2 (the b_1 > 1 domain/selector) remain
    open; no physical carrier of record exists.
C2  THE ANCHORING VALUE FORK (S5).
    Within the operative domain the admissible per-cell writes are closed
    (Lemma 1-2), so the LOOP horn binds: the anchoring value is the degenerate
    common-0-cell family, DERIVED-FAMILY(degenerate), the port structure INERT;
    the open/edge horn is EXCLUDED within the domain; M03's independent-branch
    endpoint characters remain unrealizable at the complex level (exactly
    ca7fa457 §2.3a, confirmed 4a048901 X2).  The fork's NOT-DERIVABLE gap is
    discharged by ENTRY (this rule is the holder-shaped act), not by derivation;
    the anchoring LAW (*) itself is untouched and remains derived of record.
C3  THE G3 REALIZATION (S4).
    Under the rule + the entered normalization, per admitted b_1 = 1 cell:
        ell_j = gamma_j(C_j)   (coefficient 1 at n = +1, the entry's content;
                                orientation per F08 — a naming freedom),
    and the incidence display of h_j is the cell's own signed circuit sum (the
    entered display, ca7fa457 §3 — at ENTERED strength; S1 of the G3 build is
    NOT discharged at law strength).  The block-split ceases to be a free datum:
    it becomes a COMPUTED function of the cell's embedding in K — the exact
    orthogonal projections of gamma_j(C_j) onto H and im(d_1^dagger) in K
    (computed instances: C1's splits).  G3 moves from UNSUPPLIED to
    TYPED-SUPPLIED-CONDITIONAL (on entry + a carrier).  NOT settled by the rule:
    the diamond-to-complex transport (S2 of 3e35ffe2 — now a falsifier channel,
    F3); the FR 32-dimensional refinement-lift freedom (lives at the
    refinement/transport stratum, untouched); mu/G2 and the post-limit C (the
    finite-N cross on any carrier where both projections are nonzero remains
    contingent on mu alone — f1881511 §6, quoted, not evaluated).
C4  IF THE RULE IS LATER REFUTED (any of F1-F3, or F5-disagreement):
    C1 reverts to TYPING-DEPENDENT (the sealed analyses stand as analyses);
    C2 reverts to NOT-DERIVABLE with the holder re-opened;
    C3 reverts to the G3 build's free space (§6 of 3e35ffe2);
    every claim TYPE-P on the entry voids (F4's scope);
    the sealed record itself is untouched — the rule was a premise, not a law.
```

---

## 10. ENTRY CONDITIONS — WHAT THE PRINCIPAL RATIFIES AGAINST

```text
E1  OPPOSITE-LANE CHECK FIRST.  A blind, cross-lineage, DEFAULT-REFUTE check at
    this artifact's sealed digest must: re-verify every seal in §1 at path;
    re-derive Lemmas 1-2 and certificates K-1..K-3 independently; re-open every
    FORCED row of §4 at its span and attack the FORCED/AUTHORED boundary (an
    authored clause found presented as forced VOIDS the candidate); re-derive
    the §9 map's exact entries against the sealed analyses; and return
    VERDICT = SURVIVES-FOR-ENTRY with scope stated (the ad9fc14e trigger
    pattern).  Until that check exists and passes, entry may not be proposed.
E2  WHAT THE PRINCIPAL MUST SEE: (i) the rule text §3 and the ledger §4 with the
    authored core (L2/L4/L5) explicitly before them; (ii) the structural case §5
    INCLUDING the anti-outcome-shopping declaration §5.4; (iii) the neutral
    consequence map §9 — the principal ratifies knowing WHAT the rule decides,
    on the record's own analyses, but is asked to ratify FOR the structural
    grounds, not for any outcome; (iv) the falsifier/void set §7 and the open
    forcing slot F5; (v) confirmation that all gates remain false and no
    physical quantity is decided by entry.
E3  THE ENTRY'S TYPING.  Entry makes R-CLW an ENTERED-AUTHORED PREMISE:
    TYPE-P | premises: DoR-008, DoR-009 (M03's chain), the Attach entry chain
    (ad9fc14e, 420ab02f, ccca6bb4), and the entry addendum itself — for every
    downstream claim consuming the typing.  Claims consuming ONLY the
    containment clause (L1/L2) carry TYPE-P on M03's chain + the addendum,
    without the Attach chain (they survive an Attach void, per F4).  The three
    carried Attach conditions ride in unchanged.  The entry decides a TYPING,
    not a value; alpha_computed / kappa_record_computed / proof_authorized
    remain false at entry and after it.
E4  ENTRY MECHANICS.  Ratification is a SEPARATE principal act: an entry
    addendum naming this artifact's digest and the check's digest, recording the
    principal's explicit confirmation (the ad9fc14e :60-66 mechanics).  The
    registrar may not enter; the lanes may not enter; no conversational summary
    substitutes.  Until then: PROPOSED_NOT_ADOPTED, and no construction may
    consume R-CLW as premise.
```

---

## 11. OVERCLAIM AND FLATTENING AUDIT

```text
IT DOES NOT claim the rule is derived: the authored core is L2/L4/L5, disclosed;
   the unconditional status of the typing question remains NOT-DERIVABLE of
   record until entry or a future derivation (F5).
IT DOES NOT discharge S1 of the G3 build at law strength, extend the Attach
   domain past b_1 = 1, admit any cell, select any cycle class, license any
   gluing act, or promote any carrier.
IT DOES NOT amend, re-read, or contest any sealed sentence; the F = 0 scope, the
   containment law, the anchoring law, and the three sealed analyses stand
   exactly as sealed.
IT DOES NOT evaluate anything: the only numbers displayed are sealed-text
   quotations (c_0/27, 3/4, h/4, census integers) or exact structural dimensions;
   mu, n, kappa, alpha never appear as values.
FLATTENING CHECK: "typed-satisfiable" is not flattened into "satisfied of
   record" (no physical carrier exists — C1's carried line); "the fork is
   discharged by entry" is not flattened into "the fork is derived" (C2);
   "the split becomes computed" is not flattened into "the split is computed of
   record" (conditional on entry + carrier — C3); "holder-shaped act" is not
   flattened into "holder's act performed" (entry remains the principal's alone).
```

---

## FLAG BLOCK

```text
THE_RULE = R-CLW (CIRCUIT-LOCAL WRITE TYPING): on a multi-cell record complex K
  with admitted cells {C_j} (connected admitted subcomplexes), "cell-local" for
  the write means exactly (a) CONTAINMENT — the per-cell write chain ell_j
  (identically the chain the per-cell holonomy reads) has support(ell_j) ⊆
  C_j^(1), reading no 1-cell outside its own cell; (b) NO BOUNDING CLAUSE —
  nothing requires ell_j to bound (containment, not contractibility); (c) NO
  FURTHER CLAUSE.  Jointly with the sealed conserved/gauge-invariant condition
  (one condition, ⊥ im(d_0)) the on-cell admissible set is exactly the cell's
  cycle space Z_1(C_j;Q) (Lemma 1): the circuit line Q·gamma_j at b_1 = 1, empty
  on trees (reproducing the entered inert disposition), the unselected cycle
  space at b_1 > 1.
GROUNDED_VS_AUTHORED = FORCED: containment at the one-cell stratum (M03 :127-140,
  :387-399 tensor row; GC :153, :203-206 containment-only); the NO-BOUNDING
  exclusion (GC :122-148 — the bounding inference requires an unsealed barred
  import; 22f69dd3 §3.4).  DERIVED-EXACT: Lemmas 1-2, certificates K-1..K-3.
  AUTHORED-CONTENT (the complete core, disclosed): L2 the multi-cell extension of
  containment to C_j^(1) (no sealed multi-cell definition exists — 92dba8d3
  §3.3/N-3, 22f69dd3 §3.3); L4 the cell shape = the entered b_1 = 1 subcomplex
  shape lifted to a general cell concept (TYPE-P-grounded on addendum :33-34,
  V007 :162-172; lift authored); L5 exclusivity (excludes T0; structural case
  only).
ANTI_OUTCOME_SHOPPING = the motivation chain is (1) M03's ratified per-cell
  tensor factorization → clause (a); (2) GC's containment-only ruling + sealed
  bar on the bounding import → clause (b); (3) the entered Attach's own operative
  construction (circuit read, cell shape, tree disposition, no-selector bar) →
  the cell concept and closed-read shape; (4) the anchoring boundary equation
  consulted for consistency only.  No step consults the AND verdict, the
  anchoring value, the G3 outcome, or any number.  Pinned three ways: T0 vs
  R-CLW produce the SAME AND outcome (both SATISFIABLE), so the AND hinge cannot
  have selected between them; T1's exclusion rests on GC's sealed ruling and the
  entered cell's F = 0 census, independent of every verdict; the consequence map
  reports pre-existing sealed analyses and was assembled after the clauses were
  fixed, with no clause revised in response.
DOMAIN_FALSIFIERS_VOIDS = DOMAIN: connected multi-cell complexes with declared
  admitted cells; types every b_1 (line at b_1 = 1, empty at 0, unselected space
  at > 1); admission, gluing, carrier promotion, and the Attach domain all
  OUTSIDE the rule.  FALSIFIERS/VOIDS: F1 failed cascade blind score (Q-212
  blast radius); F2 sealed structural contradiction (an outside-cell read, a
  forced open single-branch chain, or a forced bounding clause) — immediate
  void; F3 a ratified diamond-to-complex transport landing outside the cell's
  cycle space — void; F4 Attach-entry void kills the circuit clauses (containment
  survives at M03 strength; the three carried Attach conditions ride in);
  F5 open forcing slot future_derivation_can_select_the_write_typing =
  NO_VERDICT — an agreeing derivation dissolves the entry, a disagreeing one is
  F2.  NO-FREEZE: no cycle selector at b_1 > 1 (V007 bar verbatim); no cell
  admission; no gluing license (ports inert under the rule); no carrier
  promotion (NONE_SEALED stands); no value (all gates false); no sealed-text
  amendment.
CONSEQUENCE_MAP = neutral, decided-if-entered: (C1) the AND hinge becomes
  TYPED-SATISFIABLE on both exhibited carriers — K_3 witness the 3-circuit
  cochain, split c_0/27 + (lam − c_0/27); K_dd witness chi_j = h/4 + (3/4)w
  (sealed analyses 92dba8d3 §3.4 / f1881511 §5, both checked SOUND) — while
  N-1 (the physical carrier construction) and N-2 (the b_1 > 1 selector supply)
  stay open and no physical carrier exists; (C2) the anchoring fork resolves to
  the LOOP horn by entry: degenerate common-0-cell family, ports inert, open
  horn excluded in-domain, the anchoring LAW untouched (ca7fa457 §2.3a,
  4a048901 X2); (C3) G3 becomes TYPED-SUPPLIED-CONDITIONAL: ell_j = gamma_j(C_j)
  per admitted b_1 = 1 cell at the entered normalization, incidence display =
  the cell's signed circuit sum at ENTERED strength, block-split = the computed
  projection of gamma_j(C_j) in K — with the diamond transport (F3 channel),
  the FR 32-dim refinement freedom, mu/G2, and the post-limit C all untouched;
  (C4) on refutation everything reverts: AND to TYPING-DEPENDENT, the fork to
  NOT-DERIVABLE, G3 to its free space, TYPE-P dependents void, the sealed
  record untouched.
ENTRY_CONDITIONS = E1 blind DEFAULT-REFUTE opposite-lane check at this digest
  (seals re-verified; lemmas re-derived; the FORCED/AUTHORED boundary attacked —
  an authored clause presented as forced VOIDS; map re-derived) returning
  VERDICT = SURVIVES-FOR-ENTRY; E2 the principal sees the rule, the ledger with
  the authored core, the structural case with the anti-outcome-shopping
  declaration, the neutral map, the falsifier set, and the standing gates;
  E3 entry types R-CLW as ENTERED-AUTHORED PREMISE, TYPE-P on DoR-008/009 +
  the Attach entry chain + the addendum (containment-only consumers: TYPE-P
  without the Attach chain); a typing is entered, no value; E4 entry is a
  separate principal addendum naming candidate + check digests with explicit
  confirmation (ad9fc14e mechanics) — registrar and lanes may not enter.
STATUS = AUTHORED-PROPOSED-PHYSICS-NOT-ENTERED
SEALS_VERIFIED = ad9fc14e (supply-mandate DoR), 420ab02f (Attach addendum),
  ccca6bb4 (V007), 92dba8d3 (both-blocks) + 22f69dd3 (check), f1881511
  (min-carrier) + bb2e6570 (check), ca7fa457 (anchoring) + 4a048901 (check),
  8ed95b5a (GC), 2cd1ffce (M03), 3e35ffe2 (G3 build), 20a3a17d (V011, consumed
  only at spans confirmed at bytes by the verified checks) — all recomputed at
  path this session before reliance; none unverifiable.
FORBIDDEN_IMPORTS = none (no scale, no imported GR, no faithfulness authority;
  the one scale/GR-adjacent line — diamond contractibility — appears only as the
  sealed BAR grounding clause (b); no continuum object consumed; no
  register/tracker/plan/road/ledger/lens read).
MACHINERY_INVOKED = no (exact structural algebra only — two one-line lemmas on
  chain support and cycle spaces; every displayed number a sealed quotation or an
  exact structural dimension; no numeric evaluation, no fixed-point execution,
  no end test, no member binding; output name probed ABSENT before write; no git
  action; no register/commit/push).
alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
