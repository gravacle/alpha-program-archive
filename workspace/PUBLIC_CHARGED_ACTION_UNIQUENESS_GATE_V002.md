# Public Charged-Action Uniqueness Gate v002

## Supersession

Version 002 preserves the central v001 result and incorporates hostile review.
It narrows the zero-bare argument, states the scale convention for the
coupling, and labels executable checks as regression guards rather than
mathematical evidence.

## Question

Do the sealed pre-alpha principles and the derived primitive action character
force one absolute electromagnetic stiffness?

No measured coupling or coupling-derived target is used.

## Compact-connection convention

Let `a` be a compact unit-character connection with curvature `f=da`. The
primitive holonomy functional is

```text
W_1(gamma) = exp[i integral_gamma a].
```

The primitive record result fixes the unit winding and action-character
normalization. It does not determine the field-history weight.

At a declared response scale `mu`, the parity-even local public action can be
written

```text
S_K[a;mu]
  = -(hbar K_R(mu)/4)
      integral f_mu_nu f^(mu_nu) sqrt(-g) d^4x.
```

For every positive `K_R(mu)`, this action has:

1. the same compact connection periods and unit-character holonomy
   functional;
2. Lorentz covariance;
3. local compact-`U(1)` gauge invariance; and
4. a positive ordinary Maxwell stiffness.

The theories do not have identical Wilson-loop expectation values or
curvature responses. Those differences are precisely the physical effect of
changing `K_R`.

In rationalized unit-character normalization,

```text
a = e_HL A_can,
f = e_HL F_can,
K_R(mu) e_HL(mu)^2 = 1,
alpha(mu) = 1/[4 pi K_R(mu)].
```

The measured low-energy constant requires Thomson-limit matching after
nonlocal response, running, thresholds, and possible mixing are resolved.

## Non-selection result

Changing positive `K_R` cannot be removed by continuously rescaling `a`,
because such a rescaling would change the compact periods and the declared
unit character. The positive-`K_R` family is therefore a genuine family of
different public actions compatible with the currently sealed kinematic and
compact-holonomy premises.

The current pre-alpha sources contain no rule selecting one member. This
establishes non-selection by the present premises. It is not a universal
no-go against a future Gravacle dynamical selector, and it does not claim that
two complete quantum specifications have already been constructed.

The result does not rely on the Gaussian family retired by
`MICROSCOPIC_EXHAUSTION_IDENTIFIABILITY_GATE_V002.md`.

## Zero-bare route

Setting a microscopic trial Maxwell coefficient to zero is not yet a
selection rule because the complete microscopic operator, state, measure,
regulator, boundary reduction, coincident-point extension, and
renormalization condition have not been derived.

At this incomplete-specification stage, local Lorentz and compact-`U(1)`
symmetry alone do not distinguish an induced functional from one shifted by

```text
Delta Gamma_c
  = -(hbar c/4)
      integral f_mu_nu f^(mu_nu) sqrt(-g) d^4x.
```

The ordinary positive branch additionally requires the resulting stiffness
to remain positive.

This does **not** assert that arbitrary `c` values are all generated by one
fixed, fully specified zero-bare microscopic theory. The exact statement is:
until the physical measure and finite extension are uniquely derived, the
zero-bare declaration alone does not determine the finite induced coefficient.

## Consequence

The existing result fixes how a connection history can enter a public action
comparison. It does not fix the absolute cost of curvature. That missing
normalization is the quantity needed before alpha can be computed.

## Closure condition

A target-independent derivation must:

1. derive the complete public quantum specification;
2. derive the compact-connection curvature operator and physical measure;
3. select one finite coincident-point extension and renormalization condition;
4. forbid every competing finite local response compatible with that same
   complete specification; and
5. yield one positive `K_R(mu)` before coupling comparison.

Only then may the response be run and matched to the Thomson limit.

## Executable role

The companion script verifies source hashes, status discipline, target-literal
absence, and elementary normalization identities. It is a regression guard.
It is not evidence for the field-theoretic non-selection argument.

## Status

```text
primitive_unit_character_holonomy_derived = true
absolute_Maxwell_stiffness_selected = false
zero_bare_alone_selects_finite_induced_coefficient = false
complete_public_charged_action_unique = false
unique_finite_coincident_extension_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
