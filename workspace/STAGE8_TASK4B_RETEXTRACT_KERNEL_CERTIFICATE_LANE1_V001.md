# STAGE 8 TASK 4B — RETEXTRACT KERNEL CERTIFICATE — LANE 1 V001

Date: 2026-08-03  
Task: PASTE 471 / Task 4b  
Lane: CODEX LANE 1  
Custody: derivation only; no registration authority

```text
REGISTER_HEAD_AT_PREFLIGHT = Q-392
PREFLIGHT = PASS

LEAD_RESULT = THE_KERNEL_IS_CHARACTERIZED_BUT_TYPE_DOES_NOT_DECIDE_THE_IMAGE

P38_BY_CERTIFICATE = UNDECIDED_BY_TYPE |
  gap: COMPLETED_BASE_P_JET_KELDYSH_SUPPORT_CERTIFICATE_OR_INSTANCE

RETEXTRACT_KERNEL_CHARACTERIZED = true | TYPE-P |
  on the ratified R5 generated Schur domain
BASE_SCHUR_P_DERIVATIVE_IMAGE_TYPED = true | TYPE-P |
  image subset Hom(K_cycle,K_cycle^*) on the stationary R5 family
IMAGE_SUBSET_KERNEL_FORCED_BY_TYPE = false | TYPE-R
IMAGE_NOT_SUBSET_KERNEL_FORCED_BY_TYPE = false | TYPE-R

FINITE_RESTRICTION_ZEROS_REPRODUCED = true | TYPE-P
REGISTERED_P_VERDICT_WRITTEN = false
P_VERDICT_DECLARED_BY_THIS_ARTIFACT = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The certificate route reaches its lawful maximum.  On the ratified completed
R5 carrier, `RetExtract` is the fixed CTP congruence followed by the ordered
`(delta,c)` block projection.  Its kernel is therefore exactly the Schur
forms whose ordered mixed block vanishes.  The total base Schur
`p_i`-derivative

```text
X_i
 := dot D_i
    -dot C_i A^(-1)B
    +C A^(-1)dot A_i A^(-1)B
    -C A^(-1)dot B_i
```

is correctly typed as a covariant/reality-compatible operator
`K_cycle -> K_cycle^*`, but that carrier type does not determine its Keldysh
block support.  Both a pure `(delta,delta)` derivative in the kernel and a
reality-completed mixed `(delta,c)/(c,delta)` derivative outside the kernel
obey the same carrier, covariance, reality, unit, and restriction-interface
types.  The finite theorems select the first behavior on their active finite
reference surfaces; no ratified theorem transports that support statement to
the completed stationary pullback.

Accordingly, type proves neither `p`-freeness nor `p`-dependence.  The exact
remaining datum is a family-wide Keldysh-support theorem for the completed
base stationary `p`-jet, or one executable completed base instance.  The
parallel instance route carries that load.

---

## 0. Preflight, process, and authority ledger

### 0.1 Locked-process preflight

`alpha_supervision/LOCKED_PROCESS.md` was read before this artifact was
written.  The live and archive copies of the questions-settled register were
byte-identical and their sidecars verified at head `Q-392`.

```text
DOES_THE_OBJECT_EXIST = no |
  no prior completed-carrier kernel/image certificate decides P3-8

IS_THE_VERSION_CURRENT = yes |
  Q-392 is the register head; DoR-016/017/018/019 are in force

ARE_ITS_INPUTS_PRESENT = yes |
  enough to characterize the structural kernel and image type, and to decide
  whether those types force containment

PREFLIGHT = PASS
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Use |
|---|---|---|
| p-verdict computation | `27790d53b018a84f5f02e97f68e885de0ebb332735307ae0fa433322a6053189` | exact `(P3-5)` and `(P3-8)` forms |
| p-verdict cross-verification | `7aa42db3876711b0327a6ef3a4d4ad33603a0db290bc64427a1fc21c25e683fd` | K7 boundary and required certificate |
| carrier metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | completed carriers, Riesz maps, unit routing |
| square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | R5 domain, Hessian cube, Schur and retarded covariance |
| raw correlator-to-retarded-Hessian specification | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | pinned CTP rotation and ordered extraction |
| Q-243 finite Keldysh transport | `70185aa842bc500724719c65bd66b5f07005e2214b97be0d35e07fd029d5c68c` | exact finite `(delta,c)` zero |
| Q-279 nonzero-probe finite reference | `c7624d88aedaa0659755b5c566121b24c2ad0c29a0060daada43a31b22a6ddfb` | probe-on finite block and `kappa_eta` form |
| Q-309 finite kernel determination | `a4c916a7cfa82c2130c82d8947c869f118e224959d7824bba45695711b4919c3` | finite kernel/complement/probe zeros |

The register and locked-process mirrors verified as:

```text
QUESTIONS_SETTLED_REGISTER_V001.md =
  99ae628aace9bfbe90baf0ddef72ba9c38353b67c27d568d26b93bfa907a5fc4
LOCKED_PROCESS.md =
  e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2
```

### 0.3 Register sweep

The following entries were checked before deriving.  The sweep was for
`RetExtract`, retarded kernel, Schur `p`-jet, finite zeros, restriction
separation, completed carrier, and stationary evaluation:

```text
Q-243   finite CTP rotation; p-weight is in delta/delta noise and the
        ordered (delta,c) block is zero;
Q-247   separation holds on named norm/module/left-multiplier classes,
        fails on the bidual, and does not automatically type the physical
        retarded-Hessian class;
Q-279   the ordered finite retarded block remains zero with nonzero R probes;
Q-309   finite kernel, kernel-complement, and kernel-R blocks are zero;
Q-313   finite source-to-cycle factorization and zero bridge;
Q-315   physical restriction squares exist on their bounded lawful scope;
Q-365   rho_H,N cube and inverse/Schur/RetExtract covariance on the
        R5-generated Hessian class;
Q-367   DoR-017 ratification of the square and N member;
Q-384   DoR-019 ratification of the completed carriers, metrics, and units;
Q-390   DoR-018 ratification of Shape K jets;
Q-391   exact P3-8 criterion localized to the base Schur p-derivative;
Q-392   computation confirmed; certificate and instance routes commissioned.
```

No swept entry contains a Keldysh-support theorem for the completed base
stationary `p`-jet.  Q-247 cannot be imported by name: R5's generated,
possibly graph-closed Hessian class has not been identified with Q-247's
bounded represented left-multiplier class, and `rho_H,N` is not identified
with Q-247's canonical multiplier retraction.

### 0.4 Symbol and scope ledger

```text
C := C_prop, the reducing/propagating complement carrier;
K := K_cycle, the physical cycle carrier;
A := H_CC:C->C^*;
B := H_CK:K->C^*;
Cblk := H_KC:C->K^*;  formulas below retain the established symbol C;
D := H_KK:K->K^*;
S := an admitted K/K Schur form K->K^*;
X_i := total p_i derivative of Schur_base on the completed stationary family;
RetExtract := structural ordered retarded extraction, not a response value;
rho_H,N := R5-generated Hessian restriction, not Q-247's Pi_N;
finite zero := a finite active-reference/restriction fact, not a completed
               stationary evaluation;
p_i, nu := symbolic inputs; neither is evaluated.
```

---

## 1. L1 — structural characterization of `ker(RetExtract)`

### 1.1 The sealed extraction

The raw-map specification pins

```text
A_c     =(A_+ + A_-)/2,
A_delta = A_+ - A_-,

[A_+]   [1  1/2] [A_c    ]
[A_-] = [1 -1/2] [A_delta],
```

with the displayed matrix named `T_CTP`.  For a contour Hessian `S_C`, set

```text
S_(c,delta) := T_CTP^T S_C T_CTP.                (L1-1)
```

The sealed extraction is then

```text
RetExtract_G(S_C)
  :=(S_(c,delta))_(delta,c)
   =i_delta,G^* S_C i_c,G,                       (L1-2)
```

where `i_c,G` and `i_delta,G` are the common and difference injections of
the package.  If an R5 Schur form is already written in the pinned
`(c,delta)` coordinates, `(L1-2)` is simply its ordered `delta,c` block.
This is exactly square V004 R5's statement
`RetExtract_m = sealed Keldysh/retarded extraction on Schur_m`; no new
extraction is authored here.

### 1.2 Exact kernel

Let `SchurAdm_G` be the K/K Schur forms generated by R5 actions on the common
domain `D_017,G`, including only the graph/reducing domain on which R5
licenses `(L1-2)`.  Then

```text
ker(RetExtract_G)
 := {S in SchurAdm_G:
       i_delta,G^* S_C i_c,G = 0}

  = {S in SchurAdm_G:
       (T_CTP^T S_C T_CTP)_(delta,c)=0}.          (L1-3)
```

Thus the kernel annihilates exactly:

```text
pure (c,c) blocks;
pure (c,delta) blocks when they have no ordered (delta,c) companion;
pure (delta,delta) noise blocks;
and arbitrary sums whose ordered (delta,c) component cancels.
```

The kernel condition does **not** say that the full Schur form is zero.  In
particular Q-243/Q-279's nonzero `delta,delta` noise is an explicit finite
kernel element.

`(L1-3)` is a typed linear subspace condition.  No unnecessary topological
upgrade is made: it is closed in every declared R5 graph/operator topology
for which the coordinate injections and `RetExtract` are continuous; this
artifact does not assert continuity in an unnamed stronger topology.

### 1.3 Covariance and reality

Square V004 proves, on the R5-generated class,

```text
RetExtract_(G')(
  alpha_H,K S_G alpha_K^(-1))
 =alpha_R(RetExtract_G(S_G)).                     (L1-4)
```

Therefore

```text
S_G in ker(RetExtract_G)
 iff
alpha_H,K S_G alpha_K^(-1)
  in ker(RetExtract_(G')).                        (L1-5)
```

For orientation/reality reversal, `alpha_H,K` is semilinear: it conjugates
the coefficient and carries the cycle sign.  In block language the required
reality completion pairs a retarded block with its advanced adjoint; it does
not force the retarded block itself to vanish.  Hence the kernel is
family-covariant and reality-stable, but neither covariance nor reality
collapses it to the whole Schur class.

### 1.4 Restriction square

On the R5-generated class, `(R5-3)` and `(O1-SCHUR)` give

```text
rho_H,N(S_G)=S_G,N,

RetExtract_N(rho_H,N S_G)
 =rho_R,N(RetExtract_G S_G).                      (L1-6)
```

The forward implication is exact:

```text
S_G in ker(RetExtract_G)
 => rho_H,N S_G in ker(RetExtract_N) for every N. (L1-7)
```

The converse is not a theorem on this class.  Q-247's converse belongs to
specified bounded norm/module/represented-left-multiplier classes and their
canonical retractions.  R5 supplies graph-closed generated Hessians and
`rho_H,N`; it does not identify them with that Q-247 class and retraction.
No topology is switched to manufacture the converse.

```text
L1_KERNEL_FORMULA = PROVED | TYPE-P | (L1-3)
L1_KERNEL_COVARIANCE = PROVED | TYPE-P | (L1-5)
L1_FORWARD_RESTRICTION = PROVED | TYPE-P | (L1-7)
L1_REVERSE_SEPARATION_ON_R5 = NOT_PROVED | TYPE-U
```

---

## 2. L2 — image of the base Schur `p`-derivative

### 2.1 Block types

On the ratified completed stationary family, use the established R5 block
types:

```text
A=H_CC:C->C^*,
B=H_CK:K->C^*,
C=H_KC:C->K^*,
D=H_KK:K->K^*,
A^(-1):C^*->C on C_red.                          (L2-1)
```

Every total `p_i` derivative, including the stationary pullback through
`c_star(p,k)`, retains the corresponding domain and codomain.  Therefore

```text
dot D_i                         :K->K^*,
dot C_i A^(-1)B                 :K->K^*,
C A^(-1)dot A_i A^(-1)B        :K->K^*,
C A^(-1)dot B_i                :K->K^*.           (L2-2)
```

The exact inverse derivative

```text
dot(A^(-1))=-A^(-1)dot A_i A^(-1)
```

then gives

```text
X_i:=dot Schur_base
 =dot D_i
  -dot C_i A^(-1)B
  +C A^(-1)dot A_i A^(-1)B
  -C A^(-1)dot B_i
 in Hom(K,K^*).                                   (L2-3)
```

The image relevant to `(P3-8)` is

```text
I_p
 :=span{X_i(p,k):
         i a source-sector coordinate,
         (p,k) on the completed base stationary family}
 subset SchurAdm.                                  (L2-4)
```

This is the strongest carrier-level image characterization ratified without
an executable `Gamma_base`.

### 2.2 Covariance, reality, restrictions, and units

Differentiating the R5 Hessian covariance at symbolic `p_i` and using that
the admitted realization actions do not select a numerical `p_i` gives

```text
X_(alpha.i),(G') alpha_K
 =alpha_H,K X_i,G.                                (L2-5)
```

Here `alpha.i` is the transported source-sector label; it equals `i` when
the admitted automorphism fixes that label.  Covariance therefore never
silently turns a relabeling into invariance.

Reality reversal conjugates coefficients and reverses the ordered cycle
orientation exactly as in `(O1-4)`.  Restriction commutes on every R5
generated derivative for which the base family is supplied:

```text
rho_H,N X_i = X_i,N rho_K,N.                      (L2-6)
```

Metric V005's unit calculation gives every term in `(L2-3)` the same class

```text
[X_i]=U_action U_K^(-2),                          (L2-7)
```

and every C/K conversion in the Schur product routes through the declared
R4 blocks.  No implicit cross-sector unit appears.

These certificates place `I_p` in the correct covariant, reality-compatible,
unit-homogeneous K/K Schur class.  They do not distinguish the four Keldysh
blocks inside a K/K form.

### 2.3 What the symbolic `p` entries do and do not prove

The exact base inputs remain

```text
p_i -> omega_i(p_i), kappa_i(p_i)
    -> Gamma_base and its stationary blocks
    -> A,B,C,D and X_i.                            (L2-8)
```

At the finite active reference, `omega_i` carries coherent/difference data
and `kappa_i` carries noise.  Q-243 and Q-279 prove that the finite
`p`-carrying Hessian has only the `delta,delta` J/J block, with the ordered
`delta,c` block zero.  But the completed `X_i` is the total derivative of a
Schur complement at `c_star(p,k)`.  The terms in `(L2-3)` include the
completed stationary pullback and the reducing inverse.  The types in
`(L2-1)`–`(L2-7)` contain no theorem that these operations preserve
`delta,delta` support.

In particular:

```text
K/K sector type != Keldysh (delta,delta) block type;
reality covariance != vanishing ordered retarded block;
unit homogeneity != Keldysh support;
finite active zero != completed stationary zero. (L2-9)
```

```text
L2_IMAGE_CARRIER = Hom(K_cycle,K_cycle^*) | TYPE-P
L2_IMAGE_COVARIANCE = PROVED_ON_SUPPLIED_R5_BASE_FAMILY | TYPE-P
L2_IMAGE_KELDYSH_SUPPORT = NOT_TYPED | TYPE-U
```

---

## 3. L3 — containment determination

### 3.1 The required containment statement

By `(L1-3)` and `(L2-4)`, the certificate sought is exactly

```text
I_p subset ker(RetExtract)

iff

(T_CTP^T X_i T_CTP)_(delta,c)=0
for every i and throughout the completed stationary family. (L3-1)
```

This is `(P3-8)` written as a support theorem.

### 3.2 Kernel-side interface model

The finite sealed theory supplies a concrete model of the kernel-side
support.  In pinned coordinates, let

```text
X_i^(0)
 :=[[0,0],
    [0,N_i]]_(c,delta),                           (L3-2)
```

where `N_i:K_delta->K_delta^*` has the declared unit class and is transported
as an R5 Hessian covector.  Then

```text
RetExtract(X_i^(0))=0.                            (L3-3)
```

Choosing `N_i` with the Q-243/Q-279 reality behavior gives a covariant,
reality-compatible, nonzero Schur derivative in the kernel.  Thus the
carrier and symmetry types do not force noncontainment or `p`-dependence.

### 3.3 Retarded-visible interface model

Conversely, let `R_i` be a nonzero retarded-capable map on the declared
causal/contact support and include its advanced adjoint as required by the
sealed reality law:

```text
X_i^(R)
 :=[[0,R_i^A],
    [R_i,0]]_(c,delta),

R_i^A(x,y)=R_i(y,x)^dagger.                       (L3-4)
```

Then

```text
RetExtract(X_i^(R))=R_i !=0.                      (L3-5)
```

`X_i^(R)` has the same map type `K->K^*`, the same R4 unit class, and the
same signed/semilinear covariance law as `X_i^(0)`.  On a realization orbit,
assign it by

```text
X_(alpha.i),(G')^(R)
 =alpha_H,K X_i,G^(R) alpha_K^(-1),               (L3-6)
```

so no realization, orientation, frame, filtration, or cycle basis is
selected.  This is an interface-level countermodel, not an installation of
a physical base action: it proves only that the ratified type and symmetry
clauses permit a nonkernel image unless an additional support or instance
clause excludes it.

The local action-level realization is also immediate on the declared
Frechet chart: pair the reality-completed bilinear form `(L3-4)` with a
smooth scalar coefficient that is flat on the finite active section and
nonzero at an off-section stationary point.  Its finite active Hessian jet
is zero, while its stationary Hessian has ordered mixed block `R_i`.  This
uses only the already-admitted flat-germ operation and demonstrates logical
compatibility; it does not propose that term as `Gamma_base`.

The finite active-reference obligations do not distinguish `(L3-2)` and an
off-section realization of `(L3-4)`.  R5's base bottom leg fixes finite jets,
while the completed stationary point is not a finite stationary point.
A smooth completed action can have the sealed zero mixed jet on every
tested finite active section and a mixed stationary jet away from that
section unless a support-preservation or evaluation theorem forbids it.
No such theorem is in the swept stack.  This is precisely the gap, not a
proposal to adopt that action.

### 3.4 Type-level verdict

The two interface models establish:

```text
TYPE_AND_SYMMETRY imply I_p subset ker(RetExtract)       = false | TYPE-R
TYPE_AND_SYMMETRY imply I_p not-subset ker(RetExtract)   = false | TYPE-R
```

The ratified data fix the kernel and image **types**, but do not fix their
relative position.  Therefore

```text
P38_BY_CERTIFICATE = UNDECIDED_BY_TYPE |
  COMPLETED_BASE_P_JET_KELDYSH_SUPPORT_CERTIFICATE_OR_INSTANCE
                                                               (L3-7)
```

The exact missing certificate is one of the following equivalent forms:

```text
SUPPORT FORM:
  for every i, X_i has zero ordered (delta,c) block on the completed
  stationary family;

KERNEL FORM:
  I_p subset ker(RetExtract);

FACTORIZATION FORM:
  I_p -> Noise_(delta,delta) -> SchurAdm
  with the first arrow family-covariant and restriction-natural;

INSTANCE FORM:
  executable Gamma_base, c_star, A,B,C,D and total p-jets, permitting direct
  evaluation of RetExtract(X_i).
```

Any one proves `p`-freeness if it gives the zero inclusion.  One covariant
completed stationary witness with `RetExtract(X_i)!=0` proves
`p`-dependence.  None exists in the present certificate route.

---

## 4. L4 — finite falsifiers and regressions

### 4.1 Q-243 finite rotation, rerun

The sealed branch matrix is

```text
M_DD=[[1,-1],[-1,1]],
T_CTP=[[1,1/2],[1,-1/2]].
```

Direct multiplication gives

```text
T_CTP^T M_DD T_CTP=[[0,0],[0,1]].                (L4-1)
```

For the finite `p`-carrying Hessian

```text
H_N
 =i hbar kappa_N
  [[0,0],[0,ell_N tensor ell_N]]_(c,delta),
```

and also for its symbolic `p_i` derivative,

```text
RetExtract_N(H_N)=0,
RetExtract_N(partial_(p_i)H_N)=0.                 (L4-2)
```

The scalar coefficient is never divided out; it is annihilated only because
the ordered block is absent.

### 4.2 Q-279 probes-on reference, rerun

At nonzero probe `R_eta`, Q-279 gives

```text
kappa_eta
 =p(1-p)u_eta/(1-p+p u_eta)^2,

H_W,N^(J,J)(R_eta)
 =[[0,0],
   [0,i hbar kappa_eta ell_N tensor ell_N]]_(c,delta).
```

Hence

```text
RetExtract_N(H_W,N^(J,J)(R_eta))=0               (L4-3)
```

for symbolic `p`, every finite `N`, every admitted rank class, and every
admitted finite probe.  The nonzero `J_delta/R` block is not the
`(delta,c)` block and is not silently retyped as one.

### 4.3 Q-309 kernel-sector regression

The finite kernel, kernel-complement, and kernel-probe blocks remain

```text
FINITE_KERNEL_BLOCK = 0,
FINITE_KERNEL_COMPLEMENT_MIXING = 0,
FINITE_KERNEL_R_MIXING = 0.                       (L4-4)
```

The present theorem neither deletes the cycle carrier nor promotes the
source-kernel zero to an unevaluated completed stationary response.  It
therefore preserves Q-309's exact distinction between finite support
separation and physical cancellation.

### 4.4 Standing regression table

| Regression | Computation / reason | Result |
|---|---|---|
| reciprocal loop | each ordered system response keeps the finite `(delta,c)` zero; no joint contraction introduced | **PASS** |
| S8-A exchange | `(L1-4)` transports kernel membership and any nonkernel witness covariantly; invariance is not substituted for covariance | **PASS** |
| one-edge/tree | zero cycle class remains zero; no cycle content manufactured | **PASS** |
| pendant / restriction | forward kernel restriction `(L1-7)` holds; no forbidden upward inverse is claimed | **PASS** |
| reality | retarded-visible model includes the advanced adjoint; kernel noise model is conjugation-stable | **PASS** |
| batching | direct-sum block extraction is componentwise; no cross-system scalar product appears | **PASS** |
| identity zero-extension | `(L4-1)`–`(L4-3)` unchanged under appended identity cells | **PASS** |
| no implicit C/K unit | every Schur term uses R4 blocks and has class `U_action U_K^(-2)` | **PASS** |
| member selection | no N-member alternative, orientation, frame, realization, rank, or ratio selected | **PASS** |
| finite authority | every computable finite retarded target remains exactly zero | **PASS** |

### 4.5 Falsifier interpretation

The finite suite refutes any proposed completed base instance whose
restriction produces a nonzero finite ordered `(delta,c)` block.  It does not
refute a completed stationary mixed block merely because the finite active
surface has none: the comparison points are different, and no ratified
support-preservation theorem identifies them.  Conversely, a future
support certificate must reproduce `(L4-1)`–`(L4-4)` exactly.

```text
FINITE_FALSIFIER_RUN = PASS
FINITE_ZERO_PROMOTED_TO_COMPLETED_STATIONARY_ZERO = false
TOPOLOGY_SWITCH_USED = false
MACHINERY_APPEAL = false
```

---

## 5. Accounting and exact handoff

### 5.1 Kernel/image account

| Operation | Domain | Image | Kernel / loss | Restriction | Completion / topology | Status |
|---|---|---|---|---|---|---|
| CTP congruence | R5 contour Schur form | pinned `(c,delta)` form | none; invertible coordinate change | exact finite matrix | declared R5 graph domain | **TYPE-P** |
| ordered block extraction | pinned Schur form | retarded carrier | all forms with zero `(delta,c)` block | forward square `(L1-6)` | no stronger topology asserted | **TYPE-P** |
| complement inverse | `C^*` on `C_red` | `C` | outside reducing domain excluded by R5 | inverse square | DoR-019 completed C carrier | **TYPE-P on R5 domain** |
| Schur `p` derivative | completed base stationary family | `I_p subset Hom(K,K^*)` | none declared | finite active bottom data | base component instance absent | **TYPE-P signature / TYPE-U values** |
| containment test | `I_p` and `ker(RetExtract)` | Boolean support fact | fails to decide by type | finite zeros pass | needs support theorem or instance | **UNDECIDED_BY_TYPE** |
| response derivative | `X_i` | `RetExtract(X_i)` | exactly `(L1-3)` | finite output zero | not evaluated here | **NO_VERDICT** |

### 5.2 Exact handoff to the instance route

The parallel completed-base instance must supply only what this structural
certificate cannot:

```text
1. executable Gamma_base on the completed R5 domain;
2. its covariant stationary family c_star(p,k);
3. A,B,C,D and their total p_i jets at that family;
4. X_i from (L2-3);
5. the ordered block (T_CTP^T X_i T_CTP)_(delta,c);
6. restriction, covariance, reality, batching, and R4-unit checks.
```

Its final test is now literal membership in `(L1-3)`.  No additional carrier,
metric, member, normalization, or response convention is needed.

---

## 6. Final board

```text
L1_RETEXTRACT_KERNEL = CHARACTERIZED | TYPE-P
L1_KERNEL_CONDITION = ordered (delta,c) block equals zero
L1_KERNEL_COVARIANCE = PROVED
L1_FORWARD_RESTRICTION = PROVED
L1_REVERSE_SEPARATION_ON_R5 = TYPE-U

L2_BASE_SCHUR_P_IMAGE = I_p subset Hom(K_cycle,K_cycle^*) | TYPE-P
L2_COVARIANCE_REALITY_UNITS = PROVED_ON_SUPPLIED_R5_BASE_FAMILY
L2_KELDYSH_SUPPORT = TYPE-U

L3_CONTAINMENT_BY_TYPE = NOT_FORCED | TYPE-R
L3_NONCONTAINMENT_BY_TYPE = NOT_FORCED | TYPE-R

P38_BY_CERTIFICATE = UNDECIDED_BY_TYPE |
  COMPLETED_BASE_P_JET_KELDYSH_SUPPORT_CERTIFICATE_OR_INSTANCE

FINITE_RESTRICTION_FALSIFIERS = PASS
INSTANCE_ROUTE_REMAINS_LOAD_BEARING = true

REGISTERED_P_VERDICT_WRITTEN = false
P_VERDICT_DECLARED_BY_THIS_ARTIFACT = false
ALPHA_EVALUATED = false
K_STAR_OR_ROOT_EVALUATED = false
NUMERIC_RESPONSE_VALUE_EVALUATED = false
RANK_OR_RATIO_EVALUATED = false
MEASURED_CONSTANT_COMPARISON = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No register, plan, tracker, git, commit, or push action was performed.
