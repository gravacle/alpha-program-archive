CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 13 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO (verifier)   SUBJECT_LANE = CODEX 2 (producer)
SUBJECT_KIND = build of the PRINCIPAL'S entered objects
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V001.md` | `89aae8a7113098b6b3a6bf282e3df38c2eeae53dca16f51b55aaf7c710344a3b` | the subject under check |
| 02 | `relay_inbox/RELAY_PASTE_940_ENTERED_OBJECTS_CROSSCHECK_DARIO_V001.md` | `738d63b9c472d8015287d9b8d23f811a8a1bf913058118f97ad76062259952e7` | assignment |
| 03 | `ENTRY_ROUND_2_DECISION_OF_RECORD_V001.md` | `bca010f10771ba7fcd81cbfc2e4a430c2add46486ac829a856d8546a71288cca` | the principal's ten entries |
| 04 | — charge declaration `[1274,1370)`; Q-838 disclosure `[1371,1556)` | `1e012579a78e646557ec168233d981d709c0f597ace5c13742f2263b599bfb18`; `e2ac06420863f0dd8baaa1c1f747bccd86b69e43fec930cc052fc8800cbbecce` | where charge lives; the travelling condition |
| 05 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1` | closed route law and the eight price drafts |
| 06 | `JOINT_ANCHOR_DECISION_OF_RECORD_V001.md`, pointer `[699,750)` | `f0179d43b4b2ed89f4cfb9adc1daa8c7cc0c9c5f038e3bbe7332727fdddaaa8f`; `9337dd7e5782af58639241cf5a06fb5578b7666423e5f0578877052695b6ce76` | frozen round-1 act; blind fiber pointer |
| 07 | `JOINT_ANCHOR_CERTIFICATE_MANIFEST_V001.md` | `589c12f7cb76b5f4a2ba895d5942061ec3d801d6983f656304b172bfde964b27` | second frozen round-1 site |
| 08 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md`, `Delta_0^joint` `[14222,14296)` | `58b966ed371b23b29b9e3ceed280eb30c804484becb2b17ca6ea465668e951bc`; `bb73a8ec8816bddc9c84d84e48ca81bd8d315f316092cdf295dc8fb474e2826f` | the exact receiver space |
| 09 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, spans `[7724,7761)`, `[15760,15855)`, `[23013,23148)`, `[23081,23085)` | `42cf9687751d682994e000005064874359cb1244a96c4a0906eee166415ddaad`; `df65b27745ba70db6459a80b14d636727db89c0aac868953ba73efee2ff55302`; `ec8de612fda9d57203e6d60fff37217cc4b088c805a559bb62e2f3f9d9f39c80`; `a47f5039baec20312dc62b9c68d9a98133a8a6d02636773068c231ef61b9acda` | `E_ch` formula, HS seed, `P_ch` tuple and token |
| 10 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md`, product pair `[8840,8925)` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98`; `cc0c7710c4d9c52eae9bf3d0cab81bfccb366b806ad9694b286ff151ccdaa737` | the definition of `tau_0` — the faithfulness premise |
| 11 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md`, `[10436,10550)`; `STAGE8_CTP_..._C0_DOR008_ASSEMBLY_..._V001.md`, `[5058,5861)` | `7cae6616e1efa006575161d98811d8e3c1beb588dde2b1f820e0787877dc8235`; `a4892d4c5881d236cf993347ced33f8e30513e07b4b630c7f223c52d05d0ebd5` | the `A_C0` carrier and its decomposition |
| 12 | `STAGE8_AXN_RECORD_LABELLING_DARIO_V001.md`, `[10554,10816)`, `[11485,11670)` | `aa20388d86c74dff70f86b67b891b3a4f7df93809c08e510608ed047dce5e3b6`; `5450f120bde8fa0a0cf8d65d81ee6f28f1e8066f12d999b7c044b0f8105b9ad7` | my 930 two-branch consequence |
| 13 | `STAGE8_TASK4A_ORIGIN_FED_..._LANE2_V001.md`, `[781,794)`; `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md`, `[12114,13268)`; `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `2290a253f8465c7aadc4a97fb8dc0e3503d1c89f523c976276fc3d3bb761709d`; `fcf8d90eecb442ce114689d2c73bb546b32fe769a14de28c5e32dc8365ad8ce6`; `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | `I_A` site, K4 control, state pin, process law, S01-S37 |

```text
RECOMPUTED BY ME FOR THIS CHECK: 14/14 spans, 18/18 canonical payloads, 17/17 named object
digests, the 1463-byte price regenerated from member 05, the 1557-byte manifest, and both
frozen round-1 whole-file digests.  EVERY SUBJECT AND SIDECAR VERIFIED BEFORE READING.
THE PRINCIPAL'S ENTRIES WERE READ BEFORE THE BUILD'S ACCOUNT OF THEM.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN ENTERED-OBJECTS CROSS-CHECK — DARIO LANE — V001
## RELAY 940 — `[PLAN:AXN-BUILD-D39]` — THE CHECK THAT CONVERTS CLAIMED TO BOOKED

Date: 2026-08-10
Status: **SEVENTEEN OF EIGHTEEN OBJECTS CONFIRMED ON THEIR MATHEMATICS AND THEIR BYTES. ONE
STOP-GRADE FINDING, NARROW AND ONE-KEY: THE MANIFEST DOES NOT CARRY THE PRINCIPAL'S MANDATORY
DISCLOSURE.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. The standard, and why the bar sits where it does

The principal has entered. My check is what converts CLAIMED to booked, so a defect I miss here is not
returned in a later round — it becomes a booked object carrying a warrant it never earned, and
everything downstream of the pairing inherits that pairing's faithfulness.

I apply the bar I set at 936 and honour it in both directions: **STOP-grade only where a defect would
corrupt a principal act or a certificate's warrant; packaging nits are findings, not blocks.** I do
not soften a real failure because the principal has already acted, and I do not inflate a nit into a
block because the stakes are high.

I read the principal's decision before the build's account of it, and where the relay said "verify, do
not assume", I verified. In one place that check could have gone badly and did not; in one place it
found something.

## 2. The pairing [PROVABLE — CONFIRMED-FAITHFUL]

### 2.1 The extension, re-derived

The sealed seed at member 09 `[15760,15855)` is `<X,Y>_Tr := Tr_A(X^dagger Y)`. The entered object's
extension law is

```text
<a (x) r (x) b, c (x) s (x) d>_joint = Tr_A(a^dag c) * tau_R(r^dag s) * omega_H(b^* d)
```

with `tau_R(1)=omega_H(1)=1`. Setting `r=s=1` and `b=d=1` returns `Tr_A(a^dag c)` — **byte-for-byte the
sealed seed**, which is exactly what the object's normalization clause claims. The extension is a
cross-sector extension of the seed, and the build does not call the joint pairing source-derived.

### 2.2 Faithfulness — the check the relay told me not to assume

The certificate's premise is that `Tr_A`, `tau_R` and `omega_H` are each faithful. Each was checked.

- **`tau_R`** is declared as the inductive-limit state with `tau_R,N = tr_3 (x) ... (x) tr_3` on
  `R_N = (x)_(j=1)^N M_3(C)`. Normalized traces are compatible with the sealed `iota_NM(A) = A (x) I_(M-N)`,
  so the limit exists and is the unique tracial state of the UHF algebra — **faithful**.
- **`Tr_A`** is the source trace of the sealed seed, finite on the unit (the equivalence derivation
  uses and cancels `Tr_A(1)`) — **faithful**, and unnormalized-but-finite is harmless since a positive
  faithful trace with finite trace of the unit is a positive multiple of a faithful state.
- **`omega_H = tau_0,+ (x) tau_0,-`** is where this could have gone wrong, and it is the one I pushed
  hardest. The sealed span at member 10 `[8840,8925)` names the product; the surrounding sealed text
  defines the one-factor functionals:

```text
tau_0(U_lambda)=0 for lambda!=0, tau_0(1)=1,
epsilon(U_lambda)=1 for every lambda.
The first is the coefficient-at-identity/Haar state; the second is evaluation
at the trivial character.
```

  `tau_0` is the **Haar state**, which has full support on the compact carrier and is **faithful**.
  Note what sits immediately beside it in the same sealed bytes: `epsilon`, the evaluation functional,
  which is **not** faithful. Had the build extended with `epsilon`, the faithfulness certificate's
  premise would have been false and the pairing's warrant would have collapsed. **It used `tau_0`.**
  The distinction is drawn correctly at the sealed bytes.

The minimal tensor product of faithful states is faithful, so `Tr_joint = Tr_A (x) tau_R (x) omega_H`
is faithful on `A_C0`. Faithfulness on the whole algebra restricts to any subset, so imposing the two
`Delta_0^joint` equations preserves the implication — the certificate's final clause is sound, and its
receiver is exactly member 08's pinned space, not a substituted source space.

**PAIRING = CONFIRMED-FAITHFUL.**

### 2.3 One unnamed hypothesis [finding, not STOP-grade]

The proof invokes faithfulness for the **minimal** tensor product while the carrier is the **graded**
tensor product. The corresponding theorem requires the component states to be **even**. All three are —
two traces and a Haar state are grading-invariant — so the conclusion holds and the warrant stands.
But the evenness hypothesis is not named, and a replayer following the proof exactly would have to
supply it. This is the same shape as the finding I returned at 935 §5.6 and the same class as the
RL-06 defect: the conclusion is true, the stated grounds are one clause short.

## 3. `E_joint` and the four certificates [PROVABLE — 4/4 REPLAYED]

`E_ch` is pinned to member 09 `[7724,7761)`: `Phi_N(rho)=P_0 rho P_0+P_ch rho P_ch`, with
`P_0+P_ch=I_src` in the adjacent sealed text. That is the charge-block pinching, and it is a genuine
conditional expectation: `E_ch(I)=P_0+P_ch=I_src` (unital), and
`P_0(P_0 rho P_0 + P_ch rho P_ch)P_0 = P_0 rho P_0`, so `E_ch^2=E_ch` (idempotent). Both replay.

| Certificate | Replay | Result |
|---|---|---|
| total typing | type `E_ch` on `A_src`, identities on the two sealed factors, tensor the three total maps | **REPLAYS** |
| conditional expectation / idempotence | tensor `E_ch`'s unit, positivity, bimodule and idempotence identities with two identity maps | **REPLAYS** — verified from the pinched form above |
| **charge-diagonal fixed space** | `Fix(E_ch (x) id (x) id) = Fix(E_ch) (x) R_inf (x) B` | **REPLAYS** — true for any idempotent `E_ch` |
| commutation | evaluate `E_joint i_src = i_src E_ch`, `E_joint i_R = i_R`, `E_joint i_B = i_B` on elementary tensors | **REPLAYS** |

### 3.1 Pressing the fixed-space certificate, which is where the risk lives

My own 930 established that `E_joint = E_ch (x) id_R_inf (x) id_B` is not merely unforced but **FALSE**
under a substantive record-labelling, because `id_R_inf` cannot be the charge-diagonal projection of a
factor carrying charge. If an unforced labelling claim were going to hide anywhere, it would hide in a
certificate named "charge-diagonal fixed space".

It does not hide there. The certificate's `procedure` reads "apply the principal declaration that
record and history carry trivial charge", it pins `declaration_source` to the principal's exact span
`[1274,1370)`, and it carries the Q-838 disclosure digest. **It computes the fixed space conditionally
on the declaration and claims nothing about which labelling is correct.** That is the honest shape: the
mathematics is unconditional given the declaration, and the declaration is flagged as the authored,
possibly-false input it is. The build's §10 says the same in its own words — `E_joint` is authored under
the declaration, "not inferred from the tensor presentation".

## 4. The Haar equivalence — exact, not plausible [PROVABLE — EXACT-CONFIRMED]

This is the certificate that supersedes the principal's frozen round-1 enum, so it must be exact.
Replayed:

```text
omega_hist(b) = <1_A_C0, i_B(b)>_joint / <1_A_C0, 1_A_C0>_joint
              = [Tr_A(1) * tau_R(1) * omega_H(b)] / [Tr_A(1) * tau_R(1) * omega_H(1)]
              = omega_H(b) / omega_H(1)
              = omega_H(b)                       since omega_H(1) = 1
```

The identity holds **for every `b` in `B`** by exact algebraic cancellation. `Tr_A(1)` and `tau_R(1)`
cancel and need not equal 1; only `omega_H(1)=1` is used, and the pairing object declares it. There is
no approximation, no limit, and no appeal to plausibility. The certificate therefore forces
`control_relation = PRODUCT_HAAR_EQUIVALENT` on its own derivation, and the build is right that it does
not use K4 to select the functional — the K4 negative control is disclosed as a price, not used as a
warrant.

**HAAR_CERT = EXACT-CONFIRMED.**

### 4.1 The frozen act [PROVABLE — BYTE-UNTOUCHED-VERIFIED]

I rehashed both round-1 artifacts myself rather than reading the assertion that they were untouched:

```text
JOINT_ANCHOR_DECISION_OF_RECORD_V001.md      f0179d43b4b2ed89...   BYTE-UNTOUCHED
JOINT_ANCHOR_CERTIFICATE_MANIFEST_V001.md    589c12f7cb76b5f4...   BYTE-UNTOUCHED
```

The populated `FrozenEnumDisposition.v001` instance takes the `SUPERSEDED_BY_NEW_ACT` / `H-HAAR` arm,
carries `round1_bytes_mutated:false`, `round1_post_freeze_mutation:"FORBIDDEN"`, and
`downstream_enum_authority:"ROUND2_ONLY"`, and supplies both required digests. It matches the arm the
schema defines for a certified `H-HAAR`, and it is a new record layered on top rather than an edit.

## 5. Pointers, embedding, and blind custody [PROVABLE — 4/4]

```text
F-DOR013   label bytes "F-DOR013" -> f4c5d3408f70a7aa...   target [699,750) 9337dd7e5782af58...  MATCH
IA-STATUS  [781,794)  2290a253f8465c7a...                                                       MATCH
PCH-TUPLE  ctx [23013,23148) ec8de612fda9d572... ; member [23081,23085) a47f5039baec2031...     MATCH
S-TENSOR   proof re-derived below                                                                MATCH
```

**S-TENSOR re-derived.** `i_src(a) = a (x) 1_R_inf (x) 1_B`, domain `A_src`, codomain
`A_C0 = A_src (x) R_inf (x) B`. The two sealed factors are unital, so the map is unital
(`i_src(1)=1_A_C0`), it is a total `*`-homomorphism, and it is the canonical injection into a tensor
product with unital factors, hence injective. It lands at `A_C0` — not at `A_SRF_CTP`, the
wrong codomain of the three declarations the instrument listed. The proof is correct, and this is the
route my own 927 survey failed to enumerate.

**Blind custody, audited by endpoints.** The `I_A` and both `P_ch` extents are clean of rank-shaped
tokens. The DoR-013 extent contains the token `rank`, and I opened it to resolve exactly that: the span
is `a0_rank_fiber = the sealed\n  DoR-013 fixed-A0 fiber` — a **field name** bound to a prose pointer.
The digits present are identifier components (`a0`, `DoR-013`, `A0`); there is no rank value, no
dimension, and no ratio. Nothing rank-shaped is dereferenced. **BLIND HELD.**

## 6. Price and manifest [PROVABLE]

**PRICE = BYTE-MATCHED, regenerated from the instrument.** I extracted the eight selected drafts from
member 05's own option tables — not from the build's §7 table — and concatenated them in entry order
with zero separators:

```text
my concatenation = 1463 bytes    my SHA-256 = 7f94eb5240890cb341cc2b1475637051bd4826358ab042f437e894b0ed54d8f9
build claims       1463 bytes                 7f94eb5240890cb341cc2b1475637051bd4826358ab042f437e894b0ed54d8f9
```

All eight individual draft digests match the build's table as well. No draft is shortened, restated,
reordered, or omitted, and the whole-price rule is honoured — a partial price is not a filled option.

**MANIFEST = 18/18.** All eighteen JSON payloads re-parse and are **byte-exactly canonical** under the
declared law — lexicographic keys, comma/colon with no whitespace, `ensure_ascii`, no terminal
linefeed. All seventeen named object digests reproduce, and the manifest itself is 1557 bytes hashing
to `f4a6b127830c4d10...`.

## 7. THE STOP-GRADE FINDING: the disclosure does not reach the manifest [PROVABLE]

The principal's entry makes one condition explicit and mandatory:

```text
DISCLOSED: this declaration is unforced on a notational record-labelling and FALSE on a
substantive one (Q-838, undecidable of record); the disclosure travels with every consumer.
```

The build states its own implementing rule: **"Every builder-authored schema that consumes `E_joint`
below carries `q838_disclosure_sha256`."**

I tested that rule against every payload. Two consume `E_joint` without carrying the disclosure.

**One has a valid, disclosed excuse.** `PairingEJointCrossReference.v001` is a **closed schema fixed by
the instrument** — I re-read its definition at member 05 and it admits exactly nine keys with no room
for a tenth. The build says so and mitigates correctly: both objects it references carry the disclosure
digest, and the adjacent record carries the disclosure verbatim. That is handled properly.

**The other has no excuse.** `rd22.axn-entered-objects-manifest.v001` is **builder-authored**, consumes
`E_joint` by referencing both `joint_superselection_map_sha256` and `ejoint_bundle_sha256`, and does
**not** carry `q838_disclosure_sha256`. Nothing constrains its key set; the build defined it.

The build's own internal precedent settles the question:

```text
rd22.axn-entered-ejoint-bundle.v001      4 keys   q838 = YES
rd22.axn-entered-objects-manifest.v001  17 keys   q838 = NO
```

The **bundle** is an index object that carries only digests of other objects, and the build gave it the
disclosure. The **manifest** is an index object of exactly the same class, one level up, and it did not
get it. Same kind of object, opposite treatment, and the rule that distinguishes them is the build's
own.

**Why this is STOP-grade and not a packaging nit.** The manifest is the top-level index — the object
that actually travels to downstream rounds and the natural entry point for any consumer of the entered
objects. A consumer reading the manifest receives the `E_joint` bundle digest with no attached notice
that the underlying declaration is unforced on one live labelling branch and **false** on the other.
That is precisely the propagation failure the principal's condition exists to prevent, and it defeats
that condition at the one object designed to carry it onward.

The obvious reply — that the manifest references the bundle, and the bundle carries the disclosure — is
the flattening the build itself disclaims elsewhere: its own S01-S37 walk insists "an enum is not its
warrant" and "a digest is not a certificate". A reference to a disclosed object is not the disclosure.

**The fix is one key, and it is contained.** Add `q838_disclosure_sha256` to the manifest. That changes
the manifest digest only; no other payload in the build references
`ENTERED_OBJECT_MANIFEST_SHA256`, so nothing else moves. Everything in §§2-6 stands unchanged.

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the principal's ten entries and the Q-838 disclosure at its exact span;
  the sealed HS seed, E_ch formula, tau_0 definition, and A_C0 decomposition;
  the frozen round-1 bytes; the blind pointer extents.

CONDITIONED-ON:
  the principal's authored declaration that charge acts on the source factor only — carried as the
    authored, disclosed input it is, and not treated as settling the labelling;
  evenness of the three component states for the graded-tensor faithfulness step (true, and named
    here because the certificate does not name it).

SUBSTITUTED:
  NOTHING. I selected no route, entered no field, authored no functional, and adopted nothing.
  I did not relitigate the labelling branch, and I did not treat my own 930 as settling it.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 9. Flattening, custody, byte audit

- **S01-S37 FLATTENING CHECK:** walked. A source seed was not identified with a joint pairing — I
  verified the restriction returns the seed exactly. A faithful-looking functional was not identified
  with a faithful one — `tau_0` was checked against its sealed definition and distinguished from the
  `epsilon` beside it. A reference to a disclosed object was not identified with the disclosure (§7).
  A conditional certificate was not identified with a settled labelling. A true conclusion was not
  identified with a complete proof (§2.3).
- **F_PLDEC:** digests, span arithmetic and symbolic algebra only. No physical quantity evaluated; no
  measured constant consulted.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string and byte-span checks covered
  `tau_0`, `epsilon`, `E_ch`, `Phi_N`, `Delta_0^joint`, `q838_disclosure_sha256`, every schema name,
  and every entered route identifier.
- **BLIND:** held. No rank read, no dimension read, no ratio formed, no fiber compared or opened. The
  one rank-shaped token found at an endpoint was resolved as a field name bound to prose (§5).
- **PE-1..PE-14:** pointer-only, zero verdict weight. PE-14's witness status is recorded, not consulted.
- **BR-1:** my own 930 defined what the fixed-space certificate had to survive; it was not used to
  convict or exculpate. The principal's entry is authority for what was chosen, never for whether the
  build executed it correctly.
- **BUILDER-NEVER-VERIFIES:** I verified the opposite lane's build; I verified nothing of mine.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet, numerical
  evaluation of a physical quantity, or comparison with a measured constant was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 4464
PREDECLARATION_OUTPUT_SCAN = 0 hits
SPANS_RECOMPUTED = 14/14 ; PAYLOADS_CANONICAL = 18/18 ; OBJECT_DIGESTS = 17/17
PRICE_REGENERATED_FROM_INSTRUMENT = byte-identical (1463 B)
ROUND1_REHASHED = byte-untouched (both artifacts)
STOP_GRADE_FINDINGS = 1 ; NON_BLOCKING_FINDINGS = 1
```

Self verb audit: "confirmed" applies to the pairing's faithfulness, the four certificates, the Haar
equivalence, the pointers, the price and the manifest, each with its check displayed. "Verified" is
used only where I recomputed bytes or replayed algebra myself. "STOP-grade" is used once, against a
stated bar, with the reason and the one-key remedy named. I entered no field, selected no route, and
adjudicated no labelling branch. `VERB_AUDIT_SELF = CLEAN`.

## 10. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 4464; scan 0 hits)
PAIRING = CONFIRMED-FAITHFUL. The extension restricts at r=b=1 to the sealed HS seed EXACTLY. All three component functionals verified faithful at their sealed bytes rather than assumed: tau_R is the inductive-limit trace of the UHF record algebra, compatible with the sealed iota_NM; Tr_A is the seed's finite faithful source trace; and omega_H = tau_0,+ (x) tau_0,- where member 10's sealed text defines tau_0(U_lambda)=0 for lambda!=0, tau_0(1)=1 and names it the coefficient-at-identity/HAAR state. THE DISTINCTION THAT MATTERED: epsilon, evaluation at the trivial character and NOT faithful, sits in the same sealed span; had the build extended with it the certificate's premise would be false. It used tau_0. Minimal-tensor faithfulness then gives Tr_joint faithful, and faithfulness on the algebra restricts to Delta_0^joint, whose receiver is member 08's pinned space and not a substituted source space
PAYLOAD = REPRODUCED (18/18 payloads byte-exactly canonical under the declared law; 17/17 named object digests recomputed; pairing payload 908 B -> aaa3b217d945c7c7...; manifest 1557 B -> f4a6b127830c4d10...)
EJOINT_CERTS = 4/4 REPLAYED. E_ch is pinned to the sealed pinching rho -> P_0 rho P_0 + P_ch rho P_ch with P_0+P_ch=I_src, which I verified unital and idempotent, so the conditional-expectation certificate replays and Fix(E_ch (x) id (x) id) = Fix(E_ch) (x) R_inf (x) B follows. PRESSED HARDEST WHERE MY 930 SAYS THE RISK LIVES: the charge-diagonal fixed-space certificate states its conclusion CONDITIONALLY on the principal's declaration, pins declaration_source to the exact span [1274,1370), and carries the Q-838 disclosure — it computes the fixed space given the declaration and claims nothing about which labelling is correct. Correct shape; no unforced labelling claim hides there
HAAR_CERT = EXACT-CONFIRMED. omega_hist(b) = omega_H(b) for every b by exact algebraic cancellation: Tr_A(1) and tau_R(1) cancel and need not be 1, and only omega_H(1)=1 is used, which the pairing object declares. No approximation, no limit, no plausibility step — which is the standard this certificate must meet, since it is what supersedes a frozen principal act
ROUND1 = BYTE-UNTOUCHED-VERIFIED (both artifacts rehashed by me to f0179d43b4b2ed89... and 589c12f7cb76b5f4...; the disposition instance takes the SUPERSEDED_BY_NEW_ACT/H-HAAR arm with round1_bytes_mutated:false and ROUND2_ONLY authority, layering a new record rather than editing)
POINTERS = 4/4 (F-DOR013 label and target, IA-STATUS, PCH-TUPLE context and member all MATCH; S-TENSOR re-derived as a total unital injective *-homomorphism landing at A_C0 rather than the three wrong-codomain carriers). BLIND HELD: the one rank-shaped token at an endpoint resolves to the FIELD NAME a0_rank_fiber bound to prose, with identifier digits only and no rank value, dimension, or ratio
PRICE = BYTE-MATCHED (regenerated from member 05's own option tables, not from the build's table: 1463 bytes, 7f94eb5240890cb3..., with all eight individual draft digests matching)
MANIFEST = 18/18
VERDICT = REMAINS (one key). Seventeen of eighteen objects book on their mathematics and their bytes. THE ONE STOP-GRADE ITEM: the manifest is builder-authored, consumes E_joint by referencing joint_superselection_map_sha256 and ejoint_bundle_sha256, and does NOT carry q838_disclosure_sha256 — violating the build's own rule that every builder-authored E_joint consumer carries it, and the principal's own condition that the disclosure travels with every consumer. The cross-reference's identical omission IS excused and disclosed, since the instrument fixed that schema at nine keys with no room; the manifest has no such constraint. The build's own precedent decides it: the ejoint BUNDLE, an index object of the same class, DOES carry the disclosure. Answering that the manifest merely references a disclosed object is the flattening the build's own S01-S37 walk disclaims — a digest is not a certificate, and a reference is not the disclosure. This is STOP-grade because the manifest is what travels: a downstream consumer would carry an unforced and possibly-false labelling claim with no notice. FIX IS ONE KEY AND CONTAINED — add q838_disclosure_sha256 to the manifest; only the manifest digest moves, since no other payload references it, and everything else stands. ALSO RETURNED, NON-BLOCKING: the faithfulness proof invokes MINIMAL-tensor faithfulness while the carrier is the GRADED tensor, whose theorem needs the component states EVEN; all three are (two traces and a Haar state), so the warrant holds, but the hypothesis is unnamed — same shape as my 935 RL-06 finding, conclusion true and grounds one clause short
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
