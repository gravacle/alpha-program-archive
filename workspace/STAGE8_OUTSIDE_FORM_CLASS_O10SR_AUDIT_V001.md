# STAGE 8 — OUTSIDE AUDIT OF THE FORM-CLASS DETERMINATION — O10SR AUDIT V001

## AUDITOR — CODENAME OUTSIDE-AUDIT — COMMISSION O10SR — 2026-08-15

DEFAULT-REFUTE. Testimony given ZERO weight: every claim below was re-derived at
the record's own bytes or re-run in a fresh venv. DETERMINATION ONLY. This audit
**PROPOSES NOTHING**, **ADOPTS NOTHING**, **RECOMMENDS NOTHING**, and **AUTHORS
NO CERTIFICATE**. No value, no number, no measured-constant comparison.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `STAGE8_OUTSIDE_FORM_CLASS_O10SR_AUDIT_V001.md` and its
`.seal.sha256` sidecar — **both probed ABSENT** (`ls` exit 1) before any write.

TARGET SEAL, verified FROM THE ARTIFACT'S OWN DIRECTORY before any reliance:

```text
cd /Users/bgm/MB Work/alpha-program-archive/workspace
shasum -a 256 -c STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md.seal.sha256
  -> STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md: OK
0e34a5379c8de740cc0169857efc8be5d7cce0256ce53f31f41f677d30457125
```

---

## 0. VERDICT IN ONE LINE

```text
**CONFIRMED-WITH-CORRECTIONS.**

THE ARTIFACT'S THREE LOAD-BEARING FINDINGS ALL SURVIVE RE-DERIVATION AT BYTES:
  (1) the REFUTATION OF ITS OWN BRIEF at FP-2 — that the site's demand is NOT
      analytic — is CORRECT, verbatim, at W2 :131 and :136-137.  The artifact
      was handed "every certification type in the menu at these sites is
      ANALYTIC" as established context, tested it, and found it false at one of
      three sites.  I re-derived the same refutation independently.  It holds.
  (2) the FP-3 finding that the exclusion sits at the CONSUMPTION site (R.3's
      inequality between HS norms) and NOT at the admission rule F3-a — so that
      no act on F3-a would make a topological certificate posable — holds at
      E1 :811-813 and E1 :1137-1144, both verbatim.
  (3) the THREE-WAY ABSENCE — no record-native type at once strictly
      localizing, scale-free, and magnitude-carrying — holds.  Its central
      quotation is verbatim at bytes (though not at the file the artifact
      names: see K-1).

FIFTEEN CORRECTIONS ARE OWED.  NONE OVERTURNS A VERDICT.  Every one is a
  PROVENANCE defect, a COUNT defect, or a CHECK-STRENGTH defect:
  K-1  the artifact's single most load-bearing quotation is attributed to the
       WRONG FILE, six times, and again in the IMPORT AUDIT.  (B3-4) is R4
       :683-687, not 4B.  4B contains no "(B3-4)" and none of that text.
  K-2  the headline "FIVE OF THESE CARRY BOTH COMMISSIONED PROPERTIES" cites
       [X10d]; X10d's computed set has SIX members, EXCLUDES one of the five
       named, and INCLUDES the one the sentence explicitly excludes.
  K-3  N-5d's localization is stated FOUR different ways in one artifact.
  K-4  N-1's scale-freedom rests on "entries in {-1,0,+1}", which the cited
       definition does not state, and which X4a "verifies" on the artifact's
       own hand-built matrix rather than on the record's operator.
  K-5  the record's cycle space is ker(B_G^T); the artifact's prose says
       ker(B_G) throughout.
  K-6  the gloss "gamma_RL … i.e. an oriented 1-cycle carried of record" is
       NOT supported at the byte the artifact cites (W2 §4.2 says "named
       oriented object … surface anchor", not cycle).  I then found it
       CORROBORATED off-ground, in a file the artifact never opened.  So:
       citation defect, not a false claim.  I report the corroboration
       because DEFAULT-REFUTE is not licence to leave a true claim broken.
  K-7  X2a is HARDCODED, not derived; the claim that the shortfall triple is
       "re-derived independently here at X2a-X2c" is false for one of three.
  K-8  X2d's label asserts a proposition its code does not test, and which is
       FALSE under the very correction (K-4/RULES-A) the artifact consumes.
  K-9  X3c and X7e are vacuous: they compute M/M = 1 and (-1)^2 = 1.
  K-10 15 of 55 checks are bookkeeping presented as derivation; the honest-
       scoping paragraph declares only 5 of them.
  K-11 the FP-1 demand is cited "verbatim at its sealed source (WALL
       :408-413)" — WALL is NOT among the 12 seals the artifact verified, and
       at WALL's bytes :408-413 is the F2 LOCUS step, a DIFFERENT failure
       point.  Both bad pins are inherited verbatim from RULES.
  K-12 several quotes marked "verbatim" are re-cased for emphasis.
  K-13 SWEEP COMPLETENESS: uncapped over both roots, a U(1)-valued HOLONOMY
       IS defined on the record's own stage-N edge set and on a cycle lattice
       the corpus calls "a ratified sublattice" of "the already-ratified cycle
       carrier".  N-7's "HOLONOMY ... NOT DEFINED" survives ONLY under the
       artifact's declared §1.1 bound.  The inventory is INCOMPLETE at corpus
       scope.  *** IT DOES NOT BREAK THE FINDING: a U(1) holonomy is a phase
       of modulus 1 and carries NO magnitude, so the missed row OBEYS the
       artifact's pattern rather than violating it. ***
  K-14 §6.1 mislocates a clause within its own sentence ("third sentence").
  K-15 §4.4's "there is no inequality anywhere in it" is overstated against
       the CONTINUITY obligation the same artifact quotes — disclosed
       elsewhere, so overstatement and not concealment.

WHAT I HUNTED AND DID NOT FIND — reported as an absence, not manufactured:
  WISHFUL TYPING: NONE.  The artifact nowhere declares a record-native
    invariant able to serve where the demand cannot receive it.  At FP-2 it
    shows the DEMAND is itself record-native-shaped and then REFUSES to supply
    the object, the datum, the cell, or the member.  At FP-1 and FP-3 it
    denies service outright.  The one near-miss (N-5b) it classes
    ANALYTIC-DERIVED against its own convenience.
  UNPOSABLE-FROM-NOTATION: NONE.  At FP-1 a restatement IS available at bytes
    (W1-A's surviving singular-value-counting re-typing) and the artifact
    finds it, displays it, and SPLITS its verdict rather than suppressing it.
    That is the correct handling of the failure mode I was sent to find.
  PROPOSAL / ADOPTION / RECOMMENDATION / AUTHORED CERTIFICATE: NONE.  Every
    occurrence of those words in the artifact is a DISCLAIMER.  §6 displays
    which rule and which discipline would govern and then says "Anyone reading
    this artifact as a case for amending F3-a is reading it backwards."
  LENS TOKEN: NONE.  MEASURED CONSTANT: NONE.  Fences correctly declared false.

NOT REFUTED, carried in full: every negative here is ONE-SIDED.  I create no
impossibility, retire no witness, form no cell, supply no datum, and move no
fence.  FP-1 / FP-2 / FP-3 all STAND.
```

---

## 1. SEALS RE-VERIFIED INDEPENDENTLY; SWEEP CUTOFF DECLARED

Every seal below was re-run by me with `shasum -a 256 -c` **from the artifact's
own directory**, against its own `.seal.sha256` sidecar, before any reliance.
I did not consume the audited artifact's seal table as testimony.

```text
TARGET (1), OK:
0e34a537…  STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md              [TARGET]

THE ARTIFACT'S CLAIMED GROUND (12) — ALL 12 OK, AND ALL 12 PREFIXES MATCH THE
PREFIXES THE ARTIFACT PRINTS, character for character:
5da05d53  STAGE8_CERTIFICATION_RULES_O8SR_V001.md              [RULES]     OK
1e50c520  STAGE8_CERTIFICATION_RULES_O8SR_AUDIT_V001.md        [RULES-A]   OK
241262ff  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                   [W3]        OK
d05f22be  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md             [W3-A]      OK
806d5ec4  STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md                [W1]        OK
2dcf3167  STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md          [W1-A]      OK
eacd810c  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md              [W2]        OK
d95f4285  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md        [W2-A]      OK
a04c446a  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md               [ORIG]      OK
0db4de90  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md         [ORIG-A]    OK
ccb94dfa  STAGE8_TASK5_R4_KERNEL_..._LANE2_V001.md             [R4]        OK
70dde1c0  STAGE8_TASK4B_LOCAL_MAXWELL_..._LANE2_V001.md        [4B]        OK

FIVE FURTHER FILES THE ARTIFACT QUOTES "AT ITS OWN SEALED SOURCE" BUT DOES NOT
LIST AMONG ITS VERIFIED SEALS.  I verified them myself, from their own
directory, against the digests RULES pins.  ALL FIVE OK — so the artifact's
quotations rest on verified bytes even though the artifact did not say so:
46846730  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md          [E1]        OK
80db260f  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md            [WALL]      OK
61c6de58  STAGE8_AUTHORITY_CHAIN_PRECEDENCE_BINDING_V001.md    [AUTH]      OK
e6838ffc  STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001.md            [CERT]      OK
afc3a79c  STAGE8_COMPLETION_MAP_T17SR_V001.md                  [MAP]       OK
THIS IS THE CHAIN-OF-CUSTODY HALF OF K-11.  17 files verified, 0 mismatches,
0 unverifiable.
```

### 1.1 SWEEP CUTOFF — DECLARED

```text
SWEEP_CUTOFF = 2026-08-15, this session.
ROOTS, BOTH CONSUMED (the artifact consumed only the first):
  (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace
        1,889 .md files.
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
        2,308 .md files (7,805 files total).
THIS BOUNDS EVERY NEGATIVE BELOW.

SWEEPS RUN UNCAPPED OVER BOTH ROOTS, patterns declared verbatim, filename
census by `grep -rli --include='*.md'`, matched files opened ONLY when the hit
was a candidate record-native invariant's own defining line:
  holonom            R-a 409 files   R-b 380 files
  wilson loop        R-a  13         R-b  18
  loop cell/-cell    R-a   1         R-b   0
  winding number     R-a   5         R-b   5
  chern              R-a  12         R-b  10
  betti              R-a   4         R-b   3
  cohomolog          R-a   9         R-b   4
  homolog            R-a  22         R-b   8
  index theorem      R-a   6         R-b   8
  spectral flow      R-a   1         R-b   1
  euler characteristic / linking number / fredholm index — ZERO in both roots.
  gamma_RL           EVERY occurrence in both roots read (14 lines) — K-6.
  Hol definitional forms (`Hol...:=`) — every hit read — K-13.
EXCLUDED UNREAD, by standing rule: every register / tracker / road / plan /
  continuation file.  "Q-…" items are EXPECTED-UNLOCATABLE; none was chased.

THE SWEEP'S HONEST BOUND: the four zero-hit patterns are zero across BOTH
roots and are therefore corpus-wide absences, not scoped ones.  The high-count
patterns were resolved by reading definitional lines only; a record-native
invariant defined in prose without any of these tokens would not have been
seen.  K-13 is what the sweep DID surface.
```

---

## 2. THE CAS BATTERY, RE-RUN IN A FRESH VENV — AND WHAT IT ACTUALLY ESTABLISHES

### 2.1 Reproduction: EXACT

I extracted the script from the sealed artifact **programmatically**, between its
own fence lines (artifact :838 ```` ```python ```` and :1202 ```` ``` ````), so
that the bytes I ran are the bytes the artifact published — not a retyping.
Fresh venv `o10auditvenv`, `pip install sympy` → **1.14.0**, the version claimed.

```text
extracted lines: 364
run: ./o10auditvenv/bin/python o10_battery_replay.py
  -> checks run: 55 ; FAIL: 0 ; ALL PASS

DIFF of my captured stdout against the artifact's §7.2 "Output (verbatim)"
(artifact :1207-1306, 100 lines):
  *** OUTPUT IDENTICAL, BYTE FOR BYTE.  ZERO DIFFERENCES. ***
```

**REPRODUCTION: CONFIRMED.** The artifact did not fabricate its output, did not
edit it, and did not report a run it did not make. 55/55 PASS is real.

### 2.2 But 55/55 PASS is not 55 derivations — K-7 through K-10

The artifact's §7 preamble scopes its own honesty: *"Groups X1-X8 and X10 are
DERIVATIONS ... **Group X9 is BOOKKEEPING, not derivation**"*. I re-classified
every check by what its code actually touches. The scoping is **understated**.

```text
K-7  X2a IS HARDCODED, NOT DERIVED.  The loop reads:
       sh = simplify(adeg - req) if name != "FP-1" else Rational(3,2)
     so short["FP-1"] never touches the FP-1 row, and X2a then asserts
     short["FP-1"] == Rational(3,2), i.e. 3/2 == 3/2.
     FALSIFICATION PROBE, run: I replaced the FP-1 row with (999, -77/13) and
     with (0, 0).  X2a PASSES in every case.  X2b and X2c, by contrast, ARE
     genuinely computed (-1-(-3)=2 ; 0-(-2)=2) and they PASS honestly.
     CONSEQUENCE: the artifact's §1 claim that the shortfall triple "is
     re-derived independently here at X2a-X2c" is TRUE FOR TWO OF THREE.
     RULES-A's own re-derivation of the triple (audit B1-B4, PASS) is
     undisturbed and I confirmed its text at RULES-A :350-351.

K-8  X2d's LABEL ASSERTS WHAT ITS CODE DOES NOT TEST — AND THE LABEL IS FALSE.
     label: "EVERY asset degree is >= 0 (O6-8d) while EVERY requirement is < 0
             or is a strictly-smaller exponent"
     code : all(a >= -1 for _, a, _ in rows)          -> True
     the label's own proposition all(a >= 0)          -> FALSE
     because FP-2's asset degree is -1 — which is EXACTLY the value K-4
     supplies and which the artifact consumes as binding at §1 and §5.1.  The
     check passes only because the code tests a strictly weaker predicate than
     its label announces.  A mislabelled PASS in the load-bearing group.

K-9  TWO CHECKS ARE VACUOUS.
     X3c  code: limit(simplify(c0/M), r, 0, '+') == 1, with M = c0.
          c0/M is literally 1.  PROBE: substitute M := zzz, 7, or r^-5 — the
          check passes for every one, because it computes M/M.  Its label,
          "a degree-0 certificate majorizes only degree->=0 quantities", is
          not tested at all.  (X3a/X3b, the real majorant argument, ARE
          genuine: limit(r^-p/c0, r->0+) = oo.)
     X7e  code: Integer(-1)**2 == 1.  A fact about the integer -1.  Its label,
          "the reversal identity's value set is the SIGN GROUP {+1,-1}", is a
          claim about Phi and CE-2a and is not reached by the code.

K-10 BOOKKEEPING PRESENTED AS DERIVATION.  X8a-X8e read the hand-authored
     `inv` list; X10a-X10h read the hand-authored `varkind`, `assetdeg` and
     `native` dicts.  These are table lookups of the author's own §3/§5
     classification — precisely the thing X9 is (creditably) declared to be.
     TALLY, by what the code touches:
       genuinely symbolic/derived      : X1a-X1f, X2b, X2c, X2f, X2g, X3a,
                                         X3b, X4a-X4g, X5a-X5d, X6a-X6c,
                                         X7a-X7d, X8f, X10i          = 35
       hand-authored table lookups     : X2d, X2e, X8a-X8e, X9a-X9e,
                                         X10a-X10h                    = 20
       of those 20, DECLARED as such   : X9a-X9e                      =  5
       => 15 checks are presented as derivation while being bookkeeping.
     CH-8 takes credit for a disclosure the preamble under-scopes by 3x.

WHAT SURVIVES INTACT, and it is the substantive half: X4 (incidence, cycle
space, the finite Ward identity on two graphs, and X4g's non-vacuity), X5
(invariance of ker and of the identity under EVERY positive edge rescaling),
X6 (support separation), X7a-X7d (the four CE-2 identities on a generic
model), X1 (the log-log degree arithmetic), X2b/X2c/X2f/X2g and X3a/X3b.
These are real exact-symbolic computations and I re-ran every one.  NONE of
K-7..K-10 touches a verdict; all four inflate what the battery is said to
establish, and none inflates a finding.
```

---

## 3. DELIVERABLE (1) — THE TYPE MENU, RE-DERIVED AT BYTES: **CONFIRMED**

I demanded each site's demand at its own bytes and checked that the artifact's
type call matches the demand's form, not its vocabulary.

### 3.1 The FP-2 refutation — the artifact's best finding — HOLDS

```text
THE ARTIFACT REFUTES ITS OWN BRIEF at FP-2.  I tried to break that refutation
and could not.  At W2's own bytes, quoting the obstruction verbatim
(W2 :126-138, itself quoting fdf20bd4 §3.5):

  W2 :130-131   Phitilde_N(s)(delta_e)
                 :=incidence/integration/evaluation of s on the named k-cell e,
                                                                       (JD-3)
  W2 :133-134   Phitilde_N(s)(sum_e a_e delta_e)
                 :=sum_e a_e Phitilde_N(s)(delta_e).                  (JD-3a)
  W2 :136-137   "and must prove orientation covariance, presentation
                 independence, continuity, and `Ctc_N^sym subset
                 ker(Phitilde_N)`"

EVERY WORD THE ARTIFACT QUOTES IS THERE, EXACTLY.  The demand is a COCHAIN
EVALUATION ON A NAMED ORIENTED k-CELL.  Three of its four obligations
(orientation covariance, presentation independence, contact-kernel
containment) are structural; one (continuity) is analytic.  The brief handed
to the build — "every certification type in the menu at these sites is
ANALYTIC" — is therefore FALSE AT FP-2, and the build was right to say so
rather than consume it.  *** CONFIRMED. ***  This is the finding I would have
been sent to manufacture if it were not there, and it is there.

THE EVIDENCE FORM likewise CONFIRMS at bytes:
  W2 :101-107  "THE FROZEN SPEC, row R1, verbatim (9f0d12b4, §1 row R1)" —
               "…itself, as bytes, plus one displayed evaluation on a
               **named** oriented k-cell"
  W2 :396-398  "A declaration or commuting square in abstract carriers is not
               a surface `PASS`.  A `PASS` below requires the displayed map or
               equality on the named actual stage, cycle, arrow, or certified
               diamond."
Four structural nouns, no inequality in the evidence form.  CONFIRMED.

K-15, THE ONE OVERSTATEMENT.  §4.4 writes "THE SHAPE: (B) — AN EXHIBITION.
  There is no inequality anywhere in it."  The (JD-3) obligation set the same
  artifact quotes includes CONTINUITY, which is not exhibition-shaped.  The
  artifact discloses continuity as the one analytic obligation twice (§2.1's
  "THREE NO, ONE YES (continuity)"; §4.4's "three structural, one analytic"),
  so this is a sentence overstated against the artifact's own table, not a
  concealment.  CORRECTION, not a kill.
```

### 3.2 FP-1 and FP-3 menus — CONFIRMED, with a provenance kill

```text
FP-3, ALL FOUR TYPES CONFIRMED AT E1's OWN BYTES (E1 :1137-1144, verified at
digest 46846730):
  "C-L2  COMMUTATOR ERROR — … Control [h_0, M(t) ⊗ S] in a QUADRATIC-FORM /
   Besov-type norm.  Norm-based control is unavailable (||[h_0, 1_B]|| =
   +infinity for first-order h_0 against a sharp indicator), so the
   certification must be in a quadratic form and the artifact must state WHICH
   form and WHY the operator-norm route is excluded."
  => BESOV NAMED THEN DROPPED: CONFIRMED — named in the first sentence,
     absent from the disposition.  The artifact's reading is exact.
  => OPERATOR NORM NAMED AND EXCLUDED WITH ITS REASON: CONFIRMED verbatim.
  => QUADRATIC FORM IN FORCE: CONFIRMED.
  => HS AT THE CONSUMING DISPLAY: CONFIRMED at E1 :811-813 —
     "Derivation: `||A_s||_2 <= ||A(0)||_2 + ||Delta||_2 <= |C|_4^{beta} G_cm +
      |C|_4^{alpha} G_hs`".
  All four analytic.  The artifact's §2.2(a) "CONFIRMED AT FP-3, EXACTLY AND
  WITHOUT RESIDUE" is correct.

K-14, MINOR.  §6.1 calls "the certification must be in a quadratic form" the
  clause's "third sentence".  At bytes it sits inside the SECOND sentence
  (E1 :1139-1143).  §2.1's phrasing, "its THIRD clause", is accurate.  The two
  descriptions in one artifact disagree; the §2.1 one is right.

FP-1 — THE QUOTED TEXT IS GENUINE, THE PIN IS NOT.  *** K-11. ***
  THE ARTIFACT WRITES (§4.2, and again in §8):
    "THE DEMAND, verbatim at its sealed source (WALL :408-413):
     'Escape requires a SUB-VOLUME TRACE/HS RATE: verbatim MO-3, open of
      record.'"
  AT WALL's OWN BYTES (verified by me at digest 80db260f):
    :402-407 = F1 THE CONVERSION STEP, ending "…Escape requires a
               sub-volume trace/HS rate: verbatim MO-3, open of record."
    :408-413 = F2 THE LOCUS STEP — a DIFFERENT failure point entirely
               (boundary support, the ADOPTED R5 locus refinement, the
               |x-y|^{-3} integral at the volume diagonal).
  So the text is real and is at WALL :406-407; the cited range points at the
  wrong clause.  The SAME defect recurs at §2.1, which pins "The four tasked
  quantities are traces and HS masses" to WALL :400-407; at bytes that sentence
  is WALL :300-301.
  WHY THIS MATTERS AND WHAT IT PROVES: both bad pins are inherited VERBATIM
  from RULES (:172 "WALL :408-413, the F1 verdict"; :183 "(WALL :400-407)").
  The artifact therefore did NOT re-derive these citations at WALL's bytes —
  it copied a quoting artifact's pin while writing "verbatim at its sealed
  source".  That framing is not earned.  WALL is also absent from the
  artifact's own 12-file seal table (§1 above).
  MITIGATION, stated because it is true: I verified WALL myself at RULES'
  pinned digest, and every quoted WORD is exact.  The SUBSTANCE of FP-1's
  demand — that it is a RATE — stands.  PROVENANCE DEFECT, NOT A FALSE QUOTE.

K-12, THE CASE QUESTION.  WALL's bytes read "sub-volume trace/HS rate"
  (lower case); the artifact prints "SUB-VOLUME TRACE/HS RATE" under the word
  "verbatim".  The same re-casing is applied to R4's (B3-4) block and to W2's
  pass criterion.  Emphasis is the evident intent and no word is changed, but
  in this chain "verbatim" is a load-bearing word and re-casing should be
  marked.  CORRECTION.
```

---

## 4. DELIVERABLE (2) — THE RECORD-NATIVE INVENTORY: **CONFIRMED-WITH-CORRECTIONS**

This is where the commission told me to hunt hardest: an inventory item that is
not actually defined on the record's own cells or stages — an imported invariant
wearing native clothes. I checked **every** entry against **its own cited
definition**, not against its plausibility.

### 4.1 What verifies exactly

```text
N-3  THE COCHAIN PAIRING / FINITE WARD IDENTITY (B2-3).  *** EXACT. ***
     R4 :332-344, at digest ccb94dfa, verbatim and complete:
       "For a compactly supported scalar test function `varphi`,
          <partial J_c,varphi>
           :=<J_c,d varphi>
           =sum_e c_e[varphi(t(e))-varphi(s(e))]
           =<B_G^T c,varphi>
           =0.                                            (B2-3)
        Thus every cycle current is distributionally conserved and annihilates
        exact connection variations.  `(B2-3)` is the finite Ward identity
        derived directly from incidence; it uses no gauge representative."
     The artifact reproduces this correctly, cites R4 §2.1 correctly (§2.1 is
     "Conserved currents as distributions", R4 :310-357), and its X4d/X4f
     re-derivation of the identity on two graphs is genuine.  CONFIRMED.

N-4  THE ORIENTED-CELL EVALUATION AND CE-2a..CE-2d.  *** EXACT. ***
     (JD-3)/(JD-3a) at W2 :130-134; the four obligations at W2 :136-137; the
     four identities at W2 :379-382, all verbatim; CE-1 at W2 :359-360 —
       "= lambda_JD . integral_(gamma_(cell_N(e))) Pi_iR( flat_g( M_1( sect_N(
          [gr_N(s)] ) ) ) ).                                          (CE-1)"
     — reproduced by the artifact word for word, including all three
     dressings.  W2 §4.1/§4.2 are the correct section pins.  CONFIRMED.

N-6  chi_K.  *** EXACT. ***  4B :608 "chi_K:=p_loc[Rhat_K]" in §5.1; the
     dichotomy at 4B :695-696 verbatim; the fence at 4B :209 "Thus no live
     clause contains an equation with left-hand side `chi_K`"; and 4B :576
     "finite silence is not a kernel theorem".  All four confirmed, and the
     artifact's decision to inventory it and use it for nothing (CH-6) is
     consistent with 4B's own fencing.  CONFIRMED.

N-5a THE SEALED RANK CAP.  WALL :302 verbatim: "conversion from operator grade
     requires the rank factor: rank <= 4n^3 on ran C_n (of record)".
     CONFIRMED (the pin :302 is correct here — contrast K-11).

N-7's "degree" EQUIVOCATION FLAG.  *** CONFIRMED, AND CREDITABLE. ***  I
     grepped all 12 ground files for "mapping degree" and "winding": ZERO hits
     in every one.  The artifact's warning that "degree" throughout this ground
     is a SCALING EXPONENT and not a mapping degree is exactly right, and
     flagging it unprompted is good practice, not padding.
```

### 4.2 K-1 — THE CENTRAL QUOTATION IS ATTRIBUTED TO THE WRONG FILE

```text
THE ARTIFACT'S SINGLE MOST LOAD-BEARING QUOTATION — the one it calls "§5.3's
central quotation", the one it uses to close §8's one-sentence answer to the
commissioned question — is attributed to **4B (B3-4)** in SIX places
(artifact :79, :329, :688, :711, :1010, :1013) and once more in the IMPORT
AUDIT (§9, row "homogeneity of the Ward identity … RECORD-NATIVE (4B (B3-4))").

AT BYTES, IT IS NOT IN 4B.
  grep "(B3-4)"                       in 4B -> ZERO hits.
  grep "supplies no local coefficient norm"  in 4B -> ZERO hits.
  grep "cylindrical Ward identity is homogeneous" in 4B -> ZERO hits.

IT IS IN R4, at :680-687, under the heading "## 5. B3 — reader fiber after the
realized kernel", introduced by R4's own sentence "No normalization or
magnitude bound follows:" —

  p_loc(L_T)=1 does not identify L_T with Rhat_K;
  the cylindrical Ward identity is homogeneous;
  the memberwise Banach norm is reader-dependent;
  the finite kernel map is faithful but supplies no local coefficient norm.
                                                          (B3-4)

THE QUOTATION IS OTHERWISE EXACT, word for word, and R4 is one of the twelve
files the artifact DID verify — so the claim rests on verified bytes and the
finding it supports is undamaged.  What is damaged is the provenance call, and
it is damaged inside the IMPORT AUDIT, the section on whose honesty this
commission's whole value depends.  The tag "(B3-4)" is an R4 tag: R4 carries
(B2-1)…(B2-5), (B3-4), and the §5 B3 block.  The artifact read the right
bytes and wrote the wrong file name, seven times.
*** CORRECTION, NOT A KILL — but it is the correction I would most want a
registrar to see, because a reader who goes to 4B to check the artifact's
central quotation will not find it. ***
```

### 4.3 K-4, K-5, K-6 — three entries checked against their cited definitions

```text
K-4  N-1's SCALE-FREEDOM RESTS ON AN ATTRIBUTE ITS CITED SOURCE DOES NOT STATE.
  THE ARTIFACT: "N-1 INCIDENCE AND ORIENTATION DATA — the incidence operator
    B_G.  DEFINED AT: R4 §2.1 (B2-3) … SCALE-FREE: *** YES *** — entries lie
    in {-1, 0, +1}; no metric, no unit appears in its definition.  VERIFIED
    X4a."  §9 then classes "incidence operator B_G, s(e), t(e), entries
    {-1,0,1}" as RECORD-NATIVE (R4 §2.1).
  AT BYTES: B_G occurs in R4 exactly TWICE — :157 `K_G^fin=ker(B_G^T)` and
    :338 `=<B_G^T c,varphi>`.  R4 never defines B_G, never calls it "the
    incidence operator", and NOWHERE states its entries.  I searched both
    roots: no statement of B_G's entries exists.
  AND X4a DOES NOT REPAIR THIS.  X4a is
      chk("X4a  incidence entries lie in {-1,0,+1} …",
          all(x in (-1,0,1) for x in BA))
    where BA is the artifact's OWN hand-typed triangle matrix.  It verifies
    the model's entries, not the record's operator.  The check cannot fail.
  WHAT IS FAIR TO THE ARTIFACT: the coefficients ARE displayed at R4 :337 —
    `varphi(t(e))-varphi(s(e))` exhibits +1 on the target and -1 on the
    source — so {-1,0,+1} is a FORCED reading of the record's own formula.
    The claim is true and derivable.  It is not, however, record-STATED, and
    §9 books it as record-native definitional content with a section pin.
  VERDICT: the inventory row SURVIVES; the IMPORT AUDIT's call on this row is
    wrong.  It should read: derived from (B2-3)'s displayed coefficients, not
    quoted from a definition.

K-5  THE TRANSPOSE.  The record's finite cycle space is ker(B_G^T):
    R4 :157, "K_G^fin=ker(B_G^T)".  The artifact writes **ker(B_G)** in its
    N-2 header, in N-2's scale-free line ("dim ker(B_G) is fixed by
    rank-nullity alone"), in the §3.2 table, in the §5.2 table, and in §9.
    Its CAS models the RECORD's object correctly — the script's `BB` is the
    node x edge matrix, so `BB.nullspace()` is ker(B_G^T) — so only the prose
    is transposed.  A naming slip, consistently made.  CORRECTION.

K-6  gamma_RL AS "AN ORIENTED 1-CYCLE".  This is the entry I expected to be
    the imported invariant in native clothes, and the honest answer is more
    interesting than that.
  THE ARTIFACT (§3.1 N-2, and §0 :63): "The record's own loop-cell object is
    the reciprocal stage's oriented primitive: W2 §4.2, 'Reciprocal stage
    `RL`; primitive `gamma_RL`, reverse `-gamma_RL`; …' — a loop WITH its
    reverse, i.e. an oriented 1-cycle carried of record."
  AT THE CITED BYTE, W2 :371-373 says: "The record's own named oriented object
    is the reciprocal stage's SURFACE ANCHOR (fdf20bd4 §5.2, row 1,
    verbatim): …".  Not a cycle.  Not a loop.  Not closed.  W2-A :335 calls
    it "the record's named oriented PAIR".  I read every occurrence of
    gamma_RL in BOTH roots (14 lines): none calls it a cycle or a loop.
  SO THE GLOSS IS NOT AT ITS CITATION.  But DEFAULT-REFUTE is not licence to
    leave a true claim broken, so I kept looking, and the record DOES carry
    the loop reading — off the commissioned ground:
      STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V002.md :835
      "| reciprocal-LOOP stage `RL` | actual CYCLE-CARRYING Hodge/period
       stage | stage stock exists; physical Loc/seed subject unformed |"
    and R4 §1.4 is itself headed "Reciprocal-loop coordinate count", 4B §5.3
    "Reciprocal-loop rank-one stage".
  VERDICT: the artifact's gloss is CORRECT ABOUT THE RECORD and UNSUPPORTED
    AT THE BYTE IT CITES.  A citation defect, not wishful typing.  Note the
    shape of the luck: the artifact reached a true conclusion from a source
    that does not carry it, in a file it never opened.  That is the same
    mechanism as K-11 and K-1, and it is the artifact's characteristic defect.
```

### 4.4 K-13 — THE SWEEP, RE-RUN UNCAPPED OVER BOTH ROOTS

The commission is explicit that a missed native invariant voids the inventory's
completeness. I re-ran the record-native sweeps uncapped over both roots. **One
material item is missing from the inventory, and the artifact predicted it.**

```text
THE ARTIFACT'S CLAIM (§3.1, N-7): "HOLONOMY as a group-valued invariant of a
  closed loop: NOT DEFINED on the record's own cells within the commissioned
  ground.  What IS present is the loop-cell (N-2) and the incidence-derived
  Ward identity (N-3) — which R4 states 'uses NO GAUGE REPRESENTATIVE', i.e.
  it is precisely the gauge-independent SHADOW of a holonomy, not a holonomy."

AT COMMISSIONED-GROUND SCOPE THIS IS TRUE.  I confirmed it: `holonom` and
`Hol_` return ZERO hits in R4, in 4B, in W2, and in RULES.

AT CORPUS SCOPE IT IS NOT.  Uncapped, `holonom` hits 409 files in R-a and 380
in R-b.  Reading every definitional line, the record carries a genuine
group-valued holonomy ON ITS OWN STAGE-N EDGE SET AND CYCLE LATTICE:

  STAGE8_TASK5_XI_N_CANDIDATE_DARIO_V002.md :71   (present in BOTH roots)
    Hol_A(z) := product_(e in E_N) h_e(A)^(z_e)   on z in Lambda_N^cyc,
  STAGE8_TASK5_XI_N_REVIEW_AND_DISPLAYS_LANE3_V001.md :260
    Hol_A(z):=product_(e in E_N) h_e(A)^(z_e).                       (D1-4)
  and, elsewhere in the same family,
    Hol_(A_N)(z) := exp(i u_z(A_N)),                                   (D1)
    Hol_N := Hol_(A_N) compose Xi_N : D_N^Loc -> U(1),
    Hol_(gamma_j)(A) := exp( i theta_j(A) )        [F05, verbatim rule]

THE STAGE INDEX IS THE SAME `N` THAT CARRIES W2's Ctc_N^sym, Phitilde_N and
cell_N(e).  E_N is the stage's own edge set; Lambda_N^cyc is its cycle
lattice.  And the carrier is NOT a private construction — the same file says
at :270 that Lambda_N^cyc is a "sublattice of the ALREADY-RATIFIED cycle
carrier — not a new carrier.  The holonomy is **derived**", and its flag block
at :388 records "Lambda_N^cyc a ratified sublattice".

SO THE INVENTORY IS INCOMPLETE AT CORPUS SCOPE.  A U(1)-valued invariant of
the record's own cycles exists, is derived rather than authored, and sits on a
ratified carrier.  It is not in §3.

THREE THINGS MUST BE SAID AGAINST OVERREADING THIS, and I say them because
each is true:
 (1) THE ARTIFACT DECLARED THIS EXPOSURE IN ADVANCE, PRECISELY.  CH-7: "A
     holonomy or a winding number defined on record cells in an unopened file
     would not have been seen.  THIS IS THE LARGEST NEGATIVE EXPOSURE IN THE
     ARTIFACT."  §1.1 bounds every "NONE" and "ABSENT" to the commissioned
     ground and says they "must not be overread".  The artifact named the hole
     it left and I fell into exactly the hole it named.  That is disclosure
     working, not disclosure failing.
 (2) THE CONTAINING ARTIFACT IS NOT ADOPTED.  Its own status line reads
     "STATUS = DRAFT FOR RE-REVIEW.  NOTHING ADOPTED." (:381).  So this is a
     derived object on a ratified carrier inside an unadopted draft — a real
     inventory candidate, not a sealed record type.
 (3) *** IT DOES NOT BREAK THE ARTIFACT'S FINDING; IT OBEYS IT. ***  A U(1)
     holonomy is a phase: exp(i theta) has modulus 1.  It carries NO
     MAGNITUDE.  Adding it to §3.2 adds a row that is scale-free, localizing
     on its cycle support, and magnitude-free — i.e. another instance of the
     exact opposition the artifact reports, and NOT the missing
     localizing+scale-free+magnitude-carrying type.  The three-way absence
     survives the widened sweep.

VERDICT ON DELIVERABLE (2): CONFIRMED-WITH-CORRECTIONS.  The inventory's
ENTRIES verify at bytes (with K-1, K-4, K-5, K-6 owed on their citations);
its COMPLETENESS is bounded, the bound was declared, and widening the bound
adds a row that confirms rather than contradicts the finding.
```

---

## 5. DELIVERABLE (3) — THE POSABILITY TEST: **CONFIRMED**

The commission's second-hardest hunt: an UNPOSABLE verdict asserted merely
because the demand is written in analytic notation, where a restatement is
available at bytes. I went looking for that and **it is not there**.

```text
FP-1 — THE RESTATEMENT EXISTS AT BYTES, AND THE ARTIFACT FOUND IT ITSELF.
  W1-A :233, verbatim: "…sub-volume singular-value counting statement" —
  SURVIVES, and is correct".  A counting statement is NOT a norm inequality,
  so a restatement of FP-1's demand in non-norm vocabulary IS on the record.
  THE ARTIFACT DOES NOT SUPPRESS IT.  It surfaces it in three places (§2.1's
  menu row, §2.2(b), §4.2's "THE ONE NEAR MISS, displayed honestly"), and it
  SPLITS the verdict rather than returning one word: "UNPOSABLE for a
  topological certificate; posable-but-unmatched for a counting certificate."
  CH-5 books the split as an unforced choice taken to avoid concealing the
  near-miss.  THIS IS THE CORRECT HANDLING OF THE FAILURE MODE I WAS SENT TO
  FIND, and I record that the artifact performed it unprompted.
  I then tested the reason the counting route still fails, and it is a real
  reason, not a stipulation: the record's own scale-free counting invariant is
  the rank cap, whose degree is +3 in n (X1e, genuinely computed via the
  log-log derivative), i.e. it GROWS with the carrier; and X2f computes the
  rank x op ratio 4n^3·k / 2n^3 = 2k, n-free and never o(1).  Both re-run by
  me.  The counting SHAPE is posable; the record's counting CONTENT is on the
  wrong side of the exponent.  CONFIRMED.
  RESIDUAL: §0 :44 states "AT FP-1 and FP-3 — UNPOSABLE" without the split,
  while §0 :30-34, §4.2, §4.5 and the FLAG BLOCK all carry it.  One unsplit
  sentence against four split ones.  Noted, not charged.

FP-3 — UNPOSABLE, AND THE SHARPEST FINDING IN THE ARTIFACT, CONFIRMED.
  The consuming display is an inequality between HS norms (E1 :811-813,
  verified verbatim).  The admission criterion is an inequality between a
  form's absolute value and a product of two norms (W3 §3 from CERT §3.2).
  The deciding object's own noun is "a TRACE/HS-GRADE RATE" (W3 :701-703,
  verified verbatim: "(a) is NOT translation-invariant … (b) is n-FREE …").
  THE ARTIFACT'S DEEPEST CLAIM — that widening the ADMISSION rule F3-a would
  not make a topological certificate posable, because the unposability sits at
  CONSUMPTION, which F3-a does not govern — I tried to break and could not.
  F3-a's text (E1 :1137-1144) governs which certification TYPE is permitted.
  R.3's display (E1 :811-813) asks for an object X with ||A(0)||_2 <=
  |C|_4^beta · X.  These are different sites, and no act on the first reaches
  the second.  The artifact then explicitly forecloses the misreading:
  "Anyone reading this artifact as a case for amending F3-a is reading it
  backwards."  CONFIRMED, and this is a finding the rule-hunt structurally
  could not have produced.

FP-2 — POSABLE.  Confirmed in §3.1 above at W2's own bytes.  And the four
  blocks are confirmed to be non-type blocks: H-1 (datum unformed, "STOPPED at
  Joints A--D"), H-2 (COMMON_CELL_FORMED = false; R9-JII PENDING; CARRY-1
  "add no route, branch, address, or member selection"), H-3 (authored gauge),
  W-2b (MEMBER_FOUND = none).  Not one is a type exclusion.  I also confirmed
  RULES §3.4 (n-2) at RULES :471-472 verbatim: "EVERY FP-2 RULE IS CLASS (ii)
  — adopted, and adopted openly, each with its own displayed sealed act.
  FP-2 has no hidden convention."  So "no rule of record excludes a
  record-native certificate at FP-2" is correct at bytes.  CONFIRMED.

AND THE ARTIFACT DOES NOT THEN SUPPLY WHAT IT IS FORBIDDEN TO SUPPLY.  It
  forms no cell, names no member, supplies no datum, assigns no unit, and
  states CARRY-1 as binding on itself.  I checked: no cell is formed anywhere
  in the artifact, and R9-JII is left PENDING.  CONFIRMED.
```

---

## 6. DELIVERABLE (4) — THE MATCH TEST: **CONFIRMED-WITH-CORRECTIONS**

The three-way absence is right. The count attached to it is not.

### 6.1 K-2 — the headline "FIVE" is not what its own cited check computes

```text
THE SENTENCE (§5.3, and repeated in §0 :69-71 and §8):
  "*** YES — FIVE OF THEM DO. ***  N-1, N-3 and N-4 carry BOTH strict
   localization AND scale-freedom, fully and without qualification; N-2 and
   N-5d carry both with localization PARTIAL.  X10d."

WHAT X10d ACTUALLY COMPUTES.  I re-ran the artifact's own `native` dict:
  both = [k for k,v in native.items() if v[0] and v[1]]   ->  SIX members:
    incidence/orientation data
    cycle space ker(B) / finite Ward (B2-3)
    cochain-chain pairing on oriented cells
    contact-kernel membership (CE-2d)
    orientation sign / reversal (CE-2a)
    W2 per-cell EVALUATION value (CE-1)          <-- N-4'
  Is the Betti-type count (= N-5d) in it?  *** NO *** — its tuple is
    ("chain/cycle rank (Betti-type count)": (False, True, False, False)),
    i.e. loc_in_space = False.
  Is N-4' in it?  *** YES *** — and §5.3's sentence exists precisely to
    EXCLUDE N-4' from the five.

SO THE CITED CHECK: has a different CARDINALITY (6, not 5), EXCLUDES a member
the sentence includes (N-5d), and INCLUDES the member the sentence excludes
(N-4').  [X10d] does not support the sentence it is attached to.
```

### 6.2 K-3 — N-5d's localization is stated four different ways

```text
  §3.1, N-5 prose : "LOCAL: *** NO *** — every one of these is a count on a
                     whole carrier or a whole stage … None distinguishes a
                     region from its complement.  DISPLAYED AS AN ABSENCE."
  §3.2, table row : N-5d  LOCAL = PARTIAL
  §5.2, table row : N-5d  STRICT LOCALIZATION = PARTIAL
  CAS `native`    : loc_in_space = False
Two of the four say NO/False; two say PARTIAL.  §3.1's prose is the artifact's
own reasoned statement and the CAS agrees with it, so the defensible reading
is that N-5d does NOT strictly localize — which REMOVES it from the five.

AND N-2 IS INCONSISTENT THE OTHER WAY: §3.1 and §5.2 both say PARTIAL, the
CAS dict says True.

CONSEQUENCE FOR THE HEADLINE.  Under the artifact's own prose and its own CAS,
the members carrying strict localization AND scale-freedom AND no magnitude
are N-1, N-3, N-4 outright, with N-2 partial — **FOUR, not five** — and N-5d
drops out.  The artifact's affirmative finding is therefore slightly
overstated in its most quoted sentence.  IT IS STILL AFFIRMATIVE, AND THAT IS
WHAT MATTERS: the record is not devoid of the type; the count is four, not
zero.  CORRECTION, not a kill.

FOUR PARALLEL INVENTORIES, WHICH IS THE ROOT CAUSE.  The artifact carries the
same inventory four times — §3.2's 9-row table, §5.2's 9-row table, X8's
9-entry `inv` list, and X10's 10-entry `native` dict — with different
decompositions (X8 has no separate incidence row; X10 splits N-4 into CE-2a
and CE-2d; X10 splits "scale-free" into count-freedom and unit-freedom while
§5.2 keeps it as one column).  Nothing reconciles them, and K-2/K-3 are what
falls out of the gaps.
```

### 6.3 What the match test gets right — and it is the substance

```text
THE THREE-WAY ABSENCE HOLDS.  I re-derived each leg:
  - the scale-free localizing types supply no magnitude.  N-3's value on the
    exact class is IDENTICALLY ZERO — X4d/X4f, genuinely computed on two
    graphs with generic symbolic cochains, and re-run by me.  And the record
    states the consequence itself, at R4 :680-687 (NOT 4B — K-1), introduced
    by R4's own sentence "No normalization or magnitude bound follows:".
  - the ONE localizing type that carries a magnitude loses unit-freedom in
    the same line that supplies it.  W2's CE-1 (:359-360) displays
    lambda_JD, sect_N and flat_g together, and W2's own bytes call lambda_JD
    "EXPLICIT, SYMBOLIC, UNASSIGNED" with SK-10 anti-tuning standing and
    sect_N "an authored gauge … no canonical section is claimed".  CONFIRMED.
  - the two properties live in DIFFERENT VARIABLES, which is what dissolves
    the apparent contradiction.  W3 :701-703's clause (a) is spatial
    localization; clause (b) is n-freedom.  RULES-A's K-4 (:308, :339-348) is
    what supplies this, verbatim: "FP-1's variable is a CARRIER COUNT.
    Degree 0 in a carrier count is n-UNIFORMITY … CORRECTED COUNT:
    LOCALIZATION-blindness proper survives at FP-3 ONLY — ONE of three."
    The artifact consumes it as binding (CH-3) and records that doing so
    prevented it from reporting a false contradiction.  I confirm the
    consumption is faithful and that the correction is load-bearing.
  - AND THE STRENGTH IS CORRECTLY LOWERED.  §5.3 offers the
    scale-freedom/magnitude opposition as "an observed exceptionless pattern
    ACROSS THE COMMISSIONED GROUND, NOT as a theorem", and CH-9 books that
    choice.  Under K-13 the widened sweep adds a row that obeys the pattern,
    which is what a correctly-stated observation should do.  CONFIRMED.
```

---

## 7. DELIVERABLE (5) AND THE COMMISSION'S BRIGHT LINE: **CONFIRMED — CLEAN**

The commission is absolute here: any proposal, adoption, or recommendation
language, or any authored certificate, **voids the artifact**. I scanned for it
directly rather than trusting the artifact's disclaimers.

```text
SCAN RUN over the whole artifact for: "I propose|recommend|suggest|advise",
"should be amended|widened|changed|adopted", "we propose|recommend",
"recommend(ed|ation)", "proposal".
HITS: FOUR — and every one is a DISCLAIMER, not an act:
  :8     "does not propose, recommend, draft, or perform one"
  :1378  "NO RULE IS PROPOSED, ADOPTED, OR RECOMMENDED.  NO CERTIFICATE IS
          AUTHORED."
  :1557  "It is NOT a certificate; NOT a proposal; …"
  :1561  "NOT a recommendation of one."
ZERO instances of proposing, adopting, or recommending anything.

AUTHORED CERTIFICATE: NONE.  The artifact authors no certificate, and its one
  near-occasion — §3's quotation of W2's CE-1 evaluation FORM — is explicitly
  flagged in TOY_SEPARATION as "THE ONE PLACE A READER MIGHT MISTAKE DISPLAY
  FOR CONSTRUCTION", with the record's own disclaimer repeated: CE-1 is NOT
  the demanded evaluation.  I checked W2 :399-400 and it says exactly that.

§6 STAYS ON THE RIGHT SIDE OF THE LINE, and does so actively.  It names F3-a
  as the rule that would have to change for ADMISSIBILITY, then immediately
  displays that changing it would NOT achieve posability, and closes the door
  on being cited as a case for the change: "Anyone reading this artifact as a
  case for amending F3-a is reading it backwards."  §6.2 names the instrument
  class (a sealed amendment or principal decision entering E1's authority
  table by name and hash) and then states "THE ACT IS THE PRINCIPAL'S."  That
  is display of which discipline governs — exactly what was authorised — and
  it stops there.  I confirmed the AUTH binding quote is carried through
  RULES §6.4 rather than lifted, and the artifact says so.

INADMISSIBLE-BY-RULE = DOES NOT APPLY ANYWHERE.  I looked for a rule of record
  naming a non-analytic type and excluding it, independently.  I did not find
  one either.  The only exclusion of record is of the OPERATOR-NORM route — an
  analytic type — with its reason displayed (E1 :1140-1141).  The artifact
  reports this absence rather than manufacturing a rule to blame, and it is
  right to.  CONFIRMED.

LENS TOKEN: ZERO occurrences.  MEASURED CONSTANT: ZERO — the only "137" in the
  artifact is the line-number citation E1 :1137-1144.  FENCES: all three
  declared false, at :12 and again at :1601-1603.  NO VALUE IS COMPUTED, no
  program quantity is valued, no threshold satisfaction is claimed.  CLEAN.
```

---

## 8. HUNT 5 — THE IMPORT AUDIT ITSELF: **CONFIRMED-WITH-CORRECTIONS**

The commission says this commission's whole value depends on the native/imported
classification being honest. I checked every row of §9 against its cited origin.

```text
ROWS THAT ARE HONEST, AND SOME THAT ARE UNUSUALLY SO:
  cycle / cycle current J_c, ker(...)        RECORD-NATIVE — correct (R4 §2.1),
                                             modulo the transpose (K-5).
  coboundary d varphi, the pairing           RECORD-NATIVE — correct, R4 (B2-3).
  oriented k-cell, delta_e, chains,          RECORD-NATIVE — correct, W2
    orientation reversal                     (JD-3)/(JD-3a).
  contact kernel Ctc_N^sym                   RECORD-NATIVE — correct, W2 §1.
  rank, rank cap 4n^3                        RECORD-NATIVE — correct, WALL :302.
  chi_K detection dichotomy                  RECORD-NATIVE — correct, 4B §5.
  rank-nullity / dim ker                     IMPORTED (linear algebra) —
    declared even though it is the weakest import in the artifact.  Correct,
    and volunteering it is creditable.
  SCALING DEGREE deg_r f                     IMPORTED — ANALYTIC, and flagged
    as "*** THE MOST LOAD-BEARING IMPORT IN THE ARTIFACT ***".  Correct: X1's
    scaling_degree() is d/du log f(e^u), an analytic construction, and the
    whole match test runs in it.
  limit as r -> 0+                           IMPORTED — correct.
  norms, quadratic form, Besov                IMPORTED, used only to quote —
    correct: I checked, the artifact computes with none of them.
  singular values, threshold s                IMPORTED — correct, and this is
    the classification that costs the artifact its nearest FP-1 candidate.
  Riesz inverse, flat_g, Schwartz kernel      IMPORTED — ANALYTIC/METRIC,
    correct.
  TOTAL ORDER on the reals, "<="              IMPORTED — ANALYTIC, and the
    artifact says the posability claim "is a claim about an ANALYTIC relation,
    made from outside it."  Correct and unusually candid.
  lambda_JD                                   RECORD-NATIVE AS A SYMBOL, with
    nothing assigned.  Correct — I verified no value is assigned anywhere.

TWO ROWS ARE WRONG, AND BOTH MATTER:
  *** (i) "homogeneity of the Ward identity — RECORD-NATIVE — (4B (B3-4))". ***
      The ORIGIN CALL (record-native) is right; the SOURCE is wrong.  It is
      R4 :683-687.  4B contains neither the tag nor the text.  K-1.
  *** (ii) "incidence operator B_G, s(e), t(e), ENTRIES {-1,0,1} —
      RECORD-NATIVE (R4 §2.1)". ***  s(e) and t(e) are record-native at that
      pin.  "ENTRIES {-1,0,1}" is NOT: R4 states no entries for B_G anywhere,
      and never defines B_G at all.  The attribute is DERIVABLE from the
      coefficients displayed in (B2-3), but it is derived, not native-stated,
      and the row books it as native definitional content.  K-4.

THE AUDIT'S SELF-INDICTMENT IS CORRECT AND SHOULD BE PRESERVED.  §9 closes:
  "the sentence 'no record-native type has the needed properties' is itself
   stated in imported terms.  I do not know how to state it otherwise and I do
   not pretend to … asking whether a topological certificate can serve is
   already asking it to be measured on an analytic scale."
  I tested this and it is true: the match test's properties ("degree", "rate",
  "n-free") are analytic properties, and X1/X2/X10's arithmetic is analytic.
  A test for them cannot be purely record-native.  Displaying that rather than
  burying it is the strongest single act of the artifact's import audit, and I
  confirm it stands.

VERDICT ON §9: CONFIRMED-WITH-CORRECTIONS — 13 rows honest (one of them
against the artifact's own interest), 2 rows wrong, and the section's central
self-criticism sound.
```

---

## 9. VERDICT, PER DELIVERABLE AND OVERALL

```text
DELIVERABLE                                            VERDICT
──────────────────────────────────────────────────────────────────────────────
(1) THE TYPE MENU                    CONFIRMED-WITH-CORRECTIONS
    Every type verified at its own bytes.  The refutation of the artifact's
    own brief at FP-2 is CORRECT and I re-derived it independently.
    Corrections owed: K-11 (WALL pins wrong and inherited; WALL not in the
    artifact's seal table), K-12 (re-casing under "verbatim"), K-14, K-15.

(2) THE RECORD-NATIVE INVENTORY      CONFIRMED-WITH-CORRECTIONS
    Entries verify at bytes.  Corrections owed: K-1 (central quotation
    attributed to 4B; it is R4), K-4 ({-1,0,+1} not at its cited definition;
    X4a verifies the model not the record), K-5 (ker(B_G) vs ker(B_G^T)),
    K-6 (the 1-cycle gloss unsupported at its citation, corroborated
    off-ground), K-13 (incomplete at corpus scope — a U(1) holonomy on a
    ratified cycle lattice is missing, and it OBEYS the finding).

(3) THE POSABILITY TEST              *** CONFIRMED ***
    No correction owed.  The FP-1 restatement available at bytes is found,
    displayed, and used to SPLIT the verdict rather than suppressed — the
    exact opposite of the failure mode I was commissioned to hunt.  The FP-3
    consumption-vs-admission finding holds and is the artifact's deepest.

(4) THE MATCH TEST                   CONFIRMED-WITH-CORRECTIONS
    The three-way absence holds and is correctly stated at the lower strength
    (observed pattern, not theorem).  Corrections owed: K-2 (the headline
    "FIVE" is not what [X10d] computes — six members, different membership),
    K-3 (N-5d stated four ways; under the artifact's own prose and CAS the
    count is FOUR).  The affirmative finding survives: the record is not
    devoid of the type.

(5) DISPLAY ONLY — WHICH RULE        *** CONFIRMED ***
    No proposal, no adoption, no recommendation, no authored certificate.
    Bright line held, and actively defended against misreading.

IMPORT AUDIT (§9)                    CONFIRMED-WITH-CORRECTIONS
    13 rows honest, 2 wrong (K-1, K-4), self-indictment sound.

CAS BATTERY                          CONFIRMED-WITH-CORRECTIONS
    Reproduces BYTE-FOR-BYTE in a fresh venv, 55/55 PASS, sympy 1.14.0.
    Corrections owed: K-7 (X2a hardcoded), K-8 (label asserts what code does
    not test, and the label is false), K-9 (X3c, X7e vacuous), K-10 (15 of
    55 are bookkeeping presented as derivation).

──────────────────────────────────────────────────────────────────────────────
OVERALL:                     *** CONFIRMED-WITH-CORRECTIONS ***
──────────────────────────────────────────────────────────────────────────────
NOT ONE OF THE FIFTEEN CORRECTIONS OVERTURNS A VERDICT.  Every one is a
provenance defect, a count defect, or a check-strength defect.  The artifact's
three load-bearing findings — the FP-2 refutation of its own brief, the FP-3
consumption-vs-admission exclusion, and the three-way absence — each survived
an adversarial re-derivation at bytes conducted with testimony set to zero.

THE ARTIFACT'S CHARACTERISTIC DEFECT, named once so it can be looked for
again: it reaches TRUE conclusions from citations that do not carry them.
K-1, K-6 and K-11 are the same failure three times — the right bytes exist,
the artifact knows what they say, and the pin points somewhere else (a
different file, a different clause, a different document).  Twice the bad pin
is inherited verbatim from a quoting artifact rather than re-derived at the
source, which is precisely what "quote at its own sealed source" is meant to
prevent.  Nothing here is fabrication: every quoted WORD I checked was real,
and I checked all of them.

STANDING, UNCHANGED BY THIS AUDIT:
  FP-1 / FP-2 / FP-3: ALL STAND.  EVERY WITNESS STANDS.  Nothing retired,
  nothing adopted, nothing authored, nothing proposed.  COMPLETION MAP
  UNTOUCHED.  R9-JII PENDING.  COMMON_CELL_FORMED = false.  No fence moved.
  No impossibility created or extended.  All consuming quantities UNDECIDED.
```

---

## 10. IMPORT AUDIT — THIS AUDIT'S OWN NOTIONS, CLASSIFIED

As commissioned, for every notion **I** use: record-native or imported.

```text
NOTION                          ORIGIN            USED FOR
──────────────────────────────────────────────────────────────────────────────
incidence data s(e), t(e);      RECORD-NATIVE     §4.1, §4.3
  the pairing <J_c, d varphi>   (R4 §2.1, B2-3)
oriented k-cell, delta_e,       RECORD-NATIVE     §3.1, §4.1
  (JD-3)/(JD-3a), CE-2a..d      (W2 :126-138, :379-382)
contact kernel Ctc_N^sym        RECORD-NATIVE     §4.1
rank cap 4n^3                   RECORD-NATIVE     §5 (WALL :302)
chi_K dichotomy                 RECORD-NATIVE     §4.1 (4B §5)
homogeneity of the Ward         RECORD-NATIVE     §4.2, §6.3
  identity                      (R4 (B3-4) :683-687 — NOT 4B)
Lambda_N^cyc, E_N, Hol_A        RECORD-NATIVE     §4.4 (K-13), on a carrier
                                                  the corpus calls ratified
──────────────────────────────────────────────────────────────────────────────
SHA-256 / seal verification     IMPORTED — CRYPTOGRAPHIC.  The only tool I use
                                to establish that bytes are the bytes.  It
                                needs no mathematics of the record.
exact symbolic algebra          IMPORTED — CAS (sympy 1.14.0).  Used ONLY to
  (sympy)                       re-run the artifact's own battery and to run
                                falsification probes on four of its checks.
                                I introduce no new derivation of my own.
scaling degree / limits         IMPORTED — ANALYTIC.  Present only because the
                                artifact's battery uses them and I re-ran it.
                                I inherit this import; I do not add to it.
norms, HS grade, quadratic      IMPORTED — ANALYTIC.  Used ONLY to quote the
  form, Besov                   sites' demands.  I compute with none of them.
U(1), "phase of modulus 1"      IMPORTED — GROUP THEORY.  Used at K-13 to say
                                that a U(1)-valued holonomy carries no
                                magnitude.  DECLARED: this is the one place I
                                reason about a record object with a notion the
                                record does not supply at that point, and the
                                conclusion it supports is one that WEAKENS my
                                own finding (it is why K-13 does not overturn
                                the artifact).  Stated so it can be discounted.
"cardinality", set membership   IMPORTED — ELEMENTARY.  Used at K-2/K-3 to
                                count the artifact's own dict.
──────────────────────────────────────────────────────────────────────────────
THIS AUDIT'S OWN FINDING ABOUT ITSELF: my kills are almost entirely
BIBLIOGRAPHIC — they are claims about where text is, which is settled by
grep and shasum and needs no mathematics.  That is why I can state K-1, K-11,
K-4, K-5 and K-6 at full strength: they are decidable at bytes.  K-2 and K-3
are arithmetic on the artifact's own data structures, equally decidable.  The
ONE finding of mine that requires judgement rather than lookup is K-13's third
clause — that a U(1) holonomy carries no magnitude — and it is the clause that
argues AGAINST my own finding's importance.  I have no kill that rests on my
opinion of the physics, and I claim none.
```

---

## 11. CHOICE LEDGER (commission O10SR-AUDIT; every unforced choice)

```text
AC-1  VERIFYING THE FIVE FILES THE ARTIFACT QUOTES BUT DID NOT SEAL (E1, WALL,
      AUTH, CERT, MAP).  UNFORCED.  I could have recorded "quotes rest on
      unverified sources" and stopped, which would have made K-11 a much
      heavier kill.  CHOSEN: verify them myself at the digests RULES pins.
      All five OK.  CONSEQUENCE, against my own interest: K-11 downgrades from
      "unverified reliance" to "provenance framing not earned", because the
      bytes are in fact sound.  Taken because DEFAULT-REFUTE is a standard of
      proof, not a target.

AC-2  EXTRACTING THE CAS SCRIPT PROGRAMMATICALLY RATHER THAN RETYPING IT.
      FORCED in substance: retyping would test my transcription, not the
      artifact.  I pulled artifact lines 839-1202 between its own fences, so
      the bytes I ran are the bytes it published.  Recorded because the
      byte-for-byte output match is only meaningful under this method.

AC-3  RUNNING FALSIFICATION PROBES RATHER THAN READING THE CHECKS.  UNFORCED.
      For X2a, X3c, X7e I substituted deliberately false inputs and confirmed
      the checks still PASS.  A check that cannot fail is not evidence, and
      this is the only way to show it.  K-7 and K-9 rest on these probes.

AC-4  PURSUING K-6 PAST THE CITATION.  UNFORCED, AND IT COST ME A KILL.  Once
      I established that W2 §4.2 does not call gamma_RL a cycle, I had a clean
      "imported invariant in native clothes" finding — the exact thing the
      commission sent me to hunt.  CHOSEN: keep searching for corroboration,
      and I found it off-ground (EQ6_MEMBERSHIP_THEOREM :835, "reciprocal-loop
      stage RL | actual cycle-carrying Hodge/period stage").  K-6 downgraded
      from a kill to a citation defect.  Displayed because a hunt that only
      accumulates is not an audit.

AC-5  CONSUMING BOTH ROOTS WHEN THE ARTIFACT CONSUMED ONE.  FORCED by the
      commission ("uncapped over both roots").  CONSEQUENCE: K-13 exists only
      because of it, and K-13 is the only finding that touches completeness.

AC-6  STATING K-13's THIRD CLAUSE (that the missed holonomy carries no
      magnitude and therefore confirms rather than breaks the finding).
      UNFORCED — omitting it would have made K-13 look like a refutation of
      the artifact's central negative.  CHOSEN: state it.  It is true, and it
      is the difference between CONFIRMED-WITH-CORRECTIONS and REFUTED.

AC-7  NOT CHASING THE 409/380-FILE `holonom` CENSUS TO EXHAUSTION.  I read
      definitional lines (`Hol...:=`) across both roots and stopped there.
      CONSEQUENCE, stated as a bound on my own negatives: a record-native
      invariant defined in prose without any swept token would not have been
      seen by me either.  My completeness claim is no stronger than my
      patterns, and the patterns are printed at §1.1.

AC-8  DECLINING TO RE-ADJUDICATE chi_K, the S2b type ambiguity, or F3-a's
      provenance class.  FORCED: all three are fenced or owned elsewhere of
      record (4B :209/:772; CERT's ledger "carried, not repaired … the spec
      author/registrar owns it"; RULES-A K-1).  I verified the artifact's
      handling of each and resolved none.

AC-9  COUNTING THE "FIVE" AGAINST THE ARTIFACT'S OWN DICT RATHER THAN AGAINST
      MY READING OF THE RECORD.  UNFORCED.  K-2 is stated as an INTERNAL
      inconsistency — the sentence versus the check it cites — because that
      is decidable and needs no view of mine about which types localize.
```

---

## 12. TOY_SEPARATION

```text
WHAT THIS AUDIT IS.  A re-derivation at bytes of another artifact's claims,
  plus a fresh-venv re-run of its symbolic battery, plus an uncapped sweep of
  two corpus roots.  Its objects are ACTUAL SURFACE: 17 files verified by
  SHA-256 from their own directories; every quotation checked character by
  character against the file and line it is pinned to; the CAS script executed
  as extracted bytes, not as a paraphrase.

WHERE I USED A PROBE RATHER THAN THE RECORD, AND WHY THAT IS NOT A TOY.  My
  falsification probes (AC-3) substitute false inputs into the artifact's own
  code to show a check cannot fail.  They model nothing about the record and
  claim nothing about it.  They are statements about the artifact's Python.

WHAT THIS AUDIT IS NOT.  It is NOT a certificate; NOT a proposal; NOT a
  recommendation; NOT a rule change or a case for one; NOT a resolution of
  chi_K, of the S2b type ambiguity, or of F3-a's provenance; NOT a
  construction of any object; NOT an evaluation on any cell; NOT a supply of
  any datum, member, or identification.  It forms no cell, assigns no unit,
  retires no witness, and moves no fence.  It does not adopt the artifact and
  it does not adopt any correction — the corrections are DISPLAYED as owed;
  whether and how they are made is not this audit's act.

THE ONE PLACE A READER MIGHT MISTAKE THIS AUDIT FOR AN AUTHORITY.  K-2 and
  K-3 identify what the defensible count is (FOUR, not five) under the
  artifact's own prose and its own CAS.  That is a DISPLAY of an internal
  inconsistency and of which branch the artifact's own materials support.  It
  is not a re-adjudication of the inventory and it does not re-classify any
  type.  No row of §3 is edited, added, or removed by me.

NO ALPHA-ADJACENT CONTENT.  No value, no number as the value of any program
  quantity, no measured-constant comparison, no threshold satisfaction, no
  gate touched.  Every number in this audit is a line number, a file count, a
  check tally, a hash prefix, an exponent quoted from the record, or a
  cardinality of a set in someone's Python dict.
```

---

## 13. FLAG BLOCK — STAGE8_OUTSIDE_FORM_CLASS_O10SR_AUDIT_V001

```text
COMMISSION                   = O10SR OUTSIDE-AUDIT, 2026-08-15
STANCE                       = DEFAULT-REFUTE; testimony zero weight
TARGET                       = STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md
TARGET_SEAL                  = OK (verified from the artifact's own directory)
VERDICT_OVERALL              = *** CONFIRMED-WITH-CORRECTIONS ***
  DELIVERABLE (1) TYPE MENU  = CONFIRMED-WITH-CORRECTIONS
  DELIVERABLE (2) INVENTORY  = CONFIRMED-WITH-CORRECTIONS
  DELIVERABLE (3) POSABILITY = CONFIRMED
  DELIVERABLE (4) MATCH TEST = CONFIRMED-WITH-CORRECTIONS
  DELIVERABLE (5) RULE/DISC. = CONFIRMED
  IMPORT AUDIT (§9)          = CONFIRMED-WITH-CORRECTIONS (13 honest, 2 wrong)
  CAS BATTERY                = CONFIRMED-WITH-CORRECTIONS (reproduces exactly)
CORRECTIONS_OWED             = 15 (K-1 … K-15); NONE overturns a verdict
HEAVIEST_CORRECTION          = K-1 (central quotation attributed to 4B; it is
                               R4 :683-687 — 6 occurrences + the import audit)
WISHFUL_TYPING_FOUND         = NONE
UNPOSABLE_FROM_NOTATION      = NONE (FP-1's restatement is found and split on)
PROPOSAL/ADOPT/RECOMMEND     = NONE (all 4 hits are disclaimers)
CERTIFICATE_AUTHORED         = NONE
LENS_TOKEN                   = NONE
MEASURED_CONSTANT            = NONE
SEALS_VERIFIED               = 17/17 OK, each from its own directory
                               (1 target + the artifact's 12 + 5 it quoted
                               but did not seal)
CAS_REPLAY                   = 55/55 PASS, sympy 1.14.0, fresh venv
                               o10auditvenv; stdout IDENTICAL to §7.2 byte
                               for byte; 15/55 re-classified as bookkeeping
SWEEP_CUTOFF                 = 2026-08-15; BOTH roots, uncapped
                               R-a 1,889 .md ; R-b 2,308 .md
                               register/tracker/road/plan/continuation files
                               EXCLUDED UNREAD; "Q-…" EXPECTED-UNLOCATABLE
INVENTORY_COMPLETENESS       = INCOMPLETE at corpus scope (K-13: a U(1)
                               holonomy on a ratified cycle lattice), BOUND
                               DECLARED IN ADVANCE by the artifact (CH-7),
                               and the missed row OBEYS the finding
LARGEST_EXPOSURE_OF_MY_OWN   = AC-7: the 409/380-file holonomy census was
                               resolved at definitional lines only
alpha_computed               = false
proof_authorized             = false
kappa_record_computed        = false
COMMON_CELL_FORMED           = false
R9_JII                       = PENDING (untouched)
FENCES_MOVED                 = none
RULES_PROPOSED               = none
RULES_ADOPTED                = none
CERTIFICATES_AUTHORED        = none
WITNESSES_CREATED            = none
WITNESSES_RETIRED            = none
COMPLETION_MAP               = UNTOUCHED
FP-1 / FP-2 / FP-3           = ALL STAND
```
