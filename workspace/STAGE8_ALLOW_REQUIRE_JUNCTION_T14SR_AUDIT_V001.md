# STAGE 8 — ALLOW/REQUIRE JUNCTION (T14SR) — INDEPENDENT AUDIT V001

DATE: 2026-08-15
COMMISSION: JUNCTION-AUDIT (T14SR). POSTURE: DEFAULT-REFUTE; testimony zero weight;
every claim re-derived at bytes. DETERMINATION ONLY — no adoption, no authored physics,
no value, no number.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: this file — ABSENT at start of commission (ls: "No such file or
directory", probed before any write).

## §0 — STEP 0: THE CLAIMED ARTIFACT

```text
CLAIMED: STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md  (44964 bytes, 2026-08-15)
SEAL:    e1c2ac80db09eafdeb21500e277aaa070b9506994065da1c6b15c74bde2e25c1
shasum -a 256 -c => OK. Audit proceeds.
```

## §0.1 — GROUND SEALS, INDEPENDENTLY RE-VERIFIED (this commission's own shasum runs)

```text
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md
OK  STAGE8_IDEAL_EXTENT_S9AD_V001.md
OK  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md
OK  STAGE8_5D_REREAD_T13SR_V001.md
OK  STAGE8_5D_REREAD_T13SR_AUDIT_V001.md
OK  STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001.md
OK  STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001.md
OK  DEMAND_READING_DECISION_OF_RECORD_V001.md
OK  STAGE8_ALLOW_REQUIRE_MINIMALITY_SPLIT_CORRECTION_V001.md
OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md
11/11 MATCH; MISMATCHES = 0. (Same set the build names in its §0.)
```

(Audit body follows; written incrementally as each ground artifact is consumed at bytes.)

## §1 — BUILD §1 (THE DEMAND-READING) RE-CONSUMED AT ITS OWN BYTES

`STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md`, full read this commission:

```text
LINES 24-25 verbatim MATCH: "THE FIRST SENTENCE IS RIGHT. THE SECOND IS FALSE:
  *** A REQUIRE-SHAPED CONDITION WITH DERIVED EXISTENCE AND UNIQUENESS FORCES
  A NUMBER. ***"
LINES 54-57 MATCH in content: ONE criterion (admissible set a point), THREE
  shapes (single require w/ unique root; exact joint determination n/n;
  overdetermined). ONE TRANSCRIPTION VARIANCE: source has "two ways" in double
  quotes; build renders 'two ways' (nesting substitution). Content identical.
FOUR-CASE MAP (its SS3, lines 134-148): build's 4-line compression faithful
  (NO / WITHIN THE CLASS / YES via SS2 certificates / YES with structural
  evidence). "grade rides on its weakest leg" at lines 146-148 MATCH.
STATUS QUOTE (lines 6-7) verbatim MATCH; lane-adjudication status displayed,
  not upgraded, in the build — CORRECT: the build's SS1 says so in terms.
"strongest anti-fit grade" (build SHAPE-3 gloss) sourced at its line 33.
FINDING NONE at REFUTE grade for build SS1.
```

## §2 — AJ PAIR RE-CONSUMED (build rows R-1, R-2, A-1; sites J-1, J-6; SS5.1 rigidity)

`STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md` (693 lines, full read):

```text
R-1 SOURCES: SS5 route table "integer-quantized by single-valuedness (CAS
  P3a/P3b: fractional winding leaves an exact residual -2*...)" — build quote
  DROPS the trailing "*..." without ellipsis mark. MINOR QUOTE-FIDELITY
  VARIANCE (V-1). The build's own restatement "residual -2 at n = 1/2" is
  exactly P3b at bytes (exp(2*pi*i/2) - 1 = -2). Content sound.
  "an integer label, not a continuum response magnitude" — P3b output MATCH.
R-2 SOURCES: P1 (same g^n both characters) MATCH SS6.1; N4 chi_+1 chi_-1 = 1
  MATCH; N1-N3 unit modulus MATCH SS4; T1 "forces G == 1 uniquely" MATCH.
  G = 1 displayed as forced value: PROVENANCE = sealed AJ T1 bytes (a
  triviality corollary of the sealed ground, not a new number). PASS under
  the no-number fence as sealed-display; noted N-1 in the number audit SS8.
A-1 SOURCES: f1 = (2 - w - conj w)/4 = sin^2(Delta/2) (M1), f2 = f1^2, exact
  separation 1/4 at toy argument (M3), M8/M9 conservativity MATCH at bytes.
  "two models of every displayed clause exist that differ on the junction" —
  verbatim in AJ SS0 verdict. Act-5/NP-7 quotes belong to 5d (checked SS3).
J-6 TERMINATION QUOTES: "no clause in which a response magnitude and a
  phase-sector variable co-occur" (AJ SS6.2 ground fact) MATCH; Gleason kill
  "fibers of dimension 1 ... below the forcing threshold" — AJ SS6.3.2 at
  bytes: "fixed line bundle - fibers of dimension 1" + "The ground's own
  carrier is below the forcing threshold." MATCH (build splices with
  ellipsis, faithful).
SS5.1 RIGIDITY GROUND: Theorem II proof line "Derivability from the ground
  implies truth in every model of the ground" verbatim MATCH (AJ SS6.2);
  scope note "quantifies over ALL exit operations and ALL profiles at once"
  MATCH; residue displayed in build = AJ SS8.2 EXACTLY (C12 carries
  withheld-content + refinement-stage components; conservativity core rests
  only on displayed equation set; finite-text warrant). NO SCOPE INFLATION
  in the build's restatement of the AJ residue.
AJ QUOTE "the magnitude map is ALLOW-SIDE BY THEOREM ... provenance class
  UNINHABITED": NOT in AJ build bytes — source must be the AJ AUDIT
  (checked next).
OPEN THREADS AT THIS POINT: (T-a) build SS0 sentence promises wall-pair
  seal listings "at its point of use below" — none found in build body yet;
  (T-b) A-2 "twice-determined (T1SR)" needs a byte source inside sealed
  ground (build's SS0 list has no T1SR artifact).
```

## §3 — AJ AUDIT + 5D PAIR RE-CONSUMED (rows R-4, R-7..R-10, A-3..A-5, A-9..A-11, A-13; J-4, J-5, J-7)

`STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md` (302 lines, full read):

```text
BUILD SS2 HEADER "AJ pair CONFIRMED-WITH-CORRECTIONS (VACANCY-CLOSED stands,
  convergent verdicts by independent routes)": AJ-audit SS0 verdict line +
  independence block at bytes — MATCH. Corrections C-1/C-2/C-3 all declared
  "none load-bearing" at bytes — build's carriage at verdict-line grade is
  faithful.
```

`STAGE8_5D_REREAD_T13SR_V001.md` (616 lines, full read):

```text
"ALLOW-SIDE BY THEOREM ... lawful provenance class UNINHABITED" — LOCATED at
  5d lines 160-162 (NP-7). Build's A-1 splice faithful (ellipsis marked).
THREAD T-b RESOLVED: 5d SS3 carries BOTH bit rows at bytes — S9AD row
  "UNDECIDABLE-TODAY + positive two-model certificate" and T1SR row
  "UNDECIDABLE-TODAY, independently ... twice-determined stop." The build's
  A-2/SS5.2/SS5.3 "twice-determined (T1SR)" chain resolves INSIDE sealed 5d
  bytes at carried grade. NO missing-chain defect. (The T1SR pair also exists
  on disk with seals — not needed for the build's chain.)
THREAD T-a PARTIALLY RESOLVED: every wall quote in the build (R-4, J-4)
  resolves verbatim inside 5d SS3/SS5.2 bytes (FAILS-AT F1/F2/F3; THREE OF
  FOUR DECIDED; tr H_A^Sigma = 0; ||H_A^Sigma||_2^2 = 0 EXACT; two
  independent cancellation mechanisms; DISCHARGED B-L2*; log GENUINE at
  census grade; per-composite untouched; VOLUME DIAGONAL x = y). The chain is
  intact AT CARRIED GRADE via the sealed 5d (whose own SS0 verified 66/66
  including the wall pairs). BUT the build's SS0 sentence "Wall pairs cited by
  the ground were seal-verified as consumed; each is listed at its point of
  use below with its verification result" is UNFULFILLED AT BYTES: no wall-
  pair verification result is listed at any point of use in the build.
  FINDING F-1 (correction grade): over-claiming consumption sentence; the
  actual chain (carried-grade consumption inside sealed 5d) is sound.
ROW-BY-ROW AT BYTES (all MATCH): R-4 (wall summed re-quant row verbatim);
  R-7 (r-2 "B1-DERIVED: kappa_n -> +infinity, full-family"; MO-3 "DERIVED
  p = -3 ... CLOSED"; T4SR "one-vertex trace-parity lemma DERIVED"; clock
  "PINNED alpha-k in [1, 3/2]"); R-8 (V1 "vanishes IDENTICALLY, dim H = 0
  exact ... Unconditional at that stratum"; V2 "phi_f == 0 (F = 0 mandated)"
  under TYPE-P, voids with it); R-9 (SS5.1 channel rider verbatim incl. "n
  enters the derived form exactly once, as the global n^2 prefactor of an
  n-blind form"); R-10/J-7 (SS4.1 D1-D9, all nine termination phrases
  verbatim; "Nine links; zero displayable at forced grade"); A-3 (SS1+SS5.1
  NOT-FORCED + no-comparand); A-4 (NP-4; build truncates the void-trigger
  clause without ellipsis — the trigger IS displayed in build SS6 as F-iii;
  variance V-2, minor); A-5 (NP-7 verbatim); A-9 (NP-2 verbatim); A-10
  (NP-5 verbatim); A-11 (clock row + "five of them FORCED-undecidable at
  displayed-hypothesis grade"); A-13 (SS4.2 verbatim); SS5.3(iii) "this
  verdict FORCES NOTHING SHUT" + conversion map = 5d SS6/flag CONVERSION_MAP
  verbatim (F-i/F-ii/F-iii; nine links; sealed target + cluster kernel).
LENS HUNT IN THE BUILD: exactly one lens token in a display — J-4 cell
  "the log's coupling bearing lens-fenced (permission to read it as coupling
  REFUSED of record)". The lens READING itself is never named in the build;
  the token names the close's own fence (5d flag block: "its coupling
  bearing lens-fenced by the close's own display"). Still: the 5d's own
  discipline confines lens naming to discipline blocks; the build placed the
  token inside a SS4 display table. FINDING F-2 (correction grade, low):
  lens token in a display row; no lens content consumed as ground anywhere.
```

## §4 — EXTENT PAIR, JD PAIR, MINIMALITY SPLIT, DEMAND DECISION AT BYTES

```text
EXTENT BUILD (501 lines, full read): R-5 quotes (X-1 flag line; SS4.1 upper-
  bound-by-faithfulness + (R4) lower witness) MATCH. R-6 (X-2 "dim ker ...
  in {1,2} — no third extent exists"; dim E_N = 3 at SS3.3; X-3 zero-value
  exclusion chain) MATCH. A-2 quotes ((LM1-10) "does not assume ... not
  ... independent"; (S8) "Mixed relations may identify") MATCH verbatim at
  SS3.2. A-8 ("coefficient a_K != 0 undisplayed"; (b)-branch exhibition)
  MATCH at X-2/SS0. J-2 termination quotes (PRIOR completed physical
  carrier; C-sector consumed INSIDE the Schur complement; "does not assume
  they are independent" in so many words) MATCH at SS0. SS5.2's witness
  content (E-1 => "per round 3's biconditional, the fiber INHABITED (modulo
  TOP-1/TOP-2 and the dim M_N cell)"; E-2 => "kills BOTH families (NG-3'
  ..." on dim M_N >= 2) MATCH at extent SS0 header + MISSING CLOSURE OBJECT.
  The "TWO-MODEL WITNESS of record" recast is byte-licensed: the 5d S9AD
  row itself says "positive two-model certificate."
EXTENT AUDIT: VERDICT = CONFIRMED-WITH-CORRECTIONS at bytes (its SS0);
  build's SS2-header carriage faithful.
JD BUILD (766 lines, full read): R-3 quotes (SS1.3 target form "FORCED;
  nothing in it is authored here"; obligation sentence; (JD-3c)) MATCH.
  J-3 quote (SS2 rho_N block, verbatim incl. "the type mismatch both
  independent routes died on") MATCH. A-6 (T2-5 EXPLICIT/SYMBOLIC/
  UNASSIGNED; anti-tuning SK-10 sentence; DoR-019 explicitness-obligatory
  as carried at K6.1) MATCH. A-7 (T2-3 "distinct admissible sections are
  DISTINCT CANDIDATES"; CL-4 reference-covector "displayed as rejected";
  CL-2 representative-route displayed; SK-3/SS6 "only TYPE slots are of
  record"; authored core = SS6 rows 5-13, lambda_JD correctly carried
  separately as A-6) MATCH. Door-map SK-6 quote MATCH verbatim (arrow
  rendered as dash). R9-JII "as a FALSIFIER, never a constructor" MATCH
  at T2-5.
JD CHECK: "OVERALL VERDICT: CHECK-PASS-WITH-CORRECTIONS. No self-kill
  clause fires." at bytes line 5 — build's carriage faithful.
MINIMALITY SPLIT (117 lines, full read): CELL_CONSTRAINT_ONLY flag line
  MATCH; five pure sites + their five J-8 quotes MATCH verbatim; three
  misattributions + two missed termini counts MATCH; "not a theorem" at
  tau = 1 MATCH; D-BRIDGE's "no-independent-row-renormalization bridge
  attached to the source-mass depth ratio site" MATCH (Missed Termini 2).
  FINDING F-5 (correction): SS2 boundary note's "the charge-structure
  require R-2 reaches it" is an UNCHAINED cross-artifact identification —
  the sealed byte says only "inherited from a different carrier/holonomy
  input" (naming NO artifact); the recovery sweep it points into
  (4ae55f95) is outside the build's consumed ground. The identification is
  plausible, verdict-neutral (feeds no site class, no meta-verdict), but
  asserted, not displayed-at-bytes.
DEMAND DECISION (61 lines, full read): RATIFIED — PRINCIPAL DECISION;
  ONE-OBJECT receiver ruling MATCH; revival falsifier MATCH (build splice
  faithful); build's "demand-COUNT ruling, not the forcing-shape taxonomy"
  disposition is correct at bytes. Build quote truncates the trailing
  "carrying its common-origin certificate..." without ellipsis — variance
  V-3, minor.
```

## §5 — THE BUILD'S SWEEPS, RE-RUN (this commission's own runs, 2026-08-15)

```text
SWEEP 1 RE-RUN (declared pattern, case-insensitive, same exclusions, FULL
  listing): 48 basenames now; minus the build's own output and this audit
  (both post-sweep) = 46 pre-existing vs the build's claimed 40. The
  build's declared cap was "first 120 names per root" — an enumeration-
  order-dependent truncation of the LISTING, not of the matches; under a
  full listing the count is 46. The cap was DECLARED, so the count is
  per-cap, not false; but the six additional names were checked here:
  none is a demand-reading carrier (they are descent-demand relay files
  and require-build lane files; none carries the forcing-shape taxonomy —
  confirmed by the content sweep below). MATERIAL CONCLUSION UNCHANGED.
  Root 2: 31 names, ALL present in the primary listing (comm -23 = 0):
  "proper subset" claim TRUE.
SWEEP 2 RE-RUN (declared pattern): case-SENSITIVE run does NOT hit the
  adjudication's line 54 ("THREE shapes" defeats "three (require )?shapes");
  case-INSENSITIVE run reproduces the build's results EXACTLY: 6 pre-
  existing files (primary), 3 (root 2), adjudication line 54 the unique
  forcing-shape carrier. VARIANCE V-4: the sweep-2 declaration omits the
  case flag its executed sweep evidently used ("same exclusions" is
  declared; "case-insensitive" is not).
THE FIVE NON-TARGET HITS, EACH INSPECTED AT BYTES THIS COMMISSION:
  MAGNETIC_SECTOR_HUNT :233  "the three shapes this thread has already
    been" — thread shapes, different object. CONFIRMED.
  AXN GENERIC_U1_CENSUS :100 "a CONJUNCTION of three, not one proof" —
    proof-conjunction count, different object. CONFIRMED.
  DESC_B03 :16/:890 span-convention shapes — different object. CONFIRMED.
  IDEAL_EXTENT (+AUDIT) — the (S1-5) "three shapes any closure must take"
    — CLOSURE shapes, not forcing shapes; and both files are consumed
    ground. CONFIRMED different object.
  => CH-1's SUBSTANCE (unique location, forced-by-sweep) SURVIVES
     independent re-derivation. FINDING F-3 (correction): CH-1's pointer
     "the other five hits are displayed and disposed in §0.1" is FALSE at
     bytes — §0.1's ADJACENT block displays the demand DECISION and the
     minimality split (sweep-1 objects, neither a sweep-2 hit) and never
     displays the five; CH-1's four disposition labels ("a demand-count
     ruling; span conventions; a proof-conjunction count; thread shapes")
     do not map one-to-one onto the five hits (the extent pair carries no
     label; "a demand-count ruling" fits a non-hit). The dispositions
     that ARE given are correct in substance; the cross-reference and the
     label-to-hit mapping are defective.
```

## §6 — CAS RE-VERIFICATION (this commission's own battery; fresh venv)

Environment: fresh venv under the session scratchpad, sympy 1.14.0; no evalf, no N(),
no float; independent FORMULATIONS (symbolic all-n winding; matrix nullspaces for the
extent menu), not a re-typed copy of the build's script. Script sha256
eb37aea8a2e8c382af52ef4bd0c7d673fcb734f17ed8a5c73e2c71e77227e040. Output, verbatim:

```text
T14SR JUNCTION-AUDIT CAS -- exact symbolic, zero numerics (sympy)
[A-J1a] PASS -- integer winding closes single-valuedly, ALL integer n (symbolic n)
[A-J1b] PASS -- fractional winding exact residual -2 at n = 1/2
[A-J2a] PASS -- transition covariance: same g^n on both characters (all n)
[A-J2b] PASS -- conjugate pair chi_+1 chi_-1 == 1 closes magnitude-free
[A-J2c] PASS -- on-record amplitude forced to trivial constant G = 1 uniquely
[A-J3a] PASS -- toy f1 == sin^2(Delta/2) exactly
[A-J3b] PASS -- f1 != f2; exact separation residual 1/4 at the toy argument
[A-J4a] PASS -- E-1 independence: dim ker = 1 exactly
[A-J4b] PASS -- E-2 mixing: dim ker = 2 exactly
[A-J4c] PASS -- menu is exactly {1,2}; no third extent at dim E_N = 3, im nonzero
[A-J4d] PASS -- a*L_T,N = 0 has ONLY a = 0 over nonzero L_T,N: a zero-value MIXING relation (a != 0) is impossible -- antecedent empty
[A-X-A] PASS -- a=0 slice = the same dim-1 (R4)-type line {b+c=0} in BOTH E-1 and E-2 (generic a_K != 0)
[A-X-B] PASS -- every displayed clause used holds at SYMBOLIC integer n: no clause pins |n|
[A-X-C] PASS -- E-2's second line carries a != 0: it is a mixing line, absent from E-1
TOTAL: 14 checks; FAIL count = 0
```

All 11 build checks re-derived (A-J1a..A-J4d cover J1a..J4d one-for-one, most at a
STRONGER quantifier: A-J1a/A-J2a run at symbolic integer n, all n at once). Audit
strengthenings: A-X-A proves the build's §5.2 sentence "the a=0 slice is the same
dim-1 line in both" (the build cited J4a/J4b, which check only the dims — the
slice-identity itself was not among its 11 checks; now verified); A-X-B grounds the
A-3 gloss "any integer inhabits every displayed clause" at the same grade the sealed
AJ battery used; A-X-C confirms E-2's second line is a genuine mixing line. One audit
formulation error (sympy Ne-in-solve) was caught and repaired during the run; no
build claim was ever implicated.

## §7 — FENCE-SCAN AND NUMBER AUDIT (at bytes)

```text
FENCE-SCAN of the build: every "alpha" hit inspected at bytes — the fence
  lines themselves (x2), path strings (x3), and sealed-carried phrases
  (ALPHA-FORCING LADDER / alpha-forcing link / alpha's magnitude payload /
  "alpha' A SYMBOL" / "alpha-k in [1, 3/2]"), each traced verbatim into 5d
  bytes. No alpha value, no computation, no comparison. FORBIDDEN TOKENS
  (137|thomson|K_KK|planck|ell_P|measured|electron|QED|4pi|sphere|
  carleman|S16): ZERO hits. FLOAT PATTERN: every hit is a section number
  or a (JD-3a)-style token; no numeric float. evalf/N(): only the build's
  own fence-declaration sentence. CLEAN.
NUMBER AUDIT (the commission's "any number" hunt): every number in the
  build traced to sealed provenance — -2, G = 1, 1/4, chi_+1 chi_-1 = 1
  (AJ CAS bytes); dim 1 / {1,2} / dim E_N = 3 / (0,1,-R_K,N) (extent
  bytes); [1, 3/2], p = -3, super-n^{3/2}, b_1 = 1, b_1 >= 2, n^2,
  dim H = 0, tr = 0, ||.||_2^2 = 0 (5d bytes); d = 4, C_R = 1, tau = 1,
  spin-1/2 (minimality-split bytes); remaining numerals are structural/
  process counts (rows, checks, caps, dates). NO new number appears as a
  value of anything. ADJUDICATED-PASS on R-2's "an actual forced value:
  the trivial amplitude" (G = 1): a sealed derived triviality corollary
  (AJ T1) re-displayed with provenance — same license as displaying the
  sealed "p = -3"; the flag block's "no number as a value of anything"
  reads as "none authored/frozen HERE," which is held.
```

## §8 — THE COMMISSIONED DEFECT CLASSES, HUNTED ONE BY ONE

```text
(1) INVENTORY ROW WITHOUT ITS CHAIN AT BYTES: hunted across all 10 R-rows
  and 13 A-rows. Every quoted phrase was located verbatim (or with marked
  ellipsis) inside the sealed carrier its citation names; the two carried-
  grade chains that looked unchained at first (T1SR "twice-determined";
  the wall quotes) both resolve inside sealed 5d §3 bytes. ONE instance
  found OUTSIDE the tables proper: the §2 boundary note's "the charge-
  structure require R-2 reaches it" (FINDING F-5) — an identification no
  consumed byte makes.
(2) TERMINATION-SITE CLASSIFICATION ASSERTED NOT DERIVED: hunted at §4.1.
  Each cluster assignment rests on the site's own quoted stop language
  (carrier-edge: "no sealed arrow of this type" / "no clause ... co-occur"
  / "below the forcing threshold" / "both sealed candidates unavailable"
  / "no block/direct-sum separation survives"; quantifier-crossing:
  "summed quantifier; per-composite untouched" and the displayed-
  hypothesis-vs-full-range grade vocabulary the corpus itself uses;
  boundary-adjacent: the five split quotes "in so many words"). The
  negative certification at J-4 (F2: the fatal locus is THE DIAGONAL, not
  the boundary => NOT boundary-adjacent) is byte-grounded. The fourth
  family is expressly NOT asserted as a new structural kind (CH-4
  displays this). ADJUDICATED: DERIVED, displayed. Soft spot noted, not a
  finding: J-5's reading of rate-blockers as quantifier objects leans on
  the corpus's own grade vocabulary (full-family / displayed-hypothesis /
  o(kappa_n)); it is display-supported.
(3) META-VERDICT QUANTIFIER GAP (the c-A pattern): hunted hardest, both
  directions.
  N-INDEXING: the contingency witness is stage-indexed; the build carries
    the per-isometry-class quantifier INSIDE the decider's name ("one
    anchor stage per isometry class") — load-bearing because the extent's
    X-4 u-collapse is CONDITIONAL (possibly TWO independent bits), and
    the build's decider covers all classes. NO GAP.
  MODEL-CLASS SCOPE: §5.1's rigidity quantifies over models of the AJ
    displayed clause set; §5.2's witness over models of the extent
    clause set. The overall CONTINGENT verdict needs the witness pair to
    agree on every displayed require GLOBALLY: the build displays
    agreement on "every §2 row that touches them" (re-verified here:
    slice identity A-X-A; menu = the pair; X-3 model-independent), and
    the untouched rows extend identically into both models (disjoint
    sectors). SOUND.
  THE ASYMMETRY ITSELF (why E-2 is a same-clause model but an AJ profile
    is not): the mixing relation is a candidate FACT about a PRIOR
    completed sealed carrier, expressible in ground vocabulary (the
    sealed question R_K_EQUALS_L_T with its own flag of record), while
    any profile is EXTENSION vocabulary (M8: no co-occurrence clause
    exists; every exit operation is adjoined). Epistemic openness of an
    existing carrier vs structural absence from the language. The build
    displays this through its conservativity-pattern note (A-2 "the
    record's second live instance" vs the vacancy rows "no clause to
    differ on"). NO CATEGORY SLIP. Rigidity's residue carried EXACTLY as
    the AJ seals it (C12 + finite-text; conservativity core exempt).
  VERDICT ON THE META-VERDICT: JUNCTION-CONTINGENT with the rigid J-6
    sub-region STANDS at bytes. The overall word follows the build's own
    displayed definition (CH-5: one sealed witness moving the line
    suffices) — a declared definitional choice, not a smuggled one.
(4) DOOR-MAP SMUGGLED DERIVATION: hunted row by row. Every cell cites
  sealed words; arrivals are conditional-mood shape classifications
  ("would be"); the one derivation-flavored cell ("every surface-native
  route is closed by theorem") is the AJ's own sealed theorem, cited.
  The banner "no cell asserts that any arrival WILL occur ... no door is
  opened" is TRUE at bytes. NONE FOUND.
(5) LENS LANGUAGE IN A DISPLAY: ONE token found (J-4 row, "lens-fenced")
  — FINDING F-2. It names the close's own fence; no lens content is
  consumed as ground anywhere; the lens reading itself is never named.
(6) ANY NUMBER: §7's number audit — all sealed-carried or structural;
  NONE authored. CLEAN.
```

## §9 — FINDINGS (all correction-grade; none verdict-bearing)

```text
F-1  BUILD §0: "Wall pairs cited by the ground were seal-verified as
     consumed; each is listed at its point of use below with its
     verification result" — UNFULFILLED: no wall-pair verification result
     appears at any point of use. The actual chain is sound at CARRIED
     grade (every wall quote resolves inside sealed 5d §3/§5.2, and the
     5d's own §0 verified the wall pairs 66/66). The sentence over-claims
     the build's own verification acts. FIX: strike the sentence or list
     the results it promises.
F-2  ONE lens token inside a §4 display row ("lens-fenced", J-4). The 5d
     confines lens naming to discipline blocks. FIX: replace with the
     row's own parenthetical ("permission to read it as coupling REFUSED
     of record") or move the token to a discipline block.
F-3  CH-1/§0.1 cross-reference defect: "the other five hits are displayed
     and disposed in §0.1" is false at bytes; the four disposition labels
     do not map onto the five hits (extent pair unlabeled; "a demand-count
     ruling" fits a non-hit). Substance verified true here (§5): the
     location is unique; every non-target hit is a different object. FIX:
     point the dispositions at their actual hits.
F-4  R-5's SHAPE-2 gloss "two conditions meeting exactly (upper bound +
     lower witness), one freedom" contradicts the displayed SHAPE-2
     definition ("n conditions, n freedoms") on its face. The SHAPE-2
     assignment itself is the best available (each condition alone
     insufficient — not corroborating surplus, so not SHAPE-3; two
     conditions — not SHAPE-1); the count gloss needs repair or the fit
     needs the qualifier "by minimal joint sufficiency, not by count."
F-5  §2 boundary note: "the charge-structure require R-2 reaches it"
     (the mixed site's unit-charge half) is an unchained cross-artifact
     identification; the sealed byte says only "inherited from a
     different carrier/holonomy input," naming no artifact, and the
     recovery sweep behind it is outside the consumed ground. FIX: mark
     the identification as the build's own reading or drop it.
VARIANCES (sub-correction, quote fidelity): V-1 AJ "residual -2*..."
     quoted as "residual -2" (truncation unmarked); V-2 NP-4's void-
     trigger clause dropped unmarked (trigger displayed elsewhere at §6
     F-iii); V-3 demand-decision trailing certificate clause dropped
     unmarked; V-4 sweep-2 declaration omits the case-insensitivity its
     executed sweep used. Plus benign style transforms (nested quote
     marks; emphasis-to-caps), each checked content-identical.
```

## §10 — CHOICE LEDGER (commission JUNCTION-AUDIT T14SR)

```text
ACH-1  READING ORDER (adjudication -> AJ pair -> 5d -> extent pair -> JD
       pair -> split -> decision): PREMISE(named), presentation-grade;
       every artifact was read in full regardless of order. IMMATERIAL.
ACH-2  INDEPENDENT CAS FORMULATIONS (symbolic all-n; matrix nullspaces)
       instead of re-typing the build's script: PREMISE(named) — a
       re-formulation catches formulation-dependent errors a re-run
       cannot; the build's own script was NOT re-executed byte-identically
       here (its output block was consumed as the sealed display it is;
       the CLAIMS were re-derived, which is this commission's posture).
ACH-3  SWEEP RE-RUNS at FULL listing (no 120-name cap): FORCED in
       substance by DEFAULT-REFUTE — the cap is the build's declared
       bound, and refuting/confirming its location claim requires the
       uncapped run. Both sweeps re-run; results displayed §5.
ACH-4  CARRIED-GRADE ACCEPTANCE for wall/T1SR content (chains resolving
       inside sealed 5d bytes rather than at the wall-pair files
       directly): FORCED by the commission's own ground rule (scoped
       reads of named artifacts; the 5d is the named ground; its 66/66
       verification is its own sealed display). The wall-pair FILES were
       not opened here.
ACH-5  FINDINGS SEVERITY (all corrections, none refuting): FORCED by the
       displays — every finding is a display/process defect whose
       underlying chain or substance was independently verified sound;
       no inventory row, site, classification, or verdict changes under
       any of F-1..F-5.
ZERO entries in class OPEN.
```

## §11 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  This audit touched toys only where the sealed ground quarantines them:
  the AJ pair's f1/f2 re-derived at A-J3a/A-J3b exactly as meta-level
  derivability counterexamples; the extent menu re-derived on generic
  placeholder matrices (a_K symbolic, never valued) mirroring the sealed
  battery's own scaffolds. No toy value, profile, or model is asserted as
  surface content; none leaks into any finding, adjudication, or verdict.
```

## §12 — FLAG BLOCK AND VERDICT

```text
COMMISSION = T14SR (JUNCTION-AUDIT; DEFAULT-REFUTE; testimony zero
  weight; every claim re-derived at bytes).
STEP_0 = claimed artifact present, seal e1c2ac80... shasum -c OK.
GROUND_SEALS = 11/11 independently re-verified MATCH (§0.1); wall/T1SR
  content consumed at carried grade inside sealed 5d bytes (ACH-4);
  MISMATCHES = 0.
SWEEPS = both build sweeps re-run UNCAPPED (§5): sweep-2 location claim
  TRUE and UNIQUE (6 pre-existing primary hits, 3 root-2, adjudication
  line 54 the sole forcing-shape carrier; every non-target hit verified
  a different object at bytes); sweep-1 subset claim TRUE; count-per-cap
  explained, material conclusion unchanged. SWEEP_CUTOFF (this audit) =
  the 11 named ground artifacts at verified seals + the two declared
  uncapped sweeps of the two corpus roots, 2026-08-15; no register/
  tracker/road/plan/continuation file read; "Q-..." tokens
  EXPECTED-UNLOCATABLE, cited only as carried.
CAS = fresh venv, sympy 1.14.0, INDEPENDENT formulations, 14/14 PASS
  (script sha256 eb37aea8...): all 11 build checks re-derived (several at
  stronger quantifier: symbolic all-n) + 3 strengthenings (slice
  identity across both extent models — the build's §5.2 sentence, now
  proven; the all-integer inhabitance gloss; the mixing-line witness).
FENCE_SCAN = clean (§7): all alpha hits fence/path/sealed-carried; zero
  forbidden tokens; zero floats; number audit ALL sealed-carried or
  structural, none authored.
DEFECT_HUNT = six commissioned classes hunted (§8): no missing chain in
  any inventory ROW (one boundary-note instance, F-5); classification
  DERIVED not asserted; meta-verdict survives the c-A hunt at both
  n-indexing (per-isometry-class quantifier carried) and model-class
  scope (asymmetry byte-grounded: ground-expressible undecided fact vs
  extension vocabulary); no door-map smuggling; one lens token (F-2);
  no authored number.
FINDINGS = F-1..F-5 all CORRECTION-grade + variances V-1..V-4 (§9);
  NONE verdict-bearing; no REFUTED item anywhere.

VERDICT, PER DELIVERABLE:
  D1 REQUIRE-SIDE INVENTORY (§2, 10 rows) = CONFIRMED-WITH-CORRECTIONS
     (F-4 count gloss; F-5 boundary note; every row's chain verified).
  D2 ALLOW-SIDE INVENTORY (§3, 13 rows)   = CONFIRMED (all witnesses/
     vacancies/exercised-permission displays verified at bytes; V-2 only).
  D3 TERMINATION SITES + CLASSIFICATION (§4) = CONFIRMED-WITH-CORRECTIONS
     (F-2 lens token; classification derived and stands: carrier-edge /
     quantifier-crossing / boundary-adjacent / instrument-status exactly
     as displayed).
  D4 META-VERDICT (§5) = CONFIRMED — JUNCTION-CONTINGENT with the rigid
     J-6 sub-region, residue carried exactly; witness sealed twice at
     carried grade; deciders all the record's own.
  D5 DOOR MAP (§6) = CONFIRMED — display discipline held; no door
     opened; no arrival asserted.
  GROUND/PROCESS (§0-§1) = CONFIRMED-WITH-CORRECTIONS (F-1, F-3, V-4).

OVERALL = CONFIRMED-WITH-CORRECTIONS.

DETERMINATION_ONLY = held by this audit: no adoption, no authored
  physics, no value, no number authored, no gate/flag/witness of any
  consumed artifact moved; nothing here discharges any named absence.
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT
  before first write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = the junction display survives DEFAULT-REFUTE: ten require chains,
  thirteen permissions, eight termination sites in three derived clusters
  plus the record's own instrument-status family, and the site-split
  meta-verdict (rigid exactly where the conservativity theorem reaches;
  contingent exactly where the sealed E-1/E-2 witness stands) all
  re-derive at bytes; the five findings are display and process repairs
  that move no verdict.
```
