# Stage-7 Q_spec Review Candidate v002

Date: 2026-07-24

## Candidate status

```text
INDEPENDENT_REVIEW_PASS_WITH_CONDITIONS
CONDITIONS_C1_THROUGH_C6_LANDED
C7_DEPENDENCY_ORDER_FROZEN
STAGE8_CROSS_EXECUTION_READY
COMPLETE_Q_SPEC_NOT_YET_ESTABLISHED
```

This append-only successor implements the unanimous conditions returned by
the three independent reviews of v001. The reviewers reproduced the
load-bearing algebra and packet provenance. No review reported a failure or
blocking finding.

This remains a durability-parent candidate. It is not a coupling
calculation.

## Declared branch

```text
ordinary 3+1 Lorentzian globally hyperbolic spin spacetime;
one massless-bare vectorlike charged Dirac pair;
one smooth compact relative-U(1) connection a;
finite primitive causal diamonds;
distinct even M3 record factors;
a flat source-free asymptotic region;
and a causally sequential cell exhaustion.
```

Every new cell in the proven exhaustion class opens after all prior closure
faces. Spacetime-overlapping concurrent-cell compatibility is not derived.

## Premise visibility

The six adopted premise classes are:

```text
Fundamental Boundary Record Action;
Parent-State Covariance;
Causal Incidence Support;
Boundary Superconnection / Single-Operator Completeness;
Global Boundary Descent / Quasi-Free Completeness;
and zero independent bare F^2 compositeness.
```

The standard-theory or kinematic inputs are separately disclosed:

```text
Dirac spinors and the CAR framework;
standard CPT and spin-statistics structure;
ordinary functional-analysis conventions;
and the ready, pointer, completed, and recoverable record axioms.
```

The spacetime branch, one-pair source inventory, smooth compact connection,
stationary quasifree in-state, and envelope realization `ER-A` are disclosed
inputs. Additional vectorlike pairs and alternate envelope parents are not
excluded.

## Fields and carriers

On a Cauchy surface `Sigma`:

```text
K_Sigma=L2(Sigma,S tensor L^q);
R(K)=tensor_(c in K) R_c;
R_c=span{|r_c>,|p_c>,|e_c>}.
```

There is one global `CAR(K_Sigma)` source algebra. Record factors are even,
distinguishable, and are not fermionized.

The primitive relative-phase character has unit winding. The smooth
principal relative-`U(1)` bundle and connection are adopted microscopic
field content, not inferred from a passive ray phase.

## Primitive cell, measure, and envelope realization

For each primitive cell:

```text
Gamma_c=diag(+1,+1,-1);
b_c=
  [[0,0,-1],
   [0,0,+1],
   [-1,+1,0]];
c_c=i Gamma_c b_c.
```

Normalized four-volume is the unique intrinsic probability measure in the
classified flat-diamond measure class. Its time marginal is:

```text
w(s)=32 min(s,1-s)^3,  0<=s<=1;
integral_0^1 w(s) ds=1.
```

The measure fixes this shape. It does not alone fix how the shape enters the
operator. The current branch discloses:

```text
ER-A: the time marginal is used as the interaction amplitude.
```

Hence:

```text
v_c(t)=(tau_R/T_R) w((t-t_c)/T_R)
```

inside the cell and zero outside. The replacement for the old pushforward
check is:

```text
shape clause:     w is the normalized time marginal;
amplitude clause: integral v_c(t) dt = tau_R.
```

The dimensionless opening interval is:

```text
tau_R=pi/sqrt(2).
```

The physical duration `T_R>0` remains unfixed.

## First-order parent and descendants

With:

```text
S_n=-i slash(n) gamma^5,
```

the one-particle parent is:

```text
h_K(t)
 =h_0[g,a]
  +sum_(c in K) v_c(t) M_c(t)
     tensor S_n tensor iota_c(c_c).
```

The finite many-source lift is:

```text
H_K(t)=dGamma_R(h_K(t)).
```

The covariant first-order kernel is:

```text
D_K=i gamma^mu nabla_mu+i gamma^5 C_K(x),
C_K(x)=sum_c v_c(x) M_c(x) iota_c(c_c).
```

Direct squaring gives:

```text
D_K^2
 =(i gamma^mu nabla_mu)^2
  -C_K^2
  -gamma^mu gamma^5 nabla_mu C_K.
```

The finite-cell descendants are generated by this operator without an
independent descendant coefficient. Zero independent bare `F^2` stiffness
is adopted compositeness content; exclusion of an arbitrary finite
renormalized `c F^2` deformation remains open.

## State, contour, and analytic scope

The incoming stationary quasifree state of `h_0` is disclosed ordinary-branch
state content. The positive/negative spectral projectors of that state
derive the free Schwinger-Keldysh contour and its free propagators.

For finite `K`, the interaction is a bounded, compact-time-supported
self-adjoint perturbation of `h_0`. The sealed analytic authority gives:

```text
a common domain by bounded Kato-Rellich;
a unique finite propagator by the interaction-picture Dyson series;
and smooth-to-sharp convergence when the approximants converge in
L1 operator norm.
```

`H_K(t)` and the finite `W_N` are finite-regularization/algebraic
Bogoliubov-automorphism objects. No unitary implementability of
intermediate-time dynamics in the asymptotic quasifree representation is
claimed; the one-particle off-diagonal part is not Hilbert-Schmidt there.
Only the completed maps in the scoped outgoing construction are claimed to
be implementable.

A normalized interacting CTP amplitude and its gauge/ghost/edge functional
measure remain open.

## Tail, excitation class, and durability

In the flat source-free asymptotic branch:

```text
h_0(p)=alpha_D dot p.
```

The admitted class `E_fin` consists of normalized `L2` source states in the
quadratic-form domain of `|h_0|` whose spectral measure has an `L1` density.
The threshold theorem is per state:

```text
for every psi in E_fin and every delta>0,
there exists T(psi,delta) such that for every t>=T,
|<psi,exp(-i h_0 t)psi>|^2 < delta.
```

No state-uniform threshold time is claimed.

Causal Incidence Support prohibits reuse of a completed primitive incidence.
For causally sequential additions, later cells are the identity on completed
record factors. The compatible completed-record states define:

```text
R_infinity=inductive_limit tensor_(j=1)^N M_3(C);
(pi_out,H_out,Omega_out)=GNS(R_infinity,omega_out).
```

The coherent source-dressed record net is:

```text
Phi_N(A)=W_N^*(I_source tensor A)W_N;
j_NM=Phi_M o iota_NM o Phi_N^(-1).
```

Central pointer averages separate the public labels.

The exact compatibility statement is restricted to causally sequential
exhaustions. An external review counterexample blocks the broader
spacetime-concurrent claim. The earlier hard-coded spacelike commutation and
swap-independence booleans are not treated as derivations.

## Source-inclusive completion remains open

The completed-record GNS does not establish:

```text
a projective limit of source-inclusive states;
an infinite-future source Moller limit;
or continuum-regulator independence of that source-inclusive limit.
```

These are explicit `Q_spec` slots.

## Derived scope

In the declared causally sequential branch, the candidate derives:

```text
the finite causal durability parent;
its generated descendants;
the finite common domain and propagator;
the free contour from the disclosed quasifree in-state;
the free source tail and per-state thresholded source nonreturn;
the outgoing record inductive state and GNS;
the coherent dressed record net;
the recoverable central sequence;
and the scoped Causal Direct-Limit Record Principle.
```

## Open `Q_spec` slots

```text
absolute physical T_R;
full gravity action and gravitational quantum measure;
dynamical U(1) action;
gauge fixing, ghosts, and gauge edge modes;
normalized interacting CTP amplitude;
parent-derived functional regulator and finite renormalization;
polarization transversality and photon-mass exclusion;
Lorentz- and packing-independent renormalized response;
finite c F^2 deformation exclusion;
source-inclusive state projective limit;
infinite-future source Moller limit;
continuum-regulator independence of that limit;
interacting charged pole or infraparticle threshold;
complete charged-species and threshold map;
enlarged-branch exhaustion;
threshold-conditioned Thomson matching;
interacting CISP descendant test;
and one unused structure-sensitive prediction.
```

No response normalization may depend on cell count, packing density,
triangulation valence, arbitrary refinement rate, or regulator-specific
overlap multiplicity.

Packing-dependent response normalization is forbidden.

## Frozen electromagnetic dependency order

```text
dynamical U(1)
-> gauge fixing / ghosts / edge modes
-> normalized interacting CTP amplitude
-> parent-derived regulator and finite renormalization
-> transversality / photon-mass exclusion
-> Lorentz- and packing-independent response
-> charged pole or infraparticle threshold
-> threshold-conditioned Thomson matching
-> interacting outgoing-sector CISP test.
```

Thomson matching may not presuppose a sharp charged pole. An infraparticle
outcome requires a matching reformulation.

## Frozen authorization state

```text
stage7_independent_reviews_completed = true
stage7_review_conditions_landed = true
stage8_cross_execution_ready = true
stage8_theorem_battery_authored = false
stage8_cross_execution_completed = false
complete_parameter_free_Q_spec_frozen = false
physical_Thomson_stiffness_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
