# Stage 8 Slot-18 Derivation and Dependency Map v001

Date: 2026-07-30

## Verdict

**MISSING SPECIFICATION. None of the six families is presently reachable as a
Slot-18 structure-sensitive prediction.**

Three theory-side results already exist without a written direct dependency
on the Q-21 parent-functional construction: finite holonomy,
temporal/connected-kernel diagnostics, and causal-record durability. Their
physical observable, A32 candidate, comparator, and sealed uncertainty bridges
are missing. The flux protocol and both charged families require missing
complete microscopic or charged objects on the current construction path.

This map specifies required derivations. It derives no prediction and asserts
no admissibility because Q-23 leaves `unused` undefined.

## Dependency rule and careful reading

The current Q-21 ruling says at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:906-920`:

```text
`Gamma_K` + `C_record(K)` IS THE SOLE CONSTRUCTION TARGET
...
IT IS THE ROOT OF THE DEPENDENCY GRAPH WITH NOTHING UPSTREAM
...
Everything waits on it; it waits on nothing.
```

The same ruling expressly preserves Slot-18 parallelism at `:933-935`:

```text
SLOT 18 RUNS IN PARALLEL
...
`Gamma_K` for alpha's VALUE, slot 18 for its STANDING
```

Those statements are reconciled here as follows:

- `DIRECT` means a family's own live carrier expressly requires the missing
  microscopic object that Q-21 is chartered to construct.
- `CURRENT-PATH COMPONENT` means Q-21 supplies a named prerequisite but is not
  sufficient for the family.
- `NO WRITTEN DIRECT EDGE` means the bounded family carriers do not name the
  Q-21 object; it does not prove mathematical independence from every future
  completion.
- Existing results may be cited in parallel. A full Slot-18 prediction is
  `reachable now` only if every theory, physical-interface, comparator, and
  seal dependency is already specified. None meets that stronger test.

Q-22 also prevents treating the current-carrier Q-21 construction as a
continuum/stitching theorem. The question register says at `:982-986`:

```text
`Gamma_K` on the current carrier lands at RANK 6, not rank 1 — it supplies
the CTP construction and the measure, not spacetime finiteness.
```

No family below is credited with a continuum bridge merely because Q-21 might
succeed.

## Rank by reachability without the Q-21 construction

| Rank | Family | Theory-side state without Q-21 | Q-21 dependency for the missing Slot-18 derivation | Reachable now as Slot 18? |
|---:|---|---|---|---|
| 1 | Causal-record durability / recoverability | Existing non-alpha principle | **No written direct edge**; missing quantitative operational and comparator bridge | **No** |
| 2a | Finite global-holonomy response | Existing quantitative finite diagnostic | **No written direct edge** for existing diagnostic; Q-21 is insufficient for missing continuum/physicalization | **No** |
| 2b | Temporal plaquette / connected kernel | Existing quantitative finite diagnostics | **No written direct edge** for existing diagnostics; Q-21 is insufficient for universality/locality | **No** |
| 3 | Flux / retarded response | Protocol only | **Direct current-path prerequisite overlap** with complete cell/operator/measure/quotient; Q-21 still insufficient | **No** |
| 4 | Charged spectrum / threshold / nonzero-momentum response | Prospective complete-charged-specification output | **Current-path component** behind complete charged specification; Q-21 not sufficient | **No** |
| 5 | Magnetic form factor / anomaly | No theory output or explicit dependency specification | **Procedurally downstream of the complete charged branch**; direct Q-21 edge is not written | **No** |

The rank concerns how much theory-side structure exists without Q-21. It is
not an eligibility ranking.

## Family 1: finite global-holonomy response

### Existing statement

`COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_RESULT_V001.md:12-16`:

```text
The derived complete-Qspec relative-history CTP scalar carries a strictly
positive response to the frozen total Wilson-loop coordinate on the existing
three-site finite regulator.

This result closes the finite global-holonomy diagnostic only.
```

### Exact derivations still required

The same result withholds at `:141-153`:

```text
a local transverse or plaquette Maxwell response;
continuum or regulator independence;
packing independence;
...
or proof authorization.
```

To make a structure-sensitive physical statement, derive:

1. a refinement/regulator limit and packing/cellulation independence for the
   response;
2. a map from the limiting response to one scalar public physical observable
   with defined domain and units;
3. an immutable map from the sealed theory-output record and canonical A32
   candidate record to a prediction and theory uncertainty;
4. a candidate-compatible same-alpha comparator payload; and
5. a sealed all-eligible-candidate prediction map before beacon selection.

Items 2-4 are already summarized at
`STAGE8_SLOT18_BOUNDED_SCOPE_INVENTORY_V001.md:655-657`:

```text
a regulator/refinement-independent physical observable
map, an A32-eligible canonical record, a concrete preregistered same-alpha
comparator, and the frozen prediction/uncertainty payload.
```

### Q-21 dependency

The existing result predates and does not name Q-21. Its missing
refinement/physicalization bridge is not supplied by Q-21 under the Q-22
ceiling. Classification: **NO WRITTEN DIRECT EDGE; Q-21 INSUFFICIENT.**

## Family 2: temporal plaquette / connected kernel

### Existing statement

`COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_RESULT_V001.md:13-20`:

```text
The already frozen two-cell complete-Qspec parent has a strictly positive
response
...
This two-cell object has not earned a local Maxwell coefficient
```

The connected-kernel result is explicitly only a prerequisite at
`COMPLETE_QSPEC_CONNECTED_KERNEL_LOCALITY_DIAGNOSTIC_RESULT_V001.md:58-64`:

```text
This is the
correct prerequisite for a later temporal-gradient/Maxwell comparison; it
is not itself that comparison.
```

### Exact derivations still required

The next mathematics is fixed at
`COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_RESULT_V001.md:134-149`:

```text
derive the connected translation-invariant many-cell limit
and test whether:
the response kernel becomes local at long wavelength;
the normalized low-frequency diagonals converge to one value;
cross-frequency terms vanish;
the intensive result is independent of cellulation and packing.
...
combine the result with a spatial magnetic plaquette response and run the full
Maxwell tensor/Hodge-duality test.
```

After those gates, derive:

1. a physical measurement interface at fixed frequency/momentum domain;
2. a scalar canonical candidate map with units;
3. a sealed theory uncertainty including regulator and truncation effects;
4. a candidate-compatible comparator payload; and
5. the immutable prediction map.

### Q-21 dependency

The finite diagnostics do not name Q-21. Their universal long-wavelength,
cellulation, and packing gates are not supplied by the Q-21 current carrier
under Q-22. Classification: **NO WRITTEN DIRECT EDGE; Q-21 INSUFFICIENT.**

## Family 3: electric/magnetic flux and retarded response

### Existing state

`FINITE_RECORD_CELL_FLUX_RESPONSE_PROTOCOL_V001.md:3-8`:

```text
This is a preregistered evaluation protocol, not a specification of the
microscopic record cell. It may run only after one complete finite
spacetime-plus-internal `Q_cell` has been derived, sealed, and passed through
the provenance gate.
```

### Exact derivations still required

The complete cell must fix every item at `:15-24`, including:

```text
the complete gauge-covariant operator or update;
all species, masses, doublers, and zero-mode prescriptions;
the state, CTP contour, boundary data, edge modes, and gauge quotient;
the measure and exact normalized partition functional;
...
the rule that forbids an independent finite `c_R F^2` mutation.
```

Then derive and execute, without selecting a result:

1. the electric and magnetic responses from that same sealed functional;
2. the Lorentzian continuation and CTP retarded Hessian, including contact and
   edge terms, required at `:75-95`;
3. the complete target-independent mutation audit required at `:97-99`;
4. the stitching/refinement and continuum bridge;
5. a non-alpha scalar physical response with fixed domain and units;
6. its candidate map, comparator payload, and uncertainty propagation; and
7. the immutable prediction map.

The authorization state at `:112-124` keeps the complete cell, response
authorization, retarded agreement, mutation exclusion, and proof flags false.

### Q-21 dependency

The missing complete operator, CTP state/contour, quotient, measure, normalized
functional, and retarded map overlap the object-level work Q-21 is chartered
to supply on the current path. Q-22 still withholds spacetime
finiteness/stitching, so Q-21 is necessary on that path but not sufficient.
Classification: **DIRECT CURRENT-PATH PREREQUISITE OVERLAP; Q-21
INSUFFICIENT.**

## Family 4: causal-record durability / recoverability

### Existing statement

`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md:13-22`:

```text
A durable public record is the compatible outgoing sector of a
future-directed Lorentz-covariant, causally sequential exhaustion governed
by the same finite parent. Its durability consists jointly of thresholded
source nonreturn and exact completed-record persistence; its public content
is the recoverable quasi-local record state and central sequence.
```

### Exact derivations still required

The principle withholds at `:24-30`:

```text
arbitrary spacetime-overlapping concurrent cells
...
a derived spacelike causal-factorization/light-cone lemma
...
neither a source-inclusive projective state limit nor an infinite-future
source Moller unitary.
```

Its status at `:35-40` keeps the concurrent extension, projective/source
limits, regulator independence, and absolute response normalization false.

To make a structure-sensitive statement, derive:

1. one quantitative operational scalar from persistence, nonreturn, or
   recoverability, including an intervention/readout protocol;
2. its finite-domain definition, dimension or unit, limiting procedure, and
   theory uncertainty;
3. the concurrent-cell and continuum bridges if the selected observable
   consumes them;
4. a canonical public candidate map;
5. a published same-alpha comparator for that exact operational quantity; and
6. the immutable prediction map.

### Q-21 dependency

The live principle does not name Q-21. The missing operational quantity and
comparator are independent missing specifications on the face of the current
record. This family is therefore the most reachable without Q-21, but it is
not reachable now as Slot 18. Classification: **NO WRITTEN DIRECT EDGE;
INDEPENDENT MISSING INTERFACE.**

## Family 5: charged spectrum, thresholds, nonzero-momentum response

### Existing state

V011:1592-1605 requires a distinct sealed complete charged object containing:

```text
all charged source sectors and their statistics;
the gauge, constraint, ghost if required, and public edge sectors;
...
the microscopic measure and regulator;
the regulator-removal and locality theorem;
the Ward identity and transverse physical quotient;
the derived charged spectrum and every threshold entering the response;
decoupling and matching rules;
```

V011:1630-1635 additionally requires:

```text
the Ward identity, gauge-parameter independence on the
physical quotient, regulator independence, threshold matching, and a finite
path-independent limit
```

### Exact derivations still required

Derive:

1. the complete target-independent charged transition amplitude with every
   source, gauge/constraint/edge, state, measure, and regulator sector;
2. its Ward identity and transverse physical quotient;
3. the charged spectrum and every threshold used by the selected response;
4. regulator removal, locality, decoupling, and matching;
5. one nonzero-momentum scalar response at a predeclared species, kinematic
   point/domain, normalization, and unit;
6. its theory uncertainty and canonical A32 candidate map;
7. a candidate-compatible published same-alpha comparator; and
8. the immutable prediction map.

V011:2207-2213 expressly preserves the ceiling:

```text
The completed branch would not by itself determine:
...
the full physical charged spectrum unless Q_spec derives it;
...
or the full matter spectrum.
```

### Q-21 dependency

The Q-21 parent functional/measure is a major current-path component of the
complete amplitude, but it does not itself derive the charged sectors,
spectrum, thresholds, Ward quotient, regulator removal, or matching.
Classification: **CURRENT-PATH COMPONENT; Q-21 NECESSARY ON THAT PATH BUT NOT
SUFFICIENT.**

## Family 6: charged magnetic form factor / anomaly

### Existing state

`BID_CONSOLIDATED_HOSTILE_AUDIT_MATRIX_V005.md:71`:

```text
No finite-cell frequency, anomalous moment, mass, or alpha evidence is claimed.
```

V011:1061-1065 blocks the available relabeling shortcut:

```text
It is not an anomalous magnetic
moment and is not extracted from a scalar effective action.
```

### Exact derivations still required

For the one comparator-ready subcase, the electron anomaly:

1. derive the complete charged spin-dependent on-shell response to an external
   magnetic field in the zero-momentum-transfer limit from the sealed complete
   charged theory output;
2. prove the Ward/gauge, physical-quotient, regulator-removal, infrared, and
   normalization conditions needed to identify the dimensionless electron
   anomaly;
3. show that no primitive scalar coefficient has merely been renamed;
4. produce the theory uncertainty and deterministic map to the dimensionless
   canonical electron-anomaly record;
5. bind the fixed comparator payload in
   `STAGE8_SLOT18_COMPARATOR_PREREGISTRATION_V001.md`; and
6. seal the immutable prediction map before candidate selection.

The external comparator's operational definition is supported by Aoyama et
al., arXiv:`2412.06473`, Introduction:

```text
The QED contribution to a_e can be obtained by considering the scattering
amplitude of an on-shell single electron by an external magnetic field in the
zero-momentum transfer limit.
```

Publication URL: `https://arxiv.org/html/2412.06473v3`.

### Q-21 dependency

No current cleanroom carrier writes the direct dependency edge for this
form-factor derivation. Physically and procedurally it is downstream of the
complete charged branch in Family 5, for which Q-21 is a current-path
component. Classification: **DIRECT EDGE MISSING; PROCEDURALLY DOWNSTREAM;
Q-21 NOT SUFFICIENT.**

## Reachability summary

```text
theory_side_result_exists_without_Q21:
  causal_record = true
  finite_holonomy = true
  temporal_connected_kernel = true
  flux_retarded = false
  charged_spectrum_response = false
  magnetic_form_factor = false

slot18_structure_sensitive_prediction_reachable_now:
  causal_record = false
  finite_holonomy = false
  temporal_connected_kernel = false
  flux_retarded = false
  charged_spectrum_response = false
  magnetic_form_factor = false
```

No absence statement above is corpus-wide. It is bounded to the named live
family carriers, the current supervision Q-21/Q-22 rows, and the Slot-18
inventory. No Q-21 construction artifact is modified, staged, or claimed as a
derived dependency theorem.

## Status

```text
RETURN_VERDICT = MISSING SPECIFICATION
families_mapped = 6
prediction_derivable_now = false
prediction_attempted = false
candidate_eligibility_asserted = false
unused_defined = false
measured_value_comparison_performed = false
significance_evaluated = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
