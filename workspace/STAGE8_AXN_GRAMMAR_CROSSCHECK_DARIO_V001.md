# STAGE 8 / [PLAN:AXN-BOUND-3] — CROSS-CHECK OF THE MEMBER GRAMMAR: THE COMPLETION CAMPAIGN'S GATE
## DARIO LANE (Builder B, independent verifier) — V001

RELAY 818. Lane guard PASS (header names DARIO). Inbox
`RELAY_PASTE_818_GRAMMAR_CROSSCHECK_DARIO_V001.md`
= `c27894922daadff182f3e96e3541af390259bf3f4c4b9c3a0cd9a27e47fab94b`, seal verified BEFORE
reading. State-brief pinning: `supervision/PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`,
verified and read before task work.

SUBJECT (builder-never-verifies), both verified at their adjacent seals BEFORE reading:

| role | file | SHA-256 (16) | seal |
|---|---|---|---|
| subject, relay 814 | `STAGE8_AXN_MEMBER_GRAMMAR_CODEX2_V001.md` | `a036bcca07e8405c` | `.md.seal` OK |
| governing, relay 812 | `STAGE8_AXN_FAMILY_BOUNDING_CODEX2_V001.md` | `a449cfb31b4c9656` | `.md.seal` OK |

GATES DECLARED AND HELD: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No selector; no member authored or bound; no completion
attempted; no smooth import; no EM identification; no fixed-point execution; no end test; no
numeric evaluation of physical quantities; no comparison to measured constants. PE-1..PE-11
pointer-only — recorded, not opened, not consulted. Builder-B independence held: no
`evaluator_build_A/` or `checks/` file was opened (46 such paths were digest-indexed only, never
read). No register, plan, tracker, git action.

**ALL HEADLINE ITEMS ARE CLAIMED.**

---

## 0. METHOD, AND A DISCLOSURE ABOUT IT

Three things about how this cross-check was produced, stated first because they bear on how much
weight each finding should carry.

**(0a) The span base was verified deterministically and by me.** I built a SHA-256 index of all
5,512 archive files, resolved each of 814's 21 cited sources by digest, and recomputed all 34 span
digests from live bytes. No part of that is delegated or inferred.

**(0b) A parallel multi-agent review was run, and it is not the author of any finding here.**
Every claim that survived into this artifact was re-verified by me against raw bytes. Two of the
review's verdicts did **not** survive that check and are recorded as refuted in §1.5 — including
one `material` overreach call against 814 that the decline register itself disproves. The review's
value was coverage and lead generation, not adjudication. I adjudicate.

**(0c) OWN-CORPUS DISCLOSURE — 4 of the 34 spans are from my own lane.** A01, A02 (assembly V005)
and A13, A17 (tower contest; 809 action-as-role) are DARIO artifacts. When I confirm those four I
am partly confirming my own prior work, and that is weaker corroboration than the other thirty.
Stated rather than presented as thirty-four independent confirmations.

---

## 1. AS1 — THE SPAN BASE

### 1.1 Digests: 34/34 byte-exact

All 21 cited sources resolved uniquely — **no digest-prefix collisions**, and no cited source
resolves into Builder-A territory. All 34 span digests recomputed from live bytes:

```text
A01..A17  (17 governing spans, digests taken from 812 §1.1)   17/17 MATCH
W01..W17  (17 widened spans, digests displayed in 814 §2.2)   17/17 MATCH
                                                     TOTAL   34/34 MATCH, 0 mismatch
```

Structural checks I added beyond the relay's ask: the 34 spans are **pairwise disjoint**, every
span lies wholly inside its file, the A-source set (11 files) and W-source set (10 files) are
**disjoint**, and `SPAN_CENSUS = 17 + 17 = 34` is arithmetically correct with no double-counting.
Where two spans share a source they are adjacent or separated, never overlapping. **The span base
is sound.**

One widening in 814's favour: at W14 it takes `closespec [10755,12717)` where 812 took
`[10755,11784)` — 933 bytes further, capturing the I2 base-equivalence section. That is a
substantive improvement over its governing document, and it is what makes 814's `E_phys`
rendering correct (§3.1).

### 1.2 A custody distinction 812 drew and 814 erased — CLAIMED

Law-8 probe (all three modes) over the 21 sources returns **19 sealed, 2 unsealed**:

```text
adjacent seal (.md.seal or .seal)   17
packet-manifest membership           2   (R3 result; architecture adjudication)
UNSEALED after all three modes       2   (public charged-action gate; PRIN)
```

This is not a defect in 812. 812 §0 discloses it exactly, in its own words: these are
*"content-pinned mirrored authorities … neither source's custody is upgraded beyond that pin."* I
verified both pins independently and they hold — `1fae8b74…` is pinned in the sealed assembly
review (and two further sealed files), `ae1d0492…` in seven sealed files including three Codex 2
cross-checks. **Content-pinning is a real and legitimate fourth custody mode, and 812 handled it
honestly.**

**814 does not restate it.** Its §0 certifies custody for the ten widened-probe authorities
("all matched their adjacent seals" — accurate, I re-checked all ten) and says nothing about the
A-source custody, deferring wholesale to 812. The result is that **3 of the 34 spans (A03, A04
from the public gate; A14 from PRIN) sit in a uniform table at apparent parity with
adjacent-sealed spans**, and a downstream consumer reading 814 alone inherits a custody claim its
own governing document declined to make. Not a refutation of the spans; a flattening of their
provenance. **CORRECTED.**

Related and minor: 814 asserts the A-spans "were re-read here" but displays no digests of its own
for them, so that verification claim is unauditable from 814 alone. I audited it. **The claim is
true** — all seventeen match.

### 1.3 A11 — MISCITED (verified, exhibit displayed)

814's A11 row reads the span as *"geometric orthogonality is not automatically an action."*

Direct byte test on `BOUNDARY_RECORD_ONSET_SATURATION_ACTION_GATE_V003.md [1952,2636)`, sha
`1ea9e09c21653837`:

```text
b'action'    in span  ->  False
b'geometric' in span  ->  False
```

What A11's span actually says, verbatim in full:

> *"Global orthogonality is not yet a durable public record. Perfect readout from the designated
> record subsystem requires orthogonal supports of its reduced conditional states. Durability
> additionally requires persistence and recoverability or redundancy."* — then the adopted onset
> rule and `J_FS,rel = pi hbar/2`.

The content 814 attributes to A11 **is of record — one span over.** `A12 [3371,4269)` contains
verbatim *"No geometric uncertainty budget may be substituted for physical action without a
theorem"* and lists *"identification of J_FS,rel with microscopic dynamical action"* under **Not
established**.

Diagnosis: 812's A11 row bundles a **conjunction** — (i) "orthogonality budget is geometric, not
automatically an action" and (ii) "public record needs reduced-support orthogonality plus
persistence/recoverability". Only (ii) is in the span. 814 inherited the row and carried forward
**only clause (i) — the half that is not there** — dropping the half that is. No sealed claim is
invented; the span→claim mapping is wrong.

This is the **fourth conjunction failure of record** in this program (742, 788, 801, now 814/A11),
and it carries a general lesson worth stating plainly: **34/34 digests matching proves the bytes
are what they were. It does not prove the bytes say what the table says they say.** Digest
verification and licensing verification are different checks, and A11 is the case that separates
them. **MISCITED — material for citation hygiene, immaterial for the substantive verdict.**

### 1.4 W11 — imprecise in BOTH directions (verified)

814 §4.3 states *"W11 also records that H1–H8 are not discharged for the completed corpus."*
The span, `hdthm [6621,8622)`, sha `836b7918968353eb`, is a **differentiated ledger**, verbatim:

> *"**H1 fails** as a discharged corpus premise … **H2 is unsealed** … **H3–H6 are unbuilt** at
> the complete level … **H7 is the strongest native support.** It blocks a field redefinition that
> rescales incidence weights or changes holonomy merely to remove an operator … **H8 is absent**."*

So the blanket rendering is wrong in both directions at once: H1 does not merely fail to be
discharged, it **fails**; and H7 is not undischarged, it is the theorem's **strongest supported
hypothesis**. Direction of consequence: the understatement concerns a relation 814 declines to use
anyway, so no verdict moves. **CORRECTED.**

### 1.5 Two review verdicts I do NOT sustain

**A15, called `OVERREACH / material` — REFUTED.** The claim was that A15 is a second-hand citation
(the held-fixed file reporting what `BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md`
records) and that 814 treats it as primary. The bytes support that description. **The decline
register disposes of it.** `DECLINE_REGISTER_V002` (`957476c8c605a370`) pins row S34 to:

```text
source:   STAGE8_HELD_FIXED_BRANCH_DERIVABILITY_AND_AXIOM_PRICE_V001.md (39f7ea6d95dcdad3…)
span:     [3765,4145)
span sha: 80ef473adc99fea95b682ad7be25e67ffeb2e0c16fd12c3743ee069455431e3f
```

That is **exactly** 814's A15, byte for byte. 814 did not pick a weak citation; it used the
registrar's own canonical source and span for S34. The attack failed because it skipped the
register grep — which is precisely what **RULING-LAST** exists to prevent, and a clean vindication
of that law.

*Residual, flagged not adjudicated and expressly NOT charged against 814:* the register's own
designated S34 source is itself a reported citation of `BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md`,
which is present in the archive (`8f9bdd93cd32fbf1`) and **unsealed in both sidecar modes**. That
is a provenance question about the decline register's chain. Registrar-shaped.

**A02, called `OVERREACH / minor` — NOT SUSTAINED.** The span displays release branches (a)/(b)/(c)
and *"with the family first bounded"* verbatim. 814's one-line reading is a fair compression.

### 1.6 MISSED SPANS — six sealed sources, all seal-verified, none cited — CLAIMED

This is the largest AS1 finding. Each is sealed, each bears directly on 814's subject, and 814
cites none of them.

| # | source | SHA-256 (16) | seal | why it bears |
|---|---|---|---|---|
| M1 | `STAGE8_ACTION_COMPLETION_COUNT_ERRATUM_V001.md` | `ee2d9490c4759078` | OK | corrects the exhibited-completion count (§2.1) |
| M2 | `STAGE8_P5_FAMILY_EXCLUSION_THEOREM_ATTEMPT_V001.md` | `00300b0a888fd3ef` | OK | the P5 exclusion theorem, attempted and diagnosed |
| M3 | `STAGE8_ROUTE1_CUTOFF_EXISTENCE_AUDIT_V001.md` | `66d40ccd0cf5ea62` | OK | dedicated audit of all-orders closure route 1 |
| M4 | `STAGE8_ROUTE4_NATIVE_FINITE_ALGEBRA_THEOREM_AUDIT_V001.md` | `4065fdcc77211d27` | OK | dedicated audit of route 4 |
| M5 | `STAGE8_SECTION_5_3_UNIQUENESS_GATE_PASSABILITY_DETERMINATION_V001.md` | `728543cb0d06b259` | OK | whether §5.3's gate is passable at all |
| M6 | `STAGE8_SECTION53_ADMITTED_FAMILY_CENSUS_REACHABILITY_AUDIT_V001.md` | `4411b91a9081b146` | OK | **enumerability of 814's exact class** |

### 1.7 The pattern behind the misses — CLAIMED

The six misses are not random. Together with three under-read spans that 814 *did* cite, they show
one habit: **814 mines its sources for their negatives and leaves their positive closure
specifications behind.** Byte-verified instances:

1. **W02** — 814 quotes the cardinality sentence and leaves, *inside the same cited span*, the
   four-part freeze specification `[3187,~3480)` sha `ee98e783d0dd6922` and the membership rule
   `[2973,3212)` sha `f2835402ccd337a2`: *"The family therefore consists at minimum of `S_0`,`S_1`
   … no later member may be silently added after a root."* A sealed **lower bound** and a sealed
   **anti-postselection rule**, both uncarried.
2. **A06** — 814 takes "no declared minimality" and leaves three **named sufficient outcomes** for
   closing step 5 (a first-order minimality theorem; a total-space geometric action with all
   charged vertices as fixed components of one connection; a finite quantum-record update unique
   up to unitary equivalence).
3. **W13** — 814 takes the no-outside requirement and leaves two sealed **sufficiency** results:
   `exhaustive_generation_rule_suffices_for_Section_5_3 = true` and
   `exhaustive_bound_with_no_outside_decision_suffices_for_Section_5_3 = true`.

The consequence is not that 814's verdict is wrong. It is that **the missing object is far better
specified in the record than 814's F0_ABSENCE_2 reports.** That is an AS4 result and is carried
there (§4).

---

## 2. AS2 — THE FORM/SCHEMA SEPARATION

### 2.1 REFUTED, with exhibit: the concrete category is not two — CLAIMED

814 states the concrete layer as *"the two parent-action controls `S_0,S_1`"* (§1 item 1, §3.2 G1,
§5.1) and rests it on W02's *"Known concrete members: **2** (`S_0`, `S_1`)."*

**A sealed archive erratum says otherwise.** `STAGE8_ACTION_COMPLETION_COUNT_ERRATUM_V001.md`,
`ee2d9490c4759078`, **`shasum -c` OK**, status `APPEND_ONLY_ERRATUM`. Span `[2120,2474)` sha
`1dceb9fdef9939a0`, verbatim:

> *"The exhibited parameter-free parent-action completions with displayed action terms **are not
> two. They are at least four**:  `S_0` `S_1` `S_rot` `S_BF`.  This correction is lower-bounded
> only. The larger equivalent mutation class remains unbounded and unenumerated."*

and in its flag block:

```text
exhibited_parameter_free_parent_action_completions_corrected = at_least_four
previous_two_completion_count_stale = true
```

Three facts make this decisive rather than a curiosity:

- **Its correction target is a file both lanes verified.** The erratum names
  `STAGE8_ACTION_FORM_UNDERDETERMINATION_INVENTORY_V001.md` at `cafad33ed99b04b5…`. That is the
  same "action inventory `cafad33e…`" in 812's custody table and in 814 §0. **Both lanes verified
  the custody of the file this erratum corrects, and neither carried the erratum.**
- **The correction is still live.** The inventory's live digest today is `cafad33ed99b04b5` —
  byte-identical to what the erratum corrected. The stale count has not been repaired at source.
- **The erratum is cited by nothing.** `grep` for `ACTION_COMPLETION_COUNT_ERRATUM` across the
  entire archive returns **zero references**, and nothing anywhere re-corrects the count back to
  two. It is an **orphaned sealed correction** — a correction that propagated to no consumer. That
  is exactly the failure mode **law 7 (CORRECTION PROPAGATION)** was created to prevent, and law 7
  exists because of a disclosure I made at 781. Here is a sealed correction that never propagated,
  and two subsequent relays re-published the stale count.

**Scope caveat, stated because it limits the finding.** `S_rot` and `S_BF` are typed by the
erratum as *"Two Additional Direct-Ancestor Completions"*, exhibited in a **cleanroom v001** file
outside this archive and cited by content-pin (`b3f96a02b1b70ed3…`). ARCHIVE-SIDE ONLY bars me from
opening it, so I verify the erratum, not its cleanroom exhibit. And whether **ancestor-lineage**
completions are admitted members of the *current* family is a typing question the erratum does not
settle. What is refuted of record is the **count of exhibited parameter-free parent-action
completions with displayed action terms**, not necessarily the membership of the current family.

Direction: this **strengthens** 814's headline. A sealed lower bound of ≥4 rather than 2 makes the
family strictly larger of record, so `CENSUS = OPEN OF RECORD` survives *a fortiori*. What fails is
the grammar's concrete layer as stated.

### 2.2 The "concrete" category contains an undefined functional — CLAIMED

`S_0 = ∫d⁴x √-g  i ℏ ψ̄ γ^μ D_μ ψ + S_record[R,a,g]`. 814 reproduces this byte-faithfully from
A05 (I compared them). But **`S_record[R,a,g]` is never given a form.**

Wide probe over 5,512 files, six spellings: the bracketed functional occurs in exactly **three**
files — the gate A05 itself, the action inventory, and 814. Every other `S_record` occurrence in
the corpus is `Delta S_record`, a **relative action marker** (a scalar, `= pi hbar` or `E_R T_R`),
a different object whose own bridge is repeatedly declared unclosed.

So the "concrete" control is concrete in its **source sector only**; its record sector is a named
placeholder, and `S_1` inherits this since `S_1 = S_0 + Pauli`. What the record fully exhibits is
the **difference** `S_1 − S_0` — the Pauli term with its fixed integer coefficient.

This is corroborated by 814's own cited source: W02's field table types the two displayed actions
as **`partial`** in domain/codomain and in source/record embeddings. **814's split is binary; the
record's is not.** The concrete column is itself partial, and 814 does not say so. **CORRECTED.**

### 2.3 `derived=false` exclusions — CONFIRMED correctly excluded

W05/W06 bar the promotion in their own voice: *"a constructed **candidate manifest**, not a census
freeze. All newly written members are marked `derived = false` and are structural schemas only"*
and *"with the displayed forms treated as **CHOICES**, not corpus derivations."* 814 excludes
`S_HD[n,c]`, `U_Δ^ret`, `U_Δ^loc`, `O_curv`, `O_diss` on exactly that ground. **Correct, and
correctly sourced.** No candidate that the census build actually derived is excluded, because it
derived none.

### 2.4 A number that is not of record — CORRECTED

814 §1 pass-3 table, row 3: *"the **146** candidate formulae are the grammar."* No sealed source
counts 146 candidate formulae. The census build displays exactly **five** (`S_HD[n,c]`, `U_Δ^ret`,
`U_Δ^loc`, `O_curv`, `O_diss`). 814 itself writes **`Q-146`** correctly twice (§0 and its register
list, *"Q-146 candidate census written, explicitly not frozen and not derived"*) before dropping
the `Q-` once. A dropped-prefix slip — but it deposits a fabricated-looking **count** in a table a
downstream consumer would mine for counts, in a program whose whole discipline is numbers-of-record.
The row's verdict (`BARRED`) is unaffected and correct.

---

## 3. AS3 — THE EQUIVALENCE LAYERS

### 3.1 CONFIRMED — no unlawful promotion survives. Both attacks failed.

**Transport → action equivalence: fenced four times.** Pass-3 table (*"common-refinement equality
is complete-action equivalence | **TYPE ERROR**"*); §4.3 (*"It does not say the underlying complete
actions are equivalent"*); §5.1 (*"The `1887` remains only the finite transport projection's
dimension. It is not the family dimension"*); §7 disclosure 4. I scanned 814 for any use of a
transport conclusion to support an action-level claim and **found none**. Clean.

**`~_R` conditionality: preserved four times.** §4.1 (applicability underived); §4.3 (*"assembled
as a conditional relation and cannot be used here to erase `S_1-S_0`, the public stiffness ray, or
an unspecified open-class member"*); §4.4; §7 disclosure 3. Clean.

**A concern I raised and then withdrew on checking.** 814 renders `E_phys` as a five-term union
including *"any additional null transformation actually derived by the completed problem"*, where
W14's Amendment-001 text is a **prohibition** (may not call a modulus null absent a derivation). I
suspected a re-typing of a bar into a permissive conjunct. **W12 settles it against me**: the
sealed spec itself defines *"Let `~_phys` be the live physical equivalence relation … gauge, public
isometry, charge-conjugate orientation, Boundary-Resolved equivalence, and any additional null
transformation derived by the completed problem."* 814's rendering is the spec's own, and its added
word "actually" tightens rather than loosens. **Concern withdrawn; recorded because it was the
strongest available line of attack on this section and it failed.**

**S25** verified in `DECLINE_REGISTER_V002`: 814's use is faithful; the `K_R` ray is not collapsed
and no equal-action route is taken.

### 3.2 One correction inside the confirmation

The W11 misrendering of §1.4 sits in this section. `EQUIVALENCE = assembled` is itself
well-qualified in 814's final line (it says "conditional" and "global action-null extension
absent"), so the headline does not overstate. The defect is confined to the blanket "H1–H8 are not
discharged" sentence. **EQUIVALENCE = CONFIRMED, with that correction.**

---

## 4. AS4 — THE OPEN-OF-RECORD VERDICT

### 4.1 The three load-bearing quotes are verbatim accurate

I checked all three against bytes. W02's *"Therefore total cardinality is not fixed"*, W09's *"do
not give a finite enumeration, a generator, a coverage proof, or an equivalence relation that
collapses P5 variants"*, and W14's *"Current sealed text does not supply"* the complete action
universe are each **quoted accurately**. (814 silently elides an internal file-citation inside the
W02 quote; unmarked, immaterial.) W16, W13, W12, W07, W05, A05, A01, A02, A03, A04, A06 all
**LICENSE** their claimed contributions on my own reading.

### 4.2 CONFIRMED absent — nothing is PROVABLE

I hunted for a sealed uniqueness theorem, a sealed finiteness result, or a sealed exclusion theorem
that would supply any part of the universe/generator/descent/no-outside decision. **None exists.**
Fixed-order HD finiteness is not a candidate: W11 conditions it on H1–H8, and W11's own adversarial
section reports H1 failing, H2 unsealed, H3–H6 unbuilt, H8 absent. **No piece is provable. The open
verdict stands.**

### 4.3 SHRUNK in specification — the false-boundary distinction cuts against 814's presentation

The relay asks me to separate **absence-of-statement** from **unattempted-proof**. On that axis 814
is materially misleading, and this is the substantive AS4 result. Four pieces 814 presents as
absent are, of record, **attempted, executed to a named stopping point, typed, and carrying sealed
release conditions**:

**(a) The P5 exclusion theorem — attempted, stopped at a named step.** M2, `[319,976)` sha
`19c66682740aaf1e`: *"**Coverage fails. The P5 census freezes seven ledger rows** … two baseline
roles, one adopted state/contour branch, and four open schemas. **Zero rows carry a complete
certified state/effect/domain/dynamics descent presentation.**"* — with `P5_frozen_row_count = 7`,
and at `[1286,1556)` sha `e771404d45a8a1f7`:

```text
P5_exclusion_theorem_completed = false | TYPE-C |
  constraint: forcing-protocol STEP 4 coverage failed;
  release: prove package-level coverage without adding or deleting members
  after any downstream output is known
```

**TYPE-C with a sealed release condition** — not TYPE-U, not absent. And the artifact fences itself
correctly: *"This is not a theorem that multiple inequivalent P5 packages physically survive. It is
a theorem-attempt failure caused by an uncovered quantification domain."*

**(b) All-orders routes 1 and 4 — separately audited.** 814 says of W11's four routes *"None is
presently sealed."* True as to the routes; false as to the record. M3 diagnoses route 1
structurally (the tower is *"an external/mixed object"*, not internal to the finite record
complex). M4 returns *"Route 4 does not close on the current corpus"* — but with a sealed
**positive**: `route4_native_skeleton_found = true`, and *"The native skeleton is real"*, naming
five components (universal incidence algebra, augmentation-ideal filtration, represented filtration
at finite order, degree-one primitive/single-operator completeness postulate, quasi-free CAR lift).

**(c) §5.3 passability — determined.** M5: *"The broad tension is over-identified, but the gate is
still not executable"*, and the admitted family is **not** the whole allowance envelope (eight axes
named).

**(d) Enumerability — already determined, and 814 asserts it unaided.** 814's pass-3 row *"failure
to find a finite generator proves non-enumerability | REFUTED BY SCOPE: no impossibility theorem
against future exhaustive generation is sealed"* is asserted on 814's own authority. **The record
has a sealed typed determination for it.** M6, `[223,544)` sha `f06c099fa828369b`: *"The evidence
supports **NOT YET ENUMERATED**, not 'not enumerable' … Section 5.3 is therefore reachable in
principle, but not executable today"*, with
`OPEN_ACTION_FORM_not_enumerable = false | TYPE-S`. **814's conclusion is better supported than
814 knows.**

**Net.** `CENSUS = OPEN OF RECORD` and `no-outside = FAILS OF RECORD` are **CONFIRMED as
outcomes**. But "FAILS OF RECORD" is the right phrase for the *decision* and the wrong frame for
the *record*: three named routes and the P5 exclusion were attempted and diagnosed, one of W02's
four freeze requirements (an adversarial omitted-member test owned by a named reviewer) **exists of
record as the very W07 source 814 itself cites** — lane CODEX 1, whether a lane satisfies "a named
reviewer" being a registrar question I mark and do not rule — and the enumerability question is
already typed. **Object 1 does not shrink in existence; it shrinks substantially in
specification**, and 814's F0_ABSENCE_2 does not carry that.

---

## 5. FREEDOMS-CONSUMED (mandatory, law 2 / law 2a)

```text
CARRIED, NOT CONSUMED:
  the action family, every member          NONE BOUND, NONE SELECTED, NONE AUTHORED
  G0 / the K_R ray, S_0, S_1               CARRIED AS THE SUBJECT'S OBJECTS; not adopted
  S_rot, S_BF                              CARRIED AS THE ERRATUM'S COUNT ONLY; not typed into
                                             the current family, not adopted as members
  G_named, M_F2..M_P5                      CARRIED AS 814'S NON-EXHAUSTIVE AUDIT NAMES
  ~_R and H1-H8                            CARRIED AT SEALED SCOPE, hypotheses NOT discharged
  the 1887 transport stratum               CARRIED AS A TRANSPORT DIMENSION ONLY
  route4_native_skeleton_found = true      CARRIED AS AN AUDITED STATUS FLAG (see S12 below)
  the four all-orders routes               CARRIED AS UNCLOSED
DERIVED HERE:                              nothing. This is a verification artifact.
COMPUTED HERE:                             34 span digests; 1 archive-wide digest index;
                                             2 byte-membership tests; 6 seal probes. No physical
                                             quantity, no member, no coefficient.
SCALING WEIGHTS (law 2a):                  NONE CONSUMED. No weight, multiplier, coefficient or
                                             normalization is fixed anywhere in this artifact.
DECLINED:                                  promoting the review's A15 overreach call (refuted by
                                             the register); promoting its A02 call (not sustained);
                                             reading the cleanroom exhibit behind S_rot/S_BF
                                             (ARCHIVE-SIDE ONLY).
SUBSTITUTED:                               NONE.
```

## 6. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

All 37 rows walked before sealing. Live intersections and their discharge:

- **S12** (*status flags are never substituted for member manifests, generators, or proofs*) —
  **LIVE AND LOAD-BEARING HERE.** My §4.3(b) finding cites `route4_native_skeleton_found = true`,
  which is a **status flag**. I carry it strictly as evidence that an audit was run and returned a
  positive, and **not** as a generator, a manifest, or a proof. The distinction is the whole point
  of the finding: an audited positive is not a supplied object. **Discharged explicitly.**
- **S34** — used only to *adjudicate* the A15 citation via the register's own pinning; not
  activated as a selector or a result.
- **S25** — verified as honored by 814; not invoked by me.
- **S03, S26, S28** — carried as typed; no undecidable in this artifact is rescued by reaching for
  an axiom. Every gap I report is reported as a gap.
- **S08, S10, S14/S15/S23, S19/S24, S22, S36/S37** — untouched.

`FLATTENING_CHECK = clean (37/37 rows walked; S12 live, explicit, and discharged).`

## 7. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) MY STRONGEST FINDING CAME FROM A DELEGATED HUNT, NOT FROM ME.** The completion-count erratum
(§2.1) — the only refutation in this artifact — was surfaced by the parallel review, not by my own
sweep. I verified it end to end and I own the verdict, but I did not find it. My own widened probe
ranked sources by signal density and put the erratum nowhere near the top, because a 3,062-byte
erratum has low signal density by construction. **My probe design has a size bias against exactly
the artifact class most likely to carry a correction.** That is a real instrument defect and it is
the fifth consecutive relay in which my instruments, not my reasoning, were the weak part.

**(2) I REFUTED TWO OF THE REVIEW'S FINDINGS, INCLUDING ITS ONLY `material` ONE.** Reported because
a cross-check that passes through everything its helpers say is not a cross-check. The A15 refutation
came from the register — the instrument RULING-LAST mandates and the review skipped.

**(3) FOUR OF THE 34 SPANS I VERIFIED ARE MY OWN LANE'S.** A01, A02, A13, A17. Confirming them is
weaker corroboration than confirming the other thirty, and my confirmations there are not
independent of me.

**(4) A CWD ARTIFACT PRODUCED TWO FALSE SEAL FAILURES DURING THIS RELAY**, one of them on the very
file that became finding M2. `shasum -c` run from the archive root reports `FAILED open or read`
for sidecars containing bare filenames. Both times the seal was **fine**. Had I reported either at
first sight I would have filed a false custody break — the exact error I made at 792 that created
law 8. Law 8 caught it twice. **Recommendation, not a ruling: law 8's three modes should be
executed from the file's own directory, because the probe itself can manufacture the failure it is
testing for.**

None of the four flatters this artifact's conclusions. The subject's central verdicts survive my
attacks; what I add — one sealed refutation it missed, an orphaned correction, six uncited sealed
sources, and a systematic negatives-only reading habit — all cuts against the subject, and I have
said plainly where my own attacks failed (§3.1) and where my instruments did (§7.1, §7.4).

---

```
SPAN_BASE = CONFIRMED (34/34) / issues (listed) + missed spans: 6
  DIGESTS: 34/34 recomputed byte-exact from live bytes; 21/21 sources resolved with no prefix
    collision; spans pairwise disjoint; A-set and W-set disjoint; 17+17=34 arithmetically correct;
    no double-counting; no cited source in Builder-A territory.
  ISSUES (4):
   (i)   CUSTODY FLATTENED: 3 of 34 spans (A03, A04 = public gate; A14 = PRIN) rest on
         CONTENT-PIN custody, not an adjacent seal. 812 disclosed this verbatim ("neither source's
         custody is upgraded beyond that pin"); 814 does not restate it and tables all 34 at
         apparent parity. Both pins independently re-verified by me and they HOLD.
   (ii)  A11 MISCITED (exhibit): byte test on [1952,2636) returns b'action' -> False and
         b'geometric' -> False. 814 reads it as "geometric orthogonality is not automatically an
         action"; that sentence lives in A12, a span 814 also cites. 812 bundled a conjunction,
         814 inherited the half that is NOT in the span and dropped the half that is. FOURTH
         conjunction failure of record (742, 788, 801, 814/A11). Lesson: matching digests prove
         the bytes are unchanged, NOT that they say what the table says.
   (iii) W11 IMPRECISE IN BOTH DIRECTIONS: 814's "H1-H8 are not discharged" flattens a
         differentiated ledger — H1 FAILS, H2 unsealed, H3-H6 unbuilt, H8 absent, but H7 is "the
         strongest native support."
   (iv)  A-span re-reading asserted without displayed digests, so unauditable from 814 alone. I
         audited it: the claim is TRUE, 17/17.
  TWO REVIEW VERDICTS NOT SUSTAINED: A15 "OVERREACH/material" REFUTED — DECLINE_REGISTER_V002 pins
    S34 to exactly that file, span [3765,4145) and span sha 80ef473a…; 814 used the registrar's own
    canonical citation. A02 "OVERREACH/minor" not sustained.
  MISSED SPANS = 6 sealed, seal-verified, uncited sources: completion-count erratum (ee2d9490),
    P5 exclusion theorem attempt (00300b0a), Route-1 audit (66d40ccd), Route-4 audit (4065fdcc),
    §5.3 passability determination (728543cb), census reachability audit (4411b91a).
  PATTERN: 814 mines sources for negatives and leaves their POSITIVE closure specifications —
    W02's four-part freeze spec and "at minimum S_0,S_1 / no later member silently added after a
    root" (both INSIDE a span 814 cited), A06's three named sufficient outcomes, W13's two
    sufficiency results.

SEPARATION = CORRECTED (1 refutation with exhibit + 3 corrections)
  REFUTED, EXHIBIT: the concrete category is not two. Sealed APPEND_ONLY_ERRATUM
    STAGE8_ACTION_COMPLETION_COUNT_ERRATUM_V001.md (ee2d9490c4759078, shasum -c OK), span
    [2120,2474) sha 1dceb9fdef9939a0: "The exhibited parameter-free parent-action completions with
    displayed action terms are not two. They are at least four: S_0 S_1 S_rot S_BF", with
    previous_two_completion_count_stale = true. Its correction target is the action inventory
    cafad33e… — the file BOTH 812 AND 814 verified in preflight — whose live digest still matches,
    so the stale count is unrepaired at source. AND THE ERRATUM IS CITED BY NOTHING: zero
    references archive-wide, nothing re-corrects to two. An ORPHANED SEALED CORRECTION — the exact
    failure LAW 7 exists to prevent, and two later relays re-published the stale count.
    CAVEAT STATED: S_rot/S_BF are "direct-ancestor" completions exhibited in a cleanroom v001 file
    outside this archive, content-pinned; ARCHIVE-SIDE ONLY bars me from that exhibit, and whether
    ancestor-lineage completions are members of the CURRENT family is a typing question the erratum
    does not settle. DIRECTION: this STRENGTHENS 814's headline — a sealed lower bound of >=4
    makes the family strictly larger of record.
  CORRECTED (a): the "concrete" category contains an UNDEFINED FUNCTIONAL. S_record[R,a,g] occurs
    in exactly 3 of 5,512 files and is given a form in none; every other S_record occurrence is
    "Delta S_record", a scalar marker — a different object. S_0 is concrete in its SOURCE sector
    only; what is fully exhibited is the DIFFERENCE S_1 - S_0. 814's own cited W02 agrees, typing
    the displayed pair "partial" in domain/codomain and source/record embeddings. The split is
    binary; the record's is not.
  CORRECTED (b): "the 146 candidate formulae" is not of record. No sealed source counts 146; the
    census build displays FIVE; 814 writes Q-146 correctly twice then drops the prefix once. The
    row's BARRED verdict is unaffected.
  CONFIRMED: the derived=false exclusions are CORRECTLY excluded — W05/W06 bar the promotion in
    their own voice ("candidate manifest, not a census freeze"; forms "treated as CHOICES"). No
    candidate the census build derived is excluded, because it derived none.

EQUIVALENCE = CONFIRMED (no unlawful promotion)
  TRANSPORT: fenced four times (pass-3 TYPE ERROR row; §4.3; §5.1 "not the family dimension"; §7
    disclosure 4). I scanned 814 for any transport conclusion supporting an action-level claim and
    found NONE.
  ~_R: conditionality preserved four times (§4.1, §4.3, §4.4, §7 disclosure 3); 814 expressly
    declines to use it to erase S_1-S_0, the K_R ray, or any open-class member. S25 verified
    honored; no equal-action route taken; the K_R ray is not collapsed.
  MY STRONGEST ATTACK ON THIS SECTION FAILED AND I REPORT IT: I read 814's five-term E_phys as
    re-typing Amendment-001's PROHIBITION into a permissive conjunct. W12 refutes me — the sealed
    spec itself defines ~_phys with that fifth term verbatim, and 814's added "actually" tightens
    it. Concern withdrawn.
  CARRIES the W11 correction above; 814's final EQUIVALENCE line is otherwise well-qualified.

OPEN_VERDICT = CONFIRMED absent / SHRUNK in specification (named routes displayed)
  CONFIRMED: no sealed uniqueness, finiteness, or exclusion theorem supplies any part of the
    universe/generator/descent/no-outside decision. NO PIECE IS PROVABLE. Fixed-order HD finiteness
    is not a candidate — W11's own adversarial section has H1 failing and H2/H3-H6/H8
    unsealed/unbuilt/absent. The three load-bearing quotes (W02, W09, W14) are VERBATIM ACCURATE.
  SHRUNK: on the relay's own false-boundary standard, 814 presents as ABSENT four things the record
    shows ATTEMPTED, DIAGNOSED, TYPED and RELEASE-CONDITIONED —
    (a) P5 exclusion theorem: forcing protocol executed, STOPPED AT STEP 4 for coverage failure;
        P5_frozen_row_count = 7; P5_exclusion_theorem_completed = false | TYPE-C | with a sealed
        release condition. TYPE-C, not absent.
    (b) all-orders routes 1 and 4: separately audited; Route 4 returns the sealed POSITIVE
        route4_native_skeleton_found = true and "The native skeleton is real", naming five
        components (carried as an audited STATUS FLAG only — S12 discharged, not a generator).
    (c) §5.3 passability: determined — "the gate is still not executable", family != envelope.
    (d) enumerability: ALREADY DETERMINED, and 814 asserts it unaided —
        OPEN_ACTION_FORM_not_enumerable = false | TYPE-S, "NOT YET ENUMERATED, not 'not
        enumerable'". 814's conclusion is better supported than 814 knows.
    Also: one of W02's four freeze requirements — an adversarial omitted-member test owned by a
    named reviewer — EXISTS OF RECORD as the very W07 source 814 itself cites (lane CODEX 1).
    Whether a lane satisfies "a named reviewer" is REGISTRAR-SHAPED: marked, not ruled.
  NET: object 1 does not shrink in existence; it shrinks substantially in SPECIFICATION, and
    814's F0_ABSENCE_2 does not carry that.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) MY STRONGEST FINDING CAME FROM A DELEGATED HUNT, NOT FROM ME. The completion-count erratum —
      the only refutation here — was surfaced by the parallel review. I verified it end to end and
      own the verdict, but my own probe ranked sources by signal density and a 3,062-byte erratum
      has low signal density by construction. My probe design has a SIZE BIAS AGAINST EXACTLY THE
      ARTIFACT CLASS MOST LIKELY TO CARRY A CORRECTION. Fifth consecutive relay in which my
      instruments, not my reasoning, were the weak part.
  (2) I REFUTED TWO OF THE REVIEW'S FINDINGS, INCLUDING ITS ONLY "material" ONE (A15), using the
      register the review skipped. A cross-check that passes through everything its helpers say is
      not a cross-check.
  (3) FOUR OF THE 34 SPANS ARE MY OWN LANE'S (A01, A02, A13, A17). Those confirmations are not
      independent of me.
  (4) A CWD ARTIFACT PRODUCED TWO FALSE SEAL FAILURES this relay, one on the file that became
      finding M2; both seals were fine and law 8 caught both. Had I reported either at first sight
      I would have filed a false custody break — the 792 error that created law 8. RECOMMENDATION,
      NOT A RULING: law 8's modes should be run from the file's own directory, because the probe
      can manufacture the failure it tests for.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
