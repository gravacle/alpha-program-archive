# STAGE 8 / CROSS-BLOCK / MIN-CARRIER-CHECK — BLIND ADVERSARIAL VERIFICATION OF THE MINIMAL BOTH-BLOCKS CANDIDATE

Lane: FABLE blind adversarial verifier, codename MIN-CARRIER-CHECK, cross-lineage.
DEFAULT = REFUTE. Date: 2026-08-13.
Under test: `STAGE8_MIN_CARRIER_CANDIDATE_V001.md`
sha256 `f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647` — recomputed
at path THIS session, MATCH vs the tasked digest, before any reliance.

## Verdict — one screen

**MC_VERDICT = SOUND.** Every attack ran to completion and none landed. The
candidate's incidence matrices were rebuilt independently from the sealed
D_square(u=1) display and agree entrywise; every block dimension, witness, null
space, and cross-form coefficient was recomputed with a DIFFERENT exact instrument
(integer Smith normal form over Z, versus the artifact's Fraction-RREF) and every
number matches. The in-artifact script reruns with exit 0 and byte-identical output.
The minimality claim survived an exhaustive hunt BROADER than the artifact's own
certificate — and came out strengthened: the census floor (F,E,V) = (1,5,4) holds
even if the sealed face-shape constraint is dropped. The gluing ledger is complete.
All 12 seals verified at path; both relied-on byte spans rehashed EXACT. The
candidate's conditionality (candidate-not-of-record; two unsupplied authored-physics
objects; unbuilt gluing license; TYPE-P premise) is carried on every load-bearing
claim. Honest outcomes preserved.

---

## 0. Preflight, seals, and fences

Output name probed before any write: `STAGE8_MIN_CARRIER_CHECK_V001.md` ABSENT
(artifact and sidecar). No register, tracker, plan, road, ledger, or lens file read.
Every digest below recomputed from bytes at path THIS session BEFORE reliance.

```text
SEALS VERIFIED (shasum -a 256 at path; all MATCH the tasked/quoted digests):
f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md              MATCH (the artifact under test)
92dba8d33b09c5cd2a9ff5dab122f2cf2d3420d521cdf422d210d988f1ab6ee9  STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md          MATCH
22f69dd3a87a1f71cc79d47606de2f547348a5619ba330cf31d53d013c67350e  STAGE8_BOTH_BLOCKS_CHECK_V001.md                  MATCH
a3bedc7ef092ebd932d254331dc10053aa123334a013b95fb1e15019bdfea016  STAGE8_B1_CELL_INSTANCE_V001.md                   MATCH
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md             MATCH
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md               MATCH
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md                MATCH
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                     MATCH
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md    MATCH
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md     MATCH
1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb  STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md    MATCH (FN)
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md              MATCH (FORM)
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md                   MATCH (GC)

BYTE SPANS rehashed THIS session before extraction:
FN   [20396,20529)  f7c7261e6444057eb63fe7d139b4351092d86f4d18b91f2eb1857252bd0c060a  EXACT
     (133 bytes; verbatim finite-N cross form + all-pairs kernel C_jk = mu — matches
      the artifact's quotation word for word)
V011 [30559,30843)  96e164afdbddafea6f26832db83b8ebd69de637e0d8994b9e58bb63872212370  EXACT
     (the frozen plaquette boundary word: partial_2 f = e_mu(x) + e_nu(x+mu)
      - e_mu(x+nu) - e_nu(x) — four DISTINCT edges, sign shape (+ + - -); this span's
      digest is also independently pinned inside sealed 22f69dd3 :62)
UNVERIFIABLE: none.
```

FENCES HELD BY ME: exact structural algebra only (integers and exact Fractions); no
physical quantity computed, bounded, or evaluated; mu/n/Phi left symbolic; no scale,
no imported GR, no faithfulness authority; no continuum object consumed; no
register/tracker/plan/road/ledger/lens file read. K_dd treated as a CANDIDATE object
at every step. MACHINERY_USED_BY_ME = no.

---

## 1. ATTACK 1 — CONFORMANCE: every sealed constraint re-checked at bytes

```text
C-1  B3 / K_SQUARE VERBATIM.  V011 :1840-1851 re-read: 4 named vertices, 4 named
     oriented edges (e_a0: v_00->v_10; e_0b: v_00->v_01; e_ab: v_10->v_11;
     e_ba: v_01->v_11), "no 2-cell".  Frozen bases :1896-1901; sealed incidence
     D_square(Phi) :1903-1911 — I rebuilt the incidence from the vertex/edge display
     alone and it equals the sealed matrix at u = 1 ENTRYWISE, and the candidate's
     first four columns equal it, with column e_0b' = copy of column e_0b as claimed.
     The ordered loop word :1860-1863 (u_ab u_a0 (u_ba u_0b)^(-1)) abelianizes to
     gamma_j = (+1,-1,+1,-1) in the frozen C_1 order — the candidate's gamma.  PASS.
C-2  B3 / SECTOR PIN.  V007 :162-164 re-read: "connected admitted cells with b_1 = 1
     — the specified square sector, member 10 :1840-1852"; :170-171: "DISPOSITION OF
     b_1 > 1: OUT OF SCOPE ... would require a lawful cycle selector".  Addendum
     :33-34 carries the same operative domain.  a3bedc7e §1.6 re-read: the span
     "pins the census EXACTLY ... CONFORMANCE OVER CONVENIENCE" — abstractly smaller
     b_1 = 1 complexes are barred as trace realizations.  The candidate's use of the
     pin (rejecting W1 and KLf) is the sealed ruling, correctly applied.  PASS.
C-3  B3 / F = 0 ON THE TRACE CARRIER + CLASS SURVIVAL IN THE GLUED COMPLEX.
     V011 :1881-1882 re-read at bytes: "No filled 2-cell is included in the trace
     carrier."  The trace subcomplex of K_dd (K_square's 4 vertices + 4 edges)
     contains NO 2-cell.  Scope: sealed 22f69dd3 :130 "SCOPE_READING = CONFIRMED
     (TRACE-CARRIER-SPECIFIC)" — the reading K_dd conforms to is the one confirmed
     of record; the stricter unwritten horn is honestly disclosed (§2.4) with the
     A5 fallback COMPUTED, and I verified A5's numbers (§3 below).
     THE GLUING-KILL ATTACK (does the filled face kill the theta class?): computed
     INTEGRALLY, my instrument — Smith normal form of partial_2 on K_dd gives
     elementary divisors [1]: im(partial_2) = Z·w primitive, and gamma_j is NOT in
     Q·w (rank test) — so [gamma_j] != 0 in H_1 over Z as well as Q; NO rational OR
     torsion kill.  The harmonic generator pairs it: <h, gamma_j> = 7 != 0.
     partial f = w != +-gamma (the face does not fill the trace cycle).  PASS.
C-4  B4 / THE RESPONSE FACE.  Q-active: w = (1,0,1,-1,-1) != 0, rank d_1 = 1 >= 1;
     F_phys = im(d_1) is the sealed response domain (V011 :1322-1355 re-read).
     Face shape: the frozen plaquette word span 96e164af REHASHED EXACT — an
     oriented 4-gon on four DISTINCT edges, signs (+ + - -); K_dd's attaching walk
     v_00 -e_a0-> v_10 -e_ab-> v_11 -(-e_ba)-> v_01 -(-e_0b')-> v_00 is a valid
     closed walk realizing exactly that shape.  PASS.
C-5  B5 / THE WRITE CONSTRAINTS.  GC :82-91 re-read at bytes: conservation is NOT
     d_1 ell = 0; conserved = gauge-invariant = the ONE condition ⊥ im(d_0)
     (= ker bdry).  G3 :130-135 (Q6 inventory) and :165-183 (R4 two-port law;
     "NOT SEALED: the identification of the two ports with named 0-cells") re-read.
     All three witnesses verified conserved (bdry·witness = 0), total-nonzero;
     the three typings T0/T1/T2 match the sealed typing map VERBATIM
     (92dba8d3 :285-289); no fourth typing is hidden.  PASS.
C-6  B6 / CAUSAL-ACYCLICITY TYPING.  I searched V011 independently: NO sealed span
     stating the time-cycle bar was located by me either — the candidate's
     disclosure ("carried from the tasking; span not independently located") is
     accurate and honest.  K_dd assigns NO time-typing to any edge; both cycle
     classes are spatial-DECLARED (an authored candidate-level declaration, riding
     the candidate's disclosed authored status); a typing that bars time-cycles and
     is silent on spatial cycles binds nothing here.  Honored by construction,
     leaned on nowhere.  PASS (with the disclosure verified).
C-7  B1/B2.  Connected (components = V - rank d_0 = 1, both instruments);
     d_1 d_0 = 0 entrywise over Z (asserted in both scripts); every face word the
     abelianization of an explicit closed oriented walk.  PASS.
CONFLICT CHECK: no two binding constraints clash on K_dd.  The one reading risk is
     disclosed, not papered over, and its fallback is computed in full.  NO
     CONSTRAINT CONFLICT — CONFIRMED.
```

---

## 2. ATTACK 2 — MINIMALITY: the hunt for a smaller conforming complex

My hunt was strictly BROADER than the artifact's certificate: every possible extra
edge at E = 5 (loops at each vertex, parallels in BOTH orientations, both chords in
both orientations, pendant edges to a new vertex) and EVERY closed-4-walk face on
each resulting multigraph — plus the relaxed regime with the face-shape constraint
dropped (repeated edges allowed).

```text
M-1  E = 4 IMPOSSIBLE — STRENGTHENED.  Cycles supported on K_square's own four
     edges are EXACTLY span{gamma} (b_1 = 1), so ANY Q-active face word on
     K_square alone — any length, any shape, repeats or not — is k·gamma, k != 0,
     which puts gamma in im(partial_2) and kills the class (A4 recomputed: H^1 = 0,
     harm = 0, MATCH).  The artifact's closed-4-walk certificate re-derived
     independently: word set = {0, +gamma, -gamma} EXACT.  E >= 5 holds not just
     for 4-gons but for EVERY face shape.  CONFIRMED+.
M-2  E = 5, V = 5 IMPOSSIBLE.  A pendant edge is a bridge: no cycle crosses it, so
     no admissible face word uses it; exhaustive enumeration found NO conforming
     face.  (The artifact's backtrack argument reaches the same conclusion.)
     CONFIRMED.
M-3  E = 5, V = 4: THE K_dd FAMILY IS EXACTLY THE CONFORMING SET.  Exhaustive
     enumeration over ALL 16 possible extra edges x ALL closed-4-walk faces:
     under the sealed face shape (4 distinct edges) the conforming realizations
     are EXACTLY the 16 labeled (doubled-ring-edge, word) pairs = the artifact's
     8 isomorphic variants x face orientation.  NO loop conforms, NO chord
     conforms (the artifact's ring-step parity argument covers the chord; the
     loop case — not named in the artifact's step 3 text — falls to the SAME
     parity argument: 3 ring steps are odd, a loop adds 0; noted as a
     completeness nit in the argument PROSE, with no effect on the certificate,
     which my enumeration closes exhaustively).  CONFIRMED.
M-4  THE CENSUS FLOOR DOES NOT RIDE B4 — STRENGTHENING FINDING.  With the face
     shape DROPPED entirely (repeats, bigon words, backtracks all admitted), 48
     additional labeled realizations appear — ALL at the same census 4/5/1, NONE
     smaller.  So B4 (frozen face shape, 96e164af) is load-bearing only for
     UNIQUENESS-up-to-isomorphism, not for the minimum census (1,5,4).  The
     minimality verdict is therefore MORE robust than the artifact claims.
M-5  SMALLER NAMED CANDIDATES.  W1 (1/2/1): bare arithmetic passes (recomputed:
     b_1 = 2, blocks 0/1/1) — rejected ONLY by the sector pin + face shape, both
     verified at bytes (C-2, C-4); the rejection is lawful, not aesthetic.
     KLf (5/6/1): recomputed, MATCH; fails the sector pin and is larger anyway.
     A4 (4/4/1): recomputed, class dead.  All rejections stand.
M-6  ORDER DEPENDENCE.  The declared order is lexicographic (F,E,V), declared
     before the family ran; K_dd also wins under total cell count V+E+F = 10
     (nearest conforming competitor: 11).  No order gaming detected.
MINIMALITY = CONFIRMED (no smaller conforming complex exists under the sealed
     constraint set; the floor is robust even one constraint down).
```

---

## 3. ATTACK 3 — TOPOLOGY + WITNESSES: independent rebuild, different instrument

My instrument: integer Smith normal form over Z (naive exact SNF) for every rank —
a genuinely different algorithm and arithmetic path from the artifact's
Fraction-RREF-with-three-prime-agreement. Projections computed by independent
Gram algebra.

```text
R-1  RERUN OF THE ARTIFACT'S SCRIPT.  Extracted verbatim from §9, run: exit 0;
     stdout BYTE-IDENTICAL to §10 (diff empty).  RERUN = MATCH.
R-2  INCIDENCE REBUILD.  From the sealed vertex/edge display alone (not the
     artifact's matrix): equals sealed D_square(u=1) entrywise; K_dd bdry equals
     the artifact's matrix entrywise.  MATCH.
R-3  BLOCKS (SNF).  gauge 3 / flux 1 / harmonic 1; sum 5 = E; b_1 = 2; H^1 = 1;
     connected.  Elementary divisors of d_1: [1] (primitive — H_1 torsion-free,
     stronger than the artifact needed).  MATCH.
R-4  HARMONIC GENERATOR.  Independently derived from the pairing data
     (<gamma,w> = 3, <w,w> = 4): h = 4·gamma - 3·w = (1,-4,1,-1,3); <h,w> = 0;
     bdry h = 0; <h,gamma> = 7; <h,h> = 28.  ALL MATCH.
R-5  T0 WITNESS ell = h + w = (2,-4,2,-2,2): conserved; phi_f(ell) = w != 0 and
     phi_H(ell) = h != 0 by independent projection (coefficient <ell,w>/<w,w> = 1).
     SATISFIABLE — MATCH.
R-6  T2 WITNESS lam = chi_j = (1,-1,1,-1,0): conserved; support = the sealed trace
     cell (a connected b_1 = 1 subcomplex — the sealed T2 cell shape, 92dba8d3
     :289); independent projection gives the EXACT split lam = h/4 + (3/4)·w with
     both parts nonzero and gauge part 0 (lam ∈ ker bdry).  The rotation finding
     (chi_j purely harmonic on K_square alone — a3bedc7e re-read, flux block {0}
     there — but 3/4-flux on K_dd) is real and correctly attributed to the
     shared-boundary gluing.  SATISFIABLE — MATCH.
R-7  T1 RESTRICTED NULL SPACE.  On the face's four support edges: restricted rank
     3 (SNF), nullity 1, null basis = w restricted; w ∈ im(d_1^T), <w,h> = 0 =>
     phi_H ≡ 0 on the whole on-cell admissible set.  UNSATISFIABLE — MATCH; the
     working-class collapse mechanism is reproduced mechanism-exactly.
R-8  A5 FALLBACK.  Census 4/8/1; b_1 = 5; blocks 3/1/4; <gamma,w5> = 0 (gamma
     purely harmonic; no rotation); dim H = 4 (selector question returns); T2
     mixed-circuit witness: coefficient 1/2, both projections nonzero; T1
     restricted nullity 1.  ALL MATCH.
R-9  FAMILY TABLE.  All nine rows recomputed by SNF: every V/E/F/comp/b_1/
     rank-d_1/gauge/flux/harm/H^1 entry MATCHES; sum rule gauge+flux+harm = E
     holds on every row.
WITNESSES = CONFIRMED.  RERUN = MATCH.
NOTE (non-refuting): the exact split coefficients (3/4, 1/4, 3/16, <w,w> = 4,
     <h,h> = 28) are stated in the counting metric EXTENDED to the authored edge
     e_0b' at unit weight — the definitional census extension, riding ledgered
     item L-3.  The satisfiability verdicts themselves are robust to that
     extension: <gamma,w> = 3 is carried entirely by the four SEALED edges (gamma
     vanishes on e_0b'), so T2's both-blocks landing survives ANY weight on the
     authored edge; only the displayed coefficients ride the unit choice.
```

---

## 4. ATTACK 3b — THE FINITE-N CROSS FORM

```text
X-1  THE SEALED FORM.  FN span [20396,20529) rehashed f7c7261e EXACT; the
     artifact's verbatim quotation matches the bytes word for word.  FORM D4
     :215-231 re-read: rank-one Hessian n^2·mu·Phi_tot⊗Phi_tot; mu symbolic;
     "mu ≡ 0 iff w_0 w_ch = 0" verbatim at bytes.  The kernel C_jk = mu as a
     PAIRING matrix, not an incidence structure: hunt :219-222 verbatim.  MATCH.
X-2  COEFFICIENTS ON THE WITNESSES.  T2: phi_f = (3/4)w, phi_H = (1/4)h =>
     (3/4)(1/4) = 3/16 on the (w,h) block pair; T0: 1·1 = 1.  Grams <w,w> = 4,
     <h,h> = 28, <w,h> = 0 — the blocks are metrically orthogonal, so the
     coupling is carried entirely by the kernel, exactly as the sealed text has
     it.  Recomputed independently.  MATCH.
X-3  CONTINGENCY DISCHARGE CLAIM.  FN :334-340 re-read at bytes: the branch order
     is exactly as the artifact carries it — (1) G3 both-blocks straddle, (2) G2
     mu != 0, and ONLY THEN the hand-off to G1.  The witnesses supply input (1)
     ON THIS CANDIDATE; the artifact claims no more.  The four OPEN items ((i)
     post-limit C, (ii) typing supply N-3, (iii) mu/G2, (iv) physical-domain
     status) are all present and correctly scoped.  MATCH.
CROSS_FORM = CONFIRMED.
```

---

## 5. ATTACK 4 — THE GLUING LEDGER

```text
L-1  Vertex identifications (O12-shaped): the sealed non-display is REAL — hunt
     G-4 :193-206 re-read verbatim ("their identification with 0-cells of the
     complex is not displayed"; "NOTHING licenses ... and nothing forbids").  ROW
     CORRECT.
L-2  Edge-read identifications (O11-shaped): GB S1 as quoted in hunt G-4 ("which
     edges the holonomy reads: not in the sealed text").  ROW CORRECT.
L-3  The authored duplicate 1-cell e_0b': no sealed complex displays parallel
     edges — grepped the sealed displays; none found.  ROW CORRECT.  (My metric
     note in §3 R-9 rides this row; the unit counting weight on e_0b' is the
     definitional census extension, not a further identification.)
L-4  Response role import: hunt :324-326 re-read verbatim ("no sealed map sends
     either audit object onto the working class or vice versa").  ROW CORRECT.
L-5  Connected composition (O-D1-shaped): hunt G-1 :165-173 re-read (coproduct;
     "composition is defined of record ONLY for disjoint components"); V011 :1740
     ("monoidal on disjoint cells").  ROW CORRECT.
PLUS  TYPE-P: the trace role rides the entered Attach premise (420ab02f verified
     at path) and voids with it.  CARRIED.
COMPLETENESS HUNT: I looked for identifications the ledger might have missed —
     face orientation (a face-orientation freedom, standard, verdict-invariant);
     duplicate-edge orientation (disclosed as naming freedom, 8 variants);
     the metric extension (see L-3 note; definitional).  NOTHING LOAD-BEARING IS
     MISSING.  Conditionality is carried on every load-bearing claim (the
     CONDITIONALITY flag line names both supplies AND the gluing license).
LEDGER = COMPLETE.
```

---

## 6. ATTACK 5 — PROVENANCE + INJECTION

```text
P-1  All 12 relied-on seals recomputed at path this session (§0); both byte spans
     rehashed EXACT; the 96e164af span digest independently pinned inside sealed
     22f69dd3 :62.  The artifact's digest-quote chains verified: 92dba8d3 :55-58
     quotes FORM 5e49d209 and GC 8ed95b5a; hunt :51/:59 quotes FN 1a96e095 and GC
     8ed95b5a; 22f69dd3 §0 pins V011 spans.  None unverifiable.
P-2  The tasked flag block and the artifact's in-file flag block agree (prefix and
     six distinctive probes verified after whitespace normalization).
P-3  Stratum discipline: candidate-not-of-record carried throughout; NONE_SEALED
     at the physical write-carrier stratum preserved; the two supplies and the
     gluing license named in the flag block itself; honest outcomes present
     (alpha_computed = false, kappa_record_computed = false,
     proof_authorized = false).  No promotion detected anywhere in the artifact.
P-4  Forbidden imports: none found — no scale, no imported GR, no faithfulness
     authority, no continuum object consumed; mu/n/Phi symbolic throughout; the
     only non-integers are the exact Fractions 1/4, 3/4, 1/2, 3/16.
P-5  INJECTION SCAN: grep for directive/override patterns over the artifact under
     test — none.  The artifact contains no instruction-shaped content aimed at
     a verifier.
PROVENANCE = CLEAN.  INJECTION = none.
```

---

## 7. Findings register (all non-refuting)

```text
F-1  STRENGTHENING: the census floor (1,5,4) survives DROPPING the face-shape
     constraint B4 entirely (48 relaxed realizations exist, all at the same
     census, none smaller).  B4 is load-bearing only for uniqueness.
F-2  STRENGTHENING: [gamma_j] != 0 in H_1 holds INTEGRALLY (SNF divisors [1]);
     no torsion kill is possible, not just no rational kill.
F-3  NIT (prose only): the artifact's E=5 argument text names pendant/chord/
     parallel but not the LOOP extra-edge case; the loop falls to the same
     ring-step parity argument and my exhaustive enumeration closes it.  The
     certificate and verdict are unaffected.
F-4  NOTE: the exact split coefficients are stated in the counting metric
     extended to the authored edge at unit weight (definitional census
     extension, rides ledger row L-3); the AND verdicts are robust to that
     choice — <gamma,w> = 3 lives entirely on the sealed edges.
F-5  VERIFIED DISCLOSURE: B6 (causal acyclicity) has no independently locatable
     sealed span — I searched too, found none; the candidate's disclosure is
     accurate, and the constraint binds nothing on a complex with no time-typed
     edges.
```

---

## FLAG BLOCK

```text
CONFORMANCE = CONFIRMED (every binding sealed constraint re-checked at bytes: the
  trace subcomplex is the sealed K_square verbatim with F = 0 on it (V011
  :1840-1851, :1881-1882; incidence rebuilt independently, equals sealed
  D_square(u=1) entrywise); the theta-readable class is genuinely unfilled AND
  genuinely non-contractible IN THE GLUED complex — [gamma_j] != 0 in H_1 verified
  INTEGRALLY (SNF divisors of partial_2 = [1]: no rational and no torsion kill),
  <h,gamma_j> = 7 != 0, partial f = w != ±gamma; scope reading is the
  TRACE-CARRIER-SPECIFIC horn confirmed of record (22f69dd3 :130) and the stricter
  horn is disclosed with fallback A5 computed; face shape = the frozen (+ + - -)
  4-distinct-edge plaquette word (96e164af REHASHED EXACT); write constraints per
  GC :82-91 (one condition, ⊥ im(d_0)) verified on every witness; typings T0/T1/T2
  match the sealed map 92dba8d3 :285-289; B6 causal acyclicity honored vacuously —
  no time-typed edge, disclosure verified (I found no independent span either);
  no constraint conflict).
MINIMALITY = CONFIRMED (independent exhaustive hunt BROADER than the artifact's
  certificate: E = 4 impossible for ANY face word of any shape — cycles on
  K_square's own edges are exactly span{gamma}, so any Q-active face there kills
  the class (A4: H^1 = 0 recomputed); E = 5 V = 5 impossible — a pendant edge is a
  bridge, no cycle crosses it; at E = 5 V = 4 the conforming set is EXACTLY the
  K_dd family — all 16 extra-edge choices x all closed-4-walk faces enumerated: no
  loop and no chord conforms; closed-4-walk word set on K_square = {0, ±gamma}
  re-derived EXACT.  STRENGTHENED: the census floor (F,E,V) = (1,5,4) survives
  DROPPING the face-shape constraint — 48 relaxed realizations, all at the same
  census, none smaller; B4 is load-bearing only for uniqueness.  W1/KLf rejections
  ride the sector pin verified at bytes (V007 :162-164, a3bedc7e §1.6); no smaller
  conforming complex exists).
RERUN = MATCH (script extracted verbatim from §9; exit 0; stdout byte-identical to
  §10; and the whole computation re-done with a DIFFERENT instrument — integer
  Smith normal form over Z — all nine family-table rows, all blocks, all ranks
  MATCH; sum rule holds on every row).
WITNESSES = CONFIRMED (blocks 3/1/1 with H^1 = 1 recomputed by SNF; h =
  4·gamma - 3·w = (1,-4,1,-1,3) re-derived from the pairing data with <h,w> = 0,
  <h,gamma> = 7, <h,h> = 28; T0 ell = h + w conserved with both projections
  nonzero; T2 lam = chi_j conserved, support = the sealed trace cell, exact split
  h/4 + (3/4)w re-derived by independent projection, both parts nonzero, gauge
  part 0 — the 3/4-rotation finding is real and correctly attributed to the
  shared-boundary gluing (chi_j purely harmonic on K_square alone, a3bedc7e
  re-read); T1 restricted nullity 1 with basis w, phi_H ≡ 0 on-cell — collapse
  mechanism reproduced; A5 fallback all recomputed: blocks 3/1/4, gamma purely
  harmonic, T2 split 1/2 + 1/2, T1 nullity 1 — MATCH).
CROSS_FORM = CONFIRMED (FN span [20396,20529) rehashed f7c7261e EXACT, quotation
  verbatim at bytes; FORM D4 :215-231 rank-one form and mu ≡ 0 iff w_0 w_ch = 0
  verbatim; coefficients recomputed: T2 (3/4)(1/4) = 3/16 on the (w,h) pair, T0
  coefficient 1; Grams <w,w> = 4, <h,h> = 28, <w,h> = 0 — metrically orthogonal
  blocks, coupling carried entirely by the kernel as sealed; FN :334-340 branch
  order verified — the witnesses discharge input (1) on this candidate only, and
  the four OPEN items are correctly scoped).
LEDGER = COMPLETE (L-1 through L-5 each verified against the sealed non-display
  they cite — hunt G-4 :193-206, GB S1, hunt :324-326, hunt G-1 :165-173 + V011
  :1740 all re-read verbatim; TYPE-P carried; completeness hunt found nothing
  load-bearing missing; one non-refuting NOTE: the exact split coefficients ride
  the unit-counting-weight extension to the authored edge (definitional, rides
  L-3) while the AND verdicts are robust to it — <gamma,w> = 3 lives entirely on
  the sealed edges).
MC_VERDICT = SOUND (as a candidate-stratum determination: every headline claim —
  the exhibited K_dd, the blocks, the typing-dependent AND, the cross-form
  discharge, the minimality — verified by independent instrument; conditionality
  carried on every load-bearing claim; NONE_SEALED at the physical write-carrier
  stratum survives; nothing promoted; three prose-level non-refuting findings
  registered (F-1 strengthening, F-3 loop-case nit, F-4 metric note)).
PROVENANCE = CLEAN (all 12 seals + the artifact under test recomputed at path THIS
  session before reliance; both byte spans rehashed EXACT; digest-quote chains
  verified; tasked flag block matches the in-file flag block; honest outcomes
  preserved: alpha_computed = false, kappa_record_computed = false,
  proof_authorized = false).
INJECTION = none.
MACHINERY_USED_BY_ME = no.
ALL_RESULTS of the candidate: CLAIMED -> CHECKED at this lane's stratum; the
  candidate remains candidate-not-of-record, conditional on the two supplies and
  the gluing license exactly as flagged.
```
