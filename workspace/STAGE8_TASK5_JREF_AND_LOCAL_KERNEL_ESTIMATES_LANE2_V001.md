# STAGE 8 TASK 5 / R4+R12 — J_REF AND LOCAL-KERNEL ESTIMATES — LANE 2 V001

Date: 2026-08-04  
Task: PASTE 488 / Task 5 / R4+R12  
Lane: Codex Lane 2  
Custody: adversarial cross-verification followed by derivation-first build

## Lead result

```text
JREF = OBSTRUCTED |
  BUILT: the maximal derived finite realization skeleton: admitted finite
  Gate-4 complexes, identity/isomorphism/relabeling arrows, their conserved
  current and bilocal-kernel realizations, and every already-ratified W3
  restriction square.

  OBSTRUCTED: the sealed corpus supplies no target-independent geometric
  refinement arrows, no relation saying when two complexes represent the
  same physical region, no coframe/volume/current-density rule, and no
  response pullback on a common refinement.  A free or barycentric formal
  category can be authored, but it does not derive the missing geometric
  data and is not installed here.  DoR-007 forbids using the adopted smooth
  background as the source of those data.

ESTIMATES = FIXED_STAGE_BUILT / UNIFORM_COMPLETED_ESTIMATES_OBSTRUCTED |
  BUILT: for every fixed finite stage and fixed admitted realization, explicit
  current and bilocal-kernel estimates follow from path-current continuity
  and finite dimensionality.

  OBSTRUCTED: the C1 constants must be uniform over the directed core and the
  complete no-selection realization family.  That family has no ratified
  path-length, mesh, shape-regularity, volume, wavefront, or boundary bound.
  The same sealed finite algebra admits a length/rescaling family for which
  the order-zero current bound grows like L and a rank-one bilocal bound like
  L^2 while the A4/profile coefficient class is unchanged.  Hence the
  required uniform estimate is not derivable from current authority.

C1 = CONDITIONAL_UNDISCHARGED |
  unique extension theorem CONFIRMED; fixed-stage estimates do not satisfy
  its family-uniform hypotheses; completed faithfulness remains independent.

C2 = CONDITIONAL_UNDISCHARGED |
  kernel-stitching theorem CONFIRMED; J_ref, common-refinement response
  naturality, density normalization, and BoundaryContactCert remain absent.

C3 = PARTIAL_CONDITIONAL |
  finite Ward and fixed-stage kernel calculus remain exact; completed Ward is
  conditional on C1; contact/local-order-two/Hodge/domain clauses remain open.

COMPLETED_SYMBOL = NOT_BUILT / FINITE_CYLINDRICAL_ONLY
COMPLETED_KERNEL_FAITHFULNESS = OPEN
FIBER_CONSTRAINT_COMPLETED = UNDECIDED
```

The failure is not a fence.  It is the exact DoR-007 boundary: the record
surface has not supplied the geometric refinement and uniform-locality data
from which the physical limit is required to be derived.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
member_selected = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight, seals, and register sweep

### 0.1 Preflight

The locked process was read in full.  The supplied artifact and its sidecar
were hash-verified before the artifact was opened.

| Object | SHA-256 / result |
|---|---|
| `supervision/LOCKED_PROCESS.md` | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` |
| `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `6c494e5582b0c83f8e565650bdeacadf87858dd11be6d3f3ad27a90f41037558` |
| Lane-1 Maxwell-symbol certificates | `2aad6d8379777b8651a7e8dda78e9b085a69aa0203f455209eeb5e6e7da3adeb` — exact match; sidecar OK |
| Q-408 kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c`; sidecar OK |
| Q-406 residue ledger | `c35ef84636ec1501bd99858538988711d0c717ac98671e6162abb0a0e5978c74`; sidecar OK |
| metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961`; sidecar OK |
| DoR-007 | `17dc023e297cfd6960a36623af2aa8efd57210574076590ef10b579b72c1808b`; sidecar OK |
| DoR-008 | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19`; sidecar OK |
| Task-4d stitching half | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4`; sidecar OK |

The register head is Q-409.

```text
DOES_THE_OBJECT_EXIST = partially |
  conditional theorems and finite kernel maps yes;
  full J_ref and uniform local estimates no
IS_THE_VERSION_CURRENT = yes | through Q-409
ARE_ITS_INPUTS_PRESENT = yes_for_cross_verification_and_fixed_stage_build |
  no_for_geometric_refinement_and_uniform_completion
PREFLIGHT = PASS_WITH_MAXIMAL_BUILD_AND_TYPED_OBSTRUCTION
```

### 0.2 Register and source sweep

Searches covered `J_ref`, `Ref`, `common refinement`, `response naturality`,
`shape regular`, `bounded geometry`, `path length`, `cell volume`, `coframe`,
`density normalization`, `local kernel`, `Ward`, `contact`, `Hodge`,
`faithfulness`, and `moving tail`.

| Entry/source | Binding result |
|---|---|
| Q-217/Q-246 | a realization needs algebra, representation, source incidence, degree, and intertwiners; support order alone is insufficient |
| Q-257/Q-258 | `T_cyl`, density of its finite core, retractions, and separation are proved only in the sequential norm/module classes |
| Q-261 | the 1,088 survivors are support filtrations of one fixed complex, not geometric realization members |
| Q-288 | every topology and completion must be named; no weak-* or bidual tail may be hidden |
| Q-384 / metric V005 | A4/profile completions, Riesz maps, units, and W3 adjoints exist; they carry no local-kernel seminorm or mesh bound |
| Q-396 | a completed nonzero class can have all active finite shadows zero; completion faithfulness cannot be inferred from finite injectivity |
| Q-400/Q-406 | the local-symbol map and R12 package are constructible steering objects, not already instantiated structures |
| Q-407/Q-408 | the reader fiber and finite path-bilocal Ward calculus are exact; locality/completion remain open |
| Q-409 | C1/C2 are conditional theorems; C3 is partial; local estimates and `J_ref` are the named premises |
| DoR-007 | smooth structure must arise from a cellulation-independent refinement theorem; the adopted smooth background is not a refinement input |
| DoR-008 | every completed construction must reproduce every sealed finite restriction; the completion has no independent authority |
| Task-4d Sections 6–9 | thin zero-extension is not geometric refinement; the eight-field package and composition theorem are exact |

### 0.3 Symbol/domain separations

```text
T_cyl       = sequential character-label norm/module completion
Ref_0       = the derived identity/isomorphism finite realization skeleton
Ref         = the unbuilt geometric refinement category
J_0         = the built finite current/kernel realization on Ref_0
J_ref       = the unbuilt target-independent geometric realization functor
zero_ext    = append identity coordinates; no mesh shrinkage
refinement  = a certified map of the same represented physical region
tau_A4      = the ratified current/profile coefficient topology
tau_ker     = the unbuilt local distribution/kernel topology
Ward        = annihilation of exact one-forms
locality    = diagonal/conormal local operator structure modulo contact terms
```

No shared word identifies either pair on adjacent lines.

---

## 1. D1 — cross-verification of C1, C2, and C3

### 1.1 C1 unique-extension theorem

Let `D_prof` be the finite profile core, `Profbar` its completion, and
`Ker_tau` a Hausdorff complete locally convex kernel space with seminorms
`p_Q`.  Assume

```text
p_Q(Kern_fin(H)) <= C_Q ||H||_prof              (D1-1)
```

uniformly on the directed finite core and no-selection family, together with
the analogous current bounds and continuity of every covariance, reality,
restriction, and batching map.

For a Cauchy sequence `H_n`, equation `(D1-1)` makes `Kern_fin(H_n)` Cauchy
in every target seminorm.  Target completeness supplies a limit; the same
estimate makes it independent of representative; density makes the extension
unique.  Continuous finite-core squares extend by equality on a dense set.

This recomputes Lane 1's C1 theorem exactly.

```text
C1_UNIQUE_EXTENSION_THEOREM = CONFIRMED / TYPE-P |
  premises: C1.a-C1.e
```

Continuity does not imply injectivity.  A continuous projection can be
injective on a dense nonclosed subspace and acquire a nonzero kernel after
completion.  Thus the independent lower estimate or zero-kernel condition
is necessary:

```text
||H||_prof <= C s(Kern_fin(H))
or
ker(Kernbar)={0}.                                (D1-2)
```

```text
COMPLETED_FAITHFULNESS_SEPARATE_FROM_C1 = CONFIRMED
```

### 1.2 C2 kernel-stitching theorem

Given a genuine `Ref`, a functorial `J_ref`, one topology `tau_ker`, coherent
connection/curvature/density maps, response naturality, common refinements,
and a boundary/contact certificate, two representatives may be pulled to a
shared refinement.  Naturality identifies their interior response there;
the boundary certificate kills the intensive difference; C1 makes the limit
representative-independent; P7 separation excludes an invisible nonzero
class in the stated topology.

This proof is valid under its displayed assumptions.  It does not manufacture
any assumption, and it does not treat the 1,088 support filtrations or the
thin `N<=M` system as `Ref`.

```text
C2_CONDITIONAL_KERNEL_STITCHING_THEOREM = CONFIRMED / TYPE-P |
  premises: C2-3, C2-4, C1, and the stated P7 predicates
```

### 1.3 C3 partial theorem

For exact `d alpha`, the contraction

```text
W_alpha(K)=K(d alpha,.)                         (D1-3)
```

vanishes on every finite kernel.  If C1 makes `W_alpha` continuous, its
kernel is closed and contains the dense finite image, so it contains the
completion.  This confirms completed Ward transversality conditionally.

The implication stops there.  A transverse path-bilocal kernel can retain
off-diagonal support, so Ward does not construct a contact ideal, diagonal
locality, an order-two principal symbol, a Hodge/magnetic partner, operator
domains, harmonic-sector control, or reader normalization.

```text
C3_COMPLETED_WARD = CONFIRMED_CONDITIONAL
C3_CONTACT_HODGE_LOCAL_SYMBOL = NOT_BUILT / TYPE-U
WARD_IMPLIES_LOCALITY = false | TYPE-R
```

### 1.4 D1 verdict

| Item | Verdict | Precision |
|---|---|---|
| C1 extension | **PASS** | upper continuity and completed faithfulness are independent |
| C2 stitching | **PASS** | theorem is conditional on an actual geometric diagram, not a formal name |
| C3 Ward half | **PASS** | additionally needs continuous exact-form contraction |
| C3 contact/Hodge half | **PASS as open typing** | no hidden construction found |
| named obstructions | **PASS** | `J_ref` and family-uniform local-kernel estimates are exactly first on the live path |

---

## 2. D2 — `J_ref` build attempt

### 2.1 Required contract

The R4 specialization requires

```text
J_ref : Ref -> CompletedCurrentKernelClass,      (D2-1)
```

where an object of `Ref` carries at least:

```text
finite oriented record complex;
Gate-4 boundary/incidence and degree;
connection and source incidence;
algebra and representation;
coframe/volume and current-density normalization;
```

and every refinement arrow carries compatible intertwiners, restriction,
current/curvature transport, and response pullback.  Every pair representing
the same region must admit a common refinement.  No choice of cellulation,
filtration, frame, or realization member may enter the output.

### 2.2 Maximal derived skeleton

From sealed content one can construct the category `Ref_0` whose objects are
the already admitted finite Gate-4 realizations and whose arrows are only:

```text
identity arrows;
certified signed relabelings/isomorphisms;
already-ratified rank-preserving W3 restriction/inclusion squares. (D2-2)
```

Define `J_0` objectwise by

```text
J_0(G)=(K_G^fin, c|->J_c, H|->Kern_G^fin(H)).   (D2-3)
```

Q-408 proves `(D2-3)` is faithful and covariant, real, quotient-compatible,
restriction-natural on its actual scope, and batch-compatible.  Hence:

```text
FINITE_REALIZATION_SKELETON_J_0 = BUILT / TYPE-P
```

But `Ref_0` is not directed by geometric refinement.  Its W3 arrows preserve
the declared rank/scoped source structure; they do not say that cells shrink,
that two complexes cover the same region, or that a new cell refines an old
one.  Therefore `J_0` cannot be relabeled `J_ref`.

### 2.3 Why a geometric arrow is not forced

The same finite incidence and holonomy data admit distinct positive coframe
and cell-volume assignments.  For a scale parameter `lambda>0`, the
assignments

```text
ell_e -> lambda ell_e,
V_cell -> lambda^d V_cell                       (D2-4)
```

leave the sealed incidence, boundary, character, restriction, and finite
holonomy equations unchanged.  They change current-density normalization and
the order-two local symbol.  Neither DoR-007 nor DoR-008 chooses `lambda` or
supplies a scale law.

Likewise, an abstract barycentric subdivision of one complex gives a formal
combinatorial chain, but it does not prove that two different complexes
represent the same physical region, furnish a common geometric refinement,
or transport coframes, volumes, sources, and connected response.  Taking the
free category on formal subdivision symbols merely moves the missing data
into unproved generators.

```text
INCIDENCE_FORCES_GEOMETRIC_SCALE = false | TYPE-R |
THIN_ZERO_EXTENSION_FORCES_REFINEMENT = false | TYPE-R
FREE_REFINEMENT_CATEGORY_DISCHARGES_J_REF = false | TYPE-R
```

### 2.4 Response-naturality and common-refinement square

The lawful square is only an interface:

```text
Response(K) --eta_resp(K->R)--> Response(R)
    |                                |
 finite restriction                 common restriction
    v                                v
sealed shadow K  ---------------> sealed shadow R.          (D2-5)
```

The bottom finite restrictions exist where already ratified.  The top arrow
does not: no completed response assignment on geometric refinements, density
map, or common-region relation is present.  The square cannot be proved by
using zero-extension, because the latter appends identity cells and has no
mesh or density semantics.

```text
RESPONSE_NATURALITY_ON_J_REF = NOT_BUILT / TYPE-U
COMMON_GEOMETRIC_REFINEMENT = NOT_BUILT / TYPE-U
```

### 2.5 Authored alternatives disclosed, none installed

| Field | Authored alternatives | What must be proved | Void condition |
|---|---|---|---|
| `Ref` | all certified PL/cellular refinements; a target-independent causal-complex family; reject | common refinement and same-region relation without smooth-target import | chosen cellulation/filtration or desired coefficient |
| geometric realization | coframe/volume functor family; independent discrete density family; reject | incidence, degree, source, algebra, representation, intertwiners | smooth background assumed as source; untyped conversion |
| refinement normalization | volume/coframe natural density; symbolic bounded-geometry family; reject | composition and common-refinement coherence | scale selected from Maxwell or fixed-point output |
| `eta_resp` | connected-response natural transformation; proved equivalent family; reject | W3/DoR-008 square on every refinement | zero-extension renamed response pullback |
| boundary/contact | quotient ideal; invariant direct-sum ideal; reject | subextensivity/contact theorem in one topology | record-visible off-diagonal content deleted |

No row is selected.  Installing any row would add structure beyond the
derivation/audit commission and, for smooth-target input, contradict DoR-007.

### 2.6 D2 verdict

```text
JREF_IDENTITY_AND_ISOMORPHISM_SKELETON = BUILT
JREF_GEOMETRIC_REFINEMENT_PACKAGE = OBSTRUCTED / TYPE-U |
  exact missing: Ref + same-region/common-refinement relation +
                 coframe/volume/density + response naturality + boundary cert
AUTHORED_JREF_MEMBER_INSTALLED = none
```

---

## 3. D3 — local-kernel estimate build

### 3.1 Fixed-stage current estimate

Fix one finite stage `G` and one admitted realization `B`.  For a compactly
supported smooth one-form `a`, Q-408 gives

```text
<J_c,a>=sum_e c_e integral_(gamma_e) a.          (D3-1)
```

For the order-zero test seminorm on a compact set containing the paths,

```text
|<J_c,a>|
 <= sum_e |c_e| Len_B(gamma_e) ||a||_infinity
 <= ||c||_2 (sum_e Len_B(gamma_e)^2)^(1/2)
      ||a||_infinity.                            (D3-2)
```

Finite-dimensional norm equivalence supplies `m_(G,B)<infinity` with

```text
||c||_2 <= m_(G,B)||c||_K.                       (D3-3)
```

Thus

```text
p_0(J_c)
 <= C_cur(G,B)||c||_K,
C_cur(G,B)
 :=m_(G,B)(sum_e Len_B(gamma_e)^2)^(1/2).        (D3-4)
```

Higher distribution seminorms admit fixed-stage constants because the path
family is finite and every individual path-current is a continuous
distribution.  These constants may depend on path derivatives, coordinate
charts, support compacta, and the realization member.

### 3.2 Fixed-stage bilocal estimate

Let `A_G` be the finite analysis map and `R_K,G` the ratified Riesz map.  For
the Q-408 kernel,

```text
Kern_G^fin(H)[a,b]
 =H(R_K,G^(-1)A_G a,R_K,G^(-1)A_G b).           (D3-5)
```

Trace norm dominates operator norm, so for fixed test seminorms

```text
|Kern_G^fin(H)[a,b]|
 <= ||H||_1 ||R_K,G^(-1)A_G a||_K
              ||R_K,G^(-1)A_G b||_K
 <= C_A(G,B)^2 ||H||_prof q(a)q(b).              (D3-6)
```

The corresponding bilocal kernel seminorm obeys

```text
p_Q(Kern_G^fin(H))
 <= C_ker(Q;G,B)||H||_prof.                      (D3-7)
```

This constructs every estimate needed at a fixed finite stage/member.

```text
FIXED_STAGE_CURRENT_ESTIMATES = BUILT
FIXED_STAGE_BILOCAL_ESTIMATES = BUILT
```

### 3.3 Uniformity countermodel

C1 does not ask for `(D3-4)`–`(D3-7)` memberwise.  It asks for constants
uniform over the directed finite core and the complete no-selection
realization family.

Take the reciprocal-loop incidence data and its normalized A4 coefficient
class.  The retained family contains no axiom relating that coefficient norm
to the geometric length of its realized closed path.  Hence it admits the
countermodel family in which the finite/A4 coefficient form is held fixed and
the same incidence and holonomy data are realized by a loop of length `L`.
A smooth tangential test one-form with
`||a||_infinity<=1` may be chosen along the path, giving

```text
p_0(J_c) >= |<J_c,a>| = L                       (D3-8)
```

up to the fixed orientation sign, while the sealed coefficient/A4 class and
all finite incidence restrictions are unchanged.  Hence no authority-derived
constant `C` satisfies

```text
p_0(J_c) <= C||c||_K                            (D3-9)
```

uniformly for all members.  For the normalized rank-one profile
`H=c^flat tensor c^flat`, evaluating both legs on the same test form gives

```text
p_(Q_0)(Kern_G^fin(H)) >= L^2,                  (D3-10)
```

while its profile coefficient norm is fixed.  Thus the bilocal constant is
also unbounded.

This is a derivability countermodel, not a claim that no bounded-geometry
subfamily could satisfy the estimates.  Such a subfamily would add exactly
the missing path/mesh/shape/volume bounds and would require its own disclosed
authorization.

### 3.4 Refinement-normalization countermodel

Subdivide one path into `n` consecutive subpaths.  Holonomy/current additivity
requires the coarse integral to equal the sum of fine integrals, but the
fine coefficient vector has norm proportional to `sqrt(n)` under an
unweighted Euclidean coefficient presentation.  Rescaling fine coefficients,
edge volumes, or density by `n^(-1/2)`, `n^(-1)`, or a coframe-derived factor
produces inequivalent norm/local-symbol laws while preserving the same
coarse finite integral.  No sealed refinement rule chooses among them.

```text
FINITE_ADDITIVITY_FORCES_DENSITY_NORMALIZATION = false | TYPE-R
```

### 3.5 Tautological-topology attack

One can define a graph/image norm by decree,

```text
||Kern_fin(H)||_graph:=||H||_prof.               (D3-11)
```

This makes continuity and faithfulness tautological.  It is not a local
distribution-kernel topology, carries no diagonal support or principal
symbol, and was defined from the very map it is meant to certify.  It cannot
discharge C2/C3 and is rejected as circular.

### 3.6 D3 verdict

```text
LOCAL_KERNEL_ESTIMATES_FIXED_STAGE = BUILT
LOCAL_KERNEL_ESTIMATES_FAMILY_UNIFORM = OBSTRUCTED / TYPE-U |
  exact missing: bounded geometry + named tau_ker + refinement density law
C1_UNIFORM_CONSTANTS_DERIVED = false
```

---

## 4. D4 — discharge of C1/C2/C3

### 4.1 C1

The fixed-stage estimates do not meet C1.c.  There is also no named complete
`tau_ker` receiving every family member.  Therefore C1.a-e are not jointly
instantiated and the unique-extension theorem cannot be applied.

Even if a future upper estimate closes C1, completed faithfulness still needs
the independent lower/separation certificate `(D1-2)`.

```text
C1 = CONDITIONAL_UNDISCHARGED
COMPLETED_CURRENT_MAP = NOT_BUILT
COMPLETED_KERNEL_MAP = NOT_BUILT
COMPLETED_KERNEL_FAITHFULNESS = OPEN
```

### 4.2 C2

`J_0` supplies the already known finite realization but not a geometric
directed diagram.  Since `Ref`, `J_ref`, density normalization,
`eta_resp`, and `BoundaryContactCert` are absent, the C2 premises remain
uninstantiated.

```text
C2 = CONDITIONAL_UNDISCHARGED
CELLULATION_INDEPENDENT_LOCAL_CLASS = NOT_BUILT
```

### 4.3 C3

Finite Ward transversality remains exact.  Completed Ward remains conditional
on C1.  Contact/local-order-two/Hodge/domain/normalization clauses require C2
and further operator data, so they do not close.

```text
C3_FINITE_WARD = CLOSED
C3_COMPLETED_WARD = CONDITIONAL_ON_C1
C3_CONTACT_HODGE_SYMBOL = OPEN
C3 = PARTIAL_CONDITIONAL
```

### 4.4 Completed symbol and reader fiber

The chain stops before a completed local kernel exists:

```text
finite path-bilocal Ward kernel                    BUILT
  -> family-uniform continuous completion          OPEN
  -> common-refinement diagonal/contact class      OPEN
  -> local order-two Maxwell/Hodge symbol           OPEN
  -> completed normalized reader                    OPEN.    (D4-1)
```

Therefore no completed kernel may be quotient out and no `(chi_K,T)`
coordinate may be removed.  The exact future split remains:

```text
ker(Kernbar)=0    => completion adds no reader relation;
ker(Kernbar)!=0   => a reader must annihilate that proved kernel.
```

```text
COMPLETED_SYMBOL = NOT_BUILT / FINITE_CYLINDRICAL_ONLY
FIBER_CONSTRAINT_FINITE = NO_NEW_RELATION
FIBER_CONSTRAINT_COMPLETED = UNDECIDED
```

### 4.5 Discharge table

| Certificate | Cross-verification | Premises discharged here | Standing after D2/D3 |
|---|---|---:|---|
| C1 extension | confirmed | fixed-stage only | **CONDITIONAL / UNDISCHARGED** |
| C1 faithfulness | correctly separate | 0 | **OPEN** |
| C2 stitching | confirmed | identity/isomorphism skeleton only | **CONDITIONAL / UNDISCHARGED** |
| C3 finite Ward | confirmed | all finite stages | **CLOSED finite scope** |
| C3 completed Ward | confirmed conditional | 0 completed | **CONDITIONAL on C1** |
| C3 contact/Hodge | correctly open | 0 | **OPEN** |
| completed Maxwell symbol | — | finite cylinder only | **NOT BUILT** |

---

## 5. D5 — falsifiers, regressions, and anti-tuning

### 5.1 Falsifier table

| Test | Execution | Result |
|---|---|---|
| DoR-008 finite restriction | `J_0` is exactly Q-408's finite map; no finite kernel changes | **PASS** |
| reciprocal loop | current and rank-one kernel estimates reproduce the one-cycle carrier | **PASS** |
| S8-A | all exchange-covariant cycle directions remain; no coordinate is selected | **PASS** |
| one-edge/tree | `ker(B^T)=0`; the scalar cycle current/kernel image is zero | **PASS** |
| pendant/tree quotient | coboundary content remains outside the physical cycle quotient | **PASS** |
| rank-preserving W3 | only the ratified isometry/adjoint scope is used | **PASS** |
| cycle-creating addition | no upward geometric refinement map is inferred | **PASS** |
| identity zero-extension | treated as identity append, never as `h->0` | **PASS** |
| reality/orientation | signs conjugate covariantly; no orientation member selected | **PASS** |
| batching | componentwise ordered batching only; no scalar product introduced | **PASS** |
| moving tail | no weak-* or bidual kernel completion is formed | **PASS** |
| completed faithfulness | not inferred from finite injectivity | **PASS** |
| graph-norm shortcut | `(D3-11)` rejected as circular and nonlocal | **PASS** |
| smooth-target import | prohibited by DoR-007 and absent | **PASS** |

### 5.2 Fresh hostile attacks

1. **All-formal-refinements attack.**  The free category on symbols is easy
   to write, but has no same-region relation, density, or response map.  It
   cannot satisfy C2's premises.

2. **Barycentric-canonical attack.**  Barycentric subdivision is a
   combinatorial rule for one complex.  It neither compares arbitrary
   physical cellulations nor derives coframe, volume, or local kernel scale.

3. **Memberwise-estimate promotion.**  Taking
   `sup_(G,B) C_ker(G,B)` silently assumes that supremum finite.  Equations
   `(D3-8)`–`(D3-10)` show it is unbounded on the retained family.

4. **Ward-as-bound attack.**  Conservation removes exact directions but does
   not bound transverse path length or off-diagonal support.  It cannot supply
   C1.c.

5. **Zero-shadow quotient attack.**  Deleting every completed class with zero
   active finite shadow would delete the Q-396 standing witness without a
   faithfulness theorem and violate finite-authority/refusal discipline.

6. **Target-tuned bounded geometry.**  Choosing mesh or shape bounds after
   inspecting a desired Maxwell coefficient, reader coordinate, modulus, or
   fixed point voids the package.  No such choice is made.

### 5.3 Anti-tuning order

```text
1  Verify C1/C2/C3 without changing their premises.
2  Build the entire existing finite realization skeleton J_0.
3  Test whether sealed arrows carry geometric refinement semantics.
4  Derive fixed-stage estimates before considering any completion.
5  Run the family-uniform length and refinement-normalization attacks.
6  Record J_ref and uniform estimates as obstructed before revisiting readers.
7  Infer no relation on (chi_K,T), no coefficient, and no downstream value.
```

```text
p_symbolic = true
nu_symbolic = true
chi_K_symbolic = true
T_symbolic = true
rank_pair_selected = none
realization_member_selected = none
refinement_member_selected = none
topology_selected = none
numeric_response_evaluated = false
protected_value_evaluated = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 6. Final board

```text
D1_C1 = CONFIRMED
D1_C2 = CONFIRMED
D1_C3_PARTIAL = CONFIRMED

FINITE_REALIZATION_SKELETON_J_0 = BUILT
JREF = OBSTRUCTED |
  missing Ref, same-region/common-refinement relation, geometric density,
  response naturality, and BoundaryContactCert

FIXED_STAGE_CURRENT_ESTIMATES = BUILT
FIXED_STAGE_BILOCAL_ESTIMATES = BUILT
ESTIMATES = FAMILY_UNIFORM_OBSTRUCTED |
  countermodel: unbounded path/local-kernel scale on the no-selection family

C1 = CONDITIONAL_UNDISCHARGED
C2 = CONDITIONAL_UNDISCHARGED
C3 = PARTIAL_CONDITIONAL

COMPLETED_SYMBOL = NOT_BUILT / FINITE_CYLINDRICAL_ONLY
COMPLETED_KERNEL_FAITHFULNESS = OPEN
FIBER_CONSTRAINT_COMPLETED = UNDECIDED

AUTHORED_FIELDS_INSTALLED = 0
MEMBER_SELECTED = none
MACHINERY_APPEAL = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
