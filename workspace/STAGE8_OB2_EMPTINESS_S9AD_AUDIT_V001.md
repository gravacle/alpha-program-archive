# STAGE 8 — OB-2 EMPTINESS DETERMINATION — INDEPENDENT ADVERSARIAL AUDIT — V001

## AUDITOR — CODENAME OB2-AUDIT — [SEALED]

Date: 2026-08-14 (session CDT; write ~2026-08-15T02:5xZ). Commission:
independent adversarial audit of `STAGE8_OB2_EMPTINESS_S9AD_V001.md`
(`c8053f6c…`), NOT told its outcome, DEFAULT REFUTE, my verdict governs.

Fences held throughout: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. DETERMINATION-AUDIT ONLY: nothing here
adopts, authors, binds a member, or moves any score; the held construction
(`506bfe2a…`/`5fa46838`) untouched and unpresupposed. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…" tokens
EXPECTED-UNLOCATABLE (cited only as they appear inside sealed artifacts).
No git; no existing file edited; this output + its seal sidecar probed
ABSENT at session start and re-probed ABSENT immediately before this
write; ONE output + sidecar. Bytes-level + exact-symbolic; CAS-grade
battery run in a fresh venv with exact integers/rationals only; no float
anywhere; no value frozen.

**SWEEP CUTOFF DECLARED: 2026-08-14 21:34 CDT (= 2026-08-15T02:34Z, the
build's mtime); corpus roots swept: alpha-program-archive + alpha_supervision
minus fenced classes. Exhaustiveness is sweep-displayed only.**

## 0. INDEPENDENCE PROTOCOL, EXECUTED

Sub-tasks (1)–(2) were worked from seed stock alone and FIXED, verdict
included, in hashed scratchpad notes BEFORE the build was opened:

```text
PRE-COMMIT FILE  scratchpad/OB2_AUDIT_PRECOMMIT_V001.txt
SHA-256          0c7f49f00ca673bfd0a2c7efb4a907f24f14ffaaf78c40dcdec858e8b21e681c
FIXED AT         2026-08-14 21:40:58 CDT
BUILD SEAL VERIFIED AND FILE FIRST OPENED   after that timestamp
```

My pre-fixed independent answer to the question of record: EMPTY not
provable from anything derivable at seed bytes; NON-EMPTY unconditional
provable only if member-independent fixed-data conditions on the sealed
carriers (my P1 = the (SPEC-11b) one-value clause over the sealed
`qtilde_N` relation ideal; my P2 = the X5-class topology certificates)
are decided at bytes; honest best achievable without new bytes =
conditional/UNDECIDED-AT those named conditions. Pre-fixed attack list:
base-fixedness, joint-clause walk, P1/P2 handling, abstractness,
quantifier conflation, blocker genuineness/completeness, seals/battery.

## 1. SEALS — ALL RE-VERIFIED INDEPENDENTLY AT PATH (shasum -a 256)

```text
BUILD  c8053f6c  STAGE8_OB2_EMPTINESS_S9AD_V001.md                MATCH (read once, in full, after pre-commit)
SEEDS  fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md       MATCH (read in full)
       ba9430c1  STAGE8_TASK5_JII_REVIEW_DARIO_V001.md            MATCH (K1.3/N1/K3.3-B/K4 read)
       b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md     MATCH ((S1)-(S8) + anti-formal clause at bytes)
       add4c806  STAGE8_GRAVITY_ACT_DRAFTS_S9AD_V001.md           MATCH (§2 D-1 read in full)
       69d8b24b  STAGE8_GRAVITY_ACT_DRAFTS_S9AD_CHECK_V001.md     MATCH (SK-2 cell verified)
PINS   ccb94dfa  …R4_KERNEL_REALIZATION…LANE2_V001.md             MATCH ((B2-2)-(B2-8),(B2-13) at bytes)
       3fd4b924  …EQ6_PHYSICAL_J2_LANE2_V001.md                   MATCH (§1 carrier naming at bytes)
       9c74ed3d  …EQ6_J7_FACE_AND_J2_DISPLAY_LANE3_V001.md        MATCH
       e0b66180  DOR_020_A9_XI_N_ADOPTED (alpha_supervision)      MATCH
       ac9335dc  …XI_N_CANDIDATE_LANE3_V004.md                    MATCH ((Z1)-(Z4),(Z3) at bytes)
       288a3d41  …V005_FINAL_CHECK_AND_SINGLE_BEDROCK…V001.md     MATCH (six-fiber bar at bytes)
       075a2b16  …EXTSRC_BUILD_LANE3_V001.md                      MATCH
       d662889b  …EXTSRC_BUILD_LANE3_V002.md                      MATCH ((R4)/(R8)/(B3) at bytes)
MISMATCHES = 0. The build's own seal table is accurate, including the
e0b66180 location (supervision root, not workspace).
```

## 2. BYTES VERIFICATION OF EVERY LOAD-BEARING CITATION

| Build citation | My bytes check | Result |
|---|---|---|
| (OB-1)/(OB-2) at fdf20bd4 §4 + terminal flag | lines read verbatim | VERBATIM |
| (SPEC-2) tuple contains `FullJ7_N`; (SPEC-12d) "required solely to recover J7's declared full domain"; §1.5 "An H member carrying…" conditional phrasing | all three at bytes | VERBATIM; fork B-2(iii) honestly surfaced, not resolved |
| "Neither `P_H,N Loc_N^C(kappa_T,N) != 0` nor its negation is a membership clause" + (STRUCK) not restored (fdf20bd4 §1.3) | at bytes | VERBATIM — W-0's zero tail is not excluded by any displayed law |
| (SPEC-12a): `iota_N^Q408` and `Sch_N` carry "continuous and injective" ONLY (no linearity) | at bytes | VERBATIM — load-bearing for NG-2 p5 and for my Finding F-A |
| LOC (S3) `Krawhat_N` continuous LINEAR; (S4) `kappa_T,N = Sch_N(M_N^loc)`; (S5); (S8) equality; anti-formal clause "A formal tag adjoined solely to make (S4) parse fails the specification" | at bytes | VERBATIM |
| KER (B2-2)/(B2-4) onto f.d. dual; (B2-5) on `Hom(K_G^fin,(K_G^fin)^*)`; (B2-6) finite sums; (B2-7) faithful; (B2-8) path-bilocal support | at bytes | VERBATIM |
| PJ2 §1 "`Ker_N^Q408` = actual finite path-bilocal kernel class" | at bytes (line 180) | VERBATIM |
| (Z3) "the zero-map case does not satisfy that antecedent"; `Xi_N(0)=0` DEFINED | ac9335dc at bytes | VERBATIM — W-HOL's zero map is admitted at the candidate's own bytes |
| ExtSrc round 2: (R4) pure-cycle zero class; (R8) mixed-relation gap; terminal "ABSTRACT_EXTSRC_FAMILY_EMPTY = not proved. (B3)" | d662889b at bytes | VERBATIM |
| Six-fiber bar (288a3d41 §4.1/§5.3) | at bytes | VERBATIM — "separate nonemptiness … does not imply joint inhabitance; one compatible tuple in the fiber product" |
| SK-2 / option (b) consequence structure (add4c806 §2.5/§2.8; 69d8b24b SK-2 GENUINE) | at bytes | VERBATIM |
| SW-3 count (11 workspace files bearing `Ker_N^Q408` besides itself) | my census: 12 including the build | CONSISTENT |
| B-3(i): no sealed line fixes `Ker_N^Q408`'s extent to the full Hom image | corpus grep, definitional sites | SURVIVES — no such line found |
| B-3(iii): no sealed rank-≥3 admitted-and-carried J-II stage display | corpus grep "rank 3/cycle rank" | SURVIVES — hits are 7A/MAP/check-script carriers, not J-II stages |
| SW-1: no competing OB-2 determination | corpus grep | SURVIVES |

## 3. ADVERSARIAL RE-DERIVATIONS (my own battery; DEFAULT REFUTE applied)

### 3.1 NG-2 (the FullJ7 dimension pinch) — RE-DERIVED, SOUND

Independently re-walked at bytes: p1–p4, p6 as cited; p5's derivation of
`iota_N^Q408`-linearity ON `Ker_N^rep` re-derived myself — (S5)+(S3) give
`iota(K̄Q(0,b,C)) = Krawhat_N(0,b,C)` with `Krawhat_N` linear, whence
`iota(0)=0`, well-definedness across representatives (via `Rel_N ⊆
ker(Krawhat_N)`, (S6)), additivity and scalars — EXACT. d2/d3's
invariance-of-domain step needs NO linearity on the full domain (the build
was right to avoid assuming it) and needs p6 (f.d.), grounded at the
"actual finite … class" bytes (PJ2 §1; KER's distribution-topology
EXTENSION is explicitly unbuilt at (B2-14)-era bytes, so no infinite
class is sealed). The inequality `dim Ker_N^Q408 <= 1 + dim
Kernbar_N^cyc(O_N^cyc)` is a genuinely NEW, PROVABLE necessary condition
on any H member at every carried stage. Threshold arithmetic re-run
exact in a fresh venv: real reading first violation at cycle rank 3
(9 > 1+6); complex/sesquilinear at rank 2 (8 > 1+5) — MATCHES §4.2.
Its three stop-points (extent unfixed; no rank census; (SA-1) closure
absent) each verified undisplayed by sweep. NG-2 CONFIRMED as displayed.

### 3.2 NG-3 (the mixed-relation pinch) — SOUND IN SHAPE, ONE UNSEALED PREMISE UNFLAGGED (Finding F-A)

Consistency direction re-derived myself and model-checked exact: forced
kappa-values from any two mixing relations agree automatically because
their normalized difference is a pure-cycle element of `ker(qtilde_N)`,
hence zero in `O_prof,N` by the sealed quotient typing, hence killed by
the sealed `Kernbar_N^cyc` — CONFIRMED (this half is PROVABLE, and
sharper than my own pre-commit, which had not localized the residue to
zero-cycle-side mixing relations).

Kill direction: a mixing relation with zero cycle-side value forces
`kappa_T,N = Sch_N(M_N^loc) = 0`. The displayed contradiction —
"contradicting `Sch_N` injective and `M_N^loc != 0`" — DOES NOT CLOSE at
the displayed bytes: (S4)/(SPEC-12a) give `Sch_N` only "continuous and
injective"; injectivity without linearity (or without `Sch_N(0)=0`, or
without pin B-2(i)'s actual-Schwartz-kernel reading, under which `Sch_N`
is linear) does not make `Sch_N(M_N^loc)=0` contradictory, and no sealed
clause supplies any of the three (`Sch_N`-linearity is NOT derivable the
way p5 derives iota's — `Sch_N` enters the ExtSrc identities at the
single point `M_N^loc` only; the (S4) anti-formal clause does not bar the
zero distribution by its letter). The build applied exactly this
omission-is-meaningful reading standard itself at p5; by its own
standard, NG-3's named-premise list (which names only `M_N^loc != 0`) is
incomplete. VERDICT-NEUTRAL: NG-3 fires nothing today under either
reading; the kill condition stays armed with one more named premise.

### 3.3 W-0 / W-HOL (the zero-tail witnesses) — LEGAL AND HONESTLY FENCED

Every clause the build lists as degenerate-met I re-walked: (JD-3a),
the four (JD-3) obligations, (JD-3c)/(SPEC-9) confinement, the
(S24)/(S25)/S25-data squares with zero legs, (Z1)-(Z4a)/(Z-A7)/(SPEC-16)/
(16a)/(17) over the one-orbit model — all degenerate-true at bytes; the
nonvanishing-exclusion refusal and the (Z3) zero-map sentence are
verbatim. The build's fencing is exactly right: fiber-local only, no
promotion past the six-fiber bar, Class-1 clauses explicitly NOT met,
and the (S4)-vs-tails anti-degeneracy asymmetry displayed as unsealed
pin B-2(ii) rather than resolved. NO member binding, NO adoption: the
witnesses are displays; §7's consistency block is accurate.

### 3.4 Completeness of the blockers (the attack my default demanded)

(a) Could EMPTY close today? Only via NG-1/NG-2/NG-3. NG-1 needs pin
B-2(i) (verified unsealed at the (SPEC-12a)/(S2b) bytes; fdf20bd4 Joint B
holds the door open verbatim). NG-2's evaluation needs B-3(i)+(iii),
both verified undisplayed by my own sweep. NG-3 needs B-3(ii) (verified
undecided at (R8)/(B3) bytes) — plus the F-A premise. No fourth pinch
found: I re-hunted the Class-1 pair space myself (the (S23) bases on
u/r_nm constrain member data only; (SPEC-10a')/X5 topology certificates
are fixed-data conditions no display decides — they BLOCK witnesses, not
prove emptiness). EMPTY is not reachable today. CONFIRMED.
(b) Could NON-EMPTY close today? My pre-commit's free/colimit witness
strategy for the common fiber fails exactly where the build says: the
S_N one-value clause needs `ker(qtilde_N)` displayed (it is not); the
X5-class certificates are fixed-data and undisplayed; FullJ7+NG-2 must
hold at the witness's carried stages (undisplayed). No abstract witness
is displayable today. CONFIRMED. The localization (§5.3) is the real
content: both tails accept zero at their own bytes, so OB-2 = the common
ExtSrc fiber + NG-2. I verified each leg of that reduction.

### 3.5 Quantifier honesty and the named hazard

The build nowhere conflates the construction quantifier with the
abstract quantifier — it is the first artifact in this lineage to
separate them cleanly (§3.2 Class 1/2/3), and it respects the
joint-fiber bar at every promotion point. The consequence posture (SK-2
NOT fired; ACT-3 NOT forced; D-1 menu unchanged) is correct at the
add4c806/69d8b24b bytes.

## 4. FINDINGS

```text
F-A  CORRECTION (substantive, verdict-neutral): NG-3's kill direction
     consumes an unsealed premise — Sch_N linearity / Sch_N(0)=0 / pin
     B-2(i) — absent from its named-premise list. Repair: add the
     premise to §4.3 and to the FLAG BLOCK's NG-3 line; NG-3's grade
     becomes "PROVABLE-shape UNDER PIN B-2(i) (or sealed Sch-linearity),
     antecedent undecided". The build's own B-2(i) fork already carries
     the needed pin; it was applied to NG-1 but not to NG-3.
F-B  CORRECTION (provenance, verdict-neutral): §6.3's parenthetical
     quotation "UNDECIDABLE-TODAY … the proposal's ground stays as the
     obstruction left it", attributed to "the third branch its drafters
     displayed", exists NOWHERE in add4c806/69d8b24b — or the corpus
     (both phrases occur only in the build itself; swept). D-1 option
     (b) displays exactly TWO outcome branches; the SK-2 cell's "NOT
     proved either way" describes the pre-determination STATE. The
     UNDECIDABLE-TODAY return is the build's own lawful outcome (the
     commission's own menu anticipates it); the attribution is false at
     bytes. Repair: strike the quotation marks and the attribution
     clause; the substantive §6.3 content stands unchanged.
N-1  NOTE: §1's reading-scope line for b53d9e93 ("(S1)-(S8)") understates
     what the tail-clause walk consumed from LOC; consumption was routed
     through fdf20bd4's sealed consolidation (read in full), so it is
     lawful — a scope-line clarification only.
N-2  NOTE: NG-2's p6 rests on the "actual finite … class" reading of
     PJ2 §1 plus the unbuilt-extension fact at KER; sound today; if a
     distribution-topology kernel extension is ever sealed, p6 (and NG-2)
     must be re-proved on the extended class.
```

No finding of: base-swap (all fixed carriers held at sealed identity),
member binding, adoption, quantifier conflation, fence breach, seal
error, sweep misstatement, false verbatim quote OTHER than F-B, or
missed decisive display that would flip the verdict to EMPTY or
NON-EMPTY today.

## 5. VERDICT

```text
VERDICT = CONFIRMED-WITH-CORRECTIONS

  The determination stands: OB-2 = UNDECIDABLE-TODAY for BOTH families;
  emptiness NOT proved, non-emptiness NOT proved; SK-2 NOT fired; ACT-3
  NOT forced; the D-1 menu unchanged. The two new armed kill conditions
  stand: NG-2 exactly as displayed (independently re-derived, exact);
  NG-3 with the F-A premise added. The localization (§5.3) and the
  blocker board (§6.2 B-1/B-2/B-3) are genuine and, under my own
  adversarial completeness hunt, complete at today's displayed bytes.

  CORRECTIONS REQUIRED (both repairable in a V002 or a registrar note;
  neither reopens the determination):
    C-1 = F-A: add the Sch_N-linearity/pin premise to NG-3's display
          and flag line.
    C-2 = F-B: strike §6.3's fabricated quotation and attribution.

  INDEPENDENCE RECORD: my pre-committed verdict class (hash 0c7f49f0…,
  fixed 21:40:58 CDT, before the build was opened) was "EMPTY not
  provable; NON-EMPTY only conditional on the named fixed-data
  conditions; honest best = UNDECIDED-AT those conditions" — the same
  outcome class the build returned, reached blind on both sides. The
  build additionally found NG-2, which my pre-commit had not derived;
  I re-derived it from scratch and it is correct.
```

## CHOICE LEDGER (terminal)

```text
CL-1  SWEEP BOUNDARY: seed+pin set as listed in §1, read at the cited
      spans; corpus greps as displayed in §2/§3.4. A wider sweep could
      change this audit only by exhibiting a sealed display deciding
      B-3(i)/(ii)/(iii) or fixing a B-2 pin. PREMISE(named).
CL-2  READING STANDARD: omission-is-meaningful for typed-map adjectives
      ("continuous and injective" vs "continuous linear") — adopted
      because the build itself applies it at p5; F-A follows from the
      build's own standard, not an imported one. PREMISE(named).
CL-3  My re-derivations import the same standard external mathematics
      the build names (invariance of domain; f.d. Hausdorff TVS
      uniqueness); exact, non-numeric. PREMISE(named).
CL-4  DISCLOSURE: directory listings displayed fenced-class BASENAMES;
      no fenced file's content was opened or used. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  My battery's exact-rational model (the 2x2 L-map agreement check) and
  threshold table are verification instruments for displayed symbolic
  claims only; no toy object, value, or stage enters any conclusion,
  premise, or the verdict. The zero-tail witnesses audited in §3.3
  remain, as in the build, displays — promoted to nothing.
```

## FLAG BLOCK

```text
OB2_AUDIT = CONFIRMED-WITH-CORRECTIONS (build c8053f6c; verdict
  UNDECIDABLE-TODAY for both families SURVIVES adversarial audit)
CORRECTIONS = 2: F-A (NG-3 owes the unsealed Sch_N-linearity/pin premise,
  unflagged; kill condition stays armed with one more named premise);
  F-B (§6.3's drafter-attributed quotation is fabricated — the phrase
  exists only in the build; strike quotes+attribution; substance stands)
NOTES = 2 (N-1 reading-scope line; N-2 NG-2's p6 tied to the sealed
  finite class — re-proof owed if a kernel extension is ever sealed)
NG2 = CONFIRMED as displayed (re-derived independently: p5 linearity
  derivation exact; invariance-of-domain step exact; thresholds rank>=3
  real / rank>=2 complex re-computed exact in fresh venv)
NG3 = CONFIRMED IN SHAPE with F-A premise added; consistency half
  PROVABLE (re-derived + exact model check)
W0_WHOL = legal at bytes, fiber-local, honestly fenced; no member bound
EMPTY_SIDE = complete at displayed bytes (no fourth pinch found)
NONEMPTY_SIDE = complete at displayed bytes (free/colimit route blocked
  exactly at the build's B-1/B-3; my pre-commit route independently died
  at the same walls)
SK2 = NOT FIRED (confirmed at add4c806/69d8b24b bytes) ; ACT3 = NOT
  FORCED ; D1_MENU = UNCHANGED
INDEPENDENCE = pre-commit hash 0c7f49f0… fixed 2026-08-14 21:40:58 CDT
  BEFORE build opened; convergent outcome class, blind
SEALS = build 1/1, seeds 5/5, pins 8/8 MATCH; MISMATCHES = 0
JOINT_FIBER_BAR = respected by build and by this audit (no fiber result
  promoted anywhere)
HELD_CONSTRUCTION = untouched, unopened, unpresupposed
DETERMINATION_ONLY = held (no member bound, no adoption, no selection,
  no common cell, no number, no float, no value frozen)
SWEEP_CUTOFF = 2026-08-14 21:34 CDT (build mtime); exhaustiveness
  sweep-displayed only
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the OB-2 determination is sound where it is load-bearing and
  honest where it is open: UNDECIDABLE-TODAY stands, SK-2 does not fire,
  and the record gains two armed kill conditions — one exactly as
  displayed (NG-2), one owing a named premise (NG-3+F-A). Two
  corrections; neither moves the verdict.
```
