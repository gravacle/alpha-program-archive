# STAGE 8 — THE R.3'(c) SUMMED SECOND-ORDER REMAINDER (Q-1049's EXACT RESIDUE) AND THE HALF-PLANE LOCALIZATION: TWO CONSTRUCTIONS ATTEMPTED AGAINST THE THREE WALLS

## BLIND ANALYST — CODENAME R3C-BUILD — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (R3C-BUILD). Two constructions attempted against the
sealed record as amended (adoption of record cbfbb74c): (A) a summed-level
second-order identity or estimate for sum_surv c_p (rho_p - 1 - tr Delta_p)
with n-uniform constants, NOT factoring through per-pair HS majorants — the
exact residue of Q-1049, engaged at the three walls W-1/W-2/W-3 of record
(e5b0879b §4.2, audited cbe6ff4f); (B) derivation of the corrected
half-plane separation Re(e^{i theta} rho_p) >= delta > 0 for the five
surviving pairs at a summed-compatible (n-uniform) quantifier, H-R
consumable only as the named hypothesis. Honest outcomes equal citizens per
task: DELIVERED / PARTIAL-DELIVERED / FAILS-AT / REFUTED.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — two CAS batteries (sympy
1.14.0, fresh venv `r3cvenv`), reproduced in full in §2.8 and §3.8 with
output verbatim (14/14 and 10/10 PASS); NOTHING numeric evaluated; every
constant symbolic or an exact rational/surd; the dilation parameter L is a
FORMAL symbol, never instantiated; no physical quantity computed, bounded
numerically, or evaluated; alpha never computed or approached. No scale,
imported GR, or faithfulness authority. M(t) sharp (nothing mollified
anywhere below). Equal-time FALSE by C6 (every HS object referenced is
two-time). No carrier or cellulation datum introduced. No
register/tracker/plan/road/ledger/lens file read. Output name probed before
write: ABSENT. No git action; no existing file edited; not registered, not
committed, not pushed. No flag flipped; no witness retired (witness status
is the registrar's). Marks: DERIVED / CLAIMED / CONDITIONAL(premise named)
span by span.

---

## 0. VERDICT IN ONE LINE

**TASK A: FAILS-AT — with the residue SHARPENED and localized. A
summed-level second-order IDENTITY exists and is derived exactly (new, the
c-weighted quadratic polarization): sum_p c_p e_2(M_p) = e_2(sum_p c_p M_p)
+ sum_{p<q} c_p c_q e_2(M_p - M_q), with e_2 the determinant's exact
second-order content (CAS-pinned on five fully symbolic matrices at the
derived weights, with its bilinear polarization covering every second-order
cross term) — but it CONTROLS nothing on the sealed basis: the first term
is majorized only by the SURVIVING-instance HS square (exactly D-i's open
transfer), and the ten pairwise-difference terms re-enter per-pair-type
objects one level down — the four (0,0)-to-quartet differences carry bare
degree-0 content, and the six within-quartet differences are odd-in-lambda
(interband-only in the slab closed form, exact) whose leading surviving
stratum is the single-J5 stay class, BELOW the HS threshold, with no sealed
kill applicable — W-2 bites again at the difference level. The kill route
is closed EXACTLY: the C4 kill is a FIRST-moment mechanism (m_0 = m_1 = 0)
and the second-order form consumes SECOND moments — CAS-exact: with branch
content entering linearly, sum_{mu,lambda} w_mu^* w_lambda
tr[(A_mu B_lambda)^2] = tr[(A_1 B_1)^2] (m_2^2 = 1): the content the
first-order kill removes returns SQUARED; no analogue of the double kill
exists at second order (W-1 at the algebra: non-commutative ordering bars
the factorization; trace cyclicity cannot adjacent-ize the doubled branch
appearances). The dilation covariance DOES transfer: rho_p^{(L)}(a) =
rho_p^{(1)}(La) exactly (det/tr conjugation invariance on the per-pair
covariance of record), so the summed remainder obeys R^{(L)}(a) =
R^{(1)}(La) and carries similarity-class weight |C|_4^{1/2} at the collapse
level (the remainder starts at a-order m >= 2; two polydisc powers,
(24/pi)^{1/2}|C|_4^{1/2} = L^2 exact) and |C|_4^{1/4} floor in general —
but ONLY conditional on the carrier dilation-closure premise P-1, which for
determinants is NOT dischargeable by the compression line (dets are not
compression-monotone; daylight vs the first-order case, named), and the
covariance cannot touch the carrier axis (infinity times L is infinity, of
record). NOT REFUTED: the object stays undecided — majorants diverge, no
lower bound exists sealed. EXACT SHARPENED RESIDUE: unit-cell, carrier
axis, collapse level — n-uniform control of (i) the surviving-instance
square (D-i) and (ii) the pairwise-difference e_2 form; plus H-R and the
C-L2 error input for the beyond-collapse/beyond-e_2 tail; plus P-1 for the
class reduction. TASK B: FAILS-AT — the half-plane separation is NOT
derivable at the n-uniform quantifier: the per-pair majorant chain is dead
of record (52f2490b; ZF o-1), and H-R-conditionality is INSUFFICIENT by an
exact new witness — a family with rho_res IDENTICALLY 1 whose Carleman
segment is t - log(1+t) -> infinity: the segment is NOT a function of
rho_res; the per-pair HS factors are load-bearing; no summed-to-per-pair
bridge exists sealed and a control-carrying one is the V-2 void-trigger
shape; fixed-n localizations exist exactly (disc-to-half-plane) but their
radius is n-dependent (o-1). DELIVERED MEANWHILE, exact: the region
condition CHARACTERIZED — five surviving rho's in a region S force
Z_hat != 0 IFF 0 lies outside the c-Minkowski mix (1/2)S + (1/8)S x 4;
half-plane separation (0 outside conv S) is SUFFICIENT of record and NOT
NECESSARY (new exact witness: S = the cube roots of unity has 0 = centroid
in conv S, yet all 3^5 = 243 assignments give Z_hat != 0 — the derived
weight multiset {4,1,1,1,1}/8 cannot 3-balance, 3 does not divide 8); the
corrected consumable condition of record is unweakened. IF the corrected
localization ever closes, the licensed consequence is exactly: R-L0 is not
needed for the n = 1 leg — NO STRONGER; it does not close here. NEITHER
task's findings bear on the ADOPTED summed estimate at its own quantifier
(the amendment's clause is the operator HS family; every finding here lives
at the determinant/second-order scalar level or the region level); no
falsifier V-1..V-6 engages. Gate-list membership unchanged.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest below recomputed from bytes at path THIS session by full digest;
each compared against its `.seal.sha256` sidecar AND (for the 14 sources)
against the §1 seal table of the artifact under consumption (e5b0879b),
AND against the tasked prefixes. 16/16 unique files; ALL MATCH; NONE
UNVERIFIABLE. (The tasked "15 digests" count of the e5b0879b table
resolves at the bytes to 14 table rows, one of which is the ENTRY addendum
also tasked separately; all rows recomputed.)

```text
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md                  MATCH (tasked e5b0879b — Tasks B/C of record; walls W-1/W-2/W-3; corrected region condition)
cbe6ff4f6fa6cd3c94486dc27966f508e974e0f2004c0294ef3849c14a5fd132  STAGE8_S2_DISCHARGE_AUDIT_V001.md            MATCH (tasked cbe6ff4f — all five audits CONFIRMED)
cbfbb74c59f5a88742524dc41958514b4c38418d025f85154f6e1d3594ee1a43  STAGE8_SUMMED_S2_ENTRY_ADDENDUM_V001.md      MATCH (tasked cbfbb74c — the ADOPTION of record)
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md           MATCH (S2CAND — §4.3 R.3'(a)-(e); the (c) per-pair BAR verbatim)
c234600005c83b2373ce1d3a33d680eacc3444964c5f0eb8f5421c546f30bb7b  STAGE8_SUMMED_S2_CANDIDATE_CHECK_V001.md     MATCH (S2CAND-CHK)
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md         MATCH (RL4RL0 — §3.1-§3.5; derived weights; convexity footing)
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                 MATCH (RL4RL0-CHK)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM — trilemma bound; double m_0 kill; parity floors; dominated a-series)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (REM-CHK, CONFIRMED)
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH (ZF — §4.1(c), §4.2, §4.3 o-1/o-2/o-3)
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH (ZF-CHK — landing-set correction)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH (R2 — slab closed forms; tau_R = pi/sqrt2; weights/moments)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH (R2-CHK)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1 — :652-666 Carleman display; :720-727 H-R no-default; :803-848 R.3/R-L2b/colour sum; :851-894 R-L0/R-L0b)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA — carrier; profile; J linear in the generator)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (per-pair refutation; flags read in full)
```

Sealed content read at path this session and consumed: e5b0879b §4.1 (the
exact decomposition), §4.2 (W-1/W-2/W-3 at the bytes), §4.3, §5.1-§5.3
(the corrected half-plane condition and its exact witnesses), §2.3 (the
covariance chain STEPS 1-7), §2.6 (r-A1..r-A4); cbe6ff4f §2-§6 (all
CONFIRMED; A13 witnesses; A14/A15); cbfbb74c in full (adoption; falsifiers
in force; witnesses LIVE); S2CAND §3.3 (S2'-O), §4.1 (alpha' a symbol),
§4.3 (R.3'(a)-(e); the (c) BAR: "the per-pair route to it is REFUTED of
record (52f2490b) and is hereby BARRED"), §4.4 (D-i..D-iv), §5.3 (V-1..
V-6), §5.4 (NF-1); REM §2.1-§2.4 (X; the strata taxonomy; parity floors
2o + c >= 4 assembled; the (c=1,o=0) exclusion UNDER THE WEIGHT SUM), §3.1
(one power of escape is not enough — degree-(-1) HS^2 linear in carrier),
§3.2 (the double kill), §3.5 (dominated a-series, all polydisc orders), §5
(grade e-i/e-ii; overturn conditions); RL4RL0 §3.1-§3.5 (R-L0 quantifier;
u-1..u-4; the convexity footing all c > 0; R-L0b shape); ZF §4.1(b)(c)
(entirety at fixed n; per-pair carrier limit does not exist), §4.2
(normalization; c = (1/2, 1/8 x 4); the nonvanishing display), §4.3
(o-1/o-2/o-3); E1 :652-666 (the linear Carleman display, VALID ONLY ON THE
SURVIVING SECTOR), :679-689 (S1 certified outward enclosure required; cell
sup deferred to R-L0), :720-727 (S4 H-R: rho_res a NAMED HYPOTHESIS, "May
NEVER default to 1 or to any other value (F'-3)"), :803-833 (R.3; the
|tr[BXY]| <= ||B||_op ||X||_2 ||Y||_2 derivation line; :817-824 the
bookkeeping downgrade; R-L2b :826-832), :835-848 (colour-sum closure;
sector hygiene), :851-894 (R-L0/R-L0b; :884-885 the licensed conditional);
PA :75-128 (carrier; r(t); sharp M(t); v(t); b_D; J = -(Q b_D Q) (x)
alpha_x); 52f2490b §0 + §5 (RL2b_uniformity_refuted = true;
X_HS_norm_infinite = true).

---

## 2. TASK A — THE R.3'(c) SUMMED SECOND-ORDER REMAINDER (Q-1049's RESIDUE)

### 2.1 The object and the question, located exactly

The object (e5b0879b §4.1, derived on E1 :663-666 + R.3'(b), consumed at
its sealed grade):

```text
  R_n(a) := Z_hat_n(a) - 1 - tr[Delta^{Sigma,surv,c}_n(a)]
          = sum_surv c_p ( rho_{p,n}(a) - 1 - tr[Delta_{p,n}(a)] )
          = sum_surv c_p ( - seg_{p,n} + (e^{Log rho_p} - 1 - Log rho_p) ),
  seg_p = int_0^1 tr[(1 + A_{s,p})^{-1} A_{s,p} Delta_p] ds ,
  c = (1/2, 1/8, 1/8, 1/8, 1/8),  sum c = 1, all c > 0  [re-derived, RA1].
```

The question of record: does a summed-level second-order identity or
estimate exist controlling R_n with n-uniform constants, NOT factoring
through per-pair HS majorants — an object-level cancellation? The per-pair
route is BARRED by the adopted text (S2CAND §4.3(c), verbatim at bytes).
Five candidate directions were built and worked; each is reported with its
exact wall engagement. Baseline structure used throughout (DERIVED, CAS
RA8b): det(I + sD) = 1 + s tr D + s^2 e_2(D) + ..., e_2(M) := ((tr M)^2 -
tr(M^2))/2 — so AT THE COLLAPSE LEVEL (A_p(0) = 0; CONDITIONAL on R-L4a/b
+ the C-L2 error input, as R.3'(a) itself is) the remainder starts at
a-order m >= 2 and its a^2-leading content is EXACTLY sum_surv c_p
e_2(Delta_p^{[1]}); beyond the collapse level the segment's A(0)-coupled
term contributes at a-order 1, weighted by the C-L2 error (G_cm — D-iii)
and consuming H-R. Nothing below papers over that conditionality.

### 2.2 Route I — the summed-level second-order identity (variance/e_2 polarization)

**THE IDENTITY EXISTS. DERIVED, exact, new** (CAS RA3a on five fully
symbolic matrices at the derived weights; algebraic proof by the c-mean
polarization, valid for ANY weights with sum c = 1):

```text
  sum_p c_p e_2(M_p)
    = e_2( sum_p c_p M_p )  +  sum_{p<q} c_p c_q e_2( M_p - M_q ) ,
and its bilinear polarization with b(X,Y) := tr X tr Y - tr(XY)  [RA3b]:
  sum_p c_p b(M_p, N_p)
    = b( sum c M, sum c N ) + sum_{p<q} c_p c_q b( M_p - M_q, N_p - N_q ),
```

which covers every cross term of the a-series at second determinant order.
Applied at M_p = Delta_{p,n}(a): the c-weighted second-order content of
R_n equals the second-order content OF THE SUMMED SURVIVING OBJECT
Delta^{Sigma,surv,c}_n plus the pairwise-difference form. This is the one
summed-level second-order identity the sealed algebra admits, and it is
exactly an accounting of W-1: it does not commute the sum with the
nonlinearity; it quantifies the failure as the ten difference terms.

**What it controls, exactly — and where W-2 re-bites.**

```text
(i)  |e_2(Delta^{Sigma,surv,c}_n)| <= (1/2)(|tr Delta^Sigma_n|^2
     + ||Delta^{Sigma,surv,c}_n||_2^2): the second factor is the
     SURVIVING-instance HS square. Of record the discharged/delivered HS
     control is at the ALL-PAIRS instance only (REM; e5b0879b §2 at
     alpha' = 1/4); the surviving transfer is EXACTLY obligation D-i
     (S2CAND §4.4; the m_0-factorization break, ZF o-2(ii)). OPEN — but
     note honestly: this piece is SUMMED-level, not per-pair; if D-i ever
     delivers the surviving instance, this term is majorized without any
     per-pair input. The trace factor consumes R.3'(b)'s object (S1-type
     enclosure — uncertified, E1 :687).
(ii) THE DIFFERENCE FORM sum_{p<q} c_p c_q e_2(Delta_p - Delta_q): ten
     unordered pairs — four (0,0)-to-quartet differences at weight
     c_00 c_q = 1/16 each; six within-quartet differences at weight 1/64
     each. These are per-pair-TYPE objects one level down, and W-2
     re-engages on them (§2.3, §2.4): no sealed majorant certifies any of
     them n-uniformly finite, and their leading strata sit AT or BELOW the
     HS threshold. The identity therefore RELOCATES the obstruction; it
     does not cancel it.
```

Wall engagement, Route I: W-1 does NOT bar the identity (it is an exact
quadratic polarization, not a linearity claim); W-1's content reappears as
the nonvanishing of the difference form (the exact price of moving the sum
inside the square). W-2 BITES at both right-hand pieces: (i) is D-i-gated
(summed-level, so the bite is an OPEN OBLIGATION, not a refuted route);
(ii) is per-pair-type and majorant-dead (§2.4). W-3 BITES beyond the
e_2/b-content: the all-orders tail is the segment of record, consuming
rho_res (H-R, no-default, E1 :725-727) — and beyond the collapse level
also at first order (§2.1). Marks: identity DERIVED; the application to
Delta_{p,n} CONDITIONAL(collapse level: R-L4a/b + C-L2) for the "starts at
m >= 2" scoping; the majorant statements GROUNDED at the cited records.

### 2.3 Route II — the double m_0 kill and the parity floors at second order

**THE KILL DOES NOT EXTEND — exact, and now located at the moments.** The
C4 kill is a FIRST-moment mechanism: m_0 = m_1 = 0 [RA2, of record]. The
second-order (determinant-level) form consumes SECOND moments, and
m_2 = -1 != 0 (m_4 = -2). Two exact displays:

```text
(RA4a, scalar) content f(l) = f_0 + f_1 l is KILLED at first order
   (sum_l w_l f = 0 exactly) yet returns SQUARED at second order:
   sum_l w_l f^2 = m_2 f_1^2 = -f_1^2 != 0.
(RA4b, operator — the sharp one) with branch factors entering LINEARLY in
   the dressing content, A_mu = A_0 + mu A_1, B_lam = B_0 + lam B_1 (2x2
   fully symbolic), the all-pairs weighted FIRST-order trace is killed:
   sum_{mu,lam} w_mu^* w_lam tr[A_mu B_lam] = 0; while the all-pairs
   weighted SECOND-order trace is
   sum_{mu,lam} w_mu^* w_lam tr[(A_mu B_lam)^2] = tr[(A_1 B_1)^2] EXACTLY
   (coefficient m_2^2 = 1): the weighted second-order form PROJECTS onto
   the square of precisely the content the first-order kill removed.
```

The structural reason the operator case cannot be repaired (W-1 at the
algebra): in tr[(A_mu B_lam)^2] = tr[A_mu B_lam A_mu B_lam] the two mu-
appearances are SEPARATED by lam-factors; trace cyclicity permutes
cyclically and cannot adjacent-ize them (RA5's witness is the scalar
shadow of this: the weighted sum of squares is not the square of the
weighted sum, gap = the variance term, exact). So no rearrangement
re-creates a factorized (sum w f)(sum w g) shape on which m_0 could act.
The parity floors of REM §2.3 do not rescue this either: they are floors
for the WEIGHT-SUMMED factors (odd n dies under sum_l w_l f(l) = 0 for f
odd); the second-order object is not a weight-summed-linear functional of
either branch. Wall engagement, Route II: W-1 BITES at the algebra (the
route dies before any norm is taken); W-2/W-3 unreached. Marks: RA4a/RA4b
DERIVED (exact lemmas, CAS); their application to the sealed strata
GROUNDED at REM's carried grade (the sealed per-branch content is an
all-orders series; the lemma is the moment-structure witness that no
first-moment kill survives quadratic appearances). This finding CONTRADICTS
no sealed bytes: REM's kill claims are per-branch-LINEAR (a-order-
independent at the OPERATOR level, REM §3.5); the leading V_2 (x) V_2
stratum of record is itself m_2-content — consistent, not contradicted.

### 2.4 The difference form: parity protection and where it ends

The ten differences of §2.2(ii), at the collapse level (V_p(0) = I on the
surviving sector), with p = (mu, lam), first order in a:

```text
  Delta^{[1]}_{mu lam} = a_+ C u_mu(0)^dag F_lam C + a_- C F~_mu^dag u_lam(0) C ,
  D_{++} - D_{+-} = a_+ C u_+^dag (F_+ - F_-) C + a_- C F~_+^dag (u_+ - u_-) C   (etc.),
  D_{00} - D_{quartet}: no parity relation between u_0 (undressed) and u_±.
```

(1) WITHIN-QUARTET (six differences, weight 1/64): the difference
u_+ - u_- (and F_+ - F_-) is the ODD-in-lambda part of the dressing. In
the slab closed form of record this is EXACT [RA7]: the intraband
amplitude A(mu) is EVEN (A(-mu) = A(mu)) and the interband amplitude
B(mu) is ODD (B(-mu) = -B(mu)) — so the within-quartet difference is
interband-only in the slab reduction. DERIVED (slab, exact). At the full
stratum level (GROUNDED, REM §2.3 carried at grade): odd-in-lambda content
is the odd-S-insertion-count strata, and WITHOUT a weight sum the odd-n
sea-sea class EXISTS — its leading member is the single-J5 stay stratum
(n = 1, flips = 0, stays = 1): c = 1, o = 0, pointwise degree -1, i.e.
2o + c = 1, BELOW the HS threshold (REM §3.1: a degree-(-1) intraband
amplitude has HS^2 linear in the carrier). REM's "there is NO (c = 1,
o = 0) class anywhere" is a statement about the WEIGHT-SUMMED factors —
the parity kill that removed it is exactly what a bare difference lacks.
No sealed identity kills it in the difference; its coefficient is the J5
factor of record (the sealed contrast: nonvanishing). CLAIMED at taxonomy
grade: the within-quartet differences are not carrier-uniformly HS; at
minimum, NOTHING SEALED certifies them finite — which alone kills the
route as a discharge.
(2) (0,0)-TO-QUARTET (four differences, weight 1/16): u_0 is the
UNDRESSED branch; the difference u_0 - u_± retains bare degree-0 content
(the J vertex's O(1) longitudinal intraband part has NO coincidence zero
— the sealed contrast, REM §2.2), the class whose HS^2 scales with the
carrier volume (REM §3.1; the 52f2490b mechanism). Same conclusion, worse
degree.

Wall engagement: W-2 BITES on every difference (their sealed-basis
majorants are per-pair-type HS norms, not certified, leading strata at or
below threshold; the scaling covariance cannot rescue an n-wise divergence
at fixed cell — infinity times L is infinity, of record e5b0879b §4.2
W-2). The derived weights' POSITIVITY again turns against the estimate at
the majorant level: for self-adjoint shadows every difference term ADDS
(tr[H^2] >= 0, RA6), and for the general complex objects NO sign exists at
all (tr[K^2] = -2 < 0 witness, RA6) — so neither a cancellation NOR a
coercive lower bound is available at the majorant level. This extends the
S3c pattern of record to second order.

### 2.5 Route III — the exact dilation covariance at second order

CONDITIONAL(P-1, named below) on the covariance chain of record (e5b0879b
§2.3 STEPS 1-6, audited; identity-grade given the sealed constructions):
V^{(L)}_{mu lam}(a) = U_L V^{(1)}_{mu lam}(La) U_L^dag at ALL a-orders
including a = 0, per pair, weight-blind. Then, at fixed carrier n (finite
rank, so tr and det are defined):

```text
  det and tr are conjugation-invariant [RA8a, exact]:
     rho_p^{(L)}(a) = rho_p^{(1)}(La),  tr[Delta_p^{(L)}(a)] = tr[Delta_p^{(1)}(La)]
  => R^{(L)}_n(a) = R^{(1)}_{n'}(La)   — THE REMAINDER IS COVARIANT, EXACTLY.
  At the collapse level R starts at a-order m >= 2 [RA8b], so for
  0 < L <= 1: (L eps)^m = L^2 L^{m-2} eps^m <= L^2 eps^m  [RA8c], and
     L^2 = (24/pi)^{1/2} |C|_4^{1/2}                        [RA8d]:
  ON THE SIMILARITY CLASS the summed remainder carries the weight
  |C|_4^{1/2} — ONE POWER MORE than the first-order object's derived
  alpha' = 1/4. Beyond the collapse level the A(0)-coupled first-order
  term caps the class weight at |C|_4^{1/4} (one power, m >= 1).
```

**PREMISE P-1 (named, NOT discharged, and NOT dischargeable the r-A2
way):** the covariance moves the carrier index (n -> n'); for the
FIRST-order HS object the analogous premise was dischargeable by the one
sealed compression line ||Pi X Pi||_2 <= ||X||_2 (e5b0879b r-A2). For the
remainder R_n — a DETERMINANT-level scalar — no compression monotonicity
exists: det of a compression is not dominated by det of the full object,
and R_n is not a norm. P-1 therefore stands as a REAL premise here (the
D3/Hermite-Galerkin family's closure under dilation), a point of exact
daylight between the first-order and second-order transfers. Wall
engagement, Route III: W-1/W-2/W-3 do NOT bite the covariance itself; it
delivers the CELL axis on the similarity class and delivers NOTHING on the
carrier axis, where W-2 stands untouched. CONSEQUENCE (descriptive): IF a
unit-cell n-uniform bound Q^Sigma_1(eps) ever exists (the open residue),
the class-wide R.3'(c) bound follows as (24/pi)^{1/2} |C|_4^{1/2}
Q^Sigma_1-shaped at the collapse level, under P-1 — the cell axis of
R.3'(c) REDUCES to the unit cell.

### 2.6 Routes IV/V — weight-vector identities; trace-cyclicity/unitary invariance

(IV) The identity of Route I holds for ANY weights with sum c = 1 —
nothing special to (1/2, 1/8 x 4) improves it. What IS special to the
derived vector: (a) POSITIVITY — all five c >= 1/8 > 0 — which turns
AGAINST majorant-level cancellation (§2.4, the S3c pattern at second
order); (b) the arithmetic of the multiset {4,1,1,1,1}/8, which delivers a
REAL exact result — in Task B's region characterization (§3.6), not here.
(V) Trace cyclicity cannot adjacent-ize the doubled branch appearances
(§2.3); unitary invariance delivers exactly Route III's covariance and
nothing else. Wall engagement: W-1 bites (IV at the majorant sign, V at
the ordering); nothing reaches W-3.

### 2.7 Task A verdict and the exact residue

```text
TASKA = FAILS-AT( the unit-cell carrier axis, at every route:
  W-1 bites at the algebra (Routes II/V: no factorization for m_0 to act
  on — the second-order form consumes m_2 = -1, exact; Route I is the
  exact accounting of W-1's price); W-2 bites at every majorant (Route I's
  difference form: per-pair-type, strata at/below threshold, positivity
  adverse, no sign; the covariance cannot rescue the carrier axis); W-3
  bites at the all-orders tail and, beyond the collapse level, at first
  order (the segment consumes rho_res = H-R, no-default).
  NOT REFUTED: the remainder itself is not shown infinite — the majorants
  diverge; the object is undecided; no norm lower bound exists sealed.
  DELIVERED MEANWHILE (all exact, CAS-pinned): (d-1) the summed-level
  second-order identity and its bilinear polarization (RA3a/RA3b) — the
  answer to "does a summed-level second-order IDENTITY exist" is YES, and
  it is THIS one; the answer to "does it or any estimate CONTROL R_n
  n-uniformly on the sealed basis" is NO; (d-2) the moment localization of
  the kill's failure (RA4a/RA4b: first-moment kill, m_2^2 = 1 projection
  onto killed-content-squared); (d-3) the slab parity split (RA7) and the
  taxonomy placement of the difference form's leading strata; (d-4) the
  remainder's exact dilation covariance R^{(L)}(a) = R^{(1)}(La) with
  similarity-class weight |C|_4^{1/2} at the collapse level, |C|_4^{1/4}
  floor in general, CONDITIONAL(P-1).
  EXACT RESIDUE, SHARPENED FROM Q-1049's: at the UNIT CELL, on the
  CARRIER axis, at the collapse level, n-uniform control of exactly two
  summed-level pieces — (i) e_2/b of the SURVIVING summed instance
  (majorized by the surviving HS square: D-i's open transfer + the
  R.3'(b) trace enclosure), and (ii) the pairwise-difference form
  sum_{p<q} c_p c_q e_2(Delta_p - Delta_q) (ten terms, weights 4 x 1/16 +
  6 x 1/64, leading strata: bare degree-0 for the (0,0)-differences,
  single-J5 stay for the within-quartet differences) — on no sealed
  artifact; PLUS H-R and the C-L2 error input for the tail; PLUS P-1 for
  the class reduction. The per-pair route stays BARRED (adopted text);
  witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS. )
```

### 2.8 THE TASK A CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `r3cvenv` (under /private/tmp scratch; nothing
written to the workspace but this artifact and its seal).

```python
# R3C-BUILD Task A CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv r3cvenv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated.
# Groups: RA1/RA2 record re-derivations (census, weights, moments); RA3 the summed-level
# second-order identity (variance/e_2 decomposition, NEW) and its bilinear polarization;
# RA4 the moment analysis of the kill at second order (first-moment kill, m_2 survival,
# the exact all-pairs second-order projection); RA5/RA6 wall dressings (pair-diagonality
# at second order; sign unavailability); RA7 slab parity (even/odd split of the dressing);
# RA8 the dilation covariance at second order (det/tr conjugation invariance, Jacobi,
# the m >= 2 polydisc split, the |C|_4^{1/2} arithmetic); RC1 continuity re-run.
import itertools
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

# ===== RA1 — census and derived weights, re-derived from the sealed data =====
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
ph = [1, -1, -1]
pairs = [(i, j) for i in range(3) for j in range(3)]
surv = [(i, j) for (i, j) in pairs if ph[i] == ph[j]]
Nsurv = sp.nsimplify(sum(w_v[i]*w_v[j] for i, j in surv))
c_w = {p: sp.nsimplify(w_v[p[0]]*w_v[p[1]]/Nsurv) for p in surv}
cs = [c_w[p] for p in surv]
ok("RA1 census: 5 surviving; N_surv = 1/2; c = (1/2, 1/8 x 4) all > 0; sum c = 1",
   len(surv) == 5 and Nsurv == R(1, 2) and sorted(cs) == [R(1, 8)]*4 + [R(1, 2)]
   and sum(cs) == 1 and all(c > 0 for c in cs))

# ===== RA2 — the moment ledger: the kill is FIRST-moment; m_2 = -1 is NOT zero =====
mom = lambda n: sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v)))
ok("RA2 moments: m_0 = 0, m_1 = 0 (the kills); m_2 = -1, m_3 = 0, m_4 = -2 (m_2 != 0)",
   [mom(n) for n in range(5)] == [0, 0, -1, 0, -2] and mom(2) != 0)

# ===== RA3 — THE SUMMED-LEVEL SECOND-ORDER IDENTITY (variance/e_2 decomposition) =====
# e_2(M) := ((tr M)^2 - tr(M^2))/2 is the determinant's exact second-order content.
# CLAIM (exact, any square matrices, any weights with sum c = 1):
#   sum_p c_p e_2(M_p) = e_2(sum_p c_p M_p) + sum_{p<q} c_p c_q e_2(M_p - M_q).
def e2(M):
    return sp.expand(((M.trace())**2 - (M*M).trace())/2)

Ms = [sp.Matrix(2, 2, lambda i, j, k=k: sp.Symbol(f'm{k}_{i}{j}')) for k in range(5)]
cvals = [R(1, 2), R(1, 8), R(1, 8), R(1, 8), R(1, 8)]
lhs3 = sum(c*e2(M) for c, M in zip(cvals, Ms))
Msum = sum((c*M for c, M in zip(cvals, Ms)), sp.zeros(2, 2))
rhs3 = e2(Msum) + sum(cvals[p]*cvals[q]*e2(Ms[p] - Ms[q])
                      for p in range(5) for q in range(p + 1, 5))
ok("RA3a IDENTITY sum c_p e2(M_p) = e2(sum c M) + sum_{p<q} c_p c_q e2(M_p - M_q)"
   " (5 fully symbolic 2x2, derived weights)",
   sp.expand(lhs3 - rhs3) == 0)
# bilinear polarization b(X,Y) := tr X tr Y - tr(XY): the same decomposition —
# covers every second-determinant-order cross term of the a-series.
def bform(X, Y):
    return sp.expand(X.trace()*Y.trace() - (X*Y).trace())

Ns = [sp.Matrix(2, 2, lambda i, j, k=k: sp.Symbol(f'n{k}_{i}{j}')) for k in range(5)]
Nsum = sum((c*N for c, N in zip(cvals, Ns)), sp.zeros(2, 2))
lhs3b = sum(c*bform(M, N) for c, M, N in zip(cvals, Ms, Ns))
rhs3b = bform(Msum, Nsum) + sum(cvals[p]*cvals[q]*bform(Ms[p] - Ms[q], Ns[p] - Ns[q])
                                for p in range(5) for q in range(p + 1, 5))
ok("RA3b polarized: sum c b(M_p,N_p) = b(sum cM, sum cN) + sum_{p<q} c c b(M_p-M_q, N_p-N_q)",
   sp.expand(lhs3b - rhs3b) == 0)

# ===== RA4 — the kill does NOT extend to second order: moment analysis, exact =====
# RA4a scalar display: content f(l) = f0 + f1 l is KILLED at first order
# (m_0 f0 + m_1 f1 = 0) yet returns SQUARED at second order via m_2 = -1.
f0, f1 = sp.symbols('f0 f1')
first = sp.expand(sum(w*(f0 + f1*l) for w, l in zip(w_v, lam_v)))
second = sp.expand(sum(w*(f0 + f1*l)**2 for w, l in zip(w_v, lam_v)))
ok("RA4a f = f0 + f1 l: sum w f = 0 EXACT (first-moment kill); sum w f^2 = -f1^2 != 0"
   " (m_2 resurrection)",
   first == 0 and sp.simplify(second + f1**2) == 0)
# RA4b THE ALL-PAIRS SECOND-ORDER PROJECTION (exact, 2x2 fully symbolic): with
# A_mu = A0 + mu A1, B_lam = B0 + lam B1 (branch factors entering LINEARLY in the
# dressing content), the weighted first-order trace is KILLED, while the weighted
# second-order trace equals tr[(A1 B1)^2] EXACTLY (m_2^2 = 1): the survivor is the
# m_1-killed first-power content, squared. No analogue of the double kill exists.
A0 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'a0_{i}{j}'))
A1 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'a1_{i}{j}'))
B0 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'b0_{i}{j}'))
B1 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'b1_{i}{j}'))
first_op = sp.zeros(1, 1)
second_tr = sp.Integer(0)
for i, mu in enumerate(lam_v):
    for j, la in enumerate(lam_v):
        Am = A0 + mu*A1
        Bl = B0 + la*B1
        W = w_v[i]*w_v[j]
        second_tr += W*((Am*Bl*Am*Bl).trace())
first_tr = sp.expand(sum(w_v[i]*w_v[j]*((A0 + lam_v[i]*A1)*(B0 + lam_v[j]*B1)).trace()
                         for i in range(3) for j in range(3)))
ok("RA4b all-pairs: sum W tr[A_mu B_lam] = 0 (kill); sum W tr[(A_mu B_lam)^2]"
   " = tr[(A1 B1)^2] EXACT (m_2^2 = 1 — killed content returns squared)",
   first_tr == 0
   and sp.expand(sp.nsimplify(second_tr) - ((A1*B1*A1*B1).trace())) == 0)

# ===== RA5 — W-1 in second-order dress: the quadratic trace is pair-diagonal =====
# scalar witness at the grouped derived weights (1/2, 1/2):
M_a, M_b = sp.Integer(1), sp.Integer(3)
lhs5 = R(1, 2)*M_a**2 + R(1, 2)*M_b**2                    # sum c tr[M^2] = 5
rhs5 = (R(1, 2)*M_a + R(1, 2)*M_b)**2                     # tr[(sum c M)^2] = 4
gap5 = R(1, 2)*R(1, 2)*(M_a - M_b)**2                     # the variance term = 1
ok("RA5 sum c tr[M^2] != tr[(sum c M)^2]: 5 != 4; gap = c c (M_a - M_b)^2 = 1 EXACT",
   lhs5 == 5 and rhs5 == 4 and lhs5 != rhs5 and lhs5 - rhs5 == gap5 == 1)

# ===== RA6 — no sign is available for the correction terms (general matrices) =====
K = sp.Matrix([[0, 1], [-1, 0]])
h_a, h_d = sp.symbols('h_a h_d', real=True)
h_b = sp.Symbol('h_b')
H = sp.Matrix([[h_a, h_b], [sp.conjugate(h_b), h_d]])
tr_H2 = sp.expand((H*H).trace())
ok("RA6 tr[K^2] = -2 < 0 (no favorable sign, general M); hermitian shadow:"
   " tr[H^2] = a^2 + d^2 + 2|b|^2 >= 0 (positivity ADDS — adverse)",
   (K*K).trace() == -2
   and sp.simplify(tr_H2 - (h_a**2 + h_d**2 + 2*h_b*sp.conjugate(h_b))) == 0)

# ===== RA7 — slab parity: intraband EVEN / interband ODD in the coupling =====
om = sp.symbols('omega', nonnegative=True)
mu_, T_ = sp.symbols('mu T', positive=True)
Om = lambda m: sp.sqrt(om**2 + m**2)
A_sl = lambda m: sp.cos(Om(m)*T_) + sp.I*om*sp.sin(Om(m)*T_)/Om(m)
B_sl = lambda m: -sp.I*m*sp.sin(Om(m)*T_)/Om(m)
ok("RA7 slab: A(-mu) = A(mu) (intraband EVEN); B(-mu) = -B(mu) (interband ODD):"
   " the within-quartet difference u_+ - u_- is interband-only in the slab form",
   sp.simplify(A_sl(mu_) - A_sl(-mu_)) == 0 and sp.simplify(B_sl(mu_) + B_sl(-mu_)) == 0)

# ===== RA8 — the dilation covariance at second order =====
# RA8a det/tr conjugation invariance (the covariance transfers rho_p exactly):
U = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'u_{i}{j}'))
Y = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'y_{i}{j}'))
conj = U*(sp.eye(2) + Y)*U.adjugate()/U.det()             # U (1+Y) U^{-1}, exact inverse
ok("RA8a det(U(1+Y)U^{-1}) = det(1+Y) and tr(U Y U^{-1}) = tr Y (symbolic, exact)",
   sp.simplify(conj.det() - (sp.eye(2) + Y).det()) == 0
   and sp.simplify((U*Y*U.adjugate()/U.det()).trace() - Y.trace()) == 0)
# RA8b Jacobi: det(I + sD) = 1 + s tr D + s^2 e2(D) + s^3 det D (3x3 exact):
s_ = sp.Symbol('s')
D3 = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'd_{i}{j}'))
poly = sp.expand((sp.eye(3) + s_*D3).det())
ok("RA8b det(I+sD) coefficients: [s^1] = tr D ; [s^2] = e2(D) = ((trD)^2 - trD^2)/2"
   " (3x3 exact) — the remainder rho - 1 - tr Delta STARTS at second order",
   sp.expand(poly.coeff(s_, 1) - D3.trace()) == 0
   and sp.expand(poly.coeff(s_, 2) - ((D3.trace())**2 - (D3*D3).trace())/2) == 0)
# RA8c the polydisc split at m >= 2 (two full powers of L on 0 < L <= 1):
L, eps_, m_ = sp.symbols('L epsilon m', positive=True)
ok("RA8c (L eps)^m = L^2 * L^(m-2) eps^m (exact split); d/dm L^m = L^m log L"
   " (nonincreasing on 0 < L <= 1: L^(m-2) <= 1 for m >= 2)",
   sp.simplify((L*eps_)**m_ - L**2*L**(m_ - 2)*eps_**m_) == 0
   and sp.simplify(sp.diff(L**m_, m_) - L**m_*sp.log(L)) == 0)
# RA8d the exponent arithmetic: two powers of L ARE |C|_4^{1/2}:
C4L = sp.pi*L**4/24
ok("RA8d (24/pi)^(1/2) |C_L|_4^(1/2) = L^2 EXACT — the second-order remainder carries"
   " similarity-class weight |C|_4^(1/2), one power more than the first-order 1/4",
   sp.simplify((24/sp.pi)**R(1, 2)*C4L**R(1, 2) - L**2) == 0)

# ===== RC1 — continuity re-run of the record's W-1 witness =====
fA = lambda x: x/(1 + x)
lhsC = R(1, 2)*fA(sp.Integer(1)) + R(1, 2)*fA(sp.Integer(2))
rhsC = fA(R(1, 2)*1 + R(1, 2)*2)
ok("RC1 resolvent pair-diagonality re-run: (1/2)f(1) + (1/2)f(2) = 7/12 != 3/5 = f(3/2)",
   lhsC == R(7, 12) and rhsC == R(3, 5) and lhsC != rhsC)

print("RA-BATTERY-DONE")
```

Output, verbatim (14/14 PASS):

```text
RA1 census: 5 surviving; N_surv = 1/2; c = (1/2, 1/8 x 4) all > 0; sum c = 1: PASS
RA2 moments: m_0 = 0, m_1 = 0 (the kills); m_2 = -1, m_3 = 0, m_4 = -2 (m_2 != 0): PASS
RA3a IDENTITY sum c_p e2(M_p) = e2(sum c M) + sum_{p<q} c_p c_q e2(M_p - M_q) (5 fully symbolic 2x2, derived weights): PASS
RA3b polarized: sum c b(M_p,N_p) = b(sum cM, sum cN) + sum_{p<q} c c b(M_p-M_q, N_p-N_q): PASS
RA4a f = f0 + f1 l: sum w f = 0 EXACT (first-moment kill); sum w f^2 = -f1^2 != 0 (m_2 resurrection): PASS
RA4b all-pairs: sum W tr[A_mu B_lam] = 0 (kill); sum W tr[(A_mu B_lam)^2] = tr[(A1 B1)^2] EXACT (m_2^2 = 1 — killed content returns squared): PASS
RA5 sum c tr[M^2] != tr[(sum c M)^2]: 5 != 4; gap = c c (M_a - M_b)^2 = 1 EXACT: PASS
RA6 tr[K^2] = -2 < 0 (no favorable sign, general M); hermitian shadow: tr[H^2] = a^2 + d^2 + 2|b|^2 >= 0 (positivity ADDS — adverse): PASS
RA7 slab: A(-mu) = A(mu) (intraband EVEN); B(-mu) = -B(mu) (interband ODD): the within-quartet difference u_+ - u_- is interband-only in the slab form: PASS
RA8a det(U(1+Y)U^{-1}) = det(1+Y) and tr(U Y U^{-1}) = tr Y (symbolic, exact): PASS
RA8b det(I+sD) coefficients: [s^1] = tr D ; [s^2] = e2(D) = ((trD)^2 - trD^2)/2 (3x3 exact) — the remainder rho - 1 - tr Delta STARTS at second order: PASS
RA8c (L eps)^m = L^2 * L^(m-2) eps^m (exact split); d/dm L^m = L^m log L (nonincreasing on 0 < L <= 1: L^(m-2) <= 1 for m >= 2): PASS
RA8d (24/pi)^(1/2) |C_L|_4^(1/2) = L^2 EXACT — the second-order remainder carries similarity-class weight |C|_4^(1/2), one power more than the first-order 1/4: PASS
RC1 resolvent pair-diagonality re-run: (1/2)f(1) + (1/2)f(2) = 7/12 != 3/5 = f(3/2): PASS
RA-BATTERY-DONE
```

---

## 3. TASK B — THE HALF-PLANE LOCALIZATION (THE CORRECTED CONDITION)

### 3.1 The condition of record and the tasked question

Of record (e5b0879b §5.2, audited): star-shaped-about-1 + zero-free is
INSUFFICIENT (exact witness: (1/2)(+i) + 4(1/8)(-i) = 0); the sufficient
and consumable condition is HALF-PLANE SEPARATION — if
Re(e^{i theta} rho_p) >= delta > 0 for all five surviving p (some fixed
theta), then |Z_hat| >= delta exactly [re-run, RB1/RB2a]. The tasked
question: can that separation be DERIVED for the five surviving rho's at a
summed-compatible (n-uniform) quantifier from the sealed record, consuming
H-R only as the named hypothesis (a conditional-on-H-R closure acceptable
and marked; H-R never defaulted)? Four routes were attempted.

### 3.2 Attempt 1 — the per-pair majorant chain

To place rho_{p,n} in a fixed half-plane one must control Log rho_{p,n} =
tr[Delta_{p,n}] - seg_{p,n} (E1 :663-666, surviving sector) PER PAIR,
n-uniformly. The trace term requires S1's certified outward enclosure —
uncertified of record (E1 :687). The segment's sealed majorant route is
|tr[BXY]| <= ||B||_op ||X||_2 ||Y||_2 (E1's own R.3 derivation line,
:811-814), which consumes the per-pair HS norms ||A_{s,p}||_2,
||Delta_{p,n}||_2 — whose carrier sups are +infinity OF RECORD (52f2490b:
RL2b_uniformity_refuted = true, X_HS_norm_infinite = true; ZF §4.1(c)).
Dead n-uniformly — refuted-in-substance (note exactly: the adopted BAR of
S2CAND §4.3(c) bars the per-pair route as a DISCHARGE ROUTE FOR R.3'(c);
for the R-L0b localization the per-pair route is not barred by decree — it
is dead on the record). FAILS. No pair is exempt: even the undressed
(0,0) pair's first-order object retains the J vertex's degree-0
longitudinal intraband content (no coincidence zero — the sealed
contrast), the class whose HS^2 scales with carrier volume (REM §3.1).

### 3.3 Attempt 2 — the H-R-conditional route (and its exact insufficiency)

The acceptable conditional was attempted: GRANT H-R (rho_res finite,
named, never valued). Does the segment then close n-uniformly? **NO — and
the insufficiency is now EXACT (new witness, RB4):** on the family
A_s = s t P, Delta = t P (P a rank-one projector, t > 0):

```text
  (1 + A_s)^{-1} = I - (st/(1+st)) P  EXACTLY; eigenvalues {1/(1+st), 1}:
      sup_{s in [0,1]} ||(1 + A_s)^{-1}||_op = 1  — rho_res IDENTICALLY 1;
  yet  seg(t) = int_0^1 tr[(1+A_s)^{-1} A_s Delta] ds = t - log(1+t)
      -> +infinity  as t -> infinity.
```

The Carleman segment is NOT a function of rho_res: no bound of the form
seg <= f(rho_res) exists on any operator family. H-R addresses the
resolvent factor ONLY; the HS factors are load-bearing, and they are the
refuted ones. CONSEQUENCE, exact: the conditional-on-H-R closure closes
NOTHING additional here — the missing content is the per-pair n-uniform HS
control (Attempt 1), which H-R does not touch. W-3 is thereby engaged
honestly: the segment consumes H-R AND MORE; discharging or granting H-R
alone cannot deliver the localization. (H-R is nowhere defaulted: rho_res
enters this artifact only as a granted symbol in a conditional that FAILS
to close, and as the exact witness that it is insufficient.)

### 3.4 Attempt 3 — a summed route to a per-pair conclusion

The separation is a PER-PAIR-QUANTIFIED conclusion (all five rho's
individually). The only n-uniform controls sealed are SUMMED (all-pairs
instance). A derivation summed -> per-pair would be a bridge of exactly
the shape the adopted falsifier V-2 declares void-triggering when it
carries control ("If a sealed derivation is exhibited running summed ->
per-pair control ... the amendment's premise set collapses: VOID" —
S2CAND §5.3 V-2, in force by ENTRY): the route is ABSENT from every
sealed artifact, and a control-carrying instance would void the amendment
rather than close R-L0b. No such bridge is constructed here; none exists
to consume. FAILS (and is program-adverse as a target).

### 3.5 Attempt 4 — fixed-n localization plus a compactness/limit passage

At FIXED n everything is finite (ZF o-1: the chain closes with x_n finite
but n-DEPENDENT) and the geometry is exact [RB3]: |z - 1| <= d < 1 forces
Re z >= 1 - d > 0, so wherever x_n e^{x_n} < 1 the five rho's ARE
half-plane separated at theta = 0 with delta_n = 1 - x_n e^{x_n}. The
localization EXISTS per n; the REGION IS n-DEPENDENT — exactly what the
consumable condition forbids — and sup_n x_n = +infinity per-pair of
record kills every candidate uniform delta. No compactness passage exists
to repair it: the per-pair carrier limit does not exist on the sealed
basis (ZF §4.1(c) — "only the n-indexed family exists"), so no
normal-families or limit-object argument can even be POSED. FAILS.

### 3.6 DELIVERED MEANWHILE — the region condition characterized exactly

For a region S in the plane and the derived weights, define the
c-Minkowski mix Sigma_c(S) := (1/2)S + (1/8)S + (1/8)S + (1/8)S + (1/8)S
(setwise). DERIVED, elementary and exact:

```text
(r-1) CHARACTERIZATION (iff): "every configuration of the five surviving
   rho's inside S has Z_hat != 0" holds IF AND ONLY IF 0 is not in
   Sigma_c(S). (Z_hat ranges over exactly Sigma_c(S) as the five rho's
   range independently over S — the worst-case quantifier a consumable
   region condition must survive.)
(r-2) SUFFICIENCY of the corrected condition, re-derived: every point of
   Sigma_c(S) is a 5-point convex combination of S (sum c = 1, all c > 0
   [RB2b]), so Sigma_c(S) is contained in conv(S): half-plane separation
   (0 outside conv S — the Hahn-Banach normal form of record) implies
   0 outside Sigma_c(S). [RB1/RB2b]
(r-3) NON-NECESSITY (new, exact): the convex-hull condition is NOT
   necessary. WITNESS [RB2c]: S = {1, omega, omega^2} (cube roots of
   unity): S is zero-free, 0 = (1 + omega + omega^2)/3 lies in conv(S)
   (the centroid), yet ALL 3^5 = 243 assignments of the five rho's to S
   give Z_hat != 0 — exhaustively verified in exact root-of-unity
   arithmetic: 8 Z_hat = a + b omega + c omega^2 with (a, b, c) the group
   sums of the weight multiset {4, 1, 1, 1, 1}, and zero forces
   a = b = c, i.e. 3a = 8 — no integer solution (3 does not divide 8).
   The derived weight vector's arithmetic — c in units of 1/8 — is doing
   exact work here.
(r-4) SCOPE, stated honestly: the witness S is DISCONNECTED (three
   points). For connected/star regions — the shape a localization program
   would deliver — nothing here weakens the corrected condition of
   record: the +-i witness (star, zero-free, Z_hat = 0) stands [RB2a],
   and whether 0-in-conv(S) forces 0-in-Sigma_c(S) for CONNECTED S is not
   settled by this artifact. The CONSUMABLE condition of record remains
   half-plane separation; r-1 is its exact boundary.
(r-5) SHARPNESS of the separation bound: |Z_hat| = delta under the
   condition forces Re(e^{i theta} rho_p) = delta for EVERY p (all
   coefficients >= 1/8 > 0 [RB5]) — the bound is tight exactly on the
   boundary line.
```

### 3.7 Task B verdict

```text
TASKB = FAILS-AT( the n-uniform quantifier, on all four routes: the
  per-pair majorant chain consumes S1's uncertified enclosure and the
  per-pair HS norms carrier-divergent of record (52f2490b; ZF o-1); the
  H-R-conditional closure FAILS by exact witness — rho_res identically 1
  with segment t - log(1+t) unbounded: the segment is not a function of
  rho_res, so granting H-R closes nothing (H-R consumed only as the named
  hypothesis; never defaulted, never valued); no summed -> per-pair
  bridge exists sealed and a control-carrying one is the V-2 void shape;
  fixed-n localizations exist exactly but their radius is n-dependent
  with no limit object sealed to pass to. NOT REFUTED: no sealed
  configuration exhibits the five rho's violating separation — the
  condition is underivable, not false.
  DELIVERED MEANWHILE, exact: the region condition CHARACTERIZED — the
  exact criterion is 0 outside Sigma_c(S) (the c-Minkowski mix); the
  corrected half-plane condition is its sufficient normal form (0 outside
  conv S, re-derived) and is NOT necessary (cube-roots witness, 3 does
  not divide 8, exhaustive 243-case exact enumeration); scope caveat
  r-4; sharpness r-5.
  THE LICENSED CONSEQUENCE, stated exactly and only conditionally: IF the
  corrected localization ever closes (an act consuming, per pair, an
  n-uniform phase bound on no sealed artifact, plus H-R, plus the
  R-L4-administered sector definition), THEN R-L0 is not needed for the
  n = 1 leg — NO STRONGER (E1 :884-885 verbatim shape). It does not
  close here. R-L0b remains OPEN; witnesses
  SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND and
  SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STAND (retirement is the
  registrar's, never this artifact's). )
```

### 3.8 THE TASK B CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

Tooling disclosure, on the record: two harness-form corrections were made
during development and are visible in the final script — (i) bare
`simplify` leaves exp(2 i pi/3)-expressions in (-1)^{1/3} form, so the
RB2c identities use `expand_complex` (the S4a lesson of e5b0879b §7,
re-learned); (ii) sympy does not reduce Max(1, 1/(1+st)) under positivity
assumptions, so RB4a states the operator norm by the exact ordering
1 - 1/(1+st) = st/(1+st) >= 0 instead. No mathematical claim changed.

```python
# R3C-BUILD Task B CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv r3cvenv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated
# (the RB2c enumeration is exact integer/root-of-unity arithmetic, 3^5 cases).
# Groups: RB1 half-plane sufficiency re-run (of record); RB2 the exact region
# characterization (the c-Minkowski mix criterion; the +-i star witness of record;
# the cube-roots non-necessity witness, NEW); RB3 the fixed-n disc-to-half-plane
# geometry (where n-uniformity dies); RB4 the H-R-only insufficiency witness (NEW:
# rho_res identically 1 on a family with segment unbounded); RB5 sharpness/equality.
import itertools
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

cvals = [R(1, 2), R(1, 8), R(1, 8), R(1, 8), R(1, 8)]

# ===== RB1 — half-plane sufficiency, re-run with the rotation carried =====
th = sp.Symbol('theta', real=True)
dlt = sp.Symbol('delta', positive=True)
xs = sp.symbols('x0:5', real=True)
ys = sp.symbols('y0:5', real=True)
rhos = [x + sp.I*y for x, y in zip(xs, ys)]
Z = sum(c*r for c, r in zip(cvals, rhos))
lin = sp.expand(sp.re(sp.expand_complex(sp.exp(sp.I*th)*Z))
                - sum(c*sp.re(sp.expand_complex(sp.exp(sp.I*th)*r))
                      for c, r in zip(cvals, rhos)))
xr, yi = sp.symbols('xr yi', real=True)
ok("RB1 Re(e^{i th} Z) = sum c_p Re(e^{i th} rho_p) EXACT (sum c = 1, all c > 0);"
   " |z|^2 - (Re z)^2 = (Im z)^2 >= 0: separation delivers |Z| >= delta",
   lin == 0 and sum(cvals) == 1
   and sp.expand((xr**2 + yi**2) - xr**2 - yi**2) == 0)

# ===== RB2 — the exact region characterization =====
# RB2a the star insufficiency witness of record, re-run:
rho_p = 1 + sp.sqrt(2)*sp.exp(3*sp.I*sp.pi/4)
rho_m = 1 + sp.sqrt(2)*sp.exp(-3*sp.I*sp.pi/4)
ok("RB2a of record: rho_pm = +-i EXACT; (1/2)(+i) + 4(1/8)(-i) = 0 EXACT"
   " (zero-free star about 1 does NOT separate)",
   sp.expand_complex(rho_p - sp.I) == 0 and sp.expand_complex(rho_m + sp.I) == 0
   and sp.expand_complex(R(1, 2)*rho_p + 4*R(1, 8)*rho_m) == 0)
# RB2b mix subset of hull: any point sum c_p z_p (z_p in S) IS a 5-point convex
# combination of S at the derived weights — sum c = 1, all c > 0 (RA1/RB1 pins);
# hence 0 outside conv S (half-plane separation) => 0 outside the mix: SUFFICIENCY.
ok("RB2b Sigma_c(S) subset conv(S): the mix point is itself a convex combination"
   " (sum c = 1, min c = 1/8 > 0)",
   sum(cvals) == 1 and min(cvals) == R(1, 8) and all(c > 0 for c in cvals))
# RB2c NON-NECESSITY witness (exact): S = {1, w, w^2}, w = e^{2 i pi/3}.
# 0 IS in conv(S) (the centroid), S is zero-free, yet NO assignment of the five
# surviving rho's to S gives Z_hat = 0: the derived weight multiset {4,1,1,1,1}/8
# cannot 3-balance (a = b = c forces 3a = 8, no integer solution).
w3 = sp.exp(2*sp.I*sp.pi/3)
ok("RB2c-i 1 + w + w^2 = 0 EXACT; centroid (1+w+w^2)/3 = 0 lies in conv(S);"
   " |w^k| = 1 (S zero-free)",
   sp.expand_complex(1 + w3 + w3**2) == 0
   and sp.expand_complex((1 + w3 + w3**2)/3) == 0
   and all(sp.simplify(sp.Abs(w3**k) - 1) == 0 for k in range(3)))
# basis identity: a + b w + c w^2 = (a - c) + (b - c) w  (w^2 = -1 - w), and
# for rational a, b, c: zero iff a = b = c (1, w rationally independent).
a_, b_, c_ = sp.symbols('a b c', rational=True)
ok("RB2c-ii a + b w + c w^2 = (a-c) + (b-c) w EXACT (w^2 = -1 - w)",
   sp.simplify(sp.expand_complex(a_ + b_*w3 + c_*w3**2
                                 - ((a_ - c_) + (b_ - c_)*w3))) == 0)
weights8 = [4, 1, 1, 1, 1]                       # 8 c_p, p = ((0,0), quartet)
hits = []
for assign in itertools.product(range(3), repeat=5):
    tot = [0, 0, 0]
    for wgt, k in zip(weights8, assign):
        tot[k] += wgt
    a8, b8, c8 = tot
    if a8 - c8 == 0 and b8 - c8 == 0:            # Z_hat = 0 iff a = b = c
        hits.append(assign)
ok("RB2c-iii exhaustive 3^5 = 243 exact assignments: Z_hat != 0 in EVERY case"
   " (no 3-balance of {4,1,1,1,1}; 8/3 is not an integer)",
   len(list(itertools.product(range(3), repeat=5))) == 243 and hits == []
   and R(8, 3).q != 1)

# ===== RB3 — the fixed-n geometry and exactly where n-uniformity dies =====
z_ = sp.Symbol('z')
u_ = sp.Symbol('u')
d_ = sp.Symbol('d', positive=True)
# |z - 1| <= d < 1 => Re z >= 1 - d > 0: the disc buys the half-plane at theta = 0.
ok("RB3 Re z = 1 + Re(z-1) and (Re u)^2 <= |u|^2 EXACT: |z-1| <= d < 1 forces"
   " Re z >= 1 - d > 0 (fixed-n localization EXISTS; its d = x_n e^{x_n} is"
   " n-DEPENDENT of record)",
   sp.expand(sp.re(1 + u_.as_real_imag()[0] + sp.I*u_.as_real_imag()[1])
             - (1 + sp.re(u_))) == 0
   and sp.expand((xr**2 + yi**2) - xr**2 - yi**2) == 0)

# ===== RB4 — H-R-ONLY INSUFFICIENCY (NEW, exact): rho_res carries no carrier axis =====
# Family (t > 0): A_s := s t P, Delta := t P, P = diag(1, 0) (rank-one projector).
# (i) (1 + A_s)^{-1} = I - (st/(1+st)) P EXACTLY; its eigenvalues are {1/(1+st), 1}:
#     sup_s ||(1+A_s)^{-1}||_op = 1 for all t — rho_res IDENTICALLY 1 on the family.
# (ii) the Carleman segment integrand tr[(1+A_s)^{-1} A_s Delta] = s t^2/(1+st);
#     seg(t) = int_0^1 = t - log(1+t) -> infinity as t -> infinity.
# CONSEQUENCE: no bound of the form seg <= f(rho_res) exists — H-R (even granted)
# cannot close the segment; the per-pair HS factors are load-bearing.
s_, t_ = sp.symbols('s t', positive=True)
P = sp.Matrix([[1, 0], [0, 0]])
inv = (sp.eye(2) + s_*t_*P).inv()
ok("RB4a (1 + stP)^{-1} = I - (st/(1+st))P EXACT; eigenvalues {1/(1+st), 1};"
   " 1 - 1/(1+st) = st/(1+st) >= 0 exactly: sup_s ||.||_op = 1 (rho_res = 1"
   " identically on the family)",
   sp.simplify(inv - (sp.eye(2) - (s_*t_/(1 + s_*t_))*P)) == sp.zeros(2, 2)
   and sorted(inv.eigenvals().keys(), key=sp.default_sort_key)
       == sorted([1/(1 + s_*t_), sp.Integer(1)], key=sp.default_sort_key)
   and sp.simplify(1 - 1/(1 + s_*t_) - s_*t_/(1 + s_*t_)) == 0
   and (s_*t_/(1 + s_*t_)).is_nonnegative)
integrand = sp.simplify((inv*(s_*t_*P)*(t_*P)).trace())
seg = sp.integrate(integrand, (s_, 0, 1))
ok("RB4b segment integrand = s t^2/(1+st) EXACT; seg(t) = t - log(1+t);"
   " limit t -> oo is +oo: the segment is NOT a function of rho_res",
   sp.simplify(integrand - s_*t_**2/(1 + s_*t_)) == 0
   and sp.simplify(seg - (t_ - sp.log(1 + t_))) == 0
   and sp.limit(seg, t_, sp.oo) == sp.oo)

# ===== RB5 — sharpness/equality of the half-plane bound =====
# |Z| = delta under Re(e^{i th} rho_p) >= delta forces every Re(e^{i th} rho_p) = delta:
# sum c_p q_p = 0 with q_p >= 0 and every c_p >= 1/8 > 0 forces q_p = 0 coefficientwise.
qs = sp.symbols('q0:5', nonnegative=True)
expr = sum(c*q for c, q in zip(cvals, qs))
ok("RB5 sum c_p q_p has every coefficient >= 1/8 > 0 (q_p >= 0): the sum is 0 only"
   " if every q_p = 0 — the separation bound is tight exactly on the boundary line",
   all(co >= R(1, 8) for co in sp.Poly(expr, *qs).coeffs()))

print("RB-BATTERY-DONE")
```

Output, verbatim (10/10 PASS):

```text
RB1 Re(e^{i th} Z) = sum c_p Re(e^{i th} rho_p) EXACT (sum c = 1, all c > 0); |z|^2 - (Re z)^2 = (Im z)^2 >= 0: separation delivers |Z| >= delta: PASS
RB2a of record: rho_pm = +-i EXACT; (1/2)(+i) + 4(1/8)(-i) = 0 EXACT (zero-free star about 1 does NOT separate): PASS
RB2b Sigma_c(S) subset conv(S): the mix point is itself a convex combination (sum c = 1, min c = 1/8 > 0): PASS
RB2c-i 1 + w + w^2 = 0 EXACT; centroid (1+w+w^2)/3 = 0 lies in conv(S); |w^k| = 1 (S zero-free): PASS
RB2c-ii a + b w + c w^2 = (a-c) + (b-c) w EXACT (w^2 = -1 - w): PASS
RB2c-iii exhaustive 3^5 = 243 exact assignments: Z_hat != 0 in EVERY case (no 3-balance of {4,1,1,1,1}; 8/3 is not an integer): PASS
RB3 Re z = 1 + Re(z-1) and (Re u)^2 <= |u|^2 EXACT: |z-1| <= d < 1 forces Re z >= 1 - d > 0 (fixed-n localization EXISTS; its d = x_n e^{x_n} is n-DEPENDENT of record): PASS
RB4a (1 + stP)^{-1} = I - (st/(1+st))P EXACT; eigenvalues {1/(1+st), 1}; 1 - 1/(1+st) = st/(1+st) >= 0 exactly: sup_s ||.||_op = 1 (rho_res = 1 identically on the family): PASS
RB4b segment integrand = s t^2/(1+st) EXACT; seg(t) = t - log(1+t); limit t -> oo is +oo: the segment is NOT a function of rho_res: PASS
RB5 sum c_p q_p has every coefficient >= 1/8 > 0 (q_p >= 0): the sum is 0 only if every q_p = 0 — the separation bound is tight exactly on the boundary line: PASS
RB-BATTERY-DONE
```

---

## 4. NET — WHAT THE GATE LIST BECOMES IF THESE RESULTS HOLD (descriptive only)

```text
MEMBERSHIP UNCHANGED: R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D (the ENTRY
addendum's list), H-R/H-B0 carried inside as sealed. Nothing retired,
nothing flipped, nothing entered by this artifact.
INTERIOR, descriptive (all subject to check):
  i-1  R.3'(c) remains THE one live analytic wall of the summed horn
       (e5b0879b n-2, unchanged) — with its residue now LOCALIZED: the
       summed-level second-order identity of record is RA3a/RA3b; the
       open content is exactly (a) the surviving-instance square (D-i's
       transfer + the trace enclosure) and (b) the ten-term
       pairwise-difference e_2 form (leading strata: bare degree-0 for
       the four (0,0)-differences; the single-J5 stay class for the six
       within-quartet differences); plus H-R + C-L2 for the tail. Any
       future discharge attempt can now aim at (a)/(b) rather than at
       "some object-level cancellation".
  i-2  On the SIMILARITY CLASS the cell axis of R.3'(c) REDUCES to the
       unit cell: R^{(L)}(a) = R^{(1)}(La) exactly, weight |C|_4^{1/2}
       at the collapse level (|C|_4^{1/4} floor in general), CONDITIONAL
       on premise P-1 (carrier dilation-closure) — which, unlike the
       first-order r-A2, has NO compression-line discharge and must be
       carried as a named premise.
  i-3  R-L0b: OPEN, unchanged in status; its region condition now carries
       the exact characterization (0 outside Sigma_c(S)) with the
       corrected half-plane condition as its sufficient normal form —
       sufficient-NOT-necessary (the cube-roots/3-does-not-divide-8
       witness); the consumable condition of record is unweakened; the
       per-pair localization residue is unchanged in substance and now
       carries the exact H-R-insufficiency witness (the segment is not a
       function of rho_res).
  i-4  H-R: named hypothesis, undischarged, NEVER defaulted — and now of
       record as INSUFFICIENT ALONE for both the segment remainder and
       the localization (the carrier axis is not its axis).
  i-5  R-L4a/b, R-L0, C-L2, OBL-D, H-B0: untouched, stand as sealed. The
       per-pair record (52f2490b) untouched. The adopted amendment and
       its delivered estimate (alpha' = 1/4, all-pairs, similarity class,
       e5b0879b) untouched at their own quantifier.
NO FLAG FLIPS (GOV-F reserved); no witness retired (both summed witnesses
and SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND stand); no verdict on
FIXED/RUNNING advanced or retarded.
```

---

## 5. FLAG BLOCK

```text
TASKA_STATUS = FAILS-AT(
  the unit-cell carrier axis: the summed-level second-order IDENTITY
  exists and is derived exactly (RA3a/RA3b — the c-weighted e_2/b
  polarization), but no ESTIMATE assembles on the sealed basis — W-1
  bites at the algebra (the kill is first-moment; the second-order form
  consumes m_2 = -1, with the exact projection sum W tr[(A_mu B_lam)^2] =
  tr[(A_1 B_1)^2]), W-2 bites at every majorant (the difference form is
  per-pair-type with leading strata at/below the HS threshold; positivity
  adverse; no sign), W-3 bites at the tail (the segment consumes H-R,
  no-default) — NOT REFUTED (the object stays undecided); residue
  SHARPENED: (i) D-i's surviving-instance square + trace enclosure,
  (ii) the ten-term pairwise-difference e_2 form, (iii) H-R + C-L2 for
  the tail, (iv) premise P-1 for the |C|_4^{1/2} similarity-class
  reduction (no compression-line discharge exists for determinants);
  per-pair route BARRED as adopted;
  witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS. )

TASKB_STATUS = FAILS-AT(
  the n-uniform quantifier, all four routes: per-pair majorants
  carrier-divergent of record (52f2490b; ZF o-1; S1 enclosure
  uncertified); the H-R-conditional closure fails by the exact witness
  rho_res = 1 with seg(t) = t - log(1+t) unbounded (the segment is not a
  function of rho_res — H-R consumed only as the named hypothesis, never
  defaulted); no summed-to-per-pair bridge sealed (a control-carrying one
  is the V-2 void shape); fixed-n regions exist but are n-dependent with
  no limit object to pass to — DELIVERED MEANWHILE: the exact region
  criterion 0 outside Sigma_c(S), half-plane sufficient-not-necessary
  (cube-roots witness, 3 does not divide 8, 243/243 exact), sharpness on
  the boundary line; licensed consequence held to the sealed conditional
  (IF the localization ever closes, R-L0 is not needed for the n = 1 leg
  — NO STRONGER); R-L0b OPEN;
  witness SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND STANDS. )

ADOPTED_ESTIMATE_BEARING = NONE(
  every finding lives at the determinant/second-order scalar level (the
  e_2 identity, the m_2 projection, the covariance of the remainder) or
  at the region level; the adopted clause S2'/R-L2b^Sigma quantifies the
  OPERATOR HS family sup_n ||Delta^Sigma_n(a)||_2, which nothing here
  touches — in particular RA4b concerns the determinant expansion's
  quadratic trace, NOT a Dyson stratum of Delta^{Sigma,all}_n, so V-5 has
  no purchase; no falsifier V-1..V-6 engages; the amendment stands as
  adopted with its estimate delivered of record at alpha' = 1/4
  (all-pairs instance, similarity class, e5b0879b) — unmoved here. )

NET = MEMBERSHIP-UNCHANGED-RESIDUE-SHARPENED(
  the "C exists" gate list stays R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D
  with H-R/H-B0 inside; R.3'(c) remains the one live analytic wall, its
  residue localized to the surviving-instance square plus the ten-term
  difference form (unit cell, carrier axis, collapse level) with the
  |C|_4^{1/2} class reduction conditional on named premise P-1; R-L0b
  open with its region condition exactly characterized; H-R of record as
  insufficient alone on both tasks; no flag flips, no witness retired,
  no FIXED/RUNNING verdict moved, per-pair record and adopted amendment
  untouched. )

SEALS_VERIFIED = 16/16(
  e5b0879b DISCHARGE; cbe6ff4f DISCHARGE-AUDIT; cbfbb74c ENTRY; ce59b480
  S2CAND; c2346000 S2CAND-CHK; a903716c RL4RL0; 685afac8 RL4RL0-CHK;
  2e4e5163 REM; 89641f76 REM-CHK; 26f9314b ZF; aed551e3 ZF-CHK; a7969f0c
  R2; a4f2e1b7 R2-CHK; 46846730 E1; 789338ad PA; 52f2490b RL2B-REFUTED —
  all recomputed from bytes at path this session by full digest; all
  match their sidecars, the e5b0879b §1 table, and the tasked prefixes;
  none unverifiable. )

FORBIDDEN_IMPORTS = none(
  no scale authority (L a FORMAL symbol, never instantiated); no imported
  GR; no faithfulness authority; no physical quantity computed, bounded
  numerically, or evaluated — every constant symbolic or an exact
  rational/surd; the only orderings used are exact (L^{m-2} <= 1 on
  0 < L <= 1 via log L <= 0; st/(1+st) >= 0; coefficient positivity;
  3 does not divide 8); alpha never computed or approached; M(t) sharp;
  equal-time FALSE by C6; no carrier or cellulation datum introduced; no
  register/tracker/plan/road/ledger/lens file read; no number frozen —
  the |C|_4^{1/2} and |C|_4^{1/4} weights are DERIVED exponents of the
  bookkeeping variable, CLAIMED pending check. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv r3cvenv (under
  /private/tmp scratch), two batteries: Task A 14 checks, Task B 10
  checks, 24/24 PASS, scripts and outputs reproduced verbatim in §2.8 and
  §3.8; two harness-form corrections disclosed in §3.8 (expand_complex;
  Max replaced by exact ordering), no mathematical claim changed by
  either; shasum/file reads otherwise; NOTHING numeric evaluated.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
R3C_SUMMED_REMAINDER_ATTEMPT_RESULT = SEALED.
```
