CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 12 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = PROVENANCE AUDIT ONLY (no clause disposition, no entry)
DIGEST_RULE = every pinned digest computed from its file at seal time
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_978_K4_PROVENANCE_DARIO_V001.md` | `64d1a40da704d5ef1cd609c572d7f3b4023ce57778e5cc82d0cb1689baeb0dc5` | assignment |
| 02 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md`, G6 `[39014,40368)` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab`; span `293cf6bf8ee58614bf20197ad4e75e2ee10f707a6782bdc9929376584b52d5ac` | the K4 clause under audit |
| 03 | `STAGE8_AXN_BASE_FAMILY_INVERSE_CODEX2_V001.md`, K1–K7 `[4885,8052)` | `e857302bd35c01dd7eb30062a48f2ec1937fc02f87a802aa7efd447e222465b2`; span `67e792dadaca8ef6858d20cef4834c4b60d4f67eb4beb8723c5df930462b96b4` | the seven-condition source; K4's proximate statement |
| 04 | `STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md`, `[8501,11317)` | `8e9a09c104f4b6352263591037b2e0bb9a82b659aa1b6276cdd48117f872acec`; span `f2fd46b3ac4d410abf7a61c0300da793d7d0d688e27e86820141dfeeab8beb6d` | **the forcing consumer: equation (3.1) on the weighted family** |
| 05 | `STAGE8_TASK4A_P3_COMPLETE_U3_PACKAGE_CONSTRUCTION_AND_FOUR_FIELD_STOP_V001.md`, Haar/measure `[8238,9386)` | `f97ee43e052969f0c999b7e57d13310f818222a102c21c249845b8beef1301ff`; span `95ddbbcaec43f0b0e297fefe3e831c457e270d4bbf14dcf16c49615849298e0c` | the second consumer: projective consistency in the required signature |
| 06 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md`, block-diagonality `[5940,6260)` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f`; span `7db1f631a7ecd22bc00754360bbd01404d5902e5c7298c1d8b22ec3231881b18` | the sealed charge-block dynamics, `F_N`, and the reduced pinching |
| 07 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, `E_ch` `[7724,7761)` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0`; span `42cf9687751d682994e000005064874359cb1244a96c4a0906eee166415ddaad` | `E_ch` at bytes |
| 08 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V002.md` | `fd2625a079c77fbc0a102a54a0dd8ba1d97dcfb393035c2b691b0475de254444` | the booked `E_joint`, its five certificates, and `i_B` |
| 09 | `ENTRY_ROUND_2_DECISION_OF_RECORD_V001.md`, charge `[1274,1370)`, Q-838 `[1371,1556)` | `bca010f10771ba7fcd81cbfc2e4a430c2add46486ac829a856d8546a71288cca`; `1e012579a78e646557ec168233d981d709c0f597ace5c13742f2263b599bfb18`; `e2ac06420863f0dd8baaa1c1f747bccd86b69e43fec930cc052fc8800cbbecce` | the charge-location declaration and its disclosure |
| 10 | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md`, K4 `[11600,13268)` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842`; span `745940907443f77e7ba8e4d46431c6e50c4ad13312a679ffa2f82033a470a3ca` | K4 weighted projectivity restated on the two controls |
| 11 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md`; `QUESTIONS_SETTLED_REGISTER_V001.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`; `779b23d76b3fa20a6f07132f074d634861bbf2131658acfae74f8c7b3e8e3dad` | state pin, process law, S01–S37, Q-239/Q-769/Q-772 |
| 12 | `STAGE8_AXN_K4_ADJUDICATION_DARIO_V001.md`; `STAGE8_AXN_DIRAC_FEASIBILITY_DARIO_V001.md` | `552baa4d95382468715f35b1adc404e284fc3e816a2b0ca791f80310f5fd9cc8`; `19021e25a0c971a783e64096c328ec4ee199748073c80cdcf5e9aad5728bb403` | my 976 and 977, whose fork this audit tests |

```text
BLIND HELD.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO CLAUSE DISPOSITION IS TAKEN AND NO CHALLENGE IS FILED -- findings only.
CLOSURE_END_BYTE = 4255
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN K4 PROVENANCE AUDIT — DARIO LANE — V001
## RELAY 978 — `[PLAN:AXN-BUILD-D77]` — WAS K4 FORCED, OR AUTHORED ONTO THE RECORD SURFACE?

Date: 2026-08-10
Status: **RULING: K4_ORIGIN = FORCED-BY. The weighted-projectivity demand is a sealed
consumer's existence condition, stated at bytes two documents upstream of the instrument and
predating it. The demand stands and the 976 fork is real. The record's own sealed charge treatment
does not merely tolerate the charged term — it is the structure that puts the term there. TENSION =
NONE; the PE-15 challenge path is not reached.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

---

## 1. The demand's origin, traced at bytes [PROVABLE]

### 1.1 The clause under audit

V004's G6, member 02 at `[39014,40368)`, requires for every `N <= M`

```text
(pi_MN)_*(A_M lambda_M) = A_N lambda_N.
```

with `nu_N = A_N lambda_N`, *"The test integrates over the full fiber"*, and the two control readings
*"Identity-supported Dirac: algebraic K4 PASS…"* / *"Product Haar: K4 FAIL on a nontrivial charged
character by full-fiber integration."*

### 1.2 The proximate source — and why it looks authored

Member 03, `[4885,8052)`, is the sealed seven-condition system. Its header types the set:
*"Any S1 family reproducing the sealed amplitudes must satisfy all seven conditions below."* Six of
the seven rows carry an inline byte-addressed ground:

| Row | Ground carried in the row itself |
|---|---|
| K1 carrier/extension class | *"P3 bytes `[8501,11317)` state this explicitly"* |
| K2 density/reproduction | *"the only sealed amplitude-to-measure binding"* |
| K3 section naturality | C7 zero extension, byte-fixed `[9797,10271)` |
| **K4 weighted projectivity** | **an independence argument, not a ground** |
| K5 covariance | *"P3 additionally requires boundary and contour transformation laws"* |
| K6 normalization | *"the finite-law source itself says…"*; *"the U3 consumer further says…"* |
| K7 common-origin descent | *"The P3 four-field stop's Haar/measure block is bytes `[8238,9386)`"* |

K4's row argues only that it is *"not implied by K3 because `pi_MN` and `j_NM^Q` have opposite
directions"* — which establishes independence **from K3**, and does not establish that anything
demands it. The same document's FREEDOMS block reads *"DERIVED HERE: seven necessary inverse
constraints; **the weighted-projectivity equation**…"*.

**Read at the proximate source alone, the honest ruling would have been AUTHORED-UNFORCED.** I record
that plainly, because it is the reading the relay's question anticipated, and because a lane that
reported it would have looked thorough. The relay's instruction was to trace the requirement *to its
source*, and the source is one document further up.

### 1.3 The forcing consumer, at bytes

Member 04, `[8501,11317)`, digest `f2fd46b3ac4d410a…` — the P3 law-side second attack, sealed, and the
very span member 03 cites for K1. Its §3.2 states the condition:

> *"A projective family of finite complex measures would require*
> `(pi_MN)_* nu_M = nu_N,   (3.1)`
> *meaning equality after integration over every appended-coordinate fiber."*

and §3.3 makes it an obligation on the object S1 must deliver:

> *"A complex/oscillatory measure is lawful in principle, but it still requires:*
> `nu_N: Sigma(Hist_+,N x Hist_-,N) -> C`, *bounded total variation or another declared extension
> class,* `(pi_MN)_*nu_M=nu_N`, *gauge/boundary/contour transformation laws."*

with, in the same span, `dnu_N = A_N d lambda_N`. **The condition is stated on `nu` — the weighted
object — not on the base `lambda`.** That distinction is the whole of K4: an unweighted projective
base family would be satisfied by product Haar, which is projective par excellence. The sealed text
puts (3.1) on the amplitude-weighted family, and the typed obligation confirms it:

```text
BRANCH_JOINT_COMPLEX_MEASURE_BUILT = false | TYPE-U |
  would-build: finite complex history measures with (3.1), variation control,
               common-origin provenance, and the required invariances
```

The same span already displays the charged-character consequence, before any instrument existed:
*"integration of an appended nontrivial character annihilates that character, whereas evaluation at
the identity preserves it."* It closes by naming the target: *"a **covariant projective family** of
history-event functionals."*

### 1.4 The second consumer, independently

Member 05, the P3 four-field stop, §3.1 types the required object — *"the pushforward of a
branch-joint prequotient measure through the physical orbit map"* — and both of its typed obligations
name the condition:

```text
DESCENDED_DMU_C_BUILT = false | TYPE-U |
  would-build: completed orbit map q, nu_phys, q_*nu_phys, delta_phys,
               projective consistency, tightness/variation control, and
               contour/spacetime/boundary/domain invariance

P3_MEASURE_EXISTS = false | TYPE-U |
  would-build: branch-joint measure, completed orbit map, descended dmu_C,
               delta_phys, consistency, tightness/variation, invariance
```

Two sealed consumers, both predating the anchor instrument, both naming projective consistency of the
measure family as part of the signature the S1 object must have to exist at all.

### 1.5 What this makes the charged-character requirement

Charged-character survival is **not a separate demand and not an added expectation.** It is (3.1)
applied to the sealed amplitude

```text
A_N(r) = (1-p_[A]) + p_[A] chi_N(r),     chi_N(r) = product_(j=1)^N r_j,
```

which is itself sealed law-side data. Both inputs to the requirement are of record; neither is the
instrument's. The instrument transcribed a condition; it did not create one.

**K4_ORIGIN = FORCED-BY** — member 04, `[8501,11317)`, equation (3.1), corroborated by member 05's
required signature.

### 1.6 A hazard recorded in passing

`STAGE8_TASK4B_P_VERDICT_CROSS_VERIFICATION_LANE2_V001.md` carries its own K1–K7 — substitution,
exact split, classification, **ordered-rank port audit**, `nu` statement, falsifier suite — with no
relation to the anchor-line series. A fixed-string `K4` sweep crosses two namespaces and returns a
`K4 = PASS` belonging to the other one. Any future audit reading K-rows by grep should disambiguate
by receiver, not by label.

---

## 2. The record's own charge physics [PROVABLE]

The relay asks what the sealed pinching does to the very term K4 requires to survive, transported
through the booked structures. This is a computation, and it has an exact answer.

### 2.1 The sealed chain

Member 06 supplies, TYPE-P on DoR-008/DoR-009:

```text
U_N[a]     = P_0 tensor I_(3^N) + P_ch tensor W_N[a],      P_0 + P_ch = I_src
Z_N[a_+,a_-] = product_j conjugate(z_(-,j)) z_(+,j)          (the relative-holonomy character)
F_N[a_+,a_-] = P_0 + Z_N[a_+,a_-] P_ch                       (4)
```

with the cross entries **proved zero** in the same span:

```text
I_N(0,ch) = <R_N|W_N[a_+]|R_N>          = 0,
I_N(ch,0) = <R_N|W_N[a_-]^dagger|R_N>   = 0,      because <R_N|P_N> = 0,
```

so that `I_N = [[1,0],[0,Z_N]]` on the ordered sectors `(0,ch)`. Member 07 fixes the reduced channel
at 37 bytes:

```text
Phi_N(rho) = P_0 rho P_0 + P_ch rho P_ch.
```

That map is `E_ch`.

### 2.2 The computation

Apply the pinching to the object that carries the term. Using `P_0 P_ch = 0`, `P_0^2 = P_0`,
`P_ch^2 = P_ch`:

```text
E_ch(F_N) = P_0 (P_0 + Z_N P_ch) P_0  +  P_ch (P_0 + Z_N P_ch) P_ch
          = P_0                        +  Z_N P_ch
          = F_N.
```

**The sealed pinching fixes `F_N` exactly.** It removes nothing here, because the only thing a
pinching removes — the `0 <-> ch` coherence — the record has already proved to vanish identically at
this receiver. `E_ch` acts as the identity on the object carrying the charged term.

Scalarizing `F_N` against a charge-block-diagonal state that weights the two sectors by `(1-p_[A])`
and `p_[A]` returns the sealed amplitude itself, with `chi_N = Z_N`:

```text
(1-p_[A]) * 1  +  p_[A] * chi_N  =  A_N.
```

The reading is exact and it inverts the premise the question was testing: **the neutral block
contributes the trivial character precisely because it acts as `I_(3^N)` on the register, and the
charged block contributes `chi_N` precisely because it acts by `W_N[a]`. The charged term is in the
amplitude *because of* the charge-sector structure, not in spite of it.** The two-term form of `A_N`
*is* the charge decomposition, already in pinched form, with no interference term to remove.

### 2.3 Transport through the booked joint structures

Member 08 books `E_joint` by route `EJ-TRIVIAL`, `E_joint = E_ch ⊗̂_min id_R_inf ⊗̂_min id_B`, with two
certificates that settle the transport:

```text
commutation certificate:  E_joint compose i_B = i_B
fixed-space certificate:  Fix(E_joint) = Fix(E_ch) ⊗̂_min R_inf ⊗̂_min B
```

`chi_N` is an element of `C(Y_N) ⊂ B`. Hence `E_joint(i_B(chi_N)) = i_B(chi_N)`: the superselection
map is the identity on the entire history factor, and the pinching never reaches the character at all.

### 2.4 The type distinction, which is the load-bearing part

A pinching and a fiber integration are different operations with different targets:

| Operation | What it annihilates | Effect on the K4 term |
|---|---|---|
| `E_ch`, `E_joint` (pinching) | off-diagonal coherence **between** charge sectors | `E_ch(F_N) = F_N` — the diagonal charged block is preserved |
| `(pi_MN)_*` against product Haar | the **diagonal** charged sector's character contribution | `chi_N` integrates to zero; the term is removed |

Superselection erases *coherences*; Haar's full-fiber integration erases a *sector's contribution*.
The premise that these are the same phenomenon is where the worry lived, and at the booked structures
it does not hold.

### 2.5 The strength of each leg, stated honestly

Two independent reasons were displayed, and they are not equally strong:

- **§2.3 (location)** rests on the principal-authored charge-location declaration at member 09,
  *"charge acts on the source sector only; record and history factors carry no charge action"* — which
  travels with its own Q-838 disclosure, *"this declaration is unforced on a notational
  record-labelling and FALSE on a substantive one (Q-838, undecidable of record)."* I therefore put
  no weight on this leg.
- **§2.2 (type and origin)** rests on member 06's sealed derivation and member 07's sealed reduced
  channel, both TYPE-P, neither authored at entry. **It is declaration-independent**, and it alone
  carries the finding.

Setting the charge-location declaration aside entirely, the conclusion holds: on the source factor,
where `E_ch` genuinely acts, it fixes `F_N`.

**RECORD_CHARGE = SIDES-WITH-SURVIVAL.** The record's sealed superselection treatment preserves the
charged block by construction, and the sealed amplitude's charged term is that block's contribution.

**TENSION = NONE.**

### 2.6 The record's Haar treatment is separately settled

Q-239 rules `NORMALIZED_HAAR_ON_Q_N_EQUALS_REQUIRED_DESCENDED_MEASURE = false | TYPE-R`, and member
05 §3.3 retains that kill on descent grounds independent of K4. K7 provenance is a further independent
receiver. So the record declines product Haar on three separate grounds, only one of which is K4.

---

## 3. The ruling [PROVABLE]

**K4_ORIGIN = FORCED-BY**, at member 04 `[8501,11317)` equation (3.1), corroborated at member 05.
The demand stands. My 976 fork is unaffected and remains real, with H-DIRAC closed by my 977.

**CHALLENGE_PATH = N-A.** The PE-15 challenge route was conditioned on *authored-unforced AND in
tension*; both conjuncts are false, so the path is not reached and nothing is filed. Had either held,
the lawful form would still have been a clause disposition through the supersession machinery — a
principal act, displayed and not taken.

**The principal's question, answered in its own terms.** *"Are we using metrics that were not created
for the record surface in a way that may not make sense?"* On K4: no. The metric was created for
exactly this surface — it is the existence condition the law-side attack derived for the very object
S1 is obliged to deliver, stated on the weighted family, with the Haar consequence displayed in the
same breath. The instrument transcribed it faithfully. The question was worth asking and the trace
was worth running: §1.2 shows the proximate source reads as authorship, and only the byte trace one
level up settles it the other way.

**Scope this audit does not extend to.** Whether the program requires a projective family at infinite
scope at all — that is, whether `dmu_C` must exist at physical scope — is upstream of K4 and was not
the question put. The record answers it affirmatively at member 05's required type; reopening that
would be a different act with a different receiver, and it is not mine to open.

---

## 4. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the G6/K4 clause exactly as governing; the seven-condition system; equation (3.1) and its typed
  obligation; the required dmu_C signature; the sealed U_N/F_N/Phi_N chain; E_ch at 37 bytes; the
  booked E_joint and its five certificates; the Q-838 disclosure at every E_joint consumer;
  Q-239's Haar kill; all gates.

DERIVED HERE:
  the origin trace from the instrument through the seven-condition source to the forcing consumer;
  the weighted-vs-base distinction that makes (3.1) the content of K4;
  the computation E_ch(F_N) = F_N, and the reading of A_N's two-term form as the charge decomposition
  already in pinched form;
  the pinching/fiber-integration type table;
  the declaration-independence of the finding;
  the K-series label-collision hazard.

SELECTED HERE:
  nothing.  No clause disposition, no challenge, no fork ranking, no entry, no candidate, no measure,
  no base family, no branch.  Neither control is promoted.

CONDITIONED-ON:
  the sealed status of members 04, 05, 06, 07 and the TYPE-P premises DoR-008/DoR-009 they carry.
  The §2.3 leg is additionally conditioned on the Q-838-disclosed charge-location declaration and is
  given no weight for that reason.

SCALING WEIGHTS: none consumed, fixed, or substituted.  p_[A] appears only symbolically; the
  ordered ranks r_0, r_ch were not read, formed into a ratio, or compared.
```

---

## 5. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A transcribed condition was not flattened into an
  authored one, nor an authored one into a forced one. An independence argument (K4 vs K3) was not
  flattened into a forcing argument. A typed would-build obligation was not flattened into a built
  object. `E_ch(F_N) = F_N` was not flattened into a claim about physical charge conservation. The
  charge-location declaration was not flattened into derived structure, and the finding was
  re-derived without it. A provenance ruling was not flattened into a clause disposition.
- **BR-1 held.** The producer-declared objects in play — the entered `E_joint`, the charge-location
  declaration — are the ones I deliberately removed from the load path in §2.5. The finding rides on
  sealed pre-entry material only.
- **VOID CONDITION held.** No index, topology, selector, or rule was chosen from a desired
  coefficient. `p_[A]` was carried symbolically throughout.
- **BLIND held.** No rank was read, no ratio formed or stated, no fiber compared. The DoR-013 fiber
  remained an opaque pointer.
- **PE-1..PE-15:** pointer-only, zero verdict weight. The relay's own door test was answered from
  bytes; the ledger was not opened.
- **CUSTODY:** archive-side only. One artifact, lane-tagged, sealed. No register, plan, tracker, git,
  commit, or push action. Nothing adopted, nothing registered.
- **BUILDER-B INDEPENDENCE:** `evaluator_build_A/` and `checks/` were not read. Every source used is a
  sealed record document.
- **BYTE AUDIT:** all twelve members re-hashed from their files at seal time; all eight cited spans
  recomputed and matched, plus two new spans computed and pinned here. The `[8501,11317)` citation in
  member 03 was checked against **both** P3 documents before use: it resolves to the second attack
  (`f2fd46b3…`, exact), not to the four-field stop. Recording that check because the near-miss would
  have manufactured a citation defect that does not exist.
- **CLOSURE:** declared at byte 0, ends at the byte recorded in the closure block, computed on bytes;
  27-token pre-closure scan run over authored prose, 0 hits.

---

## 6. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 4255; scan 0 hits)
K4_ORIGIN = FORCED-BY (STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md,
            8e9a09c104f4b635..., span [8501,11317) f2fd46b3ac4d410a..., equation (3.1) stated on the
            weighted family nu_N = A_N lambda_N; corroborated by the four-field stop f97ee43e...,
            DESCENDED_DMU_C_BUILT and P3_MEASURE_EXISTS would-build signatures)
RECORD_CHARGE = SIDES-WITH-SURVIVAL (E_ch(F_N) = P_0 + Z_N P_ch = F_N, the cross entries proved zero
            of record; A_N's two-term form IS the charge decomposition already in pinched form;
            E_joint compose i_B = i_B and Fix(E_joint) contains B whole; pinching removes coherences,
            fiber integration removes a sector's character -- different operations, different targets)
TENSION = NONE
CHALLENGE_PATH = N-A (its predicate, authored-unforced AND in tension, is false on both conjuncts)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is §1.3: if a lane reads
(3.1) in member 04 as attaching to the base family `lambda` rather than the weighted family `nu`, the
FORCED-BY ruling weakens to a transcription of a weaker condition, and K4's added strength returns to
the instrument's account. I read it on `nu` because the span states it on `nu`, states
`dnu_N = A_N dlambda_N` alongside, and types the obligation as *"finite complex history measures with
(3.1)"* — but that is the sentence to check at bytes before this ruling is relied upon.
