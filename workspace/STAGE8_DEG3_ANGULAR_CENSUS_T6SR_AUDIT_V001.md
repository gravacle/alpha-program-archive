# STAGE 8 — ADVERSARIAL AUDIT OF THE DEGREE-3 ANGULAR CENSUS (STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md, 11dd8ac5) — T6SR AUDIT V001

## ADVERSARIAL AUDITOR — CODENAME CENSUS-AUDIT — COMMISSION T6SR — [SEALED]

Date: 2026-08-15 (session CDT early). Target:
`STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md` (11dd8ac5cc519061..., FULL
digest recomputed at path, sidecar MATCH via `shasum -a 256 -c`, verified
BEFORE the file was opened). Posture: DEFAULT-REFUTE; testimony carries
ZERO weight — the same sealed ground consumed independently (the T4SR
pair; the WALL_SUMMED_REQUANT pair; the WALL_BOUNDARY_CLOSURE pair — all
seals verified by me before consumption), every displayed parity and
first moment re-computed exactly in a fresh venv, every absence claim
re-swept by my own token greps, the enumeration re-derived from the
summed assembly's own bookkeeping.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until registered.
DETERMINATION ONLY — no adoption, no authored physics, no member binding.

Fences held: EXACT SYMBOLIC ONLY — the target's 5-check CAS battery
extracted VERBATIM from its §8 and re-run in a fresh venv
(`t6srauditvenv`, sympy 1.14.0, session scratchpad): 5/5 PASS, output
BYTE-IDENTICAL to the recorded block (diff empty); plus my own 7-check
adversarial battery (verbatim §7 below, output verbatim; 7/7 PASS on a
single run; NO pre-final corrections). Every constant symbolic or an
exact rational/surd; NOTHING numeric evaluated (Lambda FORMAL; V_2,
beta_s, all coefficients generic symbols); no floats as ground; no
measured constant; no measured-constant comparison; no value frozen.
Scoped reads: the target plus the SIX named ground artifacts and NO
others; no file matching register|road_|ledger|lens|plan|tracker|
THE_HANDOFF opened (fenced classes and all other files matched by
FILENAME ONLY in sweeps, never opened). "Q-..." tokens
EXPECTED-UNLOCATABLE per the tasking; noted, never chased. No git; no
existing file edited; ONE output (this artifact) plus its seal sidecar
at the commission-distinct path, probed ABSENT at session start AND
re-probed ABSENT immediately before write. SWEEP CUTOFF DECLARED (§6).
Homonym discipline inherited: n = carrier index; k = momentum inside |k|
and khat; (o, c) = the REM strata indices as quoted at bytes.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — the T6SR verdict CENSUS-INCOMPLETE is CORRECT and stands adjudicated at the bytes: the degree-3 class ENUMERATION is genuinely delivered whole at the ground pair's own granularity, every displayed parity and first moment is exact at my independent re-computation, the COR-A moment sum is displayed faithfully and ONLY as the unevaluated total, and the commissioned per-class displays are genuinely uncompletable on the six named seals. PER CLAIM: (1) ENUMERATION COMPLETENESS — CONFIRMED at my own re-derivation from the summed assembly's own bookkeeping (fresh venv, CA2): the ordered integer splits of assembled degree -3 with both post-double-m_0-kill factors at degree <= -1 are EXACTLY (-1,-2) and (-2,-1) — verified over a WIDER window than the build displayed ([-10,-1]; the forcing dU = -3 - dF >= -2 displayed) — and the e-2 pin (the degree -1 slot is the V_2-form subsidiary and NOTHING else) follows from the sealed unqualified ONLY-stratum text (WSR t-1 at bytes, re-read by me) by exact set arithmetic: a second degree -1 factor entry would assemble MORE THAN ONE degree -2 stratum, contradicting the sealed cardinality-one; the slot list {A-U, A-F; the B c-routes in both placements} is COMPLETE at the ground pair's granularity (the c = 0 / c-bearing partition of the degree -2 factor slot is exhaustive by definition; both quoted floor routes re-located at bytes; "there is NO (c = 1, o = 0) class anywhere" re-located; the finer per-factor (o, c) table correctly marked undisplayed) — NO MISSING CLASS, and no ALL-EVEN verdict was claimed that a missing class could void; (2) EVERY DISPLAYED PARITY AND FIRST MOMENT — CONFIRMED: the target's battery re-runs BYTE-IDENTICAL (5/5), and my independent battery re-derives each display on fresh code paths (CA1 the A-U trace slot tr[alpha_x C(k)] = -2 khat_x with moments 0 and 4pi/3; CA5 the c-class slots tr[jet] = 4 khat_x(beta_+ - beta_-) and tr[alpha_x jet] = 4 khat_x^2(beta_+ + beta_-), BOTH joint-swap-EVEN — no parity kill in either slot — with constant-coefficient integrals 0 and (16pi/3)(beta_+ + beta_-) and the 8pi/3 defeat exhibit; CA4 the C3 odd-part reduction STRENGTHENED from the build's schema instances to the COMPLETE generic forms: all ten degree-3 odd monomials x all seven even quadratic monomials + degree-4 spots, the even part dies with no monomial escape); the class-A parities are correctly displayed as UNDISPLAYED (AC-4 re-located verbatim at WSR-AUDIT :558-561; B-1 re-confirmed by MY OWN token sweep of the six); the class-B moment blockage is real (NO first-angular-moment certificate for beta_s in the six — my sweep concurs; the moments turn exactly on the undisplayed odd content, CA5's decomposed functional (16pi/3)c_1); (3) THE SUM PER COR-A — CONFIRMED (CA6): L_deg3 = mu_AU + mu_AF + SUM mu_B aggregates EXACTLY the enumerated slots (one term per class, none missing, none extra), is displayed ONLY as the unevaluated total (no closed form smuggled — span-checked), and honors COR-A's non-IFF (my exhibit: the accidental cross-stratum zero mu_AU = -mu_AF, mu_B = 0 gives L = 0 — not excluded, exactly as the audited correction requires); (4) THE CERTIFICATION CHAIN — CONFIRMED: every lemma application is inside the audited all-lengths scope (the S-conjugation identity re-verified INDEPENDENTLY at generic length-4 words plus an exhaustive 127-word single-momentum sweep one full length past the T4SR build, CA3; the alphabet closure rides the record's own type ledger as of record), and the branch-table degree count lands step-by-step (CA7): the degree -2 stratum killed of record (first moment zero) -> the count at -3 (log Lambda, divergent formal) -> L = 0 kills the -3 total -> count at <= -4 = carrier integral 1 - 1/Lambda -> 1 BOUNDED; L != 0 -> the log GENUINE; NEITHER branch claimed decided — correct on the absences; (5) SCOPE — CONFIRMED span by span: summed quantifier only; V-2 not crossed in either direction; no flag of the record flipped; the jet consumed at the T4SR pair's verbatim byte-quotes with the MO4 pair unopened (disclosed, C-5) — the same second-hand-at-verified-seal grade the T4SR audit assigned; no witness action; (6) FENCE-SCAN — CLEAN: no float ground (regex hits are version/section numbers only), no numeric-eval tokens, no fenced-class content, gates present at header and flag block, one output + sidecar, cutoff (03:33:52) before seal (03:38) in order, sw-1/sw-3 independently verified (filename and mtime re-sweeps). CORRECTIONS, all display-level and VERDICT-NEUTRAL: COR-1 — the sweep line for "V_3" is imprecise at bytes: §0's "'V_3': zero hits" and sw-2's "zero hits outside the T4SR audit's own absence claims" miss that the token occurs TWICE in the six, once in the T4SR BUILD (:182, a-6(i)) and once in the T4SR AUDIT (:230, r-12), BOTH inside absence claims; the substantive claim (no V_3 angular-symbol DISPLAY anywhere in the six; B-1 stands) is TRUE at my own sweep — no flag moves; N-1 — §0/§5's "the only 'o = 3' line ... the only 'segment-corrected' line" compress the token families to their content-bearing members (the T4SR pair's absence-discussion hits exist; the target's own §7 sw-2 displays those two families exactly and is controlling), and "a strictly larger six" for the T4SR audit's basis is loose set arithmetic (that basis adds the MO4 pair — the relevant direction for the absences — but is not a superset of the target's six); N-2 — C3's "generic odd T (degrees 1 and 3)" runs schema instances (the degree-3 form is one generic-coefficient monomial), typed as such in the target's own T-2; my CA4 closes the gap at the complete generic forms in the build's favor. THE HEADLINE, adjudicated at the bytes: CENSUS-INCOMPLETE, with the enumeration skeleton delivered, the moment functionals displayed exact and unevaluated, both COR-A branches armed and neither decidable on the six, and the completing object (the DEGREE-3 SYMBOL-AND-MOMENT DISPLAY PAIR, over already-sealed stock outside the named six) correctly named — CONFIRMED. NOT REFUTED discipline held on both sides: no divergence of tr K^Sigma_n exhibited anywhere. CAS: target battery 5/5 BYTE-IDENTICAL re-run; audit battery 7/7 PASS single run.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest via
`shasum -a 256 -c` against its `.seal.sha256` sidecar; the target
verified BEFORE opening. 7/7 MATCH (OK); NONE UNVERIFIABLE. Scoped
reads: these seven files and NO others.

```text
11dd8ac5cc519061  STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md          OK  [THE TARGET]
172e8bbff1c9c17a  STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md            OK  [T4SR — the lemma; B-1/B-2; the census commission §4; read IN FULL]
8241cb31accb96ba  STAGE8_WALL_LOGFREE_RATE_T4SR_AUDIT_V001.md      OK  [T4SR-AUDIT — R2 all-lengths; COR-A; r-12/r-13; read IN FULL]
6e5762a8b785f4b7  STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md          OK  [WSR — the bookkeeping ground; read IN FULL]
549f14238ec42496  STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md    OK  [WSR-AUDIT — :152; A3 :500; AC-4 :558-561; read at tasked spans]
80db260fa1561d76  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md        OK  [WBC — CS-5 J = -(Q b_D Q) tensor alpha_x at bytes; §0/§2/§3]
46938251aeaa9af3  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md  OK  [WBC-AUDIT — CONFIRMED-WITH-CORRECTIONS; COR index at bytes]
```

Load-bearing byte-pins chased independently of the target's citations:
WSR §5.1 t-1 (the degree -2 start; "the ONLY degree -2 assembled
stratum" UNQUALIFIED at bytes; the two floor routes verbatim: "c >= 2
per factor (that factor degree <= -2, the OTHER factor degree <= -1
post-kill: assembled <= -3)"; "floor 2o + c >= 3 per factor"; "There is
NO (c = 1, o = 0) class anywhere"), t-2 (U-side -i V_2(x)/(2|k|)
angular-SCALAR; F-side +i V_2-form/(2|k|) x (-khat_x C(k))), t-3, :393
("o >= 2 segment-corrected strata" — the naming line, verified at line
number), :58/:885 ("first angular moment" — the degree -2 kill spans,
verified at line numbers); WSR-AUDIT :152 ("degree -3 strata exist
(o = 3, c = 0; c-bearing routes)" — verified at line number), :500 (A3),
:558-561 (AC-4 verbatim: "no stronger claim (e.g., stratum-by-stratum
parity at degree -3) is made by me or by the build"); T4SR §3.1
a-1..a-6 (one J vertex at bytes; the alphabet closure at the record's
type ledger; B-1/B-2 grounds), §4 (the census commission; the deciding
object), §7 T1-T8; T4SR-AUDIT §3.1 r-1..r-4 + R2 (the all-lengths
upgrade: joint inversion = S-conjugation), §3.5 r-12/r-13 (the absence
sweeps on the audit's seven-file basis incl. the MO4 pair), §4 COR-A
WHOLE (the branch condition = the SUM of the displayed first moments;
the accidental cross-stratum zero not excluded; the census decides both
ways) + N-a/N-b, §10 flag restatement; the jet display consumed at the
T4SR pair's verbatim byte-quotes ("k_hat_x [(beta_+ - beta_-) I +
(beta_+ + beta_-) alpha.k_hat]" — T4SR §1/:87-88; T4SR-AUDIT :81-82) —
the MO4 pair itself OUTSIDE the named six and NOT opened, matching the
target's own C-5 grade; WBC §0/§2 CS-1..CS-9 (CS-5: J = -(Q b_D Q)
tensor alpha_x at :152-153 with PA byte-pins), §3.3; WBC-AUDIT §0
(CONFIRMED-WITH-CORRECTIONS) + corrections index (:232-258). "Q-..."
tokens: EXPECTED-UNLOCATABLE; noted, never chased.

---

## 2. METHOD — WHAT ZERO-WEIGHT TESTIMONY MEANT HERE

```text
m-1 Every load-bearing quote in the target was re-located at the ground
    bytes by my own reads/greps (§1 pins) — line numbers verified by my
    own sed/grep, not trusted from the target.
m-2 The enumeration (the census's headline structural claim) was
    re-derived from the bookkeeping ground ALONE (WSR t-1 at bytes)
    before comparing with the target's §3: wider split window, the
    forcing inequality displayed, the ONLY-stratum contradiction as
    exact set arithmetic (CA2).
m-3 Every displayed parity and first moment was re-computed on fresh
    code paths (my own Dirac construction alpha_j = gamma^0 gamma^j —
    assembled, not copied; my own angular integrator), THEN the
    target's battery was re-run verbatim and diffed: BYTE-IDENTICAL.
m-4 Every absence claim (B-1/B-2 re-confirmations, sw-2) was re-swept
    by my own token greps over all six ground artifacts; the one
    imprecision found is COR-1 (§4).
m-5 The lemma consumption was tested INSIDE its audited scope by
    independent re-verification (generic-letter S-conjugation at
    length 4; the 127-word exhaustive sweep one length past the T4SR
    build) — the census consumes, and my checks confirm it consumes
    correctly.
m-6 The hunt list worked item by item: missing class (none — CA2);
    smuggled moment value or closed form for L_deg3 (none — span-check:
    every displayed value is either of record (0, 4pi/3, 8pi/3, 16pi/3
    exhibits) or an unevaluated functional); per-stratum-IFF relapse
    against COR-A (none — s-1 states the non-exclusion verbatim);
    quantifier leakage (none); hidden numerics (none); sweep-display
    errors (ONE — COR-1, verdict-neutral).
```

---

## 3. THE CLAIMS, RE-DERIVED

### 3.1 Enumeration completeness (target §3, e-1..e-4): CONFIRMED

```text
c-1 THE SPLIT SKELETON: dU + dF = -3 with dU, dF integers <= -1 (the
    double m_0 kill of record: degree-0 factor content gone; symbol
    expansions descend from 0) has EXACTLY the solutions (-1,-2) and
    (-2,-1): dF <= -1 forces dU = -3 - dF >= -2 — my CA2 enumerates
    the window [-10,-1] (wider than the build's displayed [-3,-1]) and
    concurs: no deeper split exists. The build's displayed window was
    sufficient (a factor below -3 would need a partner above 0,
    nonexistent post-kill); the wider display closes the question.
c-2 THE e-2 PIN: WSR t-1 at bytes states UNQUALIFIED "it is the ONLY
    degree -2 assembled stratum" (V_2 (x) V_2, o = 2, c = 0). The
    target's contradiction argument consumes exactly this: a second
    per-factor degree -1 entry W would assemble W (x) V_2 (and
    V_2 (x) W) — a degree -2 assembled stratum count > 1 (CA2's set
    arithmetic: cardinality 4 vs the sealed 1). Sound at the ground
    pair's bookkeeping granularity (strata = per-factor products,
    degrees additive — the ground's own floor grammar); no stratum
    content authored.
c-3 THE DEGREE -2 SLOT PARTITION: c = 0 content = the segment-corrected
    subsidiary stratum (the ground's own naming, WSR :393; existence
    WSR-AUDIT :152 with total label o = 3) — CLASS A, both placements;
    c-bearing content = the two quoted floor routes (c >= 2 saturated
    at -2; interband 2o + c >= 3 landing at -2 by c-1), both
    placements — CLASS B; "no (c = 1, o = 0) class anywhere" of
    record. The c = 0 / c >= 1 partition is exhaustive by definition
    of c: NO MISSING CLASS at this granularity. The finer per-factor
    (o, c) table is correctly marked undisplayed in the six (e-4;
    C-4) — my sweep concurs no such table exists in scope.
c-4 The commission's voiding trigger (a missing class voids ALL-EVEN)
    does not fire: no ALL-EVEN verdict was claimed; the enumeration
    is complete regardless (c-1..c-3).
```

### 3.2 The displayed parities and moments (target §3.2, C1/C3/C4): CONFIRMED

```text
c-5 A-U SLOT: the F-side V_2-vertex traced factor re-derived on my own
    Dirac construction: tr[alpha_x C(k)] = -2 khat_x EXACT; first
    moment 0; second moment 4pi/3 (CA1). The U-side degree -2
    amplitude s_U^(2): parity correctly displayed as UNDISPLAYED —
    AC-4 verbatim at WSR-AUDIT :558-561 (re-located), and my own token
    sweep finds no angular symbol for any degree -2 subsidiary in the
    six. mu_AU displayed as the exact unevaluated functional with the
    C3 reduction: correct (CA4).
c-6 A-F SLOT: same structure; the spinor word undisplayed but typed
    joint-odd by the consumed lemma (inside scope: the word is over
    the sealed alphabet with one alpha_x — the record's own type
    ledger, r-1/r-2 of record); the angular-scalar U-side V_2
    prefactor legitimately carried outside the angular integral (WSR
    t-2 at bytes: -i V_2(x)/(2|k|), angular-SCALAR). Correct.
c-7 B SLOTS: my independent re-derivation (CA5): tr[jet] = 4 khat_x
    (beta_+ - beta_-); tr[alpha_x jet] = 4 khat_x^2 (beta_+ + beta_-);
    BOTH joint-swap-EVEN — parity supplies NO kill in either traced
    slot (matching R6 of record); constant-coefficient integrals 0 and
    (16pi/3)(beta_+ + beta_-); the 8pi/3 defeat exhibit (the record's
    own f(khat) - f(-khat) convention, T6/R4 of record — reproduced
    faithfully); the decomposed functional mu_B^I = (16pi/3) c_1 at
    (beta_+ - beta_-) = c_0 + c_1 khat_x + c_2 khat_y: the moment is
    nonzero IFF a khat_x-odd component exists — the census's typing
    (the moments turn exactly on the undisplayed beta_s content) is
    exact. The pure-khat-inversion parity correctly marked
    UNDETERMINED (beta_s khat-dependence undisplayed). The assembled
    combination correctly marked undisplayed.
c-8 THE C3 REDUCTION: strengthened at my CA4 to the complete generic
    forms (all ten degree-3 odd monomials; all seven even quadratic
    monomials; degree-4 spots): int T a dOmega = int T a_odd dOmega
    EXACTLY — no monomial escape. The build's schema instances (typed
    as such in its T-2) are correct and now covered generally (N-2).
c-9 B-1/B-2 RE-CONFIRMED ON MY OWN SWEEPS of the six: "o = 3" — T4SR
    pair (absence discussions: 9 + 3 hits) and WSR-AUDIT :152
    (existence) ONLY; "segment-corrected" — WSR :393 (naming, 1 hit)
    and the T4SR pair (7 + 2) ONLY; beta/ray tokens — the T4SR pair
    ONLY (7 + 3: the jet byte-quotes and their discussions); "first
    angular moment" — WSR :58/:885 (the degree -2 kill), the T4SR
    pair's deciding-object/COR-A spans ONLY; the WBC pair: ZERO hits
    on every class (exact). "V_3": TWO hits, both absence claims —
    see COR-1. NO angular-symbol display for any degree -3 stratum
    and NO beta_s moment certificate exist in the six: B-1 and B-2
    REAL, exactly as the target claims (and as audited of record,
    r-12/r-13, on a basis including the MO4 pair).
```

### 3.3 The sum per COR-A (target §4): CONFIRMED

```text
c-10 COR-A re-read WHOLE at bytes (T4SR-AUDIT §4): the branch
    condition is "the class's summed first angular moment vanishes";
    all-even is the sufficient structural case; the census displays
    each stratum's exact first moments and the SUM decides both ways.
    The target's s-1 consumes this VERBATIM, including the
    accidental-zero non-exclusion — no per-stratum-IFF relapse
    anywhere in the target (span-checked).
c-11 AGGREGATION (CA6): L_deg3 = mu_AU + mu_AF + SUM_{c-routes} mu_B —
    one term per enumerated class, none missing, none extra; the
    all-zero substitution gives L = 0; the accidental cross-stratum
    zero (mu_AU = -mu_AF, mu_B = 0) also gives L = 0 and is displayed
    CONSISTENT — the non-IFF honored.
c-12 NO CLOSED FORM SMUGGLED: span-check of the target finds no
    displayed value for L_deg3, for any mu, or for any beta_s moment
    beyond the of-record exhibits at constant/decomposed coefficients
    — evaluation correctly typed BLOCKED on B-1/B-2 (s-2), the
    authoring fence held.
```

### 3.4 The certification chain and branch table (target §4 s-3): CONFIRMED

```text
c-13 LEMMA APPLICATIONS INSIDE AUDITED SCOPE: the consumed instrument
    is the all-lengths trace-parity identity (T4SR-AUDIT R2: joint
    inversion = S-conjugation; W(-) = S W S; tr[alpha_x W(-)] =
    -tr[alpha_x W]). My independent re-verification (CA3): the
    conjugation identity on four generic letters and the generic
    LENGTH-4 product (one past the audit's displayed generic length-3),
    plus the exhaustive 127-word single-momentum sweep to length 6
    (one full length past the T4SR build's 63): odd-or-zero
    throughout. Every census application (the A-U trace slot; the A-F
    typed word; the one-vertex count r-1 at bytes) is inside this
    scope; the alphabet closure rides the record's own type ledger
    exactly as of record (r-2, GROUNDED — not extended here).
c-14 THE DEGREE COUNT, STEP BY STEP (CA7): (i) the degree -2 stratum's
    angular integral is zero of record (first moment of the single
    khat_x, CA1) — the count sits at -3, carrier integral log Lambda
    (divergent, formal); (ii) branch L_deg3 = 0: the ENTIRE degree -3
    angular total vanishes (COR-A: the total IS L), so every surviving
    diagonal contribution has degree <= -4 — carrier integral
    int_1^Lambda k^2 k^-4 dk = 1 - 1/Lambda -> 1 BOUNDED: the log
    eliminated, RATE-CERTIFIED mechanically on the lemma, exactly the
    audited landing (r-5 of record); (iii) branch L != 0: the log
    GENUINE at the class total (I3 = log Lambda). Lambda FORMAL
    throughout; the target decides NEITHER branch — correct, since
    every mu is unevaluable on the six (B-1/B-2).
c-15 THE COMPLETING OBJECT: named over already-sealed stock (the
    REM/BL2STAR/R2 stock named of record at T4SR a-6/§4; the MO4
    stock) OUTSIDE the named six — consistent with the T4SR successor
    note (census-shaped work at the KF1/KF2 license, not new physics);
    the two-part shape makes each half independently auditable. No
    objection.
```

### 3.5 Scope and quantifier honesty (target §6): CONFIRMED

```text
c-16 SUMMED ONLY, span-checked: every claim is OF K^Sigma's diagonal
    strata at the summed quantifier; no per-composite or per-pair
    quantity is quoted, moved, or decided; per-composite content
    appears only as of-record contrast citations (the o-2-OBS
    grammar). V-2 not crossed in either direction. No flag of the
    record flipped; the T4SR verdict is consumed, not moved; no
    witness action.
c-17 THE JET CONSUMPTION GRADE: the MO4 pair is outside the target's
    named six and was not opened by it; the jet display rides the T4SR
    pair's verbatim byte-quotes — I verified those quote spans at the
    T4SR pair's bytes (§1) and the target's C-5 disclosure is
    accurate. Nothing in the target rests on MO4 content beyond the
    quoted bytes.
```

---

## 4. THE CORRECTIONS

```text
COR-1 (sweep display; VERDICT-NEUTRAL; the one finding of substance):
  The target's §0/§5 "'V_3': zero hits" and §7 sw-2's "'V_3' — zero
  hits outside the T4SR audit's own absence claims" are imprecise at
  bytes: the token "V_3" occurs TWICE in the six — T4SR :182 (the
  BUILD's a-6(i): "nothing pins V_3/segment-corrected angular
  character") and T4SR-AUDIT :230 (r-12: "NO V_3, NO degree-3 angular
  symbol display") — BOTH inside absence claims, but one of them
  outside "the T4SR audit's own". CORRECT FORM (registrar margin; no
  re-issue forced): "V_3": two hits in the six, both absence claims
  (T4SR a-6(i); T4SR-AUDIT r-12); no V_3 angular-symbol DISPLAY
  anywhere. WHY VERDICT-NEUTRAL: the substantive absence (B-1) is
  real at my own sweep and audited of record; the census's fences
  scope each enumeration sentence to its displayed sweep, and the
  corrected display strengthens, not weakens, the absence.

N-1 (wording compressions; notes only): (i) §0/§5's "the only 'o = 3'
  line in the six is the WSR-AUDIT existence claim at :152" and "the
  only 'segment-corrected' line is WSR :393's naming" compress the
  token families to their content-bearing members — the T4SR pair's
  absence-discussion hits exist (9 + 3 and 7 + 2 respectively); the
  target's own §7 sw-2 displays those two families EXACTLY ("T4SR
  pair (absence discussions) and WSR-AUDIT :152 (existence) ONLY";
  "WSR :393 (naming) and the T4SR pair ONLY") and is the controlling
  display. (ii) §5's "a strictly larger six that included the MO4
  pair" is loose set arithmetic: the T4SR audit's sweep basis (its
  target + six ground artifacts) adds the MO4 pair — the relevant
  direction for the absences, where beta_s lives natively — but is
  not a superset of the target's six (it lacks the T4SR audit
  itself). The absences hold on BOTH bases; nothing moves.

N-2 (CAS display grade; note only, closed in the build's favor): C3's
  "generic odd T (degrees 1 and 3)" runs schema instances (the
  degree-3 odd form is the single monomial u1 khat_x khat_y khat_z
  with generic coefficient; the even amplitude omits two quadratic
  monomials) — correctly typed as identity-schema stand-ins in the
  target's own T-2. My CA4 re-runs the reduction at the COMPLETE
  generic forms (all ten degree-3 odd monomials, all seven even
  monomials, degree-4 spots): PASS — the gap is closed generally;
  the census's typing claim was never wider than its license.
```

---

## 5. FENCE-SCAN (standard)

```text
f-1 FLOATS/NUMERICS: grep for float literals over the target — every
    regex hit is a version number ("sympy 1.14.0") or a section/line
    reference (":152", "§3.1", "t-1..t-3"); no evalf/N()/float()/
    numpy tokens; every displayed constant is an exact rational, surd,
    or pi multiple; Lambda formal throughout. CLEAN.
f-2 FENCED CLASSES: the fenced-class tokens appear only in the
    target's own fence declarations (:29, :357) and record-vocabulary
    section names (CHOICE LEDGER); no fenced-class file content
    appears. CLEAN.
f-3 GATES: present at header (:17-18) and flag block (:719-720);
    DETERMINATION-ONLY posture held — no adoption, no authored
    stratum (span-checked: every undisplayed symbol is carried as an
    unevaluated functional, never valued), no member binding, no
    witness action, no flag of the record flipped. CLEAN.
f-4 HYGIENE: ONE output + sidecar at the commission-distinct path;
    seal verifies (11dd8ac5... OK); cutoff 03:33:52 declared BEFORE
    the seal mtime 03:38 — in order; sw-1 (occupation: only the
    target matches T6SR|DEG3|ANGULAR_CENSUS in the workspace; the
    cleanroom root: zero matches — re-swept by me), sw-3 (adjacency:
    T4SR-AUDIT 03:22 > MO4R_GRAM_MASS_T3SR_AUDIT 03:21 >
    JD_MEMBER_CANDIDATE_T2SR_CHECK 03:15 — verified at mtimes)
    both independently verified; the other CENSUS-named workspace
    files are family/carrier/slot censuses by name, different
    commissions, unopened. CLEAN.
```

---

## 6. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 03:51:57 -0500**. Corpus roots:
`/Users/bgm/MB Work/alpha-program-archive/workspace` (primary) and the
cleanroom root
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`
— swept by FILENAME ONLY except the seven §1 files (token greps ran
inside those seven only); fenced classes
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF) and ALL other
files matched by filename only, never opened. Artifacts sealed after
the cutoff are UNSWEPT and unclassed here.

```text
sb-1 OCCUPATION: no filename in either root matches an audit of the
     target (this artifact's path was ABSENT at session start and at
     the pre-write re-probe); the workspace files matching
     T6SR|DEG3|ANGULAR_CENSUS are the target and its sidecar ONLY;
     the cleanroom root: zero matches.
sb-2 ADJACENCY AT CUTOFF (filename/mtime only, unopened): newest
     workspace artifacts are the target (03:38), then the T4SR audit
     pair member (03:22), STAGE8_MO4R_GRAM_MASS_T3SR_AUDIT_V001
     (03:21), STAGE8_JD_MEMBER_CANDIDATE_T2SR_CHECK_V001 (03:15) —
     the latter two different commissions by name; not opened. No
     artifact newer than the target exists at this cutoff.
sb-3 Enumerations here claim only their displayed sweeps.
```

---

## 7. THE CAS BATTERIES (VERBATIM) AND OUTPUT (VERBATIM)

Fresh venv `t6srauditvenv` (sympy 1.14.0) under the session scratchpad;
nothing written to the workspace but this artifact and its seal.

RUN 1 — the target's battery, extracted verbatim from its §8 (awk fence
extraction, 147 lines): 5/5 PASS, output BYTE-IDENTICAL to the target's
recorded block (diff empty; it is the target's own §8 text, verified
identical — not reproduced here).

RUN 2 — my adversarial battery: 7/7 PASS on a single run; NO pre-final
corrections.

```python
# CENSUS-AUDIT adversarial battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
# t6srauditvenv). Commission T6SR audit. Checks the CENSUS BUILD DID NOT RUN plus
# independent re-derivation of every displayed parity and first moment. Nothing
# numeric evaluated (Lambda FORMAL; all coefficients generic symbols).
# CA1 Dirac ground INDEPENDENT (alphas assembled from gamma^0 gamma^j, not copied);
#     the A-U trace slot tr[alpha_x C(k)] = -2 khat_x; moments 0 and 4pi/3.
# CA2 ENUMERATION COMPLETENESS, wider than the build: ordered integer splits of -3
#     over the WIDER window [-10, -1] (the build displayed [-3, -1]) — exactly
#     (-1,-2), (-2,-1); the forcing arithmetic dF <= -1 ==> dU = -3 - dF >= -2;
#     and the e-2 ONLY-stratum bookkeeping contradiction as exact set arithmetic:
#     a second degree -1 factor entry W would make the assembled degree -2 stratum
#     set {V2xV2, WxV2, V2xW, WxW} with cardinality > 1, contradicting the sealed
#     "ONLY degree -2 assembled stratum is V_2 (x) V_2" (cardinality 1).
# CA3 THE CONSUMED LEMMA re-verified INDEPENDENTLY: S-conjugation on generic
#     letters (my own construction) => joint inversion = S-conjugation at every
#     length; trace flip tr[alpha_x W(-)] = -tr[alpha_x W] on a generic length-4
#     product (one deeper than the audit's generic length-3); AND the exhaustive
#     single-momentum sweep EXTENDED one full length past the T4SR build (all 127
#     words over {alpha.khat, S} to length 6): odd-or-zero throughout.
# CA4 THE C3 REDUCTION STRENGTHENED to FULL generic forms: T_odd the complete
#     generic odd polynomial (3 deg-1 + ALL TEN deg-3 odd monomials, generic
#     coefficients); a_even the complete generic even quadratic (1 + all six
#     deg-2 monomials) plus three deg-4 spots: int T a dOmega = int T a_odd
#     dOmega exactly; the kill and (4pi/3)c1 defeat exhibits re-derived.
# CA5 THE c-CLASS SLOTS INDEPENDENT: jet re-assembled; tr[jet] = 4 khat_x(b+ - b-)
#     and tr[alpha_x jet] = 4 khat_x^2 (b+ + b-); BOTH joint-swap-even; constant-
#     coefficient integrals 0 and (16pi/3)(b+ + b-); the 8pi/3 defeat; AND the
#     mu_B^I functional at a decomposed ray average (b+ - b-) = c0 + c1 khat_x +
#     c2 khat_y: mu_B^I = (16pi/3) c1 — nonzero IFF a khat_x-odd component exists
#     (the kill fails exactly on the undisplayed content, as the census types).
# CA6 THE SUM PER COR-A + AGGREGATION: L = mu_AU + mu_AF + mu_B symbolic; the
#     enumerated slot set maps one-one onto the sum's terms; L vanishes iff all
#     displayed moments do ONLY under the all-zero substitution exhibited (the
#     accidental-zero non-exclusion kept: a nontrivial zero sum mu_AU = -mu_AF,
#     mu_B = 0 is displayed CONSISTENT — COR-A's non-IFF honored).
# CA7 THE BRANCH-TABLE DEGREE COUNT STEP-BY-STEP (Lambda FORMAL): p = 2 stratum
#     killed of record (first moment zero, CA1) moves the count to p = 3 (its
#     carrier integral log Lambda, divergent formal); L = 0 kills the p = 3 total
#     => count at p = 4: 1 - 1/Lambda -> 1 BOUNDED; L != 0 => log genuine. The
#     exact integrals for p = 2, 3, 4 displayed: Lam - 1, log Lam, 1 - 1/Lam.
import sympy as sp
from sympy import Rational as R
from sympy import I as Im
from itertools import product as iproduct

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)
Zq = lambda M: sp.expand(M) == sp.zeros(*M.shape)

# ===== CA1 — Dirac ground INDEPENDENT + the A-U trace slot =====
s0 = sp.eye(2); sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -Im], [Im, 0]]); sz = sp.Matrix([[1, 0], [0, -1]])
Z2 = sp.zeros(2, 2)
blk = lambda a, b, c, d: sp.Matrix(sp.BlockMatrix([[a, b], [c, d]]))
gamma0 = blk(s0, Z2, Z2, -s0)
gammas = [blk(Z2, s, -s, Z2) for s in (sx, sy, sz)]      # gamma^j, Dirac basis
al = [sp.expand(gamma0*g) for g in gammas]               # alpha_j = gamma^0 gamma^j
g5 = blk(Z2, s0, s0, Z2)
S_op = sp.expand(-Im*gamma0*g5)
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
th, ph = sp.symbols('theta phi', real=True)
sub = {kx: sp.sin(th)*sp.cos(ph), ky: sp.sin(th)*sp.sin(ph), kz: sp.cos(th)}
def ang(e):
    return sp.integrate(sp.integrate(e.subs(sub)*sp.sin(th), (ph, 0, 2*sp.pi)),
                        (th, 0, sp.pi))
A1 = kx*al[0] + ky*al[1] + kz*al[2]
Ck = sp.expand((sp.eye(4) - A1)/2)
anti = all(Zq(sp.expand(al[i]*al[j] + al[j]*al[i] - 2*sp.KroneckerDelta(i, j)*sp.eye(4)))
           for i in range(3) for j in range(3))
ok("CA1 INDEPENDENT DIRAC GROUND + A-U TRACE SLOT: alpha_j = gamma^0 gamma^j"
   " (assembled, not copied) satisfy {alpha_i, alpha_j} = 2 delta_ij; S = -i"
   " gamma^0 gamma^5 Hermitian, S^2 = I, {S, alpha_j} = 0; tr[alpha_x C(k)] ="
   " -2 khat_x EXACT (A3 of record re-derived); int_S2 khat_x dOmega = 0 and"
   " int_S2 khat_x^2 dOmega = 4pi/3 — the record's kill and refusal moments",
   anti and Zq(sp.expand(S_op - S_op.H)) and Zq(sp.expand(S_op*S_op - sp.eye(4)))
   and all(Zq(sp.expand(S_op*a + a*S_op)) for a in al)
   and sp.expand((al[0]*Ck).trace() + 2*kx) == 0
   and sp.simplify(ang(kx)) == 0
   and sp.simplify(ang(kx*kx) - R(4, 3)*sp.pi) == 0)

# ===== CA2 — enumeration completeness, wider window + set arithmetic =====
splits_wide = [(dU, dF) for dU in range(-10, 0) for dF in range(-10, 0)
               if dU + dF == -3 and dU <= -1 and dF <= -1]
forcing = all(-3 - dF >= -2 for dF in range(-10, 0) if dF <= -1 and -3 - dF <= -1)
deg1_single = {('V2',)}                                   # the sealed ONLY deg -1 entry
deg1_double = {('V2',), ('W',)}                           # hypothetical second entry
asm2 = lambda s: {(a, b) for a in s for b in s}           # assembled deg -2 strata set
contradiction = (len(asm2(deg1_single)) == 1 and len(asm2(deg1_double)) == 4
                 and len(asm2(deg1_double)) > 1)
ok("CA2 ENUMERATION COMPLETENESS (wider than the build's display): ordered"
   " integer splits of -3 with both factors <= -1 over the window [-10, -1] are"
   " EXACTLY (-1, -2) and (-2, -1) — no deeper split exists (dF <= -1 forces"
   " dU = -3 - dF >= -2, displayed); and the e-2 ONLY-stratum contradiction at"
   " exact set arithmetic: one deg -1 factor entry => ONE assembled deg -2"
   " stratum (V_2 x V_2); a second entry W would yield FOUR (> 1), contradicting"
   " the sealed 'ONLY degree -2 assembled stratum' — the deg -1 slot is the"
   " V_2-form subsidiary and nothing else, so the census slot list {A-U, A-F,"
   " B-routes} is COMPLETE at the ground pair's granularity",
   sorted(splits_wide) == [(-2, -1), (-1, -2)] and len(splits_wide) == 2
   and forcing and contradiction)

# ===== CA3 — the consumed lemma, independent re-verification =====
p1, p2, p3, q1, q2, q3, r1, r2, r3, s1, s2, s3 = sp.symbols(
    'p1 p2 p3 q1 q2 q3 r1 r2 r3 s1 s2 s3', real=True)
e1, e2, e3, e4, f1, f2, f3, f4 = sp.symbols('e1 e2 e3 e4 f1 f2 f3 f4', real=True)
def gen_letter(v, b_c, c_c):
    return sp.expand(v[0]*al[0] + v[1]*al[1] + v[2]*al[2] + b_c*S_op + c_c*sp.eye(4))
Ls = [gen_letter((p1, p2, p3), e1, f1), gen_letter((q1, q2, q3), e2, f2),
      gen_letter((r1, r2, r3), e3, f3), gen_letter((s1, s2, s3), e4, f4)]
Lsm = [gen_letter((-p1, -p2, -p3), e1, f1), gen_letter((-q1, -q2, -q3), e2, f2),
       gen_letter((-r1, -r2, -r3), e3, f3), gen_letter((-s1, -s2, -s3), e4, f4)]
conj_each = all(Zq(sp.expand(S_op*Ls[i]*S_op - Lsm[i])) for i in range(4))
W4 = sp.expand(Ls[0]*Ls[1]*Ls[2]*Ls[3])
W4m = sp.expand(Lsm[0]*Lsm[1]*Lsm[2]*Lsm[3])
conj_w4 = Zq(sp.expand(S_op*W4*S_op - W4m))
flip4 = sp.expand((al[0]*W4m).trace() + (al[0]*W4).trace()) == 0
inv1 = {kx: -kx, ky: -ky, kz: -kz}
odd_all6, nz6, total6 = True, 0, 0
for L in range(7):
    for word in iproduct((A1, S_op), repeat=L):
        Mw = al[0]
        for f in word:
            Mw = Mw*f
        tr = sp.expand(Mw.trace())
        total6 += 1
        odd_all6 = odd_all6 and sp.expand(tr + tr.subs(inv1, simultaneous=True)) == 0
        if tr != 0:
            nz6 += 1
ok("CA3 THE CONSUMED LEMMA, INDEPENDENT: S L S = L(-momenta) for four generic"
   " letters (my own construction); S W S = W(-) on the generic LENGTH-4 product"
   " (one past the T4SR audit's generic length-3) and tr[alpha_x W(-)] ="
   " -tr[alpha_x W] there; exhaustive single-momentum sweep to length 6 (all 127"
   " words, one full length past the T4SR build's 63): every tr[alpha_x W]"
   " odd-or-zero under inversion, nonzero instances present — the census's"
   " consumption of the all-lengths lemma is inside its audited scope",
   conj_each and conj_w4 and flip4 and odd_all6 and nz6 > 0 and total6 == 127)

# ===== CA4 — the C3 reduction at FULL generic forms =====
tc = sp.symbols('t1:4', real=True)
uc = sp.symbols('u1:11', real=True)
ec = sp.symbols('ee0:7', real=True)
cc = sp.symbols('c1:4', real=True)
kv = [kx, ky, kz]
T1_full = tc[0]*kx + tc[1]*ky + tc[2]*kz
odd3_monos = [kx**3, ky**3, kz**3, kx**2*ky, kx**2*kz, ky**2*kx, ky**2*kz,
              kz**2*kx, kz**2*ky, kx*ky*kz]
T3_full = sum(u*m for u, m in zip(uc, odd3_monos))
even2_monos = [sp.Integer(1)] + [kv[i]*kv[j] for i in range(3) for j in range(i, 3)]
A_even_full = sum(e*m for e, m in zip(ec, even2_monos))
A_odd = cc[0]*kx + cc[1]*ky + cc[2]*kz
red1 = sp.simplify(ang(sp.expand(T1_full*(A_even_full + A_odd)))
                   - ang(sp.expand(T1_full*A_odd))) == 0
red3 = sp.simplify(ang(sp.expand(T3_full*(A_even_full + A_odd)))
                   - ang(sp.expand(T3_full*A_odd))) == 0
spot4 = all(sp.simplify(ang(T1_full*m)) == 0 for m in (kx**4, kx**2*ky**2, ky**2*kz**2))
kill_ex = sp.simplify(ang(sp.expand(T1_full*A_even_full))) == 0
defeat = sp.simplify(ang(kx*(cc[0]*kx)) - R(4, 3)*sp.pi*cc[0]) == 0
ok("CA4 THE MOMENT-FUNCTIONAL REDUCTION AT FULL GENERIC FORMS (the census's C3"
   " ran schema instances): T_odd = COMPLETE generic odd (3 deg-1 + all TEN"
   " deg-3 odd monomials), a_even = COMPLETE generic even quadratic (all 7"
   " monomials) + three deg-4 spots: int T a dOmega = int T a_odd dOmega EXACTLY"
   " at both degrees — the even part dies against every odd trace with no"
   " monomial escape; kill exhibit 0; defeat exhibit (4pi/3) c1 — the census's"
   " typing (each first moment depends ONLY on the amplitude's odd part) is"
   " CONFIRMED and STRENGTHENED",
   red1 and red3 and spot4 and kill_ex and defeat)

# ===== CA5 — the c-class slots, independent + decomposed ray average =====
bp, bm, c0d, c1d, c2d = sp.symbols('b_plus b_minus c0d c1d c2d', real=True)
jet = sp.expand(kx*((bp - bm)*sp.eye(4) + (bp + bm)*A1))
trj = sp.expand(jet.trace())
traxj = sp.expand((al[0]*jet).trace())
swap = lambda e: e.subs({kx: -kx, ky: -ky, kz: -kz, bp: bm, bm: bp},
                        simultaneous=True)
diff_amp = c0d + c1d*kx + c2d*ky            # (b+ - b-)(khat) = const + odd parts
muBI_dec = sp.expand(4*ang(sp.expand(kx*diff_amp)))
moment8 = sp.simplify(ang(kx*(kx - kx.subs(inv1, simultaneous=True))))
ok("CA5 THE c-CLASS SLOTS, INDEPENDENT: tr[jet] = 4 khat_x (b+ - b-) and"
   " tr[alpha_x jet] = 4 khat_x^2 (b+ + b-) re-derived; BOTH joint-swap-EVEN"
   " (khat -> -khat, b+ <-> b-): no parity kill in either slot; constant-"
   "coefficient integrals 0 and (16pi/3)(b+ + b-); the 8pi/3 defeat exhibit;"
   " AND the decomposed functional: at (b+ - b-) = c0 + c1 khat_x + c2 khat_y,"
   " mu_B^I = 4 int khat_x (b+ - b-) dOmega = (16pi/3) c1 EXACTLY — nonzero IFF"
   " a khat_x-odd component exists: the census moments turn exactly on the"
   " undisplayed beta_s content, as typed (B-2)",
   sp.expand(trj - 4*kx*(bp - bm)) == 0
   and sp.expand(traxj - 4*kx**2*(bp + bm)) == 0
   and sp.expand(swap(trj) - trj) == 0
   and sp.expand(swap(traxj) - traxj) == 0
   and sp.simplify(ang(trj)) == 0
   and sp.simplify(ang(traxj) - R(16, 3)*sp.pi*(bp + bm)) == 0
   and sp.simplify(moment8 - R(8, 3)*sp.pi) == 0
   and sp.simplify(muBI_dec - R(16, 3)*sp.pi*c1d) == 0)

# ===== CA6 — the sum per COR-A + aggregation + the non-IFF honored =====
muAU, muAF, muB = sp.symbols('mu_AU mu_AF mu_B', real=True)
L_sum = muAU + muAF + muB
slots = {'A-U': muAU, 'A-F': muAF, 'B': muB}
agg = (set(slots.values()) == set(L_sum.free_symbols)) and len(slots) == 3
all_zero = sp.simplify(L_sum.subs({muAU: 0, muAF: 0, muB: 0})) == 0
accidental = sp.simplify(L_sum.subs({muAU: sp.Symbol('a_x'),
                                     muAF: -sp.Symbol('a_x'), muB: 0})) == 0
ok("CA6 THE SUM PER COR-A: L_deg3 = mu_AU + mu_AF + mu_B aggregates EXACTLY the"
   " enumerated slots (one term per census class, none missing, none extra);"
   " the all-zero substitution gives L = 0 (the all-even structural case); AND"
   " the accidental cross-stratum zero mu_AU = -mu_AF, mu_B = 0 also gives"
   " L = 0 — COR-A's non-IFF (an accidental zero is not excluded) is honored"
   " by the census's branch condition being the SUM, not per-stratum parity",
   agg and all_zero and accidental)

# ===== CA7 — the branch-table degree count, step by step (Lambda FORMAL) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
I2 = sp.integrate(kk**(2 - 2), (kk, 1, Lam))
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
I4 = sp.integrate(kk**(2 - 4), (kk, 1, Lam))
ok("CA7 THE BRANCH TABLE STEP BY STEP (Lambda FORMAL, never valued): p = 2"
   " carrier integral Lam - 1 (the count the record's deg -2 kill removes:"
   " first moment zero, CA1); p = 3 integral log Lam, divergent formal (the"
   " census class's slot — L != 0 makes it GENUINE); p = 4 integral 1 - 1/Lam"
   " -> 1 BOUNDED (the landing if L = 0: every surviving diagonal contribution"
   " at degree <= -4) — the census's armed branch table is exact arithmetic on"
   " the consumed lemma, neither branch decided",
   sp.simplify(I2 - (Lam - 1)) == 0
   and sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo
   and sp.simplify(I4 - (1 - 1/Lam)) == 0 and sp.limit(I4, Lam, sp.oo) == 1)

print("T6SR-AUDIT-BATTERY-DONE")
```

Output, verbatim (7/7 PASS, single run):

```text
CA1 INDEPENDENT DIRAC GROUND + A-U TRACE SLOT: alpha_j = gamma^0 gamma^j (assembled, not copied) satisfy {alpha_i, alpha_j} = 2 delta_ij; S = -i gamma^0 gamma^5 Hermitian, S^2 = I, {S, alpha_j} = 0; tr[alpha_x C(k)] = -2 khat_x EXACT (A3 of record re-derived); int_S2 khat_x dOmega = 0 and int_S2 khat_x^2 dOmega = 4pi/3 — the record's kill and refusal moments: PASS
CA2 ENUMERATION COMPLETENESS (wider than the build's display): ordered integer splits of -3 with both factors <= -1 over the window [-10, -1] are EXACTLY (-1, -2) and (-2, -1) — no deeper split exists (dF <= -1 forces dU = -3 - dF >= -2, displayed); and the e-2 ONLY-stratum contradiction at exact set arithmetic: one deg -1 factor entry => ONE assembled deg -2 stratum (V_2 x V_2); a second entry W would yield FOUR (> 1), contradicting the sealed 'ONLY degree -2 assembled stratum' — the deg -1 slot is the V_2-form subsidiary and nothing else, so the census slot list {A-U, A-F, B-routes} is COMPLETE at the ground pair's granularity: PASS
CA3 THE CONSUMED LEMMA, INDEPENDENT: S L S = L(-momenta) for four generic letters (my own construction); S W S = W(-) on the generic LENGTH-4 product (one past the T4SR audit's generic length-3) and tr[alpha_x W(-)] = -tr[alpha_x W] there; exhaustive single-momentum sweep to length 6 (all 127 words, one full length past the T4SR build's 63): every tr[alpha_x W] odd-or-zero under inversion, nonzero instances present — the census's consumption of the all-lengths lemma is inside its audited scope: PASS
CA4 THE MOMENT-FUNCTIONAL REDUCTION AT FULL GENERIC FORMS (the census's C3 ran schema instances): T_odd = COMPLETE generic odd (3 deg-1 + all TEN deg-3 odd monomials), a_even = COMPLETE generic even quadratic (all 7 monomials) + three deg-4 spots: int T a dOmega = int T a_odd dOmega EXACTLY at both degrees — the even part dies against every odd trace with no monomial escape; kill exhibit 0; defeat exhibit (4pi/3) c1 — the census's typing (each first moment depends ONLY on the amplitude's odd part) is CONFIRMED and STRENGTHENED: PASS
CA5 THE c-CLASS SLOTS, INDEPENDENT: tr[jet] = 4 khat_x (b+ - b-) and tr[alpha_x jet] = 4 khat_x^2 (b+ + b-) re-derived; BOTH joint-swap-EVEN (khat -> -khat, b+ <-> b-): no parity kill in either slot; constant-coefficient integrals 0 and (16pi/3)(b+ + b-); the 8pi/3 defeat exhibit; AND the decomposed functional: at (b+ - b-) = c0 + c1 khat_x + c2 khat_y, mu_B^I = 4 int khat_x (b+ - b-) dOmega = (16pi/3) c1 EXACTLY — nonzero IFF a khat_x-odd component exists: the census moments turn exactly on the undisplayed beta_s content, as typed (B-2): PASS
CA6 THE SUM PER COR-A: L_deg3 = mu_AU + mu_AF + mu_B aggregates EXACTLY the enumerated slots (one term per census class, none missing, none extra); the all-zero substitution gives L = 0 (the all-even structural case); AND the accidental cross-stratum zero mu_AU = -mu_AF, mu_B = 0 also gives L = 0 — COR-A's non-IFF (an accidental zero is not excluded) is honored by the census's branch condition being the SUM, not per-stratum parity: PASS
CA7 THE BRANCH TABLE STEP BY STEP (Lambda FORMAL, never valued): p = 2 carrier integral Lam - 1 (the count the record's deg -2 kill removes: first moment zero, CA1); p = 3 integral log Lam, divergent formal (the census class's slot — L != 0 makes it GENUINE); p = 4 integral 1 - 1/Lam -> 1 BOUNDED (the landing if L = 0: every surviving diagonal contribution at degree <= -4) — the census's armed branch table is exact arithmetic on the consumed lemma, neither branch decided: PASS
T6SR-AUDIT-BATTERY-DONE
```

---

## 8. CHOICE LEDGER (commission T6SR — CENSUS-AUDIT)

```text
AA-1 [PROVABLE, run first]: seal verification of all seven inputs
     (shasum -a 256 -c) before any reliance; the target verified
     before opening; the verbatim battery re-run and diffed; the
     byte-pins of §1 re-located by my own line-numbered greps; the
     mtime/filename sweeps — all mechanical, all before judgment.
AA-2 VERDICT CLASS [YOURS]: CONFIRMED-WITH-CORRECTIONS over CONFIRMED.
     Ground: COR-1 is a real (if small) imprecision in a displayed
     sweep line of a sweep-fenced artifact ("every enumeration
     sentence claims only its own displayed sweep") — the record's
     convention for audits that repair display while every chain
     stands (the WSR-AUDIT COR-1 / WBC-AUDIT COR-2 precedents).
     REFUTED requires a false load-bearing claim; none exists: the
     enumeration, every displayed parity and moment, the sum, the
     branch table, and the verdict all stand at my independent
     re-derivation.
AA-3 ENUMERATION FIRST [YOURS]: the completeness check was re-derived
     from the bookkeeping ground BEFORE comparing with the target's
     table (m-2), because a missing class is the one defect that
     could not be repaired by margin note — none was found.
AA-4 LEMMA GRADE [YOURS, displayed]: my CA3 re-verifies the consumed
     all-lengths identity independently (generic length-4; 127-word
     sweep) but the census's consumption is adjudicated AT the
     audited grade of record (T4SR-AUDIT R2) — the census re-proves
     nothing and needed not to; my re-verification closes the
     consumption-fidelity question, not the lemma itself.
AA-5 THE 8pi/3 CONVENTION [YOURS, noted, no correction]: the defeat
     exhibit's figure rides the record's own unhalved f(khat) -
     f(-khat) convention (T4SR T6 / T4SR-AUDIT R4 verbatim); the
     census reproduces it faithfully and consistently with its
     (16pi/3) c_1 decomposed form (CA5 pins both) — no ambiguity
     survives the displays.
AA-6 COR-1 SEVERITY [YOURS]: correction, not refutation and not mere
     note: sw-2's "V_3" line is a displayed sweep result that a
     byte-check contradicts in attribution (two hits, one outside
     "the T4SR audit's own"); but both hits are absence claims, the
     substantive absence is true and audited of record, and no flag,
     class, or verdict consumes the miscounted line — VERDICT-NEUTRAL,
     registrar margin note sufficient, NO re-issue forced.
AA-7 HOMONYM SPLIT [inherited, disclosed in header]: n carrier index;
     k momentum; (o, c) the REM strata indices as quoted.
```

---

## 9. TOY_SEPARATION

```text
BT-1 CA1/CA3/CA5 use the EXACT 4x4 Dirac algebra of the record (not a
     toy: the sealed spinor structure itself), assembled independently
     (alpha_j = gamma^0 gamma^j); CA3's generic letters are the sealed
     alphabet with symbolic coefficients — type-level identities valid
     termwise for the operator instantiation (the same license as WSR
     T-2/WS6, T4SR T1-T3, T4SR-AUDIT R2). No matrix-grade result is
     carried to the carrier quantifier.
BT-2 CA4's complete generic odd/even polynomials are identity-schema
     coverage of the lemma's joint-odd trace outputs against
     undisplayed amplitudes at the record's KF1/KF2 effective-degree
     license — used ONLY to type the census moments' dependence,
     never to value an undisplayed symbol.
BT-3 CA2 is exact integer/set combinatorics over the sealed degree
     bookkeeping; CA6 is exact symbol arithmetic; CA7 is exact
     formal-Lambda carrier arithmetic (the KF1 license); Lambda never
     valued.
BT-4 No testbed evaluation appears; nothing witness-family-grade is
     promoted; no spectral datum of the actual family is valued; no
     stratum symbol is authored — every undisplayed symbol stays a
     symbol here exactly as in the target.
```

---

## 10. FLAG BLOCK

```text
T6SR_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the target's
  CENSUS-INCOMPLETE stands adjudicated at the bytes, per claim: the
  ENUMERATION is complete at the ground pair's granularity (my own
  re-derivation from the sealed bookkeeping — wider split window, the
  ONLY-stratum contradiction at exact set arithmetic, the exhaustive
  c = 0 / c-bearing partition; NO missing class; CA2); EVERY displayed
  parity and first moment re-computed exact (target battery 5/5
  BYTE-IDENTICAL in a fresh venv; my independent CA1/CA4/CA5 on fresh
  code paths, the C3 reduction strengthened to complete generic
  forms); the class-A UNDISPLAYED marks and class-B blockage are real
  (B-1/B-2 re-confirmed by my own token sweeps of the six; AC-4
  re-located verbatim); the SUM per COR-A aggregates exactly the
  enumerated classes, displayed ONLY as the unevaluated total, the
  accidental-zero non-exclusion honored (CA6); the BRANCH TABLE is
  exact and its degree count lands step-by-step (-2 killed of record
  -> -3 = log Lambda -> L = 0 => <= -4 = 1 - 1/Lambda bounded; L != 0
  => log genuine; CA7) with every lemma application inside the audited
  all-lengths scope (CA3); scope held (summed only, V-2 uncrossed, no
  flag flipped, the jet at the T4SR pair's verbatim quotes with MO4
  unopened); fence-scan CLEAN. CORRECTIONS, display-level and
  verdict-neutral: COR-1 the "V_3" sweep line (two hits in the six,
  both absence claims — T4SR :182, T4SR-AUDIT :230 — not zero, and
  one outside "the T4SR audit's own"); N-1 the §0/§5 token-family
  compressions (§7 sw-2 controlling and exact) and the loose
  "strictly larger six"; N-2 C3's schema-instance "generic" wording
  (closed generally in the build's favor by CA4). Registrar margin
  notes suffice; NO re-issue forced. )

HEADLINE_ADJUDICATION = CONFIRMED( CENSUS-INCOMPLETE: the degree-3
  enumeration is delivered whole at the ground pair's granularity —
  every degree-3 diagonal stratum of K^Sigma splits as [the V_2-form
  subsidiary at -1] x [the partner at exactly -2], classes A-U/A-F
  and the B c-routes, spinor side joint-odd by the consumed audited
  lemma — BUT the commissioned per-class parities and exact first
  moments are NOT completable on the six named seals (B-1: no
  degree-3 angular symbol display, AC-4 marks the parity claimed by
  no one; B-2: no beta_s first-angular-moment certificate, parity
  refused in both traced slots), so the moments stand as exact
  unevaluated functionals, the COR-A sum L_deg3 has no displayable
  closed form, and NEITHER armed branch (L = 0 => RATE-CERTIFIED at
  1 - 1/Lambda; L != 0 => the log genuine) is decidable today; the
  completing DEGREE-3 SYMBOL-AND-MOMENT DISPLAY PAIR is correctly
  named over already-sealed stock outside the six. NOT REFUTED: no
  divergence of tr K^Sigma_n exhibited anywhere in target or ground. )

CAS = 5/5 verbatim re-run BYTE-IDENTICAL (fresh venv t6srauditvenv,
  sympy 1.14.0) + 7/7 audit battery (single run, NO pre-final
  corrections; NOTHING numeric evaluated).

SEALS_VERIFIED = 7/7( the target before opening + the six named
  ground artifacts (T4SR pair, WSR pair, WBC pair) — every digest
  recomputed from bytes at path this session by full digest via
  shasum -a 256 -c against sidecars; all OK; none unverifiable; every
  load-bearing span read at path; scoped reads of these seven ONLY;
  the MO4 pair NOT opened — the jet display adjudicated at the T4SR
  pair's verbatim byte-quotes, the same grade the target used. )

PER_COMPOSITE_WALL = UNTOUCHED( summed quantifier only, verified span
  by span in target and held here; no per-composite or per-pair
  quantity quoted, moved, or decided; V-2 not crossed in either
  direction; T_n, o-2-OBS, B-L2*, alpha' = 1/4, MO-2, MO-3/p = -3,
  the WBC enclosure, and every sealed identity at their own
  quantifiers; no flag of the record flipped; no witness action —
  witness status is the registrar's. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant
  symbolic or an exact rational/surd; NOTHING numeric evaluated
  (Lambda formal; V_2, beta_s, all coefficients generic); no float
  ground; no measured constant; no measured-constant comparison; no
  value frozen; alpha never computed or approached; kappa_record
  nowhere; no per-pair route traversed; no fenced-class or
  out-of-scope file opened (filename matches only); "Q-..." tokens
  noted, never chased; no flag flipped; no witness action; no git;
  ONE output + sidecar at the commission-distinct path, probed ABSENT
  at session start and re-probed ABSENT immediately before write. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv t6srauditvenv
  under the session scratchpad, TWO runs (the target's battery
  verbatim, diffed byte-identical; my 7-check battery, 7/7 PASS
  single run, script and output reproduced verbatim in §7);
  shasum/file reads/filename+token sweeps otherwise; NOTHING numeric
  evaluated.

SWEEP_CUTOFF = 2026-08-15 03:51:57 -0500 (workspace root + cleanroom
  root; fenced classes and all non-scoped files filename-matched
  only, never opened; token greps inside the seven scoped files only;
  artifacts sealed after the cutoff UNSWEPT).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until registered; witness/flag actions are the
registrar's.
T6SR_DEG3_ANGULAR_CENSUS_AUDIT = SEALED.
```
