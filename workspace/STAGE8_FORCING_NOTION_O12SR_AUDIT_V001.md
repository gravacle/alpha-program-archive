# STAGE 8 — AUDIT OF THE FORCING-NOTION DETERMINATION (O12SR) V001

DATE: 2026-08-15
COMMISSION: O12SR (FORCING-NOTION-AUDIT). DEFAULT-REFUTE. Testimony zero weight — every claim
re-derived at bytes from the sources, not from the target's report of them.
TARGET: `STAGE8_FORCING_NOTION_O12SR_V001.md` + `.seal.sha256`.
STATUS: DETERMINATION ONLY — display and adjudication of displays. No row reclassified, no
verdict overturned, no definition proposed, none adopted. No value, no number as a value of
anything, no measured-constant comparison.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/`
`STAGE8_FORCING_NOTION_O12SR_AUDIT_V001.md` — ABSENT at start of commission (probed before any
write; `ls` returned "No such file or directory").

---

## §0 — STEP 0: TARGET AND GROUND, SEALS VERIFIED AT BYTES

`shasum -a 256 -c` run FROM the artifact's own directory
(`/Users/bgm/MB Work/alpha-program-archive/workspace`) in every case.

```text
TARGET
  OK  STAGE8_FORCING_NOTION_O12SR_V001.md
      839f5079bb4ff89f2d02e35a60333fc888643300feda8b0e17d610fb54e207bb
      (recomputed independently; identical to the sidecar. 900 lines, 59813 bytes.)

THE TARGET'S NINE CONSUMED ARTIFACTS — every seal re-verified by this audit, 9/9 OK, 0 mismatch
  OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md
  OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md
  OK  STAGE8_PARTITION_THEOREM_T16SR_V001.md
  OK  STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md
  OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md
  OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md
  OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md
  OK  STAGE8_5D_REREAD_T13SR_V001.md
  OK  STAGE8_5D_REREAD_T13SR_AUDIT_V001.md
```

## §0.1 — SWEEP DECLARATION AND CUTOFF (this audit's own)

```text
ROOT 1 = /Users/bgm/MB Work/alpha-program-archive/workspace          (6801 files)
ROOT 2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
         alpha_fundamental_record_action_cleanroom_v003              (7805 files)

RE-RUN, UNCAPPED, BOTH ROOTS, ALL FILE TYPES (grep -r, no --include filter):
  SWEEP A  the target's own generative-forcing pattern, verbatim.
  SWEEP A' MY OWN WIDER PATTERN, written independently before reading the target's
           disposal: (across (every|all|any) lawful | every lawful (route|record|
           generation|production|construction|creation|authoring) | invariant
           (across|under) (every|all|any) (production|generation|construction|
           creation|realization|authoring|way|route|history) | lawful
           (generation|production|creation)s? of (a|the) record | however (the
           record|it) (was|is) (made|produced|created) | regardless of how
           .{0,40}(produced|created|generated|made)).
  SWEEP C  the target's native-model-definition pattern, verbatim.
  SWEEP D  the target's model-class/producible-record pattern, verbatim.
  SWEEP D' MY OWN WIDER COINCIDENCE PATTERN, written independently: ((model|models)
           .{0,50}(producible|produceable|constructible|writable|authorable|
           creatable) | (producible|constructible) record | class of
           (producible|possible|lawful) records | every model .{0,60}record |
           record .{0,30}(is|as) a model).
  SWEEP E  the target's invariance pattern, verbatim, RAW (fenced names NOT filtered,
           so the fence event could be seen rather than taken on testimony).
  BINARY-REACH CHECK: file-extension census of both roots, to test whether any
           prose source could lie outside grep's reach (the unlocated-source hunt).
SCOPED READS: the nine consumed artifacts at their operative sections, plus
  STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V001.md at the single sweep-A hit line
  and its surrounding table (to dispose the hit at bytes rather than on report).
CUTOFF = the above. NOTHING ELSE OPENED. No register/tracker/road/plan/continuation
  file was read. "Q-..." tokens EXPECTED-UNLOCATABLE, none resolved. No git.
```

---

## §1 — HUNT A (HARDEST FIRST): MANUFACTURED MISMATCH

The commission's standard: for every claimed mismatch, the TWO OPERATIVE SENTENCES quoted at
their own bytes, plus a display that they are NOT EQUIVALENT ON THE ROWS IN QUESTION. A mismatch
that dissolves on inspection would overturn the target's headline. Three mismatches are claimed.
Each is put to the standard below.

### §1.1 MISMATCH 1 — require-side CA/PT vs allow-side DG/PR. SURVIVES THE STANDARD.

SENTENCE ONE (require side, row R-1), at its own bytes — `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md`
line 130:

> "one condition (single-valued closure) whose admissible set is the integer lattice ℤ — a
>  CLASS, not a point"

SENTENCE TWO (the governing sentence of the nine forcing tests and of allow rows A-5/9/10/11/13),
at its own bytes — `STAGE8_5D_REREAD_T13SR_V001.md` lines 234–235:

> "A FORCED sentence requires every forcing link displayed at its own quantifier from sealed
>  artifacts."

NON-EQUIVALENCE, DISPLAYED ON THE ROWS IN QUESTION — not asserted:

```text
SENTENCE ONE's test is passed by SOLVING AN EQUATION. Re-executed at this audit's
  own hands (fresh venv, §8/O1): solveset(exp(2*pi*I*x) = 1) returns Integers over C
  and over R via the real closure component. The test's outcome is FIXED BY THE
  ALGEBRA and is INDIFFERENT to what any artifact displays: the set is what it is
  whether or not the record ever writes it down.
SENTENCE TWO's test is passed by A DISPLAY EXISTING IN SEALED ARTIFACTS. Its outcome
  is INDIFFERENT to what is solvable: D3 fails because "the cell-local write-typing
  rule is UNWRITTEN of record", D4 because "the ledger identifications remain
  performed-not-licensed; no docket artifact licenses them". A rule can be unwritten
  and the underlying content still entailed; a licence can be unissued and the
  identification still true. Neither test entails the other IN EITHER DIRECTION.
THE ROWS THIS SEPARATES ARE NAMED AND COUNTED: R-10 on the require side and
  A-5/A-9/A-10/A-11/A-13 on the allow side — 6 of 23 rows, exactly the target's
  count, re-derived here (10 require + 13 allow = 23; 1 + 5 = 6).
```

VERDICT ON MISMATCH 1: **REAL, NOT MANUFACTURED.** The two sentences are quoted at their own
bytes above; their tests have independent truth conditions; the rows they separate are named.

### §1.2 MISMATCH 2 — the partition theorem's "one notion, two model classes". SURVIVES, BUT THE TARGET STATES IT MORE STARKLY THAN ITS SOURCE. (FINDING F-A1)

SENTENCE ONE, `STAGE8_PARTITION_THEOREM_T16SR_V001.md` lines 149–151:

> "**(I) REQUIRE-HALF.** In EVERY model of Σ, the quantization class holds: every sector/winding
>  label admissible under Σ4's single-valued closure lies in ℤ — exactly ℤ, no more"

SENTENCE TWO, same artifact, lines 153–157:

> "**(II) ALLOW-HALF.** In NO model of Σ is the crossing's magnitude forced: over every model of
>  Σ, the expansion by profile f₁, the expansion by profile f₂, and the junction-free reduct are
>  all again models of Σ"

Both verified verbatim. The target's rendering of both is faithful, and its "NOTION AS PROVEN"
column is confirmed at the proof's own bytes: (I) is carried by S3–S4 exact algebra
("The full solution set of exp(2πi·x) = 1 is EXACTLY ℤ … [CAS PT3d]", T16SR line 185) plus the
one-step bridge S5; (II) is carried by S7 (vocabulary fact) + S8 (invariance lemma) + S9, with no
solved set anywhere. Re-derived independently at §8.

THE DEFECT. The class split rests entirely on audit finding F-1, which the target quotes with an
ellipsis. Here is F-1 at its own bytes (`STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md`
lines 213–222), with the ELIDED SPAN restored and marked:

> "The require-half's 'in EVERY model of Σ' is honest over the model class in which the character
>  notation carries its standard (continuous, exponential) denotation **— the sealed tower's own
>  uniform reading, and arguably what §2's model notion already implies (χ is absent from §1's
>  symbol inventory, hence fixed notation, not free vocabulary) —** but the clauses AS ENUMERATED,
>  with χ read as free unit-modulus vocabulary, admit wild-character structures over which claim
>  (I)'s wording loses its referent"

The span the target replaced with "…" is the source's OWN COUNTER-CONSIDERATION to the reading
the target builds on it: if χ is fixed notation rather than free vocabulary, then §2's model
notion ALREADY excludes the wild-character structures, and the two halves quantify over the SAME
class. The target's §2.3 states the split flatly — "the halves quantify over DIFFERENT MODEL
CLASSES" — where its single source hedges its own finding in the elided clause.

DOES THE MISMATCH DISSOLVE? NO — and this is displayed, not assumed. The audit's per-claim
verdict lines (its §A6, lines 278–287) survive the restoration intact:

> "(I)   REQUIRE-HALF … = CONFIRMED-WITH-CORRECTIONS (F-1: the model-class quantifier is honest
>  over the standard-denotation reading, which must be named …)"
> "(II)  ALLOW-HALF … = CONFIRMED (denotation-robust; vocabulary disjointness verified …)"

An asymmetry of DEPENDENCE ON AN INTERPRETIVE PREMISE therefore stands at bytes: the require half
needs a reading NAMED as a premise; the allow half needs none. What does not stand at the source's
own strength is the flat "different model classes". CORRECTION, not refutation — recorded F-A1.

### §1.3 MISMATCH 3 — "a PROVENANCE predicate is tested by a MODEL-THEORETIC test". SURVIVES IN SHAPE; ITS SHARPEST CLAIM DOES NOT. (FINDING F-A4)

SENTENCE ONE (the predicate), `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md` lines 105–106, verbatim:

> "**(J-der)** DERIVED: every ingredient forced by the ground's self-consistency — no authored
>  ingredient; an authored ingredient presented as forced voids the candidate (mandate pattern)."

SENTENCE TWO (the test that discharges it), same artifact lines 245–248 and 262–263:

> "there exist two models of every displayed ground clause that agree on all ground content and
>  differ on the junction. Hence no Class II candidate satisfies J-der."
> "By J-der (the mandate pattern), every Class II candidate is void as *derived*."

NON-EQUIVALENCE, DISPLAYED: J-der quantifies over the INGREDIENTS OF A CONSTRUCTION; Theorem II
quantifies over MODELS OF A CLAUSE SET. Those are different domains, and the target is right that
the record moves between them. The shape of the mismatch is real.

WHERE THE TARGET OVERREACHES. Its §1.5 says the inference "is valid ON THE ASSUMPTION that a
producer of the object has nothing but Σ to work from … It is nowhere stated in the AJ." But
J-der's own bytes NAME THE GROUND as the forcing source — "every ingredient forced by **the
ground's self-consistency**". The step from "not derivable from the ground" to "not forced by the
ground's self-consistency" is licensed by the predicate's own wording, not by an unstated
coincidence. What remains genuinely imported is the SECOND step — from "not derivable" to "false
in some model", i.e. soundness — which the target displays correctly elsewhere. The claim
"nowhere stated in the AJ" is not sustainable at bytes for the first step. CORRECTION — F-A4.

### §1.4 THE DISJOINTNESS CLAIM, TESTED AGAINST THE TARGET'S OWN BATTERY. (FINDING F-A2)

The target states, in §2.2, §6(2) and its FLAG BLOCK, an ABSOLUTE disjointness:

> "no require-side row is established model-theoretically; no allow-side row is established
>  constructively or proof-theoretically."

Its own §5, at the same seal, says the opposite of half of that:

> "SUBSTANTIVE NOTE THE BATTERY DISPLAYS (O4): the two-model witness is ITSELF a construction —
>  two objects exhibited. It therefore satisfies the require half's AS criterion as well"

And its §2.4B: A-1/A-2 "SURVIVE … under AS that is a constructive demonstration".

Under the target's own §1.0 definition of CA — "forced by an exhibited construction or identity:
an equation SOLVED, a residual EXHIBITED, a generator DISPLAYED" — row A-1's bytes exhibit two
functions and an exact residual, which is the CA marker as the target itself wrote it. So "allow
side: CA 0 rows" is contradicted by the artifact's own battery note.

THE DISTINCTION THAT WOULD SAVE IT IS REAL BUT NEVER STATED AS THE GROUND OF THE TALLY. I ran it
mechanically (§8/X1): the require-side CA rows RETURN THE WHOLE ADMISSIBLE SET (solveset →
Integers; "the a=0 slice of ker(qtilde_N) = the (R4) line … dim 1"), whereas the two-model witness
EXHIBITS TWO MEMBERS of a set it never produces — a lower bound on cardinality, not the set. That
is a genuine difference in constructive act, and it is what the tally needs; the target rests the
tally instead on an unqualified "CA 0 rows" that its own §5 note refutes. CORRECTION — F-A2.

### §1.5 HUNT A, DISPOSITION

```text
NO CLAIMED MISMATCH DISSOLVES ON INSPECTION. Mismatch 1 survives with its two
  operative sentences quoted at bytes and its non-equivalence displayed on the six
  rows it separates. Mismatch 2 survives as an asymmetry of dependence on a named
  interpretive premise, but not at the flat strength the target states it, because
  the target's ellipsis removes its source's own counter-consideration (F-A1).
  Mismatch 3 survives in shape but not in its sharpest claim (F-A4). The absolute
  disjointness statement is contradicted by the target's own battery note (F-A2).
THE HEADLINE IS NOT MANUFACTURED. It is over-stated at three seams, each nameable
  and each correctable by restoring text the target already had in hand.
```

---

## §2 — HUNT B: COMPLACENT UNIFORMITY

The opposite failure: a ONE-NOTION verdict reached by treating different operative sentences as
the same notion because they share the word "forced". The commission demands each row's operative
test be displayed IN ITS OWN TERMS.

```text
STRUCTURAL DISPOSITION: the target does NOT return a one-notion verdict. It returns
  a five-notion inventory plus a route-neutral criterion and a one-off. The failure
  mode this hunt guards against is therefore not the one the artifact could commit —
  but its MIRROR is: an inventory padded by splitting one notion into several. That
  is what I tested instead, row by row, at the sources' bytes.
```

EVERY ROW'S OPERATIVE SENTENCE CHECKED AGAINST ITS SOURCE. I read the junction survey's §2 table
(its lines 130–139) and §3 table (its lines 159–171) in full and compared each quoted fragment to
the row it is drawn from. RESULT: **every one of the 23 quoted fragments is verbatim from its own
row.** No fragment is imported from a neighbouring row, and no row is classified on a word taken
from elsewhere.

THE PADDING TEST — are the three named OTHERS (DG, PR, AS) distinct notions or one notion under
three labels? Each is anchored to a DIFFERENT sealed sentence, and I verified all three anchors:

```text
DG anchor  5d line 234: "A FORCED sentence requires every forcing link displayed at
           its own quantifier from sealed artifacts."      VERIFIED VERBATIM.
PR anchor  AJ line 105: "every ingredient forced by the ground's self-consistency —
           no authored ingredient".                        VERIFIED VERBATIM.
AS anchor  forcing-boundary adjudication §1 POINT 1, lines 54-57 as reproduced at
           T14SR lines 81-84: "Forcing has ONE criterion (the admissible set is a
           point) and THREE shapes".                       VERIFIED VERBATIM.
DISTINCTNESS, DISPLAYED: DG's subject is AN ARTIFACT ("displayed … from sealed
  artifacts"); PR's subject is AN INGREDIENT ("every ingredient … no authored
  ingredient"); AS's subject is A SET ("the admissible set is a point"). Three
  different subjects, three different sentences, three different sources. They are
  not one notion relabelled.
```

THE REVERSE PADDING TEST — is MT split from CA merely by vocabulary? No: §8/X1 exhibits the
difference mechanically (whole set returned vs. two members exhibited), and the two halves of the
partition theorem are carried by structurally different proofs (S3–S4 algebra vs. S7–S9
semantics) which I read in full at T16SR lines 174–213.

HUNT B DISPOSITION: **NO COMPLACENT UNIFORMITY, AND NO INVENTORY PADDING.** Each row's operative
test is displayed in its own terms, and the notion labels are anchored to distinct sealed
sentences verified at bytes.

---

## §3 — HUNT C: THE GENERATIVE-NOTION SWEEP (UNCAPPED, BOTH ROOTS) AND THE ABSENCE/UNLOCATED DISTINCTION

This is the hunt a prior commission failed. An UNLOCATED SOURCE IS NOT AN ABSENT ONE.

### §3.1 Sweep A re-run, and the single hit disposed at ITS OWN bytes

```text
SWEEP A, the target's pattern, re-run UNCAPPED over both roots, all file types:
  4 FILE PATHS, 2 DISTINCT ARTIFACTS, 1 DISTINCT LINE:
    [R1]/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V001.md:107
    [R1]/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md:205
    [R2]/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V001.md:107
    [R2]/STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md:205
  The target reports this as "2 files". Run over BOTH roots as it declares, the
  pattern returns FOUR paths (the artifact is mirrored into root 2). The count is
  de-duplicated by basename and reported as a file count. Immaterial to the
  disposal but inaccurate as stated — FINDING F-A6.
```

THE HIT, DISPOSED AT ITS OWN BYTES (I opened the artifact at line 107 and read its §3 table
rather than accepting the target's disposal):

> "## 3. Pairing root: every lawful construction route and price [CLAIMED]"

The table beneath it enumerates four pairing families — `P-HS`, `P-CT`, `P-NEW`, `P-BLANK` — each
with a column headed "Per-option physical-price draft" whose cells begin "AUTHORED:" and end "No
joint pairing is called source-derived." **This is an authorship-price schedule per construction
family. It is not a forcing predicate, and it quantifies over ROUTES A PRINCIPAL MAY TAKE, not
over productions across which something is invariant.** The target's disposal is CORRECT at bytes.

### §3.2 My own wider pattern, written independently: also nothing

SWEEP A' (§0.1, six alternative phrasings of the generative shape) returned **the same 4 paths
and no others**, uncapped over both roots. An independently-worded search does not find a
generative notion the target's wording missed.

### §3.3 The unlocated-source test — the error that killed a prior commission

```text
COULD A PROSE SOURCE LIE OUTSIDE GREP'S REACH? File-extension census, both roots:
  py 4241 · md 4198 · sha256 3749 · json 1679 · pyc 545 · txt 42 · al 32 · bin 22
  · member 14 · png 8 · xsl 6 · sh 4 · npz 4 · lark 4 · jsonl 4 · g4 4 · zip 2
  All sweeps were run WITHOUT an --include filter, so md/py/json/txt/member/al were
  all searched. The only grep-opaque containers are: 14 hash-named .bin blobs, 4
  .npz numeric arrays, 545 .pyc, and STAGE7_QSPEC_CANDIDATE_V001.zip — whose
  contents are separately present, unpacked, under review_packets/ and WERE swept.
  NO PROSE ARTIFACT IN EITHER ROOT LIES OUTSIDE THE SWEEP'S REACH.
```

### §3.4 The fence event — verified independently, and the qualifier the verdict drops (F-A7)

I ran SWEEP E **raw, without filtering fenced filenames**, precisely so the fence event could be
seen rather than taken on the target's testimony. RESULT, both roots, uncapped:

```text
EXACTLY ONE HIT IN EITHER ROOT. Its file is QUESTIONSSETTLED_REGISTER_V001.md — a
  REGISTER file, in the fenced class. THE FILE WAS NOT OPENED BY THIS AUDIT AND THE
  LINE IS NOT QUOTED HERE. The target's FENCE EVENT display is therefore CONFIRMED
  at bytes and its handling is correct: it declared the event, did not open the
  file, did not consume the line, and carried no premise from it.
```

THE QUALIFIER, AND WHERE IT IS LOST. The target states the result correctly twice — §0.1
("ZERO hits in non-fenced files") and §3.2 ("SWEEP E ZERO hits in non-fenced files, both roots") —
and then states it UNQUALIFIED in its verdict, §6(6): "ZERO". The honest form of the finding is:
**absent among all readable files in both roots, with exactly one unread candidate inside a file
class this commission may not open.** That is precisely the absent-vs-unlocated distinction this
hunt exists to enforce, and the verdict line drops it. CORRECTION — F-A7.

### §3.5 Sweeps C and D re-run: the two supporting absences

```text
SWEEP C (a native definition of "model"), UNCAPPED both roots: 5 LINES, ALL inside
  STAGE8_PARTITION_THEOREM_T16SR_V001.md (2) and its audit (3). CONFIRMED: the only
  definition of the model notion in either root is the one the target names, and it
  is one day old. (These two artifacts are not mirrored into root 2 — hence 5, not
  10, which is consistent.)
SWEEP D (model-class / producible-record coincidence), UNCAPPED both roots: ZERO.
SWEEP D' (my own wider coincidence pattern): 8 lines, ALL incidental and NONE a
  coincidence claim — six are the spec sentence "Every model uses a unit-normalized
  local generator in record units" (a test-model convention), two are a table cell
  "countermodel (L2-5)-(L2-8) | open | AUTHORABLE". NO ARTIFACT IN EITHER ROOT
  ASSERTS, ARGUES, OR ASSUMES THE COINCIDENCE. Absence CONFIRMED INDEPENDENTLY.
```

HUNT C DISPOSITION: **THE SWEEP WAS GENUINELY UNCAPPED OVER BOTH ROOTS, AND THE CLAIMED ABSENCES
ARE GENUINE ABSENCES, NOT UNLOCATED SOURCES** — with the single qualification of F-A7, which is a
loss of a qualifier in the verdict line, not a mislocated source.

---

## §4 — HUNT D: RECLASSIFICATION, OVERTURNING, OR A PROPOSED/ADOPTED DEFINITION

Any one of these voids the artifact. The commission requires the sentence be quoted if found.

```text
MECHANICAL SCAN of the target, case-insensitive, for the acts themselves:
  (we|I) (adopt|propose|define) · "is hereby" · "therefore reclassif" ·
  "reclassify <ROW>" · "now reads" · "should read" · "must be reclassified" ·
  "overturn(ed|s) the" · "we adopt" · "is adopted here"
  => ZERO HITS. No sentence in the target performs any of the four voiding acts.
```

THE THREE CANDIDATES THAT HAD TO BE ADJUDICATED, NOT COUNTED:

```text
(i) §1.0's "classification vocabulary" — does naming DG/PR/AS PROPOSE DEFINITIONS?
    ADJUDICATED: NO. Each of the three is a LABEL FOR A SEALED SENTENCE, and all
    three anchors were verified verbatim at bytes in §2 above. The target's own CH-1
    grades the extension FORCED in substance because the commission's class list
    ends in "OTHER (name it)". A label attached to an already-sealed sentence is a
    display of that sentence, not a new definition of forcing. The target adds
    nothing to the record's stock of predicates; it names what is already there.
(ii) §3.2's naming of the search target — "a predicate of the form 'invariant across
    every lawful generation/production of a record'". ADJUDICATED: NO. The sentence
    that carries it says so in its own words: "named here as the search target, NOT
    proposed and NOT adopted", and §4.4 repeats the refusal: "no generative notion
    is defined, proposed, adopted, or applied to any row". A search target that is
    then displayed ABSENT is not a definition entering the record.
(iii) §2.4's cross-application — does saying what the other half's test "would and
    would not reach" RECLASSIFY? ADJUDICATED: NO, and the target guards it at every
    entry: "DISPLAYED, NOT RECLASSIFIED — R-1 stands as sealed"; "Under the require
    half's own notion their status would be UNDETERMINED-BY-THIS-TEST, not ALLOW.
    DISPLAYED, NOT RECLASSIFIED"; and for A-11, "Both readings are displayed;
    neither is adopted." The modal is counterfactual throughout.
```

VERDICTS OF CONSUMED ARTIFACTS, CHECKED FOR MOVEMENT. I compared every consumed verdict as the
target reports it against the artifact's own verdict line:

```text
T16SR audit (I) CwC / (II) CONFIRMED / (III) CwC   — reported accurately (§A6, verified).
T14SR meta-verdict JUNCTION-CONTINGENT             — reported accurately (its §5.3, verified).
T14SR RIGID-AT-J-6 with named residue              — reported accurately (its §5.1, verified).
AJ VACANCY-CLOSED, magnitude ALLOW-SIDE BY THEOREM — reported accurately (5d lines 160/408/538).
5d verdict FREE                                    — reported accurately; not moved.
NOT ONE consumed verdict is restated, upgraded, downgraded, or moved anywhere in the target.
```

HUNT D DISPOSITION: **THE DETERMINATION-ONLY FENCE HOLDS.** No reclassification, no overturning,
no definition proposed, none adopted. The artifact is not voided.

---

## §5 — HUNT E: IS THE VULNERABILITY DISPLAY INFLATED?

Standard: a row called vulnerable must actually rest on a TWO-MODEL WITNESS AT BYTES, and the
absence of the model-class/producible-class coincidence must be SHOWN, not assumed.

### §5.1 The named rows, checked at bytes. NOT INFLATED.

```text
A-1, at STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md line 159, verbatim:
  "TWO-MODEL WITNESS, executed: f₁ = (2 − w − conj w)/4 = sin²(Δ/2) and f₂ = f₁²
   satisfy every displayed clause simultaneously (AJ CAS M8/M9) and differ as
   functions (M3, exact residual 1/4)"
  => A GENUINE TWO-MODEL WITNESS AT BYTES. Both members re-derived by this audit in
     a fresh venv (§8/O4, O5): f₁ reduces to sin²(Δ/2) exactly and the pair's exact
     separation residual is confirmed. VERIFIED.
A-2, same artifact line 160, verbatim:
  "TWO-MODEL WITNESS of record: both extents are lawful over the displayed clauses"
   … "UNDECIDABLE-TODAY + positive two-model certificate" … twice-determined (T1SR)
  => A GENUINE TWO-MODEL WITNESS AT BYTES. VERIFIED.
NO THIRD ROW IS CALLED VULNERABLE. A-3 and A-8 are displayed as the DEGENERATE form
  and are explicitly separated from the two witness rows — which the survey itself
  does, at its line 175-177: A-3/A-8 are among the rows for which "model freedom is
  textual (no clause to differ on)". The target does not smuggle them into the
  vulnerable set. VERIFIED.
THE RIDING LIST, each checked at its own source:
  AJ Theorem II / VACANCY-CLOSED           rides A-1's pair — AJ lines 245-263. YES.
  Partition theorem (II) and (III)(b)      rides the same pair — T16SR 153-157. YES.
  Survey RIGID-AT-J-6                      its §5.1 argues from Theorem II. YES.
  Survey META-VERDICT JUNCTION-CONTINGENT  its §5.2/§5.3 rest on the E-1/E-2
                                           meta-witness. YES.
  5d magnitude leg ALLOW-SIDE BY THEOREM   5d lines 160/408/538. YES.
EVERY ITEM ON THE RIDING LIST GENUINELY RIDES A TWO-MODEL WITNESS. NOT INFLATED.
```

### §5.2 Is the coincidence's absence SHOWN or ASSUMED? SHOWN — and independently re-shown here.

```text
SHOWN (1) BY SWEEP: SWEEP D re-run uncapped over both roots returns ZERO, and MY OWN
  wider pattern D' returns only non-coincidence lines (§3.5). The absence is not
  taken on the target's word; it is re-established here.
SHOWN (2) BY THE RECORD'S OWN DISCIPLINE, verified verbatim at AJ lines 636-638:
  "(b) never asserted as surface content, candidates, or physics; … (d) constructed
   honoring the ground's own fiats (no log, no metric, no scale) precisely so that
   their admissibility as models is unimpeachable."
  and at the battery's own comment bytes, AJ lines 422-423:
  "# TOY MODELS ONLY. f1, f2 are meta-level counterexample profiles about
   DERIVABILITY; neither is asserted as surface content anywhere."
  BOTH QUOTED ACCURATELY BY THE TARGET. The separation the target displays is drawn
  by the AJ itself, in the direction the target says. VERIFIED.
```

### §5.3 Where the display IS over-framed (F-A5)

The target's §4.1 frames the gap as the step from "not entailed by Σ" to **"the OBJECT is free"**.
No consumed artifact takes that step, and the junction survey blocks it in its own words — a
sentence the target never displays anywhere. At `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md`
lines 154–155:

> "'Permission of record' = a quantity the sealed surface leaves unfixed, displayed as such at
>  bytes; per §1, never a licence from the allow/require cell constraint."

The allow side is thus typed RECORD-RELATIVELY at its own source: it says what the sealed surface
leaves unfixed, not what the object permits. The exposure the target displays is therefore
narrower than §4.1's framing: it is an exposure of what a HYPOTHETICAL generative notion would
reach, against rows that never claimed object-freedom. The target's own §4.4 and §6(7) get the
grade right — "The exposure displayed is a property of the WITNESS TYPE, not a defect found in
any row" — but §4.1's framing sentence asserts a step the record does not take, and omits the one
sentence in the consumed ground that types the allow side against it. CORRECTION — F-A5.

HUNT E DISPOSITION: **THE VULNERABILITY DISPLAY IS NOT INFLATED IN ITS ROWS OR ITS RIDING LIST —
both were verified at bytes — AND THE ABSENCE IS SHOWN, NOT ASSUMED. It is over-framed in its
STEP (F-A5).**

---

## §6 — HUNT F: THE IMPORT AUDIT'S OWN CALLS, VERIFIED

Each of the target's import calls put to the sources. Nothing taken on report.

### §6.1 The four IMPORTED items — ALL FOUR CONFIRMED AT BYTES

```text
CALL 1  "model of Σ" = Tarskian structure/satisfaction.
  T16SR line 137-139, verbatim: "a MODEL OF Σ is a structure interpreting the
  displayed ground vocabulary (§1's symbol inventory) and satisfying every clause
  Σ1–Σ7; junction/response quantities enter as EXPANSIONS by new symbols."
  Structure · interpretation · satisfaction · expansion. CONFIRMED.
CALL 2  "derivability implies truth in every model" = the SOUNDNESS THEOREM, asserted.
  AJ line 248 (inside Theorem II's proof) and T16SR S5 lines 191-194, both verbatim,
  the latter self-labelled "[soundness — the AJ's displayed logical step]". Asserted,
  not proved and not booked to any sealed derivation. CONFIRMED.
CALL 3  the coincidence/relevance lemma. T16SR S8 lines 211-212, verbatim: "The
  satisfaction of a clause depends only on the interpretation of the clause's own
  vocabulary." Self-labelled "[invariance lemma — displayed logical step]". CONFIRMED.
CALL 4  "expansion" / "reduct" used by name. "EXPANSIONS by new symbols" (T16SR 139);
  "the junction-free reduct" (T16SR 154). CONFIRMED.
```

### §6.2 The four EVIDENCE calls — three confirmed, one truncated (F-A3)

```text
EVIDENCE 1  "THE NOTION IS USED BEFORE IT IS DEFINED."
  I counted "model" in the AJ: 25 occurrences. I then ran every definitional pattern
  (a model (of|is) …:=/means/is defined/is a structure · MODEL NOTION · define[sd]?
  (a )?model · by a model we mean) over the AJ: ZERO HITS. The AJ uses the notion 25
  times and defines it nowhere. CONFIRMED — and this is the load-bearing evidence.
EVIDENCE 2  "THE ONLY DEFINITION IN THE CORPUS IS ONE DAY OLD."
  SWEEP C re-run uncapped over BOTH roots: 5 lines, ALL inside T16SR (2) and its
  audit (3). The AJ is dated 2026-08-14, T16SR 2026-08-15. The definition's own
  parenthesis reads "(the AJ's own, made explicit)" — a reconstruction of prior use,
  in its own words. CONFIRMED.
EVIDENCE 3  "VERBATIM STANDARD TARSKIAN SEMANTICS." Confirmed under CALL 1/3/4 above,
  plus my own check that NO COMPLETENESS STEP is taken: grep for completeness and for
  "true in every model … therefore derivable" over T16SR and the AJ returns only "the
  COMPLETE displayed equation set" (an enumeration claim, T16SR 388) and "complete
  list" (AJ 645). The target's "no completeness step is taken anywhere" is CONFIRMED,
  and it is a point in the record's favour, correctly displayed as such.
EVIDENCE 4  "THE CHOICE LEDGER GRADES THE IMPORT BY CITING ITS OWN PRIOR USE."
  PARTIALLY CONFIRMED. T16SR CH-1 at its own bytes (lines 490-496) reads IN FULL:
    "CH-1  MODEL NOTION (structures interpreting the displayed ground vocabulary,
     junction quantities as expansions): FORCED in substance — it is the AJ Theorem
     II's own proof form ("extend the displayed ground structure...") and the survey
     §5.1's quantifier; THE PROOF CONSUMES ONLY SOUNDNESS + VOCABULARY INVARIANCE,
     WHICH ANY FAITHFUL MODEL NOTION SUPPLIES."
  The target quotes only as far as "the AJ Theorem II's own proof form" and concludes
  "The justification for the notion is the artifact that first used it undefined …
  The import is INHERITED, never audited against the record's own mode of being."
  THE ELIDED CLAUSE IS A SECOND, INDEPENDENT JUSTIFICATION — a robustness claim that
  the proof does not depend on WHICH faithful model notion is used. That clause makes
  the grade non-circular, and its removal is what makes the target's "never audited"
  read as strongly as it does. The IMPORTED verdict itself is untouched by this: it
  rests on EVIDENCE 1-3, all confirmed. CORRECTION — F-A3.
  Related over-statement in the same block: "No CHOICE-LEDGER entry anywhere in the
  consumed ground classifies 'use model-theoretic entailment' as a choice." CH-1 IS
  such an entry; it classifies the notion and grades it FORCED in substance. The
  defensible claim is that no entry grades it OPEN — which is not what is written.
```

### §6.3 The four RECORD-NATIVE contrast items and the four NEAREST APPROACHES

```text
NATIVE (a) J-der — AJ 105-106, verified verbatim. CONFIRMED native; and its discharge
  by an MT argument is confirmed at AJ 245-263 (§1.3 above).
NATIVE (b) DISPLAY-GRADE — 5d 234-235, verified verbatim; and it is nowhere defined
  as a notion of forcing. I checked: the sentence states a REQUIREMENT on a FORCED
  sentence, not a definition of forcing. CONFIRMED.
NATIVE (c) the AS criterion and three shapes — verified verbatim (§2 above). CONFIRMED.
NEAREST-3, the sharpest of the four, verified at the exact line the target cites:
  AJ line 693, verbatim within the NET line: "the referent's creation cannot be a
  derivation on this ground". CONFIRMED AT THE CITED BYTE.
NEAREST-1/2/4 all trace to sentences verified elsewhere in this audit. CONFIRMED.
```

HUNT F DISPOSITION: **THE IMPORT AUDIT'S CALLS ARE SOUND. The IMPORTED verdict is CONFIRMED on
independently verified evidence. One supporting quotation is truncated in a way that strengthens
its own conclusion beyond what the source supports (F-A3).**

---

## §7 — HUNT G: LENS TOKENS; NUMBERS; FENCE-SCAN

```text
LENS TOKEN SCAN, case-insensitive, whole target: ZERO OCCURRENCES of "lens" in any
  form. (Note for completeness: the token DOES occur in a consumed source — the
  junction survey's J-4 row carries "the log's coupling bearing lens-fenced". The
  target did not carry it across. CLEAN.)
FENCE LINES: present and unmoved — line 11 declares all three false; lines 887-888
  restate "alpha_computed = false ; proof_authorized = false ; kappa_record_computed
  = false — ALL REMAIN." No fence is flipped anywhere in the artifact.
VALUE / MEASURED-CONSTANT SCAN: (we adopt|is adopted here|hereby|measured value|
  CODATA|experimental value|137|1/137|fine.structure) => ZERO HITS.
EVERY NUMBER IN THE TARGET, CLASSIFIED — none is a value of anything:
  (a) COUNTS OF ITS OWN INVENTORY: 10 require rows, 13 allow rows, 23 total, 6
      cross-gap rows, 9 forcing tests, 5/2/2/1 and 5/3/2/2/1 tallies, 9 seals, 5 CAS
      checks, 4 nearest approaches, 8 ledger entries. I re-derived every tally
      arithmetically: require 5+2+2+1 = 10 ✓; allow 2+2+5+3+1 = 13 ✓; 10+13 = 23 ✓;
      1+5 = 6 ✓. ALL CORRECT.
  (b) EXACT ALGEBRA RESIDUALS of identity checks on ground-symbolic or quarantined-toy
      objects: −2 (label 1/2), 1/4 (toy separation), 1 (the trivial amplitude G = 1,
      quoted from R-2). Re-derived independently at §8. Not physical values.
  (c) QUOTED SEALED MARKERS carried inside quotation marks from their own rows:
      "p = −3", "alpha-k in [1, 3/2]", "dim 1", "{1,2}", "b_1 >= 2", "n^2". These are
      the sources' words, displayed, not asserted by the target.
  (d) INFRASTRUCTURE: dates, section numbers, sympy 1.14.0, sha256 digests.
  NO MEASURED CONSTANT APPEARS. NO COMPARISON TO ONE APPEARS. NO VALUE IS FROZEN.
```

HUNT G DISPOSITION: **CLEAN.** No lens token, no value, no measured-constant comparison, fences
declared and held, and every count in the artifact re-derived correct.

---

## §8 — CAS RE-VERIFICATION: EVERY SYMBOLIC STEP RE-RUN (FRESH VENV, EXACT SYMBOLIC)

Environment: fresh venv created for this audit under the session scratchpad, sympy 1.14.0. No
`evalf`, no `N()`, no float literal, no decimal — mechanical scan of my script returns ZERO hits
for `evalf|\bN\(|float|[0-9]+\.[0-9]+`. Exact integers/rationals appearing (1, 1/2, 1/3, 1/4, −2)
are algebra residuals of identity checks on ground-symbolic or quarantined-toy objects, never
physical values. Script sha256
`9d50d37370d34212dea5a7cca2600ce2d3130d3497bea14920459f4fb4e7d599`. Output, verbatim:

```text
[O1] PASS -- closure solution set is EXACTLY the integers by BOTH sound forms (over C: Integers; over R via the real closure component: Integers) -- a SOLVED set, produced not quantified over
[O1b] PASS -- the NAIVE real-domain form on the complex exponential returns the sympy domain artifact {0} -- independently reproducing the repair T16SR itself discloses at its own bytes
[O2] PASS -- non-integer label 1/2 leaves the EXHIBITED exact residual -2 (a construction, not a model quantifier)
[O3] PASS -- closure holds at SYMBOLIC integer n: the clause forces the CLASS and pins NO member (require R-1 / allow A-3 boundary)
[O4] PASS -- TOY witness f1 == sin^2(Delta/2) exactly (Delta typed real, as the sealed build types it)
[O5] PASS -- TOY witnesses differ; exact separation residual 1/4 at the toy argument: |admissible set| >= 2 EXHIBITED
[X1] PASS -- the witness EXHIBITS exactly 2 members (a produced pair) whereas O1 RETURNS THE WHOLE SET: the two are different constructive acts -- lower bound on cardinality vs. the set solved
[X2] PASS -- UNTYPED Delta does NOT reduce to sin^2(Delta/2) -- the target's reported first-run FAIL is a symbol-typing artifact, reproduced here exactly; no content claim rested on it
[X3] PASS -- a second non-integer label 1/3 also fails exactly (residual -3/2 + sqrt(3)*I/2): the CA marker is not an artifact of 1/2
TOTAL: 9 checks; FAIL count = 0
```

CORRESPONDENCE TO THE TARGET'S BATTERY: my O1/O2 re-derive its O1/O2; my O3 re-derives its O5; my
O4/O5 re-derive its O3/O4. **All five of the target's checks are independently reproduced.**

TWO FORMULATION REPAIRS OF MY OWN, DISPLAYED SO THE RUN IS AUDITABLE:

```text
MY FIRST RUN returned 2 FAILs, both mine, neither a content failure:
  (1) I wrote O1 as solveset over R directly on the complex exponential; it returned
      {0}. This is the SAME sympy domain artifact T16SR discloses in its own words at
      its lines 325-329 ("the first PT3d formulation (solveset over ℝ directly on the
      complex exponential) returned a sympy domain artifact ({0}); reformulated to the
      two sound exact forms"). I reformulated identically and now RETAIN the artifact
      as check O1b, so the disclosure in the sealed build is corroborated rather than
      merely believed.
  (2) I evaluated the toy separation at Δ = π, where f₁ − f₂ = 0 (both toys equal 1
      there). The sealed residual 1/4 sits at Δ = π/2. My argument choice was wrong;
      the identity was not. Corrected.
NEITHER REPAIR TOUCHED ANY CLAIM IN THIS AUDIT.
```

THE ONE CHECK THE TARGET DID NOT RUN, AND WHY IT MATTERS (X1). The target's disjointness tally
needs a difference between "the admissible set is produced" and "two members are exhibited". X1
displays it mechanically: O1 RETURNS THE WHOLE SET (`Integers`), while the witness pair returns
exactly two objects and never produces the set they inhabit. This is the distinction that would
carry the target's §2.2 tally — and the target never states it as the tally's ground (F-A2).

---

## §9 — FINDINGS

```text
F-A1  QUOTE TRUNCATION THAT STRENGTHENS THE "TWO MODEL CLASSES" CLAIM. The target's
      §1.4/§2.3 elide, behind "…", the T16SR audit's own counter-consideration to the
      class split — "the sealed tower's own uniform reading, and arguably what §2's
      model notion already implies (χ is absent from §1's symbol inventory, hence
      fixed notation, not free vocabulary)". Restored at §1.2 above. The asymmetry
      survives (the audit still grades (I) CwC and (II) CONFIRMED); the flat "the
      halves quantify over DIFFERENT MODEL CLASSES" does not, at the source's own
      strength. GRADE: CORRECTION. Fix: restore the elided clause and state the
      finding as an asymmetry of dependence on a named interpretive premise.
F-A2  THE DISJOINTNESS IS STATED ABSOLUTELY AND IS CONTRADICTED BY THE TARGET'S OWN
      BATTERY. "no allow-side row is established constructively" (§2.2, §6(2), FLAG
      BLOCK) vs its own §5: "the two-model witness is ITSELF a construction — two
      objects exhibited". The distinction that saves the tally — whole set produced
      vs two members exhibited — is real (exhibited mechanically at §8/X1) but is
      never stated as the tally's ground. GRADE: CORRECTION. Fix: qualify the tally
      as by-principal-notion and name the produce-vs-exhibit distinction.
F-A3  IMPORT-AUDIT QUOTATION TRUNCATED AT ITS DECISIVE CLAUSE. T16SR CH-1's second,
      independent justification — "the proof consumes only soundness + vocabulary
      invariance, which any faithful model notion supplies" — is elided, which is what
      lets "INHERITED, never audited" stand as written. Also "No CHOICE-LEDGER entry
      … classifies [it] as a choice" is untrue as written: CH-1 is such an entry,
      graded FORCED. The IMPORTED verdict is untouched (EVIDENCE 1-3 independently
      confirmed). GRADE: CORRECTION.
F-A4  "NOWHERE STATED IN THE AJ" IS NOT SUSTAINABLE FOR THE FIRST STEP. J-der's own
      words name the ground as the forcing source ("every ingredient forced by the
      ground's self-consistency"), so the move from "not derivable from the ground"
      to "not forced by the ground's self-consistency" is licensed by the predicate
      itself. The genuine import is the SECOND step (soundness), which the target
      displays correctly elsewhere. GRADE: CORRECTION.
F-A5  THE VULNERABILITY IS OVER-FRAMED IN ITS STEP. §4.1 poses the gap as carrying
      "not entailed by Σ" to "the OBJECT is free"; no consumed artifact takes that
      step, and the junction survey types its allow side record-relatively in a
      sentence the target never displays — "'Permission of record' = a quantity the
      sealed surface leaves unfixed, displayed as such at bytes". The ROWS and the
      RIDING LIST are correct and were verified at bytes; the framing is not.
      GRADE: CORRECTION. Fix: display the survey's typing sentence alongside §4.1.
F-A6  SWEEP A FILE COUNT. Reported "2 files"; run uncapped over both roots as
      declared, the pattern returns 4 paths (2 artifacts, mirrored). De-duplicated
      count reported as a file count. The disposal is correct at bytes (verified by
      opening the artifact). GRADE: CORRECTION, minor.
F-A7  THE ABSENCE QUALIFIER IS DROPPED IN THE VERDICT LINE. §0.1 and §3.2 correctly
      say "ZERO hits in NON-FENCED files"; §6(6) says "ZERO". The only sweep-E hit in
      either root sits in a fenced-class file that neither the build nor this audit
      may open. This is exactly the absent-vs-unlocated distinction. The target
      handles it correctly where it declares it and loses it where it concludes.
      GRADE: CORRECTION. Fix: carry "in non-fenced files" into §6(6).

NOTHING FOUND AT REFUTATION GRADE. No claimed mismatch dissolves; no vulnerable row
  fails its two-model test; no claimed absence is an unlocated source; no fence is
  breached; no row is reclassified and no verdict is moved.
ALL SEVEN FINDINGS ARE CORRECTIONS OF STRENGTH-OF-STATEMENT OR OF QUOTATION SCOPE.
  Five of the seven (F-A1, F-A2, F-A3, F-A4, F-A5) share ONE pattern, named here
  because the pattern is the finding: AT EVERY SEAM WHERE THE SOURCE HEDGES ITS OWN
  CLAIM, THE TARGET STATES THE CLAIM UNHEDGED. In three cases the hedge is inside the
  very sentence quoted, removed by an ellipsis. This is the signature of a build with
  an incentive to find a mismatch — which is what this commission was told to hunt.
  It did not manufacture one; it over-stated the one it found.
```

---

## §10 — VERDICT, PER DELIVERABLE AND OVERALL

```text
DELIVERABLE 1 — THE NOTION INVENTORY (target §1)
  = CONFIRMED. All 23 row quotations verified verbatim against the junction survey's
    own §2/§3 tables; the three sealed require-shapes, the partition theorem's three
    claims, the vacancy theorem and J-der, and the nine forcing tests all verified at
    their own bytes; all three OTHER labels anchored to distinct sealed sentences;
    every tally re-derived arithmetically correct.

DELIVERABLE 2 — THE CONSISTENCY TEST (target §2)
  = CONFIRMED-WITH-CORRECTIONS (F-A1, F-A2, F-A4). The two-notion mismatch is REAL:
    its two operative sentences are quoted at bytes and their tests are shown
    non-equivalent in both directions on the six rows they separate. The DISJOINTNESS
    is stated more absolutely than the target's own battery supports (F-A2), and the
    partition theorem's class split is stated more flatly than its source (F-A1).

DELIVERABLE 3 — THE IMPORT AUDIT (target §3, §7)
  = CONFIRMED-WITH-CORRECTIONS (F-A3, F-A6, F-A7). IMPORTED is independently
    established: the AJ uses the notion 25 times and defines it zero times; the only
    definition in either root is one day later and self-described as a reconstruction;
    soundness and the invariance lemma are asserted, not booked; no completeness step
    is taken anywhere. The generative-notion absence is a GENUINE ABSENCE, re-verified
    uncapped over both roots with my own independently-worded patterns, with the
    unlocated-source risk closed by an extension census. One supporting quotation is
    truncated at its decisive clause (F-A3).

DELIVERABLE 4 — THE VULNERABILITY DISPLAY (target §4)
  = CONFIRMED-WITH-CORRECTIONS (F-A5). Both named rows rest on genuine two-model
    witnesses at bytes; both witnesses re-derived here in a fresh venv; every item on
    the riding list genuinely rides one; the coincidence's absence is SHOWN by sweep
    and by the AJ's own quarantine text, both re-verified independently. The framing
    step ("the OBJECT is free") is not taken by any consumed artifact and the survey's
    own record-relative typing is never displayed.

FENCES AND DISCIPLINE
  = CONFIRMED. Determination-only held (no reclassification, no overturning, no
    definition proposed or adopted — the three candidate acts adjudicated individually
    at §4); all three fences declared and unmoved; no lens token; no value; no
    measured-constant comparison; toy separation clean; sweep cutoff declared; the
    fence event honestly displayed and independently confirmed.

CAS
  = CONFIRMED. All five of the target's checks independently reproduced, 9/9 PASS in a
    fresh venv, exact symbolic, zero numerics. The target's disclosed formulation
    repair is corroborated; two repairs of my own are displayed above.

OVERALL VERDICT = **CONFIRMED-WITH-CORRECTIONS**
  The headline — TWO-NOTION-MISMATCH ∧ IMPORTED-PREDICATE — STANDS at bytes. The
  mismatch is not manufactured: it survives the two-sentences-plus-non-equivalence
  standard on the rows in question. The import is not asserted: it is established on
  evidence re-derived here. The corrections are seven, all of strength-of-statement or
  quotation scope, five of them one pattern: the target states unhedged what its
  sources hedge, three times by an ellipsis that removes the hedge from the quoted
  sentence itself. None of the seven moves any verdict, any row, or any fence.
```

## §11 — CHOICE LEDGER (this audit's own; every unforced choice, classified)

```text
CH-1  RE-RUNNING THE TARGET'S SWEEPS WITH MY OWN INDEPENDENTLY-WORDED PATTERNS (A', D')
      IN ADDITION TO ITS OWN: FORCED by DEFAULT-REFUTE — a sweep verified only against
      its author's wording proves the wording, not the absence.
CH-2  RETAINING THE REPRODUCED SYMPY DOMAIN ARTIFACT AS CHECK O1b RATHER THAN DELETING
      IT: FORCED — it converts T16SR's disclosed repair from testimony into a
      corroborated fact at my own hands.
CH-3  ADDING THREE CHECKS THE TARGET DID NOT RUN (X1, X2, X3): FORCED in substance —
      X1 tests the exact distinction its disjointness tally needs, X2 tests its own
      disclosed repair, X3 tests whether the CA marker is an artifact of one label.
CH-4  GRADING F-A1..F-A7 AS CORRECTIONS RATHER THAN REFUTATIONS: FORCED by the bytes —
      in every case the target's underlying claim survives verification and only its
      strength or quotation scope exceeds the source. No finding reaches a claim that
      is false at bytes.
CH-5  READING "propose a definition" AS EXCLUDING LABELS ATTACHED TO ALREADY-SEALED
      SENTENCES: PREMISE(named) — adjudicated at §4(i) against the three anchors,
      each verified verbatim. Under the opposite reading the target would be voided;
      I record the reading rather than leaving the adjudication silent.
CH-6  NOT OPENING THE FENCED FILE THAT CARRIES THE ONLY SWEEP-E HIT: FORCED by the
      commission's fence. CONSEQUENCE RECORDED, NOT HIDDEN: the generative-notion
      absence is therefore an absence among readable files with one unread candidate,
      which is exactly what F-A7 says must be carried into the verdict line.
CH-7  DECLARING THE ARITHMETIC RE-DERIVATION OF EVERY TALLY (§7(a)) RATHER THAN
      SPOT-CHECKING: FORCED — a notion inventory whose counts do not add is a
      different finding than one whose counts do.
ZERO entries in class OPEN.
```

## §12 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The only toy objects touched by this audit are the AJ's own quarantined model
  profiles f₁, f₂, re-executed at O4/O5/X1/X2 EXACTLY as the sealed build quarantines
  them, and quoted in §5.2 in their quarantine's own words. They enter here ONLY as
  (i) the objects row A-1's witness consists of, whose two-model character had to be
  verified at bytes rather than believed, and (ii) the objects whose declared status
  ("admissibility as models is unimpeachable"; "never asserted as surface content,
  candidates, or physics") is the §5.2 display. No toy value, profile, or model is
  asserted as surface content, candidate, or physics; none leaks into any finding,
  grade, verdict clause, or flag. Every load-bearing object in §1-§7 is a sealed
  artifact's own operative sentence read at a verified seal, a sweep result re-run at
  my own hands, or an exact-algebra identity re-derived in a fresh venv.
  NO TOY NOTION: this audit names no notion of its own and adopts none.
```

## §13 — IMPORT AUDIT (this audit's own calls, so the audit is two-sided)

```text
IMPORTED BY THIS AUDIT, NAMED:
  1. Nothing from mathematical logic is used as a premise here. The audit's own
     reasoning consumes only: (a) byte comparison of quotations against sources,
     (b) arithmetic on the target's own tallies, (c) exact symbolic identity checks,
     (d) regex sweeps whose patterns are declared in full at §0.1.
  2. The soundness sentence and the invariance lemma are DISPLAYED as the corpus's
     imports (§6.1) and are nowhere USED by this audit to establish anything.
RECORD-NATIVE, USED:
  a. seal verification (shasum -c from the artifact's own directory) — the record's
     own instrument, run 10 times this commission (target + 9 consumed), 10/10 OK.
  b. the fenced-file class — the record's own discipline, honoured; one consequence
     recorded at CH-6 rather than absorbed.
NOT USED ANYWHERE: any notion of forcing. This audit classifies no row and defines
  no predicate; it checks whether the target's classifications match their sources.
```

## §14 — FLAG BLOCK

```text
COMMISSION = O12SR (FORCING-NOTION-AUDIT; DEFAULT-REFUTE; testimony zero weight;
  re-derivation at bytes of the forcing-notion determination).
SEALS = 10/10 MATCH. Target seal verified from the artifact's own directory
  (839f5079…e207bb, recomputed independently); all NINE artifacts the target consumed
  re-verified by this audit from the same directory. MISMATCHES = 0.
HUNT_A MANUFACTURED MISMATCH = NOT FOUND. Three mismatches claimed; each put to the
  two-operative-sentences-plus-non-equivalence standard with both sentences quoted at
  their own bytes. NONE DISSOLVES. Mismatch 1 (require CA/PT vs allow DG/PR) survives
  outright, with non-equivalence displayed in both directions on the six rows it
  separates. Mismatches 2 and 3 survive in substance but are over-stated (F-A1, F-A4).
HUNT_B COMPLACENT UNIFORMITY = NOT FOUND, and its mirror (inventory padding) tested
  too: all 23 row quotations verbatim from their own rows; DG/PR/AS anchored to three
  DIFFERENT sealed sentences with three different subjects (an artifact, an
  ingredient, a set), each verified verbatim.
HUNT_C GENERATIVE-NOTION SWEEP = GENUINELY UNCAPPED, BOTH ROOTS, ALL FILE TYPES,
  RE-RUN AT MY OWN HANDS with the target's patterns AND my own independently-worded
  ones. The single sweep-A hit was disposed by OPENING the artifact at its line, not
  by report: it is an authorship-price schedule per construction family, not a forcing
  predicate. The coincidence absence is GENUINE (D and my wider D' both). The
  unlocated-source risk is CLOSED by an extension census of both roots: no prose
  artifact lies outside the sweep's reach. FENCE EVENT independently confirmed —
  exactly one sweep-E hit in either root, in a fenced REGISTER file; NOT OPENED BY
  THIS AUDIT EITHER, line not quoted. Qualifier dropped in the target's verdict line
  (F-A7).
HUNT_D DETERMINATION-ONLY = HELD. Mechanical scan for the four voiding acts: ZERO
  hits. Three candidate acts adjudicated individually (the OTHER labels; the named
  search target; the cross-application) — none is a reclassification, an overturning,
  or a proposed or adopted definition. Every consumed verdict re-read at its own
  source and compared: NOT ONE IS MOVED. THE ARTIFACT IS NOT VOIDED.
HUNT_E VULNERABILITY DISPLAY = NOT INFLATED IN ITS ROWS. A-1 and A-2 verified as
  genuine two-model witnesses at bytes; both witness profiles re-derived in a fresh
  venv; every item on the riding list verified to ride one. The model-class /
  producible-record coincidence is SHOWN absent, not assumed — by uncapped sweep at my
  own hands and by the AJ's own quarantine text quoted verbatim. Over-framed in its
  STEP (F-A5): the survey's own record-relative typing of "permission of record" is
  never displayed by the target.
HUNT_F IMPORT AUDIT'S OWN CALLS = VERIFIED. All four imported items confirmed at
  bytes. EVIDENCE 1 re-derived independently (25 uses of "model" in the AJ, ZERO
  definitions). EVIDENCE 2 re-derived (sweep C: 5 lines, all in T16SR + audit).
  EVIDENCE 3 confirmed, including the no-completeness-step check. EVIDENCE 4
  truncated at its decisive clause (F-A3).
HUNT_G LENS/NUMBER = CLEAN. Zero lens tokens (the token exists in a consumed source
  and was not carried across). Zero value/measured-constant hits. Every number in the
  target classified as an inventory count, an exact algebra residual, a quoted sealed
  marker, or infrastructure; every tally re-derived arithmetically correct
  (5+2+2+1=10; 2+2+5+3+1=13; 10+13=23; 1+5=6).
CAS = fresh venv, sympy 1.14.0, 9/9 PASS, exact symbolic, zero numerics (scan clean).
  All five of the target's checks independently reproduced. Its disclosed formulation
  repair corroborated by reproducing the same sympy domain artifact. Two repairs of my
  own displayed. Script sha256 9d50d373…e7d599.
FINDINGS = F-A1..F-A7, ALL CORRECTION-GRADE, NONE VERDICT-BEARING. Five share one
  pattern: the target states unhedged what its sources hedge, three times by an
  ellipsis that removes the hedge from the quoted sentence itself.
VERDICT = CONFIRMED-WITH-CORRECTIONS, per deliverable and overall (§10).
  D1 CONFIRMED · D2 CwC · D3 CwC · D4 CwC · fences CONFIRMED · CAS CONFIRMED.
SWEEP_CUTOFF = DECLARED (§0.1): the nine consumed artifacts at re-verified seals read
  at their operative sections, plus sweeps A/A'/C/D/D'/E uncapped over both roots and
  all file types, plus the extension census, plus one scoped read of the AXN artifact
  at the sweep-A hit line. NOTHING ELSE OPENED. No register/tracker/road/plan/
  continuation file read. "Q-..." EXPECTED-UNLOCATABLE, none resolved. No git.
CHOICE_LEDGER = CH-1..CH-7 (§11): 6 FORCED/FORCED-in-substance, 1 PREMISE(named);
  ZERO OPEN. TOY_SEPARATION = clean (§12). IMPORT AUDIT = §13, two-sided.
DETERMINATION_ONLY = held by this audit too: no row reclassified, no verdict
  overturned, no definition proposed, none adopted; no value, no number as a value of
  anything, no measured-constant comparison; no gate, flag, witness, or status of any
  artifact moved; nothing here discharges any named absence.
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT before first
  write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = The determination survives a default-refute re-derivation. Its headline claim is
  the one a build had every incentive to manufacture, and it is not manufactured: the
  require half's tests are passed by solving equations, the allow half's majority tests
  are passed by displays existing in sealed artifacts, and neither test entails the
  other in either direction on the six rows where the partition's line crosses between
  them. The imported predicate is imported, on evidence re-derived here rather than
  believed. The generative alternative is absent from both roots, and the absence is an
  absence and not an unlocated source — with the single honest qualification that the
  only line matching its shape anywhere sits inside a file class no one on this
  commission may open. What the audit corrects is not the finding but its pitch: at
  five seams the target states flatly what its own sources state with a hedge, and
  three of those hedges were removed by an ellipsis from inside the quoted sentence.
  Restore the five hedges and the determination reads as what the bytes support.
```
