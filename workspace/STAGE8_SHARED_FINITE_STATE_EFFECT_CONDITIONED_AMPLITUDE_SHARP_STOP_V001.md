# Stage 8 Shared Finite State/Effect-Conditioned Amplitude Sharp Stop v001

Date: 2026-08-01  
Lane: CODEX LANE 2  
Task: Task 2d  
Register head consulted: Q-235  
Standing: `TYPE-P | premises: DoR-008, DoR-009` wherever the ratified carrier
or source-coupled transition law is consumed

## 0. Lead determination

**The shared finite amplitude is not canonical from the named ingredients.**
The sealed ready-cell product vector has already contracted the record carrier,
and the built inclusive identity fixes the effect to the identity, but neither
fixes the normalized weight of the neutral and charged source sectors left in
`F_N`.

The construction therefore stops sharply at the exhaustive one-parameter
response-visible family

```text
p_ch := omega(P_ch),                    0 <= p_ch <= 1,
omega(P_0)=1-p_ch,

A_N^(p_ch)[a_+,a_-]
  := omega(F_N[a_+,a_-])
   = (1-p_ch) + p_ch Z_N[a_+,a_-],

Z_N[a_+,a_-]
  := product_(j=1)^N conjugate(z_j[a_(-,j)]) z_j[a_(+,j)].
```

This family is derived given DoR-008 and DoR-009. No member is selected. The
missing datum is the **charged-sector occupation of the concrete initial source
state**, equivalently the restriction of the physical `rho_pre`/StatePort to
the central two-sector algebra generated by `P_0,P_ch`.

```text
CONDITIONAL_FINITE_AMPLITUDE_FAMILY_CONSTRUCTED = true | TYPE-P |
  premises: DoR-008, DoR-009

RESPONSE_VISIBLE_FREE_PARAMETER = p_ch in [0,1]
RESPONSE_VISIBLE_PARAMETER_FAMILY_CARDINALITY = CONTINUUM

CANONICAL_SHARED_FINITE_AMPLITUDE_FROM_NAMED_INGREDIENTS = false | TYPE-R |
  test: p_ch=0 and p_ch=1 are normalized positive source-sector states,
        both use the same sealed ready record and inclusive identity, and give
        different amplitudes whenever Z_N!=1

PHYSICAL_P_CH_SELECTED = false | TYPE-U |
  would-build: one concrete normalized positive source state, its charged
               marginal p_ch, and its common-origin descent certificate

PHYSICAL_FINITE_AMPLITUDE_INSTANCE_BUILT = false | TYPE-U |
  would-build: the preceding StatePort datum applied to the derived family
```

This is a one-parameter stop at the finite amplitude interface. It is not a
claim that the complete physical StatePort has only one free parameter: the
completed field/CTP scalarization, density, domains, and provenance remain
separate missing data.

No second variation, response kernel, `B_ind`, stiffness, coupling, scale, or
root is constructed or evaluated.

## 1. Preflight, scope, and authorities

### 1.1 Preflight

```text
DOES_THE_NEED_EXIST = true
  Q-235 certifies the shared scalar-amplitude need for U2, Task 3a, and Task 3c

IS_THE_VERSION_CURRENT = true
  Q-234 and Q-235 are the live finite-N handoff lineage consulted

ARE_THE_NAMED_INPUTS_PRESENT = true
  the finite ready record, F_N, and inclusive module identity are instances

DO_THE_NAMED_INPUTS_SELECT_A_SOURCE_STATE = false | TYPE-R
  test: their exact domains/codomains leave P_0 and P_ch unsandwiched
```

### 1.2 Authorities used

| Authority | Exact content used |
|---|---|
| `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md:142-157` | source projectors, ratified `U_N`, ready record |
| same, `:169-218` | record contraction and exact `F_N=P_0+Z_NP_ch` |
| same, `:221-282` | explicit `N=1,2` operator functionals |
| same, `:552-569` | source scalarization is not supplied by the record sandwich |
| `STAGE8_RATIFIED_FINITE_N_INFLUENCE_FUNCTIONAL_FOUR_CONSUMER_HANDOFF_RECEIPTS_V001.md:116-153` | `omega(F_N)` depends on sector weights |
| same, `:324-368` | Task 3c requires a complete normalized scalar amplitude; step 1 is not reopened |
| `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md:148-228` | physical `rho_pre` and missing scalar state realization |
| same, `:254-310` | exact StatePort and built inclusive module identity |
| `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md:173-213` | exhaustive normalized positive-functional family has continuum size |
| same, `:305-360` | physical `rho_pre` identity/common-origin descent remain unbuilt |
| same, `:421-444` | inclusive identity enforces normalization but does not select a state |
| `STAGE8_TASK2D_STATE_CLASS_STRATUM_FORCING_PROTOCOL_RESULT_V001.md:561-578` | every charge-superselected source state and ready-cell product state is admitted; the parent does not select the physical in-state |
| `STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md:155-202` | exact controlled write, arbitrary charge-superselected `rho_S`, and canonical finite record trace |

The local cleanroom root was entered. `alpha_supervision/LOCKED_PROCESS.md` was
read from the archive supervision root. The archive workspace copies were used
only to confirm custody lineage where necessary. The private custodian root was
not entered.

### 1.3 Queries actually used

Case-sensitive exact-symbol and case-insensitive prose checks were made for:

```text
F_N | P_0 | P_ch | ready | product state | charge-superselected
inclusive identity | StatePort | EffectPort | scalarization | rho_pre
omega(F_N) | continuum | one-cell | step 1 reopened
```

No corpus-wide absence claim is made from this authority-bounded construction.

## 2. Type audit: what the named ingredients actually contract

### 2.1 The ready state contracts the record leg only

The exact ratified transition is

```text
U_N[a]
  = P_0 tensor I_(3^N) + P_ch tensor W_N[a],

|R_N> = |r>^(tensor N).
```

The sealed record matrix element gives

```text
<R_N|U_N[a_-]^dagger U_N[a_+]|R_N>
  = P_0 + Z_N[a_+,a_-]P_ch
  = F_N[a_+,a_-].
```

Thus `|R_N>` is already consumed. It is a product state on the record cells;
it is not a normalized functional on the remaining source-sector algebra.
The exact source carrier after the sandwich is

```text
P_0 H_src direct-sum P_ch H_src.
```

```text
READY_RECORD_STATE_IS_SOURCE_SECTOR_STATE = false | TYPE-R |
  test: |R_N> lies in the record factor and its sandwich leaves P_0,P_ch

READY_RECORD_SANDWICH_ALREADY_EXECUTED = true | TYPE-P |
  premises: DoR-008, DoR-009
```

### 2.2 The inclusive identity fixes the effect, not the state

The built U2 effect is

```text
I_inc_008 := identity in L_B(E_C0).
```

It descends to the identity under every later lawful scalar realization. Hence
it supplies a choice-free **inclusive effect**. But it supplies no positive
functional `B->C`, no scalar Hilbert realization, no density, and no trace.
Its action in the finite source contraction is therefore only

```text
omega(I_inc F_N)=omega(F_N).
```

```text
INCLUSIVE_IDENTITY_EFFECT_FIXED = true | TYPE-P | premises: DoR-008

INCLUSIVE_IDENTITY_SUPPLIES_SOURCE_SCALARIZATION = false | TYPE-R |
  test: an identity endomorphism is not a normalized positive functional

INCLUSIVE_IDENTITY_SELECTS_P_CH = false | TYPE-R |
  test: every normalized positive source-sector state has omega(I)=1
```

### 2.3 Exhaustive response-visible source-state quotient

Restrict any normalized positive source state `omega` to

```text
C_src := span_C{P_0,P_ch},
P_0 P_ch=0,
P_0+P_ch=I_src.
```

Positivity and normalization imply

```text
omega(P_0)>=0,
omega(P_ch)>=0,
omega(P_0)+omega(P_ch)=1.
```

Set `p_ch=omega(P_ch)`. Then `p_ch in [0,1]`. Conversely, for each
`p in [0,1]`,

```text
omega_p(x_0 P_0+x_ch P_ch):=(1-p)x_0+p x_ch
```

is a normalized positive functional. Therefore `[0,1]` is not merely a lower
bound; it is the exact quotient of normalized source states visible to the
diagonal operator `F_N`. Any further degrees of a full source state are
invisible to this particular amplitude.

The sealed charge-superselection condition removes neutral/charged coherence;
it does not select their relative occupation. Even if coherence were admitted
hypothetically, it would not enter this diagonal `F_N`; no such hypothetical
extension is used as an asserted premise.

```text
RESPONSE_VISIBLE_SOURCE_STATE_QUOTIENT = [0,1]
RESPONSE_VISIBLE_QUOTIENT_COVERAGE_PROVED = true
SOURCE_SUPERSELECTION_SELECTS_A_WEIGHT = false | TYPE-R |
  test: all p in [0,1] define charge-superselected normalized states
```

## 3. Exact construction at N=1 and N=2

### 3.1 One cell

Define the exact relative holonomy character

```text
q_1 := conjugate(z_-)z_+.
```

From the sealed operator

```text
F_1[a_+,a_-]=P_0+q_1 P_ch,
```

the inclusive-effect contraction with an arbitrary normalized source-sector
state is

```text
A_1^(p)[a_+,a_-]
  = omega_p(I_inc F_1)
  = omega_p(P_0)+q_1 omega_p(P_ch)
  = (1-p)+p conjugate(z_-)z_+.
```

Every equality is exact. No holonomy value is assigned.

### 3.2 Two cells

Define

```text
q_1 := conjugate(z_(-,1))z_(+,1),
q_2 := conjugate(z_(-,2))z_(+,2).
```

The sealed operator is

```text
F_2[a_+,a_-]=P_0+q_1 q_2 P_ch.
```

Therefore

```text
A_2^(p)[a_+,a_-]
  = omega_p(I_inc F_2)
  = (1-p)+p q_1 q_2
  = (1-p)
    +p conjugate(z_(-,1))z_(+,1)
       conjugate(z_(-,2))z_(+,2).
```

Again this is an exact conditional family, not a selected physical member.

### 3.3 Arbitrary finite N

The same calculation gives

```text
A_N^(p)[a_+,a_-]
  = (1-p)
    +p product_(j=1)^N conjugate(z_j[a_(-,j)])z_j[a_(+,j)].
```

Its squared modulus can be exposed without spectral or response evaluation:

```text
|A_N^(p)|^2
  = 1-2p(1-p)(1-Re Z_N).
```

This identity shows why taking `|F_N|=I` is not a substitute for the scalar
contraction: for interior `p`, interference depends on the relative history.

## 4. Canonicity attack and alternative-contraction ledger

### 4.1 Initial sector weighting — genuine continuum choice

The endpoints

```text
p=0: A_N=1,
p=1: A_N=Z_N
```

use exactly the same `F_N`, ready record, inclusive effect, CTP order, and
ratified law. For any admitted unequal history with `Z_N!=1`, the two
amplitudes differ. Every intermediate `p` is also normalized and positive.

No sealed constraint consulted here cuts the interval:

| Constraint | Result |
|---|---|
| positivity | every `p in [0,1]` passes |
| normalization | every `p` has `omega_p(I)=1` |
| charge superselection | every `omega_p` is sector diagonal |
| U1 branch/reality conventions | act on histories/order, not the occupation weight |
| inclusive identity | every `p` sees the same identity effect |
| one-cell algebraic restriction | exact for every `p`; no physical one-cell marginal is sealed |

```text
INITIAL_SOURCE_SECTOR_WEIGHTING_IS_FIXED = false | TYPE-U |
  would-build: a concrete source marginal of rho_pre with common-origin
               provenance

FINITE_CHECK_BATTERY_SELECTS_ONE_P = false | TYPE-R |
  test: the complete interval passes all executable checks
```

### 4.2 Non-inclusive effects — genuine interface, no competing instance

The inclusive effect is the only effect instance currently built. A
non-inclusive `E_r` would alter the contraction and requires its own label,
domain, completeness, and common-origin certificate. The U2 EffectPort
specifies those fields but does not instantiate them.

For clarity only, the following line is **HYPOTHETICAL_NOT_ASSERTED**: if a
future sector-diagonal effect were `E=e_0P_0+e_chP_ch`, the normalized
contraction would depend on `e_0,e_ch` as well as `p`. This line is not used in
the family or verdict.

```text
NONINCLUSIVE_EFFECT_INSTANCE_AVAILABLE = false | TYPE-U |
  would-build: one admitted nontrivial E_r with domain, completeness, and
               common-origin certificates

NONINCLUSIVE_EFFECT_FAMILY_CARDINALITY = NO_VERDICT
  reason: current authority contains an interface and census rows, not an
          instantiated covered family

NONINCLUSIVE_EFFECT_SILENTLY_INSERTED_HERE = false | TYPE-S |
  scope: equations in Sections 2-3 use I_inc only
```

Thus fixing the effect to `I_inc` is canonical within this task. It does not
make the state contraction canonical.

### 4.3 Ordering — sealed, not a free choice

The CTP ordering is already fixed as

```text
U_N[a_-]^dagger U_N[a_+].
```

Reversing it exchanges the histories and gives the adjoint amplitude; that is
the sealed CTP reality operation, not a second admissible ordering for the same
ordered input.

```text
CONTRACTION_ORDER_IS_AN_UNLICENSED_FREE_CHOICE = false | TYPE-R |
  test: U1/DoR-008 branch orientation and Q-234 CTP-Hermiticity certificate

ALTERNATE_ORDER_USED_HERE = false | TYPE-S |
  scope: exact construction
```

### 4.4 Canonicity verdict

After removing the two false alternatives — effect choice inside the
inclusive task and free CTP ordering — one genuine choice remains:

```text
FREE_CONTRACTION_DATA = {p_ch}
FREE_DATA_DIMENSION = one real probability coordinate
FREE_DATA_DOMAIN = closed interval [0,1]
FREE_DATA_PHYSICAL_MEANING = initial charged-sector occupation
```

The physical statement that would fix it is:

```text
one concrete normalized positive rho_pre/source state, on one lawful scalar
realization, with

p_ch = Tr_omega(rho_pre pi_omega(P_ch)),

and a frozen common-origin descent certificate tying that state to the same
origin as U_N and the admitted effects.
```

A declaration that “the source occupies one sector” would fix an endpoint;
a declaration that both sectors are weighted would still require the weight.
Neither statement is derived by the current finite dynamics.

## 5. Failure-capable checks

### 5.1 Equal-history identity

For `a_+=a_-`, every unit character obeys `Z_N=1`. Hence

```text
A_N^(p)[a,a]=(1-p)+p=1,
|A_N^(p)[a,a]|=1.
```

```text
CHECK_EQUAL_HISTORY_NORMALIZATION = PASS_FOR_ALL_P
EQUAL_HISTORY_CHECK_SELECTS_P = false | TYPE-R |
  test: the result is independent of p
```

### 5.2 Gauge covariance/invariance

Under the sealed common gauge transformation, the endpoint phases cancel in
each relative factor, so `Z_N` is invariant. Since `p` is a source-sector
occupation scalar,

```text
A_N^(p)[a_+^g,a_-^g]=A_N^(p)[a_+,a_-].
```

```text
CHECK_COMMON_GAUGE_INVARIANCE = PASS_FOR_ALL_P | TYPE-P |
  premises: DoR-008, DoR-009
GAUGE_CHECK_SELECTS_P = false | TYPE-R
```

### 5.3 Sequential zero-extension

For `N<=M`, zero-extension gives unit relative factors on the added cells.
Therefore `Z_M=Z_N` on the embedded history, and on the same source marginal

```text
A_M^(p)[(a_+,0),(a_-,0)]=A_N^(p)[a_+,a_-].
```

```text
CHECK_SEQUENTIAL_ZERO_EXTENSION = PASS_FOR_ALL_P | TYPE-P |
  premises: DoR-008, DoR-009
ZERO_EXTENSION_CHECK_SELECTS_P = false | TYPE-R
```

### 5.4 One-cell falsifier arm

At `N=1` the formula is exactly

```text
A_1^(p)=(1-p)+p conjugate(z_-)z_+,
```

and at equal/zero history it returns the inclusive normalized identity. It is
the scalar contraction of the already-verified sealed one-cell operator, with
no modification of the one-cell write.

No sealed one-cell **physical state marginal** exists against which one value
of `p` could be tested. The algebraic falsifier therefore passes every `p` and
cannot be inflated into a state selector.

```text
CHECK_ONE_CELL_OPERATOR_RESTRICTION = PASS_FOR_ALL_P | TYPE-P |
  premises: DoR-008, DoR-009

ONE_CELL_FALSIFIER_FIRES = false | TYPE-R |
  test: exact restriction and equal-history identity

ONE_CELL_SPECIFIC_PHYSICAL_SOURCE_MARGINAL_FOUND = false | TYPE-S |
  scope: the Q-218/Q-219/Q-224/Q-234/Q-235 authority set
```

### 5.5 Adversarial countermodels

The canonicity claim is killed by an explicit same-input pair:

```text
omega_0(P_ch)=0,   A_N^(0)=1,
omega_1(P_ch)=1,   A_N^(1)=Z_N.
```

Both are normalized, positive, charge-superselected, inclusive-effect
contractions on the same carrier and with the same CTP order. They disagree on
every unequal history for which `Z_N!=1`.

A second attack uses any two distinct interior weights `p!=q`; then

```text
A_N^(p)-A_N^(q)=(p-q)(Z_N-1),
```

which again separates them on the same unequal-history domain. Therefore the
continuum is physical-input-sensitive at the finite amplitude interface; it is
not descriptive multiplicity erased by the current conventions.

```text
ADVERSARIAL_CANONICITY_COUNTERMODEL_EXISTS = true
CHOICE_FREE_CONSTRUCTION_DERIVED = false | TYPE-R
```

## 6. Consumer receipts after the sharp stop

### 6.1 Task 3a

Task 3a now receives the exact conditional scalar family

```text
{A_N^(p) : p in [0,1]}.
```

It does not receive a physical scalar CTP instance because `p`, the complete
StatePort, domains, and common-origin trace remain unbuilt.

```text
TASK3A_CONDITIONAL_FINITE_SCALAR_FAMILY_SUPPLIED = true | TYPE-P |
  premises: DoR-008, DoR-009

TASK3A_PHYSICAL_FINITE_SCALAR_CTP_INSTANCE_SUPPLIED = false | TYPE-U |
  would-build: select or derive p through the physical StatePort and certify
               common origin
```

### 6.2 Task 3c

The exact finite family does not reopen Task 3c step 1. That step requires one
complete normalized scalar source-inclusive CTP amplitude on the physical
quotient, with a fixed nonzero Log neighborhood, causal/Ward domains,
connected-density and boundary interfaces, and pre-output provenance.

```text
TASK3C_STEP1_REOPENED = false | TYPE-C |
  constraint: no selected physical amplitude and no complete carrier/quotient/
              Log/domain/density presentation |
  release: instantiate the Q-202 step-1 presentation without output inspection

TASK3C_NORMALIZED_AMPLITUDE_RECEIVED = false | TYPE-U |
  would-build: a selected physical member plus the complete-carrier fields
```

The step remains closed for the same reason Q-235 recorded; this run sharpens
the finite scalar part of that reason to the single response-visible parameter
`p_ch`.

### 6.3 U2 finite StatePort

U2 receives a precise finite interface requirement:

```text
StatePort_N must supply p_ch and certify that it is the P_ch marginal of the
same concrete initial source state that participates in the common-origin
dynamics/effect package.
```

That is not the complete StatePort, which still also requires `omega_phys`,
`H_omega`, `pi_omega`, a trace-class `rho_pre`, state domain, descent map, and
provenance.

```text
U2_FINITE_AMPLITUDE_FACING_STATE_DATUM_LOCALIZED = true
U2_FINITE_STATEPORT_INSTANTIATED = false | TYPE-U |
  would-build: concrete state/scalarization plus p_ch and common-origin trace
```

### 6.4 Net handoff

```text
FULLY_UNBLOCKED_CONSUMER_COUNT = 0
CONDITIONALLY_ADVANCED_CONSUMER_COUNT = 3
SHARED_FINITE_RESPONSE_VISIBLE_FREE_PARAMETER_COUNT = 1
```

This is not a regression of Q-234. `F_N` remains built. The new result
identifies the exact state datum required to turn it into a scalar amplitude.

## 7. Complete typed negative ledger

| Negative | Type | Basis / release |
|---|---|---|
| Named inputs determine one canonical scalar amplitude | `TYPE-R` | `p=0` and `p=1` same-input countermodel |
| Ready record state is the remaining source state | `TYPE-R` | Record/source factor type test |
| Inclusive identity scalarizes `F_N` | `TYPE-R` | Endomorphism versus positive-functional codomain |
| Inclusive identity selects `p_ch` | `TYPE-R` | Every normalized state sees identity as one |
| Source superselection selects a sector weight | `TYPE-R` | Exact interval of diagonal states |
| Physical `p_ch` selected | `TYPE-U` | Concrete source marginal/common-origin descent absent |
| Physical finite amplitude instance built | `TYPE-U` | Depends on physical `p_ch` |
| Finite checks select one `p_ch` | `TYPE-R` | Entire interval passes |
| Noninclusive effect instance available | `TYPE-U` | EffectPort has only the inclusive instance |
| Noninclusive effect family sized | `NO_VERDICT` | No covered instantiated family |
| Noninclusive effect inserted here | `TYPE-S` | Scope uses `I_inc` only |
| CTP contraction ordering is a free choice | `TYPE-R` | Sealed branch orientation/reality |
| Alternate ordering used | `TYPE-S` | Scope inspection |
| Equal-history check selects `p_ch` | `TYPE-R` | Identity for all `p` |
| Gauge check selects `p_ch` | `TYPE-R` | Invariant for all `p` |
| Zero-extension check selects `p_ch` | `TYPE-R` | Natural for all `p` |
| One-cell falsifier fires | `TYPE-R` | Exact restriction passes |
| One-cell physical source marginal found | `TYPE-S` | Current authority set supplies none |
| Choice-free amplitude derived | `TYPE-R` | Continuum countermodel |
| Task 3a physical scalar instance supplied | `TYPE-U` | State/common-origin instance missing |
| Task 3c step 1 reopened | `TYPE-C` | Complete step-1 presentation absent |
| Task 3c normalized amplitude received | `TYPE-U` | Physical member and complete carrier absent |
| U2 finite StatePort instantiated | `TYPE-U` | Full state/scalarization/descent missing |
| Any consumer fully unblocked | `TYPE-U` | Each still consumes the physical state datum |
| Second variation taken | `TYPE-S` | Outside this artifact |
| Response kernel, `B_ind`, or stiffness built | `TYPE-S` | Outside this artifact |
| Coupling, scale, root, eigenvalue, or measured comparison produced | `TYPE-S` | Outside this artifact |

Only `TYPE-R` rows carry refutational content. `TYPE-U`, `TYPE-S`, `TYPE-C`,
and `NO_VERDICT` do not establish a physical impossibility.

## 8. Custody and terminal fences

This lane writes and seals this append-only artifact, verifies its sidecar,
mirrors only the artifact and sidecar to the archive workspace, reports, and
stops. It does not edit the register, a Decision of Record, any prior artifact,
or a baseline. It runs no git, deployment, gate, second-variation, response,
or production act.

```text
alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
coupling_evaluation_authorized = false [TERMINAL_FENCE_DECLARATION]
production_authorized = false [TERMINAL_FENCE_DECLARATION]

SECOND_VARIATION_TAKEN = false | TYPE-S | scope: this artifact
RESPONSE_KERNEL_EXTRACTED = false | TYPE-S | scope: this artifact
B_IND_CONSTRUCTED = false | TYPE-S | scope: this artifact
STIFFNESS_EVALUATED = false | TYPE-S | scope: this artifact
```

