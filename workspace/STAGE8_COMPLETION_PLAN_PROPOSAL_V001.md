# Stage-8 Completion Plan — PROPOSAL V001

Date: 2026-07-26

## Status

```text
PROPOSAL, NOT AN ADOPTION. Parts of this reorder the principal's standing
sequencing, so nothing here is in force until he ratifies it. No obligation
is discharged, no premise adopted, no class narrowed, nothing scheduled.
PRODUCTION REMAINS PROHIBITED. alpha_computed = false.
```

## §0 — WHAT "FINISHING STAGE 8" MEANS, stated first because it decides the plan

```text
STAGE 8 ENDS WHEN, AND ONLY WHEN:
  (1) kappa_record is COMPUTED from a derived chain, not fitted;
  (2) the THEOREM BATTERY VERDICT IS ISSUED BY THE EVALUATOR SCRIPT — no
      lane, including this one, may issue it;
  (3) the verification phase's blind lanes have cleared what the charters
      require of them;
  (4) every surviving named hypothesis is DISCLOSED and CLASSIFIED under
      the Q2 pre-registered standard.
STAGE 8 DOES NOT INCLUDE alpha. Coupling evaluation is separately gated
(coupling_evaluation_authorized = false) and is a later stage.

*** AND THE OUTCOME MAY LEGITIMATELY BE CONDITIONAL. *** Four named
hypotheses are currently undischarged — H-B, H-B0, H-R, H-Z0 — plus the ER
fork carried as the disclosed premise ER-A. A Stage 8 that ends
"kappa_record derived CONDITIONAL on H-B and rho_bar, classified at tier N
under the Q2 standard" IS A COMPLETED STAGE 8, not a failed one. Planning
as though only the unconditional outcome counts is how programs drift.
```

## §1 — THE CRITICAL PATH, and it is shorter than the obligation list looks

Everything that matters runs through **T7(iii)**, the connected
linked-cluster density. It is the one gate whose failure has no workaround
and whose success unblocks the rest.

```text
        transport rule  (DOES NOT EXIST)
               |
               +--> R-L2b  (exponent, sliver direction)
               |        |
               |        +--> SCAD S1..S5  --> R-L0 / R-L0b  --> eta_1 finite
               |        |
               |        +--> B-L2*  (common wall; NO anisotropy clause yet)
               |
               +--> A-L5 / recast Q6   (measure side)
               |
               +--> T11 naturality

        A-L0 ARM 2  (one-line connected cross term)   <-- INDEPENDENT
               Huygens collar (R^3 -> R^2)  AND  >= R^-1 after
               cell-time integration.  NEITHER FACTOR SUFFICES ALONE.

        T7(iii) closes  <==  A-L0 arm 2  AND  eta_1 finite  AND  A-L5
               |
               +--> epsilon_star frozen --> T7(ii) kappa_record
               +--> T7(iv) Duhamel completion (pieces 1-2 already DERIVED)
               +--> T9 unblocks (needs the T7 Hessian it waits on)
               |
        BATTERY VERDICT (evaluator only)
```

**The single point of failure is A-L0 arm 2.** It has no sealed supplier
for factor (ii), it is independent of the transport rule, and the whole
program rests on it. It should be attacked in parallel from day one, not
queued behind the definitional work.

## §2 — PHASE PLAN

### PHASE A — DEFINITIONAL (unblocks four items; bounded; no pipeline)

```text
A1  TRANSPORT-RULE DETERMINATION, and do this FIRST because it is cheap and
    tells us whether the principal is needed:
    IS THE TRANSPORT RULE DERIVABLE FROM SEALED INPUTS, OR IS IT A CHOICE?
      (a) derivable from A1/A2 + D2 + the Duhamel bound -> lane work
      (b) not determined by the corpus -> A MODELLING DECISION, the
          principal's, and a genuine physics decision
    Deliverable: a factual determination in the same form as the C_ref/D3
    one. Ends with either a derivation or a precisely stated choice.
A2  THE BINARY IT MUST SETTLE: is the per-cell insertion domain |C|_4, or
    tau_R x |C|_3?  |C|_4 -> volume-natural, proceed.
    tau_R x |C|_3 -> the invariance FAILS on D3, and the majorant needs a
    different frame. Finding out costs a read; being wrong about it later
    costs the estimates built on top.
A3  E-Q1, THE PRINCIPAL'S, MANDATORY BEFORE E1 EXECUTES: is the pinned
    skeleton's cell 4-volume an admissible constant under spec-header
    scoping clause 1? Weakening F'-5 is not a lane's act.
A4  C_ref/D3 DISPOSITION — the principal's, and NOW DEFERRABLE: today's
    finding is that restricting to shape-regular would NOT rescue the
    object, so this no longer gates anything. Take it after A1/A2, when the
    transport rule may have reframed or mooted it.
```

### PHASE B — THE ANALYTIC CORE (this is where the physics lives or dies)

Run B1 in parallel with Phase A. It does not wait.

```text
B1  A-L0 ARM 2 — THE ONE THAT DECIDES THE PROGRAM. Target
    E1_ONE_LINE_CONNECTED_CROSS_TERM_FREE_HUYGENS_BOUND. Needs BOTH the
    Huygens collar and >= R^-1 after cell-time integration, giving a
    summable R^-2. Three uncertified components already named: U1
    time-profile W^{1,1}/BV norms, U2 light-cone transversality over the
    RATIFIED unrestricted D3 quantifier, U3 the finite-Q projection tail.
    *** U1..U3 AND THE PROJECTION-TAIL CONDITIONAL ARE NOT YET CARRIED
    INTO THE E1 SPEC. That defect is open and must be closed by amendment
    before execution. ***
B2  R-L2b — derive the exponent, uniform over UNBOUNDED ASPECT RATIO, not
    merely on the isotropic orbit. Downstream: B-L2* restated with an
    anisotropy clause it currently lacks.
B3  R-L0 / R-L0b and eta_1's FINITENESS. The surviving hostile-review
    finding L7-2 is live here: eta_1 requires sup_C g < infinity, which
    R-L0's deliverable does not imply, and R-L0's ground (i) as worded
    satisfies R-L0's letter while forcing sup_C g = +infinity. A repair
    created this. It must be fixed, not inherited.
B4  R-L4a/R-L4b — the opposite-phase sector. Note the sealed erratum: the
    blessed Carleman route and the stricken inequality may be the same
    argument; that survived refutation as a MINOR and should be re-read
    before relying on either.
B5  A-L5 / recast Q6 — after A1/A2 only. Recast Q6 is currently in a third
    category: neither ill-posed nor well-posed, quantified over a class on
    which its own weight is half-constructed.
```

### PHASE C — E1 EXECUTION AND ASSEMBLY

```text
C1  CLOSE THE E1 v002 GATE. Its hostile review is NOT cleared: 36 of 55
    findings tested, FIVE SURVIVORS (the orphaned arm-2 binding, F-T1,
    V-2, L7-2, the four-vs-five constants count), and 19 MINOR untested.
    Plus the arm-2 binding is uncarried and E-Q1 is unanswered. E1 v002 is
    NOT EXECUTABLE until these close.
C2  EXECUTE E1 -> epsilon_star frozen by the E1 rule (outcome-blind, dyadic
    grid, 1/2 threshold). If no grid point qualifies the verdict is
    T7III_BLOCKED with witness EPSILON_STAR_VACUOUS — a real outcome.
C3  T7(iii) verdict.
C4  T7(iv) Duhamel completion — pieces 1 and 2 already DERIVED, schema and
    L-ADD sealed. The A4(3) erratum still needs the principal's sign-off.
C5  T7(ii) kappa_record COMPUTED. Nothing before this point computes it.
C6  T9 unblocks once the T7 Hessian with certified enclosure exists.
```

### PHASE D — BATTERY COMPLETION

```text
D1  T2 — the REPRESENTED exact order-2 witness. Universal algebra is
    derived with an explicit gr^2 witness; represented order 2 is NOT
    certified and T2 MAY NOT be recorded as discharged on the universal
    computation alone. Missing: a sealed represented nonzero gr_pi^2
    witness.
D2  T1 — needs a sealed represented link/holonomy object for the basepoint.
    Formula derived; value undetermined.
D3  T11 — refinement naturality, after Phase A.
D4  T3/T6/T10 — independently DERIVED, not CONFIRMED. Verification-phase
    blind lanes. T16 and T8 are already credited as confirmed; do not redo
    them.
D5  EVALUATOR ISSUES THE BATTERY VERDICT. No lane may pre-empt it.
```

### PHASE E — VERIFICATION AND PRODUCTION (parallel; NOT the critical path)

This is the phase to stop treating as the prerequisite.

```text
E1  v003 at N_t = 96 under A1/A2, then the A2 rehearsal. Tests the frozen
    P-D1/P-D2/P-D3 AND exercises the never-executed accepting branch. If
    zero failures: record which of the five never-run gates fired and
    whether each behaved.
E2  A4 audit of v003 — a new, less-audited lane in the production path.
    That cost is real and must be paid, not assumed.
E3  PASS-reachability — only if E1 still shows failures. Otherwise E1
    answers it for free.
E4  REPRODUCIBILITY (G1), cheap items first: the cwd-aware seal verifier
    (71 of 507 seals fail the documented procedure; the single most
    embarrassing gap), the environment lock, ABSENCE MARKERS for missing
    evaluator inputs, and publishing the rehearsal outputs. The portable
    reproducer and the command DAG are substantial — scope, don't start.
E5  CONTROLS (G2), five named gaps: control-4 detector teeth,
    D3/T11 sliver control, T2 flat/commuting refusal, T9 wrong-Hessian
    routing, and the CAR predeclared-scaling control.
E6  F-8 clause 3 — cannot discharge before Phase A of the pipeline
    executes. Sequencing fact, not a lane failure.
```

## §3 — WHAT I PROPOSE CHANGING ABOUT THE CURRENT ORDER

```text
STANDING ORDER: (1) sliver attempt [DONE], (2) v003, (3) A4 audit.
PROPOSED: run PHASE A1/A2 and PHASE B1 in the analytical lane NOW, and let
Phase E (v003, A4) proceed BEHIND them rather than ahead.
REASON, and it is a fact about dependencies rather than a preference: the
derivation needs NO PIPELINE. Not the launcher, not the fence, not the
manifests, not a production run. Phase E validates one comparison; Phases
A-D are paper mathematics on sealed inputs. Keeping Phase E on the critical
path is what has held the program in the measurement cycle — each pipeline
finding was genuinely blocking FOR PRODUCTION, and production was never the
next step for the physics.
Phase E is still worth doing: it carries the principal's frozen predictions
and it is the only thing that will ever exercise the accepting branch. It
simply should not gate Phase B.
```

## §4 — HONEST FAILURE MODES, named in advance

```text
F1  A-L0 ARM 2 DOES NOT CLOSE. The most likely single failure, and the
    independent system's P-C1 predicts E1S_BLOCKED. Consequence: T7(iii)
    blocks, kappa_record is not reached, and Stage 8 ends with a named
    block rather than a value. THAT IS A LEGITIMATE STAGE-8 OUTCOME and
    the program's discipline exists to make it reportable.
F2  THE TRANSPORT RULE RESOLVES TO tau_R x |C|_3. Then the invariance fails
    on D3 and the majorant needs a different frame — the largest-scope
    failure mode on the board, and the cheapest to detect. Detect it early.
F3  eta_1 IS INFINITE (L7-2). Then the n = 1 leg is blocked regardless of
    everything else, and a repair created the defect.
F4  THE NAMED HYPOTHESES NEVER DISCHARGE. Then Stage 8 completes
    CONDITIONAL, and the Q2 pre-registered standard governs the tier. Plan
    for this outcome; it is not the bad one.
F5  THE PIPELINE NEVER PASSES. Then kappa_record has no independent
    cross-check even if derived, and the verification phase must say so.
    Note the accepting branch has still never executed.
```

## §5 — WHAT THE PRINCIPAL IS ACTUALLY NEEDED FOR

```text
NOW:      ratify or amend this plan; and answer A3 (E-Q1) since E1 cannot
          execute without it.
SOON:     A1's outcome may hand him a MODELLING DECISION (case (b)) — the
          only genuine physics decision currently foreseeable.
DEFERRED: C_ref/D3 (no longer gating), Q2 (untriggered), A4(3) erratum
          sign-off, Q6 re-cast scheduling, the methods-record verbatim
          item, quarantine of launchers v005/v006.
NOT HIS:  everything in Phases B, D and E except sign-offs. Those are lane
          work.
```

## Protected status

```text
plan_status = PROPOSAL_NOT_ADOPTED
critical_path = T7(iii) via A_L0_arm2 + eta_1_finite + A_L5
single_point_of_failure = A_L0_arm_2
pipeline_on_critical_path = false          (the proposed change)
conditional_completion_is_a_valid_outcome = true
kappa_record_computed = false
battery_verdict_issued = false
alpha_computed = false
coupling_evaluation_authorized = false
proof_authorized = false
production_authorized = false
```
