# STAGE 8 — RANK PINNING: DOES THE K_SQUARE ARM PIN (r_0, r_ch)? — OF RECORD
## BLIND DERIVER (RANK-PIN) — DERIVATION FROM SEALED CONTENT ONLY — [CLAIMED]

Date: 2026-08-13
Role: pin the ordered rank pair `(r_0, r_ch)` from sealed content, or report exactly
why not. The sector weights `(w_0, w_ch) = (1 - kappa_ch, kappa_ch)` with
`kappa_ch = Tr_A(P_ch)/Tr_A(I_src)` are pinned SYMBOLICALLY on the ratified chain
(G2 determination f7fd0c60, panel-audited); the VALUES ride `(r_0, r_ch)` — sealed
NO_VERDICT (D14 :14). The named route: the K_square arm (capped, C29) + the
rank-pinning package (D14 :14-17). The b1-cell instance (a3bedc7e, 2026-08-13) has
since instantiated the K_square carrier of record; this build decides what that
changes.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`

Fences held: derivation from sealed content only; exact structural algebra only —
ranks/dimensions carried as structural integers, no physical quantity evaluated; NO
scale, NO imported GR, NO faithfulness; no register/tracker/plan/road/ledger/lens
file read; every seal recomputed at path BEFORE reliance; the principal-selection bar
honored as hard: no rank value is selected, assumed, or preferred anywhere below
(D14 :25-27). Output name probed before write: ABSENT.

---

## 0. VERDICT IN ONE LINE

**NOT-PINNABLE, and not by a near miss: the K_square arm with the b1-cell
instantiation pins NEITHER `r_0` NOR `r_ch` — the instance supplies only the
TARGET-side carrier (and is barred by construction from ever serving as the
completion side: it is rebuilt from the sealed target spec, after the sealed
outcomes, which is exactly the Q-217 tautology the C29 temporal bar exists to
refuse); the completion-side package (X_K, T_K, rho_C0,K, A_C0,K, E_C0,K) remains
absent of record, so the comparison equation still has no left-hand operator; and a
STRUCTURAL CEILING derived exactly here from the sealed germ forms caps the route in
advance: the sealed germ consumes the pair ONLY through the reduced ratio
`kappa_ch = r_ch/(r_0 + r_ch)`, so any scalar/trace-side comparison is invariant
under `(r_0, r_ch) -> (k r_0, k r_ch)` — the arm's maximal possible positive yield
is the exact rational RATIO, never the pair, unless the package carries fixed source
multiplicities and a scalar trace (SGV4 :304-306), objects no sealed artifact
supplies and whose entry as chosen values would VOID the step. What stands fixed of
record is unchanged: `r_0, r_ch` positive integers (D14 :10), sum
`r_0 + r_ch = Tr_A(I_src)` symbolic-finite (GOV3 A0), `kappa_ch` an exact rational
in `Q ∩ (0,1)` — and hence `mu != 0` symbolically on the ratified chain, magnitude
and sign still riding the one unpinned rational.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All recomputed by `shasum -a 256` at path this session BEFORE reading; sidecar
compared where present (all matched). Workspace root
`/Users/bgm/MB Work/alpha-program-archive/workspace/`; DoRs at `…/supervision/`.

| Tag | Source | SHA-256 (recomputed) | Status |
|---|---|---|---|
| D14 | `DECISION_OF_RECORD_014_SOURCE_GERM_PHYS_RATIFIED_2026-08-02_V001.md` | `b6e4116df63403478d28be8cdb6589b091cc1aa8b6ad5a40776a28b135cd138f` | MATCHES-TASKED (b6e4116d…); NOTE: lives under `supervision/`, not `workspace/` |
| D13 | `DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | SEALED-OK; matches SGV4's cited digest |
| GOV3 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | MATCHES-TASKED |
| SGV4 | `STAGE8_SOURCE_GERM_PHYS_ADOPTION_PROPOSAL_V004.md` | `d4cdbb6623797df6accb7dc9b24134d179bfc8e8d039c585d5c91ae23255869e` | MATCHES-TASKED |
| Q272 | `STAGE8_SOURCE_GERM_PHYS_V004_FINAL_CONFIRMATION_REVIEW_DETERMINATION_V001.md` | `fc13f4e1ec42a6aef280e231f2e0da3246e5557facb380e4b0865abeb2a28c0c` | LOCATED-FROM-D14 (D14 :6 "fc13f4e1…"); SEALED-OK |
| T2F | `STAGE8_TASK2F_K_SQUARE_CHOICE_INVARIANT_FALSIFIER_CONTENT_DETERMINATION_V001.md` | `db9e5104f62f4211e65d15b909f87c4272a6a030dc071f2c83e383b295b3093c` | LOCATED-FROM-SGV4 (:77 lineage row); SEALED-OK |
| BIDU | `BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md` | `b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f` | LOCATED-FROM-SGV4 (:76 lineage row); SEALED-OK |
| G2 | `STAGE8_G2_STATE_PORT_DETERMINATION_V001.md` | `f7fd0c6048dfec8f08bd02f2ae008bcb329ccf4c0d7cb5b14a8c25359b57767a` | MATCHES-TASKED |
| PO | `STAGE8_G2_PANEL_OVER_V001.md` | `c8bde5c9c9d228faae799ea8433a01c31472c7029fdec3ffd3497ab22779e27f` | MATCHES-TASKED (verdict BUILD_SOUND) |
| PU | `STAGE8_G2_PANEL_UNDER_V001.md` | `2bf4c1a2238ad3241bf290b4b69a8be657d3a335d026b9024feec3bf4f0df7d1` | MATCHES-TASKED (FC6: pair GENUINELY_FREE; its corpus search predates the b1-cell build) |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | MATCHES-TASKED |
| B1C | `STAGE8_B1_CELL_INSTANCE_V001.md` | `a3bedc7ef092ebd932d254331dc10053aa123334a013b95fb1e15019bdfea016` | MATCHES-TASKED (the K_square instance of record) |
| KDD | `STAGE8_MIN_CARRIER_CANDIDATE_V001.md` | `f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647` | MATCHES-TASKED (K_dd — CANDIDATE, not of record at the physical stratum) |

C29, resolved of record: the collapse-condition audit types C29 as
"NOT-A-COLLAPSE-CONDITION … the temporal bar: one trace supplies state, effects,
domains, dynamics BEFORE any response output is inspected"
(`STAGE8_DESC_B03_DARIO_V001.md` :509; its check `STAGE8_DESC_B03_CHECK_CODEX2_V001.md`
:185 "demand §3D: temporal ordering bar"). This matches T2F's package clause verbatim:
"provenance fixing A_C0,K, E_C0,K, rho_C0,K, X_K, and T_K BEFORE the sealed B_K
outcome is inspected" (T2F :397-398). The cap on the arm is the ordering bar, read
here as exactly that and nothing more.

---

## 2. DELIVERABLE 1 — THE RANK-PINNING PACKAGE, EVERY MEMBER, TYPED AT SPANS

D14 :14-17 names the package in one clause: "the missing rank-pinning package
(sealed scalar source representation + rank-preserving intertwiner) is the
require-shaped would-build." Traced through D14's adopted text (SGV4) and SGV4's
own lineage citation (T2F), the package resolves into two sealed member lists plus
the target side they compare against.

### 2.1 The A0 rank-pinning package (SGV4 §4.3 :310-315; re-sealed at :480-481 and Q272 :425-427)

```text
A0_RANK_PINNING_PACKAGE_EXISTS = false | TYPE-U  (SGV4 :311)
members, typed as of this build:
A1  fixed scalar source representation          UNBUILT  (TYPE-U, SGV4 :311-315)
A2  fixed P_0/P_ch ranks                        UNBUILT — and REQUIRE-SHAPED: this
    member cannot be supplied as an input without voiding the step (a chosen value,
    D14 :25-27); it can only arrive as the OUTPUT of a derivation not on record
A3  faithful normalized trace/state             PARTIAL — the family-level objects
    exist symbolically (faithful ordinary trace Tr_A, GOV3 :356; rho_P = omega_tr
    forced family-wide, GOV3 :812) but over the UNFIXED carrier; the package needs
    them over a FIXED representation (A1), which does not exist
A4  mandatory A0-to-sealed intertwiner          UNBUILT  (TYPE-U)
A5  restriction theorem                         UNBUILT  (TYPE-U, Q272 :425-427)
```

### 2.2 The K_square arm's executables — the C0 preimage package (SGV4 §4.2 :294-299; T2F §6 :388-399)

```text
C0_FINITE_INCIDENCE_OPERATOR_PREIMAGE_PACKAGE := ( A_C0,K, E_C0,K, rho_C0,K, X_K,
  T_K, equations T_K rho_C0,K(a) = rho_K(a) T_K and T_K X_K = B_K T_K, provenance
  fixing all five BEFORE the sealed B_K outcome is inspected )        (T2F :388-399)
members, typed as of this build:
B1  finite C0 algebra/subquotient A_C0,K        UNBUILT
B2  finite comparison-capable carrier E_C0,K    UNBUILT ON THE COMPLETION SIDE —
    see §3.2: the b1-cell instance is the TARGET-side carrier and is barred from
    completion-side service (Q-217 tautology + the C29 ordering bar)
B3  representation rho_C0,K                     UNBUILT
B4  source incidence operator X_K               UNBUILT — the record's own typed
    corpus search returned found = false (TYPE-S, T2F :414-423); "no pullback from
    B_square is accepted as an independent source" (T2F :420)
B5  comparison intertwiner T_K                  UNBUILT (NONTAUTOLOGICAL_B_SQUARE_
    PREIMAGE_FIXED = false | TYPE-U, T2F :424-425)
B6  the C29 provenance clause                   UNSATISFIABLE RETROACTIVELY for any
    object rebuilt from the target spec after the sealed outcomes (§3.2)
```

### 2.3 The target side the arm compares against — BUILT

```text
C1  the sealed K_square result set, choice-invariant across all 1,088 admissible
    filtrations (unitary permutation conjugacy; R_square = 3/16 carried as sealed
    text, not evaluated here)                          BUILT   (T2F :23-59, :450-461)
C2  the K_square carrier INSTANCE of record            BUILT 2026-08-13 — the
    b1-cell instance (B1C): V = 4, E = 4, F = 0; C^0 = C^1 = Q^4; blocks
    im(d_0) = 3, H = 1, im(d_1^dagger) = 0; b_1 = 1; H^1 = 1 EXACT; unique cycle
    class chi_j = (+1,-1,+1,-1) — at ENTERED-AUTHORED-PREMISE strength, TYPE-P on
    the Attach entry 420ab02f, voiding with it (B1C lead, §2, S-3(f))
C3  the sealed source-sector split + superselection    BUILT   (M03 :142-149 the
    two-sector span with P_0 + P_ch = I_src; :205-213 zero cross-history entries
    via <R_N|P_N> = 0; :560-564 codomain End(P_0 H_src ⊕ P_ch H_src) "represented
    by the displayed 2x2 diagonal operator on SECTOR LABELS")
C4  the symbolic ratio machinery                       BUILT SYMBOLICALLY
    (GOV3 :816-826: kappa_ch = Tr_A(P_ch)/Tr_A(I_src) = r_ch/(r_0 + r_ch);
    KAPPA_CH_VALUE_EVALUATED = false, GOV3 :839)
```

Adjacent sealed carrier, for completeness of the tasked list: K_dd (KDD) is a
CANDIDATE only — K_square + one duplicate edge + one filled face (V = 4, E = 5,
F = 1; blocks 3/1/1; b_1 = 2; H^1 = 1), "NONE_SEALED at the physical stratum"
(KDD H-1), and b_1 = 2 places it OUTSIDE the entered Attach operative domain
(b_1 = 1 cells only, addendum :32-35 via B1C §1.1). It supplies no rank datum and
no completion-side object; a future arm could consume it only through a new entry.

**PACKAGE BOTTOM LINE: of the eleven would-build members (A1-A5, B1-B6), zero are
BUILT, one is PARTIAL (A3, family-level trace/state over an unfixed carrier), ten
are UNBUILT; the b1-cell build added target-side instance C2 only.**

---

## 3. DELIVERABLE 2 — THE K_SQUARE ARM, WORKED EXACTLY

Four steps; each runs on sealed displays only.

### 3.1 STEP 1 — the structural ceiling: the pair is scalar-invisible beyond its reduced ratio

The sealed germ calculus after both quotients (SGV4 §6 :343-357) consumes the A0
rank data through exactly one scalar:

```text
p_[A] := Tr_A(P_ch)/Tr_A(I_A) = r_ch/(r_0 + r_ch)
Z_[A],n[J,R] = (1 - p_[A]) + p_[A] exp(Xi_n[J,R])
```

and every displayed derivative (`D Z`, `D_R Z`, `D Log_0 Z`, `W_[A]`) factors
through `p_[A]` and the rank-independent `Xi_n` (SGV4 :347-357). EXACT CONSEQUENCE,
derived here: for every integer `k >= 1`, the pairs `(r_0, r_ch)` and
`(k r_0, k r_ch)` produce IDENTICAL germ outputs — `p_[A]` is invariant under
common integer scaling. SGV4 itself seals the awareness at :248-250: "Different
ordered rank pairs remain distinct … even when two pairs happen to yield the same
reduced symbolic ratio" — distinct of record, yet germ-indistinguishable.
Therefore ANY comparison that reaches the A0 data through the scalar germ or any
trace of it — which is the only sealed route the arm's scalar side has — can at
most pin the reduced ratio `kappa_ch = r_ch/(r_0 + r_ch)`, NEVER the pair. Pinning
the pair requires multiplicity-sensitive operator-level data: exactly SGV4
:304-306's condition, "unless the comparison package includes fixed source
multiplicities and a scalar trace." No sealed artifact supplies either; BIDU
:41-69 seals the opposite standing — unitary changes of basis on the UNRESOLVED
multiplicity spaces `M_q` are admissible source equivalences, verified with
rank-greater-than-one sectors (BIDU :67-70). The ceiling is in force BEFORE any
executability question.

### 3.2 STEP 2 — what the b1-cell instantiation supplies, and what it is barred from supplying

The b1-cell instance realizes the K_square carrier combinatorially and exactly
(C2 above). But the arm's missing side is the COMPLETION side, and the instance
cannot serve there, twice over:

(i) TAUTOLOGY BAR (Q-217, carried at T2F :38-40, :403-411): the instance is rebuilt
FROM the sealed target specification (BID V011 :1840-1911 — B1C CHECK 0 asserts
entrywise equality with the sealed `D_square(u=1)`). Defining the completion-side
carrier or operator by pulling the target backward "would make the falsifier pass
by definition and is the tautology expressly refused in Q-217."

(ii) THE C29 ORDERING BAR (T2F :397-398; B03 :509): completion-side provenance must
be fixed BEFORE the sealed `B_K` outcome is inspected. The b1-cell build is dated
2026-08-13; the sealed K_square outcomes it reproduces predate it (the 2026-08-02/04
layer, PU FC6). Any attempt to re-enter the instance as `E_C0,K` fails the ordering
bar retroactively and permanently.

The instance's own seal says the same in its own words: "It does NOT make K_square
a physical carrier … no complex instance with H^1 > 0 is sealed as a physical
carrier"; "no sealed map sends K_square into the write-carrier complex"
(B1C S-3(a),(b)). So after the b1-cell build the arm's standing is UNCHANGED:
`T_K X_K = B_K T_K` still has no left-hand operator (T2F :32-38); the arm is still
UNEXECUTABLE (T2F :427-430).

### 3.3 STEP 3 — what the sealed carrier structure forces about the two ranks: NOTHING NEW

Worked at the three tasked posts:

(i) THE TRACE CELL'S DIMENSIONS. The instance's numbers are pinned exactly on the
trace side — `C^1 = Q^4`, blocks `3/1/0`, `dim H = 1`, unique class. But those
numbers attach to the RECORD/trace tensor factor. The ratified coupling between
the factors is `U_N^(n)[a] = P_0 ⊗ I + P_ch ⊗ W_N^(n)[a]` (M03 :146-149; the
entered constructor at n = +1, addendum :32-35 via B1C §1.1), and the cell enters
it through exactly one scalar per cell, `z_j = chi_n(h_j[a_j])` — a U(1) character
value multiplying `P_ch` (M03 :128-133, :228-247). A scalar is RANK-BLIND: it
commutes with every unitary on the source factor and carries no multiplicity
datum. No sealed object routes `V = 4`, `E = 4`, `dim H = 1`, or `b_1 = 1` into
`rank P_0^A` or `rank P_ch^A`.

(ii) THE SOURCE ALGEBRA'S SEALED SECTOR SPLIT. M03's codomain is
`End(P_0 H_src ⊕ P_ch H_src)`, "represented by the displayed 2x2 diagonal operator
on sector labels" (M03 :560-564) — an INDEX-space display over the two labels
`{0, ch}`, expressly not a dimension statement. GOV3's A0 authors the carrier with
`dim(H_src^A) < infinity` as an AUTHORING CONDITION and seals "No dimension is
selected or evaluated. The ranks remain symbolic";
`A0_NUMERIC_DIMENSION_SELECTED = false | TYPE-S` (GOV3 :352-357, :369, :384-385).

(iii) THE SUPERSELECTION. It forces sector-DIAGONALITY — the admitted states
satisfy `rho_S = P_0 rho_S P_0 + P_ch rho_S P_ch`, and the law's own identity
kills the cross-entries (`<R_N|P_N> = 0`, M03 :205-213). Diagonality constrains
the OFF-diagonal blocks, i.e. it says nothing about the two diagonal blocks'
dimensions beyond both sectors being nonzero subspaces — which is D14 :10's
already-sealed durable-branch premise, not a new yield.

CLOSING THE TEMPTING DOOR EXPLICITLY: every candidate identification of carrier
block dimensions with sector ranks — `r_ch := dim H = 1`, `r_0 := dim im(d_0) = 3`,
`r_0 + r_ch := E = 4`, `r_ch := b_1 = 1`, or any other — requires an identification
map that exists in NO sealed artifact (B1C S-3(b); T2F :356-361 seals the analogous
refusal for the Q-239 phase map: "Orbit invariance alone does not identify the
domains"). Choosing such a map is choosing the value it delivers: a principal
selection in disguise, and the step VOIDS under D14 :25-27. This build refuses all
of them.

### 3.4 STEP 4 — the arm's logical shape even when complete: a rejector, not a selector

Sealed twice, independently: "a rank-dependent comparison failure would REJECT the
mismatched A0 presentation; invariant agreement would not by itself select ranks
unless the comparison package includes fixed source multiplicities and a scalar
trace" (SGV4 :301-306); "a future independently fixed completion-side scalar
representation, source operator, and intertwiner may REJECT rank members. Until
then it neither pins nor clears the family" (Q272 :392-394). So the fully-built
arm's positive mode is family-wide EXCLUSION — it carves the admitted set of pairs;
by STEP 1 the finest carving any scalar-trace-side comparison can reach is a
verdict on the reduced ratio. A unique-pair pin is reachable only through the
multiplicity-fixed operator package, whose rank member (A2) is itself
require-shaped (§2.1).

---

## 4. DELIVERABLE 3 — THE VERDICT, WITH THE STANDING FIXED CONTENT AND THE MISSING OBJECTS

```text
RANKS = NOT-PINNABLE by the named route as of this build. The K_square arm +
b1-cell instantiation pins NEITHER r_0 NOR r_ch, and adds NO new constraint on
either — not a value, not a ratio, not a bound.

WHAT STANDS FIXED OF RECORD (unchanged by this build; listed against flattening):
  r_0 >= 1, r_ch >= 1, both integers            D14 :10 (durable branch) — a sealed
                                                 PREMISE of the germ, not an arm yield
  r_0 + r_ch = Tr_A(I_src) = dim(H_src^A)       finite by the A0 authoring condition
                                                 (GOV3 :354); SYMBOLIC — the sum is a
                                                 named unvalued structural integer
  kappa_ch = r_ch/(r_0 + r_ch)                  exact rational in Q ∩ (0,1), forced
                                                 form (GOV3 :822-825; D14 :13); value
                                                 open
  same-rank presentations GAUGE                  the trace-preserving block-unitary
                                                 quotient (SGV4 §3, D14 :11-12)
  NEW OF THIS BUILD (structural, negative): the ceiling of §3.1 — the pair is
  scalar-invisible beyond its reduced ratio; the named route's maximal possible
  positive yield is kappa_ch, never (r_0, r_ch).

THE EXACT MISSING OBJECTS AND THEIR HOLDERS:
  M1  X_K — the completion-side source incidence operator.
      HOLDER: lane-derivable IF an independent preimage exists in sealed content —
      undetermined of record, and the record's own typed search found none
      (T2F :414-423, TYPE-S); FALLBACK: authored-physics via a disclosed entry
      (the D13/Attach pattern — a principal act on an OBJECT, which the record
      permits); NEVER principal selection of rank VALUES (barred, D14 :25-27).
  M2  T_K — the nontautological comparison intertwiner. Same holder standing
      (T2F :424-425); the pullback definition is refused of record (Q-217).
  M3  rho_C0,K + A_C0,K + completion-side E_C0,K — the independently fixed finite
      representation. Same holder standing; the C29 ordering bar binds its
      provenance (fixed before outcome inspection), which no target-rebuilt object
      can satisfy (§3.2).
  M4  fixed source multiplicities + scalar trace INSIDE the package — required for
      the arm's agreement mode to select rather than merely reject (SGV4 :304-306).
      REQUIRE-SHAPED: the multiplicities ARE the ranks; they cannot be entered as
      chosen values (voids, D14 :25-27); they can only arrive as the OUTPUT of a
      derivation not on record. This is the pair's single lawful pinning door, and
      it is the same door the package's member A2 names.
  M5  physical-carrier status for any K_square-side object — NONE_SEALED at the
      physical write-carrier stratum (B1C S-3(a); glued check via B1C :288-291);
      the b1-cell instance is entered-authored, TYPE-P on the Attach entry, and
      voids with it.
```

Honest-outcome note: NOT-PINNABLE here is a statement about the present sealed
record and the named route's structure, not a physical refutation and not a
clearance — the sealed standing `FULL_DOR008_A0_PINNING = NO_VERDICT` (SGV4 :478;
Q272 :423-424) is confirmed, not converted.

---

## 5. DELIVERABLE 4 — THE MU BEARING, SYMBOLIC

Carried on the G2 determination's audited chain (G2 §6; PO P6); the one structural
property used is FORM's sealed dichotomy `mu(w) ≡ 0 iff w_0 w_ch = 0`.

```text
UNCHANGED BY THIS BUILD — Outcome A standing (ratified chain carried, all TYPE-P):
  (w_0, w_ch) = (1 - kappa_ch, kappa_ch)
              = ( r_0/(r_0 + r_ch) , r_ch/(r_0 + r_ch) )      [exact rational forms;
                GOV3 :822-825 + G2's audited weight identification]
  r_0 >= 1, r_ch >= 1  =>  both weights in Q ∩ (0,1)  =>  w_0 w_ch != 0
  =>  mu != 0  PINNED SYMBOLICALLY; magnitude and sign NOT pinned.

WHAT THIS BUILD ADDS TO THE BEARING (exact, symbolic):
  (i)   the ENTIRE value freedom of the weight pair is ONE rational:
        kappa_ch = r_ch/(r_0 + r_ch) ∈ Q ∩ (0,1); w_0 = 1 - kappa_ch rides the
        same rational. Nothing else about the weights is open on the ratified chain.
  (ii)  the named route's CEILING (§3.1) is exactly that rational: if the arm is
        ever completed and returns agreement-with-ratio-content, the yield is
        kappa_ch exact-rational — mu's weight-borne magnitude factor
        mu-dependence-through-(w_0 w_ch) = kappa_ch(1 - kappa_ch)
        = r_0 r_ch/(r_0 + r_ch)^2 becomes an exact rational; the SIGN burden is
        untouched (it rides the Gate-5 limit per FORM D5, as G2 carries).
  (iii) a REJECTION outcome from a completed arm fires the DoR-008 restriction
        falsifier chain: the germ's downstream positives VOID (D14 :24-27), the
        weights revert to the free simplex over Sigma_src, and mu falls to
        UNDETERMINED (G2 Outcome B) — mu = 0 then sits exactly on the one-sector
        simplex boundary, forced by nothing executable.
  (iv)  NO outcome of this build moves mu to zero: mu = 0 remains pinned by no
        sealed outcome (G2 Outcome C; DoR-006 bar via M06), and the NOT-PINNABLE
        verdict here does not touch the strict positivity that pins mu != 0
        symbolically — positivity rides D14 :10, not the arm.
No number is evaluated: kappa_ch, the weights, mu, r_0, r_ch, and every ratio
above are carried as exact symbolic/rational FORMS only.
```

---

## 6. WHAT THIS DOES NOT SAY / OVERCLAIM AUDIT

```text
IT DOES NOT convert NO_VERDICT into refutation: the pair remains exactly
   NO_VERDICT of record; this build reports the named route's present incapacity
   and its structural ceiling, both at spans.
IT DOES NOT touch the b1-cell instance's own standing: the instance is sound at
   its stratum (carrier exhibited, AND-class mirror collapse); this build consumes
   its census and its self-declared limits (S-3), nothing more.
IT DOES NOT diminish the arm as a future falsifier: rejection capability is
   confirmed binding of record (Q272 :392-394); what is denied is present
   executability and unique-pair selection capacity absent M4.
IT DOES NOT select, prefer, or shortlist any rank pair, any ratio value, any
   identification map, or any package realization; the tempting carrier-block
   identifications are enumerated ONLY to refuse them (§3.3).
IT DOES NOT read the register, tracker, plan, road, ledger, or lens; the C29
   resolution was taken from the sealed B03 lane artifacts at path.
YOURS assemblies, marked: the scalar-invisibility ceiling of §3.1 (one-line
   invariance of p_[A] under common integer scaling, run on SGV4's sealed germ
   display); the retroactive C29 argument of §3.2(ii) (dates read from sealed
   artifact headers); the w_0 w_ch = r_0 r_ch/(r_0+r_ch)^2 display in §5(ii)
   (exact rational algebra on the sealed forms). Each runs on quoted sealed
   identities; none consumes an unsealed value.
FLATTENING CHECK: NOT-PINNABLE is not flattened into "never pinnable" — the M4
   door is named and lawful; PARTIAL standing content (positivity, symbolic sum,
   exact-rational form) is listed, not suppressed; the arm's rejector power is
   carried, not erased.
```

---

## 7. FINAL FLAG BLOCK

```text
PACKAGE = D14 :14-17 ("sealed scalar source representation + rank-preserving
  intertwiner"), resolved through D14's adopted text into eleven members at spans:
  A0 side (SGV4 §4.3 :310-315, :480-481; Q272 :425-427): A1 fixed scalar source
  representation UNBUILT; A2 fixed P_0/P_ch ranks UNBUILT+REQUIRE-SHAPED (cannot
  be an input — voids per D14 :25-27); A3 faithful normalized trace/state PARTIAL
  (exists family-level over the unfixed carrier, GOV3 :356, :812); A4 mandatory
  A0-to-sealed intertwiner UNBUILT; A5 restriction theorem UNBUILT. K_square
  executables (SGV4 §4.2 :294-299; T2F §6 :388-399): B1 A_C0,K UNBUILT; B2
  completion-side E_C0,K UNBUILT (the b1-cell instance is TARGET-side and barred
  from completion service); B3 rho_C0,K UNBUILT; B4 X_K UNBUILT (typed search
  found-false, T2F :414-423); B5 T_K UNBUILT (T2F :424-425); B6 C29 provenance
  clause UNSATISFIABLE-RETROACTIVELY for target-rebuilt objects. Target side
  BUILT: C1 sealed choice-invariant K_square results (T2F :23-59); C2 the b1-cell
  carrier instance of 2026-08-13 (a3bedc7e, TYPE-P on Attach entry 420ab02f);
  C3 M03 sector split + superselection (M03 :142-149, :205-213, :560-564);
  C4 symbolic kappa_ch machinery (GOV3 :816-826). K_dd (f1881511) is CANDIDATE
  only, b_1 = 2, outside the Attach domain, no rank datum. Tally: 0 of 11
  would-build members BUILT, 1 PARTIAL, 10 UNBUILT.

K_SQUARE_ARM = FORCES NOTHING ABOUT (r_0, r_ch), exactly: (1) STRUCTURAL CEILING
  (derived, §3.1): the sealed germ consumes the pair only through
  kappa_ch = r_ch/(r_0 + r_ch) (SGV4 :343-357; GOV3 :822-825), so every
  scalar/trace-side comparison is invariant under (r_0, r_ch) -> (k r_0, k r_ch);
  the route's maximal positive yield is the reduced RATIO, never the pair, absent
  fixed source multiplicities + scalar trace in the package (SGV4 :304-306;
  BIDU :41-69 seals multiplicities unresolved). (2) STILL UNEXECUTABLE: the
  b1-cell instance supplies the TARGET-side carrier only; completion side absent
  (T2F :414-434); T_K X_K = B_K T_K has no left-hand operator; the instance is
  barred from completion-side service by the Q-217 tautology bar and the C29
  temporal bar (provenance postdates the sealed outcomes). (3) CARRIER STRUCTURE
  RANK-BLIND: the cell couples through the scalar z_j = chi_n(h_j) only
  (M03 :128-149); M03's 2x2 display is on sector LABELS (M03 :560-564); A0's
  dimension is authored-finite, never valued (GOV3 :354, :384-385);
  superselection forces diagonality, not dimensions (M03 :205-213). Every
  carrier-block-to-rank identification (r_ch := dim H = 1 etc.) is an unsealed
  map = a selection in disguise = VOID (D14 :25-27) — refused. (4) EVEN COMPLETE,
  the arm is a REJECTOR (SGV4 :301-306; Q272 :392-394): agreement selects nothing
  absent M4; rejection carves the family and at scalar depth carves at most the
  ratio.

RANKS = NOT-PINNABLE(missing: M1 X_K, M2 T_K, M3 rho_C0,K/A_C0,K/completion-side
  E_C0,K — holder for M1-M3: lane-derivable IF an independent preimage exists
  (undetermined; record's own search found none, T2F :414-423), fallback
  authored-physics by disclosed entry (the D13/Attach pattern), NEVER principal
  value-selection (barred, D14 :25-27); M4 fixed source multiplicities + scalar
  trace inside the package — the single lawful pinning door, require-shaped, can
  only arrive as derivation output; M5 physical-carrier status for the K_square
  stratum — NONE_SEALED (B1C S-3(a))). STANDING FIXED CONTENT unchanged:
  r_0 >= 1, r_ch >= 1 integers (D14 :10); r_0 + r_ch = Tr_A(I_src) symbolic-
  finite (GOV3 :354); kappa_ch = r_ch/(r_0 + r_ch) exact rational in Q ∩ (0,1),
  form forced, value open; same-rank gauge quotient (SGV4 §3). NEW: the §3.1
  ceiling (ratio-only scalar visibility). NO new bound, value, or ratio is
  produced; NO_VERDICT of record is confirmed, not converted.

MU_BEARING = symbolic only: on the ratified chain (w_0, w_ch) =
  (r_0/(r_0+r_ch), r_ch/(r_0+r_ch)), both in Q ∩ (0,1) on the durable branch
  => mu != 0 PINNED SYMBOLICALLY (unchanged; rides D14 :10, not the arm);
  magnitude/sign open — the weights' ENTIRE value freedom is the one rational
  kappa_ch; w_0 w_ch = r_0 r_ch/(r_0+r_ch)^2 exact-rational FORM carried, not
  evaluated. Route ceiling: a completed arm could at most deliver kappa_ch
  exact-rational (then mu's weight-borne magnitude factor is exact-rational;
  Gate-5 sign burden untouched); a rejection outcome fires the DoR-008 falsifier
  => weights revert to the free simplex, mu UNDETERMINED (G2 Outcome B); mu = 0
  remains pinned by NO sealed outcome (G2 Outcome C; DoR-006 bar).

SEALS_VERIFIED = D14 b6e4116d… ; D13 f2a7838d… ; GOV3 1be5f6a7… ; SGV4 d4cdbb66… ;
  Q272 fc13f4e1… ; T2F db9e5104… ; BIDU b786db3a… ; G2 f7fd0c60… ;
  PANEL_OVER c8bde5c9… ; PANEL_UNDER 2bf4c1a2… ; M03 2cd1ffce… ; B1C a3bedc7e… ;
  KDD f1881511… — all 13 recomputed by shasum -a 256 at path BEFORE reading;
  sidecars matched where present; full digests in §1; no seal failed. C29 spans
  taken from the sealed B03 lane artifacts at path (STAGE8_DESC_B03_DARIO_V001.md
  :509; STAGE8_DESC_B03_CHECK_CODEX2_V001.md :185).

FORBIDDEN_IMPORTS = none — no scale, no imported GR, no faithfulness; no physical
  quantity evaluated; ranks/dimensions handled as structural integers and symbols
  only; no rank value selected, assumed, preferred, or shortlisted; no
  register/tracker/plan/road/ledger/lens file read; no git action.

MACHINERY_INVOKED = no (seal recomputation + file reads + exact symbolic algebra
  on sealed displays only; no numeric evaluation; no script needed — every integer
  cited is a sealed-text quotation).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
```
