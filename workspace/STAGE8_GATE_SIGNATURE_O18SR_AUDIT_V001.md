# STAGE 8 — GATE SIGNATURE (O18SR) — ADVERSARIAL AUDIT V001

DATE: 2026-08-15
COMMISSION: O18SR-AUDIT (GATE-SIGNATURE-AUDIT). POSTURE: **DEFAULT-REFUTE.** The build's
testimony carries ZERO weight; every claim below was re-derived at bytes from the sealed
sources, not read off the build's own displays.
STATUS: DETERMINATION ONLY. Nothing proposed, authored, adopted, or reclassified. No value,
no number, no measured-constant comparison. No git.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_GATE_SIGNATURE_O18SR_AUDIT_V001.md` — **ABSENT** at start of commission (`ls`
returned "No such file or directory"; the `.seal.sha256` sidecar likewise ABSENT).

---

## §0 — STEP 0: TARGET PROBE AND SEAL

```text
TARGET      STAGE8_GATE_SIGNATURE_O18SR_V001.md          PRESENT   41514 bytes, 730 lines
SIDECAR     STAGE8_GATE_SIGNATURE_O18SR_V001.md.seal.sha256   PRESENT   102 bytes
VERIFY      shasum -a 256 -c, executed FROM THE ARTIFACT'S OWN DIRECTORY   *** OK ***
            3bb7be90bdd47ade2b928e65bab29e0cd6b7951207136e2cbd85711bb5dd59b6
```

---

## §1 — SWEEP DECLARATION AND CUTOFF

```text
SWEEP CUTOFF: 2026-08-15, at the byte-states verified in §2.  Anything written after this
              artifact's seal is outside my sweep by construction.
ROOTS SWEPT:  (P) /Users/bgm/MB Work/alpha-program-archive/workspace
              (S) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
                  alpha_fundamental_record_action_cleanroom_v003
NEVER READ:   no register, tracker, road, plan, or continuation file was opened.
"Q-..." :     EXPECTED-UNLOCATABLE, not chased, carried as opaque tokens.

MY OWN SWEEPS, DECLARED (recursive, *.md, both roots):
  AS1  'admissible set (over|on|for|indexed|at)'
  AS2  'admissible (set|class|values)[^.]{0,60}(every|each|all|per |indexed)'
  AS3  word-boundaried case-insensitive vocabulary counts over G1, whole file
  AS4  'the admissible set' exact, over G1
```

**AS1 AND AS2 ARE THE SWEEPS THAT DECIDE THIS AUDIT.** They are the sweeps the build itself
declared as its SWEEP 4 and whose hit count it never displayed. §5 reports what they return.

---

## §2 — EVERY CONSUMED SEAL RE-VERIFIED INDEPENDENTLY

I did not accept the build's seal table. I re-ran `shasum -a 256 -c` on every artifact it
names, **from that artifact's own directory**, against that artifact's own sidecar.

```text
G1  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md            (P)   *** OK ***
G2  STAGE8_TREE_LOOP_VERSUS_ALLOW_REQUIRE_ADJUDICATION_EINSTEIN_V001.md (P) *** OK ***
G3  STAGE8_DESC_B03_DARIO_V002.md                                    (S)   *** OK ***
G4  STAGE8_DESC_B03_DARIO_V003.md                                    (S)   *** OK ***
G5  STAGE8_TASK5_EQ6_WHERE_CLAUSES_LANE2_V005.md                     (S)   *** OK ***
G6  STAGE8_AXN_BUILD_H1_INTEGRAND_DARIO_V001.md                      (S)   *** OK ***
G7  STAGE8_7A_REFINEMENT_BRIDGE_DARIO_V001.md                        (S)   *** OK ***
X   STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md  (the EXCLUDED one)  (P)   *** OK ***
```

**NO CONSUMED ARTIFACT FAILED ITS SEAL. NO CONSUMED ARTIFACT LACKED A SIDECAR.** The build's
§2 claim on this point re-derives exactly.

**ONE ACCOUNTING ERROR.** The build states G2 is "329 lines". `wc -l` returns 328 and the
file terminates with a newline, so 328 is the count. G1's "205 lines, 14469 bytes" is exact.
Immaterial to any typing; recorded because the build offered it as a byte fact.

---

## §3 — HUNT (A): THE SELECTOR CALL, ATTACKED

SELECTOR is the wanted answer and therefore the suspect one. I demanded of every selector
call an operative sentence with a stage or state on the INPUT side and a SET on the OUTPUT
side, and I was prepared to kill any call resting on a name, a purpose, or a paraphrase.

### 3.1 THE BUILD MAKES NO FULL SELECTOR CALL — VERIFIED

```text
OBJECT A  "the admissible set"   typed by the build as
          "SELECTOR-SHAPED ON ITS OUTPUT SIDE, CONDITION-INDEXED ON ITS INPUT SIDE",
          coined "A HALF-SELECTOR", with "NOT a stage. NOT a state." stated in the row.
VERDICT   *** THE CALL SURVIVES, BECAUSE IT IS NOT THE CALL THE HUNT WAS AIMED AT. ***
          The build DECLINES the selector label on the input side, states the declination
          in the typing row itself, and rejects SELECTOR-SHAPED as a verdict at its C7.
```

**AND IT DID NOT TYPE FROM THE NAME.** The trap here was live: the record HAS the word
"selector" and uses it for a rule aiming at a singleton. The build's I1 records the
collision and I tested the claim independently — **no object in the build's §4 is a site
containing the string "selector".** Objects A-D are all at G1, and G1 contains the string
nowhere. CONFIRMED.

### 3.2 THE SINGLETON TRAP — RE-RUN

The hunt requires: is the "set" ever displayed with more than one member? A singleton-valued
selector is a classifier.

```text
G1 :27, :54   BOTH operative sentences for Object A display the set AS A POINT.
              *** ON G1 ALONE, OBJECT A IS SINGLETON-VALUED AND WOULD BE A CLASSIFIER. ***
              The build does not hide this; its whole §5.3 exists to answer it.
```

I re-derived §5.3's four cardinality sites at bytes, from each artifact's own directory:

```text
G5 :606   "both laws explicitly permit empty admissible sets"              *** VERBATIM ***
G6 :124-5 "the situation is the / opposite, an unbounded admissible set"   *** VERBATIM ***
G7 :303   "a `k-1`-parameter family of admissible values"                  *** VERBATIM ***
G2 :53    "A loop is ONE of five admissible values, co-equal with four
           non-loop values."                                               *** VERBATIM ***
G1 :135   "ALLOW + RANGE   NO   NO — any selection is a choice."           *** VERBATIM,
           BUT SEE C-6 BELOW: THIS LINE CONTAINS NO ADMISSIBLE SET. ***
```

*** THE NON-SINGLETON FINDING SURVIVES ON THREE INDEPENDENTLY SEALED SITES THAT SAY
"admissible" IN THE QUOTED WORDS. OBJECT A IS GENUINELY SET-VALUED. THE SINGLETON TRAP DOES
NOT CLOSE. ***

### 3.3 THE THREE CLASSIFIER CALLS — RE-DERIVED FROM G1'S BYTES, NOT FROM THE BUILD

```text
OBJECT B  "Forcing"          G1 :54 "Forcing has ONE criterion (the admissible set is a
                             point)"; G1 :134 column head "FORCES?"; values displayed at
                             :135/:137/:142/:145 are NO / WITHIN THE CLASS / YES / YES.
                             INPUT one quantity (cardinality).  OUTPUT a label.  CLASSIFIER.
OBJECT C  the three shapes   G1 :54-58.  INPUT a candidate + a count of freedoms.  OUTPUT
                             one of three names.  CLASSIFIER.
OBJECT D  the four-case map  G1 :134-148.  INPUT (VERB, SOLUTION-STRUCTURE), both
                             propositions.  OUTPUT a label pair.  CLASSIFIER.
```

*** ALL THREE RE-DERIVE. I READ G1 DIRECTLY AND REACHED THE SAME THREE TYPINGS WITHOUT
CONSULTING THE BUILD'S ROWS. HUNT (A) AND HUNT (C) FIND NO CLASSIFIER-TYPING IMPOSED ON A
SET-VALUED OBJECT AT G1. ***

---

## §4 — HUNT (B) AND (G): CONTAMINATION AND QUOTATION INTEGRITY

### 4.1 SUBJECT CONTAMINATION — CLEAN, AND THE MARKS RE-DERIVE

The hazard: the build typing the registrar's analytical inventory instead of the record's
own content. The build was required to display how it told them apart, and it did (four
marks). I tested the two checkable ones at bytes.

```text
MARK 2  T14SR's own header, re-read at source, states its object VERBATIM as
        "the boundary between what the record FORCES (requires) and what it PERMITS
        (allows), displayed as an object of record"                    *** CONFIRMED ***
MARK 3  T14SR's header states "DATE: 2026-08-15"                       *** CONFIRMED ***
```

**AND THE DECISIVE TEST IS NOT THE MARKS — IT IS PROVENANCE OF THE ROWS.** I re-derived all
four typing rows (§3.3 above) from G1's bytes alone, having never opened T14SR's body.
**Every row traces to a sealed record sentence at a cited G1 line. NO ROW TRACES TO THE
INVENTORY.** HUNT (B): *** NO KILL. ***

### 4.2 QUOTATION INTEGRITY — EVERY QUOTATION CHECKED AGAINST ITS FULL SOURCE SPAN

The day's recurring defect is hedges dropped from inside quoted sentences. I checked each
quotation against the source span surrounding it, not merely against the quoted characters.

```text
G1 :53-58   the three-shapes enumeration                    *** VERBATIM, SPAN INTACT ***
G1 :25, :27-28  the criterion sentences                     *** VERBATIM ***
G1 :134-148 the four-case map, all fifteen lines            *** VERBATIM ***
G1 :22, :30-31  standing sentence + proposed correction     *** VERBATIM ***
G2 :51, :53  the five-valued carrier and its gloss          *** VERBATIM ***
G2 :144-145  the covector ray's limit                       *** VERBATIM ***
G2 :258-260  N2's "exactly one ray"                         *** VERBATIM ***
G3 :283-285  the withdrawal                                 *** VERBATIM ***
G5 :606 / G6 :124-125 / G7 :303                             *** VERBATIM ***
```

**THE TWO PLACES A HEDGE COULD HAVE BEEN DROPPED, AND WAS NOT:**

```text
H1  G3's withdrawal.  The build quotes the concessive clause IN FULL — "not because such a
    condition would be unwelcome, but because the object it would range over is not where
    the record locates the freedom."  *** THE HEDGE IS CARRIED. ***  Dropping it would have
    turned a scoped reframe into a flat refusal, which would have helped the build's §6.
    It did not drop it.
H2  G2 :144-145.  The build quotes the covector ray's "positive scale free / direction, not
    a magnitude" limit and uses it AGAINST its own population count.  *** CARRIED. ***
H3  G1's proposed correction is carried WITH its self-typing "(proposed, derived = false,
    principal's to adopt)", verified verbatim at G1 :29, and adopted nowhere.  *** CARRIED.
    *** The build rules between the standing sentence and the correction NOWHERE.
```

**ONE CONTEXT OMISSION, REPORTED AS A CORRECTION AND NOT AS A HEDGE-DROP (C-7).** The build's
unbounded-cardinality row quotes G6 :124-125 verbatim, but does not carry the source's own
adjacent self-limitation at G6 :122-123: `Not \`FAMILY\`: I cannot give a dimension or a
parametrization` and `Reporting a dimension would be fabrication.` The quoted words are
exact; the source's refusal to type its own set is not shown beside them.

HUNT (G): *** NO KILL. ONE CONTEXT CORRECTION. ***

---

## §5 — THE KILL: HUNT (D). THE CONSEQUENCE STEP'S ABSENCE CLAIM IS REFUTED AT CORPUS SCOPE

This is where the build fails, and it fails on the deliverable it argued hardest.

### 5.1 WHAT THE BUILD CLAIMS

```text
§5.1  G3 :283 is "THE RECORD'S ONE STATE-INDEXED ADMISSIBLE SET", called the "ONE EXCEPTION"
§5.2  "There is no stage index to be per-stage over" ; "NEITHER. IT IS PER-CONDITION."
§6.1  "Across the whole subject exactly one occurrence ANYWHERE states a domain"
      "THE DOMAIN IS NOT MERELY UNPOPULATED — IT IS UNFORMED."
§6.3  (a) a single map    ABSENT      (b) a domain for "every"    ABSENT
N6    admissible_set_is_stage_indexed = false | TYPE-R | EXECUTED
N7    a_single_selector_with_a_named_domain_exists = false | TYPE-R | EXECUTED
F6    "IT DOES NOT TOUCH §6.1, WHICH IS AN ABSENCE OF STRUCTURE, NOT OF MEMBERS."
```

### 5.2 WHAT SWEEPS AS1 AND AS2 RETURN — SEALED, PRIMARY ROOT, NO EXCLUDING MARK

```text
K1  STAGE8_TYPING_RULE_CANDIDATE_V001.md  :318-321   seal *** OK ***   root (P)
    NO "SR" suffix.  NO lane suffix.  NOT excluded by ANY of the build's four marks.
    VERBATIM:
      "D2  CELLS: the rule TYPES any connected admitted cell (Lemmas 1-2 give its
       admissible set at every b_1); its non-vacuous single-line sector is b_1 = 1,
       the entered shape.  At b_1 > 1 the rule types the SPACE and selects no class
       (§8 N1).  At b_1 = 0 the admissible set is empty (K-2)."
    *** THIS IS AN ADMISSIBLE SET INDEXED BY A NAMED INDEX, UNIVERSALLY QUANTIFIED OVER IT
    ("at EVERY b_1"), WITH THE SET DISPLAYED TAKING DIFFERENT VALUES AT DIFFERENT INDEX
    VALUES — EMPTY AT ONE, NON-VACUOUS AT ANOTHER, CLASS-FREE AT A THIRD. ***
    STATUS AT ITS OWN HEAD: "PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY", "[CLAIMED]".

K2  STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md  :769-773   seal *** OK ***   root (P)
    NO "SR" suffix.  NOT excluded by ANY of the build's four marks.
    QUOTED WITH ITS ENUMERATION ELIDED, BECAUSE MY COMMISSION BARS NUMBERS — the elision is
    marked and removes only a count and a parenthetical list of variant labels:
      "at the / full require set R-a..R-g the admissible class = the [...] variants
       ∪ every larger conforming complex ([...] unbounded upward)"
    The words "the admissible class = ... ∪ every larger conforming complex" and "unbounded
    upward" are VERBATIM and are the whole of what I rest on.
    *** A NAMED REQUIRE SET ON THE INPUT SIDE; AN ADMISSIBLE CLASS, EXPLICITLY NON-SINGLETON
    AND EXPLICITLY UNBOUNDED, ON THE OUTPUT SIDE. THE INPUT IS AN INDEX ("AT the full
    require set"), NOT MERELY A CONDITION IN HAND. ***
```

### 5.3 WHAT THIS DOES TO THE BUILD'S DELIVERABLE 4

```text
§5.1's "ONE EXCEPTION"                     *** REFUTED as a universal. ***  K1 is a second
                                           index-stated site, and unlike G3's it is NOT
                                           withdrawn anywhere in its own text.
§5.2's "no stage index to be per-stage      *** REFUTED at corpus scope. ***  K1 supplies an
over"                                      index and displays the set varying across it.
§6.1's "exactly one occurrence ANYWHERE"   *** REFUTED at bytes. ***
§6.1's "THE DOMAIN IS ... UNFORMED"        *** REFUTED as stated. ***  K1 forms one.
§6.3 (a) and (b) "ABSENT"                  *** HOLD AT G1 SCOPE ONLY. ***  Not corpus-wide.
N6, N7 as NAMED                            *** REFUTED. ***  Both are typed TYPE-R EXECUTED
                                           and neither carries a G1-only restriction in its
                                           name; their justification lines are G1-scoped but
                                           the negatives they assert are not.
F6's immunity claim for §6.1               *** FALSIFIED. ***  F6 concedes the sweep limit
                                           bounds §6.2 and expressly exempts §6.1.  The
                                           limit reaches §6.1 exactly.
```

*** AND THE SWEEP THAT WOULD HAVE CAUGHT THIS IS THE BUILD'S OWN. Its declared SWEEP 4
pattern ('admissible set|set of admissible|admissible values|...') hits K1 and K2 directly.
The build displayed a hit count for SWEEP 1 and SWEEP 2 and displayed NONE for SWEEP 4. THE
ONE SWEEP BEARING ON THE ABSENCE CLAIM IS THE ONE WHOSE RESULTS WERE NEVER SHOWN. ***

### 5.4 THE MITIGATION, STATED BECAUSE IT IS REAL

K1 is self-typed PROPOSED_NOT_ADOPTED / [CLAIMED]. It is sealed content of the record whose
STATUS is proposed. **The build's own discipline settles how that should have been handled:**
at its §3.4 it carried G1's proposed correction WITH its status rather than omitting it, and
said so. The same discipline applied to K1 yields DISPLAY-WITH-STATUS, not omission. With K1
displayed, the honest absence claim becomes "no ADOPTED single map with a named domain" —
materially weaker than, and different from, "the domain is UNFORMED".

K2 carries no proposed-status marking of that kind at the quoted lines.

### 5.5 A THIRD SITE, OUTSIDE THE BUILD'S DECLARED SUBJECT, RECORDED FOR COMPLETENESS

```text
K3  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md :814   seal *** OK ***   root (P)
    VERBATIM: "PASS  [X4] PAIRED SITES: require fixes the admissible SET (never a point);
    allow is the SELECTION within it (3/3)"
    *** A REQUIRE-SIDE MAP, A NAMED DOMAIN (PAIRED SITES), AND A POPULATED COUNT — i.e.
    PIECES (a), (b) AND (c) TOGETHER — AND A DIRECT CONTRADICTION OF G1's "the admissible
    set is a point" CRITERION, IN THE SAME ROOT. ***
    EXCLUDED BY THE BUILD UNOPENED, ON FILENAME ALONE (MARK 1).
```

**I DO NOT RULE THAT THE EXCLUSION WAS WRONG** — O11SR carries the registrar suffix and the
build's rule is stated in advance. I record that the build's own C2 concedes "I am recording
that I applied two standards", and that K3 is what the weaker standard cost. K3 does not
enter my verdict; K1 and K2 do, and neither carries any excluding mark.

---

## §6 — REMAINING HUNTS

### 6.1 HUNT (D), SECOND HALF — DOMAIN CALLED POPULATED WITHOUT MEMBERS EXHIBITED

*** NO KILL. *** The build's §6.2 exhibits four candidate members, each with a quoted status
line at a cited seal. I re-derived all four:

```text
R-L2b            G1 :154-156 and :163                                *** VERBATIM ***
S9-A successor   G1 :195-197 "is unbuilt; its require-status and root-structure
                 certificates cannot be attempted ... Nothing here starts it."  *** VERBATIM ***
marker/C_R       G1 :146-148                                         *** VERBATIM ***
Gate 4 ray       G2 :258-260, limited by G2 :144-145                 *** VERBATIM ***
```

The members ARE exhibited, and the build's incommensurability observation (different
carriers) re-derives: G1 :154-155 names a Hilbert-Schmidt bound, G2 :72-73 names readout
functionals on the first-opening record complex, G1 :55 names n freedoms, G3 :283 names
`State(B)`. Four different carriers, verified at four sites.

**BUT THE CONSEQUENCE STEP IS ARGUED WHERE IT MATTERS MOST.** §6.1's move from "two
occurrences, neither stating a domain" to "THE DOMAIN IS UNFORMED" is an inference from a
sweep, presented in display formatting. §5 above shows the sweep was incomplete. *** THE
DISPLAY WAS SOUND; THE UNIVERSAL DRAWN FROM IT WAS NOT. ***

### 6.2 HUNT (E) — RECLASSIFICATION, PROPOSAL, ADOPTION

*** NONE. *** Scanned for first-person proposal and adoption verbs and for
should-be-built constructions. The only hits are the build's own negations at :495 and
:726 ("not say whether any of this should be built"). The build displays G1's proposed
correction and rules between it and the standing sentence nowhere (verified §4.2 H3). Its
one coinage, "half-selector", is declared at I3 as not proposed as a class. **The build
does not reclassify K1, K2 or anything else — it never reached them.** An omission is not
a reclassification, and I type it as an omission.

### 6.3 HUNT (F) — LENS TOKENS AND NUMBERS

```text
LENS TOKENS   *** ZERO. ***  Scanned the whole build for lens vocabulary and the
              divergence/scale-dependent family.  No hit.
              AND THE NEAR MISS IS TO THE BUILD'S CREDIT: its G7 source carries the lens
              vocabulary two lines above the line it quotes.  It quoted :303 and left
              :301 alone.
NUMBERS       *** NONE. ***  Every decimal-shaped hit is a section reference or a sha256
              digest.  No value, no measured constant, no comparison.  Symbolic counts
              ("n conditions, n freedoms", a `k-1`-parameter family) are carried inside
              quotations as record text, not asserted as values.
```

### 6.4 FENCE-SCAN

```text
BUILD, HEAD :13   alpha_computed = false ; proof_authorized = false ;
                  kappa_record_computed = false                        *** PRESENT, ALL FALSE ***
BUILD, TAIL :729  identical                                            *** PRESENT, ALL FALSE ***
NO FENCE FLIPPED. NO FENCE TOUCHED. G1's own fence block at :10 likewise intact.
```

### 6.5 SYMBOLIC RE-RUN

*** NOT APPLICABLE, AND THIS IS ITSELF A FINDING. *** The build executes no symbolic step,
no computation, and no numeric derivation. Its only executed operations are string sweeps
and seal checks. I re-ran every one of them (§2, §3.2, §3.3, and AS1-AS4) in the shell
rather than in a venv, because there is nothing to import and no expression to evaluate.
The build's vocabulary sweep reproduced EXACTLY, count for count:

```text
stage 0 | step 0 | transition 0 | continuation 0 | next 0 | configuration 0 | state 1
set 2 | range 1 | class 8 | point 11 | unique 10 | grade 6 | label 0 | admissible 3
```

and "the admissible set" returns exactly two hits in G1, at :27 and :54, and the single
`state` hit is the hyphen-split `CORRECT-AS-` / `STATE` at :70-71. *** ALL CONFIRMED. ***

---

## §7 — IMPORT AUDIT

```text
J1  "SELECTOR" / "CLASSIFIER" / "input side" / "output side" / "signature"
    IMPORTED — commission vocabulary, mine and the build's alike.  I preserved the build's
    I1 distinction: the record's own "selector" means a rule aiming at a singleton and is a
    DIFFERENT object from the commission's set-yielding map.  I typed nothing from the word.
J2  "index" / "index-stated" / "index-quantified"          MY PHRASING for what K1's "at
                                                           every b_1" displays.  Not a
                                                           record term.  Nothing rests on
                                                           the phrasing that is not in the
                                                           quoted sentence.
J3  "half-selector"                                        THE BUILD'S COINAGE, carried in
                                                           quotation marks only, adopted
                                                           nowhere by me.
J4  K1/K2/K3 as labels                                     MY ORGANIZATION of three sweep
                                                           hits.  The sentences are the
                                                           record's; the labels are mine.
J5  NO PHYSICS PREMISE introduced.  No quantity computed, compared, or named as a value.
    b_1, K_dd, R-a..R-g, R-L2b, C_R, alpha and the covector ray are carried as OPAQUE
    TOKENS inside quotations only.
```

---

## §8 — TOY_SEPARATION

```text
THE SUBJECT IS ACTUAL SURFACE, NOT A TOY:
  - Every claim I confirm or refute is anchored to a line in a sealed file whose seal I
    verified myself with shasum -a 256 -c from that file's own directory.
  - I constructed NO example condition, NO specimen admissible set, NO illustrative index,
    and NO hypothetical selector to test the build against.
  - *** THE KILL IN §5 IS NOT AN ARGUMENT THAT A DOMAIN COULD EXIST. IT IS TWO SEALED
    SENTENCES, ALREADY ON DISK BEFORE THE BUILD RAN, THAT STATE ONE. *** Had AS1 and AS2
    returned only G3 :283, this audit would have said CONFIRMED and the build's §6 would
    have stood.
  - Where I could not resolve a question at bytes — whether O11SR is in-subject — I recorded
    the site and REFUSED to rule, rather than inventing a criterion to settle it (§5.5).
```

---

## §9 — CHOICE LEDGER

```text
C-1  I RE-DERIVED THE FOUR TYPINGS FROM G1 BEFORE READING THE BUILD'S §4 ROWS as evidence.
     Chose this over checking the build's rows against G1, which would have let its framing
     set my reading.  COST: more reading.  BENEFIT: §3.3's agreement is independent.
C-2  I RAN THE BUILD'S UNDISPLAYED SWEEP.  The build displayed hit counts for two of its
     four sweeps.  Chose to run the two content sweeps myself rather than accept an
     undisplayed result.  *** THIS CHOICE IS THE ENTIRE DIFFERENCE BETWEEN CONFIRMED AND
     CONFIRMED-WITH-CORRECTIONS HERE. ***
C-3  I DID NOT OPEN O11SR OR O12SR, and I did not open T14SR's body.  I read T14SR's header
     only, to test MARK 2 and MARK 3, exactly as the build declared it had.  COST: I cannot
     rule on whether K3 is in-subject, and I say so at §5.5 instead of ruling.
C-4  I TREATED K1's PROPOSED_NOT_ADOPTED STATUS AS A MITIGATION, NOT A DISQUALIFICATION.
     Chose this because the build itself displayed a proposed-status sentence with its
     status rather than omitting it (§3.4), and consistency demands the same for K1.
     COST IF WRONG: if proposed content is out-of-subject by some rule I have not found,
     §5.3's kill narrows to K2 alone — *** AND K2 ALONE STILL REFUTES §6.1's UNIVERSAL. ***
C-5  I GRADED THE CITATION ERRORS AS CORRECTIONS, NOT KILLS.  The quoted TEXT is verbatim in
     every case; only line numbers slip.  A wrong line number with right text misleads a
     re-checker but does not misstate the record.
C-6  I REPORTED THE WEAK CARDINALITY ROW even though it does not change the outcome.  The
     build's CARD >1 entry citing G1 :135 rests on the case-name "RANGE", not on a displayed
     admissible set; that line contains no such phrase.  Reported because the build presents
     it in a table of displays.
C-7  I GRADED THE G6 CONTEXT OMISSION AS A CORRECTION, NOT A HEDGE-DROP.  The commission
     flags hedges dropped from INSIDE quoted sentences.  G6's self-limitation sits in
     ADJACENT sentences, and the quoted sentence is intact.  Different defect, lesser grade.
C-8  I DID NOT LET THE BUILD'S HONESTY BUY IT THE VERDICT.  Its §5.4 near-miss report, its
     C2 two-standards confession, its F6 limit statement and its carried hedges are real and
     I confirm them — *** AND THEY DO NOT REPAIR §6.1, WHICH IS WRONG AT BYTES. ***
```

---

## §10 — FLAG BLOCK

```text
FA  MY OWN SWEEPS AS1/AS2 ARE PATTERN-BOUND.  A domain-stated admissible set phrased without
    the words I searched would escape them.  *** MY §5 KILL IS THEREFORE A LOWER BOUND ON
    THE BUILD'S OMISSION, NEVER AN UPPER ONE. ***
FB  I DID NOT OPEN ANY REGISTER, TRACKER, ROAD, PLAN OR CONTINUATION FILE.  Such a file
    could name further index-stated sites.  This bounds §5 in the same direction as FA.
FC  K1 AND K2 ARE SEALED AND I VERIFIED BOTH SEALS, BUT I READ ONLY THE CITED SPANS AND THE
    HEAD OF K1.  I do not know what the rest of either artifact says, and I make no claim
    about it.  In particular I do NOT assert that K1's index and G1's "admissible set" are
    the same object — *** I ASSERT ONLY THAT THE BUILD'S CLAIM "EXACTLY ONE OCCURRENCE
    ANYWHERE STATES A DOMAIN" IS FALSE AT BYTES, WHICH REQUIRES NO SUCH IDENTIFICATION. ***
FD  THE BUILD'S §4 TYPINGS AND MINE AGREE, AND WE READ THE SAME FOUR SENTENCES.  Agreement
    between two readings of one artifact is weaker evidence than it looks.  I state the
    typings as CONFIRMED at bytes, not as independently corroborated by a second source.
FE  I DID NOT RULE ON WHETHER O11SR IS IN-SUBJECT (C-3, §5.5).  If it is, the build's
    absence claim fails harder and its §6.2 population count is also wrong.  If it is not,
    my verdict is unchanged.  *** EITHER WAY THE VERDICT STANDS; THE MAGNITUDE MOVES. ***
FF  THE BUILD'S OWN INTERNAL DISAGREEMENT (G1's standing sentence vs its proposed
    correction) is displayed and ruled on by neither the build nor me.
```

---

## §11 — TYPED NEGATIVES

```text
M1  target_seal_verified_from_own_directory = true | TYPE-R | EXECUTED
M2  every_consumed_seal_verified = true | TYPE-R | EXECUTED
      All eight, each from its own directory.  None failed; none lacked a sidecar.
M3  build_committed_wishful_selector_typing = false | TYPE-R | EXECUTED
      It declines the selector label on Object A's input side IN THE TYPING ROW, coins
      "half-selector", and rejects SELECTOR-SHAPED as its verdict.  HUNT (A): no kill.
M4  build_selector_call_rests_on_a_name_or_purpose = false | TYPE-R | EXECUTED
      No §4 object is a site containing the string "selector"; G1 contains it nowhere.
M5  set_displayed_with_more_than_one_member = true | TYPE-R | EXECUTED
      Three sites whose quoted words contain "admissible": G5 :606, G6 :124-125, G7 :303.
      The singleton trap does not close.  Object A is genuinely set-valued.
M6  build_typed_the_registrar_analytical_inventory = false | TYPE-R | EXECUTED
      All four typings re-derived from G1's bytes with T14SR's body unopened.  No row
      traces to the inventory.  MARK 2 and MARK 3 confirmed verbatim at T14SR's header.
M7  classifier_typings_re_derive = true | TYPE-R | EXECUTED
      Objects B, C, D re-derived independently at G1 :54-58 and :134-148.
M8  hedge_dropped_from_inside_a_quoted_sentence = false | TYPE-R | EXECUTED
      Every quotation checked against its full source span.  G3's concessive clause, G2's
      scale-free limit and G1's "proposed, derived = false" self-typing are all CARRIED.
M9  fences_intact = true | TYPE-R | EXECUTED    Head and tail, all three false.
M10 lens_token_or_number_present = false | TYPE-R | EXECUTED
      Zero lens tokens; zero values.  The build quoted around a lens token two lines from
      the line it used.
M11 reclassification_proposal_or_adoption_present = false | TYPE-R | EXECUTED
M12 *** build_absence_claim_holds_at_corpus_scope = false | TYPE-R | EXECUTED ***
      K1 (TYPING_RULE_CANDIDATE :318-321, sealed, primary root, no excluding mark) states
      an admissible set "at every b_1" and displays it taking different values at different
      index values.  K2 (ACT2_CARRIER_PACKAGE_S9AD :769-773, sealed, primary root, no
      excluding mark) states an admissible class at a named require set, non-singleton and
      unbounded upward.  §6.1's "exactly one occurrence ANYWHERE", §5.1's "ONE EXCEPTION",
      §5.2's "no stage index", N6, N7 and F6's immunity claim are REFUTED as stated.
M13 build_absence_claim_holds_at_G1_scope = true | TYPE-R | EXECUTED
      Within G1, "the admissible set" occurs twice, both with a definite article and no
      stated domain.  The claim survives under a G1-only restriction it does not carry.
M14 new_class_required = false.  Nothing proposed, authored, adopted, or reclassified here.
```

---

## §12 — VERDICT

*** **CONFIRMED-WITH-CORRECTIONS** *** — overall.

```text
PER DELIVERABLE:

D1  SIGNATURE TYPING OF THE REQUIRE/ALLOW CONTENT (Objects A-D)      *** CONFIRMED ***
    Re-derived from G1's bytes without recourse to the build's rows.  Three classifiers,
    one set-valued substrate.  Quotations verbatim; vocabulary sweep reproduced count for
    count; the record's own "selector" sense kept apart from the commission's.

D2  THE THREE REQUIRE SHAPES TYPED AS A CLASSIFIER                   *** CONFIRMED ***
    G1 :54-58 puts a candidate and a count of freedoms on the input side and one of three
    names on the output side.  The record's own stated use — "a registry of forcing
    candidates" (:58) — is verbatim and is a classification table.

D3  THE SELECTOR-SHAPED PIECE, DISPLAYED          *** CONFIRMED-WITH-CORRECTIONS ***
    CONFIRMED: set-valued at three sealed sites; the reverse disguise check run and sound;
    the near-miss at :137 honestly reported.
    CORRECTED: §5.1's "ONE EXCEPTION" and §5.2's "no stage index" are overstated — see D4.
    CORRECTED: one cardinality row (G1 :135) displays no admissible set (C-6).
    CORRECTED: G6's row omits the source's adjacent refusal to type its own set (C-7).

D4  THE CONSEQUENCE                                                   *** REFUTED ***
    As stated, the determination is corpus-wide: "exactly one occurrence ANYWHERE states a
    domain"; "THE DOMAIN IS NOT MERELY UNPOPULATED — IT IS UNFORMED"; (a) and (b) ABSENT;
    N6 and N7 typed TYPE-R EXECUTED without a scope restriction; F6 expressly exempting
    §6.1 from the sweep limit.
    *** TWO SEALED ARTIFACTS IN THE BUILD'S OWN PRIMARY ROOT, CARRYING NONE OF ITS FOUR
    EXCLUDING MARKS, STATE A DOMAIN FOR AN ADMISSIBLE SET — ONE OF THEM QUANTIFYING OVER IT
    EXPLICITLY AND DISPLAYING THE SET VARYING ACROSS IT.  THE BUILD'S OWN DECLARED SWEEP 4
    HITS BOTH, AND ITS RESULT WAS NEVER DISPLAYED. ***
    The determination SURVIVES only under an explicit G1-only restriction, which it does
    not carry, and which would make it a much smaller claim than the one it states.

CITATION CORRECTIONS (text verbatim in every case, line numbers wrong):
    G3's foreclosure sentence is at :289-290; cited as :288 and within ":284-288".
    G6's quoted words span :124-125; cited as :125.
    G2 is 328 lines; stated as 329.
```

**WHAT SURVIVES INTACT.** The build's central finding — that the three "shapes" are buckets
and not shapes of a selector, and that the record's require/allow adjudication is a
classifier laid over a set-valued substrate — is CONFIRMED at bytes and was re-derived
independently. Its refusal to over-claim a selector, its carrying of hedges and proposed
statuses, its zero lens tokens and zero numbers, its intact fences, and its clean separation
from the registrar's inventory all hold.

**WHAT FAILS.** The one place the build stopped sweeping is the one place its claim was
universal. **THE TYPING IS RIGHT AND THE ABSENCE IS WRONG.**

**DISPLAY ONLY.** This audit types what exists and stops. Nothing proposed, nothing
authored, nothing adopted, nothing reclassified. No registry nominated, no index set
suggested, no member put forward, and no statement about whether anything should be built.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
