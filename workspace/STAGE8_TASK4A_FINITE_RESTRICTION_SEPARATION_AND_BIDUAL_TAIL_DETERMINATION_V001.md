# Stage 8 Task 4a Finite-Restriction Separation and Bidual-Tail Determination v001

Date: 2026-08-01
Lane: CODEX LANE 1
Task: 4a
Authority: DoR-011, construction and proof only
Register head consulted at completion: Q-246

## 0. Lead determination

**CATEGORY-RELATIVE SPLIT.** The finite-stage restrictions separate the
ratified norm C-star algebra and its standard Hilbert C-star module, under
both module-null and represented-left-multiplier kernel notions. The same
finite stages do **not** separate the bidual: the Q-245 moving tail has a
nonzero weak-star ultrafilter limit killed by every bidual restriction.

The physical retarded-Hessian separation theorem remains `NO_VERDICT`.
Neither `RetHess_phys` nor

```text
rho_H,N : RetHess_phys -> RetHess_N
```

is instantiated by Q-238 or Q-245. Q-238 supplies a finite dynamics
`PathCert_N^dyn` and explicitly leaves physical restrictions and the complete
path absent. Q-245 introduces `rho_H,N` conditionally to state the needed
criterion and types the maps uninstantiated. Q-246, which landed during this
run, independently confirms that the ratified sequential presentation has no
finite-complex restriction package.

The exact result is therefore:

```text
intersection_N ker(r_N^B) = {0}
  on B = A_F,+ tensor_min (A_F,-)^op;

intersection_N ker(q_N) = {0}
  on E_F = B_B;

intersection_N ker(Pi_N) = {0}
  on represented left multipliers L(B) in L_B(E_F);

intersection_N ker((r_N^B)**) != {0}
  on the bidual B**.
```

Here `r_N^B`, `q_N`, and `Pi_N` are the canonical **sequential-cylinder**
retractions derived in this artifact from the ratified zero-extension labels.
They are not the unbuilt physical `rho_H,N` and are not the absent
finite-incidence restriction package of Q-246.

The falsifier's pinning argument needs the third kind of statement on the
actual physical retarded-operator class. The norm-module proof reaches it only
if a future producer types every admissible `H_R` as a norm-continuous
adjointable left multiplier and proves that physical restriction is `Pi_N`.
The bidual counterexample shows why that typing is load-bearing: if the
physical class admits bidual, weak-star, distributional, or other
restriction-invisible content, a nonzero tail can survive.

```text
RATIFIED_NORM_CSTAR_SEPARATION = true | TYPE-P |
  premises: DoR-008

RATIFIED_STANDARD_MODULE_INNER_NULL_SEPARATION = true | TYPE-P |
  premises: DoR-008

RATIFIED_LEFT_MULTIPLIER_REPRESENTATION_SEPARATION = true | TYPE-P |
  premises: DoR-008

BIDUAL_SEPARATION = false | TYPE-R |
  test: free-ultrafilter weak-star limit of a moving-tail character sequence

PHYSICAL_RETHESS_SEPARATION = NO_VERDICT |
  prerequisite: RetHess_phys, rho_H,N, their topology/domain, and the T5
                restriction square are unbuilt

TAIL_REENTRY_CHANNEL_CLOSED = NO_VERDICT
```

Channel (a) therefore does not close. It is absent in the ratified norm
kinematic class, present in the mathematical bidual countermodel, and
undecided in the physical response class.

## 1. Preflight, currency, premises, and scope

### 1.1 Corrected preflight

```text
DOES_THE_SEPARATION_STATEMENT_EXIST = true
  Q-245 states it as the criterion needed for complete pinning.

ARE_PHYSICAL_RHO_H_N_INSTANTIATED = false | TYPE-R |
  test: Q-238 explicitly leaves physical Res_N and complete PathCert fields
        absent; Q-245 explicitly calls RetHess_phys and rho_H,N
        uninstantiated; raw-map T5 says the intertwiners remain TYPE-U

ARE_RATIFIED_SEQUENTIAL_CYLINDER_INPUTS_PRESENT = true | TYPE-P |
  premises: DoR-008 supplies Lambda_N, zero-extension, Lambda, A_F, the CTP
            tensor completion, and the standard Hilbert B-module

IS_THE_VERSION_CURRENT = true_through_Q_246
  Q-246 landed during this proof and bears on applicability: the
  finite-complex restriction package does not build from C0_008.
```

The relay preflight phrase that Q-238/Q-245 instantiate `rho_H,N` is refuted.
This does not prevent the structural theorem on the objects DoR-008 actually
ratifies.

### 1.2 Premises and imported mathematics

Every positive construction below is

```text
TYPE-P | premises: DoR-008.
```

The following standard mathematical facts are used and declared:

1. for a discrete abelian group `Lambda`, `C*(Lambda)` is canonically
   `C(Hom(Lambda,U(1)))`;
2. a continuous function vanishing on a dense subset is zero;
3. a group homomorphism induces a unital star-homomorphism of full group
   C-star algebras;
4. the bidual of a bounded star-homomorphism is a normal star-homomorphism;
5. the unit ball of a dual space is weak-star compact, and a bounded sequence
   has free-ultrafilter weak-star limits in the bidual; and
6. a weak-star-closed ideal in a von Neumann algebra is generated by a unique
   central projection.

These are imported functional-analysis results. No physical source topology,
physical response class, measure, contour, or domain is selected by using
them.

### 1.3 Current authorities

| Authority | SHA-256 | Load-bearing content |
|---|---|---|
| `DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md` | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | sequential labels, zero-extension, C-star/CTP completion, standard module, standing finite falsifier |
| `DECISION_OF_RECORD_011_TASK4_TRANSPORT_CONSTRUCTION_AUTHORIZED_2026-08-01_V001.md` | `1a54d50a53da30efe5af39b17d81a908eec75daa6ff450563edffed1d616a660` | construction/proof-only authorization |
| `STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md` | `76916244bdbcac7c2a6d4afae40f35127540d1d9e4cfc86fb72318506671161f` | exact ratified labels, algebras, module, representation |
| `STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DOR008_ASSEMBLY_AND_FIRST_RESTRICTION_TEST_V001.md` | `1467ab9850022931e68dc9ffa625d95099d8f6bb74d734dc9d0466f169bd00b6` | executed C0_008 and response/inverse exclusions |
| `STAGE8_ENVELOPE_FINITE_PATHCERT_DYNAMICS_SUBTRACE_CONSTRUCTION_V001.md` | `74a1f903ce79fc76a0f32a036a872b2d59b8944aa9e4211135d65ba8e9800db0` | finite PathCert_N^dyn only; complete path and physical restrictions absent |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | T5 physical rho_G/rho_H square and current inapplicability |
| `STAGE8_TASK4A_CORRELATOR_COMPLETION_ARROW_AND_RESTRICTION_PINNING_DETERMINATION_V001.md` | `5b9a4a8b000c313049caa71aff4235cc9eb4b0f98bb2af9931fd8820930ed856` | Q-245 criterion and moving-tail warning |
| `STAGE8_TASK2F_C0_008_FINITE_COMPLEX_RESTRICTION_PACKAGE_BUILD_AND_ARM_EXECUTION_ATTEMPT_V001.md` | `5515517ca2d1b48dd439fe97f2972292620132605767c4dd5de1007f67152c5a` | Q-246: sequential-cylinder analogues exist, finite-complex restriction package absent |

## 2. The ratified objects and the canonical sequential maps

### 2.1 Labels and one-branch algebra

The ratified presentation defines
`STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md:214-269`:

```text
Lambda_N := Z^N,
j_NM^Lambda(n_1,...,n_N) := (n_1,...,n_N,0,...,0),
Lambda := direct-sum_(j>=1) Z e_j.
```

For each `N`, the ordered direct-sum presentation canonically supplies the
truncation homomorphism

```text
p_N : Lambda -> Lambda_N,
p_N(sum_j n_j e_j) := (n_1,...,n_N).
```

It satisfies

```text
p_N compose j_N,infinity^Lambda = id_Lambda_N,
p_N = p_NM compose p_M,  N<=M.
```

No choice enters `p_N`: the same frozen ordered coordinates used by the
ratified zero-extension determine it.

Let

```text
A := A_F = C*(Lambda),
A_N := C*(Lambda_N).
```

The induced retraction is

```text
r_N^A := C*(p_N) : A -> A_N,
r_N^A(U_lambda) := U_(p_N lambda).
```

Under Gelfand duality,

```text
X := Hom(Lambda,U(1)) = product_(j>=1) U(1),
X_N := Hom(Lambda_N,U(1)) = U(1)^N,
i_N^X(z_1,...,z_N) := (z_1,...,z_N,1,1,...),

A = C(X),
A_N = C(X_N),
r_N^A(f) = f compose i_N^X.
```

This map is a sequential-cylinder restriction. It is not a map from the
completion to a finite cell complex.

### 2.2 CTP coefficient algebra

The ratified CTP algebra at v002 `:331-347` is

```text
B := A_F_CTP = A_+ tensor_min A_-^op.
```

Because `A` is commutative and nuclear,

```text
B = C(Y),
Y := X_+ x X_-.
```

Define

```text
B_N := A_N,+ tensor_min A_N,-^op,
r_N^B := r_N^A tensor r_N^(A,op) : B -> B_N.
```

In spectrum form, `r_N^B` restricts a continuous function on `Y` to

```text
Y_N := i_N^X(X_N)_+ x i_N^X(X_N)_-.
```

The family is consistent and each `r_N^B` is a surjective unital
star-homomorphism.

### 2.3 Standard Hilbert-module and represented kernels

The ratified Field 7 at v002 `:368-386` is

```text
E_F := B_B,
<x,y>_B := x* y,
L_b(x) := b x,
L : B -> L_B(E_F).
```

Define the module restriction

```text
q_N : E_F -> (B_N)_(B_N),
q_N(x) := r_N^B(x).
```

It preserves the coefficient-valued inner product:

```text
<q_N(x),q_N(y)>_(B_N) = r_N^B(<x,y>_B).
```

Two module kernel notions are therefore:

```text
K_N^vec := {x in E_F : q_N(x)=0},
K_N^ip  := {x in E_F : r_N^B(<x,x>_B)=0}.
```

They coincide at every stage because the Hilbert-module inner product on
`B_N` is definite.

For the represented left multipliers define

```text
Pi_N : L(B) -> L(B_N),
Pi_N(L_b) := L_(r_N^B(b)).
```

Since `B` is unital and `E_F=B_B`, every adjointable right-`B`-linear map is
left multiplication by its value on `1`. Thus

```text
L_B(E_F) is star-isomorphic to B
```

and the represented kernel theorem is exactly the coefficient-algebra
theorem.

These constructions do not define a restriction on every operator in the
joint module `E_C0=H_SR external-tensor B`, because a complete finite-stage
retraction of the source-record factor is not frozen. They also do not define
physical `rho_H,N`.

## 3. Counterexample-first run

### 3.1 The moving tail inside the norm C-star algebra

Use one branch and define

```text
v_m := U_(e_m),+ tensor 1_- - 1_B in B.
```

For every fixed `N`,

```text
r_N^B(v_m)=0 whenever m>N.
```

This is **eventual invisibility of a sequence**, not one element invisible at
all stages. Each term is detected at its own stage:

```text
r_m^B(v_m)=U_(e_m),+ tensor 1_- - 1_(B_m) != 0.
```

The sequence has no norm limit in `B`:

```text
||v_m||=2,
||v_m-v_n||=2 for m!=n.
```

It also has no weak cluster point in `B`. Let `h` be the product Haar state on
`B=C(Y)`. Then

```text
h(v_m)=-1 for every m.
```

Any weak cluster point `v` in `B` would satisfy `r_N^B(v)=0` for every `N`,
because each bounded `r_N^B` sends the tail eventually to zero. Section 4
proves that this forces `v=0`, contradicting `h(v)=-1`.

```text
MOVING_TAIL_PRODUCES_NONZERO_NORM_CSTAR_TAIL = false | TYPE-R |
  test: every term is detected at its own stage and the sequence has no norm
        or weak cluster point in B
```

This is the direct answer to the relay's first counterexample attempt in the
ratified norm topology.

### 3.2 The same moving tail in the bidual

View `B` canonically in its bidual `B**`. Choose a free ultrafilter `U` on the
stage labels and set

```text
v_U := weak-star-lim_(m->U) v_m in B**.
```

This element exists by weak-star compactness of the bounded ball. For every
fixed `N` and every functional `eta in (B_N)*`,

```text
< (r_N^B)**(v_U), eta >
  = lim_(m->U) < r_N^B(v_m), eta >
  = 0,
```

because the finite restriction is zero for all `m>N`. But

```text
<v_U,h> = lim_(m->U) h(v_m) = -1,
```

so `v_U` is nonzero. Therefore

```text
intersection_N ker((r_N^B)**) != {0}.
```

```text
BIDUAL_RESTRICTIONS_SEPARATE_THE_MOVING_TAIL_LIMIT = false | TYPE-R |
  test: v_U is killed by every bidual restriction and detected by the Haar
        state
```

No physical measure is installed by this argument. Haar is used only as one
bounded mathematical functional separating a nonzero bidual element; it is
not promoted to the program's state or descended measure.

### 3.3 Complete characterization of the bidual tail

Let

```text
S_cyl := union_N Y_N subset Y.
```

It is a dense Borel `F_sigma` subset of `Y`. The adjoint range

```text
(r_N^B)*(B_N*) subset B*=M(Y)
```

is exactly the finite regular measures concentrated on `Y_N`. Since the
`Y_N` are nested, the norm closure of their union is

```text
M_cyl(Y) := {mu in M(Y) : |mu|(Y without S_cyl)=0}.
```

Hence the whole bidual tail is exactly

```text
Tail_B**
  := intersection_N ker((r_N^B)**)
   = M_cyl(Y)^perp
   = {Phi in B** : Phi(mu)=0 for every mu concentrated on S_cyl}.
```

This is a weak-star-closed two-sided ideal. Therefore there is a unique
central projection `z_tail in B**` such that

```text
Tail_B** = z_tail B**.
```

This gives the requested generator and closure without claiming that one
chosen ultrafilter witness spans the whole tail:

```text
generator of the weak-star-closed ideal: z_tail;
closure notion: weak-star;
complete membership test: annihilation of M_cyl(Y);
explicit nonzero witness: v_U.
```

The product Haar state assigns zero measure to every `Y_N` and hence to
`S_cyl`, which is consistent with its detection of `v_U`.

### 3.4 Does the coherent p_ch block lie in the tail?

In the mathematical bidual countermodel, scalar multiplication gives

```text
p_ch v_U in Tail_B**,
```

which is nonzero whenever the scalar is nonzero. Thus finite restrictions do
not, by themselves, forbid **some** p_ch-dependent bidual tail.

This is not an identification of Q-243's coherent one-point block with
`v_U`. The coherent block is a first-variation source covector; the pinning
target is a retarded second-variation operator. No sealed map lifts the former
to the latter, and neither is typed as an element of `B**`.

```text
BIDUAL_FINITE_RESTRICTIONS_EXCLUDE_P_CH_DEPENDENT_TAIL = false | TYPE-R |
  test: p_ch v_U is a nonzero bidual-tail element when p_ch is nonzero

Q243_COHERENT_BLOCK_HAS_NONZERO_PHYSICAL_TAIL_COMPONENT = NO_VERDICT |
  prerequisite: the completed source-germ, stationary reduction, RetHess_phys,
                and coherent-to-retarded map are unbuilt
```

## 4. Separation proof in the ratified norm completion

### 4.1 Density of the finite-stage spectra

Let

```text
X_cyl := union_N i_N^X(X_N).
```

Every basic product-topology neighborhood in `X` constrains only finitely
many coordinates. Choosing `N` beyond those coordinates and setting the tail
to the identity produces a point of `X_cyl` in that neighborhood. Therefore
`X_cyl` is dense in `X`.

The same argument on both CTP branches gives

```text
S_cyl = union_N Y_N dense in Y=X_+ x X_-.
```

### 4.2 Coefficient-algebra separation

Let `b in B` satisfy `r_N^B(b)=0` for every `N`. Under `B=C(Y)`, the continuous
function `b` vanishes on every `Y_N`, hence on dense `S_cyl`, hence on all of
`Y`. Thus `b=0`.

```text
intersection_N ker(r_N^B) = {0}.
```

No weakening to a dense subalgebra occurred: the theorem is proved on the
full ratified norm completion `B`.

### 4.3 Inner-product-null module separation

If `x in E_F=B_B` satisfies

```text
r_N^B(<x,x>_B)=0 for every N,
```

then `r_N^B(x* x)=0` for every `N`. Coefficient-algebra separation gives
`x* x=0`, so `x=0`. Equivalently, if every `q_N(x)=0`, then `x=0`.

Therefore

```text
intersection_N K_N^vec = intersection_N K_N^ip = {0}.
```

The two module notions do not split on the ratified standard module.

### 4.4 Representation-kernel separation

Let `L_b in L_B(E_F)` satisfy `Pi_N(L_b)=0` for every `N`. Faithfulness of
left multiplication on `B_N` gives `r_N^B(b)=0` for every `N`, and
coefficient-algebra separation gives `b=0`. Hence `L_b=0`.

```text
intersection_N ker(Pi_N) = {0}.
```

This is the kernel notion nearest to the falsifier's operator-pinning need,
but it applies only to the represented left-multiplier class ratified in C0.

## 5. Why the physical pinning theorem still does not follow

### 5.1 The physical maps are not Q-238's maps

Q-238 states at
`STAGE8_ENVELOPE_FINITE_PATHCERT_DYNAMICS_SUBTRACE_CONSTRUCTION_V001.md:12-51`
that it constructs only the finite lower-row dynamics path. At `:387-394` it
lists physical `Res_N` and the complete PathCert fields as absent.

Q-245 states at
`STAGE8_TASK4A_CORRELATOR_COMPLETION_ARROW_AND_RESTRICTION_PINNING_DETERMINATION_V001.md:496-539`
that `rho_H,N` is conditional on a future physical package and that both
zero-tail and nonzero-tail verdicts are `NO_VERDICT` because `RetHess_phys`
and `rho_H,N` are uninstantiated.

Raw-map T5 at
`STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md:960-996`
requires the complete physical package, finite-to-complete intertwiners,
finite raw `G`, an independently fixed `E_R^fin`, and identical domain data.
It remains TYPE-U.

Q-246 adds that even the finite-complex restriction package on C0_008 does not
build from the sequential signature. Its six components remain TYPE-U.

### 5.2 Which kernel notion the falsifier needs

The T5 square's right vertical arrow acts on retarded Hessian **operators**.
It therefore needs representation/operator separation:

```text
intersection_N ker(rho_H,N) = {0}
  on the declared admissible RetHess_phys class.
```

Hilbert-module vector separation is not enough. To transport the proved
`Pi_N` theorem, a future producer must certify all of:

1. `RetHess_phys` is a subspace of norm-continuous adjointable left
   multipliers on the ratified standard module;
2. physical `rho_H,N` is the canonical `Pi_N` on that subspace;
3. the finite-complex/source restriction package of Q-246 exists and agrees
   with the sequential restriction where both apply;
4. measure, contour, contacts, boundary data, and domains are preserved; and
5. inversion and retarded extraction commute with those restrictions.

No item is supplied by DoR-008. Conversely, if `RetHess_phys` is permitted to
live in the bidual/self-dual completion, a distributional dual, or another
weak completion, the norm proof does not apply and the explicit bidual tail
shows that density of finite spectra alone is insufficient.

```text
NORM_MODULE_PROOF_TRANSPORTS_TO_PHYSICAL_RETHESS = false | TYPE-U |
  would-build: items 1-5 above

BIDUAL_COUNTEREXAMPLE_IS_THE_PHYSICAL_TAIL = NO_VERDICT |
  prerequisite: no sealed object types RetHess_phys as B** or admits z_tail

PHYSICAL_TAIL_SPACE_ZERO = NO_VERDICT
PHYSICAL_TAIL_SPACE_NONZERO = NO_VERDICT
```

### 5.3 Consequence for p_ch

The finite direct mixed retarded block remains zero and p_ch-free by Q-243.
The ratified norm C-star/module completion has no sequential-cylinder tail.
But the physical response class is not the ratified kinematic module, and a
larger mathematically natural completion has an explicit tail.

The lawful conclusion is:

> **Finite restrictions close tail re-entry only for norm-continuous
> adjointable left-multiplier content on the ratified standard module. They do
> not yet close tail re-entry for the physical retarded Hessian.**

The stationary-background channel from Q-245 remains independently open.

## 6. Topology and category table

| Object class | Limit/restriction topology | Separation | Standing |
|---|---|---|---|
| `B=C(Y)` | C-star norm / continuous functions | holds | `TYPE-P | DoR-008` |
| `E_F=B_B` | Hilbert-module norm | holds under vector and inner-product kernels | `TYPE-P | DoR-008` |
| `L_B(E_F)=L(B)` | represented left-multiplier norm | holds | `TYPE-P | DoR-008` |
| `B**` | weak-star bidual | fails; `Tail_B**=z_tail B**` | `TYPE-R` counterexample |
| `RetHess_phys` | not instantiated | `NO_VERDICT` | `TYPE-U` prerequisite stack |

This table is the result's category boundary. No row is transported into
another by shared vocabulary.

## 7. Kill-passes

### 7.1 No theorem weakened to a dense subspace

The positive theorem is on the full norm algebra `B`, full standard module
`E_F`, and full represented left-multiplier class. Density is used only to
prove equality of continuous elements.

### 7.2 No topology changed mid-proof

Norm/weak convergence inside `B` and weak-star convergence inside `B**` are
separate sections with separate verdicts. The bidual witness is never called
an element of `B`.

```text
TOPOLOGY_CHANGED_MID_PROOF = false | TYPE-R |
  test: every limit and closure above names B-norm, B-weak, or B**-weak-star
```

### 7.3 No proxy restriction promoted

The canonical `r_N^B` acts on the ratified sequential-character algebra. It
is not promoted to the physical `rho_H,N`, the finite-complex package, or the
T5 square. Q-246 is carried rather than bypassed.

### 7.4 No physical state or measure imported

Haar appears only as a separating functional in a mathematical
counterexample. It is not adopted as `rho_pre`, the U3 descended measure, or
the CTP physical state.

### 7.5 No p_ch identity smuggled

`p_ch v_U` proves that a p_ch-dependent bidual tail is mathematically
possible. It does not identify Q-243's first variation with the retarded
Hessian or assert physical re-entry.

## 8. Bearing symbol collisions

### 8.1 `rho_H,N` versus finite state restrictions

`rho_H,N` in T5 is an operator restriction on retarded Hessians. It is not
Q-238's density matrix `rho_N`, record state `omega_N`, or `Rec_N` marginal.

### 8.2 `r_N` versus `R_N`

This artifact uses lowercase `r_N^A/r_N^B` for canonical sequential algebra
retractions. Q-238 and Q-201 use uppercase `R_N` for the finite record algebra
`tensor_(j=1)^N M_3(C)`. They are different objects.

### 8.3 `H_R` versus `H_SR`

`H_R` is the physical retarded Hessian/operator sought downstream. `H_SR` is
the inherited source-record Hilbert carrier in C0. No carrier name supplies a
response operator.

### 8.4 `B` versus `B_ind`

`B=A_F_CTP` is the ratified coefficient C-star algebra. It is not the induced
response `B_ind(K)` and no value-path object is computed here.

## 9. Scope and bounded searches

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
```

Excluded:

```text
.git, binary/media payloads, archive mirrors as independent authority,
superseded variants except for declared lineage, and
a32_holdout/custodian_private (not entered, listed, searched, opened, or read).
```

Word-boundaried, case-insensitive searches included:

```text
PathCert | rho_H | physical Res_N | finite restriction map |
restriction intertwiner | cylindrical | Lambda_N | zero-extension |
C*(Lambda) | Hilbert B-module | representation kernel | inner-product zero |
weak-star | bidual | moving tail | annihilator | finite-complex restriction
```

The negative about physical maps is bounded to the current governing chain:
DoR-008, C0_008, Q-238, Q-245, Q-246, Item-2 physical source germ, and raw-map
T5. It is not a claim that no similarly spelled map exists in unrelated
historical material.

## 10. Answers in relay order

1. **Exact theorem and kernels.** The ratified sequential data canonically
   produce `r_N^B`, module maps `q_N`, and represented left-multiplier maps
   `Pi_N`. Inner-product-null and vector-null kernels coincide on `E_F`; the
   represented kernel is the one nearest the falsifier. Physical `rho_H,N`
   is not instantiated.
2. **Counterexample first.** The moving tail fails as a counterexample in the
   norm C-star algebra but succeeds in the bidual. The exact bidual tail is
   `M_cyl(Y)^perp=z_tail B**`. A p_ch-dependent bidual tail exists
   mathematically; the actual coherent block's physical component is
   `NO_VERDICT`.
3. **Proof.** Sequential finite spectra are dense in the ratified compact
   spectrum, so continuous coefficients, module vectors, and represented
   left multipliers are separated by all finite restrictions.
4. **Typed ruling.** Norm/module/left-multiplier separation is
   `TYPE-P | DoR-008`; bidual separation is refuted `TYPE-R`; physical
   retarded-Hessian separation is `NO_VERDICT` because its class and maps are
   TYPE-U.
5. **Kill-passes.** The full norm theorem was proved, each topology was named,
   no proxy was promoted, and Q-246 was incorporated.

## 11. Final flags and custody

```text
SEQUENTIAL_CYLINDER_RETRACTIONS_CONSTRUCTED = true | TYPE-P |
  premises: DoR-008

NORM_CSTAR_SEPARATION_PROVED = true | TYPE-P |
  premises: DoR-008

STANDARD_MODULE_INNER_NULL_SEPARATION_PROVED = true | TYPE-P |
  premises: DoR-008

LEFT_MULTIPLIER_REPRESENTATION_SEPARATION_PROVED = true | TYPE-P |
  premises: DoR-008

BIDUAL_SEPARATION_REFUTED = true | TYPE-R |
  test: nonzero v_U in intersection_N ker((r_N^B)**)

BIDUAL_TAIL_CHARACTERIZED = true | TYPE-P |
  premises: DoR-008 and the declared standard bidual construction |
  content: Tail_B**=M_cyl(Y)^perp=z_tail B**

PHYSICAL_RHO_H_N_INSTANTIATED = false | TYPE-U |
  would-build: the T5 complete physical package and finite-to-complete
               retarded-Hessian restriction intertwiners

PHYSICAL_RETHESS_SEPARATION_PROVED = false | TYPE-U |
  would-build: RetHess_phys typing, rho_H,N, the Q-246 finite-complex package,
               domain-preserving T5 square, and either a norm-class theorem
               or a tail-exclusion theorem in the chosen physical category

TAIL_REENTRY_CHANNEL_CLOSED_FOR_RATIFIED_NORM_LEFT_MULTIPLIERS = true | TYPE-P |
  premises: DoR-008

TAIL_REENTRY_CHANNEL_CLOSED_FOR_PHYSICAL_RETHESS = NO_VERDICT

Q_246_CARRIED = true
Q_246_EFFECT = sequential restriction is not finite-complex or physical
               restriction

FENCE_BLOCKED_STRUCTURAL_RESULT = false
MACHINERY_APPEAL = NOT_TRIGGERED

alpha_computed = false [TERMINAL_FENCE_DECLARATION]
proof_authorized = false [TERMINAL_FENCE_DECLARATION]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION]
coupling_evaluation_authorized = false [TERMINAL_FENCE_DECLARATION]
production_authorized = false [TERMINAL_FENCE_DECLARATION]
```

Custody under `LOCKED_PROCESS.md`: this lane seals this append-only artifact,
verifies its sidecar, mirrors only the artifact and sidecar to the archive
workspace, reports, and stops. It does not register, commit, or push.
