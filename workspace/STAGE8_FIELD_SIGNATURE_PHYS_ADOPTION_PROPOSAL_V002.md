# Stage 8 FIELD_SIGNATURE_PHYS Adoption Proposal V002

Date: 2026-08-02  
Lane: CODEX LANE 2  
Task: 4a, relay 373  
Register head at start: Q-290  
Status: **PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-015 RESERVED)**

This is the adjudicated merge of the two Q-289/Q-290 race arms.  It is V001
reduced by every premise-conditional component proved by the derivation arm.
No derived component is re-authored.  The remaining external realization
fields are proposals, not TYPE-P facts.

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
DOR_015_STATUS = RESERVED

MERGE_RESULT = SIX AUTHORED PHYSICAL STRUCTURES
               + FOUR DERIVED/PREMISE-CONDITIONAL FAMILIES
               + REQUIRED GOVERNANCE

AUTHORED_V001_FIELD_COUNT = 9
V002_INDEPENDENT_AUTHORED_PHYSICAL_STRUCTURE_COUNT = 6
DERIVED_FAMILY_COUNT = 4
GOVERNANCE_WRAPPER_COUNT = 1

FIELD_SIGNATURE_PHYS_V002_ADOPTION_STATUS = PENDING_DOR015_PRINCIPAL_RATIFICATION
```

The four derived families are retained with their original standing:

```text
D1 rooted signed local incidence/orientation       TYPE-P | DoR-013
D2 finite tensor locality, zero-extension, E_post  TYPE-P | DoR-009/013
D3 finite CTP orientation/metric/reality/symmetry  TYPE-P | DoR-008 + sealed conventions
D4 separated T_cyl norm completion/retractions     TYPE-P | DoR-008 + T_cyl theorem

plus Gate-4 unit-weight covariant incidence         DERIVED_OVER_ENUMERATED_FAMILY
```

The six authored structures are:

```text
A1 LocInc4_ext       external physical-background family
A2 Real_ext          record-to-external causal/support realization family
A3 Conn_ext          external compact connection carrier and realization
A4 Rig_ext           localized physical source rigging and source intertwiners
A5 AlgField_phys     algebraic physical field-coordinate representation
A6 BilocClass_phys   separated physical bilocal/raw-G carrier class
```

Two V001 entries are no longer independent proposal fields:

```text
V001 F5 U1_phys -> REMOVED FROM AUTHORSHIP; D3 extends pointwise by theorem
V001 F8 Emb_cyl -> CONDITIONAL MATHEMATICAL CONSEQUENCE of A2/A3 and D4
V001 F9 ProvFam -> REQUIRED GOVERNANCE, not physical authorship
```

The physical raw-`G` image, measure, contour, boundary/contact reduction,
unbounded closure, stationary solution, response, and evaluation remain
unbuilt operations.  This proposal supplies their field-signature carriers;
it does not perform them.

---

## 1. Preflight, custody scope, and authorities

### 1.1 Preflight

```text
DOES_THE_OBJECT_EXIST = true | this merged V002 proposal
IS_THE_VERSION_CURRENT = true | through Q-290 at construction start
ARE_THE_INPUTS_PRESENT = true |
  authored V001, derivation arm, Q-288 ledger, and raw-G contract all present
```

### 1.2 Roots entered and exclusions

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha-program-archive/cleanroom_output
```

```text
a32_holdout/custodian_private/                   NOT ENTERED
alpha/kappa/coupling/root/scale evaluation       NOT PERFORMED
physical rank-pair selection or ratio evaluation NOT PERFORMED
measured-constant comparison                     NOT PERFORMED
physical background/realization member selection NOT PERFORMED
register/plan/tracker/git/commit/push             NOT TOUCHED
```

### 1.3 Frozen authorities

| Authority | SHA-256 | Load-bearing use |
|---|---|---|
| `STAGE8_FIELD_SIGNATURE_PHYS_ADOPTION_PROPOSAL_V001.md` | `d2d88dc5916a75ff172d26873678f510efe6868fe2ca99ee0acdc636b8f99f24` | Q-289 nine-field authored arm and its proposal-level doors |
| `STAGE8_TASK4A_RECORD_SURFACE_TO_PHYSICAL_FIELD_SIGNATURE_DERIVATION_AND_BETA_GAP_ATTACK_V001.md` | `65e4dd6a6e2926c9c100edb162f57352311e73438f23dd19509dda0403e2e4f6` | Q-290 four derived families, ten-item residue, unit-modulus no-go, and write-shadow door |
| `STAGE8_TASK4A_P5_MAXIMAL_TRANSPORT_CHAIN_AND_DEPENDENCE_ACCOUNTING_PACKAGE_V002.md` | `31a738ec17696ea01e1cb6a6ee7a37a29e6c0ca24d6fb8cc050d06aed32a583f` | Q-288 mandatory class-formation doors and six physical accounts |
| `STAGE8_TASK4A_RAW_G_SOURCE_TO_PHYSICAL_FIELD_LIFT_CONSTRUCTION_AND_TCYL_VERDICT_V001.md` | `3ef35b34cfdeb7f7b8381dce65a37d026769f29f4e2bbc6184fb581b5a394024` | Q-287 exact lift contract, port distinctions, and bilocal counterexample |
| `STAGE8_RAW_CORRELATOR_TO_RETARDED_HESSIAN_MAP_SPEC_V001.md` | `57c06fcbedf4a486b87b42c02e6d13a92096ac6a56751d3ae5fd55f4b11deb57` | raw-`G` domain/codomain and retarded map (`:641-819`) |
| `STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md` | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` | D3 finite CTP signature (`:131-357`) |
| `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | D1/D2 ratified primitive tuple and maps (`:603-671`) |
| `STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md` | `430f09715146cc03dabb0e349c422ae2499cff893d4e46b490fc0870954d1cb4` | D4 `T_cyl` and unbuilt `T_ref/T_phys` (`:213-299`) |
| `STAGE8_TASK4D_CELLULATION_INDEPENDENCE_OD3_VERDICT_INVARIANCE_THEOREM_V001.md` | `f20639a6a1d5c8d73312bd646ceb2e0c74059c6f6206dca04032289f307e217b` | exact nonfactorization deficits (`:376-409`) |

### 1.4 Symbol and identity fences

1. Gen_Omega `S_inc`, Gate-4 `D_e`, and a physical exterior derivative are
   different maps.  V002 relates the first two only through the proposed A2/A3
   commuting seam; it does not identify them.
2. `E_post` is the finite endpoint-charge orientation.  The global causal
   support relation in A1/A2 remains authored.
3. `T_cyl` is the separated character-algebra completion.  It is not the full
   algebraic field representation A5 or the raw-`G` class A6.
4. The finite CTP index `(a,lambda)` and physical compound index `(a,mu,x)`
   are not synonyms.  A4 supplies the proposed pullback between them.

---

## 2. V001 nine-field subtraction ledger

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
```

| V001 field | Merge verdict | Removed derived content | Minimal retained remainder |
|---|---|---|---|
| F1 `LocInc4` | **AUTHORED** | none: local write orientation does not derive global causal orientation or background | A1 external smooth locally covariant background family |
| F2 `Real_inc` | **PARTIALLY DERIVED** | `K_rat`, root, signs, local adjacency, finite tensor locality, zero-extension, and `E_post` are D1/D2 | A2 injective external realization, causal paths/supports, global coherence, and finite holonomy-surjectivity family |
| F3 `Conn_U1` | **PARTIALLY DERIVED** | compact U(1), unit-weight graph transport, vertex rephasing, and unit-modulus holonomy are D1/D2 plus Gate 4 | A3 external connection one-form carrier, physical gauge quotient, currents, and record-to-connection intertwiner |
| F4 `Rig_src` | **PARTIALLY DERIVED** | finite cell labels, support adjacency, branch grammar, and zero-extension are D1-D3 | A4 localized orthonormal source family, frame norm, Hilbert/rigged completion, and `K_J,K_R` |
| F5 `U1_phys` | **DERIVED / REMOVED** | all branch orientation, metric, reality, source symmetry, and ordering are D3; pointwise extension is a product theorem once A1/A5 exist | no independently authored field; only Seam S4/S5 |
| F6 `AlgField_phys` | **AUTHORED** | backward-opposite branch and CTP involution are D3 | A5 physical field-coordinate algebra and multiplication representation |
| F7 `BilocClass_phys` | **AUTHORED** | symmetric/CTP-real restriction is D3; finite source trace class already exists on the source side | A6 transported physical bilocal class, bounded-bilinear raw class, connected-subtraction codomain, and density premise |
| F8 `Emb_cyl` | **PARTIALLY DERIVED / NO INDEPENDENT CHOICE** | `T_cyl`, norm separation, and retractions are D4 | the physical holonomy map/restrictions arise by theorem from A2/A3/A4; no separate authored toggle |
| F9 `ProvFam_phys` | **GOVERNANCE / REMOVED FROM PHYSICAL AUTHORSHIP** | DoR provenance, family discipline, finite falsifier, and Q-288 doors are already mandatory | V002 freeze/certification wrapper only |

No derived component is retained in the A1-A6 choice table.  Derived data may
appear in an authored field's **domain** or **compatibility condition**; that
does not make it authored again.  For example, A2 consumes the existing
signed write complex and authors only its external realization.

```text
V001_AUTHORED_CTP_DUPLICATION_REMOVED = true | STRUCTURAL
V001_INDEPENDENT_EMB_CYL_CHOICE_REMOVED = true | STRUCTURAL
V001_GOVERNANCE_COUNTED_AS_PHYSICS = false | TYPE-R |
  test: F9 adds no carrier, field equation, or physical value

V002_AUTHORED_STRUCTURE_COUNT = 6 | PROPOSAL-LEVEL COUNT
```

---

## 3. Derived foundation — retained without weakening

### 3.1 D1: rooted signed local write

On the ratified one-cell carrier:

```text
S_inc v_0    = v_1,
S_inc v_1    = v_0,
S_inc v_perp = -v_perp.
```

The local root, successor, complement sign, and one-cell adjacency remain
exactly Q-290's TYPE-P claims (`...BETA_GAP_ATTACK_V001.md:187-214`).  V002
does not upgrade their pre-DoR standing.

```text
D1_LOCAL_ROOTED_SIGNED_WRITE_EXISTS = true | TYPE-P |
  premises: DoR-013
```

### 3.2 D2: finite locality, orientation, and character covariance

The ratified law supplies finite tensor-factor locality, exact `N<=M`
zero-extension, faithful compact-character covariance, and `E_post`.  It does
not supply global causal order or cross-cell propagation
(`...BETA_GAP_ATTACK_V001.md:216-240,345-377`).

```text
D2_FINITE_LOCALITY_ZERO_EXTENSION_EXISTS = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013
D2_EPOST_ENDPOINT_ORIENTATION_EXISTS = true | TYPE-P |
  premises: DoR-009
D2_GLOBAL_CAUSAL_SUPPORT_DERIVED = false | TYPE-R |
  test: Q-290 stage-inclusion/causal-order distinction
```

### 3.3 D3: finite CTP convention layer

```text
B_CTP={+,-}, epsilon=(+1,-1), eta_CTP=diag(+1,-1),
Theta_F=branch exchange plus adjoint,
I_008=(a,lambda),
R symmetric and Theta-real.
```

The Keldysh order and ordered retarded block are unchanged
(`...BETA_GAP_ATTACK_V001.md:262-284`).  V002 adds no
branch sign, normalization, or second doubling.

```text
D3_FINITE_CTP_SIGNATURE_EXISTS = true | TYPE-P |
  premises: DoR-008 and sealed U1 conventions
```

### 3.4 D4: separated cylindrical completion

```text
Lambda=direct-sum_j Z e_j,
A_F=C*(Lambda),
B=A_F,+ tensor_min A_F,-^op.
```

The norm-dense finite cylinders, canonical retractions, separation, and zero
common-restriction tail are inherited exactly from `T_cyl`
(`...BETA_GAP_ATTACK_V001.md:286-302`).

```text
D4_TCYL_SEPARATED_NORM_COMPLETION_EXISTS = true | TYPE-P |
  premises: DoR-008 and T_cyl theorem
```

### 3.5 Gate-4 invariant

Gate 4 supplies unit-weight covariant incidence modulo vertex gauge, over its
enumerated family.  It supplies no physical base or one-form realization.

```text
GATE4_UNIT_WEIGHT_COVARIANT_INCIDENCE_EXISTS = true |
  DERIVED_OVER_ENUMERATED_FAMILY
```

---

## 4. Exact V002 proposed tuple — authored remainder only

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

FIELD_SIGNATURE_PHYS_V002_prop := (
  derived_foundation=(D1,D2,D3,D4,Gate4_invariant),
  authored_remainder=(A1,A2,A3,A4,A5,A6),
  generated_consequences=(C_Emb,S1-S7),
  governance=Gov_V002
).
```

### 4.1 A1 — `LocInc4_ext`: external physical-background family

Retain V001's complete background family, not a selected member:

```text
B=(M,g,o,t;P_M),
```

where `M` is smooth and paracompact, `g` is globally hyperbolic with the live
four-dimensional Lorentz signature, `o,t` are orientations, and `P_M` is a
principal U(1) bundle.  Morphisms preserve the background, orientations,
causal relation, and bundle type.

Only the external existence/category is authored.  The compact comparison
group, finite endpoint orientation, and record incidence are not part of A1's
price.  No member `(M,g)` is selected.

```text
A1_LOCINC4_EXT_PROPOSED = true [PROPOSAL FIELD]
A1_EXTERNAL_BACKGROUND_DERIVED_FROM_WRITE_SURFACE = false | TYPE-R |
  test: Q-290 exact component comparison
A1_SMOOTH_LOCALLY_COVARIANT_MEMBER_IS_MINIMAL = false | TYPE-U |
  would-build: an exhaustive comparison against the live nonsmooth causal-locale
               alternative under the complete raw-G contract
A1_MEMBER_SELECTED = false | TYPE-S |
  scope: the full proposed background family
```

### 4.2 A2 — `Real_ext`: external realization of the derived write complex

Let `K_write,N` denote the already-existing D1/D2 finite signed write system.
A2 proposes the complete family of injections

```text
iota_N:K_write,N -> M
```

satisfying:

```text
R1 vertices map to events;
R2 each D2-oriented write edge maps to a future-directed causal path gamma_j;
R3 each record cell maps to a causally convex support region D_j;
R4 iota_N intertwines the existing signed incidence with physical boundaries;
R5 pullback of physical support adjacency equals the existing record adjacency;
R6 iota_M compose j_NM = iota_N under the existing zero-extension;
R7 Hol_N:Conn_ext(M)->U(1)^N is onto at every finite stage;
R8 every realization satisfying R1-R7 remains; no filtration/member selected.
```

A2 authors `iota_N`, the event/path/support codomain, causal convexity,
global coherence, and R7.  It does not author a second root, sign, adjacency,
stage order, or character.

```text
A2_REAL_EXT_PROPOSED = true [PROPOSAL FIELD]
A2_DUPLICATE_INCIDENCE_GENERATOR_PRESENT = false | TYPE-R |
  test: A2's incidence occurs only as the domain of an intertwining equation
A2_REALIZATION_MEMBER_SELECTED = false | TYPE-S |
  scope: full R1-R8 family
```

### 4.3 A3 — `Conn_ext`: physical connection carrier and realization

On A1 define

```text
Conn_ext(P_M)=affine space of smooth U(1) connections on P_M,
Gauge_c(P_M)=compactly supported U(1) gauge transformations,
V_M=Omega^1(M;iR)/{d lambda},
Curr_Epost(M)=compactly supported one-currents with boundary only at the
              declared E_post endpoints,
Hol_A(gamma)=exp(integral_gamma A).
```

A3 authors the external one-form carrier, current pairing, physical exact-test
quotient, and the realization relation

```text
record edge transport U_e  <->  Hol_A(iota_N(e)).
```

It does not author compactness, unit incidence magnitude, vertex rephasing,
or the finite endpoint sign.  Those are inherited from D2/Gate 4.

The proposed physical compound index is

```text
I=(a,mu,x),
```

where `a` is D3-derived while `(mu,x)` comes from A1/A3.

```text
A3_CONN_EXT_PROPOSED = true [PROPOSAL FIELD]
A3_GAUGE_REPRESENTATIVE_SELECTED = false | TYPE-S |
  scope: quotient carrier only
A3_WARD_OPERATOR_SUPPLIED = false | TYPE-U |
  would-build: Q-288 physical quotient/Ward/domain operation
```

### 4.4 A4 — `Rig_ext`: localized source rigging

For each A2 realization, propose

```text
u_j in Curr_Epost(M),
support(u_j) subset D_j,
u_j(A)=integral_(gamma_j)A,
H_1(B)=closure_span{u_j} in the declared frame norm,
U_B:ell^2 -> H_1(B), e_j -> u_j,
U_B^*U_B=I, U_BU_B^*=I_(H_1),

H_CTP(B)=H_1(B)_+ direct-sum H_1(B)_-,
D_0(B)=union_N span{u_1,...,u_N}_+/- ,
J_phys(B)=U_B(ell^1_+ direct-sum ell^1_-),
D_0 dense-subset J_phys subset H_CTP subset J_phys^*,

K_J(J)=U_B J,
K_R(R)=U_B R U_B^*.
```

The index set, branch grammar, and zero-extension are inherited from D2/D3.
The localized frame, norm, completion, and source-field intertwiners are the
authored remainder.

```text
A4_RIG_EXT_PROPOSED = true [PROPOSAL FIELD]
A4_ORTHONORMAL_FRAME_FORCED_BY_INCIDENCE = false | TYPE-U |
  would-build: an independent source-normalization theorem
A4_FRAME_SELECTED_AFTER_OUTPUT = false | TYPE-S |
  scope: frozen proposal family
```

### 4.5 A5 — `AlgField_phys`: algebraic field-coordinate representation

Let `Cyl_poly(Conn_ext(M))` be generated by affine coordinates

```text
A(f):Conn_ext(M)->C,  A(f)(A)=f(A),  f in J_phys(B),
```

modulo the A3 exact test directions.  Use the D3 branch pair and backward
opposite ordering.  On the common algebraic cylinder domain define

```text
(pi_alg(A_a(f))Psi)(A_+,A_-)=A_a(f)Psi(A_+,A_-).
```

A5 authors the external coordinate algebra and multiplication representation.
D3 supplies its branch exchange, reality, and symmetric-source rules.  A5
does not supply a state, measure, scalar physical functional, CCR relation,
field equation, or closure.

```text
A5_ALG_FIELD_PHYS_PROPOSED = true [PROPOSAL FIELD]
A5_STATE_MEASURE_OR_DYNAMICS_SUPPLIED = false | TYPE-S |
  scope: A5 definitions
```

### 4.6 A6 — `BilocClass_phys`: separated bilocal/raw class

Propose

```text
E_J,phys(B)=J_phys(B),
E_R,phys(B)=S_1,sym(H_CTP(B)),
Bil_phys(B)=Bil_cont(J_phys(B) x J_phys(B);C),
RawGClass(B)=Bil_phys(B) intersect D3-real symmetric forms,

Conn_B(alpha,beta)(f,h)=alpha(f)beta(h).
```

`Bil_phys` uses the bounded-bilinear norm transported from A4's `ell^1`
source norm.  Its finite core is dense by the proposed completion; therefore
a continuous bilinear form killed by every finite restriction is zero.

A6 authors the transported physical bilocal topology, the raw carrier, the
connected-subtraction codomain, and the density premise.  It does not assert
that a physical scalar functional exists, that raw `G` has been differentiated,
or that the connected subtraction has run.

```text
A6_BILOC_CLASS_PHYS_PROPOSED = true [PROPOSAL FIELD]

EVERY_AUTHORED_POSITIVE_STANDING = PROPOSED_NOT_ADOPTED
AUTHORED_POSITIVE_MARKED_TYPE_P = false | TYPE-S |
  scope: A1-A6 and all consequences that consume them
A6_RAW_G_PHYSICAL_IMAGE_EXISTS = false | TYPE-U |
  would-build: scalar functional, differentiation, subtraction, quotient,
               restriction run, and Door-D image certificate
```

### 4.7 `C_Emb`: generated consequence, not an authored field

Given A2-R7 and A3, define

```text
Emb_N:C(U(1)^N)->C_b(Conn_ext(M)),
(Emb_N f)(A)=f(Hol_A(gamma_1),...,Hol_A(gamma_N)).
```

Surjectivity of `Hol_N` makes `Emb_N` injective and isometric.  A2-R6 and D2
give naturality, so D4's universal property yields

```text
Emb_cyl:T_cyl -> C_b(Conn_ext(M)_+ x Conn_ext(M)_-).
```

The physical restrictions are

```text
rho_A,N(A)=(Hol_A(gamma_j))_(j<=N),
rho_J,N(f)=(<u_j,f>)_(j<=N),
rho_R,N(R)=(<u_j,Ru_k>)_(j,k<=N),
rho_G,N(G)=(G(u_j,u_k))_(j,k<=N).
```

These maps are consequences of A2-A4 plus D4.  The proposal authors their
premises, not the universal-property proof.

```text
C_EMB_CONDITIONAL_THEOREM_PROVED = true [PROPOSAL-CONDITIONAL MATHEMATICS]
C_EMB_IS_INDEPENDENT_AUTHORED_CHOICE = false | TYPE-R |
  test: fixed uniquely by A2 holonomies, A3 carrier, A4 frame, and D4 universality
C_EMB_IDENTIFIES_TCYL_WITH_FULL_FIELD_ALGEBRA = false | TYPE-R |
  test: bounded holonomy cylinders and unbounded algebraic coordinates differ
```

### 4.8 Governance wrapper

```text
Gov_V002 := (
  hashes and standings of D1-D4 and A1-A6,
  full background/realization/frame families,
  no selected anchor, physical rank pair, realization, or background,
  naturality requirement for every consumer,
  DoR-008 restriction falsifier,
  Q-279 full-tuple target,
  Q-288 class-formation doors,
  no post-output supplementation
).
```

Governance is mandatory and adds no physical field.

---

## 5. Choice table — authored fields only

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
```

| Field | Proposed choice | Live alternatives | Why retained in V002 | Void condition |
|---|---|---|---|---|
| A1 `LocInc4_ext` | full family of oriented, time-oriented, globally hyperbolic four-dimensional Lorentzian U(1) backgrounds | fixed member; nonsmooth causal locale; Euclidean family | preserves V001's explicit authored route and gives the raw-`G` external point/support and causal grammar; leastness versus the nonsmooth alternative remains TYPE-U | family empty; live signature mismatch; retarded support cannot be stated |
| A2 `Real_ext` | complete coherent family of causal/support realizations of the **existing** write complexes | select one filtration; arbitrary supports; no realization | authors only the missing internal/external map and preserves D1/D2 exactly | any seam S1-S3 fails; R7 fails; hidden member selected |
| A3 `Conn_ext` | external compact U(1) connection one-form carrier modulo exact test directions | curvature-only; noncompact additive field; generic vector field | realizes already-derived compact transport without re-authoring its group or magnitude | finite characters fail to be realized holonomies; extra gauge scale introduced |
| A4 `Rig_ext` | localized incidence-indexed orthonormal physical source basis in a declared norm | Parseval frame; weighted/Riesz frame; arbitrary basis; no Hilbert rigging | supplies the field/source intertwiner and one topology while consuming derived labels unchanged | localization or zero-extension fails; normalization becomes output-dependent |
| A5 `AlgField_phys` | polynomial affine-coordinate algebra with multiplication representation | Weyl/CCR; Fock representation; bounded holonomy algebra only | represents `A^I` without pre-forming dynamics, state, or closure | relation set requires a hidden state/measure or violates D3 |
| A6 `BilocClass_phys` | trace-class symmetric source and separated bounded-bilinear raw class on A4's transported source space | Hilbert-Schmidt; C-star norm raw class; weak-star/bidual; finite-only | provides Q-288's explicit separated norm route without silently creating a tail | finite-core density or continuity fails; nonzero all-finite-invisible bilinear survives |

The proposal does not select among the alternatives after output.  DoR-015
would ratify A1-A6 as one package or reject/return the package; this lane does
not choose a background or realization member.

```text
AUTHORED_ASK_V002 = (A1,A2,A3,A4,A5,A6)
INDEPENDENT_AUTHORED_CHOICE_COUNT = 6
```

---

## 6. Seam theorems

Every theorem below is conditional on the authored field(s) it names.  The
conditional proof does not promote those fields to TYPE-P.

### S1. D1/D2 orientation to A2 causal realization

A2-R2 maps the already-oriented edge, without reversal, to a
future-directed path.  A3 open currents take their boundary at the same
`E_post` endpoints.  Therefore

```text
iota_N(source(e)) = initial(gamma_e),
iota_N(target(e)) = final(gamma_e),
E_post(e)         = endpoint orientation(gamma_e).
```

The exchanged orientation is not a member.  This proves compatibility; it
does not derive the external future relation.

```text
S1_ORIENTATION_SEAM = PASS_AS_PROPOSAL
```

### S2. D1/D2 locality to A2 physical support

A2-R4/R5 require the two commuting equalities

```text
boundary_phys compose iota_N = iota_N compose boundary_write,
Adj_phys(D_j,D_k) iff Adj_write(j,k), with the same incidence sign.
```

Deleting a write edge while holding the physical adjacency fixed, or adding a
physical adjacency with no write edge, violates the seam.  It is therefore
failure-capable and does not define a second locality rule.

```text
S2_LOCALITY_SEAM = PASS_AS_PROPOSAL
```

### S3. Gate-4 transport to A3 connection holonomy

For every realized edge, require

```text
T_iota(U_e)=Hol_A(gamma_e),
T_iota(h_t U_e h_s^(-1))
  =h_(iota(t)) Hol_A(gamma_e) h_(iota(s))^(-1).
```

The right-hand side is the endpoint action of physical gauge transformation;
the left is Gate-4 vertex rephasing.  Composition of paths multiplies both
sides and unit magnitude is preserved.  The square therefore commutes on
the generated edge/path algebra.  A3 authors the existence of `T_iota`; the
intertwining law is forced once it exists.

```text
S3_CONNECTION_INCIDENCE_SEAM = PASS_AS_PROPOSAL
```

### S4. D3 reality to A5 algebraic representation

On the algebraic cylinder domain define

```text
(C_CTP Psi)(A_+,A_-)=conjugate(Psi(A_-,A_+)).
```

Direct substitution gives

```text
C_CTP pi_alg(A_+(f)) C_CTP^(-1)
  =pi_alg(A_-(conjugate(f))),
```

and the opposite branch equation similarly.  Thus A5 represents D3's
involution exactly.  No scalar product, measure, or state is used.

```text
S4_CTP_REALITY_REPRESENTATION_SEAM = PASS_AS_PROPOSAL
```

### S5. D3 finite index to the authored physical compound index

A4 maps each derived label `lambda=e_j` to the localized current `u_j`, whose
support and one-form component live in `(mu,x)`.  Branch is untouched:

```text
(a,e_j) -> (a,u_j) -> (a,mu,x)|support(u_j).
```

Pullback by `rho_J,N` recovers `(a,e_j)` exactly.  Hence branch order and
Keldysh derivative order are preserved.  A1/A3/A4 author `(mu,x)` and the
map; D3 supplies `a`.

```text
S5_COMPOUND_INDEX_SEAM = PASS_AS_PROPOSAL
```

### S6. D2 zero-extension to A4 source rigging

With `U_B e_j=u_j`, the finite inclusions satisfy

```text
U_B compose j_NM = inclusion_NM^phys compose U_B
```

on the finite core.  The same identity holds for `K_R` on matrix corners.
Thus the A4 restriction square preserves the exact D2 stage grammar.

```text
S6_SOURCE_RIGGING_SEAM = PASS_AS_PROPOSAL
```

### S7. D4 `T_cyl` to `C_Emb`

A2-R7 makes each holonomy pullback injective/isometric; A2-R6 supplies
naturality; D4's universal property supplies the unique norm-continuous
`Emb_cyl`.  Both classes are separated norm classes, so no common-restriction
tail is introduced at this seam.

```text
S7_TCYL_PHYSICAL_HOLONOMY_SEAM = PASS_AS_PROPOSAL
S7_CREATED_TAIL_IMAGE = zero | conditional on A2-R6/R7
```

### 6.8 Seam scope ceiling

The seams prove consistency and exact finite pullback.  They do not prove
that A1-A6 exist, that a scalar physical functional exists, that the physical
raw-`G` image exists, or that the stationary/background maps close.

```text
ALL_SEAMS_PROVE_AUTHORED_FIELD_EXISTENCE = false | TYPE-R |
  test: every seam is an implication with an authored existence antecedent
```

---

## 7. Ten-residue to nine-field reconciliation

Q-290's ten items are consumer-level obligations.  V001's nine entries mix
physical structures, inherited conventions, and governance.  They were never
required to be in one-to-one correspondence.

| Q-290 residue | V002 discharger/interface | Current proposal standing |
|---|---|---|
| R1 public geometric/support realization | A1+A2 | proposed carrier/family |
| R2 physical field/source intertwiner | A3+A4+A5, Seams S3/S5/S6 | proposed, conditional seams proved |
| R3 external support and `(mu,x)` realization | A1+A3+A4, Seam S5 | proposed |
| R4 causal-support structure | A1+A2, Seam S1 | proposed; retarded boundary-value operation still unbuilt |
| R5 common scalar physical CTP representation | A5 supplies scalar-valued algebraic coordinate representation; D3 supplies CTP reality | proposed at algebraic carrier level; scalar functional/measure remains Q-288 TYPE-U |
| R6 physical quotient, Ward operator, endpoint maps | A3 exact-test quotient and E_post currents | quotient carrier proposed; Ward/domain operation remains TYPE-U |
| R7 invariant pairing/measure/contact/boundary | A1 density carrier+A2 supports | carriers proposed; measure/contact/boundary operations remain TYPE-U |
| R8 completed bilocal class and lift | A4+A6 | class proposed; physical image/lift and Door D remain TYPE-U |
| R9 stationary-background/Schur package | A1+A5+A6 provide the carrier/interface | stationary fiber and Schur operation remain TYPE-U |
| R10 finite-to-physical restriction theorem | D4+A2-A4 imply `C_Emb` and `rho_*` | component squares posed; full Q-279/physical square remains TYPE-U |

This table explains both counts without rounding:

```text
NINE_V001_FIELDS = six retained authored structures
                   + one removed derived convention field
                   + one conditional consequence
                   + one governance wrapper

TEN_Q290_RESIDUES = ten consumer obligations, several sharing one field and
                    several requiring later analytic operations
```

```text
NINE_TO_TEN_COUNT_RECONCILED = true | STRUCTURAL
RESIDUE_ITEM_SILENTLY_DROPPED = false | TYPE-R |
  test: every R1-R10 appears exactly once in the reconciliation table
```

---

## 8. Merged Q-288 class-formation doors

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
```

### 8.1 Door 0 — derived write-shadow formation

This is inherited without change from Q-290:

```text
CLASS_FORMATION_DOOR_D0 := (
  input_class=finite signed record cells and finite CTP character data,
  input_topology=finite-dimensional stage norms,
  input_restrictions=N<=M zero-extension,
  formation_or_completion_operation=algebraic colimit plus C-star/module norm completion,
  output_class=T_cyl separated norm C-star/module class,
  output_topology=C-star/module norm,
  output_restrictions=canonical finite-coordinate retractions,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=C-star/module norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} by T_cyl separation,
  created_tail_image=zero,
  class_separation_proved=true,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true,
  restriction_square=PASS,
  Q279_full_tuple_reproduced=TYPE-U | raw G not formed,
  common_origin_provenance=PASS | TYPE-P | DoR-008/013,
  target_independence=PASS,
  door_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL
).
```

### 8.2 Door A — D4 `T_cyl` to physical holonomy cylinders

```text
CLASS_FORMATION_DOOR_A := (
  input_class=T_cyl norm C-star,
  input_topology=C-star norm,
  input_restrictions=canonical finite-coordinate retractions,
  formation_or_completion_operation=C_Emb holonomy pullback,
  output_class=bounded holonomy-cylinder subalgebra of C_b(Conn_ext,+ x Conn_ext,-),
  output_topology=sup norm,
  output_restrictions=finite holonomy coordinates,
  topology_changed=false,
  every_limit_named=true,
  limit_topology=C-star norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on A2-R7,
  created_tail_image=zero conditional on injective isometry,
  class_separation_proved=true conditional on A2-R7,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true conditional on A2/A3 by D4 universality,
  restriction_square=PASS_AS_PROPOSAL,
  Q279_full_tuple_reproduced=TYPE-U,
  common_origin_provenance=TYPE-U | external realization is proposed, not descended,
  target_independence=PASS_AS_PROPOSAL,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015 and physical provenance
).
```

Door 0 and Door A compose without conflict: Door 0 forms `T_cyl`; Door A
embeds that already-separated class isometrically.  Neither is the refuted
trace-class-to-`T_cyl` bilocal map.

### 8.3 Door B — finite modes to A4 Hilbert carrier

```text
CLASS_FORMATION_DOOR_B := (
  input_class=finite-support physical source modes,
  input_topology=finite-dimensional stage norm,
  input_restrictions=coordinate truncations,
  formation_or_completion_operation=A4 orthonormal Hilbert completion,
  output_class=H_CTP(B),
  output_topology=Hilbert norm,
  output_restrictions=orthogonal frame truncations,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=Hilbert norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on A4 frame completeness,
  created_tail_image=zero conditional on dense orthonormal span,
  class_separation_proved=true conditional on A4,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true for a fixed proposed frame norm,
  restriction_square=PASS_AS_PROPOSAL via Seam S6,
  Q279_full_tuple_reproduced=TYPE-U,
  common_origin_provenance=TYPE-U | A4 external rigging is proposed,
  target_independence=PASS_AS_PROPOSAL,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015
).
```

### 8.4 Door C — finite symmetric matrices to trace class

```text
CLASS_FORMATION_DOOR_C := (
  input_class=finite symmetric matrices on A4 frame modes,
  input_topology=finite trace norm,
  input_restrictions=finite matrix corners,
  formation_or_completion_operation=trace-norm completion R->U_B R U_B^*,
  output_class=S_1,sym(H_CTP(B)),
  output_topology=trace norm,
  output_restrictions=frame matrix corners,
  topology_changed=true,
  every_limit_named=true,
  limit_topology=trace norm,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on finite-rank density,
  created_tail_image=zero conditional on A4,
  class_separation_proved=true conditional on A4,
  quotient_exactness_proved=NOT_APPLICABLE,
  closure_uniqueness_proved=true for trace-norm completion,
  restriction_square=PASS_AS_PROPOSAL,
  Q279_full_tuple_reproduced=TYPE-U,
  common_origin_provenance=TYPE-U | A4 external rigging is proposed,
  target_independence=PASS_AS_PROPOSAL,
  proposal_conditional_verdict=CLOSED_SEPARATED_ZERO_CREATED_TAIL,
  door_verdict=UNDECIDED_TYPE_U pending DoR-015
).
```

### 8.5 Door D — source tests to A6 raw class

```text
CLASS_FORMATION_DOOR_D := (
  input_class=J_phys(B) x J_phys(B),
  input_topology=A4 transported ell^1 norm,
  input_restrictions=frame-mode evaluations,
  formation_or_completion_operation=continuous-bilinear dual formation,
  output_class=RawGClass(B),
  output_topology=bounded-bilinear norm,
  output_restrictions=rho_G,N,
  topology_changed=false,
  every_limit_named=true,
  limit_topology=NOT_APPLICABLE,
  weak_star_invoked=false,
  bidual_invoked=false,
  distributional_completion_invoked=false,
  nonseparating_extension_invoked=false,
  Tail_output={0} conditional on A6 finite-core density,
  created_tail_image=zero for the abstract continuous class; physical raw-G image TYPE-U,
  class_separation_proved=true conditional on A6,
  quotient_exactness_proved=TYPE-U,
  closure_uniqueness_proved=TYPE-U | physical image absent,
  restriction_square=TYPE-U | physical image absent,
  Q279_full_tuple_reproduced=TYPE-U,
  common_origin_provenance=TYPE-U,
  target_independence=PASS_AS_PROPOSAL,
  proposal_conditional_verdict=UNDECIDED_TYPE_U,
  door_verdict=UNDECIDED_TYPE_U
).
```

### 8.6 Door merge verdict

```text
UNFLAGGED_CLASS_FORMATION_STEP_FOUND = false | TYPE-S |
  scope: D0 and Doors A-D in V002
WEAK_STAR_OR_BIDUAL_STEP_PROPOSED = false | TYPE-S |
  scope: D0 and Doors A-D
MERGED_DOOR_TOPOLOGY_CONFLICT_FOUND = false | TYPE-R |
  test: each seam uses the same declared input/output topology on both sides
Q288_REQUIRED_DOOR_FIELD_COUNT_PRESENT_FOR_EACH_OF_D0_A_B_C_D = true |
  MECHANICAL_AUDIT
PHYSICAL_TAIL_CREATION_STATUS = NO_VERDICT |
  prerequisite: physical Door-D image and restriction map
```

---

## 9. Consumer statements on V002

### 9.1 Raw-`G` bounded-extension rerun

Q-290's theorem remains binding:

```text
S_1,sym(H_CTP) -> T_cyl
```

is unbounded for every unit-modulus signature weight supplied by D1-D4 and
Gate 4.  V002 does not repair or withdraw that TYPE-R result.

The proposed post-ratification test is different and frozen now:

```text
Domain:   S_1,sym(H_CTP(P2))
Map 1:    R -> U_B R U_B^*
Codomain: S_1,sym(H_CTP(B))
Topology: trace norm
Map 2:    algebraic quadratic insertion on A5's common cylinder domain
Test:     continuity or closability into A6's physical raw-G image
```

It must execute:

1. Q-287/Q-290's moving-support family;
2. uniform trace-norm boundedness of Map 1;
3. domain/closability of Map 2;
4. connected-subtraction typing;
5. every `rho_G,N` restriction square;
6. full Q-279 tuple reproduction;
7. Door-D image intersection with physical `Tail_R`.

```text
RAW_G_BOUNDED_EXTENSION_RERUN_POSED_ON_V002 = true [PROPOSAL-LEVEL FACT]
RAW_G_BOUNDED_EXTENSION_RERUN_EXECUTED = false | TYPE-U |
  would-build: ratified A1-A6, scalar functional, analytic package, and seven checks
UNIT_MODULUS_TCYL_NO_GO_REOPENED = false | TYPE-S |
  scope: V002 uses a different codomain and leaves its test unexecuted
```

### 9.2 Four analytic fields

| Q-288 account | V002 gain | Still missing |
|---|---|---|
| measure | A1 external carrier/density type; A2 support regions; A4 test space | branch-joint history measure, marginal consistency, scalar functional, descended convolution measure |
| contour | D2 endpoint orientation; D3 CTP grammar; A1 time orientation; A6 output class | interacting prescription, boundary-value family, named limit and topology |
| boundary/contact | D1 incidence boundary; A2 physical supports/boundaries; C_Emb restrictions | physical boundary form, contacts, null/private reduction, microcausal gluing |
| domain closure | A5 algebraic common domain; A4 rigged source triple; A6 norm class | closability, closed/self-adjoint extension, graph/resolvent topology, Ward-compatible endpoints |

The derivation removes duplicated conventions from these interfaces; it does
not build the four operations.

### 9.3 Background interface

V002 supplies the external field carrier, compound-index realization,
bilocal carrier, source intertwiners, and finite restrictions needed to pose
the existing `STAT_BG_LIFT_FIBER`.  It does not supply a stationary member,
Legendre map, 2PI blocks, evaluation rule, or Schur solution.

```text
STAT_BG_LIFT_INTERFACE_POSED_ON_V002 = true [PROPOSAL-LEVEL FACT]
PHYSICAL_STATIONARY_BACKGROUND_BUILT = false | TYPE-U |
  would-build: scalar physical package, 2PI blocks, stationary fiber, and selector
```

### 9.4 Consumer readiness

```text
RAW_G_CONSUMER_READY_FOR_EXECUTION = false | TYPE-U |
  would-build: DoR-015 plus Q-288 measure/contour/boundary/domain interfaces
FOUR_ANALYTIC_OPERATIONS_BUILT = false | TYPE-U
BACKGROUND_MAP_BUILT = false | TYPE-U
PHYSICAL_RESTRICTION_THEOREM_EXECUTED = false | TYPE-U
```

---

## 10. Mandatory self-kill passes

### K1. Complete subtraction audit

Every derived Q-290 phrase was searched against the A1-A6 choice table.
Root, sign, local adjacency, finite locality, zero-extension, compact group,
unit incidence magnitude, finite CTP orientation/metric/reality, and `T_cyl`
completion occur only as inherited inputs or seam constraints.

```text
DERIVED_COMPONENT_REAUTHORED = false | TYPE-R |
  test: per-field subtraction ledger and A1-A6 choice table
```

### K2. No weakening of the derivation

V002 retains Q-290's finite/cylindrical scope and its negative results:
global causal order is not derived; physical `(mu,x)` is not derived; the
complete field signature is not derived; unit-modulus weights do not bound
the `T_cyl` bilocal lift; the beta gap does not close by that route.

```text
Q290_NEGATIVE_WEAKENED_OR_REPAIRED = false | TYPE-S |
  scope: every Q-290 TYPE-R statement bearing on V002
```

### K3. Seam counterexample hunt

The following mutations kill the corresponding seam:

```text
reverse one A2 path orientation                 -> kills S1;
add a physical adjacency with no write edge     -> kills S2;
change edge phase without endpoint rephasing    -> kills S3;
use same-branch conjugation instead of exchange -> kills S4;
permute branch behind (mu,x) inconsistently     -> kills S5;
change zero-extension image of one source mode  -> kills S6;
drop finite holonomy surjectivity                -> kills S7/injectivity.
```

No mismatch appears under the stated A1-A6 predicates.  This is conditional
compatibility, not an existence proof.

```text
SEAM_MISMATCH_FOUND = false | TYPE-R |
  test: S1-S7 equations and named mutation victims
```

### K4. Hidden import and target-tuning audit

Smooth Lorentz geometry, external causal realization, physical connection
carrier, source frame norm, algebraic field representation, and bilocal norm
are openly A1-A6 proposal fields.  None is called standard-and-therefore-
derived.  No membership predicate reads a response, state-weight consequence,
residual, or target output.

```text
HIDDEN_AUTHORED_FIELD_FOUND = false | TYPE-S |
  scope: A1-A6 definitions and choice table
TARGET_TUNING_FOUND = false | TYPE-S |
  scope: all V002 membership predicates
```

### K5. Family and common-origin audit

No background, realization, frame, anchor, or physical rank-pair member is
selected.  A1-A6 are proposed as a family.  Physical common-origin descent is
not inferred from proposal packaging; Doors A-D keep it TYPE-U where the
external fields enter.

```text
HIDDEN_FAMILY_MEMBER_SELECTION_FOUND = false | TYPE-S |
  scope: A1-A6 and Gov_V002
PACKAGING_COUNTS_AS_COMMON_ORIGIN = false | TYPE-R |
  test: authored external realization has no Gen_Omega descent map
```

### K6. Analytic-operation smuggling audit

No measure integration, interacting contour limit, contact reduction,
unbounded closure, stationary solve, raw-`G` image, inverse, retarded
extraction, induced response, or consumer evaluation is performed.

```text
Q288_ANALYTIC_OPERATION_SMUGGLED_INTO_SIGNATURE = false | TYPE-S |
  scope: A1-A6, seams, and Doors D0/A-D
```

### K7. Count audit

All nine V001 fields and all ten Q-290 residues appear in explicit tables.
The count reduction is semantic, not arithmetic deletion: F5 is inherited,
F8 is a consequence, and F9 is governance.

```text
V001_FIELD_UNACCOUNTED = false | TYPE-R |
Q290_RESIDUE_UNACCOUNTED = false | TYPE-R
```

---

## 11. Void conditions and review package

Any one of the following voids V002 as written:

```text
V1  A1/A2 family is empty or cannot state the raw-G support relation;
V2  any of Seams S1-S7 fails;
V3  finite holonomy surjectivity fails, making C_Emb a quotient;
V4  no localized orthonormal A4 source family exists coherently under D2;
V5  A4 introduces an output-dependent weight or physical scale;
V6  A6 contains a nonzero continuous bilinear invisible to every finite restriction;
V7  A5 cannot represent D3 reality on the common algebraic domain;
V8  any class-formation act is unflagged or silently weak-star/bidual;
V9  a derived D1-D4 component appears as an independent authored toggle;
V10 a future physical restriction disagrees with the full finite tuple;
V11 a consumer selects an A1/A2/A4 family member without a separate rule;
V12 the raw-G contract needs a signature carrier absent from the R1-R10 table.
```

Recommended cross-lane attacks:

1. test A2-R7 against causal convexity and the full realization family;
2. attack existence/coherence of the A4 localized orthonormal source family;
3. test S3 without identifying Gate-4 incidence and Gen_Omega `S_inc`;
4. test S4 on the opposite algebra rather than only scalar notation;
5. execute the Q-290 moving-support family on the proposed Map 2 domain;
6. search A6 for an all-finite-invisible continuous bilinear;
7. verify that A1 smoothness is genuinely required rather than excess proposal structure;
8. inspect the R1-R10 map for an operation mislabeled as a carrier.

```text
V002_SELF_KILL_STATUS = SURVIVED_INTERNAL_MERGE_PASS_WITH_DOOR_D_OPEN
ADVERSARIAL_REVIEW_REQUIRED = true [GATE PROCESS FACT]
DOR_015_RATIFICATION_READY = false | TYPE-C |
  constraint: cross-lane adversarial review not yet returned
```

---

## 12. Final proposal ledger

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION
DOR_015_STATUS = RESERVED
GATE_VERDICT = V002_COMPLETE_FOR_CROSS_LANE_ADVERSARIAL_REVIEW

D1_LOCAL_ROOTED_SIGNED_WRITE_EXISTS = true | TYPE-P | DoR-013
D2_FINITE_LOCALITY_ZERO_EXTENSION_EXISTS = true | TYPE-P | DoR-008/009/013
D3_FINITE_CTP_SIGNATURE_EXISTS = true | TYPE-P | DoR-008 + sealed conventions
D4_TCYL_SEPARATED_NORM_COMPLETION_EXISTS = true | TYPE-P | DoR-008 + T_cyl
GATE4_UNIT_WEIGHT_COVARIANT_INCIDENCE_EXISTS = true | DERIVED_OVER_ENUMERATED_FAMILY

A1_LOCINC4_EXT_PROPOSED = true [PROPOSAL FIELD]
A2_REAL_EXT_PROPOSED = true [PROPOSAL FIELD]
A3_CONN_EXT_PROPOSED = true [PROPOSAL FIELD]
A4_RIG_EXT_PROPOSED = true [PROPOSAL FIELD]
A5_ALG_FIELD_PHYS_PROPOSED = true [PROPOSAL FIELD]
A6_BILOC_CLASS_PHYS_PROPOSED = true [PROPOSAL FIELD]
EVERY_AUTHORED_POSITIVE_STANDING = PROPOSED_NOT_ADOPTED
AUTHORED_POSITIVE_MARKED_TYPE_P = false | TYPE-S

V001_F5_REMOVED_FROM_AUTHORSHIP = true | STRUCTURAL
V001_F8_RECLASSIFIED_AS_CONDITIONAL_THEOREM = true | STRUCTURAL
V001_F9_RECLASSIFIED_AS_GOVERNANCE = true | STRUCTURAL
INDEPENDENT_AUTHORED_CHOICE_COUNT = 6

ALL_SEAMS_POSED_AND_PROVED_CONDITIONALLY = true [PROPOSAL-CONDITIONAL MATHEMATICS]
NINE_TO_TEN_COUNT_RECONCILED = true | STRUCTURAL

DOOR_D0_VERDICT = CLOSED_SEPARATED_ZERO_CREATED_TAIL
DOOR_A_VERDICT = UNDECIDED_TYPE_U | conditional proposal theorem passes
DOOR_B_VERDICT = UNDECIDED_TYPE_U | conditional proposal theorem passes
DOOR_C_VERDICT = UNDECIDED_TYPE_U | conditional proposal theorem passes
DOOR_D_VERDICT = UNDECIDED_TYPE_U

UNIT_MODULUS_TCYL_BILOCAL_EXTENSION_EXISTS = false | TYPE-R
RAW_G_BOUNDED_EXTENSION_RERUN_POSED_ON_V002 = true [PROPOSAL-LEVEL FACT]
RAW_G_BOUNDED_EXTENSION_RERUN_EXECUTED = false | TYPE-U
PHYSICAL_RAW_G_IMAGE_EXISTS = false | TYPE-U
PHYSICAL_RESTRICTION_THEOREM_EXECUTED = false | TYPE-U
PHYSICAL_TAIL_CREATION_STATUS = NO_VERDICT

DERIVED_COMPONENT_REAUTHORED = false | TYPE-R
Q290_NEGATIVE_WEAKENED_OR_REPAIRED = false | TYPE-S
UNFLAGGED_CLASS_FORMATION_STEP_FOUND = false | TYPE-S
WEAK_STAR_OR_BIDUAL_STEP_PROPOSED = false | TYPE-S
TARGET_TUNING_FOUND = false | TYPE-S
HIDDEN_FAMILY_MEMBER_SELECTION_FOUND = false | TYPE-S

REGISTER_HEAD_AT_START = Q-290
REGISTER_HEAD_AT_SEND_TIME = Q-290
LATER_BEARING_RULINGS_CONSUMED = none |
  send-time register and tracker recheck found no post-Q-290 entry

alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```

V002 preserves the derived record-surface shadow exactly and asks DoR-015 to
author only the external structures that shadow does not contain.  It neither
claims that spacetime was derived nor pays twice for incidence, orientation,
locality, CTP reality, or the cylindrical completion.
