# STAGE 8 / TASK 6 / SUBGATE — CUSTODY REVIEW OF THE LP-QSPEC EIGHTEEN-SLOT ASSEMBLY + THREE APPEAL DISPLAYS — LANE 3 V001

Date: 2026-08-06  
Lane: Codex Lane 3  
Custody: cross-family review of Dario's assembly; this artifact adopts nothing  
Review target: `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V001.md`  
Review target SHA-256: `a2fdd7c0502083cc9973b766464a30807a3ba3b36b9305011df404671635422c`

```text
REVIEW_DISPOSITION = REFUTED_WITH_BOUNDED_CORRECTIONS
FILLED_SLOTS = 0/18
CORRECTED_GRADES = 18 TYPE-U / 0 PART-PROVABLE
GRADE_OVERRIDE = S18 PART-PROVABLE -> TYPE-U
APPEAL_1 = DISSOLVED_BY_REGISTERED_PRINCIPAL_RULING
APPEAL_2 = DISSOLVED_AS_FALSE_EXCLUSIVITY
APPEAL_3 = CONFIRMED_FOR_RULING_WITH_SCOPE_QUALIFICATION
```

The refutation is not a rejection of the assembly's entire demand-side work. The
eighteen concepts, their order, C7, the target-value-free/historically-target-aware
standard, the zero-filled count, S01--S17's TYPE-U grades, S16's three precisions,
and the declination of OBS-22 survive. The bounded defects are displayed below.

---

## 0. Preflight and method

### 0.1 Access, head, collision, and fences

| Check | Displayed result |
|---|---|
| Living questions register | Current head is Q-555; Q-554 records the assembly under review. This review cites the living register by entry, not by whole-file hash. |
| Review artifact | SHA-256 recomputed as `a2fdd7c0502083cc9973b766464a30807a3ba3b36b9305011df404671635422c`; exact match. |
| Governing slot ledger | `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md`; SHA-256 recomputed as `7995f6fda75e78795cbfe167f8c8df634170ea3b43affd5bbe6e22bcda8f6ffe`; exact match. |
| Output collision | `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_REVIEW_LANE3_V001.md` was absent locally and in `alpha-program-archive/workspace/` before writing. |
| Fences | `alpha_computed = false`; `proof_authorized = false`; `kappa_record_computed = false`. No fence was lifted. |

No member was bound, no fixed point was executed, no end test was run, no
physical quantity was evaluated, and no measured constant was consulted.

### 0.2 Hash-verified load-bearing sources

| Source | SHA-256 | Use |
|---|---|---|
| `STAGE8_Q13_Q19_GOVERNING_REGISTRATION_RECORD_V001.md` | `ca1b1685d0623e28a008d6929d3685ed74d9c74a6ca5a85899ddbe5281d26962` | live registration of the S06/S12 ruling |
| `SLOT12_SCHEME_COVARIANCE_PRINCIPAL_DECISION_2026-07-30.md` | `028e355afc5f5d078ee39beb18de01269c57e09b85ba39a1721ebbf8e56ad451` | principal resolution of Appeal 1 |
| `COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md` | `ddb51c32e1ec45e6145be4c688f2af5fa87823ab20b4f523fc4657823165e544` | actual external-connection CTP type |
| `STAGE7_QSPEC_REVIEW_CANDIDATE_V002.md` | `5026afb89b52ead9f309168cb2fa4f06b81039f8dd01b8940f40e64aad679a7e` | open interacting amplitude/measure statement |
| `BOHM_EM_ORDER_TRIAGE_2026-07-28.md` | `bfbba5882892c85a1d2e4f2e68e6f0e1443553c04d1fffcf72c7197d25611125` | triaged S05 readings, not a second built type |
| `STAGE8_TASK19_SLOT3_DYNAMICAL_U1_ACTION_OBSTRUCTION_V001.md` | `e6fe0873c12e9372a42099869313033b96fb4f6ef488d767c5711da2d782b613` | S03 construction stop |
| `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md` | `1fae8b741ff1809351eb48e3db7969aa2556c0d141aa9c97441959fac7b4bf84` | action-family nonuniqueness |
| `COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md` | `67816cfeec8d61e4bcafec90c6c8a10fa239f91355c95f424b75621f6e2f3bd6` | two-completion countermodel |
| `STAGE8_ACTION_FORM_UNDERDETERMINATION_INVENTORY_V001.md` | `cafad33ed99b04b5c95fb1e2c82750f25e567aba2c3c6c08dc7aa3be61c2b022` | bounded inventory and unbounded remainder class |
| `STAGE8_SLOT16_REPOSED_SCOPE_AND_FK1_AUDIT_V001.md` | `df0d1c7430a80fa8b3927e3252c81f77424e16b679c091022b6d63f43e321d55` | S16 source and threshold typing |
| `STAGE8_TASK6_A32_PREP_LANE3_V002.md` | `c5d1090b1ec1862c59c1281845c9ad74d0b143e9a57f195c099adb16e56ae4ea` | ratified P4 crossing row |
| `STAGE8_TASK6_EVALUATION_DOR_LANE3_V006.md` | `30b17b2a0cedd09046a12d5733109cfedc74d181d394a23eecfb00597be2cc0b` | LOCAL-SHADOW product type |
| `STAGE8_TASK5_XI_N_CANDIDATE_LANE3_V004.md` | `ac9335dc7a2e90ba088467ae0b6409d178c53950568be42af976469b5f917ede` | exact `(Z1)/(Z3)/(Z4)/(Z8)` content |
| `DOR_020_A9_XI_N_ADOPTED_2026-08-05.md` | `e0b661807f172337ad1cca1b89feec2b6472d531f6dbaae3bd3e39a953510c15` | A9 adoption and non-inhabitance |
| `STAGE8_SLOT18_Q23_Q28_GOVERNING_REGISTRATION_V001.md` | `84966722ba7d7430a66f360d8f5ca3edde19f5b5035a26c7eb374f88e477424f` | operative unused/admissibility state |
| `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | full Slot-18 eligibility predicate |

### 0.3 M-2 guards

The review applied all three guards:

1. fixed-string searches for each slot name, object name, status token, and
   quoted appeal sentence;
2. whitespace-normalized searches for wrapped quotations and displays; and
3. scope/synonym searches for the same mathematical object under action,
   weight, measure, quotient, response, source-limit, spectrum, transport, and
   prediction language.

Roots were the sealed cleanroom Markdown corpus plus the specifically admitted
supervision decisions above. Private holdout/custodian content, physical output
records, and measured-value payloads were excluded. A zero hit is used only as a
bounded no-member finding; it is never promoted to impossibility.

---

## 1. V1 — census, C7, and target-value freedom

### 1.1 The eighteen-slot literal comparison

The governing list is `STAGE6_PARENT_ACTION_AND_QSPEC_LEDGER_V002.md:119-139`.
The concepts occur once each and in the correct order in the assembly. The
assembly nevertheless labels its column **"ledger verbatim"**, so literal
carriage is the test.

| Slot | Governing ledger text | Assembly carriage | Verdict |
|---|---|---|---|
| S01 | `absolute physical T_R` | identical | PASS |
| S02 | `full gravitational action and gravitational quantum measure` | identical | PASS |
| S03 | `dynamical U(1) action` | identical | PASS |
| S04 | `gauge fixing, ghosts, and gauge edge modes` | identical | PASS |
| S05 | `normalized interacting CTP amplitude` | identical | PASS |
| S06 | `parent-derived functional regulator and finite renormalization` | identical | PASS |
| S07 | `induced-polarization transversality and photon-mass exclusion` | identical | PASS |
| S08 | `Lorentz- and packing-independent renormalized response` | identical | PASS |
| S09 | `finite c F^2 deformation exclusion` | identical | PASS, with successor-status correction in §2.3 |
| S10 | `source-inclusive state projective limit` | identical | PASS |
| S11 | `infinite-future source Moller limit` | assembly writes `Møller` | **FAIL-LITERAL** |
| S12 | `continuum-regulator independence of the source-inclusive limit` | identical | PASS |
| S13 | `interacting charged pole or infraparticle threshold` | identical | PASS |
| S14 | `complete charged-species and threshold map` | identical | PASS |
| S15 | `enlarged-branch exhaustion` | identical | PASS |
| S16 | `threshold-conditioned Thomson matching` | identical | PASS |
| S17 | `CISP descendant test in the interacting outgoing sector` | identical | PASS |
| S18 | `and one unused structure-sensitive prediction` | assembly drops list-leading `and` | **FAIL-LITERAL** |

[PROVABLE] The conceptual census is complete: 18 of 18 concepts, in order, with
none dropped. [PROVABLE] The stronger **verbatim** claim is false in exactly two
bounded renderings, S11 and S18. Punctuation delimiters were normalized but do
not change a slot.

### 1.2 C7 membership and order

The ledger's frozen order at lines 142-156 is:

```text
S03 -> S04 -> S05 -> S06 -> S07 -> S08 -> S13 -> S16 -> S17,
and no later item may be used to select an earlier one.                 (C7)
```

The assembly quotes the same nine members in the same order and correctly uses
C7 as an anti-selection rule. Nothing in the review reorders that chain.

### 1.3 Target-value-free, historically target-aware

The governing ledger at line 40 says:

> These are target-value-free but historically target-aware.

The assembly carries that distinction exactly. Its two disclosed adjacent pins
also check out:

- `PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:59-60` names Thomson matching
  only as a downstream obligation; it is not an action filling.
- `BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:1657-1663` makes the unit
  convention conditional on `kappa_Thomson` first being derived; it supplies no
  value and is used as no filling.

V1 verdict: the demand census, order, and target-value standard are substantively
correct; the claim of verbatim carriage has the two literal defects displayed in
§1.1.

---

## 2. V2 — adversarial grade audit

### 2.1 Row-by-row board

`TYPE-U` means that the demanded member has not been produced; it does not mean
that the fiber is empty. `PART-PROVABLE` requires that the claimed structural
form actually follow from the cited adopted interface.

| Slot | Assembly grade | Review | Load-bearing check |
|---|---|---|---|
| S01 | TYPE-U | SUSTAIN | The scale-orbit gate leaves the absolute scale unidentified; the conditional interval artifact self-types `absolute_record_interval_derived = false`. The null-action normalization gives every positive radius. No genuine S01 member was killed. |
| S02 | TYPE-U | SUSTAIN | The exact unresolved gravitational action/quantum-measure object remains open in `STAGE8_QSPEC_SLOT_DEEP_INVENTORY_V001.md:50`; M-2 found no measure member. |
| S03 | TYPE-U | SUSTAIN twice | The primitive holonomy is an input, not a complete charged action; the finite background Ward identity is conditional and supplies neither the complete action nor photon-pole closure. See §3.3. |
| S04 | TYPE-U | SUSTAIN | Gauge fixing, ghost condition, and edge-mode completion remain downstream of S03; no missed member was found (`STAGE8_QSPEC_SLOT_DEEP_INVENTORY_V001.md:52`). |
| S05 | TYPE-U | SUSTAIN | The normalized external-history scalar is built, while the interacting continuum promotion/measure is not. Appeal 2's exclusivity claim is dissolved in §3.2, but that does not fill S05. |
| S06 | TYPE-U | SUSTAIN | A regulator class does not supply the required parent-derived finite condition or its covariance certificate. The registered re-posing removes a contradiction, not the missing member. |
| S07 | TYPE-U | SUSTAIN | The subtracted one-loop transverse form lacks the demanded interacting Ward identity, physical quotient, and photon-mass exclusion. Conditional/full98 and two-cutoff statements retain their explicit scope ceilings. |
| S08 | TYPE-U | SUSTAIN | No Lorentz- and packing-independent complete renormalized response member was found (`STAGE8_QSPEC_SLOT_DEEP_INVENTORY_V001.md:56`). |
| S09 | TYPE-U | SUSTAIN, retag required | The former finite-`c F^2` formulation was retired into S9-A determination plus S9-B exit. The zero-bare branch is adopted, not derived, and universal deformation exclusion remains false. The assembly's TYPE-U result is right, but the row must carry the successor typing. |
| S10 | TYPE-U | SUSTAIN | The finite outgoing construction does not produce the source-inclusive state projective limit. |
| S11 | TYPE-U | SUSTAIN | The finite Moller objects do not produce the demanded infinite-future source limit. |
| S12 | TYPE-U | SUSTAIN | The principal ruling re-poses this as regulator-family scheme covariance; neither that covariance nor O-SC1/O-SC2 is inhabited. |
| S13 | TYPE-U | SUSTAIN | The complete interacting pole-versus-infraparticle outcome remains open and gates S16. |
| S14 | TYPE-U | SUSTAIN | The charged-spectrum and threshold maps remain incomplete; primitive spectrum statements do not exhaust the complete interacting theory. |
| S15 | TYPE-U | SUSTAIN | Global source/trace/branch exhaustion remains unproved; no complete branch family member was found. |
| S16 | TYPE-U | SUSTAIN | The source is re-posed, the inline protocol is unexecuted, the S13 condition is open, and the physical carrier crossing is TYPE-U. See §4.1. |
| S17 | TYPE-U | SUSTAIN | Primitive CISP support is not the descendant test in the complete interacting outgoing sector. |
| S18 | PART-PROVABLE | **OVERRIDE -> TYPE-U** | The selected claim says integrality is derived, while `(Z1)/(Z8)` expressly say it is an authored codomain condition and not derived. A9 licenses the interface and inhabits nothing. See §2.2 and §4.2. |

The M-2 spot check found no missed member for S02, S04, S06, S08, or S10--S17.
Relevant bounded-negative anchors include
`STAGE8_QSPEC_SLOT_DEEP_INVENTORY_V001.md:50-56`,
`R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md:128-164`,
`R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md:157-189`, and
`STAGE8_SLOT16_REPOSED_SCOPE_AND_FK1_AUDIT_V001.md:452-479,713-744`.
These checks support only **no sealed member located in the demanded scope**.

### 2.2 S18 cannot receive the claimed upward grade

The adopted candidate's own type is decisive. V004 `(Z1)` says:

```text
Xi_N : D_N^Loc -> Lambda_N^cyc
Integrality is the AUTHORED codomain condition of this clause
and is not derived anywhere else.                                  (Z1)
```

V004 `(Z8)` repeats:

```text
Xi_N(x) in Lambda_N^cyc is the AUTHORED codomain condition of (Z1).
It is NOT a unit consequence and is NOT supplied or derived by (Z2). (Z8)
```

A9 adopts a lawful interface but says at lines 6-14:

```text
ADOPTION LICENSES; IT INHABITS NOTHING.
ExtSrc, G4-D, G5, FULL-G4, and G2-N remain open.
```

The assembly instead asserts:

```text
CHARGE QUANTIZATION IS DERIVED, NOT IMPOSED.                       (P-S18)
```

The entailment fails by direct comparison:

```text
authored integral codomain + uninhabited map
  does not imply
derived integrality of an actual source-to-loop assignment.
```

`(Z3)` supplies a conditional discontinuity theorem if a nonzero member is
formed. `(Z4)` specifies an addressed support-stratum factorization but marks it
uninhabited/unrunnable. Neither converts `(Z1)/(Z8)`'s authored integrality into
a derivation. A narrower conditional discontinuity statement may be
PART-PROVABLE; it is not the prediction the assembly chose and does not fill S18.

Therefore:

```text
CORRECTED_GRADE_BOARD = 18 TYPE-U / 0 PART-PROVABLE
FILLED_SLOTS = 0/18
```

### 2.3 Two non-grade corrections

1. S01's `T_R` must not be identified with `T_K`. The living Q-11 entry types
   `T_K` as a distinct K-indexed stationary-interval name with no supplied
   defining equation; the route was retired. S01 asks for absolute physical
   `T_R`. The grade remains TYPE-U.
2. S09 must be reported under the registered S9-A determination/S9-B exit that
   superseded the old formulation. The old ledger wording remains historical;
   it is not the operative proof demand. The grade remains TYPE-U.

---

## 3. V3 — the three appeal displays

### 3.1 Appeal 1: S06 versus S12 — DISSOLVED

The historical sentence exists at living-register entry Q-13:

> If slot 12 is discharged as written, slot 6 CANNOT supply a scale.

It is not the current state. The principal decision at lines 27-35 re-poses S12
and retains S06:

```text
S06: the normalization may originate at the regulator/finite-renormalization step.

S12: physical outputs are covariant over the declared regulator family;
     exactly one finite condition is admissible only when derived from the parent,
     never when selected by its outcome.
```

The live cleanroom registration at
`STAGE8_Q13_Q19_GOVERNING_REGISTRATION_RECORD_V001.md:162-200` carries that
ruling. In displayed functional form the two obligations are compatible:

```text
C_fin = C_fin(parent structure),                                  (O-SC1)
Output(R_1, C_fin) = Output(R_2, C_fin)
  for R_1,R_2 in the preregistered regulator family.              (O-SC2)
```

O-SC1 concerns the origin of the finite condition; O-SC2 concerns covariance of
outputs once the same parent-derived condition is carried. Neither is inhabited,
but they are not mutually exclusive. The assembly consumed a true historical
quote after its explicit supersession and incorrectly called the conflict
unadjudicated.

```text
APPEAL_1_VERDICT = DISSOLVED
```

### 3.2 Appeal 2: S05 external-A versus integrated-A arity — DISSOLVED

The actually sealed constructed type is external-history valued:

```text
Z_ext : A_hist x A_hist -> C,
Z_ext[A_+,A_-]
  = omega_in(W[A_-]^dagger W[A_+]),
Z_ext[A,A] = 1.
```

`COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md:30-48` displays
that object. Its own lines 112-127 state that the interacting continuum CTP
measure is unbuilt.

The integrated-A wording exists as a **triaged possible promotion**, not as a
second built and adopted type. `STAGE7_QSPEC_REVIEW_CANDIDATE_V002.md:199-200`
merely says that the interacting amplitude and its gauge/ghost/edge functional
measure remain open. `BOHM_EM_ORDER_TRIAGE_2026-07-28.md:52-58` says all sealed
executed artifacts keep A external; its lines 77-95 introduce the two readings
as a choice needing work.

The claimed logical exclusivity also fails. If a lawful gauge-history measure
and coupling were later supplied, an integrated object could consume the
external functional as an integrand:

```text
Z_int[J_+,J_-]
 := integral Dmu_gauge(A_+,A_-)
      Z_ext[A_+,A_-;J_+,J_-].                                    (A5-bridge)
```

The display is a type witness only, not a construction: `Dmu_gauge`, its lawful
domain, gauge quotient, and the promotion/response bridge are all missing. It
shows that the external and integrated forms can be successive layers rather
than mutually exclusive laws.

S05 therefore remains TYPE-U, now for the exact missing promotion, measure, and
arity-reconciliation bridge. A principal may still rule the intended terminal
type, but no contradiction between two sealed built types exists.

```text
APPEAL_2_VERDICT = DISSOLVED
```

### 3.3 Appeal 3: S03 construction choke — CONFIRMED-FOR-RULING

The sealed pins jointly display:

```text
complete_public_charged_action_unique = false;
Task19 = BLOCKED_AS_CONSTRUCTION;
larger_equivalent_mutation_class_bounded = false;
no sealed selector chooses weight, stiffness, extension, Pauli term,
or mutation-exclusion rule.
```

`PUBLIC_CHARGED_ACTION_UNIQUENESS_GATE_V002.md:62-100,108-138` gives the
admissible action family without a selector.
`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md:24-64,77-90` gives two
concrete completion countermodels and states that merely declaring one minimal
is not a derivation. `STAGE8_TASK19_SLOT3_DYNAMICAL_U1_ACTION_OBSTRUCTION_V001.md:67-116,130-140`
records the construction stop. No later supersession was located.

[PROVABLE] In the absence of a selector, choosing one completion is adoption,
not derivation. [PART-PROVABLE boundary] `unbounded` here means **not bounded or
finitely enumerated by sealed stock**; it is not a theorem that the mutation
class has infinite cardinality. The displayed response difference is symbolic,
not an executed finite-response calculation. Those qualifications do not remove
the construction choke.

```text
APPEAL_3_VERDICT = CONFIRMED-FOR-RULING
```

---

## 4. V4 — S16 and S18 physics sections

### 4.1 S16's three precisions

#### Precision 1 — source re-posed, not discharged

The dedicated audit's exact source transition is:

```text
old question: kappa_record -> kappa_Thomson
re-posed question: K_* -> kappa_Thomson.
```

The audit expressly says the obligation is not discharged, weakened, or
deleted; its source object changes. The assembly carries this correctly.

#### Precision 2 — threshold means S13

The conditioning variable is the interacting pole-versus-infraparticle outcome,
not the onset root. Thus:

```text
S13 unformed => S16 threshold-conditioned matching unformed.
```

The assembly carries this correctly.

#### Precision 3 — S16/LP-JII coupling

The DoR V006 product is typed `LOCAL-SHADOW`. The ratified P4 row identifies the
exact bridge from that shadow to a physical alpha-adjacent carrier as the same
J-II-class TYPE-U crossing. The necessary carrier chain is therefore:

```text
K_* on LOCAL-SHADOW
  --[LP-JII physical-carrier realization, TYPE-U]-->
K_*^phys
  --[threshold-conditioned comparison, S13-gated]-->
kappa_Thomson.
```

Without the first arrow, the source and target of the physical comparison do not
share a carrier. The inference is confirmed as a **necessary** condition only;
J-II inhabitance would not by itself supply S13 or execute the matching protocol.

```text
S16_JII_COUPLING = CONFIRMED
```

### 4.2 S18 exact adjudication

| Question | Verdict | Display |
|---|---|---|
| Is `(P-S18)` blind? | **YES, narrowly** | The assembly evaluates no assignment, physical quantity, measured value, or comparator. Blindness does not cure false provenance. |
| Is `(P-S18)` derivable from `(Z3)/(Z4)`? | **NO** | `(Z3)` is a conditional discontinuity theorem; `(Z4)` is an uninhabited support-stratum gate; `(Z1)/(Z8)` call integrality authored and not derived. |
| Is `(F-S18)` well-formed for the headline claim? | **NO** | Arms (a)--(c) can attack membership, structural emptiness, or `(Z3)` typing, but none falsifies the headline's provenance claim `derived, not imposed`. Arm (a) also tests an output excluded by the authored integral codomain rather than an independently derived theorem. |
| Is A9 inhabitance present? | **NO** | A9 licenses the interface and explicitly inhabits nothing. |
| Is OBS-22 correctly declined? | **YES** | It predicts the program's obstruction topology, not a public physical observable with a measured value/uncertainty and a same-alpha comparator. |

The falsifier can be repaired only after a valid S18 candidate is supplied. This
review does not author one.

### 4.3 Fresh attack — the operative Slot-18 eligibility predicate

The assembly's weakest point is not only the authored/derived reversal. It tests
four informal properties instead of the operative V011 predicate.
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md:2080-2089` requires an eligible
candidate to be:

```text
(i) absent as an outcome or target from the complete historical lineage;
(ii) computable from sealed BID output with no fitted parameter or post-selection;
(iii) paired with a public independently measured value and uncertainty;
(iv) not alpha or a deterministic re-expression of alpha; and
(v) structure-sensitive operationally: BID and at least one preregistered
    same-alpha comparator make distinct predictions.
```

The governing registration at
`STAGE8_SLOT18_Q23_Q28_GOVERNING_REGISTRATION_V001.md:331-397` adds that the
candidate-specific complete-lineage test has not been run, the current
eligibility stage has not been executed, and zero current families satisfy all
three registered screening conditions.

For OBS-21 the assembly supplies neither a bounded complete-lineage zero-result,
nor a public observable/uncertainty payload, nor a preregistered same-alpha
comparator with a distinct prediction. Saying that a quantity depends on
winding/topology is not the operational comparator test. Therefore even a
proven conditional discontinuity theorem would not yet qualify as S18.

This fresh attack independently sustains:

```text
S18 = TYPE-U
```

It uses no measured central value and performs no eligibility execution.

---

## 5. Consequence board

### 5.1 Items that survive review

1. Eighteen slot concepts are present in the correct order.
2. C7's nine-member dependency order and anti-selection rule are exact.
3. The standard is target-value-free but historically target-aware.
4. Zero of eighteen slots is filled.
5. S01--S17 are TYPE-U; no genuine member was killed by the assembly's six
   adversarial downgrades.
6. S16 is re-posed, S13-conditioned, and necessarily coupled to the J-II carrier
   crossing.
7. OBS-22 is correctly declined as a physical Slot-18 prediction.
8. S03's construction choke is a genuine item for ruling.

### 5.2 Defects requiring correction

| ID | Defect | Required correction |
|---|---|---|
| D1 | `ledger verbatim` false at S11 and S18 | restore `Moller` and list-leading `and`, or weaken the heading from verbatim |
| D2 | S06/S12 called an unadjudicated contradiction | carry the registered scheme-covariance ruling; remove this appeal/freeze blocker |
| D3 | S05's two readings called mutually exclusive sealed types | carry the external type as built and the integrated form as an unbuilt promotion/measure layer; retain TYPE-U |
| D4 | S18 upgraded on a claim opposite to `(Z1)/(Z8)` | downgrade S18 to TYPE-U; remove `(P-S18)` as a derived-integrality claim |
| D5 | `(F-S18)` called a falsifier of the provenance headline | do not call it such; it omits the derivation-versus-imposition failure and the operational eligibility tests |
| D6 | S09 reported without successor typing | carry S9-A/S9-B while retaining TYPE-U |
| D7 | S01 discussion risks `T_R`/`T_K` conflation | keep the two interval objects distinct |

Because D2 removes the alleged internal contradiction, the assembly's phrase
`ready_pending_evaluator` cannot retain its stated explanation. Because D4/D5
remove the only PART-PROVABLE prediction, the corrected demand-side board is
still incomplete at S18. This review does not freeze, adopt, or ratify a Q-spec.

---

## 6. Battery

### 6.1 F_PLDEC and fence discipline

The reader, response value, false anchor, shadow number, measured constants, and
eventual numeric consequences appear in no premise of a grade or appeal ruling.
The only equations executed are type comparisons and symbolic map composition.
No physical value was evaluated.

### 6.2 Anti-tuning

| Possible tuning path | Result |
|---|---|
| Use S16/Thomson matching to select S03--S13 | barred by C7; not done |
| Preserve S18 because it would be an attractive second prediction | rejected; its source says integrality is authored |
| Select an S03 completion because it yields a useful response | rejected; no sealed selector |
| Choose the S05 terminal arity by downstream convenience | rejected; the missing bridge is kept TYPE-U |
| Choose a finite condition by its output | barred by O-SC1; the principal ruling permits only a parent-derived condition |
| Use a cross-sector conversion silently | no cross-sector numerical conversion is formed; row is not applicable to this review |

### 6.3 Surface anchor

**Named actual objects:** the eighteen ledger entries, C7, the external-history
functional `Z_ext[A_+,A_-]`, the S03 action family and its two displayed
countermodels, `K_*`, `kappa_Thomson`, the LOCAL-SHADOW product type, the
candidate interface `Xi_N : D_N^Loc -> Lambda_N^cyc`, and the A9 gate list.

**Rails without members:** the complete S03 action, S05 gauge-history measure and
promotion, O-SC1/O-SC2 certificates, S13 outcome, J-II physical realization,
actual `Xi_N` inhabitance, the complete-lineage unused certificate, a public
observable payload, and a same-alpha comparator prediction.

No rail is treated as an object and no declared family is treated as inhabited.

### 6.4 MACHINERY-APPEAL handling

- Appeal 1 is dissolved by existing registered law, not by a lane-authored
  replacement.
- Appeal 2 is dissolved by type comparison and an explicit composability witness;
  its missing bridge remains TYPE-U.
- Appeal 3 remains a genuine ruling item. No completion is selected.

No fence blocked a structural result, so no additional MACHINERY-APPEAL is raised.

### 6.5 Self verb audit

| Verb/claim | Display above | Audit |
|---|---|---|
| `REFUTED` | D1--D7 include two stale/false appeals and a false upward grade | CLEAN |
| `SUSTAIN` for S01--S17 | §2.1 gives a demanded-object check per row; M-2 found no missed member | CLEAN; no emptiness claim |
| `OVERRIDE -> TYPE-U` for S18 | `(Z1)/(Z8)`, A9, failed entailment, and V011 eligibility attack displayed | CLEAN |
| Appeal 1 `DISSOLVED` | principal text plus live registration and compatible O-SC1/O-SC2 equations | CLEAN |
| Appeal 2 `DISSOLVED` | external built type, open integrated layer, and typed composition witness | CLEAN; no integrated member claimed |
| Appeal 3 `CONFIRMED-FOR-RULING` | construction stop, two countermodels, no selector, and scope qualification | CLEAN |
| S16/J-II `CONFIRMED` | necessary carrier chain displayed; sufficiency expressly refused | CLEAN |
| OBS-22 `correctly declined` | V011 physical-observable and comparator criteria applied | CLEAN |
| `blind` for `(P-S18)` | restricted to non-evaluation, not correctness or admissibility | CLEAN |
| `0/18 filled` | all rows remain TYPE-U after the single downward override | CLEAN |

ASSEMBLY = REFUTED (+verbatim defects S11/S18; stale S06/S12 appeal; false S05 exclusivity; S18 provenance/grade/falsifier defects; S01/S09 typing corrections)
GRADE_OVERRIDES = S18 PART-PROVABLE -> TYPE-U
APPEAL_1_S06_S12 = DISSOLVED
APPEAL_2_S05_ARITY = DISSOLVED
APPEAL_3_S03_CHOKE = CONFIRMED-FOR-RULING
S16_JII_COUPLING = CONFIRMED
VERB_AUDIT_SELF = CLEAN
