# STAGE 8 — REQUIRE-BUILD G1: THE MATERIALITY FORK — DOES R_record,L FACTORIZE?
## BLIND RECORD-NATIVE DETERMINATION — REQUIRE-BUILD-G1 — [CLAIMED]

Date: 2026-08-13
Role: DETERMINATION. The single node G1 — whether the record's energy-response
`R_record,L` FACTORIZES — is decided, or the precise minimal unbuilt object(s) are
named. Honest outcomes only: FACTORS(free) / DOES-NOT-FACTOR(forced) / GAP. No value
of `n`, `kappa`, `alpha`, any coupling, scale, length, coefficient, spectrum, or
constant is computed, bounded, estimated, or compared. Every symbol is symbolic;
every numeric string is a sealed-text quotation or an exact structural integer.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

Fences held throughout: CONNECTION-ONLY, SCALE-FREE. NO scale (`ell_P`, `K_KK`,
`c^2`, metric length, `G_4`, `Vol_4`, Lorentzian causal diamond, sea-covariance-on-
diamonds). NO imported GR (KK ansatz, Einstein-Hilbert, quasilocal energy MS/BY,
`kappa_record = kappa_Thomson`). NO faithfulness premise. Where certifying `C`
requires any of these, the object is NAMED as the GAP and NOT imported. No register,
tracker, plan, road, ledger, or lens read.

---

## 0. VERDICT IN ONE LINE

**The fork is a GAP, and the gap is exactly the seam Q-1013 names. The object `C`
that decides the fork is TYPED connection-only/scale-free (the FORM's Gate-5 kernel,
intensive per the count `N_4`, no metric argument), but its EXISTENCE has no
connection-only certification of record: the ONLY written existence-certification
route — R-L2b's Schatten-2 estimate on `X_n = C_n(V_n(a)-V_n(0))C_n` — is built on
the sealed SEA COVARIANCE `C(r) = (1/2)delta^3(r)I - i alpha·r/(2 pi^2 |r|^4)`
(position-space, homogeneous degree -3) over LORENTZIAN CAUSAL DIAMONDS with volumes
`|D|_3 ~ L^3`, `|D|_4 ~ L^4`, and it is refuted-as-written / "short one power" on
cluster-summability with NO SEALED REPLACEMENT. Because `C` is not certified to exist
connection-only, the cross-block `Phi_f^T C Phi_H` cannot be evaluated; the write-
chain block datum `G3` and the scalarization pair `G2` (fixing `mu`) are separately
unbuilt. NET = GAP, with three named minimal unbuilt objects, the primary one being
the connection-only cluster-summability sub-estimate certifying `C`.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Full digests
recomputed by `shasum -a 256` at path this relay before reading; first 16 hex shown.
For each: CONN = connection-only/scale-free of record; SCALE = scale/GR-bearing.

| Tag | Source | Seal (first 16) | Type | Role |
|---|---|---|---|---|
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b` MATCHES-TASKED | CONN | closes R to `n^2 Phi^T C Phi`; the cross term; the factorization fork; typing of C |
| POSE | `STAGE8_REQUIRE_TARGET_POSED_V001.md` | `e3c482b5facfb741` MATCHES-TASKED | CONN | pins C as THE ONE OBJECT; the forced/free binary; C=R-L2b at the gate |
| CHK | `STAGE8_REQUIRE_TARGET_POSE_CHECK_V001.md` | `86068343c6e0ac1e` MATCHES-TASKED | CONN | adversarial check: C_EQ_RL2B OVERSTATED; the scale/metric seam flagged |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d4904706276` SEALED-OK | CONN | bedrock: `Gamma_K = sum_(connected C) gamma_C`; the Gate-5 obligation; Duhamel-reproduction; tangent complex |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03` SEALED-OK | CONN | ratified `F_N = P_0 + Z_N P_ch`; connection/holonomy-only; source operator |
| M06 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac5` SEALED-OK | CONN | scalarization unbuilt: survivor CONTINUUM; state-port TYPE-U |
| ECO | `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md` | `0f3082cab910f2eb` SEALED-OK | SCALE | R-L2b carries connected extensivity; the sea covariance; the ~eps^-3 divergence |
| RFA | `STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md` | `2ede02aea415157a` SEALED-OK | SCALE | R-L2b object `X_n`; the exponent target `alpha >= 1`; `|D|_3`, `|D|_4`; the cancellation |
| R4O | `STAGE8_RL2B_REFUTING_BRANCH_FOUR_OBLIGATION_ANALYSIS_PROPOSED_V001.md` | `f6fe40724ff4dfeb` SEALED-OK | SCALE | refuted-as-written; `connected_extensivity = UNRESOLVED_BY_RL2B_REFUTING_BRANCH` |
| XSM | `STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md` | `3c008ecccc2b01ac` SEALED-OK | SCALE | internal/external metric conversion; `ell_P`, `G_4`, Lorentzian diamond, `beta` — DISTINCT node, fenced OUT |
| T1 | `STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md` | `e6fae1428cecffb3` MATCHES-FORM | CONN | U2 (conversion) vs U4 (R-form) census; void-overdetermination background |

All eleven seals match. No register/tracker/plan/road/ledger/lens read. No git action.
Output name probed before write: ABSENT.

---

## 2. DELIVERABLE 1 — THE POST-LIMIT C AND THE CROSS-BLOCK, OF RECORD

**The finite-N kernel (coherent, all-pairs) [FORM D4 :217-231, §4.2 :315].** With the
record sandwich leaving the source operator `F_N = P_0 + Z_N P_ch` (M03; FORM B5) and
any scalarization producing sector weights `w_0, w_ch`, the exact scalarized finite-N
Hessian is

```text
Hess Gamma_N(a,b) = n^2 · mu(w) · Phi_tot[a] Phi_tot[b],   Phi_tot[a] = sum_j <ell_j,a>,
  => the finite-N coherent kernel is C_jk^(finite) = mu  for ALL pairs (j,k)  (RANK ONE),
  mu(w) symbolic rational in (w_0, w_ch, rel. phase);  mu ≡ 0  iff  w_0 w_ch = 0.
```

This is an ALL-PAIRS, separation-independent, rank-one weight. It is an INTERFERENCE
term: on the neutral sector alone the amplitude is constant; on the charged sector
alone `|Z_N| = 1` makes `-log|A|` identically zero; the entire connection response
lives in the neutral×charged cross term (FORM D4(ii) :226-229).

**The post-limit object [FORM D5 :234-265, B1/B3 from V011].** The sealed
susceptibility is intensive and post-limit — `R_record,L = d^2/ds dt
[Gamma_K(sa+tb)/N_4(K)]` "evaluated after the thermodynamic/linked-cluster limit"
(V011 :1292-1300), with the sealed constraint "The Duhamel covariance computed from
`G_L` must reproduce this Hessian" (V011 :1299-1300). Pulling the covariance back
through the write differentials `Phi = (<ell_j,·>)_j`:

```text
R_record,L(a,b) = n^2 · sum_(j,k) C_jk <ell_j,a><ell_k,b>  =:  n^2 (Phi^T C Phi)(a,b)
                                                     on difference-branch tangents.
```

The finite-N all-pairs weight `C_jk = mu` is NOT cluster-summable, so it CANNOT be the
post-limit form; the thermodynamic/linked-cluster limit must replace it by a cluster-
decaying `C_jk` (FORM D5 :254-262; V011 :1264-1281). `C` is TYPED of record: symmetric,
PSD, intensive (per `N_4`), additive over disjoint cells with no extensive factor,
n-independent (`chi_n(identity)=1`) — and REQUIRED cluster-summable with subextensive
boundary corrections. The typing is connection-only: the ratified object is "indexed by
CONNECTION / HOLONOMY HISTORIES ALONE — no metric argument anywhere" (FORM B5, on M03/T3).

**The cross-block [FORM §4.2 :310-331].** On the derived tangent split
`P_h A^1 = H ⊕ im(d_1^dagger)` (from the sealed `d_1 d_0 = 0`), with `phi_f`, `phi_H`
the flux- and H-components of the write functionals:

```text
R_record,L = n^2 [ Phi_f^T C Phi_f   Phi_f^T C Phi_H ]   over [ flux = im(d_1^dagger) | H = dim-32 block ].
                 [ Phi_H^T C Phi_f   Phi_H^T C Phi_H ]
```

`Phi_f^T C Phi_H` is a SUB-BLOCK of the same one kernel `C` (CHK Attack 1 CONFIRMED),
not a second object. Its vanishing is the post-limit fork decider (FORM :331).

---

## 3. DELIVERABLE 2 — CONNECTION-ONLY EXISTENCE OF C

**The question:** can the intensive post-limit kernel `C` be certified to exist
WITHOUT the scale-bearing sea-covariance/R-L2b route? Either (a) exhibit the
connection-only certification (or the precise missing sub-estimate), or (b) show the
certification provably needs a scale/GR import.

### 3.1 There is NO connection-only certification of record

V011 states the enabling limit as an OBLIGATION, not a construction:

```text
V011 :1264-1281: "the complete amplitude must additionally admit a linked-cluster
  expansion Gamma_K = sum_(connected C subset K) gamma_C, with a target-free locality
  bound that makes gamma_density = lim Gamma_K / number_of_4_cells(K) exist and makes
  boundary corrections subextensive. ... The disjoint theorem is proved; the connected
  thermodynamic limit remains a Gate-5 obligation."
V011 :1741, :1751-1752 (Gate-5 pass conditions): requires "a local linked-cluster
  thermodynamic density on connected cellulations" and "the full-neighborhood analytic
  locality bound and cellulation-independent thermodynamic limit to pass".
V011 :2185:  connected_linked_cluster_density_proved = false
```

So the DISJOINT additivity is proved connection-only (FORM P2/B2), but the CONNECTED
limit that makes `C` an intensive object — the exact property that certifies `C`
exists at all — is named and NOT built. No connection-only certification exists of
record.

### 3.2 The ONLY written certification route imports scale/GR

Of record, the connected-extensivity obligation is bound to exactly one written
estimate, R-L2b, and there is no other and no replacement:

```text
ECO §2 :92-100: obligation 4 = CONNECTED EXTENSIVITY = "convergence of
  sum_n tau_R^n <B_K(A)^n>_connected / n! uniformly as cells shrink ... depends
  entirely on the shrink rate of the connected cumulants, which IS R-L2b's exponent.
  ... IF R-L2b CLOSES, THE EXTENSIVITY QUESTION CLOSES WITH IT."
ECO §4 :132-134: "IF connected extensivity does fail, NO SEALED REPLACEMENT
  CONSTRUCTION EXISTS — both lanes confirm this independently."
```

And R-L2b's object is built entirely on scale/metric-bearing content:

```text
ECO §5 :151-171: THE SEALED SEA COVARIANCE, verbatim:
  C(r) = (1/2) delta^3(r) I - i alpha·r / (2 pi^2 |r|^4),
  off-diagonal modulus EXACTLY 1/(2 pi^2 |r|^3); HOMOGENEOUS DEGREE -3.
  || 1_D C 1_D ||_2^2 = int int_{DxD} |C|^2 diverges ~ eps^-3 : NOT Hilbert-Schmidt.
RFA §4 :66-89:  HS norm over the SPATIAL one-particle space int int d^3x d^3y;
  ISOTROPIC diamond |D|_3 ~ L^3, |D|_4 ~ L^4;  ||C(V(a)-V(0))C||_2 <~ M·L^4 = M·|D|_4.
RFA §3 :44-61:  D5 requires the diamond exponent alpha >= 1 (aggregation flat);
  alpha < 1 gives aggregate divergence A^(3(1-alpha)) in the diamond aspect ratio.
```

Every load-bearing ingredient here is a forbidden import: the position-space kernel
`C(r)` with an explicit length `|r|` and the coupling `alpha`; the Lorentzian causal
diamond `D`; its 3- and 4-volumes `|D|_3, |D|_4` and the scale `L`; the Schatten-2
norm over a spatial/metric measure; and an exponent target that is a statement about
`|D|_4` scaling. The pose-check reaches the same finding independently (CHK Attack 3
OVERSTATED, Attack 4 flagged seam): "the existence-certificate it binds to `C` (R-L2b)
lives on causal diamonds with an explicit degree -3 kernel, whereas `C` is asserted
scale-free" (CHK :117-130, :150-161).

### 3.3 That route is itself incomplete — "short one power"

R-L2b is not merely scale-bearing; it is unbuilt on exactly the cluster-summability
power:

```text
RFA §4 :63-81:  alpha = 1 IFF the degree -3 singularity FULLY cancels in the
  difference V(a)-V(0); the withdrawn v001 value alpha = 1/2 is an AREA law — a
  support-drawn bound — struck by C-13; "whether the singularity fully cancels is UNKNOWN".
R4O §4 :102-140:  refuted as written under the H1/F'-5 chain — "no admissible uniform
  M exists for sup_n ||X_n||_2 <= M";  connected_extensivity = UNRESOLVED_BY_RL2B_
  REFUTING_BRANCH (R4O :139, :206-208).  No sealed replacement (ECO §4).
```

The deficit is the "short one power": cluster-summability (aggregation flat, `alpha=1`,
the volume law) versus the naive support-drawn area law (`alpha=1/2`). The missing
sub-estimate is the full cancellation of the degree -3 singularity in the difference —
i.e. the connected-cumulant shrink rate — and it is UNKNOWN of record.

### 3.4 Determination: GAP (not NO)

- **Not YES:** no connection-only certification of `C` exists of record (§3.1).
- **Not NO:** the certification does NOT provably REQUIRE scale/GR. The object `C` is
  TYPED intensive/connection-only with no metric argument (FORM D5 :248-252, B5), and
  RFA §2 :29-42 argues the finiteness "cancellation rate does not care how the atom was
  cut up" — a UV/intra-diamond phenomenon "occurring BELOW the diamond scale", so the
  load-bearing quantity is a separation/shrink rate, not the volume. The scale-bearing
  sea-covariance framing is the only WRITTEN realization, not a proof that a connection-
  only realization is impossible. Impossibility is not established; so this is not NO.
- **Therefore GAP.** The exact scale/metric object the only-written certification
  imports (§3.2), and the exact connection-only sub-estimate that is missing (§3.3),
  are both named. The missing connection-only sub-estimate is precisely the cluster-
  summability / connected-cumulant shrink-rate estimate — V011's cellulation-independent
  linked-cluster thermodynamic limit / "full-neighborhood analytic locality bound"
  (:1751-1752) — re-expressed on the connection-only Duhamel covariance of the write
  generators (the decay of `C_jk` in cell separation replacing the all-pairs `mu`),
  WITHOUT the sea-covariance-on-diamonds representation. That is the "short one power".

---

## 4. DELIVERABLE 3 — THE CROSS-BLOCK'S FATE, AND MU

### 4.1 The cross-block is a clean binary with unbuilt inputs [FORM §4.2, CHK Attack 2]

`Phi_f^T C Phi_H != 0` (SURVIVES / does-not-factor / forced) iff, jointly:

```text
(F-i)   phi_f != 0            write chains reach the flux block im(d_1^dagger)  — datum G3
(F-ii)  phi_H != 0            write chains reach the dim-32 flux-free block H    — datum G3
(F-iii) Phi_f^T C Phi_H != 0  the kernel C does not annihilate the cross pairing — object G1
```

The algebra is exhaustive and admits no third case: a rank-one form is never a nonzero
direct sum, and block-diagonal (sector-respecting) congruence sends the cross block
`M_fH -> S_f^T M_fH S_H` with `S_f, S_H` invertible, preserving nonvanishing (FORM
:315-331, verified CHK :64-92). The complement, `Phi_f^T C Phi_H = 0`, is FACTORS(free):
at post-limit, with both diagonal blocks nonzero, this is a NON-degenerate direct sum
`R_flux ⊕ R_H` — both sectors respond, decoupled (CHK CORRECTION :79-92, :263-265),
which is exactly the tasked FACTORS reading.

### 4.2 Why the fate is UNDETERMINED

Each deciding input is unbuilt of record:

- **(F-iii) cannot be evaluated** because `C` itself is not certified to exist
  connection-only (§3): the post-limit cross pairing has no value until the cluster-
  summability sub-estimate is discharged.
- **(F-i)/(F-ii) cannot be evaluated** because the write chains `ell_j` support/current-
  density realization is `G3`, NOT SUPPLIED (FORM B7, D6/G3 :278-282, §4.3 :334-343).
  "No sealed object forces it either way" (FORM :340-342).
- The finite-N anchor is additionally gated by `mu` (§4.3).

Therefore CROSS_BLOCK = UNDETERMINED, pending three named objects: the connection-only
existence of `C` (the missing cluster-summability sub-estimate, G1), the write-chain
block datum `G3`, and the scalarization pair `G2` fixing `mu`.

### 4.3 MU (node G2): UNBUILT, and it gates the whole response

`mu(w)` is the scalarization coefficient, `mu ≡ 0 iff w_0 w_ch = 0` (FORM D4 :222).
The pair `(w_0, w_ch)` is fixed by the state-port, which is sealed unbuilt:

```text
M06 lead + §6 :32-56, :500-505:
  UNIQUE_SCALARIZATION_FORCED = false | TYPE-R  (a CONTINUUM omega_t = (1-t)omega_H +
    t omega_epsilon of pairwise-inequivalent normalized positive functionals survives
    EVERY executable constraint — normalization, U1 reality, one-cell, inclusive identity)
  RHO_PRE_PLACED = false | TYPE-U ;  U2_STATE_PORT_INSTANTIATED = false | TYPE-U
  PHYSICAL_SCALARIZATION_SURVIVOR_COUNT = NO_VERDICT
```

So `mu` is UNBUILT: `(w_0, w_ch)` ranges over a continuum, and whether `w_0 w_ch = 0`
(hence whether `mu = 0`) is not settled — some survivors give `mu != 0`, some (supported
on a single source sector) give `mu = 0`. **Bearing on the cross-block:** `mu` is a
GLOBAL prefactor on the entire finite-N response `n^2 mu (phi_f + phi_H) ⊗ (phi_f +
phi_H)`. Because the whole connection response is the neutral×charged interference term
(FORM D4(ii)), `mu = 0` kills `R_record,L` in full — cross term and both diagonals. Thus
`mu != 0` (equivalently both source-sector weights nonzero) is a NECESSARY condition for
the cross-block to survive, and it is unbuilt. Even given `G1` (C exists, cross pairing
nonzero) and `G3` (write straddles both blocks), the cross-block still vanishes if the
unbuilt state-port selects `w_0 w_ch = 0`.

---

## 5. DELIVERABLE 4 — NET

**NET = GAP.** The materiality fork G1 cannot be decided FACTORS or DOES-NOT-FACTOR of
record, because every input to the decider `Phi_f^T C Phi_H` is unbuilt. The precise
minimal unbuilt objects that would close the fork, in load-bearing order:

```text
[1] (PRIMARY — the Q-1013 seam)  The CONNECTION-ONLY existence certification of the
    post-limit Gate-5 cluster kernel C: the missing cluster-summability / connected-
    cumulant shrink-rate sub-estimate (V011's cellulation-independent linked-cluster
    thermodynamic limit; the "short one power"), re-expressed on the connection-only
    Duhamel covariance of the write generators. Of record this is reachable ONLY via
    the scale/GR-bearing R-L2b sea-covariance-on-diamonds route, which is refuted-as-
    written with NO sealed replacement. Until it is discharged, (F-iii) has no value.
[2] The write-chain support/current-density realization G3 — whether phi_f != 0 AND
    phi_H != 0 (RA27-3 fields NOT SUPPLIED). Decides (F-i)/(F-ii).
[3] The scalarization state-port G2 — the pair (w_0, w_ch) fixing mu; mu != 0 (i.e.
    w_0 w_ch != 0) is necessary for any nonzero response. A CONTINUUM survives; unbuilt.
Then the fork decision itself is the vanishing/non-vanishing of Phi_f^T C Phi_H,
computable only once [1] constructs C.
```

This is sharper than, and consistent with, the FORM's FACTORIZES = UNDETERMINED: the
undecidability is not merely the datum `G3` but is anchored, at post-limit strength, on
the fact that `C`'s own connection-only existence is unbuilt. The winding is not
reached here (the FORM already fixes it as a global `n^2` prefactor, NOT-FORCED on
either branch); this determination concerns only whether the response factorizes.

---

## 6. OVERCLAIM / IMPORT AUDIT

```text
DOES NOT import the sea covariance, any diamond, |D|_3, |D|_4, ell_P, G_4, c^2, K_KK,
  the Fubini-Study/Lorentzian metric conversion, or alpha: each is NAMED as the GAP's
  scale/metric content (§3.2) and held OUT. The determination runs on the connection-
  only ratified object and the typed (not constructed) kernel C.
DOES NOT flatten GAP into DOES-NOT-FACTOR by the attractiveness of the interference
  cross term: the cross term's very existence depends on C, G3, and mu, all unbuilt.
DOES NOT flatten GAP into FACTORS: the decoupled (Phi_f^T C Phi_H = 0) branch is equally
  unreachable without C.
DOES NOT claim C provably needs scale (that would be NO): impossibility of a connection-
  only certification is not established (§3.4); the object is typed scale-free and the
  cancellation rate is argued volume-independent (RFA §2).
DOES NOT decide the winding, compute mu/C/beta/any value, or consume faithfulness.
XSM (the internal/external metric conversion, ell_P/G_4/Lorentzian diamond/beta) is a
  DISTINCT node (T1 U2, not U4) and is the ENERGY comparand's object, NOT C's
  certification route; fenced OUT, not consumed.
```

---

## 7. FLAG BLOCK

```text
C_CONNECTION_ONLY_EXISTENCE = GAP(missing connection-only sub-estimate: the cluster-
  summability / connected-cumulant shrink-rate estimate that certifies the post-limit
  Gate-5 kernel C exists — V011's "cellulation-independent thermodynamic limit" /
  "full-neighborhood analytic locality bound" (:1751-1752) / "the connected
  thermodynamic limit remains a Gate-5 obligation" (:1281) — re-expressed as the decay
  of C_jk in cell separation replacing the all-pairs weight mu, on the connection-only
  Duhamel covariance of the write generators. This is the "short one power": cluster-
  summability (alpha=1, volume law) vs the support-drawn area law (alpha=1/2). Of
  record it is reachable ONLY via the scale/GR-bearing R-L2b route, which imports:
  the sealed sea covariance C(r)=(1/2)delta^3(r)I - i alpha·r/(2 pi^2 |r|^4) [position-
  space, degree -3, carries |r| and alpha]; the Lorentzian causal diamond D with
  |D|_3~L^3, |D|_4~L^4 and scale L; the spatial one-particle Schatten-2 norm; and the
  exponent target alpha>=1 (a |D|_4-scaling statement) — ECO §5 :151-171, RFA §3-§4
  :44-89. That route is refuted-as-written / short-one-power (R4O :139; whether the
  degree -3 singularity fully cancels is UNKNOWN, RFA §4) with NO sealed replacement
  (ECO §4 :132-134). NOT "NO": C is TYPED intensive/connection-only, no metric argument
  (FORM D5 :248-252, B5), and impossibility of a scale-free certification is not proven.)

CROSS_BLOCK = UNDETERMINED(pending three objects: (i) the connection-only existence of C
  above — until then Phi_f^T C Phi_H has no value; (ii) the write-chain block datum G3
  (phi_f != 0 AND phi_H != 0), RA27-3 fields NOT SUPPLIED, FORM §4.3 :334-343; (iii) the
  scalarization pair G2 fixing mu. The algebra is a clean binary with no third case
  (rank-one; block-diagonal congruence preserves a nonzero cross block, FORM :315-331):
  Phi_f^T C Phi_H != 0 => SURVIVES/DOES-NOT-FACTOR; Phi_f^T C Phi_H = 0 with both
  diagonal blocks nonzero => VANISHES/FACTORS as a non-degenerate direct sum R_flux ⊕
  R_H, CHK :263-265. The decider is well-posed; its inputs are unbuilt.)

MU_G2 = unbuilt(mu(w) symbolic in the sector weights (w_0, w_ch); mu ≡ 0 iff w_0 w_ch =
  0, FORM D4 :222. The state-port that fixes (w_0, w_ch) is sealed unbuilt: a CONTINUUM
  omega_t of inequivalent normalized positive functionals survives every executable
  constraint; UNIQUE_SCALARIZATION_FORCED=false TYPE-R; U2_STATE_PORT_INSTANTIATED=false
  TYPE-U; PHYSICAL_SCALARIZATION_SURVIVOR_COUNT=NO_VERDICT — M06 :32-56,:500-505. Bearing
  on the cross-block: mu is a GLOBAL prefactor on the entire finite-N response
  n^2 mu (phi_f+phi_H)⊗(phi_f+phi_H); mu=0 kills R_record,L in full (the whole response
  is the neutral×charged interference term, FORM D4(ii) :226-229). So mu != 0 (both
  source-sector weights nonzero) is a NECESSARY condition for the cross-block to survive,
  and it is undetermined over a continuum.)

NET = GAP(precise minimal unbuilt objects, load-bearing order: [1 PRIMARY, the Q-1013
  seam] the connection-only cluster-summability sub-estimate certifying the post-limit
  Gate-5 kernel C — currently only reachable via the refuted-as-written, scale/GR-bearing
  R-L2b sea-covariance-on-diamonds route, no sealed replacement; [2] the write-chain
  support/current-density realization G3 (phi_f, phi_H); [3] the scalarization state-port
  G2 fixing mu != 0. The fork decision is the vanishing of Phi_f^T C Phi_H, evaluable
  only once [1] constructs C. FACTORS vs DOES-NOT-FACTOR cannot be issued of record.)

FORBIDDEN_IMPORTS = none entered the determination. FLAGGED as the GAP's content, held
  OUT: the R-L2b certification route carries SCALE + GR — the degree -3 sea covariance
  C(r) (with |r| and alpha), Lorentzian causal diamonds, |D|_3/|D|_4 volumes and scale
  L, the spatial one-particle Schatten-2 norm, the alpha>=1 exponent; and, as the DISTINCT
  energy-comparand node (not C's route), XSM's ell_P/G_4/Lorentzian-diamond/beta. None
  consumed; all named.

MACHINERY_INVOKED = no. Nothing computed, bounded, estimated, or compared; C, mu, alpha,
  n, kappa, beta and every scale/spectrum left symbolic; the cross-block decider posed,
  not evaluated; seals recomputed at path before reading (V011 packet member aa7c6d49);
  no register/tracker/plan/road/ledger/lens read; no git action.

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel.
```
