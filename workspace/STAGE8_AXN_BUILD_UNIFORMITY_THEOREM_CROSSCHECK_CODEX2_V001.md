# STAGE 8 / [PLAN:AXN-BUILD-A39] — UNIFORMITY THEOREM CROSS-CHECK
## CODEX 2 LANE — V001

Relay 872. The relay sidecar, lane guard, `PROGRAM_STATE_BRIEF_V005.md`, and
the subject were verified before reading. Subject:
`STAGE8_AXN_BUILD_UNIFORMITY_THEOREM_DARIO_V001.md` =
`6b676829f46ecb2a28ade2646efc5830484d7ac0c0202bc9623d4dfc5133a02b`.

GATES: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. No member was bound; no fixed point, end
test, smooth import, electromagnetic identification, common-cell formation,
junction evaluation, physical numerical evaluation, measured-constant
comparison, register write, plan write, tracker write, git action, or chain
invocation occurred. PE-1 through PE-13 remained pointer-only and were not
opened.

**CLAIM STATUS: every headline and verdict in this report is CLAIMED.** The
finite matrix values below are structural operator diagnostics, not physical
quantities.

---

## 0. Lead

**`THEOREM = PARTIAL, WITH TWO CORRECTIONS.`** The finite-incidence theorem
and its anchor stand. The two corrections are:

1. a scalar split is rejected because the census is the closed pair of
   images of two fixed operator-space projectors. Merely observing that both
   split pieces have the same parity does not make a projector applied to an
   individual split piece return the unsplit block;
2. a mixing unitary is a valid algebraic negative control, but it is not in
   the currently sealed admitted action. The sealed generators are
   grading-homogeneous, so the homogeneous-action condition is
   **EXCLUDABLE** at the action bytes rather than live.

The local `circle x Z2` classification and the census invariance across that
family are sound. At more than one record edge, independently admissible
local choices give the larger product family `(circle x Z2)^{N_p}`; the
diagonal `circle x Z2` used in 871 is a sufficient subfamily, not the full
global multiplicity unless equal choices at every edge are separately
required. The invariance proof covers the full product family.

The remaining live condition is finite incidence. The closure contains
finite-parent constructions and a quasi-local inductive limit, but neither a
class-wide finite-incidence constraint nor an infinite/incomplete Hilbert
tensor implementation of the grading. Thus universal promotion beyond the
finite-incidence sub-class remains stopped.

---

## 1. Source closure and content-addressed spans

### 1.1 Pins

The complete `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_V001` bundle
rehashes against its group seal, including its audit, verifier, tests,
results, specification, and review. The specification's full 15-authority
table was then parsed from bytes and all 15 files independently rehashed;
every row matched.

Load-bearing source spans are:

| role | sealed source and SHA-256 | byte span | span SHA-256 |
|---|---|---:|---|
| `Adm_rec` predicate cuts | `STAGE8_AXN_BUILD_GATED_DEMAND_RETYPING_CODEX2_V001.md` = `e681754ffb104723b322d72e6ac0fc271a34e1710fafe0f39bc19023b3f3cb48` | `[12418,14680)` | `5eca1150e970bf4897dfb03504e3820fa9b2f15eb746fd425240f7712a94eeb0` |
| exact theorem demand | same | `[14680,17706)` | `a0b1a6ca719f6375946050e7908cdd0042a5cfca6cbcffb6d82eee347fdaebf8` |
| cellular grading and quadrature | `BID_GRADED_BOUNDARY_SUPERCONNECTION_REPAIR_V001.md` = `2215f79cbe69f1de1643427ad5d422b0c3064ff758327e43ff1629de8633f72d` | `[344,1502)` | `c61643e4d3990bf10ae8d3707761d2dffaa9689d3aca4614fa94a4fa220c32ba` |
| finite global carrier and admitted action | `BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md` = `949181d78aca143ebef06eacc4ab1018d43cdf79962ee8c78c350f654a7555dd` | `[1103,4006)` | `398b250f0a59ddf8ba1d51ffdb7f80d89c460dd4683068edbf76c523436d9e8a` |
| finite exhaustion and inductive/projective limits | `PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md` = `532b0f0eac4ac749ba3e24954db356f7ca0f98c4f730030075c463efa3158efb` | `[424,1619)` | `5611edac866e670f51f851964b3d0de3da29fd6cd2d664442c2b9eadf303e40c` |
| parent record tensor | `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md` = `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9` | `[4287,4905)` | `a4953cb66daa6dacf588351447c24defea88068681cbd586e7dd8452a1207f6a` |
| finite first-order parent | same | `[5711,6867)` | `eddc2e9ab66e1036e7defdc514b61214e0adef3b48fced3c3aa7a67b6df5f2c3` |
| quasi-local record limit | same | `[9198,10268)` | `0f94b343e788e87e85e7cd3d6d7b53f4aba2745caaf3f10cb994f9352d8065eb` |
| typed orientation carrier | `STAGE8_7A_ORIENTATION_UNITARY_CODEX2_V001.md` = `831c0dd826c40a4e156e5d6104ca87cd08116ba2d30af1f7b1d36f31c2c72ed2` | `[2488,4016)` | `8421e1197fa00a1ab64dc63652225ac462ac97b7a332764c5da318a40231b2ab` |
| complete orientation square | same | `[4016,4891)` | `32b4ef29b2f31f7870435872a52230c96d20a776a80cc3945dde195fc3ce68ae` |

Every whole-file digest is fixed either by its verified sidecar or by the
verified parent pin table; every span row carries a full content digest.

### 1.2 Closure-first declaration for the two conditions

Before any absence statement, define:

```text
C0 := unique union of
  the six 870 quantifier-cut suppliers:
    EXITB_SCHEME, SC4_RECORD_TYPING, DIRECTION_RELATION_CROSSCHECK,
    CALCULUS_V2, CARRIER_CLASS_FORCING, GLOBAL_BOUNDARY_DESCENT;
  the two local grading suppliers:
    GRADED_BOUNDARY_SUPERCONNECTION_REPAIR,
    COMPLETE_ONE_NORMAL_ZERO_FORM_ENUMERATION;
  all fifteen authorities pinned by the R3_4 parent specification.

|C0| = 20 unique sealed files after overlap removal.

C_action := C0 union
  STAGE8_7A_ORIENTATION_UNITARY_CODEX2_V001.md.

|C_action| = 21 unique sealed files.
```

Each cut-source sidecar and all 15 parent-authority hashes matched before the
meaning probe. M-2 covered exact tokens, whitespace normalization,
hyphen/space/underscore variants, and scope/self-reference. The search
included `infinite tensor`, `incomplete tensor`, `reference sequence`,
`reference vector`, `reference state`, `von Neumann tensor`, `inductive
limit`, `projective limit`, `quasi-local`, `finite K`, `incidence count`, and
semantic variants.

---

## 2. Task A(1): parity projectors and clauses 1/2/3/4/6

Let `J_G(M)=G M G`, with `G=G*` and `G^2=I`. Then `J_G^2=id` and

```text
P_even = (id+J_G)/2,       P_odd = (id-J_G)/2.
```

Direct algebra gives:

```text
P_even + P_odd = id;
P_even^2 = P_even;
P_odd^2 = P_odd;
P_even P_odd = P_odd P_even = 0.
```

Because conjugation by a self-adjoint unitary is self-adjoint on the
Hilbert-Schmidt operator space, the two images are orthogonal. Therefore:

| clause | independent finding |
|---:|---|
| 1 | **CONFIRMED.** `Can_p(D_p^2)` is the receiver-bound image pair of two fixed maps of `D_p^2` and the sealed grading. |
| 2 | **CONFIRMED.** Reconstruction is the first identity above on `dom D_p^2`. |
| 3 | **CONFIRMED.** Idempotence, orthogonality, order invariance, and parity-name invariance follow from the complementary projector pair. The two parity labels remain the index even if one image vanishes. |
| 4 | **RESULT CONFIRMED; RATIONALE CORRECTED.** If `O` is odd, then `lambda O` and `(1-lambda)O` are indeed odd, but `P_odd(lambda O)=lambda O`, not `O`. The split is rejected because `Can_p` is defined by applying the closed pair to the unsplit input `D_p^2`; it has no input slot for a producer-supplied refinement. Parity alone would not forbid arbitrary internal refinements of an odd block. |
| 6 | **CONFIRMED under the stated branch antecedent.** Record-even `h` and record-odd `W` imply `h^2+W^2` even and `hW+Wh` odd. No branch premise is derived here. |

Thus the two-block census is canonical under the stated closed-map law. This
does not canonically refine its even image into `T1` and `T2`.

---

## 3. Task A(2): the involution family and family invariance

### 3.1 Exact classification

The sealed matrix

```text
c = [[0,0,-i],
     [0,0,+i],
     [+i,-i,0]]
```

is Hermitian with simple spectrum `{-sqrt(2),0,+sqrt(2)}`. Let
`(e_-,e_0,e_+)` be corresponding orthonormal eigenvectors. If a Hermitian
involution `G'` anticommutes with `c`, it must map the `+sqrt(2)` line
unitarily to the `-sqrt(2)` line, map the latter back, and preserve the
one-dimensional kernel. Consequently, and exhaustively,

```text
G'(theta,sigma)e_+ = exp(+i theta)e_-,
G'(theta,sigma)e_- = exp(-i theta)e_+,
G'(theta,sigma)e_0 = sigma e_0,
theta in R/(2 pi Z), sigma in {+1,-1}.
```

This is `circle x Z2`. The classification follows from the three simple
eigenspaces; it is not inferred from sampling.

Independent reconstruction at the same ten sample pairs used in 871 gave:

```text
max ||G'^2-I||                         = 2.16e-15
max ||G'c+cG'||                        = 5.45e-16
max min(||G'-Gamma_cell||,
        ||G'+Gamma_cell||)             = 2.347745934051
max even-image residual               = 3.42e-14
max odd-image residual                = 3.42e-14
```

This reproduces the claimed distance `2.348` and the reported residual
scale. The tiny variation from `3.1e-14` is eigensolver/basis rounding.

### 3.2 Structural invariance, including unsampled members

For arbitrary local choices `G'_j(theta_j,sigma_j)`, set
`G'_p=tensor_j G'_j`. Since `h_p` carries the record identity,

```text
G'_p h_p G'_p = h_p.
```

Each summand of `W_p` carries exactly one `c_j` and identities on the other
record factors, so exactly one minus sign occurs:

```text
G'_p W_p G'_p = -W_p.
```

It follows for every parameter tuple, not merely the sampled diagonal
family, that

```text
P_even^{G'_p}(D_p^2) = h_p^2+W_p^2,
P_odd ^{G'_p}(D_p^2) = h_pW_p+W_ph_p.
```

The family-invariance argument is therefore sound and stronger than the
sample. Scope correction: `circle x Z2` is the exact **one-cell** family of
Hermitian `c`-compatible involutions. With independently variable record
edges the full family is its `N_p`-fold product. Also, the packet's cellular
grading `Gamma_cell=+1` on `C0` and `-1` on `C1` remains a fixed sealed
grading; the broader family is a robustness class defined by involution,
Hermiticity, and anticommutation, not a claim that the packet left its
cellular sign convention free.

---

## 4. Task A(3): covariance and the mixing control

For any `R` satisfying `[R,G]=0`, covariance of either projector follows
immediately. If `{R,G}=0`, then

```text
G R = -R G,       R* G = -G R*,
G(RMR*)G = (-RG) M (-GR*) = R(GMG)R*.
```

The two-sign cancellation is correct; unitarity is not needed for that
identity itself. Hence every grading-homogeneous `R` preserves the split.

Two qualifications are required.

1. At the `N_p=2` anchor, `G` has `60` positive and `48` negative
   dimensions (`tr G=12`). An invertible, hence a unitary, operator cannot
   anticommute with this `G`, because anticommutation would biject its
   positive and negative eigenspaces. The anticommuting case is a correct
   algebraic branch but has no unitary anchor representative. 871 does not
   display a matrix for its named “record-odd” anchor action.
2. A unitary rotating one positive basis vector into one negative basis
   vector is neither commuting nor anticommuting. An independent such
   rotation gave
   `||P_even(RD^2R*)-R P_even(D^2) R*||=1.162135908157`, strictly nonzero.
   Thus the mixing counterexample exists. The particular `1.63e+01` in 871
   is not reproducible from 871 alone because its mixing matrix, angle, or
   seed is not supplied; the verdict, not that scalar, is independently
   confirmed.

The negative control does not type the admitted action. Section 6 below
shows that the currently sealed action contains no mixing generator.

---

## 5. Task A(4): anchor reduction

I reconstructed the finite parent from the sealed matrices rather than
reusing 862's verdict: cyclic three-site first difference, the Dirac
matrices, `source_incidence_spin=-i gamma0 gamma5`, two source masks, and two
record embeddings of `c`.

For `N_p=2`,

```text
G  = I_12 tensor (Gamma_cell tensor Gamma_cell),
h  = h_free,
W  = write_0+write_1,
D  = h+W,
T1 = h^2,
T2 = W^2,
T3 = hW+Wh.
```

The independent structural replay returned exact floating-array zeros for:

```text
||D^2-(T1+T2+T3)||,
||G^2-I||,
||P_even(D^2)-(T1+T2)||,
||P_odd(D^2)-T3||,
projector idempotence residuals,
and <P_even(D^2),P_odd(D^2)>.
```

Thus `G=I_12 tensor (Gamma tensor Gamma)` and the Q-767 census
`{T1+T2,T3}` are independently confirmed.

---

## 6. Task B: type the two conditions

### 6.1 Finite incidence — `GENUINE-CONDITION`

The closure-first search over `C0` found:

- `CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md` makes each primitive
  incidence's **support** finite; it does not bound the number of incidences;
- `BID_GLOBAL_BOUNDARY_DESCENT_QUASI_FREE_COMPLETENESS_V001.md` and the
  R3.4 parent explicitly construct **finite** `K`; those statements discharge
  the finite sub-class but do not constrain every member of 870's generic
  predicate-defined `Adm_rec` to finite `K`;
- `PARENT_STATE_COVARIANCE_PRINCIPLE_V001.md` and the parent specification
  supply compatible finite states, an algebraic inductive/projective limit,
  and a quasi-local outgoing record algebra;
- no closure member supplies an infinite or incomplete **Hilbert tensor**,
  reference sequence/vector/state, or an implementation of the product
  grading on a generic unbounded `D_p^2` domain;
- no closure member bounds the primitive-incidence count of every generic
  `Adm_rec` member.

The algebraic inductive limit is meaningful stock: the finite grading
automorphisms are compatible with `A -> A tensor I`. But the closure does
not state or prove that a generic U1-produced, possibly unbounded `D_p^2` is
affiliated with that quasi-local graded algebra or that the limit
automorphism is spatially implemented on its domain. Treating the
quasi-local limit as the missing `Gamma_p` would therefore add a domain and
implementation theorem.

**Classification: `GENUINE-CONDITION`.** Finite-incidence members are
discharged; generic infinite-incidence promotion is not.

### 6.2 Homogeneous admitted action — `EXCLUDABLE`

The sealed admitted finite action is generated by:

1. vertex relabeling, represented on the source factor and identity on the
   record factor, hence commuting with `G`;
2. cellular orientation reversal. The later sealed typed carrier proves
   `U_c=Gamma_cell` and, for any reversed subset `O`,
   `U_O=tensor_c(Gamma_cell if c in O else I)`. Therefore
   `[U_O,G]=0`;
3. record/cell relabeling permutations where applicable. Because every
   factor carries the same `Gamma_cell`, tensor-factor permutations commute
   with their total tensor product.

Products and adjoints of these generators still commute with `G`. No mixing
element occurs in the sealed action. The 871 generic mixing rotation is not
generated by, or typed as a member of, that action.

**Classification: `EXCLUDABLE`.** This is not a newly imposed restriction;
the mixing case is outside the action at the sealed action/carrier bytes.
Any future enlarged action would require a new covariance check and could
reopen the condition, but it is not part of the present admitted action.

---

## 7. Consumer restatement

There is no longer a homogeneous-action condition for the currently sealed
action. The theorem's consumer effect is therefore:

| consumer from 871 | restated consequence |
|---|---|
| G1 universal / STEP 2 | the theorem-shaped projector/census conjunct is met **for the finite-incidence sub-class**; generic-U1 promotion remains conditional on `COND_FINITE` |
| Census B at the carrier gate | the U2/canonicity conjunct is met for finite incidence; U3 same-parent exhaustiveness remains wholly unsupplied |
| D-1 build-wide repair | the law branch is available on finite-incidence members; a build-wide generic installation still requires the finite condition to be discharged or the scope to be declared finite-parent only |
| 866 item 6, C2+C3 | the finite-parent disjunct is now supported; universal generic-U1 promotion is not |
| typed `D_N(t)` / G6 | only its theorem-shaped per-member conjunct is supplied on finite incidence; the actual typed descendant family/domain object remains unsupplied |
| H1 SC4 | only its uniformity conjunct is supplied on finite incidence; declared `A_c`, membership/path, and primitive-scope completeness remain unsupplied |

Accordingly, **no mixed site closes outright**, and no universal consumer is
promoted beyond finite incidence. Within the already sealed finite-parent
scope, covariance is outright rather than conditional.

---

## 8. FREEDOMS-CONSUMED

```text
CARRIED UNCHANGED:
  the seven adopted-branch hypotheses as antecedents;
  the restricted/non-exhaustive scope of Adm_rec;
  the outside-branch even-detuning, Pauli, and enlarged-geometric competitors;
  U3 same-parent exhaustiveness as unsupplied;
  the D_N(t)/G6 and H1-SC4 instance conjuncts as unsupplied;
  the packet-fixed cellular grading and all gates.

DERIVED HERE:
  complementary-projector algebra;
  the corrected scalar-split rationale;
  the full one-cell c-compatible involution classification;
  invariance over independently chosen local family members;
  covariance sign cancellation;
  existence of a mixing negative control;
  nonexistence of an anticommuting unitary at the unequal-signature anchor;
  anchor reconstruction;
  closure-first typing of the two conditions;
  admitted-action homogeneity from its sealed generators.

SELECTED HERE: NOTHING.
  No member, incidence bound, infinite tensor, reference state, grading member,
  action enlargement, carrier, branch, or preferred family parameter was chosen.

NOT DONE AND DISCLOSED:
  no infinite-incidence grading/domain theorem;
  no universal generic-U1 promotion;
  no exact reproduction of 871's unnamed 1.63e+01 mixing matrix;
  no instance conjunct and no U3 exhaustiveness theorem.

SCALING WEIGHTS: none consumed.  SUBSTITUTED: none.
```

---

## 9. Flattening check and battery

**FLATTENING CHECK — CLEAN.** The following identifications were rejected:

1. finite support of each incidence `=>` finitely many incidences;
2. finite parent sequence `=>` class-wide incidence bound;
3. quasi-local inductive limit `=>` spatial infinite tensor grading on an
   unbounded generic U1 domain;
4. same parity of split pieces `=>` a projector maps each piece to the whole;
5. diagonal `circle x Z2` sample `=>` full multi-edge family;
6. c-compatible involution family `=>` freedom in the packet-fixed cellular
   sign convention;
7. algebraically possible mixing unitary `=>` admitted action member;
8. algebraically valid anticommuting covariance branch `=>` existence of an
   anticommuting unitary at unequal grading signature;
9. finite theorem conjunct `=>` actual `D_N(t)` family, SC4 carrier, or U3
   exhaustiveness.

`F_PLDEC = PASS`: all numbers are finite operator residuals or dimensions.
`ANTI_TUNING = PASS`: no target value, coefficient fit, or post-evaluation
choice entered. `M-2 = PASS`: all four search modes were used over the
declared closures. Charter and standing gates remained held.

CLAUSES = CORRECTED (clauses 1/2/3/6 confirmed; clause 4 result confirmed but its scalar-split rationale corrected: rejection comes from the closed two-image census, not from parity alone)
FAMILY_INVARIANCE = CONFIRMED (structural argument sound; local circle x Z2 classification confirmed, full multi-edge multiplicity is its product, and the packet-fixed grading distinction is retained)
COVARIANCE = CORRECTED (sign cancellation and a mixing counterexample confirmed; the reported 1.63e+01 lacks a supplied R; anticommuting unitary branch is empty at the anchor; sealed admitted action excludes mixing)
ANCHOR = CONFIRMED (N_p=2 gives G=I_12 tensor (Gamma tensor Gamma) and census {T1+T2,T3} at exact structural residual zero)
COND_FINITE = GENUINE (C0 closure displayed; finite sub-class supplied, no generic incidence bound or infinite/incomplete tensor-domain implementation)
COND_HOMOG = EXCLUDABLE (sealed generators are source relabelings, tensor-factor relabelings, and U_c=Gamma_cell orientation reversals; all commute with total G)
CONSUMERS = restated (finite-incidence theorem conjuncts supplied; admitted-action covariance outright; generic promotion, U3, D_N(t)/G6 instance, and H1-SC4 instance conjuncts remain open)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
