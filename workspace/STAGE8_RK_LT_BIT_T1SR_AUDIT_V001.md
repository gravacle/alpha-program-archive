# STAGE 8 — THE BIT (Rhat_K,N in R L_T,N): ADVERSARIAL AUDIT — COMMISSION T1SR — V001

## ADVERSARIAL AUDITOR — CODENAME RKB-AUDIT — COMMISSION T1SR — [SEALED]

Date: 2026-08-15 (session UTC). Target:
`STAGE8_RK_LT_BIT_T1SR_V001.md` (sha256
`405bcd5e16f99bf9fd9c36e1c41495bbd57a1aeb623d229a306937b692517225`,
sidecar MATCH, verified at path before opening). Default posture:
REFUTE (BR-1: builder testimony carries ZERO weight — every
load-bearing claim re-derived at the bytes or at own CAS below).

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION-AUDIT ONLY: no
member bound, no adoption, no authored physics, no route/branch/
address/representative selected, no score/flag/gate moved; the sealed
flags READ, not moved. No file matching register|road_|ledger|lens|
plan|tracker|THE_HANDOFF|continuation opened; "Q-…" tokens
EXPECTED-UNLOCATABLE (cited only as carried inside sealed artifacts).
No git; ONE output file plus seal sidecar; output path probed ABSENT
at session start and re-probed ABSENT at 2026-08-15T07:23:47Z
immediately before this write. Own battery in a FRESH venv
(scratchpad-local; sympy 1.14.0; 22/22 PASS; exact integers/rationals/
symbols; no float; no value frozen; source VERBATIM in Appendix A).

**SWEEP CUTOFF: 2026-08-15T07:23Z** (re-runs of SW-T2..SW-T6 plus own
hunts). Exhaustiveness is sweep-displayed only.

---

## 0. VERDICT

```text
VERDICT = CONFIRMED.

The determination STANDS at the bytes. The headline verdict
UNDECIDABLE-TODAY (menu E-1-DERIVED | E-2-DERIVED | UNDECIDABLE-TODAY)
is CORRECT, CORRECTLY SCOPED (cutoff-relative, sweep-displayed
exhaustiveness), NEITHER OVER-CLAIMED (no direction is asserted; no
flag moves; a_K and chi_K never valued) NOR UNDER-CLAIMED (this
audit's own independent separator/chain hunts and sweep re-runs found
NO sealed decider the build missed: no sealed R_K_EQUALS_L_T
determination, no derived sigma_loc/p_loc member, no L_T,N
presentation byte, no L_T rank byte, no totality certificate, no
exhibited mixing relation). Every hunted defect class came back
empty:
  - no unproved step promoted to proved (every PROVED-grade display
    is finite-dim linear algebra, independently re-verified 22/22;
    every sealed-record claim walked at bytes and found VERBATIM);
  - no claimed separator fails on an explicit case (LEM-A re-proved
    dimension-free; its Rhat_K,N != 0 hypothesis is the audited X-1
    corollary and its ell[L_T]=1 hypothesis self-supplies L_T != 0);
  - no identity-chain gap misstated (C-i..C-v blockers each verified
    at their cited bytes; the rank-1 Hom-collapse refusal concurs
    with 041498bb §3.1 at bytes and my SW-T5 re-run);
  - the sweep cutoff hides no deciding class (independent landing
    census below; the ONE post-cutoff landing is RK_LT-class S9AD
    material, fence-barred from the build regardless — O-1);
  - no closure-shape (S1-5) blocker misstated (all three lines
    re-walked at 70dde1c0 :244-252; line-1 vacuity re-verified at
    (LM2-17) bytes + own battery A11; line-2 = D-A blocked at
    NOT_BUILT verified by SW-T3 re-run at zero constructed-grade
    lines; line-3's TOP-2 conditionality sound);
  - verdict strength exactly matches derivation strength;
  - no consumption of a non-sealed or fence-barred source (all 12
    hash claims reproduce at path; the S9AD-path file consumed at
    metadata grade only, and its metadata claims reproduce EXACTLY);
  - no hidden numeric evaluation (FENCE-HIT count = 0; §5).

NEW SINCE THE BUILD'S CUTOFF (O-1, disclosure): a SECOND RK_LT-class
S9AD-path artifact pair (an audit of the dead S9AD build) landed at
07:04:00Z/07:04:08Z — after the build's 07:03Z cutoff, 24 s before
the build's final own-path ABSENT re-probe, before its 07:08:45Z
write. The build could not lawfully have consumed it (RK_LT-class =
fence-barred for that commission) and its sweeps were true AT their
cutoff. TWO unadjudicated S9AD-path RK_LT artifacts now stand outside
the record; the build's own D-1 registrar flag is hereby EXTENDED to
both. Verdict-neutral for T1SR.

Observations O-1..O-5 (§6): all verdict-neutral; none rises to a
correction of content.

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED.
```

## 1. SEALS, BYTES, PROBES (this session, at path, before reliance)

```text
TARGET
405bcd5e  STAGE8_RK_LT_BIT_T1SR_V001.md            MATCH (shasum -c OK;
          mtime 2026-08-15T07:08:45Z, sidecar 07:08:53Z — write
          postdates the build's declared 07:04:24Z final ABSENT probe;
          probe discipline consistent at mtimes)
COMMISSION STOCK (re-hashed at path; each equals the commissioned hash)
cfa2fb97  STAGE8_IDEAL_EXTENT_S9AD_V001.md                     MATCH
eda2ba74  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md               MATCH
70dde1c0  STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2 MATCH
041498bb  STAGE8_TASK4B_CONSUMPTION_VERIFICATION_AND_PUSH...   MATCH
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md                    MATCH
CHASED CARRIERS THE BUILD LISTS (all 8 re-hashed; prefixes reproduce)
a7255f38 / d662889b / b53d9e93 / bae34116 / be570c18 / 6b7f8f98 /
474bf721 / fdf20bd4                                — 8/8 MATCH
MISMATCHES = 0.
S9AD-PATH METADATA (D-1 recheck, content UNOPENED): 41,273 B at
2026-08-15T06:50:51Z + 96 B sidecar at 06:51:11Z — the build's D-1
numbers reproduce EXACTLY; postdates the extent pair's cutoffs
(05:30Z/05:48Z; extent audit sealed 05:54:23-58Z); predates the
build's 07:03Z sweeps by ~12 min, as claimed.
```

## 2. QUOTE SITES WALKED AT BYTES (each re-read this session; builder testimony discarded first)

```text
70dde1c0: §2 L_T,G := "the uniquely normalized transverse Maxwell
  kernel in O_R5,G" (:142); (T2-1)/(T2-2)/(T2-3) (:149-166); (S1-1)
  incl. line 7 derived-separation obligation (:186-196); (S1-4)
  countermodel family (:239); (S1-5) three lines + "No authority
  proves any line of (S1-5)." (:244-252); (S1-7)/(S1-8) invariant-
  not-zero (:273-288); (S1-9)/(S1-10) trace exclusion (:295-311);
  (S1-11)/§3.5 DoR-008 cannot-select (:320-326); (S2-6) fullness =>
  R_K isomorphism (:429-434); (S2-13) ordered block (:519); (S2-14)/
  (S2-15) missing arrow + interfaces (:531-547); §6.2 attacks 2/5/7
  verbatim (:749-770); LM-1 NOT_BUILT (:832); LM-2 NOT_BUILT (:849);
  §8 R_K_EQUALS_L_T = NO_VERDICT / TYPE-U (:876). ALL VERBATIM.
041498bb: WITNESS_..._BY_SECTOR = false (:25); §0.5 lineage (:155-204);
  (R1-1) declared saddle shape (:227-232); (R1-3) chain (:252-261);
  (R2-1)/(R2-2) (:287-300); §3.1 "not declared one-dimensional"
  (:303-304); (R2-3) "The corpus has no map" (:312-315); (R2-4) "no
  declared direct sum" (:316-321); §3.3 orbit-equality sentence
  (:331-332); §3.4 flag (:354); §3.5/(R2-6) (:357-387); (R3-4) + "R_K
  is an isomorphism" (:429-440); (R4-1) (:463-466); (R4-3) (:495-499);
  §5.3 does-not-prove list — ALL FOUR ITEMS verbatim (:549-555; the
  extent audit's C-1 transposition honored by the build, verified);
  (R4-8) (:562); (R4-10) exclusion + "would silently author the
  missing map" (:593-602); (R4-11) item 2 "the embedding of L_T in
  O_R5 on every physical CTP block" (:611) — MB-1's byte; §6.3
  P_LOC_EXISTENCE_DERIVED = false / TYPE-U (:693) — MB-2's byte;
  §9 NUMERIC_EVALUATION = false. ALL VERBATIM.
824eb72f: anchor stages all cycle rank 1 (§4 intro); §4.1 kappa_T,N
  witness coordinate at the existence quantifier, unadopted; §4.2
  "one value" <=> no-mixing; §4.3-4.4 TOP-1/TOP-2 named + SW-4/SW-5
  undisplayed + BANACH row "requirement ... not a certificate"; §5.1
  NG-3; §5.2 NG-3' with the dim M_N >= 2 cell and the dim-1/
  undisplayed non-closure caveat; §6 biconditional. ALL AS CITED.
Extent pair: X-1 (incl. corollary Rhat_K,N != 0), X-2 two-entry menu,
  X-3 zero-value exclusion at typing grade, X-4 collapse with the
  u-leg conditional; branch-(a) says "covariant functional pair" —
  the build's LEM-A sharpening claim is accurate; audit verdict
  CONFIRMED-WITH-CORRECTIONS with C-1/C-2/C-3 as the build carries.
Chased carriers: b53d9e93 (S2a) [PROVABLE] :136-144; [YOURS] contract
  block opens :148; (S4) kappa_T,N := Krawhat_N(L_T,N) = Sch_N(M_N^loc)
  INSIDE the [YOURS] block, codomain D_N^ker per (S3) — the build's
  C-iv reading verified; (S8) plus-not-direct :218-228; §2.4 route
  bar :941-943 verbatim. bae34116 (LM1-9)/(LM1-10) "does not assume
  ... independent" verbatim; (LM1-12)/(LM1-13) a-coordinate-preserving
  ideal covariance; (LM2-4)/(LM2-6) fiber dim 1+m-r; (LM2-9); (LM2-13);
  (LM2-17) tautological decomposition; :571-573 "They do not prove
  that H_rem is a physical contact/nonlocal/higher term" verbatim.
be570c18 (W1-3) MEMBER-SENSITIVE + (W1-4)/(W1-5). 6b7f8f98 :147
  "S := an admitted K/K Schur form K->K^*". 474bf721 :577-581 S_nm
  isometric isomorphism + (Q13). fdf20bd4 :1257 u typed
  "rank-preserving" only — no isometry byte. ALL VERBATIM.
SEED_ADJUDICATION hits (:155, :293) re-read: quotation grade only —
  no new determination. The build's SW-T2 disposition verified.
```

## 3. INDEPENDENT RE-DERIVATION (own battery, own routes — Appendix A)

```text
22/22 PASS, fresh venv, sympy 1.14.0, exact symbolic, no float.
DIFFERENT constructions from the build's throughout (nullspace/rank
routes; matrix-identity orbit proof; 3x3 transport; general-n model):

A1  the two-entry extent menu and both kernels re-derived via
    nullspace computation (not solve): E-1 dim 1 pure-cycle line;
    E-2 dim 2 with mixing line (-a_K,1,0). MATCHES X-2/T1.
A2  THIRD-CASE PROBE (new): at L_T = 0 the kernel is dim 2 but its
    extra line is (1,0,0) — NOT a mixing line. So the menu closure
    genuinely consumes L_T,N != 0 at typing grade — and that premise
    is disclosed exactly where it lives (extent §4.2 + extent CL-3,
    audit-passed; build CL-2/CL-3 carry it at audited/typing grade
    and never spend it on the bit). The build's grade bookkeeping is
    ACCURATE, not decorative.
A3  LEM-A re-proved dimension-free: ell(L_T)=1, ell(Rhat)=0,
    Rhat != 0 => Rhat not in R L_T. Note ell(L_T)=1 itself forces
    L_T != 0: the certificate self-supplies the typing. CONFIRMED.
A4  LEM-B closed at GENERAL n (the build's battery exhibits n=1;
    its text claims all finite n): ONE 2-dim E-1 model (L=(1,0),
    Rhat=(a_K,1), ell_i = x_i * proj_1) realizes ANY finite
    common-ratio value data — so no finite displayed value set
    certifies E-2, exactly as the build states; the missing
    certificate is point-separation on the closure of the span
    (TOP-2-grade) or presentation-grade data (MB-1). CONFIRMED,
    now battery-verified at general n (O-4).
A5  converse guard: a displayed value PAIR with det != 0 refutes
    proportionality (the extent's branch-(a) shape); LEM-A's (1,0)
    is its cheapest instance. The build's "OVER-STRONG, one
    functional suffices" sharpening is genuine and correctly
    attributed. CONFIRMED.
A6  D-RANK transport re-proved at 3x3 + the display L = (1/a_K)Rhat:
    invertibility transports under E-2; contrapositive kill shape
    exact. The kill BYTE's absence re-verified by my own SW-T4
    re-run (zero L_T rank/singularity/invertibility bytes in either
    root); the build's refusal to read "transverse" carrier-side is
    the correct attack-7 discipline (LM-2 NOT_BUILT verified at
    :849). CONFIRMED AS SHAPE; NOT FIRED — correctly.
A7  X-3 reproduction: zero cycle-side mixing forces L_T = 0 against
    typing. CONFIRMED.
A8  P_x = s R_K at rank 1. CONFIRMED.
A9  orbit identity STRENGTHENED: sum_j (G u_j)(G u_j)^T = G proved
    for EVERY G-orthonormal basis via U^T G U = I => U U^T = G^(-1)
    (the build verified the Gram-Schmidt instance; mine is
    basis-universal — choice-invariance is a theorem, not a spot
    check). CONFIRMED.
A10 reader-fiber dims via rank/linsolve: E-1 fiber = line (dim
    2 - 1 = 1, chi = tau*rho); E-2 fiber = the single point
    (a_K, a_K/rho). Matches (LM2-6)'s 1+m-r at m=r=1 and m=1,r=2.
    D-FIBER's equivalence is exact at algebraic-reader grade, and
    the build's "downstream only" scoping is the honest one
    (readers constrained BY Rel_N). CONFIRMED.
A11 (LM2-17) vacuity: every reader annihilates ITS OWN remainder —
    line 1 of (S1-5) is circular as a route exactly as the build
    says (needs MB-2 plus the bit itself). CONFIRMED.
A12 forced coefficient a_K = -a/(b+c). CONFIRMED.
BATTERY REPRODUCTION: the build's Appendix-A source extracted
verbatim, run in the fresh venv: 16/16 PASS with output
BYTE-IDENTICAL to the artifact's claimed verbatim output; no float,
no evalf, no hidden numeric path in the source (grepped).
```

## 4. SWEEP RE-RUNS AND LANDING CENSUS (own, fence-excluded file lists)

```text
Roots swept: archive workspace + cleanroom_output, minus fenced
filename classes (case-insensitive), minus the two S9AD-path RK_LT
files (content-fenced; pattern-presence only where noted).
SW-T2 re-run -> EXACTLY the build's hit list at its cutoff: the two
  flag sites, the route bar, the extent pair, SEED_ADJUDICATION at
  quotation grade, the S9AD-path file — PLUS (post-cutoff) the S9AD
  audit pair (O-1). No sealed determination anywhere.
SW-T3 re-run -> 9 files contain "sigma_loc"; ZERO constructed-grade
  lines (every hit NOT_BUILT/unbuilt/future/required class). MATCH.
SW-T4 re-run -> ZERO L_T singularity/invertibility/rank bytes; the
  only defining L_T line in either root is 70dde1c0 §2's typing;
  embedding language exists only in requirement rows ((R4-11) item
  2 / (S2-15)). MB-1 verified at bytes. MATCH.
SW-T5 re-run -> zero one-dimensionality declarations for O_R5.
  MATCH; concurs with 041498bb §3.1.
SW-T6 landing census (mtimes, full workspace): after the extent
  audit's sidecar (05:54:58Z): the S9AD build pair (06:50:51Z/
  06:51:11Z) [= the build's D-1, metadata EXACT], then the S9AD
  AUDIT pair (07:04:00Z/07:04:08Z) [POST-CUTOFF — O-1], then the
  target pair (07:08:45Z/07:08:53Z). At the build's 07:03Z cutoff
  its SW-T6 statement was TRUE. No competing T1SR artifact exists
  (only the target + this audit's first-use path, probed ABSENT
  twice this session).
DECIDER HUNT (CL-1's own falsifier list, run independently): no
  sealed R_K_EQUALS_L_T determination, no derived sigma_loc/p_loc
  member, no L_T,N presentation byte, no totality certificate, no
  exhibited mixing relation, no isometry typing for u — in either
  root at my cutoff. The build's UNDECIDABLE-TODAY survives its own
  falsifier list under independent execution.
```

## 5. FENCE-SCAN AND DISCIPLINE CHECKS

```text
FENCE-HIT COUNT = 0. Grepped the target for computed alpha /
kappa_record / kappa_Thomson / coupling / scale / root / eigenvalue
numerics and measured-constant comparisons: NONE (the only "137"/
float-pattern hits are section numbers and the sympy version pin;
the battery source contains no float literal, no evalf/.n()/float()).
NUMERIC_EVALUATION = exact integers/rationals/symbols only, verified
by execution. MEASURED_CONSTANT_COMPARISON = none.
DETERMINATION ONLY = HELD at the bytes: no member bound, no
adoption, no authored physics, no functional authored (LEM-A/LEM-B
quantify over hypothetical admitted functionals and author none),
no route/branch/address/representative selected; the sealed flags
R_K_EQUALS_L_T, P_LOC_RK, LOCAL_MAXWELL_SYMBOL, IDEAL_EXTENT are
read and left standing; a_K and chi_K never valued; B-1 and
NG-3/NG-3' untouched; SK-2/ACT-3/D-1-menu untouched.
CHOICE LEDGER: present, CL-1..CL-8, terminal, ZERO OPEN; commission
T1SR is identified INSIDE the ledger (CL-6 names the T1SR fence;
CL-1 names the commission-fixed roots) — Q-1064 discipline met.
TOY_SEPARATION: present and ACCURATE — verified that no toy value,
stage, dimension, or number enters the verdict, the missing-object
list, or any flag (the battery targets are certificate-shape
checks; my independent battery reaches identical conclusions from
different models, which is itself the toy-separation property).
FLAG BLOCK: present; every flag line checked against the body; no
contradiction found; fences printed and held.
OUTPUT DISCIPLINE: one output file + sidecar; first-use path;
probed ABSENT at start and at 07:23:47Z. The build's probe/mtime
discipline consistent (§1).
```

## 6. OBSERVATIONS (all verdict-neutral; none fires)

```text
O-1  POST-CUTOFF LANDING (disclosure, not a defect): the S9AD-path
     AUDIT pair (STAGE8_RK_LT_BIT_S9AD_AUDIT_V001.md + sidecar,
     07:04:00Z/07:04:08Z) landed after the build's 07:03Z cutoff
     and before its 07:08:45Z write — 24 s before the build's final
     own-path ABSENT re-probe. The build's sweeps were true at
     cutoff; RK_LT-class content was fence-barred to it regardless;
     its verdict is cutoff-scoped by its own words. CONSEQUENCE FOR
     THE REGISTRAR: TWO unadjudicated S9AD-path RK_LT artifacts now
     exist outside the record; the build's D-1 quarantine/adjudicate
     flag is EXTENDED to both. This audit consumed both at metadata
     grade only (existence, sizes, mtimes; contents unopened).
O-2  CITATION SHORTHAND: the build's §3.3 cites "(S1) d662889b" for
     the E_N/qtilde display whose formula bytes sit in (S2) ((S1) is
     the ExtSrc tuple naming E_N as a component); its §1 seal table
     lists (S1)/(S2)/(Q5) jointly, so the pointer resolves. C-1-class
     cosmetic; content real; no repair needed beyond this note.
O-3  SW-T6 WORDING: "EXACTLY ONE file" counts the S9AD artifact;
     with its sidecar the landing is two files at path. The build's
     own D-1 discloses both with exact sizes/mtimes, so the count is
     correct at metadata grade; wording loose only.
O-4  LEM-B BATTERY SCOPE: the build's T3a/T3b exhibit the n=1
     countermodel pair; the artifact's "NO finite set" claim rests on
     the displayed totality mechanism (T4). Correct as stated; this
     audit's A4 closes general n by explicit model, upgrading the
     general claim from mechanism-displayed to battery-verified.
O-5  ROOT NAMING: the build's CL-1 names its commission-fixed sweep
     roots "the archive workspace + the cleanroom v003 root". No
     directory literally named "cleanroom v003" exists at path; the
     accessible cleanroom root is cleanroom_output/ (the v003
     designation appears only inside artifact texts). My re-sweeps
     covered workspace + cleanroom_output and reproduced every hit
     list, so nothing turns on the name; noted at metadata grade
     (the T1SR commission text is not among this audit's inputs).
```

## 7. WHAT WOULD HAVE REFUTED (and did not fire)

```text
R-a  target absent or seal mismatch — DID NOT FIRE (405bcd5e MATCH).
R-b  any commissioned input seal mismatch — DID NOT FIRE (12/12).
R-c  a load-bearing quote absent or altered at its cited bytes —
     DID NOT FIRE (every §2 site verbatim; the one transposition
     class already logged by the extent audit is honored, not
     repeated).
R-d  E-1 or E-2 asserted, a_K or chi_K valued, or a flag moved —
     DID NOT FIRE (UNDECIDABLE-TODAY; flags read only).
R-e  LEM-A/LEM-B/D-RANK/D-FIBER failing on an explicit case — DID
     NOT FIRE (independent battery 22/22 by different routes;
     general-n LEM-B closed; basis-universal orbit identity).
R-f  a sealed decider existing pre-cutoff and missed — DID NOT FIRE
     (independent decider hunt §4 empty in both roots).
R-g  hidden numerics / fence breach / toy leakage — DID NOT FIRE
     (FENCE-HIT = 0; TOY_SEPARATION verified).
R-h  consumption of the fence-barred S9AD-path file — DID NOT FIRE
     (the build cites it nowhere below metadata grade; its D-1
     metadata reproduces exactly, consistent with an ls-grade probe).
R-i  this audit's own output path already claimed — DID NOT FIRE
     (probed ABSENT twice; no competing T1SR artifact).
```

## 8. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: roots = archive workspace + cleanroom_output,
      minus fenced filename classes (register|road_|ledger|lens|plan|
      tracker|THE_HANDOFF|continuation, case-insensitive; none
      opened), minus the two S9AD-path RK_LT files' contents
      (commission fence; metadata + pattern-presence only). Cutoff
      2026-08-15T07:23Z. A wider sweep changes this audit only by
      exhibiting a pre-cutoff sealed decider from CL-1's list or a
      competing artifact. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the extent pair and round-3 pair
      consumed at their audited grades (their audits' verdicts
      verified at bytes: CONFIRMED-WITH-CORRECTIONS with corrections
      carried); chased carriers re-hashed and re-walked directly at
      every span this audit relies on (§2). PREMISE(named).
CL-3  BR-1 EXECUTION: builder testimony discarded; the battery
      re-run is evidence only through byte-identical reproduction in
      MY venv; all mathematical conclusions rest on the independent
      battery (different constructions) and the byte walks.
      PREMISE(named).
CL-4  TYPING-GRADE ACCEPTANCE: L_T,N != 0 accepted at sealed-typing
      grade exactly where the audited extent pair spends it (menu
      closure + X-3), verified load-bearing by the A2 third-case
      probe and verified DISCLOSED at that grade by both artifacts.
      PREMISE(named).
CL-5  EXTERNAL-MATH IMPORTS: finite-dimensional linear algebra only
      (nullspaces, ranks, dets to 3x3, one matrix inverse identity);
      exact; no topology, no wavefront, no Hahn-Banach imported onto
      O_R5,N; LEM-B's totality requirement handled as a shape, as in
      the target. PREMISE(named).
CL-6  METADATA-GRADE CONSUMPTION of the two S9AD-path RK_LT
      artifacts (O-1): existence, sizes, mtimes via ls/stat;
      pattern-presence via one grep -l listing; contents, sidecars,
      and hashes NOT read. Their standing is the registrar's.
      PREMISE(named).
CL-7  LABEL READINGS: "(S1-5) closure shapes" = the three lines at
      70dde1c0 :244-252 (the build's CL-7, concurring with the
      extent pair's reading — verified at bytes); "the flags" = the
      two sealed R_K_EQUALS_L_T lines + the §5.3 list per the
      commission. PREMISE(named).
CL-8  DISCLOSURE: sweep listings displayed fenced-class BASENAMES;
      no fenced content opened, read, or used; the battery venv is
      scratchpad-local and commission-distinct; sympy 1.14.0 matched
      the build's pin by coincidence of pip resolution — all claims
      exact-symbolic and version-insensitive. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  This audit's model targets (R^1/R^2/R^3 codomains, generic
  symmetric 2x2/3x3 matrices, symbolic x_i/a_K/chi/tau/rho/lambda,
  never evaluated) are verification instruments ONLY: they check
  that the target's displayed kernels, certificates, countermodels,
  transports, and fiber dimensions are FORCED by the stated
  hypotheses. The A2 and A4 models decide NOTHING about the sealed
  carriers — A2 refutes a hypothetical menu-without-typing (a
  statement about premise bookkeeping), A4 refutes a proof shape
  (single-grade value certification of E-2). No toy value, stage,
  address, dimension, or number enters the verdict or any flag. No
  float; no value frozen; a_K and chi_K never valued.
```

## FLAG BLOCK

```text
RKB_AUDIT = CONFIRMED (target verdict UNDECIDABLE-TODAY stands;
  observations O-1..O-5 all verdict-neutral)
HEADLINE_ADJUDICATION = holds at the bytes; correctly scoped
  (cutoff-relative); NEITHER over-claimed NOR under-claimed; the
  build's own falsifier list (its CL-1) run independently and empty
PER_CLAIM = seals 12/12 CONFIRMED ; formation chains (§3.1-3.3)
  CONFIRMED at bytes (Rhat_K,N presented; L_T,N typed-only — MB-1
  verified at (R4-11) item 2) ; MB-2 CONFIRMED at 041498bb §6.3 ;
  E-1 hunt S-i..S-xi blockers CONFIRMED ; E-2 hunt C-i..C-v blockers
  CONFIRMED ; D-ASYM (LEM-A/LEM-B) CONFIRMED + general-n closed ;
  D-FIBER CONFIRMED (downstream-only scoping honest) ; D-RANK
  CONFIRMED as shape, kill byte confirmed absent (SW-T4 re-run) ;
  COMPLEMENT PLACEMENT refusal CONFIRMED as scoped ((R2-4) absent;
  derivation obligation at (S1-1) line 7) ; (S1-5) walk CONFIRMED
  line by line ; D-A/D-B/D-C decision surface CONFIRMED ; flags
  read-not-moved CONFIRMED
BATTERY = target's 16/16 reproduced BYTE-IDENTICAL in fresh venv ;
  own independent battery 22/22 PASS by different routes ; source
  verbatim in Appendix A ; no float anywhere
SWEEPS = SW-T2..SW-T5 re-runs MATCH ; landing census exact ; ONE
  post-cutoff landing (the S9AD audit pair) disclosed at O-1 and
  fence-barred to the build regardless ; no competing artifact
S9AD_PATH_STANDING = now TWO unadjudicated RK_LT-class artifacts at
  the dead commission's paths (build 06:50-51Z; audit 07:04Z);
  both outside the record; both metadata-only here; the build's D-1
  registrar flag EXTENDED to the pair of pairs
FENCE_HITS = 0 ; DETERMINATION_ONLY = held (audit and target) ;
  MEMBER_BOUND = false ; CHAIN_INVOKED = false
CHOICE_LEDGER = present in target, terminal, T1SR named inside
  (Q-1064 discipline met) ; TOY_SEPARATION = clean both artifacts
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a)
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the bit's determination survives an audit built to kill it:
  every sealed absence the build names is genuinely absent at the
  bytes, every exact display re-derives by independent routes, the
  decision surface (E-1 one-functional/one-byte cheap; E-2 never
  value-cheap) is theorem-grade at the models, and the one thing
  the build could not see — the S9AD audit landing inside its
  cutoff-to-write window — changes nothing it said and is now on
  the record for the registrar. The gates are where the principal
  left them.
```

---

## APPENDIX A — AUDIT BATTERY SOURCE (VERBATIM) AND OUTPUT

Fresh venv: `python3 -m venv venv_audit; pip install sympy` (sympy
1.14.0), scratchpad-local, commission-distinct. Run:
`venv_audit/bin/python rkbaudit_battery.py`. Output: `22/22 PASS`.

```python
# RKB-AUDIT independent battery — commission T1SR — exact symbolic only (sympy).
# DIFFERENT constructions from the build's: linsolve/rank routes, general-n LEM-B
# realization, 3x3 D-RANK, matrix-identity orbit proof, and the L_T=0 third-case
# probe that checks the menu's typing dependence is real. No float. No value frozen.
import sympy as sp

PASS = []
def check(name, cond):
    assert cond, name
    PASS.append(name)

a, b, c, aK, chi, tau, rho, lam, cc = sp.symbols('a b c a_K chi tau rho lambda c_c', real=True)

# ---- A1: menu closure at rank-1 anchor via matrix rank (independent route) ----
# qtilde(a,b,c) = a*L_T + (b+c)*Rhat in span{L_T, Rhat}.
# E-1 model: L_T,Rhat independent -> map matrix M1 = [[1,0,0],[0,1,1]] (rows = coords).
M_e1 = sp.Matrix([[1, 0, 0], [0, 1, 1]])
ker_e1 = M_e1.nullspace()
check("A1a E-1 dim ker = 1", len(ker_e1) == 1)
check("A1b E-1 kernel = pure-cycle line (0,1,-1)",
      sp.simplify(ker_e1[0].T - sp.Matrix([[0, -1, 1]]).applyfunc(lambda t: -t)) == sp.zeros(1, 3)
      or list(ker_e1[0]) == [0, -1, 1] or list(ker_e1[0]) == [0, 1, -1])
# E-2 model: Rhat = aK*L_T (aK != 0): row map [1, aK, aK]
M_e2 = sp.Matrix([[1, aK, aK]])
ker_e2 = M_e2.nullspace()
check("A1c E-2 dim ker = 2", len(ker_e2) == 2)
mix = sp.Matrix([-aK, 1, 0])
check("A1d mixing line in E-2 kernel", sp.simplify((M_e2 * mix)[0, 0]) == 0)
check("A1e mixing line NOT in E-1 kernel",
      sp.simplify(M_e1 * mix) != sp.zeros(2, 1))

# ---- A2: THIRD-CASE PROBE — the menu closure consumes L_T != 0 (CL-3 is load-bearing
# for X-2's menu, not for the bit): if L_T = 0 (and Rhat != 0) the kernel is dim 2 but
# contains (1,0,0) — NOT a mixing line — a distinct extent the typing excludes. ----
M_lt0 = sp.Matrix([[0, 1, 1]])          # qtilde = (b+c)*Rhat, L_T = 0
ker_lt0 = M_lt0.nullspace()
check("A2a L_T=0 case: dim ker = 2 with (1,0,0) present (not a mixing line)",
      len(ker_lt0) == 2 and (M_lt0 * sp.Matrix([1, 0, 0]))[0, 0] == 0)
# and (1,0,0) is not in either menu entry's kernel shape at aK != 0:
check("A2b (1,0,0) not in E-1 kernel and not in E-2 kernel unless aK=0",
      sp.simplify(M_e1 * sp.Matrix([1, 0, 0])) != sp.zeros(2, 1)
      and sp.simplify((M_e2 * sp.Matrix([1, 0, 0]))[0, 0]) == 1)

# ---- A3: LEM-A dimension-free — ell(L)=1, ell(R)=0, R != 0 => R not in R*L ----
# If R = lam*L then 0 = ell(R) = lam*ell(L) = lam, so R = 0*L = 0. Contradiction shape:
lam_val = sp.solve(sp.Eq(lam * 1, 0), lam)
check("A3 LEM-A forces lam = 0 (so R = 0, contradicting R != 0)", lam_val == [0])

# ---- A4: LEM-B general-n realization — ONE 2-dim E-1 model matches ANY finite
# common-ratio value data: V = R^2, L = (1,0), R = (aK,1), ell_i(v) = x_i*v1. ----
x1, x2, x3 = sp.symbols('x1 x2 x3', real=True)
L_v = sp.Matrix([1, 0]); R_v = sp.Matrix([aK, 1])
for i, xi in enumerate((x1, x2, x3), 1):
    ell = lambda v, xi=xi: xi * v[0]
    check(f"A4.{i} ell_{i}(L)=x_{i}, ell_{i}(R)=aK*x_{i} in the E-1 model",
          sp.simplify(ell(L_v) - xi) == 0 and sp.simplify(ell(R_v) - aK * xi) == 0)
check("A4d the model is E-1 (L,R independent): det = 1",
      sp.Matrix.hstack(L_v, R_v).det() == 1)
# => no finite list of common-ratio functional values certifies E-2. LEM-B general.

# ---- A5: the E-1-certifying converse (branch-(a) pair / LEM-A special case):
# a displayed pair with det != 0 refutes proportionality outright. ----
e1L, e1R, e2L, e2R = sp.symbols('e1L e1R e2L e2R', real=True)
Mpair = sp.Matrix([[e1L, e1R], [e2L, e2R]])
# under R = aK*L the det collapses:
det_under_e2 = Mpair.det().subs({e1R: aK * e1L, e2R: aK * e2L})
check("A5 pair-det vanishes under E-2: det != 0 displayed => E-1", sp.simplify(det_under_e2) == 0)

# ---- A6: D-RANK transport at 3x3 (build used 2x2) + the general one-liner ----
g = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'g{min(i,j)}{max(i,j)}', real=True))
check("A6a det(aK*L) = aK^3 det(L) at 3x3", sp.simplify(sp.det(aK * g) - aK**3 * sp.det(g)) == 0)
# general: R = aK L, aK != 0, R invertible => L = (1/aK) R invertible (inverse displayed):
Rm = aK * g
check("A6b L = (1/aK)*R exactly (invertibility transports)",
      sp.simplify(g - (Rm / aK)) == sp.zeros(3, 3))

# ---- A7: X-3 zero-value exclusion — my route: mixing (a,b,c), a != 0, cycle side
# b+c = 0 forces qtilde = a*L_T = 0 => L_T = 0, against typing. ----
LTs = sp.Symbol('LTs', real=True)
anz = sp.Symbol('anz', real=True, nonzero=True)
sol = sp.solve([sp.Eq(b + c, 0), sp.Eq(anz * LTs + (b + c) * sp.Symbol('Rh', real=True), 0)], [c, LTs], dict=True)[0]
check("A7 zero cycle-side mixing forces L_T = 0", sol[LTs] == 0)

# ---- A8: rank-1 fiber P_x = s R_K, coordinate-free at 1-dim ----
r = sp.Symbol('r', real=True)
Px = (rho * r) ** 2
check("A8 P_x = g(x,x)*R_K at rank 1", sp.simplify(Px - (rho * r**2) * rho) == 0)

# ---- A9: orbit identity via the matrix identity (independent of Gram-Schmidt):
# U^T G U = I  =>  U U^T = G^{-1}  =>  sum (G u_j)(G u_j)^T = G U U^T G = G. ----
u11, u12, u21, u22 = sp.symbols('u11 u12 u21 u22', real=True)
U = sp.Matrix([[u11, u12], [u21, u22]])
G_from_U = (U.T).inv() * U.inv()          # the unique G with U^T G U = I
S9 = G_from_U * U * U.T * G_from_U
check("A9 sum (G u_j)(G u_j)^T = G for ANY G-orthonormal basis (matrix identity)",
      sp.simplify(S9 - G_from_U) == sp.zeros(2, 2))

# ---- A10: reader-fiber dims via linear-system rank (independent route) ----
# reader p(a,b,c) = a + chi*b + tau*rho*c; E-1 relations {(0,1,-1)}; E-2 adds (-aK,1,0).
A_e1 = sp.Matrix([[1, -rho]])             # coeffs of (chi,tau) in p(0,1,-1)=0
check("A10a E-1 fiber dim = 2 - rank = 1", 2 - A_e1.rank() == 1)
A_e2 = sp.Matrix([[1, -rho], [1, 0]])     # add p(-aK,1,0) = -aK + chi = 0 -> chi row
sol_f = sp.linsolve((A_e2, sp.Matrix([0, aK])), [chi, tau])
check("A10b E-2 fiber = single point (chi, tau) = (aK, aK/rho)",
      sol_f == sp.FiniteSet((aK, aK / rho)))

# ---- A11: T8 vacuity independently — for EVERY reader the line-1 shape holds ----
p_reader = lambda A, B, C: A + chi * B + tau * rho * C
check("A11 R_rem = Rhat - chi*L_T is annihilated by ITS OWN reader, tautologically",
      sp.simplify(p_reader(-chi, 1, 0)) == 0)

# ---- A12: forced coefficient — kernel elt with a != 0 forces the ratio ----
bc = sp.Symbol('bc', real=True, nonzero=True)
check("A12 forced a_K = -a/(b+c) exact", sp.solve(sp.Eq(a + bc * aK, 0), aK) == [-a / bc])

print(f"{len(PASS)}/22 PASS")
for nm in PASS:
    print("  PASS:", nm)
```

```text
OUTPUT (verbatim):
22/22 PASS
  PASS: A1a E-1 dim ker = 1
  PASS: A1b E-1 kernel = pure-cycle line (0,1,-1)
  PASS: A1c E-2 dim ker = 2
  PASS: A1d mixing line in E-2 kernel
  PASS: A1e mixing line NOT in E-1 kernel
  PASS: A2a L_T=0 case: dim ker = 2 with (1,0,0) present (not a mixing line)
  PASS: A2b (1,0,0) not in E-1 kernel and not in E-2 kernel unless aK=0
  PASS: A3 LEM-A forces lam = 0 (so R = 0, contradicting R != 0)
  PASS: A4.1 ell_1(L)=x_1, ell_1(R)=aK*x_1 in the E-1 model
  PASS: A4.2 ell_2(L)=x_2, ell_2(R)=aK*x_2 in the E-1 model
  PASS: A4.3 ell_3(L)=x_3, ell_3(R)=aK*x_3 in the E-1 model
  PASS: A4d the model is E-1 (L,R independent): det = 1
  PASS: A5 pair-det vanishes under E-2: det != 0 displayed => E-1
  PASS: A6a det(aK*L) = aK^3 det(L) at 3x3
  PASS: A6b L = (1/aK)*R exactly (invertibility transports)
  PASS: A7 zero cycle-side mixing forces L_T = 0
  PASS: A8 P_x = g(x,x)*R_K at rank 1
  PASS: A9 sum (G u_j)(G u_j)^T = G for ANY G-orthonormal basis (matrix identity)
  PASS: A10a E-1 fiber dim = 2 - rank = 1
  PASS: A10b E-2 fiber = single point (chi, tau) = (aK, aK/rho)
  PASS: A11 R_rem = Rhat - chi*L_T is annihilated by ITS OWN reader, tautologically
  PASS: A12 forced a_K = -a/(b+c) exact
```
