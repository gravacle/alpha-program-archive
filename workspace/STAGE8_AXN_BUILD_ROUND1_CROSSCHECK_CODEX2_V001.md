# STAGE 8 / [PLAN:AXN-BUILD-A8] — ROUND 1 CROSS-CHECK AND CHARGED-VERTEX READING
## CODEX 2 LANE — V001

All headline items in this report are **CLAIMED**.

## 0. Custody, jurisdiction, and gates

Relay 841 was admitted through the numbered-relay protocol. Inbox
`relay_inbox/RELAY_PASTE_841_ROUND1_CROSSCHECK_CODEX2_V001.md` hashes to
`3dd94d684c09a01830be0ae0db6971ca2c38d5cdd43f0e88223b6d07278afdac`; its adjacent seal was
verified before reading and `relay_outbox/841_ACK.md` was written first. The lane guard returned
`CODEX 2`.

`PROGRAM_STATE_BRIEF_V005.md` hashes to
`e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; its adjacent seal was
verified and it was read before task work. The settled-question register was searched for the
charged vertex, `BOX_record`, ordering, `K_R`, gravity, and the round-1 census. Q-739 through Q-744
were read at their exact current text; this artifact independently checks rather than merely repeats
Q-744.

Opposite-lane subjects, each verified against its adjacent seal before reading:

| subject | SHA-256 | bytes |
|---|---|---:|
| `STAGE8_AXN_BUILD_PACKAGE_V3_DARIO_V001.md` | `e7c4776cc43f4f3b5c004fd9823078cdef009dc1c27cbcbbfac0b172e306f88a` | 9,760 |
| `STAGE8_AXN_BUILD_CLOSURE_ROUND1_DARIO_V001.md` | `481b89cb3cf154086b5402a39258c238ec206b3e039185aca1b98e7a16460318` | 24,909 |

The sealed Stage-7 review packet was verified as a whole with
`STAGE7_PACKET_MANIFEST_V001.sha256`; every listed member returned `OK`. Packet citations below are
to those sealed packet copies, never an unsealed root copy.

The gates remain:

```text
alpha_computed          = false
proof_authorized        = false
kappa_record_computed   = false
```

No member is bound; no fixed point or end test is executed; no physical quantity is numerically
evaluated; no measured constant is compared; no smooth carrier is imported; and no electromagnetic
identification is made. PE-1 through PE-11 remain pointer-only and were neither opened nor
consulted. No register, plan, tracker, or git action was taken.

## 1. Package V3 census — a sixteenth receiver is present

### 1.1 Exact agreement with 838's returned list

Package V3 faithfully carries all fifteen rows expressly enumerated by relay 838:

```text
H1-H6  = six local holes                    6/6
I1-I5  = five package interfaces            5/5
P1-P4  = four present-unresolved items      4/4
D-2    = retired, not carried
```

That is an exact transcription check against
`STAGE8_AXN_BUILD_SKELETON_V2_CROSSCHECK_CODEX2_V001.md`
(`24001c12ce7f12a15a8f3cffe8d8838e1acf32a55e2fa31e67d77a67f914841b`). The V3 table itself is at
lines 40–91, bytes `[2163,7003)`, span SHA-256
`f080ce82ffe9866d7790baccf70ec2d94631b1fff334fc3bd71d40a795197d93`.

### 1.2 Law-9 hunt beyond the returned list

The “complete census” claim nevertheless fails. Skeleton V2 carries a build obligation that V3
mentions in its prose summary but omits from its fifteen-row census:

```text
I6 := SLOT9_F2_EXCLUSION_THEOREM

receiver:
  a theorem excluding independent finite c F^2 deformation after regulator
  removal, not a postulate relabeling.

failure guard:
  premise-level T04 absence, or Single-Operator Completeness alone, does not
  discharge the theorem.
```

The obligation is explicit in `STAGE8_AXN_BUILD_SKELETON_V2_DARIO_V001.md`
(`5964b9c5ab6e8b429338d0c76c3b1d2b337c1d2bf6dd9f3caf1442df7b8d0f7a`), lines 68–84, bytes
`[4433,5693)`, span SHA-256
`c75dfc3f5dc7dde85faabd5c9d171a831a10e1b2e8d3e6add6de8be4600e7e41`. The independent slot
census types slot 9 as a NODE-FACE and gives the same theorem demand
(`STAGE8_D2_SLOT_CENSUS_DARIO_V001.md`,
`7df0df0f4dfb91fe2054076a08873b77fb9c4a54828bc7f93327bd2fb12daa9a`, lines 93–104, bytes
`[4631,8442)`, span SHA-256
`9f129a7e2b5e45552b0846037aa42dabcb28e57a1c0b140e5929eda782a0c2b5`).

I6 is not silently subsumed by I3 `ACTION_P5_NO_OUTSIDE`: I3 is a family-presentation,
equivalence/exclusion, and no-outside certificate. I6 has a different receiving predicate—finite
`F^2` deformation **after regulator removal**—and an explicit anti-relabel failure mode. One future
proof may discharge both, but that possible common supplier does not identify their predicates.

U1 remains a separate procedure carrier already carried by the package prose. A named future
supplier does not make it discharged; its current status is `AWAITING BUILD / GATED ON H1`, not
“resolved.” This is an accounting correction, but I do not count U1 as a second new mathematical
receiver: it is the already-declared action-to-operator procedure through which several rows are
tested.

Therefore the closure census has **at least sixteen** rows:

```text
6 holes + 6 interfaces + 4 present-unresolved = 16.
```

No claim is made that sixteen proves global closure; I3 remains the missing no-outside certificate.

## 2. Ordering — full verdict blocked, partial evaluation lawful

The predicate source is
`STAGE8_AXN_BUILD_SKELETON_CODEX2_V001.md`
(`5a51b94039bc4a9e078e4f11cc8b74b05973d1b8e547e78fd1f4728d586af76c`), lines 272–300, bytes
`[15709,17099)`, span SHA-256
`e394a5b9247653da37f3caf57279da24e9ae0a6d6f9c542a94869b636227574f`. It is a conjunction on
`BIND_PACKET_PARENT(delta S_complete)`.

Round 1 is right about the **full verdict**: without an integrand and domain for `BOX_record`, the
complete variation and its finite restriction are not posable. Therefore no full predicate PASS
and no final predicate-selected stratum may be produced for `S_0` or `S_1`.

The claimed absolute inversion is too strong. A conjunction with an unavailable operand can still
be evaluated on every independently available operand, and a known false conjunct can reject a
candidate without waiting for unknown conjuncts. The sealed predicate contains no short-circuit law
forbidding clause-local comparison. Lawful partial outputs include:

- the required intrinsic packet envelope as a receiver, without claiming the action integrand;
- the first-order charged-vertex comparison in §6 below;
- the mandatory square-generated descendant subset, without claiming U3 exhaustiveness; and
- structural non-reach findings for public `K_R`, gravity dynamics, quantum measure, and infinite
  source-tail interfaces.

Thus `BOX_record` is upstream of **full PASS and stratum selection**, but not upstream of every
clause-local determination. In particular, the charged-vertex mismatch rejects the `chi_P=1`
displayed control branch even while other clauses remain unavailable. The honest order is:

```text
partial predicate evaluation
-> press H1
-> complete the variation
-> full predicate replay
-> derive the surviving stratum.
```

`INVERSION_ATTACK = PARTIAL: full selection blocked; the ban on partial evaluation is refuted.`

## 3. H1 — six receiving constraints, corrected binding levels

The primitive support law is the sealed packet member
`CAUSAL_INCIDENCE_SUPPORT_PRINCIPLE_V001.md`
(`b0c636f3b2b00f0694ad001cb32a3a84c5d4fc09c25c57fd4fdb8885e8206b30`), lines 12–47, bytes
`[332,1842)`, span SHA-256
`84efce645bffbed46a43976864653be9f46b215af1cdf1daf75266988fdd25d3f`. The envelope and parent
operators are in packet spec
`R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md`
(`40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9`), lines 86–116 and 118–175,
with respective byte spans/hashes `[4904,5710)` / `55f398a3462ed28dff7aa32660f7b0659406419ca7d83bba1d18e5a7c63273a6`
and `[5711,7251)` / `8ed0819504462add0f2410d87b9b7fee503a19d34e367f635f552f153d6170f4`.

Each round-1 constraint contains a lawful kernel, but several conclusions bind that kernel to the
wrong object:

| # | round-1 reading | cross-check |
|---:|---|---|
| 1 | `BOX_record` is a sum over primitive incidences and cannot be nonlocal | **OVERBOUND.** CIS says every **primitive incidence component** has one `Omega_c`, one `L_c`, and `support(L_c) subset Omega_c`. It expressly permits effective record/source/gauge/gravitational/environmental descendants from the complete parent. It neither defines all of `S_record` as `sum_c L_c` nor bans every nonlocal effective contribution. |
| 2 | one-use on new record factors | **CONFIRMED AT PRIMITIVE SCOPE.** A completed primitive incidence leaves the active generator, and later primitive incidences use new record factors. This is not a statement that every term in the full record functional is one-use. |
| 3 | `w(s)=32 min(s,1-s)^3`, no fitted profile | **CONFIRMED AT THE RECEIVER, NOT AS LITERAL ACTION CONTENT.** The packet fixes the envelope and forbids a residual interior scalar profile, post-evaluation reshaping, or independently normalized tail. `delta BOX_record` must reproduce that output. The clauses do not require the action integrand literally to contain `w(s)`. The no-fitted-profile claim survives at this corrected binding. |
| 4 | first-order finite restriction supplies the record-incidence block | **CONFIRMED AS AN OUTPUT TEST.** The complete variation must reproduce the sealed first-order parent. This does not fix the action functional's own domain; clause 5 fixes the receiver's common self-adjoint domain. |
| 5 | square-generated overlap/closure descendants with no independent coefficient | **CONFIRMED AS A MANDATORY SUBSET, NOT A COMPLETE INVENTORY.** Direct squaring generates those descendants. U3 remains open, so “descendant inventory fixed” is false. |
| 6 | exact completed-record persistence | **CONFIRMED AS A PASS CONDITION.** It constrains admissible output dynamics but neither selects an action normalization nor supplies the still-open durability map. |

The corrected H1 narrowing is:

```text
The primitive-incidence component of the completed variation must reproduce:
  causal support and one-use on new record factors;
  the intrinsic, unfitted packet envelope;
  the sealed first-order record-incidence receiver;
  its mandatory square-generated overlap/closure descendants with no
    independent coefficient; and
  exact completed-record persistence.

Still open:
  the action integrand and action domain;
  whether all of S_record is a primitive-incidence sum;
  nonprimitive/effective content;
  normalization selection;
  exhaustive descendant inventory; and
  the durability map.
```

So H1 remains `NARROWED`, but the claims that domain, descendant inventory, and cell coupling are
fixed at the action-functional level, and that normalization is thereby supplied, are withdrawn.

## 4. The four NARROWED and seven UNMOVED verdicts

### 4.1 Four rows returned as NARROWED

| row | verdict |
|---|---|
| H1 `BOX_record` | **NARROWED, CORRECTED** exactly as §3. |
| H3 `chi_P` | **DECIDED ON THE SEALED PACKET-PARENT BRANCH**, not merely narrowed; §6 derives `chi_P=0`. This is not a global theorem excluding every Pauli mutation. |
| H4 `BOX_HD` | **NARROWED CONFIRMED.** Any surviving action contribution must have a finite variation/restriction whose packet receiver remains first-order. This constrains the finite receiver, not the continuum differential order in isolation. |
| H5 `BOX_UPDATE` | **NARROWED CONFIRMED.** Its induced evolution must reproduce the unique finite propagator and finite compact-support Møller maps. No update rule or roster is supplied. |

The corrected tally over the eleven pressed rows is:

```text
DECIDED-BY-PACKET  1  (H3, branch-scoped)
NARROWED           3  (H1, H4, H5)
UNMOVED            7  (H2, H6, I1-I5)
```

I6 is newly restored to the census and was not pressed in round 1.

### 4.2 Seven rows returned as UNMOVED

All seven non-reach reasons survive, with the `K_R` category correction in §5:

| row | structural check |
|---|---|
| H2 `K_R` | **UNMOVED CONFIRMED.** The predicate receives the microscopic finite parent, while `K_R` belongs to the induced public stratum. No predicate clause maps to it. |
| H6 `BOX_gravity` | **UNMOVED CONFIRMED.** See §5.2; the packet uses `g` as background carrier but supplies no gravitational-action receiver. |
| I1 environment closure | **UNMOVED CONFIRMED.** Record-incidence constraints do not prove that record/P5 stock exhausts the charter's environment sector. |
| I2 gravitational quantum measure | **UNMOVED CONFIRMED.** No clause receives a gravitational measure. |
| I3 action/P5 no-outside | **UNMOVED CONFIRMED.** A per-candidate reproduction predicate cannot certify the candidate universe. |
| I4 O1 source tail | **UNMOVED CONFIRMED.** Clause 6 is finite and compact-support; O1 is source-inclusive and infinite-future. |
| I5 O3 cross-regulator | **UNMOVED CONFIRMED.** It remains behind O1 plus a cross-presentation comparison; the E4a template is not its discharge. |

## 5. `K_R` and `BOX_gravity`

### 5.1 `K_R`: immediate non-selection confirmed; “not ruling-shaped” corrected

The three cited bars have a narrower joint meaning than round 1 gives them:

- Q-spec S03's decision bars an ungrounded lane adoption from the construction end;
- C7 bars later response or matching objects from selecting the earlier action; and
- decline S25 bars a reparameterization/equal-action collapse of the positive `K_R` ray.

Those three grounds correctly require this lane to leave `K_R` unmoved. They do **not** prove that a
principal ruling is structurally unavailable. The exact S03 release condition expressly includes:

> “(c) a principal re-ruling of record,” with any future adoption arriving through the
> authored-residue discipline and the family first bounded.

This is sealed in `STAGE8_TASK6_LP_QSPEC_ASSEMBLY_DARIO_V005.md`
(`76589e94bb4af318880c61c3d677dc2518add8480100a7afaf675e4dd3a394a8`), lines 319–337, bytes
`[26636,27672)`, span SHA-256
`6301113ef56ad87310f276574f1156c81f88d41725ef0f5e66be9e46a86660c9`. C7's exact ordered bar is
in the same source, lines 252–266, bytes `[18457,19066)`, span SHA-256
`26f2e6b49ce71fa25c00019adfd24f25285389fc818c7494fc3e7efc2ce89d07`.

Therefore:

```text
K_R NOW          = UNMOVED; no lane selection lawful.
RULING-SHAPED    = reserved principal-release branch, not zero.
```

The slogan “a freedom the instrument cannot reach is not thereby a freedom a ruling should fill” is
a sound anti-tuning caution, not a derivation that deletes the recorded principal-re-ruling branch.

### 5.2 `BOX_gravity`: declination confirmed

No reaching clause was found. Packet spec lines 118–175 display
`h_0[g,a]`, the record-incidence insertion, and the first-order kernel. The metric `g` enters the
Dirac/spin connection as a background carrier. There is no metric Euler receiver, gravitational
kinetic term, gravitational measure, or gravitational descendant requirement in the seven clauses.
Thus clause 1 reaches the **background-dependent source operator**, not a gravitational action
functional.

The falsifier interface similarly tests incidence, record, finite propagation, outgoing source, and
descendant properties. A hypothetical gravitational descendant cannot be demanded from a sealed
parent whose operator contains no gravitational-dynamics sector. The antecedent “if the pressure
forces it” fails. Refusing to import the disqualified KK candidate is correct.

`BOX_GRAVITY_DECLINATION = CONFIRMED.`

## 6. Builder-level charged-vertex reading

### 6.1 Two exact vertex surfaces

The displayed control pair is sealed in
`COMPLETE_PARENT_ACTION_UNDERDETERMINATION_GATE_V001.md`
(`67816cfeec8d61e4bcafec90c6c8a10fa239f91355c95f424b75621f6e2f3bd6`), lines 20–54, bytes
`[506,1268)`, span SHA-256
`e3a123bd3cb063666280ca13cbbe8b3d273d85cf297a59e776f4f89484c1f83b`. Its binary vertex family is:

```text
Gamma_H3^mu(chi_P;p+q,p)
  = gamma^mu + 2 i chi_P ell_* sigma^(mu nu) q_nu,

chi_P in {0,1}  on the displayed pair.
```

The sealed packet spec calls its operator “One complete first-order parent” and gives:

```text
D_K = i gamma^mu nabla_mu + i gamma^5 C_K(x),
C_K(x) = sum_c v_c(x) M_c(x) iota_c(c_c).
```

The charged connection occurs only inside `nabla_mu`; `C_K` carries the record-incidence term and no
`sigma^(mu nu) F_(mu nu)` source insertion. Therefore the first-order charged vertex is:

```text
Gamma_packet^mu(p+q,p) = gamma^mu.
```

This reading is grounded in packet spec lines 118–175, bytes `[5711,7251)`, span SHA-256
`8ed0819504462add0f2410d87b9b7fee503a19d34e367f635f552f153d6170f4`, and corroborated by packet
result `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md`
(`345d447eaf6d730caa6fa655b92a7b0cd93a68b5f86e0c8929cf969f40aeb7cb`), lines 8–58, bytes
`[97,1422)`, span SHA-256
`85c66c6d1015d419fb8936bf6904d4f19d53b218e369ee6f6e6cbc215e9f0e01`.

### 6.2 Forced comparison

Equality of the two formal vertices for arbitrary formal `q` gives:

```text
gamma^mu + 2 i chi_P ell_* sigma^(mu nu) q_nu = gamma^mu

=> chi_P = 0
```

because the `sigma^(mu nu) q_nu` Clifford structure is independent of the minimal `gamma^mu`
structure. This is a symbolic coefficient comparison, not a numerical evaluation of a physical
quantity and not a selected fit.

The result is exactly scoped:

```text
chi_P = 0 on the sealed packet-parent branch under BIND clause 1.
```

It is **not** a theorem excluding primitive Pauli mutations from the complete action family. V011
separately records that Single-Operator Completeness postulates independent primitive Pauli away,
while `primitive_Pauli_theorem_excluded = false`; and the square of the minimal Dirac operator
generates a kinematic `sigma.F` curvature contraction. That square-generated structure is not H3's
independent microscopic Pauli vertex and does not change `chi_P=0` at first order. See sealed packet
V011 (`aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a`), lines 1033–1094,
bytes `[35078,37306)`, span SHA-256
`ba0d83a890578a37c9813d2bca8b8c2a10c9f062e1d6fd16c570379d6607472d`.

## 7. FREEDOMS-CONSUMED

```text
CARRIED, NOT SELECTED:
  the corrected sixteen-row lower-bound closure census
  S_0/S_1 as displayed controls, neither adopted as the complete action
  BOX_record's still-open integrand, action domain, normalization, effective
    content, exhaustive descendants, and durability map
  the K_R positive ray whole
  H4/H5 as narrowed receivers, with no term or update rule supplied
  H6 and I1-I6 open at their exact receiver scopes
  U1 awaiting build, U2/U3 supplierless, D-1 unselected, E4c open

STRUCTURALLY DETERMINED HERE:
  I6 SLOT9_F2_EXCLUSION_THEOREM is restored to the census
  full predicate PASS is blocked by H1 while partial evaluation is lawful
  six H1 kernels are re-bound to primitive-incidence/output scope
  H3 chi_P = 0 on the sealed packet-parent branch
  K_R's present non-selection does not erase the principal re-ruling branch
  no predicate clause reaches BOX_gravity

NOT SELECTED OR ADOPTED:
  no complete action member or family representative
  no BOX_record integrand, profile insertion, domain, or normalization
  no K_R value, response target, equal-action rule, or rescaling quotient
  no gravity action, environment action, measure, no-outside theorem, O1 state,
    or O3 comparison
  no global Pauli-exclusion theorem; packet-branch vertex binding only
  no smooth carrier or electromagnetic identification

SCALING WEIGHTS CONSUMED = none
SUBSTITUTED = none
```

## 8. Flattening check and battery

All 37 rows of `DECLINE_REGISTER_V002.md`
(`957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`) were walked. Load-bearing
dispositions:

| row | disposition |
|---|---|
| decline S03 | **CLEAN and disambiguated.** Its three `U(1)` objects remain distinct. This is not the Q-spec S03 decision whose release condition governs the action family. |
| S05 | **CLEAN.** Source/record co-location is not substituted for a complete action. |
| S08 | **CLEAN.** The packet connection is not identified with a smooth public field or electromagnetism. |
| S12 | **LIVE / CLEAN.** `COMPLETE`, `NARROWED`, and `UNMOVED` labels are not proof objects; the census and H1 overclaims are corrected by their receiving predicates. |
| S19/S24 | **CLEAN.** No decay or clustering law is imported into O1. |
| S25 | **LIVE / CLEAN.** No equal-action/reparameterization collapse of the `K_R` ray occurs. It does not delete the separate principal-re-ruling branch. |
| S26 | **CLEAN.** No smooth reference or KK ansatz is used as a source. |
| S28 | **CLEAN.** Boundary/action pressure is applied before comparison; no free `D_BR` datum is selected. |
| S34 | **NOT TRIGGERED.** No absolute scale is fixed. |

The remaining 27 rows are not activated. Separations preserved:

```text
complete against 838's list       != globally complete census
named supplier                    != discharged carrier
full predicate unavailable        != every conjunct unavailable
primitive incidence locality      != all-action locality/no nonlocal content
fixed packet output profile       != literal action-integrand profile
mandatory generated descendants   != exhaustive descendant inventory
exact persistence test            != selected action normalization
minimal first-order vertex        != square-generated kinematic Pauli term
branch-bound chi_P=0               != global Pauli-exclusion theorem
present lane non-selection         != deletion of principal re-ruling
background metric dependence      != gravitational dynamics
```

`F_PLDEC` and anti-tuning pass. No physical quantity, response, target, action value, or measured
constant was evaluated. The `chi_P` result is an exact symbolic branch comparison against a sealed
operator, not a numerical physical evaluation. M-2 covered exact phrase, hyphen/reflow,
status/flag, and semantic-equivalent false-negative modes, plus primitive/effective,
action/output, finite/infinite, and branch/global receiver distinctions.

PRE-SEAL PIN CHECK: both subjects, the state brief, charter, decline register, assembly, slot census,
V2, skeleton, A05 gate, and the complete Stage-7 packet rehashed to their stated pins; every byte
span quoted above was independently extracted and rehashed; output and sidecar names were absent
before writing. The sidecar is computed only from final artifact bytes.

Verb-audit scope includes headings, tables, displays, and final lines. `CONFIRMED` is limited to the
seven non-reach results, H4/H5's corrected receiver constraints, the no-fitted packet profile, and
the gravity declination. `DECIDED` is branch-scoped to the packet vertex. No global action closure,
Pauli theorem, ruling, authorization, or physical computation is claimed.

CENSUS = sixteenth row FOUND (I6 SLOT9_F2_EXCLUSION_THEOREM; 16 remains a lower bound, not no-outside closure)
INVERSION = partial evaluation lawful (full PASS/stratum selection blocked by H1; clause-local constraints and H3 exclusion remain evaluable)
H1_CONSTRAINTS = issues (six kernels confirmed only at primitive/output scope; global sum/nonlocal ban, action-domain fixation, exhaustive-inventory claim, and normalization reading corrected)
VERDICTS = corrections (H1 re-scoped; H3 decided; H4/H5 and seven UNMOVED reasons confirmed; corrected tally 1 decided / 3 narrowed / 7 unmoved, plus I6 unpressed)
K_R_PRINCIPLE = corrected (UNMOVED now; triple bar blocks lane/response/rescaling selection but the sealed release condition reserves principal re-ruling)
BOX_GRAVITY_DECLINATION = confirmed (background g is reached; no gravitational-action or measure receiver is)
CHI_P = decided by the vertex (chi_P=0 on the sealed packet-parent branch; generated square-level Pauli structure and global theorem remain distinct)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN / binding levels, branch scope, and census lower-bound status displayed
