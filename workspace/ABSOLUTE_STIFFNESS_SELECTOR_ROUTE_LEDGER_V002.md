# Absolute Stiffness Selector Route Ledger v002

Date: 2026-07-23

## Unchanged hard gate

Alpha has not been computed while an arbitrary finite

```text
delta S = -(hbar c/4) integral F wedge *F
```

or an equivalent action/measure/species normalization remains admissible at
the matching scale.

## Routes now retired as standalone selectors

### Symmetry, compactness, charge quantization

They fix the active pointwise `U(1)_rel` character lattice and unit charge.
They permit every positive parity-even Maxwell stiffness.

```text
status = KINEMATICS_ONLY
```

### Allow/require threshold alone

It constrains when declared support can become a record. It does not alone fix
state preparation, complete species, measure, or curvature weight.

```text
status = CELL_CONSTRAINT_ONLY
```

### Projective-lift bundle

The common phase of a ray lift is representational redundancy, not the active
relative charged handle. Its connection patch law is a conditional bundle
lemma.

```text
status = NOT_THE_PHYSICAL_CHARGED_CONNECTION_DERIVATION
```

### Squared-fidelity action

The pointwise identity

```text
F_R(theta) = cos^2(theta/2)
```

is correct. The proposed weight `exp(-S/hbar)=F_R` is one postselected
preserved-reference outcome. The complete two-outcome CTP kernel is
trace-preserving and is not that weight.

```text
status = DIAGNOSTIC_ONLY_NOT_PARENT_ACTION
```

### Minimal isotropic BCC Weyl walk

The free walk is unitary and target-free, but it contains four Weyl cones, two
of each chirality. It does not preserve the frozen one-Dirac inventory, and
its unique interacting gauging, state, boundary, mass, and finite response are
not selected.

```text
status = PARENT_PROPAGATOR_CANDIDATE_ONLY
```

No doubler-removal term may be added to repair its species count after the
fact.

## Routes still under comparison

```text
1. exact spectral/determinant parent with fully derived finite geometry;
2. finite quantum-link/record Hamiltonian with all coefficients fixed by the
   primitive interval and compact character;
3. coupled gravity-charged fixed point after the complete spectrum and
   universality class are derived;
4. a boundary-current or duality selector only if it fixes the parity-even
   normalization rather than merely charge or a topological level.
```

## Required output of any surviving route

Before evaluation, one route must provide:

```text
one complete parent action and measure;
one complete physical species inventory;
one causal cell or finite microscopic domain;
one state, contour, boundary, and edge prescription;
one gauge/diffeomorphism-compatible regulator;
one finite response prescription;
one generated mass/threshold map to the Thomson limit;
and no independent finite c F^2 deformation.
```

## Current state

```text
complete_dynamical_parent_kernel_frozen = false
complete_parameter_free_Q_spec_frozen = false
finite_c_F2_deformation_excluded = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```

