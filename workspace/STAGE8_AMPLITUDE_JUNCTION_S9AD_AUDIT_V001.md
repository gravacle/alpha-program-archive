# STAGE8 — THE AMPLITUDE JUNCTION, UN-HELD RUN — S9AD — AUDIT — V001 (2026-08-15)

Auditor: AJ-AUDIT (independent adversarial auditor; briefed to REFUTE; not told the build's
outcome; default stance: the build is wrong until each claim survives independent
re-derivation). Governing design: `BARE_SURFACE_AMPLITUDE_JUNCTION_DESIGN_V001.md` (506bfe2a;
seal verified; read in full FIRST; its question, ground, honest-outcome menu, and every
requirement bind verbatim; HELD banner historical — un-hold instrument of record
`AMPLITUDE_JUNCTION_UNHOLD_2026-08-15.md`, 3504c629, verified). Ground currency: surface of
record V003 (`SURFACE_DEFINITION_OF_RECORD_V003.md`, c80c09a2), consumed; V001 byte-carriage
independently re-proven (§1). Under audit: `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md`
(15148ab5; seal verified at path).

**INDEPENDENCE PROTOCOL EXECUTED:** the design and full ground were read and my own route
enumeration, pre-verdict, and attack checklist were FIXED in a hashed scratchpad note BEFORE
the build was opened (`AJ_AUDIT_PRECOMMIT_NOTES.md`, SHA-256
`c29d02474f101632aca2efb0b067fa9088a761778f5bfaaceeeb5c9af4c0ddf0`, written and hashed prior
to first read of the build). My pre-fixed verdict on the design's menu: VACANCY-CLOSED,
conditional on C12, via §I.1's "alone" + the §II.6 categorical booking + polar typology. The
build's verdict, read afterward: VACANCY-CLOSED via a DIFFERENT and stronger engine (two-model
conservativity + reading case-split, closing both §II.6 readings where my route needed the
broad one). Convergent verdicts by independent routes.

Fences live throughout: alpha_computed = false · proof_authorized = false ·
kappa_record_computed = false. Exact symbolic only; no numeric evaluation of any physical
constant; no comparison to any measured value; no value frozen; PROPOSED-NOT-ADOPTED —
nothing here is record.

---

## 0. VERDICT (one line)

**CONFIRMED-WITH-CORRECTIONS** — the build's VACANCY-CLOSED stands under independent
adversarial re-derivation at its exact quantifier (route space partitioned by a
complement-total derived partition; Class I killed by the constant-modulus theorem, Class III
by the crossing requirement, Class II by the two-model conservativity theorem, all by proof,
none by sweep; residue honestly named as the C12 categorical-scope premise plus the
finite-sealed-text enumeration warrant; TYPE-R not re-walked; no junction candidate produced;
emergent-magnitude reading engaged and not foreclosed), with three corrections named exactly
and none load-bearing: (C-1) the strongest natural candidate family — the booked response
magnitudes (first-opening probabilities, projective lengths) with L2's indexing assertion as
the crossing — is never NAMED in the build's route tables; its kill follows from the build's
own Theorem II identification prong + §6.4 + §6.5 and is verified here (CAS X7, X11); (C-2)
§6.3's second Gleason kill conflates the line bundle's fiber dimension with the dimension of
the would-be frame-function substrate (which the authored Hilbert space would set, not the
fiber) — kills 1 and 3 are general and sufficient, kill 2 covers only the minimal fiber-local
substrate; (C-3) CAS M8 mechanically checks only the lock half of the "full displayed equation
set" co-occurrence claim, the phase-law half resting on prose inspection — completed
mechanically here (CAS X5), claim true at bytes.

---

## 1. SEALS AND MECHANICAL VERIFICATIONS

All verified at path with `shasum -a 256` this session:

| Document | SHA-256 (computed) | Check |
|---|---|---|
| design `BARE_SURFACE_AMPLITUDE_JUNCTION_DESIGN_V001.md` | `506bfe2ae9ff5dfecce5f399a6a68761d83c9361f52ead304666f89a67601980` | = sidecar; = commission's `506bfe2a`, MATCH |
| un-hold `AMPLITUDE_JUNCTION_UNHOLD_2026-08-15.md` | `3504c62917b3e9f4591f9470bc74e2fb82472217ace22815f4d4528adebe9255` | = sidecar, MATCH |
| surface `SURFACE_DEFINITION_OF_RECORD_V003.md` | `c80c09a2078fc5fb40b6695943f29262306c4e2d6dddb6563e6a28d42b747181` | = sidecar; = commission's `c80c09a2`, MATCH |
| build `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md` | `15148ab5687a0afe46fcb5d2c5cd310879d49557beef878791f6823c017cd4af` | = sidecar, MATCH |
| I-2 determination (ground row 2) | `9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c` | = design's `95235195`, MATCH |
| I-2 audit (ground row 2) | `97ae605d065b7b614ed2927602403cd72550507b26bf71fb0f7299be4c545d73` | = design's `97ae605d`, MATCH |

- **This artifact's path probed ABSENT before any write** (both .md and sidecar).
- **Ground currency, re-proven independently:** V001's body-from-`**Purpose.**` and separately
  its §I–§II block re-checked as exact substrings of V003 by my own script: both TRUE. The
  build's K1 is verified, not merely trusted.
- **Build battery fidelity:** the embedded script was mechanically extracted (exactly one
  python block, one output block) and re-run in a fresh venv (sympy 1.14.0, matching): re-run
  output **byte-identical** to the embedded output block (diff: IDENTICAL; 22/22 PASS).
  Independent scans of script and output: no `evalf`, no `N(`, no float literal, no
  digit-dot-digit numeric outside section tags; the exact integers/rationals (1, 1/2, 1/4,
  −2) are algebra residuals, not evaluations of any physical constant.
- **Forbidden-token sweep of the build:** K_KK, Thomson, S16, κ_Thomson, Carleman, S2′, K_n,
  Λ^even, ℓ_P, Planck, diamond, Finish-A/B, 4π, sphere, 137, electron, QED — every hit falls
  inside the build's §13 denial list; zero substantive uses. "Gleason" appears only inside
  §6.3's objection-kill (generic mathematics, used to defeat an objection, same license as the
  I-2 pair's polar decomposition).

---

## 2. THE ATTACK, EXECUTED (pre-fixed checklist, applied at bytes)

### 2.1 The closure's quantifier (attack priority: vacancy-CLOSED)

- **Partition totality.** Class I (phase-driven, magnitude factors through modulus on the
  native algebra **A**) vs Class II (phase-driven, does not) is a complement pair; Class III
  is the complement of "phase-driven." X vs not-X is total — this is the strongest available
  form of DERIVED exhaustion (a tautology of the partition's definition), with the derived
  content correctly located in the inventories (finite sealed text; the I-2 corrected
  inventory as ground row 2). The design's "class exhaustion itself derived, not asserted" is
  satisfied. **HOLDS.**
- **Class I kill.** Theorem I re-derived (my X6, X9; build N1–N4, T1 re-run byte-identical):
  modulus identically 1 on **A**, refinement step included (C12-conditional, marked so in K4).
  Any function of a constant is constant; no phase-driven candidate survives in Class I.
  **HOLDS.**
- **Class III kill.** Non-phase-driven content fails J-cross; inventory swept (locks, counting
  data, labels, H_R·T_R/ℏ, onset bound); bare T_R/H_R additionally fail J-β. **HOLDS** —
  subject to C-1's naming gap (below): the booked response magnitudes are not in the §5 table.
  As candidates they are phase-driven per L2, hence Class II, hence covered — but the build
  never says so (C-1).
- **Class II kill — the load-bearing piece, attacked hardest.** Theorem II's logic: derivable
  ⇒ invariant across all models of the ground; two expansions of the ground (profiles f₁ =
  sin²(Δ/2) and f₂ = f₁²) both satisfy every displayed clause (disjoint vocabulary — M8/M9,
  completed at X5) and differ (exact witness 1/4 at the toy argument, X4); hence no profile is
  forced; hence every Class II candidate carries an authored ingredient and fails J-der.
  Sound. **Strongest counter-attack I constructed (fixed pre-read as the definability
  attack):** ambient complex arithmetic makes profiles like (2−w−w̄)/4 explicitly DEFINABLE
  from ground data — a defined term is carried identically by every model, so the two-symbol
  freedom alone would not kill it. **Resolution, present in the build's own text:** Theorem II
  names THREE authored ingredients — the exit operation, the profile, AND "the identification
  of the output with the write structure's response." Definability supplies candidates
  (infinitely many: f₁, f₂, Δ-powers, …); no ground clause selects one OR identifies any as
  the surface's response; selection + identification remain authored. The kill survives the
  attack at its sharpest form. **HOLDS.**
- **Existence not forced either (my strengthening):** the junction-free expansion also models
  every displayed clause (X11) — the ground forces neither the junction's form nor its
  existence. The build did not display this third model; it is consistent with and strengthens
  §8.1.
- **Withheld content.** §6.4 inherits Chain E typing at C12's exact strength; honestly marked
  CONDITIONAL (K10) and carried into the verdict line's residue. The §8.2 partition of the
  residue (conservativity core unconditional; withheld-content extension C12-conditional) is
  accurate — noting (sharpening S-1) that §8.1 item 3 read ALONE cites Theorem II without
  visibly carrying the C12 arm; §8.2 + K10 + the flag block carry it, so the artifact as a
  whole states its quantifier honestly.

### 2.2 TYPE-R fence (any unit-modulus route recast?)

- No trace-class/T_cyl machinery re-run or extended anywhere (token and content scan).
- The one place amplitude is EXTRACTED from unit-modulus objects — the toys' superposition —
  is meta-level (a derivability counterexample), never a candidate, never surface content
  (TOY_SEPARATION verified below). The dead route sought to DERIVE the junction from the
  write signature; the toys prove the opposite (nothing is derivable).
- My X1 confirms the trap the build had to dodge and did: RAW sector-superposition magnitude
  |z⁺+z⁻| is transition-VARIANT (exact residual 2cos(2h+2θ)−2cos(2h)) — a build walking naive
  interference as a candidate would have been refuted here. This build factored its toys
  through the invariant w (X2, full per-sector transition action — the C-A lesson correctly
  applied, not a common shift). **TYPE_R_REWALKED = no, CONFIRMED.**

### 2.3 R9-JII exposure statement

Present (§11) and correct at bytes: no junction candidate exists, so the kill-test has no
object; the standing-armed statement for any future candidate is displayed; R9-JII's content
was not opened (cited by name as the commission requires). **CONFORMS.**

### 2.4 Design fidelity

- Question executed verbatim (§2.1 of the build = the design's THE QUESTION in substance);
  menu entry 2 returned in the menu's own terms; STOP clause never reached (no candidate).
- Output path: the design names `workspace/BARE_SURFACE_AMPLITUDE_JUNCTION_V001.md`; the
  build sits at the commission-distinct S9AD path with the mapping displayed (§1 note). My
  own commission independently names the same S9AD build path, corroborating the
  commissioning-instrument assignment. Ruled: disclosed commission-level renaming, not a
  design breach; the design's named path remains unclaimed.
- Flag block: JUNCTION_STATUS, MACHINERY_INVOKED = no, SCALE_TOUCHED, VALUE_EVALUATED = no,
  CHOICE_LEDGER (10 rows; the extra FORCED class is supererogatory — FORCED rows are not
  unforced choices and their inclusion hides nothing), claim-by-claim marks K1–K12 —
  all design-required elements present. **CONFORMS.**
- Ground discipline: V003 §III–§VI and §VII non-load-bearing as declared — scanned; the
  refinement handling uses §I.5 + §II.6 typing only; §VII.17/18 nowhere consumed; the
  un-hold's historical notes supply no premise. **CONFORMS.**

### 2.5 Emergent-magnitude engagement (attack priority 6)

The build engages the reading exactly where the design and un-hold demand: §8.3 places
downstream emergence outside the closure's quantifier and explicitly does not foreclose it;
the NET flag repeats it. A VACANCY-CLOSED at the origin is CONSISTENT with the emergent
reading (the vacancy at the origin is what that reading predicts); the build claims no more
than "neither used nor evaluated" — the conservative statement. **NOT FORECLOSED; CONFORMS.**

### 2.6 Injection, blindness, fences

- Injection: no directive content aimed at auditor, registrar, or acceptance anywhere in the
  build; §11's "must face it at its landing" is program logic; acceptance left to the design's
  own note. **None found.**
- Blindness: file list (§13) is the six sealed documents + outputs + scratchpad process
  files; every load-bearing quote traces into the sealed ground ("no native metric argument",
  "provably do not carry", "fixed line bundle", the I-2 C-A/C-B/C-C content — all grep-traced
  into V003/the I-2 pair); "Q-…" tokens treated as opaque; no fenced-class file opened.
  **CORROBORATED.**
- Fences: VALUE_EVALUATED = no confirmed independently (scans of build text, script, output);
  no closed form of any would-be solution (none exists — the run ends at a closure); π, √2, ℏ
  symbolic everywhere; no empirical token outside denial lists; the toy-argument substitution
  Δ = π/2 yielding 1/4 is an exact algebra residual, not an evaluation. **INTACT.**

---

## 3. CORRECTIONS (named exactly; none load-bearing) AND SHARPENINGS

- **C-1 (route-inventory naming gap; completeness of the displayed sweep).** The I-2 pair's
  Chain D carried a row for the booked response-side magnitudes (first-opening probabilities,
  orthogonality, endpoint transfer, projective lengths). The build's §5 Class III table drops
  that row, and no route table names the natural candidate family "identify the junction with
  a booked response magnitude, with L2's indexing assertion supplying the crossing." That
  family is the closest thing the ground has to an existence assertion of the junction and an
  auditor's first refutation target. Its kill IS in the build's machinery — the candidate is
  Class II (phase-driven per L2, form adjoined); Theorem II's identification/profile prongs
  void it (my X7: P := f₁ and P := f₂ both nonconstant and both normalizing — L2's existence
  assertion selects nothing), §6.4/C12 types the converter non-magnitude, §6.5's broad horn
  closes it directly — but the build never says so. Effect: displayed completeness corrected;
  verdict untouched.
- **C-2 (§6.3 kill 2, dimension argument mis-scoped).** "On the ground's own dim-1 line
  bundle" applies §I.4's fiber dimension to the frame-function substrate. A Gleason-style
  forcing would run on the span of superposed record data — a space the AUTHORED Hilbert
  structure would set, not the fiber — so kill 2 reaches only the minimal fiber-local
  substrate. Kills 1 (the substrate is itself adjoined — decisive and fully general) and 3
  (the displayed joint constraints do not separate the models — CAS-verified) carry the
  objection's defeat alone. Effect: "killed three independent ways" over-counts by scope;
  the objection stays dead; verdict untouched.
- **C-3 (CAS M8 coverage).** M8's mechanical content is `lock_vars ∩ phase_sector_syms = ∅` —
  the lock half of the co-occurrence claim; the phase-law half (character map, transition/
  cocycle law, conjugate closure contain no response symbol) rested on prose inspection.
  Completed mechanically here (X5): TRUE. Effect: wording-level; the conservativity fact holds
  at bytes on both halves.
- **S-1 (sharpening, no error).** §8.1 item 3 read alone does not carry the C12 arm that
  extends non-forcing from displayed to withheld content; §8.2, K10, and the flag block do.
  Recommend any consumer read §8.1 with §8.2 attached, as the build itself directs.
- **S-2 (sharpening, no error).** The definability attack (ambient ℂ-arithmetic makes
  profiles ground-definable) is defeated by Theorem II's identification prong, not by the
  model-pair freedom alone; the build's three-ingredient wording already contains this, and
  it deserves the emphasis: selection-from-infinitely-many-definables plus
  identification-with-the-response are the authored acts no definability can discharge.

---

## 4. DEFEAT-PROVENANCE (this audit's own objections)

- **Sustained (C-1, C-2, C-3):** each stands on the sealed ground pair, the build's own text,
  and exact algebra (X-battery) alone. No machinery object needed to state or prove any.
  VALID.
- **Adjudicated and rejected on bedrock (genuine objections, defeated fairly):** (i) "L2 +
  booked probabilities assert an existing magnitude-junction, so the verdict should be
  PARTIAL-DELIVERED, not VACANCY-CLOSED" — dies on the design's own question (a DERIVATION
  is what is asked; the form is unforced (X7), existence is not forced by the displayed
  clauses either (X11), and the converter's magnitude-typing is barred at C12) — an
  existence-assertion with underivable form is exactly the vacancy of a DERIVED junction.
  (ii) "the two-model theorem ignores explicit definability" — defeated at the
  identification prong (S-2).
- **Candidate refutations REJECTED as FALSE defeats (machinery-dependent; labeled, never
  used):** (a) "the machinery track's convergence (S2′/K_n) shows amplitude structure exists,
  so the vacancy cannot close" — the design bars it verbatim ("the convergence MOTIVATES this
  test; it may not enter"); (b) "K* (V003 §VII.18) is a booked magnitude-valued
  induced-response quantity, so the junction already exists" — §VII is off-ground for this
  design, and K* is bytes-typed APPARATUS-ONLY on metric-carrying cell data (forbidden
  metric import). Neither is used anywhere in this audit's adjudication.

## 5. CHOICE LEDGER (this audit's unforced choices)

| # | Choice | Class | Disposition |
|---|---|---|---|
| 1 | Pre-commit notes fixed before opening the build (independence protocol) | FORCED | commission requirement; hash c29d0247 displayed §0 header |
| 2 | Independent battery formulation (X1–X11) rather than variant re-run | IMMATERIAL(proven) | build battery ALSO re-run byte-identically (§1); both routes agree on every shared claim |
| 3 | Attack priority order (quantifier → TYPE-R → fidelity → fences) | IMMATERIAL | commission's own priority list for a vacancy-CLOSED outcome; all priorities executed regardless of order |
| 4 | Ruling the S9AD path a disclosed renaming, not a breach | PREMISE(named) | rests on my own commission naming the same build path (corroboration displayed §2.4); the commissioning instrument itself was not opened by me |
| 5 | CAS environment (fresh venv, sympy 1.14.0) | IMMATERIAL | exact symbolic identities; any exact CAS reproduces them |
| 6 | Treating Gleason facts as generic mathematics for adjudicating §6.3 | IMMATERIAL(proven) | used only to SCOPE the build's kill 2 (C-2); the objection's defeat rests on kills 1 and 3, both ground-native |

OPEN choices: none.

## 6. TOY_SEPARATION

**TOY_SEPARATION = clean** — in the build (verified: f₁/f₂ declared TOY at introduction and
in the battery comments; confined to §6.2/§7/§9/§12; never asserted as surface content; no
leakage into any verdict or flag line — the verdict cites them only as model exhibits, which
IS the theorem's content) and in this audit (my X1 superposition objects and the f₁/f₂
handling are meta-level exhibits about derivability and invariance-failure only; nothing toy
enters any sustained finding as surface content).

## 7. SWEEP CUTOFF (declared)

**SWEEP_CUTOFF = the sealed instrument set at verified seals (design 506bfe2a, un-hold
3504c629, V003 c80c09a2, V001 20ee87c0, I-2 pair 95235195/97ae605d) plus the build under
audit (15148ab5), and nothing beyond.** Route space swept per my pre-fixed enumeration
(R-1…R-9, hashed notes): functionals of the displayed ground under its displayed operations,
addition-if-adjoined, response-side identification routes, counting routes, and
named-but-undisplayed items as typed by the categorical bookings; NOT swept: route classes
requiring structure neither displayed nor named by the ground (nonstandard/categorical
completions, measure-theoretic enlargements, off-record boundary constructions) — outside
"surface-native" by the design's own wording, and in any case inside the build's Class II
where Theorem II quantifies over them. No corpus sweep; no file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…" tokens treated as opaque
(EXPECTED-UNLOCATABLE); no git command run; no existing file edited; exactly this artifact +
its seal sidecar written, path probed ABSENT first.

---

## 8. FLAG BLOCK

AJ_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS — the build's VACANCY-CLOSED stands under independent adversarial re-derivation at its exact quantifier: partition complement-total (derived), Class I killed by constant modulus (re-derived), Class III by the crossing requirement (re-derived), Class II by two-model conservativity (re-derived; strongest counter-attack — ambient definability — defeated at the build's own identification prong; my third model X11 strengthens non-forcing to existence); residue honestly named (C12 categorical-scope premise + finite-sealed-text warrant); corrections C-1 (the booked-response-magnitude/L2 candidate family never named in the route tables; killed by the build's own machinery, verified X7/X11), C-2 (Gleason kill 2 mis-scopes fiber dimension for substrate dimension; kills 1 and 3 suffice), C-3 (CAS M8 covers only the lock half of the co-occurrence claim mechanically; completed at X5); none load-bearing, verdict chain intact
INDEPENDENCE = protocol executed — route enumeration, pre-verdict (VACANCY-CLOSED conditional on C12, by a different closure route), and attack checklist fixed in hashed notes (c29d0247) BEFORE the build was opened; build then read once in full and attacked at bytes; convergent verdicts by independent engines
TYPE_R_REWALKED = no — confirmed: no trace-class/T_cyl machinery re-run or extended; the toys' superposition is meta-level derivability evidence, not a candidate; the naive interference candidate route (which WOULD be a TYPE-R recast and is transition-variant, exact residual exhibited at X1) was not walked by the build
REVERTED_TO_MACHINERY = no — every build chain stands on the sealed ground pair and generic mathematics; forbidden-token sweep clean (all hits inside the §13 denial list); no lens/ether motivation anywhere; V003 §III–§VII non-load-bearing as declared
R9_JII_EXPOSURE_CHECK = conforms — no junction candidate produced, kill-test objectless and untouched, standing-armed statement displayed, content not opened
EMERGENT_MAGNITUDE = engaged, not foreclosed — the closure is placed at the origin only; downstream emergence explicitly left outside the quantifier, consistent with the un-hold's live honest reading
DESIGN_FIDELITY = conforms — the design's question executed verbatim, menu entry 2 returned in the menu's own terms, all required flags present, choice ledger conformant (0 OPEN), claim marks K1–K12 honest (2 CONDITIONAL = the C12 inheritance); S9AD path ruled a disclosed commission-level renaming corroborated by this audit's own commission, not a breach
CAS = build battery re-run BYTE-IDENTICAL (22/22 PASS, fresh venv, sympy 1.14.0, zero numerics on independent scan); audit battery independent formulation 11/11 PASS (X1–X11), exact symbolic only
VALUE_EVALUATED = no — confirmed for build and audit: no numeric evaluation of any physical constant, no closed form of any would-be solution, no measured value, no comparison; exact rationals appearing are algebra residuals
TOY_SEPARATION = clean — build and audit both; no toy quantity leaks into any verdict, flag, or ground claim
SWEEP_CUTOFF = the sealed instrument set + the build at verified seals, nothing beyond; exhaustion in the closure is by PROOF, not sweep, so the cutoff bounds file access, not the quantifier
FENCES = intact — alpha_computed = false · proof_authorized = false · kappa_record_computed = false live throughout; no scale fixed, anchored, or converted; PROPOSED-NOT-ADOPTED preserved: the build's closure is CLAIMED, panel discipline pending, and this audit decides nothing beyond its own verdict
NET = The un-held run did the opposite of chasing machinery: it proved the machinery cannot be chased into existence on this ground — the sealed surface contains no equation that could force any magnitude-valued internal-to-external response junction, the record's own algebra yields only the constant 1, and every construction route necessarily carries an authored ingredient — so the founding caution is discharged by theorem rather than by restraint, the vacancy is CLOSED at the origin conditional on exactly the named C12 residue, and the only supply route is a change of ground: new booked structure carrying both the exit operation and its forcing law, with R9-JII armed at precisely that door.
