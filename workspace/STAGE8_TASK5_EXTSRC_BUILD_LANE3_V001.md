# STAGE 8 / TASK 5 / STEP 4 — THE EXTSRC MEMBER, ROUND 1 OF THE TIMEBOX

Lane: **Codex Lane 3 (Sol, high effort)**  
Artifact: `STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md`  
Register preflight head: **Q-523**  
Mode: **symbolic only; construction/determination; no member binding**

## Lead determination

The Joint-A specification can be isolated exactly, and the quotient descent
inside it is automatic once a continuous source-extension member exists.  The
sealed stock does not, however, supply that member.  The direct source formula
stops before it becomes a map on the physical R5 image: its two terms do not
yet land through sealed arrows in one named `D_N^ker`, mixed R5 relations have
no proved common value, and continuity for the inherited R5 topology is not
proved.  The addressed `Res/Ker` factor and the required nonidentity transport
squares are separately uninhabited.

The review's near-miss is real and is tested here as a component rather than
discarded.  The built map

```text
for G=G_N and T_N=Omega_c^1(M_N;iR),

A_N:=A_(G_N):T_N -> (K_N^fin)^*,
(A_N a)(c)=sum_e c_e integral_(gamma_(N,e)) a                     (L1)
```

is the stage-indexed form of the review's `A_G`; it is basis-free,
reader-free, and onto.  It nevertheless cannot be composed
into ExtSrc without two additional maps of the exact types

```text
b_N:O_R5,N^src -> T_N,
e_N:(K_N^fin)^* -> D_N^ker.                                     (L2)
```

Neither is sealed.  Restricting a support or a cell set does not turn an R5
profile into a compactly supported connection one-form; this is the review's
sector-not-scope finding in the Joint-A type.  Surjectivity of `A_N` gives
individual preimages, not a canonical, linear, support-compatible, covariant
section, and it does not repair the codomain seam.

Accordingly Round 1 stops with a sealed-stock `TYPE-U` obstruction.  This is
not a proof that the ExtSrc family is empty.  Round 2 is specified in Section
5 and remains wholly inside the ruled ExtSrc timebox.

## 0. Preflight and authority ledger

### 0.1 Access, register, decision, seals, and no-clobber

Before substantive work:

```text
cleanroom workspace = readable and writable
archive workspace   = readable
supervision         = readable
register head       = Q-523
requested output    = absent in cleanroom and archive
requested sidecar   = absent in cleanroom and archive
```

The live register entry `Q-523` states the sequenced program, the four-round
ExtSrc timebox, the standing explicit-incompleteness fallback, and the
cross-sector-unit obligation.  The decision file was read and its sidecar
verified before construction.

### 0.2 Verified load-bearing authorities

| Key | Sealed authority | Verified SHA-256 | Use here |
|---|---|---|---|
| `SEQ` | `DECISION_SEQUENCED_PROGRAM_2026-08-06.md` | `7984d6aba553d50bb1bf5013c687e14e7d77a8a43f53e5101fcece1fc98ba5ac` | ExtSrc first; four-round timebox; incompleteness fallback; unit obligation |
| `JII` | `STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md` | `fdf20bd475b875ee000157d367869f4d7c31e18590b6eeb2ce1f60345c881e70` | consolidated specification, Joint A, `(L2)`, S22/S22a, surface board |
| `JII-R` | `STAGE8_TASK5_JII_REVIEW_DARIO_V001.md` | `ba9430c17c61adbfce41df21dc5a14a34707c210cb69fc9c7455dd0530c19236` | confirmed obstruction; `A_G` near-miss; sector-not-scope finding |
| `SEAM1` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V001.md` | `5de94e16db09b982f85fdf117281af62df6ab0d05c9e7577ac258d47594b69b7` | corestriction, automatic descent, exact ExtSrc and transport declarations |
| `SEAM2` | `STAGE8_TASK5_COMMON_SEAMS_LANE3_V002.md` | `2525096ba06973b70064d6b9f9578470e0afca9c48e2cca6cf0f5c1194d12c52` | addressed orbit and repaired diagonal R9 quantifier |
| `KER` | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md` | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual currents, test forms, onto evaluation map, bilocal kernel |
| `Q408` | `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V004.md` | `c5254f663a1e3a6d1079027184c6dea96a35ab7255a856aa39e584b3d7c32dc2` | surface-visible test quotient, `Abar_G` isomorphism; test transport built on actual finite/`Ref_path` core and conditional beyond |
| `MET` | `DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md` | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | derived-or-declared unit discipline; no implicit cross-sector unit |
| `A9` | `DOR_020_A9_XI_N_ADOPTED_2026-08-05.md` | `e0b661807f172337ad1cca1b89feec2b6472d531f6dbaae3bd3e39a953510c15` | HOL interface adopted; ExtSrc remains open and uninhabited |

The local and archive copies of `JII` agree at the cited digest.  Every
authority above having a sidecar was checked successfully.

### 0.3 Status vocabulary and gates

```text
[PROVABLE]      = displayed consequence of verified sealed stock.
[PART-PROVABLE] = conditional consequence on the stated uninhabited premise.
[YOURS]         = candidate/specification stated here; not adopted or bound.

TYPE-U = a required member or theorem is not produced by sealed stock.
TYPE-R = already typed requirements are mutually inconsistent.
```

The outcome below is `TYPE-U`, not `TYPE-R`.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_bound = false
fixed_point_executed = false
end_test_executed = false
numeric_evaluation = false
comparison_to_measured_constants = none
```

No fence blocks the structural determination.  The gaps are absent
mathematical members/certificates, so no fence-based `MACHINERY-APPEAL` is
entered.

## 1. A1 — Joint-A specification isolated

### 1.1 The common consumer intersection

[PROVABLE] The common part of the two A8 routes is exactly the addressed
source-extension prefix:

```text
H route:
O_a^resp --Ker_(a,K)--> D_N^Loc
         --Loc_N^C--> C_N^k;

HOL route:
O_a^resp --Ker_(a,K)--> D_N^Loc
         --Xi_N--> Lambda_N^cyc.                               (A1-1)
```

Thus Joint A must supply the first arrow with a physical source agreement;
S22/S22a separately consume its rank-preserving family carriage.  It need
not supply `Loc_N^phys`, `iota_N^H`, a symbol-to-cochain map, `Xi_N`, `G4-D`,
`G5`, `FULL-G4`, or `G2-N`.  A9 adopts the HOL interface but explicitly
inhabits none of ExtSrc or those gates.

The literal route-prefix intersection of `(A1-1)` with S22/S22a is

```text
RoutePrefix_N^lit
 :=(D_N^Loc,
    Ker_(a,K):O_a^resp->D_N^Loc,
    etahat_f:D_N^Loc->D_M^Loc;
    identity and composition),

RoutePrefix_N^phys
 :=(RoutePrefix_N^lit,PhysAgree_(a,K)).                         (A1-2r)
```

`RoutePrefix_N^lit` is what the already-formed route tails would consume
**after** Joint A.  `PhysAgree` is added before the prefix can count as a physical Joint-A
construction.  To close Joint A rather than retain a bare route
signature, the **Joint-A construction-minimal** package is

```text
ExtSrc_N^JA :=
 (E_N,qtilde_N,Q_N^quot,O_prof,N,
  O_R5,N^src,D_N^ker,D_N^Loc,
  iota_N^Q408,Sch_N,S_N,Kraw_N,Sbar_N,Kernbarhat_N,
  Ker_(a,K):O_a^resp->D_N^Loc,
  PhysAgree_(a,K),
  AddrOrb_N,
  etahat_f:D_N^Loc->D_M^Loc;
  source agreement, image/topology, physical response agreement,
  addressed-orbit compatibility,
  identity and composition).                                  (A1-2)
```

Here `PhysAgree_(a,K)` is a genuine agreement of `Ker_(a,K)` with an actual
unaddressed response realization; its smallest displayed witness is given in
`(A1-18m)` below.  Without it the zero map would satisfy the bare signature
while ignoring the physical source.

The longer `Res/QuotResCert/Ker^im/Ker^ker`, full `tau`, common-refinement,
support, and downward-`rho` package from `JII` is a **sufficient realization
witness** for `(A1-2)` and for later consumers.  It is not silently called a
syntactic requirement of Gamma-Joint-I, `(L2)`, or S22/S22a.  Sections
1.3--1.4 retain and test it because it is the only consolidated construction
route presently declared.  This is an isolated specification, not a claim
that either the minimal tuple or its longer witness exists.

| Consumer | Exact ExtSrc content consumed | Excluded tail |
|---|---|---|
| `Gamma^H`, route after Joint I | `RoutePrefix_N^lit`, especially `Ker_(a,K):O_a^resp->D_N^Loc` | physical `Loc/iota^H` belongs to Joint II |
| `Gamma^H`, Joint-I construction contract | `(A1-3)`--`(A1-13)` plus physically agreed addressed `Ker` | the route cannot inherit these from the bare arrow signature |
| HOL route `(A1-1)` | the same `D_N^Loc` and addressed `Ker` | `Xi_N`, its transports, and its A9 gates are separate |
| S22 | `etahat_f:=tau_f^Loc:D_N^Loc->D_M^Loc` | symbol/cochain transports are not ExtSrc |
| S22a | identity and composition for `etahat_f` | the construction debt is extending generator maps to a relation-respecting functor; `(A1-26)` then follows |
| later ExtSrc/A6 carriage | the same functor-extension regression and downward `rho_h^Loc` on old images | not charged to the three route-prefix interfaces; no upward target-new-cycle lift |

### 1.2 Raw source, topology, and automatic descent

[PROVABLE] The sealed raw presentation is

```text
E_N
 :=R L_T,N direct-sum R Rhat_K,N
       direct-sum S_(1,sa)(K_N),

qtilde_N:E_N->O_R5,N,
I_N:=im(qtilde_N),
Rel_N:=closure(ker(qtilde_N)),

O_prof,N:=E_N/Rel_N,
Q_N^quot:E_N->>O_prof,N,

I_N^q :=I_N with the qtilde_N quotient topology,
I_N^R5:=I_N with the O_R5,N subspace topology.                 (A1-3)
```

The literal arrows are

```text
Q_N^core:E_N->>I_N^q,
Q_N^core(z)=qtilde_N(z),

kappa_N:I_N^q->I_N^R5,  kappa_N(x)=x,
jmath_N:I_N^R5->O_R5,N, jmath_N(x)=x.                         (A1-4)
```

The underlying-set identity does not prove that `kappa_N` is continuous or a
topological embedding.  If `Rel_N=ker(qtilde_N)`, the first-isomorphism arrow
is

```text
beta_N:O_prof,N->I_N^q,
beta_N(Q_N^quot z):=qtilde_N(z),
Q_N^core=beta_N Q_N^quot.                                    (A1-5)
```

The extension member must supply

```text
D_N^ker                         Hausdorff TVS,
O_R5,N^src subset O_R5,N        vector subspace,
I_N^R5 subset O_R5,N^src,

S_N:O_R5,N^src->D_N^ker         continuous linear,
Kraw_N:=S_N qtilde_N:E_N->D_N^ker
 continuous and linear.                                       (A1-6)
```

[PART-PROVABLE] Once `(A1-6)` is inhabited, descent is automatic:

```text
r in ker(qtilde_N)
 =>Kraw_N(r)=S_N(qtilde_N(r))=S_N(0)=0,

Rel_N=closure(ker(qtilde_N)) subset ker(Kraw_N),

Sbar_N:O_prof,N->D_N^ker,
Sbar_N continuous and linear,
Sbar_N(Q_N^quot z):=Kraw_N(z),                                (A1-7)

Q_N^quot z=Q_N^quot z'
 =>z-z' in Rel_N
 =>Kraw_N(z-z')=0
 =>Sbar_N(Q_N^quot z)=Sbar_N(Q_N^quot z').                    (A1-8)
```

Therefore quotient descent is not counted as an independent missing member.
What remains is the physical source extension and topology in `(A1-6)`.

The source formula's two branches have the local types

```text
E_N^cyc
 :={0} direct-sum R Rhat_K,N direct-sum S_(1,sa)(K_N),

O_N^cyc
 :=Q_N^quot(E_N^cyc) subset O_prof,N,

Kernbar_N^cyc:O_N^cyc->Ker_N^Q408,                             (A1-8a)

iota_N^Q408:Ker_N^Q408->D_N^ker
 continuous and injective,

M_N^loc in Op_N^(loc,2),
Sch_N:Op_N^(loc,2)->D_N^ker
 continuous and injective.                                    (A1-8b)
```

`E_N^cyc`, `O_N^cyc`, and `Kernbar_N^cyc` are sealed on the displayed
cycle/profile branch.  The common landing maps `iota_N^Q408` and `Sch_N` are
required ExtSrc members, not sealed constructions.  In particular,
`Q_N^quot(0,b,C)` is now visibly in `O_N^cyc` before it is consumed.

The exact source agreement, including `L_T`, is

```text
S_N(qtilde_N(a,b,C))
 =a Sch_N(M_N^loc)
  +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)),                 (A1-9)

S_N(qtilde_N(1,0,0))
 =Sch_N(M_N^loc)=:kappa_T,N,                                  (A1-10)

S_N(qtilde_N(0,b,C))
 =iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                 (A1-11)
```

Only for a member satisfying these clauses may one define

```text
D_N^Loc:=im(Sbar_N)=im(Kraw_N) subset D_N^ker,

D_N^Loc carries the quotient topology from E_N/ker(Kraw_N),
D_N^Loc->D_N^ker is a topological embedding,

Kernbarhat_N:=corestr_(D_N^Loc)(Sbar_N)
 :O_prof,N->D_N^Loc
 continuous and linear.                                       (A1-12)
```

The exact Joint-A image conditions are

```text
kappa_T,N in D_N^Loc,

iota_N^Q408 Kernbar_N^cyc(O_N^cyc) subset D_N^Loc,

D_N^Loc
 =span{kappa_T,N}
  +iota_N^Q408 Kernbar_N^cyc(O_N^cyc).                         (A1-13)
```

The plus sign in `(A1-13)` is not asserted to be direct.

`JII` also carries

```text
D_N^Loc subset Sch_N(Op_N^(loc,2))                             (A1-14)
```

as the physical-kernel eligibility premise for the next, H-tail Joint B.  It
is recorded here to preserve the package boundary but is **not charged to
Joint A or to the HOL composite `(A1-1)`**.

### 1.3 Addressed response and Joint-I arrow

At unaddressed scope the sealed relabelling is only

```text
O_w^resp:=O_R5,w,
iota_w^resp:O_R5,w->O_w^resp,
iota_w^resp(Y):=Y.                                             (A1-15)
```

It neither constructs nor inverts the addressed restriction.  At the
consumer-minimal level, a direct continuous linear

```text
Ker_(a,K):O_a^resp->D_N^Loc                                    (A1-15m)
```

is sufficient only when it is physically anchored.  The smallest retained
agreement witness has the form

```text
Res_(w,a)^resp:O_w^resp->O_a^resp,
Res_(w,a)^resp continuous, linear, and K-independent,

S_(w->N,K):O_w^resp->D_N^ker,
S_(w->N,K) continuous and linear,

im(S_(w->N,K)) subset D_N^Loc,

Ker_(a,K) Res_(w,a)^resp
 =corestr_(D_N^Loc)(S_(w->N,K)).                              (A1-18m)
```

This prevents the vacuous zero-map/signature cheat.  `JII` gives the following
stronger quotient-factor/extension package as a sufficient way to witness
`(A1-18m)`.  A member of that witness package must supply

```text
Res_(w,a)^resp:O_w^resp->O_a^resp
 continuous, linear, K-independent,

res_(w,a)^supp:Supp_w->Supp_a,

supp_a(Res_(w,a)^resp(Y))
 subset res_(w,a)^supp(supp_w(Y)),

QuotResCert_(w,a):Res_(w,a)^resp is quotient onto its image,   (A1-16)

S_(w->N,K):O_w^resp->D_N^ker
 continuous and linear,

T_(w->N,K)^D:D_w^ker->D_N^ker
 continuous and linear,

t_(w->N,K)^supp:Supp_w^ker->Supp_N^ker.                        (A1-17)
```

The quotient factor and full addressed extension are

```text
ker(Res_(w,a)^resp) subset ker(S_(w->N,K)),

Ker_(a,K)^im:im(Res_(w,a)^resp)->D_N^ker,

Ker_(a,K)^im(Res_(w,a)^resp(Y)):=S_(w->N,K)(Y),

Ker_(a,K)^im Res_(w,a)^resp=S_(w->N,K),                        (A1-18)

Ker_(a,K)^ker:O_a^resp->D_N^ker,
Ker_(a,K)^ker|_(im Res_(w,a)^resp)=Ker_(a,K)^im,

im(Ker_(a,K)^ker) subset D_N^Loc,

Ker_(a,K):=corestr_(D_N^Loc)(Ker_(a,K)^ker)
 :O_a^resp->D_N^Loc.                                          (A1-19)
```

The stage source and addressed factor must be connected, not juxtaposed:

```text
S_(w->N,K)|_(iota_w^resp(O_R5,w^src))
 =T_(w->N,K)^D S_w (iota_w^resp)^(-1),                         (A1-20)

supp_N(T_(w->N,K)^D x)
 subset t_(w->N,K)^supp(supp_w(x)).                            (A1-21)
```

The whole two-address orbit is retained:

```text
[a]:={a,a^-},
mu(a^-)=-mu(a),
no address in [a] is selected as the positive representative.                (A1-21a)
```

Where a future G5/source-reversal member is admitted on a specifically typed
sign-odd domain, ExtSrc must respect that orbit by the conditional square

```text
O_(a,K)^odd subset O_a^resp,
O_(a^-,K^-)^odd subset O_(a^-)^resp,

r_(a,K)^O:O_(a,K)^odd->O_(a^-,K^-)^odd,
r_N^D:D_N^(Loc,odd)->D_N^(Loc,odd),

Ker_(a,K)(O_(a,K)^odd) subset D_N^(Loc,odd),

r_N^D Ker_(a,K)|_(O_(a,K)^odd)
 =Ker_(a^-,K^-) r_(a,K)^O.                                   (A1-21b)
```

`SEAM2` builds the carried orbit, not `r_N^D`, `r_(a,K)^O`, or the displayed
square.  No global reversal outside the admitted sign-odd domain is asserted.

### 1.4 S22/S22a, followed by the stronger family carriage

The consumer-minimal common portion of S22/S22a is exactly

```text
etahat_f:D_N^Loc->D_M^Loc,                                    (A1-24)

etahat_id=id_DLoc,
etahat_(g compose f)=etahat_g etahat_f.                        (A1-25)
```

The simultaneous `j_f^Sym` laws are H-tail data and are not charged to this
round.  `S23`--`S25` are also outside the three named consumer-minimal
interfaces.

The consolidated `JII` realization route defines `etahat_f:=tau_f^Loc` and
ties it to the physical source by the stronger source, ambient-kernel, and
addressed squares

```text
tau_f^src:O_R5,N^src->O_R5,M^src,
tau_f^D:D_N^ker->D_M^ker,
tau_f^a:O_a^resp->O_(f.a)^resp,
tau_f^K:Kpar_N->Kpar_M,
tau_f^K(K)=:f.K,

tau_f^D S_N=S_M tau_f^src,
tau_f^D(D_N^Loc) subset D_M^Loc,

tau_f^D Ker_(a,K)^ker
 =Ker_(f.a,f.K)^ker tau_f^a,                                  (A1-22)

tau_f^Loc
 :=corestr_(D_M^Loc)(tau_f^D|_(D_N^Loc))
 :D_N^Loc->D_M^Loc,

tau_f^Loc Ker_(a,K)
 =Ker_(f.a,f.K) tau_f^a.                                     (A1-23)
```

with `etahat_f:=tau_f^Loc`.  Once the generator assignment has been proved to
extend to a functor on the presented arrow category, a common-refinement
relation is a **derived regression** of S22a:

```text
g_1 f_1=g_2 f_2
 =>etahat_g1 etahat_f1
   =etahat_(g_1 f_1)
   =etahat_(g_2 f_2)
   =etahat_g2 etahat_f2.                                      (A1-26)
```

The debt is not an independent common-refinement axiom.  It is construction
of the generator maps plus proof that their finite-pasting assignment respects
the presented relations, so that the functor law is well defined on the
generated closure.  Two displayed generator squares alone do not prove that.

For cycle creation `h:N->M`, only the downward old image is legal:

```text
rho_h^src:O_R5,M^(src,old)->O_R5,N^src,
rho_h^D:D_M^(ker,old)->D_N^ker,
rho_h^a:O_(a_M^old)^resp->O_(a_N)^resp,
rho_h^K:Kpar_M^old->Kpar_N,
K_M in Kpar_M^old,
K_N:=rho_h^K(K_M),

D_M^(Loc,old):=D_M^Loc intersection D_M^(ker,old),

rho_h^D(D_M^(Loc,old)) subset D_N^Loc,

rho_h^Loc
 :=corestr_(D_N^Loc)(rho_h^D|_(D_M^(Loc,old)))
 :D_M^(Loc,old)->D_N^Loc,                                    (A1-27)

Ker_(a_M^old,K_M)^ker(O_(a_M^old)^resp)
 subset D_M^(ker,old),

Ker_(a_M^old,K_M)(O_(a_M^old)^resp)
 subset D_M^(Loc,old),

rho_h^D S_M|_(O_R5,M^(src,old))=S_N rho_h^src,

rho_h^Loc Ker_(a_M^old,K_M)
 =Ker_(a_N,rho_h^K(K_M)) rho_h^a.                             (A1-28)
```

There is no upward map on the target-new-cycle complement.

### 1.5 Where the `A_G` near-miss would have to sit

[PROVABLE] For each actual stage `N`, let `B_N=(M_N,g_N,...)` denote its
sealed physical background and `G_N` its sealed finite-graph realization.  Set

```text
T_N:=Omega_c^1(M_N;iR),
K_N^fin:=K_(G_N)^fin,

<J_(N,c),a>
 :=sum_e c_e integral_(gamma_(N,e)) a,

A_N:=A_(G_N):T_N->(K_N^fin)^*,
(A_N a)(c):=<J_(N,c),a>,                                      (A1-29)

A_N is onto.                                                   (A1-30)
```

This is `KER`'s generic `A_G` at the graph `G=G_N`; it is an actual evaluation
map, not a declaration.  The notation is fiberwise over every admitted sealed
realization `(B_N,G_N)`; no background, graph representative, or member is
selected or bound here.  For an admitted stage arrow `f:N->M`, write
`G_M=f.G_N` for its sealed graph transport and `A_M:=A_(G_M)`.  To use the
evaluation as an ExtSrc
component, its domain would have to be reached from the physical response
source and its codomain would have to reach the common kernel carrier:

```text
O_R5,N^src
 --b_N--> T_N
 --A_N--> (K_N^fin)^*
 --e_N--> D_N^ker.                                             (A1-31)
```

The resulting candidate would be

```text
S_N^AG:=e_N A_N b_N:O_R5,N^src->D_N^ker                        (A1-32)
```

and would have to satisfy, not replace, the exact agreement

```text
e_N A_N b_N(qtilde_N(a,b,C))
 =a Sch_N(M_N^loc)
  +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                 (A1-33)
```

It would also have to satisfy the consumer-minimal image, physical-agreement,
and functor laws `(A1-12)`, `(A1-13)`, `(A1-15m)`, `(A1-18m)`, `(A1-24)`, and
`(A1-25)`.  A claim to inhabit the longer `JII` witness would additionally
owe `(A1-16)`--`(A1-23)` and `(A1-26)`--`(A1-28)`.  Thus the near-miss would
sit before `S_N`, not in place of its response, topology, image, or transport
obligations; it is not charged with the H-tail condition `(A1-14)`.

## 2. A2 — assembly-first piece ledger

Every piece used below is listed before testing a joint.

| # | Piece and exact type | Status |
|---:|---|---|
| 1 | `E_N=R L_T,N direct-sum R Rhat_K,N direct-sum S_(1,sa)(K_N)` | `SEALED` carrier presentation |
| 2 | `qtilde_N:E_N->O_R5,N`; `I_N=im(qtilde_N)` | `SEALED` |
| 3 | `Q_N^quot:E_N->>O_prof,N`; `Rel_N=closure ker(qtilde_N)` | `SEALED` quotient presentation |
| 4 | `I_N^q`, `I_N^R5`, `Q_N^core`, `jmath_N` | `SEALED` as separately topologized carriers/arrows |
| 5 | `beta_N:O_prof,N->I_N^q` | `PART-PROVABLE` under the displayed kernel condition |
| 6 | `kappa_N:I_N^q->I_N^R5` | underlying-set identity `SEALED`; continuity/embedding `REQUIRED` |
| 7 | `O_w^resp:=O_R5,w`; `iota_w^resp=id` after codomain relabelling | `SEALED` unaddressed only |
| 8 | `Kernbar_N^cyc:O_N^cyc->Ker_N^Q408` | `SEALED` cycle/profile branch |
| 9 | `T_N`, currents `J_(N,c)`, and `A_N=A_(G_N):T_N->(K_N^fin)^*` | `SEALED`; `A_N` onto |
| 10 | `D_N^ker` Hausdorff TVS | `REQUIRED`; no member |
| 11 | `iota_N^Q408:Ker_N^Q408->D_N^ker` continuous injective | `REQUIRED`; no member |
| 12 | `Sch_N:Op_N^(loc,2)->D_N^ker` continuous injective | `REQUIRED`; no member |
| 13 | `S_N:O_R5,N^src->D_N^ker` and `Kraw_N=S_N qtilde_N` | `REQUIRED`; no member |
| 14 | `Sbar_N:O_prof,N->D_N^ker` | automatic only after row 13 |
| 15 | `D_N^Loc=im(Kraw_N)=im(Sbar_N)` with topology/embedding | `REQUIRED`; no certificate |
| 16 | `Res_(w,a)^resp` and `S_(w->N,K)` satisfying minimal `(A1-18m)` | `REQUIRED` physical witness; no members |
| 17 | support, quotient certificate, `T_(w->N,K)^D,t_(w->N,K)^supp` | stronger `JII` witness route; no members |
| 18 | `Ker^im`, `Ker^ker`, `Ker:O_a^resp->D_N^Loc` | stronger `JII` witness route; direct `Ker` still uninhabited |
| 19 | nonidentity `etahat_f` and S22/S22a identity/composition; full `tau` witness route | `REQUIRED`; no nonidentity family member |
| 20 | functor-extension/relation-respect proof and cycle-creating downward `rho` family | later/full ExtSrc carriage; no new-cycle lift allowed |
| 21 | `b_N:O_R5,N^src->T_N` | `REQUIRED` only for the `A_G` attempt; not sealed |
| 22 | `e_N:(K_N^fin)^*->D_N^ker` | `REQUIRED` only for the `A_G` attempt; not sealed |
| 23 | explicit ExtSrc and stage-indexed `A_N`-factor unit torsors | `[YOURS]` required candidate coordinates; no representative selected |
| 24 | whole `AddrOrb_N` with `mu(a^-)=-mu(a)`; conditional sign-odd square `(A1-21b)` | orbit `SEALED`; reversal maps/square `REQUIRED`, uninhabited |
| 25 | `Tbar_N=T_N/ker(A_N)`, `q_N^test:T_N->>Tbar_N`, `Abar_N:Tbar_N isomorphic_to (K_N^fin)^*` | `SEALED` finite surface-visible quotient |
| 26 | `R_K,N^fin:K_N^fin isomorphic_to (K_N^fin)^*`; `sigma_cyl,N:Hom(K_N^fin,(K_N^fin)^*)->D'_biloc,N` | `SEALED` finite only; completion `TYPE-U` |
| 27 | `P_alg,f:Tbar_N->Tbar_M` | built on actual finite/`Ref_path` core; conditional beyond; physical use requires `OLD_FID/LR/RNL` as scoped |
| 28 | `j_N^fin:K_N^fin->K_N`, its dual, and compressed `C_fin` | `SEALED` inclusion/finite restriction operations |
| 29 | `I_K,N:K_N^fin->J_cyc,N` | `SEALED` finite current injection |
| 30 | `ell_N^(cyc->ker):J_cyc,N->D_N^ker`; `i_cyl,N:D'_biloc,N->D_N^ker` | Round-2 candidate landings; not sealed |

No declaration is promoted to a member in this ledger.

## 3. A2 — inhabitance attempt, joint by joint

### 3.1 Joint A-I — direct source formula on the physical image

First expose the algebraic condition hidden by `(A1-9)`.  If the required
target pieces existed, define on the raw source

```text
F_N:E_N->D_N^ker,

F_N(a,b,C)
 :=a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)).                 (A2-1)
```

For `S_N^I(qtilde_N z):=F_N(z)` to be a function on the underlying physical
image `I_N`, it is necessary and sufficient that

```text
qtilde_N z=qtilde_N z'
 =>F_N(z)=F_N(z'),                                              (A2-2)

equivalently,

r in ker(qtilde_N)
 =>F_N(r)=0.                                                    (A2-3)
```

Writing `r=(a,b,C)` makes the required mixed-relation certificate explicit:

```text
qtilde_N(a,b,C)=0
 =>a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C))=0.              (A2-4)
```

No sealed equality proves `(A2-4)`.  More basically, rows 10--12 of the piece
ledger are required declarations: sealed stock has not put the two terms of
`(A2-1)` into one constructed Hausdorff carrier.  Formally declaring a direct
sum to make them composable would be an organizing rail; it would not prove
the physical mixed-relation, image, support, or transport laws.

```text
JOINT_A-I = STOPPED
gap A-I.1 = COMMON_DKER/SCH/IOTA_MEMBER
gap A-I.2 = MIXED_R5_RELATION_CERTIFICATE (A2-4)
```

### 3.2 Joint A-II — inherited topology and source scope

Independently grant `(A2-4)` to test the next joint.  The smallest permitted
scope option is

```text
O_R5,N^src:=I_N^R5,

S_N^I:I_N^R5->D_N^ker,
S_N^I(qtilde_N z):=F_N(z).                                     (A2-5)
```

Continuity of the factor from the quotient topology would establish

```text
S_N^q:I_N^q->D_N^ker,                                         (A2-6)
```

but the consumed domain in `(A2-5)` has the inherited R5 topology.  The
underlying-set identity

```text
kappa_N:I_N^q->I_N^R5                                         (A2-7)
```

has no sealed continuity/topological-embedding certificate.  Nor is every
response consumed by `(A1-20)` exhibited in `I_N^R5`.  Therefore choosing
`O_R5,N^src=I_N^R5` avoids a larger extension domain only conditionally; it
does not produce the required continuous map.

Even after a continuous `S_N`, the package must prove

```text
D_N^Loc=im(Kraw_N)=im(Sbar_N),

quotient topology on D_N^Loc
 =topology induced by its embedded inclusion in D_N^ker.       (A2-8)
```

Continuity alone does not prove `(A2-8)`.

```text
JOINT_A-II = STOPPED
gap A-II.1 = KAPPA_TOPOLOGY/CONTINUITY_CERTIFICATE
gap A-II.2 = SOURCE_SCOPE_COVERAGE
gap A-II.3 = DLOC_IMAGE_TOPOLOGY/EMBEDDING_CERTIFICATE
```

The automatic descent `(A1-7)`--`(A1-8)` remains valid conditionally and is
not relabelled as a gap.

### 3.3 Joint A-III — addressed `Res/Ker` factor

Independently grant a stage source extension to test the addressed joint.  No
sealed map

```text
Ker_(a,K):O_a^resp->D_N^Loc                                    (A2-8m)
```

comes with a physical agreement of the form `(A1-18m)`.  Thus the
consumer-minimal addressed joint already stops; a bare zero map cannot count.
The longer `JII` witness is then tested to learn whether it closes that stop.

If
`Res_(w,a)^resp` and `S_(w->N,K)` existed and obeyed

```text
ker(Res_(w,a)^resp) subset ker(S_(w->N,K)),                    (A2-9)
```

then the quotient universal property would define

```text
Ker_(a,K)^im(Res_(w,a)^resp(Y)):=S_(w->N,K)(Y),                (A2-10)
```

and every representative equality would be

```text
Res(Y)=Res(Y')
 =>Y-Y' in ker(Res)
 =>S(Y-Y')=0
 =>Ker^im(Res(Y))=Ker^im(Res(Y')).                             (A2-11)
```

This is the lawful factorization.  Sealed stock supplies neither the
addressed `Res`, `(A2-9)`, its quotient certificate, nor the required
continuous extension

```text
Ker_(a,K)^ker:O_a^resp->D_N^ker,
Ker_(a,K)^ker|_(im Res)=Ker_(a,K)^im,
im(Ker_(a,K)^ker) subset D_N^Loc.                              (A2-12)
```

Choosing a complement of `im(Res)` would select extra structure and is not
licensed by a general continuous-extension theorem in the sealed stock.
The carried orbit `(A1-21a)` supplies both addresses without a chosen sign,
but neither `Ker` nor the conditional reversal members exist, so the
sign-odd compatibility square `(A1-21b)` is also unformed.

```text
JOINT_A-III = STOPPED
gap A-III.0 = DIRECT_ADDRESSED_KER/PHYSICAL_AGREEMENT
gap A-III.1 = ADDRESSED_RES/SUPPORT/QUOTIENT_MEMBER
gap A-III.2 = KERNEL_INCLUSION (A2-9)
gap A-III.3 = FULL_ADDRESSED_CONTINUOUS_EXTENSION (A2-12)
gap A-III.4 = ADDRESSED_ORBIT_REVERSAL_SQUARE (A1-21b)
```

### 3.4 Joint A-IV — S22/S22a and old-image transport

At the consumer-minimal level, sealed stock supplies no nonidentity maps

```text
etahat_u:D_(N_RL)^Loc->D_(M_RL)^Loc,
etahat_rnm:D_(N_n)^Loc->D_(N_m)^Loc.                           (A2-12m)
```

Consequently the S22 family is not inhabited and the S22a identity/composition
formula cannot bootstrap it; `etahat_id=id` is only the identity case.

Independently grant the objectwise maps to test the stronger `JII` family
carriage.  The actual
reciprocal generator `u:N_RL->M_RL` and the nontrivial refinement
`r_nm:N_n->N_m` would each have to satisfy

```text
tau_u^D S_N=S_M tau_u^src,
tau_u^Loc Ker_(a,K)=Ker_(u.a,u.K) tau_u^a,                     (A2-13)

tau_rnm^D S_N=S_M tau_rnm^src,
tau_rnm^Loc Ker_(a,K)=Ker_(r_nm.a,r_nm.K) tau_rnm^a.           (A2-14)
```

Only then could identities and composition start the displayed induction

```text
tau_id^Loc=id,
tau_(g compose f)^Loc=tau_g^Loc tau_f^Loc,                     (A2-15)

g_1 f_1=g_2 f_2
 =>tau_g1^Loc tau_f1^Loc
   =tau_(g_1 f_1)^Loc
   =tau_(g_2 f_2)^Loc
   =tau_g2^Loc tau_f2^Loc.                                    (A2-16)
```

No actual nonidentity ExtSrc member instantiates `(A2-13)` or `(A2-14)`, so
the generator assignment and its functor-extension/relation-respect proof in
`(A2-15)`--`(A2-16)` cannot start.  For cycle creation the
downward equations `(A1-27)`--`(A1-28)` likewise remain types, not members.

```text
JOINT_A-IV = STOPPED
gap A-IV.0 = NONIDENTITY_ETAHAT_MEMBERS (A2-12m)
gap A-IV.1 = RECIPROCAL_EXTSRC_BASE
gap A-IV.2 = Q2_REFINEMENT_EXTSRC_BASE
gap A-IV.3 = FUNCTOR_EXTENSION/RELATION_RESPECT
gap A-IV.4 = CYCLE_CREATING_OLD_IMAGE_MEMBER
```

### 3.5 The explicit `A_G` bridge joint

The candidate composite is `(A1-31)`--`(A1-33)`.  Its first formation step
already fails:

```text
domain(A_N)=T_N=Omega_c^1(M_N;iR),
domain required by S_N=O_R5,N^src,

O_R5,N^src != T_N by type,
b_N:O_R5,N^src->T_N is not sealed.                             (A2-17)
```

Its landing also fails:

```text
codomain(A_N)=(K_N^fin)^*,
codomain required by S_N=D_N^ker,

(K_N^fin)^* != D_N^ker by type,
e_N:(K_N^fin)^*->D_N^ker is not sealed.                        (A2-18)
```

The review's sector-not-scope result is visible directly: replacing
`O_R5,N^src` by a smaller support subspace leaves its elements R5
profiles/responses, while `T_N` remains a space of compactly supported
connection one-forms at the same `B_N`.  A support restriction cannot
discharge `(A2-17)`.

Surjectivity does not reverse the arrow:

```text
A_N onto
 =>for every lambda in (K_N^fin)^*,
   there exists a in T_N with A_N(a)=lambda,                    (A2-19)

A_N onto
 does_not_imply a sealed, canonical, continuous, linear,
 support-compatible natural section
s_N^(sec):(K_N^fin)^*->T_N.                                   (A2-20)
```

The surface-visible quotient gives a basis-free isomorphism

```text
Tbar_N:=T_N/ker(A_N),
q_N^test:T_N->>Tbar_N,

Abar_N:Tbar_N isomorphic_to (K_N^fin)^*,
Abar_N q_N^test=A_N.                                          (A2-21)
```

but `(A2-21)` supplies no representative lift to `T_N`, no map from
`O_R5,N^src` into either side, and no map from either side into `D_N^ker`.
A chosen right inverse would select test-form representatives and would still
leave `(A2-18)` open.

The existing bilocal formula does not help:

```text
R_K,N^fin:=R_K,G_N:K_N^fin isomorphic_to (K_N^fin)^*,

Kern_N^fin(H)[a,b]
 =H((R_K,N^fin)^(-1)A_N a,(R_K,N^fin)^(-1)A_N b).              (A2-22)
```

It consumes two test forms; it does not map a profile `H` to one test form or
to an ExtSrc kernel element.  Freezing either argument would select new data,
and using the same argument in both slots would be quadratic rather than the
required linear `S_N`.

The closest lawful use must first be restricted to the finite cylindrical
scope on which `sigma_cyl` is sealed.  Write

```text
z_fin=(a,b,C_fin),
j_N^fin:K_N^fin->K_N,
j_N^(fin,*):K_N^*->(K_N^fin)^*,

C in S_(1,sa)(K_N),
C_fin:=j_N^(fin,*) C j_N^fin
 in Hom(K_N^fin,(K_N^fin)^*),

D'_biloc,N
 :=D'(M_N x M_N;T^*M_N boxtimes T^*M_N),

sigma_cyl,N:=sigma_cyl,G_N:
 Hom(K_N^fin,(K_N^fin)^*)->D'_biloc,N,

pr_H^fin(z_fin):=b R_K,N^fin+C_fin.                            (A2-22a)
```

This does not identify the raw R5 generator `Rhat_K,N` with the metric Riesz
map.  The sealed source presentation is

```text
qtilde_N(a,b,C)
 =a L_T,N+j_R4,N(b R_K,N+C),                                  (A2-22a')
```

so `j_R4,N` dresses the undressed profile `b R_K,N+C` on entry to `O_R5,N`.
`sigma_cyl,N` consumes that profile operator on the finite carrier.

Then the sealed finite realization is

```text
sigma_cyl,N(pr_H^fin z_fin)[u,v]
 :=(b R_K,N^fin+C_fin)
   ((R_K,N^fin)^(-1)A_N u,(R_K,N^fin)^(-1)A_N v),              (A2-22b)
```

which lands in the path-bilocal distribution carrier and consumes the two
independent test legs `u,v`.  On the finite `L_T` generator,

```text
z_T^fin:=(1,0,0),
pr_H^fin(z_T^fin)=0
 =>sigma_cyl,N(pr_H^fin(z_T^fin))=0
   in D'_biloc,N.                                               (A2-22c)
```

The codomains are not identified.  If a further **hypothetical** linear
landing existed,

```text
i_cyl,N:D'_biloc,N->D_N^ker,

i_cyl,N sigma_cyl,N(pr_H^fin(z_T^fin))=i_cyl,N(0)=0.           (A2-22d)
```

ExtSrc separately requires

```text
Kraw_N(1,0,0)=Sch_N(M_N^loc)=kappa_T,N.                        (A2-22e)
```

Equating the hypothetical finite landing with `(A2-22e)` would require the
additional statement `kappa_T,N=0`, which is not sealed and is not assumed or
evaluated.  The finite construction therefore supplies only `KER`'s
path-bilocal cylindrical image.  Its equality with
`iota_N^Q408 Kernbar_N^cyc(...)` in the common `D_N^ker` is also unproved.
The completed-current to distribution embedding is independently
`NOT_BUILT / TYPE-U` in `KER` `(B2-16)`, so the finite formula cannot be
extended silently to the completed `E_N` source.

Finally, even hypothetical `b_N,e_N` must prove

```text
tau_f^D e_N A_N b_N
 =e_M A_M b_M tau_f^src,                                      (A2-23)

supp_X^ker:D_X^ker->P(Supp_X^ker) for X in {N,M},
tau_f^supp:Supp_N^ker->Supp_M^ker,
tau_f^(supp,*)(S):={tau_f^supp(s):s in S},

supp_M^ker(e_M A_M b_M tau_f^src(Y))
 subset tau_f^(supp,*)
          (supp_N^ker(e_N A_N b_N(Y))).                        (A2-24)
```

as well as `(A1-33)`.  None is sealed.

```text
AG_BRIDGE_TEST = STOPPED
gap AG.1 = O_R5_SOURCE_TO_TEST_FORM_MAP b_N
gap AG.2 = CYCLE_DUAL_TO_DKER_MAP e_N
gap AG.3 = SOURCE_AGREEMENT (A1-33)
gap AG.4 = SUPPORT/NATURALITY (A2-23)--(A2-24)
gap AG.5 = FINITE_CYLINDRICAL_SCOPE/COMMON_DKER_LANDING;
           L_T generator would additionally require unproved kappa_T,N=0
```

This is a failure to derive a bridge from sealed stock, not a theorem that no
abstract bridge can exist.

### 3.6 Cross-sector-unit declaration

DoR-019 and Q-523 require every cross-sector supplier to make its unit visible.
DoR-019 fixes the dual relation

```text
U_Kstar,N:=U_K,N^(-1).                                         (A2-25a)
```

[YOURS — required candidate torsors, not DoR-019-derived] Let

```text
U_src,N, U_T,N, U_Dker,N                                       (A2-25b)
```

denote the unselected unit torsors carried by a future source/test/kernel
candidate.  Every future ExtSrc candidate is
indexed by, and must display, a unit arrow in the class

```text
U_Ext,N:=U_Dker,N U_src,N^(-1),

[S_N]=U_Ext,N.                                                  (A2-26)
```

The candidate family ranges over all lawfully declared unit arrows of this
class; this artifact selects and binds none.  If the stage-indexed `A_N`
factor is used, its
three displayed classes must be

```text
[b_N]  =U_T,N U_src,N^(-1),
[A_N]  =U_Kstar,N U_T,N^(-1),
[e_N]  =U_Dker,N U_Kstar,N^(-1),

[e_N A_N b_N]
 =U_Dker,N U_Kstar,N^(-1)
  U_Kstar,N U_T,N^(-1)
  U_T,N U_src,N^(-1)
 =U_Dker,N U_src,N^(-1)
 =U_Ext,N.                                                      (A2-27)
```

The middle class `[A_N]=[A_(G_N)]` is the native unit class of the already sealed
current/test evaluation pairing.  In `sigma_cyl`, `j_R4` is the sole
action-unit dressing of the profile operator `H` before realization (`KER`
`(B2-14)`); `A_N` itself is not re-described as an R4 dressing.  The newly
declared obligations are the adjacent `b_N`, `e_N`, and direct `S_N` seams.

The direct formula also requires

```text
[Sch_N(M_N^loc)]
 =[iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C))]
 =U_Dker,N                                                     (A2-28)
```

before its terms may be added.  No factor in `(A2-26)`--`(A2-28)` is silently
set to `1`, called dimensionless, or tuned to a downstream quantity.  The unit
declaration is an obligation on candidates, not a construction of `b_N`,
`e_N`, or `S_N`.

### 3.7 Round-1 determination

```text
DIRECT_SOURCE_FORMULA = STOPPED at A-I/A-II
ADDRESSED_FACTOR       = STOPPED at A-III
S22/S22a FAMILY        = STOPPED at A-IV
A_G/A_N COMPONENT      = STOPPED at AG.1--AG.5

no ExtSrc member is derivable by this Round-1 assembly;
not proved: family(ExtSrc_N^JA)=emptyset.                        (A2-29)
```

## 4. A3 — consequence and timebox board

### 4.1 Route consequences

```text
Gamma^H Joint I = STOPPED
  because Ker_(a,K):O_a^resp->D_N^Loc is unformed;

HOL common leg = STOPPED
  at the same Ker/D_N^Loc prefix;

S22/S22a = SPECIFIED, NOT INHABITED
  because no nonidentity tau_f^Loc member/base exists;

cycle-creating carriage = old-image type retained,
  member absent, no upward lift introduced;

A9 ExtSrc = OPEN;
A9 adoption is not ExtSrc inhabitance.                             (C1)
```

The H-only symbol/cochain tail and the HOL-only `Xi/G4-D/G5/FULL-G4/G2-N`
tail are unchanged.  No downstream tail is used to repair the common source.

### 4.2 Timebox status and standing fallback

```text
EXTSRC TIMEBOX = ROUND 1 OF 4 COMPLETE
ROUND-1 OUTCOME = HONEST STOP
MANDATORY PRINCIPAL CHECKPOINT = not yet triggered
CARRY-1 EXPLICIT INCOMPLETENESS = retained and available
HOL completion phase = not opened
reserved alternatives 1--3 = not opened
both A7 E_C branches = retained; none selected.                    (C2)
```

An honest stop is a successful Round-1 result under `SEQ`; it preserves the
typed families and makes no emptiness claim.

## 5. A3 — exact Round-2 plan

Round 2 remains ExtSrc-only.  It will try the following objects that Round 1
could type but could not derive.

### 5.1 Quotient rather than a selected `A_G` preimage

Use the canonical quotient isomorphism only:

```text
Tbar_N:=T_N/ker(A_N),
Abar_N:Tbar_N->(K_N^fin)^*.                                    (R2-1)
```

Attempt a covariant, support-compatible map

```text
bbar_N:O_R5,N^src->Tbar_N,                                     (R2-2)
```

not a representative-valued section.  On an arrow in Q408's sealed actual
finite/`Ref_path` core, the built test transport provides the covariance
target

```text
P_alg,f:Tbar_N->Tbar_M,

bbar_M tau_f^src=P_alg,f bbar_N.                               (R2-2b)
```

Outside that core, `P_alg,f` and `(R2-2b)` are conditional on the still-
proposed R1 path/current member; no global sealed transport is asserted.
Even on the constructible scope, physical use must pass Q408's failure-capable
`OLD_FID_f` and `LR_f` predicates, and `RNL_f` wherever relative no-leakage is
consumed; algebraic construction of `P_alg,f` alone is not a physical PASS.
Stop if `(R2-2)` or the correctly scoped `(R2-2b)` cannot be anchored on the
actual reciprocal and refinement sources.

### 5.2 Codomain route through sealed current/Riesz data

Test the longest already typed prefix

```text
I_K,N:K_N^fin->J_cyc,N,

(K_N^fin)^*
 --(R_K,N^fin)^(-1)--> K_N^fin
 --I_K,N--> J_cyc,N
 --ell_N^(cyc->ker)?--> D_N^ker.                                (R2-3)
```

Here `ell_N^(cyc->ker)?` names the exact missing current/kernel realization;
it is not invented by the notation and is not the cochain differential
`d_N`.  The prefix through `I_K,N` lands a one-current, not a bilocal kernel;
Round 2 must stop this route unless `ell_N^(cyc->ker)` supplies a lawful
arity-changing physical construction with its action, topology, support, unit
class, and transport.

If that last arrow were constructed, define only then

```text
e_N^(2)
 :=ell_N^(cyc->ker) I_K,N (R_K,N^fin)^(-1)
 :(K_N^fin)^*->D_N^ker,

S_N^(2)
 :=e_N^(2) Abar_N bbar_N
 :O_R5,N^src->D_N^ker.                                        (R2-3a)
```

Round 2 must test the assembled candidate against the full source agreement,
not merely its cycle leg:

```text
S_N^(2)(qtilde_N(a,b,C))
 ?=a Sch_N(M_N^loc)
   +iota_N^Q408 Kernbar_N^cyc(Q_N^quot(0,b,C)),                 (R2-3a')
```

and must display the corresponding `e^(2)`/`bbar` transport and support
squares.  The question mark is an obligation, not an asserted equality.

The arity-matched finite profile route is separately

```text
Hom(K_N^fin,(K_N^fin)^*)
 --sigma_cyl,N--> D'_biloc,N
 --i_cyl,N?--> D_N^ker.                                        (R2-3b)
```

Its last arrow and the `KER (B2-16)` completion seam are both stopped in
Round 1.  Round 2 may test them, but may not identify `D'_biloc,N` with
`D_N^ker` by notation or use the finite route on completed `E_N` without the
completion theorem.

### 5.3 Mixed-relation theorem on actual bottoms

Attack `(A2-4)` directly on the actual finite assembly relations:

```text
qtilde_N(z)=qtilde_N(z')
 =>F_N(z)=F_N(z'),                                              (R2-4)
```

first at `RL`, then on the nontrivial refinement, then through the admitted
finite-pasting induction.  No discrepancy may be quotiented away without an
independent physical-null theorem.

### 5.4 Topology and minimal source scope

Attempt the certificate

```text
kappa_N:I_N^q->I_N^R5
 continuous and a topological embedding,                       (R2-5)
```

or prove the exact consumer subscope on which

```text
O_R5,N^src=I_N^R5                                               (R2-6)
```

contains every response used by `(A1-20)`.  Then prove, rather than stipulate,
the image/subspace equality `(A2-8)`.

### 5.5 Addressed quotient and extension

Build `Res_(w,a)^resp` on a named actual address, prove

```text
ker(Res_(w,a)^resp) subset ker(S_(w->N,K)),                    (R2-7)
```

and use the quotient property to form `Ker^im`.  A full `Ker^ker` may count
only if a lawful continuous extension with image in `D_N^Loc` is displayed;
Round 2 will not select a complement.  If the conditional sign-odd reversal
members are available on a named domain, it must then execute `(A1-21b)` on
both addresses of the carried orbit; the built orbit alone is not the square.

### 5.6 Actual-arrow regressions

If the objectwise construction survives, execute `(A2-13)` and `(A2-14)` on
the reciprocal and `q=2` generators, then prove the generator assignment
extends to a functor respecting every admitted presented relation/finite
pasting, and display the cycle-creating downward old-image equations.

```text
ROUND_2 TARGET
 = quotient-domain bridge + current-to-kernel landing
   + mixed-relation theorem + topology certificate
   + addressed quotient/extension + actual-arrow bases.         (R2-8)
```

This plan is analysis, not adoption, member binding, or a promise that the
listed arrows exist.

## 6. Mandatory SURFACE ANCHOR

### 6.1 Pass criterion and named objects

A formal quotient, direct sum, universal property, or generic commuting
square is an organizing rail.  An ExtSrc surface `PASS` requires the member
and equality on the named actual stage, cycle, address, arrow, or certified
diamond.

| Actual anchor | Display required for an ExtSrc PASS | Round-1 result |
|---|---|---|
| reciprocal stage `RL` | `(A1-10)` and `(A1-11)` with `N=RL` | `STOPPED`; `S_RL`, `Sch_RL`, and the common landing are not members |
| actual Q-408 reciprocal kernel | its image in the same `D_RL^ker`, then in `D_RL^Loc`, with topology/support | `STOPPED`; the Q-408 kernel is sealed input, not an ExtSrc landing |
| reciprocal-stage `A_RL:=A_(G_RL):T_RL->(K_RL^fin)^*` | `(A1-29)` on the named reciprocal paths/currents | `SEALED GEOMETRY`; does not pass `(A1-31)` |
| actual address `a_RL` and reverse | `Res`, quotient certificate, `(A2-9)`, `Ker_(a_RL,K)`, and conditional `(A1-21b)` on any admitted odd domain | `STOPPED`; orbit built, addressed/reversal members absent |
| reciprocal generator `u:N_RL->M_RL` | both equalities in `(A2-13)` | `STOPPED`; no objectwise member/base |
| nontrivial `q=2` refinement `r_nm` | both equalities in `(A2-14)` | `STOPPED`; no objectwise member/base |
| cycle-creating arrow | downward old-image equations `(A1-27)`--`(A1-28)` | typed only; no upward lift |
| `D-sub`, `D-disj`, `D-contact` | ExtSrc equations on each named prototype | geometry exists; no realization members |
| generated `D_cert` | covariance-orbit, identity, transpose, certified whiskering, finite-pasting induction | no closure-wide ExtSrc induction |
| consumer-only and unresolved manifest arrows | explicit maps/equalities on each exhibit | not silently deleted; unbuilt boundary retained |

No generic formula is counted as a surface `PASS`.

### 6.2 Geometry versus rails

**Physical geometry already present:** actual paths `gamma_e`, currents `J_c`,
the stage-indexed compactly supported test-form spaces `T_N`, the constructed
onto maps `A_N=A_(G_N)`,
the Q-408 cycle/profile kernel, `L_T`, actual response stages and supports,
and the named reciprocal/refinement/cycle-creating arrows.

**Organizing rails awaiting members:** `O_R5,N^src`, `D_N^ker`, `D_N^Loc`,
`Sch_N`, `iota_N^Q408`, `S_N/Sbar_N`, addressed `Res/Ker`, `b_N`, `e_N`,
their topologies, unit coordinates, supports, and transports.

The `A_N` row is genuine surface geometry.  It cannot make either adjacent
rail in `(A1-31)` exist.

### 6.3 R9 quantification check

The repaired falsifier remains exactly

```text
for every common physical cell c
on which both typed period-return constructions are formed,

Bbar^H(c)=Bbar^Hol(c).                                         (R9-1)
```

There is no independent quantifier over `Triv_[a]`, a HOL scalarization
family, or their product.  Round 1 forms neither ExtSrc nor either full route
return.  Even successful ExtSrc would form only their common prefix, not the
two returns.

```text
R9_STATUS = UNEXECUTABLE/PENDING
R9_QUANTIFIER_DOMAIN_ELEMENT = none
R9_PASS = not asserted
R9_FAILURE = not asserted
R9_VACUOUS_TRUTH = not asserted.                                (R9-2)
```

R9 is not a premise of any attempted construction above.

## 7. Mandatory dependency re-audit

Every argument depending on a stated or attempted clause was re-walked after
the Round-1 stop.

| Root clause or attempted datum | Re-audited dependents | Result |
|---|---|---|
| raw source formula `(A2-1)` | mixed relations, `S_N`, `Kraw_N`, `kappa_T,N` | no map claimed before `(A2-4)` |
| source scope `O_R5,N^src` | every unaddressed response consumed by `(A1-20)` | minimal `I_N^R5` option remains conditional; no scope shrink adopted |
| common `D_N^ker` | `Sch`, `iota`, the sum, `D_N^Loc`, both route domains | formal direct sum rejected as surface inhabitant |
| `S_N` continuity | automatic descent, `Sbar_N`, image topology | descent retained only conditionally; not double-counted |
| `D_N^Loc` topology | `Kernbarhat_N`, `Ker` codomain, `tau/rho` corestrictions | all remain unformed |
| addressed `Res/Ker` | Gamma-H Joint I and HOL common prefix | both routes remain stopped at the same arrow |
| addressed orbit `(A1-21a)`--`(A1-21b)` | no-sign selection, conditional G5/source reversal compatibility | whole orbit retained; reversal maps/square uninhabited |
| stage-indexed `A_N` (`A_G` generically) bridge | domains, codomains, units, support, covariance, source agreement | onto property not promoted to a section or ExtSrc member |
| unit declaration `(A2-26)`--`(A2-28)` | every candidate cross-sector composite | candidate class visible; no unit representative or scale selected |
| rank-preserving `tau` | S22/S22a, reciprocal/refinement bases, presented relations | no functor extension/relation-respect proof without actual bases |
| cycle-creating `rho` | A6 old-image carriage | downward scope retained; target-new-cycle lift absent |
| any Round-1 stop | H and HOL tails, seed, coefficient face, threshold | no downstream consequence inherited |
| both A7 branches | all future route families | both positively retained; nonselection alone is not substituted for carriage |
| R9 `(R9-1)` | first common-cell falsifier | remains pending; not used to tune a source member |
| standing fallback | timebox status | explicit incompleteness remains available without declaring family emptiness |

Permanent regressions rerun:

1. quotient descent was not called a source-extension member;
2. the built stage maps `A_N=A_(G_N)` were not retyped by notation;
3. onto was not read as a canonical right inverse;
4. a generic direct sum was not counted as physical `D_N^ker`;
5. no addressed complement or test-form representative was selected;
6. no route return or R9 equality entered the source construction.

## 8. A4 — battery

### 8.1 F_PLDEC and false-anchor regression

The only dependency direction used was

```text
actual paths/currents/supports
 + raw qtilde/Q quotient data
 + Q-408 Kernbar
 + actual L_T/local operator declaration
 + DoR-019 metric and unit classes
 + sealed stage-indexed `A_N=A_(G_N)` evaluation maps
 -> attempted ExtSrc source/response member.                    (B1)
```

The following were absent from every defining line:

```text
reader_f, chi, reader T, desired coefficient,
pi_Mx compose Loc compose Kernbar compose Q = 1,
seed polarity or nonvanishing,
Xi/HOL output, R9 equality,
threshold, fixed point, end test,
numeric value, measured constant.                               (B2)
```

In particular, no `A_N` preimage was chosen from a desired period or return.

### 8.2 Anti-tuning and no-selection ledger

| Attack | Check | Result |
|---|---|---|
| set a cross-sector conversion scale to `1` or call it dimensionless | unit classes `(A2-26)`--`(A2-28)` retained symbolically | clean |
| tune the cross-sector unit/normalization to a coefficient or R9 equality | no downstream quantity consumed; no unit representative selected | clean |
| identify `O_R5,N^src=T_N` by notation | rejected at `(A2-17)` | clean |
| identify `(K_N^fin)^*=D_N^ker` by notation | rejected at `(A2-18)` | clean |
| choose a right inverse or representative for onto `A_N=A_(G_N)` | rejected at `(A2-19)`--`(A2-21)` | clean |
| choose `O_R5,N^src=I_N^R5` merely to avoid extension | kept conditional on continuity and consumer coverage | clean |
| quotient away a mixed relation | `(A2-4)` retained as failure-capable theorem debt | clean |
| choose a direct-sum `D_N^ker` to force both branches to land | classified as rails, not a physical member | clean |
| select a complement to extend `Ker^im` | no complement chosen | clean |
| choose address or A7 branch | whole addressed orbit and both branches retained | clean |
| choose frame, gauge, basis, topology, smoothing, or boundary condition | none selected | clean |
| delete hard arrows or consumer-only scope | unresolved manifest boundary retained | clean |
| create an upward new-cycle lift | only downward old-image equations used | clean |
| use reader, fixed point, end test, numeric or measured data | none used | clean |

The cross-sector-unit row is explicit and failure-capable: any future candidate
that cannot display `(A2-26)`--`(A2-28)` is outside this family.

### 8.3 Fence and obstruction board

```text
FENCE_BLOCKED_STRUCTURAL_RESULT = false

TYPE-U GAPS =
  common Dker/Sch/iota member;
  mixed-R5-relation certificate;
  inherited-topology/source-scope/image certificate;
  addressed Res/quotient/Ker extension;
  reciprocal/refinement/common-refinement/old-image transports;
  A_N domain bridge b_N and codomain bridge e_N.

ABSTRACT_EXTSRC_FAMILY_EMPTY = not proved.                       (B3)
```

### 8.4 Self verb audit

| Verb used | Supporting display | Audit |
|---|---|---|
| `isolated/specifies` | `(A1-1)`--`(A1-28)` | clean; no inhabitance claim |
| `sealed/constructed` for stage-indexed `A_N=A_(G_N)` | `(A1-29)`--`(A1-30)` and verified `KER` | clean; scope remains `T_N->(K_N^fin)^*` |
| `automatic descent` | conditional chain `(A1-7)`--`(A1-8)` | clean; premise stated |
| `attempted` for `F_N`, `S_N^AG`, and unit factor | `(A2-1)`, `(A1-32)`, `(A2-25)`--`(A2-28)` | clean; candidates not bound |
| `stopped` | each exact missing arrow/equality in A-I through A-IV and AG | clean |
| `declared` for cross-sector unit | candidate-family classes `(A2-26)`--`(A2-28)` | clean; no representative or scale selected |
| `onto` | sealed statement `(A1-30)` | clean; not upgraded to split epimorphism |
| `not proved empty` | `(A2-29)` and `(B3)` | clean; stock obstruction is not a no-go theorem |
| `Round 1 complete` | determinate stop plus Section 5 handoff | clean; not called ExtSrc completion |
| `pending` for R9 | `(R9-1)`--`(R9-2)` | clean; neither vacuous truth nor execution claimed |

Every status verb is matched to the display above it.  No occurrence of
`constructs`, `inhabits`, `proves`, or `closes` exceeds the stated scope.

SPEC = ISOLATED
EXTSRC = STOPPED (+gaps: common Dker/Sch/iota, mixed-relation, inherited-topology/source-scope/image, addressed Res/Ker, and actual transport members; +round-2 plan: quotient-domain/current-landing, relation, topology, addressed-factor, and generator-base attacks in Section 5)
AG_BRIDGE = STOPPED (+gap: no sealed b_N:O_R5,N^src->T_N or e_N:(K_N^fin)^*->D_N^ker; onto A_N=A_(G_N) supplies no canonical covariant section)
CROSS_SECTOR_UNIT = declared
VERB_AUDIT_SELF = CLEAN
