# STAGE 8 — AXN BUILD — B0 ACCEPTANCE INVERSE
## CODEX 2 LANE — RELAY 895 — `[PLAN:AXN-BUILD-B23]`

**CLAIM STATUS:** every headline determination authored here is **CLAIMED** pending the next
opposite-lane check.

## 0. Exact law-9b closure declared first

```text
C_895 := {
  relay_inbox/RELAY_PASTE_895_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md,
  PROGRAM_STATE_BRIEF_V005.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md,
  STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md,
  STAGE8_AXN_BUILD_B0_PARTIAL_ROWS_DARIO_V001.md,
  STAGE8_MINIMAL_OMEGA_P5_COMMON_ORIGIN_ROLE_REALIZATION_ATTEMPT_V001.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DOR008_ASSEMBLY_AND_FIRST_RESTRICTION_TEST_V001.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_U1_DOR008_ASSEMBLY_DETERMINATION_V001.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md,
  STAGE8_CTP_PHYS_INPUT_PACKAGE_U3_DOR008_DOR009_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md,
  QUESTIONS_SETTLED_REGISTER_V001.md::{Q-106,Q-110,Q-111,Q-112,Q-113,Q-114,Q-194},
  DECLINE_REGISTER_V002.md::{S01-S37},
  LOCKED_PROCESS.md::{alignment laws,law-9b}
}
```

| closure member | SHA-256 |
|---|---|
| relay 895 | `af4382677838641937f719ec50cf5901636da23aefb7a004755a2fd519ca96e8` |
| state brief V005 | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` |
| B0 load-bearing stop specification | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` |
| B0 substitute admissibility adjudication | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2` |
| B0 root census | `a614a69409b40ab839e036d9439ac0591df64854d4275d721a624454b7213141` |
| B0 partial-row check | `2cb5b317aa244739a952353f3200660dd296d907405e290a5cdf37c7571d11ff` |
| minimal-Omega relabeling attempt | `576da30f300a0362469d6b4f447423a2298efc0dd28a60f38057e7f4cc8bd888` |
| C0 DoR-008 assembly | `1467ab9850022931e68dc9ffa625d95099d8f6bb74d734dc9d0466f169bd00b6` |
| U1 DoR-008 assembly | `1b0e928c452c10a8be72be22ff81fd7677f5045d2ad8d398a9f7f7f57b9ab3b0` |
| U2 DoR-008 partial assembly | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` |
| U3 DoR-008/009 partial assembly | `b0118d89e0b4b321fbf9caab5bfb811a0b5fd572e808d02615ee8503db2bc1ac` |
| settled register, current verified bytes | `b9987975aee209b92ce5aec6ef93726305896018f0000a72f706268e22849cb8` |
| decline register | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` |
| locked process | `8fbb9e08561c8542257dc0fad14b6a178b439dd31ade0b2262864d5973f31fbc` |

Every listed seal passed before reliance. The register pin is the verified live-append snapshot read
for this relay. “Missing,” “absent,” and `NO_VERDICT` below are bounded to `C_895` at the named
receiver or are quoted statuses; they are not corpus-wide token claims.

The root census and minimal-Omega attempt are same-lane antecedents and receive no self-verifying
weight. The opposite-lane partial-row artifact independently checked all thirteen partial rows and
expressly rechecked the relabeling result; the load specification and substitute adjudication supply
the governing acceptance law.

### Exact span index

| key | sealed bytes | span SHA-256 | use |
|---|---|---|---|
| `BAR-ROLE` | B0 stop specification `[32696,43442)` | `9350c58c857c2b10a8c5ed1346b4f89fbe3dcf2db352fe56df8e0db94865094d` | candidate, signature, descendants, four maps, provenance, no-supplement rule |
| `BAR-TESTS` | B0 stop specification `[43442,56199)` | `ffe3e286c357d0ce2b6e7c7d2130033ed8ab403477d7ecbd17a1998e841319c2` | T1–T6 applicability, failure, no-verdict, router law |
| `SUB-SPLIT` | substitute adjudication `[300,4981)` | `362c1fafc41754815c103bc948b0b99cf7c26fb7999d212b223d4a8ddb754cb0` | complete-role-realizer bar and partial-substitute refutation |
| `SUB-CONSUMERS` | substitute adjudication `[7767,13984)` | `bfa9629257b572cfbff209f10b14dac53f5cbeaa6aeca0c08b6e8ad095679a87` | descendant consumers and full-package equivalence/naturality debt |
| `CENSUS-17` | root census `[10712,16345)` | `3506433d0e09c837583edfa9e9fa3e4a94c034046c50dcdea9a64eae77119cf4` | exact 17-component material partition |
| `PARTIAL-13` | partial-row check `[4673,14595)` | `21c57b6a5de718784fcc316d7fffe8485ac8a81a72170dba43f881d7ebd4f95d` | per-row sources, wrong-domain lessons, certificate/test ceilings |
| `RELABEL-LEAD` | minimal-Omega attempt `[280,2580)` | `d396dcccf96b3d9853afff3ce7ee4ccfc8562532b45f5e701292647ad0b8e348` | executed relabeling result and its test clause |
| `RELABEL-TEST` | minimal-Omega attempt `[15017,19252)` | `13233d50a07c77b954b188ffbcd345e1a4939f90fd7981669d70307aa2277329` | mandatory root-versus-bag procedure |
| `SIX-ROUTES` | settled register `[273966,297020)` | `08c44274f5b2276cb973cc09728dff183dc2d25765bfdbc4a8765a2531ce670a` | Q-106 and Q-110–Q-114 route dispositions |
| `ROLE-RULING` | settled register `[517331,519690)` | `bec3589bb9d2775ce06c7bfe79c860a3d0dc7657e33e4cbbc51b2460274f793e` | Q-194 role-realizer split and six-route interpretation |

### Gates and task boundary

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

This report preregisters an acceptance interface. It does not instantiate that interface, build B0,
run a B0 test, move SC5-A, weaken the complete-only bar, or authorize a proof. No member was bound; no
fixed point was executed; no end test was run; no smooth carrier was imported; no electromagnetic
identification was made; no common cell was formed; no junction was evaluated; no physical quantity
was numerically evaluated; and no measured constant was consulted. PE-1 through PE-13 remained
pointer-only and were not opened.

---

## 1. Acceptance record and aggregate semantics

The inverse of `CENSUS-17` is a closed, 17-clause acceptance record:

```text
B0AcceptanceRecord.v001 := {
  A01_candidate_inhabitant,
  A02_primitive_inventory,
  A03_construction_rule_and_trace,
  A04_signature,
  A05_provenance_and_domain_certificate,
  A06_C0_descendant,
  A07_U1_descendant,
  A08_U2_descendant,
  A09_U3_descendant,
  A10_d_C0,
  A11_d_U1,
  A12_d_U2,
  A13_d_U3,
  A14_property_certificates,
  A15_no_post_output_certificate,
  A16_full_package_equivalence_naturality,
  A17_T1_T6_ledger
}

component_result := PASS(citations) |
                    FAIL(component,predicate,citations) |
                    NO_VERDICT(missing_applicability_input,citations)
```

The aggregate discipline is complete-only:

1. a component `FAIL` rejects only the named candidate/property at the named route;
2. `NO_VERDICT` is not evidence against existence, but it cannot be cited as a pass or used to seal a
   partial substitute as accepted;
3. every T1–T6 leg whose applicability inputs exist must run and must retain its own verdict owner;
4. no property-test pass constructs B0 or sets a derived flag; and
5. acceptance requires the construction witness and complete role realization in addition to the
   applicable test ledger.

Thus `SYSTEM-COMPLETE` below describes the **acceptance law**, not an accepted inhabitant.

---

## 2. The 17 constraints, derived component by component

### A01 — inhabited frozen B0-role candidate

The realization must exhibit one content-addressed, pre-test-frozen tuple

```text
CompleteMicroscopicBoundaryOriginCandidate :=
  (Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)
```

with `Carrier_B0` and `Core_B0` present whenever the declared type requires them. A schema, family,
codomain fiber, or empty candidate slot does not pass inhabitance. The type may be an operator, flow,
family, relation, algebraic generator, or another fully declared mathematical type; acceptance does
not demand a unique microscopic ontology.

### A02 — frozen primitive-input inventory

The record must exhibit a content-addressed inventory frozen before construction or target
inspection. Each row must name the primitive, mathematical type, domain, authority, and byte digest,
and the inventory must state its coverage boundary. It may not certify its own membership or import
descendant C0/U1/U2/U3 fields as upstream primitives. The six Level-1 premises are stock, not an
exhaustive `B0` inventory.

### A03 — target-independent construction rule and frozen trace

The record must exhibit an executable or mechanically replayable rule from A02's primitives to A01's
candidate, plus a frozen trace. The rule may not consume C0/U1/U2/U3, item 1, response output, a B0
discharge flag, or a target-selected success value as an upstream constructor. All inputs and rule
bytes must be frozen before descendant or response inspection.

This clause is the positive receiver corresponding to
`B0_CONSTRUCTION_WITNESS := (inventory, target-independent rule, provenance/domain certificate)`.

### A04 — complete candidate signature

The realization must fill—not merely quote—the sealed schema:

```text
Sig_B0 := {
  Type_B0,
  Arity_B0,
  Dom_B0,
  Cod_B0,
  Params_B0
}
Obj_B0 conforms_to Sig_B0
```

For a function/operator type, the applicable `Dom_B0 -> Cod_B0` map must be declared. For a flow,
family, relation, generator, or other type, its full arity, parameter, domain, and codomain data must
be declared at that type. The record must also exhibit a conformance witness and an independent
signature/domain/codomain oracle for T1. No undeclared linearity, boundedness, self-adjointness,
unitarity, locality, representation, or existence property is inherited from the schema.

### A05 — provenance and domain certificate

The certificate must bind `Obj_B0` to A02/A03's exact inventory and construction trace, bind every
declared carrier/core/domain to the operations that consume it, and prove every declared
carrier/domain/codomain/algebraic/causal/analytic property at its actual scope. Finite bounded-domain
receipts do not certify later unbounded endpoint domains. The certificate must carry every premise
on which its construction depends and must not absorb DoR-008/009 as unconditional facts.

### A06 — complete C0 descendant

The realization must exhibit, as one typed reference/output object:

```text
C0 := {
  joint source-record-field carrier/algebra,
  representation,
  common domain,
  branch embeddings,
  physical source maps
}
```

State, dynamics, quotient, measure, effects, contacts, Ward identities, and inverse results remain
outside C0. The current `C0_008` reference is real at its sealed DoR-008 premise scope; it is not the
common-origin map from the candidate.

### A07 — complete U1 descendant

The realization must exhibit on C0:

```text
U1 := {
  physical branch/source orientation,
  branch metric,
  reality involution,
  compound-index order,
  source symmetry,
  embeddings
}
```

Formal conventions alone do not pass the physical U1 receiver. The current `U1_008` reference is
real at DoR-008 scope; it does not instantiate A11.

### A08 — complete U2 descendant

The realization must exhibit on common physical domains:

```text
U2 := {
  microscopic action/evolution,
  positive normalized pre-state,
  inclusive identity,
  admitted record effects,
  action/source contact rules,
  common domains
}
```

Finite transition, state/effect ports, or a skeleton do not pass the complete content clause. Any
claim using the ratified transition law must carry DoR-009 in addition to DoR-008.

### A09 — complete U3 descendant

The realization must exhibit:

```text
U3 := {
  physical orbit/constraint map,
  gauge-fixed quotient,
  descended contour/spacetime measure,
  boundary/edge/gluing data,
  endpoint operator domains,
  predeclared contour prescription
}
```

Representation-required ghost/Jacobian data, when applicable, remain U3 descendants rather than
external supplements. The current finite skeleton is DoR-008/009 conditional and does not supply the
complete measure, interacting contour, boundary/edge carrier, or unbounded endpoint-domain package.

### A10 — right-domain `d_C0`

The required arrow is exactly

```text
d_C0 : CompleteMicroscopicBoundaryOriginCandidate -> C0
C0 = d_C0(B0_candidate)
```

It must be total and signature-conformant on admitted B0 inputs and compare to the independently
derived C0 reference on the identical input/domain declared by T3C. The existing total
`d_C0^P : P_008 -> C0_008` has the wrong domain and cannot pass by renaming.

### A11 — right-domain `d_U1`

The required arrow is

```text
d_U1 : (CompleteMicroscopicBoundaryOriginCandidate,C0) -> U1
U1 = d_U1(B0_candidate,C0)
```

It must consume the same candidate and the actual A10-descended C0, be total on admitted inputs, and
compare to the independent U1 reference on identical input/domain. A premise-level projection or
map that never consumes the candidate is the wrong receiver.

### A12 — right-domain `d_U2`

The required arrow is

```text
d_U2 : (CompleteMicroscopicBoundaryOriginCandidate,C0) -> U2
U2 = d_U2(B0_candidate,C0)
```

It must output the complete A08 package. Finite PathCert state/effect/domain/dynamics legs and
source-law material may be inputs or comparison stock at their premise scopes; their union is not a
total B0-domain map.

### A13 — right-domain `d_U3`

The required arrow is

```text
d_U3 : (CompleteMicroscopicBoundaryOriginCandidate,C0) -> U3
U3 = d_U3(B0_candidate,C0)
```

It must output the complete A09 package. A finite quotient/law-side restriction map cannot be
promoted while the complete measure, contour, boundary/edge, and endpoint-domain fields remain
unbuilt.

### A14 — full property-certificate suite

The realization must carry a closed certificate inventory. Every row must name:

```text
{
  predicate_id,
  subject_digest,
  premise_digests,
  receiving_operation_or_test,
  procedure_or_replay_object,
  result,
  failure_verdict_owner
}
```

Coverage must include every property declared by A04/A05, the exact C0/U1/U2/U3 content predicates,
the totality/typing/correspondence predicates for A10–A13, and the property legs whose applicability
inputs are present. Finite covariance, restriction, target-independence, bounded-domain, or local-law
certificates retain their recorded scopes and cannot stand in for the full suite.

### A15 — no-post-output supplementation as an executable test

Before construction or descendant inspection, freeze:

```text
{
  primitive_inventory,
  candidate_and_signature,
  construction_trace,
  exhaustive_C0_U1_U2_U3_output_checklist,
  four_descent_maps,
  allowed_pure_label_chart_convention_inputs
}
```

Trace every required physical datum to the common A01–A03 origin. The test **fails** if a state,
action, carrier, source map, quotient, measure, effect, boundary datum, or domain enters from an
undeclared supplement, or if the allowed-input list expands after any descendant/item-1/response
output is observed. A datum bagged independently before output may pass literal timing while still
failing A03/A05 common derivational provenance; both predicates are required.

### A16 — full-package equivalence/naturality rule

No relation instance exists in `C_895`, but its consumers determine the acceptance shape without
selecting an equivalence class. A realization must exhibit the following closed record, frozen before
testing:

```text
FullPackageEquivalenceNaturality.v001 := {
  realization_domain: complete A01-A15 records,
  Eq_pkg: declared equivalence relation on complete package presentations,
  pure_mutations: exhaustive label/chart/convention transformations,
  induced_maps: transformations on C0,U1,U2,U3 and d_C0,d_U1,d_U2,d_U3,
  classifier: independently fixed representation-equivalence oracle,
  naturality_rule:
    DESCEND_B0(T_realizer(R)) Eq_pkg T_package(DESCEND_B0(R)),
  physical_input_exclusion:
    physical mutations are handled by T6B, never classified as pure,
  certificates: equivalence-law and commuting/naturality replay objects
}
```

Because `Eq_pkg` is called an equivalence relation, its declared domain and relation laws must be
explicit. It must compare the **whole** C0/U1/U2/U3 presentation and all four descent witnesses, not
only U2/P5-facing fields. It may identify package presentations without identifying their unused
microscopic interiors; unique-origin reconstruction remains unnecessary. No relation, classifier,
or naturality proof is supplied here.

### A17 — applicable T1–T6 ledger

The realization must carry one row per sealed test leg:

| leg | applicability supplied | pass exhibit | failure owner |
|---|---|---|---|
| T1 | candidate, signature, carrier/core if applicable, independent oracle | conformance of every declared operation/input/output without repair | `B0_OBJECT_DOMAIN_CONFORMANCE` |
| T2 | candidate, exhaustive descendant checklist, four maps, frozen allowed pure inputs | every physical datum traces to the same origin before output | `B0_SINGLE_ORIGIN_PRODUCTION_CONFORMANCE` |
| T3C | candidate, `d_C0`, independent C0 reference/oracle | total right-domain map agrees with reference | `B0_TO_C0_CORRESPONDENCE` |
| T31 | candidate/C0, `d_U1`, independent U1 reference/oracle | total right-domain map agrees with reference | `B0_TO_U1_CORRESPONDENCE` |
| T32 | candidate/C0, `d_U2`, independent U2 reference/oracle | total right-domain map agrees with reference | `B0_TO_U2_CORRESPONDENCE` |
| T33 | candidate/C0, `d_U3`, independent U3 reference/oracle | total right-domain map agrees with reference | `B0_TO_U3_CORRESPONDENCE` |
| T4 | candidate plus independently typed `U_BR`, `rho_pre`, effects/domains and maps | compatible domains and fixed-signature correspondence | `B0_TO_PRIMARY_OUTPUT_CORRESPONDENCE` |
| T5A | independent B0 and Boundary-Resolved generator plus map/reference | typed map agrees with generator reference | `B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE` |
| T5B | generator, fixture, global domain, support and boundary evaluators | support remains in frozen D and global boundary form vanishes on admitted variations | `BOUNDARY_RESOLVED_GENERATOR_CAUSAL_BOUNDARY_CONFORMANCE` |
| T6A | complete joint package, evaluator/state/effects, item-1 signature | frozen item-1 signature realized with predeclared evaluator/source/domain identity | `JOINT_PACKAGE_TO_ITEM1_REALIZATION_CORRESPONDENCE` |
| T6B | exhaustive physical-input mutation list plus candidate/four maps | no undeclared physical input changes a descendant | `DESCEND_B0_INPUT_COMPLETENESS` |
| T6C | exhaustive pure-mutation list, A16 classifier, complete realization | no pure change alters physical output outside `Eq_pkg` | `PACKAGE_U3_REPRESENTATION_NATURALITY` |

Each row must contain its applicability digest set, procedure transcript/replay object, result,
failure owner, and citations. A missing candidate, map, reference, fixture, domain, classifier, or
oracle returns the leg's sealed `NO_VERDICT`; it never becomes a candidate-existence refutation and
never masquerades as a pass. T5/T6 downstream property legs do not report into generic B0 existence.

---

## 3. Negative acceptance data

### 3.1 The executed relabeling negative

The sealed negative is:

```text
CANDIDATE_SURVIVES_RELABELING_TEST = false | TYPE-R |
  test: deleting the name Omega and expanding the tuple leaves exactly the
        two-independent-input presentation
```

It yields a mandatory negative test for any candidate:

1. erase the candidate/role label;
2. inline its fields and map bodies;
3. trace every descendant backward through A03 to A02;
4. reject the candidate at A03/A05/A10–A13 if the alleged maps reduce to coordinate projections,
   renamings, or selections of already supplied output fields with no antecedent generator.

A candidate must therefore **not** be a declared bag of C0/U1/U2/U3 outputs wearing a new name.
Total projections, target-independence, and literal pre-output freeze do not repair missing common
derivational provenance.

### 3.2 Six refuted/closed routes as negative evidence, not existence refutations

| route of record | what its result bars as an acceptance argument | what it does not bar |
|---|---|---|
| accessor theorem, Q-106 | definite-description access without existence and unique row witnesses | a constructed complete realizer |
| codomain route, Q-110 | choosing an arbitrary point of the codomain-compatible fiber | a point supplied with A01–A17 evidence |
| `I_prim` route, Q-111 | presentation conventions substituted for physical type/domain/maps/provenance | a realizer that consumes declared primitives through A03 |
| version/history route, Q-112 | claiming a historical alias or superseded partial as the complete object | a new construction or supplied complete record |
| joint constraints, Q-113 | treating a shrunken residual fiber as an inhabitant/selector/equivalence theorem | a witnessed member passing the full system |
| incidence route, Q-114 | transporting an incidence object with no direction-bearing construction arrow | a future explicit A03 construction using independently derived incidence data |

These are negative acceptance data: none of the six route outputs is an A01–A17 record. Their failure
does not refute the existence of a future role realizer.

---

## 4. DoR-008/009 premise overlay

The acceptance law is not made conditional on the current partial stock. The **current evidence**
offered toward several clauses is premise-conditioned, and any future record reusing it inherits the
premises exactly as follows:

| acceptance scope | current-stock premise overlay | acceptance consequence |
|---|---|---|
| A01 inhabitant | none; no inhabitant supplied | no premise can be inferred for an absent receiver |
| A02–A05 structural law | schema/rule requirements are premise-independent; finite stock used to populate them carries its own tags | a new inventory/trace/signature/certificate declares its actual premises; current TYPE-P stock is not promoted |
| A06 C0 content/reference | `DoR-008` | any use of `C0_008` or its certificates carries DoR-008 |
| A07 U1 content/reference | `DoR-008` | any use of `U1_008` or its certificates carries DoR-008 |
| A08 U2 stock | `DoR-008`; additionally `DoR-009` where the ratified transition law is consumed | skeleton-only and law-consuming rows remain distinguishable |
| A09 U3 stock | `DoR-008, DoR-009` | the finite skeleton and its maps/certificates remain jointly conditional |
| A10 `d_C0` stock | `DoR-008` | the premise-level map remains total from the wrong domain and conditional |
| A11 `d_U1` stock | `DoR-008` | the premise-level map remains conditional and wrong-domain |
| A12 `d_U2` stock | `DoR-008`; `DoR-009` on source-law/dynamics pieces | no mixed-premise union becomes a total B0-domain map |
| A13 `d_U3` stock | `DoR-008, DoR-009` | finite restriction maps remain conditional and incomplete |
| A14 property-certificate stock | premise union of the subject/map it certifies | each certificate carries, rather than discharges, its subject's premises |
| A15 finite no-supplement pass | `DoR-008, DoR-009` for the PathCert/minimal-Omega subtrace | local sequencing pass is not aggregate T2 |
| A16 equivalence/naturality | none; rule instance absent | no premise or relation is invented from current descendants |
| A17 test definitions/evaluations | definitions are premise-independent; any execution inherits all applicability-input premises | a PASS is conditional on the union displayed in that test row |

This displays the correlated risk without converting it into thirteen independent hedges. Withdrawal
or narrowing of DoR-008 would remove or narrow most current material together; DoR-009 additionally
affects the law-consuming U2 pieces and U3/PathCert material. Neither premise is discharged here.

---

## 5. Typed control — why the six origin routes could not pass

**CONTROL TYPE: `EXPLAINS`.**

The six routes returned an accessor condition, a codomain-compatible fiber, presentation structure,
a negative history result, a residual constraint fiber, and an incidence route with no construction
arrow. None returned the A01 inhabitant together with A02–A17's frozen construction, descendants,
right-domain maps, certificates, equivalence/naturality rule, and applicable test ledger. Therefore
none of those route results could pass the acceptance system by itself.

The reason is structural: the six questions asked whether a unique or determined microscopic object
could be recovered from a theorem, consumers, primitives, history, constraints, or incidence. The
sealed role bar asks a different question—whether **one complete target-independent,
codomain-compatible role realization** is exhibited. It permits multiple internal mathematical types
and does not require uniqueness, while remaining complete-only at its operational interface.

This control does not claim that corpus search can never locate a complete preexisting acceptance
record. If a future hunt finds one, the **record** must pass A01–A17. It says only that the six
executed route outputs, and a name/identity hunt of their form, do not constitute that record.

### Independence index

```text
explanation sources:
  BAR-ROLE + BAR-TESTS + SIX-ROUTES

sentence explained:
  ROLE-RULING's statement that the six failed routes sought a unique microscopic
  origin that the abstract B0 role did not require
```

Deleting the explanatory sentence leaves the six route results and the role/test schema intact, so
the explanation survives deletion. The control is independent and is not a transcription.

---

## 6. Verdict and protected SC5-A status

All 17 material-census components have a receiving field, a pass exhibit, and a failure or
`NO_VERDICT` discipline. A16's relation instance remains absent, but its required schema is derived
from the full-package identification consumer and T6C without selecting the relation. No acceptance
component is underived.

```text
VERDICT = SYSTEM-COMPLETE
meaning = the complete acceptance constraint system is displayed;
          no inhabitant is supplied, no component is executed here, and no candidate is accepted.
```

The material census remains exactly **2 ABSENT / 2 PRESENT / 13 PARTIAL**. The inhabited candidate
and full-package equivalence/naturality instance remain the two absent components; this report's
schema is not partial evidence of either inhabitance. SC5-A therefore remains **ABSENCE /
COMPLETE-ONLY**. The ce46032d partial-substitute bar is neither modified nor bypassed.

**CORRECTION PROPAGATION:** future B0 candidates and SC5-A adjudications must cite this 17-clause
interface; must run the relabeling negative; must use the right domains for A10–A13; must keep
DoR-008/009 premise tags on reused stock; and must not cite a six-route failure, local certificate, or
property-test pass as construction or inhabitance.

---

## 7. FREEDOMS-CONSUMED

```text
CARRIED-AS-PARAMETER:
  the complete-role-realizer bar and partial-substitute refutation;
  all 17 census components at 2 ABSENT / 2 PRESENT / 13 PARTIAL;
  Sig_B0's five-field schema;
  C0/U1/U2/U3 content inventories;
  the four exact direction-bearing map signatures;
  T1-T6 applicability, failure, no-verdict, and router laws;
  CANDIDATE_SURVIVES_RELABELING_TEST = false at its sealed scope;
  DoR-008/009 as premises, not discharged results;
  SC5-A as ABSENCE / COMPLETE-ONLY.

CONDITIONED-ON:
  every future certificate on its declared subject, scope, replay object, and premise set;
  every future test on its sealed applicability inputs and independent oracle/reference;
  package equivalence/naturality on an independently frozen Eq_pkg and mutation classifier;
  any route reuse on an explicit A03 construction arrow and the full A01-A17 record.

SUBSTITUTED:
  NOTHING. No B0 candidate, primitive inventory, constructor, trace, signature values, carrier,
  core, provenance, descendant, descent map, property certificate, equivalence relation,
  naturality map, test result, selector, or accepted record was authored or selected.

SCALING WEIGHTS:
  NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

---

## 8. Flattening and verb audit

**FLATTENING CHECK — S01–S37 walked, clean.** A role schema was not identified with an inhabitant;
codomain compatibility was not identified with a selected point; presentation structure was not
identified with physical construction; a total premise-level map was not identified with a B0-domain
descent; literal pre-output timing was not identified with common derivational provenance; a local
certificate was not identified with full-package coverage; a test pass was not identified with B0
construction; `NO_VERDICT` was not identified with failure; package equivalence was not identified
with unique microscopic origin; and a six-route failure was not identified with nonexistence.

**BUILDER-NEVER-VERIFIES:** same-lane census and minimal-Omega bytes are used only as subjects whose
relevant findings were checked by the opposite-lane partial-row artifact or settled by the register.
No same-lane prior receives self-confirming weight.

**SELF VERB AUDIT:** “derived” applies to the acceptance constraints and A16's consumer-forced schema,
not to a realizer. “System-complete” applies only to the preregistered law. “Present,” “partial,” and
“absent” retain the census rubric. No candidate, test pass, physical object, construction, proof,
acceptance, or status movement is claimed.

---

## 9. Final lines

```text
CLOSURE = declared-first (C_895 starts at byte 274)
CONSTRAINTS = derived (17, one per sealed bar component)
NEGATIVE_DATA = folded (relabeling + six refuted routes)
DOR_OVERLAY = displayed (law independent; current stock conditional on DoR-008, plus DoR-009 where consumed)
VERDICT = SYSTEM-COMPLETE (acceptance law whole; no realization accepted)
CONTROL = EXPLAINS (six-route origin-hunt failure reproduced; independent source index)
SC5A = ABSENCE / COMPLETE-ONLY (material census unchanged: 2 ABSENT / 2 PRESENT / 13 PARTIAL)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
