# STAGE 8 — ADVERSARIAL AUDIT OF THE MO-4-R ATTEMPT (STAGE8_MO4R_GRAM_MASS_T3SR_V001) — T3SR V001

## ADVERSARIAL AUDITOR — CODENAME MO4R-AUDIT — COMMISSION T3SR — [SEALED]

Date: 2026-08-15 (session CDT). Role: DEFAULT-REFUTE auditor; the
builder's testimony carries ZERO weight — every load-bearing step
re-derived at the bytes, in a fresh venv, against independently
seal-verified ground. My verdict governs.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. EXACT SYMBOLIC ONLY; no floats; no
numeric evaluation; no measured-constant comparison; symbolic scales
only; NO value frozen. No fenced-class file opened (register|road_|
ledger|lens|plan|tracker|THE_HANDOFF|continuation — one all-caps
REGISTER-named cleanroom filename surfaced at FILENAME level in a
sweep listing, §5 f-4, and was NOT opened). "Q-..." tokens
EXPECTED-UNLOCATABLE by design; none chased. No git. DETERMINATION
ONLY — no adoption, no authored physics, no member binding; the
registrar consumes. ONE output (this artifact) + seal sidecar at the
commission-distinct path, probed ABSENT at session start AND re-probed
immediately before write.

SWEEP CUTOFF, DECLARED: all sweeps of this audit executed against the
tree state as of **2026-08-15 03:14:29 CDT**.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — one correction, display-grade, NOT
verdict-chain. The headline verdict UNDECIDABLE-TODAY is adjudicated
CORRECT AT THE BYTES: both blockers re-verified as genuine absences by
my own independent sweep at a LATER cutoff (no kappa_n ceiling below
D4's 2n^3 exists in either commissioned root — every "kappa_n <=" hit
is the D4 display or an absence disclaimer; no 1/2-approach/eigenvector
localization law exists — only the four known absence carriers), and
the forcing argument survives attack (the R8 witness pair re-derived
exact, PLUS my own 4x4 rank-2 witness showing the underdetermination is
not a 2x2 artifact: identical projections, clock value, and profile
bounds realize fraction 0 AND fraction 1 — no identity-grade route from
the displayed hypothesis class decides THE QUESTION). Every load-bearing
DERIVED step re-derived independently at GENERAL symbolic grade where
the builder used instances: the window identity and class equivalence
(AR1), the class domination E_n(eps) <= (1/4 - eps^2)^{-1} Y_n Y_n^dag
with boundary-tightness proved SYMBOLICALLY in eps (AR1, stronger than
the builder's rational exhibit), the transfer through PSD conjugation
with arbitrary PSD b in the T-eigenbasis (AR2/AR3, 3x3 all-symbolic),
the spectator ceiling X_n <= kappa_n/2 and the count twin (AR4/AR5/
AR14/AR15), the T-4 assembly with the D4-vacuity display (AR6a), the
far-class sqrt(n) scope display (AR7), the crossing/commutator-clock
algebra at a FRESH instance distinct from both the builder's and the
ground's (AR9), the O-a square and class commutation (AR12/AR13), and
the O-1/O-2 decider arithmetic (AR10/AR11). The builder's CAS battery
re-ran VERBATIM in my fresh venv: exit 0, 9/9 PASS, output
BYTE-IDENTICAL. All 10 input seals verified by me at path before
consumption; the residue-naming quote is verbatim at T-G1 §4.5; D4
(kappa_n = 2 sum s_i(1-s_i) <= N/2 = 2n^3, N = 4n^3), D9/F-3 (no upper
rate), :469/:808 (missing 1/2-approach law), and MO-2's floor
quantifier (kappa_n >= C_* n/8, n >= N_0) all re-verified at their
source bytes. FENCE-SCAN: CLEAN — no computed alpha/kappa/coupling/
scale value, no measured-constant comparison, no float as ground
(only the sympy version string and exact-rational toy values). H-R
held: rho_n appears in no display of the target as a bound. No refused
cancellation mechanism is consumed anywhere in T-1..T-4 (the transfer
consumes the ground's own sealed profile split plus classical spectral/
trace positivity only; the four refusals of record are not re-walked).
THE CORRECTION (c-1): the §6 trichotomy's middle line "alpha-k = 3/2
==> localization alone decides" (and its flag-block echo) is FALSE as
universally quantified — my polylog probe (AR6b): kappa_n = C n^{3/2}/
log n has alpha-k = 3/2 exactly, yet the T-4 ceiling still decides
o(1); the builder's own O-1 equivalence clause ("any X_n = o(n^{3/2})
certificate") already covers that subcase, so the trichotomy prose
contradicts the artifact's own decider definition in a measure-zero-
of-consequence display; the verdict chain (reduction + blockers +
witness pair + sweep) nowhere consumes the trichotomy. SWEEP DELTA at
my later cutoff: ONE new carrier of the residue's name exists —
STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md (sealed 03:03 CDT, AFTER the
builder's declared 02:57:40 cutoff; seal verified) — boundary/
consistency mentions ONLY: it does not claim MO-4-R, seals neither
decider, and its own text holds "MO4's PARTIAL and its residual MO-4-R
... at their own quantifiers"; the builder's sw-1 occupation claim is
TRUE at its declared cutoff and the residue remains unoccupied at mine.
Nothing fires: MO-4 stays open, MO-4-R stays open and stands REDUCED
exactly as claimed; no flag flips; the registrar consumes. My battery:
16/16 PASS (one pre-final check-form correction of MY OWN disclosed in
§6: AR6a's divergence conjunct was sign-ill-posed for unconstrained
symbolic eps and was restructured to the sign-free form — a defect of
my check, not of the builder's R6, which never asserted that limit).**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
file consumed at ANY grade this session was seal-verified by me by FULL
digest against its sidecar BEFORE consumption. 11/11 MATCH (10 inputs +
the post-cutoff sweep hit).

```text
TARGET (read in full, then attacked):
A-T  299b68d1652bf2081844b911ac938267262fd5f5222d9378f99602b97abef5e5
     STAGE8_MO4R_GRAM_MASS_T3SR_V001.md            MATCH
GROUND PAIR (read in full, consumed independently of the target):
A-G1 0e894372d1c53dc6d66d171da08b13c4a756bea9da9bcd7c4c2863645300e127
     STAGE8_MO4_CANCELLATION_S9AD_V001.md          MATCH
A-G2 bfd86217319ecabca67352fa23d0cf4d0f9926b564924681cb383c57622678b0
     STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md    MATCH
SWEEP-CONSUMED (match-level lines only, seal-verified before reliance):
A-S1 STAGE8_R3_JOINT_RATE_S9AD_V001.md             MATCH — D4 bytes
     (:704 "kappa_n = 2 sum s_i(1-s_i) <= 2 N (1/4) = N/2 = 2n^3 with
     N = 4n^3 EXACT"); §4.6 bytes (:429-433, denominator "admits NO
     sealed upper bound at all (D9). NO RATE sealed (F-3)"); :469 and
     :808 (the missing quantified 1/2-approach/avoidance law); the MO
     list (:804-815).
A-S2 STAGE8_R3_JOINT_RATE_S9AD_AUDIT_V001.md       MATCH — R-a/F5/B1
     ceiling lines: all D4-2n^3.
A-S3 STAGE8_MO2_KAPPA_RATE_S9AD_V001.md            MATCH — floor
     quantifier at :400 "kappa_n >= C_* n / 8 for n >= N_0"; X-1 at
     :457 (linear-to-2n^3 bracket; no new ceiling).
A-S4 STAGE8_MO2_KAPPA_RATE_S9AD_AUDIT_V001.md      MATCH — CL-4 at
     :192 "the build does NOT cite Widom/Sobolev-class".
A-S5 STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md MATCH — ceiling
     mentions D4-references only.
A-S6 STAGE8_REFUTING_BRANCH_S9AD_V001.md           MATCH — same.
A-S7 STAGE8_RATE_GAP_LINKAGE_S9AD_V001.md          MATCH — same.
POST-CUTOFF SWEEP HIT (match-level lines only, §5 f-2):
A-S8 STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md         MATCH — MO-4-R
     mentions at :255/:348/:736 are boundary/consistency only.
```

Consumption discipline: every r-3/MO-2/MO-3/PA display relied on below
enters through A-G1/A-G2 at audited grade; the A-S reads corroborate
the SAME displays at their source bytes. No other file opened. No
fenced-class file opened. "Q-..." tokens: none encountered beyond the
ground pair's own notations.

---

## 2. THE TARGET UNDER ATTACK — EVERY LOAD-BEARING CLAIM, PER-CLAIM VERDICTS

```text
K-1  O-a (1 + A_n(0) = 1 - 2 T_n on ran C_n; R_n = (1 - 2 T_n)^{-1}
     functional calculus of T_n, commutes with every spectral
     projection): CONFIRMED. AR12 (the square identity for ARBITRARY
     T, matrix-general), AR13 (commutation, symbolic eigenvalues);
     consistent with the ground's V5/V6 at audited grade — (1-2T)^{-1}
     = (1+A_0)(1-4YY^dag)^{-1} is the same operator.
K-2  O-b (Y_n Y_n^dag = T_n - T_n^2; tr = kappa_n/2; kappa_n =
     ||[P, C_n]||_2^2): CONFIRMED. AR9 at a FRESH exact instance
     (rank-2 frame (1,2,2,0)/3, (2,-2,1,0)/3; C = diag(1,0,1,0) —
     distinct from builder's R3a/R4 and ground's V5/W4), orthonormality
     verified; general proofs are projection algebra + cyclicity,
     re-derived by hand: tr((PC-CP)^dag(PC-CP)) = 2[tr(CPC) -
     tr((CPC)^2)]. D4 rank/ceiling bytes verified at :704 (A-S1).
K-3  O-c (window-exact class: E_n(eps) = 1_{|s_i - 1/2| <= eps}(T_n)):
     CONFIRMED. AR1 general symbolic, including the eps < 1/2
     requirement (0 outside the window class — the restriction-to-
     ran-C_n phrase is anyway carried in the target).
K-4  O-d (objects pinned at of-record naming; denominator = the
     of-record G_n): CONFIRMED AT BYTES. The §2 residue quote is
     VERBATIM at A-G1 §4.5 (:432-436, diffed modulo the stripped code-
     fence indentation); G_n's naming and two-sided certification
     (given CL-A) are A-G1 §4.2/4.3 at audited grade; CH-2's refusal
     to substitute the C_n-restricted alternative is correct and the
     alternative is consumed nowhere (verified by inspection).
K-5  T-1 CLASS DOMINATION (boundary-tight): CONFIRMED, STRENGTHENED.
     AR1 proves the scalar gap vanishes at s = 1/2 +- eps SYMBOLICALLY
     in eps (builder exhibited it at eps = 1/8); AR2 gives the termwise
     domination against ARBITRARY PSD b in the T-eigenbasis with the
     on-window excess (eps-d)(eps+d)/(1/4-eps^2) exact — the spectral-
     theorem reduction the builder displayed in text, now CAS-pinned at
     general grade.
K-6  T-2 THE TRANSFER: CONFIRMED. AR3 (3x3 all-symbolic conjugation
     ground, stronger than the builder's 2x2); tr(E b~ E) = tr(b~ E)
     for a projection E is cyclicity + idempotence, re-derived.
K-7  T-3 SPECTATOR CEILING + COUNT TWIN: CONFIRMED. AR4 (3x3 trace
     positivity), AR14 (b_D <= 1 re-derived: monotonicity in u exact,
     (s(1-s))^2 <= 1/16 by exact factorization, e^{16-16} = 1), AR15
     (the unit s-integral), AR5 (the constant algebra: the SAME
     (1/4-eps^2)^{-1} carries mass_n and tr E_n).
K-8  T-4 ASSEMBLY + FRACTION CEILING: CONFIRMED given CL-A exactly as
     graded. AR6a: gamma-ceiling => limit 0 at rate n^{-gamma};
     exact-3/2 => n-free; D4 substitution = 4 n^{3/2}/((1-4 eps^2)c_G)
     EXACT, divergent factor sign-free — VACUOUS at power 3/2. The
     conditional grade ("given CL-A through the sealed floor") is
     carried at the claim site and in the flag block; §0's "certified"
     is the record's own vocabulary for that grade (A-G1 flag block).
K-9  THE STOP (no sub-3/2 ceiling; reduction, not verdict): CONFIRMED
     at bytes AND at my later sweep cutoff (§5 f-1): every "kappa_n <="
     carrier in both roots is D4's 2n^3 or an absence disclaimer; D9/
     F-3 re-verified at :429-433; CL-4 re-verified at :192. The
     reduction claim is correctly one-sided ("No converse is claimed").
K-10 RT-A..RT-E (the other routes): CONFIRMED. RT-A: every counting
     route factors through tr(T_n - T_n^2) — the clock again; the rank
     bound tr E <= 4n^3 is vacuous against n^{3/2} — verified. RT-B:
     no sealed datum locates crossing mass relative to the central box
     — verified as absence (§5); the refusal is two-sided and honest.
     RT-C: consumed at the sealed refusal displays, NOT re-walked —
     verified (no involution/parity content enters T-1..T-4). RT-D:
     the out-of-ground ruling for r-2 realization bytes is correct
     (r-2 is not in the T3SR ground; CH-8 enters as named inherited
     premise only). RT-E: AR7 — the ratio (K C_G n^{3/2}/(2 eps_n))/
     (C_* n/8) = (4 K C_G/(C_* eps_n)) sqrt(n) EXACT; the claim is an
     INSUFFICIENCY scope display (F_n = 0 would not close MO-4 through
     today's displayed chains at the certified clocks) — the safe
     direction; consistent with the ground's own sqrt(n) tightening.
K-11 §5 WITNESS PAIR / FORCED UNDECIDABILITY: CONFIRMED, STRENGTHENED.
     AR8 re-derives the 2x2 pair exactly AND adds my own 4x4 rank-2
     witness (class b-mass 0 vs 2 at tr b = 2): the underdetermination
     of the fraction by {projections, clock value, profile bounds,
     total profile mass} is structural, not a minimality artifact. The
     quantifier discipline is correct: the pair refutes derivability
     from the DISPLAYED hypothesis class only; family-specific routes
     (actual b_D bytes + actual C_n realization) are exactly what O-1/
     O-2 commission — the target says so itself (RT-D, §6).
K-12 §6 BLOCKERS + DECIDER MENU: CONFIRMED (with c-1 on the trichotomy
     prose). BLOCKER-1/BLOCKER-2 absences re-verified independently at
     my cutoff (§5 f-1); AR10 (count floor forces the clock floor —
     rearrangement exact); AR11 (O-2's yield c'' c' m_0 / C_G n-free,
     via the certified Gram CEILING — correct side of the two-sided
     bound); O-1's kappa-form sufficiency through X_n <= kappa_n/2 —
     exact.
K-13 §7 SWEEP CLAIMS: CONFIRMED at the builder's declared cutoff;
     sweep delta at mine is §5 f-2 (one post-cutoff boundary carrier;
     no occupation, no decider). sw-6's out-of-root note is outside my
     commissioned roots; not chased (discipline held).
K-14 §8 CONSISTENCY POINTS X-1..X-6: CONFIRMED. rho_n grep: the target
     uses rho_n in NO display (fence declaration, ground-content
     description, and disclaimers only) — H-R held; the far-class
     bound is spectral (K-10). No flag of the sealed pair is touched.
K-15 §9 CAS BATTERY: CONFIRMED. Verbatim extraction (sha256
     e8dfe62d43ad2ba9c17bffdfd855b5e4b05f0a834c0574ac1fe642f15fbcf7c7),
     fresh venv `mo4rauditvenv` (sympy 1.14.0): exit 0, 9/9 PASS,
     output BYTE-IDENTICAL to the recorded block (diff empty). R3b's
     instance-grade conjuncts are disclosed as exhibits in the check
     name and TOY_SEPARATION — no promotion beyond the displayed
     general proofs (and my AR1/AR2 now pin those at general grade).
K-16 FENCES/HYGIENE: CONFIRMED. Fence-scan (§4) clean; gates present
     top and bottom; ONE output + sidecar; no git tokens beyond
     declarations; no fenced-class opening; the commission id T3SR
     occurs only in the target and its sidecar.
OVERALL: CONFIRMED-WITH-CORRECTIONS (c-1 display-grade); headline
     verdict UNDECIDABLE-TODAY stands AT THE BYTES.
```

---

## 3. CORRECTIONS

```text
c-1 (display-grade, non-verdict-chain; the ONE correction). §6
    TRICHOTOMY, middle line (:365 "route saturates; localization alone
    decides.", echoed in the flag block :716 "= 3/2 the ceiling
    saturates n-free and localization alone decides"): FALSE as
    universally quantified over alpha-k = 3/2. Countermodel (AR6b,
    exact): kappa_n = C n^{3/2}/log n gives alpha-k = limsup log
    kappa_n/log n = 3/2 EXACTLY while the T-4 ceiling 2 kappa_n/((1-4
    eps^2) c_G n^{3/2}) -> 0 — the ceiling route DOES decide o(1)
    there. The target's own O-1 clause ("equivalently any X_n =
    o(n^{3/2}) certificate") already covers this subcase, so the prose
    contradicts the artifact's own decider definition. EXACT REPAIR:
    "alpha-k = 3/2 ==> the pure-POWER ceiling route saturates (T-4
    n-free at kappa = C n^{3/2}, CAS R6); any kappa_n = o(n^{3/2})
    certificate (e.g. sub-polylog) still decides o(1) via T-4/O-1;
    absent one, localization decides." The verdict chain (T-1..T-4 +
    blockers + witness pair + sweep) consumes the trichotomy NOWHERE;
    R6's CAS conjuncts are themselves exact and correctly scoped.
    Noted for the registrar's consumption of §6 in isolation.
```

No other correction. In particular the following were attacked and
survived: the "certified" wording of §0 vs T-4's given-CL-A grade
(record vocabulary, grade carried at the claim sites); R3b/R4
instance-grade conjuncts (disclosed, and now generalized by AR1/AR2/
AR9); the RT-E insufficiency claim (correctly scoped to today's
displayed chains); the shrinking-window constant (2/c_G exact); the
BLOCKER-2 count-to-clock arithmetic (AR10 exact).

---

## 4. FENCE-SCAN (executed, results verbatim-summarized)

```text
Float-literal scan of the target: ONE hit — "sympy 1.14.0" (version
string; not ground). Measured-constant / computed-value scan (137,
fine-structure, alpha = <number>, kappa = <number>, hbar, planck):
ONE regex hit — R8's toy display "clock kappa = 2 tr YY^dag = 1/2 in
BOTH" — an exact-rational 2x2 witness value, TOY-SEPARATED by the
target itself, not the record kappa_n, not a measured constant, no
comparison to any measurement. NO FENCE-HIT: alpha_computed remains
false; kappa_record_computed remains false; no coupling or scale
valued anywhere; every constant in the target is symbolic or an exact
rational/closed form. proof_authorized untouched (the target authors
determinations and refusals, no proof adoption).
```

---

## 5. THE AUDIT'S OWN SWEEP (cutoff 2026-08-15 03:14:29 CDT)

```text
ROOTS (as commissioned): /Users/bgm/MB Work/alpha-program-archive/
workspace (primary) and /Users/bgm/Documents/New project/
gravity_emergence_evidence_program/
alpha_fundamental_record_action_cleanroom_v003 (cleanroom). Fenced name
classes excluded from every opening; grep -l listings are FILENAME
level only. KEYS: the builder's full set ("MO-4-R"; "MO4R"; "T3SR";
"near-1/4"; "1/2-approach"; "eigenvector localiz"; "clock ceiling";
"kappa ceiling"; "upper rate for kappa"; "Widom") PLUS audit attack
keys the builder did not run ("Szego"; "Sobolev"; "localization law";
"avoidance law"; "eigenvalue counting"; "polylog"; "kappa_n <=").
FINDINGS:
 f-1 CLOCK-CEILING AND LOCALIZATION CARRIERS: every "kappa_n <=" hit
     in both roots is D4's 2n^3 (or the rank 4n^3) or an absence
     disclaimer — match lines listed per file, A-S1..A-S7; NO sub-3/2
     ceiling exists. Localization keys hit ONLY the four known absence
     carriers (target, ground build, r-3 pair). BLOCKER-1 and
     BLOCKER-2 are GENUINE ABSENCES at this later cutoff.
 f-2 SWEEP DELTA vs the builder's cutoff: STAGE8_WALL_LOGFREE_RATE_
     T4SR_V001.md (mtime 03:03:xx CDT > 02:57:40; seal verified,
     A-S8) carries "MO-4-R" at :255/:348/:736 — boundary/consistency
     mentions only ("its remaining refinement axis (the resolvent-
     weighted factor, MO-4-R) concerns R_n, which does not appear in
     the baseline-free K^Sigma"; "MO4's PARTIAL and its residual
     MO-4-R ... at their own quantifiers ... no flag of the record
     flipped"). It does NOT claim MO-4-R, does NOT seal a kappa
     ceiling, does NOT seal a localization law (decider probes: zero
     hits). The builder's sw-1 is TRUE at its declared cutoff; MO-4-R
     remains unoccupied at mine; T3SR occurs nowhere but the target
     pair (T4SR is a distinct commission id).
 f-3 EXTRA-KEY HITS: (i) vendored third-party library sources under
     .proof_deps/sympy/** in both roots (mpmath/sympy code mentioning
     Sobolev/polylog etc.) — library code, not corpus artifacts, typed
     out; (ii) corpus artifacts (7A_DBR_SPECTRUM, AXN_*, BR_CLOSURE,
     FIELD_CTP, TASK4A/TASK5 families, MO-2 pair, the target): probed
     for the deciding objects — ZERO of the non-MO files mention
     C_n P C_n, kappa_n, Y_n Y_n, or the crossing class; none can
     carry either decider. No deciding subclass hides under the
     builder's key choice.
 f-4 FENCED-NAME HANDLING: one cleanroom filename QUESTIONSSETTLED_
     REGISTER_V001.md surfaced in a FILENAME-level listing because my
     grep exclusion patterns were case-sensitive and missed the
     all-caps form; the file was NOT opened, no match-level line was
     taken from it, and it is excluded from every finding above.
     relay_inbox/relay_outbox plumbing filenames likewise listed
     only, unopened.
 f-5 CLEANROOM ROOT: zero hits on every residue/decider key among
     corpus artifacts (the only cleanroom hits are f-3's vendored
     library files and the f-4 filename).
```

Each sentence claims only this displayed sweep at the declared cutoff.

---

## 6. THE AUDIT CAS WORK (VERBATIM) AND OUTPUTS (VERBATIM)

One fresh venv under the session scratchpad: `mo4rauditvenv`, sympy
1.14.0, created this session and used for BOTH the verbatim re-run and
my own battery.

VERBATIM RE-RUN OF THE TARGET'S BATTERY: extracted bytes :453-597 of
the sealed target (extraction sha256
e8dfe62d43ad2ba9c17bffdfd855b5e4b05f0a834c0574ac1fe642f15fbcf7c7);
exit 0; 9/9 PASS; output diffed against the target's recorded block
(:603-613): EMPTY — byte-identical.

TOOLING DISCLOSURE, on the record: ONE pre-final check-form correction
in MY battery — AR6a as first drafted asserted
`sp.limit(sub_D4, n, oo) == oo` with epsilon_0 an UNCONSTRAINED
positive symbol, so the sign of (1 - 4 eps^2) is unknown to sympy and
the conjunct FAILed as ill-posed; it was restructured to the sign-free
divergent factor `sp.limit(4*n**R(3,2)/cG, n, oo) == oo` (the equality
display already pins the prefactor; AR1 pins its positivity for eps in
(0,1/2)). A defect of MY check-form, NOT of the builder's R6, which
never asserted that limit. No derivation content changed. The final
battery then ran ONCE: 16/16 PASS. Final script sha256
3d9e7d53fd842dcf421001712b88774b6d307d9f10891a00fdeccf8aff7af999.

```python
# MO4R-AUDIT independent battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh
# venv mo4rauditvenv under the session scratchpad). Commission T3SR.
# DEFAULT-REFUTE: every load-bearing step of STAGE8_MO4R_GRAM_MASS_T3SR_V001
# re-derived at GENERAL symbolic grade where the builder used instances,
# plus targeted attacks (polylog probe of the trichotomy; higher-rank witness;
# fresh crossing instance distinct from builder's AND ground's).
# No floats. No numeric evaluation. Every constant symbolic or exact rational.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

s, e, d = sp.symbols('s epsilon delta', real=True)

# ===== AR1 -- window identity + scalar domination, GENERAL symbolic =====
# s(1-s) = 1/4 - (s-1/2)^2; on |s-1/2| <= eps the domination coefficient
# c_eps s(1-s) - 1 = (eps^2 - (s-1/2)^2)/(1/4 - eps^2): numerator a product of
# nonnegatives on the window, denominator > 0 iff eps < 1/2; boundary-tight
# GENERALLY: gap exactly 0 at s = 1/2 +- eps (symbolic eps, not an instance).
ceps = 1/(R(1,4) - e**2)
gap = sp.simplify(ceps*s*(1-s) - 1)
num = sp.factor(sp.expand((s*(1-s) - (R(1,4) - e**2))))
ok("AR1 WINDOW+DOMINATION general: s(1-s)-(1/4-(s-1/2)^2)=0; numerator factors"
   " -(s-1/2-eps)(s-1/2+eps); gap at s=1/2+-eps is EXACTLY 0 symbolically;"
   " 1/4-eps^2>0 iff eps<1/2 (0 outside the window class)",
   sp.expand(s*(1-s) - (R(1,4) - (s - R(1,2))**2)) == 0
   and sp.expand(num + (s - R(1,2) - e)*(s - R(1,2) + e)) == 0
   and sp.simplify(gap.subs(s, R(1,2) + e)) == 0
   and sp.simplify(gap.subs(s, R(1,2) - e)) == 0
   and sp.solve_univariate_inequality(R(1,4) - e**2 > 0, e, relational=False,
                                      domain=sp.Interval.open(0, sp.oo))
       == sp.Interval.open(0, R(1,2)))

# ===== AR2 -- T-1+T-2 COMBINED at GENERAL grade: in the eigenbasis of T
# (spectral theorem), with b = M^T M PSD arbitrary and window offsets |d_i|<=eps:
# c_eps*s_i(1-s_i)*b_ii - 1_W(i)*b_ii >= 0 termwise because
# (i) b_ii = sum_k m_ki^2 >= 0 (sum of squares, GENERAL 3x3),
# (ii) on-window coefficient excess = (eps-d)(eps+d)/(1/4-eps^2), nonneg product,
# (iii) off-window addition s(1-s) >= 0 on [0,1] (product of the factors).
Mm = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'm{i}{j}', real=True))
bmat = Mm.T*Mm
diag_sos = all(sp.expand(bmat[i, i] - sum(Mm[k, i]**2 for k in range(3))) == 0
               for i in range(3))
excess = sp.simplify(ceps*(R(1,2) + d)*(R(1,2) - d) - 1
                     - (e - d)*(e + d)/(R(1,4) - e**2))
ok("AR2 CLASS DOMINATION + TRANSFER general: diagonal entries of PSD b are sums"
   " of squares (3x3 all-symbolic); on-window excess c_eps s(1-s) - 1 ="
   " (eps-d)(eps+d)/(1/4-eps^2) EXACT with s = 1/2+d -- so tr(EbE) <="
   " c_eps tr((T-T^2)b) termwise in the T-eigenbasis, all quantifiers displayed",
   diag_sos and excess == 0)

# ===== AR3 -- conjugation preserves PSD order, 3x3 GENERAL (stronger than
# builder's 2x2): tr(X^T (M^T M) X) = ||MX||_F^2 =====
Xm = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'x{i}{j}', real=True))
MX = Mm*Xm
ok("AR3 TRANSFER GROUND 3x3: tr(X^T (M^T M) X) = ||MX||_F^2 EXACT all-symbolic"
   " => A <= B implies tr(X^T A X) <= tr(X^T B X)",
   sp.expand((Xm.T*(Mm.T*Mm)*Xm).trace()
             - sum(MX[i, j]**2 for i in range(3) for j in range(3))) == 0)

# ===== AR4 -- trace positivity, 3x3 GENERAL: tr((N^T N)(M^T M)) = ||M N^T||_F^2
# => 0 <= tr(A b) <= tr A for PSD A, 0 <= b <= 1 (apply to b and 1-b) =====
Nm = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'n{i}{j}', real=True))
MN = Mm*Nm.T
ok("AR4 X-BRACKET GROUND 3x3: tr((N^T N)(M^T M)) = ||M N^T||_F^2 >= 0 EXACT"
   " all-symbolic => X_n <= kappa_n/2 through 0 <= b_D <= 1",
   sp.expand((Nm.T*Nm*(Mm.T*Mm)).trace()
             - sum(MN[i, j]**2 for i in range(3) for j in range(3))) == 0)

# ===== AR5 -- count twin + T-4 constant algebra EXACT =====
kap = sp.Symbol('kappa', positive=True)
ok("AR5 COUNT TWIN: (1/4-eps^2)^{-1} kappa/2 = 2 kappa/(1-4 eps^2) EXACT"
   " (the same constant carries mass_n and tr E_n)",
   sp.simplify(ceps*kap/2 - 2*kap/(1 - 4*e**2)) == 0)

# ===== AR6 -- assembly, trichotomy, D4 vacuity, and the POLYLOG PROBE =====
cG, CG, n, g, Cc = sp.symbols('c_G C_G n gamma C', positive=True)
ep0 = sp.Symbol('epsilon_0', positive=True)
Frac = 2*kap/((1 - 4*ep0**2)*cG*n**R(3,2))
sub_gamma = Frac.subs(kap, Cc*n**(R(3,2) - g))
sub_pow32 = Frac.subs(kap, Cc*n**R(3,2))
sub_D4 = Frac.subs(kap, 2*n**3)
sub_polylog = Frac.subs(kap, Cc*n**R(3,2)/sp.log(n))
alpha_k_polylog = sp.limit(sp.log(Cc*n**R(3,2)/sp.log(n))/sp.log(n), n, sp.oo)
ok("AR6a ASSEMBLY: gamma-ceiling => limit 0 (rate n^{-gamma}); exact-3/2"
   " ceiling => n-free; D4 2n^3 => 4 n^{3/2}/((1-4eps^2)c_G) EXACT with the"
   " sign-free divergent factor n^{3/2}/c_G -> oo (VACUOUS at power 3/2;"
   " prefactor positive and n-free for every eps in (0,1/2) by AR1)",
   sp.limit(sub_gamma, n, sp.oo) == 0
   and sp.simplify(sp.diff(sub_pow32, n)) == 0
   and sp.simplify(sub_D4 - 4*n**R(3,2)/((1 - 4*ep0**2)*cG)) == 0
   and sp.limit(4*n**R(3,2)/cG, n, sp.oo) == sp.oo)
ok("AR6b POLYLOG PROBE (audit attack on the trichotomy's middle line):"
   " kappa = C n^{3/2}/log n has alpha-k = limsup log kappa/log n = 3/2 EXACTLY,"
   " yet the T-4 ceiling STILL decides o(1) (limit 0) -- so at alpha-k = 3/2"
   " 'localization alone decides' is FALSE as universally quantified; the"
   " builder's own O-1 equivalence ('any X_n = o(n^{3/2}) certificate') already"
   " covers this subcase -- display imprecision, not verdict-chain",
   alpha_k_polylog == R(3,2) and sp.limit(sub_polylog, n, sp.oo) == 0)

# ===== AR7 -- far-class ratio EXACT + overshoot =====
Keps, Cst, eN = sp.symbols('K C_star epsilon_n', positive=True)
far = (Keps*CG*n**R(3,2)/(2*eN))/(Cst*n/8)
ok("AR7 FAR-CLASS: (K C_G n^{3/2}/(2 eps_n))/(C_* n/8) = (4 K C_G/(C_* eps_n))"
   " sqrt(n) EXACT and -> oo: even total near-class vanishing leaves sqrt(n)"
   " in the far class at the linear clock -- insufficiency scope display",
   sp.simplify(far - 4*Keps*CG*sp.sqrt(n)/(Cst*eN)) == 0
   and sp.limit(far.subs(eN, R(1,2)), n, sp.oo) == sp.oo)

# ===== AR8 -- witness pair re-derived + MY OWN higher-rank (4x4) witness =====
P2 = sp.Matrix([[R(1,2), R(1,2)], [R(1,2), R(1,2)]])
C2 = sp.diag(1, 0)
T2 = C2*P2*C2
YY2 = T2 - T2*T2
vcl = sp.Matrix([1, 0])
w2ok = (sp.simplify(P2*P2 - P2) == sp.zeros(2,2) and T2 == sp.diag(R(1,2), 0)
        and YY2 == sp.diag(R(1,4), 0) and 2*YY2.trace() == R(1,2)
        and (vcl.T*sp.diag(0,1)*vcl)[0,0] == 0
        and (vcl.T*sp.diag(1,0)*vcl)[0,0] == 1)
# my 4x4: P onto span{(e1+e2)/sqrt2, (e3+e4)/sqrt2}, C = diag(1,0,1,0)
u1 = sp.Matrix([1, 1, 0, 0])/sp.sqrt(2)
u2 = sp.Matrix([0, 0, 1, 1])/sp.sqrt(2)
P4w = u1*u1.T + u2*u2.T
C4w = sp.diag(1, 0, 1, 0)
T4w = C4w*P4w*C4w
bA4 = sp.diag(0, 1, 0, 1)
bB4 = sp.diag(1, 0, 1, 0)
clsA = (sp.Matrix([1,0,0,0]), sp.Matrix([0,0,1,0]))   # the two s=1/2 eigenvectors
massA = sum((v.T*bA4*v)[0,0] for v in clsA)
massB = sum((v.T*bB4*v)[0,0] for v in clsA)
w4ok = (sp.simplify(P4w*P4w - P4w) == sp.zeros(4,4)
        and T4w == sp.diag(R(1,2), 0, R(1,2), 0)
        and massA == 0 and massB == 2
        and bA4.trace() == 2 and bB4.trace() == 2
        and all(sp.simplify(T4w*v - R(1,2)*v) == sp.zeros(4,1) for v in clsA))
ok("AR8 WITNESS PAIR: builder's 2x2 re-derived EXACT (fraction 0 vs 1 at"
   " identical projections/clock/profile bounds); AND audit's own 4x4 rank-2"
   " witness reproduces the phenomenon (class b-mass 0 vs 2 at tr b = 2,"
   " per-vector 0 vs 1) -- the underdetermination is not a 2x2 artifact;"
   " refutes derivability-from-displayed-hypotheses only (TOY-SEPARATED)",
   w2ok and w4ok)

# ===== AR9 -- crossing algebra + commutator clock at a FRESH instance
# (distinct from builder's R3a/R4 and ground's V5/W4) =====
q1 = sp.Matrix([1, 2, 2, 0])/3
q2 = sp.Matrix([2, -2, 1, 0])/3
P4f = q1*q1.T + q2*q2.T
C4f = sp.diag(1, 0, 1, 0)
I4 = sp.eye(4)
T4f = C4f*P4f*C4f
ok("AR9 CROSSING+CLOCK fresh instance: T - T^2 = C P C^perp P C and"
   " ||[P,C]||_2^2 = 2 tr(T - T^2) EXACT (orthonormality of the rank-2 frame"
   " verified); general proofs are projection algebra + cyclicity as displayed",
   sp.simplify(q1.T*q2) == sp.zeros(1,1)
   and (q1.T*q1)[0,0] == 1 and (q2.T*q2)[0,0] == 1
   and sp.simplify(P4f*P4f - P4f) == sp.zeros(4,4)
   and sp.simplify(T4f - T4f*T4f - C4f*P4f*(I4 - C4f)*P4f*C4f) == sp.zeros(4,4)
   and sp.simplify(((P4f*C4f - C4f*P4f).T*(P4f*C4f - C4f*P4f)).trace()
                   - 2*(T4f - T4f*T4f).trace()) == 0)

# ===== AR10 -- BLOCKER-2 arithmetic: count floor => clock floor =====
cpr = sp.Symbol("c'", positive=True)
trE_floor = cpr*n**R(3,2)
kap_forced = (1 - 4*ep0**2)*trE_floor/2
ok("AR10 BLOCKER-2 arithmetic: tr E >= c' n^{3/2} + count twin tr E <="
   " 2 kappa/(1-4 eps^2) forces kappa >= (1-4 eps^2) c' n^{3/2}/2 EXACT"
   " (rearrangement); such a floor is UNSEALED (sweep) -- consistency display",
   sp.simplify(2*kap_forced/(1 - 4*ep0**2) - trE_floor) == 0)

# ===== AR11 -- O-2 yield arithmetic: n-free positive floor =====
cpp, m0 = sp.symbols("c'' m_0", positive=True)
ok("AR11 O-2 YIELD: c'' (c' n^{3/2}) m_0 / (C_G n^{3/2}) = c'' c' m_0 / C_G"
   " EXACT n-free (uses the certified Gram CEILING)",
   sp.simplify(cpp*(cpr*n**R(3,2))*m0/(CG*n**R(3,2))
               - cpp*cpr*m0/CG) == 0)

# ===== AR12 -- O-a square: (1-2T)^2 = 1 - 4(T - T^2) matrix-GENERAL =====
Tg = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f't{i}{j}'))
I3 = sp.eye(3)
ok("AR12 O-a SQUARE general: (1-2T)^2 - (1 - 4(T - T^2)) = 0 for ARBITRARY T"
   " -- the claimed display's square IS the sealed crossing identity",
   sp.expand((I3 - 2*Tg)*(I3 - 2*Tg) - (I3 - 4*(Tg - Tg*Tg))) == sp.zeros(3,3))

# ===== AR13 -- resolvent commutes with the class (functional calculus):
# diagonal exhibit + the general fact is simultaneous diagonalization =====
t1, t2, t3 = sp.symbols('t1 t2 t3')
Td = sp.diag(t1, t2, t3)
Ed = sp.diag(1, 1, 0)
Rd = sp.diag(1/(1 - 2*t1), 1/(1 - 2*t2), 1/(1 - 2*t3))
ok("AR13 COMMUTATION: (1-2T)^{-1} E - E (1-2T)^{-1} = 0 in the common"
   " eigenbasis (symbolic diagonal, general eigenvalues) -- R_n commutes with"
   " every spectral projection of T_n",
   sp.simplify(Rd*Ed - Ed*Rd) == sp.zeros(3,3))

# ===== AR14 -- profile ceiling b_D <= 1 re-derived =====
u = sp.Symbol('u', nonnegative=True)
sig = (s**2 - u)*((1 - s)**2 - u)
x14 = sp.Symbol('x14', nonnegative=True)
ok("AR14 PROFILE CEILING: d sigma/du = -[(s^2-u) + ((1-s)^2-u)] EXACT;"
   " 1/4 - s(1-s) = (s-1/2)^2 >= 0; 1/16 - x^2 = (1/4-x)(1/4+x) >= 0 for"
   " x in [0,1/4]; e^{16-16} = 1: b_D <= 1 with equality only at (1/2, 0)",
   sp.expand(sp.diff(sig, u) + (s**2 - u) + ((1 - s)**2 - u)) == 0
   and sp.expand(R(1,4) - s*(1-s) - (s - R(1,2))**2) == 0
   and sp.expand(R(1,16) - x14**2 - (R(1,4) - x14)*(R(1,4) + x14)) == 0
   and sp.exp(16 - 16) == 1)

# ===== AR15 -- the s-integral spectator: int_0^1 (const) ds = const =====
const = sp.Symbol('X_const', positive=True)
ok("AR15 s-INTEGRAL: int_0^1 tr(Y Y^dag) ds = tr(Y Y^dag) (unit-length cell"
   " integral, weight 1) EXACT -- X_n <= kappa_n/2 assembly is complete",
   sp.integrate(const, (s, 0, 1)) == const)

print("MO4R-AUDIT-BATTERY-DONE")
```

Output, verbatim (16/16 PASS):

```text
AR1 WINDOW+DOMINATION general: s(1-s)-(1/4-(s-1/2)^2)=0; numerator factors -(s-1/2-eps)(s-1/2+eps); gap at s=1/2+-eps is EXACTLY 0 symbolically; 1/4-eps^2>0 iff eps<1/2 (0 outside the window class): PASS
AR2 CLASS DOMINATION + TRANSFER general: diagonal entries of PSD b are sums of squares (3x3 all-symbolic); on-window excess c_eps s(1-s) - 1 = (eps-d)(eps+d)/(1/4-eps^2) EXACT with s = 1/2+d -- so tr(EbE) <= c_eps tr((T-T^2)b) termwise in the T-eigenbasis, all quantifiers displayed: PASS
AR3 TRANSFER GROUND 3x3: tr(X^T (M^T M) X) = ||MX||_F^2 EXACT all-symbolic => A <= B implies tr(X^T A X) <= tr(X^T B X): PASS
AR4 X-BRACKET GROUND 3x3: tr((N^T N)(M^T M)) = ||M N^T||_F^2 >= 0 EXACT all-symbolic => X_n <= kappa_n/2 through 0 <= b_D <= 1: PASS
AR5 COUNT TWIN: (1/4-eps^2)^{-1} kappa/2 = 2 kappa/(1-4 eps^2) EXACT (the same constant carries mass_n and tr E_n): PASS
AR6a ASSEMBLY: gamma-ceiling => limit 0 (rate n^{-gamma}); exact-3/2 ceiling => n-free; D4 2n^3 => 4 n^{3/2}/((1-4eps^2)c_G) EXACT with the sign-free divergent factor n^{3/2}/c_G -> oo (VACUOUS at power 3/2; prefactor positive and n-free for every eps in (0,1/2) by AR1): PASS
AR6b POLYLOG PROBE (audit attack on the trichotomy's middle line): kappa = C n^{3/2}/log n has alpha-k = limsup log kappa/log n = 3/2 EXACTLY, yet the T-4 ceiling STILL decides o(1) (limit 0) -- so at alpha-k = 3/2 'localization alone decides' is FALSE as universally quantified; the builder's own O-1 equivalence ('any X_n = o(n^{3/2}) certificate') already covers this subcase -- display imprecision, not verdict-chain: PASS
AR7 FAR-CLASS: (K C_G n^{3/2}/(2 eps_n))/(C_* n/8) = (4 K C_G/(C_* eps_n)) sqrt(n) EXACT and -> oo: even total near-class vanishing leaves sqrt(n) in the far class at the linear clock -- insufficiency scope display: PASS
AR8 WITNESS PAIR: builder's 2x2 re-derived EXACT (fraction 0 vs 1 at identical projections/clock/profile bounds); AND audit's own 4x4 rank-2 witness reproduces the phenomenon (class b-mass 0 vs 2 at tr b = 2, per-vector 0 vs 1) -- the underdetermination is not a 2x2 artifact; refutes derivability-from-displayed-hypotheses only (TOY-SEPARATED): PASS
AR9 CROSSING+CLOCK fresh instance: T - T^2 = C P C^perp P C and ||[P,C]||_2^2 = 2 tr(T - T^2) EXACT (orthonormality of the rank-2 frame verified); general proofs are projection algebra + cyclicity as displayed: PASS
AR10 BLOCKER-2 arithmetic: tr E >= c' n^{3/2} + count twin tr E <= 2 kappa/(1-4 eps^2) forces kappa >= (1-4 eps^2) c' n^{3/2}/2 EXACT (rearrangement); such a floor is UNSEALED (sweep) -- consistency display: PASS
AR11 O-2 YIELD: c'' (c' n^{3/2}) m_0 / (C_G n^{3/2}) = c'' c' m_0 / C_G EXACT n-free (uses the certified Gram CEILING): PASS
AR12 O-a SQUARE general: (1-2T)^2 - (1 - 4(T - T^2)) = 0 for ARBITRARY T -- the claimed display's square IS the sealed crossing identity: PASS
AR13 COMMUTATION: (1-2T)^{-1} E - E (1-2T)^{-1} = 0 in the common eigenbasis (symbolic diagonal, general eigenvalues) -- R_n commutes with every spectral projection of T_n: PASS
AR14 PROFILE CEILING: d sigma/du = -[(s^2-u) + ((1-s)^2-u)] EXACT; 1/4 - s(1-s) = (s-1/2)^2 >= 0; 1/16 - x^2 = (1/4-x)(1/4+x) >= 0 for x in [0,1/4]; e^{16-16} = 1: b_D <= 1 with equality only at (1/2, 0): PASS
AR15 s-INTEGRAL: int_0^1 tr(Y Y^dag) ds = tr(Y Y^dag) (unit-length cell integral, weight 1) EXACT -- X_n <= kappa_n/2 assembly is complete: PASS
MO4R-AUDIT-BATTERY-DONE
```

---

## 7. CHOICE LEDGER (every unforced choice of this audit, classified; commission T3SR)

```text
CH-A GROUND CONSUMED INDEPENDENTLY BEFORE ATTACK-COMPARISON: the pair
     A-G1/A-G2 was read in full and its displays re-verified at source
     bytes (A-S reads) rather than trusted through the target's
     citations: FORCED by the commission ("consume the same sealed
     ground independently"; "re-derive at the bytes").
CH-B GENERAL-GRADE RE-DERIVATIONS WHERE THE BUILDER USED INSTANCES
     (AR1 symbolic boundary-tightness; AR2 arbitrary-PSD-b transfer;
     AR9 fresh instance; AR8 higher-rank witness): audit-strengthening;
     IMMATERIAL(derived) to the verdict boundary — every instance the
     builder displayed was ALSO correct.
CH-C EXTRA SWEEP KEYS BEYOND THE BUILDER'S (Szego/Sobolev/localization
     law/avoidance law/eigenvalue counting/polylog/"kappa_n <="):
     FORCED by the sweep-cutoff-hiding hunt item; outcome: nothing
     hides (f-3).
CH-D POST-CUTOFF CARRIER HANDLED AT MATCH LEVEL (A-S8, seal-verified
     first): FORCED by the verify-every-input-seal clause + the scoped-
     read discipline; its three mention lines are quoted, nothing else
     consumed.
CH-E CORRECTION KEPT AT c-1/DISPLAY-GRADE rather than forcing REFUTED:
     FORCED by the menu's semantics — the trichotomy is consumed
     nowhere in the verdict chain (verified by inspection: T-1..T-4,
     the blockers, §5, and §7 never cite it), the target's own O-1
     definition already contains the repair, and no displayed
     inequality, exponent, grade, seal, or sweep finding moves.
CH-F ONE VENV FOR BOTH THE VERBATIM RE-RUN AND MY BATTERY (mo4rauditvenv,
     sympy pinned 1.14.0, created fresh this session): economy; the
     re-run executed FIRST, before my battery ever ran in it, and the
     target's battery imports nothing but sympy — no contamination
     path exists; the builder's venv was never touched.
CH-G POLYLOG PROBE SHAPE (C n^{3/2}/log n): IMMATERIAL(derived) — any
     sub-power refinement with limsup exponent exactly 3/2 works.
MACHINERY/RELEVANCE: classical operator theory (spectral calculus,
     PSD-order conjugation, trace positivity) + one countermodel
     grade; CAS exact throughout; SURFACE-DERIVED, not surface-native.
```

---

## 8. TOY_SEPARATION (self-assessment)

```text
CLAIMED CLEAN at the stated quantifiers. AR1-AR7, AR10-AR15 are
all-parameter identity/inequality exhibits with universal quantifiers
displayed in the check names (AR2's termwise domination is stated in
the T-eigenbasis the spectral theorem grants; AR13's commutation
likewise). The exact instances (AR8's 2x2 and 4x4 witnesses; AR9's
rank-2 frame) are used ONLY as (i) derivability countermodels against
universally-quantified-over-displayed-hypotheses claims — which a
single exact countermodel lawfully refutes — and (ii) identity-grade
exhibits of displayed general facts whose general proofs are displayed
(projection algebra; cyclicity). The AR6b polylog countermodel refutes
ONLY the trichotomy prose's universal at alpha-k = 3/2; it asserts
NOTHING about the record family's kappa_n, which stays an uncontrolled
symbol. No spectral datum of the actual family is valued anywhere;
kappa_n, rho_n, eps_*, ell, c_G, C_G, C_*, K(eps_*), C-dag, gamma, c',
c'', m_0 and all thresholds stay symbols. The witnesses assert nothing
about the record family's own fraction, which stays UNDECIDED of
record (the target, the ground pair, and this audit all hold that
line). The RULING on consumption is the registrar's, not this
artifact's.
```

---

## 9. FLAG BLOCK

```text
MO4R_AUDIT = CONFIRMED-WITH-CORRECTIONS( headline verdict
  UNDECIDABLE-TODAY adjudicated CORRECT AT THE BYTES: the reduction
  T-1..T-4 re-derived at general symbolic grade (AR1-AR6, AR14-AR15);
  the blockers re-verified as genuine absences by an independent sweep
  at a LATER cutoff with EXTRA keys (f-1); the witness-pair forcing
  argument re-derived and STRENGTHENED to rank 2 (AR8); the far-class
  scope display exact (AR7); the decider menu arithmetic exact (AR10/
  AR11). ONE correction c-1, display-grade, non-verdict-chain: the §6
  trichotomy middle line "alpha-k = 3/2 ==> localization alone
  decides" is false as universally quantified (AR6b polylog
  countermodel: kappa_n = C n^{3/2}/log n); exact repair displayed in
  §3; the target's own O-1 equivalence clause already covers the
  subcase. Per-claim: K-1..K-16 all CONFIRMED (K-12 carries c-1). )
HUNT_RESULTS = ALL-NEGATIVE( rate-without-certificate: NONE (every
  rate conditional on a named certificate or graded given-CL-A);
  o(1)-assuming-the-conclusion: NONE (no o(1) claimed; the reduction
  is one-sided and says so); lower-bound-with-gap: NONE (no lower
  bound claimed; NOT-o(1) correctly refused); sweep-cutoff-hiding:
  NONE (extra keys + later cutoff found no hidden decider; the one
  post-cutoff carrier is boundary-only, f-2); refused-mechanism-
  consumption: NONE (T-1..T-4 consume the sealed profile split +
  classical spectral/trace positivity only; the four refusals of
  record are cited, never re-walked, never consumed); hidden numerics:
  NONE (fence-scan §4 clean). )
FENCE_SCAN = NO-FENCE-HIT( no computed alpha/kappa/coupling/scale
  value; no measured-constant comparison; no float as ground; the two
  regex hits are the sympy version string and a TOY-SEPARATED 2x2
  exact-rational witness value. alpha_computed = false ;
  proof_authorized = false ; kappa_record_computed = false — all held
  by the target and by this audit. )
SWEEP_DELTA = ONE-BOUNDARY-CARRIER( STAGE8_WALL_LOGFREE_RATE_T4SR_
  V001.md, sealed 03:03 CDT (after the builder's 02:57:40 cutoff),
  seal verified: mentions MO-4-R at :255/:348/:736 as boundary/
  consistency only; claims neither the residue nor either decider;
  the builder's occupation and absence claims hold at both cutoffs;
  T3SR occurs nowhere but the target pair. Noted for the registrar. )
CONSUMPTION_BOUNDARY = NOTHING-FIRED( MO-4 stays open; MO-4-R stays
  open and stands REDUCED exactly as the target claims; r-3, R-L4b,
  F-d, all witnesses of the sealed stock untouched; no flag of any
  prior artifact flips; H-R never defaulted (re-verified: rho_n in no
  display of the target as a bound); whether to commission O-1 or O-2
  is the registrar's call, not made here. )
SEALS_VERIFIED = 11/11( target 299b68d1; ground pair 0e894372 +
  bfd86217 read in full; A-S1..A-S7 seal-verified before match-level
  reliance; post-cutoff A-S8 seal-verified before its three lines were
  read. Builder's battery re-run VERBATIM: exit 0, 9/9 PASS, output
  byte-identical (extraction sha256 e8dfe62d...). "Q-..." tokens:
  none encountered beyond the ground pair's own notations. )
SWEEP_CUTOFF = 2026-08-15 03:14:29 CDT( keys and findings §5; both
  commissioned roots; fenced name classes never opened (one all-caps
  REGISTER filename listed at filename level only, f-4); no sub-3/2
  clock-ceiling carrier; no localization-law carrier; cleanroom corpus
  zero hits; vendored .proof_deps library files typed out. )
FORBIDDEN_IMPORTS = none( exact symbolic only; no floats as ground; no
  numeric evaluation; no measured constant; NO value frozen — every
  constant and threshold symbolic or an exact rational; symbolic
  scales only; no fenced-class file opened; no git action; no existing
  file edited; ONE file written plus its seal; output path probed
  ABSENT at session start and re-probed immediately before write. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv mo4rauditvenv
  under the session scratchpad; the target's battery re-run verbatim
  FIRST (9/9 PASS, diff empty); my battery final run ONCE, 16/16 PASS,
  script and output reproduced verbatim in §6; one pre-final
  check-form correction of MY OWN (AR6a sign-free restructuring)
  disclosed in §6; no derivation content changed.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
ALL_RESULTS of the target = CLAIMED -> now AUDITED at this artifact's
grade; the registrar consumes.
MO4R_AUDIT_RESULT = SEALED.
```
