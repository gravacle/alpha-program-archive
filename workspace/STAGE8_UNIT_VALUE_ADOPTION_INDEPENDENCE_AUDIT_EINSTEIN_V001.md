# Unit-Value Adoption Independence Audit (EINSTEIN) V001

LANE: EINSTEIN. CHARTER: paste 159. DATE: 2026-07-30. REGISTER HEAD AT START: Q-55.
**REGISTER MOVED MID-RUN: Q-56 landed during verification (Codex 1, paste 160, theory-001 closure
retype). REPORTED PER THE CHARTER, §6 — it is COUNT-NEUTRAL for this item, and that neutrality was
pre-registered by this audit before Q-56 landed, then confirmed against the landed text.**
STATUS: LANE FINDING. No register row written — rows are the reviewer's.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
F-GK3 DECLARATION AT THE OUTSET: NO premise beyond the sealed stack is used anywhere in this
audit. (The verification recomputed only sealed algebra already displayed in the audited
artifacts — quote checks, not new computations.)
FITTING FENCE HELD: tau_R was evaluated against NOTHING measured; NO inference about alpha is
drawn from pi/sqrt(2) anywhere in this artifact. It is a cell-time integral.
FENCES HELD: response-extraction layer, correlator-to-Hessian map, scalarization bridge, Gamma_K
untouched (fresh searches surfaced Gamma_K hits; NOT OPENED). S8 cited via Q-55, not reopened.
a32_holdout/custodian_private/ not opened.
```

---

## §0 — THE FOUR ANSWERS

```text
NO LEAD-WITH CONDITION FIRES: THE PROGRAM DOES *NOT* HAVE MORE THAN ONE UNSELECTED ABSOLUTE
SCALE. It has EXACTLY ONE (C_R = 1) — and separately, MORE THAN ONE unselected UNIT VALUE,
which are different findings and must not be conflated (§4).

1. INVENTORY: the five objects are NOT the same kind of quantity. Kind-split 2/1/2:
   KIND A (dimensionless adopted unit values): C_R = 1, tau = 1.
   KIND B (derived dimensionless number with a second, ADOPTED role): tau_R.
   KIND C (dimensionful proper intervals): T_R (derived-conditional), tau_orth (untested
   identification).
2. INDEPENDENCE: they are NOT one choice appearing five times, and NOT five choices. Exactly ONE
   fully dependent pair: T_R IS DERIVED FROM C_R = 1. Net resolution: TWO unit-value choices
   (C_R = 1, tau = 1) + one derived number (tau_R) + one derived-conditional consequence (T_R) +
   one untested identification (tau_orth), with ER-A as a third scale-bearing ADOPTION closing
   tau_R's deployment.
3. COUNT (by branch of the two live conditionalities — the charter's own requirement):
   [tau_orth = T_R HOLDS, ER-A IN FORCE]   2 unit-value scale choices; 3 scale-bearing adoptions
                                           counting ER-A; 4 counting the row-6 budget-saturation
                                           adoption inside the T_R marker chain.
   [HOLDS, ER-A SUPERSEDED]                3 genuinely free — A_c reverts to an explicit external
                                           parameter (Q-55: "Without ER-A, covariance leaves A_c
                                           external") unless the derived-S8-law release fires.
   [FAILS, ER-A IN FORCE]                  2 (3/4) choices PLUS tau_orth as an additional
                                           UNDETERMINED open scale quantity (TYPE-U — the
                                           durable-record generator is unbuilt).
   [BOTH FAIL]                             up to 4 open scale quantities.
   THE NUMBER IS DETERMINATE ON SEALED TEXT AT THE OBJECT LEVEL; the single residual
   indeterminacy is N2 (§5), TYPE-S with the search stated.
4. ABSOLUTE SCALE: *** Q-45 SURVIVES — NOT REFUTED. C_R = 1 IS THE ONLY ABSOLUTE-SCALE
   SELECTOR, IN EVERY BRANCH. *** With two recorded caveats (§4) — including that the sole
   selector HAS NOT YET SELECTED (conditional formula only).
```

---

## §1 — QUESTION 1. THE INVENTORY, TYPED. (What each object IS.)

```text
C_R = 1    A dimensionless compactness RATIO at its threshold value. ADOPTED — the Level-1
           marginal public-closure rule; "This fourth input is the selector"
           (BID_MINIMAL_PUBLIC_CAUSAL_CELL:49-60). Premise row 1, UNTESTED. A falsifier is now
           DESIGNED but NOT FROZEN (lane 2's reconciliation artifact; LANE_STATUS:353).
tau = 1    The dimensionless unit VALUE of the proper-time floor — the lower endpoint of V011's
           admitted positive-tau competitor family (V011:1094-1105: neither the unit interval nor
           the unit amplitude power follows). ADOPTED, "a frozen target-independent branch
           convention, not a theorem". Row 7, UNTESTED — COMPETITOR OPEN (Q-49).
tau_R      = pi/sqrt(2). A dimensionless action/evolution INTERVAL with TWO EPISTEMIC ROLES:
           (i) DERIVED as the least positive common orthogonality zero — conditional on
           |w| = 1 (tau_R(w) = pi/(sqrt(2)|w|), FIRST_OPENING:94), with the coefficient fixed by
           the compression identity (UNIQUE_CHARGED:205) — "a check rather than a normalization
           premise"; (ii) ADOPTED as ER-A's amplitude clause fixing S8's integrated write action
           (Q-55, TYPE-C). ONE NUMBER, TWO ROLES; *** THE ADOPTION IS WHAT BINDS AT S8. ***
T_R        A Lorentz-scalar dimensionful proper interval — the only Planck-unit quantity of the
           five. Its value 2 sqrt(pi) t_P is a DERIVED FORMULA CONDITIONAL ON C_R = 1 (§4), not
           an independent adoption. absolute_record_interval_derived_in_declared_branch = false.
tau_orth   A dimensionful comparator orthogonalization time (|Delta| tau_orth / hbar = pi).
           tau_orth = T_R is a CYCLE-6 CONDITIONALITY, REGISTER-ONLY, row 22 UNTESTED, with a
           DISCLOSED CONFLICT: the binary gate says "tau_orth is not yet tau_record" and
           durable_record_condition_established = false. Its very existence as an exact zero
           depends on the underived balanced calibration p = 1/2.
```

**Sweep additions, dispatched:** `N_BR(k_R) = 1` **EXCLUDED** from the unit-value class — its "1"
is a **cardinality** (a count of records at onset), `ADOPTED_PHYSICAL_CONSTRAINT_NOT_UNIT_
DEFINITION`, and the split correction removed its prior ONSET+OTHER misattribution. `c = 1` (unit
amplitude power): its fixing mechanism is declared but unbuilt — and a possible misreading is
killed as **N7 [TYPE-R]** (§5): the primitive attenuation multiplier = 1 is *conditionally
derived*, so c = 1 is not a second free unit value. `K_bare = 0`: a zero-value adoption, row 2
TESTED — PENDING. `s = pi` and `xi = 1/2`: load-bearing normalizations **inside** the C_R route's
own deformation control — orbit-invariant (they set the coefficient, not the scale; §4).
**Two disclosed tensions reported, not repaired:** winding `k = +1` (Stage-1 says DERIVED; ER-A's
inherited-stack register lists it as imported) and fidelity multiplicity = 1 (retired from the
alpha path yet still listed in ER-A's inherited stack).

---

## §2 — QUESTION 2. THE INDEPENDENCE MATRIX. (Typed; name-matching used nowhere.)

```text
[C_R , tau]       INDEPENDENT CHOICES. Separate adoption rows (1 vs 7); no sealed derivation
                  links them; fixing either leaves the other's competitor family open.
[C_R , tau_R]     INDEPENDENT — AND ONLY ONE IS A CHOICE. tau_R's derivation contains no
                  compactness input (verified: FIRST_OPENING:55-98 has no C_R appearance).
[C_R , T_R]       *** DEPENDENT, BY DERIVATION. C_R = 1 SELECTS; T_R IS SELECTED. ONE CHOICE,
                  NOT TWO *** (conditional on the open energy identification, §4). Counting them
                  separately would double-count one selector.
[C_R , tau_orth]  Linked only THROUGH T_R under untested row 22. No direct sealed relation.
[tau , tau_R]     UNDETERMINED — NO_VERDICT (N3, TYPE-U): no sealed deformation control tests
                  tau_R under tau != 1. Treated as independent-as-far-as-sealed-text.
[tau , T_R]       INDEPENDENT ON SEALED TEXT. Fixing T_R does not select the unit tau; row 7
                  stays open either way. (Near-miss recorded: the CAR primary source's
                  tau = s k_R^2 coordinate link — but "the chain is not built" and k_R has one
                  substantive occurrence in the cleanroom; the typing stands.)
[tau , tau_orth]  No sealed relation found (search stated in N-set).
[tau_R , T_R]     DISTINCT KINDS WITH A SEALED CONVERSION: tau_R is the dimensionless
                  action-length OF the interval whose duration is T_R (tau(t) = tau_R t / T_R;
                  H_physical = (hbar tau_R / T_R) B). FIXING ONE DOES NOT FIX THE OTHER; NEITHER
                  IS A FREE CHOICE.
[tau_R , tau_orth] Same KIND (first-orthogonalization times), different generators and gates; no
                  sealed relation between their VALUES. NOTE A KIND-MISMATCH INSIDE ROW 22'S OWN
                  CONFLICT: the register identifies tau_orth with the DIMENSIONFUL T_R, not the
                  dimensionless tau_R.
[T_R , tau_orth]  THE LIVE CYCLE-6 CONDITIONALITY ITSELF. Register-only, UNTESTED, conflicting
                  gate text. NO_VERDICT (N6); both branches carried through §3's count.
```

**The standing conditionality `tau_orth = T_R` was CHECKED, not assumed** — it is arithmetically
`|Delta| = E_R`, its promotion is blocked by the binary gate, and it carries no premise-failure
rule (that absence is itself typed, N3/N6).

---

## §3 — QUESTION 3. THE COUNT. (Given in §0; defended here.)

Derivation of the number: DERIVED objects are not choices (tau_R role (i), T_R's formula);
ADOPTED-with-open-competitor objects are (C_R = 1, tau = 1, ER-A's clause, row-6 saturation);
CONDITIONAL identifications collapse or split by branch (tau_orth = T_R; ER-A in force vs
superseded). All four branches enumerated in §0.3. **Exactly ONE object is absolute-scale-bearing
in every branch.** The verification re-derived the ER-A clause's orbit-invariance independently
(`integral (tau_R/T_R) w((t-t_c)/T_R) dt = tau_R`, T_R-independent — so the clause fixes a
dimensionless action, not a scale).

---

## §4 — QUESTION 4. THE ABSOLUTE-SCALE TEST. **Q-45 SURVIVES.**

The record layer alone has **no** absolute scale: the sealed scale orbit
`T_R -> lambda T_R, H_R -> H_R/lambda` leaves every observable unchanged (QSPEC audit §1).
`T_R = 2 sqrt(pi) t_P` comes from three equations — `E_R T_R = pi hbar`, `R_R = c T_R / 2`,
`2 G E_R/(c^4 R_R) = 1` — **and the third IS C_R = 1.** The deformation control
`T_R/t_P = sqrt(2s/(xi chi_*))` makes the roles exact: `s = pi` and `xi = 1/2` are
**orbit-invariant** (a product and a shape ratio select nothing); **only `chi_* = C_R = 1` breaks
the orbit** — *"Without it, public recoverability gives a half-line of allowed durations and no
absolute record scale"* (MPCC:59-60). `E_R` has **no independent fixing** (`E_R = pi hbar / T_R`
enters by substitution): the "something fixing E_R" **is** C_R = 1.

```text
=> T_R IS NOT A SECOND ABSOLUTE SCALE. IT IS C_R = 1's CONSEQUENCE.
=> tau = 1 is dimensionless and orbit-invariant: A SECOND UNSELECTED UNIT VALUE, NOT A SECOND
   ABSOLUTE SCALE. (Lane 2's open flag TAU_EQUALS_1_SECOND_SCALE_CARRYING_ADOPTION_RULED = false
   is hereby answered IN THE NEGATIVE for the absolute-scale sense — a lane finding, for the
   reviewer to register.)
=> tau_R is derived and dimensionless; its physics deployment RIDES ON T_R via the sealed
   conversion. tau_orth carries scale only through untested row 22.
=> ADDITIONAL SEALED CORROBORATION (verifier's find): lane 2's reconciliation artifact :262 —
   "it is the corpus's ONLY absolute-scale selector."
TWO CAVEATS, RECORDED NOT SOFTENED:
  (i)  THE SOLE SELECTOR HAS NOT YET SELECTED. absolute_record_interval_derived = false; the
       energy identification (CTP record-action energy = gravitating energy) is an unbuilt
       interface, and admitted finite-boundary energy alternatives move T_R by sqrt(2) AT
       marginality. C_R = 1 currently selects a CONDITIONAL FORMULA, not a consummated scale.
  (ii) "ONLY ABSOLUTE-SCALE SELECTOR" MUST NOT BE READ AS "ONLY UNSELECTED UNIT VALUE." The
       latter reading would be FALSE: tau = 1 is a second unselected unit value of a different,
       non-absolute kind. Q-45's formulation invites the conflation; this audit closes it.
```

**Consequence for a computed alpha, one sentence per outcome (nothing computed):** under the
actual situation — ONE unselected absolute scale — a computed alpha could honestly claim to be a
conditional derivation, exact modulo one named untested absolute-scale adoption (C_R = 1) plus
disclosed dimensionless unit/branch adoptions (tau = 1, ER-A, saturation), each independently
testable. Under MORE THAN ONE unselected absolute scale, it could claim only that some point in a
multi-parameter family reproduces the number — **operationally indistinguishable from a fit.** The
program is in the first case, **conditionally on the §4 caveats.**

---

## §5 — TYPED NEGATIVES (Q-54), COMPLETE

```text
N1 [TYPE-S] NO SECOND ABSOLUTE-SCALE SELECTOR EXISTS IN SEALED TEXT. Roots: all three;
   exclusions: custodian_private, Codex 2's fenced layer; queries: "absolute scale", "selector",
   "t_P", "2 sqrt(pi)", "E_R T_R", "Record-Interval Authority" (+ verifier's fresh patterns:
   "selects the absolute", "fixes one SI", "unique SI"). Hits: only the C_R = 1 route; V009's
   Record-Interval Authority and the identifiability gate are OBLIGATIONS on a future supplier
   ("the existence of the Planck time does not by itself select T_R = t_P"), not selectors.
N2 [TYPE-S] WHETHER tau = 1 AND THE ER-A AMPLITUDE CLAUSE ARE ONE NORMALIZATION CHOICE IS
   UNDETERMINED. Queries: "tau = 1" x "amplitude clause"/"A_c"/"envelope" cross-greps, all roots;
   no sealed sentence identifies or separates them. THE "2 UNIT-VALUE CHOICES" COUNT CARRIES THIS
   CAVEAT: it could compress to 1 if a future derivation identifies them.
N3 [TYPE-U] tau = 1 vs tau_R dependence: no tau-floor deformation control exists for tau_R's
   deployment. NO_VERDICT on the pair; independent-as-far-as-sealed-text.
N4 [TYPE-C] A_c = tau_R closed by ER-A's disclosed clause, not by physics. Constraint: ER-A
   adoption; release: derived alternate parent branch or supersession. CITED from Q-55 verbatim;
   S8 not reopened.
N5 [TYPE-U] T_R's ABSOLUTE VALUE: the energy identification is an unbuilt interface; NO_VERDICT
   on T_R's physical value — even the sole selector has not yet selected.
N6 [NO_VERDICT] tau_orth = T_R: row 22 UNTESTED, register-only, disclosed conflict; both
   branches rest on the unbuilt durable-record generator (TYPE-U). Count given for both.
N7 [TYPE-R — against a possible misreading, not a sealed claim] c = 1 IS NOT A SECOND FREE UNIT
   VALUE: the primitive attenuation multiplier is conditionally derived ("No independent positive
   real multiplier survives"), consistent with V011:1104's Fubini-Study-only scope.
```

---

## §6 — REGISTER MOVEMENT, REPORTED PER THE CHARTER

**Q-56 landed mid-verification** (register :2330; Codex 1 under paste 160: zero of theory 001's
five closures are unconditional TYPE-R; closure 3 rests on the ADOPTED retraction axiom;
K_bare = 0 typed TYPE-C). **BEARING ON THIS ITEM: COUNT-NEUTRAL.** This audit pre-registered
exactly that neutrality before Q-56 landed — tau = 1's SOURCE functional becomes
adoption-on-adoption, but its VALUE was already counted as a free unit-value choice — and the
landed text confirms it. Q-56 introduces no new scale object and no second absolute-scale
selector. (Also noted: paste 160 confirms this inventory is fenced to Einstein; no lane
collision.)

## §7 — METHOD AND DISCIPLINE

```text
Three tasks (inventory / pairwise matrix / count-and-absolute), EACH adversarially verified:
CONFIRMED, CONFIRMED, WEAKENED-on-perimeter-only (register currency + two near-miss unit values —
"fidelity action k = 1", "winding k = +1" — added as TYPE-S for scale content, changing no
count). All 28 evidence quotes of the count task verified at source; the T_R algebra recomputed
as a quote check (2 sqrt(pi) exact); the orbit argument re-derived independently by the verifier.
This lane ALSO verified the decisive §4 chain directly, before the workflow returned.
Every DEPENDENT matrix cell carries a deriving chain at file:line; every INDEPENDENT cell rests
on sealed text, not name-difference; every negative is typed with roots/exclusions/query where
TYPE-S. One artifact relied on carries a known erratum discipline (nothing here relies on a
retracted section).
```
