# STAGE 8 — ADVERSARIAL AUDIT OF THE WALL'S BOUNDARY-CLOSURE ROUTE ATTEMPT (STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md, 80db260f)

## INDEPENDENT ADVERSARIAL AUDITOR — CODENAME WBC-AUDIT — COMMISSION S9AD — [SEALED]

Date: 2026-08-14
Role: independent adversarial auditor of the boundary-closure route build.
Did NOT write the build; verdict NOT known before the protocol below.
DEFAULT POSTURE: REFUTE; this audit's verdict governs.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS of this audit = CLAIMED until the
registrar's check.

Fences held: EXACT SYMBOLIC ONLY — my own CAS battery (sympy 1.14.0, fresh
venv `wbcvenv` under the session scratchpad — NAME COLLISION with the
build's venv is coincidental, both derive from the WBC codename; mine was
created fresh this session before the build was opened) plus a SECOND fresh
venv `rebuildvenv` for the verbatim re-run of the build's battery; every
constant symbolic or an exact rational/surd; nothing numeric evaluated as
ground; no measured constant; no value frozen. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-..." tokens
EXPECTED-UNLOCATABLE, not chased. No git; no register/commit/push; no
existing file edited; ONE output file (this artifact) plus its seal sidecar
at the commission-distinct path, probed ABSENT twice (session start and
immediately before write). Every exhaustiveness sentence below claims only
its own displayed sweep.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — the build's FAILS-AT verdict (F1 rank x op conversion ceiling; F2 the adopted diagonal-locus ruling; F3 the uncertified C-L2 error layer), its NO-single-composite-kill answer for the odd sector, its NOT-REFUTED discipline (all four quantities UNDECIDED, no divergence exhibited), its graveyard distinctness, and its around-not-through handling of the sealed obstruction ALL SURVIVE independent hostile examination — my own route attempt, FIXED AND HASHED BEFORE OPENING THE BUILD (note 2302dea9, battery 37baacd9, 7/7 PASS), reached the SAME failure class at the SAME three stops (stay-strata shortfall untouched by every closure asset; C-L2 absent; rank x op trap the only conversion) and independently derived the build's WB3 same-phase observation (the (+sqrt2,-sqrt2) pair has phase product +1) before reading it; the build's CAS battery re-ran BYTE-IDENTICAL (8/8 PASS, fresh venv rebuildvenv); the delivered op-grade enclosure ||K_H,n||_op <= 2, ||H_A,n||_op <= 2 is VERIFIED sound at every step (b_D <= 1 with unique attainment re-derived independently as my A5; TD1's S_mu = int_0^1 W^dag J W dt verified at 798de0dd bytes :531-534/:817; C-contraction sealed at 52f2490b LINK 1 via r-3 :135; rank <= 4n^3 of record) and none of my eight pre-registered defect patterns (D1-D8) materialized — the neutrality clause is HELD (grep + full-read: thesis language occurs only in the clause header and compliance declarations; F1/F2/F3 each rest on sealed text); ONE CONFIRMED byte-level CORRECTION: the prose splitting display "tr K_n = -i tr[U(S_- - S_+)C] + i tr[S_+[C, U]]" carries a SIGN ERROR on term two at five instances (:48, :197, :360, :425, :839) — the correct form is -i tr[S_+[C, U]] (equivalently +i tr[S_+[U, C]]), CAS-verified both directions, and the build's own WB4 proves exactly the correct pre-factor identity, so the error is transcription-only and VERDICT-NEUTRAL (every use of the splitting is structural: term two routes through [C, U] and vanishes at U = I, both sign-invariant); plus three wording/grading corrections (the flag block's "F1 = MO-3 exactly" overstates the build's own correct ":319 MO-3 shape" — MO-3 of record is the Delta_n(a) rate, F1's escape object is the same SHAPE for the dressed parity composites; CN-1/CN-2's carrier "Bearing" sentences are shape-class analogies not byte-level identities, CN-2 weakest — R1's J-II member evaluation would not obviously touch the spine quantities — mitigated by the section's explicit SUBSIDIARY marking and by CN-3 being genuine at bytes; F2's consumption of the R5 locus ruling extends it beyond its native C6/Q2 context — same kernel class, class-level transfer, and the build itself flags it "sealed text, not an estimate" — so F2 should be graded CORROBORATING rather than independently sufficient, which changes nothing: F1 and F3 are EACH independently sufficient for FAILS-AT); the wall stands where the build leaves it: the four quantities UNDECIDED, the odd sector's single-composite kill nonexistent on the sealed basis, the route's residue = the MO-3-shaped sub-volume trace/HS rate + the C-L2 certification + (subsidiary) the R10 magnitude junction.**

---

## 1. INDEPENDENCE DISCLOSURE, WITH HASHES

Protocol executed in order, on the record:

```text
STEP 1  Sealed stock read FIRST (build unopened): d66a922c (KCH, FULL);
        46846730 at :330-364 (C4/C6+R5 locus), :735-766 (R.2), :1125-1168
        (C-L1/C-L2); 789338ad :90-134 (b_D bytes); b4034f04 (FULL);
        3b5e95b6 (FULL); bebc0f08 at :30-110, :330-474 (+ targeted greps);
        ae2202e0 (FULL); ee91d85c (FULL); 798de0dd at TD1/TD2 bytes
        (:219-220, :531-538, :817-818) after the build read, to verify the
        load-bearing S_mu form; audits 4ebe6b8f/a4cf394c/72c95d42 at §0.
STEP 2  MY OWN ROUTE ATTEMPT worked and MY VERDICT FIXED in
        wbc_prebuild_note.md, hashed BEFORE the build was opened:
          2302dea9a38386ca266e2640cac04c20b4c65d69fe815a31a4c273e01ef62a3e
            wbc_prebuild_note.md
          37baacd92aaf020bdf7aa09a37472fedea15c1b7d19062ef6eb0374f8d233fc7
            wbc_audit_battery.py   (my battery, 7/7 PASS, fresh venv)
        (scratchpad files; timestamp 2026-08-14 23:30:56 UTC; the build
        was first opened after this hash was taken.)
FIXED PRE-BUILD VERDICT (verbatim class from the note): "THE ROUTE CANNOT
        DELIVER THE ENCLOSURE ON THE SEALED BASIS. The honest outcome
        class for a correct build is FAILS-AT / NOT-DERIVABLE-TODAY /
        CARRIERS-NEEDED", stoppage at (i) the stay-strata shortfall
        untouched by every closure asset (promotion = weight-sum, absent
        from the composite; monodromy/saturation = anti-decay; b_D =
        op-grade only; kappa_n and the R_n ceiling = divergence
        certificates); (ii) C-L2 absent; (iii) magnitude landings
        vacancy/theorem-barred. Allowed positives: identity-layer
        deliverables only, "never VALUES, never an n-uniform majorant"
        OF THE FOUR TASKED QUANTITIES (trace/HS grade).
        Pre-registered defect patterns D1-D8 (neutrality-as-ground;
        rank x op re-entry as a RESULT; weight-sum kill transported;
        V-2 bridge; R_n defaulted; fixed-n regions; onset budget
        substituted; divergent quantities consumed as controls).
STEP 3  The build read ONCE in full; then attacked at bytes (greps,
        byte-verification of every load-bearing quote, verbatim battery
        re-run in a second fresh venv, independent CAS on the disputed
        sign).
CONCURRENCE: my fixed verdict and the build's verdict agree in CLASS
        (FAILS-AT) and at every named stop; my (i) = its F2+§3.2 sweep,
        my (ii) = its F3, my (iii)-trap = its F1 (displayed as a failure,
        not committed as a result), my vacancy/theorem bar = its CS-8.
        The build's WB3 same-phase observation was in my pre-fixed note
        (A3) independently. NONE of D1-D8 materialized. The build's
        delivered op-grade enclosure does not contradict my "never an
        n-uniform majorant" clause — that clause was scoped to the four
        tasked trace/HS quantities, which the build (ledger C-1) refuses
        to claim; the op-grade layer is the identity/structure-grade
        deliverable my note allowed.
```

---

## 2. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`; every
digest recomputed from bytes at path THIS session by full digest and
compared against sidecar and tasked prefix. 16/16 MATCH; none unverifiable.

```text
80db260f  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md      MATCH (THE BUILD; sidecar)
d66a922c  STAGE8_K_CHANNEL_CONTROL_V001.md               MATCH (sidecar + tasked)
4ebe6b8f  STAGE8_K_CHANNEL_CONTROL_AUDIT_V001.md         MATCH (sidecar + tasked)
798de0dd  STAGE8_SECOND_ORDER_TOTAL_CONTENT_V001.md      MATCH (sidecar)
c944a901  STAGE8_SECOND_ORDER_TOTAL_CONTENT_AUDIT_V001.md  MATCH (sidecar)
46846730  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md    MATCH (sidecar + tasked)
789338ad  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (sidecar + tasked)
b4034f04  STAGE8_C4_SATURATION_STRUCTURAL_BLIND_ANSWER_V001.md  MATCH (sidecar + tasked)
ae2202e0  STAGE8_EARN_THE_TRACE_S9AD_V001.md             MATCH (sidecar + tasked)
a4c2b7df  STAGE8_EARN_THE_TRACE_S9AD_AUDIT_V001.md       MATCH (sidecar + tasked)
ee91d85c  STAGE8_GRAVITY_CLOSE_SCORING_V001.md           MATCH (sidecar + tasked)
3b5e95b6  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md   MATCH (sidecar + tasked)
a4cf394c  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_AUDIT_V001.md  MATCH (sidecar)
bebc0f08  STAGE8_R3_JOINT_RATE_S9AD_V001.md              MATCH (sidecar + tasked)
72c95d42  STAGE8_R3_JOINT_RATE_S9AD_AUDIT_V001.md        MATCH (sidecar)
(the r-2/r-3 pair located by tasked digest prefix, then sidecar-verified —
the same method the build discloses)
```

Pins chased at bytes this session: the R5 locus refinement (46846730
:359-364, verbatim as the build quotes it); C-L2's exclusion clause
(:1137-1144); the b_D display (789338ad :99-113); TD1's S_mu form
(798de0dd :531-534, output :817); r-3's LINK 1/LINK 2 citations (bebc0f08
:135-136); r-3's kappa ceiling 2n^3 (:349) and D10 ratio (:353, :710); the
KCH audit's 3/2-gap correction (4ebe6b8f :41, :301-312, :778, :1040); the
scoring artifact's missing_carrier strings for R1/R5/R10 (ee91d85c §2.1/
§2.2/§2.3, byte-compared against the build's §6 quotes — VERBATIM MATCH).

---

## 3. MY INDEPENDENT PASSES (fixed pre-build; battery 7/7 PASS, verbatim in §7)

```text
A1  channel identities re-derived (4x4 generic): Re tr[K^2] = ||K_H||_2^2
    - ||H_A||_2^2; Re (tr K)^2 = (tr K_H)^2 - (tr H_A)^2. PASS.
A2  threshold arithmetic re-run (Lambda formal): trace needs symbol degree
    < -3; stay stratum (deg -1) is 2+ powers short; HS^2 of deg -1 linear
    in carrier. PASS.
A3  THE SAME-PHASE OBSERVATION (independently derived, pre-build): at full
    tau_R the monodromies are (+1,-1,-1) exact and the (+sqrt2,-sqrt2)
    cross pair has phase product +1 — R.2's -2P display does NOT attach to
    the pair K_n is built on; its baseline is V(0) = I up to the C-L2
    error. PASS. (= the build's WB3, reached blind.)
A4  saturation closed form S(tau) = sin^2(sqrt2 tau/2), S(tau_R) = 1 =
    sum|w| — the l1 step TIGHT: an ANTI-enclosure statement. PASS.
A5  b_D profile: s <= 1/16 on the diamond with equality only at (1/2, 0),
    so b_D <= 1 attained only at the center — an OPERATOR-grade sup. PASS.
    (= the build's WB1, independent derivation via the same gap algebra.)
A6  the poisoning ceiling: min_i d_i >= prod_i d_i on (0,1], so ||R_n|| <=
    exp(-log|det_n(0)|), divergent like e^{+kappa_n}: the closure
    structure certifies divergences, not enclosures. PASS.
A7  (my addition, corroborating the build's F2 without the locus ruling)
    endpoint-flat time profiles give decay ONLY in the energy transfer:
    int_0^1 f e^{iwt} = -(1/(iw)) int f' e^{iwt} exactly (iterable), while
    at the stay/diagonal channel w = 0 the SAME integral is a constant
    (1/30 for the displayed f) — NO closure smoothness mechanism reaches
    the stay strata. PASS. This is an independent, ruling-free ground for
    the F2 conclusion at the mechanism level.
```

---

## 4. THE ATTACK, PRIORITY BY PRIORITY

```text
P1 NEUTRALITY (attacked hardest). Grep over the build for closure-as-
   ground language ("closure must|should|has to", "must bound|enclose|
   discipline", "disciplin", "the thesis", "motivating", "gravity"):
   hits ONLY at :19-23 (the clause header itself), :39 ("Homonym
   discipline"), :779 and :893 (compliance declarations), :444/§6 (the
   scoring artifact's name). Full-read check of every DERIVED step: the
   op-grade chain rests on b_D/unitarity/C-contraction (sealed); F1 on
   r-3's proven arithmetic; F2 on the adopted R5 text quoted verbatim;
   F3 on E1's own witness. No step consumes the motivating reading.
   NEUTRALITY: HELD.
P2 RE-DERIVATION OF EVERY CLAIMED ENCLOSURE. (i) ||J(t)||_op <= 1: sealed
   (r-3 P-2 D5/D6/D7) + my A5. (ii) ||S_mu||_op <= 1: TD1 verified at
   798de0dd bytes — S_mu = int_0^1 W^dag J W dt, W unitary, UNIT time
   extent; Bochner triangle + unitary invariance; SOUND. (iii) ||K_n||_op
   <= 2: C-contraction sealed (52f2490b LINK 1 via bebc0f08 :135,
   sup_n ||C_n|| <= 1 both schemes), U unitary, triangle; SOUND, n-free,
   ell-free, both branches. (iv) ||K_H||_op, ||H_A||_op <= ||K||_op: exact.
   (v) rank <= 4n^3: of record. (vi) THE STAY-STRATA BREAK ATTEMPT: my A7
   exhibits that the only closure decay mechanism (endpoint-flat time
   profiles / oscillation) dies identically at the zero-transfer stay
   channel — there is NO way through that the build missed; the n-uniform
   quantifier cannot be reached by any conversion in the sealed stock
   (F1 verified: the budgets carry n^3 exactly; r-3 D10's cancellation
   arithmetic re-checked in my WB6 re-run). THE RANK x OP TRAP IS NOT
   SILENTLY RE-ENTERED: the rank-grade budgets are displayed as FAILURES
   (:305-309) and ledger C-1 refuses PARTIAL for exactly this reason.
P3 CARRIERS (subsidiary claim). The three §6 strings byte-match ee91d85c.
   CN-3 (R10 <-> entry-15 magnitude vacancy): GENUINE at bytes (the
   scoring artifact's own R10 row and CORR-0004 make the link; the four
   quantities are magnitude-typed per ae2202e0 S-A/R-8). CN-1 (R5 <->
   the F1 topology/rate absence): SHAPE-genuine, overstated as "exactly"
   — R5's topologies belong to the J-II object family, not the spine;
   the same ABSENCE-CLASS, different objects. CN-2 (R1): WEAKEST — the
   bearing sentence is speculative; a J-II member evaluation is a
   different object entirely and would not obviously touch the wall.
   MITIGATION: §6 is marked SUBSIDIARY throughout, U1 is noted-not-
   claimed, and C-L2 is correctly NOT converted into a junction carrier.
   The stoppage itself is REAL (verified at P2): the route stops at
   absent structure whether or not the junction carriers would cure it.
P4 GRAVEYARD DISTINCTNESS. At bytes: no per-pair majorant chain (the op
   bound is on the assembled composite — the wall's own tasked object);
   rho_res/R_n never consumed (WB8 display bars the detour; H-R never
   defaulted — F'-3 respected); no extraction from B-L2*'s summed sup
   (the V-2 bridge untouched); no fixed-n region presented as a limit
   object (the rank-grade bounds are displayed as failures). DISTINCT;
   the three stops are sealed-text grounds, not graveyard mechanisms.
P5 THE SEALED OBSTRUCTION. Gone AROUND: o-2-OBS/KF1/the census are
   consumed as ground and nowhere contradicted; no weight-sum kill is
   transported to the composite (WB5 re-pins the doubling); no
   certification is attempted through the (c=1, o=0) class. VERIFIED.
P6 SEALS/BYTES/CAS. 16/16 seals (my §2). Every load-bearing quote
   byte-verified (R5 locus; C-L2; b_D; TD1; LINK 1/2; 2n^3 ceiling; the
   scoring strings; the audit's 3/2 correction). The build's battery
   EXTRACTED VERBATIM and re-run in a second fresh venv (rebuildvenv,
   sympy 1.14.0): 8/8 PASS, output BYTE-IDENTICAL to the recorded block
   (modulo trailing whitespace). My own battery: 7/7 PASS (§7).
P7 BLOCKER COMPLETENESS. My pre-fixed blocker list maps onto the build's
   with nothing left over: stay-strata shortfall -> F2 + §3.2 sweep +
   WB7; C-L2 -> F3; conversion trap -> F1; vacancy/theorem bar -> CS-8;
   anti-decay saturation -> CS-6/§3.4. The build additionally displays
   the MO-map and the R_n door-closing (WB8). I find NO missing blocker;
   my A7 adds one independent corroborating display (recorded in §3),
   not a new stop.
```

---

## 5. CORRECTIONS

```text
COR-1 [CONFIRMED — CAS, both directions] SIGN ERROR in the prose
   splitting display, five instances (:48, :197, :360, :425, :839):
   "tr K_n = -i tr[U(S_- - S_+)C] + i tr[S_+[C, U]]" is FALSE as
   written; the correct form is
      tr K_n = -i tr[U(S_- - S_+)C] - i tr[S_+[C, U]]
   (equivalently +i tr[S_+[U, C]]). My CAS: the prose form does NOT
   equal tr K_n on generic matrices; the corrected form DOES. The
   build's own WB4 proves exactly the correct pre-factor identity
   tr[C(U S_- - S_+ U)C] = tr[U(S_- - S_+)C] + tr[S_+[C, U]]; the error
   entered at the -i multiplication in prose only. VERDICT-NEUTRAL:
   every use is structural (term two routes through [C, U]; vanishes at
   U = I; class membership c-iii) — all sign-invariant. The registrar
   should carry the corrected display.
COR-2 [CONFIRMED at bytes] "F1 = MO-3 exactly" (:430, :862) overstates;
   the build's own :319/:855 "the MO-3 shape" is the correct form. MO-3
   of record (bebc0f08 §5) is the sub-volume trace-norm rate for
   Delta_n(a); F1's escape object is the same-SHAPE rate for the
   dressed parity composites — same class, different object.
   VERDICT-NEUTRAL.
COR-3 [PLAUSIBLE — grading] CN-1's "exactly the member-named-topology
   absence" and CN-2's bearing sentence are shape-class analogies, not
   byte-level identities; CN-2 is the weakest row (a J-II member
   evaluation would not obviously bear on the spine quantities). The
   SUBSIDIARY marking and the honest U1/C-L2 handling keep this
   verdict-neutral; the carrier claims should be read as
   absence-CLASS matches, with CN-3 the only byte-genuine row.
COR-4 [PLAUSIBLE — grading] F2 consumes the adopted R5 locus ruling
   beyond its native context (C6's n = 1 equal-time block) into the
   stay-strata carrier accounting — same |x-y|^{-3} kernel class,
   class-level transfer, and the build itself marks F2 "sealed text,
   not an estimate". GRADE F2 as CORROBORATING rather than
   independently sufficient. NO verdict effect: F1 and F3 are each
   independently sufficient for FAILS-AT, and my A7 supplies a
   ruling-free mechanism display of the same conclusion.
COR-5 [OBSERVATION] "the first n-uniform bound of any grade on the
   parity sectors" (:48, :297) is true at the displayed-sweep grade (no
   sealed artifact displays such a bound) but an n-free op bound on K_n
   was implicitly derivable from sealed P-2 plus a Cauchy estimate on
   the polydisc (with worse constants, c(eps_*)/eps_*); read "first" as
   "first displayed/sealed". The build's direct chain is cleaner and
   polydisc-free; the deliverable itself is sound.
```

No finding above moves the verdict class. No neutrality violation, no
graveyard re-walk, no obstruction crossing, no quantifier slippage, no
forbidden import was found. THE BUILD'S FAILS-AT STANDS.

---

## 6. WHAT THE AUDIT ESTABLISHES FOR THE WALL (descriptive, no flag action)

```text
- The untried route is now TRIED, and its failure is a RESULT, per the
  neutrality clause: the boundary-closure structure yields op-grade
  control (||K_H,n||_op <= 2, ||H_A,n||_op <= 2, verified) and cannot
  reach trace/HS grade on the sealed basis — F1 (conversion), F3 (C-L2)
  each sufficient; F2/A7 corroborating at the locus/mechanism level.
- The odd sector's single-composite kill DOES NOT EXIST on the sealed
  closure stock (CS-1..CS-9 sweep verified; the promotion and moment
  kills are sum facts with no composite object; the same-phase collapse
  spends itself on the baseline = X-5).
- All four quantities remain UNDECIDED — nothing here or in the build
  exhibits a divergence; NOT REFUTED discipline held on both sides.
- The residue, exactly: a sub-volume trace/HS rate for the dressed
  parity composites (the MO-3 SHAPE, open of record); the C-L2
  quadratic-form certification (E1's own witness, standing); and —
  subsidiary, at absence-class grade — the R10 magnitude junction
  (byte-genuine), with R5/R1 shape-adjacent only.
```

---

## 7. MY CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

Run in fresh venv `wbcvenv` BEFORE the build was opened; hash fixed in the
pre-build note (37baacd9...). Disclosed pre-final-run harness corrections,
per the record's own discipline: the first draft's A6 check compared a
`sp.factor` output against a hand-ordered product (form-sensitive, not
math-sensitive) and its A7 used `sp.limit` on a complex exponential
(NotImplementedError in sympy 1.14.0); both were replaced by exact-identity
displays BEFORE the final run — one FAIL and one exception occurred on the
draft harness forms, no mathematical claim changed, and the final battery
ran ONCE: 7/7 PASS. The A6/A7 mathematical content (min >= product on
(0,1]; integration-by-parts with vanishing endpoint terms) is unchanged.

```python
# WBC-AUDIT independent CAS battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0,
# fresh venv wbcvenv under /private/tmp scratchpad). Run BEFORE opening the
# build. Every constant symbolic or exact rational/surd; nothing numeric
# evaluated as ground.
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def hs2(M):
    return sp.expand((M * M.H).trace())

def cmat(pre, n):
    return sp.Matrix(n, n, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im * sp.Matrix(n, n, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

# ===== A1 -- channel identities re-derived independently (4x4 generic) =====
Kg = cmat('k', 4)
KH = sp.expand((Kg + Kg.H) / 2)
KA = sp.expand((Kg - Kg.H) / 2)
HA = sp.expand(-Im * KA)
trK2 = sp.expand((Kg * Kg).trace())
trKsq = sp.expand((Kg.trace()) ** 2)
ok("A1 Re tr[K^2] = ||K_H||_2^2 - ||H_A||_2^2 and Re (tr K)^2 = (tr K_H)^2 -"
   " (tr H_A)^2 (independent re-derivation of the KCH channel identities)",
   sp.expand((trK2 + sp.conjugate(trK2)) / 2 - (hs2(KH) - hs2(HA))) == 0
   and sp.expand((trKsq + sp.conjugate(trKsq)) / 2
                 - ((KH.trace()) ** 2 - (HA.trace()) ** 2)) == 0
   and sp.expand(HA - HA.H).applyfunc(sp.expand) == sp.zeros(4, 4))

# ===== A2 -- threshold arithmetic re-run (carrier radius formal) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
tr3 = sp.integrate(kk ** 2 * kk ** -3, (kk, 1, Lam))
tr4 = sp.integrate(kk ** 2 * kk ** -4, (kk, 1, Lam))
hs1 = sp.integrate(kk ** 2 * kk ** -2, (kk, 1, Lam))
ok("A2 trace threshold: deg -3 gives log Lambda (divergent), deg -4 gives"
   " 1 - 1/Lambda (convergent) -> trace needs symbol degree < -3; HS^2 at"
   " deg -1 symbol (p=1) linear in carrier: stay stratum (deg -1) is 2+"
   " powers short of trace threshold",
   sp.simplify(tr3 - sp.log(Lam)) == 0
   and sp.simplify(tr4 - (1 - 1 / Lam)) == 0
   and sp.simplify(hs1 - (Lam - 1)) == 0)

# ===== A3 -- the (+sqrt2,-sqrt2) branch pair is a SAME-phase pair =====
# phases (+1,-1,-1) on lambda = (0, +sqrt2, -sqrt2): phase(+s2)*phase(-s2)=+1
tau = sp.pi / sp.sqrt(2)
lams = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
phases = [sp.exp(-Im * l * tau) for l in lams]
ok("A3 monodromies at full tau_R = pi/sqrt2 are (+1,-1,-1) EXACT; the"
   " (+sqrt2,-sqrt2) CROSS pair K_n is built on is SAME-phase"
   " (phase product +1): R.2's opposite-phase -2P display does NOT attach"
   " to the (+,-) pair; its baseline is V(0) = I up to the C-L2 error",
   sp.simplify(phases[0] - 1) == 0
   and sp.simplify(phases[1] + 1) == 0
   and sp.simplify(phases[2] + 1) == 0
   and sp.simplify(phases[1] * phases[2] - 1) == 0)

# ===== A4 -- saturation closed form and its anti-decay content =====
tt = sp.Symbol('tau', positive=True)
S = -R(1, 4) * (sp.exp(-Im * sp.sqrt(2) * tt) - 2 + sp.exp(Im * sp.sqrt(2) * tt))
Ssimp = sp.simplify(sp.expand_complex(S - sp.sin(sp.sqrt(2) * tt / 2) ** 2))
ok("A4 S(tau) = sin^2(sqrt2 tau/2); S(tau_R) = 1 = sum|w_lambda| (l1 step"
   " TIGHT -- the zero-sum weights buy NO decay at full tau_R: the"
   " saturation is an ANTI-enclosure statement)",
   Ssimp == 0 and sp.simplify(S.subs(tt, sp.pi / sp.sqrt(2)) - 1) == 0)

# ===== A5 -- b_D profile: bounded by 1 exactly, attained ONLY at center ====
t, x = sp.symbols('t x', real=True)
sm = t ** 2 - x ** 2
sps_ = (1 - t) ** 2 - x ** 2
s = sm * sps_
# on the axis x=0: s = t^2(1-t)^2 <= 1/16 with equality iff t = 1/2
smax = sp.maximum(s.subs(x, 0), t, sp.Interval(0, 1))
# x-monotonicity: ds/d(x^2) < 0 inside the diamond
u = sp.Symbol('u', nonnegative=True)  # u = x^2
s_u = (t ** 2 - u) * ((1 - t) ** 2 - u)
dsdu = sp.expand(sp.diff(s_u, u))
ok("A5 b_D = exp(16 - 1/s): s <= 1/16 on the diamond (axis max 1/16 at"
   " t = 1/2; ds/du = -(t^2 + (1-t)^2) + 2u < 0 for u < min(t,1-t)^2),"
   " so b_D <= 1 with equality ONLY at (1/2, 0) -- an OPERATOR-grade sup,"
   " ||J|| <= 1; trace-grade use costs rank 4n^3 (the rank x op trap)",
   sp.simplify(smax - R(1, 16)) == 0
   and sp.expand(dsdu - (2 * u - t ** 2 - (1 - t) ** 2)) == 0)

# ===== A6 -- the poisoning ceiling ||R_n|| <= 1/|det_n(0)| and divergence =
s1, s2, s3 = sp.symbols('s_1 s_2 s_3', positive=True)
# |1-2s_i| =: d_i in (0,1]; min >= product for d_i in (0,1]
d1, d2, d3 = sp.symbols('d_1 d_2 d_3', positive=True)
# product <= d1 when d2,d3 <= 1: d1 - d1 d2 d3 = d1(1 - d2 d3) >= 0
ok("A6 ||R_n|| = 1/min_i d_i <= 1/prod_i d_i = exp(-log|det_n(0)|)"
   " (d_i in (0,1]: min >= product since d1 - d1 d2 d3 = d1(1 - d2 d3),"
   " a product of nonnegatives when d2 d3 <= 1); with"
   " |det_n(0)| <= e^{-kappa_n} -> 0 the ONLY sealed R_n ceiling DIVERGES"
   " like e^{+kappa_n}: the boundary-closure structure certifies"
   " divergences, not enclosures",
   sp.expand(d1 - d1 * d2 * d3 - d1 * (1 - d2 * d3)) == 0)

# ===== A7 -- endpoint-flat time profile buys NOTHING at the stay stratum ==
# oscillatory channel: int_0^1 f(t) e^{iwt} dt -> O(w^-N) for smooth
# compactly supported f (displayed at N=1,2 by exact integration by parts);
# stay/diagonal channel: zero frequency, the SAME integral is int f(t) dt --
# a CONSTANT, no decay. Exact display with f = t^2(1-t)^2 (flat to 1st order).
w = sp.Symbol('w', positive=True)
f = t ** 2 * (1 - t) ** 2
fp = sp.diff(f, t)
# exact integration-by-parts identity, boundary terms vanishing displayed:
# int_0^1 f e^{iwt} dt = -(1/(iw)) int_0^1 f' e^{iwt} dt  (one power per pass)
I_osc = sp.integrate(f * sp.exp(Im * w * t), (t, 0, 1))
I_ibp = sp.expand(-(1 / (Im * w)) * sp.integrate(fp * sp.exp(Im * w * t), (t, 0, 1)))
I_diag = sp.integrate(f, (t, 0, 1))
ok("A7 time-smoothness gives decay ONLY in the energy TRANSFER: with"
   " f(0)=f(1)=f'(0)=f'(1)=0 (endpoint-flat profile) int f e^{iwt} ="
   " -(1/(iw)) int f' e^{iwt} EXACTLY (one 1/w power per pass, iterable);"
   " at the stay/diagonal channel w = 0 the SAME integral is the constant"
   " 1/30 -- no decay: no closure smoothness mechanism reaches stay strata",
   f.subs(t, 0) == 0 and f.subs(t, 1) == 0
   and fp.subs(t, 0) == 0 and fp.subs(t, 1) == 0
   and sp.simplify(I_osc - I_ibp) == 0
   and I_diag == R(1, 30))

print("WBC-AUDIT-BATTERY-DONE")
```

Output, verbatim (7/7 PASS, single final run):

```text
A1 Re tr[K^2] = ||K_H||_2^2 - ||H_A||_2^2 and Re (tr K)^2 = (tr K_H)^2 - (tr H_A)^2 (independent re-derivation of the KCH channel identities): PASS
A2 trace threshold: deg -3 gives log Lambda (divergent), deg -4 gives 1 - 1/Lambda (convergent) -> trace needs symbol degree < -3; HS^2 at deg -1 symbol (p=1) linear in carrier: stay stratum (deg -1) is 2+ powers short of trace threshold: PASS
A3 monodromies at full tau_R = pi/sqrt2 are (+1,-1,-1) EXACT; the (+sqrt2,-sqrt2) CROSS pair K_n is built on is SAME-phase (phase product +1): R.2's opposite-phase -2P display does NOT attach to the (+,-) pair; its baseline is V(0) = I up to the C-L2 error: PASS
A4 S(tau) = sin^2(sqrt2 tau/2); S(tau_R) = 1 = sum|w_lambda| (l1 step TIGHT -- the zero-sum weights buy NO decay at full tau_R: the saturation is an ANTI-enclosure statement): PASS
A5 b_D = exp(16 - 1/s): s <= 1/16 on the diamond (axis max 1/16 at t = 1/2; ds/du = -(t^2 + (1-t)^2) + 2u < 0 for u < min(t,1-t)^2), so b_D <= 1 with equality ONLY at (1/2, 0) -- an OPERATOR-grade sup, ||J|| <= 1; trace-grade use costs rank 4n^3 (the rank x op trap): PASS
A6 ||R_n|| = 1/min_i d_i <= 1/prod_i d_i = exp(-log|det_n(0)|) (d_i in (0,1]: min >= product since d1 - d1 d2 d3 = d1(1 - d2 d3), a product of nonnegatives when d2 d3 <= 1); with |det_n(0)| <= e^{-kappa_n} -> 0 the ONLY sealed R_n ceiling DIVERGES like e^{+kappa_n}: the boundary-closure structure certifies divergences, not enclosures: PASS
A7 time-smoothness gives decay ONLY in the energy TRANSFER: with f(0)=f(1)=f'(0)=f'(1)=0 (endpoint-flat profile) int f e^{iwt} = -(1/(iw)) int f' e^{iwt} EXACTLY (one 1/w power per pass, iterable); at the stay/diagonal channel w = 0 the SAME integral is the constant 1/30 -- no decay: no closure smoothness mechanism reaches stay strata: PASS
WBC-AUDIT-BATTERY-DONE
```

THE SIGN CHECK (post-read, targeted; same venv): on generic 3x3 matrices
with C = diag(1,1,0), K = -i C(U S_- - S_+ U)C:

```text
prose form (+i tr[S_+[C,U]]) equals tr K_n: False
corrected form (-i tr[S_+[C,U]]) equals tr K_n: True
```

THE VERBATIM RE-RUN (second fresh venv `rebuildvenv`, sympy 1.14.0): the
build's §7 python block extracted byte-for-byte and executed once — 8/8
PASS, stdout BYTE-IDENTICAL to the build's recorded output block (modulo
trailing whitespace).

---

## 8. CHOICE LEDGER

```text
CH-1 SUB-TASK INTERPRETATION [YOURS]: "work sub-tasks (1)-(2) yourself"
     read as (1) attempt the route myself from the sealed stock and
     (2) fix my own verdict on it — both executed and hashed pre-build
     (§1). If the tasking meant the attack priorities (1)-(2) instead,
     those too were pre-registered in the same note (the D1-D8 defect
     list and the re-derivation plan).
CH-2 READ SCOPE [PREMISE(named)]: E1/PA/R3/798de0dd consumed at tasked
     spans + targeted greps, not cover-to-cover (KCH, r-2, ETT, scoring,
     C4-answer read in full). Every load-bearing quote was byte-verified
     at its span; no claim rests on an unread span.
CH-3 F2 GRADING [YOURS]: corroborating, not independently sufficient
     (COR-4). The verdict is insensitive (F1, F3 each sufficient).
CH-4 KCH IDENTITY LAYER consumed at AUDITED grade (4ebe6b8f CONFIRMED)
     with my A1 spot re-derivation; not fully re-derived here.
CH-5 VENV NAME COLLISION [disclosed]: my `wbcvenv` and the build's
     `wbcvenv` are distinct, independently created environments (mine
     created this session in my scratchpad before the build was opened);
     the re-run used a THIRD environment (`rebuildvenv`) to keep the
     provenance clean.
CH-6 HARNESS CORRECTIONS [disclosed, §7]: two draft-harness form defects
     (A6 form-sensitive equality; A7 sympy limit) replaced by exact
     displays before the final run; one draft FAIL and one exception
     occurred on the harness forms; no mathematical claim changed.
CH-7 SWEEP GRADE [PROVABLE]: the neutrality grep, the sign-instance
     count (five), the quote byte-checks, and the correction list claim
     only their displayed sweeps of the named files.
```

---

## 9. TOY_SEPARATION

```text
T-1 My CAS instances are identity-grade displays: generic 3x3/4x4
    matrices for all-matrix identities (A1, the sign check), formal
    symbols for threshold/ceiling arithmetic (A2, A6), exact scalar
    closed forms (A3, A4, A5). No instance is quoted wider than its
    displayed quantifier.
T-2 A7's profile f = t^2(1-t)^2 is a WITNESS-grade mechanism exhibit
    (endpoint-flat to first order, not all orders): it is consumed ONLY
    to corroborate a NEGATIVE (no decay at zero transfer — which for the
    diagonal channel is profile-independent: the w = 0 integral of any
    nonnegative nonzero profile is a positive constant) and to display
    the one-power-per-pass mechanism; no positive certification rests
    on it.
T-3 No witness-family object is promoted to the carrier quantifier;
    every carrier-axis statement cites sealed carrier-axis text or my
    A2 formal-integral re-run.
```

---

## 10. FLAG BLOCK

```text
WBC_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the build's FAILS-AT
  (F1 rank x op conversion; F2 locus; F3 C-L2 error layer) STANDS under
  independent adversarial examination — the auditor's own pre-fixed
  blind attempt (hashes 2302dea9 note / 37baacd9 battery, taken BEFORE
  the build was opened; 7/7 PASS) reached the same failure class at the
  same stops and independently derived the WB3 same-phase observation;
  the delivered op-grade enclosure ||K_H,n||_op <= 2, ||H_A,n||_op <= 2
  verified sound at every step (TD1 S_mu form at 798de0dd bytes; LINK 1
  C-contraction; b_D <= 1 unique attainment re-derived); the build's
  battery re-ran BYTE-IDENTICAL 8/8 in a second fresh venv; neutrality
  HELD (grep + full read: no step consumes the motivating reading);
  graveyard NOT re-walked; the sealed obstruction gone AROUND; NOT
  REFUTED discipline held — all four quantities UNDECIDED. )
CORRECTIONS = FIVE( COR-1 CONFIRMED sign error in the prose splitting
  display at five instances (:48, :197, :360, :425, :839) — term two
  must read -i tr[S_+[C, U]] (equivalently +i tr[S_+[U, C]]);
  CAS-verified both directions; the build's own WB4 proves the correct
  pre-factor identity; VERDICT-NEUTRAL (all uses structural).
  COR-2 CONFIRMED wording: "F1 = MO-3 exactly" -> "the MO-3 SHAPE" (the
  build's own §3.3 form); different object, same class. COR-3 PLAUSIBLE
  grading: §6 carrier bearings are absence-class analogies (CN-3
  byte-genuine; CN-1 overstated "exactly"; CN-2 weakest/speculative);
  mitigated by SUBSIDIARY marking. COR-4 PLAUSIBLE grading: F2 is
  CORROBORATING (the R5 ruling consumed beyond its native C6 context;
  build's own "sealed text, not an estimate" flag); F1 and F3 each
  independently sufficient — verdict unmoved; auditor's A7 adds a
  ruling-free mechanism display of the same conclusion. COR-5
  OBSERVATION: "first n-uniform bound of any grade" true at
  displayed-sweep grade; a Cauchy corollary of sealed P-2 was
  implicitly available at worse constants. )
INDEPENDENCE = PROTOCOL-EXECUTED( sealed stock read first; own attempt
  + verdict FIXED AND HASHED before opening the build (2302dea9,
  37baacd9, 2026-08-14 23:30:56 UTC); build then read ONCE in full and
  attacked at bytes; pre-registered defect patterns D1-D8 — NONE
  materialized; concurrence and differences disclosed in §1. )
SEALS_VERIFIED = 16/16( the build 80db260f + the 15-artifact stock —
  all recomputed from bytes at path this session, all matching sidecar
  and tasked prefixes; every load-bearing quote byte-verified at its
  span; the r-2/r-3 pair located by tasked prefix. )
FORBIDDEN_IMPORTS = none( exact symbolic only; nothing numeric as
  ground; no measured constant; no value frozen; alpha never computed
  or approached; kappa_record never computed; H-R never defaulted; the
  BAR not crossed; no register|road_|ledger|lens|plan|tracker|
  THE_HANDOFF file opened; "Q-..." tokens EXPECTED-UNLOCATABLE not
  chased; no git action; no existing file edited; ONE output + sidecar
  at the commission-distinct path, probed ABSENT twice; no flag
  flipped; no witness action — witness status is the registrar's. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0; TWO fresh venvs (wbcvenv:
  my battery 7/7 PASS single final run, two draft-harness form defects
  disclosed §7/CH-6; rebuildvenv: the build's battery re-run verbatim,
  8/8 PASS, byte-identical output) plus one targeted post-read sign
  check (displayed verbatim §7); shasum/grep/file reads otherwise.
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
WBC_WALL_BOUNDARY_CLOSURE_AUDIT_RESULT = SEALED.
```
