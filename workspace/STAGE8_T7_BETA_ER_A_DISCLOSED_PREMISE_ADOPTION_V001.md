# Stage-8 T7 Beta Adoption — ER-A as Disclosed Premise V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_PRINCIPAL_ADOPTION — BETA IN FORCE
```

Brian's decision: beta is ADOPTED. Its contingency on the kill-test is
discharged — the kill-test blocked on CONTROL DESIGN, not on physics,
and gamma's refutation never rested on it (see
STAGE8_T7_GAMMA_GATE_FINALITY_DECISION_RECORD_V001).

## The premise

```text
ER-A is carried as a DISCLOSED PREMISE:
  the intrinsic-action envelope realization used throughout is
  ER-A (write envelope tau_R w(t) M(t), w(t) = 32 r(t)^3),
  NOT derived and NOT selected — assumed, and disclosed as assumed.
ER-B (tau_R rho M(t), rho = 24/pi) remains UNEXCLUDED but
  BATTERY-UNDEFINED: the sealed battery pins ER-A and never computes
  ER-B; the absence of ER-B obligations is NOT an exclusion of ER-B,
  and no later reader may read it as one.
```

## Basis of record (do not overstate)

```text
Gamma (record-level insensitivity as an operator theorem) is REFUTED by
  the STRUCTURAL argument of ROUTE_MEMO_2026-07-25_gamma_record_level_
  insensitivity.md: an exact counterexample carrying the sealed algebra
  ({S, alpha_x} = 0, exact pointer weights) in which the per-history
  connection Hessian depends on the envelope's FULL CUMULATIVE PROFILE
  with an O(1) shape coefficient at leading order ||J||^2. The record
  CAN see the envelope.
The gamma GATE is cited here ONLY as a blocked gate whose computation is
  preserved (finality record). It is not cited as refutation evidence.
```

## Conditionality clause (MANDATORY on every headline downstream of kappa_record)

```text
This result is CONDITIONAL ON THE ER-A ENVELOPE PREMISE, which is
disclosed and underived. An alternative admissible envelope (ER-B)
exists, is not excluded, and would in general give a different value:
the record is provably sensitive to the envelope's cumulative profile.
```

## CUMULATIVE CONDITIONALITY REGISTER (the one place; keep it here)

Stage-8-era disclosed premises, authoritative list:

```text
C1. ER-A envelope realization              (this artifact; new)
C2. complex-vs-real selection              (standing; assumed, not
    derived — the program's long-recorded recurring gap)
```

Inherited historical Level-1 stack, recorded in the continuation state
and the premise ledger, listed here so the TOTAL is visible in one
place rather than scattered:

```text
imported winding k = +1; MPCP three-axis / d = 4; onset rule;
transport-only; QFC; DCC; vectorlike inventory (undeclared minimality);
K_bare = 0; fidelity action k = 1; PSC and CISP (sealed principles);
tau_orth = T_R (cycle-6 conditionality).
```

Any artifact adding a disclosed premise MUST add it to this register in
the same change. The register, not the individual artifacts, is the
authority for "what the result is conditional on".

## Scope of any future closure (never global)

```text
Any future discharge is "ER fork closed FOR kappa_record" ONLY.
ER_fork_closed (global) stays FALSE. Downstream consumers that evaluate
at finite connection strength a — Q_spec, the EM order steps — REOPEN
the fork at their own stage and must say so.
```

## Observation of record (Brian's direction note, banked here)

The counterexample that refuted gamma establishes something in its own
right: kappa depends on the FULL CUMULATIVE PROFILE sigma(.) of the
action, not on any endpoint datum — at equal total action the shape
coefficients differ (1/3 flat, 5/9 for the sealed r(t)^3 bump, 1
concentrated). THE RECORD RETAINS HOW ACTION ACCUMULATED, NOT MERELY
HOW MUCH. Consequently the ER fork is a PHYSICAL question about how
action accumulates on a causal diamond, not a representational one, and
the eventual route to discharging this premise is to DERIVE the
write-rate from the record principle itself — not to show the fork does
not matter. That route is explicitly NOT opened here; it is a
principal-level scoping question for later.

## Protected status

```text
ER_A_disclosed_premise_adopted = true
ER_A_selected = false
ER_B_selected = false
ER_B_excluded = false
envelope_realization_derived = false
ER_fork_closed = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
