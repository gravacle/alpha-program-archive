# STAGE 8 — AUDIT OF THE K-CHANNEL CONTROL BUILD: THE DRESSED-TRACE ENCLOSURE (o-2) AND THE EXCHANGE CHANNEL (o-1), RE-DERIVED BY ROUTES THE BUILD DID NOT USE

## INDEPENDENT AUDITOR — CODENAME KCH-AUDIT — BRIEFED TO REFUTE — [SEALED]

Date: 2026-08-14
Under audit: STAGE8_K_CHANNEL_CONTROL_V001.md, digest
d66a922cfe023284890de8a335c38028efe94fb7a9d31b1f779bb0a0513b95cc
(recomputed at path this session, MATCH against sidecar and tasking).
Default stance held: WRONG UNTIL EACH CLAIM SURVIVED the auditor's own
re-derivation by routes the build did not use.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked (and the
checks are below).

Fences held by the auditor: EXACT SYMBOLIC COMPUTATION ONLY — one CAS
battery (sympy 1.14.0, fresh venv `kchaudvenv` under /private/tmp
scratch), reproduced in full in §2.6 with output verbatim (17/17 PASS on
the recorded run, which was TERMINATED at the orchestrator's deadline
inside the FINAL check's last corroborative conjunct with no FAIL
recorded; the load-bearing content of that final check delivered by the
disclosed 1/1 supplementary run X8s, also verbatim in §2.6; two aborted
harness-form runs additionally disclosed there — no mathematical claim
changed by any of the three disclosures); every constant symbolic or an
exact rational/surd; NOTHING numeric evaluated (Lambda formal; testbed
frequencies formal); no physical quantity computed or bounded
numerically; alpha never computed or approached; H-R nowhere defaulted
(rho_res never valued); equal-time FALSE by C6 respected (the
equal-source slice audited as a SOURCE-polydisc slice throughout); the
per-pair route BARRED and not traversed by the auditor either (identities
and byte-verifications only; no majorant certified through any per-pair
chain); no carrier or cellulation datum introduced; no
register/tracker/plan/road/ledger/lens file read; no flag flipped; no
witness action. ONE file written (this artifact) plus its seal; no git
action; no existing file edited.

---

## 0. VERDICT IN ONE LINE

**O2 = CONFIRMED (FAILS-AT stands) / O1 = CONFIRMED (PARTIAL-DELIVERED stands) / NET = CONFIRMED-WITH-CORRECTIONS — the identity layer of both channels survives hostile re-derivation by routes the build did not use (nilpotent two-segment time-ordered model exact to m^6; H+iG channel algebra at dims 3 and 5; characteristic-polynomial det-jet; exact-exponential/Duhamel testbed landing on the record's C1/C2 with certificates -16i/3, -8i, -256/9; full-measure thresholds with symbolic-limit classification; chiral-representation odd-trace kill on the x-polar axis; the auditor's own no-kill integral int_S2 (q.khat) khat_x dOmega = (4pi/3) q_x != 0), 17/17 PASS on the recorded run plus the disclosed 1/1 X8s covariance supplement (the recorded run terminated at the orchestrator's deadline inside the final check's corroborative 4x4-adjugate conjunct, no FAIL — §2.6), with the build's own battery re-run BYTE-IDENTICAL at 20/20 in the auditor's fresh venv; the obstruction on BOTH channels is real and unpassable on the sealed basis — the sealed (c = 1, o = 0) exclusion is derived UNDER the weight kill (a sum fact with no object in the composite; the strata double instead), the surviving S1 leg does not engage (even integrand, X6b), no oscillatory power exists at stay strata, the bare class is already spent at total order 0, and 52f2490b's +infinity is verified at its own bytes to be bare-driven and hence detached from the dressed-only object with NO illegitimate re-import by the build; ONE named verbal correction (the thrice-instanced "ONE FULL POWER" gloss — the named thresholds sit 3/2 symbol-degree powers apart, the clause true only as the Lambda-rate gap at degree -1 or the o = 1 requirements gap; verdict-neutral); quantifiers audited clean (narrowing licensed at 798de0dd :924-927; equal-source polydisc slice, not equal-time; H-R never defaulted by build or auditor; no per-pair carrier sup consumed; conditionals P-1 and R-L4a/b + C-L2 carried as named); bearing NONE (adopted amendment, alpha' = 1/4, B-L2*, the 798de0dd/c944a901 identity layer, 52f2490b, RA4b/CA3 — all unmoved at their own quantifiers; V-1..V-6 severally NOT ENGAGED); provenance 10/10 plus 52f2490b at path; Q-1054 UNLOCATABLE by the auditor's own grep — flagged, not defaulted; injection CLEAN; fences HELD by build and auditor; witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS.**

---

## 1. SEALS INDEPENDENTLY VERIFIED AT PATH (shasum -a 256, FULL digests, THIS session)

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes by the auditor; each compared against its
`.seal.sha256` sidecar and against the build's §1 table and the tasked
prefixes. 10/10 MATCH plus one EXTRA verification the build consumed
through quotation (52f2490b) — verified here at its own file. NONE
UNVERIFIABLE.

```text
d66a922cfe023284890de8a335c38028efe94fb7a9d31b1f779bb0a0513b95cc  STAGE8_K_CHANNEL_CONTROL_V001.md  MATCH (the build under audit)
798de0dd032c8831458b9a52b67ef92c913aa5691c219fe13b084e443f25497a  STAGE8_SECOND_ORDER_TOTAL_CONTENT_V001.md  MATCH (S2TOT)
c944a901a81eae33063fb5ef0f2aac73db45e6f6ae5c68bdc76e6f6e25a071bd  STAGE8_SECOND_ORDER_TOTAL_CONTENT_AUDIT_V001.md  MATCH (S2TOT-AUDIT)
57edbb964610bb7c9715bccdf88cf0400ef5af230bf1be139a0be300d90994d8  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md  MATCH (R3C)
631356a36ed93cd61eab1b0177bbbe456eb1fcfc51ef9b5eca4cdc388910016b  STAGE8_P1_DILATION_CLOSURE_V001.md  MATCH (P1)
e5b0879bb4f272f709b2f70b50244d1c0bf6f392a2053d6368e477022f8f6e53  STAGE8_S2_DISCHARGE_V001.md  MATCH (DISCHARGE)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md  MATCH (REM)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md  MATCH (R2)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (RL2B-REFUTED — the build consumed this through the DISCHARGE quotation; the auditor verified it at its own bytes)
```

Every load-bearing span the build cites was re-read AT ITS LOCATION this
session and found as cited: 798de0dd §2.4 k-1/k-2 (:225-241, the TE2
display and the X-5 confinement), §2.5 (:252-276 master form), §2.6
(:278-318, s-1/s-2/s-3, TE4/TE5/TE8, the slice scoping "NOT an
equal-time object — C6 untouched" :280-282, the BAR-honoring parenthesis
:313-317), §3.3 (:916-937, o-1/o-2/o-3 verbatim, the o-2 narrowing to
"the dressed cross-branch trace alone" :924-927, o-1's "single-J5 stay
class leading for the dressed-odd content" :919-923), TG2 closed forms
(:837-840 — C1/C2 printed EXACTLY as the build's §2.5 repeats them);
c944a901 verdict + AU20a/AU20b (:264-265, :949-950 slot-insertion law),
AU24b (:957 swap literal), AU24d/AU24e (:959-960 sin-forms + Taylor
certificates -16i/3, -8i), §2.8 a-1 (:253) and a-3/CA6 (:270-271);
57edbb96 (:48-51, :305-320, :415-416 — bare degree-0 typed to the
(0,0)-differences, single-J5 stay leading for the weight-free
differences, "the parity kill that removed it is exactly what a bare
difference lacks"); e5b0879b §4.2 (:392-416 W-1/W-2/W-3 VERBATIM
including the 52f2490b clause and the BAR verbatim; :281-285 alpha' =
1/4 and the V-1..V-5 severally text; :432 the standing witness);
2e4e5163 §2.1 (:128-134 the trilemma form on the WEIGHT-SUMMED X),
§2.2-§2.3 (:136-202 the (o,c) taxonomy; :141-145 the J5 stay factor and
the J vertex's longitudinal part with NO coincidence zero; :167-171
"ODD n DIES EXACTLY" as a WEIGHT-SUM consequence; :172 "With n even,
band parity closes the enumeration"; :199-202 the (c = 1, o = 0)
exclusion DERIVED UNDER THAT PARITY), §3.1 (:38 HS^2 linear in carrier),
§3.3a (:277-279 rate), §3.4 (:337 |k|^{-1/2}), §3.5 (:348 2o + c
thresholds); a7969f0c (-1_ball; :347 V4a; :383 the V_2 subsidiary
-i V_2(x)/(2|k|)); 46846730 :652-666 (R.0), :679-694 (order of
quantification; S1 at :691-694 with the double-kill annotation; the
enclosure requirement at :686-687), :720-727 (S4/H-R "may NEVER
default", F'-3); 789338ad :95-128 (b_D smooth compactly supported
bounded; J = -(Q b_D Q) (x) alpha_x Hermitian; h_lambda = h_0 +
lambda v M (x) S + a J LAMBDA-LINEAR at :124-127), :165 (branch set
{-sqrt2, 0, +sqrt2}); 631356a3 :36 (U_L intertwiner in the verdict),
:168-190 (index map; R^{(L)}(a) = R^{(1)}(La)), :254-286 (e-2/e-3 ray
quantifier); 52f2490b §0 (:20-26 the refuted object X_n =
C_n(V_n(a) - V_n(0))C_n — BARE-INCLUSIVE), LINK 6 (:116-119 the
divergence carried by the degree -3 off-diagonal kernel, i.e. the bare
degree-0 momentum class of the record's typing). ONE citation is
paraphrase-grade and found in SUBSTANCE: the build's "E1 :654
one-per-branch family" (the :652-654 bytes give the one-propagator-
per-branch display u_mu^{(c)}; same convention the sealed audit accepted
for ":687").

Q-1054 (the tasking's token for the sealed evidence): the auditor's OWN
full grep of the workspace corpus finds the token ONLY inside the build
itself (3 occurrences, quoting its tasking); Q-1050 (x13) and Q-1051
(x10) exist elsewhere. The build's UNLOCATABLE flag is CONFIRMED;
tasking-corpus mismatch on the tasking side, NOT defaulted by build or
auditor; the SUBSTANCE the tasking attributes to the token (the K_n
object, dressed-only, and its identity layer) IS at path in 798de0dd +
c944a901 and seal-verifies. Precedent (the sealed audit's Q-1051
handling) honored.

---

## 2. TASK A ADJUDICATION — o-2, THE DRESSED-TRACE ENCLOSURE AT THE NARROWED QUANTIFIER

### 2.1 The narrowing: LICENSED at the cited bytes

The narrowing of the S1-type enclosure to the ONE number per (n, cell)
tr K_n is not the build's invention: 798de0dd §3.3 o-2 (:924-927) says
it in the record's own voice ("the enclosure needed is of the dressed
cross-branch trace alone"), resting on TE2/TE3/X-5 (:225-241: the bare
degree-0 class — the 52f2490b divergence mechanism — is CONFINED to the
(a_+ - a_-)-sector; K_n is dressed-only) and on the [a^1] identification
Delta_{+-}(a,a)^{[1]} = a K_n (the k-2 display read on the slice). The
auditor re-verified each byte. S1's own definition (E1 :691-694) and its
double-kill annotation were re-read; the requirement line :686-687
("Each requires a certified outward enclosure") found as cited.

### 2.2 The identity layer, re-derived INDEPENDENTLY (17/17 recorded + 1/1 X8s, §2.6)

Every identity the build delivers was re-derived by a route the build
did not use, and every one held:

```text
(i-1) THE SWAP LAW AND ITS CONSEQUENCES (build KB1-KB4; audit X1a-X1d):
      re-derived on a NILPOTENT TWO-SEGMENT model with GENUINE
      time-ordering (two noncommuting piecewise generators, exact
      polynomial exponentials, exact Duhamel a-derivative) — not the
      build's free-coefficient slot model, and not truncated products.
      G_-+(m) = G_+-(-m) and G_--(m) = G_++(-m) EXACT at all orders
      (X1a); [m^0]K = 0 by the G^0 subtraction alone (X1b — the X-5
      mechanism); [m^k]K = C(g^(k) + (-1)^k g^(k)dag)C with even strata
      Hermitian and odd strata anti-Hermitian, verified to ORDER m^6,
      beyond the build's n <= 3 (X1c); the equal-weight quartet sum EVEN
      in m while the composite carries the DOUBLED odd strata
      [m^odd](G_+- - G_-+) = 2 g^(odd) (X1d). The mathematical load of
      the parent instantiation is PA :124-127 (h lambda-LINEAR, branch
      set {-sqrt2, 0, +sqrt2} at :165, slots branch-shared) — read at
      bytes; given that display the swap is a substitution identity, and
      the build says no more than that.
(i-2) THE CHANNEL IDENTITIES (build KC1-KC3; audit X2a-X2c): re-derived
      by the Cartesian H+iG route at dims 3 AND 5 (the build used 4x4):
      Re tr[K^2] = ||K_H||_2^2 - ||H_A||_2^2; Re (tr K)^2 =
      (tr K_H)^2 - (tr H_A)^2; Re e_2(K) = e_2(K_H) + e_2(K_A);
      tr[K_H K_A] purely imaginary; the dependency tr[K^2] =
      (tr K)^2 - 2 e_2(K); the sum-of-squares form; and the det-jet by
      the CHARACTERISTIC-POLYNOMIAL route at 4x4 (X2b). All exact,
      dimension-generic.
(i-3) THE SLOT-INSERTION TRACE FORM (build KD2; audit X3a-X3b):
      K = -i C(U S_- - S_+ U)C and tr K_n = -i tr[(U S_- - S_+ U)C]
      re-assembled directly from AU20b + TD1/TD2 (both at sealed bytes),
      with the U = 1, S_- = S_+ vanishing re-derived by ATOMIC
      substitution, and the TE3 bare-cancellation shape (G^0
      anti-Hermitian cancels inside the composite) verified separately
      (X3b).
(i-4) THE TESTBED (build KE1-KE4; audit X4a-X4d): rebuilt by the
      EXACT-EXPONENTIAL/DUHAMEL route — the coupling INSIDE the
      frequency Om = sqrt(E^2 + m^2), closed-form 2x2 exponentials, one
      Duhamel integral, THEN the m-jet — no Dyson simplex integral
      anywhere. Landing EXACTLY on the record's closed forms
      (798de0dd :839-840): [J_p1s1] tr K^(1) = C1 = -i/(2P) +
      e^{8iP}/(32P^2) - e^{-8iP}/(32P^2) = i(sin 8P - 8P)/(16P^2),
      [J_p2s2] tr K^(1) = C2 = i(sin 12P - 12P)/(36P^2), with Taylor
      certificates [P^1]C1 = -16i/3 != 0, [P^1]C2 = -8i != 0 (X4b/X4c);
      parity localization instantiated ([m^1]K anti-Hermitian, [m^2]K
      Hermitian, [m^0]K = 0; X4a); tr[(K^(1))^2] = -||K^(1)||_2^2 with
      [J_p1s1^2] tr[(K^(1))^2] = C1^2 and [P^2]C1^2 = -256/9 != 0
      (X4d). The build's "no hidden kill" inference (a structural
      identity would have to kill the testbed instance; it is nonzero)
      is valid and now doubly instantiated.
```

### 2.3 The obstruction, attacked as tasked — and it HOLDS

The auditor attempted to derive PAST the obstruction before accepting
it. The census first: REM §2.3's exclusion "There is NO (c = 1, o = 0)
class anywhere" (:199-202) is DERIVED UNDER "ODD n DIES EXACTLY. With n
even, band parity closes the enumeration" (:171-172) — i.e. under the
WEIGHT KILL, which is a sum fact (X7a re-proves it at an odd QUINTIC:
sum_l w_l f(l) = 0 for odd f BECAUSE w_+ = w_-; m_0 = m_1 = m_3 = 0,
m_2 = -1 with the record surds). In the composite K_n the weight sum
does not exist; the G^0 subtraction removes ONLY the total-order-0
stratum (X1b); total order 1 (one S-stay in one factor, the J vertex's
longitudinal intraband part in the other — the sealed contrast, REM
:143-145, NO coincidence zero) is PRESENT and DOUBLED (X1d). So the
(c = 1, o = 0) intraband class OPENS in the composite exactly as the
build's o-2-OBS states: diagonal symbol degree -1, 2o + c = 1. The
sealed precedent agrees: R3C already recorded that WEIGHT-FREE
differences re-open the single-J5-stay class ("the parity kill that
removed it is exactly what a bare difference lacks", :305-311).

Thresholds re-derived at the FULL 3D measure with divergence classified
by symbolic limit (X5a): int_{1<|k|<Lam} |k|^-p d^3k = 4pi(Lam^2-1)/2,
4pi(Lam-1), 4pi log Lam, 4pi(1 - 1/Lam) at p = 1..4 — trace-class
absolute control needs p > 3 STRICTLY; HS^2 of a degree-1 symbol is
LINEAR in the carrier (the REM §3.1 mechanism); HS needs 2p > 3. The
leading composite strata sit at degree -1 (odd sector) and -1/-2 (even
sector): two-plus powers short on the majorant route. Then EVERY
candidate route past the obstruction was worked to its end:

```text
(r-1) THE ANGULAR/SPINOR KILL (S1's surviving leg): re-derived in the
      CHIRAL representation on the x-polar axis (X6a): tr[P_s alpha_x] =
      2 s khat_x, first angular moment ZERO, second moment 4pi/3 != 0 —
      worth exactly ONE power and ONLY on integrands odd under the joint
      inversion. At the leading odd stratum the J5 difference factor
      (odd, homogeneity degree 0) PAIRS with the longitudinal khat_x
      (odd): even integrand. The auditor's own check X6b computes the
      paired leading term directly: int_S2 (q.khat) khat_x dOmega =
      (4pi/3) q_x != 0 — NO angular kill exists there. DEAD.
(r-2) THE WEIGHT KILLS (m_0, m_1, |m_0|^2 — S1's second leg): sum facts
      (X7a); no weight sum exists in the composite; the strata DOUBLE
      instead (X1d). Transport of the kill is exactly the summed-to-
      per-pair bridge the adopted text refuses (V-2 shape). DEAD.
(r-3) THE OSCILLATORY POWER (IBP): o counts plus-band segments (REM
      :146-158, read at bytes); a stays-only stratum has NO plus-band
      segment, and the intraband phase carries no large-|k| decay — the
      sealed o-accounting itself assigns o = 0. No sealed license to
      extract any power. DEAD on the sealed basis.
(r-4) THE BARE-CLASS CANCELLATION (X-5): already SPENT — it removes
      total-order 0 exactly (X1b) and nothing else. And the CONVERSE
      re-import check the tasking ordered: the build's obstruction
      nowhere consumes 52f2490b's +infinity — (s-ii) refuses it in BOTH
      directions; the FAILS-AT rests on majorant ABSENCE, not on any
      divergence. The auditor verified at 52f2490b's OWN bytes (§0,
      LINK 6) that the of-record divergence is carried by the
      bare-inclusive object's degree -3 off-diagonal kernel — the bare
      degree-0 class of the record's typing (R3C :48-49, :415-416;
      798de0dd X-5) — hence genuinely DETACHED from the dressed-only
      composite. NO ILLEGITIMATE RE-IMPORT. DEAD.
```

The supply enumeration (s-i)..(s-v) was checked for EXHAUSTIVENESS by
the auditor's own sweep of the tasked sealed basis for carrier-level
quantitative control: B-L2*'s summed-X sup (REM §2.1/§5 — its object is
the WEIGHT-SUMMED X, :128-134, odd content zero), the per-branch rate
(REM §3.3a, degree -1), the slab/testbed closed forms (witness-family
grade), 52f2490b (bare-inclusive, detached), S1's annotation kills (one
leg disengaged, one leg absent), the covariance (cell axis only, carrier
axis untouched — e5b0879b §4.2's own note), H-R (a named HYPOTHESIS,
not control, and NEVER defaulted). Nothing else exists on that basis.
CONFIRMED EXHAUSTIVE.

And the negative's edge is honest BOTH WAYS: no sealed artifact
evaluates tr K_n at the carrier quantifier or bounds it below — the
testbed values are finite closed forms and carry no carrier axis; the
majorant divergence (Lam^2 on the leading stratum) is a divergence OF
THE MAJORANT, not of the trace; oscillation/resummation is unevaluated
of record. UNDECIDED is the true state; FAILS-AT (not REFUTED) is the
right class, in the record's own convention.

### 2.4 Quantifier audit (each item checked)

```text
(q-1) n-UNIFORMITY: the delivered identities are structural at EVERY n
      (no n-dependent constant consumed anywhere — the only quantitative
      items, KF1 thresholds and KG1 covariance arithmetic, are n-blind);
      no fixed-n result is dressed as uniform; the NEGATIVE (no sealed
      majorant at any composite stratum) is quantified over all n.
(q-2) PER-PAIR CARRIER SUP: no step consumes one — the build's only
      contacts with 52f2490b are refusals (s-ii, w-2), and the auditor's
      re-derivations consume none.
(q-3) SLICE SCOPING: equal-source polydisc slice throughout (798de0dd
      :280-282 read at bytes; C6 untouched; no equal-time object
      anywhere in build or audit).
(q-4) H-R: rho_res appears in the build only inside its fence line and
      the o-3/W-3 citations; NEVER valued, NEVER defaulted — by build or
      auditor.
(q-5) CONDITIONALS: KG1 marked CONDITIONAL(P-1 ray quantifier) exactly
      as 631356a3 e-2/e-3 requires (read at bytes); the deg-2 collapse
      bearing marked CONDITIONAL(R-L4a/b + C-L2) exactly as of record;
      the matrix identities themselves carried unconditional, which they
      are (X2a).
(q-6) THE NARROWED QUANTIFIER itself: licensed at 798de0dd :924-927
      (§2.1 above); the tasked object and the audited object coincide.
```

### 2.5 Corrections (named exactly)

```text
(c-1) THE "ONE FULL POWER" GLOSS — the only defect found, and it is
      verbal, thrice-instanced (:54 "one full power apart"; :213 "THE
      TRACE THRESHOLD SITS ONE FULL POWER PAST THE HS THRESHOLD"; :603 +
      :685 the KF1 label and output). As a statement about the two
      thresholds named in its own parenthetical "(3 vs 3/2)" the gap is
      3/2 powers of symbol degree, NOT one. The clause is exactly true
      in two senses the build itself exhibits — the Lambda-DIVERGENCE-
      RATE gap at the leading degree-1 stratum ((Lam^2-1)/2 vs Lam-1:
      one power of Lambda, KF1's own p = 1 entries, re-derived X5a) and
      the o = 1 flip-strata requirements gap (o + c > 3 vs 2o + c > 3
      differ by o) — but as written against "(3 vs 3/2)" it is loose.
      VERDICT-NEUTRAL: nothing downstream consumes the gloss; every
      consumed number (thresholds 3 and 3/2; shortfalls TWO and
      TWO-plus at the leading strata) is exact and re-verified.
(c-2) PRECISION NOTE, not a correction: "E1 :654 one-per-branch family"
      is substance-accurate paraphrase of the :652-654 display (the
      record's own span convention, per the sealed audit's p-2).
```

### 2.6 THE AUDIT CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM) — SERVES §2 AND §3

sympy 1.14.0, fresh venv `kchaudvenv` under /private/tmp scratch;
nothing written to the workspace but this artifact and its seal. Exact
symbolic / exact rational/surd only; nothing numeric evaluated. ONE
recorded battery, both tasks; labels X1-X8 cited throughout.

Tooling disclosure, on the record — THREE disclosures, no mathematical
claim changed by any. (Abort 1: the X2b det-jet as first drafted used
full expansion of det(1 + aK) at 5x5, combinatorially impractical;
stopped mid-X2b after X1a-X2a printed PASS — those lines byte-match the
recorded run — and X2b was moved to the characteristic-polynomial route
at 4x4. Abort 2: the auditor caught two defects in his OWN harness
before they executed — X3a's bare-stratum substitution targeted
compound matrix entries instead of atomic symbols (would have
false-FAILed a true identity), and X4's zero-tests needed the exact
exp-rewrite normal form to cancel trig identities structurally; both
fixed; stopped after X1a-X1b printed PASS, byte-matching the recorded
run. Termination 3: the recorded run printed X1a-X7a, 17/17 PASS, and
was TERMINATED at the orchestrator's structured-return deadline while
still expanding the LAST conjunct of the LAST check — X8a's
corroborative adjugate instance at 4x4 — with NO FAIL recorded; the
load-bearing X8 content (diamond volume derived by integration; the
(24/pi)L^{-3} transport weight; L-inversion; order-1..3 jets; the
adjugate identity at 3x3, exceeding the build's own 2x2 pin, which
separately reproduced byte-identically) was delivered by ONE
supplementary run, X8s, script and output verbatim below.) The recorded
run: 17/17 PASS on X1a-X7a. The supplement: 1/1 PASS.

SEPARATELY, the build's OWN §2.7 battery was extracted VERBATIM from the
sealed artifact and re-run in the auditor's fresh venv: its output is
BYTE-IDENTICAL to the output block printed in the build (20/20 PASS plus
the three closed-form lines) — the build's claimed output is genuine and
reproducible.

```python
# KCH-AUDIT battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv kchaudvenv).
# Routes chosen to be DIFFERENT from the build's (KCH-BUILD §2.7):
#  X1 swap/parity on a NILPOTENT TWO-SEGMENT genuinely TIME-ORDERED model
#     (exact polynomial exponentials; the build used a free-coefficient formal
#     slot model KB1/KB2 and truncated slab products KE4);
#  X2 channel identities at dims 3 AND 5 via the H+iG Cartesian decomposition
#     (build: generic 4x4); det-jet re-pin at 5x5 (build KA3: 3x3);
#  X3 trace form + TE3 bare-cancellation shape assembled the other way around;
#  X4 testbed by the EXACT-EXPONENTIAL/DUHAMEL route — coupling m INSIDE the
#     frequency Om = sqrt(E^2+m^2), no Dyson simplex integrals (build KE: nested
#     simplex integrals); lands on the record's TG2 closed forms C1/C2;
#  X5 thresholds with the FULL 3D measure and divergence classified by symbolic
#     limits (build KF1: radial integrals only);
#  X6 odd spinor trace in the CHIRAL representation with the polar axis ALONG x
#     (build KF2: Dirac rep, z-polar spherical); plus the auditor's own no-kill
#     check int (q.khat) khat_x dOmega = (4pi/3) q_x != 0;
#  X7 weight kill at an odd QUINTIC with the record surds; census re-pin;
#  X8 covariance with |C_L|_4 DERIVED by integration from the PA diamond
#     geometry (build KG1 consumed pi L^4/24 as given); adjugate at 3x3 AND 4x4.
# Every constant symbolic or an exact rational/surd; NOTHING numeric evaluated.
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def e2(M):
    return sp.expand(((M.trace())**2 - (M*M).trace())/2)

def hs2(M):
    return sp.expand((M*M.H).trace())

def cmat(pre, n, m=None):
    m = n if m is None else m
    return sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im*sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

def Zq(M):
    return sp.expand(M) == sp.zeros(*M.shape)

def upper_nilp(pre, n):
    # strictly upper-triangular generic complex nxn (nilpotent)
    M = sp.zeros(n, n)
    for i in range(n):
        for j in range(i+1, n):
            M[i, j] = sp.Symbol(f'{pre}_{i}{j}', real=True) + Im*sp.Symbol(f'{pre}i_{i}{j}', real=True)
    return M

def nilexp(M, n):
    # exact exp for nilpotent M (M^n = 0)
    S = sp.eye(n)
    T = sp.eye(n)
    for k in range(1, n):
        T = sp.expand(T*M)/k
        S = S + T
    return sp.expand(S)

def mcoeff(M, var, k):
    return M.applyfunc(lambda e: sp.expand(e).coeff(var, k))

# ===== X1 — nilpotent two-segment time-ordered model: swap law and parity =====
# u(s;a) = exp(-i(A2+sB2+aJ2)) * exp(-i(A1+sB1+aJ1)), all slots strictly upper
# 4x4 (exact polynomial exponentials; genuine noncommuting time segments).
# F(s) = d/da u(s;a)|_{a=0}. G_{+-}(m)=u(m)^dag F(-m); G_{-+}(m)=u(-m)^dag F(m);
# G_{++}(m)=u(m)^dag F(m); G_{--}(m)=u(-m)^dag F(-m). C = diag(1,0,1,0).
n4 = 4
A1, B1, J1 = upper_nilp('xa1', n4), upper_nilp('xb1', n4), upper_nilp('xj1', n4)
A2, B2, J2 = upper_nilp('xa2', n4), upper_nilp('xb2', n4), upper_nilp('xj2', n4)
mm = sp.Symbol('m_aud', real=True)
aa = sp.Symbol('a_aud', real=True)

def u_of(s, a):
    G2 = sp.expand(-Im*(A2 + s*B2 + a*J2))
    G1 = sp.expand(-Im*(A1 + s*B1 + a*J1))
    return sp.expand(nilexp(G2, n4)*nilexp(G1, n4))

uf = u_of(mm, aa)
u0a = mcoeff(uf, aa, 0)          # u(m; 0)
Fm = mcoeff(uf, aa, 1)           # d/da at 0 (u is affine-polynomial in a per entry? no:
# a enters polynomially; the Duhamel derivative is the a^1 coefficient exactly.)
u_p = u0a                        # u(+m)
u_m = u0a.subs(mm, -mm)          # u(-m)
F_p = Fm                         # F(+m)
F_m = Fm.subs(mm, -mm)           # F(-m)
Gpm = sp.expand(u_p.H*F_m)
Gmp = sp.expand(u_m.H*F_p)
Gpp = sp.expand(u_p.H*F_p)
Gmm_ = sp.expand(u_m.H*F_m)
ok("X1a SWAP LAW on the nilpotent two-segment time-ordered model (exact, ALL"
   " orders at once — polynomial exponentials, noncommuting segments):"
   " G_-+(m) = G_+-(-m) and G_--(m) = G_++(-m)",
   Zq(sp.expand(Gmp - Gpm.subs(mm, -mm))) and Zq(sp.expand(Gmm_ - Gpp.subs(mm, -mm))))
C4a = sp.diag(1, 0, 1, 0)
G0n = Gpm.subs(mm, 0)
gpm_n = sp.expand(Gpm - G0n)
gmp_n = sp.expand(Gmp - G0n)
Kn_ = sp.expand(C4a*(gpm_n + gmp_n.H)*C4a)
ok("X1b DRESSED n = 0 ABSENCE on the model: g(0) = 0 and [m^0]K = 0 (the G^0"
   " subtraction removes exactly the total-order-0 stratum)",
   Zq(mcoeff(Kn_, mm, 0)) and Zq(sp.expand(gpm_n.subs(mm, 0))))
par_ok = True
deg_max = 6
gstr = {k: mcoeff(gpm_n, mm, k) for k in range(1, deg_max + 1)}
for k in range(1, deg_max + 1):
    lhs = mcoeff(Kn_, mm, k)
    rhs = sp.expand(C4a*(gstr[k] + (-1)**k*gstr[k].H)*C4a)
    par_ok = par_ok and Zq(sp.expand(lhs - rhs))
    if k % 2 == 0:
        par_ok = par_ok and Zq(sp.expand(lhs - lhs.H))       # Hermitian stratum
    else:
        par_ok = par_ok and Zq(sp.expand(lhs + lhs.H))       # anti-Hermitian stratum
ok("X1c PARITY LOCALIZATION on the model, orders m^1..m^6 (beyond the build's"
   " n <= 3): [m^k]K = C(g^(k) + (-1)^k g^(k)dag)C; even strata Hermitian,"
   " odd strata anti-Hermitian — K_H = even, K_A = odd, coefficientwise",
   par_ok)
Qsum_n = sp.expand((Gpp + Gpm + Gmp + Gmm_)/16)
quart_ok = all(Zq(mcoeff(Qsum_n, mm, k)) for k in (1, 3, 5))
dbl_ok = True
for k in range(1, deg_max + 1):
    dif = mcoeff(sp.expand(Gpm - Gmp), mm, k)
    sm = mcoeff(sp.expand(Gpm + Gmp), mm, k)
    if k % 2 == 1:
        dbl_ok = dbl_ok and Zq(sp.expand(dif - 2*gstr[k])) and Zq(sm)
    else:
        dbl_ok = dbl_ok and Zq(dif) and Zq(sp.expand(sm - 2*gstr[k]))
ok("X1d QUARTET KILL vs COMPOSITE DOUBLING on the model (orders to m^6): the"
   " equal-weight quartet sum is EVEN in m (odd strata vanish identically);"
   " the cross composite carries [m^odd](G_+- - G_-+) = 2 g^(odd) and"
   " [m^even](G_+- + G_-+) = 2 g^(even) — the doubling, at genuine time-ordering",
   quart_ok and dbl_ok)

# ===== X2 — channel identities via H+iG at dims 3 and 5; det-jet at 5x5 =====
x2_ok = True
for dim in (3, 5):
    Kg = cmat(f'yk{dim}', dim)
    Hh = sp.expand((Kg + Kg.H)/2)
    Gg = sp.expand(-Im*(Kg - Kg.H)/2)      # H_A
    x2_ok = x2_ok and Zq(sp.expand(Hh - Hh.H)) and Zq(sp.expand(Gg - Gg.H))
    x2_ok = x2_ok and Zq(sp.expand(Kg - (Hh + Im*Gg)))
    trK2 = sp.expand((Kg*Kg).trace())
    x2_ok = x2_ok and sp.expand((trK2 + sp.conjugate(trK2))/2
                                - (hs2(Hh) - hs2(Gg))) == 0
    trKsq = sp.expand((Kg.trace())**2)
    x2_ok = x2_ok and sp.expand((trKsq + sp.conjugate(trKsq))/2
                                - ((Hh.trace())**2 - (Gg.trace())**2)) == 0
    re_e2 = sp.expand((e2(Kg) + sp.conjugate(e2(Kg)))/2)
    x2_ok = x2_ok and sp.expand(re_e2 - (((Hh.trace())**2 - (Gg.trace())**2)/2
                                         - (hs2(Hh) - hs2(Gg))/2)) == 0
    x2_ok = x2_ok and sp.expand(re_e2 - (e2(Hh) + e2(Im*Gg))) == 0
    x2_ok = x2_ok and sp.expand((Hh*(Im*Gg)).trace()
                                + sp.conjugate((Hh*(Im*Gg)).trace())) == 0
    x2_ok = x2_ok and sp.expand(trK2 - (trKsq - 2*e2(Kg))) == 0
ok("X2a CHANNEL IDENTITIES via K = H + iG (H = K_H, G = H_A Hermitian), dims 3"
   " AND 5: Re tr[K^2] = ||K_H||_2^2 - ||H_A||_2^2 ; Re (tr K)^2 = (tr K_H)^2"
   " - (tr H_A)^2 ; Re e2(K) = (1/2)[...] - (1/2)[...] = e2(K_H) + e2(K_A) ;"
   " tr[K_H K_A] purely imaginary ; tr[K^2] = (tr K)^2 - 2 e2(K)",
   x2_ok)
dimd = 4
K5 = cmat('yd4', dimd)
lv = sp.Symbol('lam_cp')
cp = K5.charpoly(lv)   # det(lv - K) = lv^4 - e1 lv^3 + e2 lv^2 - e3 lv + e4
ok("X2b det-jet re-pin by the CHARACTERISTIC-POLYNOMIAL route at 4x4 (build:"
   " 3x3 det-subtraction): det(lv - K) has [lv^3] = -tr K and [lv^2] = e2(K),"
   " hence det(1 + aK) = 1 + a tr K + a^2 e2(K) + O(a^3) — the slice"
   " determinant's a^2-jet is the two-channel object",
   sp.expand(cp.coeff_monomial(lv**3) + K5.trace()) == 0
   and sp.expand(cp.coeff_monomial(lv**2) - e2(K5)) == 0)
A5r = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'zr_{i}{j}', real=True))
B5r = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'zi_{i}{j}', real=True))
M5 = A5r + Im*B5r
ok("X2c SUM-OF-SQUARES at 3x3 (build: 4x4): ||M||_2^2 = SUM (Re)^2 + (Im)^2 —"
   " formal nonnegativity of each channel norm-square",
   sp.expand(hs2(M5) - sum(A5r[i, j]**2 + B5r[i, j]**2
                           for i in range(3) for j in range(3))) == 0)

# ===== X3 — trace form assembled the other way; TE3 bare-cancellation =====
n3 = 3
Ug = cmat('zu', n3)
Sp_ = sp.expand((cmat('zsp', n3) + cmat('zsp', n3).H)/2)
Sm_ = sp.expand((cmat('zsm', n3) + cmat('zsm', n3).H)/2)
C3a = sp.diag(1, 0, 1)   # different projector than the build's diag(1,1,0)
Gpm3 = sp.expand(Ug*(-Im*Sm_))          # G_+- = U G_--, G_-- = -i S_-
Gmp3 = sp.expand(Ug.H*(-Im*Sp_))        # G_-+ = U^dag G_++, G_++ = -i S_+
K3_ = sp.expand(C3a*(Gpm3 + Gmp3.H)*C3a)
bare_subs = {}
for i_ in range(n3):
    for j_ in range(n3):
        bare_subs[sp.Symbol(f'zu_{i_}{j_}', real=True)] = sp.Integer(1 if i_ == j_ else 0)
        bare_subs[sp.Symbol(f'zui_{i_}{j_}', real=True)] = sp.Integer(0)
        bare_subs[sp.Symbol(f'zsm_{i_}{j_}', real=True)] = sp.Symbol(f'zsp_{i_}{j_}', real=True)
        bare_subs[sp.Symbol(f'zsmi_{i_}{j_}', real=True)] = sp.Symbol(f'zspi_{i_}{j_}', real=True)
ok("X3a TRACE FORM assembled from the insertion law directly: K = C(G_+- +"
   " G_-+^dag)C = -i C(U S_- - S_+ U)C ; tr K = -i tr[(U S_- - S_+ U)C]"
   " (C^2 = C, cyclicity); at U = 1, S_- = S_+ (atomic substitution):"
   " K = 0 identically — the dressed-only trace re-pin",
   Zq(sp.expand(K3_ - (-Im)*C3a*(Ug*Sm_ - Sp_*Ug)*C3a))
   and sp.expand(K3_.trace() - (-Im)*((Ug*Sm_ - Sp_*Ug)*C3a).trace()) == 0
   and Zq(sp.expand(K3_.subs(bare_subs))))
G0h = cmat('zg0', n3)
G0anti = sp.expand((G0h - G0h.H)/2)     # generic anti-Hermitian
gp3, gm3 = cmat('zgp', n3), cmat('zgm', n3)
lhs3 = sp.expand(C3a*((G0anti + gp3) + (G0anti + gm3).H)*C3a)
rhs3 = sp.expand(C3a*(gp3 + gm3.H)*C3a)
ok("X3b TE3 BARE-CANCELLATION SHAPE: with G^0 anti-Hermitian, C((G^0 + g_+-) +"
   " (G^0 + g_-+)^dag)C = C(g_+- + g_-+^dag)C — the bare stratum cancels inside"
   " the composite (the X-5 confinement at the K-level)",
   Zq(sp.expand(lhs3 - rhs3)))

# ===== X4 — testbed by the exact-exponential/Duhamel route =====
P_ = sp.Symbol('P', positive=True)
tt = sp.Symbol('t_aud', real=True)
mS = sp.Symbol('m_tb', real=True)
E1v, E2v = 4*P_, 6*P_
J4s = sp.zeros(4, 4)
jsym = {}
for i_ in range(4):
    for k_ in range(i_, 4):
        s_ = sp.Symbol(f'J{i_}{k_}', real=True)
        jsym[(i_, k_)] = s_
        J4s[i_, k_] = s_
        J4s[k_, i_] = s_

def blk(Ev, s, m):
    # exact exp(-i [[Ev, m],[m, -Ev]] s) via Om = sqrt(Ev^2 + m^2)
    Om = sp.sqrt(Ev**2 + m**2)
    Hb = sp.Matrix([[Ev, m], [m, -Ev]])
    return sp.cos(Om*s)*sp.eye(2) - Im*sp.sin(Om*s)/Om*Hb

def Uex(s, m):
    return sp.diag(blk(E1v, s, m), blk(E2v, s, m))

ordm = 3
Ex = lambda e: sp.expand(sp.expand(e).rewrite(sp.exp))   # exact exp-normal form
ZqE = lambda M: sp.expand(M.applyfunc(Ex)) == sp.zeros(*M.shape)
User = Uex(1, mS).applyfunc(lambda e: Ex(sp.series(e, mS, 0, ordm).removeO()))
integrand = sp.expand(Uex(1 - tt, mS)*J4s*Uex(tt, mS))
integrand_ser = integrand.applyfunc(
    lambda e: Ex(sp.series(e, mS, 0, ordm).removeO()))
Fser = sp.expand(-Im*sp.integrate(integrand_ser, (tt, 0, 1)))
# G_{+-}(m) = u(m)^dag F(-m); G_{-+}(m) = u(-m)^dag F(m); truncate at m^2
Gpm_t = sp.expand(User.H*Fser.subs(mS, -mS))
Gmp_t = sp.expand(User.H.subs(mS, -mS)*Fser)
G0_t = Gpm_t.subs(mS, 0)
Ct = sp.diag(0, 1, 0, 1)
Kfull_t = sp.expand(Ct*((Gpm_t - G0_t) + (Gmp_t - G0_t).H)*Ct).applyfunc(Ex)
K1t = mcoeff(Kfull_t, mS, 1)
K2t = mcoeff(Kfull_t, mS, 2)
ok("X4a exact-exponential route re-pins the parity localization at the sealed"
   " testbed: [m^1]K anti-Hermitian, [m^2]K Hermitian; [m^0]K = 0 (dressed)",
   ZqE(sp.expand(K1t + K1t.H)) and ZqE(sp.expand(K2t - K2t.H))
   and ZqE(mcoeff(Kfull_t, mS, 0)))
K1c = sp.Matrix([[K1t[1, 1], K1t[1, 3]], [K1t[3, 1], K1t[3, 3]]])
trK1t = Ex(K1c.trace())
C1_rec = -Im/(2*P_) + sp.exp(8*Im*P_)/(32*P_**2) - sp.exp(-8*Im*P_)/(32*P_**2)
C2_rec = -Im/(3*P_) + sp.exp(12*Im*P_)/(72*P_**2) - sp.exp(-12*Im*P_)/(72*P_**2)
c1a = sp.expand(trK1t.coeff(jsym[(0, 1)]))
c2a = sp.expand(trK1t.coeff(jsym[(2, 3)]))
sin1 = Im*(sp.sin(8*P_) - 8*P_)/(16*P_**2)
sin2 = Im*(sp.sin(12*P_) - 12*P_)/(36*P_**2)
ok("X4b THE DIAGONAL CHANNEL, independently: [J_p1s1] tr K^(1) = C1 and"
   " [J_p2s2] tr K^(1) = C2 EXACTLY as printed at 798de0dd :839-840, equal to"
   " the audit sin-forms i(sin 8P - 8P)/(16P^2), i(sin 12P - 12P)/(36P^2);"
   " purely imaginary trace; no J_p1s1 in the off-diagonal/partner entries",
   Ex(c1a - C1_rec) == 0 and Ex(c2a - C2_rec) == 0
   and Ex(c1a - sin1) == 0 and Ex(c2a - sin2) == 0
   and Ex(trK1t + sp.conjugate(trK1t)) == 0
   and Ex(Ex(K1c[0, 1]).coeff(jsym[(0, 1)])) == 0
   and Ex(Ex(K1c[1, 0]).coeff(jsym[(0, 1)])) == 0
   and Ex(Ex(K1c[1, 1]).coeff(jsym[(0, 1)])) == 0)
ser1a = sp.series(c1a, P_, 0, 2).removeO()
ser2a = sp.series(c2a, P_, 0, 2).removeO()
ok("X4c Taylor certificates, independently: [P^1]C1 = -16i/3 != 0 and"
   " [P^1]C2 = -8i != 0 (nonvanishing of the dressed trace at the testbed)",
   sp.expand(ser1a.coeff(P_, 1) + 16*Im/3) == 0
   and sp.expand(ser2a.coeff(P_, 1) + 8*Im) == 0)
trK1sq_a = Ex((K1c*K1c).trace())
coJ2 = sp.expand(trK1sq_a).coeff(jsym[(0, 1)], 2)
serX = sp.series(sp.expand(c1a**2), P_, 0, 3).removeO()
ok("X4d THE EXCHANGE CHANNEL, independently: tr[(K^(1))^2] = -||K^(1)||_2^2"
   " EXACTLY (anti-Hermitian pin); [J_p1s1^2] tr[(K^(1))^2] = C1^2;"
   " [P^2] C1^2 = -256/9 != 0",
   Ex(trK1sq_a + hs2(K1c)) == 0
   and Ex(coJ2 - c1a**2) == 0
   and sp.expand(serX.coeff(P_, 2) + R(256, 9)) == 0)

# ===== X5 — thresholds with the full 3D measure; divergence classified =====
kk = sp.Symbol('k_aud', positive=True)
Lam = sp.Symbol('Lambda_aud', positive=True)
th_, ph_ = sp.symbols('th_m ph_m', real=True)
x5_ok = True
vals = {}
for p in (1, 2, 3, 4):
    I3d = sp.integrate(kk**2*sp.sin(th_)*kk**(-p),
                       (ph_, 0, 2*sp.pi), (th_, 0, sp.pi), (kk, 1, Lam))
    vals[p] = sp.simplify(I3d)
x5_ok = x5_ok and sp.simplify(vals[1] - 4*sp.pi*(Lam**2 - 1)/2) == 0
x5_ok = x5_ok and sp.simplify(vals[2] - 4*sp.pi*(Lam - 1)) == 0
x5_ok = x5_ok and sp.simplify(vals[3] - 4*sp.pi*sp.log(Lam)) == 0
x5_ok = x5_ok and sp.simplify(vals[4] - 4*sp.pi*(1 - 1/Lam)) == 0
div = {p: sp.limit(vals[p], Lam, sp.oo) for p in (1, 2, 3, 4)}
x5_ok = x5_ok and div[1] == sp.oo and div[2] == sp.oo and div[3] == sp.oo \
    and div[4] == 4*sp.pi
hs_lin = sp.integrate(kk**2*sp.sin(th_)*kk**(-2),
                      (ph_, 0, 2*sp.pi), (th_, 0, sp.pi), (kk, 1, Lam))
x5_ok = x5_ok and sp.simplify(hs_lin - 4*sp.pi*(Lam - 1)) == 0
ok("X5a THRESHOLD ARITHMETIC, full 3D measure (Lambda FORMAL, classified by"
   " symbolic limit): int_{1<|k|<Lam} |k|^-p d^3k diverges for p = 1 (Lam^2),"
   " p = 2 (Lam), p = 3 (log Lam) and converges for p = 4 — TRACE needs degree"
   " p > 3 strictly; HS^2 of a degree-1 symbol = int |k|^-2 d^3k is LINEAR in"
   " the carrier (REM 3.1 mechanism), and HS^2 needs 2p > 3 — the trace"
   " threshold (p = 3) sits 3/2 powers of symbol degree past the HS threshold"
   " (p = 3/2), i.e. one full power of Lambda in divergence rate at the"
   " leading degree-1 stratum (Lam^2 vs Lam)",
   x5_ok)

# ===== X6 — chiral representation; polar axis along x; the no-kill integral ===
s1p = sp.Matrix([[0, 1], [1, 0]])
s2p = sp.Matrix([[0, -Im], [Im, 0]])
s3p = sp.Matrix([[1, 0], [0, -1]])
alC = [sp.Matrix(sp.BlockMatrix([[s, sp.zeros(2, 2)], [sp.zeros(2, 2), -s]]))
       for s in (s1p, s2p, s3p)]   # CHIRAL representation
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
adkC = kx*alC[0] + ky*alC[1] + kz*alC[2]
sB = sp.Symbol('s_bd', real=True)
PsC = (sp.eye(4) + sB*adkC)/2
unit_red = lambda e: sp.expand(e).subs(kx**2, 1 - ky**2 - kz**2)
x6a = Zq(unit_red(sp.expand(adkC*adkC - sp.eye(4)))) \
    and sp.expand((PsC*alC[0]).trace() - 2*sB*kx) == 0 \
    and sp.expand((PsC*alC[1]).trace() - 2*sB*ky) == 0
# polar axis ALONG x: khat_x = cos(u), measure sin(u) du dv
u_, v_ = sp.symbols('u_ax v_ax', real=True)
m1 = sp.integrate(sp.cos(u_)*sp.sin(u_), (v_, 0, 2*sp.pi), (u_, 0, sp.pi))
m2 = sp.integrate(sp.cos(u_)**2*sp.sin(u_), (v_, 0, 2*sp.pi), (u_, 0, sp.pi))
mxy = sp.integrate(sp.cos(u_)*sp.sin(u_)*sp.cos(v_)*sp.sin(u_),
                   (v_, 0, 2*sp.pi), (u_, 0, sp.pi))
x6a = x6a and m1 == 0 and sp.simplify(m2 - 4*sp.pi/3) == 0 and mxy == 0
ok("X6a ODD-TRACE KILL in the CHIRAL representation, polar axis along x:"
   " (alpha.khat)^2 = I on the sphere; tr[P_s alpha_x] = 2 s khat_x (and the"
   " y-component likewise, = -2 khat_x on the sea band s = -1, the E1"
   " :691-694 annotation); int_S2 khat_x dOmega = 0 (the kill), int_S2"
   " khat_x^2 dOmega = 4pi/3 != 0 (worth exactly ONE power),"
   " int khat_x khat_y = 0 (isotropy)",
   x6a)
qx, qy, qz = sp.symbols('q_x q_y q_z', real=True)
# int_S2 (q.khat) khat_x dOmega with x-polar axis: khat = (cos u, sin u cos v, sin u sin v)
qdotk = qx*sp.cos(u_) + qy*sp.sin(u_)*sp.cos(v_) + qz*sp.sin(u_)*sp.sin(v_)
nokill = sp.integrate(qdotk*sp.cos(u_)*sp.sin(u_), (v_, 0, 2*sp.pi), (u_, 0, sp.pi))
ok("X6b THE AUDITOR'S NO-KILL CHECK at the (1,0)/(0,1) stratum: the large-k"
   " J5-leading factor pairs with the longitudinal khat_x as (q.khat) khat_x;"
   " int_S2 (q.khat) khat_x dOmega = (4pi/3) q_x != 0 — the paired EVEN"
   " integrand survives angular averaging: the surviving S1 leg does NOT"
   " engage, exactly as the build's o-2-OBS states",
   sp.simplify(nokill - 4*sp.pi*qx/3) == 0)

# ===== X7 — weight kill at an odd quintic; census re-pin =====
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
a1q, a3q, a5q = sp.symbols('a1_q a3_q a5_q', real=True)
oddq = lambda l: a1q*l + a3q*l**3 + a5q*l**5
kill5 = sp.nsimplify(sum(w*oddq(l) for w, l in zip(w_v, lam_v)))
mom = lambda k: sp.nsimplify(sum(w*l**k for w, l in zip(w_v, lam_v)))
ph = [1, -1, -1]
surv = sorted([(i, j) for i in range(3) for j in range(3) if ph[i]*ph[j] == 1])
N = sum(w_v[i]*w_v[j] for (i, j) in surv)
cw = {p: sp.nsimplify(w_v[p[0]]*w_v[p[1]]/N) for p in surv}
ok("X7a WEIGHT KILL at an odd QUINTIC (beyond the build's cubic), record surds"
   " (0, +-sqrt2), weights (1/2, -1/4, -1/4): sum_l w_l f(l) = 0; moments"
   " m_0 = m_1 = m_3 = 0, m_2 = -1; census surv = {(0,0)} + quartet with"
   " c = (1/2, 1/8 x 4), sum 1 — the kills are weight-SUM facts, with no"
   " counterpart in the single composite",
   sp.expand(kill5) == 0 and mom(0) == 0 and mom(1) == 0 and mom(2) == -1
   and mom(3) == 0
   and surv == [(0, 0), (1, 1), (1, 2), (2, 1), (2, 2)] and N == R(1, 2)
   and cw[(0, 0)] == R(1, 2) and all(cw[p] == R(1, 8) for p in surv if p != (0, 0))
   and sum(cw.values()) == 1)

# ===== X8 — covariance: diamond volume DERIVED; jets to order 3; adjugate ====
Lsym = sp.Symbol('L_cov', positive=True)
tv, rv = sp.symbols('t_cov r_cov', positive=True)
# |C_L|_4 = int_0^L (4pi/3) min(t, L-t)^3 dt, derived from PA :110-111 geometry
vol = sp.integrate(R(4, 3)*sp.pi*tv**3, (tv, 0, Lsym/2)) \
    + sp.integrate(R(4, 3)*sp.pi*(Lsym - tv)**3, (tv, Lsym/2, Lsym))
x8a = sp.simplify(vol - sp.pi*Lsym**4/24) == 0
wgt = sp.simplify((1/vol)*Lsym - (24/sp.pi)*Lsym**(-3)) == 0
Linv = sp.simplify((24*(sp.pi*Lsym**4/24)/sp.pi)**R(1, 4) - Lsym) == 0
r1, r2, r3 = sp.symbols('r1_c r2_c r3_c')
av2 = sp.Symbol('a_cov')
Rser = r1*av2 + r2*av2**2 + r3*av2**3
jets = all(sp.expand(Rser.subs(av2, Lsym*av2).coeff(av2, j)
                     - Lsym**j*[r1, r2, r3][j-1]) == 0 for j in (1, 2, 3))
adj_ok = True
for dim in (3, 4):
    Vg = cmat(f'w{dim}v', dim)
    Mg = cmat(f'w{dim}m', dim)
    adj_ok = adj_ok and sp.expand((Vg*Mg*Vg.adjugate()).trace()
                                  - Vg.det()*Mg.trace()) == 0
ok("X8a COVARIANCE: |C_L|_4 DERIVED by integration from the PA :110-111"
   " diamond (= pi L^4/24, the build consumed it); S1 transport weight"
   " |C_L|_4^{-1} L = (24/pi) L^{-3} EXACT and L = (24|C|_4/pi)^{1/4}; jet"
   " transport [a^j] R(La) = L^j r_j for j = 1,2,3; division-free"
   " tr[V M adj V] = det(V) tr M at 3x3 AND 4x4",
   x8a and wgt and Linv and jets and adj_ok)

print("KCH-AUDIT-BATTERY-DONE")
```

Output, verbatim (17/17 PASS, the recorded run, terminated as disclosed
inside X8a's final corroborative conjunct — no FAIL):

```text
X1a SWAP LAW on the nilpotent two-segment time-ordered model (exact, ALL orders at once — polynomial exponentials, noncommuting segments): G_-+(m) = G_+-(-m) and G_--(m) = G_++(-m): PASS
X1b DRESSED n = 0 ABSENCE on the model: g(0) = 0 and [m^0]K = 0 (the G^0 subtraction removes exactly the total-order-0 stratum): PASS
X1c PARITY LOCALIZATION on the model, orders m^1..m^6 (beyond the build's n <= 3): [m^k]K = C(g^(k) + (-1)^k g^(k)dag)C; even strata Hermitian, odd strata anti-Hermitian — K_H = even, K_A = odd, coefficientwise: PASS
X1d QUARTET KILL vs COMPOSITE DOUBLING on the model (orders to m^6): the equal-weight quartet sum is EVEN in m (odd strata vanish identically); the cross composite carries [m^odd](G_+- - G_-+) = 2 g^(odd) and [m^even](G_+- + G_-+) = 2 g^(even) — the doubling, at genuine time-ordering: PASS
X2a CHANNEL IDENTITIES via K = H + iG (H = K_H, G = H_A Hermitian), dims 3 AND 5: Re tr[K^2] = ||K_H||_2^2 - ||H_A||_2^2 ; Re (tr K)^2 = (tr K_H)^2 - (tr H_A)^2 ; Re e2(K) = (1/2)[...] - (1/2)[...] = e2(K_H) + e2(K_A) ; tr[K_H K_A] purely imaginary ; tr[K^2] = (tr K)^2 - 2 e2(K): PASS
X2b det-jet re-pin by the CHARACTERISTIC-POLYNOMIAL route at 4x4 (build: 3x3 det-subtraction): det(lv - K) has [lv^3] = -tr K and [lv^2] = e2(K), hence det(1 + aK) = 1 + a tr K + a^2 e2(K) + O(a^3) — the slice determinant's a^2-jet is the two-channel object: PASS
X2c SUM-OF-SQUARES at 3x3 (build: 4x4): ||M||_2^2 = SUM (Re)^2 + (Im)^2 — formal nonnegativity of each channel norm-square: PASS
X3a TRACE FORM assembled from the insertion law directly: K = C(G_+- + G_-+^dag)C = -i C(U S_- - S_+ U)C ; tr K = -i tr[(U S_- - S_+ U)C] (C^2 = C, cyclicity); at U = 1, S_- = S_+ (atomic substitution): K = 0 identically — the dressed-only trace re-pin: PASS
X3b TE3 BARE-CANCELLATION SHAPE: with G^0 anti-Hermitian, C((G^0 + g_+-) + (G^0 + g_-+)^dag)C = C(g_+- + g_-+^dag)C — the bare stratum cancels inside the composite (the X-5 confinement at the K-level): PASS
X4a exact-exponential route re-pins the parity localization at the sealed testbed: [m^1]K anti-Hermitian, [m^2]K Hermitian; [m^0]K = 0 (dressed): PASS
X4b THE DIAGONAL CHANNEL, independently: [J_p1s1] tr K^(1) = C1 and [J_p2s2] tr K^(1) = C2 EXACTLY as printed at 798de0dd :839-840, equal to the audit sin-forms i(sin 8P - 8P)/(16P^2), i(sin 12P - 12P)/(36P^2); purely imaginary trace; no J_p1s1 in the off-diagonal/partner entries: PASS
X4c Taylor certificates, independently: [P^1]C1 = -16i/3 != 0 and [P^1]C2 = -8i != 0 (nonvanishing of the dressed trace at the testbed): PASS
X4d THE EXCHANGE CHANNEL, independently: tr[(K^(1))^2] = -||K^(1)||_2^2 EXACTLY (anti-Hermitian pin); [J_p1s1^2] tr[(K^(1))^2] = C1^2; [P^2] C1^2 = -256/9 != 0: PASS
X5a THRESHOLD ARITHMETIC, full 3D measure (Lambda FORMAL, classified by symbolic limit): int_{1<|k|<Lam} |k|^-p d^3k diverges for p = 1 (Lam^2), p = 2 (Lam), p = 3 (log Lam) and converges for p = 4 — TRACE needs degree p > 3 strictly; HS^2 of a degree-1 symbol = int |k|^-2 d^3k is LINEAR in the carrier (REM 3.1 mechanism), and HS^2 needs 2p > 3 — the trace threshold (p = 3) sits 3/2 powers of symbol degree past the HS threshold (p = 3/2), i.e. one full power of Lambda in divergence rate at the leading degree-1 stratum (Lam^2 vs Lam): PASS
X6a ODD-TRACE KILL in the CHIRAL representation, polar axis along x: (alpha.khat)^2 = I on the sphere; tr[P_s alpha_x] = 2 s khat_x (and the y-component likewise, = -2 khat_x on the sea band s = -1, the E1 :691-694 annotation); int_S2 khat_x dOmega = 0 (the kill), int_S2 khat_x^2 dOmega = 4pi/3 != 0 (worth exactly ONE power), int khat_x khat_y = 0 (isotropy): PASS
X6b THE AUDITOR'S NO-KILL CHECK at the (1,0)/(0,1) stratum: the large-k J5-leading factor pairs with the longitudinal khat_x as (q.khat) khat_x; int_S2 (q.khat) khat_x dOmega = (4pi/3) q_x != 0 — the paired EVEN integrand survives angular averaging: the surviving S1 leg does NOT engage, exactly as the build's o-2-OBS states: PASS
X7a WEIGHT KILL at an odd QUINTIC (beyond the build's cubic), record surds (0, +-sqrt2), weights (1/2, -1/4, -1/4): sum_l w_l f(l) = 0; moments m_0 = m_1 = m_3 = 0, m_2 = -1; census surv = {(0,0)} + quartet with c = (1/2, 1/8 x 4), sum 1 — the kills are weight-SUM facts, with no counterpart in the single composite: PASS
```

The X8s supplement, script verbatim:

```python
# KCH-AUDIT supplementary check X8s — EXACT SYMBOLIC ONLY (same venv kchaudvenv).
# Lean replacement for the recorded battery's X8a, whose FINAL corroborative
# conjunct (the adjugate identity at 4x4) was still mid-expansion when the
# orchestrator's deadline arrived; the recorded run was terminated there with
# NO FAIL. This supplement carries ALL load-bearing X8 content: the diamond
# 4-volume DERIVED by integration from the PA :110-111 geometry; the S1
# transport weight; the L-inversion; jet transport to order 3; the adjugate
# identity at 3x3 (already exceeding the build's 2x2 instance — and the
# build's own 2x2 pin was separately reproduced BYTE-IDENTICALLY, §2.6).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def cmat(pre, n):
    return sp.Matrix(n, n, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im*sp.Matrix(n, n, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

Lsym = sp.Symbol('L_cov', positive=True)
tv = sp.Symbol('t_cov', positive=True)
vol = sp.integrate(R(4, 3)*sp.pi*tv**3, (tv, 0, Lsym/2)) \
    + sp.integrate(R(4, 3)*sp.pi*(Lsym - tv)**3, (tv, Lsym/2, Lsym))
x8_vol = sp.simplify(vol - sp.pi*Lsym**4/24) == 0
x8_wgt = sp.simplify((1/vol)*Lsym - (24/sp.pi)*Lsym**(-3)) == 0
x8_Linv = sp.simplify((24*(sp.pi*Lsym**4/24)/sp.pi)**R(1, 4) - Lsym) == 0
r1, r2, r3 = sp.symbols('r1_c r2_c r3_c')
av2 = sp.Symbol('a_cov')
Rser = r1*av2 + r2*av2**2 + r3*av2**3
x8_jets = all(sp.expand(Rser.subs(av2, Lsym*av2).coeff(av2, j)
                        - Lsym**j*[r1, r2, r3][j-1]) == 0 for j in (1, 2, 3))
Vg = cmat('w3v', 3)
Mg = cmat('w3m', 3)
x8_adj = sp.expand((Vg*Mg*Vg.adjugate()).trace() - Vg.det()*Mg.trace()) == 0
ok("X8s COVARIANCE (lean): |C_L|_4 DERIVED by integration from the PA"
   " :110-111 diamond (= pi L^4/24, which the build consumed as given); S1"
   " transport weight |C_L|_4^{-1} L = (24/pi) L^{-3} EXACT; L ="
   " (24|C|_4/pi)^{1/4}; jet transport [a^j] R(La) = L^j r_j for j = 1,2,3;"
   " division-free tr[V M adj V] = det(V) tr M at 3x3 generic complex"
   " (exceeds the build's 2x2 instance)",
   x8_vol and x8_wgt and x8_Linv and x8_jets and x8_adj)
print("KCH-AUDIT-X8S-DONE")
```

X8s output, verbatim (1/1 PASS, single run):

```text
X8s COVARIANCE (lean): |C_L|_4 DERIVED by integration from the PA :110-111 diamond (= pi L^4/24, which the build consumed as given); S1 transport weight |C_L|_4^{-1} L = (24/pi) L^{-3} EXACT; L = (24|C|_4/pi)^{1/4}; jet transport [a^j] R(La) = L^j r_j for j = 1,2,3; division-free tr[V M adj V] = det(V) tr M at 3x3 generic complex (exceeds the build's 2x2 instance): PASS
KCH-AUDIT-X8S-DONE
```

### 2.7 Task A verdict

```text
TASKA-AUDIT/O2 = CONFIRMED( the build's FAILS-AT stands exactly as
  built, at its stated quantifier: the identity layer (narrowing;
  coupling-parity split; slot-insertion trace form; testbed
  nonvanishing; cell-axis transport weight (24/pi)L^{-3} CONDITIONAL
  P-1 — re-derived X8s with |C_L|_4 = pi L^4/24 DERIVED from the PA
  diamond geometry by integration, closing a loop the build consumed as
  given) is DELIVERED and survives independent re-derivation by
  disjoint routes (X1-X4, X8s); the obstruction is REAL — the composite
  opens at (c = 1, o = 0), degree -1, where every sealed kill is either
  a weight-sum fact with no object (X7a/X1d) or an angular kill that
  does not engage (X6b), no sealed majorant reaches degree < -3 at any
  composite stratum (supply enumeration confirmed exhaustive), and no
  route past it exists on the sealed basis (r-1..r-4 each dead);
  UNDECIDED, not divergent — FAILS-AT, not REFUTED, is the honest
  class; ONE verbal correction (c-1), verdict-neutral. )
```

---

## 3. TASK B ADJUDICATION — o-1, THE EXCHANGE CHANNEL tr[K_n^2]

### 3.1 The identity layer: CONFIRMED

The exchange-channel reduction Re tr[K_n^2] = ||K_H,n||_2^2 -
||H_A,n||_2^2, the four-quantity survivor form T_n = (a^2/8)[
(tr K_H)^2 - (tr H_A)^2 - ||K_H||_2^2 + ||H_A||_2^2 ], the dependency
tr[K^2] = (tr K)^2 - 2 e_2(K), and the sum-of-squares definiteness of
each sector quantity were re-derived at dims 3 AND 5 by the H+iG route
(X2a/X2c) and instantiated at the testbed by the exact-exponential
route (X4d: tr[(K^(1))^2] = -||K^(1)||_2^2 exactly; [J_p1s1^2]
tr[(K^(1))^2] = C1^2; certificate [P^2]C1^2 = -256/9 != 0). The
"consumed part is the REAL part" scoping matches TE5 of record
(798de0dd :285-289, both e_2's real). The matrix identities are
unconditional; the deg-2 collapse bearing is carried
CONDITIONAL(R-L4a/b + C-L2) exactly as of record. CONFIRMED.

### 3.2 Where W-2's per-pair HS divergence does and does not enter: CONFIRMED AT BYTES

The build's (w-1)..(w-5) were each checked at their sources: (w-1) the
convexity clause quantifies the FIVE c-weighted per-pair majorants
(e5b0879b :402-405, "ALL FIVE c ARE POSITIVE" read verbatim) — the
composite carries no c-weights, so the clause has no object; (w-2) the
of-record +infinity is the BARE-INCLUSIVE per-pair carrier sup
(52f2490b §0 + LINK 6 at its own bytes; typing R3C :48-49/:415-416;
confinement 798de0dd X-5): it does NOT attach to the dressed-only
composite, and the build claims no finite majorant from that quarter
either; (w-3) B-L2*'s sup covers the weight-summed X alone (REM
:128-134), whose odd-insertion content is ZERO by the weight kill —
parity-orthogonal to ||H_A||_2's entire content — and extraction would
be the V-2-refused bridge; (w-4) the counting position re-derived
(§2.3): ||H_A||_2 opens at 2o + c = 1 (the class o-1's own text names,
798de0dd :919-923), ||K_H||_2 at 2o + c = 2, both below the sealed HS
threshold, neither shown infinite (no sealed lower bound; testbed
values finite); (w-5) the BAR intact — the identities are equalities
for the summed slice content of record (TE8's own parenthesis,
798de0dd :313-317), no per-pair majorant chain is invoked by build or
auditor, and the obstruction only NAMES uncontrolled quantities. ALL
CONFIRMED.

### 3.3 Task B verdict

```text
TASKB-AUDIT/O1 = CONFIRMED( the build's PARTIAL-DELIVERED stands at its
  stated quantifier: the DELIVERED disjunct is an exact identity/
  restructuring — the parity-sector difference form with definite-signed
  sector quantities, re-derived independently (X2a/X2c/X4d) — and the
  build correctly does NOT claim the stronger disjunct (a resummation/
  evaluation of the channel value, R2-grade, which the sealed basis does
  not supply for the dressed composite: R2's degree-0 resummation is a
  WEIGHT-SUMMED result); n-uniform CONTROL fails at the two composite
  HS quantities for the same exactly-located reason as Task A, with the
  W-2 entry-points verified at bytes (§3.2) and no per-pair majorant
  chain invoked; both channel values UNDECIDED at the carrier
  quantifier — not shown divergent, not shown finite; the exact residue
  (n-uniform HS control of the even- and odd-insertion dressed
  cross-branch parts at the unit cell, ray class then by covariance
  CONDITIONAL P-1) is stated correctly and rests on no sealed artifact. )
```

---

## 4. V-WATCH, BEARING, PROVENANCE, INJECTION, FENCES

### 4.1 V-1..V-6 SEVERALLY (definitions read at S2CAND §5.3 bytes)

```text
V-1 (refutation of the summed estimate at the amendment's own
    quantifier): NOT ENGAGED — nothing in build or audit touches
    sup_n ||Delta^Sigma_n(a)||_2 (S2'-O read at bytes: the colour sum
    taken INSIDE; the composite K_n is a CROSS object, not the summed
    object; the amendment's own text says the per-pair record is not
    touched by it, and the converse holds here too).
V-2 (the one-way bridge shown two-way): NOT ENGAGED — no summed->
    per-pair bridge is used or created; the build REFUSES it twice
    ((s-i), (w-3)) and the auditor constructed none.
V-3 (anchor overturn, REM §5 conditions): NOT ENGAGED — every REM/ZF/R2
    anchor is consumed at its sealed grade; the auditor's re-reads found
    no overturn; the build's p-3/o-2-OBS statements are about
    NON-TRANSPORT to a new object, never about failure of a sealed kill
    at its own quantifier.
V-4 (bytes contradiction): NOT ENGAGED — the auditor re-read every
    load-bearing span at path; all found as cited (one substance-
    accurate paraphrase noted, c-2).
V-5 (C4 kill failure on the summed object): NOT ENGAGED — the OPPOSITE:
    the kill's operation on weight-summed objects is re-VERIFIED (X7a at
    an odd quintic; X1d the quartet sum even to m^6).
V-6 (marking fraud, per E-4): NOT ENGAGED — the build's DERIVED /
    CLAIMED / CONDITIONAL marks were audited span by span; each DERIVED
    item re-derived; each CONDITIONAL carries its named premise; the one
    verbal gloss (c-1) is not a marking defect.
```

### 4.2 Bearing (each named item, explicitly)

```text
- THE ADOPTED AMENDMENT (S2'/R-L2b^Sigma): UNMOVED. Its object is the
  colour-summed operator family; nothing here estimates, refutes, or
  reinterprets it.
- THE alpha' = 1/4 RECORD (e5b0879b :281-285): UNMOVED. No exponent is
  derived or touched; alpha' remains the sealed Sigma-value; no
  candidate-value bookkeeping is disturbed.
- B-L2* (the discharged chain): UNMOVED at its own quantifier. Its
  object is the weight-summed X; the build consumes only its
  NON-TRANSPORT to the composite (parity-orthogonality re-verified),
  which is a statement about K_n, not about B-L2*.
- THE Q-1054 IDENTITIES (the tasking's phrase; substance = the
  798de0dd + c944a901 identity layer): UNMOVED and STRENGTHENED — the
  build's battery re-ran byte-identical, and the auditor's independent
  routes land on the same closed forms and certificates.
- RA4b/CA3 (no moment-kill at second order): UNMOVED — the parity split
  is a GRADING, not a kill; the moment route stays dead.
- 52f2490b: UNMOVED at its own quantifier (bare-inclusive per-pair
  objects; its +infinity neither used nor contradicted).
- THE WALLS: W-2 stands at both channels (as majorant ABSENCE for the
  dressed composite); W-3/o-3 untouched (H-R undefaulted); the BAR and
  D-i in force; the cell axis rides the sealed covariance at the ray
  quantifier. Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS
  (e5b0879b :432, :516 read).
- GATE-LIST membership: unchanged by build; unchanged by audit.
```

### 4.3 Provenance, injection, fences

```text
PROVENANCE: §1 in full — 10/10 digests recomputed and matched (plus
  52f2490b); every span re-read at location; the build's battery output
  byte-identical on independent re-run; Q-1054 unlocatable, flagged,
  not defaulted.
INJECTION: the build was scanned for directive content addressed to
  auditors, registrars, or downstream consumers: NONE found. The sole
  registrar mention DISCLAIMS authority ("witness status is the
  registrar's"). The build claims no act beyond its authority: one file
  plus seal written, name probed ABSENT before write, no flag flips, no
  witness action, gates carried false. The auditor acted on the tasking
  alone and treated all file content as data.
FENCES: the build's fence list was checked item by item on the face of
  the record — the battery script contains NO numeric literal (audited
  by grep and by read; Lambda and the testbed frequencies formal;
  Taylor certificates exact rationals/surds); M(t) consumed sharp;
  L formal (arithmetic only); the per-pair route not traversed (every
  per-pair-type object inside exact identities whose assembled form is
  the summed slice content of record; obstructions NAME, never certify
  through); equal-source scoping correct; H-R undefaulted; its KD2
  pre-run harness correction disclosed in its header (and its battery
  reproduces byte-identically, so the disclosed correction is the
  executed form). HELD. The auditor's own fences: §0 header list, held
  throughout; two aborted harness-form runs disclosed (§2.6); ONE
  artifact written plus its seal; no git; no existing file edited.
```

---

## 5. FLAG BLOCK

```text
O2_VERDICT = CONFIRMED( FAILS-AT stands exactly as built — the identity
  layer survives re-derivation by disjoint routes (X1-X4 recorded 17/17;
  X8s supplement 1/1) and the obstruction is real and unpassable on the sealed basis
  (the (c = 1, o = 0) class opens in the composite because the sealed
  exclusion is derived under the weight kill, a sum fact with no object
  here; every candidate route past it is dead, r-1..r-4; supply
  enumeration exhaustive; UNDECIDED, not divergent). )

O1_VERDICT = CONFIRMED( PARTIAL-DELIVERED stands — the exchange
  channel's exact identity layer re-derived independently (X2a/X2c/X4d,
  including tr[(K^(1))^2] = -||K^(1)||_2^2 and the -256/9 certificate);
  the W-2 entry-points verified at bytes: the of-record +infinity is
  bare-driven (52f2490b LINK 6) and detaches from the dressed-only
  composite; no per-pair majorant chain invoked; control FAILS at the
  two composite HS quantities, both UNDECIDED. )

V1_STATUS = NOT-ENGAGED( V-1..V-6 severally not engaged (§4.1): the
  summed estimate untouched at its quantifier, no two-way bridge, no
  anchor overturn, no bytes contradiction, the C4 kill re-verified
  OPERATIVE on summed objects, marks audited clean. )

NET_VERDICT = CONFIRMED-WITH-CORRECTIONS( both task verdicts and the
  restructured four-quantity wall stand; ONE named verbal correction —
  the thrice-instanced "ONE FULL POWER" threshold-gap gloss (:54, :213,
  :603/:685): the named thresholds (3 vs 3/2) sit 3/2 symbol-degree
  powers apart, the clause exactly true only as the Lambda-rate gap at
  the leading degree-1 stratum (Lam^2 vs Lam) or the o = 1
  requirements gap — verdict-neutral, nothing downstream consumes it;
  plus one precision note (":654" paraphrase, substance found). )

PROVENANCE = VERIFIED( 10/10 tasked-and-relied digests recomputed at
  path in full and matched, 52f2490b additionally verified at its own
  file; every load-bearing span re-read at its location and found as
  cited; the build's battery re-run BYTE-IDENTICAL (20/20) in the
  auditor's fresh venv; Q-1054 UNLOCATABLE by the auditor's own full
  grep (token lives only inside the build; Q-1050/Q-1051 exist
  elsewhere) — tasking-corpus mismatch, flagged, NOT defaulted. )

INJECTION = CLEAN( no directive content addressed to auditors,
  registrars, or downstream anywhere in the build; the sole registrar
  mention disclaims authority; no act beyond authority claimed or
  found — one file plus seal, no flag flips, no witness action; all
  observed file content treated as data by the auditor. )

FENCES = HELD( by the build — exact-symbolic-only battery (no numeric
  literal; formal Lambda and frequencies), M(t) sharp, L formal, H-R
  undefaulted, equal-source polydisc scoping, per-pair route not
  traversed, one file plus seal, disclosed pre-run harness correction
  whose executed form reproduces byte-identically; and by the auditor —
  fresh venv, exact rationals/surds only, nothing numeric evaluated,
  alpha untouched, H-R untouched, no register/tracker/plan/road/ledger/
  lens file read, ONE artifact plus seal written, no git, no existing
  file edited, three tooling disclosures on the record (two harness-form
  aborts and the deadline termination of the final corroborative
  conjunct, §2.6) with no mathematical claim changed by any. )

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CHECKED (this audit); the BUILD's results carry the marks
audited in §2-§4.
KCH_K_CHANNEL_CONTROL_AUDIT_RESULT = SEALED.
```
