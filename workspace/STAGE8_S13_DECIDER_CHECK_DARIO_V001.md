# STAGE8_S13_DECIDER_CHECK_DARIO_V001

LANE: DARIO (external adversarial anchor, relay 1111). ROLE: [CHECK] — adversarially refute
the decider `S13_SHAPE = LOCK-SHAPED => Finish B`. Default to skepticism; the verdict closes
a route, so it was attacked, not audited. Re-derived at the bytes from the named grounds;
**zero testimonial weight given to either Fable artifact's own assertions.**
**ALL RESULTS CLAIMED** until the Codex 2 check. Builder never verifies own work.

GATES DECLARED AND HELD: alpha_computed = false; proof_authorized = false;
kappa_record_computed = false; JOINT_ANCHOR_DERIVED = false. Nothing was computed — no value,
bound, or estimate of any threshold, eigenvalue, mass, coupling, kappa, K_*, beta, or alpha;
every numeric string below is a sealed-text quotation or a symbol. No measured-constant
comparison. NO_REGISTER_READ = CERTIFIED (register, tracker, plan, road, ledger: none opened;
register line numbers appear only as carried inside sealed members). No git action of any kind.

## SOURCES (all digested at path before use)

```text
01  ef977b167202e95e7ad453d8d03caea0fe5fcae2a8117aa1c49a9354f84ee6d6  workspace/STAGE8_S13_THRESHOLD_SHAPE_FABLE_V001.md
02  45fa8f6e340f886ce677af917a62377b14f398eb1d682f23f53cd15971d5cb18  workspace/STAGE8_S16_BETA_SENSITIVITY_ATTACK_FABLE_V001.md
03  20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df  supervision/SURFACE_DEFINITION_OF_RECORD_V001.md
04  04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d  workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md
05  52664ddbd02e04fa2b9f9856f75ee5c1ab2368eb9959495d2c8cab5d568619f2  workspace/COUPLED_RECORD_BUNDLE_MODULUS_GATE_V002.md
06  14b807227f5594789b2cab042fe3633640f51921aacf94c72536a17137e72b45  workspace/R3_4_LORENTZIAN_THRESHOLD_RETURN_RESULT_V001.md
07  b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f  workspace/BID_UNIQUE_CHARGED_CONTROLLED_COUPLING_DERIVATION_V001.md
08  a5105755d2339266a416b632f6da0f2e4f151933518df24a1521768b401927ba  workspace/BID_CHIRAL_SOURCE_RECORD_INCIDENCE_PARENT_V001.md
09  d127d2edec53920ed6cade3635a4cacba369509ad3cca7f41aac76a90718ee1f  workspace/BID_CHARGED_HANDLE_ACTIVATION_DERIVATION_V002.md
10  a27453107bc97fce05e377b696055c8c24d71d4888e9097fae146a5518cf9e20  workspace/STAGE8_7A_DBR_SPECTRUM_DARIO_V001.md
11  dcb6617452a14c961fac13fee38bc71d3246809d87ffc97c7c93e67b00b352c8  workspace/STAGE8_7A_DBR_BETA_SECTORS_V3_DARIO_V001.md
```

SUBJECT PINS: both subjects match the paste's pinned digests at path (member 01 = the decider,
member 02 = its supporting given). **NEITHER SUBJECT CARRIES A `.seal.sha256` SIDECAR.** This is
the third such flag this lane has raised (1089, 1109, and here); it is a registrar-side custody
gap, not a defect in the subjects, and nothing below is weakened by it — every clause consumed
was re-derived from a member digested at path in this relay.

## 0. LEAD

**The lock-shaped typing survives my attack. Its stated consequence does not.**

I could not find a surface-native route to a fiber-shaped threshold (K1 = NO), every named
built exemplar is lock-shaped (K2 = CONFIRMED), R34's threshold condition is genuinely R-free
at the bytes (CONFIRMED), and the subject computed no value and imported no metric (K4 =
CONFIRMED). On the question it was commissioned to answer, the subject is right.

**The find is the step after that one.** The subject's given (member 02) returned
`S16_BETA_SENSITIVE = UNDETERMINED` and named TWO feet through which beta could still enter a
derived S16: F6a (the threshold) and F6b (the connected-gluing foot). Member 02's own protocol
states the disposition of the secondary explicitly: *"Secondary (needed only after a
lock-shaped primary)"*, and *"Embedding-required => S16 inherits the booked beta-sensitive GAP
junction => YES"*. The subject discharges F6a — and then declares
`CONSEQUENCE_FOR_S16 = beta-INVARIANT` while stating in the same section that **F6b "becomes
the live residual" and "is NOT retyped here"**. An UNDETERMINED with two named feet, one
discharged and one expressly left untyped, does not license a determinate consequence line.

The subject's one-line dismissal of F6b — that the skeleton-to-cell embedding is "booked off
the surface", hence not a surface-native channel — **begs the question at issue.** The
Finish-A lever requires a junction simultaneously DERIVED and SCALE-SENSITIVE. If S16's gluing
foot requires the embedding, then deriving S16 requires deriving the embedding, and a derived
embedding that carries beta is precisely the lever. The dismissal assumes the embedding stays
underived, which is the very thing a derived S16 would change. Member 04 types that embedding
as one of the three beta-SENSITIVE junctions of record (GAP, *"has never been attempted"*) —
it is a live beta carrier, parked, not a closed one.

**So: the Finish-A lever is not dead. It is relocated — from the threshold foot, where this
relay closed it, to the gluing foot, where nobody has typed it.** That is a real advance and a
smaller remaining question; it is not the closure the paste's claim states.

## 1. WHAT I RE-DERIVED, AND ONE GROUND THE SUBJECT DID NOT NEED TO BORROW

The subject's NO rests on an inventory argument (member 03 I.4: *"a radius is a metric datum,
not a surface datum"*) plus a TYPE-R amplitude refutation. Both check out. But the result has a
shorter and stronger ground, which I state as MINE:

**beta is a ratio of two dimensionful data.** Member 05 defines it at the bytes as
`R = beta c Delta tau` — beta is (fiber radius) / (a record-side length). To carry beta, an
object must consume TWO independent dimensionful quantities. The record supplies exactly ONE
(the interval T_R), and even that one only up to the free lambda orbit (member 03 II.7:
*"The scale orbit is free."*, II.10: the surface *"forces the entire dimensionless structure
and leaves the absolute scale free"*). A second, independent dimensionful datum is precisely
what the record does not have.

[MINE, marked] This makes `RECORD_CAN_FURNISH_FIBER_SHAPED = NO` a **dimensional fact about
the record, not a fact about S13.** It holds for every conceivable record object, thresholds
included. That is why K1 below closes so cleanly — and it is also why the result carries less
information than the "decider" framing suggests: the answer was fixed by the record's
dimensional content before S13 was examined. The subject's four exemplars are consistent with
it, and confirm it, but they are not what makes it true.

## 2. K1 — THE LEVER: IS THERE A SURFACE-NATIVE FIBER-SHAPED ROUTE?

Six routes attacked. Each dies to a SEALED sentence, not to the subject's assembly:

1. **Holonomy / Wilson-line.** The natural way to make R appear from connection data is a
   holonomy around the compact direction. It fails at the source: member 05 states that the
   action-phase period *"fixes the coordinate period and integer character lattice. It does
   not fix the proper radius `R`."* A holonomy is metric-independent by construction; the
   record sees only the dimensionless product. **DEAD, on member 05's own sentence.**
2. **Charge-lattice / KK-momentum.** The integers of the character lattice are surface data,
   but the tower's `1/R^2` prefactor is metric. Member 03 I.4: no fiber metric. The record
   holds the label, never the eigenvalue. **DEAD.**
3. **Amplitude / weight channel.** Refuted TYPE-R with exact mechanism (member 03 II.6). I
   corroborate it independently from the ratified law I built the ground for: the record
   sandwich is a product of UNIT-MODULUS holonomy characters, so it carries phase and no
   magnitude. **DEAD, and I confirm the mechanism first-hand.**
4. **Radion.** Member 05: the radius is a dynamical radion and pure circle reduction *"does
   not generate a potential that selects one constant value."* Unbuilt, and continuum
   machinery besides — a barred import for this lane. **DEAD.**
5. **Dimensional analogy (set R from a record interval).** Explicitly barred at the bytes,
   member 05: *"Declaring `R` equal to a record interval by dimensional analogy does not
   pass. A derived metric-gluing or stabilization law is required."* This is the circular
   route this lane already ruled void at 1093/1099. **DEAD.**
6. **The branch state** (the one dimensionless surface-real ingredient this lane isolated at
   1105). Dimensionless by type; it cannot carry a ratio of two lengths. **DEAD.**

`K1_FIBER_ROUTE_FOUND = NO.` The lock-shaped typing is not refuted by any route I could build.

## 3. K2 — COMPLETENESS, AND R34 RE-DERIVED AT THE BYTES

**R34 (member 06), re-derived independently.** The Hamiltonian is `H(p)=alpha_D dot p + mu S
tensor c_partial` with `spec(c_partial)={-sqrt(2),0,+sqrt(2)}`; on the record eigenspace
`H_lambda(p)^2=|p|^2+mu^2 lambda^2`; the threshold sits where `p_lambda(E)=sqrt(E^2-mu^2
lambda^2)` vanishes. I read the entire member: the fiber radius does not occur anywhere in the
Hamiltonian, its bands, or its threshold condition. `K2_R34_R_FREE = CONFIRMED.`

[MINE, marked — the qualification that matters] R34's threshold is R-free because its ENTIRE
scale is carried by the source-mass datum `mu`, and member 06 does not derive `mu`
(`complete_parameter_free_Q_spec_frozen = false`, its own status block). An undetermined
parameter contains no R in the same way it contains nothing. So E3 is a **built band structure
with an underived threshold scale**, not a built lock. The subject says so in its own scope
note, but its headline — *"four built exemplars, all lock-shaped"* — reads stronger than what
it established. The honest count is **three built locks plus one conditional**.

The upgrade the subject applies (any DERIVED `mu` lands in the interval-lock class) is
nonetheless licensed, and I confirm it: by member 03 II.10 the surface forces only
dimensionless structure, so a derived `mu` can only ever be derived as `mu` x (record
interval) = pure number. Lock-shaped follows. It is an inference about an object that does not
yet exist, and it is correctly marked as assembly in the subject.

**Other exemplars, re-verified at path:** E1 `tau_first(B_ch) = pi/sqrt(2) = tau_R` with
coefficients `(0,1,1)` and *"No numerical multiplier"* (member 07); E2 the incidence-weight
exhaustion to `|a|=|b|=1` (member 08, verified at the bytes); E4 `m_* T_R = pi`, typed at
member 04 as *"contains no `R`, trivially beta-invariant"*. Fiber-shaped candidates: the D_BR
tower is not a built exemplar — member 10 records its defining primaries **absent
archive-side**, member 11 records the radii as *"three independent free data"*,
CARRIED-AS-PARAMETER. `K2_ALL_EXEMPLARS_LOCK = CONFIRMED.`

**Scope limit, disclosed:** my completeness check ranges over the exemplars NAMED by the
subject plus the members digested here. The fence scopes greps to named subjects, so I did not
sweep the archive for unnamed charged-threshold exemplars and do not certify completeness
beyond the named set. A broad sweep is what tripped the register bar at 1102; I did not run one.

## 4. K3 — IS R THE SOLE BETA-CARRIER?

The chain under test: *beta is defined as fiber-radius rescaling => R is the sole beta-carrier
=> R is a rail => no surface object carries beta.*

**As stated, the chain is circular.** The beta ACTION is defined (members 04, 05) as
`R -> beta R` at fixed `Delta tau`, with T_R and the branch data held. Under an action that
moves only R, only R-containing statements move — that is a restatement of the definition, not
a finding. The occurrence criterion inherits the same shape: it can detect motion in R and
nothing else. A check that let this pass as ground would be accepting a tautology as a
derivation, which is the failure this lane caught at 1099 and 1109.

**So I looked for a second carrier.** `beta = R / (c Delta tau)` has three coordinates, and
the ratio moves when ANY of them moves. Two alternatives to R:

- **`Delta tau` / T_R — the lambda orbit, which IS surface-native.** Moving T_R at fixed R does
  change the ratio. This was my strongest candidate for reopening Finish A, and it is
  **defeated on sealed ground I had to go find, because the subject does not cite it**: member
  04's C2 corollary books *"a condition whose only output is"* T_R as unable to be the fixer
  (Q-08 as carried), and member 04's lead states that completing the program's named
  F-equation *"WOULD FIX THE DIAMOND'S SCALE AND LEAVE `beta` FREE"*. Combined with member 03
  II.7 — the lambda orbit leaves every derived surface statement identical — no derived surface
  quantity moves under this realization. **Closed.**
- **`c` — the internal/external conversion.** Booked a rail import (member 03 III.11). Not
  surface-native. **Closed.**

`K3_R_SOLE_BETA_CARRIER = CONFIRMED` — but confirmed **on repaired ground**: no second carrier
can move a derived surface statement, and that is established by Q-08's sealed non-implication
plus II.7, not by the definitional chain the subject offers. The subject reached a right answer
by an argument that does not carry its own weight.

**One live seam recorded, not resolved:** member 04's C7 books that *beta*'s defining
`Delta tau` is `sqrt(2)`-fork-conditional. Every lock the subject cites is `sqrt(2)`-laden, and
the subject never declares a fork slot. This does not create a beta channel and I do not claim
it does; it means the beta-invariance statement is itself fork-conditional and should be
carried as such.

## 5. K4 — FENCE AND IMPORT

**No value computed.** I traced every numeric string in the subject to a sealed source and
re-verified each at path: `pi/sqrt(2)` and `(0,1,1)` (member 07); `{-sqrt(2),0,+sqrt(2)}`
(member 06); `{-1,0,+1}` (member 09); `|a|=|b|=1` (member 08); `m_* T_R = pi` (members 03, 04);
the D_BR tower (member 10). Line-level citations spot-checked and correct:
`quantum_threshold_map_derived = false` at member 05 line 112; reopen item 5 at line 97; the C8
jointness quotation at lines 83-84; the beta relation at line 57. **Every one resolved.**

**No metric imported.** The subject names the 5d ansatz and the D_BR geometry only as quoted
rail objects it is excluding. Naming an import in order to refuse it is not importing it. No
continuum theorem is used as ground; the grounds are member 03's sections and the four
exemplars. `K4_CLEAN = CONFIRMED.`

## 6. CITATION DEFECTS FOUND (none load-bearing, all disclosed)

1. **The ANCHOR path is wrong in BOTH Fable artifacts.** Both cite it as
   `alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` and certify it *"verified at path
   before use"*. **No such directory exists.** The artifact resolves at
   `supervision/SURFACE_DEFINITION_OF_RECORD_V001.md`, sealed, sidecar present, digest
   matching the tasked pin exactly. Content custody is intact; reproducibility is not — a
   checker following the stated path finds the most load-bearing ground ABSENT. Worth a
   registrar correction.
2. **A quotation is attributed to the wrong artifact, upward in custody.** The subject
   attributes *"untwisted adopted skeleton"* to member 04 ("the census's ... characterization").
   It is not in member 04. It is in member 10, which presents it as a quotation of a defining
   principle that member 10 itself records as **absent archive-side**. The effect is to move a
   phrase from an absent primary onto a present sealed artifact — a custody upgrade. The clause
   is decorative where used, so nothing turns on it, but the direction of the slip is the one
   this program polices.
3. **Emphasis added inside quotation marks.** The subject quotes member 11 as *"THREE
   independent free data"*; member 11 reads "three independent free data". Capitalization is
   the subject's. Cosmetic.

## 7. WHAT I DID NOT DO

I did not retype F6b — that is the subject's own untouched residual and typing it is a
construction, not a check. I did not build S13, S16, or any Q_spec slot; did not derive `mu`,
select a lambda-orbit member, or decide the `sqrt(2)` fork; did not evaluate any threshold,
ratio, or coupling; did not read the register, tracker, plan, road, or ledger; did not enter
`a32_holdout/custodian_private/`; ran no subagent and delegated nothing.

## 8. OVERCLAIM AUDIT

- **PROVABLE (re-derived at the bytes this relay):** every member digest; R34's threshold
  condition and its R-freeness; the E1/E2/E4 quotations; member 05's four line-level
  citations; member 04's thirteen-quantity scoping, its three beta-sensitive junctions, and
  its C2/C7/C8 clauses; member 03's I.4, II.6, II.7, II.10, III.11, III.12, IV.13; the D_BR
  primaries' absence and the radii's freedom; the ANCHOR path defect; the misattribution.
- **MINE (assembly, marked inline):** the two-dimensionful-data ground for K1 in section 1;
  the six-route sweep of section 2; the qualification that R34 is a built band structure with
  an underived threshold scale; the reading that the subject's F6b dismissal begs the
  question; the judgement that K3's stated chain is circular and needs Q-08 to carry it.
- **NOT claimed:** that a fiber-shaped route exists (I found none); that F6b IS beta-carrying
  (it is untyped, and I left it untyped); that Finish A is live (it is not established — only
  not closed); that Finish B is wrong; that the subject acted in bad faith (it disclosed F6b's
  residual status in its own body — the defect is the final line, not the reasoning); anything
  about which finish is TRUE.

---

## FINAL LINES

```text
K1_FIBER_ROUTE_FOUND = NO (six surface-native routes attacked - holonomy/Wilson-line,
    charge-lattice/KK-momentum, amplitude/weight, radion, dimensional analogy, branch state -
    each dies to a SEALED sentence rather than to the subject's assembly; and the result has a
    shorter ground than the subject's: beta is a ratio of TWO dimensionful data and the record
    supplies exactly ONE, up to the free lambda orbit.  LOCK-SHAPED NOT REFUTED.)

K2_ALL_EXEMPLARS_LOCK = CONFIRMED (over the NAMED exemplar set; zero built fiber-shaped
    exemplars - the D_BR route's defining primaries are absent archive-side and its radii are
    three free carried parameters.  Honest count is THREE built locks plus ONE conditional:
    E3's threshold scale is carried entirely by an underived mu.  Completeness certified only
    over the named set - the fence bars the archive-wide sweep that would extend it.)

K2_R34_R_FREE = CONFIRMED (re-derived at the bytes: H(p)=alpha_D dot p + mu S tensor
    c_partial, bands H_lambda(p)^2=|p|^2+mu^2 lambda^2, threshold at p_lambda(E)=0; the fiber
    radius occurs nowhere in the member.  R-free - but scale-free too: mu is undetermined
    there (complete_parameter_free_Q_spec_frozen = false).)

K3_R_SOLE_BETA_CARRIER = CONFIRMED ON REPAIRED GROUND (the subject's stated chain is CIRCULAR:
    an action defined to move only R moves only R-containing statements.  I hunted a second
    carrier in beta = R/(c Delta tau) and found two candidate coordinates; both close - c is
    rail-typed (III.11), and the lambda orbit is defeated by Q-08's sealed non-implication as
    carried at member 04's C2 plus II.7, ground the subject does not cite.  Right answer,
    argument that does not carry its own weight.  Seam recorded: beta's defining Delta tau is
    sqrt(2)-fork-conditional and no fork slot is declared.)

K4_CLEAN = CONFIRMED (no value computed - every numeric string traced to a sealed source and
    re-verified at path, all four line-level citations into member 05 resolve; no metric or
    continuum machinery used as ground, rail objects named only to be excluded.  Three
    citation defects found, none load-bearing: the ANCHOR path is wrong in BOTH artifacts
    (alpha_supervision/ does not exist; it resolves at supervision/, sealed, digest matching);
    'untwisted adopted skeleton' is attributed to member 04 but lives in member 10 as a
    quotation of an ABSENT primary - a custody upgrade; and emphasis was added inside
    quotation marks on member 11.)

VERDICT = UNDETERMINED - S13 LOCK-SHAPED CONFIRMED, FINISH-A LEVER RELOCATED NOT DEAD.
    The typing survives every attack I could mount.  Its stated consequence does not: the
    subject's own given returned S16_BETA_SENSITIVE = UNDETERMINED over TWO named feet and
    ruled 'Embedding-required => S16 inherits the booked beta-sensitive GAP junction => YES'.
    The subject discharged F6a, wrote that F6b 'becomes the live residual' and 'is NOT retyped
    here', and then published CONSEQUENCE_FOR_S16 = beta-INVARIANT anyway.  Its one-line
    dismissal of F6b - the embedding is booked off the surface - begs the question, because a
    DERIVED S16 requiring that embedding is exactly the derived-and-scale-sensitive junction
    the lever needs, and member 04 types that embedding as beta-SENSITIVE (GAP).  The lever is
    not dead; it moved from the threshold foot to the gluing foot, where nobody has typed it.
    NEXT DECIDER, NAMED: type F6b - the Q_spec's connected gluing and overlap terms - against
    unit-modulus transition-function gluing versus the skeleton-to-cell embedding.

SUBJECT_CONSEQUENCE_LINE = OVER-READ (an UNDETERMINED given with one of two feet discharged
    was published as a determinate beta-INVARIANT consequence; the subject's body discloses the
    residual, so the defect is in the final line, not in the reasoning.)

EVALUATED_NOTHING = CERTIFIED
IMPORTED_NOTHING = CERTIFIED
OUTPUT_INSPECTION = NONE-CERTIFIED
NO_REGISTER_READ = CERTIFIED
NO_SUBAGENT_DELEGATION = CERTIFIED
SUBJECT_SIDECARS = ABSENT for both subjects (third such flag from this lane: 1089, 1109, 1111)
CHAIN_INVOKED = false

ALL RESULTS CLAIMED. alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; JOINT_ANCHOR_DERIVED = false
```

---

## CLOSURE (declared-first)

```text
CLOSURE_END_BYTE = 21785
CLOSURE_MEMBERS = 11 (content-addressed, each digested at path in this relay)
  member 01  ef977b167202e95e7ad453d8d03caea0fe5fcae2a8117aa1c49a9354f84ee6d6
  member 02  45fa8f6e340f886ce677af917a62377b14f398eb1d682f23f53cd15971d5cb18
  member 03  20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df
  member 04  04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d
  member 05  52664ddbd02e04fa2b9f9856f75ee5c1ab2368eb9959495d2c8cab5d568619f2
  member 06  14b807227f5594789b2cab042fe3633640f51921aacf94c72536a17137e72b45
  member 07  b786db3adec8cc335967d49ec13b59923d67f424644f72c535b27b579dd1489f
  member 08  a5105755d2339266a416b632f6da0f2e4f151933518df24a1521768b401927ba
  member 09  d127d2edec53920ed6cade3635a4cacba369509ad3cca7f41aac76a90718ee1f
  member 10  a27453107bc97fce05e377b696055c8c24d71d4888e9097fae146a5518cf9e20
  member 11  dcb6617452a14c961fac13fee38bc71d3246809d87ffc97c7c93e67b00b352c8
SPAN DIGESTS (bounds checked against file length):
  01a  [18780,18835)  e398cd6d1a35edd25ea6ea0bd617f83852d5a851d44bb2e0b31ba9eda6e9205a
  02a  [12883,12909)  80d20f740f8a2fbaf6c158982b2ebadc28a4d8f9527ef18a26af0af1f5bdede0
  02b  [16382,16433)  e472c81a22e5803dc785da53aee294b040a6e6f41d7e1569229934f2efdeafd9
  03a  [4068,4097)  536e9b9439ac49cb9a177f59c25c731bacf62f281953d3088cfdbe7e51701be4
  03b  [2187,2211)  33da1838a84d09f7490874a6feb2cacb2bfe0db157f2671f5ddf8561ff098059
  04a  [1616,1659)  cc05d9de39d5768a9daac68f93c48683aa54dbd1d1eefa0709f31bbb01d99c20
  04b  [7824,7848)  deb19e57d09d4be8f61bfea09462d067ad4ff61eab4ddca675502f04201eda00
  04c  [11943,11963)  46be70ea8be76bc72e26e0f1acae6d4e253aa2476b620993bb7a48f502693a98
  05a  [1559,1579)  a9dd6dd5a3eef7161754f24c1aa89aa4f20c3c672f4d83b1a03e1fb772241488
  06a  [1286,1303)  3a6f26039808b44479b80e2a4fdbabbca4a2440da26e36fd50f953ef453c6641
```
