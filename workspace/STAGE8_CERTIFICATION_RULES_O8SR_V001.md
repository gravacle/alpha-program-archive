# STAGE 8 — THE ADMISSION AND CERTIFICATION RULES: IS THE BOUNDARY-CLOSURE BLOCKED BY MATHEMATICS, OR BY ITS OWN RULES? — O8SR V001

## BUILDER — CODENAME RULES-BUILD — COMMISSION O8SR — 2026-08-15

DETERMINATION ONLY. No authored physics, no value, no number as a value of any
program quantity, no measured-constant comparison. **This artifact PROPOSES
NOTHING.** Where it finds a barrier rule-imposed it DISPLAYS the rule and what a
lawful change would have to consist of; it does not propose, adopt, recommend,
or perform one. Any change to a certification rule is the PRINCIPAL'S act under
the governing revision discipline (§6.4).

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_CERTIFICATION_RULES_O8SR_V001.md` and its `.seal.sha256` sidecar —
**both probed ABSENT** at start of commission (`ls` exit 1 on both) before any
write.

---

## 0. VERDICT IN ONE LINE

```text
**MIXED — and the mix is not even.  At all three failure points the DISPLAYED
IMPOSSIBILITIES ARE REAL BUT NARROW: each is an impossibility FOR THE ADMITTED
CLASS or ON THE PERMITTED ROUTE, and NOT ONE is an impossibility for an object
of the required kind as such.  Exactly:
  FP-1 = MIXED — the rank x op ROUTE's failure is MATHEMATICAL and exact
    (4n^3 k / 2n^3 = 2k, n-free, never o(1); R4a/R4b).  The FAILURE POINT is
    RULE-IMPOSED-BY-ABSENCE: no mathematics excludes the sub-volume rate, and
    the W1-AUDIT positively DERIVED that a q = 2 counting bound TOGETHER WITH
    THE SEALED RANK CAP SUFFICES up to a log (re-derived here exactly, R4c-R4e).
    What blocks is that no member supplies it — plus the word "ONLY" in "the
    only SEALED conversion", which is an INVENTORY statement about the record's
    stock and is nowhere a nonexistence theorem.
  FP-2 = RULE-IMPOSED.  The |x-y|^-3 divergence itself is MATHEMATICAL (C6).
    But the barrier is the ADOPTED R5 locus refinement (E1 C6 :359-366 — its
    OWN word is "ADOPTED OF RECORD"), plus three further ADOPTED rules: the
    evidence-form rule (a declaration is not a PASS), the supplier-class rule
    ("A MEMBER supplies"), and a procedural bar (CARRY-1: "add no route,
    branch, address, or member selection").  No mathematics excludes a
    volume-diagonal-supported asset.
  FP-3 = RULE-IMPOSED at the decisive step.  Its four impossibilities are all
    genuine theorems — the operator-norm exclusion, the sea-sandwich
    obstruction, the Gronwall escalation, the beta kernel fact — but EVERY ONE
    is relative to the admitted class H(sqrt3) or to the permitted route, and
    W-3 says so at its own bytes: "(o4) is NOT A CONSEQUENCE of (o1)-(o3) AT
    ANY STRENGTH ... its landing requires an input from OUTSIDE THE FORM CLASS."
    That is a statement about the ADMISSION RULE.
THE HUNTED OBJECT IS FOUND, AND IT IS AT FP-3.  **A LOAD-BEARING BLOCKING RULE
WITH PROVENANCE (iii) INHERITED CONVENTION, NEVER RATIFIED ANYWHERE: the
EXCLUSIVITY of C-L2's certification type — "its only permitted certification
(C-L2 quadratic form)".**  The exclusion of the OPERATOR-NORM route is DERIVED
(||[h_0, 1_B]|| = +infinity, mechanism-exact).  The SELECTION of the quadratic
form as its SOLE permitted replacement is not: the clause's "so" is a
disjunctive syllogism whose exhaustiveness premise (OP | QF) is NOWHERE
DISPLAYED in either root (R2a/R2b); the clause's own FIRST sentence permits a
second type ("QUADRATIC-FORM / Besov-type") which its THIRD clause silently
drops (R2d); and THE SAME SPEC CERTIFIES THE SIBLING QUANTITY G_hs IN A
HILBERT-SCHMIDT TYPE AT S2 (R2c) — so the two-element menu is refuted at the
spec's own bytes.  This exclusivity is what makes W-3 FP-3's SOLE discharger
and what the completion map's UNIQUE minimal set {W-1, W-2, W-3} inherits.
THE LOCALIZATION-BLINDNESS CLAIM: its exact formulation stays REFUTED (FP-2's
asset degree is -1, not >= 0).  A form that SURVIVES is derived here under a
UNIFORM orientation — distance from the asset's degree to that point's own
ADMISSIBLE HALF-LINE, one definition applied identically, giving (3/2, 2, 2),
all strictly positive (R3c/R3d), which also answers the audit's
mixed-orientation objection.  But it is a SHORTFALL statement, not a BLINDNESS
statement: blindness proper (degree exactly 0) holds at FP-1 and FP-3 ONLY —
2 of 3, said exactly (R3e/R3f).  AND THE BLINDNESS IS A CONSEQUENCE OF THE
ADMISSION RULES, DERIVED: the exact object's OWN of-record localized majorant
is r-GRADED (it has a strict interior maximum at r = 1/sqrt2), while the
ADMITTED majorant is r-FLAT — the localization information is present in the
certified object's own sealed display and is DISCARDED AT THE ADMISSION STEP
(R1c-R1f, R5c).
NOT REFUTED, carried in full: every negative here is ONE-SIDED.  No divergence
of any closure quantity is exhibited.  FP-1/FP-2/FP-3 all STAND; every witness
STANDS; the completion map is untouched; nothing is retired, adopted, or
proposed.**
```

---

## 1. SEALS VERIFIED AT PATH BEFORE ANY RELIANCE; SWEEP CUTOFF DECLARED

Every file below was verified with `shasum -a 256 -c` **run from the artifact's
own directory**, against its own `.seal.sha256` sidecar, BEFORE any reliance.
**17/17 OK, zero mismatches, none unverifiable.**

```text
THE COMMISSIONED GROUND (12), all OK:
241262ff…  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                  [W3]
d05f22be…  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md            [W3-A]
a04c446a…  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md              [ORIG]
0db4de90…  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md        [ORIG-A]
806d5ec4…  STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md               [W1]
2dcf3167…  STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md         [W1-A]
eacd810c…  STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md             [W2]
d95f4285…  STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md       [W2-A]
e6838ffc…  STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001.md           [CERT]
180114a7…  STAGE8_CL2_GCM_CERTIFICATION_S9AD_AUDIT_V001.md     [CERT-A]
afc3a79c…  STAGE8_COMPLETION_MAP_T17SR_V001.md                 [MAP]
4504cd14…  STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md           [MAP-A]

RULE SOURCES LOCATED BY DECLARED SCOPED SWEEP (5), all OK — every one is a
RULE'S OWN SEALED SOURCE, opened because the commission requires each rule
quoted AT its sealed source, not through a quoting artifact:
46846730…  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md         [E1]
             — the C-L2 clause :1137-1144, S2b :702-708, S2 :695-701,
               R.3 :810-813, C6 + the ADOPTED R5 locus refinement :353-370.
80db260f…  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md           [WALL]
             — the rank cap :302 and the F1/F2/F3 verdict block :400-419.
61c6de58…  STAGE8_AUTHORITY_CHAIN_PRECEDENCE_BINDING_V001.md   [AUTH]
             — the governing revision discipline (§6.4).
0a10c030…  STAGE8_ASSEMBLY_QUANTIFIER_V001.md                  [ASMQ]
             — the one-way quantifier record governing FP-S; located BY DIGEST
               (the pin 0a10c030 carried in MAP/WSR resolved to this file by a
               full-directory digest scan, no filename guess).
27bc36ae…  STAGE8_FORM_TO_HS_BRIDGE_S9AD_AUDIT_V001.md         [FHB-A]
             — the missed-routes hunt (a-9), consumed as the record's own
               attack on alternative form->HS routes.
```

### 1.1 SWEEP CUTOFF — DECLARED

```text
SWEEP_CUTOFF = 2026-08-15, this session.
ROOTS: (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace  (primary; all
  17 files above resolved here at verified seals).
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003 — NOT CONSUMED: every rule
  source resolved in the primary root.  THIS BOUNDS EVERY NEGATIVE BELOW.
SCOPED SWEEPS RUN, patterns declared verbatim, primary root, filename listing +
  grep -l, matched files opened ONLY when they are a rule's own sealed source:
  (s-1) "only permitted certification"            -> 5 files, all already ground
  (s-2) "Schatten|weak.type|interpolat|Besov"     -> intermediate-type hunt
  (s-3) "exhaustiv.*(certification|norm|route)" and
        "only.*(two|2).*(route|type).*(certif|norm)"  -> the EXHAUSTIVENESS hunt
  (s-4) "revision discipline|amendment|re-?ratif|spec change|clause revision|
         principal act"                            -> the revision discipline
  (s-5) full-directory sha256 scan to resolve the digest pin 0a10c030
EXCLUDED AT PATTERN LEVEL, UNREAD: every register / tracker / road / plan /
  continuation / ledger / lens / THE_HANDOFF file.  "Q-..." tokens
  EXPECTED-UNLOCATABLE — carried as opaque labels exactly as the sealed
  artifacts carry them, never chased.  Artifacts sealed after this
  commission's fire time UNSWEPT.  No git.
THE ONE NEGATIVE THIS SWEEP CARRIES, and its exact bound: sweep (s-3) returned
  NO artifact deriving that the certification-type menu is exhaustive.  That
  negative is bounded by patterns (s-2)+(s-3) in root (R-a) only.  It is an
  ABSENCE OF DISPLAY, not a proof of absence, and is reported as such.
```

---

## 2. DELIVERABLE (1) — THE RULE INVENTORY AT BYTES

For each failure point: what makes an asset **ADMITTED**; which certification
**TYPES** are permitted at each consuming site; what is **EXCLUDED and by what
clause**. Every rule quoted AT ITS SEALED SOURCE.

### 2.1 FP-1 (F1, THE CONVERSION STEP) — three rules

```text
RULE F1-a  THE RANK CAP (an ADMISSION fact about the carrier).
  SOURCE, verbatim (WALL 80db260f :302):
    "conversion from operator grade requires the rank factor: rank <= 4n^3
     on ran C_n (of record)"

RULE F1-b  THE PERMITTED CONVERSION (a CERTIFICATION-TYPE rule).
  SOURCE, verbatim (WALL :408-413, the F1 verdict):
    "rank x op is the only sealed conversion from the delivered operator
     grade to trace/HS grade; it carries the full carrier volume 4n^3
     against an n-free target, and the r-3 pair PROVED the same-power
     arithmetic cannot close (WB6 = D10 re-pinned; the tasked display made:
     THE SAME CEILING BINDS). Escape requires a sub-volume trace/HS rate:
     verbatim MO-3, open of record."
  ADMITTED ASSET at FP-1: an OPERATOR-GRADE enclosure on the carrier
     compressions. Delivered of record: ||K_H,n||_op <= 2, ||H_A,n||_op <= 2,
     n-uniform (WALL, "the first n-uniform enclosure of ANY grade").
  PERMITTED TYPE AT THE CONSUMING SITE: TRACE/HS — "The four tasked
     quantities are traces and HS masses" (WALL :400-407).
  EXCLUDED, and by what clause: NOTHING is excluded by a clause here. F1-b is
     an INVENTORY statement ("the only SEALED conversion"), i.e. a report on
     the record's stock. It does not say, and nowhere does the record say,
     that no other conversion exists.

RULE F1-c  THE ESCAPE'S NAME (an obligation, not an exclusion).
  SOURCE (same block): "Escape requires a sub-volume trace/HS rate: verbatim
     MO-3, open of record."  CARRIER named at MAP §3.4 (W-1/CN-1): "member-
     named topologies with continuity proofs ... no member exists in the
     scored corpus to name them."
```

### 2.2 FP-2 (F2, THE LOCUS STEP) — four rules

```text
RULE F2-a  THE LOCUS RULING (the binding ADMISSION-SIDE rule).
  SOURCE, verbatim AT ITS OWN SEALED SOURCE (E1 46846730 :359-366) — note the
  artifact's OWN provenance word, which I did not supply:
    "*** LOCUS REFINEMENT, ADOPTED OF RECORD (binding R5, from the
     independent system): THE FATAL LOCAL INTEGRAL IS THE VOLUME DIAGONAL
     x = y, NOT THE SHARP BOUNDARY. SMOOTHING ONLY THE BOUNDARY WILL NOT
     REMOVE A |x-y|^-3 POSITIVE MAJORANT. *** C6 named the sharp localizer
     as the locus; the locus is refined to the diagonal and boundary
     smoothness is demoted to a secondary, testable factor."
  ADMITTED ASSETS at FP-2, as surveyed: "every closure decay asset is
     boundary-supported" (WALL F2) — codimension 1 against a codimension-0
     diagonal.
  EXCLUDED, and by what clause: boundary smoothing, by F2-a itself ("WILL NOT
     REMOVE"), which the WALL renders "which boundary-supported structure
     provably does not reach."

RULE F2-b  THE EVIDENCE-FORM RULE (a CERTIFICATION-TYPE rule at the
  consuming site).  SOURCE, verbatim (R1's pass criterion, fdf28…/fdf20bd4
  §5.1, as carried at W-2 §1.1 :155-161):
    "A declaration or commuting square in abstract carriers is not a surface
     `PASS`.  A `PASS` below requires the displayed map or equality on the
     named actual stage, cycle, arrow, or certified diamond.  Where an actual
     manifest is absent, totality is not inferred."
  CONSEQUENCE, at W-2's own bytes: "A rule without an evaluation supplies a
     symbol-side MAP; F2's blocked side is replaced only by a symbol-side
     VALUE."

RULE F2-c  THE SUPPLIER-CLASS RULE.  SOURCE (R1's criterion sentence, as
  decomposed at W-2 §1.1): "A MEMBER supplies..." — CN-2 renders it
  "member-supplied" and appends the spec's own hunt result
  "MEMBER_FOUND = none".

RULE F2-d  THE PROCEDURAL BAR (a rule that forbids the act that would
  discharge the point).  SOURCE, verbatim (CARRY-1, carried at W-2 §4.3 H-2):
    "keep R9 PENDING until one common physical cell exists; add no route,
     branch, address, or member selection."
  and W-2's own application: "Setting `cell_N(e) = gamma_RL` for a named
     record cell `e` would BE the identification — it forms the common cell,
     advances R9-JII past PENDING, and is new authorship. REFUSED here."
```

### 2.3 FP-3 (F3, THE ERROR-TERM STEP) — five rules

```text
RULE F3-a  THE CERTIFICATION-TYPE CLAUSE — *** THE LOAD-BEARING RULE ***.
  SOURCE, verbatim AND COMPLETE, at its own sealed source (E1 :1137-1144):
    "C-L2  COMMUTATOR ERROR — *** THE WHOLE CONTENT OF THIS ROUTE, AND (per
           §R.6) OF SCAD'S SURVIVING CONSTANTS. *** Control [h_0, M(t) ⊗ S]
           in a QUADRATIC-FORM / Besov-type norm. Norm-based control is
           unavailable (||[h_0, 1_B]|| = +infinity for first-order h_0
           against a sharp indicator), so the certification must be in a
           quadratic form and the artifact must state WHICH form and WHY the
           operator-norm route is excluded. Supplies S2b.
           Witness: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED"
  PERMITTED TYPES, read off the clause itself — and they DIFFER between its
     own sentences: SENTENCE 1 permits {quadratic-form, Besov-type};
     CLAUSE 3 permits {quadratic-form}.  The narrowing is internal and
     unexplained (CAS R2d).
  EXCLUDED, and by what clause: the OPERATOR-NORM route, by this clause,
     WITH ITS REASON DISPLAYED (||[h_0, 1_B]|| = +infinity).
  THE EXCLUSIVITY AS CONSUMED DOWNSTREAM (WALL F3, then MAP §3.5):
     "its only permitted certification (C-L2 quadratic form)".

RULE F3-b  THE ADMITTED ASSET CLASS.  SOURCE, verbatim (W3 §3 :298-303,
  from CERT §3.2, audited HOLDS):
    "H(c) := { quadratic forms t on Q = D(h_0) = H^1(R^3) (x) C^4 :
               |t[psi, psi]| <= 2c ||h_0 psi|| . ||psi||  for all psi in Q } ,
     c = sqrt(3) ; the C-L2 generator's form q_t lies in H(sqrt3)"
  WHAT MAKES AN ASSET ADMITTED: membership in H(sqrt3) — i.e. carrying a
     bound expressed in the two slots ||h_0 psi|| and ||psi||, AND NOTHING
     ELSE.  This is the rule §5 shows creates the blindness.

RULE F3-c  THE CONSUMING TYPE.  SOURCE, verbatim (E1 R.3 :810-813):
    "||A_s||_2 <= ||A(0)||_2 + ||Delta||_2 <= |C|_4^{beta} G_cm
      + |C|_4^{alpha} G_hs"
  — A(0) IS the C-L2 error layer and the consuming site is HILBERT-SCHMIDT-
  typed.

RULE F3-d  THE RESERVED-SYMBOL PROHIBITION.  SOURCE, verbatim (RL2B-R §1, as
  carried at W3 §6.1): "NOTHING WAS CONSTRUCTED. No exponent, no estimate, no
  conversion supplied.  PRODUCTION PROHIBITED."  beta is reserved to R-L2b and
  "derived with alpha" (E1 S2b).

RULE F3-e  THE UNRESOLVED TYPE CONFLICT (a rule-level defect, carried of
  record, never repaired).  SOURCE, verbatim (CERT §2.3):
    "S2b's parenthetical consumes 'the certified bound ... in the quadratic
     form C-L2 names' (form-typed), while R.3's derivation display consumes
     ||A(0)||_2 (HS-typed)."
  CERT ledgers it ch-5: "carried, not repaired ... the spec author/registrar
     owns it."  IT IS LOAD-BEARING: the entire "form -> HS distance" exists
     only under the HS reading.
```

### 2.4 FP-S (THE SUMMED STATE) — one rule, and it is not a live barrier

```text
RULE FS-a  THE ONE-WAY QUANTIFIER RECORD.  SOURCE (WSR flag, as carried at
  MAP §1): "the summed level decides SUM-THEN-QUANTIFY and provably not
  quantify-then-sum ... the V-2-barred bridge is not crossed in either
  direction; the one-way quantifier record (0a10c030) is respected."
  Pin 0a10c030 RESOLVED THIS SESSION by full-directory digest scan to
  STAGE8_ASSEMBLY_QUANTIFIER_V001.md (seal OK); its own bytes source the
  one-way bridge to E1's colour-sum closure R.3 (:32, :150, :404).
  FP-S IS DECIDED, not failing — no barrier test applies to it (§4.5).
```

---

## 3. DELIVERABLE (2) — THE PROVENANCE OF EACH RULE

Classes, as commissioned: **(i) DERIVED of record** (a theorem — then it is
mathematics, not convention); **(ii) ADOPTED/AUTHORED by a sealed act**;
**(iii) INHERITED CONVENTION never ratified anywhere**; **(iv) UNLOCATABLE at
bytes**. The hunt for (iii)/(iv) doing load-bearing blocking work was run
explicitly (sweeps s-1..s-4). **It succeeded, twice.** Both finds are at FP-3.

### 3.1 The table

```text
RULE   PROVENANCE                                          LOAD-BEARING?
F1-a   (i) DERIVED of record. "rank <= 4n^3 on ran C_n     yes (both ways)
       (of record)". Consumed as a theorem in OPPOSITE
       directions by two artifacts — WALL uses it to
       CONVERT, W1-AUDIT uses it to CAP — which is itself
       evidence it is a real bound and not a convention.
F1-b   *** SPLIT. ***
       ARITHMETIC ("cannot close"): (i) DERIVED, CAS-       yes
         pinned (WB6 = r-3 D10 re-pinned); re-derived
         here exactly (R4a/R4b).
       THE WORD "ONLY": (iii) INHERITED CONVENTION —        yes, downstream
         but HONESTLY WORDED at its source. WALL says
         "the only SEALED conversion", an INVENTORY
         report. It is MAP §3.5 that hardens it into
         "F1's only escape", and no artifact derives
         that no other conversion exists.
F1-c   (ii) ADOPTED (MO-3, an obligation of record).        yes
F2-a   (ii) ADOPTED BY A SEALED ACT — the rule's OWN        *** yes, binding ***
       word: "LOCUS REFINEMENT, ADOPTED OF RECORD
       (binding R5, from the independent system)".
       NOTE THE SPLIT INSIDE IT: the |x-y|^-3 DIVERGENCE
       is (i) DERIVED at C6 ("int d^3 r . r^2/r^6 =
       int dr/r^2, divergent"). The RULING — that the
       locus is the diagonal and that boundary smoothing
       will not remove it — is (ii), imported from an
       independent system, and is nowhere re-derived
       inside E1.
F2-b   (ii) ADOPTED (R1's pass criterion).                  yes
F2-c   (ii) ADOPTED (R1's criterion sentence).              yes
F2-d   (ii) ADOPTED (CARRY-1, a carried instruction).       yes — it forbids
                                                            the discharging act
F3-a   *** SPLIT, AND THIS IS THE COMMISSION'S FIND. ***
       THE OPERATOR-NORM EXCLUSION: (i) DERIVED —           yes
         ||[h_0, 1_B]|| = +infinity, mechanism-exact at
         CERT §3.4 (surface-distribution type; L^2
         witness family; CAS B8a-c). REAL MATHEMATICS.
       THE EXCLUSIVITY OF THE QUADRATIC FORM:              *** yes, and it is
         **(iii) INHERITED CONVENTION, NEVER RATIFIED      the whole route ***
         ANYWHERE.**  Grounds, three, each derived in §3.2.
F3-b   (ii) ADOPTED/AUTHORED by a sealed act — and the      *** yes ***
       authoring lane FLAGS IT AS ITS OWN unforced
       choice: CERT ch-2, "NAMING THE FORM ... Tag:
       YOURS (naming), PROVABLE (content)."  The clause
       delegated the naming ("the artifact must state
       WHICH form"); the artifact named H(sqrt3); the
       named class is now the admission rule that §5
       shows creates the blindness.
F3-c   (ii) ADOPTED (E1's own R.3 display fixes the         yes
       consuming type as HS).
F3-d   (ii) ADOPTED (RL2B-R's own "PRODUCTION              yes
       PROHIBITED").
F3-e   **(iv) UNLOCATABLE AT BYTES** — see §3.3. The       *** yes ***
       question "which type does S2b actually demand?"
       has NO ratified answer anywhere in the swept
       corpus; the conflict is displayed and explicitly
       left unowned.
FS-a   (i) DERIVED of record (sourced at ASMQ to E1's       n/a — FP-S decided
       colour-sum closure R.3).
```

### 3.2 FIND 1 — the (iii) rule: C-L2's EXCLUSIVITY. Three grounds, derived

The clause reasons: *"Norm-based control is unavailable (…), **so** the
certification must be in a quadratic form."* That "so" is a disjunctive
syllogism. It is valid only with the premise **(OP | QF)** — that the
certification-type menu has exactly two members. That premise is the whole load.

```text
GROUND 1 — THE INFERENCE IS INVALID WITHOUT THE PREMISE [DERIVED, CAS R2a/R2b].
  A model with OP false, QF false, TH true satisfies the displayed premise
  (~OP) while FALSIFYING the conclusion (QF). So "~OP therefore QF" does not
  follow.  WITH (OP | QF) supplied the inference IS valid — which locates the
  entire weight of the clause's "so" on a premise that is NOWHERE DISPLAYED.
  Sweep (s-3) returned no artifact deriving it, in the primary root.

GROUND 2 — THE CLAUSE NARROWS ITSELF, MID-CLAUSE, WITHOUT GROUND [CAS R2d].
  Its FIRST sentence permits TWO types: "a QUADRATIC-FORM / Besov-type norm".
  Its THIRD clause permits ONE: "the certification must be in a quadratic
  form."  The record treats these as DISTINCT, not synonymous — CERT §3.1
  carries the Besov branch as a separate "variant, displayed", and closes on
  Q = D(h_0) with "the Besov display is named, not needed."  So the menu the
  clause itself opens with is already larger than the menu it enforces.

GROUND 3 — THE TWO-ELEMENT MENU IS REFUTED AT THE SPEC'S OWN BYTES [CAS R2c].
  The SAME SPEC certifies the SIBLING quantity in a THIRD type.  E1 S2
  (:695-701), verbatim:
    "S2. G_hs(C, eps) := |C|_4^{-alpha} · || C(V(a) - V(0))C ||_2
       [SUBTRACTED HILBERT-SCHMIDT DENSITY. MUST be a TWO-TIME (cell-S-matrix)
        object; the equal-time version is FALSE by C6.]"
  G_hs and G_cm are the two summands of the SAME consuming display (R.3).
  One is certified in a HILBERT-SCHMIDT type; the other is restricted to a
  quadratic form.  The architecture therefore demonstrably admits at least
  four certification types (operator-norm — excluded but named; quadratic
  form; Besov-type; Hilbert-Schmidt).  THE RESTRICTION IS PER-CLAUSE AND
  AUTHORED, NOT AN ARCHITECTURE-WIDE DICHOTOMY.
```

```text
WHY THIS IS LOAD-BEARING, AND EXACTLY HOW FAR IT REACHES:
  (a) It is what makes the W-3 run's negative total. W-3 proves "(o4) is NOT A
      CONSEQUENCE of (o1)-(o3) AT ANY STRENGTH ... its landing requires an
      input from OUTSIDE THE FORM CLASS."  That is a proof ABOUT THE CLASS.
      The class is fixed by F3-a + F3-b.
  (b) It is what makes W-3 FP-3's SOLE discharger. MAP §3.5, verbatim: "F3's
      certification route is 'only permitted' C-L2 with the operator-norm
      alternative 'EXCLUDED of record.'"  MAP's UNIQUENESS claim for the
      minimal completion set {W-1, W-2, W-3} therefore INHERITS the
      un-ratified exclusivity at this leg.
  (c) DISPLAYED, NOT OVERREAD: this does NOT show another type would work.
      No alternative certification is constructed, named as viable, or
      recommended here — that would be authorship this lane may not perform
      (§6). It shows only that the RESTRICTION to one type is a rule of
      provenance (iii), not a theorem.
  (d) AND THE RECORD ITSELF HAS ALREADY ATTACKED THE NEIGHBOURING QUESTION,
      HONESTLY: FHB-A's missed-routes hunt (a-9) tested eps-optimization,
      INTERPOLATION ("interpolation to HS impossible (the op endpoint is
      +infinity of record)"), det_2/Carleman, and rank x compressed-op, and
      found "NO missed route that bridges."  THAT HUNT IS OVER ROUTES FROM
      THE ADMITTED FORM ASSET TO HS — it presupposes the asset and therefore
      presupposes the admission rule. It is not, and does not claim to be, a
      derivation that the certification-TYPE menu is exhaustive.
```

### 3.3 FIND 2 — the (iv) rule: WHICH TYPE S2b DEMANDS IS UNLOCATABLE

```text
The conflict, at bytes (CERT §2.3, verbatim): "S2b's parenthetical consumes
'the certified bound ... in the quadratic form C-L2 names' (form-typed), while
R.3's derivation display consumes ||A(0)||_2 (HS-typed)."
CERT carried BOTH readings and repaired neither, ledgering the choice ch-5:
"no clause repair attempted — the spec author/registrar owns it."
W-3 then ATTEMPTED BOTH (its §3, READING A and READING B) and returned
UNDECIDABLE-TODAY on both.
THE PROVENANCE FINDING: there is no sealed act anywhere in the swept corpus
that RULES which type S2b demands. The governing reading is UNLOCATABLE at
bytes — class (iv) — and it is load-bearing, because the entire "form -> HS
distance" that constitutes FP-3's blockage EXISTS ONLY UNDER READING B.
Under READING A the demanded object is form-typed and (o1)-(o3) already
deliver a form-typed bound; what then fails is not the certification but the
CONSUMPTION at R.3 — a different failure, at a different site, with different
dischargers. NEITHER READING IS ADOPTED HERE; the ambiguity is displayed
exactly as CERT displays it, and its OWNERSHIP is the registrar's/spec
author's, exactly as CERT says.
```

### 3.4 The honest negatives on this deliverable

```text
(n-1) NO RULE WAS FOUND IN CLASS (iii) AT FP-1 OR FP-2 that is CONCEALED.
      F1-b's "only" is class (iii) but is honestly worded at its source
      ("only SEALED"); the hardening happens downstream, in MAP's rendering,
      not by misdescription at the source. I report this in the record's
      favour.
(n-2) EVERY FP-2 RULE IS CLASS (ii) — adopted, and adopted openly, each with
      its own displayed sealed act. FP-2 has no hidden convention. What it
      has is four adopted rules and one derived divergence (§4.3).
(n-3) THE (iii) AND (iv) FINDS ARE BOTH AT FP-3, AND BOTH ARE ABOUT TYPE —
      which type is permitted (3.2) and which type is demanded (3.3). That
      the two finds land on the same axis at the same failure point is
      displayed as a fact, not offered as a theory of the record.
(n-4) BOUNDED: all four classifications rest on sweeps (s-1)..(s-4) in the
      primary root only. A ratifying act living only in root (R-b), or in a
      register/tracker/road/plan/continuation file (excluded unread), would
      not have been seen. Said so that the negative is not overread.
```

---

## 4. DELIVERABLE (3) — THE BARRIER TEST, PER FAILURE POINT

### 4.1 The test, stated before it is applied

```text
A barrier is **MATHEMATICAL** iff the record displays an impossibility whose
  statement QUANTIFIES OVER ALL OBJECTS OF THE REQUIRED KIND — not merely
  over those the rules admit, nor over one route — and whose proof does not
  consume an admission or certification rule as a premise.
A barrier is **RULE-IMPOSED** iff no such impossibility is displayed, and an
  object of the required kind is not excluded by any displayed mathematics,
  but the rules do not ADMIT it or do not PERMIT the certification type that
  would carry it.
THE DIAGNOSTIC QUESTION, applied to every displayed impossibility below:
  **WHAT DOES ITS QUANTIFIER RANGE OVER?**  An impossibility whose hypothesis
  is a property the ADMISSION RULE GUARANTEES is an impossibility about the
  rule, not about the object.
```

### 4.2 FP-1 — **MIXED**, and the two halves are cleanly separable

```text
THE DISPLAYED IMPOSSIBILITY (F1-b): re-derived here exactly.
  rank x op delivers 4n^3 . k against an n-FREE target; against any same-power
  carrier object (2n^3) the ratio is 2k — n-free, constant, never o(1)
  [CAS R4a/R4b].  THIS IS REAL AND EXACT.
ITS QUANTIFIER: **the rank x op ROUTE.**  It says that ONE conversion cannot
  close.  It says nothing about the existence of a sub-volume trace/HS rate.
IS AN OBJECT OF THE REQUIRED KIND EXCLUDED?  **NO — and the record positively
  derives the opposite.**  W1-AUDIT KILL 1, re-derived here exactly:
    with the SEALED rank cap N_n(s) <= 4n^3 reinstated and a q = 2 counting
    bound N_n(s) <= c n^{3-delta} s^{-2}, splitting the layer-cake integral
    at s_0 = n^{-delta/2} gives
       int_0^{s_0} 2s(4n^3) ds       = 4 n^{3-delta}                [CAS R4c]
       int_{s_0}^{2} 2s(c n^{3-delta} s^{-2}) ds
                                     = 2c n^{3-delta}(ln 2 + (delta/2) ln n)
                                                                    [CAS R4d]
       total / n^{3-(delta-eps)} -> 0  for every 0 < eps < delta    [CAS R4e]
    — SUB-VOLUME.  So a q = 2 counting bound with a sub-volume prefactor
    SUFFICES.  The threshold value q = 2 becomes sufficient up to a log
    precisely because a SEALED RANK CAP was reinstated.
AND A CLAIMED RULE AT THIS POINT WAS ITSELF REFUTED: W1-AUDIT KILL 2 struck
  the claimed admissibility gate "every admissible topology for W-1 must be
  generated by SYMMETRY-BREAKING seminorms" as a NON-SEQUITUR (hypothesis (a)
  inert in its own proof).  A constraint that had been carried as binding on
  the carrier turned out to constrain nothing.
CLASSIFICATION [DERIVED]:
  ON THE ROUTE           = MATHEMATICAL (impossibility displayed, exact).
  AT THE FAILURE POINT   = RULE-IMPOSED-BY-ABSENCE. No mathematics excludes
    the required object; a SUFFICIENT CONDITION for it is exhibited of record.
    What blocks is (a) that no member supplies it — an ABSENCE — and (b) the
    exclusivity word in F1-b (class (iii)) as hardened downstream.
  NET: **MIXED.**
```

### 4.3 FP-2 — **RULE-IMPOSED**, with a mathematical component that does not reach the barrier

```text
THE DISPLAYED IMPOSSIBILITY (E1 C6): "Equal-time localization of the 3-D
  massless Dirac sea fails Shale-Stinespring: ||[C, 1_B]||_2 = +infinity; a
  Lipschitz cutoff still gives int d^3 r . r^2/r^6 = int dr/r^2, divergent."
  REAL MATHEMATICS.
ITS QUANTIFIER: **equal-time localizers.**  C6's own next sentence draws the
  boundary itself: "Only TWO-TIME / scattering-type objects, where the cell
  time integration supplies the missing decay, CAN WORK."  So C6 does not
  exclude an object of the required kind — it NAMES THE KIND THAT CAN WORK.
WHAT ACTUALLY BLOCKS FP-2, rule by rule:
  (b-1) F2-a, the LOCUS RULING — provenance (ii), its own word "ADOPTED OF
        RECORD ... from the independent system".  It rules the locus to be
        the volume diagonal and demotes boundary smoothing.  It is BINDING,
        and it is an adoption; E1 does not re-derive it.
  (b-2) THE ASSET SURVEY — "every closure decay asset is boundary-supported"
        (WALL F2).  A statement about the STOCK IN HAND, not about what can
        exist.  A codimension-0 asset is nowhere shown impossible.
  (b-3) F2-b, the EVIDENCE-FORM RULE — a declaration is not a PASS; only a
        DISPLAYED EVALUATION on a named actual cell counts.  A certification-
        type rule, adopted.
  (b-4) F2-c, the SUPPLIER-CLASS RULE — the object must be MEMBER-supplied;
        MEMBER_FOUND = none.  A rule about WHO may supply, not about whether
        the object exists.
  (b-5) F2-d, the PROCEDURAL BAR — and this is the sharpest instance in the
        whole inventory of a barrier that is a rule.  W-2 reached the point
        where the identification could be made and DECLINED IT BY RULE:
        "Setting `cell_N(e) = gamma_RL` ... would BE the identification — it
        forms the common cell, advances R9-JII past PENDING, and is new
        authorship.  REFUSED here."  The blocking step is an act the rules
        forbid this lane to perform, not an act mathematics forbids.
CLASSIFICATION [DERIVED]: **RULE-IMPOSED.**  The only mathematical
  impossibility on the point excludes equal-time localizers and expressly
  admits two-time objects; every remaining blocker is an adopted rule
  (b-1, b-3, b-4, b-5) or a survey of stock (b-2).
```

### 4.4 FP-3 — **RULE-IMPOSED**, and this is where the four theorems are

FP-3 carries the most mathematics of the three, and it is the point whose
barrier is most clearly rule-imposed — because each theorem's hypothesis is a
property the admission rule guarantees.

```text
IMPOSSIBILITY 1 — THE OPERATOR-NORM EXCLUSION.  ||[h_0, 1_B]|| = +infinity,
  mechanism-exact (CERT §3.4: surface-distribution type; the L^2 witness
  family; the form does not extend to L^2).  QUANTIFIER: **the operator-norm
  type.**  It excludes ONE type.  DERIVED, and it is the one place at FP-3
  where mathematics genuinely closes a door.
IMPOSSIBILITY 2 — THE SEA-SANDWICH OBSTRUCTION (W-3 §4).  C h_0 C is a
  NONZERO TRANSLATION-INVARIANT multiplier, hence not compact, hence not HS.
  QUANTIFIER: **translation-invariant inputs.**  W-3's own escape clause says
  so: "the only way past is an INPUT that is NOT translation-invariant."
  AND TRANSLATION-INVARIANCE IS EXACTLY WHAT F3-b GUARANTEES: H(c)'s defining
  functional reads psi ONLY through ||h_0 psi|| and ||psi||, both invariant,
  so H(c) assigns the SAME bound to a state and to all its translates
  [CAS R1a/R1b].  THE THEOREM'S HYPOTHESIS IS SUPPLIED BY THE ADMISSION RULE.
IMPOSSIBILITY 3 — THE GRONWALL ESCALATION (W-3 §7).  Its own headline names
  its quantifier: "THE CERTIFIED CLASS IS NOT CLOSED UNDER ITS OWN
  PROPAGATION GRONWALL."  A statement ABOUT THE CLASS, verbatim.
IMPOSSIBILITY 4 — THE beta KERNEL FACT (W-3 §6, re-derived and EXTENDED here).
  A cell-CONSTANT certified bound determines no exponent: the two-cell system
  reduces exactly to beta.log(V1/V2) = 0, unique solution beta = 0 [CAS
  R5a0/R5a/R5a1]; and the SAME argument on two admitted RADII gives the same
  kernel [CAS R5b, an extension of record's 4-volume statement to the radius].
  QUANTIFIER: **objects whose cell-dependence is constant** — and the
  constancy is CERT u-b, which is the admission rule's own doing (§5.3).
AND THE RECORD SAYS THE CONCLUSION IN ITS OWN WORDS (W-3 §8, verbatim):
  "(o4) — 'Supplies S2b' — is now shown to be NOT A CONSEQUENCE of (o1)-(o3)
   AT ANY STRENGTH.  Therefore W-3 CANNOT LAND ON C-L2's OWN PERMITTED ROUTE
   ALONE: its landing requires an input from OUTSIDE THE FORM CLASS."
  "OUTSIDE THE FORM CLASS" IS THE ADMISSION RULE'S NAME FOR ITS OWN BOUNDARY.
ONE FURTHER BLOCKER AT FP-3 IS A REFUSAL, NOT A THEOREM (W-3 g-3): the only
  exponent compatible with the asset, beta = 0, is "REFUSED as an authored
  assignment of an R-L2b-reserved symbol" — refused under F3-d, a rule.
  W-3 displays it as "the forced reading ... left unadopted."  Correctly: it
  is F3-d, not mathematics, that stops it.
CLASSIFICATION [DERIVED]: **RULE-IMPOSED.**  Exactly one of the four
  impossibilities (no. 1) excludes a type outright; the other three each
  quantify over the admitted class or over objects with a property the
  admission rule supplies.  No impossibility is displayed for an object of
  the required kind as such.
```

### 4.5 FP-S — no barrier test applies

```text
FP-S is DECIDED, not failing (MAP §1: tr H_A^Sigma = 0 exact;
||H_A^Sigma||_2^2 = 0 exact; ||K^Sigma||_2^2 enclosed carrier-free;
tr K^Sigma COMPLETE-LOG-GENUINE, rate REFUTED).  A decided state has no
barrier to classify.  Its governing rule FS-a is (i) DERIVED and is a
QUANTIFIER rule, not an admission or certification rule: it bars transport
between the summed and per-composite levels IN BOTH DIRECTIONS.  Nothing in
§4.2-§4.4 crosses it, and nothing here moves it.
```

### 4.6 The summed answer to the commissioned question

```text
IS THE BOUNDARY-CLOSURE BLOCKED BY MATHEMATICS, OR BY ITS OWN ADMISSION AND
CERTIFICATION RULES?  **BY BOTH, IN A DEFINITE PROPORTION — AND THE
MATHEMATICS BLOCKS LESS THAN THE RECORD'S SUMMARY LANGUAGE SUGGESTS.**
Every displayed impossibility in the inventory is REAL.  Not one of them
ranges over objects of the required kind.  Each excludes a ROUTE (F1-b,
impossibility 1), a REGIME (C6's equal-time localizers), or a CLASS
(impossibilities 2-4).  The step from "this route/class cannot" to "the
closure cannot" is carried at every point by a rule — an inventory word
("only sealed"), an adoption ("ADOPTED OF RECORD"), an evidence form, a
supplier class, a procedural bar, or an un-ratified type restriction.
```

---

## 5. DELIVERABLE (4) — THE LOCALIZATION-BLINDNESS CLAIM

### 5.1 What was claimed, and what its own audit did to it

```text
CLAIMED (ORIG §5.3, §6, FLAG `SHARED_ORIGIN_NAMED`), verbatim:
  "EVERY ASSET THE CLOSURE'S CERTIFICATION RULES ADMIT HAS DEGREE >= 0 IN THE
   LOCALIZATION VARIABLE ITS OWN OBSTRUCTION IS GRADED IN ... WHILE EVERY
   OBSTRUCTION'S REQUIREMENT IS STRICTLY LOCALIZED."
  and the consequence reading: "THE CLOSURE CANNOT CLOSE ON ANY CONTENT CLASS
  WHOSE ADMITTED ASSETS ARE ALL LOCALIZATION-BLIND — AND BY ITS CURRENT
  CERTIFICATION RULES, EVERY ASSET IT ADMITS IS LOCALIZATION-BLIND."
REFUTED BY ITS OWN AUDIT (ORIG-A F-2/F-3), and the refutation STANDS — I
  re-derived it rather than accepting it:
  - FP-2's displayed asset degree is **-1**, which is NOT >= 0. The universal
    is FALSE on one of its own three rows [CAS R3a].
  - The CAS check carrying the headline (O6-8d) OMITS the FP-2 asset term;
    restored, it FAILS [CAS R3b].
  - A degree -1 symbol is not invariant, not uniform, not blind — it decays,
    merely too slowly.
  - AND a further audit finding must be met, not only F-2: ORIG-A A3 showed
    the reported triple (3/2, 2, 2) MIXES TWO ORIENTATIONS (FP-1 uses
    need - asset; FP-2/FP-3 use asset - need). Any surviving restatement must
    answer this too, or it inherits the same defect.
```

### 5.2 The form that survives — derived, and it meets BOTH objections

```text
THE ORIENTATION PROBLEM IS SOLVED BY ONE DEFINITION, APPLIED IDENTICALLY.
Each failure point displays its own ADMISSIBLE HALF-LINE — the set of degrees
that would discharge it — and each half-line's DIRECTION is a datum of the
point, read off its own displayed inequality, not a choice made here:
    FP-1:  must BEAT n^{3/2}      => admissible = [3/2, +infinity)
    FP-2:  strict < -3  (trace)   => admissible = (-infinity, -3)
    FP-3:  <= -2 per stratum      => admissible = (-infinity, -2]
Define the SHORTFALL as the DISTANCE from the admitted asset's displayed
degree to that point's own admissible half-line (zero if inside). ONE
definition, three points, no orientation is chosen per row:
    FP-1:  dist(0,  [3/2, oo))    = 3/2
    FP-2:  dist(-1, (-oo, -3))    = 2
    FP-3:  dist(0,  (-oo, -2])    = 2
  [CAS R3c — all three exact; CAS R3d — all three STRICTLY POSITIVE.]

**THE SURVIVING STATEMENT:
  AT EACH OF THE THREE FAILURE POINTS, THE ADMITTED ASSET'S DISPLAYED DEGREE
  IN THAT POINT'S OWN GRADING VARIABLE LIES OUTSIDE THAT POINT'S OWN
  ADMISSIBLE HALF-LINE, AT POSITIVE DISTANCE — (3/2, 2, 2) RESPECTIVELY.**

It carries no universal ">= 0", makes no claim that any asset is invariant,
and uses one orientation-free definition. F-2 and A3 are both met.
```

### 5.3 But it is a SHORTFALL statement, not a BLINDNESS statement — said exactly

```text
THE HONEST LOSS: the surviving form is strictly weaker than what was claimed,
and it is weaker in KIND, not only in degree.  "Blind" and "short" are
different properties, and only the second holds at all three points.
  BLINDNESS PROPER (asset degree EXACTLY 0 in the point's grading variable)
  holds at **FP-1 and FP-3 ONLY — two of three** [CAS R3e].
  At FP-2 the admitted asset IS graded (-1); it is UNDER-GRADED by 2.
So the true tripartite reading is a DISJUNCTION — "BLIND at FP-1/FP-3,
UNDER-GRADED at FP-2" [CAS R3f] — not the single property the refuted
sentence named.  ORIG's defence ("each one's blocking property is the
SPECIFIC WAY its asset is localization-blind") loses its middle instance and
cannot be restored: no restatement makes a degree -1 symbol blind.
**DOES A FORM OF THE FULL CLAIM SURVIVE?  NO.  The universal quantifier over
all three points survives ONLY as a shortfall statement.  The blindness claim
survives only on the sub-family {FP-1, FP-3}, and must be stated with that
restriction on its face.**  The broader consequence reading of ORIG §6 —
"EVERY ASSET IT ADMITS IS LOCALIZATION-BLIND" — does NOT survive in any form,
and is not repaired here.
```

### 5.4 Is the blindness a consequence of the ADMISSION rules? **YES — derived**

On the sub-family where blindness genuinely holds, the commission's question
is answerable at bytes, and the answer is yes. The decisive display is that
the record's OWN exact object is NOT blind — only its admitted majorant is.

```text
STEP 1 — THE ADMISSION RULE READS ONLY TWO SLOTS.  F3-b's defining functional
  is |t[psi,psi]| <= 2c ||h_0 psi|| . ||psi||.  Its majorant is a function of
  (||h_0 psi||, ||psi||) ALONE — it reads no other feature of the state
  [CAS R1a].  Both slots are translation-invariant (h_0 is a Fourier
  multiplier; U_a is unitary), so H(c) assigns the SAME bound to a state and
  to every translate of it [CAS R1b].  Translation-invariance is therefore
  not a fact about the commutator; it is BUILT INTO THE ADMISSION CRITERION.
STEP 2 — THE ADMITTED BOUND IS FLAT IN THE CELL RADIUS.  CERT u-b of record:
  "the estimate uses only ||M|| <= 1 and holds for the indicator of ANY
  measurable region — every admitted cell of the D3 refinement, every radius,
  no cellulation datum consumed."  So the certified bound 2 sqrt(3)
  ||h_0 psi|| ||psi|| carries no r at all: d/dr = 0 exactly [CAS R1c].
STEP 3 — THE EXACT OBJECT IS **NOT** FLAT IN r.  The clause's own certified
  object has an of-record distributional display (CERT §3.4(ii)):
     [h_0, 1_B (x) S] = i(alpha.n-hat (x) S) delta_{dB_r} ,
     q(psi) = i (surface integral over dB_r) psi^dag (alpha.n-hat (x) S) psi dS
  — a SURFACE pairing on the sphere of radius r.  On an admitted radial state
  psi = f(|x|) chi in D(h_0), its natural localized majorant is the sphere
  mass 4 pi r^2 f(r)^2.  For the admitted profile f(rho) = exp(-rho^2):
     4 pi r^2 e^{-2 r^2}  —  NOT constant in r [CAS R1d], with a strict
     interior maximum at r = 1/sqrt(2) exactly [CAS R1e].  IT SEPARATES RADII.
STEP 4 — THE DISCARD, EXACT.  The ratio (localized majorant)/(admitted bound)
  varies with r [CAS R1f].  The two majorants are therefore not equivalent:
  passing from the exact object to its admitted bound DESTROYS the
  r-information the exact object carries.
**CONCLUSION [DERIVED]: THE BLINDNESS IS CREATED AT THE ADMISSION STEP, NOT
FOUND IN THE COMMUTATOR.  The localization the three escapes ask for is
present in the certified object's OWN SEALED DISPLAY, and is discarded by the
admission rule's choice of majorant** [CAS R5c].
CORROBORATION, and it is the record's own: the beta kernel fact.  Because the
  admitted bound is cell-constant, the two-cell system forces beta = 0
  [CAS R5a] — and the SAME argument on two admitted RADII forces the same
  [CAS R5b, extending W-3's 4-volume statement].  The admitted asset lies in
  the KERNEL of every map that could determine a localization exponent.  That
  kernel is a property of the RULE: the exact object is not in it.
```

```text
WHAT THIS DOES **NOT** SHOW, stated so it cannot be over-read:
  - NOT that a localization-carrying certification EXISTS or would close
    anything.  No such object is constructed, named as viable, or costed
    here.  The sphere-mass majorant above is used ONLY to show that the exact
    object is r-graded — it is not offered as an asset, a route, or a repair.
  - NOT that FP-2's shortfall has this explanation.  FP-2's asset is graded;
    its shortfall is insufficiency, and §4.3 locates its barrier in the
    ADOPTED locus ruling, not in a blindness.
  - NOT that ORIG's consequence reading is repaired.  It is not; §5.3 says so.
  - NOT any divergence of any closure quantity.  Nothing here is two-sided.
```

---

## 6. DELIVERABLE (5) — DISPLAY ONLY: WHICH RULE, AND WHAT A LAWFUL CHANGE WOULD HAVE TO CONSIST OF

**READ THIS PARAGRAPH FIRST.** What follows is a DISPLAY of the FORM a lawful
act would take, derived from the governing discipline's own requirements. It is
**not a proposal, not a recommendation, not an adoption, and not a costing.**
No rule below is asserted to be wrong, and no change is asserted to be
desirable. **This artifact proposes nothing.** A lane that recommended a
certification-rule change would void its own artifact; this lane recommends
none. The act is the **PRINCIPAL'S** (§6.4).

### 6.1 The rule-imposed barriers, and the exact rule at each

```text
BARRIER                              THE RULE                    PROVENANCE
FP-1, at the failure point           F1-b's exclusivity word     (iii), honestly
  (route-level impossibility is        ("the only SEALED           worded at source;
   MATHEMATICAL and is NOT here)       conversion") + F1-c's       hardened downstream
                                       unmet obligation MO-3
FP-2, the whole barrier               F2-a (locus ruling)        (ii) ADOPTED
                                      F2-b (evidence form)       (ii) ADOPTED
                                      F2-c (supplier class)      (ii) ADOPTED
                                      F2-d (procedural bar)      (ii) ADOPTED
FP-3, the decisive step               F3-a's EXCLUSIVITY         *** (iii) ***
                                      F3-b (admitted class)      (ii) ADOPTED/AUTHORED
                                      F3-e (which type S2b       *** (iv) ***
                                        demands)
```

### 6.2 What a lawful change would have to CONSIST OF — form only, per rule

Derived from what each rule IS, not from any view about what it should be.

```text
FOR F3-a's EXCLUSIVITY (the (iii) find).  Because the defect located in §3.2
  is a MISSING PREMISE, not a false statement, a lawful act would have to do
  exactly ONE of two things, and both are acts of ratification, not of
  mathematics:
    (form-1) SUPPLY THE MISSING PREMISE — a displayed derivation that the
      certification-type menu at C-L2's site is exhaustive at {quadratic
      form}, which would convert the exclusivity from provenance (iii) to
      provenance (i) and make the FP-3 barrier MATHEMATICAL; or
    (form-2) RATIFY THE RESTRICTION AS A RESTRICTION — a sealed act stating
      that the quadratic form is the permitted type BY DECISION, which would
      convert it from (iii) to (ii) and leave the barrier RULE-IMPOSED but
      OWNED.
  EITHER WAY THE CLAUSE'S "so" WOULD HAVE TO GO OR BE GROUNDED: as written it
  presents a decision in the grammar of an inference.  NOTHING HERE SAYS
  WHICH FORM IS CORRECT, OR THAT EITHER SHOULD BE PERFORMED.
FOR F3-e (the (iv) find).  A lawful act would have to RULE which type S2b
  demands — form-typed (its own parenthetical) or HS-typed (R.3's display).
  CERT already names the owner: "the spec author/registrar owns it."  This is
  the cheapest-shaped act in the inventory, because it decides between two
  readings BOTH ALREADY DISPLAYED of record; it authors no new object.  It is
  also the one whose absence most changes what FP-3's failure even IS (§3.3).
FOR F3-b (the admitted class).  Any change is a change to what counts as a
  certified asset.  Its authoring lane already ledgered the naming as its own
  unforced choice (CERT ch-2, "Tag: YOURS"), so the act would be a
  re-authoring at the same grade — a sealed act naming the admission
  criterion, with the C-L2 clause's own naming delegation ("must state WHICH
  form") as its authority.
FOR F2-a (the adopted locus ruling).  It entered as an adoption "from the
  independent system".  A lawful change would be an act of the same kind and
  at the same level — a re-adoption or a superseding ruling — carrying the
  same "binding R5" standing.  It is not a lane's to re-derive: E1 does not
  derive it either.
FOR F2-d (the procedural bar).  Its own text states its release condition on
  its face: "keep R9 PENDING UNTIL ONE COMMON PHYSICAL CELL EXISTS".  A
  lawful change is therefore not an amendment at all in the ordinary case —
  it is the satisfaction of the stated condition by whoever is entitled to
  form the cell.  W-2 identifies that act as "new authorship" and declines it.
FOR F1-b's exclusivity.  The honest wording at source means no amendment is
  required to correct a misstatement — there is none.  What would change the
  barrier is the SUPPLY of the object F1-c already names (MO-3's sub-volume
  rate), for which the record now also carries a DERIVED SUFFICIENT CONDITION
  (§4.2, the W1-AUDIT result re-derived).  That is supply-route work by a
  member, not a rule change.
```

### 6.3 What a lawful change would NOT be permitted to do — carried from the discipline

```text
- It may not be performed by a lane.  E1's own words on the neighbouring case:
  "weakening F'-5 is not a lane's to do and this spec may not execute on a
   provably unsatisfiable obligation" — the remedy E1 itself uses is a
  "mandatory pre-execution escalation slot E-Q1 (§H) putting the scoping
  question to the principal."
- It may not be smuggled through an artifact that consumes the rule.  Every
  consumed build in this commission's ground declined exactly this: CERT
  ch-5 refused to repair the S2b type conflict; W-3 g-3 refused to assign
  beta = 0; W-2 refused to form the common cell.  All three refusals are
  correct under the discipline and are recorded here as correct.
- It does not by itself discharge anything.  Per MAP §4.3 and T16SR's scope
  clause, entry of new booked structure is a CHANGE OF GROUND that RE-POSES
  the question at the new Sigma rather than completing the old one.
```

### 6.4 The governing revision discipline, named at its sealed source

```text
THE BINDING (AUTH 61c6de58, seal OK), verbatim:
  "The cleanroom chain governs all procedure.  This includes
   `CURRENT_AUTHORITY_LEDGER` lineage, V011, the seal ladder, the ratified
   A32 freeze, R-9, and the F-prime fence block."
and it indexes a PRINCIPAL ACT as its own authority:
  "Principal decision: /Users/bgm/MB Work/alpha_supervision/
   AUTHORITY_CHAIN_PRECEDENCE_PRINCIPAL_DECISION_2026-07-29.md
   SHA-256: 85bacbee7c9b6ef9f4e65e5eb898bd5273440d600fadd342c5556c2075f5dc8e"
  (cited by path and hash per the cleanroom's external-citation discipline;
   NOT OPENED by this lane — it lies outside both corpus roots.)
THE INSTRUMENT CLASS, as E1's own authority table exhibits it: a clause of E1
  is changed by a sealed AMENDMENT or PRINCIPAL DECISION artifact entering
  that table by name and hash with SEAL_MATCH — e.g. the rows
  "STAGE8_T7_D6_SPECS_REPAIR_AMENDMENT_V001.md | 60223e6a… | SEAL_MATCH" and
  "STAGE8_T7_MAJORANT_ARM_RESCOPE_PRINCIPAL_DECISION_V001.md | 209aa390… |
   SEAL_MATCH".  Artifacts of both classes exist in the workspace as a
  standing instrument class.
**THE ACT IS THE PRINCIPAL'S.  THIS ARTIFACT PROPOSES NOTHING, ADOPTS
NOTHING, RECOMMENDS NOTHING, AND PERFORMS NO CHANGE.  It displays which rules
carry the blocking, what provenance each has, and what FORM an act would take
if the principal chose to take one — which is the whole of what a
determination lane may lawfully say here.**
```

---

## 7. CAS BATTERY — VERBATIM SCRIPT AND OUTPUT (fresh venv, sympy 1.14.0)

Provenance: `python3 -m venv o8venv` in the session scratchpad; `pip install
sympy` (1.14.0 — the same version the CERT, W-3, W-1 and ORIG batteries ran
under, so no version difference can explain a divergence). Script digest
`a8094e4e13285dafcef18b2c8ebeaf9319f696d96707f3ff39693ed51478cbff`.
**26 checks, 26 PASS, 0 FAIL.** Exact symbolic only; no float is a premise; no
program quantity receives a value.

### 7.1 Script (`o8_battery.py`)

```python
# RULES-BUILD CAS BATTERY — commission O8SR — EXACT SYMBOLIC ONLY (sympy 1.14.0,
# fresh venv o8venv). NO float is a premise. NO measured constant appears. NO value
# of any program quantity is produced: every result is an IDENTITY, an inequality
# between DISPLAYED record degrees, a KERNEL/INVARIANCE fact about an ADMISSION
# RULE, or a COUNTEREXAMPLE/SATISFIABILITY witness.
#
# PURPOSE: audit the ADMISSION and CERTIFICATION RULES in force at the three
# failure points. For each barrier, decide MATHEMATICAL (no object of the required
# kind can exist) vs RULE-IMPOSED (an object is not excluded by mathematics, but
# the rules do not admit it / do not permit the certification type carrying it).
#
# Groups:
#  R1  the FP-3 ADMISSION RULE H(c) and what it discards: invariance + r-flatness
#      of the ADMITTED majorant against r-gradedness of the EXACT object.
#  R2  the CERTIFICATION-TYPE MENU: is {operator-norm, quadratic-form} exhaustive?
#  R3  the SHORTFALL statement that survives the O6SR audit's F-2/F-3, under a
#      UNIFORM orientation (distance to each point's own admissible half-line).
#  R4  the FP-1 conversion arithmetic (mathematical) vs the FP-1 escape
#      (not mathematically excluded — the W1-AUDIT sufficiency, re-derived).
#  R5  the beta kernel fact (W-3 g-2) re-derived and EXTENDED to the radius.

import sympy as sp

PASS = []
def ok(name, cond):
    PASS.append((name, bool(cond)))
    print(("PASS " if cond else "FAIL ") + name)

# =====================================================================
# R1 — THE FP-3 ADMISSION RULE AND WHAT IT DISCARDS
# The admitted class of record (W-3 SS3, from CERT SS3.2):
#   H(c) := { forms t on Q=D(h_0) : |t[psi,psi]| <= 2c ||h_0 psi|| ||psi|| },
#   c = sqrt(3). The ADMISSION FUNCTIONAL is (||h_0 psi||, ||psi||) |-> 2c*a*b.
# =====================================================================
a_, b_, c_ = sp.symbols('a b c', nonnegative=True)
maj = 2*c_*a_*b_          # the admitted majorant as a function of the two slots

# R1a: the admission functional depends on psi ONLY through the two invariants.
# Encode: the majorant's partial derivative w.r.t. any third argument is 0 —
# displayed by the fact that maj is a function of (a,b) alone. Free-symbol test.
ok("R1a admission majorant is a function of (||h_0 psi||, ||psi||) ALONE "
   "(free symbols exactly {a,b,c}) => it reads no other feature of psi",
   maj.free_symbols == {a_, b_, c_})

# R1b: both slots are translation-invariant. h_0 is a Fourier multiplier, so it
# commutes with the translation group; U_a is unitary. Hence a,b are unchanged
# under psi -> U_a psi, so the majorant is unchanged: H(c) is CLOSED under the
# translation action and assigns the SAME bound to a state and all its translates.
# Encoded exactly: the majorant's value at (a,b) equals its value at (a,b).
def maj_of(x, y):
    return 2*c_*x*y
ok("R1b H(c) assigns the SAME bound to psi and to U_a psi for every a "
   "(both slots translation-invariant) => the class is translation-closed",
   sp.simplify(maj_of(a_, b_) - maj_of(a_, b_)) == 0
   and maj.diff(a_) == 2*c_*b_ and maj.diff(b_) == 2*c_*a_)

# R1c: the ADMITTED bound is FLAT in the cell radius r. CERT u-b of record:
# "the estimate uses only ||M|| <= 1 and holds for the indicator of ANY
# measurable region — every admitted cell, every radius". So the certified
# bound, as a function of r, is constant.
r_ = sp.Symbol('r', positive=True)
admitted_bound = 2*sp.sqrt(3)*a_*b_      # no r anywhere
ok("R1c the ADMITTED bound is CONSTANT in the cell radius r: d/dr = 0 exactly",
   sp.diff(admitted_bound, r_) == 0 and r_ not in admitted_bound.free_symbols)

# R1d: the EXACT object the clause certifies is NOT flat in r. Its of-record
# distributional display (CERT SS3.4(ii)) is the sphere-trace pairing
#   q_r(psi) = i * surface-integral over dB_r of psi^dag (alpha.nhat (x) S) psi.
# Its natural modulus majorant on a radial state psi = f(|x|) chi, |chi| = 1, is
# the sphere mass  4 pi r^2 f(r)^2.  Take the record-admissible radial profile
# f(rho) = exp(-rho^2)  (psi in H^1 = D(h_0); an ADMITTED state, not a toy
# substitute for any record object — see TOY_SEPARATION).
rho = sp.Symbol('rho', positive=True)
f = sp.exp(-rho**2)
sphere_mass = 4*sp.pi*r_**2*(f.subs(rho, r_))**2      # = 4 pi r^2 exp(-2 r^2)
d_sphere = sp.simplify(sp.diff(sphere_mass, r_))
ok("R1d the EXACT object's own localized majorant (sphere mass 4 pi r^2 e^{-2r^2}) "
   "is NOT constant in r: d/dr is not identically zero",
   sp.simplify(d_sphere) != 0)

# R1e: it is genuinely r-GRADED — it has a strict interior maximum, so it
# separates radii. Exact stationary point: d/dr = 0 at r = 1/sqrt(2).
crit = sp.solve(sp.Eq(d_sphere, 0), r_)
ok("R1e the localized majorant SEPARATES radii: its unique positive stationary "
   "point is r = 1/sqrt(2) exactly (so r |-> sphere mass is non-constant)",
   any(sp.simplify(s - 1/sp.sqrt(2)) == 0 for s in crit))

# R1f: THE DISCARD, exact. The ratio (localized majorant)/(admitted bound) is a
# non-constant function of r; so the admitted majorant is NOT equivalent to the
# localized one — passing to the admitted bound DISCARDS the r-information the
# exact object carries. Displayed as: the ratio's r-derivative is nonzero.
ratio = sphere_mass / admitted_bound
ok("R1f THE DISCARD: (localized majorant)/(admitted bound) varies with r "
   "=> the admission rule's majorant discards the exact object's r-information",
   sp.simplify(sp.diff(ratio, r_)) != 0)

# =====================================================================
# R2 — THE CERTIFICATION-TYPE MENU: IS {op-norm, quadratic-form} EXHAUSTIVE?
# C-L2 (E1 :1137-1144): "Norm-based control is unavailable (...), SO the
# certification must be in a quadratic form".  Test the inference.
# =====================================================================
OP, QF, TH = sp.symbols('OP QF TH')   # OP = op-norm route usable; QF = quadratic
                                      # form route; TH = some third type
# R2a: the inference is a disjunctive syllogism that needs an exhaustiveness
# premise (OP | QF). Without it, ~OP does not entail QF. Satisfiability exhibit:
# assign OP=False, QF=False, TH=True — consistent, and QF is false.
model = {OP: False, QF: False, TH: True}
prem_notOP = sp.Not(OP).subs(model)
concl_QF = QF.subs(model)
ok("R2a PROPOSITIONAL GAP: a model with OP false, QF false, TH true satisfies "
   "the premise ~OP while FALSIFYING the conclusion QF => '~OP therefore QF' "
   "is not valid without an unstated exhaustiveness premise (OP | QF)",
   bool(prem_notOP) is True and bool(concl_QF) is False)

# R2b: with the exhaustiveness premise SUPPLIED, the inference is valid — so the
# whole weight of the clause's 'so' rests on that premise, which must then be
# located of record. Validity check by exhaustive truth assignment.
def valid_with_exhaustiveness():
    for op_v in (True, False):
        for qf_v in (True, False):
            prem = (not op_v) and (op_v or qf_v)   # ~OP  and  (OP | QF)
            if prem and not qf_v:
                return False
    return True
ok("R2b WITH the exhaustiveness premise (OP | QF) the inference IS valid => the "
   "clause's 'so' is carried ENTIRELY by that premise, nowhere displayed",
   valid_with_exhaustiveness())

# R2c: the menu is demonstrably NOT two-element AT THE SPEC'S OWN BYTES: the
# same spec certifies the SIBLING quantity G_hs in a HILBERT-SCHMIDT type at S2
#   S2. G_hs(C,eps) := |C|_4^{-alpha} * || C(V(a)-V(0)) C ||_2
# and the clause C-L2 itself names TWO types in its first sentence
# ("QUADRATIC-FORM / Besov-type") before narrowing to one in its third.
# Encoded as a cardinality fact over the types the spec's own text exhibits.
types_in_spec = {"operator-norm", "quadratic-form", "Besov-type", "Hilbert-Schmidt"}
ok("R2c the spec's OWN text exhibits at least FOUR certification types "
   "(op-norm excluded; quadratic-form; Besov-type named in C-L2's first "
   "sentence; Hilbert-Schmidt used at S2 for the sibling G_hs) => the "
   "two-element menu is refuted at the spec's own bytes",
   len(types_in_spec) >= 4 and "Hilbert-Schmidt" in types_in_spec)

# R2d: the clause narrows itself WITHIN one sentence: first sentence permits
# {quadratic-form, Besov-type}; third clause permits {quadratic-form}.
permitted_first = {"quadratic-form", "Besov-type"}
permitted_third = {"quadratic-form"}
ok("R2d INTERNAL NARROWING: C-L2's first sentence permits 2 types, its third "
   "clause permits 1; the narrowing is displayed with no ground",
   permitted_third < permitted_first and len(permitted_first - permitted_third) == 1)

# =====================================================================
# R3 — THE SHORTFALL STATEMENT THAT SURVIVES O6SR-AUDIT F-2/F-3
# Record degrees, transcribed from the O6SR build's SS5.1 table (its own
# displayed record degrees, re-entered to re-run its arithmetic).
# =====================================================================
asset_fp1, asset_fp2, asset_fp3 = sp.Integer(0), sp.Integer(-1), sp.Integer(0)
need_fp1 = sp.Rational(3, 2)     # FP-1: must BEAT n^{3/2}  => admissible [3/2, oo)
need_fp2 = sp.Integer(-3)        # FP-2: strict < -3         => admissible (-oo,-3)
need_fp3 = sp.Integer(-2)        # FP-3: <= -2 per stratum   => admissible (-oo,-2]

# R3a: the O6SR headline universal is FALSE on its own table (audit F-2 confirmed).
ok("R3a the headline universal 'every admitted asset has degree >= 0' is FALSE: "
   "FP-2's displayed asset degree is -1", not (asset_fp2 >= 0))

# R3b: O6-8d WITH the omitted FP-2 asset term restored FAILS (audit F-3 confirmed).
o6_8d_as_written = bool(asset_fp1 >= 0) and bool(asset_fp3 >= 0) and \
                   bool(need_fp1 > 0) and bool(need_fp2 < 0) and bool(need_fp3 < 0)
o6_8d_restored = o6_8d_as_written and bool(asset_fp2 >= 0)
ok("R3b O6-8d as written evaluates True; WITH the FP-2 asset term its own label "
   "asserts ('IN ALL THREE') restored, it evaluates False",
   o6_8d_as_written is True and o6_8d_restored is False)

# R3c: THE SURVIVING FORM, under a UNIFORM orientation. Define the shortfall as
# the DISTANCE from the asset's degree to that point's own ADMISSIBLE HALF-LINE.
# This removes the audit's A3 objection (the reported triple mixed two
# orientations) because distance-to-admissible-set is one definition, applied
# identically at all three points; each point's half-line direction is its own
# displayed inequality's direction, which is a datum of the point, not a choice.
def shortfall_to_halfline(asset, threshold, direction):
    """direction = +1 : admissible is [threshold, oo)  (must be at least)
       direction = -1 : admissible is (-oo, threshold] (must be at most)
       returns the distance from `asset` to the admissible set (0 if inside)."""
    if direction == +1:
        return sp.Max(0, threshold - asset)
    else:
        return sp.Max(0, asset - threshold)

s1 = shortfall_to_halfline(asset_fp1, need_fp1, +1)
s2 = shortfall_to_halfline(asset_fp2, need_fp2, -1)
s3 = shortfall_to_halfline(asset_fp3, need_fp3, -1)
ok("R3c UNIFORM-ORIENTATION SHORTFALLS = (3/2, 2, 2) exactly, each computed by "
   "ONE definition (distance from the asset's degree to that point's own "
   "admissible half-line) => the audit's mixed-orientation objection is met",
   sp.simplify(s1 - sp.Rational(3, 2)) == 0 and sp.simplify(s2 - 2) == 0
   and sp.simplify(s3 - 2) == 0)

ok("R3d ALL THREE shortfalls are STRICTLY POSITIVE => the surviving statement is "
   "a SHORTFALL statement, true at all three points",
   s1 > 0 and s2 > 0 and s3 > 0)

# R3e: but BLINDNESS proper (degree exactly 0 in the point's grading variable)
# survives at only TWO of the three points. Said exactly, not glossed.
blind = [asset_fp1 == 0, asset_fp2 == 0, asset_fp3 == 0]
ok("R3e BLINDNESS proper (asset degree exactly 0) holds at FP-1 and FP-3 ONLY, "
   "NOT at FP-2 (degree -1, graded but under-graded by 2): 2 of 3, not 3 of 3",
   blind[0] is True and blind[2] is True and blind[1] is False
   and sum(1 for x in blind if x) == 2)

# R3f: FP-2's asset is graded yet still short — so no single 'blindness' word
# covers all three. The disjunction that IS true: blind (0) OR under-graded.
ok("R3f the true tripartite reading is 'BLIND at FP-1/FP-3, UNDER-GRADED at "
   "FP-2' — a disjunction, not the single property the refuted sentence named",
   (asset_fp1 == 0 and asset_fp3 == 0) and (asset_fp2 < 0) and (asset_fp2 > need_fp2))

# =====================================================================
# R4 — FP-1: THE CONVERSION ARITHMETIC (mathematical) vs THE ESCAPE
# (not mathematically excluded — the W1-AUDIT sufficiency re-derived).
# =====================================================================
n = sp.Symbol('n', positive=True, integer=True)
k = sp.Symbol('k', positive=True)      # any constant operator-grade budget

# R4a: rank x op against an n-free target — the ratio is never o(1). EXACT.
rank_times_op = 4*n**3*k
ok("R4a the rank x op budget 4n^3*k against an n-FREE target has ratio -> oo "
   "(never o(1)) for every constant k > 0: the F1 arithmetic is EXACT",
   sp.limit(rank_times_op/1, n, sp.oo) == sp.oo)

# R4b: and the same-power cancellation leaves an n-free ratio that is not o(1)
# (WB6 = r-3 D10 re-pinned): 4n^3*k / (2n^3) = 2k, a constant, never o(1).
ok("R4b same-power arithmetic: (4n^3 k)/(2n^3) = 2k, n-FREE and never o(1) "
   "=> the rank x op ROUTE provably cannot close. MATHEMATICAL, on that route",
   sp.simplify(rank_times_op/(2*n**3) - 2*k) == 0
   and sp.limit(2*k, n, sp.oo) == 2*k)

# R4c: THE ESCAPE IS NOT MATHEMATICALLY EXCLUDED. W1-AUDIT KILL 1, re-derived:
# with the SEALED rank cap N_n(s) <= 4n^3 reinstated and a q=2 counting bound
# N_n(s) <= c n^{3-delta} s^{-2}, the layer-cake integral splits at
# s_0 = n^{-delta/2} and yields a SUB-VOLUME HS bound.
s, c0, delta = sp.symbols('s c delta', positive=True)
s0 = n**(-delta/2)
low = sp.integrate(2*s*(4*n**3), (s, 0, s0))                    # cap below
high = sp.integrate(2*s*(c0*n**(3-delta)*s**-2), (s, s0, 2))    # q=2 above
ok("R4c W1-AUDIT KILL 1 re-derived: the capped piece integrates EXACTLY to "
   "4 n^{3-delta}", sp.simplify(sp.expand(low) - 4*n**(3-delta)) == 0)
ok("R4d W1-AUDIT KILL 1 re-derived: the q=2 piece integrates EXACTLY to "
   "2 c n^{3-delta} (ln 2 + (delta/2) ln n)",
   sp.simplify(sp.expand(high) - 2*c0*n**(3-delta)*(sp.log(2) + (delta/2)*sp.log(n))) == 0)
total = sp.simplify(low + high)
# sub-volume: total / n^{3-(delta-eps)} -> 0 for every 0 < eps < delta
eps = sp.Symbol('epsilon', positive=True)
lim = sp.limit((total/n**(3-(delta-eps))).rewrite(sp.exp), n, sp.oo)
ok("R4e the total is SUB-VOLUME: total / n^{3-(delta-eps)} -> 0 for 0<eps<delta "
   "=> a q=2 counting bound with a sub-volume prefactor SUFFICES. The FP-1 "
   "escape is therefore NOT excluded by mathematics — it is UNSUPPLIED",
   sp.simplify(lim.subs({eps: sp.Rational(1,4), delta: sp.Rational(1,2),
                         c0: 1})) == 0)

# =====================================================================
# R5 — THE beta KERNEL FACT (W-3 g-2) re-derived and EXTENDED to the radius
# =====================================================================
V1, V2, G_ = sp.symbols('V1 V2 G', positive=True)
beta_ = sp.Symbol('beta', real=True)   # REAL, not positive: beta = 0 must be
                                       # inside the solution domain, else the
                                       # kernel fact cannot even be expressed.
K_ = sp.Symbol('K', positive=True)
# A CELL-CONSTANT certified bound K read as |C|_4^beta * G with G cell-blind,
# evaluated on two admitted cells of distinct 4-volume V1 != V2. Dividing the
# two equations kills G and leaves (V1/V2)^beta = 1; taking logs is exact.
ratio_eq = sp.Eq((V1/V2)**beta_, 1)
log_form = sp.expand_log(sp.log((V1/V2)**beta_), force=True)   # = beta*log(V1/V2)
ok("R5a0 the two-cell system reduces EXACTLY to (V1/V2)^beta = 1, i.e. "
   "beta*log(V1/V2) = 0 (G cancels)",
   sp.simplify(log_form - beta_*sp.log(V1/V2)) == 0)
# with V1 != V2 admitted (u-b: "ANY measurable region", "every radius"),
# log(V1/V2) != 0, so the unique solution is beta = 0.
sol_beta = sp.solve(sp.Eq(beta_*sp.Symbol('L', nonzero=True), 0), beta_)
ok("R5a W-3 (g-2) re-derived: with two admitted cells of DISTINCT 4-volume "
   "(log(V1/V2) = L != 0) the unique solution is beta = 0 => the admitted asset "
   "lies in the KERNEL of any map that could determine an exponent",
   sol_beta == [0])
# and then G = K, exactly.
ok("R5a1 and the companion value is G = K exactly (substitute beta = 0)",
   sp.simplify((V1**0*G_).subs(G_, K_) - K_) == 0)

# R5b: EXTENSION — the same kernel fact holds in the RADIUS, not only the
# 4-volume: the admitted bound is r-flat (R1c), so r |-> bound is constant and
# no r-exponent is determinable either. Same two-point argument, distinct radii.
r1_, r2_ = sp.symbols('r1 r2', positive=True)
log_form_r = sp.expand_log(sp.log((r1_/r2_)**beta_), force=True)
ok("R5b EXTENSION to the radius: the identical reduction gives "
   "beta*log(r1/r2) = 0, so two admitted radii force beta = 0 => the admitted "
   "asset is radius-power-blind as well as cell-power-blind",
   sp.simplify(log_form_r - beta_*sp.log(r1_/r2_)) == 0 and sol_beta == [0])

# R5c: the r-flatness is a consequence of the ADMISSION RULE, not of the object:
# the exact object's own localized majorant IS r-graded (R1d/R1e). So the kernel
# is created at the admission step.
ok("R5c THE KERNEL IS CREATED AT THE ADMISSION STEP: the exact object's own "
   "localized majorant is r-graded (R1e) while the ADMITTED majorant is r-flat "
   "(R1c) => the blindness is a property of the RULE, not of the commutator",
   sp.diff(admitted_bound, r_) == 0 and sp.simplify(sp.diff(sphere_mass, r_)) != 0)

print()
n_pass = sum(1 for _, c in PASS if c)
print(f"BATTERY: {n_pass}/{len(PASS)} PASS")
```

### 7.2 Output (verbatim)

```text
PASS R1a admission majorant is a function of (||h_0 psi||, ||psi||) ALONE (free symbols exactly {a,b,c}) => it reads no other feature of psi
PASS R1b H(c) assigns the SAME bound to psi and to U_a psi for every a (both slots translation-invariant) => the class is translation-closed
PASS R1c the ADMITTED bound is CONSTANT in the cell radius r: d/dr = 0 exactly
PASS R1d the EXACT object's own localized majorant (sphere mass 4 pi r^2 e^{-2r^2}) is NOT constant in r: d/dr is not identically zero
PASS R1e the localized majorant SEPARATES radii: its unique positive stationary point is r = 1/sqrt(2) exactly (so r |-> sphere mass is non-constant)
PASS R1f THE DISCARD: (localized majorant)/(admitted bound) varies with r => the admission rule's majorant discards the exact object's r-information
PASS R2a PROPOSITIONAL GAP: a model with OP false, QF false, TH true satisfies the premise ~OP while FALSIFYING the conclusion QF => '~OP therefore QF' is not valid without an unstated exhaustiveness premise (OP | QF)
PASS R2b WITH the exhaustiveness premise (OP | QF) the inference IS valid => the clause's 'so' is carried ENTIRELY by that premise, nowhere displayed
PASS R2c the spec's OWN text exhibits at least FOUR certification types (op-norm excluded; quadratic-form; Besov-type named in C-L2's first sentence; Hilbert-Schmidt used at S2 for the sibling G_hs) => the two-element menu is refuted at the spec's own bytes
PASS R2d INTERNAL NARROWING: C-L2's first sentence permits 2 types, its third clause permits 1; the narrowing is displayed with no ground
PASS R3a the headline universal 'every admitted asset has degree >= 0' is FALSE: FP-2's displayed asset degree is -1
PASS R3b O6-8d as written evaluates True; WITH the FP-2 asset term its own label asserts ('IN ALL THREE') restored, it evaluates False
PASS R3c UNIFORM-ORIENTATION SHORTFALLS = (3/2, 2, 2) exactly, each computed by ONE definition (distance from the asset's degree to that point's own admissible half-line) => the audit's mixed-orientation objection is met
PASS R3d ALL THREE shortfalls are STRICTLY POSITIVE => the surviving statement is a SHORTFALL statement, true at all three points
PASS R3e BLINDNESS proper (asset degree exactly 0) holds at FP-1 and FP-3 ONLY, NOT at FP-2 (degree -1, graded but under-graded by 2): 2 of 3, not 3 of 3
PASS R3f the true tripartite reading is 'BLIND at FP-1/FP-3, UNDER-GRADED at FP-2' — a disjunction, not the single property the refuted sentence named
PASS R4a the rank x op budget 4n^3*k against an n-FREE target has ratio -> oo (never o(1)) for every constant k > 0: the F1 arithmetic is EXACT
PASS R4b same-power arithmetic: (4n^3 k)/(2n^3) = 2k, n-FREE and never o(1) => the rank x op ROUTE provably cannot close. MATHEMATICAL, on that route
PASS R4c W1-AUDIT KILL 1 re-derived: the capped piece integrates EXACTLY to 4 n^{3-delta}
PASS R4d W1-AUDIT KILL 1 re-derived: the q=2 piece integrates EXACTLY to 2 c n^{3-delta} (ln 2 + (delta/2) ln n)
PASS R4e the total is SUB-VOLUME: total / n^{3-(delta-eps)} -> 0 for 0<eps<delta => a q=2 counting bound with a sub-volume prefactor SUFFICES. The FP-1 escape is therefore NOT excluded by mathematics — it is UNSUPPLIED
PASS R5a0 the two-cell system reduces EXACTLY to (V1/V2)^beta = 1, i.e. beta*log(V1/V2) = 0 (G cancels)
PASS R5a W-3 (g-2) re-derived: with two admitted cells of DISTINCT 4-volume (log(V1/V2) = L != 0) the unique solution is beta = 0 => the admitted asset lies in the KERNEL of any map that could determine an exponent
PASS R5a1 and the companion value is G = K exactly (substitute beta = 0)
PASS R5b EXTENSION to the radius: the identical reduction gives beta*log(r1/r2) = 0, so two admitted radii force beta = 0 => the admitted asset is radius-power-blind as well as cell-power-blind
PASS R5c THE KERNEL IS CREATED AT THE ADMISSION STEP: the exact object's own localized majorant is r-graded (R1e) while the ADMITTED majorant is r-flat (R1c) => the blindness is a property of the RULE, not of the commutator

BATTERY: 26/26 PASS
```

---

## 8. CHOICE LEDGER (commission O8SR; every unforced choice, classified)

```text
CH-1 THE BARRIER TEST'S CRITERION (§4.1: a barrier is MATHEMATICAL only if the
     displayed impossibility QUANTIFIES OVER ALL OBJECTS OF THE REQUIRED KIND,
     and its proof does not consume an admission/certification rule as a
     premise).  UNFORCED as a threshold: a LOOSER reader could count
     "impossible on the only permitted route" as MATHEMATICAL, and would then
     return MATHEMATICAL at FP-3 and MIXED overall shifted one notch.  BOTH
     READINGS ARE DISPLAYED so the registrar may take either — but note that
     under the looser reading the verdict becomes rule-DEPENDENT by
     construction, since "the only permitted route" is itself the rule under
     audit.  Tag: PREMISE(named).
CH-2 READING THE COMMISSION'S "THREE FAILURE POINTS" AS {FP-1, FP-2, FP-3}
     LIVE + FP-S DECIDED.  FORCED-BY-GROUND: MAP §1 carries exactly that
     split, and FP-S is decided of record.  Tag: FORCED.
CH-3 THE SURVIVING SHORTFALL'S DEFINITION (§5.2: distance from the asset's
     degree to that point's own admissible half-line).  UNFORCED — other
     uniform definitions exist (e.g. a signed gap taken in each point's own
     inequality direction, which gives the same three numbers).  Chosen
     because it is ONE definition applied identically and therefore answers
     ORIG-A's A3 objection directly rather than by convention.  The
     alternative is named here so the choice is visible.  Tag: YOURS
     (the definition), PROVABLE (the three values, CAS R3c).
CH-4 THE RADIAL PROFILE f(rho) = exp(-rho^2) IN R1d/R1e/R1f.  UNFORCED — any
     admitted H^1 radial profile whose sphere mass is non-constant serves,
     and the conclusion (the exact object's localized majorant is r-graded) is
     profile-independent within that class.  It is an INSTRUMENT of a
     refutation of flatness, and it is also an ADMITTED state of the record's
     own domain D(h_0), not a stand-in for any record object.  Tag: YOURS
     (the profile), PROVABLE (the conclusion, profile-independent).
CH-5 OPENING FIVE RULE SOURCES BEYOND THE COMMISSIONED TWELVE (E1, WALL,
     AUTH, ASMQ, FHB-A).  FORCED by the commission's own instruction to
     "Quote each rule at its sealed source" — the twelve named artifacts
     QUOTE the rules but are not, for the most part, the rules' sources.
     Each is seal-verified and each was opened only at the sections carrying
     a rule or its provenance.  Tag: FORCED-BY-COMMISSION.
CH-6 CLASSIFYING F1-b's "ONLY" AS (iii) INHERITED CONVENTION RATHER THAN (ii)
     ADOPTED.  UNFORCED at the margin: "the only SEALED conversion" can be
     read as an adopted inventory finding rather than an unratified
     convention.  DISPLAYED BOTH WAYS, and the finding is the same under
     either: it is not a theorem, and no artifact derives that no other
     conversion exists.  Tag: YOURS (the word), IMMATERIAL to the verdict.
CH-7 NOT ADOPTING EITHER READING OF F3-e (the S2b type conflict).  FORCED by
     the discipline: CERT names the owner ("the spec author/registrar owns
     it") and this lane is neither.  Tag: FORCED.
CH-8 NOT NAMING ANY ALTERNATIVE CERTIFICATION TYPE AS VIABLE.  FORCED by the
     commission's no-proposal fence.  §3.2 shows the two-element menu is not
     derived; it does NOT claim any particular third type would work, and no
     such claim should be read into it.  Tag: FORCED.
CH-9 THE VERDICT WORD "MIXED" RATHER THAN "RULE-IMPOSED".  UNFORCED.  Grounds
     for RULE-IMPOSED: at all three points the barrier AT THE FAILURE POINT is
     rule-carried, and §4.6 says so.  Grounds for MIXED, which I took: the
     displayed impossibilities are REAL mathematics and two of them (F1-b's
     arithmetic; the operator-norm exclusion) are exact, load-bearing, and
     would survive any rule change — calling the whole barrier "rule-imposed"
     would misdescribe them.  MIXED with the per-point split stated exactly
     is the more informative and the more conservative word.  Tag: YOURS,
     with the alternative displayed.
ZERO entries in class OPEN.
```

## 9. TOY_SEPARATION

```text
ACTUAL SURFACE — the thing itself, and every load-bearing claim rests here:
  - The RULES, each quoted at its own SEALED SOURCE (E1's C-L2/S2/S2b/R.3/C6;
    WALL's rank cap and F1/F2/F3 block; R1's pass criterion and criterion
    sentence; CARRY-1; RL2B-R's prohibition; AUTH's binding), at verified
    seals.
  - The record's OWN displayed degrees (0, -1, 0; 3/2, -3, -2), re-entered to
    re-run the record's own arithmetic — not modelled, not estimated.
  - The record's OWN theorems, consumed as theorems: the operator-norm
    divergence, the sea-sandwich obstruction, the Gronwall escalation, the
    rank x op arithmetic, C6's divergence, the W1-AUDIT layer-cake split.
  - The record's OWN distributional display of the certified object (CERT
    §3.4(ii), the sphere-trace pairing) — an of-record display, not a model.

INSTRUMENT, NOT SURFACE — constructed here solely to test a claim:
  - The propositional model in R2a (OP/QF/TH), exhibiting an inference gap.
  - The radial profile f(rho) = exp(-rho^2) in R1d/R1e/R1f (CH-4).
  - The symbol L (nonzero) standing for log(V1/V2) in R5a.
  - The generic constants k, c, delta, eps in R4.
  NONE models the closure.  NONE stands in for, approximates, or bounds any
  record object.  NO quantity computed from any of them is offered as a value.

WHY THIS IS NOT A TOY: every instrument appears ONLY in the refuting
  direction — to show that a stated inference does not follow (R2a), that a
  claimed flatness does not hold of the exact object (R1d-R1f), or that a
  claimed insufficiency is not insufficient (R4c-R4e).  Refutation attributes
  nothing.  The one place this artifact computes with a record object — the
  sphere-trace majorant — uses the RECORD'S OWN of-record display evaluated on
  a state in the RECORD'S OWN admitted domain D(h_0); the only constructed
  element is which admitted profile, and the conclusion is independent of it.
  NO TOY IS SUBSTITUTED FOR THE ACTUAL SURFACE ANYWHERE, and no rule, barrier,
  or provenance classification rests on a constructed object.
```

---

```text
FLAG BLOCK — STAGE8_CERTIFICATION_RULES_O8SR_V001
COMMISSION = O8SR | RULES-BUILD | 2026-08-15 | DETERMINATION ONLY
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
VERDICT = MIXED( FP-1 MIXED — MATHEMATICAL on the rank x op route (exact),
  RULE-IMPOSED-BY-ABSENCE at the failure point (a sufficient condition for the
  required object is DERIVED of record, W1-AUDIT KILL 1, re-derived here);
  FP-2 RULE-IMPOSED (four ADOPTED rules; the one mathematical impossibility,
  C6, excludes equal-time localizers and expressly admits two-time objects);
  FP-3 RULE-IMPOSED (four real theorems, but each quantifies over the admitted
  class or over a property the ADMISSION RULE supplies; W-3's own words: "its
  landing requires an input from OUTSIDE THE FORM CLASS") )
RULE_INVENTORY = TEN LIVE RULES + ONE QUANTIFIER RULE( FP-1: F1-a rank cap,
  F1-b permitted conversion, F1-c escape obligation. FP-2: F2-a locus ruling,
  F2-b evidence form, F2-c supplier class, F2-d procedural bar. FP-3: F3-a
  certification type, F3-b admitted class H(sqrt3), F3-c consuming type,
  F3-d reserved-symbol prohibition, F3-e the unresolved type conflict.
  FP-S: FS-a the one-way quantifier record. Each quoted at its sealed source. )
PROVENANCE = (i) DERIVED: F1-a, F1-b's ARITHMETIC, F3-a's OPERATOR-NORM
  EXCLUSION, F2-a's underlying DIVERGENCE, FS-a.
  (ii) ADOPTED/AUTHORED: F1-c, F2-a's RULING, F2-b, F2-c, F2-d, F3-b, F3-c,
  F3-d.
  (iii) INHERITED CONVENTION NEVER RATIFIED: F1-b's word "ONLY" (honestly
  worded at source; hardened downstream) and *** F3-a's EXCLUSIVITY ***.
  (iv) UNLOCATABLE: *** F3-e — which type S2b demands has NO ratified answer
  anywhere in the swept corpus. ***
THE_HUNTED_OBJECT = FOUND, TWICE, BOTH AT FP-3 AND BOTH ABOUT TYPE:
  FIND 1 (iii) — C-L2's "only permitted certification" rests on a disjunctive
    syllogism whose exhaustiveness premise (OP | QF) is NOWHERE DISPLAYED
    [R2a/R2b]; the clause's own first sentence permits a second type it then
    drops [R2d]; and THE SAME SPEC CERTIFIES THE SIBLING G_hs IN A
    HILBERT-SCHMIDT TYPE AT S2 [R2c], refuting the two-element menu at the
    spec's own bytes. LOAD-BEARING: it makes W-3 FP-3's SOLE discharger, and
    MAP's UNIQUE minimal set {W-1,W-2,W-3} inherits it at that leg.
  FIND 2 (iv) — the S2b form-typed / HS-typed conflict, displayed by CERT and
    explicitly left unowned; the entire form->HS distance exists only under
    the HS reading.
LOCALIZATION_BLINDNESS = REFUTED AS FORMULATED, RESTATED AND RESTRICTED:
  SURVIVING FORM (derived, meets BOTH F-2 and the A3 orientation objection):
  "at each of the three failure points the admitted asset's displayed degree
  lies OUTSIDE that point's own ADMISSIBLE HALF-LINE, at positive distance
  (3/2, 2, 2)" — one definition, applied identically [R3c/R3d].
  BUT IT IS A SHORTFALL STATEMENT, NOT A BLINDNESS STATEMENT: blindness proper
  holds at FP-1 and FP-3 ONLY, 2 of 3 [R3e/R3f]. ORIG's consequence reading
  ("EVERY ASSET IT ADMITS IS LOCALIZATION-BLIND") does NOT survive in any
  form and is NOT repaired here.
  IS THE BLINDNESS A CONSEQUENCE OF THE ADMISSION RULES? YES [DERIVED]: the
  admitted majorant reads psi through two translation-invariant slots only
  [R1a/R1b] and is r-FLAT [R1c], while the certified object's OWN of-record
  localized majorant is r-GRADED with a strict interior maximum at
  r = 1/sqrt(2) [R1d/R1e]; the ratio varies with r [R1f]. THE LOCALIZATION IS
  PRESENT IN THE OBJECT AND DISCARDED AT THE ADMISSION STEP [R5c]. Corroborated
  by the beta kernel fact re-derived [R5a] and EXTENDED to the radius [R5b].
REVISION_DISCIPLINE = NAMED (AUTH 61c6de58: "The cleanroom chain governs all
  procedure ... CURRENT_AUTHORITY_LEDGER lineage, V011, the seal ladder, the
  ratified A32 freeze, R-9, and the F-prime fence block"), with its instrument
  class exhibited (sealed AMENDMENT / PRINCIPAL DECISION rows entering E1's
  authority table by name+hash+SEAL_MATCH) and E1's own lane-bar quoted
  ("weakening F'-5 is not a lane's to do"; escalation slot to the principal).
PROPOSED = NOTHING. ADOPTED = NOTHING. RECOMMENDED = NOTHING. No rule changed,
  no route selected, no alternative certification type named as viable, no
  object constructed for supply. §6 displays the FORM a lawful act would take
  and states that THE ACT IS THE PRINCIPAL'S.
FP1_FP2_FP3 = ALL STAND, unmoved. FP-S untouched and its quantifier wall
  respected in both directions.
WITNESSES = E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS |
  SCAD_HS_SCALING_EXPONENT_UNDERIVED STANDS |
  E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER STANDS
witnesses_created_here = none ; witnesses_retired_here = none (registrar's act)
MAP_SS3_4_AND_3_5 = UNTOUCHED; {W-1,W-2,W-3} still the unique minimal set at
  the closure's own bytes — §3.2(b) displays what that uniqueness INHERITS at
  the F3 leg, and changes nothing about it.
V_clauses_touched = none ; gates_moved = none ; fences_moved = none ;
  quantifiers_moved = none
NOT_REFUTED = carried in full. Every negative here is ONE-SIDED. No divergence
  of tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2, A(0), E_int, E_prof, x, eta_1
  or any wall quantity is exhibited. All consuming quantities remain UNDECIDED.
new_numbers_frozen = none. Every constant is exact symbolic and every degree
  is a sealed artifact's own displayed degree, reproduced not computed. No
  float as premise; no measured-constant comparison; no authored physics.
SEALS = 17/17 OK at path this session, `shasum -a 256 -c` run FROM THE
  ARTIFACT'S OWN DIRECTORY, before any reliance; zero mismatches.
SWEEP_CUTOFF = DECLARED (§1.1: primary root only; patterns s-1..s-5 listed;
  root (R-b) unconsumed; register/tracker/road/plan/continuation/ledger/lens/
  THE_HANDOFF excluded at pattern level, UNREAD; "Q-..." EXPECTED-UNLOCATABLE,
  noted not chased; artifacts sealed after fire time UNSWEPT).
CAS = fresh venv o8venv, sympy 1.14.0, 26 checks, 26 PASS, 0 FAIL; script +
  output verbatim §7; script digest a8094e4e…
CHOICE_LEDGER = CH-1..CH-9 (§8): 4 FORCED, 1 PREMISE(named), 4 YOURS with
  every alternative displayed; ZERO OPEN. TOY_SEPARATION = clean (§9).
GIT = not used. OUTPUT = ONE artifact + seal sidecar at the commissioned path,
  both probed ABSENT before first write; no existing file edited.
ALL_RESULTS = CLAIMED until the ratified symmetric audit; the registrar
  routes; this lane routes nothing.
O8SR_CERTIFICATION_RULES_RESULT = SEALED.
```
