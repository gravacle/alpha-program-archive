# STAGE 8 — CANC-PANEL-SYMHUNT: MISSED-SYMMETRY HUNT AGAINST THE CANCELLATION BUILD'S B2

## BLIND ADVERSARIAL VERIFIER — ONE LENS OF THREE — [SEALED]

Date: 2026-08-13
Role: BLIND adversarial verifier, cross-lineage to the builder. DEFAULT = REFUTE. Target:
STAGE8_REQUIRE_BUILD_CANCELLATION_V001.md, claim B2 (CANCELLATION_SYMMETRY = PARTIAL),
attacked by exhausting cancellation mechanisms the build never tried: (1) composition
parity of the sandwiched object, (2) CTP two-branch pairing, (3) lattice Ward /
telescoping, (4) n-parity at the response level. No STAGE8_CANC_PANEL_* artifact was
opened (blind to co-panelists; none existed at path when probed).

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held: connection-only, symbolic. NO value of `n`, `kappa_record`, `alpha`
(coupling), any exponent, norm, scale, length, or spectrum computed, bounded, or
evaluated — everything below is order/parity/degree bookkeeping and exact
Fourier/Clifford ALGEBRA on sealed forms (algebra, not evaluation). NO scale, imported
GR, or faithfulness premise used as authority by me. Scale-bearing sealed text (ECO/RFA)
read SUSPECT-ONLY, to classify and to attack the build's symmetry-exhaustion claim at
its own cited bytes — never as a positive certifier of any physical value. No
register/tracker/plan/road/ledger/lens read. No git action. Output name probed before
write: ABSENT.

---

## 0. VERDICT IN ONE LINE

**FOUND — mechanism (1), COMPOSITION PARITY. The build decided the symmetry binary on
the bare difference `V(a) - V(0)` and expressly declined the composed object
`X = C (V(a) - V(0)) C` — but the composed object is the one R-L2b actually bounds
(ECO :174), and its parity/Clifford bookkeeping is symmetry-level algebra, not a metric
computation. Worked here: the sealed sea kernel `C` is a PROJECTOR (symbol
`(1/2)(I - alpha.k_hat)`, idempotent by Clifford algebra; corroborated by the sealed
"sea_projector" flag, ECO :222); the difference's leading survivor has Fourier symbol
proportional to `alpha.ell_perp / |k|` — purely TRANSVERSE; and a projector sandwich
annihilates a transverse Clifford vector EXACTLY: `P (alpha.ell_perp) P = 0`. So the
degree -2 residual the build says SURVIVES is killed identically, at the symmetry level,
in the sandwiched object. B2's exhaustion claim ("the one exact symmetry present is
powerless for the HS object") is REFUTED one step past where the build stopped — exactly
where it stopped short. This does NOT certify full cancellation or force alpha: the
post-parity remainder (write-chain regularity G3, NOT SUPPLIED of record; IR/boundary)
is a genuine non-symmetry computation, and RFA :137-138's UNKNOWN stands — relocated one
order down. Mechanisms (2), (3), (4): CLOSED, each against the sealed nonzero-write
facts. HUNT_VERDICT = B2_REFUTED(COMPOSITION_PARITY).**

---

## 1. SEALS RECOMPUTED AT PATH (shasum -a 256), BEFORE READING

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Full digests recomputed
at path this relay; first 16 hex shown. CONN = connection-only; SCALE = scale-bearing
(read suspect-only, to classify and attack).

```text
BUILD UNDER TEST:
  STAGE8_REQUIRE_BUILD_CANCELLATION_V001.md   4971e2739666bd95  MATCHES-TASKED   (CANC)
TASKED SECONDARY:
  STAGE8_REQUIRE_BUILD_CLUSTER_SUMMABILITY_V001.md 5cdd5dafccd1dfd5 MATCHES-TASKED CONN
  STAGE8_REQUIRE_CLUSTER_CHECK_V001.md        a7f75d0f2c4ed960  MATCHES-TASKED   CONN
  STAGE8_R_RECORD_L_FORM_FABLE_V001.md        5e49d2093d4ee178  MATCHES-TASKED   CONN (FORM)
FROM THE BUILD'S REFERENCES, recomputed at path:
  STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md
                                              0f3082cab910f2eb  MATCHES build-recorded  SCALE (ECO)
  STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md
                                              2ede02aea415157a  MATCHES build-recorded  SCALE (RFA)
```

Six digests verified; all match (four tasked digests exactly; ECO/RFA against the
digests the build §1 records). CONN/SCALE typing recomputed, not inherited: FORM carries
connection/holonomy content only ("no metric argument anywhere", FORM :98-99); ECO/RFA
carry `C(r)` with explicit length `|r|`, diamonds, `|D|_4`, the exponent — SCALE.
No STAGE8_CANC_PANEL_* file existed at path (glob probed: no matches) — panel blindness
intact.

Symbol disambiguation, load-bearing for everything below: in the sealed kernel
`C(r) = (1/2) delta^3(r) I - i alpha.r / (2 pi^2 |r|^4)` (ECO :153), `alpha.r` is the
DIRAC ALPHA-MATRIX vector dotted with `r` — matrix-valued — NOT the coupling and NOT the
exponent `alpha` of `|D|_4^alpha`. Evidence at the bytes: the explicit `I` on the delta
term (matrix-valued kernel); "off-diagonal modulus EXACTLY 1/(2 pi^2 |r|^3)" and
`||C_off(r)||_op` (ECO :154-155) — an operator norm, exact because
`||alpha.r||_op = |r|` when `(alpha.r_hat)^2 = I`; the HS integrand "goes as
1/(4 pi^4 |r|^6)" (ECO :159) — ALL singular values of `alpha.r_hat` equal 1, which for a
Hermitian matrix forces `(alpha.r_hat)^2 = I` (the Clifford relation); and the protected
flag `sea_projector_restricted_to_a_diamond_is_HS = false` (ECO :222) — the corpus's own
word for `C` is PROJECTOR, and the worked identity `||[C,P]||_2^2 = 2 sum sigma(1-sigma)`
(ECO :192) is the exact two-projection formula. Three independent corroborations, all
sealed text.

---

## 2. WHERE THE BUILD STOPPED — ITS OWN WORDS

The build's B2-B4 chain runs on the BARE difference: B1 gives
`V(a)-V(0) = [exp(i Delta)-1] V(0)`, `Delta = n<ell,r> + O(r^2)`; the delta part dies at
coincidence; the survivor is `i n<ell,r> * C_off(r)`, degree -2 (CANC §3.1-3.2). B2's
exhaustion (CANC §3.3 :181-214) tests four mechanisms (phase structure, conservation,
charge conjugation, oddness-of-C_off) on THAT object and finds the oddness "POWERLESS
for the HS object because the HS integrand |C_off|^2 is EVEN" (CANC :205-213). Then the
build states, verbatim (CANC :234-242): "What remains a COMPUTATION (not a symmetry) is
whether the surviving degree -2 residual is nonetheless rendered HS-finite by the
OPERATOR COMPOSITION in the sandwich C(...)C ... I decline to evaluate it" — and files
the open question under RUNS on the tasking's instruction.

The attack point: the parity/degree/Clifford bookkeeping of the COMPOSED kernel is
symmetry-level algebra — the same class of move the build itself used throughout
(symbolic orders of homogeneous factors, `|odd|^2 = even`). The build could have taken
this step without touching a single value. It did not. Section 3 takes it.

---

## 3. MECHANISM 1 — COMPOSITION PARITY: FOUND (EXTRA_CANCELLATION, symmetry-level)

### 3.1 The tested object is the sandwich, of record

ECO :174, verbatim: "R-L2b asks for `|| C (V(a) - V(0)) C ||_2 <= |D|_4^alpha G_hs`."
The physically tested object is `X = C (V(a)-V(0)) C` — never the bare middle factor.
Every B2/B3/B4 conclusion the build filed about "the HS object" must survive the
sandwich to bind R-L2b. (Diamond restrictions sit at the boundary; the singularity in
question is at the interior diagonal — RFA :33, "r -> 0 IS INTERNAL TO WHATEVER DIAMOND
ONE IS IN" — so restriction cutoffs do not enter the diagonal bookkeeping.)

### 3.2 `C` is a projector — exact algebra on the sealed kernel

Fourier transform of the sealed kernel (exact identities, no value evaluated;
convention `f_hat(k) = int e^(-ik.r) f(r) d^3r`; constants carried symbolically):

```text
FT[ 1/|r|^2 ]   = 2 pi^2 / |k|                       (standard exact identity, 3D)
r/|r|^4         = -(1/2) grad(1/|r|^2)
FT[ r/|r|^4 ]   = -(1/2)(i k)(2 pi^2/|k|) = -i pi^2 k_hat
FT[ C_off ]     = (-i/(2 pi^2)) alpha.(-i pi^2 k_hat) = -(1/2) alpha.k_hat
C_hat(k)        = (1/2)( I - alpha.k_hat ).
```

With the Clifford relation `(alpha.k_hat)^2 = I` (forced by the sealed EXACT modulus,
§1), `C_hat(k)^2 = (1/4)(I - 2 alpha.k_hat + I) = C_hat(k)` — IDEMPOTENT. Hermitian.
`C` is exactly the band projector the corpus already names ("sea_projector", ECO :222;
the two-projection commutator identity, ECO :192). Any overall sign convention flips
`(I - alpha.k_hat)` to `(I + alpha.k_hat)` — still a projector; every step below is
sign-robust.

### 3.3 The difference's leading survivor is purely transverse

The build's own B1 survivor (taken as given here; it is the claim under test by the
panel's other lenses): `D_lead(r) = i n<ell,r> * C_off(r) = n <ell,r> alpha.r /
(2 pi^2 |r|^4)` — REAL, EVEN in r, homogeneous degree -2 (the build's own bookkeeping,
confirmed: odd x odd = even, +1 + (-3) = -2). Its Fourier symbol, exact
(`FT[r_m g(r)] = i d/dk_m g_hat`; write current `ell` frozen at the cell — the
symmetry-level, principal-symbol move; corrections carry derivatives of `ell` and are
scoped in §3.6):

```text
FT[ alpha.r/(2 pi^2 |r|^4) ] = -(i/2) alpha.k_hat
D_hat(k) = n ell_m . i d/dk_m [ -(i/2) alpha.k_hat ]
         = (n/2) ell_m [ alpha_m/|k| - (alpha.k)k_m/|k|^3 ]
         = (n/(2|k|)) . alpha.ell_perp ,      ell_perp := ell - k_hat(k_hat.ell).
```

Differentiating the unit vector `k_hat` is automatically transverse
(`d k_hat_j/d k_m = (delta_mj - k_hat_m k_hat_j)/|k|`), so the longitudinal part cancels
inside `D_hat` identically: the leading symbol is a purely TRANSVERSE Clifford vector,
even in k, homogeneous degree -1. Convention-robust.

### 3.4 The parity zero: the projector sandwich annihilates the survivor EXACTLY

Naive parity alone is INCONCLUSIVE — this must be said first, against manufacturing a
false FOUND: `C = (1/2)delta + C_off`, so
`C D C = (1/4)D + (1/2)(C_off*D + D*C_off) + C_off*D*C_off` (convolutions);
odd*even*odd = even and every term is even degree -2 — parity by itself allows the terms
to add. What decides it is the CLIFFORD structure. Write `M = alpha.k_hat`, `M^2 = I`,
and note `{M, alpha.ell_perp} = 2 k_hat.ell_perp I = 0` (transversality) — so
`(alpha.ell_perp) M = -M (alpha.ell_perp)`. Then, exactly:

```text
4 C_hat D_hat C_hat / (n/(2|k|))
  = (I - M)(alpha.ell_perp)(I - M)
  = alpha.ell_perp - M(alpha.ell_perp) - (alpha.ell_perp)M + M(alpha.ell_perp)M
  = alpha.ell_perp - M(alpha.ell_perp) + M(alpha.ell_perp) - M^2(alpha.ell_perp)
  = alpha.ell_perp - alpha.ell_perp
  = 0.
```

**The principal symbol of `C (V(a)-V(0)) C` VANISHES IDENTICALLY** — at every k, every
cell, every n, by projector idempotency + Clifford anticommutation. Equivalently, with
`P = C_hat`: `P (alpha.ell_perp) P = (alpha.ell_perp)(I-P)P = 0` and likewise
`(I-P)(alpha.ell_perp)(I-P) = 0` — the survivor is purely INTERBAND (off-diagonal
between the sea band and its complement). Term-by-term reading of the four-term display:
the dangerous `(1/4)D` (the delta-part sandwich, which alone does NOT vanish) is
cancelled exactly by `C_off*D*C_off = -(1/4)D` at symbol level, while the cross terms
kill each other by anticommutation. This is a cancellation ACROSS the sandwich — invisible
by construction to any analysis of the middle factor alone. The build's (D) statement
"the HS integrand |C_off|^2 is EVEN — parity gives nothing" is true of the bare object
and simply does not survive composition: the sandwich changes the integrand.

### 3.5 Corroboration by the corpus's own route

Purely-interband + projector gives the exact identity `D_lead = [C,[C,D_lead]]` at
principal symbol (for any A with `PAP = (I-P)A(I-P) = 0`:
`[P,[P,A]] = PA + AP - 2PAP = A`). That is EXACTLY the structural premise of the sealed
commutator route — ECO :195-197: "If `C(V(a)-V(0))C` can be written with the difference
acting as a commutator with C, the finite object is the natural one" — established here
at the symmetry level. The route the corpus filed as ROUTE_NOT_LEMMA (ECO :199-201,
:227) has its structural half PROVED by the parity zero: the difference's leading part
IS commutator-shaped, which is precisely WHY the sandwich annihilates it
(`C [interband] C = 0` identically).

### 3.6 Exact scope — what this does and does not change (against over-claim)

Degree bookkeeping after the zero, symbolic only:

```text
bare difference (build B3):    leading kernel degree -2, |.|^2 = degree -4,
                               int d^3r r^2 . r^-4 ~ eps^-1  -> HS-SINGULAR. CONFIRMED
                               for the middle factor alone.
sandwiched object X:           principal symbol (degree -1 in k) == 0. Everything that
                               remains LOCALLY is one order down: kernel degree <= -1,
                               |.|^2 <= degree -2, int d^3r r^2 . r^-2 ~ int dr -> LOCALLY
                               HS-INTEGRABLE at the diagonal, by degree count alone.
also symmetry-safe already:    O(r^2) phase terms x C_off = degree -1 (safe);
                               all phase orders x the delta part = 0 exactly (r delta^3(r) = 0).
```

What the finding does NOT establish, stated hard:

- NOT full cancellation certified, NOT alpha forced, NOT alpha = 1. The vanishing is of
  the PRINCIPAL symbol at frozen write current. The subleading terms carry derivatives
  of `ell(x)` — and the write chains' support/current-density realization is exactly
  FORM's G3, "NOT SUPPLIED" of record (FORM :269-282, :353-357). If `ell` is
  cell-piecewise, the corrections are face-localized, and their HS fate is a genuine
  metric/analytic computation, not symmetry. The IR/boundary (diamond-restriction) side
  is likewise untouched. RFA :137-138 "whether the singularity fully cancels is UNKNOWN"
  STANDS — the unknown is relocated one order down and one object over (from the bare
  difference's degree -2 leading term, now dead in the sandwich, to the non-symmetry
  remainder).
- The build's B3 (SURVIVES) remains TRUE of the bare middle factor. It is FALSE as a
  statement about the tested object X: no HS-obstructing singularity survives AT THE
  SYMMETRY LEVEL in the sandwich.
- The build's B4 filing (NET = RUNS) rested on "composition NOT analyzED" plus the
  tasking's fiat ("needing a computation to see the residual = RUNS"). The composition's
  leading term needed NO computation — only the algebra above — and it cancels. The fiat
  was applied one step too early: at the symmetry level the correct filing of the binary
  is UNDETERMINED pending the non-symmetry remainder (G3 + IR), not RUNS. Whether the
  panel re-files it under the tasking's no-third-road rule is the panel's call, not this
  lens's; what this lens establishes is that the symmetry road did not end where the
  build said it did, and its next step points toward MORE cancellation, not less.
- Fairness to the builder: the caveat was honestly flagged in the build itself
  (CANC :234-242, :303-317). This is a stopped-short refutation, not an integrity break.

Conditionality, named once: the finding runs on the build's own B1 survivor form
(`i n<ell,r> x C_off`). If B1's identification of `V(0)`'s leading singularity with the
sealed `C(r)` fails, then B2-B4 fall for a different reason and this lens's finding is
moot in the same stroke; either way the B2->B3->B4 chain does not stand as filed.

---

## 4. MECHANISM 2 — CTP TWO-BRANCH PAIRING: CLOSED (NO_HELP)

The record is two-branch with conjugate phases:
`Z_N = prod_j conj(z_j[a_-]) z_j[a_+]` (FORM B5 :100-105), and the sealed derivation D3
(FORM :180-199) shows: single-branch charged amplitude IDENTICALLY ZERO (:185-187); the
two-branch object carries `Z_N[a_+,a_-]` (:188); the branch DIAGONAL `a_+ = a_-` gives
`Z_N = prod |z_j|^2 = 1` — FLAT (:189); the channel is carried by `a_Delta = a_+ - a_-`
alone (:193-199).

Why pairing +Delta against -Delta cannot cancel the odd leading term:

```text
(i)   The branch conjugation is ALREADY SPENT. Exact cancellation between branches
      happens exactly and only on the branch diagonal a_+ = a_- (sealed: Z_N = 1,
      FORM :189) — which is a_Delta = 0, i.e. NO write. The pairing's entire content is
      to remove the common-branch phase and leave the a_Delta-only (relative-holonomy)
      dependence; asking it to ALSO cancel the a_Delta first-order term is asking the
      connection response to vanish identically — contradicting the sealed nonzero write
      for n != 0 (FORM B6 :107-113) and D3's "the doubled algebra is not optional."
(ii)  For a_Delta != 0 the branch swap acts as a_Delta -> -a_Delta plus conjugation; on
      the quadratic response this gives R[-a_Delta] = R[a_Delta] by BILINEARITY — a
      relation (evenness of a quadratic form), not a kernel cancellation. Same shape as
      the build's (C) closure, which this confirms from the branch side.
(iii) The oddness under attack is odd-in-r of the KERNEL at fixed a_Delta. Branch
      conjugation acts on the connection arguments a_+/-, not on the spatial separation
      r; it cannot touch r-parity at all.
```

CTP_BRANCH_PAIRING = NO_HELP. Spans: FORM :100-105, :180-199; B6 :107-113.

---

## 5. MECHANISM 3 — LATTICE WARD / TELESCOPING: CLOSED (NO_HELP)

The hunt: per-cell `<ell_j, r> != 0`, but does the SUM over the write chain telescope
under `d ell_j = 0`, killing the aggregate first-order term with no single cell's term
vanishing?

```text
(i)   What conservation buys is sealed and already spent: closedness makes each
      <ell_j, .> kill exact forms (gauge moves) — FORM D3 :198-199, "invariant under the
      licensed common-gauge moves ... abelian relative holonomy." So the aggregate
      Phi_tot[a] = sum_j <ell_j, a> (FORM D4 :208-209) reads exactly and only the
      FLUX / relative-holonomy class of a. A discrete Ward identity is a statement that
      the gauge-trivial part drops — it has no power over the flux part.
(ii)  A FULL telescope (sum_j ell_j = 0, or its flux part zero) would make Phi_tot == 0
      — ZERO VARIATION under every external history. That is verbatim the sealed
      criterion that ELIMINATED n = 0 (FORM B6 :111-113: the trivial character "has zero
      variation under every external history and fails the surface's own
      charge/flux-access requirement"). Applying it at n != 0 contradicts the admitted
      nonzero write. Full telescoping <=> trivial write <=> contradiction. Same closure
      wall the build hit at (A)/(B) — it holds at the aggregate level too.
(iii) A PARTIAL telescope (cells pairing with opposite currents) would need a sealed
      pairing structure on the write chains. None exists of record: the chains'
      support/current-density realization is exactly G3, NOT SUPPLIED (FORM :269-282).
      Nothing licenses anti-alignment; positing it would be manufacturing a mechanism.
```

WARD_TELESCOPING = NO_HELP. The build's (B) closure ("divergence-free is not zero",
CANC :189-194) is CONFIRMED and extended from the per-cell to the aggregate/chain level:
conservation's whole content is gauge-invariance, and the surviving flux pairing is the
sealed nonzero write itself.

---

## 6. MECHANISM 4 — n-PARITY AT THE RESPONSE LEVEL: CLOSED (SQUARES_NO_HELP)

Sealed: `R_record,L(n) = R_record,L(-n)` EXACTLY — "the quadratic response can at most
ever reach |n|, never the sign" (FORM 5.1 pt 4, :384-386). The first-order kernel term
`i n <ell,r> C_off` is odd in n. Settle whether response-level evenness forces the odd
part to cancel, or merely squares it:

```text
IT SQUARES. The derived form is R = n^2 Phi^T C Phi (FORM D5 :241-242) — the winding
enters EXACTLY ONCE, as the global quadratic prefactor of an n-blind form; the finite-N
Hessian "scales as n^2 and as nothing else in n" (FORM D4 :231). The odd-in-n kernel
term reaches the response only through TWO insertions of V (each proportional to n, FORM
D2 :168) — its contribution is automatically proportional to n^2. R(n) = R(-n) is
therefore a CONSEQUENCE of quadraticity, satisfied identically by the square; it is not
an independent constraint with anything left to force on the kernel. If evenness were to
force the odd-in-n part to vanish it would force n.Phi_tot == 0 — the trivial write —
contradicting n != 0 (FORM B6) yet again. Note also mechanisms (2) and (4) are one
symmetry seen twice: n and a_Delta enter only through the product n.Phi_tot[a_Delta]
(FORM D4 :208-209), so branch swap ~ n -> -n; both land on the build's (C) closure
(CANC :196-203), which is CONFIRMED at the response level.
```

N_PARITY = SQUARES_NO_HELP.

---

## 7. IMPORT / MACHINERY AUDIT (MINE)

```text
No value of n, kappa_record, alpha (coupling), any exponent, norm, length, scale, or
  spectrum computed, bounded, estimated, evaluated, or compared. No norm was assigned a
  number anywhere; HS-integrability statements are degree bookkeeping (int r^2 r^-2 dr
  converges at 0 is a symbolic order statement, no cutoff evaluated).
The algebra performed — Fourier symbols of the sealed kernel, Clifford anticommutation,
  projector idempotency, the four-term sandwich display — is exact symbolic algebra on
  sealed forms; the constants (1/2, 2 pi^2) are carried verbatim from sealed text and
  never evaluated against anything. alpha-matrices vs coupling-alpha disambiguated at
  first use (§1), on sealed bytes (ECO :153-155, :159, :192, :222).
Scale-bearing text (ECO/RFA) read SUSPECT-ONLY: to classify (SCALE), to fix the tested
  object (ECO :174), and to attack B2's exhaustion claim at the same bytes and the same
  use-class the build itself operated under (its tasking permitted reading the sealed
  kernel FORM to analyze cancellation symmetry). The finding refutes a symmetry-
  exhaustion claim; it certifies NO physical value, NO bound, NO exponent — the fence's
  positive-certifier ban is not touched. Connection-only bedrock carries the closures
  in §4-§6 (FORM B5/B6/D2/D3/D4/5.1 only).
No GR, no faithfulness premise, no register/tracker/plan/road/ledger/lens read. No
  STAGE8_CANC_PANEL_* artifact opened. Machine use: shasum -a 256 (hash integrity) and
  file reads only. No git action.
```

---

## 8. FLAG BLOCK

```text
COMPOSITION_PARITY = EXTRA_CANCELLATION(symmetry-level: the tested object is the
  SANDWICH X = C(V(a)-V(0))C (ECO :174), and C is the sealed sea PROJECTOR — symbol
  C_hat(k) = (1/2)(I - alpha.k_hat), idempotent by the Clifford relation
  (alpha.k_hat)^2 = I, grounded at ECO :153-155 (kernel + EXACT modulus), :159 (all
  singular values 1), :192 (two-projection commutator identity), :222 (sea_projector
  flag). The difference's leading survivor (build B1: i n<ell,r> x C_off, real, even,
  degree -2) has Fourier symbol (n/(2|k|)) alpha.ell_perp — purely TRANSVERSE — and a
  projector sandwich annihilates a transverse Clifford vector EXACTLY:
  (I-M)(alpha.ell_perp)(I-M) = 0 with M = alpha.k_hat, since {M, alpha.ell_perp} = 0 and
  M^2 = I. Naive parity alone is inconclusive (odd*even*odd = even); the Clifford/
  projector algebra decides it. The survivor is purely INTERBAND (PAP = (I-P)A(I-P) = 0,
  hence A = [C,[C,A]] at principal symbol) — the sealed commutator route's structural
  premise (ECO :195-197, ROUTE_NOT_LEMMA :199-201) PROVED at the symmetry level. WHAT IT
  CHANGES: B3's SURVIVES is false for the tested object X — the degree -2 residual is
  killed identically in the sandwich, and everything remaining locally is degree <= -1,
  HS-integrable at the diagonal by degree count; B4's RUNS-by-fiat was filed one step
  early (the composition's leading term needed algebra, not a computation, and it
  CANCELS). NOT full-cancellation-certified, NOT alpha forced: the vanishing is of the
  principal symbol at frozen write current; the remainder carries derivatives of ell —
  exactly G3, the write chains' support/current-density realization, NOT SUPPLIED of
  record (FORM :269-282) — plus IR/boundary, a genuine non-symmetry computation; RFA
  :137-138 UNKNOWN stands, relocated one order down. Conditional on the build's own B1
  survivor form; if B1 falls, B2-B4 fall with it by the other lenses' route.)

CTP_BRANCH_PAIRING = NO_HELP(the branch conjugation is already spent producing the
  a_Delta-only dependence: exact between-branch cancellation occurs exactly and only on
  the branch diagonal a_+ = a_- (sealed Z_N = prod|z_j|^2 = 1, FORM :189) — i.e. at zero
  write; for a_Delta != 0 the swap gives R[-a_Delta] = R[a_Delta] by bilinearity — a
  relation, not a kernel cancellation; killing the first-order a_Delta term would make
  the connection response vanish identically, contradicting the sealed nonzero write for
  n != 0 (FORM B6 :107-113) and D3's two-branch necessity (:180-199); and branch
  conjugation acts on a_+/-, not on r — it cannot touch r-parity.)

WARD_TELESCOPING = NO_HELP(d ell_j = 0's whole content is sealed as gauge-invariance —
  each <ell_j,.> kills exact forms (FORM D3 :198-199) — so the aggregate
  Phi_tot = sum_j <ell_j,.> (D4 :208-209) reads exactly the flux/relative-holonomy
  class; a full telescope (sum_j ell_j = 0 in flux) would make Phi_tot == 0 = ZERO
  VARIATION, verbatim the sealed criterion that eliminated n = 0 (B6 :111-113), a
  contradiction at n != 0; a partial telescope needs an anti-alignment pairing of cell
  currents that is exactly the unsupplied G3 datum — nothing sealed licenses it.
  Confirms and extends the build's (B) closure from per-cell to aggregate.)

N_PARITY = SQUARES_NO_HELP(R(n) = R(-n) (FORM :384-386) is REALIZED by the global n^2
  prefactor of an n-blind form (D5 :241-242; D4 :231 "scales as n^2 and as nothing else
  in n"): the odd-in-n kernel term enters the quadratic response through two insertions
  each proportional to n — it squares; evenness is a consequence of quadraticity, not a
  constraint, and forcing the odd part to vanish would force the trivial write,
  contradicting n != 0. Mechanisms 2 and 4 are one symmetry seen twice (n and a_Delta
  enter only as n.Phi_tot[a_Delta]); both land on and confirm the build's (C).)

MISSED_MECHANISM = FOUND(COMPOSITION_PARITY — the projector-sandwich / Clifford-
  transversality cancellation of the composed object's leading term, worked in §3.
  CONSEQUENCE: partial-only is wrong for the object R-L2b actually bounds — the sandwich
  forces one MORE exact symmetry-level cancellation, so the build's B2 exhaustion, B3
  SURVIVES, and B4's RUNS filing do not stand as written; the symmetry-level filing of
  the binary is UNDETERMINED pending the non-symmetry remainder (G3 write-chain
  realization + IR/boundary), NOT RUNS. It is NOT "full cancellation -> alpha forced":
  no value, bound, or exponent is certified by this lens, and the deciding remainder is
  a genuine metric computation that stays open of record.)

HUNT_VERDICT = B2_REFUTED(COMPOSITION_PARITY: the build's claim that the one exact
  symmetry present (the C_off oddness/Clifford structure) is "POWERLESS for the HS
  object" is false for the HS object of record (the sandwich, ECO :174) — the very same
  structure, acting through the projector composition the build declined to analyze
  (CANC :234-242), annihilates the leading survivor exactly, one step past where the
  build stopped. The refutation is conditional only on the build's own B1 survivor form
  and on the same kernel-symmetry use-class the build's tasking licensed. The builder's
  caveat was honestly flagged; this is a stopped-short refutation, not an integrity
  break.)

PROVENANCE = CLEAN(six seals recomputed at path before reading, all matching (four
  tasked; ECO/RFA against the build-recorded digests — the build's §1 seal claims verify
  at the bytes); no panel artifact opened; scale-bearing text read suspect-only to
  classify and attack, never as a positive certifier; every §4-§6 closure stands on
  connection-only FORM bedrock; no register/tracker/plan/road/ledger/lens read; no git
  action.)

MACHINERY_USED_BY_ME = no(nothing computed, bounded, estimated, or evaluated; the
  Fourier/Clifford steps are exact symbolic algebra on sealed forms with sealed
  constants carried verbatim; degree/parity bookkeeping only; machine use limited to
  shasum -a 256 and file reads).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
CANC_PANEL_SYMHUNT_RESULT = SEALED.
```
