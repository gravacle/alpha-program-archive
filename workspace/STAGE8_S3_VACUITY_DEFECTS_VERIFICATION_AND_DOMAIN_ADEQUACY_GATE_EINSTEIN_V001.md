# STAGE8_S3_VACUITY_DEFECTS_VERIFICATION_AND_DOMAIN_ADEQUACY_GATE_EINSTEIN_V001

LANE: EINSTEIN
RELAY: 186 ("THE THREE VACUITY DEFECTS BLOCKING THE S3 JOIN")
DATE: 2026-07-31
REGISTER HEAD AT ISSUE: Q-87 (per the charter). Q-88 through Q-92 had landed by execution time and
were consumed as status only; Q-91 (no lane commits) and Q-92 (lanes may build) are applied below
and their effective-relay discrepancy is recorded in §8.
ROAD JUSTIFICATION (Q-83): ADVANCES STEP 2 — the scale selector. The S3 join is the only known
route by which the `sqrt(2)` fork closes by derivation rather than by choice (Q-87).

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false;
coupling_evaluation_authorized = false; production_authorized = false.
NOTHING WAS COMPUTED. No alpha, kappa_record, kappa_Thomson, coupling, scale, root, eigenvalue,
beta function, E_R, T_R, k_R or absolute interval was produced. No measured-constant comparison.
*** THE MISNER-SHARP / BROWN-YORK FORK WAS NOT RESOLVED, NOT CHOSEN, NOT EVALUATED, NOT RANKED,
AND NEITHER CANDIDATE WAS FAVOURED. *** Three doors through which it could be closed by picking
are NAMED AS HAZARDS in §5.4 and were not taken. S3 was cited and not respecified; the S3 join was
not executed. The `D_BR`/`L_BR` spectral theorem and the producer-algebra class definition were
not used. `a32_holdout/custodian_private/` was not entered, listed, or inferred.

SEAL PROVENANCE OF INPUTS (verified byte-identical, archive workspace vs cleanroom v003):
  S1  STAGE8_BRIDGE_ITEM2_COMPLETE_CTP_ACTION_BOUNDARY_TIMEFLOW_SPEC_V001.md   e6bf47f6…
  S2  STAGE8_BRIDGE_ITEM3_CONSTANT_HJ_RECORD_ENERGY_SPEC_V001.md               ecfbab95…
  S3  STAGE8_C_R_SELECTOR_S3_ENERGY_IDENTIFICATION_JOIN_SPEC_V001.md           fd157d3f…
  S4  STAGE8_C_R_SELECTOR_S4_REFERENCE_CO_PARTICIPATION_SPEC_V001.md           f305e0d7…
S3's hash matches the Q-87 register record at `:3564`.

METHOD: five independent attack tasks, each adversarially verified (run wf_d820479c-790, ten
agents), then one corrected re-specification round with two independent attackers on separate
lenses (run wf_63b68b01-876, three agents). All five verifiers returned WEAKENED with substantive
corrections; every correction is folded below and recorded under this lane's name in §8. Searches
word-boundaried, scopes stated per negative, all sealed text read-only.

---

## 0. LEAD

*** THE REPAIRS ARE NECESSARY AND NOT SUFFICIENT, AND THE INSUFFICIENCY IS NOT MARGINAL: THE THREE
REPAIRS SUPPLY **ZERO** OF THE SIX `Dom(S3_CORE)` MEMBERS. NOT ONE. ***

The charter's second lead governs, and it governs by a wide margin. After all three vacuity
repairs land, the join's core-equality leg still needs six unbuilt domain members (S3 `:773-781`,
every one TYPE-U), and the repairs narrow that count by nothing at all.

```text
 6  Dom(S3_CORE) members still unbuilt after all three repairs   supplied by the repairs: 0
 9  objects for the CORE-EQUALITY leg (those 6 + P13 + core witness + checkers)   supplied: 0
14  the same, counting S4's own four auxiliaries S4.P9-S4.P12 and the S4 witness  supplied: 0
17  adding the NAMED-CLASSIFICATION leg: P9, P12, the classification witness      supplied: 0
```

*** AND THE THING THE PROGRAM SHOULD NOT LEARN LATE: ALL FOURTEEN CORE OBJECTS COULD BE BUILT, THE
CORE LEG COULD PASS COMPLETELY, AND THE `sqrt(2)` FORK WOULD STILL BE OPEN. *** The equality is
provable while the naming stays blocked. `Cod(S3_CORE)` (`:806-820`) contains no named-candidate
output; T13A's non-target column reads *"Never `S3_derived`; never classification"* (`:1090`);
S3 records this itself as an executed TYPE-R at `:1024-1028`. Fork closure lives only at T10A's
exactly-one-match (`:1084`), which consumes P9 and P12 — neither a core input, neither touched by
any repair. The primary bridge gate's condition 4 (`:42-43`) is likewise outside the core leg.

**THE SECOND LEAD, WHICH IS TRUE AND WHICH THE CHARTER RANKS SECOND — AND WHICH MUST BE STATED IN
ITS CORRECTED FORM.** The two vacuity defects and P8's nonemptiness leg **do** share one discharge
object, and **the corpus already names it**: S3's P8,
`S3_COMMON_NONEMPTY_FIRST_DURABLE_DOMAIN_AND_PULLBACK_CERTIFICATE` (S3 `:192-196`). But
**THREE GATES, THREE HOSTS, ONE DISCHARGE OBJECT** is the whole of the finding, and its sufficiency
is **not** established:

```text
one_existence_certificate_sufficient_for_all_three_defect_sites = NO_VERDICT | TYPE-U |
  would-build: the four pullback maps of P8's second half (S3 :194-196), PLUS a derivation
  establishing the inclusion ordering among S1's admitted class, S2's advertised first-durable
  class, and S4's P9 class — no artifact establishes any ordering among the three
```

*** SO THE REGISTER MUST NOT BOOK "THE BLOCKER COUNT DROPS FROM THREE TO ONE." THAT WOULD
UNDERSTATE THE DEBT. *** One candidate object, whose sufficiency for the three sites is itself an
open question gated on four unbuilt pullbacks and an unproved ordering of three textually distinct
classes. Booking the reduction would be drift toward a fit arriving as bookkeeping rather than as
a chosen constant.

**AND A THIRD FINDING THE CHARTER DID NOT ASK FOR, WHICH I LEAD WITH ANYWAY BECAUSE IT IS ABOUT
THE PROGRAM'S NEW BUILD PERMISSION.** See §6: this relay attempted three constructions under Q-92
and **all three were killed by adversarial attack, each reproducing the very defect it was sent to
repair.** They are reported, not repaired.

---

## 1. ANSWER 1 — THE DEFECTS, STATED AT file:line AND VERIFIED INDEPENDENTLY AT SOURCE

*** ALL EIGHT CONFIRMED. NONE REFUTED. NONE REPAIRED. *** Every confirmation is by my own
whole-file search with my own scope stated (§7); "Codex 2 says so" was not accepted as a search at
any point. Codex 2's self-refutation of its own S4 **holds**, and I confirm it more strongly than
Codex 2 argued (§1.4).

### 1.1 The two vacuity defects, with their countermodels executed

**S2.** `S2_stationary_domain_nonempty_gate_found = false | TYPE-S` (S3 `:591-600`);
`S2_constancy_test_nonvacuous_as_written = false | TYPE-R`, test EMPTY-STATIONARY-CLASS-
COUNTERMODEL (S3 `:602-606`). At source, S2's T3 reads:

> *"Given a well-typed HJ record energy, test whether it is constant on the S1 stationary
> record-cell solution class."* (S2 `:249-250`)

reporting to `record_energy_constant_on_stationary_cell` (S2 `:255`), with failure legs:

> *"Failure is TYPE-R only against a supplied S1 package and a supplied constancy certificate.
> Absence of either prerequisite is TYPE-U / NO_VERDICT."* (S2 `:258-259`)

*** THE HOLE IS EXACT: S2 HANDLES A **MISSING** PREREQUISITE AND HAS NO LEG AT ALL FOR A
**SUPPLIED-BUT-EMPTY** CLASS. *** COUNTERMODEL, EXECUTED: supply an S1 package whose declared
stationary class is empty, plus a constancy certificate. Constancy over an empty class is
vacuously true; no failure leg fires; T3 reports PASS to
`record_energy_constant_on_stationary_cell`; a reader books the program's load-bearing constancy
claim. Nothing was established. My independent whole-file search of S2 (293 lines) for
{nonempty, non-empty, not empty, inhabited, at least one, nonvacuous, exists, existence, witness,
instantiate} returns **one** hit, at `:279`, and it concerns S1 existing rather than the domain.

**S4.** `S4_stationary_domain_nonempty_gate_found = false | TYPE-S` (S3 `:637-645`);
`S4_candidate_test_nonvacuous_on_S4_P9_as_written = false | TYPE-R`, test EMPTY-P9-COUNTERMODEL
(S3 `:647-650`). At source, S4.P9 is:

> ```
> S4_admissible_stationary_class_derived = false | TYPE-U |
>   would-build: derive from the completed S1 stationary problem the full class
>   on which reference and sector statements must hold
> ```
> (S4 `:772-774`)

and T8 requires every leg *"to be applicable and PASS over the full declared S1 class"*
(S4 `:976`). COUNTERMODEL, EXECUTED: a derived-empty P9 with otherwise universal certificates —
T2A/T2B (shift invariance, non-erasure), T4 (no-spectator witness search) and T5 (same support)
are all universally quantified or witness-seeking over that class, so none can fire; T0 (`:965`)
and T8 (`:976`) contain no empty-P9 rejection; the composite `S4_candidate_valid_on_declared_S1_class`
returns PASS. Nothing was established.

### 1.2 The remaining six, confirmed

- `S1_gravitating_closure_energy_functional_export_found = false | TYPE-S` (S3 `:548-557`).
  CONFIRMED over three roots (Codex 2 scoped to two files). In S1's 328 lines the token
  `functional` occurs **zero** times, `export` **zero**, `inventory` **zero**, `instance`
  **zero**, `retained`/`excluded`/`partition`/`support` **zero**; `sector` once, inside a failure
  test at `:119`; `gravitat*` twice and both are prohibitions or non-deliveries (`:218`, `:245-246`).
- `S1_spec_contract_sufficient_for_S3_closure_RHS = false | TYPE-R` (S3 `:559-562`). CONFIRMED by
  independent re-execution of S1-TO-S3-CODOMAIN-COMPARISON: a package can satisfy F1-F8 and pass
  **all five** of S1's own tests T1-T5 (`:230-315`) and still leave S3's right-hand side empty.
- `S2_energy_to_S4_reference_application_adapter_found = false | TYPE-S` (S3 `:665-675`).
  CONFIRMED on a wider scope than Codex 2's — see §4.
- `S1_S2_timeflow_to_required_proper_interval_correspondence_found = false | TYPE-S`
  (S3 `:686-696`). CONFIRMED; two hits across S1 and S2, both the standing fence header.
- The two S1-side structural absences at `:183-195` (a stationary class specified, no nonempty
  first-durable class) and `:283-295` (reference slot fixed **or explicitly missing**).

**A FAIRNESS FINDING, RECORDED BECAUSE IT CHANGES WHO OWES WHAT.** *** S1 IS NOT DEFECTIVE
RELATIVE TO ITS OWN DECLARED SCOPE. *** S1 affirmatively disclaims the right-hand side at `:218`
(`record_energy_equals_total_gravitating_energy_derived = false | TYPE-U`) and at `:245-246`
forbids its own tests from reporting to a gravitating-energy flag. **The defect is an INTERFACE
CONTRACT GAP between two correctly-scoped artifacts, not a flaw inside either.** Codex 2's TYPE-R
typing is right; the repair belongs to whoever owns S1's contract, not to S1's author as an error.

### 1.3 Two widenings I found that Codex 2 did not record

1. *** S4.P10 IS UNGUARDED ON THE SAME PATTERN AS S4.P9. *** The sector/support inventory is
   quantified over by T3/T4/T5 (`:971-973`) and by the §4.4 relations (`:833`) with no
   empty-inventory rejection leg. **The defect is one member wider than the record states.**
2. **S2 carries an internal two-class mismatch.** Its would-build advertises constancy *"on the
   first durable-record saddle"* (S2 `:71-73`) while its test quantifies over *"the S1 stationary
   record-cell solution class"* (S2 `:249-250`). Nonemptiness of the latter does not give
   nonemptiness of the former. This is the seed of the ordering problem in §2.3.

### 1.4 Why the S4 self-refutation is more damning than Codex 2 argued

*** THE SAME FILE ALREADY CONTAINS THE ANTI-VACUITY GUARD, APPLIED TWICE AND WITHHELD ONCE. ***

> T1A: *"Require P11 to be nonempty, pre-target, and exhaustive over every admissible reference
> completion… If P11 is absent, return NO_VERDICT; if supplied empty, unsupported,
> downstream-selected, or incomplete, fail inventory validity."* (S4 `:966`)
>
> T2P: *"…if supplied empty, unsupported, or incomplete, fail inventory validity."* (S4 `:968`)

So S4 guards P11's and P12's emptiness explicitly, with the two-row idiom (ABSENT → NO_VERDICT;
SUPPLIED-EMPTY → FAIL) — and gives P9 no such leg. **The concept was present in the document and
was selectively not applied to the domain.** That is not ignorance of vacuity; it is an
incompletely applied discipline, which is a different and more tractable finding: the repair has a
sealed template in the same file.

**AND THE PROGRAM HAD ALREADY TYPED THE SHARED GAP, INSIDE THE ACCUSED FILE, AT A BLOCK NOBODY
CITED:**

> ```
> S1_first_durable_record_saddle_designation_found = false | TYPE-S |
>   roots: primary bridge gate, exact S1 spec, exact S2 spec | … 
>   fences: domain designation is not inferred from the S2 target label |
>   qualifying_evidence_file_list: EMPTY
>
> S2_test_sufficiency_for_advertised_first_durable_domain = NO_VERDICT |
>   reason: the S1-to-first-durable domain-selection interface is unbuilt
> ```
> (S4 `:623-636`)

### 1.5 Are the two the same failure mode? (charter item 1e)

**SAME GENUS, DIFFERENT SPECIES, AND THE DISCRIMINATOR IS THE REPAIR.** The genus is `cannot_fail`
(register `:2690`, `:3573`): a test whose PASS carries no information. The species differ —
universal-quantification-over-a-possibly-empty-set is not the same as a test wired to the wrong
verdict. *** THE DECISIVE ARGUMENT IS THAT THEIR REPAIRS ARE NON-INTERCHANGEABLE AND EACH LEAVES
THE OTHER INTACT: *** adding a domain gate does not fix a mis-wired verdict, and re-wiring a
verdict does not fix an empty domain. Both are live in this relay — §6 records that the second
species appeared inside my own repair.

---

## 2. ANSWER 2 — THE DOMAIN-ADEQUACY GATE: ONE OR THREE?

### 2.1 The ruling

*** THREE GATES, THREE HOSTS, **ONE** DISCHARGE OBJECT — AND THE ONE OBJECT IS SEALED P8, NOT A
NEW OBJECT. SUFFICIENCY OF THAT ONE OBJECT FOR ALL THREE SITES IS `NO_VERDICT | TYPE-U`. ***

The three gates must stay **distinct** — merging them is precisely the Q-69 violation. What they
demand is a single derived object, and Codex 2 already specified it:

> *"P8. `S3_COMMON_NONEMPTY_FIRST_DURABLE_DOMAIN_AND_PULLBACK_CERTIFICATE_derived = false |
> TYPE-U`. Would-build: derive a nonempty first-durable stationary solution domain and exact
> pullbacks showing that S2's energy, S4's reference and sector certificates, and the S1 closure
> functional all apply to that same domain."* (S3 `:192-196`)

P8's stated job — showing that the S1, S2 and S4 objects *"all apply to that same domain"* — is
exactly the three gates' job. **Any lane specifying a fresh certificate here is respecifying P8
under a new name**, which is both a charter breach and a manufactured object. This lane did
exactly that in round 2 and the attack caught it (§6.3).

### 2.2 The Q-69 separation, which is the substance of the answer

*** THREE NODES AT EACH SITE, NEVER TWO, AND NEVER ONE. *** The defect is not that a flag is false.
The defect is that **the middle node is missing at all three sites, so the declaration flag has
been silently doing the existence object's work.**

```text
NODE 1  DECLARATION FLAG   (exists at all three sites; asserts only that a class was NAMED)
   S1  stationary_problem_specified                 :192
   S2  stationary_solution_class_declared           :159
   S4  S4_admissible_stationary_class_derived       :772
NODE 2  THE GATE           (ABSENT at all three; a failure-capable leg that REJECTS an empty
                            or silent domain — it can never MAKE a class inhabited)
NODE 3  EXISTENCE OBJECT   (ABSENT; the derived object that EXHIBITS members and lets a gate PASS)
   = sealed P8, S3 :192-196
```

**S3 ALONE CARRIES THE FULL TRIPLE**, which is the proof that the program already knows this
separation and kept it in only one place: P8 as premise (`:192-196`); the gate as T2A
(`:1073`, verdict `S3_common_domain_valid_and_nonempty`); the object as a codomain slot
(`:811`). *** S1, S2 AND S4 HAVE NODE 1 ONLY. THAT — EXACTLY THAT — IS WHAT THE THREE DEFECTS
ARE. ***

Non-substitution clauses, binding: a gate PASS asserts only that a supplied artifact carried a
valid adequacy certificate; it never asserts that any class is inhabited. Node 3 existing sets no
gate verdict. Neither node may set `record_energy_constant_on_stationary_cell` — that flag is
discharged by S2's constancy proof, a **fourth** and separate object.

### 2.3 Why sufficiency is NO_VERDICT — the ordering nobody established

The three sites do **not** demonstrably quantify over the same class:

| site | the class the host claim quantifies over | at |
|---|---|---|
| S1.F7 | *"the class of stationary solutions/saddles it admits"* | S1 `:185-186` |
| S2.T3 | *"the S1 stationary record-cell solution class"* — while S2's own would-build advertises *"the first durable-record saddle"* | S2 `:249-250` vs `:71-73` |
| S4.P9 | *"the full class on which reference and sector statements must hold"* | S4 `:773-774` |

S4's P9 is defined **by a requirement**, not by narrowing first-durability, and S3 `:633` calls it
*"Its local S4.P9 stationary class"* — the opposite of an inclusion claim. My word-boundaried scan
over all four files for {inclusion, subset, subclass, nested, narrower, contained, coincide},
context-filtered to class/domain/stationary/saddle, returns **zero qualifying hits**. So an
existence exhibit sited at the first-durable class may transport nowhere, and P8's own second half
(the four pullbacks C4→C1, C4→C2, C4→C2′, C4→C3) is TYPE-U. **A single object whose sufficiency
requires four unbuilt maps and an unproved ordering has not reduced three debts to one; it has
re-partitioned them.**

### 2.4 Where the gate belongs, and its Q-92 disposition

Attachment points (named, **not** respecified — the host specs are sealed and unmodified):

| host site | attaches at | and must acquire a **dedicated verdict owner**, which does not exist |
|---|---|---|
| S1 F7 | `:183-195` | `stationary_class_domain_adequacy_verdict_owner_exists = false \| TYPE-U` |
| S2 T3 | `:247-259`, ordered before the constancy test | same, at S2 |
| S4 P9 | `:771-774`, as a sibling of T1A `:966` and T2P `:968`, mandatory in T0 `:965` and T8 `:976` | `S4_admissible_stationary_class_valid_and_complete` — absent |

*** THAT LAST COLUMN IS A FINDING IN ITS OWN RIGHT. *** Every sealed analogue **owns its own
verdict**: T2A owns `S3_common_domain_valid_and_nonempty`, distinct from T0A's
`S3_core_input_applicability` (`:1070`); T1A and T2P own their inventory-validity verdicts,
both distinct from T0's `S4_domain_applicability` (`:965`). The corpus separates *applicability*
from *domain-validity* twice, in two files, deliberately (S3 `:1059-1062`; S4 `:955-958`). A gate
routed into an existing foreign verdict violates that separation — and at S2 it would be worse
than a violation: routing a certificate-hygiene FAIL conjunctively into
`record_energy_constant_on_stationary_cell` would convert a bookkeeping defect into a **TYPE-R
refutation of a physical constancy claim**, which S3 `:1059-1060` and S4 `:956-957` forbid
explicitly. **That is the mirror hazard of vacuity — a fabricated negative — and it is named here
so a future builder does not walk into it.**

**Q-92 DISPOSITION: SPECIFIED, NOT CONSTRUCTED.** `domain_adequacy_gate_derived = false | TYPE-U`.

Condition (c) fails: every decisive branch of any such gate consumes an object that does not
exist — S1 `:323`, S2 `:288`, S4 `:739` are all TYPE-U, verified at source. Condition (b) **also**
fails while no dedicated verdict owner exists at any of the three sites, since Q-92(b) requires
tests *"WIRED to the verdicts they test"* (register `:3785`) and naming a foreign identifier a leg
blocks is not that. **The gate is two conditions short of construction, not one.** Per Q-92's own
clause — *"If (c) fails, specify and name what is missing — that is still the right answer and not
a lesser one"* — this is the answer, and §6 records what happened when this lane tried to do
better.

**WHAT A SOUND GATE MUST CONTAIN**, established by the two rounds of attack and drawn entirely
from sealed corpus idiom rather than from any outside import:

1. A **certificate-validity** leg. It must never accept the discharge object on the strength of
   its type label, nor a class on the strength of its designator. Both name-match surfaces killed
   a construction this relay.
2. A **missing-pullback** fail leg and an **exhaustiveness** fail leg — both sealed in T2A
   (`:1073`), both dropped by round 1.
3. Two **separate rows**, per S4 T1A/T2P: SUPPLIED-AND-EMPTY (including derived-empty) → FAIL;
   ABSENT → NO_VERDICT. Round 1 collapsed them.
4. **PASS unreachable while the certificate is absent** (S1 `:261-266`; S2 `:258-259`; S4 `:965`;
   S3 `:1059-1060`). This is the Q-69-correct shape: the gate *consumes* the certificate as a
   prerequisite and returns NO_VERDICT in its absence.
5. **Domain adequacy, not mere inhabitance.** A singleton must not stand in for a declared class —
   S2's F4 already penalizes *"constancy … proved only for an example or endpoint rather than the
   declared stationary class"* (S2 `:159-161`).
6. **No ordering assumption**, and a transport obligation **between the gate's PASS and the
   quantifier of the verdict it discharges** — not only between the certificate and the host
   field. Round 2 died in that second gap.

---

## 3. ANSWER 3 — WHAT S1's CONTRACT MUST EXPORT

**THE MISSING CLAUSE, NAMED:** `S1_CLOSURE_CONSUMED_ENERGY_FUNCTIONAL_EXPORT_CLAUSE`.

It is an **export clause of S1's contract** — a normative field requirement of the same class as
S1's existing F1-F8 — and it is a distinct name from every flag and from P11. S1 is sealed and was
not modified; the clause is reported as a named missing requirement, not inserted.

**WHAT IT MUST SAY** (signature slots only — no formula is written, and no named candidate is or
may be used to discharge it):

- **DOMAIN.** A configuration-indexed functional whose domain is the same package's own admitted
  stationary configuration class and the same configuration variable over which that package's
  on-shell CTP action difference (S1 `:87-88`) ranges; **total** on that class — not at one
  distinguished configuration, not at an endpoint, not on a later-chosen sub-class.
- **CODOMAIN.** The energy argument slot the gravitational closure condition consumes, with a
  **role certificate** that the closure condition consumes *this* export, in S3's sense at `:901`
  and `:904-905` (*"a role certificate, not a numerical use of the closure condition"*). The
  codomain inherits S1's output fence verbatim (`:88-89`): not a value, interval, response, or
  stiffness. It must be declared so that an extensional identity statement against a
  reference-fixed HJ branch-energy functional is **well-formed** — typing is not proving, and the
  identity itself is S3's O4 (`:511-512`), owned by T7B (`:1079`).
- **PROVENANCE.** Bound to the same S1 package instance that carries the action; pre-target;
  excluded from discharge by a different action, a partial action, a finite-cell diagnostic, or an
  adopted marginal public-closure rule.
- **SUPPORT.** Exposed against S4's retained/excluded partition, so that a contribution S4 retains
  appears as an argument.
- **CANDIDATE-NEUTRALITY, BINDING.** *** THE CLAUSE MUST NOT BE DISCHARGEABLE BY A NAMED CANDIDATE
  FORMULA. *** S3 `:217-218` forbids exactly that, and §5.4 records why this clause is the single
  most dangerous surface in the relay.

**ANTI-VACUITY, SEPARATED UNDER Q-69:** the clause must **not** absorb nonemptiness. That belongs
to the gate and to P8. A clause that quietly supplied nonemptiness would let a package pass by
declaration what must be proved by existence. **But the converse trap is real and this lane fell
into it (§6.2): a clause that is silent about nonemptiness passes vacuously on an empty class.**
The sealed resolution is neither absorption nor silence — it is the third option the corpus uses
four times: **consume the adequacy certificate as a prerequisite and return NO_VERDICT in its
absence, never PASS.**

**THREE OBJECTS, NOT ONE (Q-69).** The export clause (a requirement on S1's contract) ≠ **P11**
(the derivation act, S3 `:213-217`) ≠ **the functional itself** (`Dom(S3_CORE)` slot 6, S3 `:780`;
`E_closure` at `:899`). *** DISCHARGING THE CLAUSE DOES NOT DISCHARGE P11 — IT ONLY MAKES P11
ATTACHABLE. *** Naming the clause does not require the functional to exist; **executing** any
conformance test for it does, and that was not done and is not claimed.

---

## 4. ANSWER 4 — THE S2/S4 REFERENCE-APPLICATION ADAPTER

*** RULING: A MISSING OBJECT, NOT AN UNSEPARATED ASPECT. NO NEW CLASS WARRANTED. ***
`S2_energy_to_S4_reference_application_adapter_found = false | TYPE-S` — CONFIRMED on a wider scope
than Codex 2 used (all three roots, not three files). Word-boundaried `adapter` occurs in
**exactly one file in the entire archive workspace — the S3 spec itself** (`:22`, `:492`, `:668`,
`:670`, `:705`, `:706`, `:717`, `:1075`, `:1238`), plus its byte-identical cleanroom mirror, plus
re-reports in the supervision lane. A full identifier inventory of every reference-bearing name
returns exactly one application-shaped identifier, `_REFERENCE_APPLICATION_ADAPTER_` (S3 `:207`) —
**the name of the missing object, not an object.**

**ALL THREE "UNSEPARATED ASPECT" HORNS FAIL, AND EACH FAILS FOR A DIFFERENT REASON — WHICH IS
ITSELF THE FINDING: THE THREE SPECS DO NOT MERELY OMIT THE ADAPTER, EACH ACTIVELY FENCES IT OUT.**

- **Not an aspect of S1.** S1 permits its reference slot to be fixed **or explicitly missing**
  (`:283-295`) — a slot is not a map that fills it.
- **Not an aspect of S4.** S4 derives a forced reference from the reference-free S1 core and stops:
  *"It does not choose a reference functional, prove a sector theorem, or instantiate a closure
  energy."* (S4 `:886-887`).
- **Not an aspect of S2 — and the charter's own third horn is false at source.** The framing that
  S2 "fixes its energy against an S1 reference whose provenance it never binds" does not hold: S2
  contains exactly two occurrences of `reference` (`:195`, `:213`) and **both are prohibitions**.
  S2 is reference-**silent**, and therefore silently inherits whatever reference term S1's action
  already carries inside F4's boundary sector. **That is worse in one specific way: when the
  adapter is built it must not double-subtract.**

**CODEX 2's T7 EXCLUSION IS UPHELD, AND ITS STATED REASON CORRECTED.** Codex 2 excluded S4.T7 as a
*"future S4.T7 promise"* (S3 `:667`). T7 (`:975`) in fact **consumes a candidate S3 interface** —
it cannot supply the adapter, so the exclusion is right in outcome and **wrong in kind**.

**A STRENGTHENING, AND A NEW NEGATIVE.** The sealed depth report enumerates S3's additional leaves
as exactly two and assigns the baseline/reference-subtraction leaf to S4
(`STAGE8_BRIDGE_ITEM4_SIX_OBJECT_DEPTH_REPORT_V001.md:171`, `:212-215`), with zero
adapter/application hits in the whole file. **So this is not merely "not found in three specs" —
it is an obligation the sealed leaf enumeration dropped**, while the primary gate's clause 2
requires it: *"the CTP branch-energy difference equals the complete gravitating cell energy after
one fixed reference subtraction"* (gate `:38-39`). The diagnosis is that **leaf enumeration by
node cannot see obligations that live on edges** — recorded as a diagnosis, **not** constituted as
a Q-80 class. Separately: `reference_free_S1_core_defined = false | TYPE-S` — the phrase occurs
five times across three roots (S4 `:223`, `:783`, `:966`, `:967`; S3 `:660`) and is **never
defined**.

**NO NEW CLASS.** The object fits TYPE-U cleanly: it has a name, a would-build at S3 `:207-211`,
and no builder. Q-80 fires when the categories are wrong; here they are not. **I decline to define
a class whose only candidate member I have just ruled a non-member** — a manufactured class is as
bad as a forced fit.

**Q-92: SPECIFIED ONLY.** Condition (c) fails on all three prerequisites — S2's energy object
(`:288`), S4's certificate (`:739`), and S4's forced-reference prescription (`:781-786`) are all
TYPE-U.

---

## 5. ANSWER 5 — COULD THE JOIN EXECUTE AFTER THE REPAIRS?

### 5.1 No. The route, walked

| link | consumes | sealed status | supplied by the three repairs? |
|---|---|---|---|
| same S1 origin | the S1 package object | `false \| TYPE-U` (S1 `:323`) | **no** |
| S2 constant HJ energy | the S2 certificate | `false \| TYPE-U` (S2 `:288`) | **no** |
| apply S4's reference via P10 | the adapter | `false \| TYPE-U` (S3 `:207-211`) | **no** — the repair names the clause; it does not build P10 |
| closure energy via P11 | the functional derived from S1 | `false \| TYPE-U` (S3 `:213-217`) | **no** — the export clause makes P11 *attachable*, not discharged |
| full-domain identity | P8's common domain + pullbacks | `false \| TYPE-U` (S3 `:192-196`) | **no** — the gate demands P8; it cannot supply it |
| representation invariance | P13 | `false \| TYPE-U` (S3 `:229-235`) | **no** |
| time correspondence | P9 | `false \| TYPE-U` (S3 `:198-205`) | **no** |
| candidate signatures | P12 | `false \| TYPE-U` (S3 `:219-227`) | **no** |

### 5.2 Necessity and sufficiency, indexed — because the unindexed question misleads

Necessity and sufficiency are **imports** from logic and dependency analysis; they are indexed to a
construction goal, and the three repairs have no construction goal — their entire product is
failure-capability. So the trichotomy is given indexed rather than forced into one verdict:

- **Index A — for S2's and S4's own verdicts, and as build contracts:** all three are **(ii)
  NECESSARY, NOT SUFFICIENT**. Without them the specs would be built as objects that record a
  clean-looking pass on an empty domain, and S1's contract would be built without the export P11
  consumes.
- **Index B — for the S3 join's *correctness*:** all three are **(iii) NOT EVEN NECESSARY**. S3
  independently re-gates every one of them at its own boundary — T2A (`:1073`), T3 (`:1074`), T4
  (`:1075`, *"exactly S4's forced prescription"* with *"no additional reference/subtraction"*), T5
  (`:1076`). A vacuous or export-less upstream object **fails at the join** rather than passing
  through it. *** HAZARD, STATED SO IT CANNOT BE MISREAD: "not necessary for S3 correctness" MUST
  NEVER BECOME "make the repair cheap because S3 catches it anyway." Weakening S2's or S4's own
  test is the exact defect under repair. The correct reading is the opposite: the repairs cost the
  program nothing in progress terms and buy soundness. ***
- **Index C — sufficiency for the join to execute:** REFUTED under every index; zero of six.
- **Index D — sufficiency for closing the fork:** REFUTED twice over (§5.3).

### 5.3 The core leg does not close the fork

Even a complete core-equality leg leaves the `sqrt(2)` fork open — `Cod(S3_CORE)` (`:806-820`)
carries no candidate identifier; T13A cannot name a candidate (`:1090`, `:1113-1115`); S3 records
this itself at `:1024-1028`. **This is not new, but it is now quantified: the equality is provable
while the naming stays blocked, and the naming leg needs P9, P12 and the classification witness —
none of which any repair touches.**

### 5.4 Three doors through which the fork could be closed by picking — named, not taken

1. **The S1 export clause creates a slot** into which an adopted closure-side formula (i.e. either
   named candidate) could be dropped, making the equality provable **and** naming the energy in one
   move. S3 `:217-218` forbids exactly this; the clause's candidate-neutrality requirement (§3)
   exists for this reason alone.
2. **P9's time-parameter normalization.** The gate (`:74-79`) records that the two candidates are
   conjugate to **different** boundary/time choices — so choosing the time normalization *is*
   choosing the fork, wearing a conjugacy costume.
3. **P10's reference subtraction.** The gate (`:62-64`) shows the candidate ratio is generated by
   the reference subtraction itself — an unforced reference inside the adapter is a fork pick.

*** THE FORK IS EXPOSED TO A PICK THROUGH AT LEAST THREE SEPARATE DOORS, AND EVERY ONE OF THEM IS
AN OBJECT THE REPAIRS DO NOT BUILD BUT DO MAKE ATTACHABLE. That is the strongest argument for
building them carefully rather than quickly. ***

---

## 6. THE CONSTRUCTION RECORD — THREE ATTEMPTS UNDER Q-92, THREE KILLS

*** THIS RELAY IS THE FIRST TO WORK UNDER Q-92's BUILD PERMISSION. IT ATTEMPTED THREE
CONSTRUCTIONS. ADVERSARIAL ATTACK KILLED ALL THREE, AND EACH DIED BY REPRODUCING THE DEFECT IT WAS
SENT TO REPAIR. They are reported here, not repaired. ***

**6.1 Gate v1 — killed by UNVALIDATED-CERTIFICATE-COUNTERMODEL.** Its only PASS row fired on
*"EXHIBITED, membership proof checked valid"*, where EXHIBITED meant *a handle to an instance of*
the certificate. An artifact carrying a handle to an object merely **labelled** with the
certificate's name — pullback half absent — plus a valid membership proof, hit no FAIL row and
returned PASS. **The gate accepted the object by its name**: same-token name-matching compiled
into a pass condition. It had also dropped T2A's missing-pullback and exhaustiveness legs, making
it a strictly weaker copy of the sealed gate it claimed to generalise, and it claimed
`derived = true` on meta-tests that were **reasoned, not run**.

**6.2 The S1 export clause v1 — killed by XC1-EMPTY-CLASS-EMPTY-INVENTORY-COUNTERMODEL.** A package
declaring an **empty** admitted stationary class and an **empty** contribution inventory, exporting
the empty function with an identifier and a role-certificate declaration, passed **every**
sub-clause: totality on ∅ is vacuous, neither stated failure leg can fire, and the remaining
clauses are presence checks. Such a package also passes all of S1's F1-F8 and T1-T5, since F7's
failure test fires on a **missing** class and never on an **empty** one.

**6.3 Gate v2 — killed at the class designator, one level up from where v1 died.** v2 genuinely
closed v1's hole (I could not resurrect the type-name trap, and PASS is unreachable on an absent
certificate). It died anyway, on two independent lenses:

- **Vacuity lens: DEAD.** Its PASS row matched the certificate's certified extension to the host's
  class **by syntactic designator** — the sixth strike relocated from the certificate's type name
  to the class name.
- **Wiring lens: WEAKENED, fatally for sealing.** Its PASS discharges host verdicts quantified over
  **different class expressions** than the one it checked — S4 `:976`'s *"the full declared S1
  class"* versus P9's class at `:773-774`; S2 `:255`'s flag advertised over the first durable-record
  saddle versus T3's class at `:249-250` — with the ordering among them typed absent by my own
  scan. *** THE GATE TRANSPORTED ITS OWN PASS ACROSS AN UNPROVED CLASS IDENTITY: THE DEFECT IT WAS
  BUILT TO BLOCK, RELOCATED INTO ITS OWN WIRING. *** It also re-issued sealed P8 under a new name
  (§2.1) and rested its central defence on an **undeclared import** (checker name-blindness, which
  is software-testing practice and appears nowhere in the corpus — Q-80 requires imports to be
  named).

**WHAT THIS IS EVIDENCE FOR, STATED CAREFULLY.** Three authors — Codex 2 in S4, and this lane
twice — produced the same class of defect independently, and in this relay two of the three were
produced **while explicitly hunting it**. *** THAT IS EVIDENCE THAT THE DEFECT IS STRUCTURAL TO
THIS CORPUS'S TEST IDIOM RATHER THAN A LAPSE BY ANY AUTHOR: universally-quantified certificate
clauses are the corpus's default form, and every one of them is vacuous until something forces its
domain to be inhabited and adequate. *** It also carries a direct lesson for Q-92: **the first
relay under the build permission shows that "constructed" is cheap to claim and expensive to
earn**, and that the thing which caught all three failures was not the construction discipline but
the **adversarial countermodel**. A build rule without an attack rule would have sealed three
defective objects today.

**NO Q-80 CLASS IS CONSTITUTED FOR ANY OF THIS.** Round 1 invented `CONSTRUCTED_INERT_GUARD` to
protect a `derived = true`; it is struck as manufactured — Q-92 already has the cell
(*"specify and name what is missing"*), and inventing a category to escape an inconvenient verdict
is the failure mode Q-80's second direction names.

---

## 7. TYPED NEGATIVES (Q-54)

- **N1 [TYPE-R, executed]** `S2_constancy_test_nonvacuous_as_written = false`. Test:
  EMPTY-STATIONARY-CLASS-COUNTERMODEL, re-executed by me against S2's full battery F1-F7/T1-T4. No
  leg blocks it; T3 reports PASS.
- **N2 [TYPE-R, executed]** `S4_candidate_test_nonvacuous_on_S4_P9_as_written = false`. Test:
  EMPTY-P9-COUNTERMODEL, re-executed against S4's full table T-1..T8. No leg blocks it.
- **N3 [TYPE-R, executed]** `S1_spec_contract_sufficient_for_S3_closure_RHS = false`. Test:
  S1-TO-S3-CODOMAIN-COMPARISON, re-executed.
- **N4 [TYPE-S]** `S1_gravitating_closure_energy_functional_export_found = false`. Roots: three
  declared roots. Query (word-boundaried): functional, export, closure energy, gravitating, tuple,
  Output, inventory, instance, retained, excluded, partition, support, sector, `must export`.
  Files: S1 in full (328 lines) plus all three roots for alternate carriers. Exclusions: prose
  completeness requirements, future-use flags, named closure-side candidates. Qualifying list:
  EMPTY. (`must export` returns zero files in all three roots.)
- **N5 [TYPE-S]** `S2_stationary_domain_nonempty_gate_found = false`. Roots: S1, S2 in full. Query:
  nonempty, non-empty, not empty, inhabited, at least one, nonvacuous, vacuous, exists, existence,
  witness, instantiate, first durable. Hits: one (S2 `:279`), concerning S1's existence, not the
  domain. Qualifying list: EMPTY.
- **N6 [TYPE-S]** `S4_stationary_domain_nonempty_gate_found = false`. Roots: S4 in full. Query as
  N5. Hits: `:229`, `:790`, `:966` (P11), `:968` (P12) — **none attached to P9**. Qualifying list:
  EMPTY.
- **N7 [TYPE-S]** `S2_energy_to_S4_reference_application_adapter_found = false`. Roots: all three.
  Query: adapter, apply, application, map, reference-fixed, reference-subtracted, baseline
  subtraction, reference-free core, plus a full identifier inventory of reference-bearing names.
  Qualifying list: EMPTY. Exclusions: the shared token `reference`; S4.T7 (excluded, and Codex 2's
  reason corrected — see §4); flags; the S3 spec itself.
- **N8 [TYPE-S]** `S1_S2_timeflow_to_required_proper_interval_correspondence_found = false`. Roots:
  primary gate, S1, S2. Hits: two, both the standing fence header. Qualifying list: EMPTY.
- **N9 [TYPE-S, MINE, NEW]** `S1_admitted_to_S2_first_durable_to_S4_P9_class_ordering_established
  = false`. Roots: S1, S2, S3, S4. Query: inclusion, subset, subclass, nested, narrower, contained,
  coincide — context-filtered to class/domain/stationary/saddle. Raw hits: two in S4 (`:866`
  support correspondence; `:1059` roster membership), both non-qualifying. Qualifying list: EMPTY.
  **This is the negative that makes §2.1's sufficiency NO_VERDICT.**
- **N10 [TYPE-S, MINE, NEW]** `reference_free_S1_core_defined = false`. Roots: all three. Five
  uses (S4 `:223`, `:783`, `:966`, `:967`; S3 `:660`), zero definitions.
- **N11 [TYPE-S, MINE, NEW]** `S4_P10_sector_support_inventory_nonempty_gate_found = false`. Roots:
  S4 in full. The sector/support inventory is quantified over by T3/T4/T5 (`:971-973`) and §4.4
  (`:833`) with no empty-inventory rejection leg. **The vacuity defect is one member wider than
  Codex 2 recorded.**
- **N12 [TYPE-U ×3, MINE, NEW]** `stationary_class_domain_adequacy_verdict_owner_exists = false` at
  S1, at S2, and at S4. Would-build: a dedicated per-prerequisite verdict at each site, on the
  sealed pattern of `S3_common_domain_valid_and_nonempty` (`:1073`) and S4's `:966`/`:968` owners.
  **Q-92(b) fails for any gate until these exist.**
- **N13 [TYPE-U, carried]** `S3_COMMON_NONEMPTY_FIRST_DURABLE_DOMAIN_AND_PULLBACK_CERTIFICATE_derived
  = false` (S3 `:192-196`), together with S1 `:323`, S2 `:288`, S4 `:739`, and P9/P10/P11/P12/P13.
  Unaltered by this artifact.
- **N14 [NO_VERDICT]** `one_existence_certificate_sufficient_for_all_three_defect_sites`. Reason:
  information missing — the four P8 pullbacks and the class ordering (N9). **NO_VERDICT here means
  MISSING INFORMATION, not wrong categories; no class is invented for it.**
- **N15 [TYPE-R, executed, MINE]** `gate_v1_nonvacuous_as_written = false`;
  `S1_export_clause_v1_nonvacuous_as_written = false`; `gate_v2_pass_row_name_blind = false`. Tests
  named in §6. **These are refutations of this lane's own constructions, reported and not
  repaired.**
- **N16 [TYPE-C]** `codex_2_defect_refuted = false`. All eight accusations were checked at the
  accused specs' own lines by whole-file search under spellings Codex 2 did not use. **Not one was
  refuted.** Release: a sealed clause supplying any of the allegedly-absent obligations.
- **N17 [TYPE-U]** `S3_join_executable_after_the_three_repairs = false`. Would-build: the six
  `Dom(S3_CORE)` members (S3 `:773-781`). Supplied by the repairs: zero.

---

## 8. CORRECTIONS FOLDED, UNDER THIS LANE'S NAME

1. **`ONE_OBJECT` downgraded** to one candidate object with sufficiency `NO_VERDICT | TYPE-U`
   (§2.1, §2.3). The register must not book a three-to-one blocker reduction.
2. **Both v1 constructions and the v2 gate struck** (§6); `derived = true` withdrawn everywhere;
   the correct disposition is SPECIFIED under Q-92's own clause.
3. **`CONSTRUCTED_INERT_GUARD` struck** as a manufactured Q-80 class; **`FALSIFIABILITY_RESTORING_REPAIR`
   struck** as well — it rested on a road taxonomy ("prerequisite / blocker / link") that does not
   exist in the register; the sealed taxonomy is Q-83's `ADVANCES STEP n` / `UNBLOCKS`, and the
   three repairs fit `UNBLOCKS` cleanly.
4. **The shared object is sealed P8, not a new certificate** — a lane-coined replacement was
   respecification of Codex 2's object and is withdrawn.
5. **A fabricated-negative hazard added** (§2.4): a certificate-hygiene FAIL must never be routed
   conjunctively into a physical constancy verdict (S3 `:1059-1060`; S4 `:956-957`).
6. **Citation repairs:** T2P (`:968`) does **not** contain "downstream-selected" — only T1A
   (`:966`) does; S3 `:585` cites the S4 first-durable audit at `:598-635` while the block runs
   `:606-637` (flags at `:623-636`); one round-1 evidence entry carried a fabricated `"Method:"`
   prefix and one gate quote was cited six lines off — both struck rather than corrected in place.
7. **Q-92 currency discrepancy, recorded not resolved:** register `:3781` dates Q-92 *"effective
   relay 188"* while this relay's charter states it is in force now. **The answer is SPECIFIED
   under both readings**, so nothing here turns on it; the discrepancy is flagged for the reviewer.

---

## 9. DISCIPLINE

- **Q-52:** nothing discharged, nothing declared derived. Every specified object carries
  `derived = false`.
- **Q-54:** every negative typed, with roots, query, exclusions and candidate files. `NO_VERDICT`
  used twice, both times for missing information, never as a disposal.
- **Q-69:** the three-node separation (declaration flag / gate / existence object) is maintained in
  every sentence; no flag is identified with its discharge object; no gate is permitted to stand in
  for a certificate.
- **Q-80:** no class constituted. Two candidate classes proposed during the work were struck as
  manufactured (§8.3). Imports named: *necessity/sufficiency* (logic and dependency analysis —
  applies only when indexed to a construction goal, §5.2); *checker name-blindness* (software
  testing — **undeclared in round 2, named here, and not relied on**).
- **Q-83:** ADVANCES STEP 2. This relay completes no step: it removes a false credit and names what
  is missing.
- **Q-91:** no `git` command of any kind was run. The baseline was not touched. `deploy_status.sh`
  was not run. No publication authorization is requested.
- **Q-92:** nothing reported constructed; the two failing conditions are named per object.
- **NO TEST WAS WEAKENED.** Every guard whose absence is confirmed above would **strictly shrink**
  a PASS set; none was applied, and no host failure leg was removed, relaxed, or given a
  disjunctive satisfaction route.

alpha_computed = false; proof_authorized = false; kappa_record_computed = false.
