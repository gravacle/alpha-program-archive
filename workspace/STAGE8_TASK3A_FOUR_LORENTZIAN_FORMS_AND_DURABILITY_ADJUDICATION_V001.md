# Stage 8 Task 3a: Four Lorentzian Forms and Durability Adjudication v001

Date: 2026-08-01

## Status and gates

```text
ARTIFACT_STATUS = SOURCE_LEVEL_ADJUDICATION
REGISTER_HEAD_CONSULTED = Q-209
NEWER_BEARING_RULING_CONSULTED = false | TYPE-S |
  roots: alpha_supervision register through Q-209 and the sealed sources listed in Section 8 |
  excl: rulings after Q-209, a32_holdout/custodian_private, external material |
  fences: source-level only; no construction or response evaluation |
  query: exact Q-209 lineage and source-object identifiers

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No premise beyond the sealed source stack is added. This artifact does not
construct a Lorentzian action, CTP kernel, closure potential, state, dynamics,
quotient, measure, or durability theorem.

## 1. Result first

Two premises of the question do not survive source-level type checking.

First, the four listed Lorentzian forms are not four peer candidates for one
slot. They are a **LAYER-MIXED ENUMERATION**:

```text
LAYER-MIXED ENUMERATION:
  a list whose members occupy different input, complete-functional, and
  derivative/kernel levels, so selection among the members is ill-typed.
```

The sealed root principle specifies the complete object as a doubled complex
CTP functional. A single-branch amplitude can supply its evolution input, and
the coherent kinetic and dissipative/noise kernels are different derivatives
or components of that one functional. The formal output architecture is
specified; its complete common-origin microscopic instance remains unbuilt.

Second, the continuous phase orbit of the two trial closure potentials is not
identified with the declared vector `U(1)_rel`. The potentials are explicitly
vector-`U(1)_rel` neutral, and a passive record-basis rephase expressly does not
set the closure component `b_R` to zero. Therefore the existing physical gauge
quotient does not remove this orbit. The isolated-background failure in Q-209
stands.

Durability is a separate operational condition. Sealed text defines it by
thresholded source nonreturn plus exact completed-record persistence, with
recoverable quasi-local public content. It does not define durability as
lifting, quotienting, or boundary-pinning the closure-field phase orbit.

```text
four_forms_are_peer_complete_counterparts = false | TYPE-R |
  test: compare each form's domain and role with the complete CTP functional
        and its response/noise decomposition

complete_CTP_output_architecture_specified = true
complete_common_origin_CTP_instance_constructed = false | TYPE-U |
  would-build: one complete microscopic operator supplying U_BR, rho_pre,
               effects/domains, physical quotient/measure, and mixed response,
               noise, and record overlap in one provenance chain

trial_continuous_phase_orbit_is_declared_vector_U1_rel_gauge_orbit = false | TYPE-R |
  test: compare the trial potentials' declared transformation type with the
        sealed vector-U(1)_rel charge and passive-rephase statements

durability_requires_resolution_of_trial_phase_orbit = false | TYPE-R |
  test: compare the exact durability definition with the isolated-background
        reopen condition
```

## 2. The four-form adjudication

### 2.1 The complete source-level object

`primitive_complete_boundary_transition_functional_principle_v002.md:5-11`
corrects the attenuation-only v001 object precisely because a pure phase can
have unit modulus while carrying a nonzero Maxwell kinetic term. It says the
complete complex CTP functional must be retained and assigns attenuation and
coherent response to different derivatives of that one object.

The same source defines (`:18-35`):

```text
Z_r[A_+,g_+;A_-,g_-]
  = Tr(E_r U_BR[A_+,g_+] rho_pre U_BR[A_-,g_-]^dagger)
    / Tr(E_r U_BR[0,g_0] rho_pre U_BR[0,g_0]^dagger),

Gamma_r = -i log Z_r.
```

Its quadratic decomposition is (`:39-65`):

```text
Gamma_r
  = integral A_delta D_R^(-1) A_c
    + (i/2) integral A_delta N A_delta
    + higher terms.
```

Here `D_R^(-1)` is the coherent retarded response and its local transverse
part is the Maxwell-stiffness slot; positive `N` controls attenuation and
record distinguishability. The source expressly says `-log|Z_r|` is record/noise
information and is not by itself the Maxwell action.

The independent complete-history spec agrees. It defines the algebraic
Schwinger-Keldysh/influence functional

```text
Z_K[A_+,A_-] = omega_in(W_K[A_-]^dagger W_K[A_+])
```

at `COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md:28-48`, and
requires diagonal normalization, CTP reality, contractivity, common gauge
covariance/invariance, and disjoint factorization at `:50-71`.

This corrects, but does not erase, the narrower one-cell statement.
`ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md:97-111` correctly says the
one-cell probability-level fidelity alone does not distinguish the four
meanings. It does not say that the whole sealed corpus lacks a complete-output
type. Q-209 transported that local insufficiency into a corpus-wide
nonselection claim at
`STAGE8_TASK3A_GAMMA_REST_RECORD_CONTENT_AT_SOURCE_DETERMINATION_V001.md:163-201`.
That transport is refuted by the complete CTP sources above.

The source-level positive is nevertheless formal, not a complete instance.
The root principle's own hard gates at
`primitive_complete_boundary_transition_functional_principle_v002.md:106-125`
leave `complete_transfer_operator_constructed=false`, and require all inputs
and kernels to descend from one complete microscopic operator. The later
inventory therefore types the object as `FLAGGED_ORIGIN_CLAIM`
(`STAGE8_PRIMITIVE_INVENTORY_CORPUS_WIDE_EXHAUSTIVENESS_AUDIT_V001.md:49-59`),
and the bounded-positive audit types its `*_derived=true` flag only as a
`trace-form partial`
(`STAGE8_CTP_PHYS_INPUT_PACKAGE_ITEM3_P_ROW_DEPTH_AND_DERIVED_FLOOR_AUDIT_V001.md:335-350`).

### 2.2 Form-by-form result

| Listed form | Sealed role | Failure-capable test | Result |
|---|---|---|---|
| Single-branch amplitude action | Possible upstream evolution input. The complete object uses both `U_BR[A_+]` and `U_BR[A_-]^dagger`. | It cannot alone satisfy two-history CTP reality, diagonal normalization, common gauge covariance, or record conditioning. | Killed **as the complete counterpart**, not as an input. |
| Doubled CTP probability/influence action | The specified complete output architecture: one complex functional of two histories. | It must satisfy the exact CTP identities and common-origin hard gates. | Survives as the required architecture; its physical instance is unbuilt. |
| Dissipative/noise kernel | The positive difference-history kernel `N`, controlling attenuation and distinguishability. | Alone it omits the mixed retarded kernel and coherent response. Positivity constrains it but does not generate the rest of the functional. | Killed **as the complete counterpart**, retained as a required/allowed component. |
| Real unitary Maxwell kinetic action | The coherent local transverse part of `D_R^(-1)`. | Alone it omits record probability, noise, branch structure, effects, and the complete CTP identities. | Killed **as the complete counterpart**, retained as the coherent response component. |

Typed statements:

```text
single_branch_amplitude_is_complete_record_counterpart = false | TYPE-R |
  test: complete two-history identity and conditioning requirements

doubled_CTP_influence_functional_is_specified_complete_output_type = true
doubled_CTP_influence_functional_common_origin_instance_derived = false | TYPE-U |
  would-build: the hard-gate package in Section 2.1

noise_kernel_alone_is_complete_record_counterpart = false | TYPE-R |
  test: absence of the coherent mixed retarded kernel

unitary_Maxwell_kinetic_action_alone_is_complete_record_counterpart = false | TYPE-R |
  test: absence of branch, probability/effect, and noise data

single_branch_amplitude_as_CTP_input_excluded = false | TYPE-S |
  roots: sources listed in Section 8 |
  excl: unsealed microscopic completions and post-Q-209 rulings |
  fences: no construction |
  query: exact single-branch, U_BR, W_K, amplitude-action occurrences

noise_kernel_as_CTP_component_excluded = false | TYPE-R |
  test: root v002 explicitly includes positive N in Gamma_r

unitary_kinetic_response_as_CTP_component_excluded = false | TYPE-R |
  test: root v002 explicitly includes D_R^(-1) and explains why unit-modulus
        phase can carry coherent Maxwell response
```

### 2.3 Does irreversibility kill the unitary form?

No. Sealed text makes the finite write reversible and separates it from
durability. `PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md:67-73` says a
reversible endpoint write establishes distinguishability but not persistence;
a later closure map, invariant record sector, or environmental amplification
is additionally required. `CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md:10-30`
then locates durability in source nonreturn and completed-record persistence.

Therefore irreversibility does not refute a coherent/unitary kernel inside the
complete CTP functional. It refutes only the inference that a reversible
kinetic or endpoint-write term by itself is a durable record.

```text
unitarity_is_incompatible_with_durable_record_architecture = false | TYPE-R |
  test: sealed architecture composes reversible write with nonreturn and
        invariant completed-record persistence
unitary_term_alone_establishes_durability = false | TYPE-R |
  test: reversible-write durability boundary
```

## 3. What durability actually requires

The current successor definition is exact. Within its declared ordinary
`3+1` flat-asymptotic and causally sequential scope,
`CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md:10-30` says:

```text
durability = thresholded source nonreturn
             + exact completed-record persistence;
public content = recoverable quasi-local record state and central sequence.
```

The parent result supplies the spectral half from a free asymptotic source
tail: Fourier multiplication and coarea give an absolutely continuous `L1`
spectral density and no source point spectrum, so Riemann-Lebesgue gives
thresholded source-root nonreturn
(`R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md:107-124`).
It separately identifies the record label as an invariant public sector.

The finite primitive durability result states that completed pointer
observables are not rewritten by later primitive events
(`R3_4_CAUSAL_SHARED_SOURCE_MOLLER_DURABILITY_RESULT_V001.md:9-17,19-44`).
It also withholds complete physical durability until all effective descendants
of the complete action are derived and exhausted (`:90-108`).

Nothing in these definitions requires a preferred closure-field phase, a
quotient of that phase, or a boundary condition pinning it.

```text
primitive_public_durability_has_source_nonreturn_component = true
primitive_public_durability_has_completed_record_persistence_component = true
complete_physical_durability_derived = false | TYPE-U |
  would-build: derive and exhaust complete-action descendants and certify
               their action on the public pointer algebra and source tail
durability_definition_contains_closure_phase_orbit_condition = false | TYPE-R |
  test: exact durability definitions and proofs contain no such condition
```

## 4. The trial orbit is not the declared gauge orbit

The trial closure field is

```text
r = (a_R+i b_R)/E_*.
```

Both potentials depend only on `|r|` and are expressly
`vector-U(1)_rel neutral`
(`SOURCE_RECORD_CLOSURE_ACTION_UNDERDETERMINATION_GATE_V001.md:10-52`).
The source's left- and right-handed fields have equal vector charge, so the
odd bilinear is vector neutral
(`PRIMITIVE_SOURCE_RECORD_CHIRAL_OPERATOR_GATE_V002.md:104-118`).

The declared pointwise `U(1)_rel` is instead the active projective endpoint
stabilizer, and its later localization as a compact connection is adopted
charged-comparison field content
(`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:32-68`). The record
degree and that connection are distinct (`:70-81`).

The most direct no-identification statement is
`SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md:85-99`: a passive record-basis
rephase can make an isolated exchange coefficient real, but it **does not** set
`b_R=0` and does not remove an axial Jacobian or topological term.

Accordingly:

```text
trial_radial_phase_orbit_equals_active_vector_U1_rel_orbit = false | TYPE-R |
  test: vector-neutral closure field versus charged endpoint stabilizer

existing_vector_U1_rel_gauge_quotient_removes_trial_orbit = false | TYPE-R |
  test: no declared vector action on r; passive rephase expressly leaves b_R

trial_orbit_is_axial_chiral_gauge_redundancy = NO_VERDICT |
  prerequisite: no sealed gauge action, quotient, or anomaly-free redundancy
                on the closure-field phase is specified
```

This distinction removes the proposed shortcut. Testing an isolated physical
background before quotienting the declared vector gauge group is not a
category error here, because the unresolved orbit is not in that declared
gauge group.

## 5. What can resolve the orbit

The closure-action gate itself gives the complete source-level list at
`SOURCE_RECORD_CLOSURE_ACTION_UNDERDETERMINATION_GATE_V001.md:73-82`:

```text
parity/CP rule;
anomaly-induced term;
boundary condition;
complete record dynamics.
```

It selects none. Its exact reopen condition requires one isolated physical
background **modulo declared gauge symmetries** (`:105-116`). The qualifier does
not declare the trial phase to be gauge; it only prevents counting genuine
gauge copies as distinct backgrounds.

Thus the currently licensed outcomes are:

1. A physical lifting/pinning mechanism could select a point or discrete set
   on the neutral phase orbit.
2. A future theorem could declare an additional gauge redundancy and quotient
   the orbit, but no such theorem is sealed.
3. A boundary/edge condition is a named possible physical selector, not an
   existing condition.

No tension with vector gauge invariance is established. Selecting the phase of
a vector-neutral closure field is not, on the supplied charges, breaking the
declared vector `U(1)_rel`.

```text
closure_phase_resolution_mechanism_derived = false | TYPE-U |
  would-build: one pre-response complete closure dynamics or declared symmetry
               theorem selecting an isolated physical background modulo the
               gauge symmetries it actually defines

boundary_or_edge_phase_pin_derived = false | TYPE-U |
  would-build: one target-independent boundary/edge condition, its action on r,
               and compatibility with the complete closure dynamics

preferred_closure_phase_conflicts_with_vector_U1_rel = false | TYPE-R |
  test: the closure bilinear and both trial potentials are vector neutral
```

## 6. Consequence for Q-209

Q-209 remains correct that:

```text
the one-cell Euclidean fidelity does not determine its Lorentzian completion;
the finite-cell-to-ensemble map is unbuilt;
the Euclidean-to-CTP realization is unbuilt;
the live Gamma_rest embedding is unbuilt;
both unchanged trial potentials lack an isolated physical background;
complete physical durability is unbuilt.
```

It requires two narrow corrections:

```text
CORRECTION 1
  Replace the corpus-wide claim that four Lorentzian counterparts remain
  unselected with:

  The one-cell fidelity alone does not determine the complete counterpart.
  The sealed corpus specifies the complete output architecture as a doubled
  complex CTP influence functional, with single-branch evolution upstream and
  coherent-response and noise kernels as components. The common-origin
  physical instance and the map from the Euclidean one-cell observable remain
  TYPE-U.

CORRECTION 2
  Do not identify the trial potentials' continuous phase orbit with the
  declared vector U(1)_rel. The orbit remains a physical isolation problem
  unless a new sealed gauge action or a physical lifting condition resolves it.
```

These corrections do not select a closure potential, derive a complete CTP
functional instance, or reopen response evaluation.

## 7. Final verdict

```text
FOUR_FORM_VERDICT = LAYER_MIXED_ENUMERATION
COMPLETE_OUTPUT_TYPE = DOUBLED_COMPLEX_CTP_INFLUENCE_FUNCTIONAL
COMPLETE_OUTPUT_INSTANCE = UNBUILT_TYPE_U

TRIAL_ORBIT_VERDICT = PHYSICAL_ISOLATION_PROBLEM_UNDER_CURRENT_DECLARATIONS
TRIAL_ORBIT_IS_VECTOR_GAUGE = REFUTED_TYPE_R
ORBIT_RESOLUTION_ROUTE = UNBUILT_TYPE_U

DURABILITY_VERDICT = NONRETURN_PLUS_COMPLETED_RECORD_PERSISTENCE
DURABILITY_IS_PHASE_ORBIT_RESOLUTION = REFUTED_TYPE_R

Lorentzian_record_counterpart_fully_instantiated = false | TYPE-U |
  would-build: the common-origin CTP package and the finite-cell/Euclidean
               restriction maps described above

trial_closure_action_selected = false | TYPE-U |
  would-build: one complete authority-admissible closure action satisfying the
               isolated-background and durability reopen conditions
```

## 8. Scope and searches

Roots entered:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
```

Files used materially:

```text
primitive_complete_boundary_transition_functional_principle_v002.md
primitive_record_cell_selection_principle_v004.md
COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md
ONE_CELL_FIDELITY_ACTION_SELECTOR_GATE_V001.md
STAGE8_TASK3A_GAMMA_REST_RECORD_CONTENT_AT_SOURCE_DETERMINATION_V001.md
SOURCE_RECORD_CLOSURE_ACTION_UNDERDETERMINATION_GATE_V001.md
PRIMITIVE_SOURCE_RECORD_CHIRAL_OPERATOR_GATE_V002.md
SOURCE_RECORD_GENERATOR_STRUCTURE_GATE_V002.md
PRIMITIVE_RELATIVE_PHASE_STABILIZER_V002.md
FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md
PRIMITIVE_REVERSIBLE_RECORD_WRITE_PRINCIPLE_V002.md
CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V002.md
R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_RESULT_V001.md
R3_4_CAUSAL_SHARED_SOURCE_MOLLER_DURABILITY_RESULT_V001.md
STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md
STAGE8_PRIMITIVE_INVENTORY_CORPUS_WIDE_EXHAUSTIVENESS_AUDIT_V001.md
STAGE8_CTP_PHYS_INPUT_PACKAGE_ITEM3_P_ROW_DEPTH_AND_DERIVED_FLOOR_AUDIT_V001.md
```

Queries were word-boundaried and case-insensitive where applicable:

```text
Lorentzian; CTP; influence; amplitude action; noise; dissipative; kinetic;
Gamma_r; Z_r; D_R; N; unitarity; positivity; reality; branch;
durable; durability; nonreturn; persistence; recoverability; pointer;
phase orbit; isolated background; modulo declared gauge; U(1)_rel;
vector neutral; b_R; passive rephase; parity; CP; anomaly; boundary condition.
```

Exclusions:

```text
a32_holdout/custodian_private/ was not opened, searched, summarized, or read;
no external physics source was used;
no post-Q-209 ruling was used;
no physical value or response was evaluated;
cleanroom_output/ was not entered because the governing source declarations
and their current cleanroom audits supplied the relevant objects and status.
```

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
