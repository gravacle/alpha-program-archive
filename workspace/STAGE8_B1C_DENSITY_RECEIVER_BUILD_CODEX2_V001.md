# STAGE 8 / 7A / [PLAN:B1c-2] — THE DENSITY RECEIVER AND REFINEMENT BRIDGE BUILD

## Lead determination — CLAIMED

A single `Ref_a` current-density **receiver type** is constructible from sealed
stock as a dependent equalizer: object rows carry the existing finite conserved
current, derived coframe, intrinsic `Vol_4`, and the opaque-but-typed positive R4
density slot; arrow rows must carry current and density transports satisfying the
nine B1c interfaces.  This seals a type, not an inhabitant.

The refinement bridge stops after `A0`.  The actual finite/`Ref_path` core supplies
the `A0` current maps.  Neither `A1` nor `A2` has a sealed joint field-extension
member assigning the new interior-edge cochain and the compatible positive
density/measure transport.  The same missing refinement stratum blocks the exact
`Vol_4` quadratic compatibility.  Consequently no full receiver inhabitant is
constructible and the forcing question remains gated.

Every headline determination below is `CLAIMED` pending independent cross-check.
Nothing is adopted or selected by outcome.

## 0. Preflight, custody, and source pins

- Relay `793` was uniquely located, its adjacent seal verified, and
  `relay_outbox/793_ACK.md` written before the relay body was read.  Lane guard:
  `CODEX 2`.
- `PROGRAM_STATE_BRIEF_V005.md` was verified and read at
  `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`.
- Governing relay-791 artifact `STAGE8_B1C_CURRENT_DENSITY_HUNT_CODEX2_V001.md`
  was verified and read at
  `f7b218cd6c7a23bb19d87413576147fcdf64b1d0e7705bf2b1e14ab5727ccd37`.
- The existing-output stop check passed.
- The live-appended register was seal-verified at
  `85aca12cabcebaa4bffe8fed5fcb156085742a09f001a8bb1819a55c0eacefec`;
  Q-697 records the relay-791 partial result and uniform refinement-stratum
  observation.  Live append changed the file digest, not the cited ruling.
- `LOCKED_PROCESS.md` and `DECLINE_REGISTER_V002.md` were seal-verified at
  `9bc4a8fad15ef5c711b2ebf9b2ae07012308be6d04aaf2fa12ce2d89b7140074`
  and `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`.
- `PE-1` through `PE-7` were not consulted and have zero weight.

Decisive construction stock:

| ID | sealed member; full-file SHA-256 | decisive span; span SHA-256 |
|---|---|---|
| G1 | `STAGE8_7A_RA27_2_INDEX_BUILT_DARIO_V001.md`; `66f078baf5ff980f2ae2b57894154353721727ec0bf49b025b15746f4b5fbdc3` | closed `Ref_a` statement `[4826,7883)`; `9b450c5ab92c9bba7a8405851ac51b37ba473e049bb1570724574d56fa40ca8a`; measure-transport finding `[7951,13376)`; `b7d052e5171f7ac821f17d48009aa11376a44a58ff357acba76032ac19b4b9e8` |
| G2 | `STAGE8_7A_REFINEMENT_BRIDGE_DARIO_V001.md`; `972eae9ac7bbc66203fb43c547eebc68b85fe7ebcfc2f22780ed82340668cf4f` | flux definition `[3757,6353)`; `d812ca1bb38291c3cf55e074ac26b4c8f1548b7a291296fb5ca782cb11f498ff`; subdivision derivation `[6402,12096)`; `e26b3d3c387c9dea791271f7c7eba9ea9fea6489223a113eea51a3f8458d5960` |
| G3 | `STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md`; `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | current seam `[7633,8436)`; `8537aaedfd15bd0a3c06d9cd0131facada9c4f07cf370a0ad82fc4f9fb4bd8e7`; conserved distributions `[12210,13564)`; `aea3958392712d89e06ea2939ccccbbb3c72f67855c3fed2891cfffbc1463966` |
| G4 | `STAGE8_TASK5_EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V004.md`; `c5254f663a1e3a6d1079027184c6dea96a35ab7255a856aa39e584b3d7c32dc2` | R1 path/current law `[12570,15287)`; `ede584606605e8e0d8cf9690392a46c323e75b33d140b40d9917d79ada9648eb`; R4 field law `[15287,18485)`; `8f7d57476d89955f03e33a61ba73b51fd4bf355d1e1226c13371647ab3c97853`; actual `Ref_path` core `[19302,20226)`; `63bc600f64c717452a8c96e48a79b50b63f6f518967e2d069e53c2cd51e76e73` |
| G5 | `STAGE8_TASK5_EQ6_WHERE_CLAUSES_LANE2_V005.md`; `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | P4/X4 density slot `[4505,5661)`; `2284937b39caf114bbc3641bb57a28914bd9335324967849cb882400b6cbfb75`; path/current plus field laws `[6510,14711)`; `163a6a24f1b667d2e64ce3ec09951ee11687f8ba9834aacdda2142025f715dfb` |
| G6 | `STAGE8_7A_MEASURE_ONELINER_CODEX2_V001.md`; `84db6f531e2eb42ee5e4993ed3e9f9e7265df28cfb82cec4b1284ffbb4c1ad8f` | forced denotation `[2904,3579)`; `bcd61c8d30ba3c01f4b67d3fa295f7096e4d86945d60b46e53d7ee3ff29db091`; coframe transports `[3579,7639)`; `d1702d21f3c5f4fd25a26a80c683f02d99b862a70f00dc46a67bac2b4056dc49` |
| G7 | `STAGE8_7A_SUPPORT_AND_FORCING_DARIO_V001.md`; `9685af44cc48f01fb04e57329cedf4f9a871eb393c6d41396179776957287e9b` | support law and gap `[4644,8660)`; `775c1d2e3c24494e05daec039c852247b84ba7c2c392170aa4a6495f67306a2f`; forced intrinsic measure `[10095,11777)`; `21834fa17c487c7dcc599a0bdceb6ff3c5f14f1da37c6f74ac24d1b6742844fd` |

The lane-opposite 788 carrier mentioned by the relay is not present as a sealed
artifact in current cleanroom custody.  Shared typing therefore uses G1's exact
`Ref_a` statement rather than reconstructing or anticipating 788.

## 1. AS1 — the single receiver type — CLAIMED

### 1.1 Ground sorts

For each `K` in the sealed `Ref_a` object class (G1), use only existing sorts:

```text
Curr(K)   := K_K^fin = ker(B_K^T)                         [G3]
Real(K)   := J_cyc,K, with c |-> J_K(c), partial J_K(c)=0 [G3]
Cof(K)    := the derived A0/A1/A2 coframe carrier         [G6]
Vol(K,C)  := Vol_4(C), the forced intrinsic cell measure  [G6/G7]
R4Dens(K) := the pre-existing positive density sort in the R4 unit class,
             dual to Cof(K)                              [G5]
```

`R4Dens(K)` is deliberately opaque.  G5 types it as a positive density relation
dual to the coframe but does not provide a completed pointwise codomain.  Refining
that sort here would author the very object the build is meant to locate.

### 1.2 Closed receiver schema

Define the **type**, not a chosen member:

```text
CDensRecv(Ref_a)
 := Equalizer {
      object data, for every K:
        delta_K in R4Dens(K),
        Pos(delta_K),
        Dual(delta_K,Cof(K)),
        VolNorm(delta_K,Vol_4|K),
        CurrentDomain(delta_K)=Curr(K);

      arrow data, for every R:K->K' in Ref_a:
        S_R : Curr(K) -> Curr(K'),
        d_R : R4Dens(K) -> R4Dens(K');

      equations:
        d_id=id, d_(S o R)=d_S o d_R,
        S_id=id, S_(S o R)=S_S o S_R,
        J_K'(S_R c)=R_* J_K(c),
        IncidenceIntertwines(R,S_R),
        DualitySquare(R,d_R,Cof_R),
        ExactVol4Quadratic(R,d_R),
        OrientationReality(R,d_R,S_R),
        SupportCommonRefinementTau(R,d_R,S_R)
    }.
```

This is a single dependent receiver over all objects and arrows, rather than one
unrelated density slot per cell.  Each field and equation comes from the nine-item
interface in relay 791 and G1–G7.  The construction consumes no member and asserts
neither nonemptiness nor uniqueness.

```text
RECEIVER_TYPE = CONSTRUCTED
RECEIVER_NONEMPTY = UNDECIDABLE_FROM_STOCK
RECEIVER_MEMBER_BOUND = false
```

## 2. AS2 — refinement bridge and generator transports — CLAIMED

### 2.1 Bridge type

The only bridge shape already sealed is the G4/G5 path-current/field relation.
For each `g:K->K'` in `Ref_a`, a bridge member must be

```text
B_g=(f_g,F_g,s_g,S_g; E_g)

f_g   proper orientation-compatible local map,
F_g   support map,
s_g   signed path-chain map with boundary intertwining,
S_g   s_g restricted to Curr(K), injective and current-preserving,
E_g   compatible target field member (e_K',mu_K',a_K',F_K')
      carrying positive density, coframe duality, Curv(a_K')=F_K',
      old-image fidelity, support, covariance, and composition.
```

`Bridge_a/path` is the dependent family of such members whose source/target are
the G1 objects and whose arrows lie over the G1 `A0/A1/A2` generators.  This
types the bridge without identifying the two categories or choosing a member.

### 2.2 Generator execution

| generator | current/path bridge | density/field bridge | disposition |
|---|---|---|---|
| `A0` identities, certified signed isomorphisms, ratified W3 restriction/inclusion squares | **BUILT** by G4's actual finite/`Ref_path` core; identities and admitted surface isomorphisms inherit `S_g` and composition | conditioned on the object density slot; identity transport is typed, but no global `delta_K` family is bound | **PARTIAL BUILD** — current bridge exists; no receiver inhabitant selected |
| `A1` cubical bisection | no sealed `PathRel`/field-extension member over the exact G1 generator | coframe and `Vol_4` are derived (G6), but no `a'` on new interior edges and no compatible `delta_K'`/`d_g` member are supplied | **STOPPED** |
| `A2` oriented simplicial/barycentric subdivision | same absence | coframe and simplex `Vol_4=|det E|/4!` are derived (G6), but the same interior-edge and density member is absent | **STOPPED** |

The obstacle is not a missing formula for curvature on a sub-face.  G2 proves
`F'=d_1'a'`; once `a'` exists, sub-face flux is determined.  What is absent is
`a'` on the new interior edges—equivalently the `k-1`-parameter distribution of
parent flux among `k` children—and the density member compatible with that choice.
The total flux transports; the exact quadratic does not follow from the total.

Therefore finite composites containing `A1` or `A2` cannot be constructed by
composition.  Assigning them independently would violate the interface.

```text
BRIDGE_TYPE = CONSTRUCTED
BRIDGE_A0_CURRENT = BUILT
BRIDGE_A1 = STOPPED
BRIDGE_A2 = STOPPED
```

## 3. AS3 — nine-interface package — CLAIMED

The test is against the receiver plus bridge just constructed, not merely against
the older objectwise current.

| item | result | proof or exact stop |
|---|---|---|
| I1 one field in the full realization | **PROVED AS TYPE** | `CDensRecv(Ref_a)` binds current, density, coframe, volume and arrow equations in one dependent object; no post-processing scalar is introduced. |
| I2 totality on every `A0/A1/A2` generator and composite | **FAILED AS BUILD OBLIGATION** | `A1/A2` bridge members are absent, so composites containing them have no transport. This is a missing inhabitant, not a refutation of possible existence. |
| I3 positive current/volume-density type | **PROVED AS TYPE** | `Curr(K)`, opaque `R4Dens(K)`, positivity, coframe duality and `VolNorm` are all sealed sorts/predicates. The opaque pointwise codomain remains unrefined. |
| I4 functoriality | **UNDECIDABLE** | identity/current composition exists on `A0`; no `A1/A2` members exist whose compositions could be tested. Missing object: the full bridge family. |
| I5 conservation/current transport | **UNDECIDABLE FOR THE PACKAGE** | objectwise conservation and `A0` transport are proved (G3/G4); `J_K'(S_g c)=g_*J_K(c)` cannot be evaluated on `A1/A2` without `S_g`. |
| I6 incidence intertwining | **UNDECIDABLE FOR THE PACKAGE** | G4 types the square and proves it on the actual core; the exact G1 `A1/A2` chain maps are not supplied as admitted members. |
| I7 coframe duality | **UNDECIDABLE** | coframe transports are derived, and the duality predicate is typed, but no `delta_K/d_g` family inhabits it. |
| I8 intrinsic `Vol_4` / exact energy | **FAILED AS BUILD OBLIGATION** | G1/G2 display the obstruction: fixed total flux leaves `k-1` child-flux parameters, while the measure is quadratic; the higher-order remainder is uncontrolled. No exact-energy transport proof exists. |
| I9 covariance/common carrier/support/anti-tuning | **UNDECIDABLE** | orientation/reality and no-tuning are predicates in the receiver, but refinement support and a common instantiated `tau_ker` remain absent (G7). |

Strict construction tally: **2/9 proved as types, 2/9 failed as build
obligations, 5/9 undecidable pending named members**.  “Failed” here means the
commissioned construction stopped on an absent sealed carrier; it does not assert a
mathematical impossibility.

## 4. AS4 — inhabitant and forcing — CLAIMED

### 4.1 Inhabitant attempt

An inhabitant would have to provide, simultaneously for every G1 generator,

```text
H = ((delta_K)_K, (B_g,d_g)_g) in CDensRecv(Ref_a)
```

and in particular a complete `A1` and `A2` field-extension member.  The sealed
stock supplies objectwise `J_c`, the coframes and `Vol_4`, and the law predicates.
It does not supply either generator's joint member.  The smallest missing sealed
object is therefore:

```text
JOINT_A1_A2_FIELD_EXT_MEMBER
  for each g in {A1,A2}:
    an admitted (f_g,F_g,s_g,S_g; e_K',mu_K',a_K',F_K',delta_K',d_g)
    with a_K' assigned on new interior edges, F_K'=d_1'a_K',
    current/incidence preservation, positive coframe-dual density,
    exact intrinsic-Vol_4 quadratic transport, support, covariance,
    identity/composition, and common-refinement coherence.
```

The connection component is the same refinement-layer datum identified in the B1a
lineage.  This report shares G1's `Ref_a` citation but does not import, coordinate
with, or anticipate a lane-opposite member.  No candidate such as equal split or
area-proportional split is chosen; both would consume the free interior-edge datum.

```text
INHABITANT = NOT CONSTRUCTIBLE FROM SEALED STOCK
MISSING = JOINT_A1_A2_FIELD_EXT_MEMBER
```

### 4.2 Forcing status

Forcing is **still gated**.  The receiver is a typed dependent equalizer whose
nonemptiness is undecidable.  Uniqueness versus two complete families cannot be
posed until at least one complete family exists and the admissible member class is
sealed.  The gate is exactly `JOINT_A1_A2_FIELD_EXT_MEMBER`, not an unspecified
“more physics” remainder.

## 5. AS5 — freedoms, flattening, and audit

### 5.1 `FREEDOMS-CONSUMED`

| free datum touched | alignment-law tag | treatment |
|---|---|---|
| objectwise density member `delta_K` | **CARRIED-AS-PARAMETER** | typed in `R4Dens(K)`; none selected |
| `A1/A2` bridge/field-extension member `B_g` | **CONDITIONED-ON** | construction stops until a sealed member exists |
| refined interior-edge cochain `a_K'` | **CARRIED-AS-PARAMETER** | free locus displayed; no split rule chosen |
| child-flux distribution at fixed parent total | **CONDITIONED-ON** | determined only after `a_K'`; no equal/area split substituted |
| current-chain map `s_g/S_g` on `A1/A2` | **CARRIED-AS-PARAMETER** | exact admissible member absent |
| density normalization/scale | **CARRIED-AS-PARAMETER** | constrained by `VolNorm`; no coefficient fixed |
| support map `F_g` and refinement-density support law | **CARRIED-AS-PARAMETER** | G7 gap preserved |
| common topology `tau_ker` | **CARRIED-AS-PARAMETER** | same-topology predicate retained; no topology selected or switched |
| higher-order term in the flux/coframe relation | **CARRIED-AS-PARAMETER** | control under subdivision remains a named obligation |
| `Ref_a` directedness | **CONDITIONED-ON** | existing adopted axiom in G1, cited as adopted; not re-derived or substituted |

`SUBSTITUTED` entries: **none**.  `Vol_4` is not listed as a freedom because G6/G7
already force it; no alternative measure is introduced.

### 5.2 Flattening check

- S08 (`DECLINE_REGISTER_V002.md[3879,4287)`, span SHA
  `15fdbb0216cd7066b37b2b884bba0f2f9116d8e1118fae24af4f7541fcffd5d1`)
  remains intact.  No current, connection, density, or bridge is identified with
  electromagnetism, Maxwell data, a smooth public field, or a response object.
- S26 (`DECLINE_REGISTER_V002.md[10631,10949)`, span SHA
  `9cfa74c5031c4690348c9b3cf79ace86549ce8dce0c81a78194d018e9b438ec7`)
  remains intact.  `C_ref` is not used as a source; no smooth `(M,g)` is imported.
- No common cell was formed and no junction map was evaluated.  No declined
  identification is hidden inside `Bridge_a/path`; it is a dependent relation type,
  not an equality of categories.

### 5.3 Battery and self verb audit

- **F_PLDEC:** no physical quantity was evaluated numerically.
- **Anti-tuning:** no desired verdict, response, reader, threshold, or measured
  constant selected a member, split, density, topology, or coefficient.
- **M-2:** exact tokens, symbol/formula forms, generator aliases, and semantic
  member tests were applied; law-only predicates were not counted as inhabitants.
- **PIN CHECK:** all load-bearing full-file and span hashes above were recomputed;
  adjacent seals verified where present.
- **Verb audit:** `constructed` applies to a mathematical receiver/bridge type and
  the already-sealed `A0` current core.  It does not claim nonemptiness, a physical
  field, authorization, or adoption.  `proved as type` is explicitly distinct from
  proving an inhabited transport.

RECEIVER = typed and sealed (grounding listed; dependent equalizer, nonemptiness undecidable)
BRIDGE = stopped (type constructed and `A0` current core built; `A1/A2` joint field-extension members absent)
PACKAGE = 2/9 proved as types / 2 build failures displayed / 5 undecidables named
INHABITANT = not constructible (missing object named: `JOINT_A1_A2_FIELD_EXT_MEMBER`)
FORCING = still gated (on a sealed complete `JOINT_A1_A2_FIELD_EXT_MEMBER` family and receiver nonemptiness)
B1C_STATUS = remaining stated (populate `A1/A2`; prove conservation/incidence, duality, exact `Vol_4` energy, support and composition; then rerun forcing)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (all headlines CLAIMED; type construction separated from inhabitance; zero substitutions)
