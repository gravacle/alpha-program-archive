# STAGE 8 / TRANSPORT LAW / MINIMAL SUB-LAW — THE AND-CLASS MEMBER-INDEPENDENCE DECISION

Lane: FABLE blind construction, codename AND-CLASS. One structural question decided by
exact algebra over sealed data. **All headline results CLAIMED until checked.**

## Lead determination — CLAIMED

**AND_CLASS = CONSTANT_FALSE — by KINEMATIC COLLAPSE, not by member selection.** The
harmonic block `H = ker(d_1) ∩ im(d_0)^perp` of V011's sealed tangent complex is the
**ZERO SUBSPACE on every sealed complex of the working class**: `dim H = 0` exactly on
the parent K (16/32/24), A1 (81/216/216), A2-F (16/65/110), A2-B (81/544/1232), and the
807 common refinement Z (81/544/1232) — certified by an exact rational rank sandwich
(three primes, 807's own instrument) plus full `Fraction`-exact ranks on the two
smallest complexes, and corroborated by 807's OWN sealed kernel display on Z. Hence:

```text
PHI_H_VANISHING = IDENTICAL   — the projection P_H is the ZERO MAP; phi_H(ell) = 0 for
                                EVERY 1-cochain, admissible or not; no variety member,
                                F-rule, or freedom value can move it.
PHI_F_VANISHING = NEVER       — on the admissible set: the sealed total-nonzero
                                constraint (phi_f + phi_H != 0 for n != 0) with
                                phi_H ≡ 0 forces phi_f != 0 at every member.
AND_CLASS       = CONSTANT_FALSE (the H sector is dead — by exact dimension).
```

The member-independence certificate is the rank identity itself: the AND-boolean is the
same (FALSE) at every point of the 1887-dim joint variety, every value of the 32-dim
non-coboundary freedom, and every admissible write — the posed falsifier (a witness pair
with different boolean patterns) CANNOT exist. The derived-form dichotomy resolves to the
**degenerate one-sector factorization** `R = R_flux ⊕ 0` at the sealed working-class
instance, member-independently, with no member selection. The FORM's label "the dim-32
flux-free physical block H" is corrected of record: `dim H = 0`; the quoted 32 is the FR
equal-energy assignment freedom, which inhabits `im(d_0) ⊕ im(d_1^dagger)`, not H.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false
       coupling_evaluation_authorized = false.  No physical quantity (n, kappa, alpha,
       mu, any coupling/norm/scale) computed, bounded, or evaluated.  Every displayed
       number is a structural integer, an exact rank, or a sealed-text quotation.
```

---

## 0. Preflight and seals

Output name probed before any write: `STAGE8_AND_CLASS_INDEPENDENCE_V001.md` ABSENT
(artifact and sidecar). No register, tracker, plan, road, ledger, or lens file read.
All verification by `shasum -a 256` at path BEFORE reliance:

```text
VERIFIED AT PATH (workspace/ = /Users/bgm/MB Work/alpha-program-archive/workspace/):
22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md            MATCH (the posed sub-law)
a5c71b2ac7cd198bbac188e865d139a71340f1728f72fe56384aa5d60489110c  STAGE8_TRANSPORT_LAW_POSE_CHECK_V001.md       MATCH (C2-C4: the AND-boolean is the minimum object)
bb1b88ad22ad2746a59957936785e9e44e3c2c4c1abdf7cf417096ff982d08c4  STAGE8_D0_SQUARE_CERTIFICATE_V001.md          MATCH (block pair well-defined under transport)
d83655ae50b4203ce42be6fd4fc0a50f03f601c96f55eb3c3afab71807de492e  STAGE8_D0_SQUARE_CHECK_V001.md                MATCH (CERT_VERDICT = CONFIRMED; matrix fidelity 1906/0)
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md          MATCH (FORM: three-block split; the dichotomy)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md           MATCH (G3: the admissible-space constraints, §6)
9cf9b329bfad1656f91eb75600ca2a60d44853fbe4a1172186beef78e34f1eb9  STAGE8_B1A_CORRECTED_JOINT_SOLVE_CODEX2_V001.md  MATCH ("807": the 1887-dim joint variety; Z)
4d072e7671113357764b61e11ef1d3d336ce34e307ba07c545ec7cf1a38a6abc  STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md       MATCH (FR: the 32-dim non-coboundary freedom)
1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb  STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md   MATCH (GB: the carried constraint inventory)
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md               MATCH (GC: SOUND verdict; the barred forcing line)
aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a  review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  MATCH (packet member; the top-level same-named file is NOT this file and was not read — T6 honored)
614e20c8bfd1978a4273c831b76bd6145483876c975ea87f80ef31a589b8bdc7  STAGE8_B1A_CARRIER_CROSSCHECK_NATURALITY_CODEX2_V001.md  MATCH (the sealed complex censuses)
```

Spans consumed, extracted from the sealed bytes and rehashed (sha256 of the byte span)
before use:

```text
V011 [44595,44690)  c6cd568bec678df676737009466e3537dfe64fd6962fdb3200b9f90b9dc14eb5
     the d_0 / d_1 laws and d_1 d_0 = 0 (matches the D0-SQUARE lineage's pin)
V011 [44530,45446)  9bbd9525cbd84a6b6365383da6cd6e38d33f945e01c98c1a0d3052f322b74efc
     the tangent complex A^0/A^1/A^2; ker(d_0) = constants; the lift clause naming
     "ker(d_1) intersect im(d_0)^perp" (H's own sealed definition)
807  [6502,6638)    7ed9e19279ce8af4e599e3d87419ccbea1b16df7be2dd0f8ede2251a2f85b457
     "H_Z = im(d'_1), dim H_Z = 544-(81-1) = 464" ... "with the 80-dimensional kernel
     exactly vertex gauge"  — the sealed control: ker d_1 = im d_0 on Z
XCHK [4056,5082)    6ae8c4c440588efe7c0b107da8f003dbc35e24648a4817f76f87e428862ee447
     the sealed complex censuses: parent 16/32/24; A1 81/216/216; A2-F 16/65/110;
     A2-B 81/544/1232
GB   [10009,10771)  57bd32337d3e2c554fb8780f05deace9d62b244e9cb1c35163770245511f11c7
     the consolidated FIXED/FREE display (the admissible constraints of record)
GB   [19950,20394)  cbcd471d75d1cf46c0e462474778bf071db6df1afed2fc37a899ca2b7612a0b1
     PHI_H = CONTINGENT flag (carries the "H inhabited by the dim-32 ... freedom" claim)
GC   [4700,5150)    1bfbc1e4d858cae91e182e1ddce935af73412b104476cf1c386161f837ecfc38
     conservation ≡ gauge-invariance (both exactly ⊥ im(d_0))
GC   [7386,9452)    8e1f821b421a21b8887d383d29bfc06aea74cdf58814e87373f0b2a911146d35
     sub-attack (c): the BARRED forcing line (Omega_c continuum contractibility)
GC   [15605,16527)  b066aa94d4dabfc48dd715239f923380369ddd1fe997a52d955495b91ef77b36
     SPLIT_FORCED = NO_CONTINGENCY_CONFIRMED flag
GC   [17677,18327)  0e491616b9b92e16db13ea89682424187e672a146e7a697ab6b213c6456bdc0f
     G3_BUILD_VERDICT = SOUND, incl. the minor-imprecision note: "H inhabited" is
     justified by non-gauge-complement inhabitation, weaker than the cited seal
FORM [8682,8867)    065546b53604f60cdd8a93f83b1d3a171a1132fb26b56e79a023111ed74ddd86
     D1's three-block split display A^1 = im(d_0) ⊕ H ⊕ im(d_1^dagger)
FORM [9339,9559)    8f61455615a248ab5fcc2c94bf5bde2dbe7b8ac5c3ae21c0112b4a276655bbc5
     D1: the freedom lives in the physical complement of im(d_0); "the exact
     structural integer 32 is quoted, not derived here"
FORM [19112,19244)  66a9cd20862d7b86c169a032fa8b1e05733df742030e6638d531b6a2fb08ba25
     §4.3's phrase "the dim-32 flux-free physical block H" (the label corrected below)
G3   [20660,21397)  ba7d1013229e8c2b285ec3c5b738d6e06589dd8fbe29439515b758de596a0ce5
     §6 THE FREE SPACE — the admissible realizations of G3 (the space quantified over)
```

---

## 1. The posed question, of record

Per the pose (POSED §3 MINIMAL_SUBLAW) as corrected by the check (POSE_CHECK C4: "the
minimum object is the AND-boolean class, strictly weaker than the posed pair"), the
decision commissioned here: over the sealed admissible space of write realizations —
the 1887-dim joint variety (807) carrying the 32-dim non-coboundary freedom (FR), under
the sealed constraints (gauge-invariant, cell-local, conserved, total-nonzero for
n != 0, two-port boundary data; GB/GC as carried by G3 §2 Q6) — decide for the two
block projections `phi_f` (the `im(d_1^dagger)` flux component) and `phi_H` (the
`H = ker(d_1) ∩ im(d_0)^perp` component) whether each vanishes IDENTICALLY / NEVER /
on a PROPER SUBVARIETY, and hence the class of the AND-boolean
`AND = [phi_f != 0 AND phi_H != 0]`. The transported write chain itself is sealed
NOT-SUPPLIED (GB S1 / O11–O13) and is NOT supplied here: the quantification is over the
ADMISSIBLE space of record, with the F-rule and member freedom carried as parameters.

**The admissible space, quoted (G3 §6, span ba7d1013):** "THE FREE SPACE (the
admissible realizations of G3): per admitted cell, the PHYSICAL-BLOCK component of
ell_j — an element of H (+) im(d_1^dagger) constrained only by (a) total-nonzero for
n != 0 (phi_f + phi_H != 0, carried Q6), (b) cell-local support (R1), (c) the two-port
boundary data (R4, pinning only the single-branch gauge-block part) — together with,
over the working refinement class, the refinement-lift freedom ... The block-split
weights (phi_f, phi_H) are coordinates on this space; no sealed object fixes them
either way (Q6)." Conservation is the SAME condition as gauge-invariance (GC span
1bfbc1e4: both exactly ⊥ im(d_0)); the two-port data pin only the gauge block, which
is invisible to both projections.

---

## 2. The block projections as exact functions of the parameters

On V011's sealed tangent complex (span 9bbd9525): `A^1 = R^(K_1^+)` with the sealed
orthogonal split (FORM D1, span 065546b5, one line from sealed `d_1 d_0 = 0`):

```text
A^1 = im(d_0) ⊕ H ⊕ im(d_1^dagger),    H := ker(d_1) ∩ im(d_0)^perp.
```

Writing `w` for the admissible write's physical-block coordinate (the G3 §6 free
coordinate) the projections are EXACTLY LINEAR:

```text
phi_f(w) = P_f w ,   phi_H(w) = P_H w ,
```

with `P_f`, `P_H` the orthogonal projectors onto `im(d_1^dagger)` and `H` — fixed
exact rational matrices of the sealed complex, INDEPENDENT of the variety point and of
the freedom value. The variety (807) and the FR freedom enter only through transport
across the licensed refinement moves, under which the block pair is well-defined of
record: the d_1-square (carrier + crosscheck, 771/0 exact) and the d_0-square
(bb1b88ad, CERTIFIED_ALL; d83655ae, CONFIRMED) make `im(d_0)` transport-stable and the
quotient blocks transport-coherent on every licensed generator and composite. So the
algebraic form of the question is honest and fully determined by sealed data: decide
the vanishing loci of two FIXED linear maps over a product parameter space. No
polynomial (nonlinear) dependence enters the projections themselves.

---

## 3. The decision — exact dimensions of the three blocks

### 3.1 Method

For every sealed complex of the working class (parent K; A1; A2-F; A2-B; and 807's
common refinement Z — the very complex the 1887-dim variety's quotient variables live
on), reconstruct `d_0` and `d_1` from the sealed laws (V011 [44595,44690)) and the
sealed constructive displays (XCHK [4056,5082); 807 §2.1 presentation), then:

1. verify the census against the sealed displays;
2. verify `d_1 . d_0 = 0` entrywise over Z (exact integers);
3. verify `d_0 . (constants) = 0`  ⇒  `rank_Q d_0 <= V-1`;
4. compute `rank_p d_0`, `rank_p d_1` for p ∈ {1000003, 1000033, 1000037} (807's own
   exact-rank instrument; `rank_Q >= rank_p` always);
5. SANDWICH (exact, not probabilistic): `rank_p d_0 = V-1` forces `rank_Q d_0 = V-1`;
   then `d_1 d_0 = 0` gives `rank_Q d_1 <= E-(V-1)`, and `rank_p d_1 = E-(V-1)` forces
   `rank_Q d_1 = E-(V-1)` exactly — hence `ker d_1 = im d_0` and `H = {0}`;
6. independently, full `Fraction`-exact RREF ranks over Q on the two smallest
   complexes (parent, A2-F) as a second instrument;
7. exhibit an explicit exact admissible witness member and its projections.

Rank is orientation-convention-invariant (a re-orientation of a face or edge is a row
sign flip), so the verdicts do not depend on the lexicographic conventions used;
`d_1 d_0 = 0` is verified for the constructed signs in every case.

### 3.2 Result table (all entries exact)

```text
complex                      V    E    F     rank d_0   rank d_1   dim im(d_0)  dim H  dim im(d_1^dagger)
parent K (unit 4-cube)       16   32   24    15         17         15           0      17
A1 (cubical bisection)       81   216  216   80         136        80           0      136
A2-F (Freudenthal)           16   65   110   15         50         15           0      50
A2-B (barycentric)           81   544  1232  80         464        80           0      464
Z (807 common refinement)    81   544  1232  80         464        80           0      464
sum check per row: dim im(d_0) + dim H + dim im(d_1^dagger) = E   (exact, all five)
```

**Sealed control:** 807's own display (span 7ed9e192) — `dim H_Z = 544-(81-1) = 464,
with the 80-dimensional kernel exactly vertex gauge` — asserts `ker d_1 = im d_0` on Z
of the sealed bytes, which is exactly `dim H = 0` there. The reproduction agrees.

### 3.3 The three verdicts

```text
PHI_H_VANISHING = IDENTICAL.  dim H = 0 on every sealed complex of the working class,
  so P_H is the zero map and phi_H(ell) = 0 for EVERY 1-cochain — a fortiori for every
  admissible write, at every point of the 1887-dim variety and every value of the
  32-dim freedom.  This is the strongest possible member-independence: the projection
  map itself is zero, so no parameter can move it.

PHI_F_VANISHING = NEVER on the admissible set.  The sealed constraint set (G3 §6 (a);
  GB span 57bd3233) forces phi_f + phi_H != 0 for n != 0; with phi_H ≡ 0 this forces
  phi_f != 0 at every admissible member.  Non-vacuous: the admissible set is nonempty
  (807 seals the variety NONEMPTY with the identity member; the write coordinate space
  im(d_1^dagger) has dim 17 >= 1 on the parent complex) and an explicit exact witness
  is exhibited in §4.

AND_CLASS = CONSTANT_FALSE (the H sector is dead).  AND = [phi_f != 0 AND phi_H != 0]
  = [TRUE AND FALSE] = FALSE at every admissible realization.  Constancy is certified
  by the rank identity, not by sampling; the posed falsifier — two members with
  different boolean patterns — CANNOT exist, since every member realizes the SAME
  pattern (phi_f != 0, phi_H = 0).
```

---

## 4. The explicit admissible witness (exact integers, parent complex)

`ell_w := d_1^dagger e_(f0)` for the lexicographically first face
`f0 = (base 0000, directions (0,1))`:

```text
ell_w = -1@0000->0100 +1@0000->1000 -1@0100->1100 +1@1000->1100
```

Verified exactly: `d_0^dagger ell_w = 0` (conserved; zero gauge component — the
two-port datum realized with `e_out = e_in`, an admissible port value, carried as a
parameter); `ell_w != 0`; `ell_w ∈ im(d_1^dagger)` by construction. Its pattern is
`(phi_f = ell_w != 0, phi_H = 0)` ⇒ `AND = FALSE`. Every other admissible member
realizes the same pattern (§3.3). Cell-locality is immediate at the sealed single-cell
instance.

---

## 5. Why this is NOT the barred forcing line — the fence audit, head-on

GC sub-attack (c) (span 8e1f821b) barred exactly one route to `phi_H = 0` FORCED: the
import of the CONTINUUM diamond `Omega_c`'s contractibility — "Omega_c is typed 'A
LORENTZ-COVARIANT CONTINUUM DIAMOND, NOT A CW OBJECT AT ALL'"; "its H_1 is not sealed";
"To run the forcing I would have to IMPORT Omega_c's diamond contractibility as
authority — a forbidden SCALE/GR import." G3 §4 carries the same bar ("BARRED SHORTCUT,
not taken").

This decision does not touch that route at any point:

```text
(i)   NO continuum object is consumed.  No Omega_c, no smooth density, no R7 realization,
      no support/homology datum of any continuum region.  The computation is the exact
      rank of the SEALED RECORD COMPLEX's OWN integer incidence matrices — the sealed
      combinatorial objects (V011 d_0/d_1 laws; the crosscheck censuses; 807's Z
      presentation) that the D0-SQUARE lineage already extracted, cross-verified (1906
      entries, 0 discrepancies), and consumed as sealed displays.
(ii)  The object decided is DIFFERENT.  The barred line argued about the homology class
      of a particular transported chain INSIDE Omega_c.  This artifact computes the
      ambient block H of the tangent complex the projections are DEFINED against (V011
      span 9bbd9525 names it; FORM D1 displays it).  If that block is {0}, every
      cochain's H-component is zero — no statement about any particular chain's class
      inside any region is made or needed.
(iii) The instrument is already licensed in this lineage.  Exact integer/rational rank
      computation on sealed displays is the instrument class of 738, 795, 807 (whose
      1887 is itself a modular-rank-sandwich result), and the D0-SQUARE pair.  GC's own
      machinery line ("no value of ... Betti ... computed") records that GC did NOT run
      this computation — it fenced itself to symbolic reads; nothing in GC bars the
      computation, and this lane's tasking commissions it explicitly (step 1: "state
      dimensions").
```

GC's finding "There is NO sealed connection-only combinatorial substitute" was made of
`Omega_c`'s H_1 — true and untouched. The ambient record complex's block dimensions ARE
sealed connection-only combinatorial content, and they decide the question at its own
definition site.

---

## 6. Corrections and reconciliations of record

```text
C-1  FORM §4.3's LABEL, corrected.  "the dim-32 flux-free physical block H" (FORM span
     66a9cd20) attaches FR's quoted 32 to H.  Exact computation: dim H = 0 on every
     sealed instance.  FORM D1 itself disclosed the caveat (span 8f614556): the freedom
     "lives in the physical complement of im(d_0)" and "the exact structural integer 32
     is quoted, not derived here."  The correct statement: the 32-dim equal-energy
     assignment freedom inhabits im(d_0) ⊕ im(d_1^dagger) with a NONZERO FLUX part —
     consistent with CV's non-coboundary certificate, which bars only the gauge block
     (x_alt - x_F not in im(d_0) requires a nonzero physical = flux component; equal
     ENERGY constrains |d_1 x|^2, not d_1 x, so a nonzero flux difference at equal
     energy is consistent).  No sealed derivation is contradicted; one label is.

C-2  GB's PHI_H = CONTINGENT and GC's SPLIT_FORCED = NO_CONTINGENCY_CONFIRMED,
     reconciled.  Both are CONSTRAINT-level verdicts: no sealed CONSTRAINT forces the
     split — true, and untouched here.  Neither artifact computed the block's
     DIMENSION; GC's own verdict flagged GB's "H inhabited" claim as "justified by
     non-gauge-complement inhabitation, weaker than the cited seal" (GC span 0e491616).
     This artifact completes that flag: H is NOT inhabited at all on the sealed
     carrier.  The kinematic collapse SUPERSEDES the contingency reading at the sealed
     instance — the coordinate phi_H ranges over a zero-dimensional space — without
     contradicting the constraint-level findings.

C-3  The pose-check's Correction A (post-limit needs the Gate-5 kernel C on the cross
     pairing), sharpened.  With Phi_H ≡ 0 the cross pairing Phi_f^T C Phi_H vanishes
     for EVERY kernel C — the dichotomy's resolution at the sealed instance is
     kernel-independent.  (The flux-block response Phi_f^T C Phi_f still needs C; not
     this question.)

C-4  The pose-check's Correction B (the sub-law consumes the unbuilt write chain),
     dissolved as tasked: the decision quantifies over the ADMISSIBLE space of record
     (G3 §6), not the unsupplied chain, and the answer is the same for every candidate
     chain the space admits — which is exactly what makes the unsupplied chain
     harmless for THIS consumer.
```

---

## 7. Scope — what the verdict binds to, exactly

```text
BINDING.  The verdict binds to the sealed working-class instance: the unit-4-cell
  record complex and its licensed refinements (A0/A1/A2 and composites; the five
  complexes of §3.2) — the same instance every artifact of this lineage binds to
  (738's energy solve, 795's coframe, 807's variety, the D0-SQUARE pair), and the
  instance on which the tasked admissible space (the 1887-dim variety + the 32-dim
  freedom) is DEFINED of record.

THE EXACT REOPENING CONDITION, stated rather than hidden.  dim H = dim ker(d_1) -
  rank(d_0) is precisely the first-cohomology dimension of the sealed 2-skeleton.  On
  ANY sealed record complex whose 2-skeleton has ker(d_1) = im(d_0) — in particular
  every licensed subdivision of a single 4-cell, and every cellulation of a
  contractible record region — phi_H ≡ 0 identically and this verdict carries over
  verbatim.  A sealed record complex with dim H > 0 (nontrivial first cohomology)
  would give phi_H a nonzero-dimensional range and REOPEN the AND-class question at
  that level.  NO such complex is sealed of record: the multi-cell/exhaustion side is
  UNBUILT (charter O-D1; port anchoring O12), and C_ref is a frozen AUDIT class with
  no sealed incidence realization (trap T1).  This is a scope boundary, not a caveat
  on the decision: the question as posed — over the sealed admissible space — is
  DECIDED, not conditional.
```

---

## 8. What would refute this

```text
R-1  An exact integer discrepancy between the reconstructed d_0/d_1 and the sealed
     constructive displays (censuses, laws, presentations).  Checked: all five
     censuses MATCH; d_1 d_0 = 0 entrywise on all five; the parent/A1/A2-F/A2-B d_0
     matrices are the D0-SQUARE lineage's cross-verified objects (1906 entries, 0
     discrepancies of record).
R-2  A sealed working-class complex with rank d_1 < E - (V-1).  Checked on all five:
     equality, at three primes each, with the rational sandwich closing exactly; and
     confirmed by full Fraction-exact rank on parent and A2-F.
R-3  A demonstration that the block split of the posed sub-law is defined against an
     ambient complex OTHER than the sealed tangent complex (V011 span 9bbd9525) — e.g.
     a sealed multi-cell realization with dim H > 0.  That is the reopening condition
     of §7, named; nothing of record supplies it.
R-4  A sealed constraint making the admissible set empty (then NEVER would be vacuous).
     Refuted by exhibit: §4's witness plus 807's sealed NONEMPTY.
```

---

## 9. Fences and audit

```text
EXACT STRUCTURAL ALGEBRA ONLY: integer matrix construction from sealed displays; exact
  mod-p ranks with a rational sandwich (807's own instrument); Fraction-exact RREF; no
  floats anywhere; no numeric value of any physical quantity computed, bounded, or
  evaluated; no scale, no imported GR, no faithfulness; the barred Omega_c route not
  taken (§5).  Structural integers and dimensions are quotations or exact ranks.
NO register / tracker / plan / road / ledger / lens file read.  No git action.
SEALS: 12 files verified at path before reliance (§0); 15 byte spans rehashed from the
  sealed bytes; the top-level same-named V011 copy NOT read (T6).
NOTHING SELECTED BY OUTCOME: the decision procedure (sandwich + second instrument +
  witness) was fixed before the first rank ran; all three outcome branches (IDENTICAL /
  NEVER / SUBVARIETY-with-witness-pair) were live, and UNDECIDABLE-AT was the standing
  default had the admissible space or the ambient complex been undetermined of record.
DISCLOSURES:
  (1) No sealed artifact stores dense d_1 matrices; they are determined uniquely (up to
      orientation conventions that cannot change rank) by the sealed d_1 law and the
      sealed complex presentations; d_1 d_0 = 0 verified entrywise in every case.
  (2) Z's 2-cells are derived from 807's sealed Freudenthal-of-each-subcube rule (807
      §2.1); 807 displays V/E and the 4-cells; the derived triangle census (1232) and
      the resulting kernel dimension agree exactly with 807's own sealed display
      (span 7ed9e192), which independently seals ker d_1 = im d_0 on Z.
  (3) The witness's port datum (e_out = e_in) is one admissible value of a carried
      parameter, not a selection: the verdicts hold at every port value, since the
      gauge block is invisible to both projections.
  (4) This artifact does NOT decide the flux-block response, the Gate-5 kernel C, the
      scalarization pair, mu, the winding, or any consumer question beyond the AND
      class; it does NOT supply the write chain, a member, an F-rule, or a stitching/
      continuum rule; it does NOT touch the materiality fork.
```

---

## 10. Script, verbatim (`and_class_independence_v001.py`, Python 3 stdlib only)

```python
#!/usr/bin/env python3
# and_class_independence_v001.py — AND-CLASS blind construction. Exact structural
# algebra only (integers, Fractions, exact mod-p ranks with a rational sandwich).
# No physical quantity computed, bounded, or evaluated.
#
# QUESTION: on every sealed complex of the working class, compute the exact
# dimensions of the three tangent blocks of V011's sealed complex
#     A^1 = im(d_0) (+) H (+) im(d_1^dagger),   H := ker(d_1) ∩ im(d_0)^perp,
# and hence decide the vanishing classes of phi_H and phi_f over the sealed
# admissible space of write realizations.
#
# SEALED INPUTS (constructions reproduced from the sealed byte spans):
#   d_0 / d_1 laws:  V011 packet member aa7c6d49... [44595,44690):
#                    (d_0 lambda)_e = lambda_t - lambda_s ;
#                    (d_1 a)_f = sum_(e in boundary f) incidence(f,e) a_e ; d_1 d_0 = 0.
#   H's definition:  V011 [44530,45446): the lift orthogonal to
#                    "ker(d_1) intersect im(d_0)^perp"; ker(d_0) = constants per component.
#   complexes:       crosscheck 614e20c8... [4056,5082): parent oriented unit 4-cube
#                    16/32/24; A1 grid {0,1,2}^4 81/216/216; A2-F Freudenthal chains of
#                    Boolean vertices 16/65/110; A2-B barycentric chains of nonempty
#                    4-cube faces 81/544/1232.
#   Z:               807 (9cf9b329...) presentation: vertices {0,1,2}^4/2 count 81,
#                    edges (u,v) v-u in {0,1}^4 minus {0} count 544, cells 384;
#                    2-cells derived from the sealed Freudenthal-of-each-subcube rule.
#   sealed control:  807 [6502,6638): dim H_Z = 544-(81-1) = 464 "with the
#                    80-dimensional kernel exactly vertex gauge" (= ker d_1 = im d_0 on Z).
#
# METHOD (exact, no sampling): for each complex,
#   (1) verify the census against the sealed displays;
#   (2) verify d_1 d_0 = 0 entrywise over Z (integer arithmetic);
#   (3) verify d_0 . 1 = 0 (constants in ker d_0)  =>  rank_Q d_0 <= V-1;
#   (4) compute rank_p d_0 and rank_p d_1 for p in {1000003, 1000033, 1000037}
#       (807's own exact-rank instrument: rank_Q >= rank_p always);
#   (5) SANDWICH: rank_p d_0 = V-1  =>  rank_Q d_0 = V-1 exactly; then d_1 d_0 = 0
#       gives rank_Q d_1 <= E-(V-1), and rank_p d_1 = E-(V-1)  =>  rank_Q d_1 =
#       E-(V-1) exactly  =>  ker d_1 = im d_0  =>  H = {0}. Exact, not probabilistic.
#   (6) for the two smallest complexes (parent, A2-F) additionally run a full
#       Fraction-exact RREF rank over Q as an independent second instrument.
#   (7) exhibit an explicit admissible witness member (exact integer vector) and its
#       block projections.
from fractions import Fraction
from itertools import product

PRIMES = [1000003, 1000033, 1000037]

# ---------- constructors (sealed constructive displays; lex cell order) ----------
def cube_complex(k):
    """grid {0..k}^4: vertices, positive edges, oriented squares.
    Square (v,i,j): boundary = +(v,v+ei) +(v+ei,v+ei+ej) -(v+ej,v+ej+ei) -(v,v+ej)."""
    V = sorted(product(range(k + 1), repeat=4))
    vi = {v: n for n, v in enumerate(V)}
    E = []
    for v in V:
        for i in range(4):
            if v[i] < k:
                w = list(v); w[i] += 1
                E.append((v, tuple(w)))
    E = sorted(E); ei = {e: n for n, e in enumerate(E)}
    F = []
    for v in V:
        for i in range(4):
            for j in range(i + 1, 4):
                if v[i] < k and v[j] < k:
                    F.append((v, i, j))
    def d1_rows():
        rows = []
        for (v, i, j) in F:
            a = list(v); a[i] += 1; a = tuple(a)          # v+ei
            b = list(a); b[j] += 1; b = tuple(b)          # v+ei+ej
            c = list(v); c[j] += 1; c = tuple(c)          # v+ej
            rows.append({ei[(v, a)]: 1, ei[(a, b)]: 1, ei[(c, b)]: -1, ei[(v, c)]: -1})
        return rows
    return V, vi, E, ei, F, d1_rows()

def chain_complex(V, comparable):
    """simplicial chain complexes: edges = strict comparable pairs (u,v);
    triangles = strict chains u<v<w with all three edges present.
    Triangle boundary = +(u,v) +(v,w) -(u,w)."""
    V = sorted(V); vi = {v: n for n, v in enumerate(V)}
    E = sorted((u, v) for u in V for v in V if comparable(u, v))
    ei = {e: n for n, e in enumerate(E)}
    F, rows = [], []
    for (u, v) in E:
        for w in V:
            if comparable(v, w) and (u, w) in ei:
                F.append((u, v, w))
                rows.append({ei[(u, v)]: 1, ei[(v, w)]: 1, ei[(u, w)]: -1})
    return V, vi, E, ei, F, rows

def d0_rows(E, vi):
    """(d_0 lambda)_e = lambda_t - lambda_s (V011 [44595,44690))."""
    return [{vi[s]: -1, vi[t]: 1} if vi[s] != vi[t] else {} for (s, t) in E]

# parent K and A1
PK = cube_complex(1)
A1 = cube_complex(2)
# A2-F: Freudenthal chains of Boolean vertices (u <= v coordinatewise, u != v)
def bool_leq(u, v):
    return u != v and all(u[k] <= v[k] for k in range(4))
A2F = chain_complex(list(product((0, 1), repeat=4)), bool_leq)
# A2-B: barycentric chains of nonempty 4-cube faces (digit 2 = free coordinate)
def subface(g, f):
    return g != f and all(f[k] == 2 or g[k] == f[k] for k in range(4))
A2B = chain_complex(list(product((0, 1, 2), repeat=4)), subface)
# Z: Freudenthal of each of the 16 A1 subcubes (807 presentation; grid units {0,1,2}^4).
# Edges: v-u in {0,1}^4 minus 0; triangles: steps with disjoint supports (all three
# vertices in one subcube <=> w-u in {0,1}^4).
def z_step(u, v):
    return u != v and all(v[k] - u[k] in (0, 1) for k in range(4))
Z = chain_complex(list(product((0, 1, 2), repeat=4)), z_step)

# ---------- exact instruments ----------
def check_d1d0_zero(d1r, d0r, nV):
    """verify d_1 . d_0 = 0 entrywise over Z (sparse integer product)."""
    for row in d1r:
        acc = {}
        for e, c in row.items():
            for v, c0 in d0r[e].items():
                acc[v] = acc.get(v, 0) + c * c0
        if any(x != 0 for x in acc.values()):
            return False
    return True

def check_constants_in_ker(d0r):
    return all(sum(r.values()) == 0 for r in d0r)

def rank_mod_p(rows, p):
    """sparse Gaussian elimination over GF(p); exact."""
    pivots = {}   # col -> reduced row (dict), pivot value normalized to 1
    rank = 0
    for row in rows:
        r = {c: v % p for c, v in row.items() if v % p}
        while r:
            c = min(r)
            if c in pivots:
                f = r[c]
                pr = pivots[c]
                for cc, vv in pr.items():
                    r[cc] = (r.get(cc, 0) - f * vv) % p
                    if r[cc] == 0:
                        del r[cc]
            else:
                inv = pow(r[c], p - 2, p)
                pivots[c] = {cc: (vv * inv) % p for cc, vv in r.items()}
                rank += 1
                r = {}
    return rank

def rank_exact_Q(rows, ncols):
    """dense Fraction RREF rank over Q; independent second instrument."""
    M = []
    for row in rows:
        v = [Fraction(0)] * ncols
        for c, x in row.items():
            v[c] = Fraction(x)
        M.append(v)
    m = len(M); r0 = 0
    for c in range(ncols):
        piv = next((r for r in range(r0, m) if M[r][c] != 0), None)
        if piv is None:
            continue
        M[r0], M[piv] = M[piv], M[r0]
        pv = M[r0][c]
        M[r0] = [x / pv for x in M[r0]]
        for r in range(m):
            if r != r0 and M[r][c] != 0:
                f = M[r][c]
                M[r] = [a - f * b for a, b in zip(M[r], M[r0])]
        r0 += 1
        if r0 == m:
            break
    return r0

# ---------- the run ----------
CASES = [
    ('parent K (unit 4-cube)', PK, (16, 32, 24), True),
    ('A1 (cubical bisection)', A1, (81, 216, 216), False),
    ('A2-F (Freudenthal)', A2F, (16, 65, 110), True),
    ('A2-B (barycentric)', A2B, (81, 544, 1232), False),
    ('Z (807 common refinement)', Z, (81, 544, 1232), False),
]
print('== BLOCK DIMENSIONS ON EVERY SEALED COMPLEX OF THE WORKING CLASS ==')
print('   (sealed censuses: crosscheck 614e20c8 [4056,5082); Z: 807 presentation,')
print('    2-cells derived from the sealed Freudenthal-of-each-subcube rule)')
all_H_zero = True
for name, (V, vi, E, ei, F, d1r), sealed, do_Q in CASES:
    d0r = d0_rows(E, vi)
    nV, nE, nF = len(V), len(E), len(F)
    print(f'\n-- {name} --')
    print(f'  census V/E/F = {nV}/{nE}/{nF}   expected {sealed[0]}/{sealed[1]}/{sealed[2]}'
          f'   -> {"MATCH" if (nV, nE, nF) == sealed else "MISMATCH"}')
    ok_dd = check_d1d0_zero(d1r, d0r, nV)
    ok_ct = check_constants_in_ker(d0r)
    print(f'  d_1 . d_0 = 0 entrywise over Z : {ok_dd}')
    print(f'  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = {nV - 1}) : {ok_ct}')
    r0p = [rank_mod_p(d0r, p) for p in PRIMES]
    r1p = [rank_mod_p(d1r, p) for p in PRIMES]
    print(f'  rank_p d_0 (p = {PRIMES}) : {r0p}')
    print(f'  rank_p d_1 (p = {PRIMES}) : {r1p}')
    sandwich0 = all(r == nV - 1 for r in r0p)
    print(f'  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = {nV - 1} '
          f'exactly : {sandwich0}')
    ub1 = nE - (nV - 1)
    sandwich1 = sandwich0 and all(r == ub1 for r in r1p)
    print(f'  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = {ub1}; '
          f'rank_p d_1 = {ub1}  =>  rank_Q d_1 = {ub1} exactly : {sandwich1}')
    if do_Q:
        rq0 = rank_exact_Q(d0r, nV)
        rq1 = rank_exact_Q(d1r, nE)
        print(f'  full Fraction-exact rank over Q (second instrument): '
              f'rank_Q d_0 = {rq0}, rank_Q d_1 = {rq1} '
              f'-> {"MATCH" if (rq0, rq1) == (nV - 1, ub1) else "MISMATCH"}')
    dim_im_d0 = nV - 1
    dim_flux = ub1
    dim_H = (nE - dim_flux) - dim_im_d0   # dim ker d_1 - dim im d_0
    print(f'  BLOCKS: dim im(d_0) = {dim_im_d0} ; dim im(d_1^dagger) = rank d_1 = '
          f'{dim_flux} ; dim H = ker(d_1) ∩ im(d_0)^perp = {dim_H}')
    print(f'  sum check: {dim_im_d0} + {dim_H} + {dim_flux} = '
          f'{dim_im_d0 + dim_H + dim_flux} = E = {nE} : '
          f'{dim_im_d0 + dim_H + dim_flux == nE}')
    all_H_zero &= (dim_H == 0) and sandwich1 and ok_dd and ok_ct and \
                  ((nV, nE, nF) == sealed)

# 807 sealed control on Z
print('\n== 807 SEALED CONTROL (span [6502,6638)) ==')
print('  sealed display: dim H_Z = 544-(81-1) = 464, "with the 80-dimensional kernel')
print('  exactly vertex gauge" — i.e. ker d_1 = im d_0 on Z of the sealed bytes.')
print(f'  my Z: rank d_1 = 464 = 544-80, dim H = 0 : '
      f'{"AGREES" if all_H_zero else "CHECK ABOVE"}')

# ---------- explicit admissible witness (parent complex) ----------
print('\n== EXPLICIT ADMISSIBLE WITNESS (parent complex; exact integers) ==')
V, vi, E, ei, F, d1r = PK
d0r = d0_rows(E, vi)
f0 = F[0]
w = d1r[0]                       # ell_w = d_1^dagger e_{f0}: the signed boundary of face 0
lab = {n: f"{''.join(map(str, e[0]))}->{''.join(map(str, e[1]))}" for e, n in ei.items()}
print(f'  face f0 = base {"".join(map(str, f0[0]))}, directions ({f0[1]},{f0[2]})')
print('  ell_w = d_1^dagger e_f0 =',
      ' '.join(f'{"+" if c > 0 else ""}{c}@{lab[n]}' for n, c in sorted(w.items())))
# conserved / gauge-free: ell_w . d_0 = 0 (row vector times matrix)
acc = {}
for e, c in w.items():
    for v, c0 in d0r[e].items():
        acc[v] = acc.get(v, 0) + c * c0
print(f'  d_0^dagger ell_w = 0 (conserved, no gauge component) : '
      f'{all(x == 0 for x in acc.values())}')
print(f'  ell_w != 0 : {any(c != 0 for c in w.values())}')
print('  ell_w in im(d_1^dagger) by construction => phi_f(ell_w) = ell_w != 0 ;')
print('  phi_H(ell_w) = 0 (H = {0}). Pattern (phi_f != 0, phi_H = 0) -> AND = FALSE.')

print('\n== VERDICT ==')
print(f'  dim H = 0 on EVERY sealed complex of the working class : {all_H_zero}')
print('  PHI_H_VANISHING = IDENTICAL (the projection P_H is the zero map; no')
print('    admissible member, variety point, or freedom value can move it).')
print('  PHI_F_VANISHING = NEVER on the admissible set (total-nonzero for n != 0:')
print('    phi_f + phi_H != 0 with phi_H = 0 forces phi_f != 0 at every member).')
print('  AND_CLASS = CONSTANT_FALSE (the harmonic sector is dead by exact dimension).')
```

---

## 11. Exact script output, verbatim (`python3 and_class_independence_v001.py`, exit code 0)

```text
== BLOCK DIMENSIONS ON EVERY SEALED COMPLEX OF THE WORKING CLASS ==
   (sealed censuses: crosscheck 614e20c8 [4056,5082); Z: 807 presentation,
    2-cells derived from the sealed Freudenthal-of-each-subcube rule)

-- parent K (unit 4-cube) --
  census V/E/F = 16/32/24   expected 16/32/24   -> MATCH
  d_1 . d_0 = 0 entrywise over Z : True
  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = 15) : True
  rank_p d_0 (p = [1000003, 1000033, 1000037]) : [15, 15, 15]
  rank_p d_1 (p = [1000003, 1000033, 1000037]) : [17, 17, 17]
  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = 15 exactly : True
  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = 17; rank_p d_1 = 17  =>  rank_Q d_1 = 17 exactly : True
  full Fraction-exact rank over Q (second instrument): rank_Q d_0 = 15, rank_Q d_1 = 17 -> MATCH
  BLOCKS: dim im(d_0) = 15 ; dim im(d_1^dagger) = rank d_1 = 17 ; dim H = ker(d_1) ∩ im(d_0)^perp = 0
  sum check: 15 + 0 + 17 = 32 = E = 32 : True

-- A1 (cubical bisection) --
  census V/E/F = 81/216/216   expected 81/216/216   -> MATCH
  d_1 . d_0 = 0 entrywise over Z : True
  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = 80) : True
  rank_p d_0 (p = [1000003, 1000033, 1000037]) : [80, 80, 80]
  rank_p d_1 (p = [1000003, 1000033, 1000037]) : [136, 136, 136]
  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = 80 exactly : True
  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = 136; rank_p d_1 = 136  =>  rank_Q d_1 = 136 exactly : True
  BLOCKS: dim im(d_0) = 80 ; dim im(d_1^dagger) = rank d_1 = 136 ; dim H = ker(d_1) ∩ im(d_0)^perp = 0
  sum check: 80 + 0 + 136 = 216 = E = 216 : True

-- A2-F (Freudenthal) --
  census V/E/F = 16/65/110   expected 16/65/110   -> MATCH
  d_1 . d_0 = 0 entrywise over Z : True
  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = 15) : True
  rank_p d_0 (p = [1000003, 1000033, 1000037]) : [15, 15, 15]
  rank_p d_1 (p = [1000003, 1000033, 1000037]) : [50, 50, 50]
  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = 15 exactly : True
  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = 50; rank_p d_1 = 50  =>  rank_Q d_1 = 50 exactly : True
  full Fraction-exact rank over Q (second instrument): rank_Q d_0 = 15, rank_Q d_1 = 50 -> MATCH
  BLOCKS: dim im(d_0) = 15 ; dim im(d_1^dagger) = rank d_1 = 50 ; dim H = ker(d_1) ∩ im(d_0)^perp = 0
  sum check: 15 + 0 + 50 = 65 = E = 65 : True

-- A2-B (barycentric) --
  census V/E/F = 81/544/1232   expected 81/544/1232   -> MATCH
  d_1 . d_0 = 0 entrywise over Z : True
  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = 80) : True
  rank_p d_0 (p = [1000003, 1000033, 1000037]) : [80, 80, 80]
  rank_p d_1 (p = [1000003, 1000033, 1000037]) : [464, 464, 464]
  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = 80 exactly : True
  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = 464; rank_p d_1 = 464  =>  rank_Q d_1 = 464 exactly : True
  BLOCKS: dim im(d_0) = 80 ; dim im(d_1^dagger) = rank d_1 = 464 ; dim H = ker(d_1) ∩ im(d_0)^perp = 0
  sum check: 80 + 0 + 464 = 544 = E = 544 : True

-- Z (807 common refinement) --
  census V/E/F = 81/544/1232   expected 81/544/1232   -> MATCH
  d_1 . d_0 = 0 entrywise over Z : True
  d_0 . (constants) = 0 (=> rank_Q d_0 <= V-1 = 80) : True
  rank_p d_0 (p = [1000003, 1000033, 1000037]) : [80, 80, 80]
  rank_p d_1 (p = [1000003, 1000033, 1000037]) : [464, 464, 464]
  SANDWICH d_0: rank_p = V-1 and rank_Q <= V-1  =>  rank_Q d_0 = 80 exactly : True
  SANDWICH d_1: d_1 d_0 = 0 => rank_Q d_1 <= E-(V-1) = 464; rank_p d_1 = 464  =>  rank_Q d_1 = 464 exactly : True
  BLOCKS: dim im(d_0) = 80 ; dim im(d_1^dagger) = rank d_1 = 464 ; dim H = ker(d_1) ∩ im(d_0)^perp = 0
  sum check: 80 + 0 + 464 = 544 = E = 544 : True

== 807 SEALED CONTROL (span [6502,6638)) ==
  sealed display: dim H_Z = 544-(81-1) = 464, "with the 80-dimensional kernel
  exactly vertex gauge" — i.e. ker d_1 = im d_0 on Z of the sealed bytes.
  my Z: rank d_1 = 464 = 544-80, dim H = 0 : AGREES

== EXPLICIT ADMISSIBLE WITNESS (parent complex; exact integers) ==
  face f0 = base 0000, directions (0,1)
  ell_w = d_1^dagger e_f0 = -1@0000->0100 +1@0000->1000 -1@0100->1100 +1@1000->1100
  d_0^dagger ell_w = 0 (conserved, no gauge component) : True
  ell_w != 0 : True
  ell_w in im(d_1^dagger) by construction => phi_f(ell_w) = ell_w != 0 ;
  phi_H(ell_w) = 0 (H = {0}). Pattern (phi_f != 0, phi_H = 0) -> AND = FALSE.

== VERDICT ==
  dim H = 0 on EVERY sealed complex of the working class : True
  PHI_H_VANISHING = IDENTICAL (the projection P_H is the zero map; no
    admissible member, variety point, or freedom value can move it).
  PHI_F_VANISHING = NEVER on the admissible set (total-nonzero for n != 0:
    phi_f + phi_H != 0 with phi_H = 0 forces phi_f != 0 at every member).
  AND_CLASS = CONSTANT_FALSE (the harmonic sector is dead by exact dimension).
```

---

## FLAG BLOCK

```text
PHI_F_VANISHING = NEVER(on the admissible set: the sealed total-nonzero constraint for
  n != 0 — phi_f + phi_H != 0, G3 §6 span ba7d1013 / GB span 57bd3233 — together with
  phi_H ≡ 0 forces phi_f != 0 at every admissible member, at every point of the
  1887-dim variety and every value of the 32-dim freedom; non-vacuous by exhibit —
  ell_w = d_1^dagger e_f0 = -1@0000->0100 +1@0000->1000 -1@0100->1100 +1@1000->1100,
  conserved, gauge-free, nonzero — and by 807's sealed NONEMPTY)
PHI_H_VANISHING = IDENTICAL(kinematic collapse: dim H = dim[ker(d_1) ∩ im(d_0)^perp]
  = 0 EXACTLY on every sealed complex of the working class — parent K 16/32/24, A1
  81/216/216, A2-F 16/65/110, A2-B 81/544/1232, Z 81/544/1232 — certified by the
  exact rational rank sandwich at three primes (rank_p d_1 = E-(V-1) with rank_Q d_1
  <= E-(V-1) from verified d_1 d_0 = 0 and rank_Q d_0 = V-1), doubly certified by full
  Fraction-exact ranks on parent and A2-F, and corroborated by 807's OWN sealed kernel
  display on Z (span 7ed9e192: "the 80-dimensional kernel exactly vertex gauge");
  hence P_H is the zero map and phi_H(ell) = 0 for every 1-cochain whatsoever)
AND_CLASS = CONSTANT_FALSE(the H sector is DEAD — by exact dimension, not by member
  selection: AND = [phi_f != 0 AND phi_H != 0] = FALSE at every admissible
  realization; the member-independence certificate is the rank identity itself (the
  projection P_H is the zero matrix, so no variety point, F-rule, or freedom value can
  move the pattern); the posed falsifier — a witness pair with different boolean
  patterns — CANNOT exist, every member realizing the same pattern (phi_f != 0,
  phi_H = 0))
CONSEQUENCE = the derived-form dichotomy resolves to the DEGENERATE ONE-SECTOR
  factorization R = R_flux ⊕ 0 at the sealed working-class instance, member-
  independently and kernel-independently (Phi_H ≡ 0 makes the cross pairing
  Phi_f^T C Phi_H vanish for EVERY Gate-5 kernel C), with no member selection; FORM
  §4.3's label "the dim-32 flux-free physical block H" is corrected (dim H = 0; the
  quoted 32 is the FR equal-energy assignment freedom, which lives in im(d_0) ⊕
  im(d_1^dagger) with nonzero flux part, consistent with the CV non-coboundary
  certificate); GB's "H inhabited" is refuted at the sealed carrier, completing GC's
  own minor-imprecision flag (span 0e491616); the EXACT reopening condition is named —
  a sealed record complex with dim H > 0 (nontrivial first cohomology of the sealed
  2-skeleton), which nothing of record supplies.
SEALS_VERIFIED = 22a2a478 POSED ; a5c71b2a POSE_CHECK ; bb1b88ad D0_SQUARE_CERT ;
  d83655ae D0_SQUARE_CHECK ; 5e49d209 FORM ; 3e35ffe2 G3_REALIZATION ; 9cf9b329
  B1A_CORRECTED_JOINT_SOLVE ("807") ; 4d072e76 FR ; 1a96e095 GB ; 8ed95b5a GC ;
  aa7c6d49 V011 packet member ; 614e20c8 CARRIER_CROSSCHECK — all recomputed at path
  by shasum -a 256 BEFORE reliance; 15 byte spans rehashed from the sealed bytes
  (hashes in §0); the top-level same-named V011 copy NOT read; no seal failed.
SCRIPT_REPRODUCED = yes (script verbatim §10; output verbatim §11; exit code 0;
  Python 3 stdlib only; no floats)
FORBIDDEN_IMPORTS = none (no scale, no imported GR, no faithfulness, no continuum
  object: the barred Omega_c-contractibility route NOT taken — the computation is the
  exact rank of the sealed record complex's own integer incidence matrices, the
  object the projections are defined against, at its sealed definition site; fence
  audit §5)
MACHINERY_INVOKED = no (reads, shasum -a 256 at path, byte-span extraction and
  rehashing, and one exact integer/rational script; no numeric evaluation of any
  physical quantity, no member binding, no fixed-point execution, no end test, no
  comparison to any measured constant; no register/tracker/plan/road/ledger/lens
  read; no git action; output name probed ABSENT before write; nothing selected by
  outcome — all decision branches, including UNDECIDABLE-AT, were live before the
  first rank ran)
alpha_computed=false, kappa_record_computed=false, proof_authorized=false
ALL_RESULTS = CLAIMED until checked.
```
