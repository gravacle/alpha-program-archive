# STAGE 8 — THE FORCING NOTION THE RECORD ACTUALLY TESTS WITH (O12SR) V001

DATE: 2026-08-15
COMMISSION: O12SR (FORCING-NOTION-BUILD). OBJECT: the PREDICATE beneath the allow/require
partition — which notion of "required"/"forced" each verdict actually uses, at bytes.
STATUS: DETERMINATION ONLY — display of sealed content and classification of the displays.
No reclassification of any row, no overturn of any verdict, no definition proposed, no
definition adopted. No value, no number, no measured-constant comparison.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: /Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_FORCING_NOTION_O12SR_V001.md — ABSENT at start of commission (probed before any
write; ls returned "No such file or directory").

---

## §0 — GROUND CONSUMED, SEALS VERIFIED

`shasum -a 256 -c` run FROM the artifact's own directory
(`/Users/bgm/MB Work/alpha-program-archive/workspace`), 9/9 OK, 0 mismatches:

```text
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md          (both inventories; the 8 sites)
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md    (its audit)
OK  STAGE8_PARTITION_THEOREM_T16SR_V001.md               (require half + allow half)
OK  STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md         (its audit; F-1)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md               (the two-model conservativity theorem)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md         (its audit)
OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md (the three require shapes)
OK  STAGE8_5D_REREAD_T13SR_V001.md                       (the nine forcing tests; freedom exhibits)
OK  STAGE8_5D_REREAD_T13SR_AUDIT_V001.md                 (its audit)
```

Operative sections read in full, not by grep, before any classification was written.

## §0.1 — SWEEP DECLARATION AND CUTOFF

```text
SWEEP A (generative-forcing language), BOTH ROOTS, UNCAPPED as commissioned:
  pattern (every lawful (generation|production|construction|history|way)|
  invariant across (every|all) (lawful|admissible) (generation|production|
  construction)|any lawful production|generative (forcing|notion)|forced by
  (creation|production|generation)). HITS: 2 files, both the same artifact in
  V001/V002 (STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2), at one line —
  "Pairing root: every lawful construction route and price [CLAIMED]". Read at
  its own bytes (§3 table): it enumerates AUTHORSHIP PRICES per construction
  family (P-HS / P-CT / P-NEW), not a forcing predicate. NOT a notion of
  forcing. Disposed.
SWEEP B (model-theory vocabulary), primary root: (two models|every model of|
  model-theoretic|models of the ground|model class). 19 files (one is a vendored
  sympy .py, excluded). Used for §3's import audit.
SWEEP C (native definition of "model"): (a model (of|is) ...(:=|means|is
  defined|is a structure)|MODEL NOTION|define[sd]? (a )?model|by a model we
  mean). HITS: 5 lines, ALL inside T16SR + its audit. Result carried in §3.
SWEEP D (model-class / producible-record coincidence): (model class .{0,60}
  (coincide|equal|same as|correspond)|models? (that )?(are|is) (physically )?
  realiz|realizable record|physical realizability of (a|the) model|every model
  .{0,40}(is a|corresponds to a) record). HITS: ZERO, both roots.
SWEEP E ("invariant across every/all ... realization/generation/production/
  construction/route/history/process"), both roots, fenced filenames filtered
  out of results: ZERO hits in non-fenced files.
SWEEP F (created-record premise): (records are created|record is created|
  creation of the record|created, not (found|discovered)|referent's creation).
  HITS: 3, of which one is operative — AJ line 693, consumed in §3.
FENCE EVENT, DISPLAYED: SWEEP E's raw pass surfaced ONE line from a file in the
  fenced class (a "...REGISTER..." filename). The file was NOT opened; the
  incidental line is NOT consumed, carries NO premise here, and is not quoted;
  it is recorded so the sweep's honesty is auditable. Its "Q-..." token is
  EXPECTED-UNLOCATABLE and was not resolved. No register/tracker/road/plan/
  continuation file was read this commission.
SWEEP CUTOFF = the nine commissioned artifacts at verified seals, read in full
  at their operative sections, PLUS sweeps A–F as declared (A and E uncapped per
  commission; B/C/D/F pattern-bounded, no cap reached — every match is displayed
  or disposed above). Directory listings (filenames only) for path resolution.
  NOTHING ELSE OPENED. No git.
```

---

## §1 — THE NOTION INVENTORY (deliverable 1)

### §1.0 The classification vocabulary (the commission's four, with the OTHERs named)

```text
MT   MODEL-THEORETIC — true in every model of the displayed clause set; the
     negative form is a two-model witness (two models of every displayed clause
     differing on the quantity). Operative locus: AJ §6.2, T16SR §2/§3.2.
PT   PROOF-THEORETIC — derivable from sealed stock; the warrant is a citation of
     a sealed derivation or a sealed demand, at its recorded grade (DERIVED).
CA   CONSTRUCTIVE/ALGEBRAIC — forced by an exhibited construction or identity:
     an equation SOLVED, a residual EXHIBITED, a generator DISPLAYED, a rank
     computed. The admissible set is produced, not quantified over.
OTHER, three named, because the record uses three predicates that are none of
     the above:
  DG   DISPLAY-GRADE (evidential). "Forced" = DISPLAYABLE at forced grade, at
       its own quantifier, from sealed artifacts. A link fails not because a
       counter-model exists but because the record does not carry the display.
       Operative locus: 5d §4.1 D1–D9.
  PR   PROVENANCE. "Forced" = every ingredient forced by the ground's self-
       consistency, NO AUTHORED INGREDIENT. A predicate about how the object was
       PRODUCED. Operative locus: AJ §2.3 (J-der).
  AS   ADMISSIBLE-SET CARDINALITY (route-neutral). "Forcing is a fact about the
       admissible set (it is a point)." A criterion that fixes WHAT forcing is
       without fixing HOW the set is computed — it is satisfiable by CA (solve
       the set) or by MT (quantify over models). Operative locus: the
       forcing-boundary adjudication §1 POINT 1.
```

### §1.1 The three sealed require-shapes (forcing-boundary adjudication, at its own lines)

OPERATIVE SENTENCE, its §1 POINT 1 lines 54–57, read at its own bytes:

> "Forcing has ONE criterion (the admissible set is a point) and THREE shapes: a single require
>  with a unique root; EXACT JOINT DETERMINATION (n conditions, n freedoms — the marker/C_R
>  case, which the reviewer's 'two ways' silently folds in); and overdetermined joint
>  determination (surplus conditions)."

```text
SHAPE-1 (single require, unique root)     NOTION = AS, route-neutral as stated.
SHAPE-2 (exact joint determination)       NOTION = AS, route-neutral as stated.
SHAPE-3 (overdetermined)                  NOTION = AS, route-neutral as stated.
```

CLASSIFICATION OF THE SHAPES THEMSELVES: **AS (OTHER)** — all three. The sealed taxonomy is a
taxonomy of the ADMISSIBLE SET'S GEOMETRY (point vs. not; how many conditions produced the
point), and it is silent on the computation route. Two further operative sentences of the same
artifact fix where its own weight falls:

> "(R2) ROOT STRUCTURE, DERIVED AND FAILURE-CAPABLE. Existence AND uniqueness of the solution
>  are THEOREMS, and the SOLVABILITY COUNTERFACTUAL is exhibited: what would have made the root
>  count 0, and what would have made it > 1"

> "Forcing is a fact about the admissible set (it is a point); anti-fitting is a fact about how
>  we know the point was not chosen."

DERIVED READING (display only): the sealed shapes commit to AS, and their certificate (R2)
demands EXISTENCE AND UNIQUENESS AS THEOREMS with an exhibited counterfactual — i.e. the
adjudication's own discharge route for AS is **CA/PT, not MT**. The word "model" does not occur
in the shapes, the criterion, or the certificates. NO MT ANYWHERE IN THE SEALED SHAPES.

### §1.2 REQUIRE-SIDE ROWS R-1 … R-10 (junction survey §2), notion identified at bytes

| # | OPERATIVE SENTENCE (the words that do the forcing work) | NOTION |
|---|---|---|
| R-1 | "integer-quantized by single-valuedness (CAS P3a/P3b: fractional winding leaves an exact residual −2)"; chain: "one condition (single-valued closure) whose admissible set is the integer lattice ℤ" | **CA**. An equation is SOLVED and a residual EXHIBITED (−2 at n = 1/2). No model quantifier in the row. MT arrives only later, at T16SR S5. |
| R-2 | "cocycle consistency + single-valuedness jointly pin the character lattice and its conjugation; the modulus theorem is an induction over the displayed operations (base + step + closure)"; corollary "on-record amplitude uniquely the trivial constant G = 1 (CAS T1)" | **CA + PT**. The lattice pinning is CA (identities checked); "an induction over the displayed operations" is PT (a proof schema over sealed stock). G = 1 is CA (identity solved). |
| R-3 | "The target form is FORCED; nothing in it is authored here"; chain: "a sealed demand fixing the FORM of any lawful supplier" | **PT**. The warrant is a SEALED DEMAND cited at its grade — derivability from sealed stock, not a model quantifier and not a construction. Terminates at a supplier CLASS. |
| R-4 | "tr H_A^Sigma = 0 and \|\|H_A^Sigma\|\|_2^2 = 0 EXACT (two independent cancellation mechanisms)"; chain: "two independent mechanisms deliver the same exact zeros — surplus conditions agreeing" | **CA**, doubled. Two exhibited cancellations. SHAPE-3's "overdetermination" is here two CONSTRUCTIONS agreeing, not two proofs and not a model class. |
| R-5 | "CLOSED EXACTLY (X-1): the a=0 slice of ker(qtilde_N) = the (R4) line span{(0,1,−R_K,N)}, dim 1"; chain: "bound ABOVE by (B2-7) faithfulness … met BELOW by the displayed (R4) generator" | **CA**. A generator is DISPLAYED and a bound computed; the point is produced, not quantified over. |
| R-6 | "dim ker(qtilde_N) is in {1,2} — no third extent exists"; chain: "rank-nullity on the displayed carriers forces a TWO-POINT admissible set". X-3: "value = 0 would force L_T,N = 0 against the sealed typing" | **CA** (menu, by rank-nullity) **+ PT** (X-3 is a reductio against a SEALED TYPING — sealed stock, not algebra). |
| R-7 | "r-2 B1-DERIVED: kappa_n → +infinity, full-family"; "DERIVED p = −3: no coincidence vanishing"; "one-vertex trace-parity lemma DERIVED"; "PINNED alpha-k in [1, 3/2]" | **PT**. Every warrant is a DERIVED-grade marker on a sealed derivation, consumed by citation. The chain-level sentence is "each a single displayed derivation with its own unique conclusion at its own quantifier". |
| R-8 | "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic, member-independent, kernel-independent. Unconditional at that stratum"; and "mirror collapse phi_f == 0 (F = 0 mandated) under the entered Attach TYPE-P premise" | **CA** (an identity) for the first; **CA-RELATIVE-TO-A-CARRIED-PREMISE** for the second — the row's own words: "forced RELATIVE to a carried TYPE-P premise and voids with it". |
| R-9 | "a surviving cross-block forces the CHANNEL (interference present, irreducible rank-one, no sector-respecting basis removes it), NOT the winding magnitude \|n\|" | **CA with an invariance quantifier** — "no sector-respecting basis removes it" quantifies over BASES (representations), not over models of a clause set. Conditional antecedent. The nearest thing on the require side to a quantified-over-a-class test, and its class is bases. |
| R-10 | "each alpha-forcing link displayed NOT displayable at forced grade"; the governing sentence is the 5d's: "A FORCED sentence requires every forcing link displayed at its own quantifier from sealed artifacts" | **DG (OTHER)**. Not one of MT/PT/CA. The nine links fail for want of a DISPLAY, not for want of a proof and not against a counter-model. See §1.5. |

```text
REQUIRE-SIDE TALLY (at bytes, by principal notion):
  CA  : R-1, R-4, R-5, R-8, R-9        (5 rows)
  CA+PT: R-2, R-6                      (2 rows, mixed)
  PT  : R-3, R-7                       (2 rows)
  DG  : R-10                           (1 row)
  MT  : ZERO ROWS. No require-side row of the junction survey's inventory
        establishes its forcing by quantifying over models of a clause set.
```

### §1.3 ALLOW-SIDE ROWS A-1 … A-13 (junction survey §3), notion identified at bytes

| # | OPERATIVE SENTENCE (the words that do the freeing work) | NOTION |
|---|---|---|
| A-1 | "TWO-MODEL WITNESS, executed: f₁ … and f₂ = f₁² satisfy every displayed clause simultaneously (AJ CAS M8/M9) and differ as functions"; "two models of every displayed clause exist that differ on the junction" | **MT**. The record's executed instance. |
| A-2 | "TWO-MODEL WITNESS of record: both extents are lawful over the displayed clauses"; "UNDECIDABLE-TODAY + positive two-model certificate" | **MT**. The record's second live instance, twice-determined. |
| A-3 | "WINDING_FORCED = NOT-FORCED for \|n\| absent an external n-blind comparand"; "no displayed clause pins \|n\|; any integer inhabits every displayed clause" | **MT-DEGENERATE (vacancy)**. The survey's own typing: "model freedom is textual (no clause to differ on)". |
| A-4 | "mu != 0 pinned SYMBOLICALLY ONLY, authored physics by principal act, sealed non-derivable; magnitude and sign OPEN (NO_VERDICT)" | **PR/DG (OTHER)** — an AUTHORED-ACT display plus a PT-NEGATIVE ("sealed non-derivable"). No model pair. |
| A-5 | "no new require reached the magnitude; the record did not supply the coupling content on this ground"; Act 5 RESOLVED (a) AUTHOR; "no value exists" | **DG + PR (OTHER)**. A statement about what the record CARRIES and how the content would ENTER, not about models. |
| A-6 | "declared EXPLICIT, SYMBOLIC, UNASSIGNED"; "any assignment, rescaling, or normalization … is a tuning surface of record" | **PR (OTHER)** — permission EXERCISED, displayed as an authored act. |
| A-7 | "distinct admissible sections are DISTINCT CANDIDATES; no canonical section is claimed"; the alternatives "displayed as rejected" / "displayed" | **SUPPLIER-MULTIPLICITY (OTHER)** — alternatives are EXHIBITED, but over SUPPLIERS of a type slot, not over models of a clause set. The nearest native analogue in the corpus to a production-side witness (see §3.3). |
| A-8 | "coefficient a_K != 0 undisplayed"; "never valued" | **MT-DEGENERATE (vacancy)**. |
| A-9 | "stays NO_VERDICT of record, value selection barred" | **DG (OTHER)**. A record-status verdict. |
| A-10 | "alpha' A SYMBOL"; the adopted spec content decides "a QUANTIFIER/TEST, not a verdict, a value, or a flag" | **DG (OTHER)** + spec-act typing. |
| A-11 | "exponent PINNED alpha-k in [1, 3/2]"; the location within the interval "UNDECIDABLE-TODAY, forced at displayed-hypothesis grade" | **DG (OTHER)** for the interior; the pin itself is R-7's PT. |
| A-12 | "adopted"; "frozen branch convention"; "supplies the selected boundary point"; "not a theorem" said in so many words at tau = 1 | **PR (OTHER)** — adoption displays: derived bound, then an unforced SELECTION, displayed as authored. |
| A-13 | "no sealed running target exists … of record 'would be a NEW PRINCIPLE,' a principal/spec-author act" | **DG (OTHER)** — an absence-of-artifact display. |

```text
ALLOW-SIDE TALLY (at bytes, by principal notion):
  MT (true two-model witness) : A-1, A-2                     (2 rows)
  MT-DEGENERATE (vacancy)     : A-3, A-8                     (2 rows)
  DG (record-status)          : A-5*, A-9, A-10, A-11, A-13  (5 rows)
  PR (authored act)           : A-4*, A-6, A-12              (3 rows)
  SUPPLIER-MULTIPLICITY       : A-7                          (1 row)
  (*A-4 and A-5 carry both DG and PR components; counted at their principal.)
  => TWO of thirteen allow-side rows rest on a two-model witness. The
     commission's motivating observation (a) is TRUE OF THE THEOREM-BEARING
     ROWS and FALSE AS A DESCRIPTION OF THE ALLOW SIDE AS A WHOLE.
```

### §1.4 THE PARTITION THEOREM (T16SR §2/§3), each half's notion at bytes

The theorem is the ONE place in the consumed ground where a single notion is declared for both
halves. Its declaration, verbatim (T16SR §2):

> "MODEL NOTION (the AJ's own, made explicit): a MODEL OF Σ is a structure interpreting the
>  displayed ground vocabulary (§1's symbol inventory) and satisfying every clause Σ1–Σ7;
>  junction/response quantities enter as EXPANSIONS by new symbols."

| Component | OPERATIVE SENTENCE | NOTION AS STATED | NOTION AS PROVEN |
|---|---|---|---|
| (I) REQUIRE-HALF | "In EVERY model of Σ, the quantization class holds: every sector/winding label admissible under Σ4's single-valued closure lies in ℤ — exactly ℤ, no more" | **MT** | **CA + a one-step PT bridge.** S3–S4 are exact algebra ("the full solution set of exp(2πi·x) = 1 is EXACTLY ℤ … [CAS PT3d]"); S5 is the bridge: "Derivability from the ground implies truth in every model of the ground." The MT statement is REACHED, not used. |
| (II) ALLOW-HALF | "In NO model of Σ is the crossing's magnitude forced … the expansion by profile f₁, the expansion by profile f₂, and the junction-free reduct are all again models of Σ" | **MT** | **MT, natively.** S7 (vocabulary disjointness) + S8 (invariance lemma: "the satisfaction of a clause depends only on the interpretation of the clause's own vocabulary") + S9 (the exhibited pair). The argument is semantic end to end; no solved admissible set anywhere. |
| (III) EXCLUSIVITY | (a) "a structure violating the class fails Σ4's own closure — it is not a model of Σ at all"; (b) "S8's invariance says any junction interpretation in M can be exchanged for a disagreeing one"; (c1) "the closure equation's solution set is a discrete label set (PT3d)"; (c2) "no such clause exists on the sealed surface" | **MT** | **(a) CA→MT via S5; (b) MT; (c1) CA; (c2) a VOCABULARY FACT — a lexical property of a finite sealed text, classified here as CA (an enumeration performed).** |

**THE BRIDGE, ISOLATED.** The single sentence that carries the require half from CA to MT is
S5, and it is used in both directions across the theorem:

> "Derivability from the ground implies truth in every model of the ground."

Forward (require half): derived ⟹ true in every model. Contrapositive (allow half): two models
disagree ⟹ not derivable. Both directions are SOUNDNESS. **No completeness step is taken
anywhere**, which is displayed here as a point in the record's favour: the theorem never needs
"true in every model ⟹ derivable", so the MT reading is never asked to do work soundness alone
cannot do. That is an accurate and conservative use of the imported apparatus.

**THE MODEL-CLASS FACT (audit F-1, at its own bytes).** The two halves do not run over the same
model class. The T16SR audit's finding, verbatim in its operative part:

> "The require-half's 'in EVERY model of Σ' is honest over the model class in which the
>  character notation carries its standard (continuous, exponential) denotation … but the
>  clauses AS ENUMERATED, with χ read as free unit-modulus vocabulary, admit wild-character
>  structures over which claim (I)'s wording loses its referent ('label admissible under the
>  closure' presupposes the exponential family). (II), (III)(b), (III)(c2), (III)(c3) are
>  denotation-robust (vocabulary disjointness only)."

And its per-claim verdict lines:

> "(I) REQUIRE-HALF … = CONFIRMED-WITH-CORRECTIONS (F-1: the model-class quantifier is honest
>  over the standard-denotation reading, which must be named …)"
> "(II) ALLOW-HALF … = CONFIRMED (denotation-robust; vocabulary disjointness verified …)"

DISPLAY: the require half is honest over a NARROWED model class (standard character
denotation, a premise the audit requires be NAMED); the allow half is honest over the FULL
class. One notion, two classes.

### §1.5 THE VACANCY/CONSERVATIVITY THEOREM (AJ §6.2) — the original of the pattern

> "**Theorem II.** For every Class II candidate — any adjoined exit operation, any profile — the
>  ground does not force it: there exist two models of every displayed ground clause that agree
>  on all ground content and differ on the junction. Hence no Class II candidate satisfies
>  J-der."
> "*Proof.* Derivability from the ground implies truth in every model of the ground. Exhibit two
>  models (TOYS, quarantined — §12) …"
> "**Scope note.** Theorem II quantifies over ALL exit operations and ALL profiles at once … it
>  attacks the forcing relation itself: the ground has no equation that could do the forcing."

NOTION OF THE THEOREM'S BODY: **MT** (two models; soundness contrapositive).

NOTION OF THE PREDICATE IT DISCHARGES: **PR**, and this is the deepest displayed fact in the
inventory. J-der is defined at AJ §2.3, verbatim:

> "**(J-der)** DERIVED: every ingredient forced by the ground's self-consistency — no authored
>  ingredient; an authored ingredient presented as forced voids the candidate (mandate pattern)."

J-der is a predicate about the CANDIDATE'S INGREDIENTS — about how the object was PRODUCED. It
is discharged by an MT argument. **A PROVENANCE predicate is tested by a MODEL-THEORETIC test.**
The theorem's own closing sentence names the substitution without flagging it:

> "Every Class II construction therefore contains at least one unforced — AUTHORED — ingredient
>  … By J-der (the mandate pattern), every Class II candidate is void as *derived*."

The inference runs: not entailed by Σ ⟹ some ingredient authored. It is valid ON THE ASSUMPTION
that a producer of the object has nothing but Σ to work from. That assumption is exactly the
coincidence audited in §4. It is nowhere stated in the AJ.

### §1.6 THE NINE FORCING TESTS (5d §4.1 D1–D9)

GOVERNING SENTENCE, at its own bytes:

> "A FORCED sentence requires every forcing link displayed at its own quantifier from sealed
>  artifacts."

| Test | OPERATIVE FAILURE PHRASE | NOTION |
|---|---|---|
| D1 CARRIER | "no derived connected physical b_1 >= 2 filled-face carrier exists; K_dd stands at ENTERED strength only … NOT DISPLAYABLE as forced" | **DG** (grade boundary: ENTERED vs DERIVED) |
| D2 SUPPLY (i) | "the domain extension is ENTERED, not derived … Entered content cannot anchor a FORCED link" | **DG** (grade boundary) |
| D3 SUPPLY (ii) | "the cell-local write-typing rule is UNWRITTEN of record; the verdict TURNS on it (T1 kills, T0/T2 pass)" | **DG** (absent instrument) |
| D4 GLUING | "the ledger identifications remain performed-not-licensed; no docket artifact licenses them" | **DG** (absent licence — a PERMISSION fact, not a truth fact) |
| D5 ATTACH PREMISE | "TYPE-P, carried, undischarged … the earned-trace determination's magnitude routes are NOT-DERIVABLE-TODAY" | **DG + PT-negative** (a carried premise; a sealed non-derivability) |
| D6 mu | "symbolic-only, authored, magnitude/sign OPEN … 'no value exists'. A forcing link cannot rest on a pending authored permission" | **DG + PR** (a pending AUTHORED act) |
| D7 POST-LIMIT | "the summed certification's own witnesses are LIVE, r-3/R-L4b is open … the Gate-5 cluster kernel does not exist of record" | **DG** (open quantifier + absent object) |
| D8 G2 PORT | "stopped of record — TYPE-R/TYPE-U, omega_phys STOPPED both routes, K4 NOT-MET" | **DG** (a typed STOP of record) |
| D9 G3 REALIZATION | "RA27 legs PARTIAL; eta_resp NOT-DERIVABLE-TODAY; the realization datum is not supplied" | **DG** (unsupplied datum) |

```text
NINE-TEST TALLY: DG = 9 of 9. MT = 0 of 9. CA = 0 of 9. PT enters only as the
NEGATIVE grade marker NOT-DERIVABLE-TODAY inside D5/D9.
The 5d verdict — the program's live verdict — is therefore established with a
notion of forcing that is neither model-theoretic nor constructive: it is
EVIDENTIAL. "Not forced" here means "the record does not display the link",
NOT "a counter-model exists" and NOT "the admissible set has two points".
```

---
## §2 — THE CONSISTENCY TEST (deliverable 2, the commission's centre)

### §2.1 Is ONE notion used throughout? — NO. Displayed exactly.

```text
NOTIONS IN ACTUAL USE ACROSS THE CONSUMED GROUND: five, plus one route-neutral
criterion and one one-off.
  AS  criterion (route-neutral)  the sealed three shapes                §1.1
  CA  constructive/algebraic     require rows R-1,4,5,8,9 (+2 mixed)    §1.2
  PT  proof-theoretic            require rows R-3,7 (+2 mixed)          §1.2
  MT  model-theoretic            allow rows A-1,A-2; both theorems      §1.3-§1.5
  DG  display-grade/evidential   allow rows A-5,9,10,11,13; ALL NINE
                                 forcing tests D1-D9; require row R-10  §1.6
  PR  provenance                 allow rows A-4,A-6,A-12; and J-der,
                                 the predicate Theorem II discharges    §1.5
  SUPPLIER-MULTIPLICITY (one-off)  allow row A-7                        §1.3
```

### §2.2 Do the two halves rest on DIFFERENT notions? — YES, and the split is DISJOINT

```text
THE MISMATCH, AT THE INVENTORY LEVEL, DISPLAYED EXACTLY:

  REQUIRE HALF (10 rows)          ALLOW HALF (13 rows)
  --------------------------      --------------------------
  CA   5 rows (+2 mixed)          CA   0 rows
  PT   2 rows (+2 mixed)          PT   0 rows
  DG   1 row (R-10)               DG   5 rows
  PR   0 rows                     PR   3 rows
  MT   0 ROWS                     MT   2 rows (+2 degenerate)
                                  SUPPLIER-MULTIPLICITY 1 row

  THE TWO PRINCIPAL NOTIONS ARE DISJOINT ACROSS THE HALVES:
    no require-side row is established model-theoretically;
    no allow-side row is established constructively or proof-theoretically.
  The commission's motivating hypothesis is CONFIRMED IN FORM and CORRECTED IN
  CONTENT: the halves do rest on different kinds of test — but the allow half's
  actual majority notion is not MT (2 of 13); it is DG (5 of 13, plus 3 PR).
  The MT half of the record is exactly: A-1, A-2, the AJ's Theorem II, and the
  partition theorem. Everything else on the allow side is EVIDENTIAL or
  AUTHORED-ACT display.
```

### §2.3 The one site where ONE notion governs both halves — and what happens there

At the partition theorem the notion IS single (MT, declared at §2 of that artifact). The
mismatch does not vanish there; it MOVES from the notion to the MODEL CLASS:

```text
PARTITION THEOREM, ONE NOTION, TWO CLASSES (audit F-1, per-claim verdicts):
  (I)  require half : MT over the NARROWED class — "honest over the model class
       in which the character notation carries its standard (continuous,
       exponential) denotation"; over the clauses AS ENUMERATED the class admits
       "wild-character structures over which claim (I)'s wording loses its
       referent". Audit verdict: CONFIRMED-WITH-CORRECTIONS, the reading "must
       be named".
  (II) allow half  : MT over the FULL class — "denotation-robust (vocabulary
       disjointness only)". Audit verdict: CONFIRMED.
  => Where the record uses ONE notion for both halves, the halves quantify over
     DIFFERENT MODEL CLASSES, and the difference is load-bearing for exactly the
     require half. The allow half needs less and gets more.
```

### §2.4 Would any classification CHANGE if the other half's notion were applied?

DISPLAY ONLY. Nothing below reclassifies any row; each entry states what the OTHER half's own
test would and would not reach.

**(A) Applying the ALLOW half's notion (MT over Σ, full model class) to the require side.**

```text
REACHABLE AT ALL: only R-1 and R-2. Sigma's symbol inventory is
  {m_*, T_R, H_R, hbar, |dS|, lambda} u {h, n, theta} (T16SR PT1; tau_R to be
  added per audit F-2). The carriers of R-3 (the crossing form), R-4
  (H_A^Sigma), R-5/R-6 (ker qtilde_N, R_K, L_T), R-7 (kappa_n, p, alpha-k),
  R-8 (cross-block, phi_f), R-9 (the channel), R-10 (the nine links) DO NOT
  OCCUR IN Sigma. The MT test cannot STATE eight of the ten require rows. This
  is not a change of classification; it is a change of REACH, displayed.
R-1 : NOTION-SENSITIVE, and the record says so itself. Under the allow half's
  own full model class the audit's F-1 holds: claim (I)'s wording "loses its
  referent". R-1's require status survives only with the Sigma4-READING named as
  a premise. DISPLAYED, NOT RECLASSIFIED — R-1 stands as sealed; what is
  displayed is that its establishment is not notion-invariant.
R-2 : SURVIVES. The audit's A-PT4 shows the consumed invariances hold under BOTH
  transition readings ("READING-ROBUST"); COR-1/COR-2 = CONFIRMED.
R-10: THE TEST INVERTS. Under MT, "not forced" means a counter-model exists.
  D1-D9 fail for absence of DISPLAY, and an absent display is not a counter-
  model. Under a strict MT reading the nine tests would be neither satisfied nor
  refuted — they are not MT-shaped statements at all.
```

**(B) Applying the REQUIRE half's notion (AS, discharged by CA/PT) to the allow side.**

```text
A-1, A-2 : SURVIVE. Two profiles / two extents are EXHIBITED objects; under AS
  that is a constructive demonstration that the admissible set has at least two
  members, i.e. is not a point. The two-model witness is ALSO a construction.
A-3, A-8 : SURVIVE VACUOUSLY. No condition is imposed on the quantity, so the
  admissible set is unconstrained — not a point.
A-4, A-6, A-7, A-12 : SURVIVE VACUOUSLY, same reason (an authored act imposes no
  condition to solve). The AS test grades them "not forced" but says nothing the
  adoption display did not already say.
A-5, A-9, A-10, A-11, A-13 : DO NOT SURVIVE AS ALLOW-SIDE. These five rest on DG
  — "no value exists", "NO_VERDICT of record", "a QUANTIFIER/TEST, not a
  verdict", "UNDECIDABLE-TODAY", "no sealed running target exists". The AS test
  asks whether the admissible set is a point; for these five THE RECORD HAS NOT
  COMPUTED THE SET. Under the require half's own notion their status would be
  UNDETERMINED-BY-THIS-TEST, not ALLOW. DISPLAYED, NOT RECLASSIFIED.
  (A-11 is the sharpest: its pin [1, 3/2] IS an admissible set, and it is not a
  point — so under AS its interior would read allow-side after all; the row is
  displayed as DG at bytes because the record's own words are "UNDECIDABLE-
  TODAY". Both readings are displayed; neither is adopted.)
```

### §2.5 The bridge inventory — what connects the notions, and what does not

```text
THE ONLY NOTION-BRIDGE THE CORPUS CARRIES is soundness, one sentence:
  "Derivability from the ground implies truth in every model of the ground."
  (AJ 6.2 proof; T16SR S5.) It bridges PT/CA -> MT, in both directions of use
  (forward for the require half, contrapositive for the allow half). No
  completeness step is taken anywhere; the apparatus is used conservatively.
NO BRIDGE EXISTS, at bytes, for:
  DG <-> MT   (an absent display is not a counter-model; a counter-model is not
              an absent display)
  DG <-> AS   (an undisplayed link is not a computed admissible set)
  PR <-> MT   (see 1.5: Theorem II SUBSTITUTES MT for PR without a stated
              bridge; the substitution is valid only under the coincidence
              audited in 4)
  PR <-> AS   (no sentence in the consumed ground relates ingredient-provenance
              to admissible-set cardinality)
Consequence, displayed: the record's two largest notion-populations - CA on the
require side and DG on the allow side - are connected by NOTHING. They are not
in conflict; they are incommensurable as displayed, and the partition's "line"
is drawn across that gap at 6 of its 23 rows (R-10 and A-5/9/10/11/13).
```

---
## §3 — THE IMPORT AUDIT OF THE PREDICATE ITSELF (deliverable 3)

### §3.1 Is model-theoretic entailment over a displayed clause set RECORD-NATIVE? — NO. IMPORTED.

```text
EVIDENCE 1 — THE NOTION IS USED BEFORE IT IS DEFINED.
  AJ (2026-08-14) uses it with no definition: "there exist two models of every
  displayed ground clause that agree on all ground content and differ on the
  junction"; "Derivability from the ground implies truth in every model of the
  ground." Neither "model" nor the inference is defined, derived, or cited.
EVIDENCE 2 — THE ONLY DEFINITION IN THE CORPUS IS ONE DAY OLD AND IS EXPLICITLY
  A RECONSTRUCTION OF THE PRIOR USE. SWEEP C returned 5 lines, ALL inside T16SR
  and its audit. The definition: "MODEL NOTION (the AJ's own, made explicit)".
  Made explicit — not derived, not booked, not adopted.
EVIDENCE 3 — THE DEFINITION IS VERBATIM STANDARD TARSKIAN SEMANTICS.
  "a structure interpreting the displayed ground vocabulary … and satisfying
  every clause Σ1–Σ7; junction/response quantities enter as EXPANSIONS by new
  symbols" — structure, interpretation, satisfaction, expansion; the companion
  term REDUCT is used in the same proof ("the junction-free reduct"). The
  bridging sentence "derivability … implies truth in every model" is the
  SOUNDNESS THEOREM of first-order logic, asserted without proof and without a
  record-native warrant. The invariance lemma S8 ("the satisfaction of a clause
  depends only on the interpretation of the clause's own vocabulary") is the
  standard coincidence/relevance lemma, likewise asserted.
EVIDENCE 4 — THE CHOICE LEDGER GRADES THE IMPORT BY CITING ITS OWN PRIOR USE.
  T16SR CH-1: "MODEL NOTION … FORCED in substance — it is the AJ Theorem II's
  own proof form". The justification for the notion is the artifact that first
  used it undefined. The import is INHERITED, never audited against the
  record's own mode of being. No CHOICE-LEDGER entry anywhere in the consumed
  ground classifies "use model-theoretic entailment" as a choice.

VERDICT (display): IMPORTED — from mathematical logic (Tarskian model theory +
  soundness). Used CORRECTLY and CONSERVATIVELY (no completeness step is ever
  taken; §2.5), but nowhere derived from, or reconciled with, anything the
  record says about what a record is.
```

### §3.2 The uncapped sweep for a RECORD-NATIVE (generative) notion of forcing

The shape sought, as the commission names it (named here as the search target, NOT proposed and
NOT adopted): a predicate of the form "invariant across every lawful generation/production of a
record". Sweeps A, D, E were run uncapped over both roots (§0.1).

```text
RESULT: EXACT ABSENCE.
  SWEEP A  2 hits, one artifact in two versions, one line: "every lawful
           construction route and price" — an enumeration of AUTHORSHIP PRICES
           per pairing family (P-HS/P-CT/P-NEW). Not a forcing predicate.
  SWEEP D  ZERO hits, both roots.
  SWEEP E  ZERO hits in non-fenced files, both roots.
NO ARTIFACT IN EITHER ROOT DEFINES, ADOPTS, OR USES A NOTION OF FORCING AS
INVARIANCE ACROSS THE LAWFUL PRODUCTIONS OF A RECORD. The absence is displayed,
not filled.
```

### §3.3 The four nearest approaches, displayed (none is such a notion)

```text
NEAREST-1  J-der (AJ §2.3): "every ingredient forced by the ground's self-
  consistency — no authored ingredient". THE CORPUS'S ONLY PRODUCTION-SIDE
  FORCING PREDICATE. It quantifies over the INGREDIENTS OF A CONSTRUCTION —
  i.e. over how the object was made. It is never discharged on its own terms:
  Theorem II discharges it model-theoretically (§1.5).
NEAREST-2  "its lawful provenance class UNINHABITED" (AJ, carried at 5d §4.3
  and T14SR A-1). A quantifier over PROVENANCES, returning empty — the closest
  SHAPE in the corpus to "across every lawful production". But "provenance"
  here means DERIVATION ROUTE ON THIS GROUND, not production of a record; the
  class is uninhabited because no derivation exists, not because no production
  does.
NEAREST-3  AJ line 693 (the conversion, in one sentence): "the referent's
  creation cannot be a derivation on this ground". The record states the
  substitution outright — a CREATION question is answered by a DERIVABILITY
  verdict. This is the import at its most visible, and it is displayed by the
  record itself without being flagged as a choice.
NEAREST-4  A-7's supplier multiplicity: "distinct admissible sections are
  DISTINCT CANDIDATES; no canonical section is claimed". Alternatives are
  exhibited over SUPPLIERS of a type slot — producers, not models. It is a
  candidate-face display, not a notion, and it is used on exactly one row.
```

### §3.4 The created-record premise, checked rather than assumed

```text
The commission names the corpus's ground premise as: records are CREATED.
SWEEP F located THREE hits; ONE is operative (AJ 693, NEAREST-3 above). I did
NOT locate, in the consumed ground or the declared sweeps, a booked sentence of
the form "records are created" standing as a named ground premise. What the
corpus carries instead, pervasively and at bytes, is the PRODUCTION VOCABULARY
that presupposes it — AUTHORED / ENTERED / ADOPTED / PERFORMED-NOT-LICENSED /
"by the principal's hand" / "an exercised permission" / "authorship prices" —
and one artifact (AXN) that enumerates lawful CONSTRUCTION ROUTES and prices
them. DISPLAYED AS FOUND: the premise is operative in the corpus's vocabulary
and its cost accounting; it is not, in what I read or swept, written as a clause
the forcing predicate was ever tested against.
```

---
## §4 — THE VULNERABILITY DISPLAY (deliverable 4)

### §4.1 The gap, stated exactly

A two-model witness establishes: the DISPLAYED CLAUSE SET does not entail the quantity. To carry
that to "the OBJECT is free" requires the model class of Σ and the class of producible records to
coincide — otherwise a quantity could be invariant across every producible record while
underdetermined by Σ, and both witnesses could fail to be records at all.

### §4.2 Does the corpus establish the coincidence? — NO. And its own discipline separates them.

```text
SWEEP D (model class <-> realizable/producible record), both roots: ZERO HITS.
No artifact asserts, argues, or assumes the coincidence.

STRONGER, AT BYTES: the record's own toy quarantine POSITIVELY SEPARATES the two
classes at the exact objects that carry the witness. AJ §12, verbatim:
  "(b) never asserted as surface content, candidates, or physics; …
   (d) constructed honoring the ground's own fiats (no log, no metric, no scale)
   precisely so that their admissibility as models is unimpeachable."
And in the battery's own comment bytes:
  "# TOY MODELS ONLY. f1, f2 are meta-level counterexample profiles about
   DERIVABILITY; neither is asserted as surface content anywhere."
The two witnesses are declared ADMISSIBLE AS MODELS and expressly NOT asserted
as surface content, candidates, or physics. The distinction a coincidence claim
would have to bridge is drawn by the record itself — and drawn in the direction
of SEPARATION. This is not a defect of the AJ: it is the AJ being exact. The
consequence for the predicate is nonetheless displayed here: the record has no
sentence licensing the step from "not entailed by Sigma" to "free in the object",
and its one relevant discipline runs the other way.

T16SR's scope clause is the nearest thing to an acknowledgement, and it fences
GROUND-CHANGE, not object-freedom: "A change of ground … RE-POSES the question;
the theorem forecloses nothing about changed ground." Ground-change is a clause
question. Producibility is not addressed anywhere.
```

### §4.3 Exactly which allow-side rows rest on a two-model witness

```text
VULNERABLE TO A GENERATIVE NOTION OF FORCING — the two-model rows, named exactly:
  A-1  THE MAGNITUDE PROFILE. "TWO-MODEL WITNESS, executed: f1 … and f2 = f1^2
       satisfy every displayed clause simultaneously … and differ as functions".
       The witnesses are the quarantined toys of §4.2.
  A-2  THE EXTENT BIT (E-1 vs E-2). "TWO-MODEL WITNESS of record: both extents
       are lawful over the displayed clauses"; "UNDECIDABLE-TODAY + positive
       two-model certificate", twice-determined.

SAME SPECIES, DEGENERATE FORM (description-relative, no clause to differ on) —
displayed for completeness because the survey itself types them this way ("model
freedom is textual (no clause to differ on)"):
  A-3  |n|.        A-8  the mixing coefficient a_K.

DERIVED OBJECTS THAT RIDE THE TWO-MODEL ROWS, and therefore inherit the exposure:
  * AJ Theorem II and the VACANCY-CLOSED verdict (rides A-1's witness);
  * the PARTITION THEOREM's half (II) and its exclusivity clause (III)(b)
    (rides the same pair, uniformly over the model class);
  * the junction survey's RIGID-AT-J-6 finding (its §5.1, quantifying over all
    models);
  * the junction survey's META-VERDICT JUNCTION-CONTINGENT, which is carried by
    the E-1/E-2 meta-level witness at J-2 (its §5.2) — i.e. the survey's overall
    verdict word rests on a two-model witness;
  * the 5d verdict's magnitude leg, "the magnitude map at the landed close is
    ALLOW-SIDE BY THEOREM" (§4.3) — the theorem being Theorem II.

NOT VULNERABLE TO A GENERATIVE NOTION, and displayed as such because the reason
is structural: these rows are ALREADY stated in production or record-status
terms, so a production-side test does not reach past them —
  PR rows  : A-4 (authored symbolic pin), A-6 (permission exercised, value
             withheld), A-12 (five adoption displays);
  DG rows  : A-5, A-9, A-10, A-11, A-13 (record-status: "no value exists",
             "NO_VERDICT of record", "a QUANTIFIER/TEST", "UNDECIDABLE-TODAY",
             "no sealed running target exists");
  A-7      : supplier multiplicity — alternatives exhibited over PRODUCERS.
  And the entire require side except R-1: eight of ten require rows are outside
  Sigma's vocabulary (§2.4A) and none is established model-theoretically (§1.2).
```

### §4.4 What is and is not displayed here

```text
DISPLAYED: (i) the coincidence is nowhere established, both roots, uncapped
  sweep; (ii) the record's own toy discipline separates the two classes at the
  witness objects; (iii) the exact rows and derived verdicts that rest on a
  two-model witness.
NOT DONE, and refused by fence: no row is reclassified; no verdict is overturned;
  no generative notion is defined, proposed, adopted, or applied to any row; no
  claim is made that any listed row is WRONG. The exposure displayed is a
  property of the WITNESS TYPE, not a defect found in any row.
```

---
## §5 — CAS RE-VERIFICATION (fresh venv; exact symbolic; displayed bytes only)

Environment: fresh venv under the session scratchpad, sympy 1.14.0; no evalf, no N(), no float
literal (mechanical scan: the only pattern hit is the fence-declaration comment itself). Exact
integers/rationals appearing (1, 1/2, 1/4, −2) are algebra residuals of identity checks on
ground-symbolic or quarantined-toy objects, never physical values. NO NEW CONTENT: the battery
re-verifies only the two displayed identities on which the CA-vs-MT classification turns.
Script sha256 `6fed7bed21f5fc214eaf21cc069ecec47933ac3a158f19f721058c28235431f0`. Output,
verbatim:

```text
O12SR NOTION-AUDIT CAS -- exact symbolic, zero numerics (sympy)
[O1] PASS -- R-1's admissible set is SOLVED to exactly the integers (a set produced, not a class quantified over): CONSTRUCTIVE/ALGEBRAIC marker confirmed
[O2] PASS -- R-1's non-integer failure is an EXHIBITED exact residual (-2 at label 1/2): the row's warrant is a construction, not a model quantifier
[O3] PASS -- A-1's witness f1 == sin^2(Delta/2) exactly (toy, quarantined as in the sealed build)
[O4] PASS -- A-1's two witnesses differ, exact separation residual 1/4 at the toy argument: |admissible set| >= 2 EXHIBITED (the witness is also a construction)
[O5] PASS -- the closure holds at SYMBOLIC integer n: the clause forces the CLASS and pins no member (R-1 require / A-3 allow boundary, re-verified)
TOTAL: 5 checks; FAIL count = 0
```

ONE FORMULATION REPAIR, DISPLAYED: the first run left the phase datum Δ untyped, so
`conjugate(exp(IΔ))` did not reduce and O3/O4 returned FAIL — a symbol-typing artifact, not a
content failure. Δ was typed real, as the sealed build types it, and both checks pass exactly.
No claim ever rested on the failed formulation. First-run script sha256
`00fede316ba5df894e5c482a752630f6cda30ea395c1df64e7da01101cde1dd9`, recorded so the repair is
auditable.

SUBSTANTIVE NOTE THE BATTERY DISPLAYS (O4): the two-model witness is ITSELF a construction —
two objects exhibited. It therefore satisfies the require half's AS criterion as well (§2.4B).
The mismatch of §2 is NOT that the allow half's evidence would fail the require half's test; it
is that the two halves' *populations* of notions are disjoint (§2.2), and that five allow rows
and one require row rest on notions the other half's test cannot reach at all (§2.4).

---
## §6 — VERDICT

```text
VERDICT = MIXED — exactly: TWO-NOTION-MISMATCH ∧ IMPORTED-PREDICATE.
  Both hold at bytes; neither alone states the finding, so both are given.

(1) NOT ONE NOTION. Five notions are in actual use across the consumed ground —
    MODEL-THEORETIC (MT), PROOF-THEORETIC (PT), CONSTRUCTIVE/ALGEBRAIC (CA), and
    two OTHERS the record uses without naming as notions: DISPLAY-GRADE (DG,
    "forced" = displayable at forced grade from sealed artifacts) and PROVENANCE
    (PR, J-der's "every ingredient forced … no authored ingredient") — plus the
    sealed shapes' route-neutral ADMISSIBLE-SET criterion (AS) and one
    supplier-multiplicity one-off.

(2) THE HALVES REST ON DIFFERENT NOTIONS, AND THE SPLIT IS DISJOINT.
    Require side (10 rows): CA 5 (+2 mixed), PT 2 (+2 mixed), DG 1, MT ZERO.
    Allow side (13 rows): DG 5, PR 3, MT 2 (+2 degenerate), supplier 1, CA ZERO,
    PT ZERO. No require-side row is established model-theoretically; no
    allow-side row is established constructively or proof-theoretically.
    THE COMMISSION'S HYPOTHESIS IS CONFIRMED IN FORM, CORRECTED IN CONTENT: the
    allow side's majority notion is NOT the two-model witness (2 of 13) — it is
    DISPLAY-GRADE (5) plus AUTHORED-ACT (3).

(3) WHERE ONE NOTION DOES GOVERN BOTH HALVES — the partition theorem — the
    mismatch moves to the MODEL CLASS: its require half is honest only over the
    narrowed standard-denotation class (audit F-1: over the clauses as
    enumerated, claim (I) "loses its referent"); its allow half is
    denotation-robust over the full class. One notion, two classes; the
    difference is load-bearing for the require half alone.

(4) CLASSIFICATIONS THAT WOULD NOT SURVIVE THE OTHER HALF'S TEST — named, NOT
    reclassified: under MT-over-Σ at the full model class, R-1's establishment
    fails without the Σ4-READING premise the audit requires be named, and eight
    of the ten require rows are not even STATABLE (their carriers are outside
    Σ's symbol inventory). Under AS/CA, the five DG allow rows (A-5, A-9, A-10,
    A-11, A-13) would read UNDETERMINED-BY-THIS-TEST rather than ALLOW.

(5) THE PREDICATE IS IMPORTED. Model-theoretic entailment over a displayed
    clause set is NOT record-native: it is used undefined in the AJ, defined for
    the first time one day later in T16SR as "the AJ's own, made explicit", in
    verbatim Tarskian terms (structure/interpretation/satisfaction/expansion/
    reduct) with the SOUNDNESS THEOREM asserted as its bridge; and the only
    choice-ledger entry touching it grades it FORCED by citing the artifact that
    first used it undefined. Its use is conservative and correct — no
    completeness step is taken anywhere — but it is imported, and it is nowhere
    reconciled with what the record says a record is.

(6) THE RECORD-NATIVE ALTERNATIVE IS DISPLAYED ABSENT. Uncapped sweeps over both
    roots for a forcing notion of the form "invariant across every lawful
    generation/production of a record": ZERO. The four nearest approaches are
    displayed (J-der; "lawful provenance class UNINHABITED"; the AJ's own
    sentence "the referent's creation cannot be a derivation on this ground";
    A-7's supplier multiplicity) and none is such a notion. The record's one
    production-side predicate, J-der, is discharged MODEL-THEORETICALLY.

(7) THE COINCIDENCE IS NOT ESTABLISHED, AND THE RECORD'S OWN DISCIPLINE RUNS THE
    OTHER WAY. Nothing in either root asserts that the model class of Σ
    coincides with the class of producible records (sweep: ZERO). The AJ's toy
    quarantine states the opposite separation at the exact witness objects:
    admissible "as models" is declared "unimpeachable" while the same objects are
    "never asserted as surface content, candidates, or physics". VULNERABLE ROWS,
    named exactly: A-1 and A-2 (two-model witnesses), with A-3 and A-8 as the
    degenerate textual form; and riding them, AJ Theorem II / VACANCY-CLOSED, the
    partition theorem's half (II) and (III)(b), the survey's RIGID-AT-J-6, the
    survey's overall JUNCTION-CONTINGENT verdict (carried by the E-1/E-2
    meta-witness), and the 5d verdict's magnitude leg. VULNERABILITY DISPLAYED,
    NOTHING RECLASSIFIED.

WHAT IS NOT CLAIMED: that any row is wrong; that any verdict should move; that a
  generative notion exists, should exist, or would decide anything. No definition
  is proposed and none is adopted. The exposure displayed is a property of the
  WITNESS TYPE, not a defect located in any row.
UNDECIDABLE-TODAY, EXACT: whether the two notions the record's two halves lean on
  are extensionally equivalent on this surface is NOT settled here and cannot be
  settled without a definition this commission is fenced from writing.
```

---
## §7 — IMPORT AUDIT SECTION (summary of §3, at commission grade)

```text
IMPORTED, NAMED:
  1. "model of Σ" — Tarskian structure/satisfaction. Source: mathematical logic.
     First corpus definition: T16SR §2, 2026-08-15, self-described as making the
     AJ's prior undefined use explicit.
  2. "derivability implies truth in every model" — the SOUNDNESS THEOREM.
     Asserted, never proved or booked; used in both directions (forward for the
     require half, contrapositive for the allow half).
  3. The coincidence/relevance lemma (T16SR S8: "satisfaction of a clause depends
     only on the interpretation of the clause's own vocabulary"). Asserted.
  4. "expansion" / "reduct" — the model-theoretic operations, used by name.
RECORD-NATIVE, NAMED (for contrast, so the audit is two-sided):
  a. J-der (PR) — ingredient provenance. Native; discharged by an import.
  b. DISPLAY-GRADE (DG) — "displayed at its own quantifier from sealed
     artifacts". Fully native: it is a predicate about the record's own
     artifacts. It carries the entire nine-test verdict and 5 of 13 allow rows,
     and it is the one notion in the corpus that is native to a record's mode of
     being. It is nowhere defined as a notion of forcing, and nowhere bridged to
     any other.
  c. the ADMISSIBLE-SET criterion (AS) and the three shapes. Native as stated
     (no model vocabulary), and route-neutral.
NOT PRESENT ANYWHERE, both roots, uncapped sweep: any notion of forcing as
  invariance across lawful generations/productions of a record.
```

## §8 — CHOICE LEDGER (commission O12SR; every unforced choice, classified)

```text
CH-1  THE FOUR-CLASS VOCABULARY EXTENDED BY THREE NAMED OTHERS (DG, PR, AS):
      FORCED in substance — the commission's own class list ends in "OTHER (name
      it)", and three predicates in the consumed ground fit none of MT/PT/CA. The
      names are descriptive labels for sealed language, not new objects; each is
      anchored to an operative sentence at bytes (§1.0).
CH-2  CLASSIFYING R-1 AS CA RATHER THAN MT: FORCED by the row's own bytes — the
      junction survey's R-1 cell contains no model quantifier; its warrant is a
      solved set and an exhibited residual (O1/O2). That the SAME content is
      given an MT statement one artifact later (T16SR (I)) is displayed, not
      merged: the notion audit is per-row-at-bytes, as commissioned.
CH-3  COUNTING A-1/A-2 AS THE ONLY TRUE TWO-MODEL ROWS, with A-3/A-8 as a
      degenerate form: FORCED by the survey's own CONSERVATIVITY PATTERN NOTE,
      which types A-3/A-8 as textual ("no clause to differ on") and separates
      them from the two witness rows in so many words.
CH-4  READING THE PARTITION THEOREM'S HALVES AS "one notion, two model classes"
      RATHER THAN "two notions": FORCED by the audit's per-claim verdict lines —
      (I) CwC over the narrowed denotation class, (II) CONFIRMED as
      denotation-robust. The audit, not this commission, draws the class line.
CH-5  TREATING THE SOUNDNESS SENTENCE AS THE CORPUS'S ONLY NOTION-BRIDGE:
      PREMISE(named) — established by sweep over the consumed ground plus sweeps
      B/C; a bridge sentence elsewhere in the corpus would not change any
      classification above, only the §2.5 gap display.
CH-6  CAS BATTERY SCOPE (5 checks, re-verification only): PREMISE(named) — this
      determination is textual/structural and stands without CAS; the battery
      re-executes displayed identities solely to certify the CA-vs-MT marker.
CH-7  DISPLAYING THE FENCE EVENT (a fenced-class filename matched sweep E and
      one line surfaced in raw grep output; file not opened, line not consumed,
      not quoted): FORCED — concealment would corrupt the sweep declaration.
CH-8  NOT ADJUDICATING extensional equivalence of the two notions: FORCED by the
      commission's own fence (no definition may be proposed or adopted); recorded
      as UNDECIDABLE-TODAY in §6 rather than left silent.
ZERO entries in class OPEN.
```

## §9 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The only toy objects touched are the AJ pair's own quarantined model profiles
  f1, f2, re-executed at O3/O4 EXACTLY as the sealed build quarantines them, and
  cited in §4 in their quarantine's own words. They enter this artifact ONLY as
  (i) the objects the A-1 row's witness consists of, and (ii) the objects whose
  declared status ("admissible as models"; "never asserted as surface content,
  candidates, or physics") is the §4.2 display. No toy value, profile, or model
  is asserted as surface content, candidate, or physics; none leaks into any
  classification, notion label, verdict clause, or flag. Every load-bearing
  object in §1–§4 is a sealed artifact's own operative sentence read at a
  verified seal, or an exact-algebra identity re-verified here.
  NO TOY NOTION: none of the seven notion labels is a toy or a proposal — each
  is a label for language already sealed in the corpus.
```

## §10 — FLAG BLOCK

```text
COMMISSION = O12SR (FORCING-NOTION-BUILD; which notion of "required"/"forced"
  the record actually tests with; display and classification only).
SEALS = 9/9 commissioned artifacts MATCH, shasum -a 256 -c run from the
  artifacts' own directory; MISMATCHES = 0; operative sections read in full.
NOTION_INVENTORY = COMPLETE (§1): 10 require rows, 13 allow rows, the partition
  theorem's three claims, the vacancy/conservativity theorem plus the predicate
  it discharges, the three sealed require-shapes, and the nine forcing tests —
  each with its operative sentence quoted and its notion identified at bytes.
  Classes used: MT, PT, CA, and three named OTHERS (DG display-grade, PR
  provenance, AS admissible-set), plus one supplier-multiplicity one-off.
CONSISTENCY_TEST = TWO-NOTION-MISMATCH, DISJOINT (§2): require side CA/PT-borne
  with MT = 0 rows; allow side DG/PR-borne with CA = 0 and PT = 0 rows, MT = 2
  rows. At the one site with a single notion (the partition theorem) the halves
  quantify over DIFFERENT MODEL CLASSES (audit F-1). Cross-application displayed
  both ways: R-1 notion-sensitive and 8 of 10 require rows not statable under
  MT-over-Σ; A-5/A-9/A-10/A-11/A-13 would read UNDETERMINED under AS. NOTHING
  RECLASSIFIED. The corpus's only notion-bridge is soundness; DG and PR are
  bridged to nothing.
IMPORT_AUDIT = IMPORTED (§3, §7): the model notion, soundness, the coincidence
  lemma, and expansion/reduct are imported from mathematical logic; used
  conservatively (no completeness step anywhere); defined in the corpus for the
  first time one day after first use, as a reconstruction of that use; graded
  FORCED in a choice ledger by citing the artifact that used it undefined.
  RECORD-NATIVE GENERATIVE NOTION = DISPLAYED ABSENT (uncapped sweeps A/D/E,
  both roots, ZERO). Four nearest approaches displayed; none is such a notion.
  The one fully record-native forcing predicate in the corpus is DG, which is
  never defined as a notion and never bridged.
VULNERABILITY = DISPLAYED (§4): the model-class / producible-record coincidence
  is NOWHERE established (sweep: ZERO), and the AJ's own toy quarantine
  separates the classes at the witness objects ("admissible as models"
  unimpeachable; "never asserted as surface content, candidates, or physics").
  ROWS RESTING ON A TWO-MODEL WITNESS: A-1, A-2 (with A-3, A-8 degenerate).
  RIDING THEM: AJ Theorem II / VACANCY-CLOSED, partition theorem (II) and
  (III)(b), the survey's RIGID-AT-J-6, the survey's overall JUNCTION-CONTINGENT
  verdict, the 5d verdict's magnitude leg. NOTHING RECLASSIFIED, NOTHING
  OVERTURNED, NO NOTION PROPOSED OR ADOPTED.
VERDICT = MIXED — exactly TWO-NOTION-MISMATCH ∧ IMPORTED-PREDICATE (§6), with
  one item recorded UNDECIDABLE-TODAY: whether the halves' notions are
  extensionally equivalent on this surface, unsettleable without a definition
  this commission is fenced from writing.
CAS = fresh venv, sympy 1.14.0, 5/5 PASS, exact symbolic only, zero numerics
  (scan: only the fence-declaration comment matches); script sha256 6fed7bed...;
  one formulation repair displayed (§5) with the first-run hash recorded.
SWEEP_CUTOFF = DECLARED (§0.1): nine commissioned artifacts at verified seals +
  sweeps A–F (A and E uncapped as commissioned; every match displayed or
  disposed) + filename listings. One FENCE EVENT displayed: a fenced-class
  filename matched sweep E; file NOT opened, line NOT consumed or quoted. No
  register/tracker/road/plan/continuation file read. "Q-..." tokens
  EXPECTED-UNLOCATABLE, none resolved.
CHOICE_LEDGER = CH-1..CH-8 (§8): 6 FORCED/FORCED-in-substance, 2 PREMISE(named);
  ZERO OPEN. TOY_SEPARATION = clean (§9).
DETERMINATION_ONLY = held: no row reclassified, no verdict overturned, no
  definition proposed, none adopted; no adoption, no authored physics, no value,
  no number as a value of anything, no measured-constant comparison; no gate,
  flag, witness, or status of any consumed artifact moved; nothing here
  discharges any named absence.
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT before
  first write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = the partition's predicate is not one predicate. The require half is
  carried by solved sets and cited derivations; the allow half is carried, in
  its majority, by what the record does or does not DISPLAY — with the celebrated
  two-model witness holding exactly two of thirteen rows. The one notion that
  spans both halves is imported from mathematical logic, defined in the corpus a
  day after it was first used, and it tests a DESCRIPTION; the one notion native
  to a record's own mode of being (display-grade) is never defined as a notion at
  all; and a notion native to records as CREATED — invariance across their lawful
  productions — is absent from both roots entirely. The line between allow and
  require is therefore drawn, at six of its twenty-three rows, across a gap that
  no sentence in the corpus spans.
```
