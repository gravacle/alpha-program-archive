# STAGE 8 TASK 5 / EQ6 — FINITE MAXWELL/HODGE PROJECTOR CERTIFICATE — LANE 2 V001

Date: 2026-08-04  
Custody: Codex Lane 2, derivation-first finite construction  
Register head verified before work: Q-466  
Status: fixed-stage Hodge license derived; full projector certificate blocked by actual support geometry

## Lead result

```text
LICENSE = DERIVED |
  DoR-019 changes the fixed-stage license. On each actual finite record
  complex, the ratified positive carrier metric and the sealed differential
  determine the adjoint, Laplacian, harmonic space, and orthogonal Hodge
  decomposition by finite-dimensional linear algebra. The historical kill
  forbade inferring Hodge NATURALITY from an isometry alone on an unlawful
  stand-in carrier; it did not forbid Hodge construction from a ratified
  metric and the actual differential.

PROJECTOR_CERT = BLOCKED |
  the resulting harmonic/Maxwell projector is not support-local on the
  actual reciprocal-loop complex. An edge-supported input with nonzero
  harmonic pairing projects to the full two-edge cycle. The same failure
  prevents a general contact-quotient descent unless the contact subspace is
  separately proved reducing. Neither DoR-019, DoR-020-A1, nor the strict
  contact pushout supplies that reducing/support theorem.

READER_REFERENCED = no |
  d, delta, Delta, the spectral projector, and the normalized Maxwell
  coefficient candidate are constructed before and without any reader,
  chi_K, T, response, threshold, fixed point, or end consequence.
```

The result distinguishes two questions that the earlier history had to keep
together. The finite Hodge decomposition now exists lawfully. The stronger
certificate required by physical J2 additionally asks that its projector be
compatible with actual support, contact, and every refinement arrow. That
stronger statement is false on the present actual support model.

```text
ALPHA_COMPUTED = false
PROOF_AUTHORIZED = false
KAPPA_RECORD_COMPUTED = false
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_RESPONSE_EVALUATED = false
```

---

## 0. Preflight, seals, and authority ledger

### 0.1 No-clobber and register

The commissioned filename and sidecar did not exist before construction.
The sealed questions-settled register verified byte-for-byte and ended at
Q-466. Q-466 names exactly the object built here and opens exactly the
metric-license question addressed in Section 1.

```text
OUTPUT_PREEXISTED = false | PASS
REGISTER_SEAL = PASS
REGISTER_HEAD = Q-466 | PASS
PREFLIGHT = PASS
```

### 0.2 Hash-verified authorities

| Authority | Verified SHA-256 | Content used |
|---|---:|---|
| `DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md` | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | ratified finite carrier metrics, Riesz maps, W3 precision, R4-only units |
| metric V005 | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | exact `g_K`, `g_C`, adjoint restrictions, quotient, unit algebra |
| Maxwell-symbol certificate build | `2aad6d8379777b8651a7e8dda78e9b085a69aa0203f455209eeb5e6e7da3adeb` | C1–C3 contract; Ward/contact/Hodge distinction |
| Q-408 physical kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual finite currents, differential Ward law, faithful kernel |
| F_PLDEC hostile check | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | circular `pi_Mx`, abstract-kernel, Hodge-from-isometry kills |
| DoR-020-A1 decision | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | actual support-local path/current and U(1)-bundle field laws |
| adopted where-clauses | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | bundle pullback, support transport, historical no-Hodge inference boundary |
| finite Ward/symbol package stock | `e21cee3081da06417109697352abe570c58dc6dda7d46e78f690d666bed128f5` | `C3_N^fin=(Ward_N^fin,sigma_N^fin)` and its exact scope |
| local orthogonal excision certificate | `d61a550a33bf1215c35f4d6f27cd2ec5d644b93a05e16d08fe3d43ded3416817` | OLD_FID/RNL/LR distinction and nonlocal-projector control |
| DoR-020-A4 strict contact pushout | `5fd95472bd6f0507a371779505fe91e2c3c4657ee3afc664ca563a3743d668a4` | current contact gluing law; no orthogonal-reducing claim |
| physical J2 attempt | `3fd4b924ec1a70f3e730bcf99f406d697c53937b26467d2ae6d5aa8c1bb6822c` | exact certificate interface and reciprocal-loop stopping witness |

Every named sidecar passed before the corresponding artifact was read.

### 0.3 Register sweep

The register and sealed corpus were searched for `Hodge`, `Laplacian`,
`adjoint`, `Maxwell projector`, `Ward`, `contact`, `support locality`,
`Riesz`, `DoR-019`, `pi_Mx`, and `reader`. The bearing record is:

```text
Q-383  DoR-019's metric/units candidate passes final review;
Q-408  actual finite Ward kernel exists; local Maxwell calculus does not;
Q-409  Hodge/contact is open because no licensed metric/local package was
       then assembled;
Q-415  Hodge from isometry and pi_Mx from reader are killed;
Q-430  an algebraically commuting Riesz map may mix support sectors;
Q-432  relative no-leakage replaces global orthogonality;
Q-444  local orthogonal excision is a genuine certificate, not automatic
       from positivity;
Q-465  strict contact pushout is ratified, without an orthogonal-splitting
       claim;
Q-466  the finite Maxwell/Hodge certificate is the named J2 residue and the
       post-DoR-019 license is explicitly reopened.
```

No settled row has already constructed the target certificate. In
particular, the local orthogonal excision certificate is not a Hodge
projector, and the strict contact pushout is not a reducing-subspace theorem.

---

## 1. G1 — the license theorem

### 1.1 What the historical kill actually forbade

The F_PLDEC witness made three substitutions relevant here:

```text
actual Q-408 kernel        -> abstract trace-class operator;
local/Hodge structure      -> identity localization plus a carrier isometry;
physical Maxwell projector -> a functional defined from the chosen reader.
```

Its Hodge claim relied on an isometric subdivision map `S` but did not prove
that `S` was a chain map for both the differential and its adjoint. The
explicit control was

```text
Delta_2 S(1)=(1,1) != (2,2)=S Delta_1(1).        (1-1)
```

Thus the valid negative theorem was:

> An isometry of carrier spaces, without an independently fixed metric
> differential complex and an intertwining theorem, does not determine a
> Hodge Laplacian or make Hodge projectors natural.

It was not the stronger statement “no Hodge decomposition can be formed on
a finite actual record complex after a positive metric is ratified.”

The DoR-020-A1 regression `Hodge from isometry = killed` has the same scope.
It prevents its support-local bundle lift from being relabeled a Hodge map.
It does not repeal a later construction using DoR-019 plus the actual
differential.

### 1.2 What DoR-019 adds

For each actual finite stage `N`, DoR-019 now fixes positive inner products
on the full retained finite carrier degrees used by the record complex. In
the established two-sector notation:

```text
Y_N = C_prop,N direct-sum K_N,
g_Y,N = g_C,N direct-sum g_K,N,                  (1-2)

R_C,N:C_prop,N -> C_prop,N^*,
R_K,N:K_N -> K_N^*.                              (1-3)
```

The actual incidence/connection complex supplies finite-rank spaces
`C_N^k` and sealed differentials

```text
... -> C_N^(k-1) --d_(k-1),N--> C_N^k
    --d_k,N--> C_N^(k+1) -> ...,

d_k,N d_(k-1),N=0.                               (1-4)
```

The degree-one conserved-current equation in Q-408 is the actual Ward face
of `(1-4)`. The A1 bundle member supplies the typed pullback of the same
differential; it does not supply the adjoint.

Because the metrics in `(1-2)` are now law, the adjoint is no longer an
authored choice. In Riesz notation,

```text
delta_k,N
 := R_(k-1),N^(-1) d_(k-1),N^* R_k,N.           (1-5)
```

Equation `(1-5)` uses only the ratified Riesz maps and the actual
differential. It selects no basis, orientation member, frame, refinement,
or reader.

### 1.3 Finite Hodge theorem

Every range of a linear map between finite-dimensional spaces is closed.
Hence

```text
im d_(k-1),N and im delta_(k+1),N are closed.     (1-6)
```

Define

```text
Delta_k,N
 := d_(k-1),N delta_k,N + delta_(k+1),N d_k,N,

H_k,N:=ker Delta_k,N
      =ker d_k,N intersect ker delta_k,N.         (1-7)
```

For `x in C_N^k`,

```text
<x,Delta_k,N x>
 =||delta_k,N x||^2+||d_k,N x||^2.               (1-8)
```

Thus `(1-7)` follows from positivity. Adjointness and `d^2=0` make the
three spaces below pairwise orthogonal:

```text
C_N^k
 = im d_(k-1),N
   orthogonal-direct-sum H_k,N
   orthogonal-direct-sum im delta_(k+1),N.        (1-9)
```

This is the finite Hodge decomposition of the actual record complex with
respect to the ratified metric. Equivalently, if `G_k,N` is the inverse of
`Delta_k,N` on `H_k,N^perp` and zero on `H_k,N`, then

```text
P_H,k,N
 =1-d_(k-1),N G_(k-1),N delta_k,N
    -delta_(k+1),N G_(k+1),N d_k,N              (1-10)
```

is the unique orthogonal projector onto `H_k,N`. The same object is the
zero-eigenvalue spectral projector `1_{0}(Delta_k,N)`. No choice among
degenerate eigenvectors is made.

### 1.4 Exact scope of the new license

The derivation proves `(1-9)` and `(1-10)` **at each fixed actual finite
stage** and under admitted automorphisms that intertwine `d` and preserve the
ratified metric. It does not prove, for a general refinement `f:N->M`,

```text
j_f P_H,N = P_H,M j_f.                            (1-11)
```

Equation `(1-11)` follows only if the actual transport intertwines `d` and
`delta`, equivalently reduces the Laplacians on the represented image. A
mere W3 isometry does not do that; `(1-1)` remains the permanent regression.

Therefore the license verdict is:

```text
FINITE_FIXED_STAGE_HODGE = DERIVED / TYPE-P
HODGE_FROM_ISOMETRY_ALONE = false / TYPE-R
HODGE_REFINEMENT_NATURALITY = requires_chain_and_adjoint_intertwiner
LICENSE = DERIVED
```

The A1 historical boundary does not block the fixed-stage theorem. It still
blocks the unjustified naturality promotion.

---

## 2. G2 — projector construction and the actual support test

### 2.1 Ward, harmonic, and Maxwell pieces

Apply Section 1 to the degree carrying the actual finite connection/current
symbol. Write

```text
W_N:=ker delta_N                         Ward/co-closed sector;
H_N:=ker d_N intersect ker delta_N       harmonic sector;
P_W,N and P_H,N                          orthogonal projectors. (2-1)
```

The finite ranges are closed by `(1-6)`, so these projectors exist without a
closed-range assumption. The finite local symbol shadow from the C3 stock
is reader-independent:

```text
Loc_N^fin(K):=sigma_N^fin(K).                     (2-2)
```

Let `m_T,N` denote the harmonic Maxwell symbol generated by the normalized
transverse kernel:

```text
m_T,N:=P_H,N Loc_N^fin(Kernbar_N Q_N(L_T,N)).     (2-3)
```

The normalization branch is lawful only when

```text
m_T,N!=0.                                         (2-4)
```

On that branch, the reader-independent scalar coefficient candidate is

```text
pi_Mx,N^H(s)
 := <m_T,N,P_H,N s> / <m_T,N,m_T,N>.             (2-5)
```

If the harmonic Maxwell summand has dimension greater than one, `(2-5)` is
the coefficient of the declared `L_T` line inside that summand; the residual
harmonic directions remain visible and no basis is selected. If `(2-4)`
fails, the normalization denominator vanishes and the candidate is void.

Construction order is

```text
metric + d -> delta -> Delta -> P_H
 -> actual finite symbol -> m_T -> pi_Mx^H.       (2-6)
```

No reader occurs in `(2-6)`.

### 2.2 R4-only unit calculation

`R_k` in `(1-5)` carries the corrected `U_k^-2` Riesz class. The two Riesz
maps in the adjoint definition cancel the domain/codomain unit conversion
required by `d^*`; `Delta` and `P_H` are endomorphisms of one unit class.
In `(2-5)`, numerator and denominator carry the same squared Maxwell-symbol
unit, so their ratio is dimensionless. The only action/carrier passage is
the already ratified R4 seam used before `Q_N`/`Kernbar_N`.

```text
IMPLICIT_CROSS_SECTOR_UNIT = none
NU_FIXED = false
PI_MX_OUTPUT_UNIT = dimensionless
```

### 2.3 The required support-locality equation

The certificate named by Q-466 requires the physical projector to respect
the actual support filtration. At minimum, for every local region `O`,

```text
supp(P_H,N s) subset supp(s),

or equivalently

P_H,N A_N(O) subset A_N(O),                       (2-7)
```

where `A_N(O)` is the actual Q-408 analysis/current subspace supported in
`O`. A contact quotient additionally needs

```text
P_H,N(I_contact,N) subset I_contact,N.            (2-8)
```

Equation `(2-8)` is exactly the condition for `P_H,N` to descend to the
contact quotient. The strict pushout of DoR-020-A4 identifies represented
and boundary values on their overlap; it does not assert `(2-8)`.

### 2.4 Actual reciprocal-loop counterexample

Take the actual reciprocal-loop finite complex with its two oriented edge
corridors `a,b`. Its record-visible harmonic cycle space is one-dimensional:

```text
H_N=span{c},
supp(c)={a,b}.                                    (2-9)
```

Let `epsilon_a,epsilon_b` be the two edge-local finite inputs. Positivity
gives

```text
0 < <c,c>
  = <epsilon_a,c>-<epsilon_b,c>                  (2-10)
```

after the sealed orientation signs are included. Therefore at least one
member `epsilon_e` of the full exchange-covariant edge orbit satisfies

```text
<epsilon_e,c> !=0.                               (2-11)
```

No physical edge is selected: the regression is the whole orbit and the
proof only needs the existence forced by `(2-10)`.

The Hodge projection is then

```text
P_H,N epsilon_e
 =(<epsilon_e,c>/<c,c>)c.                        (2-12)
```

Its support is `{a,b}`, while `epsilon_e` is supported on one edge. Hence

```text
supp(P_H,N epsilon_e) not_subset supp(epsilon_e). (2-13)
```

Equation `(2-13)` is an actual-surface counterexample, not an abstract
Hilbert-space mixer. It uses the ratified metric, the actual incidence
cycle, and an actual edge-local input. It is also independent of the metric
coordinates: positivity and the spanning identity `(2-10)` guarantee a
nonzero member of the exchange orbit for every admitted positive metric.

Thus the fixed-stage Hodge projector exists but is globally supported on a
connected harmonic cycle, as Hodge projectors generally are.

```text
SUPPORT_LOCALITY_OF_P_H = false / TYPE-R
WITNESS = reciprocal_loop_edge_to_full_cycle_(2-12)
```

### 2.5 Contact compatibility

If `epsilon_e` in `(2-12)` lies in an actual contact-supported subspace while
the other corridor is outside that contact support, then `(2-12)` also gives

```text
P_H,N epsilon_e notin I_contact,N,                (2-14)
```

and the contact quotient descent fails outright. The present sealed stock
does not force that antecedent at every stage, so `(2-14)` is retained as a
failure-capable contact witness rather than promoted to a universal clash.

What is universal is the missing reducing theorem `(2-8)`. Neither the
Ward identity, strict pushout, OLD_FID, RNL, nor LR proves it. The local
orthogonal excision certificate expressly warns that a positive orthogonal
projector agrees with geometric support excision only after a separate
zero-defect equation. No such equation identifies `P_H,N` with the actual
geometric excision on the full family.

```text
CONTACT_QUOTIENT_DESCENT = NOT_CERTIFIED / TYPE-U
STRICT_PUSHOUT_IMPLIES_HODGE_REDUCING = false / TYPE-R
```

### 2.6 Why a local substitute is not the Hodge projector

One could retain the actual geometric excision projector `E_N^geom`, which
is support-local by construction on its proved scope. But replacing
`P_H,N` with `E_N^geom` would require

```text
E_N^geom=P_H,N                                   (2-15)
```

on the physical symbol class. Equation `(2-13)` refutes `(2-15)` on the
reciprocal loop for a one-edge region. Gram-Schmidt rotation would alter
the actual current/support and is forbidden. Therefore no support-local
workaround is installed.

---

## 3. G3 — certificate assembly and J2 interface

### 3.1 Field-by-field ledger

The named certificate has the following exact status.

| Certificate field | Premise/proof | Status | Void condition |
|---|---|---|---|
| actual finite record complex | Q-408 incidence/current and A1 bundle member | **BUILT** | abstract kernel or unrelated surface |
| positive carrier metrics | DoR-019 `g_C,g_K` | **BUILT / RATIFIED** | null direction or changed finite form |
| `d_N` | sealed record/connection differential | **BUILT** | `d_N^2!=0` or wrong bundle pullback |
| `delta_N` | Riesz adjoint `(1-5)` | **DERIVED** | implicit unit conversion or wrong adjoint |
| closed ranges | finite rank `(1-6)` | **DERIVED** | infinite/completed promotion |
| Hodge decomposition | `(1-7)`–`(1-10)` | **DERIVED** | nonpositive metric or noncomplex |
| finite Ward/local-symbol shadow | Q-408/C3 `(2-2)` | **BUILT** | Ward or actual-kernel mismatch |
| normalized Maxwell coefficient candidate | `(2-3)`–`(2-5)` | **DERIVED ON `m_T!=0` BRANCH** | zero denominator or missing R4 seam |
| reader independence | construction order `(2-6)` | **PASS** | any use of `ell`, `chi`, or `T` |
| contact quotient descent | reducing equation `(2-8)` | **OPEN** | one contact vector projected outside ideal |
| support locality | equation `(2-7)` | **REFUTED** | witness `(2-12)` |
| refinement naturality | chain+adjoint intertwiner `(1-11)` | **OPEN GENERALLY** | historical Laplacian witness `(1-1)` |
| covariance/reality | fixed-stage functional calculus under actual unitary/antiunitary automorphisms | **DERIVED ON THAT SCOPE** | automorphism fails metric/differential intertwining |
| batching | componentwise finite Hodge construction only | **DERIVED ON SEALED PRODUCT SCOPE** | joint scalar contraction |
| DoR-008 bottom | no finite kernel or reader value changed | **PASS** | altered sealed finite shadow |

### 3.2 Certificate verdict

`FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT` is conjunctive. A single
failure blocks it. The actual support failure `(2-13)` therefore gives

```text
FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT
 = BLOCKED(
     support_locality_refuted_by_reciprocal_loop;
     contact_reducing_and_general_refinement_naturality_unproved
   ).                                             (3-1)
```

This is stronger and more informative than the Q-466 absence. The metric
does construct Hodge theory; it also exposes why the requested physical
locality certificate cannot simply be Hodge orthogonal projection.

### 3.3 What a successful certificate would hand J2

If a future law changes the locality contract or supplies a different
reader-independent **local Maxwell coefficient** compatible with actual
support/contact, a complete certificate would hand J2 exactly:

```text
O_prof,N --Q_N/Kernbar_N--> Ker_N^Q408
         --Loc_N--> Sym_N^loc/I_contact,N
         --pi_Mx,N--> R,                         (3-2)
```

with `(3-2)` total, normalized, covariant, reality-compatible, unit-correct,
and natural on the admitted finite arrows. It would make the right-hand
side of J2 **posable**. It would not select `(chi_N,T_N)` and would not prove
the equality to the algebraic reader; that comparison remains the next
theorem.

Because `(3-1)` blocks the current certificate, this artifact does not pose
J2 and assigns no scalar to a Q-408 profile.

```text
J2_RIGHT_HAND_COMPOSITE_MADE_TOTAL = false
J2_POSED = false
READER_MEMBER_SELECTED = none
```

---

## 4. G4 — honesty clause: geometry, not rails

The decomposition `(1-9)` is real mathematics on the actual finite carrier.
The obstruction `(2-13)` is equally real geometry: harmonic projection is
global along a cycle. Neither is a rail artifact.

The rail-level mistake would be to demand that an arbitrary Hilbert
orthogonal projector automatically preserve physical supports. The local
orthogonal excision program already separated these notions:

```text
positive metric projector exists
  !=
projector agrees with actual geometric support excision.       (4-1)
```

`(2-13)` is the explicit failure of the right-hand side for the Hodge
projector. The contact issue is correspondingly exact: a quotient receives
the projector only when its ideal is reducing, and the strict pushout does
not state that theorem.

No impossibility is inferred for every conceivable physical Maxwell
coefficient map. What is refuted is the commissioned conjunction

```text
orthogonal harmonic/Maxwell projector
AND support-local projector
AND contact-compatible projector                (4-2)
```

on the current actual support model.

---

## 5. G5 — batteries and regressions

### 5.1 First battery item: F_PLDEC circularity

The dependency graph is `(2-6)`. `ell_(chi,T)`, `chi_K`, and `T` do not
occur in Sections 1–2. The candidate coefficient `(2-5)` is fixed by the
ratified metric, actual differential, actual finite symbol, and the declared
`L_T` line. It is not defined by asking which scalar the reader returns.

```text
F_PLDEC_CIRCULAR_PI = EXCLUDED / PASS
ABSTRACT_KERNEL_SUBSTITUTION = false
READER_REFERENCED = no
```

### 5.2 Hodge-from-isometry distinction

| Claim | Status | Reason |
|---|---|---|
| fixed-stage Hodge from ratified metric + actual `d` | **DERIVED** | `(1-5)`–`(1-10)` |
| Hodge from an isometry alone | **REFUTED** | `(1-1)` |
| Hodge naturality under an isometric chain-and-adjoint intertwiner | **DERIVED CONDITIONALLY** | functional calculus commutes with the intertwiner |
| Hodge naturality on every `I_F` arrow | **OPEN/REFUTED AS AN AUTOMATIC INFERENCE** | no universal adjoint intertwiner |
| support locality from Hodge orthogonality | **REFUTED** | `(2-12)`–`(2-13)` |

The new metric changes the first row only. It does not erase the other
counterexamples.

### 5.3 Nine geometric regressions

| Regression | Execution | Result |
|---|---|---|
| pure new-cycle profile | receives its own fixed-stage Hodge complex; no old-stage upward quotient | **PASS** |
| covariance orbit/moduli | spectral subspaces transported as full families under actual metric/differential automorphisms | **PASS ON PROVED SCOPE** |
| all-stage skeleton | fixed-stage theorem not promoted to general refinement naturality | **PASS** |
| rank-preserving `Ref_path` | W3 isometry retained; Hodge commutation requires the additional `d/delta` square | **PASS WITH HONEST BOUNDARY** |
| cycle-creating upward quotient | none defined | **PASS** |
| arbitrary-profile restriction | target new-cycle harmonic content retained | **PASS** |
| old-to-new Riesz mixer | cannot pass `(2-7)`/LOE merely by commuting algebra | **PASS — REJECTED** |
| `P=id` overreach witness | admitted; no global old/new orthogonality imposed | **PASS** |
| bundle/topology attack | actual A1 bundle/differential required before `(1-5)` | **PASS** |

### 5.4 Further falsifiers

1. **Zero normalization.** `m_T,N=0` voids `(2-5)`; no denominator is
   inserted by hand.
2. **Contact leakage.** A contact vector whose Hodge image has noncontact
   support refutes quotient descent.
3. **Refinement mismatch.** Any arrow with `j_f Delta_N!=Delta_M j_f`
   refutes projector naturality; `(1-1)` is permanent.
4. **Ward-only shortcut.** Co-closedness does not distinguish the harmonic
   Maxwell line from other co-closed content.
5. **Local-excision substitution.** `E^geom` cannot replace `P_H` without
   the equality `(2-15)`.
6. **Hidden trace.** No operator trace, averaging, or C/K conversion outside
   R4 occurs.
7. **Support erasure.** Quotienting away the spread component in `(2-12)`
   would delete a record-visible cycle and is void.

### 5.5 Anti-tuning ledger

```text
1  Verify Q-466 and the no-clobber condition.
2  Freeze DoR-019's metric and unit classes before reopening Hodge.
3  Restate the exact historical kill before constructing an adjoint.
4  Construct delta and Delta on the whole actual finite complex.
5  prove finite Hodge decomposition before inspecting support or a reader.
6  define the candidate Maxwell line from L_T, never from ell.
7  run support/contact tests on the actual reciprocal-loop geometry.
8  stop at the first failed certificate field; do not alter the projector.
9  retain every orientation/edge/refinement orbit and select no member.
10 inspect no response, threshold, fixed point, end test, or measured datum.
```

---

## Final board

```text
FINITE_CLOSED_RANGE = DERIVED
FINITE_HODGE_DECOMPOSITION = DERIVED
HODGE_PROJECTOR_FIXED_STAGE = BUILT
HODGE_FROM_ISOMETRY_ALONE = REFUTED
MAXWELL_COEFFICIENT_CANDIDATE = BUILT_ON_NONZERO_NORMALIZATION_BRANCH
R4_UNITS = PASS
SUPPORT_LOCALITY = REFUTED_BY_ACTUAL_RECIPROCAL_LOOP
CONTACT_QUOTIENT_DESCENT = NOT_CERTIFIED
GENERAL_REFINEMENT_NATURALITY = NOT_CERTIFIED
FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT = BLOCKED
J2_RIGHT_HAND_COMPOSITE = NOT_POSED
MEMBER_SELECTED = none
```

`LICENSE = DERIVED`  
`PROJECTOR_CERT = BLOCKED (+actual reciprocal-loop Hodge projector spreads a one-edge-supported input over the full cycle; contact-reducing/refinement certificates remain absent)`  
`READER_REFERENCED = no`

