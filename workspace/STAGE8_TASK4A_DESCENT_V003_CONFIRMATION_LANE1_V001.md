# STAGE8 TASK 4A: DESCENT V003 CONFIRMATION - CODEX LANE 1 V001

Date: 2026-08-03  
Task: PASTE 439 / Task 4a / bounded confirmation  
Lane: CODEX LANE 1  
Register head at preflight: Q-356  
Custody: independent confirmation of Lane 2 V003

```text
LEAD_RESULT = CONFIRMED

DESCENT_V003 = CONFIRMED
FOUNDATION_READY_FOR_SQUARE_COMMISSION = yes

SCOPE_OF_YES =
  the fixed-stage and family-natural mathematical map foundation is ready;
  the square commission must begin by resolving the explicitly open
  action-consumer typing certificate.

P_432_3 = OPEN
EXACT_MISSING_CERTIFICATE =
  ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY

JOINT_MATHEMATICAL_MAP_LAYER = CONFIRMED
CONSUMER_FACING_INFORMATION_LAYER = INCOMPLETE

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The bounded delta check passes. The eleven actual diff hunks between V002
and V003 are completely accounted for by the F3/F6/F7 corrections ordered in
the final review. No confirmed map formula, kernel theorem, covariance law,
restriction construction, or regression was changed outside that repair
surface.

---

## 0. Preflight

The following artifacts were hash-verified before reading:

| Artifact | Expected SHA-256 | Actual SHA-256 | Result |
|---|---|---|---|
| `STAGE8_TASK4A_FAMILY_NATURAL_PREFIX_TO_CYCLE_DESCENT_LANE2_V003.md` | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | PASS |
| `STAGE8_TASK4A_FAMILY_NATURAL_PREFIX_TO_CYCLE_DESCENT_LANE2_V002.md` | `89d98c3c5c8eedc6de90c2a24569a1e91cec0dc227af6e265447924991496af3` | `89d98c3c5c8eedc6de90c2a24569a1e91cec0dc227af6e265447924991496af3` | PASS |
| `STAGE8_TASK4A_DESCENT_V002_FINAL_REVIEW_LANE1_V001.md` | `20391cb94af5a74f08db6fa4249828c8c70d3345316e8cd10d9ad45d8bb524a4` | `20391cb94af5a74f08db6fa4249828c8c70d3345316e8cd10d9ad45d8bb524a4` | PASS |

The three sidecars verified. `LOCKED_PROCESS.md` was read in full and its
sidecar passed. The questions-settled register was hash-verified and its live
head was Q-356 at preflight.

```text
DOES_THE_OBJECT_EXIST = yes
IS_THE_VERSION_CURRENT = yes | register head Q-356
ARE_THE_INPUTS_PRESENT = yes
PREFLIGHT = PASS
```

---

## 1. Verdict table

| Item | Verdict | One-line reason |
|---|---|---|
| C1 | PASS | The complete V002-to-V003 diff has eleven hunks; V003's C01-C13 table accounts for every changed block and no confirmed mathematics drifts. |
| C2 | PASS | P-432-3 is OPEN, both DoR-015 scalar routes are retained, the one-edge F7 witness is present, and the exact missing certificate is named. |
| C3 | PASS | V003 separates the confirmed mathematical map layer from the incomplete consumer-facing layer without restoring the killed universality claim. |
| C4 | PASS | The permanent regression suite is retained or rerun with its original scope, including pendant, cycle-creating, S8-A, one-edge, reality, batching, and restriction checks. |

No item kills V003.

---

## 2. C1 - complete and accurate delta

### 2.1 Mechanical diff result

The direct unified diff between V002 and V003 contains exactly these eleven
hunks:

```text
@@ -1,14 +1,14 @@
@@ -26,31 +26,38 @@
@@ -58,19 +65,19 @@
@@ -86,6 +93,7 @@
@@ -93,12 +101,16 @@
@@ -122,26 +134,33 @@
@@ -581,107 +600,142 @@
@@ -795,8 +849,10 @@
@@ -893,9 +949,9 @@
@@ -918,10 +974,13 @@
@@ -938,17 +997,19 @@
```

### 2.2 Hunk-to-repair accounting

| Actual hunk | V003 delta rows | Confirmation |
|---|---|---|
| title, task, register, custody | C01 | Administrative currency only. |
| lead block and standing | C02-C05 | Replaces the killed closed/complete claims with OPEN/INCOMPLETE and states the two routes. |
| opening scope paragraph | C06 | Says map repairs remain confirmed and only consumer universality is removed. |
| preflight and authority table | C07 | Updates from the first review to the controlling final review and Q-355 build context. |
| register sweep | C07 | Adds Q-354/Q-355 disposition without changing map content. |
| symbols and section transition | C07-C08 | Preserves the `j_NM^Q` versus `j_NM^phys` distinction and installs the bounded delta table. |
| delta table plus Section 5 | C08-C09 | Gives all C01-C13 rows, both consumer routes, the narrower theorem, and the F7 witness. |
| Section 6.6 conclusion | C10 | Keeps the pendant theorem but prevents it from deciding the endpoint-matched route. |
| final scoped prose | C11 | Removes the unsupported claim that DoR-015 resolves the consumer seam. |
| final board P-432-3 and layer status | C12 | Marks the certificate OPEN and the information layer INCOMPLETE. |
| final standing | C13 | Updates the artifact to V003 ready for confirmation. |

This table exhausts the mechanical diff. V003's own delta table appears at
lines 137-163 and makes the same complete disclosure. Its statement that all
other confirmed mathematics is carried verbatim is borne out by the diff.

### 2.3 No unrelated drift

The unchanged body retains:

```text
fixed-stage descent formula;
well-definedness under simultaneous signed relabeling;
kernel = Gate-4 vertex rephasing;
surjectivity and no record-visible cycle deletion;
S8-A covariance and survival;
contravariant cellular restriction;
cycle-rank-preserving upward naturality;
cycle-creating impossibility;
rank-0/1 agreement on the sealed square and im(j_NM^Q);
edge-resolved boundary and residual iota family;
selection and door accounting.
```

Therefore:

```text
C1 = PASS
UNLISTED_MATHEMATICAL_CHANGE = false
```

---

## 3. C2 - P-432-3 remains open on the exact two-route seam

V003 states at lines 603-620 that DoR-015 permits two physical scalar routes:

```text
route 1 = closed-cycle quotient products, with endpoint factors cancelled;
route 2 = endpoint-covariant transport contracted with matching endpoint data.
```

The quotient theorem at lines 622-656 is correctly narrowed. An edge-only
scalar invariant under vertex rephasing factors uniquely through `q_G` and
therefore may consume `D_G`. The theorem does not extend to a jointly
gauge-invariant tuple containing endpoint data.

### 3.1 F7 witness

V003 retains the final review's one-edge witness at lines 658-715. For the
tree `e:s->t`,

```text
S_e(v_t,T_e,v_s) = <v_t,T_e v_s>
```

is invariant under the simultaneous endpoint action

```text
T_e -> g_t T_e g_s^(-1),
v_s -> g_s v_s,
v_t -> g_t v_t.
```

But `Q_G` is a point for a connected one-edge tree. The two tuples

```text
(T_e,v_s,v_t) = (1,1,1),
(T_e,v_s,v_t) = (1,1,u),  u != 1,
```

have the same edge quotient while their invariant scalars are respectively
`1` and `conjugate(u)`. Hence route 2 does not factor through `q_G` alone.
The witness establishes admissibility of the second typing route; it does not
assert that the future action selects it.

### 3.2 Exact open certificate

V003 names the unresolved item at lines 717-738:

```text
P_432_3 = OPEN

EXACT_MISSING_CERTIFICATE =
  ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY:
    the future action square consumes closed-cycle/invariant-cylinder data
    only and does not consume endpoint-covariant transport contracted with
    matching endpoint data.
```

It also keeps the action-comparison square and stationary 2PI descent
`NOT_BUILT / TYPE-U`, with `PHYSICAL_P_VERDICT = NO_VERDICT`.

```text
C2 = PASS
P_432_3_SILENTLY_CLOSED = false
TWO_ROUTE_TYPING_PRESERVED = true
F7_WITNESS_PRESERVED = true
```

---

## 4. C3 - map foundation and consumer layer are separated

The final board at lines 952-1012 makes the supported split:

```text
JOINT_MATHEMATICAL_MAP_LAYER = CONFIRMED_ON_EDGE_RESOLVED_FAMILY

INFORMATION_LAYER = INCOMPLETE |
  remainder: action-consumer typing at the
  D-versus-endpoint-contraction seam

ACTION_OR_2PI_COMPARISON_SQUARE = NOT_BUILT / TYPE-U
NON_EDGE_RESOLVED_EXTENSION = NOT_BUILT / TYPE-U
```

The confirmed map layer consists of:

```text
complete prefix traces
  -> consecutive relative characters
  -> signed edge cochain
  -> Gate-4 quotient D_G in Q_G.
```

`T_G^char` remains valid on Q-313's sealed square and its proven rank-0/1
scope. It agrees with `D_G` on `im(j_NM^Q)` and does not merge with `D_G` on
the full component. V003 neither weakens that result nor turns it into a
consumer-typing theorem.

Accordingly, `FOUNDATION_READY_FOR_SQUARE_COMMISSION = yes` has a bounded
meaning: the confirmed maps are ready to be consumed, and the commission's
first obligation is the still-open consumer-class certificate. It does not
mean that the information layer or the square is already complete.

```text
C3 = PASS
MAP_LAYER_OVERCLAIM = false
CONSUMER_LAYER_OVERCLAIM = false
```

---

## 5. C4 - regression preservation

| Regression | V003 disposition | Confirmation |
|---|---|---|
| pendant `w!=1` | Rerun; `D=1`, `T^char=w`, so full-component equality is refuted | PASS |
| cycle-creating identity edge | Retained as impossibility of a canonical upward physical map | PASS |
| S8-A `c_3` | Retained covariantly and not deleted by descent | PASS |
| one-edge tree | Retained: access content maps to the zero cycle class without contradiction | PASS |
| reality | Retained under inversion/conjugation on the cycle carrier | PASS |
| batching | Retained under consecutive-ratio reconstruction and cellular composition | PASS |
| restriction | Retained with the corrected `j_NM^Q` precomposition and contravariant target map | PASS |
| cycle-rank-preserving upward map | Retained as the inverse of the physical restriction isomorphism on its exact scope | PASS |
| F7 endpoint witness | Installed as a permanent boundary against quotient-only universality | PASS |

The Section 6.6 hostile attack is also correctly scoped. It proves a
quotient-only consumer cannot recover pendant/tree gauge content after
quotienting. V003 explicitly says that this does not decide whether the
future consumer carries an independent endpoint-matched leg.

```text
C4 = PASS
REGRESSION_DROPPED = false
REGRESSION_SCOPE_EXPANDED = false
```

---

## 6. Final determination

V003 performs the bounded repair ordered by the final review:

1. It preserves the confirmed mathematical descent and extension results.
2. It retracts the unsupported universal quotient-consumer inference.
3. It states both DoR-015 scalar routes without selecting one.
4. It makes the one-edge endpoint witness permanent.
5. It keeps the consumer-facing square and its decisive typing certificate
   open.

No new selection, rank value, ratio, orientation member, endpoint frame,
filtration member, cycle basis, chain-map member, or contraction is installed.
No alpha, `K_*`, root, response value, or measured constant is evaluated.

```text
DESCENT_V003 = CONFIRMED
FOUNDATION_READY_FOR_SQUARE_COMMISSION = yes

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
