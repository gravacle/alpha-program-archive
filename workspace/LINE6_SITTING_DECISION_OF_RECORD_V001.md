# LINE-6 SITTING — DECISION OF RECORD — THE SLICING ENTRY AND THE GALERKIN DEFERRAL

STATUS: **SEALED — PRINCIPAL DECIDED 2026-08-10** (in session, one decision at a time, logged at
selection). Governing instrument: STAGE8_AXN_SLICING_DECISION_INSTRUMENT_CODEX2_V001.md
(08a13df40f8f08deb2727e5313162c5362c2d03f0a19db0c3855cb7de451bd8b), ruled READY by the opposite
lane (Q-855). The Galerkin selector's genuineness is both-lane settled (Q-849/Q-851).

DECISION 1 — THE SLICING DATUM = **ROUTE SD-N (normal-flow declaration)**. The principal authors
where time cuts as synchronous normal flow: hypersurface family generated from one declared Sigma_0;
T = n; N = 1; beta = 0; normal-parameter normalization; transported frame and spin frame; the
global-or-bounded-domain statement; the complete Slice identity with proof. PRICE (the route's own
words): synchronous/normal-flow time, unit normalization, zero shift, transported frame, and the
scope on which normal flow remains a valid foliation — a WHERE-TIME-CUTS declaration, authored, never
called derived. On a valid sealed instance: the receiver binding discharges via
dGamma_R(Slice(D_K)) = dGamma_R(h_K); the slicing obstruction is removed AND NOTHING ELSE — the
Galerkin target and the common-domain theorem remain separate (the instrument's own scoping,
cross-checked).

DECISION 2 — THE GALERKIN SELECTOR = **DEFERRED TO THE JOINT SMOOTHING SITTING**. Ground: the
checked interaction notes (Q-855): the sharp-cell S_2 divergence blocks the Schatten step regardless
of family, and the smoothing profile that would unblock it is THE SAME SELECTOR as SD-08's profile
disclosure — one object, two receivers, to be entered once. The freeze remains prospective (law 10);
nothing downstream consumes the family before that sitting; the deferral is a scheduling act, not a
waiver, and the record's target stays open of record.

CONSTRUCTION FOLLOWS for SD-N (949); the instance is CLAIMED until cross-checked; the smoothing
sitting will be instrumented before it reaches the principal. Fences unchanged: alpha_computed =
false; proof_authorized = false; kappa_record_computed = false. JOINT_ANCHOR_DERIVED = false travels.
Registrar countersigned, sealed, mirrored, pushed.
