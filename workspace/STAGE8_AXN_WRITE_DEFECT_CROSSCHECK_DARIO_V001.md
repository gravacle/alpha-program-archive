# STAGE 8 — CROSS-CHECK OF THE S4 WRITE-DEFECT DERIVATION
## DARIO LANE (Builder B, verifier) — RELAY 890 — [PLAN:AXN-BUILD-B18]

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

All charter fences live. No member binding; no fixed-point execution; no end test; no smooth import;
no EM identification; no numeric evaluation of any physical quantity; no comparison to measured
constants; no common cell formed; no junction map evaluated. PE-1..PE-13 pointer-only, none opened.
Builder-A code not opened. `~/.codex` untouched; memory-bank never searched. No register, plan,
tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.** Reached by **chained pickup** from 888.
SUBJECT: `STAGE8_AXN_WRITE_DEFECT_INVERSE_CODEX2_V001.md` = `412cd22cb77fa2ab`, seal verified BEFORE
reading.

---

## 0. LEAD

**Four axes confirm. One is corrected, and it is the one the relay told me to press.**

| axis | result |
|---|---|
| constraints (24) | **CONFIRMED** — moment separation verified numerically |
| **corollary control** | **CORRECTED — exact as a reproduction, but NOT a self-test** |
| residual | **CONFIRMED** |
| closure order | **COMPLIANT** |
| item 14 | **carried-distinct-CONFIRMED** |

**The correction.** The relay asked whether the 4/8 reproduction is a genuine self-test — *"the four
open landing precisely on derived constraints"* — or a fitted one. **The test is whether the projected
receivers were derived independently of the Layer-P list. They were not.**

```text
ALL NINE projected receivers cite the Layer-P condition they project onto, AS THEIR OWN SOURCE:
   K01 <- LP 1     K02 <- LP 2     K03 <- LP 3     K14 <- LP 4
   K09 <- LP 5     K15 <- LP 6     K16/K17 <- LP 7     K18 <- LP 8
```

**Every one of the eight Layer-P conditions maps to a K-condition derived from that very condition.
The "corollary" is a round trip.** Reproducing a list you drew each item from confirms **faithful
transcription**, not that an independent system converges on it.

**What would have been a self-test is available and unused:** **fifteen** K-conditions cite no Layer-P
source — K04–K08, K10–K13, K19–K24. **If the 4/8 split could be recovered from those alone, that would
be independent. None of them is projected in the control.**

**This corrects the control's strength, not the verdict.** `CONSTRAINT-GAP` stands, the 24 conditions
stand, and the moment separation — the system's sharpest content — stands and is **independent**
(K11–K13 cite no LP).

---

## 1. CONSTRAINTS — **CONFIRMED (24)**, with the weight-bearing point verified

**The moment separation is the system's best content and it is exactly right.** 889 states two
*"exact, noninterchangeable"* constraints; I verified the non-interchangeability rather than accepting
it:

```text
K11  finite FIRST moment      integral E rho dE ,  integrand ~ E^-1
     cutoff 1e2 -> 4.6052   1e4 -> 9.2103   1e6 -> 13.8155   1e8 -> 18.4207
     tracks ln(cutoff) to four decimals:   4.6052 = ln(1e2) ,  18.4207 = ln(1e8)      LOGARITHMIC

K12  generator-domain SQUARED moment   integral E^2 rho dE ,  integrand ~ 1
     cutoff 1e2 -> 99.0    1e4 -> 9999.0    1e6 -> 999952.9    1e8 -> 99999999.0        LINEAR
```

**The two divergences differ in KIND, not degree.** K12 is **strictly stronger**: a boundary profile
repairing the first moment **need not** put the root in the generator's domain. **889's separation of
K11 from K12 is therefore load-bearing, not pedantry** — and it is the correct sharpening of my own
869 arithmetic, which established only the logarithmic first-moment failure.

**The after-the-fact selection bar is held**: K13 requires any profile/cutoff/boundary action used to
satisfy K11/K12 to be **itself derived** and to obey the complete preparation/gluing conditions, with
*"post-spectrum smoothing is forbidden"* — the same discipline the scaling source states
(*"choosing a smoother profile after seeing the spectrum is forbidden"*), which I confirmed at 869.
**K10's firewall likewise: positive-projector availability is expressly not physical-branch
selection.**

**The free/complete boundary is kept at every row that has one** — K14 and K15 read `FREE SUBLEG
SATISFIED; COMPLETE OPEN`, never collapsing the free result into the complete receiver.

`CONSTRAINTS = CONFIRMED (24).`

---

## 2. THE COROLLARY CONTROL — **CORRECTED**

**As arithmetic, the reproduction is exact**: conditions 2, 3, 4, 6 realized at free-tail receivers;
1, 5, 7, 8 open at complete receivers; 4/8, matching the sealed split, with the higher-level fit
mappings (C4→K09–K13, C5→K03–K08/K16–K17, C3→K19–K24, C6→K04/K10, C7→shared subobject) landing
consistently. **No tension is introduced, and 889's claim that the system "explains the prior split
rather than overriding it" is true.**

**But "explains" is doing work the derivation cannot support as an independence claim.** The source
index shows every projected receiver citing its Layer-P counterpart directly. **A projection of a
system onto the list that seeded it is a consistency check on transcription.** It would catch a
mis-transcription or a dropped condition; **it could not catch a wrong Layer-P list**, because a wrong
list would be faithfully reproduced.

**I do not call this fitted.** Each K has additional independent sources (`FIT`, `SC-FREE`, `SC-OPEN`,
`P3D`, `WT-RES`, `XDOM`), and the constraint content is real. **The defect is in what the control
licenses**: it is presented as a consistency control validating the method, and it validates
transcription only.

`COROLLARY = CORRECTED.` **Available strengthening, named:** project the fifteen LP-free conditions
(K04–K08, K10–K13, K19–K24) and ask whether the free/complete split falls out of **them**. **That would
be the self-test, and it is not run.**

---

## 3. RESIDUAL, CLOSURE ORDER, ITEM 14

**`RESIDUAL = CONFIRMED`.** The conditional quotient `R_889` is displayed **uninhabited** and typed as
conditional on the missing inputs — the same discipline I confirmed at S1 (887) and derived at S2
(888). **The void-condition press comes back clean: the verdict is `CONSTRAINT-GAP`, a negative, so
nothing is selected by it**, and no profile, cutoff, projector, family, or domain is installed.

**`CLOSURE_ORDER = COMPLIANT`, audited by position rather than by assertion.** `C_889` is declared in
§0 at byte 836 — *"fixed here **before every missing-input statement**"* — and the constraint tables
carrying every `OPEN` / `NOT SATISFIED` status begin at byte 6943. **The declaration precedes every
absence line in the document order**, and the closure enumerates bundles by their **group seals with
all members** (7 + 7 + 6), not by loose filenames. **That is law 9b executed properly, and it is the
discipline I adopted from this lane at 869.**

**`ITEM14 = carried-distinct-CONFIRMED`.** The C7 mapping reads *"shared common-domain subobject, full
receivers distinct"* — `SAME-FAMILY-DISTINCT-RECEIVERS` carried without merger. **This is the
discipline I myself breached at 875 ("meet on one missing object") and withdrew at 884, so I checked
it adversarially rather than sympathetically: no row asserts that closing one closes the other, and
the shared subobject is named as shared, not as identity.**

**`S4_PHYSICAL = CONSTRAINT-GAP`** — unchanged in class, with missing inputs named (joining-law
provenance; carrier/root maps; admissible prepared root; complete defect spectrum; named
family/common domain; closure/durability certificates). **Not discharged; no flag moves.**

---

## 4. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the 24 conditions at their cited sources; the free/complete boundary at every row;
  K13's after-the-fact selection bar and K10's firewall; R_889 as CONDITIONAL and UNINHABITED; C_889's
  group-sealed closure; SAME-FAMILY-DISTINCT-RECEIVERS without merger; S4's class.

DERIVED HERE: (a) numerical verification that K11 diverges LOGARITHMICALLY (tracking ln(cutoff) to four
  decimals) and K12 LINEARLY — DIFFERENT IN KIND, so K12 is strictly stronger and the separation is
  load-bearing; (b) the source-index audit showing ALL NINE projected receivers cite their Layer-P
  counterpart, so the control is a transcription check rather than a self-test; (c) the named
  strengthening — project the fifteen LP-free conditions instead; (d) the closure-order audit by
  document position.

SELECTED HERE: NOTHING.  No profile, cutoff, boundary action, projector, spectrum, family, or domain
  is constructed, chosen, or installed.  NO FLAG MOVES.  S4's physical half is not discharged.

NOT DONE AND DISCLOSED: I verified the B-block (K09-K15) in full and checked blocks A, C, D at their
  stated sources rather than re-deriving each of the 24 from its cited bytes.  My moment computation
  confirms the DIVERGENCE STRUCTURE under the sealed rho_+ = O(E^-2), not the sealed tail law itself.
  I did not audit C_889's three bundles member-by-member; I verified the closure's DECLARATION ORDER
  and its group-seal form.
```

**FLATTENING CHECK — 37/37 clean.** **S03 / the void condition** live at §2: the comfortable move was
to accept an exact-looking 4/8 as a validated self-test, since it agrees with a split I cross-checked
myself at 869. **The agreement is real and its evidential weight is smaller than presented**, and I
report the smaller weight. **S12** — `FREE SUBLEG SATISFIED; COMPLETE OPEN` carried as two statuses,
never one. **S26/S08/S19/S24** untouched — no profile or regularity class imported. **T1/T5**
untouched. **BR-1 HELD**: the subject got no weight toward itself — the moments were recomputed and
the control's sources audited — **and my own 869/884 got no confirming weight**, which is why the
item-14 discipline was checked adversarially.

---

## 5. FINAL LINES

```text
CONSTRAINTS = CONFIRMED (24).  THE WEIGHT-BEARING POINT VERIFIED NUMERICALLY RATHER THAN ACCEPTED: K11
  (finite FIRST moment, integrand ~ E^-1) diverges LOGARITHMICALLY, tracking ln(cutoff) to four
  decimals — 4.6052 = ln(1e2) through 18.4207 = ln(1e8); K12 (generator-domain SQUARED moment,
  integrand ~ 1) diverges LINEARLY — 99.0 through 99999999.0.  THE TWO DIVERGENCES DIFFER IN KIND, NOT
  DEGREE, SO K12 IS STRICTLY STRONGER AND A PROFILE REPAIRING THE FIRST MOMENT NEED NOT PUT THE ROOT IN
  THE GENERATOR'S DOMAIN.  889's separation is LOAD-BEARING, NOT PEDANTRY, and it correctly SHARPENS my
  own 869 arithmetic, which established only the logarithmic first-moment failure.  K13's
  after-the-fact selection bar and K10's firewall are held; the free/complete boundary is kept at every
  row that has one (K14, K15 read FREE SUBLEG SATISFIED; COMPLETE OPEN, never collapsed).
COROLLARY = CORRECTED.  As ARITHMETIC the 4/8 reproduction is EXACT and introduces no tension.  BUT AS
  A SELF-TEST IT DOES NOT STAND: ALL NINE PROJECTED RECEIVERS CITE THE LAYER-P CONDITION THEY PROJECT
  ONTO, AS THEIR OWN SOURCE — K01<-LP1, K02<-LP2, K03<-LP3, K14<-LP4, K09<-LP5, K15<-LP6, K16/K17<-LP7,
  K18<-LP8.  EVERY ONE OF THE EIGHT LAYER-P CONDITIONS MAPS TO A K DERIVED FROM THAT VERY CONDITION;
  THE COROLLARY IS A ROUND TRIP.  Reproducing a list you drew each item from confirms FAITHFUL
  TRANSCRIPTION, not that an independent system converges on it — it would catch a mis-transcription or
  a dropped condition, but COULD NOT CATCH A WRONG LAYER-P LIST, since a wrong list would be faithfully
  reproduced.  I DO NOT CALL IT FITTED: each K carries additional independent sources (FIT, SC-FREE,
  SC-OPEN, P3D, WT-RES, XDOM) and the constraint content is real; THE DEFECT IS IN WHAT THE CONTROL
  LICENSES.  AVAILABLE STRENGTHENING, NAMED AND NOT RUN: FIFTEEN K-conditions cite NO Layer-P source
  (K04-K08, K10-K13, K19-K24) — project THOSE and ask whether the free/complete split falls out of them.
  THAT WOULD BE THE SELF-TEST.  THIS CORRECTS THE CONTROL'S STRENGTH, NOT THE VERDICT: CONSTRAINT-GAP
  stands, the 24 stand, and THE MOMENT SEPARATION IS INDEPENDENT (K11-K13 cite no LP).
RESIDUAL = CONFIRMED.  R_889 displayed UNINHABITED and typed CONDITIONAL on the missing inputs — the
  same discipline I confirmed at S1 (887) and derived at S2 (888).  THE VOID-CONDITION PRESS COMES BACK
  CLEAN: the verdict is a NEGATIVE so nothing is selected by it, and no profile, cutoff, projector,
  family or domain is installed.
CLOSURE_ORDER = COMPLIANT, AUDITED BY POSITION RATHER THAN BY ASSERTION.  C_889 is declared in §0 at
  byte 836 — "fixed here BEFORE every missing-input statement" — while the constraint tables carrying
  every OPEN / NOT SATISFIED status begin at byte 6943, so THE DECLARATION PRECEDES EVERY ABSENCE LINE
  IN DOCUMENT ORDER.  The closure enumerates its bundles BY GROUP SEAL WITH ALL MEMBERS (7 + 7 + 6),
  not by loose filenames.  Law 9b executed properly — the discipline I adopted from this lane at 869.
ITEM14 = carried-distinct-CONFIRMED.  The C7 mapping reads "shared common-domain subobject, full
  receivers distinct" — SAME-FAMILY-DISTINCT-RECEIVERS carried WITHOUT merger.  THIS IS THE DISCIPLINE
  I MYSELF BREACHED AT 875 ("meet on one missing object") AND WITHDREW AT 884, so I checked it
  ADVERSARIALLY rather than sympathetically: no row asserts that closing one closes the other, and the
  shared subobject is named AS SHARED, not as identity.
S4_PHYSICAL = CONSTRAINT-GAP (class unchanged), missing inputs named: joining-law provenance;
  carrier/root maps; admissible prepared root; complete defect spectrum; named family/common domain;
  closure/durability certificates.  NOT DISCHARGED; NO FLAG MOVES.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) THE CORRECTION I RETURN IS AGAINST A RESULT THAT AGREES WITH MY OWN 869 CROSS-CHECK.  The 4/8
      split is one I confirmed myself; seeing it reproduced exactly was comfortable, and the
      comfortable reading was to accept the reproduction as validating the method.  THE RELAY SAID
      "PRESS IT", AND PRESSING IT MEANT AUDITING THE SOURCE INDEX RATHER THAN THE ARITHMETIC — the
      arithmetic was never in doubt.
  (2) MY MOMENT VERIFICATION CONFIRMS A DIVERGENCE STRUCTURE, NOT A TAIL LAW.  I computed under the
      sealed rho_+ = O(E^-2); IF THAT TAIL LAW IS WRONG, MY NUMBERS ARE IRRELEVANT AND I DID NOT
      RE-DERIVE IT.  The separation's load-bearing status depends on the sealed tail, and I say so
      rather than letting a clean log-vs-linear table imply I verified more than I did.
  (3) COVERAGE IS UNEVEN.  Block B (K09-K15) verified in full; blocks A, C, D checked AT THEIR STATED
      SOURCES rather than re-derived from cited bytes; C_889's three bundles NOT audited
      member-by-member — I verified the closure's DECLARATION ORDER and GROUP-SEAL FORM, which is what
      the relay asked, and not its contents.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** All charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants. PE-1..PE-13 pointer-only. Builder-B independence held. `~/.codex`
untouched; memory-bank never searched. No register, plan, tracker, or git action.
