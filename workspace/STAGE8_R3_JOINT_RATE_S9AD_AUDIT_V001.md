# STAGE 8 — ADVERSARIAL AUDIT OF THE r-3 DETERMINATION (THE JOINT POLYDISC RATE)

## INDEPENDENT AUDITOR — CODENAME R3-AUDIT — COMMISSION S9AD — [SEALED]

Date: 2026-08-14
Role: independent adversarial auditor of
`STAGE8_R3_JOINT_RATE_S9AD_V001.md` (digest bebc0f08, sidecar MATCH,
verified at path before reading). Default posture REFUTE. The
independence protocol was followed: sub-tasks (1) and (2) were worked and
FIXED in scratchpad working notes BEFORE the build was opened (notes
digest df095ae43a8ca1930a6547ce1da269d7fdccd3a393faaecb1270a6cd42441227,
hashed 16:04:45 CDT; the build was first opened after 16:07 CDT); the
build was then read ONCE, in full, and attacked at its cited bytes.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ONLY — two CAS batteries this session (the
build's §7 battery re-run VERBATIM in a fresh venv `r3verifyvenv`, sympy
1.14.0, 10/10 PASS reproduced, output byte-identical to the build's
printed block; my own adversarial battery in a separate fresh venv
`r3auditvenv`, sympy 1.14.0, 12/12 PASS, script and output verbatim in
§6); every constant symbolic or an exact rational/surd; nothing numeric
evaluated as ground; no measured constant; no value frozen (no numeric
kappa, M, delta, D, or rate constant; polydisc gauges symbolic);
M(t)/1_{D_t} consumed SHARP; the stricken display (E1 :773-778) consumed
NOWHERE as ground; no register/road_/ledger/lens/plan/tracker/
THE_HANDOFF file opened (every sweep pipe filtered on those name
classes before any open); "Q-..." tokens inside sealed artifacts treated
EXPECTED-UNLOCATABLE, noted, not chased; no git action; no existing file
edited; ONE file written (this artifact) plus its seal sidecar at the
commission-distinct path, probed ABSENT before write (twice: at session
start and immediately before the write). Every enumeration/uniqueness
sentence below claims only its own displayed sweep. Tooling disclosure:
my adversarial battery took TWO pre-final harness repairs (sympy's
evaluation route for a finite harmonic-type Sum was moved to the
index-shifted closed form, the same route R1's N6b uses) — mechanical
CAS-form only; no mathematical claim changed; final run 12/12 PASS.

---

## 0. VERDICT LINE

**CONFIRMED-WITH-CORRECTIONS — the build's verdict (NOT-DERIVABLE-TODAY:
the r-3 joint rate ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|) uniformly on
the closed pair polydisc is a race of two certified divergences with a
sealed rate on NEITHER side, undecided in BOTH directions) SURVIVES
independent re-derivation: my own frame pass and refusal pass, fixed in
hashed working notes BEFORE the build was opened, reached the SAME
verdict, the SAME frame hazards, and a blocker list that maps exactly
onto the build's MO-1..MO-4; every seal re-verified (16/16 at path this
session); every quoted byte re-checked at its cited line (all verified,
one quotation-scope note, c-3); every load-bearing identity re-derived
exactly (my battery 12/12 PASS; the build's battery reproduced VERBATIM
in a fresh venv, 10/10 PASS, output byte-identical); the commissioned
hazard was CONFRONTED by the build, not dodged — §2.2 makes per-member
invertibility GENERIC-ONLY at the sealed bytes, and FRAME-N2/N3 make
||R_n||_op uncontrolled of record with kappa_n -> infinity consistent
with ||R_n||_op -> infinity at any speed; the blocker list was
adversarially stress-tested and every MO is GENUINELY missing (my
independent corpus sweep adjudicated four additional rate-shaped hits —
REM R8c, R3C x2, RUNS, SLIVER — none a rate carrier, one of them, SLIVER,
a sealed NEGATIVE certificate that positively corroborates F-2/MO-3).
THREE corrections, ALL COSMETIC, NONE VERDICT-CHAIN: c-1 the
missing-object list omits its weakest member (MO-0, cofinal
frame-definedness: no sealed byte excludes det_n(0) = 0 at every n — my
TOY-0 realizes frame-emptiness at every member consistently with every
sealed interface byte; subsumed by MO-1, so the list closes the gap but
does not name it); c-2 the §4.4 sweep display omits the four rate-shaped
hits named above (adjudicated here: none carries a rate; verdict
unaffected); c-3 the F-2/G-10 gloss of check n-3 widens n-3's literal
target (the "no admissible M" attribution) to the adjacent "carrier
volume" phrase — immaterial because the demotion is independently TRUE
at 52f2490b's own bytes (its chain is LSC-based, LINK 6/7; no rate
display exists in that artifact). TWO audit ADDITIONS strengthen the
verdict without changing it: add-1 the exact poisoning ceiling
||R_n||_op <= 1/|det_n(0)| (min-factor lemma, CAS F2) — the only
derivable resolvent ceiling is the EXPONENTIAL of the denominator
itself, so every ||R_n||-mediated route is self-poisoned (CAS T2); add-2
a two-toy interface separation (TOY-A / TOY-B, CAS T1) realizing BOTH
truth values of the uniform o(.) inside the full sealed interface
constraint set — the refusal is not merely "no carrier found" but
interface-underdetermination EXHIBITED at witness-family grade (with the
honest caveat that actual-family operator data at rate grade — exactly
the MO list — could still decide it, the same escape r-2 executed for
kappa_n). F-d NOT exhibited confirmed; nothing fires; R-L4b stays NOT
discharged and NOT refuted; all three R-L4 witnesses STAND; the
registrar consumes; this audit fires nothing.**

---

## 1. INDEPENDENCE PROTOCOL, EXECUTED

Order of operations this session, on the record: (1) all nine
commissioned seals verified at path vs sidecar and tasked digest (build
included, bytes unopened); (2) the sealed stock read in full at its
tasked sections — clause 72d5343e (N.1-N.8); candidate e1da7446 (§2.6
battery incl. CAS C3 :486-503, §3); r-2 3b5e95b6 (in full) and its audit
a4cf394c (verdict + protocol); E1 46846730 (:650-800; :320-370; :400-402);
CERT a903716c (§2.3-2.4); ZF 26f9314b (§4.1-4.4); PA 789338ad (A1-A3);
(3) chased at pinned digest and read: 52f2490b (in full); (4) the corpus
sweep run (fenced name classes excluded from every pipe), hits
adjudicated; (5) my frame pass, refusal pass, blocker list, and toy
separations FIXED in the scratchpad notes and hashed (df095ae4...);
(6) my adversarial battery written and run (12/12); (7) ONLY THEN the
build opened and read once in full; (8) its two additional seals
(685afac8, d1807b96) and four sweep-file seals verified before citation;
(9) its battery extracted byte-verbatim (extraction digest ac255b4d...)
and re-run in a second fresh venv.

My pre-fixed answers vs the build (the comparison the protocol exists
for): frame pass = P-1 in substance (same contraction fact, same
invertibility residue at CERT :289-295, same S4/H-R identification; my
notes additionally fixed the exact ceiling ||R_n||_op <= 1/|det_n(0)|,
which the build does not display — add-1); refusal pass = the verdict
verbatim ("race of two certified rate-free divergences"); my blockers
b-1..b-5 map onto MO-2 (b-1), MO-3 (b-2), MO-1 (b-3), MO-4 (b-4), and
s-2/K-4 (b-5, trace-grade polydisc uniformity). No disagreement of
substance anywhere; where my notes go further (TOY-0/A/B, the poisoning
ceiling) the build's verdict is strengthened, never weakened.

---

## 2. SEAL TABLE (16/16 MATCH at path THIS session, before reliance)

```text
S-1  72d5343e...4839aa7  R_L4A_N_CLAUSE_V002_ADOPTED_2026-08-14.md  MATCH
     (path, sidecar, tasked) — read in full.
S-2  e1da7446...c690ed6  STAGE8_R1_NAMING_CANDIDATE_V001.md         MATCH
S-3  3b5e95b6...7e7ab9f  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md MATCH
S-4  a4cf394c...44521fb  STAGE8_R2_..._AUDIT_V001.md                MATCH
     (r-2 consumable at audited grade: CONFIRMED-WITH-CORRECTIONS, all
     cosmetic).
S-5  46846730...384cccb5 STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md MATCH
S-6  a903716c...6de132e0 STAGE8_RL4_RL0_CERTIFICATION_V001.md       MATCH
S-7  26f9314b...48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md        MATCH
S-8  789338ad...7bed5bc3 STAGE8_T7_ACTUAL_PARENT_..._SPEC_V001.md   MATCH
S-9  bebc0f08...         STAGE8_R3_JOINT_RATE_S9AD_V001.md          MATCH
     (THE BUILD; sidecar; bytes opened only after step (7) of §1).
S-10 52f2490b...bba47ed6241 STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md
     MATCH (chased at the pinned digest of r-2 G-7 / ZF §4.1(c); read in
     full).
S-11 685afac8...8d74af52 STAGE8_RL4_RL0_CHECK_V001.md               MATCH
     (n-1 :310-317; n-2 :318-323; n-3 :319-329 re-read at bytes).
S-12 d1807b96...42068f86 STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_
     TARGET_SHARPENED_V001.md                                       MATCH
     (§4 :110-137 re-read: "R-L2b OPEN. No bound. alpha not derived.
     p unknown.").
S-13 2e4e5163...  STAGE8_REMAINDER_UNIFORMITY_V001.md               MATCH
     (matches CERT's own pin; :836-856 adjudicated, c-2).
S-14 57edbb96...  STAGE8_R3C_SUMMED_REMAINDER_ATTEMPT_V001.md       MATCH
     (matches the candidate's :807 pin; :313-325, :637-648 adjudicated).
S-15 80dd5b52...  STAGE8_RUNS_TARGET_POSED_V001.md                  MATCH
     (:35-45 adjudicated).
S-16 218be86d...  STAGE8_T7_SLIVER_NATURALITY_ATTEMPT_RESULT_V001.md MATCH
     (:80-100 adjudicated — the F-2 corroboration).
```

EXPECTED-UNLOCATABLE tokens this session: "Q-1059", "Q-1062", "Q-1054"
(register pointers inside sealed artifacts) — noted, not chased.

---

## 3. INDEPENDENT PASS 1 — THE FRAME (fixed pre-build; the commissioned hazard)

```text
IF-1 SPECTRUM/CONTRACTION. s_i in [0,1] (CERT §2.3(c)) => spectrum of
     1 + A_n(0) = 1 - 2 C_nPC_n is {1 - 2 s_i} subset [-1,1]:
     ||1 + A_n(0)||_op <= 1 EXACT (CAS F1). [= build FRAME-N1.]
IF-2 INVERTIBILITY PER MEMBER: holds iff no s_i = 1/2 iff det_n(0) != 0.
     NOT certified of record: CERT §2.4 :289-292 supplies no coincidence
     and EXCLUDES none; the clause (N.3 :87-89) and the candidate (C3)
     carry invertibility only as the hypothesis "where det(1+A0) != 0";
     V-N1 covers definedness (trace class), not nonvanishing. The frame
     is PER-MEMBER CONDITIONAL, and NOTHING sealed makes the condition
     hold at even one n, let alone cofinally (TOY-0, §5). [= build §2.2,
     which the build states correctly and quantifies over frame-defined
     members; the cofinality gap is c-1.]
IF-3 THE POISONING CEILING (add-1, mine): for the self-adjoint
     contraction 1 + A_n(0) with det != 0, every factor |1 - 2 s_i| <= 1
     gives |det_n(0)| <= min_i|1 - 2 s_i| (min-factor lemma, CAS F2),
     hence EXACTLY
        ||R_n||_op = 1/min_i|1 - 2 s_i| <= 1/|det_n(0)|
                   = exp(-log|det_n(0)|):
     THE DENOMINATOR'S OWN DIVERGENCE IS THE ONLY DERIVABLE CEILING ON
     THE NUMERATOR'S RESOLVENT FACTOR, and it is the EXPONENTIAL of the
     quantity the numerator must be o(.) of — every ||R_n||-mediated
     upper route is self-poisoned (exp(D)/D -> infinity, CAS T2). The
     build's FRAME-N2 ("no n-uniform bound on any sealed artifact") is
     TRUE and is the consumable statement; add-1 sharpens it to "and the
     only derivable non-uniform ceiling is exponentially poisoned."
IF-4 THE r-2 INPUT DOES NOT TAME R_n: s(1-s) maximal at s = 1/2, so the
     eigenvalues that blow up R_n carry kappa mass most efficiently —
     kappa_n -> infinity is consistent with ||R_n||_op -> infinity at
     any speed. [= build FRAME-N3; check n-1's shape; verified at CAS.]
ANSWER TO THE COMMISSION'S QUESTION: yes, the build confronted the
     hazard — §2.2 (generic-only invertibility, both sides degenerate at
     a bad member), FRAME-N2 (uniform R_n bound <=> 1/2-avoidance = an
     unsealed B2 hypothesis), FRAME-N3 (no taming by r-2), MO-1 (named
     missing). The one layer it does not NAME is cofinal
     frame-definedness (c-1); its CH-h books the conditional
     quantification honestly.
```

## 4. INDEPENDENT PASS 2 — THE ESTIMATE/REFUSAL (fixed pre-build)

```text
ROUTES, EACH CLOSED EXACTLY (pre-fixed; the build's §4 matches):
R-a  rank x op budget: ||Delta_n(a)||_1 <= 4n^3 c; kappa_n <= 2n^3
     (dimension ceiling, PA A1, CAS F5); even granting MO-1 the
     certified ratio floor is 2Kc, n-FREE — the carrier power cancels;
     the budget can NEVER certify o(.) (CAS B1). [= build §4.2/D10.]
R-b  resolvent route: ceiling e^{D_n} 4n^3 c vs target o(D_n):
     self-poisoned (IF-3, CAS T2). [The build reaches the same closure
     via FRAME-N2 = MO-1; add-1 is the sharper display.]
R-c  direct trace-norm consumption: NO sealed carrier (sweep, §1 step 4
     and c-2). [= build §4.4.]
R-d  REFUTATION route: needs a numerator lower RATE against a
     denominator UPPER rate; the numerator lower control is rate-free
     (52f2490b via ||R_n Delta||_1 >= ||Delta_n(a)||_2, CAS F3a/F3b;
     the sealed sup_n = +infinity at the refutation's own polydisc
     locus, ZF §4.1(c) display quantifier); the denominator has NO
     sealed upper bound at all (single-eigenvalue witness, check n-1).
     CLOSED — refutation equally underivable. [= build §4.6.]
VERDICT FIXED PRE-BUILD: NOT-DERIVABLE-TODAY in both directions —
     identical to the build's. My b-1..b-5 = its MO-2/MO-3/MO-1/MO-4 +
     s-2. The denominator chain -log|det_n(0)| >= kappa_n re-derived
     exactly (CAS F4; the commission's kappa_n/2 form is the same chain
     weakened — as the build also displays at D8).
BLOCKER-LIST STRESS TEST (genuineness, adversarial): for each MO I
     hunted a sealed supplier in the corpus sweep — MO-1: none (H-R
     undischarged everywhere; no 1/2-avoidance law; FRAME-N2(i) exact);
     MO-2: none (r-2's LSC method rate-free BY CONSTRUCTION; no other
     kappa_n determination exists); MO-3: none (SCAD_HS_SCALING_
     EXPONENT_UNDERIVED standing; G-11 "p unknown"; SLIVER: "the sealed
     corpus contains NO derived statement of how the response scales
     with cell 4-volume, in EITHER direction" — a sealed NEGATIVE
     certificate the build did not cite, corroborating F-2); MO-4: none
     (no cancellation identity; my sweep agrees with s-6). COMPLETENESS:
     one omission at the weakest layer, MO-0 (c-1), subsumed by MO-1.
     Every MO is genuine; none is phony; the list plus MO-0 is complete
     at this sweep.
```

---

## 5. THE TOY SEPARATIONS (mine; witness-family grade — the RB4/CB4
license of record, as R1 §2 and r-2 X-4 use it: abstract eigenvalue/
norm-data models of the sealed INTERFACE constraint set {s_i in [0,1];
kappa_n -> oo; tr_n -> oo; sup_n ||Delta_n(a)||_2 = oo; numerator >=
||Delta_n(a)||_2; numerator <= ||R_n||_op ||Delta_n||_1; rank <= 4n^3;
Delta_n(0) = 0}, NEVER the sealed operators; they certify
UNDERDETERMINATION and no bound on the actual family)

```text
TOY-0 (frame-void; grounds c-1): at every n one eigenvalue s = 1/2 plus
   the V-model tail s_i = 1/(i+2): det_n(0) = 0 at EVERY n (frame empty,
   R_n nonexistent), while kappa_n -> oo (tail harmonic divergence,
   exact), tr_n -> oo, |det|^2 <= e^{-2 kappa} trivially. Every sealed
   interface byte holds. (CAS T0.) => even the WELL-POSEDNESS of r-3's
   frame at infinitely many members is not derivable from sealed stock.
TOY-A (o(.) FAILS): n eigenvalues with u_i = 1/8 (s = (2-sqrt2)/4
   exact surd): kappa_n = n/4 -> oo; |det_n|^2 = 2^{-n} so D_n =
   (n/2) log 2; ||R_n||_op = sqrt2 (healthy frame). Numerator data
   |a|_* n^2 (consistent with every interface byte, CAS T1b): ratio
   -> infinity at every a != 0 — the uniform o(.) FAILS.
TOY-B (o(.) HOLDS): same spectrum; numerator data |a|_* sqrt(n)
   (consistent, CAS T1c): sup-ratio -> 0 — the uniform o(.) HOLDS.
CONCLUSION (add-2): both truth values AND frame-voidness are realized
   inside the sealed interface constraint set: the build's
   "UNDECIDED IN BOTH DIRECTIONS" is not merely a no-carrier report; it
   is interface-underdetermination, EXHIBITED. Caveat held: a future
   derivation may escape by actual-family operator data at rate grade
   (exactly MO-1..MO-4), as r-2 escaped the V/NV relativity for kappa_n.
```

---

## 6. THE CAS BATTERIES

### 6.1 The build's battery, re-run VERBATIM

Extraction: the build's §7 python block (its :550-695) copied
byte-verbatim; extraction digest
ac255b4df0b8f3a6c0d9b1aeb59cc16c41a67b7b14bb83f3c36bda2bfdddcb79.
Fresh venv `r3verifyvenv` (sympy 1.14.0) under the session scratchpad.
Result: 10/10 PASS; output byte-identical to the build's printed output
block (diff empty). Output, verbatim:

```text
D1 frame division-free: (1+A0)(D0 I + adj(1+A0)(A - A0)) = D0 (1+A) EXACT; det(XY) = det X det Y EXACT -- jointly: where det(1+A0) != 0, det(1+A) = det(1+A0) det(1 + (1+A0)^{-1}(A - A0)): PASS
D2 (1-2s)^2 <= 1 EXACTLY on s in [0,1] (solution set of the inequality IS [0,1]); s(1-s) = 1/4 - (s-1/2)^2 (per-factor kappa ceiling 1/4, maximum AT s = 1/2: near-1/2 eigenvalues MAXIMIZE kappa mass): PASS
D3 Delta = (1+A_n(0)) (R_n Delta) EXACT (row scaling; eigenbasis of the baseline); ||Delta||_F^2 = sum_i (1-2s_i)^2 ||row_i(R Delta)||^2 term by term, so with D2 ||Delta||_2 <= ||R_n Delta||_2; and trace >= HS: (sum sigma)^2 - sum sigma^2 = 2 sum_{i<j} sigma_i sigma_j >= 0: PASS
D4 trace-norm budget sum_{i<=3} sigma_i <= 3 sigma_max (3 sigma_max - sum = sum(sigma_max - sigma_i), each term >= 0 by definition of max); kappa ceiling: kappa_n = 2 sum s_i(1-s_i) <= 2 N (1/4) = N/2 = 2n^3 with N = 4n^3 EXACT: PASS
D5 Duhamel shape, scalar identity-grade: e^{-i(H+aJ)} - e^{-iH} = -i a J int_0^1 e^{-iH(1-s)} e^{-i(H+aJ)s} ds EXACT on BOTH integration branches (generic aJ != 0 reduces to 0; the aJ = 0 branch expression vanishes at a = 0) -- the operator variation-of-constants derived in the text; a enters ONLY through aJ: PASS
D6 b_D <= 1 = b_D(1/2,0): s_- <= t^2 and s_+ <= (1-t)^2 (drop x2 >= 0); t(1-t) <= 1/4 (1/4 - t(1-t) = (t-1/2)^2); hence s = s_- s_+ <= t^2(1-t)^2 <= 1/16, so 16 - 1/s <= 0 on 0 < s <= 1/16 and b_D = e^{16 - 1/s} <= 1; b_D(1/2,0) = e^{16 - 16} = 1 EXACT: PASS
D7 monotone assembly: e^{eps C_b} <= e^{eps} for C_b <= 1 (exp increasing: eps - eps C_b = eps(1 - C_b) >= 0); two-factor split e^{eps}(eps e^{eps}) + (eps e^{eps}) 1 = eps e^{eps}(1 + e^{eps}) =: c(eps) EXACT: PASS
D8 from |det_n(0)|^2 <= e^{-2 kappa_n}: -log|det_n(0)| >= kappa_n (log e^{-kappa} = -kappa exact; d^2 <= e^{-2k} => d <= e^{-k} for positive quantities) and kappa_n >= kappa_n/2 (the tasked weaker form): PASS
D9 witness shape (the check's n-1, made exact): s = (1-delta)/2 gives 1 - 2s = delta with -log delta -> +infinity as delta -> 0+, while the kappa mass 2s(1-s) = (1 - delta^2)/2 <= 1/2 stays bounded: -log|det| admits NO upper bound by kappa; o(kappa_n) is SUFFICIENT-ONLY: PASS
D10 budget ratio (||R_n|| N c(eps)) / (N/2) = 2 rho c(eps) -- the carrier power CANCELS EXACTLY: rank x op-norm numerator budget against the kappa ceiling is n-FREE and never o(1); any product-of-norms discharge REQUIRES a sub-volume trace-norm rate (the missing MO-3): PASS
R3-BATTERY-DONE
```

### 6.2 My adversarial battery (VERBATIM), fresh venv `r3auditvenv`

```python
# R3-AUDIT adversarial CAS battery — EXACT SYMBOLIC ONLY (sympy, fresh venv
# r3auditvenv under scratch). Every constant symbolic or an exact rational/surd.
# Nothing numeric evaluated as ground. Groups:
#   F* — frame lemmas fixed in the pre-build notes (independent pass 1).
#   T* — the three interface toy families (witness-family grade ONLY: abstract
#        eigenvalue/norm-data models of the sealed interface constraint set,
#        never the sealed operators; they certify underdetermination, no bound
#        on the actual family).
#   B* — budget-route no-close exhibits (independent pass 2).
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== F1 — contraction of the baseline: (1-2s)^2 <= 1 on s in [0,1] =====
s = sp.Symbol('s', real=True)
ok("F1 1 - (1-2s)^2 = 4s(1-s) >= 0 on s in [0,1] (||1+A_n(0)||_op <= 1 via"
   " spectral mapping; equality iff s in {0,1})",
   sp.expand(1 - (1 - 2*s)**2 - 4*s*(1 - s)) == 0
   and sp.simplify(4*s*(1 - s)) == 4*s*(1 - s))

# ===== F2 — min-factor lemma: |det| <= min|1-2s_i| (factors in [0,1]) =====
f1, f2, f3 = sp.symbols('f1 f2 f3', nonnegative=True)
# with each f_j <= 1: f1*f2*f3 <= f1 (and symmetrically): displayed as the
# exact nonneg decomposition f1 - f1 f2 f3 = f1[(1-f2) + f2(1-f3)]
g2, g3 = sp.symbols('g2 g3', nonnegative=True)  # g_j := 1 - f_j >= 0
expr = f1 - f1*(1 - g2)*(1 - g3)
ok("F2 f1 - f1 f2 f3 = f1[(1-f2) + f2(1-f3)] EXACT (each factor in [0,1] =>"
   " product <= min factor; hence |det_n(0)| <= min_i|1-2s_i| and"
   " ||R_n||_op = 1/min_i|1-2s_i| <= 1/|det_n(0)| — the poisoning ceiling)",
   sp.expand(expr - (f1*(g2 + (1 - g2)*g3))) == 0)

# ===== F3 — the s-number chain floor: ||Delta||_2 <= ||R Delta||_1 =====
# (a) ||X||_1 >= ||X||_2 for singular values: (sum sig)^2 - sum sig^2 =
#     2 sum_{i<j} sig_i sig_j >= 0 (3-value display).
sig1, sig2, sig3 = sp.symbols('sigma1 sigma2 sigma3', nonnegative=True)
ok("F3a (sig1+sig2+sig3)^2 - (sig1^2+sig2^2+sig3^2) = 2(sig1 sig2 + sig1 sig3"
   " + sig2 sig3) >= 0 (trace norm dominates HS, displayed at 3 s-numbers;"
   " general case identical term by term)",
   sp.expand((sig1 + sig2 + sig3)**2 - (sig1**2 + sig2**2 + sig3**2)
             - 2*(sig1*sig2 + sig1*sig3 + sig2*sig3)) == 0)
# (b) Delta = (1+A0)(R Delta) identity, division-free (3x3 fully symbolic):
A0 = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'a0_{a}{b}'))
Dl = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'd_{a}{b}'))
I3 = sp.eye(3)
D0 = (I3 + A0).det()
# adj(1+A0)*Delta plays D0 * (R Delta): (1+A0) adj(1+A0) = D0 I exactly.
ok("F3b (1+A0)·adj(1+A0)·Delta = det(1+A0)·Delta EXACT 3x3 symbolic (the"
   " division-free form of Delta = (1+A0)(R Delta); with F1's contraction:"
   " ||Delta||_2 <= ||R Delta||_2 <= ||R Delta||_1 where det != 0)",
   sp.expand((I3 + A0)*(I3 + A0).adjugate()*Dl - D0*Dl) == sp.zeros(3, 3))

# ===== F4 — denominator floor of record: -log|det| >= kappa =====
u = sp.Symbol('u', nonnegative=True)
h = sp.exp(-4*u) - 1 + 4*u
ser = sp.Poly(sp.series(sp.exp(4*u) - 1, u, 0, 6).removeO(), u)
ok("F4 1 - 4u <= e^{-4u} on u >= 0 (re-derived): |det|^2 = prod(1-4u_i) <="
   " e^{-2 kappa} => -log|det| >= kappa (the of-record chain, r-2 X-1)",
   h.subs(u, 0) == 0
   and sp.simplify(sp.diff(h, u) - 4*(1 - sp.exp(-4*u))) == 0
   and all(cf > 0 for cf in ser.coeffs()))

# ===== F5 — dimension ceiling: kappa_n <= 2 n^3 =====
n = sp.Symbol('n', positive=True, integer=True)
Nslots = 4*n**3
ok("F5 u_i = s_i(1-s_i) <= 1/4 (= 1/4 - (s-1/2)^2) and #slots <= dim ="
   " 4n^3 => kappa_n = 2 sum u_i <= 2·(4n^3)·(1/4) = 2n^3 EXACT ceiling",
   sp.expand(R(1, 4) - s*(1 - s) - (s - R(1, 2))**2) == 0
   and sp.simplify(2*Nslots*R(1, 4) - 2*n**3) == 0)

# ===== B1 — budget no-close: the carrier power cancels =====
c, K = sp.symbols('c K', positive=True)
ratio_floor = (K*4*n**3*c)/(2*n**3)
ok("B1 GRANTING ||R_n||_op <= K (unsealed) and ||Delta_n||_1 <= 4n^3 c:"
   " certified ratio ceiling-over-floor (K·4n^3·c)/(2n^3) = 2Kc — n-FREE;"
   " the budget route cannot certify o(.) against the kappa_n <= 2n^3"
   " ceiling (limit of the bound is 2Kc != 0)",
   sp.simplify(ratio_floor - 2*K*c) == 0
   and sp.limit(ratio_floor, n, sp.oo) == 2*K*c)

# ===== T0 — TOY-0 (frame-void): one eigenvalue exactly 1/2 at every n =====
i = sp.Symbol('i', positive=True, integer=True)
s_tail = 1/(i + 2)                       # V-model tail (R1 N6, known of record)
m = sp.Symbol('m', positive=True, integer=True)
jj = sp.Symbol('j', positive=True, integer=True)
tr_tail = sp.Sum(1/m, (m, 3, n + 2)).doit()   # index shift i = m - 2, pinned:
shift_pin = sp.simplify((1/(i + 2)).subs(i, jj - 2) - 1/jj) == 0
ssq_tail = sp.summation(s_tail**2, (i, 1, sp.oo))
ok("T0 TOY-0: spectrum {1/2} + V-tail: det_n(0) = (1-2·1/2)·prod(...) = 0"
   " EVERY n (frame empty; R_n nonexistent); tail kappa = 2(tr_n - sum s^2)"
   " with tr_n = harmonic(n+2) - 3/2 -> oo and sum s^2 = pi^2/6 - 5/4 FINITE"
   " => kappa_n -> oo; every sealed interface byte holds — well-posedness"
   " of r-3 is interface-underdetermined",
   (1 - 2*R(1, 2)) == 0
   and shift_pin
   and sp.simplify(tr_tail - (sp.harmonic(n + 2) - R(3, 2))) == 0
   and sp.limit(sp.harmonic(n + 2), n, sp.oo) == sp.oo
   and sp.simplify(ssq_tail - (sp.pi**2/6 - R(5, 4))) == 0)

# ===== T1 — TOY-A (o(.) fails) and TOY-B (o(.) holds), same spectrum =====
# spectrum: n eigenvalues with u_i = 1/8 => (1-2s)^2 = 1 - 4/8 = 1/2:
# |det_n| = 2^{-n/2}; D_n = -log|det_n| = (n/2) log 2; min|1-2s| = 1/sqrt2;
# ||R_n||_op = sqrt2 (frame healthy). kappa_n = 2·n·(1/8) = n/4 -> oo.
s_toy = sp.nsimplify((2 - sp.sqrt(2))/4)
u_toy = sp.simplify(s_toy*(1 - s_toy))
detn2 = (1 - 4*u_toy)**n                  # |det|^2
Dn = -sp.log(sp.sqrt(detn2))
a_ = sp.Symbol('abar', positive=True)     # polydisc gauge |a|_* in (0,1]
numA = a_*n**2                            # TOY-A numerator ||R Delta||_1
numB = a_*sp.sqrt(n)                      # TOY-B numerator
ok("T1a shared spectrum: s = (2-sqrt2)/4 in [0,1]; u = s(1-s) = 1/8 EXACT;"
   " |det_n|^2 = 2^{-n}; D_n = (n/2) log 2; kappa_n = n/4 -> oo;"
   " ||R_n||_op = sqrt2 (healthy frame; 1/2-avoidance delta = 1/(2sqrt2))",
   sp.simplify(u_toy - R(1, 8)) == 0
   and 0 < s_toy < 1
   and sp.simplify(detn2 - 2**(-n)) == 0
   and sp.simplify(Dn - n*sp.log(2)/2) == 0
   and sp.simplify(1/sp.Abs(1 - 2*s_toy) - sp.sqrt(2)) == 0
   and sp.limit(n/4, n, sp.oo) == sp.oo)
ok("T1b TOY-A: numerator a·n^2 (consistent: >= ||Delta||_2 = a·n -> oo, the"
   " 52f2490b divergence respected; <= ||R||·||Delta||_1, ||Delta||_1 ="
   " a·n^2/sqrt2 <= 4n^3 c): sup-ratio n^2/D_n -> oo — o(.) FAILS",
   sp.limit(numA/Dn, n, sp.oo) == sp.oo
   and bool(sp.simplify(numA - a_*n) != 0)
   and sp.limit(a_*n, n, sp.oo) == sp.oo)
ok("T1c TOY-B: numerator a·sqrt(n) (>= ||Delta||_2 = a·sqrt(n) -> oo, same"
   " interface bytes): sup-ratio sqrt(n)/D_n -> 0 — o(.) HOLDS uniformly"
   " (ratio a-free after gauge cancellation, sup over closed polydisc = at"
   " a-gauge 1)",
   sp.limit(numB/Dn, n, sp.oo) == 0
   and sp.limit(a_*sp.sqrt(n), n, sp.oo) == sp.oo)

# ===== T2 — the poisoning exhibit: e^{D} beats o(D) =====
D = sp.Symbol('D', positive=True)
ok("T2 the resolvent ceiling exp(D_n)·(4n^3 c) is NOT o(D_n): with D_n >="
   " kappa_n -> oo, lim exp(D)/D = oo (the frame's only sealed R_n ceiling"
   " is the exponential of the denominator itself)",
   sp.limit(sp.exp(D)/D, D, sp.oo) == sp.oo)

print("R3-AUDIT-BATTERY-DONE")
```

Output, verbatim (12/12 PASS):

```text
F1 1 - (1-2s)^2 = 4s(1-s) >= 0 on s in [0,1] (||1+A_n(0)||_op <= 1 via spectral mapping; equality iff s in {0,1}): PASS
F2 f1 - f1 f2 f3 = f1[(1-f2) + f2(1-f3)] EXACT (each factor in [0,1] => product <= min factor; hence |det_n(0)| <= min_i|1-2s_i| and ||R_n||_op = 1/min_i|1-2s_i| <= 1/|det_n(0)| — the poisoning ceiling): PASS
F3a (sig1+sig2+sig3)^2 - (sig1^2+sig2^2+sig3^2) = 2(sig1 sig2 + sig1 sig3 + sig2 sig3) >= 0 (trace norm dominates HS, displayed at 3 s-numbers; general case identical term by term): PASS
F3b (1+A0)·adj(1+A0)·Delta = det(1+A0)·Delta EXACT 3x3 symbolic (the division-free form of Delta = (1+A0)(R Delta); with F1's contraction: ||Delta||_2 <= ||R Delta||_2 <= ||R Delta||_1 where det != 0): PASS
F4 1 - 4u <= e^{-4u} on u >= 0 (re-derived): |det|^2 = prod(1-4u_i) <= e^{-2 kappa} => -log|det| >= kappa (the of-record chain, r-2 X-1): PASS
F5 u_i = s_i(1-s_i) <= 1/4 (= 1/4 - (s-1/2)^2) and #slots <= dim = 4n^3 => kappa_n = 2 sum u_i <= 2·(4n^3)·(1/4) = 2n^3 EXACT ceiling: PASS
B1 GRANTING ||R_n||_op <= K (unsealed) and ||Delta_n||_1 <= 4n^3 c: certified ratio ceiling-over-floor (K·4n^3·c)/(2n^3) = 2Kc — n-FREE; the budget route cannot certify o(.) against the kappa_n <= 2n^3 ceiling (limit of the bound is 2Kc != 0): PASS
T0 TOY-0: spectrum {1/2} + V-tail: det_n(0) = (1-2·1/2)·prod(...) = 0 EVERY n (frame empty; R_n nonexistent); tail kappa = 2(tr_n - sum s^2) with tr_n = harmonic(n+2) - 3/2 -> oo and sum s^2 = pi^2/6 - 5/4 FINITE => kappa_n -> oo; every sealed interface byte holds — well-posedness of r-3 is interface-underdetermined: PASS
T1a shared spectrum: s = (2-sqrt2)/4 in [0,1]; u = s(1-s) = 1/8 EXACT; |det_n|^2 = 2^{-n}; D_n = (n/2) log 2; kappa_n = n/4 -> oo; ||R_n||_op = sqrt2 (healthy frame; 1/2-avoidance delta = 1/(2sqrt2)): PASS
T1b TOY-A: numerator a·n^2 (consistent: >= ||Delta||_2 = a·n -> oo, the 52f2490b divergence respected; <= ||R||·||Delta||_1, ||Delta||_1 = a·n^2/sqrt2 <= 4n^3 c): sup-ratio n^2/D_n -> oo — o(.) FAILS: PASS
T1c TOY-B: numerator a·sqrt(n) (>= ||Delta||_2 = a·sqrt(n) -> oo, same interface bytes): sup-ratio sqrt(n)/D_n -> 0 — o(.) HOLDS uniformly (ratio a-free after gauge cancellation, sup over closed polydisc = at a-gauge 1): PASS
T2 the resolvent ceiling exp(D_n)·(4n^3 c) is NOT o(D_n): with D_n >= kappa_n -> oo, lim exp(D)/D = oo (the frame's only sealed R_n ceiling is the exponential of the denominator itself): PASS
R3-AUDIT-BATTERY-DONE
```

---

## 7. THE ATTACK, ITEM BY ITEM (the build read once, then attacked at bytes)

```text
A-1  SEALS: all 10 of the build's §1 digests re-verified at path (S-1..
     S-11 above, plus its sweep-located d1807b96 = S-12): 12/12 MATCH.
A-2  QUOTED BYTES, re-read at their cited lines, ALL VERIFIED: clause
     N.3 :84-89, N.5 :101-131; candidate C3 :486-503 with output :525;
     r-2 verdict :33-55, X-1 :371-379, CH-A :511-526; E1 C1 :320-329,
     C6 :353-370, M-2/D4 :400-402 (polydisc max(|a_+|,|a_-|) <= eps_*,
     adjoint-continued bra branch — exactly what K-5 consumes), R.0
     :650-666, S2 :696-700, S4 :720-727, R.2 :739-762, clauses :766-790;
     CERT §2.3(c) :228-249, §2.3(d) :251-273, §2.4 :287-314; ZF §4.1(b)
     :332-339, §4.1(c) :340-346, o-1 :394-399; PA A1 :48-57 (Hermite
     triples tensor C^4 — rank ceiling 4n^3 FORCED), b_D :99-113
     (b_D = exp(16 - 1/s), zero-extension, b_D(1/2,0) = 1 — D6's algebra
     verified against these exact bytes), J/h :117-128 (a multiplies J
     alone), A3 :181-182; 52f2490b §0 :17-26, LINK 1 :78-82, LINK 2
     :83-98, LINK 6 :116-119, LINK 7 :120, flag :189
     V_factor_uniform_bound = true; CHECK n-1 :310-317, n-2 :318-323,
     n-3 :319-329; G-11 §4 :110-137. ONE quotation-scope note: c-3.
A-3  IDENTITIES: every load-bearing identity re-derived (my §6.2
     battery, independent forms) AND the build's battery reproduced
     verbatim in a fresh venv, 10/10 PASS, output byte-identical (§6.1).
     Hand-checks beyond CAS: the Duhamel operator identity behind K-5
     (variation of constants for a time-dependent generator, a entering
     linearly), the log-norm bound ||u(a; s, 0)|| <= e^{|Im a| C_b}
     (the LINK 2 mechanism, sealed), the two-factor split of V(a) - V(0)
     with the adjoint-continuation closure (|a-bar| = |a|), and
     ||X||_1 <= sqrt(rank)||X||_2 in §4.3 (Cauchy-Schwarz on s-numbers).
     ALL SOUND. K-5's constant c(eps_*) = eps_* e^{eps_*}(1 + e^{eps_*})
     re-assembled exactly; symbolic throughout; no value frozen.
A-4  HUNT (i) unproven conveniences doing load-bearing work: NONE FOUND.
     The one new derived object (K-5/P-2) is machinery-honest (classical
     variation of constants on sealed constructions, booked in the
     ledger) and is used only as an UPPER budget — it cannot manufacture
     the refusal. The pure-realization premise is inherited and booked
     (CH-g), load-bearing only for the spectral displays; FRAME-N1
     survives the mixed scheme (contraction squeeze), and the refusal
     itself is scheme-blind (its sweep ground) — checked.
A-5  HUNT (ii) fixed-a quoted at the uniform quantifier: NOT FOUND. The
     numerator divergence is quoted at ZF §4.1(c)'s own display
     quantifier with the locus honestly scoped (§4.5, CH-e), and the
     verdict does not lean on it (CH-e: NOT-DERIVABLE stands on
     §4.2/§4.4 alone). P-4 (numerator = 0 at a = 0) is exact.
A-6  HUNT (iii) verdict stronger than the displayed derivation: NOT
     FOUND — the opposite: my add-2 toy separation EXHIBITS the
     both-directions undecidability the build argues by absence of
     carriers. "PROVABLY cannot close" is claimed only of the BUDGET
     ROUTE (§4.2), which is proved (D10/B1); the target itself is left
     undecided — correctly.
A-7  HUNT (iv) missed sealed objects: my sweep surfaced FOUR rate-shaped
     hits absent from the build's §4.4 display — REM :846 (R8c,
     "degree-0 => HS^2 ~ carrier volume": a CLASS-level marginality
     implication consumed in a reductio about the SUMMED object, not a
     per-pair rate certificate at any polydisc quantifier); R3C :313-325
     and :637-648 (same class remark, citing "the 52f2490b mechanism" —
     and 52f2490b's own chain is rate-free, verified at LINK 6/7); RUNS
     :35-45 ("EXHIBITS the running SHAPE ... FORCES no flow" — shape,
     not rate); SLIVER :80-100 (a sealed NEGATIVE certificate: "the
     sealed corpus contains NO derived statement of how the response
     scales with cell 4-volume, in EITHER direction", witness
     SCAD_HS_SCALING_EXPONENT_UNDERIVED — this CORROBORATES F-2/MO-3
     and could have been cited). None is a rate carrier; the refusal
     STANDS; registered as c-2 (COSMETIC, sweep-display completeness).
A-8  HUNT (v) fence/ledger violations: NONE FOUND. Stricken display
     consumed nowhere (Y-6 verified: the only continuum input is
     52f2490b's own chain about the regularized numerator); no value
     frozen (eps_*, C_b, c(eps_*) symbolic; battery exact); sharp
     M(t)/1_{D_t} untouched; the ledger's PREMISE/FORCED/IMMATERIAL
     classifications each carry displayed grounds and I verified each
     (CH-a..CH-h): all sound; CH-e and CH-g are the two that could hide
     a slip and neither does (A-5, A-4).
A-9  HUNT (vi) NOT-DERIVABLE branch — blocker completeness and
     genuineness: stress-tested in §4 (each MO hunted against the
     corpus; all genuine; completeness gap only at the subsumed MO-0,
     c-1). The MO-1 F-a caution (cannot trigger N.5 consistently with
     r-2) re-checked against N.5's F-a text (fires only on BOTH B2
     hypotheses): CORRECT.
A-10 F-d BOUNDARY: the build exhibits nothing bounding det(1 + A_n(a*))
     away from 0 along the family — F-d NOT exhibited is the correct
     report; N.5 does not fire; the consumption boundary (R-L4b not
     discharged, not refuted; witnesses stand) is exactly the clause's.
```

## 8. CORRECTIONS (each tagged; none disturbs the verdict chain)

```text
c-1 COSMETIC (completeness of the missing-object list): add MO-0 — a
    certificate that the frame is defined cofinally (det_n(0) != 0 for
    cofinally many n, equivalently 1/2 not an eigenvalue of C_nPC_n).
    No sealed byte supplies it (CERT :289-292 supplies no coincidence
    and excludes none); TOY-0 (CAS T0) realizes frame-emptiness at
    EVERY member inside the sealed interface constraint set. MO-1
    subsumes MO-0 (a uniform resolvent bound certifies invertibility
    everywhere), so the build's list CLOSES the gap without NAMING it;
    the build's §2.2 and CH-h handle the conditionality honestly.
    Verdict unchanged (the omission only ADDS a reason for refusal).
c-2 COSMETIC (sweep display): §4.4's displayed sweep omits four
    rate-shaped corpus hits (REM :846; R3C :313-325/:637-648; RUNS
    :35-45; SLIVER :80-100). Adjudicated here (A-7): none is a rate
    carrier; SLIVER is a sealed corroboration of F-2/MO-3 the build
    left unclaimed. Verdict unchanged.
c-3 COSMETIC (quotation scope): the build's G-10/F-2 gloss attributes to
    check n-3 the demotion of ZF o-1's "carrier volume" phrase; n-3's
    literal sentence targets the adjacent "no admissible M" phrase in
    the SAME o-1 parenthetical. The demotion itself is independently
    TRUE at 52f2490b's own bytes (LSC chain; no rate display anywhere in
    that artifact — re-verified at LINK 6/7 this session), so the
    consumed grade ("divergence yes, rate no") is right. Verdict
    unchanged.
```

## 9. CHOICE LEDGER (Actual-Surface Guard discipline; every unforced choice, classified)

```text
CH-i   READING FOUR SWEEP-HIT FILES BEYOND THE TASKED STOCK (REM, R3C,
       RUNS, SLIVER): FORCED — the commission mandates sweeping for any
       further sealed carrier of the named content before concluding
       non-derivability; seals verified before citation (S-13..S-16).
CH-ii  THE TOY FAMILIES (TOY-0/A/B): witness-family-grade interface
       models under the record's RB4/CB4 license (R1 §2 pattern, r-2
       X-4 pattern); they certify underdetermination ONLY and bound
       nothing about the actual family; IMMATERIAL(derived) for the
       CONFIRMATION itself (the build's own sweep ground suffices);
       they are additions of strength, PREMISE-free.
CH-iii INHERITED PREMISE (r-2 CH-A pure Hermite realization) for every
       spectral-frame display I re-derived (F1/F2/F5, T0/T1):
       PREMISE(named), the same booking as the build's CH-g; the
       confirmation is IMMATERIAL(derived) to it — the refusal ground
       is scheme-blind, and FRAME-N1's contraction survives the mixed
       scheme (C_n Hermitian contraction => 0 <= C_nPC_n <= 1 by the
       same squeeze).
CH-iv  PROTOCOL ORDER (notes fixed and hashed before the build was
       opened): FORCED by the commission; disclosed in §1 with digests.
CH-v   TWO PRE-FINAL HARNESS REPAIRS in my battery (T0's Sum evaluation
       moved to the index-shifted closed form): IMMATERIAL(derived) —
       mechanical CAS-evaluation route, the identical route R1 N6b uses;
       no claim changed; disclosed in the fences block.
MACHINERY/RELEVANCE (guard append honored): classical operator theory
       and exact CAS identities applied to SEALED constructions with
       booked surface traces; SURFACE-DERIVED, not surface-native; no
       surface verdict anchored beyond the named conditionals.
```

## 10. TOY_SEPARATION (self-assessment, and the ruling owed on the build's)

```text
MINE: CLAIMED CLEAN at the stated quantifiers. TOY-0/A/B are abstract
eigenvalue/norm-data models of the sealed INTERFACE constraint set,
declared as such at every appearance, quoted at no wider quantifier than
"the interface does not determine"; no toy datum values any actual-family
object; the caveat (actual-family rate data could still decide) is
displayed wherever the toys are consumed. All other CAS instances are
identity-grade all-matrix/all-scalar exhibits with their universal
quantifier in the check name.
THE BUILD'S (the ruling this audit owes): CLEAN. Its only witness-shaped
exhibit (D9) instantiates the check's OWN sealed n-1 sentence at a
single eigenvalue and refutes only a universal reverse bound — quoted
exactly at its derivation's width; no model family is used as a premise
anywhere in the build; no instance is promoted to a family claim.
```

---

## 11. FLAG BLOCK

```text
R3_AUDIT = CONFIRMED-WITH-CORRECTIONS( the build's verdict
  NOT-DERIVABLE-TODAY for r-3 — ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|)
  uniformly on the closed pair polydisc, undecided in BOTH directions on
  the sealed stock — SURVIVES: same verdict reached independently and
  FIXED pre-build (notes df095ae4, hashed before the build was opened);
  frame hazards confronted by the build verified at bytes (per-member
  invertibility GENERIC-ONLY; ||R_n||_op uncontrolled; kappa_n -> oo
  consistent with ||R_n||_op -> oo at any speed); numerator floor
  ||R_n Delta||_1 >= ||Delta_n(a)||_2 with sup_n = +oo of record;
  denominator floor -log|det_n(0)| >= kappa_n -> oo of record; budget
  no-close (carrier power cancels) re-proved; blocker list MO-1..MO-4
  stress-tested GENUINE and complete modulo the subsumed MO-0. )
CORRECTIONS = THREE-ALL-COSMETIC( c-1 name MO-0 cofinal
  frame-definedness (subsumed by MO-1; TOY-0 exhibits interface-
  consistency of det_n(0) = 0 at every n); c-2 sweep display omitted
  four rate-shaped hits (REM R8c; R3C x2; RUNS; SLIVER) — adjudicated,
  none a rate carrier, SLIVER corroborates F-2/MO-3; c-3 the n-3 gloss
  widens the check's literal sentence — demotion independently true at
  52f2490b's own bytes. NONE VERDICT-CHAIN. )
AUDIT_ADDITIONS = TWO( add-1 the poisoning ceiling, exact:
  ||R_n||_op <= 1/|det_n(0)| = exp(-log|det_n(0)|) (min-factor lemma,
  CAS F2) — the only derivable resolvent ceiling is the exponential of
  the denominator itself; every ||R_n||-mediated route is self-poisoned
  (CAS T2). add-2 interface underdetermination EXHIBITED: TOY-A (o(.)
  fails) and TOY-B (o(.) holds) both realize the full sealed interface
  constraint set (CAS T1) — the refusal is proved at witness-family
  grade, not only by absence of carriers; escape requires actual-family
  rate data = exactly MO-1..MO-4. )
INDEPENDENCE = PROTOCOL-EXECUTED( sub-tasks (1)-(2) worked and FIXED in
  hashed scratchpad notes (df095ae43a8c..., 16:04:45 CDT) BEFORE the
  build was opened; my battery (6122b18d... / output c8c839d4...) run
  pre-open; the build then read ONCE in full and attacked at bytes. )
SEALS_VERIFIED = 16/16( 72d5343e clause; e1da7446 R1; 3b5e95b6 r-2;
  a4cf394c r-2 audit; 46846730 E1; a903716c CERT; 26f9314b ZF; 789338ad
  PA; bebc0f08 THE BUILD; 52f2490b RL2B refutation; 685afac8 CHECK;
  d1807b96 R-L2b sharpened target; 2e4e5163 REM; 57edbb96 R3C; 80dd5b52
  RUNS; 218be86d SLIVER. EXPECTED-UNLOCATABLE register tokens noted,
  not chased. )
CAS = TWO-BATTERIES( the build's §7 battery extracted byte-verbatim
  (ac255b4d...) and re-run in fresh venv r3verifyvenv: 10/10 PASS,
  output BYTE-IDENTICAL to the build's printed block; my adversarial
  battery in fresh venv r3auditvenv: 12/12 PASS, script + output
  verbatim in §6.2; two pre-final harness repairs disclosed, no claim
  changed. )
F_D_HAZARD = NOT-EXHIBITED-CONFIRMED( nothing in the build or this
  audit bounds det(1 + A_n(a*)) away from zero along the family; N.5
  does not fire. )
CONSUMPTION_BOUNDARY = NOTHING-FIRED( R-L4b NOT discharged and NOT
  refuted; the r-3 slot of N.3's discharge condition stays OPEN at the
  audited grade; no flag flipped, no witness retired, no gate moved;
  all three R-L4 witnesses STAND; the registrar consumes; commissioning
  any MO is not a lane's call. )
FORBIDDEN_IMPORTS = none( exact symbolic only; no floating-point
  numerics as ground; no measured constant; no value frozen (no numeric
  kappa, M, delta, D, eps, or rate constant; toy data exact
  rationals/surds at witness-family grade); M(t)/1_{D_t} sharp; the
  stricken display consumed nowhere as ground; no
  register/road_/ledger/lens/plan/tracker/THE_HANDOFF file opened; no
  git action; no existing file edited; ONE file written plus its seal
  sidecar; commission-distinct path probed ABSENT before write. )
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
ALL_RESULTS = CLAIMED until checked.
R3_JOINT_RATE_AUDIT = SEALED.
```
