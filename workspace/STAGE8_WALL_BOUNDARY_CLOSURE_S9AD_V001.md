# STAGE 8 — THE WALL'S FIRST UNTRIED ROUTE: THE n-UNIFORM FOUR-QUANTITY ENCLOSURE ATTEMPTED THROUGH THE SEALED BOUNDARY-CLOSURE STRUCTURE OF THE RECORD CELL

## BLIND BUILDER — CODENAME WBC-BUILD — COMMISSION S9AD — [SEALED]

Date: 2026-08-14
Role: BLIND builder (WBC-BUILD). Commission: the wall's first untried route
— derive the n-uniform enclosure of the FOUR definite-signed quantities per
(n, cell) — tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2 — FROM the sealed
boundary-closure structure of the record cell (the projector promotion, the
boundary-supported commutator, the R.2 collapse display, the b_D profile,
the phase-skeleton saturation, the onset/action budgets), the four
graveyarded routes having attacked the quantities directly. Derivation
only. Honest outcome menu, none preferred: DERIVED / PARTIAL / FAILS-AT /
CARRIERS-NEEDED / NOT-DERIVABLE-TODAY.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

NEUTRALITY CLAUSE, held throughout: the standing motivating reading (that
gravity, as the cell's boundary-closure, is what disciplines these
quantities) CHOSE this route and is BARRED as ground. No step below reads
"the closure must bound it"; every step derives from sealed bytes, and the
failures below register as results.

Fences held: EXACT SYMBOLIC ONLY — one CAS battery (sympy 1.14.0, fresh
venv `wbcvenv` under the session scratchpad), reproduced verbatim in §7
with output verbatim (8/8 PASS, single run, no pre-final corrections);
every constant symbolic or an exact rational/surd; NOTHING numeric
evaluated (the carrier radius Lambda in WB7 is a FORMAL symbol); no floats
as ground; no measured constant; no value frozen. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-..." tokens
EXPECTED-UNLOCATABLE, noted not chased. No git; no register/commit/push; no
existing file edited; ONE output file (this artifact) plus its seal sidecar
at the commission-distinct path, probed ABSENT twice (session start and
immediately before write). Every enumeration/exhaustiveness sentence below
claims only its own displayed sweep. Marks: DERIVED / CLAIMED /
CONDITIONAL(premise named) span by span.

Homonym discipline, declared up front: n = the CARRIER index of the family
of record (the quantifier of the tasked enclosure); k = the INSERTION order
(the coupling-parity grading K_H/H_A rides on). The sealed artifacts use
"n" for both; every span below uses the split notation.

---

## 0. VERDICT IN ONE LINE

**FAILS-AT (the four-quantity enclosure — three named steps, each displayed and each a sealed-text ground, none a neutrality-clause import) / DELIVERED MEANWHILE (the route's genuine positive yield, new at this wall): the boundary-closure structure DOES yield an n-UNIFORM, branch-uniform, cell-per-unit OPERATOR-grade enclosure of both parity sectors — ||K_H,n||_op <= 2 and ||H_A,n||_op <= 2 EXACTLY, from four sealed closure facts alone (b_D <= 1 attained only at the diamond center, WB1 + PA bytes; unit cell-time extent; branch-propagator unitarity; C-contraction) — the first n-uniform bound of any grade on the parity sectors, with ray-class transport CONDITIONAL(P-1) as of record; and the exact closure splitting tr K_n = -i tr[U(S_- - S_+)C] + i tr[S_+[C, U]] (WB4) decomposes the composite into the branch-asymmetry term (pure odd-strata content, DOUBLED by the swap law, WB5) and the boundary-commutator term (routed through [C, U]); BUT the conversion of the operator-grade enclosure to ANY of the four tasked trace/HS quantities FAILS-AT (F1) the rank x op ceiling — the budgets |tr| <= 4n^3 · 2 and ||.||_2^2 <= 4n^3 · 4 carry the FULL carrier volume against an n-free target, and the r-3 pair PROVED at identical carrier-power arithmetic that rank x op cannot close a carrier-uniform estimate (the power cancels only against n^3, never to n^0; WB6 = r-3 D10 re-pinned) — the tasked display is made: THE SAME CEILING BINDS HERE; FAILS-AT (F2) the locus ruling — every decay asset the closure structure owns is BOUNDARY-supported (b_D vanishes to all orders at the diamond edge; [C_n, P] is ball-surface class with HS mass kappa_n; the phase collapse is exact on the ball), while the ADOPTED locus refinement (E1 C6, binding R5 at bytes: "THE FATAL LOCAL INTEGRAL IS THE VOLUME DIAGONAL x = y, NOT THE SHARP BOUNDARY. SMOOTHING ONLY THE BOUNDARY WILL NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT") places the divergent locus of exactly this kernel class at the DIAGONAL, which boundary-supported structure provably does not reach — the stay-stratum shortfall (diagonal degree -1 vs the trace threshold degree < -3, WB7) is a diagonal statement the closure cannot touch; FAILS-AT (F3) the closure's own error term — every exact operator identity the closure supplies at the stay strata (promotion WB2, collapse, monodromy WB3) holds modulo the C-L1 [error], whose sole permitted certification is C-L2's quadratic form, UNCERTIFIED of record (witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED stands) with the operator-norm route EXCLUDED of record (||[h_0, 1_B]|| = +infinity), and the odd sector's single-composite kill would have to live in exactly that uncertified layer — the same-phase monodromy U = I (the (+sqrt2,-sqrt2) pair has phase product +1, WB3) annihilates only the BASELINE, already spent as X-5 dressed-only; SUB-ROUTE VERDICTS: (a) even/odd through the closure — NO single-composite kill or sub-threshold majorant exists at the stay strata on the typed closure stock CS-1..CS-9 (sweep displayed §3.2; the phase skeleton GRADES, never kills — RA4b/CA3 of record; the swap law DOUBLES the odd strata, WB5); (b) boundary-localization — delivers the op-grade enclosure and then stops at F1+F2; (c) saturation — converts the BASELINE to a closed form (V(0) = I on the surviving sector, modulo C-L2) and nothing else: the saturation is the defining property of tau_R (one origin, b4034f04), so its exactness is spent the moment the dressed-only subtraction is made; the onset/action budgets REFUSED as majorants on type alone (J_FS,rel >= pi hbar/2 is a LOWER bound, action-typed, with its gate's own no-substitution bar and the entry-15 magnitude vacancy); CARRIERS-NEEDED, subsidiary, by the gravity scoring artifact's own names (§6): R5's member-named topologies with continuity proofs (the certified-topology absence at this wall = the MO-3 shape), R1's member-supplied sealed prequotient rule with one displayed evaluation on a named oriented k-cell, R10's candidate normalization object with its two typed inputs named to one record cell; MO-map: the F1 stop IS MO-3; MO-4's absence re-confirmed by the splitting sweep; MO-1/MO-2 never enter (the four quantities are baseline-free — R_n appears nowhere, H-R never defaulted, and the poisoning ceiling ||R_n||_op <= exp(-log|det_n(0)|) with -log|det_n(0)| >= kappa_n -> +infinity is displayed WB8 only to close the door on any future R_n detour); NOT REFUTED: no divergence of any of the four quantities is exhibited — all four remain UNDECIDED; the graveyard is not re-walked and the sealed obstruction is gone AROUND, not through (§5); CAS battery 8/8 PASS single run.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest; each
compared against its `.seal.sha256` sidecar AND the tasked prefixes
(where tasked). 14/14 MATCH; NONE UNVERIFIABLE. The r-2/r-3 pair was
LOCATED by tasked digest prefix (3b5e95b6, bebc0f08) via a full-workspace
hash sweep, then sidecar-verified.

```text
d66a922cfe023284  STAGE8_K_CHANNEL_CONTROL_V001.md            MATCH (sidecar + tasked d66a922c)
4ebe6b8ff26036f0  STAGE8_K_CHANNEL_CONTROL_AUDIT_V001.md      MATCH (sidecar + tasked 4ebe6b8f)
798de0dd032c8831  STAGE8_SECOND_ORDER_TOTAL_CONTENT_V001.md   MATCH (sidecar)
c944a901a81eae33  STAGE8_SECOND_ORDER_TOTAL_CONTENT_AUDIT_V001.md  MATCH (sidecar)
468467303a109dc8  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md MATCH (sidecar + tasked 46846730)
789338adb7d3d36d  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (sidecar + tasked 789338ad)
b4034f0476e34db8  STAGE8_C4_SATURATION_STRUCTURAL_BLIND_ANSWER_V001.md  MATCH (sidecar + tasked b4034f04)
ae2202e0f6d128ab  STAGE8_EARN_THE_TRACE_S9AD_V001.md          MATCH (sidecar + tasked ae2202e0)
a4c2b7dfd196a0fa  STAGE8_EARN_THE_TRACE_S9AD_AUDIT_V001.md    MATCH (sidecar + tasked a4c2b7df)
ee91d85c0798d73b  STAGE8_GRAVITY_CLOSE_SCORING_V001.md        MATCH (sidecar + tasked ee91d85c)
3b5e95b6da98a99e  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md  MATCH (sidecar + tasked 3b5e95b6)
bebc0f085d10082e  STAGE8_R3_JOINT_RATE_S9AD_V001.md           MATCH (sidecar + tasked bebc0f08)
a4cf394c7c94feab  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_AUDIT_V001.md  MATCH (sidecar; CONFIRMED-WITH-CORRECTIONS)
72c95d42308921ad  STAGE8_R3_JOINT_RATE_S9AD_AUDIT_V001.md     MATCH (sidecar; CONFIRMED-WITH-CORRECTIONS)
```

Sealed content read at path this session and consumed: d66a922c §0, §2
(KB/KC/KD/KF layers; the s-i..s-v supply enumeration; o-2-OBS), §3
(KC1 channel identities; w-1..w-5), §4 n-1 (the four-quantity form of the
wall), §5; 4ebe6b8f §0 (identity layer CONFIRMED; "the obstruction on BOTH
channels is real and unpassable on the sealed basis"; the ONE-FULL-POWER
gloss corrected to the 3/2 symbol-degree gap — the corrected form is used
below); 46846730 :339-352 (C4 promotion), :353-370 (C6 + the R5 locus
refinement at :359-366), :652-666 (R.0), :720-727 (S4/H-R F'-3), :739-800
(R.2 + R-L4a/b + kappa_bal), :803-833 (R.3, R-L2b), :1131-1168 (C-L1/C-L2/
C-L3/C-L5); 789338ad :94-142 (A2: b_D bytes :99-113; J = -(Q b_D Q) tensor
alpha_x :117-127; h_lambda lambda-linear :124-128), A1 (carrier, nested,
strong); b4034f04 §1-§6 (the one-origin ruling; Ground B scale protection;
S(tau) = sin^2(sqrt2 tau/2)); 3b5e95b6 §0 (kappa_n -> +infinity full-family
B1-DERIVED), §2.1-§2.2 (C = P_-, symbol (I - alpha.p-hat)/2, off-diagonal
modulus 1/(2 pi^2 |r|^3); P = sharp causal-ball projector, r(t) = min(t,
1-t); kappa_n = 2 sum_i s_i(1-s_i)); bebc0f08 §0 (P-1..P-5; MO-1..MO-4),
:255-291 (P-2: ||J_n(t)||_op <= ||b_D||_inf = 1 derived exact, CAS D5/D6/
D7; ||Delta_n(a)||_op <= c(eps_*) n-free), :344-353 + D10 (the rank x op
non-closure), §4.4 (the MO-4 corpus sweep: EMPTY); a4cf394c and 72c95d42
§0 (both CONFIRMED-WITH-CORRECTIONS, verdict chains intact — the pair is
consumable at audited grade); ae2202e0 §0-§3 (the phase-skeleton landing;
Z-1..Z-7: the onset bound J_FS,rel >= pi hbar/2 with its gate bars at
bytes; the action lock |Delta S| = pi hbar with its bar; Z-7 the entry-15
magnitude vacancy); a4c2b7df (audit of the landing, consumed at audited
grade); ee91d85c §0 (15/15 NOT_RECOGNIZABLE_AS_STATED; zero INERT), rows
R1/R5/R10 with their missing_carrier strings at bytes, U1 note. "Q-..."
tokens inside the sealed stock: EXPECTED-UNLOCATABLE per the tasking,
noted, not chased, nothing defaulted.

---

## 2. SUB-TASK (1) — THE CLOSURE STRUCTURE, TYPED EXACTLY

What the sealed record actually holds about the cell's boundary closure.
Sweep basis: the tasked stock of §1, read at the cited bytes; this typing
claims exhaustiveness at that displayed sweep only.

```text
CS-1 THE PROJECTOR PROMOTION (E1 C4 :339-352, frozen input, reviewer-
     confirmed in exact Q(sqrt2)): at full tau_R, lambda tau_R in
     {0, +pi, -pi}, on the ball where M(t) = 1, exp(-i lambda tau_R S) =
     (+1,-1,-1)·I EXACTLY (S^2 = I); sum_lambda w_lambda phase_lambda =
     1 = sum_lambda |w_lambda| (l1 step TIGHT, zero-sum weights buy NO
     decay); operator level: sum_lambda w_lambda u_lambda = m_0(I-P) +
     1·P = P. Re-pinned WB2 (linear in P; no idempotency needed).
     GRADE: exact, WEIGHT-SUM-level (needs the sum over lambda).
CS-2 THE R.2 COLLAPSE DISPLAY (E1 :739-762): V_{mu lambda}(0) - 1 =
     (phase_mu phase_lambda - 1)P = 0 on SAME-phase pairs, -2P on
     OPPOSITE-phase pairs; weight bookkeeping S_+ = 1/2, S_- = -1/2;
     offered as obligations R-L4a/b, never as results; the opposite-phase
     tr A(0) = -2 tr(CPC) = -infinity (C2's own divergence).
     GRADE: exact display; BASELINE-level; the same-phase branch holds
     "up to the C-L2 commutator error" AT BYTES (:754-756).
CS-3 THE BOUNDARY-SUPPORTED COMMUTATOR (3b5e95b6, of record, audited):
     kappa_n := ||[C_n, P]||_2^2 = 2 sum_i s_i(1-s_i) -> +infinity at the
     full-family quantifier (every subsequence), radius-independent by
     the exact dilation invariance of the sealed sea symbol; the
     continuum ||[C, 1_B]||_2 = +infinity is E1 C6 frozen input.
     GRADE: exact and DIVERGENT — the closure's boundary commutator
     carries INFINITE HS mass along the family; it is a divergence
     source, not a budget.
CS-4 THE POISONING CEILING (r-2 X-1 chain, re-pinned WB8 at its identity
     layer): |det_n(0)|^2 <= exp(-2 kappa_n), so -log|det_n(0)| >=
     kappa_n -> +infinity, and ||R_n||_op = 1/min_i|1-2 s_i| <=
     prod_i 1/|1-2 s_i| = exp(-log|det_n(0)|): any chain through the
     baseline resolvent carries an exponentially divergent ceiling; no
     n-uniform bound on ||R_n||_op exists on any sealed artifact (r-3
     P-1; S4/H-R may NEVER default, E1 :720-727 F'-3).
CS-5 THE b_D PROFILE (PA A2 :99-113, bytes): b_D = exp(16 - 1/s),
     s = (t^2-|x|^2)((1-t)^2-|x|^2) on the open unit diamond, zero
     outside; C-infinity, compactly supported, vanishing to ALL ORDERS
     at the diamond boundary and tips; b_D(1/2, 0) = 1. DERIVED here
     (WB1): 0 <= b_D <= 1 with equality ONLY at the diamond center
     (unique interior critical point, displayed solve). The record
     vertex J = -(Q b_D Q) tensor alpha_x has ||J(t)||_op <=
     ||b_D||_inf = 1 (r-3 P-2, sealed, CAS D5/D6/D7).
CS-6 THE PHASE-SKELETON SATURATION (b4034f04, decisive, three grounds):
     lambda_max tau_R = pi IS the first-orthogonality criterion — one
     origin, not two derivations; S(tau) = sin^2(sqrt2 tau/2), S(tau_R)
     = 1 (re-pinned WB2); the product is scale-orbit-invariant (Ground
     B) and unconditional where the individual values are Gate-4-
     conditional. The exact monodromies (+1,-1,-1) and the parity
     grading of the dressed objects are DERIVED of record from this one
     condition (ae2202e0 verdict, audited).
CS-7 THE PARITY GRADING (d66a922c KB1-KB3, unconditional matrix
     identities, audited): the swap law G_{-+}(m) = G_{+-}(-m); K_H =
     even-insertion strata, H_A = -i K_A = odd-insertion strata; the
     quartet weight-sum kill is ABSENT from the composite — odd strata
     DOUBLE (KB4; re-pinned at the S level WB5).
CS-8 THE ONSET/ACTION BUDGETS AT THE CELL EDGE (ae2202e0 Z-2/Z-5, gate
     cc113a16 at bytes): J_FS,rel >= pi hbar/2 — a LOWER bound, derived
     from the imported FS/Mandelstam-Tamm theorem, saturation ADOPTED
     not derived; |Delta S_record| = pi hbar with the bridge gate's bar
     ("No geometric uncertainty budget may be substituted for physical
     action without a theorem"); the entry-15 vacancy bars every
     magnitude-typed landing on today's booked surface (Z-7).
CS-9 THE LOCUS RULING (E1 C6 :353-370; the R5 refinement :359-366,
     ADOPTED OF RECORD, binding): "THE FATAL LOCAL INTEGRAL IS THE
     VOLUME DIAGONAL x = y, NOT THE SHARP BOUNDARY. SMOOTHING ONLY THE
     BOUNDARY WILL NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT." Boundary
     smoothness is demoted to a secondary factor; sharp consumption of
     M(t) and 1_{D_t} is MANDATED (D6').
```

---

## 3. SUB-TASK (2) — THE ROUTE

### 3.1 The frame: the four quantities in closure terms (DERIVED, exact)

The wall's object (d66a922c §4 n-1, audited): per (n, cell), the four
definite-signed real quantities tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2,
with K_n = -i C(U S_- - S_+ U)C (KD2 of record), U = u_+(0)^dag u_-(0)
unitary, S_mu = int_0^1 W_mu^dag J W_mu dt Hermitian (TD1/TD2 of record),
J = -(Q b_D Q) tensor alpha_x (CS-5). The closure-adapted decomposition
[DERIVED, CAS WB4 — exact matrix identity + cyclicity with C^2 = C]:

```text
  U S_- - S_+ U = U(S_- - S_+) + [U, S_+] ,
  tr K_n = -i tr[U(S_- - S_+)C] + i tr[S_+ [C, U]] .
```

Term one is the BRANCH-ASYMMETRY term: by the swap law S_-(m) = S_+(-m)
(CS-7), its even strata cancel EXACTLY and its odd strata are EXACTLY
-2 [m^odd]S_+ — the doubled odd content (CAS WB5). Term two is the
BOUNDARY-COMMUTATOR term: it routes through [C, U], and at the pure-phase
level U = I (WB3) it VANISHES — its entire content is the deviation of U
from the monodromy identity. This is the best decomposition the closure
structure supplies, and it makes the route's stops explicit before any
estimate is attempted: term one is governed by the parity structure
(grading, doubling — no kill), term two by the closure's commutator and
error-term stock (CS-3: divergent; C-L2: uncertified).

### 3.2 Sub-route (a) — the even/odd split through the closure

TASKED QUESTION: does the closure structure bound the odd sector's
composite where the weight-sum kills cannot reach — a single-composite
kill or majorant at the stay strata?

ANSWER: NO on the typed closure stock, by displayed sweep of CS-1..CS-9
against the odd sector (tr H_A, ||H_A||_2^2; leading strata 2o + c = 1,
single-J5 stay, diagonal degree -1 of record):

```text
CS-1 (promotion): WEIGHT-SUM-level — the identity needs the sum over
     lambda; the composite carries no weight sum (KB4/KF3 of record,
     re-pinned WB5). NO single-composite content. NOT A KILL.
CS-2 (collapse): BASELINE-level — the (+sqrt2, -sqrt2) pair has phase
     product (-1)(-1) = +1: SAME-phase, surviving sector, V(0) = I up to
     the C-L2 error. This annihilates the k = 0 stratum — EXACTLY the
     dressed-only fact X-5, ALREADY SPENT in the wall's own statement
     (the four quantities are dressed-only). NOT A KILL of any k >= 1
     stratum.
CS-3 (boundary commutator): DIVERGENT of record (kappa_n -> +infinity).
     A divergence source cannot majorize. NOT A MAJORANT.
CS-4 (poisoning ceiling): exponentially divergent; and R_n does not even
     appear in the four quantities (baseline-free objects). NOT A
     MAJORANT; displayed only to bar the detour.
CS-5 (b_D profile): supplies the OPERATOR-grade constant (see 3.3) —
     degree 0 in the diagonal-symbol count, 3+ symbol-degree powers
     above the trace threshold and 3/2-grade short of the HS threshold
     shape (WB7; the audit-corrected gap form). NOT sub-threshold.
CS-6 (saturation): the exactness IS the definition of tau_R (one
     origin); its operator content at the stay strata is CS-1 + CS-2,
     both dispatched above. NOT an additional kill.
CS-7 (parity): a GRADING, not a kill (RA4b/CA3 of record); the odd
     strata DOUBLE (WB5). It DELIVERS the split; it cannot close it.
CS-8 (onset/action budgets): typed refusal, three independent grounds —
     (i) DIRECTION: J_FS,rel >= pi hbar/2 is a LOWER bound; an enclosure
     needs an upper budget; (ii) TYPE: the budgets are phase/action-
     typed; the four quantities are magnitude-typed and the entry-15
     vacancy bars magnitude landings on today's surface (Z-7, audited);
     (iii) THE GATE'S OWN BAR: "No geometric uncertainty budget may be
     substituted for physical action without a theorem" (cc113a16 at
     bytes, via ae2202e0 Z-5). NOT A MAJORANT.
CS-9 (locus ruling): NEGATIVE for the route — see F2 below.
```

The only remaining closure-side object that COULD carry a single-composite
bound is the C-L1 [error] term (u_lambda = (I-P) + phase_lambda P +
[error], E1 :1131-1136): the odd sector IS deviation content (3.1, term
structure), so any closure-side majorant of it lives in exactly that
layer. Of record: the certification obligation is C-L2 — the commutator
[h_0, M(t) tensor S] in a QUADRATIC FORM, with the operator-norm route
EXCLUDED ("||[h_0, 1_B]|| = +infinity for first-order h_0 against a sharp
indicator", E1 :1137-1144) — UNCERTIFIED, witness
E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS. Sub-route (a)
FAILS-AT F3. NOT REFUTED: no lower bound on the odd sector is exhibited
either; tr H_A and ||H_A||_2^2 remain UNDECIDED.

### 3.3 Sub-route (b) — boundary-localization

DELIVERED, the route's positive yield [DERIVED]: the closure structure
yields an n-uniform OPERATOR-grade enclosure of both parity sectors.
Chain, with each inequality a named standard exact-analysis step and each
constant sealed or CAS-pinned:

```text
  ||J(t)||_op <= ||b_D(t,.)||_inf <= 1        [r-3 P-2 sealed (D5/D6/D7);
                                               re-derived WB1: b_D <= 1,
                                               equality only at center]
  ||S_mu||_op <= int_0^1 ||W_mu^dag J W_mu||_op dt = int_0^1 ||J(t)||_op dt
              <= 1                            [triangle for the Bochner
                                               integral; unitary invariance;
                                               UNIT cell-time extent]
  ||K_n||_op = ||C(U S_- - S_+ U)C||_op <= ||U S_-||_op + ||S_+ U||_op
             <= 2                             [C-contraction ||C_n|| <= 1
                                               (52f2490b LINK 1 via r-3 G-9);
                                               U unitary; triangle]
  ||K_H,n||_op <= ||K_n||_op <= 2 ,   ||H_A,n||_op <= ||K_n||_op <= 2
                                              [K_H = (K+K^dag)/2, H_A =
                                               -i(K-K^dag)/2; triangle +
                                               adjoint isometry]
```

n-FREE, ell-free (both frozen ell), branch-uniform, polydisc-free (the
object is the [a^1] slot; no eps_* enters), per UNIT cell; the ray class
transports outright at operator grade (the dilation intertwiner is
unitary) CONDITIONAL(P-1 ray quantifier, exactly as of record). This is
the first n-uniform enclosure of ANY grade on the parity sectors — the
P-2 asset pattern extended from Delta_n(a) to K_H/H_A.

THE CONVERSION, and the tasked display. The four tasked quantities are
traces and HS masses; conversion from operator grade requires the rank
factor: rank <= 4n^3 on ran C_n (of record), so

```text
  |tr K_H,n| <= 4n^3 · 2 = 8n^3 ,      ||K_H,n||_2^2 <= 4n^3 · 4 = 16n^3 ,
  |tr H_A,n| <= 8n^3 ,                 ||H_A,n||_2^2 <= 16n^3
```

— per-(n, cell) enclosures EXIST at rank grade, but carry the FULL
carrier volume n^3 against an n-FREE target. THE TASKED DISPLAY (does the
r-3 ceiling bind here, or provably not?): IT BINDS, by identical
carrier-power arithmetic [DERIVED, CAS WB6 = r-3 D10 re-pinned]: the r-3
pair PROVED that a rank x op budget (4n^3 c) against any same-power
carrier object (there 2n^3) leaves an n-free ratio that is never o(1) —
the carrier power cancels only against another n^3, NEVER down to n^0.
Here the target is n^0: the gap is the entire carrier volume, one full
power WORSE than the r-3 instance, and the only escape is a sub-volume
trace/HS-norm rate for the dressed composite — which is verbatim the
OPEN MO-3 shape (r-3 §5; R-L2b sharpened target: p unknown, alpha
underived, of record). No sealed artifact supplies it (the d66a922c
supply enumeration s-i..s-v, audited exhaustive on its basis, is consumed
here unchanged). Sub-route (b) FAILS-AT F1 for all four quantities.

FAILS-AT F2, the locus ground [sealed text, not an estimate]: the decay
this sub-route would need beyond the op-grade constant must come from the
closure's supports — b_D's all-orders vanishing at the diamond EDGE, the
ball-SURFACE support of [C_n, P], the exactness of the phase on the ball.
All are boundary-located. The adopted locus refinement (CS-9, binding R5)
rules the fatal integral of exactly this kernel class (|x-y|^{-3}, the
sealed sea kernel modulus) to sit at the VOLUME DIAGONAL x = y, and rules
that boundary smoothing "WILL NOT REMOVE" it. The stay-stratum shortfall
is a diagonal-symbol statement (degree -1 vs trace threshold < -3, WB7).
Boundary-supported structure therefore provably does not reach the
divergent locus: the route's defining hope fails on the record's own
ruling, independently of F1.

### 3.4 Sub-route (c) — the saturation route

TASKED QUESTION: at full tau_R the phase skeleton is exact and the weight
moments vanish; does exactness at the stay strata convert any of the four
quantities to a closed form or a difference of controlled terms?

ANSWER, displayed:

```text
(c-i)  WHAT SATURATION CONVERTS: the BASELINE. The pure-phase monodromy
       gives U = (I-2P)^2 = I on the same-phase pair (WB3), hence V(0) =
       I on the surviving sector (up to C-L2 error) and the KD2 kill at
       U = 1, S_- = S_+ (WB3, matrix grade): the k = 0 stratum vanishes
       IDENTICALLY. This is a closed form — and it is ALREADY SPENT: it
       is X-5/dressed-only, the wall's own premise.
(c-ii) WHAT SATURATION CANNOT ALSO DO: the saturation is the DEFINING
       property of tau_R (b4034f04: one condition, three descriptions;
       criterion = condition, not two equations with a shared root). Its
       entire exact content at the stay strata is CS-1 + CS-2 + the
       monodromy — all consumed in (c-i). A second, independent closed
       form from the same identity would need the identity to say more
       than it says; the sweep of §3.2 displays that it does not.
(c-iii) THE DIFFERENCE DISPLAY that DOES follow (3.1): tr K_n =
       -i tr[U(S_- - S_+)C] + i tr[S_+[C, U]] — a difference/sum of two
       exactly-typed terms. It is NOT a difference of CONTROLLED terms:
       term one is the doubled odd content (uncontrolled — §3.2), term
       two vanishes at the monodromy level and its deviation content has
       exactly two closure-side budget candidates, the boundary
       commutator class (CS-3: kappa_n, divergent of record) and the
       C-L1 error layer (F3: C-L2 uncertified). Stated as CLASS
       membership, not as an equation — no identification of [C, U]
       with [C_n, P] is made or needed for the negative.
(c-iv) The weight-moment vanishing (m_0 = m_1 = 0) is a SUM fact with
       no object in the composite (KB4/KF3 of record, WB5): at the
       single-composite stay strata it converts NOTHING.
```

Sub-route (c): no closed form beyond the spent baseline; the difference
display delivered with both terms uncontrolled. FAILS-AT F3 (term two)
and the §3.2 sweep (term one).

### 3.5 Other sub-routes derived and dispatched

```text
(d) PROMOTION-AS-RESOLUTION: insert I = P + (I-P) around each S_mu and
    use CS-1 to evaluate the P-block at the stay strata. Dispatched: the
    promotion identity is weight-sum-level (CS-1); per branch the phase
    factor is a SIGN, not a projector identity, and the P-block of S_mu
    still carries the sharp-indicator commutator against h_0 — the C-L2
    object again (F3). No new ground.
(e) EDGE BUDGETS AS UPPER BOUNDS: dispatched at CS-8 (direction, type,
    and the gate's own bar).
(f) R_n-MEDIATED ROUTES: the four quantities are baseline-free; R_n
    never appears; and CS-4 shows any detour through it inherits
    exp(-log|det_n(0)|) with -log|det_n(0)| >= kappa_n -> +infinity
    (WB8). Closed before opening; H-R never defaulted.
```

---

## 4. SUB-TASK (3) — THE VERDICT

```text
WBC/WALL = FAILS-AT( the n-uniform four-quantity enclosure via the
  boundary-closure structure, at three named steps:
  F1 THE CONVERSION STEP — rank x op is the only sealed conversion from
     the delivered operator grade to trace/HS grade; it carries the full
     carrier volume 4n^3 against an n-free target, and the r-3 pair
     PROVED the same-power arithmetic cannot close (WB6 = D10 re-pinned;
     the tasked display made: THE SAME CEILING BINDS). Escape requires a
     sub-volume trace/HS rate: verbatim MO-3, open of record.
  F2 THE LOCUS STEP — every closure decay asset is boundary-supported;
     the ADOPTED R5 locus refinement (E1 C6 :359-366) places the fatal
     |x-y|^{-3} integral at the VOLUME DIAGONAL and rules boundary
     smoothing insufficient; the four quantities' shortfall is a
     diagonal-symbol statement (degree -1 at the stay strata vs trace
     threshold < -3, HS threshold 3/2 — WB7, audit-corrected gap form).
  F3 THE ERROR-TERM STEP — the closure's exact identities hold modulo
     the C-L1 [error]; its only permitted certification (C-L2 quadratic
     form) is UNCERTIFIED of record with the operator-norm route
     excluded (||[h_0, 1_B]|| = +infinity); the odd sector's would-be
     single-composite majorant lives in exactly that layer. Witness
     E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS.
  DELIVERED MEANWHILE, exact and CAS-pinned: (i) the n-uniform
  operator-grade enclosure ||K_H,n||_op <= 2, ||H_A,n||_op <= 2 per unit
  cell (ray class CONDITIONAL P-1) — the first n-uniform bound of any
  grade on the parity sectors, from CS-5 + unitarity + unit time extent
  + C-contraction alone; (ii) the exact closure splitting tr K_n =
  -i tr[U(S_- - S_+)C] + i tr[S_+[C, U]] with term one = doubled odd
  content and term two = monodromy-vanishing commutator content (WB4/
  WB5); (iii) the b_D profile bound sharpened to unique attainment at
  the diamond center (WB1); (iv) the closure-structure typing CS-1..CS-9
  at bytes.
  MO-MAP (tasked): F1 = MO-3 exactly; MO-4's absence independently
  re-confirmed (the splitting IS the exhaustive product-level
  decomposition on this route and cancels nothing — §3.4 c-iii; r-3's
  §4.4 corpus sweep consumed unchanged); MO-1/MO-2 do not enter this
  route (baseline-free objects; R_n nowhere; kappa_n consumed only as a
  divergence source). F3 is a blocker OUTSIDE MO-1..MO-4: it is E1's own
  named witness, and on this route it is load-bearing.
  NOT REFUTED: no divergence of tr K_H, tr H_A, ||K_H||_2^2, or
  ||H_A||_2^2 is exhibited; no sealed +infinity attaches (the of-record
  divergence is bare-driven and detached from the dressed-only object —
  d66a922c s-ii, audit-verified); all four quantities UNDECIDED.
  CARRIERS-NEEDED, SUBSIDIARY: where the route stops at ABSENT closure
  structure (F1's missing topology/rate; the absent junction the
  magnitude typing needs), the supplying carriers are named in §6 by the
  gravity scoring artifact's own strings. )
```

---

## 5. SUB-TASK (4) — CONSISTENCY: THE GRAVEYARD AND THE OBSTRUCTION

```text
vs GRAVEYARD ROUTE 1 (per-pair majorants, carrier-divergent): NOT
   re-walked. No per-pair majorant chain appears; the operator bound is
   COMPOSITE-level (the assembled K_n); the BAR (V-2-refused bridge)
   untouched; B-L2*'s summed sup never transported.
vs GRAVEYARD ROUTE 2 (H-R alone; exact witness rho_res = 1 with
   unbounded segment): NOT re-walked. rho_res/R_n never enters (the four
   quantities are baseline-free); H-R never defaulted; CS-4 is displayed
   only to close the detour.
vs GRAVEYARD ROUTE 3 (summed-to-per-pair bridge, none sealed): NOT
   re-walked. No extraction from any weight-summed sup is attempted; the
   odd sector's parity-orthogonality to B-L2* is consumed as the sealed
   non-transport, unchanged.
vs GRAVEYARD ROUTE 4 (fixed-n regions, no limit object): NOT re-walked.
   Every delivered statement is all-n with explicit n-dependence; the
   operator-grade enclosure is n-FREE outright; no fixed-n region is
   invoked anywhere.
DISTINCTION OF THIS ROUTE: it attacked through the closure structure and
   DELIVERED the op-grade layer (new — none of the four graveyard routes
   produced an n-uniform bound at any grade), then failed at three
   steps that are all SEALED-TEXT grounds (r-3's proven ceiling; the
   adopted locus ruling; E1's own uncertified error form) — not at a
   re-walk of any graveyarded mechanism.
vs THE SEALED OBSTRUCTION (d66a922c o-2-OBS; audit: "real and unpassable
   on the sealed basis"): gone AROUND, not through. The obstruction's
   content (thresholds KF1; strata census; the weight-sum/composite
   mechanism) is CONSUMED as ground and nowhere contradicted; no attempt
   is made to certify through the (c = 1, o = 0) class; the route sought
   a DIFFERENT mechanism (closure structure) and its failure registers
   as this artifact's result — per the neutrality clause.
```

---

## 6. CARRIERS-NEEDED — BY THE GRAVITY SCORING ARTIFACT'S OWN NAMES (ee91d85c)

The route stops at absent structure in two places: the certified
sub-volume rate/topology (F1) and the magnitude-typed junction the four
quantities' typing needs (CS-8's vacancy ground). The carriers whose
supply would furnish them, quoted from the scoring artifact's own
missing_carrier strings at bytes:

```text
CN-1 (row R5, WARD_SYMBOL_MAP_JII): "member-named topologies with
     continuity proofs for Loc_N^phys, iotatilde_N^H, iota_N^H, Loc_N^C
     — no member exists in the scored corpus to name them." Bearing: the
     F1 escape is a certified sub-volume rate, i.e. a TOPOLOGY in which
     the dressed composites are controlled uniformly — exactly the
     member-named-topology absence; the scoring artifact's own G-2 note
     (refusing to pre-fix a topology from the HS divergences) marks the
     same absence from the other side.
CN-2 (row R1, WARD_SYMBOL_MAP_JII): "a member-supplied sealed
     prequotient rule (JD-3/JD-3a) with one displayed evaluation on a
     named oriented k-cell — absent from the scored corpus" (the spec's
     own hunt: MEMBER_FOUND = none). Bearing: a sealed member evaluation
     on a named cell would replace the diagonal-majorant accounting
     (F2's blocked side) with the junction object's own symbol-side
     value.
CN-3 (row R10, LENGTH_NORMALIZATION): "a candidate normalization object
     with its two typed inputs named to one and the same record cell —
     no candidate exists in the scored corpus, and the surface of record
     currently carries no slot for one." Bearing: the four quantities
     are magnitude-typed; the entry-15 vacancy (the missing "derived,
     beta-invariant, magnitude-valued internal->external response
     junction") is what CS-8 grounds its typed refusal on; this is the
     carrier that would fill it.
```

Noted, not claimed: U1's spec-named missing shared-core carrier is
adjacent (the scoring artifact's own CORR-0004 links it to the same
vacancy); it is listed there as UNGROUNDED and is not named here as a
supplying carrier. C-L2 (F3) is NOT on the scoring artifact's carrier
list — it is E1's own named witness and is reported as such, not
converted into a junction carrier.

---

## 7. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `wbcvenv` under the session scratchpad; nothing
written to the workspace but this artifact and its seal. Exact symbolic /
exact rational/surd only; nothing numeric evaluated. ONE battery, 8
checks, 8/8 PASS on a single run; NO pre-final corrections.

```python
# WBC-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv wbcvenv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated.
# Groups: WB1 the b_D closure profile (bound, unique attainment, boundary vanishing);
# WB2 the projector-promotion and saturation arithmetic (weights/phases; S(tau));
# WB3 the same-phase monodromy annihilation (U = 1 at the pure-phase level; KD2 kill);
# WB4 the exact closure splitting of the composite and its trace form (cyclicity);
# WB5 the swap-parity doubling (odd strata double, even strata cancel in S_- - S_+);
# WB6 the rank x op carrier-power arithmetic (the r-3 ceiling re-pinned at this wall);
# WB7 the threshold re-pin (trace/HS carrier integrals at formal Lambda);
# WB8 the pair-of-projections / poisoning-ceiling identity chain.
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def cmat(pre, n, m=None):
    m = n if m is None else m
    return sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im*sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

def Zq(M):
    return sp.expand(M) == sp.zeros(*M.shape)

# ===== WB1 — the b_D closure profile (PA A2 bytes :99-113) =====
t, x = sp.symbols('t x', real=True)
s_m = t**2 - x**2
s_p = (1 - t)**2 - x**2
s = sp.expand(s_m*s_p)
crit = sp.solve([sp.diff(s, t), sp.diff(s, x)], [t, x], dict=True)
interior = [c for c in crit if c[t].is_real and c[x].is_real
            and 0 < c[t] < 1 and abs(c[x]) < min(c[t], 1 - c[t])]
gap1 = sp.expand(t**2 - s_m)                      # = x^2 >= 0 (drop-x display)
gap2 = sp.expand((1 - t)**2 - s_p)                # = x^2 >= 0
gap3 = sp.expand(R(1, 4) - t*(1 - t))             # = (t - 1/2)^2 >= 0
ok("WB1 THE b_D PROFILE: unique interior critical point of s = s_- s_+ at"
   " (t, x) = (1/2, 0) with s = 1/16 (solve displayed); s_- <= t^2 and"
   " s_+ <= (1-t)^2 with gaps EXACTLY x^2, and 1/4 - t(1-t) = (t - 1/2)^2 —"
   " so s <= 1/16 with equality ONLY at the diamond center, hence"
   " b_D = e^{16 - 1/s} <= e^0 = 1 with b_D(1/2, 0) = 1 EXACT; and b_D -> 0"
   " to all orders at the boundary (1/s -> +infinity as s -> 0+): the closure"
   " profile is bounded by 1, attained at the center, vanishing at the edge",
   len(interior) == 1 and interior[0][t] == R(1, 2) and interior[0][x] == 0
   and s.subs({t: R(1, 2), x: 0}) == R(1, 16)
   and gap1 == x**2 and gap2 == x**2 and gap3 == sp.expand((t - R(1, 2))**2)
   and sp.limit(sp.exp(16 - 1/sp.Symbol('s_v', positive=True)),
                sp.Symbol('s_v', positive=True), 0, '+') == 0)

# ===== WB2 — promotion and saturation arithmetic (C4 :339-352; R.2 :749-752) =====
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
ph = [1, -1, -1]
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
p = sp.Symbol('p')          # the projector, in the commutative algebra it generates;
                            # every displayed expression is LINEAR in p (no p^2 arises)
promo = sp.expand(sum(w*((1 - p) + phv*p) for w, phv in zip(w_v, ph)))
tau = sp.Symbol('tau', positive=True)
S_tau = sp.simplify(-R(1, 4)*(sp.exp(-Im*sp.sqrt(2)*tau) - 2 + sp.exp(Im*sp.sqrt(2)*tau)))
S_sin = sp.sin(sp.sqrt(2)*tau/2)**2
ok("WB2 PROMOTION + SATURATION: m_0 = sum w = 0; sum w.phase = 1 = sum|w|"
   " (the l1 step TIGHT); at the operator level sum_l w_l[(I-P) + phase_l P]"
   " = m_0 (I-P) + 1.P = P — the zero-sum weights PROMOTE the sharp"
   " causal-ball projector (C4 re-pin, linear in P); S(tau) ="
   " sin^2(sqrt2 tau/2) with S(pi/sqrt2) = 1 (saturation) and"
   " S(pi/(2 sqrt2)) = 1/2 (least-ness control) — both re-pinned exactly;"
   " lambda_max tau_R = sqrt2 . pi/sqrt2 = pi (the one-origin condition)",
   sum(w_v) == 0 and sum(w*phv for w, phv in zip(w_v, ph)) == 1
   and sum(abs(w) for w in w_v) == 1 and promo == p
   and sp.simplify(S_tau - S_sin) == 0
   and sp.simplify(S_sin.subs(tau, sp.pi/sp.sqrt(2))) == 1
   and sp.simplify(S_sin.subs(tau, sp.pi/(2*sp.sqrt(2)))) == R(1, 2)
   and sp.simplify(sp.sqrt(2)*(sp.pi/sp.sqrt(2)) - sp.pi) == 0)

# ===== WB3 — same-phase monodromy annihilates the BASELINE, not the sectors =====
# Scalar-idempotent display (p^2 -> p substitution DISPLAYED):
u_phase = 1 - 2*p                       # (I-P) + (-1).P — the phase -1 monodromy
Usq = sp.expand(u_phase**2)             # = 1 - 4p + 4p^2
Usub = Usq.subs(p**2, p)                # p idempotent
# Matrix-grade instantiation (TOY_SEPARATION: identity schema only), KD2 kill:
C3 = sp.diag(1, 1, 0)
S3 = cmat('s3', 3)
S3h = sp.expand((S3 + S3.H)/2)          # generic Hermitian
K_at_U1 = sp.expand(C3*(sp.eye(3)*S3h - S3h*sp.eye(3))*C3)
P3 = sp.diag(1, 0, 0)
ok("WB3 SAME-PHASE MONODROMY: (I - 2P)^2 = I on idempotents (displayed"
   " substitution p^2 -> p) — the cross-branch pair (+sqrt2, -sqrt2) is"
   " SAME-phase ((-1)(-1) = +1), so at the pure-phase level U ="
   " u_+^dag u_- = (I-2P)^2 = I; and at U = I with S_- = S_+ the composite"
   " vanishes IDENTICALLY (KD2 kill re-pin, matrix grade): the closure's"
   " exactness annihilates the BASELINE (already spent as X-5 dressed-only),"
   " and matrix instance (I-2P)^2 = I checked",
   Usub == 1 and Zq(K_at_U1)
   and sp.expand((sp.eye(3) - 2*P3)*(sp.eye(3) - 2*P3)) == sp.eye(3))

# ===== WB4 — the exact closure splitting and its trace form =====
U3 = cmat('u3', 3)
Sm3 = cmat('sm3', 3); Sm3 = sp.expand((Sm3 + Sm3.H)/2)   # Hermitian S_-
Sp3 = cmat('sp3', 3); Sp3 = sp.expand((Sp3 + Sp3.H)/2)   # Hermitian S_+
lhs = sp.expand(U3*Sm3 - Sp3*U3)
rhs = sp.expand(U3*(Sm3 - Sp3) + (U3*Sp3 - Sp3*U3))
tr_lhs = sp.expand((C3*lhs*C3).trace())
tr_rhs = sp.expand((U3*(Sm3 - Sp3)*C3).trace()
                   + (Sp3*(C3*U3 - U3*C3)).trace())
ok("WB4 THE CLOSURE SPLITTING (exact, generic matrices): U S_- - S_+ U ="
   " U(S_- - S_+) + [U, S_+]; and with C^2 = C, tr[C(U S_- - S_+ U)C] ="
   " tr[U(S_- - S_+)C] + tr[S_+ [C, U]] — the composite trace splits"
   " EXACTLY into the branch-asymmetry term (pure odd content, WB5) and the"
   " boundary-commutator term (routed through [C, U]); at U = I the second"
   " term VANISHES and the first is the doubled odd part",
   Zq(sp.expand(lhs - rhs)) and sp.expand(tr_lhs - tr_rhs) == 0
   and sp.expand((Sp3*(C3*sp.eye(3) - sp.eye(3)*C3)).trace()) == 0)

# ===== WB5 — swap-parity doubling (the KB4 mechanism at the S level) =====
m_ = sp.Symbol('m_c', real=True)
A_k = [cmat(f'A{k}', 3) for k in range(4)]
S_plus = sum((m_**k*A_k[k] for k in range(4)), sp.zeros(3, 3))
S_minus = S_plus.subs(m_, -m_)          # the swap law S_-(m) = S_+(-m)
diff = sp.expand(S_minus - S_plus)
wb5 = True
for k in range(4):
    ck = diff.applyfunc(lambda e: sp.expand(e).coeff(m_, k))
    if k % 2 == 1:
        wb5 = wb5 and Zq(sp.expand(ck + 2*A_k[k]))   # odd: -2 A_k (DOUBLED)
    else:
        wb5 = wb5 and Zq(ck)                          # even: cancels
ok("WB5 SWAP-PARITY DOUBLING: with S_-(m) = S_+(-m) (the sealed swap law),"
   " the branch-asymmetry term S_- - S_+ has even strata EXACTLY ZERO and"
   " odd strata EXACTLY -2 A_k — the odd sector is DOUBLED, never killed,"
   " in the single composite (KB4 mechanism re-pinned at the S level):"
   " no weight sum exists here to engage the m_1 = 0 kill",
   wb5)

# ===== WB6 — rank x op carrier-power arithmetic (the r-3 ceiling AT THIS WALL) =====
n = sp.Symbol('n', positive=True)
c0 = sp.Symbol('c_0', positive=True)     # the n-free op-grade constant (= 2 here)
rank_n = 4*n**3                          # ran C_n rank budget, of record
tr_budget = sp.expand(rank_n*c0)         # |tr| <= rank x op
hs_budget = sp.expand(rank_n*c0**2)      # ||.||_2^2 <= rank x op^2
kappa_ceiling = 2*n**3                   # kappa_n <= 2 sum s(1-s) <= 2n^3, of record
ratio_r3 = sp.simplify((rank_n*c0)/kappa_ceiling)
ok("WB6 RANK x OP CARRIER-POWER ARITHMETIC: the conversion budgets are"
   " |tr| <= 4n^3 c_0 and ||.||_2^2 <= 4n^3 c_0^2 — FULL carrier volume n^3;"
   " the enclosure target is n-FREE (n^0): lim_n (4n^3 c_0) = +infinity, the"
   " gap is the whole carrier volume; and the r-3 ratio shape"
   " (4n^3 c_0)/(2n^3) = 2 c_0 EXACTLY — the carrier power CANCELS, the"
   " ratio is n-free and never o(1): the r-3 proven ceiling (rank x op"
   " cannot close a carrier-uniform estimate) binds IDENTICALLY here",
   sp.limit(tr_budget, n, sp.oo) == sp.oo
   and sp.limit(hs_budget, n, sp.oo) == sp.oo
   and ratio_r3 == 2*c0
   and sp.simplify(sp.limit(ratio_r3, n, sp.oo) - 2*c0) == 0)

# ===== WB7 — the threshold re-pin (KF1 arithmetic, formal Lambda) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
tr_stay = sp.integrate(kk**2*kk**(-1), (kk, 1, Lam))     # degree -1 stay stratum
hs_stay = sp.integrate(kk**2*kk**(-2), (kk, 1, Lam))     # HS side, degree -1
tr_conv = sp.integrate(kk**2*kk**(-4), (kk, 1, Lam))     # degree -4: converges
ok("WB7 THRESHOLD RE-PIN (exact symbolic integrals, Lambda FORMAL): at the"
   " stay-stratum diagonal degree -1, the trace-side carrier integral is"
   " (Lambda^2 - 1)/2 and the HS-side is Lambda - 1 — both DIVERGENT in the"
   " formal limit; the trace converges only past degree -3 (at -4:"
   " 1 - 1/Lambda) — op-grade uniformity (degree 0 in the symbol count)"
   " sits the full 3+ powers above the trace threshold",
   sp.simplify(tr_stay - (Lam**2 - 1)/2) == 0
   and sp.simplify(hs_stay - (Lam - 1)) == 0
   and sp.simplify(tr_conv - (1 - 1/Lam)) == 0
   and sp.limit(tr_stay, Lam, sp.oo) == sp.oo
   and sp.limit(hs_stay, Lam, sp.oo) == sp.oo)

# ===== WB8 — pair-of-projections / poisoning-ceiling identity chain =====
s1, s2 = sp.symbols('s_1 s_2', real=True)
id1 = sp.expand(1 - (1 - 2*s1)**2 - 4*s1*(1 - s1))       # = 0 identity
det2 = (1 - 2*s1)*(1 - 2*s2)                              # |det| = prod |1-2s_i|
y = sp.Symbol('y', positive=True)
ser = sp.series(sp.exp(-y) - (1 - y), y, 0, 4).removeO()
ok("WB8 POISONING CHAIN: 1 - (1-2s)^2 = 4s(1-s) EXACT (per-eigenvalue kappa"
   " mass 2s(1-s); s in [0,1] => |1-2s| <= 1 formal); det_n(0) ="
   " prod_i (1-2s_i) so 1/min_i|1-2s_i| <= prod_i 1/|1-2s_i| ="
   " exp(-log|det_n(0)|) — each dropped factor >= 1; and e^{-y} - (1-y) ="
   " y^2/2 - y^3/6 + ... (leading coefficient 1/2 > 0): the r-2 chain"
   " |det_n(0)|^2 <= exp(-2 kappa_n) re-pinned at its identity layer, so"
   " -log|det_n(0)| >= kappa_n -> +infinity of record — any route through"
   " R_n = (1 + A_n(0))^{-1} carries an exponentially divergent ceiling",
   id1 == 0
   and sp.expand(det2 - (1 - 2*s1)*(1 - 2*s2)) == 0
   and ser.coeff(y, 2) == R(1, 2) and ser.coeff(y, 3) == -R(1, 6)
   and ser.coeff(y, 0) == 0 and ser.coeff(y, 1) == 0)

print("WBC-BATTERY-DONE")
```

Output, verbatim (8/8 PASS, single run):

```text
WB1 THE b_D PROFILE: unique interior critical point of s = s_- s_+ at (t, x) = (1/2, 0) with s = 1/16 (solve displayed); s_- <= t^2 and s_+ <= (1-t)^2 with gaps EXACTLY x^2, and 1/4 - t(1-t) = (t - 1/2)^2 — so s <= 1/16 with equality ONLY at the diamond center, hence b_D = e^{16 - 1/s} <= e^0 = 1 with b_D(1/2, 0) = 1 EXACT; and b_D -> 0 to all orders at the boundary (1/s -> +infinity as s -> 0+): the closure profile is bounded by 1, attained at the center, vanishing at the edge: PASS
WB2 PROMOTION + SATURATION: m_0 = sum w = 0; sum w.phase = 1 = sum|w| (the l1 step TIGHT); at the operator level sum_l w_l[(I-P) + phase_l P] = m_0 (I-P) + 1.P = P — the zero-sum weights PROMOTE the sharp causal-ball projector (C4 re-pin, linear in P); S(tau) = sin^2(sqrt2 tau/2) with S(pi/sqrt2) = 1 (saturation) and S(pi/(2 sqrt2)) = 1/2 (least-ness control) — both re-pinned exactly; lambda_max tau_R = sqrt2 . pi/sqrt2 = pi (the one-origin condition): PASS
WB3 SAME-PHASE MONODROMY: (I - 2P)^2 = I on idempotents (displayed substitution p^2 -> p) — the cross-branch pair (+sqrt2, -sqrt2) is SAME-phase ((-1)(-1) = +1), so at the pure-phase level U = u_+^dag u_- = (I-2P)^2 = I; and at U = I with S_- = S_+ the composite vanishes IDENTICALLY (KD2 kill re-pin, matrix grade): the closure's exactness annihilates the BASELINE (already spent as X-5 dressed-only), and matrix instance (I-2P)^2 = I checked: PASS
WB4 THE CLOSURE SPLITTING (exact, generic matrices): U S_- - S_+ U = U(S_- - S_+) + [U, S_+]; and with C^2 = C, tr[C(U S_- - S_+ U)C] = tr[U(S_- - S_+)C] + tr[S_+ [C, U]] — the composite trace splits EXACTLY into the branch-asymmetry term (pure odd content, WB5) and the boundary-commutator term (routed through [C, U]); at U = I the second term VANISHES and the first is the doubled odd part: PASS
WB5 SWAP-PARITY DOUBLING: with S_-(m) = S_+(-m) (the sealed swap law), the branch-asymmetry term S_- - S_+ has even strata EXACTLY ZERO and odd strata EXACTLY -2 A_k — the odd sector is DOUBLED, never killed, in the single composite (KB4 mechanism re-pinned at the S level): no weight sum exists here to engage the m_1 = 0 kill: PASS
WB6 RANK x OP CARRIER-POWER ARITHMETIC: the conversion budgets are |tr| <= 4n^3 c_0 and ||.||_2^2 <= 4n^3 c_0^2 — FULL carrier volume n^3; the enclosure target is n-FREE (n^0): lim_n (4n^3 c_0) = +infinity, the gap is the whole carrier volume; and the r-3 ratio shape (4n^3 c_0)/(2n^3) = 2 c_0 EXACTLY — the carrier power CANCELS, the ratio is n-free and never o(1): the r-3 proven ceiling (rank x op cannot close a carrier-uniform estimate) binds IDENTICALLY here: PASS
WB7 THRESHOLD RE-PIN (exact symbolic integrals, Lambda FORMAL): at the stay-stratum diagonal degree -1, the trace-side carrier integral is (Lambda^2 - 1)/2 and the HS-side is Lambda - 1 — both DIVERGENT in the formal limit; the trace converges only past degree -3 (at -4: 1 - 1/Lambda) — op-grade uniformity (degree 0 in the symbol count) sits the full 3+ powers above the trace threshold: PASS
WB8 POISONING CHAIN: 1 - (1-2s)^2 = 4s(1-s) EXACT (per-eigenvalue kappa mass 2s(1-s); s in [0,1] => |1-2s| <= 1 formal); det_n(0) = prod_i (1-2s_i) so 1/min_i|1-2s_i| <= prod_i 1/|1-2s_i| = exp(-log|det_n(0)|) — each dropped factor >= 1; and e^{-y} - (1-y) = y^2/2 - y^3/6 + ... (leading coefficient 1/2 > 0): the r-2 chain |det_n(0)|^2 <= exp(-2 kappa_n) re-pinned at its identity layer, so -log|det_n(0)| >= kappa_n -> +infinity of record — any route through R_n = (1 + A_n(0))^{-1} carries an exponentially divergent ceiling: PASS
WBC-BATTERY-DONE
```

---

## 8. CHOICE LEDGER

```text
C-1 VERDICT CLASS [YOURS]: FAILS-AT with a DELIVERED-MEANWHILE layer,
    not PARTIAL. Ground: PARTIAL is reserved by the tasking for "named
    quantities or sectors enclosed"; NO tasked quantity is enclosed at
    its own (trace/HS) grade — the delivered enclosure is operator-grade.
    Reporting op-grade as PARTIAL would overstate.
C-2 HOMONYM SPLIT [YOURS, disclosed in header]: n = carrier index, k =
    insertion order. The sealed artifacts overload "n"; the tasked
    quantifier is the carrier index.
C-3 THE 52f2490b TENSION NOT RESOLVED [YOURS]: d66a922c (audited) rules
    the of-record +infinity bare-driven and detached from the dressed-
    only K_n; bebc0f08 P-5 cites a per-pair dressed-difference
    divergence for Delta_n(a) at its own object. These are different
    objects at different slots; NEITHER direction is consumed for the
    four quantities here (no +infinity attached, no finite majorant
    claimed). The wall's "the of-record divergence provably does NOT
    attach" is carried at d66a922c's audited grade.
C-4 [C, U] BUDGET AS CLASS, NOT EQUATION [YOURS]: §3.4 c-iii names the
    boundary-commutator CLASS and the C-L1 error layer as the only
    closure-side budget candidates for term two; no identification of
    [C, U] with [C_n, P] is asserted (none is needed for a negative).
C-5 CAS SCOPE [YOURS]: identities and exact integrals only; the standard
    inequalities (triangle, unitary invariance, |tr| <= rank·||.||_op,
    ||.||_2^2 <= rank·||.||_op^2, Bochner triangle) are named
    exact-analysis steps in text — the same license the sealed r-3
    artifact used for its Duhamel chain.
C-6 CELL AXIS [PROVABLE part first]: the op-grade enclosure is stated
    per UNIT cell (DERIVED); ray-class transport carried
    CONDITIONAL(P-1, ray quantifier) exactly as of record — operator
    norms are invariant under the unitary dilation intertwiner.
C-7 CARRIER NAMES [PROVABLE]: §6 quotes ee91d85c's own missing_carrier
    strings verbatim; no carrier is invented; U1 noted but not claimed
    (UNGROUNDED per the scoring artifact's own spec handling); C-L2
    reported as E1's witness, not converted into a junction carrier.
C-8 NEUTRALITY [held]: the motivating reading appears nowhere as a step;
    F1 rests on r-3's proven arithmetic, F2 on the adopted R5 locus
    text, F3 on E1's own witness — all sealed-text grounds; the route's
    failure is registered as the result.
C-9 SWEEP GRADE [PROVABLE]: every exhaustiveness claim (CS typing; §3.2
    item sweep; §3.5 dispatch list) claims only its displayed sweep of
    the §1 stock at the cited bytes.
```

---

## 9. TOY_SEPARATION

```text
T-1 WB3/WB4/WB5 use GENERIC finite matrices (3x3) as identity-schema
    instantiations of exact operator identities — the identity layer
    only, the same license as the sealed d66a922c KC/KD blocks. No
    matrix-grade result is carried to the carrier quantifier; every
    carrier-axis statement in this artifact cites sealed carrier-axis
    text (r-2, r-3, C6, KF1) or is a named FAILS-AT.
T-2 WB3's scalar-idempotent display (p with p^2 -> p substitution shown)
    is a commutative shadow, valid because the displayed expressions are
    linear in P or polynomial with the substitution displayed; the
    matrix instance (I-2P3)^2 = I is checked alongside.
T-3 WB6's symbols n, c_0 are FORMAL; the limit statements are formal
    carrier-power arithmetic, not evaluations. WB7's Lambda is FORMAL
    (the record's standing license). WB8's s_1, s_2 are formal
    eigenvalue symbols.
T-4 The two-mode testbed of the sealed stock is NOT used here; no
    witness-family object is promoted to any quantifier.
T-5 The delivered op-grade constants (2 = 2·||b_D||_inf·(unit time)) are
    exact rationals times sealed profile bounds; no float appears
    anywhere in this artifact.
```

---

## 10. FLAG BLOCK

```text
WALL_ROUTE_STATUS = FAILS-AT( the n-uniform four-quantity enclosure via
  the sealed boundary-closure structure — F1 the rank x op conversion
  ceiling (the r-3 proven arithmetic binds IDENTICALLY: full carrier
  volume 4n^3 against an n-free target; escape = sub-volume trace/HS
  rate = MO-3, open of record; WB6); F2 the adopted locus ruling (all
  closure decay assets are boundary-supported; the fatal |x-y|^{-3}
  integral sits at the VOLUME DIAGONAL of record, E1 C6 R5 :359-366;
  the shortfall is diagonal degree -1 vs trace threshold < -3, WB7);
  F3 the closure's own error term (C-L1's [error]; C-L2 quadratic-form
  certification UNCERTIFIED, operator-norm route excluded of record;
  witness E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED STANDS). NOT
  REFUTED: no divergence of any of the four quantities exhibited; all
  four UNDECIDED. )

DELIVERED_MEANWHILE = OPERATOR-GRADE-ENCLOSURE + SPLITTING(
  ||K_H,n||_op <= 2 and ||H_A,n||_op <= 2, n-uniform, branch-uniform,
  per unit cell (ray class CONDITIONAL P-1), from b_D <= 1 (unique
  attainment at the diamond center, WB1) + unit time extent + unitarity
  + C-contraction — the first n-uniform bound of any grade on the parity
  sectors; the exact closure splitting tr K_n = -i tr[U(S_- - S_+)C] +
  i tr[S_+[C, U]] (WB4) with the odd content DOUBLED in term one (WB5)
  and term two vanishing at the pure-phase level (WB3); the closure-
  structure typing CS-1..CS-9 at bytes. )

ODD_SECTOR_ANSWER = NO-SINGLE-COMPOSITE-KILL-ON-THE-SEALED-BASIS(
  sweep CS-1..CS-9 displayed §3.2: the promotion and the weight-moment
  kills are SUM facts with no object in the composite; the collapse
  annihilates only the baseline (X-5, already spent); the phase skeleton
  GRADES, never kills; the boundary commutator is a divergence source
  (kappa_n -> +infinity of record); the would-be closure majorant lives
  in the uncertified C-L2 layer; the edge budgets are refused on
  direction, type, and their own gate bar. tr H_A and ||H_A||_2^2
  UNDECIDED. )

CARRIERS_NEEDED = SUBSIDIARY( by ee91d85c's own missing_carrier strings:
  R5 member-named topologies with continuity proofs (= the certified-
  topology absence at F1, the MO-3 shape); R1 a member-supplied sealed
  prequotient rule with one displayed evaluation on a named oriented
  k-cell; R10 a candidate normalization object with its two typed inputs
  named to one record cell (= the entry-15 magnitude vacancy). U1 noted
  adjacent, not claimed. C-L2 reported as E1's own witness, not a
  junction carrier. )

MO_MAP = ( F1 = MO-3 exactly; MO-4 absence re-confirmed by the exact
  splitting (no product-level cancellation on this route; r-3 §4.4 sweep
  consumed unchanged); MO-1/MO-2 NOT ENGAGED (the four quantities are
  baseline-free; R_n nowhere; H-R never defaulted; the poisoning ceiling
  ||R_n||_op <= exp(-log|det_n(0)|), -log|det_n(0)| >= kappa_n ->
  +infinity, displayed WB8 only to bar the detour); F3 is a blocker
  OUTSIDE MO-1..MO-4 — E1's own named witness, load-bearing on this
  route. )

GRAVEYARD_CONSISTENCY = NOT-RE-WALKED( no per-pair majorant chain; no
  H-R default or use; no summed-to-per-pair bridge; no fixed-n region;
  the route's distinction is the closure attack and its three sealed-text
  stops; the sealed obstruction (o-2-OBS, audited "real and unpassable")
  is consumed as ground and gone AROUND, not through. )

SEALS_VERIFIED = 14/14( d66a922c; 4ebe6b8f; 798de0dd; c944a901;
  46846730; 789338ad; b4034f04; ae2202e0; a4c2b7df; ee91d85c; 3b5e95b6;
  bebc0f08; a4cf394c; 72c95d42 — all recomputed from bytes at path this
  session by full digest; all match sidecars and every tasked prefix;
  none unverifiable; every load-bearing span read at path; the r-2/r-3
  pair located by tasked prefix via full-workspace hash sweep. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant symbolic
  or an exact rational/surd; NOTHING numeric evaluated (Lambda, n, c_0,
  eps-free throughout — the [a^1] slot needs no polydisc radius); no
  measured constant; no value frozen; alpha never computed or
  approached; kappa_record never computed; M(t) and 1_{D_t} sharp
  (D6' respected); the stricken display (E1 :773-778) consumed nowhere
  as ground; H-R nowhere defaulted; the BAR not crossed (no per-pair
  majorant chain); no register|road_|ledger|lens|plan|tracker|
  THE_HANDOFF file opened; "Q-..." tokens EXPECTED-UNLOCATABLE, noted
  not chased; the NEUTRALITY CLAUSE held — the motivating reading
  appears in no step; no flag flipped; no witness action. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv wbcvenv under
  the session scratchpad, ONE battery, 8 checks, 8/8 PASS on a single
  run, NO pre-final corrections, script and output reproduced verbatim
  in §7; shasum/file reads otherwise; NOTHING numeric evaluated.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
WBC_WALL_BOUNDARY_CLOSURE_RESULT = SEALED.
```
