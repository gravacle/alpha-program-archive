# STAGE8 — THREE ABSENCES — O41SR — DEFAULT-REFUTE AUDIT — V001 (2026-08-16)

Lane: **DEFAULT-REFUTE AUDIT.** Default verdict REFUTED. Adversarial by mandate. Nothing in the
build was taken on report; every load-bearing span was re-derived at bytes in this session.

**TARGET:** `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_THREE_ABSENCES_O41SR_V001.md`
sha256 `dddf438fc72f4df2cfbc0d3110befb5d7b24f100ebdd98dce2afffcb8ce4e830` — re-hashed here and
`shasum -a 256 -c` verified from its own directory: **OK**. Both its sidecars present and OK.

**FENCES, live throughout:** `alpha_computed = false` · `proof_authorized = false` ·
`kappa_record_computed = false`. This audit computes no numeric value of any coupling, scale,
root, eigenvalue, norm, or constant. It does not attempt, reach, or evaluate the magnitude. It
authors no object and no clause, adopts nothing, advocates nothing, fills no gap, and re-reads
no catalogued negative. Every numeral below is a quoted dimension, a hit count, a line number,
or a digest.

**STATUS:** CLAIMED / PROPOSED-NOT-ADOPTED.

---

## §0 — VERDICT BLOCK

```text
PER-DIMENSION GRADES

  D1  The three absences (Q1)          CONFIRMED-WITH-CORRECTIONS
        - all three located correctly    CONFIRMED
        - graded off deciding words      CONFIRMED (the hunted defect is absent)
  D2  Fillability (Q2)                 CONFIRMED-WITH-CORRECTIONS
        - all three NOT-HELD-NOT-EXCL.   SUSTAINED
        - "no impossibility anywhere"    REFUTED  (COR-B)
  D3  The forcing threshold (Q3)       CONFIRMED-WITH-CORRECTIONS
        - threshold definition           CONFIRMED
        - crossing dimension 3           SUSTAINED (provenance corrected, COR-G)
        - provenance ENTERED             SUSTAINED (on the carriers read)
        - "ADOPTED is excluded at bytes" REFUTED  (COR-A)
  D4  Phase data (Q4)                  CONFIRMED-WITH-CORRECTIONS
        - no adjacency inflated          CONFIRMED (the hardest test; passed clean)
        - enumeration closed             REFUTED  (COR-D, COR-E)
  D5  The two candidates (Q5)          CONFIRMED-WITH-CORRECTIONS
        - both identifications           CONFIRMED
        - UNBUILT / BARRED-BY-A-RULE     SUSTAINED
        - "neither excluded by any proof
           anywhere"                     REFUTED  (COR-B)
  D6  Bars and fences                  CONFIRMED-WITH-CORRECTIONS
        - fences, bars, leak counter     CONFIRMED (zero incidents)
        - seal tally 33/34               REFUTED -> 34/34  (COR-F)

OVERALL:  CONFIRMED-WITH-CORRECTIONS

  No graded ANSWER of the build is overturned. Six of its stated WARRANTS
  are: the exclusion of ADOPTED (COR-A), the absence of any impossibility
  (COR-B), the "sentence after the impossibility" (COR-C), the closedness
  of the Q4 enumeration (COR-D, COR-E), and the seal declaration (COR-F).

  THE SEVERE ERROR THE COMMISSION NAMED — a barred-by-rule candidate
  graded excluded-by-proof, or a derived dimension graded entered — IS NOT
  PRESENT. The build's failures are all of ONE shape, twice: a narrow
  search, a zero, and a universal asserted over it. At the DOR rulings it
  ran eight literals, got zeroes, and declared ADOPTED excluded. At S-05 it
  ran one phrase, got nine files, opened none, and declared no
  impossibility exists anywhere. Both declarations are false at bytes.
  Neither answer they support is.
```

---

## §1 — SEALS, RE-RUN

`shasum -a 256 -c` run from each artifact's **own directory**, this session.

**WORKSPACE — 20 / 20 consumed sources OK.** Every one of the build's twenty named sources
verified OK, **including `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md` [BRG], which
the build declared unsealed.** See COR-F.

**SUPERVISION (DOR_* only, as expressly permitted) — 14 / 14 OK.** All fourteen `DOR_*.md`
rulings verified against their `.md.seal.sha256` sidecars: 14 OK, 0 FAIL. No other file in that
directory was opened.

**TARGET — 1 / 1 OK.**

**TOTAL SEAL TALLY THIS AUDIT: 35 / 35 OK. Zero unsealed sources.**

**REACH LIMIT, declared, same as the build's.** `SURFACE_DEFINITION_OF_RECORD_V00{1,2,3}.md`
sits in the barred supervision directory and was **NOT opened** here either. Every §I / §II
clause below is quoted as a workspace artifact carries it, carrier named.

---

## §2 — D1: THE THREE ABSENCES — **CONFIRMED-WITH-CORRECTIONS**

### 2.1 Did the build locate the right sentences at all?

**Yes, all three, verified by independent grep and by reading the surrounding lines.**

| Absence | Build's cite | Verified at bytes |
|---|---|---|
| A-1 clause | [AJ] §0 :29–35; §6.2 :237–243 | `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md` — §0 verdict Class II arm spans **:29–34**; §6.2 "Ground fact" spans **:237–243**. Both located correctly; the §0 span is :29–34 not :29–35 (off-by-one, immaterial) |
| A-2 object | [RL] §5.2 :393–402; §5.3 / flags | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` — §5.2 heading :392, body :394–403; §5.3 :405–417; flag block :497–510. Located correctly |
| A-3 value | [AJ] §6.3 kill 2 :282–285 | `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:282-285` — **exact**. Governing header §6.3 at :270 |

**The digest identification is correct.** `shasum -a 256 STAGE8_R_RECORD_L_FORM_FABLE_V001.md`
= `5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37`. The corpus's `5e49d209`
is that file. Re-derived independently.

### 2.2 Were the grades read off DECIDING WORDS or off HEADLINES?

**Off deciding words, in all three cases. This is the defect the dimension was sent to hunt and
it is not present.**

**A-1.** The build's grade (NEGATIVE-RESULT scoped to the displayed set; ABSENCE at any wider
quantifier) rests on the enumeration warrant, not on the word "Ground fact". Warrant verified
whole across a line wrap at `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:144-145`:

> "the ground is a finite sealed text, so its displayed objects and operations are enumerable
> by inspection"

and its §8.2 restatement at `:538-541` — `**(Finite-text warrant)** [DERIVED, same warrant as
the audited I-2 pair]`. The prose half of M8 is completed mechanically by the sealed audit;
[AJ-A] C-3 verified whole at `STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md:210-214`. And the
reverting words the build carried are real: `:535-538` reads verbatim

> "Theorem II's conservativity core does NOT depend on C12 — it rests only on the displayed
> equation set — so even under a weakened §II.6, what would remain is: no junction is FORCED by
> the displayed ground; only the impossibility-of-supply-from-withheld-content would reopen."

**A-2.** Every absence-vocabulary phrase the build made deciding is at bytes: `"both unavailable
of record"` (`STAGE8_R_RECORD_L_FORM_FABLE_V001.md:398`), `"blocked on SPEC GAPS"` (`:400`),
`"sealed UNAUTHORIZED until"` (`:401`), `"So the forcing, if it exists, lives in an unbuilt
external crossing, not in the cross-sector term."` (`:402-403`), and the site heading
`WINDING_FORCED = NOT-FORCED` (`:408`, `:497`). **The corpus's vocabulary separation is real and
was checked here independently**: [XBR] U-F1 verified whole at
`STAGE8_CROSS_BLOCK_READ_V001.md:265-267` — `"HOLDER: a lane derivation (none exists; 92dba8d3
records absence-of-construction, not prohibition)"` — and the phrase `absence of construction,
not prohibition` occurs in exactly 1 workspace file / 0 cleanroom, as the build reported.

**A-3.** The build did NOT read the grade off the headline "The ground's own carrier is below
the forcing threshold." It read it off `carrier uniqueness NOT derived` at
`STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md:74-76`, verified whole across
three source lines, and off [AJ-A] C-2 at `STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md:203-211`,
verified whole. **This is the correct reading and it is the adversarially harder one** — the
build graded its own strongest-looking separator as the softest of the three.

### 2.3 Line-wrap check on every load-bearing span

Twelve load-bearing spans were re-checked by reading the surrounding lines, not by
line-oriented grep. **All twelve are contiguous and faithful.** Two traps were live:

- `[GLUE]` §6.2's quote wraps as `"define a complex line / bundle"` across
  `STAGE8_GLUING_CANDIDATE_O23SR_V001.md:563-564`. **A line-oriented grep for the phrase returns
  ZERO.** This audit's first pass returned zero and nearly filed a fabrication finding; reading
  `:556-570` shows the build's quotation is exact. Disclosed as own-draft defect OD-1.
- `[EIN]` §1.1's `carrier uniqueness NOT derived (...)` wraps across `:74-76`; the build carried
  it whole.

### 2.4 Corrections in D1

**COR-J** (this section) and **COR-K**, **COR-L** (see §8). None disturbs an A-grade.

**Also carried against the build, because it cuts toward A-3's threshold sentence being MORE
load-bearing than the build allowed:** §2.3 of the build says the threshold sentence "is
load-bearing for this commission only because [JCT], [TYP], and [THF-A] all carry it forward".
At bytes **[AJ] carries it forward twice in its own voice**, and neither is quoted in the build:

- `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:36-37` (the §0 verdict itself): `"the strongest
  forcing sub-route (a Gleason-style consistency forcing) killed three independent ways
  including on the ground's own dim-1 line bundle (§6.3)"`
- `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:682` (the sealed `JUNCTION_STATUS` flag): `"the
  Gleason-style forcing objection killed three ways (adjoined substrate; dim-1 line bundle below
  the forcing threshold; joint constraints absorbed by both models)"`

This does not overturn the build's grade — [AJ-A] C-2 is the corpus's settled narrowing and the
build applied it correctly — but the build's account of WHERE the sentence travels is incomplete
at bytes.

**D1 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected sub-grades: A-1 **NEGATIVE-RESULT scoped to the displayed equation set; ABSENCE at any
wider quantifier — SUSTAINED**. A-2 **ABSENCE, unambiguous — SUSTAINED**. A-3 **ABSENCE as to
the value, with a negative result narrower than its headline — SUSTAINED**, with the
carry-forward list corrected to include [AJ]'s own :36-37 and :682.

---

## §3 — D2: FILLABILITY — **CONFIRMED-WITH-CORRECTIONS**

### 3.1 Every SHOWN-IMPOSSIBLE: quantifier verified, and the sentence AFTER it read

| Impossibility | Build's quantifier | Verified at bytes |
|---|---|---|
| Theorem II | `"Let G be the sealed ground (§2.2)"` … `¬∃J` | `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:507-508` **verbatim**. Ground row 3 `"3. Nothing else."` at `:92`. Quantified over candidate junctions on one named ground — **CORRECT** |
| [AJ] §6.4 | four named withheld items; CONDITIONAL (K10, C12) | `:294-303` for the claim; `:571` reads `\| K10 \| Withheld content cannot supply the forcing equation \| CONDITIONAL (C12 — inherited Chain E typing, §6.4) \|` — **CORRECT** |
| [RL] §5.3 | n-blind channel *INSIDE* `R_record,L`, on the ratified content | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md:415-417` **verbatim**; §5.2's closing sentence does sit immediately before §5.3 (`:402-403` then heading at `:405`) — **CORRECT** |
| Gleason kill 2 | `dim ≤ 2`; narrowed by [AJ-A] to the minimal fiber-local substrate | `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:283-284` and `..._AUDIT_V001.md:203-211` — **CORRECT**, and the next sentence naming kills 1 and 3 is verbatim at `:208-210` |

**Three of the four "sentence after" checks are right. The fourth is wrong at bytes — see COR-C.**

### 3.2 COR-C — the sentence the build said was "immediately following" is not

Build `:364-370` claims: *"[AJ] §8.3's closing sentence, **immediately following the supply-route
statement**, quoted whole"* and then quotes the downstream-emergence sentence.

At bytes there is a sentence **between** them, `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:552-554`,
quoted whole here:

> "An exit operation alone leaves the two-model freedom intact; a forcing clause alone is barred
> at C12's strength unless the categorical wording is simultaneously re-scoped by a booked
> result."

That interposed sentence is **the one adverse clause at A-1's supply site** — a *bar* on
supplying the very clause A-1 names absent. Grep of the build for `exit operation alone`,
`forcing clause alone`, `re-scoped by a booked result`: **0 hits.** It appears nowhere in the
build artifact.

**Does it overturn the grade? No.** It is a bar with a stated lifting condition
(`"unless the categorical wording is simultaneously re-scoped by a booked result"`), which by
the build's own — correct — test is not a proof of impossibility. **A-1 fillability stays
NOT-HELD-NOT-EXCLUDED.** But the build's evidence base at that site is incomplete and its
"immediately following" is false at bytes.

### 3.3 COR-B — the independent exclusion hunt found what the build's own sweep surfaced and it did not read

The build states, of A-2, at `:392`: **"No impossibility of the comparand is shown anywhere."**
And at `:962-963`: **"NEITHER CANDIDATE IS SUPERSEDED, AND NEITHER IS EXCLUDED BY ANY PROOF
ANYWHERE IN EITHER CORPUS ROOT."**

**Both absolutes are false at bytes.** The decisive fact is that the build's **own sweep S-05
(`n-blind comparand`, 9 workspace files) returned both of the artifacts below.** The count was
reported; the hits were not inspected; a universal negative was then asserted over them.

**S-05's nine files, re-derived here in full:** `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md` ·
**`STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md`** · `STAGE8_FORCING_NOTION_O12SR_V001.md` ·
`STAGE8_CROSS_BLOCK_READ_V001.md` · `STAGE8_5D_SYMMETRIC_AUDIT_T13SR_V001.md` ·
`STAGE8_R_RECORD_L_FORM_FABLE_V001.md` · `STAGE8_5D_REREAD_T13SR_V001.md` ·
`STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md` · **`STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md`**.

**FINDING 1 — a structural exclusion attached to CANDIDATE 2 by name.**
`STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md:420-425`, quoted whole across five source lines:

> "This is the same NOT-FORCED landscape T1 reached from the intrinsic side, sharpened by one
> new structural fact: T1 left the `Z_Q` comparand as "sealed-unauthorized until built"; this
> construction shows the stronger statement that even WHEN built from the record's ratified
> charged content, the `Z_Q` comparand CANNOT be n-blind — the unavailability is structural, not
> merely a build-state fact."

and its flag, `:405-416`, final clause quoted whole:

> "The one n-blind reading of the comparand is barred three ways: faithfulness-fed provenance,
> sealed post-derivation ordering of the unit-character convention, and circularity against the
> equation's own output."

`CANNOT`, `structural, not merely a build-state fact`, and `the stronger statement` are
**prohibition-vocabulary by the build's own §2.0 taxonomy**, applied to the exact property
(n-blindness) that defines A-2's absent object, on the exact candidate the build graded
barred-by-rule-only.

**Seal status of this artifact, declared, not buried.** Its sidecar
`STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md.seal.sha256` contains the digest **without the
filename field**, so `shasum -a 256 -c` cannot parse it. The digest matches byte-for-byte:
`68cce5235ebda5864894701425e183a73919bca4eec20bb350c1dca10c4d5e52`. It is corroborated inside
two artifacts whose own seals verify **OK** here:
`STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md:62` (`68cce523` MATCHES-TASKED) and
`STAGE8_NDIST_CHECK_DARIO_V001.md:28`. **Grade: SEALED-IN-SUBSTANCE, SIDECAR MALFORMED.**

**FINDING 2 — a derived structural dichotomy closing the neutral survey.**
`STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md` (seal **OK**), `:31-32`:

> "No n-blind neutral comparand derives — the survey closes under a structural dichotomy"

**AND ITS OWN SELF-LIMIT, CARRIED WHOLE BECAUSE IT CUTS AGAINST READING FINDING 2 AS AN
EXCLUSION** — `:374-378`:

> "IT DOES NOT say the winding can never be fixed. It says the neutral side and the boundary do
> not force it ON THE RECORD'S DERIVED CONTENT; F-OBJ-1 and F-OBJ-2 are named, genuinely open
> build targets, and a derived n-blind comparand would reopen the question at exactly §5's typed
> mechanics."

and `:359-363`: `"Both routes fail on the same single absent object ... the failure is
existence, not uniqueness."`

**THE CORRECTED GRADE, stated exactly.**

```text
A-2 FILLABILITY, corrected:

  AT THE OBJECT LEVEL (an external n-blind comparand as such)
    NOT-HELD-NOT-EXCLUDED  —  SUSTAINED, and now CORROBORATED by a sealed
    artifact the build never opened, which self-limits in its own voice:
    "IT DOES NOT say the winding can never be fixed ... genuinely open
    build targets."

  THE BUILD'S ABSOLUTES  —  REFUTED at bytes:
    "No impossibility of the comparand is shown anywhere" (build :392)
    "NEITHER IS EXCLUDED BY ANY PROOF ANYWHERE IN EITHER CORPUS ROOT"
      (build :962-963)
    An impossibility IS shown of record, at a quantifier the build never
    reported: Z_Q comparands built from the record's ratified charged
    content CANNOT be n-blind, "structural, not merely a build-state fact".

  THE QUANTIFIER OF THAT IMPOSSIBILITY, reported as the commission requires:
    scope  = the Z_Q comparand, built from the record's RATIFIED CHARGED
             CONTENT. Not over external comparands generally; candidate 1
             (the energy form) is untouched by it.
    header = EQUATION_FORCES_UNIQUE_N = DOES-NOT-FORCE — never REFUTED.
    limit  = the same artifact's own "WHAT THIS DOES NOT SAY" block:
             "IT DOES NOT say Z_Q is built. Slot 5 is MISSING_SPEC".
```

**Nothing is filled here.** This audit adopts neither finding, builds on neither, and relates
neither to any magnitude. Both are reported as byte-facts with their own self-limits carried.

### 3.4 A-1 and A-3: independent exclusion hunt, run

Patterns run over both roots (counts in §9): `no comparand` 0/0 · `no such comparand` 0/0 ·
`no external comparand can exist` 0/0 · `clause is impossible` 0/0 · `forcing equation is
impossible` 0/0 · `provably no clause` 0/0 · `no change of ground` 0/0 ·
`WINDING_FORCED = REFUTED` 0/0 · `carrier is unique` 0/0 · `must be one-dimensional` 0/0 ·
`rank one is forced` 0/0. **No exclusion of A-1's clause or A-3's value found beyond the ones
the build reported.** A-1 and A-3 fillability grades **SUSTAINED**.

**D2 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected: all three fillability grades **NOT-HELD-NOT-EXCLUDED — SUSTAINED**. The build's
absolutes at `:392` and `:962-963` **REFUTED**. A-1's site carries an undisplayed conditional
bar (`STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:552-554`).

---

## §4 — D3: THE FORCING THRESHOLD — **CONFIRMED-WITH-CORRECTIONS**

### 4.1 The threshold's definition — **CONFIRMED**

`STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:283-285`, re-read at bytes with surrounding lines:

> "Frame-function forcing fails in dimension ≤ 2 even when full inner-product structure is
> granted; on dim-1 fibers there is no nontrivial frame consistency to force anything. The
> ground's own carrier is below the forcing threshold."

The build's symbolisation (`QUANTITY: dim` · `FAILURE: dim <= 2` · `SITE: dim = 1` · `VERDICT:
below`) is a faithful restatement of the displayed sentence. Independent sweep confirms **no
separate threshold constant is displayed anywhere**: `forcing threshold` = 5 workspace / 0
cleanroom files, all five tracing to `[AJ] :285` and its citations. **CONFIRMED.**

### 4.2 The crossing dimension — **CONFIRMED, provenance corrected (COR-G)**

`STAGE8_THRESHOLD_FORM_O19SR_AUDIT_V001.md:421-424` verified whole:

> "A THRESHOLD IN A DIMENSION, with a quantity (fiber dimension), a crossing (≤ 2 fails / 3 and
> above forces), and the record's own site displayed BELOW it. AND IT IS A **FORCING**
> THRESHOLD — it governs whether content is FORCED, i.e. it sits directly on the allow/require
> axis this commission was sent to survey."

**`3 and above forces` is at bytes and the build did not invent it. But it is [THF-A]'s own
gloss, not a ground clause** — `[AJ] :283` states only the failure side, `"fails in dimension
≤ 2"`. The build's IMPORT AUDIT row I-1 names exactly two import sites, *"inside the quoted [AJ]
§6.3 kill 2, and inside the quoted [AJ-A] C-2"*, and **omits [THF-A] — the site the entire Q3
crossing answer rests on.** COR-G.

**CORRECTED: crossing dimension = 3, as the corpus states it — SUSTAINED. Its provenance is
IMPORTED-BY-[THF-A], a third Gleason-import site that must be added to the IMPORT AUDIT.**

### 4.3 Provenance of the fiber dimension — the chain, re-derived step by step

| Step | Build's claim | Verified at bytes |
|---|---|---|
| 1 | `"fibers of dimension 1"` is [AJ]'s unpacking of §I.4's "fixed line bundle"; §I.4 as carried does not contain the phrase | `STAGE8_S13_THRESHOLD_SHAPE_FABLE_V001.md:93-95` carries §I.4 verbatim and contains **no** "fibers of dimension"; `[JCT-A] :85-88` types the splice `MATCH (build splices with ellipsis, faithful)` — **CONFIRMED** |
| 2 | ray-in-C² + U(1) redundancy ⇒ line bundle **is** displayed and **is** derived | `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:18-20`, `:27-41`, flag `local_projective_record_bundle_derived = true` at `:122` — **CONFIRMED**. [GLUE] corroboration verified at `STAGE8_GLUING_CANDIDATE_O23SR_V001.md:562-565` (wrap trap; see §2.3) |
| 3 | the premise is flagged not-derived and imported | `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md:208-211` and `STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md:76` — **CONFIRMED** (with COR-K) |
| 4 | ADOPTED excluded — 0 hits across 14 DOR_* | **REFUTED — COR-A** |
| 5 | grade **ENTERED** | **SUSTAINED on the carriers read**, with ADOPTED no longer excluded |

### 4.4 COR-A — the heaviest correction in this commission

**The build's Step-4 conclusion, at `STAGE8_THREE_ABSENCES_O41SR_V001.md:580`:**

> "**No authored ruling of record touches the fiber dimension. ADOPTED is excluded at bytes.**"

**and its supporting inspection claim, at `:575-577`:**

> "`rank` 8 -> ALL are cycle-rank / rank-two MEMBER classes (graph and member objects), NOT
> bundle-fiber rank. Inspected, not counted."

**BOTH ARE FALSE AT BYTES.**

**(a) A sealed, ADOPTED ruling of record types a bundle with connection over the record
surface.** `/Users/bgm/MB Work/alpha-program-archive/supervision/DOR_020_A1_WHERE_CLAUSES_AMENDMENT_2026-08-04.md`
— seal verified **OK** this session — reads, whole, at `:10-15`:

> "1. THE PATH/CURRENT SUPPORT CORRESPONDENCE — the law assigning currents to the
>    paths that carry them (what lives where), law-only.
> 2. THE LOCAL FIELD MEMBERS — typed as a U(1) BUNDLE WITH CONNECTION over the
>    record surface, with the bundle lift/pullback-bundle isomorphism, smooth
>    full-rank, and characteristic-class compatibility; transport derived from
>    the declared members. Law-only."

Its title line is `# DECISION OF RECORD 020 — AMENDMENT 1: THE WHERE-CLAUSES (ADOPTED)` (`:1`),
its ruling line `"Principal: Brian (ruling: "Adopt the amendment")"` (`:3`), and its Of-record
line at `:28-30`: `"gravity's declared contribution is a stage already fibered for charge — the
U(1) bundle geometry of electromagnetism lives inside the where-declaration."`

**(b) The `rank`-row inspection claim is false for at least one hit and inaccurate for a
second.** `DOR_020_A1...:14`'s `full-rank` sits **inside the bundle-typing clause** — it is a
bundle rank clause, not a cycle-rank or member-class one. `DOR_019_CARRIER_METRIC_AND_UNITS_
RATIFICATION_2026-08-03.md:14`'s `"the W3 rank-preserving isometry"` is a metric-isometry rank,
also neither. The remaining six (`DOR_017:43`, `DOR_018:24`, `DOR_020_A3:16,27,41`,
`DOR_020_A6:8`) **are** cycle-rank / rank-two member classes, as the build said.

**(c) The build's eight DOR patterns could not have found it.** All eight are literal strings
that `DOR_020_A1` does not contain. **A broader DOR re-sweep, run here across all 14 rulings**
(counts in §9), returns file-hits the build never ran: `bundle` **1** · `dimension` **1** ·
`carrier` **3** · `fiber` **5** · `U(1)` **1** · `C2` **1** · `line` **7**. Every one was opened
and inspected; only `DOR_020_A1` bears on the question.

**THE CORRECTED GRADE, stated exactly.**

```text
Q3 PROVENANCE OF THE FIBER DIMENSION OF 1 — CORRECTED

  NOT DERIVED   SUSTAINED. The bundle-rank step is derived; its carrier
                premise is flagged not-derived and imported at
                BRG :208-211 / EIN :76.

  NOT ADOPTED   *** REFUTED ***  ->  ADOPTED IS **NOT EXCLUDED**.
                A sealed ADOPTED ruling of record (DOR_020_A1 :12-15) types
                THE LOCAL FIELD MEMBERS as "a U(1) BUNDLE WITH CONNECTION
                over the record surface ... smooth full-rank". Whether that
                bundle is the same object as SI.4's "fixed line bundle" /
                [LPRB]'s "principal U(1) comparison bundle" is
                INDETERMINATE-AT-BYTES: the ruling names the continuum
                theory's local field members and gravity's declared
                contribution; [LPRB] names the primitive projective record
                carrier on admissible causal record cells. Neither names
                the other. THIS AUDIT DOES NOT IDENTIFY THEM.

  ENTERED       SUSTAINED **on the carriers read**, with the ADOPTED
                question left open rather than closed.

  DIRECTION OF THE COMMISSION'S ANSWER — UNCHANGED. On the commission's
  own test (a derived dimension is a wall, an entered one is a choice),
  an ADOPTED dimension is also a CHOICE, not a wall. The build's
  headline "This is a choice, not a wall" SURVIVES. What does not
  survive is the sentence "ADOPTED is excluded at bytes" and the
  DOR rank-row inspection that was offered as its warrant.
```

### 4.5 COR-H — the universal negative at §4.4 rests on sweeps that do not exist

Build `:648-650`: *"**NOTHING FOUND ON THE OTHER SIDE.** No counterfactual, no statement, and no
parameter anywhere in either corpus root asserts the fiber dimension is forced, unique, or
underived-but-necessary. **Sweeps in §10.**"*

§10 contains **no** corpus sweep for `fiber dimension`, `forced`, `unique carrier`, or any
negative-hunting pattern. S-04 swept only `fibers of dimension`. **The cited warrant does not
exist.**

Run here: workspace `fiber dimension` = **5** files, two of which the build never touched —
`STAGE8_RK_LT_BIT_T1SR_AUDIT_V001.md:416` (`"transports, and fiber dimensions are FORCED by the
stated hypotheses"`) and `STAGE8_RK_LT_BIT_T1SR_V001.md:679`. **Both are inside TOY_SEPARATION
blocks about toy-model reader fibers**, each carrying its own quarantine — `"The A2 and A4
models decide NOTHING about the sealed carriers"` (`:414-415`) and `"No toy value, stage,
address, dimension, or number enters the verdict or any flag"` (`:417-418`). **The build's claim
therefore survives in substance, on this audit's sweep rather than on its own.**

### 4.6 The three things bearing on other-than-1 — **CONFIRMED**

(i) `unique_primitive_carrier_derived = false` / `pure_C2_comparison_representation_imported =
true` — verified at `BRG :208, :211` and `EIN :76`. `bloch_ball_derived = false` /
`M2C_algebra_derived = false` — verified at `BRG :209-210` **only** (COR-K).
(ii) The **ray** (`LPRB :19-20`) and the **U(1)** redundancy (`LPRB :31`) are both displayed and
neither is derived; the ambient `z_i : U_i -> C^2` is displayed at `LPRB :23` and again at
`STAGE8_GLUING_CANDIDATE_O23SR_V001.md:557`. **The build's refusal to relate the ambient
dimension to `dim ≤ 2` is verified as held — no such relation appears anywhere in the build.**
(iii) [AJ-A] C-2's ruling that the fiber's dimension is not the dimension the threshold governs
— verified whole at `..._AUDIT_V001.md:204-206`.

**D3 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected: threshold definition **CONFIRMED**; crossing dimension **3 — SUSTAINED**, provenance
corrected to IMPORTED-BY-[THF-A]; fiber-dimension provenance **ENTERED — SUSTAINED on the
carriers read**; **"ADOPTED is excluded at bytes" — REFUTED**, replaced by ADOPTED NOT EXCLUDED /
INDETERMINATE-AT-BYTES; §10's DOR `rank`-row inspection claim **REFUTED**; §4.4's universal
negative **sustained in substance, warrant void**.

---

## §5 — D4: PHASE DATA — **CONFIRMED-WITH-CORRECTIONS**

### 5.1 The test that mattered most: was any adjacency inflated into a link? — **NO. CLEAN.**

Checked exhaustively across the build artifact. **No link is constructed anywhere.**

- §5.4 draws the scope line **before** the finding (`[AJ] §2.2 row 3 "Nothing else."`, verified
  at `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:92`), states `"NO CORPUS ARTIFACT JOINS THIS SITE TO
  THE GROUND WHERE A-1 LIVES"`, and refuses in its own voice: `"This lane relates them nowhere
  either."`
- The two displayed `π`s (`Delta Phi_* = pi` and `|ΔS_record|/ℏ = π`) are recorded and **not**
  related. CHOICE LEDGER row 8 marks the refusal FORCED. Verified: no sentence anywhere in the
  build relates them.
- ADJ-2 and ADJ-3 are correctly typed **phase-to-phase, not phase-to-magnitude** —
  `BARE_SURFACE_I2_DETERMINATION_V001.md` L1 and `..._AUDIT_V001.md:63` verified whole.
- ADJ-4 correctly reports L2 as an assertion with no displayed form, and correctly attributes the
  pairing to [AJ-A] C-1 rather than to itself — verified whole at
  `STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md:39-42`.

**This is the strongest part of the build and it holds under adversarial reading.**

### 5.2 COR-D — an adjacency site missed, inside a source the build itself consumed and sealed

The build's §5.3 criterion is *"every site where a phase datum and a response magnitude sit on
ONE carrier"*, and §5.5 states the result as a closed count: `"ADJACENCY ON THE GROUND: FOUR
sites"` + `"ADJACENCY OFF THE GROUND: ONE site"`.

**At bytes there is at least one more, on the very carrier the build's own Q3 analysed.**
`STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` — [EIN], seal **OK**, the
build's own source #14 — displays, in §1.1's single paragraph on the primitive projective record
carrier, immediately after the two lines the build did quote:

`:76-78`, quoted whole:

> "**Fubini-Study**: fixes *dimensionless* distances only — V002 `:63-65`; the convention itself
> is imported (`Fubini_Study_convention_imported = true`)."

`:81-84`, quoted whole across four source lines:

> "**Phase periodicity**: compact action coordinate period `2 pi hbar`, winding `k = +1`
> (imported premise); balanced-geodesic content `p = 1/2, theta = pi mod 2 pi`; `J_FS = pi hbar
> / 2`; `Delta S_record = pi hbar = 2 J_FS` — an **action**, with the non-identifications sealed
> (not the cell's duration or volume)."

**WHAT SITS ON ONE CARRIER, reported and nothing more.** In the clause `"balanced-geodesic
content \`p = 1/2, theta = pi mod 2 pi\`"` a **probability-valued datum** and a **phase** are
displayed in one expression. In the same sentence `"Delta S_record = pi hbar"` — the ground's
write action, which [I2] §2.2(b) books in the write/response sector — stands beside
`"theta = pi mod 2 pi"`. And Fubini–Study is displayed as fixing **distances** — magnitude-valued
— on a projective (phase) carrier.

**Why this is on the build's own criterion, not an import.** `BARE_SURFACE_I2_DETERMINATION_
V001.md:37` books in the write/response sector both `"first-opening probabilities"` and
`"the balanced-geodesic identities (§II.9 — named; form not displayed)"`. [EIN] displays *content*
for a ground-**named** object whose form the ground does not display.

**NOTHING IS RELATED HERE.** This audit does not relate `theta = pi mod 2 pi`,
`Delta S_record = pi hbar`, `Delta Phi_* = pi`, or `|ΔS_record|/ℏ = π` to one another or to
anything else. No value is computed; no identity is applied; no clause is authored. The
co-display is reported as a byte-fact, with its own adverse flags carried: [EIN] marks the
Fubini–Study convention `imported`, the winding `k = +1` an `(imported premise)`, and says of the
adjacent onset budget *"Saturation is adopted, not derived."* (`:85`).

**CORRECTED: the Q4 adjacency enumeration is NOT closed. At least FIVE sites, not four-plus-one.
The build's no-link discipline is CONFIRMED; its count is REFUTED as exhaustive.**

### 5.3 COR-E — the on-ground phase break-out drops two of the six items in its own quoted inventory

The build quotes `BARE_SURFACE_I2_DETERMINATION_V001.md:36` whole as the ground's phase-sector
inventory, then breaks it out as ten rows P-1..P-10. **Two of the six items in that very sentence
never become rows:**

- `"affine connection space, no fiber metric, no distinguished connection (§I.4)"` — the affine
  connection space and the connection `a` itself, booked by [I2] inside the phase/holonomy sector.
- `"the exact refinement/coarsening law of the cell (§I.5 — named as exact and derived; its
  explicit form is not displayed on the sealed ground)"`.

**Also absent, though displayed on a carrier the build consumed and quoted from:**
- the curvature `f|U_i = d a_i` (`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:76`);
- the comparison character `W_n(gamma) = exp(i n integral_gamma a), n in Z`
  (`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:86-88`);
- the connection one-forms `a_i` and the patching law `a_j = a_i + d theta_ij`
  (`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:54-69`).

**CORRECTED: §5.1's quotation of the inventory is CONFIRMED whole; its break-out is REFUTED as
exhaustive. The on-ground phase-object count is at least 12–14, not 10.** No grade in the build
rests on the count, so no verdict moves.

### 5.4 What the build got right in Q4, verified

All fifteen rows spot-checked against their cited carriers; the four sampled in full
(P-1 `[I2] :36`; P-6 `[I2-A] :63`; P-10 `[I2-A] :9`; P-11/P-15 `[DPHI] :128-155`) are faithful,
whole, and carrier-named. The [DPHI] site at `STAGE8_DELTAPHI_PI_PROTECTION_DETERMINATION_
V001.md:136-147` is quoted exactly, including `Delta Phi_* = pi`; the four adverse flags at
`:164-168` are quoted exactly; the denial at `:180-181` (`"It is not sealed as a Berry phase,
spectral flow, index, or already physical electromagnetic holonomy."`) is quoted whole and
correctly used to bound the enumeration. **S-15 re-run: 1 workspace / 1 cleanroom — the same
file. CONFIRMED.**

**D4 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected: **no adjacency inflated into a link — CONFIRMED, clean.** Adjacency count corrected
from **four-on-ground + one-off-ground, closed** to **at least five sites, NOT CLOSED**.
On-ground phase-object count corrected from **10** to **at least 12–14**.

---

## §6 — D5: THE TWO CANDIDATES — **CONFIRMED-WITH-CORRECTIONS**

### 6.1 Both identifications — **CONFIRMED at bytes**

`STAGE8_R_RECORD_L_FORM_FABLE_V001.md:397-402` names exactly two: the energy form
`c_L <.,.>_(2,ell)` and the conversion of `kappa_record` to a physical action coefficient. The
flag-block restatement at `:505-510` is quoted by the build **verbatim and whole**. The corpus's
own typing, `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:368` — `"both prior candidates
unavailable — one fork-gated, one UNAUTHORIZED"` — verified whole. **CONFIRMED.**

### 6.2 Candidate 1 — UNBUILT — **CONFIRMED, with COR-I**

The deciding sentence is at bytes, whole across a line wrap,
`STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md:527-528`:

> "**Neither path is available today.** Both are blocked on an object that does not exist in
> sealed form, not on an unperformed computation."

Both SPEC GAPS verified whole at `:512-518` (Path A, `R_record,L`'s form) and `:520-525` (Path B,
the evaluated-pair set `D`). The three-outcome fork verified at `:531-535`. **Grade UNBUILT —
CONFIRMED.** Not excluded by proof — **CONFIRMED**.

**COR-I.** The build's flat *"Not superseded, not barred by a rule"* (`:904`) is unqualified. At
bytes `STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md:534` displays a bar in the same fork, on the
DOES NOT FACTOR outcome: `"the **VOID CONDITION** bars choosing it from a desired coefficient,
and the only sealed clause that would single out \`x_F\` — C_ref's *"preserving the same smooth
coframe and connection"* — is **barred as a source**, with DoR-007 foreclosing the lift."` That
bar attaches to a *selector* on one of three outcomes, not to the comparand today — so the grade
holds — but the sentence should read **"not barred by a rule today; a bar is displayed on one of
its three outcomes."**

### 6.3 Candidate 2 — BARRED BY A RULE — **CONFIRMED, and INCOMPLETE (COR-B)**

`STAGE8_R_RECORD_L_FORM_FABLE_V001.md:125-128` verified whole:

> "**B9 — the phase object and its bar (V011).** `Gamma_Q[A] = -i Log(Z_Q[A]/Z_Q[0])` with
> `c = 1` fixed by the complete charged amplitude; "Until `Z_Q` is constructed from complete
> `Q_spec`, the conversion from `kappa_record` to a physical action coefficient remains
> unauthorized.""

**The build's reading is exactly right and it stated the required thing explicitly**: the bar is
a rule, not mathematics, and a bar naming the act that lifts it cannot be a proof of
impossibility. **This is the direction the commission flagged as the severe-correction case —
"a candidate graded excluded-by-proof that is actually barred-by-rule" — and it is NOT present.**

**But the mirror error is present.** Per COR-B (§3.3), `STAGE8_ZQ_STRUCTURE_FORCING_FABLE_
V001.md:420-425` states of this same candidate that *"even WHEN built from the record's ratified
charged content, the `Z_Q` comparand CANNOT be n-blind — the unavailability is structural, not
merely a build-state fact."* That artifact was returned by the build's **own** S-05 sweep and
never opened.

```text
CANDIDATE 2 — CORRECTED GRADE

  BARRED BY A RULE, NOT BY MATHEMATICS      SUSTAINED  ([RL] :125-128)
  EXCLUDED BY PROOF?                        the build answered NO flatly.
    CORRECTED: NO as to the candidate's EXISTENCE or AUTHORIZATION; but a
    STRUCTURAL EXCLUSION OF THE PROPERTY THAT WOULD MAKE IT THE COMPARAND
    — its n-blindness — IS SHOWN OF RECORD, at the quantifier
    "Z_Q comparands built from the record's ratified charged content."
  SUPERSEDED?                               NO — SUSTAINED.

  The build's Q5 closing line "One is missing; one is forbidden-for-now"
  therefore UNDERSTATES candidate 2. At bytes it is forbidden-for-now AND
  structurally excluded from n-blindness on the surveyed construction.
  This audit adopts that exclusion for nothing, builds on it for nothing,
  and carries the source's own limit: "IT DOES NOT say Z_Q is built."
```

**D5 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected: both identifications **CONFIRMED**; candidate 1 **UNBUILT — SUSTAINED**, with the
flat "not barred by a rule" qualified; candidate 2 **BARRED BY A RULE — SUSTAINED**, with the
build's absolute non-exclusion **REFUTED** and the structural exclusion of its n-blindness added
at its own quantifier.

---

## §7 — D6: BARS AND FENCES — **CONFIRMED-WITH-CORRECTIONS**

### 7.1 Fences — **INTACT**

All three fence flags carried live at build `:8-9` and `:1115-1116`. Read end to end: **no
magnitude attempted, reached, or evaluated; no numeric value of any coupling, scale, root,
eigenvalue, norm, or constant computed; no CAS run; no expression simplified.** The only
arithmetic act is `"two integer steps"` between the quoted dimensions 1 and 3 — none of the
barred quantity classes. **CONFIRMED.**

### 7.2 Authoring, advocacy, adoption, re-read negatives — **NONE FOUND**

- **No object authored.** §8's claim is exact: no profile, clause, comparand, carrier, bound, or
  model is constructed anywhere in the artifact. Verified by reading.
- **No adoption.** Status line `CLAIMED / PROPOSED-NOT-ADOPTED` at `:14`.
- **No advocacy.** The build grades its own strongest-looking separator (A-3) as the softest, and
  carries six adverse spans against itself — all six located and verified at their sites.
- **No catalogued negative re-read.** TYPE-R appears three times, all as fence declarations.
  [AJ]'s toy profiles are referenced only by the fact of their existence; `Δ` is expressly
  excluded from the Q4 enumeration. [AJ] §12's TOY declaration verified at
  `STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:630-635`. **TOY_SEPARATION = clean — CONFIRMED.**

### 7.3 Sweep block reconciled against the headline

**19 of the build's 24 corpus patterns reproduce EXACTLY** on independent re-run (fixed-string,
case-insensitive, exclusions as a bash ARRAY). Five differ:

| # | Pattern | Build R1/R2 | Re-run R1/R2 | Disposition |
|---|---|---|---|---|
| S-11 | `unique_primitive_carrier_derived` | 5 / 5 | 6 / 5 | **+1 = `STAGE8_THE_JOIN_O42SR_V001.md`, mtime 02:05 — one minute AFTER the target's 02:04. Post-cutoff sibling lane. NOT a build error.** |
| S-18 | `holonom` | 566 / 565 | 567 / 565 | same post-cutoff sibling |
| S-20 | `winding` | 158 / 98 | 157 / 97 | −1 / −1, unreconciled; no grade rests on it |
| S-21 | `transition function` | 25 / 14 | 25 / 15 | R1 exact; R2 +1, unreconciled |
| S-22 | `character` | 869 / 854 | 868 / 801 | R2 differs by 53. **INDETERMINATE-AT-BYTES** as to cause. The build explicitly claims no exhaustiveness for these counts (`"NO SWEEP-BACKED EXHAUSTIVENESS IS CLAIMED FOR §5.2"`); no grade rests on it |

**LEAK COUNTER: 0 on every one of the 24 re-run patterns, both roots.** Self-exclusion held.
Barred-basename self-scan of the build reconciles at **9 line-level hits** (7 lines carrying
uppercase barred tokens at `:1037-1039`, `:1050`, `:1131-1133`, all inside the displayed
exclusion array / leak pattern / bar declaration; plus 2 `"Register :224"` — one inside the [S13]
quotation of §I.4 at `:500`, one in the flag's own description at `:1140`). **All benign. No
barred file is named as a source and none was opened. BAR INCIDENTS: ZERO — CONFIRMED.**

### 7.4 The unreconciled headline disagreement — reported as the commission requires

**§0's `"No DOR_* ruling touches it (0 hits across 14 sealed rulings)"` does not reconcile with
§10's own DOR block.** §10 reports `rank` **8**, disposes of all eight by an inspection claim,
and that claim is false at bytes for `DOR_020_A1:14` and inaccurate for `DOR_019:14` (COR-A).
The headline is therefore asserted over an inspection that does not hold. **This is the one
unreconciled disagreement between the build's headline and its own sweep block.**

### 7.5 COR-F — the seal declaration is false at bytes

Build `:76`, `:90-92`, `:1126-1129` declare `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_
V002.md` **NO SIDECAR** — *"It carries no `.sha256` and no `.seal.sha256` in either corpus root."*

**At bytes the sidecar exists in BOTH roots**, under the archive's older naming convention
(`.seal.sha256` **replacing** the `.md`, not appended to it):

```text
/Users/bgm/MB Work/alpha-program-archive/workspace/
  PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.seal.sha256
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003/
  PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.seal.sha256
```

`shasum -a 256 -c` run from each own directory: **OK**. The same convention is visible on
neighbouring files in the same directory (e.g. `ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003.
seal.sha256`), so the build's probe tested only one of the two conventions the archive uses.

**CORRECTED: SEAL TALLY = 34 / 34 OK, not 33 / 34. There is no unsealed source.** The build's
substituted corroboration (cross-root byte-identity + verbatim carriage in [EIN]) was unnecessary
but not wrong. Direction of error is conservative — the build **under**-claimed — but it is a
false statement of fact in a FLAG BLOCK, which is where a lane's seal claims are relied upon.

**D6 GRADE: CONFIRMED-WITH-CORRECTIONS.**
Corrected: fences **INTACT — CONFIRMED**; bars **HELD, zero incidents — CONFIRMED**; leak counter
**0 — CONFIRMED**; self-exclusion **HELD — CONFIRMED**; **seal tally REFUTED (34/34, not 33/34)**;
**one unreconciled headline/sweep disagreement, at the DOR rank row**.

---

## §8 — CORRECTIONS, IN SEVERITY ORDER

**COR-A — D3 — THE PROVENANCE EXCLUSION IS REFUTED.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/supervision/DOR_020_A1_WHERE_CLAUSES_AMENDMENT_2026-08-04.md:12-15`
(with `:1`, `:3`, `:28-30`), against build `:580` and `:575-577`.
A sealed **ADOPTED** ruling of record types THE LOCAL FIELD MEMBERS as *"a U(1) BUNDLE WITH
CONNECTION over the record surface … smooth full-rank"*. The build's *"ADOPTED is excluded at
bytes"* is unsupported, and its DOR `rank`-row disposal (*"ALL are cycle-rank / rank-two MEMBER
classes … NOT bundle-fiber rank"*) is false for `DOR_020_A1:14` and inaccurate for
`DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md:14`.
**CORRECTED GRADE: provenance = ENTERED on the carriers read — SUSTAINED; NOT-ADOPTED —
REFUTED, replaced by ADOPTED NOT EXCLUDED; whether DOR_020_A1's bundle is §I.4's fixed line
bundle — INDETERMINATE-AT-BYTES. The commission's answer "a choice, not a wall" is unaffected in
direction: an adopted dimension is also a choice.**

**COR-B — D2 / D5 — TWO EXCLUSION-SHAPED STATEMENTS, SURFACED BY THE BUILD'S OWN SWEEP AND NOT
READ.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md:420-425`
and `:405-416`; corroborating limit at
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md:31-32`
and `:374-378`; against build `:392` and `:962-963`.
Both artifacts are in the build's own S-05 (`n-blind comparand`, 9 files) result set. The first
states *"the `Z_Q` comparand CANNOT be n-blind — the unavailability is structural, not merely a
build-state fact."*
**CORRECTED GRADE: A-2 fillability NOT-HELD-NOT-EXCLUDED at the object level — SUSTAINED (and
corroborated). The build's absolutes "No impossibility of the comparand is shown anywhere" and
"NEITHER IS EXCLUDED BY ANY PROOF ANYWHERE IN EITHER CORPUS ROOT" — REFUTED. Candidate 2 is
barred by a rule AND structurally excluded from n-blindness at the quantifier "built from the
record's ratified charged content".**

**COR-C — D2 — "THE SENTENCE AFTER THE IMPOSSIBILITY" IS NOT THE SENTENCE AFTER IT.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:552-554`,
against build `:364-370`.
The interposed sentence — *"An exit operation alone leaves the two-model freedom intact; a
forcing clause alone is barred at C12's strength unless the categorical wording is
simultaneously re-scoped by a booked result."* — is the one adverse clause at A-1's supply site
and appears nowhere in the build (0 grep hits).
**CORRECTED GRADE: A-1 fillability NOT-HELD-NOT-EXCLUDED — SUSTAINED (the bar names its own
lifting condition). The build's "immediately following" — REFUTED. A-1's supply site carries an
undisplayed conditional bar.**

**COR-D — D4 — THE Q4 ADJACENCY ENUMERATION IS NOT CLOSED.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md:76-78`
and `:81-84`, against build `:842-850`.
Inside the build's own sealed source #14, on the very carrier its Q3 analyses, a phase and
magnitude-valued objects are co-displayed (`p = 1/2, theta = pi mod 2 pi`; `Delta S_record = pi
hbar`; Fubini–Study fixing *distances*).
**CORRECTED GRADE: no adjacency inflated into a link — CONFIRMED, clean. Adjacency count
corrected from "FOUR on-ground + ONE off-ground" to AT LEAST FIVE SITES, NOT CLOSED.**

**COR-E — D4 — THE ON-GROUND PHASE BREAK-OUT DROPS TWO OF SIX ITEMS FROM ITS OWN QUOTED
INVENTORY.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/BARE_SURFACE_I2_DETERMINATION_V001.md:36`,
against build `:679-690`; plus `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:54-69`, `:76`, `:86-88`.
The affine connection space (§I.4) and the refinement/coarsening law (§I.5) are booked in the
phase/holonomy sector and never become rows; so are the connection `a_i`, the curvature `f = da`,
and the comparison character `W_n(gamma)`.
**CORRECTED GRADE: the inventory QUOTATION — CONFIRMED whole; the BREAK-OUT — REFUTED as
exhaustive. On-ground phase-object count corrected from 10 to at least 12–14.**

**COR-F — D6 — THE SEAL DECLARATION IS FALSE AT BYTES.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.seal.sha256`
(and its cleanroom twin), against build `:76`, `:90-92`, `:1126-1129`.
The sidecar exists in both roots under the archive's `.seal.sha256`-replacing-`.md` convention
and verifies **OK** from each own directory.
**CORRECTED GRADE: SEAL TALLY = 34 / 34 OK, not 33 / 34. There is no unsealed source.**

**COR-G — D3 — A THIRD IMPORT SITE IS MISSING FROM THE IMPORT AUDIT.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_THRESHOLD_FORM_O19SR_AUDIT_V001.md:421-422`,
against build `:1016`.
The entire Q3 crossing answer rests on [THF-A]'s gloss *"(≤ 2 fails / 3 and above forces)"*;
`[AJ] :283` states only the failure side. IMPORT AUDIT row I-1 names only [AJ] and [AJ-A].
**CORRECTED GRADE: crossing dimension = 3 — SUSTAINED; its provenance corrected to
IMPORTED-BY-[THF-A], which must be added to the IMPORT AUDIT.**

**COR-H — D3 — A UNIVERSAL NEGATIVE CITED TO SWEEPS THAT DO NOT CONTAIN THE PATTERN.**
Deciding file:line — build `:648-650` (`"Sweeps in §10."`) against build `:1053-1082`, which
contains no `fiber dimension`, `forced`, or `unique carrier` corpus pattern.
Run here: `fiber dimension` = 5 workspace files; the two the build never touched
(`STAGE8_RK_LT_BIT_T1SR_AUDIT_V001.md:416`, `STAGE8_RK_LT_BIT_T1SR_V001.md:679`) are
toy-quarantined reader fibers.
**CORRECTED GRADE: claim SUSTAINED in substance, on this audit's sweep; its cited warrant is
VOID.**

**COR-I — D5 — CANDIDATE 1's "NOT BARRED BY A RULE" IS UNQUALIFIED.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md:534`,
against build `:904`.
**CORRECTED GRADE: candidate 1 UNBUILT — SUSTAINED; the sentence should read "not barred by a
rule today; a bar is displayed on one of its three outcomes."**

**COR-J — D1 — THE CARRY-FORWARD LIST FOR A-3 OMITS [AJ]'s OWN TWO CARRIES.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:36-37`
and `:682`, against build `:321-323`.
**CORRECTED GRADE: A-3's grade — SUSTAINED; the carry-forward list corrected to include [AJ]'s
§0 verdict and its sealed `JUNCTION_STATUS` flag.**

**COR-K — D3 — "CARRIED VERBATIM INSIDE SEALED [EIN]" IS TRUE FOR TWO OF FOUR FLAGS.**
Deciding file:line — `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md:76`
vs `PRIMITIVE_RECORD_ACTION_CHARACTER_BRIDGE_GATE_V002.md:208-211`, against build `:554-561`.
`bloch_ball_derived = false` and `M2C_algebra_derived = false` are [BRG]-only, and §4.4(i) uses
both — in tension with `"Nothing load-bearing rests on it alone."`
**CORRECTED GRADE: immaterial once COR-F establishes [BRG] is sealed OK; the sentence should say
"two of four".**

**COR-L — D1 — "ELISIONS BY THIS LANE: NONE" HOLDS ONLY FOR INTERIOR ELLIPSES.**
Deciding file:line — build `:1158`, against four end-truncations of spans presented as "quoted
whole": `[AJ] :34` (verdict truncated before *"— with the norm/metric sub-route additionally
banned…"*), `[AJ] :302-303` (§6.4 truncated mid-sentence at *"not informational"*),
`[THF-A] :424` (MISSED-1 truncated before *"Its carrier is named at [AJ] :682 and :570."*),
`[I2] :69` (§3.1 truncated before its `[DERIVED — enumeration…]` tag).
**None of the dropped text is adverse to the grade it supports.**
**CORRECTED GRADE: QUOTATION_INTEGRITY sustained for interior fidelity; the "NONE" should read
"no interior elisions; four end-truncations."**

---

## §9 — REQUIRED BLOCKS

### CHOICE LEDGER (every unforced choice this audit made)

| # | Choice | Class | Disposition |
|---|---|---|---|
| 1 | Re-sweep the DOR rulings with patterns broader than the build's eight literals | FORCED | the commission directs "check the DOR_* rulings yourself, since an ADOPTED dimension would sit there"; the literal eight cannot decide an exclusion |
| 2 | Grade D3 CONFIRMED-WITH-CORRECTIONS rather than REFUTED, though its most consequential supporting step is refuted | AUTHORED-READING (declared) | the graded answer (ENTERED; "a choice, not a wall") survives, because an ADOPTED dimension is also a choice. A reader who grades the dimension by its warrants rather than its answer would write REFUTED; that reading is displayed here rather than suppressed |
| 3 | Grade D2 CONFIRMED-WITH-CORRECTIONS though the build asserted a false universal | AUTHORED-READING (declared) | all three fillability grades are correct at bytes and the missed artifacts self-limit in their own voices. The competing reading (REFUTED on the false universal) is displayed at §3.3 |
| 4 | Report the [EIN] §1.1 co-display as a missed adjacency | FORCED | D4 directs the hunt; suppressing the clearest instance inside the build's own sealed source would be the unforced act |
| 5 | Decline to relate `theta = pi mod 2 pi`, `Delta S_record = pi hbar`, `Delta Phi_* = pi`, and `\|ΔS_record\|/ℏ = π` to one another | FORCED | relating them would author the clause A-1 names absent — barred |
| 6 | Decline to identify DOR_020_A1's U(1) bundle with §I.4's fixed line bundle | FORCED | identifying them would be authoring; the bytes do not decide it. INDETERMINATE-AT-BYTES said |
| 7 | Attribute the S-11/S-18 count differences to a post-cutoff sibling artifact on mtime evidence | PREMISE (named) | `STAGE8_THE_JOIN_O42SR_V001.md` mtime 02:05 vs target 02:04; no content of that file was opened or consumed |
| 8 | Verify all 24 corpus sweep patterns but read hits only for the load-bearing ones | PREMISE (named) | the nine large descriptive patterns (S-16..S-24) carry no grade in the build and none here |
| 9 | Open two artifacts outside the build's 20 consumed sources (`ZQ_STRUCTURE_FORCING`, `NEUTRAL_COMPARAND_FAITHFULNESS`) | FORCED | both were returned by the build's own S-05; D2 directs an independent exclusion hunt |
| 10 | Report the ZQ artifact's malformed sidecar rather than treating it as unsealed or as sealed | FORCED | the digest matches byte-for-byte and is corroborated inside two OK-sealed artifacts; the sidecar is unparseable. Graded SEALED-IN-SUBSTANCE, SIDECAR MALFORMED |

**OPEN choices: none. AUTHORED-READINGS: 2, both displayed with the competing reading carried.**

### TOY_SEPARATION

**TOY_SEPARATION = clean.** This audit constructs **no** object — no clause, comparand, carrier,
bound, model, profile, or dimension. Nothing here could be a toy because nothing here is built.
Toys encountered and handled:

- **[AJ]'s two model profiles `f₁`, `f₂`** (`STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md:250-251`,
  declared TOY at `:630-635`) — referenced only by the fact of their existence, never by form,
  never evaluated. Their argument symbol `Δ` enters no enumeration here.
- **The RK_LT_BIT toy reader fibers** (`STAGE8_RK_LT_BIT_T1SR_AUDIT_V001.md:409-418`;
  `STAGE8_RK_LT_BIT_T1SR_V001.md:671-685`) — quoted **only** to show that the two `fiber
  dimension` hits the build missed are quarantined, and quoting their own quarantine lines. No
  toy dimension enters any grade.
- **No toy is introduced by this lane.** No worked example, no "suppose the dimension were 3",
  no sketched comparand. No toy quantity appears in any verdict line, grade, or flag.

### IMPORT AUDIT (every non-corpus notion, declared)

| # | Notion | Where it enters | Status |
|---|---|---|---|
| A-1 | **Gleason / frame-function mathematics** | inside the *quoted* `[AJ] :283-284`, the *quoted* `[AJ-A] :204-206`, **and the *quoted* `[THF-A] :421-422`** | **IMPORTED BY THE SOURCES, NOT BY THIS LANE.** [AJ-A]'s Choice Ledger row 6 (`..._AUDIT_V001.md:257`) declares it IMMATERIAL(proven). This audit asserts **no** independent Gleason fact, and adds [THF-A] as the third site (COR-G) precisely because the build omitted it |
| A-2 | **"line bundle ⇒ fibers of dimension 1"** | [AJ]'s unpacking, quoted | IMPORTED BY [AJ]. Reported, neither endorsed nor disputed |
| A-3 | **"U(1) structure group ⇒ rank-1 associated complex line bundle"** | **NOT USED.** Named here only to say it is NOT the basis of COR-A | COR-A rests on the ruling's *displayed words* (`U(1) BUNDLE WITH CONNECTION`, `smooth full-rank`) and on the build's inspection claim being false at bytes — **not** on any bundle-theoretic inference. This audit draws none |
| A-4 | **Reading `p = 1/2` as probability-valued and `theta = pi mod 2 pi` as a phase** | §5.2 | **MINIMAL READING OF A DISPLAYED FORM**, on the same footing as the build's own I-3. Nothing evaluated, no value computed, no identity applied |
| A-5 | **Filesystem sidecar naming conventions** | §7.5 / COR-F | a fact about files, not a record notion |
| A-6 | Ordinary English audit vocabulary ("quantifier", "provenance", "warrant") | throughout | **COMMISSION VOCABULARY.** Every grade is anchored to quoted corpus words |

**FORBIDDEN IMPORTS: NONE USED.** No `Λ^even(C⁵)`; no `K_KK`; no fiber proper radius; no metric
or fiber metric; no `ℓ_P`; no length or scale object; no β conversion; no S16/Thomson matching;
no `κ_Thomson`; no "α rides a scale"; no KK action; no Finish-A/Finish-B framing; no
E1/Carleman/S2′ objects; no GR, sphere, or 4π reading; no continuum-diamond import; no empirical
constant; no measured value. **The lens/ether reading motivates nothing here and appears in no
chain.** TYPE-R is not re-walked and its machinery is not touched. `|n| = 1` is reported as a
thing the sources bar/type; it is **not consumed** here.

### SWEEP CUTOFFS (pattern · hit count · leak counter)

**METHOD.** Exclusions passed as a bash **ARRAY** on every invocation, never as a string:

```bash
EXC=( --exclude='*REGISTER*' --exclude='*TRACKER*' --exclude='THE_PLAN*' \
      --exclude='ROAD_REMAINING*' --exclude='THE_HANDOFF*' \
      --exclude='OBSERVATIONS_REGISTER*' --exclude='*DECISION_SHEET*' \
      --exclude='STAGE8_THREE_ABSENCES_O41SR_V001.md' \
      --exclude='STAGE8_THREE_ABSENCES_O41SR_AUDIT_V001.md' )
```

**SELF-EXCLUSION: HELD.** This audit's own basename is in the array of every sweep, alongside the
target's.

| Group | Patterns | Result | LEAK |
|---|---|---|---|
| **A** — re-run of the build's 24 corpus patterns, both roots | 24 | 19 exact matches; 5 differences tabulated at §7.3 | **0** |
| **B** — the build's 8 DOR literal patterns, 14 rulings | 8 | reproduced exactly: `line bundle` 0 · `fiber dimension` 0 · `dimension 1` 0 · `forcing threshold` 0 · `ray` 0 · `C^2` 0 · `two-dimensional` 0 · `rank` **8** (all 8 opened and read — 6 cycle-rank/member, 1 bundle full-rank, 1 isometry rank: COR-A) | **0** |
| **C** — broader DOR patterns the build never ran, 14 rulings | 13 | `bundle` **1** · `carrier` **3** · `dimension` **1** · `projective` 0 · `qubit` 0 · `C2` **1** · `spinor` 0 · `two-level` 0 · `fibre` 0 · `fiber` **5** · `line` **7** · `U(1)` **1** · `phase` 0. Every hit opened; only `DOR_020_A1` bears | **0** |
| **D** — DOR adoption-term patterns, 14 rulings | 8 | `primitive` 0 · `microscopic` 0 · `record carrier` 0 · `Boundary Record Action` 0 · `stabilizer` 0 · `two-character` 0 · `adopted` **9** · `ADOPTED` **9** | **0** |
| **E** — independent exclusion hunt, both roots | 24 | `no n-blind comparand can` 0 · `no such comparand` 0 · `no external comparand can exist` 0 · `clause is impossible` 0 · `forcing equation is impossible` 0 · `provably no clause` 0 · `no change of ground` 0 · `WINDING_FORCED = REFUTED` 0/0 · `REFUTED for \|n\|` 0/0 · `n-blind…no-go` 0 · `carrier is unique` 0 · `must be one-dimensional` 0 · `rank one is forced` 0 · `unique carrier` 2 · `carrier is forced` 1 · `dimension is forced` 1 · `fiber dimension` **5** · `comparand cannot` **1 → COR-B** · `cannot be supplied` 6/5 · `no comparand` 0 · plus `n-blind comparand` (9/1, full list read), bare `n-blind` (62), `fixed line bundle` (9/6, full list read), `both sealed candidates unavailable` (5/0, full list read) | **0**, after one repair — see below |
| **F** — self-scan of the build for barred basenames | 6 | 9 line-level hits, all benign (§7.3) | **0** |

**TOTAL: 83 patterns run. TOTAL LEAK ACROSS ALL PATTERNS AND BOTH ROOTS: 0.**

**ONE LEAK-COUNTER INCIDENT, DISCLOSED.** The pattern `REFUTED for |n|` was first issued to
`grep -E`, where `|` is alternation; it matched effectively every file and its leak counter
returned **2**. The result was never printed and **no file was opened**; the count alone was
observed. Re-issued as a fixed string: **0 / 0, LEAK 0.** This is a defect in this audit's
checker, not in the corpus, and it is the mirror of the build's own `≤`/`<=` checker slip.

**SWEEP_CUTOFF, declared:** the two named corpus roots at their files as of this session
(2026-08-16), plus the 14 `DOR_*` rulings. No file matching a barred class was opened. Sweeps
bound **this audit's search**; they bound no quantifier reported above — every quantifier is
quoted from its own source.

### FLAG BLOCK

```text
FENCES = INTACT — alpha_computed = false · proof_authorized = false ·
  kappa_record_computed = false, live throughout. NO numeric value of any
  coupling, scale, root, eigenvalue, norm, or constant computed. No magnitude
  attempted, reached, or evaluated. No CAS run; nothing simplified. The
  numerals here are quoted dimensions (1, 2, 3), quoted corpus symbols,
  digests, line numbers, and hit counts.

SEAL_TALLY = 35 / 35 OK, each `shasum -a 256 -c` run from the artifact's own
  directory: 20 workspace consumed sources (20/20 OK — see COR-F, the build's
  [BRG] IS sealed), 14 DOR_* rulings (14/14 OK), the target itself (OK).
  ONE ADDITIONAL ARTIFACT opened for the D2 exclusion hunt:
  STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001.md — SEALED-IN-SUBSTANCE, SIDECAR
  MALFORMED (digest 68cce523… matches byte-for-byte; sidecar carries the
  digest without the filename field so `shasum -c` cannot parse it;
  corroborated inside two OK-sealed artifacts). Declared, not buried.
  A second, STAGE8_NEUTRAL_COMPARAND_FAITHFULNESS_FABLE_V001.md — seal OK.

BAR_INCIDENTS = ZERO. No file whose name contains REGISTER or TRACKER was
  opened; no THE_PLAN*, ROAD_REMAINING*, THE_HANDOFF*, OBSERVATIONS_REGISTER*,
  or *DECISION_SHEET* was opened. In the supervision directory ONLY the 14
  DOR_* files and their sidecars were read; SURFACE_DEFINITION_OF_RECORD_V001/
  2/3 were NOT opened — declared as the reach limit at S1, and the one place
  it could change a grade is the build's own D-2, which stands.
  LEAK COUNTER: 0 across all 83 patterns, after the one disclosed checker
  repair (see SWEEP CUTOFFS).

SELF_EXCLUSION = HELD. Own basename and the target's in the exclusion ARRAY of
  every sweep.

QUOTATION_INTEGRITY = every span quoted above was verified by READING THE
  SURROUNDING SOURCE LINES, not by line-oriented grep. Twelve load-bearing
  spans cross 2-5 source lines and are carried whole. ELISIONS BY THIS LANE:
  NONE, interior or terminal — where a source sentence continues, this audit
  either carries it or says it truncates.

BARS_HELD = nothing authored (no object, clause, comparand, carrier, bound,
  model, or dimension); nothing adopted; nothing advocated; no catalogued
  negative re-read; no gap filled or repaired; no defect reported here is
  fixed here. INDETERMINATE-AT-BYTES said THREE times: (1) whether
  DOR_020_A1's U(1) bundle is SI.4's fixed line bundle (S4.4); (2) the cause
  of the `character` R2 sweep divergence (S7.3); (3) carried from the build
  and re-checked: whether the DeltaPhi carrier and the surface-of-record
  carrier are one object — the bytes still do not decide it.

OWN_DRAFT_DEFECTS, disclosed:
  OD-1  A line-oriented grep for [GLUE]'s quotation returned ZERO and this
        audit nearly filed a false fabrication finding; the phrase wraps as
        "complex line / bundle" across :563-564. Caught by reading the
        surrounding lines. Exactly the failure the standing discipline names.
  OD-2  The `REFUTED for |n|` regex leak incident (SWEEP CUTOFFS). Repaired;
        no file opened.
  OD-3  COR-A refutes an EXCLUSION, not the ENTERED grade. This audit does
        not regrade the provenance as ADOPTED and expressly declines to
        identify the two bundles. A reader wanting a one-word Q3 answer
        should read: ENTERED, with ADOPTED no longer excluded.
  OD-4  COR-D's status as an "adjacency" depends on [I2] :37 booking
        balanced-geodesic identities in the write/response sector. If the
        commission means only SII.7 kinematics by "response magnitude",
        COR-D narrows to the theta/Delta-S co-display in one sentence. Both
        stand as reported byte-facts; neither is related to anything.
  OD-5  Grades D2, D3 and D5 are CONFIRMED-WITH-CORRECTIONS on the reading
        that a dimension's grade follows its ANSWERS. On the competing
        reading — that it follows its WARRANTS — D3 would be REFUTED on
        COR-A and D2 on COR-B. That reading is declared at CHOICE LEDGER
        rows 2 and 3 rather than suppressed.

MAGNITUDE_APPROACHED = NO. This audit maps what the build mapped and checks
  it; it crosses nothing. It supplies no comparand, no clause, no carrier of
  higher dimension, and no route to any. Where the corpus's own bytes sit
  closest to a crossing (S5.2, S3.3) the finding is reported and expressly
  not related to anything.

NET = The build's three absences are correctly located, correctly graded off
  DECIDING WORDS rather than headlines, and correctly typed against the
  corpus's own two vocabularies. Its Q5 identifications and reasons are
  right at bytes, and the severe error the commission named — a
  barred-by-rule candidate graded excluded-by-proof — is not present. Its
  discipline held: fences intact, bars clean, no object authored, no
  adjacency inflated into a link, leak counter genuinely 0. What fails is
  its SEARCH, twice in the same shape: at the DOR rulings it ran eight
  literals, got zeroes, and declared ADOPTED excluded (COR-A); at S-05 it
  ran one phrase, got nine files, read none of them, and declared no
  impossibility exists anywhere (COR-B). Both declarations are false at
  bytes; neither answer they support is. The Q3 answer stands — the fiber
  dimension of 1 is ENTERED, a choice and not a wall, and an adopted
  dimension would be a choice too. The Q2 answers stand — all three
  absences remain NOT-HELD-NOT-EXCLUDED, now with two conditional bars and
  one structural exclusion displayed that the build did not display.
  NOTHING HERE REACHES, OR ATTEMPTS TO REACH, THE MAGNITUDE.
```

---

## §10 — CLOSE

Artifact: `/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_THREE_ABSENCES_O41SR_AUDIT_V001.md`
Sealed from its own directory to `.sha256` and `.seal.sha256`; both verified. No git action.
