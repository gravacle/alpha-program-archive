# STAGE 8 — THE JOINT-COMPLETION INVERSE ATTACK: S3'S CONSTRAINT SYSTEM
## DARIO LANE (Builder B) — RELAY 892 — [PLAN:AXN-BUILD-B20]

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

All charter fences live. No smooth import; no numeric evaluation of any physical quantity; no member
binding; no fixed-point execution; no end test; no comparison to measured constants; no EM
identification; no common cell formed; no junction map evaluated. PE-1..PE-13 pointer-only, none
opened. Builder-A code not opened. `~/.codex` untouched; memory-bank never searched. No register,
plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.**

Subjects seal-verified BEFORE reading: four-field census `d3668b908777ea76` (mine) + its check
`c9e5b5d5…`; both completions at their source (`8e9a09c104f4b635` §5.1); the C4 attack
`93467eb40363bb13` (mine) + its check `145eae2f…` = `STAGE8_AXN_T10_C4_CROSSCHECK_CODEX2_V001.md`.

**DISCLOSED.** The typed-control discipline this relay imposes is **my own 890 correction, now
standing** — and it binds me. **S3 is also my own 864 typing (the "richest" field).** The method has
returned `CONSTRAINT-GAP` three times; **a fourth by pattern would be comfortable and uninformative**,
so I looked for a refutation rather than assuming one is unavailable.

---

## 0. LEAD

**`CONSTRAINTS = derived (7, at bytes)`. `VERDICT = CONSTRAINT-GAP`. `CONTROL = EXPLAINS, source-index
independent.`**

**The finding is a near-miss refutation that does not transfer, and it is the sharpest thing in root
D's four fields.** The record seals:

```text
P2_SOURCE_LIMIT_IDENTIFIED_WITH_RECORD_GNS_LIMIT = false | TYPE-R |
  test: source Banach space and record GNS have distinct carriers and maps
```

**A `TYPE-R` — a refutation, not a `TYPE-U` gap.** S3 is therefore the **one** field of root D where a
sealed refutation sits adjacent to the missing object.

**And it does not reach the missing object.** What is refuted is **identification** — that the two
completions are the *same*. **A joint completion does not identify them; it is a third carrier
receiving both via intertwiners.** Identification and joint completion are different relations, and
the sealed test (*"distinct carriers and maps"*) bars the first while saying nothing against the
second.

**`COMPLETION-REFUTED` is therefore NOT available — and a reader could easily take the `TYPE-R` for
exactly that.** That mis-reading is the specific flattening risk S3 carries, and S12 is what forbids
it: the flag's own `test:` clause names what it tested, and it tested identification.

**The control explains, and its independence is auditable.** *"No new finite glue is needed"* follows
because **both completions complete THE SAME finite data by DIFFERENT procedures** — the source maps
in P2's Banach norm on `E_src`, the record-state embeddings by GNS isometries — and **enlarging the
finite data feeds both procedures equally**, reproducing the two-carrier structure at every `N`.
**The gap is a procedure/carrier difference, not a data shortfall, and no quantity of finite glue
alters a difference in what the two procedures retain.**

---

## 1. THE SEALED RECEIVERS

**Both objects exist, which is what distinguishes S3 from S1, S2 and S4.**

```text
BdGlue_N^law := ( open-chain endpoints and endpoint gauge actions ; ready preparation rho_ready,N ;
                  E_post transition U_N ; completed finite density rho_N ; quotient q_N and
                  equal-history identity ; zero extensions j_NM^Q and iota_NM ; record restriction
                  Rec_N ; outgoing state omega_N and GNS embedding J_NM )

TWO EXACT COMPLETION STATEMENTS:
  1. the source maps complete in P2's BANACH NORM on E_src, finite source core dense, moving tails
     excluded;
  2. the record-state embeddings complete to (R_infinity, omega_out, GNS_out) by the GNS ISOMETRIES.

BdGlue_law_limit := ( BdGlue_law_src , BdGlue_law_rec )     — the record's own words: their
                                                              NON-IDENTIFIED pair

LAW_SIDE_BOUNDARY_GLUE_LIMIT_PAIR_BUILT = true | TYPE-P | premises: DoR-008, 009, 013, 014
```

**The demand**, from the S3 stop's would-build: *"a common-origin completed joint carrier plus
preparation/gluing variation, boundary orbit, edge reduction/functionals, microcausal support, global
boundary-form certificate, and intertwiners to both exact limits."*

---

## 2. THE CONSTRAINT SYSTEM — seven necessary conditions

**Law 9b, closure declared BEFORE any absence line.** `C_JNT :=` {the P3 second attack §5.1–§5.2; the
S3 stop requirement span `8368dcbbc5d4af2f`; my 864 census and its Codex check at their S3 sections;
my C4 attack and its check; the boundary TARGET specification's Q-750/752 lineage}. **"Absent" below
means absent from `C_JNT`.**

### J1 — a common-origin joint carrier

The carrier must be **derived from the common origin**, not assembled from the two limits. **Sealed
demand** (*"a **common-origin** completed joint carrier"*). **Status: ABSENT.**

### J2 — intertwiners to BOTH exact limits, and NOT an identification

The joint carrier must receive **both** `BdGlue_law_src` and `BdGlue_law_rec` via intertwiners.
**This is the system's strongest derived condition, and the sealed `TYPE-R` shapes it rather than
blocking it:**

```text
the intertwiner pair may NOT be an identification —
  "source Banach space and record GNS have DISTINCT CARRIERS AND MAPS"
so any joint carrier must receive two structurally DIFFERENT completions
  (a norm completion of the SOURCE ; a GNS limit that is RECORD-ONLY, Rec_N having removed the source)
WITHOUT collapsing their difference.
```

**Derived, not assumed: the two completions differ in what they RETAIN** — one keeps the source, the
other has removed it. **An intertwiner pair must therefore be non-injective on at least one side or
land in a carrier strictly larger than either.** **I state that as a constraint on the intertwiners,
not as an obstruction**, because no sealed sentence says such a carrier cannot exist.

### J3 — preparation/gluing variation

The completion must carry the **variation** of preparation and gluing, not merely their finite values.
**Sealed demand. Status: ABSENT.**

### J4 — physical boundary gauge orbit, edge reductions and boundary functionals

**Sealed demand. Status: ABSENT.** The finite package supplies *endpoint gauge actions*; the physical
**orbit** and the edge **reductions/functionals** are not those.

### J5 — microcausal support

**Sealed demand. Status: ABSENT** of record in `C_JNT`.

### J6 — the global boundary-form certificate, carried AS THE TARGET STATES IT

The vanishing condition is *"make the global Dirac boundary form vanish under the CTP
preparation/gluing variations."* **Carried with its disclosed defects and NOT repaired:** it is an
obligation on a **different named generator**, and
`B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false | TYPE-U` — **my own C4's
`CORRESPONDENCE_GAP`, which fired at exactly this point.**

**So J6 is conditional on an object that does not exist**, and repairing the generator/scale here
would be authorship **and** would contradict my own sealed C4 finding.

### J7 — descent to the per-cell closure-face receivers

The joint completion must reach **every** `Ω_c` closure face. **This is C4's `DESCENT_GAP`** — SC5-E,
*"Law 9 found no carrier, schema, or restriction theorem"*. **Status: ABSENT.**

---

## 3. THE CONDITIONAL RESIDUAL — displayable in form, and that places S3

**Unlike S2, the quotient's defining map does not presuppose the missing object.** Given a candidate
joint carrier `C`, the assignment

```text
C  |-->  ( iota_src : BdGlue_law_src -> C ,  iota_rec : BdGlue_law_rec -> C )
```

**is definable, because both completions exist.** So the residual would be joint carriers admitting
such a pair and satisfying J3–J7, modulo those inducing the same pair.

```text
S1 : quotient DISPLAYABLE, uninhabited        (the map D_A existed)
S2 : quotient NOT DISPLAYABLE                 (the map presupposed the absent family)
S3 : quotient DISPLAYABLE IN FORM             (both objects to be related EXIST)
```

**S3 is the S1 situation, not the S2 situation** — and that is the precise sense in which my 864
"richest" typing was correct. **I display no quotient**, because J1's carrier and J3–J7's structures
are absent and writing one would require supplying them.

---

## 4. THE CONTROL — **EXPLAINS**, with its source index

**Typed per Q-797, which is my own 890 correction and which I do not exempt myself from.**

**Claim reproduced:** *"no new finite glue is needed. The missing datum is the joint physical
completion and its boundary structure."*

**Derivation from the system's own structure:** `BdGlue_N^law` is finite data. **Both** completion
statements are completions **of that same data**, by **different procedures** — a Banach-norm
completion on `E_src`, and a GNS completion of the record-state embeddings after `Rec_N` has removed
the source. **Enlarging the finite data enlarges `BdGlue_N^law`, which feeds both procedures
equally**, so at every `N` the same two-carrier structure reappears. **The gap is a difference in what
the two procedures RETAIN, and no quantity of finite glue alters that.**

**CONTROL TYPE: `EXPLAINS`.** It does not eliminate a candidate (there is none to eliminate — contrast
S1's Haar, which **eliminated**), and it is not a transcription (contrast 889's 4/8, which projected
onto the list it drew from).

**SOURCE INDEX, displayed so independence is auditable:**

```text
the explanation draws on   §5.1's two completion descriptions (Banach norm on E_src ; GNS isometries)
                           and the TYPE-R distinct-carriers test          -- both in §5.1
the sentence explained     "no new finite glue is needed"                 -- in §5.2
=> DIFFERENT SPANS.  The explanation does NOT cite the sentence it reproduces.
```

**Independent. It would survive deletion of the "no new finite glue" sentence**, which is the test my
890 correction says a control must pass.

---

## 5. VERDICT — `CONSTRAINT-GAP`

| terminal value | available? |
|---|---|
| `COMPLETION-FORCED` | **NO** — nothing forces a joint carrier; J2 constrains its intertwiners, which is far short of forcing it |
| `COMPLETION-REFUTED` | **NO — and this is the finding.** The sealed `TYPE-R` refutes **identification**, per its own `test:` clause. **A joint completion is not an identification**, so the refutation does not transfer |
| `RESIDUAL-FREEDOM` | **NO** — the quotient is displayable in form (§3) but J1 and J3–J7 are absent, so it is uninhabited |
| **`CONSTRAINT-GAP`** | **YES** |

```text
CONSTRAINT-GAP, missing inputs:
  J1  a common-origin joint carrier
  J3  preparation/gluing variation
  J4  physical boundary gauge orbit, edge reductions and boundary functionals
  J5  microcausal support
  J6  the global boundary-form certificate — CONDITIONAL on the absent B0/generator correspondence
  J7  descent to every per-cell closure face  (= C4's DESCENT_GAP / SC5-E)
  J2 is NOT a missing input: it is a DERIVED CONSTRAINT the record supplies on any future intertwiner
     pair — the two completions differ in what they retain and their difference may not be collapsed.
```

**`S3 = MATERIAL-PARTIAL` (class unchanged from my 864), gap now typed as seven conditions**, of which
**one (J2) is a derived constraint the record supplies** and six are missing inputs. **Not
discharged; no flag moves.**

---

## 6. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: both completions at their sealed types, as a NON-IDENTIFIED PAIR; the TYPE-R as a
  refutation OF IDENTIFICATION and nothing wider; the boundary form as a TARGET with WRONG GENERATOR
  AND SCALE CARRIED, NOT REPAIRED; the B0/generator correspondence as false | TYPE-U; SC5-E's descent
  as ABSENT; S3's class MATERIAL-PARTIAL.

DERIVED HERE: (a) J2 — that an intertwiner pair must receive two completions differing in what they
  RETAIN, without collapsing the difference; (b) that the sealed TYPE-R refutes IDENTIFICATION and
  does NOT transfer to joint completion, so COMPLETION-REFUTED is unavailable; (c) the residual's
  displayability, placing S3 in the S1 situation rather than the S2 one; (d) the EXPLAINS control and
  its cross-span source index.

SELECTED HERE: NOTHING.  No joint carrier, intertwiner, variation, orbit, edge functional, support
  condition, certificate, or descent is constructed, chosen, or installed.  NO QUOTIENT IS WRITTEN.
  NO FLAG MOVES.  The boundary form's generator/scale is NOT repaired.

NOT DONE AND DISCLOSED: J2's non-collapsing condition is DERIVED from the two completions' descriptions
  and the TYPE-R test; I did NOT prove that a carrier receiving both exists, NOR that one cannot —
  BOTH DIRECTIONS ARE OPEN AND I ASSERT NEITHER.  I did not re-derive the two completion statements
  from P2 and PathCert; I read them sealed.  J5's absence is taken over C_JNT, not corpus-wide.
```

**FLATTENING CHECK — 37/37 clean.** **S03 / the void condition** live at §5: **the `TYPE-R` was
available to be read as `COMPLETION-REFUTED`**, which would have been a striking result and the first
refutation in root D. **It refutes identification only, by its own `test:` clause, and I report the
narrower reading.** **S12** — the `TYPE-R` carried at exactly what it tested; `TARGET` never drifting
to installed. **S26/S08/S19/S24** untouched — no completion procedure, norm, or state imported.
**T1/T5** untouched. **BR-1 HELD** — my own 864 and 880 got no confirming weight; J6 and J7 are
carried from my C4 **as constraints on this system**, not as evidence for it.

---

## 7. FINAL LINES

```text
CONSTRAINTS = derived (7, at bytes).  J1 a COMMON-ORIGIN joint carrier (sealed demand; ABSENT).  J2
  INTERTWINERS TO BOTH EXACT LIMITS AND NOT AN IDENTIFICATION — the system's strongest derived
  condition: the sealed test says "source Banach space and record GNS have DISTINCT CARRIERS AND
  MAPS", and the two completions DIFFER IN WHAT THEY RETAIN (one keeps the source; the other has
  removed it via Rec_N), SO ANY INTERTWINER PAIR MUST BE NON-INJECTIVE ON AT LEAST ONE SIDE OR LAND IN
  A CARRIER STRICTLY LARGER THAN EITHER, WITHOUT COLLAPSING THE DIFFERENCE.  J3 preparation/gluing
  VARIATION, not finite values.  J4 physical boundary gauge ORBIT and edge REDUCTIONS/FUNCTIONALS —
  the finite package's endpoint gauge actions are not these.  J5 microcausal support.  J6 the global
  boundary-form certificate CARRIED AS THE TARGET STATES IT — wrong generator and scale CARRIED, NOT
  REPAIRED — hence CONDITIONAL on B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false |
  TYPE-U, MY OWN C4's CORRESPONDENCE_GAP, which fired at exactly this point.  J7 descent to every
  per-cell closure face = C4's DESCENT_GAP / SC5-E.  C_JNT declared BEFORE any absence line.
VERDICT = CONSTRAINT-GAP.  COMPLETION-FORCED unavailable — J2 constrains intertwiners, far short of
  forcing a carrier.  COMPLETION-REFUTED UNAVAILABLE, AND THIS IS THE FINDING: S3 IS THE ONE FIELD OF
  ROOT D WHERE A SEALED REFUTATION SITS ADJACENT TO THE MISSING OBJECT —
  P2_SOURCE_LIMIT_IDENTIFIED_WITH_RECORD_GNS_LIMIT = false | TYPE-R, A REFUTATION AND NOT A TYPE-U GAP
  — BUT IT REFUTES *IDENTIFICATION*, PER ITS OWN test: CLAUSE, AND A JOINT COMPLETION IS NOT AN
  IDENTIFICATION: IT IS A THIRD CARRIER RECEIVING BOTH VIA INTERTWINERS.  THE REFUTATION DOES NOT
  TRANSFER, AND A READER COULD EASILY TAKE THE TYPE-R FOR EXACTLY THAT — THE SPECIFIC FLATTENING RISK
  S3 CARRIES.  RESIDUAL-FREEDOM unavailable: the quotient IS displayable in form, because both objects
  to be related EXIST and C |-> (iota_src, iota_rec) is definable — S3 IS THE S1 SITUATION, NOT THE S2
  ONE, which is the precise sense in which my 864 "richest" typing was correct — but J1 and J3-J7 are
  absent, so it is uninhabited AND NONE IS WRITTEN.  MISSING INPUTS: J1, J3, J4, J5, J6, J7.  J2 IS NOT
  A MISSING INPUT — IT IS A DERIVED CONSTRAINT THE RECORD SUPPLIES ON ANY FUTURE INTERTWINER PAIR.
CONTROL = EXPLAINS ; result: "no new finite glue is needed" REPRODUCED AS A COROLLARY ; source-index
  status: INDEPENDENT AND AUDITABLE.  DERIVATION: BdGlue_N^law is FINITE DATA, and BOTH completion
  statements are completions OF THAT SAME DATA BY DIFFERENT PROCEDURES — a Banach-norm completion on
  E_src, and a GNS completion of the record-state embeddings AFTER Rec_N has removed the source.
  Enlarging the finite data enlarges BdGlue_N^law, WHICH FEEDS BOTH PROCEDURES EQUALLY, so the same
  two-carrier structure reappears at every N.  THE GAP IS A DIFFERENCE IN WHAT THE TWO PROCEDURES
  RETAIN, AND NO QUANTITY OF FINITE GLUE ALTERS THAT.  TYPE JUSTIFIED PER Q-797 (MY OWN 890
  CORRECTION, WHICH I DO NOT EXEMPT MYSELF FROM): it does NOT eliminate a candidate — there is none,
  contrast S1's Haar which ELIMINATED — and it is NOT a transcription — contrast 889's 4/8 which
  projected onto the list it drew from.  SOURCE INDEX: the explanation draws on §5.1's two completion
  descriptions and the TYPE-R test, while the sentence explained sits in §5.2 — DIFFERENT SPANS, SO
  THE EXPLANATION DOES NOT CITE THE SENTENCE IT REPRODUCES AND WOULD SURVIVE ITS DELETION.
S3 = MATERIAL-PARTIAL (class unchanged from my 864), GAP NOW TYPED AS SEVEN CONDITIONS — ONE (J2) A
  DERIVED CONSTRAINT THE RECORD SUPPLIES, SIX MISSING INPUTS.  NOT DISCHARGED; NO FLAG MOVES.  ROOT D
  NOW HAS ALL FOUR FIELDS INVERTED (S1 887, S2 888, S4-physical 890, S3 here), ALL FOUR RETURNING
  CONSTRAINT-GAP — BUT FOR STRUCTURALLY DIFFERENT REASONS: S2's gap INCLUDES THE OBJECT, S1's and S3's
  SURROUND IT, AND ONLY S3 HAS A SEALED REFUTATION ADJACENT TO IT.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) I LOOKED FOR THE REFUTATION AND FOUND ONE THAT DOES NOT COUNT, WHICH IS THE HONEST VERSION OF THE
      RESULT I WANTED.  I said at pickup that a fourth CONSTRAINT-GAP by pattern would be comfortable
      and uninformative, and that if a refutation existed anywhere in root D it would be here.  A
      sealed TYPE-R IS here — AND IT REFUTES A DIFFERENT RELATION.  Reporting COMPLETION-REFUTED would
      have been the first refutation in root D and would have been WRONG BY ONE WORD IN THE FLAG'S OWN
      test: CLAUSE.
  (2) THE DISCIPLINE I IMPOSED AT 890 NOW BINDS ME AND I APPLIED IT TO MYSELF.  My control is typed
      EXPLAINS with a cross-span source index showing it does not cite the sentence it reproduces.
      Had I not run that check, an EXPLAINS control drawing on §5.2 would have been a transcription
      wearing a better label — exactly what I convicted 889 of.
  (3) J2 IS MINE, DERIVED, AND UN-CROSS-CHECKED, AND IT IS THE PART MOST WORTH ATTACKING.  "The two
      completions differ in what they RETAIN, so an intertwiner pair must be non-injective on at least
      one side or land in a strictly larger carrier" follows from the completions' descriptions plus
      the TYPE-R test — BUT I DID NOT PROVE SUCH A CARRIER EXISTS, NOR THAT ONE CANNOT.  BOTH
      DIRECTIONS ARE OPEN AND I ASSERT NEITHER; if J2 is wrong, six of the seven conditions still
      stand and the verdict does not move.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** All charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-13
pointer-only. Builder-B independence held. `~/.codex` untouched; memory-bank never searched. No
register, plan, tracker, or git action.
