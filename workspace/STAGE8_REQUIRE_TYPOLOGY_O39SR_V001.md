# STAGE 8 — THE TYPOLOGY OF REQUIRES: WHAT SHAPE EACH REQUIRE OF RECORD CUTS OUT — COMMISSION O39SR — V001

## BUILD LANE, 2026-08-15 — READ AND CLASSIFY AT BYTES ONLY

DETERMINATION ONLY. Nothing is proposed, authored, adopted, retired, or repaired.
Every row below is either (i) sealed bytes quoted whole, (ii) a SHAPE-NAME assigned
to a quoted admissible set, or (iii) an absence stated as an absence. No value of
any coupling, scale, root, eigenvalue, norm, or constant is produced, approximated,
bounded, or compared to anything measured. Naming the shape of an admissible set is
structural; no member of any such set is evaluated here.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_REQUIRE_TYPOLOGY_O39SR_V001.md` — probed ABSENT before any write (`ls`
returned "No such file or directory"). No existing file edited. NO GIT ACTION. No
register, tracker, plan, road, handoff, decision-sheet, addendum, or
alpha_supervision file was opened, grepped, or cited.

---

## §0 — GROUND CONSUMED, SEALS VERIFIED

`shasum -a 256 -c` run **from the artifact's own directory**
(`/Users/bgm/MB Work/alpha-program-archive/workspace`), before any reliance.

```text
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md            [JCT]   the sole
      require-side inventory of record (§2, ten rows) + termination sites (§4)
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md      [JCT-A] CONFIRMED-
      WITH-CORRECTIONS; findings F-4, F-5, number audit
OK  STAGE8_PARTITION_THEOREM_T16SR_V001.md                 [PT]    the magnitude
      is allow-side by theorem; scope §6
OK  STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md           [PT-A]
OK  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md          [DVP]   the "never a
      point" claim, at three sites in this file
OK  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_AUDIT_V001.md    [DVP-A] K-1 [KILL]
OK  STAGE8_THRESHOLD_FORM_O19SR_V001.md                    [THF]   the threshold
      inventory; TH-0's congruence disqualification; the WINDOW
OK  STAGE8_THRESHOLD_FORM_O19SR_AUDIT_V001.md              [THF-A]
OK  STAGE8_COMPLETION_AND_FORCING_O37SR_V001.md            [CAF]   the just-
      completed commission's forcing-side inventory
OK  STAGE8_COMPLETION_AND_FORCING_O37SR_AUDIT_V001.md      [CAF-A] its adversarial
      audit; COR-F, COR-H, COR-I, COR-J, COR-L
OK  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md              [WBC]   the NEUTRALITY
      CLAUSE at its origin
OK  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md        [WBC-A] its own test of
      whether the neutrality clause HELD
OK  STAGE8_RATE_GAP_LINKAGE_S9AD_V001.md                   [RGL]   the corpus's
      SECOND, independent neutrality clause, barring a different reading
OK  STAGE8_GATE_SIGNATURE_O18SR_AUDIT_V001.md              [GS-A]  its K3 record
      of the same claim
OK  STAGE8_FORCING_NOTION_O12SR_V001.md                    [FN]    opened to
      dispose a sweep hit: it grades the SAME ten rows, it is not a second
      inventory (§6, SWEEP CUTOFFS, disposition D)

15/15 OK.  MISMATCHES = 0.
```

CARRIED-GRADE DECLARATION: artifacts cited INSIDE the twelve — the AJ pair, the 5d,
the extent pair (S9AD), the JD build, O12SR, O6SR/O8SR, T17SR, T1SR — are consumed
AT CARRIED GRADE, i.e. through the sealed bytes of the twelve, whose own §0 seal
blocks record their verification ([CAF-A] re-verified 34/34 + 3; [THF] verified its
six + six). Their FILES were not opened here. This commission makes no claim it did
not perform.

### §0.1 — CORPUS, AND WHAT THE SECOND ROOT CONTAINS

```text
ROOT 1 (all reads, all content sweeps):
  /Users/bgm/MB Work/alpha-program-archive/workspace                  3671 entries
ROOT 2 (swept, negative for the apparatus; no artifact consumed from it):
  /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
    alpha_fundamental_record_action_cleanroom_v003                    3133 entries

ROOT 2 FINDING, STATED SO THE NEGATIVE IS BOUNDED: the allow/require apparatus
  this commission types is NOT present in root 2 at the sweep terms used.
  `never a point` 0 hits · `closure must bound it` 0 hits · `derives from
  sealed bytes` 0 hits · `admissible set is a point` 0 hits · `require-side
  inventory` 0 hits · `permitted below` 0 hits.  `ALLOW/REQUIRE` returns 143
  lines in 69 files there, but no require-side INVENTORY and no shape claim.
  `NEUTRALITY CLAUSE` returns exactly 1 line there and it is a different object
  ("partial raw-neutrality clause", TASK5 XI_N review, a scalar-route matter).
  Every require of record typed below is located in ROOT 1.
```

REGISTER BAR HELD. Every sweep in this artifact was run with the exclusion set
`!*REGISTER* !*EXECUTION_TRACKER* !*THE_PLAN* !*ROAD_REMAINING* !*THE_HANDOFF*
!OBSERVATIONS_REGISTER* !*DECISION_SHEET* !*_ADDENDUM* !alpha_supervision/**`.
`QUESTIONSSETTLED_REGISTER_V001.md` was never opened, listed, or hit. See FLAG BLOCK.

---

## §1 — Q1: THE TYPOLOGY OF REQUIRES

### §1.0 WHERE THE REQUIRES OF RECORD LIVE, AND WHY THIS IS THE WHOLE SET

There is exactly ONE require-side inventory of record in the corpus. Sweep for the
string `require-side inventory` (case-insensitive, root 1, register-bar exclusions)
returns **3 lines in 3 files**, and they are: the inventory itself
(`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:119` — "## §2 — THE REQUIRE-SIDE
INVENTORY (deliverable 1)"), its audit's grade line
(`STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md:516` — "D1 REQUIRE-SIDE
INVENTORY (§2, 10 rows) = CONFIRMED-WITH-CORRECTIONS"), and one downstream
re-display (`STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md:437`). No second
inventory exists at these terms.

The site set this commission types is therefore **[JCT] §2's ten rows R-1…R-10, plus
the two site-groups of [JCT] §2's own boundary note, plus W-2a** — which is the
14-site set the O37SR audit fixed as the corrected count. [CAF-A] `:630`, whole:

> "**Corrected values, on the build's own §3.3 grading:
> CLOSURE-SUPPLIED 0 · CONTENT-SUPPLIED 10 · UNATTRIBUTED 4 · SITES 14.**"

and [CAF-A] `:1072`, whole: "**No missed require-site found.**"

WHERE THE CORPUS STATES A SHAPE, THE CORPUS GOVERNS. Every class assignment below
that could be read off a formula is instead read off the corpus's own shape word
where one exists — `the integer lattice ℤ`, `a CLASS, not a point`, `a point of the
lattice of subspaces`, `a TWO-POINT admissible set`, `an INTERVAL, not a point`,
`the class of compliant suppliers, not a point`, `a half-line`. Each is quoted in
its row.

### §1.1 THE ROWS

```text
═════════════════════════════════════════════════════════════════════════════
R-1   WINDING / QUANTIZATION                                LATTICE / DISCRETE
  PARAMETER ACTED ON: the winding / sector label n.
  DEFINING TEXT, whole (STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:130,
  the row's "Forcing chain, at bytes" cell):
    "one condition (single-valued closure) whose admissible set is the integer
     lattice ℤ — a CLASS, not a point; the same closure kills continuum winding
     output ("an integer label, not a continuum response magnitude")"
  AND THE CORPUS'S OWN SHAPE RULING, whole
  (STAGE8_THRESHOLD_FORM_O19SR_V001.md:413-423):
    "TH-0  THE QUANTIZATION CONDITION  [PT §3.1 S3/S4, verified]
       BYTES: "closure <=> exp(2 pi i n) = 1" and "The full solution set of
         exp(2 pi i . x) = 1 is EXACTLY Z ... non-integers fail exactly (residual -2
         at 1/2; nonzero at 1/3)".
       FORM [T19-2a/b/c]: the indicator PASSES, FAILS, AND PASSES AGAIN along an
         increasing probe — NON-MONOTONE. **THIS IS A CONGRUENCE (LATTICE)
         CONDITION, NOT A THRESHOLD.** No crossing turns it on and leaves it on.
       WHY IT IS DISPLAYED HERE: it is the require-half of the partition itself, it
         is the sharpest condition in the corpus, and it is NOT threshold-shaped.
         Counting it as a threshold would have inflated the inventory and misdescribed
         the partition's own require-side."
  CLASS: LATTICE / DISCRETE. The corpus supplies both the word `lattice` and the
    word `CONGRUENCE`, and states the solution set exactly: ℤ.
  NAMED CONTENT, at its own bytes and no further: `single-valuedness`,
    `single-valued closure`, `integer-quantized`. [CAF-A] COR-L `:1028-1041`
    withdrew a `holonomy / monodromy / closed cycle` gloss from this row; that
    gloss is NOT used here. See IMPORT AUDIT IMP-1.

═════════════════════════════════════════════════════════════════════════════
R-2   CHARGE STRUCTURE — the structure half            LATTICE / DISCRETE
      (the corpus itself splits this row into two shapes; see R-2/G below)
  PARAMETER ACTED ON: the U(1) character index / the character lattice.
  DEFINING TEXT, whole (T14SR:131, both cells of the row):
    "R-2 | CHARGE STRUCTURE: U(1) characters χ_n with cocycle/transition
     covariance (both characters transform with the same g^n — AJ §6.1, CAS P1);
     the displayed conjugate pair closes magnitude-free, χ_{+1}χ_{−1} = 1 (CAS N4);
     every native composite unit-modulus (CAS N1–N3); on-record amplitude uniquely
     the trivial constant G = 1 (CAS T1) | cocycle consistency + single-valuedness
     jointly pin the character lattice and its conjugation; the modulus theorem is
     an induction over the displayed operations (base + step + closure) | SHAPE-2
     (joint determination of the structure); the G = 1 corollary is SHAPE-1 (single
     require, unique root — an actual forced value: the trivial amplitude) |"
  CLASS: LATTICE / DISCRETE — the corpus's own object is "the character lattice".
  CARRIED, because it cuts across the class: the row also carries two POINT-valued
    conjuncts inside itself — `χ_{+1}χ_{−1} = 1` and `every native composite
    unit-modulus` (a modulus pinned to one). These are sub-conjuncts of this row,
    not separate sites; they are counted in the ROW tally, not the SITE tally.

═════════════════════════════════════════════════════════════════════════════
R-2/G  THE G = 1 COROLLARY (a sub-row of R-2, split by the corpus)      POINT
  PARAMETER ACTED ON: the on-record amplitude G.
  DEFINING TEXT, whole (T14SR:131): "on-record amplitude uniquely the trivial
    constant G = 1 (CAS T1)"; SHAPE cell, whole: "the G = 1 corollary is SHAPE-1
    (single require, unique root — an actual forced value: the trivial amplitude)".
  CLASS: POINT. The corpus names it "an actual forced value" and "unique root".
  ADJUDICATED OF RECORD, whole (STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md
  :318-322): "ADJUDICATED-PASS on R-2's "an actual forced value:
    the trivial amplitude" (G = 1): a sealed derived triviality corollary
    (AJ T1) re-displayed with provenance — same license as displaying the
    sealed "p = -3"; the flag block's "no number as a value of anything"
    reads as "none authored/frozen HERE," which is held."
  SEPARATION FROM THE MAGNITUDE, carried because §4 turns on it: this row's own
    text says the conjugate pair "closes magnitude-free", and [PT] §2 (III)(c)
    says the class's "output is an integer label, not a continuum response
    magnitude". G is not the crossing's magnitude.

═════════════════════════════════════════════════════════════════════════════
R-3   THE CROSSING'S FORM OBLIGATIONS                            NON-NUMERIC
  PARAMETER ACTED ON: none. It constrains the FORM/typing of a lawful supplier.
  DEFINING TEXT, whole (T14SR:132, chain cell): "a sealed demand fixing the FORM
    of any lawful supplier: several displayed clauses jointly pin the shape and its
    four proof obligations; the admissible set is the class of compliant suppliers,
    not a point"
  AND the content cell, whole: "THE CROSSING'S FORM OBLIGATIONS: the
    (JD-1)/(JD-2)/(JD-3)/(JD-3a)/(JD-3c) target form — "The target form is FORCED;
    nothing in it is authored here" (JD §1.3, quoting fdf20bd4 §3.5) — plus the
    obligation sentence: orientation covariance, presentation independence,
    continuity, contact-kernel"
  CLASS: NON-NUMERIC. Orientation covariance, presentation independence,
    continuity and a contact-kernel are conditions on a structure; the corpus's own
    admissible object is a CLASS OF SUPPLIERS, not a set of values. It cuts out no
    set of values at all.

═════════════════════════════════════════════════════════════════════════════
R-4   THE EXACT SQUARES (wall, summed quantifier)                      POINT
  PARAMETER ACTED ON: tr H_A^Σ and ‖H_A^Σ‖₂², at the summed quantifier.
  DEFINING TEXT, whole — the ENTIRE row cell, including the final conjunct that
  [CAF-A] COR-H found dropped from the prior commission (T14SR:133):
    "R-4 | THE EXACT SQUARES (wall, summed quantifier): "tr H_A^Sigma = 0 and
     \|\|H_A^Sigma\|\|_2^2 = 0 EXACT (two independent cancellation mechanisms); HS
     enclosure carrier-free via the DISCHARGED B-L2*" (5d §3); the fourth summed
     quantity's shortfall is a LOGARITHM, and the log is GENUINE at the census grade
     (display pair, fired branch)"
  CLASS: POINT — each of the two named quantities is pinned to exactly one value
    (zero), at the summed quantifier. The corpus's own audit says so, whole
    (STAGE8_DISCHARGERS_VS_PARTITION_O11SR_AUDIT_V001.md:481-485):
      "R-4  "tr H_A^Sigma = 0 and ||H_A^Sigma||_2^2 = 0 EXACT (two independent
       cancellation mechanisms)."  Exact zeros are points.  AND THE TARGET ITSELF
       CALLS THIS ROW "THE CONTROL CASE" at its §3.2, precisely because the record
       forces there "WITHOUT any permission in its legs."  The artifact supplies its
       own counterexample, two sections earlier, and does not notice."
  CARRIED AGAINST THE ROW: the final conjunct records a NON-vanishing (a genuine
    logarithm) at the same site. That conjunct is not a point-pin; it is a
    non-vanishing. Both are in the row and both are displayed here.
  SCOPE OF THE PIN, carried: "decided of record at the summed quantifier ONLY
    ("per-composite untouched")" (T14SR:133, chain cell). The point is cut at Σ,
    not per-composite.

═════════════════════════════════════════════════════════════════════════════
R-5   EXTENT, PURE-CYCLE SLICE                                         POINT
  PARAMETER ACTED ON: the a = 0 slice of ker(q̃_N), inside the lattice of subspaces.
  DEFINING TEXT, whole (T14SR:134): "EXTENT, pure-cycle slice: "CLOSED EXACTLY
    (X-1): the a=0 slice of ker(qtilde_N) = the (R4) line span{(0,1,−R_K,N)}, dim 1"
    — bound ABOVE by (B2-7) faithfulness through the [PROVABLE] (S2a) formation, met
    BELOW by the displayed (R4) generator (extent §4.1) | two conditions meeting
    exactly (upper bound + lower witness), one freedom — the slice is a point of the
    lattice of subspaces | SHAPE-2 (exact joint determination)"
  CLASS: POINT. The corpus uses the word: "the slice is a point of the lattice of
    subspaces". Exactly one subspace satisfies it, and it is displayed by generator.
  CARRIED AGAINST THE ROW: [JCT-A] finding F-4, whole (T14SR-AUDIT:415-421):
    "F-4  R-5's SHAPE-2 gloss "two conditions meeting exactly (upper bound +
       lower witness), one freedom" contradicts the displayed SHAPE-2
       definition ("n conditions, n freedoms") on its face. The SHAPE-2
       assignment itself is the best available (each condition alone
       insufficient — not corroborating surplus, so not SHAPE-3; two
       conditions — not SHAPE-1); the count gloss needs repair or the fit
       needs the qualifier "by minimal joint sufficiency, not by count.""
    F-4 disturbs the SHAPE-n label, NOT the admissible set. The set is a singleton
    either way, and the word "point" is the row's own.

═════════════════════════════════════════════════════════════════════════════
R-6   EXTENT, MENU                        WINDOW / BAND (closed two-entry menu)
  PARAMETER ACTED ON: dim ker(q̃_N).
  DEFINING TEXT, whole (T14SR:135): "EXTENT, menu: "dim ker(qtilde_N) is in {1,2}
    — no third extent exists" (X-2, exact linear algebra at dim E_N = 3);
    zero-value mixing EXCLUDED of record (X-3: value = 0 would force L_T,N = 0
    against the sealed typing) | rank-nullity on the displayed carriers forces a
    TWO-POINT admissible set; the X-3 exclusion is faithfulness + the sealed typing
    jointly | menu: SHAPE-1 form terminating at a two-point set (ONE BIT — §4 site
    J-2); X-3: SHAPE-2"
  AND the corpus's own shape ruling, whole (O19SR:396-403):
    "TH-2  THE EXTENT THRESHOLD  [EXT X-2, verified]
       A THRESHOLD IN: the DIMENSION OF THE RELATION IDEAL'S EXTENT (a dimension).
       BYTES [EXT :53-59]: "THE FULL EXTENT MENU AT EACH ANCHOR STAGE HAS EXACTLY TWO
         ENTRIES ... E-1 (no mixing): ... dim 1 — PURE-CYCLE-COMPLETE; E-2 (mixing):
         ... dim 2 — ONE mixing line ... dim ker(qtilde_N) is in {1,2} — no third
         extent exists."
       SHARP OR LIMIT: **SHARP.** The menu is CLOSED and has exactly two entries
         [T19-3a]; the transition is a change of entry, not a limit of entries."
  CLASS: WINDOW / BAND, on this commission's own definition of that class ("a
    two-sided interval or a closed menu of admissible values"). The corpus's
    governing words are `menu`, `CLOSED`, `EXACTLY TWO ENTRIES`, `no third extent
    exists`, and `a TWO-POINT admissible set`.
  DUAL READING, DISPLAYED SO IT IS NOT HIDDEN: a two-entry set of integers is also
    countably-many-isolated-values, i.e. it satisfies the LATTICE / DISCRETE
    definition as well. The corpus's own shape word is `menu`/`closed`, so WINDOW
    governs per the commission's precedence rule; the reading is recorded because
    it is real, and §4 counts this row under BOTH headings so that no §4 grade
    depends on the choice. CH-2.
  X-3 LEG: "zero-value mixing EXCLUDED of record" removes a single value against a
    sealed typing — a punctured exclusion, not a cut. NON-NUMERIC in its own right.

═════════════════════════════════════════════════════════════════════════════
R-7   ALPHA-FORCING LADDER, DERIVED RUNGS                MIXED — ITEMIZED BELOW
  DEFINING TEXT, whole (T14SR:136): "ALPHA-FORCING LADDER, derived rungs of record
    (5d §3): r-2 "B1-DERIVED: kappa_n → +infinity, full-family"; MO-3 "DERIVED
    p = −3: no coincidence vanishing — that route CLOSED"; T4SR "one-vertex
    trace-parity lemma DERIVED"; clock exponent "PINNED alpha-k in [1, 3/2]" | each
    a single displayed derivation with its own unique conclusion at its own
    quantifier; the exponent pin forces an INTERVAL, not a point | SHAPE-1 each
    (single require, unique root/conclusion); the pin terminates at an interval —
    §4 site J-5"
  THE FOUR RUNGS, EACH TYPED SEPARATELY BECAUSE THE ROW ITSELF LISTS THEM
  SEPARATELY ("SHAPE-1 each"):

  R-7/r-2   "B1-DERIVED: kappa_n → +infinity, full-family"        NON-NUMERIC
     PARAMETER: none is cut. The clause asserts a BEHAVIOUR of a family
     (divergence of a sequence), not an admissible set of values of a parameter.
     No value satisfies or fails it. NON-NUMERIC at bytes.

  R-7/MO-3  "DERIVED p = −3: no coincidence vanishing — that route CLOSED"  POINT
     PARAMETER: p, a degree.
     CLASS: POINT — one value. The corpus's own audit says so, whole
     (O11SR-AUDIT:490): "R-7  carries "DERIVED p = -3" as a rung — a single forced
     degree, not a set."

  R-7/T4SR  "one-vertex trace-parity lemma DERIVED"        INDETERMINATE-AT-BYTES
     PARAMETER: not displayed in this row. The row names a lemma by title and
     marks its grade; it displays no condition, no variable, and no admissible
     set. A parity lemma WOULD be congruence-shaped if its statement were
     displayed — it is not displayed here, and supplying it would be a gloss of
     exactly the kind [CAF-A] COR-L struck. INDETERMINATE-AT-BYTES.

  R-7/clock "PINNED alpha-k in [1, 3/2]"                          WINDOW / BAND
     PARAMETER: α-k, a rate exponent.
     CLASS: WINDOW / BAND — a closed two-sided interval. The corpus states the
     shape three times over: T14SR:136 "the exponent pin forces an INTERVAL, not a
     point"; O11SR:812 CAS line, whole: "PASS  [X3a] R-7: the exponent pin is an
     INTERVAL of nonzero extent -- not a point"; O11SR-AUDIT:522-523, whole: "the
     clock pin is an interval of exact extent 1/2".

═════════════════════════════════════════════════════════════════════════════
R-8 leg 1  CARRIER VANISHING, working class                            POINT
  PARAMETER ACTED ON: the cross-block, via dim H.
  DEFINING TEXT, whole (T14SR:137, leg 1): "CARRIER VANISHINGS (5d §4.3): working
    class "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic,
    member-independent, kernel-independent. Unconditional at that stratum""
    — chain, whole: "the first is a kinematic identity (unconditional at its
    stratum)"; SHAPE cell: "SHAPE-1 (identity)".
  CLASS: POINT — dim H pinned to exactly 0, declared EXACT and UNCONDITIONAL at
    its stratum. The corpus's own audit says so, whole (O11SR-AUDIT:488-489):
      "R-8  "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic,
       member-independent ... Unconditional at that stratum."  A point."

═════════════════════════════════════════════════════════════════════════════
R-8 leg 2  CARRIER VANISHING, the b₁ = 1 cell             POINT (premise-relative)
  PARAMETER ACTED ON: φ_f, and F.
  DEFINING TEXT, whole (T14SR:137, leg 2): "b_1 = 1 cell "mirror collapse phi_f == 0
    (F = 0 mandated) under the entered Attach TYPE-P premise"" — chain, whole:
    "the second is forced RELATIVE to a carried TYPE-P premise and voids with it".
  CLASS: POINT — φ_f ≡ 0 and F = 0 are singletons. THE PIN IS PREMISE-RELATIVE and
    the row itself says the forcing "voids with it". Graded POINT with that clause
    displayed, never without it.

═════════════════════════════════════════════════════════════════════════════
R-9   CHANNEL RIDER                                              NON-NUMERIC
  PARAMETER ACTED ON: none, on the row's own account. It forces a STRUCTURE.
  DEFINING TEXT, whole (T14SR:138): "CHANNEL RIDER (5d §5.1): "a surviving
    cross-block forces the CHANNEL (interference present, irreducible rank-one, no
    sector-respecting basis removes it), NOT the winding magnitude \|n\| — n enters
    the derived form exactly once, as the global n^2 prefactor of an n-blind form"
    | conditional forcing: IF the cross-block survives, the channel is forced; the
    magnitude is expressly NOT — the require's own text draws the junction |
    SHAPE-1, conditional antecedent; terminates at the channel/magnitude split —
    §4 site J-1"
  CLASS: NON-NUMERIC. The forced object the row names is THE CHANNEL — a presence
    (interference present) that no basis in a named class removes. The row's own
    negative clause states what it does NOT reach: the winding magnitude.
  CARRIED, because it cuts across the class: the row displays a rank datum,
    `irreducible rank-one`, which is a single integer. Read as a constraint on the
    rank parameter that datum is POINT-shaped. It is recorded as a cross-cut, not
    as the row's class, because the row's own text names the forced object as the
    channel and not as a rank value. CH-3.

═════════════════════════════════════════════════════════════════════════════
R-10  THE NINE FORCED-FAILURE DISPLAYS D1–D9                     NON-NUMERIC
  PARAMETER ACTED ON: none.
  DEFINING TEXT, chain cell whole (T14SR:139): "nine would-be require-chains, each
    terminating BEFORE forced grade at a named site; the FORCED sentence needs all
    nine (a SHAPE-2 conjunction), and zero are displayable"
  CLASS: NON-NUMERIC. What requires here is an evidentiary/display standard, and
    [CAF] §3.2 quotes O12SR naming it whole: "the governing sentence is the 5d's:
    'A FORCED sentence requires every forcing link displayed at its own quantifier
    from sealed artifacts'" — classified "**DG (OTHER)**. Not one of MT/PT/CA."
    It cuts out no set of values of anything.

═════════════════════════════════════════════════════════════════════════════
W-2a  THE SUPPLIED REQUIRE-SIDE HALF OF W-2                      NON-NUMERIC
  IDENTITY: the same object as R-3. [CAF] §3.2 `:1059-1061`, whole: "W-2a THE
    SUPPLIED REQUIRE-SIDE HALF OF W-2             CONTENT-SUPPLIED
      (the one require-site inside the W-set; identified by O11SR §2.3 and by the
       completion map's CONV-5 as the SAME object as R-3)".
  CLASS: NON-NUMERIC, as R-3. Same four obligations: orientation covariance,
    presentation independence, continuity, contact-kernel.

═════════════════════════════════════════════════════════════════════════════
MIN-5 THE FIVE MINIMALITY DERIVED-BOUND HALVES (one grouped site)  RAY / HALF-LINE
  PARAMETERS ACTED ON: d; C_R; the orthogonalization/saturation quantity; τ; the
    spin class index.
  THE BOUNDARY NOTE THAT PUTS THEM ON THE REQUIRE SIDE, whole (T14SR:141-147):
    "Boundary note (per §1's minimality-split consumption): the five pure minimality
     sites (d = 4 lower-bound selection; C_R = 1 boundary point; orthogonalization
     saturation; tau = 1 floor; spin-1/2 lowest class) are NOT require-side rows —
     each is a derived BOUND (require) followed by an ADOPTED boundary-point
     selection (permission exercised); the derived-bound halves belong to the
     require side, the selections to §3. The mixed site (one vectorlike pair + unit
     charge) carries its unit-charge component "inherited from a different
     carrier/holonomy input" — the charge-structure require R-2 reaches it; the
     pair-count half is minimality-shaped (allow-side)."
  THE SHAPE, in the corpus's own words at the termination row J-8 (T14SR:223),
  whole: "each derived bound ends AT ITS OWN BOUNDARY POINT: "a strict untrapped
    inequality leaves a half-line; the marginal public-closure rule supplies the
    selected boundary point"; "the lower bound is derived, but saturation is
    adopted"; "a derived lower bound is followed by an adopted lower-bound
    selection"; "the unit floor is a frozen branch convention, not a theorem"; "the
    sign selects half-integer classes; primitive minimality selects the lowest
    class""
  CLASS: RAY / HALF-LINE. The corpus supplies the word: "leaves a half-line".
    Four of the five are one-sided (a half-line, a lower bound, a lower bound, a
    floor).
  ITEMIZED EXCEPTION, DISPLAYED: the fifth — "the sign selects half-integer
    classes" — cuts a DISCRETE class set, not a half-line, and its second clause
    ("primitive minimality selects the lowest class") is the ADOPTED selection, not
    the require. So this grouped site is 4 RAY + 1 DISCRETE at the row level.
  GRADE OF THE FORCER: [CAF] graded this site UNATTRIBUTED as to content and did
    not read behind the note; [CAF-A] CH-6 recorded the resulting possible
    understatement. This commission likewise does not read behind the note. The
    SHAPE above is read only from J-8's own displayed stop language.

═════════════════════════════════════════════════════════════════════════════
MIX-1 THE MIXED SITE'S UNIT-CHARGE COMPONENT           INDETERMINATE-AT-BYTES
  PARAMETER ACTED ON: the charge (a unit charge).
  DEFINING TEXT: the boundary note, quoted whole at MIN-5 above.
  WHY INDETERMINATE: the note displays no admissible set for this component. Its
    routing into R-2's character lattice — which would make it LATTICE — is an
    identification the junction survey's OWN AUDIT found unchained. [JCT-A]
    :422-427, whole:
      "F-5  §2 boundary note: "the charge-structure require R-2 reaches it"
         (the mixed site's unit-charge half) is an unchained cross-artifact
         identification; the sealed byte says only "inherited from a
         different carrier/holonomy input," naming no artifact, and the
         recovery sweep behind it is outside the consumed ground. FIX: mark
         the identification as the build's own reading or drop it."
    With F-5 on the record the routing cannot be used to assign a shape.
    INDETERMINATE-AT-BYTES.
═════════════════════════════════════════════════════════════════════════════
```

### §1.2 THE TALLY BY CLASS

```text
BY SITE (the corpus's own 14-site count, [CAF-A] :630):

  POINT                    4    R-4, R-5, R-8 leg 1, R-8 leg 2
  LATTICE / DISCRETE       2    R-1, R-2
  WINDOW / BAND            1    R-6            (closed two-entry menu; see the
                                                dual reading recorded in the row)
  RAY / HALF-LINE          1    MIN-5          (grouped; 4 ray + 1 discrete)
  NON-NUMERIC              4    R-3, R-9, R-10, W-2a
  MIXED (itemized)         1    R-7            (POINT + WINDOW + NON-NUMERIC +
                                                INDETERMINATE across four rungs)
  INDETERMINATE-AT-BYTES   1    MIX-1
                          ──
                          14

BY ROW (itemizing only splits the corpus itself makes — R-2's two shapes, R-7's
"SHAPE-1 each" four rungs, R-8's two legs):

  POINT                    6    R-2/G (G = 1), R-4, R-5, R-7/MO-3 (p = −3),
                                R-8 leg 1 (dim H = 0), R-8 leg 2 (φ_f ≡ 0, F = 0)
  LATTICE / DISCRETE       2    R-1 (the integer lattice ℤ), R-2 (the character
                                lattice)
  WINDOW / BAND            2    R-6 (the closed two-entry extent menu),
                                R-7/clock (α-k in [1, 3/2])
  RAY / HALF-LINE          1    MIN-5 (grouped: "a strict untrapped inequality
                                leaves a half-line")
  NON-NUMERIC              5    R-3, R-7/r-2, R-9, R-10, W-2a
  INDETERMINATE-AT-BYTES   2    R-7/T4SR, MIX-1
                          ──
                          18

THE POINT ROWS, ITEMIZED WITH THEIR PINNED PARAMETER:
  R-2/G        the on-record amplitude G, pinned to the trivial constant
  R-4          tr H_A^Σ and ‖H_A^Σ‖₂², each pinned to zero, at the summed
               quantifier only
  R-5          the a = 0 slice of ker(q̃_N), the unique dim-1 line span{(0,1,−R_K,N)}
  R-7/MO-3     the degree p, pinned at −3
  R-8 leg 1    dim H, pinned to zero, kinematic and unconditional at its stratum
  R-8 leg 2    φ_f and F, pinned to zero RELATIVE TO the entered Attach TYPE-P
               premise, and voiding with it

THE LATTICE / DISCRETE ROWS, ITEMIZED WITH THEIR CUT SET:
  R-1          the winding/sector label n; admissible set EXACTLY ℤ; the corpus
               calls the condition a CONGRUENCE (LATTICE) CONDITION and states the
               indicator is non-monotone
  R-2          the U(1) character index; admissible object "the character lattice
               and its conjugation"

BOTH-KIND SITES: the corpus's require side contains BOTH point-cutting and
band/ray/non-numeric requires. Six rows cut points and two cut lattices; three cut
bands or rays; five cut nothing numeric. THE KINDS ARE NOT UNIFORM, AND WHAT A
REQUIRE CAN DELIVER DEPENDS ON WHICH KIND IT IS.
```

---

## §2 — Q2: THE THRESHOLD SITES, ENUMERATED

### §2.0 A PREMISE OF THE COMMISSION, CORRECTED AT BYTES BEFORE THE ENUMERATION

The commission states that the audit "corrected the count of sites exhibiting the
shape 'same named content permitted below a parameter, required at or above it'
from one to **at least five**." **At bytes that correction is about a different
count.** The "1 → at least 5" correction is COR-J, and COR-J is about the number of
sites at which the corpus states the **uniqueness-does-not-entail-existence
non-implication** operatively. [CAF-A] §6, COR-J, whole (`:1019-1026`):

> "COR-J  A COUNT IN §1.4 CONTRADICTS THE BUILD'S OWN SWEEP RECORD.
>   DECIDED BY  TARGET:1738-1741 (SW-9: "the three that state it operatively")
>               against TARGET:434-435 ("at one site it states the
>               non-implication"); externally, by
>               STAGE8_ACCESSOR_THEOREM_SUBSTITUTION_ADJUDICATION_EINSTEIN_V001.md:123
>               and STAGE8_CONTINUUM_TARGET_INDEPENDENCE_GATE_DARIO_V001.md:226-231.
>   CORRECTED VALUE  The non-implication is stated operatively at AT LEAST FIVE
>               sites, not one."

And [CAF-A] §7 files it under Q1, not Q4, whole (`:1049-1057`):

> "Q1  BUILD: PRESENT-BUT-SCOPED-AWAY (completion-principle axis) / ABSENT
>            (commissioned uniqueness=>existence shape).
>     AUDIT: GRADE UPHELD. Corrections to its supporting statements:
>       - bridge-clause-shaped objects located: 1 -> 2 (both unadopted)   [COR-A]
>       - §1.6 result sentence: the limb "is thereby of record" struck;
>         the limbs "exists" and "may be consumed" stand                  [COR-A]
>       - near-neighbour disposals: (a)-(f) -> (a)-(g)                    [COR-B]
>       - executed precedents of refusal-on-determination: 2 -> 3         [COR-B]
>       - sites stating the non-implication operatively: 1 -> at least 5  [COR-J]"

The Q4 (threshold) corrections are a different two, and neither touches the count of
exact-shape sites. [CAF-A] §7, whole (`:1080-1085`):

> "Q4  BUILD: THRESHOLD-STRUCTURE-PRESENT, seven parameters, one exact-shape site.
>     AUDIT: GRADE UPHELD. Every claimed threshold verified as a threshold in the
>            corpus's own statement; none is an onset read into an unconditional
>            clause; TH-0's disqualification exact; the J-2 instance exact.
>       - parameter list must record the TH-3/TH-4 WINDOW [T19-1b6]        [COR-I]
>       - the negative column must carry O19SR §3.4 alongside TH-0         [COR-I]"

**FINDING. The count of sites exhibiting the permitted-below / required-above shape
is ONE at bytes, not five, and the audit UPHELD it as one.** The build's own
sentence, whole ([CAF] `:1420-1423`): "THE ONE INSTANCE OF THE COMMISSION'S EXACT
SHAPE — one named content PERMITTED below an index and REQUIRED at and above it — is
at the EXTENT site J-2, attaching to require-row R-6, and it needs TWO thresholds
crossed together (TH-2 with TH-1)." And the originating carrier's own sentence,
whole ([THF] `:469-471`): "**=> THE ALLOW/REQUIRE BOUNDARY MOVES WITH A PARAMETER.
THE PARAMETER IS A DIMENSION. THE INSTANCE IS THE B-1 PERMISSION AT J-2, AND IT IS
THE ONLY ONE THE DECLARED SWEEP RETURNS.**"

Independent sweep, this lane, root 1, register-bar exclusions: `permitted below`
returns **7 lines in 3 files** — [CAF] `:1206, :1421, :1877`, [THF-A] `:391`, [THF]
`:78, :426, :633`. Every one of the seven refers to the same single J-2 instance. No
second instance is returned at these terms. The enumeration below therefore
enumerates **every threshold site of record**, exact-shape or not, and marks which
one carries the exact shape.

### §2.1 THE SITES

```text
═════════════════════════════════════════════════════════════════════════════
TH-2 / J-2   THE EXTENT THRESHOLD — THE ONE EXACT-SHAPE SITE
  PARAMETER: the DIMENSION OF THE RELATION IDEAL'S EXTENT — dim ker(q̃_N).
  THRESHOLD VALUE, SYMBOLIC: the upper entry of the closed two-entry menu
    dim ker(q̃_N) ∈ {1,2}; the crossing is 1 → 2. [THF] :398-401, whole:
    "BYTES [EXT :53-59]: "THE FULL EXTENT MENU AT EACH ANCHOR STAGE HAS EXACTLY TWO
       ENTRIES ... E-1 (no mixing): ... dim 1 — PURE-CYCLE-COMPLETE; E-2 (mixing):
       ... dim 2 — ONE mixing line ... dim ker(qtilde_N) is in {1,2} — no third
       extent exists.""
  WHERE THE RECORD'S OWN SITE SITS: **UNKNOWN — UNDECIDED OF RECORD.** [THF]
    :448-449, whole: "STATUS OF THE BIT: 'UNDECIDABLE-TODAY + positive two-model
    certificate' (S9AD), independently re-determined (T1SR) — the witness is
    SEALED, twice." The record does not sit at a determined entry; it carries a
    certificate that both entries model the displayed clause set.
  WHAT BECOMES REQUIRED ON CROSSING: the negation of B-1 inhabitance — a kill.
    [THF] :455-459, whole: "BELOW THE THRESHOLD (extent dimension at its lower
    index): "the B-1 permission OPENS here" — **PERMITTED / ALLOW-SIDE.**
    ABOVE THE THRESHOLD (extent dimension at its upper index): "the SAME quantity
      is not a permission but a forced kill" — **REQUIRED / REQUIRE-SIDE**, and the
      carrier's own words insist it is the SAME quantity."
    Stated precisely, and the carrier's own precision is carried: [THF] CH-5,
    whole (:631-635): "CH-5  THE J-2 FLIP IS STATED PRECISELY, NOT MAXIMALLY. The
      carrier's words are that the same quantity "is not a permission but a forced
      kill". I therefore display: the CONTENT is permitted below, and its NEGATION
      (the kill) is required above. I DO NOT claim B-1 inhabitance itself becomes
      required above — that would overstate the byte, and the byte is the point."
  DOES ANYTHING SUPPLY THE CROSSING? NO. The bit is UNDECIDABLE-TODAY, and the
    require-side assignment additionally needs a SECOND threshold crossed with it.
    [THF] :464-467, whole: "COMPOUNDING [T19-3e3]: the require-side cell
      additionally carries TH-1 — the kill is armed "on the dim M_N >= 2 cell ONLY".
      SO THE REQUIRE-SIDE ASSIGNMENT NEEDS **TWO** THRESHOLDS CROSSED TOGETHER, IN
      TWO DIFFERENT DIMENSIONS. Below either one, the require-side assignment does
      not close."
  GRADE: **UNSUPPLIED.** The crossing is stated, and the record displays no object
    or mechanism placing itself on either side of it.
  REQUIRE-ROW IT ATTACHES TO: R-6 (WINDOW / BAND).

═════════════════════════════════════════════════════════════════════════════
TH-1   THE DIMENSION-THRESHOLD KILL
  PARAMETER: the STAGE DIMENSION, dim M_N.
  THRESHOLD VALUE, SYMBOLIC: dim M_N ≥ 2 — a strict inequality at a stated index.
  BYTES, whole ([THF] :383-394):
    "TH-1  THE DIMENSION-THRESHOLD KILL  [EXT, verified; the one the commission named]
       A THRESHOLD IN: the STAGE DIMENSION (a dimension). Symbolically: the kill NG-3'
         is armed once the stage dimension reaches the stated index and at every index
         above it.
       BYTES [EXT :8-12]: "at dim M_N >= 2 with TOP-1/TOP-2, B-1 INHABITED <=> NO
         MIXING RELATION at the anchor stages; any mixing relation at a dim M_N >= 2
         stage kills BOTH families (NG-3' under forced PIN-1 ...)".
       AND ITS COMPLEMENT, DISPLAYED [EXT :100-101]: "the kill it arms is NG-3' on the
         dim M_N >= 2 cell ONLY (at undisplayed dim M_N the kill does not close)".
       SHARP OR LIMIT: **SHARP — a strict inequality at a stated index** [T19-3e2].
         The carrier writes an inequality against an index, not a rate, not a limit,
         not an asymptotic. Nothing tends to anything."
  WHERE THE RECORD'S OWN SITE SITS: **AT-OR-ABOVE at the one displayed cell; UNKNOWN
    elsewhere.** The carrier's own complement clause says the kill closes "on the
    dim M_N >= 2 cell ONLY (at undisplayed dim M_N the kill does not close)" — i.e.
    one cell is displayed at or above the index and the rest of the stage dimension
    is UNDISPLAYED. No byte at verified seal in this lane's ground states where the
    record's own stage sits generally.
  WHAT BECOMES REQUIRED ON CROSSING: the kill NG-3′ is ARMED — "any mixing relation
    at a dim M_N >= 2 stage kills BOTH families".
  DOES ANYTHING SUPPLY THE CROSSING? **CANNOT-DETERMINE.** The armed-kill cell is
    displayed, but whether an object of record sits at it is not stated in the bytes
    consumed here, and [EXT] itself was not opened (carried grade only, §0).
  GRADE: **CANNOT-DETERMINE.**
  REQUIRE-ROWS IT ATTACHES TO: the R-5 / R-6 extent family; compounds with TH-2.

═════════════════════════════════════════════════════════════════════════════
TH-3   AXIS A — THE OBSTRUCTION'S MULTIPLICITY THRESHOLD
  PARAMETER: the MULTIPLICITY of the generator's NEGATIVE SPECTRAL SUBSPACE per
    momentum — the corpus's own symbol μ, "a rank; symbolic, unassigned".
  THRESHOLD VALUE, SYMBOLIC: μ ≥ m_min, where the corpus defines
    "m_min = min{ r : the negative subspace is inhabited at r }". [THF] :245-250,
    whole: "AXIS A — THE OBSTRUCTION'S OWN TURN-ON (hypothesis (h-4)).
       THRESHOLD IN: the MULTIPLICITY of the generator's NEGATIVE SPECTRAL SUBSPACE
         per momentum — call it  mu  (a rank; symbolic, unassigned).
       CONDITION:  the sandwich is nonzero  <=>  mu >= m_min, where m_min denotes the
         minimal inhabited index, i.e. m_min = min{ r : the negative subspace is
         inhabited at r }. STATED SYMBOLICALLY; NOT EVALUATED."
  WHERE THE RECORD'S OWN SITE SITS: **STRICTLY ABOVE.** [THF] :260-262, whole:
    "SLACK [T19-1a5]: the record's own site is STRICTLY ABOVE m_min. THE RECORD DOES
      NOT SIT AT THIS MARGIN — a fact worth displaying, because a threshold whose
      record site sits strictly inside is not a knife-edge."
  WHAT BECOMES REQUIRED ON CROSSING: the sea-sandwich obstruction becomes nonzero —
    i.e. the theorem's hypothesis (h-4) holds and the obstruction applies.
  DOES ANYTHING SUPPLY THE CROSSING? **YES — AN OBJECT IS EXHIBITED.** [THF]
    :251-257, whole: "THE RECORD'S OWN THREE DISPLAYED VALUES OF mu, at bytes:
        O6-1b2  definite scalar symbol: "{m < 0} is EMPTY => C = 0 => C T C = 0
                => THEOREM VACUOUS"                                  — mu at its floor
        O6-1c2  indefinite scalar symbol: "{m<0} = half-space {k3<0} is NONEMPTY
                => C != 0"                                           — mu inhabited
        O6-1a5  the record's Clifford site: "rank(P_-) = 2 per k (tr P_- = 2)"
                                                                     — mu ABOVE m_min"
  GRADE: **SUPPLIED.** Three displayed values of the parameter, and the record's own
    Clifford site sits strictly above the turn-on with the multiplicity displayed.
  REQUIRE-ROW IT ATTACHES TO: none of the ten. [CAF] :1311-1317 states this whole:
    "TH-3  MULTIPLICITY of the generator's negative spectral subspace per momentum
          (m >= m_min)                              -> the sea-sandwich
          obstruction's applicability, i.e. the W-3 / FP-3 thread. NOT a
          require-side row of the junction inventory; a threshold on an
          OBSTRUCTION's applicability."

═════════════════════════════════════════════════════════════════════════════
TH-4   AXIS B — THE ESCAPE'S POSITION-DEGREE THRESHOLD
  PARAMETER: the DEGREE OF THE GENERATOR'S SYMBOL IN THE POSITION VARIABLE — the
    corpus's own symbol δ, "a structural degree; symbolic, unassigned".
  THRESHOLD VALUE, SYMBOLIC: δ at its floor. [THF] :267-277, whole:
    "AXIS B — THE ESCAPE'S TURN-ON (hypotheses (h-1)/(h-2)).
       THRESHOLD IN: the DEGREE OF THE GENERATOR'S SYMBOL IN THE POSITION VARIABLE —
         call it  delta  (a structural degree; symbolic, unassigned).
       CONDITION:  the theorem APPLIES  <=>  delta is at its floor (constant
         coefficients); it CEASES TO APPLY once delta exceeds that floor.
       FORM [T19-1b1..b5]: the applicability indicator is monotone NONINCREASING in
         delta and non-constant  =>  **THRESHOLD FORM IN A STRUCTURAL DEGREE**,
         turning OFF at the minimal index above the floor.
       SHARP OR LIMIT: **SHARP.** O6-1d5 is `d/dx = 0` exactly and O6-1d3 is
         `A(x+a) - A(x) = a != 0` exactly — an identity against a non-identity, not a
         limit."
  WHERE THE RECORD'S OWN SITE SITS: the corpus displays THREE cases and sorts them
    by this degree, but the crossing case is a COUNTERFACTUAL display. [THF] :206-207,
    whole: "[ORIG] §4.3 Counterfactual B is the escape display. Its CAS rows O6-1d1..d5
    are quoted at bytes from [ORIG] :1084-1111". At the floor: O6-1d5 "a CONSTANT
    potential shifts the symbol only: d/dx = 0 => translation invariance RESTORED
    (pure-gauge case)". Above the floor: O6-1d3 "a NON-CONSTANT U(1) potential BREAKS
    translation invariance: A(x+a) - A(x) = a != 0".
  WHAT BECOMES REQUIRED ON CROSSING: **NOTHING BECOMES REQUIRED — this is a turn-OFF,
    and above it the record decides nothing.** Carried whole because it cuts against
    any stronger reading ([THF] :326-339):
    "WHAT THIS DOES TO THE FORM ANSWER [DERIVED]:
       Above Axis B's threshold the obstruction's fate is **NOT DECIDED OF RECORD**.
       So the escape does not even map into a two-valued codomain {blocker holds,
       blocker escapes}: it maps into {THEOREM APPLIES, THEOREM DOES NOT APPLY}, and
       the second cell is UNDETERMINED, not "escaped".
       CONSEQUENCE, EXACT: what is threshold-shaped is the APPLICABILITY OF A
       SUFFICIENT CONDITION — not the presence of the obstruction. A BINARY reading
       is refused twice over: once because U(1)-presence is not the coordinate
       (§2.2), and once because the codomain above the threshold is not a decided
       alternative at all. THIS IS THE STRONGEST STATEMENT THE SEALED GROUND
       SUPPORTS, AND IT IS WEAKER THAN "THE ESCAPE IS THRESHOLD-SHAPED" WOULD BE IF
       READ AS "THE OBSTRUCTION TURNS OFF ABOVE A THRESHOLD". IT DOES NOT SAY THAT,
       AND NEITHER DOES THIS ARTIFACT."
  DOES ANYTHING SUPPLY THE CROSSING? An OBJECT is exhibited — a non-constant U(1)
    potential with its exact difference `A(x+a) - A(x) = a != 0` — but it is exhibited
    inside a COUNTERFACTUAL, and what lies above the crossing is undecided.
  GRADE: **SUPPLIED as to the crossing object; the consequence UNDETERMINED of
    record.** This site does not exhibit the commission's shape: nothing is required
    at or above it.
  FENCE NOTE, CARRIED: this axis is marked "**COUPLING-EXPRESSIBLE — DISPLAYED AND
    STOPPED AT §2.4**" ([THF] :410). This lane does not go behind that stop.

═════════════════════════════════════════════════════════════════════════════
TH-3 + TH-4 JOINTLY — THE WINDOW
  This is the WINDOW the commission names. It is NOT a require's admissible set; it
  is the applicability region of an OBSTRUCTION THEOREM. [THF] :279-283, whole:
    "THE JOINT SHAPE [T19-1b6]: the two axes run in OPPOSITE SENSES — Axis A is a
      LOWER condition (mu at or above m_min) and Axis B an UPPER condition (delta at
      its floor). The sea-sandwich theorem therefore applies inside a WINDOW in two
      structural quantities, NOT on a single presence/absence bit. That is the
      form answer, and it is derived from the sealed rows alone."
  [CAF-A] COR-I ordered this into the parameter list, whole (:790-792): "A WINDOW in
    two structural quantities is a materially different threshold structure from two
    independent thresholds, and it is [T19-1b6]-DERIVED, not a reading. Its omission
    understates Q4."
  GRADE: n/a — this is a joint region, not a site with its own crossing.

═════════════════════════════════════════════════════════════════════════════
ONSET-A   THE CROSS-BLOCK NONVANISHING ONSET  (R-8 leg 1 against R-9)
  PARAMETER: the RANK of the cross-block (equivalently, its vanishing or survival).
  THRESHOLD VALUE, SYMBOLIC: rank 0 → rank ≥ 1; the surviving object is displayed at
    "irreducible rank-one".
  WHERE THE RECORD'S OWN SITE SITS: **BELOW, at the working class.** R-8 leg 1,
    whole: "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic,
    member-independent, kernel-independent. Unconditional at that stratum".
  WHAT BECOMES REQUIRED ON CROSSING: the CHANNEL. R-9, whole: "a surviving
    cross-block forces the CHANNEL (interference present, irreducible rank-one, no
    sector-respecting basis removes it), NOT the winding magnitude |n| — n enters the
    derived form exactly once, as the global n^2 prefactor of an n-blind form".
  DOES ANYTHING SUPPLY THE CROSSING? **NO, at derived grade.** The row's forcing is
    stated as a conditional — "conditional forcing: IF the cross-block survives, the
    channel is forced" — and the carrier the crossing would need is displayed as
    ENTERED, not DERIVED. [JCT] :370, whole: "A-9 (r_0, r_ch) | D-ENTER at DERIVED
    grade: D1's own demand is "a DERIVED connected physical b_1 >= 2 filled-face
    carrier" — carrier + domain extension landing together is SHAPE-2; rank data stay
    barred while the entry is ENTERED |". R-10's D1 states the same in one phrase:
    "D1 no derived carrier (ENTERED is not DERIVED)".
  GRADE: **UNSUPPLIED.**
  DECLARED AS A LANE READING OF RECORD, carried: [CAF-A] :767-770, whole:
    "ONSET-A / ONSET-B.  These ARE lane readings, and the build says so in IMP-3
       with the corpus's own contrary vocabulary displayed ("the corpus does not
       call these onsets; it calls R-9 'conditional forcing' and R-4 an 'exact
       cancellation'"). That is the correct handling. NOT A DEFECT."

═════════════════════════════════════════════════════════════════════════════
ONSET-B   THE FIRST-NONVANISHING-MOMENT ONSET  (R-4, read as an onset)
  PARAMETER: the ORDER OF THE ANGULAR / BRANCH MOMENT.
  THRESHOLD VALUE, SYMBOLIC: the first order at which a moment fails to vanish;
    the corpus's displayed vanishing runs at orders 1 and 3 in λ ("m_1 = m_3 = 0
    exact at full tau_R") and its displayed firing is at the degree-3 class.
  WHERE THE RECORD'S OWN SITE SITS: **THE CORPUS DISPLAYS BOTH SIDES.** Below, whole:
    "each branch factor's weight sum annihilates every odd function of lambda — m_1 =
    m_3 = 0 exact at full tau_R — so K^Sigma is EVEN in the coupling". At/above,
    whole as [CAF] :1358-1361 carries it from T17SR: "THE ARMED BRANCH FIRED:
    L_deg3 != 0 ==> THE LOG IS GENUINE at the degree-3 class total — the carrier
    integral log Lambda survives with displayed nonzero coefficient; the commissioned
    log-free rate is REFUTED."
  WHAT BECOMES REQUIRED ON CROSSING: a genuine logarithm at the degree-3 class total,
    with displayed nonzero coefficient.
  DOES ANYTHING SUPPLY THE CROSSING? **YES — a fired branch of record.** The display
    pair fired; the log is graded GENUINE at the census grade.
  GRADE: **SUPPLIED.**
  BOUNDARY CARRIED WHOLE, because it limits the row ([CAF] :1363-1367): "HONEST
    BOUNDARY, stated because it limits the row: this onset governs a DECIDED
    summed-quantifier state (FP-S), and the corpus bars its transport: "the V-2-barred
    bridge is not crossed in either direction; the one-way quantifier record
    (0a10c030) is respected". ONSET-B decides at its own quantifier and nowhere else."

═════════════════════════════════════════════════════════════════════════════
EXP-1   F2's DIAGONAL-SYMBOL THRESHOLDS
  PARAMETER: a DEGREE (a diagonal symbol degree).
  THRESHOLD VALUE, SYMBOLIC: trace threshold < −3 (STRICT); HS threshold stated at
    3/2. [CAF] :1379-1383, whole: "EXP-1  F2's DIAGONAL-SYMBOL THRESHOLDS. WBC
    verdict, whole: "the four quantities' shortfall is a diagonal-symbol statement
    (degree -1 at the stay strata vs trace threshold < -3, HS threshold 3/2 — WB7,
    audit-corrected gap form)". PARAMETER: a DEGREE. The trace threshold is STRICT
    ("< -3"); the HS threshold is stated at 3/2."
  WHERE THE RECORD'S OWN SITE SITS: **STRICTLY ON THE FAILING SIDE** — the quoted
    verdict displays "degree -1 at the stay strata" against a threshold of "< -3".
  WHAT BECOMES REQUIRED ON CROSSING: nothing is displayed as required at or above it.
  DOES ANYTHING SUPPLY THE CROSSING? **NO.**
  GRADE: **UNSUPPLIED.**
  CONVENTION CONFLICT CARRIED, not reconciled ([THF] CH-8, whole, :645-648):
    "CH-8  I DID NOT RECONCILE [ORIG]'s OWN FLAGGED SIGN-CONVENTION CONFLICT (its
      FLAG-3: "HS threshold 3/2" vs "HS threshold -3/2" in two sealed carriers).
      It bears on a different question and reconciling two sealed conventions
      would be authorship. CARRIED FORWARD UNTOUCHED."

═════════════════════════════════════════════════════════════════════════════
EXP-2   THE SUMMED TRACE'S LOG THRESHOLD
  PARAMETER: a DEGREE (effective diagonal degree).
  THRESHOLD VALUE, SYMBOLIC: < −3, STRICT. [CAF] :1384-1387, whole: "EXP-2  THE
    SUMMED TRACE'S LOG THRESHOLD. WSR, whole as T17SR carries it: "effective diagonal
    degree -3 = log Lambda (WS4, exact); the threshold (< -3) is strict; NO swept
    sealed asset crosses it". PARAMETER: a DEGREE, threshold strict, and the corpus
    records that nothing crosses it."
  WHERE THE RECORD'S OWN SITE SITS: **AT −3, i.e. AT the boundary and on the failing
    side of a strict inequality** — "effective diagonal degree -3 = log Lambda".
  WHAT BECOMES REQUIRED ON CROSSING: nothing is displayed as required at or above it.
  DOES ANYTHING SUPPLY THE CROSSING? **NO, and the corpus says so in the same
    sentence**: "NO swept sealed asset crosses it".
  GRADE: **UNSUPPLIED.**

═════════════════════════════════════════════════════════════════════════════
EXP-3   W-1's COUNTING THRESHOLD
  PARAMETER: an EXPONENT q (a threshold-decay exponent).
  THRESHOLD VALUE, SYMBOLIC: q < 2, STRICT, uniformly in n. [CAF] :1388-1394, whole:
    "EXP-3  W-1's COUNTING THRESHOLD. W-1 §4 T-2d, whole: "The topology carrier must
      deliver a threshold-decay exponent `q` STRICTLY BELOW 2, uniformly in n." and
      its sharpness, whole: "The Chebyshev exponent produced by the rate itself is
      `q = 2`, and `q = 2` is **precisely** the divergent case. So the necessary
      condition T-2c is *never* sufficient: the gap is not an artifact of a crude
      estimate, it is a boundary case sitting exactly on the failure line."
      PARAMETER: an EXPONENT q, with the onset AT the strict inequality q < 2."
  WHERE THE RECORD'S OWN SITE SITS: **EXACTLY AT THE FAILURE LINE** — q = 2, "a
    boundary case sitting exactly on the failure line", on the failing side of a
    strict inequality.
  WHAT BECOMES REQUIRED ON CROSSING: nothing is displayed as required at or above it;
    the crossing is an OBLIGATION ON AN UNBUILT CARRIER.
  DOES ANYTHING SUPPLY THE CROSSING? **NO.**
  GRADE: **UNSUPPLIED.**

═════════════════════════════════════════════════════════════════════════════
TH-0   THE QUANTIZATION CONDITION — DISPLAYED SO IT IS NOT COUNTED
  NOT A THRESHOLD SITE. The corpus disqualifies it, and its disqualification is the
  hinge of this whole commission. Quoted whole in §1.1 at row R-1, and verified by
  the adversarial audit ([CAF-A] :759-761, whole): "TH-0 NOT A THRESHOLD.  VERIFIED.
    O19SR's own disqualification is quoted whole and accurately, including the
    non-monotone indicator argument. The build's use of it as the governing negative
    is exact."
  GRADE: n/a — it is a CONGRUENCE (LATTICE) condition, not a threshold.

═════════════════════════════════════════════════════════════════════════════
J-6      THE MAGNITUDE SITE — AN EXPLICIT NON-THRESHOLD
  Displayed here because Q2 asks for every site of the shape and this is the record's
  own display that one site DOES NOT have it. [THF] §3.4, whole (:478-492):
    "AT THE MAGNITUDE SITE (J-6) THE BOUNDARY DOES NOT MOVE WITH ANY PARAMETER.
       [PT] §2 (I): the require-half holds "In EVERY model of Σ".
       [PT] §2 (II): the magnitude "sits allow-side in every model, with the two-model
         witness uniform over the whole model class."
       [PT] §3.1: "every phase-law clause of Σ holds at SYMBOLIC integer n [CAS PT4]
         — the clauses force the CLASS and pin no member. The member |n| stays
         allow-side."
       [T19-4a/b] re-derives the uniformity: the class condition holds at symbolic
         integer n, so no index range separates a permitted regime from a required
         one at this site. THE SPLIT IS CLASS-VERSUS-MEMBER, NOT LOW-VERSUS-HIGH.
       CORROBORATED INDEPENDENTLY BY [DVP] :508-515: "the record forces the
         ADMISSIBLE SET and leaves the SELECTION WITHIN IT free. Class / member. Menu
         / bit. Pin / location. Three sites, one pattern, and never a point on the
         require side ... The axis is CLASS/MEMBER"."
  NOTE ON THE ELISION INSIDE THAT QUOTED BLOCK: the ellipsis in the [DVP] citation is
    the corpus's own, not this lane's, and §3 below displays what it removes.
  GRADE: n/a — a displayed absence of threshold structure, not a threshold site.
═════════════════════════════════════════════════════════════════════════════
```

### §2.2 THE Q2 RESULT

```text
SITES ENUMERATED: 9 threshold sites (TH-1, TH-2/J-2, TH-3, TH-4, ONSET-A, ONSET-B,
  EXP-1, EXP-2, EXP-3), plus one joint region (the TH-3/TH-4 WINDOW), plus two
  displayed NON-thresholds (TH-0, J-6).

SITES EXHIBITING THE EXACT SHAPE — the SAME named content permitted below a
  parameter and required at or above it: **ONE.** TH-2 / J-2, the extent site,
  attaching to require-row R-6. That is the count at bytes and it is the count the
  adversarial audit UPHELD ([CAF-A] :1080-1083). It is NOT five; the "1 -> at least
  5" correction of record is COR-J and concerns the non-implication count (§2.0).

SUPPLIED GRADES:
  SUPPLIED            2    TH-3 (three displayed values of μ; the record's Clifford
                             site strictly above with the multiplicity displayed);
                           ONSET-B (an armed branch of record FIRED, L_deg3 != 0)
  SUPPLIED, CONSEQUENCE UNDETERMINED
                      1    TH-4 (a non-constant potential is exhibited, but above
                             the crossing the obstruction's fate is NOT DECIDED OF
                             RECORD)
  UNSUPPLIED          5    TH-2/J-2 (bit UNDECIDABLE-TODAY; needs two crossings
                             together); ONSET-A (carrier ENTERED, not DERIVED);
                           EXP-1; EXP-2 ("NO swept sealed asset crosses it");
                           EXP-3 (obligation on an unbuilt carrier, record sitting
                             exactly on the failure line)
  CANNOT-DETERMINE    1    TH-1 (the armed cell is displayed; where the record's own
                             stage sits is not stated in the consumed bytes, and
                             [EXT] was not opened)

WHERE THE RECORD SITS, COLLECTED:
  STRICTLY ABOVE          TH-3 (with displayed slack; "the record does not sit at
                          this margin")
  BOTH SIDES DISPLAYED    ONSET-B (odd moments exactly zero; degree-3 branch fired)
  BELOW                   ONSET-A (cross-block vanishes identically at the working
                          class)
  AT THE FAILING BOUNDARY EXP-2 (degree −3 against a strict < −3); EXP-3 (q = 2
                          against a strict q < 2, "exactly on the failure line")
  STRICTLY ON THE
    FAILING SIDE          EXP-1 (degree −1 against < −3)
  UNKNOWN / UNDECIDED     TH-2/J-2 (UNDECIDABLE-TODAY, twice-sealed certificate);
                          TH-1 (undisplayed stage dimension); TH-4 (the crossing
                          case is a counterfactual display)

THE ONE EXACT-SHAPE SITE IS UNSUPPLIED, AND THE TWO SUPPLIED SITES DO NOT HAVE THE
EXACT SHAPE: TH-3 is a threshold on an OBSTRUCTION'S APPLICABILITY, not on a
permitted/required assignment; ONSET-B is a lane reading of an exact-cancellation
row, declared as such of record and decided at one quantifier only.
```

---

## §3 — Q3: THE "NEVER A POINT" CLAIM

### §3.1 WHERE IT IS STATED, AND WHAT IT IS

Sweep, root 1, register-bar exclusions, this commission's own output excluded,
`never a point`: **14 lines in 5 files.** The claim ORIGINATES in one file at three
places; two files carry it; one file adjudicates it.

```text
ORIGIN — STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md
  :510   the §4.3 prose (the claim proper)
  :814   the CAS PASS line [X4]
  :996   the §11 verdict block
CARRIED — STAGE8_THRESHOLD_FORM_O19SR_V001.md :491
CARRIED — STAGE8_GATE_SIGNATURE_O18SR_AUDIT_V001.md :295   (as its K3 record)
CARRIED — STAGE8_COMPLETION_AND_FORCING_O37SR_AUDIT_V001.md :810, :813, :998
ADJUDICATED — STAGE8_DISCHARGERS_VS_PARTITION_O11SR_AUDIT_V001.md
  :462, :473, :525, :705, :706, :879   (the KILL and its correction)
```

**THE CLAIM, QUOTED WHOLE WITH ITS SCOPE CLAUSE** — [DVP] §4.3, `:508-515`, the
entire paragraph including the sentence that scopes it to three sites and the
sentence that reaches past them to "the partition":

> "**WHAT THE PARTITION IS ACTUALLY TRACKING, DERIVED FROM THE SAME THREE SITES:** the record
> forces the **ADMISSIBLE SET** and leaves the **SELECTION WITHIN IT** free. Class / member. Menu
> / bit. Pin / location. Three sites, one pattern, and never a point on the require side [CAS X4].
> The record says it in its own words at the partition theorem's class-vs-member note: "every
> phase-law clause of Σ holds at SYMBOLIC integer n [CAS PT4] — **the clauses force the CLASS and
> pin no member.** The member \|n\| stays allow-side." The axis is CLASS/MEMBER, i.e.
> admissible-set-shape versus selection-within-it — which is exactly the forcing criterion's own
> axis ("the admissible set is a point") and has no localizability content whatsoever."

**ITS CAS TAG, QUOTED WHOLE** — [DVP] `:814`, including the scope parenthesis the
prose drops:

> "PASS  [X4] PAIRED SITES: require fixes the admissible SET (never a point); allow is the SELECTION within it (3/3)"

**ITS VERDICT-BLOCK RESTATEMENT, QUOTED WHOLE** — [DVP] `:994-998`:

> "  WHAT THE PARTITION ACTUALLY TRACKS, derived from those three pairs: it forces the
>     ADMISSIBLE SET and leaves the SELECTION WITHIN IT free — class/member, menu/bit,
>     pin/location; never a point on the require side.  That is the forcing criterion's
>     own axis ("the admissible set is a point"), and it carries no localizability
>     content.  The record states it: "the clauses force the CLASS and pin no member.""

### §3.2 IS IT A THEOREM, A SWEEP RESULT, OR AN OBSERVATION?

**AN OBSERVATION, GENERALIZED FROM THREE SITES.** At bytes:

- It is NOT a theorem. No proof, no model quantifier, no clause base appears with it.
  Its own header calls it "DERIVED FROM THE SAME THREE SITES", and the three sites
  are the J-1 / J-2 / J-5 pairs displayed immediately above it at [DVP] `:494-499`.
- It is NOT a sweep result. Nothing in [DVP] §4.3 declares a sweep behind it; the
  supporting apparatus is the CAS battery, and the CAS line [X4] tests the three
  paired sites and reports "(3/3)".
- It IS an observation, and its own file states the derivation base twice: "DERIVED
  FROM THE SAME THREE SITES" (prose) and "derived from those three pairs" (verdict
  block).

**WHAT IT QUANTIFIES OVER — TWO DIFFERENT SCOPES IN THE SAME FILE, AND THE
DIFFERENCE IS THE WHOLE QUESTION.**

```text
SCOPE AS THE CAS TAG WRITES IT:   the three PAIRED SITES J-1 / J-2 / J-5, "(3/3)".
SCOPE AS THE PROSE AND THE VERDICT BLOCK WRITE IT: "WHAT THE PARTITION IS ACTUALLY
  TRACKING" / "WHAT THE PARTITION ACTUALLY TRACKS" — i.e. the require side of the
  partition, unrestricted. Neither sentence carries a restricting clause; both
  generalize from three sites to the partition.
```

So the claim, AS STATED in the prose and the verdict block, quantifies over the
require side generally — not over one artifact's rows and not over one kind of
require. That is the scope this commission tests it at, because that is the scope
its own bytes assert.

### §3.3 THE TEST AGAINST THE §1 TALLY — **CONTRADICTED AT BYTES**

**Yes. Requires classified POINT and requires classified LATTICE/DISCRETE both fall
inside the claim's stated scope. Both texts are displayed.**

```text
THE CLAIM:   "Three sites, one pattern, and never a point on the require side"
             ([DVP] :510, quoted whole above)

AGAINST THE §1 POINT ROWS — every one of them a require of record on the require
side, and every quotation whole:

  R-2/G   "on-record amplitude uniquely the trivial constant G = 1 (CAS T1)"
          SHAPE cell: "the G = 1 corollary is SHAPE-1 (single require, unique root
          — an actual forced value: the trivial amplitude)"       [JCT :131]
  R-4     "tr H_A^Sigma = 0 and \|\|H_A^Sigma\|\|_2^2 = 0 EXACT (two independent
          cancellation mechanisms)"                                [JCT :133]
  R-5     "two conditions meeting exactly (upper bound + lower witness), one
          freedom — the slice is a point of the lattice of subspaces"  [JCT :134]
  R-7/MO-3 "DERIVED p = −3: no coincidence vanishing — that route CLOSED" [JCT :136]
  R-8 lg1 "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic,
          member-independent, kernel-independent. Unconditional at that stratum"
                                                                   [JCT :137]
  R-8 lg2 "mirror collapse phi_f == 0 (F = 0 mandated) under the entered Attach
          TYPE-P premise"                                          [JCT :137]

AGAINST THE §1 LATTICE ROWS — a congruence cuts discrete points, which is what the
claim's own word "point" denies:

  R-1     "one condition (single-valued closure) whose admissible set is the integer
          lattice ℤ"                                               [JCT :130]
          and the corpus's own shape ruling: "**THIS IS A CONGRUENCE (LATTICE)
          CONDITION, NOT A THRESHOLD.**"                           [THF :418-419]
  R-2     "cocycle consistency + single-valuedness jointly pin the character lattice
          and its conjugation"                                     [JCT :131]

=> THE CLAIM IS CONTRADICTED AT BYTES, ON THE VERY INVENTORY IT RUNS ON.
```

### §3.4 AND THE CONTRADICTION IS ALREADY ADJUDICATED OF RECORD — K-1 [KILL]

This lane did not find this. The corpus found it, in the audit of the artifact that
made the claim, and ordered the sentence STRUCK. [DVP-A] §5.2, quoted whole
including the clause naming what survives:

> "### §5.2 **K-1 [KILL]** — "NEVER A POINT ON THE REQUIRE SIDE" IS FALSE ON THE SURVEY'S OWN ROWS
>
> Having refuted the principal's reframe, the target installs a replacement in its place. That
> replacement is asserted from three examples and is false as stated. This is the same failure
> the commission named — an axis read off examples rather than derived — displaced onto the
> successor claim."

and the counterexample block, whole ([DVP-A] `:469-491`):

> "THE CLAIM AUDITED, target §4.3 and §11, verbatim: "WHAT THE PARTITION IS ACTUALLY
>   TRACKING, DERIVED FROM THE SAME THREE SITES: the record forces the ADMISSIBLE SET and
>   leaves the SELECTION WITHIN IT free.  Class / member.  Menu / bit.  Pin / location.
>   Three sites, one pattern, AND NEVER A POINT ON THE REQUIRE SIDE [CAS X4]" ; "The axis IS
>   CLASS/MEMBER ... which is exactly the forcing criterion's own axis."
>
> THE COUNTEREXAMPLES, ALL FROM T14SR §2 — the very inventory the classification runs on:
>   R-2  "on-record amplitude uniquely the trivial constant G = 1 (CAS T1)", and the survey's
>        own SHAPE cell: "the G = 1 corollary is SHAPE-1 (single require, unique root — AN
>        ACTUAL FORCED VALUE: the trivial amplitude)."  A POINT, on the require side, named
>        as such by the survey.
>   R-4  "tr H_A^Sigma = 0 and ||H_A^Sigma||_2^2 = 0 EXACT (two independent cancellation
>        mechanisms)."  Exact zeros are points.  AND THE TARGET ITSELF CALLS THIS ROW "THE
>        CONTROL CASE" at its §3.2, precisely because the record forces there "WITHOUT any
>        permission in its legs."  The artifact supplies its own counterexample, two sections
>        earlier, and does not notice.
>   R-5  "two conditions meeting exactly (upper bound + lower witness), one freedom — THE
>        SLICE IS A POINT of the lattice of subspaces."  The survey uses the word.
>   R-8  "cross-block vanishes IDENTICALLY, dim H = 0 exact — kinematic, member-independent
>        ... Unconditional at that stratum."  A point.
>   R-7  carries "DERIVED p = -3" as a rung — a single forced degree, not a set.
>   [audit D2 PASS — 5 counterexamples enumerated against the generalisation.]
>
> THE INTERNAL REDUCTIO, which is why this cannot be repaired by softening.  The forcing
>   criterion IS "the admissible set is a point" and the four-case map has a LIVE row
>   "REQUIRE + UNIQUE SOLUTION — *** YES ***".  If the require side never reached a point,
>   that row would be vacuous and NOTHING COULD EVER FORCE — which the target itself denies
>   at §3.3: "NOT 'the record forces nothing without permission.'  FALSE at bytes."  The
>   claim contradicts the criterion it says it is identical to.  [audit D3 PASS.]"

and the correction ordered, whole ([DVP-A] `:518-533`):

> "WHAT SURVIVES, EXACTLY, AND IT IS WORTH KEEPING:
>   The target's OWN CAS tag is already scoped — "[X4] PAIRED SITES ... (3/3)".  At J-1,
>   J-2 and J-5 the display is exact and I re-derived every part of it: the closure's
>   admissible label set is the CLASS of integers with an exact residual -2 at n = 1/2 and a
>   second exact failure at 1/3; the extent menu is a TWO-point set; the clock pin is an
>   interval of exact extent 1/2; and in each pair the allow row is the selection inside the
>   forced set.  [audit A1-A5, B1-B2, C1-C2, D1, I7 PASS.]
> CORRECTION REQUIRED: strike "never a point on the require side" and "the axis IS
>   CLASS/MEMBER" as statements ABOUT THE PARTITION, in §4.3 and in the §11 NET.  Replace
>   with the scoped display: AT THE THREE PAIRED TERMINATION SITES J-1/J-2/J-5, the require
>   half fixes an admissible set, the allow half is the selection within it, and both halves
>   are of the same localizability type.
> NON-DISTURBANCE, CHECKED EXPLICITLY: D3(a) does not rest on this.  The refutation of the
>   principal's reframe stands on A-3 alone and on four more rows.  §2's classifications,
>   §3's category statement, §5's counter-test and §6's consequence display are all
>   independent of K-1 and all stand."

The audit also names WHY the generalization was guaranteed to look true — the
selection of sites is not neutral. Quoted whole ([DVP-A] `:500-508`):

> "THE SELECTION IS NOT NEUTRAL — this is the root of the defect.  T14SR §4 is headed "THE
>   JUNCTION ITSELF (deliverable 3): TERMINATION SITES".  J-1 ... J-6 are BY DEFINITION the
>   sites where a forcing chain DIES.  A pattern of the form "require fixes a set, allow
>   takes the selection" read off three termination sites is GUARANTEED BY THE SELECTION OF
>   SITES, not derived from the partition.  Three of the six are not displayed (J-3, J-4,
>   J-6), and J-4's require content is a point.  What the target has actually shown is:
>   AT TERMINATION SITES, the require half fixes an admissible set and the allow half takes
>   the selection within it — which is close to the survey's definition of a termination
>   site, and is a much smaller claim than "what the partition is tracking"."

### §3.5 A LIVE DEFECT OF RECORD: THE KILLED CLAIM IS STILL BEING CARRIED AS
### CORROBORATION, BY TWO ARTIFACTS, ONE OF WHICH HAD THE KILL IN ITS GROUND

This is reported because it is exactly the kind of thing a typology commission is
positioned to see, and because the commission's own framing treats the claim as
live. **Both carries are graded here as findings, not repaired.**

```text
CARRY 1 — [THF] §3.4 :489-492 cites [DVP] :508-515 as "CORROBORATED INDEPENDENTLY".
  [THF]'s seal-verified ground list at :135-140 contains
  `STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md [DVP]` and DOES NOT contain its
  audit. But [THF]'s OWN SWEEP RETURNED THE AUDIT. [THF] :356-371 lists the 40
  carriers sweep (S-ii) returned, and line :360 reads, whole:
      "DISCHARGERS_VS_PARTITION_O11SR_AUDIT ; EXTSRC_ROUND3_S9AD (+AUDIT) ;"
  So the artifact whose audit struck the sentence was returned by [THF]'s own
  declared sweep, was not carried into its verified ground, and the struck sentence
  was then cited as independent corroboration. FINDING, not repaired here.
  MITIGATION, STATED BECAUSE IT IS REAL: [THF]'s §3.4 use of the sentence is at a
  site (J-6) where the surviving scoped form and the struck general form agree —
  the magnitude is allow-side there in every model by [PT], independently of the
  claim. Nothing in [THF]'s J-6 finding depends on the struck limb.

CARRY 2 — [CAF-A] §4.2(ii) :796-819 quotes the same [DVP] block whole and then says,
  whole (:813-819):
      "**`never a point on the require side`** is the corpus's own summary of the
      structure the whole commission is probing, and it corroborates the build's Q2
      and Q3 at once. The build's row-by-row termination notes (R-1 "a CLASS, not a
      point"; R-3 "a supplier class"; R-6 "a two-point set"; R-7 "an INTERVAL") assemble
      to exactly it, and the build never states it. **This omission cuts in the
      build's own favour** — which is why it is graded an incompleteness and not a
      break."
  [CAF-A]'s ground list at :50-56 includes the "O11SR pair" — i.e. the audit that
  killed the sentence WAS in its verified ground. Sweep of [CAF-A] for `K-1`,
  `strike`, and `never a point`: 3 lines, all three of them carries of the killed
  sentence (:810, :813, :998); NO line carries the kill. FINDING, not repaired here.
  AND NOTE WHICH ROWS IT ASSEMBLES: R-1, R-3, R-6, R-7 — four rows, none of them a
  POINT row. The six POINT rows of §1 are the four it omits plus two.
```

### §3.6 Q3 VERDICT

```text
STATUS OF THE CLAIM                 OBSERVATION, generalized from three termination
                                    sites; not a theorem, not a sweep result.
SCOPE AS STATED                     the require side of the partition, unrestricted
                                    ("WHAT THE PARTITION IS ACTUALLY TRACKING").
SCOPE AS ITS OWN CAS TAG WRITES IT  the three PAIRED SITES J-1/J-2/J-5, "(3/3)".
VERDICT                             **CONTRADICTED AT BYTES.** Six POINT rows and
                                    two LATTICE/DISCRETE rows of the §1 typology
                                    fall inside the stated scope. Five of the eight
                                    are the corpus's OWN enumerated counterexamples.
STATUS OF RECORD                    ALREADY KILLED. [DVP-A] K-1 ordered the sentence
                                    STRUCK from §4.3 and the §11 NET and replaced by
                                    the scoped display at J-1/J-2/J-5.
WHAT SURVIVES, AND IT IS NOT NOTHING: at J-1, J-2 and J-5 the require half fixes an
  admissible set and the allow half is the selection inside it, and the two halves
  are of the same localizability type. That scoped statement is re-derived and PASSED
  by the audit at A1-A5, B1-B2, C1-C2, D1, I7 — and it is consistent with §1, because
  the require shapes at exactly those three sites are LATTICE (R-1, ℤ), WINDOW (R-6,
  the two-entry menu) and WINDOW (R-7/clock, [1, 3/2]) — none of them a point.
  THE THREE SITES THE CLAIM WAS READ OFF ARE PRECISELY THE THREE WHERE IT HOLDS.
```

---

## §4 — Q4: IS THERE A POINT-CUTTING REQUIRE ANYWHERE NEAR THE MAGNITUDE GROUND?

TAKEN AS GIVEN AND NOT RE-LITIGATED: no require on the magnitude ground reaches the
magnitude; the magnitude is allow-side by theorem ([PT] §2 (II), (III)). Nothing
below re-derives, disturbs, tests, or attempts that. **The question answered here is
strictly typological: what KINDS of require live in that region, and where the
nearest point-cutting or lattice-cutting require sits.** No value is computed, no
member of any admissible set is evaluated, and no route to the magnitude is
described.

### §4.1 THE REGION, DEFINED BY THE CORPUS'S OWN BYTES

```text
THE MAGNITUDE GROUND, as the corpus locates it:
  SITE          J-6, which the partition theorem names in its own title clause:
                "**THE PARTITION THEOREM (rigid sub-region, magnitude site J-6).**"
                ([PT] :146)
  THE OBJECT    the crossing's magnitude — the response profile ([PT] :153-157)
  ITS ALLOW ROW A-1: "the magnitude profile — a **continuum response magnitude** as
                a function of a phase datum" ([DVP] :458, the §4.2 allow table)
  ITS REQUIRE   the row whose chain terminates INTO it: [JCT] §4's J-6 row names it
                in the row header, whole: "| J-6 | R-8 carrier vanishings → the
                magnitude |"                                          ([JCT] :221)

A SECOND MAGNITUDE, SHARING A SITE WITH TWO REQUIRES — carried because the
commission asks about "on, adjacent to, or sharing a carrier with":
  SITE          J-1
  THE OBJECT    the winding magnitude |n| — the site's allow half is written
                "A-3: the choice of \|n\|"                            ([JCT] :216)
  ITS REQUIRES  R-1 (winding/quantization) and R-9 (channel rider), both terminating
                at J-1                                                ([JCT] :216)

THE STRUCTURAL FAMILY BOTH SITES BELONG TO, in the survey's own classification
([JCT] :230-236, quoted whole):
  "CARRIER-EDGE (the chain stops because the record's carrier ends or cannot
   separate): J-2 ("prior completed physical carrier ... no block/direct-sum
   separation survives"); J-3 ("no sealed arrow of this type exists ... the
   sector crossing itself"); J-6 ("no clause in which a response magnitude and
   a phase-sector variable co-occur"; "carrier ... below the forcing
   threshold"); J-1 (continuation requires an EXTERNAL comparand — an object
   the carrier does not hold: "both sealed candidates unavailable")."
```

### §4.2 WHAT KINDS OF REQUIRE LIVE IN THAT REGION

```text
─────────────────────────────────────────────────────────────────────────────
ON THE MAGNITUDE GROUND — the require-chain that terminates INTO the magnitude
─────────────────────────────────────────────────────────────────────────────
R-8 leg 1     **POINT.**  "cross-block vanishes IDENTICALLY, dim H = 0 exact —
              kinematic, member-independent, kernel-independent. Unconditional at
              that stratum"                                        [JCT :137]
              The corpus's own audit types it in one word ([DVP-A] :488-489,
              whole): "R-8  "cross-block vanishes IDENTICALLY, dim H = 0 exact —
              kinematic, member-independent ... Unconditional at that stratum."
              A point."

R-8 leg 2     **POINT, premise-relative.**  "mirror collapse phi_f == 0 (F = 0
              mandated) under the entered Attach TYPE-P premise" — and the row's own
              chain: "forced RELATIVE to a carried TYPE-P premise and voids with it"
                                                                   [JCT :137]

=> THE REQUIRE-CHAIN OF RECORD ON THE MAGNITUDE GROUND IS POINT-CUTTING ON BOTH
   ITS LEGS. It is not a band, not a ray, and not non-numeric.

─────────────────────────────────────────────────────────────────────────────
SHARING THE SAME CARRIER — the cross-block
─────────────────────────────────────────────────────────────────────────────
R-9           **NON-NUMERIC** (a channel: a presence no sector-respecting basis
              removes), with a POINT-shaped rank datum inside it ("irreducible
              rank-one"). Its antecedent is R-8's own object: "a surviving
              cross-block forces the CHANNEL ... NOT the winding magnitude \|n\|"
                                                                   [JCT :138]
              R-8 and R-9 are the two sides of one carrier — the SAME cross-block,
              vanishing in R-8 and surviving in R-9.

─────────────────────────────────────────────────────────────────────────────
AT THE ADJACENT MAGNITUDE SITE J-1
─────────────────────────────────────────────────────────────────────────────
R-1           **LATTICE / DISCRETE.**  "whose admissible set is the integer lattice
              ℤ"; the corpus rules its form "**A CONGRUENCE (LATTICE) CONDITION**"
                                                    [JCT :130 ; THF :418-419]
R-9           **NON-NUMERIC** (as above) — the second require at J-1.
THE ALLOW HALF AT THIS SITE IS A MAGNITUDE: "A-3: the choice of \|n\|" [JCT :216]

─────────────────────────────────────────────────────────────────────────────
ON THE SAME PHASE/CHARGE CARRIER, one step out
─────────────────────────────────────────────────────────────────────────────
R-2           **LATTICE / DISCRETE** (the character lattice), and its corollary
R-2/G         **POINT** — "on-record amplitude uniquely the trivial constant G = 1",
              typed by the survey as "an actual forced value"       [JCT :131]
              The corpus's own separation clause, carried whole so this is not
              over-read: the displayed conjugate pair "closes magnitude-free"
              ([JCT] :131), and [PT] §2 (III)(c) says the class's "output is an
              integer label, not a continuum response magnitude" ([PT] :161-162).
              G is a forced point on the amplitude, not on the crossing's magnitude.
─────────────────────────────────────────────────────────────────────────────
```

### §4.3 WHERE THE NEAREST POINT-CUTTING REQUIRE SITS, AND WHAT SEPARATES IT

```text
DISTANCE: **ZERO SITES.** The nearest point-cutting require is not near the magnitude
  ground — it IS the require-chain of the magnitude ground's own termination site.
  [JCT] §4's J-6 row is headed "R-8 carrier vanishings → the magnitude", and R-8's
  leg 1 is a point.

WHAT SEPARATES THE POINT FROM THE MAGNITUDE — the corpus's own two separators at
J-6, quoted whole from the row ([JCT] :221):
  "forcing ends at the DISPLAYED EQUATION SET'S EDGE: "the ground's displayed
   equation set contains no clause linking any response magnitude to any phase datum
   (CAS M8)"; the Gleason-style rescue dies at the carrier: "fibers of dimension 1
   ... The ground's own carrier is below the forcing threshold" (AJ §6.3.2)"

  SEPARATOR 1  A MISSING CLAUSE — a vocabulary non-co-occurrence. Not a distance in
               any parameter; the absence of a displayed clause in which the two
               vocabularies appear together.
  SEPARATOR 2  A CARRIER SHORTFALL — "fibers of dimension 1", "below the forcing
               threshold". A dimension, not a magnitude.

AND THE THEOREM STATES THE SAME SEPARATOR IN ITS EXCLUSIVITY CLAUSE
([PT] :159-163, whole):
  "**(III) EXCLUSIVITY.** No model of Σ places them otherwise: (a) no model places the
   quantization class allow-side; (b) no model places the magnitude require-side; (c) neither
   side can be re-read as the other — the class's output is an integer label, not a continuum
   response magnitude, and no clause of Σ exists through which a require-chain could reach a
   magnitude."

WHAT SEPARATES THE LATTICE FROM THE ADJACENT MAGNITUDE AT J-1 — a MISSING OBJECT,
quoted whole from the row ([JCT] :216):
  "forcing ends at the INTEGER CLASS and (conditionally) the CHANNEL:
   "integer-quantized by single-valuedness"; "forces the CHANNEL ... NOT the winding
   magnitude \|n\| — n enters ... exactly once, as the global n^2 prefactor of an
   n-blind form"; the forcing would continue only through "an external n-blind
   comparand," and "both sealed candidates unavailable""

  SEPARATOR   AN EXTERNAL COMPARAND THE CARRIER DOES NOT HOLD, with both sealed
              candidates recorded unavailable.

SO, TYPOLOGICALLY: the separators in this region are ALL of one kind — an absence
(a clause not displayed, an object not held, a fiber dimension short of a threshold).
None of them is a band, an interval, or a rate. The point-cutting requires and the
magnitudes are separated by MISSING STRUCTURE, not by distance in a parameter.
```

### §4.4 THE Q4 GRADE

```text
GRADE = **POINT-OR-LATTICE-REQUIRE-PRESENT-IN-REGION**

  CITED, ON THE GROUND ITSELF:
    R-8 leg 1  POINT   "cross-block vanishes IDENTICALLY, dim H = 0 exact —
               kinematic, member-independent, kernel-independent. Unconditional at
               that stratum"  —  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:137,
               the require-chain of J-6, the magnitude site
    R-8 leg 2  POINT (premise-relative)  "mirror collapse phi_f == 0 (F = 0
               mandated) under the entered Attach TYPE-P premise"  —  same line
  CITED, SHARING A CARRIER (the cross-block):
    R-9        NON-NUMERIC channel with a rank-one datum  —  :138
  CITED, AT THE ADJACENT MAGNITUDE SITE J-1:
    R-1        LATTICE / DISCRETE  "the integer lattice ℤ"  —  :130
  CITED, ONE CARRIER OUT:
    R-2        LATTICE / DISCRETE (character lattice)  —  :131
    R-2/G      POINT (the amplitude G), and the corpus's own clause separating it
               from the crossing's magnitude ("closes magnitude-free")  —  :131

  THE COMPETING GRADE, AND WHY IT FAILS: ONLY-BAND-RAY-OR-NONNUMERIC-IN-REGION would
  require that every require on, adjacent to, or sharing a carrier with the magnitude
  ground be WINDOW, RAY, or NON-NUMERIC. It fails on the very first row: the J-6
  require-chain's own leg 1 pins a dimension to exactly zero and the corpus's audit
  calls it "A point."

  THE BAND/RAY/NON-NUMERIC REQUIRES ARE NOT ABSENT FROM THE CORPUS — they are absent
  from THIS REGION. R-6's closed menu sits at J-2; R-7's interval at J-5; MIN-5's
  half-lines at J-8; R-3/W-2a's supplier class at J-3. None of the four is a J-6 or
  J-1 row.

  WHAT THIS DOES NOT SAY, STATED SO IT CANNOT BE MIS-READ: it does not say a require
  reaches the magnitude, does not say one could, does not describe a route, and does
  not disturb [PT]. It says only that the requires living in this region are of the
  POINT and LATTICE kinds, and that what stands between them and the magnitude is a
  missing clause, a missing object, and a short carrier — all of them absences the
  corpus displays as absences.
```

---

## §5 — Q5: THE CLOSURE'S NEUTRALITY CLAUSE

### §5.1 THE CLAUSE, QUOTED WHOLE AT ITS ORIGIN, IN ITS OWN SURROUNDINGS

The clause lives in `STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md` (seal OK) at
`:19-23`. It is quoted here with the four lines above it and the block below it,
because those lines are what fix its scope and they are not part of the sentence
the prior commission quoted.

> "## BLIND BUILDER — CODENAME WBC-BUILD — COMMISSION S9AD — [SEALED]
>
> Date: 2026-08-14
> Role: BLIND builder (WBC-BUILD). Commission: the wall's first untried route
> — derive the n-uniform enclosure of the FOUR definite-signed quantities per
> (n, cell) — tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2 — FROM the sealed
> boundary-closure structure of the record cell (the projector promotion, the
> boundary-supported commutator, the R.2 collapse display, the b_D profile,
> the phase-skeleton saturation, the onset/action budgets), the four
> graveyarded routes having attacked the quantities directly. Derivation
> only. Honest outcome menu, none preferred: DERIVED / PARTIAL / FAILS-AT /
> CARRIERS-NEEDED / NOT-DERIVABLE-TODAY.
>
> Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
> `proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.
>
> NEUTRALITY CLAUSE, held throughout: the standing motivating reading (that
> gravity, as the cell's boundary-closure, is what disciplines these
> quantities) CHOSE this route and is BARRED as ground. No step below reads
> "the closure must bound it"; every step derives from sealed bytes, and the
> failures below register as results.
>
> Fences held: EXACT SYMBOLIC ONLY — one CAS battery (sympy 1.14.0, fresh
> venv `wbcvenv` under the session scratchpad), reproduced verbatim in §7
> with output verbatim (8/8 PASS, single run, no pre-final corrections);"

### §5.2 ITS EXACT SCOPE, READ OFF ITS OWN WORDS

```text
(1) DOCUMENT SCOPE — ONE ARTIFACT.
  "held throughout" and "No step BELOW" and "the failures BELOW". "Below" is
  positional: below this line, in this artifact. The clause sits inside WBC-BUILD's
  own opening discipline block, between "Gates:" and "Fences held:" — a block of
  per-artifact self-bindings, each written in the same voice. It is not addressed to
  the corpus, to Σ, to the junction survey, or to any require-site outside itself.
  It binds the steps of commission S9AD.

(2) SUBJECT SCOPE — ONE MOTIVATING READING, ABOUT FOUR NAMED QUANTITIES.
  What is barred is "the standing motivating reading (that gravity, as the cell's
  boundary-closure, is what disciplines these quantities)". "These quantities" is
  fixed four lines above: "the FOUR definite-signed quantities per (n, cell) —
  tr K_H, tr H_A, ||K_H||_2^2, ||H_A||_2^2". The bar names a reading and names its
  four objects.

(3) MODE SCOPE — BARRED **AS GROUND**, AND BARRED **AS A READING OF A STEP**.
  The clause has two limbs and they say the same thing twice:
    LIMB A: the motivating reading "is BARRED as ground."
    LIMB B: "No step below READS "the closure must bound it"; every step DERIVES
            FROM SEALED BYTES."
  Both limbs are about what a step may CITE as its warrant. Neither limb says the
  closure does not bound, cannot bound, or forces nothing. LIMB A bars a reading
  from occupying the ground slot. LIMB B bars a specific sentence from being what a
  step reads, and requires the positive alternative: sealed bytes.

  THE ARTIFACT'S OWN COMPLIANCE LANGUAGE CONFIRMS THIS AND NOT MORE. Its verdict
  line, the relevant clause whole (:48): "**FAILS-AT (the four-quantity enclosure —
  three named steps, each displayed and each a sealed-text ground, none a
  neutrality-clause import)**". "None a neutrality-clause IMPORT" is a provenance
  statement about the three steps' grounds. And at :893, whole: "the NEUTRALITY
  CLAUSE held — the motivating reading". Its audit says the same, whole
  (STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md :32, the clause inside the
  verdict): "the neutrality clause is HELD (grep + full-read: thesis language occurs
  only in the clause header and compliance declarations; F1/F2/F3 each rest on
  sealed text)". The test applied of record is a TEXT test — where thesis language
  occurs — not a test of whether the closure forces.

(4) THE DECISIVE INTERNAL EVIDENCE THAT IT DOES NOT BAR CLOSURE-DERIVED FORCING:
  THE SAME ARTIFACT, BELOW THE CLAUSE, DERIVES BOUNDS FROM THE CLOSURE. Its verdict
  line, whole (:48): "the boundary-closure structure DOES yield an n-UNIFORM,
  branch-uniform, cell-per-unit OPERATOR-grade enclosure of both parity sectors —
  ||K_H,n||_op <= 2 and ||H_A,n||_op <= 2 EXACTLY, from four sealed closure facts
  alone". And its audit at :285, whole: "neutrality clause: the boundary-closure
  structure yields op-grade". A clause that barred the closure from bounding
  anything would have been violated by the artifact's own headline result. It was
  not violated; the audit checked and found it HELD. **Therefore what is barred is
  the bare appeal, not the closure's content.**

(5) THE DEVICE IS PER-ARTIFACT, AND THE CORPUS USES IT MORE THAN ONCE WITH A
    DIFFERENT BARRED READING EACH TIME.
  Sweep `NEUTRALITY CLAUSE`, root 1, register-bar exclusions: 17 lines in 7 files.
  A SECOND, INDEPENDENT INSTANCE bars a DIFFERENT reading:
  `STAGE8_RATE_GAP_LINKAGE_S9AD_V001.md` (seal OK) :28-32, whole:
    "NEUTRALITY CLAUSE honored: the prior "one object"
     reading was consumed as input NOWHERE (the supervision-root briefing/plan
     files that carry it were never opened — filename-level sweep only);
     SAME-from-vocabulary was treated as the named hazard throughout; every
     linkage claim below rests on a BOOKED chain at bytes."
  and :475-477, whole: "and NEVER OPENED (neutrality clause — the barred prior
     reading lives there); the only supervision-root object consumed is the adopted
     clause, and only via r-2/r-3's sealed verbatim quotes (ledger CH-5)."
  Same device, different artifact, different barred reading, same mode: a reading
  barred from being consumed as input/ground. This is a LANE DISCIPLINE, written
  fresh per commission — not a standing law of the corpus.
```

### §5.3 THE DISTINCTION THE COMMISSION ASKS FOR, ANSWERED DIRECTLY

```text
Q: Does it bar the closure from forcing at ALL require-sites, or only within one
   derivation / one artifact / one mode of argument?
A: **ONE ARTIFACT AND ONE MODE.** It binds the steps of commission S9AD
   (WBC-BUILD), about four named quantities, and it binds them as to what they may
   read as ground. It reaches no require-site of the junction inventory, no clause
   of Σ, and no artifact but its own. There is no byte in it addressed to the
   require side, to the partition, or to any of the fourteen sites.

Q: Does it bar the closure from FORCING at all, or only from being CITED as the
   forcer?
A: **ONLY FROM BEING CITED — and more precisely, only from being cited BARE.** The
   barred string is "the closure must bound it": an assertion of discipline with no
   content behind it. The positive requirement in the same sentence — "every step
   derives from sealed bytes" — is what the closure's own content satisfies, and
   the artifact goes on to derive an operator-grade enclosure "from four sealed
   closure facts alone" below the clause, with its audit certifying the clause HELD.
   The clause is a provenance rule about warrants, not a claim about forcing power.
```

### §5.4 WHAT THIS DOES AND DOES NOT DO TO "CLOSURE-SUPPLIED = 0"

Reported precisely, because the prior commission leaned on the clause and its audit
upheld the count on different evidence.

```text
THE PRIOR COMMISSION'S USE, quoted whole ([CAF] :842-848 and :863-866):
  "  ZERO require-sites are CLOSURE-SUPPLIED. The boundary-closure's own text
     BARS that mode of forcing in a standing clause (§3.1). Every forcing that
     is attributed at all is attributed to CONTENT the record carries, and the
     content is named in every row below. The unattributed residue is
     unattributed AS TO CONTENT — in each case the row names a supplier that is
     neither the closure nor record-carried structure, and that supplier is
     named in the row."
  "**"No step below reads 'the closure must bound it'" is, at bytes, a refusal of
   closure-supplied forcing inside the boundary-closure itself.** Consistently with
   it, the closure's own two require sentences both appeal to content, not to a
   closure clause:"

READ AT BYTES: the second sentence is EXACT — "inside the boundary-closure itself"
  is precisely the clause's document scope, and the prior commission stated it
  correctly there. The first sentence calls it "a standing clause", and at bytes the
  clause is not standing: it is one artifact's self-binding, written in that
  artifact's discipline block, and the same device is written afresh with a
  different barred reading in another artifact (§5.2(5)).

WHAT CARRIES THE COUNT INSTEAD, AND IT CARRIES IT WITHOUT THE CLAUSE: the audit
  established CLOSURE-SUPPLIED = 0 ROW BY ROW, not from the clause. [CAF-A] §3.0,
  whole (:532-540):
    "WHAT SURVIVED: I re-read all ten require-side rows at their own bytes
      (T14SR :130-139) and matched every one of the build's row quotations against
      them. NINE OF TEN ARE EXACT. The content the build names in each
      CONTENT-SUPPLIED row is genuinely appealed to by the clause, not merely
      mentioned in passing — I checked this row by row (§3.2). I found NO MISSED
      require-site: T14SR §2 carries exactly ten rows plus the boundary note; §3
      carries thirteen allow rows A-1..A-13; W-2a is a genuine additional
      require-side site and it is genuinely SUPPLIED. CLOSURE-SUPPLIED = 0 is
      correct and the NEUTRALITY CLAUSE is quoted whole and accurately."
  The audit's warrant for the count is the row-by-row re-derivation; its statement
  about the clause is that the QUOTATION is whole and accurate — which it is. The
  count does not depend on the clause's scope, and this commission disturbs neither.

Q5 SCOPE FINDING, IN ONE LINE: **the clause bars ONE ARTIFACT's steps from citing a
  named motivating reading as their ground; it does not bar the closure from
  forcing anywhere, and it says nothing at all about the fourteen require-sites.**
```

---

## §6 — CHOICE LEDGER (commission O39SR; every choice that could have gone otherwise)

```text
CH-1  THE SITE SET. I typed the FOURTEEN-site set the O37SR audit fixed (COR-F),
      not the ten T14SR rows alone, and I reported TWO tallies — by site and by row
      — rather than choosing one. Alternative not taken: report only by row, which
      would silently double-count R-8 and R-2 against the corpus's own site count;
      or only by site, which would hide a POINT inside R-2 and a POINT and a WINDOW
      inside R-7. Both tallies are displayed so neither choice is load-bearing.

CH-2  R-6's CLASS: WINDOW, NOT LATTICE. The corpus's governing words are `menu`,
      `CLOSED`, `EXACTLY TWO ENTRIES`, `a TWO-POINT admissible set`, and this
      commission's WINDOW class explicitly covers "a closed menu of admissible
      values". A two-entry integer set ALSO satisfies the LATTICE definition. THE
      DUAL READING IS DISPLAYED IN THE ROW, and §4 counts R-6 under neither heading
      (it is a J-2 row, outside the magnitude region), so NO GRADE IN THIS ARTIFACT
      DEPENDS ON THIS CHOICE.

CH-3  R-9's CLASS: NON-NUMERIC, NOT POINT. The row's own named forced object is THE
      CHANNEL — a presence no sector-respecting basis removes. Its `irreducible
      rank-one` datum would be POINT-shaped read as a constraint on the rank, and
      that reading is recorded in the row as a cross-cut. Alternative not taken:
      grade R-9 POINT, which would have STRENGTHENED §4's grade. I took the weaker
      reading against my own §4 finding, because the row's text names a channel.

CH-4  R-7 SPLIT INTO FOUR RUNGS. Licensed by the row's own SHAPE cell, "SHAPE-1
      each (single require, unique root/conclusion)". Alternative not taken: one
      class for the whole row — which would have concealed a POINT (p = −3) and a
      WINDOW ([1, 3/2]) inside a single label.

CH-5  R-7/T4SR GRADED INDETERMINATE-AT-BYTES, NOT LATTICE. A trace-parity condition
      is congruence-shaped in ordinary usage. The row displays only the lemma's
      TITLE and its GRADE ("one-vertex trace-parity lemma DERIVED") — no condition,
      no variable, no admissible set. Supplying the shape would be the exact
      gloss-beyond-bytes that [CAF-A] COR-L struck at R-1. REFUSED.

CH-6  MIN-5 GRADED RAY FROM J-8's OWN STOP LANGUAGE, WITHOUT READING BEHIND THE
      BOUNDARY NOTE. The five minimality carriers were not opened; the shape is read
      only from the termination row's displayed quotes ("leaves a half-line", "lower
      bound", "floor"). Alternative not taken: open the five, which is outside this
      commission's declared sweep and was outside [CAF]'s (its CUTOFF-3, and
      [CAF-A] CH-6 records the resulting possible understatement).

CH-7  MIX-1 GRADED INDETERMINATE-AT-BYTES. Following the boundary note's routing to
      R-2 would make it LATTICE. [JCT-A] F-5 found that routing "an unchained
      cross-artifact identification". I did not use an identification the inventory's
      own audit unchained.

CH-8  Q3 TESTED AT THE SCOPE THE PROSE ASSERTS, NOT AT THE CAS TAG'S. The claim has
      two scopes in one file (§3.2). I tested it at the unrestricted scope its prose
      and verdict block assert, because that is what the sentence says. AT THE CAS
      TAG'S SCOPE (J-1/J-2/J-5, "3/3") THE CLAIM SURVIVES, and §3.6 says so and
      shows why: the require shapes at exactly those three sites are LATTICE, WINDOW
      and WINDOW. Both scopes and both outcomes are displayed.

CH-9  Q2 REPORTED AGAINST THE COMMISSION'S OWN STATED PREMISE. The commission states
      the exact-shape count as "at least five". At bytes it is ONE, and the "1 -> at
      least 5" correction of record (COR-J) is about a different count entirely.
      Alternative not taken: accept the premise and assemble four further sites from
      the threshold inventory by relaxing "same named content". REFUSED — that would
      have manufactured the answer the commission expected.

CH-10 Q4's REGION DEFINED AS J-6 + J-1 + THE SHARED CROSS-BLOCK CARRIER. J-6 is
      [PT]'s own "magnitude site"; J-1 carries the winding magnitude |n| as its
      allow half; the cross-block is literally the same object in R-8 and R-9. A
      NARROWER definition (J-6 alone) leaves the grade UNCHANGED — R-8 leg 1 decides
      it by itself. A wider one was not taken.

CH-11 CARRIED GRADE ONLY FOR THE INNER CARRIERS. [EXT], [ORIG], the AJ pair, the 5d,
      the JD build, T17SR, T1SR, O6SR/O8SR were NOT opened. One grade pays for this
      directly and is marked so: TH-1's supply grade is CANNOT-DETERMINE because
      where the record's own stage dimension sits is not in the bytes I consumed.

CH-12 THE TWO §3.5 CARRIES REPORTED AS FINDINGS, NOT REPAIRED. No correction is
      authored, no replacement sentence drafted, no artifact edited. Describing a
      gap is permitted; filling one is not.

CH-13 SWEEP COUNTS REPORTED WITH THIS COMMISSION'S OWN OUTPUT EXCLUDED. Once this
      artifact existed on disk it began returning its own hits. Both figures are in
      §7 so the exclusion is checkable and nothing is hidden by it.

CH-14 I OPENED ONE FILE PURELY TO DISPOSE OF A SWEEP HIT. `STAGE8_FORCING_NOTION_
      O12SR_V001.md` returned ten `| R-` row headers and could have been a second
      require inventory. It is not: it grades the SAME ten T14SR rows (its :148-157,
      its own §0 consuming T14SR at :26-27). Opened, sealed, disposed, recorded.
```

---

## §7 — TOY_SEPARATION

```text
ACTUAL SURFACE — the object of this determination, all of it sealed corpus content:
  · the fourteen require-sites of record and the admissible set each one's own text
    displays;
  · the nine threshold sites, the TH-3/TH-4 WINDOW, and the two displayed
    non-thresholds (TH-0, J-6);
  · the "never a point" claim at its three originating bytes, its four carries, and
    its adjudication (K-1 [KILL]);
  · the neutrality clause at its origin bytes, its compliance declarations, its
    audit's HELD finding, and the corpus's second, independent instance of the
    device.

NOT ACTUAL SURFACE, AND NOT PRESENT IN THIS ARTIFACT AT ALL:
  · NO toy. NO model. NO illustrative instance. NO worked example. NO schematic.
  · NO candidate clause, NO drafted principle, NO proposed repair, NO replacement
    sentence for the struck claim, NO supplied statement for the trace-parity lemma,
    NO content for the five minimality bounds, NO routing for the mixed site.
  · NO numeric evaluation of any admissible set's member. Every integer that appears
    (0, 1, 2, 3, −3, 4, {1,2}, [1, 3/2], q = 2, ℤ) is a STRUCTURAL INDEX quoted from
    a sealed display — a rank, a dimension, a degree, a menu size, a bound — and
    each appears inside a quotation of the bytes that carry it.

THE CLASSIFICATION VOCABULARY IS A LABEL SET, NOT AN OBJECT. POINT / LATTICE /
  WINDOW / RAY / NON-NUMERIC name shapes; they add nothing to the corpus, change no
  artifact, and every row states its class WITH the corpus quote that decides it. If
  every label in §1 were deleted, the quoted sets would still say what they cut.
```

---

## §8 — IMPORT AUDIT

```text
IMP-1  `holonomy` / `monodromy` / `closed cycle` at R-1 — **NOT USED.**
  These are the prior build's gloss, struck by [CAF-A] COR-L (:1028-1041) on the
  ground that R-1's own bytes contain only `single-valuedness`, `single-valued
  closure`, `integer-quantized`. This artifact uses the corpus's words and does not
  reinstate the gloss anywhere. The word `holonomy` appears in this artifact ONLY
  inside quotations of the boundary note and of [CAF]'s own §3.3/§3.5 blocks, where
  it is the quoted text's word about the MIXED SITE, a different object.
  SURVIVAL: R-1's LATTICE grade rests on `the integer lattice ℤ` and on [THF]'s
  `CONGRUENCE (LATTICE) CONDITION`. It survives entirely without the import.

IMP-2  THE FIVE CLASS NAMES — supplied by the COMMISSION, not by the corpus.
  FOUR OF THE FIVE HAVE CORPUS-NATIVE ANCHORS, quoted in the rows that use them:
    POINT            "the slice is a point of the lattice of subspaces" [JCT :134];
                     "an actual forced value" [JCT :131]; "A point." [DVP-A :489]
    LATTICE/DISCRETE "the integer lattice ℤ" [JCT :130]; "**THIS IS A CONGRUENCE
                     (LATTICE) CONDITION**" [THF :418-419]
    WINDOW/BAND      "menu", "CLOSED", "EXACTLY TWO ENTRIES" [THF :398-403]; "an
                     INTERVAL, not a point" [JCT :136]; "a WINDOW in two structural
                     quantities" [THF :281-282]
    RAY/HALF-LINE    "a strict untrapped inequality leaves a half-line" [JCT :223]
  ONE IS PURELY THE COMMISSION'S: NON-NUMERIC. Where it is used, the row states what
  the require constrains instead (a supplier class, a channel, a display standard),
  in the corpus's words.
  SURVIVAL: yes. Strip every label and each row still quotes the set.

IMP-3  `congruence` — CORPUS-NATIVE, not an import. [THF] :418 writes it in capitals
  as its own form ruling. Sweep `congruence`, root 1, own output excluded: 114 lines
  in 54 files.

IMP-4  Q4's LOCATIONAL VOCABULARY — `region`, `adjacent`, `sharing a carrier` —
  supplied by the commission. Each is discharged into a corpus object before use:
    region          -> the site J-6, which [PT] :146 names "magnitude site J-6", plus
                       the site J-1, whose allow half [JCT] :216 writes "A-3: the
                       choice of |n|"
    sharing a carrier -> the CROSS-BLOCK, which is literally the same object in R-8
                       ("cross-block vanishes IDENTICALLY") and R-9 ("a surviving
                       cross-block")
    adjacent        -> the survey's OWN cluster: [JCT] :230-236 places J-1, J-2, J-3
                       and J-6 in one family, CARRIER-EDGE
  SURVIVAL: yes. §4.4's citations are all site-and-row identifications the junction
  survey itself makes; the words are shorthand for them.

IMP-5  NO PHYSICS IMPORT. No coupling, no scale, no constant, no measured quantity,
  no interpretation. The word `gravity` occurs in this artifact five times and NOT
  ONCE as a term of this lane's own — counted, not asserted: twice inside the
  filesystem path of corpus root 2 (§0.1, §9); once inside the quoted neutrality
  clause (§5.1), where it is the corpus's own word naming the reading that clause
  BARS; once in §5.2 re-quoting that same clause phrase to fix its subject scope;
  and once here, counting them. No lens reading is named, used, or motivated
  anywhere in this artifact.

IMP-6  THE FORCING CRITERION `the admissible set is a point` is corpus-native, from
  the forcing-boundary adjudication as [DVP] §1 consumes it. It is quoted, not
  applied: this commission classifies shapes, and does not grade anything FORCED.
```

---

## §9 — SWEEP CUTOFFS (every pattern WITH the hit count actually obtained)

```text
CUTOFF: 2026-08-15, this session, the bytes as sealed at read time. Every
exhaustiveness sentence in this artifact is sweep-relative and key-relative.
Nothing absolute is claimed.

EXCLUSION SET APPLIED TO EVERY SWEEP BELOW, verbatim:
  !*REGISTER*  !*EXECUTION_TRACKER*  !*THE_PLAN*  !*ROAD_REMAINING*
  !*THE_HANDOFF*  !OBSERVATIONS_REGISTER*  !*DECISION_SHEET*  !*_ADDENDUM*
  !alpha_supervision/**       and, for the reported figures, !this artifact
All patterns case-insensitive.

ROOT 1 = /Users/bgm/MB Work/alpha-program-archive/workspace
ROOT 2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
         alpha_fundamental_record_action_cleanroom_v003

──────────────────────────── ROOT 1 ────────────────────────────
PATTERN                          FILES   LINES   DISPOSITION
congruence                          54     114   A
never a point                        5      14   B  — every hit enumerated at §3.1
closure must bound it                2       3   C  — [WBC] :22 and [CAF] :860,:863
derives from sealed bytes            2       2   C  — same two carriers
admissible set is a point           11      28   D
not a point                         28      47   D
half-line                           45      97   D
two-point                          108     335   D
NEUTRALITY CLAUSE                    7      17   E  — all 17 displayed at §5.2(5)
                                                       inspection; 2 independent
                                                       instances, 5 commentaries
permitted below                      3       7   B  — all 7 enumerated at §2.0
required above                       5       7   A
lattice condition                   12      12   D
require-side inventory               3       3   B  — all 3 enumerated at §1.0
a point of the lattice               2       2   B  — [JCT] :134, [DVP-A] :487
forced content of record             2       5   A
require of record                    1       1   A
requires of record                   0       0   —
divisibility                         6       9   D
integrality                         29     144   D
quantization                        84     182   D

──────────────────────────── ROOT 2 ────────────────────────────
PATTERN                          FILES   LINES   DISPOSITION
congruence                          35      66   A  — all are Gate-3 "modulo overall
                                                       congruence" / Keldysh
                                                       congruence, a different object
never a point                        0       0   negative
closure must bound it                0       0   negative
derives from sealed bytes            0       0   negative
admissible set is a point            0       0   negative
NEUTRALITY CLAUSE                    1       1   inspected: TASK5 XI_N review, "partial
                                                 raw-neutrality clause" — different object
permitted below                      0       0   negative
require-side inventory               0       0   negative
ALLOW/REQUIRE                       69     143   A — no inventory, no side-assignment
                                                     apparatus, no shape claim

DISPOSITIONS, DEFINED:
  A = counted, not individually read; no claim in this artifact rests on any
      unread hit.
  B = every hit individually inspected and displayed in the body.
  C = every hit individually inspected; the pattern is the Q5 subject.
  D = counted, then DISPOSED BY AN EXECUTED CROSS-CHECK, not by assertion — see
      the cross-check block below.

──────────── THE EXECUTED CROSS-CHECK BEHIND DISPOSITION D ────────────
The risk these large returns carry is that a require of record sits outside the
inventory. I tested it rather than asserting it. For each D-pattern I intersected
its file set with the set of files carrying an allow/require SIDE ASSIGNMENT
(`require-side` or `allow-side`), then removed the files already consumed here.
RESULT, actually obtained:

  PATTERN                     FILES  ALSO SIDE-ASSIGNING  OF THOSE, NOT CONSUMED
  admissible set is a point      11          8                     3
  not a point                    28          7                     2
  half-line                      45          5                     0
  two-point                     108          6                     1
  lattice condition              12          1                     0
  quantization                   84         11                     5
  divisibility                    6          0                     0
  integrality                    29          0                     0

POOLED, THE NOT-CONSUMED SIDE-ASSIGNING FILES ARE SIX, AND ALL SIX WERE OPENED AT
THEIR HEADERS AND COUNTED FOR REQUIRE-ROW STRUCTURE:
  STAGE8_CONSTRAIN_OR_CREATE_O36SR_AUDIT_V001.md     0 `| R-` rows — PRODUCER vs
                                                     CONSTRAINT reading; different object
  STAGE8_COUNTERFACTUAL_PROFILE_O35SR_V001.md        0 `| R-` rows — present/absent
                                                     structural profile; different object
  STAGE8_EM_PARTICIPATION_O4SR_V001.md               0 `| R-` rows — EM participation
                                                     in the closure's mechanism
  STAGE8_FORCING_NOTION_O12SR_AUDIT_V001.md          0 `| R-` rows — audit of the below
  STAGE8_FORCING_NOTION_O12SR_V001.md               10 `| R-` rows — **OPENED AND SEALED
     (§0). ITS TEN ROWS ARE THE SAME TEN**, at :148-157, over T14SR consumed at its own
     §0 :26-27. It grades their forcing NOTION (MT/PT/CA/DG); it is not a second
     inventory and adds no site.
  STAGE8_U1REL_EM_IDENTIFICATION_O14SR_V001.md       0 `| R-` rows — the U(1)_rel/EM
                                                     identification as an object

=> NO ELEVENTH REQUIRE ROW AND NO FIFTEENTH REQUIRE-SITE WAS RETURNED BY ANY SWEEP
   RUN HERE. That is this lane's independent confirmation of [CAF-A] :1072's "No
   missed require-site found", reached by a different route.

WHAT THESE TERMS WOULD NOT RETURN, STATED SO THE NEGATIVE IS BOUNDED: a require
  written with no shape vocabulary and no side-assignment vocabulary anywhere in its
  file. A natural-language sweep over 3671 root-1 entries would not have terminated
  in this commission and would have returned unbounded noise. THE LIMIT IS THE
  SWEEP'S AND IT IS NOT HIDDEN.
```

---

## §10 — FLAG BLOCK

```text
FENCE LINE, HELD AND RE-CHECKED AGAINST THE FINISHED TEXT:
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
  No coupling, scale, root, eigenvalue, norm, or constant is computed, approximated,
  bounded, or compared to any measured value. Every integer in this artifact is a
  quoted structural index. The one COUPLING-EXPRESSIBLE row in the ground ([THF]
  §2.4, Axis B) is displayed at its marker and NOT gone behind — see §2.1, TH-4.

SEAL TALLY: 15/15 OK, every check run with `shasum -a 256 -c` FROM
  /Users/bgm/MB Work/alpha-program-archive/workspace (the artifacts' own directory).
  MISMATCHES = 0. NONE UNVERIFIABLE.

REGISTER-BAR INCIDENTS: **NONE.** No file matching QUESTIONS_SETTLED_REGISTER*,
  QUESTIONSSETTLED_REGISTER*, EXECUTION_TRACKER*, THE_PLAN*, ROAD_REMAINING*,
  THE_HANDOFF*, OBSERVATIONS_REGISTER*, *DECISION_SHEET*, *_ADDENDUM*, or anything
  under alpha_supervision/ was opened, listed, grepped, or cited. The named
  QUESTIONSSETTLED_REGISTER_V001.md never appeared in any sweep return, because the
  exclusion set was applied to every sweep at invocation, not after.

NO GIT ACTION. No existing file edited. ONE output file plus its two seal sidecars.

DEFECTS FOUND IN MY OWN DRAFT ON SELF-CHECK, EACH CORRECTED BEFORE SEALING:
  D-1  §0.1 first draft reported root-2 `ALLOW/REQUIRE` as "181 lines in 72 files".
       That figure came from a sweep whose exclusion globs were passed unquoted and
       word-split by the shell. Re-run with a properly quoted exclusion array:
       143 lines in 69 files. CORRECTED. Every figure in §9 is from the re-run.
  D-2  §3.1 first draft wrote `never a point`: "14 lines in 6 files". The correct
       file count is 5; my own enumeration in the same block listed 5 files
       (3+1+1+3+6 = 14 lines). The "6" was an unchecked carry from a run that
       included this artifact itself. CORRECTED.
  D-3  §0's seal block first listed 12 artifacts while §5 quoted [WBC-A] and [RGL]
       and §9 disposed [FN]. All three verified and added; tally corrected 12 -> 15.
  D-4  IMP-5 first draft asserted "the word `gravity` occurs in this artifact exactly
       once". I counted it instead of asserting it: it occurs FIVE times — twice in
       corpus root 2's filesystem path, twice in and about the quoted neutrality
       clause, once in the count itself. The substance (no physics import, the word
       is never this lane's own term) is unchanged; the count was wrong and is now
       displayed. CORRECTED. This is precisely the defect class the artifact
       criticises in §3.5 and §2.0 — an unchecked count in prose — found in my own
       draft, and I record it rather than quietly fixing it.
  NONE of the four changed a grade, a tally by class, or a verdict.

HEADLINE-AGAINST-SWEEP RECONCILIATION, run before sealing, claim by claim:
  CLAIM: "the require side carries BOTH point-cutting and band/ray/non-numeric
    requires."  CHECKED AGAINST: §1's fourteen rows, each quoted from
    T14SR :130-147 and :223 and [CAF] :1059-1073.  CONSISTENT.
  CLAIM: "'never a point' is contradicted at bytes."  CHECKED AGAINST: the `never a
    point` sweep, 14 lines / 5 files — of which SIX LINES ARE THE KILL ITSELF
    ([DVP-A] :462, :473, :525, :705, :706, :879). The sweep does not merely permit
    the finding; six of its fourteen lines ARE the finding.  CONSISTENT.
  CLAIM: "the exact-shape count is ONE."  CHECKED AGAINST: `permitted below`, 7
    lines / 3 files, all seven enumerated at §2.0, all seven about J-2.  CONSISTENT.
  CLAIM: "POINT-OR-LATTICE-REQUIRE-PRESENT-IN-REGION."  CHECKED AGAINST: T14SR :137
    (R-8's two legs) and :221 (the J-6 row naming R-8 as the magnitude site's
    chain).  CONSISTENT — and it does not depend on CH-2 or CH-3, the two class
    choices that could have gone the other way.
  CLAIM: "the neutrality clause is one-artifact scoped."  CHECKED AGAINST:
    `NEUTRALITY CLAUSE`, 17 lines / 7 files, all inspected — TWO independent
    instances written in two different artifacts barring two different readings.
    CONSISTENT.
  NO DISAGREEMENT FOUND between any headline and any sweep result.

QUOTATION INTEGRITY, RE-CHECKED AGAINST BYTES BEFORE SEALING — METHOD AND RESULT:
  METHOD, executed, not asserted: (i) 30 load-bearing [JCT] spans checked as EXACT
  fixed strings with `rg -F` against the file — 30/30 OK, 0 FAIL; (ii) 27 [THF] and
  13 [DVP]/[DVP-A] spans checked the same way — 38/40 OK exact, and BOTH apparent
  failures traced to source line-wrapping and then confirmed present, so 40/40 by
  content; (iii) 41 further spans across [CAF], [CAF-A], [PT], [JCT-A], [WBC],
  [WBC-A], [RGL], [GS-A] checked whitespace-normalized — 41/41 OK, 0 FAIL; (iv) the
  §5.1 [WBC] header block diffed LINE FOR LINE against source lines 3-27 —
  25 lines, 0 differences, blockquote markers excepted.
  TOTAL: 111 spans checked, 0 content failures.

  ELISIONS. No elision made by this lane. THREE ellipses appear inside quoted
  material and ALL THREE ARE THE CORPUS'S OWN, each marked where it occurs:
    · [THF] :491-492's ellipsis inside its own [DVP] citation — flagged at §2.1's
      J-6 block, and the text it removes is displayed WHOLE at §3.1;
    · [DVP-A] :474's ellipsis inside its quotation of the claim it audits — the
      full claim is displayed immediately above it at §3.1;
    · [JCT] :221's ellipsis inside its own citation of AJ §6.3.2 ("fibers of
      dimension 1 ... The ground's own carrier is below the forcing threshold") —
      quoted at §4.3 exactly as [JCT] carries it, and declared here.

  LINE-JOINS AND RE-INDENTATION, declared because they are transforms even though
  they are not elisions: two quotations re-join or re-indent the source's own line
  breaks — [DVP-A] :522-523 at §1.1's R-7/clock row (joined to one line), and
  [THF] :489-492 at §2.1's J-6 block (re-indented one level because it is nested
  inside a quotation of [THF]'s own block). Both were verified content-identical
  whitespace-normalized. Every other quotation preserves the source's line breaks.

  ADVERSE SPANS. Spans that cut AGAINST this artifact's findings are carried in
  full and marked "carried because it cuts against" — at R-4 (the dropped
  non-vanishing conjunct), R-5 (F-4), R-9 (the rank cross-cut), R-6 (the dual
  reading), TH-4 (the consequence undetermined), ONSET-A/ONSET-B (declared lane
  readings), §3.5 (the mitigation for CARRY 1), §3.6 (the scope at which the
  killed claim SURVIVES), and §5.4 (what the prior commission got exactly right).

WHAT THIS ARTIFACT DOES NOT DO: it adopts nothing, advocates nothing, recommends
  nothing, retires nothing, re-reads no catalogued negative, authors no principle,
  clause, or object, repairs no defect it reports, and fills no gap it describes.
  Where the bytes do not decide it says INDETERMINATE-AT-BYTES, and it says so
  three times (R-7/T4SR, MIX-1, and TH-1's supply grade as CANNOT-DETERMINE).
```

---

## §11 — THE FIVE ANSWERS IN ONE BLOCK

```text
Q1  TYPOLOGY.  BY SITE (14): POINT 4 · LATTICE/DISCRETE 2 · WINDOW/BAND 1 ·
    RAY/HALF-LINE 1 · NON-NUMERIC 4 · MIXED 1 (R-7) · INDETERMINATE 1.
    BY ROW (18): POINT 6 · LATTICE/DISCRETE 2 · WINDOW/BAND 2 · RAY/HALF-LINE 1 ·
    NON-NUMERIC 5 · INDETERMINATE 2.
    THE KINDS ARE NOT UNIFORM. The corpus's require side contains congruence
    conditions that cut lattices, exact identities that cut single points, a closed
    menu, a closed interval, half-lines, and requires that cut no set of values at
    all — and what a require can deliver depends on which it is.

Q2  THRESHOLD SITES.  NINE enumerated, plus one joint WINDOW region and two
    displayed non-thresholds. SITES WITH THE EXACT SHAPE (same named content
    permitted below, required at or above): **ONE** — TH-2 / J-2, attaching to R-6,
    and it is UNSUPPLIED. Grades: SUPPLIED 2 (TH-3, ONSET-B) · SUPPLIED-with-
    consequence-undetermined 1 (TH-4) · UNSUPPLIED 5 · CANNOT-DETERMINE 1 (TH-1).
    The commission's "at least five" is a misattribution of COR-J, which corrects a
    different count (§2.0).

Q3  "NEVER A POINT".  An OBSERVATION generalized from three termination sites, not a
    theorem and not a sweep result. Stated scope: the require side of the partition,
    unrestricted. **CONTRADICTED AT BYTES** — six POINT rows and two LATTICE rows
    fall inside it, five of them the corpus's own enumerated counterexamples. The
    corpus already KILLED it (K-1) and ordered it struck; two downstream artifacts
    still carry it as corroboration, one of them with the kill in its own verified
    ground (§3.5). What survives is the scoped form at J-1/J-2/J-5, where the
    require shapes are LATTICE, WINDOW and WINDOW — and there it holds.

Q4  THE MAGNITUDE REGION.  **POINT-OR-LATTICE-REQUIRE-PRESENT-IN-REGION.** The
    require-chain of the magnitude site J-6 is R-8, and both its legs are POINT
    ("dim H = 0 exact"; "phi_f == 0, F = 0 mandated"). R-9 shares that carrier. At
    the adjacent magnitude site J-1 the require is R-1, a LATTICE (exactly ℤ).
    DISTANCE: zero sites. SEPARATOR: not a distance in any parameter but three
    absences the corpus displays as absences — a clause not displayed (no response
    magnitude and phase datum co-occur), an object not held (no external n-blind
    comparand, both candidates unavailable), and a carrier short of a threshold
    (fibers of dimension 1). Nothing here reaches, or attempts to reach, the
    magnitude.

Q5  THE NEUTRALITY CLAUSE.  Scope is **ONE ARTIFACT AND ONE MODE**: it binds the
    steps of commission S9AD, about four named quantities, as to what they may cite
    as ground. It bars a named motivating reading from being CITED as the forcer —
    not the closure from forcing. The same artifact, below the clause, derives an
    operator-grade enclosure "from four sealed closure facts alone", and its audit
    certifies the clause HELD. The device is per-commission: a second artifact
    writes its own, barring a different reading. CLOSURE-SUPPLIED = 0 does not rest
    on this clause; it rests on the audit's row-by-row re-derivation, and this
    artifact disturbs neither.
```

---

*END OF ARTIFACT. Seal follows in the sidecars.*
