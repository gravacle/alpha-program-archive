# STAGE 8 / ROAD ITEM 6 / OBL-D — THE TRANSPORT PACKAGE'S NEXT PIECE: THE r_F^* (COMMON-REFINEMENT FLUX-PULLBACK) CERTIFICATE

Lane: FABLE blind construction, codename OBD-BUILD (S9AD). One next-obligation
identification at bytes, one exact certificate package built. Date: 2026-08-14.
**ALL_RESULTS = CLAIMED until checked.**

## VERDICT IN ONE LINE

**BUILT — with exact quantifiers.** The transport package's next first-buildable
piece after the d_0-square is NOT the AND-class member-independence certificate —
that was found ALREADY BUILT AND CONFIRMED at path (38bbb9fc, check c0e7192b:
`AND_VERDICT = CONFIRMED(CONSTANT_FALSE)`) — so per the POSED ordering the build
advances to the first-buildable fragment of O7/RA27-4, the obligation OBL-D rides:
**the field-side leg `r_F^*` of the exactly-commuting common-refinement square.**
Delivered exact, per licensed generator (L_id, A0, A1, A2-F, A2-B): the derived
candidate pullback `r*_k := transpose sign pattern of the sealed aggregation sd*_k`
satisfies the SECTION identities `sd*_k r*_k = m_k I` with exact integer
multiplicities; the HORIZONTALITY intertwining `d_0'^T r*_1 = r*_0 d_0^T` holds
entrywise over Z; the induced flux pullback `r_F^* := d_1' r*_1 Q_flux : F_phys(K)
-> F_phys(K')` commutes with the sealed horizontal lift with **ZERO naturality
defect** — `Q'_flux r_F^* = r*_1 Q_flux` exactly — and aggregates back exactly:
`sd*_2 r_F^* = m_1 I`. The honest negative is displayed with exact witnesses: `r*`
is NOT a cochain map on any proper refinement (`r*_1 d_0 != d_0' r*_0` on A1, A2-F,
A2-B), so the naive potential-pullback route to RA27-4's square is DEAD; the
flux-level pullback is the object that works. The full RA27-4 square remains
UNBUILT — its response legs (`eta_resp`, the named response topology/quotient) are
the named blockage — and OBL-D's clause 2 and the full-D3 quantifier are carried
OPEN, not decided.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       EXACT SYMBOLIC ONLY: every number below is a structural integer, an exact
       rank, an exact rational of an elimination, or a sealed-text quotation. No
       physical quantity computed, bounded, or evaluated. No scale, no imported
       GR, no faithfulness. No member of the 1887-dim variety bound; no F-rule,
       J_1 section value, or write chain supplied.
```

---

## 0. Preflight, seals, spans, sweep cutoff

Output name probed BEFORE any write: `STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md`
ABSENT (artifact and sidecar; probed at 18:48:55 CDT and re-probed at 19:18 CDT).
No register, tracker, plan, road, ledger, lens, or THE_HANDOFF file read; "Q-..."
tokens carried as cited inside sealed artifacts only (EXPECTED-UNLOCATABLE). No
git action. Fresh venv (Python 3.9.6, stdlib only) for the one script.

**SWEEP CUTOFF, DECLARED: 2026-08-14 19:18:08 CDT** (first full-corpus sweep
18:55:34 CDT; final re-sweep and path probe 19:18:08 CDT; both roots `workspace/`
and `cleanroom_output/` swept for RA27-4/RA27-6 content, member-independence /
block-class artifacts, `1887` carriers, and any existing pullback/`r_F^*` build —
none exists; parallel S9AD lanes are landing same-night artifacts on OTHER
lineages — CL2-GCM, CARRIER-TRIAGE, HALF-PLANE, WALL-BOUNDARY at 16:33–19:18 —
which are gravity-side, not transport-package; artifacts landing after the cutoff
are outside this sweep by the tasking's own rule.)

Seals — every file below verified by `shasum -a 256` at path BEFORE reliance,
all MATCH (against the adjacent `.seal.sha256` sidecar, and against the tasked
digest where one was given):

```text
22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md            POSED (tasked digest MATCH)
a5c71b2ac7cd198bbac188e865d139a71340f1728f72fe56384aa5d60489110c  STAGE8_TRANSPORT_LAW_POSE_CHECK_V001.md       POSE_CHECK (SOUND-WITH-CORRECTIONS)
bb1b88ad22ad2746a59957936785e9e44e3c2c4c1abdf7cf417096ff982d08c4  STAGE8_D0_SQUARE_CERTIFICATE_V001.md          D0SQ (tasked digest MATCH)
d83655ae50b4203ce42be6fd4fc0a50f03f601c96f55eb3c3afab71807de492e  STAGE8_D0_SQUARE_CHECK_V001.md                D0SQ-CHECK (CONFIRMED)
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md         AND-CLASS (found in sweep; step 2 of record)
c0e7192be7d5d93d74a28d66c9c0e39543f4cb755c222e7b1a9cb7f3b4b72f46  STAGE8_AND_CLASS_CHECK_V001.md                AND-CLASS-CHECK (CONFIRMED)
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md          FORM
9cf9b329bfad1656f91eb75600ca2a60d44853fbe4a1172186beef78e34f1eb9  STAGE8_B1A_CORRECTED_JOINT_SOLVE_CODEX2_V001.md  "807" (the 1887-dim variety)
97f073c101d8cf4a6743660b96e3861e21914ac48877ce538314616b51d70cb6  STAGE8_B1A_REFINEMENT_CARRIER_DARIO_V001.md   CARRIER (sd*_1 law)
614e20c8bfd1978a4273c831b76bd6145483876c975ea87f80ef31a589b8bdc7  STAGE8_B1A_CARRIER_CROSSCHECK_NATURALITY_CODEX2_V001.md  XCHK (censuses; d_1-square 771/0)
590b3979d5a0fadfd570e3a73a13bb3a717d5450f7eb5c9f2e79f481039fc1e2  STAGE8_B1A_COFRAME_HALF_DARIO_V001.md         795 (A0 instance; L_id = id)
4d072e7671113357764b61e11ef1d3d336ce34e307ba07c545ec7cf1a38a6abc  STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md       FR (dim-32 freedom; fields NOT SUPPLIED)
74bbb7aa971554f83d5ce2eb38710b6aae38d340055ab31eca1c23379bc685da  STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md    D012 (the A27 matrix; RA27-4 row; §2.6.3)
9a0c20458c98b27aadeef0b2daee6329a0a6eef93ea78569c0a1e74a5fe20bb9  STAGE8_T7_D3_QUANTIFIER_READING_FREEZE_AND_LEMMA_CONSUMPTION_V001.md  D3-FREEZE (tasked digest MATCH)
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034  STAGE8_G1_KERNEL_CERTIFICATE_V001.md          G1-CERT (OBL-D standing, §4.1)
3534ca534709a3c8ac591994a9ae650224a8594b3f1a8b2a96494a69ab9653e6  STAGE8_G1_KERNEL_CERTIFICATE_CHECK_V001.md    G1-CERT-CHECK (OBL-D CONFIRMED OPEN)
aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a  review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  V011 packet member
```

The V011 two-copy hazard (POSED trap T6) honored: every V011 span binds to the
packet member `aa7c6d49...` ONLY; the same-named top-level file (`20a3a17d...`)
was not read.

Spans consumed, re-extracted from the sealed bytes and rehashed THIS session
(sha256 of the byte span):

```text
V011   [44595,44690)  c6cd568bec678df676737009466e3537dfe64fd6962fdb3200b9f90b9dc14eb5  d_0 / d_1 laws, d_1 d_0 = 0 (matches the D0SQ/AND-CLASS pin)
V011   [46712,47445)  2a19441f8d7d6367dd67e74e6093f2d7cfa62cafbf5384fde2f6d2e6cbe4c0e7  OBL-D: the frozen C_ref class + the two clauses (16-hex prefix matches G1-CERT's pin)
V011   [47025,47247)  9ff6852634e78e6d886896c27bce16b4ec9e092620642a86bae00848df2f276c  the two OBL-D clauses verbatim (matches 807's own pin)
XCHK   [4056,5082)    6ae8c4c440588efe7c0b107da8f003dbc35e24648a4817f76f87e428862ee447  the three refined complexes + per-row aggregation rules (matches the D0SQ pin)
CARRIER[5151,9996)    4eb23669b793b5531388e9c506970c004f4e13e9f62060fa5f01190e9ed20d68  sd*_1 law + direction theorem (matches the D0SQ pin)
795    [7747,9120)    5ed8d194448db5045dc077f177372f7ac81278353c3bd885a1727c0c0dae8cba  A0 instance sigma=(1,0,3,2); L_id = id (matches the D0SQ pin)
POSED  [17736,18509)  f7d3367826ebddb1aadc7b859a5c8aaf7f5747ae2f9bd9626cb481f766ccda98  the O7 / RA27-4 obligation row
POSED  [24873,26213)  9b1f64e7d0ef05c31253cad91a2e0fc49c645304436400c8c0899368c40c5e50  §3 MINIMAL_SUBLAW posing (step 2 of record)
POSED  [38167,38821)  a0bc0a64ee5e2f08b1c570136695d6ebd8fd4b8523c7345cf2c1c68f8bfc9850  flag MINIMAL_SUBLAW
D0SQ   [8320,8960)    0c40d7bdedd7f27e5af81cd1aa15b28856cc42de9e0db89d1a104d45e801bd02  reproduced-vs-sealed census table
D0SQ   [13954,14765)  871db8a30882c087f52010a221ee409df2b5e6fecfd1700f121b595825cc1b5f  §5 yield: block pair well-defined under transport
D012   [45008,45355)  26eaccab4c4c0f33a6ddbe543d1b54be0a36191ac77fca4a004414dd2ad56cf6  the RA27-4 row (exactly commuting square; TYPE-U)
D012   [31536,33632)  3ac02940680001a067cddb3ab9fc8ba96e2d801c575a0d5acbe70c414259bbe7  §2.6.3: the square's shape; r_F^* : F_phys(K) -> F_phys(R); N_r(d_K) = 0
G1CERT [13899,15572)  891f681bcc6fec0bfc718c6f97817c7014d3fda5fbb60a9ae75bcf1df3a071bc  §4.1 OBL-D OPEN; rides RA27-4/RA27-6; f_g/F_g/eta_resp survive
FORM   [8560,8871)    00bd391bc5f8ab515c04284ade72ae81fa62de37469f785a168e65aeacfb5f99  D1 three-block split; Q_flux = d_1^+(d_1 d_1^+)^+ on F_phys
807    [6502,7613)    bb1f17df19affe157dc50f5bbeaa1459dfd8594f517c86346e07035909341555  the variety's variable inventory (S only; no chain-transport variable)
807    [10514,10951)  86a6979caf2f38a8b870a8b28880d109da79e0edd53b14d8e168726e04d54c96  dim = 2784 - 897 = 1887
FR     [10137,10626)  182b75b04f18bd5e2754af5fa2830ddd39a4531885fd55f516675d85e5302223  the 33-free/32-at-fixed-energy assignment freedom; non-coboundary
```

No seal failed. No file matching register|road_|ledger|lens|plan|tracker|
THE_HANDOFF was opened (the stiffness "LEDGER" of the POSED's sources was NOT
read here — its content is carried only as quoted inside the sealed POSED).

---

## 1. SUB-TASK 1 — THE NEXT OBLIGATION, IDENTIFIED AT BYTES

**Step 1 of record (built, confirmed).** POSED flag `FIRST_BUILDABLE` = the
d_0-square; built at bb1b88ad (`D0_SQUARE = CERTIFIED_ALL`), confirmed at
d83655ae. Its yield (D0SQ [13954,14765)): `im(d_0)` transport-stable; the block
pair well-defined under transport; "the precondition of MINIMAL_SUBLAW named by
the posing is discharged at degree 0."

**Step 2 of record (found already built and confirmed — the tasked target).**
POSED §3 [24873,26213) poses the MINIMAL_SUBLAW: the AND-boolean block-class
`cls(j) = ([phi_f != 0],[phi_H != 0])` with a member-independence certificate
over the sealed 1887-dim joint variety. The commissioned question ("do phi_f,
phi_H vanish identically / never / on a proper subvariety") is ANSWERED OF
RECORD: `STAGE8_AND_CLASS_INDEPENDENCE_V001.md` (38bbb9fc, 2026-08-13):
`PHI_H_VANISHING = IDENTICAL` (kinematic collapse — `dim H = 0` exactly on every
sealed complex of the working class, so the projection `P_H` is the zero map);
`PHI_F_VANISHING = NEVER` on the admissible set (sealed total-nonzero for
n != 0); `AND_CLASS = CONSTANT_FALSE`, the posed falsifier pair CANNOT exist,
the split does NOT devolve to member selection. Independently re-verified
cross-lineage at c0e7192b: `AND_VERDICT = CONFIRMED(CONSTANT_FALSE)`
(unconditional fraction-free Q-elimination on all five complexes; different
primes; different constructors). Both seals verified at path this session.
REBUILDING IT WOULD DOUBLE-BUILD; per the commission's own ordering rule the
build advances to what is NEXT.

**What is NEXT per the POSED artifact's own list.** With O6b (the chain map
sd*_0/sd*_1/sd*_2, both squares) BUILT+CONFIRMED and the MINIMAL_SUBLAW decided,
the remaining transport-package obligations of the POSED §2 list that OBL-D
rides are O7 (RA27-4), O8 (RA27-5), O9 (RA27-6), O10/E4:

```text
O7  RA27-4 (POSED [17736,18509); D012 row [45008,45355)): "eta_resp, a named
    response topology/quotient, and an EXACTLY commuting common-refinement
    square ... zero naturality defect; no boundary/contact or asymptotic
    tolerance may substitute". D012 §2.6.3 [31536,33632) displays the square's
    SHAPE: it consumes a field-side leg  r_F^* : F_phys(K) -> F_phys(R)  (the
    pullback of coarse fluxes to the refinement), the response legs eta_resp /
    Eval, and demands N_r(d_K) = 0 exactly in a named carrier.
O8  RA27-5: tau_ker + CoefficientBoundaryCert — the coefficient object is
    absent; the counting shell alone carries no load. NOT first-buildable.
O9  RA27-6: assembly conditional on O5-O8. NOT buildable before O7/O8.
O10 E4 cofinal-invariance: the CDL colimit object is absent. NOT buildable.
```

**Input availability audit for O7, at bytes.** Its three constituents:
(a) `eta_resp` — ABSENT of record (one of the three record-voiced undecidables
f_g / F_g / eta_resp; G1-CERT §4.1 [13899,15572) re-verified them surviving on
2026-08-14; G1-CERT-CHECK: "OBL-D: CONFIRMED OPEN"). NOT sealed-available.
(b) the named response topology/quotient — naming is spec/principal business,
not a lane certificate. NOT buildable here.
(c) **the field-side leg `r_F^*`** — D012 §2.6.3 types it as part of the future
package; NO sealed artifact supplies it (swept both roots to the declared
cutoff: zero hits). Its INPUTS are all sealed: the transports sd*_0/sd*_1/sd*_2
(CARRIER [5151,9996); XCHK [4056,5082); D0SQ; 795 [7747,9120)); the tangent
complex laws (V011 [44595,44690)); `Q_flux` and the three-block split (FORM
[8560,8871), one line from sealed d_1 d_0 = 0); `dim H = 0` on every working-
class carrier (AND-CLASS, CONFIRMED). **FIRST-BUILDABLE = the r_F^* leg, with
its exact coherence certificates.** The blockage of the FULL square is named
exactly: (a) + (b).

This is the piece OBL-D's clause 1 consumes first: V011 [47025,47247) — "The
response map must commute with pullback to a common refinement" — cannot even be
POSED until the pullback exists; the sealed kinematic factor of the response map
(the lift `Q_flux` onto the flux block) is exactly the part whose commutation
can be decided from sealed content alone.

---

## 2. SUB-TASK 2 — THE BUILD: THE r_F^* CERTIFICATE PACKAGE

### 2.1 The derived candidate, and what is (and is not) claimed for it

Sealed of record: the aggregations `sd*_k : C^k(K') -> C^k(K)` (k = 0,1,2), each
a sum of child cells with orientation signs. DERIVED HERE (no new choice): the
**transpose sign pattern**

```text
r*_k : C^k(K) -> C^k(K'),   (r*_k xi)_{c'} := sum_c orientation(c',c) xi_c
```

— each child cell reads its parent's value through the SAME sealed incidence
signs. This is the unique uniform-unit-weight map supported on the sealed
child-of-parent incidences; it is exhibited as THE CANDIDATE pullback and NOT
adopted — RA27-4's naming obligation stands. All certificates below are exact
matrix identities over Z (or exact rational solves), decided entrywise.

### 2.2 The certificates (all exact; script §5, output §6)

For every licensed generator — L_id, A0 (sigma = (1,0,3,2)), A1 (cubical
bisection, 81/216/216), A2-F (Freudenthal, 16/65/110), A2-B (barycentric,
81/544/1232) — with all censuses matching the sealed displays and both sealed
squares (d_0-square, d_1-square) reproduced entrywise as controls (the
d_1-square control simultaneously validates the reconstructed sd*_2 orientation
signs against the sealed identity):

```text
C-A  SECTION IDENTITIES, exact over Z:
       sd*_0 r*_0 = I ;  sd*_1 r*_1 = m_1 I ;  sd*_2 r*_2 = m_2 I
     with the exact integer multiplicity table
       generator   m_0  m_1  m_2      (children per parent cell at each level)
       L_id        1    1    1
       A0          1    1    1
       A1          1    2    4
       A2-F        1    1    2
       A2-B        1    2    8
     The pullback is an exact section of the sealed transport, level by level.

C-B  HORIZONTALITY INTERTWINING, exact over Z (the counting-metric adjoint):
       d_0'^T r*_1 = r*_0 d_0^T .
     Consequence: r*_1 maps horizontal (divergence-free) parent cochains to
     horizontal refined cochains — the pullback respects the sealed gauge split.

C-C  THE HONEST NEGATIVE — r* IS NOT A COCHAIN MAP on any proper refinement:
       r*_1 d_0 = d_0' r*_0  is TRUE on L_id and A0 (the m = 1 relabelings,
       where r* is the inverse iso) and FALSE on A1, A2-F, A2-B, with exact
       integer witnesses displayed, e.g. A1: child edge (0000 -> 0001) (a half-
       edge), parent vertex 0001: LHS 1 != RHS 0 — the constant extension of a
       gradient is not a gradient (its defect concentrates at non-copy
       vertices). The naive potential-pullback route to RA27-4's square is
       therefore DEAD; only the flux-level pullback below survives. Dual note:
       this is the mirror of the sealed section-side finding (XCHK: the
       min-norm SECTION of sd*_1 is NOT NATURAL; J_1 carried) — neither naive
       inverse direction is a cochain map; the sealed aggregation direction is.

C-D  THE FLUX PULLBACK AND ITS TWO EXACT NATURALITY IDENTITIES. Define
       r_F^* := d_1' . r*_1 . Q_flux  :  F_phys(K) -> F_phys(K').
     Well-typed: lands in im(d_1') by construction; linear; consumes only
     sealed objects (Q_flux is V011's unique horizontal minimum-norm lift).
     Then, on every licensed generator:
       (i)  Q'_flux . r_F^* = r*_1 . Q_flux        (ZERO naturality defect)
       (ii) sd*_2 . r_F^* = m_1 . Id_{F_phys(K)}   (aggregation returns m_1 I;
                                                    in particular r_F^* is
                                                    INJECTIVE)
     PROOF OF (i), from certified premises only: for xi in F_phys(K), let
     x := Q_flux xi — the unique horizontal 1-cochain with d_1 x = xi
     (uniqueness: horizontal = im(d_0)^perp = H (+) im(d_1^+), and dim H = 0 of
     record [AND-CLASS, CONFIRMED; ranks re-certified this session at three
     primes with the exact sandwich], so ker d_1 ∩ horizontal = 0). Then
     y := r*_1 x is horizontal by C-B, and d_1' y = r_F^* xi by definition; by
     the same uniqueness on K' (dim H(K') = 0, certified), y IS Q'_flux of its
     own flux. QED. PROOF OF (ii): sd*_2 d_1' y = d_1 sd*_1 y (the sealed
     d_1-square, reproduced entrywise this session) = d_1 (m_1 x) (C-A)
     = m_1 xi. QED.
     VERIFIED end-to-end, independently of the proof, on the FULL 17-dim basis
     of F_phys(K): Fraction-exact both sides on L_id, A0, A1, A2-F (17/17
     each); on A2-B Fraction-exact left side + exact mod-p solves at three
     primes (1000003, 1000033, 1000037) with uniqueness certified (17/17);
     identity (ii) Fraction-exact on all five (17/17 each).

C-E  COMPOSITES, formal: r*[g then h] := r*_h . r*_g. C-A composes with
     multiplicities multiplying (sd*[gh] r*[gh] = m_1[g] m_1[h] I via C-A
     twice); C-B composes by pasting the two intertwining squares; C-D(i)/(ii)
     compose by the same uniqueness argument on the composite carrier (H = 0
     certified on Z = the 384-cell common refinement of record — AND-CLASS
     table row Z, corroborated by 807's own sealed kernel display). Carried at
     the same strength as the sealed composite-closure step (CARRIER §4 / D4
     §3.2: "a composition of chain maps is a chain map"); not recomputed.
```

### 2.3 Member-independence carry (the commissioned question, at this step)

The commissioned member-independence question for step 2 is of record
(CONSTANT_FALSE, no devolution — §1). For THIS step: `r*_k` and `r_F^*` consume
NO variety member — 807's variable inventory ([6502,7613)) is the quotient
two-form family `S` alone; the chain/cochain transports are NOT variety
variables, and no F-rule, no J_1 section value, no member of the 1887-dim
variety, and no value of the 33-free/32-at-fixed-energy assignment freedom (FR
[10137,10626)) enters any certificate above. The r_F^* leg is member-free by
construction; the falsifier of record (two variety members realizing different
transported patterns) cannot be realized through anything built here.

### 2.4 What would refute this package

```text
R-1  An exact integer discrepancy in any reproduced sealed object: censuses,
     d_1 d_0 = 0, the d_0-square, the d_1-square. (All reproduced MATCH/zero.)
R-2  A licensed generator where C-A fails (some sd*_k r*_k not m_k I) or C-B
     fails entrywise. (None; both hold on all five.)
R-3  A basis flux where (i) or (ii) fails exactly. (None; 17/17 on all five.)
R-4  dim H != 0 on a working-class carrier (kills the uniqueness premise).
     (Excluded at three primes with the exact sandwich, this session, on all
     five carriers — and of record at AND-CLASS/CHECK.)
R-5  A sealed artifact supplying a DIFFERENT r_F^* of record. (Swept to the
     declared cutoff: none exists.)
```

---

## 3. SUB-TASK 3 — THE OBL-D CONSEQUENCE, HELD TO SEALED CONDITIONALS

OBL-D of record (V011 [46712,47445); G1-CERT §4.1 CONFIRMED OPEN 2026-08-14):
the frozen C_ref class; clause 1 "the response map must commute with pullback to
a common refinement"; clause 2 intensive-coefficient invariance up to a
vanishing boundary/four-volume term; the warning "One hypercubic sequence is a
regression fixture, not proof of universality."

**What tonight's piece contributes, exactly:**

```text
K-1  CLAUSE 1 IS NOW POSABLE: its consuming arrow r_F^* exists as a derived,
     exhibited, member-free candidate with exact coherence certificates —
     before tonight no pullback of any kind existed of record (D012 §2.6.3
     types it "the future realization/naturality package").
K-2  CLAUSE 1's SEALED-KINEMATIC HALF IS DISCHARGED AT THE LICENSED-GENERATOR
     QUANTIFIER: the response map's only sealed factor — the horizontal lift
     Q_flux onto the flux block, the carrier of the FORM's derived
     R_record,L = n^2 Phi^T C Phi — commutes with the candidate pullback with
     ZERO defect (C-D(i)), and the pullback aggregates back to m_1 I (C-D(ii)),
     on every licensed generator and formally on composites. The remaining
     clause-1 load is EXACTLY the unsealed response legs: eta_resp and the
     kernel C — i.e. the load G1-CERT's map already places there; that
     placement is now certificate-backed at the kinematic level, not assumed.
K-3  CLAUSE 2 ACQUIRES ITS EXACT FINITE SKELETON, NOT ITS PROOF: the level
     multipliplicity mismatch (m_1 vs m_2: A1 2 vs 4; A2-F 1 vs 2; A2-B 2 vs 8)
     is the exact reason no intensive normalization of r* can be a chain map at
     all levels at once — invariance can only hold with per-level weights,
     which is the finite shape of clause 2's boundary-term tolerance and
     consistent with 807's forced volume weights (1/16, 1/24, 1/384). Displayed
     as structure; NOT adjudicated.
K-4  MEMBER-FREEDOM PRESERVED: the OBL-D quantifier gains a leg that consumes
     no variety member, keeping cellulation-independence questions separated
     from member selection (which remains principal business).
```

**What remains, carried OPEN and NOT decided here:**

```text
O-1  The full RA27-4 square: eta_resp ABSENT; the response topology/quotient
     UNNAMED; N_r = 0 undecidable until both exist. O8 (tau_ker/
     CoefficientBoundaryCert), O9 (RA27-6 assembly), O10/E4 all UNBUILT.
O-2  THE LICENSED-GENERATOR vs FULL-D3 GAP, carried, not decided: D3's frozen
     reading (9a0c2045) quantifies over ALL common refinements — star-refined
     atoms of unbounded facet count included — and V011's C_ref is a frozen
     AUDIT class (POSED trap T1). Tonight's certificates bind to the five
     exhibited generator instances and their formal composites on the sealed
     working-class carrier; they add the simplicial and barycentric families
     beyond "one hypercubic sequence", which answers the regression-fixture
     warning IN PART and proves NO universality.
O-3  Multi-cell / glued carriers: no sealed multi-cell record complex exists at
     the physical stratum (the O11/O12/O-D1 gap of record); loop-carrying
     sealed carriers (H^1 > 0) live at the AUDIT stratum and are OUTSIDE the
     licensed C_ref inventory — extending r_F^* there is a NEW pose, not taken.
O-4  OBL-D clause 2's actual limit statement (boundary/four-volume ratio -> 0
     for the COEFFICIENT) needs the coefficient object (the Gate-5 kernel C /
     tau_ker) — G1-CERT: PARTIAL, one of three blockers discharged; untouched
     here.
```

---

## 4. CHOICE LEDGER (every discretionary choice, tagged)

```text
CH-1 TARGET SELECTION (YOURS, forced by the ordering): the tasked step 2 was
     found ALREADY BUILT+CONFIRMED at path during the pre-build sweep; per the
     commission's own rule ("the POSED artifact's obligation list and first-
     buildable ordering govern what is NEXT") the build advanced to the O7
     field-side leg — the only transport-package fragment whose inputs are all
     sealed (§1 audit). Rebuilding step 2 was rejected as double-building.
CH-2 THE CANDIDATE r* (YOURS, derived not adopted): transpose sign pattern of
     the sealed aggregations — the unique uniform-unit-weight map on the sealed
     incidences. No alternative weighting was tried, selected, or tuned; the
     multiplicities m_k are OUTPUTS (exact integers), not inputs.
CH-3 CELL ORDER lexicographic throughout (as in the sealed D0SQ lineage);
     verdicts are ordering-invariant (conjugation by permutations).
CH-4 A2-B TRIANGLE SIGNS from the barycentric-frame determinant (integers-only
     doubled barycenters); A0 FACE SIGNS from the permutation frame — both
     VALIDATED against the sealed d_1-square identity reproduced entrywise
     (a reconstruction-to-seal check, not a fit-to-outcome: the identity is
     sealed of record, 614e20c8, 771/0).
CH-5 FLUX BASIS: the greedy first-17 independent columns of d_1 (displayed in
     the output). Any basis is equivalent; the identities are linear.
CH-6 INSTRUMENT ASSIGNMENT BY SIZE (declared before running): Fraction-exact
     end-to-end where E' <= 300; on A2-B (E' = 544) Fraction-exact left side +
     exact mod-p solves at three primes with certified uniqueness. The proof
     route of C-D is instrument-independent (premises all entrywise-exact).
CH-7 ONE CODE CORRECTION, disclosed: the first run's mod-p back-substitution
     processed pivots in reverse INSERTION order and returned 0/17 on A2-B's
     redundant check while C-B (which logically forces the identity) was
     already TRUE — the contradiction flagged the solver bug; fixed to
     decreasing-column order (each pivot is its row's minimum column); rerun
     landed 17/17. No certificate identity was altered; both runs' Fraction
     legs agreed throughout.
CH-8 NOTHING SELECTED BY OUTCOME: all decision branches (each C-x could land
     TRUE/FALSE with witnesses) were implemented before the first run; the C-C
     negative is REPORTED, not suppressed — it kills the naive route this
     builder had considered first.
```

## TOY_SEPARATION

```text
The five carriers are the sealed WORKING-CLASS INSTANCE: the single oriented
unit 4-cell and its licensed refinements — the same instance the whole lineage
binds to (738/795/807/D0SQ/AND-CLASS). They are a TOY relative to: (t1) the
record complex proper — C_ref is a frozen AUDIT class with no sealed incidence
realization (POSED trap T1), and no multi-cell physical complex exists (O11/
O12/O-D1 gap); (t2) full D3 — "all common refinements", star-refined atoms of
unbounded facet count (9a0c2045); (t3) loop-carrying carriers (H^1 > 0), where
the harmonic block is nonzero and the AND-class question REOPENS of record.
NOTHING in this artifact transfers from the toy to those strata by itself:
C-A/C-B/C-C/C-D are proved ONLY on the five exhibited instances + formal
composites; the H = 0 premise of C-D's uniqueness is a working-class fact, not
a universal one; V011's own sentence — "One hypercubic sequence is a regression
fixture, not proof of universality" — is quoted, honored, and still binding.
No physical reading of any certificate is made: r_F^* is finite linear algebra
on integer incidence matrices, not a physical pullback of any field.
```

---

## 5. The script, verbatim (`rfstar_certificate_v001.py`, fresh venv, Python 3.9.6 stdlib only)

```python
#!/usr/bin/env python3
# rfstar_certificate_v001.py — THE r_F^* (COMMON-REFINEMENT PULLBACK) CERTIFICATE.
# Exact integer/rational algebra only (int + fractions.Fraction; no floats anywhere).
#
# TARGET (RA27-4 / OBL-D field-side leg): for every licensed generator g, derive the
# candidate pullback r*_k := transpose sign pattern of the SEALED aggregation sd*_k
# (k = 0,1,2), and decide EXACTLY:
#   C-A (SECTION):        sd*_0 r*_0 = I ; sd*_1 r*_1 = m_1 I ; sd*_2 r*_2 = m_2 I
#                         with the exact integer multiplicities m_k displayed.
#   C-B (HORIZONTALITY):  d_0'^T r*_1 = r*_0 d_0^T   (divergence intertwining in the
#                         sealed counting metric; horizontal -> horizontal).
#   C-C (COCHAIN-MAP?):   r*_1 d_0 = d_0' r*_0 ?  decided entrywise; exact witness
#                         displayed wherever it FAILS (honest negative).
#   C-D (FLUX PULLBACK):  with r_F^* xi := d_1'( r*_1 Q_flux xi ) on F_phys = im(d_1):
#                         (i)  Q'_flux r_F^* = r*_1 Q_flux   (zero naturality defect)
#                         (ii) sd*_2 r_F^* = m_1 I           (aggregation returns m_1)
#                         derived from C-A + C-B + H = 0 + the d_1-square, AND verified
#                         end-to-end on a FULL basis of F_phys (Fraction-exact where
#                         feasible; exact mod-p at three primes on the largest carrier).
# CONTROLS (sealed reproductions): censuses; d_1 d_0 = 0; the d_0-square
# (sd*_1 d_0' = d_0 sd*_0, sealed bb1b88ad/CONFIRMED d83655ae); the d_1-square
# (sd*_2 d_1' = d_1 sd*_1, sealed 97f073c1/CONFIRMED 614e20c8) — the d_1-square control
# also validates the reconstructed sd*_2 orientation signs; rank sandwich at three
# primes reproducing dim H = 0 (sealed 38bbb9fc/CONFIRMED c0e7192b).
#
# SEALED INPUTS (constructions reproduced from sealed constructive displays):
#   d_0 / d_1 laws:  V011 packet member aa7c6d49... [44595,44690)
#   sd*_1 law:       carrier 97f073c1... [5151,9996)
#   complexes/rows:  crosscheck 614e20c8... [4056,5082): parent 16/32/24;
#                    A1 = grid {0,1,2}^4 81/216/216; A2-F Freudenthal 16/65/110;
#                    A2-B barycentric 81/544/1232. "Cubical rows sum the two child
#                    edges or four child squares. Freudenthal rows retain parent edges
#                    and sum the two oriented boundary triangles. Barycentric rows sum
#                    the two half-edges and eight oriented vertex-edge-face triangles."
#   A0 instance:     795 (590b3979...) sigma = (1,0,3,2), sd*_1 = permutation P
#   sd*_0:           the sealed d_0-square canonical iota* (bb1b88ad section 4)
from fractions import Fraction
from itertools import product

PRIMES = [1000003, 1000033, 1000037]

# ---------------- sparse-map helpers (maps as lists of sparse rows) ----------------
def compose(M, N):
    """(M o N)[i] = sum_j M[i][j] * N[j]  — M: R^n->R^m rows over cols(n)=rows(N)."""
    out = []
    for row in M:
        acc = {}
        for j, c in row.items():
            for k, c2 in N[j].items():
                acc[k] = acc.get(k, 0) + c * c2
        out.append({k: v for k, v in acc.items() if v != 0})
    return out

def transpose(M, ncols):
    out = [dict() for _ in range(ncols)]
    for i, row in enumerate(M):
        for j, c in row.items():
            out[j][i] = c
    return out

def eq_maps(A, B):
    if len(A) != len(B):
        return False, ('rowcount', len(A), len(B))
    for i, (ra, rb) in enumerate(zip(A, B)):
        cols = set(ra) | set(rb)
        for c in cols:
            if ra.get(c, 0) != rb.get(c, 0):
                return False, (i, c, ra.get(c, 0), rb.get(c, 0))
    return True, None
def is_scaled_identity(M, m):
    for i, row in enumerate(M):
        for j, c in row.items():
            if (j == i and c != m) or (j != i and c != 0):
                return False, (i, j, c)
        if m != 0 and row.get(i, 0) != m:
            return False, (i, i, row.get(i, 0))
    return True, None

def rank_mod_p(rows, p):
    piv = {}
    r = 0
    for row in rows:
        cur = {c: v % p for c, v in row.items() if v % p}
        while cur:
            c = min(cur)
            if c in piv:
                f = cur[c]
                base = piv[c]
                nxt = dict(cur)
                for k, v in base.items():
                    nv = (nxt.get(k, 0) - f * v) % p
                    if nv:
                        nxt[k] = nv
                    elif k in nxt:
                        del nxt[k]
                cur = nxt
            else:
                inv = pow(cur[c], p - 2, p)
                piv[c] = {k: (v * inv) % p for k, v in cur.items()}
                r += 1
                cur = {}
    return r

def solve_exact(rows, ncols, rhs):
    """Solve M x = rhs over Q by dense Fraction elimination. Returns (x, unique, ok):
    unique iff rank = ncols; ok iff consistent. Exact; no floats."""
    m = len(rows)
    A = []
    for i in range(m):
        v = [Fraction(0)] * (ncols + 1)
        for c, x in rows[i].items():
            v[c] = Fraction(x)
        v[ncols] = Fraction(rhs.get(i, 0))
        A.append(v)
    r0 = 0
    pivots = []
    for c in range(ncols):
        piv = next((r for r in range(r0, m) if A[r][c] != 0), None)
        if piv is None:
            continue
        A[r0], A[piv] = A[piv], A[r0]
        pv = A[r0][c]
        A[r0] = [x / pv for x in A[r0]]
        for r in range(m):
            if r != r0 and A[r][c] != 0:
                f = A[r][c]
                A[r] = [a - f * b for a, b in zip(A[r], A[r0])]
        pivots.append(c)
        r0 += 1
        if r0 == m:
            break
    for r in range(r0, m):
        if A[r][ncols] != 0:
            return None, False, False
    x = [Fraction(0)] * ncols
    for r, c in enumerate(pivots):
        x[c] = A[r][ncols]
    return x, (len(pivots) == ncols), True

def solve_mod_p(rows, ncols, rhs, p):
    """Solve M x = rhs over GF(p), sparse; returns (x, unique, ok)."""
    piv = {}       # col -> (normalized sparse row, rhs value)
    order = []
    for i, row in enumerate(rows):
        cur = {c: v % p for c, v in row.items() if v % p}
        b = rhs.get(i, 0) % p
        while cur:
            c = min(cur)
            if c in piv:
                f = cur[c]
                base, bb = piv[c]
                nxt = dict(cur)
                for k, v in base.items():
                    nv = (nxt.get(k, 0) - f * v) % p
                    if nv:
                        nxt[k] = nv
                    elif k in nxt:
                        del nxt[k]
                b = (b - f * bb) % p
                cur = nxt
            else:
                inv = pow(cur[c], p - 2, p)
                piv[c] = ({k: (v * inv) % p for k, v in cur.items()}, (b * inv) % p)
                order.append(c)
                cur = {}
                b = 0
        if not cur and b % p:
            return None, False, False
    x = [0] * ncols
    for c in sorted(piv, reverse=True):
        # pivot c is the MINIMUM column of its reduced row, so every other column
        # in the row is > c: substituting in decreasing column order is sound.
        row, b = piv[c]
        s = b
        for k, v in row.items():
            if k != c:
                s = (s - v * x[k]) % p
        x[c] = s
    return x, (len(order) == ncols), True

# ---------------- parent K: oriented unit 4-cube (16/32/24) ----------------
def cube_complex(k):
    V = sorted(product(range(k + 1), repeat=4))
    vi = {v: n for n, v in enumerate(V)}
    E = []
    for v in V:
        for i in range(4):
            if v[i] < k:
                w = tuple(v[t] + (1 if t == i else 0) for t in range(4))
                E.append((v, w))
    E = sorted(E)
    ei = {e: n for n, e in enumerate(E)}
    F = []
    d1 = []
    for v in V:
        for i in range(4):
            for j in range(i + 1, 4):
                if v[i] < k and v[j] < k:
                    a = tuple(v[t] + (1 if t == i else 0) for t in range(4))
                    c = tuple(v[t] + (1 if t == j else 0) for t in range(4))
                    b = tuple(a[t] + (1 if t == j else 0) for t in range(4))
                    F.append((v, i, j))
                    d1.append({ei[(v, a)]: 1, ei[(a, b)]: 1,
                               ei[(c, b)]: -1, ei[(v, c)]: -1})
    return V, vi, E, ei, F, d1

def d0_rows(E, vi):
    return [{vi[t]: 1, vi[s]: -1} for (s, t) in E]

PV, pvi, PE, pei, PF, d1K = cube_complex(1)
pfi = {f: n for n, f in enumerate(PF)}
d0K = d0_rows(PE, pvi)

# ---------------- order complexes (A2-F, A2-B) ----------------
def order_complex(verts, lt):
    V = sorted(verts)
    vi = {v: n for n, v in enumerate(V)}
    E = sorted((u, v) for u in V for v in V if lt(u, v))
    ei = {e: n for n, e in enumerate(E)}
    F, d1 = [], []
    for (u, v) in E:
        for w in V:
            if lt(v, w) and (u, w) in ei:
                F.append((u, v, w))
                d1.append({ei[(u, v)]: 1, ei[(v, w)]: 1, ei[(u, w)]: -1})
    return V, vi, E, ei, F, d1

# ---------------- the five licensed generator instances ----------------
GENS = {}

# L_id — identity sanity
GENS['L_id'] = dict(
    Kp=(PV, pvi, PE, pei, PF, d1K), d0p=d0K,
    sd0=[{i: 1} for i in range(16)],
    sd1=[{i: 1} for i in range(32)],
    sd2=[{i: 1} for i in range(24)],
    m=(1, 1, 1))

# A0 — sealed relabeling sigma = (1,0,3,2) (795): v[k] -> v[p[k]], p an involution
p = (1, 0, 3, 2)
def sig_v(v): return tuple(v[p[k]] for k in range(4))
def sig_e(e): return (sig_v(e[0]), sig_v(e[1]))     # coordinate perms keep u<v
sd1_A0 = [dict() for _ in range(32)]
for e in PE:
    sd1_A0[pei[e]][pei[sig_e(e)]] = 1
sd0_A0 = [dict() for _ in range(16)]
for v in PV:
    sd0_A0[pvi[v]][pvi[sig_v(v)]] = 1
sd2_A0 = [dict() for _ in range(24)]
for (v, i, j) in PF:
    ii, jj = p[i], p[j]
    sgn = 1
    if ii > jj:
        ii, jj = jj, ii
        sgn = -1
    sd2_A0[pfi[(v, i, j)]][pfi[(sig_v(v), ii, jj)]] = sgn
GENS['A0'] = dict(Kp=(PV, pvi, PE, pei, PF, d1K), d0p=d0K,
                  sd0=sd0_A0, sd1=sd1_A0, sd2=sd2_A0, m=(1, 1, 1))

# A1 — cubical bisection: grid {0,1,2}^4 (81/216/216)
GV, gvi, GE, gei, GF, d1_A1p = cube_complex(2)
gfi = {f: n for n, f in enumerate(GF)}
d0_A1p = d0_rows(GE, gvi)
sd0_A1 = [{gvi[tuple(2 * x for x in v)]: 1} for v in PV]
sd1_A1 = [dict() for _ in range(32)]
for (s, t) in PE:
    i = next(k for k in range(4) if s[k] != t[k])
    a = tuple(2 * x for x in s)
    mid = tuple(a[k] + (1 if k == i else 0) for k in range(4))
    b = tuple(a[k] + (2 if k == i else 0) for k in range(4))
    sd1_A1[pei[(s, t)]][gei[(a, mid)]] = 1
    sd1_A1[pei[(s, t)]][gei[(mid, b)]] = 1
sd2_A1 = [dict() for _ in range(24)]
for (v, i, j) in PF:
    base = tuple(2 * x for x in v)
    for s_ in (0, 1):
        for t_ in (0, 1):
            child = tuple(base[k] + (s_ if k == i else 0) + (t_ if k == j else 0)
                          for k in range(4))
            sd2_A1[pfi[(v, i, j)]][gfi[(child, i, j)]] = 1
GENS['A1'] = dict(Kp=(GV, gvi, GE, gei, GF, d1_A1p), d0p=d0_A1p,
                  sd0=sd0_A1, sd1=sd1_A1, sd2=sd2_A1, m=(1, 2, 4))

# A2-F — Freudenthal chains of Boolean vertices (16/65/110)
def bool_lt(u, v): return u != v and all(u[k] <= v[k] for k in range(4))
FV, fvi, FE, fei, FF, d1_A2Fp = order_complex(list(product((0, 1), repeat=4)), bool_lt)
ffi = {f: n for n, f in enumerate(FF)}
d0_A2Fp = d0_rows(FE, fvi)
sd0_A2F = [{fvi[v]: 1} for v in PV]
sd1_A2F = [dict() for _ in range(32)]
for (s, t) in PE:
    sd1_A2F[pei[(s, t)]][fei[(s, t)]] = 1       # Freudenthal retains parent edges
sd2_A2F = [dict() for _ in range(24)]
for (v, i, j) in PF:
    a = tuple(v[t] + (1 if t == i else 0) for t in range(4))
    c = tuple(v[t] + (1 if t == j else 0) for t in range(4))
    b = tuple(a[t] + (1 if t == j else 0) for t in range(4))
    sd2_A2F[pfi[(v, i, j)]][ffi[(v, a, b)]] = 1   # +T1 = (v, v+ei, v+ei+ej)
    sd2_A2F[pfi[(v, i, j)]][ffi[(v, c, b)]] = -1  # -T2 = (v, v+ej, v+ei+ej)
GENS['A2-F'] = dict(Kp=(FV, fvi, FE, fei, FF, d1_A2Fp), d0p=d0_A2Fp,
                    sd0=sd0_A2F, sd1=sd1_A2F, sd2=sd2_A2F, m=(1, 1, 2))

# A2-B — barycentric chains of nonempty 4-cube faces (81/544/1232); digit 2 = free
def subface(g, f): return g != f and all(f[k] == 2 or g[k] == f[k] for k in range(4))
BV, bvi, BE, bei, BF, d1_A2Bp = order_complex(list(product((0, 1, 2), repeat=4)),
                                              subface)
bfi = {f: n for n, f in enumerate(BF)}
d0_A2Bp = d0_rows(BE, bvi)
sd0_A2B = [{bvi[v]: 1} for v in PV]                 # vertex-face is the vertex itself
sd1_A2B = [dict() for _ in range(32)]
for (s, t) in PE:
    i = next(k for k in range(4) if s[k] != t[k])
    ef = tuple(2 if k == i else s[k] for k in range(4))
    sd1_A2B[pei[(s, t)]][bei[(s, ef)]] = 1          # + (s -> {e})
    sd1_A2B[pei[(s, t)]][bei[(t, ef)]] = -1         # - (t -> {e})  ({e}->t reversed)
def bary2(g):                                       # 2x the barycenter (integers)
    return tuple(1 if g[k] == 2 else 2 * g[k] for k in range(4))
sd2_A2B = [dict() for _ in range(24)]
for (v, i, j) in PF:
    ff = tuple(2 if k in (i, j) else v[k] for k in range(4))
    for (u, ef, fw) in BF:
        if fw == ff and subface(u, ef):             # chains u < {edge} < {face}
            b_u, b_e, b_f = bary2(u), bary2(ef), bary2(ff)
            v1 = (b_e[i] - b_u[i], b_e[j] - b_u[j])
            v2 = (b_f[i] - b_e[i], b_f[j] - b_e[j])
            det = v1[0] * v2[1] - v1[1] * v2[0]
            sd2_A2B[pfi[(v, i, j)]][bfi[(u, ef, fw)]] = 1 if det > 0 else -1
GENS['A2-B'] = dict(Kp=(BV, bvi, BE, bei, BF, d1_A2Bp), d0p=d0_A2Bp,
                    sd0=sd0_A2B, sd1=sd1_A2B, sd2=sd2_A2B, m=(1, 2, 8))

SEALED_CENSUS = {'L_id': (16, 32, 24), 'A0': (16, 32, 24), 'A1': (81, 216, 216),
                 'A2-F': (16, 65, 110), 'A2-B': (81, 544, 1232)}

# ---------------- parent-side objects, flux basis ----------------
d0K_T = transpose(d0K, 16)                    # d_0^T : C^1(K) -> C^0(K)
# full basis of F_phys(K) = im(d_1): pick 17 columns a with d_1 e_a independent
cols = [{r: d1K[r][a] for r in range(24) if a in d1K[r]} for a in range(32)]
basis_cols = []
seen = []
for a in range(32):
    trial = seen + [cols[a]]
    if rank_mod_p(trial, PRIMES[0]) == len(trial):
        seen = trial
        basis_cols.append(a)
print('parent flux basis: columns d_1 e_a for a in', basis_cols,
      '| count =', len(basis_cols))
r0K = rank_mod_p(d0K, PRIMES[0])
r1K = rank_mod_p(d1K, PRIMES[0])
print(f'parent ranks (p={PRIMES[0]}): rank d_0 = {r0K} (V-1 = 15), '
      f'rank d_1 = {r1K} (E-V+1 = 17)  => dim H(K) = {32 - r0K - r1K}')

overall = True
for name in ['L_id', 'A0', 'A1', 'A2-F', 'A2-B']:
    G = GENS[name]
    Vp, vip, Ep, eip, Fp, d1p = G['Kp']
    d0p = G['d0p']
    nV, nE, nF = len(Vp), len(Ep), len(Fp)
    m0, m1, m2 = G['m']
    print(f'\n================ {name} ================')
    cen = (nV, nE, nF)
    print(f'census V\'/E\'/F\' = {cen}  sealed {SEALED_CENSUS[name]}  ->',
          'MATCH' if cen == SEALED_CENSUS[name] else 'MISMATCH')
    overall &= (cen == SEALED_CENSUS[name])

    # controls: d_1' d_0' = 0 ; the sealed d_0- and d_1-squares
    z, w = eq_maps(compose(d1p, d0p), [dict() for _ in range(nF)])
    print(f'CONTROL  d_1\' d_0\' = 0 entrywise: {z}')
    overall &= z
    sd0, sd1, sd2 = G['sd0'], G['sd1'], G['sd2']
    z1, w1 = eq_maps(compose(sd1, d0p), compose(d0K, sd0))
    print(f'CONTROL  d_0-square  sd*_1 d_0\' = d_0 sd*_0 : {z1}' +
          ('' if z1 else f'  WITNESS {w1}'))
    z2, w2 = eq_maps(compose(sd2, d1p), compose(d1K, sd1))
    print(f'CONTROL  d_1-square  sd*_2 d_1\' = d_1 sd*_1 : {z2}' +
          ('' if z2 else f'  WITNESS {w2}'))
    overall &= z1 and z2

    # rank controls (three primes) — dim H(K') = 0 sandwich
    rp0 = [rank_mod_p(d0p, q) for q in PRIMES]
    rp1 = [rank_mod_p(d1p, q) for q in PRIMES]
    okH = all(r == nV - 1 for r in rp0) and all(r == nE - (nV - 1) for r in rp1)
    print(f'CONTROL  rank_p d_0\' = {rp0} (V\'-1 = {nV-1}); '
          f'rank_p d_1\' = {rp1} (E\'-V\'+1 = {nE-nV+1})')
    print(f'         sandwich (rank_Q d_0\' <= V\'-1 from constants; rank_Q d_1\' <= '
          f'E\'-(V\'-1) from d_1\'d_0\'=0; rank_p attains both) => dim H(K\') = 0 '
          f'EXACT : {okH}')
    overall &= okH

    # the pullback: transposes of the sealed aggregations
    r0 = transpose(sd0, nV)
    r1 = transpose(sd1, nE)
    r2 = transpose(sd2, nF)

    # C-A sections
    a0, wa0 = is_scaled_identity(compose(sd0, r0), m0)
    a1, wa1 = is_scaled_identity(compose(sd1, r1), m1)
    a2, wa2 = is_scaled_identity(compose(sd2, r2), m2)
    print(f'C-A  sd*_0 r*_0 = {m0}·I : {a0} ; sd*_1 r*_1 = {m1}·I : {a1} ; '
          f'sd*_2 r*_2 = {m2}·I : {a2}')
    for tag, ok, wit in (('k=0', a0, wa0), ('k=1', a1, wa1), ('k=2', a2, wa2)):
        if not ok:
            print(f'     C-A FAILURE WITNESS {tag}: {wit}')
    overall &= a0 and a1 and a2

    # C-B divergence intertwining
    d0p_T = transpose(d0p, nV)
    cb, wcb = eq_maps(compose(d0p_T, r1), compose(r0, d0K_T))
    print(f'C-B  d_0\'^T r*_1 = r*_0 d_0^T (horizontality preserved) : {cb}' +
          ('' if cb else f'  WITNESS {wcb}'))
    overall &= cb

    # C-C cochain-map trichotomy (honest decision, witness if it fails)
    cc, wcc = eq_maps(compose(r1, d0K), compose(d0p, r0))
    if cc:
        print('C-C  r*_1 d_0 = d_0\' r*_0 : TRUE — r* is a cochain map at degree 0')
    else:
        i, c, va, vb = wcc
        print(f'C-C  r*_1 d_0 = d_0\' r*_0 : FALSE — exact witness: child edge '
              f'{Ep[i]} , parent vertex {PV[c]} : LHS {va} != RHS {vb} '
              f'(r* is NOT a cochain map on this generator)')

    # C-D flux pullback on the FULL parent flux basis
    stackedK = d0K_T + d1K            # 16+24 rows over 32 cols
    stackedKp = d0p_T + d1p           # nV+nF rows over nE cols
    exact_mode = nE <= 300            # Fraction-exact where feasible; mod-p beyond
    n_ok = 0
    agg_ok = 0
    for a in basis_cols:
        xi = {16 + r: d1K[r][a] for r in range(24) if a in d1K[r]}  # rhs rows offset
        x, uniq, cons = solve_exact(stackedK, 32, xi)
        assert cons and uniq, 'parent horizontal-lift solve must be unique'
        # y = r*_1 x  (exact rationals)
        y = [Fraction(0)] * nE
        for j, row in enumerate(r1):
            s = Fraction(0)
            for c2, v2 in row.items():
                if x[c2]:
                    s += v2 * x[c2]
            y[j] = s
        # horizontality of y (must follow from C-B):
        hor = all(sum(v * y[c2] for c2, v in row.items()) == 0
                  for row in d0p_T)
        # flux' = d_1' y ; aggregation check sd*_2 flux' = m_1 * xi
        fluxp = [sum(v * y[c2] for c2, v in row.items()) for row in d1p]
        agg = [sum(v * fluxp[c2] for c2, v in row.items()) for row in sd2]
        target = [Fraction(0)] * 24
        for r in range(24):
            if a in d1K[r]:
                target[r] = Fraction(m1 * d1K[r][a])
        agg_ok += (agg == target)
        if exact_mode:
            rhs = {len(d0p_T) + r: fluxp[r] for r in range(nF) if fluxp[r] != 0}
            zsol, uq, cs = solve_exact(stackedKp, nE, rhs)
            assert cs and uq, 'refined horizontal-lift solve must be unique'
            n_ok += (hor and zsol == y)
        else:
            okp = hor
            for q in PRIMES:
                den = 1
                for val in y:
                    den = den * val.denominator // __import__('math').gcd(
                        den, val.denominator)
                assert den % q != 0
                rhs_p = {len(d0p_T) + r:
                         (fluxp[r].numerator * pow(fluxp[r].denominator, q - 2, q))
                         % q for r in range(nF) if fluxp[r] != 0}
                zp, uqp, csp = solve_mod_p(stackedKp, nE, rhs_p, q)
                assert csp and uqp
                yp = [(val.numerator * pow(val.denominator, q - 2, q)) % q
                      for val in y]
                okp &= (zp == yp)
            n_ok += okp
    mode = 'Fraction-exact end-to-end' if exact_mode else \
           f'Fraction-exact one side + exact mod-p at {PRIMES} (uniqueness certified)'
    print(f'C-D  (i) Q\'_flux r_F^* = r*_1 Q_flux on all {len(basis_cols)} basis '
          f'fluxes : {n_ok}/{len(basis_cols)}   [{mode}]')
    print(f'C-D  (ii) sd*_2 r_F^* = m_1·I = {m1}·I on all basis fluxes : '
          f'{agg_ok}/{len(basis_cols)}')
    overall &= (n_ok == len(basis_cols)) and (agg_ok == len(basis_cols))

print('\n================ NET ================')
print('ALL CONTROLS AND CERTIFICATES (C-A, C-B, C-D, controls, ranks, censuses) '
      'LANDED :', overall)
print('C-C is reported per generator above (an honest trichotomy, not a pass/fail '
      'gate).')
```

---

## 6. Exact script output, verbatim (`python3 rfstar_certificate_v001.py`, exit code 0; runtime 4:02)

```text
parent flux basis: columns d_1 e_a for a in [0, 1, 2, 4, 5, 7, 8, 10, 12, 13, 15, 17, 20, 21, 23, 25, 28] | count = 17
parent ranks (p=1000003): rank d_0 = 15 (V-1 = 15), rank d_1 = 17 (E-V+1 = 17)  => dim H(K) = 0

================ L_id ================
census V'/E'/F' = (16, 32, 24)  sealed (16, 32, 24)  -> MATCH
CONTROL  d_1' d_0' = 0 entrywise: True
CONTROL  d_0-square  sd*_1 d_0' = d_0 sd*_0 : True
CONTROL  d_1-square  sd*_2 d_1' = d_1 sd*_1 : True
CONTROL  rank_p d_0' = [15, 15, 15] (V'-1 = 15); rank_p d_1' = [17, 17, 17] (E'-V'+1 = 17)
         sandwich (rank_Q d_0' <= V'-1 from constants; rank_Q d_1' <= E'-(V'-1) from d_1'd_0'=0; rank_p attains both) => dim H(K') = 0 EXACT : True
C-A  sd*_0 r*_0 = 1·I : True ; sd*_1 r*_1 = 1·I : True ; sd*_2 r*_2 = 1·I : True
C-B  d_0'^T r*_1 = r*_0 d_0^T (horizontality preserved) : True
C-C  r*_1 d_0 = d_0' r*_0 : TRUE — r* is a cochain map at degree 0
C-D  (i) Q'_flux r_F^* = r*_1 Q_flux on all 17 basis fluxes : 17/17   [Fraction-exact end-to-end]
C-D  (ii) sd*_2 r_F^* = m_1·I = 1·I on all basis fluxes : 17/17

================ A0 ================
census V'/E'/F' = (16, 32, 24)  sealed (16, 32, 24)  -> MATCH
CONTROL  d_1' d_0' = 0 entrywise: True
CONTROL  d_0-square  sd*_1 d_0' = d_0 sd*_0 : True
CONTROL  d_1-square  sd*_2 d_1' = d_1 sd*_1 : True
CONTROL  rank_p d_0' = [15, 15, 15] (V'-1 = 15); rank_p d_1' = [17, 17, 17] (E'-V'+1 = 17)
         sandwich (rank_Q d_0' <= V'-1 from constants; rank_Q d_1' <= E'-(V'-1) from d_1'd_0'=0; rank_p attains both) => dim H(K') = 0 EXACT : True
C-A  sd*_0 r*_0 = 1·I : True ; sd*_1 r*_1 = 1·I : True ; sd*_2 r*_2 = 1·I : True
C-B  d_0'^T r*_1 = r*_0 d_0^T (horizontality preserved) : True
C-C  r*_1 d_0 = d_0' r*_0 : TRUE — r* is a cochain map at degree 0
C-D  (i) Q'_flux r_F^* = r*_1 Q_flux on all 17 basis fluxes : 17/17   [Fraction-exact end-to-end]
C-D  (ii) sd*_2 r_F^* = m_1·I = 1·I on all basis fluxes : 17/17

================ A1 ================
census V'/E'/F' = (81, 216, 216)  sealed (81, 216, 216)  -> MATCH
CONTROL  d_1' d_0' = 0 entrywise: True
CONTROL  d_0-square  sd*_1 d_0' = d_0 sd*_0 : True
CONTROL  d_1-square  sd*_2 d_1' = d_1 sd*_1 : True
CONTROL  rank_p d_0' = [80, 80, 80] (V'-1 = 80); rank_p d_1' = [136, 136, 136] (E'-V'+1 = 136)
         sandwich (rank_Q d_0' <= V'-1 from constants; rank_Q d_1' <= E'-(V'-1) from d_1'd_0'=0; rank_p attains both) => dim H(K') = 0 EXACT : True
C-A  sd*_0 r*_0 = 1·I : True ; sd*_1 r*_1 = 2·I : True ; sd*_2 r*_2 = 4·I : True
C-B  d_0'^T r*_1 = r*_0 d_0^T (horizontality preserved) : True
C-C  r*_1 d_0 = d_0' r*_0 : FALSE — exact witness: child edge ((0, 0, 0, 0), (0, 0, 0, 1)) , parent vertex (0, 0, 0, 1) : LHS 1 != RHS 0 (r* is NOT a cochain map on this generator)
C-D  (i) Q'_flux r_F^* = r*_1 Q_flux on all 17 basis fluxes : 17/17   [Fraction-exact end-to-end]
C-D  (ii) sd*_2 r_F^* = m_1·I = 2·I on all basis fluxes : 17/17

================ A2-F ================
census V'/E'/F' = (16, 65, 110)  sealed (16, 65, 110)  -> MATCH
CONTROL  d_1' d_0' = 0 entrywise: True
CONTROL  d_0-square  sd*_1 d_0' = d_0 sd*_0 : True
CONTROL  d_1-square  sd*_2 d_1' = d_1 sd*_1 : True
CONTROL  rank_p d_0' = [15, 15, 15] (V'-1 = 15); rank_p d_1' = [50, 50, 50] (E'-V'+1 = 50)
         sandwich (rank_Q d_0' <= V'-1 from constants; rank_Q d_1' <= E'-(V'-1) from d_1'd_0'=0; rank_p attains both) => dim H(K') = 0 EXACT : True
C-A  sd*_0 r*_0 = 1·I : True ; sd*_1 r*_1 = 1·I : True ; sd*_2 r*_2 = 2·I : True
C-B  d_0'^T r*_1 = r*_0 d_0^T (horizontality preserved) : True
C-C  r*_1 d_0 = d_0' r*_0 : FALSE — exact witness: child edge ((0, 0, 0, 0), (0, 0, 1, 1)) , parent vertex (0, 0, 0, 0) : LHS 0 != RHS -1 (r* is NOT a cochain map on this generator)
C-D  (i) Q'_flux r_F^* = r*_1 Q_flux on all 17 basis fluxes : 17/17   [Fraction-exact end-to-end]
C-D  (ii) sd*_2 r_F^* = m_1·I = 1·I on all basis fluxes : 17/17

================ A2-B ================
census V'/E'/F' = (81, 544, 1232)  sealed (81, 544, 1232)  -> MATCH
CONTROL  d_1' d_0' = 0 entrywise: True
CONTROL  d_0-square  sd*_1 d_0' = d_0 sd*_0 : True
CONTROL  d_1-square  sd*_2 d_1' = d_1 sd*_1 : True
CONTROL  rank_p d_0' = [80, 80, 80] (V'-1 = 80); rank_p d_1' = [464, 464, 464] (E'-V'+1 = 464)
         sandwich (rank_Q d_0' <= V'-1 from constants; rank_Q d_1' <= E'-(V'-1) from d_1'd_0'=0; rank_p attains both) => dim H(K') = 0 EXACT : True
C-A  sd*_0 r*_0 = 1·I : True ; sd*_1 r*_1 = 2·I : True ; sd*_2 r*_2 = 8·I : True
C-B  d_0'^T r*_1 = r*_0 d_0^T (horizontality preserved) : True
C-C  r*_1 d_0 = d_0' r*_0 : FALSE — exact witness: child edge ((0, 0, 0, 0), (0, 0, 0, 2)) , parent vertex (0, 0, 0, 1) : LHS 1 != RHS 0 (r* is NOT a cochain map on this generator)
C-D  (i) Q'_flux r_F^* = r*_1 Q_flux on all 17 basis fluxes : 17/17   [Fraction-exact one side + exact mod-p at [1000003, 1000033, 1000037] (uniqueness certified)]
C-D  (ii) sd*_2 r_F^* = m_1·I = 2·I on all basis fluxes : 17/17

================ NET ================
ALL CONTROLS AND CERTIFICATES (C-A, C-B, C-D, controls, ranks, censuses) LANDED : True
C-C is reported per generator above (an honest trichotomy, not a pass/fail gate).
```

---

## FLAG BLOCK

```text
NEXT_OBLIGATION_IDENTIFIED = O7/RA27-4 FIELD-SIDE LEG r_F^* (step 2 of the
  POSED ordering — the AND-class member-independence certificate — found
  ALREADY BUILT AND CONFIRMED at path during the pre-build sweep: 38bbb9fc
  PHI_H=IDENTICAL / PHI_F=NEVER / AND_CLASS=CONSTANT_FALSE; check c0e7192b
  CONFIRMED; both seals verified; not rebuilt. First-buildable audit at bytes:
  POSED O7 row [17736,18509); D012 RA27-4 row [45008,45355) + §2.6.3 square
  shape [31536,33632) typing r_F^* : F_phys(K) -> F_phys(R) as unbuilt; V011
  OBL-D clauses [47025,47247); G1-CERT §4.1 [13899,15572) OBL-D CONFIRMED OPEN,
  riding RA27-4/RA27-6; eta_resp and the named response topology are the exact
  blockage of the FULL square — named, not worked around.)

RFSTAR_CERTIFICATE = BUILT(exact, per licensed generator L_id/A0/A1/A2-F/A2-B,
  composites formal via the sealed composite-closure strength:
  C-A sd*_0 r*_0 = I, sd*_1 r*_1 = m_1 I, sd*_2 r*_2 = m_2 I with exact
      multiplicities (m_1,m_2) = (1,1)/(1,1)/(2,4)/(1,2)/(2,8);
  C-B d_0'^T r*_1 = r*_0 d_0^T entrywise over Z on all five (horizontality
      preserved);
  C-C r* is NOT a cochain map on any proper refinement — exact witnesses
      displayed (A1 half-edge/vertex 1 vs 0; A2-F diagonal 0 vs -1; A2-B
      half-edge 1 vs 0); TRUE only on the m = 1 relabelings — the naive
      potential-pullback route to RA27-4's square is DEAD of this certificate;
  C-D r_F^* := d_1' r*_1 Q_flux : F_phys(K) -> F_phys(K'):
      (i)  Q'_flux r_F^* = r*_1 Q_flux — ZERO naturality defect — proved from
           C-A + C-B + dim H = 0 (of record, re-certified at three primes) +
           the sealed d_1-square, and verified end-to-end on the full 17-dim
           flux basis: 17/17 on all five generators (Fraction-exact; A2-B
           mod-p at 1000003/1000033/1000037 with certified uniqueness);
      (ii) sd*_2 r_F^* = m_1 I — 17/17 Fraction-exact on all five; r_F^*
           injective.
  CONTROLS all MATCH: censuses; d_1 d_0 = 0; the sealed d_0-square and
  d_1-square reproduced entrywise (the latter validating the reconstructed
  sd*_2 signs); rank sandwich at three primes reproducing dim H = 0 on all
  five carriers.)

MEMBER_INDEPENDENCE = CARRIED + PRESERVED (step-2 verdict of record:
  AND_CLASS = CONSTANT_FALSE, no devolution to member selection; this step
  consumes NO variety member, F-rule, J_1 value, or freedom value — 807's
  variable inventory [6502,7613) contains no chain-transport variable — so the
  r_F^* leg is member-free by construction).

OBLD_CONSEQUENCE = CLAUSE 1 POSABLE + SEALED-KINEMATIC HALF DISCHARGED AT THE
  LICENSED-GENERATOR QUANTIFIER (the response map's sealed factor Q_flux
  commutes with the candidate pullback with zero defect; the remaining
  clause-1 load is exactly eta_resp + the Gate-5 kernel C, where G1-CERT's map
  places it); CLAUSE 2 = OPEN (its exact finite skeleton displayed: the
  m_1 vs m_2 level mismatch is why no intensive normalization of r* is a chain
  map; consistent with 807's forced 1/16, 1/24, 1/384; not adjudicated);
  FULL-D3 GAP = OPEN AND CARRIED (9a0c2045 frozen reading, star-refined atoms
  of unbounded facet count; C_ref = frozen AUDIT class, trap T1; loop-carrying
  H^1 > 0 carriers outside the licensed inventory — extending there is a new
  pose, not taken; V011's regression-fixture warning quoted and honored — the
  certificate adds the simplicial + barycentric families beyond one hypercubic
  sequence, and proves NO universality).

VERDICT = BUILT(the r_F^* certificate package, with exact quantifiers: five
  exhibited licensed generators, full 17-dim flux basis, formal composites) —
  the FULL RA27-4 square remains UNBUILT with its blockage named (eta_resp
  ABSENT; response topology UNNAMED; both re-verified open of record at
  G1-CERT §4.1 on 2026-08-14); nothing here decides OBL-D, RA27-6, the
  materiality fork, the winding, or any consumer question.

SWEEP_CUTOFF_DECLARED = 2026-08-14 19:18:08 CDT (first sweep 18:55:34; both
  corpus roots; same-night parallel S9AD lanes landing on other lineages noted;
  post-cutoff landings outside this sweep by the tasking's rule).

SEALS_VERIFIED = 17 files at path (list §0), 18 byte spans re-extracted and
  rehashed; V011 = packet member aa7c6d49 only (trap T6 honored); no seal
  failed.
SCRIPT_REPRODUCED = yes (verbatim §5; output verbatim §6; exit 0; fresh venv
  Python 3.9.6; stdlib only; no floats anywhere; one disclosed solver bugfix,
  CH-7).
FORBIDDEN_IMPORTS = none (no scale, no imported GR, no faithfulness, no
  continuum object, no measured constant; no member binding; no fixed-point
  execution; no end test).
FENCES = alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false ; no file matching register|road_|ledger|lens|
  plan|tracker|THE_HANDOFF opened; no git; ONE output + seal sidecar at the
  commission-distinct path, probed ABSENT first.
ALL_RESULTS = CLAIMED until checked.
```
