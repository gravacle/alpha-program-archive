# DEFAULT-REFUTE AUDIT OF THE DISCREPANCY-COCYCLE DETERMINATION — COMMISSION O38SR

AUDIT LANE OF A COMMISSIONED PAIR — 2026-08-15
DEFAULT VERDICT IS REFUTED. THE BUILD'S ARTIFACT STANDS ONLY WHERE IT COULD NOT
BE BROKEN AT BYTES. TESTIMONY CARRIED ZERO WEIGHT: EVERY LOAD-BEARING STATEMENT
BELOW WAS RE-DERIVED FROM THE PRIMARY SOURCE'S OWN BYTES BY THIS LANE.
NOTHING IS ADOPTED, ADVOCATED, RECOMMENDED, OR SUPPLIED. NO MISSING OBJECT,
PRINCIPLE, OR CLAUSE IS AUTHORED ANYWHERE BELOW — NOT IN A TOY, A FOOTNOTE, OR
AN ASIDE. GAPS ARE DESCRIBED, NEVER FILLED.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

TARGET UNDER AUDIT:
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_DISCREPANCY_COCYCLE_O38SR_V001.md`
SHA-256 recomputed by this lane: `d8da07ffefc6433cf66d9432bb33bdf8c720785d520c911f2de819e9f5c677d4`
— matches the build's reported digest. Both sidecars verify `OK` from the
artifact's own directory. The target is intact and is the artifact the build
described.

CORPUS (only readable surface, both roots swept by this lane):
```text
ROOT1 = /Users/bgm/MB Work/alpha-program-archive/workspace
ROOT2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
```

**QUOTATION CONVENTION, DECLARED ONCE.** Every corpus quotation below is wrapped
in a **four-backtick** fence so that the SOURCE artifact's own three-backtick
`` ```text `` delimiters can be reproduced verbatim inside it. No delimiter line
and no word is dropped from any quoted span. Line ranges are stated as the
actual first and last physical lines of the quoted text, checked against
`awk`-numbered output.

---

## VERDICT AT A GLANCE

```text
D1  NONVANISHING (Q1)      CONFIRMED-WITH-CORRECTIONS
D2  DOMAIN COMPARISON (Q2) CONFIRMED-WITH-CORRECTIONS
D3  SUPPORT LAW (Q3)       CONFIRMED-WITH-CORRECTIONS
D4  ADOPTION STATUS (Q4)   CONFIRMED-WITH-CORRECTIONS
D5  REACH (Q5)             CONFIRMED-WITH-CORRECTIONS
D6  BARS                   CONFIRMED (no violation found)

OVERALL                    CONFIRMED-WITH-CORRECTIONS
```

Every one of the build's six answer-grades survives re-derivation at bytes. None
was overturned. What did not survive is a set of **universal negatives**, one
**sweep universe**, one **declared undecidability**, one **supersession trace**,
and a layer of **citation ranges** — eleven corrections, listed in severity
order in the CORRECTIONS block below.

---

## §1 — D1: NONVANISHING (Q1)

### §1.1 THE DEFINING SITE, RE-DERIVED AT BYTES — HOLDS

`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md` (seal verified
`OK` from its own directory), `:242-254`, quoted WHOLE including every clause
cutting against a finding of emptiness, and including the source's own fences:

````text
Define the response/contact transport `rho_f^C2` by the same test/current
pullback, including the endpoint and bundle data supplied by DoR-020-A1.
Its discrepancy cocycle is

```text
beta_f := rho_f^C2(C2_m^fin) - C2_n^fin = 0       (C1-7)
```

on the old image. For a cycle-creating arrow, `(C1-7)` says the old response
restricts exactly; it says nothing false about the target-only new-cycle
component. The latter remains visible in `C2_m^fin`. The local orthogonal
excision certificate ensures contact and disjoint support pieces do not leak
into the old response coordinate.
````

Line `:247` carries the `= 0` inside the defining display. The scope clause "on
the old image" is on line `:250`. The build's account of this site is exact and
its line range is correct.

### §1.2 CORRECTION — THE ZERO CENSUS IS AN UNDERCOUNT

The build states, §1: "Three independent sites evaluate it. All three give
zero." At bytes there are more. **Six** sealed ROOT1 artifacts display a zero for
this object:

| # | Artifact | Line | Display |
|---|---|---|---|
| 1 | `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md` | `:247` | `beta_f := rho_f^C2(C2_m^fin) - C2_n^fin = 0       (C1-7)` |
| 2 | `...HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001.md` | `:217`, `:220` | `beta_nm=0` via `(R1-2)`; `beta_nl=0=beta_ml+eta_ml(beta_nm).                 (R1-7)` |
| 3 | `...HOSTILE_WITNESS_CROSS_CHECK_LANE1_V001.md` | `:186` | `beta_nn=0.` |
| 4 | `STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md` | `:442`, `:448` | `beta_nm:=Kern_m^fin(j_nm^H H)-Pull_nm(Kern_n^fin H)=0. (Q2-16)`; `beta_nl=0=beta_ml+eta_ml(beta_nm).                (Q2-17)` |
| 5 | `STAGE8_TASK5_EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001.md` | `:412` | `beta_nl=0=beta_ml+eta_ml(beta_nm).` |
| 6 | `...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md` | `:1109`, `:1112`, `:1117` | `beta_nn=0.                                        (W-13)`; "Equation `(W-12a)` gives `beta_nm=0` for the witness."; `\|\|beta_nm\|\|/Vol_m=0.                              (W-14)` |

Sites 4, 5 and 6 are absent from the build's artifact entirely. All six agree on
zero, so the **grade is unaffected and is in fact better supported than the
build reported**. The correction is to the census, not to the verdict.

Site 4 carries its own limiting clause, and it is quoted WHOLE here because it
cuts both ways — `STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md:451-460`
(seal `OK`):

````text
Thus the cocycle law holds exactly.  The generated contact ideal is `{0}`;
it deletes no visible bulk cycle.  Reality, orientation, batching, and
restriction are inherited from the physical square.  The algebraic
discrepancy is identically zero, but the physical volume-normalized
subextensive statement is only conditional until the missing P4 density is
supplied; this subpackage does not call `0/Vol_R` defined before `Vol_R`
exists.

This is not a blanket declaration that every future boundary discrepancy
vanishes.  It is a computed statement on the exact subdivision subcategory.
````

### §1.3 CORRECTION, SEVEREST — THE UNIVERSAL NEGATIVE IS FALSE AT BYTES

The build, §1.6, states without qualification:

> **Nonzero exhibition:** NONE, in either root. No worked instance, no example,
> no lower bound, no nonvanishing proof, no statement that it is nonzero in any
> case.

**Two of those clauses are refuted at bytes.** This lane ran patterns the build
did not run. The build's S-33 tested `beta != 0` and `beta_f != 0`, both of
which require the literal token `beta`/`beta_f` immediately before a spaced
`!=`. The corpus writes its nonzero instances differently, and they are not
reached by either pattern.

**(a)** `STAGE8_TASK5_V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001.md`
(seal verified `OK` from its own directory), `:477-490`, quoted WHOLE — heading,
set-up, display, and the paragraph after it, so that the subjunctive framing is
not lost and no clause is elided:

````text
### 3.7 Independence proof 3: refinement does not imply response/boundary coherence

Take a refinement diamond with two composite paths from `G_0` to `G_2`.
Let all geometric/current maps exist and commute, but assign actual boundary
discrepancies whose cocycle sums are

```text
beta_path_1=0,
beta_path_2=b != 0.                                (P2-9)
```

No single restriction-natural cocycle can satisfy both path values.  The
primitive physical refinements and upper kernel estimates can all exist;
J12/J13 still have an empty solution fiber.
````

**(b)** `STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md:470-484` (seal `OK`),
quoted WHOLE through the closing fence of the block it introduces:

````text
A common refinement involving a new edge can have two paths through the
refinement category.  No actual response transport or boundary discrepancy
exists before the new physical maps exist.  Even after P4/P5, cocycle
compatibility is independent: a diamond with path sums `0` and `b!=0` has
no response-natural solution.

The full witness therefore still needs:

```text
nonempty response naturality solution;
actual beta_R on every primitive;
path-independent cocycle proof;
safe closed contact ideal;
subextensive boundary theorem in the same tau_ker. (Q2-18)
```
````

**(c)** `STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md:808-829`
(seal `OK`), quoted WHOLE from the introducing sentence to the sentence that
types the block:

````text
The geometric common-refinement, boundary, and completed-faithfulness tests
are not finitely executable because their objects do not exist.  Their
failure-capable tests are:

```text
REFINEMENT_TEST:
  two same-region cellulations disagree after common-refinement pullback;

DENSITY_TEST:
  composition or orientation changes the normalized current;

RESPONSE_TEST:
  Eta_(S compose R)!=Eta_S Eta_R or the W3/DoR-008 square fails;

BOUNDARY_TEST:
  normalized beta does not vanish or deletes a visible bulk cycle;

FAITHFULNESS_TEST:
  a nonzero completed profile has Kernbar(H)=0.       (F4-5)
```

Each test is typed `NOT_YET_EXECUTABLE / TYPE-U`, not passed by absence.
````

**(d)** `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md:1120-1125`
(seal `OK`), quoted WHOLE:

````text
The zero ideal is closed, invariant, restriction-natural, and has zero
intersection with every bulk profile.  Consequently no record-visible bulk
cycle is called contact.  Equations `(W-11)`-`(W-14)` prove J12/J13,
including path independence, in the single topology `tau_PLDEC`.  Nonzero
boundary-cocycle alternatives remain in the choice table; none is smuggled
into this witness.
````

### §1.4 WHY THE GRADE NEVERTHELESS SURVIVES

The commission's own D1 rule governs: an instance counts only if it is
"genuinely nonzero and genuinely `beta` — not a differently-named object, not a
hypothetical, not inside a subjunctive or a refused branch." Applied at bytes:

- **(a)** is inside a subjunctive independence construction. Its governing verbs
  are "Take a refinement diamond" and "assign actual boundary discrepancies". It
  exists to show a solution fiber is empty, not to exhibit a record value.
- **(b)** is the same construction in a second artifact, introduced by "can
  have" and "a diamond with path sums".
- **(c)** is the failure condition of a test the corpus expressly declares not
  executable, typed `NOT_YET_EXECUTABLE / TYPE-U` and "not passed by absence".
  It states what would falsify, not what holds.
- **(d)** asserts nonzero alternatives "remain in the choice table". This lane
  read that artifact's choice table at `:493-503`; its P7 row reads
  `quotient contact ideal with (V2-J13); invariant direct-sum ideal with
  (V2-J13); reject`. No nonzero cocycle is displayed there. Whether the sentence
  at `:1124` refers to a row not displayed is **INDETERMINATE-AT-BYTES**; this
  lane does not resolve it and authors nothing to close it.

So **no actual, record-native, nonhypothetical nonzero `beta` is exhibited**, and
the build's grade holds. What fails is the build's *phrasing*: "no example" and
"no statement that it is nonzero in any case" are both false of the bytes at
`...V005_FINAL_CHECK...:485` and `...THE_WITNESS_HUNT...:473-474`. A
default-refute lane must say so.

### §1.5 THE OTHER TWO STANDING NEGATIVES — RE-DERIVED AND HELD

`STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:559-575`, quoted WHOLE across both halves
of its TOY_SEPARATION block so no clause is lost (the build cites `:564-570`;
the text runs `:564-572`, and this lane extends the quote to the block's own
boundaries):

````text
ACTUAL SURFACE OF THE RECORD (what this determination touched):
  g_ij = exp(i theta_ij) in U(1) and its triple-overlap cocycle, as quoted
    inside two sealed artifacts;
  u_c, the conserved path current on connection variations, and the finite
    Ward identity (B2-3);
  beta_nk, the direct/two-step discrepancy cocycle, and the J13 status rows;
  H_source tensor H_record tensor H_boundary/environment;
  O6SR's COUNTERFACTUAL B (O6-1d);
  M-1 unmatched carriers / M-2 overlapping-not-identical cells / M-3 adjacency.

NOT SURFACE — NOT TOUCHED, NOT BUILT, NOT SUPPLIED:
  no patch family, cover, nerve, or topology was constructed for U_i;
  no transfer of the Cech/nerve construction to the Gate-4 incidence graph;
  no discrepancy family was supplied for beta to range over;
  no content, dimension, algebra, state, or dynamics for the environment factor;
  no object joining FP-3's escape to the mismatch-content typing;
  no gauge lift was selected anywhere.
````

`STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005.md:482` (seal `OK`;
own header `Standing: **PROPOSED_NOT_ADOPTED**; DoR-020 reserved`, verified at
`:7`), quoted WHOLE:

````text
| J13 | boundary/contact cocycle and safe ideal | **CONDITIONAL THEOREM ONLY** | closure hull `(O1-13)` preserves a supplied cocycle; no actual P7 discrepancy family exists to close |
````

Both hold exactly as the build reported.

### §1.6 D1 GRADE

```text
D1_GRADE = CONFIRMED-WITH-CORRECTIONS

Q1_GRADE, CORRECTED AND RESTATED:
  PROVEN-ZERO at every site where the corpus actually evaluates the object —
  SIX sealed artifacts, not three — with scope stated by the corpus itself as
  "on the old image"; AND nonzero written in the corpus in exactly two places,
  both inside subjunctive independence constructions, plus one unexecuted
  failure-test condition and one unlocated reference to "nonzero
  boundary-cocycle alternatives". NO ACTUAL NONZERO INSTANCE OF RECORD.
```

---

## §2 — D2: DOMAIN COMPARISON (Q2)

### §2.1 BOTH DOMAINS RE-DERIVED FROM THEIR DEFINING TEXT

`beta_f`'s index set — `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:69-81`
(the build cites `:68-79`; the text is at `:69-81` — see COR-F), quoted WHOLE
with the source's own fences:

````text
`I_F` is exactly the exhibition's full actual finite consumer/refinement
category. Its objects include all sealed active stages and every covariance,
reality, frame, bundle-gauge, orientation, Ref_path, flip, common-refinement,
and finite-consumer image. Its arrows are generated by:

```text
(i)   identities and admitted surface isomorphisms;
(ii)  W3/DoR-008 finite restrictions;
(iii) rank-preserving physical path subdivisions;
(iv)  cycle-creating flip attachments;
(v)   actual disjoint/contact common refinements;
(vi)  every finite-bottom arrow used by a sealed consumer.
```
````

`g_ij`'s index set — `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:18-43` (the build
cites `:19-38`; the text is at `:18-43`, and beginning at `:19` would cut the
first sentence mid-clause — see COR-F), quoted WHOLE with the source's own
fences reproduced:

````text
The new principle places one primitive record degree on every admissible
causal record cell. Its physical state is a ray in the primitive complex
two-dimensional carrier. On a local patch `U_i`, choose a normalized lift

```text
z_i : U_i -> C^2,
z_i^dagger z_i = 1.
```

Two lifts represent the same local ray exactly when, on an overlap,

```text
z_j = g_ij z_i,
g_ij = exp(i theta_ij) in U(1).
```

On triple overlaps, equality of the represented ray requires

```text
g_ij g_jk g_ki = 1.
```

The local lifts and transition functions therefore define a complex line
bundle, equivalently a principal `U(1)` comparison bundle. This `U(1)` is a
local representative redundancy of the adopted projective record field. It
is not obtained by mistaking a passive basis change for a new physical force.
````

This lane confirms independently that `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md`
**has no sidecar in either root**: `find -iname "LOCAL_PROJECTIVE*"` over both
roots returns the two `.md` copies and two unrelated `results/*.json` files, and
no `.sha256` of any kind. The build's flag on this is accurate, and the quote it
drew from the unsealed file is reproduced above from the same unsealed bytes,
with the same flag.

### §2.2 THE SPECIFIC HUNT D2 DEMANDS — IS ONE CONSTRUCTED FROM THE OTHER?

Run by this lane, and negative, on evidence the build did not display.

**Co-occurrence test.** ROOT1 files containing both the refinement category
`I_F` (matched on its backticked form) and any of `patch famil` / `U_i` /
`nerve`: the raw intersection returns one filename plus four `evidence/`
duplicates of it; opened at the hit line, the `I_F` match is a substring inside
an unrelated filename in a citation list, not the category. **No corpus artifact
places `I_F` and a patch family in contact.**

The corpus's own attempt at the bridge —
`STAGE8_CANONICAL_PATCH_COVER_TO_INCIDENCE_GRAPH_FUNCTOR_ATTEMPT_V001.md:19-43`,
quoted WHOLE from the verdict heading through the closing fence of its status
block:

````text
## Lead verdict

The nerve construction gives a **candidate data map per chosen cover**, but it
does **not** build the canonical functor the program needs.

The obstruction is the cover selector and refinement law:

```text
CANONICAL_PATCH_COVER_TO_INCIDENCE_GRAPH_FUNCTOR_BUILT = false | TYPE-U |
would-build: a target-independent distinguished-cover selector, or an
equivalent theorem proving that the PRPS/LPRB cover nerve is canonically the
Gate-2/Gate-4 rooted incidence graph, independent of cover, lift, path
representative, ordering, and refinement.

NERVE_ONE_SKELETON_CANDIDATE_PER_COVER = true | IMPORTED_GENERAL_TOPOLOGY |
scope: given a fixed indexed cover, its Cech nerve has vertices = patches and
edges = nonempty pairwise overlaps; transition functions form U(1)-valued
1-cochain data.

NERVE_CANDIDATE_TRANSFERS_CANONICALLY_TO_THIS_STRUCTURE = false | TYPE-U |
would-build: a proof that the imported Cech/nerve construction applies to
the PRPS endpoint-comparison patches and lands in the fixed Gate-4 incidence
graph without introducing a nonsealed choice of cover, lift, path
representative, ordering, or refinement.
```
````

Neither domain is constructed from the other. The build's DIFFERENT-OBJECT
verdict is re-derived and **holds**, and it is reached from displayed defining
text, not from names.

The missing patch definition, quoted WHOLE this time —
`STAGE8_RECORD_SIDE_PATCH_DEFINITION_SWEEP_V001.md:42-45` (seal `OK`); this is
the complete sentence the build cut off at line 42:

````text
It does not have the missing object: a definition of what the patch family
`{U_i}` is, what space it covers on the record side, what topology or smooth
structure makes the `U_i` open/smooth patches, or which cover is the PRPS
endpoint-comparison cover.
````

### §2.3 CORRECTION — THE TYPING CLAIM IS NOT UNIVERSAL AT BYTES

The build, §2.1, states:

> `beta_f` is therefore **not** a map with its own source and target. At bytes it
> is an **element** of the stage-`n` C2 datum type, indexed by the arrow `f`.

For the token `beta_f` at `(C1-7)` that reading is sound: the display subtracts
`C2_n^fin` from `rho_f^C2(C2_m^fin)`, two objects of one datum type.

But the corpus carries a **second defining display of the same discrepancy
cocycle**, which the build did not find, and it types the object the other way.
`STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md` (seal verified
`OK`; own header `Standing: **PROPOSED_NOT_ADOPTED**; DoR-020 reserved` at
`:7`), `:1088-1118`, quoted WHOLE from the sentence introducing the response
transport through the closing fence of the volume display, so that no
qualifying clause is lost:

````text
Define the connected response on the nested cylindrical images by exact
inductive transport,

```text
Resp_m compose S_nm=eta_nm compose Resp_n,        (W-12a)
```

Initialize `Resp_1` with the independent sealed finite connected-response
bottom leg, and put the new orthogonal detail sector on the local DEC block of Section
8.5.  The canonical multiresolution splitting of the edgewise divisibility
system makes this definition independent of a factorization of `m/n`.
For the connected response, define its refinement discrepancy

```text
beta_nm:=Resp_m compose S_nm-eta_nm compose Resp_n. (W-12)
```

Associativity and `(W-11)` compute, rather than assume,

```text
beta_nk=beta_mk compose S_nm+eta_mk compose beta_nm,
beta_nn=0.                                        (W-13)
```

Equation `(W-12a)` gives `beta_nm=0` for the witness.  Hence the closed
invariant boundary/contact ideal is the lawful zero ideal and, in every
edgewise `d`-refinement,

```text
||beta_nm||/Vol_m=0.                              (W-14)
```
````

Three consequences at bytes:

1. `(W-12)` is a **defining display with `:=` and no `= 0` on the defining
   line**. The zero arrives one paragraph later, at `:1112`, and is scoped "for
   the witness".
2. `(W-12)` defines the discrepancy as a **difference of two morphism
   composites**, `Resp_m compose S_nm` minus `eta_nm compose Resp_n`. That is a
   map-typed difference, not an element of a datum type. The corpus therefore
   types this object **both ways, in two different artifacts, and does not
   reconcile them**.
3. `(W-13)` at `:1108-1109` is **the source of the typed form** that
   `...HOSTILE_WITNESS_CROSS_CHECK_LANE1_V001.md:181` attributes to "W-13". The
   build quoted that attribution and never located W-13. It is here.

This does not overturn the DIFFERENT-OBJECT verdict — neither typing brings
`beta` anywhere near a patch family. It corrects the build's claim that the
element typing is what the bytes say, full stop.

### §2.4 THE ELEMENT-APPLIED USAGE — RE-DERIVED AND HELD

`beta_N([r,b])` occurs at `...LANE1_V003.md:412` and nowhere else in the
element-applied sense. Rather than guess patterns, this lane **extracted every
`beta`-prefixed token in ROOT1** and counted them: `beta_N` returns 34
occurrences across 15 files; opening them reproduces the build's finding that
all but `:412` are unrelated homonyms in other artifacts. **The corpus writes no
bridge between `beta_N` and `beta_f`.** Held.

One homonym the build's census omits, and it is in the defining artifact itself
— `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:285-289`, quoted WHOLE:

````text
```text
Kern_n^fin(H_n)(u_n,d beta_n)
 = Kern_m^fin(j_f^H H_n)(S_f^J u_n,d(j_f beta_n))
 = 0.                                             (C1-9)
```
````

`beta_n` here is an argument whose differential is paired in the finite Ward
annihilation, not a discrepancy. The build's homonym sentence is scoped to
`beta_N`/`beta_NM` "in a different artifact", so it is not falsified in letter;
the lowercase homonym inside the defining artifact is nonetheless unlisted.

### §2.5 D2 GRADE

```text
D2_GRADE = CONFIRMED-WITH-CORRECTIONS

Q2 VERDICT, CORRECTED AND RESTATED:
  DIFFERENT OBJECT — held, and independently re-derived from both defining
  texts, plus a negative I_F-vs-patch co-occurrence test and the corpus's own
  failed bridge attempt (`..._FUNCTOR_BUILT = false | TYPE-U`). Neither domain
  is constructed from the other, and the identification is stated nowhere.
  CORRECTED: the object's TYPE is not settled as "element" at bytes — a second
  defining display, (W-12), types it as a difference of morphism composites,
  and the corpus does not relate the two typings.
```

---

## §3 — D3: THE SUPPORT LAW (Q3)

### §3.1 THE WRAP CHECK, RE-RUN INDEPENDENTLY — THE BUILD'S NUMBERS ARE EXACT

This lane measured the physical line lengths of the display itself with `awk`
`length($0)`, without reference to the build's table:

```text
LINE 389  ```text                                                        len 7
LINE 390  Eta_g Eta_f q_N = Eta_gf q_N,                                  len 29
LINE 391  beta_gf          = beta_g + Eta_g(beta_f),                     len 42
LINE 392  Res_f Eta_f       = id on each licensed old-image scope,       len 56
LINE 393  Theta_M Eta_f    = Eta_f Theta_N,                              len 33
LINE 394  Ward_M Eta_f     = Ward_N,                                     len 26
LINE 395  supp(Eta_f x)    subset f(supp x) union supp(beta_f).          (J13-1)   len 70
LINE 396  ```                                                            len 3
```

**Every value matches the build's table exactly.** Line 395 terminates in a
period before its label, and line 396 is the closing fence. **(J13-1) IS
COMPLETE ON ONE PHYSICAL LINE. There is no continuation, no truncation, and no
fabrication.** The build's line-wrap finding is confirmed at bytes by an
independent measurement.

The surrounding derivation, `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:386-406`
— quoted WHOLE by this lane, and carried **five lines further than the build
carried it**, through `:406`, because the paragraph at `:403-406` scopes one of
the displayed equalities and the build's quote stopped at `:401`:

````text
More explicitly, with `q_N` the quotient map and with all expressions on their
declared scopes, the induced data satisfy

```text
Eta_g Eta_f q_N = Eta_gf q_N,
beta_gf          = beta_g + Eta_g(beta_f),
Res_f Eta_f       = id on each licensed old-image scope,
Theta_M Eta_f    = Eta_f Theta_N,
Ward_M Eta_f     = Ward_N,
supp(Eta_f x)    subset f(supp x) union supp(beta_f).          (J13-1)
```

The first equality was proved in (J12-4).  For each remaining equality, both
sides agree after precomposition with the represented injection and with the
contact injection.  They also agree on `C_N` by (J12-1).  The pushout universal
property therefore makes the equality hold on all of `P_N`.

Here `Res_f` denotes only the independently sealed response restriction on
the scope where it exists.  It is not inferred from an adjoint compression
and is not an upward map.  Outside that licensed old-image scope, no inverse
claim is made.
````

The extra paragraph does not bear on `supp(beta_f)`; it scopes `Res_f`. Reported
because the build's span stopped short of it.

### §3.2 THE SWEEP, RE-RUN INDEPENDENTLY — CONFIRMED EXACTLY

`supp(beta` swept by this lane over both roots with barred globs applied:

```text
ROOT1  2 files / 8 hits
  ...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:395      (the law itself)
  STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001.md:388,403,411,912,1160,1241,1300
ROOT2  1 file / 1 hit
  ...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:395
```

All nine hits opened. The seven O32SR-audit hits quote or discuss `:395`; none
computes, bounds, localizes, or places `supp(beta_f)`. **The build's counts are
exact and its NEVER-CONSTRAINED grade is re-derived and holds.**

### §3.3 CORRECTION — THE DECLARED UNDECIDABILITY IS SPOKEN TO BY BYTES THE BUILD DID NOT SWEEP

The build, §3.1, discloses:

> ONE AMBIGUITY, REPORTED RATHER THAN RESOLVED:
>   The label `(J13-1)` is placed at the end of a SIX-LINE display. The bytes do
>   not disambiguate whether it labels the whole display or only its last line.
>   This artifact does not decide it, and nothing below depends on which reading
>   is correct.

The disclosure is honest, but the claim that "the bytes do not disambiguate" is
made without running the pattern that reaches them. The build's SWEEP CUTOFFS
carry no row for `J13-1`; its S-12 row (`J13`) says it "Narrowed to
law-statement and status contexts" and then **reports only the status rows**.
This lane ran `J13-1`:

```text
J13-1   ROOT1  6 files / 12 hits      ROOT2  3 files / 3 hits    leak = 0
```

Opened at every hit. The corpus attaches the label `(J13-1)` to **two different
equations in two different artifacts**:

`STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md:445-463` (seal
`OK`), quoted WHOLE — the whole J13 clause, both displays, and the closing
sentence:

````text
### J13. Boundary/contact coherence

Let `beta_R` be the boundary/contact discrepancy along a refinement.  Path
independence forces the cocycle law

```text
beta_(S compose R)=beta_S+Eta_S(beta_R),            (J13-1)
```

up to the declared sign convention.  `Bound` must prove in the one topology

```text
beta_R / Vol_R -> 0 on every admitted cofinal family,
I_contact is closed, invariant, restriction-natural, and contains no
record-visible bulk cycle class.                    (J13-2)
```

The quotient/direct-sum member and its signs must be stated.  Calling every
off-diagonal path kernel `contact` fails `(J13-2)`.
````

Read at bytes: in this artifact `(J13-1)` is **the composition law**, `(J13-2)`
is the subextensivity-plus-ideal block, and **there is no support law in the J13
clause at all**.

Against that, `STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001.md:1298-1302` reads the
label the other way, and is quoted WHOLE:

````text
                (1) The disqualifier "no support, no locus, no carrier, and no
                    place in any expression" is FALSE OF THE CORPUS.  The record
                    names a "discrepancy cocycle" beta_f, writes supp(beta_f),
                    uses it as a term in the sealed inclusion (J13-1), and gives
                    it the composition law beta_gf = beta_g + Eta_g(beta_f).
````

**Stated plainly:** the corpus does not use `(J13-1)` consistently. One sealed
artifact labels the composition law with it; one sealed artifact labels the
support inclusion with it; and a prior audit distinguishes "the sealed inclusion
(J13-1)" from "the composition law" as two things. Which of these the label
means **in V003's six-line display** remains **INDETERMINATE-AT-BYTES**, and
this lane does not resolve it and authors nothing to close it. The correction is
that the build declared the bytes silent on the question when three sealed sites
speak to it and were never swept.

**This does not disturb the Q3 grade.** Whatever `(J13-1)` labels,
`supp(beta_f)` is written at exactly one line of the corpus and is never
computed, bounded, localized, or tied to any boundary, seam, or overlap. The
build's reading of what the inclusion bounds — an additive allowance on how far
support may spread beyond `f(supp x)` — is a faithful description of the
displayed inclusion, and this lane could not break it.

### §3.4 D3 GRADE

```text
D3_GRADE = CONFIRMED-WITH-CORRECTIONS

Q3_GRADE, UNCHANGED: NEVER-CONSTRAINED.
  supp(beta_f) is written once in the corpus, used as a term in one inclusion,
  and given no extent, bound, locus, or relation to any boundary, interface,
  seam, overlap, or carrier edge. Sweep independently re-run: 2/8 ROOT1, 1/1
  ROOT2, all nine hits opened.
CORRECTED: the (J13-1) label ambiguity the build declared undecidable at bytes
  is addressed by three sealed sites the build never swept; the ambiguity
  survives, but it is now an ambiguity IN THE CORPUS'S USE OF THE LABEL, not an
  absence of evidence.
```

---

## §4 — D4: ADOPTION STATUS (Q4)

### §4.1 EVERY STANDING CLAIM RE-DERIVED — ALL HOLD

Each header block below was opened by this lane in the artifact itself, after
verifying that artifact's seal from its own directory.

| Claim under test | Re-derived at bytes | Verdict |
|---|---|---|
| V003 graded `DEFECTIVE (H1,H5,H6)` | `STAGE8_TASK5_EQ6_STAGE1_V003_REVIEW_LANE2_V001.md:12` — `STAGE1_V003 = DEFECTIVE (H1,H5,H6)` | HOLDS |
| That review nowhere examines `beta` | `J13\|beta\|subextensiv` returns exactly one line in it, `:290` | HOLDS |
| LANE2_V001 graded `DEFECTIVE (V1..V6)` | `STAGE8_TASK5_EQ6_ASSEMBLY_STAGE1_REVIEW_LANE1_V001.md:9` — `STAGE1_ASSEMBLY = DEFECTIVE (V1,V2,V3,V4,V5,V6)` | HOLDS |
| LANE2_V001 called "killed" / "not an authority" | `...EQ6_MEMBERSHIP_THEOREM_LANE3_V001.md:49` and `:382` | HOLDS |
| V004 contains 0 `beta`, 0 `J13` | `rg -c 'beta\|J13'` on V004 returns nothing | HOLDS |
| V004 repairs the J4 face only | V004 `:43` | HOLDS |
| V004 sidecar malformed, digest matches | `od -c` shows ONE space between digest and filename where `shasum -c` needs two; computed digest `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` equals the recorded digest | HOLDS |
| V005 `PROPOSED_NOT_ADOPTED` | V005 `:7` | HOLDS |
| DoR-020-A3/A4 absent from both roots | `find -iname "*DOR_020*"` → 0 in ROOT1, 0 in ROOT2; `-iname "*dor*020*"` → 0 | HOLDS |
| SHA `5fd95472…` appears only as citation | 8 ROOT1 files contain the string; none is the file | HOLDS |
| The four J13 status rows | V005 `:482`; `...PACKAGE_ADJUDICATION_AND_WITNESS_LANE1_V001.md:137`; `...HOSTILE_WITNESS_CROSS_CHECK_LANE1_V001.md:412`; `...THE_WITNESS_HUNT_LANE1_V001.md:604` | HOLDS |
| `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` unsealed | no sidecar of any kind in either root | HOLDS |

**No seal mismatch was found by this lane on any artifact it consumed.**

### §4.2 CORRECTION — THE SUPERSESSION TRACE IS INCOMPLETE

D4 requires the standing of every consumed artifact "including sibling versions
the build did not open." Four exist, all sealed `OK`, none listed in the build's
23-row standing table.

**(i) `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md`** — the
sealed successor of the very artifact that defines `beta_f`. Its header,
`:1-26`, quoted WHOLE:

````text
# STAGE 8 TASK 5 / EQ6 — JOINT FINITE ASSEMBLY STAGE 1 V002 — LANE 2

```text
ARTIFACT_TYPE = FINITE_PHYSICAL_FACE_REPAIR
REGISTER_HEAD_RELAY = Q-459
REGISTER_HEAD_LIVE = Q-460
SOURCE_V001 = STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md
SOURCE_V001_SHA256 = e21cee3081da06417109697352abe570c58dc6dda7d46e78f690d666bed128f5
REVIEW_OF_RECORD = STAGE8_TASK5_EQ6_ASSEMBLY_STAGE1_REVIEW_LANE1_V001.md
REVIEW_SHA256 = 049f6386835adcf5089a74d49add67c76973c5bb89fe3571c1e5fbceb8b0f5df

ACTION_FAMILY_CERT = BUILT
FORWARD_ETA_SQUARE = BUILT
PACKAGE_SQUARE = BUILT
CLASH_FOUND = none

MEMBER_BOUND = false
COMPLETED_AXIOM_INVOKED = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
````

`SOURCE_V001_SHA256` is byte-identical to the digest the build cites for the
definition-carrying artifact. Line `:35`, quoted WHOLE:

````text
The V001 closures are struck. This build accepts the review's distinction:
````

**And V002 contains ZERO occurrences of `beta`** (verified: `rg -n 'beta'`
returns nothing across its 693 lines). So the definition-carrying artifact has a
sealed successor which strikes its closures and does not restate the definition.
The build presented LANE2_V001 as having no successor at all.

**(ii) `STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md`** (seal `OK`), which
grades that successor. `:6-11`, quoted WHOLE:

````text
ARTIFACT_UNDER_REVIEW = STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md
ARTIFACT_UNDER_REVIEW_SHA256 = e5381e6687dbdd5aed792bdddd1050ea7f39d17e748d6c9e374746c4cee37731
PRIOR_REVIEW_STANDARD_SHA256 = 049f6386835adcf5089a74d49add67c76973c5bb89fe3571c1e5fbceb8b0f5df

STAGE1_V002 = DEFECTIVE (Y1,Y2,Y3,Y4,Y5)
J4_J12_J15 = OPEN (J4 finite-coordinate map; J12 contact extension; J15 covariant bottom leg)
````

**(iii) `STAGE8_TASK5_EQ6_STAGE1_V004_REVIEW_LANE2_V001.md`** (seal `OK`) — the
review of record of V004, which the build's chain omits entirely. `:4-14` and
`:22-27`, quoted WHOLE:

````text
ARTIFACT_TYPE = ADVERSARIAL_REVIEW_OF_RECORD
LANE = CODEX_LANE_2
ARTIFACT_UNDER_REVIEW = STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md
ARTIFACT_UNDER_REVIEW_SHA256 = 9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514
REGISTER_HEAD_STATED = Q-472
REGISTER_HEAD_VERIFIED = Q-472
R1_CARRIED = pass
R2_CARRIED = pass
R3_CARRIED = pass
R4_CARRIED = pass
R5_CARRIED = pass
````

````text
```text
DOES_THE_OBJECT_EXIST = yes
IS_THE_VERSION_CURRENT = yes through Q-472
ARE_THE_INPUTS_PRESENT = yes (Q-467/468 history, STAGE3 source chain, prior V003 self-review, corridor artifacts)
NO_CLOBBER = pass (target filename absent locally and in archive)
```
````

That review mentions neither `beta` nor `J13` nor `supp` anywhere in a
`beta`-bearing sense. So the repaired successor V004 is reviewed and passed,
and the review too is silent on the object.

**(iv) `STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V002.md`** (seal `OK`) exists
and supersedes the `LANE3_V001` from which the build drew both "killed"
quotations. This lane swept it for `beta` and for `killed`: **zero hits on
either**. The "killed" language is therefore carried only by the V001 the build
quoted, and its successor does not repeat it.

**Effect on the grade: none, and if anything it strengthens it.** Every one of
these four artifacts is either DEFECTIVE, silent on `beta`, or both.

### §4.3 CORRECTION — V003 DOES NOT CITE THE ARTIFACT THAT DEFINES `beta_f`

`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:49-52`, quoted
WHOLE (the build cites `:48-49` for the two DoR rows; the rows are at `:49` and
`:50`, and `:48` is a different row entirely — see COR-F):

````text
| `DOR_020_A3_J4_RELATIONAL_INCREMENT_GROUPOID_2026-08-04.md` | adopted J4 law | SHA-256 `07e0e50145314fe5c30b7f7b5637d4c8add0834c631ad9c2e16209bf3b5a9d6f` |
| `DOR_020_A4_J12_CONTACT_GLUE_STRICT_PUSHOUT_2026-08-04.md` | adopted J12 law | SHA-256 `5fd95472bd6f0507a371779505fe91e2c3c4657ee3afc664ca563a3743d668a4` |
| `STAGE8_TASK5_EQ6_DETERMINATION_MAP_LANE1_V001.md` | mathematical source for the adopted rows and J15 mate | SHA-256 `76ee3c695b1c0c02986a13ff64d6db93f76e39c6861b40273bd31aed1c3a2eb0` |
| `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md` | represented `Eta` geometry carried forward | SHA-256 `e5381e6687dbdd5aed792bdddd1050ea7f39d17e748d6c9e374746c4cee37731` |
````

Read at bytes: **V003's own authority table names LANE2_V002, not LANE2_V001.**
The artifact that carries the J13 display therefore has **no authority link at
all** to the artifact that carries the only `beta_f` definition. The build's §4.2
did not report this, and it is a strengthening of the build's own conclusion.

One clause cutting the other way, disclosed rather than suppressed:
`STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md:33` consumes LANE2_V001 as a
source, with its digest, quoted WHOLE:

````text
| `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md` | `e21cee3081da06417109697352abe570c58dc6dda7d46e78f690d666bed128f5` | finite C3 datum and its field/test pullback |
````

So the corpus is not uniform: one lane calls LANE2_V001 killed and not an
authority; another lane cites it as an input for the C3 datum. Both are lane
artifacts. Neither is of record. Reported and left as it lies.

### §4.4 D4 GRADE

```text
D4_GRADE = CONFIRMED-WITH-CORRECTIONS

Q4 VERDICT, UNCHANGED AND REINFORCED: NOTHING OF RECORD.
  V003 is a condition-tagged lane build graded DEFECTIVE (H1,H5,H6) by its own
  review; the sole beta_f definition sits in a lane build graded DEFECTIVE
  (V1..V6), called "killed" and "not an authority", AND superseded by a sealed
  repair that strikes its closures and contains no beta at all; the J13 grades
  readable at bytes are CONDITIONAL THEOREM ONLY (in a PROPOSED_NOT_ADOPTED
  artifact), KILL / J13, KILL ON WITNESS, and one Ref_path-scoped affirmative
  stating the object as beta=0; and DoR-020-A4, the cited authority for the J13
  laws, is absent from both roots.
CORRECTED: the standing table is incomplete by four sealed artifacts —
  LANE2_V002, its review, the V004 review of record, and MEMBERSHIP_THEOREM
  LANE3_V002 — and V003's authority table names LANE2_V002, not the
  definition-carrying LANE2_V001.
```

---

## §5 — D5: REACH (Q5)

### §5.1 CORRECTION — THE SWEEP UNIVERSE IS WRONG

The build's token-presence test is headed "TOKEN-PRESENCE TEST, RUN FILE BY FILE
OVER EVERY ARTIFACT THAT CARRIES THE `beta` OBJECT" and lists **six** artifacts.

This lane enumerated the universe rather than assuming it. ROOT1 files carrying
any discrepancy-cocycle token
(`beta_f\b|beta_nm|beta_nk|beta_gf|beta_R\b|beta_path|beta_id|beta_ml|beta_nl`),
barred globs applied, `evidence/` duplicates and `.py` excluded: **20 files**,
not six. The build's universe is missing fourteen, including
`...THE_WITNESS_HUNT_LANE1_V001.md`,
`...CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md`,
`...V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001.md`,
`...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002/V003/V004/V005.md`,
`...EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V002.md`,
`...EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001.md`,
`...EQ6_STAGE1_V002_REVIEW_LANE1_V001.md`, and the three prior audits.

### §5.2 THE TEST, RE-RUN OVER THE CORRECTED UNIVERSE

```text
ARTIFACT                                                W-1 W-2 W-3 FP-1 FP-2 FP-3 G_cm prq svol
STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001                   0   0   0   0    0    0    0   0    0
STAGE8_GAUGE_AS_CONTENT_O34SR_AUDIT_V001                  0   0   0   0    0    2    0   0    0
STAGE8_GAUGE_AS_CONTENT_O34SR_V001                        0   0   0   0    0    6    0   0    0
...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002         35   5   4   0    0    0    0   0    0
...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V003          0   0   0   0    0    0    0   0    0
...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V004          0   0   0   0    0    0    0   0    0
...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005          0   0   0   0    0    0    0   0    0
...CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001             0   0   0   0    0    0    0   0    0
...EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001                0   0   0   0    0    0    0   0    0
...EQ6_DETERMINATION_MAP_LANE1_V001                        0   0   0   0    0    0    0   0    0
...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003             0   0   0   0    0    0    0   0    0
...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001             0   0   0   0    0    0    0   0    0
...EQ6_MEMBERSHIP_THEOREM_LANE3_V001                       0   0   0   0    0    0    0   0    0
...EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V002         0   0   0   0    0    0    0   0    0
...EQ6_STAGE1_V002_REVIEW_LANE1_V001                       0   0   0   0    0    0    0   0    0
...HOSTILE_WITNESS_CROSS_CHECK_LANE1_V001                 20   0   1   0    0    0    0   0    0
...HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001                0   0   0   0    0    0    0   0    0
...PACKAGE_ADJUDICATION_AND_WITNESS_LANE1_V001             0   0   0   0    0    0    0   0    0
...THE_WITNESS_HUNT_LANE1_V001                             0   0   0   0    0    0    0   0    0
...V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001          0   0   0   0    0    0    0   0    0

(prq = prequotient; svol = "sub-volume")
```

**Two of the build's universal statements fail on the corrected universe.**

**(a)** The build, §5.2: "`FP-1`, `FP-2`, `FP-3` occur **0 times** in every
artifact carrying `beta` (table above)." At bytes,
`STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md` carries `beta_nk` **and** `FP-3` on six
lines, and its audit carries both on two. Opened at every one of the eight
lines: they are the O34SR gauge/FP-3 discussion, and none relates `beta` to
`FP-3`. The nearest, `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:515`, quoted WHOLE
with its enclosing line so the sense is not clipped:

````text
THE SAME STRUCTURE APPEARS ON BOTH SIDES OF THE RECORD'S TWO OPEN PLACES:
   at FP-3, switching real gauge structure ON is what DESTROYS the obstruction;
````

That sentence is about gauge structure, not about `beta`. The **grade survives**;
the universal token claim does not.

**(b)** The build's `W-1`/`W-3` name-coincidence finding, which it displayed for
one artifact, recurs in a second the build never tested.
`STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md` returns 35/5/4
on `W-1`/`W-2`/`W-3`. Opened: they are that artifact's own equation labels —
`(W-1)` at `:942` is `w=(G,v_G,I_G,ell_G)`, and the series runs on through
`(W-10)`, `(W-11)`, `(W-12)`, `(W-13)`, `(W-14)`. A third namespace, not the
three demands. The build's reasoning generalizes correctly; its universe did
not.

### §5.3 THE GAUGE RELATION — RE-DERIVED WITH A WIDER TOKEN SET, AND HELD

This lane re-ran the co-occurrence test with a **wider** `beta` token set than
the build used, adding `beta_R` and `beta_path`:

```text
files containing (beta_f|beta_nm|beta_nk|beta_gf|beta_R|beta_path) AND g_ij
  ROOT1: 3   STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001.md
             STAGE8_GAUGE_AS_CONTENT_O34SR_AUDIT_V001.md
             STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md
  ROOT2: 0

same beta set AND (transition function|triple-overlap|triple overlap)
  ROOT1: 3   identical file set
  ROOT2: 0
```

**Identical result on a strictly larger pattern.** All three are prior
commission audits; none is a construction artifact; each places the two objects
side by side only to type them apart. The build's finding is confirmed under a
test designed to break it.

The build's disclosed adjacency — the `U(1)`-bundle lift `tilde_f : P_n -> P_m`
sitting among the same package morphism's forward legs at
`...LANE2_V001.md:97`, and `j_f^fld` "the support-local field/test transport
derived from the adopted U(1)-bundle lift" at `:280-281` — was re-read at bytes
and is exactly as reported. It is proximity inside one structured tuple. The
corpus draws no relation between it and `g_ij`, and this lane authors none.

### §5.4 D5 GRADE

```text
D5_GRADE = CONFIRMED-WITH-CORRECTIONS

Q5 GRADES, ALL UNCHANGED after re-running over a 20-file universe instead of a
6-file one:
  FP-1 / FP-2 / FP-3                     DOES-NOT-TOUCH
  W-1 certified sub-volume rate          DOES-NOT-TOUCH
  W-2 member-supplied prequotient rule   DOES-NOT-TOUCH
  W-3 G_cm at a consuming HS type        DOES-NOT-TOUCH
  M-1 / M-2 / M-3                        DOES-NOT-TOUCH
  U(1) transition functions / gauge      DOES-NOT-TOUCH
CORRECTED: the tested universe was six artifacts described as "every artifact
  that carries the beta object"; at bytes it is twenty. And "FP-1, FP-2, FP-3
  occur 0 times in every artifact carrying beta" is false — FP-3 co-occurs with
  beta_nk in two prior audits, eight lines, none relating them.
```

---

## §6 — D6: BARS

### §6.1 SCAN RUN ON THE BUILD'S ARTIFACT — NO VIOLATION FOUND

Scanned for advocacy, adoption, recommendation, re-reading of a catalogued
negative, and any authored or drafted candidate object, including in toys,
footnotes, and asides.

```text
adoption / advocacy / recommendation verbs      0 in an operative sense
  (every hit is either the artifact's own bar declaration at :4-5, :1257,
   :1408-1409, or the QUOTED phrase "PROPOSED_NOT_ADOPTED" / the filename
   "ADOPTION_PROPOSAL", or the word "suggest" inside "what a name-based
   reading would suggest" at :491 — a description of a reading, not a
   recommendation)
authored candidate object / toy / model         0
"we define" / "let us" / "consider the model"   0
numeric evaluation of any coupling/scale/root   0
fence tokens outside quotation/declaration      0
```

### §6.2 THE ONE BAR-EDGE, EXAMINED AND CLEARED

The build's §3.2 contains a counterfactual re-display:

> **IF `supp(beta_f)` WERE EMPTY**, the law would read `supp(Eta_f x) subset
> f(supp x)` — exact support transport with no spreading whatever.

That inclusion, with the allowance term deleted, does not appear in the corpus.
This lane examined whether writing it constitutes authoring a missing clause.
**It does not**, on three grounds readable in the build's own bytes: it is
explicitly governed by a subjunctive ("WERE"); the build states in the next
paragraph that it "does not evaluate whether the allowance is empty, does not
derive the emptiness from `(C1-7)`, and does not author the missing statement";
and its CH-6 records the refusal to draw that inference as a closed choice. It
is a statement of the structural stake, not a supplied object. Cleared — and
named here so it is on the record as examined rather than missed.

The under-brace gloss in the same section is declared by the build as a
typographic gloss on a quoted line and is correctly separated in its
TOY_SEPARATION block. No finding rests on it.

### §6.3 D6 GRADE

```text
D6_GRADE = CONFIRMED
No adoption, no advocacy, no recommendation, no re-reading of a catalogued
negative, and no authored object, principle, or clause was found in the build's
artifact — not in a toy, a footnote, or an aside. One bar-edge examined
(§6.2) and cleared.
```

---

## CORRECTIONS, IN SEVERITY ORDER

Each names the file:line that decides it. No correction overturns a grade; five
of the eleven materially change what the build's artifact asserts.

### COR-A — Q1's universal negative is false at bytes

**Decided by:** `STAGE8_TASK5_V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001.md:485`
— `beta_path_2=b != 0.                                (P2-9)` (seal `OK`).

The build, §1.6: "**Nonzero exhibition:** NONE, in either root. No worked
instance, no example, no lower bound, no nonvanishing proof, no statement that
it is nonzero in any case." The clauses "no example" and "no statement that it
is nonzero in any case" are false. Corroborated at
`STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md:473-474` ("a diamond with path sums
`0` and `b!=0`") and at
`STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md:823`
(`BOUNDARY_TEST: normalized beta does not vanish or deletes a visible bulk
cycle`, typed `NOT_YET_EXECUTABLE / TYPE-U` at `:829`).

**Why the build missed it, at bytes:** its S-33 patterns require a `beta` token
immediately before the comparison. This lane ran
`beta[_a-zA-Z0-9]*\s*(!=|≠)\s*0` over both roots: **1 file / 1 hit in each, and
it is a Python `require(beta != 0, ...)` line in a script.** The corpus writes
its nonzero instances through an intermediate symbol — `beta_path_2=b != 0` —
which no `beta…!=0` pattern can reach. The pattern that reaches them is
`b != 0` / `b!=0` (6/10 and 3/3 in ROOT1), which the build did not run.

**Corrected statement:** Q1 = **PROVEN-ZERO** at every actual evaluation site;
nonzero written in exactly two places, both inside subjunctive independence
constructions, plus one unexecuted failure-test condition. **Grade unchanged.**

### COR-B — a second defining display of the discrepancy cocycle exists, and it types the object the other way

**Decided by:** `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md:1102`
— `beta_nm:=Resp_m compose S_nm-eta_nm compose Resp_n. (W-12)` (seal `OK`).

Three effects. **(i)** It is a `:=` defining display with **no `= 0` on the
defining line**; the zero arrives at `:1112` scoped "for the witness". **(ii)**
It defines the discrepancy as a difference of two **morphism composites**,
contradicting the build's §2.1 claim that "At bytes it is an **element**" as a
statement about the corpus rather than about `(C1-7)`. **(iii)** `:1108-1109`
is `(W-13)` — the source the build quoted `...HOSTILE_WITNESS...:181` as citing
("W-13 supplies the typed form") and never located.

**Why the build missed it:** its S-8 pattern was `beta_f :=` with a space. This
lane ran `beta_nm:=` — **2 files / 2 hits ROOT1, 2/2 ROOT2** — a pattern absent
from the build's SWEEP CUTOFFS. **Q2's DIFFERENT-OBJECT verdict unchanged; the
TYPE claim is corrected from settled to unreconciled.**

### COR-C — the Q5 sweep universe is six artifacts where the corpus has twenty

**Decided by:** `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:515` — an `FP-3` line in
an artifact that also carries `beta_nk` at `:274`.

The build's table is headed "RUN FILE BY FILE OVER EVERY ARTIFACT THAT CARRIES
THE `beta` OBJECT" and lists six. Enumerated at bytes, ROOT1 has **20** files
carrying a discrepancy-cocycle token. Consequently the build's §5.2 sentence
"`FP-1`, `FP-2`, `FP-3` occur **0 times** in every artifact carrying `beta`" is
false: `FP-3` occurs 6 times in `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md` and 2
times in its audit, both of which carry `beta`. This lane re-ran the full test
over all 20 (table in §5.2). **Every Q5 grade survives**, including the
name-coincidence reasoning, which recurs in a third namespace at
`...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md:942` (`w=(G,v_G,I_G,ell_G),
(W-1)`).

### COR-D — the supersession trace omits four sealed artifacts, one of them the successor to the definition-carrying build

**Decided by:** `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md:7`
— `SOURCE_V001 = STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md`,
and `:35` — `The V001 closures are struck.` (seal `OK`; zero occurrences of
`beta` in its 693 lines).

Also omitted: `STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md:10`
(`STAGE1_V002 = DEFECTIVE (Y1,Y2,Y3,Y4,Y5)`);
`STAGE8_TASK5_EQ6_STAGE1_V004_REVIEW_LANE2_V001.md:24`
(`IS_THE_VERSION_CURRENT = yes through Q-472`, R1–R5 all `pass`, silent on
`beta`); and `STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V002.md`, the successor
to the artifact from which both "killed" quotations were drawn, which repeats
neither `beta` nor `killed`. **Q4's NOTHING-OF-RECORD verdict is unchanged and
reinforced.**

### COR-E — a declared undecidability was declared without running the pattern that reaches it

**Decided by:** `STAGE8_TASK5_CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001.md:451`
— `beta_(S compose R)=beta_S+Eta_S(beta_R),            (J13-1)` (seal `OK`).

The build's §3.1 states "The bytes do not disambiguate whether it labels the
whole display or only its last line." At bytes, one sealed artifact attaches
`(J13-1)` to the **composition law** and has no support law in its J13 clause at
all; V003 attaches it to the **support inclusion**; and
`STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001.md:1300-1302` distinguishes "the sealed
inclusion (J13-1)" from "the composition law" as two things. The build's S-12
row (`J13`) says it narrowed "to law-statement and status contexts" and then
reports only status rows; no `J13-1` row exists in its SWEEP CUTOFFS. This lane
ran it: **6 files / 12 hits ROOT1, 3/3 ROOT2, leak 0.** The ambiguity survives as
an ambiguity *in the corpus's use of the label*; it is not an absence of
evidence. **Q3's NEVER-CONSTRAINED grade unchanged.**

### COR-F — a filename that exists in neither root appears twice, once in the load-bearing comparison table

**Decided by:** `STAGE8_DISCREPANCY_COCYCLE_O38SR_V001.md:429` — the DEFINING
SITE cell of §2.4's side-by-side table reads `...LANE1_LANE2_V001.md:247`. The
same string appears at `:243`.

`find -iname "*LANE1_LANE2*"` over both roots returns **0 files**. The intended
referent is unambiguous from §1.1
(`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md`), so no finding
changes — but the cell that identifies where `beta_f` is defined names a file
that does not exist.

**Eight further citation ranges do not match the text quoted under them.** The
quoted *text* is correct in every case; the *labels* are not:

| Build's citation | Actual span of the text it quotes |
|---|---|
| `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:19-38` (§2.4) | `:18-43` — and `:19` begins mid-sentence |
| `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:48-49` (§4.3) | `:49-50` — `:48` is an unrelated row |
| `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:305-312` (§2.3) | `:310-317` |
| `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:68-79` (§2.3) | `:69-80` |
| `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:85-110` (§2.1) | `:85-104` |
| `...EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:126-132` (§2.1) | `:126-134` |
| `...HOSTILE_WITNESS_CROSS_CHECK_LANE1_V001.md:179-195` (§1.3) | `:179-192` |
| `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:564-570` (§1.4) | `:564-572` |

The build's own FLAG BLOCK entry D-5 records four such defects found and fixed.
**Nine remain.** This is the single most reproducible defect class in the
artifact.

### COR-G — two clipped quotes survive the build's self-check

**Decided by:** `STAGE8_DISCREPANCY_COCYCLE_O38SR_V001.md:484-488` — it cites
`STAGE8_RECORD_SIDE_PATCH_DEFINITION_SWEEP_V001.md:42-45` and then displays only
line 42, cutting the sentence mid-clause at "what the patch family". Lines 43-45
are elided **inside the cited span**. The whole sentence is restored in §2.2
above.

Second instance, same section: the build quotes
`STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:644-645` as a fragment beginning
mid-sentence ("triple-overlap cocycle …") and ending on the word "and". This is
the exact defect the build logs as its own D-2 and D-6 and reports fixed; it was
fixed in §5.3 and left standing in §2.4.

### COR-H — the zero census is an undercount

**Decided by:** `STAGE8_TASK5_THE_WITNESS_HUNT_LANE1_V001.md:442` —
`beta_nm:=Kern_m^fin(j_nm^H H)-Pull_nm(Kern_n^fin H)=0. (Q2-16)` (seal `OK`).

The build reports "Three independent sites evaluate it." At bytes there are six
(table, §1.2). **This strengthens the PROVEN-ZERO grade rather than weakening
it**, and is listed as a correction because the build states the census as
complete.

### COR-I — the Q6 census of located volume-normalized statements is incomplete

**Decided by:** `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002.md:1117`
— `||beta_nm||/Vol_m=0.                              (W-14)` (seal `OK`).

The build's §6.3 says "Two located statements exist" about `beta` and volume. At
bytes there are more. This lane swept the volume tokens the build did not:

```text
Vol_R   ROOT1 5/5   ROOT2 5/5     `beta_R / Vol_R -> 0` at CONSTRAINT_ARM:457,
                                  PROPOSAL_V002:449, V003:443, V004:166
Vol_m   ROOT1 1/1   ROOT2 1/1     `||beta_nm||/Vol_m=0.  (W-14)` at V002:1117
Vol_f   ROOT1 1/1   ROOT2 1/1     `beta_f/Vol_f -> 0`   (the build found this)
Vol_N   ROOT1 1/1   ROOT2 1/1     ONLY the citing sentence, V003:416
```

**The build's core Q6 finding is confirmed exactly**: `Vol_N` occurs on one
physical line of the entire readable corpus and that line is V003's own citation
of the estimates. `a_N/Vol_N` and `b_N/Vol_N` each 1/1, same line. **The
estimates V003 calls "sealed" have no source, and Q6 =
INDETERMINATE-AT-BYTES is unchanged.** What is corrected is the claim that only
two related statements exist.

### COR-J — the homonym census omits a homonym inside the defining artifact

**Decided by:** `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:286`
— `Kern_n^fin(H_n)(u_n,d beta_n)`.

The build's §2.2 enumerates `beta_N`/`beta_NM` homonyms and says each is "a
different object in a different artifact". A lowercase `beta_n` — a Ward-identity
argument, not a discrepancy — sits three sections below `(C1-7)` in the same
artifact. The build's sentence is scoped to `beta_N` and so is not falsified in
letter; the census is nonetheless incomplete.

### COR-K — V003's authority table names LANE2_V002, not the definition-carrying LANE2_V001

**Decided by:** `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:52`
— `| STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md | represented
`Eta` geometry carried forward | SHA-256 `e5381e66…` |`.

The artifact carrying the J13 display has no authority link to the artifact
carrying the only `beta_f` definition. The build did not report this; it
strengthens the build's own Q4 conclusion. Disclosed against it:
`STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md:33` consumes LANE2_V001 as a
source with its digest, so the corpus is not uniform about the "killed" status.
Both are lane artifacts; neither is of record.

---

## EVERY CORRECTED GRADE AND TALLY, STATED EXPLICITLY

```text
BUILD'S GRADE                    AUDITED GRADE
Q1  PROVEN-ZERO                  PROVEN-ZERO — UNCHANGED
Q2  DIFFERENT OBJECT             DIFFERENT OBJECT — UNCHANGED
    (+ "beta_f is an element")   (+ TYPE CORRECTED: not settled; (W-12) types it
                                    as a difference of morphism composites)
Q3  NEVER-CONSTRAINED            NEVER-CONSTRAINED — UNCHANGED
Q4  NOTHING OF RECORD            NOTHING OF RECORD — UNCHANGED, REINFORCED
Q5  DOES-NOT-TOUCH (all rows)    DOES-NOT-TOUCH (all rows) — UNCHANGED
Q6  INDETERMINATE-AT-BYTES       INDETERMINATE-AT-BYTES — UNCHANGED

TALLY                            BUILD      AUDITED
zero-evaluation sites              3          6
artifacts carrying the object      6         20
defining displays of the object    1 (beta_f) 2 (beta_f (C1-7); beta_nm (W-12))
                                              + 2 further beta_nm := displays
                                                at (R1-6) and (Q2-16)
located beta-vs-volume statements  2          4 distinct volume tokens
                                              (Vol_R, Vol_m, Vol_f, Vol_N)
artifacts in the supersession      23         27 (four sealed siblings added)
  / standing table
(J13-1) label senses in corpus     1 declared 2 displayed + 1 audit reading
  ambiguous, undecidable              them as distinct
citation-range defects remaining    0 claimed  9 (8 ranges + 1 nonexistent
  after self-check                              filename, twice)
clipped quotes remaining after      0 claimed  2
  self-check
seals: OK / malformed / none       21 / 1 / 1  CONFIRMED IDENTICAL by this lane
seal MISMATCH                       0          0 — CONFIRMED
```

**Sweep rows independently reproduced exactly:** `beta_f` 30/52 · `beta_f :=`
2/3 · `supp(beta` 2/8 & 1/1 · `beta_R` 10/20 & 8/17 · `discrepancy cocycle` 6/25
& 4/5 · `DOR_020` 31/63 & 28/60 · `Vol_N` 1/1 · `a_N/Vol_N` 1/1 · `b_N/Vol_N`
1/1 · `beta/Vol` 1/1 · `g_ij` 26/83 & 9/25 · `triple-overlap` 11/22 & 4/4 ·
`transition function` 20/46 & 13/26 · `patch famil` 6/16 & 1/5 · `subextensiv`
91/177 & 85/153 · `prequotient` 47/108 & 25/48 · `G_cm` 46/198 & 6/12.
**Seventeen of the build's thirty-six rows reproduce to the digit.** The build's
counting discipline, having survived its own mid-build correction, is sound.

**SWEEP DRIFT, DISCLOSED.** On the demand/failure-point rows this lane obtains
slightly higher counts than the build (`W-1` 82/555 vs 81/524; `FP-1` 32/338 vs
30/313; `FP-2` 28/320 vs 27/310; `FP-3` 31/364 vs 30/353). **This is not a build
error.** Two sibling commission artifacts —
`STAGE8_COMPLETION_AND_FORCING_O37SR_AUDIT_V001.md` (mtime 19:39) and
`STAGE8_CONSTRAIN_OR_CREATE_O36SR_AUDIT_V001.md` (mtime 19:46) — landed in ROOT1
**after** the target sealed (mtime 19:24), and both carry that vocabulary. The
corpus grew between the two sweeps. Stated so the delta is not mistaken for a
miscount.

---

## CHOICE LEDGER

| # | Choice | Alternatives considered | Why this one | Status |
|---|---|---|---|---|
| CH-1 | Grade D1 **CONFIRMED-WITH-CORRECTIONS**, not REFUTED, despite finding nonzero `beta` written in the corpus | REFUTED, on the strength of `beta_path_2=b != 0` alone | The commission's own D1 rule excludes hypotheticals and subjunctives. Both instances are governed by "Take a refinement diamond" / "can have", and both exist to prove a solution fiber empty. Grading REFUTED would require reading a counterexample construction as an exhibition. Both instances are quoted WHOLE, with their framing, so a reader can regrade on the same bytes. | **CLOSED** |
| CH-2 | Do **not** promote the `BOUNDARY_TEST` line to a nonvanishing claim | Treat "normalized beta does not vanish" as corpus evidence that it does not vanish | It is the *failure condition* of a test the same artifact types `NOT_YET_EXECUTABLE / TYPE-U` and says is "not passed by absence" (`:829`). Reading a failure condition as an assertion would invert the source. Quoted whole so the typing line travels with it. | **CLOSED** |
| CH-3 | Report the `(W-12)` display as a **second defining display**, not as a different object | Treat `beta_nm` (W-12) as unrelated to `beta_f` (C1-7) | Its own artifact calls it "its refinement discrepancy" and derives from it the same `(W-13)` typed form that `...HOSTILE_WITNESS...:181` attributes to W-13 and that the build quotes as `beta`'s typed form. Calling them unrelated would sever a chain the corpus itself walks. Calling them *identical* would author an identification; this lane does neither and says so. | **CLOSED** |
| CH-4 | Leave the `(J13-1)` label sense **INDETERMINATE-AT-BYTES** after finding two conflicting usages | Decide it for the composition law, since CONSTRAINT_ARM's J13 clause has no support law at all | Two sealed artifacts use the label for two different equations and a third distinguishes them. Choosing one would author a disambiguation the corpus does not supply. The finding is that the ambiguity is *in the corpus*, which is stronger and cheaper than a ruling. | **CLOSED** |
| CH-5 | Enumerate the `beta`-bearing universe by extraction rather than accept the build's six | Re-run the build's table on the build's six artifacts | D5 requires independently hunting reach the build missed on DOES-NOT-TOUCH rows; that is impossible on a universe inherited from the build. Extraction found 20 in ROOT1, 17 in ROOT2. | **CLOSED** |
| CH-6 | Report the `W-1`/`FP-1` count deltas as **corpus drift**, not as build miscounts | Report them as corrections | Two sibling commission artifacts landed in ROOT1 after the target sealed (mtimes 19:39 and 19:46 vs 19:24) and carry that vocabulary. Attributing the delta to the build would be false at bytes. | **CLOSED** |
| CH-7 | Do **not** re-read the build's declined/stopped sweep rows as catalogued negatives | Treat S-6 (`beta_R`, "Stopped deliberately") as closed | A declared sweep cutoff in a *build* is not a catalogued negative of the program; the commission expressly directs this lane to run patterns the build listed without a hit count and to run S-6-class residues. `beta_R` is where `(J13-1)`-as-composition-law and the "actual `beta_R` … still needs" sentence live. | **CLOSED** |
| CH-8 | Rebuild the sweep harness after the first one returned all zeros, and validate with a **positive control** before using any count | Accept the zeros; or rebuild without validating | All-zero rows on patterns already seen to have hits by direct query is a silent-failure signature. Cause found (`rg` is a shell function, absent from a script's PATH). Positive control: 123 barred-name files exist; an unbarred `Q-4` sweep returns 1 barred path, the barred sweep returns 0. Every count in this artifact post-dates that control. | **CLOSED** |
| CH-9 | Quote every corpus span inside a **four-backtick** fence, reproducing the source's own three-backtick delimiters | Strip the source's fence markers, as the build did in §2.4 | Stripping them is silent alteration inside a quoted span, and a three-backtick wrapper would have been closed early by the source's own closers — which reads as elision. Declared once at the head. | **CLOSED** |
| CH-10 | State the citation-range defects as one correction (COR-F) with a table, not eleven | One correction per bad range | They are one defect class with one cause, and the build's own D-5 already names the class. Separating them would inflate the count without adding information. | **CLOSED** |
| CH-11 | Do **not** grade the build on whether `(C1-7)`'s zero is *true* | Assess the mathematics of the vanishing claim | Out of commission scope and would require the absent DoR-020-A4 and an unsupplied response transport. `proof_authorized = false`. This lane reports what the corpus states and its standing. | **CLOSED** |
| CH-12 | Read the sibling commission artifacts that landed after the target **only** for the drift diagnosis (mtime + token counts), not for content | Open them and compare findings | They are outside this audit's target and consuming them would import another lane's conclusions. Only their mtimes and the fact that they carry FP/W vocabulary are used, and only to explain a count delta. | **CLOSED** |

```text
OPEN CHOICES AT SEAL = 0
```

---

## TOY_SEPARATION

```text
ACTUAL SURFACE OF THE RECORD TOUCHED BY THIS AUDIT (all seal-verified from each
artifact's own directory, except the one flagged unsealed):
  beta_f := rho_f^C2(C2_m^fin) - C2_n^fin = 0   (C1-7), and its scope clause;
  beta_nm := Resp_m compose S_nm - eta_nm compose Resp_n   (W-12);
  beta_nk = beta_mk compose S_nm + eta_mk compose beta_nm, beta_nn = 0  (W-13);
  ||beta_nm||/Vol_m = 0   (W-14);
  beta_nm(H) := Kern_m^fin(...) - Pull_nm Kern_n^fin(H)   (R1-6) and (Q2-16);
  beta_nl = 0 = beta_ml + eta_ml(beta_nm)   (R1-7), (Q2-17);
  beta_ga = beta_g + Eta_g(beta_a) = beta_h + Eta_h(beta_f) = beta_hf;
  beta_(S compose R) = beta_S + Eta_S(beta_R)   (J13-1, CONSTRAINT_ARM sense);
  beta_R / Vol_R -> 0 and the I_contact clause   (J13-2);
  beta_gf = beta_g + Eta_g(beta_f) and supp(Eta_f x) subset f(supp x) union
    supp(beta_f)   (J13-1, V003 sense);
  ||beta_N([r,b])|| <= a_N ||r|| + b_N ||b||, and its citing sentence;
  beta_path_1 = 0, beta_path_2 = b != 0   (P2-9);
  the BOUNDARY_TEST / FAITHFULNESS_TEST block   (F4-5) and its TYPE-U typing;
  P_N := (R_N^rep direct_sum I_N^contact)/Delta_N   (J12-2);
  I_F, its objects and its six generator classes; the FinPackage_020 morphism's
    forward and backward legs; the symbol ledger;
  Kern_n^fin(H_n)(u_n, d beta_n) = 0   (C1-9);
  g_ij = exp(i theta_ij) in U(1), g_ij g_jk g_ki = 1, z_i : U_i -> C^2;
  CANONICAL_PATCH_COVER_TO_INCIDENCE_GRAPH_FUNCTOR_BUILT = false | TYPE-U |;
  the J13 status rows; W-1/W-2/W-3 as the demand register displays them;
  FP-1/FP-2/FP-3; M-1/M-2/M-3;
  the header/standing blocks of LANE2_V001, LANE2_V002, LANE1_V003, LANE1_V004,
    their four reviews of record, V005, PROPOSAL_V002, CONSTRAINT_ARM,
    THE_WITNESS_HUNT, V005_FINAL_CHECK, and the two prior commission audits.

*** NO ILLUSTRATIVE, TOY, MODEL, HYPOTHETICAL, OR CANDIDATE OBJECT WAS
    CONSTRUCTED BY THIS AUDIT — NOT ONE, ANYWHERE, INCLUDING IN FOOTNOTES AND
    ASIDES. THERE IS THEREFORE NO NON-CORPUS OBJECT TO DECLARE, AND NO FINDING
    RESTS ON ONE. ***

The only non-corpus devices used are MEASUREMENT INSTRUMENTS, declared here and
carrying no content: (i) the leak-checked sweep harness and its positive
control; (ii) the `awk length($0)` line-length measurement in §3.1; (iii) the
token-extraction census of `beta`-prefixed strings; (iv) the file-count tables.
None introduces a symbol, asserts anything about the record, or appears in any
grade except as a count.

NOT SURFACE — NOT TOUCHED, NOT BUILT, NOT SUPPLIED BY THIS AUDIT:
  no value, extent, bound, or locus for supp(beta_f);
  no inference from beta_f = 0 to supp(beta_f) = empty;
  no bridge between beta_f, beta_nm (W-12), and beta_N([r,b]);
  no disambiguation of the (J13-1) label;
  no reconstruction of a_N/Vol_N, b_N/Vol_N, Vol_N, a_N, or b_N;
  no relation between beta and g_ij, and no gauge typing of beta;
  no patch family, cover, or nerve;
  no location of the "nonzero boundary-cocycle alternatives" row;
  no restatement, repair, or withdrawal of any J13 law;
  no repair of the malformed V004 sidecar or the missing LOCAL_PROJECTIVE one;
  no adoption, advocacy, or recommendation of anything.
```

---

## IMPORT AUDIT

| Import | Source | Where used | Does the finding survive without it? |
|---|---|---|---|
| "cocycle", "composition law", "coboundary", "support", "index set", "homonym" as descriptive vocabulary | standard mathematical usage, outside this corpus | §1-§3 description only | **YES.** No theorem about any of them is applied. The corpus uses "cocycle" of its own accord at `(C1-7)`, `(R1-7)`, `(W-12)` and in the heading "Zero kernel-discrepancy cocycle", so at those sites even the name is record-native. Nothing is concluded from a name. |
| "subjunctive", "counterexample", "independence proof" as descriptions of a passage's mood | ordinary language | §1.4, CH-1 | **YES.** The classification rests on the quoted governing verbs ("Take", "Let", "assign", "can have"), which are displayed, not on the label. |
| "element" vs. "map" typing distinction | standard mathematical usage | §2.3 | **YES.** Both typings are read off displayed formulas: `(C1-7)` subtracts two objects of one datum type; `(W-12)` subtracts two morphism composites. Neither reading imports a theorem. |
| `shasum`, `rg`, `awk`, `find`, `od`, `stat` behaviour | tool semantics, outside the corpus | seal checks, sweeps, line lengths, sidecar byte inspection, mtimes | **YES**, and the one place tool behaviour could have produced a false finding — the silent-zero harness — was caught and neutralized with a positive control before any count was used (FLAG BLOCK D-1). |
| mtime ordering as evidence of write order | filesystem semantics, outside the corpus | CH-6, sweep-drift note only | **PARTIALLY.** The drift *explanation* depends on it. The finding it supports is a disclosure that a count delta is not a build error — nothing in any grade rests on it. If mtimes were unreliable, the delta would simply be reported as unexplained. |

**NO OTHER IMPORT IS MADE.** In particular: no vanishing criterion, no
cohomological interpretation, no consequence of any composition law, no
nerve/Cech theorem, and no property of a crossed homomorphism is imported or
applied anywhere. Where the corpus states a law, this audit reports the law and
stops.

---

## SWEEP CUTOFFS

**HARNESS.** All counts below come from a single leak-checked harness run
separately over each root, with the ten bar globs held in a **bash array** (one
argv word per glob) and executed in the profile-initialized shell where `rg`
resolves. Every row carries a per-pattern barred-leak counter; **leak = 0 on
every row in both roots.** This artifact's own file and the target are excluded
from ROOT1 counts by `!*STAGE8_DISCREPANCY_COCYCLE_O38SR*`.

**POSITIVE CONTROL, RUN BEFORE ANY COUNT WAS USED.** 123 files with barred names
exist across the two roots. An **unbarred** `rg -l 'Q-4'` returns 1 path with a
barred name; the **barred** sweep returns 0. The exclusion demonstrably fires.

```text
BAR GLOBS APPLIED TO EVERY ROW BELOW:
  !*REGISTER*  !*EXECUTION_TRACKER*  !*THE_PLAN*  !*ROAD_REMAINING*
  !*THE_HANDOFF*  !*OBSERVATIONS_REGISTER*  !*DECISION_SHEET*  !*_ADDENDUM*
  !alpha_supervision/**  !*STAGE8_DISCREPANCY_COCYCLE_O38SR*
```

| # | Pattern | ROOT1 f/hits | ROOT2 f/hits | Leak | Where this sweep was stopped, and why |
|---|---|---|---|---|---|
| A-1 | `beta_f` | 30 / 52 | 27 / 30 | 0 | Reproduces the build's S-1 to the digit. Stopped after typing every ROOT1 hit. |
| A-2 | `beta_f :=` | 2 / 3 | 1 / 1 | 0 | **Exhaustive.** Reproduces S-8 exactly. |
| A-3 | `beta_nm:=` | 2 / 2 | 2 / 2 | 0 | **Exhaustive — both opened.** NOT IN THE BUILD'S SWEEP SET. Found `(W-12)` and `(Q2-16)`. Basis of COR-B. |
| A-4 | `beta_R` | 10 / 20 | 8 / 17 | 0 | **All 10 ROOT1 files opened.** The build stopped this row deliberately (its S-6); this lane ran it because it carries `(J13-1)`-as-composition-law and "actual `beta_R` on every primitive". Basis of COR-E. |
| A-5 | `beta_path` | 1 / 2 | 1 / 2 | 0 | **Exhaustive.** NOT IN THE BUILD'S SWEEP SET. The nonzero instance. Basis of COR-A. |
| A-6 | `beta_id` | 3 / 3 | 3 / 3 | 0 | **Exhaustive.** NOT IN THE BUILD'S SET. `beta_id=0` in the V2-J13 law across three proposal versions. |
| A-7 | `beta_nn` | 3 / 3 | 2 / 2 | 0 | **Exhaustive — all opened.** All display `beta_nn=0`. |
| A-8 | `beta_ml` | 3 / 3 | 3 / 3 | 0 | **Exhaustive.** All inside `(R1-7)`/`(Q2-17)`-class zero displays. |
| A-9 | `beta_nl` | 3 / 3 | 3 / 3 | 0 | **Exhaustive.** Same. |
| A-10 | `beta_mk` | 3 / 3 | 2 / 2 | 0 | **Exhaustive.** All inside the typed form `(W-13)`. |
| A-11 | `beta_hf` | 1 / 1 | 1 / 1 | 0 | **Exhaustive — opened.** `...MEMBERSHIP_THEOREM_LANE3_V001.md:364-366`, a diamond consistency chain `beta_ga = beta_g + Eta_g(beta_a) = beta_h + Eta_h(beta_f) = beta_hf`. Neither zero nor nonzero asserted. |
| A-12 | `supp(beta` | 2 / 8 | 1 / 1 | 0 | **Exhaustive — all 9 opened.** Reproduces S-7 exactly. Q3 basis. |
| A-13 | `J13-1` | 6 / 12 | 3 / 3 | 0 | **Exhaustive — all 15 opened.** NOT IN THE BUILD'S SWEEP SET. Basis of COR-E. |
| A-14 | `J13-2` | 1 / 2 | 1 / 2 | 0 | **Exhaustive.** NOT IN THE BUILD'S SET. The subextensivity/ideal clause. |
| A-15 | `Vol_N` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** Confirms the build's Q6 core finding: one physical line, the citing sentence. |
| A-16 | `Vol_R` | 5 / 5 | 5 / 5 | 0 | **Exhaustive — all opened.** NOT IN THE BUILD'S SET. Basis of COR-I. |
| A-17 | `Vol_m` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** NOT IN THE BUILD'S SET. `(W-14)`. Basis of COR-I. |
| A-18 | `Vol_f` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** Reproduces S-16. |
| A-19 | `beta/Vol` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** Reproduces S-17. |
| A-20 | `a_N/Vol_N` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** Reproduces S-13. |
| A-21 | `b_N/Vol_N` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** Reproduces S-14. |
| A-22 | `DOR_020` | 31 / 63 | 28 / 60 | 0 | Reproduces S-32. All checked to be citations; paired with A-50. |
| A-23 | `discrepancy cocycle` | 6 / 25 | 4 / 5 | 0 | **Exhaustive — all 6 ROOT1 files opened.** Reproduces S-10. |
| A-24 | `W-1` | 82 / 555 | 8 / 62 | 0 | **NOT exhausted corpus-wide.** Exhaustive on all 20 `beta`-bearing artifacts; the two nonzero cells opened and typed as different namespaces. |
| A-25 | `W-2` | 67 / 539 | 2 / 7 | 0 | Same method, same cutoff. |
| A-26 | `W-3` | 70 / 530 | 3 / 7 | 0 | Same method, same cutoff. |
| A-27 | `FP-1` | 32 / 338 | 0 / 0 | 0 | Same method. Zero in all 20 `beta`-bearing artifacts. |
| A-28 | `FP-2` | 28 / 320 | 0 / 0 | 0 | Same method. Zero in all 20. |
| A-29 | `FP-3` | 31 / 364 | 0 / 0 | 0 | Same method. **NOT zero in all 20** — 6 hits in O34SR, 2 in its audit, all 8 opened. Basis of COR-C. |
| A-30 | `G_cm` | 46 / 198 | 6 / 12 | 0 | Same method. Zero in all 20. |
| A-31 | `prequotient` | 47 / 108 | 25 / 48 | 0 | Same method. Zero in all 20. |
| A-32 | `g_ij` | 26 / 83 | 9 / 25 | 0 | Read at the defining artifact and at the three audits that also carry `beta`. Reproduces S-19. |
| A-33 | `triple-overlap` | 11 / 22 | 4 / 4 | 0 | Reproduces S-20. |
| A-34 | `transition function` | 20 / 46 | 13 / 26 | 0 | Reproduces S-21. |
| A-35 | `patch famil` | 6 / 16 | 1 / 5 | 0 | **Exhaustive — all opened.** Reproduces S-22. |
| A-36 | `subextensiv` | 91 / 177 | 85 / 153 | 0 | **NOT exhausted.** Read only where it co-occurs with `beta`. Reproduces S-18. Residue disclosed unread. |
| A-37 | `nonzero` | 936 / 3630 | 876 / 3261 | 0 | **NOT exhausted — far beyond byte-level reading.** Narrowed by A-38 through A-42, which localize the object. Residue disclosed unread. |
| A-38 | `does not vanish` | 11 / 11 | 7 / 7 | 0 | **Exhaustive — all 18 opened.** The one `beta`-relevant hit is CONSTRAINT_ARM `:823`. Basis of COR-A(c). |
| A-39 | `nonvanish` | 90 / 171 | 49 / 72 | 0 | **NOT exhausted.** Intersected with the `beta` token set: zero `beta`-bearing hits. Residue disclosed unread. |
| A-40 | `b != 0` | 6 / 10 | 5 / 8 | 0 | **Exhaustive — all opened.** NOT IN THE BUILD'S SET. This is the pattern that reaches `(P2-9)`. |
| A-41 | `b!=0` | 3 / 3 | 3 / 3 | 0 | **Exhaustive.** NOT IN THE BUILD'S SET. Reaches the `...THE_WITNESS_HUNT...:473` sentence. |
| A-42 | regex `beta[_a-zA-Z0-9]*\s*(!=\|≠)\s*0` | 1 / 1 | 1 / 1 | 0 | **Exhaustive.** The sole hit in each root is a Python `require(beta != 0, ...)` guard in a script. Demonstrates why the build's S-33 patterns could not reach the prose instances. |
| A-43 | `!= 0` | 338 / 1475 | 181 / 433 | 0 | **NOT exhausted.** Narrowed by A-40/A-41/A-42. Residue disclosed unread. |
| A-44 | `!=0` | 124 / 291 | 108 / 254 | 0 | **NOT exhausted.** Same narrowing, same disclosure. |
| A-45 | `I_F` | 258 / 504 | 243 / 483 | 0 | **NOT exhausted** (the token is a substring of many unrelated strings). Used only for the co-occurrence test A-49. |
| A-46 | token census `beta[_a-zA-Z0-9]*` (extraction, not match count) | 60 distinct tokens, 2431 bare `beta` | comparable | 0 | Every token with a count of 3 or more was opened at a representative site. Basis of the homonym work and of COR-J. |
| A-47 | co-occurrence: (`beta_f\|beta_nm\|beta_nk\|beta_gf\|beta_R\|beta_path`) × `g_ij` | 3 files | 0 files | 0 | **Exhaustive — all 3 opened.** A strictly wider `beta` set than the build used; identical result. |
| A-48 | same × (`transition function\|triple-overlap\|triple overlap`) | 3 files | 0 files | 0 | **Exhaustive.** Identical file set to A-47. |
| A-49 | co-occurrence: `` `I_F `` × (`patch famil\|U_i\|nerve`) | 1 file + 4 `evidence/` duplicates | — | 0 | **Exhaustive — opened.** The single hit is a false positive: `I_F` inside an unrelated filename in a citation list. Basis of the D2 negative. |
| A-50 | `find -iname "*DOR_020*"` and `"*dor*020*"` | 0 | 0 | n/a | **Exhaustive.** Confirms the build's §4.3. |
| A-51 | `find -iname "*LANE1_LANE2*"` | 0 | 0 | n/a | **Exhaustive.** Basis of COR-F. |
| A-52 | `find -iname "LOCAL_PROJECTIVE*"` | 2 `.md` + 2 unrelated `.json` | same | n/a | **Exhaustive.** No sidecar of any kind. Confirms the build's unsealed flag. |
| A-53 | `beta`-bearing artifact enumeration (`beta_f\b\|beta_nm\|beta_nk\|beta_gf\|beta_R\b\|beta_path\|beta_id\|beta_ml\|beta_nl`, `evidence/` and `.py` excluded) | 20 files | 17 files | 0 | **Exhaustive — the Q5 universe.** Basis of COR-C. |
| A-54 | bar-glob positive control (`Q-4`, barred vs unbarred) | 1 barred path unbarred → 0 barred | same method | n/a | **Exhaustive.** Validates every row above. |

---

## FLAG BLOCK

### Fence line

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No numeric value of any coupling, scale, root, eigenvalue, norm, or constant was
computed by this lane. No comparison to any measured value was made. Every
statement in this artifact is symbolic, structural, or a count of files, hits,
lines, or bytes. Fence tokens appear only inside quotation and in this
declaration.

### Seal tally

```text
ARTIFACTS SEAL-CHECKED BY THIS LANE = 34
  shasum -a 256 -c OK, run from each artifact's own directory   = 32
  sidecar malformed but content hash MATCHES exactly            =  1
    (STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md)
  no sidecar in either root — UNVERIFIABLE                      =  1
    (LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md)
  MISMATCH                                                      =  0
```

**No seal mismatch was found, so no STOP condition fired.** This is 34, not the
build's 23, because this lane additionally verified the four sealed siblings of
COR-D (`...LANE2_V002`, `...STAGE1_V002_REVIEW_LANE1_V001`,
`...STAGE1_V004_REVIEW_LANE2_V001`, `...MEMBERSHIP_THEOREM_LANE3_V002`), the
four artifacts carrying the material behind COR-A and COR-B
(`...CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V002/V003/V004`,
`...CONTINUUM_PACKAGE_CONSTRAINT_ARM_LANE1_V001`,
`...V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001`,
`...THE_WITNESS_HUNT_LANE1_V001`), and three opened to type hits
(`...EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001`,
`...EQ6_Q408_PRIMITIVE_GENERATOR_EXHIBIT_LANE2_V002`,
`...EQ6_LOC_NATURALITY_LANE3_V001`, `STAGE8_TASK6_LP_MATRIX_LEDGER_LANE3_V001`).
**The build's own 21 / 1 / 1 / 0 tally is reproduced exactly on its subset.**

Both of the build's seal disclosures were independently reproduced:

1. `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md.seal.sha256` —
   `od -c` shows a **single space** between digest and filename where
   `shasum -c` requires two, so it reports "no properly formatted SHA checksum
   lines found". Recomputed digest
   `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` is
   byte-identical to the digest the sidecar records. **The artifact is intact;
   the sidecar's formatting is defective.** Reported, not repaired.
2. `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` has **no sidecar in either root**,
   confirmed by `find` over both roots. The `g_ij` quotation in §2.1 above is
   drawn from those unsealed bytes and is flagged at its point of use.

The target's own two sidecars both verify `OK` from its directory, and its
recomputed digest matches the build's reported digest exactly.

### Register-bar incidents

```text
INCIDENTS = 0
```

No barred file was opened, grepped for content, or quoted by this lane at any
stage. Three disclosures, none of them an incident:

- **Barred filenames appear inside permitted corpus files** and were read there
  as content: `...LANE1_V003.md:48` carries a `QUESTIONS_SETTLED_REGISTER_V001.md`
  row in its authority table, `...LANE1_V004.md:39` references the same register
  by name, and `...STAGE1_V002_REVIEW_LANE1_V001.md:25` names an
  `alpha_supervision/` path. Reading a permitted artifact that *mentions* a
  barred file is not reading the barred file. None of these rows was followed,
  and no barred file's contents entered this artifact.
- **The bar-glob positive control (A-54) deliberately counted** how many paths a
  barred name would occupy in an unbarred result. The count was taken with
  `grep -c`; **no filename was printed, displayed, or opened.** This was a
  measurement of the exclusion mechanism, run precisely so that the build's own
  reported incident class could not recur silently here.
- The build's own incident R-1 (a barred filename surfacing in a `-l` listing
  because string-passed globs stopped firing) was **not reproduced**: this
  lane's first harness failed for a different reason (D-1 below), and the
  array-passing discipline the build adopted after its incident is the same one
  used here, validated by A-54.

**ONE METHODOLOGICAL DISCLOSURE ABOUT THE MANDATED GLOB, AFFECTING BOTH LANES.**
The commission mandates `--glob '!*REGISTER*'` to exclude the barred
`QUESTIONSSETTLED_REGISTER_V001.md`. That glob is broader than the bar list and
also suppresses `STAGE8_DEMAND_REGISTER_O25SR_V001.md` — the artifact that
**defines W-1, W-2 and W-3**, which is not on the bar list and which both lanes
quote directly. So the Q5 target definitions are invisible to every barred sweep
in both artifacts. This lane tested whether the over-exclusion could hide a
`beta`-bearing artifact: **22 non-barred `*REGISTER*`-named files exist across
the two roots, and ZERO of them carry a discrepancy-cocycle token.** The
over-exclusion therefore costs nothing at bytes, and the Q5 grades are unaffected
— but the fact is stated rather than left implicit.

### Defects found in this audit's own draft on self-check

```text
DEFECTS FOUND AND FIXED BEFORE SEAL = 4
```

| # | Defect | Fix |
|---|---|---|
| D-1 | **The most dangerous one.** The first sweep harness was written as a standalone `zsh` script and **returned `files=0 hits=0` for every one of fifteen patterns** — including patterns this lane had already seen hits for by direct query. Had those numbers been trusted, this audit would have reported a fabricated corpus-wide absence. Cause, diagnosed rather than guessed: `rg` in this environment is a **shell function**, not an executable on a script's `PATH`, so every invocation inside the script failed silently and `wc -l` counted an empty stream. | Harness rebuilt to run inline in the profile-initialized shell, with bar globs held in a **bash array**. A **positive control** (A-54) was then run and passed *before any count in this artifact was used*: 123 barred-name files exist; an unbarred sweep returns 1 barred path; the barred sweep returns 0. Every number above post-dates that control. |
| D-2 | §3.3 of the draft reported the `J13-1` sweep as **"7 files / 12 hits"** in ROOT1. The validated harness returns **6 / 12**. The hit count was right; the file count was one too many, from counting a listing by eye. | Corrected to 6 / 12, and cross-checked against the enumerated file list (V005, CONSTRAINT_ARM, V003, O34SR, O34SR_AUDIT, O32SR_AUDIT = six). |
| D-3 | The first draft wrapped corpus quotations in **three-backtick** fences. Several quoted spans contain the source artifact's own three-backtick closers, which would have terminated this artifact's fence early and rendered the remainder of the quote as body text — indistinguishable from elision, and exactly the defect class this program has recorded fifteen-plus times. | Every corpus quotation rewritten inside a **four-backtick** fence with the source's own `` ```text `` delimiters reproduced verbatim, and the convention declared once at the head of the artifact. |
| D-4 | The first draft's §1.3 asserted "**Three of those five clauses** are refuted at bytes." Recounted against the build's actual sentence: only **two** clauses ("no example", "no statement that it is nonzero in any case") are refuted by a subjunctive assignment. "No worked instance", "no lower bound", and "no nonvanishing proof" are **not** refuted by `(P2-9)`, because an assigned counterexample value is neither worked, nor a bound, nor a proof. | Corrected to "Two of those clauses", and the reasoning that keeps the other three standing is now stated in §1.4 rather than implied. |

### Quotation-integrity self-check, run mechanically before seal

Every fenced corpus quotation in this artifact was verified **line by line
against the source bytes**, not by eye: each non-blank physical line of each
cited source range was tested for exact whole-line presence in this artifact
with `grep -Fqx`.

```text
SPANS CHECKED                                        20
NON-BLANK SOURCE LINES MISSING FROM THIS ARTIFACT     0
```

Spans checked: `...LANE2_V001:242-254`, `:69-81`, `:285-289`;
`...V005_FINAL_CHECK...:477-490`; `...THE_WITNESS_HUNT...:451-460`, `:470-484`;
`...CONSTRAINT_ARM...:445-463`, `:808-829`; `...PROPOSAL_LANE2_V002:1088-1118`,
`:1120-1125`; `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001:18-43`;
`...FUNCTOR_ATTEMPT...:19-43`; `...RECORD_SIDE_PATCH_DEFINITION_SWEEP...:42-45`;
`...O34SR_V001:559-575`; `...LANE1_V003:386-406`, `:49-52`;
`...LANE2_V002:1-26`; `...STAGE1_V002_REVIEW...:6-11`;
`...STAGE1_V004_REVIEW...:4-14`; `...O32SR_AUDIT...:1298-1302`.

**No elision inside any quoted span. No line-wrap clip. No fabricated line.**

### Standing of this artifact

```text
ARTIFACT_TYPE = DEFAULT_REFUTE_AUDIT_OF_A_COMMISSIONED_BUILD
DETERMINATION ONLY. Nothing is constructed, proposed, adopted, advocated,
recommended, or supplied. No object, principle, or clause is authored.
NO GIT ACTION TAKEN. NO REGISTER, TRACKER, PLAN, ROAD, HANDOFF, DECISION SHEET,
ADDENDUM, OR alpha_supervision FILE WAS OPENED, GREPPED FOR CONTENT, OR QUOTED.
```

### Grades at a glance

```text
D1  NONVANISHING       CONFIRMED-WITH-CORRECTIONS  (COR-A severest; COR-H)
D2  DOMAIN COMPARISON  CONFIRMED-WITH-CORRECTIONS  (COR-B, COR-F, COR-G)
D3  SUPPORT LAW        CONFIRMED-WITH-CORRECTIONS  (COR-E)
D4  ADOPTION STATUS    CONFIRMED-WITH-CORRECTIONS  (COR-D, COR-K)
D5  REACH              CONFIRMED-WITH-CORRECTIONS  (COR-C)
D6  BARS               CONFIRMED — no violation; one bar-edge examined, cleared

OVERALL                CONFIRMED-WITH-CORRECTIONS

The build's artifact could not be broken at bytes on any of its six answers.
It was broken on four universal negatives, one sweep universe, one declared
undecidability, one supersession trace, and nine citation labels.
```

**END OF AUDIT.**
