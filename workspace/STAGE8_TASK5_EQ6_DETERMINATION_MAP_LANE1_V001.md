# STAGE 8 TASK 5 / EQ6 — DETERMINATION MAP — LANE 1 V001

```text
ARTIFACT_TYPE = FORWARD_DETERMINATION_ANALYSIS
REGISTER_HEAD_CHECKED = Q-463
SOURCE_REVIEW = STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md
SOURCE_REVIEW_SHA256 = 312dbe14efebd167b5287ccde773d69e64eb17b9bcfe91137d1e4cea50b15e0e

J4 = DERIVABLE_MODULO
J12 = AUTHORABLE
J15 = OBSTRUCTED
J15_TRUE_RESTRICTION = DERIVABLE_CONTRAVARIANT
AMENDMENTS_PROPOSED = J4_RELATIONAL_INCREMENT_GROUPOID + J12_CONTACT_GLUE

MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Preflight, authorities, and tags

The requested output and sidecar did not exist at preflight. The source
review and its sidecar verified before reading. The sealed questions-settled
register verified at SHA-256
`f8e13ea4592d2a0dd138de07c22fb464c793eecd04eb39251eed7bc6c96c5c3a`
and ended at Q-463. Q-463 commissions this determination map and prohibits a
third blind rebuild. `alpha_supervision/LOCKED_PROCESS.md` was read in full.

| Authority | Verified SHA-256 | Use |
|---|---|---|
| representative-family theorem | `f0f8b09b1aa6a16c0ed0dffedbd275aba9e647fb5841fc8ce06ce9e6a8b00857` | `Adm_base`, R2 increments, cocycle, physical flat-family freedom |
| DoR-017 square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | exact R2 class and differentiation |
| DoR-017 decision | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | N member, symbolic normalization, no-selection discipline |
| DoR-020 package V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | adopted package fields and obstruction ledger |
| DoR-020 decision | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | conditional J1-J15 equalizer |
| DoR-020-A1 decision | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | adopted WHERE laws and prior clause-complete ruling |
| J1-J15 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | current J4/J12/J13/J15 text |
| local excision/contact certificate | `d61a550a33bf1215c35f4d6f27cd2ec5d644b93a05e16d08fe3d43ded3416817` | exact contact stock and its open physical term |
| prefix-to-cycle descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | `D_G^*` injectivity, restriction, cycle-creating no-lift theorem |
| Map-1 extension V002 | `eb3675d525af7d1420c4ed033a5e5b94eb7494c1bac1305029b25ac9169567a0` | rank-two no-selection obstruction and visible-cycle witness |

### 1.1 Register sweep

The questions-settled register was swept before deriving. The load-bearing
entries were:

```text
Q-313                 primitive Map 1 and exact restriction scope;
Q-347                 rank >= 2 no-selection obstruction;
Q-348-Q-357           prefix sufficiency, descent, D* injectivity,
                      contravariant restriction, cycle-creating no-lift;
Q-391/Q-396           completed flat-family content is physical, not gauge;
Q-411                 J1-J15 equalizer text;
Q-421                 six-generator bedrock and joint-family discipline;
Q-455/Q-456           finite-package frontier and FC4 honesty;
Q-459/Q-463           the two stage-1 kills and the changed diagnosis.
```

No entry supplies the contact-overlap equality under another name. No entry
licenses a cycle-creating upward physical or bottom map.

### 1.2 Claim tags

```text
PROVABLE      = forced by the sealed/ratified stock cited above;
PART-PROVABLE = the form or consistency is proved, but actual physical
                inhabitance is not;
YOURS         = a genuine law/member choice requiring the principal's gate.
```

### 1.3 Symbol collisions bearing on this question

```text
psi           = a physical Adm_base flat deformation; not gauge;
nu            = DoR-017's symbolic N-member normalizer;
                psi's coefficient is NOT proved equal to nu;

rho_f         = contravariant physical cycle restriction Q_M -> Q_N;
rho_Gamma,N   = completed-action coordinate projection;
                neither is a cycle-creating upward map;

Eta_rep       = the derived represented-response forward map;
eta_boundary  = the unbuilt contact transport;
                equality on their overlap is exactly D2's issue;

D_G^*         = the injective dual of the prefix-to-cycle descent;
D^2           = action differentiation; no relation is inferred from notation.
```

The register's informal comparison between the vertical/flat freedom and
`nu` is correct at the level of symbolic, non-selected physical freedom. It
does not license identifying their coefficients; Q-396 expressly preserves
additional flat-family coordinates independently of `nu`.

## 2. D1 — J4 and vertical-increment freedom

### 2.1 The current functional row is false on cycle creation

The current J4 first line is

```text
rho_Gamma,N(I_M)=I_N on the proved restriction scope.        (J4-old)
```

On a cycle-creating arrow `f:N->M`, R2 instead supplies

```text
I_M=I_N o rho_f+v_f,                                (D1-1)
```

where `v_f` is physical target-stage action data. For a composable
`g:M->L`, R2 gives

```text
v_(gf)=v_f o rho_g+v_g.                             (D1-2)
```

Both equations are **PROVABLE** from R2. A source coordinate is not a
function of the target coordinate because `(I_N,v_f)` can change while
`I_M` remains fixed.

### 2.2 The derived relation/span

For every admitted refinement arrow define the action relation

```text
Rel_f := {
  (I_N,I_M,v_f):
  I_M=I_N o rho_f+v_f,
  v_f in V_f^inc,
  all reality, batching, quotient, unit, and support certificates hold
}.                                                       (D1-3)
```

It is represented by the span

```text
             E_f
            /   \
           /     \
      Adm_N       Adm_M,

E_f=Rel_f,                                             (D1-4)
```

not by a function `Adm_M->Adm_N`. The left leg returns `I_N`; the right leg
returns `I_M`. Composition is fiber product over the middle action, followed
by `(D1-2)`. Identity uses `v_id=0`. Associativity follows directly:

```text
v_((hg)f)
 =v_f o rho_g o rho_h+v_g o rho_h+v_h
 =v_(h(gf)).                                           (D1-5)
```

Thus the R2 stock derives a category-valued action relation on every arrow
carrying the sealed contravariant carrier restriction. This is
**PROVABLE**. It requires no action representative, frame, orientation,
filtration, or cycle basis.

### 2.3 The flat action groupoid

Let `psi_N` be an admissible physical flat deformation at `N`. It acts on a
relation term by

```text
psi_N . (I_N,I_M,v_f)
 :=(I_N+psi_N,
    I_M,
    v_f-psi_N o rho_f).                              (D1-6)
```

The target equation is invariant:

```text
(I_N+psi_N) o rho_f+(v_f-psi_N o rho_f)
 =I_N o rho_f+v_f
 =I_M.                                               (D1-7)
```

Composition is equivariant because substituting `(D1-6)` into `(D1-2)`
cancels the transported `psi_N` term. Covariance transports `psi`, `v`, and
the action together. Reality and batching act componentwise. These are
**PROVABLE** consequences of the representative-family theorem.

This is an action groupoid, not a gauge quotient. The objects remain
physically distinct because Q-396 proves that admissible flat deformations
can change the completed response. Passing to the set of orbits would delete
record-visible completed content. The lawful no-selection object is the
whole groupoid/family.

### 2.4 The review witness is absorbed, not excluded

The review's witness was

```text
I'_N=I_N+psi_N,
I'_M=I_M,
v'_f=v_f-psi_N o rho_f.                             (D1-8)
```

Equation `(D1-6)` shows that `(D1-8)` is exactly one action-groupoid
morphism. It is absorbed by the derived relation and does not violate it.
The two source actions are not identified as gauge; they occur in the same
fiber over the fixed target with the compensating increment recorded.

On a rank-preserving identity extension R2 requires `v_f=0`. The relation
then reduces to the old on-the-nose restriction isomorphism, so every passing
rank-preserving regression is retained.

### 2.5 Hessians and stationary data

Differentiating `(D1-1)` on the declared R2/R5 domain gives

```text
D^2 I_M
 =rho_f^*(D^2 I_N)+D^2 v_f.                        (D1-9)
```

Under `(D1-6)`,

```text
D^2 I_N -> D^2 I_N+D^2 psi_N,
D^2 v_f -> D^2 v_f-rho_f^*(D^2 psi_N),             (D1-10)
```

so `(D1-9)` is invariant. The same relational rule must carry the critical
family, reducing domain, and inverse cube. A consumer invariant under
`(D1-6)` may descend to an orbit-valued output. A member-sensitive consumer
must return the complete family with its sensitivity tag. Selecting a
cleavage `(I_M -> I_N)` is not derived.

The DoR-020 completed-existence principle already returns a covariant family
with no member selected. Requiring its finite face to choose one source for
each target would add a choice function absent from the completed output and
would contradict the axiom's no-selection guard. This coherence is
**PROVABLE**.

### 2.6 Exact J4 amendment proposal

The following replaces only the false functional part of J4; all existing
covariance, reality, batching, quotient, unit, finite-jet, and reducing-domain
requirements remain:

```text
PROPOSED AMENDMENT J4-R (DERIVED CONTENT; PRINCIPAL ADOPTION REQUIRED)

For every admitted arrow f:N->M, an R1 family member supplies a relation term
(I_N,I_M,v_f) in Rel_f satisfying

  I_M=I_N o rho_f+v_f,
  v_(gf)=v_f o rho_g+v_g,
  v_id=0,

and the differentiated relation

  D^2 I_M=rho_f^*(D^2 I_N)+D^2 v_f.

The admissible flat family acts by

  (I_N,v_f)->(I_N+psi_N,v_f-psi_N o rho_f),

with all members retained. No source member is selected from a target. On a
cycle-rank-preserving identity extension, v_f=0 and the old restriction
isomorphism is recovered. Consumers are groupoid-invariant or explicitly
family-valued/member-sensitive.
```

The content is **PROVABLE**; adoption is the principal's ruling because it
changes a ratified J-row. No authored physical parameter is introduced.

```text
D1_RESULT = DERIVABLE_MODULO
J4_FUNCTIONAL_MAP = REFUTED_ON_CYCLE_CREATION
J4_TRUE_OBJECT = R2_INCREMENT_ACTION_GROUPOID / RELATION_SPAN
```

## 3. D2 — J12 and the contact-overlap equality

### 3.1 The exact missing equation

Let

```text
C_N:=R_N^rep intersect I_contact,N.                 (D2-1)
```

The represented map is derived:

```text
Eta_f^rep(Kernbar_N H)=Kernbar_M(j_f H).            (D2-2)
```

A full response map on the stated quotient/direct-sum carrier exists only if
the contact transport obeys

```text
eta_f^boundary|_(C_N)=Eta_f^rep|_(C_N)              (D2-3)
```

after the declared signs and quotient maps. This is the overlap equality
isolated as `(Y2-4)`.

### 3.2 What the sealed contact/Ward stock proves

The stock proves, conditionally on an actual physical contact term:

```text
I_contact is closed, invariant, restriction-natural, and bulk-cycle safe;
beta_(gf)=beta_g+Eta_g(beta_f);
beta_f/Vol_f -> 0 on admitted cofinal families;
OLD_FID + exclusive-region RNL + LR + Ward/bundle compatibility;
contact and disjoint diamonds compose when a coherent physical section exists.
                                                               (D2-4)
```

These facts are **PROVABLE**. The local-excision artifact expressly leaves
the positive-source physical contact term and coherent all-rank section
unproved. None of `(D2-4)` identifies the two maps on `C_N`.

### 3.3 Independent non-derivability test

The missing implication can be tested without choosing a physical member.
Take a one-arrow model with a one-dimensional closed contact ideal `C`, put
`C=R^rep intersect I_contact`, and let the Ward differential vanish on `C`.
Let

```text
Eta^rep|_C=id_C,
eta^boundary|_C=u,                                  (D2-5)
```

where `u` is any reality/unit-compatible contact automorphism. On a one-arrow
category the cocycle is automatic. On a chain choose a multiplicative natural
family `u_(gf)=u_g u_f`; the boundary discrepancy then satisfies the same
composition law. A fixed contact element is subextensive against growing
volume, and no bulk cycle is introduced.

Both `u=id` and `u!=id` satisfy the sealed properties `(D2-4)`. Only the first
satisfies `(D2-3)`. Therefore the sealed stock does not derive the overlap
equality. This non-derivability is **PROVABLE**.

The `u=id` model also proves there is no formal inconsistency in strict
gluing. Actual physical inhabitance remains unproved. Hence J12 is not
OBSTRUCTED; it is AUTHORABLE.

### 3.4 Full-gate choice table

| Candidate | Exact content | Genuine alternative/consequence | Standing |
|---|---|---|---|
| strict pushout gluing | impose `(D2-3)` and define `R_phys=R_rep plus_(C) I_contact` | no new contact twist; gives one-valued `Eta` | **YOURS** |
| twisted gluing | choose a covariant reality/unit-compatible automorphism family `u_f` on `C_N` and glue by `eta_boundary=u_f Eta_rep` | adds a contact-holonomy/member fiber | **YOURS** |
| zero intersection | prove `C_N={0}` on the actual family, then use the direct sum | derivable only if that new separation theorem lands | **PART-PROVABLE / CURRENTLY OPEN** |
| partial response | retain `Eta_rep` only and do not form a total J12 package map | preserves current proofs but leaves `[EQ6]` open | **LAWFUL REJECTION ALTERNATIVE** |

The minimal no-new-parameter candidate is strict pushout gluing. Its
minimality is carrier-structural: it states only that one physical element
has one image and introduces no complement, projection, phase, or contact
normalization. This is not a recommendation; it is the least authored law
that closes the exact gap.

Void conditions for any adopted contact-gluing row are:

```text
1  a represented/contact overlap element receives unequal images;
2  the chosen gluing deletes a D_G^*-visible bulk cycle;
3  the beta cocycle, restriction, reality, units, Ward, OLD_FID, RNL, LR,
   or subextensivity fails;
4  a contact automorphism is selected from a response, threshold, fixed-point,
   end-test, or desired numerical consequence;
5  actual physical contact data exhibit a nontrivial twist inconsistent with
   the adopted member.
```

### 3.5 Exact J12/J13 amendment candidate

```text
PROPOSED AMENDMENT J12-G (AUTHORED; PRINCIPAL CHOICE REQUIRED)

For C_N=R_N^rep intersect I_contact,N, the represented and boundary transports
must form a gluing datum. Under the strict candidate,

  eta_f^boundary|_(C_N)=Eta_f^rep|_(C_N).

The total response carrier is the signed pushout

  R_N^phys=R_N^rep plus_(C_N) I_contact,N,

and Eta_f is the unique induced map. The induced maps obey J12 composition
and the J13 cocycle, restriction, reality, unit, Ward, support, and
subextensivity laws. Failure of any overlap equality voids the member.
```

Because DoR-020-A1 states that the clause layer is complete, adopting this
candidate requires an explicit amendment superseding that closure statement.
A lane cannot silently treat single-valuedness as already adopted.

```text
D2_RESULT = AUTHORABLE
CONTACT_OVERLAP_EQUALITY_DERIVABLE = false | PROVABLE
STRICT_GLUE_CONSISTENT = true | PART-PROVABLE
ACTUAL_PHYSICAL_GLUE_INHABITED = OPEN
```

## 4. D3 — J15 and the new-cycle bottom

### 4.1 The visibility and restriction theorems

The descent theorem gives

```text
D_G:P_G^fam -> Q_G is surjective,
ker(D_G^*)={0}.                                     (D3-1)
```

Thus every nonzero integral cycle direction is record-visible. For every
declared cellular arrow, including cycle creation, the lawful map is

```text
rho_f:Q_M->Q_N,
D_N P_f=rho_f D_M.                                 (D3-2)
```

This contravariant square is **PROVABLE**. An upward map exists only when
cycle rank is preserved and `rho_f` is an isomorphism.

### 4.2 Cycle-creating no-lift proof

Use the permanent one-edge-to-parallel-edge witness. The source graph is a
tree, so

```text
Q_N={*}.
```

Adding a second parallel edge creates one cycle, and the target quotient has
holonomy

```text
H([h_e,h_a])=h_e h_a^(-1).                         (D3-3)
```

The target classes `[1,1]` and `[w,1]` have the same old restriction but
different cycle holonomy for `w!=1`. By `(D3-1)` their difference is
record-visible. Any map from the one-point old bottom to the target bottom
has one value and cannot reproduce both target shadows. Therefore no
representative-independent covariant lift can satisfy the package square for
all admitted target data.

The rank-two edge-exchange theorem strengthens the conclusion: any
no-selection single lift is invariant under the exchange and consequently
kills the record-visible S8-A difference cycle. Choosing a non-invariant lift
selects an edge/cycle direction. Hence the covariant new-cycle bottom lift is
OBSTRUCTED under the current no-selection laws. This is **PROVABLE**, not a
missing construction.

### 4.3 The true package-bottom square

Let

```text
pi_N:R_N^phys->Bot_N^resp,
pi_M:R_M^phys->Bot_M^resp
```

be the independently sealed finite-bottom maps, and let

```text
r_f^Bot:Bot_M^resp->Bot_N^resp
```

be the lawful old-image restriction. Combining represented `Eta_f` with the
sealed restriction square gives the mate equation

```text
r_f^Bot pi_M Eta_f^rep=pi_N                         (D3-4)
```

on the represented source carrier. After a lawful D2 gluing, the same
equation extends to the total contact carrier. This is the mixed-variance
J15 square. It retains every target-only new-cycle shadow at `M`; only its
old restriction is compared at `N`.

Equation `(D3-4)` is exactly consistent with FC4:

```text
cycle-creating arrows have downward/old-image restrictions only;
no representative-independent upward quotient or lift is invented.
```

The current ratified J15 text is already contravariant (`r_N` applied to the
completed/target object) and does not require a covariant finite-bottom lift.
Therefore no J15 amendment is needed. Stage 1 must correct its package
category and use `(D3-4)` rather than attempting an ordinary covariant
natural transformation on that coordinate.

An authored section of `r_f^Bot` would be a new edge/cycle selection and
would violate FC4/no-selection unless separately ratified as symmetry-
breaking physics. It is not an admissible repair of the current theory.

```text
D3_RESULT = COVARIANT_LIFT_OBSTRUCTED
J15_TRUE_SQUARE = DERIVABLE_CONTRAVARIANT_MATE
FC4 = RESPECTED_BY_(D3-4)
J15_AMENDMENT_REQUIRED = false
```

## 5. D4 — consequence board

### 5.1 Determination table

| Face | Determination | PROVABLE content | PART-PROVABLE / YOURS residue |
|---|---|---|---|
| J4 | **DERIVABLE_MODULO** | R2 relation, cocycle, flat action groupoid, witness absorption, Hessian relation | principal must adopt J4-R because the ratified functional row is false |
| J12 | **AUTHORABLE** | represented `Eta`; contact laws; non-derivability of overlap equality; consistency of strict gluing | actual gluing law/member is YOURS; physical inhabitance remains open |
| J15 face | **OBSTRUCTED** as covariant lift | cycle-creating no-lift theorem and D* visibility | none within current law; a selected lift would be new symmetry-breaking physics |
| J15 true row | **DERIVABLE** contravariantly | mate square `(D3-4)` and exact old-image restriction | extends to full contact carrier only after D2 closes |

### 5.2 What Stage 1 must build after rulings

Stage 1 is no longer an ordinary one-functor construction. In lawful order it
must:

```text
1  after principal adoption of J4-R, install the R2 action relation/span and
   its flat action groupoid on every admitted arrow; carry Hessians and R5
   data by (D1-9), with no cleavage/member selected;

2  retain the already-derived represented Eta;

3  after a principal J12 gluing ruling, construct the actual physical contact
   transport satisfying the chosen overlap law and all J13 certificates;

4  assemble the package in the mixed-variance category using the
   contravariant bottom mate (D3-4), never a new-cycle upward lift;

5  rerun Gate 1-4, Q-243, Q-279, Q-309, Ward, contact, reality, batching,
   restriction, the vertical-freedom witness, overlap witness, and the
   one-edge/S8-A no-lift regressions.
```

### 5.3 What Stage 2 must build

Only after Stage 1 passes on the amended types may Stage 2 build:

```text
1  physical J2: reader=pi_Mx o Loc o Kernbar o Q, independently;
2  J7 on the same physical reader tuple;
3  simultaneous J1-J15 overlap diamonds in the action-groupoid/contact-
   pushout/mixed-bottom package;
4  one joint [EQ6] inhabitant or family, without selecting its member.
```

### 5.4 Principal rulings and obstructions

Principal rulings required:

```text
A-J4  adopt the derived J4-R relational/increment-groupoid replacement;
A-J12 choose and adopt a contact-gluing law (strict, twisted, zero-intersection
       theorem if later derived, or reject total J12).
```

No J15 ruling is required to restore the current law: the existing
contravariant row and FC4 already decide it. A covariant cycle-creating lift
is obstructed and must be removed from the build contract.

Nothing proves the joint physical contact gluing inhabited. Nothing proves
the entire `[EQ6]` fiber inhabited. Those remain construction/witness burdens,
not contradictions.

## 6. Surface geometry versus rails and stop

```text
SURFACE_GEOMETRY:
  vertical increments and D*-visible new cycles are real surface content;
  the represented/contact overlap is a real gluing locus whose transport is
  not fixed by the current Ward/contact stock.

RAILS:
  a function-valued J4 and a covariant Bot arrow made the package look like
  one ordinary functor by suppressing the increment fiber and new-cycle
  kernel. The correct rails are a relation/action groupoid, a pushout only
  after a gluing law, and a mixed-variance bottom mate.

NEW_PHYSICS_TYPED:
  only the J12 overlap law is authorable physics;
  J4-R and contravariant J15 are derived structural corrections;
  the covariant J15 lift is impossible under current admissibility.
```

No construction, amendment adoption, member binding, fixed-point execution,
end test, or numerical evaluation was performed.

```text
J4 = DERIVABLE_MODULO
J12 = AUTHORABLE
J15 = OBSTRUCTED
J15_TRUE_RESTRICTION = DERIVABLE_CONTRAVARIANT
AMENDMENTS_PROPOSED = J4_RELATIONAL_INCREMENT_GROUPOID + J12_CONTACT_GLUE

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
