CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00014569
VERDICT_BEARING_SET = exactly the 14 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LIVE_MEMBERS = NONE
LANE = CODEX 2
ROLE = OPPOSITE-LANE ENTRY-GATE CHECK OF ATTACH CANDIDATE V002 (relay 1058)
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `cleanroom/relay_inbox/RELAY_PASTE_1058_V002_CHECK_CODEX2_V001.md` | `69816b4148daa293b77bdbb5d8b9b454730ac4cd20bfa9055e774eebc273e841` | assignment |
| 02 | `cleanroom/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V002.md` | `14a811a8d5a507c5d20e26ef40fa53661e22b0b39f869edd68ad700bc3765f82` | candidate under attack |
| 03 | `cleanroom/gen_stage8_attach_candidate_proposal_dario_v002.py` | `7915aa6d6212b3f22b4f0dab492ec5cd82b877c5d684902626f91d2af1355838` | candidate generator and ten refusals |
| 04 | `cleanroom/STAGE8_ATTACH_CANDIDATE_CHECK_CODEX2_V001.md` | `617e0144c0adfad97ec14ae85fbb76efd9787a63f1c0b77b778c9768c747a528` | V001 check and repair inventory |
| 05 | `cleanroom/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | authoring mandate and void law |
| 06 | `cleanroom/STAGE8_DESC_OPEN_CONDITIONS_SWEEP_CODEX2_V001.md` | `85514b04912f77e9276aca131b41d62d400a3e555e21492eb43a1df53dd5d588` | six-check acceptance registry |
| 07 | `cleanroom/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | quotient, record category, and two graph carriers |
| 08 | `cleanroom/STAGE8_HOLONOMY_FIRST_OPENING_TENSION_ADJUDICATION_EINSTEIN_V001.md` | `8fbdc8300b28166564e4ba32e6747bc643135e25d674cc37da706de3afbbf019` | emptiness-on-purpose and later-carrier ruling |
| 09 | `cleanroom/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | `U(1)_rel`, auxiliary connection, and cell obligation |
| 10 | `cleanroom/PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` | `aa2ef0e78a1ad975a04596831eef2ea7587b78c4fa9d979ae8a742308cd7c214` | unit character and rank-one comparator action |
| 11 | `cleanroom/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | exact ratified `D_n S` law and slot identity |
| 12 | `cleanroom/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | inherited law presentation |
| 13 | `cleanroom/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | unphased pulse and underived parent |
| 14 | `cleanroom/STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V001.md` | `8c257818b55c66aef1842024601c51f3a22599a949db7de4280d7763fc9dcdbc` | superseded ledger tables and withdrawn claims |

```text
BLIND HELD. EVERY SCALE REMAINS SYMBOLIC.
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
NO MEMBER BOUND. NO PHYSICAL FIXED POINT EXECUTED. NO END TEST. NO FREEZE.
NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEASURED-CONSTANT COMPARISON.
PE-1..17 = POINTER-ONLY. omega_phys = UNTOUCHED.
CANDIDATE_STATUS = PROPOSED_NOT_ADOPTED
```

CLOSURE_DECLARATION_END

# STAGE 8 — ATTACH CANDIDATE V002 OPPOSITE-LANE ENTRY-GATE CHECK

Date: 2026-08-11  
Lane: Codex 2  
Status: CHECKED — CORRECTED, NOT ENTRY-READY

## 1. Preflight and custody

Members 02–06 rehashed to their assigned seals before semantic use. Member 02's declared-first
closure independently resolves and rehashes all `24/24` members with zero live members and ends at
byte 6927 exactly. Member 03 contains ten distinct fail-closed routes R1–R10. Member 14 remained
byte-identical at `8c257818b55c66aef1842024601c51f3a22599a949db7de4280d7763fc9dcdbc`
after every read-only check.

The mandate authorizes a proposed authored object; it does not authorize entry or cure an internally
false carrier. This check enters, ratifies, promotes, or supplies nothing.

## 2. A4 — gauge-triviality attack

### 2.1 The quotient

Member 07 `:210-239` defines `G_joint`, its faithful character `rho_joint`, the associated Hermitian
line bundle, and the projective quotient. The quotient removes a phase shared by every component of
the complete Hilbert state. The same span says `rho_joint` converts action comparisons into relative
phases on associated vertex fibres; those are not automatically the common central scalar.

Member 09 `:35-51` separately distinguishes the active `U(1)_rel` from the common ray-lift phase.
Member 10 `:30-37` gives the comparator receiver `Q=|1><1|`, and member 02 `:511-515` instantiates the
same receiver shape as

```text
Q_cell = |p_Q><p_Q|,
D(z) = exp(i theta Q_cell) = diag(1,z,1).
```

For `z != 1`, `D(z)` is not `lambda I`: the first and third diagonal entries force `lambda=1`, while
the second would force `lambda=z`. Thus its projective class is nontrivial. The phase would be removed
only if it were applied as `z I` to the complete state. At the candidate's actual `Q_cell` receiver it
is relative and survives.

The A4 bridge is authored, not derived, but no sealed span forces it into the removed central
component. The gauge-triviality shot therefore does not fire V7 on the loop sector.

## 3. Circuit, canonicity, and domain

The two incidence matrices were rebuilt from member 07's displayed integer data.

```text
K_(1,3):
  d_1 = [[-1,-1,-1],[1,0,0],[0,1,0],[0,0,1]]
  rank(d_1)=3, |E|=3, b_1=0.

K_square:
  d_1 = [[-1,-1,0,0],[1,0,-1,0],[0,1,0,-1],[0,0,1,1]]
  rank(d_1)=3, |E|=4, b_1=1.
```

Member 08 `:70-77` correctly rules the first-opening emptiness deliberate. Member 07 `:1840-1863`
specifies `K_square` and its ordered loop holonomy, `:1790` mandates the composition-loop result, and
`:2321` records that it is not sealed. The candidate's residuals R1–R3 are all accurate: the carrier
is specified/mandated but unsealed; no proof makes it the physical causal-cell domain; and
admissibility is not universality.

Exactly one independent cycle makes the circuit selector-free on `K_square`, or more generally on a
connected cell with `b_1=1`. It does not make every cell in the candidate's stated `b_1>=1` domain
selector-free: cells with `b_1>1` have multiple independent cycle classes. Member 02 uses `b_1>=1`
at `:192,508` but uses `b_1=1` at `:269`; those are different domains. The record types both the
`BareRec_2` category and the specified square sector, so the restriction is record-typed as a
mathematical sector. Entry nevertheless requires narrowing the operative domain to the specified
square/`b_1=1` class or supplying a lawful selector for the wider class. Neither makes the unsealed
sector the derived physical domain.

## 4. A6 — order and parent

Member 11 `:121-150` and member 12 `:647-655` write the ratified cell factor as `D_n[a_j] S_j`.
Under the ordinary operator convention, the rightmost `S_j` acts first. That supports the proposed
stage order if a sequential physical realization is authored.

No sealed member supplies a phased one-stage parent or contradicts the two-stage proposal. Member 13
`:16-34` supplies a single unphased base pulse only, and `:77-101` says the complete parent has not
been derived. Thus the one-stage attack does not make A6 fall; A6 remains authored and conditional.

There is, however, an algebraic error in the authored carrier. Member 02 `:325-328` states

```text
H_j^(2)(t)=w_j(t) theta_j Q_cell,  integral w_j dt=1,
T exp[-i integral H_j^(2)]=exp(+i theta_j Q_cell)=D_n.
```

The displayed Hamiltonian actually gives `exp(-i theta_j Q_cell)`. To obtain the ratified `D_n` for
the adopted `n=+1`, the Hamiltonian must carry the opposite sign (or the phase convention must be
changed consistently). Therefore A6 stands as an authored response to the one-stage question but is
not executable as written; it needs the sign repair and remains contingent on a derived parent.

The noncommutation check independently reproduces
`D(z)S-SD(z)=[[0,1-z,0],[z-1,0,0],[0,0,0]]`, which vanishes exactly at `z=1`.

## 5. Ledger and closed claim

The row census is correct: thirteen forced rows, eight authored rows, seven of the latter new because
A3 is already adopted. F07 and F10 carry the required receiver limits. Fixed-string and context
review finds the V001 one-ingredient, representation-met, and 5/5 assertions only in explicit
withdrawals; none survives as a live claim.

The ledger is not complete. `rho_joint` appears in the operative §9.1 expression at member 02 `:513`
and is expressly included in the re-invited test at `:390`. It is in neither category:

- no F01–F13 row names `rho_joint` as a forced ingredient;
- A4 authors `iota_rep`, while `rho_joint` is carried from member 07 and is not authored by A4.

Putting a forced representation inside the prose of an authored bridge does not classify it. A new
forced row for the pinned `rho_joint` representation, with its quotient scope, would close this
specific escape. The closed claim is therefore broken again on `rho_joint`.

## 6. A5 exactness

The displayed basis map `E_j(e_s)=|r>`, `E_j(e_t)=|p_Q>` is an exact unitary identification once the
authored endpoint assignment is accepted. Its stated intertwining law is false for nontrivial phase.
Let `z=rho_joint(iota_rep(Hol))`. Reading the scalar in the only type-complete default way—as `zI` on
`L_s direct-sum L_t`—gives

```text
on e_s:  E_j(z e_s)=z|r>,       D(z)E_j(e_s)=|r>;
on e_t:  E_j(z e_t)=z|p_Q>,     D(z)E_j(e_t)=z|p_Q>.
```

The law holds only at `z=1`. The text mentions `U_(gamma_j)` but the operator never appears in the
equation. The lawful receiver would be an explicitly typed
`R_j(z)=1_(L_s) direct-sum z 1_(L_t)` satisfying `E_j R_j(z)=D(z)E_j`, together with a derivation or
authored rule connecting the ordered transport to `R_j`. That object is absent.

This is a correctable representation/embedding defect rather than evidence that the noncentral
`D(z)` phase itself is projectively removed.

## 7. Registry and typed requirements

The two prior FAIL rows are not genuinely closed:

- Registry AC-2 at member 06 `:249-254` requires rejection of the stationary same-GNS overclaim and
  a derived moving-front or sector-changing implementation. A4 supplies a group bridge only. Member
  02 `:443-446` itself says the SR representation obstruction remains open.
- Registry AC-3 requires a named and grounded causal parent/time-ordering. A6 names an authored
  sequence and correctly keeps the parent conditional, but supplies no derived parent; its displayed
  Hamiltonian also has the sign error above. This is partial addressing, not closure.

The two NOT-YET rows remain honestly open. Candidate-certificate AC5 is correctly reduced to a
predicate, and its AC6 marginal-binding gap remains explicit.

The five-requirement count is also mis-stated. Orientation is the one fully forced-met requirement.
Inter-cell ordering is only one half of the single time-ordering requirement; it cannot be counted as
a second fully met requirement while intra-cell ordering is authored and parent-conditional.
Representation is incomplete, A5's intertwiner fails, time-ordering needs the sign/parent repairs,
and common origin is shape-only with a named gap. The accurate current count is `1/5 forced-met`, not
`2/5 forced-met`; it is not an entry-complete five-requirement object.

## 8. Mechanics and custody

Member 02's closure was independently parsed and all 24 members rehashed `24/24`, zero live,
`STRICT==STABLE`. The ten generator refusal routes are present. V001 remained byte-untouched.

Sixteen fresh output-inspection patterns, independently worded from the generator's list, were run
over member 02's authored prose and returned zero hits. Circuit evidence consumed only graph
incidence, the record's scope ruling, seal status, and the holonomy form. No prediction value,
direction, sign, battery result, target, or fitness fact was used. `OUTPUT_INSPECTION_AUDIT` is clean.

## 9. Flattening and freedoms consumed

Flattening check:

- a surviving relative phase was not flattened into a valid A5 intertwiner;
- a scalar character was not flattened into its missing two-line receiver representation;
- `b_1=1` on `K_square` was not flattened into uniqueness on every `b_1>=1` cell;
- a record-typed sector was not flattened into the derived physical causal-cell domain;
- a ratified endpoint order was not flattened into a derived two-stage parent;
- partial registry addressing was not flattened into discharge;
- one forced half of time-ordering was not counted as a whole second requirement;
- an eight-row ledger was not flattened into an exhaustive ledger.

```text
FREEDOMS-CONSUMED
  cited inputs: exactly the 14 closed members and only their declared roles
  derived here: projective-survival test; two exact incidence ranks; A5 basis attack;
                A6 sign audit; closed-ledger counterexample; registry/requirements recount
  not consumed: candidate entry; physical cell selection; member binding; physical fixed point;
                end test; numeric physical scale; measured target; PE content; freeze; omega_phys
  JOINT_ANCHOR_DERIVED = false
```

CLOSURE = declared-first (byte 0; end byte 00014569; 14 members; candidate closure 24/24 strict-stable; fresh scan clean)
A4_PHASE = SURVIVES (member 07 :231-239; member 02 :511-515 gives noncentral diag(1,z,1), not common scalar zI)
CIRCUIT = CANONICITY-CONFIRMED FOR K_square (b1=1); DEFECT on the broader b1>=1 selector-free claim
DOMAIN = RECORD-TYPED (BareRec_2/K_square mathematical sector; physical-domain proof absent; operative scope must narrow to b1=1 or gain a selector)
A6 = STANDS-AS-AUTHORED against the one-stage test; CORRECTION REQUIRED (stage-2 Hamiltonian sign; parent still underived)
LEDGER = 13+8-COUNT-CONFIRMED; INCOMPLETE (rho_joint unclassified)
CLOSED_CLAIM = BROKEN (rho_joint)
A5 = GAP (stated intertwining fails for z!=1; missing 1 direct-sum rho_joint receiver)
REGISTRY = OVERCLAIM (AC-2 unresolved; AC-3 only partial; requirements are 1/5 forced-met, not 2/5)
OUTPUT_INSPECTION_AUDIT = CLEAN
PROSE_DIGESTS = 24/24 STRICT==STABLE
RESIDUE_GREP = CLEAN (16 own patterns; 0 hits)
VERDICT = CORRECTED (entry blocked: domain scope, A5 receiver/intertwiner, A6 sign/parent, rho_joint ledger row, registry/requirements recount)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
