CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00006449   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 20 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member carries its FULL path from the alpha-program-archive root and is rehashed
  at that path before sealing (Q-913 standard).
ANCHOR_RULE = CLOSURE_MEMBER_CITATION_RULE_V001. Every span cites member + line locus. NO FLAG NAME
  IS USED AS AN ANCHOR anywhere in this artifact.
LANE = DARIO   ROLE_THIS_RELAY = AUTHORED CANDIDATE UNDER MANDATE (relay 1052)
AUTHORITY = member 02, the ATTACH SUPPLY MANDATE, a RATIFIED PRINCIPAL DECISION.  Authoring is
  lawful here, and ONLY here, in exactly the form that mandate types.  It ratifies THE MANDATE AND
  ITS STRUCTURE ONLY — it ratifies NO CONTENT, and no content is entered by this artifact either.
ALL_RESULTS = CLAIMED until the opposite-lane check.  Builder never verifies own work.
SELF_CITATION_BAR = DARIO's own prior outputs are NOT record witnesses.  My relay-1050 artifact is
  therefore NOT a member here.  Where its results are needed, member 06 — the CODEX 2 check that
  independently re-derived them — is cited instead.  That is the lawful route and it is the one used.
LIVE_MEMBERS = NONE.  Strict and stable prose-digest modes coincide by construction (§8.2).
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1052_ATTACH_CANDIDATE_DARIO_V001.md` | `39f36f1915f6f8e8217b7958214a8dcede9e964916faf2465f7c2bb70ffae8a4` | assignment |
| 02 | `workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213` | **THE AUTHORITY — ratified principal decision mandating this supply** |
| 03 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 04 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 05 | `workspace/STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md` | `ab156ee764db9d0bd48f54f1b879f1bafcfac08b45520ca6c4fb582e48edf572` | **THE TYPE AND THE ABSENCE ADJUDICATION; the placement family; the prior kill** |
| 06 | `workspace/STAGE8_DESC_SURFACE_CHECK_CODEX2_V001.md` | `912c708e328a313cebe90314d7b893a87746b0a18084c35af5c2e44b612f54bd` | **the opposite-lane check whose three corrections this artifact carries** |
| 07 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **`d_law` — the ratified DoR-009 codomain the candidate must land in. READ ONLY WITH 08 AND 09.** |
| 08 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 07 (`:234-245`) |
| 09 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | the ratification governing 07; the no-member clause |
| 10 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | tensor factors, `res_B`, the non-uniqueness theorem |
| 11 | `workspace/FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md` | `a27a2d571273494a0787e2283734ef1405d74dadfe16d64d3450bb4536e50732` | **the holonomy normalization, and the connection's stated physical role** |
| 12 | `workspace/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48` | **the representation: `rho_joint`, `L = P x_(rho_joint) C`, `U_e` as parallel transport** |
| 13 | `workspace/PRIMITIVE_TRANSPORT_ONLY_PHASE_COMPLETE_GENERATOR_PRINCIPLE_V001.md` | `aa2ef0e78a1ad975a04596831eef2ea7587b78c4fa9d979ae8a742308cd7c214` | **transport-only entry; curvature as failure of composed transports to close; `q_primitive=1`** |
| 14 | `workspace/STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md` | `c49d31200460e96209375f06a7a655d343767695ef09805f1e396d1814833b6b` | the Q-227 baseline: full cell action, `P_0=I-P_ch`, `U_N^0` |
| 15 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md` | `2be95d5b58ea000df9a30b717a809374a90693f78a893d87b73a12e4be97fa21` | the computed cell endpoint and the arbitrary-`N` theorem |
| 16 | `workspace/R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md` | `ed4572ebfece9da2d57a2baede144f996aee1b196c26182c0d9e61f879bf9ef9` | **the time-ordering facts, and their own conditionality** |
| 17 | `workspace/R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_SPEC_V002.md` | `4a7600caa23d0c7a98eeef8a79941c20ca4e28a4f5a2c1cf5c2362e88c7d4721` | the charge projector at bytes |
| 18 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | P0–P7 surface content; the completed-object receiver |
| 19 | `workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | the nine fields — used ONLY for the §7 coherence display |
| 20 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | `B iso C(Y)`; the continuum survivor table |

```text
RE-PINNED SPAN (mandate §"THE OBJECT TO BE SUPPLIED" requires it, under the citation rule):
  member 05, lines 314-334, span SHA-256
  be0be903041fcface7336a2facf157c5459d7e4ccb8e3d1deb0785fbf08d52ad
  content: the Attach type, its five required adjuncts, and "No such `Attach` exists."

BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO MEMBER OF THE ORIGIN FAMILY BOUND.  NO FIXED-POINT EXECUTION.  NO END TEST.  NO FREEZE.
NO MEASURED-CONSTANT COMPARISON.  PE-1..17 POINTER-ONLY, LEDGER SHUT.  omega_phys UNTOUCHED.
CASCADE-TERRITORY STOP FORM LIVE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL ENTRY (ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001, ad9fc14e1f07…)

# STAGE 8 — THE ATTACH CANDIDATE
## RELAY 1052 — `[PLAN:DESC-33]` — DARIO LANE — AUTHORED UNDER MANDATE — [CLAIMED]

Date: 2026-08-12

**WHAT THIS IS.** A concrete candidate for the map the record types and adjudicates absent. Its
authored core is **one new ingredient**: the identification of the ratified transition law's abstract
history label with the *cell-circuit holonomy* of the already-adopted connection. Everything else in
the candidate is either forced at bytes or already adopted elsewhere in the record's premise set.

**WHY THAT ONE INGREDIENT AND NOT ANOTHER.** Because the record already killed the alternative and
said why. The prior attachment attempt died in part because *the tree carrier made all connection
phases gauge-removable* (member 05 `:378`). Gate 4's own analysis says exactly where that stops
being true: *vertex rephasing removes the common phase on trees; **loop holonomy remains***
(member 05 `:219`). The candidate is placed on a loop for that reason and no other.

---

## 0. THE CORRECTIONS FROM THE OPPOSITE LANE, CARRIED

The check at member 06 returned `VERDICT = CORRECTED` on my relay-1050 artifact. Its corrections are
carried here as binding, not re-argued — the builder-never-verifies rule runs in this direction too.

1. **`KCH_ROUTES = ONE-COMPUTATION`** (member 06 `:140-145`). My claim that the sector-dimension
   ratio was reached by two independent routes was wrong: it is `Tr_A(rho_P P_ch)` under the same
   normalized identity, exposed after a different partial trace. **No independence claim of any kind
   is made in this artifact**, and where a structure appears twice below (§4.3) I say explicitly that
   it is coherence between two adopted premises and not confirmation of either.
2. **`LOCALIZATION = WIDER`** (member 06 `:207-226`). My headline that all residual freedom was one
   tensor factor wide was contradicted by my own cited members. The open-conditions list is carried
   in full at §6.3 and **this candidate is not claimed to close any of it.**
3. **`RULING_STANDARD = STANDS`** (member 06 `:171-183`). The completed-object standard is upheld;
   an SR-face determination is a restriction or lower bound, not a populated `Obj_0` entry. §7 uses
   that standard and does not soften it.

**And the custody finding, accepted without qualification: `QUARANTINE = BROKEN`** (member 06
`:228-251`). My relay-1050 §0.1 said sections 1–2 "are that text, carried". They were not: I revised
them after the form was opened. The revisions were disclosed individually and none was shaped by the
nine fields — but *the sentence as written claimed byte-carriage and byte-carriage did not hold*, and
a custody claim that is stronger than the facts is broken whether or not its substance survives. I do
not repeat the pattern here: **this artifact makes no byte-carriage claim at all.** What it claims
about its own authoring discipline is stated at §8.4 in a form that can be checked against its own
declared members and nothing else.

---

## 1. THE MANDATE, AND THE EXACT SHAPE OF WHAT IS PERMITTED

Member 02 is a ratified principal decision. It mandates the supply of an Attach candidate as
**authored proposed physics**, under the record's own precedent for authored objects — the member-12
pattern: authored as a proposal, carried PROPOSED_NOT_ADOPTED, ratified only by a subsequent
principal entry naming the sealed bytes by digest.

Three clauses of the mandate bind this artifact hardest, and are quoted because they are the
standard I am to be held to:

- member 02 `:22-23`: the decision *"ratifies THE MANDATE AND ITS STRUCTURE ONLY. It ratifies NO
  CONTENT: no candidate exists at sealing time, and no content is entered here."*
- member 02 `:39-43`: the candidate *"MUST declare, ingredient by ingredient, which parts are FORCED
  (with spans) and which constitute the minimal AUTHORED CORE. An authored ingredient presented as
  forced voids the candidate."*
- member 02 `:55-58`: the Attach content feeds the full preregistered battery, all frozen before any
  evaluation, and *"the candidate may not be shaped by, or tested against, any downstream target
  during authoring."*

**The open forcing slot is carried as the mandate states it** (member 02 `:51-54`):
`future_derivation_can_select_the_attach_map = NO_VERDICT`. This candidate does not prove that no
target-independent derivation could select the Attach content. If one lands, the entry dissolves into
a derivation and the authored core vanishes from the program's premise set. **I would regard that as
the best available outcome for this artifact**, and nothing here is arranged to make it harder.

---

## 2. THE TYPE TO BE INHABITED

Re-pinned from member 05 `:314-334`, span digest
`be0be903041fcface7336a2facf157c5459d7e4ccb8e3d1deb0785fbf08d52ad`:

```text
Attach : (connection/source history, finite incidence, U_N^0) -> U_N[A,J,g]

together with its representation, carrier embeddings, orientation law,
time-ordering, and common-origin certificate.  No such `Attach` exists.
```

### 2.1 The observation that sizes the problem

**The codomain is not empty, and this has not been said plainly before.** The ratified DoR-009
transition law at member 07 `:650-654` already carries a history argument and a character:

```text
D_n[a_j]      = diag(1, chi_n(a_j), 1),
W_N^(n)[a]    = tensor_(j=1)^N (D_n[a_j] S_j),
d_law(Omega_prim,N; a) = P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[a].
```

So a history-dependent transition **already exists and is ratified** — over an *abstract* discrete
label `a`. What the record lacks is not the dependence; it is the map from the physical
connection/source history to that label. Member 05 `:299-334` is exactly this: it distinguishes the
history-free `U_N^0` from the connection-bearing `U_N[A,J,g]` and rules that no map identifies them.

**The Attach problem therefore reduces to supplying one arrow into a slot that is already built and
already ratified.** That reduction is not a result about physics; it is a reading of the two sealed
signatures side by side, and it is what makes a *minimal* authored core possible at all.

---

## 3. THE CANDIDATE

### 3.1 Definition

Let `gamma_j` be the closed circuit of record cell `j` in the causal cell complex. Let `A` be the
adopted auxiliary compact connection of member 11 `:47-51`. Define:

```text
CANDIDATE ATTACH  (relay 1052, authored under member 02)

  theta_j(A)   := contour-integral_(gamma_j) A                    [the cell-circuit holonomy angle]

  Hol_(gamma_j)(A) := exp( i theta_j(A) )                          [member 11 :62-68, verbatim rule]

  chi_n(a_j)   := Hol_(gamma_j)(A)^n = exp( i n theta_j(A) )       [n the integer character]

  Q_cell       := |p_Q><p_Q|                                       [the cell comparator charge]

  D_n[a_j]     := diag(1, chi_n(a_j), 1) = exp( i n theta_j(A) Q_cell )

  W_N^(n)[A]   := tensor_(j=1)^N ( D_n[a_j] S_j )

  U_N[A,J,g]   := P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[A]      with n = +1
```

The candidate is the assignment `a_j := theta_j(A)` feeding the ratified law. **That single
assignment is the whole of the new authored content.** Everything displayed above it is the sealed
law; everything below it is forced consequence.

### 3.2 Why a loop, and not a line — the design reason, upstream

This is the one place where a reader should demand a reason, so it is given first and separately.

Member 05 `:366-380` records that the earlier attachment attempt was executed and refused promotion
on four grounds, the fourth being *"the tree carrier made all connection phases gauge-removable."* A
phase that vertex rephasing can remove carries no physical content, so an attachment built on it
attaches nothing.

Gate 4's own analysis states where removability stops (member 05 `:219`):

> Vertex rephasing removes the common phase on trees; loop holonomy remains.

and again, sharply, at member 05 `:262-266`: on a loop with holonomy `H` the character changes the
invariant to `H^n`, and *this is not removed by vertex rephasing in general*.

**So the record itself identifies the closed circuit as the only carrier on which a connection phase
survives gauge.** The candidate is placed there for that reason. This is a structural reason drawn
from a sealed adjudication of a prior failure — not a reason drawn from any downstream fitness, which
would be the one unforgivable act.

Member 13 `:19-22` independently constrains the entry route: *the compact charged handle enters the
microscopic generator only through unit-character parallel transport; curvature is the failure of
composed transports to close; it is not an independent primitive vertex.* A composed transport that
fails to close **is** a circuit holonomy. The adopted transport-only rule therefore admits this entry
route and excludes an independent vertex, which is what the candidate uses.

### 3.3 Zero-source reduction — forced, and checked

At `A = 0` the holonomy is `1`, so `chi_n = 1` and `D_n = I`, giving

```text
U_N[0,J,g] = P_0 tensor I_(3^N) + P_ch tensor tensor_j S_j = U_N^0,
```

the sealed history-free transition of member 14 `:158-169`. The candidate therefore reduces
**exactly** to the sealed object at zero connection, which is member 07 `:761`'s C1 condition and one
of the void conditions of §6.1. This is a consequence, not a design choice: any holonomy-valued
assignment satisfies it automatically.

---

## 4. THE FIVE TYPED REQUIREMENTS

### 4.1 Representation

`rho_joint`, strongly continuous and faithful on the additive-action comparison group `G_joint`;
the vertex lines form the associated Hermitian line bundle `L = P x_(rho_joint) C` for a principal
`G_joint` bundle `P`, and `U_e` is parallel transport in that associated line bundle (member 12
`:224-239`). The candidate uses `rho_joint` exactly as that member types it and adds no
representation of its own.

Member 12 `:218-222` labels the Common Total-Action Character Premise *"explicit new target-aware
physical content"* — i.e. a **premise**. The candidate carries it as a premise (TYPE-P) and does not
present it as derived.

The integer character `n` acts as `U_e^(n) = (U_e)^n` (member 05 `:241-247`). The candidate takes
`n = +1`, which is already available as an adopted premise via member 13 `:24-31` (`q_primitive = 1`)
and is recorded as such at member 05 `:426-429`. **It is not derived here and is not claimed to be.**

### 4.2 Carrier embeddings

```text
iota_cell : C[K_cell] -> span{ |r>, |p_Q>, |e_Q> }        [the record cell carrier]
q_src     : End(P_0 H_src (+) P_ch H_src) -> A_src^A       [member 07 :352-363, source port only]
L_(s(j)), L_(t(j)) -> the cell's comparator pair           [the incidence-line fibres at cell j]
```

The first two are sealed. The third — the identification of the incidence-line fibres at cell `j`
with the compared alternatives `{|r>, |p_Q>}` of that cell — is the embedding the candidate must
supply, and it is **constrained but not forced**: member 11 `:70-82` states the physical role that
constrains it (*the record carrier supplies the compared alternatives; the connection transports
their active relative phase*), while the cell itself is postulated rather than derived (member 11
`:168`). It is ledgered AUTHORED at §5, A2.

### 4.3 Orientation law — FORCED

```text
U_bar(e)^(n) = ( U_e^(n) )^-1,          U_(e2 o e1)^(n) = U_e2^(n) U_e1^(n)
```

(member 05 `:257-261`). Reversing the circuit therefore conjugates the holonomy,
`theta_j -> -theta_j`, and `D_n -> D_n^dagger`. The root orientation is separately settled: plus root
only, the exchanged root **refuted** rather than chosen against (member 07 `:586-598`). The candidate
consumes both and authors no orientation content.

**A coherence note, stated as coherence and not as confirmation** (per §0's first correction): the
ratified law puts its phase in the second slot only, `diag(1, chi, 1)`, so the cell comparator charge
is the rank-1 `Q_cell = |p_Q><p_Q|`; the adopted transport-only comparator at member 13 `:33-38`
likewise carries a rank-1 charge, `Q = |1><1|`. These are two adopted premises exhibiting the same
rank-1 shape. **That is agreement between premises, not evidence for either**, and it is recorded
only because a reader comparing the two spans would otherwise wonder whether they conflict. They do
not.

### 4.4 Time-ordering — FORCED, and already discharged where it bites

Member 16 `:16-48` gives, for the sealed write: the generator is constant through the pulse, so
`[H_j(t), H_j(s)] = 0` and `T exp[-i integral H_j] = exp(-i tau_R B_j)` — the time-ordering is
**trivial within a cell**; and `[B_j, B_k] = 0` across distinct cells, so the endpoint map is
**independent of the causal linear extension**.

The candidate's `D_n[a_j]` acts on cell `j` alone, so both facts survive it unchanged: the added
phase is cell-local and commutes across cells for the same reason the generators do. **The record had
already fixed the time-ordering; the candidate consumes that and authors nothing.**

**Its conditionality is carried, not hidden:** member 16 `:77-101` states that the live parent
authority has not derived the one-use moving-front action, so these results are conditional on that
form. The candidate inherits that condition; it does not repair it. See §6.3.

### 4.5 Common-origin certificate — SHAPE STATED, nothing instantiated

Per the mandate's "stated as a certificate, nothing instantiated numerically". The shape follows the
record's own two certificate grammars — member 07 `:774-789` (CO1–CO4) and member 10 `:327-336`
(SM-1…SM-8):

```text
CERT_ATTACH (shape only; no field instantiated here)

  AC1  OUTPUT-FREE PRIMITIVES.  The tuple feeding Attach contains no response, coupling, kappa,
       residual, battery output, or measured datum.  [inherits member 07 :775-777's CO1 form]

  AC2  ENTRY BEFORE OUTPUT.  gamma_j, rho_joint, n and Q_cell are frozen before any transition is
       evaluated; the freeze order is member 07 :662-671's T0..T8 with Attach entered at T6.

  AC3  EXECUTABLE DESCENT.  a_j is obtained from A by contour integration over gamma_j and by
       nothing else; no auxiliary selector, table, or fitted quantity intervenes.

  AC4  SEALED-INTERFACE REPRODUCTION.  At A = 0 the map returns U_N^0 exactly (§3.3), discharging
       DoR-008's standing falsifier form at member 09 :27-29 for this ingredient.

  AC5  SAME-SOURCE SUPPLY.  A is not an external field imported for this purpose: it is the
       auxiliary compact connection of the SAME complete specification Q_spec[g,a,psi,R] that
       supplies the record closure R (member 11 :83-95).  This is the clause that makes the
       candidate common-origin rather than co-located.

  AC6  MARGINAL BINDING (NOT DISCHARGEABLE NOW).  The analogue of member 10 :331-332's SM-3/SM-4
       requires a joint state on the completed object.  It is UNAVAILABLE while the completed
       object is unbuilt, and is recorded as a GAP rather than asserted.
```

**AC6 is a gap and is declared as one.** Five clauses of the certificate shape are statable now; the
sixth is not, and its span is member 10 `:209-221`, where `res_B` is shown to consume
`Omega_C0 in State(A_C0)` — an object no construction supplies. A certificate stated with a hole
named is worth more than a certificate stated whole and quietly conditional.

---

## 5. THE INGREDIENT LEDGER

*The mandate's honesty clause. An authored ingredient presented as forced voids the candidate; where
there was doubt, I labeled AUTHORED.*

### 5.1 FORCED — with spans

| # | Ingredient | Span |
|---:|---|---|
| F01 | The codomain law shape `P_0 tensor I + P_ch tensor W_N^(n)[a]` (ratified DoR-009) | member 07 `:650-654` |
| F02 | `D_n[a_j] = diag(1, chi_n(a_j), 1)` — the phase occupies the second slot only, hence `Q_cell` is rank-1 | member 07 `:650-651` |
| F03 | The cell carrier `span{|r>,|p_Q>,|e_Q>}` and the full swap incl. `S|e_Q> = -|e_Q>` | member 14 `:150-152`; member 15 `:33-38` |
| F04 | `P_ch = 1_(R\{0})(Q)`, `P_0 = I - P_ch` | member 17 `:35`; member 14 `:158` |
| F05 | Holonomy normalization `Hol_gamma(a) = exp(i integral_gamma a)`, **no additional charge-normalization factor may be inserted later** | member 11 `:62-68` |
| F06 | Gate 4's unit-weight normalized incidence class, `|a_e| = 1` | member 05 `:75-78`, `:208-217` |
| F07 | **Loop holonomy is not vertex-gauge-removable; tree phases are** — the design driver of §3.2 | member 05 `:219`, `:262-266` |
| F08 | Orientation and composition laws for the character transport | member 05 `:257-261` |
| F09 | Plus root only; exchanged root refuted | member 07 `:586-598` |
| F10 | Time-ordering: intra-cell trivial, inter-cell commuting, linear-extension independent | member 16 `:16-48` |
| F11 | Zero-history reduction `D_n[0] = I` | member 07 `:761` |
| F12 | Transport-only entry; curvature as failure of composed transports to close, not an independent vertex | member 13 `:19-22` |
| F13 | The connection's stated physical role: the record carrier supplies the compared alternatives, the connection transports their active relative phase | member 11 `:76-82` |

**13 FORCED.**

### 5.2 AUTHORED — the minimal core, counted and justified

| # | Ingredient | Status | Justification — internal coherence ONLY |
|---:|---|---|---|
| **A1** | **`a_j := theta_j(A) = contour-integral_(gamma_j) A`** — the identification of the ratified law's abstract history label with the cell-circuit holonomy | **NEW HERE. The whole of the new authored content.** | It is the unique assignment consistent with F05 (the holonomy rule, which forbids any extra normalization factor), F07 (only loop phases survive gauge), F12 (the connection may enter only through transport, and curvature is exactly failure to close), and F02 (the slot expects a `U(1)` phase). Justified by coherence with those four and by nothing downstream. |
| **A2** | `gamma_j :=` the record cell's own closed circuit | AUTHORED, constrained | The cell is postulated, not derived (member 11 `:168`), so the circuit inherits that status. Constrained by F13's role statement: the circuit must be the one whose transport compares the cell's own alternatives. |
| **A3** | `n = +1` | AUTHORED, **already adopted elsewhere — adds no new authored content** | Available as adopted premise via member 13 `:24-31` and recorded at member 05 `:426-429`. Member 05 `:280-290` is explicit that Gate 4 does **not** select `n`, that the faithful subfamily has two members, and that identifying `n = +1` with `n = -1` is `NO_VERDICT`. Carried as premise, not derived. |

**3 AUTHORED, of which exactly 1 is new to the program's premise set.**

### 5.3 What is deliberately NOT claimed

- The candidate does **not** supply the actual Lorentzian parent action, and therefore does not
  repair three of the four grounds on which the prior attempt was refused promotion (member 05
  `:370-377`). It repairs the fourth — gauge-removability — and only that. **Three of four stand.**
- The `J` and `g` dependence of `U_N[A,J,g]` is **inherited, not extended**: `J` enters only through
  the already-ratified charge control `P_ch`, and `g` only through the postulated cell complex that
  fixes `gamma_j`. A candidate with genuine source-history dependence beyond the charge sector is
  **not supplied here**, and the type's `J` and `g` slots are therefore occupied in the weakest way
  that types.
- No claim is made that this is the *only* possible Attach. Member 05 `:356-360` gives at least four
  placements with coverage unproved; that remains true and this candidate is one inhabitant, not a
  selection.

---

## 6. FALSIFIER HOOKS

### 6.1 Structural void conditions — sealed forced results this candidate must never contradict

Per member 02 `:49-50`: if any is later shown at bytes to contradict the content, the entry is void
without awaiting the cascade.

```text
V1  Gate 4's unit-weight normalized incidence class          member 05 :75-78
V2  Hol_gamma(a) = exp(i integral a), NO additional charge-normalization factor
                                                              member 11 :62-68
V3  Plus-root orientation; exchanged root refuted             member 07 :586-598
V4  DoR-008's falsifier: every output reproduces every sealed finite result on restriction
                                                              member 09 :27-29
V5  Zero-source reduction to the sealed U_N^0                 member 14 :158-169; member 07 :761
V6  Cell-local commutation and linear-extension independence  member 16 :16-48
V7  Charge/flux access: the write must vary with admitted history
                                                              member 07 :284-290
V8  The record/history non-flattening: a record state is not a state on B
                                                              member 10 :148-167
```

**7 NAMED + 1.** `V7` deserves a note: the candidate satisfies it only when the holonomy is not
identically trivial. On a carrier where every `gamma_j` has unit holonomy the candidate collapses to
`U_N^0` and access fails — which is the same defect that killed the tree-carrier attempt, now stated
as an explicit void condition against my own candidate rather than left for someone else to find.

### 6.2 Blind-test exposure — POINTERS ONLY, NEVER OPENED

Per member 02 `:47-49` and `:55-58`, the Attach content feeds the full preregistered battery, frozen
before any evaluation. Named here as pointers, and **not one of them was opened, consulted, imagined,
or optimized toward during authoring**:

```text
the preregistered battery at its frozen manifest;  kappa_record;  alpha;
every scored output downstream of the transition law.
```

That is the complete statement. No content, count, threshold, or expected direction of any battery
output appears in this artifact, because none was looked at.

### 6.3 The open-conditions list — carried, and NOT closed by this candidate

Carried from member 06 `:300` and member 02 `:71-74`, per correction 2 of §0:

```text
O1  A0 finiteness (the authored finite scalar source realization)      member 07 :896-906
O2  SR representation not closed against a stationary parent           member 15 :108-128
O3  moving-front causal parent not derived                             member 16 :77-101
O4  write/tail join and analyticity not derived                        member 11 (R3.4 tail) :36-41
O5  continuum ordering / connected preparation                         member 15 :123-133
O6  residual family and character freedom                              member 07 :707-714; member 05 :273-290
```

**This candidate touches O6 only, and only by carrying `n = +1` as an already-adopted premise — which
narrows nothing that was not already narrowed.** O1–O5 are untouched. The Attach wall was never the
sole unresolved load, and this artifact does not pretend that filling it closes the section.

---

## 7. Q-92(c) EXHIBITION — COHERENCE DISPLAY ONLY

*Not an exhibition of entries. The §3.1 ruling standard of my relay-1050 artifact, upheld by member
06 `:171-183`, governs: an SR-face or partial determination is a restriction or lower bound, not a
populated completed-`Obj_0` entry. The question here is only: **which fields would the candidate's
existence determine, if it were entered?** Each is traced, and each is conditional on entry — which
has not occurred.*

The nine fields are at member 19 `:601-613`.

| # | Field | Would the candidate determine it? | Trace |
|---:|---|---|---|
| 1 | `PhysicalCategory_0` | **No** | The producer-class membership test at member 18 `:405-417` turns on P4–P7 as a package; Attach supplies none of the quotient/measure/effects/domain data. |
| 2 | `PhysicalObjectType_0` | **No** | Member 19 `:614-616` refuses the operator/functional/flow/generator union; Attach adds an argument to an existing map, not a type for `Obj_0`. |
| 3 | `PhysicalArityAndOperations_0` | **Yes, bounded** | The operation consuming the field sector would exist: `U_N[A,J,g]` with the connection as a genuine argument, arity and composition fixed by F01/F10. Bounded: it is one operation, not the full P4–P7 operation set. |
| 4 | `PhysicalOperationDomains_0` | **Yes, bounded** | The domain of that operation would be fixed — connection configurations on the cell complex, times the finite source-record carrier. Bounded: P4's gauge-fixed physical quotient and invariant contour measure are **not** supplied, so the field sector's own domain package stays open. |
| 5 | `PhysicalOperationCodomains_0` | **Yes, bounded** | Codomain is the ratified law's, unchanged (F01). Bounded for the same reason as #4. |
| 6 | `PhysicalParameterRecord_0` | **No** | The record would *gain* parameters (the connection's own configuration data) while retaining the unfixed transient `lambda` and the unselected character freedom of O6. Gaining parameters is not determining the record. |
| 7 | `PhysicalSupportAndCommonCore_0` | **No** | AC6 is a declared gap: the completed-object common core requires a joint state that no construction supplies (member 10 `:209-221`). |
| 8 | `PhysicalRepresentationData_0` | **No** | O2 stands: the stationary parent is not implemented in the endpoint GNS (member 15 `:108-128`). Attach does not touch it. |
| 9 | `PhysicalAlgebraicCausalAnalyticConditions_0` | **No** | Algebraic and causal conditions would extend to the field argument; the **analytic** third remains unreached (O4, O5). A field that splits three ways and closes two is not determined. |

```text
EXHIBITION_COHERENCE = 3/9-WOULD-DETERMINE (fields 3, 4, 5; each bounded, each conditional on entry)
```

**This is a coherence display and nothing more.** No field moves. The tally of record stands at
`0/9/0` and this artifact does not alter it, because entry has not occurred and the mandate reserves
that act to the principal.

---

## 8. FREEDOMS, FLATTENING, AUDITS, CUSTODY

### 8.1 FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN (unchanged; DoR-013 family level, no member bound)
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 02's mandate structure, its honesty clause, its open forcing slot and its entry mechanics;
  member 05's Attach type, absence adjudication, placement family, character non-selection and the
    prior kill's four grounds; member 06's three corrections and its custody finding; member 07's
    ratified law, freeze order, root determination and self-kill; members 08/09's read rule and
    no-member clause; member 10's restriction and non-uniqueness; member 11's holonomy rule and role
    statement; member 12's representation premise; member 13's transport-only rule and q_primitive;
    members 14/15/16/17's cell, endpoint, write geometry and projectors; members 18/19/20's
    receivers; all charter fences.

AUTHORED HERE:
  ONE new ingredient — A1, the identification a_j := theta_j(A).
  Two further authored ingredients that add nothing new to the premise set: A2 (constrained by an
    already-postulated cell) and A3 (already adopted elsewhere).

DERIVED HERE:
  the reduction of the Attach problem to one arrow into an already-ratified slot (§2.1);
  the zero-source reduction of the candidate to U_N^0 (§3.3);
  the survival of the sealed time-ordering facts under a cell-local phase (§4.4);
  the certificate shape and the identification of AC6 as its gap;
  the void-condition list, including V7 against the candidate itself.

SELECTED HERE:
  nothing.  No member of the origin family, no state, no scalarization, no placement beyond the one
  authored inhabitant offered as a candidate, no dimension, no scale, no battery output.
  NOTHING IS ADOPTED, RATIFIED, ENTERED, PROMOTED, OR FROZEN.  No stop is lifted.  No decline filled.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  Every scale symbolic.
```

### 8.2 Prose-digest audit

```text
LIVE MEMBERS DECLARED: NONE.
STRICT = every member rehashed at its full archive-root path at seal time.
STABLE = the digest pinned in the closure table above.
RESULT: 20/20, STRICT == STABLE.   Re-pinned span be0be903… recomputed from member 05 at seal time.
```

### 8.3 Flattening check — S01–S37 walked

- An **authored ingredient** was not flattened into a **forced** one. §5 is the whole answer to this,
  and where doubt existed (A2's circuit, A3's character) the label went to AUTHORED.
- A **ratified slot** was not flattened into a **ratified filling**: member 07's law is ratified with
  an *abstract* label; the identification of that label with holonomy is A1 and is authored.
- **Agreement between two adopted premises** was not flattened into **confirmation** (§4.3) — this is
  the shape of the error member 06 corrected in `kappa_ch`, and it is not repeated.
- A **candidate** was not flattened into a **selection**: at least four placements remain; this is one
  inhabitant.
- **Repairing one of four kill-grounds** was not flattened into **repairing the kill** (§5.3).
- A **certificate shape** was not flattened into a **discharged certificate**: AC6 is a named gap.
- **Filling the Attach wall** was not flattened into **closing the section**: §6.3 carries O1–O6.
- A **coherence display** was not flattened into an **exhibition**: §7 moves no field.

### 8.4 Custody, and the authoring-discipline claim in a form that can be checked

**No byte-carriage claim is made anywhere in this artifact** (see §0). What I claim instead is narrow
and checkable against the declared members alone:

> Every ingredient of §3.1 is either (a) quoted from a member span listed in §5.1, or (b) listed in
> §5.2 as authored. There is no third category, and no ingredient appears in the candidate that is
> absent from both tables.

That is a closed, finite claim about two tables and one definition, and the opposite lane can falsify
it by finding a single symbol in §3.1 that appears in neither. I invite exactly that test.

- **BUILDER NEVER VERIFIES OWN WORK.** Everything here is **CLAIMED** until the Codex 2 check.
- **SELF-CITATION BAR OBSERVED.** My relay-1050 artifact is not a member and is not cited as a
  witness; where its results were needed, member 06 — the opposite lane's independent re-derivation —
  was cited instead.
- **NO SUBAGENT DELEGATION.** One lane, whole relay.
- **WRITES:** this artifact and its generator in `workspace/`; ACK and DONE in `relay_outbox/`.
  **No register, plan, tracker, or git action of any kind, read-only included.**
- **PE-1..PE-17** pointer-only, ledger shut. **omega_phys** untouched. **No member bound.** No freeze.

---

## 9. THE CERTIFICATE

The certificate is the digest table: twenty members at their full archive-root paths, rehashed at
seal time, plus the re-pinned Attach span `be0be903041fcface7336a2facf157c5459d7e4ccb8e3d1deb0785fbf08d52ad`
recomputed from member 05 in this relay. There is nothing below it.

---

## 10. FINAL LINES

```text
CLOSURE = declared-first (byte 0; end byte 00006449 computed as a fixed point on bytes; 20 members;
     0 live; re-pinned span be0be903… recomputed from member 05)

CANDIDATE = CONSTRUCTED (typed requirements 5/5)
     representation §4.1 (rho_joint carried as premise, n=+1 carried as adopted premise);
     carrier embeddings §4.2 (two sealed, one authored — A2);
     orientation law §4.3 (FORCED, authored content nil);
     time-ordering §4.4 (FORCED, authored content nil; its own conditionality carried);
     common-origin certificate §4.5 (shape stated: AC1–AC5 statable, AC6 a NAMED GAP).
     NOT CLAIMED: the Lorentzian parent action.  Three of the prior kill's four grounds STAND;
     the candidate repairs the fourth (gauge-removability) and only that.  J and g dependence is
     inherited, not extended.

LEDGER = 13-FORCED / 3-AUTHORED
     FORCED  F01–F13, each with its span at §5.1.
     AUTHORED A1 (NEW — a_j := theta_j(A), the cell-circuit holonomy identification: the whole of the
       new authored content), A2 (constrained by the already-postulated cell), A3 (n=+1, already
       adopted elsewhere — no new authored content).
     THE MINIMAL AUTHORED CORE IS ONE INGREDIENT.

CERT_SHAPE = STATED (AC1–AC5) / GAP (AC6, span member 10 :209-221 — res_B consumes an Omega_C0 that
     no construction supplies)

VOID_CONDITIONS = NAMED (8: V1–V8), including V7 stated against the candidate itself — trivial
     holonomy collapses it to U_N^0 and fails charge/flux access

EXHIBITION_COHERENCE = 3/9-WOULD-DETERMINE (fields 3, 4, 5; each bounded, each conditional on entry;
     no field moves and the 0/9/0 tally of record is unaltered)

OUTPUT_INSPECTION = NONE-CERTIFIED.  The battery, kappa_record and alpha are named as pointers at
     §6.2 and were not opened, consulted, imagined, or optimized toward.  The candidate's one design
     decision (§3.2) is justified by a sealed adjudication of a PRIOR FAILURE, upstream in every
     direction.

PROSE_DIGESTS = 20/20, STRICT==STABLE (0 live members)
CHAIN_INVOKED = true (RELAY_PASTE_1054_..._DARIO_V001 present in relay_inbox and picked up after
     this DONE; 1053 is a CODEX 2 assignment and is not this lane's to obey)
VERB_AUDIT_SELF = CLEAN

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**THIS ARTIFACT IS PROPOSED_NOT_ADOPTED.** Per member 02 `:60-66`, ratification of content is a
**second, separate principal act**: an ENTRY addendum must name this artifact's digest and the
check's digest and record the principal's explicit confirmation. **Until that addendum exists, no
construction may consume this candidate as premise.** The registrar may not enter it; the lanes may
not enter it; a conversational summary may not substitute for the addendum. Nothing downstream —
Obj_0 inhabitation, ExactSig_0, the inventory, the ports, Θ_hist, the six legs, B03 — may treat any
line of this artifact as available.

All findings **CLAIMED** until the opposite-lane check.

**Where I would attack this first.** Not A1's coherence — it is tightly constrained by F05, F07, F12
and F02, and I do not think it can be moved much without breaking one of them. **The soft place is
A2**, the circuit. The candidate needs `gamma_j` to be a genuine closed loop with non-trivial
holonomy, and the record does not derive the cell (member 11 `:168`), so nothing at bytes guarantees
the postulated cell *has* such a circuit rather than a tree. **If the cell's causal structure is
tree-like, V7 fires against my own candidate and it collapses to `U_N^0`** — the very failure that
killed the prior attempt, re-entering through the postulate instead of through the carrier. That is
the substitution the check should try, and A2 is where I would send it.

A second target: §2.1's reduction. I claim the codomain slot is already ratified and the gap is one
arrow. If the check reads member 07 `:650-654`'s `a` as *not* the same kind of object the Attach type
means by "source history", then the reduction is wrong and the authored core is larger than one
ingredient. I read them as the same slot; that reading is the load the whole ledger rests on.
