# STAGE 8 — TRANSPORT LAW POSING, ADVERSARIALLY CHECKED: SEALS, SPANS, COMPLETENESS, SUB-LAW, TRAPS, FIRST STEP
## BLIND ADVERSARIAL VERIFIER (TRANSPORT-POSE-CHECK), CROSS-LINEAGE — DEFAULT = REFUTE — [CLAIMED]

Date: 2026-08-13
Subject: STAGE8_TRANSPORT_LAW_POSED_V001.md
Subject digest, recomputed at path this session BEFORE reading:
  22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  (MATCHES the tasked digest and the .seal.sha256 sidecar)

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held: CONNECTION-ONLY, SCALE-FREE, symbolic. No numeric value of anything
computed, bounded, or evaluated — every integer displayed below is a sealed-text
quotation or an exact structural count. No scale as authority; scale-bearing sealed
text read SUSPECT-ONLY (MAJ quotations inside FR, charter diamond text). No imported
GR; no faithfulness premise. No register, tracker, plan, road, or lens file read.
DISCLOSURE: the stiffness ledger (ABSOLUTE_STIFFNESS_SELECTOR_ROUTE_LEDGER_V003) is
in the subject's SEALS_VERIFIED block and its span [4221,4312) is load-bearing for
trap T4; I verified its seal and extracted THAT SPAN ONLY — the same single-sentence
consumption the posing disclosed. No git action. Output name probed at path before
write: ABSENT (artifact and sidecar).

---

## 1. SEALS VERIFIED AT PATH (ALL 30 SOURCES + SUBJECT)

Every file in the subject's §1 SOURCES block was re-hashed at path with
`shasum -a 256` this session BEFORE reliance. ALL 30 DIGESTS MATCH the subject's
pasted values exactly — the tasked three (3e35ffe2 GB, 4d072e76 FR, 5e49d209 FORM),
the seven located sources, the V011 packet member (aa7c6d49, under
review_packets/STAGE7_QSPEC_CANDIDATE_V001/), the 18-artifact B1 arc, the five
RA27-2/coframe lineage files, and the six consumer/panel files. The T6
discriminator was independently re-verified: the TOP-LEVEL same-named V011 file
hashes 20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48 — differs
from the packet member exactly as the subject states. NO SEAL FAILED. Digests are
not re-pasted here; they are the subject's §1 values, each reproduced bit-exact.

Approximately 40 byte spans cited by the subject's flag block were extracted at
path and compared against the claims they anchor. EVERY EXTRACTED SPAN MATCHED its
claimed content; no span landed mid-statement or on foreign text. Spot list of the
decisive ones: charter [674,1548) and [8846,10060); D012 rows [44158,46053) all six;
OD3-LEVER [196,236) [8468,8638) [12065,12267) and file lines :128-135; CREF-VS-D3
[177,321) [3969,4366) [8126,8401); LEDGER [4221,4312); REVIEW [7220,...) [8235,8372)
[8409,8465); V011 [44595,44690) [46712,47444) [47025,47247); LANE2 [27184,27469);
FR [1779,1830) [12864,13402) [13701,13729) [14730,14926) + §4 board; B1B [1402,1577)
[2239,2275) [8516,8795) [9451,9954); 792 [23332,23876); 807 [15778,16317); 806
[16553,17290); 0c535497 [5891,6558) [6859,6959) [9381,9472) [12363,12740); 811
[17191,17496); 813 [17330,18123) [18641,19104); 812 [21770,21923); GB [14940,16364)
[17360,17952) [26512,27279); FORM [19503,20201); CANC [16274,17158); STRUCT
[9163,9249); ANNIH [21727,22778) [22319,22726); 660e0c14 [382,1625); 66f078ba
[4826,7883) boundaries.

---

## 2. ATTACK 1 — COMPLETENESS OF THE OBLIGATION LIST

```text
FINDING 1 (the one refuting item): THE VOLUME FIELD IS MISSING FROM O6.
  D012's RA27-3 row at 74bbb7aa[44665,45007) names SEVEN fields verbatim:
  "incidence, degree, coframe, connection, VOLUME, support, and current density".
  The subject's 6a-6g itemization covers incidence (as two halves, 6a/6b), coframe
  (6c), connection (6d), degree (6e), support (6f), current density (6g) — SIX of
  the seven fields. VOLUME appears nowhere as its own item. It is not folded into
  6a: FR's own board (§4) carries them as SEPARATE rows — row 1 "incidence —
  four-volume half | SUPPLIED | 742 §3" and row 5 "volume | SUPPLIED | 739 §2.2;
  volume_weight_natural_on_full_D3 = true, exact" — and FR's own tally reads "Two
  of seven supplied (volume; incidence's four-volume half)".
  IMPACT: LOW, and in the flattering direction — the omitted field is the one
  field sealed SUPPLIED, so the omission hides no gap and UNDERSTATES the built
  stock (the tally's "fully BUILT 2 sub-fields" would read 3 with volume). But the
  list as typed does not cover its own sealed demand, so completeness is REFUTED
  as posed, correctable by one added line.

CHECKED AND CLEAN — NOTHING INVENTED: every listed obligation traces to a
  verified span. Charter block [8846,10060) contains exactly O-D1/O-D2/O-D3 and
  the flag line reads "accepted_obligations = O-D1 O-D2 O-D3" with
  "new_obligation_flagged_not_accepted = O-D4" — O1-O4 exactly as typed. D012
  RA27-2..6 rows match O5-O9. GB S1-S4 at [14940,16364) match O11-O13 (S2 = the
  receiver+bridge, distributed over O6g/O10). B1B E4/E5 at [9451,9954) grounds
  O10's MISSING clause verbatim. The O14 member display at 0c535497[12363,12740)
  is the 10-component tuple as claimed.

RA27-1 EXCLUSION — DEFENSIBLE OF RECORD: its row at [44158,44323) is a local
  J_star matrix certificate ("exact local J_star matrix and J_star^2=-I_6
  certificate ... closes C27.2"), finite work, not transport-shaped; and RA27-6's
  own row names its premises as "RA27-2 through RA27-5" only. The subject cites
  the RA27-1 span in its §2 preamble, so the row was seen and excluded, not
  missed. No refutation.

O2 PROBE REPLICATED: grep over workspace .md for the amendment's own wording
  ("EXHAUSTION UNIT" + FULL tau_R) hits only the charter, one role-grid artifact
  (a P3/O-D1 discussion, not an amendment), and the subject itself. Wider "FULL
  tau_R" mentions exist (RL2B opening, Ward-identity answer, T7 addendum) but all
  are mentions of the D2 typing, none is the append-only re-typing amendment.
  O2 = UNBUILT stands.

TYPING SPOT-CHECK, THREE MOST LOAD-BEARING BUILT CLAIMS — ALL CONFIRMED AT
CLAIMED STRENGTH:
  6a  FR §4 board row 1: "incidence — four-volume half | SUPPLIED | 742 §3" —
      verbatim as carried.
  6b  Carrier 97f073c1: sd*_1/sd*_2 typed with the direction theorem, cochain-map
      property sd*_2 d_1' = d_1 sd*_1 "derived combinatorially and
      MACHINE-VERIFIED on all 24 parent squares of BOTH A1 and A2"; crosscheck
      614e20c8 flags verbatim: INCIDENCE_HALF = CONFIRMED; COCHAIN_MAP =
      replicated (257 trials each on A1/A2-Freudenthal/A2-barycentric; 771 total,
      0 mismatches; exact matrix mismatches 0); NATURALITY = NOT NATURAL — the
      subject carries the dead min-norm rule and the J_1 parameter honestly;
      B1B[8516,8795) "E1 PROVED. Q-702 ..." verbatim. Status "BUILT at generator
      level, CLAIMED/cross-confirmed" is exactly right.
  6c  d6f490b8 (753) discharges FR's named simplicial-coframe spec gap in its own
      words ("At 751 I named the simplicial coframe a spec gap ... computing the
      frame is verification of what is sealed"); 806 [16553,17290): a' supplied
      on BOTH generators, components e_K'/mu_K'/a_K'/F_K' = 4 of 10, exact
      identity-law exhibit — "BUILT AS NON-UNIQUE FAMILY (no member selected)"
      is the correct type, with 6d carrying the FREE caveat.

TWO TYPING SLIPS FOUND (corrections, not refutations):
  SLIP 1 — the §2 tally line "UNBUILT 9" enumerates ELEVEN members (O1 O2 O3 O4
      O6e O7 O8 O9 + the trio O11-O13); it counts 9 only if the trio collapses
      to one entry, which the sentence does not say. The per-item typings above
      the tally are all correct; only the count is inconsistent.
  SLIP 2 — O14's "MISSING: the residual 3-item path f_g, F_g, eta_resp" conflates
      811's D_K_PATH with the member tuple. Of the 10 components the missing ones
      are f_g, F_g, s_g, S_g (812's sealed classification at [21770,21923):
      DERIVABLE-FORCED 2 (s_g, S_g) / UNDECIDABLE 3 / OPACITY-BOUND 2, the
      opacity-bound pair since supplied per 811/813); eta_resp is O7's object,
      not a tuple component. The subject carries s_g/S_g correctly inside 6g and
      then drops them from O14's MISSING line.
```

---

## 3. ATTACK 2 — THE MINIMAL SUB-LAW

```text
GENUINELY WEAKER THAN THE FULL LAW: CONFIRMED. cls(j) forgets the chain to two
  booleans (strictly smaller than the functor) and consumes no density slot, no
  d_g, no tau (strictly smaller than the receiver). It does not secretly need the
  full functor, and no forbidden import is consumed by its statement: the blocks
  are FORM's derived kinematics from sealed d_1 d_0 = 0 (span verified at V011
  [44595,44690)), and the variety is the sealed 807 object (span verified).

BUT A STRICTLY WEAKER SUFFICIENT OBJECT EXISTS — THE POSING'S PAIR IS NOT THE
MINIMUM:
  FORM's cited span [19503,20201) states the dichotomy's exact demand:
  "specifically whether the write direction has nonzero components in BOTH the
  flux block im(d_1^dagger) and the dim-32 flux-free physical block H". That is
  ONE boolean per cell — AND(j) := [phi_f(j) != 0 AND phi_H(j) != 0] — not the
  pair. The dichotomy (degenerate one-sector factorization XOR irreducible
  rank-one cross term) closes on AND(j) alone: cross term present iff both
  components nonzero. And the AND-class's member-independence certificate is
  STRICTLY WEAKER than the pair's: two variety members realizing patterns (1,0)
  and (0,1) kill the pair-class (the posing's falsifier fires) yet leave AND(j)
  constant at false — the sub-law SURVIVES in AND form where the posed pair form
  dies. Same first-buildable precondition, same secondary falsifier, weaker
  primary falsifier (only a both-vs-not-both disagreement kills it).
  => MINIMAL_SUBLAW_SOUND = STRONGER_FOUND (the weaker object: the per-cell
  AND-boolean block-class with its member-independence certificate).

TWO CORRECTIONS TO THE SUFFICIENCY CLAIM AS WORDED:
  CORRECTION A — "unblocks consumer (a) exactly and only" drops a conjunct
  sitting inside the very span cited: FORM [19503,20201) demands the pattern
  "together with, AT POST-LIMIT STRENGTH, the Gate-5 kernel C on the cross
  pairing". The block-class closes the dichotomy at derived-form strength; at
  post-limit strength consumer (a) additionally needs the Gate-5 kernel C, which
  no transport fragment supplies. The sub-law unblocks the FORM-level split, not
  the post-limit claim.
  CORRECTION B — the sub-law's input list omits the chain it projects. cls(j)
  (and AND(j)) is a function of the TRANSPORTED WRITE CHAIN, and the write
  chain's realization is itself sealed NOT-SUPPLIED: GB S1 ("Nowhere in the
  sealed construction is h_j displayed as an incidence sum"; FORM :164-165 as
  quoted there: "an incidence datum of the write, NOT sealed as an object");
  GB S4 (pairing probed along ONE frozen profile; a chain is determined only
  against a spanning family); ANNIH [22319,22726) ("exactly FORM's G3
  NOT-SUPPLIED write-chain realization"). Building the sub-law therefore
  consumes an object whose determination is the subject's own O11/O13 (or the
  f_g bridge) — all UNBUILT. The "WHY WELL-FORMED FROM SEALED CONTENT" paragraph
  names the blocks, the variety, and the chain map, but not this input. Not a
  forbidden import and not the full functor — but an unbuilt input left unstated
  in the well-formedness argument.

CONSUMERS (b)/(c) FRAGMENTS: verified at spans (CANC [16274,17158) — the
  difference structure with the O(|r|) prefactor whose leading coefficient is
  the write current; STRUCT [9163,9249) verbatim; ANNIH [21727,22778) and
  [22319,22726) verbatim). The claim that neither is comparable to (a)'s
  fragment in a common order is fair: (b)/(c) need support/regularity/derivative
  typing, (a) needs projections — neither contains the other.
NONE_SHORT_OF_FULL = FALSE: CONFIRMED (and doubly so — an even shorter object
  exists, above).
```

---

## 4. ATTACK 3 — THE TRAPS

```text
TRAPS_FAITHFUL = YES. All seven carried at spans, nothing dropped, nothing
overstated:
  T1  V011 [46712,47444): the C_ref freeze is a declaration of a test class —
      verbatim. LANE2 [27184,27469): "classifies C_ref as a frozen audit class.
      No theorem ... derives it as the unique physical record complex" —
      verbatim.
  T2  6934f858 [177,321) "(i) SAME OBJECT ... HIS to resolve"; [3969,4366)
      slivers inside D3 outside C_ref, "NO SEALED ARTIFACT ANYWHERE RECONCILES";
      [8126,8401) conflict_resolved = false, resolution_holder = principal,
      D3_narrowed = false, D3_freeze_withdrawn = false — "D3 spec HELD" is a
      fair carry of those two false flags.
  T3  Charter [674,1548): Option 4 = diamond decomposition, exhaustion side —
      verbatim; OD3-LEVER [8468,8638) "a DIFFERENT obligation ... I do not
      identify them" and [12065,12267) "Different kinds, on separate dockets,
      fenced apart" — verbatim.
  T4  OD3-LEVER [196,236) "lever_status = LEVER_ILLUSORY_WRONG_TYPE" — exact;
      LEDGER [4221,4312) "a finite-cell answer is called physical without a
      derived stitching or continuum rule" — exact; the no-cross-booking reading
      is the lever artifact's own.
  T5  REVIEW [7220,...) is the W3 CIRCULARITY section, CONFIRMED; [8235,8372)
      "It is unambiguously a premise" and [8409,8465) "assume response
      naturality -> classify intrinsic measure" — verbatim; the twin name-match
      trap is present in B1B §1.3 in the artifact's own words ("Reading a
      verdict name as the proposition it resembles is the name-match trap").
  T6  Both digests re-verified at path this session; packet member cited,
      top-level 20a3a17d not cited anywhere in the subject.
  T7  FR [13701,13729) "**No transport law exists.**" and [14730,14926) the
      ZERO-corpus-wide census — verbatim, dated census facts; the B1-arc
      supersession is real (carrier + crosscheck seals verified); and the
      subject's consistency note about G3-PANEL-UNDER is confirmed at that
      artifact's own summary ("the sealed corpus contains no content that FIXES
      the block-split ... the diamond-to-complex transport ..."), which indeed
      coexists with typed partials that fix nothing. The double-booking bar is
      stated in both directions, correctly.
ONE NOTE, NOT A FAILURE: the O1 gating observation (O-D1 gates O-D3, quantifier
  over a class not known non-empty) is carried from OD3-LEVER file lines
  :128-135 — verified verbatim at those lines.
```

---

## 5. ATTACK 4 — THE FIRST BUILDABLE STEP

```text
FIRST_BUILDABLE_SOUND = YES. Every named input is sealed, seal-verified at path,
and sufficient; the step consumes nothing unbuilt, unsealed, or forbidden:
  - sd*_1 exists of record with the d_1-square: carrier 97f073c1 (sd*_2 d_1' =
    d_1 sd*_1 derived and machine-verified) + exact crosscheck 614e20c8 (771
    trials, 0 mismatches, exact matrix identity) + 795 (590b3979): "Clause 1 —
    L_id = id. PROVED" and "Clause 2 — per-generator existence. PROVED" —
    verbatim.
  - the composite-closure step is sealed in the carrier's own text ("a
    composition of chain maps is a chain map, and functoriality on composites
    follows by induction on composite length", applied to sd*), so restricting
    the certificate to generators is licensed.
  - the cochain structure is sealed at V011 packet member [44595,44690):
    "(d_0 lambda)_e=lambda_t-lambda_s, (d_1 a)_f=..., d_1 d_0=0" — verbatim.
  - the integer complexes are sealed: FR carries "16 subcubes" (MAJ quotation)
    and "24 simplices, 16 vertices, 65 edges" (738 lineage, independently
    reconstructed there) — the exact counts the subject pins.
  - TYPE CHECK, run by me: sd*_0 : C^0(K')->C^0(K); both composites
    sd*_1 . d_0' and d_0 . sd*_0 are maps C^0(K') -> C^1(K). The posed identity
    is well-typed with the aggregation variance (K'->K) the direction theorem
    fixes for sd*_1 — the same variance trap that killed V011's J_1/J_2 reuse
    is not being re-run here.
  - it consumes NO member of the 1887-dim variety, no density, no response
    value, no scale; it selects nothing. The yield claim (transport-stability
    of im(d_0) => well-defined quotient => block pair well-defined under
    transport) is the correct precondition of the block-class sub-law in either
    the posed pair form or the weaker AND form.
ONE WORDING CAVEAT (does not unsound the step): the refuter clause grounds the
  finite exhaustion of sd*_0 candidates in "the sealed direction theorem", but
  the direction theorem of record covers sd*_1/sd*_2, read off the sealed
  Ext_g/Flux_g displays — no sealed Vert_g display exists for degree 0. The
  exhaustion is nonetheless decidable without it: compatibility is LINEAR in
  sd*_0 over finite-dimensional spaces (16-vertex complexes), so existence /
  nonexistence / universal-counterexample are exact finite rank computations.
  The builder should cite finite-dimensional linear decidability, not the
  direction theorem, for the quantifier.
```

---

## 6. ATTACK 5 — PROVENANCE AND INJECTION

```text
PROVENANCE = CLEAN, both directions.
  BACKWARD: every load-bearing claim in the subject's flag block traced to a
  verified seal and a verified span; ~40 extractions, zero mismatches, zero
  mid-statement landings; V011 spans bind to the packet member only (re-hashed
  both copies). The B1-arc statuses are carried at CLAIMED strength throughout —
  no silent promotion found anywhere in the subject.
  FORWARD: the posed law's definition is grounded in GB [17360,17952) — the
  sealed naming of "the Ref_a-indexed current-density receiver PLUS the
  exhaustion<->refinement index bridge ... equivalently a ratified successor of
  the transition law displaying h_j[a_j] as an explicit incidence sum" — i.e.
  the object posed is the object the sealed record itself names as missing, not
  an object invented by the tasking. Register numbers (Q-697/701/702) appear
  only as carried citations inside sealed artifacts; I did not check any
  register and neither did the subject.
INJECTION = none. No verdict in the subject rests on its tasking text in place
  of a span; the two tasking-supplied items (the three tasked sources, the
  ledger read) are disclosed as tasked and were consumed at verified seals and,
  for the ledger, at one verified span. My own verdicts above each cite a span
  or a recomputed digest, not my tasking.
```

---

## 7. FINAL FLAG BLOCK

```text
LIST_COMPLETE = REFUTED(missing item: the VOLUME field of RA27-3 — D012
  74bbb7aa[44665,45007) names seven fields including "volume"; the subject's
  6a-6g covers six (incidence twice as halves), and FR §4 carries volume as its
  own row 5, SUPPLIED per 739 §2.2, distinct from row 1's incidence/four-volume
  half; FR's own tally "Two of seven supplied (volume; incidence's four-volume
  half)". IMPACT LOW: the omitted field is sealed SUPPLIED, so no gap is hidden
  and the built stock is understated; fix is one added line "6h volume BUILT".
  Nothing invented: all 14 items trace to verified spans; RA27-1's exclusion is
  defensible (local J_star certificate, closes C27.2; RA27-6's premises row
  names RA27-2..5 only). Typing of the three most load-bearing BUILT claims
  (6a, 6b, 6c) CONFIRMED at claimed strength at spans. Two typing slips: tally
  "UNBUILT 9" enumerates 11 items; O14's MISSING line should read f_g/F_g/
  s_g/S_g of the 10-tuple (812[21770,21923): s_g/S_g DERIVABLE-FORCED,
  admissibility open) with eta_resp belonging to O7, not the tuple.) ;
MINIMAL_SUBLAW_SOUND = STRONGER_FOUND(the weaker object: the per-cell AND-boolean
  class AND(j) := [phi_f(j) != 0 AND phi_H(j) != 0] with its member-independence
  certificate — FORM[19503,20201) demands exactly "nonzero components in BOTH"
  blocks, one boolean; the dichotomy closes on AND(j) alone; and its certificate
  is strictly weaker: members realizing (1,0) vs (0,1) kill the posed pair-class
  but leave AND constant. Two corrections to the posed claim besides: (A)
  "unblocks (a) exactly and only" holds at derived-form strength only — the same
  span conjoins "together with, at post-limit strength, the Gate-5 kernel C on
  the cross pairing", which the sub-law does not supply; (B) the sub-law's
  argument — the transported WRITE CHAIN — is itself sealed NOT-SUPPLIED (GB S1
  [14940,16364); GB S4; ANNIH[22319,22726)), so building cls or AND consumes an
  O11/O13-shaped unbuilt input the WHY-WELL-FORMED paragraph does not name. Not
  the full functor, no forbidden import.) ;
TRAPS_FAITHFUL = YES (T1-T7 all verified verbatim at spans: T1 V011[46712,47444)
  + LANE2[27184,27469); T2 6934f858 all three spans, "spec HELD" = the two false
  withdrawal flags; T3 charter[674,1548) + OD3-LEVER[8468,8638),[12065,12267);
  T4 OD3-LEVER[196,236) + LEDGER 980a38ca[4221,4312); T5 REVIEW[8235,8372),
  [8409,8465) + B1B §1.3 name-match trap present; T6 both V011 digests re-hashed
  this session, only the packet member cited; T7 FR[13701,13729),[14730,14926)
  verbatim with the supersession real and the G3-PANEL-UNDER consistency note
  confirmed at that artifact. Nothing dropped, nothing overstated.) ;
FIRST_BUILDABLE_SOUND = YES (all inputs sealed and verified: carrier 97f073c1
  d_1-square + composite closure; crosscheck 614e20c8 771/0 exact; 795 L_id=id
  and per-generator existence PROVED; V011 aa7c6d49[44595,44690) cochain display
  verbatim; complexes 16 subcubes / 24 simplices / 16 vertices / 65 edges
  confirmed in FR; identity type-checks, both sides C^0(K')->C^1(K); consumes no
  variety member, no density, no scale, selects nothing. One wording caveat: the
  refuter's finite-exhaustion should rest on finite-dimensional linear
  decidability, not "the sealed direction theorem", which of record covers
  degrees 1/2 only — no sealed degree-0 display exists.) ;
INJECTION = none ;
POSE_VERDICT = SOUND-WITH-CORRECTIONS(C1 add the volume field as O6h BUILT (FR §4
  row 5; 739 §2.2) — the seven-field itemization currently covers six; C2
  restate MINIMAL_SUBLAW sufficiency as derived-form strength only, carrying
  FORM's post-limit Gate-5 kernel C conjunct; C3 name the write-chain
  realization (GB S1/S4; ANNIH's G3 NOT-SUPPLIED naming) as an input of the
  sub-law — it is O11/O13-shaped and unbuilt; C4 the minimum object is the
  AND-boolean class, strictly weaker than the posed pair; C5 fix the tally
  arithmetic ("UNBUILT 9" vs 11 enumerated, or say the trio counts as one);
  C6 fix O14's MISSING line to f_g/F_g/s_g/S_g of the tuple, eta_resp to O7;
  C7 reground the d_0-square refuter's quantifier in finite-dimensional linear
  decidability. None of the seven touches the posing's architecture: the
  14-item decomposition (modulo C1), the trap set, and the first step all
  stand.) ;
PROVENANCE = CLEAN ;
MACHINERY_USED_BY_ME = no (reads, shasum -a 256 at path, byte-span extraction,
  grep existence probes, and one symbolic type-check; no numeric evaluation of
  any physical quantity, no member binding, no register/tracker/plan/road/lens
  read; ledger consumed at its one tasked sentence only; no git action).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until checked.
```
