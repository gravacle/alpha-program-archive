# Primitive Source-Record Odd-Component Identification Principle v002

Date: 2026-07-23

## Why v002 exists

Hostile review found that version 001 equated two differently typed objects:
a source-only Hermitian chiral block and a complete durable-record closure
map. Version 002 preserves the adopted physical idea but states it in a
well-typed form.

Version 001 is rejected as authority.

## Logical status

This is a target-value-free, target-aware, forward-sealed Level-1 Gravacle
principle adopted after `SOURCE_RECORD_MASS_TRANSFER_GATE_V001.md`.

It is an adopted hypothesis, not a theorem of the older carrier,
co-registration, recoverability, or comparator results. It contains no
measured mass, alpha value, cosmological endpoint, fitted coefficient,
response normalization, or post-response operator choice.

Historical target blindness is not claimed. A genuinely unused prediction is
required after the complete dynamics is derived.

## Complete closure object

The complete record-forming generator must act on the source, primitive
record carrier, and all required boundary/environment degrees:

```text
G_closure acts on
  H_source tensor H_record tensor H_boundary/environment.
```

It must generate source-conditioned distinguishable record states, stable
pointer sectors, persistence, and recoverability or redundancy. A
source-only mass block is not this complete generator and is not a record
instrument.

## Adopted physical identification

On the ordinary one-source charged branch, the local chiral-odd scalar
component of `G_closure` is supplied by the same boundary closure field that
participates in making the record durable. No independent source-mass field
or post-closure mass insertion exists.

Writing the two real closure components as `a_R` and `b_R`, the source-odd
part of the action is

```text
S_source,odd
  = -integral d^4x sqrt(-g) [
      a_R bar(psi) psi
      + b_R bar(psi) i gamma^5 psi
    ]

  = -integral d^4x sqrt(-g) [
      phi_R bar(psi_L) psi_R
      + phi_R^* bar(psi_R) psi_L
    ],

phi_R = a_R + i b_R.
```

The adopted identification is therefore

```text
boundary field in the durable-record closure generator
  =
boundary field multiplying the source's chiral-odd scalar/pseudoscalar
  bilinear.
```

It does not say that the source bilinear equals the complete closure map.

## Representation statement

For the declared branch:

```text
one vectorlike Dirac source;
q(psi_L) = q(psi_R) = +1;
no additional flavor or internal index;
local, algebraic, non-derivative source-odd component.
```

The proper-Lorentz scalar/pseudoscalar bilinear space has the two real basis
elements

```text
bar(psi) psi,
bar(psi) i gamma^5 psi.
```

Equivalently, its coefficient is one complex scalar `phi_R`. This is a
classification of the declared local bilinear class. It is not a proof that
the complete closure generator is unique.

Hermiticity requires the conjugate coefficients in the two chiral
orientations. CPT does not fix the phase. Unless later parity/CP dynamics
selects `b_R=0`, the full measure, regulator, topology, and boundary analysis
must retain the pseudoscalar component and its possible axial Jacobian.

## What the identification fixes

If a complete parameter-free closure action later derives a constant stable
background

```text
(a_R,b_R) = (a_*,b_*),
```

then the free quadratic source kernel has invariant mass parameter

```text
m_free^2 c^4 = a_*^2 + b_*^2.
```

This is a free mass-shell statement. The physical interacting pole and
residue still require the complete 1PI two-point function.

## Durability discipline

The source-odd bilinear does not by itself write a record. Durability must be
proved from the complete `G_closure` by deriving:

```text
the recorded source observable;
source-conditioned distinguishable record states;
stable pointer sectors;
a persistence interval;
recoverability or redundancy;
causal and gauge-compatible cell stitching.
```

Accordingly, version 002 does not revive either rejected shortcut:

```text
sqrt(a_R^2+b_R^2) = pi hbar/tau_R    not derived;
controlled-X generator = G_closure   not derived.
```

## No-retuning and failure rule

After this file is sealed:

```text
no independent source mass term may be added;
no separate record-gap parameter may be introduced;
no measured particle mass may fix a_R or b_R;
no alpha value may select a closure saddle;
no binary/CNOT shortcut may substitute for complete closure dynamics;
no parity/CP choice may be introduced after inspecting the response.
```

If the completed closure action generates no durable record, no isolated
stable background modulo declared symmetries, more than one inequivalent
unselected closure magnitude, or no acceptable interacting source pole, this
branch fails. Multiple stable pointer sectors required to encode alternatives
are not a failure when the complete dynamics relates them by its declared
record symmetry.

## Status

```text
source_record_odd_component_identity_adopted = true
principle_derived_from_older_record_theorems = false
source_block_identified_with_complete_closure_map = false
complete_source_record_generator_derived = false
scalar_pseudoscalar_bilinear_class_declared = true
closure_background_derived = false
record_generated_free_mass_parameter_derived = false
interacting_source_pole_and_residue_derived = false
durable_record_dynamics_derived = false
finite_response_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
