# STAGE 8 — THE PHASE-FREE CHANNEL OF E_int, RESUMMED EXACTLY: THE TOTAL DEGREE-0 INTRABAND CONTENT OF E IS -1_ball

## BLIND ANALYST — CODENAME R2-RESUM — [SEALED]

Date: 2026-08-13
Role: BLIND analyst (R2-RESUM). ONE exact resummation attempted and reported
honestly: the all-orders phase-free channel of E_int in the sealed collapse
E = U(0) - P = E_prof + E_int, and with it the exact resummed degree-0
intraband P-block content of E — deciding between the order-by-order
cancellation pattern (toward -1_ball-shape), the eq-resummed profile
((g-1)1_ball-shape), and any third form. All four tasked outcomes equal
citizens. Both prior parties' exact displays are inherited; both their
conclusions were refuted/withdrawn and are NOT inherited.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — operator/Clifford/symbol algebra,
exact ordered-integral and stencil-trig arithmetic, exact series with dominated
convergence; the one script is exact symbolic CAS identity verification (sympy
1.14.0, fresh venv `r2venv`), reproduced in full in §7; NOTHING numeric
evaluated, every constant symbolic. No physical quantity computed, bounded
numerically, or evaluated. No scale, imported GR, or faithfulness authority.
The sealed sharp M(t) NOT softened (E1 :465-470 respected as written; the
jump discontinuities of the switched coupling are retained EXACTLY and handled
by Riemann-Lebesgue/BV arguments that need no smoothing). The equal-time route
NOT resurrected (C6 respected as FALSE; the eq-object appears below only as
the sealed C-L1 display's own bookkeeping intermediate — and its exact status
is identified: it is the omega = 0 slice of the exact amplitude, not a
certification). No register/tracker/plan/road/ledger/lens file read. Output
name probed before write: ABSENT. No git action.

---

## 0. VERDICT IN ONE LINE

**NET = ESCAPE-SHAPE. The phase-free channel of E_int resums in closed form:
per lambda it is (1 - cos(lambda Theta(x)))·1_ball ⊗ I, weight-summed exactly
-g(x)·1_ball ⊗ I with g = cos^2(8 pi |x|^4). Hence the TOTAL degree-0
intraband P-block content of E = E_prof + E_int(phase-free) is
(g-1)·1_ball - g·1_ball = -1_ball ⊗ I EXACTLY: the order-by-order
cancellation pattern SURVIVES the full sum; the eq-resummed (g-1)-profile does
NOT — it is completed to -1_ball by the resummed phase-free channel. The
unitarity self-test PASSES: the resummed P + E has degree-0 intraband content
1 - 1 = 0, and the direct computation from the sealed one-cell propagator
(per-lambda adiabatic decoupling, lambda-uniform, pinned by the exact
unitarity |A|^2 + |B|^2 = 1 of the two-level reduction; then the sealed
two-moment kill m_0 = 0) gives U(0)'s degree-0 intraband symbol = 0 — both
ways agree. In the summed assembly the survivor
-i a_Delta k_hat_x b~(x,k_hat) C(k) loses its degree-0 coefficient — the
endpoint weights U(0) and the F-factor weight-sum both evaporate at degree 0:
SURVIVOR_FATE = CANCELS at exactly the level of record at which it was
exhibited. THE SUMMED-FIXED HORN REVIVES AT THIS LEVEL — revived, not
completed: the degree-(<= -1) remainder's carrier-uniformity (the sealed r2
joint multi-scale norm) is now the deciding object in the POSITIVE direction,
and debts 2 (F'-14) and 3 (summed-S2') are carried regardless.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session; all match sidecars and tasked
prefixes. None unverifiable.

```text
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md                                MATCH (tasked 3b9730e0)
b4a55baa5cb46de474be6e03c7fbdea728118c38ebc24529c34ae4b77e184bad  STAGE8_CL_ERROR_CHECK_V001.md                                        MATCH (tasked b4a55baa)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md                          MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md MATCH (PA; tasked 789338ad)
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                                       MATCH (tasked 6e81ae92)
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                                         MATCH (tasked 2699af25)
18af3dee8e3e5b17fe624d1b0547f4713359473a373df1562ff584647244ccf1  STAGE8_TWO_TIME_REANALYSIS_V001.md                                   MATCH (tasked 18af3dee)
6a2aa0fd18b9a0ea4f370dfbea278563ba02552e64b48ac0a7d91fbd7f90bc9e  STAGE8_TWO_TIME_CHECK_V001.md                                        MATCH (tasked 6a2aa0fd)
```

Sealed content read at path this session: E1 :339-352 (C4: weights
(1/2,-1/4,-1/4) on lambda = (0,+sqrt2,-sqrt2); moments m_0 = m_1 = 0,
m_{2j} = -2^{j-1}; the saturation display sum_l w_l u_l = P), :353-364 (C6 +
volume-diagonal locus), :465-470 (D6' sharp M(t) = Q 1_{|x|<=r(t)} Q,
r(t) = min(t,1-t), v(t) = tau_R 32 r(t)^3, int v = tau_R = pi/sqrt2, frozen),
:702-708 (S2b), :1106-1126 (B-L2/B-L2* typing), :1131-1136 (C-L1),
:1137-1144 (C-L2, quadratic-form demand); PA :75-128 (A1/A2: h_0, S_n =
-i slash(n) gamma^5, M(t), v(t), b_D with open-diamond slices |x| < r(t),
J = -(Q b_D Q) ⊗ alpha_x), :367-384 (Strang); TWO-TIME :278-291 (massless
on-shell marginality: intraband |omega| <= |q| bounded; interband
|k'|+|k| -> infinity); BL2STAR attempt §4.4-§6 (the survivor display
X = a_+ C U(0)^dag F C + a_- C F~^dag U(0) C; sigma_intra(X) =
-i a_Delta k_hat_x b~_P C(k); the saturation-clause fences F1-F3) + check §3.3
(the +(theta_1-theta_0)^2/2 probe, the r2 naming); CL-ERROR §2 (the exact
two-piece resolution E = E_prof + E_int; subsidiaries s1, s2 — both SURVIVING
per the check), §7 (J1-J12); CL-ERROR-CHECK §3 (the phase-free channel, K1-K6,
the exact n = 2 ledger E_int^(2)|deg-0 = -Theta^2/2 ⊗ I), §8 (NET_VERDICT =
REFUTED; UNDECIDABLE-AT(the resummation of the phase-free channel / r2)).

INHERITED OF RECORD (exact displays only, conclusions of both parties not
inherited): (i) E = E_prof + E_int exactly, E_prof = (g-1)1_ball ⊗ I,
g = cos^2(8 pi |x|^4), Theta(x) = tau_R(1-16|x|^4) (s1, SURVIVES);
(ii) the exact D-kernel (e^{i(s'|k'|-s|k|)t}-1) chi_hat_{r(t)}(q)
P_{s'}(k')P_{-s}(k) S (s2, SURVIVES); (iii) the check's phase-free channel
with its counter-battery K1-K6 and the exact n = 2 instance; (iv) the
fixed-order-vs-eq-resummed tension, open BOTH ways as of the check.

---

## 2. STEP 1 — THE ALL-ORDERS PHASE-FREE CHANNEL, SET UP EXACTLY

### 2.1 Frame (sealed)

U(0) = sum_l w_l u_lambda(0); u_lambda(0) = e^{-i h_0} Omega_lambda;
Omega_lambda = T exp(-i lambda int_0^1 v(t) R(t) dt);
R(t) = e^{i h_0 t} N(t) e^{-i h_0 t}, N(t) = M(t) ⊗ S. The free factor
e^{-i h_0} is a band-diagonal modulus-1 unitary (sea-band symbol e^{+i|k|}),
common to all lambda: carried, content-neutral (the check's o4 note honored).

### 2.2 The degree-0 intraband reduction (the level of record)

At bounded transfer q and |k| -> infinity — the volume-diagonal regime where
the survivor lives and every lineage verdict is read — the exact band algebra
gives (all CAS-verified, §7 V3):

```text
(b1) spatial multiplication operators (M(t), 1_ball, b_D) are band-diagonal
     at degree 0: their interband elements carry the exact coincidence factor
     P_s(k')P_{-s}(k) = -(s/2) P_s(k')(alpha.(k_hat-k_hat')) — one power
     (V3e = J5), degree <= -1;
(b2) S is PURELY interband with FULL O(1) amplitude: P_s S P_s = 0 and
     S P_s = P_{-s} S (V3b = J2/J10);
(b3) the two-hop composite is FULL, no loss: P_- S P_+ S P_- = P_- (V3c;
     K2 form P_-(u) S e^{-iHs} S P_-(u) = e^{-i w s} P_-(u) re-verified, V3d);
(b4) free evolution is band-diagonal with phases e^{-+ i sigma |k| s}.
```

Consequently the degree-0 intraband P-block dynamics of u_lambda(0) reduces
EXACTLY to a two-level system per symbol point (x, k_hat, omega = |k|): band
label sigma in {-,+} with energies -+omega, driven interband by the
spinor-trivial coupling

```text
    i d psi/dt = [ omega sigma_3 + lambda v~(t,x) sigma_1 ] psi ,
    v~(t,x) = v(t) 1_{|x| <= r(t)} = v(t) 1_{t in [|x|, 1-|x|]}  (|x| < 1/2),
    v~ = 0 identically for |x| >= 1/2.
```

(The even-S chains that produce intraband content are spinor-trivial by
S^2 = I; odd-S chains are interband. The 2-dim per-band degeneracy rides
along trivially in this channel.) The dropped strata — every term with at
least one (b1) coincidence factor — are degree <= -1 PER ORDER at bounded
transfer; their SUM is the sealed r2-type joint object: CARRIED as the
degree-(<= -1) remainder, not used, not decided (§5, §6). This is the same
grade at which the survivor itself is exhibited.

The sharp M(t) is retained exactly: for fixed x the coupling v~(t,x) switches
ON at t = |x| and OFF at t = 1-|x| with JUMPS of height v(|x|) =
tau_R 32|x|^3 (nonzero for x != 0) and has the v-corner at t = 1/2. Nothing
below smooths, mollifies, or needs to.

### 2.3 The exact Dyson/excursion form of the sea-sea amplitude

Interaction picture w.r.t. omega sigma_3: the sea-sea (intraband P-block)
amplitude of Omega_lambda at symbol point (x, omega) is the exact series

```text
    A_lambda(x, omega) = sum_{n even >= 0} A^(n),
    A^(n) = (-i lambda)^n int_{1 > t_1 > ... > t_n > 0} prod_j v~(t_j)
            · exp( -2 i omega SUM_{j=1}^{n/2} (t_{2j-1} - t_{2j}) ) dt ,
```

because each insertion flips the band and each PLUS-BAND EXCURSION
[t_{2j}, t_{2j-1}] costs the relative phase e^{-2 i omega (duration)}
(energy gap 2 omega; the check's n = 2 phase -2i omega(t-u) is the first
instance). Odd n vanishes for the sea-sea element. Uniform domination, exact:

```text
    |A^(n)(omega)| <= (|lambda| Theta(x))^n / n!   for ALL omega
```

(modulus-1 phases; ordered-simplex identity int_{simplex} prod v~ =
(int v~)^n / n! = Theta(x)^n / n!, CAS-verified for the generic cubic
amplitude class covering the sealed v, §7 V5c; Theta(x) = tau_R(1-16|x|^4)
re-verified, §7 V2a). The series is dominated by e^{|lambda| Theta},
uniformly in omega. Every statement below about limits of the series is
licensed by THIS domination (dominated convergence over n) — no estimate of
any physical quantity, an exact analysis step.

### 2.4 The phase-free channel, defined exactly (the D-kernel's "-1" part)

Split each excursion factor exactly as the sealed D-kernel splits —
(e^{i omega t} - 1) = oscillatory + ("-1"):

```text
    e^{-2 i omega s_j} = 1 + ( e^{-2 i omega s_j} - 1 ),   s_j = t_{2j-1} - t_{2j},
```

and expand the product over the m = n/2 excursions by subsets T of excursions
taking the (e - 1) factor:

```text
    A^(n) = SUM_{T subset of [m]} A^(n)_T ,
    A^(n)_{T = empty} = (-i lambda)^n Theta(x)^n / n!    — THE PHASE-FREE CHANNEL:
        every adjacent S-insertion pair composes with NO free phase between
        them (S·S = I, spinor-trivial, full O(1) intraband, no coincidence
        factor, no time variable to integrate by parts).
```

THE COMPOSITION POINT, stated exactly: the phase-free channel's n-fold
composition of sharp localizers is the exact x-space product
prod_j 1_{|x| <= r(t_j)} = 1_{|x| <= min_j r(t_j)} — indicators commute and
multiply. NO momentum-space factor-wise transfer bound is invoked anywhere in
this channel; chi_hat's L^2-not-L^1 failure (the r2 wall) NEVER ENGAGES here.
This is the exact reason the phase-free channel is resummable in closed form
while the oscillatory channels' joint norm remains the sealed r2 residue.

Channel identification with the sealed two-piece resolution:

```text
(c1) The T = empty channel of Omega_lambda IS the eq-object's stratum:
     sum over even n of (-i lambda Theta)^n/n! = cos(lambda Theta) =
     the sea-sea element of Omega_lambda^{eq} = e^{-i lambda Theta S} 1_ball
     (+ off-ball identity). E_prof's strata ARE the phase-free channels.
(c2) E_int = Omega_lambda - Omega_lambda^{eq} (per lambda, weight-summed
     later) = all T != empty channels. Its PHASE-FREE (omega-independent)
     content at order n is the "-1"-part selection:
         SUM_{T != empty} (-1)^{|T|} · (-i lambda)^n Theta^n/n!
         = [ (1-1)^{n/2} - 1 ] · (-i lambda)^n Theta^n/n!
         = - (-i lambda Theta)^n / n!            (n >= 2 even; §7 V5b)
     — EXACTLY MINUS E_prof's order-n stratum, at every order. The check's
     n = 2 instance is the first case: per-lambda +lambda^2 Theta^2/2,
     weighted by m_2 = -1 to -Theta^2/2 ⊗ I (§7 V6a — matches the sealed
     check value exactly).
(c3) The same "-1" from the D-insertion bookkeeping: expanding Omega_lambda
     over patterns of N's and D's (R = N + D) and taking each D's "-1" part:
     SUM_{j=1}^{n} C(n,j)(-1)^j = (1-1)^n - 1 = -1 for n >= 1 — the two
     bookkeepings agree (§7 V5b).
(c4) Interband/odd-n phase-free content: proportional to sin(lambda Theta)
     channels — weight-killed exactly (sum_l w_l sin(l th) = 0, §7 V1c).
     The one-D slice's weight sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th)
     is NON-killing (§7 V1d = the check's K4) — the channel is alive per
     insertion, exactly as the check exhibited; it is the DERIVATIVE slice of
     (c2)'s closed form, and (c2) is its all-orders completion.
```

The generating identity of the whole result is the binomial kill
(1-1)^m = 0: at every order n >= 2 the phase-free channel of E_int is exactly
minus the phase-free channel of the eq-object.

---

## 3. STEP 2 — THE RESUMMATION, IN CLOSED FORM

All sums below are absolutely convergent (entire series in Theta), termwise
exact; weights and moments CAS-verified (§7 V1).

```text
PER LAMBDA (sea-sea, on the ball; off-ball everything is 0 exactly since
v~ = 0 there at degree 0):

  pf( Omega_lambda )  = sum_{n even} (-i lambda Theta)^n/n! = cos(lambda Theta(x))
                                                              (§7 V5a)
  pf( E_int,lambda )  = sum_{n >= 2 even} [ -(-i lambda Theta)^n/n! ]
                      = 1 - cos(lambda Theta(x))               (closed form)

WEIGHT-SUMMED (w = (1/2,-1/4,-1/4) on lambda = (0,+sqrt2,-sqrt2)):

  sum_l w_l cos(lambda Theta) = c(Theta) = (1-cos(sqrt2 Theta))/2 = g(x)
                              = cos^2(8 pi |x|^4)              (§7 V1b, V2b)
  RESUMMED PHASE-FREE CHANNEL OF E_int:
      sum_l w_l [1 - cos(lambda Theta)] = m_0 - g = -g(x)
      =>  pf(E_int) = -cos^2(8 pi |x|^4) · 1_ball ⊗ I  EXACTLY.

TOTAL DEGREE-0 INTRABAND P-BLOCK CONTENT OF E:

      E_prof + pf(E_int) = (g-1)·1_ball ⊗ I + (-g)·1_ball ⊗ I
                         = -1_ball ⊗ I        EXACTLY            (§7 V6d)

and hence of the corrected collapse:

      [P + E]_deg-0,intra = 1_ball + (g-1)·1_ball - g·1_ball = 0  (§7 V6c),

with E total = E_prof + E_int(phase-free) + REMAINDER, the remainder being
the oscillatory/coincidence strata: degree <= -1 per order at bounded
transfer, CARRIED (its resummed control = the sealed r2 joint multi-scale
norm; §5, §6).
```

THE VERDICT BETWEEN THE TWO INHERITED SHAPES: the order-by-order cancellation
pattern SURVIVES the full sum — the resummed phase-free channel is exactly
omega-independent term by term, the binomial kill is exact per order, and the
sum is dominated: there is NO resummation-reversal escape in this channel.
The C4 reversal lesson (per-stratum coincidence/transfer gains with growing
constants defeated by the sum) has no purchase here: the phase-free channel
contains NO coincidence factors and NO transfer compositions to reverse — its
compositions are exact x-space products (§2.4). The eq-resummed
(g-1)1_ball-shape does NOT survive as the content of E: it is the omega = 0
slice (§4), and at degree 0 it is completed to -1_ball by pf(E_int).

Fence compliance with the sealed saturation clause (BL2STAR §6): this
determination is NOT obtained by per-stratum coincidence orders (the F2-
forbidden route) — it is obtained by exact channel resummation with dominated
convergence; per-stratum coincidence bookkeeping is used ONLY to confine the
carried remainder, the lineage's own licensed use. The record dressing is
treated non-perturbatively (F3 honored): the two-level amplitude is the full
resummed in-cell object.

---

## 4. THE UNITARITY SELF-TEST — COMPUTED BOTH WAYS

The sealed one-cell propagator u_lambda(0) is UNITARY, exactly, per lambda.
Its band-diagonal part is therefore NOT free: with B_lambda the interband
(sea->plus) amplitude, |A_lambda|^2 + |B_lambda|^2 = 1 EXACTLY — the
intraband modulus is pinned to 1 up to interband leakage.

### WAY 1 (the resummation, §3): [P + E]'s degree-0 intraband content
= 1 + (g-1) + (-g) = 0. So the resummed collapse says: U(0) has NO degree-0
intraband P-block content.

### WAY 2 (direct, from the sealed one-cell propagator):

```text
(d1) TERMWISE LIMIT: for each n >= 2, A^(n)(omega) -> 0 as omega -> infinity.
     Exact: Fubini onto the total plus-excursion duration sigma =
     SUM s_j > 0 a.e.; the pushforward of prod v~ · (simplex Lebesgue) is a
     finite absolutely continuous measure; Riemann-Lebesgue. (The sealed
     sharp M(t) is retained: RL needs only integrability — the jumps at
     t = |x|, 1-|x| are harmless. The n = 2 closed form is the check's K3.)
(d2) DOMINATION: |A^(n)(omega)| <= (|lambda| Theta)^n/n! uniformly in omega
     (§2.3): dominated convergence over n licenses the exchange:
         lim_{omega -> infinity} A_lambda(x, omega) = A^(0) = 1 ,
     LAMBDA-UNIFORMLY (the bound uses only |lambda| <= sqrt2, Theta <= tau_R).
     Adiabatic decoupling, obtained here as an exact dominated limit — no
     adiabatic theorem imported.
(d3) UNITARITY PIN: |A_lambda|^2 = 1 - |B_lambda|^2 exactly; the limit in
     (d2) forces B_lambda -> 0 — the interband leakage dies and the intraband
     modulus returns to 1: the demanded unitary budget is EXACTLY restored in
     the limit, with the lambda-dependence of A_lambda vanishing.
(d4) THE WEIGHT SUM: sum_l w_l A_lambda(x, omega) -> m_0 · 1 = 0
     (m_0 = 0, §7 V1a/V8c). With the carried free phase e^{+i omega}
     (modulus 1): U(0)'s degree-0 intraband symbol = 0.
```

WAY 1 = WAY 2 = 0. **UNITARITY_SELFTEST = PASS.**

### The closed-form slab exhibit (the interpolation, exact)

On any interval where the coupling is constant (mu), the two-level evolution
is EXACT (§7 V4a, verified as the unique ODE solution with H_2^2 = Om^2 I):

```text
  e^{-i(omega sigma_3 + mu sigma_1)T} = cos(Om T) I - i sin(Om T)
                                        (omega sigma_3 + mu sigma_1)/Om ,
  Om = sqrt(omega^2 + mu^2);   sea-sea:  A = cos(Om T) + i omega sin(Om T)/Om;
  UNITARITY: |A|^2 + |B|^2 = 1 exactly (§7 V4b);
  EXACT identities: A - e^{i Om T} = -i sin(Om T) mu^2/(Om(Om + omega)) (V4c),
                    Om - omega = mu^2/(Om + omega) -> 0 (V4d);
  =>  A_int := e^{-i omega T} A -> 1 as omega -> infinity, both magnitude
      factors vanishing exactly (V4f/V4g).

  THE WEIGHTED SLAB  Phi(omega) = sum_l w_l A(lambda mu):
      omega = 0   :  Phi = c(mu T)      — THE EQ / g-SHAPE END (§7 V4e);
      omega -> oo :  e^{-i omega T} Phi -> m_0 = 0 — THE ESCAPE END (V4g/V4h).
```

ONE exact function carries BOTH prior parties' shapes as its two ends: the
eq-display (the certification's (g-1)-profile) is exactly the omega = 0 slice
— which is why it is a lawful sealed bookkeeping intermediate and NOT the
degree-0 content — and the degree-0 content is read at the omega -> infinity
end, where the content of E is -1_ball. The tasked "third form" question is
answered: the full resummed intraband symbol is this omega-interpolating
family; its DEGREE-0 content is exactly the -1_ball shape (option (b)), with
the (g-1) shape recovered only as the omega = 0 slice.

SUBSIDIARY (exact, grade: leading term of the second-order averaged system,
identified — not verdict-bearing): the leading degree-(-1) intraband
coefficient of U(0). The level-repulsion phase per lambda is
e^{i lambda^2 y}, y = V_2(x)/(2 omega), and the stencil gives EXACTLY

```text
  sum_l w_l e^{i lambda^2 y} = -i e^{i y} sin(y)          (§7 V1e)
  V_2(x) = int v~(t,x)^2 dt = (8 pi^2/7)(1 - 128 |x|^7)   (§7 V7a)
```

— so U(0)'s intraband symbol falls off as -i V_2(x)/(2|k|) at leading order
(times the free phase), nonzero on the open ball: E's remainder is genuinely
degree -1, not smaller; the escape is by ONE power, exactly as the massless
marginality (TWO-TIME :278-291) permits and no faster.

---

## 5. STEP 3 — THE VERDICT AGAINST THE TASKED SHAPES, AND THE SURVIVOR

```text
(a) ZERO: E's degree-0 intraband content is NOT zero — it is -1_ball ⊗ I.
    (The TOTAL P + E content IS zero — that is unitarity's demand, restored.)
(b) -1_ball SHAPES: EXACT MATCH, coefficient 1: E_deg-0 = -1_ball ⊗ I.
    ESCAPE-SHAPE. The horn's named escape is REALIZED, not merely possible.
(c) THE SURVIVOR -i a_Delta k_hat_x b~(x,k_hat) C(k): CANCELS at degree 0
    in the summed assembly. The sealed display X = a_+ C U(0)^dag F C
    + a_- C F~^dag U(0) C (BL2STAR §4.4):
    - the U(0) endpoints: degree-0 intraband content 0 (§3/§4) — the
      endpoint weight the survivor's chain C P [...] P C relied on
      evaporates;
    - the F-factor: F = -i int sum_l w_l W_lambda(1,t') J(t') W_lambda(t',0);
      at degree 0 the W's are the same two-level amplitudes on [t',1] and
      [0,t']: per lambda each -> free phase, LAMBDA-INDEPENDENTLY (d2 on the
      sub-intervals), so the weight sum -> m_0 · (free) J (free) = 0; the
      sin·sin channel (the S-dressed route that built c's cross term at
      omega = 0, §7 V8a/V8b) requires TWO interband hops, each vanishing in
      the limit: F's degree-0 intraband content = 0.
    - the eq c-weight c(theta_1 - theta_0) — strictly positive, the source
      of b~_P > 0 and of the certification's b~_g — is the omega = 0 slice
      of this weight, not its degree-0 content.
    IDENTITY OF THE CANCELLATION: the sealed two-moment kill m_0 = 0 applied
    to the lambda-uniform dominated limit A_lambda -> 1 (equivalently: the
    resummed phase-free channel of E_int cancels E_prof AND P's intraband
    mass identically). The survivor's degree-0 coefficient dies
    ERROR-INCLUDED; what remains of the assembled response is degree <= -1
    at bounded transfer, CARRIED to the r2-type completion. The
    certification's s3 (the b~_g "error-included survivor") is negated at
    degree 0, exactly as the check's conditional flag anticipated.
    J12 STANDS: no anti-Hermitian k_hat_x-odd opposite component exists or
    was needed — the escape runs entirely through the spinor-trivial
    multiplicative endpoint-weight channel, the check's direction nuance,
    now decided.
(d) FAILS-AT / UNDECIDABLE-AT: does not obtain — the tasked resummation
    closed. The check's UNDECIDABLE-AT(r2) for THIS question is superseded:
    the phase-free channel's resummation did NOT need the r2 norm (exact
    x-space compositions + dominated convergence, §2.4/§3); r2 remains as
    the control of the degree-(<= -1) REMAINDER only — relocated, not
    discharged.
```

---

## 6. STEP 4 — CONSEQUENCE FOR THE SUMMED-FIXED HORN, AT LICENSED STRENGTH

```text
THE HORN REVIVES AT THIS LEVEL OF RECORD. The summed-FIXED horn died (BL2STAR,
check-CONFIRMED) at the survivor: degree-0 intraband at the volume diagonal,
HS^2 ~ carrier momentum volume. That term's degree-0 coefficient CANCELS
error-included (§5(c)) at exactly the level — degree-0 symbol content at
bounded transfer — at which it was exhibited. The single named escape of the
horn (the exact-cancellation property of the C-L1/C-L2 error) is REALIZED.

REVIVED, NOT COMPLETED — what is and is not delivered, exactly:
  DELIVERED: the exact resummed phase-free channel (closed form); the total
    degree-0 intraband content of E (-1_ball ⊗ I); the unitarity cross-check
    both ways; the survivor's degree-0 cancellation; the exact leading
    degree-(-1) coefficient V_2 (subsidiary).
  NOT DELIVERED (named, carried):
    (r-a) carrier-uniformity/HS-finiteness of the degree-(<= -1) remainder —
          the resummed oscillatory/coincidence strata: the sealed r2 joint
          multi-scale norm, NOW NEEDED IN THE POSITIVE DIRECTION to complete
          B-L2*. My degree-0 determination is graded like every lineage
          exhibition: per-order-confined remainder; a resummation-reversal
          INSIDE the coincidence strata is not excluded by this artifact and
          is exactly r2's remit. (It is excluded for the phase-free channel
          — §3 — which is where both prior parties' dispute lived.)
    (r-b) Q-resolved refinement effects (Q passed through at the sealed-
          display level, exactly as the sealed C4 display writes P sharp).
    (r-c) the free factor e^{-i h_0}: carried as a common modulus-1
          band-diagonal phase; content-neutral in every comparison above.
    (r-d) a successor spec act could lawfully re-form the object — a
          spec-author's act, not a property of E.
  DEBTS: debt 1 (B-L2*) moves from failing-conditionally-at-the-sealed-
  collapse to OPEN-AT-THE-REMAINDER (r2-type completion), its exhibited
  killer cancelled error-included; debts 2 (F'-14 log-of-sum passage) and 3
  (the unsealed summed-S2' re-quantification act) REMAIN REGARDLESS as
  sealed obligations of the architecture — the horn is not completable
  without them irrespective of this result. Per-pair nothing moves; RUNNING
  stays an unforced lean, no target created; M(t) stays sharp; equal-time
  stays FALSE by C6 (the eq-object is the omega = 0 slice — bookkeeping
  intermediate, never a certification, and its exact status is now
  identified rather than assumed).

RELATION TO THE PRIOR PARTIES, plainly: the certification's NO-OPPOSITE-
COMPONENT direction stays refuted (its own withdrawal stands) — but its J12
component analysis and its subsidiaries s1/s2 survive and are load-bearing
here. The check's escape-shape lean is CONFIRMED at degree 0 — but its
UNDECIDABLE-AT(r2) for the tasked question is superseded: the deciding
object was not r2; it was the exact x-space resummability of the phase-free
channel plus dominated convergence, neither of which was available to
fixed-order reasoning. Both prior displays are honored; both prior
conclusions were each half right, and the halves are now assigned.
```

---

## 7. THE SCRIPT (exact symbolic verification; reproduced in full) AND OUTPUT

Run with sympy 1.14.0 (fresh venv `r2venv`). Exact symbolic identity
verification only; nothing numeric evaluated; all constants symbolic or exact
rationals/surds.

```python
# R2-RESUM: exact symbolic verification of the load-bearing identities.
# EXACT SYMBOLIC ONLY: matrix/trig/integral/series identities over symbols;
# nothing numeric evaluated; all constants symbolic or exact rationals/surds.
import sympy as sp

I2, Z2 = sp.eye(2), sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])

# Dirac representation, exactly as the PA-pinned executor (dirac_data()):
g0 = sp.diag(1, 1, -1, -1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit()) for s in (sx, sy, sz)]
g5 = sp.I * g0 * gs[0] * gs[1] * gs[2]
S = -sp.I * g0 * g5                    # S = -i slash(n) gamma^5, slash(n) = gamma^0
al = [g0 * g for g in gs]              # alpha_j = gamma^0 gamma^j
I4 = sp.eye(4)

ux, uy, uz, vx, vy, vz = sp.symbols('ux uy uz vx vy vz', real=True)
w_, s_, th, th0, th1 = sp.symbols('w_ s_ theta theta0 theta1', real=True)
rho = sp.symbols('rho', nonnegative=True)
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [sp.Rational(1, 2), sp.Rational(-1, 4), sp.Rational(-1, 4)]

UGENS = [ux**2 + uy**2 + uz**2 - 1, vx**2 + vy**2 + vz**2 - 1]
def red(M):
    # entrywise polynomial reduction modulo <u.u-1, v.v-1> (EX domain carries
    # trig/phase coefficients through exactly)
    M = sp.expand(M)
    out = sp.zeros(*M.shape)
    for i in range(M.shape[0]):
        for j in range(M.shape[1]):
            e = sp.expand(M[i, j])
            r = sp.reduced(sp.Poly(e, ux, uy, uz, vx, vy, vz, domain='EX').as_expr(),
                           UGENS, ux, uy, uz, vx, vy, vz)[1]
            out[i, j] = sp.simplify(r)
    return out

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

Au = ux*al[0] + uy*al[1] + uz*al[2]    # alpha.u  (u = k_hat)
Av = vx*al[0] + vy*al[1] + vz*al[2]
Pp = (I4 + Au)/2                        # P_+(u)
Pm = (I4 - Au)/2                        # P_-(u) sea
Pp_v = (I4 + Av)/2
Pm_v = (I4 - Av)/2

# ---------- V1: weights, moments, the trig weight sums (sealed C4 stencil) ----------
mom = lambda n: sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v)))
c_th = (1 - sp.cos(sp.sqrt(2)*th))/2
ok("V1a m_0=0, m_1=0, m_2=-1, m_3=0, m_4=-2                        ",
   [mom(n) for n in range(5)] == [0, 0, -1, 0, -2])
ok("V1b sum_l w_l cos(l th) = c(th) = (1-cos(sqrt2 th))/2          ",
   sp.simplify(sum(w*sp.cos(l*th) for w, l in zip(w_v, lam_v)) - c_th) == 0)
ok("V1c sum_l w_l sin(l th) = 0  (odd channel dead)                ",
   sp.simplify(sum(w*sp.sin(l*th) for w, l in zip(w_v, lam_v))) == 0)
ok("V1d sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th) != 0 (K4) ",
   sp.simplify(sum(w*l*sp.sin(l*th) for w, l in zip(w_v, lam_v))
               + sp.sqrt(2)*sp.sin(sp.sqrt(2)*th)/2) == 0
   and sp.simplify(sum(w*l*sp.sin(l*th) for w, l in zip(w_v, lam_v)).subs(th, sp.pi/4)) != 0)
y_ = sp.symbols('y_', real=True)
ok("V1e sum_l w_l exp(i l^2 y) = -i e^{iy} sin(y)  (averaged form) ",
   sp.simplify(sp.expand(sp.expand_complex(
       sum(w*sp.exp(sp.I*l**2*y_) for w, l in zip(w_v, lam_v))
       - (-sp.I*sp.exp(sp.I*y_)*sp.sin(y_))))) == 0)

# ---------- V2: the sealed profile arithmetic ----------
tauR = sp.pi/sp.sqrt(2)
tt = sp.Symbol('tt', real=True)
prof = sp.integrate(32*tt**3, (tt, rho, sp.Rational(1, 2))) \
     + sp.integrate(32*(1-tt)**3, (tt, sp.Rational(1, 2), 1-rho))
ok("V2a Theta(rho)/tau_R = int_rho^{1-rho} 32 min(t,1-t)^3 dt = 1-16 rho^4",
   sp.simplify(prof - (1 - 16*rho**4)) == 0)
g = c_th.subs(th, tauR*(1 - 16*rho**4))
ok("V2b g := c(Theta) = cos^2(8 pi rho^4)  [via 1-g = sin^2]       ",
   sp.simplify(sp.expand_trig(1 - g - sp.sin(8*sp.pi*rho**4)**2)) == 0
   and sp.simplify(sp.cos(y_)**2 - (1 - sp.sin(y_)**2)) == 0)
ok("V2c int_0^1 v = tau_R (v = tau_R 32 r^3)                        ",
   sp.simplify(sp.integrate(tauR*32*sp.Min(tt, 1-tt)**3, (tt, 0, 1)) - tauR) == 0)

# ---------- V3: the band algebra behind the degree-0 two-level reduction ----------
ok("V3a S^2=I, S=S^dag, {S,alpha_j}=0                               ",
   S*S == I4 and sp.simplify(S.H - S) == sp.zeros(4, 4)
   and all(sp.expand(S*a + a*S) == sp.zeros(4, 4) for a in al))
ok("V3b S P_s(u) = P_{-s}(u) S ; P_s(u) S P_s(u) = 0 (S interband) ",
   sp.expand(S*Pp - Pm*S) == sp.zeros(4, 4)
   and sp.expand(S*Pm - Pp*S) == sp.zeros(4, 4)
   and red(Pm*S*Pm) == sp.zeros(4, 4) and red(Pp*S*Pp) == sp.zeros(4, 4))
ok("V3c P_-(u) S P_+(u) S P_-(u) = P_-(u)  (two-hop FULL, no loss)  ",
   red(Pm*S*Pp*S*Pm - Pm) == sp.zeros(4, 4))
Efree = sp.cos(w_*s_)*I4 - sp.I*sp.sin(w_*s_)*Au    # e^{-iHs}, H = w alpha.u
ok("V3d P_-(u) S e^{-iHs} S P_-(u) = e^{-iws} P_-(u)  (K2 form)     ",
   red(Pm*(S*Efree*S)*Pm - (sp.cos(w_*s_) - sp.I*sp.sin(w_*s_))*Pm) == sp.zeros(4, 4))
Duv = (ux-vx)*al[0] + (uy-vy)*al[1] + (uz-vz)*al[2]
ok("V3e P_s(v)P_{-s}(u) = -(s/2)P_s(v)(a.(u-v))  (J5: dropped strata cost)",
   red(sp.expand(Pp_v*Pm + sp.Rational(1, 2)*Pp_v*Duv)) == sp.zeros(4, 4)
   and red(sp.expand(Pm_v*Pp - sp.Rational(1, 2)*Pm_v*Duv)) == sp.zeros(4, 4))

# ---------- V4: the exact two-level slab (one interval, constant coupling) ----------
om, mu, T = sp.symbols('omega mu T', positive=True)
H2 = om*sz + mu*sx
Om_R = sp.sqrt(om**2 + mu**2)
U2_pred = sp.cos(Om_R*T)*I2 - sp.I*sp.sin(Om_R*T)*(H2)/Om_R
# verified as the unique solution of the ODE: H2^2 = Om^2 I, dU/dT = -i H2 U, U(0) = I
ok("V4a H2^2 = Om^2 I ; dU_pred/dT = -i H2 U_pred ; U_pred(0) = I   ",
   sp.simplify(H2*H2 - Om_R**2*I2) == sp.zeros(2, 2)
   and sp.simplify(sp.diff(U2_pred, T) + sp.I*H2*U2_pred) == sp.zeros(2, 2)
   and U2_pred.subs(T, 0) == I2)
A2 = U2_pred[1, 1]   # sea-sea (lower level, energy -om)
B2 = U2_pred[0, 1]   # sea->plus
ok("V4b unitarity: |A|^2 + |B|^2 = 1 exactly                        ",
   sp.simplify(sp.expand_trig(A2*sp.conjugate(A2) + B2*sp.conjugate(B2) - 1)) == 0)
ok("V4c A - e^{i Om T} = -i sin(Om T) mu^2/(Om(Om+om))  (exact)     ",
   sp.simplify(A2 - sp.exp(sp.I*Om_R*T) + sp.I*sp.sin(Om_R*T)*mu**2/(Om_R*(Om_R + om))) == 0)
ok("V4d Om - om = mu^2/(Om + om) (exact); -> 0 as om -> infinity    ",
   sp.simplify(Om_R - om - mu**2/(Om_R + om)) == 0
   and sp.limit(Om_R - om, om, sp.oo) == 0)
# the weighted slab: Phi(om) = sum_l w_l A(l mu); its two ends:
A_of = lambda m: (sp.cos(sp.sqrt(om**2 + m**2)*T)
                  + sp.I*om*sp.sin(sp.sqrt(om**2 + m**2)*T)/sp.sqrt(om**2 + m**2))
Phi = sum(w*A_of(l*mu) for w, l in zip(w_v, lam_v))
ok("V4e om = 0 slice: Phi(0) = c(mu T)  (the eq/g-shape end)        ",
   sp.simplify(Phi.subs(om, 0) - c_th.subs(th, mu*T)) == 0)
# V4f/V4g via the exact decomposition (V4c/V4d): with A_int := e^{-i om T} A,
#   A_int - 1 = (e^{i(Om-om)T} - 1) - i e^{-i om T} sin(Om T) mu^2/(Om(Om+om)),
# |e^{iz}-1| = 2|sin(z/2)|, |sin| <= 1, |e^{-i om T}| = 1: both magnitude factors
# vanish as om -> infinity (generic positive coupling m, hence lambda-uniform):
m_pos = sp.symbols('m_pos', positive=True)
Om_m = sp.sqrt(om**2 + m_pos**2)
decomp = (sp.exp(sp.I*(Om_m-om)*T) - 1) \
         - sp.I*sp.exp(-sp.I*om*T)*sp.sin(Om_m*T)*m_pos**2/(Om_m*(Om_m + om))
A_int_m = A_of(m_pos)*sp.exp(-sp.I*om*T)
ok("V4f exact: A_int - 1 = (e^{i(Om-om)T}-1) - i e^{-i om T} sin(Om T) m^2/(Om(Om+om))",
   sp.simplify(sp.expand(A_int_m - 1 - decomp)) == 0)
ok("V4g both magnitude factors -> 0: sin((Om-om)T/2) -> 0 and m^2/(Om(Om+om)) -> 0",
   sp.limit(sp.sin((Om_m - om)*T/2), om, sp.oo) == 0
   and sp.limit(m_pos**2/(Om_m*(Om_m + om)), om, sp.oo) == 0)
# hence per-lambda A_int(l mu) -> 1 (lambda-uniform), and the weighted slab
# Phi_int - m_0 = sum_l w_l (A_int(l mu) - 1) -> 0 with m_0 = 0 (V8c):
ok("V4h weighted slab: Phi_int = m_0 + sum_l w_l (A_int(l mu)-1) (algebraic)",
   sp.simplify(Phi*sp.exp(-sp.I*om*T) - (sp.nsimplify(sum(w_v))
       + sum(w*(A_of(l*mu)*sp.exp(-sp.I*om*T) - 1) for w, l in zip(w_v, lam_v)))) == 0)

# ---------- V5: the phase-free channel combinatorics (all orders, exact) ----------
m_, j_ = sp.symbols('m_ j_', integer=True, positive=True)
x_ = sp.symbols('x_', real=True)
ser = sp.summation((-1)**m_*(x_)**(2*m_)/sp.factorial(2*m_), (m_, 0, sp.oo))
ok("V5a sum_{n even} (-i x)^n/n! = cos(x)  (pf channel resums to cos)",
   sp.simplify(ser - sp.cos(x_)) == 0)
binsum = sp.summation(sp.binomial(m_, j_)*(-1)**j_, (j_, 0, m_))
ok("V5b sum_{j=0}^{m} C(m,j)(-1)^j = 0 for m >= 1 (binomial kill)   ",
   sp.simplify(binsum) == 0
   and all(sum(sp.binomial(n, j)*sp.Integer(-1)**j for j in range(1, n+1)) == -1
           for n in range(1, 9)))
# ordered-simplex identity for a generic cubic amplitude (covers the sealed v):
a_, b_ = sp.symbols('a_ b_', real=True)
c0, c1, c2, c3 = sp.symbols('c0 c1 c2 c3', real=True)
t1, t2, t3 = sp.symbols('t1 t2 t3', real=True)
f = lambda t: c0 + c1*t + c2*t**2 + c3*t**3
F1 = sp.integrate(f(t1), (t1, a_, b_))
D2 = sp.integrate(sp.integrate(f(t1)*f(t2), (t2, a_, t1)), (t1, a_, b_))
D3 = sp.integrate(sp.integrate(sp.integrate(
        f(t1)*f(t2)*f(t3), (t3, a_, t2)), (t2, a_, t1)), (t1, a_, b_))
ok("V5c ordered n-simplex = (int f)^n/n!  (n = 2, 3, generic cubic) ",
   sp.simplify(D2 - F1**2/2) == 0 and sp.simplify(D3 - F1**3/6) == 0)

# ---------- V6: the ledger — pf channel of E_int vs E_prof, and the total ----------
lam = sp.symbols('lambda_', real=True)
Th = sp.symbols('Theta', real=True)
Eint_pf = 1 - sp.cos(lam*Th)          # per-lambda pf channel of E_int (n >= 2)
ok("V6a n=2 jet of per-l pf(E_int) = +l^2 Th^2/2 ; weighted = -Th^2/2",
   sp.simplify(sp.series(Eint_pf, Th, 0, 4).removeO() - lam**2*Th**2/2) == 0
   and sp.simplify(sum(w*(l**2*Th**2/2) for w, l in zip(w_v, lam_v)) + Th**2/2) == 0)
Eint_pf_w = sp.simplify(sum(w*(1 - sp.cos(l*Th)) for w, l in zip(w_v, lam_v)))
ok("V6b weighted pf(E_int) = -c(Theta)  (= -g on the ball)          ",
   sp.simplify(Eint_pf_w + c_th.subs(th, Th)) == 0)
ok("V6c TOTAL: P + E_prof + pf(E_int) = 1 + (g-1) + (-g) = 0        ",
   sp.simplify(1 + (g - 1) + (-g)) == 0)
ok("V6d E-total degree-0 content: (g-1) + (-g) = -1  (times 1_ball) ",
   sp.simplify((g - 1) + (-g) + 1) == 0)

# ---------- V7: subsidiary — the exact degree-(-1) coefficient integrand ----------
V2int = sp.integrate((tauR*32*tt**3)**2, (tt, rho, sp.Rational(1, 2))) \
      + sp.integrate((tauR*32*(1-tt)**3)**2, (tt, sp.Rational(1, 2), 1-rho))
ok("V7a V_2(rho) = int_rho^{1-rho} v^2 = (8 pi^2/7)(1 - 128 rho^7)  ",
   sp.simplify(V2int - sp.Rational(8, 7)*sp.pi**2*(1 - 128*rho**7)) == 0)

# ---------- V8: the survivor's endpoint weight at the two ends ----------
ok("V8a eq (om=0) slice: sum_l w_l cos(l(th1-th0)) = c(th1-th0)     ",
   sp.simplify(sum(w*sp.cos(l*(th1-th0)) for w, l in zip(w_v, lam_v))
               - c_th.subs(th, th1-th0)) == 0)
ok("V8b cos.cos + sin.sin channel = cos(l(th1-th0)) (the eq c-weight)",
   sp.simplify(sp.cos(lam*th1)*sp.cos(lam*th0) + sp.sin(lam*th1)*sp.sin(lam*th0)
               - sp.cos(lam*(th1-th0))) == 0)
ok("V8c large-om end: lambda-independent limits weight to m_0 = 0   ",
   sp.nsimplify(sum(w*1 for w in w_v)) == 0)
```

Output, verbatim:

```text
V1a m_0=0, m_1=0, m_2=-1, m_3=0, m_4=-2                        : PASS
V1b sum_l w_l cos(l th) = c(th) = (1-cos(sqrt2 th))/2          : PASS
V1c sum_l w_l sin(l th) = 0  (odd channel dead)                : PASS
V1d sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th) != 0 (K4) : PASS
V1e sum_l w_l exp(i l^2 y) = -i e^{iy} sin(y)  (averaged form) : PASS
V2a Theta(rho)/tau_R = int_rho^{1-rho} 32 min(t,1-t)^3 dt = 1-16 rho^4: PASS
V2b g := c(Theta) = cos^2(8 pi rho^4)  [via 1-g = sin^2]       : PASS
V2c int_0^1 v = tau_R (v = tau_R 32 r^3)                        : PASS
V3a S^2=I, S=S^dag, {S,alpha_j}=0                               : PASS
V3b S P_s(u) = P_{-s}(u) S ; P_s(u) S P_s(u) = 0 (S interband) : PASS
V3c P_-(u) S P_+(u) S P_-(u) = P_-(u)  (two-hop FULL, no loss)  : PASS
V3d P_-(u) S e^{-iHs} S P_-(u) = e^{-iws} P_-(u)  (K2 form)     : PASS
V3e P_s(v)P_{-s}(u) = -(s/2)P_s(v)(a.(u-v))  (J5: dropped strata cost): PASS
V4a H2^2 = Om^2 I ; dU_pred/dT = -i H2 U_pred ; U_pred(0) = I   : PASS
V4b unitarity: |A|^2 + |B|^2 = 1 exactly                        : PASS
V4c A - e^{i Om T} = -i sin(Om T) mu^2/(Om(Om+om))  (exact)     : PASS
V4d Om - om = mu^2/(Om + om) (exact); -> 0 as om -> infinity    : PASS
V4e om = 0 slice: Phi(0) = c(mu T)  (the eq/g-shape end)        : PASS
V4f exact: A_int - 1 = (e^{i(Om-om)T}-1) - i e^{-i om T} sin(Om T) m^2/(Om(Om+om)): PASS
V4g both magnitude factors -> 0: sin((Om-om)T/2) -> 0 and m^2/(Om(Om+om)) -> 0: PASS
V4h weighted slab: Phi_int = m_0 + sum_l w_l (A_int(l mu)-1) (algebraic): PASS
V5a sum_{n even} (-i x)^n/n! = cos(x)  (pf channel resums to cos): PASS
V5b sum_{j=0}^{m} C(m,j)(-1)^j = 0 for m >= 1 (binomial kill)   : PASS
V5c ordered n-simplex = (int f)^n/n!  (n = 2, 3, generic cubic) : PASS
V6a n=2 jet of per-l pf(E_int) = +l^2 Th^2/2 ; weighted = -Th^2/2: PASS
V6b weighted pf(E_int) = -c(Theta)  (= -g on the ball)          : PASS
V6c TOTAL: P + E_prof + pf(E_int) = 1 + (g-1) + (-g) = 0        : PASS
V6d E-total degree-0 content: (g-1) + (-g) = -1  (times 1_ball) : PASS
V7a V_2(rho) = int_rho^{1-rho} v^2 = (8 pi^2/7)(1 - 128 rho^7)  : PASS
V8a eq (om=0) slice: sum_l w_l cos(l(th1-th0)) = c(th1-th0)     : PASS
V8b cos.cos + sin.sin channel = cos(l(th1-th0)) (the eq c-weight): PASS
V8c large-om end: lambda-independent limits weight to m_0 = 0   : PASS
```

(32/32 PASS. The Riemann-Lebesgue step (d1) and the dominated-convergence
exchange (d2) are exact-analysis steps stated and justified in §4; the CAS
battery verifies every algebraic/trig/integral/series identity they rest on,
including the closed-form slab instance where the full limit is verified
end-to-end symbolically, V4a-V4h.)

---

## 8. FLAG BLOCK

```text
CHANNEL_SETUP = EXACT(
  Frame: U(0) = sum_l w_l e^{-i h_0} Omega_lambda at the sealed collapse
  (E1 C4 :339-352; C-L1 :1131-1136; C-L2 :1137-1144 + S2b :702-708; D6'
  :465-470; PA :75-128, :367-384), inherited two-piece resolution
  E = E_prof + E_int (CL-ERROR §2, s1/s2 SURVIVING per b4a55baa). Degree-0
  intraband reduction at bounded transfer (the level of record): spatial
  operators band-diagonal at degree 0 (J5 coincidence factor per interband
  element, V3e), S purely interband with FULL two-hop composite (V3b/V3c/
  V3d), free evolution band-diagonal phases: the intraband P-block dynamics
  is EXACTLY the two-level system i psi' = [omega sigma_3 + lambda v~(t,x)
  sigma_1] psi, v~ = v(t) 1_{t in [|x|,1-|x|]}, M(t) sharp, jumps retained.
  Sea-sea Dyson series in plus-excursion form: A^(n) = (-i lambda)^n
  int_simplex prod v~ · e^{-2 i omega SUM(t_{2j-1}-t_{2j})}, |A^(n)| <=
  (|lambda| Theta)^n/n! uniformly in omega (V5c). PHASE-FREE CHANNEL =
  the all-"1" selection of e^{-2i omega s} = 1 + (e^{-2i omega s} - 1)
  (the sealed D-kernel's "-1" split, s2): T = empty; its n-fold localizer
  composition is the EXACT x-space product prod 1_{|x|<=r(t_j)} =
  1_{|x|<=min r} — chi_hat's L^1-failure (r2) never engages. E_int's
  phase-free channel at order n = [(1-1)^{n/2} - 1]·(-i lambda Theta)^n/n!
  = -(-i lambda Theta)^n/n! (binomial kill, V5b; D-pattern bookkeeping
  agrees: sum_j C(n,j)(-1)^j = -1); n = 2 instance reproduces the check's
  -Theta^2/2 ⊗ I exactly (V6a). Interband/odd phase-free content
  weight-killed (V1c); the one-D slice's non-killing weight
  -(sqrt2/2) sin(sqrt2 th) (V1d = K4) is its derivative slice.)

RESUMMED_FORM = CLOSED(
  per lambda: pf(Omega_lambda)|intra = cos(lambda Theta(x)) — exactly the
  eq-object (V5a); pf(E_int,lambda)|intra = 1 - cos(lambda Theta(x)).
  Weight-summed: sum_l w_l cos(lambda Theta) = c(Theta) = g =
  cos^2(8 pi |x|^4) (V1b/V2b);
  RESUMMED pf(E_int) = -g(x)·1_ball ⊗ I = -cos^2(8 pi |x|^4)·1_ball ⊗ I.
  The resummation is termwise omega-independent, absolutely convergent,
  dominated by e^{sqrt2 tau_R}: NO resummation-reversal is available in
  this channel — the C4 reversal mechanism (growing transfer/coincidence
  constants) has no object here; F2/F3 of the sealed saturation clause
  honored (no per-stratum-coincidence certification of a resummed object;
  record dressing treated non-perturbatively).)

UNITARITY_SELFTEST = PASS(
  WAY 1 (resummed): [P + E]_deg-0,intra = 1 + (g-1) + (-g) = 0 (V6c).
  WAY 2 (direct from the sealed one-cell propagator): per-lambda termwise
  Riemann-Lebesgue (excursion-duration pushforward absolutely continuous;
  sharp M(t) fine — RL needs only L^1) + dominated convergence over n:
  A_lambda(x,omega) -> 1 lambda-uniformly; unitarity |A|^2 + |B|^2 = 1
  (V4b) pins the intraband modulus; weight sum -> m_0 = 0 (V1a/V8c): U(0)'s
  degree-0 intraband symbol = 0. AGREEMENT: 0 = 0. Closed-form slab exhibit
  verified end-to-end (V4a-V4h): ONE exact function Phi(omega) with
  Phi(0) = c(mu T) (the eq/g-shape end) and e^{-i omega T} Phi -> 0 (the
  escape end) — both prior parties' shapes are the two ends of one exact
  object; the eq display is exactly the omega = 0 slice (lawful bookkeeping
  intermediate, not degree-0 content). Subsidiary: leading degree-(-1)
  coefficient of U(0): -i V_2(x)/(2|k|)-form, V_2 = (8 pi^2/7)(1-128|x|^7)
  (V1e/V7a) — the escape is by exactly ONE power, as massless marginality
  permits (TWO-TIME :278-291), no faster.)

TOTAL_DEGREE0_CONTENT = EXACT(
  E = E_prof + E_int(phase-free) + remainder:
      degree-0 intraband P-block content of E = (g-1)·1_ball - g·1_ball
      = -1_ball ⊗ I EXACTLY (V6d); off-ball zero; free factor e^{-i h_0}
      carried (common modulus-1 band phase, content-neutral).
  Equivalently: U(0) = P + E has ZERO degree-0 intraband P-block content —
  E cancels the sharp projector's intraband mass identically at degree 0.
  The order-by-order cancellation pattern SURVIVES the full sum; the
  eq-resummed (g-1)1_ball-shape does NOT survive as content — it is the
  omega = 0 slice. Verdict letter: (b) ESCAPE-SHAPE, coefficient exactly 1.
  Remainder: oscillatory/coincidence strata, degree <= -1 per order,
  CARRIED (resummed control = sealed r2 joint multi-scale norm).)

SURVIVOR_FATE = CANCELS(
  the identity: the sealed two-moment kill m_0 = 0 applied to the
  lambda-uniform dominated limit A_lambda -> 1 — equivalently the resummed
  phase-free channel of E_int cancels E_prof AND P's intraband mass
  identically. In the sealed assembly X = a_+ C U(0)^dag F C + a_- C F~^dag
  U(0) C: the U(0) endpoints have zero degree-0 intraband content, and F's
  weight sum sum_l w_l W_lambda(1,t') J W_lambda(t',0) -> m_0 · (free) J
  (free) = 0 at degree 0 (the eq c-weight c(theta_1-theta_0), V8a/V8b, is
  the omega = 0 slice; the sin·sin channel needs two interband hops, both
  dying in the limit). The survivor -i a_Delta k_hat_x b~(x,k_hat) C(k)
  loses its degree-0 coefficient ERROR-INCLUDED, at exactly the level of
  record at which it was exhibited; the certification's s3 (b~_g
  error-included survivor) is negated at degree 0. J12 STANDS — no opposite
  component exists or was needed; the escape is the spinor-trivial
  multiplicative endpoint-weight channel, the check's direction nuance
  decided. The assembled response's remaining intraband content is degree
  <= -1, carried.)

NET = ESCAPE-SHAPE—HORN-REVIVES-AT-LEVEL—REVIVED-NOT-COMPLETED(
  the summed-FIXED horn's named killer cancels error-included at degree 0:
  the horn REVIVES at exactly the licensed strength — the degree-0
  symbol/bounded-transfer level of record, the survivor's own exhibition
  level. NOT delivered, named exactly: (r-a) carrier-uniformity of the
  degree-(<= -1) remainder — the sealed r2 joint multi-scale norm, NOW the
  deciding object in the POSITIVE direction for B-L2* (a reversal inside
  the coincidence strata is not excluded here and is r2's remit; it IS
  excluded for the phase-free channel, where the prior dispute lived);
  (r-b) Q-resolution (Q passed through as the sealed C4 display writes P
  sharp); (r-c) free-factor bookkeeping (carried, content-neutral); (r-d)
  successor-spec possibility (a spec-author's act). Debt 1 (B-L2*): from
  failing-conditionally-at-the-sealed-collapse to OPEN-AT-THE-REMAINDER,
  exhibited killer cancelled. Debts 2 (F'-14) and 3 (summed-S2') CARRIED
  REGARDLESS — the horn is not completable without them irrespective of
  this result. Per-pair nothing moves; RUNNING stays an unforced lean;
  M(t) sharp; equal-time stays FALSE by C6. Prior parties: certification's
  direction stays refuted (J12 + s1 + s2 survive and are load-bearing);
  check's escape lean CONFIRMED at degree 0 but its UNDECIDABLE-AT(r2) for
  the tasked question SUPERSEDED — the deciding object was the exact
  x-space resummability of the phase-free channel plus dominated
  convergence, not r2; r2 is relocated to the remainder, not discharged.)

SEALS_VERIFIED = 8/8(
  3b9730e0 CL-ERROR certification; b4a55baa CL-ERROR check; 46846730 E1;
  789338ad PA; 6e81ae92 BL2STAR attempt; 2699af25 BL2STAR check; 18af3dee
  TWO-TIME; 6a2aa0fd TT-CHECK — all recomputed from bytes at path this
  session, all matching sidecars and tasked prefixes; every cited line span
  read at path; none unverifiable.)

FORBIDDEN_IMPORTS = none(
  no scale, no imported GR, no faithfulness authority; no physical quantity
  computed, bounded numerically, or evaluated — exact identities, exact
  series with dominated convergence, symbolic limits only, all constants
  symbolic; M(t) not softened (jumps retained exactly; RL/BV arguments need
  no smoothing); equal-time not resurrected (the eq-object used only as the
  sealed bookkeeping intermediate it is, its exact status identified as the
  omega = 0 slice); no adiabatic theorem imported (the decoupling limit is
  derived as a dominated exact limit in-artifact); no
  register/tracker/plan/road/ledger/lens file read.)

MACHINERY_INVOKED = yes(CAS identity checks only — sympy 1.14.0, fresh venv
  r2venv, 32 exact identity groups V1-V8 over symbols, script and output
  reproduced verbatim in §7; shasum/file reads otherwise; NOTHING numeric
  evaluated.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
R2_RESUMMATION_RESULT = SEALED.
```
