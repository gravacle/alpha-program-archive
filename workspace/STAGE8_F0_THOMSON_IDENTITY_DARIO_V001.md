# STAGE 8 / [PLAN:F0-2] — THE THOMSON IDENTITY QUESTION: WHO HOLDS THE SOURCE QUESTION ON ALPHA'S PATH?
## DARIO LANE (Builder B) — V001

RELAY 827. Lane guard PASS (DARIO). Inbox `RELAY_PASTE_827_THOMSON_IDENTITY_DARIO_V001.md`
= `7124f370088dff6989c8cd5c106e0c81f1ef5c9e9bc0e9df5fc6d9f89be61346`, seal verified BEFORE reading.
State-brief pinning: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`, verified and read.

GATES DECLARED AND HELD: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. **Comparison by content, no identity assumed (S13's lesson).**
No smooth import; no EM identification; no member bound; no numeric evaluation of physical
quantities; no comparison to measured constants. PE-1..PE-11 pointer-only.
Builder-B independence held. No register, plan, tracker, git action.

**ALL HEADLINE ITEMS ARE CLAIMED.**

**WHAT THIS RELAY IS.** It settles an ambiguity I displayed and declined to resolve at 824, edge (c).
The question is not whether the source-inclusive objects exist — it is **whose problem they are**.

---

## 1. THE THOMSON CHAIN, AS THE RECORD DEFINES IT — CLAIMED

### 1.1 Its position: F0 link 4, one of four absent

Q-696 (my own 790, F0) reconstructs K_*'s defining chain as **16 links: 5 EXIST, 7 PLAN-DELIVERABLE,
4 ABSENT**. The fourth absence, verbatim:

> *"(4) the derived `K_*`/`kappa_Thomson` relation — the historical `kappa_record=kappa_Thomson`
> derive-or-fail clause and Q-34's re-posing to `K_* -> kappa_Thomson` are both located, and **NO
> sealed `K_* = kappa_Thomson` statement or proof exists**."*

And the plan's key typing: *"`kappa_record` is the record-side intensive Hessian; **ALPHA'S STIFFNESS
IS `K_*`**."* So the Thomson relation is **link 4 of the chain that defines alpha's stiffness** —
on the value path by the plan's own construction, and absent.

The identification is separately fenced: `STAGE8_INSIDE_OUTSIDE_CONSISTENCY_CONDITION_EINSTEIN_V001.md`
records that assuming `kappa_record = kappa_Thomson` **"TRIPS A SEALED FALSIFIER"**. So link 4 must be
*derived*; it may not be assumed.

### 1.2 Its needs, verbatim — and this is the decisive extraction

`STAGE8_TASK4A_STATIONARITY_REQUIREMENT_AND_LAST_WALL_SYNONYM_AUDIT_DETERMINATION_V001.md`
(`671a94a7f55a5649`, `.md.seal` OK), span `[9678,10168)` sha `3de949cc508dc65d`:

> *"| `kappa_Thomson` | **V011:1587-1655: complete `Q_spec`; `Z_Q[A]/Z_Q[0]`; branch at `A=0`;
> transverse coefficient and `q^2->0` limit** | **DECLARED REFERENCE** | No; the required complete
> charged amplitude and its checks are absent from C1 | `DECLARED_REFERENCE_REQUIRED=true` |"*

and its §2.4 `[13419,…)`:

> *"V011 defines the complete charged specification as a distinct sealed object `Q_spec` and then
> defines `Z_Q[A]/Z_Q[0]`, `Gamma_Q[A] = -i Log(Z_Q[A]/Z_Q[0])`, with the logarithm branch fixed
> continuously at `A=0`. The transverse quadratic response and its finite path-independent limit are
> then defined at V011:1621-1635. **Pass additionally requires the Ward identity, gauge-parameter
> independence, regulator independence, and threshold matching.**"*
>
> ```text
> KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false | TYPE-S | scope: V011's own definition
> KAPPA_THOMSON_REQUIRES_DECLARED_A0_REFERENCE = true
> ```

**The sealed requirement-list for `kappa_Thomson` is therefore, in full:** complete `Q_spec`;
`Z_Q[A]/Z_Q[0]` with the branch fixed at `A=0`; the transverse coefficient and its `q^2->0` limit;
Ward identity; gauge-parameter independence; regulator independence; threshold matching.

**"Source-inclusive continuum/projective completion" is not in it.**

*Custody note, applying my own 818 lesson.* This is a **reported citation** of `V011:1587-1655`, not
V011's bytes. I flagged exactly this pattern at 818 (A15) and I flag it here: the determination is
sealed and its extraction is what I rely on; the V011 line-range is second-hand. I do not upgrade it.

### 1.3 The other list — and what it actually is

The list that started this question is `STAGE8_TASK4A_RESPONSE_CLASS_FORCING_AND_TAIL_OUTPUT_INERTNESS_DETERMINATION_V001.md`
`[85,1065)` sha `c0e90ace7af7fcf6`:

> *"**The proof attempt fails at the physical source completion.** The sealed finite operations are
> norm/module preserving, but the physical chain then requires: source differentiation in
> `topology_src/Diff_src`; **source-inclusive continuum/projective completion**; convolution
> inversion on the completed quotient; **stationary 2PI-to-1PI reduction and Schur inversion**;
> contour/i-epsilon boundary values; retarded extraction on completed domains; and, downstream, a
> `q^2 -> 0` Thomson limit."*

**This is a failed attempt's post-mortem, and the sealed `kappa_Thomson` specification contradicts
part of it outright.** TASK4A's route lists *stationary 2PI-to-1PI reduction*; V011's own definition
sets `KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false`. So the two lists are not two
statements of one requirement set — **one is the specification of the object, the other is the
requirement set of a particular route to it, and the specification disowns part of that route.**

---

## 2. THE R-L2b / LINKED-CLUSTER CAMPAIGN, AS THE RECORD DEFINES IT — CLAIMED

**Position.** Plan V004, Phase E (*"CLOSE 7A"*), item E2, verbatim:

> *"E2 R-L2b discharge state — **UNKNOWN AND AMBIGUOUS OF RECORD**: 'SUPERSEDED / RE-POSED, NOT
> SOLVED' vs 'REFUTED AS WRITTEN' unreconciled; **four obligations** and an unattacked
> response-pullback half named. Relay 782 maps this now."*

**Content.** `STAGE8_RL2B_OVER_CHARTER_AND_BLIND_ROUTE_FINDING_V001.md` (`1497d017175c900f`, 15,986 B).
Its subject is the **record-side density and its universality over refinements**: whether
`kappa_record` is a property of the theory or of the fixture; the D3 quantifier over cellulations;
exact additivity covering the disconnected part with `connected_cross_cell_terms_derived = FALSE`;
the cluster expansion existing *"precisely to handle the CONNECTED corrections"*; and whether a
physical criterion picks the cell scale (*"ABSENT, AND THE CORPUS APPEARS TO DENY IT"*).

Its own statement of the route's limit:

> *"THE HONEST LIMIT OF THIS ROUTE … exact additivity covers the DISCONNECTED part.
> `connected_cross_cell_terms_derived = FALSE`. … this route does not bypass A-L0 arm 2 — **IT
> ISOLATES IT**."*

**Q-691** (my own 782) maps the adjacent ratio route and records the standing obligation:
*"`kappa_record = kappa_Thomson` must be DERIVED or the route fails"* — i.e. the register treats the
Thomson relation as an obligation *upon* the route, not as part of the R-L2b machinery.

---

## 3. THE COMPARISON, BY CONTENT — CLAIMED

The relay bars assuming identity. I tested it three ways, and all three separate.

**(a) Different plan phases.** R-L2b is **Phase E** (close 7A), item E2. K_*/Thomson is **Phase F**
(compute alpha), F0 link 4. The plan's off-path typing is attached to the Phase-E campaign.

**(b) Zero lexical overlap on every distinguishing term.** Counts over the R-L2b over-charter's
15,986 characters:

```text
thomson 0 | q^2 0 | retarded 0 | 1PI 0 | 2PI 0 | source-inclusive 0
linked-cluster 0 | linked cluster 0 | cumulant 0 | extensiv 3
```

The campaign the plan calls "R-L2b/linked-cluster" **never uses the word Thomson, never mentions the
retarded response, never mentions 1PI/2PI, and never mentions source-inclusive completion.**

**(c) The specifications point at different objects.** R-L2b's object is `kappa_record`'s density and
its refinement-universality — the **record-side intensive Hessian**, which the plan explicitly says
is *not* alpha's stiffness. `kappa_Thomson`'s object is the transverse quadratic response of the
complete charged specification at `q^2 -> 0` — a **response-side** object. And V011's spec
affirmatively sets `KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false`, separating it from the
stationary-background machinery.

**`COMPARISON = DISTINCT.`** Separating spans displayed above. No overlap survives content
comparison, and the one shared-looking term — "extensivity" — appears only in R-L2b and is absent
from the Thomson specification.

**Consequence, and it is the consequence that matters:** *the plan's typing "the R-L2b/linked-cluster
campaign is OFF alpha's value path" does **not** transfer to the Thomson chain.* My 824 was right to
refuse the transfer. The Thomson relation is on alpha's value path, as F0 link 4.

---

## 4. THE VERDICT — CLAIMED

**The source question does NOT sit on alpha's value path on the sealed stock — but not for the
reason 824 entertained.**

The reasoning, in order:

1. The two chains are **distinct** (§3), so the off-path typing does not cover Thomson.
2. The Thomson relation **is** on alpha's value path — F0 link 4, absent (§1.1).
3. **But `kappa_Thomson`'s sealed requirement-list does not contain the source-inclusive
   completion** (§1.2). The only document that puts it in a Thomson-bound chain is a **failed
   attempt's post-mortem** whose opening words are *"The proof attempt fails"*, and whose list the
   sealed specification partly disowns (§1.3).

So: **the Thomson chain is on the path; the source-inclusive need is not in it.** The need lives in a
dead route to the Thomson object, not in the object's specification. **824's verdict survives, and
its edge (c) closes on the side 824 hoped for — by evidence, not by assumption.**

### 4.1 The residual, named exactly — CLAIMED

I will not overstate this. `kappa_Thomson`'s sealed list contains two entries whose own requirements
I have **not** settled here, and either could reopen the question one level up:

```text
RESIDUAL-1  "complete Q_spec".  complete_parameter_free_Q_spec_frozen = false, and that flag sits in
            R3's status block ALONGSIDE the source-inclusive ones.  Whether the complete Q_spec's
            construction requires a source-inclusive object is NOT settled by anything I read.
RESIDUAL-2  "regulator independence".  V011 requires it of the charged response.  The completion has
            a separate open named continuum_regulator_independence_derived = false (my 820's O3).
            THESE MAY OR MAY NOT BE THE SAME PROPERTY — one is regulator independence of the charged
            response computation, the other of the source-inclusive limit.  I APPLY THE SAME BAR THE
            RELAY SET ME: no identity assumed.  I do not merge them, and I do not separate them
            either; no sealed sentence does.
```

**The missing sealed sentence, named for the record:** a statement of what the **complete `Q_spec`**
requires of the source sector — specifically whether its construction consumes a source-inclusive
projective limit. With that sentence, RESIDUAL-1 resolves and this question closes completely.
Without it, the verdict above is exact but one level shallow, and I say so rather than paper it.

### 4.2 What this does to F0's chain and F-phase pricing

```text
F0 link 4 (K_* / kappa_Thomson)   remains ABSENT; unchanged by this relay.
Its requirement list                is SEALED and does NOT include O1/O2/O4.
F-phase pricing                     need NOT carry the infinite-future source objects on the
                                      Thomson link's account.
What F-phase DOES carry             complete Q_spec (RESIDUAL-1), and the derive-or-fail obligation
                                      on the K_*/kappa_Thomson relation, which may not be assumed —
                                      assuming it TRIPS A SEALED FALSIFIER.
My 824's re-pricing                 STANDS, and is now better grounded: O1/O2/O4 are off the junction
                                      runnability path (824) AND off the Thomson link's sealed
                                      requirement list (here).  They are not thereby discharged.
```

---

## 5. FREEDOMS-CONSUMED (law 2 / law 2a)

```text
CARRIED, NOT CONSUMED:
  O1, O2, O4                     CARRIED AS NAMED OPENS; not discharged by this relay
  K_*'s 16-link chain            CARRIED AS Q-696 STATES IT; link 4 remains ABSENT
  kappa_Thomson's requirement    CARRIED AS THE SEALED DETERMINATION EXTRACTS IT, second-hand to
    list                           V011 and DISCLOSED as such, custody NOT upgraded
  R-L2b's four obligations       CARRIED AS PLAN V004 NAMES THEM; discharge state UNKNOWN, untouched
  kappa_record = kappa_Thomson   CARRIED AS A DERIVE-OR-FAIL OBLIGATION; NOT assumed — assuming it
                                   trips a sealed falsifier
DERIVED HERE:                    nothing.  A comparison and a placement verdict.
COMPUTED HERE:                   a distinguishing-term count over the R-L2b over-charter; span
                                   digests.  No physical quantity, no member, no coefficient.
NO IDENTITY ASSUMED (S13):       the relay's bar, applied THREE times — R-L2b vs Thomson (separated
                                   on evidence); TASK4A's route vs kappa_Thomson's specification
                                   (separated on evidence); and V011's "regulator independence" vs
                                   the completion's O3 (NEITHER merged NOR separated — no sealed
                                   sentence does it, so I leave it typed as a residual).
SCALING WEIGHTS (law 2a):        NONE CONSUMED.
SUBSTITUTED:                     NONE.
```

## 6. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

37 rows walked. Live and discharged:

- **S13** (*"arm 2 may consume R-L2b, never the reverse"*) — **LIVE AND DIRECTLY ON POINT.** This
  relay compares R-L2b to another chain and must not let the comparison run backwards. It does not:
  I take nothing from the Thomson side into R-L2b, and the R-L2b over-charter's own sentence — *"this
  route does not bypass A-L0 arm 2 — IT ISOLATES IT"* — is carried unchanged.
- **S12** — LIVE; the typed flags (`KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false`,
  `complete_parameter_free_Q_spec_frozen = false`) are carried as the record's typed statuses, never
  as objects.
- **S26 / S08** — untouched; no smooth import, no EM identification. Note the temptation was real
  here: `kappa_Thomson` is an electromagnetic-sounding object, and I have typed it **only** through
  V011's own definition of the transverse charged response, with no EM reading attached.
- **S25, S03, S28, S34** — untouched or carried as typed.

**No undecidable is rescued by reaching for an axiom.** Where the chain of requirements ran one level
deeper than the sealed stock reaches, I named the missing sentence (§4.1) instead of supplying it.

`FLATTENING_CHECK = clean (37/37 rows walked; S13 live, on point, and discharged).`

## 7. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) THIS RELAY SETTLES MY OWN AMBIGUITY IN MY OWN FAVOUR, AND THAT IS THE THING TO WATCH.** 824
displayed edge (c) and declined to resolve it; 827 resolves it in the direction that leaves 824's
verdict standing. That is exactly the shape a lane produces when it is grading its own homework.
Three checks against it: the settlement rests on a **sealed requirement-list I did not write**
(`671a94a7f55a5649`, Codex lane); it turns on a **positive absence** (source-inclusive completion is
not in that list) rather than on my reading of an ambiguous sentence; and it **names a residual that
could overturn it** (§4.1). If the complete `Q_spec` turns out to consume a source-inclusive object,
my verdict flips and 824's edge (c) reopens.

**(2) FOUR OF THE PRIMARY SOURCES ARE MY OWN LANE'S** — Q-696 (my 790), Q-691 (my 782), 824, and the
plan entries I previously audited. The Codex-lane determination in §1.2 is the one load-bearing
source that is not mine, and it is the one doing the decisive work. Stated so the balance is visible.

**(3) I APPLIED MY OWN 818 LESSON AGAINST MYSELF AND IT COST ME THE CLEANEST VERSION OF THE
FINDING.** The `kappa_Thomson` requirement-list is a **reported citation** of `V011:1587-1655`, not
V011's bytes — the same second-hand pattern I flagged at 818 (A15). The clean move would have been to
resolve it against V011 directly; ARCHIVE-SIDE ONLY plus the V011 two-copy hazard made me decline to
treat a line-range as a byte-range. So the decisive extraction is sealed but second-hand, and I have
not upgraded its custody.

**(4) I LEFT ONE COMPARISON DELIBERATELY UNMADE.** V011's "regulator independence" and the
completion's `continuum_regulator_independence_derived` look like the same words. I neither merged
nor separated them. Merging would have closed the residual and produced a tidier verdict; separating
would have produced a cleaner negative. **No sealed sentence does either**, and the relay's own bar
was "no identity assumed" — which cuts against assuming *difference* just as much as sameness.

---

```
THOMSON_CHAIN = extracted (needs verbatim)
  POSITION: F0 link 4 of K_*'s 16-link defining chain (Q-696: 5 EXIST / 7 PLAN-DELIVERABLE / 4
  ABSENT), the fourth absence being "the derived K_*/kappa_Thomson relation … NO sealed
  K_* = kappa_Thomson statement or proof exists".  Plan V004: "ALPHA'S STIFFNESS IS K_*", so this
  link is ON alpha's value path by construction.  Assuming the identity TRIPS A SEALED FALSIFIER
  (INSIDE_OUTSIDE_CONSISTENCY_CONDITION), so it must be DERIVED.
  SEALED NEEDS LIST, verbatim (671a94a7f55a5649 .md.seal OK, [9678,10168) sha 3de949cc508dc65d, and
  §2.4 [13419,…)): "complete Q_spec; Z_Q[A]/Z_Q[0]; branch at A=0; transverse coefficient and
  q^2->0 limit", plus "the Ward identity, gauge-parameter independence, regulator independence, and
  threshold matching"; with KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false and
  KAPPA_THOMSON_REQUIRES_DECLARED_A0_REFERENCE = true.
  "SOURCE-INCLUSIVE CONTINUUM/PROJECTIVE COMPLETION" IS NOT IN THAT LIST.
  CUSTODY DISCLOSED: this is a REPORTED CITATION of V011:1587-1655, not V011's bytes — the same
  second-hand pattern I flagged at 818 (A15).  Sealed, relied on, custody NOT upgraded.

RL2B = extracted
  POSITION: plan V004 PHASE E ("close 7A"), item E2 — "R-L2b discharge state — UNKNOWN AND AMBIGUOUS
  OF RECORD: 'SUPERSEDED / RE-POSED, NOT SOLVED' vs 'REFUTED AS WRITTEN' unreconciled; four
  obligations and an unattacked response-pullback half named."
  CONTENT (1497d017175c900f): the RECORD-SIDE density and its universality over refinements — whether
  kappa_record is a property of the theory or of the fixture; the D3 quantifier over cellulations;
  exact additivity covering the DISCONNECTED part with connected_cross_cell_terms_derived = FALSE;
  the cluster expansion existing "precisely to handle the CONNECTED corrections"; and no physical
  criterion for the cell scale ("ABSENT, AND THE CORPUS APPEARS TO DENY IT").  Its own limit: "this
  route does not bypass A-L0 arm 2 — IT ISOLATES IT."

COMPARISON = DISTINCT (spans displayed)
  (a) DIFFERENT PLAN PHASES: R-L2b is Phase E item E2; K_*/Thomson is Phase F, F0 link 4.  The plan's
      off-path typing attaches to the Phase-E campaign.
  (b) ZERO LEXICAL OVERLAP on every distinguishing term across R-L2b's 15,986 chars:
      thomson 0 | q^2 0 | retarded 0 | 1PI 0 | 2PI 0 | source-inclusive 0 | linked-cluster 0 |
      cumulant 0 | (extensiv 3).  The campaign the plan CALLS "linked-cluster" does not contain the
      phrase, and never mentions Thomson, the retarded response, or source-inclusive completion.
  (c) DIFFERENT OBJECTS: R-L2b's object is kappa_record's density — the record-side intensive
      Hessian, which the plan says is NOT alpha's stiffness.  kappa_Thomson's object is the
      transverse quadratic response of the complete charged specification at q^2 -> 0.  And V011's
      spec affirmatively sets KAPPA_THOMSON_REQUIRES_STATIONARY_2PI_BACKGROUND = false.
  CONSEQUENCE: the plan's "R-L2b/linked-cluster is OFF alpha's value path" DOES NOT TRANSFER to the
  Thomson chain.  824 was right to refuse the transfer.

SOURCE_QUESTION_HOLDER = off alpha's path on the sealed stock — but NOT by the identity
  The identity is REFUTED, so the Thomson relation IS on alpha's value path (F0 link 4).  BUT
  kappa_Thomson's SEALED REQUIREMENT LIST DOES NOT CONTAIN THE SOURCE-INCLUSIVE COMPLETION.  The only
  document placing it in a Thomson-bound chain is a FAILED ATTEMPT'S POST-MORTEM —
  TASK4A_RESPONSE_CLASS_FORCING [85,1065) sha c0e90ace7af7fcf6, opening "The proof attempt FAILS at
  the physical source completion" — and the sealed specification DISOWNS PART OF THAT LIST (V011
  requires no stationary 2PI background, which that route lists).  The need lives in a DEAD ROUTE to
  the Thomson object, not in the object's specification.
  824's VERDICT SURVIVES and its edge (c) CLOSES on the side 824 hoped for — by evidence, not
  assumption.
  RESIDUAL, NAMED, AND IT COULD FLIP THIS: kappa_Thomson requires (1) "complete Q_spec", whose flag
  complete_parameter_free_Q_spec_frozen = false sits in R3's status block ALONGSIDE the
  source-inclusive ones, and whether its construction consumes a source-inclusive object is NOT
  settled by anything I read; and (2) "regulator independence", which MAY OR MAY NOT be the
  completion's continuum_regulator_independence_derived — I merge neither nor separate them, because
  no sealed sentence does and the relay's bar cuts both ways.
  THE MISSING SEALED SENTENCE, NAMED: a statement of what the COMPLETE Q_SPEC requires of the source
  sector.  With it, RESIDUAL-1 resolves and this question closes completely.  Without it this verdict
  is exact but one level shallow, and I say so rather than paper it.
  CONSEQUENCES FOR F0 / F-PHASE: link 4 stays ABSENT, unchanged; its requirement list is sealed and
  excludes O1/O2/O4, so F-phase pricing need not carry the infinite-future source objects on the
  Thomson link's account; what F-phase DOES carry is complete Q_spec and the derive-or-fail
  obligation, which may not be assumed.  824's re-pricing STANDS and is better grounded — O1/O2/O4
  are off the junction path (824) AND off the Thomson link's sealed list (here).  THEY ARE NOT
  THEREBY DISCHARGED.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) THIS RELAY SETTLES MY OWN AMBIGUITY IN MY OWN FAVOUR — the shape a lane produces when grading
      its own homework.  Three checks against it: the settlement rests on a SEALED REQUIREMENT LIST I
      DID NOT WRITE (Codex lane, 671a94a7f55a5649); it turns on a POSITIVE ABSENCE from that list
      rather than on my reading of an ambiguous sentence; and it NAMES A RESIDUAL THAT COULD OVERTURN
      IT.  If complete Q_spec consumes a source-inclusive object, my verdict flips and 824's edge (c)
      reopens.
  (2) FOUR OF THE PRIMARY SOURCES ARE MY OWN LANE'S (Q-696/790, Q-691/782, 824, and plan entries I
      previously audited).  The one load-bearing source that is NOT mine is the one doing the
      decisive work.
  (3) I APPLIED MY OWN 818 LESSON AGAINST MYSELF AND IT COST ME THE CLEANEST VERSION OF THE FINDING —
      the requirement list is a REPORTED CITATION of V011:1587-1655, not V011's bytes.  ARCHIVE-SIDE
      ONLY plus the V011 two-copy hazard made me decline to treat a line-range as a byte-range, so
      the decisive extraction is sealed but SECOND-HAND and I have not upgraded its custody.
  (4) I LEFT ONE COMPARISON DELIBERATELY UNMADE: V011's "regulator independence" versus the
      completion's continuum_regulator_independence_derived.  Merging would have closed the residual
      and produced a tidier verdict; separating would have produced a cleaner negative.  No sealed
      sentence does either, and "no identity assumed" cuts against assuming DIFFERENCE just as much
      as sameness.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
