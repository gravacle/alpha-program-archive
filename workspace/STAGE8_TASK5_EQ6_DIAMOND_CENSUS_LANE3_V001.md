# STAGE 8 / TASK 5 / EQ6 — DIAMOND CENSUS FOR THE J7 COEFFICIENT FACE

Lane: **Codex Lane 3**  
Version: **V001**  
Date: **2026-08-05**  
Register head checked before construction: **Q-489**

This artifact classifies the actual exhibited common-refinement diamonds and
audits the larger guard-required census domain. It does not repair or reassert
the killed membership theorem. The Lane 2 artifacts below are hash-cited
inputs, not Lane 3 work product.

The lead result is:

```text
EXPLICIT_ACTUAL_DIAMOND_PROTOTYPES = 3
  = {D-sub, D-disj, D-contact}

ASSERTED_BUT_UNEXHIBITED_TRANSPORT_CLASS = D-MIX
SEALED_CONSUMER_BOTTOM_FAMILIES = 10
EXPLICIT_CLAUSE_VI_BASE_ARROW_WITNESSES = 1
  = {r_asym:G->G_a}
FULL_I_F_DIAMOND_CARDINALITY = UNDETERMINED

CENSUS = DEMANDED_UNFORMABLE
  by O1 at an exhibited acyclic source
  and O2 on exhibited target-only new-cycle positions.

MACHINERY_APPEAL =
  FULL_CONSUMER_ARROW_EXHIBITION
  + GUARD_ARROW_COMPLETION
  + ORBITWISE_DIAMOND_MANIFEST
```

The machinery appeal records missing finite data and does not stop the
structural classification requested below.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_binding = none
fixed_point_execution = none
end_test = none
numeric_evaluation = none
measured_constant_comparison = none
```

## 0. Preflight and sealed inputs

The cleanroom, archive workspace, and supervision directory were readable.
The requested Lane 3 filename was absent in both the cleanroom and archive
workspace before construction. Q-489 was the register head. The commissioned
inputs and the supporting explicit-diamond stock were independently hashed
before use:

| Input | Verified SHA-256 | Use here |
|---|---|---|
| `STAGE8_TASK5_EQ6_J7_FACE_AND_J2_DISPLAY_LANE3_V001.md` | `9c74ed3dbe748450e525102128b0367ce21c0e5029929eab33a111ca9802dc3c` | built `A_RP+` face and obstructions O1/O2 |
| `STAGE8_TASK5_EQ6_MEMBERSHIP_THEOREM_LANE3_V001.md` | `0d00c9cfdb7187d45bb7892a5f71131b2cf1e0d5b17f871bdde3a770de2c0ae6` | proposed `I_F`, M4 overlap classes, and the D1-defective consumer omission |
| `STAGE8_TASK5_EQ6_PARTIAL_JOINT_DIAMONDS_LANE2_V001.md` | `5539b53ddeb208638af314f34e018591e1e6bd93403906e6a5edd46bc34e4766` | universal J7 overlap contract |
| `STAGE8_TASK5_EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001.md` | `a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b` | guard B1.1 clause (vi), D-RP/D-FLIP/D-MIX scope |
| `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md` | `a46beae26048a0d093de339363fb8597d362a0101d103cfc06305edabac53f4c` | ten bottom-coordinate consumer families |
| `STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md` | `9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514` | confirmed assembly successor; no additional diamond manifest |
| `STAGE8_TASK5_EQ6_ALLRANK_W1W4_MEMBERS_LANE2_V001.md` | `dec994976774bf598e79cd496f6b424d777d2321df2b41c0393219a3247c3ad6` | the three executed actual four-stage diamonds and their cycle carriers |
| `STAGE8_TASK5_EQ6_LOCAL_ORTHOGONAL_EXCISION_CERT_LANE2_V001.md` | `d61a550a33bf1215c35f4d6f27cd2ec5d644b93a05e16d08fe3d43ded3416817` | disjoint/contact cycle ranks and actual face geometry |
| `STAGE8_TASK5_EQ6_SECTION_DEEPER_CONSTRUCTION_LANE1_V001.md` | `a78c2450985429f6c9e152eb52ab6f133b013e1a1b1d695e2cc5241617b4bdb4` | later unconditional disjoint/contact flip terms on `I_flip` |
| `STAGE8_TASK5_EQ6_FLIP_SECTION_CHECK_AND_SCOPED_WITNESS_LANE2_V001.md` | `0e61e6ebe8a7d69390c60f0fa1490c46a866c95e1f4a00ad6ffb7646bec62ee7` | independent confirmation of the three constructed diamonds on `I_flip` |
| `STAGE8_TASK5_EQ6_MAXWELL_HODGE_PROJECTOR_CERT_LANE2_V001.md` | `f074ca24e8b96c576f5c64b856377f39ed8d4fc729c02cbf591326322558f816` | retained `m_T != 0` and `m_T = 0` branches |
| `STAGE8_TASK5_EQ6_MEMBERSHIP_REVIEW_DARIO_V001.md` | `9caef0f7fa23b7f77fd2dfa27e003b706b3572419480e6333e8b851cfcfe2906` | D1's exact extensional-exhaustion defect |

No register, plan, tracker, git, commit, push, member, fixed point, end test,
or numerical evaluation is performed by this artifact.

## 1. The census domain and its exact exhibition boundary

### 1.1 What the J7 contract quantifies over

The diamonds input gives the generic common-refinement square

```text
      a
  N -------> B
  |          |
f |          | g
  v          v
  C -------> D
      h

g o a = h o f.                                   (CEN-1)
```

Its D2 J7 slot requires physical coefficient extraction on each overlap
arrow and carrier-level naturality on both routes. Consequently, for every
square admitted to FC11, the contract demands:

```text
four scalar stage readers at N, B, C, D;
four coefficient-transport faces on a, f, g, h;
one equality of the two N-to-D routes.            (CEN-2)
```

The contract contains no acyclic-vertex exemption and no cycle-creating
new-factor exemption. Calling either position vacuous would change D2 rather
than satisfy it.

### 1.2 The three exhibited actual prototypes

The explicit all-rank source states

```text
ACTUAL_DIAMONDS_COMPUTED = 3,
```

and exhibits exactly these four-stage prototypes:

```text
D_exec := {D-sub, D-disj, D-contact}.             (CEN-3)
```

In the all-rank source, the unrestricted disjoint/contact candidates are
executed but not admitted. The later section-deeper construction and its
flip review construct flippable representatives with the same four-stage
cycle patterns and make those representatives unconditional on `I_flip`.
Sections 4--5 classify those later guard-admitted flippable representatives;
they do not promote the earlier unrestricted candidate families. The later
sources add no fourth geometric prototype.

The complete stage and arrow displays for the three are Sections 3--5. They
exhaust the all-rank source's *explicit executed-three prototype list*.
Assembly V003/V004 supplies no finite diamond list and no orbitwise
manifest. Accordingly, `(CEN-3)` does not exhaust all squares of the guard's
`I_F`.

The guard and membership M4 also name a mixed subdivision/flip class. Its
maximal licensed display is the schematic class in Section 6: the guard
calls the joint diamond unbuilt, and no sealed source supplies its four
actual stages. It is therefore recorded and not counted as a fourth
executed actual prototype.

### 1.3 Clause (vi): the ten consumers are not ten arrows

The guard requires

```text
A_cons
 := { e:X->Y : e is a finite-bottom arrow needed by a sealed
                package consumer }.              (CEN-4)
```

The text of clause (vi) supplies no arrow symbol, source, target, cycle
rank, or common-refinement partner. Guard B2.6 and the flip-scope attack do,
however, exhibit one separate asymmetric base-arrow witness, displayed in
Section 1.4. The assembly enumerates the following ten *bottom-coordinate
families* without assigning their full base-arrow sets:

| # | Sealed consumer family | Governing comparison in the assembly | Exhibited clause-(vi) base arrow and endpoints | Structural class licensed there |
|---:|---|---|---|---|
| 1 | Gate 1--4 | sealed restriction/zero-extension equations | none | not supplied |
| 2 | Q-243 | exact finite scalar shadow | none; one rank-preserving identity-extension exercise is shown, not an exhaustion of clause (vi) | rank-preserving for that exercise only |
| 3 | Q-279 | exact finite bilocal shadow | none; same limitation as Q-243 | rank-preserving for that exercise only |
| 4 | Q-309 | exact old-kernel/mixing-zero restriction | none; one old-image cycle-creating exercise is shown | `A_CC` old-image for that exercise only |
| 5 | Q-408 | represented/contact response old-image restriction | none | not supplied family-wide |
| 6 | action/Hessian | J4 relational comparison | none | not supplied |
| 7 | Ward/C3 | pushout Ward map and sealed restriction | none | not supplied |
| 8 | reader | sealed algebraic reader bottom | none | not supplied |
| 9 | WHERE/bundle | A1 restriction and bundle transport | none | not supplied |
| 10 | C1/Faith | finite norm, bound, and separation legs | none | not supplied |

Thus

```text
CONSUMER_FAMILIES_ENUMERATED = 10,
EXPLICIT_A_cons_BASE_ARROW_WITNESSES = 1
  = {r_asym:G->G_a}.                              (CEN-5)
```

Treating the ten coordinates as ten geometric arrows would be a type error.

### 1.4 The exhibited asymmetric consumer-arrow witness

The guard's fresh attack appends one active asymmetric cycle-creating
primitive to the flip scope. The flip review displays its source/target
status and finite content. Introduce the census label

```text
r_asym : G -> G_a,

G in Obj(I_flip),
G_a outside Obj(I_flip),
r_asym has actual Q-408 data and an actual finite bottom,
r_asym has no proved R1 naturality or six-component overlap maps. (ASY-1)
```

This is one clause-(vi) base-arrow witness because its finite bottom is live
and the anti-shrink rule forbids deleting it. Dario review section 1.2
expressly identifies the B2.6 asymmetric primitive as being of the same kind
as the active finite-consumer witness; that linkage, rather than finite-bottom
existence alone, licenses the `A_cons` label. Its asymmetric decoration has no
current-reversing stabilizer and its target has an old/new harmonic block. It
is not identified by the sealed stock with `A_RP`, the flippable part of
`A_CC`, or `A_CR`; its census class is therefore `A_cons / asymmetric
cycle-creating`.

`r_asym` labels the source's full symbolic asymmetric witness family. No
value of its deformation parameter and no family member is selected.

The symmetry-breaking common-refinement attack has the maximal displayed
template

```text
                   u
              G --------> G_u
              |            |
       r_asym |            | r_u
              v            v
              G_a -------> G_ua
                   u_a

r_u o u and u_a o r_asym are the two attack routes.        (ASY-2)
```

Here `u` is a flippable `A_CC` arrow. The pullback of the asymmetric
decoration to `G_ua` breaks the path-exchange stabilizer. The sources do not
supply the classes of `r_u,u_a`, the equality of the six package routes, or
the missing overlap maps; accordingly `(ASY-2)` is an attack template, not
a certified FC11 diamond.

| Attack position | Cycle information supplied | `m_T` status | Coefficient-face status |
|---|---|---|---|
| `G` | source cycle content not fixed by the attack | `UNDETERMINED` | branch and demand position await the full overlap exhibit |
| `G_u` | contains the flippable new cycle | `UNDETERMINED` | scalar position conditional on nonvanishing; route data incomplete |
| `G_a` | contains the asymmetric target-only new cycle | `UNDETERMINED` | target-only scalar route absent, the same new-cycle territory as O2-new |
| `G_ua` | retains the symmetry-breaking decoration; exact rank not supplied | `UNDETERMINED` | six-component overlap and scalar route absent |

The four arrow labels in `(ASY-2)` are therefore enumerated at their maximal
sealed precision: `u` is classified, `r_asym` is the one explicit consumer
base-arrow witness, and the two common-refinement attack legs remain
unclassified. This witness does not constitute the complete set `(CEN-4)`.

The membership build instead defines only `A_iso`, `A_RP`, `A_CC`, and
`A_CR`; it neither includes `(CEN-4)` nor proves that those four classes
extensionally contain `(CEN-4)`. Closure under composition cannot create a
missing generator.

The same domain audit retains Dario's two narrower supporting observations:
guard clause (ii) includes every sealed W3/DoR-008 restriction, whereas the
membership build places only rank-preserving W3/DoR-008 arrows in `A_RP`;
and guard clause (v) covers refinements built for clauses (i)--(iv), whereas
the build's `A_CR` is restricted to `A_RP union A_CC`. Neither omitted part
has an orbitwise diamond manifest in the cited chain.

The exact remaining missing exhibition is

```text
FULL_CONSUMER_ARROW_EXHIBITION
 := { (consumer, e:X->Y, class(e), beta_1(X), beta_1(Y),
        all built common-refinement partners of e) },

GUARD_ARROW_COMPLETION
 := { unqualified W3/DoR-008 arrows and clause-(i) common-refinement
      legs omitted by the membership build, with the same endpoint data }.
                                                        (CEN-6)
```

The single witness `(ASY-1)` does not fill either set in `(CEN-6)`. No cited
source contains their full extension. Moreover, `Obj(I_F)` is closed under
relabeling, reality, frame, bundle-gauge, orientation, and covariance
images. The phrase "finite closure" means closure under finite words; it is
not a finite-cardinality theorem. Therefore a numerical global diamond
count is not licensed.

```text
FULL_CENSUS_COUNT = UNDETERMINED;
MACHINERY_APPEAL = FULL_CONSUMER_ARROW_EXHIBITION
                    + GUARD_ARROW_COMPLETION
                    + ORBITWISE_DIAMOND_MANIFEST. (CEN-7)
```

## 2. Symbolic `m_T` classifier and face-formability rules

At a stage `X`, write

```text
s_T,X := Loc_X Kernbar_X Q_X(L_T,X),
m_T,X := P_H,X s_T,X,
H_X   := im(P_H,X).                               (MT-1)
```

Only the following structural implications are licensed:

```text
H_X = {0}
  => P_H,X = 0
  => m_T,X = 0
  => m_T status = ZERO.                           (MT-2)

H_X != {0}
  does not imply P_H,X s_T,X != 0.                (MT-3)
```

For a rank-one harmonic sector `H_X=span{c}`, the exact symbolic test is

```text
m_T,X
 = <s_T,X,c>_X / <c,c>_X c,

m_T,X != 0  iff  <s_T,X,c>_X != 0.               (MT-4)
```

For higher rank,

```text
m_T,X = 0
  iff <s_T,X,h>_X = 0 for every h in H_X.         (MT-5)
```

The sources prove the existence of harmonic cycle vectors at cyclic stages,
but do not prove `(MT-4)`'s nonzero pairing for the particular `L_T` symbol.
They expressly retain both normalization branches. Hence every exhibited
cycle-carrying stage below is `UNDETERMINED`, not `NONZERO`. Equations
`(MT-4)`--`(MT-5)` display the missing premise; no admissible-model claim is
needed to retain the sealed zero branch.

The face-formability rules are:

```text
F-RP:
  on a wholly nonzero rank-preserving square, every leg is in A_RP+
  and the Lane 3 coefficient face is FORMABLE;

O1-Z:
  at a proved m_T=0 stage, lambda_X contains <0,0> in its denominator,
  so the scalar reader and coefficient pair are UNFORMABLE;

BRANCH-U:
  at an UNDETERMINED stage, formability is itself UNDETERMINED:
  the scalar face is FORMABLE if m_T!=0 and UNFORMABLE(O1) if m_T=0;

O2-old:
  on an A_CC old-image leg, stock supplies only
      J_X^bot : D_e^old -> Bot_X^resp,
  while the coefficient face requires an R-valued scalar reader;
  no Bot_X^resp -> R map closes that typing gap;

O2-new:
  on a target-only new-cycle complement K_e^new, D_e^old supplies no
  map at all, so scalar coefficient transport is UNFORMABLE.          (MT-6)
```

`O2-old` and `O2-new` distinguish the two failures on a cycle-creating leg.
The A6 bottom equality is not substituted for the J7 scalar equality.

## 3. `D-sub`: rank-preserving subdivision diamond

To distinguish its first-cycle source from the tree source below, write it
as `G_0^sub`. The actual four stages and their cycle content are:

| Position | Stage | Harmonic/cycle content | `m_T` classification | J7 stage coefficient demanded? | Scalar stage coefficient formable from sealed stock? |
|---|---|---|---|---|---|
| source | `G_0^sub` | rank one, `H=span{c}` | `UNDETERMINED` by `(MT-4)` | yes | `UNDETERMINED`: formable on the nonzero branch, O1 on the zero branch |
| upper | `G_s` | same rank-one cycle after subdivision at `s` | `UNDETERMINED` | yes | same branch dichotomy |
| lower | `G_t` | same rank-one cycle after subdivision at `t` | `UNDETERMINED` | yes | same branch dichotomy |
| target | `G_st` | same rank-one cycle after both subdivisions | `UNDETERMINED` | yes | same branch dichotomy |

The four actual subdivision arrows are displayed as

```text
r_0s  : G_0^sub -> G_s,
r_0t  : G_0^sub -> G_t,
r_sst : G_s     -> G_st,
r_tst : G_t     -> G_st.                         (DS-1)
```

| Arrow | Structural class | Cycle effect | J7 transport demanded? | Formability |
|---|---|---|---|---|
| `r_0s` | `A_RP`, branch undetermined | rank one to rank one | yes | `A_RP+` and formable iff the component is nonzero; otherwise both ends are zero and O1 bites |
| `r_0t` | `A_RP`, branch undetermined | rank one to rank one | yes | same |
| `r_sst` | `A_RP`, branch undetermined | rank one to rank one | yes | same |
| `r_tst` | `A_RP`, branch undetermined | rank one to rank one | yes | same |

For every one of these arrows, rank-preserving transport gives

```text
m_T,target = j^C m_T,source.                     (DS-2)
```

The forward Hodge map is injective. Hence the connected square has exactly
the displayed dichotomy:

```text
all four m_T are nonzero
  => every leg is A_RP+ and the full face is FORMABLE;

one m_T is zero
  => all four m_T are zero and every scalar position is UNFORMABLE(O1).
                                                        (DS-3)
```

No sealed premise selects either line of `(DS-3)`, so all four positions
remain `UNDETERMINED` in this census.

## 4. `D-disj`: disjoint cycle-addition diamond

The actual disjoint-corridor square is

```text
                 r_0U
       G_0^disj --------> G_U^disj
          |                   |
     r_0V |                   | r_UV
          v                   v
       G_V^disj --------> G_UV^disj
                 r_VU                            (DD-1)
```

Its four stages are:

| Position | Stage | Harmonic/cycle content | `m_T` classification | J7 stage coefficient demanded? | Scalar stage coefficient formable from sealed stock? |
|---|---|---|---|---|---|
| source | `G_0^disj` | connected tree; `H={0}` | `ZERO` by `(MT-2)` | yes | no: `O1-Z` |
| upper | `G_U^disj` | `H=span{c_U}` | `UNDETERMINED` by `(MT-4)` | yes | `UNDETERMINED`: formable if nonzero, O1 if zero |
| lower | `G_V^disj` | `H=span{c_V}` | `UNDETERMINED` by `(MT-4)` | yes | same branch dichotomy |
| target | `G_UV^disj` | `H=span{c_U,c_V}` | `UNDETERMINED` by `(MT-5)` | yes | same branch dichotomy |

The four arrows and every old/new position are:

| Arrow | Class | Licensed old harmonic image | Target-only new-cycle position | J7 scalar transport demanded? | Formability |
|---|---|---|---|---|---|
| `r_0U:G_0^disj->G_U^disj` | `A_CC` | `{0}` | all of `span{c_U}` | yes | old scalar stage fails O1-Z; new position fails O2-new |
| `r_0V:G_0^disj->G_V^disj` | `A_CC` | `{0}` | all of `span{c_V}` | yes | old scalar stage fails O1-Z; new position fails O2-new |
| `r_UV:G_U^disj->G_UV^disj` | `A_CC`, also an upper common-refinement leg | `span{c_U}` | complement generated by `c_V` | yes | old-image scalar route fails O2-old; new position fails O2-new; cyclic-vertex scalar normalization remains branch-undetermined |
| `r_VU:G_V^disj->G_UV^disj` | `A_CC`, also an upper common-refinement leg | `span{c_V}` | complement generated by `c_U` | yes | old-image scalar route fails O2-old; new position fails O2-new; cyclic-vertex scalar normalization remains branch-undetermined |

The target-only complements in the final column exist structurally regardless
of whether any `m_T` later proves nonzero. Thus O2 is not removed by resolving
O1 favorably.

## 5. `D-contact`: endpoint-contact cycle-addition diamond

Let the superscript `ct` distinguish this actual endpoint-contact geometry.
The two corridors meet only at the recorded endpoint `q`; their cycle
carriers have the same rank pattern as the disjoint square:

```text
                 r_0U^ct
       G_0^ct ----------> G_U^ct
         |                    |
 r_0V^ct |                    | r_UV^ct
         v                    v
       G_V^ct ----------> G_UV^ct
                 r_VU^ct                         (DC-1)
```

| Position | Stage | Harmonic/cycle content | `m_T` classification | J7 stage coefficient demanded? | Scalar stage coefficient formable from sealed stock? |
|---|---|---|---|---|---|
| source | `G_0^ct` | connected tree; `H={0}` | `ZERO` | yes | no: `O1-Z` |
| upper | `G_U^ct` | `H=span{c_U^ct}` | `UNDETERMINED` | yes | `UNDETERMINED`: formable if nonzero, O1 if zero |
| lower | `G_V^ct` | `H=span{c_V^ct}` | `UNDETERMINED` | yes | same branch dichotomy |
| target | `G_UV^ct` | `H=span{c_U^ct,c_V^ct}` | `UNDETERMINED` | yes | same branch dichotomy |

| Arrow | Class | Licensed old harmonic image | Target-only new-cycle position | J7 scalar transport demanded? | Formability |
|---|---|---|---|---|---|
| `r_0U^ct:G_0^ct->G_U^ct` | `A_CC` | `{0}` | all of `span{c_U^ct}` | yes | O1-Z on the source and O2-new at the target |
| `r_0V^ct:G_0^ct->G_V^ct` | `A_CC` | `{0}` | all of `span{c_V^ct}` | yes | O1-Z on the source and O2-new at the target |
| `r_UV^ct:G_U^ct->G_UV^ct` | `A_CC`, also an upper contact common-refinement leg | `span{c_U^ct}` | complement generated by `c_V^ct` | yes | O2-old on the old-image scalar route; O2-new on the complement; cyclic-vertex scalar normalization remains branch-undetermined |
| `r_VU^ct:G_V^ct->G_UV^ct` | `A_CC`, also an upper contact common-refinement leg | `span{c_V^ct}` | complement generated by `c_U^ct` | yes | O2-old on the old-image scalar route; O2-new on the complement; cyclic-vertex scalar normalization remains branch-undetermined |

Endpoint contact changes support/excision tests; it does not remove either
new cycle. The coefficient-face classification is therefore the displayed
one, not a disjointness inference.

## 6. `D-MIX`: named transport class, no actual four-stage exhibit

The guard describes subdivision-then-flip versus flip-then-subdivision. Its
maximal schematic typing is

```text
                 s : A_RP
             N ------------> N_s
             |                 |
    c : A_CC  |                 | c_s : A_CC/A_CR
             v                 v
             N_c -----------> N_sc
                 s_c : A_RP/A_CR,                (DM-1)

c_s o s = s_c o c.
```

For a one-cycle attachment and `r=dim H_N`, its schematic cycle ranks are

```text
dim H_N   = r,
dim H_Ns  = r,
dim H_Nc  = r+1,
dim H_Nsc = r+1.                                  (DM-2)
```

The possible symbolic classifications are:

| Schematic source case | `N` | `N_s` | `N_c` | `N_sc` | Coefficient consequence if the square is later exhibited |
|---|---|---|---|---|---|
| `r=0` | `ZERO` | `ZERO` | `UNDETERMINED` | `UNDETERMINED` | O1 at `N,N_s`; O2 on `c,c_s` new-cycle positions |
| `r>0` | `UNDETERMINED` | `UNDETERMINED` | `UNDETERMINED` | `UNDETERMINED` | no nonvanishing seed; O2 on `c,c_s` new-cycle positions |

This table is a parametric classification, not a fabricated actual diamond.
The guard calls the joint mixed diamond unbuilt, and the cited sources give
no concrete `N,N_s,N_c,N_sc`. Therefore `D-MIX` is recorded as an unexhibited
class and contributes no integer to `(CEN-3)`.

## 7. Clause-(vi) consumer-arrow diamond positions

For a concrete consumer arrow `e:X->Y`, a common-refinement entry would have
to display something of the form

```text
                  e
             X --------> Y
             |           |
           u |           | v
             v           v
             X' -------> Y'
                  e'

v o e = e' o u,                                  (CONS-1)
```

together with the four cycle carriers, the classes of `e,u,v,e'`, and the
old/new decomposition on every cycle-creating leg. Apart from the partial
asymmetric attack `(ASY-2)`, the ten-family table in Section 1.3 supplies
none of `X,Y,X',Y',e,u,v,e'`. Hence the remaining clause-(vi) consumer rows
have no licensed four-stage instantiation.

For every future exhibited `(CONS-1)`, the deterministic classifier is:

```text
acyclic vertex                    -> m_T ZERO;
cycle-carrying vertex             -> m_T UNDETERMINED absent a seed theorem;
FC11 coefficient position         -> DEMANDED by (CEN-2);
wholly A_RP+ route                 -> FORMABLE;
A_RP route on an undetermined branch -> formability UNDETERMINED;
A_RP route with a zero end         -> UNFORMABLE(O1);
A_CC old-image scalar route        -> UNFORMABLE(O2-old);
A_CC target-only new-cycle route   -> UNFORMABLE(O2-new).       (CONS-2)
```

This records every available consumer family without pretending that a
coordinate name determines a geometric arrow.

## 8. Census verdict and exact demanded-unformable positions

The obstruction is not vacuous. The following exhibited positions are
already demanded by `(CEN-2)` and are not formable from sealed stock:

| Actual diamond | Demanded-unformable position | Obstruction | Exact need at that position |
|---|---|---|---|
| `D-disj` | `G_0^disj` | O1-Z | a normalized scalar reader at a stage where `m_T=0`, which cannot be formed |
| `D-disj` | targets of `r_0U,r_0V` | O2-new | scalar coefficient data on the wholly new rank-one cycle positions |
| `D-disj` | old/new routes of `r_UV,r_VU` | O2-old and O2-new | an R-valued old-image comparison and scalar maps on the complementary new cycles |
| `D-contact` | `G_0^ct` | O1-Z | the same impossible zero-denominator normalization |
| `D-contact` | targets of `r_0U^ct,r_0V^ct` | O2-new | scalar coefficient data on the wholly new rank-one cycle positions |
| `D-contact` | old/new routes of `r_UV^ct,r_VU^ct` | O2-old and O2-new | an R-valued old-image comparison and scalar maps on the complementary new cycles |

In particular, even a future proof that all cyclic vertices have nonzero
`m_T` would not close O2. Conversely, retaining A6 on the old-image bottom
does not close the scalar coefficient face because its codomain remains
`Bot^resp`, not `R`.

Therefore the only licensed verdict is

```text
CENSUS = DEMANDED_UNFORMABLE.                     (VER-1)
```

The exact missing result that would resolve each branch-undetermined
rank-preserving component is, at minimum, a theorem of the form

```text
<Loc_X Kernbar_X Q_X(L_T,X), c_X>_X != 0          (VER-2)
```

at one stage of every rank-preserving component. The existing edge-orbit
witness concerns a different input and does not prove `(VER-2)`.

## 9. Count, completeness, anti-tuning, and verb battery

### 9.1 Completeness statement

The all-rank source's explicit executed-three prototype list is exhausted
exactly:

```text
1. D-sub      -- four stages and four arrows in Section 3;
2. D-disj     -- four stages and four arrows in Section 4;
3. D-contact  -- four stages and four arrows in Section 5.      (BAT-1)
```

No fourth prototype occurs in that source's own count. The mixed class is
separately recorded in Section 6, every sealed consumer family is enumerated
in Section 1.3, and the one active asymmetric base-arrow witness and its
attack template are displayed in Section 1.4. Global completeness for the
guard's `I_F` is not claimed: the full sets in `(CEN-6)` are absent, generic
`H_DIA` provides only `(CEN-1)`, and covariance closure has no finite orbit
manifest. This qualification prevents the D1 omission from being silently
repeated.

### 9.2 Anti-tuning ledger

| Forbidden input or action | Census use | Result |
|---|---|---|
| numerical value or magnitude of `m_T` | none; only `ZERO` forced by `H=0` and `UNDETERMINED` otherwise | clean |
| inference `cycle present => m_T != 0` | expressly rejected by `(MT-3)`--`(MT-5)` | clean |
| response, threshold, fixed-point, or end-test consequence | none | clean |
| member, frame, orientation, or basis binding | none; displayed cycle generators describe full structural carriers | clean |
| measured-constant comparison | none | clean |
| tuning `Loc`, `P_H`, or the metric to force a branch | none | clean |
| deletion of a hard consumer arrow | none; `r_asym` is displayed in `(ASY-1)` and the remaining manifest debt is flagged by `(CEN-6)` | clean |
| substitution of A6 bottom equality for scalar J7 | none; O2-old is retained | clean |
| fabrication of D-MIX or consumer endpoints | none; both are explicitly unexhibited | clean |

### 9.3 Self status-verb audit

| Status verb | Display supporting exactly that status | Audit |
|---|---|---|
| `ENUMERATED` for actual executed prototypes | `(CEN-3)` and Sections 3--5 list all three four-stage/four-arrow squares | exact on the executed-three list only |
| `ENUMERATED` for consumer families | ten rows in Section 1.3 | exact for coordinate families; never called an arrow enumeration |
| `EXHIBITED` for one consumer base-arrow witness | `(ASY-1)` gives its source/target status and `(ASY-2)` gives the symmetry-breaking attack template | no full consumer manifest inferred |
| `UNDETERMINED` for global count | absence `(CEN-6)`, generic square `(CEN-1)`, and orbit closure | no integer invented |
| `ZERO` | `(MT-2)` at the two exhibited tree sources | structural equality displayed |
| `UNDETERMINED` for cyclic `m_T` | `(MT-3)`--`(MT-5)` and the missing pairing `(VER-2)` | harmonic presence is not promoted to nonvanishing |
| `DEMANDED` | universal contract `(CEN-2)` | every claimed demanded position lies in an actual or explicitly conditional FC11 square |
| `FORMABLE` | used only conditionally on `A_RP+` in `(MT-6)` and `(DS-3)` | scope matches the prior Lane 3 build |
| `UNDETERMINED` for branch formability | `BRANCH-U` and `(DS-3)` | neither branch is promoted |
| `UNFORMABLE` | O1-Z and O2-old/O2-new in `(MT-6)` plus the position ledger | exact denominator/domain/codomain witness displayed |
| `DEMANDED_UNFORMABLE` | exhibited position ledger and `(VER-1)` | one actual counterposition is sufficient; several are displayed |
| `MACHINERY_APPEAL` | exact missing objects `(CEN-6)`--`(CEN-7)` | appeal is confined to the unavailable global census |
| `CLEAN` for anti-tuning and verb use | Sections 9.2 and 9.3 | every verb is no stronger than its display |

Scope retained at stop:

```text
MEMBERSHIP_THEOREM = NOT_ASSERTED
CLAUSE_VI_D1 = NOT_DISCHARGED (+10 consumer families; +1 base-arrow witness; +full arrow manifest absent)
ALLRANK_EXECUTED_PROTOTYPE_LIST = EXHAUSTED(3)
FULL_I_F_DIAMOND_CENSUS = UNDETERMINED
COEFFICIENT_FACE_ON_A_RP_PLUS = FORMABLE
COEFFICIENT_FACE_ON_ACTUAL_CYCLE_CREATING_DIAMONDS = DEMANDED_UNFORMABLE
MACHINERY_APPEAL = FULL_CONSUMER_ARROW_EXHIBITION + GUARD_ARROW_COMPLETION + ORBITWISE_DIAMOND_MANIFEST
```

DIAMOND_COUNT = UNDETERMINED (+3 exhibited prototypes; +1 asymmetric clause-(vi) base-arrow witness; +remaining consumer/guard/orbit manifest MACHINERY-APPEAL)
CENSUS = DEMANDED_UNFORMABLE (+D-disj/D-contact acyclic sources O1-Z; +r_0U/r_0V new targets O2-new; +r_UV/r_VU old/new routes O2-old/O2-new, in both squares)
VERB_AUDIT_SELF = CLEAN
