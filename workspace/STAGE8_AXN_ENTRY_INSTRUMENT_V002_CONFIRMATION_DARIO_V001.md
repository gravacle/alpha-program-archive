CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 13 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO (verifier)   SUBJECT_LANE = CODEX 2 (producer)
ROUND = confirmation of a revision my own 932 returned
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md` | `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1` | the subject under check |
| 02 | `relay_inbox/RELAY_PASTE_936_INSTRUMENT_V002_CONFIRMATION_DARIO_V001.md` | `ec7d64c295787a027ded70c1fccb8124c50d00901b626dbac5de51cea26aea5a` | assignment |
| 03 | `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V001.md` | `c650c578c8625ffba7e1e33713e65a10623adb41444a5bb95f76d76c4ced5046` | byte-copy base for the carriage test |
| 04 | `STAGE8_AXN_ENTRY_INSTRUMENT_CROSSCHECK_DARIO_V001.md` | `d80107968d8254dd883dcafb745129566d7b3939e8ec6fb9aeb49121b2e93a05` | my 932: the two returned items and the confirmed-sound surface |
| 05 | `STAGE8_AXN_RECORD_LABELLING_DARIO_V001.md` | `32ad8a9fe8524742b09e6003a015e6c5fb4560a02e054d5c46bfd118b2c25402` | my 930 labelling facts |
| 06 | — its spans `[10554,10816)`; `[11485,11670)` | `aa20388d86c74dff70f86b67b891b3a4f7df93809c08e510608ed047dce5e3b6`; `5450f120bde8fa0a0cf8d65d81ee6f28f1e8066f12d999b7c044b0f8105b9ad7` | NOTATIONAL branch; SUBSTANTIVE conditional |
| 07 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V002.md` | `58b966ed371b23b29b9e3ceed280eb30c804484becb2b17ca6ea465668e951bc` | the nine schema receivers, reverse-A2, `Delta_0^joint` |
| 08 | — `Delta_0^joint` span `[14222,14296)` | `bb73a8ec8816bddc9c84d84e48ca81bd8d315f316092cdf295dc8fb474e2826f` | the pinned defining bytes |
| 09 | `JOINT_ANCHOR_DECISION_OF_RECORD_V001.md` | `f0179d43b4b2ed89f4cfb9adc1daa8c7cc0c9c5f038e3bbe7332727fdddaaa8f` | frozen round-1 act; enum and freeze spans |
| 10 | `JOINT_ANCHOR_CERTIFICATE_MANIFEST_V001.md` | `589c12f7cb76b5f4a2ba895d5942061ec3d801d6983f656304b172bfde964b27` | second frozen round-1 enum site |
| 11 | `STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V001.md` | `438e6c42bf876678eb0b3c016e231228f807e4f29469c5e221597d716642f0e6` | the pinned stage ground |
| 12 | `STAGE8_AXN_STAGE_RULE_CROSSCHECK_DARIO_V001.md` | `ccadc47a0a4722876cba5e25aa7431d162916be65387df411521e1058a559460` | my 935: what propagates into the stage pin |
| 13 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md`; `JOINT_ANCHOR_DECISION_INSTANCE_V002.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a`; `72191e0115d6f36d2327236e7a6d16e21f953422ba3fb2188b75e3db009cea99` | state pin, process law, S01-S37 authority, open receiver |

```text
DIGESTS I RECOMPUTED FOR THIS CHECK: 31/31 MATCH.
  9 schema receivers + reverse-A2 + Delta_0^joint + 3 frozen-enum sites + 2 labelling spans
  + 2 pairing seeds + 2 history grounds + 4 blind extents + 5 carried i_src spans + 2 whole files.
EVERY SUBJECT AND SIDECAR VERIFIED BEFORE READING.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN ENTRY INSTRUMENT V002 CONFIRMATION — DARIO LANE — V001
## RELAY 936 — `[PLAN:AXN-BUILD-D35]` — THE READY RULING

Date: 2026-08-10
Status: **BOTH RETURNED ITEMS ARE CLOSED. VERDICT: READY-FOR-THE-PRINCIPAL, with one clerical
correction returned that does not block the round.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. The standard, stated before the findings

At 932 I ruled this instrument NOT-READY on two items. The relay names the symmetric obligation, and
I accept it without reservation: **two fixes delivered is READY unless something else is wrong.**

That obligation exists because a verifier's incentives run one way. Finding is what a verifier is
rewarded for, and an instrument returned once can be returned forever by a reviewer who keeps raising
the bar. So I fix the bar here, before looking:

- I do not re-litigate anything my 932 confirmed sound. That surface was confirmed; it stays confirmed
  unless the revision **changed** it, which is a carriage question with a byte answer.
- A new defect blocks only if I can **show** the failure, and only if it would corrupt a principal
  act. A defect that misleads no principal act is returned as a correction, not as a block.
- The two returned items are judged on whether the mechanism works, not on whether I would have
  written it the same way.

The two 932 items shared a property worth naming, because it is the property I test against below:
each would have produced a **defective principal act** — an enum contradicting a frozen act with no
disposition, and a faithfulness certificate written over a space that did not yet exist. That is the
severity bar a new item must clear to block this round.

## 2. Finding 1 — the frozen-enum disposition [PROVABLE]

### 2.1 What the mechanism must do

My 932 objection had two halves. `NEITHER_EQUIVALENCE_PROVED` stands entered in two places while, by
the instrument's own forcing rule, no joint pairing or certificate exists to warrant it — so it is
presently unwarranted and the instrument said nothing. And two of three round-2 branches contradict
it outright while the round-1 act carries `post_freeze_mutation: FORBIDDEN`, so two principal acts
could conflict with the earlier one unmutatable.

A lawful fix must dispose of the old enum **without touching it**.

### 2.2 The pinned grounds verify

```text
round-1 enum        member 09 [752,799)    d4c1e1d19f13cccf...   MATCH
freeze clause       member 09 [977,1038)   21b2f589d07b62c0...   MATCH
second enum site    member 10 [2073,2128)  6f8e182a3a8a8243...   MATCH
```

Both whole-file digests const-pinned inside the union match members 09 and 10. The union pins the
grounds it disposes of, which is the right shape.

### 2.3 All three branches, tested

| Branch | Certificate demanded | Enum consequence | Matches §3.2's unchanged forcing rule? |
|---|---|---|---|
| `CARRIED_UNWARRANTED_NOT_REUSED` | `round2_certificate_sha256: const null` | `downstream_enum_authority: const "NONE"` | **Yes** — this is the missing-certificate case, and §3.2 says the enum is unwarranted when pairing or certificate is missing |
| `REWARRANTED_IN_NEW_ACT` | both digests required; branch const `H-OTHER` | `NEITHER_EQUIVALENCE_PROVED`, authority `ROUND2_ONLY` | **Yes** — §3.2 says NEITHER survives "Yes, and only here", on a certified `H-OTHER` |
| `SUPERSEDED_BY_NEW_ACT` / `H-HAAR` | both digests required | `PRODUCT_HAAR_EQUIVALENT`, authority `ROUND2_ONLY` | **Yes** — §3.2 bars NEITHER on `H-HAAR` |
| `SUPERSEDED_BY_NEW_ACT` / `H-DIRAC` | both digests required | `IDENTITY_SUPPORTED_DIRAC_EQUIVALENT`, authority `ROUND2_ONLY` | **Yes** — §3.2 bars NEITHER on `H-DIRAC` |

Every branch is reachable, none is empty, and the four exhaust the cases the forcing rule generates:
no certificate; certified non-control; certified Haar; certified Dirac.

### 2.4 Does it avoid mutating the frozen act? [PROVABLE — YES]

Five independent guards, each a const in the record or a sentence in the text:

```text
round1_value:                const "NEITHER_EQUIVALENCE_PROVED"     <- carried, not rewritten
round1_post_freeze_mutation: const "FORBIDDEN"
round1_bytes_mutated:        const false
§2.1  "The frozen bytes remain historical bytes and are never edited."
§3.3  "The frozen round-1 bytes are never mutated."
```

And structurally: the disposition is a **field of the round-2 record**, marked
`[MECHANICAL RECORD; NOT A PRINCIPAL FIELD]`. It layers a new act on top of the frozen one rather
than reaching back into it.

**The contradiction branch is represented rather than resolved away**, which is the part I want to
record as correct. `downstream_enum_authority: ROUND2_ONLY` leaves both acts standing in custody and
moves only the *authority*. The instrument says so in its own words — two immutable acts remain, and
only the round-2 certified value has downstream authority. That is a disposition, not an edit, and it
is exactly what my 932 asked for.

**FINDING 1 = CLOSED (mechanism lawful).**

### 2.5 Two observations that do not block

1. Two arms share `tag: "SUPERSEDED_BY_NEW_ACT"`, so `tag` alone does not discriminate the union;
   resolution needs the pair `(tag, selected_history_branch)`. No record can satisfy both arms, since
   `selected_history_branch` is const-pinned to different values, so nothing ambiguous can validate.
   A naming nit, not a defect.
2. Arm 1 permits `selected_history_branch: null` together with a non-null `round2_pairing_sha256` — a
   combination §3.2 forbids for a filled entry, since every filled combination must choose exactly one
   history branch. Because the record is mechanically populated from the round-2 entry and §3.2
   independently governs that entry, no unlawful principal act follows. One tightening line would
   close it.

## 3. Finding 2 — the `E_joint` ninth field [PROVABLE]

### 3.1 What the fix must do

My 932 objection was precise: `Delta_0^joint := {Delta : Tr_joint(Delta)=0 and E_joint(Delta)=Delta}`
is fixed by the trace **and** the superselection map jointly. V001 set `Tr_joint` and never reached
`E_joint`, so a pairing entered under it would have carried a faithfulness certificate written over a
space not yet defined. The fix must make that space defined **in every pairing route** — not in the
common one.

### 3.2 The field is genuinely present, at the right receiver

```text
joint_superselection_map demand   member 07 [10330,10364)  600765572dc0dc06...  MATCH
Delta_0^joint defining bytes      member 07 [14222,14296)  bb73a8ec8816bddc...  MATCH
  content: "Delta_0^joint := {\n  Delta : Tr_joint(Delta)=0 and E_joint(Delta)=Delta\n}."
```

It is a receiver in §1, the ninth field of the closed schema, a law-9b authority row, a blank line on
the principal panel, and a member of the substitution census. The field is carried through, not
mentioned.

### 3.3 The labelling facts are pinned at the right spans, and quoted exactly

I rehashed both spans of my own 930 report independently, from my report's bytes rather than from the
instrument's account of them:

```text
NOTATIONAL branch     [10554,10816)  aa20388d86c74dff...  MATCH
SUBSTANTIVE branch    [11485,11670)  5450f120bde8fa0a...  MATCH
```

The substantive conditional is reproduced **byte-exactly**, including its line break and indentation:

```text
=> under this branch, E_joint = E_ch (x) id_R_inf (x) id_B is not merely UNFORCED: it is FALSE,
   because id_R_inf cannot be the charge-diagonal projection of a factor carrying charge.
```

And `EJ-TRIVIAL` carries **both** consequences of my 930 findings rather than the convenient one:
"unforced on a NOTATIONAL labelling and barred on a SUBSTANTIVE labelling". A route that is barred
under one live branch is disclosed as barred. That is faithful transcription of a finding that costs
the route something.

### 3.4 The three routes are priced and exhaustive

`EJ-TRIVIAL`, `EJ-NONTRIVIAL`, `EJ-BLANK`, each with its own price draft, each closing with "No
labelling branch is called derived" or the blank-route incompleteness statement. The trichotomy is
exhaustive on its stated axis — the exact joint map is the displayed trivial extension, some other
lawful joint expectation, or absent — and that axis is total. Every price names what it prices.

### 3.5 The condition is on a DEFINED space in every route [PROVABLE — CLOSED]

This is the question that decides Finding 2, and the answer is closed in four independent places:

1. **§3.2's added sentence quantifies over all nine filled combinations:** "Every filled combination
   also requires a nonblank `joint_superselection_map` route and the
   `PairingEJointCrossReference.v001` faithfulness binding above." Not the common route — every one.
2. **The cross-reference excludes the blank route by type:**
   `joint_superselection_route_id: enum {"EJ-TRIVIAL","EJ-NONTRIVIAL"}`. A filled pairing cannot
   cross-reference an absent `E_joint`.
3. **`EJ-BLANK` states the consequence itself:** "no `Delta_0^joint` or pairing faithfulness
   certificate can complete."
4. **§10 CONDITIONED-ON carries it:** "any filled pairing certificate binds a nonblank `E_joint`
   through the defined `Delta_0^joint` space."

And the cross-reference does not merely assert the space — it pins the defining bytes by source
digest, span, and span digest, and then requires that the record "names both the `Tr_joint` carrier
and the `E_joint` carrier on which that space is defined". The certificate is bound to the definition,
not to the name of the definition.

**FINDING 2 = CLOSED (space defined).**

*Note on notation, not a defect:* the record writes `delta0_definition_span: const [14222,14296]`
while the corpus convention is half-open. The digest matches the **half-open** reading, and the
co-pinned `delta0_definition_span_sha256` disambiguates any replayer who reads it the other way. The
pin is self-checking. An explicit half-open note would remove the need to notice.

## 4. Carriage — the 33-hunk table [PROVABLE]

The instrument declares its diff convention exactly: UTF-8, LF, final LF included, minimal unified
diff with zero context (`diff -d -U0`), one hunk per `@@` header. **That declaration is what makes
the count checkable, and it is the right call** — my own default diff over the same two files yields
34 hunks, purely from context granularity. Replaying the declared convention:

```text
diff -d -U0 V001 V002 | grep -c '^@@'   ->   33        V002 claims DIFF_HUNKS = 033   MATCH
```

Every one of the 33 hunk line-spans matches the table row by row — H01 at line 7 through H33 at
534-538, including the four large insertions H11 `[118,204]`, H12 `[221,244]`, H19 `[301,326]` and
H31 `[485,528]`. Every hunk carries an authorization: carriage, finding 1, finding 2, or stage ground.
**No hunk is unassigned, and no hunk is a pure deletion** — every change is an addition or a
replacement, so nothing was silently dropped.

The confirmed-sound surface is byte-identical where it should be. Spot-verified:

```text
V002 lines 327-396 (blind fiber, i_src, I_A, P_ch sections) verbatim in V001   TRUE
V002 lines 205-220 (pairing families table region)          verbatim in V001   TRUE
member09 TENS/EMB, S-OBJ0, S-JP0, S-RANK1 carried spans                        5/5 MATCH
```

**CARRIAGE = VERBATIM-CONFIRMED.**

## 5. The stage-rule ground pin [PROVABLE]

`StageGrammarGround.v001` pins the construction artifact `438e6c42...` and the payload
`986cc0585d75...`, both of which I verified independently at 935, with
`status: const "PINNED_AS_GROUND"` and `principal_decision_field: const false`.

**Correctly non-decision.** Q-839 records the stage-rule stop overturned because a ratified family
exists of record, and my own 935 re-derivation found every element of the stage system forced by the
sealed spans. The field derives; it is not a principal freedom. The pin also refuses to let the ground
do more than ground: "No stage cutoff, finite inventory, candidate, `E_joint`, or fiber is inferred
from it" — which is precisely the over-reach the ground could otherwise invite.

**STAGE_PIN = CORRECT.**

### 5.1 One correction of mine propagates here [PART-PROVABLE]

Under the correction-propagation law I name the downstream consumer. At 935 I found that the payload
digest `986cc058...` **verifies against its displayed line but is not independently regenerable from
the record**, because §2.2 of the construction declares no serialization grammar; and that the
payload's limit field names `R_inf`, which appears in neither span its own `sources=` field pins.

V002 const-pins that digest. This does **not** make the stage pin wrong — the pin is a correct pin of
a correct non-decision, and nothing in V002 depends on regenerating the bytes. But the registrar
should know that the pinned value cannot presently be reproduced by an independent lane, and that the
fix lies in the construction rather than in this instrument.

## 6. Advocacy and blind routes [PROVABLE]

**Advocacy — independently scanned over twelve terms across the whole document: 4 hits, 0 advocacy.**
One at line 79 quoting the reverse-A2 clause's "preferred history measure"; two at line 446 inside the
instrument's own anti-advocacy sentence ("No route is selected, ranked, recommended, called cheaper");
one at line 481 in the self-verb audit ("No option receives a verdict or preference"). Every hit is
inside quoted or self-auditing text. V002's claim is stated precisely as
`ADVOCACY_TERM_HITS_OUTSIDE_AUDIT = 0`, and that is **CONFIRMED**.

**Blind routes, re-audited by endpoints** — by the extents a principal would actually seal, since
walking a blind route would breach the thing under test:

```text
IA-STATUS         member 10 [781,794)      MATCH   rank-shaped tokens: NONE
PCH-STATUS        member 10 [884,898)      MATCH   rank-shaped tokens: NONE
PCH-TUPLE ctx     member 11 [23013,23148)  MATCH   rank-shaped tokens: NONE
PCH-TUPLE member  member 11 [23081,23085)  MATCH   rank-shaped tokens: NONE
```

All four clean of `r_0`, `r_ch`, rank, dimension, ratio and `p_[A]`. Every blind field remains
enterable from opaque source+span+digest pointer records without opening anything rank-shaped. The
ninth field added no blind surface. **BLIND ROUTES CONFIRMED.**

## 7. The one defect returned [PROVABLE]

**§11's byte-audit block reports a stale closure end.**

```text
V002 ACTUAL closure end                     = 4906
V002 §13 final line states                  = 4906     correct
V002 §11 byte-audit block states            = 3926     stale — this is V001's value
V001 actual closure end                     = 3926
CLOSURE_END_BYTE sits at V002 line 470, which falls in NO hunk of the 33
```

The closure grew when the member census went from 20 to 26, so the value had to move. §11 line 472
(`FIELDS_COVERED = 9`) was updated by H28; line 470 two lines above it was not. The section's own
preamble asserts "Byte audit values are filled from the final artifact bytes after this text is
complete", so the block claims a property one of its five values lacks, and the document states two
different closure ends in two places.

**This does not block the round, and I want to be explicit about why**, because the temptation to
convert any finding into a block is exactly what the symmetric obligation guards against:

- The closure itself is **correct** — declared first, opening at byte 0, ending where §13 says, with a
  zero-hit scan. The binding discipline holds; only a reported number is stale.
- The defect is in the registrar-facing audit block. It touches no receiver, no option, no price, no
  certificate rule, and no panel line. **No principal act can be corrupted by it.**
- Measured against the bar the two 932 items set — each of which would have produced a defective
  principal act — this does not come close.

Returned as a clerical correction for in-place fix, ideally with §11's other four values re-derived
from final bytes at the same time.

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the nine schema receivers and the reverse-A2 receiver at their sealed spans;
  the Delta_0^joint defining bytes;
  the frozen round-1 act, its freeze clause, and both enum sites;
  my own 930 labelling spans and 935 stage-rule findings;
  the pinned stage-rule ground.

CONDITIONED-ON:
  the forcing rule of section 3.2 governs the round-2 entry independently of the disposition record;
  the corpus half-open span convention, which the co-pinned span digest confirms.

SUBSTITUTED:
  NOTHING. I selected no route, no pairing, no E_joint, no fiber, no pointer site, and no enum.
  I adopted nothing, registered nothing, and entered nothing. Zero principal fields are filled.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

## 9. Flattening, custody, byte audit

- **S01-S37 FLATTENING CHECK:** walked. A disposition was not identified with an edit of the frozen
  act. A pinned definition was not identified with a defined space until the binding was traced
  through every route. A matching hunk count was not identified with verbatim carriage — the spans
  and the carried text were checked separately. A clerical staleness was not identified with a
  structural closure failure. A returned item was not identified with a blocking item.
- **F_PLDEC:** schemas, digests, span arithmetic and option relations only. No physical quantity was
  evaluated; no measured constant consulted.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string and byte-span checks covered
  `joint_superselection_map`, `E_joint`, `Delta_0^joint`, `FrozenEnumDisposition`,
  `PairingEJointCrossReference`, `StageGrammarGround`, `CLOSURE_END_BYTE`, and every receiver name.
- **BLIND:** held. No rank read, no dimension read, no ratio formed or quoted, no fiber compared or
  opened. Blind routes were audited by endpoint extents only.
- **PE-1..PE-13:** pointer-only, zero verdict weight, recorded and not consulted.
- **BR-1:** no producer-declared object carried a criterion's direction. The instrument's own claims
  located what to test; every disposition was checked against sealed bytes or an executed replay. My
  own 932 and 930 were used as the standard to be met, never as confirmation that it was met.
- **BUILDER-NEVER-VERIFIES:** I verified the opposite lane's revision. I verified nothing of mine —
  where my 935 finding bears on the stage pin, I reported the propagation rather than re-adjudicating
  my own result.
- **CHAIN:** no anchor act, member binding, fixed-point execution, end test, gauntlet, numerical
  evaluation of a physical quantity, or comparison with a measured constant was invoked.

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 3412
PREDECLARATION_OUTPUT_SCAN = 0 hits
DIGESTS_RECOMPUTED = 31/31 MATCH
HUNKS_REPLAYED = 33/33 under the declared convention, all assigned
RETURNED_ITEMS_CLOSED = 2/2
DEFECTS_RETURNED = 1 (clerical; non-blocking)
PRINCIPAL_ENTRIES_FILLED = 0
```

Self verb audit: "closed" applies to the two 932 items, each tested against the sealed bytes and the
document's own forcing rule. "Confirmed" applies to the carriage, the blind endpoints, the advocacy
count and the stage pin, each with its check displayed. "Returned" names one clerical defect, shown by
computation and expressly judged non-blocking against a stated bar. No route, option, price or enum
received a preference, and no field was entered. `VERB_AUDIT_SELF = CLEAN`.

## 10. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 3412; scan 0 hits)
FINDING1 = CLOSED (mechanism lawful). FrozenEnumDisposition.v001 is a closed tagged union whose four arms exhaust the forcing rule's cases; all three branches tested — carriage with downstream_enum_authority NONE on a missing certificate, re-warranting on a certified H-OTHER only, prospective supersession with ROUND2_ONLY authority on certified H-HAAR/H-DIRAC. The frozen act is not mutated: round1_bytes_mutated const false, post-freeze mutation const FORBIDDEN, the round-1 value carried as const, and the disposition layered as a mechanical field of the round-2 record. Both round-1 grounds and all three enum-site spans verified. The contradiction is represented rather than edited away — two immutable acts stand, only the round-2 certified value carries downstream authority
FINDING2 = CLOSED (space defined). The demand is pinned at member 07 [10330,10364) and the Delta_0^joint bytes at [14222,14296), both recomputed; both my 930 labelling spans rehashed independently and MATCH, with the substantive conditional quoted byte-exactly and EJ-TRIVIAL disclosing that it is barred on a SUBSTANTIVE labelling; EJ-TRIVIAL/EJ-NONTRIVIAL/EJ-BLANK each priced and exhaustive on a total axis. The faithfulness condition is on a DEFINED space in EVERY pairing route, closed in four independent places: section 3.2 quantifies over all nine filled combinations, the cross-reference enum excludes EJ-BLANK by type, EJ-BLANK states its own incompleteness, and the CONDITIONED-ON block carries it — and the cross-reference pins the defining bytes rather than the name
CARRIAGE = VERBATIM-CONFIRMED (33/33). The declared convention diff -d -U0 replays to exactly 33 hunks and every table line-span matches row by row; declaring the convention is what made this checkable, since my own default diff granularity gives 34. Every hunk assigned; no pure-deletion hunk; sections 5-8 and the pairing-table region verbatim in V001; all five carried i_src spans MATCH
STAGE_PIN = CORRECT (pinned as ground, principal_decision_field const false; both pinned digests verified at 935; the ground is expressly barred from implying a cutoff, inventory, candidate, E_joint or fiber). PROPAGATION NAMED: my 935 finding that the payload digest verifies but is not independently regenerable, and that its limit field names R_inf absent from both pinned spans, travels to this consumer — the fix lies in the construction, not here
ADVOCACY = 4 hits, 0 advocacy (one quoting the reverse-A2 clause, two inside the anti-advocacy sentence, one in the self-verb audit); V002's precise claim ADVOCACY_TERM_HITS_OUTSIDE_AUDIT = 0 is CONFIRMED. BLIND ROUTES CONFIRMED: all four principal-sealable extents MATCH and are clean of rank-shaped tokens; the ninth field added no blind surface
VERDICT = READY-FOR-THE-PRINCIPAL. Both returned items are closed on their merits, the confirmed-sound surface is carried byte-unchanged, and 31/31 digests verify. ONE DEFECT IS RETURNED AND EXPRESSLY DOES NOT BLOCK: section 11's byte-audit block states CLOSURE_END_BYTE = 3926, which is V001's value, while the actual closure end is 4906 and section 13 says so — line 470 falls in no hunk, and the block asserts it is filled from final bytes. The closure discipline itself holds; only a reported number is stale, in a registrar-facing block that touches no receiver, option, price or panel line, so no principal act can be corrupted by it. Measured against the bar my two 932 items set — each would have produced a defective principal act — it does not come close. Fix it in place
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
