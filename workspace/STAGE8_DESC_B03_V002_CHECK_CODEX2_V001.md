CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY
PREDECLARATION_OUTPUT_SCAN = 0 hits (empty byte range)
CLOSURE_END_BYTE = 00000613
LANE = CODEX 2
ROLE = B03 V002 OPPOSITE-LANE CHECK
SUBJECT = STAGE8_DESC_B03_DARIO_V002.md
SUBJECT_SHA256 = 4ffe99b2e8160a53db03dea69f229c62992d28def6ab2e11c9487e10157fcbb4
GENERATOR_SHA256 = 5d6ef64f105a54cf8cd7322c74b3773e7b603c1d67110782b7c1104bb9bd8d1f
V001_SHA256 = 581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506
RESULT_STATUS = OPPOSITE-LANE CHECKED
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT B03 V002 CHECK — CODEX 2 — V001

Date: 2026-08-11  
Status: **CORRECTED.** The six-leg record restatement, the exactness of `res_B`, the L4/L5
dispositions, the L3 absence, and the documentary carriage all stand. The load-bearing new inference
does not. The sealed record does not identify `A_SRF_CTP` with `A_C0`; it records the former as the
codomain of the older completed-carrier declarations and the latter as the entered tensor carrier,
and requires an additional typed identification/embedding between them. Therefore an L3 state is
not, on present sealed grounds, an input to `res_B`. The completed-object gap remains, while
`omega_phys` remains **OPEN as a selector/value** rather than dissolved.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

## 1. Preflight, custody, and executable grounding

The subject, generator, and carriage base matched their adjacent seals before semantic reading:

| object | bytes | SHA-256 | result |
|---|---:|---|---|
| `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_DESC_B03_DARIO_V002.md` | 36,519 | `4ffe99b2e8160a53db03dea69f229c62992d28def6ab2e11c9487e10157fcbb4` | MATCH |
| `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/build_b03_v002_pins_v001.py` | 9,909 | `5d6ef64f105a54cf8cd7322c74b3773e7b603c1d67110782b7c1104bb9bd8d1f` | MATCH |
| `/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_DESC_B03_DARIO_V001.md` | 74,533 | `581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506` | MATCH |

The generator was first invoked from the cleanroom root and refused, as written, because its declared
paths are archive-root-relative and member M01 is not resident in the cleanroom inbox. It emitted no
table. Invoked read-only from its stated archive root, it completed with `FILES=26`, `SPANS=25`, and
`FIXED-REVERIFIED=16/16 MATCH`. The static cited records matched their seals. M22 is the declared
live-append register: its full-file digest advanced, while the generator recomputed the Q-212 and
Q-223 anchor spans at the pinned bytes and digests. This is the assignment's live-append case, not a
content substitution.

The subject's own closure is structurally exact: it starts at byte `0`; the end marker begins at byte
`7146` and ends at byte `7170`; the predeclaration range is empty, so its 27-token scan has zero
possible hits.

## 2. The identity that decides the target

### 2.1 The two objects are separately typed

The older producer signature names its completed object at the sealed span
`STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md [8085,8283)`, span SHA-256
`375dd96a7b7c3806c063075f7346685efa8fbd8c1eddab9bbb19c5e723157d6d`:

```text
P1. A completed source-record-field carrier/algebra A_SRF_CTP with a common
    dense domain or equivalent domain object.
```

The C0 algebra trace instead declares, in
`STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md [10436,11034)`, span SHA-256
`b9c7a355890def386696ac4a075b1da79420d2b03e1f0d026e83983b5e18566d`:

```text
A_SR := A_src graded-tensor_min R_inf,
B    := A_F_CTP isomorphic to C(Y),
A_C0 := A_SR graded-tensor_min B,
...
i_B : B -> A_C0,
i_B(b) := 1_A_SR tensor b.
```

No sealed equality or isomorphism identifies those codomains. The point is not merely an unsuccessful
search: two later sealed custody records state the missing bridge positively.

- `STAGE8_AXN_ENTRY_CANDIDATE_SURVEY_DARIO_V001.md`, sealed SHA-256
  `4440af4f8d4a75c7e4b026d4fd901e4e290676f928275897bb55ce803b4c03db`,
  `[7028,7663)`, span SHA-256
  `43f3129a8b5059caa7979ab9ab0274296ec49c0cbd995c1785ab66d087af764d`, records that all three
  `i_src` declarations land in `A_SRF_CTP`, not `A_C0`, and that treating the carriers as the same
  requires an identification rather than a transcription.
- `STAGE8_AXN_ENTRY_DECISION_INSTRUMENT_CODEX2_V002.md`, sealed SHA-256
  `af26ab0354420f64718942b9bdcc61a4e6826a885b7ac0440988a25d7f0c95e1`,
  `[10322,10545)`, span SHA-256
  `602ab0bff8d0d3d442271fe0850a6141f15fc1f139acb5b0311f13f63eaa26ff`, closes the `joint i_src`
  row as `EMPTY`: the declarations land in `A_SRF_CTP`, member 09 declares `A_C0`, and the required
  next act is a carrier identification plus typed embedding or a new typed embedding.

This is a two-object record with an absent transport. It directly triggers Dario's own declared
press condition at the end of V002.

### 2.2 Consequence for the dissolution

`res_B` accepts `State(A_C0)`. L3 demands a state on `A_SRF_CTP` / the full source-record-field
Hilbert space. Without an algebra arrow between those carriers, no contravariant state map is typed;
there is no lawful composition from the L3 state into `res_B`. A future sealed identification could
change this, but one is not present and this relay authors none.

Therefore:

```text
L3 OBJECT                     State(A_SRF_CTP)
EXACT RESTRICTION DOMAIN      State(A_C0)
SEALED TRANSPORT              absent
omega_phys CONSEQUENCE        not determined from L3 by the present record
SELECTOR STATUS               OPEN, not dissolved
```

## 3. Restriction exactness, attacked

The exact record span is `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md [14039,14411)`, span SHA-256
`438e92aecb461348646cdb13c4faec51fa445b68aeee0bf5d11717078a869341`:

```text
res_B : State(A_C0) -> State(B),
res_B(Omega_C0) := Omega_C0 compose i_B.
```

For positive `b=c* c` in `B`, the star-homomorphism gives
`i_B(b)=i_B(c)* i_B(c)`, positive in `A_C0`; a positive `Omega_C0` therefore makes
`Omega_C0(i_B(b))` positive. For normalization,
`res_B(Omega_C0)(1_B)=Omega_C0(i_B(1_B))=Omega_C0(1_A_C0)=1`.

The two proposed failure modes do not land:

- **Non-unitality:** the same sealed carrier span declares `i_B` canonical, unital, injective, and
  gives `i_B(1_B)=1_A_C0`.
- **Grading:** the embedding is `1_A_SR tensor b`; the unit is even, so no graded sign alters the
  star or unit identities. More importantly, the sealed source already types the canonical arrow as
  a star-homomorphism.

Exactness is thus confirmed **on its typed domain**. The correction is applicability, not the map.

## 4. Six-leg demand restatement

The generator independently reproduced all five demand spans. Each leg is read from older receivers:

| leg | older sealed ground | check |
|---|---|---|
| L1 positive `rho_pre` | producer P5 `[8954,9569)` `8917c67f...`; v004 `[7290,7829)` `f9fb7a84...` | record-read |
| L2 normalized / trace-class / unit trace | same P5 and v004 spans | record-read |
| L3 completed `A_SRF_CTP` / full SRF Hilbert space | producer P1 `[8085,8283)` `375dd96a...`; P5; v004 | record-read |
| L4 effects/domains/evolution/identity/contacts | P5 plus producer P6/P7 in `[8954,9569)`; B0 U2 role `[37523,38115)` `e16f8b94...` | record-read |
| L5 `d_U2 : (B0_candidate,C0) -> U2` | substitute-admissibility `[6266,6883)` `e6093b0f...` | record-read |
| L6 one microscopic source / no supplementation | producer P5, same `[8954,9569)` span | record-read |

The three whole older receiver files contain `rho_pre` but no `omega_phys`, `StatePort`,
`H_omega`, `pi_omega`, `state_descent_map`, `state_provenance_certificate`, or `choose or derive`
surface. The StatePort packaging is not imported into any leg. The restatement is clean.

## 5. The eleven consumers under the corrected target

Dario's re-disposition assumes that every completed pre-state can feed `res_B`. Section 2 shows that
assumption is untyped. Consequently the `PRE-STATE` label does not by itself discharge a consumer:
it must name the carrier on which the pre-state lives and a map to the consumer's carrier.

K4 is decisive without guessing. The sealed Arm A span
`STAGE8_DESC_DIAG_A_DARIO_V001.md [21194,21565)`, SHA-256
`e76be5c6f0536f2573c79f5d02a46e94f497cac5e87b744613ccf9c0636d011d`, says K4 requires an actual
`omega_hist`, hence an actual projective finite base family. On the present record, an L3 state on
`A_SRF_CTP` does not provide that value. Supplying K4 therefore still requires an independently
supplied `omega_hist`/state value or a new carrier transport. Neither is in V002.

The same receiver defect infects every row V002 moved to `PRE-STATE(-AND-MEASURE)`: a state on the
wrong algebra cannot, by adjective alone, inhabit B04, B06, B09, B13, conditions 1/7/13/18/35, K7,
or K4. This check need not inflate the count or choose a replacement state. One counterexample is
enough: K4 refutes `STILL-REQUIRES-INDEPENDENT-CHOICE = 0`.

## 6. L4 and L5 under attack

### 6.1 L4 remains not supplied

DoR-013's sealed adoption span `[355,992)`, span SHA-256
`bc83e53db7e46f27b570b1492ebd9ea53f168c344e1b06d1b82b45bb36a00750`, adopts `d_state`,
`d_ready`, and `d_law` at family level. It does not instantiate admitted record effects with their
domains, the joint action/evolution, source-contact rules, or common physical domains demanded by
P5/P6/P7 and the U2 signature.

The widest available supply audit agrees at
`STAGE8_DESC_AUDIT_CODEX2_V002.md [13466,13637)`, span SHA-256
`bf3a8d85e13017b29ec0e3bf1e89f00fd843247fa479200552d7598fb2d2affb`: U2 is `MATERIAL-PARTIAL`, with no complete
StatePort, contacts, effects, common physical domains, or total B0-domain map. Narrower finite/source
pieces do not satisfy the joint receiver. L4 stands.

### 6.2 L5 remains absent

`STAGE8_DESC_AUDIT_CODEX2_V002.md` row `d_U2` at bytes `[17351,17582)`, span SHA-256
`89c5b573ec731ce8f1d6c86d4996fbc3b0c3b79a91e7968b44b78621ca575643`, records finite
state/effect/domain/dynamics legs and source-law pieces, but no complete StatePort/contact/common-
domain output, and disposes the exact required signature as
`MATERIAL-PARTIAL / REQUIRED MAP ABSENT`. B02's G2 span `[32608,33541)`, SHA-256
`5575f6ba332dc8a73083e46b0f8138b235b0a6246333de6173051d26c1b7637b`, independently stops at the
same total right-domain map. Premise-level partial arrows do not instantiate
`d_U2 : (B0_candidate,C0) -> U2`. L5 stands.

## 7. L3 gap and survival through the reframe

The absence is stated at the same old receivers that create the demand:

- producer P1 `[8085,8283)` names the completed `A_SRF_CTP`;
- producer P5 `[8954,9569)` places `rho_pre` on that completed object;
- v004 `[7290,7829)`, SHA-256 `f9fb7a84...`, requires the full source-record-field Hilbert space and
  states that the completed Hilbert space, physical domain, and common-origin producer are absent.

The forced `d_state` remains a valid positive normalized state at source-sector scope, not a joint-
state certificate. Arm B's opposite-lane cross-check §2.3 records producer P5 as one of the three
older signatures that survives removal of the authored StatePort route and packaging. Thus the L3
obstruction predates that frame. The identity correction does not weaken the gap; it blocks the
attempt to route around it.

## 8. Carriage, folds, and custody

V001 is byte-untouched: its current sealed bytes remain exactly
`581ec505441b88b8a4d33d875ddc2d1d29ef8291fd140c4901a07a5513ac2506`, 74,533 bytes.

The four ordered narrowings are all present and correctly folded:

1. **Dependency:** member 16 `[6680,8157)` `ba3c5741...` is one exhaustive census; member 17
   `[15554,15965)` `e9dafa59...` is a verified extension theorem that imports its receiver, not a
   second independent proof.
2. **Five unpublished pins:** S05, S16, S22, S23, and S34 are displayed in §7, and the generator
   reverified all five byte spans and hashes.
3. **Property P:** V002 labels P `COMPOSED`, separating the quoted collapse core
   `[18972,19937)` `a8e6fff4...` and residual package `[18260,18656)` `349938c5...` from Dario's
   five-part assembly and record-forcing clause.
4. **Residue typing:** V002 says seven live-receiver occurrences plus one meta-label, not eight
   occurrences of one class.

The custody change is visible at both ends: closure member 03 opens the 1024 opposite-lane check as
an input, while §9 and the final postscript keep V002's results `CLAIMED until the opposite-lane
check` and identify the exact step to press. Nothing in V001 was edited or silently withdrawn.

## 9. Fresh residue scan

I used five over-generating families, case-insensitive, over the full subject bytes:

```text
P1 reproduce and all inflections
P2 reconstruct/rebuild/rerun/replay families
P3 byte-identical/identical-bytes/same-bytes/verbatim
P4 remove/delete/nothing-removed families
P5 set-delta/diff-triple/membership-movement/row-continuity
```

Eight distinct raw loci result:

| subject byte | class | disposition |
|---:|---|---|
| 1550 | `BYTE-IDENTICAL` | live-register anchor stability, digest receiver displayed |
| 5910 | same claim in member table | same bounded receiver |
| 12730 | `removed` | semantic removal of route/packaging, not file/process history |
| 17052 | same semantic removal | content statement |
| 28506 | `removed-class` | scan meta-language |
| 28609 | `reproduce` | Dario's self-naming raw hit |
| 29004 | `re-runs ... identical` | explicit negation of the retired claim |
| 33703 | semantic removal repeated in final result | content statement |

No locus asserts artifact reconstruction, a historical rerun certificate, deletion carriage, or
membership movement. Dario's narrower verb scan is exactly one raw hit; the draft miscount is openly
corrected. The broader opposite-lane scan is `8 RAW / 0 OPERATIVE` for the retired process-history
class.

## 10. Flattening, blind custody, and consequence

- **FLATTENING:** an exact map was not flattened into an applicable map; two carrier names were not
  flattened into an identity; satisfaction at source scope was not flattened into satisfaction at
  joint scope; partial arrows were not flattened into `d_U2`; a documentary correction was not
  flattened into an L3 refutation.
- **F_PLDEC / BLIND HELD:** only sealed paths, bytes, hashes, types, symbolic algebra arrows, and
  textual receivers were inspected. No physical quantity, measured constant, response, rank, ratio,
  or downstream fitness was formed, evaluated, or compared.
- **PE-1..PE-17:** pointer-only and zero verdict weight. `omega_phys` was not constructed, chosen,
  frozen, evaluated, or compared. No member was bound.
- **CUSTODY:** this check writes only this cleanroom artifact, its adjacent seal, and the required
  outbox completion report. No register, plan, tracker, git, commit, push, chain execution, end test,
  or subject edit occurred.

The corrected joint statement is:

```text
L3 GAP                         CONFIRMED
res_B                          EXACT ON State(A_C0)
A_SRF_CTP -> A_C0 TRANSPORT    ABSENT
omega_phys SELECTOR            OPEN
ZERO-CHOICE CENSUS             REFUTED BY K4
```

## 11. Final lines

```text
CLOSURE = declared-first (byte 0; end 613; empty predeclaration scan = 0 hits; subject independently 0..7170)
IDENTITY = TWO-OBJECTS (transport absent; dissolution falls to OPEN)
RESTRICTION = EXACT-CONFIRMED
LEGS = 6/6-RECORD-READ
CONSUMERS = K4 REQUIRES AN INDEPENDENT omega_hist OR A NEW TRANSPORT; ZERO-CHOICE REFUTED
L4_L5 = STAND
L3_GAP = CONFIRMED-AT-SPANS
CARRIAGE = CLEAN
RESIDUE_GREP = 8-RAW / 0-OPERATIVE (own patterns; Dario's one-hit verb scan exact)
VERDICT = CORRECTED (L3 gap stands; selector dissolution and zero-choice census do not)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
