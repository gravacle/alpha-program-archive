# STAGE 8 TASK 5 / EQ6 — FLIP SECTION CHECK AND SCOPED-WITNESS DETERMINATION — LANE 2 V001

Date: 2026-08-04  
Lane: Codex Lane 2  
Task: Paste 523 / Task 5 / EQ6  
Custody: adversarial verification and scoped-witness determination

## Lead result

```text
FLIP_SECTION = CONFIRMED

N1 = PASS_WITH_SCOPE_PRECISION |
  the flip-generated physical zero-defect section and its common-refinement
  equalizer are genuinely inhabited on I_flip; the proof uses actual
  realization stabilizers, fixed DoR-019/Q-408 data, and complete covariance
  orbits, not an abstract direct sum or a selected cycle basis.

N2 = PASS |
  A4_Q408_LOCAL_EXCISION_NATURALITY_ON_GENERAL_PRIMITIVES is the exact
  remaining general-primitive interface; the asymmetric positive-form
  countermodel blocks derivation from current stock but proves no
  impossibility.

N3 = KILL_OF_SCOPED_CERTIFICATION / NEEDS |
  Eq_flip is not a six-generator J1-J15 term, and I_flip is not an
  authorized replacement for DoR-020's adopted primitive domain.

N4 = PASS |
  the nine regressions, finite falsifiers, anti-tuning audit, and three
  fresh promotion attacks all close at the stated scope.

SCOPED_WITNESS = NEEDS(
  SCOPED_SIX_GENERATOR_J1_J15_EQUALIZER_TERM_ON_I_flip
  + DOR020_SCOPE_ADMISSIBILITY_AND_FULL_FINITE_BOTTOM_CERTIFICATE)

SCOPED_EQ6_DISCHARGED = false
CURRENT_ESCROW_CHANGE = none |
  DoR-020's pre-witness prohibitions on member binding, fixed-point execution,
  and the end test remain in force.  Its already-licensed [EQ6]-conditional
  derivations remain licensed and no more.

COUNTERFACTUAL_SCOPED_LICENSE =
  only after both missing certificates (or a principal amendment with the
  same content) would the witness-side prohibition on a bound member be
  removable on I_flip.  Q-448 independently requires a certified C_ret and
  an interval/absolute-continuity branch scope before fixed-point execution;
  no result would thereby extend to asymmetric primitives.  General A4/Q-408
  local-excision naturality would later enlarge the domain and require the
  six-generator/J1-J15 audit again on the enlarged category.

GENERAL_A4_EXTENSION = OPEN / TYPE-U
MACHINERY_APPEAL = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The decisive distinction is between two equalizers.  The reviewed artifact
constructs the zero locus of the **Q-408 refinement certificate** on the
proper physical subcategory `I_flip`.  DoR-020, by contrast, conditions its
escrow licenses on one simultaneous `J1`–`J15` equalizer term over six
irreducible generators.  The first equalizer is real and inhabited.  It is
not the second equalizer, even after restricting the word “witness” to the
flip scope.

---

## 0. Preflight, authorities, and symbol discipline

### 0.1 Preflight

```text
DOES_THE_OBJECT_EXIST = yes |
  the Lane-1 flip section and its claimed equalizer exist;
IS_THE_VERSION_CURRENT = yes |
  register head Q-447 at start;
ARE_ITS_INPUTS_PRESENT = yes |
  artifact, section specification, DoR-020/A1, J1-J15, and the nine
  regressions are present and seal-verified;
PREFLIGHT = PASS
```

`LOCKED_PROCESS.md` was read in full.  The artifact under review was verified
before reading:

```text
STAGE8_TASK5_EQ6_SECTION_DEEPER_CONSTRUCTION_LANE1_V001.md
SHA-256 = a78c2450985429f6c9e152eb52ab6f133b013e1a1b1d695e2cc5241617b4bdb4
SIDECAR = OK
```

Load-bearing sources independently verified:

| Authority | SHA-256 | Bearing content |
|---|---|---|
| record section review | `1fdd8823f046822ae1a23546cf486c5aad7b1c2a438682d5d9313e897de0c56b` | physical zero-defect-section specification and nine regressions |
| DoR-020 | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | joint six-generator witness and escrow licenses |
| DoR-020-A1 | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | complete clause layer; unchanged witness burden |
| `J1`–`J15` constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | exact joint equalizer equations |
| continuum package V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | adopted package carrier/rule scope |
| first physical cycle member | `c0cc95112bf8c213b0e2c4095a06c4e4658fa3950641b9a21fa51950a9f818fa` | nonempty actual two-path member |
| hostile check of first member | `166002e9178faefe4464f504810553a606ec6465a0e3739a70e50a5d29d8604e` | geometry and all-rank boundary |
| Q-448 conditional-chain check | `a6207597484c2f00ad37cebba34b3f135f21bc2e7a7b7a03a9e642ca7e37fe86` | send-time downstream license precision |

Register entries swept: Q-408, Q-411, Q-421/Q-422, Q-430/Q-432,
Q-438 through Q-447.  At send time Q-448 was added and reconciled below.
No settled row turns a proper Q-408 subequalizer into
a joint package witness or authorizes narrowing DoR-020's P4 quantifier.

### 0.3 Send-time Q-448 reconciliation

Q-448 kills the claim that `[EQ6]` alone supplies the independent global
stationary-return certificate `C_ret`, and it requires an interval/convex
`C1` or absolute-continuity branch before a derivative supremum can replace
the true difference-quotient modulus.  This does not alter N1, N2, or the
definition of a DoR-020 witness.  It changes only the counterfactual answer
to “what opens”: a certified scoped witness can remove the witness-side bar
to member binding on an authorized scope, but fixed-point execution still
needs

```text
CERTIFIED_C_ret
+ CONNECTED_INTERVAL_OR_ABSOLUTE_CONTINUITY_BRANCH_SCOPE.        (S0-1)
```

Those are downstream certificates, not additional components of the
scoped `[EQ6]` witness in `(N3-4)`.

### 0.2 Symbols that must not be collapsed

```text
Eq_flip       = zero-defect equalizer for the restricted physical
                Q-408 refinement family;
Eq6           = simultaneous J1-J15 equalizer over six generators;
I_flip        = proper physical refinement subcategory generated by actual
                flip stabilizers;
P4/Ref        = adopted primitive domain, including asymmetric classes;
tau_j         = actual realization automorphism, not a chosen sign of a
                cycle-basis vector;
R_K           = fixed DoR-019 Riesz map, not an adjustable direct-sum metric;
A_G/Phi_G     = fixed Q-408 analysis/isomorphism, not the U(1) connection;
scope         = an invariant domain restriction, not a proof that the
                adopted package already permits that restriction.
```

---

## 1. Verdict table N1–N4

| Item | Verdict | Reason |
|---|---|---|
| N1 flip section and equalizer | **PASS_WITH_SCOPE_PRECISION** | The four defects vanish by actual stabilizer geometry and fixed data; finite compositions and the two constructed diamonds cohere on `I_flip`. |
| N2 exact general stop | **PASS** | A trivial-stabilizer primitive admits a live old/new A4 cross block; the exact missing naturality/excision interface is correctly named, and no impossibility is inferred. |
| N3 scoped witness | **KILL OF CERTIFICATION / NEEDS** | `Eq_flip` supplies only `B_Q408_REFINEMENT|I_flip`; five generators and the simultaneous `J1`–`J15` term are absent, while DoR-020 contains no lawful scope-narrowing rule and full `J15` coverage is unproved. |
| N4 falsifiers and attacks | **PASS_WITH_DECISIVE_NEGATIVE** | The nine regressions pass on the restricted construction; marginal-equalizer and noncofinal-scope attacks refute promotion to a scoped package witness. |

---

## 2. N1 — recomputation of the flip-generated section

### 2.1 Actual flippable geometry and no-selection audit

The first-cycle authority already supplies a nonempty physical family of
two actual future-directed paths with common endpoints and nonzero difference
current.  The deeper construction restricts to those actual positive-source
attachments `r:G->G'` for which there is a realization automorphism `tau_j`
such that

```text
tau_j S_r c = S_r c              for every old cycle c,
tau_j n_j   = -n_j               for the new difference current,
tau_j preserves the surface, support, bundle/connection data,
the A4 source rigging, and the remaining old physical data.       (N1-1)
```

This is not an arbitrary sign assignment.  Conjugating a tuple by any
admitted covariance `U` carries the stabilizer to `U tau_j U^-1`, so the
property of being flippable is invariant.  Retaining every realization,
frame, gauge, orientation/reality image, path pair, and covariance image
with `(N1-1)` gives an orbit family; it does not bind one member or one cycle
basis.  Finite families of disjoint symmetric tubes give commuting
stabilizers `T_m=(Z_2)^m`.  Therefore the restricted family is genuinely
nonempty at every finite rank reached by those additions.

The price is scope, not hidden selection:

```text
FLIP_PROPERTY = COVARIANT_AND_MEMBER_FREE
I_flip_NONEMPTY = true / TYPE-P
I_flip_EQUALS_FULL_P4 = false
```

### 2.2 OLD_FID on the retained old image

For a same-carrier W1 attachment, the current realization is literally the
same A4 vector on the old image:

```text
I_K,G' S_r c = I_K,G c.                              (N1-2)
```

DoR-019 defines the finite K metric by pullback from fixed `g_A4`.  Hence

```text
g_K,G'(S_r c,S_r d)
 = g_A4(I_K,G' S_r c,I_K,G' S_r d)
 = g_A4(I_K,G c,I_K,G d)
 = g_K,G(c,d),                                      (N1-3)

S_r^* R_K,G' S_r = R_K,G.                           (N1-4)
```

Thus `Def_fid(r)=0`.  This proof is correctly restricted to actual
same-carrier current-preserving W1 arrows; it does not promote W3 to a
general cycle-creating isometry.

### 2.3 Relative orthogonality from the physical flip

DoR-019's automorphism-isometry theorem and `(N1-1)` give, for old `v` and
new `n_j`,

```text
g_K,G'(S_r v,n_j)
 = g_K,G'(tau_j S_r v,tau_j n_j)
 = g_K,G'(S_r v,-n_j)
 = -g_K,G'(S_r v,n_j).                              (N1-5)
```

Therefore

```text
g_K,G'(S_r v,n_j)=0.                                (N1-6)
```

Independent characters give the same result between separately created
new sectors.  Together, `(N1-4)` and `(N1-6)` prove the required local
old/new block structure without imposing global orthogonality on unrelated
pre-existing sectors.  The Q-432 `P=id` counterexample remains admitted.

### 2.4 Q-408 local test transport

For an old local test `a` in region `O`, Q-408 supplies

```text
(A_G a)(c)=J_c(a).                                   (N1-7)
```

On a disjoint addition, support gives `J_n(a)=0`.  At an admitted contact,
replace a target extension by its average under the actual stabilizer.  The
old current class is fixed, the support remains in the mapped old/contact
region, and the odd new current pairs to zero.  Thus an actual support-local
extension `i_O a` satisfies

```text
A_G'(i_O a)=S_(r,*) A_G(a).                         (N1-8)
```

Using the fixed old Riesz identity and cross-block zero,

```text
Phi_G'(i_O a)=S_r Phi_G(a),
P_r(a):=Phi_G'^(-1) S_r Phi_G(a)=i_O a.              (N1-9)
```

Hence the local-range defect and the relative no-leakage defect vanish.
The four physical defect rows are

```text
Def_fid  = 0 by (N1-4),
Def_orth = 0 by (N1-6),
Def_leak = 0 by (N1-6),
Def_supp = 0 by (N1-9).                              (N1-10)
```

No fixed metric, current, or Q-408 map is replaced after the test.

### 2.5 Composition and actual diamonds

For disjoint additions, the stabilizers commute and all current, bundle,
metric, and local-test data factor by the actual disjoint tubes.  Adding
`U` then `V` or `V` then `U` therefore gives the same union and the same zero
defects.

For endpoint contact, the interiors are disjoint and the closures meet only
at the recorded endpoint.  OLD_FID remains `(N1-4)`, exclusive-region RNL
is `(N1-6)`, and stabilizer-averaged test transport gives LR by `(N1-9)`.
The requirement set remains correctly scoped:

```text
D_SUBDIVISION = PASS / ADMITTED
D_DISJOINT_FLIP = PASS_UNCONDITIONALLY
D_CONTACT_FLIP = PASS_UNCONDITIONALLY
D_CONTACT_SET = OLD_FID + EXCLUSIVE_REGION_RNL + LR + W4 + LOE6. (N1-11)
```

Because finite composition is set union of the actual local additions and
the commuting stabilizers make the two routes identical, the compatible
zero-defect terms form a nonempty common-refinement equalizer:

```text
Eq_flip != empty.                                    (N1-12)
```

This confirms the reviewed TYPE-P claim exactly on `I_flip`.

---

## 3. N2 — the general stop and route-end audit

### 3.1 Failure-capable asymmetric primitive

Let an otherwise admissible cycle-creating primitive have asymmetric path,
support, or bundle-field decoration, so its actual stabilizer does not
reverse the new current.  On the old/new two-dimensional block take the
fixed target form

```text
R_K,G' = [[1,epsilon],[conj(epsilon),1]],
0 < |epsilon| < 1.                                  (N2-1)
```

It is positive, Hermitian/reality-compatible, unit-correct, and invariant
under a trivial stabilizer.  Its old block still obeys OLD_FID.  For an old
local vector with nonzero coefficient `theta`, however,

```text
Def_leak = theta epsilon != 0,                       (N2-2)
```

and the geometric old projector differs from the fixed-metric orthogonal
projector.  Current authorities neither forbid `(N2-1)` nor force
`epsilon=0` without a physical sign-reversing stabilizer.  A diagonal
metric with a support-nonlocal Q-408 analysis map independently shows that
metric orthogonality alone would not prove LR.

The zero-defect flip members and this nonzero-defect asymmetric member give
the correct two-sided result:

```text
GENERAL_ZERO_DEFECT_NONEMPTINESS_NOT_DERIVED = true / TYPE-R
GENERAL_ZERO_DEFECT_IMPOSSIBLE = false / TYPE-R
```

### 3.2 Exact missing primitive

The full primitive family needs one interface:

```text
A4_Q408_LOCAL_EXCISION_NATURALITY(r):
  (a) old-image A4 isometry on every primitive horn;
  (b) relative old/new A4 orthogonality on every active support pair;
  (c) Q-408 local-analysis excision
        Phi_G'^(-1) S_r Phi_G(Tbar_G(O))
          subset Tbar_G'(F_r(O));
  (d) covariance, restriction, composition, and actual
      common-refinement coherence of (a)-(c).         (N2-3)
```

This is missing physical surface content.  The certificate equations are
rails that test `(N2-3)`; they do not manufacture it.

### 3.3 Route-ended claim

The current routes exhaust as follows:

| Route | Exact yield |
|---|---|
| same-carrier current identity | OLD_FID only on the same-carrier W1 scope |
| W3 | rank-preserving isometry only |
| covariance | moves a defect through its orbit; cannot force zero |
| actual automorphisms | builds `I_flip`; absent on asymmetric primitives |
| support of path currents | proves local transport on constructed split geometry, not against an arbitrary fixed Riesz mixer |
| direct-sum model | proves consistency but is not the retained physical data |
| finite induction | remains inside `I_flip`; cannot add an asymmetric primitive |

No ratified map acts on `(N2-3)`.  The claim that the constructive route is
ended on current stock therefore survives.  It is not an impossibility
theorem and does not license a declaration or domain narrowing.

---

## 4. N3 — scoped-witness determination

### 4.1 What DoR-020 calls a witness

DoR-020 is explicit: a certified witness is one term in the simultaneous
fiber product over these six irreducible generators,

```text
B_R1_NATURAL,
B_Q408_REFINEMENT,
B_C1_COMPLETION,
B_FAITHFULNESS,
B_C2_RESPONSE_BOUNDARY,
B_C3_MAXWELL_HODGE,                                (N3-1)
```

with every `J1`–`J15` equation holding on the same tuple.  Separate or
marginal nonemptiness is insufficient.  The amendment adds where-laws but
states that the witness burden is unchanged.

### 4.2 Six-generator audit on the flip scope

| Generator restricted to `I_flip` | What `Eq_flip` proves | Status as an EQ6 generator certificate |
|---|---|---|
| `B_Q408_REFINEMENT` | actual W1/W4 current/bundle members, fixed-kernel local transport, zero defects, composition, and the constructed diamonds at every flip-generated finite rank | **DISCHARGED ON THE RESTRICTED Q-408 ROOT / TYPE-P** |
| `B_R1_NATURAL` | no action representative, Hessian restriction, or reducing-domain naturality is constructed by the flip section | **OPEN / TYPE-U** |
| `B_C1_COMPLETION` | finite components exist, but no one separating topology, family-uniform estimates, attained bounds, or W5 Hilbertization is supplied | **OPEN / TYPE-U** |
| `B_FAITHFULNESS` | finite Q-408 currents remain visible, but no completed lower-bound/separation theorem or safe kernel quotient is supplied | **OPEN / TYPE-U** |
| `B_C2_RESPONSE_BOUNDARY` | no action-derived `Eta`, common-refinement response transport, boundary cocycle, safe ideal, or subextensive theorem is supplied | **OPEN / TYPE-U** |
| `B_C3_MAXWELL_HODGE` | finite Ward behavior is inherited, but no completed local symbol, closed range/domain, Hodge/Maxwell projection, magnetic partner, or normalization is supplied | **OPEN / TYPE-U** |

The inherited finite C1/faithfulness/Ward projections are useful inputs.
They are not full generator terms.  Thus the restricted root count is

```text
SCOPED_GENERATORS_DISCHARGED = 1/6
SCOPED_SIX_FIBER_TUPLE = absent
```

### 4.3 `J1`–`J15` audit

The flip construction supplies the geometric part of `J10`, the adopted
coframe/density/current naturality part of `J11`, and the finite Q-408
projection part of `J15` on its own arrows.  It does not supply:

```text
J1-J3   a physical normalized reader factored through the same kernel;
J4-J7   an admissible action representative, anchored R5 path, Shape-K
        jets, and exact action-reader pairing;
J8-J9   the joint unit and ordered-network equations on that tuple;
J12     completed response naturality Eta;
J13     the boundary/contact cocycle, safe ideal, and subextensivity;
J14     completion continuity plus the faithfulness/annihilator horn;
J15     one completed-to-finite square for every package choice and every
        sealed active finite datum.                         (N3-2)
```

Therefore

```text
Eq_flip != Eq(J1,...,J15)|I_flip.                  (N3-3)
```

### 4.4 Is `I_flip` itself a lawful scope?

As a **physical Q-408 subcategory**, yes:

```text
covariance        = PASS | stabilizers conjugate under full orbit action;
no-selection      = PASS | all members and covariance images retained;
composition       = PASS | finite local additions compose by actual union;
common refinement = PASS | on the constructed disjoint/contact terms;
finite restriction= PASS_WITH_INTERNAL_SCOPE |
  included terms reproduce their exact finite restrictions.
```

As a replacement domain for the **adopted continuum package**, no current
authority says yes.  Two independent gaps remain:

1. DoR-020/A1 adopts P4 over every primitive-arrow isomorphism class and
   expressly leaves the witness burden unchanged.  It contains no rule
   narrowing the package to `I_flip`, and no theorem proves all package
   consumers factor through that subcategory.
2. `J15` requires one bottom square for every sealed active finite datum.
   The reviewed artifact proves internal finite restriction on the included
   flip objects, not that `I_flip` covers or is cofinal for the complete
   sealed finite reference category.

The scope is therefore mathematically lawful but not yet an authorized
package scope.

### 4.5 Exact residue and escrow effect

The exact scoped-witness residue is the pair

```text
SCOPED_SIX_GENERATOR_J1_J15_EQUALIZER_TERM_ON_I_flip:
  one tuple (I,ell,A,GRef,Dens,Eta,Bound,Faith) on I_flip satisfying all
  six restricted generator certificates and J1-J15 simultaneously;

DOR020_SCOPE_ADMISSIBILITY_AND_FULL_FINITE_BOTTOM_CERTIFICATE:
  a theorem or principal amendment that I_flip is an admitted package
  domain, that every scoped consumer closes there, and that J15 covers all
  finite data the scoped theory claims to consume.                 (N3-4)
```

Neither component is present.  Consequently `Eq_flip` does **not** certify
as a scoped `[EQ6]` witness and changes none of DoR-020's escrow locks.

If `(N3-4)` were later supplied and certified, DoR-020's witness-side bar to
binding a member could be removed **on `I_flip` only**, subject to the
separate binding gate.  Q-448 prevents the stronger inference: fixed-point
execution would remain blocked until an independent `C_ret` inhabitant and
the connected interval/absolute-continuity branch certificate `(S0-1)` are
built and verified.  Only then could the repaired conditional chain and its
later authorization gates be used.  Any eventual scoped end test would test
that bound scoped package, not the full asymmetric-primitive theory.  A
later proof of `(N2-3)` would add new arrows and require rechecking the six
generators and `J1`–`J15`; it could not inherit the scoped result without
that extension theorem.

```text
SCOPED_WITNESS = NEEDS(N3-4)
MEMBER_BINDING_LICENSED_NOW = false
FIXED_POINT_EXECUTION_LICENSED_NOW = false
END_TEST_LICENSED_NOW = false
POST_WITNESS_C_RET = independently_required / Q-448
POST_WITNESS_BRANCH_SCOPE = independently_required / Q-448
```

---

## 5. N4 — regressions, falsifiers, and fresh attacks

### 5.1 Nine standing regressions

| Regression | Re-execution | Result |
|---|---|---|
| abstract-kernel substitution | actual path currents, fixed `g_A4`, fixed `R_K`, fixed Q-408 analysis, and actual supports are used | **PASS** |
| circular construction | surface/stabilizer data precede every defect and equalizer calculation | **PASS** |
| false nonemptiness | `Eq_flip` is exhibited; full P4 and joint EQ6 remain open | **PASS** |
| current deletion | each odd new current is nonzero and retained | **PASS** |
| covariance overclaim | zero follows from an actual stabilizer, not orbit covariance alone | **PASS** |
| all-stage overclaim | all ranks in `I_flip` are separated from all primitive classes | **PASS** |
| Q-430 sector mixer | `(N2-1)` is rejected inside `I_flip` and retained as the general-scope countermodel | **PASS** |
| Q-432 `P=id` overreach | no global old-sector orthogonality is demanded | **PASS** |
| clause nonemptiness | A1 remains law-only; this construction proves only its exhibited restricted members | **PASS** |

### 5.2 Finite falsifiers

```text
one-edge tree:
  K=0; no scalar cycle is invented; old restriction is exact;

first flip cycle:
  K=R(1,-1); the current is nonzero; tau sends c to -c;
  old/new cross block vanishes;

two independent flip cycles:
  T_2=(Z_2)^2; independent characters kill both cross columns and the
  new/new off-diagonal; both refinement orders give the same target;

contact cycle:
  OLD_FID + exclusive-region RNL + LR all survive stabilizer averaging;

asymmetric primitive:
  epsilon in (N2-1) survives every currently applicable general clause and
  fires Def_leak, so no full-domain promotion occurs.
```

The tests are structural and symbolic.  No response value is evaluated.

### 5.3 Fresh attack 1 — marginal equalizer substitution

Hold the same `Eq_flip` term fixed and choose two separately admissible
completed candidates with distinct action-reader pairings or distinct
response natural transformations.  The Q-408 zero defects remain unchanged,
but `J2`, `J7`, or `J12` fails between the marginal choices.  Therefore an
inhabited Q-408 equalizer cannot imply a joint six-fiber equalizer.

```text
FRESH_ATTACK_1 = SURVIVES /
  Eq_flip supplies no fiber-product compatibility for the other roots.
```

### 5.4 Fresh attack 2 — noncofinal scoped finite datum

Append to a flip-generated stage one admissible asymmetric primitive of
the type `(N2-1)`.  The source is in `I_flip`; the target is not.  Internal
restriction squares inside `I_flip` remain exact, but the adopted P4 arrow
and its full `J15` bottom leg have no scoped image.  Thus covariance and
no-selection do not prove the subcategory cofinal or consumer-complete.

```text
FRESH_ATTACK_2 = SURVIVES /
  internal finite exactness is not full DoR-008/J15 coverage.
```

### 5.5 Fresh attack 3 — symmetry-breaking common refinement

Common-refine a flippable tuple with admissible asymmetric field decoration
whose pullback breaks the path-exchange stabilizer.  The resulting actual
primitive need not lie in `I_flip`, even though each flip-only diamond is
coherent.  This does not kill the restricted category; it kills any claim
that its existing diamonds establish closure under **all** adopted common
refinements.

```text
FRESH_ATTACK_3 = SURVIVES /
  general common-refinement closure still needs (N2-3) or scope amendment.
```

### 5.6 Anti-tuning ledger

```text
1  Freeze DoR-019, Q-408, DoR-020/A1, and J1-J15.
2  Recompute the flip defects on the actual surface family.
3  Audit invariance and full orbit retention before considering scope.
4  Construct the asymmetric primitive countermodel.
5  Compare Eq_flip's type with each of the six generator types.
6  Test all J rows on the same tuple; do not inherit marginal credit.
7  Audit the adopted P4 quantifier and full finite bottom square.
8  Inspect no fixed point, end test, response value, alpha consequence,
   numeric value, or measured constant.
```

No member, rank, ratio, orientation, frame, gauge, filtration, reader,
completion, action, or response was selected.

---

## 6. Geometry/rails split and final board

| Layer | Surface geometry | Rails |
|---|---|---|
| flip family | actual path pairs, difference currents, physical stabilizers, bundle/field data | definition of `I_flip` and orbit bookkeeping |
| zero defects | fixed A4 pairing and Q-408 support-local tests | four defect equations and equalizer diagrams |
| general stop | asymmetric physical primitive with live old/new coupling | statement of missing naturality square |
| scoped witness | six physical/analytic generator members on one carrier | `J1`–`J15` fiber-product certification and scope ledger |

The flip result is genuine surface geometry.  The promotion failure is not
a rail artifact: the other five physical/analytic roots are absent.  The
additional scope-admissibility requirement is partly governance/rails, but
it is necessary because the adopted physical domain contains asymmetric
primitive data and finite consumers not proved to factor through `I_flip`.

```text
FLIP_SECTION = CONFIRMED
FLIP_GENERATED_EQUALIZER = INHABITED / TYPE-P
B_Q408_REFINEMENT_ON_I_flip = INHABITED / TYPE-P

GENERAL_A4_Q408_LOCAL_EXCISION_NATURALITY = OPEN / TYPE-U
FULL_B_Q408_REFINEMENT = PARTIAL / TYPE-U

SCOPED_GENERATOR_COUNT = 1/6
SCOPED_J1_J15_TERM = absent
DOR020_SCOPE_ADMISSIBILITY = absent
FULL_FINITE_BOTTOM_CERTIFICATE_ON_SCOPE = absent

POST_WITNESS_C_RET = OPEN / TYPE-U
POST_WITNESS_INTERVAL_OR_AC_BRANCH = OPEN / TYPE-U

SCOPED_WITNESS = NEEDS(
  SCOPED_SIX_GENERATOR_J1_J15_EQUALIZER_TERM_ON_I_flip
  + DOR020_SCOPE_ADMISSIBILITY_AND_FULL_FINITE_BOTTOM_CERTIFICATE)

EQ6_DISCHARGED = false
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
MEASURED_CONSTANT_COMPARISON = false
MACHINERY_APPEAL = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
