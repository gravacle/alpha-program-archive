# STAGE 8 TASK 5 — V005 FINAL CHECK AND SINGLE-BEDROCK THEOREM — LANE 1 V001

Date: 2026-08-04  
Task: PASTE 499 / Task 5 / continuum package  
Lane: Codex Lane 1  
Custody: final adversarial reviewer and consolidation arm  
Standing: structural review only; DoR-020 reserved

## Lead result

```text
V005 = CONFIRMED

P1 = PASS |
  the P4/P5/P7/P8 procedures are now typed as partial procedures, and the
  J1--J15 audit distinguishes proved rule-level rows from honest
  obstructions.  P_CM020 and c_CM020 are no longer asserted.

P2 = KILL / SINGLE_BEDROCK_REFUTED |
  nonemptiness of the actual Q-408 primitive-refinement generator fiber is
  necessary and is the first construction stop, but is not sufficient for
  uniform completion estimates, completed faithfulness, response/boundary
  coherence, or closed-range local-symbol/Hodge/Maxwell structure.

BEDROCK = MINIMAL_SET(
  B_R1_NATURAL,
  B_Q408_REFINEMENT,
  B_C1_COMPLETION,
  B_FAITHFULNESS,
  B_C2_RESPONSE_BOUNDARY,
  B_C3_MAXWELL_HODGE
)

PHYSICAL_READER_NONEMPTY =
  DERIVED_CONDITIONALLY_FROM(
    B_C1_COMPLETION,
    B_FAITHFULNESS,
    B_C3_MAXWELL_HODGE
  )

P3 = PASS |
  DoR-020 may honestly adopt the package only as a theory conditional on
  nonemptiness of the JOINT J1--J15 equalizer over the six generators.
  A bound member remains conditional on an exhibited term and proof.

P4 = PASS |
  batteries and permanent regressions pass.  The fresh equalizer attack
  shows that separate nonemptiness of all six fibers still does not imply
  joint inhabitance.

READY_FOR_DOR020_RULING = yes |
  CONDITIONAL_PACKAGE_ONLY_ON_THE_JOINT_MINIMAL_SET;
  NO_BOUND_MEMBER_EXECUTION_AND_NO_END_TEST_BEFORE_A_CERTIFIED_WITNESS.
```

The exact consolidation is therefore not “one physical object.”  It is one
**joint fiber product** with six irreducible generator/certificate inputs.
Calling that fiber product one object would be harmless notation, but it
would not prove the stronger claim that the Q-408 primitive-refinement fiber
alone generates the other five inputs.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight, authorities, and symbol discipline

### 0.1 Three-line preflight

```text
DOES_THE_OBJECT_EXIST = yes
IS_THE_VERSION_CURRENT = yes | register head Q-420
ARE_ITS_INPUTS_PRESENT = yes
PREFLIGHT = PASS
```

The locked process was read in full.  V005 and its sidecar were verified
before the artifact was read.

| Authority | Verified SHA-256 | Role |
|---|---|---|
| locked process | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` | custody and fences |
| register through Q-420 | current head verified before review | commission and scope |
| V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | artifact under review |
| V004 final check | `542464ffdcd64e0442c779f078e9f4fade37c746663c8087451a82e09e241cfb` | N2/N4/N5 repair standard |
| Q-411 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | J1--J15 equalizer |
| Q-415 hostile witness check | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | permanent regressions |
| Q-408 finite physical kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual finite current/kernel |
| Q-409 Maxwell certificates | `2aad6d83…` | C1/C2/C3 dependency separation |
| Q-410 J_ref/estimate attempt | `8dd59b35…` | refinement and uniform-estimate boundaries |
| Q-407 reader build | `bae34116…` | algebraic reader versus physical reader |

### 0.2 Register sweep

```text
Q-407  algebraic reader family built; physical-local reader not proved;
Q-408  finite physical current/kernel and finite Ward calculus built;
Q-409  C1/C2 conditional, C3 partial, faithfulness independently open;
Q-410  J_ref physical refinement and family-uniform estimates absent;
Q-411  J1--J15 define a joint equalizer, not inhabitance;
Q-415  abstract physical kernel and circular reader witness killed;
Q-418  Ref_path square derived and Route-A boundary made exact;
Q-419  V004 member rule ill-typed and certificate asserted;
Q-420  V005 supplies partial typings and stops at O_PHYS4.
```

### 0.3 Load-bearing symbol distinctions

```text
Ref_path != Ref_PL;
finite faithfulness != completed faithfulness;
R2_alg != R2_phys;
an upper continuity estimate != a lower separation estimate;
a typed partial procedure != a term;
individual premise nonemptiness != joint equalizer nonemptiness;
P_CM020 as a name != P_CM020 as an exhibited member.
```

---

## 1. Verdict table

| Item | Verdict | One-line reason |
|---|---|---|
| P1 V005 repairs | **PASS** | all four rules are typed only on explicit premise carriers; J1--J15 are audited row by row and the first obstruction is reported rather than asserted away |
| P2 single-bedrock theorem | **KILL** | `O_PHYS4` is necessary but does not imply five independent analytic/cohomological inputs; explicit countermodels refute the missing implications |
| P3 DoR-020 form | **PASS** | the honest ruling is conditional on the nonempty joint six-input equalizer; a bound member and end test remain post-witness |
| P4 battery, regressions, fresh attack | **PASS** | V005 carries the prior attacks; the new joint-equalizer counterexample is survived because V005 does not claim inhabitance |

---

## 2. P1 — V005 repair verification

### 2.1 P4 typing

V005 replaces the untyped phrase “least physical geometry” by the premise
carrier

```text
X4(R)=(Gamma_R,P_R,S_R,Cof_R,Dens_R,
       eta_conn,R,eta_curv,R),                    (P1-1)
```

where membership requires actual path realizations and the physical Q-408
squares

```text
A_G=S_R^* A_G' P_R,
Kern_G^fin=Res_R Kern_G'^fin(j_R^H .).            (P1-2)
```

This excludes the killed abstract `UHU*` substitute before any metric is
used.  The global product index is isomorphism-class based and uses bounded
component metrics, so it does not select an edge label, orientation,
filtration member, or physical scale.  Naturality is imposed before any
linear minimum.

Crucially, the rule says:

```text
if the actual-path generator fiber is nonempty and has a unique global
minimum, choose it.                                (P1-3)
```

It does not claim `(P1-3)` is satisfied.  Actual-path membership and
positivity are expressly permitted to be nonconvex or empty.

```text
P4_RULE_TYPING = PASS / PARTIAL
P4_TOTALITY = not_claimed
```

### 2.2 P5 typing

P5 is no longer treated as a vector.  Given P4, V005 defines the initial
Hausdorff locally convex topology from the covariant physical seminorm
orbit and then takes its Hausdorff completion:

```text
tau_init=sigma({p_q,j,p_Q,j}),
tau_ker=completion_Hausdorff(S_phys,tau_init).     (P1-4)
```

The T-CM horn separately requires:

```text
uniform actual-kernel upper estimates;
an attained finite bound sequence;
a positive covariant W5 generating the same completed topology. (P1-5)
```

Thus the topological completion is canonical *conditional on its physical
seminorm data*, while Hilbertizability and uniformity remain explicit
premises.  V005 does not minimize topologies and does not infer a response
graph norm.

```text
P5_RULE_TYPING = PASS / PARTIAL
P5_UNIFORMITY = open
P5_W5 = open
```

### 2.3 P7 typing

Closed contact subspaces are represented by orthogonal projections in the
P5 Hilbertizable horn and use the gap metric.  The contact ideal is the
closed invariant restriction-natural hull of actual contact generators:

```text
I_CM=closure span orbit{actual boundary/contact generators}. (P1-6)
```

Response maps and cocycles live in declared operator/vector product
carriers.  The cocycle is imposed before closure and response selection is
conditional on a nonempty closed affine naturality fiber.  No ideal is
averaged as a vector and no visible cycle may be deleted.

```text
P7_RULE_TYPING = PASS / PARTIAL
P7_RESPONSE_FIBER = open
```

### 2.4 P8 typing

P8 uses the bounded transform of a closed operator,

```text
b(D)=D(1+D^*D)^(-1/2),                            (P1-7)
```

and the graph/gap metric.  `delta`, `Box`, the Ward/contact/harmonic
projections, and the Maxwell summand are then produced by typed adjoint and
functional-calculus operations.  The rule stops unless ranges are closed,
the local principal symbol exists, the `L_T` normalization denominator is
nonzero, and refinement commutes.

P8 is upstream of P2, so no reader can define the Maxwell projection.

```text
P8_RULE_TYPING = PASS / PARTIAL
P8_CLOSED_RANGE_AND_SYMBOL = open
```

### 2.5 Q-419 minimum/restriction repair

For isomorphism-class index `x`, V005 defines invariant summable weights and
a bounded component metric, then minimizes only on the **global natural
equalizer**.  It explicitly retains the Q-419 counterexample

```text
C_N={1}, C_M={(x,y):x+y=1}, rho(x,y)=x,
m_N=1, m_M=(1/2,1/2), rho(m_M)!=m_N.              (P1-8)
```

Objectwise minimization is therefore forbidden.  V005 does not assert the
global equalizer nonempty, closed, convex, or uniquely minimized; those are
premises of the partial rule.  This meets the N5 kill rather than hiding it.

### 2.6 J1--J15 audit

The member audit uses four statuses correctly:

```text
PROVED_AS_RULE;
PROVED_AT_TYPE_LEVEL;
CONDITIONAL_THEOREM_ONLY;
OBSTRUCTED/PARTIAL.
```

The substantive audit is:

| Families | V005 status | Review |
|---|---|---|
| J1--J2 | obstructed at absent P8/P2 and P5 maps | **honest**; Q-407 is not promoted |
| J3 | conditional covariance theorem | **honest**; no reader term claimed |
| J4 | conditional on the bound natural P1 term | **honest** |
| J5--J6 | proved for `h_CM=0` once P1 exists | **correct rule-level proof** |
| J7 | obstructed at P1/P2 | **honest** |
| J8--J9 | type/rule-level unit and batching proofs | **correct** |
| J10 | exact on `Ref_path`, obstructed on a new primitive | **correct first stop** |
| J11--J12 | obstructed at density and response naturality | **honest** |
| J13 | conditional closure/cocycle theorem | **honest** |
| J14 | obstructed at uniform estimates and faithfulness | **honest and importantly separate** |
| J15 | exact on `Ref_path`, globally obstructed | **correct scope** |

Therefore V005 makes no schema-passed-as-instance move:

```text
P_CM020_TERM_DEFINED = false
c_CM020_CONSTRUCTED = false
J1_J15_VERIFIED_ON_P_CM020 = false
```

The obstruction statement is exact as the **first construction stop**.
It is not a sufficiency theorem and V005 does not prove it to be one.

```text
P1 = PASS
```

---

## 3. P2 — the single-bedrock theorem

### 3.1 Claim under test

Let

```text
B_Q408 := NONEMPTY_ACTUAL_Q408_PRIMITIVE_REFINEMENT_GENERATOR_FIBER.
                                                               (P2-1)
```

The strong single-bedrock claim is:

```text
B_Q408 implies every remaining premise needed for P_CM020 and c_CM020.
                                                               (P2-2)
```

V005 proves only:

```text
not B_Q408 -> no full P4 -> downstream terms cannot yet be instantiated.
                                                               (P2-3)
```

Equation `(P2-3)` is a necessity/ordering statement.  It does not imply
`(P2-2)`.

### 3.2 Minimal open generator set

The exact unresolved generators/certificates are:

```text
B_R1_NATURAL :=
  a bound covariant natural P1 section on the full refinement category,
  or an honestly retained family rule with its restriction cubes;

B_Q408_REFINEMENT :=
  actual paths, test-form/current transports, coframe/density, and
  connection/curvature transports for every primitive refinement orbit,
  satisfying the exact Q-408 squares;

B_C1_COMPLETION :=
  one physical local-kernel topology, family-uniform upper estimates, an
  attained bound family, and the T-CM Hilbertizable W5 certificate;

B_FAITHFULNESS :=
  completed zero-kernel/lower-separation theorem, or a disclosed safe
  quotient with finite-visible intersection and annihilator proofs;

B_C2_RESPONSE_BOUNDARY :=
  a nonempty common-refinement response solution, actual discrepancy
  cocycle, closed safe contact ideal, and subextensive boundary theorem;

B_C3_MAXWELL_HODGE :=
  closed-range/domain/harmonic control, local principal symbol,
  Ward/contact/Hodge/Maxwell decomposition, and nonzero L_T normalization.
                                                               (P2-4)
```

These are generator **fibers with their own certificates**, not six scalar
choices.  Their lawful package is the joint J1--J15 equalizer, not the
Cartesian product.

### 3.3 Exact dependency graph

```text
ratified finite Q-408 core
          |
          v
  B_Q408_REFINEMENT ------------------------------+
          |                                       |
          | supplies actual directed core         |
          v                                       |
  B_C1_COMPLETION                                  |
          |                                       |
          +---------------> B_FAITHFULNESS        |
          |                                       |
          +----+                                  |
               |                                  |
B_R1_NATURAL --+--> B_C2_RESPONSE_BOUNDARY        |
               |            |                     |
               +------------+---------------------+
                            v
                  B_C3_MAXWELL_HODGE
                            |
           +----------------+----------------+
           |                                 |
   B_C1_COMPLETION + B_FAITHFULNESS          |
           |                                 |
           +------> PHYSICAL P2 READER <-----+
                            |
                            v
   Eq_J1--J15(B_R1,B_Q408,B_C1,B_FAITH,B_C2,B_C3)
                            |
                            v
                 [P_CM020,c_CM020]
                            |
                            v
                 downstream conditional chain
```

The arrows mean “is a required input to,” not “is proved by.”  In
particular, `B_Q408_REFINEMENT -> B_C1_COMPLETION` does not mean P4 proves
C1; it means C1 cannot even be posed on the actual full directed core until
P4 exists.

### 3.4 Reduction of each named obstruction

| Named obstruction | Exact reduction |
|---|---|
| V005 member proof | requires the joint equalizer over all six fibers |
| Route A first boundary | exactly `B_Q408_REFINEMENT` |
| C1 completed Maxwell continuity | `B_Q408_REFINEMENT + B_C1_COMPLETION`; faithfulness remains separate |
| C2 refinement/locality | `B_Q408_REFINEMENT + B_C1_COMPLETION + B_C2_RESPONSE_BOUNDARY` |
| C3 Ward/contact/Hodge/Maxwell | prior C1/C2 inputs plus `B_C3_MAXWELL_HODGE` |
| physical reader nonemptiness | induced by `pi_Mx Loc Kernbar Q` after `B_C1_COMPLETION + B_FAITHFULNESS + B_C3_MAXWELL_HODGE`; not a seventh primitive root |
| completed faithfulness | exactly `B_FAITHFULNESS`; not implied by finite faithfulness or upper continuity |
| bound P1 representative | `B_R1_NATURAL`; Q-396 proves a nontrivial admissible family, not a unique bound natural section on new refinement arrows |

### 3.5 Independence proof 1: P4 does not imply uniform C1 estimates

Let the stage-`n` profile and kernel spaces be `R^n`, with compatible
coordinate restriction, and let the physical kernel map be the identity.
Give the target the local norm

```text
||y||_(Y_n)=max_(1<=k<=n) k|y_k|,
```

while the profile carrier has its ordinary Euclidean norm.  Every finite
map is bounded and the maps commute with restriction, but the best operator
bound grows at least like `n`.  No family-uniform constant exists.

This model has a nonempty compatible primitive realization and exact finite
maps but fails `B_C1_COMPLETION`.

```text
B_Q408_REFINEMENT does_not_imply B_C1_COMPLETION.  (P2-5)
```

### 3.6 Independence proof 2: upper completion does not imply faithfulness

Let `c_00` be the finite core in `ell^2`, and choose a nonzero
`v in ell^2` with infinite support.  Define

```text
q:ell^2 -> ell^2/span{v}.                          (P2-6)
```

Because `span{v}` meets `c_00` only at zero, `q|_(c_00)` is injective: every
finite stage is faithful.  The map is continuous and extends to the
completion, but

```text
ker(q)=span{v} != {0}.                             (P2-7)
```

Thus finite faithfulness plus continuous completion does not prove
completed faithfulness.  This is the exact Q-396 danger in a concrete
functional-analytic model.

```text
B_C1_COMPLETION does_not_imply B_FAITHFULNESS.     (P2-8)
```

### 3.7 Independence proof 3: refinement does not imply response/boundary coherence

Take a refinement diamond with two composite paths from `G_0` to `G_2`.
Let all geometric/current maps exist and commute, but assign actual boundary
discrepancies whose cocycle sums are

```text
beta_path_1=0,
beta_path_2=b != 0.                                (P2-9)
```

No single restriction-natural cocycle can satisfy both path values.  The
primitive physical refinements and upper kernel estimates can all exist;
J12/J13 still have an empty solution fiber.

```text
B_Q408_REFINEMENT + B_C1_COMPLETION
  does_not_imply B_C2_RESPONSE_BOUNDARY.           (P2-10)
```

### 3.8 Independence proof 4: prior continuum data do not imply closed-range P8

On `ell^2`, define the bounded self-adjoint operator

```text
d e_n=(1/n)e_n.                                    (P2-11)
```

Its range is dense and not closed.  The operator, adjoint, and quadratic
combination exist, but the closed-range Hodge decomposition and bounded
harmonic/Maxwell projections required by P8 do not follow.  Nothing in P4,
an upper completion estimate, faithfulness, or a boundary cocycle excludes
this behavior.

```text
B_Q408_REFINEMENT + B_C1_COMPLETION
  + B_FAITHFULNESS + B_C2_RESPONSE_BOUNDARY
  does_not_imply B_C3_MAXWELL_HODGE.               (P2-12)
```

### 3.9 P1 section independence

`B_Q408_REFINEMENT` lives in the geometric/current realization type.
`B_R1_NATURAL` lives in the completed action-representative type.  No
ratified arrow maps a physical primitive path/coframe tuple to a selected
base-action representative.  Q-396 instead proves that the admissible R1
fiber has nontrivial member-sensitive flat directions.

Therefore a physical refinement generator cannot by type select P1.  A
bound P1 section or retained family rule is an additional input.

```text
B_Q408_REFINEMENT does_not_imply B_R1_NATURAL.     (P2-13)
```

### 3.10 Reader reduction

The algebraic reader family is already built.  Once `Kernbar`, its faithful
or quotient horn, the local realization, and a normalized Maxwell
projection exist, define

```text
ell=pi_Mx compose Loc compose Kernbar compose Q.   (P2-14)
```

Because `Q` is the relation quotient, `(P2-14)` annihilates the exact
relations.  `B_FAITHFULNESS` either gives zero kernel or forces the
annihilator condition.  The nonzero `L_T` normalization in
`B_C3_MAXWELL_HODGE` gives `ell(L_T)=1`.

Hence physical reader nonemptiness is a conditional consequence of the
completion/faithfulness/symbol inputs.  It is not supplied by
`B_Q408_REFINEMENT` alone, but it need not be a seventh primitive item.

```text
PHYSICAL_READER_NONEMPTY =
  CONDITIONAL_THEOREM(B_C1_COMPLETION,
                      B_FAITHFULNESS,
                      B_C3_MAXWELL_HODGE).         (P2-15)
```

### 3.11 The theorem

**Theorem (minimal continuum bedrock).**  The actual Q-408 primitive
refinement generator fiber is necessary for the full continuum package and
is the first unresolved construction object.  It is not sufficient.  The
current corpus reduces the remaining package to the nonempty joint
J1--J15 equalizer over the six fibers in `(P2-4)`.  No one of the five
additional inputs is derivable from `B_Q408_REFINEMENT` under the ratified
interfaces.  The physical normalized reader is derived conditionally by
`(P2-14)` and adds no seventh root.

```text
SINGLE_BEDROCK_Q408 = REFUTED / TYPE-R
BEDROCK = MINIMAL_SET(P2-4)
P2 = KILL / replacement_theorem_proved
```

---

## 4. P3 — honest DoR-020 form

### 4.1 Joint premise, not six unrelated assumptions

Define

```text
H_020^min :=
  Nonempty Eq_(J1,...,J15)(
    B_R1_NATURAL,
    B_Q408_REFINEMENT,
    B_C1_COMPLETION,
    B_FAITHFULNESS,
    B_C2_RESPONSE_BOUNDARY,
    B_C3_MAXWELL_HODGE
  ).                                               (P3-1)
```

`H_020^min` is stronger than asserting each component fiber separately
nonempty.  It requires one compatible tuple in their fiber product.

The lawful conditional package theorem is

```text
H_020^min
  -> exists [P_CM020,c_CM020]
  -> every licensed downstream result remains tagged by that member.
                                                       (P3-2)
```

Before a witness to `H_020^min` exists, V005 supplies only the typed partial
procedures and conditional construction theorems.  It supplies no bound
member and no member-sensitive scalar.

### 4.2 Honest ruling structure

DoR-020 can honestly adopt:

```text
1. V005's P1--P8 clause types and partial procedures;
2. the derived Ref_path square and exact Route-A boundary;
3. the six-input minimal-bedrock theorem;
4. the conditional implication (P3-2);
5. the four hostile regressions plus REG-MIN;
6. member-scoped falsification semantics;
7. no assertion that H_020^min is inhabited.
```

It cannot honestly adopt:

```text
P_CM020 as an existing term;
c_CM020 as a constructed certificate;
the Q-408 fiber as sufficient;
a physical reader before (P2-14);
completed faithfulness from finite faithfulness;
an executable sealed-number end test before a certified witness.
```

### 4.3 Sealed-number trial semantics

The sealed number can test a **certified witness of** `H_020^min`; it cannot
create or prove that witness.

| Event | Licensed conclusion |
|---|---|
| a structural root or J-row fails during construction | that candidate tuple is void before any number |
| no compatible tuple has been supplied | package remains conditional; no end test exists |
| a certified bound tuple is authorized and disagrees | the bound tuple and its Route-B model are refuted |
| a certified bound tuple agrees | that tuple survives this test; other members are not established |
| every compatible tuple is proved to disagree | the joint physical family is refuted |
| a future rigidity theorem gives one common consequence and it disagrees | the inhabited joint family is refuted |
| one different member disagrees | only that member is refuted |

Thus the number is the trial of the bound realization of the minimal set,
not an oracle for set nonemptiness.

```text
DOR020_FORM =
  CONDITIONAL_PACKAGE_ON_H_020^min
  + BOUND_MEMBER_ONLY_AFTER_WITNESS
  + MEMBER_SCOPED_END_TEST
P3 = PASS
```

---

## 5. P4 — battery, regressions, and fresh attack

### 5.1 Permanent regressions

| Regression | Recheck | Verdict |
|---|---|---|
| abstract physical kernel | P4 membership requires actual paths and Q-408 squares before operator comparison | **PASS** |
| circular `pi_Mx` | P8 remains upstream of P2; absent P8 leaves reader absent | **PASS** |
| false reader nonemptiness | J1/J2 remain obstructed; Q-407 stays algebraic | **PASS** |
| Hodge from isometry | closed range, domains, local symbol, and harmonic control remain explicit P8 premises | **PASS** |
| minimum/restriction | global natural equalizer precedes any minimum; objectwise minima forbidden | **PASS** |
| finite/completed faithfulness | separate J14 horn retained | **PASS** |

### 5.2 Battery

Target tuning, hidden unit conversion, stage/member selection, abstract
kernel substitution, response graph norm, topology-as-vector, ideal
averaging, closed-operator averaging, zero new-edge deletion, Ref_path
promotion, posterior reader selection, and DoR-008 finite mismatch attacks
all pass at the claimed partial-rule scope.

The single-bedrock refutation does not kill V005 because V005 reports
`O_PHYS4` as the **first** obstruction and its downstream ledger expressly
leaves the other certificates open.

### 5.3 Fresh attack: nonempty components, empty equalizer

Even if every fiber in `(P2-4)` is separately nonempty, their joint
equalizer can be empty.  The elementary form is:

```text
X={x in R:x=0},
Y={y in R:y=1},
compatibility requires x=y.                       (P4-1)
```

Both `X` and `Y` are nonempty, closed, convex, and have unique least-norm
members.  Their compatibility equalizer is empty.

This models, for example, a normalized P8/P2 Maxwell coefficient that does
not equal the coefficient induced by a separately valid P5 kernel
realization.  No amount of componentwise existence proves J2.

V005 survives because it explicitly says J1--J15 must be checked on an
actual term and sets `c_CM020=NOT_CONSTRUCTED`.  The attack becomes a
permanent condition on DoR-020: the premise must be `(P3-1)`, not the
conjunction of six separate nonemptiness assertions.

```text
FRESH_ATTACK = COMPONENTWISE_NONEMPTY_JOINTLY_EMPTY
V005_RESULT = PASS
P4 = PASS
```

---

## 6. Final board

```text
P1 = PASS
P2 = KILL / SINGLE_Q408_BEDROCK_REFUTED
P3 = PASS / CONDITIONAL_JOINT_MINIMAL_SET_FORM
P4 = PASS

V005 = CONFIRMED

BEDROCK = MINIMAL_SET(
  B_R1_NATURAL,
  B_Q408_REFINEMENT,
  B_C1_COMPLETION,
  B_FAITHFULNESS,
  B_C2_RESPONSE_BOUNDARY,
  B_C3_MAXWELL_HODGE
)

JOINT_BEDROCK = H_020^min / Eq_J1--J15
Q408_BEDROCK_ALONE = insufficient
PHYSICAL_READER_ROOT = no / conditionally_derived

READY_FOR_DOR020_RULING = yes /
  CONDITIONAL_PACKAGE_ONLY_ON_H_020^min;
  BOUND_MEMBER_AND_END_TEST_ONLY_AFTER_CERTIFIED_WITNESS.

p_symbolic = true
nu_symbolic = true
rank_or_ratio_selected = none
orientation_frame_filtration_selected = none
numeric_evaluation = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```
