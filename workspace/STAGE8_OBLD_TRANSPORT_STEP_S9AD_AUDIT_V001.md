# STAGE 8 / OBL-D — ADVERSARIAL AUDIT OF THE r_F^* TRANSPORT-STEP BUILD (S9AD)

Lane: OBD-AUDIT, independent adversarial auditor. DEFAULT POSTURE = REFUTE.
Subject: `STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md`
Subject digest, recomputed at path BEFORE reading (sidecar MATCH):
`4a6718fe9c9f30969e1e0edc965edd8a26aa79d968bba6a2c847341e569602d4`
Date: 2026-08-14. **ALL_RESULTS = CLAIMED until registrar handling.**

## VERDICT

**CONFIRMED-WITH-CORRECTIONS.** Every substantive certificate in the build was
re-derived with my OWN algorithms (different decomposition/elimination routes)
and landed identically — several in STRICTLY STRONGER form (full-matrix
identities on all of C^1(K) instead of the build's 17-basis checks; C-D(i)
closed UNCONDITIONALLY over Q where the build's A2-B end-to-end leg was mod-p).
The build's verbatim script re-ran in a fresh venv with BYTE-IDENTICAL output,
exit 0. The step choice is the right next obligation per the POSED ordering —
verified at bytes, and it MATCHES the next-obligation identification I fixed in
hashed notes before opening the build. Quantifier honesty holds: OBL-D is held
OPEN, the licensed-generator vs full-D3 gap is CARRIED not decided, C_ref stays
a frozen AUDIT class, no member of the 1887-dim variety is consumed. The
corrections are textual (C-1..C-3 below); none is load-bearing.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       EXACT SYMBOLIC ONLY throughout this audit: integer/rational arithmetic
       (fractions.Fraction; division-free +/-1-pivot elimination), no floats,
       no measured constant, no physical quantity computed or bounded.
```

---

## 0. Preflight, fences, sweep cutoff

Output name probed at path BEFORE write: ABSENT (artifact and sidecar; probed at
session start and re-probed 19:48:41 CDT immediately before this write). No file
matching register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened ("Q-..."
tokens and the subject-title token "ROAD ITEM 6" carried as EXPECTED-UNLOCATABLE
tasking labels — see O-b). No git action. No existing file edited. Fresh venv
(Python 3.9.6, stdlib only) for all scripts.

**SWEEP CUTOFF, DECLARED: 2026-08-14 19:47:57 CDT** (both corpus roots
`workspace/` and `cleanroom_output/` plus relay boxes swept for RA27-4/RA27-6 /
eta_resp content, prior r_F^*/pullback builds, and AND-class/1887 carriers;
parallel lanes are landing same-night artifacts — post-cutoff landings are
outside this sweep by the tasking's own rule).

## 1. INDEPENDENCE DISCLOSURE, WITH HASHES

Protocol executed in order, with the build UNOPENED until step (iii):

```text
(i)   Seed stock read and seal-verified; the next obligation identified MYSELF
      from the POSED artifact's own ordering; prediction ledger + hazard list
      fixed in scratchpad notes BEFORE opening the build:
        OBD_AUDIT_INDEPENDENT_NOTES_PREBUILD.md
        sha256 3288f6dabbfe641e475f9a393a849506a177742e557742d7e830e1525ae1800b
        fixed at 2026-08-14 19:29:55 CDT.
      MY identification (verbatim from the notes): "the KINEMATIC
      COMMON-REFINEMENT COMMUTATION FRAGMENT of O7 (RA27-4)" — with the three
      clauses: (a) exact transport commutation/composability across licensed
      generators; (b) the consumption-side corollary via d0-square + dim H = 0;
      (c) the quantifier bar: eta_resp unbuildable, C_ref/full-D3 gap must be
      carried; "the ONLY lawful headline is a PARTIAL/step outcome".
(ii)  MY OWN ATTEMPT run pre-build (fresh venv, exact only):
        obd_audit_own_attempt_v001.py
        sha256 8c00faccfecff49cfd9ad8676e2f89442075bbe564a81160a086c0916fd5c70a
        obd_audit_own_attempt_v001.out
        sha256 9275ed3b557383fbfe8afb782c24d6ec1e60767af55c569cb0628b152dfd68e7
        hashed at 2026-08-14 19:32:31 CDT.
      Results (all exact): censuses re-derived and MATCH sealed (16/32/24,
      81/216/216, 16/65/110, 81/544/1232); dim H = 0 UNCONDITIONALLY over Q on
      K/A1/A2-F/A2-B by my +/-1-pivot sparse elimination (rank d_0 = 15/80/15/80,
      rank d_1 = 17/136/50/464), hence ker d_1 = im d_0 on every carrier; the
      d0-square re-derived by a SPANNING-TREE POTENTIAL algorithm (not
      elimination) — EXISTS/integer/zero-residual/iota*-mod-constants on all
      five; the d1-square re-verified on A2-F from my own sd*_2; composite
      closure EXECUTED exactly on two composites; flux-transport
      well-definedness verified.
(iii) Build read ONCE in full; then attacked at bytes (§3).
```

The build's own step choice — the O7/RA27-4 field-side leg r_F^* — is the same
object class my notes fixed, refined by the D012 §2.6.3 typing (which I then
verified at bytes). Prediction P1 of my ledger ("lawful build = kinematic
fragment at licensed-generator level, OBL-D held OPEN, gap carried") is the
outcome that occurred.

## 2. SEAL TABLE (recomputed at path BEFORE reliance; all MATCH)

```text
4a6718fe9c9f30969e1e0edc965edd8a26aa79d968bba6a2c847341e569602d4  STAGE8_OBLD_TRANSPORT_STEP_S9AD_V001.md   (SUBJECT; sidecar MATCH)
22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md        (tasked digest MATCH)
a5c71b2ac7cd198bbac188e865d139a71340f1728f72fe56384aa5d60489110c  STAGE8_TRANSPORT_LAW_POSE_CHECK_V001.md   (tasked digest MATCH)
bb1b88ad22ad2746a59957936785e9e44e3c2c4c1abdf7cf417096ff982d08c4  STAGE8_D0_SQUARE_CERTIFICATE_V001.md      (tasked digest MATCH)
d83655ae50b4203ce42be6fd4fc0a50f03f601c96f55eb3c3afab71807de492e  STAGE8_D0_SQUARE_CHECK_V001.md            (tasked digest MATCH)
9a0c20458c98b27aadeef0b2daee6329a0a6eef93ea78569c0a1e74a5fe20bb9  STAGE8_T7_D3_QUANTIFIER_READING_FREEZE_AND_LEMMA_CONSUMPTION_V001.md  (tasked digest MATCH)
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md     (sidecar MATCH)
c0e7192be7d5d93d74a28d66c9c0e39543f4cb755c222e7b1a9cb7f3b4b72f46  STAGE8_AND_CLASS_CHECK_V001.md            (sidecar MATCH)
df4514a5b807cbc903465505ad896ffb6e72c7ab9f891a4541226d18398a7034  STAGE8_G1_KERNEL_CERTIFICATE_V001.md      (sidecar MATCH)
3534ca534709a3c8ac591994a9ae650224a8594b3f1a8b2a96494a69ab9653e6  STAGE8_G1_KERNEL_CERTIFICATE_CHECK_V001.md (sidecar MATCH)
aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a  review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  (packet member; recomputed full-file)
```

The build's remaining §0 seals (FORM 5e49d209, 807 9cf9b329, CARRIER 97f073c1,
XCHK 614e20c8, 795 590b3979, FR 4d072e76, D012 74bbb7aa) were each verified this
session in the course of span extraction — all MATCH their sidecars and the
build's pasted digests. V011 custody (trap T6): every span I extracted binds to
the packet member `aa7c6d49` (78,794 B); the top-level same-named file was NOT
read beyond digest discrimination in the sealed record.

**Span verification: 14/14 of the build's load-bearing byte spans re-extracted
and re-hashed at path — ALL MATCH the build's pasted digests**, including:
POSED [17736,18509) O7 row; POSED [24873,26213) + [38167,38821) MINIMAL_SUBLAW;
D012 [45008,45355) RA27-4 row and [31536,33632) §2.6.3 (which verbatim types
`r_F^* : F_phys(K) -> F_phys(R)`, eta_resp/Eval as the response legs, and
`N_r(d_K) = 0` as the exact demand — the build's square-shape claim is
byte-true); G1-CERT [13899,15572) §4.1 (OBL-D OPEN, rides RA27-4/RA27-6);
FORM [8560,8871) (the three-block split; see C-1); 807 [6502,7613) (variable
inventory = S alone — the member-independence carry is byte-true) and
[10514,10951) (dim 2784-897 = 1887); FR [10137,10626) (33-free / 32 at fixed
energy / non-coboundary); V011 [46712,47445) + [47025,47247) (the OBL-D clause
pair verbatim). The four remaining spans (V011 d_0/d_1 law, XCHK censuses,
CARRIER sd*_1 law, 795 A0 instance) carry the same span-hashes as the sealed
D0SQ lineage pins, re-verified this session.

## 3. THE ATTACK

### 3.1 CAS verbatim re-run

The build's §5 script was extracted at bytes and re-run in my fresh venv:
exit 0, runtime 3:59, and the output is **BYTE-IDENTICAL** to the build's §6
fence (`diff` empty). The disclosed solver bugfix (CH-7) is sound as published:
each mod-p pivot column is its reduced row's minimum column, so decreasing-
column back-substitution is well-founded.

### 3.2 Independent re-derivation, DIFFERENT routes (my attack battery)

Script `obd_audit_attack_battery_v001.py`
(sha256 in §6; output `obd_audit_attack_battery_v001.out`): dense integer
matrix arithmetic (vs the build's sparse-dict compose); my own sd*_2
constructions, including the A2-B triangle signs by a SHOELACE construction on
doubled barycenters (vs the build's frame determinant); my own +/-1-pivot exact
Q-elimination for ranks (vs the build's three-prime sandwich). Results:

```text
CONTROLS  d_1'd_0' = 0, the sealed d_0-square, the sealed d_1-square: hold
          entrywise on ALL FIVE generators with MY independently built matrices.
C-A       sections exact: multiplicities (m_0,m_1,m_2) = (1,1,1)/(1,1,1)/
          (1,2,4)/(1,1,2)/(1,2,8) — IDENTICAL to the build's table.
C-B       d_0'^T r*_1 = r*_0 d_0^T : entrywise TRUE on all five.
C-C       trichotomy REPRODUCED: TRUE on L_id/A0 only; the build's three exact
          witnesses reproduced byte-for-byte (A1 half-edge 0000->0001 / vtx
          0001: 1 vs 0; A2-F diagonal 0000->0011 / vtx 0000: 0 vs -1; A2-B
          half-edge 0000->0002 / vtx 0001: 1 vs 0).
C-D(ii)   PROVED IN STRICTLY STRONGER FORM: the FULL matrix identity
          sd*_2 . d_1' . r*_1 = m_1 . d_1 on ALL of C^1(K) (not a 17-basis
          sample) — TRUE on all five generators.
C-D(i)    CLOSED UNCONDITIONALLY OVER Q: C-B (entrywise) gives
          d_0'^T r*_1 x = 0 for every horizontal x; my pre-build unconditional
          Q-ranks give dim H = 0 on every carrier (both sides), so the
          horizontal lift is unique and Q'_flux r_F^* = r*_1 Q_flux holds for
          EVERY flux — the build's proof route verified premise-by-premise,
          and its A2-B mod-p end-to-end leg (the only non-Fraction leg in the
          build) is thereby SUBSUMED by an exact argument. Plus 3/3 end-to-end
          rational spot solves on A2-B with my own solver.
C-E       EXECUTED beyond the build's formal carry: the composite A0.A1 run as
          exact matrices — both squares, C-A with multiplicities multiplying
          (1,2,4), C-B, and the full C-D(ii) identity all hold. (My pre-build
          attempt had already executed a second composite, A1.relabel.)
Q_flux    provenance verified: V011 packet member :1338-1344 — "the unique
          horizontal, minimum-norm lift", d_1 Q_flux xi = xi — and FORM :93-94/
          :150 carry it; V011 :1352 shows Q_flux as the kinematic factor of
          mathcal_K_L, supporting the build's K-2 typing of "the response map's
          only sealed factor".
```

### 3.3 Wrong-next-obligation check (at bytes)

Step 1 (d0-square) and step 2 (AND-class member-independence) are BUILT and
CONFIRMED of record (timestamps 2026-08-13; seals verified) — rebuilding step 2
would have double-built; the build's refusal is correct. Of the remaining
transport-package items, POSED's own text bars O8 ("No instantiated common
tau_ker exists"), O9 ("conditional theorem exists; premises absent"), O10 ("E4
... MISSING"), and consumers (b)/(c) consume the write-chain realization sealed
NOT-SUPPLIED (POSE_CHECK correction B). O7's field-side leg is the unique
fragment with all inputs sealed — and D012 §2.6.3 types exactly that leg. The
build's choice AGREES with my pre-fixed identification. **STEP CHOICE = SOUND.**

### 3.4 Quantifier honesty (the named hazard)

No licensed-generator result is quoted at the C_ref or full-D3 quantifier
anywhere in the build. The flag block carries: OBL-D OPEN; clause 2 OPEN (its
finite skeleton displayed, not adjudicated); FULL-D3 GAP "OPEN AND CARRIED"
with 9a0c2045's frozen reading cited (star-refined atoms of unbounded facet
count); C_ref = frozen AUDIT class (trap T1); H^1 > 0 carriers named OUTSIDE
the licensed inventory; V011's regression-fixture warning quoted with "proves
NO universality". The carrier-family vs cellulation-class distinction (the
G1-3 wrong-quantifier trap) is stated, not blurred. **HONEST.**

### 3.5 Member-independence and custody

807's variable inventory span verified: the variety's variables are the
quotient family S alone — no chain-transport variable — so r*/r_F^* consume no
member, F-rule, J_1 value, or freedom value; the falsifier of record cannot be
realized through this step. V011 packet-member custody honored throughout
(all V011 span hashes bind to aa7c6d49). No member selection, no adoption:
r* is exhibited as CANDIDATE with RA27-4's naming obligation standing.

### 3.6 Prior-build sweep

Every pre-existing `r_F^*` occurrence in both corpus roots (D012 LANE2/LANE3,
B1C_DENSITY_LAYER, B2_GLUING, ITEMS_1_6 coverage audit) is a TYPING display of
the future package — none is a build. The subject is the first realization of
the leg (build refuter R-5 confirmed).

## 4. CORRECTIONS (none overturning)

```text
C-1  PROVENANCE-TABLE ANNOTATION (the one real correction): the build's §0 span
     row `FORM [8560,8871) ... "D1 three-block split; Q_flux = d_1^+(d_1 d_1^+)^+
     on F_phys"` — the pinned span contains ONLY the three-block split; the
     Q_flux formula lives OUTSIDE it (FORM :93-94 and :150; V011 packet member
     :1338-1344), and the gloss's first `^+` should be `^dagger` (sealed:
     `d_1^dagger (d_1 d_1^dagger)^+`). The object is genuinely sealed and the
     build's prose attribution ("V011's unique horizontal minimum-norm lift")
     is correct — the span-table gloss conflates two locations. One-line fix;
     nothing relied on is unverified.
C-2  REDUNDANCY NOTE, not a defect: C-D(i)'s A2-B "end-to-end" verification leg
     is mod-p (Fraction-exact on the horizontality side); the exact closure is
     the build's own proof (C-B + uniqueness), which is sound — and this audit
     closed C-D(i) unconditionally over Q, retiring even that dependence.
C-3  REGISTRATION GUIDANCE on K-2's word "DISCHARGED": in the build it is
     scoped to "clause 1's SEALED-KINEMATIC HALF at the LICENSED-GENERATOR
     quantifier" and the flag block holds OBL-D OPEN — honest as written. The
     register entry should carry the yield as "clause 1 POSABLE + kinematic-
     half certificate at the licensed-generator quantifier", never any OBL-D
     (or RA27-4) discharge.
```

Observations, no action required: (O-a) the subject's declared sweep cutoff
(19:18:08 CDT) precedes its own landing (19:23) — correct protocol; (O-b) the
title token "ROAD ITEM 6" is a tasking-carried label, EXPECTED-UNLOCATABLE
under the fences; the artifact's fence block declares no road file opened and
no road content is consumed anywhere in it; (O-c) float hygiene verified: the
build's script contains int/Fraction arithmetic only.

## 5. CHOICE LEDGER (mine)

```text
A-1  INDEPENDENCE ORDER (PROVABLE protocol): notes hashed 19:29:55, own attempt
     hashed 19:32:31, build first opened after — hashes in §1.
A-2  MY ALGORITHM ROUTES (YOURS, chosen for maximal difference): +/-1-pivot
     division-free sparse elimination for ranks; spanning-tree potential
     integration for the d0-square; shoelace signs for A2-B sd*_2; dense
     integer matmul for all certificates; full-matrix identity forms where the
     build sampled a basis.
A-3  SPOT-CHECK BUDGET (YOURS): C-D(i) end-to-end rational solves limited to 3
     random cochains on A2-B — justified because the unconditional closure
     (C-B + Q-ranks) covers ALL fluxes; the spots only exercise the pipeline.
A-4  SEED-STOCK SCOPE (YOURS): G1-CERT pair read for OBL-D standing only;
     AND-CLASS pair verified at flag-block level and re-derived kinematically
     (dim H = 0) rather than line-audited — it carries its own CONFIRMED check
     of record.
A-5  VERDICT WEIGHT (YOURS): C-1 is a genuine textual correction in the
     provenance table of an otherwise fully-replicated build; per the lineage's
     own precedent (non-overturning findings recorded, mathematics standing)
     the honest verdict is CONFIRMED-WITH-CORRECTIONS, not CONFIRMED.
```

## TOY_SEPARATION

```text
Everything this audit re-derived lives on the sealed WORKING-CLASS INSTANCE:
the single oriented unit 4-cell and its five licensed refinement instances plus
executed composites. It is a TOY relative to (t1) the record complex proper
(C_ref = frozen AUDIT class, no sealed incidence realization; no multi-cell
physical complex exists), (t2) full D3 ("all common refinements", star-refined
atoms of unbounded facet count, F-2 bars finite pinning), (t3) loop-carrying
carriers (H^1 > 0), where dim H = 0 FAILS by construction and both the
AND-class collapse and C-D(i)'s uniqueness premise die — the build's own
TOY_SEPARATION says the same and is correct. NOTHING here transfers beyond the
five instances + composites by itself. My confirmations are confirmations of
FINITE EXACT LINEAR ALGEBRA and of quantifier honesty — not of any physical
pullback, any OBL-D discharge, any RA27-4/RA27-6 progress beyond the one
field-side leg, or any consumer question. eta_resp, the response topology,
tau_ker, RA27-6, E4, and the write-chain realization remain UNBUILT of record.
```

## FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS(subject 4a6718fe: the r_F^*
  certificate package stands — C-A/C-B/C-C/C-D replicated exactly with
  different algorithm routes and strengthened (C-D(ii) as a full matrix
  identity on all of C^1(K); C-D(i) closed unconditionally over Q); verbatim
  script re-run byte-identical, exit 0; 14/14 load-bearing spans rehashed
  MATCH; step choice verified sound against the POSED ordering at bytes and
  matching my pre-fixed independent identification; quantifier honesty
  verified — OBL-D OPEN, licensed-generator vs full-D3 gap CARRIED, C_ref
  audit-class, member-free; corrections C-1 (FORM span-table annotation:
  Q_flux formula outside the pinned span; dagger/plus transcription), C-2
  (mod-p leg redundancy, subsumed), C-3 (registration guidance on K-2's
  scoped "DISCHARGED"); none load-bearing.)
INDEPENDENCE = EXECUTED WITH HASHES(pre-build notes 3288f6da @ 19:29:55 CDT;
  own attempt 8c00facc / output 9275ed3b @ 19:32:31 CDT; build opened only
  after; my identification = the O7/RA27-4 kinematic common-refinement
  commutation fragment — the same object the build built; prediction P1
  realized.)
MY_INSTRUMENTS = +/-1-pivot division-free exact elimination (ranks; dim H = 0
  UNCONDITIONAL over Q on all carriers); spanning-tree potential integration
  (d0-square); shoelace triangle signs (A2-B sd*_2); dense integer matmul
  (all certificates; full-matrix forms); Fraction sparse solves (C-D(i)
  spots); fresh venv, stdlib only, no floats, exit 0 everywhere.
SEALS_VERIFIED = subject + 17 sources at path, all MATCH; 14/14 spans
  re-extracted and re-hashed MATCH; V011 = packet member aa7c6d49 only.
OBLD_STATUS_AFTER_AUDIT = OPEN, unchanged of record: clause 1 now POSABLE with
  its kinematic half certificate-backed at the licensed-generator quantifier;
  clause 2 OPEN (skeleton only); RA27-4 full square UNBUILT (eta_resp ABSENT,
  response topology UNNAMED); RA27-6/O8/O9/O10 UNBUILT; the licensed-generator
  vs full-D3 gap OPEN AND CARRIED (9a0c2045; F-2); flag
  cellulation_independence_proved = false STANDS.
SWEEP_CUTOFF_DECLARED = 2026-08-14 19:47:57 CDT (both corpus roots; same-night
  parallel-lane landings after the cutoff are outside this sweep by rule).
FENCES = alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false ; no register/road_/ledger/lens/plan/tracker/
  THE_HANDOFF file opened; no git; no existing file edited; ONE output + seal
  sidecar at the commission-distinct path, probed ABSENT before write (twice).
MACHINERY_USED_BY_ME = reads, shasum -a 256 at path, byte-span extraction and
  rehashing, and three exact scripts (own attempt pre-build; verbatim re-run;
  attack battery) — no numeric evaluation of any physical quantity, no member
  binding, no end test, no comparison to any measured constant.
ALL_RESULTS = CLAIMED until registrar handling.
```
