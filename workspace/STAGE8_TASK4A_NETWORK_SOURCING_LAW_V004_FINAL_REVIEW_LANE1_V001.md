# STAGE8 TASK 4A: NETWORK-SOURCING LAW V004 FINAL REVIEW (LANE 1 V001)

**REVIEW ARTIFACT -- NO RATIFICATION ACT PERFORMED**

```text
REVIEW_TARGET =
  STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V004.md
REVIEW_TARGET_SHA256 =
  69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08

REVIEW_STANDING = FINAL_ADVERSARIAL_REVIEW
REVIEW_CUSTODY = CODEX_LANE1_REVIEWING_CODEX_LANE2
TARGET_STANDING = PROPOSED_NOT_ADOPTED

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Preflight and custody

The preflight passed before the target was read.

| Item | Result |
|---|---|
| register head | `Q-342` |
| register SHA-256 | `ef5e9cb7d354e6c4870e540682be5c8698a2356e6796ff252e3c78409c75d47e` |
| target SHA-256 before reading | exact match: `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` |
| target sidecar | verified |
| locked-process SHA-256 | `f3bd9a62e172ba58667840e7717eb7ab8cd74be9fbe8d190802794ee5c3377ba` |
| seam review | `STAGE8_TASK4A_NETWORK_SOURCING_LAW_V003_RE_REVIEW_LANE1_V001.md`, SHA-256 `daec1110cefa6ec4328594c59a030a3447807c84acba33a0ed93f12d85643b2a` |
| carried source | V003, SHA-256 `51724fae30f6985d4dbf0b4df72a47d0c4ede8dfecdabfd8266d9a60e18296ec` |

No preflight failure occurred. The review therefore ran to verdict.

## 2. Executive determination

V004 survives every requested firing. Its live mathematical law is exactly
the Branch-B block reviewed in V003: the direct block comparison has zero
changed lines. Branch A has been removed from the live proposal and retained
only as an accurately scoped kill record and fixed-filtration lemma. The W3
composition law is repaired, all six missing maps remain explicit `TYPE-U`
doors, and the standing statements do not recreate the killed no-ignition
claim.

The new Lane-1 attack applies the coarse-versus-refined batching test that
killed Branch A to the surviving CTP construction. Branch B passes: branchwise
composition is canonical, the faithful U(1) character is multiplicative, and
charged-projector multiplication is associative. Fine and coarse batching
therefore give the same finite consumed object without choosing a filtration
member.

Nothing in this review upgrades the proposal to adopted or `TYPE-P` standing.

## 3. Verdict table

| Item | Verdict | One-line reason |
|---|---|---|
| F1 zero-delta claim | **PASS** | direct extraction and byte comparison of the two 176-line Branch-B blocks produced no diff |
| F2 Branch A record | **PASS** | no live Branch-A law remains; all kills, lemma scope, and both revival conjuncts are preserved exactly |
| F3 W3 correction | **PASS** | `rho_ML=rho_NL compose rho_MN` is typed correctly and both independent refutations remain intact |
| F4 six doors | **PASS** | all six doors are present, separately named, and still `NOT_OPENED / TYPE-U` |
| F5 regressions | **PASS** | one-edge, triple disposal, backward-branch sensitivity, identity extension, and reality all recompute correctly |
| F6 standing | **PASS** | no-ignition remains `NO_VERDICT / TYPE-U`; delay is authored; all listed selections remain absent; gates are clean |
| F7 fresh attack | **PASS** | coarse-versus-refined CTP batching is invariant by exact composition and character multiplicativity |

## 4. F1 -- zero changed lines in the live law

### 4.1 Compared regions

The complete V003 Branch-B block begins at its heading
`## 7. Branch B -- doubled CTP endpoint access` and ends immediately before
`## 8. Doors`. In the sealed files these are:

```text
V003: lines 619-794
V004: lines 282-457
```

The V004 region begins at the same Branch-B heading and ends immediately
before `## 7. Standing preserved`. Both regions contain 176 lines.

### 4.2 Direct comparison

The executed comparison was equivalent to:

```text
diff -u
  <V003 lines 619-794>
  <V004 lines 282-457>
```

It returned exit status zero and no output. This is stronger than a semantic
comparison: formulas, prose, tables, standing marks, and whitespace in the
incorporated block are byte-identical.

```text
BRANCH_B_FORMULA_DELTA = empty
BRANCH_B_SEMANTIC_DELTA = empty
BRANCH_B_CHANGED_LINES = 0
F1 = PASS
```

## 5. F2 -- Branch A is a kill record, not a second law

### 5.1 No live law content

The V004 header states:

```text
BRANCH_A = KILLED_BY_DOR015_FAMILY_NATURALITY
BRANCH_A_LIVE_LAW_CONTENT_IN_V004 = none
BRANCH_A_FIXED_FILTRATION_LEMMA = BANKED_ONLY
```

The only surviving Branch-A mathematics is in Section 3, explicitly headed
as a killed-law record. No Branch-A source map, exponent, amplitude, tower, or
network output occurs in the live Branch-B definition. `Hist_A^proj` occurs
only in the banked statement `(BANK-A1)`.

### 5.2 Kill record accuracy

The V003 re-review's two decisive witnesses are preserved without softening:

1. Coarse reciprocal insertion has
   `ker(C_coarse^T)=span{(1,1)}` and admits `s(1,1)`.
2. Edge-by-edge insertion gives a one-edge prefix `(s)` with
   `B_1^T(s)=(-s,s)`, so conservation forces `s=0`.

Thus the physical nonzero source-space dimension changes with the unselected
filtration member. V004 records exactly the prior `TYPE-R` conclusion rather
than retaining a preferred coarse member.

### 5.3 Banked lemma and revival condition

The banked lemma is limited to an independently fixed filtration:

```text
q in Hist_A^proj
  iff q_base in ker(B_base^T)
      and r_j in ker(C_j^T) for every fixed increment j.
```

V004 expressly denies that this fiberwise theorem is a family-natural law.
Its revival condition is the prior review's condition verbatim:

```text
a ratified filtration/member rule PLUS a family-naturality theorem
```

Both conjuncts are required and both are absent.

```text
BRANCH_A_LIVE_CONTENT = none
BRANCH_A_KILL_RECORD = accurate
BRANCH_A_BANKED_LEMMA_SCOPE = fixed-filtration-fiberwise-only
BRANCH_A_REVIVAL_CONDITION = exact
F2 = PASS
```

## 6. F3 -- W3 correction and the two refutations

For `L<=N<=M`, V004 now states the restriction identity with the correct
domains and order:

```text
rho_ML = rho_NL compose rho_MN.
```

Here `rho_MN:C_M->C_N`, `rho_NL:C_N->C_L`, and
`rho_ML:C_M->C_L`, so the displayed composite is well typed.

The two refutations are preserved independently of that functoriality:

### 6.1 Strong-history-square refutation

For the admitted triple `q_M=(1,0,-1)`, raw coordinate restriction gives

```text
rho_raw(q_M)=(1,0),
```

while the W3 adjoint image gives

```text
rho_W3(q_M)=(1/2,1/2).
```

The two results are unequal. Hence raw history restriction cannot be silently
identified with the W3 adjoint map.

### 6.2 Global-C5 refutation

For the dangling-edge extension, the global conserved vector

```text
q_M=epsilon(0,0,1,1)
```

has nontrivial raw restriction

```text
rho_raw(q_M)=epsilon(0,0,1),
```

but W3 adjoint image zero. Global conservation therefore does not make the
W3 image reproduce the raw restricted C5 character.

V004 keeps the lawful source restriction and W3 functoriality while leaving a
history-level W3 lift unbuilt.

```text
W3_ADJOINT_FUNCTORIALITY = PASS
W3_STRONG_HISTORY_SQUARE = REFUTED | TYPE-R
W3_GLOBAL_C5_SQUARE = REFUTED | TYPE-R
W3_COMPATIBLE_HISTORY_RESTRICTION = NOT_BUILT / TYPE-U
F3 = PASS
```

## 7. F4 -- six doors remain closed

The six required doors are all present and distinct:

| Door | V004 standing | Required object |
|---|---|---|
| Branch-B scalarization | `NOT_OPENED / TYPE-U` | family-natural endpoint-transport-to-V007 scalar map |
| Branch-B network `R` | `NOT_OPENED / TYPE-U` | ratified network bilocal receiver/emitter map |
| port/access to write history | `NOT_OPENED / TYPE-U` | time-oriented next-history update |
| joint two-system contraction | `NOT_OPENED / TYPE-U` | joint state/effect and interaction contraction |
| completed physical contraction | `NOT_OPENED / TYPE-U` | completed-domain contraction with topology and finite squares |
| W3-compatible history restriction | `NOT_OPENED / TYPE-U` | history-level lift commuting with W3 and raw restriction |

The exact DoR-009 finite trace is used only at Level III-a of one finite
receiver. V004 does not relabel it as the missing network contraction,
completed contraction, scalar source, or network `R` receiver.

```text
SIX_DOORS_PRESENT = true
SIX_DOORS_OPENED = 0
SIX_DOORS_DROPPED = 0
F4 = PASS
```

## 8. F5 -- independent regression firing

### 8.1 R7 one-edge attack

For a connected one-edge stage with coefficient `s`, incidence conservation
has the form

```text
B_1^T(s)=(-s,s)=0 iff s=0.
```

The scalar cycle source is therefore exactly zero. V004's live law emits no
scalar on this stage. Its possible nonidentity output is the ordered endpoint-
covariant CTP pair `(T_+,T_-)`, which is the ratified open-path type and is
not promoted to a require-side scalar.

```text
R7_ONE_EDGE_SCALAR = 0
R7_ONE_EDGE_LIVE_EXPORT = endpoint-covariant-pair-only
R7 = PASS
```

### 8.2 S8-A triple disposal

The triple `(1,0,-1)` appears only in the killed Branch-A/W3 record. No map in
the live law consumes it. Its exact mismatch remains

```text
raw restriction = (1,0),
W3 image        = (1/2,1/2).
```

This is evidence against reviving Branch A, not an input to Branch B.

```text
S8A_TRIPLE_CONSUMED_BY_V004 = false
S8A = PASS
```

### 8.3 S8-B fixed-forward/vary-backward attack

Fix `T_+` and choose distinct backward transports `T_-` and `T_-'`. Then

```text
(T_+,T_-) != (T_+,T_-'),
T_-^dagger T_+ != (T_-')^dagger T_+
```

outside the ordinary faithful-character kernel. The ordered pair and the
relative CTP endomorphism therefore retain backward-branch data. The live law
does not collapse to a forward-only morphism.

```text
S8B_FIXED_FORWARD_VARY_BACKWARD = PASS
```

### 8.4 Reality

The ratified branch exchange/involution gives

```text
R_CTP[Theta h]=R_CTP[h]^dagger,
chi(R_CTP[Theta h])=conjugate(chi(R_CTP[h])).
```

For real symbolic `p`, this propagates exactly to

```text
F_N[Theta h]=F_N[h]^dagger,
A_N[Theta h]=conjugate(A_N[h]).
```

No endpoint frame or orientation member is selected in this computation.

### 8.5 Identity zero-extension

Appending an identity cell gives `z_+=z_-=1`, hence relative character one:

```text
Z_(N+1)^CTP=Z_N^CTP,
F_(N+1)^B=F_N^B,
A_(N+1)^B=A_N^B.
```

The extension adds no scalar source, rank choice, frame choice, or missing
contraction.

```text
REALITY_REGRESSION = PASS
IDENTITY_ZERO_EXTENSION = PASS
F5 = PASS
```

## 9. F6 -- standing and selection audit

### 9.1 No-ignition

V004 retains the required standing verbatim:

```text
AUTONOMOUS_READY_NETWORK_NO_IGNITION = NO_VERDICT / TYPE-U
missing: the port-to-write-history update and a zero-fixed-point certificate
```

It proves only the first identity emission and the conditional identity tower
for an externally stipulated all-zero history sequence. It makes no autonomous
all-tier recurrence claim.

### 9.2 Delay

Positive delay is inherited from the no-post-output-supplementation rule. The
exact value one tier is not derived: every fixed positive integer delay is a
counterfamily. V004 labels one-tier delay `AUTHORED_NOT_UNIQUELY_FORCED`.

### 9.3 No selections or forbidden evaluations

The live construction selects none of the following:

```text
rank or rank ratio,
orientation member,
endpoint frame,
filtration member,
missing contraction.
```

Branch B survives by the prior family-wide proof; it is not chosen for an
output. Branch A is removed by the DoR-015 naturality counterexample. The
proposal keeps every `p_i` symbolic and performs no root, `K_*`, alpha, or
measured-constant evaluation.

The proposal is marked `PROPOSED_NOT_ADOPTED` in its controlling flag block,
its inherited Branch-B block, and its regression block. Its terminal gates
are all false.

```text
TYPE_P_UPGRADE = none
TARGET_OUTPUT_REASONING = none
SELECTIONS = none
GATES_CLEAN = true
F6 = PASS
```

## 10. F7 -- fresh Lane-1 attack: coarse-versus-refined CTP batching

This attack is not one of V004's two self-attacks. It applies the exact
coarse-versus-refined pressure that killed Branch A to the consolidated live
law.

### 10.1 Attack statement

Take two consecutive doubled histories `h_1` and `h_2`. Compare:

1. a fine presentation retaining them as two tier factors; and
2. a coarse presentation replacing them by `h_2 compose h_1`.

If the live output depends on this batching, V004 has inherited an unratified
filtration choice and is killed.

### 10.2 Level-I composition

Branchwise transport composes canonically:

```text
T_sigma[h_2 compose h_1]=T_sigma[h_2]T_sigma[h_1].
```

Thus the fine Level-I pair maps to the coarse Level-I pair by the existing
composition law, with no frame or member choice.

### 10.3 Level-II and Level-III comparison

Write `T_(sigma,a)=T_sigma[h_a]`. The coarse relative endomorphism is

```text
R_coarse
 =(T_(-,2)T_(-,1))^dagger(T_(+,2)T_(+,1))
 =T_(-,1)^dagger T_(-,2)^dagger T_(+,2)T_(+,1).
```

On the ratified compact U(1) carrier the factors commute, so

```text
R_coarse
 =(T_(-,1)^dagger T_(+,1))(T_(-,2)^dagger T_(+,2))
 =R_1 R_2.
```

Equivalently, without using factor commutation before the trace, the faithful
one-dimensional character is multiplicative and scalar-valued:

```text
chi(R_coarse)
 =chi(T_(-,1)^dagger)chi(T_(-,2)^dagger)
  chi(T_(+,2))chi(T_(+,1))
 =chi(R_1)chi(R_2).
```

Therefore the exact finite C5 trace agrees under both presentations:

```text
Z_coarse^CTP=Z_1^CTP Z_2^CTP=Z_fine^CTP.
```

### 10.4 Tower comparison

The shared-projector law is associative:

```text
(P_0+Z_1P_ch)(P_0+Z_2P_ch)
 =P_0+(Z_1Z_2)P_ch.
```

Hence coarse and fine batching also yield the same `F`, `A`, and ordered
two-system tower entries. No joint network amplitude product is formed.

### 10.5 Attack result

Branch A failed because its admitted nonzero source space changed between
coarse and edge-by-edge realization members. Branch B has no analogous
dimension jump: canonical branch composition and multiplicativity identify
the actually consumed finite objects. The live law is therefore batching-
natural on this test.

```text
FRESH_ATTACK = COARSE_VERSUS_REFINED_CTP_BATCHING
LEVEL_I_COMPOSITION = canonical
LEVEL_III_SCALAR_DELTA = 0
TOWER_DELTA = 0
FILTRATION_MEMBER_REQUIRED = false
F7 = PASS
```

## 11. Final determination

All seven requested attacks pass. The result is only review readiness: the
principal retains the ratification decision, and all six doors remain open
work rather than implicit content of the proposed law.

```text
V004 = SURVIVES
READY_FOR_RATIFICATION_RULING = yes

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
