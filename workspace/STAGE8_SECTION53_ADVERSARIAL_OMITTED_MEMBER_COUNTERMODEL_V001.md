# Stage 8 Section 5.3 Adversarial Omitted-Member Countermodel V001

LANE: CODEX 1. CHARTER: Paste 240. DATE: 2026-08-01.
STATUS: ADVERSARIAL COVERAGE CHECK / NO ROOT EVALUATION / NO COUPLING
EVALUATION.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
coupling_evaluation_authorized = false
production_authorized = false
```

No alpha, `kappa_record`, `kappa_Thomson`, coupling, scale, root,
eigenvalue, beta function, `E_R`, `T_R`, `k_R`, or absolute interval was
computed or evaluated. No measured constant was compared. The
Misner-Sharp/Brown-York fork was not resolved. `a32_holdout/custodian_private/`
was not opened.

## Lead

An omitted non-action-form member is constructible at schema level:

```text
CTP_PRESTATE_EFFECT_BRANCH_MUTATION
```

Definition: a target-independent complete source-record-field CTP producer
variant whose action/evolution, geometry, clock, measure, regulator, and
action-partition are held fixed, but whose common-origin state/effect/domain
package differs: `rho_pre`, admitted record effects, or their domains.

This is not a concrete built producer. It is a constructible omitted-member
schema. A concrete member would require two full common-origin microscopic
producers satisfying the same upstream fences and differing in the P5
state/effect/domain package before any response/root value is inspected.

```text
concrete_omitted_member_built = false | TYPE-U | would-build:
  exhibit two target-independent complete source-record-field CTP producers
  with fixed action/evolution, geometry, clock, measure, regulator, and
  action-partition, but inequivalent common-origin rho_pre/effect/domain
  packages, then show structurally that the induced CTP functional differs
  before root evaluation

omitted_member_schema_found = true
schema_name = CTP_PRESTATE_EFFECT_BRANCH_MUTATION
```

Consequence: a Section 5.3 family freeze that covers only `S_0`, `S_1`,
higher-derivative source terms, finite causal updates, and record-curvature or
dissipative action-form classes is not coverage-complete. It must either add
the state/effect/domain axis, or derive an upstream exclusion theorem showing
that no inequivalent target-independent state/effect/domain branch can survive.

## Independence And Scope

Roots entered:

```text
/Users/bgm/.codex/attachments/15f839ff-0d04-4040-8444-dafca06b6945/pasted-text.txt
/Users/bgm/Documents/New project/gravity_emergence_evidence_program
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha-program-archive/cleanroom_output
```

Exclusions:

```text
a32_holdout/custodian_private/ was not opened
current admitted-family census build artifacts were not opened
no git command was run
```

Independence note: a term search returned census-named file paths by filename.
Those artifacts were not opened. A previously existing action-form member
identity artifact was opened during a narrow search but is not cited as
authority for this determination. The determination below relies on the primary
Section 5.3/preregistration sources, the current producer/CTP sources, and the
external `cleanroom_output` Family-D record.

Queries used, case-insensitive and word-boundaried where applicable:

```text
response-changing, mutation, admission, admitted, target-independent,
rho_pre, pre-state, state, effect, record effect, state/effects,
quotient, measure, branch metric, CTP branch, completed source-record-field,
complete microscopic, action-form, action-partition, finite causal update,
higher-derivative, record-curvature, dissipative, scalar, discrete, operator,
algebraic, admissibility, categorical
```

## 1. Section 5.3 Admission Criterion

The live preregistration requires:

```text
"one complete microscopic Gamma_K functional and measure"
...
"a mutation audit over admitted geometry, clock, measure, regulator, and
action-partition alternatives"
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/provenance/primitive_record_cell_selection_preregistration_v002.json:15-23`.

The same preregistration's success rule is broader:

```text
"The result passes only if the forward-sealed microscopic problem computes one
simple positive K_star and every admitted target-independent mutation either is
physically equivalent or is excluded by an upstream principle."
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/provenance/primitive_record_cell_selection_preregistration_v002.json:34-35`.

The construction spec restates the five named channels:

```text
Before any root solve, the construction must audit admitted mutations across
five channels:

1. geometry;
2. clock;
3. measure;
4. regulator;
5. action-partition.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:530-539`.

Its pass condition again uses the broader language:

```text
The pass condition is that every admitted target-independent mutation is
either physically equivalent under a derived equivalence relation or excluded
by an upstream principle before response/root evaluation.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:557-559`.

The uniqueness gate then requires:

```text
The uniqueness gate passes only if the completed residual has one simple
positive root and no inequivalent admitted positive root or continuous modulus.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_GAMMA_K_CONSTRUCTION_SPEC_V001.md:575-581`.

Therefore the admissibility test used here is:

```text
ADMIT if a construction is:
  (1) microscopic and target-independent,
  (2) part of the completed Gamma_K/CTP producer or its admitted mutation
      family,
  (3) response/root-facing before any value is inspected,
  (4) inequivalent unless a derived equivalence relation identifies it, and
  (5) not excluded by an upstream principle before response/root evaluation.
```

The five listed channels are named audit channels. The broader phrase "every
admitted target-independent mutation" is what lets an omitted non-action-form
axis matter; if a successor spec intends exactly the five channels and nothing
else, it must say so and derive why response-facing producer ingredients outside
those five are not admitted mutations.

## 2. Candidate Object

The normalized CTP functional consumes `rho_pre` directly:

```text
Let `rho_pre` be a positive trace-class initial density operator on the full
source-record-field Hilbert space, normalized by `Tr rho_pre=1`, and let the
inclusive final effect be the identity.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v004.md:17-25`.

It then defines:

```text
Z_inc[J,R;g_+,g_-]
  = Tr_full { I_final T_C exp[(i/hbar)
      {S_CTP + J_I A^I + (1/2)A^I R_IJ A^J}] rho_pre }.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v004.md:40-55`.

The complete history functional also consumes `rho_pre` and record effects:

```text
Z_r[A_+,g_+;A_-,g_-]
  = Tr(E_r U_BR[A_+,g_+] rho_pre U_BR[A_-,g_-]^dagger)
    / Tr(E_r U_BR[0,g_0] rho_pre U_BR[0,g_0]^dagger).
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_complete_boundary_transition_functional_principle_v002.md:16-29`.

That same source says the response kernel comes from this functional:

```text
The retarded mixed kernel `D_R^(-1)` contains the coherent public response.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_complete_boundary_transition_functional_principle_v002.md:37-65`.

The hard gate requires the state/effect/domain package to come from one
microscopic operator:

```text
U_BR, rho_pre, every admitted record effect, and their domains follow from one
complete microscopic operator;
the mixed response kernel, noise kernel, and first-record overlap are derived
from that same functional;
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_complete_boundary_transition_functional_principle_v002.md:106-118`.

The current rank-1 producer spec carries this as P5:

```text
P5. A positive normalized `rho_pre` on the completed object and admitted record
    effects `E_r`, with domains, all supplied by the same microscopic source as
    P0.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:247-253`.

It also gives a failure-capable test:

```text
Failure condition: `rho_pre`, record effects, effect domains, and operator
domains are not positive/normalized/compatible on one completed object, or are
not supplied by the same microscopic source as the candidate dynamics.
```

Source:
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:343-358`.

Thus a mutation of `rho_pre`, admitted record effects, or their domains is not
an external preference or a diagnostic convention. It changes an object inside
the trace functional from which the raw contour correlator and response-facing
kernels are derived.

## 3. Response-Changing Type Claim

This check does not evaluate any response value. The claim is structural.

Changing the P5 state/effect/domain package changes the functional being
differentiated unless a derived equivalence relation proves otherwise, because:

1. `Z_inc` is a trace with `rho_pre` inside the trace
   (`primitive_record_cell_selection_principle_v004.md:40-55`).
2. `Z_r` is a trace with both `E_r` and `rho_pre`
   (`primitive_complete_boundary_transition_functional_principle_v002.md:16-29`).
3. The coherent public response is read from the retarded mixed kernel of that
   same functional (`primitive_complete_boundary_transition_functional_principle_v002.md:37-65`).
4. The hard gate demands those objects follow from one complete microscopic
   operator and does not derive uniqueness for them
   (`primitive_complete_boundary_transition_functional_principle_v002.md:106-118`).

External `cleanroom_output` records the same non-action-form freedom:

```text
The stationary quasifree positive-energy state (=> Feynman +i0) is a
disclosed adopted branch input; the full Schwinger-Keldysh matrix is
open; alternative states/contours are not excluded.
```

Source:
`/Users/bgm/MB Work/alpha-program-archive/cleanroom_output/05_ALTERNATIVE_EXHAUSTION.md:34-39`.

and:

```text
The stationary quasifree positive-energy state (=> Feynman +i0) is a
disclosed adoption; the full CTP matrix is open. Decider: a derivation of
the record-compatible state class with uniqueness, or an explicit adopted-
branch declaration carried as BRANCH_CONDITIONAL scope.
```

Source:
`/Users/bgm/MB Work/alpha-program-archive/cleanroom_output/NEEDS_THEORY_DECISION.md:57-62`.

Typed result:

```text
state_effect_branch_structurally_response_facing = true

state_effect_branch_concrete_response_difference_evaluated = false |
  TERMINAL_FENCE | reason: Paste 240 forbids response/root evaluation; the
  response-changing claim here is structural only

state_effect_branch_unique_selector_found = false | TYPE-S |
  roots: parent gravity root, current cleanroom root, archive cleanroom_output |
  excl: a32_holdout/custodian_private; current census-build artifacts not opened |
  query: rho_pre, record effect, state/effects, CTP state/contour, complete
  microscopic operator, positive normalized, admitted record effect |
  reason: searched text supplies role and hard gate, plus external open-family
  records, but no uniqueness derivation or exclusion theorem for P5
```

## 4. Outside The Five Known Slots

The named Section 5.2 channels are geometry, clock, measure, regulator, and
action-partition. The task's minimal known set additionally lists concrete
`S_0`/`S_1` action-partition mutations and three open action-form-shaped
classes: higher-derivative source terms, different finite causal updates, and
record-curvature/dissipative mutations.

`CTP_PRESTATE_EFFECT_BRANCH_MUTATION` is outside those slots under a
word-boundaried type comparison:

```text
geometry              no: keep cell/spacetime geometry fixed
clock                 no: keep proper-time/clock data fixed
measure               no: keep invariant contour/spacetime measure fixed
regulator             no: keep regulator prescription fixed
action-partition      no: keep action/evolution and partition fixed
higher-derivative     no: no derivative source term added
finite causal update  no: no update rule changed
record-curvature      no: no curvature term added
dissipative           no: no nonunitary/dissipative dynamics added
```

It is a state/effect/domain branch in the completed source-record-field CTP
producer. If a successor census folds it into "measure" or "action-partition,"
that is an added classification rule and must be stated; the sealed channel
names do not say it.

Typed result:

```text
state_effect_branch_covered_by_five_named_channels = false | TYPE-S |
  roots: parent gravity root, current cleanroom root, archive cleanroom_output |
  excl: a32_holdout/custodian_private; current census-build artifacts not opened |
  query: rho_pre, record effect, state/effects, geometry, clock, measure,
  regulator, action-partition, higher-derivative, finite causal update,
  record-curvature, dissipative |
  reason: no searched text states that P5 state/effect/domain mutations are
  included in any of the five named channels or three open action-form classes
```

## 5. Attack On The Candidate

The candidate would fail if any of the following is derived:

1. a theorem that one complete microscopic operator uniquely fixes
   `rho_pre`, every admitted record effect, and all their domains before
   response/root evaluation;
2. a theorem that all P5 state/effect/domain alternatives are physically
   equivalent for the raw contour correlator, mixed response kernel, noise
   kernel, and first-record overlap;
3. a successor Section 5.3 specification that explicitly narrows "every admitted
   target-independent mutation" to the five named channels and derives why
   state/effect/domain branches are inadmissible rather than merely omitted;
4. a proof that any target-independent P5 variation necessarily changes
   geometry, clock, measure, regulator, or action-partition, so the present
   "outside five slots" typing is false.

Search did not find such a theorem.

```text
P5_exclusion_theorem_found = false | TYPE-S |
  roots: parent gravity root, current cleanroom root, archive cleanroom_output |
  excl: a32_holdout/custodian_private; current census-build artifacts not opened |
  query: rho_pre uniqueness, record-compatible state class with uniqueness,
  every admitted record effect, physical equivalence, upstream exclusion,
  target-independent state selector, CTP state/contour |
  reason: the located texts require P5 from one complete microscopic operator
  and record the state/contour family as open/adopted, but do not derive
  uniqueness, equivalence, or exclusion
```

## 6. Verdict

```text
ADVERSARIAL_RESULT = OMITTED_MEMBER_SCHEMA_FOUND
omitted_member_schema = CTP_PRESTATE_EFFECT_BRANCH_MUTATION
concrete_member_status = false | TYPE-U | would-build: two complete
  target-independent source-record-field CTP producers differing in P5
  state/effect/domain package, with structural response difference shown before
  root evaluation
coverage_consequence = Section 5.3 census cannot freeze as complete unless it
  covers the P5 state/effect/domain axis or derives an upstream exclusion
```

This is not a computation and not a value claim. It is a coverage obstruction:
the response-facing microscopic producer includes state/effect/domain data that
the five named audit slots do not cover on current sealed text.

