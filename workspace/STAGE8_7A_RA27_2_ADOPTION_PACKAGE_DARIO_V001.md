# STAGE 8 / 7A / STEP 8 — THE RA27-2 ADOPTION PACKAGE

**Lane:** Dario (Opus 5, cross-family from the Codex build lanes)
**Date:** 2026-08-08
**Scope:** PASTE 725 only — JJ1 restate, JJ2 the free choices, JJ3 the empty slot
**Custody:** archive-side read and write; the registrar owns any mirror
**Governing:** `DECISION_REFINEMENT_LIMIT_SUBJECT_2026-08-08.md` `112e6acb…`; frozen
prereg `9f0d12b4…`; carrier `5f4979d5…`
**Charge:** re-derive, not trust. **NOTHING IS ADOPTED HERE.** The recommendation slot
is left empty by instruction and by preference.

## Lead determination

```text
THE SUBJECT RULING NARROWS THE PACKAGE MATERIALLY.  Three changes, all from bytes:

  1. THE MEASURE HALF IS ALREADY DONE.  An intensive coefficient needs a measure
     with a naturality law.  Both are DERIVED of record: the per-cell measure is
     "forced, not chosen", and its coframe transformation "is already a pullback
     ... exactly the naturality law a measure needs. The measure's own stitching
     is done."  716's fork was written as though the whole geometric package were
     free.  Under the ruling, it is not.

  2. THE INDEX'S MISSING CONTENT IS NAMED, IN THREE ITEMS, BY SEALED TEXT.  A
     category Ref_0 is BUILT and directed by everything except geometric
     refinement.  Its stated lack: its arrows "do not say that cells shrink, that
     two complexes cover the same region, or that a new cell refines an old one."
     The free choice is not "invent a relation" -- it is those three properties.

  3. THE SHAPE CONDITION NOW GATES RA27-2.  It did not before.  An intensive
     subject makes the boundary-to-four-volume ratio the governing condition, and
     the held conflict is precisely about admitting slivers, whose risk the
     conflict determination itself states in four-volume terms.  I report this
     as a promotion of a held item, not as a resolution of it.

FREE_CHOICES = 2 (the three-property index; the shape condition).
BARRED = 2, both by sealed TYPE-R.
LAMBDA_BETA = status UNCHANGED, and I record why the tempting contrary reading
              fails: the corpus never identifies the ruled subject with the
              "order-two local symbol" that lambda moves, and the datum needed to
              settle it is absent.  I decline to compute it.
```

---

## 0. Preflight

[PROVABLE] `relay_outbox/725_ACK.md` was written **before** source work. Lane guard:
the header names **DARIO**. Read only after its sidecar verified:

```text
relay_inbox/RELAY_PASTE_725_RA27_2_ADOPTION_PACKAGE_DARIO_V001.md
  8ab77fe3c2a71855615f8b3a420196e4821a7201b26d9f58d12578c3069961cb   shasum -c OK
```

[PROVABLE] `STAGE8_7A_RA27_2_ADOPTION_PACKAGE_DARIO_V001.md` and its seal sidecar were
probed before the write and returned ABSENT.

### 0.1 Sources verified before use

```text
RULING supervision/DECISION_REFINEMENT_LIMIT_SUBJECT_2026-08-08.md  112e6acb…  sidecar OK
JREF   STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md   8dd59b35…
4d     STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md 430f0971…
V011   review_packets/.../BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md aa7c6d49…
BATT   STAGE8_T7_CODEX_BATTERY_TIER2_RETURNS_V001.md                 14ddfc15…
DEC    supervision/RESULT_STITCHING_RULE_DECOMPOSED_2026-07-30.md    578eb5e9…
CREF3  STAGE8_T7_CREF_VS_D3_FACTUAL_DETERMINATION_V001.md            6934f858…
DUH    STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_..._SCHEMA_SPEC_V001.md 4fe8d42d…
D012   STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md                    74bbb7aa…
716    STAGE8_7A_RA27_2_SAME_REGION_DARIO_V001.md                    5234141f…  (mine)
724    STAGE8_7A_TAU_DENSITY_REFERENCE_DARIO_V001.md                 6441b787…  (mine)
PREREG STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md             9f0d12b4…  unchanged
```

### 0.2 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No common cell formed, no junction map evaluated, no member bound, no fixed point, no
end test, no numeric evaluation, no measured constant. **Nothing adopted.** No
register, plan, tracker, git, commit or push action. The frozen preregistration was not
altered.

---

## 1. JJ1 — WHAT RA27-2 MUST SUPPLY, UNDER THE RULING

### 1.1 The ruling's operative content

[PROVABLE] `RULING` `112e6acb…`: the subject is **the intensive quadratic
coefficient** — V011's own invariance subject, *"computed from the compatible response
(the response acknowledged as source, NOT identified with the coefficient; the
completion is not the subject …)"*. Consequence 1: the sealed density instance is the
limit's sense for the coefficient, **pending only its index**. Consequence 2: supplying
that index **is** RA27-2's same-region relation.

### 1.2 The restatement

```text
RA27-2 MUST SUPPLY, under the subject ruling:

  THE DIRECTED INDEX of the intensive quadratic coefficient's refinement limit
  -- a directed system of admissible complexes and refinement arrows on which:

    (i)   the coefficient is defined at each stage and comparable across arrows;
    (ii)  V011's condition is expressible and testable per elementary refinement
          -- invariance up to a boundary term whose ratio to four-volume tends
          to zero;
    (iii) cofinal subfamilies give the same limit (4d's R4);
    (iv)  the same system serves as J2's re-presentation family, since J2
          quantifies invariance over exactly the re-presentations this index
          names.

  It is ONE object serving three rails: A27's root, the coefficient limit's
  index, and J2's quantifier domain.
```

### 1.3 What the ruling makes DERIVED — the measure half

[PROVABLE] `BATT` T11, `14ddfc15…[3311,3668)`:

```text
DERIVED:
  <xi, xi>_(2,ell) = sum_(mu<nu) V_cell / (ell_mu^2 ell_nu^2) xi_(mu nu)^2
  xi_(mu nu) = ell_mu ell_nu F_(mu nu)
  each cell contributes  V_cell sum F^2
  general coframe = pullback by wedge^2(e^-1) times |det e|
NO inverse weight, NO ad hoc weight, NO residual shape scalar — which is
the substantive content: the measure is forced, not chosen.
```

[PROVABLE] `DEC` `578eb5e9…[1313,1697)` draws the consequence explicitly:

```text
The per-cell contribution is already in additive, extensive form — `V_cell sum F^2` —
which is what a density looks like. And the transformation under a general coframe is
already a **pullback**, `wedge^2(e^-1) |det e|`, which is exactly the
naturality law a measure needs. **The measure's own stitching is done.**
```

[PART-PROVABLE] **This is the ruling's first narrowing.** An intensive coefficient
needs two things a bare combinatorial index cannot give: a per-cell measure, and a
naturality law for it under change of coframe. Both are derived and sealed. So the
index does **not** have to supply, adopt, or choose a measure — it has to be a system
the derived measure already transforms correctly along.

[YOURS] My 716 fork treated RA27-2 as though the entire geometric package were free,
and listed effort accordingly. Under the ruling that is too pessimistic by one whole
component, and the component is the one a reader would most expect to be hard.

### 1.4 What the ruling leaves FREE — and the corpus names it in three items

[PROVABLE] A category is already **BUILT**. `JREF` `8dd59b35…[11610,11743)`:

```text
identity arrows;
certified signed relabelings/isomorphisms;
already-ratified rank-preserving W3 restriction/inclusion squares. (D2-2)
```

with `8dd59b35…[12001,12049)`:

```text
FINITE_REALIZATION_SKELETON_J_0 = BUILT / TYPE-P
```

[PROVABLE] And its exact lack is stated, `8dd59b35…[12055,12336)`:

```text
But `Ref_0` is not directed by geometric refinement.  Its W3 arrows preserve
the declared rank/scoped source structure; they do not say that cells shrink,
that two complexes cover the same region, or that a new cell refines an old
one.  Therefore `J_0` cannot be relabeled `J_ref`.
```

[PART-PROVABLE] **This is the ruling's second narrowing, and it is the sharpest thing
in the package.** The free choice is not "author a same-region relation from nothing".
It is: **what must be added to the already-built `Ref_0` so that it becomes directed by
geometric refinement**, and the corpus names the missing content in three items:

```text
P1  cells shrink                          (a size/ordering statement on arrows)
P2  two complexes cover the same region    (the same-region relation proper)
P3  a new cell refines an old one          (a subordination statement)
```

[YOURS] `P2` is the object my 716 artifact named and the relay has been calling
RA27-2's ground. `P1` and `P3` are its neighbours, and I record all three because the
sealed sentence gives three and a package that quietly delivered one would be
under-specified against its own source.

### 1.5 The forcing re-run, where the ruling changed inputs

[PROVABLE] Re-checked against 716's findings, changing only what the ruling changes:

| 716 finding | Does the ruling change its input? | Status now |
|---|---|---|
| G1/G2 licensed, record-native, smooth-free | no | **unchanged** |
| BUILT arrows invertible-only, cannot relate distinct cellulations | no — and `Ref_0` `[12055,12336)` states it independently and more precisely | **unchanged, corroborated** |
| G3 barred as a source under DoR-007 | no | **unchanged** (span at §2.3) |
| Free/formal subdivision category refuted | no | **unchanged** (span at §2.3) |
| Uniqueness refuted by the (D2-4) `lambda` countermodel | **possibly** — see §2.4 | **unchanged, and the contrary reading fails for a named missing datum** |
| The measure is part of the free package | **YES** | **superseded** — derived; §1.3 |
| The shape condition gates only the D3 spec later | **YES** | **superseded** — it gates here; §2.2 |

[PROVABLE] Two 716 conclusions therefore stand and two are superseded. Nothing in the
ruling touches the two TYPE-R bars or the invertibility argument.

---

## 2. JJ2 — THE FREE CHOICES

### 2.1 FC-1 — the three-property index

```text
WHAT A LAWFUL ADOPTION MUST FIX
  A rule for each of P1 (cells shrink), P2 (same region), P3 (refines), such
  that the resulting system is DIRECTED and admits cofinal subfamilies, and
  such that the derived per-cell measure transforms along its arrows by the
  already-sealed pullback law.

SMALLEST SUFFICIENT ADOPTION
  P2 alone does not suffice: without P1/P3 the relation says two complexes are
  co-regional but not which refines which, and a limit needs a direction.  The
  smallest sufficient adoption is P2 TOGETHER WITH a subordination rule
  (P3) that orients it; P1 then follows as the size statement P3 induces, or
  must be adopted alongside if it does not.
  I state this as a structural reading of the sealed sentence, not as a theorem.

DOWNSTREAM COMMITMENTS
  on J2       -- J2's re-presentation family becomes exactly this system; J2
                 acquires a domain and, with the ruled sense, a truth condition.
                 The R9-JII carrier stays PENDING on its common-cell quantifier
                 regardless: this does not make J2 runnable.
  on T_ref    -- 4d's would-build field 1 (Ref) is discharged; fields 4-8 remain.
  on RA27-3..6 -- RA27-3's transports get their generators; RA27-4's
                 common-refinement square gets its cospans; RA27-5's "every
                 elementary refinement on cofinal families" gets its quantifier
                 domain; RA27-6 gets both legs of every cospan.  The chain
                 unblocks in order, nothing in it is discharged by this adoption.
  on S5.3     -- the burden carries, per the ruling's consequence 4: an
                 instantiated coefficient limit must PROVE its defining
                 convergence.  Adopting an index does not discharge (H1)/(H2)
                 and may not be used to.
```

### 2.2 FC-2 — the shape condition, which now gates here

[PROVABLE] The held conflict, `CREF3` `6934f858…[177,391)`, is `C_ref`'s shape-regular
restriction against D3's breadth — all common refinements **including slivers** — with
the spec HELD and PRODUCTION PROHIBITED, *"and per the principal's instruction it is
HIS to resolve."*

[PROVABLE] `CREF3` `6934f858…[7118,7486)` states the sliver risk in four-volume terms:

```text
IF the principal resolves toward D3's breadth: the spec proceeds as Codex
wrote it, targeting all common refinements INCLUDING slivers, and Codex's
sharp conditional governs — if a sliver of volume eps*|C|_4 produces an
O(|C|_4) response defect, the object FAILS and recast Q6 becomes
ILL-POSED rather than merely unproved. That is a FINDING, to be reported
as such.
```

[PART-PROVABLE] **Under the subject ruling this gates RA27-2, and it did not before.**
V011's condition on the ruled subject is invariance *"up to a boundary term whose ratio
to four-volume tends to zero"*. Whether that condition can hold on the index is
precisely a question about which refinements the index admits — and the held conflict
is exactly the question of whether slivers are admitted. The conflict determination
states its own risk in the same ratio the ruled subject is defined by.

[YOURS] So the honest answer to JJ2's question is **gates here** — but with two limits
I will not blur. First, this is a *promotion*, not a resolution: the conflict remains
the principal's, PRODUCTION remains PROHIBITED, and nothing in this package narrows
D3 or adopts a shape condition. Second, I have not shown that a sliver *does* break
the ratio condition — `CREF3` states it conditionally (*"if a sliver … produces an
O(|C|_4) response defect"*) and that conditional is unresolved. What I claim is that
the ruling moves the question from downstream of RA27-2 to inside it.

### 2.3 Barred candidates — two, both by sealed TYPE-R

[PROVABLE] **B-1: the smooth-preserving common-refinement family, as a source.**
`4d` `430f0971…[20690,20930)`:

```text
C_REF_AS_WRITTEN_DERIVES_SMOOTH_DATA_FROM_GATE4 = false | TYPE-R |
  test: the class definition takes the same smooth coframe and connection as
        input before the refinement conclusion is posed

C_REF_USABLE_AS_TARGET_INTERFACE = true
```

Usable as a **target interface**; refuted as a **source**. An adoption that fixed P1–P3
by reference to a smooth coframe would be this bar.

[PROVABLE] **B-2: a free/formal refinement category.** `JREF` `8dd59b35…[13290,13455)`:

```text
INCIDENCE_FORCES_GEOMETRIC_SCALE = false | TYPE-R |
THIN_ZERO_EXTENSION_FORCES_REFINEMENT = false | TYPE-R
FREE_REFINEMENT_CATEGORY_DISCHARGES_J_REF = false | TYPE-R
```

An adoption that supplied P1–P3 as formal generators without geometric content would
be this bar — and `JREF`'s own words are that it *"merely moves the missing data into
unproved generators."*

[YOURS] Both bars survive the ruling untouched: neither depends on what the limit's
subject is.

### 2.4 `lambda`/`beta` — status UNCHANGED, and why the contrary reading fails

[PROVABLE] The ruling's consequence 5 states the question *"remains open and
independent"*. JJ2 instructs me to include it **only if** the ruling changed its
status. It did not. But the forcing re-run had to test one tempting reading, and I
record the test rather than the temptation.

[PROVABLE] `(D2-4)`, `8dd59b35…[12383,12879)`, says `lambda` rescaling *"change[s]
current-density normalization and the **order-two local symbol**."* The ruled subject
is the intensive **quadratic** coefficient. If those were the same object, `lambda`
would move the subject and its status would have changed materially.

[PROVABLE] They are not identified anywhere. SEARCHED SPACE: `workspace/` +
`supervision/`, `*.md`, recursive, **excluding this artifact**, which necessarily
contains both phrases and would otherwise count itself. Four files contain `order-two
local symbol`; **every one contains zero occurrences of `intensive quadratic
coefficient`.** The two phrases never co-occur in any source of record.

[PROVABLE] And the datum needed to settle it independently is absent: the `(D2-4)`
neighbourhood states the rescaling `ell_e -> lambda ell_e`, `V_cell -> lambda^d V_cell`
but never says which of the paired quantities in `xi_(mu nu) = ell_mu ell_nu F_(mu nu)`
is held fixed under it — the neighbourhood contains zero occurrences of *"held fixed"*
or *"invariant"*.

[YOURS] So the coefficient's behaviour under `lambda` is **not determinable from sealed
text**, and I decline to determine it two ways over: the datum is missing, and
computing a downstream consequence to settle a choice is what the void condition bars.
**LAMBDA_BETA = unchanged**, with the named missing datum recorded so the next lane
does not have to re-discover it: *a rule fixing which of `xi` / `F` is invariant under
(D2-4)*.

---

## 3. JJ3 — THE RECOMMENDATION SLOT

```text
RECOMMENDATION: [ INTENTIONALLY EMPTY ]

This package presents.  The principal rules.
```

[YOURS] I leave it empty by instruction, and I would leave it empty without the
instruction. FC-2 is a conflict already reserved to the principal; FC-1 is an adoption
the corpus types as possibly-not-derivable, and a lane that recommended a member would
be selecting the object it was commissioned to describe.

### 3.1 Anti-tuning attestation — the void condition

```text
VOID_ATTESTATION = CLEAN

No candidate's downstream numeric consequence was computed, consulted, estimated,
or inspected at any point in assembling this package.  Specifically:

  -- no coefficient, response value, or alpha-facing quantity was read;
  -- the lambda question at §2.4 was closed by a MISSING-DATUM finding and an
     explicit refusal to compute, not by working out which way it came out;
  -- the sliver question at §2.2 is recorded as gating and left conditional; I
     did not evaluate whether a sliver breaks the ratio, which would have
     required exactly the downstream evaluation the condition bars;
  -- no candidate for P1/P3 was preferred, and §2.1's "smallest sufficient"
     reading is structural (a limit needs a direction), not outcome-based.

The R4-kernel void condition -- a topology or index may not be chosen from
desired coefficient -- is honoured, and the ordering of this artifact reflects
it: the free choices were enumerated from sealed text before any consequence
was considered.
```

---

## 4. SEARCHED SPACES, GROUNDING, JURISDICTION, VERB AUDIT

### 4.1 Searched spaces

| Claim | Searched space | Method |
|---|---|---|
| The subject and the order-two local symbol are never identified | `workspace/` + `supervision/`, `*.md`, recursive | fixed-string on both phrases; all four carriers of the first opened |
| The (D2-4) invariant datum is absent | the `(D2-4)` neighbourhood in `JREF` | counts of `held fixed`, `invariant`: 0 |
| `Ref_0`'s lack is stated in three items and nowhere repaired | same corpus | object-names probe on `Ref_0`, `J_0`, `directed by geometric refinement` |

[YOURS] Not searched, therefore not claimed about: the cleanroom mirror, the origin
corpus outside the archive, non-`.md` files, `rd22_run_*/evidence/` mirrors. Per
`STAGE8_LANE_STATUS.md`'s caveat every absence is a **lower bound**.

### 4.2 Grounding

| # | Step | Source + span | Tag |
|---|---|---|---|
| 1 | The ruling's subject and consequences | `112e6acb…` (sidecar OK) | PROVABLE |
| 2 | The per-cell measure is derived and forced | `14ddfc15…[3311,3668)` | PROVABLE |
| 3 | Its coframe law is already the measure's naturality; stitching done | `578eb5e9…[1313,1697)` | PROVABLE |
| 4 | `Ref_0`'s arrows | `8dd59b35…[11610,11743)` | PROVABLE |
| 5 | `Ref_0` skeleton BUILT | `8dd59b35…[12001,12049)` | PROVABLE |
| 6 | The three missing properties P1–P3 | `8dd59b35…[12055,12336)` | PROVABLE |
| 7 | V011's condition on the ruled subject | `aa7c6d49…[47025,47247)` | PROVABLE |
| 8 | The held conflict, principal's to resolve | `6934f858…[177,391)` | PROVABLE |
| 9 | The sliver risk in four-volume terms | `6934f858…[7118,7486)` | PROVABLE |
| 10 | B-1: smooth-preserving family barred as source | `430f0971…[20690,20930)` | PROVABLE |
| 11 | B-2: free refinement category refuted | `8dd59b35…[13290,13455)` | PROVABLE |
| 12 | (D2-4) moves the order-two local symbol | `8dd59b35…[12383,12879)` | PROVABLE |
| 13 | The two phrases never co-occur | §4.1 search, counts displayed | PROVABLE |
| 14 | The (D2-4) invariant datum is absent | §2.4 counts | PROVABLE |
| 15 | The measure half is discharged for the index | §1.3, from 2–3 | PART-PROVABLE |
| 16 | The free choice is P1–P3 over a BUILT `Ref_0` | §1.4, from 4–6 | PART-PROVABLE |
| 17 | The shape condition gates RA27-2 under the ruling | §2.2, from 7 + 9 | PART-PROVABLE |
| 18 | Downstream commitments of FC-1 | §2.1 | PART-PROVABLE |
| 19 | `lambda` status unchanged; missing datum named | §2.4, from 12–14 | PART-PROVABLE |
| 20 | "Smallest sufficient" reading of P2+P3 | §2.1 | **YOURS** |
| 21 | Recommendation withheld | §3 | **YOURS** |

```text
GROUNDED_STEPS = 19 / 21
YOURS, NAMED: 20 (the smallest-sufficient reading), 21 (the withheld recommendation).
Neither is a premise for any PROVABLE row.
```

### 4.3 Jurisdiction check

**The void condition.** Written against selecting an index or topology by the number it
yields. Squarely present — this is an adoption package, the most tempting place in the
thread. §3.1 attests, and the two places where a computation would have settled a
question (`lambda`, slivers) are both left open with the missing datum named.

**The held shape conflict.** Written so a lane cannot settle by production what the
principal reserved. Present and binding: §2.2 promotes it and resolves nothing, adopts
no shape condition, and does not narrow D3.

**DoR-007 and the two TYPE-R bars.** Present; §2.3 applies them to bar sources and
formal substitutes, not to bar the object. No smooth structure imported or requested.

**R9 / R9-JII.** Unchanged. §2.1 records that discharging FC-1 does not make J2
runnable — the carrier stays PENDING on its common-cell quantifier — so no inability to
see is converted into a verdict.

**The S5.3 fence.** Carried into the commitments verbatim per the ruling's consequence
4; nothing here discharges or assumes (H1)/(H2).

### 4.4 Self verb audit

| Verb or status | Warrant |
|---|---|
| `DERIVED` | the measure only, on `BATT`'s own word *"forced, not chosen"* |
| `BUILT` | `Ref_0`/`J_0` only, on `JREF`'s own `BUILT / TYPE-P` |
| `gates here` | the shape condition — with the promotion/resolution distinction stated and the conditional left conditional |
| `barred` | two candidates, each on a sealed `TYPE-R` |
| `unchanged` | `lambda`/`beta`, with the contrary reading tested and its missing datum named |
| `[ INTENTIONALLY EMPTY ]` | the recommendation slot |

[YOURS] Disclosures against myself:

1. **716's fork was too pessimistic and too coarse.** I presented RA27-2 as though the
   whole geometric package were free. The measure and its naturality law were already
   derived and sealed, and `DEC` had said *"the measure's own stitching is done"* since
   2026-07-30. The ruling did not create that; my scoping should have found it.
2. **I named one object where the corpus names three.** For three relays I have called
   RA27-2's ground "the same-region relation". The sealed sentence gives P1, P2 and P3.
   P2 is the one I named; a package delivering only it would be under-specified against
   its own source.
3. **I nearly reported `lambda`'s status as changed.** The reading — subject is a
   *quadratic* coefficient, `lambda` moves the *order-two local symbol* — is available
   and would have been a striking result. The two phrases never co-occur in the corpus
   and the datum needed to settle it is absent. I record the near-miss because the
   temptation was real and the check is what stopped it.
4. **§2.1's "smallest sufficient adoption" is my structural reading, not a theorem.**
   That a limit needs a direction is a reason, not a proof, and P1's derivability from
   P3 is stated as a fork rather than a fact.
5. **Two defects caught by my own pin check before sealing**, both familiar: a span
   three bytes short of its quoted block (the `DEC` citation, end recomputed from the
   quote's last token), and a co-occurrence count that counted this artifact itself —
   the same self-inclusion effect that bit me at 713 and 715. The claim's scope is now
   stated in the sentence that makes it. **A census run inside the corpus it is being
   written into must exclude the writer**, and I have now needed that rule three times
   without installing it.
6. No verb here proves, authorizes, computes, binds a member, forms a common cell,
   evaluates a junction map, adopts anything, narrows D3, or grants a seal.

```text
MUST_SUPPLY = restated under the ruling (§1.2): the DIRECTED INDEX of the intensive
              coefficient's refinement limit, serving simultaneously as A27's root,
              the limit's index, and J2's re-presentation family
DERIVED_VS_FREE = updated; forcing re-run at §1.5 -- 2 of 716's findings SUPERSEDED
              (the measure half is derived; the shape condition gates here), 5
              unchanged, the two TYPE-R bars untouched
FREE_CHOICES = 2 displayed (FC-1 the three-property index P1/P2/P3 over the BUILT
              Ref_0, with smallest sufficient adoption and commitments on J2, T_ref,
              RA27-3..6 and the S5.3 burden; FC-2 the shape condition), plus 2 BARRED
              candidates with TYPE-R spans
SHAPE_CONDITION = gates here (promotion, not resolution; remains the principal's,
              PRODUCTION still PROHIBITED, D3 not narrowed, no shape condition adopted)
LAMBDA_BETA = unchanged (contrary reading tested and refused; missing datum named --
              a rule fixing which of xi / F is invariant under (D2-4))
VOID_ATTESTATION = clean (§3.1)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+6 disclosures at §4.4)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
