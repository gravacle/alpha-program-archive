# STAGE 8 — THE STRATIFICATION (O27SR) — AUDIT V001

DATE: 2026-08-15
COMMISSION: O27SR (STRATA-AUDIT). POSTURE: DEFAULT-REFUTE. Testimony carries zero weight;
every claim re-derived at bytes from the sealed sources themselves.
TARGET: STAGE8_STRATIFICATION_O27SR_V001.md (sha256 verified below).
STATUS: DETERMINATION ONLY. Locate and compare. Nothing proposed, authored, adopted or
reclassified; no demand satisfied; no level crossed. No value, no number as a program
quantity, no measured-constant comparison.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE, before any write:
  /Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_STRATIFICATION_O27SR_AUDIT_V001.md
  — ABSENT (`ls` exit 1). Sidecar .seal.sha256 — ABSENT (`ls` exit 1).

---

## §0 — SEALS, GROUND, SWEEP CUTOFF

### §0.1 TARGET SEAL

```text
cd "/Users/bgm/MB Work/alpha-program-archive/workspace"
shasum -a 256 -c STAGE8_STRATIFICATION_O27SR_V001.md.seal.sha256
  => STAGE8_STRATIFICATION_O27SR_V001.md: OK
DIGEST: 3c736ea88a1a4cb88aec09ecd128555b6a52fb3741b1a86e5c539241862302f2
Run FROM THE ARTIFACT'S OWN DIRECTORY, before any reliance.  90305 bytes, 1521 lines.
```

### §0.2 THE TARGET'S OWN FOURTEEN — INDEPENDENTLY RE-VERIFIED

All fourteen re-verified by this audit, `shasum -a 256 -c <file>.seal.sha256` run FROM THE
ARTIFACT'S OWN DIRECTORY. **14/14 OK. Zero mismatches. None unverifiable.** The target's
§0 seal table is CONFIRMED at bytes.

```text
STAGE8_COMPLETION_MAP_T17SR_V001.md                  OK
STAGE8_COMPLETION_MAP_T17SR_AUDIT_V001.md            OK
STAGE8_W1_SUBVOLUME_RATE_O5SR_V001.md                OK
STAGE8_W1_SUBVOLUME_RATE_O5SR_AUDIT_V001.md          OK
STAGE8_W2_PREQUOTIENT_RULE_O2SR_V001.md              OK
STAGE8_W2_PREQUOTIENT_RULE_O2SR_AUDIT_V001.md        OK
STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md                   OK
STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md             OK
STAGE8_INGREDIENT_CENSUS_O17SR_V001.md               OK
STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md         OK
STAGE8_INDEX_VARYING_SETS_O20SR_V001.md              OK
STAGE8_INDEX_VARYING_SETS_O20SR_AUDIT_V001.md        OK
STAGE8_GLUING_CANDIDATE_O23SR_V001.md                OK
STAGE8_GLUING_CANDIDATE_O23SR_AUDIT_V001.md          OK
```

### §0.3 SWEEP CUTOFF — DECLARED

```text
SWEEP_CUTOFF = DECLARED.  2026-08-15.
PRIMARY ROOT: /Users/bgm/MB Work/alpha-program-archive/workspace
  (3622 entries; 1775 .md files; ~110 MB).
BYTES READ DIRECTLY BY THIS AUDIT: the target + the fourteen seal-verified files above,
  read in full or at located spans; plus, for the MISSED-BRIDGE hunt (D) and the
  MISSED-MISMATCH hunt (E) ONLY, files located by declared grep sweeps OUTSIDE the
  target's fourteen and opened at their located spans.  Every such file is named at
  §4 with its seal state stated.  This audit does NOT inherit the target's [2H] fence:
  where the target read second-hand and this audit also did not open the inner source,
  the audit says so and does not upgrade the target's reliance.

DECLARED SCOPED SWEEPS (lawful; filename-level listing + grep, primary root):
  S1  re-run of the target's L2 census: every occurrence of "level" (case-insensitive,
      all forms) in each of the fourteen.  Counts at §1.1.
  S2  re-run of the target's L1 census: "complex-level", "complex level", "cell-level",
      "cell level", "composite-level", "stratum-level", "record-level",
      "boundary-level", "per-complex", "per-cell", "per-composite", "edge-level".
  S3  "stratum|strata" in the fourteen, read in context.
  S4  RC-7's arithmetic claim: lines containing BOTH "composite" and "complex" in the
      seven build files.  Re-run at §5.
  S5  BRIDGE HUNT, primary root, OUTSIDE the fourteen: joining / gluing / lift /
      descent / refinement / inclusion / summation / "cell to complex" /
      "complex-level" / identification-of-cells patterns.  §4.
  S6  LENS-TOKEN and PROGRAM-QUANTITY sweep over the target's own bytes.  §7.
  S7  FENCE-SCAN of the target's own bytes: proposal / adoption / crossing verbs.  §6.
NOT READ, at pattern level: any register / tracker / road / plan / continuation file.
  None opened; none cited.  Excluded by filename pattern before any open.
ROOT 2 (/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003): listed only, NOT consumed.
"Q-..." tokens: EXPECTED-UNLOCATABLE, carried opaque, never chased.
Artifacts sealed after this audit's fire time: UNSWEPT.  No git.
```

---

## §1 — HUNT (A): AN IMPOSED HIERARCHY

The commission names this the most likely failure and the one this program has committed
repeatedly. It has two halves and they come apart. **The name half is CLEAN. The ordering
half is DEFECTIVE.**

### §1.1 THE NAME HALF — CLEAN. EVERY LEVEL NAME RE-TESTED AGAINST A CORPUS USE

Every name in the target's V-B and V-C lists was tested by this audit for an occurrence
in the fourteen. **All twenty tested names occur. None is a lane coinage. No corpus level
name is replaced, renamed, or dropped.** Occurrence counts, this audit's own grep over the
fourteen:

```text
oriented k-cell  16    0-cell   19    port      220   chain              85
diamond          10    patch    40    incidence  77   stage/carrier index 1
operator grade    2    trace/HS grade 2   FORM LEVEL 4   At symbol level  2
GENERATOR layer   1    DRESSED layer  6   presentation-grade 4  totality-grade 2
physical stratum  7    audit/prediction stratum 2   fence class 2   per-diamond 3
```

The two exceptions the target declares are correct as declared: "EDGE-LEVEL" is the
CENSUS's own heading coinage over a byte reading "edge sum" (IA-2(a)); "Sense A"/"Sense B"
are the target's labels for two corpus usages (IA-2(b)). Both are flagged in place.

### §1.2 THE VOCABULARY CENSUS — RE-RUN, AND IT REPRODUCES

```text
THIS AUDIT'S RE-RUN OF L2, per file, occurrences of "level" (all forms):
  COMPLETION_MAP 7   MAP_AUDIT 4   W1 3   W1_AUDIT 1   W2 4   W2_AUDIT 5
  W3 2   W3_AUDIT 2   CENSUS 10  CENSUS_AUDIT 1  INDEX 0  INDEX_AUDIT 13
  GLUING 1   GLUING_AUDIT 1                                    TOTAL  54
=> IDENTICAL to the target's table, file by file.  CONFIRMED.

L1 RE-RUN over the fourteen (occurrences, not lines):
  complex-level 10 (all in INDEX_AUDIT)   "complex level" 2 (same file)
  cell-level 0   "cell level" 0   composite-level 0   stratum-level 0
  record-level 0   boundary-level 0   per-complex 0
=> IDENTICAL to the target's L1 block.  CONFIRMED.
```

CORRECTION **C-13 (label, not arithmetic).** The target heads the census "occurrences of
the bare word 'level'". The 54 are TOTAL occurrences and include hyphenated compounds; the
genuinely bare-word count over the fourteen is **27**. The target's own SENSE-5 display
lists hyphenated forms ("a byte-level reading", "(line-level)"), so the content is the
total and only the label is wrong. No finding turns on it.

CORRECTION **C-13b.** The §0 L1 candidate list does not include "edge-level", yet
"edge-level" occurs twice in the fourteen and §1.2 / IA-2(a) rely on it. Disclosed in
place; the declared sweep list is short by one pattern.

### §1.3 THE ORDERING HALF — *** DEFECTIVE. AN ORDERING IS ASSERTED THAT THE CORPUS DOES
NOT STATE, AND IT IS NOT DECLARED. ***

The target states, twice and flatly: "**No sealed sentence orders any two levels. There is
no level hierarchy of record**" (§1.0), and refuses the ladder at §7 ("Imposing a level
hierarchy (cell < complex < composite < record) ... REFUSED"). Both statements are correct
at bytes — this audit tested "finer-level", "coarser-level", "one level up", "one level
below", "level of refinement below" across the seven build files and found **zero** in
every one.

**But the target then uses an ordering relation at SEVEN sites in its own voice:**

```text
:210  "CHAIN — the linear extension's index, ONE LEVEL UP FROM THE CELL"
        supporting quotation is (JD-3a) alone, which states a linear extension and
        no order.
:544  AC-CERT "it does not live at the cell level; it LIVES ABOVE EVERY CELL INDEX
        simultaneously"   — the corpus's word is constancy, not height.
:568  AC-U U-1 "the complete microscopic parent — ABOVE EVERY CELL"
        — the corpus's word is "assigns" (CIS).
:643  OB-2 "...which is a restriction DOWNWARD IN INDEX."
:769  MM-1a "...a STRICTLY SMALLER class — ONE LEVEL OF REFINEMENT BELOW where the
        demand is stated."   ("strictly smaller class" IS T-1's word; "below" is not.)
:812  MM-1c "the FINER-LEVEL gain is annihilated by the COARSER-LEVEL sum."
:978  W-3 NET "...at an index the certified asset is constant over, disjoint from,
        OR BELOW."
```

**C-1a — THE SELF-CONTRADICTION, and it is inside one entry.** §2.4 OB-2 reads, in two
consecutive sentences: "LEVEL: a SYMMETRY restriction on the carrier, **not an index
restriction**. It says the satisfying object may not be constant across translations —
i.e. it must be localized, which is **a restriction DOWNWARD in index**." The entry denies
and asserts an index restriction in the same breath. The corpus's own words at that site
are symmetry-breaking and localization (W1 §0 T-3; W3 §8 m-3); neither orders an index.

**C-1b — THE IMPORT AUDIT DOES NOT DECLARE IT.** IA-2 announces the non-corpus level
vocabulary "exhaustively" and lists only EDGE-LEVEL and Sense A / Sense B. IA-6 states "NO
MATHEMATICAL IMPORT ... no external framework". The relations *up / above / below / finer
/ coarser / downward* are neither corpus words nor declared imports. IA-2's word
"exhaustively" is false as written.

**SEVERITY, STATED AGAINST MY OWN FINDING.** This is a VOCABULARY defect, not a structural
one, and the audit says so rather than inflating it:

```text
NO ranked ladder is built anywhere in the artifact.
NO level assignment in §2 turns on the ordering — each is stated in the corpus's own
   index words (per-composite / cell / complex / stratum / diamond / pair / patch).
NO mismatch grade in §3 turns on it: MM-1a rests on T-1's "only a strictly smaller
   class can"; AC-CERT's operative assignment word is CELL-BLIND, which is W3's own;
   MM-1c rests on OB-5's "does NOT survive the stratum sum".
Strip all seven phrases and every assignment, every bridge grade, and the verdict
   stand unchanged.
```

**HUNT (A) VERDICT: the imposed-hierarchy failure IS PRESENT, in its ordering form only,
at seven undeclared sites including one self-contradiction. It is NOT load-bearing. The
name half — the half this program has previously failed — is CLEAN.**

---

## §2 — HUNT (B): A LEVEL ASSIGNMENT ASSERTED, NOT DERIVED

The target's rule (§2, CH-2) is that each item's level is read off its OWN operative
sentence and that sentence is quoted. The rule is sound and the corpus supplies it
(GLUING_CANDIDATE CL-7, verified at bytes). Applied against every assignment in §2.1–§2.4:

### §2.1 WHAT SURVIVES

```text
FP-1   per-composite, all n.  DERIVED.  The quoted sentence contains "QUANTIFIER:
       per-composite, all n" verbatim — COMPLETION_MAP :86-87, exact at bytes.
FP-3   per-composite.  DERIVED from inside the quoted sentence — "the odd sector's
       would-be SINGLE-COMPOSITE majorant" — not from the heading.  Correct method.
FP-3   V-B index NONE-OF-RECORD.  CONFIRMED: the quoted sentence names no cell, no
       complex, no stratum.  Returning it unfilled is the right act.
FP-S   summed (Sigma).  DERIVED; COMPLETION_MAP :115-116 exact.
W-1    per-composite/dressed composite class.  DERIVED; W1 :168-169 exact.
W-2    ONE NAMED ORIENTED k-CELL, three times.  DERIVED; COMPLETION_MAP :330-334 exact.
W-3    four indices (a)-(d).  DERIVED; W3 :705-712 exact, and W3 :1267 independently
       maps (d) to "the H^1-propagation lemma".
AC-*   the four asset types are the CENSUS's own §0.1 wording, quoted.  CONFIRMED.
R-1,R-2,OB-1..OB-9  each carries its own quoted sentence.  CONFIRMED at bytes.
```

### §2.2 **C-9 — FP-2's "per-composite" RESTS ON THE SECTION HEADING**

FP-2's quoted operative sentence (COMPLETION_MAP :91-98) does **not** contain the word
"per-composite". The only place it occurs for FP-2 is the section heading —
`### FP-2 — F2 THE LOCUS STEP (per-composite quantifier)` at :89 — which is *what the item
is called*, the exact ground CH-2 forbids. The assignment is nevertheless **correct of
record**, on ground the target did not display: FP-2's own sentence is about "the four
quantities' shortfall", and COMPLETION_MAP :111-113 places all four quantities at the
per-composite net. **Correction: under-derived, not wrong.**

### §2.3 *** C-4 — A UNIQUENESS CLAIM THE TARGET'S OWN CONSUMED AUDIT KILLS ***

Target §1.2, the defining entry for the corpus's most load-bearing index name:

```text
"CELL — THE ONLY CLAUSE IN THE CORPUS THAT PUTS A CELL ON AN OUTPUT SIDE, quoted by
 the census at its own bytes (CENSUS §2.1) [2H on CIS]"
```

STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md — **one of the target's own fourteen, seal
verified by the target and again by this audit** — says at KILL GRADE (§2, :153-156):

```text
"CONSEQUENCE FOR THE COMMISSION'S MOST IMPORTANT ANSWER.  §2.1's headline —
 '**EXACTLY ONE CLAUSE IN THE CORPUS**' — and FLAG `CELL_ON_OUTPUT_SIDE = 1` are
 false at bytes.  A licensed generator inventory of record puts cells on an output
 side, and the census had the artifact open when it wrote the sentence."
```

with the three counter-instances displayed at that audit's own bytes (:125-139):

```text
A1      TAKES  the parent complex K.  YIELDS a refined complex — "cubical bisection,
        16 subcubes".   TYPE  RULE.  CELLS STAND ON ITS OUTPUT SIDE.
A2-F    TAKES  the parent complex K.  YIELDS a refined complex whose cells are
        "Freudenthal chains of Boolean vertices".  TYPE  RULE.  CELLS STAND ON ITS
        OUTPUT SIDE.
A2-B    TAKES  the parent complex K.  YIELDS a refined complex whose cells are
        "barycentric chains of nonempty 4-cube faces".  TYPE  RULE.  CELLS STAND ON
        ITS OUTPUT SIDE.
```

The target's §1.2 uniqueness claim is **FALSE AT BYTES**, on the bytes of a file the
target itself consumed. Grep confirms the target never mentions the refinement generators,
`CELL_ON_OUTPUT_SIDE`, or "EXACTLY ONE CLAUSE" anywhere in its 1521 lines.

**STATED FAIRLY, AGAINST MY OWN FINDING.** The *quotation* the target hangs on that
heading — CIS's "For every primitive record-forming incidence `c` ..." and the census's
"No object in the census constructs a cell from anything more primitive than the undrived
parent" — is the **surviving** sentence: the same CENSUS_AUDIT paragraph says so
expressly ("the census's narrower sentence ... survives. The sentence that does not
survive is the count, the word EXACTLY, and the flag"). So the target quoted the right
sentence and wrapped it in the killed one. **The CELL entry's content stands; its
uniqueness header does not.**

### §2.4 *** C-5 — AC-JOIN AND U-3 ARE QUOTED AS STANDING; THE SAME AUDIT OVERTURNS BOTH ***

The target carries, three times and as a standing verdict of record —
§2.3 AC-JOIN, §3.2 B2-ii, and again in the §9 FLAG BLOCK ("The joining-of-cells class
remains EXACTLY EMPTY") — this sentence:

```text
"**NOTHING. THE CATEGORY IS EMPTY, AND THE ABSENCE IS EXACT.**"   (CENSUS :469)
```

and quotes U-3's entry as "this is exactly the empty category of §2.3". CENSUS_AUDIT §5.1
(:384-392), inside the fourteen, rules on exactly these two objects:

```text
"DOES  overturn the flat statements.  'NOTHING. THE CATEGORY IS EMPTY' (:469)
       and FLAG JOINING_OF_CELLS_ON_OUTPUT_SIDE = 0 are not what the census's
       own inventory supports.  The supportable statement is narrower:
       NOTHING ADOPTED, AT THE PHYSICAL STRATUM, YIELDS A JOINED COMPLEX;
       one census-resident PROPOSED rule glues two cells and yields a
       contractible object.
 DOES  overturn U-3 as written.  U-3's 'nothing in the census yields it' is
       asserted after checking two of eight candidate rows."
```

**AND THE TARGET CONTRADICTS ITSELF ON THE SAME PAGE.** §3.2 B2-ii says the category is
EXACTLY EMPTY; §3.2 B2-iii, eight lines later, says NET is "the corpus's ONLY instantiated
one" — an instantiated joiner. Both cannot be true as stated. The corpus's own audit
resolves it, in the narrower form quoted above, and the target never cites it.

**DOES IT INVERT MM-2?  NO — and I say why rather than leaving it implied.** The narrower
supportable statement still yields ABSENT: the one joiner is PROPOSED_NOT_ADOPTED, glues
two cells, and its output is contractible (H^1 = 0). The target reaches that same place at
B2-iii by a different route. **MM-2's grade survives; two of its three supporting
quotations must be re-stated in the narrower form the record supports, and the FLAG
BLOCK's "remains EXACTLY EMPTY" must be narrowed to "nothing ADOPTED, at the physical
stratum, yields a joined complex".**

---

## §3 — HUNT (F): QUOTATION INTEGRITY, ENFORCED TO SENTENCE END

Every quotation in the target was located in its named source and read PAST its closing
mark to the end of the source sentence. Result: **the overwhelming majority are exact and
complete**, including the two the target itself flags as restorations. **Three truncations
were found. One is material and it is repeated three times.**

### §3.1 WHAT PASSES — INCLUDING THE HARD CASES

```text
FP-1 / FP-2 / FP-3 / FP-S / PER-COMPOSITE NET   COMPLETION_MAP :81-145.  EXACT.
  FP-2 is quoted TO ITS END, including the trailing clause the WBC's §3 carries —
  "which boundary-supported structure provably does not reach."  This is the target
  doing exactly what this commission enforces.  CONFIRMED.
W-1 / W-2 / W-3 discharger blocks   COMPLETION_MAP :324-339.  EXACT, all three.
V-2 bar and the SHAPE-3 sentence   COMPLETION_MAP :142-145, :430-433.  EXACT.
"no third quantifier exists at the consumed bytes (the one-way record 0a10c030)"
   COMPLETION_MAP §5 CH-2 :519-520.  EXACT.
R-1's restoration   INDEX_AUDIT :486-488 and :507-509.  EXACT, to sentence end.
   The target's mid-span "..." at :606 elides only the clause it quotes SEPARATELY
   two lines above.  Marked, and its content displayed adjacently.  LAWFUL.
R-2   INDEX_VARYING_SETS §7.3 :403-408.  EXACT, both sentences.
W-1 target form, four-continuity-proofs count, per-stratum mechanism, route (iii),
   "And the generator is not the layer", "not at the dressed layer"   W1.  ALL EXACT.
W-2c bearing sentence, OWED-2(2b), OWED-3, AUTHORED-PREMISE-DERIVED grade   W2.  EXACT.
OB-3 kernel verdict, o4 sentence, Gronwall heading, 2 sqrt(3) certificate   W3.  EXACT.
D-6, "GRANTING ADOPTION CLOSES NONE OF D-1 THROUGH D-6", the G-6/G-7/G-10
   correction, "this audit adopts no stratum for NET either"   GLUING pair.  EXACT.
```

### §3.2 *** C-2 — THE MATERIAL TRUNCATION. IT IS REPEATED THREE TIMES. ***

The target carries this span at :283 (§1.3), :562 (§2.3 AC-JOIN) and :890 (§3.2 B2-ii),
each time inside quotation marks and presented as "the record's own enumeration of the
result":

```text
"NOTHING SEALED EXISTS:    a physical-stratum glued multi-cell record complex; a port"
```

**IT STOPS MID-ITEM AND MID-CLAUSE, UNMARKED.** The item is not "a port". The source —
STAGE8_GLUED_TOPOLOGY_HUNT_V001.md :446-452, seal verified OK by this audit from the
artifact's own directory — reads:

```text
NOTHING SEALED EXISTS:    a physical-stratum glued multi-cell record complex; a port
                          identification rule; a connected composition law; a CTP
                          trace; a sealed contour; a C_ref member.
CONSTRUCTIBLE, CONSTRUCTOR NAMED:  the H^1 > 0 physical carrier — by the unbuilt
                          O11 successor incidence-sum law + O12 port anchoring
                          (GB S1/S3) + the connected-composition closure (MON Thm 3
                          five conditions; V011 Gate-5 obligation), and — for the ...
```

Five further absent items are cut, and then the **entire adjacent clause of record** is
cut: the record does not stop at *nothing exists*. It goes on to say the physical carrier
is **CONSTRUCTIBLE, WITH THE CONSTRUCTOR NAMED** — O11 + O12 + the connected-composition
closure. That is the corpus naming, in its own words, the very bridge object the target's
§3.2 searches for and reports as an unnamed emptiness.

**PROVENANCE, STATED FAIRLY.** The truncation is not the target's invention: CENSUS :494
carries the same short fragment and cites it as ":446". The target inherited it through
its [2H] fence and could not have seen the tail without opening a file its declared cutoff
excluded. **What is the target's own is presenting a fragment as "the record's own
enumeration of the result", three times, without a terminal ellipsis and without noting
that the quoted line is one item of six.**

### §3.3 *** C-3 — AND THE CONSTRUCTOR IS NAMED INSIDE THE FOURTEEN ANYWAY ***

The [2H] excuse does not cover the whole finding, because the constructor pair is named
**twenty-one times across five of the target's own fourteen** (this audit's grep: CENSUS 9,
CENSUS_AUDIT 1, INDEX_VARYING_SETS 1, INDEX_AUDIT 3, GLUING_CANDIDATE 5, GLUING_AUDIT 2).
CENSUS §3 :525-535, quoted verbatim and completely by the census itself, reads:

```text
"(c) GENUINELY UNSPECIFIED — THE VERDICT.  The gluing rule that could create a loop
 of cells is exactly the UNBUILT constructor pair of POSED: O11 (the successor
 law displaying h_j as an incidence sum over named edges — absent, S1) + O12
 (port-to-0-cell anchoring — absent, S3), plus the connected-composition
 obligation (G-1/G-2/G-5: declared, five closure conditions named, proved = false)."
```

**The target mentions O11 and O12 zero times.** Its §3.2 enumerates four bridge candidates
(JD-3a, the joining class, NET, the near-end cell) and never names the object the corpus
names for that bridge.

**DOES THIS INVERT MM-2?  NO.** The constructor is UNBUILT ("absent, S1", "absent, S3",
"proved = false"). The bridge grade stays **ABSENT**. What changes is the character of the
finding: the target reports a bridge that is nameless and missing; the record reports a
bridge that is **named, specified, and unbuilt**. Those are different states and the
second is the one of record.

### §3.4 C-7 — TOY_SEPARATION CLAIMS A DISPLAY THAT WAS NOT MADE

§7 asserts the obstructions are "quoted whole, including the parts that cut against this
commission's own reading (**R-1's near-miss**; MM-1c's derived bridge ...)". MM-1c's
derived bridge IS displayed. **R-1's near-miss is not displayed anywhere in the artifact**
— grep for "near-miss", "CONSTANT SECTION", "cell-wise" returns only that §7 sentence and
one occurrence of "cell-wise" inside the INDEX_AUDIT quotation. The near-miss is
INDEX_VARYING_SETS :410-417, headed by the source itself:

```text
"*** THE NEAR-MISS, RECORDED SO IT IS NOT MISTAKEN FOR THE BRIDGE. ***  K2 §3.3
 (:191-196) DOES evaluate its own members at K1's index ... This is a genuine point
 of contact.  But it pins BOTH named members to the SAME b_1 value, so it supplies a
 CONSTANT SECTION over K1's index, not a variation across it."
```

Nor does the target adopt the correction its own source requires. INDEX_AUDIT §6.1
:513-517 states: *"CORRECTION REQUIRED: ... the constant-section argument must be stated
in the form 'constant over the CELL index, notwithstanding a displayed complex-level
difference' — which is what the bytes support and what the target asserts more flatly than
its own source allows."* The target's R-1 instead asserts the restriction "expressly
grants nothing to the cell" — the flatter form, in an entry whose whole point is that a
level restriction was elided.

**Verdict on R-1 itself: the ASSIGNMENT stands** (INDEX_AUDIT: "§7.3's NOT-STATABLE
verdict is CORRECT at bytes"), **the display does not.**

### §3.5 THE MINOR TRUNCATIONS AND MIS-MARKS

```text
C-10  W1 §2.2 is a TWO-COLUMN TABLE (:161-169).  The target re-flows it into a
      semicolon-joined string and stops before the CARRIER RANK row's own tail
      "(PA / FHB §3.2, consumed at sealed grade)", marking the result "verbatim"
      at THREE sites (§2.1 FP-1 READING, §2.2 W-1, §5.1 RC-2).  Nothing of level
      content is elided; the mark "verbatim" is wrong for a re-flowed table.
C-14  §4.3(iii) marks "only through INDEPENDENT PER-COMPOSITE MECHANISMS AGREEING"
      verbatim; COMPLETION_MAP :431-432 has it in lower case.  Emphasis added
      silently.  The target does this elsewhere too; it is a house habit, not a
      content change, but "verbatim" should not cover it.
C-2b  AC-OBJ's CENSUS quotation stops at the internal closing quote of "neither is
      sealed as a physical record carrier"; CENSUS :510-513 continues in the same
      sentence "K_square being a prediction carrier and K_L 'the finite
      translation-complete TEST OBJECT' for audit."  The elided tail SUPPORTS the
      target's own assignment.  Unmarked truncation, harmless in effect.
```

---

## §4 — HUNT (D): A MISSED BRIDGE. INDEPENDENT SEARCH, WIDER THAN THE TARGET'S

The commission's rule is that one missed bridge inverts the finding. This audit therefore
searched the **primary root**, not the target's fourteen, for a sealed lift, descent,
refinement, inclusion or summation connecting any mismatched pair. Files located and
opened at their spans, **each seal verified OK by this audit from the artifact's own
directory** (register / tracker / road / plan / continuation files excluded by pattern
before any open):

```text
STAGE8_GLUED_TOPOLOGY_HUNT_V001.md        OK   (2026-08-13)
STAGE8_GLUED_TOPOLOGY_CHECK_V001.md       OK   (2026-08-13)
STAGE8_TYPING_RULE_CANDIDATE_V001.md      OK   (2026-08-13)
STAGE8_ANCHORING_DERIVATION_V001.md       OK   (2026-08-13)
STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md   OK   (2026-08-15 16:42, i.e. BEFORE the
                                               target's 16:45 — a concurrent lane,
                                               lawfully outside the target's cutoff)
```

### §4.1 THE RESULT: **NO POSITIVE BRIDGE EXISTS. THE TARGET'S GRADES SURVIVE.**

```text
cell -> composite   grep over the whole primary root for cell-to-composite /
   composite-assembly patterns returns exactly TWO files, and one of them is the
   target itself.  No sealed object performs it.
per-pair cancellation at assembly   named in four files besides the target; every
   one carries it as a route WITHOUT a carrier.  W1's "CARRIER-LESS" stands.
complex-level uses OUTSIDE the fourteen   all NEGATIVE, without exception:
   GLUED_TOPOLOGY_CHECK :226 "nothing at the complex level, induces NO cyclic
     identification";
   TYPING_RULE_CANDIDATE :429 "endpoint characters remain unrealizable at the
     complex level";
   ANCHORING_DERIVATION :310 "M03 defines no complex-level gauge action at all
     (one cell, no ...)".
   None grants, lifts or descends.
JOIN_ADMISSIBILITY_O28SR (the concurrent lane, on precisely the joining question):
   "**GATE 1 FAILS. The chain stops there.**" ... "The consequence the commission
   asked me to test **is not displayed by these bytes, and I do not assert it.**"
   Gate 2 fails independently.  NO BRIDGE.
```

**Note recorded, not adjudicated.** JOIN_ADMISSIBILITY_O28SR's headline asserts the two
invariants sit "on objects ONE LEVEL APART in **the corpus's own hierarchy**" — a phrase in
direct tension with the target's §1.0 finding that no hierarchy is of record. This audit
does not adjudicate a different commission's artifact. It is recorded because it is the
same failure class this audit's §1.3 found in the target, in a stronger form, in a
contemporaneous lane; and because it supplies no bridge either way, it does not touch
O27SR's verdict.

### §4.2 C-16 — WHAT THE TARGET DID MISS INSIDE ITS OWN FOURTEEN

A sealed refinement **with an explicit summation over subdividing cells** sits inside the
fourteen and the target never located it. CENSUS_AUDIT :142-148, quoting
STAGE8_D0_SQUARE_CERTIFICATE_V001.md at that audit's own verified seal:

```text
"The subdivision relation is not inferred by this audit; it is written into the
 artifact's own transported cochain map, which quantifies over subdividing cells:
   sd*_1 : C^1(K') -> C^1(K),
   (sd*_1 a')_e  = sum_(e' subdividing e) orientation(e',e) a'_(e')"
```

That is a refinement (complex -> refined complex) carrying an index map between two
cellulations' 1-cochains, sealed, of record, **inside the fourteen**. Consequently the
target's §1.4 INC-4 sentence — *"There is no sealed sentence anywhere in the fourteen that
grants a level, licenses a lift, or licenses a descent"* — is **false at bytes**.

**DOES IT INVERT ANYTHING?  NO, and the reason is exact.** `sd*_1` connects two
cellulations of the same complex. It does not carry a cell to a composite (MM-2), does not
carry the dressed composite class to a strictly smaller class (MM-1a), and does not carry a
require-set value to a b_1 sector (R-2). Every mismatched pair in §3.4 remains unbridged.
**INC-4's absolute form must be withdrawn; no grade changes.**

### §4.3 C-11 — §4.3(i)'s CLAIM ABOUT THE FIVE BRIDGE KINDS IS FALSE AS WRITTEN

§4.3(i) states: *"Every bridge object named anywhere in the fourteen is of one of the five
kinds at §3 — lift, descent, refinement, inclusion, summation ... NO OBJECT OF ANY OTHER
KIND IS NAMED IN THE FOURTEEN."* CENSUS_AUDIT §6 :446-452, inside the fourteen, types two
named bridge objects and neither is one of the five:

```text
"O11  the successor law. ... A successor law's output side therefore carries an
      ADJACENCY that does not otherwise exist.  KIND 1.
 O12  port-to-0-cell anchoring.  Its output side is an IDENTIFICATION of two slots
      with cells of a complex; an identification YIELDS A QUOTIENT OBJECT.  KIND 1."
```

An adjacency and a quotient-yielding identification are bridge objects of two further
kinds. **§4's OPERATIVE CONCLUSION SURVIVES UNTOUCHED**: neither an adjacency nor a
quotient acts on quantifier order, so the V-2 boundary is still not crossable by any object
the fourteen name, and the three sealed statements at §4.2 still say it has never been
crossed in either direction. Only the enumeration sentence needs correcting.

---

## §5 — HUNT (C): A FALSE MISMATCH. AND THE ORDERING ROW THE TARGET DID NOT SHOW

### §5.1 THE REVERSE CHECK RE-RUN — IT HOLDS, ARITHMETIC INCLUDED

```text
RC-1  FIVE NAMES, ONE LEVEL PROPERTY.  Re-counted at bytes:
        carrier-blindness 1 (W3) ; cell-uniformity 1 (W3) ; cell-blind 1 (W3) ;
        cell-power-blind 3 (W3) ; "CARRIER-FREE per unit cell" 1 (COMPLETION_MAP).
      All five present.  CONFIRMED.  The target's express LIMIT — that it displays a
      shared LEVEL PROPERTY and does NOT equate the objects across the V-2 bar — is
      correctly stated and correctly observed throughout.  No transport occurs.
RC-2  FP-1 and W-1 same level.  CONFIRMED; both n-indexed at W1 :162.  This is a
      real false-mismatch catch and it is scored as "same level" in the §3.4 table.
RC-5  "STRATUM" two senses.  CONFIRMED by this audit's own S3 sweep: W1/W3 carry only
      the sum-index sense; CENSUS/GLUING and their audits only the object-class sense;
      no file carries both; no sentence in the fourteen notes the collision.
RC-6  "CELL" two objects.  CONFIRMED at W2 :562-563 and CENSUS "common cell formed
      = false".
RC-7  *** THE ARITHMETIC RE-RUN, AND IT IS STRONGER THAN CLAIMED. ***  Lines carrying
      BOTH "composite" and "complex": ZERO in each of the seven build files — and
      ZERO in each of the seven AUDITS as well, i.e. 0 across all fourteen.  The
      distribution is near-disjoint by file: "complex" is absent from all eight
      COMPLETION_MAP/W1/W2/W3 files; "composite" occurs 2 and 3 times in the CENSUS
      pair and 0 times in the INDEX and GLUING pairs.  CONFIRMED.
RC-8  "SUM" two operations.  CONFIRMED; the structural reason at §4.1 is sound.
RC-3, RC-4, RC-9  CONFIRMED at their own bytes.
```

**NO FALSE MISMATCH FOUND.** Every mismatch in §3.4 is between objects the corpus keeps
apart in its own words. Hunt (C) returns clean.

### §5.2 C-17 — THE SEALED "ORDERING PRESENT?" ROW IS NEVER SHOWN

RC-4 quotes the `A STAGE?` row of INDEX_VARYING_SETS §6's comparison table (:301-306). The
**next row of the same table** (:307-309) reads:

```text
ORDERING         total (integers) — but see §7    PARTIAL, by inclusion of
PRESENT?         for why the order does not       requires; the class map is
                 give a usable direction here.    ANTITONE along it.
```

The target quotes the row above it and never carries this one; "ANTITONE", "ORDERING
PRESENT", "usable direction" appear nowhere in its 1521 lines.

**RULED STRICTLY, AND AGAINST MY OWN §1.3 FINDING.** §1.0's claim is *"No sealed sentence
orders any two LEVELS"*, and this row orders **the values of one index**, not two levels
against each other — so §1.0 is **not** contradicted, and I decline to score it as one.
Two things do follow, and both are corrections:

```text
(i)  The corpus DOES carry ordering vocabulary of its own — "total", "PARTIAL",
     "ANTITONE", "ORDERING PRESENT?".  §1.3 above must therefore be read precisely:
     what the target imported is not ordering language as such, but an ordering
     BETWEEN LEVELS, which is the thing the corpus never states.
(ii) This row is the single strongest piece of counter-material to §1.0's headline,
     it sits two lines below a row the target did quote, and a DEFAULT-REFUTE
     display should have shown it — together with its own qualifier, "the order
     does not give a usable direction here", which supports the target.
```

---

## §6 — HUNT (E): A MISSED MISMATCH. TWO FOUND

The comfortable outcome for this target is that its ten rows are complete. They are not.

### §6.1 *** C-6 — W-2: TWO SEALED COMPOSITE-LEVEL FACTS ARE NEVER DISPLAYED ***

§3.2 B2-i grades the linear extension and closes with: *"It does not carry a cell to the
volume diagonal of a composite; **nothing of record says it does**."* Two sentences in W2 —
inside the fourteen, in the file the target is grading — bear on that:

```text
W2 :275   "The obstruction's four 'must prove' obligations are PROVED ON THE
           COMPOSITE inside the premise (§4): P-1 orientation covariance, P-2
           presentation independence (structural), P-3 continuity in named
           topologies, P-4 contact-kernel Ctc_N^sym subset ker(Phitilde_N)."
W2 :290-295  "One shape check, MADE AND PASSED: CN-2's bearing demands 'the junction
           object's own symbol-side value.'  The premise's per-cell value is
           symbol-side in the exact sense required — a_s is manufactured FROM the
           Ward symbol s through the premise's own entered chain ... and by nothing
           else.  THE RULE SUPPLIED IS OF THE SHAPE W-2 NAMES."
W2 :551    "...all four of the obstruction's 'must prove' obligations PROVED on the
           composite and check-confirmed."
```

The target quotes the §5 NOT-OWED list and the §6 GRADE line — the sentences on either side
of :551 — and carries the words "on the composite" **zero times**.

**DOES IT INVERT MM-2?  NO, AND THE CORPUS ITSELF SAYS WHY.** The shape check is a check on
SHAPE; the bearing turns on a VALUE, which is W-2c, which is NOT SUPPLIED and NOT
CONSTRUCTIBLE today on three named halts; and W2 §6 :573 states flatly *"FP-2 is NOT
discharged. The boundary-closure still FAILS-AT F2."* The target itself quotes the
distinguishing sentence (*"A rule without an evaluation supplies a symbol-side MAP; F2's
blocked side is replaced only by a symbol-side VALUE"*). **MM-2 stands. Its supporting
sentence "nothing of record says it does" must be replaced by the record's own narrower
statement: the obligations are proved on the composite and the shape check passes; the
VALUE is absent, and the value is what the bearing sentence turns on.**

### §6.2 *** C-8 — A DEMAND–ASSET PAIR WITH NO ROW IN §3.4 ***

W-3's clause (d) is mapped by W3 to a named object, at W3 :1267:

```text
"(d) reaches the DRESSED layer (= THE H^1-PROPAGATION LEMMA)."
```

and W3 :709, which the target quotes in full: *"Verbatim: MO-3 / R-L2b(beta) / B-L2*, plus
the H^1-propagation lemma."* The target quotes both the clause and the lemma's name, grades
MM-3d on the generator/dressed axis alone, and **never assigns the H^1 index a level**.

The pairing is displayed in the target's own §2.3 AC-OBJ, unscored:

```text
DEMAND SIDE  W-3 (d) requires an H^1-propagation lemma, at the physical stratum,
             for the dressed layer.
ASSET SIDE   the only objects of record carrying nonzero H^1 are A-1 K_square
             (H^1 = 1) and A-2 K_L (H^1 = 4), and the corpus types them OUT of the
             physical stratum at its own bytes: "neither is sealed as a physical
             record carrier" (CENSUS :506-513, quoting GLUED_TOPOLOGY_HUNT :368-369).
AND          the corpus's only instantiated joiner yields H^1 = 0 — a contractible
             object (GLUED_TOPOLOGY_HUNT G-9 :360-361, carried at CENSUS_AUDIT :356-361).
LEVELS       physical stratum (demand) vs audit/prediction stratum (asset).
             STRATUM SENSE B.  A real difference, on the target's own axis.
BRIDGE       a lift from the audit/prediction stratum to the physical stratum.
             This audit searched and found NONE; and GLUING_CANDIDATE OB-7 shows the
             stratum enumeration of record omits four of its own members.
```

**THIS IS AN ELEVENTH ROW, AND IT IS A MISMATCH.** It does not change the target's verdict
word — it adds a mismatch to a verdict that is already LEVEL-MISMATCHED, and its bridge is
ABSENT like the others. **But the target's §3.4 table is presented as the complete set of
demand–asset pairs, and it is short by this one.**

---

## §7 — HUNTS (G) AND (H): CROSSINGS, LENS TOKENS, NUMBERS. FENCE-SCAN

### §7.1 HUNT (G) — ANY PROPOSAL, ADOPTION, OR ATTEMPT TO CROSS A LEVEL: **NONE**

FENCE-SCAN run over the target's own 1521 lines for first-person proposal/adoption/
authorization/crossing constructions. **Result: CLEAN.** Every hit resolves to one of:

```text
:899   "'Even if adopted.'"                    — inside a quotation of GLUING_CANDIDATE.
:1075  "This commission does not propose one either."   — an express refusal.
:1241  "Adopting the corpus's own refusal is the only rule consistent with the ground."
       — adoption of a METHOD (the corpus's own CL-7 refusal to type by subject
       matter), not of an object, a stratum, a level or a bridge.  LAWFUL.
:1449  "ADOPTED AND PARTIAL"                   — a bridge GRADE of record, reported.
```

No object is adopted. No stratum is assigned to NET or to anything else. No bridge is
built or named beyond those the corpus names — indeed the target names **fewer** than the
corpus does (§3.3, C-3). No demand is satisfied. No level is crossed. No witness, gate or
flag is moved. The three witnesses named at §9 are correctly reported as standing.

Two boundary cases examined and cleared:

```text
- §3.1 MM-1c reports a level bridge that IS traversed of record (per-stratum ->
  stratum sum).  That is a REPORT of a crossing the corpus performed, displayed as
  its positive control.  The target crosses nothing itself.  LAWFUL.
- §5.1 RC-1 equates a LEVEL PROPERTY across the V-2 bar.  The target states the
  limit expressly and refuses to equate the OBJECTS.  Tested: no consequence is
  drawn anywhere from the identification.  LAWFUL.
```

### §7.2 HUNT (H) — LENS TOKENS: **ZERO.** NUMBERS OF PROGRAM IMPORT: **ZERO**

```text
LENS SWEEP over the target: "lens", "lens fence", "LENS_", "lensing"  ->  0 hits.
MEASURED-CONSTANT SWEEP: 137, 1/137, 7.297, 0.0072, "fine-structure", CODATA,
  "measured value", alpha = <digits>, kappa = <digits>  ->  0 hits.
EVERY numeric token in the artifact was extracted and classified.  All are one of:
  section numbers (§1.2, §3.4), line-span citations (:346-351, :45349,45957),
  digest fragments (0a10c030, 80db260f), dates, EXPONENTS (3, 3 - delta, -3, -2,
  -3/2), COUNTS quoted from sealed text (4n^3, H^1 = 1, H^1 = 4, 2 of 4, 3581/3588
  entries, 16 subcubes), THRESHOLDS quoted from sealed text, or counts of items in
  the artifact's own lists (54, 14, five names, four omitted members).
NO VALUE OF ANY PROGRAM QUANTITY APPEARS.  NO MEASURED-CONSTANT COMPARISON APPEARS
OR IS IMPLIED.  The target's IA-7 is substantively CORRECT.
```

Two clerical corrections to IA-7, neither a fence issue:

```text
C-12a  IA-7's enumeration lists "q < 2".  That token occurs NOWHERE in the artifact
       except inside IA-7's own list.
C-12b  IA-7's enumeration omits three numeric tokens that DO occur: "2 sqrt(3)"
       (:539, quoted from W3, where the source itself calls it "a pure number"),
       "M^2/eps^2" (:410, :1118), and "||M|| <= 1" (:539).  All three are quoted
       from sealed text and none is a program quantity.  The list is inaccurate in
       both directions; the fence holds.
```

### §7.3 THE SYMBOLIC RE-RUN — FRESH VENV

The target declares CH-7 NO CAS RUN, on the ground that every comparison is between index
sets and quantifiers. **That ground is correct for the comparisons.** But the target
carries one quoted DERIVATION whose content is algebraic — OB-3 / MM-3c, the step on which
"the cleanest level mismatch in the corpus" rests. This audit re-ran it rather than take it
on testimony.

```text
ENVIRONMENT   fresh python venv created this session; sympy 1.14.0.  Rationals and
              symbols only.  No record object instantiated.  No program quantity.
STEP AS THE TARGET CARRIES IT (W3 §6.2 (g-2)):
  "Evaluating on two admitted cells of distinct 4-volumes V1 != V2 gives
   V1^beta G = K = V2^beta G, whose unique solution is beta = 0 , G = K."
RE-DERIVED   ratio of the two equations = (V1/V2)**beta, required = 1.
             solve((V1/V2)**beta = 1, beta)              ->  [{beta: 0}]
             solve(exp(beta*log(t)) = 1, beta), t > 0     ->  [{beta: 0}]
             back-substitution at beta = 0                ->  G = K.
RESULT       *** THE QUOTED STEP REPRODUCES EXACTLY. beta = 0, G = K is the unique
             real solution. ***
SIDE CONDITION, RECORDED BECAUSE THE TARGET DOES NOT DISPLAY IT (C-15):
             the division requires G != 0, equivalently K != 0.  The source supplies
             it in the same passage the target quotes — "the constant 2 sqrt(3) is a
             pure number" — so the condition is MET of record.  It should have been
             shown; the conclusion is unaffected.
```

---

## §8 — CHOICE LEDGER (this audit; every unforced choice, classified)

```text
AC-1  READING THE TARGET'S FOURTEEN AS ITS GROUND: FORCED.  The target names them
      and seals them; this audit re-verified all fourteen independently.

AC-2  READING BEYOND THE FOURTEEN: *** MINE, AND REQUIRED BY THE COMMISSION. ***
      Hunt (D) directs an INDEPENDENT bridge search and states that one missed bridge
      inverts the finding; that cannot be run inside the target's own cutoff.  Five
      files outside the fourteen were located by declared grep and opened at their
      spans, each seal verified from its own directory (§4).  ALTERNATIVE: audit
      inside the fourteen only.  REFUSED — it would make the bridge hunt circular.
      COST IF WRONG: this audit consumed bytes the target lawfully did not.  Every
      such byte is used ONLY to test a claim, never to add a finding of record, and
      each is attributed to its own file and seal.

AC-3  NOT INHERITING THE TARGET'S [2H] FENCE: MINE.  Where the target read
      second-hand I opened the inner source where the commission's hunts required it
      (GLUED_TOPOLOGY_HUNT for C-2) and said so.  Where I did NOT open an inner
      source, I do not upgrade the target's reliance and I say that too.

AC-4  RUNNING CAS ON ONE STEP THE TARGET DECLINED TO RUN: FORCED-BY-COMMISSION
      ("re-run every symbolic step, fresh venv").  ALTERNATIVE: accept CH-7.
      REFUSED under DEFAULT-REFUTE.  The step reproduces; CH-7's REASONING is
      nonetheless sound for the comparisons, and I say so at §7.3.

AC-5  GRADING C-1 (the imposed ordering) AS NON-LOAD-BEARING: DERIVED, and the
      derivation is displayed at §1.3 rather than asserted — every assignment and
      every bridge grade was re-tested with the seven ordering phrases stripped.
      ALTERNATIVE: score it as the structural failure the commission predicted.
      REFUSED: it would misreport a vocabulary defect as a structural one, and the
      name half — the half this program has actually failed before — is clean.

AC-6  GRADING C-2/C-4/C-5/C-6 AS CORRECTIONS RATHER THAN REFUTATIONS: DERIVED.  In
      each case the narrower statement the record supports yields the SAME grade
      (ABSENT / mismatched).  Each is shown, not assumed, at its own subsection.

AC-7  ADDING AN ELEVENTH DEMAND-ASSET ROW (C-8): DETERMINATION ONLY.  It is
      LOCATED and COMPARED at the corpus's own bytes and its bridge is graded ABSENT.
      Nothing is proposed, adopted, or crossed by displaying it.

AC-8  NOT ADJUDICATING JOIN_ADMISSIBILITY_O28SR: FORCED.  It is a different
      commission's artifact.  It is recorded at §4.1 only for what it supplies to
      THIS question — no bridge — and for a framing conflict I name but do not rule on.

AC-9  ROOT 2 NOT CONSUMED: FORCED-BY-SWEEP.  Listed (3133 entries); every file this
      audit needed resolved in the primary root at a verified seal.

AC-10 NO GIT: FORCED by the commission.
```

---

## §9 — TOY_SEPARATION

```text
THIS ARTIFACT IS A VERIFICATION INSTRUMENT OVER SEALED TEXT.  It re-verifies fifteen
seals, re-runs two token censuses and one arithmetic claim at bytes, re-reads every
quotation past its closing mark into its source sentence, runs one symbolic step in a
fresh venv, and sweeps a 1775-file root for a bridge.  That is its whole content.

NO TOY IS BUILT AND NO TOY COULD SERVE.  No model, no stand-in, no illustrative
example, no scaled-down version of any record object appears.  Every object named is
named as the corpus names it, at its own bytes.

THE ACTUAL SURFACE, NOT A SUBSTITUTE:
  - Counts are re-run, not accepted: L1, L2 and RC-7's zero-co-occurrence arithmetic
    were each recomputed from the files and are reported whether they agreed (all
    three did) or not.
  - The bridge hunt was run OUTSIDE the target's cutoff, because inside it the hunt
    could only have confirmed the target.
  - The symbolic step was re-derived, including the side condition the target did
    not display, rather than carried on the target's citation.
  - Findings that CUT AGAINST this audit are displayed: §5.2 records that the corpus
    DOES carry ordering vocabulary, which narrows my own §1.3; §2.3 records that the
    target quoted the SURVIVING census sentence, which narrows C-4; §3.2 records
    that C-2's truncation was INHERITED, which narrows the blame.
  - Every correction states, in its own subsection, whether it inverts a grade.
    Not one does, and I say so each time rather than leaving the reader to infer it.

WHAT WOULD HAVE MADE THIS A TOY, AND WAS NOT DONE:
  - Grading the target on its own account of itself.  Testimony carried zero weight;
    every quotation was re-located, and the two the target flags as restorations
    were re-checked against their sources rather than credited.
  - Manufacturing a refutation out of C-1 because the commission predicted it.
    The predicted failure IS present, in a weaker form than predicted, and the
    artifact says which form.
  - Suppressing C-16/C-17 because they cut toward the target rather than against it.
  - Building or naming a bridge to close MM-2.  The corpus's OWN named constructor
    (O11 + O12 + connected-composition closure) is reported as UNBUILT, exactly as
    the record states it, and is not proposed, adopted or crossed here.

NOTHING HERE DISCHARGES ANY DEMAND, CROSSES ANY LEVEL, MOVES ANY GATE, RETIRES ANY
WITNESS, ADOPTS ANY OBJECT, RECLASSIFIES ANY STRATUM, OR FLIPS ANY FLAG.
```

---

## §10 — IMPORT AUDIT (this audit)

```text
AIA-1  NO LEVEL NAME IN THIS AUDIT IS MINE.  Every level name I use — cell, oriented
       k-cell, 0-cell, complex, composite, stratum, diamond, patch, pair, chain,
       port, incidence, generator layer, dressed layer, physical stratum,
       audit/prediction stratum, fence class, per-composite, summed — is the
       corpus's own and was tested for a corpus occurrence at §1.1.
AIA-2  I ADOPT NO GROUPING.  I do not endorse or replace the target's V-A/V-B/V-C;
       I test it.  §1.3's finding is about ORDERING, not about the grouping, which is
       declared by the target at CH-3/IA-1 and is lawful as declared.
AIA-3  "LOAD-BEARING" / "NON-LOAD-BEARING" as applied to C-1 is MY typing.  Its
       ground is displayed at §1.3: the assignments and grades were re-tested with
       the seven phrases stripped.
AIA-4  THE FIVE BRIDGE KINDS are the commission's list, not the corpus's.  §4.3
       reports that the fourteen name two objects outside that list (an adjacency;
       an identification yielding a quotient) — a result ABOUT the commission's list.
AIA-5  NO MATHEMATICAL IMPORT.  The one symbolic step at §7.3 is a re-derivation of a
       step already of record in its own sealed artifact, on generic symbols, with
       no record object instantiated.  exp/log over the reals is used only to state
       the uniqueness the source already claims.
AIA-6  NO VALUE, NO PROGRAM QUANTITY, NO MEASURED CONSTANT.  §7.2.
AIA-7  FILES OPENED OUTSIDE THE TARGET'S FOURTEEN are named at §4, each with its
       seal state.  No register / tracker / road / plan / continuation file was
       opened or cited, at pattern level.
```

---

## §11 — FLAG BLOCK — STAGE8_STRATIFICATION_O27SR_AUDIT_V001

```text
alpha_computed                = false
proof_authorized              = false
kappa_record_computed         = false

TARGET_SEAL_VERIFIED          = OK, from the artifact's own directory, before reliance
SEALS_VERIFIED_THIS_AUDIT     = 20/20 OK  (target + its fourteen + five located
                                outside the fourteen).  Zero mismatches.  None
                                unverifiable.
OUTPUT_PATH_PROBED_ABSENT     = true (artifact and sidecar both, before any write)
SWEEP_CUTOFF_DECLARED         = true (§0.3)
CAS_RUN                       = true (fresh venv, sympy 1.14.0; one step; §7.3)
GIT                           = not used
ROOT_2_CONSUMED               = false (listed only, 3133 entries)
REGISTER/TRACKER/ROAD/PLAN/CONTINUATION FILES READ = none
"Q-..." TOKENS                = EXPECTED-UNLOCATABLE, carried opaque, never chased
LENS TOKENS FOUND IN TARGET   = 0
PROGRAM QUANTITIES IN TARGET  = 0
FENCE-SCAN OF TARGET          = CLEAN (no proposal, adoption, reclassification,
                                demand satisfaction, or level crossing)

DETERMINATION ONLY.  NOTHING PROPOSED, AUTHORED, ADOPTED OR RECLASSIFIED BY THIS
AUDIT.  NO DEMAND SATISFIED.  NO LEVEL CROSSED.  NO BRIDGE BUILT.  NO STRATUM
ADOPTED.  NO WITNESS CREATED, MOVED OR RETIRED.  NO GATE MOVED.  NO FENCE MOVED.

STANDING OF RECORD, UNCHANGED BY THIS AUDIT:
  {W-1, W-2, W-3} remains the unique minimal completion set.  FP-1, FP-2, FP-3 STAND.
  FP-S remains decided at its own quantifier.  The V-2 bar STANDS, uncrossed in both
  directions.  NET's stratum remains UNTYPED of record.  Nothing ADOPTED, at the
  physical stratum, yields a joined complex.  E1_RECORD_KINK_COMMUTATOR_FORM_
  UNCERTIFIED, SCAD_HS_SCALING_EXPONENT_UNDERIVED and E1_SEA_OFFDIAGONAL_HS_SHARP_
  RECORD_LOCALIZER all STAND, untouched.
```

---

## §12 — VERDICT

```text
OVERALL VERDICT = CONFIRMED-WITH-CORRECTIONS
```

**The target's central determination is CORRECT AT BYTES. All three demands sit at a level
with no sealed, derived, positive bridge to the assets that could satisfy them. Every one
of its ten mismatch grades survives an independent re-derivation and an independent bridge
hunt run WIDER than its own cutoff. No finding inverts. Seventeen corrections are required
and none of them changes a grade.**

### PER DELIVERABLE

```text
D1  THE LEVEL VOCABULARY            CONFIRMED-WITH-CORRECTIONS
    Both token censuses reproduce file-by-file at bytes.  Every level name in the
    lists has a corpus use; none is imposed; none is replaced.  The five senses of
    "level" and the two of "stratum" are real and are displayed as found.
    CORRECTIONS: C-1 (an ordering between levels asserted at seven undeclared sites,
    including a self-contradiction inside OB-2), C-4 (a uniqueness claim the target's
    own consumed CENSUS_AUDIT kills at KILL GRADE), C-13/C-13b (census label; sweep
    list short by one pattern), C-16 (INC-4's absolute "no sealed sentence ...
    licenses a lift or a descent" is false at bytes), C-17 (the sealed ORDERING
    PRESENT? row never shown).

D2  THE ASSIGNMENT                  CONFIRMED-WITH-CORRECTIONS
    The assignment rule is right, is the corpus's own, and is followed at every
    item but one.  FP-3's V-B index is correctly returned NONE-OF-RECORD.
    CORRECTIONS: C-9 (FP-2's per-composite rests on the section heading, against the
    target's own CH-2; the assignment is right on ground not displayed), C-5 (AC-JOIN
    and U-3 quoted as standing when CENSUS_AUDIT §5.1 overturns both; and B2-ii
    contradicts B2-iii on the same page), C-3 (the corpus's own name for the missing
    bridge — O11 + O12 + the connected-composition closure, 21 occurrences across
    five of the fourteen — is never used).

D3  THE MISMATCH TEST               CONFIRMED-WITH-CORRECTIONS
    *** MM-2 — the finding the commission existed to detect — SURVIVES. ***  W-2 is
    stated at ONE NAMED ORIENTED k-CELL three times in its own text; FP-2 is stated
    at the PER-COMPOSITE VOLUME DIAGONAL; the junction is a would-clause W2-BUILD
    itself flagged; and this audit's independent search of the primary root — not the
    fourteen — finds NO sealed lift, descent, refinement, inclusion or summation that
    connects them.  MM-1a/b/c/d and MM-3a/b/c/d likewise survive; MM-1c's positive
    control (derived, traversed, negative) is confirmed at W1's bytes; MM-3c's
    algebra reproduces exactly in a fresh venv.
    CORRECTIONS: C-2 (a source span truncated mid-item and repeated three times, with
    the record's adjacent "CONSTRUCTIBLE, CONSTRUCTOR NAMED" clause cut), C-6 (W2's
    "PROVED ON THE COMPOSITE" and its passed shape check never displayed, making
    B2-i's "nothing of record says it does" flatter than the source allows), C-7
    (TOY_SEPARATION claims R-1's near-miss is displayed; it is not, and INDEX_AUDIT's
    "CORRECTION REQUIRED" form is not adopted), C-10/C-14/C-2b (minor mis-marks).

D4  THE UNCROSSED BOUNDARY          CONFIRMED-WITH-CORRECTIONS
    The V-2 boundary is a quantifier-order boundary; both sides carry the same n and
    the same four quantities; the three sealed statements that it has never been
    crossed in either direction are exact at bytes; and the distinction between
    COMPARING across it and TRANSPORTING across it is correctly drawn and correctly
    applied.  CORRECTION: C-11 (the fourteen DO name bridge objects outside the five
    kinds — an adjacency and a quotient-yielding identification; the conclusion is
    untouched because neither acts on quantifier order).

D5  THE REVERSE CHECK               CONFIRMED
    All nine reverse-check results hold at bytes.  RC-7's arithmetic re-runs to ZERO
    and is in fact stronger than claimed (zero across all fourteen, not only the
    seven builds).  RC-1's five names all verify and its stated LIMIT is observed
    throughout — no transport, no identification of objects across the V-2 bar.
    NO FALSE MISMATCH EXISTS ANYWHERE IN §3.  This deliverable needs no correction.

HUNT (E), separately scored, since it is the one the comfortable answer hides:
    ONE MISSED MISMATCH FOUND — C-8, W-3's clause-(d) H^1-propagation lemma against
    the only H^1-carrying objects of record, which the corpus types OUT of the
    physical stratum.  An ELEVENTH row, its bridge ABSENT.  It ADDS to a verdict
    already LEVEL-MISMATCHED; it does not change it.
```

### THE FAILURE THE COMMISSION PREDICTED — RULED ON DIRECTLY

```text
(A) AN IMPOSED HIERARCHY.  PRESENT IN ITS ORDERING FORM ONLY, AND NOT LOAD-BEARING.
    No level name is imposed, replaced or reordered — all twenty tested names are the
    corpus's own and the target declares its two exceptions.  No ranked ladder is
    built; the target expressly refuses one and is right that none is of record.
    What IS brought is an ordering RELATION between levels — up / above / below /
    finer / coarser / downward — at seven sites, undeclared in an import audit that
    calls its list exhaustive, and self-contradicting at OB-2.  Strip all seven and
    every assignment, every bridge grade and the verdict stand unchanged.
    *** The program's recurring failure did not recur in its structural form. ***
```

### WHAT THE CORRECTIONS COME TO, STATED PLAINLY

```text
The target read fourteen files, quoted them accurately in the overwhelming majority,
restored one elided level restriction to its sentence end, and got the answer right.
Its characteristic weakness is the opposite of invention: it is DEFERENCE TO A
QUOTED SENTENCE WITHOUT CHECKING WHETHER THE NEXT SENTENCE, OR ITS OWN CONSUMED
AUDIT, HAS ALREADY QUALIFIED IT.  C-2, C-4, C-5 and C-6 are all one defect wearing
four faces: a flat sentence carried forward past the narrowing the record supplies.
In each case the narrower statement yields the same grade — which is why the verdict
is CONFIRMED-WITH-CORRECTIONS and not REFUTED, and why the corrections are worth
making anyway: the record's own narrower form is the one that survives a harder read.
```

### CORRECTION INDEX — SEVENTEEN, NONE INVERTING

```text
C-1   §1.3  an ordering between levels asserted at 7 undeclared sites; OB-2 self-
            contradicts ("not an index restriction" / "a restriction DOWNWARD in
            index").  IA-2's "exhaustively" is false as written.   [HUNT A]
C-2   §3.2  "NOTHING SEALED EXISTS: ... a port" truncated mid-item, unmarked,
            THREE times; five further items and the whole adjacent
            "CONSTRUCTIBLE, CONSTRUCTOR NAMED" clause cut.  Inherited from
            CENSUS; presented as a complete enumeration.                [HUNT F]
C-2b  §3.5  AC-OBJ's CENSUS quote stops mid-sentence; elided tail supports it.
C-3   §3.3  O11 + O12 + connected-composition closure — the corpus's own name for
            the missing bridge, 21 occurrences across 5 of the 14 — never used.
                                                                    [HUNTS B, D]
C-4   §2.3  "the only clause in the corpus that puts a cell on an output side" is
            FALSE at bytes; CENSUS_AUDIT kills it at KILL GRADE.          [HUNT B]
C-5   §2.4  AC-JOIN / U-3 / FLAG BLOCK carry "THE CATEGORY IS EMPTY" as standing;
            CENSUS_AUDIT §5.1 overturns both.  B2-ii contradicts B2-iii. [HUNT B]
C-6   §6.1  W2's "PROVED ON THE COMPOSITE" (:275, :551) and its passed shape check
            (:290-295) never displayed; B2-i's "nothing of record says it does" is
            flatter than the source allows.                              [HUNT E]
C-7   §3.4  TOY_SEPARATION claims R-1's near-miss is displayed; it is not.  The
            INDEX_AUDIT "CORRECTION REQUIRED" form is not adopted.        [HUNT F]
C-8   §6.2  MISSED MISMATCH: W-3(d)'s H^1-propagation lemma vs the only H^1-carrying
            objects of record, typed OUT of the physical stratum.  Eleventh row,
            bridge ABSENT.                                               [HUNT E]
C-9   §2.2  FP-2's per-composite rests on the section heading, against CH-2.  Right
            answer, undisplayed ground.                                  [HUNT B]
C-10  §3.5  W1 §2.2 is a two-column table re-flowed and truncated, marked "verbatim"
            at three sites.                                              [HUNT F]
C-11  §4.3  "Every bridge object named in the fourteen is of one of the five kinds"
            is false — an adjacency and a quotient-yielding identification are named.
            §4's conclusion untouched.
C-12  §7.2  IA-7 lists "q < 2" (absent) and omits 2 sqrt(3), M^2/eps^2, ||M|| <= 1.
            No fence breach.                                             [HUNT H]
C-13  §1.2  "bare word 'level'" mislabels a total count (54 total, 27 bare);
      C-13b the L1 candidate list omits "edge-level", which occurs twice.
C-14  §3.5  "verbatim" applied to a silently capitalised quotation.       [HUNT F]
C-15  §7.3  the beta = 0 uniqueness needs K != 0; supplied by the source, not shown.
C-16  §4.2  INC-4's "no sealed sentence ... licenses a lift, or licenses a descent"
            is false at bytes — sd*_1 : C^1(K') -> C^1(K) sits inside the fourteen.
            Reaches no mismatched pair.                                  [HUNT D]
C-17  §5.2  the sealed "ORDERING PRESENT? — total / PARTIAL ... ANTITONE" row, two
            lines below one the target quotes, is never shown.  Does NOT contradict
            §1.0 read strictly, and I decline to score it as such.       [HUNT C]
```

---

*END OF ARTIFACT — STAGE8_STRATIFICATION_O27SR_AUDIT_V001*
