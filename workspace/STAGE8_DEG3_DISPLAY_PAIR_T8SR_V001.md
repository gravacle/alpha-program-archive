# STAGE 8 — THE DEG3 SYMBOL-AND-MOMENT DISPLAY PAIR: COMPLETING THE DEGREE-3 ANGULAR CENSUS — T8SR V001

## DISPLAY BUILDER — CODENAME DISPLAY-BUILD — COMMISSION T8SR — [SEALED]

Date: 2026-08-15 (session CDT early)
Role: DISPLAY builder (DISPLAY-BUILD). Commission T8SR: THE DEG3
SYMBOL-AND-MOMENT DISPLAY PAIR — the one named object completing the
degree-3 angular census (the T6SR completing object, named at its §5 and
audited CONFIRMED), in two deliverables plus the mechanical completion:
(i) derive and DISPLAY the o = 3 subsidiary segment-corrected angular
symbols from the stock the census pair names (the REM/BL2STAR/R2 stock,
located from the census pair's own citation chain, every seal verified);
(ii) derive the FIRST-ANGULAR-MOMENT CERTIFICATE for the sealed ray
averages beta_s(x, khat) from the MO4 stock (same chain); (iii) IF both
land: complete the census mechanically — evaluate the displayed
unevaluated total L_deg3(x) EXACTLY and fire the armed branch table.
Verdict menu, none preferred: COMPLETE-RATE-CERTIFIED /
COMPLETE-LOG-GENUINE / PARTIAL.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.
DETERMINATION ONLY — no adoption, no authored physics, no member binding.

Fences held: EXACT SYMBOLIC ONLY — CAS (sympy, fresh venv `t8srvenv`
under the session scratchpad), script and output sourced into this
artifact by file-append, verbatim; every constant symbolic or an exact
rational/surd; NOTHING numeric evaluated (Lambda, V_2, beta_s, a_Delta,
all coefficients FORMAL — the record's standing KF1 license); no floats
as ground; no measured constant; no measured-constant comparison; no
value frozen. Scoped reads of NAMED artifacts only (the eight of my
commission + the three stock artifacts + nothing else); no file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened (fenced
classes and all other files matched by FILENAME ONLY in the sweep, never
opened). "Q-..." tokens EXPECTED-UNLOCATABLE per the tasking; noted,
never chased, nothing defaulted. No git action; no existing file edited;
ONE output (this artifact) plus its seal sidecar at the
commission-distinct path, probed ABSENT at session start (04:41:07
re-probe ABSENT immediately before this write). Every enumeration
sentence below claims only its own displayed sweep. SWEEP CUTOFF
DECLARED (§7). SCOPE GUARD held: summed quantifier ONLY; V-2 not
crossed in either direction; no flag of the record flipped (§6). Marks:
DERIVED / CLAIMED / GROUNDED(record cited at grade) /
CONDITIONAL(premise named) span by span.

Homonym discipline, inherited and declared: n = the CARRIER index of the
family of record; k = the momentum variable inside |k| and khat
(contextual, inherited from the sealed bytes); o, c = the REM strata
indices (oscillation order, coincidence count) as quoted at bytes.

---

## 0. VERDICT IN ONE LINE

**COMPLETE-LOG-GENUINE — both commissioned displays are DERIVED on the citation-located, seal-verified stock, and the degree-3 angular census is COMPLETED mechanically with the armed branch FIRED on the L != 0 side: (i) the o = 3 (c = 0) segment-corrected angular symbols are ANGULAR-SCALAR — khat-FREE at the record's own displayed two-level reduction (the parity floors assign EVERY interband/transfer oscillation to c >= 1 blocks, REM §2.3 at bytes; the c = 0 channel is the displayed (x, omega) family, REM §3.3a; the free-transport stratum carrying the ray-average khat-dependence is m_0-killed identically, REM §2.3 / R2 §5(c)) — with the exact subsidiary tower displayed from the sealed stencil identity sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y) at y = V_2(x)/(2|k|), V_2(x) = (8 pi^2/7)(1 - 128|x|^7) closed of record: the degree -2 segment-corrected coefficient is +V_2(x)^2/4, REAL and khat-free (CAS D1) — joint khat-parity EVEN in both placements (AC-4's unclaimed degree-3 parity now CLAIMED AND DERIVED at the record's KF1/KF2 license), so the class-A moments die exactly on the consumed all-lengths lemma: mu_AU(x) = 0 and mu_AF(x) = 0 (CAS D2); (ii) the FIRST-ANGULAR-MOMENT CERTIFICATE for the sealed ray averages beta_s(x, khat) = int_0^1 b_D(t, x + s t khat) dt (MO3 :204, the jet home pinned at full digest by MO4 §1 G-5 and independently by WSR §1; seal verified): with b_D RADIAL at bytes (s_- = t^2 - |x|^2, s_+ = (1-t)^2 - |x|^2, b_D = e^{16 - 1/(s_- s_+)} on the open diamond) the khat-dependence passes through u = khat.x_hat alone, and the two commissioned moments reduce exactly (CAS D3): int_S2 khat_x [beta_+ - beta_-] dOmega = 4 pi x_hat_x M_1(|x|) with M_1(rho) = int_{-1}^1 u H(rho, u) du < 0 STRICT on 0 < rho < 1 (ds/d(r^2) = -(s_- + s_+) < 0 on the diamond — b_D strictly radius-decreasing; the outward ray never meets the diamond, H(rho, +1) = 0; the witness ray positive at exact rationals s = 273/6400 — CAS D4), vanishing EXACTLY on {x_hat_x = 0} u {x = 0} u {|x| >= 1}; and int_S2 khat_x^2 [beta_+ + beta_-] dOmega > 0 STRICTLY at every cell point of the open shell 0 < |x| < 1, every direction — the sealed MO3 §3.5 strict-positivity display ("a nonnegative integrand, strictly positive on an open set — exact positivity, nothing evaluated") CONSUMED at bytes; both moments ZERO exactly outside the shell (support displayed). THE COMPLETION: the census table fills — L_deg3(x) = mu_AU + mu_AF + SUM mu_B = 0 + 0 + 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x) at COR-A's displayed unit aggregation — and is NOT IDENTICALLY ZERO: on the displayed plane locus x_hat_x = 0, 0 < |x| < 1 it equals mu_B^alpha(x) > 0 STRICTLY (CAS D5), and the nonvanishing is INVARIANT under every nonzero weighting of the certified class-B moments (§5.2 L-3 — the undisplayed weight fine-structure cannot un-fire the branch). THE ARMED BRANCH TABLE FIRES: L_deg3 != 0 ==> the degree -3 carrier integral log Lambda is GENUINE at the class total — the commissioned log-free rate is REFUTED, the summed trace count does NOT land at degree <= -4, and the wall's last summed number carries its logarithmic growth at the census grade (per (n, cell); Lambda FORMAL; the 1 - 1/Lambda branch is the road not taken, its condition failing on a displayed open locus). The contributing nonzero moments are displayed WHOLE (§4.3/§5.1). NOT REFUTED discipline: no divergence of tr K^Sigma_n is exhibited — the display exhibits the class's log coefficient nonvanishing at the displayed COR-A aggregation, exactly what the armed branch consumes. Scope: summed quantifier only; V-2 uncrossed; no flag of the record flipped. CAS 6/6 PASS on the single recorded run (sympy 1.14.0, fresh venv; three mechanical pre-run script corrections disclosed §8).**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest via
`shasum -a 256 -c` against its `.seal.sha256` sidecar, BEFORE any
consumption. 12/12 MATCH (OK); NONE UNVERIFIABLE. Scoped reads: these
twelve files and NO others.

THE COMMISSION'S NAMED EIGHT (verified 04:39, before any read):

```text
STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001.md          OK  [CENSUS — the enumeration; the moment functionals; the armed branch table; the completing-object spec §5; read IN FULL]
STAGE8_DEG3_ANGULAR_CENSUS_T6SR_AUDIT_V001.md    OK  [CENSUS-AUDIT — CONFIRMED-WITH-CORRECTIONS; B-1/B-2 re-swept; CA1-CA7; read IN FULL]
STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md            OK  [T4SR — the one-vertex trace-parity lemma (CONSUMED, not re-proved); B-1/B-2; a-6 naming the stock; read IN FULL]
STAGE8_WALL_LOGFREE_RATE_T4SR_AUDIT_V001.md      OK  [T4SR-AUDIT — lemma CONFIRMED+STRENGTHENED to all lengths (R2); COR-A the branch condition of record]
STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md          OK  [WSR — K^Sigma; the floors; t-1..t-3; §1 the stock's exact identities at digest prefixes]
STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md    OK  [WSR-AUDIT — :152 existence; A3; AC-4]
STAGE8_MO4_CANCELLATION_S9AD_V001.md             OK  [MO4 — the beta_s stock side: the delta + CZ jet; the ray averages; the profile split]
STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md       OK  [MO4-AUDIT — the jet display at audited grade]
```

THE STOCK, located FROM THE CENSUS PAIR'S OWN CITATIONS and verified
(04:40): the census §5 names "the REM/BL2STAR/R2 stock (named of record
at T4SR a-6/§4)"; T4SR a-6 names the stock; WSR §1 (:81-:83, in my
named eight, seal-verified) pins the stock's EXACT artifact identities
WITH digest prefixes. My recomputed full digests MATCH the WSR-quoted
prefixes exactly:

```text
2e4e5163  STAGE8_REMAINDER_UNIFORMITY_V001.md    OK, prefix MATCH  [REM — §2.2-§2.3 the (o,c) taxonomy + parity floors; §3.2 the double degree-0 kill; §3.3 the V_2 (x) V_2 leading stratum + subsidiaries; §3.5]
6e81ae92  STAGE8_BL2STAR_ATTEMPT_V001.md         OK, prefix MATCH  [BL2STAR — :337-360 the X display + F definition; the stencil; sigma_intra(X) = -i a_Delta khat_x b~_P C(k)]
a7969f0c  STAGE8_R2_RESUMMATION_V001.md          OK, prefix MATCH  [R2 — SURVIVOR_FATE = CANCELS; the m_0 kill both factors; the V_2 subsidiary -i V_2(x)/(2|k|)]
```

THE JET HOME, one disclosed hop down the same chain (census §5 (ii) ->
the MO4 pair -> MO4 §1 G-5 pins it at FULL digest; independently pinned
in WSR §1 at prefix 9fdc3d1c as tasked ground), verified before reading:

```text
9fdc3d1c  STAGE8_MO3_P_EXPONENT_S9AD_V001.md     OK, digest MATCH against MO4 §1's G-5 pin  [MO3 — the delta + CZ jet closed coefficients; beta_s := int_0^1 b_D(t, x + s t khat) dt (:204); b_D bytes (:123-126); the parity identity (:220-221); support + ray witness + the §3.5 positivity displays; the §4.1 sandwich survivor]
```

Output path probed ABSENT at session start (04:38) and re-probed ABSENT
immediately before this write (04:41:07).

---
## 2. THE COMMISSIONED OBJECT AND ITS BAR, AT BYTES

```text
(q-1) THE OBJECT (census §5, audited CONFIRMED): the DEGREE-3
      SYMBOL-AND-MOMENT DISPLAY PAIR — (i) a sealed display of the
      (o = 3, c = 0) segment-corrected subsidiaries' angular symbols,
      one order past the record's V_2 (x) V_2 display, derived from
      the REM/BL2STAR/R2 stock; (ii) a first-angular-moment
      certificate for the ray averages beta_s(x, khat) — the exact
      values (or exact vanishing) of int_S2 khat_x [beta_+ - beta_-]
      dOmega and int_S2 khat_x^2 [beta_+ + beta_-] dOmega per cell
      point x — from the MO4 stock.
(q-2) THE BAR (census §4 s-1..s-3, COR-A of record): with the pair in
      hand every resisting slot of the census's §3.2 table fills
      mechanically at the record's KF1/KF2 license; the displayed
      moments SUM to L_deg3(x); the armed branch table fires:
      L_deg3 = 0 ==> count at degree <= -4, carrier integral
      1 - 1/Lambda BOUNDED — RATE-CERTIFIED on the consumed lemma;
      L_deg3 != 0 ==> the log GENUINE at the class total — the rate
      refuted. Lambda FORMAL throughout.
(q-3) THE CONSUMED INSTRUMENT (not re-proved): the one-vertex
      trace-parity lemma at audited all-lengths grade (T4SR §3.1
      a-1..a-3; T4SR-AUDIT R2: joint inversion = S-conjugation) —
      every census slot's spinor trace factor is joint-ODD; each first
      moment depends ONLY on its scalar amplitude's odd part (census
      C3, audit CA4 at complete generic forms — consumed).
(q-4) THE CENSUS SLOT TABLE CONSUMED WHOLE (census §3.2, audited
      CA2/CA6): CLASS A placements A-U (mu_AU) and A-F (mu_AF);
      CLASS B c-routes with the two displayed traced-slot functionals
      mu_B^I(x) = 4 int_S2 khat_x [beta_+ - beta_-] dOmega and
      mu_B^alpha(x) = 4 int_S2 khat_x^2 [beta_+ + beta_-] dOmega;
      L_deg3 = mu_AU + mu_AF + SUM_{c-routes} mu_B.
```

---
## 3. DELIVERABLE (i) — THE o = 3 SEGMENT-CORRECTED ANGULAR SYMBOLS, DERIVED AND DISPLAYED

### 3.1 The stock's c = 0 channel displays, at bytes (the ground of the derivation)

```text
(s-1) THE TAXONOMY SPLIT (REM §2.1-§2.3 at bytes): every nonvanishing
      X-term carries oscillatory factors of exactly two displayed kinds
      — "a pure excursion phase e^{-i DeltaE s} integrated against BV
      amplitudes" (the intraband two-level channel) "or the interband
      flip-time oscillation at |k'|+|k|" (REM §2.1 verbatim). The
      parity floors assign the second kind to the interband block,
      which carries stays: "interband block: flips odd (>= 1) AND
      stays = n - flips odd (>= 1): o >= 1 AND c >= 1" (REM §2.3
      verbatim). HENCE, DERIVED BY THE FLOORS ALONE: every c = 0
      per-factor stratum is PURE sea-sea two-level content — its
      oscillation is the excursion phase at DeltaE(|k|), and its
      amplitudes are the two-level amplitudes.
(s-2) THE TWO-LEVEL CHANNEL IS DISPLAYED AS AN (x, omega) FAMILY at
      the sealed bytes: "the two-level sea-sea amplitude obeys
      |A_lambda(x, omega) - 1| <= C_rate(x) lambda^2 / omega" and
      "|sigma_U^rem(x, omega)| <= 2 C_rate(x)/omega, and identically
      |sigma_F^rem(x, omega)| <= C_F(x)/omega (same mechanism through
      the J-sandwich)" (REM §3.3a verbatim); "Within the two-level
      reduction, x is a PARAMETER ... nested sharp indicators
      multiply, the band phases carry no x-derivatives" (REM §3.3
      verbatim); R2 §4 displays the full resummed intraband symbol as
      "this omega-interpolating family". NO khat appears in any
      displayed c = 0 amplitude: the channel's k-dependence enters
      ONLY through omega(|k|). This is the record's own KF1/KF2
      pointwise-symbol grade — the grade the census's completion spec
      licenses ("completes every resisting slot mechanically at the
      record's KF1/KF2 license", census §5).
(s-3) THE EXACT SUBSIDIARY TOWER (R2 §4 at bytes, the stencil
      identity): "The level-repulsion phase per lambda is
      e^{i lambda^2 y}, y = V_2(x)/(2 omega), and the stencil gives
      EXACTLY sum_l w_l e^{i lambda^2 y} = -i e^{i y} sin(y)" with
      "V_2(x) = int v~(t,x)^2 dt = (8 pi^2/7)(1 - 128 |x|^7)" (R2 §7
      V7a) — "so U(0)'s intraband symbol falls off as -i V_2(x)/(2|k|)
      at leading order (times the free phase)". The F-side leading
      coefficient rides m_2 = -1 with the OPPOSITE sign, "+i
      V_2-form/(2|k|) via m_2 = -1, §6 R1d, with V_2^{[0,t']} +
      V_2^{[t',1]} = V_2 exactly across the J-junction, §6 R6b"
      (REM §2.3 verbatim); the F-side stay vertex is the longitudinal
      intraband part "-khat_x C(k) — the smooth vertex has NO
      coincidence zero" (REM §2.2; WSR t-2; A3 of record).
(s-4) THE m_0 KILL OF THE FREE-TRANSPORT STRATUM (REM §2.3 verbatim):
      "The bare-J stratum (n = 0) is m_0-killed identically, so F has
      NO lambda-independent content at ANY degree." The khat-DEPENDENT
      null-ray-average dressing of the vertex (the beta_s structure,
      MO3 §4.1 = the sealed two-time survivor -i a_Delta khat_x
      b~ C(k) of BL2STAR) lives on exactly that killed stratum at
      c = 0 — and on the c-bearing classes (class B), where the census
      put it. R2 §5(c) at bytes: "THE SURVIVOR -i a_Delta khat_x
      b~(x,khat) C(k): CANCELS at degree 0 in the summed assembly."
```

### 3.2 THE DISPLAY — the (o = 3, c = 0) class-A angular symbols [DERIVED; CAS D1/D2]

The exact expansion of the sealed stencil identity (D1):

```text
  sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y) = (1/2)(1 - e^{2iy})
                             = -i y + y^2 + (2i/3) y^3 + O(y^4),
  y = V_2(x)/(2|k|)   [the record's leading term -i y = -i V_2(x)/(2|k|)
                       pins the normalization at bytes]
```

— term by term khat-FREE: an ANGULAR-SCALAR tower. Per slot:

```text
A-U  [U-side degree -2 segment-corrected subsidiary]
     s_U^(2)(x, khat) = s_U^(2)(x) = + V_2(x)^2 / 4     [the y^2 term;
       coefficient of 1/|k|^2 in the tower; V_2(x) = (8 pi^2/7)
       (1 - 128|x|^7) closed of record]  PLUS the second-order
       averaged-system remainder r_U(x) [same (x, omega) class at the
       displayed reduction (s-2); its x-form undisplayed and NOT
       needed: it shares the khat-free TYPE].
     JOINT KHAT-PARITY of the scalar amplitude: EVEN (khat-free).
     The slot's spinor/vertex factor: the partner F-side V_2-vertex
       traced factor T_F^{V2}(khat), first-moment class -2 khat_x
       (A3 of record; consumed).
     EXACT FIRST MOMENT (CAS D2):
       mu_AU(x) = int_S2 s_U^(2)(x) T_F^{V2}(khat) dOmega
                = s_U^(2)(x) . int_S2 (-2 khat_x) dOmega = 0  EXACTLY.

A-F  [F-side degree -2 segment-corrected subsidiary]
     a_F^(2)(x, khat) = a_F^(2)(x): the SAME lambda-stencil tower
       through the J-sandwich — the sub-interval level-repulsion
       phases compose across the junction (V_2^{[0,t']} + V_2^{[t',1]}
       = V_2, REM §6 R6b at bytes; J is lambda-independent, so the
       weight sum acts on the phases alone and gives the SAME exact
       function -i e^{iy} sin(y); D1) — with the F-side sign
       convention (+i at order y via m_2 = -1, REM §6 R1d): the
       degree -2 scalar coefficient is again the y^2 term, khat-FREE,
       x-profile V_2(x)^2/4-form times the sealed J-junction weight
       [b_D-pointwise, bounded, x-profile], PLUS the (x, omega)-class
       remainder r_F(x) (undisplayed x-form; khat-free TYPE).
     JOINT KHAT-PARITY of the scalar amplitude: EVEN (khat-free).
     The slot's spinor factor: the J-stay vertex -khat_x C(k) times
       the U-side angular-scalar V_2 prefactor -i V_2(x)/(2|k|)
       carried outside the angular integral (census e-3 grammar);
       traced slot joint-ODD (the consumed lemma).
     EXACT FIRST MOMENT (CAS D2):
       mu_AF(x) = a_F^(2)(x) . int_S2 T_F^(2)(khat) dOmega = 0
       EXACTLY (T_F^(2) joint-odd; the first moment of EVERY odd
       trace polynomial vanishes — census C3/audit CA4 grammar,
       re-instantiated D2).
```

```text
DELIVERABLE (i) STATUS: LANDED. The o = 3 (c = 0) segment-corrected
angular symbols are DISPLAYED: angular-SCALAR (khat-free) at the
record's own displayed two-level reduction — parity EVEN in both
placements, with the exact level-repulsion coefficient V_2(x)^2/4
displayed in closed form and the averaged-system remainders typed
(x, omega)-class at the same sealed displays. AC-4's unclaimed parity
is now CLAIMED AND DERIVED at this grade: the class-A moments vanish
exactly: mu_AU(x) = 0 ; mu_AF(x) = 0.
GRADE: DERIVED at the record's KF1/KF2 pointwise-symbol license —
the same license the sealed V_2 (x) V_2 kill consumptions carry and
the exact license the census's completion spec names. The khat-free
typing consumes the displayed floors (s-1), the displayed (x, omega)
family (s-2), and the displayed m_0 kill of the free-transport
stratum (s-4); no stratum content is authored beyond the displayed
tower and the typed remainders.
```

---
## 4. DELIVERABLE (ii) — THE FIRST-ANGULAR-MOMENT CERTIFICATE FOR beta_s(x, khat)

### 4.1 The sealed definition and its displayed properties, at bytes

The census names the Gram-transfer stock (the MO4 pair) for this side;
MO4's OWN §1 pins the jet's home at full digest — G-5 =
`STAGE8_MO3_P_EXPONENT_S9AD_V001.md` (9fdc3d1c..., seal verified by me,
prefix match against MO4 §1 AND against WSR §1's independent pin of the
same artifact). The citation chain census -> MO4 -> G-5 is two hops of
verified seals; MO3 is read at the tasked spans only. At bytes:

```text
(b-1) THE DEFINITION (MO3 :204): beta_s(x, khat) := int_0^1
      b_D(t, x + s t khat) dt, s in {+,-} — the null-ray averages of
      the sealed diamond profile.
(b-2) THE PROFILE BYTES (MO3 :123-126): J(t) = -(Q b_D(t,x) Q) tensor
      alpha_x; b_D(t,x) = exp(16 - 1/s(t,x)), s = s_- s_+,
      s_- = t^2 - |x|^2, s_+ = (1-t)^2 - |x|^2; support the open
      diamond {|x| < min(t, 1-t)}; C^infinity; b_D(1/2, 0) = 1,
      ||b_D||_inf = 1. RADIAL in x at every t.
(b-3) DISPLAYED PROPERTIES (MO3 :218-221): beta_s smooth in (x, khat),
      real, >= 0, <= 1; THE PARITY IDENTITY: beta_+(x, khat) =
      beta_-(x, -khat), "so Sigma := beta_+ + beta_- is even and
      beta_+ - beta_- odd under khat -> -khat; a_0 is even in khat."
(b-4) SUPPORT (MO3 §3.5 verbatim): for |x| >= 1 no null ray from
      (0, x) reaches the open diamond; at x = 0 every ray rides the
      lower light cone (s_- = 0): "beta == 0. The leading jet is
      supported exactly on 0 < |x| < 1."
(b-5) THE RAY WITNESS (MO3 §3.5 verbatim): for every 0 < |x| < 1,
      khat = -x_hat, t = |x| lands the transported point on the
      diamond's interior spine, s(t, 0) = t^2(1-t)^2 > 0; "Hence
      beta_+(x, -x_hat) > 0 and Sigma(x, .) > 0 on an open set of
      directions." Exact-rational instance at bytes: x = (1/4)e_x,
      khat = -e_x, t = 3/10: s_- = 7/80, s_+ = 39/80,
      s = 273/6400 > 0, b_D = e^{16 - 6400/273} > 0 — no float.
(b-6) THE ALPHA-SLOT POSITIVITY, DISPLAYED OF RECORD (MO3 §3.5
      verbatim): "CONTACT TERM c(x) != 0 on 0 < |x| < 1: the
      alpha_x-component of c(x) is (1/8 pi) int dOmega khat_x^2
      Sigma(x, khat) > 0 — a nonnegative integrand, strictly positive
      on an open set. Exact positivity; nothing evaluated."
(b-7) THE SANDWICH SURVIVOR (MO3 §4.1 verbatim): P_-(khat) a_0
      P_-(khat) = -i a_Delta khat_x beta_-(x, khat) P_-(khat) —
      "VERBATIM the sealed survivor sigma(C T_1^{(0)} C) = -i a_Delta
      khat_x b_tilde(x, khat) C(k)" with b_tilde = beta_-; "its
      contact coefficient's alpha-sector carries the strictly
      positive weight khat_x^2 beta_-."
```

### 4.2 The radial reduction [DERIVED; CAS D3]

b_D radial (b-2) makes the khat-dependence of each ray average pass
through the single variable u := khat . x_hat:

```text
  |x + s t khat|^2 = |x|^2 + 2 s t |x| u + t^2 ,   so
  beta_s(x, khat) = H(|x|, s u),
  H(rho, u) := int_0^1 B(t, sqrt(rho^2 + 2 t rho u + t^2)) dt,
  B(t, r) := b_D at radius r  [= exp(16 - 1/((t^2 - r^2)((1-t)^2
              - r^2))) on the diamond, 0 outside].
```

The two sphere-reduction identities (verified exactly on the monomial
basis u^m, m = 0..6, at a generic symbolic axis — CAS D3):

```text
  int_S2 khat_a f(khat.b) dOmega  = 2 pi b_a int_{-1}^1 u f(u) du ,
  int_S2 khat_a khat_c f(khat.b) dOmega
    = 2 pi int_{-1}^1 [ ((1-u^2)/2) delta_ac + ((3u^2 - 1)/2) b_a b_c ]
      f(u) du            (|b| = 1).
```

### 4.3 THE CERTIFICATE [DERIVED; CAS D3/D4]

```text
(M-I) THE ODD-SLOT MOMENT, EXACT:
   int_S2 khat_x [beta_+ - beta_-](x, khat) dOmega = 4 pi x_hat_x M_1(|x|),
   M_1(rho) := int_{-1}^1 u H(rho, u) du .
   SIGN, DERIVED EXACTLY: on the diamond, ds/d(r^2) = -(s_- + s_+) < 0
   (polynomial identity, CAS D4) — b_D is STRICTLY DECREASING in the
   spatial radius at fixed t on the diamond, zero outside; the ray
   radius sqrt(rho^2 + 2 t rho u + t^2) is STRICTLY INCREASING in u
   (t, rho > 0); hence H(rho, u) is nonincreasing in u pointwise.
   H(rho, +1) = 0 for every rho > 0 (the outward ray never meets the
   diamond: rho + t > t >= min(t, 1-t) for t <= 1/2, and rho + t >
   t > 1 - t = min for t > 1/2 — exact); H(rho, -1) > 0 on
   0 < rho < 1 (the ray witness b-5). With H continuous (b_D smooth,
   b-2), H(-u) > H(u) on an open u-interval: M_1(rho) =
   int_0^1 u [H(u) - H(-u)] du < 0 STRICTLY on 0 < rho < 1.
   THEREFORE: mu_B^I(x) = 4 . 4 pi x_hat_x M_1(|x|) = 16 pi x_hat_x
   M_1(|x|), with M_1 < 0 strict on 0 < |x| < 1 —
     = 0 exactly IFF x_hat_x = 0 (and at x = 0, |x| >= 1: zero by
       support b-4);
     != 0 exactly at every cell point with x_hat_x != 0, 0 < |x| < 1,
       with the displayed sign (-sgn(x_hat_x) . |16 pi M_1|).
(M-II) THE EVEN-SLOT MOMENT, EXACT:
   int_S2 khat_x^2 [beta_+ + beta_-](x, khat) dOmega
     = 2 pi int_{-1}^1 [ x_hat_x^2 u^2 + ((1 - u^2)/2)(1 - x_hat_x^2) ]
       SigmaH(rho, u) du ,   SigmaH := H(rho, u) + H(rho, -u) >= 0.
   POSITIVITY: the bracket is >= 0 and vanishes only on the
   measure-zero set {u^2 = 1, x_hat_x = 0} u {u = 0, x_hat_x^2 = 1};
   SigmaH > 0 on an open u-set at every 0 < rho < 1 (ray witness b-5).
   THEREFORE: mu_B^alpha(x) = 4 . (the integral) > 0 STRICTLY at
   EVERY cell point with 0 < |x| < 1, EVERY direction x_hat —
   CONSUMING the record's own displayed strict positivity (b-6):
   mu_B^alpha(x) = 8 pi . [the alpha_x-component of c(x)] > 0 —
   the certificate here is the sealed display re-derived through the
   radial reduction, not a new claim; = 0 exactly at x = 0 and
   |x| >= 1 (support b-4).
```

```text
DELIVERABLE (ii) STATUS: LANDED. The first-angular-moment certificate
for the sealed ray averages is DISPLAYED per cell point x:
  int khat_x [beta_+ - beta_-] dOmega   = 4 pi x_hat_x M_1(|x|),
     M_1 < 0 strict on 0 < |x| < 1; vanishes exactly on
     {x_hat_x = 0} u {x = 0} u {|x| >= 1};
  int khat_x^2 [beta_+ + beta_-] dOmega > 0 STRICT on the whole open
     shell 0 < |x| < 1 (every direction), = 0 exactly outside —
     the sealed MO3 §3.5 positivity display consumed at bytes.
M_1(rho) is displayed as the exact one-dimensional functional of the
sealed profile (no closed elementary form claimed; none needed: sign,
support, and vanishing locus are exact). NOTHING numeric evaluated.
```

---
## 5. THE MECHANICAL COMPLETION — L_deg3(x) EVALUATED EXACTLY; THE ARMED BRANCH FIRED

### 5.1 The census table, filled [both displays landed; CAS D2/D3/D5]

```text
  mu_AU(x)      = 0                          EXACT (deliverable (i))
  mu_AF(x)      = 0                          EXACT (deliverable (i))
  mu_B^I(x)     = 16 pi x_hat_x M_1(|x|)     EXACT (deliverable (ii));
                  M_1 < 0 strict on 0 < |x| < 1
  mu_B^alpha(x) = 8 pi c_alpha(x) > 0        EXACT (deliverable (ii));
                  c_alpha(x) := (1/8 pi) int dOmega khat_x^2 Sigma —
                  the sealed MO3 §3.5 strictly-positive display;
                  support exactly 0 < |x| < 1
```

### 5.2 The sum per COR-A, evaluated at the displayed aggregation [CAS D5]

COR-A of record (T4SR-AUDIT §4, consumed by the census s-1, audited
CA6: "one term per class, none missing, none extra"):

```text
  L_deg3(x) = mu_AU(x) + mu_AF(x) + SUM_{c-routes} mu_B(x)
            = 0 + 0 + [ mu_B^I(x) + mu_B^alpha(x) ]
            = 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x) .
```

NONVANISHING, with the witness locus displayed:

```text
(L-1) ON THE PLANE LOCUS x_hat_x = 0, 0 < |x| < 1:
      L_deg3(x) = mu_B^alpha(x) > 0 STRICTLY — an open set of cell
      points on which the displayed total is STRICTLY POSITIVE
      (exact-rational witness ray at bytes, b-5: e.g. the cell point
      x = (1/4) e_y has x_hat_x = 0 and 0 < |x| < 1; the witness ray
      khat = -x_hat with s = 273/6400-type exact rationals certifies
      Sigma > 0 there — no float).
(L-2) HENCE L_deg3(x) IS NOT IDENTICALLY ZERO: the branch condition
      L_deg3 = 0 FAILS on a displayed open locus.
(L-3) ROBUSTNESS ACROSS AGGREGATIONS (the census marked the entering
      combination undisplayed; COR-A's displayed aggregation is
      unit-weight — s-1/CA6): for EVERY aggregation w_I mu_B^I +
      w_alpha mu_B^alpha with (w_I, w_alpha) != (0, 0):
        w_alpha != 0  ==>  nonzero on the plane locus (L-1);
        w_alpha  = 0, w_I != 0  ==>  nonzero at every x_hat_x != 0,
          0 < |x| < 1 (M-I strict sign).
      EVERY nonempty weighting of the certified class-B moments is a
      nonvanishing L_deg3: the fired branch is INVARIANT under the
      undisplayed weight fine-structure. (An EMPTY class-B entry would
      contradict the census's own audited enumeration — the c-routes
      exist of record, WSR-AUDIT :152.)
(L-4) THE PLACEMENT/PHASE BOOKKEEPING (consistency; CAS D6): the two
      placements (U-side / F-side) are adjoint partners inside the
      Hermitian K^Sigma = C(U(0)^dag F + F^dag U(0))C — their
      contributions conjugate-pair to 2 Re[.]; the displayed factor
      phases ((-i) at the U-side V_2, (+i) at the F-side V_2-form,
      (i a_Delta) at the jet, real traced slots by the lemma) compose
      to REAL slot values — no hidden i-kill exists at the displayed
      grade; the sealed sea-sandwich survivor (b-7) selects the
      combination khat_x beta_- whose first moment is
      -(1/8) mu_B^I(x) — inside the (L-3) robustness family.
```

### 5.3 THE BRANCH, FIRED [CAS D5; Lambda FORMAL]

```text
  L_deg3(x) != 0  (nonvanishing displayed: L-1/L-2, strict positive
                   witness locus; robust L-3)
  ==>  per the armed branch table of record (census s-3, audited
       CA7; T4SR §4 / COR-A): THE LOG IS GENUINE AT THE CLASS TOTAL —
       the degree -3 carrier integral int_1^Lambda k^2 k^-3 dk =
       log Lambda (divergent, formal) SURVIVES with the displayed
       nonzero coefficient; the commissioned log-free rate is
       REFUTED; the summed trace count does NOT land at degree <= -4.
       The RATE-CERTIFIED branch (1 - 1/Lambda, bounded) is the road
       NOT taken: its condition L_deg3 = 0 fails on the displayed
       locus.
```

THE CERTIFICATION CHAIN, DISPLAYED WHOLE: the one-vertex trace-parity
lemma (consumed at audited all-lengths grade) reduces every slot to
its scalar amplitude's odd part (census C3/CA4) -> deliverable (i):
the class-A amplitudes have NO odd part (khat-free at the sealed
two-level displays; the exact stencil tower displayed): mu_AU =
mu_AF = 0 -> deliverable (ii): the class-B moments are the two
certified functionals, with mu_B^alpha > 0 STRICT on the open shell
(the sealed MO3 §3.5 positivity consumed at bytes) and mu_B^I of
displayed strict sign off the plane -> COR-A sums the displayed
moments: L_deg3(x) = 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x), NOT
identically zero (strictly positive on the displayed witness locus)
-> the armed table fires the L != 0 branch: the log GENUINE, the
rate refuted, the census COMPLETE.

NOT REFUTED / NOTHING OVERCLAIMED: no divergence of tr K^Sigma_n is
exhibited — the display exhibits the CLASS's log coefficient nonzero
at the COR-A aggregation grade (the class total; an accidental
cancellation AGAINST content outside the enumerated degree-3 class,
or a finer-than-displayed weighting, is excluded only to the extent
L-3 displays); the wall's last summed number acquires its GENUINE
logarithmic growth at the census grade, per (n, cell), Lambda formal.

---
## 6. SCOPE GUARD AND QUANTIFIER HONESTY

```text
(g-1) SUMMED ONLY: every claim above is OF the summed object K^Sigma
      at its own quantifier, per (n, cell). No per-composite or
      per-pair quantity is quoted, moved, or decided; V-2 is not
      crossed in either direction. The MO3 jet is consumed exactly as
      the census consumed it — as the sealed display of the c-class
      coefficients' ray-average structure (the T4SR/census typing,
      audited twice); its per-pair frame content (R_n, determinants,
      MO-4-R) is NOT touched; the per-composite wall, T_n, o-2-OBS,
      B-L2*, MO-2, MO-3's own p = -3 verdict, the WBC enclosure, and
      every sealed identity stand UNTOUCHED at their own quantifiers.
(g-2) NO FLAG FLIPPED: the census verdict (CENSUS-INCOMPLETE, audited)
      is COMPLETED by supplying its named completing object, not
      moved; the T4SR verdict (UNDECIDABLE-TODAY on its five seals)
      stands as sealed — this artifact decides the question on a
      STRICTLY LARGER named basis, exactly as T4SR's own successor
      note licensed ("census-shaped work at the KF1/KF2 license, not
      new physics"). Witness status is the registrar's; no witness
      action is taken here.
(g-3) GRADE HONESTY, span by span: deliverable (i) is DERIVED at the
      record's KF1/KF2 pointwise-symbol license from the sealed
      displays (the floors s-1; the (x, omega) family s-2; the exact
      stencil tower s-3 with its closed V_2; the m_0 kill s-4); the
      averaged-system remainders r_U, r_F are TYPED (khat-free class)
      at the same displays, their x-forms undisplayed and unneeded —
      no stratum content authored. Deliverable (ii) is DERIVED from
      the MO3 displays (definition, bytes, parity, support, witness)
      with the alpha-slot strict positivity CONSUMED at MO3 §3.5's
      own displayed grade; M_1's strict sign is DERIVED by the exact
      monotonicity mechanism (D4). The completion (iii) is evaluated
      at COR-A's DISPLAYED AGGREGATION grade (census s-1, audit CA6:
      one term per class, unit weights), with the (L-3) robustness
      display covering every nonzero weighting; the branch fired is
      invariant under that family. The lemma is consumed at audited
      all-lengths grade, never re-proved. The jet-to-c-class typing
      is GROUNDED (T4SR B-2 / census B-*, audited) — not re-derived.
(g-4) NOT REFUTED discipline: no divergence of tr K^Sigma_n is
      exhibited; what is exhibited is the CLASS's log coefficient
      L_deg3(x) nonvanishing at the displayed aggregation — the
      armed branch's own condition, fired as armed.
```

---

## 7. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 05:05:39 -0500**. Corpus roots:
`/Users/bgm/MB Work/alpha-program-archive/workspace` (primary) and the
cleanroom root
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`
— swept by FILENAME ONLY except the twelve §1 files (token greps ran
inside those twelve only); fenced classes
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF) and ALL other
files matched by filename only, never opened. Artifacts sealed after
the cutoff are UNSWEPT and unclassed here.

```text
sw-1 OCCUPATION: the ONLY filename in either root matching
     T8SR|DISPLAY_PAIR is this artifact itself (output path ABSENT at
     session start 04:38 and at the 04:41:07 pre-write re-probe; the
     cleanroom root: zero matches on T8SR|DISPLAY_PAIR|DEG3). NO
     other artifact occupies this commission.
sw-2 CITATION-CHAIN LOCATION (displayed, §1/§4.1): the stock located
     from the census pair's own citations — census §5 -> T4SR a-6/§4
     (names the stock) -> WSR §1 :81-:83 (pins the three identities
     at digest prefixes; my full digests MATCH); census §5 (ii) ->
     the MO4 pair (named in my eight) -> MO4 §1 G-5 (pins MO3 at
     full digest; my digest MATCHES; independently pinned in WSR §1
     as tasked ground). Every hop seal-verified before reading.
sw-3 ADJACENCY AT CUTOFF (filename/mtime only, unopened): the newest
     workspace artifact is STAGE8_GRAVITY_CLOSE_RESCORE_T9SR_V001
     (a different commission by name; not opened), then this
     artifact, then the census pair. "Q-..." tokens inside the
     sealed stock (Q-1042 etc.): EXPECTED-UNLOCATABLE per the
     tasking; noted, never chased, nothing defaulted.
sw-4 The displays of §3-§5 claim exhaustiveness ONLY over the eleven
     named seals at the spans read and the displayed CAS sweeps.
```

---
## 8. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `t8srvenv` under the session scratchpad;
nothing written to the workspace but this artifact and its seal. Exact
symbolic / exact rational/surd only; nothing numeric evaluated. ONE
battery, 6 checks. Disclosure (fence honesty): the script required
three mechanical sympy-API corrections before the recorded run (an
exp-rewrite ahead of one simplify; a Min-relational reformulation; the
unit-sphere substitution for the 4x4 sandwich identity) — no check was
weakened or removed; the RECORDED run below is a single run, 6/6 PASS.

```python
# DISPLAY-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv t8srvenv).
# Commission T8SR: the DEG3 symbol-and-moment display pair. The one-vertex
# trace-parity lemma is CONSUMED (T4SR, audited all lengths), NOT re-proved.
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated
# (V_2, beta_s, a_Delta, Lambda, all coefficients FORMAL). Groups:
#   D1 the sealed stencil identity and its subsidiary tower: sum_l w_l
#      e^{i lambda^2 y} = -i e^{iy} sin(y) EXACT at the record's weights; series
#      -i y + y^2 + (2i/3) y^3 + O(y^4) — the degree -2 segment-corrected
#      coefficient is +y^2 (khat-FREE); moments m_0 = m_1 = 0, m_2 = -1 and the
#      REM R1d form sum w_l (1 - i l^2 y) = +i y re-pinned;
#   D2 the class-A moment kills: tr[alpha_x C(k)] = -2 khat_x (A3 consistency);
#      int_S2 (-2 khat_x) dOmega = 0; a khat-FREE scalar amplitude times EVERY
#      complete-generic joint-odd trace polynomial (deg 1 + all ten deg-3 odd
#      monomials) has first moment 0: mu_AU = 0 and mu_AF = 0 EXACT;
#   D3 the radial reduction identities on the monomial basis f = u^m (m <= 6) at
#      a generic symbolic axis, then the two certificate reductions on a generic
#      cubic H-schema: int khat_x [H(u)-H(-u)] dOmega = 4 pi b_x M_1 and the
#      even-slot bracket reduction — the census functionals mu_B^I = 16 pi b_x
#      M_1 and mu_B^alpha = 4 x (bracket integral) EXACT;
#   D4 the b_D monotonicity and the witness: ds/d(r^2) = -(s_- + s_+) polynomial
#      identity (strictly negative on the diamond); the exact-rational witness
#      point (t, r) = (3/10, 1/20): s_- = 7/80, s_+ = 39/80, s = 273/6400; the
#      outward-ray vanishing H(rho, +1) = 0 arithmetic (rho + t > min(t, 1-t));
#      the monotone-schema exhibit int_{-1}^1 u H du < 0 at H = h(1-u), h > 0;
#   D5 the COR-A completion arithmetic: L = 0 + 0 + mu_B^I + mu_B^alpha; on the
#      plane locus b_x = 0: L = mu_B^alpha = 8 pi c_alpha > 0 (c_alpha the
#      sealed positive display, symbolic positive); branch integrals I3 =
#      log Lambda (divergent formal), I4 = 1 - 1/Lambda -> 1 bounded;
#   D6 the placement/phase and sandwich consistency: the jet traced slots
#      re-pinned (4 khat_x (b+ - b-); 4 khat_x^2 (b+ + b-)); P_- a_0 P_- =
#      -i a_Delta khat_x beta_- P_- as a matrix identity; the survivor's moment
#      = -(1/8) mu_B^I on the H-schema; (-i)(+i) = 1 (the placement pair
#      composes real).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)
Zq = lambda M: sp.expand(M) == sp.zeros(*M.shape)

# ===== shared ground: exact 4x4 Dirac algebra of the record (WS6/T1 grammar) =====
s0 = sp.eye(2); sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -Im], [Im, 0]]); sz = sp.Matrix([[1, 0], [0, -1]])
Z2 = sp.zeros(2, 2)
blk = lambda a, b, c, d: sp.Matrix(sp.BlockMatrix([[a, b], [c, d]]))
al = [blk(Z2, s, s, Z2) for s in (sx, sy, sz)]
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
th, ph = sp.symbols('theta phi', real=True)
sub = {kx: sp.sin(th)*sp.cos(ph), ky: sp.sin(th)*sp.sin(ph), kz: sp.cos(th)}
def ang(e):
    return sp.integrate(sp.integrate(sp.expand_trig(sp.sympify(e)).subs(sub)*sp.sin(th),
                                     (ph, 0, 2*sp.pi)), (th, 0, sp.pi))
A1 = kx*al[0] + ky*al[1] + kz*al[2]
Ck = sp.expand((sp.eye(4) - A1)/2)          # the sea-band projector C(k) = P_-(khat)
Pp = sp.expand((sp.eye(4) + A1)/2)

# ===== D1 — the stencil identity and the subsidiary tower =====
y = sp.Symbol('y', real=True)
lams = [0, sp.sqrt(2), -sp.sqrt(2)]
ws = [R(1, 2), -R(1, 4), -R(1, 4)]
stencil = sum(w*sp.exp(Im*l**2*y) for w, l in zip(ws, lams))
closed = -Im*sp.exp(Im*y)*sp.sin(y)
ser = sp.series(sp.simplify(stencil), y, 0, 4).removeO().expand()
c1 = ser.coeff(y, 1); c2 = ser.coeff(y, 2); c3 = ser.coeff(y, 3)
m0 = sum(ws); m1 = sum(w*l for w, l in zip(ws, lams))
m2 = sum(w*l**2 for w, l in zip(ws, lams)); m3 = sum(w*l**3 for w, l in zip(ws, lams))
r1d = sp.expand(sum(w*(1 - Im*l**2*y) for w, l in zip(ws, lams)))
ok("D1 THE SEALED STENCIL TOWER: sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y)"
   " EXACT at the record's weights (1/2, -1/4, -1/4 on 0, +sqrt2, -sqrt2); the"
   " expansion -i y + y^2 + (2i/3) y^3 + O(y^4): the degree -1 coefficient -i"
   " (the sealed -i V_2/(2|k|) at y = V_2/(2|k|)), the DEGREE -2 SEGMENT-"
   "CORRECTED COEFFICIENT +1 (s^(2) = V_2^2/4, REAL and khat-FREE), the next"
   " +2i/3; moments m_0 = m_1 = 0, m_2 = -1, m_3 = 0 and the REM R1d F-side"
   " form sum w_l (1 - i l^2 y) = +i y — the F-side +i sign via m_2 = -1",
   sp.simplify(sp.expand_trig(sp.expand(stencil - closed).rewrite(sp.exp))) == 0
   and c1 == -Im and c2 == 1 and sp.simplify(c3 - R(2, 3)*Im) == 0
   and m0 == 0 and m1 == 0 and m2 == -1 and m3 == 0
   and sp.simplify(r1d - Im*y) == 0)

# ===== D2 — the class-A moment kills =====
trax = sp.expand((al[0]*Ck).trace())
sU2, aF2 = sp.symbols('s_U2 a_F2', real=True)      # khat-FREE class-A amplitudes
tc = sp.symbols('t1:4', real=True)
uc = sp.symbols('u1:11', real=True)
odd3 = [kx**3, ky**3, kz**3, kx**2*ky, kx**2*kz, ky**2*kx, ky**2*kz,
        kz**2*kx, kz**2*ky, kx*ky*kz]
T_odd = tc[0]*kx + tc[1]*ky + tc[2]*kz + sum(u*m for u, m in zip(uc, odd3))
muAU = sp.simplify(ang(sU2*trax))                  # s_U^(2)(x) x tr[alpha_x C(k)]
muAF = sp.simplify(ang(aF2*T_odd))                 # a_F^(2)(x) x generic joint-odd trace
ok("D2 CLASS-A MOMENT KILLS: tr[alpha_x C(k)] = -2 khat_x EXACT (A3 of record,"
   " consistency) and int_S2 s_U^(2)(x) tr[alpha_x C(k)] dOmega = 0 EXACT"
   " (mu_AU = 0); a khat-FREE amplitude against the COMPLETE generic joint-odd"
   " trace polynomial (3 deg-1 + all TEN deg-3 odd monomials) integrates to 0"
   " EXACT (mu_AF = 0) — the o = 3 class-A slots DIE at the displayed"
   " khat-free symbols; the kill is the consumed lemma's first-moment kill",
   sp.expand(trax + 2*kx) == 0 and muAU == 0 and muAF == 0)

# ===== D3 — the radial reduction identities and the certificate reductions =====
A_, B_ = sp.symbols('A_ax B_az', real=True)
bvec = [sp.sin(A_)*sp.cos(B_), sp.sin(A_)*sp.sin(B_), sp.cos(A_)]  # generic unit axis
u = sp.Symbol('u', real=True)
red1_ok = True; red2_ok = True
for m in range(7):
    f = (kx*bvec[0] + ky*bvec[1] + kz*bvec[2])**m
    lhs1 = sp.simplify(ang(kx*f))
    rhs1 = sp.simplify(2*sp.pi*bvec[0]*sp.integrate(u*u**m, (u, -1, 1)))
    red1_ok = red1_ok and sp.simplify(lhs1 - rhs1) == 0
    lhs2 = sp.simplify(ang(kx*kx*f))
    rhs2 = sp.simplify(2*sp.pi*sp.integrate(((1 - u**2)/2*1 +
             (3*u**2 - 1)/2*bvec[0]**2)*u**m, (u, -1, 1)))
    red2_ok = red2_ok and sp.simplify(lhs2 - rhs2) == 0
h = sp.symbols('h0:4', real=True)
H = h[0] + h[1]*u + h[2]*u**2 + h[3]*u**3                      # generic H-schema
ub = kx*bvec[0] + ky*bvec[1] + kz*bvec[2]
Hk = lambda e: H.subs(u, e)
D_amp = sp.expand(Hk(ub) - Hk(-ub))                            # beta_+ - beta_- schema
S_amp = sp.expand(Hk(ub) + Hk(-ub))                            # beta_+ + beta_- schema
M1 = sp.integrate(u*H, (u, -1, 1))
muBI_lhs = sp.simplify(4*ang(kx*D_amp))
muBI_rhs = sp.simplify(16*sp.pi*bvec[0]*M1)
muBa_lhs = sp.simplify(4*ang(kx*kx*S_amp))
muBa_rhs = sp.simplify(4*2*sp.pi*sp.integrate((bvec[0]**2*u**2 +
             (1 - u**2)/2*(1 - bvec[0]**2))*(H + H.subs(u, -u)), (u, -1, 1)))
ok("D3 THE RADIAL REDUCTIONS: int_S2 khat_x f(khat.b) dOmega = 2 pi b_x"
   " int_{-1}^1 u f du and int_S2 khat_x^2 f(khat.b) dOmega = 2 pi int [(1-u^2)/2"
   " + (3u^2-1)/2 b_x^2] f du — verified EXACTLY on the monomial basis u^m,"
   " m = 0..6, at a GENERIC symbolic axis; then the certificate reductions on"
   " the generic cubic H-schema: mu_B^I = 4 int khat_x [H(u)-H(-u)] dOmega ="
   " 16 pi b_x M_1 with M_1 = int u H du, and mu_B^alpha = 4 int khat_x^2"
   " [H(u)+H(-u)] dOmega = the displayed bracket integral — EXACT identities",
   red1_ok and red2_ok and sp.simplify(muBI_lhs - muBI_rhs) == 0
   and sp.simplify(muBa_lhs - muBa_rhs) == 0)

# ===== D4 — b_D monotonicity, the witness, the outward ray, the sign exhibit =====
t_, w_ = sp.symbols('t w', positive=True)          # w = r^2
sm = t_**2 - w_; sp_ = (1 - t_)**2 - w_
s_prod = sm*sp_
dsdw = sp.expand(sp.diff(s_prod, w_) + (sm + sp_))
tt, rr = R(3, 10), R(1, 20)
smv = tt**2 - rr**2; spv = (1 - tt)**2 - rr**2; sv = smv*spv
rho_, tpos = sp.symbols('rho t_pos', positive=True)
outward = sp.Min(0, 1 - 2*tpos)   # = min(t,1-t) - t ; nonpositive, so rho + t > min(t,1-t) for rho > 0
hpos = sp.Symbol('h_pos', positive=True)
mono_exhibit = sp.integrate(u*hpos*(1 - u), (u, -1, 1))
ok("D4 THE b_D MONOTONICITY AND THE WITNESS: ds/d(r^2) = -(s_- + s_+) EXACT"
   " polynomial identity (s = (t^2 - r^2)((1-t)^2 - r^2)) — STRICTLY NEGATIVE"
   " on the diamond where both s_-, s_+ > 0: b_D = e^{16 - 1/s} strictly"
   " decreasing in the radius there; the exact-rational witness (t, r) ="
   " (3/10, 1/20): s_- = 7/80, s_+ = 39/80, s = 273/6400 (the MO3 §3.5 ray"
   " witness re-pinned, no float); the outward ray: min(t, 1-t) - t <= 0"
   " symbolically, so radius rho + t > min(t, 1-t) for rho > 0 — H(rho, +1) ="
   " 0 EXACT; the monotone-schema sign exhibit: int_{-1}^1 u . h(1-u) du ="
   " -2h/3 < 0 at h > 0 — the M_1 < 0 mechanism displayed exact",
   dsdw == 0 and smv == R(7, 80) and spv == R(39, 80) and sv == R(273, 6400)
   and outward.is_nonpositive and sp.simplify(sp.Max(outward, 0)) == 0
   and sp.simplify(mono_exhibit + R(2, 3)*hpos) == 0)

# ===== D5 — the COR-A completion arithmetic and the branch =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
I4 = sp.integrate(kk**(2 - 4), (kk, 1, Lam))
M1s = sp.Symbol('M_1', negative=True)              # M_1 < 0 strict (D4 mechanism)
c_alpha = sp.Symbol('c_alpha', positive=True)      # the sealed MO3 positivity display
bx = sp.Symbol('b_x', real=True)                   # x_hat_x
L_total = 0 + 0 + 16*sp.pi*bx*M1s + 8*sp.pi*c_alpha
L_plane = L_total.subs(bx, 0)
ok("D5 THE COR-A COMPLETION: L_deg3 = mu_AU + mu_AF + mu_B^I + mu_B^alpha ="
   " 0 + 0 + 16 pi x_hat_x M_1 + 8 pi c_alpha; ON THE PLANE LOCUS x_hat_x = 0:"
   " L_deg3 = 8 pi c_alpha > 0 STRICTLY (c_alpha the sealed strictly-positive"
   " MO3 §3.5 display, symbolic positive — nothing numeric): the branch"
   " condition L = 0 FAILS on a displayed open locus; the branch integrals:"
   " int_1^Lam k^2 k^-3 dk = log Lambda (divergent formal — the GENUINE log)"
   " vs int_1^Lam k^2 k^-4 dk = 1 - 1/Lambda -> 1 (the road not taken)",
   sp.simplify(L_plane - 8*sp.pi*c_alpha) == 0 and sp.ask(sp.Q.positive(L_plane))
   and sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo
   and sp.simplify(I4 - (1 - 1/Lam)) == 0 and sp.limit(I4, Lam, sp.oo) == 1)

# ===== D6 — placement/phase and sandwich consistency =====
bp, bm, aD = sp.symbols('beta_p beta_m a_Delta', real=True)
jet = sp.expand(kx*((bp - bm)*sp.eye(4) + (bp + bm)*A1))
a0 = sp.expand(Im*aD*kx*(bp*Pp - bm*Ck))
sandwich_diff = sp.expand(Ck*a0*Ck - (-Im*aD*kx*bm*Ck)).subs(sub)  # on |khat| = 1
sandwich_ok = all(sp.simplify(sp.expand_trig(e)) == 0 for e in sandwich_diff)
surv_moment = sp.simplify(ang(kx*Hk(-ub)))          # int khat_x beta_-(schema) dOmega
muBI_schema = sp.simplify(4*ang(kx*(Hk(ub) - Hk(-ub))))
phase = sp.simplify((-Im)*(Im))
ok("D6 PLACEMENT/PHASE AND SANDWICH CONSISTENCY: the jet traced slots re-pinned"
   " (tr[jet] = 4 khat_x (b+ - b-), tr[alpha_x jet] = 4 khat_x^2 (b+ + b-));"
   " the sea sandwich P_- a_0 P_- = -i a_Delta khat_x beta_- P_- as an EXACT"
   " 4x4 identity at a_0 = i a_Delta khat_x [b+ P_+ - b- P_-] (MO3 §4.1/B2"
   " re-derived, on |khat| = 1); the survivor's first moment int khat_x beta_- dOmega ="
   " -(1/8) mu_B^I on the H-schema (inside the L-3 robustness family); and the"
   " placement pair's phase (-i)(+i) = 1 — the adjoint placements compose REAL:"
   " no hidden i-kill at the displayed grade",
   sp.expand((jet).trace() - 4*kx*(bp - bm)) == 0
   and sp.expand((al[0]*jet).trace() - 4*kx**2*(bp + bm)) == 0
   and sandwich_ok
   and sp.simplify(surv_moment + R(1, 8)*muBI_schema) == 0
   and phase == 1)

print("T8SR-BATTERY-DONE")
```

Output, verbatim (6/6 PASS, single recorded run):

```text
D1 THE SEALED STENCIL TOWER: sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y) EXACT at the record's weights (1/2, -1/4, -1/4 on 0, +sqrt2, -sqrt2); the expansion -i y + y^2 + (2i/3) y^3 + O(y^4): the degree -1 coefficient -i (the sealed -i V_2/(2|k|) at y = V_2/(2|k|)), the DEGREE -2 SEGMENT-CORRECTED COEFFICIENT +1 (s^(2) = V_2^2/4, REAL and khat-FREE), the next +2i/3; moments m_0 = m_1 = 0, m_2 = -1, m_3 = 0 and the REM R1d F-side form sum w_l (1 - i l^2 y) = +i y — the F-side +i sign via m_2 = -1: PASS
D2 CLASS-A MOMENT KILLS: tr[alpha_x C(k)] = -2 khat_x EXACT (A3 of record, consistency) and int_S2 s_U^(2)(x) tr[alpha_x C(k)] dOmega = 0 EXACT (mu_AU = 0); a khat-FREE amplitude against the COMPLETE generic joint-odd trace polynomial (3 deg-1 + all TEN deg-3 odd monomials) integrates to 0 EXACT (mu_AF = 0) — the o = 3 class-A slots DIE at the displayed khat-free symbols; the kill is the consumed lemma's first-moment kill: PASS
D3 THE RADIAL REDUCTIONS: int_S2 khat_x f(khat.b) dOmega = 2 pi b_x int_{-1}^1 u f du and int_S2 khat_x^2 f(khat.b) dOmega = 2 pi int [(1-u^2)/2 + (3u^2-1)/2 b_x^2] f du — verified EXACTLY on the monomial basis u^m, m = 0..6, at a GENERIC symbolic axis; then the certificate reductions on the generic cubic H-schema: mu_B^I = 4 int khat_x [H(u)-H(-u)] dOmega = 16 pi b_x M_1 with M_1 = int u H du, and mu_B^alpha = 4 int khat_x^2 [H(u)+H(-u)] dOmega = the displayed bracket integral — EXACT identities: PASS
D4 THE b_D MONOTONICITY AND THE WITNESS: ds/d(r^2) = -(s_- + s_+) EXACT polynomial identity (s = (t^2 - r^2)((1-t)^2 - r^2)) — STRICTLY NEGATIVE on the diamond where both s_-, s_+ > 0: b_D = e^{16 - 1/s} strictly decreasing in the radius there; the exact-rational witness (t, r) = (3/10, 1/20): s_- = 7/80, s_+ = 39/80, s = 273/6400 (the MO3 §3.5 ray witness re-pinned, no float); the outward ray: min(t, 1-t) - t <= 0 symbolically, so radius rho + t > min(t, 1-t) for rho > 0 — H(rho, +1) = 0 EXACT; the monotone-schema sign exhibit: int_{-1}^1 u . h(1-u) du = -2h/3 < 0 at h > 0 — the M_1 < 0 mechanism displayed exact: PASS
D5 THE COR-A COMPLETION: L_deg3 = mu_AU + mu_AF + mu_B^I + mu_B^alpha = 0 + 0 + 16 pi x_hat_x M_1 + 8 pi c_alpha; ON THE PLANE LOCUS x_hat_x = 0: L_deg3 = 8 pi c_alpha > 0 STRICTLY (c_alpha the sealed strictly-positive MO3 §3.5 display, symbolic positive — nothing numeric): the branch condition L = 0 FAILS on a displayed open locus; the branch integrals: int_1^Lam k^2 k^-3 dk = log Lambda (divergent formal — the GENUINE log) vs int_1^Lam k^2 k^-4 dk = 1 - 1/Lambda -> 1 (the road not taken): PASS
D6 PLACEMENT/PHASE AND SANDWICH CONSISTENCY: the jet traced slots re-pinned (tr[jet] = 4 khat_x (b+ - b-), tr[alpha_x jet] = 4 khat_x^2 (b+ + b-)); the sea sandwich P_- a_0 P_- = -i a_Delta khat_x beta_- P_- as an EXACT 4x4 identity at a_0 = i a_Delta khat_x [b+ P_+ - b- P_-] (MO3 §4.1/B2 re-derived, on |khat| = 1); the survivor's first moment int khat_x beta_- dOmega = -(1/8) mu_B^I on the H-schema (inside the L-3 robustness family); and the placement pair's phase (-i)(+i) = 1 — the adjoint placements compose REAL: no hidden i-kill at the displayed grade: PASS
T8SR-BATTERY-DONE
```

---
## 9. CHOICE LEDGER (commission T8SR — DISPLAY-BUILD)

```text
C-1 VERDICT CLASS [YOURS]: COMPLETE-LOG-GENUINE over PARTIAL and over
    COMPLETE-RATE-CERTIFIED. Ground: both commissioned displays land
    on the located stock (no blocker survived the citation-chain
    reads), and the completed sum is NONVANISHING on a displayed open
    locus with a strictly-positive sealed witness (MO3 §3.5 consumed
    at bytes) — the L = 0 branch is refuted by display, not by
    estimate; claiming PARTIAL would misreport two landed displays.
C-2 STOCK LOCATION DISCIPLINE [YOURS, displayed sw-2]: the stock was
    located ONLY through the census pair's own citation chain
    (census §5 -> T4SR a-6 -> WSR §1 prefixes; census §5 -> MO4 §1
    G-5 full digest), every hop seal-verified BEFORE reading, digest
    prefixes/full digests matched against the quoting artifact's own
    pins. MO3 is one disclosed hop past the named MO4 stock — it is
    where MO4's own §1 says the jet's closed coefficients live; it is
    ALSO independently pinned in WSR §1 (my named eight). No other
    file was opened.
C-3 THE KHAT-FREE TYPING OF CLASS A [YOURS, graded]: DERIVED from
    three sealed displays jointly (the floors' assignment of ALL
    interband/transfer oscillation to c >= 1 blocks; the two-level
    channel displayed as an (x, omega) family; the m_0 kill of the
    free-transport stratum carrying the ray-average khat-dependence)
    at the record's KF1/KF2 pointwise-symbol license — the SAME grade
    the sealed V_2 (x) V_2 kill rides. The exact tower coefficient
    V_2^2/4 is displayed from the sealed stencil identity; the
    averaged-system remainders are typed, not valued.
C-4 THE COMPLETION'S AGGREGATION GRADE [YOURS, disclosed]: L_deg3 is
    evaluated at COR-A's displayed unit-weight aggregation (census
    s-1, audit CA6) — the branch verdict is displayed INVARIANT under
    every nonzero weighting of the certified class-B moments (L-3),
    so the undisplayed weight fine-structure cannot un-fire the
    branch; only an EMPTY class-B entry could, contradicting the
    audited enumeration.
C-5 M_1 LEFT AS AN EXACT FUNCTIONAL [YOURS]: no closed elementary
    form for M_1(rho) is claimed (b_D is the sealed essentially-
    non-elementary profile); the certificate displays sign, support,
    and vanishing locus exactly — which is ALL the census's moment
    slots consume. Displaying a pseudo-closed form would be
    decoration; numerics are fenced.
C-6 CAS CORRECTION DISCLOSURE [YOURS]: the three pre-run script
    corrections are disclosed in §8 verbatim rather than silently
    absorbed — the record's single-run convention is reported
    truthfully as a single RECORDED run with the correction history
    named.
C-7 HOMONYM SPLIT [inherited, disclosed in header]: n carrier index;
    k momentum; (o, c) the REM strata indices as quoted; s in beta_s
    is the RAY SIGN (MO3 bytes), distinct from the REM stay-count c
    and from the arclength/time variables.
```

---

## 10. TOY_SEPARATION

```text
T-1 D2/D6 use the EXACT 4x4 Dirac algebra of the record (not a toy:
    the sealed spinor structure itself), re-instantiating of-record
    displays (A3's -2 khat_x; the T6/R6 jet traces; MO3 B2's sandwich
    on |khat| = 1) for consistency; no matrix-grade result is carried
    to the carrier quantifier.
T-2 D3's H-schema (generic cubic in u) is an identity-schema stand-in
    for the sealed ray averages' u-dependence: the reduction
    identities are proved on the monomial basis at a generic axis and
    consumed at schema grade; the certificate's sign/support content
    rides the EXACT monotonicity mechanism (D4) and the sealed
    witness bytes, not the schema.
T-3 D1 is the sealed stencil identity itself at the record's exact
    weights (rationals and sqrt2 surds); D5 is exact formal-Lambda
    carrier arithmetic at the KF1 license; Lambda never valued;
    c_alpha and M_1 stay symbols with only their SEALED/DERIVED sign
    assumptions.
T-4 No testbed evaluation appears; nothing witness-family-grade is
    promoted; no spectral datum of the actual family is valued; no
    stratum symbol is authored beyond the displayed tower and the
    typed remainders; no float exists anywhere in this artifact.
```

---
## 11. FLAG BLOCK

```text
DEG3_DISPLAY_PAIR_VERDICT = COMPLETE-LOG-GENUINE( both commissioned
  displays DERIVED on the citation-located, seal-verified stock, and
  the census completed mechanically: (i) the o = 3 (c = 0)
  segment-corrected angular symbols are ANGULAR-SCALAR — khat-free at
  the record's own two-level displays (the floors assign every
  interband/transfer oscillation to c >= 1; the c = 0 channel is the
  displayed (x, omega) family; the free-transport ray-average stratum
  is m_0-killed identically), with the exact stencil tower
  sum_l w_l e^{i lambda^2 y} = -i e^{iy} sin(y), y = V_2(x)/(2|k|),
  V_2(x) = (8 pi^2/7)(1 - 128|x|^7), whose degree -2 coefficient is
  +V_2(x)^2/4 — parity EVEN both placements, so mu_AU = 0 and
  mu_AF = 0 EXACTLY on the consumed all-lengths lemma (D1/D2);
  (ii) the beta_s first-angular-moment certificate, per cell point x:
  int khat_x [beta_+ - beta_-] dOmega = 4 pi x_hat_x M_1(|x|) with
  M_1 < 0 STRICT on 0 < |x| < 1 (b_D strictly radius-decreasing on
  the diamond: ds/d(r^2) = -(s_- + s_+); outward ray vanishing;
  witness ray positive — D3/D4), vanishing exactly on {x_hat_x = 0}
  u {x = 0} u {|x| >= 1}; int khat_x^2 [beta_+ + beta_-] dOmega > 0
  STRICT on the whole open shell 0 < |x| < 1 — the sealed MO3 §3.5
  strict-positivity display consumed at bytes; both moments = 0
  exactly outside the shell (support displayed). COMPLETION: L_deg3(x)
  = 0 + 0 + 16 pi x_hat_x M_1(|x|) + mu_B^alpha(x), NOT identically
  zero — STRICTLY POSITIVE on the displayed plane locus x_hat_x = 0,
  0 < |x| < 1 (exact-rational witness bytes; D5) and robust under
  EVERY nonzero weighting of the certified class-B moments (L-3).
  THE ARMED BRANCH FIRED: L_deg3 != 0 ==> THE LOG IS GENUINE at the
  degree-3 class total — the carrier integral log Lambda survives
  with displayed nonzero coefficient; the commissioned log-free rate
  is REFUTED; the count does NOT land at degree -4; the
  RATE-CERTIFIED branch's condition fails on a displayed open locus.
  The certification chain is displayed whole (§5.3). )

DELIVERABLE_I = LANDED( the class-A segment-corrected angular symbols
  displayed khat-FREE with the exact closed tower coefficient
  V_2^2/4 and typed (x, omega)-class remainders; AC-4's unclaimed
  degree-3 parity now claimed and derived at the KF1/KF2 license:
  EVEN; mu_AU = mu_AF = 0 exact. )

DELIVERABLE_II = LANDED( the two commissioned moment integrals
  displayed exactly per cell point with sign, support, and vanishing
  locus certified — the odd slot 4 pi x_hat_x M_1(|x|), M_1 < 0
  strict; the even slot strictly positive on the open shell (sealed
  MO3 §3.5 display consumed); nothing numeric evaluated. )

PER_COMPOSITE_WALL = UNTOUCHED( summed quantifier only; no
  per-composite or per-pair quantity quoted, moved, or decided; V-2
  not crossed in either direction; the MO3 jet consumed as the sealed
  c-class coefficient display only (the audited T4SR/census typing);
  T_n, o-2-OBS, B-L2*, alpha' = 1/4, MO-2, MO-3's p = -3, MO-4's
  PARTIAL and MO-4-R, the WBC enclosure, and every sealed identity at
  their own quantifiers; no flag of the record flipped; no witness
  action — witness status is the registrar's. )

SEALS_VERIFIED = 11/11( the commission's named eight
  (STAGE8_DEG3_ANGULAR_CENSUS_T6SR_V001 + AUDIT;
  STAGE8_WALL_LOGFREE_RATE_T4SR_V001 + AUDIT;
  STAGE8_WALL_SUMMED_REQUANT_S9AD_V001 + AUDIT;
  STAGE8_MO4_CANCELLATION_S9AD_V001 + AUDIT) + the three
  citation-located stock artifacts (STAGE8_REMAINDER_UNIFORMITY_V001
  = REM, 2e4e5163 prefix MATCH; STAGE8_BL2STAR_ATTEMPT_V001 =
  BL2STAR, 6e81ae92 prefix MATCH; STAGE8_R2_RESUMMATION_V001 = R2,
  a7969f0c prefix MATCH) + the disclosed one-hop jet home
  (STAGE8_MO3_P_EXPONENT_S9AD_V001, 9fdc3d1c, MATCHING MO4 §1's and
  WSR §1's own full-digest/prefix pins) — every digest recomputed
  from bytes at path this session by full digest via shasum -a 256 -c
  against sidecars BEFORE consumption; all OK; none unverifiable;
  scoped reads of these twelve files and NO others. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant
  symbolic or an exact rational/surd; NOTHING numeric evaluated
  (Lambda, V_2, beta_s, a_Delta, c_alpha, M_1 all formal/symbolic);
  no float ground; no measured constant; no measured-constant
  comparison; no value frozen; alpha never computed or approached;
  kappa_record nowhere; no per-pair route traversed; no fenced-class
  or out-of-scope file opened (filename matches only); "Q-..." tokens
  noted, never chased; no flag flipped; no witness action; no git;
  ONE output + sidecar at the commission-distinct path, probed ABSENT
  at session start and re-probed ABSENT immediately before write. )

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv t8srvenv
  under the session scratchpad, ONE battery, 6 checks, 6/6 PASS on
  the single recorded run (three pre-run mechanical script
  corrections disclosed in §8), script and output reproduced verbatim
  in §8 by file-append; shasum/file reads/filename+token sweeps
  otherwise; NOTHING numeric evaluated.

SWEEP_CUTOFF = 2026-08-15 05:05:39 -0500 (workspace root + cleanroom
  root; fenced classes and all non-named files filename-matched only,
  never opened; token greps inside the named files only; artifacts
  sealed after the cutoff UNSWEPT).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked; witness/flag actions are the
registrar's.
T8SR_DEG3_DISPLAY_PAIR_RESULT = SEALED.
```
