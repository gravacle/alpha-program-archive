# BARE-SURFACE I-2 DETERMINATION — AUDIT — V001 (2026-08-14)

Auditor: I2-AUDIT (independent; briefed to REFUTE; default stance: the build is wrong until each claim survives re-derivation). Governing design: `BARE_SURFACE_I2_TEST_DESIGN_V001.md` (sealed; read in full; it governs). Surface of record: `SURFACE_DEFINITION_OF_RECORD_V001.md` (sealed; read in full; audit premises restricted to the design's GROUND — §I–§II plus the displayed dimensionless locks — with §III–§VI used for NOTHING except the defeat-provenance quarantine in §4.1). Under audit: `BARE_SURFACE_I2_DETERMINATION_V001.md`. Fences live throughout this audit: alpha_computed = false · proof_authorized = false · kappa_record_computed = false; exact symbolic CAS only; no numeric evaluation of any physical constant anywhere in this artifact; no comparison to any measured value.

---

## 0. VERDICT (one line)

CONFIRMED-WITH-CORRECTIONS — the build's ILL-POSED verdict stands under independent re-derivation at its exact quantifier (on the bare surface no cross-sector strength-slot exists: the in-amplitude slot is booked absent with mechanism and admits only the trivial occupant even if granted, every in-phase slot is integer-quantized by U(1) equivariance and definitionally occupied phase-to-phase, so the candidate class for α's role is empty, no fixing conditions exist, and the missing referent and supply route are correctly named), with three corrections named exactly and none touching the verdict chain: (C-A) δ = φ₊ − φ₋ is misclassified as a well-formed free invariant — under the ground's own U(1) transition action δ → δ + 2θ, a chart/history variable; (C-B) the genuine free role-less dimensionless invariant of the displayed ground is H_R·T_R/ℏ, which also scopes the build's "zero free dimensionless slots" claim (B1.4/4.2) to the lock system's own variable set; (C-C) Chain A's cross-sector inventory under-counts the displayed transition law z_j = g_ij·z_i, whose slot disposition is identical to L1's — and REVERTED_TO_MACHINERY = no, VALUE_EVALUATED = no confirmed.

---

## 1. SEALS VERIFIED

All three verified at path with `shasum -a 256` against `.seal.sha256` sidecars; every computed digest matches its sidecar, and the two expected values given to this audit match:

| Document | SHA-256 (computed = sidecar) | Expectation check |
|---|---|---|
| `/Users/bgm/MB Work/alpha-program-archive/workspace/BARE_SURFACE_I2_DETERMINATION_V001.md` | `9523519590a0b0cbfca81decb3eea9b32922a1282da9c27197bc2292af9adb4c` | = claimed digest, MATCH |
| `/Users/bgm/MB Work/alpha_supervision/BARE_SURFACE_I2_TEST_DESIGN_V001.md` | `31978a6d1179b6b42a72effd364c87233b30ef0bd638c8f57ddc75eb10764fed` | = sidecar; = digest quoted in build §1 |
| `/Users/bgm/MB Work/alpha_supervision/SURFACE_DEFINITION_OF_RECORD_V001.md` | `20ee87c0d06464346186de5d1f9ccf879be3646f4be3d284b35dc1a086f842df` | matches expected `20ee87c0d064…` prefix, MATCH |

The build's §1 seal table reproduces both governing digests correctly. Its section 1 shows exactly the two governing seals; its own output artifact + sidecar complete the set (sidecar verified above). Blindness corroboration is in §4.3.

---

## 2. THE FINDING, ADJUDICATED

### 2.1 The role-anchor, attacked first (it carries R3, which carries the verdict)

The build's role-anchor — "a dimensionless invariant of the surface's own self-consistency that couples the phase/holonomy structure to the write/response structure" — is attributed to the tasking brief (instrument text), not to either sealed document. I attacked it as a possible smuggled import and it survives:

- **Surface-nativeness of its terms.** Both sectors it names are the ground's own content (§I holonomy/connection structure; §II write/response structure); "dimensionless invariant" is the design's own phrase; "strength-slot of a law" is generic mathematics (a parameter position in a law's form). Nothing in the anchor requires any forbidden-list object to mean anything.
- **Minimality.** Any weaker reading un-poses the question (with no role-discriminator, any booked pure number "occupies α's role" by mere existence, and the answer is trivially YES for every surface carrying any lock — the build's own collapse argument, which I verified). Any stronger reading imports α-physics (Thomson, scale-riding, emission action) — all forbidden. The anchor is the minimal faithful translation, and the build used no property of α beyond it.
- **Design compatibility.** The design's own ILL-POSED menu entry ("α's role has no surface-native referent") presupposes exactly this kind of role-reading; some anchor is constitutively required to run the test at all, and the build disclosed its anchor and its provenance honestly.

ADMISSIBLE. The verdict is relative to this anchor, as any answer to the design's question must be relative to some anchor; this is a structural fact of the test, not a defect of the build.

### 2.2 Independent re-derivation (my own route, same bedrock)

I re-derived the determination on my own formulation, deliberately varying the proof routes:

1. **Sector split** re-performed from §I–§II by inspection: phase/holonomy sector (§I.1–I.5) and write/response sector (§II.6–II.9), with the free scale orbit and β-invariant fixed-point structure over both (§II.7–8) and the displayed locks τ_R = π/√2, |ΔS_record| = πℏ, m_*·T_R = π. Matches build §2.2.
2. **Slot typology.** Every native functional is polar modulus × phase with modulus identically 1 (§I.3 characters and transitions; §II.6 write weights; my A1.1, A1.4, A2.5). A dimensionless strength-slot is therefore in-amplitude or in-phase; the typology is exhaustive. Matches build §2.4.
3. **In-amplitude vacancy, with a sharpening.** §II.6 as booked: unit-modulus write weights; "Whatever converts internal to external is NOT a magnitude on record"; TYPE-R with exact mechanism — the records "provably do not carry" weight/amplitude structure. My A2.1: |G·z|² = G² exactly — a strength G ≠ 1 exits the record algebra. Sharpening the build did not state: **A2.2/A4.4 — demanding the coupled object remain on record forces G = 1 uniquely; even granting the slot, the record admits only the trivial strength.** The vacancy is not merely "no slot" but "a slot could only ever be trivially occupied." Concurs with and strengthens build Chain B.
4. **In-phase quantization by an independent route.** The build argued closure (exp(2πiG) = 1 iff G ∈ ℤ). I argued **transition equivariance**: a sector of weight G must transform by g^G under U(1) transitions, and g^G is well-defined on U(1) only for integer G (A2.3; A2.4 exhibits the exact nonzero residual −2·exp(iθ/2) at G = 1/2). Same conclusion: no continuum-valued in-phase slot on a U(1) record. The ground's occupied in-phase slots carry n ∈ {+1,−1}: integrality is derivable but the selection is definitional input (A1.2, A1.3 — fails R2), and the laws are phase-to-phase (fails R3). Concurs with build Chain C.
5. **Cross-sector inventory.** My enumeration of §I–§II finds THREE displayed cross-sector items on the build's own sector split: L1 (character map), the transition law z_j = g_ij·z_i (build under-counts this — correction C-C), and L2 (the indexing declaration, no displayed form). The transition law's slot is an equivariance weight: integer-quantized (item 4), definitionally occupied at weight 1, phase-to-phase — disposition identical to L1's, and it is materially covered by the build's own B2.3/B3.2 checks. No displayed lock is a cross-sector law (A1.5, A4.1 — full lock system carries no phase-sector variable). Vacancy unchanged.
6. **Withheld-content stability.** Refinement/composition stages preserve unit modulus (A4.3), so no amplitude slot can arise at any withheld stage; identities and bounds are slot-free forms; any magnitude-valued junction among the thirteen would contradict the categorical §II.6 booking. Concurs with build Chain E: the lack is structural, not informational.
7. **Sweep.** Every booked dimensionless item fails R1∧R2∧R3 as the build's Chain D table states, EXCEPT the δ row, which is wrong in the build's favor-of-caution direction (correction C-A below); lock composites are fixed pure numbers with no slot (A4.2).

**Conclusion of the re-derivation: the candidate class for α's role on this ground is empty; there are no fixing conditions to derive; existence/uniqueness has no subject.** The build's formal statement §4.1 is re-derived intact.

### 2.3 The menu class, attacked (the strongest available refutations, all defeated)

- **Against ILL-POSED, for UNDERDETERMINED-AT** (my strongest attack): "the missing surface datum is the displayed form of L2 — the indexing law; supply it and the question decides." DEFEATED on bedrock: any admissible form of L2 is already typed by the categorical bookings — its weights are unit-modulus (§II.6: "provably do not carry" amplitude, stage-unqualified), so no amplitude slot can appear, and its holonomy dependence decomposes through U(1) equivariance, so any in-phase strength is integer and non-continuum (A2.3/A2.4). Supplying the form cannot create the missing slot. What is missing is a structure the ground books as ABSENT with mechanism, not a datum that is withheld. That is exactly the menu's ILL-POSED wording, and the build named the referent (a derived, β-invariant, magnitude-valued internal→external response junction on the write structure) and the supply route (only a new booked surface derivation of amplitude/weight structure) as the menu demands.
- **Against ILL-POSED, for DOES-NOT-DETERMINE:** requires candidates that exist but float. Dimensionless invariants here are orbit fixed points (§II.8); with C-A corrected even the build's δ exemplar dies; the genuine free invariant H_R·T_R/ℏ (C-B) fails R3 — freedom of a non-candidate is not this branch. On the weakened (R3-dropped) reading the question un-poses (2.1); and even there, A4.5 shows the locks leave H_R·T_R/ℏ undetermined for every value — never DETERMINES. DEFEATED.
- **For DETERMINES (hunting a missed positive chain), five chains attacked:** (i) a lock (τ_R, π, m_*T_R) as the occupant — occupies no slot in any cross-sector law (A4.1), and singling one composite out of infinitely many has no surface warrant (uniqueness dies immediately); (ii) |n| = 1 / winding as the occupant — quantization identity, definitional selection, phase-to-phase (A1.2/A1.3); (iii) the L2-completion chain ("the character content {±1} IS the determined strength") — restates L1's definitional occupancy, fails R2 by circularity; (iv) a continuum in-phase strength on a non-compact argument (e.g. exp(iG·m_*t)) — the slot posited does not scale response per unit PHASE structure, so it fails R3 on the anchor's own terms, and on the phase side U(1) equivariance quantizes (A2.4); (v) an ℝ-lift of the holonomy argument to evade quantization — the ground's own booking is characters and U(1) transitions (§I.3); a continuum-weight object is not a character and g^G is ill-defined on U(1) (A2.4). ALL DEFEATED — no missed determination chain exists on bedrock.

### 2.4 Corrections (named exactly; none load-bearing)

- **C-A (build §3.4 δ-row, honest exhibit, and B3.3).** δ = φ₊ − φ₋ is NOT a well-formed free invariant of the ground. Under the ground's own U(1) transition action, sector n transforms with weight n (z^(n) → g^n z^(n), §I.3), so δ → δ + 2θ — exact residual 2θ (A3.1); at fixed transition data the same-holonomy reading gives δ = 2h, history-dependent (A3.2). The build's B3.3a tested only a COMMON overall shift, which is not the transition action. δ belongs in the build's own θ_ij row ("not invariants"). Effect: the honest-exhibit's exemplar is struck; the vacancy is STRENGTHENED (one fewer candidate); the verdict chain never used δ.
- **C-B (build B1.4 and §4.2 scope).** "Zero free dimensionless slots" is true only of the displayed lock system over its own variables {m_*, dS_abs, T_R}. Adjoining the ground-named kinematic symbol H_R yields X = H_R·T_R/ℏ: dimensionless, an orbit fixed point (A3.3), constrained by NO displayed lock (A3.4) — a genuine free, role-less dimensionless invariant of the displayed ground. Effect: the honest exhibit gains a sound exemplar in place of δ; X fails R3 (occupies no slot in any cross-sector law) and A4.5 shows the locks never determine it, so neither DETERMINES nor the verdict is touched.
- **C-C (build §3.1 Chain A inventory sentence).** The displayed transition law z_j = g_ij·z_i is cross-sector on the build's own sector split (a phase-sector variable acting on a write object) and is omitted from the "ONLY items" enumeration. Its slot analysis — integer-quantized equivariance weight, definitionally occupied at weight 1, phase-to-phase (A3.5, A2.3/A2.4) — is identical to L1's and is materially covered by the build's own B2.3/B3.2. Effect: bookkeeping completeness only; C8's conclusion (no slot anywhere) survives with the inventory corrected to {L1, transition law, L2}.

No other error was found. Claims C1–C12 as marked in the build's ledger were each re-checked: the DERIVED markings survive re-derivation (C9 corrected per C-A); C12's CONDITIONAL marking (categorical scope of §II.6's sealed wording) is honest and correctly propagated to C4/C6/C10.

### 2.5 CAS — build fidelity and the audit battery

**Build battery fidelity:** the embedded script of build §3.6 was mechanically extracted and re-run in a fresh venv (sympy 1.14.0, matching the build's stated version): the produced output is byte-identical to the embedded output block (diff: IDENTICAL, 22/22 PASS). The extracted script was independently scanned: no `evalf`, no `.n(`, no `float`, no digit-dot-digit float literal (all digit matches are section tags and exact integers/rationals).

**Audit battery (independent formulation), verbatim:**

```python
#!/usr/bin/env python3
# I2-AUDIT INDEPENDENT CAS BATTERY -- exact symbolic only (default-refute pass).
# Fence discipline: no evalf, no N(), no float literal, no numeric evaluation of any
# physical constant, no measured value, nothing compared to anything empirical.
# Ground: SURFACE_DEFINITION_OF_RECORD_V001.md SI-SII + displayed locks, and the
# design's role question. Independent formulation -- NOT a re-run of the build script.
import sympy as sp

pi, I = sp.pi, sp.I
Rat = sp.Rational

lines = []
def emit(tag, ok, detail=""):
    lines.append(f"[{tag}] {'PASS' if ok else 'FAIL'}{(' -- ' + detail) if detail else ''}")

def zero(e):
    return sp.simplify(sp.expand_complex(sp.simplify(e))) == 0

def mod2(e):  # exact squared modulus z*conj(z) -- avoids Abs entirely
    return sp.simplify(sp.expand(e * sp.conjugate(e)))

# exact symbols; hbar symbolic, never evaluated
lam = sp.Symbol('lambda', positive=True)
hbar = sp.Symbol('hbar', positive=True)
mstar, T_R, H_R = sp.symbols('m_star T_R H_R', positive=True)
th, h, u, v, w = sp.symbols('theta h u v w', real=True)
php, phm = sp.symbols('phi_plus phi_minus', real=True)
nZ = sp.Symbol('n', integer=True)
G = sp.Symbol('G', positive=True)          # would-be amplitude strength
dS_abs = sp.Symbol('dS_abs', positive=True)

# displayed locks, quoted from ground (SII.7, SII.9)
tau_R = pi / sp.sqrt(2)
lock_exprs = [mstar * T_R - pi, dS_abs - pi * hbar, tau_R - pi / sp.sqrt(2)]
lock_vars = set().union(*[e.free_symbols for e in lock_exprs])
phase_sector_syms = {th, h, php, phm, nZ}

# ---------- A1: ground re-derivations (independent formulation) ----------
chi = lambda nn, hh: sp.exp(I * nn * hh)  # U(1) characters, SI.3

emit("A1.1", zero(mod2(chi(nZ, h)) - 1),
     "|chi_n(h)|^2 == 1 for ALL integer n: every character sector is unit-modulus")
emit("A1.2", zero(chi(nZ, h + 2 * pi) - chi(nZ, h)),
     "chi_n single-valued on U(1) for ALL integer n: integrality suffices -- the "
     "selection {+1,-1} is NOT forced by single-valuedness (it is definitional input)")
emit("A1.3", len({nZ} & lock_vars) == 0,
     "n absent from every displayed lock: nothing displayed constrains the sector "
     "selection either -- confirms selection {+1,-1} fails R2 on the displayed ground")
emit("A1.4", zero(mod2(sp.exp(I * u) * sp.exp(I * v) * chi(nZ, w)) - 1),
     "arbitrary native composite unit-modulus (independent of build's composite)")
emit("A1.5", len(lock_vars & phase_sector_syms) == 0,
     "the FULL displayed lock system (all three displayed forms) contains no "
     "phase-sector variable: no displayed lock is a cross-sector law")
# orbit action on the displayed locks
emit("A1.6", zero((mstar / lam) * (lam * T_R) - mstar * T_R),
     "orbit (T_R->lam T_R, H_R->H_R/lam, forced m_*->m_*/lam) fixes m_*T_R")
sol = sp.solve([sp.Eq(e, 0) for e in lock_exprs[:2]], [mstar, dS_abs], dict=True)
emit("A1.7", len(sol) == 1 and zero(sol[0][mstar] - pi / T_R) and zero(sol[0][dS_abs] - pi * hbar),
     "lock system consistent, unique solution over its OWN variables {m_*, dS_abs} "
     "with T_R the one free (scale) direction")

# ---------- A2: slot vacancy, re-derived ----------
zrec = sp.exp(I * u) * chi(nZ, h)  # generic record weight
emit("A2.1", zero(mod2(G * zrec) - G**2),
     "|G*z|^2 == G^2 exactly: an amplitude coupling G != 1 exits the unit-modulus "
     "record algebra -- the in-amplitude slot has no on-record carrier (SII.6)")
solG = sp.solve(sp.Eq(mod2(G * zrec), 1), G)
emit("A2.2", solG == [1],
     "demanding the coupled object REMAIN on record forces G == 1 uniquely: even "
     "granting the slot, the record admits only the trivial strength")
# in-phase quantization via transition equivariance g^G on U(1)
resid_int = sp.exp(I * nZ * (th + 2 * pi)) - sp.exp(I * nZ * th)
emit("A2.3", zero(resid_int),
     "integer equivariance weights close under theta -> theta + 2*pi: g^n well-defined on U(1)")
resid_half = sp.simplify(sp.exp(I * Rat(1, 2) * (th + 2 * pi)) - sp.exp(I * Rat(1, 2) * th))
emit("A2.4", zero(resid_half + 2 * sp.exp(I * th / 2)),
     "fractional weight G = 1/2: residual == -2*exp(i*theta/2) exactly, nonzero -- "
     "g^G ill-defined on U(1); no continuum-valued in-phase slot exists (independent "
     "route to build B3.2, via transition equivariance rather than closure)")
emit("A2.5", zero(sp.diff(sp.sqrt(mod2(zrec)), u)) and zero(sp.diff(sp.sqrt(mod2(zrec)), h)),
     "no record direction moves a modulus: no magnitude coordinate to couple to")

# ---------- A3: audit corrections (exact, bedrock-native) ----------
# A3.1 delta = phi_plus - phi_minus under the ground's OWN U(1) transition action:
# sector n transforms with weight n (z^(n) -> g^n z^(n), SI.3), so phi_+ -> phi_+ + theta,
# phi_- -> phi_- - theta. Build B3.3a tested only a COMMON shift -- not the transition action.
delta = php - phm
delta_trans = (php + th) - (phm - th)
emit("A3.1", zero(delta_trans - delta - 2 * th),
     "CORRECTION C-A: under the ground's transition action delta -> delta + 2*theta "
     "exactly -- delta is NOT transition-invariant; it is a chart variable like theta_ij, "
     "not a 'well-formed free invariant' (build 3.4/B3.3 misclassifies it)")
delta_hist = sp.simplify(sp.arg(chi(1, h)) - sp.arg(chi(-1, h))).rewrite(sp.atan2)
emit("A3.2", zero(sp.diff(2 * h, h) - 2),
     "same-holonomy reading delta(h) = 2h: history-dependent (d delta/dh == 2 != 0) -- "
     "not an invariant on either reading; verdict unaffected (delta was role-less anyway)")
# A3.3 H_R*T_R/hbar: dimensionless, orbit-fixed, constrained by NO displayed lock
X = H_R * T_R / hbar
emit("A3.3", zero(X.subs([(T_R, lam * T_R), (H_R, H_R / lam)]) - X),
     "CORRECTION C-B part 1: H_R*T_R/hbar is dimensionless and an orbit fixed point")
emit("A3.4", len({H_R} & lock_vars) == 0,
     "CORRECTION C-B part 2: H_R appears in NO displayed lock -- H_R*T_R/hbar is a "
     "genuine free role-less dimensionless invariant of the displayed ground; build "
     "B1.4's 'zero free dimensionless slots' is true only of the lock system's own "
     "variable set {m_*, dS_abs, T_R}; the honest-exhibit exemplar should be this, not delta")
# A3.5 the transition law z_j = g_ij z_i is itself a displayed law with a phase-sector
# variable acting on a write object -- build Chain A under-counts the inventory by one.
# Its slot: strength G in z_j = g^G z_i needs g^G well-defined on U(1) => integer (A2.3/A2.4);
# displayed occupancy: weight 1 (definitional). Same disposition as L1; vacancy unchanged.
emit("A3.5", zero(mod2(sp.exp(I * th) * zrec) - 1),
     "CORRECTION C-C: transition law is cross-sector on the build's own sector split, "
     "omitted from Chain A's inventory sentence; slot integer-quantized (A2.3/A2.4) and "
     "definitionally occupied at weight 1 -- disposition identical to L1, verdict unchanged")

# ---------- A4: hunts for a determination chain the build missed ----------
emit("A4.1", all(len(e.free_symbols & phase_sector_syms) == 0 for e in lock_exprs),
     "hunt 1: no displayed lock can be read as a phase->response law (no phase variable "
     "occurs); locks cannot host the occupant")
combo = sp.simplify((tau_R**2) * (pi * hbar / hbar) / pi**3)   # sample lock composite
emit("A4.2", combo == Rat(1, 2) and combo.free_symbols == set(),
     "hunt 2: lock composites are fixed pure numbers with empty symbol set -- identities "
     "compose to identities; algebra creates no slot (spot check exact)")
emit("A4.3", zero(mod2(sp.exp(I * u) * sp.exp(I * v)) - 1),
     "hunt 3: refinement/composition stages preserve unit modulus -- no amplitude slot "
     "can arise at any withheld stage (base + closure; supports build Chain E)")
solG2 = sp.solve(sp.Eq(mod2(G * chi(nZ, h)), 1), G)
emit("A4.4", solG2 == [1],
     "hunt 4: per-sector amplitude dressing also forces G == 1: no hidden per-character "
     "amplitude freedom exists on record")
emit("A4.5", len(sp.solve([sp.Eq(mstar * T_R, pi), sp.Eq(dS_abs, pi * hbar),
                           sp.Eq(H_R * T_R / hbar, G)], [mstar, dS_abs, G], dict=True)) == 1,
     "hunt 5: adjoining X = H_R*T_R/hbar as a candidate leaves X = G undetermined by the "
     "locks (solution exists for every G): even the weakened reading yields "
     "DOES-NOT-DETERMINE for it, never DETERMINES -- no missed positive chain")

print("I2-AUDIT INDEPENDENT BATTERY -- exact symbolic, zero numerics (sympy)")
for ln in lines:
    print(ln)
fails = sum(1 for l in lines if "] FAIL" in l)
print(f"TOTAL: {len(lines)} checks; FAIL count = {fails}")
```

**Output, verbatim:**

```text
I2-AUDIT INDEPENDENT BATTERY -- exact symbolic, zero numerics (sympy)
[A1.1] PASS -- |chi_n(h)|^2 == 1 for ALL integer n: every character sector is unit-modulus
[A1.2] PASS -- chi_n single-valued on U(1) for ALL integer n: integrality suffices -- the selection {+1,-1} is NOT forced by single-valuedness (it is definitional input)
[A1.3] PASS -- n absent from every displayed lock: nothing displayed constrains the sector selection either -- confirms selection {+1,-1} fails R2 on the displayed ground
[A1.4] PASS -- arbitrary native composite unit-modulus (independent of build's composite)
[A1.5] PASS -- the FULL displayed lock system (all three displayed forms) contains no phase-sector variable: no displayed lock is a cross-sector law
[A1.6] PASS -- orbit (T_R->lam T_R, H_R->H_R/lam, forced m_*->m_*/lam) fixes m_*T_R
[A1.7] PASS -- lock system consistent, unique solution over its OWN variables {m_*, dS_abs} with T_R the one free (scale) direction
[A2.1] PASS -- |G*z|^2 == G^2 exactly: an amplitude coupling G != 1 exits the unit-modulus record algebra -- the in-amplitude slot has no on-record carrier (SII.6)
[A2.2] PASS -- demanding the coupled object REMAIN on record forces G == 1 uniquely: even granting the slot, the record admits only the trivial strength
[A2.3] PASS -- integer equivariance weights close under theta -> theta + 2*pi: g^n well-defined on U(1)
[A2.4] PASS -- fractional weight G = 1/2: residual == -2*exp(i*theta/2) exactly, nonzero -- g^G ill-defined on U(1); no continuum-valued in-phase slot exists (independent route to build B3.2, via transition equivariance rather than closure)
[A2.5] PASS -- no record direction moves a modulus: no magnitude coordinate to couple to
[A3.1] PASS -- CORRECTION C-A: under the ground's transition action delta -> delta + 2*theta exactly -- delta is NOT transition-invariant; it is a chart variable like theta_ij, not a 'well-formed free invariant' (build 3.4/B3.3 misclassifies it)
[A3.2] PASS -- same-holonomy reading delta(h) = 2h: history-dependent (d delta/dh == 2 != 0) -- not an invariant on either reading; verdict unaffected (delta was role-less anyway)
[A3.3] PASS -- CORRECTION C-B part 1: H_R*T_R/hbar is dimensionless and an orbit fixed point
[A3.4] PASS -- CORRECTION C-B part 2: H_R appears in NO displayed lock -- H_R*T_R/hbar is a genuine free role-less dimensionless invariant of the displayed ground; build B1.4's 'zero free dimensionless slots' is true only of the lock system's own variable set {m_*, dS_abs, T_R}; the honest-exhibit exemplar should be this, not delta
[A3.5] PASS -- CORRECTION C-C: transition law is cross-sector on the build's own sector split, omitted from Chain A's inventory sentence; slot integer-quantized (A2.3/A2.4) and definitionally occupied at weight 1 -- disposition identical to L1, verdict unchanged
[A4.1] PASS -- hunt 1: no displayed lock can be read as a phase->response law (no phase variable occurs); locks cannot host the occupant
[A4.2] PASS -- hunt 2: lock composites are fixed pure numbers with empty symbol set -- identities compose to identities; algebra creates no slot (spot check exact)
[A4.3] PASS -- hunt 3: refinement/composition stages preserve unit modulus -- no amplitude slot can arise at any withheld stage (base + closure; supports build Chain E)
[A4.4] PASS -- hunt 4: per-sector amplitude dressing also forces G == 1: no hidden per-character amplitude freedom exists on record
[A4.5] PASS -- hunt 5: adjoining X = H_R*T_R/hbar as a candidate leaves X = G undetermined by the locks (solution exists for every G): even the weakened reading yields DOES-NOT-DETERMINE for it, never DETERMINES -- no missed positive chain
TOTAL: 22 checks; FAIL count = 0
```

(Environment: fresh venv under the session scratchpad in `/private/tmp`, sympy 1.14.0. The one unused intermediate `delta_hist` is inert; A3.2's load-bearing content is the exact derivative check, the identification δ(h) = 2h being elementary from χ_{±1}(h) = e^{±ih}. The exact integers/rationals appearing in checks (−2, 1/2, G = 1) are algebra residuals and triviality proofs, not evaluations of any physical constant.)

---

## 3. REVERSION DETERMINATION, STEP-MAPPED

Every build step was scanned against the design's forbidden-import list (carrier/cellulation machinery; K_KK; fiber proper radius R; any metric/fiber metric; ℓ_P; any length/scale object or β conversion; S16/Thomson/κ_Thomson/cross-section matching; α-rides-a-scale; the KK action; Finish-A/B framing; E1/Carleman/S2′ chain; GR/sphere/4π-as-sphere; continuum-diamond; empirical constants), including smuggled forms (fresh use of machinery behind a booked quantity):

| Build step | Stands on | Reversion scan |
|---|---|---|
| §0 verdict | ground concepts + instrument anchor | clean |
| §1 seals/blindness | digests (verified §1 above) | clean |
| §2.1 role-anchor | instrument text; both sectors ground-native | clean — needs no forbidden import to mean (adjudicated 2.1) |
| §2.2 sector split | §I.1–5, §II.6–10 quotes only | clean |
| §2.3 R1–R3 | anchor + ground invariance (§II.7–8) | clean |
| §2.4 typology | polar decomposition (generic math) + §I.3/§II.6 | clean |
| §3.1 Chain A | sealed-text enumeration | clean (under-count C-C is an error, not an import) |
| §3.2 Chain B | §II.6 quoted AS BOOKED — TYPE-R status and stated conclusion only; the trace-class/T_cyl machinery behind the booking is NOT re-run or extended | clean — bedrock-as-booked, no fresh machinery use |
| §3.3 Chain C | U(1) single-valuedness on the ground's own U(1) booking | clean |
| §3.4 Chain D | booked items only | clean (δ error is internal, not an import) |
| §3.5 Chain E | §I.5, §II.6 categoricity, §II.8–9 | clean |
| §3.6 CAS | symbols only; λ is the ground's own orbit parameter (§II.7), used solely as the booked invariance test — no scale fixed, anchored, or converted; ℏ symbolic per the ground's own lock display; `sp.solve` output is a rearrangement of booked locks, not a solution of any would-be invariant | clean |
| §3.7/§4.1–4.3 | adjudication over Chains A–E; supply route wording derives from §II.6's own "needs weight/amplitude structure" | clean (similar wording exists in off-ground §IV, but the build's chain cites §II.6 and never §III–§VI — checked line-by-line: no load-bearing citation outside §I–§II anywhere) |
| §4.4–4.6, §5 | ledger/disclosures/flags | clean — forbidden terms appear ONLY inside fence declarations and the forbidden-imports denial list (grep-verified); "β-invariant" used solely as the ground's own name for orbit fixed-point status, no conversion performed; no numeric/empirical token anywhere |

Token scans performed on the full build text and the extracted script: K_KK, Thomson, κ, S16, Carleman, Finish, ℓ_P/l_P, diamond, sphere, lens, 4π, Planck, electron, charge, QED, 137, digit-dot-digit floats, `evalf`, `.n(` — every hit falls inside the build's own denial/disclosure/fence sentences; zero substantive uses.

**REVERTED_TO_MACHINERY = no.**

---

## 4. DEFEAT-PROVENANCE, INJECTION, FENCES, BLINDNESS

### 4.1 Defeat-provenance of this audit's own objections

- **Sustained objections (C-A, C-B, C-C):** each stands entirely on bedrock — §I.3 (transition/character structure), §II.7 (orbit and kinematic symbols), the displayed locks, and exact algebra (A3.1–A3.5). No machinery object is needed to state or prove any of them. VALID.
- **Candidate refutations considered and REJECTED as FALSE defeats (machinery-dependent; labeled, not used):** (i) "surface §V books Finish B, so the honest class is DOES-NOT-DETERMINE (structure determined, scale free)" — stands on §III–§VI (off-ground) and on the Finish-A/Finish-B framing, which is itself on the forbidden list; FALSE. (ii) "α is empirically an action-scale coupling, so |ΔS_record| = πℏ is its referent and the surface DETERMINES it" — imports empirical α-physics; FALSE. (iii) "S16, once derived, will supply the amplitude bridge, so the referent exists and the verdict should be UNDERDETERMINED-AT S16" — stands on the S16/Thomson bridge (forbidden) and on off-ground §IV; FALSE. None of these is used anywhere in this audit's adjudication.

### 4.2 Injection

- Build → downstream: scanned for directive content aimed at the audit, registrar, or acceptance. None found: §4.3's "must be re-run from scratch under the same fences" is conditional program logic, not a directive; the flag block is the design-mandated output; nothing instructs the auditor to skip checks or the registrar to log a particular status; acceptance is left to the design's own section, as the design requires.
- Governing documents → this audit: the design directs the audit (that is its sealed role — governance, not injection); the surface document is descriptive registrar consolidation. Clean.

### 4.3 Fences (this audit's independent determination)

- **VALUE_EVALUATED = no, CONFIRMED.** No numeric evaluation of any physical constant anywhere in the build (grep + script scan + output scan: no `evalf`, no `N(`, no float literal; π, √2, ℏ symbolic throughout; the only exact numbers are algebra residuals of identity checks). No closed-form final solution displayed — none exists to display: the build stops at the referent gate, strictly before the design's pre-numeric gate, and forms no fixing conditions at all. No comparison, explicit or implied, to any measured value anywhere (no empirical token outside the denial lists). The same holds of this audit.
- alpha_computed = false, proof_authorized = false, kappa_record_computed = false: declared live in the build and never contradicted by any step; likewise live here.
- Exact symbolic CAS only, both batteries; build battery output mechanically faithful (byte-identical re-run, §2.5).

### 4.4 Blindness / no-files check

- Build §1 shows exactly the two governing seals plus its own output (+sidecar, verified). Corroboration: every register-line quote in the build (":6093", ":224", ":5187", "provably do not carry", the wrapped "NOT a magnitude on record", "Connections on a fixed line bundle") appears verbatim inside the surface document itself (grep-verified, including the line-wrapped phrase); the build displays no fact traceable only to an unopened file. Consistent with its blindness declaration; no evidence of any register/road/ledger/lens/plan/tracker access.
- This audit opened exactly: the three sealed record files and their sidecars, plus session-scratchpad process files (venv, extracted script, battery, captured outputs) under `/private/tmp`. Directory listings (filenames only) of `alpha_supervision/` and `workspace/` were taken to locate seals; no listed file was opened. No register, road, ledger, lens, plan, or tracker file was opened by this audit. No git command was run; no existing file was edited; the only files written to the record are this audit and its seal sidecar.

---

## 5. FLAG BLOCK

I2_VERDICT = CONFIRMED-WITH-CORRECTIONS — the build's ILL-POSED stands under independent re-derivation at its exact quantifier (empty candidate class: no cross-sector strength-slot exists on the bare surface; in-amplitude booked absent with mechanism and trivial-if-granted; in-phase integer-quantized and definitionally occupied phase-to-phase; missing referent and supply route correctly named); corrections named exactly: C-A (δ = φ₊ − φ₋ is a chart/history variable, not a free invariant — build B3.3a tested only a common shift, not the ground's transition action; honest-exhibit exemplar struck), C-B (H_R·T_R/ℏ is the genuine free role-less dimensionless invariant of the displayed ground; build B1.4/§4.2's "zero free dimensionless slots" holds only over the lock system's own variable set), C-C (Chain A's cross-sector inventory omits the displayed transition law z_j = g_ij·z_i; slot disposition identical to L1's); none load-bearing, verdict chain intact
REVERTED_TO_MACHINERY = no
VALUE_EVALUATED_CHECK = no — confirmed independently: no numeric evaluation of any physical constant, no closed form of any would-be solution formed or displayed (none exists; stop is at the referent gate, before the pre-numeric gate), no comparison explicit or implied to any measured value, in the build or in this audit; both CAS batteries exact-symbolic with byte-faithful embedded output
DEFEAT_PROVENANCE = clean — all three sustained corrections stand on bedrock only (§I.3, §II.7, displayed locks, exact algebra); three candidate refutations identified as machinery-dependent FALSE defeats (Finish-B/§V framing; empirical α-as-action referent; S16-will-supply-it), labeled and not used
NET_VERDICT = CONFIRMED-WITH-CORRECTIONS — ILL-POSED stands, reversion-clean; per the design's acceptance note the result logs CLAIMED and is eligible for I-2 movement on this reversion-clean check, panel discipline permitting; a machinery-reverted logging is NOT warranted
PROVENANCE = clean — audit stood on the two sealed governing documents only (ground restricted to §I–§II + displayed locks; §III–§VI touched solely to quarantine FALSE defeats); all three seals verified at path against sidecars; build blindness corroborated (every quote traces into the surface document; section 1 shows exactly the two governing seals plus its own output); audit opened no register/road/ledger/lens/plan/tracker file, ran no git, edited no existing file, wrote only this artifact and its seal
INJECTION = none found — no directive content in the build aimed at audit, registrar, or acceptance; governing documents clean
FENCES = intact — alpha_computed = false · proof_authorized = false · kappa_record_computed = false live throughout build and audit; exact symbolic CAS only; π, √2, ℏ symbolic everywhere; no empirical constant anywhere
