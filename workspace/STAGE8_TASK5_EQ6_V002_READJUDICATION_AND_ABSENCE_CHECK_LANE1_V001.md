# STAGE 8 TASK 5 / EQ6 - V002 READJUDICATION AND ABSENCE CHECK - LANE 1 V001

Date: 2026-08-04  
Lane: Codex Lane 1  
Task: PASTE 531 / Task 5 / EQ6  
Custody: adversarial readjudication and absence verification; DoR-020-A2 reserved

## Lead result

```text
PART_A_REPAIR = PASS
M16_ON_V002 = PASS
N7_ON_V002 = PASS

F_ACTUAL_MEMBERSHIP = FAILED (FC2, FC3, FC10, FC11)
ABSENCE_TYPING = CONFIRMED_AS_NO_PRESENT_CLASH
PHANTOM_ABSENCES = none
SCOPE_SHRINK = NOT_FOUND

DEPENDENCY_ORDER = REQUIRES_PRECISION:
  a full package functor carrying Adm_fin and C2_fin closes the missing
  J4 and J12 naturality faces together;
  a rail/index skeleton plus a finite bottom closes J15 only.

AXIOM_V002 = NOT_READY (
  F_ACTUAL_JOINT_FINITE_PACKAGE_CERT absent;
  dependency-stage wording must distinguish a skeleton from a full functor
)

MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

"Absence confirmed" has the exact evidentiary meaning used here: on every
shared overlap where both comparison legs already exist, the legs agree. At
each reported failure, at least one required leg, common domain, or physical
comparison map is not built. This is not a proof that a future joint package
exists. It is a proof that the present record supplies no contradiction and
no already-built object under another name.

## 0. Preflight and register sweep

### 0.1 Preflight

```text
DOES THE OBJECT EXIST?   yes; V002 verified before reading.
IS THE VERSION CURRENT?  yes; live register head Q-455 verified.
ARE ITS INPUTS PRESENT?  yes; both Lane-1 standards and the cited seam
                         authorities verified.
PREFLIGHT = PASS
```

Verified hashes:

| Object | SHA-256 | Verification |
|---|---|---|
| V002 under review | `a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b` | exact; sidecar OK |
| constraint arm | `96cd90b5bdcc2b77f510ebd21882b215aa5b70c944c9d58b2bdd8855fd52bf11` | exact; sidecar OK |
| V001 adjudication | `9fe38e932a4c892b1cd9cefec3977bffdd562db1441fdd477bbe5b31035c0a6d` | exact; sidecar OK |
| Q-313 Map 1 | `ea8f4e9a79a9e4aca4a0b9e9aab470d78cd4d9c78794c20d9e979c0db16fbeab` | exact; sidecar OK |
| pass-1 subpackages | `452f1bb87aeb7f7bfe4ab4556134cf723ab6df697f93c1a59c33671146cb0083` | exact; sidecar OK |
| bedrock theorem | `288a3d4147cb10f2e51180b21f7c6b2b4749503504c57b8a1121e3925c70c94c` | exact; sidecar OK |
| scoped package audit | `3b721be750f21e78d410f118aadd1235b4c6a86c1ae874cb591136db34ac017d` | exact; sidecar OK |
| five-generator scoped attempt | `3ae580d05886bfa0df23442a3d7eaf5a4500c0280229cc615f6998f29a93be8e` | exact; sidecar OK |
| finite Q-408 kernel calculus | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | exact; sidecar OK |
| J1-J15 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | exact; sidecar OK |
| live questions-settled register | `98ff9cccdd4505b035f0f6ccf8f97ec91c021360573b01c88909183be23aa3c9` | sidecar OK; head Q-455 |

`LOCKED_PROCESS.md` was read in full before this review. Its send-time hash
was `3bd2f9dad9ba95c83ae80419b5b9e817e6dd64d6c31aa1beadd63765cf6bf7c3`.

### 0.2 Questions-settled sweep

The following entries were checked before testing the purported absences:

| Register range | Settled content relevant here |
|---|---|
| Q-313 | `T_N^char=Hol_c^-1 o Z_N` exists only for scalar relative-history content on the sealed square; arbitrary-cellulation naturality is explicitly unbuilt. |
| Q-347 through Q-357 | Map-1 extension and prefix-to-cycle descent close an information-map layer. They do not build the local Maxwell reader, an action/Hessian package functor, or a package finite bottom. |
| Q-407 through Q-410 | Algebraic readers and the finite path-bilocal kernel exist; `Loc`, `pi_Mx`, completed locality, and the physical Maxwell symbol remain absent. |
| Q-411 | J1-J15 define a simultaneous equalizer/fiber product; definition is not inhabitance. |
| Q-420 through Q-424 | The six generator roots and dependency graph are typed; the physical reader is a conditional downstream consequence, not an already-built seventh root. |
| Q-423 | Pass 1 builds finite and rank-preserving component subpackages, not a joint package. |
| Q-428 through Q-446 | False cycle compression is removed; physical target-cycle content is retained; flip/current diamonds are built only on their proved scopes. |
| Q-449 through Q-452 | `Eq_flip` is one generator; package cofinality, the package finite bottom, and the simultaneous six-generator term remain absent. |
| Q-453 through Q-455 | The lawful axiom form, the M16/N7 repair, the live-antecedent requirement, and V002's claimed absence frontier. |

This sweep found no register entry whose conclusion has the signature of
`F_ACTUAL_JOINT_FINITE_PACKAGE_CERT`.

## 1. Verdict table

| Item | Verdict | One-line reason |
|---|---|---|
| T1 Part A repair | **PASS** | FC12 is struck, the live conjunction omits it, and every return-branch disposition is confined to `C_ret` Steps 3-4. |
| T2 absence typing | **PASS** | All existing parallel legs agree; each failed condition lacks a leg/domain/comparison rather than containing unequal existing maps. |
| T2 phantom-absence hunt | **PASS** | Q-313, the map-layer artifacts, pass-1 subpackages, and the bedrock graph all have different types or narrower scopes. |
| T3 dependency order | **KILL / BOUNDED PRECISION DEFECT** | A full package functor cannot close J12 while leaving the missing J4 action/Hessian naturality outside it; the build stages must distinguish a skeleton from a component-preserving functor. |
| T4 full live scope | **PASS** | `I_F` retains all active objects, orbit images, W3/DoR-008 arrows, cycle-creating arrows, common refinements, and sealed-consumer bottom arrows; no intersection-of-scopes replacement is used. |
| T5 fresh attack | **PASS** | The duplicate-arrow/partial-monodromy attack finds W3 and `Ref_path` current transports compatible where both exist; the first undefined face is again R1/C2 package transport. |

## 2. T1 - bounded Part A readjudication

### 2.1 FC12 and the live guard

V002 states

```text
FiniteCoherent_020^V002(F)
 := FC1(F) and ... and FC11(F) and FC13(F).
```

FC12 retains its number only as

```text
FC12 = STRUCK (M16/N7).
```

It is not in the conjunction. A complete search of V002 for return,
stationarity, connectivity, interval/convexity, absolute continuity,
branch regularity, modulus, and fixed point found no residual such predicate
in the eligibility formula, its conclusion, or a void condition. The terms
that remain occur only in:

1. the statement that the axiom is silent;
2. the independent `C_ret` Steps 3-4 test;
3. the delta/battery record documenting the deletion; and
4. fence statements saying no protected operation occurred.

Thus the deletion is normative, not merely editorial.

### 2.2 Q-448 placement

For

```text
D={-1,1},
b(-1)=1,
b(1)=-1,
b'(-1)=b'(1)=0,
q_der=0,
q_true=1,
Fix(b|D)=empty,
```

V002 makes no EQ6 eligibility decision. The branch is considered only when a
consumer attempts `C_ret`; it fails the independent Step-4 return/scope test.
Adding or removing this downstream branch leaves the A2 antecedent and
conclusion unchanged. Therefore:

```text
M16_ON_V002 = PASS
N7_ON_V002 = PASS
SCOPE_UNDERBREADTH_FROM_FC12 = false
```

### 2.3 Propagation audit

| Affected surface | V002 result | Verdict |
|---|---|---|
| FC table and conjunction | FC12 struck and omitted | **PASS** |
| `C_ret` disclaimer | wholly independent | **PASS** |
| Q-448 disposition | axiom silent; Steps 3-4 reject | **PASS** |
| choice table | no EQ6 return clause | **PASS** |
| void condition 6 | bars mixer/abstract-kernel misuse and using A2 to decide return; does not bar the disconnected branch itself | **PASS** |
| battery A4 | tests layer separation rather than antecedent rejection | **PASS** |
| anti-tuning ledger | independent `C_ret` retained | **PASS** |
| final board | `C_RET_ABSORBED=false` | **PASS** |

Part A meets the two repairs ordered by the V001 adjudication. It does not
cure the separate live-antecedent failure.

## 3. T2 - adversarial absence verification

### 3.1 Test used

A **clash** requires already-built data `f` and `g` with a shared typed
source and target, a ratified obligation identifying the two routes, and a
witness for `f(x) != g(x)`. An absent `g`, an undefined common source, or two
maps with different codomains is not a clash.

A **phantom absence** occurs if the allegedly missing `g` is already built
under another name with the required carrier, variance, covariance, and
scope. Similar output or an evocative name is insufficient.

### 3.2 FC2: package finite bottom

Existing:

- each Q-408, action-shadow, Ward, reader-core, and kernel component has its
  own exact finite restriction on its proved domain;
- every claimed component shadow reproduces DoR-008;
- the one-generator flip bottom is exact on `I_flip`.

Required:

```text
one rho_N^pkg through one F_actual_N
such that every component and every sealed consumer factors through it.
```

On a consumer outside the one-generator flip bottom, the Q-408 restriction
exists while the package R1/C2/C3 legs are not jointly defined. There are no
two package-bottom maps with unequal values. There is only one existing
component map and one missing package face.

```text
FC2_CLASH = none found
FC2_STATUS = ABSENCE / package-wide bottom unbuilt
```

### 3.3 FC3: common functor domain

For a rank-preserving subdivision `s_nm:n->m`, the existing current/kernel
data obey

```text
P_nm=id,
S_nm(c_e)=(c_e,...,c_e),
A_n=S_nm^* A_m P_nm,
Kern_m^fin(j_nm^H H)=Kern_n^fin(H).
```

Composition gives `S_nl=S_ml S_nm`, and the corresponding current/kernel,
finite Ward, C1-orbit, and faithfulness faces commute. The missing parallel
face is the action/Hessian transport of `Adm_fin` on that same `Ref_path`
arrow. On old ratified arrows, where both action/Hessian and restriction
maps exist, the stage cubes pass. No old cube gives a different value for a
future `Ref_path` map.

For a flip arrow, the verified Q-408 and where-data maps likewise exist, but
some C1/C2/C3 and R1 package maps do not. The Q-428 false compression is not
retained: FC4 keeps the nonzero target-cycle kernel and permits only the
proved contravariant/old-image restriction.

```text
FC3_CLASH = none found
FC3_STATUS = ABSENCE / full component-preserving functor unbuilt
```

### 3.4 FC10: physical J2 and the finite J-family

The two sides required by J2 have signatures

```text
ell_alg : finite algebraic profile carrier -> scalar,

pi_Mx o Loc o Kernbar o Q : physical profile quotient -> local Maxwell
                            scalar.
```

Only the first side and the finite path-bilocal kernel are built. The local
realization `Loc` and normalized physical projection `pi_Mx` are not.
Therefore no physical J2 equality has two sides to compare.

Defining `pi_Mx` by demanding equality with `ell_alg` would be the permanent
circular-reader regression. The corpus correctly does not do so.

```text
J2_CLASH = none found
J2_STATUS = ABSENCE / physical comparison side unbuilt
```

The remaining finite J-ledger has no contrary pair either:

- J4 passes on old stage arrows and has no `Ref_path`/full-`I_F` action face;
- J12 passes for the finite Q-408 kernel square on `Ref_path`/flip and has no
  full-package response naturality map;
- J15 passes component bottoms and has no package bottom.

### 3.5 FC11: joint overlap diamonds

The three explicit diamonds were recomputed at the level supported by the
artifacts.

**Rank-preserving diamond.** Q-408, C1 orbit, finite faithfulness, zero
response cocycle, and finite Ward faces commute. The R1 face is undefined.

**Flip diamond.** Disjoint stabilizers commute; the covariant odd defects
satisfy `Def_j=-Def_j`, hence vanish; the local orthogonal excision and
corrected contact conditions give the same Q-408 zero-defect term. The other
five generator faces and J2/J7/J12 shared comparisons are not all defined.

**Mixed subdivision/flip diamond.** The path/current and U(1)-bundle legs
agree by additivity and pullback composition; the physical finite kernel has
equal restrictions. The `rho_Gamma`, common C1 topology/bounds, response
`Eta/beta`, physical J2 factorization, and package bottom are absent.

No diamond contains two existing unequal composites.

```text
FC11_CLASH = none found
FC11_STATUS = ABSENCE / joint faces unbuilt
```

### 3.6 Phantom-absence sweep

| Alleged missing object | Nearest existing object | Why it is not the object |
|---|---|---|
| full package functor on `I_F` | Q-313 `T_N^char` | `Q_rel,N -> Q_square`, scalar and square-scoped; arbitrary-cellulation naturality explicitly absent. |
| full package functor on `I_F` | Q-347 to Q-357 prefix/cycle maps | information-layer maps on signed edge-resolved carriers; no R1, C1, C2, C3, or package-bottom components. |
| package finite bottom | component DoR-008 squares | different component domains; Q-451 explicitly finds no consumer-complete common bottom. |
| package finite bottom | `I_flip` bottom | one-generator, noncofinal package scope. |
| physical J2 | algebraic reader `p_(chi,T)` | no independent local Maxwell map; using it to define `pi_Mx` is circular. |
| physical J2 | finite `Kern_N` / `sigma_cyl` | path-bilocal distribution, not a diagonal local field symbol or normalized Maxwell projection. |
| physical J2 | Q-313 `T_N^char` or descent `D_G` | cycle-character maps, not a local-symbol coefficient functional. |
| all-generator J4 | retained `Adm_base` stage cubes | old-arrow scope only; no `Ref_path`/new-arrow action-Hessian transport. |
| joint J1-J15 term | pass-1 compatibility subdiagram | J2 absent and J4/J12/J15 scoped; artifact records `FULL_EQ6_TERM=absent`. |
| joint overlap diamonds | `Eq_flip` and Q-408 diamonds | one generator or component faces only; separate nonemptiness is not the equalizer. |
| physical reader from bedrock graph | conditional reader theorem | derives only after C1 + faithfulness + C3 physical maps; those premises are not jointly instantiated. |

No phantom absence was found.

### 3.7 Absence verdict

```text
ABSENCE_TYPING = CONFIRMED
  meaning: no present forced mismatch on a shared built overlap.

CONTRADICTION_OF_FUTURE_COMPLETABILITY = NOT_PROVEN
CONSISTENCY_MODEL_OF_FULL_PACKAGE = NOT_PRODUCED
```

The second and third lines are required scope controls. The result verifies
V002's diagnosis without promoting non-contradiction into existence.

## 4. T3 - dependency-order audit

The J-ledger supports the following implications.

### 4.1 What a full package functor would close

If stage 1 constructs exactly what V002 names,

```text
one functor on I_F carrying all F_actual components,
```

then its morphisms must carry both `Adm_fin` and `C2_fin`. Consequently:

1. the missing action/Hessian restriction of `Adm_fin` is present and
   composes, closing the naturality part of J4 on `I_F`;
2. the `C2_fin` response maps are present and natural, closing finite J12;
3. the package finite bottom independently closes J15; and
4. package identity/composition closes the corresponding J10 face.

All other J4 certificates are already proved on their current domains. The
load-bearing missing J4 datum is precisely the extension to the new arrows.
It cannot remain wholly outside a genuinely component-preserving full
functor.

### 4.2 What a rail/index skeleton would close

If stage 1 instead constructs only

```text
the common category I_F + named component slots + a package finite bottom,
```

then J15 closes, but neither J4 nor J12 follows. Naming common arrows does not
construct the action/Hessian or response maps on them.

Therefore the statement "functor plus bottom repairs J12/J15, while J4 is
left for stage 2" is not type-stable. It equivocates between a skeleton and a
full component-preserving functor.

### 4.3 Corrected dependency order

The build can be made exact in either of two equivalent organizations:

```text
Stage 1a: common I_F indexing skeleton + consumer-complete finite bottom
          -> J15.

Stage 1b: component transports on every I_F arrow:
          Adm_fin action/Hessian maps -> J4 naturality;
          C2_fin response maps        -> J12 naturality;
          remaining component maps    -> FC3/J10.

Stage 2:  physical J2, independently induced by
          pi_Mx o Loc o Kernbar o Q;
          then J7 can be compared at the physical reader level.

Stage 3:  one simultaneous finite J1-J15 proof and every mixed/common-
          refinement overlap proof.
```

Or, if "Stage 1" is reserved for the full package functor, Stage 1 closes
both J4 and J12, while Stage 2 contains physical J2 only. The artifact or its
successor must choose one vocabulary and keep it.

```text
AFTER_A_TRUE_FULL_FUNCTOR_PLUS_BOTTOM:
  J2 = remains absent;
  J4 naturality = no longer absent;
  J12 = no longer absent;
  J15 = no longer absent.

AFTER_A_SKELETON_PLUS_BOTTOM:
  J2 = remains absent;
  J4 naturality = remains absent;
  J12 = remains absent;
  J15 = closed.
```

This is a dependency bookkeeping defect, not a clash among existing
mathematical objects. It does not overturn the absence classification.

## 5. T4 - full-live-scope check

V002 defines `I_F` extensionally by:

- all active finite Q-408, `Ref_path`, and flip stages;
- every covariance, reality, frame-torsor, bundle-gauge, and orientation
  image;
- W3/DoR-008 restriction arrows;
- rank-preserving and cycle-creating physical arrows;
- built disjoint/contact common refinements; and
- every bottom arrow required by a sealed package consumer.

It explicitly refuses to intersect the component domains. The asymmetric
primitive attack is then run inside that full domain: Q-408 data survive,
while R1 and six-component overlap maps remain absent. That attack would
disappear under the prohibited `I_flip` or common-scope shrink, but it is
present in V002.

```text
I_F_REPLACED_BY_I_flip = false
I_F_REPLACED_BY_INTERSECTION_OF_COMPONENT_SCOPES = false
ACTIVE_ASYMMETRIC_PRIMITIVE_RETAINED = true
SCOPE_SHRINK = NOT_FOUND
```

The phrase "all sealed active finite stages" is an extensional commitment,
not an enumeration. Future assembly must retain the same object and arrow
set; silently changing it would void this pass.

## 6. T5 - fresh attack: duplicate-arrow monodromy

### 6.1 Attack

Some rank-preserving refinements appear both as W3 restriction/isometry data
and as `Ref_path` subdivisions. If these names induced distinct maps on the
same physical current/kernel carrier, the package could contain a hidden
clash before any missing map is built.

Take `n->m->l`. The established formulas are

```text
S_nl=S_ml S_nm,
A_n=S_nm^* A_m P_nm,
j_nl^H=j_ml^H j_nm^H,
Kern_m^fin(j_nm^H H)=Kern_n^fin(H).
```

W3 supplies the same rank-preserving current isometry and adjoint
compression used in these equations. Thus the two labels give the same
current/kernel composite, and the response discrepancy remains
`beta_nm=0`. There is no current/kernel monodromy around the duplicate path.

### 6.2 Boundary reached

The attack reaches the same first undefined faces:

- no R1 action/Hessian map on the `Ref_path` arrow;
- no full C2 response map on every package arrow;
- no physical J2 map; and
- no package bottom/joint mixed diamond.

It therefore strengthens the absence finding rather than refuting it.

```text
FRESH_ATTACK = PASS
DUPLICATE_ARROW_CLASH = none found
FIRST_UNDEFINED_FACE = R1/C2 package transport
```

## 7. Final determination

V002 has repaired the M16/N7 defect completely. It has also done the right
adversarial thing with `F_actual`: it names the live tuple, keeps the full
scope, and reports failed membership rather than using the completion axiom
to manufacture its own finite premise.

The independent review confirms the builder's central diagnosis. FC2, FC3,
FC10, and FC11 fail because joint finite package objects are absent; no
existing pair already disagrees, and none of the missing objects is present
under a different name. But the prior triviality kill is not cured: the live
antecedent still is not inhabited. DoR-020-A2 therefore cannot yet use this
artifact to discharge Step 1.

The assembly order needs one correction in language and type. A full package
functor carrying all components necessarily includes the action/Hessian and
response naturality faces. If those faces are deferred, the first object is
only a package skeleton, not the full functor advertised by
`F_ACTUAL_JOINT_FINITE_PACKAGE_CERT`.

```text
AXIOM_V002 = NOT_READY (
  F_ACTUAL_JOINT_FINITE_PACKAGE_CERT absent;
  T3 package-functor dependency precision required
)
ABSENCE_TYPING = CONFIRMED
PHANTOM_ABSENCES = none

READY_FOR_DOR020A2_RULING = no

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

STOP: after sealing, byte-identical mirroring, verification, and hash report,
this lane performs no register, plan, tracker, git, commit, push, member
binding, fixed-point, end-test, or evaluation action.
