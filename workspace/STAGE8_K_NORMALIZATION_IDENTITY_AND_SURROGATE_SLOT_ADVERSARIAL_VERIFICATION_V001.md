# STAGE8 K Normalization Identity and Surrogate-Slot Adversarial Verification v001

LANE: CODEX 1
RELAY: 279
DATE: 2026-08-01
REGISTER HEAD CONSULTED: Q-187
CLAIM UNDER TEST: Q-186, explicitly marked reviewer-constructed and requiring
Codex verification

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 0. Verdict

**Bohm's withdrawal survives, but only after one currency correction and one
strict type fence.**

The live v004 dimension ledger independently states the local Maxwell action,
the unit-character field normalization, and

```text
alpha_micro(K) = 1/(4 pi K).
```

They use the same symbol for the same coefficient of the normalized local
Maxwell kernel. The identity therefore does not belong only to the superseded
v002 microscopic-input reading. Indeed, no historical transfer from v002 is
needed: the live v004 ledger contains the derivation's premises and conclusion
in one authority.

The type fence is load-bearing. The live authority also says that this `K` is
only a local surrogate, that `B_surrogate(K)=K`, and that the exact induced
coefficient `B_ind(K)` is not yet derived or matched to it. Thus:

```text
generic_local_Maxwell_normalization_identity_derived = true
live_v004_ledger_K_is_local_surrogate_slot = true
live_v004_ledger_K_is_exact_induced_response = false | TYPE-R |
  test: v004 expressly distinguishes B_surrogate(K)=K from B_ind(K) and
        forbids treating K*F as the exact induced displacement
physical_K_star_selected_or_derived = false | TYPE-U |
  would-build: the complete zero-bare CTP operator, raw-correlator-to-retarded-
               Hessian map, covariant local coefficient functional/projector,
               vanishing complementary residual, and unique positive root
alpha_micro_from_physical_K_star_derived = false | TYPE-U |
  would-build: the preceding physical-response derivation followed by the
               already-derived normalization conversion
```

This reconciles the live ledger with Q-140 N11. The generic normalization map
is derived. Applying it to a quantity named `K_*` as the physical coupling is
still carried/adopted because the route has not derived that physical root.
The withdrawal closes a formula-currency alarm; it does not close the eleven
false response/selection flags.

## 1. Scope and Method

### 1.1 Roots entered

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/
  alpha_fundamental_record_action_cleanroom_v003
/Users/bgm/MB Work/alpha_supervision
/Users/bgm/MB Work/alpha-program-archive/cleanroom_output
```

The archive `workspace/` mirror was excluded because it duplicates cleanroom
artifacts. `.git` was excluded. The directory
`a32_holdout/custodian_private/` was never entered, listed, read, or searched.

### 1.2 Queries

Word-boundaried, case-insensitive searches were run for:

```text
alpha_micro
e_HL
e_G
unit-character
S_M[A;K]
B_surrogate
B_ind
K-B_ind
K_R,*
K_ind
K_ext
microscopic ... surrogate
surrogate ... microscopic
same normalization slot
supersedes
active_physics_inputs
forbidden_active_dependency_paths
```

The search tested both formula currency and the existence of an explicit map
between the historical microscopic coefficient and the live surrogate. It did
not use shared symbols as evidence of identity.

```text
sealed_historical_microscopic_K_to_live_surrogate_K_map_found = false | TYPE-S |
  roots: the four roots listed above
  excl: archive workspace duplicate, .git, a32_holdout/custodian_private/
  fences: no value, response, root, scale, or measured comparison evaluated
  query: word-boundaried searches listed in Section 1.2
```

That bounded negative is not the physical-response result. The active v004
surrogate-to-induced match is named, not absent by search, and is TYPE-U.

## 2. Currency: v003 Is Not Live

The relay cites `alpha_complete_dimension_convention_ledger_v003.md:200-209`.
That file is not current:

1. `alpha_complete_dimension_convention_ledger_v004.md:5-12` says v004
   supersedes v003 for post-clean-room work and retains the surrogate/exact-
   induced distinction.
2. `alpha_post_cleanroom_route_state_v002.md:16-23` appoints the v004 ledger,
   v004 symbolic spine, and v004 selection principle as the active authorities.
3. `alpha_post_cleanroom_current_authority_spec_v001.json:2-10` lists the v004
   ledger among active physics inputs.
4. The same JSON at `:48-57` places the v003 ledger in
   `forbidden_active_dependency_paths`.

```text
dimension_ledger_v003_live = false | TYPE-R |
  test: direct supersession and active-authority declarations above
dimension_ledger_v004_live = true
```

This does not defeat Bohm's mathematical reading because v004 repeats the
same identity at `:221-234` and places it in the same document as the live
surrogate action and Hessian normalization.

## 3. The Algebraic Slot Test

The live v004 ledger fixes:

```text
S_M[A;K] = -hbar K/4 integral F[A]^2
D_mu psi = (nabla_mu+i A_mu) psi
```

at `alpha_complete_dimension_convention_ledger_v004.md:75-95`. It then defines
the canonical field by `A=e_HL A_canonical` at `:221-229`. Since curvature is
linear in the abelian connection,

```text
F[A] = e_HL F[A_canonical].
```

The coefficient of the canonical Maxwell term is therefore `K e_HL^2`.
Canonical normalization requires

```text
K e_HL^2 = 1,
e_HL^2 = 1/K.
```

With the ledger's declared Gaussian/Heaviside-Lorentz conversion
`e_G=e_HL/sqrt(4 pi)`, it follows algebraically that

```text
alpha_micro(K) = e_G^2 = e_HL^2/(4 pi) = 1/(4 pi K).
```

The same v004 ledger verifies the action-Hessian slot directly: its doubled
local Maxwell action at `:269-287` has retarded transverse kernel

```text
Pi_R,M = -hbar K (q^2 eta - q q).
```

The K in the charge identity and the K multiplying the normalized local
Maxwell kernel are therefore the same object inside the live ledger. This is
not an inference across lineages.

## 4. Six Adversarial Questions

### 4.1 Is the ledger's K the same K as v004's surrogate?

**Yes, at the normalization-slot level.** The live ledger's `K` is the
dimensionless coefficient of the fixed-`K` local surrogate action
(`:70-95`), its boundary displacement `K*F` (`:154-180`), and its normalized
transverse retarded kernel (`:269-287`). The active selection principle writes
the same candidate slot as

```text
H_R[G_K] = K L_T + declared higher/nonlocal structures
```

at `primitive_record_cell_selection_principle_v004.md:125-145`, with
`p_loc[L_T]=1`.

**No, if "same" is enlarged to mean exact physical induced response.** The
ledger at `:188-199` says the exact induced functional does not inherit the
surrogate displacement and forbids treating `K*F` as exact. At `:332-389` it
separates

```text
B_exact,total(K) = B_ind(K),
B_surrogate(K) = K,
```

and calls `K-B_ind(K)=0` prospective rather than derived.

The correct identity is therefore:

```text
ledger_K = live_local_surrogate_Maxwell_coefficient
ledger_K = exact_induced_physical_coefficient   [REFUTED as a current result]
```

### 4.2 Is ledger v003 live?

No. It is superseded and forbidden as an active dependency. Ledger v004 is
live and restates the identity. Bohm's citation was stale; the result is not.

### 4.3 What does "does not select K" mean?

It has two simultaneous consequences:

1. The identity is not vacuous as algebra. For every positive candidate
   coefficient occupying the local Maxwell normalization slot, it supplies the
   charge-normalization conversion.
2. It is insufficient as physics. It neither proves that a candidate occupies
   the exact induced slot nor chooses a value or root.

The live symbolic spine states the conditional form explicitly at
`alpha_surface_symbolic_spine_v004.md:32-39`: only once a physical
dimensionless Maxwell stiffness is independently computed does the identity
give `alpha_micro`; the identity does not compute that stiffness.

Thus Q-140 N11 and the v004 ledger are not contradictory when typed:

```text
alpha_micro(K) as generic normalization identity = DERIVED
alpha_micro = 1/(4 pi K_*) with K_* physical      = CARRIED/ADOPTED, TYPE-U
```

### 4.4 Does v001 carry a different alpha formula?

No. `primitive_record_cell_selection_principle_v001.md:65-93` defines

```text
K_R,* = 4 Delta Phi_EM,* / I_F,*,
alpha_* = 1/(4 pi K_R,*).
```

The functional relation between a canonically normalized local Maxwell
coefficient and alpha is identical. What differs is how the coefficient was
supposed to be selected: v001 used a direct phase-residual/field-integral
ratio, while the live route uses a prospective zero-bare induced-response
fixed point. Version 001 does not supply a second alpha formula.

```text
two_incompatible_alpha_formulas_found = false | TYPE-R |
  test: symbolic comparison of the v001 and live v004 displays
```

### 4.5 Is there a sealed microscopic-to-surrogate map, and is one needed?

No explicit historical map was found under the scope in Section 1. This does
not block the generic identity, because v004 derives it from the live local
surrogate action itself. A map from v002's microscopic-input coefficient to
v004's surrogate is therefore not required for normalization.

A different map is required for physical use and is explicitly unbuilt:

```text
complete microscopic zero-bare CTP object
  -> exact induced retarded action Hessian Pi_R,ind
  -> covariant local coefficient B_ind
  -> full Dyson solution with K = B_ind and R_comp = 0
  -> physical K_*
  -> generic normalization identity.
```

This is stated by the live ledger at `:355-389`, the selection principle at
`:115-168`, and the route state at `:52-72`. Calling this an old-K/new-K map
would be a type error: it is a microscopic-response-to-physical-coefficient
derivation.

### 4.6 Which K sense occurs in the identity?

It is not a fifth sense. In the live authority it is exactly the fixed-`K`
local surrogate Maxwell coefficient:

```text
K                     dimensionless coefficient of the local F^2 action and
                      normalized transverse Maxwell Hessian slot
K_ext                 extrinsic-curvature tensor in the gravitational
                      boundary action; different type
K_ind[G]              superseded v003 name for a bilocal induced Dyson kernel;
                      v004 forbids writing the factor-incomplete form and uses
                      Pi_R,ind instead; different type
K_R,*                 v001 selected local Maxwell coefficient; same algebraic
                      normalization role, different superseded selector
v002 microscopic K    local Maxwell coefficient inserted as microscopic input;
                      same algebraic normalization role, different provenance
v004 surrogate K      candidate local Maxwell coefficient, excluded from the
                      exact zero-bare functional being differentiated
```

Shared normalization role does not establish shared provenance or physical
status. That distinction is precisely why the identity survives while the
response selection remains open.

## 5. Counterexample Hunt and Final Status

The attempted counterexamples have the following dispositions:

1. **Stale ledger:** real citation defect, not a result defect; live v004
   repeats the identity.
2. **Different action/Hessian meanings:** refuted within v004; the same K is
   used in the local action, canonical rescaling, and normalized local Hessian.
3. **Identity vacuous before selection:** false as algebra, true as a statement
   of physical insufficiency; the two claims have different types.
4. **Second v001 alpha formula:** refuted; it is the same functional formula.
5. **Missing v002-to-v004 map:** real bounded absence, but irrelevant to the
   generic normalization identity.
6. **Surrogate already physical:** refuted by the live ledger itself; this is
   the surviving load-bearing gap.

Final verification:

```text
Q186_normalization_identity_currency_claim_verified = true
Q186_same_local_normalization_slot_claim_verified = true
Q186_phrase_transfers_without_rederivation_precise = false | TYPE-R |
  test: v003 is superseded, while live v004 independently restates and derives
        the identity; the result is renewal in live authority, not transfer
Q186_does_not_select_K_caveat_verified = true
Q186_upgrades_surrogate_to_exact_induced_response = false | TYPE-R |
  test: the live authority expressly forbids that upgrade
physical_induced_response_match_derived = false | TYPE-U |
  would-build: the Step-5 chain enumerated in Sections 0 and 4.5
```

**The foundational alarm stays withdrawn in its narrow form: the normalization
identity is current and does not belong to a superseded K. The physicality
alarm stays open: no live result yet makes the surrogate coefficient the exact
induced response or produces a physical `K_*`.**

No value, response, root, scale, eigenvalue, beta function, or measured
comparison was evaluated.
