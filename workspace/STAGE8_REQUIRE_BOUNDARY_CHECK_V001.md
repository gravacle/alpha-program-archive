# STAGE 8 — REQUIRE-BOUNDARY-CHECK: BLIND ADVERSARIAL VERIFICATION OF THE BOUNDARY-SPLIT BUILD
## BLIND ADVERSARIAL VERIFIER (REQUIRE-BOUNDARY-CHECK) — DEFAULT = REFUTE — [CLAIMED]

Date: 2026-08-13
Role: REFUTE. Cross-lineage byte-level attack on STAGE8_REQUIRE_BUILD_BOUNDARY_SPLIT_V001.md
(NET = UNDECIDED-GAP). Connection-only, symbolic; no value of n/kappa/alpha computed,
bounded, estimated, or compared. No scale/faithfulness/GR imported BY ME (machinery named
as suspect, never leaned on as authority). No register/tracker/plan/road/ledger/lens read.
No git action. Output name probed before write: ABSENT.

Gates: alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false ;
coupling_evaluation_authorized = false

---

## 0. SEAL VERIFICATION AT PATH (shasum -a 256, this relay)

| Tag | File | Seal recomputed at path | vs tasked/build-claimed |
|---|---|---|---|
| BUILD | STAGE8_REQUIRE_BUILD_BOUNDARY_SPLIT_V001.md | 4b3356e32920410d2d896910cbe6e006515b171a90d99a61f066ce1eb5793812 | MATCHES tasked 4b3356e3 |
| FORM | STAGE8_R_RECORD_L_FORM_FABLE_V001.md | 5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37 | MATCHES tasked 5e49d209 |
| G3D | STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md | 1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb | MATCHES tasked 1a96e095 |
| POSE | STAGE8_REQUIRE_TARGET_POSED_V001.md | e3c482b5facfb7415199969d43155ddcaeae73b7714dac30c40af35ed88db784 | MATCHES build e3c482b5 |
| CHK | STAGE8_REQUIRE_TARGET_POSE_CHECK_V001.md | 86068343c6e0ac1ecda18ac80a27038131b3d216a0de30f6afeafc378f0fa47a | MATCHES build 86068343 |
| T1B | STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md | e6fae1428cecffb3c81deb7227c389e4c9d2f2bca3bad63cb9b0f5b03a3ca6ee | MATCHES build e6fae142 |
| S1 | STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md | 660e0c1464aee4f3a0bccd60e79050d5f1573378f5b11cfe84ec1ba3a975df1f | MATCHES build 660e0c14 |
| S4 | STAGE8_7A_FLUX_CORRESPONDENCE_DARIO_V001.md | 3c6cd9a2ee1b6ea1093370e9868c93a200c3631317bb91ecaf3ec9153a744c0b | MATCHES build 3c6cd9a2 |
| S7 | STAGE8_OVERDETERMINATION_SPECIFICATION_AND_PAIR_SURVEY_EINSTEIN_V001.md | 8b8b89770a2b7c252dab73c94426c8ec7ca079559d32a6877a8a9b12510ee3db | MATCHES build 8b8b8977 |
| S8 | STAGE8_INSIDE_OUTSIDE_CONSISTENCY_CONDITION_EINSTEIN_V001.md | 15793db5d7869356947cea69b7541db96d23b351692c8543314bf9e4bcf15184 | MATCHES build 15793db5 |
| S5 | STAGE8_GRAVITY_FORCES_COUPLING_HUNT_DARIO_V001.md | 6f5d4b90d227a9aa2a012a76e45894925d7b23037092f9bbf66db085ab541b26 | MATCHES build 6f5d4b90 |

Every seal verified at path; none failed; all match the build's own table and the tasked
digests. The build's provenance table (§1) is HONEST on custody.

---

## 1. WHAT THE BUILD MUST PROVE, AND THE ONE OBJECT UNDER ALL FOUR CLAIMS

phi_H = the H-block component of the per-cell write chain ell_j, where
H = ker(d_1) ∩ im(d_0)^perp is the FLUX-FREE physical block (dim 32, non-coboundary)
(FORM D1 :140-142; G3D §3.3 :203-214). The 32-dimensional assignment freedom = the
equal-energy set = adding an H-vector to the write (energy is quadratic in the flux
F = d_1 phi, and d_1 kills H, so H moves the assignment at fixed energy) (S1 §1.6 :204-210;
G3D :90-94; CV certificate). So "phi_H != 0" ⟺ "the write populates H" ⟺ "the write reads
H" (G3D :209-214). The of-record interior constraint is only phi_f + phi_H != 0 (not both
zero); the split is FREE datum G3, NOT SUPPLIED (G3D §3.1 :166-183, §0). The build asks:
does the DERIVED boundary force phi_H != 0.

---

## 2. ATTACK 1 (HIGHEST VALUE) — CLAIM 1 IS REFUTED: A DERIVED, CONNECTION-ONLY BOUNDARY OBJECT BEARS ON phi_H

**Build's CLAIM 1 (the "pivot of the determination", build §2 :95-101; §0 :32-38; §3.2
:157-169; §6 :258-259):** "H is FLUX-FREE (H ⊆ ker(d_1)) ⇒ no curvature ⇒ no boundary
signature ... it is the block the boundary cannot see ... A connection-only boundary cannot
force a quantity it cannot detect." The face-response E4 is asserted to "read R_record,L
exactly and ONLY on this block [im(d_1^dagger)]" and to be "BLIND to phi_H, because phi_H
does not appear in it," citing FORM §4.1 :293-295 and S1 :534.

**The refutation, at the bytes.** The build's own cited source S1 refutes the structural
blindness, in two spans the build did not engage or engaged selectively:

(a) **S1 §2.4 flag 2 :405-411 (a SEALED-DERIVED typing, not conditional on any unsealed
form):** "The equal-energy set is 32-dimensional; x_alt − x_F is not a coboundary ... so the
freedom is physical, not gauge. **Fields that consume the assignment rather than the energy
— and §3 shows the face response is one — have no natural transport until that is settled.**"
S1 TYPES the face response as a consumer of the assignment (the 32-dim H-freedom = phi_H),
NOT as a flux-only/energy-only object. This is the opposite default from the build's.

(b) **S1 §3.4 :532-536 (the materiality fork):** the line the build quotes — "The
32-dimensional assignment freedom is invisible to every criterion that consumes the
response" (:534) — is the MEANING of the **FACTORS outcome only**. The adjacent DOES-NOT-
FACTOR row (:535) reads: "**Equal-energy refined configurations give different responses;
the response is not a function of the record's measure ... The assignment is physical.**"
i.e. on that branch the face response DEPENDS ON phi_H (equal-energy configs differ only by
an H-vector). Which branch holds is UNDETERMINED — both settlement paths are SPEC-GAPPED
(S1 §3.3 :511-528; §3.5 MATERIALITY = typed :551-555).

**The equivalence that collapses the build's pivot (all connection-only, quoted):**
mathcal_K_L factors through the energy (S1 FACTORS)  ⟺  Phi_f^T C Phi_f = c_L G_(2,ell) on
the flux block (FORM §4.3 :342-343)  ⟺  the response is invariant under the phi_H background
(the energy G_(2,ell) is H-invariant, so proportionality to it makes the response
H-invariant)  ⟺  "the 32-dim freedom is invisible" (S1 :534). These are ONE undetermined
fork viewed three ways. **Whether the derived boundary sees phi_H IS the materiality fork,
which is OPEN — not a settled "blind."**

**Where the build's inference breaks.** "The face response reads im(d_1^dagger) ONLY"
(FORM §4.1 :293-295) is a statement about the ARGUMENTS: Q_flux xi ∈ im(d_1^dagger) ⊥ H, so
mathcal_K_L samples the flux-flux block Phi_f^T C Phi_f. It is NOT a statement about the
VALUE. R_record,L itself carries an explicit phi_H block and cross term —
R_record,L = n^2 [ Phi_f^T C Phi_f , Phi_f^T C Phi_H ; Phi_H^T C Phi_f , Phi_H^T C Phi_H ]
(FORM §4.2 :310-315) — so the exterior-determination object is manifestly NOT flux-free-
blind; it has a whole H-block. The build conflates "mathcal_K_L's arguments ⊥ H" (true) with
"the boundary determination is blind to phi_H" (false/undetermined). The load-bearing hidden
premise — build §2 :97-99, "the exterior/boundary of the cell IS a flux datum" — is false:
the exterior determination is R_record,L(Q_flux·,Q_flux·), whose VALUE consumes the
background assignment (S1 §2.4 flag 2), not merely the flux.

**Verdict on CLAIM 1: REFUTED.** The derived object that sees (bears on) phi_H is the E4
face-response mathcal_K_L itself — a derived, connection-only, scale-free boundary object
which S1 §2.4 flag 2 :405-411 TYPES as consuming the 32-dimensional assignment freedom
(= phi_H), and which on the DOES-NOT-FACTOR branch (S1 :535) gives different values on
H-differing configurations. "Flux-free ⇒ no boundary signature" is NOT forced; it is the
FACTORS branch of an UNDETERMINED fork, asserted as if structural. The build's "the sealed
structure leans FREE" (§6 :275) and "the boundary is not the place forcing comes from"
(NET flag :377-378) are therefore unsupported: the sealed boundary structure carries a
LIVE, undetermined phi_H-channel; it does not lean FREE.

**Scope of the refutation (stated honestly, not flattened).** This REFUTES the structural-
blindness pivot and the FREE-lean. It does NOT flip NET to FORCED: on the DOES-NOT-FACTOR
branch the response depends on phi_H but is not shown to REQUIRE phi_H != 0, and whether the
write even reads H is the unsupplied G3 datum (G3D §3.3 :203-214). Nor to FREE. The corrected
picture is: the boundary HAS a phi_H-detecting channel (the face response), and whether it
forces phi_H is undetermined — blocked on the SAME object the build named (R_record,L's form,
G1). So the trichotomy LABEL UNDECIDED-GAP survives, but its stated justification is wrong.

---

## 3. ATTACK 2 — CLAIM 2 (FORCING OBJECT NON-BOUNDARY-LOCAL): CONFIRMED, WITH ONE CORRECTION

**Build's CLAIM 2:** the only object that could force phi_H is the FORM of R_record,L on
connection tangents (G1) — a post-thermodynamic-limit BULK object, not boundary-local.

**Verified against the sealed text.** The phi_H-channel (attack 1) is the face-response,
whose H-sensitivity requires (i) R_record,L, defined "evaluated after the thermodynamic/
linked-cluster limit" (FORM B1 :68-72; S1 §3.1 :447-454), and (ii) the Gate-5 connected
linked-cluster kernel C (FORM D5-D6 :233-282; "the connected thermodynamic limit remains a
Gate-5 obligation"). T1B §5 DERIVES BOUNDARY_DATUM_LOCAL = no for exactly this datum, naming
both bulk dependences — R_record,L post-limit and Q_flux a whole-complex min-norm lift
(T1B :162-193, :352-361) — with the consequence "a winding fixed through this agreement would
be fixed by the BULK record dynamics ... not by boundary structure alone." No boundary-local
route to R_record,L's form or to C exists in the sealed sources. The non-locality is FORCED
by the sealed text. **CONFIRMED.**

**Correction (bearing on the "boundary is the complete extent" thesis — stated exactly).**
The build over-frames this as "the boundary is not the place forcing comes from." Precisely:
the CHANNEL (the E4 face response) is a BOUNDARY object, kinematically defined on the cell
faces; it is only its evaluating DATUM (R_record,L's form + kernel C) that is post-limit
bulk. The phi_H question decomposes into a LOCAL, per-cell, unsupplied datum (G3 = which
edges the per-cell holonomy reads; G3D :155-157, :209-214) AND a bulk datum (C). So "the
deciding datum is non-boundary-local" is CONFIRMED; "the boundary cannot detect / is not the
channel" is REFUTED (attack 1). The forcing, if it exists, runs THROUGH the boundary face
response but is CLOSED only by the bulk form — consistent with boundary-closure requiring the
bulk limit, not with the boundary being blind.

---

## 4. ATTACK 3 — CLAIM 3 (ONLY WRITTEN ROUTE IMPORTS GR): CONFIRMED

**Verified against S8.** The only WRITTEN inside/outside consistency condition (S8) is the
threshold/Kaluza-Klein route, and it consumes forbidden imports and trips a sealed falsifier,
exactly as the build reports:
- Quasilocal energy declared on both sides: C_R = 2 G E_R/(c^4 R_R), Misner-Sharp; C_R = 1
  adopted; the Brown-York/Misner-Sharp sqrt(2) ambiguity sealed (S8 §4(iii) :189-193).
- Consumes ell_P := kappa_5^2/(2 pi R_0) (declared-not-derived), Delta tau, G, c^4, the
  Lorentzian causal-diamond side (S8 :101-116, :179-193).
- Written as alpha-equality it assumes kappa_record = kappa_Thomson and FIRES A SEALED
  FALSIFIER (S8 §0 :24-36; N2 :207): "an adoption of the kappa identification wearing a
  condition's clothing."
- TOUCHES, DOES NOT CLOSE: three unknowns, two equations; branch-indexed weight; unbuilt
  matching maps (S8 §3 :139-168; N4 :210).

The connection-only agreement (interior-total vs exterior-flux via the face response) is NOT
written — its form is the SPEC GAP (S1 §3.3 :511-518; FORM G1). So the only WRITTEN route is
S8's import-laden one. **CONFIRMED.** (Caveat, for accuracy: S8 is a coupling-matching
agreement, not itself a phi_H-forcing; the build's use of it as "the only BUILT inside/outside
agreement, import-laden" is the correct reading.)

---

## 5. ATTACK 4 — PROVENANCE (BOTH DIRECTIONS): CLEAN

The build reaches UNDECIDED-GAP by structural typing only: (a) the split-FREE interior
premise (G3D/Q-1014); (b) H flux-free (FORM D1); (c) E1-E3/E6 winding-blind (T1B §5) and E5
winding-blind (S4); (d) naming R_record,L's form as the unbuilt/non-local deciding object;
(e) naming S8 as the import-laden written route. NONE of these CONSUMES a forbidden value:
- S8 quasilocal energy / ell_P / G_4 / c^2: cited only as "what a BUILT forcing would need"
  (build §5, §7, FORBIDDEN_IMPORTS flag :383-390) — named, not consumed. The GAP conclusion
  does not rest on any S8 numeric or on the KK matching.
- XSM beta, Omega_c: appear only in the fenced-out list (:387-389); not consumed.
- W faithfulness (|n|=1-via-injectivity): explicitly barred and unused (build §7 :313).
- No scale: 32/24/4! are exact structural integers or sealed quotations.
The CLAIM-1 defect I found is a MISCHARACTERIZATION of a sealed source (selective quotation
of S1 :534; conflation of argument-blindness with value-blindness), NOT a consumption of a
forbidden import. **PROVENANCE = CLEAN.**

---

## 6. NET ASSESSMENT

The build's top-line trichotomy label (NET = UNDECIDED-GAP) survives — but only on CORRECTED
grounds, and one load-bearing claim is broken:
- CLAIM 1 (structural blindness — the build's declared "pivot of the determination"):
  **REFUTED.** The derived face-response E4 is a connection-only boundary object that bears
  on phi_H (consumes the 32-dim assignment, S1 §2.4 flag 2; R_record,L carries an explicit
  phi_H block, FORM §4.2). Whether it forces phi_H is the UNDETERMINED materiality fork —
  not a structural "cannot detect." The corollary "sealed structure leans FREE" is
  unsupported.
- CLAIMS 2, 3, PROVENANCE: upheld (with the CLAIM-2 correction that the channel is a
  boundary object; only its form-datum is bulk).
Because the corrected phi_H-channel (face response ⇒ materiality fork ⇒ R_record,L's form)
routes to the SAME unbuilt object the build named (G1), the label UNDECIDED-GAP is not
overturned to FORCED or FREE. The build's ERROR is in the justification and the FREE-lean,
not the trichotomy bucket.

---

## 7. FLAG BLOCK

```text
BOUNDARY_BLIND_TO_PHI_H = REFUTED(the derived, connection-only, scale-free boundary object
  that BEARS ON phi_H is the E4 face-response mathcal_K_L: S1 §2.4 flag 2 [S1 :405-411] types
  it as a field that "consume[s] the assignment rather than the energy" — the assignment being
  the 32-dim H-freedom = phi_H; on the DOES-NOT-FACTOR branch [S1 §3.4 :535] equal-energy,
  H-differing configurations "give different responses", i.e. the response varies with phi_H;
  and R_record,L itself carries an explicit phi_H block and cross term [FORM §4.2 :310-315].
  The build's "flux-free ⇒ no boundary signature / reads im(d_1^dagger) ONLY / phi_H does not
  appear" [build §2 :95-101; §3.2 :157-169] rests on (i) quoting the FACTORS-branch line
  "the 32-dim freedom is invisible" [S1 :534] as if unconditional, when it is one horn of the
  UNDETERMINED materiality fork [S1 §3.4-3.5 :530-555], and (ii) conflating "mathcal_K_L's
  ARGUMENTS ⊥ H" [FORM §4.1 :293-295] with "the boundary VALUE is blind to phi_H". Whether the
  boundary sees phi_H IS the open fork, not a settled blindness.)

FORCING_OBJECT_NONLOCAL = CONFIRMED(the deciding DATUM — the FORM of R_record,L, post-
  thermodynamic-limit, plus the Gate-5 connected linked-cluster kernel C [FORM B1 :68-72,
  D5-D6 :233-282] — is genuinely non-boundary-local; T1B §5 DERIVES BOUNDARY_DATUM_LOCAL = no
  with both bulk dependences named [T1B :162-193, :352-361]; no boundary-local route exists in
  the sealed sources. CORRECTION carried: the phi_H CHANNEL is the E4 face response, a BOUNDARY
  object; only its evaluating form-datum is bulk. So "deciding datum is non-local" holds; the
  build's stronger framing "the boundary is not the place forcing comes from" does not — the
  boundary IS the channel, closed only by the bulk limit.)

ONLY_WRITTEN_ROUTE_IMPORTS_GR = CONFIRMED(the only WRITTEN inside/outside agreement is S8's
  threshold/KK consistency condition, which consumes quasilocal energy MS/BY, ell_P :=
  kappa_5^2/(2 pi R_0), G, c^4 [S8 :101-116, :179-193, §4(iii) :189-193] and, as alpha-
  equality, silently assumes kappa_record = kappa_Thomson and FIRES A SEALED FALSIFIER [S8 §0
  :24-36; N2 :207] — the Q-1093 failure. The connection-only agreement via the face response
  is NOT written; its form is the SPEC GAP [S1 §3.3 :511-518]. No written connection-only
  agreement reaches phi_H.)

PROVENANCE = CLEAN(no forbidden import consumed to reach UNDECIDED-GAP: S8 quasilocal energy /
  ell_P / G_4 / c^2, XSM beta, Omega_c, and W faithfulness are all NAMED as what a built
  forcing would need and fenced OUT [build §5, §7, FORBIDDEN_IMPORTS :383-390], never consumed;
  the GAP rests on structural typing only. The CLAIM-1 defect is a selective-quotation /
  argument-vs-value conflation of a sealed source, not an import.)

BOUNDARY_BUILD_VERDICT = REFUTED(CLAIM 1 — the "structurally blind to phi_H" pivot and its
  "sealed structure leans FREE" corollary — is false: the derived E4 face-response is a
  connection-only boundary object that consumes the 32-dim assignment (= phi_H) [S1 §2.4 flag
  2], with R_record,L carrying an explicit phi_H block [FORM §4.2]; boundary phi_H-visibility
  is the UNDETERMINED materiality fork, not a structural fact. The NET trichotomy LABEL
  UNDECIDED-GAP nonetheless SURVIVES on corrected grounds — the corrected phi_H-channel routes
  to the same unbuilt object the build named (R_record,L's form, G1), so the result is neither
  FORCED nor FREE — but the build's justification and its FREE-lean are refuted. CLAIMS 2, 3,
  and PROVENANCE are upheld, with the CLAIM-2 correction that the channel is a boundary object
  and only its form-datum is bulk.)

MACHINERY_USED_BY_ME = no(connection-only, symbolic; no fixed-point execution, no member
  binding, no numeric evaluation of n/kappa/alpha/coupling; quasilocal energy / ell_P / G_4 /
  c^2 touched only as machinery-as-suspect inside quoted sealed context, never leaned on as
  authority; the refutation stands on connection-only bedrock — S1 §2.4 flag 2, the S1
  materiality fork, and FORM §4.2's block structure; seals recomputed at path; no register/
  tracker/plan/road/ledger/lens read; no git action; output name probed ABSENT before write.)

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED.
```
