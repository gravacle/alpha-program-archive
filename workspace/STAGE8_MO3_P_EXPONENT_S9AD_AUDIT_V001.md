# STAGE 8 — MO-3 P-EXPONENT — ADVERSARIAL AUDIT V001

## AUDITOR — CODENAME MO3-AUDIT — COMMISSION S9AD

Date: 2026-08-14 (UTC 2026-08-15). Role: adversarial auditor, NOT told the
outcome, default REFUTE. Target: STAGE8_MO3_P_EXPONENT_S9AD_V001.md
(seal 9fdc3d1c… VERIFIED at path against sidecar, 45,120 bytes, before any
content reliance; content opened only AFTER the pre-verdict below was hashed).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. No git action. ONE output (this artifact) +
sidecar, both probed ABSENT at path before write.

---

## 0. VERDICT

**CONFIRMED-WITH-CORRECTIONS.**

**The core result stands under maximum hostility: for the sealed two-time
object, at first order in a_Delta, record-stratum zero, the unsandwiched
difference kernel's coincidence expansion is delta + Calderon-Zygmund at
degree -3, both leading coefficients nonzero on 0 < |x| < 1; hence p = -3 in
the sealed grammar (function part) plus a contact term outside the grammar;
hence the threshold p > 3/2 is REFUTED at the sealed polydisc-sup quantifier
and the coincidence-vanishing route to R-L2b / r-3 MO-3 / the wall's F1
escape closes NEGATIVELY. No sandwiched data was used to derive p (the named
kill is CLEAR — checked at the bytes of the sealed input). No equal-time
collapse. No numeric fit. The CAS battery re-ran verbatim, 28/28,
byte-identical output. Corrections C1-C3 below weaken the flag block's
universal per-pair quantifier and tag two asserted-at-standard-grade
regularity steps; none disturbs the threshold verdict.**

---

## 1. INDEPENDENCE PROTOCOL — PRE-VERDICT FIXED BEFORE OPENING

```text
Pre-verdict file: scratchpad MO3_AUDIT_PREVERDICT.md
  sha256 = 8546a38948f055edce6d166869c227a77da522283646e2f372ea74434fda2b28
  fixed 2026-08-15T03:41:19Z, BEFORE the build was opened (its seal was
  verified by hash only; no content read until after this timestamp).
Pre-verdict content: worked the problem from 218be86d + 29084cd2 alone;
  derived the threshold p > 3/2 independently (CAS B1: 2p - 4 > -1, with
  p = 3/2 divergent, 7/4 convergent, 5/4 divergent); fixed six kill
  conditions K1-K6 in advance; ranked expected outcomes with a DERIVED
  definite p as PRESUMED KILLED until every K cleared.
The build landed a DERIVED p = -3 — the presumed-killed class. Every K was
  then attacked individually (§3). All cleared; the presumption is overturned
  by verification, not by trust.
```

## 2. WHAT I VERIFIED MYSELF (auditor's own work, not the build's)

```text
SEALS: 15/15 of the build's §1 table recomputed at path THIS session — all
  match sidecars and the build's 8-hex prefixes. Build seal 9fdc3d1c matches
  its sidecar. The two commission-referenced artifacts (218be86d, 29084cd2)
  verified before the build was opened.
CITED BYTES (each read at path): PA :75-145 (J = -(Q b_D Q) tensor alpha_x;
  b_D = exp(16 - 1/s), all-orders boundary/tip vanishing, b_D(1/2,0) = 1;
  frozen pairs (7/100,-11/100), (13/100,4/100) => a_Delta = 18/100, 9/100
  EXACT — the build's values check); PA :181-182 (u_lambda(a) propagator);
  E1 :650-665 (V = u_mu(a_-)^dag u_lambda(a_+); polydisc quantifier),
  :686-700 (S2 two-time, alpha a symbol; S1 trace), :750-756 (V(0) = I
  exactly, surviving sector), :465-470 (D6' sharp localizers), :1054-1060 +
  :1105-1118 (Gevrey clauses); 18af3dee :161-190 (T_1 and the UNSANDWICHED
  T_1^{(0)} display — verbatim the build's input), :249-266 (b_tilde with
  transport sign declared "per band-flow convention"), :295-347 (survivor,
  degrees); 6a2aa0fd :137, :414-452 (EXPANSION_FIDELITY = CONFIRMED;
  SYMBOL/SANDWICH_RERUN = MATCH; SCOPE strengthening notes (i) polydisc
  separability, (ii) record strata outside the smooth class); d1807b96 :45-57
  (threshold Ground 1), :79 (delta-times-a-bump), :115 (the sharpened
  question); 52f2490b :24-25, :116 (LINK 6).
RE-DERIVATION (independent, before/during — not copied): the exact amplitude
  a(x,k) from T_1^{(0)} via plane-wave action and FT of b_D (reproduced the
  build's §3.1 display from scratch, including operator ordering); the
  four-block band split; the intraband transported-ray reduction to
  beta_s(x,k_hat) = int_0^1 b_D(t, x + s t k_hat) dt; Lemma 1 (Gaussian
  pairing kills the PV term and isolates the delta coefficient as the angular
  mean) and Lemma 2 (homogeneity + origin-support classification + FT
  injectivity) checked by hand as standard tempered-distribution theory; the
  Hessian-of-1/4pi r toy identity; the exceptional-set geometry (x = 0 rides
  the cone: s_- = 0; |x| >= 1 misses the open diamond); the rational witness
  arithmetic 7/80, 39/80, 273/6400 recomputed by hand; the shortfall
  arithmetic 3/2 - (-3) = 9/2.
CAS, fresh venv (sympy 1.14.0):
  (a) BUILD BATTERY VERBATIM: extracted from §10 (script sha256 44017583…),
      re-run: 28/28 PASS, output BYTE-IDENTICAL to the build's §10 transcript.
  (b) AUDITOR BATTERY (checks the build did NOT run):
      B1  threshold from BOTH sides (p = 3/2 diverges; 7/4 converges;
          5/4 diverges) — the build's F2a checked only a convergent instance.
      MY-3 the q-TRANSPORTED projector limit P_s(k+q) alpha_x P_s(k) ->
          s n_x P_s(n) along a generic exact-rational direction n = (1,2,2)/3
          with q = (1,-1,2) != 0 — the build's A3/A4 verified only q = 0;
          this is the load-bearing limit of §3.3. PASS both bands.
      MY-5 an ell = 1 CZ witness independent of the build's ell = 2 toy:
          FT[k_z/|k|] = i r_hat_z / (pi^2 r^3) != 0 off origin. PASS.
      MY-8 tip vanishing beats every power: lim_{t->0+} e^{16-1/t^2}/t^N = 0
          for N = 1, 5, 10, 25 — the boundary-term ground of the interband
          integration by parts. PASS.
      MY-B2 the BARRED sandwich conversion would land the same degree (-3);
          confirmed the build never runs it (direction of derivation checked
          at bytes: sealed unsandwiched input -> jet -> sandwich recovered).
SWEEPS: float/fit sweep of the build — all decimal-pattern hits are section
  references; "4.5 powers" is exact 9/2; no .evalf, no floats-as-ground, no
  pattern-read exponent. "Q-" occurrences: 3, all fence declarations.
```

## 3. THE KILL CONDITIONS, FIXED IN ADVANCE — SCORECARD

```text
K1 SANDWICH IMPORT (the named kill): CLEAR. The derivation's input is the
   sealed UNSANDWICHED operator T_1^{(0)} (18af3dee :186-187, read at bytes).
   The sandwich appears only downstream (§4.1), derived FROM the jet, and
   reproduces the sealed survivor -i a_Delta k_hat_x beta_- C(k) exactly
   (CAS B2, re-run). conversion_step_exists_in_corpus = false is respected:
   no conversion step is performed in either direction.
K2 EQUAL-TIME COLLAPSE: CLEAR. Two-time Duhamel object from line one; the
   equal-time one-power softening is typed as surrogate-class at its sealed
   grade (4971e273/5801681a/564b7040 consumed, not converted).
K3 ORBIT-ONLY SCALING (218be86d's bar): CLEAR. Nothing here derives a
   scaling weight; the derived object is a coincidence DEGREE, invariant
   under dilation, per-cell interior (RFA grammar). 218be86d bars orbit-only
   POSITIVE weight derivations; a shape-independent NEGATIVE (no vanishing
   at all, from the propagator structure and a multiplication-operator
   vertex, for any cell with nonempty interior) transfers to slivers a
   fortiori. The §6 claim is sound though its "every diamond is isotropic"
   sentence is charter paraphrase, not load-bearing (the degree argument
   carries it).
K4 INTERCHANGES: five inventoried, each attacked. (a) Fubini on compactly
   supported smooth b_D: valid. (b) a_0 extraction: transported projector
   limit CAS-verified at q != 0 (MY-3); phase correction |q|^2/|k| bound
   elementary. (c) interband integration by parts: boundary terms vanish
   (MY-8), q-uniformity via Schwartz constants. (d) symbol-to-kernel FT:
   Lemmas 1-2 verified by hand + MY-5 instance. (e) the a-order separation:
   DEFECTIVE AS WORDED — see C1; repaired by the sealed polydisc-sup
   quantifier, under which the verdict is unconditional.
K5 PATTERN-READ p: CLEAR. p = -3 is degree counting (FT of a degree-0
   homogeneous symbol in R^3), an exact structural statement.
K6 SINGLE-p DISCIPLINE: CLEAR. One p, derived once; no p_effective.
```

## 4. CORRECTIONS (the "WITH-CORRECTIONS")

```text
C1 (PRINCIPAL — quantifier overclaim in §8 and the flag block). The sentence
   "The coincidence order of the full difference is that of its lowest
   nonvanishing a-order" is NOT a theorem at a FIXED pair: at a specific
   a_Delta, higher-order-in-a jets (which are smooth-class, unlike the record
   strata) could in principle cancel the linear jet — order separation only
   forbids cancellation over a CONTINUUM of pairs. What the derivation
   actually establishes: (i) the a-linear coefficient's jet, EXACT; (ii) by
   the sealed polydisc quantifier (E1 :686-689; 6a2aa0fd SCOPE note (i)) and
   the Cauchy coefficient formula, NO uniform coincidence vanishing on the
   polydisc is possible — so the sup-quantified S2/R-L2b certification via
   coincidence vanishing is dead REGARDLESS of any exceptional pair set;
   (iii) p = -3 pointwise for all pairs outside an at-worst-discrete
   exceptional set. The flag's "per CTP pair ... with a_Delta != 0 (both
   sealed frozen pairs qualify)" must be weakened to (ii)+(iii): the frozen
   pairs are not individually certified against an exact all-orders
   cancellation (nothing sealed supplies one; none was derived). THE
   THRESHOLD REFUTATION AND THE F1 NEGATIVE CLOSURE ARE UNAFFECTED — they
   need only (ii).
C2 (grade tag). a_{-1} in S^{-1} (kernel O(r^-2)) and the interband
   C-infinity kernel are asserted with standard-machinery outlines (symbol
   derivative gains not proven line-by-line). They carry only subleading
   orders; the leading delta + CZ jet rests on exact steps. Recorded; no
   verdict change.
C3 (headline compression). §0's "EXACT" is exact AT COMMISSION SCOPE: first
   order in a_Delta, record-stratum zero (non-cancellation CONSUMED at
   6a2aa0fd SCOPE (ii)'s sealed grade), Galerkin Q transparent (the TT
   pair's declared idealization). The flag block declares all three (CL-3,
   CL-4); the §0 headline does not. Read §0 with §8.
VERIFIED REFINEMENT (not a defect): the build's per-band beta_+/beta_-
   refines the sealed single-b_tilde symbol display; legitimate because
   18af3dee :266 declares the transport sign "per band-flow convention", and
   the sealed SANDWICHED survivor (the confirmed object) is unchanged. The
   unsandwiched jet acquires an I-component the sealed display lacked —
   correctly derived, consistent with the sealed convention caveat.
```

## 5. CHOICE LEDGER, TOY SEPARATION, SWEEP CUTOFF (auditor's own)

```text
CHOICE LEDGER:
 A-1 Attacked the build's §10 battery by verbatim re-run rather than
     rewrite — blast radius: none (byte-identical output required and got).
 A-2 Chose exact-rational direction (1,2,2)/3 and q = (1,-1,2) for MY-3 —
     any generic exact direction serves; no tuning (first choice tried).
 A-3 Treated 6a2aa0fd SCOPE notes as sealed-grade consumables (they are in
     a CONFIRMED sealed artifact) — same grade the build assigned.
 A-4 Did not attempt an independent second-order-in-a jet computation:
     C1 shows the verdict does not need it; recorded as the successor's
     natural target if the per-pair quantifier is ever needed.
TOY SEPARATION: my MY-5 (ell = 1 witness) and the build's ell = 2 display
  are worked instances only; every sealed-object claim audited rests on the
  general lemmas + ray witness, both independently verified.
SWEEP CUTOFF (declared): roots swept = alpha-program-archive/workspace,
  /supervision, /relay_inbox+outbox listings, and alpha_supervision listing;
  searches: "MO3|S9AD", the two commission hash prefixes, targeted byte-span
  reads listed in §2, float/fit/Q- sweeps of the build. NOT swept: full
  corpus; cleanroom_output; no fenced-class file OPENED — one bounded grep
  for "MO3" over supervision returned a single file-path pointer line in
  THE_HANDOFF_2026-08-15 (path only, consumed for locating the commission;
  no verdict content read; declared here for the record). No register /
  road / ledger / tracker / plan / lens content read. "Q-..." tokens not
  chased. Enumeration claims are limited to these sweeps.
```

## 6. FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS(target = STAGE8_MO3_P_EXPONENT_
  S9AD_V001.md, seal 9fdc3d1c verified at path; corrections C1 (quantifier:
  per-pair p = -3 weakened to polydisc-sup + generic-pair; frozen-pair
  pointwise instance not individually certified), C2 (S^{-1}/interband
  regularity at standard-machinery grade, subleading only), C3 (headline
  scope compression); the DERIVED core — delta + CZ degree -3 jet, both
  leading coefficients nonzero on 0 < |x| < 1, p = -3 function part +
  contact term, threshold p > 3/2 REFUTED at the sealed sup quantifier,
  coincidence-vanishing route to R-L2b/MO-3/F1 CLOSED NEGATIVELY — SURVIVES
  maximum hostility.)
INDEPENDENCE = KEPT(pre-verdict sha256 8546a389… fixed 2026-08-15T03:41:19Z
  before the build was opened; the build's DERIVED-definite-p landing was my
  pre-declared presumed-killed class; presumption overturned by the K1-K6
  scorecard, each cleared affirmatively, not by trust.)
NAMED_KILL_SANDWICH_IMPORT = CLEAR(input verified UNSANDWICHED at sealed
  bytes 18af3dee :186-187; sandwich derived downstream and matches the
  sealed survivor; no grading conversion performed in either direction.)
CAS = VERBATIM_MATCH(28/28, fresh venv, sympy 1.14.0, output byte-identical;
  auditor battery: B1 two-sided threshold, MY-3 q != 0 transported projector
  limit, MY-5 ell = 1 CZ witness, MY-8 tip vanishing, all PASS; script
  sha256s recorded in §2.)
NUMERIC_FITTING = NONE(exponent from exact degree counting; float/fit sweep
  clean; every displayed rational exact.)
SEALS = 15/15 + target + 218be86d + 29084cd2 verified at path this session.
CONSUMER_NOTE = R-L2b's four discharges, r-3, and the wall may consume the
  NEGATIVE closure (no coincidence-vanishing route) at the polydisc-sup
  quantifier; any consumer needing pointwise p at the frozen pairs must
  first close C1's exceptional-set gap.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
production_authorized = false
```
