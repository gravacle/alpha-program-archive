# STAGE 8 TASK 5 / EQ6 — COMPLETED-EXISTENCE AXIOM — AUTHOR ARM — LANE 2 V001

Date: 2026-08-04  
Lane: Codex Lane 2  
Task: PASTE 527 / Task 5 / EQ6  
Custody: author arm; full gate; DoR-020-A2 reserved

## Lead result

```text
COMPLETED_EXISTENCE_AXIOM = PROPOSED_NOT_ADOPTED
LIVE_FORM = JOINT_RELATIVE_EXISTENCE_WITHOUT_UNIQUENESS
DOMAIN = ACTUAL_FINITE_COHERENT_DOR020_DATA
OUTPUT = NONEMPTY_PROOF_CARRYING_COVARIANT_JOINT_FAMILY
JOINT_EQUALIZER = ONE J1-J15 TERM OVER ALL SIX GENERATORS
FINITE_CONSERVATIVITY = BUILT_IN_AND_PROVED_RELATIVELY
C_RET = NOT_IMPLIED; ITS INDEPENDENT CERTIFICATION REMAINS
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
ALPHA_COMPUTED = false
PROOF_AUTHORIZED = false
KAPPA_RECORD_COMPUTED = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
MACHINERY_APPEAL = false
```

The proposed axiom is a guarded completion principle. Given one already
existing, actual-surface, finite-coherent datum satisfying the qualifications
below, it supplies a nonempty covariant family of completed realizations whose
finite reduct is exactly that datum. It does not assert that arbitrary formal
diagrams are completable, does not choose a realization, and does not assert
uniqueness.

The six generators are completed **jointly**. Six unrelated existential
statements do not satisfy the axiom. This is the load-bearing response to the
permanent equalizer regression.

## 0. Preflight, authorities, and symbol discipline

### 0.1 Three-line preflight

```text
DOES THE OBJECT EXIST?   no; C61 commissions this author-arm proposal.
IS THE VERSION CURRENT?  yes; register head Q-452 verified in the supervisory register.
ARE ITS INPUTS PRESENT?  yes; all named artifacts and decisions below were hash-verified.
PREFLIGHT = PASS
```

### 0.2 Hash-verified authorities

| Authority | SHA-256 | Use |
|---|---|---|
| DoR-020 conditional ratification | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | six-generator joint premise and escrow rules |
| DoR-020-A1 where-clause amendment | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | adopted path/current and bundle-field laws |
| continuum constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | exact J1–J15 equalizer |
| six-generator bedrock theorem | `288a3d4147cb10f2e51180b21f7c6b2b4749503504c57b8a1121e3925c70c94c` | irreducibility and dependency graph |
| pass-1 finite subpackages | `452f1bb87aeb7f7bfe4ab4556134cf723ab6df697f93c1a59c33671146cb0083` | finite/orbitwise cores |
| flip equalizer construction | `a78c2450985429f6c9e152eb52ab6f133b013e1a1b1d695e2cc5241617b4bdb4` | an actual inhabited finite-coherent sector |
| five-generator scoped discharge | `3ae580d05886bfa0df23442a3d7eaf5a4500c0280229cc615f6998f29a93be8e` | common finite/completed boundary at Q-452 |
| conditional chain V002 | `1069e4f442ebfd083944c7cf6df8ba04058c531846fa61e1b6452d7ba551a269` | independent `C_ret` and twelve-step map |
| adopted where-clause text | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | actual surface/bundle eligibility |

The Q-452 register entry was read. It records that all five remaining
generators build at the finite/local level and stop at completed closure;
the pre-existing `B_Q408_REFINEMENT` flip term is the sixth finite input.
This proposal cites that evidence and does not promote it to a derivation.

### 0.3 Bearing symbol distinctions

```text
finite coherent       != completed;
stagewise inhabited   != jointly inhabited;
completed existence   != a selected completed member;
existence             != uniqueness;
actual Q-408 kernel   != an abstract isomorphic kernel;
finite faithfulness   != completed faithfulness;
completion axiom      != stationary-return certificate C_ret;
surface geometry      != categorical rails;
```

Let `Fin_020` be the category of qualifying finite data defined below. Let
`Comp_020(F)` be the groupoid of completed realizations of `F`, with
isomorphisms required to preserve all adopted structures. Let

```text
Res_fin : Comp_020(F) -> {F}
```

denote the full finite-restriction family, not one selected stage.

## 1. R1 — the proposed completed-existence axiom

### 1.1 The finite datum

A candidate finite datum `F` is indexed by an actual admitted refinement
subcategory `I_F` and contains, at every object and arrow of `I_F`, one
simultaneous tuple

```text
F = (
  I_fin, Q408_fin, C1_core, Faith_fin, C2_fin, C3_fin,
  reader_fin, action_fin, density_fin, boundary_fin,
  restrictions, refinements, common-refinement diamonds
).
```

These entries are the already-built finite kinds:

- retained R1 action/Hessian sections and their exact finite shadows;
- the actual Q-408 current/kernel maps, not stand-ins;
- the finite seminorm and initial-topology C1 core;
- finite and orbitwise kernel separation/faithfulness data;
- finite response transport, contact and Ward data;
- finite local-symbol/Hodge–Maxwell input data;
- the DoR-020-A1 path/current correspondence and U(1)-bundle field members;
- all restriction, reality, batching, unit, support, and refinement proofs.

No completed carrier, completed section, cofinal element, global Hodge
projection, or completed response boundary may occur in `F` merely to make
the axiom applicable. Such an occurrence is the circularity falsifier.

### 1.2 Qualification: `FiniteCoherent_020(F)`

`F` qualifies exactly when the following failure-capable certificate is
present.

| ID | Finite-coherence condition |
|---|---|
| FC1 actuality | Every path, current, kernel, connection and test object is anchored in the ratified incidence/cycle/refinement surface; Q-408 maps are used extensionally, not replaced by an abstract isomorphic kernel. |
| FC2 finite bottom | Every active restriction reproduces the sealed DoR-008 finite result exactly, including the Gate 1–4, Q-243, Q-279 and Q-309 shadows where defined. |
| FC3 functoriality | Identity and composition laws hold for the actual restriction/refinement arrows, and every built common-refinement diamond commutes. |
| FC4 contravariant honesty | Cycle-creating arrows have only the proved downward/old-image restrictions; no representative-independent upward quotient map is invented. |
| FC5 covariance | Relabelings, edge exchanges, orientation reversal with sign action, reality, ordered batching and W3 restrictions carry the whole tuple covariantly. |
| FC6 surface support | `OLD_FID`, relative no-leakage (`RNL`), local range (`LR`), and local orthogonal excision hold on their proved finite scopes; disjoint new-cycle sectors are not mixed. |
| FC7 bundle discipline | The U(1) lift, pullback-bundle isomorphism, smooth full-rank condition, characteristic-class compatibility, and density/coframe unit laws hold on every finite arrow. |
| FC8 analytic core | The declared seminorm cores are Hausdorff on the physical quotient; stage maps are bounded on their finite domains and use the one adopted `tau_ker`/DoR-019 unit classes. This does not presuppose a global uniform bound. |
| FC9 finite kernel honesty | The finite Ward/contact split is exact; a claimed faithful horn has zero finite kernel, while a quotient horn exposes and annihilates its finite kernel without deleting a record-visible cycle. |
| FC10 joint compatibility | The finite versions of J1–J15 hold on one tuple. Passing the rows on different tuples does not qualify. |
| FC11 equalizer coherence | On overlaps and common refinements, the six generator components agree as one finite fiber-product term, including the contact cocycle. |
| FC12 return admissibility | If the datum carries a stationary-return branch, its scalar domain is connected/interval-convex or carries the stated absolute-continuity certificate. A disconnected branch is not admissible. This condition does not construct `C_ret`. |
| FC13 target blindness | No object, map, topology, normalization, support, or branch was chosen from a response, threshold, fixed-point, end-test, or numerical consequence. |

The current finite stock supplies the kinds in FC1–FC13 on its proved
finite/`Ref_path`/flip scopes (`452f1bb8…`, `a78c2450…`, `3ae580d0…`).
DoR-020-A2 would be choosing the principle that such a coherent actual
finite system has a joint completed realization. The axiom is therefore new
physics/analysis at the completed-existence stratum; it is not a hidden
claim that Q-452 already proved the closures.

### 1.3 Completed realization

A completed realization of `F` is one proof-carrying tuple

```text
W = (
  I_bar, Q408_bar, C1_bar, Faith_bar, C2_bar, C3_bar;
  ell_bar, A_bar, GRef_bar, Dens_bar, Eta_bar, Bound_bar;
  rho_bar, Hodge_Maxwell_bar, Cof_bar;
  c_J1, ..., c_J15
)
```

with all of the following properties.

1. **Completed carriers.** The DoR-019 carriers and the adopted `tau_ker`
   closures exist; no alternative completion topology or cross-sector unit
   is inserted.
2. **Natural sections.** `I_bar` and `Q408_bar` extend the retained/action
   and physical current/kernel families over all admitted arrows, without a
   realization, orientation, edge, frame, filtration, rank, or basis choice.
3. **C1 closure.** The finite seminorm maps admit family-continuous completed
   extensions and the required uniform estimates on the completed family.
4. **Faithfulness horn.** Either the completed kernel is separated by the
   required lower certificate or a safe covariant quotient is supplied with
   the exact reader/localization annihilator. The horn is part of `W`, not a
   Boolean selected in advance.
5. **Response boundary.** `Eta_bar` is response-natural on common
   refinements; `beta` is a covariant cocycle; contact is closed, invariant,
   restriction-natural and subextensive without swallowing a visible bulk
   cycle.
6. **Hodge–Maxwell closure.** The completed local symbol, Ward/contact split,
   closed-range/functional-calculus data, Maxwell summand and normalization
   exist with R4-only units.
7. **Cofinality and factor completeness.** The admitted refinement family is
   cofinal for every sealed finite consumer, every finite bottom factors
   through it, and common-refinement comparisons are independent of route.
8. **Exact restriction.** `Res_fin(W)=F` componentwise and diagramwise.
9. **Jointness.** The certificates `c_J1,...,c_J15` are witnessed by this one
   tuple. They cannot be assembled from incompatible component witnesses.

The six named completed generator projections of `W` are exactly

```text
B_R1_NATURAL,
B_Q408_REFINEMENT,
B_C1_COMPLETION,
B_FAITHFULNESS,
B_C2_RESPONSE_BOUNDARY,
B_C3_MAXWELL_HODGE.
```

### 1.4 The axiom, exact form

```text
COMPLETED_EXISTENCE_020:

  for every F,
    FiniteCoherent_020(F)
      -> NonemptyCovariantFamily(
           { W in Comp_020(F) :
               Res_fin(W)=F
               and Eq_J1-J15(W)
               and SixGeneratorScope(W) }
         ).
```

`NonemptyCovariantFamily` means a proof-carrying family/groupoid closed
under all admitted automorphisms, not an instruction to choose a member.
The output itself carries the equality and certificate proofs. Thus the
axiom yields joint equalizer inhabitance, not six separate nonemptiness
claims.

### 1.5 What the axiom does not say

- It does not assert uniqueness, contractibility, a least norm member, or a
  canonical representative.
- It does not bind the DoR-020 computation member.
- It does not construct or certify `C_ret`; FC12 only prevents an already
  supplied return branch from being disconnected.
- It does not prove a contraction, execute a fixed point, run an end test, or
  authorize evaluation.
- It does not add a seventh generator, a new unit, a topology, or finite
  dynamics.
- It is conditional on actual qualifying data; it does not assert that every
  formal rail diagram qualifies.

## 2. R2 — exclusions built into the domain and output

### 2.1 Disconnected stationary-return domain

For the permanent counterexample

```text
D={-1,1},
b(K)=K^3/2-3K/2,
b(D)=D,
b'(-1)=b'(1)=0,
```

the derivative report gives zero while the true two-point modulus is one and
there is no fixed point. This datum fails FC12: `D` is neither an interval nor
convex, and no absolute-continuity branch joins its points. Therefore it is
outside the axiom's domain. The axiom cannot manufacture `C_ret` or a
completed return object for it.

### 2.2 Disjoint-sector Riesz mixer

Let `P_mix` satisfy all formal restriction squares while sending an old-cycle
vector with support in `S_1` to a component in a disjoint new-cycle sector
`S_2`. Then

```text
1_(S_2) P_mix 1_(S_1) != 0.
```

It fails FC6 (`RNL` and local range), independently of rail commutation.
Consequently no finite datum containing `P_mix` qualifies, and the completed
family cannot contain it. The tolerated `P=id` witness remains admitted: a
pre-existing metric cross term is not leakage created by transport.

### 2.3 Abstract-kernel stand-in

An abstract kernel `K_abs` with the same algebraic dimensions as the physical
Q-408 kernel does not meet FC1. Qualification requires the actual map and its
surface anchors, bundle field, test forms, support map, and finite Ward data.
An unproved isomorphism `K_abs ≅ K_Q408` cannot fill those fields. Hence the
abstract-kernel witness killed at Q-415 remains excluded.

### 2.4 Other permanent exclusions

The qualification/output pair also rejects:

- a circular `pi_Mx` defined from the desired reader (FC1, FC10);
- a declaration that an empty reader/member family is inhabited (the axiom
  only applies after an actual `F` is supplied);
- Hodge–Maxwell structure inferred from isometry alone (items 3 and 6 of the
  completed output remain substantive);
- objectwise least members that fail restriction naturality (the output is a
  family, not minima);
- any six separately inhabited fibers with empty J1–J15 equalizer (item 9);
- an upward cycle-creating quotient or compression that deletes a new-cycle
  kernel (FC4 and exact restriction);
- a bundle lift with wrong characteristic class or rank (FC7);
- a completed kernel that hides an infinite-support null vector while
  claiming the faithful horn (completed output item 4).

## 3. R3 — finite conservativity

### 3.1 Exact finite-reduct theorem

Let `L_fin` be the language of the ratified finite objects, arrows, and
observables. Let `L_comp` add only the completed sorts and the maps in `W`.
For every qualifying `F` and every `W` supplied by the axiom,

```text
Res_N(W)=F_N                    for every finite stage N,
Res_f(W)=F_f                    for every finite arrow f,
Obs_fin(W)|_N=Obs_fin(F_N)      for every sealed finite consumer.
```

This follows directly from completed-realization item 8 and FC2. It is an
equality, not convergence or approximation.

### 3.2 No-new-finite-content corollary

The axiom is guarded by an already supplied `F` and creates only an
`L_comp` expansion whose `L_fin` reduct is definitionally `F`. It adds no new
finite object, arrow, coefficient, source, kernel value, or finite equation.
For a sentence `phi` involving only one finite reduct,

```text
if phi follows from the chosen W by finite restriction,
then phi already follows from F.
```

Proof: replace every completed symbol in the derivation by its restriction;
item 8 turns every resulting premise into the corresponding premise of `F`.
Any failure of this replacement is a finite-conservativity falsifier and
voids the candidate axiom.

This is **relative** conservativity. The axiom adds the global assertion that
a joint completed realization exists; that assertion is deliberately not a
finite theorem. It may constrain which infinite/global families are physical
without changing a sealed finite result.

### 3.3 DoR-008 falsifier

For the named finite datum formed from the built layers, the restriction
checks are:

```text
one-edge/tree: cycle quotient remains zero;
reciprocal loop: actual Q-408 finite current/kernel and Ward result unchanged;
S8-A: all record-visible cycle directions and the exchange action are retained;
cycle creation: old-image restriction commutes and the new-cycle kernel remains;
Q-243/Q-279/Q-309: sealed finite retarded/Ward/contact shadows reproduce exactly.
```

A mismatch on any one row refutes the proposed axiom instance immediately;
no end test is needed.

## 4. R4 — full gate

### 4.1 Choice table

| Field | Live candidate | Genuine alternatives considered | Why live candidate is minimal | Void condition |
|---|---|---|---|---|
| quantifier | guarded over actual `FiniteCoherent_020(F)` | all formal diagrams; only the named flip datum | all-formal admits stand-ins; named-only selects a scope | an abstract or non-surface datum qualifies |
| completion form | one joint six-generator family | six stagewise/separate existences | separate existence fails the equalizer regression | output lacks one J1–J15 term |
| strength | existence without uniqueness | unique completion; contractible completion groupoid | no current theorem removes the flat/automorphism family | proof silently chooses a member or normalizer |
| witness form | nonempty covariant proof-carrying family | a distinguished member; bare propositional nonemptiness | preserves no-selection while carrying certificates | member, basis, orientation, filtration, rank, or frame selected |
| topology/units | adopted DoR-019 carriers, `tau_ker`, R4 seam | a new completion topology; bare C/K conversion | adds no metric or unit physics | topology switch or implicit unit appears |
| finite relation | exact reduct equality | asymptotic or approximate recovery | DoR-008 requires exact shadows | any sealed finite mismatch |
| faithfulness | certified faithful horn or certified safe quotient horn | faithful by declaration; quotient deleting visible cycles | exactly J14's forced dichotomy | hidden completed null vector or visible-cycle deletion |
| return scope | independent `C_ret`; disconnected branches excluded from eligibility | completion implies return; arbitrary disconnected return | does not bill a separate certificate to EQ6 | axiom is used as `C_ret` or accepts the Q-448 domain |
| cofinality | output carries factor-complete cofinal proof | stagewise closure only | Q-451/Q-452 show scope alone is insufficient | a sealed finite consumer does not factor |

### 4.2 Minimality theorem

Removing any of the following changes the proposition materially:

- remove jointness: componentwise-nonempty/empty-equalizer countermodel enters;
- remove actuality/support: abstract kernels or sector mixers enter;
- remove exact finite reduct: DoR-008 loses its falsifier;
- remove cofinality: the `I_flip` scope remains non-package-complete;
- remove the completed analytic fields: one of the five Q-452 closures remains
  absent;
- add uniqueness: a new selection principle is authored without evidence.

Therefore joint relative existence is the weakest form that answers C61's
commission and yields the six-generator joint inhabitance it names.

### 4.3 Void conditions

The proposal is void if any of these occurs:

1. a qualifying finite datum fails an adopted finite law;
2. an output changes a sealed finite result;
3. a completed field is defined using the response/threshold/end result it
   later controls;
4. six independent witnesses replace one joint tuple;
5. a member, orientation, rank, frame, basis, filtration, or contraction is
   selected;
6. the axiom is invoked on the disconnected-domain, sector-mixer, or
   abstract-kernel counterexample;
7. the output uses a new topology, scale, unit conversion, or finite source;
8. uniqueness is inferred from existence;
9. the principle is used to execute any DoR-020 escrowed operation without
   the required checkpoint and separate authorization.

### 4.4 Falsifier ladder

| Level | Test now or later | Failure means |
|---|---|---|
| F0 typing | actual carriers and all domains/codomains | proposal ill-typed |
| F1 finite | DoR-008 exact restrictions | axiom instance refuted immediately |
| F2 geometry | support, bundle, cycle-creation, covariance regressions | candidate datum ineligible or axiom output invalid |
| F3 joint | one W satisfies J1–J15 simultaneously | separate-witness substitution refuted |
| F4 conservativity | every finite consequence reduces to `F` | axiom too strong / void |
| F5 completion | exhibit a qualifying `F` for which no allowed W exists | completed-existence axiom refuted |
| F6 downstream structure | conditional C1–C3, symbol and return interfaces commute on W | the instantiated package or a named downstream theorem refuted |
| F7 final sealed test | after witness certification, member binding, A32 and evaluation authorization only | a wrong result refutes the bound package instance; it refutes the whole family only with a separate rigidity theorem |

The final-test semantics retain DoR-020's discipline: existence alone does
not bind the family, and disagreement of one later bound member does not
logically refute every member without uniformity/rigidity.

### 4.5 Twelve-step map consumption

Against chain V002 (`1069e4f4…`):

| Step | Effect of adopted A2 candidate |
|---|---|
| 1 joint EQ6 verification | discharged propositionally and proof-carryingly for each qualifying `F`; checkpoint certification still required before escrow opens |
| 2 finite/rail regressions | not replaced; FC1–FC13 and the finite certificates are its inputs |
| 3 construct `C_ret` | **not discharged** |
| 4 certify `C_ret` and branch scope | **not discharged** |
| 5 boundedness estimate | remains `[EQ6]+C_ret` conditional |
| 6 return closure | remains `[EQ6]+C_ret` conditional |
| 7 branch completeness | remains `C_ret_SCOPE` conditional |
| 8–10 derivative, modulus, threshold | made structurally consumable after Steps 3–7; not executed or decided here |
| 11 fixed-point consequence | still theorem-only and escrowed; not executed |
| 12 sensitivity/evaluation ladder | unchanged; A32 and evaluation authorization remain required |

Thus the axiom removes the joint completed-existence obstruction and no
other authorization gate.

## 5. R5 — hostile battery

| Attack | Execution | Verdict |
|---|---|---|
| A1 triviality / proves too much | Feed a merely commuting abstract diagram. FC1/FC6/FC7 reject it before the existential conclusion. | **PASS** |
| A2 circularity | Search FC1–FC13 for `W`, completed Hodge data, or cofinality. None is a premise; only finite cores and their certificates occur. | **PASS** |
| A3 componentwise equalizer | Give six nonempty fibers with incompatible shared scalar. Item 9 requires one tuple, so no W exists. | **PASS** |
| A4 disconnected return | Use `D={-1,1}` and the Q-448 polynomial. FC12 fails; no application. | **PASS** |
| A5 sector mixer | Use the square-commuting Riesz mixer. FC6 fails by `1_(S2)P_mix1_(S1) != 0`. | **PASS** |
| A6 abstract kernel | Substitute an isomorphic algebraic kernel. FC1 lacks the actual Q-408 anchors and Ward data. | **PASS** |
| A7 target tuning | Search the choice table and construction order for response, modulus, threshold, fixed point, end value, or measured datum. None justifies a clause. | **PASS** |
| A8 hidden selection/uniqueness | Apply an automorphism exchanging rank-two cycles. The output is closed under the action and selects neither orbit member. | **PASS** |
| A9 finite/completed faithfulness | Insert an infinite-support completed null vector over faithful finite restrictions. Output item 4 rejects the faithful horn unless a lower certificate exists. | **PASS** |
| A10 cycle-creation compression | Compress away the new-cycle test form while retaining the physical target kernel. FC4 and exact restriction fail. | **PASS** |
| A11 rail-only completion | Supply categorical diamonds with no surface support or bundle anchors. FC1, FC6 and FC7 fail. | **PASS** |
| A12 fresh topology-switch attack | Complete C1 in `tau_1` and take boundary/Hodge closure in inequivalent `tau_2`. Output item 1 requires the single adopted `tau_ker`; tuple rejected. | **PASS** |

### 5.1 Nine cumulative geometric regressions

| Regression | Result under the candidate |
|---|---|
| pure new-cycle profile | retained outside the old-image pullback |
| covariance orbit/moduli | full covariant family retained; no representative chosen |
| all-stage skeleton | finite skeleton is not mislabeled a completed realization |
| rank-preserving `Ref_path` | identity/current/kernel square reproduced exactly |
| cycle-creating upward quotient | none created |
| arbitrary-profile restriction | scoped to the proved old-image restriction; target kernel survives |
| Q-430 old-to-new mixer | rejected by FC6 |
| Q-432 `P=id` witness | admitted; relative no-leakage does not ban inherited cross terms |
| Q-435 bundle/topology witness | rejected if lift, rank, or characteristic class mismatches |

The permanent abstract-kernel, circular-reader, false-nonemptiness,
Hodge-from-isometry, objectwise-selection, and joint-equalizer attacks remain
binding and pass for the reasons above.

### 5.2 Construction-order / anti-tuning ledger

```text
1  Freeze DoR-008, DoR-019, DoR-020+A1, J1-J15, and the counterexamples.
2  Freeze the actual finite carrier and surface/bundle anchors.
3  Define FiniteCoherent_020 before naming any completed output.
4  Define completed-realization fields from the six generator interfaces.
5  Require exact finite reduct and jointness.
6  Choose existence-only strength; explicitly decline uniqueness.
7  Type C_ret as independent and preserve the twelve-step order.
8  Run the exclusions, conservativity proof, regressions, and battery.
9  Only afterward state the neutral downstream consequence map.
```

No response support, threshold behavior, fixed-point property, end-test
value, numerical value, or comparison was used at any construction step.

## 6. Surface geometry versus rails

**Surface geometry.** FC1, FC4, FC6 and FC7 require actual incidence/cycle
support, the physical Q-408 kernel, path/current correspondence, local test
support and the U(1) bundle/characteristic-class data. These are the finite
physical raw materials.

**Rails.** The category, equalizer notation, completion functor, proof-family
packaging and certificate shapes organize those materials. They cannot
create surface anchors, support locality, bundle classes, or finite kernels.

**Axiom cost.** The new premise is not a rail-generated primitive generator.
It is the global existence claim that the actual finite-coherent physical
data have a compatible completed realization. This is precisely the
finite/completed boundary stabilized at Q-452. If the full gate rejects that
claim, the finite results stand unchanged and `[EQ6]` remains open.

## 7. Final board

```text
COMPLETED_EXISTENCE_AXIOM_V001 = PROPOSED_NOT_ADOPTED
PROPOSAL_FOR = DOR_020_A2

AXIOM_FORM = GUARDED_JOINT_RELATIVE_EXISTENCE
UNIQUENESS = NOT_ASSERTED
MEMBER_SELECTION = NONE
JOINT_SIX_GENERATOR_OUTPUT = REQUIRED
J1_J15_EQUALIZER = REQUIRED_ON_ONE_TUPLE
EXACT_FINITE_REDUCT = REQUIRED
COFINALITY_FACTOR_COMPLETENESS = OUTPUT_CERTIFICATE
C_RET = INDEPENDENT_TYPE_U_GATE

DISCONNECTED_DOMAIN = EXCLUDED_BY_FC12
SECTOR_MIXER = EXCLUDED_BY_FC6
ABSTRACT_KERNEL = EXCLUDED_BY_FC1
NINE_GEOMETRIC_REGRESSIONS = PASS_WITH_STATED_SCOPES
FINITE_CONSERVATIVITY = PROVED_RELATIVELY

READY_FOR_CROSS_REVIEW = yes
READY_FOR_RATIFICATION = no_cross_review_pending

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

STOP: lane action ends after sealing, byte-identical mirroring, hash
verification, and hash report. No register, plan, tracker, git, commit, push,
member binding, fixed-point execution, end test, or evaluation action is
authorized by this artifact.
