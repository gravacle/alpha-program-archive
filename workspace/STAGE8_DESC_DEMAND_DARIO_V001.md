CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 10 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = DEMAND-SIDE READING ONLY (no construction, no candidate, no supply call)
DIGEST_RULE = every pinned digest computed from its file at seal time
CLOSURE_END_BYTE = 4192
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_986_DESCENT_DEMAND_DARIO_V001.md` | `a9fd1bcd85a85b8343d92979b687db0f4b2cd77418840d4dfcd696aceee48c4c` | assignment |
| 02 | `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md`; demand `[2044,6922)`; unpacking `[3001,5172)`; cited window `[6923,8531)`; Finding `[8533,9079)` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874`; `58ddd239b70e190b00813aa88c3bb4139cd480fb5dd6b93aef869e05ea3faf15`; `394b14cd7dc72775136ea2aeaceb1feeaddea5935c2538a1b48af8add3855801`; `f3d413e2804c19f10c0a155e0a3011a10973420e9c9e85ccd3d1a6365e7dece8`; `412e608e4378b6c92a44d434d9c6a0876081b87b915edc5d4d4f04fd26cb5281` | **the P5 requirement, and the citation this relay right-closes** |
| 03 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md`; port `[10824,11928)`; C6 `[23900,24472)`; B0 citation `[3916,4671)` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab`; `63f743bfcc1df70bccb8cf1c0e3041c7a9202009c944c33adbfe8ae75da3535e`; `19d3ca3fe6573d2714e08e3d2bec05ef6ec2a52547cccb7d45dc835dfd8c1431`; `9b57498c802ac2fe6af3a63114501f650fc4d03035e57cebea81e0525e29d1c1` | the port's two conditions and the no-inspection clause |
| 04 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md`, G7 `[40368,41551)` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab`; span `864b46ca8562526e13972bd186e56d201a0b5898de015178063ebe9acd854f2f` | K7: what it accepts, what it rejects |
| 05 | `STAGE8_TASK4A_P3_COMPLETE_U3_PACKAGE_CONSTRUCTION_AND_FOUR_FIELD_STOP_V001.md`, ledger `[19846,21324)` | `f97ee43e052969f0c999b7e57d13310f818222a102c21c249845b8beef1301ff`; span `5a4cef8ab2a29bba52bbc8bd292a228a15123a4425d72b4f3e10a0fae213ef18` | the four would-build demand signatures |
| 06 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md`; role signatures `[37523,38115)`; interface relation `[38121,38437)` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c`; `e16f8b9438c518546fa7eda93c724316f4470f9a8298a50ac8d275eb09d293ad`; `cb264602a0537a07948c31d2763094395c5670a5001ea8378d9e7f83b67ed72c` | **the B0-role bar at its sealed bytes** |
| 07 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_SUBSTITUTE_ADMISSIBILITY_ADJUDICATION_V001.md`, U2 row `[6266,6883)` | `3cd4b080aff0fc07dcc19e957655cec69cbb9f138a89f51d2605797f5ad012f2`; span `e6093b0f48c11d3f05d3c6fd3692b6ea2c4b320746bb92eebe72ef28ed697efe` | the operational requirements and the descent witness |
| 08 | `QUESTIONS_SETTLED_REGISTER_V001.md` — Q-158 (the P5 ruling of record), Q-898 (the reading rule), Q-901 (this section's opening) | `4a990973d0dfe644672b231be9da6bf170c3e7b625109b18657a98cd4fe4770c` | grepped before classification, per RULING-LAST |
| 09 | `PROGRAM_STATE_BRIEF_V005.md`; `LOCKED_PROCESS.md`; `DECLINE_REGISTER_V002.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c`; `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb`; `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | state pin, process law, S01–S37 |
| 10 | `STAGE8_AXN_INDUCTION_PROVENANCE_DARIO_V001.md` | `4710453efd3fdb96e029aa1991a7e57b34f49e71b0d48184475bd98fb0f5ec15` | my 983, whose reading rule Q-898 this relay applies |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
THIS IS THE DEMAND SIDE ONLY.  NO DESCENT OBJECT IS CONSTRUCTED, PROPOSED, SKETCHED, OR SCORED.
NO SUPPLY CALL IS MADE -- THE SUPPLY AUDIT IS THE OPPOSITE LANE'S (Q-901, relay 985).
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — THE DEMAND AT BYTES — DARIO LANE — V001
## RELAY 986 — `[PLAN:DESC-2]` — WHAT THE REQUIRE ACTUALLY REQUIRES

Date: 2026-08-11
Status: **ACCEPTANCE SHAPE ASSEMBLED: 18 conditions binding on a single descent package, each pinned
at bytes, plus 1 further condition that binds only the axis-closure verdict. No single source carries
the demand — each of the five carries a proper subset, and three of them omit something another
supplies. Four OVER-demands and six UNDER-demands are named, including one produced by the very
citation this relay was sent to read.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

---

## 1. Right-closure on the cited window, both directions [PROVABLE]

My 946 established that a cited span fails in two directions, and the relay invokes that discipline
here. Member 03 at `[3916,4671)` and again at `[11845…]` points a reader to
`STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md:152-180` as *"exactly the ceiling recorded at"*.
I checked that window against the whole document before booking anything from it.

**Left edge — the window opens on the wrong section.** Lines 152-180 are §5, headed *"Does sealed
text already supply it?"*, with subsections *"Formal P5 state/effect roles"*, *"Source-sector GNS"*,
*"Record effects and state/contour branches"*, and *"Joint-P0 result"*. **That is a supply audit.**
The document's demand lives at §§1–4, lines 48-151, bytes `[2044,6922)` — *"Sealed common-origin
requirement"*, *"Operational unpacking"*, *"The named property"*, *"Why full source determination is
unnecessary"* — none of which the window contains.

**Right edge — the window stops two lines short of the typed would-build.** Line 180 ends §5.4's
paragraph cleanly, so nothing is truncated mid-sentence. But the `### Finding` block at lines
182-193, bytes `[8533,9079)`, sits outside it, and that block carries the canonical signature:

```text
P5_common_origin_descent_presentation_derived = false | TYPE-U |
  would-build: executable descent maps, actual common construction trace, property/dynamics
  certificates, and target-independent admissibility manifest
```

**Fair reading of the citation itself.** Member 03 cites the window for a *ceiling*, and §5 does state
a ceiling; the citation is accurate for the sentence it supports. The hazard is downstream: a lane
told to *"read P5's requirement at the span the port cites"* would book a supply-status section as the
requirement, would miss all eight substantive conditions, and would miss the would-build by two lines.
**This is the first UNDER-demand of §8, and the relay's instruction to read the whole document rather
than the window is what surfaced it.**

---

## 2. P5's demand [PROVABLE — quoted]

### 2.1 The controlling signature

Member 02 §1: *"`P5. A positive normalized rho_pre on the completed object and admitted record effects
E_r, with domains, all supplied by the same microscopic source as P0.`"* Its failure-capable T4
rejects a candidate *"when the state, effects, effect domains, and operator domains are not
positive/normalized/compatible on one completed object, or are not supplied by the same microscopic
source as the candidate dynamics."*

The semantic force is fixed and is narrower than the words suggest: P5 requires **derivational**
common origin, and *"the phrase does not by itself mean that the source temporally creates the
antecedent carrier."* And: *"Independently postulated effects fail the present P5 class; that does not
prohibit a different producer class."*

### 2.2 The four descent maps

Member 02 §2 at `[3001,5172)`. Let `Omega` be a microscopic construction trace. P5 is operationally
satisfied only if there are typed descent maps

```text
d_state(Omega)      = rho_pre,
d_effect(Omega,r)   = E_r,
d_domain(Omega)     = (D_dyn, {D_r}),
d_dynamics(Omega)   = U_BR or S_CTP,
```

**Four maps, not one.** A package that descends the state alone does not meet the demand.

### 2.3 The eight certificate conditions

One certificate `Cert_P5(Omega)` must establish all of:

| # | Condition | Exact demand |
|---:|---|---|
| 1 | **Common carrier** | all outputs act on, or are faithfully represented on, the same completed physical source-record-field object and quotient |
| 2 | **State properties** | `rho_pre` positive, trace-class where that formulation applies, normalized |
| 3 | **Effect properties** | each `E_r` an admitted positive effect; the completeness/trace-preservation relation **only** *"when the family is declared exhaustive"* |
| 4 | **Domain compatibility** | state, effects, dynamics, contacts, observables have compatible common dense/invariant domains; the dynamics preserves the declared domain |
| 5 | **Dynamic compatibility** | `Tr(E_r U_BR rho_pre U_BR^dagger)` and its normalized CTP version well-defined *"with the same branch, quotient, and measure conventions"* |
| 6 | **Covariance and causality** | source/record embeddings, gauge action, Ward rules, and causal/endpoint conditions **commute with the descent maps** |
| 7 | **Provenance** | all four outputs share the same primitive inputs and construction record; *"none is imported as an independent post hoc parameter"* |
| 8 | **Target-independent admissibility** | the family and its admission criteria are *"frozen before downstream response or root information exists"* |

### 2.4 The named property, and the two distinct verdicts

Member 02 §3:

```text
P5_COMMON_ORIGIN_DESCENT_PRESENTATION
  = (descent maps,
     common-origin construction trace,
     state/effect/domain property certificate,
     dynamics-compatibility certificate,
     target-independent admissibility manifest).
```

and immediately after: *"For one producer candidate, this is sufficient to make P5 **attemptable**."*
Closing the §5.3 mutation axis needs more — *"every admitted target-independent common-origin P5
package must be generated by the manifest, proven response-equivalent to a listed package, or excluded
upstream"* — and the document draws the line itself: *"Thus 'construct one valid P5 package' and
'close every P5 alternative' are distinct verdicts."*

**The demand is therefore verdict-relative.** The coverage theorem is a condition of the second
verdict and of nothing else. §8 books it in both columns for that reason.

### 2.5 What P5 expressly does NOT demand

Member 02 §4 pre-emptively kills the obvious over-demand, twice, with failure-capable types:

```text
P5_common_origin_descent_implies_full_source_determination = false | TYPE-R
P5_logically_requires_unique_complete_origin              = false | TYPE-R
```

`Q_P5` need not be injective; *"Two microscopic sources may differ in response-invisible auxiliary
structure, off-shell presentation, or unused sectors while descending to isomorphic P5 packages."*
§6 adds `P5_and_full_origin_are_the_same_object = false | TYPE-R`. Q-158 is the register's ruling of
record on the same point.

**And the bar is not thereby lowered:** *"This does not reduce common origin to a bare existential
assertion. A concrete construction trace and commuting descent data are required. 'Some source exists'
or 'all fields appear in one file' is insufficient."* The Lead states the prohibition positively:
*"What P5 forbids is independently choosing `rho_pre`, effects, or domains after the dynamics is fixed
and merely placing them in the same container."*

---

## 3. The port's two conditions [PROVABLE — quoted]

Member 03 at `[10824,11928)`:

```text
U2_STATE_PORT_INSTANTIATED = false | TYPE-U |
  would-build: choose or derive omega_phys without output inspection, construct
               H_omega and rho_pre, and certify common origin
```

### 3.1 Condition one — what the no-inspection clause bars

Member 03's `U2-008-C6` at `[23900,24472)` states it operationally:

> *"Required: one trace supplies state, effects, domains, and dynamics **before any response output is
> inspected**. Named failure: combining the source quasifree branch, the module identity, an
> arbitrarily chosen scalarization, and a trial action from unrelated artifacts is an **ad hoc
> cross-row assembly**. Q-158 excludes that presentation."*

So the clause bars two distinct moves, and they are not the same move:

- **Temporal:** fixing any of state, effects, domains, or dynamics *after* a response output has been
  looked at. This is the freeze condition, and it is the same content as P5 condition 8.
- **Compositional:** assembling the four from unrelated artifacts — *ad hoc cross-row assembly* —
  regardless of when it happens. A builder who never looks at an output can still fail this half.

The named failure is worth carrying verbatim into any future construction, because it enumerates the
exact tempting assembly: source quasifree branch + module identity + an arbitrary scalarization + a
trial action.

### 3.2 Condition two — the certificate shape

*"certify common origin."* The shape is fixed by member 02 §3 as content — the property certificate,
the dynamics-compatibility certificate, and the target-independent admissibility manifest, over the
four maps and one trace, establishing the eight conditions of §2.3.

**Law 9b, an honest limit inside my closure.** No member of this closure fixes the certificate's
*form* — no schema, serialization, field list, or digest convention for `Cert_P5` appears at any span
I read. The demand is fully specified as content and unspecified as format. A builder therefore cannot
fail on form, and a future receiver that wants a form will have to declare one; I name this as an
open specification rather than inventing a shape for it.

---

## 4. K7 — what it accepts and what it rejects [PROVABLE — quoted]

Member 04's G7 row at `[40368,41551)`:

> *"`K7` | the base and weighted family descend from the frozen joint common-origin trace; record-only
> Q7 or a provenance status flag cannot substitute"*

**Accepts:** actual descent, from the frozen joint common-origin trace, of **both** families — the
base `lambda` and the weighted `nu = A lambda`. The conjunction is in the clause; a descent of the
measure alone does not satisfy it.

**Rejects, by name:** a record-only Q7; a provenance status flag. Both are the shape of an assertion
standing in for a derivation.

**The no-provenance-credit boundary**, from the same instrument's G6 and from member 03's branch
table: *"Identity-supported Dirac: algebraic K4 PASS, but no independent promotion or provenance
credit"*, and the H-DIRAC price *"only algebraic K4 compatibility is disclosed, with no provenance or
K7 consequence."* So a control classification transfers **nothing** to K7 in either direction: passing
a K-row earlier buys no provenance, and the boundary is stated so that no lane can spend one gate's
result at another gate's window.

---

## 5. The four-field demand signatures [PROVABLE — quoted]

Member 05's ledger at `[19846,21324)`. Each `would-build` states what the built object must **be**:

| Field | The object must be |
|---|---|
| `P3_MEASURE_EXISTS` | *"branch-joint measure, completed orbit map, descended `dmu_C`, `delta_phys`, consistency, tightness/variation, invariance"* |
| `P3_INTERACTING_CONTOUR_EXISTS` | *"regulated interacting operator/contour family and named convergence topology"* |
| `P3_COMPLETE_BOUNDARY_EDGE_EXISTS` | *"geometry, preparation/gluing variation, boundary orbit, edge reduction, boundary functional/form certificates"* |
| `P3_UNBOUNDED_ENDPOINT_DOMAINS_EXIST` | *"operators, scalar representation, common core, closures, self-adjointness/domain-invariance certificates"* |

and the roll-up: `U3_008_INSTANCE_EXISTS` and `P3_COMPLETE` both `would-build: the four fields above`;
`P5_COMPLETE_INPUT_READY` `would-build: completed P3 plus P4 physical provenance`.

Two readings matter for the acceptance shape. First, every one of the four ends in **certificates or a
named topology**, not in an object alone — the demand is object-plus-warrant throughout. Second,
`P5_COMPLETE_INPUT_READY` places the four *upstream* of P5's own input readiness, so a descent package
that satisfies §2 does not thereby complete P3; the two are stacked, not alternatives.

---

## 6. The B0-role bar [PROVABLE — quoted]

Member 06 at `[37523,38115)` fixes the role signature the descent maps must land on:

```text
U2 = microscopic action/evolution, positive normalized pre-state, inclusive
     identity, admitted effects, action/source contact rules, and common domains
```

and at `[38121,38437)` the interface relation and the supplementation bar:

```text
d_C0 : B0_candidate -> C0
d_Ui : (B0_candidate,C0) -> Ui,  i in {1,2,3}

no required physical datum in C0 or any Ui may be supplied by an undeclared
external supplement after any descendant output is inspected
```

Member 07 at `[6266,6883)` adds the operational requirements and names the witness: *"positivity,
normalization, effect completeness where claimed, common-domain compatibility, dynamics compatibility,
action/source contact rules, and the descent witness `d_U2 : (B0_candidate,C0) -> U2`."*

**Read the bar exactly.** It carries two qualifiers that paraphrase tends to drop: the supplement must
be **undeclared and external**, and the bar bites **after any descendant output is inspected**. Member
03's own §1.2 renders the same clause as *"with no post-output supplementation"* — which is
**stricter** than the source, since it drops "undeclared external". §8 books that as an OVER-demand at
its exact scope.

Note also that the B0 U2 signature names **action/source contact rules** and the **inclusive identity**
— neither of which appears in P5's eight conditions. The two sources are not nested.

---

## 7. The assembled acceptance shape [PROVABLE]

Eighteen conditions bind a single descent package. Each is pinned to a member and span.

**A — the object that must exist**

| # | Condition | Ground |
|---:|---|---|
| 1 | Four typed descent maps `d_state`, `d_effect`, `d_domain`, `d_dynamics` | 02 `[3001,5172)` |
| 2 | An actual common-origin construction trace `Omega` — concrete, not existential | 02 `[2044,6922)`, §4 close |
| 3 | `Cert_P5(Omega)`, establishing conditions 4–11 | 02 `[3001,5172)` |
| 4 | The target-independent admissibility **manifest**, as a delivered component | 02 §3, within `[2044,6922)` |
| 5 | The descent witness `d_U2 : (B0_candidate,C0) -> U2` | 07 `[6266,6883)`; 06 `[38121,38437)` |

**B — the certificate's content**

| # | Condition | Ground |
|---:|---|---|
| 6 | Common carrier | 02 `[3001,5172)` |
| 7 | State properties: positive, trace-class where applicable, normalized | 02 `[3001,5172)`; 03 `[7290,7829)` |
| 8 | Effect properties: `0 <= E_r <= I`; completeness **only where the family is declared exhaustive** | 02 `[3001,5172)`; 07 `[6266,6883)` |
| 9 | Domain compatibility, dynamics preserving the declared domain | 02 `[3001,5172)` |
| 10 | Dynamic compatibility, same branch/quotient/measure conventions | 02 `[3001,5172)` |
| 11 | Covariance and causality **commuting with the descent maps** | 02 `[3001,5172)` |
| 12 | Provenance: shared primitive inputs and construction record; no post hoc import | 02 `[3001,5172)` |

**C — the interface the maps must land on**

| # | Condition | Ground |
|---:|---|---|
| 13 | The U2 role signature, including **action/source contact rules** and the **inclusive identity** | 06 `[37523,38115)`; 07 `[6266,6883)` |

**D — process conditions**

| # | Condition | Ground |
|---:|---|---|
| 14 | Target-independent admissibility: frozen before downstream response or root information exists | 02 `[3001,5172)` |
| 15 | One trace supplies state, effects, domains, dynamics **before any response output is inspected** | 03 `[23900,24472)` |
| 16 | No **ad hoc cross-row assembly** — the named failure, whenever it occurs | 03 `[23900,24472)` |
| 17 | No **undeclared external** supplement to any required datum after a descendant output is inspected | 06 `[38121,38437)` |

**E — the downstream receiver that consumes the descent**

| # | Condition | Ground |
|---:|---|---|
| 18 | K7: the **base and weighted** family descend from the frozen joint common-origin trace; record-only Q7 or a provenance status flag cannot substitute | 04 `[40368,41551)` |

**F — verdict-relative, binding the axis-closure verdict only**

| # | Condition | Ground |
|---:|---|---|
| 19 | The coverage theorem: every admitted target-independent common-origin P5 package generated by the manifest, proven response-equivalent to a listed package, or excluded upstream | 02 §3 and §6, within `[2044,6922)` |

**The four-field signatures of member 05 are not folded into this list.** They are the demand on the
U3 fields, which `P5_COMPLETE_INPUT_READY` places upstream of P5's input readiness; a descent package
meeting 1–18 does not thereby meet them, and a list that merged the two stacks would misreport both.

**No single source carries the demand.** P5 omits contact rules and the inclusive identity; the B0
signature omits the admissibility manifest and the covariance-commutation condition; the port's
would-build names only `omega_phys`, `H_omega`, `rho_pre` and *"certify common origin"*, omitting
three of the four descent maps; K7 adds a receiver condition none of the others state. Building
against any one of the five produces an incomplete object that would read as compliant.

---

## 8. OVER-demands and UNDER-demands, named [PROVABLE]

The relay asked for both columns, so both are populated rather than one being reported clean.

### 8.1 OVER-demands — conditions no consumer requires

| Over-demand | Why it is not required |
|---|---|
| **O1. Unique / full source determination** — demanding `Q_P5` be injective, or the microscopic source be reconstructed | Killed twice at member 02 §4 with `TYPE-R`, again at §6, and ruled at Q-158. Distinct sources satisfy P5 if they descend to isomorphic certified packages |
| **O2. Unconditional effect completeness** — demanding `sum_r E_r = I` of every family | Member 02 condition 3 requires it *"when the family is declared exhaustive"*; member 07 says *"effect completeness where claimed"*. Two sources agree on the conditional |
| **O3. The coverage theorem, demanded of a single package** | Member 02 §3 separates the verdicts explicitly; a package is *"attemptable"* without it. Demanding it of a construction converts a lawful partial into an apparent deficiency |
| **O4. "No post-output supplementation", read absolutely** | The source at member 06 `[38121,38437)` bars an **undeclared external** supplement after inspection. Member 03's §1.2 paraphrase drops both qualifiers. **Scope of the over-demand is narrow**: condition 12 independently bars post hoc import of state, effects, domains, and dynamics however declared, so O4 bites only for a *declared* supplement of a datum outside that four |

### 8.2 UNDER-demands — consumer conditions a list would miss

| Under-demand | Where it would be lost |
|---|---|
| **U1. The whole of P5 §§1–4** | Following the port's `:152-180` pointer books a supply audit as the requirement — §1 of this artifact |
| **U2. The typed would-build** | Lines 182-193 sit two lines outside that window |
| **U3. Three of the four descent maps** | The port's would-build names the state leg only; `d_effect`, `d_domain`, `d_dynamics` are P5's and appear nowhere in the port's sentence |
| **U4. Covariance and causality commuting with the descent maps** (condition 11) | Absent from the B0 U2 signature and from the port; carried only by member 02 |
| **U5. Action/source contact rules and the inclusive identity** (condition 13) | Absent from P5's eight conditions; carried only by members 06 and 07 |
| **U6. K7's conjunction — base *and* weighted family** (condition 18) | A descent of the measure alone reads as compliant against every source except member 04 |

### 8.3 The one item that is both

**The coverage theorem is an OVER-demand against a package verdict and an UNDER-demand against the
axis-closure verdict.** It is the same object in both columns, and which column it belongs to is fixed
by which verdict is in play — not by the object. Any acceptance list that states it unconditionally is
wrong in one direction or the other, and a list that omits it is wrong in the other. **The demand is
verdict-relative and must be carried that way.**

---

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  P5's controlling signature, four descent maps, eight certificate conditions, named property, and
  the two TYPE-R non-demands; the port's would-build and its C6 clause; K7's row and the
  no-provenance-credit boundary; the four would-build signatures; the B0 role signatures, interface
  relation, and supplementation bar; the operational requirements and the descent witness; Q-158,
  Q-898, Q-901; all gates.

DERIVED HERE:
  the right-closure result on the cited window, in both directions;
  the assembled eighteen-condition acceptance shape with its verdict-relative nineteenth;
  the observation that no single source carries the demand, with the three omissions located;
  four OVER-demands and six UNDER-demands, each matched to the source that settles it;
  the reading that the coverage theorem occupies both columns and is fixed by the verdict, not the
    object;
  the law-9b note that Cert_P5 is specified as content and unspecified as form.

SELECTED HERE:
  nothing.  No descent object, candidate, skeleton, trace, map, certificate, or manifest is
  constructed, proposed, or scored.  No supply status is asserted.  No verdict on feasibility is
  offered, and none is implied by the assembly's size.

SCALING WEIGHTS: none consumed, fixed, or substituted.  No rank, ratio, or physical quantity was
  read, formed, or evaluated.
```

---

## 10. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A demand list was not flattened into a construction
  plan, nor into a feasibility verdict. A ceiling citation was not flattened into a requirement. A
  paraphrase was not flattened into its source, nor the source into the paraphrase. `Attemptable` was
  not flattened into `closed`. Two stacked demand sets (P5's package and the four U3 fields) were not
  flattened into one list. A would-build signature was not flattened into a built object.
- **Q-898 READING RULE APPLIED throughout**: demand structure was read from consumer signatures and
  would-build lines, never from a producing lane's own derivation claim.
- **BR-1 held.** No producer-declared object carries a demand's direction; every condition is pinned
  to a sealed span, and where a demand's form is unspecified I recorded the absence of a ground rather
  than supplying one.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **PE-1..PE-15:** pointer-only, zero verdict weight.
- **LANE SEPARATION:** this is the demand side only. The supply audit is relay 985's under Q-901, and
  nothing here anticipates, scores, or constrains its finding.
- **CUSTODY:** archive-side only. One artifact, lane-tagged, sealed. No register, plan, tracker, git,
  commit, or push action. Nothing adopted, nothing registered.
- **BUILDER-B INDEPENDENCE:** `evaluator_build_A/` and `checks/` were not read. My own
  `STAGE8_AXN_B0_ACCEPTANCE_CROSSCHECK_DARIO_V001.md` exists in the workspace and was deliberately
  **not** used as a ground; the B0 bar is read from members 06 and 07, which are not mine.
- **BYTE AUDIT:** all ten members re-hashed at seal time; all fourteen cited spans recomputed and
  matched. The register's digest advanced twice during this session's work and is pinned here at its
  value at seal. Closure declared at byte 0, end computed on bytes as a fixed point; 27-token
  pre-closure scan over authored prose, 0 hits.

---

## 11. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 4192; scan 0 hits)
P5 = quoted.  Controlling signature at member 02 §1; FOUR typed descent maps (d_state, d_effect,
     d_domain, d_dynamics) at [3001,5172); EIGHT certificate conditions; the named property with its
     five components; and TWO express non-demands (`..._implies_full_source_determination = false |
     TYPE-R`, `..._requires_unique_complete_origin = false | TYPE-R`).  Derivational common origin,
     not temporal creation.  RIGHT-CLOSURE: the port's `:152-180` window is a SUPPLY audit, not the
     demand -- the demand is lines 48-151 [2044,6922), and the typed would-build sits at lines
     182-193 [8533,9079), two lines outside the window.
PORT_CONDITIONS = 2 quoted.  (i) choose or derive omega_phys WITHOUT OUTPUT INSPECTION -- which bars
     two distinct moves per U2-008-C6 [23900,24472): fixing any of state/effects/domains/dynamics
     after a response output is inspected, AND ad hoc cross-row assembly from unrelated artifacts
     whenever it occurs.  (ii) CERTIFY COMMON ORIGIN -- shape fixed as CONTENT (property certificate,
     dynamics-compatibility certificate, admissibility manifest, over four maps and one trace);
     LAW 9b: no member of this closure fixes the certificate's FORM.
K7 = accepts/rejects quoted.  ACCEPTS: the BASE AND WEIGHTED family descending from the frozen joint
     common-origin trace (a conjunction).  REJECTS by name: record-only Q7; a provenance status flag.
     BOUNDARY: a control classification transfers nothing -- "no independent promotion or provenance
     credit", "no provenance or K7 consequence".
FOURFIELD_DEMANDS = 4 quoted (measure / interacting contour / boundary-edge / unbounded endpoint
     domains), each ending in certificates or a named topology; and P5_COMPLETE_INPUT_READY stacks
     them UPSTREAM of P5 input readiness rather than beside it.
B0_BAR = quoted.  U2 role signature [37523,38115); d_Ui interface relation and the supplementation
     clause [38121,38437), whose exact text bars an UNDECLARED EXTERNAL supplement AFTER a descendant
     output is inspected; operational requirements and the descent witness at [6266,6883).
ACCEPTANCE_SHAPE = assembled: 18 conditions binding one descent package, each at bytes, plus 1
     verdict-relative condition (the coverage theorem) binding axis closure only.  No single source
     carries the demand; three of the five omit something another supplies.
OVER/UNDER = named, both columns.  OVER: O1 unique source determination; O2 unconditional effect
     completeness; O3 the coverage theorem demanded of a single package; O4 "no post-output
     supplementation" read absolutely (narrow scope, stated).  UNDER: U1 the whole of P5 §§1-4;
     U2 the typed would-build; U3 three of the four descent maps; U4 covariance/causality commuting
     with the descent maps; U5 action/source contact rules and the inclusive identity; U6 K7's
     base-AND-weighted conjunction.  The coverage theorem occupies BOTH columns, fixed by which
     verdict is in play.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is condition 13: the U2
role signature carries **action/source contact rules** and the **inclusive identity**, neither of which
appears among P5's eight conditions, and I have booked them as binding on a descent package on the
strength of the `d_U2` witness relation alone. If a lane reads the B0 signature as typing the U2
*slot* rather than as constraining what a descent must *deliver into* it, condition 13 moves out of the
package list and U5 dissolves with it. That is the sentence to check at bytes before this shape is
built against.
