# DEFAULT-REFUTE AUDIT OF THE TWIST-EVIDENCE DETERMINATION — COMMISSION O40SR

AUDIT LANE OF A COMMISSIONED PAIR — 2026-08-15
DEFAULT VERDICT: REFUTED. ADVERSARIAL BY MANDATE. EVERY LOAD-BEARING CLAIM IN
THE TARGET WAS TREATED AS CLAIMED AND RE-DERIVED AT BYTES.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

**THE HARD BAR, RESTATED BEFORE THE FIRST FINDING.** The member the target
tests — DoR-020-A4, J12-G STRICT PUSHOUT — is a RATIFIED AUTHORED PRINCIPAL
ACT. This audit does not void it, does not declare it void, does not recommend
voiding it, does not argue for reopening it, and does not characterise the
evidence as sufficient or insufficient for any act. It reports **evidence
status at bytes** and stops. Voiding is the registrar's on a finding; the
ruling is the principal's. No missing object is authored here — not the contact
term, not the subextensivity estimate, not `u_f`, not in a toy, a footnote, or
an aside.

**TARGET.**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TWIST_EVIDENCE_O40SR_V001.md`
SHA-256 recomputed by this lane from the artifact's own directory:
`0ee7796e05d6088c0e5d166fafa230470dc6cbaa4023fbe4b8670e45c6109a63`
Both sidecars (`.md.seal.sha256`, `.md.sha256`) carry that digest and
`shasum -a 256 -c` returns **OK**. The build's reported digest matches.

**OUTPUT-PATH PROBE, BEFORE ANY WRITE.**
`.../STAGE8_TWIST_EVIDENCE_O40SR_AUDIT_V001.md` — ABSENT at commission start,
as were both sidecars.

**CORPUS (re-swept independently, not inherited).**
```text
ROOT1 = /Users/bgm/MB Work/alpha-program-archive/workspace
ROOT2 = /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
DOR   = /Users/bgm/MB Work/alpha-program-archive/supervision/DOR_*.md
        (expressly permitted and required; nothing else in that directory
         was opened)
```

---

## §0 — HEADLINE

**OVERALL: CONFIRMED-WITH-CORRECTIONS.**

Every graded answer in the target survives re-derivation at bytes. Q1's
actuality standard is the corpus's own and its operative passage is one
contiguous span, quoted whole rather than assembled from fragments. Q2's
**SPECIFIED-NOT-CONSTRUCTED**, Q3's **NO-ACTUAL-DATA-TO-TEST**, Q4's four
grades, and Q5's **NAMED-ONLY** plus three **UNRELATED** rows all hold when
re-derived independently — several of them gaining corroborating sites the
build did not cite.

What does not hold is a set of the target's **enumerations, stated bases, and
line citations**. Eight corrections are recorded below. The most serious is
that the build asserts a specific clause to be "the strongest pro-existence
clause in the corpus" while a strictly stronger one — *"extend by its
already-built transport"*, said of `eta_f^boundary` itself — sits unquoted
inside the build's own adopted universe. The second is that the build twice
asserts a universal negative ("no unequal image is exhibited anywhere in either
root") that is false at bytes, with the falsifying sentence broken across a
mid-line wrap of exactly the kind the commission warned about.

No correction changes any grade. The corrected grades are stated explicitly in
§8.

---

## §1 — IMPORT AUDIT, STATED FIRST

```text
*** THE WORDS "HOLONOMY", "MONODROMY", "TWIST", "COCYCLE", "TRANSITION
    FUNCTION", "PUSHOUT", "AUTOMORPHISM", "COUNTERMODEL" AND "GAUGE" ARE USED
    BELOW ONLY TO *DESCRIBE* WHAT THE CORPUS DISPLAYS.  NO THEOREM ABOUT ANY OF
    THEM IS IMPORTED, AND NOTHING IS CONCLUDED FROM A NAME. ***
```

| Import | Source | Where used | Does the finding survive without it? |
|---|---|---|---|
| "holonomy", "monodromy" as descriptive shape names | standard differential geometry, outside this corpus | §4 (D3) sweep vocabulary | **YES.** Supplied by the commission as search vocabulary. Every grade rests on quoted corpus bytes. `contact-holonomy` is record-native at `DETERMINATION_MAP:357`. |
| "twist" as a shape name | commission vocabulary, and record-native | §4 | **YES.** `twist`, `contact twist`, `CONTACT_TWIST_SEEN`, `Tw_f` are all the corpus's own tokens; counts in §7. |
| "pushout", "automorphism", "countermodel" | standard category/algebra usage | §§2–6 | **YES.** All three are record-native tokens quoted from the ruling and its basis. |
| "U(1)-valued transition function", "triple-overlap cocycle condition" | commission vocabulary for the D5 reach test | §6 | **YES.** Used only as search strings and as descriptions of displayed corpus equations, which are quoted whole. |
| "necessary condition" / "consequence" as logical descriptions | ordinary logical vocabulary | §2 (COR-F₁) | **YES.** The point is made by quoting the corpus's own word "therefore". |
| "off-by-one" as a description of a citation defect | ordinary editorial vocabulary | §8 (COR-F) | **YES.** Each instance is shown by displaying the cited line and the actual line. |

NO OTHER IMPORT IS MADE. No theorem about holonomy, no classification of
bundles, no vanishing criterion, no cohomological interpretation, and no
statement about what a twist "would" imply physically is imported or applied.
Where the corpus states a law, this audit reports the law and stops.

---

## §2 — D1: THE ACTUALITY STANDARD (Q1)

### GRADE: **CONFIRMED-WITH-CORRECTIONS.**

**The question the dimension poses.** Is the standard the build reports
genuinely the corpus's, quoted whole, or has it been assembled from fragments?

**Finding: it is the corpus's, and it is one contiguous span.** The build's
operative citation, `STAGE8_TASK5_EQ6_CERT_V003_REVIEW_LANE2_V001.md:120-131`
(seal OK), was re-read at bytes and is a single unbroken passage. Quoted WHOLE,
including the two clauses that scope it:

> The key check is whether this data is an actual record-surface contact witness in the
> adopted scope. It is **not**:
>
> 1. It supplies only a positive finite Hilbert pair and a closed-form `d^0` choice.
> 2. It omits the actual surface transport data required for an actual `[EQ6]` contact
>    realization on the finite live tower (path current data, admissible transport,
>    and the `Loc/σ` realization stack under `A1`-scope).
> 3. It therefore is a **pre-A5 countermodel family** (showing logical non-implying),
>    not a direct admitted object of the current surface coherence class.
>
> So A5 void-1 is **not** triggered by this model on the actual scope; `DoR-020-A5`
> remains in force.

The line numbers are exact: `:120` is "The key check…", `:131` is "So A5
void-1…". **Not assembled from fragments.**

The three corroborating sites also verify at bytes:

- `DETERMINATION_MAP:67-74`, the claim tags, exact — `### 1.2 Claim tags` is at
  `:67` and the closing fence at `:74`; `PART-PROVABLE = the form or consistency
  is proved, but actual physical / inhabitance is not;` spans `:71-72`.
- `EXCISION_CERT:47-55`, exact, including its closing clause "The distinction is
  load-bearing: compatibility is proved; physical family-level inhabitance is
  not."
- `EXCISION_CERT:128-129`, exact, and the build's byte-check note about the
  column padding at `:129` is itself correct at bytes.
- `CLOSING_LEDGERS:142`, the F18 note column, exact.

**The contact objects are also correctly typed.** `CONSTRAINT_ARM:445-463`
(J13) verifies line-for-line, including the sentence the build flagged as
cutting against a loose reading — "Calling every off-diagonal path kernel
`contact` fails `(J13-2)`." (`:462-463`). `DETERMINATION_MAP` §3.1 `(D2-1)`,
`(D2-2)`, `(D2-3)` verify at `:290`, `:296`, `:303`.

### CORRECTIONS TO D1

**COR-F₁ — the three-limb decomposition over-formalizes the corpus.** The
build's report and its §1.3 present A1/A2/A3 as "three necessary limbs, all
three necessary". At bytes the corpus's third item opens **"It therefore is"** —
it is a *consequence* drawn from items 1 and 2, not an independent necessary
limb. Deciding bytes: `STAGE8_TASK5_EQ6_CERT_V003_REVIEW_LANE2_V001.md:127`.
This changes nothing downstream: every candidate the build tests fails A1 and
A2 independently, so the disqualifications do not rest on the over-read limb.

**COR-F₂ — a load-bearing "only" is dropped.** The corpus writes "It supplies
**only** a positive finite Hilbert pair and a closed-form `d^0` choice"
(`:123`) — an *insufficiency* finding. The target's §1.3 A1 renders it as
'A "positive finite Hilbert pair and a closed-form d^0 choice" is expressly
declared NOT to satisfy this' (target `:415`), dropping "only" and converting
insufficiency into disqualification. The build report's own wording retains
"only" and is correct; the artifact's does not. Deciding bytes: target
`:413-416` against `CERT_V003_REVIEW:123`.

**One citation defect, carried to COR-F.** The target's §1.3 attributes the
phrase "the unbuilt contact transport" to `DETERMINATION_MAP:87`. At bytes:

```text
87:Eta_rep       = the derived represented-response forward map;
88:eta_boundary  = the unbuilt contact transport;
```

The phrase is at `:88`. The quotation itself is exact.

**D1 verdict.** The standard is the corpus's, quoted whole from a single span,
and correctly corroborated. Two over-readings and one line citation are
corrected. **CONFIRMED-WITH-CORRECTIONS.**

---

## §3 — D2: ACTUAL DATA (Q2)

### GRADE: **CONFIRMED-WITH-CORRECTIONS.**

### §3.1 The grade re-derived

**SPECIFIED-NOT-CONSTRUCTED holds at bytes.** Every site the build cites was
re-read; all verify:

- `EXCISION_CERT:603-620` — the `(H2-4)` specification followed, in the same
  span, by "On the program's actual fixed family, `(H2-4)` remains unproved."
  and `D_CONTACT_PHYSICAL_TERM = OPEN / TYPE-U`.
- `DETERMINATION_MAP:403-408` — `ACTUAL_PHYSICAL_GLUE_INHABITED = OPEN`.
- `DETERMINATION_MAP:573-575`, `:536-537`, `:322-324` — all exact.
- `MAXWELL_SYMBOL_CERTIFICATES:480-489` — the heading **"### 4.2 Contact split -
  not built"** is at `:480` and the restored quotation (the build's self-reported
  SC-2) is genuine and whole at bytes, `Ker_phys` spelled correctly at `:487`.
- `MEMBERSHIP_REVIEW_DARIO:251-253`, `:259-260`, `:264-268` — all exact,
  including the "an actual intersection" clause and its scoping sentence.
- `EXCISION_CERT:15-16`, `:41-42` — exact.

### §3.2 The independent hunt for a construction the build missed

Three sealed artifacts **outside the build's adopted universe** were found to
carry directly on Q2. All three **corroborate** the grade; none refutes it.

**(a) `STAGE8_TASK5_EQ6_ZERO_DEFECT_SECTION_BUILD_LANE2_V001.md` (seal OK)** —
an artifact whose stated task is "build the physical zero-defect section on the
actual fixed DoR-019/Q-408 positive-source family" (`:5`). Its `:42-50`, quoted
WHOLE:

> The physical section is not built. The failure is exact and constructive:
> the actual path/current and bundle members exist at the minimal
> positive-source diamonds, but the fixed DoR-019 metric and fixed Q-408
> analysis map do not prove that those members lie in the zero loci. The
> first rank-one-to-rank-two leg already requires a fixed-data old-image
> isometry, vanishing old/new Riesz cross term, and support-local analysis
> transport. None is supplied family-wide. Covariance cannot create a zero,
> and stagewise candidates would not by themselves inhabit the
> common-refinement equalizer.

and, on the contact diamond specifically, `:405-408`:

> Contact can silence RNL only for supports that actually meet. It does not
> remove the exclusive-region family, and it cannot prove LR on the
> exclusive segment of the new path. Thus the contact locus remains open on
> fixed physical data.

**(b) `STAGE8_TASK5_V005_FINAL_CHECK_AND_SINGLE_BEDROCK_LANE1_V001.md:213-229`
(seal OK)** — the P7 typing rule, which prescribes how the contact ideal *would*
be formed and immediately types the fiber open:

> Closed contact subspaces are represented by orthogonal projections in the
> P5 Hilbertizable horn and use the gap metric.  The contact ideal is the
> closed invariant restriction-natural hull of actual contact generators:
>
> ```text
> I_CM=closure span orbit{actual boundary/contact generators}. (P1-6)
> ```
>
> Response maps and cocycles live in declared operator/vector product
> carriers.  The cocycle is imposed before closure and response selection is
> conditional on a nonempty closed affine naturality fiber.  No ideal is
> averaged as a vector and no visible cycle may be deleted.
>
> ```text
> P7_RULE_TYPING = PASS / PARTIAL
> P7_RESPONSE_FIBER = open
> ```

**(c) `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005.md:482`
(seal OK)** — the J13 row of the highest version of a series the build quoted
from three times:

> | J13 | boundary/contact cocycle and safe ideal | **CONDITIONAL THEOREM ONLY** | closure hull `(O1-13)` preserves a supplied cocycle; no actual P7 discrepancy family exists to close |

### §3.3 COR-A — THE STRONGEST PRO-EXISTENCE CLAUSE, UNQUOTED

The target states, at `:533-535`:

> The phrase "an actual intersection" is the strongest pro-existence clause in
> the corpus. It is immediately scoped by its own next sentence to "a statement
> about the **quotient carrier**" …

**That claim is false at bytes.** A strictly stronger clause exists, it is about
`eta_f^boundary` itself rather than about the intersection, and it sits inside
the build's **own adopted universe** (basename 13 of its 23).
`STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md:279-291` (seal OK),
quoted WHOLE including every clause on both sides:

> For the declared finite boundary/contact class, extend by its already-built
> transport:
>
> ```text
> Eta_f(r+b):=Eta_f(r)+eta_f^boundary(b),
> r in R_n^rep,
> b in I_contact,n,                                  (W2-4)
> ```
>
> interpreted on the declared quotient/direct-sum member when the intersection
> is nontrivial. J13's safe-ideal law makes `(W2-4)` well defined. No arbitrary
> extension to an unrepresented response complement is claimed or consumed by
> a sealed finite consumer.

"extend by its **already-built** transport" is the corpus asserting, in a
FINITE_PHYSICAL_FACE_REPAIR build, that the object the determination map calls
"the unbuilt contact transport" is built. That is the corpus's own strongest
pro-existence sentence on the deciding question, and the target does not quote
it, cite it, or acknowledge it.

**Why the grade nevertheless survives.** The claim is struck by the review of
record. `STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md:46` (seal OK),
quoted whole:

> | Y2 forward square | **KILL** | `(W2-3)` is a valid represented-carrier map and never misuses compression, but `(W2-4)` is not well defined on a nontrivial represented/contact intersection; J13's cocycle does not supply the missing overlap equality |

and at `:286-295`, quoted whole:

> Thus the represented forward leg is built, but the claimed map on
> `R_n^phys` is not. Common-refinement agreement inherits the same unresolved
> overlap.
>
> ```text
> Y2 = KILL
> ETA_ON_R_REP = BUILT_AND_FUNCTORIAL
> W2_10_SCOPE_DISCIPLINE = PASS
> ETA_ON_R_PHYS = NOT_WELL_DEFINED_WITHOUT_INTERSECTION_CERTIFICATE
> ```

So `Eta` on the represented carrier is built; `Eta` on the physical carrier
`R_N^phys` is not — which is exactly SPECIFIED-NOT-CONSTRUCTED. **The grade
holds. The omission is a quotation-integrity failure and a false superlative,
not a wrong grade.**

**Deciding file:line:**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md:279-280`.

### §3.4 COR-D — UNIVERSE UNDER-SCOPING, CONTRA §0.1'S DEFENCE

The build's PASS C **reproduces exactly** under an independently constructed
exclusion array: 273 hits / 40 file instances / 21 distinct basenames (row
A-32), and the 21 basenames match the build's list minus its two named
extensions. The arithmetic is sound.

The *defence* is not. The target declares its universe "constructed by sweep,
stated in full, and defended", having already widened once because PASS A
"demonstrably misses" three artifacts. The same failure recurs one level out.
PASS C's eight tokens cannot see the following sealed artifacts, each carrying
load-bearing content on a commissioned question:

```text
STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md
    :242-243  "Define the response/contact transport `rho_f^C2` by the same
               test/current pullback, including the endpoint and bundle data
               supplied by DoR-020-A1."
    :247      "beta_f := rho_f^C2(C2_m^fin) - C2_n^fin = 0       (C1-7)"
    :604      "| C2 exact on component scopes | install actual old-image
               response/contact transport on every generator and composite |
               **BUILT** |"
    -- zero PASS-C tokens; the build's own universe test cannot see it.

STAGE8_TASK5_PACKAGE_ADJUDICATION_AND_WITNESS_LANE1_V001.md
    :232-244  "### 1.5 Boundary composition counterexample" -- see COR-B.

STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005.md
    :482      the J13 "CONDITIONAL THEOREM ONLY" row (§3.2c).  The build
              adopted V001/V002/V003 into the universe and consumed V004
              out-of-universe, and silently dropped the series' HIGHEST
              version.

STAGE8_TASK5_EQ6_ZERO_DEFECT_SECTION_BUILD_LANE2_V001.md
    :42, :405-408  "The physical section is not built." (§3.2a)
```

All four corroborate the grades. The defect is that a universe declared
defended provably does not contain them, and the commission's standing
discipline names exactly this failure mode.

**Deciding file:line:**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:604`.

**Is `rho_f^C2` the missing contact transport?** Tested, not assumed. It is
**not**: `LANE2_V001` carries zero occurrences of `I_contact`, `C_N`,
`eta_f^boundary`, `Eta_f^rep`, `R_N^phys`, `contact glue`, `J12-G`, or `Tw_f`
(checked directly). Its `beta_f = 0` is a zero cocycle on the *old image* of a
C2 response leg, and `:250-252` scopes it explicitly: "For a cycle-creating
arrow, `(C1-7)` says the old response restricts exactly; it says nothing false
about the target-only new-cycle component." It is a different object, and this
lane declines the identification rather than importing one.

**D2 verdict.** SPECIFIED-NOT-CONSTRUCTED re-derived independently and
corroborated by three further sealed artifacts. Two corrections (COR-A, COR-D).
**CONFIRMED-WITH-CORRECTIONS.**

---

## §4 — D3: THE TWIST (Q3) — THE HEAVIEST DIMENSION

### GRADE: **CONFIRMED-WITH-CORRECTIONS.**

The build graded **NO-ACTUAL-DATA-TO-TEST**. Per the dimension's instruction,
this lane hunted independently under vocabulary and structural forms the build
did not sweep, checked its declared universe and its leak counters, and ran the
patterns it omitted.

### §4.1 The build's own detector rows reproduce exactly

Under an independently constructed exclusion array (firing verified, §7):

```text
Tw_f              CASE-SENS   14 hits / 4 file instances   (build S-4: 14 / 4)
CONTACT_TWIST_SEEN CASE-SENS  10 hits / 6 file instances   (build S-3: 10 / 6)
contact-holonomy  CASE-SENS    2 hits / 2 file instances   (build S-5:  2 / 2)
contact holonomy  (space)      0 hits / 0                  (build S-6:  0 / 0)
contact automorphism           6 hits / 4 file instances   (build S-7:  6 / 4)
```

All four `Tw_f` sites and all six `CONTACT_TWIST_SEEN` sites were enumerated
and read. `JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:431-450` verifies
line-for-line, including `(J12-5)` at `:436` and the closing clause at `:450`
("not as a theorem that future physical contact data cannot produce one").
`STAGE1_V003_REVIEW:294-302` and `:500` verify. `LANE1_V004:257` verifies.

### §4.2 Patterns the build did not sweep — run here

| Pattern | Hits | Files | LEAK | What it returned |
|---|---|---|---|---|
| `Y2-4` | 14 | 6 | 0 | the overlap-equality token the determination map names at `:307` |
| `D2-3` | 14 | 8 | 0 | the overlap equality itself |
| `J12-5` | 4 | 2 | 0 | the detector definition and its regression row |
| `overlap equality` | 33 | 13 | 0 | — |
| `nonzero contact` | 4 | 4 | 0 | **two new exhibits** (below) |
| `nontrivial contact` | 6 | 6 | 0 | ledger rows and a P3 proposal row |
| `contact element\|contact class` | 38 | 26 | 0 | — |
| `contact transport` | 11 | 7 | 0 | the `rho_f^C2` sites of COR-D |
| `already.built` | 108 | 87 | 0 | **COR-A** |
| `actual contact` | 38 | 20 | 0 | the corroborating artifacts of §3.2 |
| `eta_boundary\|eta…^boundary` | 46 | 12 | 0 | — |

### §4.3 COR-B — THE ENUMERATION "TWO TWISTS" IS INCOMPLETE AT BYTES

The target states (Q3, and its report): "Two twists ARE exhibited, both
classified by the corpus as models". At bytes there are at least **four**, and
the two the build did not find are the two that matter most, because each
exhibits *unequal images on an overlap element* — the content of void 1.

**EXHIBIT THREE — the boundary-transport alteration, in the V002 review.**
`STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md:272-288` (seal OK), quoted
WHOLE including the clauses on both sides:

> The cited J13 law is
>
> ```text
> beta_(gf)=beta_g+Eta_g(beta_f)
> ```
>
> together with closure, invariance, restriction naturality, and absence of a
> record-visible bulk cycle in `I_contact`. It is a path cocycle. It does not
> imply equality of the represented and boundary transports on their
> intersection. Indeed one can alter `eta_f^boundary(x)` by a nonzero contact
> element and propagate the alteration by the same cocycle while preserving
> all equations V002 cites. The two decompositions then receive different
> images.
>
> Thus the represented forward leg is built, but the claimed map on
> `R_n^phys` is not. Common-refinement agreement inherits the same unresolved
> overlap.

**Note the wrap.** "different" ends `:283`; "images." begins `:284`. A
line-oriented sweep for the phrase *different images* returns **0 hits across
both roots** — measured here — which is the most probable reason the build's
line-based method did not surface it. This is precisely the mid-sentence
line-wrap hazard the commission names.

**EXHIBIT FOUR — the boundary composition counterexample.**
`STAGE8_TASK5_PACKAGE_ADJUDICATION_AND_WITNESS_LANE1_V001.md:232-244` (seal OK),
quoted WHOLE:

> ### 1.5 Boundary composition counterexample
>
> Let `R:G->G'` and `S:G'->G''` be refinement arrows.  P7 can assign separately
> subextensive discrepancies `beta_R`, `beta_S`, and `beta_(S compose R)` while
> meeting its pairwise limit statement.  Without
>
> ```text
> beta_(S compose R)=beta_S+Eta_S(beta_R)            (G1-7)
> ```
>
> up to the declared sign convention, the direct and two-step pulls can differ
> by a nonzero contact class.  Pairwise subextensivity does not prove path
> independence.  Equation `(G1-7)` is J13's missing repair.

**A fifth, named as an attack.** The same V002 review carries, at `:406-411`,
"### 7.2 Fresh attack 2: overlap-decomposition collision" — "Equation `(Y2-3)`
gives two outputs for one physical response whenever `R_n^rep intersect
I_contact,n` is nontrivial and `(Y2-4)` is absent." Its verdict row at `:49`
reads "**KILL** | … an overlap-decomposition collision defeats the full
`Eta_f`; both survive every equation V002 actually cites".

### §4.4 THE GRADE, TESTED AGAINST THE NEW EXHIBITS

Each new exhibit was run against the actuality standard of §2, on its own bytes:

```text
EXHIBIT THREE (V002_REVIEW:280-284)
  A1  "Indeed one can alter …" — a modification argument over a quantified
      alteration.  No admitted surface object is exhibited.  FAILS.
  A2  No path current data, no admissible transport, no Loc/sigma stack, no
      DoR-019 metric, no Q-408 maps.  FAILS.
  A3  Its stated purpose is that the J13 cocycle "does not imply equality of
      the represented and boundary transports on their intersection" — a
      logical non-implication device.  FAILS.

EXHIBIT FOUR (PACKAGE_ADJUDICATION:232-244)
  A1  The corpus's own heading is "Boundary composition COUNTEREXAMPLE".
      FAILS.
  A2  "P7 CAN assign" — a modal over a hypothetical assignment.  FAILS.
  A3  Its conclusion is "Pairwise subextensivity does not prove path
      independence" — again non-implication.  FAILS.
```

Neither is actual on any limb. **The grade NO-ACTUAL-DATA-TO-TEST is
CONFIRMED.** No actual contact datum exhibiting a nontrivial twist exists in
either root, and no site anywhere asserts one. What is corrected is the
*enumeration*: the target says two exhibits and reports them as exhaustive;
there are at least four, two of them in artifacts the build had in hand.

**Deciding file:line:**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TASK5_EQ6_STAGE1_V002_REVIEW_LANE1_V001.md:283-284`.

### §4.5 The distinction the grade turns on, re-tested

The build declines the corpus's own `CONTACT_TWIST_SEEN = none` as its grade,
on the ground that `Tw_f := eta_f^boundary|_C_N − Eta_f^rep|_C_N` cannot be
evaluated when one of its two terms is called unbuilt. This lane tested the
opposite reading and finds the build's the one the bytes support:

- `DETERMINATION_MAP:88` — `eta_boundary  = the unbuilt contact transport;`
- `ZERO_DEFECT_SECTION_BUILD:42` — "The physical section is not built."
- `ADOPTION_PROPOSAL_LANE2_V005:482` — "no actual P7 discrepancy family exists
  to close".
- `STAGE1_V002_REVIEW:294` — `ETA_ON_R_PHYS =
  NOT_WELL_DEFINED_WITHOUT_INTERSECTION_CERTIFICATE`.
- And the ruling's own words, `DOR_020_A4:18` (seal OK): "Twisted gluing (a
  contact-holonomy member u_f — new physical fiber; **no current evidence of
  twist**)".

The corpus's own scope word is "in actual checked stock" (`STAGE1_V003_REVIEW:500`),
and the assembly appends that its report is made "not as a theorem that future
physical contact data cannot produce one" (`LANE1_V003:450`). The build's
refusal to convert an empty input set into a negative result is correct at
bytes.

**D3 verdict.** **CONFIRMED-WITH-CORRECTIONS** (COR-B).

---

## §5 — D4: THE OTHER FOUR CONDITIONS (Q4)

### GRADE: **CONFIRMED-WITH-CORRECTIONS.**

The ledger position first, so it is not mistaken for this lane's grade.
`STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md` records the five A4 voids as rows
**F13–F17 at `:137-141`**, each `STANDING` with falsifier type `PROVABLE`.
(The target cites this span as `:138-142`; `:142` is F18, an **A5** void. See
COR-F.) This lane grades independently at bytes.

### VOID 1 — unequal images on an overlap element: **NOT-TRIGGERED** — grade confirmed, stated basis corrected

The grade holds. `STAGE1_V003_REVIEW:251-252` verifies exactly ("The equality is
imposed only on the actual common subspace `C_N`; no equality is imposed on
exclusive represented or exclusive contact directions."), the recomputation at
`:254-282` verifies whole, and the regression row at
`JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:456` reads, at bytes:

> | 1 | compare the represented and boundary images on every actual overlap class | equal by the adopted datum |

**COR-B applies here with full force.** The target asserts, twice:

- `:766` — "No unequal image is exhibited anywhere in either root."
- `:908` — "no unequal image exhibited anywhere."

Both are false at bytes. `STAGE1_V002_REVIEW:283-284` exhibits precisely an
unequal image — "The two decompositions then receive different / images." — and
`PACKAGE_ADJUDICATION:242-243` exhibits another — "the direct and two-step pulls
can differ by a nonzero contact class." Both spans are quoted whole at §4.3.

The **grade** survives because the void condition requires unequal images on an
**actual** overlap element, and both exhibits fail A1/A2/A3 (§4.4). What fails
is the universal quantifier the target used to support it. A grade defended by
a false "anywhere" is a grade defended badly, even when it is right.

**Corrected basis:** NOT-TRIGGERED because no *actual* overlap element receives
unequal images; two model-level unequal-image constructions exist and are named
above. Test set empty in the same way Q3's is.

### VOID 2 — gluing deletes a `D_G^*`-visible bulk cycle: **NOT-TRIGGERED** — CONFIRMED

The one void with a positive proof rather than an empty test, and the target's
account survives unchanged. Three independent sites verify at bytes:

- `JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V003.md:421-429`, quoted WHOLE:

> ### 4.4 No-deletion theorem
>
> Suppose `[r,0]_N = 0` in `P_N`.  By (J12-2), there is `c in C_N` with
> `(r,0)=(c,-c)`, so `c=0` and `r=0`.  Thus the represented injection is
> injective.  Similarly the contact injection is injective.  The pushout deletes
> only the duplicate presentation of the common class, not either physical leg.
>
> In particular, a `D_G^*`-visible bulk cycle cannot be deleted by contact gluing.
> Deleting one would trigger A4 void condition 2.

- `STAGE1_V003_REVIEW:287-288` — verbatim as the target reports.
- `MEMBERSHIP_REVIEW_DARIO:279-281` — verbatim, including "**PASS.**"

The clause cutting against, `MEMBERSHIP_REVIEW_DARIO:283-286`, verifies whole
and the target quotes it whole. **No correction.**

### VOID 3 — beta cocycle / … / subextensivity: **UNTESTABLE-AS-STATED** — CONFIRMED AND STRENGTHENED

The dimension names this the leg most likely to be mis-graded and instructs
this lane to test the locatability of the estimates itself. Done, from primary
sources, not inherited.

**Independent exhaustive enumeration.** Every occurrence in either root, under
the register bar and self-exclusion, of any `beta…/Vol…` form
(`beta[_a-zA-Z]{0,3} ?/ ?Vol`): **31 hits across 14 file instances, LEAK 0.**
Every one was read. They partition exhaustively into:

```text
(a) THE OBLIGATION, stated with a modal:
    CONSTRAINT_ARM:454-457  "`Bound` must prove in the one topology /
                             beta_R / Vol_R -> 0 on every admitted cofinal
                             family,"
(b) THE CLAUSE, restated in the adoption-proposal series:
    ADOPTION_PROPOSAL_LANE2_V002:449 and _V003:443
      "beta_R/Vol_R -> 0 in tau_ker on every admitted cofinal family;"
    ADOPTION_PROPOSAL_LANE2_V004:166  the labelled clause form
(c) THE DIFFERENT-NOTATION RELATIVE, inside a conditional block:
    DETERMINATION_MAP:316  "beta_f/Vol_f -> 0 on admitted cofinal families;"
      -- prefixed at :311 "The stock proves, conditionally on an actual
         physical contact term:"
(d) THE UNDEFINEDNESS SENTENCE:
    HUNT_CROSS_CHECK:225-226 (below)
(e) PRIOR COMMISSIONS' SWEEP ROWS ABOUT (a)-(d), in the two O38SR artifacts.

NO SITE ANYWHERE SUPPLIES A BOUND, A RATE, A CONSTANT, A CONVERGENCE PROOF,
OR A COFINAL-FAMILY ESTIMATE.
```

`STAGE8_TASK5_HUNT_CROSS_CHECK_AND_FRONTIER_LANE2_V001.md:223-232` (seal OK),
quoted WHOLE:

> The generated contact ideal is `{0}`, so no visible cycle is deleted.  This
> is neither circular nor a blanket declaration about future new-edge
> discrepancies.  The expression `beta/Vol` remains undefined until a positive
> P4 volume exists; the hunt correctly leaves subextensivity conditional.
>
> ```text
> REF_PATH_KERNEL_TRANSPORT = CONFIRMED
> REF_PATH_ZERO_COCYCLE = CONFIRMED
> REF_PATH_SUBEXTENSIVITY = conditional_on_P4_density
> ```

**The result reproduces at bytes: the estimates behind the subextensivity law
are not locatable in either root.** The target's grade is correct.

**Two sites the target did not reach, both strengthening the grade.**

1. `STAGE8_TASK5_PACKAGE_ADJUDICATION_AND_WITNESS_LANE1_V001.md:243-244` —
   "Pairwise subextensivity does not prove path independence.  Equation
   `(G1-7)` is J13's missing repair." The corpus states that even a
   subextensivity fact, if it were had, would not deliver the cocycle limb.
2. `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005.md:482` — the
   highest version of the very series that carries the clause at V002/V003/V004
   **drops the `beta_R/Vol_R` clause entirely** and types J13 as
   "**CONDITIONAL THEOREM ONLY** | closure hull `(O1-13)` preserves a supplied
   cocycle; no actual P7 discrepancy family exists to close". `:317` of that
   artifact reads "The cocycle is the actual discrepancy `beta_R`; `(J13-1)` is
   imposed before closure." — imposed, not derived.

**The eight-limb premise verifies.** `STAGE1_V003_REVIEW:289-291`, quoted
WHOLE:

> 3. Composition, reality, units, Ward, restriction, support, OLD_FID, RNL, LR,
>    and subextensivity descend only when their two legs agree on `C_N`; V003
>    states and uses that premise rather than deriving it from the quotient.

**Grade for VOID 3: UNTESTABLE-AS-STATED — CONFIRMED**, with two corroborating
sites added. Nothing at bytes exhibits a failure of any limb; nothing at bytes
permits the subextensivity limb to be run.

### VOID 4 — outcome-selected contact automorphism: **NOT-TRIGGERED** — grade confirmed, enumeration corrected (COR-C)

The **grade holds**: all three distinct `contact automorphism` sites in the
corpus were enumerated and read, and none is a *selection*.

**COR-C — the target's provenance enumeration is wrong at bytes in three
particulars.** The target states (§4 VOID 4): "The `contact automorphism` token
occurs 6 times in 4 file instances across both roots (S-7) — that is 2 distinct
basenames, and both are the determination map's `u` construction (`:331` …) and
its choice-table row (`:357`)."

Measured here: 6 hits / 4 file instances / 2 distinct basenames — the counts are
right. The identifications are not. The complete enumeration at bytes:

```text
DETERMINATION_MAP:338   "where `u` is any reality/unit-compatible contact
                         automorphism. On a one-arrow"
                        -- the target cites this as :331.  WRONG LINE.
DETERMINATION_MAP:374   "4  a contact automorphism is selected from a response,
                         threshold, fixed-point,"
                        -- this is the VOID-CONDITION-4 STATEMENT.  The target
                        identifies the second determination-map site as the
                        choice-table row :357; :357 does NOT contain the token
                        `contact automorphism` at all.  WRONG SITE.
CLOSING_LEDGERS:140     the F16 row.
                        -- this is the SECOND DISTINCT BASENAME.  The target
                        says "2 distinct basenames, and BOTH are the
                        determination map's …", which cannot be true of two
                        distinct basenames.  It then cites the F16 routing as
                        CLOSING_LEDGERS:141; F16 is at :140, and :141 is F17.
                        WRONG LINE, AND SELF-INCONSISTENT.
```

**The conclusion nevertheless holds at bytes.** Site `:338` is a quantifier over
a class inside a one-arrow model. Sites `:374` and `CLOSING_LEDGERS:140` are
*statements of the void condition itself* — the falsifier's text, not an act of
selection. **No occurrence anywhere in either root is a selection.** The
regression row at `LANE1_V003:459` ("inspect the provenance of every overlap
automorphism | none selected; the strict equality is the adopted datum") and
`STAGE1_V003_REVIEW:292-293` both verify exactly.

**Grade: NOT-TRIGGERED — CONFIRMED**, on a corrected enumeration.

**Deciding file:line for COR-C:**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_TASK5_EQ6_DETERMINATION_MAP_LANE1_V001.md:374`.

### §5.1 The four, re-tabulated

| Void | Target grade | This lane | Correction |
|---|---|---|---|
| 1 | NOT-TRIGGERED | **NOT-TRIGGERED — CONFIRMED** | COR-B: the supporting universal negative ("no unequal image … anywhere") is false at bytes; basis restated |
| 2 | NOT-TRIGGERED | **NOT-TRIGGERED — CONFIRMED** | none |
| 3 | UNTESTABLE-AS-STATED | **UNTESTABLE-AS-STATED — CONFIRMED** | none adverse; two corroborating sites added (`PACKAGE_ADJUDICATION:243`, `ADOPTION_PROPOSAL_V005:482`) |
| 4 | NOT-TRIGGERED | **NOT-TRIGGERED — CONFIRMED** | COR-C: enumeration wrong at bytes in three particulars; conclusion survives |

---

## §6 — D5: `u_f` AND REACH (Q5)

### GRADE: **CONFIRMED-WITH-CORRECTIONS.**

### §6.1 `u_f` — NAMED-ONLY: **CONFIRMED EXACTLY**

Re-run independently, case-sensitive, delimiter-bounded, both roots, under the
exclusion array: **8 delimited matches / 4 file instances**, resolving to
**3 distinct lines in 2 distinct basenames**, plus 1 line in the ruling.
Identical to the target's S-10. The complete enumeration:

```text
DETERMINATION_MAP:340  "family `u_(gf)=u_g u_f`; the boundary discrepancy then
                        satisfies the same"                        [1 match]
DETERMINATION_MAP:357  the choice-table row                        [2 matches]
FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_REVIEW_DETERMINATION:111
                       "u_f(v)=integral_(gamma_f) 0=0"             [1 match]
DOR_020_A4:18          the declined alternative                    [1 line]
```

The choice-table row, `DETERMINATION_MAP:357`, quoted WHOLE:

> | twisted gluing | choose a covariant reality/unit-compatible automorphism family `u_f` on `C_N` and glue by `eta_boundary=u_f Eta_rep` | adds a contact-holonomy/member fiber | **YOURS** |

and the ruling, `DOR_020_A4:18`, quoted whole with its parenthetical:

> Twisted gluing (a contact-holonomy member u_f — new physical fiber; no current evidence of twist);

**The symbol-collision exclusion is correct.** `FIELD_SIGNATURE…:111`'s `u_f` is
a linear functional on visible tangents indexed by an edge — an "A4 current" in
that artifact's own current-labelling scheme, unrelated to DoR-020-A4. The
uppercase `U_f` unit transport (54 matches / 16 file instances, reproduced
exactly) is a different object. **`contact-holonomy` occurs 2 times, both the
mirrored copies of `:357`, plus once in the ruling; `contact holonomy` with a
space occurs 0 times.** Reproduced exactly.

**NAMED-ONLY is CONFIRMED without correction.** There is no construction, no
exhibited member, no existence claim, no properties theorem, and no site
anywhere in either root that uses `u_f` after the choice-table row.

### §6.2 Reach (i) — U(1)-valued transition functions with a triple-overlap cocycle: **UNRELATED — outcome confirmed, stated basis REFUTED (COR-E)**

**The object verifies.** `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md:27-43` was read
at bytes and the target's quotation is whole and exact, including the closing
sentence "It is not obtained by mistaking a passive basis change for a new
physical force." That artifact carries **zero** occurrences of `contact`, `u_f`,
`J12`, `C_N`, `I_contact`, `glue`, or `pushout` — verified directly, and the
target's claim is exact.

**The target's stated basis is not what the bytes show.** It writes:

> - Files carrying `triple-overlap`: 21 instances / 15 distinct basenames.
>   Files carrying a contact-technical term: 190 instances / 106 basenames.
>   Their intersection is THREE basenames, and all three are prior COMMISSION
>   artifacts that swept both vocabularies as search strings —
>   STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001,
>   STAGE8_DISCREPANCY_COCYCLE_O38SR_V001 and its AUDIT.  A sweep row listing
>   two tokens is not the corpus relating two objects.

Two defects.

**(1) The figures belong to a different pattern.** Measured here:
`triple-overlap` = 42 hits / **17** file instances / **13** distinct basenames.
`triple.overlap` (either form) = 62 hits / **21** file instances / **15**
distinct basenames. The target's "21 instances / 15 distinct basenames" are
`triple.overlap`'s numbers presented as `triple-overlap`'s — and its own sweep
row S-29 says 42/17, contradicting its §5.2 text.

**(2) The intersection is larger, and one member is not a sweep row.**
`triple-overlap` ∩ any `contact`, measured here, is **seven distinct basenames**
(8 file instances):

```text
STAGE8_BOUNDARY_SHAPES_O32SR_AUDIT_V001
STAGE8_CONSTRAIN_OR_CREATE_O36SR_V001
STAGE8_COUNTERFACTUAL_PROFILE_O35SR_V001
STAGE8_DISCREPANCY_COCYCLE_O38SR_AUDIT_V001
STAGE8_DISCREPANCY_COCYCLE_O38SR_V001
STAGE8_GAUGE_AS_CONTENT_O34SR_V001
STAGE8_POST_GATE_FORCING_AND_SELECTION_BOUNDARY_CENSUS_V001
```

Of these, `STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md` (seal OK) is **not** a sweep
row listing two tokens. It carries the U(1) transition-function structure in
full at `:134-138` and then, at `:290-298`, types the J13 boundary/contact
cocycle against it. Quoted WHOLE:

> **TYPED OUT: NOT AN OBJECT OF GAUGE TYPE.** It carries no U(1), no connection,
> no group value. It is therefore not graded on the gauge/EM distinction — it is
> not a hit under Question One's typing requirement at all. It fails a second time
> on data: `STAGE8_TASK5_CONTINUUM_PACKAGE_ADOPTION_PROPOSAL_LANE2_V005.md:482`
> (seal OK):
>
> ```text
> | J13 | boundary/contact cocycle and safe ideal | **CONDITIONAL THEOREM ONLY** | closure hull `(O1-13)` preserves a supplied cocycle; no actual P7 discrepancy family exists to close |
> ```

**Effect on the grade: none, and the reason must be stated exactly.** That
passage is a *negative typing* — the corpus separating the two objects — not a
relation of the kind reach test (i) asks for. A statement that the contact
cocycle "carries no U(1), no connection, no group value" does not relate a
contact holonomy to U(1)-valued transition functions; it denies that they are
of one type. **UNRELATED is CONFIRMED as an outcome.** What is refuted is the
target's stated ground that the only co-carrying artifacts are sweep rows.

This lane declines to adopt O34SR's determination as authority; it is reported
as bytes bearing on the target's stated basis, and nothing is concluded from it
beyond what its own sentence says.

**Deciding file:line for COR-E:**
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:290-291`
— the two lines carrying the deciding sentence, "It carries no U(1), no
connection, / no group value." (the sentence is split across `:290` and `:291`).
The quoted span above is `:290-298`.

**The target's narrower test does verify.** Files carrying
(`U(1)-valued` OR `transition function`) AND the PASS-C contact-glue object:
measured here as exactly **two** — the O38SR pair — as the target states.

### §6.3 Reach (ii) — a require-site on single-valuedness around a closed cycle: **UNRELATED — CONFIRMED**

Verified at bytes. `STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md:130` carries
require-row R-1 with "integer-quantized by single-valuedness" and "one condition
(single-valued closure) whose admissible set is" the integer lattice; R-3 at
`:132` carries "contact-kernel" as the fourth of four form obligations on a
crossing's target form — a different row, a different object, with no sentence
relating them.

The determination map's two occurrences verify exactly:

```text
:356  "| strict pushout gluing | impose `(D2-3)` and define
       `R_phys=R_rep plus_(C) I_contact` | no new contact twist; gives
       one-valued `Eta` | **YOURS** |"
:401  "A lane cannot silently treat single-valuedness as already adopted."
```

Both are single-valuedness **of a map on a quotient**, not monodromy-freeness
around a loop. The target declares this "a genuine near-miss" and declines the
identification in the open. This lane tested the opposite reading and reaches
the same place: `:356`'s "one-valued `Eta`" is the descent property of the
pushout, and neither line mentions a cycle, a loop, or a closed path. **No
correction.**

### §6.4 Reach (iii) — charge-type or phase structure: **UNRELATED — CONFIRMED**

`T14SR:131` (R-2, U(1) characters with cocycle/transition covariance) and
`LOCAL_PROJECTIVE_RECORD_BUNDLE_V001:81-92` (holonomy and character section)
verify at bytes. No relating sentence to a contact holonomy or to `u_f` was
found by any of the patterns run in §4.2 or §7.

**The near-analogue is correctly reported and correctly separated.**
`STAGE8_SOURCE_COUPLED_RECORD_TRANSITION_LAW_ADOPTION_PROPOSAL_V002.md:29-32`,
`:141-145`, `:149-160` and `:162-164` all verify whole at bytes, including the
scope limit "It does not quantify over a different endpoint-charge package or
over new parent, curvature, closed-loop, state, effect, metric, continuum, or
contact data." The target's decision to report it in full while declining the
identification is the right handling of a same-word-different-object hazard, and
its reasoning ("Reporting it as a relation would be an import; suppressing it
would be a clipped sweep") is sound. **No correction.**

**One nearby datum, reported for completeness.**
`STAGE8_TASK5_EQ6_PASS2_CHECK_AND_ALLRANK_TYPING_LANE1_V001.md:108` verifies at
bytes as the target reports, and its "identity U(1) bundle/field transport" is
the corpus's only actual built member carrying a U(1) transport. The same
artifact's `:475-483` contact section is a "contact-only leakage attack"
concluding "neither the identity field horn nor recorded contact closes the
gap". Different sections, different objects, no relating sentence. The target
handled this correctly.

### §6.5 Q5 re-tabulated

| Item | Target | This lane | Correction |
|---|---|---|---|
| `u_f` defined? | NAMED-ONLY | **NAMED-ONLY — CONFIRMED** | none; counts reproduce exactly |
| Reach (i) | UNRELATED | **UNRELATED — CONFIRMED** | COR-E: stated basis refuted; outcome stands |
| Reach (ii) | UNRELATED | **UNRELATED — CONFIRMED** | none |
| Reach (iii) | UNRELATED | **UNRELATED — CONFIRMED** | none |

---

## §7 — D6: THE BARS

### GRADE: **CONFIRMED.**

The hard bar on this commission is absolute. The target was scanned pattern by
pattern for every prohibited move. Results, each run:

```text
VOIDING / DECLARING VOID / RECOMMENDING VOIDING
  Pattern `is void|be voided|voids the member|declare .{0,10}void|hereby`
  -> 2 hits.
  :15  the artifact's own disclaimer ("does not void it, does not declare it
       void, does not recommend voiding").
  :697 inside a BLOCKQUOTE reproducing STAGE1_V003_REVIEW:294 ("A future actual
       `Tw_f!=0` immediately voids the member.") -- quoted corpus text, verified
       verbatim against bytes, not the artifact's own act.
  NO VIOLATION.

RECOMMENDATION TO REOPEN
  Pattern `reopen` -> 2 hits, both the artifact's own disclaimers (:16, :1544).
  NO VIOLATION.

PRESCRIPTION / WHAT SHOULD HAPPEN NEXT
  Pattern `\bshould\b`                                    -> 0 hits.
  Pattern `next step|going forward|must now|ought|we propose|propose that`
                                                          -> 0 hits.
  Pattern `this lane recommends|record should|must be (built|supplied|authored)`
                                                          -> 0 hits.
  NO VIOLATION.

AUTHORED OBJECT
  Pattern `^ *(Define|Let|Put|Set|We define|We construct|Construct) `
                                                          -> 0 hits.
  Every displayed equation in the target sits inside a blockquote attributed to
  a named file and line range; all were checked against bytes.  Nothing is
  defined, constructed, or supplied.
  NO VIOLATION.

REGISTER BAR
  7 occurrences of barred basenames, ALL of them the artifact restating the bar
  itself (:1217 the LEAK-counter definition; :1444-1446 the bar list;
  :1456-1458 the deliberate check that STAGE8_7A_HANDOFF_PACKAGE_V001 is not
  `THE_HANDOFF*`).  That check is correct: the basename does not begin
  `THE_HANDOFF` and contains neither REGISTER nor TRACKER.
  No barred file was opened, quoted, or counted.
  ZERO INCIDENTS.

FENCES
  `alpha=alpha_0` at :469 is inside a blockquote reproducing EXCISION_CERT's
  `(H2-4)` condition list; no value is assigned or computed.  Every other
  number in the target is a count or a SHA-256 digest.
  FENCE LINE INTACT.

CATALOGUED NEGATIVES
  The two O38SR artifacts and BOUNDARY_SHAPES_O32SR_AUDIT are cited only as
  members of a co-occurrence intersection -- i.e. as objects counted, never as
  authority for a conclusion.  The subextensivity finding is re-derived from
  primary sources (CONSTRAINT_ARM:457, HUNT_CROSS_CHECK:225, ADOPTION_PROPOSAL
  V002:449/V003:443/V004:166, DETERMINATION_MAP:316) and this lane reproduced
  that derivation independently at §5.
  NO VIOLATION.
```

### COR-H — one near-miss, reported rather than counted

`STAGE8_TWIST_EVIDENCE_O40SR_V001.md:541`, quoted whole with its following
sentences:

> **Also weighed and not sufficient.** `PASS2_CHECK_AND_ALLRANK_TYPING:108`
> records a **built** member — "E1 built member, geometry first | **PASS** |
> actual two-path cycle, nonzero conserved current, identity U(1) bundle/field
> transport, and all geometric regressions pass". This is an actual built
> surface member, and it is the nearest thing in the corpus to actual data. It
> carries **no contact datum**…

The bar prohibits "any characterisation of the evidence as sufficient or
insufficient for an act". Read whole, this heading characterises whether one
candidate datum suffices to count as actual **contact** data — a sufficiency
internal to the Q2 finding, not a sufficiency for any act. The artifact twice
disclaims the barred characterisation in terms (`:16-17`, `:1544-1545`).
**Reported as a wording near-miss; NOT counted as a violation.**

**D6 verdict: CONFIRMED.** No voiding, no recommendation to void or reopen, no
characterisation of evidence as sufficient or insufficient for an act, no
authored object, no catalogued negative re-read as authority, zero register-bar
incidents, fences intact.

---

## §8 — CORRECTIONS, IN SEVERITY ORDER, AND EVERY CORRECTED GRADE

| # | Correction | Deciding file:line | Dimension | Grade effect |
|---|---|---|---|---|
| **COR-A** | The target's claim that "an actual intersection" is "the strongest pro-existence clause in the corpus" is false at bytes. A strictly stronger clause — *"For the declared finite boundary/contact class, extend by its already-built transport"*, said of `eta_f^boundary` itself — sits unquoted **inside the build's own adopted universe**. It is struck by the review of record (`STAGE1_V002_REVIEW:46`, `:286-295`), so the grade survives; the omission breaches the quotation-integrity discipline on the deciding question. | `…JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V002.md:279-280` | D2 | none |
| **COR-B** | "Two twists ARE exhibited" (Q3) and "No unequal image is exhibited anywhere in either root" (`:766`, `:908`) are both false at bytes. At least four exhibits exist; two — the boundary-transport alteration and the boundary composition counterexample — each exhibit unequal images on an overlap element. The falsifying sentence is broken across a mid-line wrap (`different` / `images.`), so a line-based sweep for the phrase returns 0. All four fail A1/A2/A3, so both grades survive. | `…STAGE1_V002_REVIEW_LANE1_V001.md:283-284` | D3, D4(void 1) | none |
| **COR-C** | The VOID-4 provenance enumeration is wrong at bytes in three particulars: the `u` quantifier is at `:338` not `:331`; the second determination-map site is the void-condition-4 statement at `:374`, not the choice-table row `:357` (which does not carry the token); and the second distinct basename is `CLOSING_LEDGERS:140`, mis-cited as `:141` and contradicted by the sentence "both are the determination map's…". The conclusion — no occurrence is a selection — holds on all three sites. | `…DETERMINATION_MAP_LANE1_V001.md:374` | D4(void 4) | none |
| **COR-D** | Universe under-scoping, contra §0.1's explicit defence. PASS C reproduces exactly (273/40/21), but the adopted universe cannot see four sealed artifacts carrying load-bearing content, including one that defines a "response/contact transport" and marks it **BUILT**, and the *highest* version of an adoption-proposal series the build quoted three times. All four corroborate the grades. | `…JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001.md:604` | D2 | none |
| **COR-E** | Reach (i)'s stated basis is refuted. The target gives `triple.overlap`'s figures (21/15) as `triple-overlap`'s (actually 17/13), contradicting its own row S-29; and its intersection is seven distinct basenames, not three, one of which is not a sweep row but a substantive typing determination placing the J13 boundary/contact cocycle against the U(1) structure. The UNRELATED **outcome** stands — a negative typing is not a relation. | `…STAGE8_GAUGE_AS_CONTENT_O34SR_V001.md:290-298` | D5 | none |
| **COR-F** | Citation defects, off-by-one class. (i) "the unbuilt contact transport" is at `DETERMINATION_MAP:88`, cited `:87`. (ii) `CLOSING_LEDGERS:138-142` is cited for "all five A4 voids"; they are F13–F17 at `:137-141`, and `:142` is F18, an **A5** void. (iii) `CLOSING_LEDGERS:141` is cited for the F16 routing; F16 is at `:140`. (iv) §1.1's quotation of determination-map §3.1 includes the heading at `:285` while citing `:286-308`. Every quotation itself is verbatim. | `…STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md:137-142` | D1, D4 | none |
| **COR-G** | §9's defect D-3 understates its own scope: `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` is unsealed in **both** roots, not only ROOT1. The two copies are byte-identical (`b96bef557c150015cf9c0c523f63f4eb53ffe45051b01db9d8b86fe109805645`), verified here. | `…/alpha_fundamental_record_action_cleanroom_v003/LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` | D2, D5 | none |
| **COR-H** | Wording near-miss at `:541` — "**Also weighed and not sufficient.**" uses the exact register the hard bar reserves. Read whole it is sufficiency for a finding, not for an act. Reported, not counted as a violation. | target `:541` | D6 | none |

### EVERY CORRECTED GRADE, STATED EXPLICITLY

```text
Q1  actuality standard        CONFIRMED-WITH-CORRECTIONS
                              -> the standard STANDS AS THE CORPUS'S, quoted
                                 whole from one contiguous span; corrected on
                                 the three-limb formalization (COR-F1), the
                                 dropped "only" (COR-F2), and one line cite.

Q2  SPECIFIED-NOT-CONSTRUCTED CONFIRMED  (grade unchanged)
                              corrections COR-A, COR-D attach to its basis.

Q3  NO-ACTUAL-DATA-TO-TEST    CONFIRMED  (grade unchanged)
                              correction COR-B attaches to its enumeration.

Q4  void 1  NOT-TRIGGERED     CONFIRMED  (grade unchanged; basis restated,
                                          COR-B)
    void 2  NOT-TRIGGERED     CONFIRMED  (no correction)
    void 3  UNTESTABLE-AS-STATED
                              CONFIRMED  (no adverse correction; two
                                          corroborating sites added)
    void 4  NOT-TRIGGERED     CONFIRMED  (grade unchanged; enumeration
                                          corrected, COR-C)

Q5  u_f     NAMED-ONLY        CONFIRMED  (no correction; counts reproduce
                                          exactly)
    reach (i)   UNRELATED     CONFIRMED as OUTCOME; stated basis REFUTED
                                          (COR-E)
    reach (ii)  UNRELATED     CONFIRMED  (no correction)
    reach (iii) UNRELATED     CONFIRMED  (no correction)

NO GRADE IN THE TARGET IS OVERTURNED.
```

---

## §9 — CHOICE LEDGER

| # | Choice made | The road not taken | Why | Status |
|---|---|---|---|---|
| CH-1 | Build an independent exclusion array of twelve `--exclude` elements (ten bar patterns, two self-exclusions) and verify firing before any count | Reuse the target's declared six-element array | The commission warns that a prior lane's multi-glob passed as one string silently stopped working. Firing was tested both ways: 0 barred basenames returned with the array, 61 barred-named entries present without it | **CLOSED** |
| CH-2 | Re-derive PASS C from its eight tokens rather than accept the target's universe | Audit inside the target's declared 23 basenames | The dimension requires the declared universe be checked. PASS C reproduced exactly (273/40/21) — and the reproduction is what exposed COR-D, since the check also showed which load-bearing artifacts those tokens cannot see | **CLOSED** |
| CH-3 | Hunt with patterns the target did not run (`Y2-4`, `D2-3`, `J12-5`, `overlap equality`, `nonzero contact`, `nontrivial contact`, `contact element`, `already.built`, `actual contact`, `contact transport`, `different images`) | Re-run the target's 54 rows and stop | D3 instructs the audit to run the patterns the build omitted. Two of these — `nonzero contact` and `already.built` — returned COR-B and COR-A. Re-running only the target's rows would have found neither | **CLOSED** |
| CH-4 | Report the third and fourth twist exhibits but grade them NOT ACTUAL, leaving Q3 CONFIRMED | Grade Q3 REFUTED on the strength of two new exhibits | Both were run against the §2 actuality standard limb by limb and fail all three. A model exhibit is not actual data, which is the target's own — and the corpus's own — distinction. Grading REFUTED would have imported an identification the corpus declines | **CLOSED** |
| CH-5 | Decline to treat `rho_f^C2` (`LANE2_V001:242`) as the missing contact transport | Report a constructed contact transport and refute Q2 | `LANE2_V001` carries zero occurrences of `I_contact`, `C_N`, `eta_f^boundary`, `Eta_f^rep`, `R_N^phys`, `contact glue`, `J12-G`, `Tw_f`; and `:250-252` scopes `(C1-7)` to the old image, saying it "says nothing false about the target-only new-cycle component". Identifying the two would be an import | **CLOSED** |
| CH-6 | Report `GAUGE_AS_CONTENT_O34SR:290-297` as bytes bearing on COR-E while declining to adopt its determination | Either (a) count it as a RELATED site and refute reach (i), or (b) omit it as a catalogued negative | (a) would convert a negative typing into a relation, which it is not; (b) would repeat the clipped-sweep failure. Reported and separated, with its verdict not taken as authority | **CLOSED** |
| CH-7 | Grade COR-A a correction rather than a refutation of Q2 | Refute Q2 on "already-built transport" | The clause is struck by the review of record at `STAGE1_V002_REVIEW:46` and `:294` (`ETA_ON_R_PHYS = NOT_WELL_DEFINED_WITHOUT_INTERSECTION_CERTIFICATE`). The grade is right; the omission is a discipline failure, and is graded as one | **CLOSED** |
| CH-8 | Treat the malformed `LANE1_V004.md.seal.sha256` as a formatting defect and continue | STOP the commission on "no properly formatted SHA checksum lines found" | Independently reconfirmed: the recorded digest equals the file's actual digest in both roots. STOP is reserved for a content mismatch, of which there are zero | **CLOSED** |
| CH-9 | Chase the two sweep-count divergences from the target to an exact cause rather than record them as unexplained drift | Report them as an unresolved 3-hit/2-hit discrepancy, as the target did for its own `holonom` drift | `twist` measures 318/69 here against the target's 315/68, and `monodrom` 53/19 against 51/18. **Cause found and it is exact: `STAGE8_REQUIRE_TYPOLOGY_O39SR_AUDIT_V001.md` (ROOT1, mtime 20:34, i.e. AFTER the target sealed at 20:26) carries exactly 3 `twist` and exactly 2 `monodrom` occurrences in one file.** 315+3=318 / 68+1=69, and 51+2=53 / 18+1=19. Both divergences close to zero residue. The target's numbers were correct at its own cutoff; the corpus moved underneath | **CLOSED** — see §11 CORPUS DRIFT |
| CH-10 | Report COR-H as a near-miss, not a bar violation | Count `:541` as a violation of the hard bar | Read whole, the sentence characterises one datum's sufficiency for the Q2 finding, not the evidence's sufficiency for an act, and the artifact twice disclaims the barred characterisation in terms. Counting it would be a finding manufactured from a word | **CLOSED** |

---

## §10 — TOY_SEPARATION

**THE ACTUAL SURFACE, NOT A TOY.** Every quoted sentence in this audit is from
a file in the corpus at the line range stated, re-read at bytes after drafting.
Every such file is sealed and was verified by `shasum -a 256 -c` from its own
directory, **with one stated exception: `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md`
carries no sidecar of any kind in either root** (COR-G); its digest is recorded
in §12 so the §6.2 quotation remains checkable, and no grade rests on it. No
object is constructed here. No model is built here. No missing object is
authored here — not in a toy, a footnote, or an aside.

**THE CORPUS'S OWN TOYS, NAMED AND SEPARATED.** This commission's central
question is exactly this separation, so the list is stated in full and extended
by the two exhibits this lane added.

| Corpus construction | What it is | What it is NOT | The corpus's own word for it |
|---|---|---|---|
| The one-arrow `u` model, `DETERMINATION_MAP:326-350` | a consistency/non-derivability device, `Eta^rep\|_C=id_C`, `eta^boundary\|_C=u` | actual contact data; an exhibited twist; a member | "a one-arrow **model**"; "without choosing a physical member" |
| The `u(c_f)!=c_f` descent counterfactual, `STAGE1_V003_REVIEW:279-282` | a failure-capability demonstration for the `Tw_f=0` detector | actual contact data | "**If** the boundary leg carried a twist" |
| **NEW — the boundary-transport alteration, `STAGE1_V002_REVIEW:280-284`** | a non-implication device against J13's cocycle | actual contact data | "Indeed **one can alter** …" |
| **NEW — the boundary composition counterexample, `PACKAGE_ADJUDICATION:232-244`** | a demonstration that pairwise subextensivity does not give path independence | actual contact data | "Boundary composition **counterexample**"; "P7 **can** assign" |
| The overlap-decomposition collision, `STAGE1_V002_REVIEW:406-411` | a named fresh attack on `(Y2-3)` | actual contact data | "Fresh **attack** 2" |
| The `span{r,c}`/`span{c,b}` computation, `STAGE1_V003_REVIEW:256-278` | an independent recomputation of pushout descent | an actual `C_N` | "Independent nontrivial-overlap computation" |
| The `A_epsilon` Hodge countermodel, `PROJECTOR_LOCALITY_REDUCING_CERT_V002:367-411` | proof that A4 does not imply contact-reducing | actual contact data (expressly) | "**countermodel**"; "**pre-A5 countermodel family**" |
| The direct-sum model, `EXCISION_CERT:41-42` | consistency witness for the certificate class | a physical inhabitant (expressly) | `DIRECT_SUM_MODEL_IS_PHYSICAL_INHABITANT = false` |

**SYMBOL COLLISIONS RE-CHECKED AT BYTES.** The target's four senses of
"contact", its `u_f`/`U_f` split, and its `twist` separation were each tested
and each holds. Two are load-bearing and were verified directly:

```text
"u_f"  (a) the declined contact-holonomy member, named only, at
           DETERMINATION_MAP:340 and :357 and DOR_020_A4:18;
       (b) an A4 CURRENT -- a functional on visible tangents indexed by an edge
           -- at FIELD_SIGNATURE_PHYS_V003_FINAL_ADVERSARIAL_REVIEW
           _DETERMINATION_V001:111, `u_f(v)=integral_(gamma_f) 0=0`.
           That artifact's "A4" is a current label, not DoR-020-A4.
"U_f"      a REQUIRED unit transport carrier map.  54 matches / 16 file
           instances, reproduced exactly.  Unrelated to (a).

"contact"  sense 1  the J12/J13 contact response/symbol subspace I_contact,N
                    and its overlap C_N.  THE OBJECT UNDER TEST.
           sense 2  the endpoint-contact DIAMOND GEOMETRY of the excision
                    certificate.
           sense 3  the one-sided multiplicative factor C(z) of the
                    source-coupled record transition law.
           sense 4  "contact term" in the Duhamel/interchange sense.
           A FIFTH, added by this lane: the "response/contact transport"
           rho_f^C2 of JOINT_FINITE_ASSEMBLY_STAGE1_LANE2_V001 -- a C2
           old-image response leg, carrying none of the J12 tokens (CH-5).
```

---

## §11 — SWEEP CUTOFFS

Every pattern below was RUN by this lane, on both roots, under this lane's own
exclusion array — not read off the target, not estimated, not taken from a
truncated display. LEAK = the number of returned paths whose basename matches
`REGISTER|TRACKER|THE_PLAN|ROAD_REMAINING|THE_HANDOFF|DECISION_SHEET`, computed
per row from that row's own result set.

**EXCLUSIONS, VERIFIED TO FIRE BEFORE ANY COUNT.**

```text
E-1  THE REGISTER BAR, passed as a shell ARRAY of TEN separate --exclude
     elements, never as one string:
       *QUESTIONS_SETTLED_REGISTER*  *QUESTIONSSETTLED_REGISTER*
       *EXECUTION_TRACKER*  THE_PLAN*  ROAD_REMAINING*  THE_HANDOFF*
       *OBSERVATIONS_REGISTER*  *DECISION_SHEET*  *REGISTER*  *TRACKER*
     TEST: enumerate every matching path under the array and count barred
     basenames -> 0.  Barred-named `*.md` present at the top level of the two
     roots without it -> 31 (61 entries of any kind, sidecars included).
     THE BAR FIRES.

E-2  SELF-EXCLUSION, TWO ELEMENTS: the TARGET
     (STAGE8_TWIST_EVIDENCE_O40SR_V001.md) and THIS ARTIFACT
     (STAGE8_TWIST_EVIDENCE_O40SR_AUDIT_V001.md), both of which live in ROOT1.
     Applied from the FIRST sweep, before any draft reached disk, because the
     target self-reports (SC-5) that omitting it inverted its own `Tw_f` and
     `contact-holonomy` counts.
     TEST: `TWIST_EVIDENCE_O40SR_AUDIT` under the array -> 0 hits.
     `TWIST_EVIDENCE_O40SR` -> 3 hits, ALL of them a THIRD artifact
     (STAGE8_REQUIRE_TYPOLOGY_O39SR_AUDIT_V001.md :82, :1037, :1204) referring
     to the target by name.  Inspected, not assumed.  THE SELF-EXCLUSION FIRES.
```

**METHOD.** HITS = `grep -rIhoiE` (case-insensitive) unless the row is marked
CASE-SENS, in which case `grep -rIhoE`. FILES = the `-l` count under the same
case discipline, counting **file instances across both roots**, so a mirrored
artifact counts twice. `--include='*.md'`, both exclusion sets, both roots.

| # | Pattern | Hits | Files | LEAK |
|---|---|---|---|---|
| A-1 | `twist` | 318 | 69 | 0 |
| A-2 | `contact.{0,12}twist` OR `twist.{0,12}contact` | 24 | 12 | 0 |
| A-3 | `CONTACT_TWIST_SEEN` — CASE-SENS | 10 | 6 | 0 |
| A-4 | `Tw_f` — CASE-SENS | 14 | 4 | 0 |
| A-5 | `contact-holonomy` — CASE-SENS | 2 | 2 | 0 |
| A-6 | `contact holonomy` (space) | 0 | 0 | 0 |
| A-7 | `contact automorphism` | 6 | 4 | 0 |
| A-8 | `monodrom` | 53 | 19 | 0 |
| A-9 | `u_f` delimited — CASE-SENS | 8 | 4 | 0 |
| A-10 | `U_f` — CASE-SENS | 54 | 16 | 0 |
| A-11 | `eta_f^boundary` / `eta^boundary` / `eta_boundary` | 46 | 12 | 0 |
| A-12 | `unbuilt` | 2945 | 859 | 0 |
| A-13 | `already.built` | 108 | 87 | 0 |
| A-14 | `contact transport` | 11 | 7 | 0 |
| A-15 | `actual contact` | 38 | 20 | 0 |
| A-16 | `nonzero contact` | 4 | 4 | 0 |
| A-17 | `nontrivial contact` | 6 | 6 | 0 |
| A-18 | `contact element` OR `contact class` | 38 | 26 | 0 |
| A-19 | `Y2-4` | 14 | 6 | 0 |
| A-20 | `D2-3` | 14 | 8 | 0 |
| A-21 | `J12-5` | 4 | 2 | 0 |
| A-22 | `overlap equality` | 33 | 13 | 0 |
| A-23 | `beta[_a-z]{0,3} ?/ ?Vol` (all notations) | 31 | 14 | 0 |
| A-24 | `subextensiv` | 373 | 168 | 0 |
| A-25 | `triple-overlap` | 42 | 17 | 0 |
| A-26 | `triple.overlap` (either form) | 62 | 21 | 0 |
| A-27 | `U(1)-valued` | 17 | 13 | 0 |
| A-28 | `transition function` | 94 | 37 | 0 |
| A-29 | `single-valuedness` | 66 | 26 | 0 |
| A-30 | `one-valued` | 2 | 2 | 0 |
| A-31 | `g_ij` | 166 | 37 | 0 |
| A-32 | PASS-C universe (the target's 8-token J12 contact-glue alternation) | 273 | 40 | 0 |
| A-33 | `different images` (line-oriented) | 0 | 0 | 0 |

**HEADLINE RECONCILIATION AGAINST THESE COUNTS.**

- **A-3/A-4/A-5/A-6/A-7/A-9/A-10 reproduce the target's S-3/S-4/S-5/S-6/S-7/
  S-10/S-12 exactly** (10/6, 14/4, 2/2, 0/0, 6/4, 8/4, 54/16). The target's
  detector rows and its `u_f` reach figure are sound at bytes. **A-25 through
  A-31 likewise reproduce S-29/S-31/S-33/S-34/S-37/S-39/S-32 exactly**, and
  **A-32 reproduces S-54 exactly** (273/40). The target's sweep table is
  accurate wherever this lane re-measured it, with the two exceptions below.
- **CORPUS DRIFT, RECORDED RATHER THAN SMOOTHED.** A-1 (318/69) and A-8
  (53/19) exceed the target's S-1 (315/68) and S-8 (51/18) by exactly 3 hits /
  1 file and 2 hits / 1 file. The cause is exact and was chased to zero
  residue: `STAGE8_REQUIRE_TYPOLOGY_O39SR_AUDIT_V001.md` landed in ROOT1 at
  20:34 — **after** the target sealed at 20:26 — and carries exactly 3 `twist`
  and exactly 2 `monodrom` occurrences. The target's numbers were correct at
  its own cutoff. Neither count is load-bearing for any grade in either
  artifact. **Recorded because a sweep run against a live corpus is not
  reproducible without its cutoff, and that fact belongs in the record.**
- **A-33 is the row that matters most.** `different images` returns **0 hits
  across both roots** on a line-oriented sweep, while the sentence "The two
  decompositions then receive different images." demonstrably exists at
  `STAGE1_V002_REVIEW:283-284`. The phrase is split by a line wrap. This single
  row is the mechanism behind COR-B, and it is the commission's warned-of
  hazard reproduced under measurement.
- **A-13 (`already.built`, 108/87)** is a large, noisy count used as evidence
  for nothing. It was run to locate a single sentence, which it did
  (`LANE2_V002:279`), and the finding rests on that sentence read whole, not on
  the count.
- **A-16 (`nonzero contact`, 4/4)** is the smallest substantive row in the
  table and returned two of the four twist exhibits. Small rows were read
  exhaustively; large ones (A-12, A-24) were used only to establish that a
  vocabulary is present, never as evidence of a relation.
- **A-23 vs A-24.** Subextensivity is discussed at 373 hits across 168 file
  instances; the ratio it is about occurs at 31 hits across 14, and all 31 were
  read and partitioned exhaustively at §5. **Discussed constantly, estimated
  nowhere.** This asymmetry is the void-3 finding and it is visible in the
  table itself.
- **A-25 vs A-26 is the byte basis for COR-E.** `triple-overlap` = 42/17 (13
  distinct basenames); `triple.overlap` = 62/21 (15 distinct basenames). The
  target's §5.2 prose reports "21 instances / 15 distinct basenames" for
  `triple-overlap`, which are A-26's figures, and contradicts its own S-29 row.
- **A-1 vs A-2.** 318 `twist` hits; only 24 sit anywhere near `contact`. This
  audit does not claim the corpus is twist-free. It claims no *actual contact
  datum* exists on which a contact twist can be tested. The ~294 non-contact
  hits are separated at §10 and are evidence for nothing here.

---

## §12 — FLAG BLOCK

**FENCE LINE.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false

No numeric value of any coupling, scale, root, eigenvalue, norm, or constant
was computed anywhere in this artifact.  No comparison to any measured value
was made.  No member was bound; no fixed point, end test, threshold, or
numerical evaluation was executed.  Every number appearing above is a COUNT
(of hits, files, lines, or basenames), a LINE NUMBER, a FILE SIZE/TIME, or a
SHA-256 digest.
```

**SEAL TALLY.** Every consumed artifact verified with `shasum -a 256 -c`
**run from that artifact's own directory**.

```text
TARGET
  STAGE8_TWIST_EVIDENCE_O40SR_V001.md                     OK
    .md.seal.sha256 and .md.sha256 both carry
    0ee7796e05d6088c0e5d166fafa230470dc6cbaa4023fbe4b8670e45c6109a63
    and both verify.  Recomputed digest matches the build's report.

CONSUMED CORPUS SET (36 basenames, resolved across both roots)
  file instances present                                  67
    verified OK by `shasum -a 256 -c` directly            63
    sidecar MALFORMED, recorded digest EQUALS actual       2
      (STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md,
       BOTH roots -- one space where `shasum -c` requires two;
       9bf34e27da9aca51966feb2b41f34b0060e2121bd85d783d35a81685bbb63514)
    NO SIDECAR OF ANY KIND                                 2
      (LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md, BOTH roots -- see COR-G)
  MISMATCHES                                               0

DOR SERIES (supervision, expressly permitted)
  DOR_020_A4_J12_CONTACT_GLUE_STRICT_PUSHOUT_2026-08-04.md   OK  (read WHOLE)
  DOR_020_A5_CONTACT_LAPLACIAN_REDUCING_2026-08-04.md        OK
  DOR_020_A2_COMPLETED_EXISTENCE_ADOPTED_2026-08-06.md       OK

BASIS-CITATION CHAIN, RECOMPUTED INDEPENDENTLY
  DoR-020-A4 cites its basis as DETERMINATION_MAP `76ee3c69…`.
  Recomputed: 76ee3c695b1c0c02986a13ff64d6db93f76e39c6861b40273bd31aed1c3a2eb0
  MATCHES.
  DOR_020_A4 itself recomputes to
  5fd95472bd6f0507a371779505fe91e2c3c4657ee3afc664ca563a3743d668a4,
  which is the digest STAGE1_V003_REVIEW:58 cites for "DoR-020-A4 J12 strict
  signed pushout" and LOC_FAMILY:89 cites for the same file.  MATCHES.
  THE CHAIN IS INTACT.  The target's basis check is CONFIRMED.

NO MISMATCH WAS FOUND ANYWHERE.  NO STOP CONDITION AROSE.
```

**DEFECTS IN THE RECORD (not in this draft).**

```text
D-1  STAGE8_TASK5_EQ6_JOINT_FINITE_ASSEMBLY_STAGE1_LANE1_V004.md.seal.sha256
     is MALFORMED in BOTH roots (one-space separator).  Content intact;
     digest matches in both roots.  Independently reconfirmed here.  The
     target's D-1 is accurate.  REPORTED, NOT REPAIRED -- this lane took no
     write action on any corpus file.

D-2  STAGE8_TASK6_CLOSING_LEDGERS_CODEX2_V001.md has no `.seal.sha256` in
     either root; a plain `.sha256` exists and verifies OK from the
     artifact's own directory in both roots.  The target's D-2 is accurate.

D-3  LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md carries NO SIDECAR OF ANY KIND
     in **BOTH** roots -- not only ROOT1 as the target's D-3 states (COR-G).
     The two copies are byte-identical:
     b96bef557c150015cf9c0c523f63f4eb53ffe45051b01db9d8b86fe109805645
     recorded here so the §6.2 quotation stays checkable.  No grade rests on
     it: reach (i) UNRELATED is established from the sealed contact
     artifacts' own side.

D-4  CORPUS DRIFT DURING A COMMISSIONED PAIR.  A sibling artifact
     (STAGE8_REQUIRE_TYPOLOGY_O39SR_AUDIT_V001.md) landed in ROOT1 at 20:34,
     after the target sealed at 20:26, and is unmirrored in ROOT2.  It shifts
     two of the target's sweep rows by 3 and 2 hits respectively (§11).
     Neither lane erred.  Flagged because it makes sweep rows in this program
     non-reproducible without a stated cutoff.
```

**REGISTER-BAR INCIDENTS: NONE.**

```text
Exclusion array of TEN bar elements plus TWO self-exclusions, passed as a
shell ARRAY, never as a single string.  Firing verified before the first
count and again before sealing: 0 barred basenames returned with the array,
31 barred-named `*.md` present at the top level of the two roots without it.
LEAK counter = 0 on every one of the 33 sweep rows, computed per row from
that row's own result set.  No barred file was opened, quoted, or counted.
Barred NAME PATTERNS appear in this artifact at exactly four places, all of
them statements of the bar rather than references to a barred file: the E-1
exclusion list and the LEAK definition (§11), and the two passages auditing
the target's own `THE_HANDOFF*` check (§7 and immediately above).  Checked by
grep against this file rather than asserted.

THE TARGET'S OWN BAR COMPLIANCE WAS AUDITED SEPARATELY (§7): its 7 mentions
of barred basenames are all its own restatement of the bar.  Its deliberate
check that STAGE8_7A_HANDOFF_PACKAGE_V001.md is NOT `THE_HANDOFF*` is correct
at bytes -- the basename does not begin `THE_HANDOFF` and contains neither
REGISTER nor TRACKER.  ZERO INCIDENTS in the target.

SUPERVISION DIRECTORY: only DOR_* files were opened -- DOR_020_A4 (read
whole and first), DOR_020_A5, DOR_020_A2.  Nothing else in
/Users/bgm/MB Work/alpha-program-archive/supervision/ was read.
```

**DEFECTS FOUND IN THIS LANE'S OWN DRAFT ON SELF-CHECK.**

```text
AC-1  MISATTRIBUTED CAUSE, CAUGHT BEFORE SEALING.  CH-9 first recorded the
      A-1/A-8 count divergence as caused by subdirectory copies
      (mirrored_sources, relay_inbox, relay_outbox) and left it there as
      unexplained methodological drift.  That was a guess dressed as a
      finding.  Chased properly, the cause is exact and closes to zero
      residue: one artifact that landed after the target sealed, carrying
      exactly 3 `twist` and exactly 2 `monodrom` occurrences.  CH-9 was
      rewritten and D-4 added.  An audit that accepts its own unexplained
      residue has no standing to correct another lane's.

AC-2  `timeout` DOES NOT EXIST ON THIS MACHINE (`which timeout` -> not
      found; `gtimeout` likewise).  The target self-reports the same trap at
      SC-1, where four wrapped sweeps returned exit 127 and printed nothing,
      reading as legitimate zeros.  No sweep in this audit was wrapped in
      `timeout`.  One consolidated sweep batch was killed by the harness at
      120s mid-run; the affected rows (A-17 onward) were RE-RUN from scratch
      in smaller batches and every figure in §11 comes from a completed
      command whose exit status was observed.

AC-3  SELF-EXCLUSION APPLIED FROM THE FIRST SWEEP, NOT RETROFITTED.  Because
      the target self-reports (SC-5) that its own file inverted its counts,
      both this artifact and the target were excluded before any measurement.
      The residual 3 hits for `TWIST_EVIDENCE_O40SR` were INSPECTED rather
      than assumed to be leakage; they are a third artifact naming the target.

AC-4  QUOTATION RE-CHECK.  Every blockquote above was re-read against bytes
      after drafting.  The spans at CERT_V003_REVIEW:120-131,
      CONSTRAINT_ARM:445-463, DETERMINATION_MAP:285-350 and :366-408,
      EXCISION_CERT:47-55/:128-129/:603-620, LANE1_V003:421-450/:454-460,
      STAGE1_V003_REVIEW:246-302, STAGE1_V002_REVIEW:44-49/:245-295/:406-411,
      LANE2_V002:266-301, LANE2_V001:230-259/:598-614,
      MEMBERSHIP_REVIEW_DARIO:248-290, MAXWELL_SYMBOL:476-492,
      CLOSING_LEDGERS:136-144, HUNT_CROSS_CHECK:214-232,
      PACKAGE_ADJUDICATION:225-265, V005:308-320/:478-486,
      ZERO_DEFECT_SECTION_BUILD:1-70/:386-410,
      LOCAL_PROJECTIVE_RECORD_BUNDLE:20-50, SOURCE_COUPLED_V002:26-34/:138-166,
      T14SR:128-134, PASS2_CHECK:106-110/:475-484, GAUGE_AS_CONTENT_O34SR:
      130-160/:288-300 and DOR_020_A4 (whole) were each displayed with line
      numbers and compared clause by clause.  No clipped quotation survived.

AC-5  ONE JUDGEMENT LEFT IN THE OPEN RATHER THAN RESOLVED.  Reach (ii) turns
      on whether DETERMINATION_MAP:356's "one-valued `Eta`" is the same
      property as single-valuedness around a closed cycle.  This lane tested
      the identification and declines it for the same reason the target does
      -- neither line mentions a cycle, a loop, or a closed path.  The
      declination is a judgement, it is recorded as one, and the bytes that
      would let a later reader disagree are displayed at §6.3.

NO OTHER DEFECT FOUND ON SELF-CHECK.
```

**WHAT THIS ARTIFACT DID NOT DO.** It did not void, declare void, recommend
voiding, or argue for reopening DoR-020-A4. It did not characterise the
evidence as sufficient or insufficient for any act. It did not author the
missing contact term, the missing subextensivity estimate, or the member `u_f`.
It did not re-read a catalogued negative as authority — where a prior
commission's artifact bore on a question here, it was read for bytes and its
determination expressly not adopted (CH-6). It took no git action. It took no
write action on any corpus file. It wrote to exactly one path.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
