# STAGE 8 / TRANSPORT LAW / O12 + O11 — THE PORT-ANCHORING DERIVATION AND THE SUCCESSOR INCIDENCE-SUM DISPLAY, FROM SEALED CONTENT

Lane: FABLE blind derivation, codename ANCHOR-DERIVE. The carrier constructor's first
fragment attempted from sealed content alone. Date: 2026-08-13.
**ALL_RESULTS = CLAIMED until checked.**

## Lead determination — CLAIMED

**THE ANCHORING LAW IS DERIVED; THE ANCHORING VALUE FORKS ON AN UNSUPPLIED TYPING.**
Exact adjoint algebra on the sealed one-cell gauge law and the sealed d_0 law proves
the ANCHORING EQUATION

```text
(*)   d_0^dagger ell_j  =  e_out - e_in
```

— a structure-preserving port-to-0-cell identification exists IF AND ONLY IF the two
ports sit at the boundary of the cell's read chain. O12 is therefore NOT a free
identification: it is a FUNCTION of O11's incidence chain, and the "license" K_dd's
ledger lacked has a derived FORM. But the VALUE of the anchoring forks on the one
datum no sealed text supplies — whether the per-cell holonomy `h_j` is LOOP-typed
(the entered Attach reading, closed chain) or EDGE-typed (open chain, the shape the
two-port law's nondegenerate covariance presupposes):

```text
ENTERED READING (TYPE-P, ell_j = gamma_j):  d_0^dagger gamma_j = 0  — the ports
  COLLAPSE onto one common 0-cell (any of the four, all equivalent: abelian
  basepoint-independence); the port action is INERT; M03's independent-branch
  endpoint characters are unrealizable at the complex level.  DERIVED-FAMILY,
  degenerate, zero structural freedom.
OPEN TYPING (unsealed demand):  the unique canonical anchoring is (in, out) =
  (v_00, v_11) — forced by (*) + the sealed source/sink census + the sealed
  forward/flow structure + the entered premise's own no-selector bar — and the
  read chain is then forced to the ONE-INTEGER family  ell_j = P1 + c*gamma_j,
  whose two orientation-respecting members are EXACTLY the two sealed forward
  paths.  DERIVED-UNIQUE conditional on the open typing + one disclosed
  canonicity demand.
UNCONDITIONALLY: which typing binds is NOT-DERIVABLE — the ratified law consumes
  h_j as given (GB S1) and M03's (t,s) are free covariance parameters, silent on
  realization.  HOLDER: an authored-physics entry (an Attach-strength successor
  supplying an open read, or a principal decision); no lane may select the horn.
```

O11: under the entered premise the successor display EXISTS and is exact —
`h_j = <A, gamma_j> = a_(e_a0) - a_(e_0b) + a_(e_ab) - a_(e_ba)` — at ENTERED
strength (voids with the Attach entry). Under the open horn the display is the
one-parameter family `<A, P1 + c*gamma_j>`. Unconditionally S1 stands. And one exact
structural identity falls out: `ker(d_0^dagger) on C_1(K_square) = Q*gamma_j` — **the
entered attach map reads precisely the anchoring-INVISIBLE component of the write;
the loop character is the ratio of the two forward-path characters at the SAME port
pair — common-port cancellation INTRA-CELL, the same sealed mechanism M03 exhibits
across the CTP branches.** That is WHY the entry could be made without ever anchoring
a port.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       No physical quantity computed, bounded, or evaluated; every displayed
       number is a sealed-text quotation or an exact structural integer.
```

---

## 0. Preflight, seals, and spans

Output name probed before any write: `STAGE8_ANCHORING_DERIVATION_V001.md` ABSENT
(artifact and sidecar). No register, tracker, plan, road, ledger(route), or lens file
read. Every digest below recomputed by `shasum -a 256` at path THIS session BEFORE
reliance; sidecars compared where present (all matched).

```text
TASKED SOURCES (all MATCH the tasked digests):
22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md            (POSED — O11/O12, traps T1-T7)
a5c71b2ac7cd198bbac188e865d139a71340f1728f72fe56384aa5d60489110c  STAGE8_TRANSPORT_LAW_POSE_CHECK_V001.md       (the pose check)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md           (GB — R4 two-port law, S1-S4)
2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md  (M03)
f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md          (K_dd + the gluing ledger L-1..L-5)
bb1b88ad22ad2746a59957936785e9e44e3c2c4c1abdf7cf417096ff982d08c4  STAGE8_D0_SQUARE_CERTIFICATE_V001.md          (the certified transport squares)
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                 (the entry of record)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md (the entered candidate)

LOCATED AND VERIFIED (cited inside verified sources; sidecar matched where present):
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md  (addendum authority chain)
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md            (G-4: the sealed port-license absence)
aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a  review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  (V011 PACKET MEMBER)
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md (top-level; NOT span-cited — T6)

V011 SPANS — ALL bound to the PACKET MEMBER aa7c6d49 only (trap T6 honored); each
byte span rehashed (sha256 of the span) THIS session before use:
[44595,44690)  c6cd568bec678df676737009466e3537dfe64fd6962fdb3200b9f90b9dc14eb5  the d_0 law: (d_0 lambda)_e = lambda_t - lambda_s ; d_1 d_0 = 0
[62567,62693)  45df9004911d8819c5c920a89658c1822ab7ddd9684fdbcef5f990725cf6c3c9  K_square census: vertices v_00,v_10,v_01,v_11; edges e_a0,e_0b,e_ab,e_ba with orientations
[62735,62805)  95bf6c03f831cdfeeca51ba95ab3cabfdbaa75eb8235f6863e4bc72bb5e279de  "The two forward paths": v_00 -e_a0-> v_10 -e_ab-> v_11 ; v_00 -e_0b-> v_01 -e_ba-> v_11
[62853,62892)  8da992752c641ed5e95f7d62b1aa4d84729f59d93a09bb7fec772c2305623ae3  the ordered loop holonomy u_ab u_a0 (u_ba u_0b)^(-1) = exp(i Phi)
[63009,63054)  479061faf26dbb8fb8c3615cdb37c3dfee588c4dc5d409008652805ac2c1a077  the per-edge rule partial_rho,e x = u_e x v_target - x v_source
[63102,63154)  24d0ec5718c7cc4b32374694df67fa89eb86ab1f4b9abd1b8a0f89b92b506212  "No filled 2-cell is included in the trace carrier."
[63368,63429)  07bebc79b46a6a5b622c800d7af4071fa96ce64cbfaee2df4c0668570e57381e  frozen bases C_0: (v_00,v_10,v_01,v_11), C_1: (e_a0,e_0b,e_ab,e_ba)
[10886,11127)  01455ee5893b679d40a55c44ecbe947fa7241c39160dc4188726702b1df4679e  the associated-bundle display: "The vertex lines form an associated Hermitian line bundle ... Vertex gauge maps are changes of local section of P."
```

No seal failed; none unverifiable. M03 content is cited at its line numbers as read
this session at the verified digest (:121-150, :177-181, :235-248, :321-354, :516).

FENCES HELD: derivation from sealed content only; exact structural algebra
(integer/Fraction/Laurent-monomial; no numeric physical value anywhere); no scale, no
imported GR, no faithfulness premise; C_ref carried as frozen AUDIT class (T1); the
charter transport never used as authority for this law (T3); no R33/naturality
premise consumed (T5); the lever-illusory status carried, no stitching cross-booking
(T4); the FR relay-751 negatives carried WITH the later partials (T7). CIRCULARITY
HUNTED FIRST, disclosed in §6: the one datum the derivation must not silently supply
— the open/loop typing of h_j — is carried as the FORK, never assumed.

---

## 1. The sealed raw material, quoted at spans

```text
Q1  THE TWO-PORT LAW (M03 :321-354; GB R4 carried).  "For one oriented cell, let a
    gauge change act on the holonomy character by z^g = t z s^dagger.  The ratified
    post endpoint representations are G_out(t) = D(t), G_in(s) = S D(s) S.  Exact
    multiplication gives W_post(z^g) = G_out(t) W_post(z) G_in(s)^dagger."  And:
    "independent transformations of the two open histories carry relative endpoint
    characters and are not silently identified with the common transformation"
    (M03 :352-354).  GB R4 (verified 3e35ffe2): "exactly TWO gauge ports per cell —
    one in, one out"; single-branch boundary d_0^dagger ell_j = e_out - e_in already
    assembled there as YOURS-exact; "port-to-0-cell anchoring UNSEALED" (GB S3).

Q2  THE ONE-CELL WRITE (M03 :121-133, :177-181, :235-248).  S = [[0,1,0],[1,0,0],
    [0,0,-1]]; D_n[a] = diag(1, z, 1); W = D S;  D_n[a] S |r> = z_n[a] |p_Q> —
    the write is ready -> pointer and the character sits on that amplitude alone;
    the eraser entry -1 is connection-independent.  h_j[a_j] is consumed as given
    ("admitted finite histories", M03 :516; GB S1).

Q3  THE d_0 LAW AND THE INCIDENCE SIGN (V011 packet [44595,44690), [63009,63054)).
    (d_0 lambda)_e = lambda_t - lambda_s ;  the sealed per-edge rule
    partial_rho,e x = u_e x v_target - x v_source — incidence -1 at source, +1 at
    target;  "Vertex gauge maps are changes of local section of P" and rho_joint
    puts "relative phases on the associated vertex fibers" ([10886,11127)): gauge
    0-cochains act AT VERTICES of record.

Q4  THE ENTERED CELL AND READING (V011 packet [62567,62693), [62735,62805),
    [62853,62892), [63368,63429); Attach V007 ccca6bb4 :162-172, :633-649; entry
    420ab02f).  K_square: v_00,v_10,v_01,v_11; e_a0: v_00->v_10, e_0b: v_00->v_01,
    e_ab: v_10->v_11, e_ba: v_01->v_11; frozen bases; the two forward paths
    v_00 -> v_11; the ordered loop word gamma_j = +e_a0 - e_0b + e_ab - e_ba (F08
    orientation); the ENTERED map a_j := theta_j(A) = the circuit holonomy on
    gamma_j, chi_n at n = +1; operative domain b_1 = 1; the no-selector bar in the
    entry's own words: "choosing among them would be exactly the selector I am
    forbidden to freeze" (V007 :159-160).  The entered receiver R_j(z) =
    1_(L_s) (+) z*1_(L_t), E_j(e_s) = |r>, E_j(e_t) = |p_Q> (V007 :639-641).

Q5  THE SEALED LICENSE ABSENCE (hunt 1892c08e G-4 :196-206).  "NOTHING licenses
    identifying any out-port with any in-port (cell-to-cell OR cell-to-itself),
    and nothing forbids it: the law is a per-cell covariance statement, silent on
    gluing."

Q6  THE LEDGER (MIN-CARRIER f1881511 §7).  L-1 four vertex identifications
    (O12-shaped); L-2 three edge-read identifications (O11-shaped); L-3 the
    authored duplicate 1-cell e_0b'; L-4 the response role import; L-5 connected
    composition (O-D1-shaped).  All performed, none licensed.

Q7  THE CERTIFIED SQUARES (D0-SQUARE bb1b88ad).  sd*_1 . d_0' = d_0 . sd*_0 exact
    over Z on all licensed generators; canonical sd*_0 = vertex-copy; freedom =
    ker d_0 = the constants.  (Consumed only as context: transport-stability of
    im(d_0) at class level; nothing here rides it.)
```

---

## 2. DELIVERABLE 1 — O12: THE ANCHORING

### 2.1 The anchoring equation, derived (the lever worked)

**What an anchoring IS, unfolded from sealed structure.** A port-to-0-cell anchoring
is an assignment `P: {in, out} -> V(K)` such that the sealed vertex gauge action
(Q3: 0-cochains act at vertices; the connection shifts a -> a + d_0 lambda) realizes
the sealed port law (Q1) with

```text
t = chi_n( g at P(out) ),    s = chi_n( g at P(in) )        [structure preservation]
```

for every gauge 0-cochain. Let `ell_j` be the 1-chain the cell's holonomy reads
(h_j = <A, ell_j>; O11's datum, here carried as an unknown). Exact algebra:

```text
theta^g = <A + d_0 lambda, ell_j> = theta + <d_0 lambda, ell_j>
        = theta + <lambda, d_0^dagger ell_j>                 [adjoint, exact: B1]
z^g     = z * exp( i n <lambda, d_0^dagger ell_j> ).
```

Matching the sealed law `z^g = t z s^dagger = z * exp( i n (lambda_P(out) -
lambda_P(in)) )` for ALL lambda forces, and is forced by,

```text
(*)   d_0^dagger ell_j = e_P(out) - e_P(in).
```

**DERIVED (exact; script B1, B2): an anchoring is structure-preserving iff (*) —
the ports are the boundary of the read chain, nothing else and nowhere else.** Three
immediate exact corollaries:

```text
(i)   TYPING of e_out - e_in: a 0-CHAIN of the record complex — integer
      coefficients (+1, -1), augmentation zero; the +1 sits at OUT = target and
      the -1 at IN = source, exactly the sealed incidence sign (-1 source / +1
      target, Q3).  The sealed law's own boundary display already types the ports
      as 0-cell-shaped; (*) says WHICH 0-cells: the endpoints of ell_j.
(ii)  O12 IS A FUNCTION OF O11.  Given the incidence chain, the anchoring is
      forced with NO residual freedom whenever d_0^dagger ell_j = e_a - e_b with
      a != b; the "identification" is not an act at all once the chain exists.
(iii) THE PORT-CHARGE MATCH: the anchored port characters are the n-th power
      characters chi_n of the vertex gauge values — the ports carry the cell's
      own charge n; at the entered n = +1, t = exp(i lambda_out).
```

This dissolves Q5's "nothing licenses / nothing forbids" into a determinate
condition: a port identification is LICENSED exactly when it is the boundary of a
displayed read chain, and FORBIDDEN otherwise.

### 2.2 The realized port representations (exact multiplication)

```text
G_in(s) = S D(s) S = diag(s, 1, 1)      [script A1 — exact]
G_out(t) = D(t)    = diag(1, t, 1)
```

The IN-port character acts on the READY line |r>; the OUT-port character on the
POINTER line |p_Q>; the eraser line is port-silent. The covariance
`W_post(t z s^dagger) = G_out(t) W_post(z) G_in(s)^dagger` holds identically in
(t, s, z) with the ports visible ONLY on the ready->pointer write entry (script A2 —
exact). Corroborating nomenclature, quoted not consumed: the entered receiver's own
lines are L_s (root, |r>) and L_t (successor, |p_Q>) — the s/t names of the sealed
ports (Q4). The record's flow typing is therefore: **in-port = ready/root side,
out-port = pointer/successor side; the cell transports FROM its in-0-cell TO its
out-0-cell in the write direction.**

### 2.3 The fork: what (*) does under each typing of h_j

**(a) THE ENTERED READING (TYPE-P on the Attach entry): ell_j = gamma_j.** The
sealed loop is CLOSED: `d_0^dagger gamma_j = 0` (script B2, B5). Then (*) forces
`e_P(out) = e_P(in)`: **the two ports anchor to ONE common 0-cell.** Every choice of
that 0-cell (four members) satisfies (*), and all four are equivalent — the abelian
loop holonomy is basepoint-independent, so the family carries ZERO structural
freedom (a naming freedom only). The realized (t, s) action collapses to the
diagonal and acts TRIVIALLY on z (B5): the port structure is INERT. Consequences,
exact:

```text
- M03's covariance certificates (C4) hold TRIVIALLY — no contradiction with any
  sealed identity.
- M03 :352-354's independent-branch relative endpoint characters are UNREALIZABLE
  at the complex level under the entered reading: transforming a_+ alone leaves
  z_+ invariant (B5), so no endpoint character is ever carried.  The sealed
  sentence retains content only at the abstract character level where (t,s) are
  free parameters.
- WHY the collapse: gamma_j = P1 - P2 (script B2) and under EVERY gauge generator
  the two forward-path readings shift EQUALLY (same port pair; script B6) — the
  loop character is the RATIO z_P1 * conj(z_P2) and the endpoint characters
  cancel port by port.  This is COMMON-PORT CANCELLATION INTRA-CELL — the
  identical mechanism M03/GB R4 exhibit ACROSS the CTP branches, here operating
  between the cell's two sealed forward paths.  The entered attach map is
  structurally a two-branch same-port conjugated read INSIDE one cell.
```

VERDICT (a): **DERIVED-FAMILY(degenerate)** — the anchoring exists, is forced by
(*) to the common-0-cell form, its four members are equivalent, and it carries no
gluing information.

**(b) THE OPEN (EDGE) TYPING — supplied, not sealed.** IF the two-port covariance is
demanded NONDEGENERATE (the (t,s) action on z faithful on t s^dagger), then (*)
forces `d_0^dagger ell_j != 0`, i.e. an OPEN read chain, and the anchoring question
becomes: which ordered vertex pair? Exact facts:

```text
STEP 1 (exact, B8): (*) alone does not select — on the connected K_square every
  one of the 12 ordered vertex pairs bounds some chain.
STEP 2 (exact, B7): the sealed oriented 1-skeleton has a UNIQUE SOURCE v_00
  (in 0 / out 2) and a UNIQUE SINK v_11 (in 2 / out 0); v_10, v_01 are
  pass-through (1/1).  The only ordered pair the sealed text DISPLAYS as the
  endpoint pair of a cell transport is (v_00, v_11) — displayed twice ("the two
  forward paths", Q4) and jointly (the loop word is their comparison).
STEP 3 (flow match, exact signs): in-port = source slot (the -1 of (*), the s of
  z^g = t z s^dagger, the ready/root line by §2.2); out-port = target slot (+1,
  t, pointer/successor).  The assignment  in = v_00, out = v_11  matches every
  sealed convention; the reverse would read the cell against every sealed edge
  orientation and against the ready->pointer write order (stage order read off
  the ratified law, V007 §3.3).
STEP 4 (the canonicity demand — DISCLOSED as supplied): an anchoring derivable
  without an authored selection must land on sealed-displayed, structurally
  distinguished vertices; pairs involving v_10 or v_01 (and sub-chain reads such
  as ell = e_a0 with ports (v_00, v_10)) are excluded ONLY by this demand plus
  the entry's own sealed no-selector bar (Q4) — NOT by exact sealed algebra.
  This leg is the supplied premise of the uniqueness claim and is flagged as
  such.
STEP 5 (exact, B4): with (in, out) = (v_00, v_11) fixed, the full solution set of
  (*) is the one-integer affine family
      ell_j = P1 + c * gamma_j ,   c in Z ,   P1 = e_a0 + e_ab ,
  every member having the SAME boundary (the anchoring is c-independent), the
  orientation-respecting members being EXACTLY c = 0 (P1) and c = -1 (P2 = the
  other sealed forward path) — the two sealed forward paths and nothing else.
  Choosing between P1 and P2 is the barred selector; c is the exact residue.
```

VERDICT (b): **DERIVED-UNIQUE conditional** — (in, out) = (v_00, v_11), unique
given (i) the open typing and (ii) Step 4's canonicity demand; both conditions
disclosed.

**(c) UNCONDITIONALLY.** Which horn binds is NOT in the sealed text: the ratified
law consumes h_j as given (Q2/GB S1); M03's (t, s) are free parameters of a
covariance law and M03 defines no complex-level gauge action at all (one cell, no
complex); the entered premise supplies the loop horn at entered-authored strength
with the forcing slot open (`future_derivation_can_select_the_attach_map =
NO_VERDICT`, 420ab02f). The two horns are MUTUALLY EXCLUSIVE as realizations of one
h_j (a chain's boundary is zero or not). VERDICT (c): **NOT-DERIVABLE(gap = the
loop/edge typing of h_j; HOLDER = an authored-physics entry — an Attach-strength
successor supplying an open read chain, or a principal decision; not a lane act).**

---

## 3. DELIVERABLE 2 — O11: THE SUCCESSOR INCIDENCE-SUM DISPLAY

```text
UNDER THE ENTERED PREMISE (TYPE-P; voids with the entry):  DISPLAYED, exact —
    h_j[a_j] = theta_j(A) = <A, gamma_j> = a_(e_a0) - a_(e_0b) + a_(e_ab) - a_(e_ba)
  an explicit incidence sum over the entered cell's four named edges with integer
  weights (+1, -1, +1, -1), gauge-invariant (B5), domain b_1 = 1 (V007 :162-172).
  WHAT THIS DISCHARGES: S1's "h_j never displayed as an edge sum" — at ENTERED
  strength only: the display IS the entry's own content (a_j := theta_j(A)); it is
  consumed-then-unfolded, not derived.  Registering it as a law-level discharge of
  O11 would be the circularity §6 hunts; it is not so registered.
  WHAT IT CANNOT SERVE: any consumer needing the nondegenerate port structure —
  under this display the ports are collapsed (§2.3a) and no cell-to-cell port
  chaining carries information.

UNDER THE OPEN HORN (conditional on §2.3b's two conditions):  the display is the
  one-integer family
    h_j[a_j] = <A, P1 + c*gamma_j> = (1+c) a_(e_a0) - c a_(e_0b)
               + (1+c) a_(e_ab) - c a_(e_ba) ,     c in Z,
  gauge-COVARIANT with z^g = t z s^dagger realized at t = chi_n(g_(v_11)),
  s = chi_n(g_(v_00)).  EXACT RESIDUE: the single integer c (the winding
  supplement; c and c-1 exchange the P1/P2 roles), plus the horn choice itself.
  The two orientation-respecting members are the two sealed forward paths (B4).

UNCONDITIONALLY:  NOT-DERIVABLE — S1 stands at law strength; no sealed object
  displays h_j as an edge sum without consuming either the entry or the horn.

THE KERNEL IDENTITY (exact, new, horn-independent; B3):
    ker( d_0^dagger restricted to C_1(K_square) ) = Q * gamma_j .
  Two reads with the same anchoring differ by a multiple of gamma_j and by
  nothing else: gamma_j spans EXACTLY the anchoring-invisible read directions.
  The entered attach map reads PRECISELY the component of the write that the
  port structure cannot see; the open horn's residue c parameterizes exactly
  that same invisible direction.  O11 and O12 are therefore COMPLEMENTARY
  COORDINATES on the read space: (*) reads the boundary (the ports), gamma_j
  reads the kernel (the winding) — and the sealed corpus supplies the kernel
  coordinate (entered) while leaving the boundary coordinate unsupplied.
```

---

## 4. DELIVERABLE 3 — THE LICENSE QUESTION: K_dd's LEDGER L-1..L-5

Against the derived content of §2-§3 (the anchoring law (*), its fork, the
conditional displays):

```text
L-1  PORT/VERTEX ANCHORING (four vertex identifications of the response face's
     boundary 0-cells with the trace cell's named 0-cells).   ACT-REQUIRED.
     WITH A CORRECTION TO THE LEDGER'S TAG: L-1 is not O12-shaped.  O12's derived
     law governs exactly TWO slots per cell (one in, one out — GB R4, §2.1(ii));
     L-1 performs FOUR identifications, which are the 2-cell f's ATTACHING-MAP
     data (which 0-cells the boundary walk visits) — C_2-level gluing, outside
     (*)'s reach entirely.  Even a FULL O12 discharge licenses at most the
     two-slot shadow, and under the entered reading that shadow is degenerate
     (the response word w is closed: its ports collapse too).  The four-vertex
     act has no derived license and its licensing instrument was never the port
     law.
L-2  EDGE-READ IDENTIFICATION (three of the face's boundary slots identified
     with named trace edges).                                  ACT-REQUIRED.
     The entered display (§3) licenses each ADMITTED b_1 = 1 cell's read on ITS
     OWN circuit only.  Two independent failures for L-2 as performed: (i) the
     response circuit (+e_a0 +e_ab -e_ba -e_0b') is not an admitted cell of any
     sealed domain — admission is itself unsealed; (ii) K_dd has b_1 = 2, outside
     the entered map's operative domain altogether (V007 :170-171).  CONDITIONAL
     PATH, named: were the w-circuit subcomplex admitted as a cell of the entered
     domain (an authored/principal act), the entered display would fix its read
     on its own circuit at entered strength — that is the exact shape of a future
     L-2 license, not a present one.
L-3  THE AUTHORED DUPLICATE 1-CELL e_0b'.                      ACT-REQUIRED.
     Untouched: no O11/O12 content speaks to the existence of cells.
L-4  ROLE IMPORT (typing f as a response face).                ACT-REQUIRED.
     Untouched: outside the derived content entirely.
L-5  CONNECTED COMPOSITION (O-D1-shaped).                      PARTIAL.
     THE DERIVED PART — the SHAPE of a lawful connected-composition license: by
     (*), cells glue through ports iff out-port of one cell and in-port of the
     next are the boundary points of their read chains and are identified as ONE
     0-cell; port-chaining is exactly anchored-boundary matching, well-typed and
     checkable once open reads exist.  L-5's shapeless gap now has a named form.
     THE RESIDUE — the license itself: (i) under the entered reading ports are
     inert and chain NOTHING (§2.3a) — connected composition gets no license
     from the entry; (ii) the open-read supply the shape presupposes is the
     unsupplied horn (§2.3c); (iii) composition of record remains disjoint-only
     (hunt G-1).  The act remains authored/principal.
```

---

## 5. DELIVERABLE 4 — TYPING

```text
TYPE-P CHAINS, per result:
  The anchoring law (*), its corollaries §2.1(i)-(iii), the port representations
    §2.2, the kernel identity §3, the fork STATEMENT §2.3(c), and the L-1..L-5
    findings' derived parts:  TYPE-P on M03 (itself TYPE-P on DoR-008, DoR-009,
    per M03's own flags) + the V011 packet member's sealed displays (QSPEC
    candidate lineage) + GB (3e35ffe2).  NO Attach premise consumed.  These
    SURVIVE an Attach void.
  The entered-horn results (the degenerate anchoring AS OPERATIVE, §2.3(a); the
    entered display, §3 first block; L-2's conditional path):  additionally
    TYPE-P on the Attach entry chain (DoR-008/009/013, A0, addendum 420ab02f,
    entered artifact ccca6bb4) — VOID WITH THE ATTACH ENTRY under the addendum's
    triggers (failed cascade blind test; structural contradiction with a sealed
    forced result), and dissolve if the open forcing slot is ever filled
    (future_derivation_can_select_the_attach_map = NO_VERDICT stands).
  The open-horn results (§2.3(b); §3 second block):  CONDITIONAL on two supplied
    premises, both disclosed — (i) the nondegeneracy/open typing of h_j (the
    fork's other horn, unsealed), (ii) Step 4's canonicity demand (grounded on
    the entry's sealed no-selector bar, V007 :159-160, but applied here beyond
    its original scope — flagged).  They bind nothing until a holder supplies
    the horn.
VOID SUMMARY: an Attach void deletes the operative anchoring VALUE and the
  operative O11 display but leaves the anchoring LAW, the fork, the kernel
  identity, and the ledger findings standing (they ride M03 + V011 + GB only).
  The three carried Attach conditions (underived causal parent; AC-2; the
  physical-domain proof) ride into every entered-horn claim unchanged.
PROVABLE / PART-PROVABLE / YOURS, tagged:
  PROVABLE (exact, script-verified): A1, A2, B1-B8 — the algebra of §2.1, §2.2,
    §2.3(a) Steps, §2.3(b) Steps 1-3+5, §3's identities.
  PART-PROVABLE: §2.3(b) Step 4 (canonicity; sealed bar + supplied scope);
    the M03 :352-354 realizability observation (exact as algebra, its weight as
    evidence FOR the open horn is interpretive and NOT consumed as authority).
  YOURS (assembly): the fork framing; the ledger-tag correction (L-1); the
    complementary-coordinates reading of O11/O12.
```

---

## 6. Circularity self-check and overclaim audit

```text
THE HUNTED FAILURE MODE: "deriving" the anchoring while silently supplying it.
  AUDIT: the derivation supplies exactly TWO premises beyond sealed content, both
  named and quarantined — the open typing (horn (b)'s hypothesis, never asserted
  as sealed) and the Step-4 canonicity demand (disclosed, PART-PROVABLE).  The
  unconditional verdict is NOT-DERIVABLE with the gap and holder named; no horn
  is promoted.  The entered display (§3) is explicitly typed as the entry's own
  content unfolded — S1 is NOT declared discharged at law strength.
IT DOES NOT identify the ports with (v_00, v_11) of record: that identification
  is conditional on the unsupplied horn plus a disclosed demand.
IT DOES NOT contradict any sealed certificate: the entered collapse satisfies
  M03's C4 trivially; the covariance identity A2 is verified, not amended.
IT DOES NOT extend the entered domain (b_1 = 1 stands; K_dd remains outside it),
  admit any cell, select between P1 and P2, fix c, build a connected composition,
  or promote K_dd — every ledger act remains performed-not-licensed except as
  §4 states.
IT DOES NOT read the charter's diamond transport as this law (T3), book any
  stitching progress (T4), consume R33 naturality (T5), or cite any V011 span
  against the top-level copy (T6 — all spans bound to aa7c6d49; the top-level
  digest is listed only to record its difference).
IT DOES NOT evaluate any physical quantity: n, mu, kappa, alpha never appear as
  values; the only integers are census integers, coefficients in {-1,0,1}, ranks,
  and the residue parameter c carried symbolically.
FLATTENING CHECK: "the anchoring law is derived" is not flattened into "the
  anchoring is derived" — the law is the iff-condition (*), the value forks;
  "DISPLAYED under the entry" is not flattened into "O11 discharged".
```

---

## 7. SCRIPT, VERBATIM (`anchoring_derivation_v001.py`, Python 3 stdlib only)

```python
#!/usr/bin/env python3
# anchoring_derivation_v001.py — ANCHOR-DERIVE: exact structural algebra for the
# port-anchoring derivation (O12) and the successor incidence-sum display (O11).
# Laurent-polynomial matrices over Z in the unit characters (t, s, z) for the
# sealed one-cell law; exact integer/Fraction chain algebra on the sealed K_square
# census.  Python 3 stdlib only.  No physical quantity is computed, bounded, or
# evaluated; every integer is a sealed-text quotation or exact structural algebra.
from fractions import Fraction

# ---------------------------------------------------------------------------
# PART A — the sealed one-cell law: Laurent-monomial matrix algebra in (t,s,z)
# entries: dict {(et,es,ez): integer coeff}; unit modulus => dagger negates exps
# ---------------------------------------------------------------------------
def mono(c=1, t=0, s=0, z=0):
    return {(t, s, z): c}

def madd(a, b):
    r = dict(a)
    for k, v in b.items():
        r[k] = r.get(k, 0) + v
        if r[k] == 0:
            del r[k]
    return r

def mmulp(a, b):  # product of two Laurent polys
    r = {}
    for (k1, v1) in a.items():
        for (k2, v2) in b.items():
            k = (k1[0] + k2[0], k1[1] + k2[1], k1[2] + k2[2])
            r[k] = r.get(k, 0) + v1 * v2
            if r[k] == 0:
                del r[k]
    return r

ZERO = {}

def MM(A, B):  # matrix product
    n, m, p = len(A), len(B), len(B[0])
    return [[
        __import__('functools').reduce(madd, (mmulp(A[i][k], B[k][j]) for k in range(m)), {})
        for j in range(p)] for i in range(n)]

def dagger(A):  # conjugate transpose: unit characters => exponent negation
    n, m = len(A), len(A[0])
    return [[{(-k[0], -k[1], -k[2]): v for k, v in A[j][i].items()} for j in range(n)]
            for i in range(m)]

def eq(A, B):
    return all(A[i][j] == B[i][j] for i in range(len(A)) for j in range(len(A[0])))

# sealed objects on the one-cell record basis (|r>, |p_Q>, |e_Q>)  [M03 :121-133]
S_mat = [[ZERO, mono(1), ZERO],
         [mono(1), ZERO, ZERO],
         [ZERO, ZERO, mono(-1)]]

def D(t=0, s=0, z=0):  # D(x) = diag(1, x, 1) with x the named character
    return [[mono(1), ZERO, ZERO],
            [ZERO, mono(1, t=t, s=s, z=z), ZERO],
            [ZERO, ZERO, mono(1)]]

G_out = D(t=1)                       # sealed: G_out(t) = D(t)          [M03 :332]
G_in = MM(MM(S_mat, D(s=1)), S_mat)  # sealed: G_in(s) = S D(s) S       [M03 :333]

# CHECK A1: G_in(s) = diag(s, 1, 1) — the in-port character acts on the READY line
G_in_expected = [[mono(1, s=1), ZERO, ZERO],
                 [ZERO, mono(1), ZERO],
                 [ZERO, ZERO, mono(1)]]
assert eq(G_in, G_in_expected)
print('A1  G_in(s) = S D(s) S = diag(s,1,1)  — in-port acts on |r>, out-port on |p_Q>,')
print('    eraser line port-silent.  EXACT.')

# CHECK A2: the sealed covariance W_post(z^g) = G_out(t) W_post(z) G_in(s)^dagger
W_post = MM(D(z=1), S_mat)                      # W_post(z) = D(z) S
LHS = MM(D(t=1, s=-1, z=1), S_mat)              # W_post(t z s^dagger)
RHS = MM(MM(G_out, W_post), dagger(G_in))
assert eq(LHS, RHS)
# and the port action is visible EXACTLY on the ready->pointer write entry:
assert RHS[1][0] == mono(1, t=1, s=-1, z=1)     # <p_Q| . |r> = t z s^dagger
assert RHS[0][1] == mono(1)                     # <r| . |p_Q> = 1 (port-free)
assert RHS[2][2] == mono(-1)                    # eraser entry -1 (port-free)
print('A2  W_post(t z s^dagger) = G_out(t) W_post(z) G_in(s)^dagger  identically in (t,s,z);')
print('    ports visible ONLY on the ready->pointer write entry.  EXACT.')

# ---------------------------------------------------------------------------
# PART B — the sealed entered cell K_square: exact chain algebra
# census sealed at V011(packet) [62567,62693); orientations source -> target
# ---------------------------------------------------------------------------
V = ['v_00', 'v_10', 'v_01', 'v_11']
E = [('e_a0', 'v_00', 'v_10'), ('e_0b', 'v_00', 'v_01'),
     ('e_ab', 'v_10', 'v_11'), ('e_ba', 'v_01', 'v_11')]
vi = {v: i for i, v in enumerate(V)}
bdry = [[0] * 4 for _ in V]          # rows = vertices, cols = edges; -1 src, +1 tgt
for j, (_, s, t) in enumerate(E):
    bdry[vi[s]][j] -= 1
    bdry[vi[t]][j] += 1

def bd(x):
    return [sum(bdry[i][j] * x[j] for j in range(4)) for i in range(4)]

def rankQ(M):
    A = [[Fraction(x) for x in r] for r in M]
    rows, cols, r = len(A), len(A[0]) if A else 0, 0
    for c in range(cols):
        p = next((i for i in range(r, rows) if A[i][c] != 0), None)
        if p is None:
            continue
        A[r], A[p] = A[p], A[r]
        pv = A[r][c]
        A[r] = [x / pv for x in A[r]]
        for i in range(rows):
            if i != r and A[i][c] != 0:
                f = A[i][c]
                A[i] = [a - f * b for a, b in zip(A[i], A[r])]
        r += 1
    return r

gamma = [1, -1, 1, -1]               # sealed loop word, F08 orientation
P1 = [1, 0, 1, 0]                    # forward path 1: +e_a0 +e_ab
P2 = [0, 1, 0, 1]                    # forward path 2: +e_0b +e_ba

# CHECK B1: adjoint identity <d_0 lambda, x> = <lambda, bdry x> on every basis pair
for iv in range(4):                  # lambda = vertex indicator
    d0lam = [bdry[iv][j] for j in range(4)]      # (d_0 lambda)_e = lam_t - lam_s
    for je in range(4):              # x = edge indicator
        x = [1 if j == je else 0 for j in range(4)]
        lhs = sum(d0lam[j] * x[j] for j in range(4))
        rhs = bd(x)[iv]
        assert lhs == rhs
print('B1  <d_0 lambda, x> = <lambda, d_0^dagger x>  entrywise over Z on all 16 basis')
print('    pairs (d_0^dagger = the sealed incidence boundary, -1 source / +1 target).')

# CHECK B2: gamma closed; P1, P2 open with the SAME boundary; gamma = P1 - P2
assert bd(gamma) == [0, 0, 0, 0]
assert bd(P1) == [-1, 0, 0, 1]       # = e_v11 - e_v00
assert bd(P2) == [-1, 0, 0, 1]
assert [a - b for a, b in zip(P1, P2)] == gamma
print('B2  bdry gamma_j = 0 ;  bdry P1 = bdry P2 = e_v11 - e_v00 ;  gamma_j = P1 - P2.')

# CHECK B3: ker(bdry) on C_1 = span{gamma} exactly (rank 3, nullity 1)
assert rankQ(bdry) == 3
assert rankQ([gamma]) == 1 and rankQ(bdry + []) == 3
# nullity = 4 - 3 = 1 and gamma is in the kernel => kernel = Q gamma
print('B3  rank d_0^dagger = 3, nullity on C_1 = 1, kernel = Q.gamma_j — the port-')
print('    invisible read directions are EXACTLY the multiples of the entered loop.')

# CHECK B4: full solution set of the open boundary equation = P1 + c.gamma
# (any solution minus P1 lies in ker = Q gamma); positivity classification
for c in range(-3, 4):
    x = [P1[j] + c * gamma[j] for j in range(4)]
    assert bd(x) == [-1, 0, 0, 1]
    nonneg = all(v >= 0 for v in x)
    assert nonneg == (c in (0, -1))
print('B4  solutions of  d_0^dagger x = e_v11 - e_v00  are exactly  P1 + c.gamma_j ;')
print('    the orientation-respecting (nonnegative) members are c = 0 (P1) and c = -1')
print('    (P2) — precisely the two sealed forward paths, and no other member.')

# CHECK B5: degenerate collapse — the induced (t,s) action on the entered reading
# theta_j = <A, gamma_j> is trivial for EVERY gauge 0-cochain lambda
for iv in range(4):
    d0lam = [bdry[iv][j] for j in range(4)]
    assert sum(d0lam[j] * gamma[j] for j in range(4)) == 0
print('B5  <d_0 lambda, gamma_j> = 0 for every vertex generator: the entered reading is')
print('    gauge-invariant; the realized port pair collapses, t s^dagger acts trivially.')

# CHECK B6: intra-cell common-port cancellation: z_gamma = z_P1 . conj(z_P2) and the
# per-path endpoint shifts cancel exactly (same port pair for both forward paths)
for iv in range(4):
    d0lam = [bdry[iv][j] for j in range(4)]
    s1 = sum(d0lam[j] * P1[j] for j in range(4))
    s2 = sum(d0lam[j] * P2[j] for j in range(4))
    assert s1 == s2                     # equal endpoint characters, path by path
print('B6  under every gauge generator the two forward-path readings shift EQUALLY')
print('    (same ports) — the loop character is their ratio: common-port cancellation')
print('    INTRA-CELL, the same mechanism M03 R4 exhibits across the CTP branches.')

# CHECK B7: vertex census — unique source and unique sink of the oriented 1-skeleton
indeg = {v: 0 for v in V}
outdeg = {v: 0 for v in V}
for (_, s, t) in E:
    outdeg[s] += 1
    indeg[t] += 1
assert [v for v in V if indeg[v] == 0] == ['v_00']
assert [v for v in V if outdeg[v] == 0] == ['v_11']
assert indeg['v_10'] == outdeg['v_10'] == 1 and indeg['v_01'] == outdeg['v_01'] == 1
print('B7  degree census: v_00 unique source (in 0/out 2), v_11 unique sink (in 2/out 0),')
print('    v_10 and v_01 pass-through (1/1).')

# CHECK B8: (*) alone does not pin the pair — every ordered vertex pair is
# chain-admissible on the connected K_square (rank test per pair)
adm = 0
for u in range(4):
    for v in range(4):
        if u == v:
            continue
        tgt = [0, 0, 0, 0]
        tgt[v] += 1
        tgt[u] -= 1
        Maug = [bdry[i] + [tgt[i]] for i in range(4)]
        if rankQ(Maug) == rankQ(bdry):
            adm += 1
assert adm == 12
print('B8  all 12 ordered vertex pairs admit a chain with that boundary: the anchoring')
print('    equation ALONE does not select the pair — the selection needs the sealed')
print('    forward/flow structure (B7) plus the no-selector bar, or it is authored.')

print()
print('RESULT  the anchoring law (*)  d_0^dagger ell_j = e_out - e_in  is exact adjoint')
print('        algebra;  entered reading => degenerate collapse (B5);  open typing =>')
print('        endpoint pair (v_00, v_11) with residue P1 + c.gamma_j (B4, B7).')
print('ALL ASSERTIONS PASSED')
```

## 8. EXACT SCRIPT OUTPUT, VERBATIM (`python3 anchoring_derivation_v001.py`, exit code 0)

```text
A1  G_in(s) = S D(s) S = diag(s,1,1)  — in-port acts on |r>, out-port on |p_Q>,
    eraser line port-silent.  EXACT.
A2  W_post(t z s^dagger) = G_out(t) W_post(z) G_in(s)^dagger  identically in (t,s,z);
    ports visible ONLY on the ready->pointer write entry.  EXACT.
B1  <d_0 lambda, x> = <lambda, d_0^dagger x>  entrywise over Z on all 16 basis
    pairs (d_0^dagger = the sealed incidence boundary, -1 source / +1 target).
B2  bdry gamma_j = 0 ;  bdry P1 = bdry P2 = e_v11 - e_v00 ;  gamma_j = P1 - P2.
B3  rank d_0^dagger = 3, nullity on C_1 = 1, kernel = Q.gamma_j — the port-
    invisible read directions are EXACTLY the multiples of the entered loop.
B4  solutions of  d_0^dagger x = e_v11 - e_v00  are exactly  P1 + c.gamma_j ;
    the orientation-respecting (nonnegative) members are c = 0 (P1) and c = -1
    (P2) — precisely the two sealed forward paths, and no other member.
B5  <d_0 lambda, gamma_j> = 0 for every vertex generator: the entered reading is
    gauge-invariant; the realized port pair collapses, t s^dagger acts trivially.
B6  under every gauge generator the two forward-path readings shift EQUALLY
    (same ports) — the loop character is their ratio: common-port cancellation
    INTRA-CELL, the same mechanism M03 R4 exhibits across the CTP branches.
B7  degree census: v_00 unique source (in 0/out 2), v_11 unique sink (in 2/out 0),
    v_10 and v_01 pass-through (1/1).
B8  all 12 ordered vertex pairs admit a chain with that boundary: the anchoring
    equation ALONE does not select the pair — the selection needs the sealed
    forward/flow structure (B7) plus the no-selector bar, or it is authored.

RESULT  the anchoring law (*)  d_0^dagger ell_j = e_out - e_in  is exact adjoint
        algebra;  entered reading => degenerate collapse (B5);  open typing =>
        endpoint pair (v_00, v_11) with residue P1 + c.gamma_j (B4, B7).
ALL ASSERTIONS PASSED
```

---

## FLAG BLOCK

```text
ANCHORING = LAW-DERIVED, VALUE-FORKED:
  (law, DERIVED-UNIQUE, exact) a structure-preserving port-to-0-cell anchoring
    exists iff  d_0^dagger ell_j = e_out - e_in  (*) — the ports are the boundary
    of the cell's read chain; +1 at out = target, -1 at in = source, exactly the
    sealed incidence sign; O12 is a FUNCTION of O11, not a free identification;
    port characters carry the cell's own charge n; realized representations
    G_in(s) = diag(s,1,1) on the ready line, G_out(t) = diag(1,t,1) on the
    pointer line, eraser port-silent (script A1/A2, B1).
  (value, horn 1 — the ENTERED reading, TYPE-P) DERIVED-FAMILY(degenerate):
    d_0^dagger gamma_j = 0 forces P(out) = P(in); four equivalent common-vertex
    members (abelian basepoint-independence), zero structural freedom; the port
    action is INERT; M03 :352-354's independent-branch endpoint characters are
    unrealizable at complex level; mechanism = common-port cancellation
    INTRA-CELL between the two sealed forward paths (gamma_j = P1 - P2, equal
    endpoint shifts; script B2/B5/B6).
  (value, horn 2 — OPEN typing, unsealed) DERIVED-UNIQUE-CONDITIONAL:
    (in, out) = (v_00, v_11) — by (*) + the sealed unique-source/unique-sink
    census + the sealed forward/flow displays + the entry's own no-selector bar;
    conditional on (i) the open typing and (ii) the disclosed canonicity demand
    (Step 4 — the only non-exact leg); read chain then forced to
    ell_j = P1 + c*gamma_j, one integer c, orientation-respecting members
    exactly the two sealed forward paths (script B4/B7/B8).
  (unconditionally) NOT-DERIVABLE(gap = the loop/edge typing of h_j — the
    ratified law consumes h_j as given (GB S1) and M03's (t,s) are free
    covariance parameters with no complex-level gauge action defined; HOLDER =
    authored-physics entry (Attach-strength successor supplying an open read) or
    principal decision; not a lane act; the horns are mutually exclusive).

SUCCESSOR_DISPLAY = CONDITIONAL:
  under the ATTACH ENTRY (TYPE-P, voids with it): DISPLAYED —
    h_j = <A, gamma_j> = a_(e_a0) - a_(e_0b) + a_(e_ab) - a_(e_ba), explicit
    incidence sum, four named edges, weights (+1,-1,+1,-1), gauge-invariant,
    b_1 = 1 domain; discharges S1 at ENTERED strength only (the display is the
    entry's own content unfolded — NOT a law-level derivation, per the
    circularity audit);
  under the OPEN horn: the one-integer family <A, P1 + c*gamma_j>, residue = the
    integer c plus the horn choice; P1/P2 selection barred (selector);
  unconditionally: NOT-DERIVABLE (S1 stands at law strength).
  KERNEL IDENTITY (exact, horn-independent): ker(d_0^dagger)|C_1(K_square) =
    Q*gamma_j — the entered attach map reads exactly the anchoring-invisible
    component; O11 and O12 are complementary coordinates (kernel vs boundary) on
    the read space, and the corpus supplies the kernel coordinate only.

LEDGER_LICENSING = L-1 ACT-REQUIRED (corrected tag: attaching-map content, not
  O12-shaped — the derived law governs two port slots, L-1 performs four vertex
  identifications at C_2-attaching level; even full O12 licenses only the
  two-slot shadow, degenerate under the entered reading) ; L-2 ACT-REQUIRED (the
  entered display licenses admitted b_1 = 1 cells' own-circuit reads only; the
  response circuit is unadmitted and K_dd's b_1 = 2 is outside the entered
  domain; conditional path named: admission of the w-circuit as an entered-domain
  cell would license L-2 at entered strength) ; L-3 ACT-REQUIRED (untouched) ;
  L-4 ACT-REQUIRED (untouched) ; L-5 PARTIAL (the SHAPE of a connected-
  composition license is derived — port-chaining = anchored-boundary matching
  per (*); the license itself is not issued: entered ports are inert, the open
  supply is the unsupplied horn, composition of record stays disjoint-only).

TYPING = anchoring law + kernel identity + fork statement + ledger findings:
  TYPE-P on M03 (DoR-008/009) + V011 packet member + GB; SURVIVE an Attach void.
  Entered-horn values and the entered display: additionally TYPE-P on the Attach
  entry chain (DoR-008/009/013, A0, 420ab02f, ccca6bb4) — VOID with the entry
  (cascade blind-test failure or structural contradiction), and dissolve if the
  open forcing slot is filled.  Open-horn values: CONDITIONAL on the unsealed
  open typing + the disclosed canonicity demand; bind nothing until a holder
  supplies the horn.  Tags: PROVABLE = script A1-A2, B1-B8 and everything
  resting only on them; PART-PROVABLE = the canonicity leg and the M03 :352-354
  realizability observation (not consumed as authority); YOURS = the fork
  framing, the L-1 tag correction, the complementary-coordinates reading.

SEALS_VERIFIED = 22a2a478 (POSED), a5c71b2a (POSE_CHECK), 3e35ffe2 (GB),
  2cd1ffce (M03), f1881511 (MIN-CARRIER), bb1b88ad (D0-SQUARE), 420ab02f
  (ATTACH addendum), ccca6bb4 (ATTACH V007), ad9fc14e (ATTACH mandate DoR),
  1892c08e (glued-topology hunt), aa7c6d49 (V011 PACKET member — all spans bound
  here; eight spans rehashed, digests in §0), 20a3a17d (V011 top-level, listed
  only as the T6 non-member, no span cited against it) — all recomputed at path
  THIS session before reliance; none failed, none unverifiable.

FORBIDDEN_IMPORTS = none. No scale, no imported GR, no faithfulness premise, no
  continuum authority; C_ref carried as frozen audit class (T1); charter
  transport not used (T3); lever-illusory carried, no stitching booking (T4); no
  R33/naturality premise (T5); V011 spans packet-member-only (T6); FR negatives
  carried with the B1-arc partials (T7).  No register/tracker/plan/road/
  route-ledger/lens file read.  No git action.

MACHINERY_INVOKED = no (exact symbolic/integer algebra only: Laurent-monomial
  matrices and Fraction-RREF ranks, stdlib; no numeric evaluation of any
  physical quantity, no member binding, no fixed-point execution, no end test;
  output name probed ABSENT before write).

alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
