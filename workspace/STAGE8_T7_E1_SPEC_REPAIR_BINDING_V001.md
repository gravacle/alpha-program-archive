# Stage-8 T7 E1 Spec Repair Binding V001

Date: 2026-07-26

## Status

```text
APPEND_ONLY_REPAIR_BINDING_SEALED_BEFORE_AUTHORING
```

Authorized by the principal (relay of 2026-07-26, item 3). Scope is the
hostile review's prescriptions plus the two required inputs the principal
named. The sealed E1 successor spec (9cfafde1…) is preserved unexecuted
and NOT edited; its repairs land in an append-only v002 successor.

## Consumed inputs (cited by hash)

```text
E1 hostile review (verdict NOT_READY)
  /Users/bgm/MB Work/alpha_supervision/E1_SPEC_HOSTILE_REVIEW_SEALED_
  TRANSCRIPT_V001.md = d95c7a1657a2e6ae…
F-8 second-half review (F8_GATE_NOT_DISCHARGED)
  .../F8_SECOND_HALF_REVIEW_SEALED_TRANSCRIPT_V001.md = 3cacd4b748bbd1c6…
This lane's errata (C3/C5 refuted; F-8 flag corrected)
  STAGE8_T7_C3_C5_REFUTATION_AND_F8_FLAG_ERRATUM_V001.md (sealed adjacent)
CODEX ASSIGNMENT A — INDEPENDENT-SYSTEM BLIND E1 RETURN
  .../CODEX_ASSIGNMENT_A_E1_BLIND_RETURN_V001.md = 20b54b2e94c9213e…
```

## R1 — The C3-demotion repair (the review's B-1)

```text
A-L0 (new numbered obligation, replacing the frozen C3 clause):
  EITHER prove Y_2 Y_1 = 0 as an EXACT IDENTITY on the sealed forms for
  every admitted cell pair (note: it does NOT hold in general — the
  reviewer's exact witness stands, so this arm must identify the precise
  sealed structure that would force it, or fail),
  OR bound tr[C Y_2 Y_1 C] with a CERTIFIED decay in the cell separation,
  uniform over the D3 quantifier.
  Named witness on failure: E1_ONE_LINE_CONNECTED_CROSS_TERM_UNCERTIFIED.
C5 and the affected lemmas (A-L3/A-L4) are RESTATED to cover the TWO-LINE
  SECTOR ONLY. No verdict of any successor may assert "the n >= 2 sums
  are certified" until A-L0 closes.
NC8 is RECAST as a TWO-SECTOR control: the one-line sector must be shown
  either to vanish or to be separately bounded; its divergence is a PASS
  only CONDITIONAL on A-L0. The old form (mandating that a one-line bound
  must fail and be dismissed) entrenched the error and is void.
The frozen-input status of C3/C5 is REVOKED; no fence may bar an executor
  from the correct algebra. C1, C2, C4 and C6 remain frozen inputs.
```

## R2 — The baseline-norming repair (B-2)

```text
Delete S3 / G_bl. The baseline is NEVER normed. Under the exact
record-phase collapse: the same-phase sector has V(0) = I exactly up to
the C-L2 commutator error; the opposite-phase sector has
tr A(0) = -2 tr(CPC) = -infinity, so its determinant vanishes at a = 0
AND at a != 0 and the sector DROPS OUT IDENTICALLY. Replace G_bl in the
bound by a certified bound on the C-L2 commutator error, with a named
witness.
CONSEQUENCE TO BE STATED IN THE SUCCESSOR'S OWN VERDICT AND PREDICTION
SECTIONS, not buried: after this repair every surviving constant of that
route rests on C-L2 — so it is NOT an independent route; it is IR-C's
wall re-expressed.
```

## R3 — The colour-sum / resolvent repair (B-3)

```text
Add the obligation: OPPOSITE-PHASE SECTOR VANISHES IDENTICALLY on the
closed pair polydisc (tr A_{mu lambda}(a) = -infinity for all a, hence
det(1 + A(a)) = 0), with a named witness.
Re-derive sum c = 1 and sum |c| = kappa_bal over the SURVIVING SECTOR
ONLY; restrict the resolvent sup and the Carleman segment to that sector;
and state that kappa_bal = 1 follows as (1/2)/(1/2) from the surviving
weights, NOT from the (1 + |D|)/|1 - D| display.
```

## R4 — Every remaining finding of the sealed transcript

```text
The authoring lane must ENUMERATE every BLOCKING and CONDITION finding in
the sealed E1 review transcript (d95c7a16…) — including B-4 and all
conditions, which this binding does not restate — and address each
explicitly, mapping finding -> repair in its change report. A finding
left unaddressed, or addressed only in prose, is a defect. This binding
does not paraphrase findings it has not enumerated; the transcript
governs.
```

## R5 — Codex reconciliation (required input (a))

Route mapping of record:

```text
Codex Route 1 (cancellation-first CTP determinant)  ~  IR-B, but broader
  and better-founded: bound determinant/log differences of the
  pair-holomorphic Ghat_K AFTER CTP subtraction and baseline
  normalization, never the raw sea trace norm.
Codex Route 2 (renormalized single-cell anchor)     ~  IR-A's spirit, BUT
  Codex treats the n >= 2 KP bound as an OBLIGATION TO PROVE. Given the
  C3/C5 refutation that is now exactly right and IR-A's framing was
  wrong. Codex's Route 2 also requires an EXACT completed-normalization
  identity, not a subtraction by convention.
Codex Route 3 (smooth-localizer sharp-limit classifier) ~ IR-C's
  classifier role; this is the Q2-trigger route.
```

INDEPENDENT CONVERGENCES (recorded; Codex was blind to this lane's work):

```text
- bound-class failure, NOT a proven divergence of the completed theory
  (matches C2 and the principal's object-vs-bound tripwire);
- the n = 1 single-cell coincidence limit as the locus (matches C6);
- the replacement must be CANCELLATION-FIRST — the root cause is that
  absolute trace-norm majorization was taken BEFORE using the CTP,
  determinant-ratio, oscillatory/PV and completed-normalization
  cancellations. This is a cleaner statement of the root than this lane's
  and is adopted as the successor's organizing principle;
- the completed chain's missing unitality anchor is binding, with
  Z_comp(0) != 0 a named hypothesis and not a structural identity.
```

WHERE CODEX DID NOT FOLLOW THIS LANE — and was right:

```text
- It never leaned on the two-line decomposition, which the hostile review
  has since refuted by exact witness. Its routes therefore do not inherit
  that error.
- REFINEMENT OF C6, adopted: the FATAL local integral is the VOLUME
  DIAGONAL x = y, not the sharp boundary; SMOOTHING ONLY THE BOUNDARY
  WILL NOT REMOVE A |x-y|^-3 POSITIVE MAJORANT. C6 named the sharp
  localizer as the locus; Codex refines the locus to the diagonal and
  demotes the boundary to a secondary, testable factor. This is a
  correction of emphasis in C6, recorded, and it BEARS DIRECTLY ON Q2:
  a smoothed-localizer successor may not fix the obstruction at all.
- The supremum over admitted cells is not the root but the enforcement
  mechanism preventing concealment in a chosen carrier/truncation/cell
  size — adopted as the successor's reading of the spec-header scoping.
```

## R6 — The B-L2 record-vertex crux, named explicitly (required input (b))

```text
BOTH ROUTE-SETS TRIANGULATE HERE. The crux obligation is:
  a CARRIER-UNIFORM HILBERT-SCHMIDT BOUND ON THE RECORD-VERTEX PAIRING,
  with the CELL-TIME INTEGRATION supplying the decay that the equal-time
  object provably lacks (equal-time localization of the 3-D massless
  Dirac sea fails Shale-Stinespring; only two-time / scattering-type
  objects can work).
The successor spec must carry this as a single named obligation with its
own witness, and must state that IR-B/Codex-Route-1 and IR-C/Codex-Route-3
both reduce to it — so that a failure there is reported once, as the
common wall, not three times as three route failures.
```

## R7 — Named slot for the Codex reconciliation addendum

```text
Codex is producing a reconciliation addendum (its Routes 1/2/3 mapped onto
IR-A/B/C, its assessment of B-L2, and its take on the Y_2 Y_1 cross-term),
arriving through Brian. A NAMED SLOT is reserved for it in the successor
spec: CODEX_RECONCILIATION_ADDENDUM (pending). Authoring does NOT wait on
it. When it arrives it is sealed by hash and consumed as an append-only
amendment; if it contradicts any repair above, the contradiction is
reported, not resolved silently.
```

## R8 — Unchanged and binding

```text
The principal's Q2 trigger and pre-registered classification standard
(STAGE8_T7_Q2_TRIGGER_AND_CLASSIFICATION_STANDARD_V001) bind the
successor verbatim, INCLUDING the object-vs-bound tripwire — which the
R5 refinement makes sharper: Codex's Route-3 prediction ("the sharp
projector is an obstruction to the OLD POSITIVE TRACE-NORM BOUND, not yet
to the completed object itself") lands squarely in the tripwire's
category (c), which EXCLUDES NOTHING. Any successor artifact reporting a
sharp-localizer exclusion must make the (a)/(b)/(c) declaration or block.
The clustering-adoption fence stands. Q2 remains the principal's.
```

## R9 — Ordering

```text
1. Author the E1 successor spec v002 per R1-R8 (append-only; v001
   preserved unexecuted).
2. Fresh-context hostile review of v002, chartered with R1-R8 and the
   sealed transcripts.
3. Execution only after that review clears. No route may execute under
   v001.
```

## Protected status

```text
E1_successor_spec_v001_executable = false
E1_successor_spec_v002_authored = false
C3_no_cross_term_claim = REFUTED
C5_long_distance_margin_claim = UNPROVEN
E1_locus_refined_to_volume_diagonal = true
F8_gate_discharged = false
connected_linked_cluster_density_proved = false
kappa_record_computed = false
alpha_computed = false
proof_authorized = false
```
