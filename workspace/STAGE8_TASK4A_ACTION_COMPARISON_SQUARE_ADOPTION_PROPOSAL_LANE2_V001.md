# STAGE8 TASK 4A: ACTION-COMPARISON SQUARE ADOPTION PROPOSAL — LANE 2 V001

Date: 2026-08-03  
Task: PASTE 441 / Task 4a / square race, author arm  
Lane: CODEX LANE 2  
Register head: Q-357  
Reserved ruling: DoR-017  
Custody: authored proposal; cross-review and principal ruling required

**PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-017 RESERVED)**

```text
PROPOSAL_PAGE_FLAG = PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION

LEAD_RESULT = TWO_VARIANT_FAMILY_PROPOSAL_SURVIVES_SELF_GATE
ACTION_COMPARISON_SQUARE_PROPOSAL = READY_FOR_CROSS_REVIEW

VARIANT_Q = QUOTIENT_ONLY | PROPOSED_NOT_ADOPTED
VARIANT_QE = QUOTIENT_PLUS_ENDPOINT_MATCHED | PROPOSED_NOT_ADOPTED
VARIANT_SELECTED = none

OUTPUT_TYPE_PER_MEMBER =
  smooth real/reality-covariant scalar action correction in Flat(S_G),
  finite-stage compatible on every typed comparison square

OUTPUT_IS_ONE_SELECTED_SCALAR_ACTION = false |
  proposal retains the complete admissible comparison-map family;
  downstream claims require family-invariance or a later ratified member rule

STANDING_FALSIFIER =
  any failure of an exact finite restriction, the D/T^char split,
  reality, E_post, batching, family naturality, or an admitted route's
  carrier typing VOIDS the affected variant

ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY = PROPOSED_VARIANT_Q_ONLY
ENDPOINT_MATCHED_CONSUMPTION = PROPOSED_VARIANT_QE_ONLY

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The proposal does not choose between the two physical scalar routes in
DoR-015. It also does not hide the normalization freedom that the transverse-
action determination proved survives. The adopted object, if the principal
chooses to adopt it, is initially a **family-level comparison law**. Only
statements invariant across that family may pass downstream without a later
member/normalization ruling.

---

## 0. Preflight, authorities, and scope

### 0.1 Mandatory preflight

All load-bearing artifacts were hash-verified before reading:

| Authority | Verified SHA-256 | Use |
|---|---|---|
| transverse action determination | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | exact `phi_div` slot, `Flat(S)` family, missing comparison package |
| descent V003 | `a03e836380cbbfa08d8763bf62d6104f70aec69ae484b3b69f63489a5ce1c68c` | all-rank physical descent, two-route seam, permanent regressions |
| extension V002 | `eb3675d525af7d1420c4ed033a5e5b94eb7494c1bac1305029b25ac9169567a0` | scoped terminal character and corrected zero-extension square |
| DoR-015 / FIELD_SIGNATURE V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical quotient and endpoint-matched physical route |
| DoR-016 / network law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled CTP network data and finite trace/tower |
| Q-313 Map 1 | `ea8f4e9a79a9e4aca4a0b9e9aab470d78cd4d9c78794c20d9e979c0db16fbeab` | sealed rank-one scalar-character factorization and source/action refusal |

All available sidecars passed. The local questions-settled register and
`LOCKED_PROCESS.md` sidecars passed, and `LOCKED_PROCESS.md` was read in full.

```text
DOES_THE_OBJECT_EXIST = no | proposal commissioned at Q-357
IS_THE_VERSION_CURRENT = yes | register head Q-357
ARE_THE_INPUTS_PRESENT = yes for a family-level authored proposal |
  no for a uniquely normalized scalar action member
PREFLIGHT = PASS_FOR_AUTHOR_ARM
```

### 0.2 Register sweep

The sweep covered Q-313, Q-315, Q-318, Q-321 through Q-324, Q-327 through
Q-329, and Q-345 through Q-357. Binding results:

```text
prefix family = complete cellwise relative-character data;
D_G exists at every signed edge-resolved rank;
ker(D_G) = vertex rephasing exactly;
T_G^char is scoped and differs from D_G on pendant content;
cycle-creating upward quotient map is impossible;
DoR-015 permits quotient and endpoint-matched physical scalar routes;
physical action restriction/comparison map is unbuilt;
Flat(S) survives all sealed active-section jets;
stationary 2PI/action inverse remains unbuilt.
```

### 0.3 Race discipline

The derive-arm result in relay 440 was not read, queried, or inferred. This
proposal carries both routes. No route, normalization, action member, rank,
orientation, frame, filtration, or endpoint state/effect is selected.

### 0.4 Symbol ledger

```text
P_G^fam       = complete every-prefix trace family modulo simultaneous
                signed relabeling; equivalently cellwise relative characters;
D_G           = P_G^fam -> Q_G, the physical Gate-4 descent;
Q_G           = U(1)^(E_G)/Gamma_G = Hom(C_G,U(1));
T_G^char      = terminal-character map on its sealed/scoped rank-one domain;
E_G^match     = endpoint-matched transport orbit carrier defined below;
Y_G^Q         = Q_G;
Y_G^QE        = Q_G x E_G^match;
S_G           = active physical section at finite stage G;
Act_G         = Flat(S_G), the proposed action-correction codomain;
Comp_G^r      = proposed comparison map Y_G^r -> Act_G;
phi_G^r       = Comp_G^r(C_G^r(input));
R_CTP,e       = T_-,e^dagger T_+,e, not the scalar trace Z;
R             = bilocal probe used elsewhere, not R_CTP.
```

---

## 1. B1 — exact object and all legs

### 1.1 Network input carrier

For each finite signed edge-resolved realization `G`, set

```text
N_G^Q := P_G^fam.                                 (B1-1)
```

Q-348/Q-349 identify this with the complete cellwise faithful relative
characters by

```text
r_j=Z_(j-1)^(-1)Z_j.                              (B1-2)
```

For the endpoint-enriched variant, the input also carries matching endpoint
data at every transport leg:

```text
N_G^QE := P_G^fam fiber_product Ports_G EndData_G. (B1-3)
```

`EndData_G` means the full no-selection family of typed source/target vectors
or effects admitted by V005's endpoint-intertwiner interface. It is not a
selected state/effect and not a completed joint contraction.

### 1.2 Physical carrier legs

The quotient-only leg is already ratified mathematics:

```text
C_G^Q := D_G:N_G^Q->Y_G^Q,
Y_G^Q := Q_G.                                     (B1-4)
```

For one oriented edge `e:s->t`, define the endpoint orbit carrier

```text
E_(G,e)^match :=
  {(v_t,R_CTP,e,v_s)} /
  (v_t,R_e,v_s)~(g_t v_t,g_t R_e g_s^(-1),g_s v_s).
                                                               (B1-5)
```

The associated scalar evaluation is

```text
S_e([v_t,R_e,v_s])=<v_t,R_e v_s>.                (B1-6)
```

The full endpoint carrier retains every edge and every matching datum:

```text
E_G^match := product_(e in E_G) E_(G,e)^match
             modulo simultaneous signed relabeling.             (B1-7)
```

No edge is selected and no sum over edges is presumed. The two-route
physical carrier and its input leg are

```text
Y_G^QE := Q_G x E_G^match,
C_G^QE(h,xi):=(D_G(h),E_G(h,xi)).                 (B1-8)
```

Here `E_G(h,xi)` uses DoR-016's relative transports and the matching data
`xi`; it does not replace `D_G` or inject endpoint content into the cycle
coordinate.

### 1.3 Exact action output type

Let `X_G^phys` be the finite physical carrier supplied by DoR-015, `S_G` its
active section, and `I(S_G)` the ideal of smooth functions vanishing there.
The proposed finite action-correction class is

```text
Act_G := Flat(S_G;R)
       := intersection_(m>=1) I(S_G)^m
          intersect C_infty(X_G^phys;R).          (B1-9)
```

Using real-valued functions is shorthand for the ratified reality-covariant
fixed class. Equivalently one may use complex CTP representatives obeying
the conjugation law; no extra complex phase is selected.

For route `r in {Q,QE}`, define the reality-fixed finite coefficient algebra

```text
Coeff_G^r:=C(Y_G^r;R)^Theta.                      (B1-10)
```

The finite-stage comparison space is the **algebraic** tensor family

```text
TensorComp_G^r:=Coeff_G^r odot_alg Flat(S_G;R).   (B1-11)
```

Thus every member has the explicit finite-rank form

```text
comp_G^r(y)(x)=sum_(k=1)^m F_k(y)b_k(x),
F_k in Coeff_G^r,
b_k in Flat(S_G;R).                               (B1-12)
```

No tensor completion, measure, or norm closure is hidden in `(B1-11)`.
For route QE, `F_k` may depend jointly on quotient and endpoint-orbit data;
this is why no additive Q/E decomposition is presumed.

The authored comparison object is the family of stage families

```text
Comp^r:={comp^r=(comp_G^r)_G:
  comp_G^r in TensorComp_G^r;
  reality, anchor, family-isomorphism covariance, and every typed
  restriction square B1-18 hold;
  the complete family collectively separates the Q_G coordinate, and for
  r=QE also every admitted E_G^match coordinate}.                (B1-12a)
```

Each member produces the exact requested type:

```text
phi_G^r(input)
  :=comp_G^r(C_G^r(input))
  in Flat(S_G;R),                                 (B1-13)
```

a scalar physical transverse-action correction, not a source correlator,
cycle coordinate, Hessian, inverse, stationary point, or response.

The proposal adopts `Comp^r`, not one member. The zero correction is a member.
Where `Flat(S_G)` has a nonzero member, the explicit flat counterfamily from
the transverse-action determination and continuous coefficient functions
supply nonzero algebraic tensors. Closing them under the forced pullbacks and
realization isomorphisms gives admitted stage families. The complete family
is collectively separating on `Y_G^r`: continuous real functions separate
distinct points of the finite-dimensional completely regular orbit carrier,
so
for `y!=y'` there are `F` and nonzero `b` with

```text
F(y)b != F(y')b.                                  (B1-14)
```

Thus the family-valued comparison deletes no carrier coordinate even though
an individual member may have a kernel. Nonzero off-section content and its
normalization remain explicit member data; they are not fabricated from a
desired downstream result. If a stage has `Flat(S_G)={0}`, its action-
correction fiber is honestly the singleton zero class and separation there
is required only of the already-built carrier leg, not of a nonexistent
transverse action direction.

### 1.4 Carrier square

For every declared signed fine-to-coarse cellular arrow `f:G_N->G_M`, the
ratified network restriction is `P_f` and the physical restriction is
`rho_f`. The quotient carrier square is

```text
N_M^Q  --C_M^Q-->  Y_M^Q
 | P_f                | rho_f
 v                    v
N_N^Q  --C_N^Q-->  Y_N^Q,

C_N^Q P_f = rho_f C_M^Q.                         (B1-15)
```

For the two-route variant, endpoint batching composes transports in E_post
order and transports matching endpoints. Call that map `rho_f^E`. Then

```text
rho_f^QE := rho_f x rho_f^E,
C_N^QE P_f^QE = rho_f^QE C_M^QE.                 (B1-16)
```

Equation `(B1-16)` is a proposal obligation at the endpoint leg and an
already-proved equality at its `D_G` leg.

### 1.5 Action-comparison square

For every arrow on which a finite physical action restriction

```text
rho_f^Act:Act_M^basic(f)->Act_N                  (B1-17)
```

is typed, the square obligation is

```text
rho_f^Act comp_M^r C_M^r
 =comp_N^r C_N^r P_f^r.                          (B1-18)
```

`Act_M^basic(f)` is the subspace of fine actions constant on the fibers of
the physical restriction relevant to `f`. This scope is necessary. When an
extension creates a cycle, V003's Z7 theorem forbids an upward physical
carrier map; no false action restriction is asserted. New-cycle-dependent
fine action content is a vertical finite increment, independently subject
to the finite falsifier.

For cycle-rank-preserving identity extensions, `rho_f` is an isomorphism and
`rho_f^Act` is ordinary transport of functions through that isomorphism.
Then `(B1-18)` is fully typed in both directions.

This is a construction, not an asserted commuting diagram.  On a pure
algebraic tensor `F_M odot b_M`, the carrier pullback sends `F_M` to
`F_M o rho_f^r`, while the typed action restriction sends `b_M` to
`rho_f^Act b_M`.  Hence the two composites in `(B1-18)` both evaluate as

```text
(F_M o rho_f^r) odot (rho_f^Act b_M).            (B1-18a)
```

Linearity proves the equality for every finite sum in `(B1-12)`, and
`Comp^r` was defined to contain exactly the globally compatible stage
families.  Thus the square follows from algebraic tensor functoriality on
every arrow for which `(B1-17)` is typed.  No tensor completion or
representative choice enters this proof.

### 1.6 Evaluation and 2PI interfaces

The comparison output feeds, but does not construct, the formal Q-408 legs:

```text
phi_G^r -> D_C D_K phi_G^r,
phi_G^r -> D_C(delta phi_G^r).                    (B1-19)
```

These arrows execute only after the stationary/2PI domain and inverse exist.
The square therefore ends at `phi_G^r in Act_G`. No second derivative,
inverse, `Pi_R,ind`, response, or downstream value is taken here.

---

## 2. B2 — two fully typed variants, neither selected

### 2.1 Variant Q: quotient-only

```text
VARIANT_Q := (
  input=N_G^Q=P_G^fam,
  physical_leg=C_G^Q=D_G,
  consumer_carrier=Y_G^Q=Q_G,
  output=phi_G^Q in Flat(S_G;R),
  comparison_family=Comp^Q with stage component comp_G^Q,
  required_kernel=Phi_G^(-1)(Gamma_G),
  endpoint_leg=forbidden,
  terminal_map=T^char only on sealed square and im(j_NM^Q)
).
```

It consumes every record-visible cycle and forbids bare or endpoint-matched
open-path content from entering this correction. On a tree `Q_G={*}`; the
cycle correction is anchored to zero. The network's nontrivial open-path
access remains upstream and is not declared nonexistent.

Variant Q leaves `TYPE-U`:

```text
selected nonzero comp_G^Q member;
its normalization;
completed action restriction outside the finite basic class;
stationary/2PI blocks and inverse;
non-edge-resolved iota extension.
```

### 2.2 Variant QE: quotient plus endpoint-matched

```text
VARIANT_QE := (
  input=N_G^QE=P_G^fam fiber_product Ports_G EndData_G,
  physical_leg=C_G^QE=(D_G,E_G),
  consumer_carrier=Y_G^QE=Q_G x E_G^match,
  output=phi_G^QE in Flat(S_G;R),
  comparison_family=Comp^QE with stage component comp_G^QE,
  cycle_kernel=Phi_G^(-1)(Gamma_G),
  endpoint_invariance=joint endpoint covariance,
  bare_open_edge_scalar=forbidden,
  additive_Q_plus_E_decomposition=not assumed
).
```

It consumes the complete physical cycle quotient and may also consume the
joint gauge-invariant endpoint carrier. It does not identify endpoint access
with a cycle, and it does not require a sum or product over edges. Mixed
cycle/endpoint dependence is retained inside the disclosed comparison-map
family.

Variant QE leaves `TYPE-U`:

```text
a selected endpoint state/effect member;
a completed/joint multi-system contraction;
a selected nonzero comp_G^QE member and normalization;
the completed endpoint-to-action map;
stationary/2PI blocks and inverse;
non-edge-resolved iota extension.
```

### 2.3 Non-selection comparison table

| Question | Variant Q | Variant QE |
|---|---|---|
| consumes `D_G` | yes | yes |
| consumes endpoint-matched orbit | no | yes, at finite typed interface |
| distinguishes F7 one-edge tuples | forbidden | permitted |
| lets pendant phase enter cycle coordinate | no | no |
| bare edge scalar physical | no | no |
| terminal `T^char` full component used | no | no |
| output type | `Flat(S_G;R)` | `Flat(S_G;R)` |
| completed contraction built | no | no |
| member/normalization selected | no | no |

```text
ROUTE_RECOMMENDATION = none
ROUTE_SELECTION_BY_OUTCOME = false
```

---

## 3. B3 — authored choice table and minimality

Every new field is proposal-level. A `PASS_WITHIN_PROPOSAL` below is not a
`TYPE-P` promotion.

| ID | Authored field | Proposed content | Alternatives considered | Minimality | Void condition |
|---|---|---|---|---|---|
| A1 | consumer-route family | retain `{Q,QE}` with no member selected | quotient only; endpoint only; additive forced sum | DoR-015 supplies exactly two lawful scalar routes | hidden selection or a third untyped route |
| A2 | action codomain | smooth `Flat(S_G;R)` | analytic flat class; merely continuous functions; unrestricted smooth actions | exactly the live all-jet survivor class; analytic would force zero, continuous lacks jets | active-section finite jet changes or reality failure |
| A3 | comparison law | global compatible family `Comp^r` built from the algebraic stage spaces `TensorComp_G^r` in B1-10–B1-14, obeying B1-15–B1-18 and all falsifiers | one selected map; terminal-scalar map; arbitrary nonnatural maps | family adoption avoids answer-defined selection and hidden completion while supplying the missing typed arrow | empty family, finite mismatch, or member-dependent downstream claim without invariance proof |
| A4 | action anchor | equal-history/identity correction is zero | arbitrary additive constant; selected reference history | least anchor compatible with normalized influence/log germ | identity history yields nonzero correction |
| A5 | normalization standing | retain the `R^x` rescaling action on nonzero members plus the distinguished zero member; no numerical unit fixed | declare unit normalization; infer it from a response target; suppress the action | exposes rather than hides the freedom proved by the flat-family countermodel | any claimed value without a ratified normalizer |
| A6 | endpoint carrier | full orbit family `(v_t,R_e,v_s)/Gamma`, all edges, matching data | bare `R_e`; selected endpoint frame; already-contracted global scalar | least carrier making route 2 gauge invariant | F7 covariance fails or an endpoint/frame is selected |
| A7 | mixed QE dependence | one joint comparison-map family; no additive decomposition assumed | `phi_Q+phi_E`; product ansatz; no mixed terms | avoids double counting and hidden contact assumptions | a mixed term is smuggled outside `Comp^QE` or finite restriction fails |
| A8 | stage covariance | contravariant carrier restriction; upward action transport only on rank-preserving isomorphisms; vertical new-cycle increments finite-tested independently | universal upward map; selected representative; ignore new cycles | exactly the V003/Z7 boundary | any cycle-creating upward square is asserted |
| A9 | endpoint batching | E_post-ordered transport composition with matching endpoints | reversed order; commutative product; selected path subdivision | forced interface shape of the ratified ordered CTP law | batching or E_post regression fails |
| A10 | family-invariance discipline | downstream result valid only if invariant across route/member/normalization family or after later ratification | silently choose a member; average members | no-selection analogue of the standing realization discipline | member-dependent result reported as physical |

### 3.1 Honest family size

The route family has two elements. At every stage where the proven flat-germ
class is nonzero, each route's comparison family is infinite-dimensional and
nonzero rescaling is not fixed. A stage with `Flat(S_G)={0}` has the honest
singleton correction fiber. The proposal does not pretend the global choice
is finite.

```text
ROUTE_FAMILY_SIZE = 2
COMPARISON_MEMBER_FAMILY_SIZE = infinite_on_nonzero_flat_stages |
  singleton_zero_on_zero_flat_stages
NONZERO_RESCALING_ACTION = explicit
ZERO_MEMBER = distinguished
```

### 3.2 Why no explicit nonzero formula is selected

The transverse-action determination exhibits

```text
phi_(d,a)(c,t)=d a c exp(-1/t^2) for t!=0,
phi_(d,a)(c,0)=0,
```

as a structural counterfamily. It proves that even a symbolic divergence
datum does not select the coefficient, cycle functional, or flat member.
Selecting one here would be answer-defined authorship hidden inside a
definition. The gate therefore proposes the complete admissible family and
requires a later normalization/member ruling before any member-sensitive
quantity is consumed.

This is still an authored physical proposal: A1–A10 define which family is
admissible and how it interfaces with ratified data. It is not a claim that
ratified data already derived a member.

---

## 4. Consistency obligations and finitely executable checks

### 4.1 DoR-008 finite falsifier

The transverse correction must not alter the exact sealed active-section
source results. For every finite stage and every `m>=1`, require

```text
(D^m phi_G^r)|_(S_G)=0.                           (C1)
```

This is true by the definition `phi_G^r in Flat(S_G)`. It is a statement on
the physical active section itself; no absent source-to-action identity map
is invoked. Therefore adding the proposed correction leaves the finite
active-section shadows exactly:

```text
Q-243: retarded candidate block zero and p-free;
Q-279: probes-on retarded pattern unchanged;
sealed noise/dephasing blocks remain in the base action, not re-authored
inside the transverse correction.
```

No source-kernel zero is renamed a stationary action Hessian. The check is
only that adding the proposed correction preserves the already sealed jets.

```text
DOR008_ACTIVE_SECTION_ALL_JETS = PASS_WITHIN_PROPOSAL
SOURCE_ACTION_CARRIER_IDENTIFIED_WITH_PHYSICAL_ACTION = false
```

Off-section physical action restrictions remain a door; no nonexistent
finite target is claimed reproduced.

### 4.2 E_post and time arrow

Route Q receives the signed edge orientation already carried by `D_G`.
Route QE constructs

```text
R_CTP,e=T_-,e^dagger T_+,e
```

and batches in E_post order. Reversing the order or using the refuted root is
outside both proposed families.

```text
E_POST_ORDER = PASS_WITHIN_PROPOSAL
EXCHANGED_ROOT_USED = false
```

### 4.3 Reality

On route Q,

```text
D_G(Theta Z)=Theta_Q D_G(Z).
```

On route QE, endpoint conjugation sends the orbit to its adjoint/reversed
orbit while preserving the matched scalar. Require each comparison member
to obey

```text
comp_G^r(Theta_Y y)(Theta_X x)
 =conjugate(comp_G^r(y)(x)).                      (C2)
```

The real fixed class in `(B1-9)` satisfies this.

```text
REALITY_COVARIANCE = PASS_WITHIN_PROPOSAL
REALITY_AS_POINTWISE_INVARIANCE = not claimed
```

### 4.4 Batching

Prefix batching is exact:

```text
r_[a,b]=product_(j=a)^b r_j^(sign_j).
```

Endpoint batching composes transports associatively in E_post order, and
intermediate endpoint transformations cancel against matching data. Thus
`(B1-15)` and the carrier part of `(B1-16)` commute.

```text
BATCHING_QUOTIENT_LEG = PASS_WITHIN_PROPOSAL
BATCHING_ENDPOINT_LEG = PASS_WITHIN_PROPOSAL |
  scope: finite matching endpoint data; no completed contraction
```

### 4.5 Identity history and zero extension

At equal history,

```text
r_j=1,
D_G=[1],
R_CTP,e=I.
```

A4 requires `phi=0` there. For a cycle-rank-preserving identity extension,
V003 proves the physical upward map and `(B1-18)` is typed. For a cycle-
creating identity edge, no upward map is claimed; the new finite stage is
tested independently.

```text
EQUAL_HISTORY_ANCHOR = PASS_WITHIN_PROPOSAL
RANK_PRESERVING_IDENTITY_EXTENSION = PASS_WITHIN_PROPOSAL
CYCLE_CREATING_UPWARD_ACTION_MAP = absent by theorem
```

### 4.6 Family naturality and no selection

For every signed realization isomorphism `alpha`, require

```text
comp_(G')^r alpha_Y = alpha_Act comp_G^r.         (C3)
```

Pullback by `alpha_Y` preserves the complete continuous coefficient algebra,
and pullback by `alpha_X` preserves `Flat(S)` because `alpha_X(S_G)=S_(G')`.
Therefore it preserves the algebraic tensor family `(B1-11)` and proves
`(C3)` at family level. The proposal does not average over or select a
realization. Its exact selection scan is

```text
rank selected = false
rank ratio evaluated = false
orientation selected = false
frame selected = false
filtration selected = false
realization member selected = false
edge selected = false
cycle basis selected = false
endpoint state/effect selected = false
comparison member selected = false
normalization selected = false
consumer route selected = false
p evaluated = false
```

```text
BRANCH_A_FAMILY_NATURALITY_STANDARD = PASS_WITHIN_PROPOSAL
```

### 4.7 Pendant split

Set all cycle characters to one and the pendant character to `w!=1`.

```text
D_G=1,
T_G^char=w.
```

Variant Q is constant under this change because it consumes `D_G`. Variant
QE may see a lawful endpoint-matched change only in `E_G^match`; it cannot
move the `Q_G` coordinate. Neither variant consumes full-component `T^char`
as the cycle leg.

```text
PENDANT_D_VS_TCHAR_SPLIT = PASS_WITHIN_PROPOSAL
TREE_CONTENT_INJECTED_INTO_CYCLE_LEG = false
```

### 4.8 One-edge F7 check

For a connected one-edge tree, `Q_G={*}`.

```text
r_1=chi_n(T_-^dagger T_+),
P_G^fam=(Z_0=1,Z_1=r_1),
D_G(P_G^fam)=[1] in Q_G={*}.

Variant Q: comp_G^Q([1])=0 by the identity/tree anchor; it cannot
           distinguish F7 tuples.
Variant QE: ([1],[v_t,T_-^dagger T_+,v_s]) is retained in Y_G^QE;
            changing its matched contraction changes the endpoint-orbit
            coordinate while the quotient coordinate remains [1].
```

This explicitly executes the one-edge carrier calculation.  It neither
calls `r_1` a cycle nor contracts it without matching endpoint data.

This is the required separation between the variants, not a reason to select
one.

```text
F7_WITNESS_Q = EXCLUDED_BY_VARIANT_DEFINITION
F7_WITNESS_QE = REPRESENTABLE_AT_FINITE_INTERFACE
```

### 4.9 S8-A and all-rank cycle visibility

Because both variants use `D_G`, the S8-A character

```text
H_(c_3)=Z_1 Z_2 Z_3^(-1)
```

survives and is inverted under the edge exchange. No record-visible cycle is
in the kernel of either cycle leg.

```text
S8A_C3_SURVIVES = PASS_WITHIN_PROPOSAL
NO_RECORD_VISIBLE_CYCLE_DELETED = PASS_WITHIN_PROPOSAL
```

---

## 5. B4 — self-kill battery

### K1 — target-tuning attack

**Attack.** Search every membership condition for a desired `p`, response,
or downstream coupling consequence.

**Result.** None occurs. `Flat(S)`, naturality, the two carriers, and finite
restrictions are stated before any outcome. `p` is not an argument of the
comparison-family definition.

```text
K1_TARGET_TUNING = PASS
```

### K2 — hidden-normalization attack

**Attack.** If `comp` is admitted, test `lambda comp` for symbolic nonzero
`lambda`.

**Result.** The structural certificates do not fix `lambda`. Therefore a
unique normalization is **not** claimed. A5 exposes the rescaling action on
nonzero members, with the zero member separately distinguished. Any later member-sensitive value is
blocked until a ratified normalizer/member rule exists.

```text
K2_UNIQUE_NORMALIZATION = REFUTED | TYPE-R
K2_PROPOSAL_SURVIVES = yes | family-level only
```

### K3 — pendant witness

**Attack.** Feed the cycle-plus-pendant history with cycle characters one and
pendant `w!=1`.

**Result.** Section 4.7 passes. No variant substitutes full `T^char` for `D`.
QE retains any endpoint effect only in the separately typed endpoint leg.

```text
K3_PENDANT = PASS
```

### K4 — cycle-creating extension

**Attack.** Add an identity parallel edge to a one-edge tree and demand an
upward quotient/action map.

**Result.** The demand is rejected, as V003 requires. B1.5 scopes action
restriction to the basic class and rank-preserving isomorphisms. New-cycle
content is finite-tested at the new stage; it is not pulled from a point.

```text
K4_CYCLE_CREATING_EXTENSION = PASS
FALSE_UPWARD_MAP_PRESENT = false
```

### K5 — F7 endpoint-contraction attack

**Attack.** Hold the one-edge quotient fixed and vary the matching endpoint
tuple so `<v_t,R_e v_s>` changes.

**Result.** Variant Q forbids this input and makes no completeness claim for
it. Variant QE retains it as a joint orbit coordinate. The attack therefore
separates the two variants without killing either.

```text
K5_F7 = PASS_WITH_VARIANT_SPLIT
```

### K6 — fresh double-counting/contact attack

**Attack.** Write `phi_QE=phi_Q+phi_E` and shift a closed-cycle contribution
between the two terms. All route-wise covariance checks survive, so the
decomposition is nonunique and can double count the same physical content.

**Result.** The additive decomposition is removed. A7 makes `Comp^QE` one
joint map family; any mixed/contact decomposition must be separately
declared and finite-tested. No coefficient `1` is silently assigned to two
legs.

```text
K6_FORCED_ADDITIVE_SPLIT = REFUTED | TYPE-R
K6_PROPOSAL_REPAIR = removal; one joint QE map
```

### K7 — batching/segmentation attack

**Attack.** Split one endpoint path into two cells and compare the contracted
scalar before and after inserting an arbitrary intermediate frame.

**Result.** E_post-ordered composition and matching intermediate endpoint
actions cancel the inserted frame. A bare product of already-contracted
scalars is not used. The finite carrier square passes.

```text
K7_BATCHING = PASS
```

### K8 — family-empty attack

**Attack.** Ask whether the conditions define an empty comparison family.

**Result.** The zero action correction is a member of both variants, is
reality covariant, natural, anchored, and in `Flat(S)`. On every stage where
the transverse-action artifact proves a nonzero flat germ, that germ together
with the continuous separating coefficient algebra supplies nonzero tensor
members; closing under restriction and realization isomorphisms supplies the
compatible stage family described after `(B1-14)`. On a zero-flat stage, the
singleton zero correction is the complete honest fiber. Thus neither route's
declared family is empty. The attack also exposes that a particular nonzero
physical member is not yet forced.

```text
K8_FAMILY_NONEMPTY = PASS
NONZERO_MEMBER_FORCED = false
```

### K9 — source/action relabeling attack

**Attack.** Define `phi` to be the sealed finite source log functional and
claim the comparison square by identity.

**Result.** Refuted. Q-313 and the action draft distinguish source characters,
physical cycle coordinates, and stationary action corrections. `Comp^r` is
an authored cross-carrier map, not an identity or projection.

```text
K9_SOURCE_EQUALS_ACTION = REFUTED | TYPE-R
```

### 5.10 Self-gate verdict

```text
SELF_KILL_ATTACKS_RUN = 9
HIDDEN_ROUTE_SELECTION_FOUND = false
HIDDEN_NORMALIZATION_FOUND = yes_and_exposed
HIDDEN_ADDITIVE_CONTACT_RULE_FOUND = yes_and_removed
TARGET_TUNING_FOUND = false
PROPOSAL_SURVIVES_SELF_GATE = yes | family-level, two variants
```

---

## 6. B5 — doors and typed interfaces

### Door AC-1 — action member and normalization

```text
INPUT = admissible global Comp^r family, explicit nonzero rescaling action, and
        distinguished zero member;
OUTPUT = one normalized nonzero comp_G^r member, or a proof that all
         downstream results are family-invariant;
STANDING = TYPE-U;
VOID = member chosen by desired response or hidden unit convention.
```

### Door AC-2 — endpoint state/effect and completed contraction

```text
INPUT = finite E_G^match orbit family, DoR-016 relative transports,
        V005 endpoint intertwiners;
OUTPUT = family-natural state/effect contraction at every finite stage and
         its declared completion;
STANDING = TYPE-U;
VOID = selected endpoint frame, bare edge scalar, or unproved joint product.
```

This combines the still-open joint two-system and completed physical
contraction interfaces only as a dependency list; it does not claim either
is built.

### Door AC-3 — physical action restrictions and off-section coverage

```text
INPUT = Act_G=Flat(S_G), finite physical carriers, signed cellular arrows;
OUTPUT = rho_Gamma,N and an action-comparison restriction family covering
         the claimed finite off-section locus, with separation;
STANDING = TYPE-U outside the finite basic class;
VOID = source restriction renamed physical action restriction.
```

### Door AC-4 — stationary/2PI block package

```text
INPUT = selected or family-invariant phi, common completed domain,
        background/locus, two-sector layout;
OUTPUT = stationary Gamma_2PI blocks, kernel/complement mixing,
         lawful Schur/inverse structure;
STANDING = TYPE-U;
VOID = zero source Hessian renamed zero action inverse or cycle inversion
       performed against the standing void clause.
```

### Door AC-5 — non-edge-resolved realization

```text
INPUT = raw history cell lattice and physical incidence family;
OUTPUT = family-natural injective iota_G:C_G->Z^N;
STANDING = TYPE-U;
VOID = any record-visible cycle enters ker(iota_G) or a member is selected.
```

### Door AC-6 — completed analytic structure

```text
INPUT = finite action families and restrictions;
OUTPUT = declared completion topology, measure/contour/boundary data if used,
         Tail_R action, common domains;
STANDING = TYPE-U;
VOID = naive norm completion, weak-* or bidual content, or imported measure
       without its own door flag and finite restriction proof.
```

### Door AC-7 — divergence/depth datum

```text
INPUT = certified divergence datum and typed depth map;
OUTPUT = a provenance-certified parameter/member instruction for Comp^r;
STANDING = TYPE-U;
VOID = shell distance, sequential stage, spectral threshold, or probe Q_N
       identified without a proved map.
```

```text
DOORS_OPEN = AC-1,AC-2,AC-3,AC-4,AC-5,AC-6,AC-7
DOOR_OPENING_CLAIMED = false
```

---

## 7. B6 — standing falsifier and executed finite board

### 7.1 Standing falsifier

For either variant, the adoption voids at the first occurrence of any of:

```text
F1 finite active-section restriction disagrees with Q-243/Q-279;
F2 equal history does not give zero correction;
F3 E_post ordering or reality covariance fails;
F4 batching/restriction square fails on a typed arrow;
F5 a cycle-creating upward map is required;
F6 pendant content enters the D cycle leg;
F7 a nonzero record-visible cycle is deleted;
F8 family naturality requires a realization/member selection;
F9 a downstream result varies across an unselected map/normalization family;
F10 route Q consumes endpoint data or route QE treats a bare edge scalar as
    gauge invariant;
F11 source/action carriers are identified rather than compared;
F12 any field is chosen for a p, response, coupling, or alpha consequence.
```

### 7.2 Every finitely computable check

| Check | Q | QE | Result |
|---|---|---|---|
| equal history `r=1`, `R_CTP=I` | anchored zero | anchored zero | PASS_WITHIN_PROPOSAL |
| one-edge tree | cycle correction zero | endpoint orbit retained | PASS_WITH_VARIANT_SPLIT |
| pendant `w!=1` | `D` unchanged | cycle leg unchanged; endpoint leg separately typed | PASS |
| S8-A `c_3` | survives/inverts | survives/inverts | PASS |
| reality | quotient conjugation | joint endpoint conjugation | PASS |
| batching | prefix/cycle batching | prefix plus E_post transport composition | PASS |
| contravariant restriction | V003 square | V003 plus endpoint square obligation | PASS on finite typed carrier |
| rank-preserving identity extension | inverse restriction isomorphism | same plus identity endpoint leg | PASS |
| cycle-creating identity edge | no upward claim | no upward claim | PASS |
| Q-243/Q-279 active jets | correction zero to all orders | correction zero to all orders | PASS |
| full off-section action value | no sealed target | no sealed target/contraction | TYPE-C, doors AC-2/AC-3 |
| stationary/2PI image | not executed | not executed | TYPE-C, door AC-4 |

No finitely executable check fails. The two `TYPE-C` rows are checks blocked
by named unbuilt interfaces, not physical negatives.

### 7.3 Six-account rows

| Operation | Kernel/applicability | Image | Sector transfer | Restriction square | Tail action | Standing |
|---|---|---|---|---|---|---|
| prefix inversion | trivial on faithful cell characters | cellwise relative characters | network trace to cells | exact | none | ratified input |
| `D_G` | vertex rephasing exactly | `Q_G` all ranks | cells to physical cycles | V003 exact | none | ratified input |
| endpoint orbit | joint vertex action | `E_G^match` | transports plus endpoints to invariant orbit | finite batching | none | PROPOSED |
| `Comp_G^Q` | individual kernel member-dependent; complete family collectively separates where `Flat(S_G)` nonzero | `Flat(S_G)` | cycle quotient to action correction | B1-18 | finite-visible off section; no `Tail_R` claim | PROPOSED family |
| `Comp_G^QE` | individual joint kernel member-dependent; complete family collectively separates where `Flat(S_G)` nonzero | `Flat(S_G)` | cycles plus endpoint access to action correction | B1-18 | finite-visible off section; no `Tail_R` claim | PROPOSED family |
| action completion | unbuilt | none | finite action to completed action | unbuilt | open | TYPE-U AC-6 |
| stationary/2PI | unbuilt kernel/inverse | none | action to 2PI/response | unbuilt | open | TYPE-U AC-4 |

```text
UNFLAGGED_CLASS_FORMATION_FOUND = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 8. Final proposal board

```text
PROPOSED_NOT_ADOPTED -- PENDING PRINCIPAL RATIFICATION (DoR-017 RESERVED)

ACTION_COMPARISON_SQUARE_OBJECT =
  network prefix/cell data
    -> {Q_G OR (Q_G x E_G^match)}
    -> family of scalar corrections in Flat(S_G)

VARIANT_Q = COMPLETE_AS_FAMILY_PROPOSAL
VARIANT_QE = COMPLETE_AS_FINITE_INTERFACE_FAMILY_PROPOSAL |
  completed contraction remains Door AC-2
VARIANT_SELECTED = none

COMMUTATION_OBLIGATIONS = ALL_STATED
FINITE_EXECUTABLE_CHECKS = PASS_WITHIN_PROPOSAL
SELF_KILL_BATTERY = SURVIVES_AT_FAMILY_LEVEL

NORMALIZED_NONZERO_ACTION_MEMBER = NOT_SELECTED / TYPE-U
ACTION_COMPARISON_CONSUMER_CLASS_IS_QUOTIENT_ONLY = UNDECIDED_BY_THIS_ARM
COMPLETED_ACTION_RESTRICTION = TYPE-U
STATIONARY_2PI_BLOCKS = TYPE-U
PHYSICAL_RESPONSE = NO_VERDICT

RATIFICATION_OPTIONS_FOR_PRINCIPAL =
  adopt Variant Q family;
  adopt Variant QE family;
  reject both;
  return for a member/normalization rule.

RATIFICATION_RECOMMENDATION = none | race adjudication and cross-review pending
ACTION_COMPARISON_SQUARE_PROPOSAL = READY_FOR_CROSS_REVIEW

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No alpha, `K_*`, root, response value, rank ratio, or measured constant was
evaluated. No register, plan, tracker, git, commit, or push action was
performed.
