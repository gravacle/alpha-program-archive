# STAGE 8 — O-1 OF RECORD: THE SUB-3/2 CLOCK CEILING FOR kappa_n — THE BETA FIRST-MOMENT TWIN, THE CARRIER-CROSSING CEILING, THE SIGN REDUCTION, AND THE VERDICT — T7SR V001

## BLIND BUILDER — CODENAME CEILING-BUILD — COMMISSION T7SR — [CLAIMED]

Date: 2026-08-15 (session CDT). Role: BLIND builder (CEILING-BUILD).
Commission: THE QUESTION, exact — prove or refuse O-1, the sub-3/2 clock
ceiling kappa_n <= C-dag n^{3/2 - gamma} for an EXPLICIT gamma > 0, where
kappa_n = ||[P, C_n]||_2^2 (the clock-commutator HS norm squared).
Derived, O-1 decides the MO-4-R mass fraction o(1) at rate n^{-gamma}
through the already-sealed reduction (T3SR T-4). Verdict menu as
commissioned: CEILING-DERIVED / CEILING-FAILS-AT-3/2 / POLYLOG-ONLY /
UNDECIDABLE-TODAY. Technique named of record: the ceiling TWIN of the
MO-2 derivation (the exponential collapse consumed r-2's C_n bytes from
below; this commission consumes the same C_n structure from the other
side) — attempted FIRST (§3). COMMISSION IDENTITY, on the record: a
prior commission (T5SR) for this same question DIED IN FLIGHT with ZERO
bytes landed; probed at session start: NO T5SR artifact and NO
CLOCK_CEILING artifact exists in either root; this is a FRESH commission
at a fresh id. "Q-..." tokens inside sealed artifacts are
EXPECTED-UNLOCATABLE by design; noted, never chased (none encountered
beyond the ground artifacts' own notations).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` (kappa_n is the clause's collapse
commutator mass, NOT the record curvature; no record-curvature value
appears anywhere below). ALL_RESULTS = CLAIMED until checked.
DETERMINATION ONLY — no adoption, no authored physics, no member
binding; the registrar consumes.

Fences held: EXACT SYMBOLIC DERIVATION ONLY — one CAS battery (sympy
1.14.0, fresh venv `ceilvenv` under the session scratchpad), reproduced
verbatim in §11 (battery script written ONCE, never corrected; executed
three times, outputs BYTE-IDENTICAL, 15/15 PASS; no pre-final
corrections; ONE non-sharp display constant in a check NAME disclosed in
§11's header — no conjunct affected); every constant symbolic or an
exact rational/closed form; NOTHING numeric evaluated; no floats as
ground; no measured constant; NO value frozen (kappa_n, J_n, S_n, C_*,
c_G, C_G, C_B, C_Q, C_T, C_F, C_N, C_s, M_{K0}, C-dag, gamma, all
thresholds stay symbols or displayed closed forms; symbolic scales
only); no file matching register|road_|ledger|lens|plan|tracker|
THE_HANDOFF|continuation opened (filename-level listing only, per the
audited discipline); no git action; no existing file edited; ONE output
(this artifact) plus its seal sidecar at the commission-distinct path,
probed ABSENT at session start AND re-probed immediately before write.
Marks: DERIVED / CLAIMED / CLASSICAL(cited) / CONDITIONAL(premise
named) span by span.

SWEEP CUTOFF, DECLARED: the corpus sweep of §9 was executed against the
tree state as of **2026-08-15 05:48:15 CDT**. Artifacts sealed after
that instant are not consumed and not swept. O-1/T7SR occupation at the
cutoff: NONE (§9 sw-1); T5SR appears NOWHERE in either root; the
commission id T7SR appears nowhere but here.

---

## 0. VERDICT IN ONE LINE

**UNDECIDABLE-TODAY for O-1 as commissioned — no kappa_n ceiling with
any positive gamma is derivable on today's stock, and the
undecidability is FORCED at the displayed-hypothesis grade (the C12
witness pair: two exact models with IDENTICAL carrier, identical chiral
grading, identical spectrum, identical ball projection, and identical
first moment realize kappa = 0 AND kappa = 1 — the displayed hypothesis
class leaves the sign-crossing free). What IS derived (new, exact,
three partials): (P-1) THE BULK CEILING — the beta first-moment twin of
MO-2 delivers kappa_n <= tr(P Q_n) + 4 <= C_B n^{3/2} + 4 (n >= n_5,
given CL-A through the sealed MO-4 diagonal ceiling; C_B symbolic
closed form): the clock's true upper exponent is now PINNED, alpha-k in
[1, 3/2] — D4's cubic ceiling 2n^3 is superseded by a factor n^{3/2},
the T3SR trichotomy's alpha-k > 3/2 regime is CLOSED, and the T-4
fraction ceiling becomes n-FREE (bounded, NOT o(1): this ceiling alone
does not decide MO-4-R, per the audited c-1 discipline — only a
sub-POWER gain decides at rate). The first-moment route stops at EXACT
power 3/2 and the stop is intrinsic: the beta sum rule computes bulk
first moments sharply (MO-2's own floor makes tr(P Q_n) >= c n^{3/2}),
while kappa_n is a second-moment quantity. (P-2) THE CARRIER-CROSSING
CEILING — the ceiling twin DELIVERS sub-3/2 for the carrier object:
J_n^2 := ||P^perp Q_n P||_2^2 <= C_Q n^{17/12} = C_Q n^{3/2 - 1/12}
(n >= N_Q; explicit gamma_Q = 1/12; constants symbolic closed forms),
by the exact far/near split (reproducing identity + CD tail + collar
geometry) over CL-A plus ONE new classical citation CL-B (the
Plancherel-Rotach turning-point sup bound sup_R |phi_m| <= C_s
m^{-1/12}, same Szego chapter as CL-A, same citation discipline;
disclosure §4/§12). Bracketed against MO-2's sealed floor: C_* n <=
J_n^2 <= C_Q n^{17/12} — the first two-sided carrier-crossing law.
(P-3) THE SIGN REDUCTION — exact and two-sided: C_n = (Q_n - sgn_n)/2 +
E_n with sgn_n := sign(H_n) on ran Q_n and ||E_n||_2 <= 1 (zero-mode
block, rank <= 4), so |sqrt(kappa_n/2) - S_n/2| <= J_n/2 + 1 with S_n
:= ||P^perp sgn_n P||_2: O-1 at any gamma <= 1/12 is EQUIVALENT to the
same ceiling for the SIGN-crossing S_n^2 — given P-2, THE QUESTION IS
EXACTLY THE SIGN-CROSSING QUESTION. The blockers, swept and exact:
(i) no sealed or classical-grade object controls sgn(H_n) — the sign of
the Galerkin-compressed Dirac operator has no kernel law anywhere in
either root (sw-2/sw-3), CL-A/CL-B are Hermite-FUNCTION statements that
control Q_n's kernel and NOT the polar data of H_n, and the audited
CL-4 trap note stands (Widom/Sobolev-class covers scaled
pseudodifferential compressions, not Galerkin spectral projections);
(ii) the smooth-resolvent route dies exactly (C13): sgn = (2/pi)
Int_0^oo H(H^2 + t^2)^{-1} dt has Lipschitz constant 1/t^2 at small t
and the t-integral diverges — closing it REQUIRES a low-energy spectral
density law for H_n (the sibling of the missing 1/2-approach law for
{s_i}, r-3 :469/:808 of record; no carrier at the cutoff). THE NEW
SEALED OBJECT THAT DECIDES: O-1-S (the sign-crossing ceiling): a
certified S_n^2 <= C' n^{3/2 - gamma'} — yields O-1 at gamma =
min(gamma', 1/12) through P-3 + P-2 with today's displays, and then
DECIDED-o(1) for MO-4-R through the sealed T-4; equally, a floor S_n^2
>= c' n^{3/2} would force kappa_n >= c'' n^{3/2} (CEILING-FAILS-AT-3/2,
the O-2 direction's evidence) — BOTH unsupplied at the cutoff. O-1-S
consumes the r-2 realization bytes of H_n (its polar part) plus
genuinely NEW analytic input beyond the corpus's classical grounds.
Nothing fires: MO-4-R stays open and stands exactly as reduced of
record (its trichotomy sharpened: alpha-k <= 3/2 now certified); no
flag of any prior artifact flips; the registrar consumes. CAS battery
15/15 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
file consumed at any grade this session was seal-verified by FULL
digest against its sidecar BEFORE consumption. 6/6 MATCH (shasum -a
256 -c, each `OK`).

```text
T-1 STAGE8_MO4R_GRAM_MASS_T3SR_V001.md            MATCH — the O-1 spec
    (§6 there): kappa_n <= C-dag n^{3/2-gamma}, gamma > 0, both
    symbolic; the reduction display F_n(eps) <= 2 kappa_n/((1-4 eps^2)
    c_G n^{3/2}) (T-4); R_n = (1 - 2 T_n)^{-1} (O-a); the R7 scope
    display (far-class sqrt(n)); kappa_n = ||[P, C_n]||_2^2 (O-b);
    D4's 2n^3 vacuous at power 3/2 (CAS R6 there); read IN FULL.
T-2 STAGE8_MO4R_GRAM_MASS_T3SR_AUDIT_V001.md      MATCH —
    CONFIRMED-WITH-CORRECTIONS; NOTE c-1 consumed at its exact repair:
    a polylog countermodel (kappa = C n^{3/2}/log n) kills the
    trichotomy's middle line — a polylog-only gain does NOT decide O-1
    as specified (O-1 demands a POWER gamma > 0); read IN FULL.
T-3 STAGE8_MO2_KAPPA_RATE_S9AD_V001.md            MATCH — THE TWIN:
    the chiral involution FACT TWO (beta C_n beta = Q_n - C_n - W_n,
    kernel dim = 4(n mod 2) <= 4, ||W_n||_2 <= 2, ||W_n||_op <= 1);
    the C_n realization at bytes (C_n = 1_{(-inf,0)}(H_n) + z_n,
    H_n = Q_n h_0 Q_n on ran Q_n, 0 <= z_n <= Z_n); Q_n = product
    Hermite projector, rank 4n^3; P = sharp ball r = min(t,1-t) > 0;
    ell in {1, sqrt2}; CL-A's sealed statement (Plancherel-Rotach
    fixed-compact, phi_k = A_k[cos theta_k + eps_k], A_k^2 =
    (2/pi)(2k)^{-1/2}, sup_{|t|<=T}|eps_k| <= e_k(T) -> 0); the block
    identity kappa = 2||P^perp C P||_2^2 (M2); the floor kappa_n >=
    C_*(r,ell) n/8, n >= N_0, and ||P^perp Q_n P||_2^2 >= C_* n
    (§4.4); the exact scaling k_n^ell(x,y) = (1/ell) k_n(x/ell,
    y/ell); read IN FULL.
T-4 STAGE8_MO2_KAPPA_RATE_S9AD_AUDIT_V001.md      MATCH —
    CONFIRMED-WITH-CORRECTIONS (COR-1..3, none chain-breaking); CL-4:
    the build "does NOT cite Widom/Sobolev-class" (which cover scaled
    pseudodifferential compressions, not Galerkin spectral
    projections) — the trap note consumed in §7/§8; the Szego 8.22.6
    transcription audit (A1); read IN FULL.
T-5 STAGE8_MO4_CANCELLATION_S9AD_V001.md          MATCH — the Gram
    ground: the CL-A diagonal CEILING display k_n(t,t) <= M_{K0} +
    (8/pi) sqrt(2n) on the fixed compact (§4.3 there, given CL-A);
    G_n two-sided c_G n^{3/2} <= G_n <= C_G n^{3/2}; the crossing
    identity and MO-4-R's naming (§4.5); read IN FULL.
T-6 STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md    MATCH —
    CONFIRMED-WITH-CORRECTIONS (c-1..c-5 cosmetic; c-4's notational
    note respected here: G_n is NEVER used below for the crossing
    operator — the scalar Gram mass alone); A-3 adjudicates the
    diagonal-ceiling display FAITHFUL to CL-A's sealed statement;
    the independent countermodel bracket consumed as typology; read
    at the commissioned depth (verdict, countermodel, per-claim table
    A-1..A-9, corrections, sweep header; battery text at match grade).
```

Consumption through sealed quotes: every r-2/r-3/PA byte relied on
below enters through T-3/T-5 at audited grade (their §1 tables pin
3b5e95b6, bebc0f08, 789338ad and the PA displays); D4's rank/ceiling
bytes (N = 4n^3, kappa_n <= 2n^3) enter through T-1/T-3 at audited
grade. No other file opened. No fenced-class file opened.

---

## 2. THE QUESTION AND THE OBJECTS, AT BYTES

```text
O-1 (the commissioned object, T-1 §6 verbatim shape): a certified
    kappa_n <= C-dag n^{3/2 - gamma}, gamma > 0, both symbolic —
    equivalently any X_n = o(n^{3/2}) certificate. YIELD once sealed:
    DECIDED-o(1) for MO-4-R at rate n^{-gamma} through the sealed T-4
    display F_n(eps) <= 2 kappa_n/((1 - 4 eps^2) c_G n^{3/2}).
kappa_n = ||[P, C_n]||_2^2 = 2 ||P^perp C_n P||_2^2 (block identity;
    T-3 M2 at audited grade; fresh CAS instance C1).
C_n = 1_{(-inf,0)}(H_n) + z_n on ran Q_n; H_n = Q_n h_0 Q_n;
    h_0 = sum_j p_j alpha_j MASSLESS; Q_n = q_n tensor q_n tensor q_n
    tensor 1_4 (index CUBE => exact product projector), rank 4n^3;
    z_n the zero-mode convention, 0 <= z_n <= Z_n, dim ran Z_n =
    4 (n mod 2) <= 4. [T-3 §2.1-2.3 at audited grade]
P = multiplication by 1_{|x| <= r}, spinor-diagonal, r = r(t) =
    min(t, 1-t) > 0 at every collapse-bearing cell time; ell frozen in
    {1, sqrt2}. [T-3 §2.1; T-5 bytes]
beta: the chiral involution — beta^2 = 1, {beta, alpha_j} = 0,
    beta h_0 beta = -h_0, beta Q_n beta = Q_n, beta P beta = P,
    C_n + beta C_n beta = Q_n - W_n, W_n = Z_n - z_n - beta z_n beta,
    ||W_n||_op <= 1, ||W_n||_2 <= 2, rank W_n <= 4. [T-3 FACT TWO]
K3(x,y) = prod_j k_n^ell(x_j, y_j): the carrier's spatial kernel;
    k_n^ell(x,y) = (1/ell) k_n(x/ell, y/ell); reproducing property
    Int_R k_n(x,y)^2 dy = k_n(x,x) EXACT (q_n^2 = q_n; C7). [T-3 §4]
CL-A (sealed classical ground, T-3 §3 verbatim): phi_k(t) =
    A_k [cos(theta_k(t)) + eps_k(t)], A_k = (2/pi)^{1/2}(2k)^{-1/4},
    theta_k = omega_k t - k pi/2, omega_k = sqrt(2k+1),
    sup_{|t| <= T} |eps_k(t)| <= e_k(T) -> 0 for every fixed T.
THE SEALED DIAGONAL CEILING (T-5 §4.3, given CL-A, audited FAITHFUL):
    k_n(t,t) <= M_{K0} + (8/pi) sqrt(2n) =: D_n on each fixed compact
    (M_{K0} symbolic; n-free head constant).
THE SEALED FLOORS (T-3): ||P^perp Q_n P||_2^2 >= C_*(r, ell) n
    (n >= max(n_1, n_2)); kappa_n >= C_* n/8 (n >= N_0).
NEW OBJECTS NAMED HERE: J_n := ||P^perp Q_n P||_2 (the CARRIER
    crossing); sgn_n := 1_{(0,inf)}(H_n) - 1_{(-inf,0)}(H_n) on
    ran Q_n (the SIGN of the compressed generator; sgn_n^2 = Q_n -
    Z_n); S_n := ||P^perp sgn_n P||_2 (the SIGN crossing).
```

---

## 3. THE TECHNIQUE OF RECORD, ATTEMPTED FIRST — THE BETA TWIN, AND EXACTLY WHERE IT STOPS

MO-2's floor ran: beta converts C_n to the explicit Q_n at the price of
a factor 2 and O(1) (triangle DOWNWARD), then CL-A values Q_n's
crossing from below. The ceiling twin must run the same conversion
UPWARD. Two exact displays result — one delivers a ceiling at exact
power 3/2, and the stop against going lower is structural.

### 3.1 The first-moment twin (DERIVED given CL-A): kappa_n <= C_B n^{3/2} + 4

```text
STEP 1 (second moment <= first moment; C5). kappa_n/2 =
   tr(T_n - T_n^2) = sum_i s_i(1 - s_i) <= sum_i s_i = tr T_n =
   tr(P C_n P)   [s - s(1-s) = s^2 >= 0 exact].
STEP 2 (the beta sum rule; C4). beta P beta = P and cyclicity give
   tr(P beta C_n beta) = tr(P C_n); FACT TWO then collapses the first
   moment EXACTLY:  2 tr(P C_n) = tr(P Q_n) - tr(P W_n),
   |tr(P W_n)| <= ||W_n||_1 <= rank x op <= 4  [C5].
STEP 3 (the bulk count, given CL-A). tr(P Q_n) = 4 Int_B K3(x,x) dx
   <= 4 vol(B) (D_n/ell)^3, and with M_{K0} <= (8/pi) sqrt(2n)
   (n >= n_5, threshold symbolic): (D_n)^3 <= 8 (8/pi)^3 (2n)^{3/2}
   [(a+b)^3 <= 8b^3 for a <= b, exact factorization C14]. Hence

     kappa_n <= tr(P Q_n) + 4 <= C_B n^{3/2} + 4,   n >= n_5,
     assembled: 4 * (4 pi/3) r^3 * [8 * (512/pi^3) * 2 sqrt2] *
     n^{3/2}/ell^3 = (16 pi/3)(8192 sqrt2/pi^3) r^3 n^{3/2}/ell^3,
     C_B := 131072 sqrt2 r^3 / (3 pi^2 ell^3)   (closed form,
     symbolic; not claimed sharp).                        [DERIVED
     given CL-A through the sealed T-5 diagonal ceiling]
```

CONSEQUENCES (exact, C14): (i) alpha-k := limsup log kappa_n / log n
<= 3/2 — CERTIFIED (was <= 3 via D4): the T3SR trichotomy's
alpha-k > 3/2 regime is CLOSED; alpha-k in [1, 3/2] (floor: MO-2).
(ii) D4's ceiling 2n^3 is superseded by a factor n^{3/2}. (iii) The
sealed T-4 fraction ceiling at this kappa ceiling has EXACT limit
2 C_B / ((1 - 4 eps^2) c_G) — n-free, POSITIVE: BOUNDED fraction, NOT
o(1). Per the audited c-1 discipline this ceiling does NOT decide O-1
or MO-4-R: at the exact power 3/2 only a sub-power gain decides at
rate. [DERIVED]

### 3.2 The stop, exact: why the twin cannot go below 3/2

```text
(a) The first-moment route is SHARP AT ITS OWN GRADE: tr(P Q_n) >=
    4 vol(ball r/2) (sqrt(2n)/(2 pi ell))^3 >= c n^{3/2} by MO-2's
    sealed diagonal floor on |t| <= r/2 — the bulk count IS n^{3/2}
    two-sidedly. The loss is STEP 1: kappa is a SECOND-moment
    (crossing) quantity; s <= s is tight only at s near 1.
(b) The second moment does NOT collapse under beta. Writing C_n =
    (Q_n - sgn_n)/2 + E_n (C2; E_n := z_n - Z_n/2, ||E_n||_2 <= 1 by
    C3), the crossing block is
      P^perp C_n P = (1/2) P^perp Q_n P - (1/2) P^perp sgn_n P
                     + P^perp E_n P,
    and the beta-EVEN part (the Q_n term) is the only part the
    involution values; the beta-ODD part (the sgn_n term — beta sgn_n
    beta = -sgn_n) is exactly what the floor's triangle discarded and
    exactly what a ceiling must control. The twin therefore REDUCES
    the ceiling question without deciding it: see §5. [DERIVED]
```

---

## 4. THE CARRIER-CROSSING CEILING (DERIVED given CL-A + CL-B): J_n^2 <= C_Q n^{17/12}

The ceiling twin DOES deliver a sub-3/2 ceiling for the object MO-2's
floor valued: the carrier crossing J_n^2 = ||P^perp Q_n P||_2^2 =
4 IntInt_{B x B^c} K3(x,y)^2 dx dy (spinor factor 4; unfold at
identity grade C6). The instrument is an exact far/near split at a
free separation delta in (0, 3r].

### 4.1 CL-B, the one new classical citation (disclosure)

```text
CL-B (Plancherel-Rotach turning-point/global sup bound; classical:
Szego, Orthogonal Polynomials, §8.91 (8.91.3) — the maximum of the
L^2-normalized oscillator eigenfunction is attained near the turning
point and decays):   sup_{x in R} |phi_m(x)| <= C_s m^{-1/12},
m >= 1, C_s an absolute symbolic constant.
```

Discipline: IDENTICAL to CL-A's (cited as classical, never re-derived;
C_s symbolic, never valued; everything used below derived exactly from
the cited statement). CL-B is Plancherel-Rotach-TYPE ground in the
strictest sense (the same Szego chapter as the sealed CL-A; the
turning-point regime rather than the fixed-compact regime). The
commissioned technique clause places this commission at CL-A's
discipline; the T3SR spec's own O-1 entry anticipates "classical
ground at CL-A's discipline". Registrar-facing note (the exact COR-3
pattern of the MO-2 audit): if the registrar rules the CL-B citation
outside the authorized class, §4-§5's forward displays downgrade to
CONDITIONAL(CL-B) — the §3 bulk ceiling (CL-A only) and the VERDICT of
§8 are unchanged either way (no O-1 closure consumes CL-B).

### 4.2 The 1D tail bound (exact given CL-A + CL-B)

For u on the fixed compact (|u| <= 1/(2 ell) <= 1/2 — the same compact
as the sealed floors/ceilings) and any d > 0, with the exact CD
formula (C7) and (a - b)^2 <= 2a^2 + 2b^2 (C8a):

```text
  Ttilde(d) := sup_{|u| <= r/ell} Int_{|v-u| > d} k_n(u, v)^2 dv
     <= n [ phi_n(u)^2 I_{n-1} + phi_{n-1}(u)^2 I_n ],
  I_m := Int_{|v-u| > d} phi_m(v)^2 / (u - v)^2 dv
     <= S_m^2 Int_{|w| > d} w^{-2} dw = 2 S_m^2 / d          [C8a],
  phi_m(u)^2 <= 4 A_m^2   on the compact for n >= n_3
     (CL-A with e_m <= 1; A_m^2 = (2/pi)(2m)^{-1/2}),
  S_m^2 <= C_s^2 m^{-1/6}                                    [CL-B],
  ==>  Ttilde(d) <= (16 C_s^2 / d) (2/pi) n (2(n-1))^{-1/2}
                    (n-1)^{-1/6}
                 <= (32 * 2^{1/6} C_s^2 / pi) n^{1/3} / d
                 <= C_T n^{1/3} / d,   n >= max(n_3, 2),
  C_T := 2^{1/6} * 64 C_s^2 / pi   (non-sharp display constant, a
  ceiling of the assembled 32 * 2^{1/6} C_s^2/pi; C8b pins the
  exponent identities n (2(n-1))^{-1/2} (n-1)^{-1/6} = 2^{-1/2}
  n (n-1)^{-2/3} <= 2^{1/6} n^{1/3} for n >= 2, exact).
```

The ell-scaling is exact and cancels (C9): the tail of k_n^ell at
separation d equals (1/ell) Ttilde(d/ell) <= C_T n^{1/3}/d.

### 4.3 The far/near split (exact) and the assembly

```text
NEAR (|x - y| <= delta): y in B^c forces dist(x, dB) <= delta; the
  3D reproducing identity (C7, product form) collapses the y-integral:
    NEAR <= 4 vol(collar) sup_B K3(x,x)
         <= 4 * 4 pi r^2 delta * (D_n/ell)^3        [collar volume
    (4pi/3)(r^3 - (r-delta)^3) <= 4 pi r^2 delta for delta <= 3r,
    exact factorization C9]
         <= C_N delta n^{3/2},  C_N := 131072 sqrt2 r^2/(pi^2 ell^3)
    (n >= n_5, via D_n^3 <= 8 (8/pi)^3 (2n)^{3/2} as in §3.1).
FAR (|x - y| > delta): |x-y|^2 > delta^2 forces max_j |x_j - y_j| >
  delta/sqrt3 (union over j = 1, 2, 3); dropping the remaining
  constraints (positivity) and collapsing the two spectator
  coordinates by the reproducing identity:
    FAR <= 4 * 3 * vol(B) * [sqrt3 C_T n^{1/3}/delta] * (D_n/ell)^2
        <= C_F n^{4/3}/delta,
    C_F := 8192 sqrt3 r^3 C_T / (pi ell^2)   (n >= max(n_3, n_5, 2);
    (D_n/ell)^2 <= 512 n/(pi^2 ell^2) as in §3.1).
BALANCE (C10). J_n^2 <= C_F n^{4/3}/delta + C_N delta n^{3/2};
  delta := n^{-1/12} (lawful: delta <= 3r once n >= (3r)^{-12}):
    J_n^2 <= (C_F + C_N) n^{17/12} =: C_Q n^{17/12}
           = C_Q n^{3/2 - 1/12},
    n >= N_Q := max(n_3, n_5, 2, ceil((3r)^{-12})).
    EXPLICIT gamma_Q = 1/12 (3/2 - 17/12 = 1/12 exact).   [DERIVED
    given CL-A + CL-B; constants symbolic closed forms; not sharp]
```

CONSISTENCY BRACKET (new, two-sided): with MO-2's sealed floor,
C_*(r, ell) n <= J_n^2 <= C_Q n^{17/12} — the first two-sided
carrier-crossing law of the corpus. Linear floor under a 17/12
ceiling: no conflict at any n; neither side claimed sharp (the truth
is plausibly n polylog n, per the of-record surface diagnosis;
ANTICIPATION only, consumed nowhere).

---

## 5. THE SIGN REDUCTION (DERIVED): O-1 IS EXACTLY THE SIGN-CROSSING QUESTION

```text
THE EXACT SPLIT (C2/C3): on ran Q_n, 1_{(-inf,0)}(H_n) =
  (Q_n - Z_n - sgn_n)/2, so C_n = (Q_n - sgn_n)/2 + E_n,
  E_n := z_n - Z_n/2, ||E_n||_2 <= sqrt(4)/2 * (2 * 1/2) <= 1
  (rank <= 4, per-eigenvalue |z - 1/2| <= 1/2 by a(1-a) >= 0, C3).
THE TWO-SIDED DISPLAY: P^perp C_n P = (1/2) P^perp Q_n P
  - (1/2) P^perp sgn_n P + P^perp E_n P, hence (triangle, exact)
    | sqrt(kappa_n/2) - S_n/2 |  <=  J_n/2 + 1.
THE EQUIVALENCE (C11, exact expansions):
    kappa_n <= (3/2) J_n^2 + (3/2) S_n^2 + 6,
    S_n^2   <= 3 (2 kappa_n + J_n^2 + 4).
  With P-2 (J_n^2 <= C_Q n^{17/12}, n >= N_Q):
  (i) any certified S_n^2 <= C' n^{3/2 - gamma'} (gamma' > 0) yields
      O-1 at gamma = min(gamma', 1/12):
        kappa_n <= (3/2) C_Q n^{17/12} + (3/2) C' n^{3/2-gamma'} + 6
                <= C-dag n^{3/2 - gamma};
  (ii) any certified kappa_n <= C n^{3/2 - gamma} yields the same
      ceiling shape for S_n^2 at min(gamma, 1/12).
  O-1 (at any gamma <= 1/12) <=> O-1-S (the sign-crossing ceiling).
  Given today's P-2, THE QUESTION and the sign-crossing question are
  ONE question. [DERIVED; the constants displayed]
FLOOR SIDE (the O-2 direction's evidence, displayed): a certified
  S_n^2 >= c' n^{3/2} would force kappa_n >= (1/2)(S_n/2 - J_n/2
  - 1)^2 >= c'' n^{3/2} cofinitely (J_n = o(S_n) then, by P-2) —
  CEILING-FAILS-AT-3/2 would follow. Equally unsupplied today.
```

---

## 6. THE WITNESS PAIR — UNDECIDABILITY AT THE DISPLAYED-HYPOTHESIS GRADE IS FORCED

CAS C12, exact: two 4x4 models. beta = diag(1, 1, -1, -1); P =
diag(1, 0, 1, 0) ([beta, P] = 0); carrier Q = 1_4 (so J = 0
IDENTICALLY); H_A = antidiag(1)-blocks (polar part U = 1_2), H_B =
antidiag(sigma_x)-blocks (U = sigma_x); both anticommute with beta,
both have H^2 = 1 (spectrum {+-1}, kernel 0), C = (1 - H)/2 = the
exact sea projection in both. Then:

```text
  IDENTICAL: carrier bytes (Q = 1, J = 0); chiral grading; spectrum
    of H; kernel dimension; ball projection; the first moment
    tr(P C) = 1 (the beta sum rule saturated identically in both).
  DIFFERENT: kappa_A = 0,  kappa_B = 1.
```

Consequence: no identity-grade route quantified over the hypothesis
class my lawful ground displays — {the chiral involution and its sum
rule; the carrier's kernel data (CL-A/CL-B grade, hence any J_n
ceiling); the spectrum/kernel bookkeeping of H_n; the sealed floors}
— can derive ANY kappa ceiling below the first-moment bulk: the
sign-crossing (the polar data U of H_n) is a FREE parameter of that
class, and it alone moves kappa from 0 to its maximum at fixed
everything-else. Deciding O-1 REQUIRES family-specific control of
sgn(H_n) — precisely the O-1-S object of §8. UNDECIDABLE-TODAY is
therefore FORCED, not a search shortfall. TOY STATUS: the pair
refutes derivability claims only; it asserts NOTHING about the record
family's own S_n, which stays undecided in both directions.

---

## 7. THE OTHER EXACT ROUTES, SWEPT — EACH REFUSED AT ITS OWN DISPLAY

```text
RT-A THE SMOOTH-RESOLVENT (DOI) ROUTE. sgn_n = (2/pi) Int_0^oo
     H_n (H_n^2 + t^2)^{-1} dt on ker^perp (exact). The integrand
     f_t(H_n) is Lipschitz with constant sup|f_t'| = 1/t^2 (EXACT,
     C13), so every Birman-Solomyak-type HS commutator bound pays
     1/t^2, and Int_0^eps t^{-2} dt DIVERGES (C13): closing the
     t-integral at small t requires per-eigenvalue smallness of
     |f_t(lambda_i)| = |lambda_i|/(lambda_i^2 + t^2) — i.e., a
     LOW-ENERGY SPECTRAL DENSITY LAW for H_n. None is sealed (sw-3);
     the only sealed low-energy fact is dim ker = 4(n mod 2).
     REFUSED at the exact divergence display.
RT-B COUNTING/MOMENT ROUTES. Every moment route factors through
     tr(T_n - T_n^2) (the clock itself) or tr T_n (the first moment,
     §3.1 — power 3/2 sharp two-sidedly) or rank bounds (4n^3,
     vacuous); the near-1/2 count ceiling needed to improve STEP 1 is
     the missing 1/2-approach/avoidance law of record (r-3 :469/:808
     absences, re-swept sw-3: no carrier). REFUSED.
RT-C THE MIXED-SCHEME DETOUR. kappa_n^mix = ||[Q_n C Q_n, P]||_2^2
     (continuum sea C): [Q_n C Q_n, P] = Q_n C [Q_n, P] + Q_n [C, P]
     Q_n + [Q_n, P] C Q_n gives kappa_n^mix <= (2 sqrt2 J_n +
     ||Q_n [C, P] Q_n||_2)^2 — the first term now certified sub-3/2
     (P-2), but the compressed continuum commutator needs the Riesz/
     CZ kernel bytes of the continuum sea symbol: NOT in this
     commission's lawful ground and NOT CL-A/CL-B-type (Hermite)
     ground; and even granted, the mixed ceiling transfers NOTHING to
     the pure-scheme kappa_n (the transfer is the same beta triangle
     that only runs floor-ward — §3.2(b)). REFUSED as out of ground,
     exactly; noted for the decider menu (§8).
RT-D WIDOM/SOBOLEV-CLASS IMPORT. The audited CL-4 trap note stands at
     bytes (T-4): those theorems cover scaled pseudodifferential
     compressions, not Galerkin spectral projections of compressed
     generators; sgn(Q_n h_0 Q_n) is the latter. No classical carrier
     exists for the needed sign-kernel law; importing one would be
     surface-native new mathematics, not citation. REFUSED.
RT-E INTERPOLATION SHORTCUTS. ||T||_2^2 <= ||T||_op ||T||_1 on
     T = [P, C_n]: ||T||_op <= 1 (projections) and ||T||_1 has no
     sealed sub-rank ceiling (the trace-norm of the commutator is
     exactly the hard object; rank gives 4n^3, vacuous). Cauchy-
     Schwarz tr T^2 >= (tr T)^2/rank subtracts O(1). REFUSED at their
     own arithmetic.
```

---

## 8. THE VERDICT AND THE DECIDER MENU

**VERDICT: UNDECIDABLE-TODAY** (for O-1 as commissioned: no explicit
gamma > 0 ceiling for kappa_n is derivable on the sealed stock at the
cutoff). NOT CEILING-DERIVED (the witness pair §6 forces the refusal
at the displayed-hypothesis grade). NOT CEILING-FAILS-AT-3/2 (no
lower-bound obstruction above MO-2's linear floor exists either; the
O-2-direction evidence display of §5 is equally unsupplied). NOT
POLYLOG-ONLY (no kappa_n gain below exact power 3/2 of ANY size is
derivable — and per the audited c-1, even a polylog-only gain would
not decide O-1 as specified).

```text
BLOCKER-1 (the odd part): no sealed or classical-grade control of
  sgn(H_n) — the sign-crossing S_n is free at the displayed-
  hypothesis grade (C12); CL-A/CL-B control the CARRIER kernel only;
  Widom-class does not apply (audited CL-4); sw-2/sw-3: no carrier.
BLOCKER-2 (the density law): the smooth-resolvent route requires a
  low-energy spectral density law for H_n (C13 divergence, exact);
  none sealed; the sibling absence of record — the quantified
  1/2-approach/avoidance law for {s_i} (r-3 :469/:808) — re-swept at
  the cutoff: no carrier.

THE NEW SEALED OBJECT THAT DECIDES:
O-1-S (THE SIGN-CROSSING CEILING): a certified S_n^2 =
  ||P^perp sgn(H_n) P||_2^2 <= C' n^{3/2 - gamma'}, gamma' > 0, both
  symbolic. CONSUMES: the r-2 realization bytes of H_n (the polar
  part sgn(H_n) = H_n |H_n|^{-1} — the object's natural form) plus
  genuinely NEW analytic input (a Galerkin-sign kernel law or an H_n
  low-energy density law; no classical carrier exists in the corpus's
  authorized ground classes). YIELD: O-1 at gamma = min(gamma', 1/12)
  through today's §5 + §4 displays — and then DECIDED-o(1) for
  MO-4-R at rate n^{-gamma} through the sealed T-4, nothing else
  needed. EQUALLY: a certified floor S_n^2 >= c' n^{3/2} yields
  CEILING-FAILS-AT-3/2 (kappa_n >= c'' n^{3/2} cofinitely, §5),
  the O-2 direction's missing evidence.
ALTERNATIVE DECIDER (equivalent core): an H_n low-energy density law
  strong enough to close RT-A's t-integral; or the joint-localization
  package O-2 of record (unchanged, T-1 §6).

POSITION AFTER THIS COMMISSION (the sharpened trichotomy): alpha-k in
  [1, 3/2] (P-1 + MO-2; the > 3/2 regime CLOSED). At alpha-k = 3/2
  exactly, the pure-power route saturates (T-1 CAS R6); any
  kappa_n = o(n^{3/2}) certificate still decides o(1) via T-4 (the
  audited c-1 repair, consumed at its exact statement); absent one,
  localization decides. The carrier object is now KNOWN sub-3/2
  (J_n^2 <= C_Q n^{17/12}): the entire remaining question is the
  polar/odd part — one named object, two directions, both open.
```

---

## 9. THE SWEEP (exhaustive at this displayed sweep only; cutoff in header)

```text
ROOTS (as commissioned): /Users/bgm/MB Work/alpha-program-archive/
workspace (primary) and /Users/bgm/Documents/New project/
gravity_emergence_evidence_program/
alpha_fundamental_record_action_cleanroom_v003 (cleanroom). Fenced
name classes (register|road_|ledger|lens|plan|tracker|THE_HANDOFF|
continuation) excluded from every opening; filename-level listing
only; .proof_deps vendored library files typed out.
KEYS RUN: "T5SR"; "T7SR"; "CLOCK_CEILING"; "clock ceiling";
"kappa_n <="; "sign(H"; "sgn("; "Widom"; "polar"; "turning point";
"8.91"; "Cramer"; "Askey"; "n^{-1/12}"; "17/12".
FINDINGS:
 sw-1 O-1/T5SR/T7SR OCCUPATION: NO T5SR artifact and NO CLOCK_CEILING
      artifact exists in either root (probed at session start AND at
      the cutoff); "clock ceiling" carriers are exactly the T3SR pair
      (the O-1 spec and its audit — the commission's own ground);
      T7SR appears nowhere but this artifact. UNOCCUPIED; the
      died-in-flight predecessor left zero bytes, as commissioned.
 sw-2 SIGN-KERNEL CARRIERS: "sign(H" has ZERO hits in both roots;
      "sgn(" hits are eight OTHER-lane artifacts (winding/index/
      coframe/solution-set families) plus one audit with an exact
      rational "17/12" — ALL probed at count level: zero occurrences
      of kappa_n, C_n P C_n, or clock-ceiling content in any of them
      (match-level probe displayed in the work log; files not
      opened). NO sign-crossing law, kernel law, or polar-part
      control exists anywhere in either root.
 sw-3 CEILING/DENSITY CARRIERS: every "kappa_n <=" hit is the D4
      display, the sealed floors, or an absence disclaimer (the same
      seven files the T3SR audit's later sweep certified, f-1 there);
      no H_n low-energy density law and no 1/2-approach law carrier
      exists (re-confirming r-3 :469/:808 and T3SR sw-3 at this later
      cutoff). "Widom" carriers: the MO-2/T3SR pairs' own disclaimer
      lines only.
 sw-4 CLASSICAL-GROUND CARRIERS: "turning point"/"Cramer"/"Askey"/
      "8.91"/"n^{-1/12}": zero corpus carriers (vendored sympy files
      and unrelated exact rationals only, typed out at filename/
      match level): CL-B is NEW citation input, nowhere sealed,
      contradicted by nothing sealed.
 sw-5 CLEANROOM ROOT: zero hits on every key among corpus artifacts
      (one "kappa_n" filename hit is a cumulant GLOSSARY symbol in an
      unrelated envelope-forcing artifact — match line displayed in
      the work log; a different object, typed out).
```

Each sentence claims only this displayed sweep at the declared cutoff.

---

## 10. CONSISTENCY WITH THE SEALED STOCK — EVERY CONTACT POINT

```text
X-1 vs D4 (kappa_n <= 2n^3): P-1's C_B n^{3/2} + 4 sits BELOW 2n^3
    cofinitely (ratio ~ n^{3/2}/2 C_B grows) — a strict tightening,
    no contradiction (both ceilings true; D4 stays of record).
X-2 vs MO-2 (kappa_n >= C_* n/8; J_n^2 >= C_* n): linear floors under
    the 3/2 and 17/12 ceilings — consistent brackets at every n;
    the floor quantifiers untouched; MO-2's floor is consumed ONLY at
    its sealed display (§3.2(a), §4.3 bracket).
X-3 vs T-1's T-4 reduction and D4-vacuity display: P-1 substituted
    into T-4 gives the n-free fraction ceiling (C14 limit exact) —
    STRICTLY MORE INFORMATION than D4's divergent substitution, still
    NOT o(1): T-1's "the o(1) side is exactly a clock-ceiling
    question" stands; its BLOCKER-1 (no sub-3/2 ceiling) is
    re-confirmed and SHARPENED (the missing piece is now exactly the
    sign-crossing, §5).
X-4 vs the audited c-1 (T-2): consumed at its exact repair — §3.1's
    exactly-3/2 ceiling is displayed as NOT deciding; no trichotomy
    middle-line claim is made; the alpha-k > 3/2 regime closure (P-1)
    refines the trichotomy without touching c-1's content.
X-5 vs T-5/T-6 (the Gram ground): the diagonal ceiling is consumed at
    its audited-FAITHFUL grade (A-3 there); G_n (the scalar Gram
    mass) is consumed nowhere below — no crossing-operator/G_n
    notational collision (c-4 respected); the T-5 crossing identity
    is not re-walked.
X-6 vs r-2's rate-free construction: P-1/P-2/P-3 bound NOTHING r-2
    forbids; the sign object sgn_n is exactly r-2's un-diagonalizable
    core, and this artifact VALUES it nowhere — it names its crossing
    as the decider. No sealed sentence contradicted.
X-7 vs H-R / FRAME-N2: rho_n = ||R_n||_op appears NOWHERE in this
    artifact as a bound (the resolvent enters no display); H-R never
    defaulted.
X-8 FENCES: no closure claimed; MO-4-R stays open and reduced; O-2
    untouched; no flag of any prior artifact flips; alpha_computed /
    proof_authorized / kappa_record_computed all false; every
    constant symbolic or an exact closed form; the witness pair's
    exact rationals are TOY-SEPARATED (§6).
```

---

## 11. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `ceilvenv` under the session scratchpad;
nothing written to the workspace but this artifact and its seal. The
final battery script was written ONCE and never corrected: 15/15 PASS;
no pre-final corrections. Run count, on the record: the identical
script executed three times total (the adjudication run, a capture run
redirecting stdout to the splice file, and a diff run) — outputs
BYTE-IDENTICAL across runs (diff empty), zero edits between. Tooling
disclosure, on the record: check C8b's NAME displays the assembled
tail constant as C_T = 2^{1/6} * 64 C_s^2/pi; the §4.2 derivation
assembles the smaller 32 * 2^{1/6} C_s^2/pi <= C_T — the displayed
C_T is a lawful non-sharp ceiling constant (constants nowhere claimed
sharp); every CAS conjunct is exact and unaffected.

```python
# CEILING-BUILD CAS battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
# ceilvenv under the session scratchpad). Commission T7SR. Every constant
# symbolic or an exact rational/closed form; nothing numeric evaluated; no
# floats as ground. All checks are exact-identity consistency checks of steps
# whose operator-theoretic content is derived in the artifact text.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== C1 -- block identity: ||[C,P]||_2^2 = 2||P^perp C P||_2^2 =====
a11, a22, a33 = sp.symbols('a11 a22 a33', real=True)
z12, z13, z23 = sp.symbols('z12 z13 z23')
Cm = sp.Matrix([[a11, z12, z13],
                [sp.conjugate(z12), a22, z23],
                [sp.conjugate(z13), sp.conjugate(z23), a33]])
Pm = sp.diag(0, 1, 1); Pp = sp.eye(3) - Pm
comm = Cm*Pm - Pm*Cm
ok("C1 BLOCK IDENTITY ||[C,P]||_2^2 = 2||P^perp C P||_2^2 for Hermitian C,"
   " projection P (fresh 3x3 symbolic-Hermitian instance, P = diag(0,1,1);"
   " general proof: [C,P] = P^perp C P - P C P^perp, mutually adjoint blocks)",
   sp.simplify(sp.expand(sp.trace(comm.H*comm)
               - 2*sp.trace((Pp*Cm*Pm).H*(Pp*Cm*Pm)))) == 0)

# ===== C2 -- the spectral split 1_{<0}(H) = (Q - Z - sgn(H))/2 on ran Q =====
Hd = sp.diag(-2, R(-1,3), 0, R(1,2))
neg = sp.diag(1, 1, 0, 0)
Z   = sp.diag(0, 0, 1, 0)
sgn = sp.diag(-1, -1, 0, 1)
Q4  = sp.eye(4)
ok("C2 SPECTRAL SPLIT: 1_{(-inf,0)}(H) = (Q - Z - sgn(H))/2 on ran Q, with"
   " sgn := 1_{(0,inf)} - 1_{(-inf,0)} (kernel excluded) -- exact diagonal"
   " exhibit at eigenvalues {-2, -1/3, 0, 1/2}; general case is the"
   " partition Q = 1_{<0} + Z + 1_{>0} (functional calculus)",
   sp.simplify((Q4 - Z - sgn)/2 - neg) == sp.zeros(4, 4)
   and sp.simplify(neg + Z + sp.diag(0,0,0,1) - Q4) == sp.zeros(4, 4))

# ===== C3 -- zero-mode block: ||z - Z/2||_2 <= sqrt(m)/2 <= 1 for m <= 4 =====
az, bz = sp.symbols('a b', real=True)
ok("C3 ZERO-MODE BLOCK: for 0 <= z <= Z (rank m <= 4): per-eigenvalue"
   " 1/4 - (a - 1/2)^2 = a(1-a) >= 0 on [0,1] EXACT factorization, so"
   " ||z - Z/2||_2^2 <= m/4 <= 1 and ||z - Z/2||_2 <= sqrt(4)/2 = 1",
   sp.expand(R(1,4) - (az - R(1,2))**2 - az*(1 - az)) == 0
   and sp.sqrt(4)/2 == 1)

# ===== C4 -- beta sum rule: 2 tr(PC) = tr(PQ) - tr(PW) =====
b2 = sp.symbols('p0:8')
P1 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'pa{i}{j}'))
P2 = sp.Matrix(2, 2, lambda i, j: sp.Symbol(f'pb{i}{j}'))
Pblk = sp.diag(P1, P2)                       # commutes with beta
beta = sp.diag(1, 1, -1, -1)
C4m = sp.Matrix(4, 4, lambda i, j: sp.Symbol(f'c{i}{j}'))
ok("C4 BETA SUM RULE: tr(P beta C beta) = tr(P C) for every P commuting with"
   " beta (4x4, P = arbitrary symbolic block-diagonal, C arbitrary) EXACT;"
   " hence C + beta C beta = Q - W (MO-2 FACT TWO) gives"
   " 2 tr(P C_n) = tr(P Q_n) - tr(P W_n) -- the first-moment collapse",
   sp.simplify(sp.expand(sp.trace(Pblk*beta*C4m*beta) - sp.trace(Pblk*C4m))) == 0)

# ===== C5 -- first-moment ceiling: s(1-s) <= s and the rank-4 trace bound =====
s = sp.Symbol('s', real=True)
w1, w2, w3, w4 = sp.symbols('w1 w2 w3 w4', real=True)
ok("C5 FIRST-MOMENT CEILING: s - s(1-s) = s^2 >= 0 EXACT (so kappa_n/2 ="
   " sum s_i(1-s_i) <= tr T_n = tr(P C_n P)); and 4 - sum|w_i| ="
   " sum(1 - |w_i|) >= 0 for ||W||_op <= 1 rank <= 4: |tr(P W)| <= 4",
   sp.expand(s - s*(1-s) - s**2) == 0
   and sp.expand(4 - (sp.Abs(w1)+sp.Abs(w2)+sp.Abs(w3)+sp.Abs(w4))
                 - ((1-sp.Abs(w1))+(1-sp.Abs(w2))+(1-sp.Abs(w3))+(1-sp.Abs(w4)))) == 0)

# ===== C6 -- discrete crossing unfold: tr(D_A K D_A^c K) = sum_{i in A, j notin A} K_ij^2 =====
Ks = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'k{min(i,j)}{max(i,j)}', real=True))
DA = sp.diag(1, 1, 0)
unfold = sp.trace(DA*Ks*(sp.eye(3)-DA)*Ks)
target = sum(Ks[i, j]**2 for i in range(2) for j in range(2, 3))
ok("C6 CROSSING UNFOLD: tr(1_A K 1_{A^c} K) = sum_{i in A, j notin A} K_ij^2"
   " EXACT for symmetric K (3x3 all-symbolic) -- the kernel-integral unfold"
   " ||P^perp Q_n P||_2^2 = 4 IntInt_{B x B^c} K3(x,y)^2 at identity grade",
   sp.expand(unfold - target) == 0)

# ===== C7 -- CD formula + reproducing property at exact members =====
x, y = sp.symbols('x y', real=True)
def phi(k):
    return (sp.hermite(k, x)*sp.exp(-x**2/2)
            / sp.sqrt(sp.sqrt(sp.pi)*2**k*sp.factorial(k)))
def kker(n, xx, yy):
    return sum((phi(k)*phi(k).subs(x, yy)).subs(x, xx) for k in range(n))
cd_ok, rep_ok = True, True
for n in (2, 3):
    kn = kker(n, x, y)
    cd = sp.sqrt(R(n, 2))*(phi(n)*phi(n-1).subs(x, y)
                           - phi(n-1)*phi(n).subs(x, y))/(x - y)
    cd_ok = cd_ok and sp.simplify(sp.together(kn - cd)) == 0
    rep_ok = rep_ok and sp.simplify(sp.integrate(kn**2, (y, -sp.oo, sp.oo))
                                    - kker(n, x, x)) == 0
ok("C7 CD FORMULA + REPRODUCING PROPERTY at n = 2, 3 EXACT (q_n^2 = q_n is"
   " the general proof of Int k_n(x,y)^2 dy = k_n(x,x); CD is classical"
   " ladder algebra) -- the exact identities the far/near split consumes",
   cd_ok and rep_ok)

# ===== C8 -- tail-bound assembly: the three exact grounds =====
aa, bb, d = sp.symbols('a b d', positive=True)
w = sp.Symbol('w', real=True)
n = sp.Symbol('n', positive=True)
tail_int = sp.integrate(w**(-2), (w, d, sp.oo))
ok("C8a TAIL GROUNDS: 2a^2 + 2b^2 - (a-b)^2 = (a+b)^2 >= 0 EXACT;"
   " Int_{|w|>d} w^{-2} dw = 2/d EXACT -- so I_m := Int_{|v-u|>d}"
   " phi_m(v)^2/(u-v)^2 dv <= 2 S_m^2/d with S_m := sup_R |phi_m|",
   sp.expand(2*aa**2 + 2*bb**2 - (aa-bb)**2 - (aa+bb)**2) == 0
   and sp.simplify(2*tail_int - 2*(2/d)/2) == 0 and sp.simplify(tail_int - 1/d) == 0)
ok("C8b TAIL EXPONENT: n (2(n-1))^{-1/2} (n-1)^{-1/6} = 2^{-1/2} n (n-1)^{-2/3}"
   " EXACT, and n (n-1)^{-2/3} <= 2^{2/3} n^{1/3} for n >= 2 since"
   " (n-1) - n/2 = (n-2)/2 >= 0 and 2^{-1/2} 2^{2/3} = 2^{1/6}: the 1D tail"
   " T~(d) <= C_T n^{1/3}/d with C_T = 2^{1/6} 64 C_s^2/pi displayed",
   sp.simplify(n*(2*(n-1))**R(-1,2)*(n-1)**R(-1,6)
               - 2**R(-1,2)*n*(n-1)**R(-2,3)) == 0
   and sp.expand((n-1) - n/2 - (n-2)/2) == 0
   and sp.simplify((n/2)**R(-2,3) - 2**R(2,3)*n**R(-2,3)) == 0
   and sp.simplify(2**R(-1,2)*2**R(2,3) - 2**R(1,6)) == 0)

# ===== C9 -- collar volume and ell-scaling =====
r, dl, ell = sp.symbols('r delta ell', positive=True)
ok("C9 COLLAR + SCALING: r^3 - (r-delta)^3 = 3r^2 delta - 3r delta^2 +"
   " delta^3 and 3r^2 delta - (r^3 - (r-delta)^3) = delta^2 (3r - delta)"
   " >= 0 for delta <= 3r EXACT: collar volume <= 4 pi r^2 delta; and the"
   " ell-scaling Int f(v/ell)^2/ell^2 dv = (1/ell) Int f^2 (exact"
   " substitution; ell cancels from the tail chain)",
   sp.expand(r**3 - (r-dl)**3 - (3*r**2*dl - 3*r*dl**2 + dl**3)) == 0
   and sp.factor(3*r**2*dl - (r**3 - (r-dl)**3)) == dl**2*(3*r - dl)
   and sp.simplify(sp.integrate(sp.exp(-(y/ell)**2)**2/ell**2, (y, -sp.oo, sp.oo))
                   - (1/ell)*sp.integrate(sp.exp(-y**2)**2, (y, -sp.oo, sp.oo))) == 0)

# ===== C10 -- the delta-balance: gamma_Q = 1/12 exact =====
CF, CN = sp.symbols('C_F C_N', positive=True)
expr = CF*n**R(4,3)/n**R(-1,12) + CN*n**R(-1,12)*n**R(3,2)
ok("C10 BALANCE: at delta = n^{-1/12}: C_F n^{4/3}/delta = C_F n^{17/12} and"
   " C_N delta n^{3/2} = C_N n^{17/12} EXACT; 3/2 - 17/12 = 1/12 EXACT --"
   " the carrier-crossing ceiling J_n^2 <= C_Q n^{17/12} = C_Q n^{3/2 - 1/12}",
   sp.simplify(expr - (CF + CN)*n**R(17,12)) == 0
   and R(3,2) - R(17,12) == R(1,12))

# ===== C11 -- the sign-reduction two-sided algebra =====
Jv, Sv, kv = sp.symbols('J S kappa', nonnegative=True)
ok("C11 SIGN REDUCTION ALGEBRA: 3(a^2+b^2+c^2) - (a+b+c)^2 = (a-b)^2 +"
   " (b-c)^2 + (a-c)^2 >= 0 EXACT: from |sqrt(kappa/2) - S/2| <= J/2 + 1"
   " follow kappa <= (3/2)(J^2 + S^2) + 6 and S^2 <= 3(2 kappa + J^2 + 4)"
   " (exact expansions displayed in text)",
   sp.expand(3*(aa**2+bb**2+d**2) - (aa+bb+d)**2
             - ((aa-bb)**2 + (bb-d)**2 + (aa-d)**2)) == 0
   and sp.expand(2*((Jv+Sv)/2 + 1)**2
                 - (Jv**2/2 + Sv**2/2 + Jv*Sv + 2*Jv + 2*Sv + 2)) == 0)

# ===== C12 -- THE WITNESS PAIR (polar freedom of the odd part) =====
s0 = sp.eye(2); sx = sp.Matrix([[0,1],[1,0]]); Z2 = sp.zeros(2,2)
def blk4(a, b, c, dd):
    return sp.Matrix(sp.BlockMatrix([[a, b], [c, dd]]))
beta4 = sp.diag(1, 1, -1, -1)
p2 = sp.diag(1, 0)
P4 = sp.diag(1, 0, 1, 0)                       # = diag(p2, p2), commutes with beta4
HA = blk4(Z2, s0, s0, Z2)                      # U = 1
HB = blk4(Z2, sx, sx, Z2)                      # U = sigma_x
CA = (sp.eye(4) - HA)/2                        # = 1_{<0}(HA): HA^2 = 1, sgn = HA
CB = (sp.eye(4) - HB)/2
kA = sp.trace(((CA*P4 - P4*CA).H*(CA*P4 - P4*CA)))
kB = sp.trace(((CB*P4 - P4*CB).H*(CB*P4 - P4*CB)))
specA = sorted(HA.eigenvals().items(), key=str)
specB = sorted(HB.eigenvals().items(), key=str)
ok("C12 WITNESS PAIR (the polar/odd-part freedom): two exact models with"
   " IDENTICAL carrier Q = 1 (J = 0), identical beta = diag(1,1,-1,-1)"
   " grading ({beta,H} = 0 both), identical spectrum {+-1} (kernel 0 both),"
   " identical ball projection P = diag(1,0,1,0) ([beta,P] = 0), identical"
   " first moment tr(P C) = 1 (sum rule saturated identically), yet"
   " kappa_A = 0 and kappa_B = 1: the displayed hypothesis class leaves the"
   " sign-crossing FREE -- no identity-grade ceiling below the first-moment"
   " bulk is derivable from it (TOY-SEPARATED: refutes derivability only)",
   sp.simplify(beta4*HA + HA*beta4) == sp.zeros(4,4)
   and sp.simplify(beta4*HB + HB*beta4) == sp.zeros(4,4)
   and sp.simplify(P4*beta4 - beta4*P4) == sp.zeros(4,4)
   and sp.simplify(HA*HA - sp.eye(4)) == sp.zeros(4,4)
   and sp.simplify(HB*HB - sp.eye(4)) == sp.zeros(4,4)
   and specA == specB
   and sp.trace(P4*CA) == 1 and sp.trace(P4*CB) == 1
   and sp.simplify(kA) == 0 and sp.simplify(kB) == 1)

# ===== C13 -- the smooth-DOI obstruction (why the resolvent route needs a density law) =====
lam, t, epsv = sp.symbols('lambda t epsilon', positive=True)
lamr = sp.Symbol('lambda', real=True)
ft = lamr/(lamr**2 + t**2)
dft = sp.simplify(sp.diff(ft, lamr))
ok("C13 SMOOTH-DOI OBSTRUCTION: f_t(lambda) = lambda/(lambda^2 + t^2) has"
   " f_t'(0) = 1/t^2 and sup|f_t| = f_t(t) = 1/(2t) (critical point EXACT);"
   " Int_eps^1 t^{-2} dt = 1/eps - 1 -> oo: the sign-integral"
   " sgn = (2/pi) Int_0^oo f_t(H) dt admits NO HS ceiling from Lipschitz-DOI"
   " grounds alone -- a low-energy density law for H_n is REQUIRED",
   sp.simplify(dft.subs(lamr, 0) - 1/t**2) == 0
   and sp.simplify(ft.subs(lamr, t) - 1/(2*t)) == 0
   and sp.simplify(dft.subs(lamr, t)) == 0
   and sp.limit(sp.integrate(t**(-2), (t, epsv, 1)), epsv, 0, '+') == sp.oo)

# ===== C14 -- the bulk ceiling at exact power 3/2 and its consequences =====
M0, Cs, cG, epw, CB4 = sp.symbols('M_K0 C_s c_G epsilon_0 C_B', positive=True)
D = M0 + (8/sp.pi)*sp.sqrt(2*n)
kap_bulk = 4*(4*sp.pi/3)*r**3*(D/ell)**3 + 4
Frac = 2*(CB4*n**R(3,2) + 4)/((1 - 4*epw**2)*cG*n**R(3,2))
ok("C14 BULK CEILING: (a+b)^3 <= 8 b^3 for a <= b (8b^3 - (a+b)^3 ="
   " (b-a)(7b^2 + 4ab + a^2) + ... verified as polynomial identity"
   " 8b^3-(a+b)^3 = (b-a)(a^2+4ab+7b^2) EXACT, both factors >= 0 for"
   " 0 <= a <= b), so kappa_n <= tr(P Q_n) + 4 <= C_B n^{3/2} + 4 once"
   " M_K0 <= (8/pi) sqrt(2n); the T-4 fraction ceiling then has EXACT limit"
   " 2 C_B/((1-4 eps^2) c_G) (n-free, NOT o(1)); and limsup log kappa/log n"
   " <= 3/2 EXACT: alpha-k is pinned in [1, 3/2] -- the trichotomy's"
   " alpha-k > 3/2 regime is CLOSED, and this ceiling alone does NOT decide"
   " O-1 (per the audited c-1 discipline: only sub-power decides at rate)",
   sp.expand(8*bb**3 - (aa+bb)**3 - (bb-aa)*(aa**2+4*aa*bb+7*bb**2)) == 0
   and sp.limit(Frac, n, sp.oo) == 2*CB4/((1 - 4*epw**2)*cG)
   and sp.limit(sp.log(CB4*n**R(3,2) + 4)/sp.log(n), n, sp.oo) == R(3,2))

print("CEIL-BATTERY-DONE")
```

Output, verbatim (15/15 PASS):

```text
C1 BLOCK IDENTITY ||[C,P]||_2^2 = 2||P^perp C P||_2^2 for Hermitian C, projection P (fresh 3x3 symbolic-Hermitian instance, P = diag(0,1,1); general proof: [C,P] = P^perp C P - P C P^perp, mutually adjoint blocks): PASS
C2 SPECTRAL SPLIT: 1_{(-inf,0)}(H) = (Q - Z - sgn(H))/2 on ran Q, with sgn := 1_{(0,inf)} - 1_{(-inf,0)} (kernel excluded) -- exact diagonal exhibit at eigenvalues {-2, -1/3, 0, 1/2}; general case is the partition Q = 1_{<0} + Z + 1_{>0} (functional calculus): PASS
C3 ZERO-MODE BLOCK: for 0 <= z <= Z (rank m <= 4): per-eigenvalue 1/4 - (a - 1/2)^2 = a(1-a) >= 0 on [0,1] EXACT factorization, so ||z - Z/2||_2^2 <= m/4 <= 1 and ||z - Z/2||_2 <= sqrt(4)/2 = 1: PASS
C4 BETA SUM RULE: tr(P beta C beta) = tr(P C) for every P commuting with beta (4x4, P = arbitrary symbolic block-diagonal, C arbitrary) EXACT; hence C + beta C beta = Q - W (MO-2 FACT TWO) gives 2 tr(P C_n) = tr(P Q_n) - tr(P W_n) -- the first-moment collapse: PASS
C5 FIRST-MOMENT CEILING: s - s(1-s) = s^2 >= 0 EXACT (so kappa_n/2 = sum s_i(1-s_i) <= tr T_n = tr(P C_n P)); and 4 - sum|w_i| = sum(1 - |w_i|) >= 0 for ||W||_op <= 1 rank <= 4: |tr(P W)| <= 4: PASS
C6 CROSSING UNFOLD: tr(1_A K 1_{A^c} K) = sum_{i in A, j notin A} K_ij^2 EXACT for symmetric K (3x3 all-symbolic) -- the kernel-integral unfold ||P^perp Q_n P||_2^2 = 4 IntInt_{B x B^c} K3(x,y)^2 at identity grade: PASS
C7 CD FORMULA + REPRODUCING PROPERTY at n = 2, 3 EXACT (q_n^2 = q_n is the general proof of Int k_n(x,y)^2 dy = k_n(x,x); CD is classical ladder algebra) -- the exact identities the far/near split consumes: PASS
C8a TAIL GROUNDS: 2a^2 + 2b^2 - (a-b)^2 = (a+b)^2 >= 0 EXACT; Int_{|w|>d} w^{-2} dw = 2/d EXACT -- so I_m := Int_{|v-u|>d} phi_m(v)^2/(u-v)^2 dv <= 2 S_m^2/d with S_m := sup_R |phi_m|: PASS
C8b TAIL EXPONENT: n (2(n-1))^{-1/2} (n-1)^{-1/6} = 2^{-1/2} n (n-1)^{-2/3} EXACT, and n (n-1)^{-2/3} <= 2^{2/3} n^{1/3} for n >= 2 since (n-1) - n/2 = (n-2)/2 >= 0 and 2^{-1/2} 2^{2/3} = 2^{1/6}: the 1D tail T~(d) <= C_T n^{1/3}/d with C_T = 2^{1/6} 64 C_s^2/pi displayed: PASS
C9 COLLAR + SCALING: r^3 - (r-delta)^3 = 3r^2 delta - 3r delta^2 + delta^3 and 3r^2 delta - (r^3 - (r-delta)^3) = delta^2 (3r - delta) >= 0 for delta <= 3r EXACT: collar volume <= 4 pi r^2 delta; and the ell-scaling Int f(v/ell)^2/ell^2 dv = (1/ell) Int f^2 (exact substitution; ell cancels from the tail chain): PASS
C10 BALANCE: at delta = n^{-1/12}: C_F n^{4/3}/delta = C_F n^{17/12} and C_N delta n^{3/2} = C_N n^{17/12} EXACT; 3/2 - 17/12 = 1/12 EXACT -- the carrier-crossing ceiling J_n^2 <= C_Q n^{17/12} = C_Q n^{3/2 - 1/12}: PASS
C11 SIGN REDUCTION ALGEBRA: 3(a^2+b^2+c^2) - (a+b+c)^2 = (a-b)^2 + (b-c)^2 + (a-c)^2 >= 0 EXACT: from |sqrt(kappa/2) - S/2| <= J/2 + 1 follow kappa <= (3/2)(J^2 + S^2) + 6 and S^2 <= 3(2 kappa + J^2 + 4) (exact expansions displayed in text): PASS
C12 WITNESS PAIR (the polar/odd-part freedom): two exact models with IDENTICAL carrier Q = 1 (J = 0), identical beta = diag(1,1,-1,-1) grading ({beta,H} = 0 both), identical spectrum {+-1} (kernel 0 both), identical ball projection P = diag(1,0,1,0) ([beta,P] = 0), identical first moment tr(P C) = 1 (sum rule saturated identically), yet kappa_A = 0 and kappa_B = 1: the displayed hypothesis class leaves the sign-crossing FREE -- no identity-grade ceiling below the first-moment bulk is derivable from it (TOY-SEPARATED: refutes derivability only): PASS
C13 SMOOTH-DOI OBSTRUCTION: f_t(lambda) = lambda/(lambda^2 + t^2) has f_t'(0) = 1/t^2 and sup|f_t| = f_t(t) = 1/(2t) (critical point EXACT); Int_eps^1 t^{-2} dt = 1/eps - 1 -> oo: the sign-integral sgn = (2/pi) Int_0^oo f_t(H) dt admits NO HS ceiling from Lipschitz-DOI grounds alone -- a low-energy density law for H_n is REQUIRED: PASS
C14 BULK CEILING: (a+b)^3 <= 8 b^3 for a <= b (8b^3 - (a+b)^3 = (b-a)(7b^2 + 4ab + a^2) + ... verified as polynomial identity 8b^3-(a+b)^3 = (b-a)(a^2+4ab+7b^2) EXACT, both factors >= 0 for 0 <= a <= b), so kappa_n <= tr(P Q_n) + 4 <= C_B n^{3/2} + 4 once M_K0 <= (8/pi) sqrt(2n); the T-4 fraction ceiling then has EXACT limit 2 C_B/((1-4 eps^2) c_G) (n-free, NOT o(1)); and limsup log kappa/log n <= 3/2 EXACT: alpha-k is pinned in [1, 3/2] -- the trichotomy's alpha-k > 3/2 regime is CLOSED, and this ceiling alone does NOT decide O-1 (per the audited c-1 discipline: only sub-power decides at rate): PASS
CEIL-BATTERY-DONE
```

---

## 12. CHOICE LEDGER (every unforced choice, classified; commission T7SR)

```text
CH-1 CL-B (the turning-point sup bound) AS THE ONE NEW CLASSICAL
     CITATION: the load-bearing unforced choice of this commission.
     Grounds: same Szego chapter and same PR family as the sealed
     CL-A; same citation discipline (cited, never re-derived, C_s
     symbolic, every consequence derived exactly); the T3SR O-1 entry
     anticipates classical ground at CL-A's discipline. Blast radius:
     §4 (P-2) and §5's equivalence constants ONLY — §3 (P-1), §6, §7,
     and the VERDICT consume CL-A/sealed ground alone. Registrar
     note: if ruled out of scope, P-2/P-3 downgrade to
     CONDITIONAL(CL-B); the verdict is unchanged (§4.1).
CH-2 delta = n^{-1/12} (the far/near separation): IMMATERIAL
     (derived) — any delta = n^{-a}, 0 < a < 1/6, gives a sub-3/2
     ceiling; a = 1/12 is the exact balance point (C10) and maximizes
     the displayed gamma_Q at this split's grade; only C_Q moves
     otherwise.
CH-3 THE UNION/COLLAR GEOMETRY (delta/sqrt3 coordinate separation;
     the 4 pi r^2 delta collar ceiling): IMMATERIAL(derived) — exact
     positivity/containment steps (C9); constants displayed, none
     sharp.
CH-4 WITNESS-PAIR SHAPE (4x4, U in {1, sigma_x}): IMMATERIAL
     (derived) — any polar pair with [U_B, p] != 0 = [U_A, p] works;
     minimality is display economy (C12).
CH-5 sgn CONVENTION (kernel excluded; z_n absorbed into E_n):
     IMMATERIAL(derived) — every zero-mode convention lands in the
     SAME ||E_n||_2 <= 1 budget (C2/C3), upgrading nothing.
CH-6 FIRST-MOMENT ROUTE DISPLAYED AT s <= s (STEP 1) rather than any
     window split: FORCED-shape — §3.2 shows every refinement of
     STEP 1 requires the near-1/2 count law (absent of record).
CH-7 P-2 STATED FOR J_n (the carrier), NOT PROMOTED TO kappa_n: FORCED
     by §3.2(b)/§6 — the promotion is exactly what the witness pair
     refutes.
CHAIN CHOICE AUDIT: no unforced choice enters the verdict chain. The
     verdict (UNDECIDABLE-TODAY) rests on: the C12 witness pair
     (exact), the sweep absences (sw-2/sw-3), and the C13 divergence
     display — none consumes CH-1..CH-5. P-1 rests on sealed ground +
     CL-A only. The verdict survives every alternative at CH-1..CH-7
     unchanged.
MACHINERY/RELEVANCE: classical operator theory (block decompositions,
     functional calculus, trace positivity, exact kernel identities)
     applied to SEALED constructions; identity cores CAS-pinned;
     SURFACE-DERIVED, not surface-native.
```

---

## 13. TOY_SEPARATION (self-assessment)

```text
CLAIMED CLEAN at the stated quantifiers. C1/C4/C5/C6/C8/C9/C10/C11/
C13/C14 are all-parameter identity/inequality exhibits with universal
quantifiers in the check names. The exact instances (C2's diagonal
spectral exhibit; C3's per-eigenvalue factorization; C7's n = 2, 3
Gaussian members — whose general proofs are the displayed q_n^2 = q_n
projection property and classical CD ladder algebra) are identity-
grade exhibits of displayed general facts. The WITNESS PAIR (C12) is
used ONLY against derivability-from-displayed-hypotheses claims,
which a single exact countermodel per direction lawfully refutes; it
asserts NOTHING about the record family's own sign-crossing S_n,
which stays undecided in both directions (§6 TOY STATUS). CL-A and
CL-B are cited at their own classical quantifiers and consumed no
wider than stated. No model family is a premise; no spectral datum of
the actual family is valued; kappa_n, J_n, S_n, C_*, c_G, C_G, C_B,
C_Q, C_T, C_F, C_N, C_s, M_{K0}, C-dag, gamma, and all thresholds
stay symbols or displayed closed forms. The RULING is the checker's/
audit's, not this artifact's.
```

---

## 14. FLAG BLOCK

```text
CLOCK_CEILING = UNDECIDABLE-TODAY( O-1 — the sub-3/2 clock ceiling
  kappa_n <= C-dag n^{3/2-gamma}, explicit gamma > 0 — is not
  derivable on the sealed stock at the cutoff, and the undecidability
  is FORCED at the displayed-hypothesis grade (C12 witness pair:
  identical carrier, chiral grading, spectrum, ball projection, and
  first moment realize kappa = 0 AND kappa = 1 — the sign-crossing is
  free). NOT polylog-only (no sub-3/2 gain of any size for kappa_n);
  NOT ceiling-fails (no super-linear floor either). Blockers exact:
  no sgn(H_n) kernel/crossing law (sw-2); no H_n low-energy density
  law and no 1/2-approach law (C13 divergence; sw-3); Widom-class
  inapplicable (audited CL-4, re-consumed at bytes). )
PARTIALS_DERIVED = THREE( P-1 THE BULK CEILING: kappa_n <= tr(P Q_n)
  + 4 <= C_B n^{3/2} + 4 (n >= n_5; given CL-A via the sealed T-5
  diagonal ceiling; C_B = 131072 sqrt2 r^3/(3 pi^2 ell^3) displayed,
  not sharp) — beta first-moment twin: STEP s(1-s) <= s + sum rule
  2 tr(P C_n) = tr(P Q_n) - tr(P W_n), |tr(P W_n)| <= 4; alpha-k
  PINNED in [1, 3/2]; D4 superseded by n^{3/2}; T-4 fraction ceiling
  becomes n-free (bounded, NOT o(1) — does not decide, per audited
  c-1). P-2 THE CARRIER-CROSSING CEILING: J_n^2 = ||P^perp Q_n
  P||_2^2 <= C_Q n^{17/12} = C_Q n^{3/2 - 1/12} (n >= N_Q; explicit
  gamma_Q = 1/12; given CL-A + CL-B; far/near split at delta =
  n^{-1/12}; constants C_F, C_N, C_T, C_Q displayed closed-form);
  two-sided bracket with MO-2's floor: C_* n <= J_n^2 <= C_Q
  n^{17/12}. P-3 THE SIGN REDUCTION: C_n = (Q_n - sgn_n)/2 + E_n,
  ||E_n||_2 <= 1; |sqrt(kappa_n/2) - S_n/2| <= J_n/2 + 1; kappa_n <=
  (3/2) J_n^2 + (3/2) S_n^2 + 6 and S_n^2 <= 3(2 kappa_n + J_n^2 +
  4): O-1 at any gamma <= 1/12 <=> O-1-S. )
DECIDER_OBJECT = O-1-S( the sign-crossing ceiling: certified S_n^2 =
  ||P^perp sgn(H_n) P||_2^2 <= C' n^{3/2 - gamma'} — yields O-1 at
  gamma = min(gamma', 1/12) and DECIDED-o(1) for MO-4-R at that rate
  through the sealed T-4; a floor S_n^2 >= c' n^{3/2} instead yields
  CEILING-FAILS-AT-3/2 (kappa_n >= c'' n^{3/2}). CONSUMES: r-2's H_n
  realization bytes (the polar part) + genuinely NEW analytic input
  (Galerkin-sign kernel law or H_n low-energy density law; no
  classical carrier in the authorized ground classes). The O-2
  localization package of record stays the other decider, unchanged. )
CLASSICAL_GROUND = CL-A(sealed, consumed at audited displays) +
  CL-B( NEW citation: Plancherel-Rotach turning-point sup bound
  sup_R |phi_m| <= C_s m^{-1/12}, Szego §8.91.3; same discipline as
  CL-A; blast radius P-2/P-3 constants only; registrar note per the
  MO-2-audit COR-3 pattern: if ruled out of scope, P-2/P-3 downgrade
  to CONDITIONAL(CL-B) and the VERDICT is unchanged. )
CONSUMPTION_BOUNDARY = NOTHING-FIRED( MO-4-R NOT decided (the
  fraction stays open both directions); MO-4/r-3/R-L4b untouched; no
  flag of any prior artifact flips; D4 and the MO-2 floor stand (P-1
  tightens, contradicts nothing); H-R never defaulted (rho_n appears
  in no display); whether to commission O-1-S, the density law, or
  O-2 is the registrar's call; the registrar consumes. )
SEALS_VERIFIED = 6/6( T3SR pair + MO-2 pair + MO-4 pair, full-digest
  sidecar checks before any reliance (§1); r-2/r-3/PA bytes consumed
  through the sealed quoting artifacts at audited grade; "Q-..."
  tokens EXPECTED-UNLOCATABLE, none encountered beyond the ground's
  own notations. )
SWEEP_CUTOFF = 2026-08-15 05:48:15 CDT( keys and findings §9; T5SR
  zero bytes confirmed (no artifact in either root); T7SR unoccupied;
  no sign-kernel carrier; no density-law carrier; no classical
  sup-bound carrier; cleanroom zero (one glossary homonym typed
  out). )
FORBIDDEN_IMPORTS = none( exact symbolic only; no floats as ground;
  no numeric evaluation; no measured constant; NO value frozen —
  every constant and threshold symbolic or an exact closed form; no
  fenced-class file opened; no git action; no existing file edited;
  ONE file written plus its seal; output path probed ABSENT at
  session start and re-probed immediately before write. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv ceilvenv
  under the session scratchpad; battery script written ONCE, never
  corrected; executed three times with BYTE-IDENTICAL output (diff
  empty), 15/15 PASS; script and output reproduced verbatim in §11;
  no pre-final corrections; one non-sharp display constant in a check
  NAME disclosed (§11 header), no conjunct affected.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
ALL_RESULTS = CLAIMED until checked.
CLOCK_CEILING_RESULT = SEALED.
```
