# STEPS 1 AND 2 — TEXT FIXED BEFORE THE NINE-FIELD FORM WAS OPENED

This file is the FORM-QUARANTINE EVIDENCE. Its content was written and closed before any
file containing `PhysicalSig_0`'s field list was opened in this session. The Step-3
exhibition may only read off this text; it may not revise it.

## STEP 1 — THE BUILD

### 1.0 What is composed, and the composition order

Four sealed surface pieces, composed in the order the physics itself imposes
(carrier before state, state before write, write before net):

```text
(a) the record action's cell structure and write geometry
      FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md  (a27a2d57...)
      R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md          (ed4572eb...)
      R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md  (2be95d5b...)
(b) Gen_Omega's source carrier A0 and channel family, read WITH overlay V002 + DoR-013
      STAGE8_GEN_OMEGA_..._V003.md (1be5f6a7...) : A0, {BI,DB,SYM}, d_state/d_ready/d_law
(c) TASK2D's sector structure
      SCALARIZATION_...RESULT_V001.md (d13920e2...) : B = A_F_CTP, B iso C(Y), State(B)<->Prob(Y)
      SOURCE_HISTORY_MAP_...ADJUDICATION_V001.md (ab156ee7...) : the attachment adjudication
(d) the descent/restriction maps' joint action
      STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md (a67ed435...) : i_R, i_B, res_B, E_out
```

### 1.1 GROUNDED — the cell carrier and the write, at bytes

`FUNDAMENTAL_BOUNDARY_RECORD_ACTION_PRINCIPLE_V002.md:70-82` : durable records are
generated by physical boundary degrees of freedom; on every admissible primitive causal
record cell the one-handle record carrier reduces to the two-endpoint projective carrier;
the record carrier supplies the compared alternatives and the connection transports their
active relative phase.

`R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md:17-38` : from `B_ch = P_ch tensor
gamma^5 tensor c_partial` and `tau_R = pi/sqrt(2)` the endpoint unitary is COMPUTED, not
inserted, and is `I_chiral tensor [[0,1,0],[1,0,0],[0,0,-1]]`, i.e. the complete record
action `|r> <-> |p_Q>` and `|e_Q> -> -|e_Q>`.

CELL CARRIER, read off: the record cell is the three-dimensional span `{|r>,|p_Q>,|e_Q>}`.
This is a SURFACE object, not a schema field: it is the carrier the incidence operator acts
on. It agrees in dimension and basis role with Gen_Omega's `iota_cell` codomain
`span{|r>,|p>,|e>}`, and the write agrees with Gen_Omega's one-cell law
`S|r>=|p>, S|p>=|r>` (V003 `:568-580`).

DECLARED COMPOSITION DETAIL, not a choice: Gen_Omega's V003 presentation states `S` only on
`|r>` and `|p>` and states `D(z)=diag(1,z,1)`; it is SILENT on `S|e>`. The incidence result
determines that entry to be `-1` and says so expressly — "the unused edge-sector action is
fixed by the incidence operator" (`R3_4_..._V002.md:44-46`). The composition therefore takes
the edge entry FROM THE INCIDENCE OPERATOR, which is the only sealed determination of it.
Nothing is chosen; a silence upstream is filled by a computation downstream on the same cell.

WRITE GEOMETRY, at bytes: `R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md:16-48` — with
`H_j(t) = v_j(t) B_j` and `integral v_j dt = tau_R`, the generator is fixed through the
pulse so `[H_j(t),H_j(s)]=0` and `T exp[-i integral H_j] = exp(-i tau_R B_j)`: the write is
PROFILE-INDEPENDENT. For distinct cells `[B_j,B_k]=0`, so the endpoint map is independent of
the causal linear extension. These are forced facts about the constructed write, not choices.

### 1.2 GROUNDED — the source carrier and the channel family's action on it

`STAGE8_GEN_OMEGA_..._V003.md:352-363` (A0): `H_src^A := P_0 H_src direct-sum P_ch H_src`,
`A_src^A := End(H_src^A)`, faithful trace `Tr_A`, orthogonal `P_0,P_ch` with
`P_0 + P_ch = I_src`.

`:401-410` (the family `G1_v003`) and `:419-430` (the three anchor members BI, DB, SYM).
Read WITH DoR-013 (`f2a7838d...:9-13`) and overlay V002 (`e85f444c...`, read rule at
`:234-245`): status(member 12) := RATIFIED_FAMILY_LEVEL, no-member clause carried.
*** NO MEMBER IS SELECTED. *** The family acts; the license for using the family rather than
a member is DoR-013's own p_ch-neutrality certificate: every member forces the same
normalized-identity invariant state.

`:628-635` (d_state): `d_state(Omega_prim,N) :=` the unique normalized fixed state of
`P_src`; the anchor theorem then PROVES, rather than defines,
`d_state = I_src / Tr_A(I_src) =: omega_tr`.

NO FIXED-POINT EXECUTION IS RUN. The characterization is cited and the already-proven value
is carried; no iteration is performed and no dimension is evaluated.

`:641-643` (d_ready): `d_ready := C |r>^(tensor N)` — the all-ready record ray, plus root only.
`:650-654` (d_law): `d_law := P_0 tensor I_(3^N) + P_ch tensor W_N^(n)[a]`, exactly DoR-009.

### 1.3 THE COMPOSITION STEP, and why its hypothesis is discharged rather than assumed

`R3_4_CHARGED_INCIDENCE_OUTGOING_STATE_RESULT_V002.md:59-84` supplies an ARBITRARY-N ANALYTIC
theorem with one hypothesis:

```text
product_(j=1)^N [P_0 tensor I + P_ch tensor S_j]
  = P_0 tensor I + P_ch tensor product_(j=1)^N S_j,

and for ANY normalized source density matrix satisfying charge superselection
  rho_S = P_0 rho_S P_0 + P_ch rho_S P_ch,
and the all-ready record state, the completed state is

  rho_N = P_0 rho_S P_0 tensor |r><r|^(tensor N)
        + P_ch rho_S P_ch tensor |p_Q><p_Q|^(tensor N).

Tracing the final record factor returns rho_(N-1) identically.
```

THE HYPOTHESIS IS DISCHARGED BY THE OTHER PIECE, EXACTLY. `d_state`'s output is
`omega_tr = I_src/Tr_A(I_src)`, and because `P_0,P_ch` are orthogonal with `P_0+P_ch=I_src`
(A0, `:352-363`),

```text
P_0 omega_tr P_0 + P_ch omega_tr P_ch
  = (P_0 I_src P_0 + P_ch I_src P_ch)/Tr_A(I_src)
  = (P_0 + P_ch)/Tr_A(I_src)
  = I_src/Tr_A(I_src)
  = omega_tr.
```

The charge-superselection hypothesis is therefore not assumed of the composed object: it is
an identity of the state the preparation channel forces, on the projectors A0 supplies.
The two sealed pieces MATE, and the mating condition discharges itself.

The ready-state hypothesis is discharged the same way: R3_4's theorem requires "the all-ready
record state", and `d_ready` (`V003:641-643`) IS the all-ready ray `|r>^(tensor N)`.

The write hypothesis is discharged the same way: R3_4's `S_j` is "the computed full
root/public swap on cell j" and `d_law` (`V003:650-654`) is the controlled write
`P_0 tensor I + P_ch tensor W_N[a]` on the same projectors. At zero source history the
character is trivial (`D_n[0]=I`, V003 `:761`) and `W_N` reduces to `tensor S_j`, which is
exactly R3_4's left-hand side.

### 1.4 THE OBJECT THAT COMPOSES

```text
Omega_SR,N := ( P_0 tensor |r><r|^(tensor N) + P_ch tensor |p_Q><p_Q|^(tensor N) )
              / Tr_A(I_src),
```

a positive, normalized, charge-superselected joint state on the source-record face
`A_SR = A_src graded-tensor_min R_inf` (typing at `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md
:126-128`), restriction-compatible in `N` by R3_4's own identity, hence a quasi-local state
in the direct limit (`R3_4_..._V002.md:88-106`, where the same central-sequence theorem is
re-established for the incidence-derived states rather than imported).

### 1.5 THE COMPOSITION THAT FAILS, named at its span

`A_C0 = A_SR graded-tensor_min B` with `B = A_F_CTP iso C(Y)`
(`STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md:126-128`). `Omega_SR,N` is a state on the FIRST
tensor factor only. `res_B : State(A_C0) -> State(B)`, `res_B(Omega) := Omega compose i_B`, is
exact (`:213-218`) — and its input is precisely what the build does not reach.

MISSING SURFACE OBJECT, named / typed / located (a physical ingredient, not a schema):

```text
NAME:  the source-history attachment (coupling) map
TYPE:  Attach : (connection/source history, finite incidence, U_N^0) -> U_N[A,J,g]
       together with its representation, carrier embeddings, orientation law,
       time-ordering, and common-origin certificate
LOCUS: STAGE8_TASK2D_SOURCE_HISTORY_MAP_COVARIANT_INCIDENCE_DERIVATION_ADJUDICATION_V001.md
       :314-334  ("No such Attach exists"; SEALED_ATTACH_MAP_EXISTS = false | TYPE-S)
```

This is the ingredient that would let the field/CTP sector `B` be acted on at all. Without it
the composed dynamics is `U_N^0` — history-independent — and the `B` factor is inert.

## STEP 2 — WHAT THE OBJECT IS

### 2.1 Carrier and sectors [forced]

Carrier: `H_src^A tensor (C^3)^(tensor N)`, `H_src^A = P_0 H_src direct-sum P_ch H_src`
(A0, V003 `:352-363`); each record cell is `span{|r>,|p_Q>,|e_Q>}` (R3_4 `:33-38`).
Sectors: source | record, exactly the two tensor factors of `A_SR`. The third sector of the
record-action's own specification `Q_spec[g,a,psi,R]` (`FUNDAMENTAL_..._V002.md:83-95`) — the
field sector — is present in the algebra as `B` and ABSENT from the built state.

### 2.2 Generator / dynamics [forced]

`exp(-i tau_R B_j)` per cell, profile-independent and order-independent
(`R3_4_CAUSAL_CELL_MOVING_FRONT_RESULT_V001.md:16-48`); globally the controlled write
`P_0 tensor I + P_ch tensor W_N[a]` (V003 `:650-654`), which at zero history is
`P_0 tensor I + P_ch tensor tensor_j S_j`.

### 2.3 Stationary structure [forced], and exactly what it extends to

`d_state`'s fixed point is `omega_tr`, unique and FAMILY-INVARIANT: identical across
{BI,DB,SYM}, which is why no member is bound (DoR-013 `:9-13`; V003 `:432-452`).

IT EXTENDS TO THE JOINT OBJECT, and this is the build's substantive result:

```text
SOURCE MARGINAL of Omega_SR,N   = (P_0 + P_ch)/Tr_A(I_src) = omega_tr.
```

The joint state's source marginal returns the preparation channel's own unique fixed state.
The extension is consistent, not merely compatible.

```text
RECORD MARGINAL of Omega_SR,N
  = (1 - kappa_ch) |r><r|^(tensor N) + kappa_ch |p_Q><p_Q|^(tensor N),
  kappa_ch = Tr_A(P_ch)/Tr_A(I_src).
```

THE TWO MARGINALS ARE BOTH FORCED. The record marginal is a classical two-point mixture of
orthogonal pointer states, and its mixing weight is EXACTLY the symbolic sector-dimension
ratio `kappa_ch` that Gen_Omega derives on the source side (V003 `:816-826`). The same
symbolic object is reached from the record side as a marginal weight. NO VALUE IS COMPUTED;
`kappa_ch` stays a ratio of symbolic dimensions, and no dimension is selected or evaluated.

Do NOT flatten: the record marginal is a state on `R_N ⊂ R_inf`. It is NOT a state on
`B = C(Y)`. The record/history distinction is the corpus's own
(`STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md:148-167`, `:271-274`).

### 2.4 Common-origin content — what "same microscopic source" is MADE TRUE BY, at bytes

The target clause is P5 (`STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md:251-253`):
"A positive normalized `rho_pre` on the completed object and admitted record effects `E_r`,
with domains, all supplied by the same microscopic source as P0."

WHAT IS NOW MADE TRUE, at bytes: on the source-record face, the state, the ready ray and the
write all descend from ONE frozen tuple, `Omega_prim,N^v003` (V003 `:609-619`), whose
freeze-order `T0..T8` (`:662-671`) puts the carrier, family and certificates before any
output. Concretely: `omega_tr` from `d_state`; `|r>^(tensor N)` from `d_ready`; the controlled
write from `d_law`; the cell carrier and edge entry from the same incidence operator that
supplies `B_ch`. One tuple, three maps, one cell operator — not a product assembled by
declaration. That is the P5 clause satisfied ON THE SR FACE.

WHAT IS NOT MADE TRUE: P5 says "on the completed object". The completed object is
source-record-FIELD (`:233-263`, P1). The built state lives on `A_SR`, which is `A_C0` minus
its `B` factor. P5 also demands "admitted record effects `E_r`, with domains"; the build
supplies pointer observables and a central sequence, and supplies NO effect/domain package —
`STAGE8_P5_SECTOR_ASSIGNMENT_V001.md` records that the domain object is not supplied and that
P5 sits at the join, needing both sides.

COMMON_ORIGIN therefore = MADE-TRUE-BY (bytes) ON THE SR FACE, NOT-REACHED at the
completed object.

### 2.5 Where the surface itself goes permissive [the decisive display]

Fix everything the surface forces. The `B` direction is still completely free, and the
surface says so in its own sealed words:

```text
STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md:260-269
  "every omega_B in State(B) produces a product state Omega_(out,B) := omega_out tensor
   omega_B ... with the same record marginal omega_out. Since the sealed history-state
   simplex is a continuum, the record marginal alone admits a continuum of such extensions.
   This is an algebraic non-uniqueness theorem at the current receiver."
```

and the survivor table `STAGE8_TASK2D_SCALARIZATION_..._RESULT_V001.md:480-487`: raw
positivity, inclusive normalization, U1 reality, one-cell C0-scope restriction, weak
`rho_pre` type-only existence — EVERY ROW RETURNS "continuum". Each constraint is
failure-capable and each one PASSES FOR ALL. Nothing is minimal; the admissible set is
upward-closed in exactly the sense the section already recorded for the form-first route.

The attachment side has the same shape: `NAMED_ATTACHMENT_PLACEMENTS_COUNT = at_least_4` and
`ATTACHMENT_FAMILY_COVERAGE_PROVED = false` (`SOURCE_HISTORY_MAP_..._V001.md:356-360`) — a
displayed family, unbounded above, with no selector.

THE FINDING: the surface reproduces the form-first route's shape. It is not that the form was
the wrong instrument; it is that the physics is permissive at the same joint, and the form was
faithfully reporting that.

END OF FIXED TEXT. Nothing below Step 2 was written before the form was opened.
