# STAGE 8 — F0-PIN-CHECK: BLIND ADVERSARIAL VERIFICATION OF THE COMPUTAND DEFINITIONAL CHAIN
## CROSS-LINEAGE REFUTATION PASS OVER STAGE8_F0_COMPUTAND_CHAIN_V001.md — [CLAIMED]

Date: 2026-08-13
Role: BLIND adversarial verifier, DEFAULT = REFUTE. Citation and typing only.
Nothing computed, bounded, estimated, evaluated, or approximated. No value of
`kappa_record`, `K_*`, `alpha`, or any quantity appears. No measured value
appears anywhere.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`

Assembly under test:
`STAGE8_F0_COMPUTAND_CHAIN_V001.md`, sha256 recomputed at path this relay:
`05eeab537de2be3653e1c0e15187e913fb8c8811b5b0cf610bfecd1e07eb7a25` —
MATCHES-TASKED, and matches its own sidecar
`STAGE8_F0_COMPUTAND_CHAIN_V001.md.seal.sha256`.

---

## 1. SEAL RE-VERIFICATION AT PATH (all 18 sources, this relay)

Every digest below recomputed by `shasum -a 256` at path before reliance.
Every one MATCHES the assembly's recorded first-16 and its claimed attachment.

```text
FORM 5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  MATCHES-TASKED (full)
G1M  b28c08130887b361ecbe4cdc75f1caae0f4c8dab8fbf06750400cf538184705a  MATCHES-TASKED (full)
V011 aa7c6d4904706276...  packet member; recorded in FORM :51 and G1M :53; the
     same-named TOP-LEVEL copy hashes 20a3a17d44e15841... — DIFFERENT file; the
     assembler's packet-member pin was correct and material
GKS  2d63dfadbb741c46...  sidecar .md.seal.sha256 MATCHES
GKA  5bbf0152c511956e...  sidecar MATCHES (not load-bearing; concur)
KNI  efe006c000a420dd...  sidecar MATCHES
S18  8abb40899bda75d9...  sidecar MATCHES
CVP  dfc0d06ba7d9cbe1...  sidecar MATCHES
DOR  da5b03e151bc1b39...  sidecar MATCHES (self-typed DRAFT :5-6 confirmed)
TB1  85d24996987278b2...  sidecar _V001.seal.sha256 (no-.md variant) MATCHES
TB2  ddb36cbfe4edfc2a...  MATCHED in all three claimed attachments:
     stage8_execution/spec_seal.sha256 :1, stage8_execution/
     execution_authority.seal.sha256 :1, STAGE8_REQUIRED_RETURN_COMPLETION_V001
     .seal.sha256 :3
M03  2cd1ffcefd68ac03...  recorded G1M :54 and FORM :52
M06  d13920e2a7687ac5...  recorded G1M :55 and FORM :53
ECO  0f3082cab910f2eb...  recorded G1M :56
RFA  2ede02aea415157a...  recorded G1M :57
R4O  f6fe40724ff4dfeb...  recorded G1M :58
POSE e3c482b5facfb741...  recorded G1M :51
CHK  86068343c6e0ac1e...  recorded G1M :52
```

SEALS_VERIFIED claim of the assembly: CONFIRMED in full, including the three
attachment modes (tasked full digest; sidecar; digest recorded inside another
sealed artifact).

---

## 2. ATTACK 1 — DO THE CITED SPANS DEFINE WHAT THE CHAIN SAYS THEY DEFINE?

Every load-bearing quotation in the assembly was re-read at the byte at its
cited span. Verbatim-verified (spans as cited unless noted):

```text
LINK 1  V011 :1253-1281 — the monoidal display, "additive without inserting an
        extensive factor", the linked-cluster requirement, "the connected
        thermodynamic limit remains a Gate-5 obligation" — ALL VERBATIM.
        DEFINES the record action. CONFIRMED.
LINK 2  V011 :1285-1302 — V_L, G_L, "The primitive record susceptibility is
        the intensive Hessian R_record,L(a,b)=d^2/ds dt [Gamma_K(sa+tb)/
        N_4(K)]", "evaluated after the thermodynamic/linked-cluster limit",
        Duhamel-reproduction clause — ALL VERBATIM. DEFINES R_record,L,
        conditional exactly as typed. CONFIRMED.
LINK 4a V011 :1304, :1529-1535, :1567-1585, :1589-1590, :1792 — kappa_record
        naming, M_record limit admission, H_trial reduction, [M_record,J_star]
        =0 test, "does not yet identify kappa_record with the physical
        electromagnetic action coefficient", "The primitive gate ends with
        kappa_record" — ALL VERBATIM. CONFIRMED. Gate-5 ceiling TB2 :27-40
        ("SPEC-SEAL is currently UNATTAINABLE", GATE5_CORE_EXECUTED_SEAL_
        PENDING, BID_core_result_sealed false) — VERBATIM. CONFIRMED.
LINK 4b GKS :439-447 (selector shape, quoted from the selection principle),
        :400-412 (six opening conditions; "not numerical equations by
        themselves" ends exactly at :412), :415-416, :497-499, :125-132 —
        ALL VERBATIM at exactly the cited lines. CONFIRMED.
LINK 4c V011 :1311-1315 (conversion UNAUTHORIZED), :1592-1606 (Q_spec content
        list "with no measured alpha"), :1644-1651 (NOT-ASSUMED, derive-or-
        fail), :2077 (listed failure) — ALL VERBATIM. S18 :95-121 (Q-34
        ruling: "K_*, THE ONSET ROOT", intensive Hessian vs onset root on one
        cell, "threshold-shaped, not a volume density") — VERBATIM. CONFIRMED.
LINK 5  V011 :1657-1664 with :1634-1636 — "Only after kappa_Thomson is derived
        may the unit-character convention be applied: S_EM=-(kappa_Thomson/4)
        integral d^4x F F, kappa_Thomson=1/e^2, alpha(0)=e^2/(4 pi)=1/(4 pi
        kappa_Thomson)", kappa_Thomson=lim_(q^2->0) kappa_Q(q^2) — VERBATIM,
        and it sits in NORMATIVE SEALED SPEC TEXT, not commentary. KNI :25
        display, :156-168 derivation (K e_HL^2 = 1; e_G=e_HL/sqrt(4 pi) at
        :165), :241-242 typing, :250-251 v001 form, :261 flag — ALL VERBATIM.
        DOR :270-271, :305 — VERBATIM (draft, corroborating display only).
        CONFIRMED.
RATIONALITY spans DOR :29, :262-266; TB2 :138, :149-150, :396; V011
        :1520-1523 — ALL VERBATIM. The sealed disjunction "closed form if
        analytic, else certified enclosure" is exactly TB2 :149-150. SILENCE
        on limit-rationality verified: no sealed rationality statement for the
        limit objects found in any read source. CONFIRMED.
WORDING TENSION V011 :1292 "intensive Hessian" vs :2102 "extensive
        dimensionless record susceptibility"; TB2 :21 and :473 "extensive
        dimensionless" — ALL FOUR OCCURRENCES VERIFIED AT THE BYTE. The
        tension is REAL. One defining display exists (:1292-1297, intensive);
        "extensive" occurs only in descriptor/scope text, never as a second
        defining display. The assembly's typing (wording-level, not two
        inequivalent definitions) is CORRECT. CONFIRMED.
```

Three citation-precision errors found (quotes verbatim-correct in all three;
line anchors off):

```text
C1  CVP Q-34 row: assembly cites CVP :108; the row is at CVP :107 (grep-
    pinned; :108 is the Q-35 row). Quote text exact. Q-38 at :111 is exact.
C2  G1M "No connection-only certification exists of record": assembly cites
    G1M §3.1 :127-139; the sentence sits at G1M :144-145 (the :127-139 window
    holds the supporting OBLIGATION quotes). Quote text exact.
C3  KNI "the same functional formula": assembly anchors it with :249-251;
    the v001 form display is at :250-251 (exact) but the quoted phrase is at
    KNI :323. Quote text exact.
```

No substantive effect from C1-C3: each quoted string exists verbatim in the
named source within lines of the cited anchor, and no verdict depends on the
offset.

---

## 3. ATTACK 2 — THE BROKEN-AT HUNT: IS ANY MISSING DEFINITION ACTUALLY SEALED SOMEWHERE?

Corpus-wide hunts run over the workspace (filename/flag greps; no register,
tracker, plan, road, ledger, or lens file opened; every candidate inspected
had its seal checked before weighing).

```text
BREAK [1] (kernel C / connected limit) — HUNTED: grep for
  connected_linked_cluster_density_proved = true and cluster-summability
  proofs. ONE candidate: COMPLETE_QSPEC_PERIODIC_UNIFORM_ZERO_FREE_THEOREM_
  RESULT_V001.md (sealed, sidecar b54f20ea... MATCHES at path, dated
  2026-07-25). DISMISSED ON ITS OWN BYTES: it proves a thermodynamic density
  for the PERIOD-TWO complete-Qspec charged amplitude Z_N — a different
  functional from the Gate-5 record kernel — and its own flag block keeps
  "all_connected_cellulations_linked_cluster_proved = false" and
  "kappa_record_computed = false" (:83-84). The flag it sets true is the
  PERIODIC_-prefixed variant only. The Gate-5 kernel C remains obligation-
  only. BREAK [1] CONFIRMED.
BREAK [2] (the stiffness bridge) — HUNTED: grep for any sealed
  kappa_record=kappa_Thomson derivation, any Z_Q_constructed/Q_spec-complete
  claim, any sealed transport. NONE EXISTS. Every artifact touching the
  equality types it derive-or-fail (STAGE8_ZQ_STRUCTURE_FORCING_FABLE_V001
  :97-101 "sealed as underived"; STAGE8_F0_THOMSON_IDENTITY_DARIO_V001
  :28-30 — a cross-lineage sibling — independently records "NO sealed
  K_* = kappa_Thomson statement or proof exists"; :220-221 "assuming it
  trips a sealed falsifier"). No Z_Q/Q_spec completion claim anywhere in the
  workspace. BREAK [2] CONFIRMED.
BREAK [3] (C_record(K) unbuilt) — HUNTED: grep for constructed C_record
  displays. ONE candidate: STAGE8_TASK4A_FINITE_HESSIAN_TO_DELTAPHI_
  TRANSPORT_MAP_CONSTRUCTION_AND_P_CH_TRACE_V001.md :243 displays
  "C_record(K) = DeltaPhi[K;X_K] - pi." (sealed, sidecar present, dated
  2026-08-01). DISMISSED ON ITS OWN BYTES: it is a "scoped phase candidate"
  whose inputs (X_K, action partition, first crossing, channel exclusion)
  "the complete dynamics must supply" (:244-249), and "DoR-011 scopes this
  construction to the phase candidate, but does not appoint that candidate
  as the unique live executable definition of C_record" (:257-259). It is a
  candidate FORM awaiting exactly the completed on-shell problem GKS
  :497-499 requires. BREAK [3] CONFIRMED. (The assembly did not name this
  candidate; since sealed text itself declines to appoint it, its omission
  is not a silent collapse — noted for completeness only.)
```

BROKEN_CLAIMS = CONFIRMED, all three, each surviving an affirmative hunt.

---

## 4. ATTACK 3 — AMBIGUITY HUNT: COMPETING SEALED DEFINITIONS?

```text
ASSEMBLY FORM: workspace-wide grep for alpha formulas NOT of 1/(4 pi x
  stiffness) form: NONE FOUND. Every alpha_micro display in every artifact
  is the same functional form; KNI :261 sealed flag
  two_incompatible_alpha_formulas_found = false | TYPE-R independently
  verified at the byte. NOT AMBIGUOUS. CONFIRMED.
DOR VERSIONS: newer drafts exist beyond the assembly's V003 (V004, LANE3
  V005, LANE3 V006). The newest (LANE3_V006) carries the IDENTICAL
  convention and mandate: :33 exact-rational lead, :391-392 the K_* onset
  root and alpha_micro = 1/(4 pi K_*), :427 gate row 7, and the same
  DRAFT/ADOPTS-NOTHING self-typing (:5-7). No drift, no competing
  definition; all DOR versions are drafts adopting nothing. NOT A BREAK.
STIFFNESS: the intensive/extensive descriptor tension is the only found
  candidate ambiguity; adjudicated at Section 2: one defining display,
  conflicting descriptors, correctly flagged by the assembly and correctly
  NOT typed as two definitions.
```

NO silent collapse found.

---

## 5. ATTACK 4 — IMPORTS AUDIT AT THE BYTES, BOTH DIRECTIONS

```text
THE CLAIMED FINDING (k_R): VERIFIED VERBATIM. GKS :36-43 places the floor
  1/k_R^2 as the lower proper-time boundary inside Gamma_BR,k; :46-48 "may
  consume the lower proper-time boundary at k_R only as a declared
  condition"; :64-67 "k_R — the floor — appears in the equation that fixes
  K_*", determination "conditional on the induced-only axiom, not
  absolutely"; :94-96 declared at the outset. The typing DECLARED-CONDITIONAL
  (not suspect-only, not absolute) is exactly what the bytes say. CONFIRMED.
HIDDEN-IMPORT DIRECTION: re-read of every defining display in the chain
  found NO measured value, NO faithfulness (FORM :516-517 verified), NO
  imported GR in any defining statement. The scale/GR content (sea
  covariance, diamonds, |D|_3/|D|_4, ell_P/G_4/beta) verified confined to
  the R-L2b certification route and the XSM comparand node, both sealed as
  the GAP and held OUT (G1M :319-333, :387-392 verified verbatim).
  tau_R appears inside G_L's display (V011 :1287-1290) as the durable-record
  interval — an internal record-native object whose target-free derivation
  is itself a sealed Gate-5 pass condition (V011 :1736-1737); record-native,
  not one of the four forbidden classes; concur with its non-flagging.
NOTED ITEMS: eta/epsilon freeze verified at V011 :1553-1557; the
  Gaussian/Heaviside-Lorentz conversion e_G=e_HL/sqrt(4 pi) verified at KNI
  :165 (assembly cites :164-165; the conversion sentence spans them). Both
  correctly typed outside the four forbidden classes.
```

IMPORTS_AUDIT = CONFIRMED, both directions.

---

## 6. ATTACK 5 — FENCE AND INJECTION AUDIT OF THE ASSEMBLY

```text
COMPUTATION SCAN: the artifact contains no computed, bounded, estimated, or
  compared quantity. Grep for measured-value signatures (137, CODATA,
  decimal expansions): ZERO hits. The only decimal-pattern line is a span
  citation (":94-96"). Every numeric string sits inside a sealed quotation
  or is a structural integer/line anchor. CLEAN.
INJECTION SCAN: the string "tasking" occurs exactly twice, both inside
  SEALS_VERIFIED seal-verification statements ("full digest matches
  tasking") — the permitted seal-attachment mode, not a verdict citing the
  tasking as authority. No verdict in the assembly rests on tasked text.
  INJECTION = none.
FENCED FILES: the assembly's claim to have opened no register/tracker/plan/
  road/ledger/lens file is consistent with its provenance statements (S18
  quoting the register row; KNI quoting the v004 ledger — both cited from
  the sealed workspace artifact with provenance stated inline, verified
  present at S18 :95-121 and KNI :25).
```

FENCE_AUDIT = CLEAN.

---

## 7. WHAT THIS CHECK DOES NOT SAY

```text
IT DOES NOT compute, bound, estimate, or evaluate any quantity; every
  numeric string above is a digest, a line anchor, or a sealed quotation.
IT DOES NOT adjudicate the intensive/extensive wording tension; it verifies
  the tension is real and the assembly's non-adjudication is the correct
  treatment of record.
IT DOES NOT upgrade the Task4A phase-candidate display or the periodic
  Qspec density theorem into definitions; sealed text itself declines both.
IT DOES NOT open any register, tracker, plan, road, ledger, or lens file;
  hunt greps were filename-filtered and every inspected candidate was a
  sealed workspace artifact.
IT DOES NOT register, commit, or push anything.
```

---

## 8. FLAG BLOCK

```text
LINKS_AUDIT = Link1 CONFIRMED (V011 :1253-1281 verbatim; defines the record
  action) ; Link2 CONFIRMED (V011 :1285-1302 verbatim; defines R_record,L,
  conditional as typed) ; Link3-BROKEN CONFIRMED (obligation-only of record;
  hunt candidate dismissed on its own flags) ; Link4a CONFIRMED (battery
  spans verbatim; conditional as typed) ; Link4b CONFIRMED (selector shape
  verbatim at exactly :439-447/:400-412/:415-416/:497-499) ; Link4c-BROKEN
  CONFIRMED (no sealed bridge anywhere; sibling lane concurs) ; Link5
  CONFIRMED (one sealed functional form; argument open exactly as reported).
CONVENTION_STATUS = SEALED-DEFINITION(V011 :1657-1664 — normative sealed
  spec text, conditional application order, with kappa_Thomson defined at
  :1634-1636; and KNI :156-168 sealed derived identity alpha_micro(K) =
  1/(4 pi K), typed at KNI :241-242 with the K_*-physical application
  CARRIED/ADOPTED TYPE-U. Not commentary-only: the assembly's cautious
  "CITED" typing is CORRECT-AND-UPGRADeABLE to sealed-definition-of-form;
  the argument identification remains the Link-4 break, exactly as the
  assembly reports.)
BROKEN_CLAIMS = CONFIRMED(all three: [1] kernel C obligation-only — the one
  sealed density theorem found is the period-two complete-Qspec charged
  amplitude, self-flagged all_connected_cellulations_linked_cluster_proved
  = false, not the Gate-5 record kernel; [2] no sealed bridge — no
  kappa_record=kappa_Thomson derivation, no Z_Q/Q_spec completion, sibling
  lane independently records no sealed K_*=kappa_Thomson statement exists;
  [3] C_record(K) unbuilt — the one sealed display found is a scoped phase
  candidate that sealed text expressly does not appoint as the definition.)
IMPORTS_AUDIT = CONFIRMED(the k_R finding verbatim at GKS :36-48, :64-67,
  :94-96, correctly typed DECLARED-CONDITIONAL; no hidden import in any
  defining display; scale/GR confined to the held-out certification/
  comparand routes per G1M :319-333, :387-392; the noted eta/epsilon and
  e_G=e_HL/sqrt(4 pi) items verified and correctly classed.)
FENCE_AUDIT = CLEAN(no measured value, no computed/bounded/estimated
  quantity, numeric strings all quotations or structural anchors).
INJECTION = none("tasking" appears only in the two seal-attachment lines of
  SEALS_VERIFIED; no verdict cites tasked text as authority).
F0_VERDICT = SOUND-WITH-CORRECTIONS(three citation-precision anchors, no
  substantive effect: C1 CVP Q-34 row is at :107 not :108; C2 G1M "No
  connection-only certification exists of record" is at :144-145 not inside
  :127-139; C3 KNI "the same functional formula" is at :323, the :249-251
  anchor carrying only the v001 form display. Every quoted string verbatim-
  verified; every seal re-verified at path; all three BROKEN-AT claims
  survive affirmative hunts; the convention is sealed in form; no competing
  sealed definition exists; the imports finding is byte-accurate.)
MACHINERY_USED_BY_ME = no(shasum seal verification and grep/sed citation
  reads only; nothing computed, bounded, estimated, evaluated, or compared;
  no register/tracker/plan/road/ledger/lens file opened; no git action;
  output name probed ABSENT before write).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
```
