# STAGE 8 TASK 5 / EQ6 — PHYSICAL J2 ON THE ACTUAL FINITE SURFACE — LANE 2 V001

Date: 2026-08-04  
Custody: Codex Lane 2, finite constructive audit  
Register head verified before work: Q-462  
Status: physical factorization failed to form on the current actual stock; no registration action taken

## Lead result

```text
J2_EQUALITY = FAILED |
  the left-hand normalized algebraic reader and the first two physical legs
  Q_N and Kernbar_N exist, but the actual finite stock supplies only the
  transported cylindrical/local-symbol SHADOW sigma_N^fin.  It supplies no
  independently normalized Hodge-Maxwell projection pi_Mx,N, and no proved
  Loc_N into a Maxwell/contact direct-sum carrier on which that projection is
  defined.  Hence the physical right-hand composite is not a map on the full
  actual finite profile carrier.  The reciprocal-loop nonzero Q-408 kernel is
  the explicit witness at which the composite stops.

PI_MX_INDEPENDENT = no |
  no reader-independent pi_Mx,N is constructible from the present sealed
  clauses.  The lawful algebraic image reader exists, but using it as pi_Mx,N
  is exactly the F_PLDEC circularity regression.

MAXIMAL_LAWFUL_RESULT = coefficient-matching theorem |
  if a future independent finite Maxwell/Hodge package supplies
  (Loc_N,pi_Mx,N), then J2 holds exactly on the subfamily whose (chi_N,T_N)
  equal the coefficients induced by that composite.  The equations and the
  failure-capable boundary are derived below; no member is selected.
```

This is a failure of the commissioned **physical** construction, not a
contradiction between two already-existing physical readers.  There is only
one total syntactic side at present: the algebraic reader is built, while the
physical Maxwell composite is absent.  The artifact therefore does not manufacture a
mismatch by defining `pi_Mx` from the desired equality.

```text
ALPHA_COMPUTED = false
PROOF_AUTHORIZED = false
KAPPA_RECORD_COMPUTED = false
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_VALUE_EVALUATED = false
```

---

## 0. Preflight, seals, and register sweep

### 0.1 No-clobber and register-head checks

The commissioned output filename did not exist before construction.  The
sealed questions-settled register verified byte-for-byte and ended at Q-462.
The Q-462 assembly artifact is used only for its shared finite tuple and
types; none of its three pending stage-1 faces is presupposed confirmed.

```text
OUTPUT_PREEXISTED = false | PASS
REGISTER_SEAL = PASS
REGISTER_HEAD = Q-462 | PASS
STAGE1_V002_CONFIRMATION_PRESUPPOSED = false
PREFLIGHT = PASS
```

### 0.2 Hash-verified authorities

Every named authority below was hash-verified before its substantive clauses
were consumed.

| Authority | Verified SHA-256 | Content used |
|---|---:|---|
| `LOCKED_PROCESS.md` | `1ee1c4188cf1ac1d37f40537a611407c0fd344387bd39421688f8b18478d8469` | no-clobber, seal, mirror, report, stop |
| sealed register at Q-462 | sidecar **OK** | current frontier and reopen condition |
| p-verdict pushforward sweep | `041498bb5a83d454212482412ab3fe0c609031f48f7adca94e34489f44bf5562` | `p_loc[R_K]`, rank-one local-symbol residue, S8-A reduction |
| local Maxwell / `p_loc[R_K]` derivation | `70dde1c0cb776abec4a61cac118d4edf1f4c75a1185a2e984dc341961d4b643c` | normalization scope; carrier symbol versus physical local symbol |
| consumption path | `cacd317391759c1b1c6925c671be33cf4e91b7cccf4c3a529669dea6c18400fc` | completed physical reception and reader typing |
| Q-407 reader build | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50` | quotient presentation and complete algebraic reader family |
| Q-408 kernel/symbol build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual finite Q-408 kernel, faithfulness, Ward identities; locality stop |
| Maxwell-symbol certificate build | `2aad6d8379777b8651a7e8dda78e9b085a69aa0203f455209eeb5e6e7da3adeb` | C1–C3 distinction; Hodge/contact/normalization premises |
| J1–J15 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | exact J1, J2, and J7 equations |
| F_PLDEC hostile check | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | abstract-kernel and circular-`pi_Mx` kills |
| DoR-020-A1 decision | `c4dc5976d7e65fe8a6597870629e64cabe6a031b95de97f5572bb36379abb588` | adopted path/current law and U(1)-bundle field law |
| adopted where-clauses | `19b2060392b6e04448c1c13416b87b67decf401246e3414a783b288fdb5d80ec` | support-local field transport; explicit no-Hodge-from-metric boundary |
| where-clause final check | `b4c901f512379251b55d31aae6914d1fce9af6280c8f65081afeabfd4e75f219` | bundle, rank, class, and flat-holonomy checks |
| `F_actual` exhibition | `a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b` | exact FC10/J2 absence and full live category `I_F` |
| stage-1 V001 component stock | `e21cee3081da06417109697352abe570c58dc6dda7d46e78f690d666bed128f5` | `C3_N^fin=(Ward_N^fin,sigma_N^fin)` and its explicit no-Hodge scope |
| stage-1 V002 shared tuple | `e5381e6687dbdd5aed792bdddd1050ea7f39d17e748d6c9e374746c4cee37731` | shared finite objects only; review status not consumed |

The DoR-020-A1 decision is especially precise: it adds where-content—a
path/current support correspondence and bundle-typed local field members.
It does not add a Hodge projector or a Maxwell coefficient rule.  The adopted
artifact itself retains “Hodge from isometry” as a killed inference.

### 0.3 Questions-settled and corpus sweep

The sealed register and workspace were searched for `reader`, `pi_Mx`,
`Loc`, `Kernbar`, `local symbol`, `Maxwell symbol`, `Hodge`, `Ward`,
`contact`, `Q_G`, `physical J2`, and `J7`.  Bearing entries checked were:

| Entry | Settled result relevant here |
|---|---|
| Q-255 | `p_loc` is an interface; no executable finite coefficient square was supplied |
| Q-397–Q-400 | the reader coordinate and local Maxwell map are independent interfaces; the latter is unbuilt |
| Q-407 | the normalized algebraic family `(chi_K,T)` is classified; physical membership is not |
| Q-408 | the actual finite path-bilocal kernel and exact Ward identities are built; completed/local Maxwell realization is not |
| Q-409 | C1/C2 are conditional and C3 has only its Ward half; contact/Hodge/local symbol remains open |
| Q-415 | abstract kernel substitution and `pi_Mx:=reader` are killed |
| Q-424 | the reader is downstream of the six physical generators, not a seventh root |
| Q-455–Q-456 | physical J2 is a genuine absence, not a phantom alias and not an existing clash |
| Q-462 | the shared finite assembly returned; physical J2/J7 remain stage 2 |

The alias sweep checked Q-313 maps, Q-315 squares, the Q-347–Q-357
prefix-to-cycle layer, the Q-408 cylindrical pullback, the pass-1
subpackages, and the stage-1 `C3^fin` coordinate.  None has the signature of
an independent map

```text
pi_Mx,N o Loc_N : actual finite Q-408 kernel -> R.
```

In particular,

```text
p_(chi,T)^cyl(sigma_cyl,N(H)) := p_(chi,T)(H)
```

is already settled as an algebraic pullback **defined from the reader**.  It
is not a physical Maxwell coefficient projection under another name.

```text
PHANTOM_PI_MX = false
PRIOR_PHYSICAL_J2 = none
REGISTER_SWEEP = COMPLETE
```

---

## 1. Types and the exact J2 contract

At an actual finite stage `N`, Q-407 supplies

```text
E_N = R L_T,N direct-sum_1 R Rhat_K,N
      direct-sum_1 S_1,sa(K_N),

Q_N:E_N -> O_prof,N:=E_N/Rel_N.                  (1-1)
```

The complete normalized algebraic reader family is

```text
ell_(chi,T),N(Q_N(a,b,C))
 = a + chi_N b + Tr(T_N C),

ell_(chi,T),N(Q_N(L_T,N))=1,                     (1-2)
```

with relation annihilation, covariance, reality, restriction, batching, and
stabilizer equations.  Here `Rhat_K` includes the ratified R4 unit seam; no
bare cross-sector conversion is used.

The physical J2 equation of record is stronger:

```text
ell_N
 = pi_Mx,N compose Loc_N compose Kernbar_N compose Q_N,

pi_Mx,N(Loc_N(Kernbar_N(Q_N(L_T,N))))=1.         (1-3)
```

The four factors in `(1-3)` must be constructed independently of `ell_N`.
Equality is a theorem only after both sides are maps on the same carrier.

For clarity, the required physical codomains are denoted

```text
Ker_N^Q408    actual finite path-bilocal kernel class;
Sym_N^loc     actual finite local symbol/contact quotient;
Mx_N          closed normalized Maxwell summand in Sym_N^loc.
```

The present stock has `Ker_N^Q408` and a transported symbol shadow.  It has
not proved the latter two objects with the direct-sum and normalization data
needed by `(1-3)`.

---

## 2. Z1 — each factor constructed or stopped independently

### 2.1 `Q_N` — built, physical quotient used

`Q_N` is exactly the Q-407 quotient `(1-1)`.  Its relation is not guessed in
this artifact:

```text
Rel_N = the closed presentation relation already fixed by the actual
        profile/current construction.                         (2-1)
```

Every reader must annihilate `Rel_N`; pendant/tree coboundaries are removed
before the profile reaches the actual cycle carrier.  No realization,
orientation, edge, cycle basis, or reader is selected.

```text
Q_N = BUILT / TYPE-P
Q_N_SOURCE = actual finite profile presentation
Q_N_TARGET = O_prof,N
```

### 2.2 `Kernbar_N` — built from the actual Q-408 map

Let `Kern_N^raw` denote the actual finite Q-408 current/kernel realization
on the prequotient.  On the cycle-profile operator component it is the
basis-free map

```text
Kern_N^raw(H)[a,b]
 = H(R_K,N^(-1) A_N a, R_K,N^(-1) A_N b),        (2-2)
```

where `A_N a(c)=<J_c,a>` uses the actual path current `J_c`.  It obeys

```text
Kern_N^raw(H)[d alpha,b]=0,
Kern_N^raw(H)[a,d beta]=0,                        (2-3)

Kern_N^raw(H)=0 iff H=0                          (2-4)
```

on the finite Q-408 operator class.  The already-proved presentation
relation is invisible to the represented kernel, so the descent

```text
Kernbar_N(Q_N z):=Kern_N^raw(z)                  (2-5)
```

is well-defined on `O_prof,N`.  This is the actual path-bilocal distribution
kernel, not `UHU*` in an abstract trace-class summand.

The adopted A1 path/current and bundle laws provide its actual surface
support and connection-test transport.  They do not change `(2-2)`.

```text
Kernbar_N = BUILT / TYPE-P / finite actual scope
KERNEL_ABSTRACT_STAND_IN_USED = false
FINITE_FAITHFULNESS = true
```

### 2.3 `Loc_N` — the maximal existing shadow and the physical stop

The shared finite tuple carries

```text
C3_N^fin=(Ward_N^fin,sigma_N^fin),               (2-6)
```

and the adopted bundle lift gives support-local test/field pullback.  Thus
the existing, reader-independent operation is

```text
Loc_N^shadow(K):=sigma_N^fin(K),                 (2-7)
```

with the sealed Ward equations and restriction pullbacks.  It is legitimate
finite symbol data and is retained.

But its sealing artifact says on the nose:

```text
no closed-range, Hodge, or completed Maxwell conclusion is made. (2-8)
```

The physical `Loc_N` required by J2 is not merely `(2-7)`.  It must land in
a named contact quotient with an order-two local symbol and a proved
Maxwell/remainder direct sum:

```text
Loc_N:Ker_N^Q408 -> Sym_N^loc,

Sym_N^loc = Mx_N direct-sum Sym_N^perp,
Mx_N contains the nonzero normalized symbol of L_T,N.          (2-9)
```

Finite Ward annihilation proves transversality.  It does not prove diagonal
locality, the contact quotient, closed range of `d` and `delta`, or the
direct sum `(2-9)`.  DoR-020-A1 supplies where-content and bundle transport,
not `(2-9)`.  The fixed-stage finite-dimensionality of the cycle carrier
does not repair this: `Kernbar_N(H)` is a distributional path-bilocal kernel,
and no sealed theorem places its image in a finite-dimensional local-symbol
space on which Hodge functional calculus is already defined.

Therefore:

```text
Loc_N^shadow = BUILT / TYPE-P
Loc_N^physical_for_J2 = NOT_BUILT / TYPE-U
```

### 2.4 `pi_Mx,N` — no independent physical map exists

If `(2-9)` existed, the lawful independent map would be the normalized
coordinate projection

```text
pi_Mx,N(m+r)=coefficient of m along L_T,N,
m in Mx_N, r in Sym_N^perp,
pi_Mx,N(L_T,N)=1.                                (2-10)
```

The P8 rule of the continuum package types precisely how to construct it:

```text
delta_N=d_N^*;
Box_N=d_N delta_N+delta_N d_N;
Ward/contact/harmonic projections by functional calculus;
Proj_Mx,N from the closed Maxwell summand;
normalization on the nonzero L_T,N symbol.        (2-11)
```

That rule is partial.  Its premises are an actual local principal-symbol
map, closed ranges, a contact quotient, a Maxwell direct summand, a nonzero
normalization denominator, and refinement intertwiners.  None is supplied
by the current actual finite tuple.  The one-edge control

```text
Delta_2 S(1)=(1,1) != (2,2)=S Delta_1(1)         (2-12)
```

already proves that current isometry or bundle covariance alone does not
give Hodge naturality.

The only total scalar map presently available on the finite Q-408 image is
the reader-defined algebraic pullback.  Defining

```text
pi_Mx,N(Loc_N(Kernbar_N(Q_N z))) := ell_(chi,T),N(Q_N z) (2-13)
```

would make `(1-3)` true by definition.  The F_PLDEC hostile check killed
exactly `(2-13)`: the map to be tested would be constructed from the answer
it must independently induce.

```text
pi_Mx,N^physical = NOT_BUILT / TYPE-U
PI_MX_CONSTRUCTION_REFERENCES_READER = forbidden
PI_MX_INDEPENDENT = no
```

### 2.5 Factor ledger

| Factor | Exact status | Lawful output | Missing physical content |
|---|---|---|---|
| `Q_N` | **BUILT** | `O_prof,N` | none |
| `Kernbar_N` | **BUILT** | actual finite Q-408 path-bilocal kernel | completed extension not needed for this finite item |
| `Loc_N^shadow` | **BUILT** | transported finite Ward/symbol shadow | contact-local quotient and Maxwell direct sum |
| `Loc_N` for J2 | **NOT BUILT** | — | `(2-9)` and its actual-kernel proof |
| `pi_Mx,N` | **NOT BUILT** | — | P8 premises `(2-11)`, normalization, refinement naturality |
| `ell_(chi,T),N` | **BUILT ALGEBRAICALLY** | scalar | physical membership is J2 itself |

The factorization stops before a scalar is assigned.  No output-dependent
choice has been made.

---

## 3. Z2 — equality test, exact boundary theorem, and witness

### 3.1 Why the full equality cannot be stated as a theorem

The left-hand side of `(1-3)` is the total map `(1-2)`.  The right-hand side
currently stops at

```text
O_prof,N --Kernbar_N--> Ker_N^Q408
         --Loc_N^shadow--> transported finite symbol shadow.  (3-1)
```

There is no next physical arrow to `R`.  Therefore the full equality is not
false because two existing numbers differ; it fails because its physical
right-hand side is not a map on the actual stock.

The strongest explicit witness is the reciprocal-loop profile.  Let
`K_N=R e` and `x=r e`, `r!=0`.  Then

```text
P_x=r^2 R_K,N,
H_x=[f(r^2)+2r^2 f_1(r^2)]R_K,N !=0,             (3-2)

Kernbar_N(Q_N H_x)!=0                            (3-3)
```

by finite Q-408 faithfulness.  The kernel in `(3-3)` is the actual
path-bilocal distribution and obeys both Ward identities.  The algebraic
reader has the exact value

```text
ell_(chi,T),N(Q_N H_x)
 =[f(r^2)+2r^2 f_1(r^2)]chi_N.                  (3-4)
```

But neither `Loc_N` into `(2-9)` nor `pi_Mx,N` exists on `(3-3)`.  Thus
`(3-2)`–`(3-4)` is the promised concrete stopping witness.  It does not set
`chi_N` to zero.

### 3.2 The coefficient-matching theorem for a future independent map

The obstruction can be stated without loss of information.  Suppose a
future finite package independently constructs `(Loc_N,pi_Mx,N)` satisfying
the P8, Ward/contact, bundle, units, and restriction certificates.  Define
its pullback

```text
ell_N^Mx:=pi_Mx,N Loc_N Kernbar_N Q_N.            (3-5)
```

Because `(3-5)` is normalized and linear, its coordinates in the already
classified dual are uniquely

```text
chi_N^Mx
 :=ell_N^Mx(Q_N(0,1,0)),                         (3-6)

Tr(T_N^Mx C)
 :=ell_N^Mx(Q_N(0,0,C)) for every represented C. (3-7)
```

The trace-class/bounded-dual pairing makes `T_N^Mx` unique modulo the
annihilator already encoded in `Rel_N`; no basis is selected.  Consequently:

**Finite J2 coefficient theorem.**  For an algebraic member `(chi_N,T_N)`,

```text
ell_(chi,T),N=ell_N^Mx

iff

chi_N=chi_N^Mx
and
Tr((T_N-T_N^Mx)C)=0 for every represented C.     (3-8)
```

On a rank-one profile this is the failure-capable scalar identity

```text
ell_(chi,T),N(H_x)-ell_N^Mx(H_x)
 = f(s)(chi_N-chi_N^Mx)
   +2f_1(s)<x,(T_N-T_N^Mx)x>_K.                 (3-9)
```

Thus a real independent Maxwell map would not certify the whole algebraic
family automatically.  It would cut out the physical subfamily `(3-8)`.
This is the exact boundary J2 is designed to impose.

Current stock does not generate `chi_N^Mx` or `T_N^Mx`, so `(3-8)` is a
future theorem interface, not an installed member or a hidden selection.

### 3.3 Direct-sum attack retained

The F_PLDEC check supplies a useful counterfactual.  If one independently
had the direct sum

```text
Sym_N^loc=R L_T,N direct-sum Sym_N^perp,
pi_Mx,N(a L_T,N+h)=a,                            (3-10)
```

and if every cycle profile localized into `Sym_N^perp`, then `(3-8)` would
reduce to

```text
chi_N=0,
T_N=0 on the represented complement.             (3-11)
```

For `z=(0,b,C)`, a generic S8-A algebraic reader yields

```text
ell_(chi,T),N(Q_N z)=chi_N b+Tr(T_N C),           (3-12)
```

whereas `(3-10)` yields zero.  This proves why one may neither claim the
whole algebraic family physical nor define `pi_Mx` by `(2-13)`.  It does
**not** authorize `(3-11)` for the actual surface, because the physical
direct sum `(3-10)` is the missing object.

### 3.4 No physical subscope is silently promoted

All algebraic readers agree on the formal normalized generator:

```text
ell_(chi,T),N(Q_N(a L_T,N))=a.                   (3-13)
```

But `(3-13)` alone is not a physical J2 subscope: the existing stock has not
proved that `Loc_N Kernbar_N Q_N(L_T,N)` lies in a nonzero normalized local
Maxwell summand.  Promoting `(3-13)` would rename normalization as the
missing surface realization and repeat the rails-level-look-alike error.

Similarly, `p_(chi,T)^cyl` is a lawful algebraic image reader, not a
reader-independent `pi_Mx`.  Therefore this artifact does not report
`SUBSCOPE`; no nonempty actual physical subcarrier with all four independent
legs is proved.

```text
FULL_J2 = NOT_FORMED
PHYSICAL_J2_SUBSCOPE_PROVED_NONEMPTY = false
J2_EQUALITY = FAILED (+right_hand_physical_composite_absent_at_(3-3))
```

---

## 4. Z3 — J7 typed precisely, not built

The comparison face induced by a valid J2 is the exact R1-reader pairing.
For the Q-396 retarded witness coefficient `mu`, with `k=(x,0)` and
`s=||x||_K^2`, it is

```text
H_mix(x)=f(s)R_K+2f_1(s)x^flat tensor x^flat,

ell_(chi,T)(mu H_mix(x))
 =mu[f(s)chi_K+2f_1(s)<x,T x>_K].                (J7-1)
```

Including the sealed base-entry derivative,

```text
ell_(chi,T)(Delta B_i)
 =dot_omega_i mu_i
   [f(s)chi_K+2f_1(s)<x,T x>_K].                 (J7-2)
```

The algebraic equalities `(J7-1)`–`(J7-2)` already follow from linearity.
They become the **physical J7 face** only after J2 independently proves that
the same `ell_(chi,T)` is `(3-5)`.  Stage 2 must still verify:

```text
J7.a  construct the actual Loc_N and pi_Mx,N without ell;
J7.b  derive (chi_N^Mx,T_N^Mx) by (3-6)-(3-7);
J7.c  prove the member lies in the coefficient equalizer (3-8);
J7.d  prove (J7-1)-(J7-2) on every I_F arrow with the R4 unit seam;
J7.e  insert J2/J7 into the simultaneous J1-J15 overlap diamonds.
```

No J7 face is built here, as commissioned.

```text
J7_ALGEBRAIC_FORMULA = BUILT
J7_PHYSICAL_FACE = OPEN_THROUGH_J2
```

---

## 5. Z4 — honesty theorem and exact remaining object

### 5.1 Absence, not clash

No pair of existing physical maps gives unequal scalars.  The precise
finding is:

```text
LHS reader                    = existing algebraic family;
Q_N and Kernbar_N             = existing actual finite maps;
Loc_N^shadow                  = existing finite Ward/symbol shadow;
physical Loc_N and pi_Mx,N    = absent;
therefore physical comparison = unformed.
```

The `FAILED` final line reports failure of this construction commission, not
a theorem that every future Maxwell map must disagree with every reader.

### 5.2 Exact next object

The residue is one finite, failure-capable certificate package:

```text
FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT := (
  Sym_N^loc and its topology on the actual Q-408 image,
  Loc_N with support/contact and Ward proofs,
  d_N, delta_N, Box_N and their actual domains,
  closed Ward/contact/harmonic/Maxwell direct sum,
  nonzero normalized L_T,N symbol,
  pi_Mx,N independent of every reader,
  R4 units and reality,
  covariance and batching,
  restriction/refinement intertwiners on every I_F arrow,
  DoR-008 finite-bottom equality
)_N.                                               (5-1)
```

The stagewise finite-symbol shadow supplies input to `(5-1)`, but does not
itself prove `(5-1)`.  The distinction is failure-capable: the Laplacian
witness `(2-12)`, a vanishing normalization denominator, a nonlocal Q-408
kernel outside the local symbol domain, or a contact quotient deleting a
record-visible cycle each kills the proposed certificate.

```text
NEXT_OBJECT = FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT
NEXT_OBJECT_TYPE = CONSTRUCTIBLE_OPEN / TYPE-U
NEW_READER_OR_MEMBER_SELECTION_REQUIRED = no
```

---

## 6. Z5 — batteries, finite checks, and anti-tuning

### 6.1 F_PLDEC circularity regression

Construction order in Section 2 stopped before a scalar map.  Neither
`Q_N`, `Kernbar_N`, nor `Loc_N^shadow` contains `chi_N`, `T_N`, or
`ell_(chi,T)`.  Equation `(2-13)` is displayed only as a forbidden control
and is not used.  Therefore the artifact does not repeat the circular
pullback.

```text
F_PLDEC_CIRCULARITY = PASS / excluded
PI_MX_REFERENCES_READER = false_for_every_constructed_map
```

The last line is deliberately distinct from `PI_MX_INDEPENDENT = no`: no
constructed map references the reader, but no physical `pi_Mx` was
constructed.

### 6.2 Minimal-stage checks

| Stage/test | Computation | Result |
|---|---|---|
| one edge / connected tree | `K_N=0`; no cycle-profile scalar is introduced | **PASS** |
| reciprocal loop | `(3-2)`–`(3-4)` give a nonzero actual kernel and expose the missing physical localization/projector | **PASS — stopping witness** |
| S8-A exchange | the full `(chi_K,tau_cross)` algebraic family remains covariant; no coordinate is deleted before `(3-8)` | **PASS** |
| Q-243/Q-279/Q-309 | active finite retarded and mixing shadows remain exactly zero and p-free; this artifact changes none | **PASS** |
| actual Q-408 Ward | `(2-3)` holds in both slots; no abstract kernel used | **PASS** |
| finite faithfulness | `(2-4)` retains every nonzero represented finite profile | **PASS** |
| normalized reader | `(1-2)` is exact; normalization does not invent `pi_Mx` | **PASS** |

### 6.3 Nine geometric regressions

| Regression | Execution here | Result |
|---|---|---|
| pure new-cycle profile | no old-stage localization or upward quotient is assigned to it | **PASS** |
| covariance orbit/moduli | the entire `(chi,T)` family and all actual surface orbits are retained | **PASS** |
| all-stage skeleton | no stagewise `pi_Mx` is promoted to an all-arrow family | **PASS** |
| rank-preserving `Ref_path` | actual Q-408 and symbol-shadow pullbacks retained; Hodge intertwining not inferred | **PASS WITH HONEST STOP** |
| cycle-creating upward quotient | none defined | **PASS** |
| arbitrary-profile restriction | only sealed old-image restrictions used; target new-cycle kernel retained | **PASS** |
| old-to-new Riesz mixer | no Riesz map is renamed a local Maxwell projector | **PASS** |
| `P=id` overreach witness | admitted at the carrier level; no global orthogonality demanded | **PASS** |
| bundle/topology attack | A1 lift, full rank, characteristic class, and actual support are required before `Loc` | **PASS** |

### 6.4 Further hostile attacks

1. **Ward-implies-Maxwell attack.**  Every finite Q-408 kernel is
   transverse, including kernels with path-bilocal support.  Therefore Ward
   annihilation cannot select the Maxwell coefficient.  **Rejected.**

2. **Finite-dimensional Hodge attack.**  The cycle operator class is finite
   dimensional, but the realized kernels are distributional and the local
   symbol/contact codomain is not supplied as the same finite Hilbert space.
   Finite-dimensionality of the source does not create the missing local
   operator class or its refinement intertwiners.  **Rejected.**

3. **Normalization-line attack.**  `ell(L_T)=1` and `p_loc(L_T)=1` are
   scalar normalizations on separately typed interfaces; without `Loc` they
   are not a commuting square.  **Rejected.**

4. **Metric-trace shortcut.**  DoR-019 supplies carrier Riesz maps, not an
   operator trace or a Maxwell principal-symbol pairing.  **Rejected.**

5. **Reader family as a physical family.**  Algebraic covariance and
   nonemptiness do not prove `(3-8)` for a physical map.  **Rejected.**

6. **Generic direct-sum promotion.**  The F_PLDEC direct-sum calculation is
   retained as a counterfactual falsifier, not adopted as actual surface
   geometry.  **Rejected.**

### 6.5 Anti-tuning ledger

```text
1  Verify Q-462 and the no-clobber condition.
2  Sweep every prior Q/reader/kernel/local-symbol alias.
3  Freeze the actual Q-408 map before reading any reader coefficient.
4  Construct Q_N and Kernbar_N on the entire actual finite carrier.
5  identify the maximal C3 finite-symbol shadow and its express no-Hodge scope.
6  test the P8 premises before attempting any scalar projection.
7  stop pi_Mx when those premises fail; do not use ell to backfill it.
8  only then compare the explicit reader family and derive (3-8)-(3-9).
9  run the reciprocal and S8-A tests without choosing chi, T, p, nu, rank,
   orientation, frame, filtration, path, bundle gauge, or member.
10 inspect no fixed point, end test, response value, or measured constant.
```

No construction choice was justified by a desired reader coefficient,
response, threshold, fixed point, verdict, or number.

---

## Final board

```text
Q_N = BUILT
Kernbar_N = BUILT / ACTUAL_Q408_FINITE_KERNEL
Loc_N_SHADOW = BUILT / FINITE_WARD_SYMBOL_ONLY
Loc_N_PHYSICAL_J2 = NOT_BUILT
pi_Mx,N = NOT_BUILT
ALGEBRAIC_READER_FAMILY = BUILT
COEFFICIENT_MATCHING_THEOREM = PROVED / (3-8)-(3-9)
J7_ALGEBRAIC = BUILT
J7_PHYSICAL = OPEN_THROUGH_J2
MEMBER_SELECTED = none
CLASH_BETWEEN_EXISTING_PHYSICAL_MAPS = none
NEXT_OBJECT = FINITE_Q408_TO_LOCAL_MAXWELL_HODGE_PROJECTOR_CERT
```

`J2_EQUALITY = FAILED (+reciprocal-loop nonzero actual Q-408 kernel reaches no licensed physical Loc/pi_Mx composite)`  
`PI_MX_INDEPENDENT = no`
