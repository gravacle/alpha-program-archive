# STAGE 8 — THE THREE SEMANTIC PINS: DETERMINATION-FIRST — V001

## BLIND BUILDER — CODENAME PIN-BUILD — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15). Commission: for each of the three
semantic pins named at the OB-2 determination of record (`c8053f6c…` §6.2 B-2,
audit `d9f4aad0…`), decide at bytes whether the sealed text, its clause
context, and its consumers' usage jointly FORCE one reading (D3-freeze
precedent, `9a0c2045…`) or whether both readings are genuinely admissible at
every sealed byte (FREE: stage the decision sheet, PROPOSED-NOT-ADOPTED).
The principal chose this route to retire his own decision where the record
permits. Nothing here adopts, seals, or fixes any pin.

Fences held throughout: `alpha_computed = false` ; `proof_authorized = false`;
`kappa_record_computed = false`. DETERMINATION ONLY: no member bound, no
candidate authored, no route/branch/address/representative selected, no
score/gate/criterion moved. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…" tokens
EXPECTED-UNLOCATABLE (cited only as they appear inside sealed artifacts).
No git; no existing file edited; output path and seal sidecar probed ABSENT
at session start and re-probed ABSENT immediately before this write; ONE
output file plus its seal sidecar. Bytes-level and exact-symbolic only; no
CAS; no float anywhere; no value frozen.

**SWEEP CUTOFF: 2026-08-15T03:10:39Z (primary token sweeps SW-A..SW-E);
supplementary context sweep (encirclement consumers) closed ~03:14Z.**
Exhaustiveness below is sweep-displayed only; nothing absolute is claimed.

---

## 0. VERDICT

```text
PIN-1 (iota_N^Q408 / Sch_N)        = FORCED-READING: the ACTUAL reading, at
       the operative sites (chain §3; residue displayed §3.5).
       Squares-only is INADMISSIBLE at the sealed bytes.
PIN-2 (anti-degeneracy scope)      = SPLIT, per half:
       (HOL half, A9 "encirclement") = FORCED-READING: NOT a membership
       clause; the adopted clause set admits the zero map at its own bytes;
       W-HOL's degenerate completion stands admitted (chain §4.1).
       (H half, (JD-3) words)        = FREE: both readings admissible at
       every sealed byte; decision sheet staged §4.2, PROPOSED-NOT-ADOPTED.
PIN-3 (FullJ7 modality)            = FORCED-READING: MANDATORY per the
       SPEC-2 tuple; §1.5 "carrying" is the stage-pair reference, not an
       optionality marker; NG-2 binds EVERY H member (chain §5).

CONSISTENCY WITH THE RECORD: OB-2 and its audit surfaced each pin as
unresolved, and BOTH artifacts' own ledgers named the exact event realized
here — OB-2 CL-1: "A wider sweep could change this determination only by
exhibiting … (b) a sealed fixing of a B-2 pin"; audit CL-1: same premise.
This artifact is that wider sweep. The sealed fixings exhibited for PIN-1
and PIN-3 live at byte-sites ABSENT from both prior artifacts' displayed
grounds (listed exactly, §3.4 and §5.3). No sealed verdict is contradicted
at its own walked bytes. Nothing fires: OB-2 remains UNDECIDABLE-TODAY
under every combination (payoff display §6).
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
c8053f6c  STAGE8_OB2_EMPTINESS_S9AD_V001.md            MATCH (read in full)
d9f4aad0  STAGE8_OB2_EMPTINESS_S9AD_AUDIT_V001.md      MATCH (pin/witness rows,
          §3.1-3.3, verdict, CL block at bytes)
9a0c2045  STAGE8_T7_D3_QUANTIFIER_READING_FREEZE_AND_LEMMA_CONSUMPTION_V001.md
                                                        MATCH (read in full;
          the FORCED-reading method G1-G4 + anti-convenience at bytes)
fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md   MATCH (read in full)
ba9430c1  STAGE8_TASK5_JII_REVIEW_DARIO_V001.md        MATCH (read in full)
b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md MATCH (§1.1-1.2 at
          bytes; §2.2 (D12); §2.3 (C1); §2.5 void conditions at bytes)
ccb94dfa  STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md
                                                        MATCH ((B2-1)-(B2-8),
          (B2-13),(B2-15) at bytes)
3fd4b924  STAGE8_TASK5_EQ6_PHYSICAL_J2_LANE2_V001.md   MATCH (§ carrier
          naming block at bytes: "Ker_N^Q408 actual finite path-bilocal
          kernel class")
9c74ed3d  STAGE8_TASK5_EQ6_J7_FACE_AND_J2_DISPLAY_LANE3_V001.md  MATCH
          (declaration-site location; consumed via fdf20bd4 (D-J7))
e0b66180  DOR_020_A9_XI_N_ADOPTED_2026-08-05.md        MATCH (read in full)
ac9335dc  STAGE8_TASK5_XI_N_CANDIDATE_LANE3_V004.md    MATCH ((Z1)-(Z4),
          (Z3) status sentence, (Z10), (Z-A7), (D8) display at bytes)
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md      MATCH (seal only;
          consumed via OB-2's displays; not independently re-walked)
```

Zero mismatches. The held construction `506bfe2a…` untouched, unopened,
unpresupposed.

## 2. THE METHOD, CONSUMED FROM THE PRECEDENT (9a0c2045)

The D3 freeze establishes HOW a forced reading is established of record:

```text
G1  the sealed text's own words compel the reading;
G2  no competing definition exists anywhere in the corpus (sweep-displayed);
G3  a sealed clause forbids the alternative reading's EFFECT;
G4  apparent counter-evidence is disposed at its own venue (a check's venue
    is not a theorem's scope; a law-section sentence does not amend a
    definition-site display);
+   ANTI-CONVENIENCE STATEMENT: consequences are displayed as consequences,
    not motives; grounds must predate/stand independent of what they enable;
+   SUPERSESSION: "If the principal holds that the sealed INTENT differed
    from the sealed TEXT, that is his to say and this freeze is superseded
    on his word; no lane may make that call." Carried verbatim here for
    every FORCED finding below.
```

Scope note, displayed: the J-II family is spec-defined ([YOURS]-tagged
authored specification, consolidated at fdf20bd4, review-confirmed at
ba9430c1, consumed as the membership content by OB-2 CL-2). FORCED below
means: forced as a reading OF THAT SEALED SPEC TEXT for its own family —
the same grade at which OB-2's Class-1/Class-2 readings operated. The
corpus's own practice treats sealed sentence-text inside specification
sections as clause content (precedent: the D3 freeze's G1 is sentence text;
OB-2 Class 1 consumes the LOC (S4) anti-formal SENTENCE as a "sealed
anti-degeneracy clause"; audit row grades it VERBATIM). That practice is
consumed here as CL-2 (ledger).

---

## 3. PIN-1 — iota_N^Q408 / Sch_N: FORCED-READING (ACTUAL)

### 3.1 The disputed words at their own bytes, clause context

The commissioned readings: (a) SQUARES-ONLY — the slots are any continuous
injective maps satisfying the displayed squares; (b) ACTUAL — iota_N^Q408
realizes the sealed kernel class as itself (actual inclusion), Sch_N is the
actual Schwartz-kernel map under a fixed density convention.

Defining carrier LOC (b53d9e93), §1.1, verbatim at bytes:

```text
(S2b intro)  "A physical Loc package must first build one named topological
     ambient kernel space D_N^ker CONTAINING THE IMAGE OF (S2a), a
     continuous injective natural embedding iota_N^Q408: Ker_N^Q408 ->
     D_N^ker, and one continuous linear raw realization Krawhat_N …"
(S4 intro)   "The local Maxwell operator must have a separately typed
     SCHWARTZ-KERNEL REALIZATION, WITH THE BUNDLE DENSITY AND DUAL-DENSITY
     CONVENTIONS FIXED: … Sch_N: Op_N^(loc,2) -> D_N^ker, Sch_N is
     continuous and injective, … kappa_T,N := Krawhat_N(L_T,N)
     = Sch_N(M_N^loc)."
(S5 intro)   "and WITH THE OLD Q-408 BRANCH REPRODUCED EXACTLY:
     Krawhat_N(0,b,C) = iota_N^Q408 Kernbar_N^cyc(Q_N(0,b,C))."
(post-S8)    "kappa_T,N must be an ACTUAL surface/bundle kernel or a
     DIAGONAL-SUPPORTED DISTRIBUTION IN THE SAME NAMED TOPOLOGY AS THE
     Q-408 IMAGE. A formal tag adjoined solely to make (S4) parse FAILS
     THE SPECIFICATION."
(§2.3 (C1))  membership display: "… Op_N^(loc,2) IS THE DECLARED PROPERLY
     SUPPORTED CLASSICAL PSEUDODIFFERENTIAL OPERATOR CLASS OF ORDER <= 2 …
     kappa_T,N = Sch_N(M_N^loc) as typed in (S4) …"
(§2.5 void conditions, verbatim)
  1. "Sch_N or kappa_T,N CANNOT BE REALIZED WITH THE FIXED BUNDLE/DENSITY
     CONVENTION in the common kernel topology, … or the embedding (S2b)
     fails continuity, injectivity, or naturality."
  3. "A Q-408 kernel is not properly supported or lies outside the symbol
     domain; EQUIVALENTLY its required diagonal pullback FAILS THE
     WAVEFRONT TRANSVERSALITY TEST."
```

The kernel class's own defining bytes: KER (ccb94dfa) (B2-5)/(B2-6) — each
element is "a unique bilocal distribution on M x M" via the Schwartz-kernel
correspondence; (B2-8) sealed support on path unions; (B2-15) — the
distribution-topology extension is displayed as "a named locally convex
kernel space D'_tau" with seminorm estimates ON THE ACTUAL KERNELS
(`p_Q(Kern_G^fin(H)) <= C_Q ||H||_prof`). PJ2 (3fd4b924) names the carrier:
"`Ker_N^Q408` actual finite path-bilocal kernel class."

### 3.2 The usage sweep (every sealed consumer, how each consumes)

```text
SW-A  content sweep "iota_N^Q408", corpus roots minus fenced classes ->
      11 files; every definitional site is in §3.1's list (LOC, fdf20bd4,
      KER-adjacent, OB-2 + audit); no competing definition anywhere (G2).
SW-B  content sweep "Schwartz" -> 13 files; the only definitional carriers
      are LOC (S4) and KER (B2-5)/(B2-15); the remainder are consumers or
      unrelated (Bohm-window artifacts predating this family).
CONSUMERS AND WHAT THEIR CONSUMPTION TYPE-CHECKS UNDER:
c1  fdf20bd4 Joint B (§3.2): tests (JB-2) D_N^Loc subset Sch_N(Op_N^(loc,2))
    as MICROLOCAL ELIGIBILITY — "A properly supported classical
    pseudodifferential operator of order at most two requires the
    appropriate conormal/wavefront, order, density, and principal-symbol
    structure." Under squares-only, membership in an abstract injection's
    image has no wavefront content and the whole joint is wrong-shaped.
    Type-checks under ACTUAL only.
c2  fdf20bd4 §7.5 alternative-1 falsifier: "one actual reciprocal/
    refinement kernel outside the claimed domain" — the discharge/void of
    (JB-2) is a fact about the ACTUAL kernels. ACTUAL only.
c3  LOC §2.5 void condition 3: seals the EQUIVALENCE "outside the symbol
    domain <=> fails the wavefront transversality test". Under
    squares-only no such equivalence could be stated. ACTUAL only.
c4  LOC §2.5 void condition 1: "Sch_N … cannot be realized with the fixed
    bundle/density convention" is a possible FAILURE — a failure mode that
    exists only if Sch_N means the actual realization (a formal injection
    always exists and can never "fail to be realized"). ACTUAL only.
c5  KER (B2-15): the named kernel space receives J_c and Kern_G^fin(H)
    THEMSELVES (seminorms applied to the actual kernels). ACTUAL shape.
c6  fdf20bd4 (SPEC-6) representation-independence via Sch_N injectivity;
    (S23)-(S25) squares; NG-2 d2 (continuity+injectivity only) — NEUTRAL:
    these type-check under both readings and force nothing (displayed for
    completeness; not counted).
```

### 3.3 The forcing chain (D3-style)

```text
FC1-G1 (words): four sealed sentences jointly compel the actual reading:
   (i)   (S4) intro TYPES Sch_N as "a Schwartz-kernel realization, with
         the bundle density and dual-density conventions fixed" — a density
         convention is a datum OF an actual kernel realization; a formal
         injection has none;
   (ii)  (S2b) intro requires D_N^ker "CONTAINING THE IMAGE OF (S2a)" —
         the image of (S2a) is a set of actual bilocal distributions
         (KER (B2-5)/(B2-6), PJ2 carrier naming), so the ambient space
         CONTAINS the actual kernels;
   (iii) (S5) intro requires "the old Q-408 branch REPRODUCED EXACTLY" —
         the cycle branch of the member's raw realization IS the sealed
         kernel realization; under a value-deforming (scrambled) embedding
         the branch is mirrored, not reproduced;
   (iv)  the post-(S8) sentence requires kappa_T,N to be an ACTUAL kernel/
         diagonal-supported distribution "IN THE SAME NAMED TOPOLOGY AS THE
         Q-408 IMAGE" — well-typed only if the Q-408 image inhabits a named
         distribution topology.
FC1-G2 (no competing definition): SW-A/SW-B — no sealed byte anywhere
   defines either slot as structure-only. The squares-only reading is an
   INFERENCE from display economy at the consolidation site (SPEC-12a
   displays "continuous and injective"), and the consolidation's own
   authority table binds LOC as "complete candidate specification
   (S1)-(S25)": fewer words in a consumer is not a competing definition.
FC1-G3 (a sealed clause forbids the alternative's effect): the squares-only
   reading's entire effect is to admit formally-adjoined realization data;
   the sealed sentence "A formal tag adjoined solely to make (S4) parse
   FAILS THE SPECIFICATION" forbids exactly that effect at the point where
   the two readings first diverge, and void conditions 1 and 3 arm the
   actual reading's failure modes as specification-level voids.
FC1-G4 (counter-evidence disposed):
   d1  "(SPEC-12a) carries only continuous+injective" — display economy at
       a consumer; disposed by G2 (the T13 pattern: a display's venue does
       not narrow the defining text).
   d2  "(S2b)'s 'natural' is cashed only as the transport squares" — TRUE,
       and not load-bearing here: the chain above never routes through the
       word "natural" (it routes through "containing", "reproduced
       exactly", the (S4) typing sentence, and the void displays).
   d3  "fdf20bd4 Joint B held the door open ('The path-bilocal description
       does not BY ITSELF exclude an abstract realization')" — the door
       held open is satisfiability-without-imported-microlocal-theorems
       ("by itself" = at sealed bytes, without the external wavefront
       step), not semantic freedom: the same Joint B consumes (JB-2) as
       microlocal eligibility (c1). Both OB-2's NG-1 display and this
       forcing cohere with that sentence.
```

VERDICT: **FORCED-READING (ACTUAL)**, stated operatively at the sites the
routes consume:

```text
(i)  Sch_N is an actual Schwartz-kernel realization of the declared
     properly supported classical pseudodifferential operator class
     Op_N^(loc,2) (order <= 2), under the member's FIXED bundle/dual
     density conventions, into a named common kernel (distribution)
     topology; membership in Sch_N(Op_N^(loc,2)) is the microlocal
     eligibility of the kernel (proper support; diagonal-pullback
     wavefront transversality) — the sealed equivalence of LOC §2.5.3.
(ii) On the REPRESENTED image, iota_N^Q408 is containment: D_N^ker
     contains im(Kernbar_N^cyc) as the actual path-bilocal kernels, the
     raw realization's cycle branch reproduces the sealed Q-408 branch
     exactly, and kappa_T,N shares their named distribution topology.
```

### 3.4 Relation to the two prior classifications (both pre-licensed this)

OB-2 §4.1/§6.2 B-2(i) classified this pin "not fixed at bytes" on the
displayed grounds: (SPEC-12a)'s economy + (S2b)'s "natural" + the (S4)
actuality "one point pinned" + Joint B's sentence. The audit "verified
unsealed at the (SPEC-12a)/(S2b) bytes" and its KER row reads
(B2-2)-(B2-8),(B2-13). The chain above rests on byte-sites ABSENT from both
displays: the (S2b)/(S4)/(S5) INTRO SENTENCES, LOC §2.3 (C1)'s
psiDO-class clause, LOC §2.5 void conditions 1 and 3, and KER (B2-15).
Both prior ledgers carried the named premise that a wider sweep exhibiting
"a sealed fixing of a B-2 pin" would change the classification (OB-2 CL-1;
audit CL-1). This is that exhibit. No prior verdict is contradicted at its
own walked bytes.

### 3.5 Residue, anti-convenience, reversal, consequence

RESIDUE (displayed, not forced): iota_N^Q408's action on any UNREPRESENTED
complement of Ker_N^Q408 carries only continuity/injectivity/naturality-as-
squares at sealed bytes. Not consequence-bearing: NG-1's refutation display
runs on "a nonzero REPRESENTED Q-408 kernel" (OB-2 §4.1) and NG-2 uses
continuity+injectivity only (OB-2 §4.2 p1/d2).

ANTI-CONVENIENCE: this forcing moves AGAINST inhabitance — it removes
NG-1's reading-stop and (per the audit's own C-1 sentence) supplies NG-3's
F-A premise, arming two kill conditions against the very family whose
member-construction proposal (D-1) is staged. No lane convenience is
served; the grounds are sealed text older than every consequence.

REVERSAL MECHANICS: a later sealed byte that ratifies a formal/abstract
realization design as a lawful member, or a sealed decision-of-record
restating the slots as structure-only, supersedes this forcing on display;
the chain names its byte-base (§3.1), so the reversal test is exact. The
principal's supersession right (§2) stands.

CONSEQUENCE, descriptive, from the record's own route analysis (nothing
new derived): NG-1's stop (i) dissolves; NG-1 stands ARMED at OB-2
B-3(iii) alone (no sealed census displays an admitted-and-carried stage
with dim M_N >= 2 and a nonzero represented kernel); its microlocal step
remains imported external mathematics exactly as OB-2 tagged it. NG-3:
the audit's F-A repair sentence ("PROVABLE-shape UNDER PIN B-2(i) (or
sealed Sch-linearity), antecedent undecided") is now satisfied on its
first disjunct; NG-3 stands PROVABLE-shape, antecedent undecided at
B-3(ii). NG-2's carrier reading is tightened; its evaluation remains
undisplayed at B-3(i). NOTHING FIRES TODAY.

---

## 4. PIN-2 — THE ANTI-DEGENERACY SCOPE: SPLIT

The commissioned pin has two textual halves with different defining
carriers. They determine differently at bytes and are displayed separately.

### 4.1 HOL half — A9's "encirclement" typing: FORCED-READING (NOT a membership clause; zero map admitted)

Disputed words at their own bytes: DoR-020-A9 (e0b66180) — title "THE
WHAT-ENCIRCLES-WHAT CLAUSE"; ruling: "the assignment of which loop a source
encircles becomes lawful structure — a set map into the integral cycle
lattice, positive-scale invariant, with the topological-charge typing AS
LAW (discontinuity at the zero stratum and at typed support-birth
boundaries; constancy only on oriented addressed strata) …"; basis line:
"Basis: STAGE8_TASK5_XI_N_CANDIDATE_LANE3_V004.md (ac9335dc…, 14 clauses)".

The adopted clause bytes (ac9335dc):

```text
(Z1)  "Xi_N : D_N^Loc -> Lambda_N^cyc, a SET MAP. No R-linearity, no
      additivity in general, and NO CONTINUITY is claimed."
(Z2)(i) "Xi_N(t x) = Xi_N(x) for every t > 0, and Xi_N(0) = 0."
(Z3)  "CONTINUITY STATUS: continuity at 0 is not a candidate requirement.
      If Xi_N has a nonzero value, the theorem below proves continuity at
      0 impossible; THE ZERO-MAP CASE DOES NOT SATISFY THAT ANTECEDENT."
(D8 display, [PROVABLE]) "continuity … is not refuted FOR THE ZERO MAP by
      this antecedent."
(the encirclement paragraph, [YOURS], its own words) "The READING, OFFERED
      as the physical content rather than as an excuse. A cycle assignment
      is a topological charge: which loop a source encircles cannot vary
      continuously to 'no loop at all'…"
```

Forcing chain:

```text
FC2H-G1 (words): the adopted law is the CLAUSE SET (the DoR's own basis
   line: "14 clauses"), and the DoR's parenthetical CASHES "the
   topological-charge typing AS LAW" as exactly the (Z3) content —
   discontinuity/constancy — which at its own bytes carves the zero map
   out of the nonzero antecedent, i.e. ADMITS it. Every displayed clause
   is zero-satisfiable ((Z1),(Z2)(i) directly; (Z2)(ii)/(iii),(Z4)
   conditional/gated; (Z13) untriggered; (Z-A7) holds with p_x = 0).
FC2H-G2 (no competing definition): SW-D sweep — "encircl*" occurs, outside
   ac9335dc/DoR-A9, only in review/adjudication prose describing the
   correspondence as NEW PHYSICAL CONTENT a candidate asserts ("which
   source kernel encircles which loop", GAMMA_H_REVIEW; LOC_REVIEW;
   AUDIT_V002_REVIEW; the seed-adjudication word-list; LP_QSPEC quoting
   V004's own reading). NO sealed byte defines an encirclement PREDICATE
   on Xi_N members anywhere.
FC2H-G3 (a sealed display makes the alternative dead text): under the
   membership reading (zero excluded as "not an encirclement datum"),
   (Z3)'s status sentence and the (D8) [PROVABLE] display — both of which
   state the zero-map member's continuity status WITHIN the specification
   — are analyses of a non-member: dead text. Under the clauses-only
   reading both have exact work.
FC2H-G4 (counter-evidence disposed): the encirclement paragraph is
   [YOURS]-tagged and SELF-DESCRIBES as "the reading, offered"; the DoR
   summary sentence is ruling-summary language, and the corpus's own
   discipline registers from clause bytes, not summaries (OB-2 CL-6
   consumed exactly this, audit-confirmed; the DoR itself: "ADOPTION
   LICENSES; IT INHABITS NOTHING"). The Fable review of record calls the
   candidate "the lawful shape of the what-encircles-what clause" —
   shape-typing, no exclusion of zero.
```

VERDICT: **FORCED-READING** — the A9 "encirclement" words are NOT a
membership clause; the membership content is the adopted clause set, which
admits the zero map at its own bytes. W-HOL's degenerate completion stands
admitted exactly as OB-2 displayed it (fiber-local only; the six-fiber bar
untouched; no promotion; the (Z1) specification remains CONDITIONAL on an
alleged ExtSrc member).

This half CONFIRMS the prior record (OB-2 flag "zero map admitted at
(Z3)'s own bytes"; audit row VERBATIM) and converts its surfaced residue to
a determination. ANTI-CONVENIENCE: the finding defeats the one lever that
could have blocked W-HOL; it neither opens nor closes any family — the
HOL frontier stays exactly on Class 1 / the gates. REVERSAL: a later
sealed DoR adopting a DEFINED, checkable encirclement membership predicate
would be new law, not a contradiction of this finding (which is about the
present bytes); a later sealed byte exhibiting a pre-cutoff clause-grade
predicate this sweep missed supersedes it on display.

### 4.2 H half — (JD-3)'s "incidence/integration/evaluation" words: FREE

Disputed words at their own bytes (fdf20bd4 §3.5):

```text
"To define Phi_N, an actual cell rule is needed. … A candidate must supply
 the sealed rule on the prequotient

   Phitilde_N(s)(delta_e)
    := incidence/integration/evaluation of s on the named k-cell e,  (JD-3)
   Phitilde_N(s)(sum_e a_e delta_e) := sum_e a_e Phitilde_N(s)(delta_e),
                                                                    (JD-3a)

 and must prove orientation covariance, presentation independence,
 continuity, and Ctc_N^sym subset ker(Phitilde_N)."
```

The two readings, verbatim:

```text
READING R-A (MEMBERSHIP / semantic): the words "incidence/integration/
  evaluation of s on the named k-cell e" are a semantic membership clause —
  a member's Phitilde_N must BE an actual incidence/integration/evaluation
  rule (its values must be evaluations of s on the cells); a functional
  without that character, the zero functional included, is not a member
  datum. W-0's degenerate H-tail completion is NOT available.
READING R-B (SCHEMA / displayed-obligations): the words describe the ROLE
  of the demanded prequotient rule; the binding membership content is
  (JD-3a) linearity + the four displayed proof obligations + (JD-3c)/
  (SPEC-9) confinement; any functional satisfying the displayed clauses
  completes the tail, the zero functional included. W-0 IS available.
```

Why neither is forced — both admissible at every sealed byte:

```text
FOR R-A, at bytes: the words sit INSIDE the demand display itself ("must
  supply … :="); fdf20bd4's lead calls the missing datum "an ACTUAL
  incidence/integration/evaluation rule (L0)" and §5.3 repeats it; the
  carrier triage of record types an inhabitant as "a package supplying the
  actual incidence/integration/evaluation rule".
FOR R-B, at bytes: NO sealed byte defines the predicate's satisfaction
  conditions — the rule's content is precisely the named missing datum
  (L0), so the phrase has no sealed cash-out to check a member against;
  the record's one anti-degeneracy device (the (S4) anti-formal sentence)
  is sealed for kappa_T,N and sealed for NOTHING in the tails (the
  asymmetry OB-2 displays at §5.1, "decided nowhere"); the record
  EXPLICITLY refuses the adjacent exclusion ("Neither
  P_H,N Loc_N^C(kappa_T,N) != 0 nor its negation is a membership clause",
  fdf20bd4 §1.3, with the (STRUCK) range clause not restored — audit
  grade VERBATIM); and "actual" in this corpus's pervasive usage contrasts
  with declared/rail (an actually-exhibited rule), a reading the zero
  functional satisfies by display.
WHY THE CONSUMERS DECIDE NOTHING: every sealed consumer of (JD-3) consumes
  it at the DERIVABILITY quantifier (does sealed stock produce the rule):
  Joint D's TYPE-U, K1.2's "genuine", K1.3/N1's hunt for "a machine of
  exactly the (JD-3) shape", K2.2's sector analysis, the OBS22 prereg's R1
  (rule bytes + one displayed evaluation — zero-displayable), the TASK6/
  U1/handoff re-displays. Stock determines NO functional (zero included:
  nothing sealed asserts a member's Phitilde is zero), so TYPE-U
  type-checks under BOTH readings, and none of these consumptions
  distinguishes them at the abstract-membership quantifier. OB-2 is the
  only artifact that consumed the pin at that quantifier, and it displayed
  the fork rather than deciding it.
```

VERDICT: **FREE.** Staging a forced reading here would be a false act in
either direction.

#### THE DECISION SHEET — PROPOSED-NOT-ADOPTED (honest two-reading menu)

Consequence rows are DESCRIPTIVE, from the OB-2 artifact's own route
analysis only. No steering. Nothing below adopts, seals, or fixes.

```text
ROW 1  W-0 (OB-2 §5.1):
  R-A: the zero tail fails the semantic quantifier ("the SEMANTIC
       quantifier, if it is ever sealed … would … exclude exactly this
       tag-like functional" — OB-2's own display); W-0 grounds nothing.
  R-B: W-0 completes every displayed Class-2 clause; the H-tail fibers are
       nonempty at their displayed quantifiers; "no emptiness proof can
       come from the tail clauses alone — every honest no-go must route
       through Class 1" (OB-2 §5.1).
ROW 2  the localization (OB-2 §5.3):
  R-A: "under the SEMANTIC reading of the unsealed pins, no such reduction
       holds and everything stays open" — the H side of OB-2's reduction
       fails; H-tail inhabitance rejoins the open surface.
  R-B: OB-2 (H side) localizes to the common ExtSrc fiber (B-1) plus the
       NG-2 inequality, exactly as §5.3 displays.
ROW 3  kill conditions (OB-2 §4, audit §3):
  IDENTICAL under both readings — NG-1, NG-2, NG-3 are Class-1/H-inequality
  objects; no displayed kill condition arises from or depends on R-A/R-B.
ROW 4  HOL side: UNAFFECTED under both (W-HOL stands admitted, §4.1).
ROW 5  the D-1 member tree (OB-2 §6.3): under R-A, any authored (JD-3)
  additionally owes its semantic character as part of its own ground;
  under R-B it owes the displayed obligations only. Either way it owes the
  NG-2 proof and the NG-3 consistency display (OB-2 §6.3, now
  unconditional for every H member per PIN-3, §5 below).
FALSIFIER / REVERSAL MECHANICS:
  - A later sealed byte adopting an anti-degeneracy clause for the H tail
    (the (S4) pattern extended to Phitilde_N), or sealing a definition of
    the incidence predicate, retires this sheet in R-A's direction.
  - A later sealed byte completing a degenerate H member through review
    grade retires it in R-B's direction.
  - If the principal adopts one reading and a later sealed byte shows the
    other was of record, the adoption is superseded on display (the D3
    supersession pattern, §2); the adopted reading must be carried as a
    DECISION-OF-RECORD naming this sheet so the reversal test is exact.
STATUS: PROPOSED-NOT-ADOPTED. This sheet returns to the registrar for the
  principal's decision; no lane may make the call.
```

---

## 5. PIN-3 — FullJ7's MODALITY: FORCED-READING (MANDATORY)

### 5.1 The disputed words at their own bytes, clause context

```text
(SPEC-2, the definition site)  "Rlz_N^H := (Sym_N^loc, Loc_N^phys,
   iotatilde_N^H, iota_N^H, Loc_N^C, j_f^Sym, j_f^C, rho_a^Sym, rho_a^C,
   S25Data_N, FULLJ7_N; S21--S25 laws)" — FullJ7_N is a displayed
   component of the H tuple, no optionality marker.
(SPEC-12d)  "FullJ7_N is REQUIRED solely to recover J7's declared full
   domain. It is a failure-capable H-tail TYPE-U seam, not an inference
   that every Q-408 kernel lies in the source-extension image."
(§1.1 lead + (D-J7))  "The three declaration chains consolidate to ONE
   exact specification" — J7's chain declares Loc_N : Ker_N^Q408 -> C_N^k
   (the FULL domain).
(§1.5, the conditional phrasing)  "An H member CARRYING FullJ7_N AND
   FullJ7_M must additionally prove the full-domain square and its direct
   cochain consequence: (S23-full-J7), (S23-full-J7-C)."
(§8.2, anti-tuning ledger row, verbatim)  "replace J7's full Q-408 domain
   by Ker_N^rep | H-only FullJ7_N/FullKerCert_N REMAINS A SEPARATE
   REQUIRED CLAUSE; Ker_N^rep is used only for named surface-base tests |
   clean"
```

### 5.2 The usage sweep and the forcing chain

```text
SW-E  content sweep "FullJ7|FullKerCert" -> exactly fdf20bd4, OB-2 + its
      audit (+ one supervision continuation note, fenced class untouched):
      the defining carrier is fdf20bd4 alone; no competing tuple exists.
FC3-G1 (words): membership in Rlz_N^H is supplying the displayed SPEC-2
   tuple; FullJ7_N is in it. (SPEC-12d) says "REQUIRED", with "solely"
   modifying its PURPOSE (the next sentence: it is "not an inference…"),
   not its modality. The spec marks genuine conditionality explicitly
   where it intends it (r_N^D "on each admitted sign-odd sector"; "No
   global r_N^D … asserted before G5 is inhabited") — no such marker
   attaches to FullJ7_N.
FC3-G2 (no competing definition): no sealed byte anywhere displays a
   FullJ7-free H membership tuple (SW-E).
FC3-G3 (a sealed row forbids the alternative's effect): the optionality
   reading's operative effect IS an H member whose J7 domain is the
   represented subscope Ker_N^rep; the §8.2 row seals exactly that move
   as a blocked attack — "REMAINS A SEPARATE REQUIRED CLAUSE".
FC3-G4 (the §1.5 participle disposed): "carrying FullJ7_N AND FullJ7_M"
   names BOTH stage-indexed instances — the signature of a STAGE-PAIR
   reference: the square (S23-full-J7) sits over an arrow f: N -> M and
   consumes the two carried instances at its endpoints. Under the
   mandatory reading the sentence is exact (a member carries FullJ7_X at
   every carried stage X; the square needs both endpoints carried). Under
   the optionality reading the double-instance naming is unexplained and
   a law-section participle would be amending a definition-site display
   from a distance — the venue conflation the D3 freeze's G4 rejects.
CONSUMERS: fdf20bd4 §7.1 lists "H-only FullJ7 seam = STOPPED" among the
   Gamma^H stops; §5.2's anchor row demands FullKerCert_N for the J7
   carrier's PASS; the terminal flag (line 1723) counts Joint HJ7 among
   the H-tail obstruction joints; the review of record quotes and
   confirms §7.2's split with Joint HJ7 inside the H tail; OB-2's §4.2
   NECESSARY CONDITION quantifies over every member ("at every stage N
   carried by a member") and its flag block states "any future H member
   owes the NG-2 proof" — a sealed sentence that type-checks only under
   MANDATORY — and the audit re-derived NG-2 "SOUND" as displayed.
```

VERDICT: **FORCED-READING (MANDATORY)** — FullJ7_N is a component of every
H member per the SPEC-2 tuple; NG-2 binds EVERY H member, not only
"full-J7 members". OB-2's own CL-3 read exactly this at the SPEC-2 bytes
and premise-tagged it; the audit verified all three byte-sites VERBATIM
and left the fork surfaced; both ledgers' CL-1(b) pre-licensed this
determination (§0). The grounds here ((α) the both-indices stage-pair
signature; (β) the §8.2 effect-block row; (γ) the consolidation lead +
(D-J7); (δ) the §7.1/§5.2/flag-block consumption) are absent from both
prior displays, which cited only "SPEC-2 tuple vs §1.5 'carrying'".

ANTI-CONVENIENCE: mandatory WIDENS NG-2's kill surface against the H
family and unconditionally burdens the staged D-1 member tree (OB-2 §6.3).
No lane convenience is served. REVERSAL: a later sealed byte displaying a
FullJ7-free H membership tuple, or a decision-of-record striking FullJ7_N
from (SPEC-2), supersedes this on display; the principal's supersession
right stands.

---

## 6. THE PAYOFF DISPLAY (descriptive; from the OB-2 artifact's own route analysis and its audit only; nothing new derived)

Pin outcomes after this artifact: PIN-1 FORCED(actual); PIN-2-HOL
FORCED(zero admitted); PIN-3 FORCED(mandatory); PIN-2-H FREE (R-A / R-B).
The combination space is therefore TWO, not eight.

```text
COMBINATION 1 — PIN-2-H resolved R-B (schema):
  OB-2 decidable today?  NO. The deciders are OB-2's B-1 (common ExtSrc
    fiber, open after two timeboxed rounds, d662889b (B3)) and B-3(i)/
    (ii)/(iii) (three undisplayed sealed facts) — all outside the pins.
  Frontier: exactly OB-2 §5.3's localization, now holding at bytes on
    both routes: the common ExtSrc fiber + the NG-2 inequality.
  Armed kill conditions (all three, each at one named finite display):
    NG-1  armed at B-3(iii) (the admitted-and-carried stage census:
          dim M_N >= 2 + nonzero represented kernel); microlocal step
          remains imported external mathematics, as OB-2 tagged it.
    NG-2  binds every H member (PIN-3); evaluation undisplayed = B-3(i).
    NG-3  PROVABLE-shape per the audit's C-1 under the now-forced B-2(i);
          antecedent undecided = B-3(ii).
  Evidence lean, as OB-2's own NET line displays it: the finite
    bytes-checkable displays that would flip the verdict (kernel-class
    extent; one mixing relation; now also the stage census) all flip
    toward EMPTY; "one abstract ExtSrc tuple would flip it toward the
    proposal's ground." Both directions displayed; neither reached.

COMBINATION 2 — PIN-2-H resolved R-A (membership):
  OB-2 decidable today?  NO — same deciders, PLUS the H-tail semantic
    quantifier reopens: "under the SEMANTIC reading … no such reduction
    holds and everything stays open" (OB-2 §5.3). The open surface is
    strictly WIDER on the H side; no displayed kill condition is added
    or removed (ROW 3 of the sheet).
  HOL side: identical to Combination 1 (W-HOL forced-admitted).

INVARIANT ACROSS BOTH: SK-2 does not fire; ACT-3 is not forced; the D-1
menu stands unchanged; any commissioned H member act owes the NG-2 proof
and the NG-3 consistency display — now unconditionally (PIN-3) — exactly
as OB-2 §6.3 displayed, with either display a self-kill if it comes out
the other way.
```

---

## 7. CONSISTENCY

Nothing here binds a member, adopts a reading, authors physics, or moves
any gate. The three FORCED findings are determinations of what the sealed
text already says, displayed D3-style, returned to the registrar for
consumption; the one FREE finding returns as a staged sheet,
PROPOSED-NOT-ADOPTED, for the principal alone. The zero functional and
zero map appear only as quoted from the sealed displays of OB-2/ac9335dc;
no witness is constructed here. The joint-fiber bar is untouched. R9
remains PENDING; A8 remains UNEXECUTABLE/PENDING.

## 8. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY (the named unforced choice): roots swept =
      alpha-program-archive/{workspace, supervision, cleanroom_output,
      relay_inbox, relay_outbox} minus fenced filename classes
      (register|road_|ledger|lens|plan|tracker|THE_HANDOFF — none opened).
      The historical cleanroom workspace path is ABSENT on disk (probed;
      only the archive tarball exists, not opened). a32_holdout was NOT
      content-swept (holdout discipline). Sweeps SW-A..SW-E + the
      supplementary encirclement-context sweep, cutoffs declared in the
      header. A wider sweep could change a FORCED finding only by
      exhibiting a sealed byte contradicting its named chain, or the FREE
      finding by exhibiting a sealed definition of the (JD-3) predicate.
      PREMISE(named).
CL-2  PROSE-IS-CLAUSE: sealed sentence text inside specification sections
      binds as clause content. Precedent consumed: D3 freeze G1 (sentence
      text); OB-2 Class-1 consumption of the LOC (S4) anti-formal
      sentence; audit VERBATIM grades. Without this premise, PIN-1's FC1
      loses (i)-(iii) but retains c1-c5 and G3; the verdict was NOT rested
      on the premise alone. PREMISE(named).
CL-3  METHOD: the D3-freeze G1-G4 + anti-convenience + supersession
      pattern applied as the standard for FORCED. PREMISE(named).
CL-4  SCOPE of PIN-1's forced reading stated operatively at the sites the
      routes consume, with the unrepresented-complement residue displayed
      as unforced (§3.5). PREMISE(named).
CL-5  RELATION TO PRIOR VERDICTS: this artifact determines two questions
      OB-2/its audit surfaced as open; both prior ledgers' CL-1 clauses
      named "a sealed fixing of a B-2 pin" exhibited by a wider sweep as
      the licensed change event; the fixing bytes displayed here are
      absent from both prior artifacts' walked-byte displays (§3.4,
      §5.2). No sealed verdict is contradicted at its own walked bytes.
      PREMISE(named).
CL-6  SUPERSEDED CARRIERS: Xi_N candidates V001-V003 were not consumed
      (superseded by the V004 adoption); their "encircl*" hits were swept
      for the G2 absence display only. PREMISE(named).
CL-7  DISCLOSURE: directory listings and grep -l output during sweeps
      displayed fenced-class BASENAMES; no fenced file's content was
      opened, read, or used. d662889b was seal-verified but consumed only
      through OB-2's displays. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  No toy object was constructed or used. The zero functional / zero map
  appear only as quoted from the sealed displays of OB-2 and ac9335dc,
  inside reading-determinations about which sealed words do or do not
  exclude them; neither is proposed, preferred, promoted, or bound. All
  chains are byte-displays plus exact reading logic; no model, no stage,
  no number enters any conclusion.
```

## FLAG BLOCK

```text
PIN1 = FORCED-READING(ACTUAL) at the operative sites (chain §3.3; residue
  §3.5); squares-only INADMISSIBLE at sealed bytes; consequence per the
  record's own displays: NG-1 armed at B-3(iii) alone; NG-3's F-A premise
  met per the audit's C-1; nothing fires
PIN2_HOL = FORCED-READING(NOT-A-MEMBERSHIP-CLAUSE); zero map admitted at
  (Z1)-(Z3)/(D8) bytes; W-HOL's degenerate completion stands admitted,
  fiber-local, six-fiber bar untouched
PIN2_H = FREE; decision sheet staged §4.2, PROPOSED-NOT-ADOPTED; no
  steering; the record's one anti-degeneracy device is kappa-scoped and
  the nonvanishing exclusion is refused of record; consumers decide only
  the derivability quantifier
PIN3 = FORCED-READING(MANDATORY per the SPEC-2 tuple); §1.5 "carrying" =
  stage-pair reference; NG-2 binds EVERY H member; the D-1 tree's NG-2/
  NG-3 obligations are unconditional
OB2_STATUS = UNCHANGED: UNDECIDABLE-TODAY under BOTH remaining
  combinations (§6); deciders = B-1 + B-3(i)/(ii)/(iii), all outside the
  pins; SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
PRIOR_VERDICTS = not contradicted at their walked bytes; both prior
  ledgers' CL-1(b) pre-licensed this exhibit (§0, §3.4, §5.2)
SEALS = 12/12 MATCH ; MISMATCHES = 0 ; HELD_CONSTRUCTION untouched
SWEEPS = SW-A..SW-E + supplementary ; CUTOFF 2026-08-15T03:10:39Z
  (supplementary ~03:14Z) ; exhaustiveness sweep-displayed only
DETERMINATION_ONLY = held (no adoption, no member, no candidate, no
  selection, no common cell, nothing numeric, no CAS, no float, no value
  frozen) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
SUPERSESSION = every FORCED finding carries the D3 clause: superseded on
  the principal's word; no lane may make that call
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = two of the three pins were already decided by sealed text the prior
  determinations had not walked, and the third genuinely is the
  principal's: the H-tail's semantic quantifier is the ONE remaining
  reading decision in OB-2's blocker board, and its sheet is staged with
  both readings' consequences displayed from the record's own analysis.
  Every other movement is arming, not firing: three kill conditions now
  stand at three named finite displays, and the family's ground is
  otherwise exactly where the principal left it.
```
