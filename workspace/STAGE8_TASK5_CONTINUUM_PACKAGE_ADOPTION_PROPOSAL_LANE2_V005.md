# STAGE 8 TASK 5 — CONTINUUM PACKAGE ADOPTION PROPOSAL — LANE 2 V005

Date: 2026-08-04  
Task: PASTE 498 / Task 5 / continuum package V005  
Lane: Codex Lane 2  
Custody: builder repair after Q-419  
Standing: **PROPOSED_NOT_ADOPTED**; DoR-020 reserved

## Lead result

```text
RULE_TYPED =
  P4 TYPED_PARTIAL_RULE;
  P5 TYPED_CANONICAL_COMPLETION_RULE;
  P7 TYPED_PARTIAL_RULE;
  P8 TYPED_PARTIAL_FUNCTIONAL_CALCULUS_RULE.

C_CM020 = OBSTRUCTED |
  first exact obstruction:
  NONEMPTY_ACTUAL_Q408_PRIMITIVE_REFINEMENT_GENERATOR_FIBER / TYPE-U.
  Ref_path is inhabited and derived, but no ratified or proposed datum
  exhibits an actual physical path/test-form/coframe/density realization
  for a genuinely new/interior PL generator.  Without that P4 term, J10
  and J11 do not close; P5/P7/P8 and J2/J12--J15 cannot be instantiated.

P_CM020_TERM_DEFINED = false
c_CM020_CONSTRUCTED = false

SQUARE = DERIVED / retained from V004
ROUTE_A_BOUNDARY = EXACT / retained from V004
RIGIDITY = NOT_DERIVED / retained from V004

READY_FOR_FINAL_CHECK = yes
READY_FOR_DOR020_RULING = no
```

V005 succeeds at the typing repair and fails honestly at inhabitance.  The
least-norm phrase is no longer applied to topologies, ideals, or closed
operator packages.  It becomes a well-typed **partial** selection procedure
on explicit premise carriers.  Applying that procedure still requires a
nonempty fiber of actual Q-408 physical refinement generators.  The corpus
contains no such generator beyond the derived `Ref_path` subcategory, and
declaring the fiber nonempty would repeat the killed witness assertion.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight and authorities

### 0.1 Three-line preflight

The locked process was read and hash-verified.  The register head is Q-419.
V004 and the Q-419 check were hash- and sidecar-verified before use.

```text
DOES_THE_OBJECT_EXIST = yes | V004 and its named member-proof problem exist
IS_THE_VERSION_CURRENT = yes | through register head Q-419
ARE_ITS_INPUTS_PRESENT = yes_for_typing_and_maximal_proof_attempt |
  no_physical_new_edge_generator_silently_assumed
PREFLIGHT = PASS_WITH_TYPED_INHABITANCE_OBSTRUCTION
```

| Authority | Verified SHA-256 | Use |
|---|---|---|
| locked process | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` | custody and fences |
| register at Q-419 | `bdad6353b89a36ceb56275ced109d85b5a3ab72d4ae55e0bcbb4f10517186def` | current scope |
| V004 | `fa73114214d7653d9a5d181a684b3bd65f0d6e9f91fd2256fb159ca97e56c4bf` | passed square/boundary and killed member rule |
| V004 final check | `542464ffdcd64e0442c779f078e9f4fade37c746663c8087451a82e09e241cfb` | N2/N4/N5 kill anatomy |
| Q-411 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | exact J1--J15 families |
| Q-408 kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual finite physical current/kernel |
| hostile witness check | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | four permanent regressions |
| metric V005 / DoR-019 candidate | `2a3790989b55ecca3f244155a11486ad0fdcba25603f36e051567ba670cd8961` | C/K metrics, Riesz maps, and R4 units |
| DoR-008 | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | finite falsifier |

### 0.2 Register sweep

```text
Q-408  actual finite path-current/bilocal distribution map exact;
Q-410  full physical refinement, density, estimates, and symbol absent;
Q-411  J1--J15 define an equalizer, not its inhabitance;
Q-415  abstract-kernel/circular-reader witness killed;
Q-417  Ref_path square derives; member-sensitive execution needs bound P;
Q-418  V004 proposes P_CM020;
Q-419  V004's cross-field minimum ill-typed and c_CM020 unproved.
```

### 0.3 Frozen results

V005 does not reopen:

```text
REF_PATH_PHYSICAL_CURRENT_SQUARE = DERIVED;
REF_PATH_Q408_KERNEL_SQUARE = DERIVED_ON_RANK_PRESERVING_IMAGE;
ROUTE_A_BOUNDARY =
  NEW_INTERIOR_PL_EDGE_CELL_PHYSICAL_REALIZATION_AND_GEOMETRY / TYPE-U;
RIGIDITY = NOT_DERIVED;
END_TEST_SCOPE = bound_member_only_if_a_certified_member_exists.
```

---

## 1. O1 — typed carrier-minimal procedure

### 1.1 Repair principle

V004 incorrectly treated every field as a vector in one DoR-019 norm.
V005 separates four kinds of premise data:

```text
linear maps and cocycles      -> Hilbert/operator carriers;
locally convex topologies     -> countable seminorm presentations;
closed contact ideals         -> closed-subspace hyperspace;
closed/Hodge/Maxwell operators-> graph/gap operator spaces.   (O1-1)
```

Only the first kind is minimized by a Hilbert norm.  P5 is generated and
completed; P7's ideal is a closed invariant hull; P8 is produced by typed
functional calculus.  The word “least” below never compares objects of
different types.

### 1.2 No-selection natural-family index

Let `C_ref` be the fixed proposed refinement category from V004, containing
the derived physical subcategory `Ref_path` and the formal PL generators.
Work on isomorphism classes, never chosen representatives.  Define the
combinatorial complexity

```text
kappa(G)=#vertices+#edges+#cells+canonical_unlabeled_incidence_length,
kappa(R)=kappa(source R)+kappa(target R)+generator_length(R). (O1-2)
```

There are finitely many finite signed incidence types at fixed complexity.
For the set `Iso_k` of isomorphism classes of complexity `k`, use the
invariant weights

```text
w([x])=2^(-kappa(x)-1)/max(1,#Iso_(kappa(x))).     (O1-3)
```

The weights sum absolutely, depend only on record combinatorics, and are
unchanged by edge exchange, orientation reversal, or relabeling.  They are
not a filtration member and carry no physical scale.

For any family of pointed complete metrics `(X_x,d_x,0_x)`, replace
`d_x` by the bounded metric `dbar_x=d_x/(1+d_x)` and define

```text
D_X(u,v)^2=sum_[x] w([x]) dbar_x(u_x,v_x)^2.       (O1-4)
```

This is a typed complete product metric.  Naturality equations define a
closed subspace/equalizer only when their component operations are
continuous; that continuity is checked per field below.  Unlike V004's
objectwise minima, `(O1-4)` poses one simultaneous problem, so the Q-419
counterexample cannot recur **if the feasible equalizer is nonempty and the
selected linear constraint set is closed convex**.

### 1.3 P4 premise carrier and rule

For a primitive refinement generator `R:G->G'`, define the actual physical
realization carrier `X4(R)` to consist of tuples

```text
x_R=(Gamma_R,P_R,S_R,Cof_R,Dens_R,eta_conn,R,eta_curv,R), (O1-5)
```

where:

```text
Gamma_R  assigns every refined edge an ACTUAL physical path modulo equality
         of all admitted Q-408 line integrals;
P_R      is the bounded physical test-form transport;
S_R      is the bounded conserved-current transport;
Cof_R    is a positive coframe operator with its declared unit torsor;
Dens_R   is a positive current/volume-density operator dual to Cof_R;
eta_conn,R and eta_curv,R are the typed connection/curvature transports.
```

The hostile-check safeguard is part of the type:

```text
A_G=S_R^* A_(G') P_R,
Kern_G^fin=Res_R Kern_(G')^fin(j_R^H .),            (O1-6)
```

using the actual Q-408 analysis and distribution-kernel maps.  A bare
operator `UHU*` not induced by `Gamma_R` is not in `X4(R)`.

After applying only declared unit isomorphisms, give `X4(R)` the product
metric

```text
d4_R^2=
 ||P-P'||_op^2+||S-S'||_op^2
 +||A-A'||_HS^2
 +||Cof-Cof'||_HS^2+||Dens-Dens'||_HS^2
 +||eta_conn-eta_conn'||_op^2+||eta_curv-eta_curv'||_op^2. (O1-7)
```

Paths are compared through their operational Q-408 analysis maps only
after restricting membership to actual path realizations.  Thus `(O1-7)`
does not enlarge the carrier to abstract analysis operators.

The P4 rule is:

1. take one `X4(R)` coordinate for every primitive-arrow isomorphism class;
2. impose covariance, reality, composition on the free category, the
   derived `Ref_path` boundary, same-region/common-refinement, positivity,
   and `(O1-6)` before selection;
3. if the resulting generator fiber is nonempty and has a unique
   `D_X`-minimum, choose it;
4. extend to composite arrows functorially, never minimizing composites
   independently.

This is a well-typed partial rule.  Functorial extension makes J10
composition exact once the primitive generators exist.  The rule is not
total: actual-path membership and positivity need not define a convex or
nonempty set.

```text
P4_RULE_TYPED = true
P4_RULE_TOTAL = false
P4_FIRST_PREMISE = nonempty_actual_Q408_primitive_generator_fiber
```

### 1.4 P5 premise carrier and rule

P5 is not minimized.  Given a P4 term, let `S_phys` be the actual physical
current/kernel image on its directed core.  Fix the countable covariant
seminorm orbit

```text
P_phys={p_q,j on currents, p_Q,j on bilocal distributions : j in N}, (O1-8)
```

generated by compact-support, distribution-order, and local-symbol tests
already appearing in the physical kernel codomain.  Define

```text
tau_init=initial Hausdorff locally convex topology generated by P_phys;
tau_ker=Hausdorff completion of S_phys in tau_init. (O1-9)
```

The live T-CM rule uses the **Hilbertizable horn** only: the P5 premise must
also exhibit a positive covariant local weight `W_5` for which

```text
||z||_5^2=<z,W_5 z>
```

generates the same completed physical topology as `(O1-9)`.  `W_5` is typed
in the physical distribution/current unit class and may depend on P4's
local geometry, but not on P2, a response, or an end result.  If no such
`W_5` exists, T-CM is undefined; the broader Fréchet/conormal alternative
remains available only under T-EX or T-FAM, where no Hilbert minimum is
claimed.

Bound data live in the positive sequence carrier

```text
X5=ell_infinity_+ x ell_infinity_+,
(C_q,j,C_Q,j),                                      (O1-10)
```

with unit classes fixed by the current/profile norms.  The rule takes the
componentwise infimum of all valid uniform bound sequences and accepts it
only if it is finite, attained, covariant, and remains valid on completion.

Initial topology and Hausdorff completion are functorial for continuous
maps, so covariance/restriction/batching follow **provided** the uniform
estimates hold.  No response graph norm or desired reader is used.

```text
P5_RULE_TYPED = true
P5_RULE_TOTAL = false
P5_PREMISES = P4_term + uniform_actual_kernel_estimates
  + Hilbertizable_weight_W5_for_T-CM
```

### 1.5 P7 premise carrier and rule

On P5's completed Hilbertizable kernel carrier `H_ker`, closed contact
subspaces are represented by orthogonal projections.  Give their hyperspace
the gap metric

```text
d_gap(I,J)=||Proj_I-Proj_J||_op.                   (O1-11)
```

Response transports and boundary cocycles lie in

```text
X7_map=product_R B(H_ker,G,H_ker,G'),
X7_beta=product_R H_ker,G',                        (O1-12)
```

with the invariant product metric `(O1-4)` and R4 unit conversions at every
C/K seam.

The contact ideal is not least norm.  It is the uniquely defined smallest
closed invariant restriction-natural ideal

```text
I_CM=closure span orbit{actual boundary/contact generators}. (O1-13)
```

The cocycle is the actual discrepancy `beta_R`; `(J13-1)` is imposed before
closure.  Response transports are the unique `D_X`-minimum of the closed
affine naturality/restriction equations if that solution fiber is nonempty.
Deletion of a record-visible bulk cycle is a void condition.

```text
P7_RULE_TYPED = true
P7_RULE_TOTAL = false
P7_PREMISES = P4/P5_terms + nonempty_response_solution_fiber
```

### 1.6 P8 premise carrier and rule

For a closed densely defined operator `D`, use its bounded transform

```text
b(D)=D(1+D^*D)^(-1/2)                              (O1-14)
```

and the graph/gap metric `||b(D)-b(D')||_op`.  A P8 premise point is

```text
x_8=(d,delta,Box,Proj_Ward,Proj_contact,Proj_Mx,
     sigma_2,Dom,harmonic_projection),             (O1-15)
```

with closed-domain, reality, support, and R4-unit types.  It is not a vector
to be averaged.

Given P4/P5/P7, the P8 rule uses:

```text
delta=d^* from the typed metric;
Box=d delta+delta d;
Ward/contact/harmonic projections from orthogonal functional calculus;
Proj_Mx from the closed Maxwell summand;
sigma_2 from the local principal-symbol map;
normalization by the unique Riesz rescaling on L_T. (O1-16)
```

The rule is defined only if the relevant ranges are closed, the local
principal-symbol map exists, the normalization denominator is nonzero, and
all operations commute with refinement.  P8 is constructed before P2, so
`Proj_Mx` cannot be defined from a chosen reader.

```text
P8_RULE_TYPED = true
P8_RULE_TOTAL = false
P8_PREMISES = P4/P5/P7_terms + closed_range/local_symbol certificates
```

### 1.7 Covariance and unit certificates

Admitted automorphisms act isometrically on `(O1-7)`, `(O1-10)`,
`(O1-11)`--`(O1-12)`, and the bounded transforms `(O1-14)`.  The weights
`(O1-3)` are isomorphism-invariant.  Therefore:

```text
unique global minimum -> covariant minimum;
initial topology/completion -> covariant completion;
closed invariant hull -> covariant contact ideal;
functional calculus -> covariant P8 tuple.         (O1-17)
```

Orientation reversal carries its declared sign and reality acts
semilinearly; neither is frozen to invariance.  Current/profile norms and
Riesz maps use DoR-019.  Coframe/density terms occur only in dual products,
and every C/K mixing map passes through the R4 conversion.  No physical
scale is fixed by making the metrics dimensionless.

The Q-419 minimum/restriction counterexample is now a permanent regression:
objectwise minima are forbidden.  The rule first restricts to a global
natural-family fiber and minimizes only its linear affine components under
`(O1-4)`.

### 1.8 Rule alternatives

| Alternative | Exact rule | Cost | Void condition | Status |
|---|---|---|---|---|
| T-CM | typed partial rules `(O1-5)`--`(O1-16)` plus global natural-family metric | requires every premise fiber and certificate | empty/nonunique generator or response fiber; failed estimate/closed range | **live repaired rule** |
| T-EX | explicitly declare every P1--P8 map and proof object | full member authorship | any unnamed physical map or circular certificate | alternative |
| T-FAM | retain all certified members with family-valued output | no bound execution | later scalar selection or false uniformity | alternative |
| T-REJ | reject Route B | none | none | alternative |

No alternative is adopted here.

---

## 2. O2 — maximal construction of `P_CM020` and `c_CM020`

### 2.1 Construction order and first obstruction

On `Ref_path`, `(O1-5)` is inhabited by the derived V004 term:

```text
Gamma_(e,j)(t)=gamma_e((j-1+t)/q),
P_nm=id,
A_n=S_nm^* A_m,
Kern_m^fin(j_nm^H H)=Kern_n^fin(H).               (O2-1)
```

Now take the first primitive `Ref_PL` generator containing an edge that is
not a restriction of an existing physical path.  Membership in `X4(R)`
requires an actual `Gamma_R` and the exact Q-408 square `(O1-6)`.  The
ratified stack supplies neither:

```text
an actual physical path for the new edge;
a physical test-form transport P_R;
a positive coframe/density pair;
a proof that their analysis map extends Q-408 and composes naturally.
                                                        (O2-2)
```

The formal PL edge alone is not a member of `X4(R)`.  Replacing `(O2-2)` by
an arbitrary bounded operator repeats the abstract-kernel substitution.
Defining it from a desired reader or response repeats circularity.  Setting
it to zero deletes potentially record-visible new-cycle content.  Importing
a smooth target violates the target-independent refinement clause.

Hence the primitive-generator fiber is not known nonempty.  A minimum over
it cannot be formed, even though its type is now exact.

```text
O_PHYS4 = NONEMPTY_ACTUAL_Q408_PRIMITIVE_REFINEMENT_GENERATOR_FIBER
O_PHYS4_STATUS = TYPE-U
P4_CM_TERM = NOT_CONSTRUCTED
P_CM020_TERM = NOT_CONSTRUCTED
```

This is an absence of physical input, not a fence block and not a theorem
that the fiber is empty.

### 2.2 Downstream premise ledger

| Field | Typing | Construction status | Exact reason |
|---|---|---|---|
| P1 | global natural representative carrier with DoR-019 metric | conditional | its completed natural constraint set is not proved closed/nonempty independently of the continuum package |
| P2 | Riesz dual on the P8 physical quotient | blocked | P8 member absent; Q-407 algebraic dimension is not physical nonemptiness |
| P3 | anchored `h_CM=0` | **built as a rule** | zero map is typed, covariant, real, and finite-flat once P1 is supplied |
| P4 | `(O1-5)`--`(O1-7)` | **obstructed first** | `O_PHYS4` unbuilt beyond `Ref_path` |
| P5 | `(O1-8)`--`(O1-10)` | blocked downstream | no full actual physical image; uniform bound sequence unproved |
| P6 | actual completed-kernel quotient | blocked downstream | P5/Kernbar absent; finite-visible intersection theorem unavailable |
| P7 | `(O1-11)`--`(O1-13)` | blocked downstream | no P4/P5 term or response solution fiber |
| P8 | `(O1-14)`--`(O1-16)` | blocked downstream | no P4/P5/P7 term, closed-range theorem, or completed local symbol |

### 2.3 J1--J15 proof audit on the proposed member

Each row is checked against an actual term, not merely copied into a future
certificate.

| Family | Required proof | Result on `P_CM020` | Proof or exact stop |
|---|---|---|---|
| J1 | normalized relation-annihilating reader | **OBSTRUCTED** | P2 term does not exist because P8 physical quotient is absent; no nonemptiness claim imported from Q-407 |
| J2 | `ell=pi_Mx Loc Kernbar Q`, normalized on `L_T` | **OBSTRUCTED** | P5/P8 maps do not exist; defining `pi_Mx` from `ell` is forbidden |
| J3 | reader covariance, reality, W3 restriction | **CONDITIONAL THEOREM ONLY** | `(O1-17)` proves covariance if the unique physical reader term exists; it does not construct that term |
| J4 | R1 representative admissibility and cubes | **CONDITIONAL** | P1 fiber is typed but its global natural minimum/nonemptiness is not independently proved |
| J5 | R5 anchored to P1 | **PROVED AS RULE** | `h_CM=0`, so `A_(I,0)(K_0)=Gamma_I` and every restriction equality reduces to P1's square |
| J6 | Shape-K prepared-direction scope | **PROVED AS RULE** | zero added action has `D_C=D_C D_K=D_K^2=0`; it introduces no complement block |
| J7 | exact R1-reader pairing | **OBSTRUCTED** | neither bound P1 coefficient nor physical P2 reader exists; the algebraic formula remains valid but uninstantiated |
| J8 | R4-only unit seam | **PROVED AT TYPE LEVEL** | `(O1-7)`--`(O1-16)` use declared unit maps; no bare C/K conversion is present |
| J9 | ordered batching/network law | **PROVED AT RULE LEVEL** | product carriers, invariant weights, completions, hulls, and functional calculus act componentwise; no amplitude product or joint contraction is introduced |
| J10 | geometric refinement/J_ref functoriality | **PARTIAL THEN OBSTRUCTED** | exact on `Ref_path`; functorial extension is proved from primitive generators, but `O_PHYS4` supplies none for a new edge |
| J11 | density/coframe naturality | **OBSTRUCTED** | no actual positive coframe/density generator beyond `Ref_path` |
| J12 | common-refinement response naturality | **OBSTRUCTED** | P4 term and P7 response solution are absent |
| J13 | boundary/contact cocycle and safe ideal | **CONDITIONAL THEOREM ONLY** | closure hull `(O1-13)` preserves a supplied cocycle; no actual P7 discrepancy family exists to close |
| J14 | uniform completion plus faithfulness/annihilator horn | **OBSTRUCTED** | full actual-kernel image and uniform estimates are absent; neither faithfulness horn is asserted |
| J15 | joint DoR-008 square | **PASS ON Ref_path; OBSTRUCTED GLOBALLY** | `(O2-1)` is exact; no completed P4--P8 diagram exists on new generators |

Therefore:

```text
c_CM020 = NOT_CONSTRUCTED;
J1_J15_VERIFIED_ON_P_CM020 = false;
BOUND_MEMBER_INHABITANCE = OPEN_AT_O_PHYS4;
C_CM020 = OBSTRUCTED(NONEMPTY_ACTUAL_Q408_PRIMITIVE_REFINEMENT_GENERATOR_FIBER).
```

### 2.4 Why the member is not silently repaired

Four apparent repairs were executed as attacks and rejected:

1. **Restrict P4 to `Ref_path`.**  This gives a genuine member only of the
   derived subpackage; it does not supply same-region common refinements for
   general carriers and fails full J10/J12.
2. **Use the PL coefficient operator as the physical map.**  This repeats
   the abstract-kernel substitution and fails `(O1-6)`.
3. **Choose zero data on new edges.**  This can delete a new record-visible
   cycle and fails P6/J13/J15.
4. **Use P2 or a desired response to choose the path/local split.**  This is
   the circular-`pi_Mx` regression and target tuning.

An explicit authored tuple under alternative T-EX could discharge
`O_PHYS4`, but no such physical maps are present in this commission.  V005
does not invent them while calling the result a proof.

---

## 3. O3 — delta, regressions, battery, and falsifiers

### 3.1 Delta table versus V004

| V005 location | Change | Status |
|---|---|---|
| lead/preflight | Q-419 custody and obstruction result | required |
| V004 square and Route-A boundary | retained | final-check PASS |
| cross-field minimum | withdrawn | Q-419 kill repaired |
| natural-family carrier | invariant weighted product metric `(O1-2)`--`(O1-4)` | new typing |
| P4 | actual-path generator carrier and partial global selection rule | typed |
| P5 | canonical initial topology/completion plus bound-sequence carrier | typed, no false norm |
| P7 | gap-metric ideal, closed invariant hull, response-map carrier | typed |
| P8 | graph/gap closed-operator carrier and functional-calculus rule | typed |
| covariance/units | proved fieldwise | O1 repair |
| member construction | pushed to first physical generator and stopped | honest O2 result |
| J1--J15 | fresh per-family proof audit | inhabitance not asserted |
| `c_CM020` | retyped from proposed certificate to obstruction | mandatory correction |
| battery/falsifiers | extended through the partial rule and member proof | rerun |
| final board | ready for review, not DoR-020 | corrected |

All V004 clause alternatives, J2/J5/J13 equations, rigidity nonderivation,
member-scoped end-test semantics, symbol distinctions, and consequence
boundaries remain in force.  No executed Route-B result survives without an
actual certified member.

### 3.2 Four hostile regressions

| Regression | Exercise | Verdict |
|---|---|---|
| abstract kernel | `X4(R)` requires actual paths and `(O1-6)` before the operator metric applies | **PASS** |
| circular `pi_Mx` | P8 construction precedes P2 pullback; absent P8 leaves P2 absent | **PASS** |
| false reader nonemptiness | J1/J2 marked obstructed; Q-407 is not promoted | **PASS** |
| Hodge from isometry | P8 requires graph/closed-range/local-symbol certificates; Ref_path isometry proves none | **PASS** |

The Q-419 restriction/minimum counterexample is added permanently:

```text
C_N={1}, C_M={(x,y):x+y=1}, rho(x,y)=x;
objectwise minima 1 and (1/2,1/2) are not natural. (REG-MIN)
```

V005 excludes it by forming the global natural equalizer before any linear
minimum.  This repairs typing; it does not prove the equalizer inhabited.

### 3.3 Hostile battery

| Attack | Result |
|---|---|
| target tuning | **PASS** — all carriers and weights use record complexity and physical-map types, never a consequence |
| hidden stage weighting | **PASS** — `(O1-3)` is isomorphism-invariant and dimensionless; alternative weights remain disclosed choices |
| objectwise-minimum naturality | **PASS** — forbidden by `(REG-MIN)`; global equalizer comes first |
| abstract path/operator substitution | **PASS** — operational metric is applied only after actual-path membership |
| circular reader | **PASS** — missing P8 blocks P2 rather than being backfilled |
| reader nonemptiness | **PASS** — explicitly obstructed |
| topology-as-vector | **PASS** — P5 uses an initial topology and completion, not a norm minimum |
| ideal-as-vector | **PASS** — P7 uses closed invariant hull and gap metric |
| closed-operator averaging | **PASS** — P8 uses bounded transform and functional calculus |
| Hodge from current isometry | **PASS** — no such inference |
| zero new-edge data | **REJECTED** — record-visible deletion risk |
| Ref_path promoted to full P4 | **REJECTED** — fails general common refinement |
| hidden R4 conversion | **PASS** — all mixed maps cite R4; coframe/density appear dually |
| finite restriction | **PASS on Ref_path / OPEN globally** — scope explicit |
| witness by certificate assertion | **PASS** — `c_CM020` is not claimed |

### 3.4 Falsifiers

The typed rule is falsified by an ill-typed carrier, non-invariant weight,
unit mismatch, noncontinuous naturality equation, or recurrence of
objectwise minimization.  The displayed definitions pass those checks.

The bound-member claim would require all of:

```text
one actual Q408 primitive realization for every generator orbit;
nonempty compatible natural-family generator fiber;
uniform actual-kernel estimates;
completed faithfulness or a safe quotient;
nonempty response solution and boundary cocycle;
closed-range/local-symbol/Hodge certificates;
a physical normalized reader;
J1--J15 and all finite regressions on the resulting term. (O3-1)
```

Failure to exhibit the first two items means there is presently no bound
member to test.  It does not prove the physical fiber empty.

### 3.5 Anti-tuning ledger

```text
1  Freeze Q-408, V004's Ref_path theorem, J1--J15, and DoR-008.
2  Separate vector, topology, ideal, and closed-operator premise types.
3  Define invariant index weights and unit conversions.
4  Define the P4 actual-path carrier before any operator distance.
5  Define P5 topology, P7 hull, and P8 functional calculus before P2.
6  Prove covariance and unit transport for the partial rules.
7  Attempt the first new-edge primitive generator.
8  Stop at O_PHYS4; propagate the absence through every J row.
9  Run hostile regressions and rejected-repair attacks.
10 Compute no response, threshold, fixed point, protected value, or number.
```

---

## 4. O4 — neutral disposition and readiness

| Route | Current result | Cost to continue | What is not licensed |
|---|---|---|---|
| A | derived through `Ref_path`; exact boundary at new/interior PL geometry | construct the actual physical generator package and downstream continuum fields | completed continuum result |
| B / T-CM | selection rule now typed as a partial procedure; member obstructed at the same physical generator | supply `O_PHYS4`, then discharge P5/P7/P8 and every J row | bound-member execution or end test |
| B / T-EX | explicit-member authorship remains available through a future gate | enumerate actual physical maps and proofs | unnamed or certificate-by-assertion member |
| B / T-FAM | honest conditional family remains logically available | retain family-valued results and supply inhabitants | member-sensitive scalar selection |
| reject | no continuum package | none | continuum-dependent chain |

No route is recommended or selected.  V005 is ready for final review of the
typing and obstruction; it is not ready for DoR-020 ratification.

---

## 5. Final board

```text
CONTINUUM_PACKAGE_V005 = TYPED_RULE_WITH_INHABITANCE_OBSTRUCTION
STANDING = PROPOSED_NOT_ADOPTED

RULE_TYPED:
  P4 = yes_partial;
  P5 = yes_canonical_completion_partial;
  P7 = yes_partial;
  P8 = yes_functional_calculus_partial.

P4_RULE_TOTAL = false
P5_RULE_TOTAL = false
P7_RULE_TOTAL = false
P8_RULE_TOTAL = false

FIRST_OBSTRUCTION =
  NONEMPTY_ACTUAL_Q408_PRIMITIVE_REFINEMENT_GENERATOR_FIBER / TYPE-U

P_CM020_TERM_DEFINED = false
c_CM020_CONSTRUCTED = false
C_CM020 = OBSTRUCTED(O_PHYS4)
J1_J15_VERIFIED_ON_P_CM020 = false

SQUARE = DERIVED
ROUTE_A_BOUNDARY = EXACT
RIGIDITY = NOT_DERIVED
BOUND_MEMBER_END_TEST_EXECUTABLE = false
FAMILY_LEVEL_REFUTATION = not_licensed

ABSTRACT_KERNEL_REGRESSION = PASS
CIRCULAR_PI_REGRESSION = PASS
READER_NONEMPTY_REGRESSION = PASS
HODGE_REFINEMENT_REGRESSION = PASS
MINIMUM_RESTRICTION_REGRESSION = PASS

ROUTE_SELECTED = none
RECOMMENDATION = none
READY_FOR_FINAL_CHECK = yes
READY_FOR_DOR020_RULING = no

p_symbolic = true
nu_symbolic = true
rank_or_ratio_selected = none
orientation_frame_filtration_selected = none
numeric_evaluation = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```
