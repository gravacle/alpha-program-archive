# STAGE 8 — AXN BUILD — CROSS-CHECK OF THE S4 FIT ATTACK
## DARIO LANE (Builder B, verifier) — RELAY 869 — [PLAN:AXN-BUILD-A36]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

No member bound; no fixed-point execution; no end test; no smooth import; no EM identification; no
common cell formed; no junction map evaluated; no numeric evaluation of any physical quantity; no
comparison to measured constants. PE-1..PE-12 pointer-only, none opened or consulted. Builder-A code
not opened. `~/.codex` untouched; memory-bank never searched. No register, plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.**

SUBJECT: `STAGE8_AXN_BUILD_S4_FIT_ATTACK_CODEX2_V001.md` = `c1562a6ef1d97cd9`, seal verified BEFORE
reading. **This check exists because Codex authored and executed its own contract and flagged the
overlap itself.** I am the required second eye on an opposite-lane product — builder-never-verifies
running as designed, with no self-adjudication on my side.

**The risk in this configuration is not deference — it is the opposite.** Codex has corrected me twice
in five relays. **A cross-check that finds fault to demonstrate independence is worth less than one
that finds none.** Every claimed-satisfied condition below was resolved at its **own source bytes**,
not against Codex's paraphrase.

---

## 0. LEAD

**The attack holds on every axis this relay asked me to test.**

| axis | result |
|---|---|
| the 4/8 | **CONFIRMED — receivers exact, not paraphrase** |
| C4 / C5 failures | **CONFIRMED — real at their named receivers; no missed supplier** |
| firewall | **HELD — and carried by the *source's* own discipline, not only the attack's** |
| scope words | **HELD — conditions 1/5/7/8 carried unpromoted; eight failed carriers named, not collapsed** |
| outcome typing | **CONFIRMED `FREE_TAIL_PARTIAL_ONLY`** |

**Custody arithmetic, verified independently:** the subject's whole-file digest matches its pin; all
**three** cited span hashes recompute exactly; and the scaling bundle's **seven** members all verify
through a law-8 **fourth-mode group sidecar** whose filename drops `RESULT`/`SPEC`
(`R3_4_INCIDENCE_CONTINUUM_SCALING_V001.seal.sha256`) — the sixth fourth-mode instance I have logged.

**AND THE FINDING THAT MATTERS MOST HERE IS AGAINST MY OWN 864.** Codex's attack found and used a
sealed file **my 864 hunt structurally could not reach.**
`R3_4_INCIDENCE_CONTINUUM_SCALING_RESULT_V001.md` contains the token `endpoint` **zero times**, and
my 864 S4 hunt was `unbounded AND endpoint AND operator`. **The conjunction excluded a file that
derives a named unbounded self-adjoint operator with an exhibited maximal domain.** §4 states exactly
what this does and does not do to my 864 classification.

---

## 1. TASK 1 — THE 4/8 AT SOURCE BYTES: **CONFIRMED**

All four claimed-satisfied conditions rest on one object,
`R3_4_INCIDENCE_CONTINUUM_SCALING_RESULT_V001.md`. I recomputed everything:

```text
whole file          computed 3f18b011ef11cdde  =  Codex pin 3f18b011ef11cdde   OK
[48,365)            computed 0152f461bf6a69e7  =  pin 0152f461bf6a69e7          OK  (verdict/scope)
[366,1320)          computed 62fd484b48d24ad0  =  pin 62fd484b48d24ad0          OK  (operator/domain)
[2499,4143)         computed 555385900b3dcb93  =  pin 555385900b3dcb93          OK  (unresolved boundary)
bundle (7 members)  all OK via the law-8 group sidecar
```

**Receiver binding, condition by condition — exact, and in two cases better than flagged:**

| claimed satisfied | receiver in the source | binding |
|---|---|---|
| strong-resolvent convergence | `strong_resolvent_limit_derived_in_flat_cubical_branch = true` | **EXACT, and the argument is displayed, not asserted**: pointwise `d_a(k) -> i k`, plus the uniform self-adjoint bound `‖(h_a − z)^(−1)‖ ≤ 1/|Im z|`, giving strong resolvent convergence **by dominated convergence** |
| free self-adjoint domain | `self_adjoint_free_outgoing_domain_derived = true` | **EXACT, and the domain is exhibited**: `D(H_0) = {psi in L2 : h(k)psi(k) is in L2}`, *"This is its self-adjoint maximal domain."* |
| free projector / root-measure receiver | `positive_energy_projector_derived = true`; `operator_derived_positive_branch_root_measure_computed = true` | **EXACT — two sealed flags** |
| free-operator spectral pairing | `free_flat_continuum_scaling_derived = true` + the root-measure flag | **EXACT** |

**Four claims, five sealed `true` flags, and two of the four carry displayed derivations rather than
bare flags.** `FOUR_EIGHT = CONFIRMED (receivers exact).`

---

## 2. TASK 2 — THE FAILURES: **CONFIRMED, no missed supplier**

### C4 — root provenance / root-in-domain — **real at its receiver**

Sealed: `sharp_root_in_generator_domain = false`, `finite_energy_physical_root_derived = false`. The
source gives the reason explicitly, and it is a **mathematical** reason, not a bookkeeping one:

> *"the sharp root is in the Hilbert space but not in `D(H_0)`. Since `rho_+(E) = O(E^-2)`, its mean
> energy diverges logarithmically: `integral E rho_+(E) dE = infinity`."*

**I checked the divergence myself rather than accepting it:** with `rho_+(E) = O(E^-2)`, the first
moment integrand is `E · E^-2 = E^-1`, whose integral is logarithmic and divergent. **Correct.** The
failure is therefore not a missing certificate — the sharp root is genuinely outside the generator's
domain, and admitting it needs *"a derived boundary profile, a fundamental finite-cell cutoff, or a
boundary-domain action"*, none of which exists.

### C5 — write attachment / competitor-spectrum closure — **real at its receiver**

Sealed: `complete_write_defect_spectrum_closed = false`,
`complete_outgoing_root_spectral_measure_derived = false`. Reason given:

> *"The complete compact write-region defect is also not yet attached to the free tail. Such a defect
> can produce **bound modes or point spectrum even when the free root measure is absolutely
> continuous**. Its spectrum must be computed from the complete write operator before promotion."*

**This is the correct obstruction and it is a substantive one:** absolute continuity of the free
measure does not survive a compact defect, so the free-tail result cannot be extended by continuity.

### No missed supplier — my own independent hunt

**I deliberately did *not* reuse the query shape that cost me at 864.** Running the families without
the `endpoint` conjunction:

```text
C4 family: "finite-energy" 41 | "root-in-domain" 2 | "boundary profile" 2 | "finite-cell cutoff" 2
C5 family: "write defect" 7 | "write-defect" 6 | "bound mode" 9 | "point spectrum" 21 | "defect spectrum" 4
```

and then the decisive direct test — does **any** sealed file assert the closures?

```text
complete_write_defect_spectrum_closed = true   ->  NO FILE
finite_energy_physical_root_derived   = true   ->  NO FILE
sharp_root_in_generator_domain        = true   ->  NO FILE
```

**No sealed source asserts either condition as closed or derived.** `FAILURES = CONFIRMED (no missed
supplier).`

### Law 9b — and Codex's implementation is stronger than mine has been

Codex's §1.1 declares a **closed enumeration** `L9-A … L9-F` (six sets: the P3 four-field stop's eight
authorities; the P3 second attack's nine; the spectral-pullback bundle's seven members plus six-author
provenance; the continuum-scaling bundle's seven members; the transported-write/tail bundle's six plus
seven pinned inputs; the stationarity audit's seventeen hash-pinned authorities) and then **binds the
word to it**:

> *"The word 'missing' below always means missing from this frozen, enumerated closure at the stated
> receiver. It never means a prose-token search over an unbounded corpus."*

**That is 9b done better than I have done it.** At 864, 866 and 867 I enumerated tables and stated
their strength — but *after* the fact. **Codex declares the closure first and binds "missing" to it,
so the word cannot quietly widen.** I record this as a method I should adopt, not merely as an
observation about someone else's artifact.

`LAW9B tables enumerated by me for this check: 3` — the continuum-scaling bundle (7 members, all
hash-pinned, **strong**); the subject's own L9-A–L9-F closure table (6 sets, hash-pinned, **strong**);
the P3 four-field stop's §1.3 (**weak — prose-named, 0 hashes**, as I disclosed at 864).

---

## 3. TASKS 3 AND 4 — FIREWALL **HELD**, SCOPE **HELD**

### Firewall — held, and by a stronger mechanism than the attack claims for itself

Nothing is selected, authored, or promoted. What I want on the record is that **the fail-closed
discipline is stated in the *source*, not only in the attack**:

> *"**Choosing a smoother profile after seeing the spectrum is forbidden.**"*

with `hypothesis_promoted_to_principle = false` and
`physical_positive_energy_record_branch_selected = false` sealed alongside. **The source anticipated
exactly the move a fit attack would be tempted into and barred it in advance.** The attack's C6
`PASS-FIREWALL / FAIL-REALIZATION` typing is therefore correct and conservative: it passes the
discipline and expressly fails the realization rather than trading one for the other.

`FIREWALL = HELD.`

### Scope — held, in three independent ways

1. **The flat qualifier travels.** The source's verdict token is
   `FREE_FLAT_TAIL_OPERATOR_AND_ROOT_MEASURE_DERIVED_WRITE_DEFECT_OPEN` and its flag reads
   `strong_resolvent_limit_derived_in_flat_cubical_branch`. Codex carries *"flat branch"*, *"flat
   outgoing"*, and *"at that declared scope"* — **the qualifier is never dropped.**
2. **The failures are named, not collapsed.** Eight failed carriers `F1–F8` are listed individually
   with frozen evidence each. Collapsing them into "S4 remains open" would have been easier and less
   informative.
3. **§4 separates realized from unmoved explicitly**, and closes with *"No field, board, seal, or
   status flag is changed by this claimed report."*

**Conditions 1/5/7/8 are carried unpromoted:** condition 5 (finite-BID-root/quasi-local-state
provenance map) sits at `F3`, condition 8 (durability-carrier identification) at `F8` with *"no
receiving sealed result in the freeze"*. Neither is credited.

`SCOPE = HELD.`

---

## 4. AGAINST MY OWN 864 — WHAT THIS CROSS-CHECK COST ME

**Codex's attack reached a file my 864 hunt could not.** At 864 I classified S4's operator conjunct
`MATERIAL-ABSENT` on a hunt of `unbounded AND endpoint AND operator` — 39 files, strongest read. The
scaling result contains `endpoint` **zero times**. **My conjunction excluded it by construction.**

**Does that refute the 864 classification? At its stated scope, no.** My claim was that no named
unbounded **physical endpoint** operator descends to `GNS_out`. The scaling operator is **free, flat,
and not physical-branch-selected** (`physical_positive_energy_record_branch_selected = false`), and it
does not descend to `GNS_out`. **The classification survives.**

**But two things about my 864 were weaker than I presented them, and I state both:**

1. **I should have surfaced this as adjacent material.** The honest 864 class was
   `ABSENT-AT-SCOPE-WITH-ADJACENT-FREE-MATERIAL`, not a bare `ABSENT`. A reader of my 864 would not
   have known the corpus contains a named unbounded self-adjoint operator with an exhibited maximal
   domain — which is exactly what a future S4 attack would want to know first, and exactly what Codex
   did with it.
2. **My corroboration was bad reasoning.** I wrote that *"`essentially self-adjoint` occurs in zero
   workspace files — what one expects when the operator whose self-adjointness would be at issue does
   not exist."* **This file derives a self-adjoint maximal domain and never uses that phrase** — it
   does not need to, because it exhibits the maximal domain directly. **Absence of a phrase does not
   imply absence of the object.** I offered a vocabulary absence as evidence about an object. That is
   the same error family as 860, in a milder dress, and no enumeration law would have caught it —
   only not treating a phrase count as an existence claim.

**This is the cross-check working in the intended direction**, and it is worth saying plainly: the
opposite lane's independent search reached material mine did not, and my report of the same field was
correspondingly poorer.

---

## 5. TASK 5 — OUTCOME TYPING: **CONFIRMED `FREE_TAIL_PARTIAL_ONLY`**

Against the contract's four available outcomes:

| outcome | fires? | why |
|---|---|---|
| `INPUT_GAP` | **NO** | the material exists, is exact, and replays at its pins — four conditions genuinely satisfied |
| `TYPE_MISMATCH` | **NO** | the receiver is right; as the attack says, *"correctly typed material for a strict sub-leg. Its failure is lack of complete-carrier coverage, not use of a wholly wrong receiver"* — I confirm that reading: `H_0` is an unbounded self-adjoint operator, which is the type S4 asks for |
| `FIT` | **NO** | C4 and C5 fail at their receivers, both confirmed above with no supplier |
| **`FREE_TAIL_PARTIAL_ONLY`** | **YES** | four of eight Layer-P conditions satisfied at exact free-tail receivers; the remaining four fail at named receivers |

**`VERDICT = CONFIRMED FREE_TAIL_PARTIAL_ONLY`**, and `S4 = NARROWED` is the correct consequence: the
free-tail leg is realized and the physical integration is open. **S4 does not close, and the attack
does not claim it does.**

---

## 6. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the scaling result's verdict token and all fourteen status flags at their sealed
  values; the FLAT CUBICAL scope qualifier carried on every use; the eight failed carriers F1-F8 as
  named; the attack's own C1-C8 typings; Codex's L9-A-L9-F closure as ITS enumeration, used as the
  claim under test and not adopted as mine; my 864 S4 classification carried and then CORRECTED IN
  STRENGTH rather than silently kept.

DERIVED HERE: (a) independent recomputation of the whole-file digest, three span hashes, and the
  seven-member bundle; (b) confirmation that two of the four satisfied conditions carry DISPLAYED
  derivations rather than bare flags; (c) independent check of the logarithmic first-moment divergence
  behind C4; (d) the direct three-flag test showing NO sealed file asserts C4's or C5's closure;
  (e) the finding that my own 864 query conjunction structurally excluded the scaling result, and the
  two consequent weaknesses in my 864.

SELECTED HERE: NOTHING. No operator, domain, profile, cutoff, boundary action, branch, preparation, or
  spectrum is constructed, selected, preferred, or promoted. I DID NOT RE-RUN THE ATTACK — the relay
  asked for verification of receivers, failures, firewall, scope and typing, not a second attempt.
  NO FLAG MOVES. S4 remains NARROWED, not closed.

NOT DONE AND DISCLOSED: I verified the scaling result's DISPLAYED derivations by reading them and
  checking the one arithmetic claim (the log divergence); I did NOT re-derive the strong-resolvent
  limit or the maximal-domain characterisation from scratch, so those two rest on displayed argument
  plus sealed flag, not on my own replay — weaker than the dim-108 replay I ran at 860/862, and I say
  so. I did not audit L9-C or L9-E's bundles member-by-member; I verified L9-D (the scaling bundle) in
  full and took the others' member counts as stated.

SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at §5.** The tempting move on a cross-check of a partial result is
to find that the four satisfied conditions "essentially" reach a `FIT`, or that C4's divergence is a
technicality admitting an obvious smoother profile. **The source itself forbids exactly that** —
*"choosing a smoother profile after seeing the spectrum is forbidden"* — and nothing here narrows C4
or C5.
**S12** — every flag carried as the status it is; `false` flags carried as sealed negatives, never as
gaps awaiting work.
**S26 / S08 / S19 / S24** untouched — the continuum limit is carried at its sealed **flat cubical**
scope and is **not** treated as a smooth import or promoted past it.
**T1 / T5** untouched.
**BR-1 HELD.** The attack is a producer-declared object: it may accuse and never exculpate. **Its own
claims of satisfaction carried no weight toward themselves** — every one was resolved at the scaling
result's bytes, and the failures were tested against my own independent hunt rather than against
Codex's enumeration.

---

## 7. FINAL LINES

```text
FOUR_EIGHT = CONFIRMED (receivers exact).  Whole-file digest computed = pin 3f18b011ef11cdde; ALL
  THREE cited span hashes recompute exactly (0152f461bf6a69e7 verdict/scope; 62fd484b48d24ad0
  operator/domain; 555385900b3dcb93 unresolved boundary); the scaling bundle's SEVEN members all
  verify via a LAW-8 FOURTH-MODE GROUP SIDECAR dropping RESULT/SPEC — the sixth fourth-mode instance
  I have logged.  The four claims are backed by FIVE sealed true flags, and TWO OF THE FOUR CARRY
  DISPLAYED DERIVATIONS RATHER THAN BARE FLAGS: strong-resolvent convergence is argued (pointwise
  d_a(k)->ik plus the uniform bound ||(h_a-z)^-1|| <= 1/|Im z|, by dominated convergence), and the
  domain is EXHIBITED (D(H_0) = {psi in L2 : h(k)psi(k) in L2}, "its self-adjoint maximal domain").
  NOT PARAPHRASE.
FAILURES = CONFIRMED (no missed supplier; tables enumerated).  C4 is real at its receiver
  (sharp_root_in_generator_domain = false; finite_energy_physical_root_derived = false) and the reason
  is MATHEMATICAL, not bookkeeping: rho_+(E) = O(E^-2) makes the first moment logarithmically
  divergent — I CHECKED THAT ARITHMETIC MYSELF (E * E^-2 = E^-1, integral logarithmic) rather than
  accepting it.  C5 is real (complete_write_defect_spectrum_closed = false;
  complete_outgoing_root_spectral_measure_derived = false) and correctly obstructed: a compact
  write-region defect can produce bound modes or point spectrum EVEN WHEN the free root measure is
  absolutely continuous, so the free-tail result cannot extend by continuity.  MY OWN INDEPENDENT
  HUNT, RUN WITHOUT THE QUERY CONJUNCTION THAT COST ME AT 864, found NO sealed file asserting
  complete_write_defect_spectrum_closed = true, finite_energy_physical_root_derived = true, or
  sharp_root_in_generator_domain = true.  TABLES: 3 — the continuum-scaling bundle (7 members,
  hash-pinned, STRONG); the subject's L9-A-L9-F closure (6 sets, hash-pinned, STRONG); the P3
  four-field stop §1.3 (WEAK, prose-named, 0 hashes, as I disclosed at 864).  SEPARATELY, CODEX'S 9b
  IMPLEMENTATION IS STRONGER THAN MINE HAS BEEN: it DECLARES the closure first and BINDS the word
  "missing" to it — "it never means a prose-token search over an unbounded corpus" — where at 864/866/
  867 I enumerated tables and stated strength AFTER the fact.  I record that as a method to adopt.
FIREWALL = HELD.  Nothing selected, authored or promoted — and the fail-closed discipline is stated in
  THE SOURCE, not only in the attack: "CHOOSING A SMOOTHER PROFILE AFTER SEEING THE SPECTRUM IS
  FORBIDDEN", with hypothesis_promoted_to_principle = false and
  physical_positive_energy_record_branch_selected = false sealed alongside.  The source anticipated
  the exact move a fit attack would be tempted into and barred it in advance; C6's
  PASS-FIREWALL / FAIL-REALIZATION typing is correct and conservative.
SCOPE = HELD, in three independent ways.  (i) THE FLAT QUALIFIER TRAVELS — the source's token is
  FREE_FLAT_TAIL_OPERATOR_AND_ROOT_MEASURE_DERIVED_WRITE_DEFECT_OPEN and its flag reads
  strong_resolvent_limit_derived_in_flat_cubical_branch; Codex carries "flat branch", "flat outgoing",
  "at that declared scope" and never drops it.  (ii) THE FAILURES ARE NAMED, NOT COLLAPSED — eight
  carriers F1-F8 with frozen evidence each, where "S4 remains open" would have been easier and less
  informative.  (iii) §4 separates REALIZED from UNMOVED AT COMPLETE S4 SCOPE and closes "No field,
  board, seal, or status flag is changed."  Conditions 1/5/7/8 carried UNPROMOTED — condition 5 sits
  at F3, condition 8 at F8 with "no receiving sealed result in the freeze".
VERDICT = CONFIRMED FREE_TAIL_PARTIAL_ONLY.  INPUT_GAP does not fire (the material exists, is exact,
  and replays at its pins).  TYPE_MISMATCH does not fire — the receiver is RIGHT: H_0 is an unbounded
  self-adjoint operator, which is the type S4 asks for, so the failure is COVERAGE, not receiver.  FIT
  does not fire (C4 and C5 fail, confirmed, no supplier).  S4 = NARROWED is the correct consequence:
  free-tail leg realized, physical integration open.  S4 DOES NOT CLOSE AND THE ATTACK DOES NOT CLAIM
  IT DOES.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) THIS CROSS-CHECK COST ME MORE THAN IT COST THE SUBJECT.  Codex's attack reached
      R3_4_INCIDENCE_CONTINUUM_SCALING_RESULT_V001.md, which contains the token "endpoint" ZERO TIMES
      — and my 864 S4 hunt was "unbounded AND endpoint AND operator".  MY CONJUNCTION EXCLUDED IT BY
      CONSTRUCTION.  My 864 ABSENT classification survives AT ITS STATED SCOPE (the operator is free,
      flat, and not physical-branch-selected, and does not descend to GNS_out), but the honest class
      was ABSENT-AT-SCOPE-WITH-ADJACENT-FREE-MATERIAL, and a reader of my 864 alone would not have
      known the corpus holds a named unbounded self-adjoint operator with an exhibited maximal domain
      — the first thing a future S4 attack would want, and exactly what Codex did with it.
  (2) MY 864 CORROBORATION WAS BAD REASONING AND I RETRACT IT.  I wrote that "essentially
      self-adjoint" occurring in ZERO files is "what one expects when the operator does not exist".
      This file DERIVES a self-adjoint maximal domain and never uses that phrase — it exhibits the
      domain directly instead.  ABSENCE OF A PHRASE DOES NOT IMPLY ABSENCE OF THE OBJECT.  Same error
      family as 860, milder dress, and NO ENUMERATION LAW WOULD HAVE CAUGHT IT — only not treating a
      phrase count as an existence claim.
  (3) I CONFIRMED ON EVERY AXIS, WHICH IS THE OUTCOME A CROSS-CHECKER SHOULD BE MOST SUSPICIOUS OF —
      especially after being corrected twice by this lane in five relays, where evening the score is
      the live temptation.  The guard is that I resolved every satisfied condition at the SCALING
      RESULT'S bytes rather than against Codex's paraphrase, tested the failures against MY OWN hunt
      rather than Codex's enumeration, and checked the one arithmetic claim by hand.  Where I did NOT
      replay — the strong-resolvent limit and the maximal-domain characterisation rest on displayed
      argument plus sealed flag, not on my own computation, unlike the dim-108 replays at 860/862 — I
      say so rather than letting "CONFIRMED" imply uniform depth.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** Charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-12
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
