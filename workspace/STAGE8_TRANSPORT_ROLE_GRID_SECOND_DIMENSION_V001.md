# The Transport Role Grid — Second Dimension Completed V001

Date: 2026-07-27

## Status — the answer

```text
**THE GRID DOES NOT FORCE A CHANGE. IT FORCES A FOOTNOTE.**
R-10 STANDS. Option 4 stands. weight(C) = sum_i |D_i|_4 = |C|_4 stands. NO RULING IS UNSEATED.

*** AND THE REASON IS SHARPER THAN "THE ADOPTED CELL WINS": THE SECOND DIMENSION IS DEGENERATE.
THE CORPUS NAMES EXACTLY ONE ROLE-3 ASSIGNMENT. There was no second named point for the memo to
have missed, so its incompleteness in that dimension was HARMLESS. ***

BUT THE COMPLETION EXPOSES A REAL OVER-CLAIM INSIDE THE SEALED CHARTER: it states that the five
pins are ALL FIXED. FOUR ARE. P3 IS NOT. Disclosure-grade, not decision-grade.
AND ON THE HONEST QUESTION: THIS MOVES R-L2b NOT ONE STEP. Four independent grounds.
NOTHING WAS CONSTRUCTED. No role assignment invented, no transport rule proposed.
PRODUCTION PROHIBITED. alpha_computed = false. proof_authorized = false.
```

## 1. THE GRID'S STRUCTURE — the 2-D space collapses to 1-D under D5

```text
G := T^2 V / (Lambda^2 |C|_4) DEPENDS ON ROLES 2 AND 3 ONLY THROUGH THE RATIO V / Lambda^2.
So D5-uniformity is the SINGLE condition
        V / Lambda^2   <~   |C|_4 / T^2
and THE PASS-SET IS A CURVE — a one-parameter family — NOT A SET OF ISOLATED WINNERS.
"Four combinations pass" is therefore four named points lying on one locus, and G == 1 is the
EXACT locus  V/Lambda^2 = |C|_4/T^2  rather than a separate kind of success.
ANCHOR VERIFIED: on a cube (T = V^{1/4} = Lambda = L) the generalized chain reproduces A-L4 term
for term — shell count R^3 dR/V = R^3 dR/L^4, pair activity s^2 V^2/R^6 = s^2 L^8/R^6, total
eps^2 L^4/2. The arithmetic closes.
```

**This lane's own tabulation, on the assignments it could name** (parametrising a cell by temporal
extent `T` and spatial extents `S_1 S_2 S_3`, so `|C|_4 = T S_1 S_2 S_3`):

| V (role 2) | Λ (role 3) | G | bounded? |
|---|---|---|---|
| `\|C\|_4` | `T` | **1** | ✓ identically |
| `\|C\|_4` | `S_min` | `T^2/S_min^2` | ✗ needle |
| `\|C\|_4` | `\|C\|_4^{1/4}` | `T^2/\|C\|_4^{1/2}` | ✗ sliver |
| `T^4` | `T` | `T^4/\|C\|_4` | ✗ sliver |
| `Λ^4` | `S_min` | `T·S_min^2/(S_1S_2S_3)` | ✗ pancake |

## 2. THE DECISIVE FINDING — the corpus names ONE role-3 assignment

```text
ROLE-2 ASSIGNMENTS NAMED: MANY. (cell time extent)^4; the literal |C|_4; a reading set by the R = L
spatial cutoff; tau_R x |C|_3; A-L1's |D_1|,|D_2| typing (the SEALED site, witness
A_L1_VOLUME_TYPING_UNPINNED); and the memo's four diamond constructions (inscribed ~ (min extent)^4,
circumscribed ~ (max extent)^4, atom indicator = |C|_4 exactly, decomposition = |C|_4 if it tiles).

*** ROLE-3 ASSIGNMENTS NAMED: EXACTLY ONE — A-L1's R = dist(D_1, D_2). ***
Pattern census, corpus-wide, all filetypes, vendored tree excluded: "minimum separation" 2,
"minimum spatial separation" 2, "lower cutoff" 2, "spatial cutoff" 1, "separation cutoff" 1,
"smallest R" 3 (1 relevant) — AND ZERO for each of: "minimal separation", "min separation",
"smallest separation", "separation scale", "minimum distance", "minimal distance", "min distance",
"distance cutoff", "closest approach", "short-distance cutoff", "UV cutoff", "inter-diamond",
"between diamonds", "diamond separation", "shortest distance", "separation between cells",
"shell index", "temporal cutoff", "cutoff set by", *** "role 3 follow" 0 ***, "role three" 0.

=> THE MEMO HELD ROLE 3 FIXED BECAUSE THERE WAS NOTHING ELSE TO VARY IT TO. Its incompleteness in
the second dimension is REAL AS A METHOD CRITICISM AND HARMLESS AS A RESULT.
AND "ROLE 3 FOLLOWING ROLE 1" APPEARS NOWHERE IN THE CORPUS. The Lambda = T cell is a hypothesis
introduced BY THE CROSS-CHECK, not a corpus-named assignment. Its absence is reported, not supplied.
```

## 3. THE FOOTNOTE THE GRID FORCES — the charter over-claims by exactly one pin

```text
THE CHARTER DOES NOT OCCUPY A SINGLE CELL. It assigns ROLE 2 (item 3, the weight, plus the
decomposition) and LEAVES ROLE 3 UNASSIGNED.
THE DISTINCTION THAT DOES THE WORK: item 5's "R = dist(D_1, D_2)" fixes THE DEFINITION OF THE
VARIABLE R. Role 3 is THE LOWER CUTOFF of int_L^inf R^-3 dR — the smallest value R takes. FIXING
WHAT R MEANS DOES NOT FIX WHERE THE INTEGRAL STARTS.
AND THE MEMO'S OWN P3 ENTRY CONCEDES IT IN THE NEXT BREATH: "on non-diamond atoms 'dist' between
what and what is itself unpinned."

*** SO CHARTER §2's "the five pins ... are ALL FIXED by items 1-5" AND ITS STATUS FLAG
five_pins_fixed_by_charter = true ARE OVER-STATED BY EXACTLY ONE PIN. FOUR ARE FIXED. ***
This is disclosure-grade and is the same class as the charter's OWN self-reported miss
(option_analysis_was_incomplete = true) — a class the charter has already shown it can carry
without the decision moving. NOT ACTED ON; the charter is not edited.

THE OPEN RESIDUE HAS A NAME AND A SHAPE, and the corpus had already localised it:
    SUP OVER EXHAUSTION UNITS OF (TEMPORAL EXTENT / MINIMUM SEPARATION), BOUNDED.
It belongs with O-D1 (the "diamonds don't tile" obligation), which is where the aspect-ratio residue
was already parked. P3 is FIXED CONDITIONALLY on the O-D1 decomposition scheme and UNDETERMINED
ABSOLUTELY.
```

## 4. WHAT G == 1 BUYS — real, and worth exactly one constant

```text
MY OWN F'-5 CONDITIONAL WAS SELF-DEFEATING AND IS CORRECTED. I proposed that a bounded-but-non-unit
G's bound might depend on aspect ratio and so be forbidden. But D5-uniformity IS "G bounded over all
aspect ratios", so K = sup G is BY CONSTRUCTION aspect-ratio-INDEPENDENT. The prohibition cannot
come from dependence.

IT COMES FROM SOMEWHERE ELSE, AND THAT IS GENUINE: K is a constant manufactured by a SUPREMUM OVER
THE ADMITTED CELL CLASS of a ratio containing |C|_4. F'-5's own precedent classifies exactly that
species as forbidden — "A-L3/A-L4's cubic shell counts ARE cellulation geometric data" — and R-L0
adds a named block: "FORBIDDEN: substituting any coarsest-cell 4-volume, any V_max, any skeleton
datum, or any cellulation-family quantity for X_*. BLOCKS with witness
SCOPING_CLAUSE_1_VIOLATION_CELLULATION_DATUM." And §N requires a certified outward enclosure for
every constant. So a non-unit K IS A REAL NEW OBLIGATION: exhibit it, enclose it, defend it as
tuple-expressible. G == 1 manufactures no constant at all — eta^2 = eps^2/2, the identical number
A-L4 already derives on a cube.

*** BUT IT IS NOT DECISIVE, AND THIS DEFEATS THE ATTRACTIVE VERSION OF MY ARGUMENT: THE FENCE FIRES
ON EVERY CELL, INCLUDING G == 1. *** A-L4's cubic shell count is ALREADY a cellulation geometric
datum inside the chain — R^3 dR/L^4 and s^2 L^8/R^6 ARE the derivation — blocked by witness
E1_SHELL_COUNT_NOT_UNIFORM_OVER_D3 in any D3-uniform claim. YOU CANNOT SELECT A CELL WITH A FENCE
THAT FIRES ON ALL OF THEM. G == 1 removes the ADDITIONAL constant; it does not remove the datum
already there. The statement stays family-A-scoped either way.
AND E-Q1's GRANT DOES NOT COVER A G-BOUND: "NOT ADMITTED: |C|_4 in any statement quantified over D3,
over the unrestricted cellulation class, or over common refinements." K is exactly such a constant.
HONEST CAVEAT, FLAGGED NOT BURIED: G == 1 escapes that only if |C|_4-as-D5-WEIGHT differs from
|C|_4-as-admitted-CONSTANT. NO SEALED TEXT ADJUDICATES THAT DISTINCTION (searched "admissible
constant" — 7 hits, all restatements of E-Q1 — plus "not a constant", "as a weight", "weight rather
than a constant"). It is a reading. If it fails, G == 1's residual purchase drops to zero — which
STRENGTHENS the do-not-unseat conclusion.

M-4: G == 1 BUYS NO EXACTNESS. R1 (4-volume re-partition) and R2 are ALREADY exact and contain no G;
R3 is the only inequality and it bounds a MODULUS OF A CLUSTER ACTIVITY, not a volume — G governs
the constant in it and cannot flip it to an identity. Note the direction: R3's hypothesis is that
the D5 bound holds on the refined cellulation WITH THE SAME eta, which G == 1 SUPPORTS without
upgrading. And O6' forecloses the over-read outright: R1-R4 "DO NOT FORCE" the intensive limit's
existence or its equality across the quantifier, witness O6_DENSITY_EQUALITY_NOT_FORCED_BY_R1_R4.
```

## 5. THE HONEST QUESTION — THIS MOVES R-L2b NOT ONE STEP

```text
THE CHARTER SAYS IT, VERBATIM (§7): "IT DOES NOT UNBLOCK R-L2b. The exponent is still underived.
The charter fixes WHAT R-L2b MAY BE STATED OVER ... and supplies NO ESTIMATE. Under this charter
the estimate's difficulty is roughly unchanged from its present state."
INDEPENDENTLY CONFIRMED ON FOUR GROUNDS, EACH SUFFICIENT:
 B1  *** ROLE 3 CANNOT TOUCH alpha EVEN IN PRINCIPLE — IT IS NOT IN THE OBJECT. *** G_hs(C,eps) :=
     |C|_4^{-alpha} ||C(V(a)-V(0))C||_2 is a SINGLE-CELL quantity in §R.3. Role 3 is the lower
     cutoff of a PAIR-SEPARATION integral at n >= 2. THERE IS NO R-INTEGRAL IN G_hs. The very
     dimension the memo held fixed is not a variable of R-L2b's object.
 B2  Assigning role 2 fixes what alpha is an exponent OF. Making a question well-posed is not
     answering it — the corpus says so in its own words: "The charter is what makes R-L2b a question
     about a number."
 B3  "D5 requires alpha >= 1" is a TARGET, derived from D5 plus the aggregation count over the
     already-chartered decomposition, and its own artifact closes "They do not say what alpha is",
     alpha_derived = false. Nothing in the grid moves it.
 B4  DECISIVE — R-L2b's BLOCKER HAS MOVED OFF THE CELLULATION-GEOMETRY AXIS ENTIRELY. Its named
     minimal missing ingredient is U3, the projection-tail / Galerkin-limit obligation: a CARRIER
     typing decision. Roles 1/2/3 are cellulation-geometry data. THE GRID AND THE BLOCKER ARE
     ORTHOGONAL — and the F'-5 provenance erratum independently records the carrier-index and
     cellulation-datum prohibitions as DIFFERENT.
AND THE DEPENDENCE RUNS THE WRONG WAY: the D5 constant G governs is DOWNSTREAM of alpha
(eta_1 contains g, which contains G_hs, which contains |C|_4^{-alpha}). The grid is not a route
into R-L2b; it is a question that becomes numerically answerable only after R-L2b closes.
*** THIS IS THE THIRD TIME AN ANALYSIS OF THE TRANSPORT/CHARTER AXIS HAS PRODUCED A RESULT THAT
READS LIKE PROGRESS TOWARD ALPHA. IT IS NOT. ***
```

## 6. TWO CORRECTIONS TO THE FRAMING I WAS GIVEN

```text
(1) "ROLE 1 *** DETERMINED ***" IS THE DETERMINATION'S WORD, AND THE MEMO THE PRINCIPAL ACTUALLY
    RULED FROM DOWNGRADED IT. P-T3 scored PARTIAL: the DIMENSION is forced by the Duhamel bound,
    the NORMALIZATION is not. So role 1 is not a fixed point of the grid either.
(2) "CELL TIME EXTENT" IS NEVER DEFINED IN THE CORPUS — 13 occurrences, all filetypes, every one a
    re-quotation of A-L4's inequality, commentary naming it as a pin, or the single line tying it to
    L on cubes. CONSEQUENCE: T, the role-1 symbol in G, IS AN UNDEFINED SYMBOL OF THIS CORPUS. Every
    cell of the grid is expressed in a quantity the corpus has not defined. That does not change the
    verdict — the ratio structure and the degeneracy hold regardless — but it is the deeper reason
    the second dimension could not be varied.
```

## Protected status

```text
grid_status = ADOPTED_CELL_ADEQUATE_NOT_UNIQUE
ruling_unseated = FALSE. R-10 stands; Option 4 stands; weight(C) = sum |D_i|_4 = |C|_4 stands.
second_dimension_named_points = 1   (A-L1's R = dist(D_1,D_2)); "role 3 follow" = 0 hits corpus-wide
memo_incompleteness = REAL AS METHOD, HARMLESS AS RESULT (nothing to vary to)
pass_set_structure = A CURVE: V/Lambda^2 <~ |C|_4/T^2. G == 1 is the exact locus, not a separate kind
charter_occupies_a_single_cell = FALSE — assigns role 2, leaves role 3 unassigned
  variable_R_fixed = true ; lower_cutoff_fixed = FALSE
*** charter_five_pins_all_fixed = OVER-STATED BY ONE. FOUR ARE FIXED. P3 IS NOT. ***
  P3_status = FIXED CONDITIONALLY on the O-D1 scheme; UNDETERMINED ABSOLUTELY
  residue_name = sup over exhaustion units of (temporal extent / minimum separation), bounded
  disposition = DISCLOSURE-GRADE; charter NOT edited; belongs with O-D1
G_equals_1_buys = ONE FEWER CONSTANT to exhibit, enclose and scope. NOT decisive.
  reason_not_decisive = the shell-count fence fires on EVERY cell incl. G == 1
  my_F5_conditional = SELF-DEFEATING AS POSED, corrected in §4
  weight_vs_constant_distinction = A READING; NO SEALED ADJUDICATION FOUND
G_equals_1_buys_exactness_in_M4 = FALSE (R1/R2 already exact and G-free; R3 bounds an activity)
moves_R_L2b = NOT ONE STEP (four independent grounds; B1 decisive: role 3 is not in G_hs)
framing_corrections = role 1 downgraded to PARTIAL by the memo; "cell time extent" NEVER DEFINED
constructed_anything = NONE
production_authorized = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
