# STAGE 8 — CARRIER-UNIFORM HS CONTROL OF THE DEGREE <= -1 REMAINDER (THE RELOCATED r2, POSITIVE DIRECTION): B-L2* DECIDED AT THE SUMMED LEVEL

## BLIND ANALYST — CODENAME REMAINDER — [SEALED]

Date: 2026-08-13
Role: BLIND analyst (REMAINDER). The FIXED path's next estimate attempted
honestly: carrier-uniform Hilbert-Schmidt control of the degree <= -1
remainder of the assembled response (the relocated r2 joint multi-scale
object, now needed in the POSITIVE direction), plus the reversal question
inside the coincidence strata. All four tasked outcomes equal citizens.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — operator/Clifford/symbol algebra,
exact degree/order/parity bookkeeping, exact closed forms; the one script is
exact symbolic CAS identity verification (sympy 1.14.0, fresh venv `remvenv`),
reproduced in full in §6 with verbatim output; NOTHING numeric evaluated,
every constant symbolic or an exact rational/surd. No physical quantity
computed, bounded numerically, or evaluated. No scale, imported GR, or
faithfulness authority. M(t) stays SHARP (every integration by parts below is
in a TIME variable against BV amplitudes — the jumps at t = |x|, 1-|x| and
the v-corner at t = 1/2 are retained exactly as boundary terms; nothing is
mollified). Equal-time stays FALSE by C6 (every HS object below is the
TWO-TIME assembled response; the eq-object appears nowhere). No
register/tracker/plan/road/ledger/lens file read. Output name probed before
write: ABSENT. No git action.

---

## 0. VERDICT IN ONE LINE

**NET = PROVED at exactly the licensed strength: the assembled response's
degree <= -1 remainder is HS-controlled CARRIER-UNIFORMLY, and no
resummation inside the coincidence strata can resurrect degree-0 content —
B-L2* is DISCHARGED at the summed level. THE DECIDING STRUCTURE, found and
verified: the tasked honest difficulty is REAL (one power of escape gives
HS^2 linear in the carrier — §3.1, exact) and the missing power is NOT
supplied by the angular zero — it is supplied by the assembly's DOUBLE
degree-0 kill: BOTH branch-factor contents of X = a_+ C U(0)^dag F C +
a_- C F~^dag U(0) C lose their degree-0 intraband parts EXACTLY (U(0) by the
sealed resummation of record; F by the same m_0 kill, of record in the same
sealed artifact), so the assembled remainder starts at degree -2, one FULL
power past the momentum-side threshold (2a = 4 > 3). The inherited "leading
degree-(-1) V_2-form" is exact PER FACTOR; the ASSEMBLED leading remainder
is the V_2 (x) V_2 product form at degree -2. The angular zero's exact role
is subordinate and is worked exactly: it caps each coincidence insertion's
joint transfer mass at HALF its pointwise power (|k|^{-1/2} per insertion,
exact mass integrals §3.4) — enough to keep every coincidence stratum past
threshold given the parity floors (every existing X-stratum has
2o + c >= 4, §2.3/§3.5), not enough to have rescued a bare degree-(-1)
object. Reversal inside the coincidence strata: EXCLUDED at the norm level —
the stratum sum of HS bounds is ABSOLUTELY convergent (the growing moments
m_{2j} = -2^{j-1} are the lambda = sqrt2 dominated exponential's own
coefficients), and an HS operator cannot carry degree-0 intraband content.
Remaining for the horn, untouched: F'-14, the summed-S2' spec act, and the
compute pre-gate.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session; all match sidecars and tasked
prefixes. None unverifiable.

```text
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md          MATCH (tasked a7969f0c)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md    MATCH (tasked a4f2e1b7)
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md         MATCH (tasked 6e81ae92)
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md           MATCH (tasked 2699af25)
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md  MATCH (tasked 3b9730e0)
b4a55baa5cb46de474be6e03c7fbdea728118c38ebc24529c34ae4b77e184bad  STAGE8_CL_ERROR_CHECK_V001.md          MATCH (tasked b4a55baa)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA; tasked 789338ad)
```

Sealed content read at path this session and relied on below: E1 :339-352
(C4 second-difference weights, m_0 = m_1 = 0, m_{2j} = -2^{j-1}, l1-tight
saturation), :353-364 (C6 two-time-only + volume-diagonal locus), :465-470
(D6' sharp M(t), v(t) = tau_R 32 r(t)^3, no-mollify clause), :1106-1126
(B-L2/B-L2* typing; only-repair clause), :1131-1144 (C-L1, C-L2
quadratic-form demand); R2-RESUM §2-§6 + flag block (the two-level intraband
reduction; the excursion/domination display |A^(n)| <= (|lambda|Theta)^n/n!;
degree-0 content of E = -1_ball; U(0)'s degree-0 intraband symbol = 0 BOTH
WAYS; F's degree-0 intraband content = 0 (§5(c)); the V_2 subsidiary
-i V_2(x)/(2|k|), V_2 = (8 pi^2/7)(1-128 rho^7); r2 relocated to THIS
remainder; reversal inside coincidence strata left open) + its check
(CONFIRMED on all four attacks; (d1)-(d5) survivor-fate audit incl. "F's
weight sum -> m_0 (free) J (free) = 0 at degree 0"; (e1) r2 relocation
UPHELD); BL2STAR attempt §2.1 (HS thresholds BOTH sides: position p < 3/2;
momentum 2a > 3), §4.1-4.3 (S interband; J5 coincidence factor; per-stratum
degree certification; the mixed-transfer-regime trade), §5 (chi_hat closed
form, L^2 not L^1, tails clear/factor-wise bounds do not exist), §6 (F1-F3
saturation fences) + its check (thresholds CONFIRMED; r2 real); CL-ERROR §2
(E = E_prof + E_int; the exact D-kernel (e^{i(s'|k'|-s|k|)t}-1)
chi_hat_{r(t)}(q) P_{s'}(k')P_{-s}(k) S; J3-J6 per-vertex identities), §3.2
(E_int channel bookkeeping; dressing derivative bound; interband blocks of F)
+ its check (K1-K6; n = 2 ledger); PA :75-128 (A1/A2 carrier, S, M(t), v(t),
b_D Gevrey, J = -(Q b_D Q) (x) alpha_x), :367-384 (Strang).

---

## 2. STEP 1 — THE REMAINDER OBJECT, EXACTLY

### 2.1 The object of record

Of record (Q-1042 chain, R2-RESUM + check): in the sealed assembly

```text
X = a_+ C U(0)^dag F C + a_- C F~^dag U(0) C + O(a^2),
U(0) = sum_l w_l u_l(0),  F = -i int_0^1 sum_l w_l W_l(1,t') J(t') W_l(t',0) dt',
```

the ENTIRE degree-0 intraband content cancels exactly:
(i) [U(0)]_{deg-0,intra} = 0 (WAY 1 = WAY 2 = 0, sealed + checked);
(ii) [F]_{deg-0,intra} = 0 (the same m_0 kill applied on the sub-intervals
[0,t'], [t',1]: per lambda both W's -> free phases LAMBDA-INDEPENDENTLY, so
the weight sum -> m_0 · (free) J (free) = 0; the sin·sin channel needs two
interband hops, both dying — R2-RESUM §5(c), check (d2) CONFIRMED);
(iii) hence SURVIVOR_FATE = CANCELS: X's degree-0 intraband content is zero.

THE REMAINDER OBJECT IS THEREFORE X ITSELF, post-cancellation: every
nonvanishing term of X carries >= 1 oscillatory factor (a pure excursion
phase e^{-i DeltaE s} integrated against BV amplitudes, or the interband
flip-time oscillation at |k'|+|k|) or >= 1 coincidence factor (the exact J5
stay-factor P_s(k')P_{-s}(k) = -(s/2) P_s(k')(alpha.(k_hat - k_hat')),
§6 R2c). Off-ball content is zero at every order (v~ = 0 there); the free
factor e^{-i h_0} is a common modulus-1 band-diagonal phase, carried,
content-neutral; the C-compression is free for norms (C a projector,
||C||_op = 1). B-L2* asks: ||X||_2 finite UNIFORMLY over the D3/
Hermite-Galerkin refinement family — and since every carrier compression
obeys ||Pi X Pi||_2 <= ||X||_2, carrier-uniformity is EXACTLY HS-finiteness
of the uncompressed X: the trilemma form sup_n ||X_n||_2 <= M.

### 2.2 Strata taxonomy: the exact (o, c) bookkeeping per factor

Each branch factor (the U(0)-factor; the F-factor) expands over record
insertions. Per insertion the band action is EXHAUSTIVE by the sealed
algebra (§6 R2b): the S-vertex either FLIPS the band — full O(1) element,
P_+(u) S P_-(u) = S P_-(u) (§6 R2d) — or STAYS — carrying the exact J5
coincidence factor (§6 R2c), one power |k_hat'-k_hat| <= 2|q|/max(|k|,|k'|)
per stay. The F-factor's single J vertex (alpha_x-type) either flips
(O(1) interband part) or stays (O(1) LONGITUDINAL intraband part
-k_hat_x C(k) — the smooth vertex has NO coincidence zero: the sealed
contrast). Every plus-band SEGMENT (opened/closed by flips) carries the
free phase at |k'|+|k| ~ 2|k| integrated against BV/Lipschitz-with-corner
amplitudes: ONE licensed integration by parts per segment, >= one power
|k|^{-1}, POWER-LAW only, jumps retained as boundary terms (the sealed
record-strata typing, M(t) untouched). Bookkeeping labels per term:

```text
o = number of oscillatory powers taken (>= number of plus-band segments;
    for the all-intraband two-level channel, o >= 1 iff >= 1 excursion
    factor survives — the phase-free part of each factor is its degree-0
    content, already cancelled);
c = number of J5 stay-factors (coincidence powers).
Pointwise amplitude degree of the term at bounded transfer: <= -(o + c).
All-orders domination, inherited exactly: the order-n coefficient is
bounded by (sqrt2 Theta(x))^n / n! (modulus-1 phases, ordered-simplex
identity, |lambda| <= sqrt2, Theta <= tau_R): the strata sums are dominated
termwise by e^{sqrt2 tau_R}, a symbolic dominator.
```

### 2.3 The parity floors (new, exact — the load-bearing enumeration)

The weight functional is a symmetric second difference: w_+ = w_- and
m_0 = m_1 = 0. Consequently it kills EVERY ODD FUNCTION of lambda
identically — not merely odd polynomials (§6 R1c): sum_l w_l f(l) =
(1/2)f(0) - (1/4)[f(sqrt2) + f(-sqrt2)] = 0 whenever f is odd. A factor
term with n S-insertions is (-i lambda)^n-graded: ODD n DIES EXACTLY.
With n even, band parity closes the enumeration (§6 R4, verified by
exhaustive enumeration n <= 8):

```text
PER-FACTOR FLOORS (nonzero classes only):
  sea-sea (intraband) block:  flips even, so stays = n - flips even:
    EITHER flips >= 2, c = 0:  o >= 1  — the oscillatory class, degree -1,
       leading coefficient the V_2-form (U-side: -i V_2(x)/(2|k|), the
       sealed subsidiary; F-side: +i V_2-form/(2|k|) via m_2 = -1, §6 R1d,
       with V_2^{[0,t']} + V_2^{[t',1]} = V_2 exactly across the J-junction,
       §6 R6b);
    OR   stays >= 2 (c >= 2):  degree <= -2 — the coincidence class;
    (flips = stays = 0 is the n = 0 stratum: m_0-killed identically.)
    FLOOR: 2o + c >= 2.
  interband block: flips odd (>= 1) AND stays = n - flips odd (>= 1):
    o >= 1 AND c >= 1: degree <= -2.  FLOOR: 2o + c >= 3.
  (F-factor: identical floors with the J flip included in the parity —
   §6 R4b. The bare-J stratum (n = 0) is m_0-killed identically, so F has
   NO lambda-independent content at ANY degree.)

ASSEMBLED X (sea-compressed, the junction is an exact band resolution
C U^dag (C + C_+) F C — no mismatch factor at the junction):
    intra x intra route:  2o + c >= 2 + 2 = 4
    inter x inter route:  2o + c >= 3 + 3 = 6
    EVERY existing X-stratum:  2o_total + c_total >= 4.        (§6 R4c)
```

There is NO (c = 1, o = 0) class anywhere: a lone coincidence factor forces
odd stays, hence an interband block, hence a flip and its oscillating
segment. This kills in advance the only configuration that would have been
HS-marginal (2o + c = 3, log-divergent) or worse (2o + c = 2).

### 2.4 Exact degree bookkeeping vs the inheritance

The inherited grading ("degree <= -1 per order, leading degree-(-1)
coefficient in exact V_2-form") is confirmed PER FACTOR and SHARPENED for
the assembled object: each factor's remainder is degree <= -1 with the
V_2-form leading; the assembled X-remainder is degree <= -2, leading
stratum the (o = 1, c = 0) x (o = 1, c = 0) product — the V_2 (x) V_2 form

```text
[X]_{-2, intra, leading} = a_± · (+i V_2(x)/(2|k|))_{U^dag-side} ·
                           (+i V_2-form/(2|k|))_{F-side} · [J-profile]
                         = -(a_±/4) V_2(x) · V_2F(x,·) / |k|^2 · [profile],
```

a lawful sharpening, not a contradiction: the sealed "<= -1" was a bound;
the degree-(-1) content of X vanishes IDENTICALLY by the double kill (§3.2).

---

## 3. STEP 2 — THE JOINT ESTIMATE

### 3.1 The honest difficulty, confirmed exactly first

The tasked difficulty is REAL and is exhibited before it is answered. A
degree-(-1) intraband amplitude at bounded transfer has HS^2 at the volume
diagonal ~ int^Lambda 4 pi k^2 (C^2/k^2) dk = 4 pi C^2 (Lambda - k_0):
LINEAR in the carrier radius (§6 R8c, exact) — 2a = 2 < 3. Concretely:
U(0)'s own remainder ALONE (leading symbol -i V_2(x)/(2|k|)) is NOT
carrier-uniformly HS: by Plancherel in the x -> q transform at fixed k
(exact, Kohn-Nirenberg kernel), HS^2 = int d^3k ||V_2/(2|k|)||^2_{L^2(ball)}
~ Lambda. The same holds for F alone. No transfer decay can repair this:
the q-integral already converges to a constant; the divergence is the
k-volume at bounded transfer. ONE POWER OF ESCAPE IS NOT ENOUGH — exactly
as tasked. Any honest discharge must find a SECOND power.

### 3.2 The second power: the assembly's DOUBLE degree-0 kill (the deciding step)

The assembled response is a PRODUCT of two branch factors, and BOTH lose
their degree-0 intraband content exactly, of record:

```text
[U(0)]_0 = 0  (sealed R2-RESUM, both ways, checked);
[F]_0    = 0  (same artifact §5(c), check (d2) CONFIRMED — the m_0 kill on
              the sub-intervals; F's n = 0 stratum is killed IDENTICALLY,
              at all degrees, since m_0 = 0 multiplies it as an operator).

Hence, grading the sea-compressed product exactly over band blocks
(the junction C U^dag (C + C_+) F C is an exact resolution — no junction
correction terms exist):

  [X]_{-1, intra} = [U^dag]_{-1}[F]_0 + [U^dag]_0[F]_{-1} = 0 + 0 = 0
                                                            (§6 R8a)
  interband route: [U^dag]_inter [F]_inter — each interband block has
  2o + c >= 3 (§2.3), the route is degree <= -3: no degree-(-1) content.
```

THE ASSEMBLED REMAINDER STARTS AT DEGREE -2. The missing power demanded in
§3.1 is supplied by the SECOND FACTOR's own escape — not by the angular
zero (§3.4), not by transfer decay. The mechanism is the same sealed
two-moment kill m_0 = 0, applied ONCE PER FACTOR; the task's "joint
structure" is, at the leading stratum, the PRODUCT structure.

### 3.3 The leading stratum (V_2 (x) V_2), worked symbolically

The leading X-stratum is the (o=1,c=0) x (o=1,c=0) product of the two-level
oscillatory channels. Within the two-level reduction, x is a PARAMETER (the
inherited x-space multiplicative structure of the phase-free channel extends
to the full two-level channel: nested sharp indicators multiply, the band
phases carry no x-derivatives), so the reduction content of each factor is
an x-parameterized symbol and the product is the pointwise product of
symbols. Three exact steps:

```text
(3.3a) RATE, per factor (quantitative — Riemann-Lebesgue alone is rate-free
   and is NOT used): the two-level sea-sea amplitude obeys
   |A_lambda(x, omega) - 1| <= C_rate(x) lambda^2 / omega
   by one integration by parts in the innermost excursion duration against
   BV amplitudes (jumps at t = |x|, 1-|x| and the v-corner retained as
   boundary terms; each further order redistributes over the simplex with a
   factor <= n, absorbed by the domination — the order-coefficient sum
   sum_n (sqrt2 Theta)^n n^2 2^n / n! is an entire-function value in closed
   form, §6 R8d). The slab instance verifies the WHOLE chain in closed form
   (§6 R5a-R5c): A_int - 1 = (e^{i(Om-om)T} - 1) - i e^{-i om T} sin(Om T)
   mu^2/(Om(Om+om)) exactly; |e^{iz}-1| = 2|sin(z/2)| <= |z|;
   Om - om = mu^2/(Om+om) <= mu^2/(2 omega) exactly:
   |A_int - 1| <= (mu^2/omega)(T/2 + 1/2) — the C/omega rate pinned by the
   unitarity decomposition of record (V4c/V4d), nothing imported.
   Weight-summed with m_0 = 0 and the absolute mass sum_l |w_l| l^2 = 1
   (§6 R5d): |sigma_U^rem(x, omega)| <= 2 C_rate(x)/omega, and identically
   |sigma_F^rem(x, omega)| <= C_F(x)/omega (same mechanism through the
   J-sandwich; J's intraband part is O(1) and bounded; b_D bounded). Both
   also obey the trivial l1 bounds |sigma_U^rem| <= 1 + 1 = 2,
   |sigma_F^rem| <= sup|b_D| — the small-omega regime is bounded.
(3.3b) PRODUCT: |sigma_X(x, omega)| <= |a| ( |sigma_U sigma_F| + ... )
   <= C_X(x) min(1, 1/omega^2), C_X symbolic (products of the above).
(3.3c) PLANCHEREL (exact — the q-integral is not estimated, it is
   EVALUATED): for an x-parameterized symbol, HS^2 = int d^3k int d^3q
   |sigma_hat(q, k)|^2 = int d^3k (2pi)^3 ||sigma(., k)||^2_{L^2(ball)}
   <= (2pi)^3 Vol(ball) [ int_{k<=k_0} C_X^2 d^3k
                          + int_{k>k_0} C_X^2 / k^4 d^3k ]
   = (2pi)^3 Vol(ball) C_X^2 [ (4 pi/3) k_0^3 + 4 pi / k_0 ]     (§6 R8b)
   — FINITE, all constants symbolic, NO carrier reference anywhere:
   CARRIER-UNIFORM. The chi_hat L^2-not-L^1 wall NEVER ENGAGES for this
   stratum: Plancherel converts the transfer integral to x-space where the
   domination is absolute — the inherited x-space multiplicative structure
   doing for the L^2 norm exactly what it did for the phase-free channel's
   resummation.
```

### 3.4 The angular zero, worked exactly (the tasked question answered)

The coincidence factor's angular structure, EXACT (k' = k + q; §6 R3):

```text
|k_hat' - k_hat|^2 = 2 |k x q|^2 / [ |k||k'| ( |k||k'| + k.k' ) ]
```

(Lagrange identity + k x k' = k x q, both CAS-verified). At |q| << |k| this
is |q|^2 sin^2(psi)/|k|^2 (psi = angle(k, q)): the factor VANISHES at
forward scattering (q parallel to k) — the tasked angular zero — and is
globally capped at 2. Its exact joint accounting against the transfer tails
(chi_hat = 4 pi (sin qr - qr cos qr)/q^3 re-derived, §6 R6c; envelopes
|chi_hat| <= (4 pi/3) r^3 and <= 8 pi r/q^2 for qr >= 1, from the exact
derivative identity §6 R6d): the per-insertion joint L^2 transfer mass is

```text
M(omega) = int d^3q |chi_hat(q)|^2 min(q^2/omega^2, 4)
         <= 64 pi^3 r/(45 omega^2)  +  256 pi^3 r^2 (2 omega - 1/r)/omega^2
            + 512 pi^3 r^2/omega
         <= 64 pi^3 r/(45 omega^2) + 1024 pi^3 r^2 / omega        (§6 R7)
```

— all three pieces exact symbolic integrals. THE ACCOUNTING: a coincidence
factor is worth |k|^{-1} POINTWISE at bounded transfer but only |k|^{-1/2}
in the joint L^2 sense — its pointwise q/|k| growth spends half the power
against chi_hat's tails (without the cap, int d^3q |chi_hat|^2 q^2/omega^2
diverges linearly: the naive full power is NOT available; without the
q^{-2} tails, nothing converges at all). The ANGULAR zero itself supplies
NO power of |k|: the sin^2(psi) weight angular-averages to a constant
(2/3) — the power comes from the RADIAL trade. VERDICT ON THE TASKED
HYPOTHESIS: the angular zero does NOT supply the missing power; the missing
power is the product structure (§3.2); the angular/radial joint trade is
what keeps the coincidence strata from falling BELOW threshold: with the
parity floors, every coincidence stratum satisfies 2o + c >= 4 and its
HS-effective k-exponent is -(2o + c) <= -4 < -3, with one full power to
spare — including the c = 2, o = 0 double-stay classes (2o + c = 4 via the
partner factor's floor).

### 3.5 The general stratum: induction over insertion order

```text
CLAIM: sum over ALL strata of ||X^(strat)||_2 < infinity, carrier-uniformly.
STRUCTURE OF THE BOUND, per stratum (n_U, n_F insertions, channel
assignment):
 (i)  peel every dressing unitary and every C at OPERATOR norm (= 1), and
      every un-designated insertion at its l1/ordered-simplex mass: the
      order coefficient is <= (sqrt2 Theta)^{n}/n! x 2^n (channel count)
      x n^2 (IBP redistribution, at most two IBPs performed) — absolutely
      summable in closed form (§6 R8d);
 (ii) keep the stratum's DECAY-BEARING structure: 2o + c >= 4 (§2.3). The
      o-powers contribute pointwise omega^{-1} factors with omega-uniform
      L^2(x) profiles (Plancherel route, §3.3c); the c-powers contribute
      the capped joint masses M(omega) <= C_coin/omega (§3.4);
 (iii) compositions over intermediate momenta: Cauchy-Schwarz in the
      intermediate variable against chi_hat in L^2 (finite ||chi_hat||_2,
      pointwise-bounded composites), with the sealed mixed-transfer-regime
      trade (an internal |q_i| ~ |k|^gamma trades coincidence gain against
      tail loss, combined weight <= |k|^{-1} pointwise, <= |k|^{-1/2} in
      L^2 mass — never better for the adversary, re-affirmed not re-proven);
 (iv) HS convergence: int d^3k omega^{-(2o+c)} with 2o + c >= 4 converges
      (§6 R8b), bounded-omega regime bounded by the l1 masses.
RESULT: ||X^(strat)||_2 <= [entire-function coefficient] x [K_HS symbolic],
Sigma over strata ABSOLUTELY convergent; the O(a^2) polydisc orders carry
the same floors (each extra J is a bounded Gevrey vertex; every
single-branch chain keeps its m_0 kill: the double-kill argument of §3.2 is
a-order-independent) and the a-series is dominated on the closed polydisc.
CARRIER-UNIFORMITY: everything above is carrier-free; every refinement
compression obeys ||Pi X Pi||_2 <= ||X||_2: sup_n ||X_n||_2 <= M delivered
in the sealed trilemma form.
```

### 3.6 Fence audit against the sealed saturation clause (F1-F3)

This is NOT the F2-forbidden route. F2 forbids certifying the RESUMMED
object by per-stratum coincidence orders where the stratum sum saturates —
i.e., forbids CONDITIONAL cancellation claims against the l1-tight sum. Here
(i) the resummed O(1) content (the sharp projector P and all degree-0
content) is handled by the EXACT cancellation of record, not by decay
claims; (ii) the remainder's stratum sum is bounded by an ABSOLUTELY
convergent sum of norms (||Sigma|| <= Sigma ||.|| holds unconditionally —
saturation has no purchase on absolute convergence); (iii) the growing
moments m_{2j} = -2^{j-1} never appear as growing constants against a
fixed gain: they are the lambda = sqrt2 values of the dominated exponential
series' own coefficients, inside (sqrt2 Theta)^n/n!. F1 untouched (no
summed decay is obtained from weight cancellation on >= 2-insertion strata;
the kills used are m_0 = 0 per factor — sealed identities — and absolute
domination). F3 honored (the record dressing enters as the FULL resummed
two-level amplitudes; the rate bounds are on the resummed objects).

---

## 4. STEP 3 — THE REVERSAL QUESTION, DECIDED

Can a resummation WITHIN the coincidence strata (the analogue of the
phase-free trick one level down) reassemble degree-0 content?

```text
(4a) STRUCTURE: the phase-free channel's exact x-space multiplicative
     composition IS inherited one level down — but only by the OSCILLATORY
     two-level channel (x remains a parameter; used exactly in §3.3c). The
     COINCIDENCE strata have NO analogous exact multiplicative structure:
     the J5 factor alpha.(k_hat - k_hat') is a momentum-ANGULAR (Riesz-
     type) factor — its x-space realization is a singular-integral kernel,
     not a multiplication; coincidence compositions neither commute nor
     collapse to a closed form. NO exact resummation of the coincidence
     strata exists to exhibit — and NONE IS NEEDED:
(4b) NORM EXCLUSION: the stratum sum converges ABSOLUTELY in HS norm,
     carrier-uniformly (§3.5). An HS operator cannot carry degree-0
     intraband content: a degree-0 intraband symbol has HS^2 ~ carrier
     momentum volume (§3.1 / §6 R8c) — incompatible with the uniform bound.
     A degree-0 resurrection by resummation would therefore contradict the
     absolutely convergent bound: EXCLUDED. (The C4-reversal mechanism —
     per-stratum gains with growing constants defeated by a CONDITIONALLY
     structured sum — has no object here: absolute convergence is immune
     to rearrangement by construction.)
(4c) LIMIT EXCLUSION (independent second way): the summed remainder's
     intraband symbol obeys |sigma| <= C(x) min(1, omega^{-2}) pointwise
     (the dominated limit exchange of §3.3a, licensed by the inherited
     domination); degree-0 content IS the large-omega limit: zero.
(4d) CROSS-CHECKS USED, as tasked: UNITARITY — the rate chain §3.3a is the
     slab unitarity decomposition of record (V4c/V4d re-derived, §6 R5a/
     R5b): the escape rate is pinned by |A|^2 + |B|^2 = 1, no independent
     smallness assumed. MOMENTS — the double kill is m_0 = 0 twice (§6
     R1a); the leading coefficients are m_2 = -1 forms (§6 R1d); the
     n = 2 ledger consistency: the jet of -i e^{iy} sin y is -iy + y^2
     (§6 R1e), matching the sealed V_2-form leading order exactly.
REVERSAL = EXCLUDED.
```

---

## 5. STEP 4 — VERDICT, AT EXACTLY THE LICENSED STRENGTH

```text
NET = PROVED( B-L2* DISCHARGED AT THE SUMMED LEVEL:
  (1) the assembled response's remainder is HS-controlled CARRIER-UNIFORMLY
      — sup over the D3/Hermite-Galerkin refinement family of ||X_n||_2
      <= ||X||_2 <= M, M symbolic and carrier-free (§3.3, §3.5): the
      trilemma form of the sealed require-clause delivered;
  (2) no resummation inside the coincidence strata resurrects degree-0
      content (§4: norm exclusion + limit exclusion, independent).
  THE CHAIN: (i) X's degree-0 content cancels exactly (of record);
  (ii) BOTH factors' degree-0 intraband contents vanish (of record), so
  the assembled remainder is degree <= -2 — the double kill, this
  artifact's deciding step (§3.2); (iii) parity floors: every existing
  X-stratum has 2o + c >= 4 (new exact enumeration, §2.3); (iv) o-powers
  certified by the quantitative rate chain pinned to unitarity (§3.3a);
  c-powers certified by the exact capped joint masses (§3.4); Plancherel
  evaluates the transfer integrals exactly where the strata are
  x-parameterized, Cauchy-Schwarz + the sealed mixed-regime trade
  elsewhere (§3.5); (v) absolute convergence of the stratum sum with
  entire-function coefficients (§3.5, §6 R8d); (vi) fences F1-F3 audited
  and honored (§3.6). )

HONEST GRADE OF THE PROOF — the licensed strength, stated plainly:
  EXACT (identity-grade): the parity floors and the (c=1, o=0) exclusion;
    the angular identity; the double-kill ledger; the mass integrals M1-M3;
    the slab rate chain; every threshold arithmetic; V_2-additivity and the
    m_2-form of F's leading coefficient; the Plancherel conversions; the
    closed-form coefficient sums. All CAS-verified (§6, 31/31).
  ESTIMATE-GRADE (symbolic constants, the sealed lineage's own currency —
    the same grade as the sealed per-stratum degree certifications and
    licensed one-IBP-per-segment claims of record):
    (e-i)  the all-orders Volterra-IBP rate |A_lambda - 1| <= C lambda^2/
           omega with the n-redistribution absorbed by domination (§3.3a);
    (e-ii) the composed-transfer control over intermediate momenta
           (Cauchy-Schwarz in L^2(chi_hat) + the mixed-regime trade, §3.5).
    Neither is a traced-constant functional-analytic norm computation; both
    are strictly stronger than what the sealed lineage's FAILS/degree
    verdicts rested on, and each is pinned by an exact closed-form instance
    (the slab; the M-integrals).

WHAT WOULD OVERTURN IT (named for the check): a failure of [F]_0 = 0 or
  [U(0)]_0 = 0 under re-derivation (both sealed + independently checked of
  record — this artifact re-verifies their moment mechanism but inherits
  the operator statements); or a stratum class outside the §2.3 enumeration
  (the enumeration is exhaustive over the sealed vertex algebra: R2b/R2d
  exhaust the S-vertex's band action — flip O(1) or stay-with-J5 — and the
  J vertex has no third option); or a composition route that evades both
  the Plancherel conversion and the capped-mass Cauchy-Schwarz (none is
  visible; the sealed r2 wall — chi_hat not L^1 — is engaged and passed
  JOINTLY, never factor-wise).

WHY NOT THE OTHER VERDICTS: not FAILS-AT — the §3.1 failure shape (the
  tasked honest difficulty) is real but attaches to each factor ALONE,
  and the assembled object is the product: the failure is exhibited and
  then discharged, not evaded. Not REFORMULATED — the tasked object itself
  (carrier-uniform HS of the remainder, positive direction) is delivered,
  not a weaker surrogate. Not UNDECIDABLE-AT — no missing sub-object
  remains inside B-L2*: the two estimate-grade steps are performed, not
  deferred, and their grade is declared.

WHAT REMAINS FOR THE SUMMED-FIXED HORN (unmoved by this artifact, carried
exactly as sealed):
  DEBT 2: F'-14 — the log-of-sum passage. Untouched here.
  DEBT 3: summed-S2' — the unsealed re-quantification act placing the
    colour sum inside the Schatten-2 norm: a SPEC-AUTHOR'S act; this
    artifact analyzes the summed object structurally and cannot create the
    sealed target (noted of record in the lineage, unchanged).
  THE COMPUTE PRE-GATE — as tasked, downstream of both.
  CARRIED RESIDUES: (r-b) Q-resolution (Q passed through exactly as the
    sealed C4 display writes P sharp); (r-c) the free factor e^{-i h_0}
    (common modulus-1 band phase, content-neutral in every step above);
    (r-d) the successor-spec possibility (a spec-author's act, not a
    property of X). Per-pair nothing moves; M(t) sharp throughout;
    equal-time stays FALSE by C6.
```

---

## 6. THE SCRIPT (exact symbolic verification; reproduced in full) AND OUTPUT

Run with sympy 1.14.0 (fresh venv `remvenv`). Exact symbolic identity
verification only; nothing numeric evaluated; all constants symbolic or
exact rationals/surds.

```python
# REMAINDER: exact symbolic verification of the load-bearing identities.
# EXACT SYMBOLIC ONLY: matrix/trig/integral/series identities over symbols;
# nothing numeric evaluated; all constants symbolic or exact rationals/surds.
import sympy as sp
from itertools import product as iproduct

I2, Z2 = sp.eye(2), sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])

# Dirac representation, exactly as the PA-pinned executor (dirac_data()):
g0 = sp.diag(1, 1, -1, -1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit()) for s in (sx, sy, sz)]
g5 = sp.I * g0 * gs[0] * gs[1] * gs[2]
S = -sp.I * g0 * g5                    # S = -i slash(n) gamma^5, slash(n) = gamma^0
al = [g0 * g for g in gs]              # alpha_j = gamma^0 gamma^j
I4 = sp.eye(4)

ux, uy, uz, vx, vy, vz = sp.symbols('ux uy uz vx vy vz', real=True)
th = sp.symbols('theta', real=True)
rho = sp.symbols('rho', nonnegative=True)
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [sp.Rational(1, 2), sp.Rational(-1, 4), sp.Rational(-1, 4)]

UGENS = [ux**2 + uy**2 + uz**2 - 1, vx**2 + vy**2 + vz**2 - 1]
def red(M):
    M = sp.expand(M)
    out = sp.zeros(*M.shape)
    for i in range(M.shape[0]):
        for j in range(M.shape[1]):
            e = sp.expand(M[i, j])
            r = sp.reduced(sp.Poly(e, ux, uy, uz, vx, vy, vz, domain='EX').as_expr(),
                           UGENS, ux, uy, uz, vx, vy, vz)[1]
            out[i, j] = sp.simplify(r)
    return out

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

Au = ux*al[0] + uy*al[1] + uz*al[2]
Av = vx*al[0] + vy*al[1] + vz*al[2]
Pp, Pm = (I4 + Au)/2, (I4 - Au)/2
Pp_v, Pm_v = (I4 + Av)/2, (I4 - Av)/2

# ---------- R1: weights, moments, l1 mass, odd-kill ----------
mom = lambda n: sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v)))
ok("R1a m_0=0, m_1=0, m_2=-1, m_3=0, m_4=-2                          ",
   [mom(n) for n in range(5)] == [0, 0, -1, 0, -2])
ok("R1b l1 mass sum |w| = 1 ; |lambda| <= sqrt2 on the stencil       ",
   sum(abs(w) for w in w_v) == 1 and max(abs(l) for l in lam_v) == sp.sqrt(2))
ok("R1c ODD-KILL: w_+ = w_- => sum_l w_l f(l) = 0 for EVERY odd f    ",
   w_v[1] == w_v[2] and
   sp.simplify(sum(w*sp.sin(l*th) for w, l in zip(w_v, lam_v))) == 0 and
   sp.simplify(sum(w*l**3*sp.sin(l*th) for w, l in zip(w_v, lam_v))
               + sp.sqrt(2)**3*sp.sin(sp.sqrt(2)*th)/2) == 0)
y_ = sp.symbols('y_', real=True)
ok("R1d m_2-form: sum_l w_l (1 - i l^2 y) = +i y  (F's V_2-coefficient weight)",
   sp.simplify(sum(w*(1 - sp.I*l**2*y_) for w, l in zip(w_v, lam_v)) - sp.I*y_) == 0)
ok("R1e sum_l w_l e^{i l^2 y} = -i e^{iy} sin(y); jet = -iy + y^2 + O(y^3)",
   sp.simplify(sp.expand_complex(sum(w*sp.exp(sp.I*l**2*y_) for w, l in zip(w_v, lam_v))
       - (-sp.I*sp.exp(sp.I*y_)*sp.sin(y_)))) == 0
   and sp.simplify(sp.series(-sp.I*sp.exp(sp.I*y_)*sp.sin(y_), y_, 0, 3).removeO()
       - (-sp.I*y_ + y_**2)) == 0)

# ---------- R2: the S / band algebra (the parity machinery) ----------
ok("R2a S^2=I, S=S^dag, {S,alpha_j}=0                                 ",
   S*S == I4 and sp.simplify(S.H - S) == sp.zeros(4, 4)
   and all(sp.expand(S*a + a*S) == sp.zeros(4, 4) for a in al))
ok("R2b S P_s(u) = P_{-s}(u) S ; P_s(u) S P_s(u) = 0 (flip generator) ",
   sp.expand(S*Pp - Pm*S) == sp.zeros(4, 4) and sp.expand(S*Pm - Pp*S) == sp.zeros(4, 4)
   and red(Pm*S*Pm) == sp.zeros(4, 4) and red(Pp*S*Pp) == sp.zeros(4, 4))
Duv = (ux-vx)*al[0] + (uy-vy)*al[1] + (uz-vz)*al[2]
ok("R2c J5 stay-factor: P_s(v)P_{-s}(u) = -(s/2)P_s(v)(a.(u-v))       ",
   red(sp.expand(Pp_v*Pm + sp.Rational(1, 2)*Pp_v*Duv)) == sp.zeros(4, 4)
   and red(sp.expand(Pm_v*Pp - sp.Rational(1, 2)*Pm_v*Duv)) == sp.zeros(4, 4))
ok("R2d flip is FULL O(1): P_+(u) S P_-(u) = S P_-(u)                 ",
   red(Pp*S*Pm - S*Pm) == sp.zeros(4, 4))

# ---------- R3: THE EXACT ANGULAR IDENTITY of the coincidence factor ----------
kx, ky, kz, qx, qy, qz = sp.symbols('kx ky kz qx qy qz', real=True)
k = sp.Matrix([kx, ky, kz]); q = sp.Matrix([qx, qy, qz]); kp = k + q
cross = lambda a, b: sp.Matrix([a[1]*b[2]-a[2]*b[1], a[2]*b[0]-a[0]*b[2], a[0]*b[1]-a[1]*b[0]])
dot = lambda a, b: (a.T*b)[0, 0]
ok("R3a Lagrange: |k|^2|k'|^2 - (k.k')^2 = |k x k'|^2                 ",
   sp.expand(dot(k, k)*dot(kp, kp) - dot(k, kp)**2 - dot(cross(k, kp), cross(k, kp))) == 0)
ok("R3b k x k' = k x q  (k' = k+q)                                    ",
   sp.expand(cross(k, kp) - cross(k, q)) == sp.zeros(3, 1))
# hence, EXACT:  |khat' - khat|^2 = 2|k x q|^2 / [ |k||k'| ( |k||k'| + k.k' ) ]
K, Kp = sp.symbols('K Kp', positive=True)   # K = |k|, Kp = |k'| carried symbolically
ck = sp.symbols('ck', real=True)            # ck = k.k'
lhs = 2 - 2*ck/(K*Kp)
rhs = 2*(K**2*Kp**2 - ck**2)/(K*Kp*(K*Kp + ck))
ok("R3c |khat'-khat|^2 = 2(|k|^2|k'|^2-(k.k')^2)/(|k||k'|(|k||k'|+k.k'))",
   sp.simplify(lhs - rhs) == 0)

# ---------- R4: the parity floors (flip/stay enumeration, exact) ----------
# A branch-factor chain: n S-insertions (n even survives the weights: R1c kills odd n),
# each insertion FLIP (O(1), R2d) or STAY (one J5 factor, R2c). J-vertex: 0 or 1 flip.
# Band bookkeeping: sea->sea needs total flips EVEN; sea->plus needs ODD.
# o >= ceil(flips_S+J / 2) plus-segments each give >= 1 oscillatory power (IBP at ~2|k|);
# c = stays. Verify the floors: per-factor min(2o+c) over NONZERO classes.
def floors(jflips_allowed):
    seen = {}
    for n in range(0, 9, 2):                      # n even only (odd weight-killed)
        for flips in range(0, n+1):
            stays = n - flips
            for jf in jflips_allowed:
                total_flips = flips + jf
                blocktype = 'intra' if total_flips % 2 == 0 else 'inter'
                if n == 0 and jf == 0:            # bare stratum: m_0-killed
                    continue
                if n == 0 and jf == 1:            # single-J, no S: m_0-killed too
                    continue
                o_min = -(-total_flips // 2)      # ceil: plus-segments
                c = stays
                cost = 2*o_min + c
                seen.setdefault(blocktype, []).append(cost)
    return {b: min(v) for b, v in seen.items()}
fU = floors([0])          # U-factor: no J vertex
fF = floors([0, 1])       # F-factor: one J vertex (0 or 1 flip)
ok("R4a U-factor floors: intraband 2o+c >= 2, interband >= 3          ",
   fU['intra'] >= 2 and fU['inter'] >= 3)
ok("R4b F-factor floors: intraband 2o+c >= 2, interband >= 3          ",
   fF['intra'] >= 2 and fF['inter'] >= 3)
ok("R4c assembled X floors: intra*intra >= 4 ; inter*inter >= 6 ; ALL >= 4",
   fU['intra'] + fF['intra'] >= 4 and fU['inter'] + fF['inter'] >= 6
   and min(fU['intra'] + fF['intra'], fU['inter'] + fF['inter']) >= 4)

# ---------- R5: the slab RATE chain (quantitative, closed form) ----------
om, mu, T = sp.symbols('omega mu T', positive=True)
Om = sp.sqrt(om**2 + mu**2)
A2 = sp.cos(Om*T) + sp.I*om*sp.sin(Om*T)/Om          # sea-sea slab amplitude
A_int = sp.exp(-sp.I*om*T)*A2
decomp = (sp.exp(sp.I*(Om-om)*T) - 1) - sp.I*sp.exp(-sp.I*om*T)*sp.sin(Om*T)*mu**2/(Om*(Om + om))
ok("R5a A_int - 1 = (e^{i(Om-om)T}-1) - i e^{-i om T} sin(Om T) mu^2/(Om(Om+om))",
   sp.simplify(sp.expand(A_int - 1 - decomp)) == 0)
ok("R5b Om - om = mu^2/(Om+om) <= mu^2/(2 omega)  (exact identity + sign)",
   sp.simplify(Om - om - mu**2/(Om + om)) == 0
   and sp.simplify(mu**2/(2*om) - mu**2/(Om + om)) == sp.simplify(mu**2*(Om - om)/(2*om*(Om+om))))
z = sp.symbols('z', real=True)
ok("R5c |e^{iz}-1|^2 = 4 sin^2(z/2) ; and u - sin u nondecreasing (u>=0)",
   sp.simplify(sp.expand_complex((sp.exp(sp.I*z)-1)*(sp.exp(-sp.I*z)-1)) - 4*sp.sin(z/2)**2) == 0
   and sp.simplify(sp.diff(z - sp.sin(z), z) - (1 - sp.cos(z))) == 0)
# => |A_int - 1| <= (Om-om)T + mu^2/(Om(Om+om)) <= mu^2 T/(2 om) + mu^2/(2 om^2) * ...
#    <= (mu^2/om)(T/2 + 1/(2 om)) : the C/omega RATE, symbolic. Weighted (l1 mass 1,
#    sum|w_l| l^2 = 1): |sum_l w_l (A_int(l mu)-1)| <= (mu^2/om)(T/2 + 1/(2 om)).
ok("R5d sum_l |w_l| l^2 = 1 (the absolute m_2 mass of the rate bound) ",
   sp.nsimplify(sum(abs(w)*l**2 for w, l in zip(w_v, lam_v))) == 1)

# ---------- R6: profile arithmetic (V_2 and the transfer kernels) ----------
tauR = sp.pi/sp.sqrt(2)
tt = sp.Symbol('tt', real=True)
V2int = sp.integrate((tauR*32*tt**3)**2, (tt, rho, sp.Rational(1, 2))) \
      + sp.integrate((tauR*32*(1-tt)**3)**2, (tt, sp.Rational(1, 2), 1-rho))
ok("R6a V_2(rho) = (8 pi^2/7)(1 - 128 rho^7)  (re-derived)            ",
   sp.simplify(V2int - sp.Rational(8, 7)*sp.pi**2*(1 - 128*rho**7)) == 0)
tp = sp.symbols('tp', positive=True)
V2a = sp.integrate((tauR*32*tt**3)**2, (tt, rho, tp))
V2b = sp.integrate((tauR*32*tt**3)**2, (tt, tp, sp.Rational(1, 2))) \
    + sp.integrate((tauR*32*(1-tt)**2*(1-tt))**2, (tt, sp.Rational(1, 2), 1-rho))
ok("R6b V_2-additivity across the F-junction: V_2^{[0,t']}+V_2^{[t',1]} = V_2",
   sp.simplify((V2a + V2b) - V2int) == 0)
r_, q_ = sp.symbols('r_ q_', positive=True)
x_ = sp.symbols('x_', positive=True)
chi_hat = sp.integrate(2*sp.pi*x_**2 * sp.sin(q_*x_)/(q_*x_) * 2, (x_, 0, r_))
chi_closed = 4*sp.pi*(sp.sin(q_*r_) - q_*r_*sp.cos(q_*r_))/q_**3
ok("R6c chi_hat_r(q) = 4 pi (sin qr - qr cos qr)/q^3  (re-derived)    ",
   sp.simplify(chi_hat - chi_closed) == 0)
ok("R6d envelope derivative: d/dz[z^3/3 - (sin z - z cos z)] = z(z - sin z) >= 0",
   sp.simplify(sp.diff(z**3/3 - (sp.sin(z) - z*sp.cos(z)), z) - z*(z - sp.sin(z))) == 0)
# => |chi_hat| <= (4 pi/3) r^3 everywhere, and |chi_hat| <= 4 pi (1+qr)/q^3 <= 8 pi r/q^2 for qr >= 1.

# ---------- R7: the per-insertion joint L^2 MASS (angular-cap trade), exact ----------
# M(om) = int d^3q |chi_hat|^2 min(q^2/om^2, 4), split at q = 1/r and q = 2 om,
# with the symbolic envelopes of R6d (om r >= 1 regime displayed; constants symbolic):
M1 = sp.integrate(4*sp.pi*q_**2 * (4*sp.pi*r_**3/3)**2 * (q_**2/om**2), (q_, 0, 1/r_))
ok("R7a small-q piece: M1 = 64 pi^3 r / (45 om^2)                     ",
   sp.simplify(M1 - sp.Rational(64, 45)*sp.pi**3*r_/om**2) == 0)
M2 = sp.integrate(4*sp.pi*q_**2 * (8*sp.pi*r_/q_**2)**2 * (q_**2/om**2), (q_, 1/r_, 2*om))
ok("R7b mid piece: M2 = 256 pi^3 r^2 (2 om - 1/r)/om^2  [<= 512 pi^3 r^2/om]",
   sp.simplify(M2 - 256*sp.pi**3*r_**2*(2*om - 1/r_)/om**2) == 0)
M3 = sp.integrate(4*sp.pi*q_**2 * (8*sp.pi*r_/q_**2)**2 * 4, (q_, 2*om, sp.oo))
ok("R7c tail piece: M3 = 512 pi^3 r^2 / om                            ",
   sp.simplify(M3 - 512*sp.pi**3*r_**2/om) == 0)
# => M(om) <= C_coin(r)/om : each coincidence insertion carries HS-effective |k|^{-1/2},
#    i.e. HALF its pointwise power — the exact content of the angular-cap trade.

# ---------- R8: the double-kill ledger and the convergence displays ----------
u1, u2, f1, f2, w0 = sp.symbols('u1 u2 f1 f2 w0')
prodser = sp.expand((0 + u1/om + u2/om**2)*(0 + f1/om + f2/om**2))
ok("R8a double kill: (0 + u1/om + ...)(0 + f1/om + ...) = u1 f1/om^2 + O(om^-3)",
   sp.limit(prodser*om**2, om, sp.oo) == u1*f1)
C_, k0 = sp.symbols('C_ k0', positive=True)
tail = sp.integrate(4*sp.pi*K**2 * C_**2/K**4, (K, k0, sp.oo))
ok("R8b HS tail: int_{k>k0} 4 pi k^2 C^2/k^4 dk = 4 pi C^2/k0  (2a=4>3: converges)",
   sp.simplify(tail - 4*sp.pi*C_**2/k0) == 0)
onepow = sp.integrate(4*sp.pi*K**2 * C_**2/K**2, (K, k0, Kp))
ok("R8c the honest difficulty: int^Lambda 4 pi k^2 C^2/k^2 dk = 4 pi C^2 (Lambda-k0): LINEAR",
   sp.simplify(onepow - 4*sp.pi*C_**2*(Kp - k0)) == 0)
n_ = sp.symbols('n_', integer=True, nonnegative=True)
xdom = sp.symbols('xdom', positive=True)
coefsum = sp.summation(xdom**n_ * n_**2 * 2**n_ / sp.factorial(n_), (n_, 0, sp.oo))
ok("R8d order-coefficient sum: sum x^n n^2 2^n/n! = 2x(2x+1)e^{2x} (entire; closed form)",
   sp.simplify(coefsum - 2*xdom*(2*xdom + 1)*sp.exp(2*xdom)) == 0)
ok("R8e Fourier display: int d^3k e^{ik.x}/k^2 = 2 pi^2/|x| (radial reduction)",
   sp.simplify(4*sp.pi/K * sp.integrate(sp.sin(z)/z, (z, 0, sp.oo)) - 2*sp.pi**2/K) == 0)
```

Output, verbatim:

```text
R1a m_0=0, m_1=0, m_2=-1, m_3=0, m_4=-2                          : PASS
R1b l1 mass sum |w| = 1 ; |lambda| <= sqrt2 on the stencil       : PASS
R1c ODD-KILL: w_+ = w_- => sum_l w_l f(l) = 0 for EVERY odd f    : PASS
R1d m_2-form: sum_l w_l (1 - i l^2 y) = +i y  (F's V_2-coefficient weight): PASS
R1e sum_l w_l e^{i l^2 y} = -i e^{iy} sin(y); jet = -iy + y^2 + O(y^3): PASS
R2a S^2=I, S=S^dag, {S,alpha_j}=0                                 : PASS
R2b S P_s(u) = P_{-s}(u) S ; P_s(u) S P_s(u) = 0 (flip generator) : PASS
R2c J5 stay-factor: P_s(v)P_{-s}(u) = -(s/2)P_s(v)(a.(u-v))       : PASS
R2d flip is FULL O(1): P_+(u) S P_-(u) = S P_-(u)                 : PASS
R3a Lagrange: |k|^2|k'|^2 - (k.k')^2 = |k x k'|^2                 : PASS
R3b k x k' = k x q  (k' = k+q)                                    : PASS
R3c |khat'-khat|^2 = 2(|k|^2|k'|^2-(k.k')^2)/(|k||k'|(|k||k'|+k.k')): PASS
R4a U-factor floors: intraband 2o+c >= 2, interband >= 3          : PASS
R4b F-factor floors: intraband 2o+c >= 2, interband >= 3          : PASS
R4c assembled X floors: intra*intra >= 4 ; inter*inter >= 6 ; ALL >= 4: PASS
R5a A_int - 1 = (e^{i(Om-om)T}-1) - i e^{-i om T} sin(Om T) mu^2/(Om(Om+om)): PASS
R5b Om - om = mu^2/(Om+om) <= mu^2/(2 omega)  (exact identity + sign): PASS
R5c |e^{iz}-1|^2 = 4 sin^2(z/2) ; and u - sin u nondecreasing (u>=0): PASS
R5d sum_l |w_l| l^2 = 1 (the absolute m_2 mass of the rate bound) : PASS
R6a V_2(rho) = (8 pi^2/7)(1 - 128 rho^7)  (re-derived)            : PASS
R6b V_2-additivity across the F-junction: V_2^{[0,t']}+V_2^{[t',1]} = V_2: PASS
R6c chi_hat_r(q) = 4 pi (sin qr - qr cos qr)/q^3  (re-derived)    : PASS
R6d envelope derivative: d/dz[z^3/3 - (sin z - z cos z)] = z(z - sin z) >= 0: PASS
R7a small-q piece: M1 = 64 pi^3 r / (45 om^2)                     : PASS
R7b mid piece: M2 = 256 pi^3 r^2 (2 om - 1/r)/om^2  [<= 512 pi^3 r^2/om]: PASS
R7c tail piece: M3 = 512 pi^3 r^2 / om                            : PASS
R8a double kill: (0 + u1/om + ...)(0 + f1/om + ...) = u1 f1/om^2 + O(om^-3): PASS
R8b HS tail: int_{k>k0} 4 pi k^2 C^2/k^4 dk = 4 pi C^2/k0  (2a=4>3: converges): PASS
R8c the honest difficulty: int^Lambda 4 pi k^2 C^2/k^2 dk = 4 pi C^2 (Lambda-k0): LINEAR: PASS
R8d order-coefficient sum: sum x^n n^2 2^n/n! = 2x(2x+1)e^{2x} (entire; closed form): PASS
R8e Fourier display: int d^3k e^{ik.x}/k^2 = 2 pi^2/|x| (radial reduction): PASS
```

(31/31 PASS. The two estimate-grade steps (e-i)/(e-ii) are exact-analysis
steps stated and justified in §3.3a/§3.5; the CAS battery verifies every
algebraic/trig/integral/series identity they rest on, including the
closed-form slab instance where the rate is verified end-to-end
symbolically, R5a-R5d, and the exact capped-mass integrals R7a-R7c.)

---

## 7. FLAG BLOCK

```text
REMAINDER_OBJECT = EXACT(
  X itself post-cancellation: X = a_+ C U(0)^dag F C + a_- C F~^dag U(0) C
  + O(a^2) with ALL degree-0 intraband content cancelled of record — BOTH
  factors: [U(0)]_0 = 0 (sealed both ways) AND [F]_0 = 0 (same m_0 kill on
  sub-intervals, sealed §5(c), check (d2)). Strata: per factor, n even
  S-insertions (the weight functional kills EVERY odd function of lambda,
  R1c), each insertion FLIP (O(1), R2d) or STAY (exact J5 coincidence
  factor, R2c); every plus-band segment oscillates at |k'|+|k| with BV
  amplitudes (one licensed IBP per segment, jumps retained, M(t) sharp).
  PARITY FLOORS (new, exact, enumeration R4): sea-sea blocks: (o >= 1,
  c = 0) at degree -1 — leading coefficient the V_2-form per factor
  (U-side -i V_2/(2|k|); F-side +i V_2-form/(2|k|) via m_2 = -1, R1d, with
  V_2-additivity across the J-junction, R6b) — or (c >= 2) at degree <= -2;
  interband blocks: o >= 1 AND c >= 1, floor 2o + c >= 3; NO (c = 1, o = 0)
  class exists anywhere. Assembled X-strata: 2o_total + c_total >= 4.
  Degree bookkeeping sharpened lawfully: the inherited "leading
  degree-(-1) V_2-form" is exact PER FACTOR; the ASSEMBLED remainder is
  degree <= -2 with leading stratum the V_2 (x) V_2 product form (double
  kill, R8a). All-orders domination inherited: (sqrt2 Theta)^n/n!,
  dominator e^{sqrt2 tau_R} symbolic.)

JOINT_ESTIMATE = PROVED-CARRIER-UNIFORM(
  LEADING STRATUM (V_2 (x) V_2, degree -2): the honest difficulty
  confirmed first — one power gives HS^2 ~ 4 pi C^2 Lambda, LINEAR in the
  carrier (R8c): each factor ALONE fails; the assembly's DOUBLE m_0 kill
  supplies the second power ([X]_{-1} = [U^dag]_{-1}·0 + 0·[F]_{-1} = 0,
  R8a). Rate: |A_lambda - 1| <= C lambda^2/omega by the unitarity-pinned
  slab chain (R5a-R5c exact: A_int - 1 decomposition, Om - om =
  mu^2/(Om+om), |e^{iz}-1| = 2|sin(z/2)|), weighted by sum|w_l| l^2 = 1
  (R5d); product symbol <= C_X min(1, omega^{-2}); PLANCHEREL evaluates
  the transfer integral exactly (x-parameterized symbol — the inherited
  x-space multiplicative structure extended to the full two-level
  channel): HS^2 <= (2pi)^3 Vol(ball) C_X^2 [(4pi/3)k_0^3 + 4pi/k_0]
  (R8b) — finite, symbolic, carrier-free. The chi_hat L^2-not-L^1 wall
  never engages factor-wise; where it must be crossed (coincidence
  strata) it is crossed JOINTLY:
  ANGULAR-ZERO ACCOUNTING (tasked question answered, hypothesis
  CORRECTED): exact identity |k_hat'-k_hat|^2 = 2|k x q|^2/[|k||k'|
  (|k||k'| + k.k')] (R3a-R3c) — vanishes at forward scattering, capped at
  2. Per-insertion capped joint mass M(omega) = int d^3q |chi_hat|^2
  min(q^2/omega^2, 4) <= 64 pi^3 r/(45 omega^2) + 1024 pi^3 r^2/omega
  (R7a-R7c exact): a coincidence factor is worth |k|^{-1} POINTWISE but
  only |k|^{-1/2} in joint L^2 — the angular zero supplies NO power of
  |k| (its angular average is a bounded weight); the missing power is the
  PRODUCT structure, and the radial q/omega-vs-tail trade keeps every
  coincidence stratum past threshold. GENERAL STRATUM (induction): peel
  unitaries/C at operator norm 1; keep 2o + c >= 4 decay (o-powers:
  omega-uniform L^2(x) profiles + Plancherel; c-powers: capped masses;
  intermediates: Cauchy-Schwarz in L^2(chi_hat) + the sealed mixed-regime
  trade); int d^3k omega^{-(2o+c)} converges with a FULL power to spare
  (2o+c >= 4 > 3); order coefficients (sqrt2 Theta)^n 2^n n^2/n! sum to
  an entire-function value in closed form (R8d): Sigma ||stratum||_2 < oo
  ABSOLUTELY, carrier-uniformly; O(a^2) polydisc orders carry the same
  floors (m_0 kill is a-order-independent). Carrier-uniformity = HS of
  the uncompressed X dominates every refinement compression: trilemma
  form sup_n ||X_n||_2 <= M delivered.)

REVERSAL = EXCLUDED(
  two independent ways. NORM: the stratum sum of HS bounds converges
  absolutely and carrier-uniformly; an HS operator cannot carry degree-0
  intraband content (degree-0 => HS^2 ~ carrier volume, R8c): a resummed
  resurrection would contradict the uniform bound; absolute convergence
  is immune to rearrangement — the C4-reversal mechanism (growing moments
  m_{2j} = -2^{j-1} against per-stratum gains) has no object: those
  moments are the lambda = sqrt2 dominated exponential's own
  coefficients. LIMIT: the summed intraband symbol obeys |sigma| <=
  C(x) min(1, omega^{-2}) (dominated exchange); degree-0 content IS the
  large-omega limit: zero. STRUCTURE: the phase-free channel's exact
  x-space multiplicative composition IS inherited by the oscillatory
  two-level channel (x a parameter — used exactly via Plancherel); the
  COINCIDENCE strata have NO analogous exact multiplicative structure
  (the J5 factor is momentum-angular/Riesz-type, not an x-multiplication:
  no closed-form resummation exists) — and none is needed: absolute norm
  convergence replaces exactness. Cross-checks used as tasked: unitarity
  (the rate chain is the slab unitarity decomposition, R5) and the sealed
  moment identities (m_0 double kill; m_2 = -1 leading forms; the n = 2
  ledger jet -iy + y^2, R1e).)

NET = PROVED(
  B-L2* DISCHARGED at the summed level, at exactly the licensed strength:
  carrier-uniform HS control of the remainder + reversal exclusion, with
  the proof grade declared honestly (§5): identity-grade for every
  algebraic/threshold/mass/rate-chain step (31/31 CAS); estimate-grade
  with symbolic constants — the sealed lineage's own currency — for the
  all-orders Volterra-IBP rate and the composed-transfer Cauchy-Schwarz,
  each pinned by an exact closed-form instance. REMAINING FOR THE HORN,
  carried unmoved: DEBT 2 (F'-14 log-of-sum passage); DEBT 3 (summed-S2'
  re-quantification — a spec-author's act this artifact cannot perform);
  the COMPUTE PRE-GATE; residues (r-b) Q-resolution, (r-c) free factor,
  (r-d) successor-spec possibility. Per-pair nothing moves; M(t) sharp;
  equal-time stays FALSE by C6; the overturn conditions for the check are
  named in §5.)

SEALS_VERIFIED = 8/8(
  a7969f0c R2-RESUM; a4f2e1b7 R2-RESUM-CHECK; 6e81ae92 BL2STAR attempt;
  2699af25 BL2STAR check; 3b9730e0 CL-ERROR certification; b4a55baa
  CL-ERROR check; 46846730 E1; 789338ad PA — all recomputed from bytes at
  path this session, all matching sidecars and tasked prefixes; every
  cited span read at path; none unverifiable.)

FORBIDDEN_IMPORTS = none(
  no scale, no imported GR, no faithfulness authority; no physical
  quantity computed, bounded numerically, or evaluated — every constant
  symbolic (pi, r, tau_R, omega, Lambda never instantiated; the carrier
  radius appears ONLY inside the exhibited failure shape of §3.1, as the
  demonstration of the tasked difficulty, never as an evaluated bound);
  M(t) not softened (all IBPs in time variables against BV amplitudes,
  jumps as boundary terms); equal-time not resurrected (all objects
  two-time; C6 FALSE respected); no adiabatic theorem imported (rates
  derived from the exact slab decomposition + domination); no
  register/tracker/plan/road/ledger/lens file read.)

MACHINERY_INVOKED = yes(CAS identity checks only — sympy 1.14.0, fresh
  venv remvenv, 31 exact identity groups R1-R8 over symbols including one
  exhaustive finite enumeration (R4, n <= 8), script and output
  reproduced verbatim in §6; shasum/file reads otherwise; NOTHING numeric
  evaluated.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
REMAINDER_UNIFORMITY_RESULT = SEALED.
```
