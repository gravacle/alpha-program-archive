# STAGE 8 — REQUIRE-G1-CHECK: BLIND ADVERSARIAL VERIFICATION OF THE MATERIALITY-FORK GAP
## BLIND CROSS-LINEAGE VERIFIER (REQUIRE-G1-CHECK) — ATTACK BOTH SIDES OF THE GAP — [SEALED]

Date: 2026-08-13
Role: BLIND, cross-lineage, default-ATTACK. This artifact tests the claim that the
materiality fork (does `R_record,L` factorize? = does `Phi_f^T C Phi_H` survive?) is a
GAP blocked on three unbuilt objects, primary among them a connection-only cluster-
summability sub-estimate certifying the post-limit Gate-5 kernel `C`. It decides NOTHING
about forced/free, factorization, or the winding, and computes/bounds NO value of
`R_record,L`, the cross block, `n`, `kappa`, `alpha`, `mu`, `C`, `beta`, or any
scale/spectrum. Every symbol is symbolic. Verification is at the bytes: seals recomputed
at path, cited spans opened and compared. Fences held: connection-only, symbolic; no
scale/faithfulness/GR imported by me as authority (scale-bearing sources read only to
CLASSIFY them as scale-bearing — the fence itself, not an import). No register/tracker/
plan/road/ledger/lens read. No git action.

---

## 0. SEALS VERIFIED AT PATH (shasum -a 256, first 16 hex)

CLAIM UNDER TEST (verified before reading):
```text
STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md
  b28c08130887b361...  MATCHES the tasked digest b28c08130887b361cd...  OK
```

SOURCES the build cites (all recomputed at path; all MATCH the build's §1 table):
```text
FORM STAGE8_R_RECORD_L_FORM_FABLE_V001.md ............................ 5e49d2093d4ee17b  OK  CONN
POSE STAGE8_REQUIRE_TARGET_POSED_V001.md ............................. e3c482b5facfb741  OK  CONN
CHK  STAGE8_REQUIRE_TARGET_POSE_CHECK_V001.md ........................ 86068343c6e0ac1e  OK  CONN
V011 review_packets/.../BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md . aa7c6d4904706276  OK  CONN
M03  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_..._V001.md .... 2cd1ffcefd68ac03  OK  CONN
M06  STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_..._V001.md ....... d13920e2a7687ac5  OK  CONN
ECO  STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md ..... 0f3082cab910f2eb  OK  SCALE
RFA  STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md ............. 2ede02aea415157a  OK  SCALE
R4O  STAGE8_RL2B_REFUTING_BRANCH_FOUR_OBLIGATION_..._V001.md .......... f6fe40724ff4dfeb  OK  SCALE
XSM  STAGE8_CROSS_SECTOR_METRIC_RULE_ADMISSIBILITY_SPEC_V001.md ....... 3c008ecccc2b01ac  OK  SCALE
T1   STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md .................. e6fae1428cecffb3  OK  CONN
```
All twelve seals match (the build's eleven plus the CHK it names as CHK). The build is
anchored on verifiable sealed bytes; no source is stale or substituted. CONN/SCALE
typing recomputed independently below, not inherited from the build.

Typing check, independent: ECO/RFA/R4O carry the sea covariance `C(r)`, Lorentzian
causal diamonds, `|D|_3`/`|D|_4`, `L`, the spatial Schatten-2 norm, and the `alpha>=1`
exponent (ECO §5 :150-171; RFA §3-§4 :44-89; R4O §4 :102-140) — SCALE, confirmed at the
bytes. XSM carries `ell_P`, `G_4`, the Lorentzian spacetime metric, and `beta` (XSM per
POSE §4.2 :193-233, CHK §4 :142-145) — SCALE, confirmed. FORM/POSE/CHK/V011/M03/M06/T1
carry only connection/holonomy-indexed, count-intensive, `n`-independent content — CONN,
confirmed.

---

## 1. ATTACK 1 (UNDER-CLAIM, highest value) — IS THERE A CONNECTION-ONLY EXISTENCE ROUTE FOR C THE BUILD MISSED?  → NO_GAP_CONFIRMED

Target: the build claims (§3.1-§3.2, flag C_CONNECTION_ONLY_EXISTENCE) that of record
NO connection-only certification of `C` exists, and the only written route is the
scale/GR-bearing R-L2b. Attack: exhibit an abstract cluster/cumulant expansion, a
Duhamel/Dyson structural bound, a combinatorial connected-graph summability, or an
operator-norm locality bound that certifies the post-limit intensive kernel WITHOUT the
sea covariance / diamonds / `|D|_4` / spatial Schatten-2.

Every candidate connection-only route, checked at the bytes, IS the named Gate-5
obligation itself — unbuilt of record — not a route the build overlooked:

```text
ABSTRACT LINKED-CLUSTER / CUMULANT EXPANSION:
  V011 :1262-1281 seals it as an OBLIGATION verbatim — "the complete amplitude must
  additionally admit a linked-cluster expansion Gamma_K = sum_(connected clusters C)
  gamma_C, with a TARGET-FREE locality bound that makes gamma_density = lim
  Gamma_K/number_of_4_cells(K) exist and makes boundary corrections subextensive ...
  The disjoint theorem is proved; the connected thermodynamic limit remains a Gate-5
  obligation."  The "target-free locality bound" IS the connection-only certification
  the attack asks for — and V011 states it as a demand, not a construction.
  STATUS: V011 :2185  connected_linked_cluster_density_proved = false ;
          V011 :2189  cellulation_independence_proved = false ;
          V011 :1751-1752 (Gate-5 pass) still REQUIRES "the full-neighborhood analytic
          locality bound and cellulation-independent thermodynamic limit to pass."
  => the abstract cluster expansion is the GAP, not a missed route.

DUHAMEL / DYSON STRUCTURAL BOUND:
  V011 :1288-1300 seals the Duhamel object G_L and the constraint "The Duhamel
  covariance computed from G_L must reproduce this Hessian." The Duhamel STRUCTURE is
  named; the locality bound that would make its post-limit covariance intensive is the
  same unbuilt object above. Not a missed route.

COMBINATORIAL CONNECTED-GRAPH SUMMABILITY:
  This is precisely cluster-summability — "the decay of C_jk in cell separation
  replacing the all-pairs weight mu" (build §3.4; FORM D5 :254-262). It is the missing
  sub-estimate itself, unbuilt. Not a missed route.

OPERATOR-NORM LOCALITY:
  No sealed operator-norm locality bound on the transported write generators discharges
  intensivity of record. V011 :2184 volume_uniform_zero_free_neighborhood_proved =
  false; :2180 monoidal_disjoint_record_composition_proved = false. The only proved
  locality is DISJOINT additivity (V011 :1250-1258, BID_MONOIDAL), which the build
  already credits (P2) and which does NOT certify the connected/intensive limit.
```

ONE ADJACENT ROUTE THE BUILD DID NOT NAME — and it does not rescue the fork:
```text
THE COMMUTATOR ROUTE (ECO §5 :189-201; RFA §6 :132-136). The corpus contains a worked
HS identity ||[C,P]||_2^2 = 2 sum_i sigma_i(1 - sigma_i), "verified on 50 exact trials
in the R-L4b analysis", and ECO opens "RE-EXPRESS R-L2b's OBJECT AS A COMMUTATOR" so
the exponent is "read off the commutator's scaling rather than posited."
  WHY IT DOES NOT HELP ATTACK 1: (a) it is built on the SAME sea covariance C and
  diamond projections P — the sigma_i are singular values of a diamond-restricted
  product, so it is SCALE/METRIC-bearing, not connection-only; and (b) ECO :198-201
  seals it "A ROUTE, NOT A LEMMA: no bound is claimed, no exponent is derived, and
  R-L2b remains OPEN." So it is neither connection-only NOR built. It is a sub-route
  INSIDE the scale-bearing R-L2b, subsumed by the build's "only written route = R-L2b."
```

VERDICT: the build is NOT too pessimistic. There is no connection-only certification
route it missed: every abstract route named by the attack (cluster expansion, Duhamel,
combinatorial summability, operator locality) is co-referential with V011's Gate-5
"target-free locality bound / cellulation-independent thermodynamic limit," which is
sealed UNBUILT (:2185, :2189, :1751-1752). The build's identification of the primary
gap = exactly this missing connection-only cluster-summability sub-estimate is correct
and exhaustive. The GAP is not advanceable connection-only of record.
CONNECTION_ONLY_ROUTE_EXISTS = NO_GAP_CONFIRMED.
DISCLOSURE NOTE (not a verdict flip): the build omits the ECO §5 commutator sub-route;
it is scale-bearing and unbuilt, so its omission changes nothing.

---

## 2. ATTACK 2 (OVER-CLAIM, opposite horn) — IS SCALE-FREE CERTIFICATION ACTUALLY IMPOSSIBLE (C SCALE-ENTANGLED)?  → NO (GENUINELY OPEN)

Target: the build says "not proven impossible" (§3.4, flag: C is TYPED intensive/
connection-only, impossibility not established). Attack the other horn: is `C`'s very
existence (the thermodynamic-limit intensivity) UNAVOIDABLY a scale/volume statement —
a decay RATE in cell-separation being metric-flavored — so that beta re-enters and
Q-1013's "C scale-free" is contradicted?

The strongest scale-entanglement evidence in the corpus, checked at the bytes:
```text
RFA §5 :98-102 (the build does NOT cite this): "A diamond is ISOTROPIC with ONE
  parameter, so the cancellation rate is a function of a SINGLE SCALE rather than of a
  shape. On a general anisotropic atom 'the exponent' would NOT be a number — there
  would be no single scale for it to be an exponent OF. The charter is what makes R-L2b
  a question about a number."
ECO §4 :133-141 + §1.2 :69-81: if connected extensivity fails, the NEAREST shape is a
  RUNNING kappa_record — "a response that depends on the cellulation scale rather than
  converging to an intensive constant, i.e. a RENORMALIZATION-GROUP structure" — "a NEW
  PRINCIPLE." A scale-dependent response is exactly beta-flavored.
```
This shows the scale-entanglement SUSPICION is real and, notably, that the build's "NOT
NO" is cleaner-sounding than the record warrants (see §5 disclosure flag).

But the opposite evidence blocks the impossibility PROOF, and it is decisive on
connection-only bedrock:
```text
RFA §2 :29-42: the divergence is a SHORT-DISTANCE (UV) divergence, "r -> 0 IS INTERNAL
  TO WHATEVER DIAMOND ONE IS IN"; the cancellation "is a UV phenomenon occurring BELOW
  the diamond scale"; "the cancellation rate DOES NOT CARE HOW THE ATOM WAS CUT UP."
ECO §5 :164-171, :173-179: the sea covariance is HOMOGENEOUS DEGREE -3 — a scale-
  INVARIANT kernel — and "THE FINITENESS ... IS NOT A PROPERTY OF THE SUPPORT VOLUME AT
  ALL — IT IS PURCHASED ENTIRELY BY CANCELLATION IN THE DIFFERENCE ... alpha IS SET BY
  THE CANCELLATION RATE, NOT BY |D|_4."
ECO §1 :30-34: "ABSENCE OF PROOF THAT THEY DECAY IS NOT PROOF THAT THEY DO NOT."
```

Structural adjudication (symbolic, no value used): intensivity of `C` requires
SUMMABILITY of `C_jk` over cell-separation, i.e. convergence of a series indexed by the
combinatorial cell/incidence complex. Convergence of a series over a combinatorial index
set is a scale-free predicate — it needs the index set and the values, not a metric
length. The scale enters only when summability is OVER-SPECIFIED as a single exponent
`alpha` in a metric volume `|D|_4` (RFA §5.1), which is more than existence requires; and
the underlying homogeneity is degree -3, hence scale-INVARIANT (ECO §5). Cellulation-
independence (V011 :1751-1752, :2189) is invariance under combinatorial REFINEMENT, a
combinatorial operation. So there is no connection-only proof that the intensive limit
requires a metric scale. The scale-entanglement horn is SUGGESTED (RFA §5.1, the running-
kappa fallback) but NOT PROVED, and the corpus explicitly declines to prove it (ECO §1).

VERDICT: I cannot, on connection-only bedrock, prove `C` is scale-entangled. The
finiteness is a scale-invariant (degree -3) UV/cancellation phenomenon and intensivity
is combinatorial summability, both scale-free-able; the scale that RFA §5.1 requires is
the scale that makes the EXPONENT a single number, not the scale of EXISTENCE. beta does
NOT provably re-enter. This CONFIRMS the build's "NOT NO": the fork is a genuinely-open
GAP, not a proven scale-entanglement. SCALE_FREE_CERT_IMPOSSIBLE = NO (genuinely open).

---

## 3. ATTACK 3 (mu / G2) — IS mu GENUINELY UNDETERMINED OVER A CONTINUUM, OR DID THE BUILD MISS A CONSTRAINT THAT FORCES mu?  → CONFIRMED_CONTINUUM_UNBUILT

Target: build §4.3 / flag MU_G2 — `mu(w)` symbolic in `(w_0, w_ch)`, `mu ≡ 0 iff
w_0 w_ch = 0` (FORM D4 :222); the state-port fixing `(w_0, w_ch)` is unbuilt over a
continuum; `mu != 0` necessary for the cross-block. Attack: find a constraint forcing
`mu` (either `mu != 0`, or `mu = 0 => FREE`).

M06 is EXHAUSTIVE at the algebraic scalarization level and leaves a continuum:
```text
M06 §2.2-§2.4 :175-257: normalized positive functionals on B = C(Y) <-> probability
  measures on the compact character space Y; an explicit continuum omega_t =
  (1-t)omega_H + t omega_epsilon of pairwise-inequivalent survivors.
M06 §3-§4 :259-444: EVERY executable constraint leaves the continuum — U1 reality
  (invariant-measure simplex, :293-297), one-cell C0 restriction (consumes omega only
  through omega(1)=1, :399-406), inclusive-identity normalization (:435-440); rho_pre
  physical placement is UNEXECUTABLE (concrete density + descent unbuilt) => NO_VERDICT
  (:349-360, :487).
M06 §6 :500-506:  SCALARIZATION_FORCING_PROTOCOL_SURVIVOR_COUNT = CONTINUUM_AT_
  EXECUTABLE_LEVEL ; PHYSICAL_VERDICT = NO_VERDICT ; RHO_PRE_PLACED = false | TYPE-U ;
  U2_STATE_PORT_INSTANTIATED = false | TYPE-U.
```

No missed forcing constraint, checked symbolically:
- No executable constraint forces `w_0 w_ch != 0`: normalization fixes only `w_0 + w_ch`
  (via omega(1)=1); U1 reality, one-cell, and inclusive identity are all satisfied by the
  entire simplex. Positivity permits states supported on a single source sector (giving
  `w_ch = 0` or `w_0 = 0`, hence `mu = 0`) AND states straddling both (giving `mu != 0`).
- The two named endpoints straddle the sign structurally: `omega_H = tau_0 ⊗ tau_0` with
  `tau_0(U_lambda)=0` for `lambda != 0` (M06 :221-233) weights only the trivial/neutral
  content (plausibly `w_ch = 0 => mu = 0`); `omega_epsilon = epsilon ⊗ epsilon` with
  `epsilon(U_lambda)=1` for all lambda weights the charged content (plausibly
  `w_ch != 0 => mu != 0`). Both survive every executable constraint.
- The `mu = 0 => FREE` reading is CORRECT and already in the build: `mu` is a GLOBAL
  prefactor `n^2 mu (phi_f + phi_H)⊗(phi_f + phi_H)`, so `mu = 0` kills `R_record,L`
  entirely (the whole response is the neutral×charged interference term, FORM D4(ii)
  :226-229) — a degenerate everything-factorizes. The build states this exactly.

VERDICT: `mu` is genuinely undetermined over a continuum; no constraint forces it. The
build's MU_G2 is not over- or under-stated. MU_G2 = CONFIRMED_CONTINUUM_UNBUILT.

---

## 4. ATTACK 4 (PROVENANCE, both directions) — DID THE BUILD SECRETLY CONSUME A FORBIDDEN IMPORT TO REACH GAP?  → CLEAN

The build reaches NET = GAP through connection-only content only:
```text
- FORM's TYPING of C (connection/holonomy-only, intensive per N_4, n-independent —
  FORM D5 :248-252, B5) — CONN.
- V011's Gate-5 obligation, sealed UNBUILT (:1264-1281, :1751-1752, :2185) — CONN.
- M03's ratified source operator F_N = P_0 + Z_N P_ch (connection-only) — CONN.
- M06's scalarization continuum (character-algebra measures) — CONN.
```
The scale-bearing sources (ECO/RFA/R4O sea covariance, diamonds, |D|_3/|D|_4, L, spatial
Schatten-2, alpha>=1) and XSM (ell_P/G_4/Lorentzian metric/beta) are consumed ONLY to
CLASSIFY the only-written route as scale-bearing and to FENCE it — a negative/fencing use
that IS the gap's content, never an authority for a positive claim. The build imports no
exponent value, no volume, no beta, no length; §6 audits this and the audit holds at the
bytes.

Two directions checked:
- OVER-DIRECTION (did a scale sneak in to force DOES-NOT-FACTOR or FACTORS?): No. The
  build leaves the cross-block UNDETERMINED and does not evaluate `Phi_f^T C Phi_H`;
  §6 explicitly refuses to flatten GAP either way. No scale consumed to decide.
- UNDER-DIRECTION (did the "NOT NO" lean on a scale-bearing source as authority?): The
  build cites RFA §2 (a SCALE source) to support "impossibility not proven." This is a
  SUSPECT-ONLY, non-importing use (it denies a conclusion rather than asserting a value),
  which the fences permit. No scale value crosses into the determination.
PROVENANCE = CLEAN.

---

## 5. G1 BUILD VERDICT AND FLAGGED (NON-FATAL) DISCLOSURE GAPS

The NET = GAP determination is SOUND at the bytes:
1. No connection-only certification of `C` exists of record (§1, V011 :2185/:2189) — the
   abstract routes ARE the named Gate-5 obligation, unbuilt.
2. The only written certification route (R-L2b) is scale/GR-bearing and refuted-as-
   written with no sealed replacement (ECO §4 :133-134; R4O §4 :102-140, :139) (§1).
3. `C` is typed scale-free and impossibility of a scale-free certification is NOT proven
   (§2) — so the verdict is GAP, not NO.
4. `G3` (write-chain datum) and `G2` (scalarization continuum, §3) are separately
   unbuilt; the three minimal objects and their load-bearing order are correctly named.
5. The build correctly ABSORBS the CHK correction: build §4.1 :238-241 states the
   post-limit `Phi_f^T C Phi_H = 0` branch is a NON-degenerate direct sum `R_flux ⊕ R_H`
   (both sectors respond, decoupled), not `R = R_sector ⊕ 0` — matching CHK :79-92,
   :263-265. It did not inherit the POSE's over-merge. Sound.

FLAGGED, disclosure-level only, neither flips SOUND:
- (D1) ASYMMETRIC CITATION OF RFA. The build cites RFA §2 (finiteness is UV, "does not
  care how the atom was cut up" — favorable to "NOT NO") but omits RFA §5 :98-102 (a
  scale is what makes the decay a single-number exponent — unfavorable). The verdict
  "genuinely open" is correct either way, but the build presents "NOT NO" as cleaner
  than the record warrants; the scale-entanglement suspicion (and the running-kappa_
  record fallback, ECO §4) should have been surfaced as the live opposite risk.
- (D2) UNNAMED COMMUTATOR SUB-ROUTE. The build's "only written route = R-L2b" is
  accurate but does not mention the ECO §5/RFA §6 commutator sub-route; it is scale-
  bearing and unbuilt, so nothing changes.

G1_BUILD_VERDICT = SOUND (with the two disclosure flags above).

---

## 6. IMPORT / MACHINERY AUDIT (MINE)

```text
No value of n, kappa, alpha, mu, C, beta, or any scale/spectrum computed, bounded,
  estimated, or compared. The cross-block decider was posed, never evaluated.
No scale/faithfulness/GR imported BY ME as authority. Scale-bearing sources (ECO/RFA/
  R4O/XSM) were read ONLY to classify them scale-bearing and to test the two horns —
  the fence itself; every refutation/confirmation rests on connection-only bedrock
  (V011's unbuilt Gate-5 obligation; the combinatorial-summability argument of §2).
No register/tracker/plan/road/ledger/lens read. Seals recomputed at path before reading.
No git action; output name probed before write.
```

---

## 7. FLAG BLOCK

```text
CONNECTION_ONLY_ROUTE_EXISTS = NO_GAP_CONFIRMED(no connection-only certification route
  was missed: every abstract route the attack names — linked-cluster/cumulant expansion,
  Duhamel/Dyson structural bound, combinatorial connected-graph summability, operator-
  norm locality — is co-referential with V011's Gate-5 "target-free locality bound /
  cellulation-independent thermodynamic limit" (:1262-1281, :1751-1752), sealed UNBUILT
  (connected_linked_cluster_density_proved = false :2185; cellulation_independence_proved
  = false :2189). The build's primary gap = exactly this missing connection-only cluster-
  summability sub-estimate is correct and exhaustive. Adjacent, non-rescuing: the ECO §5
  :189-201 / RFA §6 :132-136 commutator sub-route (||[C,P]||_2^2 = 2 sum sigma_i(1-sigma_i))
  is scale/metric-bearing (built on the sea covariance and diamond projections) and sealed
  "ROUTE, NOT A LEMMA" — the build omits it but it does not open a connection-only route.)

SCALE_FREE_CERT_IMPOSSIBLE = NO(genuinely open: cannot be proven on connection-only
  bedrock that C's intensivity requires a metric scale. The finiteness is a scale-
  INVARIANT degree -3 UV/cancellation phenomenon — "purchased entirely by cancellation in
  the difference ... NOT by |D|_4" (ECO §5 :164-179; RFA §2 :29-42), and intensivity is
  SUMMABILITY of C_jk over the combinatorial cell/incidence index, a scale-free predicate;
  cellulation-independence is invariance under combinatorial refinement. The scale-
  entanglement suspicion is real but explicitly unproven: RFA §5 :98-102 shows a single
  scale is what makes the decay a NUMBER (an exponent), and ECO §4 :133-141 names a
  scale-dependent RUNNING kappa_record as the fallback "NEW PRINCIPLE" — but ECO §1 :30-34
  "absence of proof that they decay is not proof that they do not." beta does NOT provably
  re-enter. The build's "NOT NO" is confirmed as a genuinely-open GAP, not scale-entangled.)

MU_G2 = CONFIRMED_CONTINUUM_UNBUILT(M06 is exhaustive at the algebraic scalarization level
  and leaves a CONTINUUM: normalized positive functionals on B = C(Y) <-> probability
  measures on the compact character space; the explicit family omega_t = (1-t)omega_H +
  t omega_epsilon survives normalization, U1 reality, one-cell C0, and inclusive identity
  (M06 §2-§4 :175-444); physical rho_pre placement UNEXECUTABLE => NO_VERDICT; SURVIVOR_
  COUNT = CONTINUUM_AT_EXECUTABLE_LEVEL, U2_STATE_PORT_INSTANTIATED = false | TYPE-U (§6
  :500-506). No missed constraint forces mu: positivity permits both single-sector states
  (w_0 w_ch = 0 => mu = 0, and mu=0 kills R_record,L in full — a degenerate FREE) and
  straddling states (mu != 0); the two endpoints omega_H (Haar, tau_0(U_lambda)=0 for
  lambda!=0) and omega_epsilon (trivial-character eval) straddle the sign structurally.
  mu is genuinely undetermined; the build is neither over- nor under-stated.)

PROVENANCE = CLEAN(NET = GAP reached through connection-only content only — FORM's typing
  of C, V011's unbuilt Gate-5 obligation, M03's source operator, M06's continuum. The
  scale/GR sources (ECO/RFA/R4O sea covariance, diamonds, |D|_3/|D|_4, L, spatial
  Schatten-2, alpha>=1; XSM ell_P/G_4/metric/beta) are consumed ONLY to classify-and-fence
  the only-written route, never as authority for a positive claim; no exponent/volume/beta/
  length imported. The cross-block was posed, not evaluated, so no scale decided it either
  way. The one use of RFA §2 to support "NOT NO" is suspect-only and non-importing.)

G1_BUILD_VERDICT = SOUND(the three named minimal unbuilt objects and their load-bearing
  order are correct; C is typed scale-free so impossibility is not proven => GAP not NO;
  the only written route is scale-bearing and refuted-as-written with no sealed replacement;
  G3 and G2 separately unbuilt; and the build correctly absorbs the CHK correction that the
  post-limit Phi_f^T C Phi_H = 0 branch is a non-degenerate R_flux ⊕ R_H, not R_sector ⊕ 0.
  TWO DISCLOSURE FLAGS, non-fatal: (D1) the build cites RFA §2 (favorable to "NOT NO") but
  omits RFA §5 :98-102 and the ECO §4 running-kappa fallback (the scale-entanglement
  opposite risk) — the verdict "genuinely open" survives but "NOT NO" is presented cleaner
  than the record warrants; (D2) the ECO §5 commutator sub-route is unnamed — scale-bearing
  and unbuilt, so immaterial. Neither flag flips the verdict.)

MACHINERY_USED_BY_ME = no(nothing computed, bounded, estimated, or compared; C, mu, alpha,
  n, kappa, beta and every scale/spectrum left symbolic; the cross-block decider posed, not
  evaluated; scale-bearing sources read only to classify-and-fence, not as authority; seals
  recomputed at path before reading; no register/tracker/plan/road/ledger/lens read; no git.)

alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false ; coupling_evaluation_authorized = false
REQUIRE_G1_CHECK_RESULT = SEALED.
```
