# STAGE 8 — WHAT WOULD AN INPUT FROM OUTSIDE THE FORM CLASS BE? IS A NON-ANALYTIC CERTIFICATE ADMISSIBLE, OR EVEN POSABLE? — O10SR V001

## BUILDER — CODENAME OUTSIDE-BUILD — COMMISSION O10SR — 2026-08-15

DETERMINATION ONLY. This artifact **PROPOSES NOTHING**, **ADOPTS NOTHING**, and
**AUTHORS NO CERTIFICATE**. Where it finds that a type would require a rule act
to become admissible it DISPLAYS which rule and which discipline governs; it
does not propose, recommend, draft, or perform one. No value, no number, no
measured-constant comparison.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: `/Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_OUTSIDE_FORM_CLASS_O10SR_V001.md` and its `.seal.sha256` sidecar — **both
probed ABSENT** at start of commission (`ls` exit 1) before any write.

---

## 0. VERDICT IN ONE LINE

```text
**MIXED — and the mix is sharply split by SITE, which is the finding.

THE TYPE MENU IS CONFIRMED ANALYTIC AT TWO SITES AND REFUTED AT THE THIRD.
  FP-1's consuming type is TRACE/HS.  FP-3's is HILBERT-SCHMIDT (R.3) with
  QUADRATIC-FORM admitted and OPERATOR-NORM excluded-but-named and BESOV
  named-then-dropped.  All five are analytic — CONFIRMED at bytes.
  BUT FP-2's demand is NOT analytic and never was.  R1/(JD-3) demands
  "incidence/integration/evaluation of s on the named k-cell e", a COCHAIN
  EVALUATION ON AN ORIENTED CELL, with orientation covariance and a
  contact-kernel containment among its four obligations.  The context handed
  to this commission — "every certification type in the menu at these sites is
  ANALYTIC" — is therefore CORRECT AT FP-1 AND FP-3 AND REFUTED AT FP-2, at
  the frozen spec's own bytes.  I report the refutation of my own brief.

SO THE POSABILITY ANSWER IS NOT UNIFORM:
  AT FP-1 and FP-3 — **UNPOSABLE**.  Not merely inadmissible.  Both demands are
  phrased as inequalities between NORMS of operators (||.||_2, ||.||_1,
  ||.||_op, |t[psi,psi]| <= 2c||h_0 psi||.||psi||).  A topological invariant is
  not a small quantity that fails the bound; it is not a RELATUM of the
  relation the site states.  There is no substitution instance of R.3 or of the
  F1 conversion into which a winding number, a holonomy class, or an index can
  be entered.  The demand's own grammar excludes the type before any rule does.
  DISPLAYED EXACTLY: the sites demand a MAJORANT; a topological invariant is not
  ordered against a majorant.
  AT FP-2 — **POSABLE, AND ALREADY POSED**.  The site's demand is stated in
  exactly the vocabulary a record-native certificate speaks.  What blocks FP-2
  is NOT type and NOT admissibility: it is H-1 (the datum is unformed; STOPPED
  at Joints A–D), H-2 (no common cell; R9-JII PENDING), and W-2b (MEMBER_FOUND
  = none).  None of the three is a type exclusion.

THE MATCH TEST FAILS, AND IT FAILS ON THE SECOND PROPERTY, NOT THE FIRST.
  The record's native inventory is REAL and is enumerated here at bytes: the
  oriented-cell cochain pairing with its reversal antisymmetry (CE-2a) and
  chain linearity (CE-2b); the contact kernel Ctc_N^sym subset ker (CE-2d); the
  Ward/cocycle identity and its closed-loop content; the incidence and
  orientation data of the k-cell complex; the rank/counting invariants (rank <=
  4n^3 on ran C_n; carrier volume counts); the coincidence-DEGREE quantities.
  STRICT LOCALIZATION: **PRESENT** — the cochain pairing is defined per cell
  and distinguishes a region from its complement by construction; this is
  exactly the property the analytic menu was DERIVED to lack (the admitted
  majorant is r-FLAT while the object's own localized majorant is r-GRADED).
  SCALE-FREEDOM: **ABSENT AT THE ONE TYPE THAT HAS LOCALIZATION.**  The record's
  own oriented-cell evaluation is NOT scale-free: its evaluation form CE-1
  carries `lambda_JD` — "EXPLICIT, SYMBOLIC, UNASSIGNED", pulled outside the
  line integral as a unit — and is gauge-dependent on the authored `sect_N`
  (H-3).  The types that ARE scale-free (orientation sign, degree, rank counts,
  contact-kernel membership) are the ones whose values live in {+1,-1}, Z, or
  {0}, and NONE of them is displayed of record as bounding A(0) or as entering
  any conversion.  SO: no single record-native type carries BOTH properties.
  The match is ABSENT, and its absence is displayed at the exact byte
  (`lambda_JD` unassigned; SK-10 anti-tuning standing).

WHICH RULE WOULD GOVERN, DISPLAY ONLY: at FP-3 the type restriction is RULE
  F3-a (E1 :1137-1144), whose exclusivity is provenance (iii) INHERITED
  CONVENTION NEVER RATIFIED, and whose repair is the PRINCIPAL'S act under the
  governing revision discipline (AUTH §6.4).  At FP-2 NO RULE EXCLUDES a
  record-native certificate — so no rule act is needed for admissibility there;
  what is needed is a member, a datum, and a cell, and CARRY-1 forbids this
  commission (and W2's) from supplying any of the three.

NOT REFUTED, carried in full: every negative here is ONE-SIDED.  No record-native
type is shown not to exist; no impossibility is created; FP-1/FP-2/FP-3 all
STAND; every witness STANDS; nothing is retired, adopted, authored, or
proposed.**
```

---
