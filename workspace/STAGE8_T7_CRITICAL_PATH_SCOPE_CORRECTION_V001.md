# Stage-8 T7 Critical-Path Scope Correction v001

Date: 2026-07-24

## Purpose

This append-only correction prevents two recently explored routes from being
used to discharge primitive Stage-8 T7 beyond what the sealed authority
supports.

No arithmetic result is deleted. The correction changes only the role that
those results may play in the proof.

## 1. Intrinsic-action envelope

The scalar causal-diamond disintegration

```text
dmu_D = w_T(t) dt dnu_t
```

and the algebraic identity

```text
w_T(t) [M(t)/V_3(t)] = rho_4 M(t)
```

are valid. They do not select the physical operator-valued action map.

In physical units,

```text
rho_4 = 24/(pi T_R^4),
w_T(t) = rho_4 V_3(t).
```

The proposed slice generator `tau_R rho_4 M(t)` has the wrong physical
dimension unless an additional spatial assembly is supplied. Supplying the
slice volume gives `rho_4 V_3(t)=w_T(t)`, returning the disclosed ER-A
coefficient rather than forcing ER-B. The proposed ER-B expression also does
not reproduce the sealed first-opening accumulated action as written.

Therefore
`STAGE8_T7_INTRINSIC_ACTION_ENVELOPE_ADJUDICATION_SPEC_V001.md` and its
executables establish scalar measure identities only. Their verdict
`INTRINSIC_ACTION_ENVELOPE_DERIVED_ER_B` is superseded for physical
operator selection.

The authoritative status remains:

```text
ER-A = disclosed ordinary-branch envelope premise;
ER-B = unexcluded alternative;
intrinsic_measure_selects_ER_A_or_ER_B = false.
```

## 2. CAR/Fock/Hermite calculations

The sealed BID authority states that no CAR determinant may enter the
primitive unitary-response gate. The Stage-8 battery asks primitive T7 for a
completed-record amplitude constructed from the finite Lorentzian incidence
parent, followed by its zero-free, connected-cluster, and Duhamel/Hessian
proofs.

Accordingly, the following calculations retain their exact mathematical and
numerical content:

```text
finite-Fock completed-record history expansion;
Gaussian signed-history reduction;
Hermite-Galerkin covariance construction;
pure-projector strong convergence;
exact finite-state ER-A/ER-B comparison;
independent high-precision verification.
```

They belong to the downstream complete-Q_spec continuum-provenance program.
They may not be supplied as primitive T7 closure, a primitive connected
amplitude, a primitive zero-free theorem, or a primitive linked-cluster
limit. In particular, a determinant identity or its numerical convergence
cannot replace the required finite-incidence response construction.

## 3. Restored primitive T7 path

The active path is:

```text
1. use the derived one-cell complex-linear completed-record amplitude;
2. use the derived closure-face-to-ready-root relay;
3. retain the sealed five-coordinate family resolution in the declared
   pure-charge causal branch;
4. construct the scalar connected amplitude directly from the actual finite
   Lorentzian incidence parent and its predeclared branch data;
5. prove a volume-uniform zero-free neighborhood;
6. prove the connected linked-cluster thermodynamic density;
7. prove equality of its intensive Hessian with the Duhamel covariance.
```

No source line, state, endpoint, normalization, or envelope may be selected
because it improves a response value.

## Fixed status

```text
intrinsic_action_envelope_selector_derived = false
ER_A_disclosed_branch_premise = true
ER_B_unexcluded_alternative = true
CAR_Fock_Hermite_lane_admissible_for_primitive_T7 = false
CAR_Fock_Hermite_downstream_results_retained = true
connected_primitive_amplitude_derived = false
volume_uniform_zero_free_neighborhood_proved = false
connected_linked_cluster_density_proved = false
Duhamel_intensive_Hessian_equality_proved = false
kappa_record_computed = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```
