# Locked Alpha Plan Dependency Repair v001

Date: 2026-07-24

## Reason for repair

The twelve-stage handoff placed the complete interacting source
pole/threshold before the complete charged specification `Q_spec`.
That order is cyclic. A physical charged pole or infraparticle threshold
depends on the same action, state, CTP contour, gauge/edge dressing,
environment, regulator, and matching prescription that `Q_spec` is supposed
to freeze.

Therefore:

```text
physical source threshold -> requires complete Q_spec
complete Q_spec -> includes source threshold definition and parent action
```

The threshold cannot be computed first and then used to choose the
specification.

## Repaired dependency order

1. Freeze provenance and premise status.
2. Resolve the absolute-scale gate or carry its scale orbit explicitly.
3. Construct one complete target-independent microscopic parent action.
4. Exhaust response-inequivalent parent-action competitors.
5. Execute the upstream BID carrier, Hilbert, and differential gates.
6. Execute the primitive response/durability gate.
7. Freeze complete `Q_spec`, including CTP, gauge/edge, environment,
   regulator, and matching data.
8. Derive the dressed source pole or infraparticle threshold from that
   frozen specification.
9. Derive the transverse response and Thomson map.
10. Freeze one unused structure-sensitive prediction.
11. Compute alpha once and submit the frozen result to adversarial review.

The unresolved absolute interval is carried as `T_R>0`. Every dimensionless
alpha-facing output must be invariant under

```text
T_R -> lambda T_R.
```

If it is not invariant, the alpha route remains blocked by the absolute-scale
gate. A numerical Planck-cell choice may not be inserted.

## Current next object

The immediate proof target is the complete parent action, not a dressed pole.
It must derive in one object:

```text
the source and its record-writing interaction;
the connected many-record/environment dynamics;
the gauge and gravitational couplings;
the CTP state and effects;
the closure/durability observable;
and the dimensional scaling law.
```

Only after that action survives a complete competitor audit may any physical
spectrum or response be evaluated.

## Status

```text
original_stage4_before_Q_spec_dependency_valid = false
dependency_cycle_identified = true
physical_threshold_moved_after_Q_spec_freeze = true
absolute_scale_chosen_numerically = false
scale_orbit_carried_symbolically = true
complete_parent_action_derived = false
alpha_computed = false
proof_authorized = false
```
