# STAGE 8 — O-1-S OF RECORD: THE SIGN-CROSSING QUESTION S_n — THE POLAR RESOLUTION, THE SPECTRAL COUNT LAW, THE ANGULAR-EXCESS IDENTITY, THE DEEPER WITNESS, AND THE VERDICT — T10SR V001

## BLIND BUILDER — CODENAME SN-BUILD — COMMISSION T10SR — [CLAIMED]

Date: 2026-08-15 (session CDT). Role: BLIND builder (SN-BUILD).
Commission: THE QUESTION, exact — O-1-S, the sign-crossing quantity
S_n := ||P^perp sgn_n P||_2 defined at the sealed ceiling pair's P-3
display (T7SR). Certify EITHER (a) THE CEILING S_n^2 <= C' n^{3/2 -
gamma'} for an explicit gamma' > 0 — which yields O-1 at gamma =
min(gamma', 1/12) through the sealed P-2/P-3 displays and decides
MO-4-R o(1) at rate through the sealed T-4 display; OR (b) THE FLOOR
S_n^2 >= c' n^{3/2} — which yields CEILING-FAILS-AT-3/2 (the O-2
direction's evidence). Verdict menu as commissioned: CEILING-DERIVED /
FLOOR-DERIVED / POLYLOG-ONLY / UNDECIDABLE-TODAY. HONESTY RAILS held:
a polylog-only gain does NOT decide (the audited c-1/c-A displays,
consumed at their exact statements); no crossing law is imported — the
one my ceiling route would need is DISPLAYED AS THE BLOCKER, not
assumed. "Q-..." tokens inside sealed artifacts are
EXPECTED-UNLOCATABLE by design; noted, never chased.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` (kappa_n is the clause's collapse
commutator mass, NOT the record curvature; no record-curvature value
appears anywhere below). ALL_RESULTS = CLAIMED until checked.
DETERMINATION ONLY — no adoption, no authored physics, no member
binding; the registrar consumes.

Fences held: EXACT SYMBOLIC DERIVATION ONLY — one CAS battery (sympy
1.14.0, fresh venv `signvenv` under the session scratchpad),
reproduced verbatim in §11 (ONE pre-final check-form correction
disclosed in §11's header: SN2's Hermiticity conjunct originally
tested U(p).H - U(p) directly and sympy's assumptions engine leaves
conjugate(sqrt(p_x^2+p_y^2+p_z^2)) unevaluated — the conjunct was
rewritten to test Hermiticity of the numerator (p.alpha) with the
positive real scalar |p| carried in the check NAME; no mathematical
content changed, no derivation step changed; the corrected battery
then ran twice with BYTE-IDENTICAL output, 10/10 PASS); every
constant symbolic or an exact rational/closed form; NOTHING numeric
evaluated; no floats as ground; no measured constant; NO value frozen
(kappa_n, J_n, S_n, C_*, C_Q, C', c', gamma', all thresholds stay
symbols or displayed closed forms); no file matching register|road_|
ledger|lens|plan|tracker|THE_HANDOFF|continuation opened
(filename-level listing only); no git action; no existing file
edited; ONE output (this artifact) plus its seal sidecar at the
commission-distinct path, probed ABSENT at session start AND
re-probed immediately before write. Marks: DERIVED / CLAIMED /
CLASSICAL(cited) / CONDITIONAL(premise named) span by span.

SWEEP CUTOFF, DECLARED: the corpus sweep of §9 was executed against
the tree state as of **2026-08-15 06:47:00 CDT**. Artifacts sealed
after that instant are not consumed and not swept. O-1-S/T10SR
occupation at the cutoff: NONE (§9 sw-1); the commission id T10SR and
the token SIGN_CROSSING appear nowhere in either root.

---

## 0. VERDICT IN ONE LINE

**UNDECIDABLE-TODAY for O-1-S as commissioned — neither the ceiling
S_n^2 <= C' n^{3/2-gamma'} nor the floor S_n^2 >= c' n^{3/2} is
derivable on today's stock, and the undecidability is FORCED one
level DEEPER than the sealed C12/c-A forcing: the polar datum of H_n
is NOT free (SP-1 resolves it exactly from the sealed tensor bytes),
but the ANGULAR datum still is (the W12' witness pair: two exact
models of the resolved polar form with IDENTICAL spectrum, chirality
data, carrier, ball-overlap Gram data, and first moment realize
S^2 = J^2 + 8/49 AND S^2 = J^2 + 16/49 — the angle assignment of the
overlap mass is free at the displayed byte grade). What IS derived
(new, exact, three partials): (SP-1) THE POLAR RESOLUTION — the
sealed commuting tensor structure of H_n = sum_j pi_j alpha_j
diagonalizes exactly: sgn_n = sum_{p != 0} Pi_p tensor (p.alpha)/|p|
over the joint momentum grid p in (Z_n/ell)^3, Z_n = the zeros of
phi_n (the Hermite function): the sign of the compressed Dirac
operator — the object sw-2 of record says has no law — HAS an exact
closed form at the family's own bytes; the blocker of record moves.
(SP-2) THE SPECTRAL LAW AND THE COUNT CEILING — spec(H_n|ran Q_n) =
{ +-|p| : p in grid }, each multiplicity 2, kernel = 4(n mod 2)
exactly as sealed; with ONE new classical citation CL-C (Sturm
comparison on the Hermite ODE u'' + (2n+1 - t^2)u = 0: consecutive
zeros of phi_n are separated by >= pi/sqrt(2n+1)) the low-energy
count law N_n(E) <= 4 (1 + 2 E ell sqrt(2n+1)/pi)^3 is EXACT and
n-uniform: BLOCKER-2's named absent object (the H_n low-energy
density law) is SUPPLIED — and §7 shows exactly why it alone still
does not close the DOI route (the t-integral is entrywise TIGHT:
Int_0^oo (f_t(lambda_i) - f_t(lambda_j)) dt = (pi/2)(sgn lambda_i -
sgn lambda_j) — no per-t norm assembly consuming only spectrum/count
data can beat the trivial bound; the missing ingredient is the
overlap localization, not the density). (SP-3) THE ANGULAR-EXCESS
IDENTITY — exact and two-sided: S_n^2 = J_n^2 + (1/2)||Q_n [P,
sgn_n] Q_n||_2^2 - ||Q_n^perp P Z_n||_2^2, with the last term in
[0, 4] and vanishing for n even: hence (i) THE ORDERING S_n^2 >=
J_n^2 - 4 — the sign crossing DOMINATES the carrier crossing, so
S_n^2 inherits the sealed linear floor S_n^2 >= C_*(r, ell) n - 4
(the first certified floor for the sign crossing; O-2-direction
evidence at LINEAR grade, not the commissioned 3/2); (ii) the excess
over J_n^2 is EXACTLY the compressed sign-ball commutator mass, and
in grid coordinates it is 4 sum_{p != p'} |B_pp'|^2 (1 - cos
angle(p, p')) with B_pp' := <psi_p, 1_B psi_p'> the ball-overlap
matrix: THE QUESTION IS EXACTLY THE ANGULAR LOCALIZATION OF THE
OVERLAP MASS. The blockers, swept and exact: (i) no sealed or
classical-grade object controls the off-diagonal decay of B_pp' —
CL-A is fixed-compact (audited CL-3 scope), CL-B is a global sup
only, and evaluating psi_p on the ball needs phi_k AT THE ZEROS
(argument ~ sqrt(2n): the elliptic/oscillatory full-range
Plancherel-Rotach regime, Szego §8.22.9 territory — ZERO carriers in
either root, sw-3); (ii) the W12' freedom: spectrum + count law +
Gram moduli underdetermine S_n^2 — any route consuming only them is
refuted by the pair. THE NEW SEALED OBJECT THAT DECIDES: O-1-S-B
(the overlap law): a certified angular-localization ceiling
sum_{p != p'} |B_pp'|^2 (1 - cos angle) <= C_B'' n^{3/2 - gamma_B}
— yields O-1-S at gamma' = min(gamma_B, 1/12) through SP-3 + sealed
P-2, hence O-1 and DECIDED-o(1) for MO-4-R at rate; equally an
anti-localization floor (large-angle overlap mass >= c n^{3/2})
yields the FLOOR and CEILING-FAILS-AT-3/2. Nothing fires: MO-4-R
stays open; no flag of any prior artifact flips; the registrar
consumes. CAS battery 10/10 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
file consumed at any grade this session was seal-verified by FULL
digest against its sidecar BEFORE consumption (shasum -a 256 -c, each
`OK`). 8/8 MATCH.

```text
T-1 STAGE8_CLOCK_CEILING_T7SR_V001.md            OK — THE GROUND OF
    THE QUESTION, read IN FULL: the O-1-S spec (§8 there: certified
    S_n^2 <= C' n^{3/2-gamma'} yields O-1 at min(gamma', 1/12); floor
    S_n^2 >= c' n^{3/2} yields CEILING-FAILS-AT-3/2); P-1 (kappa_n <=
    C_B n^{3/2} + 4); P-2 (J_n^2 <= C_Q n^{17/12}, gamma_Q = 1/12,
    given CL-A + CL-B); P-3 (C_n = (Q_n - sgn_n)/2 + E_n, ||E_n||_2
    <= 1; the C11 two-sided algebra; sgn_n := 1_{(0,inf)}(H_n) -
    1_{(-inf,0)}(H_n) on ran Q_n, sgn_n^2 = Q_n - Z_n; S_n :=
    ||P^perp sgn_n P||_2); the exact blockers (no sgn(H_n) crossing
    law sw-2; no low-energy density law, C13 divergence; Widom
    inapplicable, audited CL-4); the C12 witness pair and its §6
    consequence; RT-A..RT-E refusals.
T-2 STAGE8_CLOCK_CEILING_T7SR_AUDIT_V001.md      OK — CONFIRMED-WITH-
    CORRECTIONS, read IN FULL: the AB bridges (AB5 split-optimality;
    AB6 class maximum kappa(U) = (|U_12|^2 + |U_21|^2)/2; AB7 the
    padding bridge — the c-A repair of the C12 quantifier; AB8/AB9
    the sign-reduction SOS both directions; AB11 the global Lipschitz
    constant; AB15/AB16 the yield transfers); o-3's scope note
    consumed at bytes ("byte-grade routes consuming the H_n
    realization more deeply are SWEPT ... not exhausted in
    principle" — THIS commission walks through exactly that door).
T-3 STAGE8_MO4R_GRAM_MASS_T3SR_V001.md           OK — the reduction:
    T-4 display F_n(eps) <= 2 kappa_n/((1-4 eps^2) c_G n^{3/2}); the
    O-1 spec (§6); the R7 scope display (RT-E: the far class alone
    overshoots the linear clock by sqrt(n)); read at the reduction
    and R7 scope.
T-4 STAGE8_MO4R_GRAM_MASS_T3SR_AUDIT_V001.md     OK — CONFIRMED-WITH-
    CORRECTIONS; c-1 consumed at its exact repair (a polylog-only
    gain does NOT decide O-1 as specified — POWER gamma demanded).
T-5 STAGE8_MO2_KAPPA_RATE_S9AD_V001.md           OK — THE TWIN'S
    TECHNIQUE, read at bytes: the C_n realization (C_n =
    1_{(-inf,0)}(H_n) + z_n, H_n = Q_n h_0 Q_n on ran Q_n, 0 <= z_n
    <= Z_n); FACT TWO (beta bookkeeping; dim ker(H_n|ran Q_n) =
    4(n mod 2) <= 4); THE TENSOR BYTES this commission consumes:
    H_n = sum_j pi_j alpha_j with the three compressed momenta in
    DIFFERENT tensor factors (exactly commuting, M5 there); the
    sealed tridiagonal q_n p q_n (<k|p|k+1> = -i sqrt((k+1)/2)/ell,
    zero diagonal, M3 there); Q_n = q_n^{x3} tensor 1_4, rank 4n^3;
    P = sharp ball r = min(t,1-t) > 0, ell in {1, sqrt2}; CL-A's
    sealed statement; the floors ||P^perp Q_n P||_2^2 >= C_*(r,ell) n
    and kappa_n >= C_* n/8; the exact scaling k_n^ell.
T-6 STAGE8_MO2_KAPPA_RATE_S9AD_AUDIT_V001.md     OK — CONFIRMED-WITH-
    CORRECTIONS; CL-2 (Szego 8.22.6 transcription exact); CL-3
    (CL-A's certified scope is FIXED-COMPACT only — "no turning-point
    or Airy regime"); CL-4 (Widom/Sobolev-class covers scaled
    pseudodifferential compressions, not Galerkin spectral
    projections — the trap note, re-consumed at bytes here).
T-7 STAGE8_MO4_CANCELLATION_S9AD_V001.md         OK — the Gram
    ground: the CL-A diagonal CEILING k_n(t,t) <= M_{K0} + (8/pi)
    sqrt(2n) on the fixed compact (§4.3); G_n two-sided c_G n^{3/2}
    <= G_n <= C_G n^{3/2}; the crossing identity and MO-4-R's naming
    (§4.5); read at the Gram ground.
T-8 STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md   OK — CONFIRMED-WITH-
    CORRECTIONS (c-1..c-5 cosmetic; A-3 adjudicates the diagonal
    ceiling FAITHFUL); consumed at flag/adjudication grade.
```

Consumption through sealed quotes: every r-2/r-3/PA byte relied on
below enters through T-5/T-7 (their §1 tables pin 3b5e95b6, bebc0f08,
789338ad and the PA displays) and through T-1/T-2 at audited grade.
No other file opened. No fenced-class file opened.

---

## 2. THE QUESTION AND THE OBJECTS, AT BYTES

```text
O-1-S (the commissioned object, T-1 §8 verbatim shape): a certified
    S_n^2 = ||P^perp sgn(H_n) P||_2^2 <= C' n^{3/2 - gamma'},
    gamma' > 0, both symbolic (THE CEILING); or a certified
    S_n^2 >= c' n^{3/2} (THE FLOOR). YIELD once sealed: the ceiling
    gives O-1 at gamma = min(gamma', 1/12) via kappa_n <= (3/2)
    J_n^2 + (3/2) S_n^2 + 6 (sealed P-3) and J_n^2 <= C_Q n^{17/12}
    (sealed P-2), then DECIDED-o(1) for MO-4-R at rate n^{-gamma}
    via the sealed T-4 display; the floor gives kappa_n >= c''
    n^{3/2} cofinitely (sealed §5 floor-side display) =
    CEILING-FAILS-AT-3/2.
sgn_n := 1_{(0,inf)}(H_n) - 1_{(-inf,0)}(H_n) on ran Q_n (kernel
    excluded); sgn_n^2 = Q_n - Z_n, Z_n := 1_{{0}}(H_n|ran Q_n),
    rank Z_n = 4 (n mod 2) <= 4; beta sgn_n beta = -sgn_n.
    [T-1 §2 / T-5 FACT TWO at audited grade]
H_n = Q_n h_0 Q_n on ran Q_n; h_0 = sum_j p_j alpha_j MASSLESS;
    H_n = sum_j pi_j alpha_j, pi_x = (q_n p q_n) tensor q_n tensor
    q_n (and cyclically), THE THREE pi_j COMMUTE EXACTLY (different
    tensor factors); q_n p q_n = the Hermitian tridiagonal with
    <k|p|k+1> = -i sqrt((k+1)/2)/ell, zero diagonal. [T-5 §2 at
    audited grade — the tensor bytes this commission consumes]
Q_n = q_n tensor q_n tensor q_n tensor 1_4 (index CUBE => exact
    product projector), rank 4n^3; P = multiplication by
    1_{|x| <= r}, spinor-diagonal, r = r(t) = min(t, 1-t) > 0; ell
    frozen in {1, sqrt2}. [T-5 bytes]
J_n := ||P^perp Q_n P||_2 (the carrier crossing): SEALED TWO-SIDED,
    C_*(r, ell) n <= J_n^2 <= C_Q n^{17/12} (floor T-5 §4.4; ceiling
    T-1 P-2 given CL-A + CL-B).
CL-A (sealed classical ground, T-5 §3 verbatim): phi_k(t) =
    A_k [cos(theta_k(t)) + eps_k(t)], A_k = (2/pi)^{1/2}(2k)^{-1/4},
    theta_k = omega_k t - k pi/2, omega_k = sqrt(2k+1),
    sup_{|t| <= T} |eps_k(t)| <= e_k(T) -> 0 (fixed compacts ONLY,
    audited CL-3).
CL-B (sealed at T-1 §4.1): sup_R |phi_m| <= C_s m^{-1/12} (global
    sup only).
NEW OBJECTS NAMED HERE: the joint momentum grid G_n := {(z_a, z_b,
    z_c)/ell : z_* zeros of phi_n} (SP-1/SP-2); psi_p := the joint
    eigenvector of (pi_x, pi_y, pi_z) at p (product of 1D
    compressed-momentum eigenvectors); B_pp' := <psi_p, 1_B psi_p'>
    (the ball-overlap matrix, 0 <= B <= 1, spatial n^3 x n^3);
    the angular excess X_n := S_n^2 - J_n^2.
```

---

## 3. SP-1 — THE POLAR RESOLUTION (DERIVED, exact, from the sealed tensor bytes)

The door this walks through is the audited o-3 reservation (T-2): the
C12/c-A forcing refutes routes quantified over the DISPLAYED
hypothesis class; routes consuming the H_n realization bytes more
deeply were swept, not exhausted. This section consumes exactly those
bytes (T-5 §2, audited grade) and nothing else.

### 3.1 The joint eigenbasis (exact)

```text
STEP 1 (simplicity of the 1D spectrum; DERIVED). q_n p q_n is
  tridiagonal with nonzero off-diagonal entries -i sqrt((k+1)/2)/ell
  (sealed bytes). For ANY lambda, deleting the last row and first
  column of (q_n p q_n - lambda) leaves an upper-triangular matrix
  with diagonal (-i a_0/ell, ..., -i a_{n-2}/ell), a_k :=
  sqrt((k+1)/2), determinant != 0: rank(q_n p q_n - lambda) >= n-1,
  so EVERY eigenvalue is simple. [CAS SN7 minor exhibit; the
  argument is the display]
STEP 2 (the spectrum is the Hermite zero set; DERIVED + CL-classical
  recurrence). With D := diag(i^k) (unitary), D (q_n p q_n) D^{-1} =
  -(1/ell) J_x, J_x := the real symmetric tridiagonal with
  off-diagonal a_k — the Jacobi matrix of the Hermite three-term
  recurrence t phi_k = a_k phi_{k+1} + a_{k-1} phi_{k-1} (classical
  recurrence; CAS SN7 verifies members and the conjugation exactly).
  For v(lambda) := (phi_0(lambda), ..., phi_{n-1}(lambda)):
    J_x v(lambda) = lambda v(lambda) - a_{n-1} phi_n(lambda) e_{n-1}
  (exact, the recurrence row by row), and phi_0 never vanishes, so
    spec(J_x) = { z : phi_n(z) = 0 } =: Z_n^{(1)},
  the n simple zeros of the Hermite function phi_n (= zeros of the
  Hermite polynomial H_n), a set SYMMETRIC about 0 (phi_n has parity
  (-1)^n), containing 0 iff n is odd. Hence
    spec(q_n p q_n) = -Z_n^{(1)}/ell = Z_n^{(1)}/ell.
  [CAS SN7: char poly of J_3 = -(lambda^3 - (3/2) lambda), roots
  exactly the zeros of H_3; conjugation exact; consistent with the
  SEALED nullity 4(n mod 2) — 0 is a zero of phi_n iff n odd]
STEP 3 (the joint grid; DERIVED). The three pi_j commute exactly
  (sealed: different tensor factors) and each is q_n p q_n in its own
  factor: the joint spectrum is the CUBE grid
    G_n := { p = (z_a, z_b, z_c)/ell : z_* in Z_n^{(1)} },  |G_n| = n^3,
  every joint eigenvalue simple in the spatial factor, with joint
  eigenvectors the products psi_p := chi_{p_1} tensor chi_{p_2}
  tensor chi_{p_3} (chi the 1D eigenvectors) — an ONB of the spatial
  carrier. Pi_p := |psi_p><psi_p|. [DERIVED; CAS SN3 exhibits the
  full n = 2 grid]
```

### 3.2 The resolution (exact blockwise functional calculus)

On the joint eigenspace at p, H_n acts on the spinor factor as the
NUMERICAL Dirac matrix p.alpha with (p.alpha)^2 = |p|^2 1_4 (CAS
SN1). Blockwise functional calculus of the commuting decomposition
H_n = sum_p Pi_p tensor (p.alpha) gives, for p != 0, the spectral
projections (1_4 -+ (p.alpha)/|p|)/2 at eigenvalues -+|p| (CAS SN2:
(1-U)/2 is exactly the negative-energy projection), hence EXACTLY:

```text
  sgn_n = sum_{p in G_n, p != 0} Pi_p tensor (p.alpha)/|p| ,
  Z_n   = [ Pi_0 tensor 1_4  if 0 in G_n (iff n odd), else 0 ],
  spec(H_n|ran Q_n) = { -+|p| : p in G_n }, each multiplicity 2
  (the two chirality branches of p.alpha), kernel rank 4 (n mod 2).
  CONSISTENCY: rank check sum_p 4 = 4n^3 = rank Q_n (sealed); kernel
  matches the sealed 4(n mod 2) EXACTLY (0 in G_n iff some grid
  point has ALL THREE coordinates 0 iff n odd); beta sgn_n beta =
  -sgn_n holds termwise (beta (p.alpha) beta = -(p.alpha), SN1).
```

[CAS SN3: at n = 2 the constructed sum U_c is Hermitian, U_c^2 = 1,
[U_c, H] = 0, and U_c H = sqrt(3/2)/ell > 0 times the identity —
U_c = sgn(H) exactly.] THE POINT: sw-2 of record ("no sign-crossing
law, kernel law, or polar-part control exists anywhere in either
root") is now MOVED — the polar part of H_n has a CLOSED FORM at the
family's own bytes. What the closed form does NOT by itself supply is
the BALL data of the eigenvectors (§5-§7): the crossing question
becomes a question about the overlap matrix, not about the polar
datum.

---

## 4. SP-2 — THE SPECTRAL LAW AND THE COUNT CEILING (DERIVED given CL-C; supplies BLOCKER-2's named object)

### 4.1 CL-C, the one new classical citation (disclosure)

```text
CL-C (Sturm comparison; classical in the strictest sense — Sturm
1836; standard ODE ground): if u'' + q(t) u = 0 with 0 < q(t) <= Q
on an interval, consecutive zeros of a nontrivial solution are
separated by AT LEAST pi/sqrt(Q).
```

Discipline: IDENTICAL to CL-A/CL-B's (cited as classical, never
re-derived; every consequence below derived exactly from the cited
statement). Applied to THE HERMITE ODE phi_n'' + (2n+1 - t^2) phi_n
= 0 (classical equation; CAS SN8 verifies members k = 0..4 exactly)
with Q = 2n+1: consecutive zeros of phi_n are separated by >=
pi/sqrt(2n+1). Registrar note (the audited COR-3/CH-1 pattern): if
CL-C is ruled outside the authorized class, §4.2 downgrades to
CONDITIONAL(CL-C); the VERDICT is unchanged (no closure consumes
CL-C — §7's stall display shows the count law does not decide).

### 4.2 The count ceiling (exact, n-uniform)

```text
1D: m points in [-a, a] with consecutive gaps >= s satisfy
    (m-1) s <= 2a, i.e. m <= 1 + 2a/s (exact rearrangement, SN9):
    #{ z in Z_n^{(1)} : |z| <= a } <= 1 + 2 a sqrt(2n+1)/pi.
3D + Dirac: |p| <= E forces every |p_j| <= E; the per-axis counts
    multiply (SN9 product bound), and each grid point carries 4
    states (+-|p| twice each):
      N_n(E) := #{ eigenvalues of H_n|ran Q_n in [-E, E] }
             <= 4 ( 1 + 2 E ell sqrt(2n+1) / pi )^3 .     [DERIVED
    given CL-C; exact, n-uniform, every constant displayed]
```

BLOCKER-2 of record (T-1: "closing [the smooth-resolvent route]
REQUIRES a low-energy spectral density law for H_n ... none is
sealed") names exactly this object. It is now SUPPLIED at exact
grade. §7 displays exactly why it does NOT by itself decide O-1-S:
the missing ingredient after SP-2 is overlap localization, not
density. (A matching FLOOR — spacing <= pi/sqrt(2n+1-a^2) inside
[-a, a] by the same comparison from below — is derivable the same
way; displayed as available, consumed nowhere.)

---

## 5. SP-3 — THE ANGULAR-EXCESS IDENTITY (DERIVED, exact)

### 5.1 The identity

Write u := sgn_n, Q := Q_n, Z := Z_n, so u^2 = Q - Z, uQ = Qu = u,
ZQ = QZ = Z, and P the sharp ball. All traces finite (finite rank).

```text
STEP 1 (two exact expansions).
  S_n^2 = tr(P u P^perp u P) = tr(P u^2 P) - tr(PuPu)
        = tr(PQP) - tr(PZP) - tr(PuPu);
  J_n^2 = tr(P Q P^perp Q P) = tr(PQP) - tr(PQPQ).
  Hence X_n := S_n^2 - J_n^2 = tr(PQPQ) - tr(PuPu) - tr(PZP).
STEP 2 (the compressed commutator). K := Q [P, u] Q = QPu - uPQ
  (uQ = Qu = u), K^dag = -K, and expanding -tr(K^2) with the SAME
  reductions (tr(QPuQPu) = tr(PuPu) etc., each a cyclicity + uQ = u
  step):
    (1/2) ||Q [P, u] Q||_2^2 = tr(PQPQ) - tr(PuPu) - tr(Z P Q P).
STEP 3 (the correction is one-signed). tr(ZPQP) - tr(PZP) =
  tr(Z P (Q - 1) P Z) = -||Q^perp P Z||_2^2 (ZQ = Z), so EXACTLY:

    S_n^2 = J_n^2 + (1/2) ||Q_n [P, sgn_n] Q_n||_2^2
                  - ||Q_n^perp P Z_n||_2^2 ,
    0 <= ||Q_n^perp P Z_n||_2^2 <= rank Z_n <= 4,  = 0 for n even.

  [CAS SN4 (kernel-free instance, exact), SN5 (kernel instance with
  the one-signed correction, exact), SN6 (re-verified on both
  witness models)]
```

### 5.2 Grid coordinates: the excess is the angular sum

In the SP-1 basis, Q_n P Q_n has matrix B tensor 1_4 with B_pp' :=
<psi_p, 1_B psi_p'> (0 <= B <= 1), and u = diag_p (p.alpha)/|p|.
Expanding the traces of Step 1 blockwise (tr_4[(p.alpha)(p'.alpha)] =
4 p.p', CAS SN1/SN2):

```text
  J_n^2 = 4 [ tr B - tr B^2 ]   (the sealed unfold, re-derived);
  X_n + ||Q^perp P Z||_2^2 = 4 sum_{p != p'} |B_pp'|^2
                                 (1 - cos angle(p, p'))  >= 0 .
```

THE QUESTION IS EXACTLY THE ANGULAR LOCALIZATION OF THE OVERLAP
MASS: S_n^2 - J_n^2 measures how much of the off-diagonal overlap
mass |B_pp'|^2 sits at momentum pairs with a nonzero angle.

### 5.3 Corollaries (each exact)

```text
CO-1 THE ORDERING: S_n^2 >= J_n^2 - 4 (excess >= 0); with the sealed
  floor J_n^2 >= C_*(r, ell) n (n >= max(n_1, n_2)):
    S_n^2 >= C_*(r, ell) n - 4 >= (C_*/2) n  for n >= max(n_1, n_2,
    8/C_*)   [SN10]
  — the FIRST certified floor for the sign crossing (linear grade;
  the commissioned floor is 3/2 and is NOT reached — displayed, not
  decided).
CO-2 THE TOTAL CEILING RECOVERED: sum_{p != p'} |B_pp'|^2 (1 - cos)
  <= 2 tr B^2 <= 2 tr B = tr(P Q_n)/2, so S_n^2 <= J_n^2 + 4 tr(PQ_n)
  + 4 — the exact-3/2 bulk again (consistent with sealed P-1 + P-3;
  NO sub-3/2 gain from totals alone: the c-1 discipline).
CO-3 FIRST-MOMENT VANISHING: tr(P sgn_n) = 0 exactly (beta-oddness:
  tr(P sgn_n) = tr(beta P sgn_n beta) = -tr(P sgn_n); blockwise:
  tr_4(p.alpha) = 0, SN1) — the sign carries NO first moment; the
  sign question is second-moment from the start (the exact reason
  the beta first-moment twin of record cannot see it).
CO-4 THE COMPRESSION DISPLAY: (1/2)||Q_n[P, sgn_n]Q_n||_2^2 =
  S_n^2 - J_n^2 + O(4) <= S_n^2 <= (1/2)||[P, sgn_n]||_2^2 = S_n^2:
  the excess is the COMPRESSED part of the sign-ball commutator; the
  uncompressed identity ||[P, sgn_n]||_2^2 = 2 S_n^2 (block identity,
  sealed C1 pattern) closes the circle — every display consistent.
```

---

## 6. THE DEEPER WITNESS W12' — UNDECIDABILITY IS FORCED AT THE RESOLVED BYTE GRADE

CAS SN6, exact: two 12-dim models (spatial C^3 tensor spinor C^4),
BOTH of the SP-1 resolved form sum_p Pi_p tensor (p.alpha): carrier
Q = diag(1,1,0) tensor 1_4; ball P = v v^T tensor 1_4, v = (1,2,3)/
sqrt(14) (exact rational projection); grid points p_1 = (rho, 0, 0)
in both; p_2 = (0, rho, 0) in model A (ORTHOGONAL), p_2 = (-rho, 0,
0) in model B (ANTIPODAL); rho symbolic positive.

```text
  IDENTICAL between A and B: the resolved polar FORM (both are
    exactly sum Pi_p tensor p.alpha — SP-1 satisfied by both); the
    spectrum (H^2 = rho^2 Q and tr H = 0 both: eigenvalues +-rho,
    four each, plus the Q^perp zeros — hence the SAME count law
    N(E)); the chirality data ({beta, H} = 0 both, [beta, P] = 0);
    the kernel (0 on ran Q both); the carrier and ALL ball-overlap
    Gram data (same P, same B matrix, same |B_12|^2 = 1/49, same
    J^2, tr B, tr B^2); the first moment tr(P C) (equal; C = (Q-u)/2
    an exact projection in both).
  DIFFERENT: S_A^2 = J^2 + 8/49 (angle pi/2: excess = 4 * 2 *
    (1/49) * 1), S_B^2 = J^2 + 16/49 (angle pi: excess = 4 * 2 *
    (1/49) * 2). Separation S_B^2 - S_A^2 = 8/49 EXACT. The SP-3
    identity re-verified on both models.
```

Consequence: no identity-grade route quantified over the hypothesis
class my lawful ground NOW displays — {the SP-1 resolved polar form;
the full spectrum and its count law (SP-2); the chirality and kernel
bookkeeping; the ball-overlap Gram MODULI (B diagonal, |B_pp'|,
tr B, tr B^2, J_n^2); the sealed floors} — can derive EITHER
direction of O-1-S: the ANGULAR ASSIGNMENT of the overlap mass
(which momentum pairs carry it) is a FREE parameter of that class,
and it alone moves S^2 at fixed everything-else. Deciding O-1-S
REQUIRES family-specific control of the overlap matrix's angular
distribution — the O-1-S-B object of §8. The C12/c-A forcing of
record is thereby SHARPENED one level: C12 showed the polar datum
free at the displayed-hypothesis grade; SP-1 RESOLVES the polar
datum from the bytes; W12' shows the freedom reappears exactly one
level deeper, in the angular datum. QUANTIFIER NOTE (the c-A
discipline, pre-empted): the fixed 12-dim pair under-carries
n-indexed thresholded hypotheses for the same reason the audited c-A
named; the SAME padding bridge repairs it — direct-sum the record
family with m_n = ceil(n^{3/2}) copies of toy A versus toy B (HS^2
of block commutators and all listed data are exactly additive, the
audited AB7 display, consumed at audited grade; the toys contribute
identical listed data in both variants) — every listed datum
including the floors is identical between variants while S^2
separates by m_n * 8/49 >= n^{3/2}: no sub-3/2 ceiling AND no
super-linear floor quantified over the listed class survives the
pair. TOY STATUS: the pair refutes derivability claims only; it
asserts NOTHING about the record family's own angular distribution,
which stays undecided in both directions.

---

## 7. THE ROUTES, SWEPT — EACH REFUSED AT ITS OWN EXACT DISPLAY

```text
RT-A' THE DOI ROUTE WITH THE DENSITY LAW (the route BLOCKER-2 said
   the density law would open — attempted with SP-2 in hand).
   sgn = (2/pi) Int_0^oo f_t(H_n) dt, f_t(lambda) = lambda/(lambda^2
   + t^2), on ker^perp (exact; the sealed C13/AB11 grounds). In the
   H_n eigenbasis the commutator entry is (f_t(lambda_i) -
   f_t(lambda_j)) P_ij, and the t-integral of the ENTRY is EXACT:
     Int_0^oo (f_t(lambda_i) - f_t(lambda_j)) dt
       = (pi/2)(sgn lambda_i - sgn lambda_j)        [CAS SN9]
   — the representation is entrywise TIGHT: summed against |P_ij|^2
   it returns exactly 2 S_n^2, and ANY per-t norm assembly
   (Minkowski + any bounds consuming only the spectrum, the SP-2
   count law, and rotation-invariant totals like tr B, tr B^2)
   either reproduces the trivial total (CO-2's exact-3/2) or is
   refuted by W12' (two models with IDENTICAL spectrum/count/totals
   and different S^2: no such assembly can output a deciding bound).
   The density law CURES the small-t divergence of record (C13) and
   STILL does not decide: the missing ingredient is the
   LOCALIZATION of the Schur weights ||E_I P E_J||_2^2 over spectral
   windows — unlocated at the cutoff (sw-3). REFUSED at the
   tightness display + the witness.
RT-B' KERNEL/FAR-NEAR ROUTES (the P-2 twin for sgn_n). The far/near
   split needs pointwise control of the SIGN kernel sgn_n(x, y) =
   sum_p psi_p(x) psi_p(y)^dag tensor (p.alpha)/|p| on B x B^c —
   i.e. the eigenvector values ON THE BALL. chi_{p_j} = (twisted CD
   column) sum_{k<n} i^k phi_k(z_j) phi_k(t) / sqrt(k_n(z_j, z_j)):
   the coefficients are phi_k AT THE ZEROS z_j, |z_j| up to
   sqrt(2n+1) — OUTSIDE every fixed compact: CL-A cannot value them
   (audited CL-3: "no growing windows, no turning-point or Airy
   regime"); CL-B gives only sup_R |phi_k| <= C_s k^{-1/12}, which
   destroys the phase information the cancellation needs; the
   full-range Plancherel-Rotach elliptic asymptotic (Szego §8.22.9
   territory) has ZERO carriers in either root (sw-3) and is NEW
   citation input BEYOND the sealed CL-A discipline (a WIDER regime,
   not the same chapter's fixed-compact statement). Importing it
   silently would be exactly the forbidden import; it is displayed
   as the decider instead (§8). REFUSED as out of today's ground.
RT-C' WIDOM/SOBOLEV-CLASS IMPORT. The audited CL-4 trap note stands
   at bytes (T-6): those theorems cover scaled pseudodifferential
   compressions, not Galerkin spectral projections; sgn(Q_n h_0 Q_n)
   is the latter (and SP-1 makes the difference exact: the grid is
   the ZERO set, not a lattice; the eigenvectors are twisted CD
   columns, not plane waves). REFUSED.
RT-D' MOMENT ROUTES. tr(P sgn_n) = 0 EXACTLY (CO-3): the first
   moment is blind to the sign object; the second moment IS S_n^2;
   higher even moments of the compressed pair (PuP powers) need the
   same off-diagonal data (W12': totals do not determine them).
   REFUSED at CO-3 + the witness.
RT-E' INTERPOLATION/RANK. ||[P, sgn_n]||_op <= 2 (projections and a
   contraction... ||u|| <= 1), rank <= 8n^3: trace-norm interpolation
   reproduces rank-grade cubic ceilings, vacuous at 3/2 (the sealed
   RT-E arithmetic, unchanged by SP-1..SP-3). REFUSED.
RT-F' THE FLOOR DIRECTION (toward FLOOR-DERIVED). A 3/2 floor needs
   the angular sum >= c n^{3/2}, i.e. a POSITIVE fraction of the
   off-diagonal overlap mass at angles bounded away from 0 — an
   ANTI-localization law. Nothing sealed supplies one; the of-record
   surface anticipation points the OTHER way (crossing mass at
   near-diagonal momentum pairs; ANTICIPATION only, ground for
   nothing); and W12' blocks every totals-only route equally in this
   direction (model A vs B: same totals, different excess). REFUSED
   both ways — the commissioned floor is as undecidable as the
   ceiling today.
```

---

## 8. THE VERDICT AND THE DECIDER MENU

**VERDICT: UNDECIDABLE-TODAY** (for O-1-S as commissioned: neither
the ceiling S_n^2 <= C' n^{3/2-gamma'} with explicit gamma' > 0 nor
the floor S_n^2 >= c' n^{3/2} is derivable on the sealed stock at the
cutoff). NOT CEILING-DERIVED and NOT FLOOR-DERIVED (the W12' pair +
padding bridge force refusal of every route quantified over the
resolved-byte hypothesis class, BOTH directions; the byte-deeper
routes RT-A'..RT-F' each refuse at an exact display). NOT
POLYLOG-ONLY (no gain below exact power 3/2 of any size is derived
for S_n^2 — CO-2 returns the 3/2 bulk exactly; and per the audited
c-1, a polylog gain would not decide anyway).

```text
BLOCKER-1' (the overlap law): no sealed or classical-grade object
  controls the off-diagonal/angular distribution of the ball-overlap
  matrix B_pp' = <psi_p, 1_B psi_p'>; CL-A is fixed-compact (audited
  CL-3), CL-B is sup-only, and the eigenvector coefficients are
  phi_k AT THE ZEROS (arguments up to sqrt(2n+1) — the full-range
  elliptic PR regime): zero carriers in either root (sw-3).
BLOCKER-2' (the angular freedom): W12' — spectrum + count law +
  chirality + kernel + Gram moduli leave S_n^2 free at the resolved
  byte grade (§6); with the padding bridge the forcing covers the
  n-indexed thresholded class exactly as the audited c-A pattern.

WHAT MOVED THIS COMMISSION: BLOCKER-1 of record (no polar-part
  control, sw-2) is RESOLVED by SP-1 (closed form at the bytes);
  BLOCKER-2 of record (no low-energy density law) is SUPPLIED by
  SP-2 (exact count ceiling, given CL-C). The question is now ONE
  object deep, and named:

THE NEW SEALED OBJECT THAT DECIDES:
O-1-S-B (THE OVERLAP LAW): a certified angular-localization ceiling
    A_n := sum_{p != p' in G_n} |B_pp'|^2 (1 - cos angle(p, p'))
        <= C_B'' n^{3/2 - gamma_B},   gamma_B > 0, both symbolic
  — yields O-1-S CEILING at gamma' = min(gamma_B, 1/12) through
  SP-3 (S_n^2 <= J_n^2 + 4 A_n + 0) + the sealed P-2 (J_n^2 <= C_Q
  n^{17/12}), hence O-1 at gamma = min(gamma', 1/12) through the
  sealed P-3, hence DECIDED-o(1) for MO-4-R at rate n^{-gamma}
  through the sealed T-4 — nothing else needed [SN10 arithmetic].
  EQUALLY: a certified anti-localization floor A_n >= c n^{3/2}
  yields S_n^2 >= 4 c n^{3/2} - 4 (SP-3, excess one-signed):
  FLOOR-DERIVED and CEILING-FAILS-AT-3/2 through the sealed §5
  floor-side display. CONSUMES: SP-1's grid/eigenvector bytes (this
  artifact) + genuinely NEW analytic input: the full-range
  Plancherel-Rotach asymptotic of phi_k at the zeros (Szego §8.22.9
  regime — elliptic/oscillatory, with explicit remainder control)
  OR any equivalent quantified law for the twisted CD columns on
  the ball (equivalently: for the spectral-window crossing masses
  ||E_I P E_J||_2^2 in radius AND angle). No such carrier exists in
  either root at the cutoff (sw-3); it is not within CL-A's audited
  scope (CL-3) nor CL-B's.
ALTERNATIVE DECIDERS (unchanged, of record): the O-2 localization
  package (T-3 §6); any direct kappa_n route (O-1 as specified).

POSITION AFTER THIS COMMISSION: S_n^2 is now BRACKETED
  (C_*/2) n <= S_n^2 <= C_B''' n^{3/2} + O(1)   [CO-1; CO-2 + P-1
  assembly], with the excess over J_n^2 exactly identified (SP-3)
  and the deciding object reduced from "any sign-crossing law"
  (of record) to ONE named overlap law (O-1-S-B). The sign question
  and the carrier question now differ by a single explicit
  commutator compression: S_n^2 - J_n^2 = (1/2)||Q_n [P, sgn_n]
  Q_n||_2^2 - [0,4].
```

---

## 9. THE SWEEP (exhaustive at this displayed sweep only; cutoff in header)

```text
ROOTS (as commissioned): /Users/bgm/MB Work/alpha-program-archive/
workspace (primary) and /Users/bgm/Documents/New project/
gravity_emergence_evidence_program/
alpha_fundamental_record_action_cleanroom_v003 (cleanroom). Fenced
name classes (register|road_|ledger|lens|plan|tracker|THE_HANDOFF|
continuation) excluded from every opening; filename-level listing
and match-count probes only; .proof_deps vendored library files
typed out.
KEYS RUN: "T10SR"; "SIGN_CROSSING"; "O-1-S"; "sign-crossing";
"8.22.9" (fixed-string); "Golub"; "overlap law"; "Jacobi matrix";
"angular excess"; "elliptic regime"; "zeros of H_n".
FINDINGS:
 sw-1 OCCUPATION: NO T10SR and NO SIGN_CROSSING artifact exists in
      either root (probed at session start AND at the cutoff; output
      path probed ABSENT twice). UNOCCUPIED.
 sw-2 O-1-S CARRIERS: "O-1-S" and "sign-crossing" carriers are
      EXACTLY the sealed T7SR pair (the commission's own ground).
      No third carrier.
 sw-3 OVERLAP/ELLIPTIC CARRIERS: "8.22.9" (fixed-string), "elliptic
      regime", "zeros of H_n", "angular excess": ZERO carriers in
      both roots. "Golub": vendored sympy library files only (typed
      out). The full-range PR ground and any overlap-decay law are
      NEW input, nowhere sealed, contradicted by nothing sealed.
 sw-4 HOMONYMS, PROBED AT COUNT LEVEL (never opened): "overlap law"
      hits five workspace artifacts (EQ6/GC2/PRIMITIVE families) and
      their cleanroom mirrors; "Jacobi matrix" hits one spec
      (PURE_HERMITE_VACUUM lane); ALL probed at match-count level:
      zero occurrences of kappa_n, sgn(, sign(H, or B_pp in any of
      them — different objects, different lanes, typed out.
```

Each sentence claims only this displayed sweep at the declared
cutoff.

---

## 10. CONSISTENCY WITH THE SEALED STOCK — EVERY CONTACT POINT

```text
X-1 vs T-1 P-3 (the sign reduction): SP-3 is CONSISTENT and SHARPER
    where it overlaps: sealed |sqrt(kappa_n/2) - S_n/2| <= J_n/2 + 1
    and the C11 algebra are used at their sealed displays for the
    YIELD chains only; the new identity refines the RELATION between
    S_n and J_n (equality up to the compressed commutator) without
    touching any sealed quantifier.
X-2 vs T-1 sw-2 ("no polar-part control exists"): a sweep-absence
    claim at ITS cutoff (05:48:15); SP-1 is NEW derivation at a
    LATER cutoff consuming bytes (T-5 §2) the sweep never denied —
    no sealed sentence contradicted; the absence claim stands AS a
    statement about its own cutoff.
X-3 vs T-1 BLOCKER-2 / C13 / audited AB11: the divergence display
    stands (Lipschitz-DOI alone fails); SP-2 supplies the named
    density law; RT-A' shows the cured route STILL stalls at the
    localization — T-1's "REQUIRES a low-energy spectral density
    law" was necessary, not sufficient; displayed, no contradiction.
X-4 vs C12 + audited c-A (the forcing): SP-1 consumes bytes OUTSIDE
    the C12 hypothesis class — exactly the o-3 reservation; W12'
    re-forces at the deeper class with the same padding-bridge
    repair (consumed at audited grade). The C12 pair's own models
    are NOT of the record's tensor form; no claim about them is
    made or needed.
X-5 vs the sealed kernel bookkeeping (T-5 FACT TWO): SP-1 reproduces
    dim ker = 4(n mod 2) EXACTLY from the zero set (0 in Z_n^{(1)}
    iff n odd) — independent cross-check of a sealed display, match.
X-6 vs MO-2's floors: CO-1 transfers the sealed linear floor to
    S_n^2 (linear under the 3/2 bulk ceiling — consistent brackets
    at every n); no floor quantifier touched.
X-7 vs T-5/T-7 Gram ground: k_n diagonal ceiling and G_n displays
    consumed nowhere below except through the sealed P-1/P-2 quotes;
    no G_n/crossing-operator collision (the audited c-4 note
    respected: B_pp' is a NEW named object, not G_n).
X-8 vs D4 and rank displays: rank sgn_n = 4n^3 - 4(n mod 2)
    consistent with rank C_n <= 4n^3 (sealed); RT-E' reproduces the
    sealed vacuity arithmetic unchanged.
X-9 FENCES: no closure claimed; O-1, O-1-S, MO-4-R all stay open;
    no flag of any prior artifact flips; alpha_computed /
    proof_authorized / kappa_record_computed all false; every
    constant symbolic or exact closed form; the witness rationals
    are TOY-SEPARATED (§6, §13); H-R/rho_n appears in no display.
```

---

## 11. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `signvenv` under the session scratchpad;
nothing written to the workspace but this artifact and its seal.
Tooling disclosure, on the record: ONE pre-final check-form
correction — SN2's Hermiticity conjunct originally tested
U(p).H - U(p) == 0 directly; sympy's assumptions engine leaves
conjugate(sqrt(p_x^2 + p_y^2 + p_z^2)) unevaluated (the sum of
squares of real symbols is not recognized as nonnegative by the
conjugate simplifier), so the conjunct was rewritten to test
Hermiticity of the numerator (p.alpha) — the positive real scalar
|p| carried in the check NAME; the mathematical claim (U(p)
Hermitian) is unchanged and the other three SN2 conjuncts were
unaffected (the failing form and the diagnosis are on the work-log
record). After the one correction the battery ran TWICE with
BYTE-IDENTICAL output (diff empty), 10/10 PASS; no other edit at
any point.

```python
# SN-BUILD CAS battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
# signvenv under the session scratchpad). Commission T10SR. Every constant
# symbolic or an exact rational/closed form; nothing numeric evaluated; no
# floats as ground. All checks are exact-identity consistency checks of steps
# whose operator-theoretic content is derived in the artifact text.
import itertools
import sympy as sp
from sympy import Rational as R, I, sqrt, kronecker_product as kron

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

px, py, pz = sp.symbols('p_x p_y p_z', real=True)
ell = sp.Symbol('ell', positive=True)
rho = sp.Symbol('rho', positive=True)
lam = sp.Symbol('lambda', real=True)
n, t = sp.Symbol('n', positive=True), sp.Symbol('t', positive=True)

s1 = sp.Matrix([[0, 1], [1, 0]])
s2 = sp.Matrix([[0, -I], [I, 0]])
s3 = sp.Matrix([[1, 0], [0, -1]])
Z2, I2 = sp.zeros(2, 2), sp.eye(2)
alph = [sp.Matrix(sp.BlockMatrix([[Z2, s], [s, Z2]])) for s in (s1, s2, s3)]
beta4 = sp.diag(1, 1, -1, -1)

# ===== SN1 -- Dirac algebra ground =====
pa = px*alph[0] + py*alph[1] + pz*alph[2]
c1 = all(sp.expand(alph[i]*alph[j] + alph[j]*alph[i]
                   - 2*(1 if i == j else 0)*sp.eye(4)) == sp.zeros(4, 4)
         for i in range(3) for j in range(3))
c2 = all(sp.trace(alph[i]*alph[j]) == (4 if i == j else 0)
         for i in range(3) for j in range(3))
c3 = all(sp.expand(beta4*a + a*beta4) == sp.zeros(4, 4) for a in alph)
c4 = sp.expand(pa*pa - (px**2 + py**2 + pz**2)*sp.eye(4)) == sp.zeros(4, 4)
c5 = sp.trace(pa) == 0
ok("SN1 DIRAC GROUND: {alpha_i,alpha_j} = 2 delta_ij, tr(alpha_i alpha_j) ="
   " 4 delta_ij, {beta,alpha_j} = 0, (p.alpha)^2 = |p|^2 1_4 (symbolic p),"
   " tr(p.alpha) = 0 -- all EXACT", c1 and c2 and c3 and c4 and c5)

# ===== SN2 -- the polar closed form on a momentum block =====
qx, qy, qz = sp.symbols('q_x q_y q_z', real=True)
qa = qx*alph[0] + qy*alph[1] + qz*alph[2]
np_, nq = sp.sqrt(px**2+py**2+pz**2), sp.sqrt(qx**2+qy**2+qz**2)
U_p, U_q = pa/np_, qa/nq
c1 = sp.simplify(sp.expand(U_p*U_p) - sp.eye(4)) == sp.zeros(4, 4)
c2 = sp.simplify(sp.trace(U_p*U_q) - 4*(px*qx+py*qy+pz*qz)/(np_*nq)) == 0
c3 = sp.expand(pa.H - pa) == sp.zeros(4, 4)
neg_proj = (sp.eye(4) - U_p)/2
c4 = sp.simplify(sp.expand(pa*neg_proj + np_*neg_proj)) == sp.zeros(4, 4)
ok("SN2 POLAR CLOSED FORM: U(p) := (p.alpha)/|p| is Hermitian ((p.alpha)"
   " Hermitian exact; |p| a positive real scalar), U^2 = 1, tr(U(p)U(q)) ="
   " 4 p.q/(|p||q|) = 4 cos angle, and (1-U)/2 is EXACTLY the negative-energy"
   " spectral projection of p.alpha (p.alpha (1-U)/2 = -|p| (1-U)/2)"
   " -- all symbolic EXACT", c1 and c2 and c3 and c4)

# ===== SN3 -- the n = 2 member: blockwise sgn built and verified =====
m2 = (1/(ell*sqrt(2)))*sp.Matrix([[0, -I], [I, 0]])   # q_2 p q_2 (sealed bytes)
wp = sp.Matrix([1,  I])/sqrt(2)
wm = sp.Matrix([1, -I])/sqrt(2)
c_eig = (sp.expand(m2*wp - (1/(ell*sqrt(2)))*wp) == sp.zeros(2, 1)
         and sp.expand(m2*wm + (1/(ell*sqrt(2)))*wm) == sp.zeros(2, 1))
pi_ops = [kron(kron(m2, I2), I2), kron(kron(I2, m2), I2), kron(kron(I2, I2), m2)]
c_comm = all(sp.expand(pi_ops[a]*pi_ops[b] - pi_ops[b]*pi_ops[a])
             == sp.zeros(8, 8) for a in range(3) for b in range(3))
H32 = sp.zeros(32, 32)
for j in range(3):
    H32 += kron(pi_ops[j], alph[j])
Uc = sp.zeros(32, 32)
wvec = {1: wp, -1: wm}
for sx_, sy_, sz_ in itertools.product((1, -1), repeat=3):
    Pi_sp = kron(kron(wvec[sx_]*wvec[sx_].H, wvec[sy_]*wvec[sy_].H),
                 wvec[sz_]*wvec[sz_].H)
    u4 = (sx_*alph[0] + sy_*alph[1] + sz_*alph[2])/sqrt(3)
    Uc += kron(Pi_sp, u4)
c_herm = sp.expand(Uc.H - Uc) == sp.zeros(32, 32)
c_inv = sp.expand(Uc*Uc - sp.eye(32)) == sp.zeros(32, 32)
c_com = sp.expand(Uc*H32 - H32*Uc) == sp.zeros(32, 32)
c_pos = sp.expand(Uc*H32 - (sqrt(3)/(sqrt(2)*ell))*sp.eye(32)) == sp.zeros(32, 32)
c_sq = sp.expand(H32*H32 - (R(3, 2)/ell**2)*sp.eye(32)) == sp.zeros(32, 32)
ok("SN3 POLAR RESOLUTION AT THE MEMBER n = 2 (32-dim, exact): the three"
   " compressed momenta commute; H = sum_j pi_j alpha_j has H^2 ="
   " (3/(2 ell^2)) 1; U_c := sum_p Pi_p x (p.alpha)/|p| over the 8 joint"
   " momentum states is Hermitian, U_c^2 = 1, [U_c, H] = 0, and U_c H ="
   " sqrt(3/2)/ell 1 > 0 -- so U_c = sgn(H) EXACTLY (blockwise functional"
   " calculus); eigenvector bytes verified against the sealed tridiagonal",
   c_eig and c_comm and c_herm and c_inv and c_com and c_pos and c_sq)

# ===== SN4 -- the angular-excess identity, Z = 0 instance =====
A6 = sp.Matrix([[1, 0], [1, 1], [0, 1], [1, 0], [0, 1], [1, 0]])
P6 = A6*(A6.T*A6)**-1*A6.T
Q6 = sp.diag(1, 1, 1, 1, 0, 0)
u6 = sp.Matrix(6, 6, lambda i, j: 0)
u6[0, 1] = u6[1, 0] = 1
u6[2, 3] = u6[3, 2] = 1
c_pre = (sp.expand(P6*P6 - P6) == sp.zeros(6, 6) and u6*u6 == Q6
         and u6*Q6 == u6 and Q6*u6 == u6)
S2 = sp.trace((( sp.eye(6)-P6)*u6*P6).T*((sp.eye(6)-P6)*u6*P6))
J2 = sp.trace(((sp.eye(6)-P6)*Q6*P6).T*((sp.eye(6)-P6)*Q6*P6))
K6 = Q6*(P6*u6 - u6*P6)*Q6
c_id = sp.simplify(S2 - J2 - R(1, 2)*sp.trace(K6.T*K6)) == 0
ok("SN4 ANGULAR-EXCESS IDENTITY (kernel-free case): S^2 = J^2 +"
   " (1/2)||Q[P,u]Q||_2^2 EXACT on a 6-dim instance with exact rational"
   " projection P (P = A(A^T A)^-1 A^T), Q = diag(1,1,1,1,0,0), u^2 = Q",
   c_pre and c_id)

# ===== SN5 -- the identity with a zero-mode block: the one-signed correction =====
u5 = sp.Matrix(6, 6, lambda i, j: 0)
u5[0, 1] = u5[1, 0] = 1
u5[2, 2] = 1
Z5 = sp.diag(0, 0, 0, 1, 0, 0)
c_pre = (u5*u5 == Q6 - Z5 and u5*Q6 == u5 and Z5*Q6 == Z5)
S2z = sp.trace(((sp.eye(6)-P6)*u5*P6).T*((sp.eye(6)-P6)*u5*P6))
K5 = Q6*(P6*u5 - u5*P6)*Q6
corr = sp.trace(((sp.eye(6)-Q6)*P6*Z5).T*((sp.eye(6)-Q6)*P6*Z5))
c_id = sp.simplify(S2z - J2 - R(1, 2)*sp.trace(K5.T*K5) + corr) == 0
c_rng = 0 <= corr and corr <= 4
ok("SN5 ZERO-MODE CORRECTION: with u^2 = Q - Z (rank Z = 1 instance):"
   " S^2 = J^2 + (1/2)||Q[P,u]Q||_2^2 - ||Q^perp P Z||_2^2 EXACT, and the"
   " correction is one-signed in [0, rank Z] -- so S^2 >= J^2 - 4 always"
   " and S^2 - J^2 - (1/2)||Q[P,u]Q||_2^2 = 0 exactly when Z = 0",
   c_pre and c_id and c_rng)

# ===== SN6 -- THE DEEPER WITNESS PAIR W12': angular freedom at byte grade =====
v3 = sp.Matrix([1, 2, 3])
Psp = v3*v3.T/14
Qsp = sp.diag(1, 1, 0)
Pf, Qf = kron(Psp, sp.eye(4)), kron(Qsp, sp.eye(4))
bf = kron(sp.eye(3), beta4)
e11 = sp.diag(1, 0, 0); e22 = sp.diag(0, 1, 0)
HA = kron(e11, rho*alph[0]) + kron(e22, rho*alph[1])     # p2 orthogonal
HB = kron(e11, rho*alph[0]) + kron(e22, -rho*alph[0])    # p2 antipodal
uA = kron(e11, alph[0]) + kron(e22, alph[1])
uB = kron(e11, alph[0]) + kron(e22, -alph[0])
CA, CB = (Qf - uA)/2, (Qf - uB)/2
listed = (sp.expand(HA*HA - rho**2*Qf) == sp.zeros(12, 12)
          and sp.expand(HB*HB - rho**2*Qf) == sp.zeros(12, 12)
          and sp.trace(HA) == 0 and sp.trace(HB) == 0
          and sp.expand(bf*HA + HA*bf) == sp.zeros(12, 12)
          and sp.expand(bf*HB + HB*bf) == sp.zeros(12, 12)
          and sp.expand(bf*Pf - Pf*bf) == sp.zeros(12, 12)
          and sp.trace(Pf*CA) == sp.trace(Pf*CB)
          and sp.expand(CA*CA - CA) == sp.zeros(12, 12)
          and sp.expand(CB*CB - CB) == sp.zeros(12, 12))
J2w = sp.trace(((sp.eye(12)-Pf)*Qf*Pf).H*((sp.eye(12)-Pf)*Qf*Pf))
S2A = sp.trace(((sp.eye(12)-Pf)*uA*Pf).H*((sp.eye(12)-Pf)*uA*Pf))
S2B = sp.trace(((sp.eye(12)-Pf)*uB*Pf).H*((sp.eye(12)-Pf)*uB*Pf))
KA = Qf*(Pf*uA - uA*Pf)*Qf; KB = Qf*(Pf*uB - uB*Pf)*Qf
c_idA = sp.simplify(S2A - J2w - R(1, 2)*sp.trace(KA.H*KA)) == 0
c_idB = sp.simplify(S2B - J2w - R(1, 2)*sp.trace(KB.H*KB)) == 0
c_sep = (sp.simplify(S2A - J2w - R(8, 49)) == 0
         and sp.simplify(S2B - J2w - R(16, 49)) == 0
         and sp.simplify(S2B - S2A - R(8, 49)) == 0)
ok("SN6 DEEPER WITNESS W12' (angular freedom at byte grade): two exact"
   " 12-dim models, both of the EXACT diagonalized form sum_p Pi_p x (p.alpha)"
   " -- IDENTICAL spectrum (H^2 = rho^2 Q, tr H = 0: eigenvalues +-rho x4"
   " each + zeros), identical chirality data, identical carrier Q, identical"
   " ball data (same P hence same B-matrix, same J^2, tr B, tr B^2),"
   " identical first moment tr(P C); the angular datum alone (p2 orthogonal"
   " vs antipodal to p1) moves S^2: S_A^2 = J^2 + 8/49, S_B^2 = J^2 + 16/49"
   " (excess = 4 sum |B_pp'|^2 (1 - cos angle) exactly: (1-0) vs (1-(-1)));"
   " the identity of SN4 re-verified on both", listed and c_idA and c_idB and c_sep)

# ===== SN7 -- Jacobi identification: eigenvalues of q_n p q_n = zeros of phi_n =====
x = sp.Symbol('x', real=True)
def phi(k, var):
    return (sp.hermite(k, var)*sp.exp(-var**2/2)
            / sp.sqrt(sp.sqrt(sp.pi)*2**k*sp.factorial(k)))
a_ = lambda k: sp.sqrt(R(k+1, 2))
rec_ok = all(sp.simplify(lam*phi(k, lam) - a_(k)*phi(k+1, lam)
             - (a_(k-1)*phi(k-1, lam) if k > 0 else 0)) == 0 for k in (0, 1, 2))
J3 = sp.Matrix([[0, a_(0), 0], [a_(0), 0, a_(1)], [0, a_(1), 0]])
vlam = sp.Matrix([phi(0, lam), phi(1, lam), phi(2, lam)])
resid = J3*vlam - lam*vlam + a_(2)*phi(3, lam)*sp.Matrix([0, 0, 1])
c_eig = sp.simplify(resid) == sp.zeros(3, 1)
cp = sp.expand((J3 - lam*sp.eye(3)).det())
c_char = sp.expand(cp - (-(lam**3 - R(3, 2)*lam))) == 0
p3m = (1/ell)*sp.Matrix([[0, -I*a_(0), 0], [I*a_(0), 0, -I*a_(1)],
                         [0, I*a_(1), 0]])
D3 = sp.diag(1, I, -1)
c_conj = sp.expand(D3*p3m*D3**-1 + (1/ell)*J3) == sp.zeros(3, 3)
minor = sp.Matrix([[a_(0), 0], [-lam, a_(1)]])
c_simple = sp.simplify(minor.det() - a_(0)*a_(1)) == 0 and a_(0)*a_(1) != 0
c_phi0 = sp.simplify(phi(0, lam)) != 0
ok("SN7 JACOBI IDENTIFICATION (n = 3 exhibit of the displayed general proof):"
   " three-term recurrence lam phi_k = a_k phi_{k+1} + a_{k-1} phi_{k-1}"
   " EXACT (a_k = sqrt((k+1)/2)); J_x v(lam) = lam v(lam) - a_{n-1}"
   " phi_n(lam) e_n so spec(J_x) = zeros of phi_n (char poly of J_3 ="
   " -(lam^3 - (3/2)lam), roots {0, +-sqrt(3/2)} = zeros of H_3); the"
   " unitary D = diag(i^k) conjugates the sealed tridiagonal q_n p q_n to"
   " -(1/ell) J_x (so p-spectrum = zeros/ell, symmetric); simplicity: the"
   " deleted-row/column minor is prod a_k != 0 (rank >= n-1); phi_0 never"
   " vanishes", rec_ok and c_eig and c_char and c_conj and c_simple and c_phi0)

# ===== SN8 -- the Hermite ODE members (CL-C application ground) =====
ode_ok = all(sp.simplify(sp.diff(phi(k, x), x, 2) + (2*k + 1 - x**2)*phi(k, x)) == 0
             for k in range(5))
ok("SN8 HERMITE ODE: phi_k'' + (2k+1 - x^2) phi_k = 0 EXACT at k = 0..4"
   " (the classical equation CL-C's Sturm comparison consumes; general case"
   " classical)", ode_ok)

# ===== SN9 -- count-law arithmetic + the entrywise-tight DOI integral =====
m_, s_, aa = sp.symbols('m s a', positive=True)
c_cnt = sp.expand((1 + 2*aa/s_ - m_)*s_ - (2*aa - (m_-1)*s_)) == 0
M_, m1, m2_, m3 = sp.symbols('M m1 m2 m3', nonnegative=True)
c_prod = sp.expand((M_-m1)*M_**2 + m1*(M_-m2_)*M_ + m1*m2_*(M_-m3)
                   - (M_**3 - m1*m2_*m3)) == 0
lamp = sp.Symbol('lambda', positive=True)
c_int = sp.integrate(lamp/(lamp**2 + t**2), (t, 0, sp.oo)) == sp.pi/2
c_odd = sp.integrate((-lamp)/(lamp**2 + t**2), (t, 0, sp.oo)) == -sp.pi/2
ok("SN9 COUNT + DOI GROUNDS: gap-count identity (m-1)s <= 2a iff m <= 1+2a/s"
   " (exact rearrangement); per-axis-to-3D product bound M^3 - m1 m2 m3 ="
   " exact telescoping sum of nonnegatives for 0 <= m_i <= M; and the"
   " entrywise DOI integral Int_0^oo lambda/(lambda^2+t^2) dt = (pi/2)"
   " sgn(lambda) EXACT both signs -- the t-integral of the entry difference"
   " IS (pi/2)(sgn lambda_i - sgn lambda_j): entrywise TIGHT",
   c_cnt and c_prod and c_int and c_odd)

# ===== SN10 -- yield and floor arithmetic =====
g = sp.Symbol('gamma_B', positive=True)
Cst = sp.Symbol('C_star', positive=True)
c_exp = (R(3, 2) - R(17, 12) == R(1, 12)
         and sp.expand((Cst*n - 4) - Cst*n/2 - (Cst*n/2 - 4)) == 0)
ok("SN10 YIELD ARITHMETIC: 3/2 - 17/12 = 1/12 EXACT (so an O-1-S-B ceiling"
   " at power gamma_B gives S^2 <= C n^{3/2 - min(gamma_B, 1/12)} through"
   " the identity + P-2); floor transfer (C_* n - 4) - C_* n/2 = C_* n/2 - 4"
   " >= 0 for n >= 8/C_*: S_n^2 >= (C_*/2) n cofinitely", c_exp)

print("SN-BATTERY-DONE")
```

Output, verbatim (10/10 PASS):

```text
SN1 DIRAC GROUND: {alpha_i,alpha_j} = 2 delta_ij, tr(alpha_i alpha_j) = 4 delta_ij, {beta,alpha_j} = 0, (p.alpha)^2 = |p|^2 1_4 (symbolic p), tr(p.alpha) = 0 -- all EXACT: PASS
SN2 POLAR CLOSED FORM: U(p) := (p.alpha)/|p| is Hermitian ((p.alpha) Hermitian exact; |p| a positive real scalar), U^2 = 1, tr(U(p)U(q)) = 4 p.q/(|p||q|) = 4 cos angle, and (1-U)/2 is EXACTLY the negative-energy spectral projection of p.alpha (p.alpha (1-U)/2 = -|p| (1-U)/2) -- all symbolic EXACT: PASS
SN3 POLAR RESOLUTION AT THE MEMBER n = 2 (32-dim, exact): the three compressed momenta commute; H = sum_j pi_j alpha_j has H^2 = (3/(2 ell^2)) 1; U_c := sum_p Pi_p x (p.alpha)/|p| over the 8 joint momentum states is Hermitian, U_c^2 = 1, [U_c, H] = 0, and U_c H = sqrt(3/2)/ell 1 > 0 -- so U_c = sgn(H) EXACTLY (blockwise functional calculus); eigenvector bytes verified against the sealed tridiagonal: PASS
SN4 ANGULAR-EXCESS IDENTITY (kernel-free case): S^2 = J^2 + (1/2)||Q[P,u]Q||_2^2 EXACT on a 6-dim instance with exact rational projection P (P = A(A^T A)^-1 A^T), Q = diag(1,1,1,1,0,0), u^2 = Q: PASS
SN5 ZERO-MODE CORRECTION: with u^2 = Q - Z (rank Z = 1 instance): S^2 = J^2 + (1/2)||Q[P,u]Q||_2^2 - ||Q^perp P Z||_2^2 EXACT, and the correction is one-signed in [0, rank Z] -- so S^2 >= J^2 - 4 always and S^2 - J^2 - (1/2)||Q[P,u]Q||_2^2 = 0 exactly when Z = 0: PASS
SN6 DEEPER WITNESS W12' (angular freedom at byte grade): two exact 12-dim models, both of the EXACT diagonalized form sum_p Pi_p x (p.alpha) -- IDENTICAL spectrum (H^2 = rho^2 Q, tr H = 0: eigenvalues +-rho x4 each + zeros), identical chirality data, identical carrier Q, identical ball data (same P hence same B-matrix, same J^2, tr B, tr B^2), identical first moment tr(P C); the angular datum alone (p2 orthogonal vs antipodal to p1) moves S^2: S_A^2 = J^2 + 8/49, S_B^2 = J^2 + 16/49 (excess = 4 sum |B_pp'|^2 (1 - cos angle) exactly: (1-0) vs (1-(-1))); the identity of SN4 re-verified on both: PASS
SN7 JACOBI IDENTIFICATION (n = 3 exhibit of the displayed general proof): three-term recurrence lam phi_k = a_k phi_{k+1} + a_{k-1} phi_{k-1} EXACT (a_k = sqrt((k+1)/2)); J_x v(lam) = lam v(lam) - a_{n-1} phi_n(lam) e_n so spec(J_x) = zeros of phi_n (char poly of J_3 = -(lam^3 - (3/2)lam), roots {0, +-sqrt(3/2)} = zeros of H_3); the unitary D = diag(i^k) conjugates the sealed tridiagonal q_n p q_n to -(1/ell) J_x (so p-spectrum = zeros/ell, symmetric); simplicity: the deleted-row/column minor is prod a_k != 0 (rank >= n-1); phi_0 never vanishes: PASS
SN8 HERMITE ODE: phi_k'' + (2k+1 - x^2) phi_k = 0 EXACT at k = 0..4 (the classical equation CL-C's Sturm comparison consumes; general case classical): PASS
SN9 COUNT + DOI GROUNDS: gap-count identity (m-1)s <= 2a iff m <= 1+2a/s (exact rearrangement); per-axis-to-3D product bound M^3 - m1 m2 m3 = exact telescoping sum of nonnegatives for 0 <= m_i <= M; and the entrywise DOI integral Int_0^oo lambda/(lambda^2+t^2) dt = (pi/2) sgn(lambda) EXACT both signs -- the t-integral of the entry difference IS (pi/2)(sgn lambda_i - sgn lambda_j): entrywise TIGHT: PASS
SN10 YIELD ARITHMETIC: 3/2 - 17/12 = 1/12 EXACT (so an O-1-S-B ceiling at power gamma_B gives S^2 <= C n^{3/2 - min(gamma_B, 1/12)} through the identity + P-2); floor transfer (C_* n - 4) - C_* n/2 = C_* n/2 - 4 >= 0 for n >= 8/C_*: S_n^2 >= (C_*/2) n cofinitely: PASS
SN-BATTERY-DONE
```

---

## 12. CHOICE LEDGER (every unforced choice, classified; commission T10SR)

```text
CH-1 CL-C (Sturm comparison on the Hermite ODE) AS THE ONE NEW
     CLASSICAL CITATION: the load-bearing unforced choice. Grounds:
     classical in the strictest sense (older than Plancherel-Rotach);
     the consumed consequence (zero spacing >= pi/sqrt(2n+1)) is
     derived exactly from the cited statement + the CAS-verified ODE;
     same citation discipline as the sealed CL-A/CL-B. Blast radius:
     SP-2 (§4) ONLY — SP-1, SP-3, W12', and the VERDICT consume
     sealed ground + exact algebra alone. Registrar note: if ruled
     out of scope, §4 downgrades to CONDITIONAL(CL-C); the verdict
     is unchanged (§7 RT-A': the count law does not decide).
CH-2 THE WITNESS SHAPE (12-dim, two grid points, v = (1,2,3)):
     IMMATERIAL(derived) — any pair of same-radius grid vectors at
     distinct angles with any non-angular-degenerate rational ball
     projection works; minimality is display economy (SN6).
CH-3 THE ZERO-MODE CONVENTION (kernel excluded from sgn_n; the
     one-signed correction ||Q^perp P Z||_2^2): FORCED by the sealed
     P-3 definition of sgn_n; the correction term is derived, not
     chosen (SN5).
CH-4 GRID COORDINATES (working in the SP-1 basis rather than
     operator-only): IMMATERIAL(derived) — SP-3's identity is
     displayed BOTH ways (operator form §5.1, grid form §5.2), CAS
     pinned in both.
CH-5 THE DECIDER NAMED AS AN ANGULAR-SUM LAW (A_n) rather than a
     pointwise kernel law: FORCED-shape — SP-3 shows A_n is exactly
     the gap between S_n^2 and the already-sealed J_n^2; any
     pointwise law would be consumed only through A_n.
CH-6 COUNT LAW DISPLAYED AS CEILING ONLY (floor noted, unconsumed):
     IMMATERIAL — nothing below consumes the floor; displayed to
     spare the successor a re-derivation.
CHAIN CHOICE AUDIT: no unforced choice enters the verdict chain. The
     verdict (UNDECIDABLE-TODAY, both directions) rests on: the W12'
     pair + padding bridge (exact; consumes CH-2's instance only as
     an EXHIBIT of a displayed general freedom — any instance
     serves), the RT-A'..RT-F' refusal displays (exact), and the
     sweep absences (sw-3) — none consumes CH-1 (SP-2 supplies a
     partial, not the verdict), CH-4, or CH-6. The verdict survives
     every alternative at CH-1..CH-6 unchanged.
MACHINERY/RELEVANCE: classical operator theory (functional calculus
     of commuting families, block decompositions, trace cyclicity,
     Schur/entrywise displays) + classical ODE comparison applied to
     SEALED constructions; identity cores CAS-pinned;
     SURFACE-DERIVED, not surface-native.
```

---

## 13. TOY_SEPARATION (self-assessment)

```text
CLAIMED CLEAN at the stated quantifiers. SN1/SN2/SN9/SN10 are
all-parameter symbolic identity/inequality exhibits. SN3 (n = 2
member), SN7 (n = 3 Jacobi exhibit), SN8 (ODE members k = 0..4) are
identity-grade exhibits of displayed general facts whose general
proofs are displayed in the text (blockwise functional calculus;
the recurrence/minor arguments; the classical ODE). SN4/SN5 are
finite instances of the SP-3 identity whose general operator proof
is displayed step by step in §5.1 (every step a named cyclicity /
uQ = u / ZQ = Z reduction). The W12' pair (SN6) is used ONLY against
derivability-from-displayed-hypotheses claims, which a single exact
countermodel pair (plus the audited padding bridge) lawfully
refutes; it asserts NOTHING about the record family's own angular
distribution, which stays undecided in both directions. CL-A/CL-B
are consumed only through sealed displays; CL-C at its own classical
quantifier. No model family is a premise of any derived partial:
SP-1/SP-3 are exact operator identities of the record family's own
sealed bytes; SP-2 consumes the record family's own ODE. kappa_n,
J_n, S_n, A_n, B_pp', C_*, C_Q, C', c', C_B'', gamma', gamma_B, and
all thresholds stay symbols or displayed closed forms; the witness
rationals (8/49, 16/49) are TOY constants, ground for nothing. The
RULING is the checker's/audit's, not this artifact's.
```

---

## 14. FLAG BLOCK

```text
SIGN_CROSSING = UNDECIDABLE-TODAY( O-1-S — neither S_n^2 <= C'
  n^{3/2-gamma'} (explicit gamma' > 0) nor S_n^2 >= c' n^{3/2} is
  derivable on the sealed stock at the cutoff; the undecidability is
  FORCED at the RESOLVED byte grade (W12' + the audited padding
  bridge: identical resolved polar form, spectrum, count law,
  chirality, kernel, and ball-overlap Gram moduli realize distinct
  S^2 — the angular datum is free). NOT polylog-only (no sub-3/2
  gain of any size; CO-2 returns the exact-3/2 bulk). Blockers
  exact: no overlap/angular law for B_pp' (sw-3: zero full-range PR
  carriers; CL-A fixed-compact per audited CL-3; CL-B sup-only);
  totals-only routes refuted by W12'; DOI entrywise-tight (SN9) —
  the SP-2 density law cures C13's divergence and still does not
  decide. )
PARTIALS_DERIVED = THREE( SP-1 THE POLAR RESOLUTION: sgn_n =
  sum_{p != 0} Pi_p tensor (p.alpha)/|p| over the joint momentum
  grid G_n = (zeros of phi_n)^3/ell — exact, from the sealed
  commuting tensor bytes (T-5 §2); spec(q_n p q_n) = zeros/ell via
  the D = diag(i^k) conjugation to the Hermite Jacobi matrix;
  simplicity by the minor argument; kernel = 4(n mod 2) reproduced.
  SP-2 THE SPECTRAL COUNT LAW (given CL-C, Sturm on the Hermite
  ODE): spec(H_n) = {+-|p|} each x2 and N_n(E) <= 4(1 + 2 E ell
  sqrt(2n+1)/pi)^3 — exact, n-uniform: BLOCKER-2's named absent
  object (the H_n low-energy density law) SUPPLIED. SP-3 THE
  ANGULAR-EXCESS IDENTITY: S_n^2 = J_n^2 + (1/2)||Q_n [P, sgn_n]
  Q_n||_2^2 - ||Q_n^perp P Z_n||_2^2 (last term in [0,4], zero for
  n even); in grid coordinates the excess = 4 sum_{p != p'}
  |B_pp'|^2 (1 - cos angle); corollaries: S_n^2 >= J_n^2 - 4 >=
  C_*(r, ell) n - 4 (the FIRST certified sign-crossing floor,
  linear grade); tr(P sgn_n) = 0 exactly; totals give back exact
  power 3/2 (no silent gain). )
DECIDER_OBJECT = O-1-S-B( the overlap law: certified A_n =
  sum_{p != p'} |B_pp'|^2 (1 - cos angle(p, p')) <= C_B''
  n^{3/2 - gamma_B} — yields O-1-S at gamma' = min(gamma_B, 1/12)
  via SP-3 + sealed P-2, O-1 at min(gamma', 1/12) via sealed P-3,
  and DECIDED-o(1) for MO-4-R at rate through the sealed T-4;
  equally an anti-localization floor A_n >= c n^{3/2} yields
  FLOOR-DERIVED and CEILING-FAILS-AT-3/2. CONSUMES: SP-1's grid
  bytes + genuinely NEW analytic input (full-range Plancherel-
  Rotach at the zeros, Szego §8.22.9 regime with explicit
  remainders, or any equivalent law for the twisted CD columns on
  the ball / windowed crossing masses in radius AND angle); no
  carrier in either root at the cutoff. The O-2 localization
  package of record stays the other decider, unchanged. )
CLASSICAL_GROUND = CL-A(sealed, consumed only through sealed
  displays) + CL-B(sealed at T-1, consumed only through sealed P-2)
  + CL-C( NEW citation: Sturm comparison, classical; applied to the
  CAS-verified Hermite ODE; blast radius SP-2 only; registrar note
  per the audited COR-3/CH-1 pattern: if ruled out of scope, SP-2
  downgrades to CONDITIONAL(CL-C) and the VERDICT is unchanged. )
CONSUMPTION_BOUNDARY = NOTHING-FIRED( O-1, O-1-S, MO-4-R all stay
  open; MO-4/r-3/R-L4b untouched; no flag of any prior artifact
  flips (T-1's sw-2 absence claim stands at its own cutoff; SP-1 is
  new derivation at a later cutoff, X-2); D4, the MO-2 floors, P-1/
  P-2/P-3 all stand (SP-3 refines relations, touches no sealed
  quantifier); H-R never defaulted; whether to commission O-1-S-B,
  O-2, or anything else is the registrar's call; the registrar
  consumes. )
SEALS_VERIFIED = 8/8( the T7SR pair + T3SR pair + MO-2 pair + MO-4
  pair, full-digest sidecar checks before any reliance (§1);
  r-2/r-3/PA bytes consumed through the sealed quoting artifacts at
  audited grade; "Q-..." tokens EXPECTED-UNLOCATABLE, none
  encountered beyond the ground's own notations. )
SWEEP_CUTOFF = 2026-08-15 06:47:00 CDT( keys and findings §9; T10SR
  and SIGN_CROSSING unoccupied; O-1-S carriers = the T7SR pair
  only; zero full-range-PR/overlap-law carriers; homonyms typed out
  at count level, never opened. )
FORBIDDEN_IMPORTS = none( exact symbolic only; no floats as ground;
  no numeric evaluation; no measured constant; NO value frozen —
  every constant and threshold symbolic or an exact closed form; no
  crossing law imported (the needed one is DISPLAYED as the decider,
  not assumed); no fenced-class file opened; no git action; no
  existing file edited; ONE file written plus its seal; output path
  probed ABSENT at session start and re-probed immediately before
  write. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv signvenv
  under the session scratchpad; ONE disclosed pre-final check-form
  correction (SN2 Hermiticity conjunct form; §11 header; no
  mathematical content changed); the corrected battery executed
  twice with BYTE-IDENTICAL output (diff empty), 10/10 PASS; script
  and output reproduced verbatim in §11.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
ALL_RESULTS = CLAIMED until checked.
SIGN_CROSSING_RESULT = SEALED.
```
