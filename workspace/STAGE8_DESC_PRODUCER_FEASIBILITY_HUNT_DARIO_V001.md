CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
CLOSURE_END_BYTE = 00008468   (computed on bytes as a fixed point at seal time)
VERDICT_BEARING_SET = exactly the 16 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member and every pinned span is rehashed AT ITS PATH from its own bytes before
  sealing; nothing is copied from another artifact's table or completed from a display.
LANE = DARIO   ROLE_THIS_RELAY = PRODUCER FEASIBILITY HUNT (relay 1083)
ASSIGNMENT SUBSTITUTION, DECLARED — THE PASTE WAS REPLACED TWICE MID-RELAY:
  ba9dc81efd807b98… (picked up; 1083_ACK records that reading)
  35ed9c2796ce6781… (adds the LATENT-FIRST BURDEN OF PROOF)
  35ed9c… -> member 01 below (adds THE BOUNDARY AS AN EXPLICIT INPUT to question 1)
  ** The work was redone under the latent-first burden -- the verdict changed from the draft I was
  holding -- and then extended to answer the boundary clause (section 4.3). ** Member 01 as pinned
  is the paste this artifact answers; the two superseded texts are named, not pinned. The member 01
  digest is recomputed at its path immediately before sealing, so a further replacement refuses the
  seal rather than being absorbed silently.
AUTHORS NOTHING. PROPOSES NOTHING. ADOPTS NOTHING. BINDS NO MEMBER.
  The PRODUCER and omega_phys are SUBJECTS. A producer SHAPE is exhibited as the assignment
  requires; nothing is supplied, selected, adopted or preferred. The measure is treated throughout
  as a producer OUTPUT, never as supplied stock.
DIRECTION = the corrected one. Member 03 rules there is NO supply/SM-6 contradiction and NO fork.
  SM-6 is NOT relaxed here and authorship alone is NOT called provenance. Members 03 and 11 are
  CARRIED, NOT REOPENED.
SELF_CITATION_BAR = members 12 and 13 are DARIO-authored SUBJECTS and are never witnesses. Where a
  fact originates in member 12, the WITNESS cited is the opposite lane's independent adoption of it.
BLIND EXPOSURE = kappa_record, alpha and the battery are pointers only; none was opened. The sector
  weight kappa_ch is carried SYMBOLICALLY throughout, exactly as member 07 [28638,29078) carries it;
  no ratio is evaluated and no value is formed.
NO_NUMERIC_PATH = nothing is computed. The generator scans its own code and refuses on any sampling,
  numeric library, or numeric literal path.
ALL_RESULTS = CLAIMED until the opposite-lane check.
LIVE_MEMBERS = NONE.
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1083_PRODUCER_FEASIBILITY_HUNT_DARIO_V001.md` | `2cbf7b39529415cee5ecf036fe7465fcb09ff8a659132fb5dec2c0ca5297bd87` | **assignment — latent-first, with the boundary as an explicit input** |
| 02 | `workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | the mandate: object, descent map, certificate, permitted authored core |
| 03 | `workspace/STAGE8_DESC_SURFACE_NECESSITY_CHECK_CODEX2_V001.md` | `e650076cc6daa42f41487a4a891d81dcbd0019fef2840809c70afea0c138512f` | **the corrected direction — carried, not reopened** |
| 04 | `workspace/STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874` | the four executable maps and `Cert_P5` |
| 05 | `workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | SM-1..SM-8 as producer/object/marginal conditions |
| 06 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | **`Lambda`, `A_F`, the DOUBLED branch algebra, `Theta_F`, the licensed imports** |
| 07 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **the frozen tuple, its three maps, and `kappa_ch`. READ ONLY WITH 08 AND 09.** |
| 08 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule governing 07 |
| 09 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | family-level ratification; the no-member clause |
| 10 | `workspace/STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | **the ratified law, `F_N`, the phase identity** |
| 11 | `workspace/STAGE8_MEASURE_V002_DESCENT_CHECK_CODEX2_V001.md` | `6a3297513f9a1d3981cf85553062f2b1137584d76c2a2d8bdd66a9e62b3cc15c` | the opposite lane's positive countermodels |
| 12 | `workspace/STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md` | `03dcaa17c78922f90bb905cd1ea7d9e9673514b74a229f0e6808a7a1dad1edd8` | SUBJECT — the still-continuum result (witnessed via 11) |
| 13 | `workspace/STAGE8_DESC_SURFACE_NECESSITY_HUNT_DARIO_V001.md` | `6bbebedcca8e1be5e21b21f319aeffe3816380330d800838ef5e9463f0faa95b` | SUBJECT — my 1081, corrected by member 03 |
| 14 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | the state port the producer must fill |
| 15 | `relay_inbox/RELAY_PASTE_1049_FRESH_SESSION_BOOTSTRAP_DARIO_V007.md` | `8c6cd83fac688dbdb3398b2c7abe3b044149a1a7ba6a6e102da7355381742084` | governing bootstrap |
| 16 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |

```text
PINNED SPANS — each recomputed in this relay from the member's own bytes at its own path.
  member 04 [2622,5894)    e017e9da18d5476171fd0e26f4496b3fb54efbf8ea006b419e4ecda375d512c2
                           d_state / d_effect / d_domain / d_dynamics and Cert_P5's eight conditions
  member 06 [6431,7014)    3008297258b6650fcc2e9b68c399336eb962c1b6d0e01b1cd61964d23f93a4cc
                           Lambda, A_F = C*(Lambda), B = A_F,+ tensor_min (A_F,-)^op iso C(Y)
  member 07 [22959,24536)  3e0b43706ef6e3297fa099f53702e658a2d41f73c33a425ac29ed2a23aef3d43
                           the primitive tuple, the three maps, the frozen construction order
  member 07 [28638,29078)  576b520977fe04bc0552c8900d7411209502a1b1d8173384560bdd53f019672e
                           kappa_ch := Tr_A(rho_P P_ch) — SYMBOLIC, expressly not evaluated
  member 10 [4243,5977)    ed170b5ff7d6dcf236db7ca39fffc239091a2377a02e5aaf66413b75072a60bf
                           the controlled law and the exact influence functional
  member 10 [5939,6270)    c96e0be784481915cdd6790a5028dfdb4e2a58d0e0dd760cc9429e89e1a9b600
                           equation (4): F_N, the mixed observable the read-off consumes
  member 05 [17865,20008)  aa04e0ea2c901d0a33312a23d6ade023f8c1e92f2e90d1ff6b447f4e98cea171
                           SM-1..SM-8
  member 02 [1470,2433)    758023b00d6bfebf0d518036370dd625a26d206acd1c17813b8e0eac576c4c97
                           the typed object WITH its descent map and provenance certificate
  member 14 [10824,11934)  4313fb92fd78f6b26a5eba216e7ae7540f6b0698dc82eddd98236ceee5d7cf1b
                           StatePort_U2_008 — the fields the producer must fill
  member 12 [12197,13783)  57aacdc1d125eebf337da74962ce3c6ae12bad7d9f87e9b03bc96a5636ebc659
                           SUBJECT span: the realness consequence and the endpoints' attainability.
                           WITNESSED by member 11, which adopts these facts independently.

ESTABLISHED IN THIS RELAY — BY EXACT ALGEBRA OVER SEALED OBJECTS, NOTHING COMPUTED:
  the correction to the assignment's stated obstruction (section 3); the trace slice of section 4;
  the branch-state shape of section 5; the endpoint/symmetry account of section 6.
  Every expression is symbolic in kappa_ch and in N. No ratio, weight or expectation is evaluated.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO OUTPUT INSPECTION. NO MEMBER BOUND.
NO FIXED POINT. NO END TEST. NO FREEZE. NOTHING ENTERED, ADOPTED, PROPOSED OR SELECTED.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — THE PRODUCER FEASIBILITY HUNT
## RELAY 1083 — `[PLAN:DESC-39]` — DARIO LANE — [CLAIMED]

Date: 2026-08-12

**THE QUESTION, UNDER THE ASSIGNMENT'S BURDEN.** Is the common-origin producer **latent in the
sealed trace**? The default is `FEASIBLE-FROM-SEALED`; `NEEDS-NEW-PRIMITIVE` may be concluded only
with an explicit proof of non-derivability, and never because the sealed route is merely hard.

**IT IS LATENT, AND THE OBSTRUCTION THE ASSIGNMENT NAMES IS NOT THE REAL ONE.**

## 0. TWO THINGS ON THE RECORD BEFORE THE ARGUMENT

### 0.1 The assignment was replaced mid-relay, and the verdict changed

I picked this relay up against an earlier paste and had a draft that ruled `NEEDS-NEW-PRIMITIVE`.
The paste on disk was then replaced twice: first adding the latent-first burden, then adding the
boundary datum as an explicit producer input. **My draft would not have met the latent-first burden
— it set the sealed route aside after pressing four readings, which is not a proof of
non-derivability.** Under the correct burden I went back to the sealed route, and it opens; the
boundary clause then names, in the assignment's own words, the ingredient the opened route uses.
The earlier verdict is withdrawn before it was ever sealed, and it is recorded here rather than
quietly dropped.

### 0.2 The direction correction I am carrying

```text
MY 1081 RULING          "an authored object cannot carry from-trace provenance", hence a
                        contradiction and a principal fork.
MEMBER 03's CORRECTION  that conflates authorship of a post-hoc OUTPUT with authorship of a
                        PRODUCER or its premises.
                        ** WITHDRAWN, carried, not reopened. **
```

Authorship lives in a RULE frozen before output; the joint state is DERIVED from it. SM-6 is not
relaxed here, and authorship alone is not called provenance.

---

## 1. THE BURDEN, AND HOW I DISCHARGED IT

```text
DEFAULT              FEASIBLE-FROM-SEALED
SURPRISING           NEEDS-NEW-PRIMITIVE — admissible only with a non-derivability PROOF
FALLBACK IF UNPROVED UNDETERMINED, with the deciding ground named
GUARD                NEEDS-NEW-PRIMITIVE adds authorship and ratification; it is the answer that
                     ends this lane's work, so the latent route is argued FIRST and HARDEST.
```

**No non-derivability proof is claimed in this artifact, and none is needed: the latent route
opens.** Section 3 corrects the obstruction the assignment offers, section 4 finds the trace's own
correlation-carrying output, section 5 exhibits the producer shape, and section 6 says where the
coherence originates.

---

## 2. WHAT THE PRODUCER MUST DELIVER

Member 04 `[2622,5894)` fixes the receiver: typed maps

```text
d_state(Omega) = rho_pre,
d_effect(Omega,r) = E_r,
d_domain(Omega) = (D_dyn, {D_r}),
d_dynamics(Omega) = U_BR or S_CTP,
```

with `Cert_P5(Omega)` establishing common carrier, state/effect/domain/dynamic properties,
covariance, **provenance** — *"none is imported as an independent post hoc parameter"* — and
**target-independent admissibility** frozen before downstream information exists.

**A NAMING COLLISION, FLAGGED BEFORE IT DOES DAMAGE.** Member 07's `d_state` lands on the SOURCE
state; member 04's `d_state` lands on `rho_pre`, the state of the completed object. Same name,
different maps, and they must not be matched by name. (Member 10's `W_N` collides with the
outgoing-record result's `W_N` the same way; that collision cost an earlier relay real work.)

The gap the previous relays located is the **branch factor**: member 07 `[22959,24536)` freezes a
tuple whose only history-indexed primitive is the character family, and characters enter the
DYNAMICS rather than a state over histories.

---

## 3. THE OBSTRUCTION THE ASSIGNMENT NAMES IS NOT THE REAL ONE

Member 01 names the concrete obstruction to break: *the ratified source state is sector-diagonal,
`P_0 . d_state . P_ch = 0`, yet the phase is receivable only in cross-sector coherence.*

**The first clause is sealed and true. The second is false, and correcting it is what opens the
route.**

The read-off's mixed observable is member 10 `[5939,6270)`:

```text
F_N = P_0 tensor 1 + P_ch tensor Z_N.
```

`P_0` and `P_ch` are **projectors** — diagonal elements of the sector algebra. So the quantity the
read-off needs,

```text
Omega_C0(P_ch tensor Z_N),
```

is a pairing of a **diagonal** source observable with a branch observable. **What it requires is
correlation between the sector algebra and the branch algebra — not off-diagonal coherence within
the source.** A sector-diagonal source marginal is perfectly compatible with it.

**Member 03's own witness is exactly such a state**: it couples the charged sector to one history
state and the neutral sector to another, and the resulting value on `P_ch tensor Z_N` differs from
the product's by a nonzero symbolic term. That witness uses no source coherence whatever.

```text
CORRECTED OBSTRUCTION:
  what is needed is SECTOR-BRANCH CORRELATION (classical correlation across the cut suffices),
  NOT cross-sector coherence inside the source.
```

**And this is why my own V002 collapse theorem does not obstruct anything here.** That theorem says
the conditionals generated by *unitary words acting on the frozen input* are history-independent
when the source state is sector-diagonal. It is a statement about one construction family — the one
my V002 authored as A2 — and member 11 confirmed it only within that family. **A producer that
builds the branch state from the trace's own OUTPUT rather than from unitary-word conditionals is
outside that theorem's quantifier**, and section 4 builds exactly such a producer.

---

## 4. THE TRACE ALREADY EMITS THE CORRELATION-CARRYING OBJECT

### 4.1 The slice

Member 03 `§2.3` takes one slice of the mixed observable, applying a branch state to get a source
operator. **Take the other slice.** Apply the ratified source state to `F_N`:

```text
D := (omega_S tensor id_B)(F_N)
   = omega_S(P_0) . 1 + omega_S(P_ch) . Z_N
   = (1 - kappa_ch) . 1 + kappa_ch . Z_N,        an element of B.
```

Both ingredients are ratified and neither is authored: `omega_S` is member 07's `d_state`, the
normalized identity the anchor theorem proves; `kappa_ch := Tr_A(rho_P P_ch)` is member 07
`[28638,29078)`, carried there as a **symbolic ratio expressly not evaluated**, and carried
symbolically here; `F_N` and `Z_N` are member 10's ratified law.

```text
** THE FROZEN TRACE EMITS AN ELEMENT OF THE BRANCH ALGEBRA THAT CARRIES THE SECTOR WEIGHT. **
D is the trace's own two-branch amplitude: the sector is the CONTROL of the ratified transition,
so the control's weight is exactly what D carries into B.
```

### 4.2 Why this is the thing the previous relays kept missing

Six relays looked for a *state* on the branch factor and found none in the tuple. That is correct
and stays correct. **What the tuple does contain is an OBSERVABLE-valued output on the branch
factor** — and a producer needs the branch state to be *derived*, not *found*. `D` is the material
from which it can be derived, and it is material the tuple did not obviously contain until the
slice is taken.

### 4.3 The boundary as an input — the assignment's precise question, answered

Member 01 makes the entered boundary datum available to the producer **alongside** the frozen trace,
and notes that the entered connection cancels at the single-branch state level by charge
superselection and survives only in the two-branch observable. **That is the same structural fact
section 3 reaches from the other side**, and it is why the collapse theorem of my own V002 — a
single-branch statement — cannot obstruct a two-branch construction.

The question asked is whether boundary-plus-trace **determines** a state whose coherence registers
the coupling, or leaves the state free. Neither disposition is right, and the precise
split is this:

```text
BOUNDARY + TRACE  PRODUCE, BUT DO NOT DETERMINE.
  PRODUCED, WITH NO FREEDOM:  the amplitude D of section 4.1.  Its two ingredients are the
    trace's ratified sector weight and the entered boundary datum, and nothing else enters it.
    D registers the coupling: it is not constant on the branch pair, and it is not either of the
    contentless canonical states of section 6.2.
  NOT DETERMINED:  the passage from an AMPLITUDE to a WEIGHT.  A state is a positive normalized
    functional; D is neither positive nor a weight, and no sealed row says how to turn one into
    the other.
```

**So the residual freedom is real but it has changed shape**, and that is the useful finding here.
Before: any invariant probability measure on the branch pair — the still-continuum of member 12.
Now: a positive weight built from one determined amplitude. **The continuum is not closed by
boundary-plus-trace; it is re-based on an object that already carries the coupling**, and its
degenerate members — the ones whose weight ignores `D` — are exactly the contentless canonical
states the previous relays kept reaching.

---

## 5. THE PRODUCER SHAPE

**Exhibited as a SHAPE, as the assignment directs — not proposed, not adopted, not preferred.**

```text
STEP 1  the frozen trace supplies d_state (the anchor theorem's normalized identity) and the
        ratified mixed observable F_N.                                     [member 07; member 10]
STEP 2  SLICE:  D := (omega_S tensor id_B)(F_N) = (1 - kappa_ch) + kappa_ch Z_N   in B.
STEP 3  BRANCH STATE:  mu := |D|^2 . Haar_Y  /  Haar_Y(|D|^2),
        with Haar_Y the branch algebra's canonical trace state.
STEP 4  JOINT:  the trace's own maps, with mu as the branch factor's state, then the port's fields.
```

### 5.1 It is a state, and the checks are exact

```text
POSITIVITY      |D|^2 is a modulus squared: pointwise nonnegative.  SM-2 holds by construction,
                with no side condition on kappa_ch.
NORMALIZATION   Haar_Y(|D|^2) = (1-kappa_ch)^2 + kappa_ch^2, since Haar_Y(Re Z_N) = 0 by
                orthogonality of the nontrivial character Z_N.  Nonzero for every kappa_ch.
U1 REALITY      member 12's span (witnessed by member 11) has each factor of Z_N fixed by the
                reality map, so Theta_F(Z_N) = Z_N; kappa_ch is real; hence Theta_F(D) = D and
                |D|^2 is Theta_F-fixed.  Haar_Y is Theta_F-invariant by automorphism plus
                uniqueness.  So mu is U1-compatible.
```

### 5.2 The phase content it carries — symbolic, nothing evaluated

Expanding `|D|^2 = (1-kappa_ch)^2 + kappa_ch^2 + 2 kappa_ch (1-kappa_ch) Re Z_N` and using
`Haar_Y(Z_N) = 0`, `Haar_Y(conj(Z_N) Z_N) = 1`:

```text
mu(Z_N) = kappa_ch (1 - kappa_ch) / [ (1-kappa_ch)^2 + kappa_ch^2 ].
```

**Strictly inside the sealed interval whenever the sector weight is neither of its extremes** — the
coupling content is carried, not washed out. **And the shape is not mine**: member 03's independent
structural witness produces the same `weight . (1 - weight)` form for the joint term. Two different
constructions landing on the same shape is the strongest corroboration available to a hunt that
computes nothing.

**Nothing here is evaluated.** `kappa_ch` stays the symbolic ratio member 07 declares it to be, `N`
stays symbolic, and no expectation is turned into a value. I did not ask, and cannot ask, whether
any of this is favourable downstream: section 8 certifies the pointers stayed shut.

### 5.3 Where the authored core sits — disclosed, not hidden

**Step 3's functional form is authored.** `|D|^2` is one positive function of `D` among many, and
naming it is a rule, not a derivation.

```text
WHAT IS DERIVED   D itself, entirely from ratified objects (Step 2) — the sector weight and the
                  influence functional are the trace's, not mine.
WHAT IS AUTHORED  the rule that the branch state is the trace amplitude's TRANSITION PROBABILITY
                  against the branch algebra's canonical trace state.
WHAT THAT COSTS   SM-7.  A rule chosen after seeing a downstream number would be target-tuned; the
                  mandate's answer is the one this shape must accept — FROZEN BEFORE OUTPUT, with
                  the form declared in the ledger as the authored core.
```

**The provenance argument for that particular form, stated as an argument and not as a fact:**
`|D|^2` is the squared modulus of the two-branch amplitude, i.e. the branch pair's transition
probability. Using a transition probability as the weight is the record's own idiom rather than an
invented functional. **A checker should press exactly here**, because this is the one place where
"derived" shades into "chosen", and it is the place a producer candidate would have to defend.

---

## 6. WHERE THE COHERENCE ORIGINATES — AND WHY THE SYMMETRIC ANSWERS WERE EMPTY

### 6.1 The origin

```text
COHERENCE ORIGIN = THE RATIFIED LAW'S SECTOR CONTROL.
Member 10 [4243,5977) makes the transition a CONTROLLED operation whose control is the source
sector.  Slicing with the ratified source state carries the control's weight kappa_ch into the
branch algebra as the amplitude D.  The correlation the read-off needs is manufactured by the
LAW, not by an author, and not by any coherence the sector-diagonal source state lacks.
```

### 6.2 Why every previous canonical attempt came out empty

Two states on the branch factor are canonical, and both are contentless — a fact worth recording
because it is what made the gap look like a gap of law.

```text
THE PRODUCT OF BRANCH TRACES        full translation invariance forces Haar_Y by uniqueness,
                                    and Haar_Y(Z_N) = 0 — the phase averaged away.
THE DOUBLING'S OWN TRACE STATE      member 06 [6431,7014) makes B an algebra tensored with its
                                    OPPOSITE.  The state a trace induces on such a doubling is
                                    omega_tau(f_+ . g_-) = tau(f g), which on characters is 1 when
                                    the indices cancel and 0 otherwise — exactly the branch
                                    DIAGONAL, member 11's positivity counterexample.  There
                                    Z_N = 1 identically: no relative phase to average.
```

```text
** BOTH CANONICAL ANSWERS ARE CONTENTLESS, FROM OPPOSITE ENDS, AND FOR THE SAME REASON:
   each is fixed by a MAXIMAL SYMMETRY, and a maximal symmetry cannot see a phase. **
```

**The trace's own amplitude breaks that symmetry, and the physics does the breaking.** `D` is not
symmetric under the full translation group and is not the diagonal state: it is tilted by
`kappa_ch` and indexed by the law's own character sum. **So the symmetry-versus-content tension that
looked like a reason to add a primitive is resolved inside the sealed corpus** — the law is already
asymmetric, and the producer only has to use its output instead of reaching for a canonical state.

---

## 7. WHAT THIS HUNT DOES NOT SAY

```text
IT DOES NOT build, propose, prefer or adopt a producer, rule, channel, state or measure.  Section 5
   is a SHAPE, exhibited because the assignment asks feasibility to be shown by exhibiting one.
IT DOES NOT claim the package passes SM-1..SM-8.  It shows the branch factor is reachable from the
   trace and that SM-2 and U1 reality hold for the shape.  SM-3, SM-5, SM-6, SM-7 and SM-8 are a
   CANDIDATE BUILD's burden, and SM-7 is the heavy one (section 5.3).
IT DOES NOT claim d_effect and d_domain are complete.  They remain gaps of construction; what this
   relay removes is the gap of LAW at the branch factor.
IT DOES NOT prove non-derivability of anything, and claims no such proof.
IT DOES NOT reopen member 03's correction or member 11's void.
IT DOES NOT evaluate kappa_ch, N, any expectation, or any downstream quantity.
```

---

## 8. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the 16 closed members; member 03's corrected direction and its withdrawal of my contradiction
  ruling; member 11's void and its positive countermodels; the four executable maps and Cert_P5;
  the frozen tuple under the overlay and DoR-013; the ratified law, F_N and the phase identity;
  kappa_ch as a SYMBOLIC ratio; SM-1..SM-8; the port's fields; the mandate's object clause and
  permitted authored core; all charter fences.

DERIVED HERE:
  the correction to the assignment's stated obstruction — sector-branch CORRELATION, not
    cross-sector coherence, is what the mixed observable needs;
  the trace slice D = (omega_S tensor id_B)(F_N) and its identification as the trace's own
    two-branch amplitude carrying the sector weight;
  the branch-state shape, its positivity, normalization and U1 compatibility, and its symbolic
    influence expectation;
  the identification of the doubling's canonical trace state with the branch diagonal, and the
    account of why both canonical states are contentless;
  the observation that my own V002 collapse theorem does not quantify over this construction.

AUTHORED OR SELECTED HERE:
  nothing.  The functional form at Step 3 is IDENTIFIED as the place an authored core would sit in
  a candidate build; it is not adopted, and no candidate is proposed.  No rule, producer, state,
  measure, certificate content, origin-family member, scale, selector, entry or status is chosen.

NOT CONSUMED:
  kappa_record, alpha, any battery output, any downstream score, any measured datum, any member
  selection, any fixed point, any end test, any entry addendum, any freeze.
```

## 9. FLATTENING CHECK

- **A stated obstruction was not flattened into a true one** (section 3): the sealed half is
  carried, the inferred half is corrected with the reason.
- **A theorem's quantifier was not flattened away**: my V002 result is about unitary-word
  conditionals and is not claimed to cover this construction — nor is it treated as refuted.
- **An observable was not flattened into a state** (section 4.2): `D` is what the trace emits; the
  state is what a rule would make of it.
- **A derivation was not flattened over a choice** (section 5.3): the amplitude is derived, the
  functional form is authored, and the boundary is named rather than blurred.
- **A shape was not flattened into a candidate** (section 7).
- **Feasibility was not flattened into a passed certificate**: SM-3, SM-5..SM-8 remain a build's
  burden.
- **Corroboration was not flattened into proof**: member 03's matching weight-times-complement form
  is cited as independent corroboration of a shape, not as a second derivation of mine.
- **A symbolic ratio was not flattened into a value** anywhere in this artifact.

## 10. CUSTODY

- **BUILDER NEVER VERIFIES OWN WORK.** Everything here is CLAIMED until the Codex 2 check.
- Members 12 and 13 are my own lane's outputs, read as SUBJECTS; where their content is
  load-bearing the witness cited is the opposite lane's independent adoption.
- **No register, plan, tracker, or git action of any kind, read-only included.**
- Archive-side only: one artifact and one generator in `workspace/`, both sealed.

---

## 11. FINAL LINES

```text
DIRECTION_CARRIED = BUILD-FROM-TRACE-PRODUCER (Q-988).  Member 03's correction carried in full; my
     1081 contradiction ruling WITHDRAWN and not reopened.  SM-6 not relaxed; authorship alone is
     NOT called provenance.

LATENT_ROUTE = EXHAUSTED (result: IT OPENS, with the boundary datum as a producer input).
     BOUNDARY + TRACE PRODUCE BUT DO NOT DETERMINE (section 4.3): they fix the amplitude D with no
     freedom at all -- its only ingredients are the ratified sector weight and the entered boundary
     datum -- and D registers the coupling; what they do not fix is the passage from an AMPLITUDE
     to a WEIGHT, since D is neither positive nor normalized and no sealed row converts one into
     the other.  So the still-continuum is not closed by boundary-plus-trace; it is RE-BASED on an
     object that already carries the coupling, and its degenerate members -- the weights that
     ignore D -- are exactly the contentless canonical states the earlier relays kept reaching.
     The assignment's stated obstruction is corrected
     rather than broken: the read-off's mixed observable pairs the DIAGONAL sector projectors with
     a branch observable (member 10 [5939,6270)), so what the phase needs is SECTOR-BRANCH
     CORRELATION, not cross-sector coherence inside the source.  A sector-diagonal source state
     supports that correlation — member 03's own structural witness is such a state.  My V002
     collapse theorem does not obstruct it either: that theorem quantifies over unitary-word
     conditionals, and this construction is outside its family.

FEASIBILITY = FEASIBLE-FROM-SEALED.
     PRODUCER SHAPE: slice the ratified mixed observable with the ratified source state to get the
     trace's own two-branch amplitude D := (omega_S tensor id_B)(F_N) = (1 - kappa_ch) + kappa_ch
     Z_N, an element of B; take the branch state to be the amplitude's transition probability
     against the branch algebra's canonical trace state, mu := |D|^2 Haar_Y / Haar_Y(|D|^2); build
     the joint by the trace's own maps.  POSITIVE by construction, NORMALIZED with
     Haar_Y(|D|^2) = (1-kappa_ch)^2 + kappa_ch^2, and U1-COMPATIBLE because the reality map fixes
     Z_N and kappa_ch is real.
     COHERENCE ORIGIN: THE RATIFIED LAW'S SECTOR CONTROL (member 10 [4243,5977)) — the transition
     is controlled by the source sector, so slicing carries the control's weight kappa_ch into the
     branch algebra.  The correlation is manufactured by the LAW, not by an author.
     THE AUTHORED CORE, DISCLOSED: the functional form at Step 3.  D is derived; choosing the
     transition probability as the weight is a RULE, and in a candidate build it must be frozen
     before output and ledgered — that is where SM-7 will bear hardest and where a checker should
     press first.

PHASE_CONTENT = NONTRIVIAL-POSSIBLE, and now REACHED FROM THE TRACE rather than merely permitted.
     Symbolically, mu(Z_N) = kappa_ch (1 - kappa_ch) / [(1-kappa_ch)^2 + kappa_ch^2], strictly
     interior to the sealed interval away from the sector weight's extremes.  ** Member 03's
     independent structural witness produces the same weight-times-complement shape **, which is
     the strongest corroboration available to a relay that computes nothing.  Positivity does NOT
     force the trivial-phase locus, and neither does from-trace descent; what forced the empty
     answers before was SYMMETRY.  Both canonical branch states are contentless from opposite ends
     — the product of branch traces gives expectation zero (the phase averaged away), and the
     doubling's own trace state IS the branch diagonal (section 6.2) where Z_N = 1 identically (no
     relative phase to average).  The trace's amplitude is tilted by kappa_ch and indexed by the
     law's own character sum, so the physics does the symmetry breaking that an author would
     otherwise have to.

NEW_PRIMITIVE_GUARD = CLEAN.  The latent route was argued first and it opened; NO non-derivability
     proof is claimed and NEEDS-NEW-PRIMITIVE is NOT concluded.  What a new primitive would buy
     that a from-trace derivation cannot: nothing this relay can identify at the branch factor —
     which is the honest answer to the assignment's question, and the reverse of the draft I was
     holding before the assignment was replaced (section 0.1).

IMPLICATION = BUILD-CANDIDATE.  The next step is a producer candidate build — construction relay,
     ledgered, checked — carrying the Step 3 rule as its disclosed authored core, frozen before
     output, and facing SM-3, SM-5, SM-6, SM-7 and SM-8 in full.  d_effect and d_domain remain
     gaps of CONSTRUCTION; what this relay removes is the gap of LAW at the branch factor.

SM6_CARRIED = PRODUCER-CONDITION.
VERDICT = FEASIBILITY-MAPPED (implication stated).
CLOSURE = declared-first (byte 0; end byte 00008468 computed as a fixed point on bytes; 16 members;
     0 live; 10 pinned spans recomputed at their paths; residue scan 0 hits from its own run)
PROSE_DIGESTS = 16/16 STRICT==STABLE (every digest computed at its path)
PINNED_SPANS = 10/10
NO_NUMERIC_PATH = CLEAN (the generator scans its own code; nothing in this relay was computed)
RESIDUE_GREP = CLEAN (27 own patterns; 0 operative hits)
OUTPUT_INSPECTION = NONE-CERTIFIED.  kappa_ch was carried symbolically exactly as member 07
     [28638,29078) carries it; no ratio, expectation or downstream quantity was evaluated or opened.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN.  "Derived" is confined to D and its properties; "authored" is applied to
     the Step 3 form without softening; "feasible" means a shape exists whose branch factor comes
     from the trace, NOT that a package has passed SM-1..SM-8; "corroboration" is used of member
     03's matching form and is not called a proof.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**What changed, and why it matters.** Six relays treated the branch state as something to be
supplied, then argued about whether the supply could have provenance. The corrected direction asked
for a producer instead — and once the question is asked that way, the trace turns out to emit the
correlation-carrying object already: the sector is the ratified law's control, and slicing the
ratified mixed observable with the ratified source state carries that control's weight into the
branch algebra. **The gap was never a gap of law. It was a gap in what the lane had thought to
slice.** What remains authored is one functional form, disclosed, and one construction relay's worth
of certificates — which is a materially smaller thing than the new primitive I was about to ask a
principal to ratify.

All findings **CLAIMED** until the opposite-lane check.
