# STAGE 8 — ADVERSARIAL AUDIT OF THE WEIGHT-SUMMED RE-QUANTIFICATION OF THE WALL'S FOUR QUANTITIES — S9AD AUDIT V001

## ADVERSARIAL AUDITOR — CODENAME WSR-AUDIT — COMMISSION S9AD — [SEALED]

Date: 2026-08-15 (session CDT early). Target:
`STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md` (6e5762a8..., FULL digest
recomputed at path, sidecar MATCH, verified BEFORE opening). Default
posture: REFUTE; the outcome was not told to me and was not read before my
verdict was fixed (§2).

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until registered.

Fences held: EXACT SYMBOLIC ONLY — the build's CAS battery re-run VERBATIM
in a fresh venv (`wsrauditvenv`, sympy 1.14.0, session scratchpad) plus my
own adversarial battery, both reproduced verbatim in §8 with output
verbatim; no floats as ground; no measured constant; no value frozen
(Lambda, M, eps FORMAL throughout). No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened (matched by
FILENAME ONLY in sweeps, never opened). "Q-..." tokens
EXPECTED-UNLOCATABLE per the tasking; noted, never chased, nothing
defaulted. No git action; no existing file edited; ONE output (this
artifact) plus its seal sidecar at the commission-distinct path, probed
ABSENT at session start AND re-probed ABSENT immediately before write
(2026-08-15 00:26:14 -0500). SWEEP CUTOFF DECLARED (§9). Corpus roots
both (`workspace`, `supervision`), minus fenced classes.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS. The four summed-quantifier verdicts STAND as built, each re-derived independently: (1)/(2) tr H_A^Sigma = 0 and ||H_A^Sigma||_2^2 = 0 — the Hermiticity kill re-proved at the OPERATOR grade for GENERIC Hermitian C (no idempotency needed; my A1), the per-factor odd-kill re-derived (A6), and the per-pair member shape C(G_12 + G_21^dag)C confirmed generically NON-Hermitian (the contrast is structural, not notational); (3) ||K_H^Sigma||_2^2 <= M^2/eps^2 — every chain step verified at bytes: the discharged bound sup_n ||X_n||_2 <= ||X||_2 <= M is of record at e5b0879b :148-152 (REM, CONFIRMED 89641f76, "on the closed pair polydisc at radius eps", identity-grade core with estimate-grade e-i/e-ii and sealed overturn conditions — the build inherits the grade VERBATIM, no upgrade), the identification D-NOW-1 is check-verified at :143-147, the audit cbe6ff4f is CONFIRMED at its own §0, and the Cauchy slot extraction is exact (re-verified at degree 6, A2) — the consumption is direct, the allowed direction of the one-way quantifier record, NO V-2 bridge; (4) tr K_H^Sigma FAILS-AT the log threshold — the degree -2 start is REM §3.2 at bytes, the V_2 (x) V_2 leading stratum and the per-factor floors are REM §2.2-§2.3 at bytes (weight-summed statements, correctly applied to the weight-summed object), the J-vertex longitudinal intraband part "-k_hat_x C(k) — the smooth vertex has NO coincidence zero" is REM :143-145 verbatim, and the angular kill SURVIVES the spinor structure the build's WS5 toy omitted (tr[alpha_x C(k)] = -2 khat_x exact, angular integral ZERO — my A3: C(k)'s own khat-dependence feeds the trace only through the first moment), landing AT p = 3 = log Lambda, threshold strict (A7) — FAILS-AT is forced on the displayed assets, honestly held (no divergence exhibited, UNDECIDED). THE NAMED KILL WAS HUNTED AND IS ABSENT: no summed result is quoted at the per-composite or per-pair quantifier anywhere in the artifact — §7/WS9 (squares are parity-blind) is itself the proof the build cannot transport, the flag PER_COMPOSITE_WALL = UNTOUCHED is accurate, T_n/B-L2*/alpha' = 1/4/MO-2/MO-3 verified moved nowhere, and the C4/REM kill mechanics are consumed at EXACTLY their sealed quantifiers (m_0/m_1 kills only on weight-summed factors; REM floors only on the weight-summed object; the per-composite contrast separately re-pinned). CONSUMER HONESTY HOLDS: witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED attaches to the SECOND-ORDER Carleman segment (e5b0879b Task B / R.3'(c) at bytes), NOT to the first-order X bound the build consumes — the witness content the build DOES consume (summed HS uniformity) is the one delivered at alpha' = 1/4 of record; no C-L1/C-L2 dependence enters the delivered chain at all (U(0) is consumed as itself, never resummed to P — the kills need no collapse). CAS: the 14-check battery re-ran VERBATIM, fresh venv, 14/14 PASS, OUTPUT BYTE-IDENTICAL to §8 of the build including the exhibit line; my own battery 8/8 PASS (one auditor-side expectation corrected and disclosed, §8). THE CORRECTIONS (none touches a summed-side verdict): COR-1 — the comparative framing "SHRINKS THE FOURTH'S SHORTFALL FROM TWO-PLUS POWERS TO A LOGARITHM" (§0) and "the summed level pays TWO powers the per-composite level cannot ... where the per-composite level provably refuses it" (§0, §5.3) OVERSTATE the record by one power for the even trace: of record (d66a922c o-2-OBS at bytes) the per-composite EVEN sector's angular kill ENGAGES at (2,0)/(0,2) — "the odd-trace leg engages here — one power, to degree -2 — still ONE-plus power short" — and (1,1) sits at -2; the o-2-OBS REFUSAL is the ODD (1,0)/(0,1) stratum only, whose summed counterpart is killed by Hermiticity/evenness, not by the angular kill; the of-record per-composite shortfall of tr K_H is therefore ONE-plus power (linear budget, A8), and the summed net gain on the even trace is ONE power (linear -> log), the "two powers the per-composite level cannot pay" being one (the m_0 power; the angular power it pays itself at (2,0)); the SUMMED landing at -3 = log and the FAILS-AT verdict are UNAFFECTED (they stand on REM §3.2 + the angular kill alone, and the build's own C-3 concedes the verdict does not rest on the sharpening). Notes at disclosure grade: N-1 WS9's census uses the (1,1) branch-diagonal composite for BOTH diagonal slots — harmless (every branch-diagonal H_A vanishes identically by the A + A^dag shape, my A1), but the displayed tr_lin is not literally the (2,2) member's trace; N-2 sharpening: in the slot model [m^2]K^Sigma = [m^3]K^Sigma = 0 and the composite STARTS at [m^4] (both factors start at m^2 — the double kill), generically nonzero Hermitian (A4) — consistent with and confirming REM's V_2 (x) V_2 coupling order (2,2), unstated in WS2d's evenness display. Build hygiene verified: 17/17 seals independently recomputed MATCH; ONE output + sidecar (no stray file); sw-2's parallel-lane claim verified at the cutoff instant by mtime (newest pre-cutoff file IS the named seed member, 23:41:04; nothing between it and 00:02:23); the disclosed harness-form correction is credible and non-substantive (a Python sum() start-value TypeError; the fix is visible in the code; no check condition differs from its output). MY OWN PRE-COMMITTED VERDICT (hashed 3f29c1a1... BEFORE the build was opened, §2) expected exactly this shape at the correct quantifiers — with ONE divergence, resolved AGAINST me at bytes: I expected the summed HS enclosure to be conditional on C-L1/SCAD; the sealed basis had already discharged the first-order bound without either. No kill fired. CONFIRMED-WITH-CORRECTIONS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. The
target's FULL digest was recomputed and sidecar-matched BEFORE the file
was opened (6e5762a8b785f4b7... MATCH). All 17 of the build's consumed
seals independently recomputed from bytes at path this session; 17/17
MATCH sidecars (d66a922c, 4ebe6b8f, 9fdc3d1c, 80db260f, 9a295498,
6997ff61, e5b0879b, cbe6ff4f, 48ecdabe, 798de0dd, c944a901, 2e4e5163,
6e81ae92, a7969f0c, 789338ad, 0a10c030, 549362d4). Additionally verified:
STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md recomputes to 89641f76... —
matching the "REM, CONFIRMED 89641f76" citation inside e5b0879b :148, its
§0 reading "NET_VERDICT = CONFIRMED(PROVED at exactly the licensed
strength...)" at bytes. NONE UNVERIFIABLE.

Load-bearing spans re-read at path THIS session (independent of the
build's citations): e5b0879b :40-75 (§0), :95-160 (§2.1: the adopted
clause; D-NOW-1 check-verified; the discharged bound with grade and
polydisc at bytes), :420-436 (Task B walls; the SCAD segment witness),
:505-525 (n-1/n-2/n-3 witness dispositions); cbe6ff4f §0 ("All five
audits land CONFIRMED"); 2e4e5163 :40-46 (§0: the double degree-0 kill;
degree -2 start), :135-165 (§2.2: the J-vertex stay "-k_hat_x C(k) — the
smooth vertex has NO coincidence zero"; the segment IBP mechanics),
:166-204 (the per-factor floors; "ODD n DIES EXACTLY"; "There is NO
(c = 1, o = 0) class anywhere"; assembled floors 2o + c >= 4), :255-300
(§2.4 leading V_2 (x) V_2 display; §3.3a-c), :477-486 (e-i/e-ii; the
overturn conditions); d66a922c :238-274 (s-v: KF2 with "only where the
band-trace integrand is ODD under the joint inversion"; o-2-OBS COMPLETE
including the even sector's ENGAGED kill and the (1,1) double-stay at
degree -2 — the COR-1 ground), :686 (KF2 check line); a7969f0c :52, :383,
:793 (SURVIVOR_FATE = CANCELS; "-i V_2(x)/(2|k|)" the U(0) intraband
falloff at bytes); 6e81ae92 :337-366, :437 (the X display; I13/I14; the
sigma_intra display); 9a295498 :47-49, :256-262 (the FHB budgets
12 sqrt2 n^{7/2}/ell, E_n = 6n^4/ell^2 EXACT); 6997ff61 :78-79, :205,
:822 (beta C_n beta = Q_n - C_n - (rank <= 4)); 0a10c030 §0 (the
split-by-object ruling; the one-way bridge); 48ecdabe :42, :670 (route
(b) named: "weight-summed re-quantification onto the record strata");
80db260f §0/§3.3 (the op-grade chain the build transports); 798de0dd §2.1
cited via the build (F~ = F forced; audit c944a901 sidecar-verified).

---

## 2. INDEPENDENCE: THE VERDICT FIXED AND HASHED BEFORE THE BUILD WAS OPENED

Before the target's bytes were read, the summed question was worked
independently from the sealed stock (d66a922c, 0a10c030, 6e81ae92,
2e4e5163, 80db260f) and a pre-commitment was written and hashed:

```text
sha256(WSR_AUDIT_PRECOMMIT_V001.txt) =
3f29c1a17fee1b1bcbb16208969ec4bfa01f07bf955d57b03d68e2b864488500
fixed at 2026-08-15T05:13:35Z; the target's first read: after 05:14Z.
(The file is in the session scratchpad; its content is summarized here
verbatim-in-substance.)
```

Its fixed expectations: E-1 the summed odd content is ZERO exactly (the
weight kill at its native quantifier); E-2 a summed HS-grade enclosure is
derivable from sealed summed-side stock, which I expected to be
CONDITIONAL on C-L1/SCAD; E-3 nothing transports per-composite; six
pre-named kills K-1..K-6 (summed-quoted-per-composite; kills wider than
their quantifiers; consumer dishonesty; V(0) = I without the C-L1 caveat;
float/seal/CAS defects; the summed zero quoted against the doubled
strata). OUTCOME AGAINST THE PRE-COMMITMENT: E-1 and E-3 confirmed; E-2's
conditionality was WRONG-AT-BYTES — the sealed basis (REM CONFIRMED
89641f76 + e5b0879b + cbe6ff4f) had already discharged the first-order
bound with no C-L1 dependence: the delivered chain never uses the
collapse (U(0) enters as itself). None of K-1..K-6 fired (§3-§6). The
default-REFUTE posture was therefore overcome by the bytes, not by the
build's say-so.

---

## 3. ATTACK (1) — EVERY DERIVED SPAN RE-DERIVED; THE QUANTIFIER DISCIPLINE HARDEST

```text
d-1/d-2 (factorization; self-performed X-5): re-run verbatim (WS2a/WS2b
     PASS) — bilinearity plus m_0^2 = 0; also the audit-side ground that
     the (mu,lambda) member of the assembly at (+,-) is EXACTLY the
     wall's composite shape C(G_{+-} + G_{-+}^dag)C: the summed object
     is the faithful all-pairs re-quantification, not a substitute.
k-1 (Hermiticity): STRENGTHENED — my A1 proves C(A + A^dag)C Hermitian
     for GENERIC Hermitian C (the build's toy used C = diag(1,1,0)
     only); the operator-grade zero needs C = C^dag and the equal-source
     pairing, nothing else; F~ = F is forced of record (798de0dd §2.1,
     audit-confirmed). And the member shape C(G_12 + G_21^dag)C is
     generically NOT Hermitian (A1): the summed/per-pair contrast is
     structural. H_A^Sigma = 0 at the operator level; the two zero
     verdicts follow identically.
k-2 (evenness): re-derived (WS2d verbatim; A6 the moment ledger). N-2
     sharpening: the slot-model composite starts at [m^4] (A4) — the
     double kill's own coupling-order signature, consistent with REM's
     V_2 (x) V_2 at order (2,2).
h-1..h-4 (the HS enclosure): every step checked at bytes — the discharged
     bound VERBATIM at e5b0879b :148-152 with its grade and its polydisc;
     the identification at :143-147 ("all a-orders included"); REM §3.5's
     dominated series carries the closed polydisc, so the equal-source
     circle |a| = eps lies inside the sealed sup's domain; the Cauchy
     extraction is exact (WS3a verbatim; A2 at degree 6); HS Pythagoras
     exact (WS3b). The op-grade transport ||K^Sigma_n||_op <= 2 re-checked
     against the WBC chain (l1-tightness sum|w_l| = 1 re-derived, A6;
     ||J||_op <= 1 sealed r-3 P-2). GRADE HONESTY: the flag carries REM's
     e-i/e-ii and the sealed overturn conditions — read at REM :477-486 —
     with no upgrade; ray class CONDITIONAL(P-1) carried.
t-1..t-3 (the trace): the degree -2 start is REM §3.2 VERBATIM; the
     "ONLY degree -2 assembled stratum" argument re-checked against the
     per-factor floors at REM :166-204 (a lone coincidence forces c >= 2
     or interband; "There is NO (c = 1, o = 0) class anywhere" — a
     weight-summed statement applied to the weight-summed object: the
     CORRECT quantifier); the J-vertex intraband part "-k_hat_x C(k)"
     REM :143-145 verbatim; the U-side angular-scalar V_2 subsidiary
     a7969f0c :383/:793 verbatim. THE ANGULAR KILL: verified BEYOND the
     build's toy — A3 carries the spinor structure the WS5 toy dropped:
     tr[alpha_x C(k)] = -2 khat_x EXACT and the full angular integral of
     the traced leading integrand is ZERO (C(k) is NOT inversion-even,
     but the trace output is pure first-moment). The landing AT log
     (p = 3) and its non-crossing: A7. The FAILS-AT is forced on the
     displayed assets: degree -3 strata exist (o = 3, c = 0; c-bearing
     routes), no sealed trace-side stratum-sum or rate crosses log.
s-1/s-2 (involution; stencil): WS6/WS7 re-run verbatim; A5 re-derives
     the stencil identity by the anticommutation route (no matrix
     expansion) — same c(th) = sin^2(sqrt2 th/2), a square; the
     involution facts at 6997ff61 bytes; GRADES-not-kills typing
     preserved (RA4b/CA3).
WS8/§6 (the sign ledger): re-run; exact for Hermitian K^Sigma; the
     one-sided enclosure e_2 >= -M^2/(2 eps^2) follows from (3) alone;
     "PARTIAL, one-sided by construction" is the honest class.
THE QUANTIFIER SWEEP (the named kill): every claim sentence of §0, §3-§7
     and the flag block was read against the one-way record (0a10c030 at
     bytes: per-pair feeds summed, NEVER the reverse; F'-14; V-2). NO
     span quotes a summed decision at the per-composite or per-pair
     quantifier. The verdicts are stated OF the summed object with
     distinct names (^Sigma throughout); §7 (q-1/q-2/q-3) states the
     non-transport explicitly and WS9 PROVES its mechanism (squares are
     parity-blind — re-run PASS); PER_COMPOSITE_WALL = UNTOUCHED is
     accurate at bytes (d66a922c/4ebe6b8f verdicts unmoved; T_n
     untouched; no falsifier V-1..V-6 engaged; V-5's kill-before-the-
     norm is the discharged chain's own deciding step, e5b0879b :158).
     THE NAMED KILL IS ABSENT.
```

---

## 4. ATTACK (2) — THE C4/REM KILL MECHANICS AT THEIR QUANTIFIERS, NOT WIDER

```text
m-1 The m_0 = m_1 = 0 kills are applied ONLY per weight-summed factor
    (U(0), F) — their sealed quantifier (REM §2.3/§3.2; e5b0879b :150
    "the C4 kill operating BEFORE the norm"). NOWHERE is a weight-sum
    kill applied to a single composite or a per-branch object; the
    per-composite contrast (KB4 doubling) is separately re-pinned with a
    displayed NONZERO exhibit (WS2f — re-run, exhibit line identical).
m-2 REM's (o,c) floors and "NO (c = 1, o = 0) class" are stated for the
    weight-summed X and consumed for K^Sigma = [a^1]X only — d-1 makes
    the object identity exact. The K-channel itself (d66a922c :259-261)
    marks the same floors as weight-summed statements refused
    per-composite: the build uses them in EXACTLY the direction the
    record types.
m-3 The angular kill (KF2) is consumed where its own sealed condition
    holds ("only where the band-trace integrand is ODD under the joint
    inversion", d66a922c :243-245): the summed leading stratum's traced
    integrand is first-moment-pure (A3), the per-composite (1,0)
    integrand is even (o-2-OBS) — both directions correctly placed.
    The one framing defect this attack surfaced is COR-1 (§7): a
    COMPARATIVE overstatement, not a misused kill.
m-4 B-L2*/REM's M-sup is consumed AS the estimate's own object (the
    weight-summed X), never extracted from: no summed-to-per-pair
    passage exists in the artifact (checked span by span).
```

---

## 5. ATTACK (3) — CONSUMER HONESTY OF THE SUMMED ENCLOSURE

```text
c-1 WHAT THE SUMMED ENCLOSURE DOES: decides the summed object's four
    quantities (two zeros; one carrier-free HS enclosure; one log-thin
    FAILS-AT). WHAT IT DOES NOT DO — held to sealed conditionals at
    bytes: it does NOT touch the wall's per-(n,cell) four quantities
    (UNDECIDED of record, d66a922c/4ebe6b8f — re-affirmed in the build's
    own §2.1/§7/flags); it does NOT move T_n, B-L2*, alpha' = 1/4, MO-2,
    MO-3, or any sealed identity (verified: each is cited at its own
    quantifier and no flag is flipped); it does NOT claim the odd
    summed zeros control the composite's DOUBLED odd strata (the
    contrast is displayed instead).
c-2 THE SCAD QUESTION (my strongest pre-committed attack, dissolved at
    bytes): SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED attaches to the
    SECOND-ORDER Carleman segment — e5b0879b Task B (:420-436) and n-2
    (:516): "R.3'(c) is now THE one live analytic wall of the summed
    horn". The build consumes ONLY the first-order X bound, whose
    witness (SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED, n-1) has its
    content DELIVERED at alpha' = 1/4 of record with the audit
    CONFIRMED. The build touches no witness and says so; the standing
    segment witness is UNTOUCHED by every delivered claim (the build
    never enters the segment). HONEST.
c-3 NO COLLAPSE DEPENDENCE: the delivered chain nowhere uses the C4
    promotion/resummation U(0) -> P or V(0) = I; U(0) is consumed as
    the weight-summed operator itself. The C-L1/C-L2 uncertified layer
    — fatal to the per-composite routes (WBC F3) — is NOT a premise of
    any delivered claim. Checked span by span; my pre-committed K-4
    kill has no target.
c-4 GRADE HONESTY: "DERIVED(enclosure; grade inherited)" carries REM's
    estimate-grade steps and overturn conditions BY NAME in the §0
    verdict AND the flag; the two zeros are identity-grade and correctly
    so classed; the trace verdict is FAILS-AT with NOT REFUTED stated.
    Verdict classes match the record's own conventions (e5b0879b §4.3).
```

---

## 6. ATTACK (4) — SEALS, BYTES, CAS

```text
s-A Seals: target verified before opening; 17/17 re-verified
    independently; REM-check 89641f76 verified against its in-text
    citation; every load-bearing span re-read at path (§1) — no quoted
    byte was found altered, truncated, or quantifier-shifted, except
    the COR-1 aggregation (§7).
s-B CAS: the 14-check battery extracted from the artifact and re-run
    VERBATIM in a fresh venv (sympy 1.14.0): 14/14 PASS, output
    BYTE-IDENTICAL to the artifact's §8 block (diff empty), including
    the WS2f exhibit line. The disclosed harness-form correction is
    credible: the committed code carries the sp.zeros(3,3) start values
    whose absence produces exactly the disclosed TypeError; no check
    condition can have changed the recorded output (it reproduces).
s-C My battery (8 checks, §8): 8/8 PASS — A1 generic-C Hermiticity +
    member non-Hermiticity; A2 Cauchy at degree 6; A3 the spinor-traced
    angular kill; A4 the [m^4] start; A5 the stencil by anticommutation;
    A6 the moment/l1 ledger; A7 the log landing not crossed; A8 the
    COR-1 ground (p = 2 carrier integral linear). One auditor-side
    expectation error in A4's first form, corrected and disclosed in
    the script header (the failure EXPOSED the double-kill start —
    informative, not suppressed).
s-D Build hygiene: ONE output + sidecar confirmed (filename sweep);
    write time 00:09:44/56, after its declared cutoff (in order);
    sw-2's claim verified by mtime: the newest pre-cutoff file IS
    STAGE8_MO2_KAPPA_RATE_S9AD_AUDIT_V001.md(+sidecar, 23:41:04);
    nothing between 23:41:05 and 00:02:23.
```

---

## 7. THE CORRECTIONS

```text
COR-1 (comparative framing; does not touch any summed-side verdict):
  The §0 headline "SHRINKS THE FOURTH'S SHORTFALL FROM TWO-PLUS POWERS
  TO A LOGARITHM", §0's "the summed level pays TWO powers the
  per-composite level cannot ... AND the S1 odd-spinor-trace leg ENGAGES
  ... where the per-composite level provably refuses it", and §5.3's
  "(two powers relative to the per-composite leading count)" overstate
  the record by ONE power for the even trace. OF RECORD (d66a922c
  o-2-OBS at bytes :268-271): the per-composite EVEN sector's angular
  kill ENGAGES at (2,0)/(0,2) — "the odd-trace leg engages here — one
  power, to degree -2 — still ONE-plus power short" — and the (1,1)
  double-stay sits at degree -2 with "same shortfall". The o-2-OBS
  REFUSAL is the ODD (1,0)/(0,1) stratum only — a stratum whose summed
  counterpart is killed by Hermiticity/evenness, not by the angular
  kill. Correct comparison for tr K_H: per-composite shortfall ONE-plus
  power (linear budget, my A8); summed shortfall log; net gain ONE
  power. Of the "two powers", the per-composite level cannot pay one
  (the m_0 power); the angular power it pays itself at its (2,0)
  strata. WHAT SURVIVES UNCHANGED: the summed landing at effective
  degree -3 = log Lambda, the strict threshold, the FAILS-AT verdict,
  the log-thin residue naming (MO-3's shape), and the genuinely NEW
  summed-only fact that the kill engages at the summed LEADING stratum
  (per-composite the leading strata sit lower and the odd-sector
  leading stratum refuses it). The build's own WS5/(t-2) text names the
  (1,0) stratum correctly; the defect is the AGGREGATION of that
  contrast into the even trace's improvement arithmetic. Required
  rewording (for the registrar's margin, no re-issue forced): "from a
  LINEAR shortfall (per-composite even trace, o-2-OBS) to a
  LOGARITHMIC one; the odd trace's two-power shortfall is not shrunk
  but ANNIHILATED (the sector is zero)."
N-1 (note): WS9 uses the (1,1) branch-diagonal composite for both the
  (1,1) and (2,2) census slots. Harmless — every branch-diagonal H_A
  vanishes identically by the A + A^dag shape (A1) — but the displayed
  tr_lin is not literally the (2,2) member's trace.
N-2 (note, sharpening): in the slot model [m^2]K^Sigma = [m^3]K^Sigma
  = 0: the summed composite STARTS at insertion order 4 (both factors
  start at m^2 — the double kill), generically nonzero and Hermitian
  (A4). Consistent with REM's V_2 (x) V_2 coupling order (2,2);
  unstated in WS2d's evenness display; no build claim contradicted.
```

---

## 8. THE CAS BATTERIES (VERBATIM) AND OUTPUT (VERBATIM)

Fresh venv `wsrauditvenv` (sympy 1.14.0) under the session scratchpad.
RUN 1 — the build's battery, extracted verbatim from the target's §8
(awk fence extraction, 261 lines): output 14/14 PASS, BYTE-IDENTICAL to
the target's recorded block (diff empty; not reproduced here — it is the
target's own §8 text, verified identical). RUN 2 — my adversarial
battery, verbatim:

```python
# WSR-AUDIT adversarial battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
# wsrauditvenv). Checks the BUILD DID NOT RUN, hunting its named kills.
# A1 operator-grade Hermiticity: C(A + A^dag)C Hermitian for GENERIC Hermitian C
#    (the build used only C = diag(1,1,0)); and the wall's per-pair member shape
#    C(G_{mu lam} + G_{lam mu}^dag)C at (mu,lam) = (+,-) IS the wall composite form.
# A2 Cauchy slot identity beyond the cubic schema (degree-6, generic complex).
# A3 the angular kill WITH the spinor structure the build's WS5 toy omitted:
#    tr[alpha_x C(k)] = -2 khat_x EXACT (KF2 re-derivation); its angular average 0;
#    tr C(k) = 2 (the C(k) factor is NOT even under khat -> -khat, but the TRACE
#    output is pure first-moment: the kill survives the omitted structure).
# A4 the DOUBLE-KILL START, sharpened: in the slot model BOTH factors start at
#    m^2 (m_0 = m_1 = 0 per factor), so K^Sigma has [m^2] = [m^3] = 0 and starts
#    at [m^4] — generically NONZERO and Hermitian: the two zeros are sector
#    kills, not a total vanishing, and the surviving content is exactly the
#    V_2 (x) V_2 coupling order (2,2) of REM §3.3. (First run of this check
#    FAILED on the auditor's own wrong expectation [m^2] != 0; the failure
#    exposed the double-kill start and was corrected — disclosed.)
# A5 independent stencil re-derivation via the anticommutation route:
#    e^{-icS} J = J e^{+icS} ({S,J} = 0), summed: J * [m-cos profile] with the
#    sin-profile ZERO by w_+ = w_- — same c(th) = sin^2(sqrt2 th/2).
# A6 the moment ledger against the l1 ledger: m_0 = m_1 = m_3 = 0, m_2 = -1,
#    m_4 = -2, sum|w| = 1 (op-chain constant), and the survivor-census arithmetic.
# A7 the summed trace landing arithmetic: deg -2 start + ONE angular power lands
#    AT log (p = 3) NOT past it — the build's FAILS-AT is forced, not optional:
#    log Lambda -> oo (formal limit nonfinite: the threshold really is not crossed).
# A8 per-composite even-trace state re-derived from o-2-OBS bytes: (2,0) at deg -1
#    engages the kill -> deg -2 -> carrier integral LINEAR (Lam - 1), i.e. the
#    of-record per-composite shortfall for tr K_H is ONE-plus power, not two-plus
#    (the correction to the build's comparative headline).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

def cmat(pre, n, m=None):
    m = n if m is None else m
    return sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}_{i}{j}', real=True)) \
         + Im*sp.Matrix(n, m, lambda i, j: sp.Symbol(f'{pre}i_{i}{j}', real=True))

def Zq(M):
    return sp.expand(M) == sp.zeros(*M.shape)

# ===== A1 — operator-grade Hermiticity with GENERIC Hermitian C =====
Cr = sp.Matrix(3, 3, lambda i, j: sp.Symbol(f'cr_{min(i,j)}{max(i,j)}', real=True))
Ci = sp.Matrix(3, 3, lambda i, j: sp.sign(j - i)*sp.Symbol(f'ci_{min(i,j)}{max(i,j)}', real=True))
Cg = Cr + Im*Ci                      # generic Hermitian C (not assumed idempotent)
A = cmat('a3', 3)
KH = sp.expand(Cg*(A + A.H)*Cg)
G12 = cmat('g12', 3); G21 = cmat('g21', 3)
member = sp.expand(Cg*(G12 + G21.H)*Cg)   # the wall's member shape at (+,-)
ok("A1 OPERATOR-GRADE HERMITICITY: Cg Hermitian generic (no idempotency) =>"
   " Cg(A + A^dag)Cg is Hermitian IDENTICALLY — the summed kill needs only"
   " C = C^dag and the A + A^dag pairing; and the assembly member"
   " C(G_12 + G_21^dag)C is generically NOT Hermitian (the wall's per-pair"
   " shape keeps its anti-Hermitian sector: the contrast is structural)",
   Zq(sp.expand(Cg - Cg.H)) and Zq(sp.expand(KH - KH.H))
   and (not Zq(sp.expand(member - member.H))))

# ===== A2 — Cauchy slot identity at degree 6 =====
a_, eps_, t_ = sp.Symbol('a'), sp.Symbol('epsilon', positive=True), sp.Symbol('t', real=True)
cs = [sp.Symbol(f'd{k}') for k in range(7)]
f6 = sum(c*a_**k for k, c in enumerate(cs))
cau = sp.integrate(f6.subs(a_, eps_*sp.exp(Im*t_))*sp.exp(-Im*t_)/eps_,
                   (t_, 0, 2*sp.pi))/(2*sp.pi)
ok("A2 CAUCHY SLOT AT DEGREE 6 (generic complex coefficients): the [a^1]"
   " extraction is exact at every polynomial truncation — the schema is not"
   " an artifact of the cubic",
   sp.simplify(cau - cs[1]) == 0)

# ===== A3 — the angular kill WITH the spinor structure =====
s1p = sp.Matrix([[0, 1], [1, 0]]); s2p = sp.Matrix([[0, -Im], [Im, 0]])
s3p = sp.Matrix([[1, 0], [0, -1]]); I2, Z2 = sp.eye(2), sp.zeros(2, 2)
blk = lambda A_, B, Cc, D: sp.Matrix(sp.BlockMatrix([[A_, B], [Cc, D]]))
al = [blk(Z2, s, s, Z2) for s in (s1p, s2p, s3p)]
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
Ck = sp.expand((sp.eye(4) - (kx*al[0] + ky*al[1] + kz*al[2]))/2)
trax = sp.expand((al[0]*Ck).trace())
trc = sp.expand(Ck.trace())
th, phv = sp.symbols('th ph', real=True)
sub = {kx: sp.sin(th)*sp.cos(phv), ky: sp.sin(th)*sp.sin(phv), kz: sp.cos(th)}
ang_tr = sp.integrate(trax.subs(sub)*sp.sin(th), (phv, 0, 2*sp.pi), (th, 0, sp.pi))
ok("A3 THE KILL SURVIVES THE OMITTED SPINOR STRUCTURE: tr[alpha_x C(k)] ="
   " -2 khat_x EXACT (KF2's band trace at s = -1, re-derived from the Dirac"
   " algebra, NOT the toy); tr C(k) = 2; the full angular integral of the"
   " traced leading integrand is ZERO — C(k)'s own khat-dependence (C(-k) !="
   " C(k)) feeds the trace only through the first moment: the WS5 toy's"
   " conclusion holds at the spinor-trace grade",
   sp.simplify(trax + 2*kx) == 0 and trc == 2 and sp.simplify(ang_tr) == 0)

# ===== A4 — the double-kill start in the slot model =====
m_ = sp.Symbol('m_c', real=True)
NN = 3
U_k = [cmat(f'U{k}', 3) for k in range(NN + 1)]
Ph_k = [cmat(f'Ph{k}', 3) for k in range(NN + 1)]
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
lamv = [sp.Integer(0), m_, -m_]
u_l = [sum((lamv[l]**k*U_k[k] for k in range(NN + 1)), sp.zeros(3, 3)) for l in range(3)]
F_l = [sum((lamv[l]**k*Ph_k[k] for k in range(NN + 1)), sp.zeros(3, 3)) for l in range(3)]
C3 = sp.diag(1, 1, 0)
Uw = sp.expand(sum((w_v[l]*u_l[l] for l in range(3)), sp.zeros(3, 3)))
Fw = sp.expand(sum((w_v[l]*F_l[l] for l in range(3)), sp.zeros(3, 3)))
KSig = sp.expand(C3*(Uw.H*Fw + Fw.H*Uw)*C3)
K2 = KSig.applyfunc(lambda e: sp.expand(e).coeff(m_, 2))
K3 = KSig.applyfunc(lambda e: sp.expand(e).coeff(m_, 3))
K4 = KSig.applyfunc(lambda e: sp.expand(e).coeff(m_, 4))
ok("A4 THE DOUBLE-KILL START, SHARPENED: [m^2] K^Sigma = 0 AND [m^3] = 0 —"
   " both factors start at m^2 (m_0 = m_1 = 0 PER FACTOR), so the summed"
   " composite starts at [m^4], generically NONZERO and Hermitian: the"
   " surviving content sits exactly at REM §3.3's V_2 (x) V_2 coupling order"
   " (2,2); the two exact zeros are sector kills, not a total vanishing",
   Zq(K2) and Zq(K3) and (not Zq(K4)) and Zq(sp.expand(K4 - K4.H)))

# ===== A5 — stencil re-derivation via the anticommutation route =====
beta = blk(I2, Z2, Z2, -I2); g5 = blk(Z2, I2, I2, Z2)
S_op = sp.expand(-Im*beta*g5)
c_ = sp.Symbol('c_ph', real=True)
expS = lambda c: sp.expand(sp.cos(c)*sp.eye(4) - Im*sp.sin(c)*S_op)
lhs = sp.expand(expS(c_)*al[0])
rhs = sp.expand(al[0]*expS(-c_))
th0, th1 = sp.symbols('t0 t1', real=True)
prof_cos = sum(w*sp.cos(sp.sqrt(2)*l*(th1 - th0)) for w, l in zip(w_v, [0, 1, -1]))
prof_sin = sum(w*sp.sin(sp.sqrt(2)*l*(th1 - th0)) for w, l in zip(w_v, [0, 1, -1]))
ok("A5 STENCIL, INDEPENDENT ROUTE: e^{-icS} alpha_x = alpha_x e^{+icS}"
   " ({S, alpha_x} = 0), so the summed sandwich = alpha_x [sum_l w_l"
   " e^{i lam_l (th1-th0) S}]; the sin-profile is ZERO by w_+ = w_- and the"
   " cos-profile is (1 - cos(sqrt2 th))/2 = sin^2(sqrt2 th/2): the WS7"
   " identity re-derived without matrix expansion",
   Zq(sp.expand(lhs - rhs))
   and sp.simplify(prof_sin) == 0
   and sp.simplify(prof_cos - sp.sin(sp.sqrt(2)*(th1 - th0)/2)**2) == 0)

# ===== A6 — the moment/l1 ledger =====
lam_n = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
mom = lambda k: sp.nsimplify(sum(w*l**k for w, l in zip(w_v, lam_n)))
ok("A6 MOMENT/L1 LEDGER: m_0 = m_1 = m_3 = 0, m_2 = -1, m_4 = -2,"
   " sum|w| = 1 (the l1-tight op constant), w_+ = w_-; the survivor census"
   " N = 1/2 with c = (1/2, 1/8 x4), sum = 1 — all re-derived",
   mom(0) == 0 and mom(1) == 0 and mom(2) == -1 and mom(3) == 0
   and mom(4) == -2 and sum(abs(w) for w in w_v) == 1 and w_v[1] == w_v[2])

# ===== A7 — the landing is AT log, not past it =====
kk = sp.Symbol('k_r', positive=True); Lam = sp.Symbol('Lam', positive=True)
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
ok("A7 THE LANDING IS AT LOG: the p = 3 carrier integral is log Lam with"
   " lim_{Lam->oo} = oo (formal, nonfinite): the strict threshold (< -3) is"
   " genuinely NOT crossed — FAILS-AT is forced for tr K_H^Sigma on the"
   " displayed assets; no enclosure follows",
   sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo)

# ===== A8 — the per-composite even-trace state of record (the correction) =====
I2p = sp.integrate(kk**(2 - 2), (kk, 1, Lam))
ok("A8 THE CORRECTION GROUND: at the per-composite even trace the angular"
   " kill ENGAGES of record (o-2-OBS: (2,0)/(0,2) deg -1 -> -2), and the"
   " p = 2 carrier integral is Lam - 1: LINEAR — the of-record per-composite"
   " shortfall for tr K_H is ONE-plus power (o-2-OBS: 'still ONE-plus power"
   " short'), so the summed gain on the even trace is ONE power net"
   " (linear -> log), not two-plus",
   sp.simplify(I2p - (Lam - 1)) == 0 and sp.limit(I2p, Lam, sp.oo) == sp.oo)

print("WSR-AUDIT-BATTERY-DONE")
```

Output, verbatim (8/8 PASS):

```text
A1 OPERATOR-GRADE HERMITICITY: Cg Hermitian generic (no idempotency) => Cg(A + A^dag)Cg is Hermitian IDENTICALLY — the summed kill needs only C = C^dag and the A + A^dag pairing; and the assembly member C(G_12 + G_21^dag)C is generically NOT Hermitian (the wall's per-pair shape keeps its anti-Hermitian sector: the contrast is structural): PASS
A2 CAUCHY SLOT AT DEGREE 6 (generic complex coefficients): the [a^1] extraction is exact at every polynomial truncation — the schema is not an artifact of the cubic: PASS
A3 THE KILL SURVIVES THE OMITTED SPINOR STRUCTURE: tr[alpha_x C(k)] = -2 khat_x EXACT (KF2's band trace at s = -1, re-derived from the Dirac algebra, NOT the toy); tr C(k) = 2; the full angular integral of the traced leading integrand is ZERO — C(k)'s own khat-dependence (C(-k) != C(k)) feeds the trace only through the first moment: the WS5 toy's conclusion holds at the spinor-trace grade: PASS
A4 THE DOUBLE-KILL START, SHARPENED: [m^2] K^Sigma = 0 AND [m^3] = 0 — both factors start at m^2 (m_0 = m_1 = 0 PER FACTOR), so the summed composite starts at [m^4], generically NONZERO and Hermitian: the surviving content sits exactly at REM §3.3's V_2 (x) V_2 coupling order (2,2); the two exact zeros are sector kills, not a total vanishing: PASS
A5 STENCIL, INDEPENDENT ROUTE: e^{-icS} alpha_x = alpha_x e^{+icS} ({S, alpha_x} = 0), so the summed sandwich = alpha_x [sum_l w_l e^{i lam_l (th1-th0) S}]; the sin-profile is ZERO by w_+ = w_- and the cos-profile is (1 - cos(sqrt2 th))/2 = sin^2(sqrt2 th/2): the WS7 identity re-derived without matrix expansion: PASS
A6 MOMENT/L1 LEDGER: m_0 = m_1 = m_3 = 0, m_2 = -1, m_4 = -2, sum|w| = 1 (the l1-tight op constant), w_+ = w_-; the survivor census N = 1/2 with c = (1/2, 1/8 x4), sum = 1 — all re-derived: PASS
A7 THE LANDING IS AT LOG: the p = 3 carrier integral is log Lam with lim_{Lam->oo} = oo (formal, nonfinite): the strict threshold (< -3) is genuinely NOT crossed — FAILS-AT is forced for tr K_H^Sigma on the displayed assets; no enclosure follows: PASS
A8 THE CORRECTION GROUND: at the per-composite even trace the angular kill ENGAGES of record (o-2-OBS: (2,0)/(0,2) deg -1 -> -2), and the p = 2 carrier integral is Lam - 1: LINEAR — the of-record per-composite shortfall for tr K_H is ONE-plus power (o-2-OBS: 'still ONE-plus power short'), so the summed gain on the even trace is ONE power net (linear -> log), not two-plus: PASS
WSR-AUDIT-BATTERY-DONE
```

---

## 9. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 00:26:14 -0500**. Both corpus roots
(`workspace`, `supervision`) swept by filename and token grep; fenced
classes (register|road_|ledger|lens|plan|tracker|THE_HANDOFF) matched by
FILENAME ONLY, never opened (the supervision register files surfaced in a
find listing by NAME only). Artifacts sealed after the cutoff are UNSWEPT
and unclassed here.

```text
a-1 ROUTE OCCUPATION re-swept independently (tokens "K^Sigma |
    H_A^Sigma | SUMMED_REQUANT | summed re-quantif", both roots,
    non-fenced): hits are the target, its seed stock (9fdc3d1c,
    48ecdabe, 0a10c030), and STAGE8_EXTSRC_ROUND3_S9AD_AUDIT_V001.md —
    a DIFFERENT commission (external-source families) whose only
    relevant line notes the parallel lanes "touch no deciding bit". NO
    artifact occupies or contradicts this commission.
a-2 POST-CUTOFF-OF-THE-BUILD arrivals checked for bearing:
    STAGE8_MO4_CANCELLATION_S9AD_V001.md (0e894372, sidecar MATCH,
    sealed 00:05:12 — after the build's cutoff, correctly unswept
    there): PARTIAL on ||R_n Delta_n(a)||_1 — an R_n-mediated
    trace-norm object on the OTHER route family; it neither occupies
    this commission nor decides tr K_H^Sigma; noted for the registrar
    as adjacent, unconsumed here. STAGE8_EXTSRC_ROUND3 pair: different
    commission (a-1).
a-3 sw-2 of the build VERIFIED by mtime at the cutoff instant (§6 s-D).
a-4 ONE-OUTPUT verified: the only workspace files carrying this
    commission's name are the target and its sidecar (now plus this
    audit and its sidecar).
```

---

## 10. CHOICE LEDGER

```text
AC-1 [PROVABLE, run first]: seals, verbatim CAS re-run, byte-span
     verification, mtime sweep — all mechanical; run before any judgment.
AC-2 [YOURS]: the verdict class. REFUTED-AT requires a false load-bearing
     claim; COR-1 is a comparative overstatement in §0/§5.3 whose removal
     changes NO flag, NO verdict class of any quantity, and NO residue
     naming — the honest class is CONFIRMED-WITH-CORRECTIONS, per the
     record's own convention for audits that repair framing while the
     chains stand (the r-2/r-3 precedent).
AC-3 [YOURS, disclosed]: independence procedure — pre-commitment hashed
     before first read of the target (§2); the E-2 divergence resolved
     against my expectation at bytes and is reported, not suppressed.
AC-4 [YOURS]: A3's spinor-trace check treats the leading-stratum traced
     integrand at the symbol-display grade (the record's own KF1/KF2
     license); no stronger claim (e.g., stratum-by-stratum parity at
     degree -3) is made by me or by the build.
AC-5 [PROVABLE, consumed]: the SCAD dissolution (§5 c-2) is a bytes
     question (which object the witness names), settled by e5b0879b
     :420-436/:505-525 read at path.
```

---

## 11. TOY_SEPARATION

```text
AT-1 A1/A2/A4 run at the same abstract slot-model/identity-schema grade
     as the build's WS2/WS3 (the sealed KB-block license); no matrix
     result is carried to the carrier quantifier.
AT-2 A3/A5 use the EXACT 4x4 Dirac algebra of the record (not a toy);
     A3's angular integral is the exact continuum first-moment statement
     of KF2, consumed at the record's own effective-degree license.
AT-3 A7/A8 are exact formal-Lambda carrier integrals (the KF1 license);
     Lambda is never valued.
AT-4 Nothing testbed-grade appears; nothing is promoted.
```

---

## 12. FLAG BLOCK

```text
WSR_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the four summed-
  quantifier verdicts stand as built and independently re-derived:
  TRHA_SIGMA = 0 and HSHA_SIGMA = 0 exact (identity grade, operator
  level, generic-Hermitian-C proof supplied); HSKH_SIGMA <= M^2/eps^2
  n-uniform carrier-free (grade inherited from REM e-i/e-ii with sealed
  overturn conditions, correctly named; ray class CONDITIONAL(P-1));
  TRKH_SIGMA = FAILS-AT(log threshold), forced on the displayed assets,
  residue = MO-3's shape log-thin; the per-composite wall UNTOUCHED at
  its own quantifier — the named kill (a summed result quoted
  per-composite) is ABSENT after a span-by-span hunt; the C4/REM kill
  mechanics are consumed at exactly their sealed quantifiers.
  CORRECTIONS: COR-1 — the "two-plus powers -> log" comparative framing
  overstates the even trace's of-record per-composite shortfall by one
  power (o-2-OBS at bytes: engaged kill, degree -2, ONE-plus short;
  net summed gain ONE power, linear -> log); registrar margin note
  suffices, no re-issue forced; N-1, N-2 disclosure notes. )

PRECOMMIT = 3f29c1a17fee1b1bcbb16208969ec4bfa01f07bf955d57b03d68e2b864488500
  (hashed 2026-08-15T05:13:35Z, BEFORE the target was first opened).

SEALS_VERIFIED = 18/18( the target before opening + its 17, all FULL
  digests recomputed at path; REM-check 89641f76 additionally verified
  against its in-text citation; none unverifiable. )

CAS = 14/14 verbatim re-run OUTPUT-IDENTICAL (fresh venv wsrauditvenv,
  sympy 1.14.0) + 8/8 audit battery (one auditor-side expectation
  corrected and disclosed, A4 header).

FORBIDDEN_IMPORTS = none( exact symbolic only; nothing numeric
  evaluated; Lambda/M/eps formal; no float ground; no measured
  constant; no value frozen; alpha never computed or approached;
  kappa_record nowhere; no fenced-class file opened; no witness action;
  no flag of the record flipped; no git. )

SWEEP_CUTOFF = 2026-08-15 00:26:14 -0500 (both roots; fenced classes
  filename-matched only; post-cutoff arrivals unswept).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until registered; witness/flag actions are the
registrar's.
WSR_SUMMED_REQUANT_AUDIT = SEALED.
```
