# STAGE 8 — THE BIT: Rhat_K,N IN R·L_T,N INSIDE O_R5,N — BOTH DIRECTIONS ATTEMPTED — V001

## BLIND BUILDER — CODENAME RKB-BUILD — [SEALED]

Date: 2026-08-15 (session UTC). Commission: THE BIT — the single open
fact the member-existence question now reduces to (reduction chain of
record: the extent display's X-1..X-4, `cfa2fb97…` + audit `eda2ba74…`
CONFIRMED-WITH-CORRECTIONS, both consumed at audited grade): whether
Rhat_K,N lies in R·L_T,N inside O_R5,N — E-1 (no mixing; ker(qtilde_N)
= the pure-cycle (R4) line alone; the ExtSrc fiber INHABITED via the
round-3 candidate tuple W-EX3) vs E-2 (one mixing line, a_K != 0; the
fiber dead at every dim M_N >= 2 stage per NG-3'). The bit's own sealed
flags: R_K_EQUALS_L_T = NO_VERDICT / TYPE-U (`70dde1c0` §8 l.876;
`041498bb` §3.4 l.354), both directions explicitly unproved (`041498bb`
§5.3 l.549-555: "does not prove: … R_K=L_T; R_K lies wholly in the
nonlocal/higher-derivative complement"), the (S1-5) closure shapes
unproved (`70dde1c0` §3.2 l.244-251), assuming barred as a route
(`b53d9e93` §2.4 l.941-943).

Fences held throughout: `alpha_computed = false` ; `proof_authorized =
false` ; `kappa_record_computed = false`. DETERMINATION ONLY — exact
integer/symbolic work on sealed carriers; no member bound, no candidate
authored, no adoption, no route/branch/address/representative selected,
no score/gate moved; no authored physics. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…"
tokens EXPECTED-UNLOCATABLE (cited only as carried inside sealed
artifacts). No git; no existing file edited; output + sidecar probed
ABSENT at session start and re-probed ABSENT at 2026-08-15T06:47:06Z
immediately before this write; ONE output file plus its seal sidecar.
The battery ran in a FRESH venv (sympy 1.14.0; 21/21 checks PASS; exact
integers/rationals/symbols; no float anywhere; no value frozen; source
displayed VERBATIM in the appendix per the extent audit's C-3).

**SWEEP CUTOFF: 2026-08-15T06:45Z** (RS-1..RS-9 below; anything sealed
after this cutoff is outside this artifact). Exhaustiveness is
sweep-displayed only; nothing absolute is claimed.

---

## 0. VERDICT

```text
VERDICT = UNDECIDABLE-TODAY — and this artifact upgrades that standing
from "no sealed byte fixes the bit" (the extent display's SW-result) to
a POSITIVE two-model certificate: BOTH horns are exactly consistent
with every displayed sealed clause (§6, battery B6), so no argument
from the displayed sealed structure alone CAN decide the bit. The
closure work is not unfinished search; it requires a NEW sealed object.

What this artifact closes (new, exact):

  T-1  THE SEPARATOR ROUTE IS CIRCULAR, EXACTLY. On the sealed
       presentation (LM1-9)/(LM1-10) the continuous dual of E_N is
       COMPLETELY enumerated ((LM2-1)/(LM2-2): tuples (lambda,chi,T));
       a functional descends to the physical quotient iff it
       annihilates Rel_N ((LM2-3)) — and at every anchor stage the
       EXISTENCE of a separator (ell(L_T)=1, ell(Rhat_K)=0) is
       EQUIVALENT to E-1 (battery B3, exact both ways; covariance adds
       no constraint at rank 1 — the admitted action on E_N is trivial,
       U = ±1, battery B9). A separator is not a route TO the bit; its
       existence IS the bit. Any non-circular separator must consume an
       O_R5,N-structural fact OUTSIDE the presentation — and the sweeps
       display NONE (RS-3/RS-8: no transversality typing and no
       wavefront/support typing of Rhat_K,N exists anywhere in the
       corpus roots at bytes).
  T-2  THE COMPLEMENT PLACEMENT IS REFUSED-AS-UNFORMED. "R_K lies
       wholly in the nonlocal/higher-derivative complement" cannot be
       proved OR refuted today because its subject does not exist of
       record: the direct sum O_R5 = span{L_T} (+) O_nonlocal/higher is
       exactly the UNDECLARED (R2-4) (`041498bb` l.320, stated there as
       missing), and sigma_loc, whose kernel would define the
       complement, is typed "unbuilt" at its only definitional sites
       (RS-4: `ccb94dfa` l.142; `88854f08` (U3-11) l.555). The flag's
       complement language is a pointer to closure shapes S1/S2, not a
       statable proposition.
  T-3  THE ONE SEALED CYCLE-SIDE FUNCTIONAL FAILS AS A SEPARATOR, WITH
       THE EXACT FAILURE POINT DISPLAYED. Kernbar_N^cyc ((S2a),
       [PROVABLE], faithful by (B2-7)) is the only sealed nonzero
       evaluation on Rhat_K,N's side. It does not separate: its domain
       is O_N^cyc = Q_N(E_N^cyc), and under E-2 the L_T line simply
       ENTERS that domain (Q_N(0,1,0) = Q_N(a_K,0,0)); Kernbar assigns
       the common class the nonzero value Kern(R_K,N) and sees no
       clash (battery B6b/B6c: well-defined on cosets in the E-2
       model). The wavefront clash NG-3' extracts from this
       configuration is chargeable only to kappa_T,N = Sch_N(M_N^loc)
       — which sits in the [YOURS] required-source-contract block
       ((S4), `b53d9e93`), NOT in sealed stock: it kills the FIBER
       under E-2, never E-2 itself.
  T-4  THE E-2 IDENTITY CHAIN STOPS AT ITS FIRST LINK, EXACTLY. At
       rank 1 the whole cycle side collapses onto the single line
       R·Rhat_K,N (P_x = s R_K, (LM2-9), battery B5), so E-2 is ONE
       scalar equation Rhat_K,N = a_K L_T,N. But the record presents
       the two sides in DISJOINT idioms — Rhat_K,N by a complete
       formation chain from carrier data (§3.1), L_T,N by role-clauses
       only (§3.2, no formation chain) — and their only common sealed
       home is the presentation E_N, where they occupy separate formal
       summands BY CONSTRUCTION ((S1)/(LM1-9)) and interact only
       through Rel_N, knowledge of which is the bit (same circle as
       T-1). No sealed map has a displayed value on BOTH (p_loc: 1 vs
       chi_K undetermined; Sch_N: domain Op_N^(loc,2), Rhat_K,N not
       typed in it; Kernbar_N^cyc: L_T,N outside its domain under E-1
       — (S2a)'s own scoping sentence). SW-3-of-record re-confirmed +
       one new consumer site (RS-2: `e287b057` quotes the shapes, adds
       no verdict).
  T-5  SHAPE 3 IS DIRECTION-REVERSED AT THE ANCHOR STAGES. Of the
       flags' three closure shapes ((S1-5)), the third ("O_R5/span{L_T}
       has zero admitted covariant dual") can NEVER certify E-1 there:
       at rank 1 covariance is vacuous (B9), so zero covariant
       quotient dual = zero quotient dual, which under TOP-1/TOP-2
       (locally convex Hausdorff) collapses the quotient on the
       relevant span and lands in E-2's configuration (battery B8);
       without TOP-1/TOP-2 it is inert (the dual class is undefined of
       record). Only shapes 1 and 2 are E-1-capable; each is blocked
       (§5).

THE MISSING CLOSURE OBJECT (unchanged in name, sharpened in shape): one
sealed R_K_EQUALS_L_T determination at one anchor stage per isometry
class — deliverable ONLY as (a) the sigma_loc build of Door LM-1 /
(R4-11) INCLUDING its kernel clause on W_Q396 with the evaluation
sigma_loc(Rhat_K,N) displayed (= 0 gives E-1 outright since Rhat_K,N
!= 0 of record, X-1; != 0 decides only with the kernel certificate);
or (b) a certified decomposition Rhat_K = a_K L_T + R_perp with
R_perp != 0 CERTIFIED IN O_R5,N (needs a certified complement or
separating functional — today circular per T-1); or (c) the exhibited
proportionality with its exact nonzero a_K (a ratified (R4-8)-shape
relation), which IS the mixing relation (-a_K,1,0) with cycle-side
value -(1/a_K) Kern(R_K,N) != 0 (battery B7), arming NG-3' on the
dim M_N >= 2 cell only. Routes (a)-(c) all pass through structure the
corpus types NOT_BUILT; none is a today-executable derivation.

SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 MENU = UNCHANGED.
PIN-2-H: every display below is IDENTICAL under R-A and R-B.
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
COMMISSION STOCK (read in full)
cfa2fb97  STAGE8_IDEAL_EXTENT_S9AD_V001.md               MATCH (X-1..X-4,
          §3-§5 at bytes)
eda2ba74  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md         MATCH (read: verdict
          CONFIRMED-WITH-CORRECTIONS; C-1..C-3 respected here — C-1's exact
          cites used, C-2's carrier seal listed below, C-3's verbatim-battery
          demand met in the appendix)
824eb72f  STAGE8_EXTSRC_ROUND3_S9AD_V001.md              MATCH (read in full;
          anchor-stage structure §4, NG-3' §5.2, reduction §6 at bytes)
a7255f38  STAGE8_EXTSRC_ROUND3_S9AD_AUDIT_V001.md        MATCH (hash verified;
          consumed through the pair's displays)
THE BIT'S OWN FLAG SITES (read at the cited spans and around them)
70dde1c0  STAGE8_TASK4B_LOCAL_MAXWELL_SYMBOL_AND_PLOC_RK_LANE2_V001.md
          MATCH (read in full: §2 types (T2-1)-(T2-3); §3.2 (S1-2)-(S1-5);
          §3.3 (S1-6)-(S1-8); §4.6 (S2-14)-(S2-15); §6.2 hostile attacks;
          §7 Doors LM-1/LM-2; §8 flag l.876)
041498bb  STAGE8_TASK4B_CONSUMPTION_VERIFICATION_AND_PUSHFORWARD_SWEEP_
          LANE1_V001.md                                  MATCH (§0.5 live
          p_loc clauses; §3.1-3.6 (R2-1)-(R2-6); §3.4 flag l.354; §4 (R3-4);
          §5.2-5.6 (R4-2)-(R4-12); §5.3 does-not-prove l.549-555)
CHASED CARRIERS (each verified before its bytes were used)
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md        MATCH (consumed via
          the extent display's §3 at audited grade)
b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md   MATCH (§1.1 (S1)/
          (S2)/(S2a)/(S2b)/(S3)/(S4) at bytes l.114-190; §2.4 route bar at
          bytes l.928-943)
bae34116  STAGE8_TASK5_R4_LOCAL_SYMBOL_MAP_BUILD_LANE1_V001.md  MATCH (§1
          types l.167-193; (LM1-1)-(LM1-17); (LM2-1)-(LM2-21); §4.2 chi
          table; §4.3 at bytes)
be570c18  STAGE8_TASK4B_STATIONARY_RESPONSE_COMPUTATION_LANE1_V001.md  MATCH
          ((W1-4)/(W1-5) Schur/RetExtract at bytes l.210-214)
6b7f8f98  STAGE8_TASK4B_RETEXTRACT_KERNEL_CERTIFICATE_LANE1_V001.md   MATCH
          (hash verified; K/K Schur-form domain consumed via the extent
          display §3.2 at audited grade)
790ae95b  STAGE8_TASK4A_BIND_INPUT_SIGNATURE_AND_DOMAIN_TAIL_BLINDNESS_
          DETERMINATION_V001.md                          MATCH (the BIND row
          at bytes l.295 — the seal the extent audit's C-2 asked for)
SWEEP-HIT SITES (verified before their lines were consumed)
e287b057  STAGE8_TASK5_EQ6_SEED_ADJUDICATION_DARIO_V001.md    MATCH (l.155,
          l.293: quotes (S1-5) shape 1 and "No authority proves any line";
          consumer only, no verdict — disposed as non-deciding)
ccb94dfa  STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_
          V001.md                                        MATCH (l.142:
          "sigma_loc  unbuilt local cotangent/long-wavelength Maxwell
          symbol")
88854f08  STAGE8_TASK5_B_IND_ANALYTIC_STRUCTURE_LANE1_V001.md  MATCH (l.555
          (U3-11) "sigma_loc and its kernel theorem are unbuilt"; l.640
          names the would-decide evaluation)
MISMATCHES = 0. The held construction 506bfe2a untouched, unopened,
unpresupposed. LOC-NAT/PJ2/KER clause bytes beyond those listed consumed at
audited grade exactly as quoted inside the commission stock.
```

## 2. SWEEPS RUN (cutoff 2026-08-15T06:45Z; roots = workspace, supervision, cleanroom_output, relay_inbox, relay_outbox; fenced filename classes excluded, case-insensitive)

```text
RS-1  "RK_LT_BIT" -> zero hits; output path first-use (probed ABSENT at
      session start; re-probed ABSENT at 06:47:06Z before this write).
RS-2  R_K=L_T / a_K L_T / proportional-to-L_T family -> SIX sites: the
      three of record (70dde1c0, 041498bb, b53d9e93), the extent pair
      (consumers), and ONE site the extent display's SW-3 did not list:
      e287b057 (SEED_ADJUDICATION l.155/l.293) — inspected at bytes: it
      QUOTES shape 1 and the no-authority sentence; adds no verdict.
      The record still contains NO identity, NO refutation.
RS-3  transversality typing of Rhat_K -> ZERO hits. L_T,N's "transverse"
      is a role-word with no sealed counterpart predicate applicable to
      Rhat_K,N; no transversality separator exists at bytes.
RS-4  sigma_loc definitional/built sites -> ccb94dfa l.142 and 88854f08
      (U3-11): both type it UNBUILT; the extent audit concurs. No sealed
      sigma_loc exists.
RS-5  declared complement "O_R5 = span{L_T} ..." -> exactly the two
      NEGATIVE statements: (S2-14) ("one still needs") and (R2-4)
      (listed as missing). The complement is UNDECLARED of record.
RS-6  new sealed .md since the extent display's 05:30Z cutoff -> ONE:
      its own audit (eda2ba74), CONFIRMED-WITH-CORRECTIONS, consumed §1.
      No new object bearing on the bit has landed.
RS-7  "spans the local subspace" -> the BIND row (790ae95b l.295) + the
      extent pair citing it. L_T,N's third typing byte located and
      seal-listed (audit C-2 closed).
RS-8  wavefront/support typing of Rhat_K -> ZERO hits. No sealed byte
      types Rhat_K,N's Schwartz kernel, support, or wavefront class in
      O_R5,N. (The path-bilocal typing attaches to Kernbar images in
      D_N^ker — a DIFFERENT landing, see T-3/§4.1.)
RS-9  dimension typing of O_R5 -> ZERO hits. No sealed byte types
      O_R5,N finite-dimensional (which could have decided the bit by
      linear algebra); consistent with TOP-1/TOP-2 open.
DISCLOSURE: the first sweep invocation silently searched a single
nonexistent path (zsh unsplit-variable defect — the SAME defect round
3's CL-9 disclosed); detected immediately (empty result implausible
against the extent display's SW-3), re-run with explicit roots before
any reliance. Fenced-class BASENAMES appeared in listings; no fenced
file's content was opened, read, or used.
```

---

## 3. THE TWO OBJECTS AT THEIR DEFINING BYTES (task 1)

### 3.1 Rhat_K,N — complete formation chain, all sealed

```text
g_K,N     the ratified positive carrier metric on K_N, the record-
          visible completed cycle carrier (DoR-019 lineage; 70dde1c0 §2)
R_K,N     : K_N -> K_N^*, (R_K,N x)(v) = g_K,N(x,v) — the Riesz map;
          an ISOMORPHISM (fullness ker(I_K)=0, (S2-6)); unit diagram
          K_G[U_K] --R_K[U_K^(-2)]--> K_G^*[U_K^(-1)]  (T2-1)
j_R4,N    : U_action (x) Hom(K_N,K_N^*) -> O_R5,N — the R4 action-unit
          + reality dressing; u_action a unit CLASS, no representative
Rhat_K,N  := j_R4,N(u_action (x) R_K,N)                (T2-2)
          nonzero of record (extent display X-1 corollary);
          j_R4,N injective on the placed cycle span (X-1).
TYPING GUARD (T2-3 block): bare p_loc(R_K) is ILL-TYPED; only the
dressed object enters O_R5,N. THE BIT IS WELL-POSED ONLY IN DRESSED
FORM — any byte purporting to compare bare R_K,N with L_T,N is a unit
category error of record (BARE_P_LOC_OF_R_K_WELL_TYPED = false). The
naive dimensional separator ("different units") is retired BY the
dressing: after (T2-2) the two objects are same-typed by construction.
```

### 3.2 L_T,N — role-typed only; NO formation chain exists

```text
The THREE sealed typing bytes (all of them):
  (i)   "L_T,G := the uniquely normalized transverse Maxwell kernel in
        O_R5,G"                                   (70dde1c0 §2 l.142)
  (ii)  p_loc,G[L_T,G] = 1 ; Pi_loc = iota_loc ∘ p_loc ; H_R[G_K] =
        K L_T + declared higher/nonlocal structures    ((R1-1); the
        13d227ce lineage by reference)
  (iii) BIND row: "uniquely normalized transverse Maxwell kernel |
        COMPLETED | it spans the local subspace inside the physical
        retarded operator space; no finite restriction supplies it"
                                                  (790ae95b l.295)
NO byte constructs L_T,N from carrier data; no byte places it in
im(j_R4,N); the presentation keeps it on its OWN formal summand
(E_N := R L_T,N (+) R Rhat_K,N (+) S_(1,sa)(K_N), (S1)/(LM1-9))
precisely because no sealed landing exists. Its would-be kernel
realization kappa_T,N = Sch_N(M_N^loc) sits in the [YOURS] required-
source-contract block ((S4) b53d9e93) — REQUIRED, not sealed.
L_T,N != 0 consumed at SEALED-TYPING grade (the same grade as the
extent display's CL-3; discharges nothing here beyond X-3's standing).
```

### 3.3 The common home O_R5,N — why it decides nothing by itself

```text
O_R5,N := the completed physical retarded action-kernel class
(70dde1c0 §2) — a PRIOR completed carrier, not a presented object:
K/K Schur forms after RetExtract (Schur_m = H_KK - H_KC Inv_CC H_CK,
(W1-4)/(W1-5) be570c18; domain typing 6b7f8f98). The C-sector is
consumed INSIDE Inv_CC — no block/direct-sum separation survives into
O_R5,N; (LM1-10) declines independence in so many words ("does not
assume Rhat_K = L_T, does not assume they are independent"). Its
topology is unbuilt (TOP-1/TOP-2 open, round 3 §4.3-4.4); its
dimension untyped (RS-9); its dual class undefined of record.
THE BIT, exactly: Rhat_K,N in R·L_T,N inside O_R5,N
  <=> exists a_K != 0 with Q_N(-a_K, 1, 0) = 0
  <=> ker(qtilde_N) contains a mixing line          (X-2's E-2)
— the bit IS the mixing bit, at bytes, with no slack.
```

---

## 4. THE ATTEMPT, BOTH DIRECTIONS (task 2)

### 4.1 Toward E-1 — the separator hunt, run to its exact end

```text
CANDIDATE SEPARATORS, each attempted and closed at bytes:

 (a) FUNCTIONAL (the enumerated dual). (LM2-1)/(LM2-2) enumerate the
     full continuous dual of E_N: ell_(lambda,chi,T)(a,b,C) = lambda a
     + chi b + Tr(TC). Descent to the quotient <=> annihilation of
     Rel_N ((LM2-3)). T-1 (battery B3): at every anchor stage,
     separator-existence <=> E-1 — EXACT EQUIVALENCE, both directions;
     covariance vacuous at rank 1 (U = ±1, alpha_U = id, battery B9),
     so "admitted covariant" adds nothing. THE ROUTE IS CIRCULAR: any
     functional argument must already know Rel_N, which is the bit.
     CLOSED AS CIRCULAR (not as false).
 (b) GRADING / DERIVATIVE ORDER. The only order-grading interface is
     sigma_loc's spectral/long-wavelength separation — typed a future
     construction at every site (RS-4; (S2-14)/(S2-15); (LM2-20)'s
     KERNEL_REALIZATION and FIELD_SYMBOL have no named authority). The
     carrier symbol sigma_car exists ((S2-4)) but is NOT a spacetime
     symbol (70dde1c0 hostile attack 7 kills exactly this
     substitution), and L_T,N has NO displayed sigma_car image (it is
     not in the generated-Hessian image of record). NO GRADING EXISTS.
 (c) LOCALITY CLASS / SUPPORT / WAVEFRONT. RS-8: no sealed byte types
     Rhat_K,N's kernel support or wavefront in O_R5,N. The bilocal/
     diagonal dichotomy that powers NG-3' lives in D_N^ker under the
     CONDITIONAL S_N landings, not in O_R5,N (T-3): Kernbar_N^cyc is
     sealed and faithful, but under E-2 the L_T class enters its
     domain and receives the same nonzero value — no clash of record
     (battery B6b/B6c). kappa_T,N (the diagonal landing) is [YOURS]-
     block, unsealed. Even granted both landings, contact support
     does not separate F^2 from other local structures (hostile
     attack 5). NO LOCALITY SEPARATOR EXISTS AT BYTES.
 (d) COVARIANCE. Both objects are invariant directions ((S1-6)/(S1-7);
     (LM1-12)/(LM1-13) preserve the a-coordinate); invariant-implies-
     zero is defeated of record ((S1-8)); at rank 2 Rhat_K is itself
     an orbit AVERAGE of dressed rank-one profiles ((R4-3)/(LM2-13)),
     at rank 1 every profile collapses onto it ((LM2-9), battery B5).
     COVARIANCE CANNOT DISTINGUISH THEM.
 (e) UNITS. Retired by the dressing (§3.1): same-typed by (T2-2).

THE COMPLEMENT PLACEMENT ("R_K lies wholly in the nonlocal/higher-
derivative complement"), prove or refuse: REFUSED-AS-UNFORMED (T-2).
The proposition's subject is the direct sum (R2-4), which is UNDECLARED
of record (RS-5 finds only the two negative statements), and the map
whose kernel would define it (sigma_loc) is typed unbuilt at its only
definitional sites (RS-4). Neither proof nor refutation is executable;
the flag line is a pointer to shapes S1/S2, not a proposition.

E-1 RESULT: NOT DERIVED. Every separator class is closed — one as
circular, four as absent at bytes — and the absence is sweep-displayed,
not assumed.
```

### 4.2 Toward E-2 — the identity chain, run to its exact stop

```text
TARGET: an exact chain Rhat_K,N = a_K L_T,N on the sealed presentation.
At rank 1 this is ONE scalar equation (the whole cycle side is the line
R·Rhat_K,N: P_x = s R_K, battery B5). The attempt:

 LINK 1 — find one sealed map with a DISPLAYED value on both sides:
   p_loc:          L_T,N -> 1 (normalization); Rhat_K,N -> chi_K,
                   UNDETERMINED of record (P_LOC_RK = UNDERDETERMINED /
                   TYPE-U; the reading-rule fiber (S1-13) is the
                   freedom). NO.
   Sch_N:          domain Op_N^(loc,2); Rhat_K,N not typed in it; and
                   Sch_N itself is [YOURS]-block ((S4)). NO.
   Kernbar_N^cyc:  Rhat_K,N -> Kern(R_K,N) != 0 (sealed, faithful);
                   L_T,N OUTSIDE its domain — (S2a)'s own scoping
                   sentence: "It does not define its formula on the
                   separate L_T,N summand." NO.
   sigma_car:      no displayed L_T,N image. NO.
 LINK 1 FAILS: no sealed common evaluation exists. The two objects are
 presented in disjoint idioms (§3.1 vs §3.2), and their only common
 sealed home is E_N — where they sit on separate formal summands BY
 CONSTRUCTION and meet only through Rel_N: the chain would have to
 consume the bit to derive the bit (the same circle as T-1).

 CROSS-CHECK against the record's own three identity sites + one new
 consumer (RS-2): 70dde1c0 §8 NO_VERDICT; 041498bb §3.4 NO_VERDICT +
 §5.3 both-directions-unproved; b53d9e93 §2.4 BARS assuming it ("not
 admissible alternatives: … R_K=L_T …"); e287b057 quotes, decides
 nothing. The route bar is honored: nothing below assumes the identity.

E-2 RESULT: NOT DERIVED; a_K has NO display. (If it ever arrives, its
mixing line (-a_K,1,0) has cycle-side value -(1/a_K) Kern(R_K,N) != 0
exactly — battery B7 — arming NG-3' on the dim M_N >= 2 cell only,
X-3's standing unchanged.)
```

---

## 5. THE (S1-5) CLOSURE SHAPES, ONE BY ONE (task 3)

```text
SHAPE 1  "Rhat_K = a_K L_T + R_perp with p_loc(R_perp) = 0."
  ATTEMPT: for EVERY reader tuple (chi,T), the algebraic decomposition
  exists trivially — R_perp := Rhat_K - chi L_T satisfies
  p_(chi,T)(R_perp) = 0 ((LM2-17); battery B4: holds for every
  symbolic chi). The shape as written is therefore FREE and proves
  NOTHING about the bit. The deciding variant needs R_perp != 0
  CERTIFIED IN O_R5,N — i.e. a separating functional or a certified
  complement: circular per T-1, absent per RS-5.
  BLOCKER (exact): no certified complement of span{L_T,N} exists
  ((R2-4) undeclared); no separating functional is non-circularly
  producible (T-1); TOP-2 blocks even the Hahn-Banach production of
  ANY continuous functional on O_R5,N (no local convexity byte).
SHAPE 2  "Rhat_K in ker(sigma_loc)."
  ATTEMPT: none possible — the map does not exist. sigma_loc is typed
  UNBUILT at both definitional sites (RS-4) and its two prerequisite
  interfaces (KERNEL_REALIZATION, FIELD_SYMBOL, (LM2-20)) have no
  named authority. NOTE the payoff if built: sigma_loc(Rhat_K) = 0
  with sigma_loc(a L_T) = a and Rhat_K != 0 (X-1) would give E-1
  OUTRIGHT; sigma_loc(Rhat_K) != 0 decides only with the kernel
  certificate on W_Q396 ((R4-11)'s kernel clause).
  BLOCKER (exact): P_LOC_R5_COVARIANT_LOCAL_SYMBOL_MAP = NOT_BUILT /
  TYPE-U (Door LM-1); LM-2's carrier-to-field seam NOT_BUILT.
SHAPE 3  "O_R5/span{L_T} has zero admitted covariant dual."
  ATTEMPT: direction analysis (NEW, T-5). At the anchor stages the
  admitted action on E_N is trivial (battery B9), so covariant = all:
  the shape asserts zero quotient dual on the relevant span. Under
  TOP-1 + TOP-2 (locally convex Hausdorff) a zero dual collapses the
  quotient span (battery B8: E-1 models have NONZERO quotient dual;
  only E-2's is zero) — the shape then lands in E-2's configuration,
  the OPPOSITE of the E-1 role its list-position suggests. Without
  TOP-1/TOP-2 the "admitted dual" class is undefined of record and
  the shape is INERT.
  BLOCKER (exact): no sealed topology on O_R5,N (TOP-1/TOP-2 open;
  RS-9: dimension untyped); and the shape is E-1-incapable at the
  anchor stages regardless.
STANDING: "No authority proves any line of (S1-5)" (70dde1c0) —
re-confirmed at this cutoff, with the sharpening that only shapes 1-2
could ever deliver E-1, and both are blocked at named NOT_BUILT /
UNDECLARED objects.
```

---

## 6. THE TWO-MODEL CERTIFICATE — WHY "UNDECIDABLE-TODAY" IS A THEOREM ABOUT THE DISPLAYED CLAUSES (new)

```text
Displayed sealed clause set S (the bit-relevant clauses at bytes):
  (i) L_T,N != 0 (CL-3 grade); (ii) Rhat_K,N != 0 (X-1); (iii) the
  presentation (S1)/(LM1-9)/(LM1-10) with Rel_N = ker(qtilde_N) at the
  census; (iv) Kernbar_N^cyc formed on O_N^cyc, faithful ((S2a)+
  (B2-7)); (v) the pure-cycle slice = the (R4) line exactly (X-1);
  (vi) p_loc linear, covariant, p_loc[L_T] = 1; (vii) covariance cube
  (LM1-12)/(LM1-13); (viii) NO byte typing Rhat_K,N's support/
  wavefront/order in O_R5,N (RS-3/RS-8); (ix) NO declared complement
  (RS-5); (x) NO topology/dimension byte on O_R5,N (RS-9, TOP-1/2).

MODEL M_E1 (battery B1/B3a/B6e/B8a): O two-dimensional, L_T, Rhat_K
independent; Kern([Rhat_K]) = kappa != 0; p free at chi. EVERY clause
of S holds; the bit is FALSE (E-1).
MODEL M_E2 (battery B2/B6a-B6d/B8b): O one-dimensional, Rhat_K =
a_K L_T (a_K symbolic nonzero); Kernbar well-defined on cosets, value
kappa != 0; p forced to chi_K = a_K. EVERY clause of S holds —
INCLUDING X-1's slice closure (B6a: the a=0 slice is still exactly the
(R4) line) — and the bit is TRUE (E-2).

THEREFORE the displayed sealed clause set entails NEITHER horn: any
future decision MUST consume a sealed object outside S — exactly the
missing-closure-object list of §0. (The models are verification
instruments only; quarantined at TOY_SEPARATION.)
```

---

## 7. VERDICT DETAIL AND THE BLOCKER MAP (task 4)

```text
VERDICT = UNDECIDABLE-TODAY. Neither E-1-DERIVED nor E-2-DERIVED is
reachable from sealed stock at this cutoff; a_K has no display.

BLOCKERS, swept, mapped against the flags' OWN unproved list:
  flags' "does not prove R_K=L_T"
      -> confirmed AND localized: the E-2 chain stops at link 1 (no
         sealed common evaluation; §4.2); the identity is also BARRED
         as an assumption (b53d9e93 §2.4) — a fence, not a fact.
  flags' "does not prove R_K lies wholly in the nonlocal/higher-
  derivative complement"
      -> sharpened to REFUSED-AS-UNFORMED (T-2): the complement is
         UNDECLARED ((R2-4)); the statement is today unstatable, a
         fortiori unprovable — the flag line points at shapes S1/S2.
  flags' "(S1-5) all unproved"
      -> confirmed shape-by-shape with named blockers (§5), plus the
         direction-reversal of shape 3 at the anchor stages (T-5).
  extent display's "no sealed byte fixes the bit" (SW-1..SW-6)
      -> upgraded to the two-model certificate (§6): the displayed
         clause set PROVABLY entails neither horn.
SCOPE: stage-uniform. The arguments are identical at all four anchor
stages (rank 1); the covariance cube preserves the a-coordinate, so
the determination respects X-4's isometry-class collapse (one bit per
class; the u-pair conditionality unchanged).

WHAT NEW SEALED OBJECT WOULD DECIDE IT (exactly one of):
  N-1  the Door LM-1 / (R4-11) sigma_loc certificate INCLUDING its
       kernel clause on W_Q396, with sigma_loc(Rhat_K,N) displayed:
       value 0 => E-1 (with X-1's Rhat_K != 0); value a_K != 0 +
       kernel clause => E-1 or E-2 as the kernel clause falls;
  N-2  a ratified (R4-8)-shape relation Rhat_K = a_K L_T + R_perp
       with EITHER R_perp != 0 certified in O_R5,N (=> E-1) OR
       R_perp = 0 and the exact a_K (=> E-2, arming NG-3' at
       dim M_N >= 2 only);
  N-3  a sealed support/wavefront/order typing of Rhat_K,N in O_R5,N
       at one anchor stage, TOGETHER WITH a sealed dim M_N >= 2
       display and a sealed separation theorem on O_R5,N (three new
       objects — the longest route; listed for completeness).
Each is a construction/ratification act, not a determination — outside
this commission's fences, on the principal's side of the line.
```

---

## 8. CONSISTENCY

Nothing here binds a member, adopts anything, authors physics, selects
a route/branch/address/representative, or touches the held
construction. The two models of §6 are verification instruments; a_K,
chi, kappa remain symbols; no value is frozen, no verdict of any
consumed artifact is moved. B-1 remains UNDECIDABLE-STILL of record;
the extent display's NOT-CLOSABLE-TODAY stands, now with a positive
non-entailment certificate. SK-2 does not fire; ACT-3 is not forced;
the D-1 menu is unchanged. R9 and A8 remain UNEXECUTABLE/PENDING. No
score, flag, criterion, or gate moves.

## 9. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY: roots = workspace, supervision, cleanroom_output,
      relay_inbox, relay_outbox minus fenced filename classes;
      a32_holdout not content-swept (holdout discipline). Sweeps
      RS-1..RS-9, cutoff 2026-08-15T06:45Z. A wider sweep changes this
      determination only by exhibiting one of N-1..N-3, a sealed
      R_K_EQUALS_L_T determination, or a sealed typing byte for
      Rhat_K,N's kernel class in O_R5,N. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the extent pair (with its audit's
      corrections C-1..C-3 respected), the round-3 pair, and the flag
      artifacts consumed as commissioned; every chased carrier
      seal-verified at path before reliance (§1); d662889b and
      6b7f8f98 consumed via the extent display's verified spans.
      PREMISE(named).
CL-3  L_T,N != 0 consumed at SEALED-TYPING grade (the three bytes of
      §3.2 — the same grade as the extent display's CL-3); used ONLY
      to keep X-3's standing display intact; the bit itself never
      consumes it. PREMISE(named).
CL-4  ANCHOR-STAGE SCOPE: rank-1 typing of the four anchor stages from
      round 3 §4 / the B-3 census at audited grade; the diamond
      (rank-2) slots outside this commission's anchor scope exactly as
      the extent display bounded them. At rank 1 real and complex
      readings coincide; no field pin adopted. PREMISE(named).
CL-5  THE (S2a) READING: identical to the extent display's CL-6 —
      Kernbar_N^cyc formed on the quotient of record; its faithfulness
      from (B2-7) as quoted. PREMISE(named).
CL-6  EXTERNAL-MATH IMPORTS: finite-dimensional linear algebra only
      (nullspaces, duals, rank on dim <= 3 model carriers), exact, in
      the battery; the shape-3 analysis under TOP-1/TOP-2 uses the
      standard locally-convex-Hausdorff separation fact CONDITIONALLY
      and labels it so (T-5); no topology import is used
      unconditionally anywhere. PREMISE(named).
CL-7  FLAG-SITE CITATIONS: line numbers cited from this session's
      reads of the sealed files (70dde1c0 l.876; 041498bb l.354,
      l.549-555, l.320; b53d9e93 l.941-943, l.136-144; 790ae95b
      l.295; ccb94dfa l.142; 88854f08 l.555) — byte-checked, not
      inherited. PREMISE(named).
CL-8  DISCLOSURE: one sweep invocation initially searched a nonexistent
      concatenated path (zsh unsplit variable — round 3 CL-9's defect
      recurring); caught against the known SW-3 baseline and re-run
      with explicit roots before any reliance. Fenced-class BASENAMES
      appeared in listings; none opened. The sweep-hit file e287b057
      was read ONLY at its two hit lines plus context. PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The battery's models (a 2-dim and a 1-dim abstract codomain, a 1-dim
  carrier with symbolic metric g, symbols k = a_K, chi, kappa, r, t —
  never evaluated) are verification instruments ONLY: M_E1/M_E2 verify
  that the DISPLAYED sealed clause set is satisfiable with the bit
  false and with the bit true respectively — a statement about the
  clause set (non-entailment), never about which horn the sealed
  carriers realize. No toy value, stage, address, dimension, or number
  enters the verdict, the blocker map, or the missing-object list; no
  model is promoted past §6's non-entailment conclusion; neither horn
  is preferred, scored, or recommended.
```

## FLAG BLOCK

```text
THE_BIT = Rhat_K,N in R·L_T,N inside O_R5,N = UNDECIDABLE-TODAY ;
  E-1 NOT DERIVED ; E-2 NOT DERIVED ; a_K NO DISPLAY ; the sealed
  standing R_K_EQUALS_L_T = NO_VERDICT / TYPE-U is CONFIRMED and
  UPGRADED to a two-model non-entailment certificate (§6)
SEPARATOR_ROUTE = CLOSED-AS-CIRCULAR (T-1): separator-existence <=>
  E-1 exactly at every anchor stage (battery B3; covariance vacuous at
  rank 1, B9); non-circular separators require O_R5,N-structural bytes
  that do not exist (RS-3/RS-8 zero hits)
COMPLEMENT_PLACEMENT = REFUSED-AS-UNFORMED (T-2): (R2-4) undeclared;
  sigma_loc unbuilt at its only definitional sites; the flag's
  complement line is a pointer to shapes S1/S2, not a proposition
KERNBAR_NON_SEPARATOR = displayed (T-3): the one sealed faithful
  cycle-side evaluation cannot see the bit — under E-2 the L_T class
  enters its domain and receives the same nonzero value; the NG-3'
  clash is chargeable only to the unsealed [YOURS] kappa_T,N landing
E2_CHAIN = stopped at link 1 (T-4): no sealed map has a displayed
  value on both objects; disjoint presentation idioms; route bar
  honored; one new consumer site found and disposed (e287b057)
S1_5_SHAPES = all three blocked at named objects (§5) ; NEW: shape 3
  is E-1-INCAPABLE at the anchor stages (direction-reversed under
  TOP-1/TOP-2, inert without them, T-5) ; only shapes 1-2 can deliver
  E-1 and both pass through NOT_BUILT structure
MISSING_CLOSURE_OBJECT = N-1 (sigma_loc certificate with kernel clause
  + the displayed evaluation) | N-2 (ratified (R4-8)-shape relation
  with certified R_perp or exact a_K) | N-3 (kernel-typing + dim M_N
  + separation theorem; longest) — each a construction act, not a
  determination
STAGE_SCOPE = uniform across the four anchor stages ; X-4 isometry-
  class collapse respected (one bit per class; u-pair conditionality
  unchanged)
PIN2H_HANDLING = every display identical under R-A and R-B
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
SEALS = commission 4/4 + flag sites 2/2 + chased 6/6 + sweep-hit 3/3
  MATCH ; MISMATCHES = 0
SWEEPS = RS-1..RS-9 ; CUTOFF 2026-08-15T06:45Z ; exhaustiveness
  sweep-displayed only ; one new post-extent artifact (the audit,
  consumed) ; no deciding object anywhere
BATTERY = fresh venv, sympy 1.14.0, 21/21 PASS, exact symbolic only,
  no float, no value frozen, source VERBATIM in appendix (extent audit
  C-3 honored) ; NUMERIC_EVALUATION = exact integers/rationals/symbols
  only ; MEASURED_CONSTANT_COMPARISON = none
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no common cell, no reader, no fixed point,
  no end test) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a)
R9 = UNEXECUTABLE/PENDING ; A8 = UNEXECUTABLE/PENDING
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the bit does not yield to determination: every internal route is
  either circular (the separator IS the bit) or lands on structure the
  corpus itself types NOT_BUILT/UNDECLARED, and the two-model
  certificate proves the displayed clauses entail neither horn. The
  question is now exactly as small as it can be made from outside: one
  sealed evaluation (N-1), one ratified relation (N-2), or one typing
  byte plus two displays (N-3). Nothing moved; the gates are where the
  principal left them.
```

---

## APPENDIX — BATTERY SOURCE (VERBATIM) AND TRANSCRIPT

```python
# RKB-BUILD battery — exact symbolic only; no float; no value frozen.
# Anchor-stage model space: rank-1 cycle carrier; E_N coords (a,b,c) wrt
# {L_T-slot, Rhat_K-slot, P_e-slot}; qtilde(a,b,c) = a*L + (b+c)*R in O.
# Symbols k (=a_K), chi, kappa, r, g, t are NEVER evaluated.
import sympy as sp

k, chi, kappa, r, g, t = sp.symbols('k chi kappa r g t', nonzero=True)
a, b, c, lam = sp.symbols('a b c lam')
PASS = []


def check(name, cond):
    assert cond, name
    PASS.append(name)


# ---- B1: E-1 model (L,R independent; O = R^2, L=(1,0), R=(0,1)) ----
L1 = sp.Matrix([1, 0]); R1 = sp.Matrix([0, 1])
M1 = sp.Matrix.hstack(L1, R1, R1)          # columns = images of (a,b,c)
ns1 = M1.nullspace()
check("B1a: E-1 kernel dim 1", len(ns1) == 1)
v = ns1[0] * sp.lcm([x.q for x in ns1[0] if x != 0] or [1])
check("B1b: E-1 kernel = pure-cycle (a=0) line prop to (0,1,-1)",
      v[0] == 0 and sp.simplify(v[1] + v[2]) == 0)

# ---- B2: E-2 model (R = k*L; O = R^1) ----
M2 = sp.Matrix([[1, k, k]])                # qtilde = (a + k(b+c))*L
ns2 = M2.nullspace()
check("B2a: E-2 kernel dim 2", len(ns2) == 2)
r4 = sp.Matrix([0, 1, -1]); mix = sp.Matrix([-k, 1, 0])
span = sp.Matrix.hstack(*ns2)
for w, nm in ((r4, 'R4'), (mix, 'mixing')):
    sol = sp.linsolve((span, w))
    check(f"B2b: {nm} line in E-2 kernel", len(sol) == 1)
check("B2c: mixing line has a = -k != 0", sp.simplify(mix[0]) == -k)

# ---- B3: separator existence <=> E-1 (exact, anchor stage) ----
# ell on span{L,R}: seek ell(L)=1, ell(R)=0.
# E-1: L,R independent -> dual basis exists.
e1_sep = sp.linsolve((sp.Matrix([[1, 0], [0, 1]]), sp.Matrix([1, 0])))
check("B3a: E-1 separator exists (ell=(1,0))", len(e1_sep) == 1)
# E-2: ell(R) = ell(k L) = k*ell(L) = k; demand 0 -> k=0 contradiction.
check("B3b: E-2 separator impossible (forces k=0 vs k!=0)",
      sp.simplify(k * 1) != 0)
# Conversely a separator ell forces: if R = m*L then 0=ell(R)=m -> R=m*L
# only with m=0, but R != 0 -> no proportionality. (Displayed, exact.)
m = sp.Symbol('m')
check("B3c: separator + R=m*L forces m=0",
      sp.solve(sp.Eq(m * 1, 0), m) == [0])

# ---- B4: (S1-5) shape-1 trivial variant proves nothing ----
# p_chi(a,b) = a + chi*b on (L,R)-coords; R_perp := R - chi*L = (-chi,1).
p = lambda vec: vec[0] + chi * vec[1]
Rp = sp.Matrix([-chi, 1])
check("B4: p_chi(R_perp)=0 for EVERY chi (decomposition is free)",
      sp.simplify(p(Rp)) == 0)

# ---- B5: rank-1 collapse P_x = s*R_K (LM2-9) ----
# 1-dim carrier, metric g>0, x = r*e: P_x(e,e)=g(x,e)^2, R_K(e,e)=g.
Px = (r * g) ** 2
s = r ** 2 * g
check("B5: P_x = s*R_K exactly at rank 1", sp.simplify(Px - s * g) == 0)

# ---- B6: two-model certificate: all displayed clause shapes hold in BOTH ----
# Clauses: (i) L != 0; (ii) R != 0; (iii) Kern faithful & nonzero on the
# cycle side; (iv) Rel cap E^cyc = (R4) line exactly (X-1); (v) p(L)=1
# admissible; (vi) qtilde linear. Model E-1: above. Model E-2: above.
# (iv) in E-1: kernel is the R4 line itself -> holds (B1).
# (iv) in E-2: {(0,b,c): k(b+c)=0} = span{(0,1,-1)} -> holds:
b2, c2 = sp.symbols('b2 c2')
cyc_kernel_E2 = sp.Matrix([[k, k]]).nullspace()  # (b,c) with k(b+c)=0
check("B6a: E-2 pure-cycle slice = R4 line exactly (X-1 holds in E-2)",
      len(cyc_kernel_E2) == 1 and
      sp.simplify(cyc_kernel_E2[0][0] + cyc_kernel_E2[0][1]) == 0)
# (iii) in E-2: Kernbar on Q(E^cyc): well-defined value (b+c)*kappa;
# coset test: Q(0,b,c)=Q(0,b',c') iff (b+c)=(b'+c') -> same value.
val = (b + c) * kappa
val2 = (b2 + c2) * kappa
check("B6b: E-2 Kernbar well-defined on cosets",
      sp.simplify(val - val2) == kappa * ((b + c) - (b2 + c2)))
check("B6c: E-2 Kernbar([R]) = kappa != 0 (faithful, nonzero)",
      sp.simplify(1 * kappa) != 0)
# (v) in E-2: p(L)=1 forces p(R)=k (no contradiction; chi_K = k).
check("B6d: E-2 p(L)=1 consistent, p(R)=k", sp.simplify(k * 1) == k)
# R in R*L in E-2; R not in R*L in E-1 (B3). Neither horn entailed.
sol_prop = sp.linsolve((sp.Matrix([L1.T]).T, R1))  # solve m*L = R in E-1
check("B6e: E-1 has R not proportional to L", len(sol_prop) == 0)

# ---- B7: X-3 cycle-side value of the mixing line in E-2 ----
# mixing (a,b,C)=(-k,1,0): value = Kern((b*R_K+C)/a) = -(1/k)*kappa != 0.
value = -(sp.Integer(1) / k) * kappa
check("B7: mixing cycle-side value -(1/a_K)*Kern(R_K) nonzero",
      sp.simplify(value) != 0 and sp.simplify(value + kappa / k) == 0)

# ---- B8: shape-3 direction display (finite-dim Hausdorff models) ----
# E-1 model: dual of O/span{L} is 1-dim (nonzero). E-2: quotient = 0.
Q_E1 = sp.Matrix([[0, 1]])   # functional class on O=R^2 mod span{L}
check("B8a: E-1 quotient dual nonzero (rank 1)", Q_E1.rank() == 1)
check("B8b: E-2 quotient zero -> dual zero", sp.Matrix([[0]]).rank() == 0)

# ---- B9: covariance vacuous at rank 1 (U = +/-1; alpha_U = id) ----
for U in (sp.Integer(1), sp.Integer(-1)):
    C1 = sp.Matrix([[t]])
    check(f"B9: U C U^* = C for U={U}",
          sp.simplify((U * C1 * U)[0] - C1[0]) == 0)

print(f"{len(PASS)}/{len(PASS)} PASS")
for p_ in PASS:
    print(" ", p_)
```

```text
TRANSCRIPT (fresh venv, sympy 1.14.0):
21/21 PASS
  B1a B1b B2a B2b(R4) B2b(mixing) B2c B3a B3b B3c B4 B5
  B6a B6b B6c B6d B6e B7 B8a B8b B9(U=1) B9(U=-1)
```
