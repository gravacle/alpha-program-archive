# STAGE 8 — RULE-2 CANDIDATE — THE AUTHORED JOINT/COUPLING RULE FOR THE G2-F1 SLOT
## RULE2-BUILD (commission T15SR) — AUTHORED PROPOSED PHYSICS THROUGH THE MANDATE LANE

```text
STATUS BANNER, FIRST AND GOVERNING:

  PROPOSED_NOT_ADOPTED

  This artifact is a CANDIDATE authored under the principal's Act-5 word AUTHOR
  (ACT5_RULE2_AUTHOR_DECISION_2026-08-15.md, 96bd68e5...). It is an EXERCISED
  PERMISSION, never a derivation claim: the vacancy theorem STANDS — the sealed
  record does not derive this content, and this artifact does not say it does.
  No construction may consume it as premise. Ratification is a SECOND, SEPARATE
  principal act naming this artifact's digest and the opposite-lane check's
  digest in an ENTRY addendum. The registrar may not enter; the lanes may not
  enter; conversational summaries may not substitute. Until that addendum
  exists this candidate binds nothing and no fence moves.
```

Date: 2026-08-15
Role: author THE RULE-2 CANDIDATE — the joint/coupling rule the G2 state-port
F1 slot demands — as PROPOSED PHYSICS, frozen at seal, carried through the
complete common-origin descent with certificate, honesty rails on the face.
Slot: G2-F1 (debt D8, upgrade row U-D8 of the sealed conditional map).
Commissioning form: per the mandate pattern (ad9fc14e...) as instantiated by
the measure-supply mandate (b9716661...) and the Act-5 AUTHOR word (96bd68e5...).

```text
FENCES, LIVE FROM THE FIRST AUTHORED BYTE AND UNMOVED HERE:
  alpha_computed          = false
  proof_authorized        = false
  kappa_record_computed   = false
  JOINT_ANCHOR_DERIVED    = false
  no output inspection    (MANDATE CLAUSE 3: nothing downstream opened, scored,
                           evaluated, or compared during authoring)
  no downstream-target shaping ; no rank-pair selection ; no member selection
  no numeric evaluation of any physical quantity ; no measured-constant
  comparison ; every magnitude-like datum EXPLICIT, SYMBOLIC, UNASSIGNED
```

```text
NUMERAL DISCIPLINE (declared so the fence is checkable):
  numerals in this artifact occur ONLY as
    (i)  provenance coordinates — byte spans, line references, digest hex,
         dates, version tags, counts of record items; and
    (ii) sealed-text quotations and exact structural constants quoted from
         sealed formulas (the unit 1, the zero 0, sector labels, the sealed
         cell-count expressions such as 3^N, the sealed sign set {+1,-1}).
  No numeral is the value of any physical quantity. Nothing is evaluated,
  bounded, or compared to any measured constant anywhere in this artifact.
```

---

## 1. CONSUMED SEALS — EVERY ONE RECOMPUTED AT PATH BEFORE RELIANCE (2026-08-15)

Digest = full sha256 of the file at the stated path. Every row below was
recomputed by this commission before any semantic reliance; every row matched
the registrar-supplied pointer list.

| # | Path (root: /Users/bgm/MB Work/) | sha256 | Verdict |
|---:|---|---|---|
| 01 | alpha-program-archive/workspace/STAGE8_G2_STATE_PORT_DETERMINATION_V001.md | f7fd0c6048dfec8f08bd02f2ae008bcb329ccf4c0d7cb5b14a8c25359b57767a | SEAL OK |
| 02 | alpha-program-archive/workspace/STAGE8_CROSS_BLOCK_READ_V001.md | f3165db1a3b88a4d0667d3e3b214a55020aef31d424c5e3fce32d03cdb7774b1 | SEAL OK |
| 03 | alpha_supervision/FIVE_C_PRINCIPAL_ACTS_STAGING_V001.md | 064f0e2ffcdab596234e8f122202bbc30f6785ac9f36b7e70387e1e95596ee66 | SEAL OK (seal verified; content not opened — staging sheet, outside the mandate lane's read scope) |
| 04 | alpha_supervision/ACT5_RULE2_DEFERRAL_2026-08-14.md | c14d0b5c4996ab558a9046d701ccfe9f0e121751a0ec3971abf2c9c60ccf7523 | SEAL OK |
| 05 | alpha_supervision/ACT5_RULE2_AUTHOR_DECISION_2026-08-15.md | 96bd68e5a22df3a16a239d42104df361a72ccfa8dadb868e2ffda0d5532816b0 | SEAL OK |
| 06 | alpha-program-archive/workspace/ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md | ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213 | SEAL OK |
| 07 | alpha-program-archive/workspace/MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md | b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c | SEAL OK |
| 08 | alpha-program-archive/workspace/STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md | a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874 | SEAL OK |
| 09 | alpha-program-archive/workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md | 1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0 | SEAL OK |
| 10 | alpha-program-archive/workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md | e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75 | SEAL OK |
| 11 | alpha-program-archive/supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md | f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec | SEAL OK |
| 12 | alpha-program-archive/workspace/STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md | a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782 | SEAL OK |
| 13 | alpha-program-archive/workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md | 2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab | SEAL OK |
| 14 | alpha-program-archive/workspace/STAGE8_E3_EFFECT_DESCENT_V001.md | 31e69336440f057017d5e7e29d6d7a9190228a2b9ec76640cb34951ebe9e48e1 | SEAL OK |
| 15 | alpha-program-archive/workspace/STAGE8_E3_EFFECT_DESCENT_AUDIT_V001.md | b707cc8ba02fbfcff0bd40947fc21fd221ba284406e47aad4bc5c2d265b09f4b | SEAL OK |
| 16 | alpha-program-archive/workspace/STAGE8_DESC_JOINT_MAP_CHECK_CODEX2_V001.md | 9b89beeb7f5a7cc39e928efe90f032faf9fd45669716cefc692f89c75e421ba2 | VERIFIED-BY-RECORDED-DIGEST (sidecar ABSENT at path, absence itself of record; recomputed digest matches the recorded value) |
| 17 | alpha-program-archive/workspace/R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md | 10909b5c21e73ecf655462339a27bd645b8d35e3ad11fb6f8cb204c601992995 | COMPUTED AT PATH (declared scoped-sweep read; span [698,1630) rehashes to 2a99a925eb1288a8b0aa074963b2beeb4a082cb65d109e6821765a44a4f6c383, equal to the AXN map's pinned span digest) |

Span digests independently recomputed at path by this commission:

```text
member 09  [22959,24536)  3e0b43706ef6e329...  (the frozen tuple; read ONLY with
                          rows 10 and 11 per the overlay read rule)
member 08  [2622,5894)    e017e9da18d54761...  (P5 operational unpacking)
member 12  [17865,20008)  aa04e0ea2c901d0a...  (SM-1..SM-8 table)
member 13  [10824,11934)  4313fb92fd78f6b2...  (StatePort_U2_008 schema)
member 17  [698,1630)     2a99a925eb1288a8...  (omega_out inductive-limit state)
```

READ-RULE COMPLIANCE: member 09 (the ratified tuple) was consumed ONLY
together with MEMBER12_HEADER_OVERLAY_RECORD_V002.md and DoR-013.
status(member 09) := RATIFIED_FAMILY_LEVEL_BY_DOR_013, no-member clause
carried: *** NO MEMBER IS SELECTED, EVER *** — and nothing in this candidate
selects one. Its stale internal status surfaces are historical surface per the
overlay; this citation names DoR-013 and the overlay as the read rule demands.

---

## 2. THE SLOT, RESTATED FROM ITS SEALED TYPE (nothing retyped here)

The G2 state-port residual freedom F1, verbatim in force (f7fd0c60 §5):
one U1-invariant probability measure on `Y` — `omega_phys` — PLUS its
`state_descent_map` PLUS its `state_provenance_certificate`, landing in
`State(C(Y))`. The sealed completed-object architecture (a67ed435; 2b629122):

```text
A_SR  := A_src graded-tensor_min R_inf
B     := A_F_CTP  isomorphic to  C(Y)
A_C0  := A_SR graded-tensor_min B
i_R   : R_inf -> A_C0,  i_R(r) := 1_A_src tensor r tensor 1_B
i_B   : B     -> A_C0,  i_B(b) := 1_A_SR tensor b
res_B : State(A_C0) -> State(B),  res_B(Omega_C0) := Omega_C0 compose i_B
State(B) isomorphic to Prob_reg_Borel(Y)
X_N --q_N--> Q_N isomorphic to U(1)^N      (sealed history quotient)
```

The named construction of record for F1 (f7fd0c60 §5, "WHAT KIND OF OBJECT
SELECTS"): the SM-1..SM-8-conformant joint state with
`omega_hist = Omega_C0 compose i_B` — a Theta_hist-conformant descent landing
in `State(C(Y))` with provenance. The derived-effect route to the same content
is closed of record (U-D8, f3165db1 :298; the AUTHOR word, 96bd68e5). The
receiver is `StatePort_U2_008` (2b629122 [10824,11934)), which consumes
`rho_pre` itself as an input field; no invariant substitutes.

Standing bars carried into every clause below (f7fd0c60 §5 NON-SELECTORS;
f2a7838d; b9716661): selection by any response VALUE is barred; the ordered
rank pair `(r_0, r_ch)` may not be selected by any construction step; no
member of the ratified family is ever selected; a Dirac measure at one
classical history is lawful only as a disclosed authored selection and is
NOT taken here.

---

## 3. SYMBOLS — EVERY MAGNITUDE-LIKE DATUM EXPLICIT, SYMBOLIC, UNASSIGNED

Each row names its provenance. SK-8 cleanliness: no row names a downstream
target, no row carries a measured constant, no row carries lens content.
No symbol below is assigned a value anywhere in this artifact.

| Symbol | Type | Status | Provenance (sealed source) |
|---|---|---|---|
| `Omega_prim,N^v003` | frozen construction tuple, family level | consumed, never member-bound | 1be5f6a7 [22959,24536) with e85f444c + f2a7838d |
| `omega_S` | source state = `d_state(Omega_prim)` | forced output of the anchor THEOREM, symbolic | 1be5f6a7 §7.1; f2a7838d |
| `P_0, P_ch, I_src, Tr_A` | sector projectors, unit, trace | tuple members, symbolic | 1be5f6a7 [22959,24536) |
| `d_ready, d_law` | ready map, law map | tuple maps, consumed | 1be5f6a7 §7.2-7.3 |
| `Q_R, Q_P` | ready-ray and law-image projectors | derived of record (E3 item 1) | 31e69336 §2.4 |
| `omega_out` | admitted outgoing-record state on `R_inf` | forced marginal target (SM-3), symbolic | 10909b5c [698,1630); a67ed435 SM-3 |
| `Q_N` | sealed history quotient, `isomorphic to U(1)^N` | carrier, consumed | a67ed435 §2.3 |
| `B, Y, C(Y)` | branch algebra, its spectrum | carrier, consumed | a67ed435 §2.2-2.3 |
| `Z_N` | sealed unit-modulus charged-sector branch multiplier | symbolic, unassigned, never evaluated | 31e69336 §2.2 (CAS eq (4) lineage); 9b89beeb §3 R1/R4 |
| `nu` | the branch state/measure the read-off pairs with | THE SLOT — filled by this candidate, symbolically | 31e69336 §2.2 ("nu ... symbolic and unselected") |
| `nu_N` | this candidate's finite-level branch state | authored HERE, symbolic, parameter-free | this artifact §4 |
| `Haar(Q_N)` | the normalized translation-invariant state of the sealed compact group `Q_N` | exact symbolic object; existence/uniqueness is classical structure theory, no value | carrier from a67ed435 §2.3 |
| `kappa_ch` | symbolic sector-dimension ratio | untouched, unassigned | f7fd0c60 §0 |
| `(r_0, r_ch)` | ordered rank pair | UNTOUCHED — bar honored | f7fd0c60 §5 (F3) |
| `m1, m2` | formal `nu`-moments of `Z_N` | symbolic, never evaluated | 31e69336 §2.3 |
| `mu` | coherence prefactor | untouched, not computed, not classified | f7fd0c60 §0 |
| `rho_pre^SR` | sealed source-record pre-state `d_state tensor Q_R` | consumed as sealed | 31e69336 §2.2 |
| `Omega_C0^R2` | this candidate's joint state on `A_C0` | authored HERE by rule, symbolic | this artifact §4 |
| `omega_phys^R2` | this candidate's F1 measure | `= Omega_C0^R2 compose i_B`, symbolic | this artifact §4 |

No coupling constant, weight, temperature-like parameter, rank, dimension
value, or scale appears in the candidate. The authored core contains ZERO
free magnitude-like parameters (the no-dial manifest, §5.3).

---

## 4. THE CANDIDATE — RULE 2, "NORMALIZED-INVARIANT COMPLETION" (RULE2_NIC)

Rule 2 is an executable producer RULE, frozen at this artifact's seal, whose
output is then derived. The authorship lives in the rule, not in a declared
state: this is the form the sealed conditional demands (9b89beeb §5: "An
authored joint-extension rule can be SM-6-legitimate in principle only if it
is frozen before output and its resulting state, effects, domains, and
dynamics descend through one executable common-origin trace with the required
certificate. Authorship is not provenance.").

### 4.1 Clause R2-A — THE HISTORY-FACE FORM (the normalization half of the door)

```text
R2-A.  At every finite N, on the sealed history quotient Q_N isomorphic to
       U(1)^N, the history-face state is

           nu_N := Haar(Q_N)

       — the unique normalized translation-invariant state of the sealed
       compact group carrier, read in State(C(Q_N)) via the sealed
       State(B) isomorphic to Prob_reg_Borel(Y) identification at level N.

       The continuum object is the projective-limit state

           omega_phys^R2 := projlim_N nu_N

       along the sealed sequential cylinder embeddings, wherever the sealed
       embedding form is the unital inclusion f |-> f tensor 1  (certificate
       condition C-4/C-6 carries the exact consistency equation and its
       status; no unsealed embedding is invented here).
```

Reading of R2-A: the ratified family FORCES the normalized-identity invariant
state on the SOURCE face (the anchor theorem's `I_src/Tr_A(I_src)` form,
1be5f6a7 §7.1). R2-A carries THE SAME STRUCTURAL FORM — "the normalized
invariant state of the sealed carrier" — onto the history face, where the
sealed carrier is the compact group `Q_N` and the normalized invariant state
is its Haar state. The FORM-TRANSPORT IS AUTHORED (nothing sealed forces it;
§5's table says so in its own row); the normalization itself is FORCED
(probability typing, SM-2); the reality/U1 row is FORCED and is SATISFIED
rather than imposed: the Haar state is invariant under every group
translation, under inversion (the U1 reality/conjugation convention), and
under every group automorphism of the sealed carrier — whichever sealed
action the record's "U1-invariant" names, the Haar state is invariant under
it. Full support: no history, no member, no subgroup, no cell is selected —
the exact opposite of the refused Dirac form.

### 4.2 Clause R2-B — THE JOINT COMPLETION (the coupling half of the door)

```text
R2-B.  The completed joint state on A_C0 = A_src graded-tensor_min R_inf
       graded-tensor_min B is the product completion of the three sealed-or-
       R2-A faces:

           Omega_C0^R2 := Lambda_SR tensor nu ,

       where Lambda_SR is the state on A_SR induced by the SEALED
       source-record pre-state rho_pre^SR = d_state(Omega_prim) tensor Q_R
       (31e69336 §2.2 — consumed, not authored), and nu is R2-A's
       history-face state. Equivalently, on elementary tensors:

           Omega_C0^R2(a tensor r tensor f) := Lambda_SR(a tensor r) . nu(f).

       AUTHORED CONTENT OF R2-B: the residual cross covariance between the
       source-record face and the history face is set to ZERO — the joint is
       the product completion. Within every sealed quantifier this value is
       FORCED (the collapse theorem, 9b89beeb §2, and the E3 absolute
       theorem, 31e69336 §2.3: every SM-2-positive conditioned functional
       over the sealed apparatus has EXACTLY the product mixed term).
       BEYOND those quantifiers nothing sealed selects any covariance
       (9b89beeb: "no sealed selector of the residual non-product term is
       present") — R2-B authors the product there, visibly, as the minimal
       completion consistent with every forced instance.
```

Immediate typed consequences (constructions, not choices, once R2-A/R2-B
hold; f7fd0c60 §5 (F2)):

```text
omega_phys^R2 = Omega_C0^R2 compose i_B          (SM-4 as construction output)
Omega_C0^R2 compose i_R                          (the R-face marginal; C-3
                                                  carries its omega_out
                                                  reproduction status)
H_omega, pi_omega, rho_pre := canonical GNS/null-quotient completion of
Omega_C0^R2 (the sealed U2 :259-263 route; F2 remains downstream
construction, not choice, under the no-post-output-supplementation bar).
```

### 4.3 What Rule 2 does NOT contain

```text
- no free parameter, weight, density, kernel, or tilt (no-dial manifest §5.3)
- no member of the ratified family, no history point, no subgroup selection
- no rank-pair datum: (r_0, r_ch) is not read, not constrained, not used
- no response value, no downstream output, no battery datum, no lens content
- no Dirac component: the refused form is not smuggled at any weight
- no new carrier, embedding, quotient, law, or effect: every carrier object
  is the sealed one, cited; Rule 2 adds STATES only
- no numeric magnitude: the rule is closed-form symbolic on sealed objects
```

---

## 5. HONESTY OF RECORD — THE FORCED/AUTHORED TABLE, INGREDIENT BY INGREDIENT

Mandate clause in force (ad9fc14e): "The candidate MUST declare, ingredient
by ingredient, which parts are FORCED (with spans) and which constitute the
minimal AUTHORED CORE. An authored ingredient presented as forced voids the
candidate."

### 5.1 FORCED ingredients (each with its span; consumed, not chosen)

| # | Ingredient | Forcing source (span) |
|---:|---|---|
| F-01 | Slot type: `mu in Prob(Y)`, U1-invariant, + descent map + provenance certificate, landing `State(C(Y))` | b9716661 [1470,2433); f7fd0c60 §5 (F1) |
| F-02 | Statehood: positivity and normalization of every delivered state | a67ed435 SM-2 [17865,20008); a33be83c cond 2 [2622,5894) |
| F-03 | U1 reality row (the survivor table's realness requirement, consumed via the mandate's own restatement) | b9716661 [1470,2433) |
| F-04 | History binding as construction output: `omega_phys = Omega_C0 compose i_B`, never attached after the fact | a67ed435 SM-4 |
| F-05 | Record-marginal target: `Omega_C0 compose i_R` reproduces the admitted `omega_out` on every finite record subalgebra and its compatible limit | a67ed435 SM-3; 10909b5c [698,1630) |
| F-06 | Finite/quotient carriers and conventions: `q_N`, `Q_N isomorphic to U(1)^N`, sequential embeddings, sealed branch conventions, `B isomorphic to C(Y)`, `State(B) isomorphic to Prob_reg_Borel(Y)` | a67ed435 §2.2-2.3, SM-5 |
| F-07 | Source-face state `omega_S = d_state(Omega_prim)` via the anchor THEOREM (proved, not chosen), family level, no member | 1be5f6a7 §7.1 [22959,24536) with e85f444c + f2a7838d |
| F-08 | Sealed source-record pre-state `rho_pre^SR = d_state tensor Q_R` fixed at the tuple's T3-T5 stages | 31e69336 §2.2; 1be5f6a7 §7.4 |
| F-09 | The product VALUE of every mixed term inside the sealed quantifiers: (i) the unital sector-controlled Y-diagonal stock (collapse theorem); (ii) every SM-2-positive conditioned functional of the descended apparatus, selected/inclusive/mixture (E3 absolute theorem) | 9b89beeb §2, §3; 31e69336 §2.3 THEOREM, §2.4 item 7 |
| F-10 | Bars: no response-value selection; no `(r_0, r_ch)` selection by any construction step; no member selection ever; Dirac lawful only as disclosed selection (not taken) | f7fd0c60 §5 NON-SELECTORS and (F3); f2a7838d; b9716661 |
| F-11 | Target independence / no post-output supplementation; admission criteria frozen before downstream response or root information exists | a67ed435 SM-7; a33be83c cond 8 |
| F-12 | The mandate pattern itself: PROPOSED_NOT_ADOPTED carriage, second-word entry mechanics, falsifier and dissolution structure | ad9fc14e; b9716661; 96bd68e5 |

### 5.2 The minimal AUTHORED CORE (exactly two clauses; nothing else is authored)

| # | Authored clause | What exactly is authored | What is NOT authored with it |
|---:|---|---|---|
| A-01 | R2-A form transport | Carrying the normalized-invariant-state FORM from the forced source face to the history face: `nu_N := Haar(Q_N)`, and the projective-limit reading at the continuum | Normalization (F-02 forces it); reality/U1 row (F-03 forces it; Haar satisfies it); the carrier `Q_N` (F-06, sealed); no density, tilt, or support restriction is authored |
| A-02 | R2-B product completion | Setting the residual SR-face/history-face covariance to ZERO beyond the sealed quantifiers — adopting the product completion as THE joint | The product VALUE inside the sealed quantifiers (F-09 forces it there); the SR-face state (F-08, sealed); the embeddings (F-06, sealed) |

Void-on-misdeclaration acknowledged: if either A-row is later shown FORCED by
sealed bytes, the honest-declaration clause is not violated by that showing —
the entry instead DISSOLVES per §9.2 (the open forcing slot). If any F-row is
later shown to be authored rather than forced at its cited span, THIS
CANDIDATE IS VOID (ad9fc14e, honesty clause).

### 5.3 The no-dial manifest

```text
free magnitude-like parameters introduced by RULE2_NIC ......... NONE
free function-valued objects introduced by RULE2_NIC ........... NONE
selections from the ratified family ............................ NONE
histories, points, subgroups, or cells selected ................ NONE
data read from any downstream target during authoring .......... NONE
numeric values assigned to any symbol .......................... NONE
```

There is nothing in this candidate a tuner could turn. Every degree of
freedom the slot leaves open is closed by a STRUCTURAL clause (invariance or
product completion), each declared authored in §5.2, and the blind is intact:
no output was inspected, no downstream object opened, in the authoring of
either clause (MANDATE CLAUSE 3, held from the first authored byte).

---

## 6. THE COMPLETE COMMON-ORIGIN DESCENT, CARRIED WITH CERTIFICATE

Deliverable in the sealed five-part P5 form (a33be83c [2622,5894)):
(descent maps, common-origin construction trace, state/effect/domain property
certificate, dynamics-compatibility certificate, target-independent
admissibility manifest). Every obligation is proof-attempted ON THE CANDIDATE;
every failure or conditional is displayed as a falsifier or open leg, not
absorbed.

### 6.1 Descent maps (typed, executable, closed-form)

```text
d_state^R2(Omega_prim,N^v003) :=
    Omega_C0^R2 = Lambda_SR tensor Haar(Q_N)
    where Lambda_SR is induced by the sealed rho_pre^SR = d_state tensor Q_R;
    omega_phys^R2 := Omega_C0^R2 compose i_B ;
    rho_pre := canonical GNS density of Omega_C0^R2 (sealed U2 completion
    route; downstream construction, not choice).

d_effect(Omega_prim,N^v003, r)  := CONSUMED AS SEALED — the E3 canonical
    record-readout instrument (31e69336 §2.4 items 1-7, exhaustive with exact
    completeness). Rule 2 authors NO effect.

d_domain(Omega_prim,N^v003) := CONSUMED AS SEALED at finite N (the tuple's
    declared finite domains; the branch factor's sealed cylinder-function
    dense domain), extended to the candidate by: nu_N is defined on all of
    C(Q_N); the product completion is defined on the algebraic tensor product
    and extends by continuity on the sealed minimal completion.

d_dynamics(Omega_prim,N^v003) := CONSUMED AS SEALED — d_law = the ratified
    E_post law (1be5f6a7 §7.3), unchanged; its doubled/CTP use unchanged.
```

The candidate's frozen construction order, extending the tuple's sealed
T-order without moving it:

```text
R0  freeze RULE2_NIC (clauses R2-A, R2-B) at this artifact's seal —
    BEFORE any output of the rule exists anywhere;
R1  consume the sealed carriers (A_src, R_inf, B, Q_N, embeddings) by span;
R2s consume the sealed tuple maps d_state, d_ready, d_law under the overlay
    read rule (family level, no member);
R3s form Lambda_SR from the sealed pre-state (T3-T5 objects only);
R4s form nu_N := Haar(Q_N) from the sealed carrier alone;
R5s form Omega_C0^R2 := Lambda_SR tensor nu_N (product completion);
R6s read off omega_phys^R2 := Omega_C0^R2 compose i_B; deliver to the
    StatePort_U2_008 schema via the canonical completion;
R7s issue the certificate below; expose outputs only after R0-R7 are frozen.
```

No step consults any response value, any rank datum, any downstream target,
or any output of the rule itself. The dependency graph is acyclic on its
face: (tuple -> Lambda_SR), (sealed carrier -> nu_N), (Lambda_SR, nu_N ->
Omega_C0^R2 -> omega_phys^R2). No effect and no law-image datum enters any
state-forming step; `nu_N` never reads `Z_N` — it is built from the carrier,
not from the multiplier.

### 6.2 Certificate Cert_R2(Omega_prim) — the eight P5 conditions, proof-attempted

Statuses used: MET-EXACT (closes at finite N by exact symbolic structure on
sealed objects); MET-BY-CONSUMPTION (the sealed record supplies it; consumed
with span); CONDITIONAL (closes given a named sealed form this commission did
not consume; the deciding span is named); OPEN-LEG (a genuine gap, displayed
as falsifier surface).

```text
C-1 COMMON CARRIER — MET-EXACT at finite N; CONDITIONAL at the continuum.
    All outputs are states on the one sealed completed object A_C0 and its
    sealed factors; the embeddings i_R, i_B are the sealed canonical ones
    (a67ed435 §2.2). The continuum representation is the canonical GNS of
    Omega_C0^R2 (sealed U2 route) — construction, not choice.

C-2 STATE PROPERTIES — MET-EXACT.
    Haar(Q_N) is positive and normalized (classical structure of the sealed
    compact carrier; no value computed). Lambda_SR is the sealed pre-state.
    The product functional of two states, one factor commutative with
    commuting sealed ranges (a67ed435: "Their ranges commute"), is positive
    and normalized; at finite N every density is trace-class; normalization
    Omega_C0^R2(1) = 1 is the structural unit pairing, not an evaluation.

C-3 EFFECT PROPERTIES — MET-BY-CONSUMPTION.
    Rule 2 authors no effect. The record's effect family is the sealed E3
    canonical instrument: projector effects with EXACT completeness
    certificate sum_r E_r = I (31e69336 §2.4 item 1). The conditioned-effect
    port F4-F6 remains typed-no-instance of record (f7fd0c60 §5 (F4)) and is
    outside this slot.

C-4 DOMAIN COMPATIBILITY — MET-EXACT at finite N; CONDITIONAL at the limit.
    Finite N: all source/record objects live on the tuple's declared finite
    domains (31e69336 §2.4 item 3); nu_N is everywhere-defined on C(Q_N);
    the sealed cylinder dense domain carries the branch factor.
    THE LIMIT LEG, exact consistency equation displayed:
        REQUIRED:  nu_(N+1) compose j_N = nu_N   for the sealed sequential
        embedding j_N of cylinder algebras.
    PROVED for the unital coordinate/cylinder inclusion f |-> f tensor 1:
    the Haar marginal of a product compact group along a coordinate
    projection is the Haar state of the factor (exact structure).
    NOT PROVED for any other sealed embedding form. The sealed embedding's
    byte form was not consumed by this commission (it lives in the U3
    quotient artifact named at a67ed435 §2.3). If the sealed form is not the
    unital inclusion, this leg is OPEN — falsifier surface F-R2-STRUCT-3.

C-5 DYNAMIC COMPATIBILITY — MET-EXACT at finite N, symbolic.
    The sealed functional Tr(E_r U_BR rho_pre U_BR^dagger) and its
    normalized CTP version are well-defined on the candidate: by the sealed
    master lemma (31e69336 §2.2), conditioning the candidate with ANY record
    effect yields
        Lambda_E(a tensor f) = c_0(E) omega_S(P_0 a P_0) nu(f)
                             + c_ch(E) omega_S(P_ch a P_ch) nu(Z_N f)
    with nu := Haar(Q_N); both branch pairings nu(.) and nu(Z_N .) are
    everywhere-defined (Z_N is the sealed unit-modulus multiplier, a bounded
    continuous branch element); the same branch, quotient, and measure
    conventions are used on both CTP branches (the doubled law consumed
    unchanged).

C-6 COVARIANCE AND CAUSALITY — MET-EXACT at structure level; limit leg as C-4.
    Gauge: the history face is the sealed POST-quotient carrier Q_N (the
    complete common-gauge orbit invariant, a67ed435 §2.3) — the gauge action
    is already quotiented; nothing for it to move.
    U1/reality: Haar(Q_N) is invariant under every translation, every group
    automorphism, and inversion of the sealed carrier — whichever of these
    the record's U1/realness row names, the candidate satisfies it, and this
    invariance COMMUTES with restriction along the sealed embeddings.
    Causality/endpoint: the sealed causal factorization
    W_M = V_(M,N)(W_N tensor I_new) (10909b5c span) commutes with the
    product completion — restriction consistency on the record face is the
    sealed R3_4 computation itself, consumed.

C-7 PROVENANCE — PARTIAL: MET for every tuple-typed face; OPEN-LEG on strict
    one-trace unification. Displayed, not absorbed:
    (i)   source face, ready ray, law, effects, finite domains: descend from
          the ONE frozen tuple (E3 item 4 precedent, consumed);
    (ii)  nu_N: generated by the frozen clause R2-A from the SEALED carrier
          alone — no import, no post-hoc parameter (the no-dial manifest);
    (iii) omega_out (the SM-3 target): sealed sibling construction (10909b5c)
          built from (omega_in, W) — the same primitive family the tuple
          freezes — consumed as the receiver's own forced marginal target;
    (iv)  STRICT SM-6 READING ("ONE frozen construction trace", a67ed435
          SM-6; 9b89beeb §5): the candidate's faces root in the tuple PLUS
          two sealed sibling constructions (the R3_4 record limit; the U3
          history quotient). The record's own open item stands here (the
          M_2/M_3 "same-algebra binding is required" row, a67ed435). This
          is an OPEN LEG of the strict reading — falsifier surface
          F-R2-STRUCT-2 — and no claim is made that it closes.

C-8 TARGET-INDEPENDENT ADMISSIBILITY — MET.
    RULE2_NIC and its admission criteria are frozen at this seal; the rule
    consumes no downstream response, no root information, no rank datum, no
    measured constant, no battery output; the blind was held during
    authoring (no output inspection); the manifest is §5.3 and the SK set
    §9.3. Admissibility is decided by the certificate's own conditions,
    none of which reads a target.
```

### 6.3 The SM-1..SM-8 walk (the joint-state receiver's failure-capable rows)

```text
SM-1 typed source/codomain    MET — declared input class: the frozen tuple
                              (family level, under overlay) + the sealed
                              carriers; lands in State(A_C0) and, via the
                              canonical completion, the StatePort_U2_008
                              schema; total on the admitted class.
SM-2 joint positivity/norm    MET-EXACT (C-2).
SM-3 record marginal          CONDITIONAL — named SM-3-BIND: the sealed
                              architecture places rho_pre BEFORE the law and
                              omega_out AFTER it (outgoing). The obligation
                              reads: the candidate's R-face data, dressed by
                              the sealed law, reproduces omega_out — which is
                              R3_4's own construction PROVIDED its omega_in
                              is the tuple-descended Lambda_SR. That byte
                              identification was NOT consumed here; deciding
                              span: R3_4's omega_in definition. If it fails
                              at bytes: falsifier F-R2-STRUCT-4.
SM-4 history binding          MET — omega_phys^R2 := Omega_C0^R2 compose i_B
                              is a construction output of R5s-R6s, produced
                              with the joint, never attached after the fact.
SM-5 finite/quotient compat   MET at each finite N (post-quotient carrier;
                              sealed branch conventions; U1 reality by Haar
                              inversion-invariance); limit leg CONDITIONAL
                              exactly as C-4.
SM-6 common provenance        PARTIAL — C-7 verbatim; strict one-trace
                              unification OPEN-LEG, displayed.
SM-7 target independence      MET — C-8; K4 is not consulted; nothing is
                              tuned against K4-K7 or any response output.
SM-8 coverage statement       MET-BY-STATEMENT — RULE2_NIC generates exactly
                              ONE admitted joint per level (no enumeration
                              gap: the rule is deterministic on the sealed
                              inputs); bounded working class: the sealed
                              finite-N architecture plus the C-4-conditional
                              limit; REOPENING TRIGGER: any sealed
                              target-independent derivation landing an
                              SM-conformant joint — on which §9.2 dissolves
                              this entry.
```

### 6.4 The seven ground items — the executed E3 standard, met on the candidate's own construction

The sealed standard of "complete" (31e69336; audited 35/35 at b707cc8b):

```text
G-1 closed-form maps + exact completeness   DELIVERED — §6.1: every map is
    closed-form on sealed objects; the state-side completeness certificate is
    exact normalization (C-2); the effect-side certificate is consumed sealed.
G-2 embeddings                              DELIVERED-BY-CONSUMPTION — all
    five sealed embeddings named and used unchanged (i_src, record placement,
    i_R, i_B, sector embedding).
G-3 carrier domains                         DELIVERED at finite N (C-4);
    limit leg CONDITIONAL, displayed.
G-4 provenance, no import                   DELIVERED for tuple-typed faces;
    strict unification OPEN-LEG (C-7) — displayed as falsifier surface, not
    claimed.
G-5 law-generated outcome typing            DELIVERED-BY-CONSUMPTION — the
    outcomes remain the law-generated record classes of the sealed
    instrument; Rule 2 adds no outcome and authors no selection.
G-6 temporal non-circularity                DELIVERED — the R0-R7 order is
    frozen before output; the dependency graph (§6.1) is acyclic; no
    read-off object enters any state-forming step; nu_N is carrier-built and
    never reads Z_N, any effect, or any law image.
G-7 selected/inclusive/mixture calculus     DELIVERED SYMBOLICALLY — §7.
```

---

## 7. G-7 — THE CANDIDATE'S CONDITIONED CALCULUS, WITH THE SEALED DICHOTOMY CARRIED

All symbolic; nothing evaluated; `m1 := nu(Z_N)` and `m2 := nu(Z_N^2)` remain
formal symbols throughout. By the sealed master lemma applied AT the
candidate (nu := Haar(Q_N)):

```text
Lambda_E(a tensor f) = c_0(E) . omega_S(P_0 a P_0) . nu(f)
                     + c_ch(E) . omega_S(P_ch a P_ch) . nu(Z_N f)
```

The sealed positivity dichotomy (31e69336 §2.3) is a consequence map on
CONDITIONED functionals, not a selector on the pre-state; it is carried onto
the candidate without deformation:

```text
JOINT PRE-STATE  Omega_C0^R2: SM-2-positive ALWAYS (product of states, C-2).
                 Its every mixed term equals the product of its own marginals
                 BY CONSTRUCTION — hence no sealed forced mixed-term value
                 can contradict it inside the sealed quantifiers (F-09):
                 within the stock family and within the descended
                 conditioned family the sealed value IS the product value.

SELECTED ready   state; product; branch marginal nu.        (horn (a))
SELECTED written the normalized conditioned object exists as a STATE exactly
                 on the horn structure the record seals; under R2-A the
                 supported values of Z_N are those the sealed carrier gives
                 it on full support — whether horn (b)'s {Z_N = 1}-
                 conditioning is regular or singular under Haar depends on
                 the sealed byte form of Z_N on Q_N, which this commission
                 did NOT consume; both readings displayed, neither claimed.
INCLUSIVE        as a functional: the sealed F_N-weighted pairing with
                 defect kappa_ch(1-kappa_ch) c_0 c_ch (m2 - m1^2), all
                 symbols formal; wherever it is SM-2-positive it is the
                 product omega_S tensor nu (sealed theorem, consumed);
                 off-horn its non-product content is COHERENCE, NOT
                 STATEHOOD (sealed wording, carried verbatim as the
                 dichotomy's own classification — this candidate neither
                 repairs nor suppresses it).
MIXTURE          equals the inclusive functional exactly (sealed ledger
                 linearity, consumed).
```

DISPLAYED WITHOUT CLAIM: whether the sealed `Z_N` restricted to the sealed
`Q_N` is a nontrivial continuous character, some other unit-modulus
measurable form, or trivial on a sealed subset, was not decided here (the
deciding spans live in the U3/scalarization artifacts). Under R2-A the
formal moments `m1, m2` therefore REMAIN FORMAL; no horn is asserted of the
candidate's read-offs beyond what the sealed dichotomy itself forces, and no
moment is evaluated. What IS asserted: the candidate never needs a horn to
be a lawful pre-state — statehood of `Omega_C0^R2` is unconditional (C-2) —
and the ratified Z_inc chain's inclusive effect is pinned to the identity of
record (f7fd0c60 §5 (F4)), which this candidate consumes unchanged.

---

## 8. THE SM-6 LAWFULNESS CONDITIONAL — DISPLAYED IN ITS FROZEN-AND-CARRIED FORM

The sealed conditional (9b89beeb §5, RULE2_SM6 = LAWFUL-IF-FROZEN AND
COMMON-ORIGIN-CERTIFIED), instantiated on this candidate:

```text
AN AUTHORED RULE 2 IS CONDITIONALLY SM-6-LAWFUL ONLY IF:
  (i)   it is FROZEN BEFORE OUTPUT — met by construction: RULE2_NIC's
        clauses R2-A/R2-B are frozen at this artifact's seal (R0), and no
        output of the rule exists anywhere at seal time; the rule's outputs
        are then DERIVED, not declared (§6.1);
  (ii)  its resulting state, effects, domains, and dynamics DESCEND THROUGH
        ONE EXECUTABLE COMMON-ORIGIN TRACE WITH THE REQUIRED CERTIFICATE —
        carried in §6: effects/domains/dynamics are the sealed tuple-borne
        ones (consumed, unchanged); the state descends by the frozen rule
        from sealed carriers; the certificate is Cert_R2 (§6.2) with its
        conditions' statuses displayed one by one, including the strict
        one-trace OPEN LEG (C-7 iv) and the two named CONDITIONAL legs
        (C-4 limit; SM-3-BIND).

AUTHORSHIP IS NOT PROVENANCE — carried on the face: this artifact's
authorship of R2-A/R2-B contributes NOTHING to the certificate; every
certificate condition is argued from sealed structure or displayed open.
THE CONDITIONAL IS NOT DISCHARGED HERE: with C-7's strict leg OPEN and two
CONDITIONAL legs named, this candidate is SM-6-lawful CONDITIONALLY, in
exactly the sealed sense, and says so rather than rounding up.
```

---

## 9. HONESTY RAILS

### 9.1 The vacancy display (on the face, as commissioned)

```text
THE VACANCY THEOREM STANDS. The sealed record does not derive this content:
the derivation routes to the F1 measure are exhausted of record (b9716661:
"arrows do not constrain states"; f7fd0c60: THETA_HIST = STOPPED, both
routes closed, opposite-lane CONFIRMED; 9b89beeb: no sealed selector of the
residual non-product term). THIS ENTRY IS AN EXERCISED PERMISSION — the
principal's Act-5 AUTHOR word exercised through the mandate lane — and at no
point a derivation claim. Nothing here upgrades AUTHORED to FORCED, and the
candidate's adoption, if it ever occurs, is a disclosed-input decision of
record, not a theorem.
```

### 9.2 The dissolution clause (the open forcing slot, held open)

```text
future_derivation_can_select_the_joint = NO_VERDICT.
This candidate does not prove, and does not rest on, any claim that a future
target-independent derivation cannot select the joint/coupling content. IF
such a derivation lands — in particular, if a descended, non-circular
selective instrument or any sealed construction later DERIVES an
SM-conformant joint (the E3-era deciding ground, 9b89beeb §4.3; the SM-8
reopening trigger, §6.3) — THEN this entry DISSOLVES INTO THAT DERIVATION:
the authored core (A-01, A-02) vanishes from the program's premise set, and
what remains is whatever the derivation forces. Dissolution is not failure;
it is the retire-by-derivation principle this lane inherits.
```

### 9.3 SK-R2 — the voiding set, adapted from the mandate's voiding clauses

```text
SK-R2-1  AUTHORED-AS-FORCED: any ingredient of §5.2 shown presented as
         forced, or any §5.1 row shown authored at its span — CANDIDATE VOID.
SK-R2-2  BLIND BREACH: any output inspection, downstream-target shaping, or
         battery/score consultation in this candidate's authoring shown at
         bytes — CANDIDATE VOID.
SK-R2-3  BLIND TEST: the preregistered blind test at the cascade's end
         scores every output this content feeds; a failed blind score VOIDS
         the entry and everything TYPE-P on it (the sealed blast-radius
         pattern), without renegotiation.
SK-R2-4  STRUCTURAL CONTRADICTION: any sealed forced result shown, at bytes,
         to contradict the candidate's content VOIDS the entry without
         awaiting the cascade. Named live surfaces:
           (a) a sealed forced NON-PRODUCT SM-2 joint mixed term;
           (b) a sealed forced support law on the PRE-state contradicting
               full support (e.g. horn (c) forced on omega_phys itself);
           (c) the sealed sequential embedding form breaking the C-4
               consistency equation (F-R2-STRUCT-3);
           (d) SM-3-BIND failing at bytes: R3_4's omega_in shown unequal to
               the tuple-descended Lambda_SR (F-R2-STRUCT-4);
           (e) the strict SM-6 one-trace requirement adjudicated of record
               as UNMEETABLE by any multi-artifact rooting (F-R2-STRUCT-2
               resolved against the candidate);
           (f) a descended, non-circular instrument lawfully producing
               sector-DEPENDENT branch marginals (the 1082 witness realized
               by descent) — contradicts R2-B's product completion.
SK-R2-5  MEMBER/RANK/VALUE BREACH: any construction step shown to select a
         family member, a history point presented as forced, the ordered
         rank pair, or any response value — THE STEP IS VOID and with it
         this candidate's delivery (the sealed step-voiding rule carried).
SK-R2-6  NUMERIC BREACH: any numeral in this artifact shown to function as
         an evaluated physical magnitude or measured-constant comparison —
         CANDIDATE VOID (the numeral discipline is a checkable fence, not
         prose).
SK-R2-7  ENTRY BREACH: any consumption of this candidate as premise before
         the entry addendum exists — the CONSUMING construction is unlawful
         of record; this candidate remains PROPOSED_NOT_ADOPTED regardless.
SK-R2-8  PROVENANCE BREACH: any ingredient shown imported post hoc — any
         object in §6.1 not traceable to its cited sealed span or to the
         frozen clauses — CANDIDATE VOID (SM-6/SM-7 enforcement).
```

---

## 10. CHOICE LEDGER — EVERY LIVE CHOICE, ITS ALTERNATIVES, AND WHAT WAS CONSUMED

Per the program's actual-surface guard: every choice displayed with its
alternatives; no choice hidden in a convention; CHAIN CHOICE AUDIT position:
this candidate sits BEFORE fence 2 of its own chain — the ledger is complete
as of seal and must be re-walked by the opposite-lane check.

```text
CH-1  THE HISTORY-FACE STATE FORM (the authored door itself)
      chosen: Haar(Q_N) — the normalized-invariant form (R2-A).
      alternatives, each lawful only as disclosed authorship, none taken:
        (i)   Dirac at one classical history — refused-as-derivation of
              record (f7fd0c60 §5 quoting TH); lawful only as disclosed
              selection (b9716661); maximal member-like selection; NOT taken.
        (ii)  kernel-supported ("horn (c)") measures — statehood of every
              charged read-off, but: selects a proper sealed subset;
              in tension with cylinder consistency for character-shaped
              Z_N; converts a conditional dichotomy into a pre-state
              support law nothing sealed imposes; NOT taken.
        (iii) any density/tilted measure — introduces a function-valued
              dial; maximal tuning surface; violates the no-dial manifest;
              NOT taken.
      why Haar: zero parameters; full support (no selection of any kind);
      satisfies every forced invariance row under every sealed reading;
      the same structural form the ratified family FORCES on the source
      face; the unique translation-invariant state of the sealed carrier.

CH-2  THE COUPLING (the joint completion)
      chosen: product completion, zero residual covariance (R2-B).
      alternatives: any non-product covariance — nothing sealed selects one
      (9b89beeb, corrected ruling), every sealed quantifier forces the
      product value where it speaks (F-09), and any non-product choice
      would add exactly the kind of unforced structure the honesty clause
      exists to expose; NOT taken.

CH-3  SCOPE OF AUTHORSHIP
      chosen: author STATES ONLY; consume every sealed carrier, embedding,
      law, and effect unchanged.
      alternative: author a new instrument/effect to force horn structure —
      that is the E3-era's own open ground, a DIFFERENT door than the one
      the principal opened (AUTHOR on the coupling door), and would smuggle
      an unauthorized second authored object; NOT taken.

CH-4  THE LIMIT READING
      chosen: projective-limit reading conditional on the sealed embedding
      form, displayed as C-4 with its exact consistency equation.
      alternative: assert the continuum object unconditionally — would
      present an unverified sealed form as consumed; NOT taken.

CH-5  SM-3 POSITION
      chosen: display SM-3-BIND as a byte-check obligation with its
      deciding span named.
      alternative: silently identify omega_in with Lambda_SR — an
      identification by prose label, the exact flattening the record
      warns against; NOT taken.

CONSUMED-AS-PARAMETER (not choices): the 16 pointer-list seals; the overlay
read rule; the family-level ratification with no-member clause; the E3
descent and its audit; the joint-map check's corrected rulings; the mandate
pattern; the Act-5 AUTHOR word; all standing fences.
```

---

## 11. TOY_SEPARATION

```text
THE CANDIDATE IS TYPED ON THE ACTUAL SEALED SURFACE, NEVER A TOY:
  - the carriers are the sealed A_src, R_inf, B isomorphic to C(Y),
    A_C0, Q_N isomorphic to U(1)^N — cited at their sealed spans, not
    modeled, not simplified, not re-dimensioned;
  - the maps are the sealed d_state/d_ready/d_law and the sealed embeddings,
    consumed unchanged;
  - the read-off calculus in §7 is the SEALED master lemma applied at the
    candidate, not a toy re-derivation — its formulas are quoted structure,
    and every symbol in them stays formal.
NO TOY OBJECT APPEARS IN THIS ARTIFACT. No illustrative model, no
reduced-dimension example, no stand-in algebra was constructed anywhere in
the authoring. (The sealed E3 CAS battery's own finite modeling choices are
that artifact's, cited as sealed record, not re-executed here.)
```

---

## 12. FLAG BLOCK

```text
alpha_computed                 = false
proof_authorized               = false
kappa_record_computed          = false
JOINT_ANCHOR_DERIVED           = false
coupling_evaluation_authorized = false        (carried from f7fd0c60)
output_inspection              = NONE PERFORMED (mandate clause 3 held)
downstream_target_shaping      = NONE (no target named, read, or scored)
rank_pair_(r_0,r_ch)           = UNTOUCHED (bar honored)
member_selection               = NONE (no-member clause carried)
response_value_selection       = NONE
measured_constant_comparison   = NONE
numeric_evaluation_of_physics  = NONE (numeral discipline declared §0)
candidate_status               = PROPOSED_NOT_ADOPTED
entry_addendum                 = DOES NOT EXIST (and cannot be authored here)
opposite_lane_check            = NOT YET RUN (this artifact awaits it)
freeze                         = AT SEAL (sidecar digest = the freeze line)
git                            = NO ACTION
registers/trackers/roads/plans = NOT READ (row 03 seal-verified only, unopened)
```

---

## 13. SWEEP CUTOFF — DECLARED

```text
READ SET, COMPLETE: the sixteen pointer-list instruments (§1 rows 01-16;
row 03 hash-verified only, content not opened), consumed at the stated spans
or sections; plus ONE declared scoped-sweep read: row 17
(R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md, span [698,1630) only,
to type omega_out — its span digest equals the AXN map's pin).

SWEEP CUTOFF: NO OTHER FILE WAS OPENED. In particular: no register, tracker,
road, plan, ledger, lens, or continuation file; no relay inbox; no cleanroom
file (ROOT_C was not entered); no generator executed; "Q-..." row IDs are
carried as registrar cross-references only, EXPECTED-UNLOCATABLE here, and
were not resolved. Deciding spans NAMED but NOT consumed (left to the check
lane, honestly): the U3 sequential-embedding byte form (C-4); R3_4's
omega_in definition (SM-3-BIND); the Z_N-on-Q_N byte form (§7 display).
CUTOFF TIME: at this artifact's seal, 2026-08-15.
```

---

## 14. THE FREEZE

```text
THE CANDIDATE TEXT IS FROZEN AT THIS ARTIFACT'S SEAL. The rule content is
clauses R2-A and R2-B exactly as §4 states them; the sidecar
STAGE8_RULE2_CANDIDATE_T15SR_V001.md.seal.sha256 carries the full sha256 of
this file's bytes and IS the freeze line: any candidate purporting to be
Rule 2 with a different digest is a DIFFERENT candidate. No re-statement,
summary, or conversational paraphrase is the candidate. Downstream
consumption order of record: (1) opposite-lane check names this digest;
(2) the principal's ENTRY addendum names this digest and the check's digest;
(3) only then may any construction consume the content — and then only as
PROPOSED-physics-RATIFIED-BY-ADDENDUM, TYPE-P on its cited premises.

END OF CANDIDATE. PROPOSED_NOT_ADOPTED.
```

