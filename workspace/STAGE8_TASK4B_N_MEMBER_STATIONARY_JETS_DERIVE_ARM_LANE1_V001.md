# STAGE8 TASK 4B: N-MEMBER STATIONARY JETS, DERIVE ARM - LANE 1 V001

Date: 2026-08-03  
Task: PASTE 451 / Task 4b / seventh race, derive arm  
Lane: CODEX LANE 1  
Register head at preflight: Q-369  
Custody: derivation only; no authored jet content and no registration authority

```text
LEAD_RESULT = GENERATION_CHAIN_IS_INTERFACE_ONLY

JETS_DERIVED = not | exact completed stationary components remain TYPE-U
FINITE_ACTIVE_JET = fully derived zero through every order
COMPLETED_STATIONARY_JET = TYPE-U | off-section component record absent
STATIONARY_POINT = TYPE-U | no executable stationary solve without that record

UNDETERMINED_CONSTANT_LIST = not finite
RESIDUE_TYPE = executable covariant action germ plus its stationary two-jet and point

RECIPROCAL_LOOP_RESPONSE = exact Schur expression | components unevaluated
S8_A_RESPONSE = exact covariant Schur expression | components unevaluated
FINITE_ACTIVE_RESPONSE = 0 | p-free | nu-free

VOID_CANDIDATE = false
P_VERDICT_DECLARED = false
RESPONSE_VALUE_COMPUTED = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The ratified equation `Gen_m(delta_div,m)=phi_m` fixes a certified role in
the DoR-017 tuple; it does not give an executable map.  Neither R1 nor the
divergence draft supplies a formula for `delta_div,m`, `Depth_m`, `Accum_m`,
or `Gen_m`.  Consequently the divergence datum does not determine a
stationary two-jet by differentiation.  Writing
`D^2 Gen_m(delta_div,m)` would only rename the missing operation.

This does not reopen the DoR-017 member ruling.  The N member is one ratified
extensional object, not a family awaiting another selection.  The finding is
that its component representation at the stationary locus is absent from the
ratified bytes.  The derive arm therefore proves the maximal constraints on
that record and identifies the exact interface the author arm would have to
instantiate.

---

## 0. Preflight, seals, and register sweep

`alpha_supervision/LOCKED_PROCESS.md` was read in full before work began.
Its sidecar verified.  The live questions-settled register and its sidecar
verified; the head was Q-369.  The named load-bearing artifacts were
hash-verified before substantive reading.

| Authority | Verified SHA-256 | Use |
|---|---|---|
| Lane-1 stationary-response computation | `be570c182ef875b557395b62c382ee875420ac0462e2efb5774e9600f794b27a` | exact stationary equation and Schur form |
| Lane-2 response cross-verification | `cac131d949a917576e30332b2f4ec76ca7db57d6d4683e91cfeac658994c499b` | confirmed component boundary and p/nu scope |
| action-comparison square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | R1 generation interface and R5 package |
| transverse-action draft | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | divergence bridge failure and flat-family countermodel |
| DoR-017 decision | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | N-member ruling and downstream falsifier |
| DoR-016 law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled access and ordered per-system towers |
| DoR-015 field V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | physical cycle quotient and covariance class |
| Q-326 flat-family determination | `30532ecb2c08d21e28af05f1228b9c29264be99731f63c8cf5b30273bb51e7b8` | off-section jet and stationary-shift countermodel |

The register was swept before deriving for the following settled questions:

| Register item | Settled content used here |
|---|---|
| Q-327 | four inequivalent depth/divergence notions; no then-sealed map to a cycle action |
| Q-347 through Q-357 | map layer closed; descent transports data but does not supply an action coefficient or stationary jet |
| Q-358 | quotient-only comparison square forced; action member and stationary package were authored residue |
| Q-361/Q-362 | member and normalization uniqueness refuted; scalar rescaling survives finite data |
| Q-367 | N member ratified with symbolic `nu` and the three-layer test discipline |
| Q-368 | exact stationary Schur expression computed; completed value waits on the stationary jet |
| Q-369 | cross-verification confirms no executable generator in the ratified stack |

An independent full-workspace definition sweep was also run for executable
assignments to `Gen`, `Depth`, `Accum`, and `delta_div`.  Its only live R1
hits were the typed interface equations quoted in Section 1; it found no
closed form, algorithm, component table, or evaluation rule.  This repeats
Q-369's boundary rather than assuming it.

```text
DOES_THE_OBJECT_EXIST = yes | the ratified member and its jet obligation
IS_THE_VERSION_CURRENT = yes | Q-369
ARE_ITS_INPUTS_PRESENT = yes for constraints and finite jets |
  no for completed component evaluation
PREFLIGHT = PASS
```

---

## 1. D1 - the generator question

### 1.1 What R1 actually ratifies

Square V004 defines the installed member by

```text
m=(delta_div,m,Depth_m,Accum_m,Gen_m,phi_m,Norm_m,nu_m),

Gen_m(delta_div,m)=phi_m in Act_phys^017.          (D1-1)
```

Its certificate types the fields as follows:

```text
delta_div,m : certified divergence/action datum with units;
Depth_m     : common-origin depth map into the record system;
Accum_m     : stage-compatible accumulation/subtraction rule;
Gen_m       : target-independent generator;
phi_m       : smooth quotient action, reality-covariant,
              quotient-compatible, identity-anchored and all-orders flat;
Norm_m      : homogeneous normalizer on the generated image;
Norm_m(phi_m)=nu_m.
```

Those are signatures and certificates.  V004 contains no displayed rule

```text
delta_div,m -> Depth_m(delta_div,m)
            -> Accum_m(...)
            -> an explicit scalar function phi_m(y),
```

and DoR-017 adopts no additional formula.  In particular, no authority gives
the value of `delta_div,m` as a completed cycle functional, the action of
`Depth_m` or `Accum_m` on it, or a component expression for `Gen_m`.

Therefore evaluation at a completed physical point `y` cannot proceed:

```text
Gen_m(delta_div,m)(y)                  = not executable,
D_A Gen_m(delta_div,m)(y)              = not executable,
D_A D_B Gen_m(delta_div,m)(y)          = not executable.   (D1-2)
```

This is a typing obstruction, not a differentiation difficulty.

```text
GENERATOR_ROLE_RATIFIED = true
GENERATOR_FORMULA_RATIFIED = false
FORMAL_DERIVATIVE_IS_COMPONENT_EVALUATION = false | TYPE-R
```

### 1.2 The stationary jet that the response consumes

Let `y=(C,K)` be the completed R5 common domain and let `y_star,m` solve

```text
D_C Gamma_base(p_i;y_star,m)
  +D_C phi_m(y_star,m)=0.                       (D1-3)
```

The missing direct member blocks are exactly

```text
F_CC^m := D_C^2 phi_m(y_star,m),
F_CK^m := D_C D_K phi_m(y_star,m),
F_KC^m := D_K D_C phi_m(y_star,m),
F_KK^m := D_K^2 phi_m(y_star,m).                 (D1-4)
```

The stationary point also needs the first member covector
`D_C phi_m(y_star,m)`.  Neither `(D1-3)` nor `(D1-4)` is numerically or
componentwise evaluable from `(D1-1)`.

### 1.3 What is nevertheless derived exactly

Let `S_N` be the active finite section.  All-orders flatness and the R2/R4
restriction certificates give

```text
j^r(rho_Gamma,N phi_m)|_(S_N)=0 for every finite r,

D_C phi_m|_(S_N)=0,
D_C^2 phi_m|_(S_N)=0,
D_C D_K phi_m|_(S_N)=0,
D_K D_C phi_m|_(S_N)=0,
D_K^2 phi_m|_(S_N)=0.                            (D1-5)
```

Thus the complete first and second active finite jet is derived: it is zero.
This does not determine the jet at `y_star,m`, because neither DoR-017 nor
R5 proves

```text
y_star,m in S.                                   (D1-6)
```

The exact distinction is

```text
FINITE_ACTIVE_STATIONARY_JET = zero | TYPE-P under DoR-017 certificates
PHYSICAL_STATIONARY_LOCUS_IN_ACTIVE_SECTION = NO_VERDICT
COMPLETED_STATIONARY_JET = TYPE-U
```

### 1.4 The exact stationary-shift formula

Although the shift cannot be evaluated, its dependence is forced.  Along a
certified scaling orbit `phi_lambda=lambda phi_m`, at fixed carried `K`, the
implicit-function theorem on the R5 reducing complement gives

```text
d C_star(lambda)/d lambda
 =-[H_CC^base(p;C_star,K)
     +lambda D_C^2 phi_m(C_star,K)]^(-1)
    D_C phi_m(C_star,K).                         (D1-7)
```

At `lambda=1`, `(D1-7)` is the N-member stationary shift.  It is an exact
operator identity but not a component value.  It also shows why symbolic
`nu` cannot by itself determine the shift: the inverse and evaluation point
depend nonlinearly on the unknown member direction.

---

## 2. D2 - what the log divergence fixes

### 2.1 Forced information

The ratified divergence/action datum and R1 fix these facts:

1. the member is nonzero and generated rather than selected from the live
   covariant family after ratification;
2. its action units and symbolic homogeneous label `nu_m` are declared;
3. it is a natural section under every admitted realization automorphism;
4. it obeys reality, quotient, batching, identity-anchor, and finite
   all-orders-flatness certificates;
5. along the disclosed scaling orbit,

```text
phi_m -> lambda phi_m,
F_AB^m -> lambda F_AB^m at a fixed evaluation point,
nu_m -> lambda nu_m.                              (D2-1)
```

These determine transformation laws and homogeneity.  They do not determine
the tensor direction of any `F_AB^m`.

### 2.2 Why the divergence datum does not determine the jet form

The divergence draft already supplies the decisive countermodel to the
inference from a divergence scalar and flatness to a stationary jet.  In a
local real quotient chart `(x,t)`, with active section `S={t=0}`, let

```text
f_2(t)=exp(-1/t^2) for t!=0, f_2(0)=0,
f_4(t)=exp(-1/t^4) for t!=0, f_4(0)=0,

phi_(a,j)(x,t)=a x f_j(t), j in {2,4}.            (D2-2)
```

Both candidates are smooth and all-orders flat on `S`; both can be made
reality-covariant on the real chart and have the same identity anchor.  Yet

```text
D_x D_t phi_(a,j)=a f_j'(t),                      (D2-3)
```

and the two functions have different generic off-section jets.  Multiplying
either by the same symbolic divergence coefficient does not distinguish
them.  This is not a proposal to replace the ratified member.  It is a
logical model proving that the displayed divergence coefficient plus the
displayed R1 certificates do not entail a unique stationary jet formula.

The scalar equation `Norm_m(phi_m)=nu_m` does not repair the inference.  No
formula or injectivity theorem for `Norm_m` is ratified.  A scalar label is
therefore not an inverse coordinate on the infinite-dimensional action-germ
space.  Claiming otherwise would add precisely the generator-and-uniqueness
certificate that the draft identified as missing.

```text
LOG_DIVERGENCE_FIXES_UNITS_AND_SCALING = true
LOG_DIVERGENCE_FIXES_JET_DIRECTION = false | TYPE-R as an inference
NORM_IS_JET_RECONSTRUCTION_MAP = false | not ratified
JETS_DETERMINED_UP_TO_FINITE_CONSTANTS = false
```

### 2.3 Stationary-point countercheck

For the fixed test base action

```text
Gamma_base(x,t)=(1/2)x^2+(1/2)(t-t_0)^2,
```

the member `(D2-2)` gives

```text
D_x(Gamma_base+phi_(a,j))=x+a f_j(t)=0,
x_star,(a,j)(t)=-a f_j(t).                        (D2-4)
```

At the active section, all choices give the same zero jet and unshifted
shadow.  At generic `t!=0`, their stationary points and mixed jets differ.
Equation `(D2-4)` refutes any derivation of the completed stationary point
from finite flatness or from the unnamed scalar divergence coefficient.

---

## 3. D3 - exact residue

The residue is not a new member choice.  It is an executable representation
of the already-ratified N member.  The author arm's exact load is one
coherent record:

```text
N_MEMBER_STATIONARY_JET_INSTANTIATION := {
  delta_div,m as an explicit completed divergence/cycle datum,
  executable Depth_m and Accum_m maps on that datum,
  executable Gen_m producing phi_m on D_017,
  explicit Norm_m action with Norm_m(phi_m)=nu_m,
  the completed first covector D_C phi_m,
  the four completed second blocks F_CC,F_CK,F_KC,F_KK,
  the stationary point/family y_star,m solving (D1-3),
  the R5 reducing-domain and invertibility witness at y_star,m,
  the p/rank dependence or independence law of the instantiated record,
  finite restriction, covariance, reality, batching, and anchor certificates
}.
```

The first eight fields must be mutually consistent; independently assigning
four Hessian blocks would not instantiate a scalar action germ.  In
particular, symmetry/integrability of mixed derivatives and the stationary
equation are part of the interface.

```text
AUTHORED_RESIDUE = N_MEMBER_STATIONARY_JET_INSTANTIATION
RESIDUE_CARDINALITY = function/germ data | not a finite constant list
RATIFIED_MEMBER_REOPENED = false
DERIVE_ARM_AUTHORED_CONTENT = none
```

---

## 4. D4 - substitution into the confirmed response

### 4.1 General completed expression

For `AB in {CC,CK,KC,KK}`, define

```text
H_AB^m(p;y_star,m)=H_AB^base(p;y_star,m)+F_AB^m.
```

On the R5 reducing complement,

```text
I_CC^m=(H_CC^base+F_CC^m)^(-1),

S_m=H_KK^base+F_KK^m
   -(H_KC^base+F_KC^m) I_CC^m
     (H_CK^base+F_CK^m),

Response_m=RetExtract_m(S_m) at y_star,m.         (D4-1)
```

This is the maximal exact substitution.  No cancellation or component value
follows without the residue in Section 3.

### 4.2 Reciprocal-loop stage

Let `c_loop` denote the minimal reciprocal cycle.  The completed response on
that carrier is the restriction of `(D4-1)`:

```text
Response_loop,m
 =RetExtract_loop[
    H_KK,loop^base(p)+F_KK,loop^m
   -(H_KC,loop^base(p)+F_KC,loop^m)
    (H_CC,loop^base(p)+F_CC,loop^m)^(-1)
    (H_CK,loop^base(p)+F_CK,loop^m)
  ]_(y_star,m).                                   (D4-2)
```

Its active finite restriction is exact:

```text
rho_H,N F_AB,loop^m|_(S_N)=0,
rho_H,N Response_loop,m|_(S_N)=0.                 (D4-3)
```

Thus the reciprocal-loop finite retarded block is p-free and nu-free.  The
completed block remains componentwise TYPE-U.

### 4.3 S8-A stage

On the S8-A rank-two cycle carrier, `(D4-1)` holds as a covariant block
matrix.  For the signed exchange `sigma`, R1-COV and the completed Hessian
cube force

```text
F_AB,sigma(G)^m sigma_B=sigma_H,A F_AB,G^m,
S_sigma(G)^m sigma_K=sigma_H,K S_G^m,
Response_sigma(G)^m=sigma_R Response_G^m.         (D4-4)
```

Orientation reversal conjugates coefficients and reverses the cycle sign;
it does not select a cycle component.  Equations `(D4-2)` through `(D4-4)`
are exact structural results.  They do not supply the entries of `F_AB^m`.

At the active finite S8-A reference,

```text
F_AB,N^m=0,
ordered-retarded block=0,
p appearance=none,
nu appearance=none.                               (D4-5)
```

### 4.4 p/nu appearance ledger

| Object | p appearance | nu appearance | Sensitivity |
|---|---|---|---|
| finite active member jet | none; zero | none; zero | member-independent over admitted flat members |
| finite active retarded response | none; zero | none; zero | member-independent |
| completed base blocks | explicit only through the base `omega_i/kappa_i` route and its completion | none from the base | member-independent base data |
| direct completed member jet `F_AB^m` | no dependence or independence law is ratified | homogeneous with the member along `(D2-1)`, but direction unknown | member-sensitive |
| stationary point `y_star,m` | may inherit base p and any member p/rank content | nonlinear through the stationarity solve | member-sensitive |
| inverse, Schur block, response | may inherit p through base, member, and moving point | not linear in nu in general | member-sensitive value; member-independent operation |

```text
P_EXPLICIT_ENTRY = base restrictions only
P_MEMBER_DEPENDENCE = NO_VERDICT / TYPE-U
P_MEMBER_INDEPENDENCE = NO_VERDICT / TYPE-U
NU_DIRECT_JET_HOMOGENEITY = derived
NU_RESPONSE_HOMOGENEITY = not derived
P_VERDICT_DECLARED = false
```

---

## 5. D5 - falsifiers and certificate checks

### 5.1 Finite falsifiers run

| Test | Computation | Outcome |
|---|---|---|
| active two-jet | apply all-orders flatness before restriction differentiation | every member block in `(D1-5)` is zero; PASS |
| Q-243/Q-279 retarded shadow | common-source derivatives vanish before `omega_i` or `kappa_i` can enter | retarded block zero, p-free, nu-free; PASS |
| reciprocal-loop restriction | substitute `rho_H,N F_AB=0` into `(D4-2)` | exact finite zero table recovered; PASS |
| S8-A exchange | apply `(D4-4)` to both paths of the R1-COV/R5 cube | both paths equal; PASS |
| identity zero-extension | added identity cell contributes no member jet and no common-source leg | zero preserved; PASS |
| batching | retain DoR-016 ordered per-system towers and apply no joint contraction | per-system zeros preserved; PASS |
| reality | zero finite blocks are fixed; completed coefficients transform by sign plus conjugation | PASS as a transformation law |

No finite falsifier distinguishes the missing off-section components.  That
is expected: the member is all-orders flat on the very section that the
finite falsifier probes.

### 5.2 No-derivation falsifier run

The pair `(D2-2)` has identical active jets and satisfies the same displayed
flatness/anchor structure while producing different off-section mixed jets
and stationary shifts.  Therefore any claim that the displayed R1 fields
alone compute `F_CK^m` or `y_star,m` fails this concrete test.

```text
R1_SIGNATURE_IMPLIES_COMPONENT_JET = KILLED
LOG_SCALAR_PLUS_FLATNESS_IMPLIES_STATIONARY_POINT = KILLED
```

### 5.3 DoR-017 downstream certificates

The derived finite zero jets satisfy covariance, reality, batching, and
restriction exactly.  The unevaluated completed jet is constrained by
R1-COV and the R5 naturality cube, but no component claim has been made for
it.  Hence no certificate fails and the DoR-017 void clause does not fire.

```text
DOR017_COVARIANCE = PASS | finite zeros and formal completed law
DOR017_REALITY = PASS | finite zeros and formal completed law
DOR017_BATCHING = PASS | ordered per-system, no joint contraction
DOR017_RESTRICTION = PASS | exact Q-243/Q-279 reproduction
VOID_ON_DOWNSTREAM_FAILURE_TRIGGERED = false
```

The absence of an executable generator is not itself a downstream
certificate failure.  It is the exact construction boundary that DoR-018's
race is commissioned to resolve.

---

## 6. Final determination

```text
D1_GENERATOR_EXECUTABLE = false
D1_FINITE_ACTIVE_JETS = zero | derived
D1_COMPLETED_STATIONARY_JETS = TYPE-U
D1_STATIONARY_POINT = TYPE-U

D2_LOG_DIVERGENCE_FIXES = units | covariance | homogeneity | finite flatness
D2_LOG_DIVERGENCE_DOES_NOT_FIX = action-germ direction | stationary two-jet | point
D2_FINITE_CONSTANT_REDUCTION = false

D3_AUTHORED_RESIDUE = N_MEMBER_STATIONARY_JET_INSTANTIATION
D4_RESPONSE_SUBSTITUTION = exact formal Schur expression on loop and S8-A
D4_FINITE_RESPONSE = zero | p-free | nu-free
D5_FINITE_FALSIFIERS = PASS
D5_NO_DERIVATION_COUNTERMODEL = PASS

JETS_DERIVED = not
VOID_CANDIDATE = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false

P_VERDICT_DECLARED = false
RESPONSE_VALUE_COMPUTED = false
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No alpha, `K_*`, root, numerical response value, rank ratio, or measured
constant was evaluated.  No p verdict was issued.  No register, plan,
tracker, git, commit, or push action was performed.
