# BOHM CONSTRUCTION — THE DEPTH DEDUP AND THE COCYCLE BRIDGE PROPOSAL
2026-08-03. Reviewer working lane (Fable). STATUS: PROPOSED — REQUIRES CODEX ADVERSARIAL
VERIFICATION BEFORE ANY REGISTRATION AS SETTLED (standing custody; the reviewer's
scorecard is ~14 proposed / 10 refuted and this artifact expects the same fire).

GATES: alpha_computed = false; proof_authorized = false; kappa_record_computed = false.

## PART I — THE DEDUP DETERMINATION (identifications examined, not assumed)

The four sealed notions (per ed49624b Section 4):
  D1 spectral threshold x        (the singlet theorem's cutoff variable)
  D2 sequential stage N          (the directed label system, Q_N = U(1)^N, colim)
  D3 T_cyl                       (character-algebra completion; PROVEN not a
                                  depth-action completion)
  D4 the probe functional Q_N(R) (quadratic bilocal; zero-extension INVARIANT)

CLAIM I.1 (dedup verdict): the four are PAIRWISE DISTINCT AS SEALED — no identification
proofs exist to be had. Support: D2 vs D4 distinct by ed49624b:321-339 (symbol overload
documented); D3 vs D2 related only functorially (D3 is the completion CODOMAIN of D2's
system, and TCYL_IS_DEPTH_ACTION_COMPLETION = false | TYPE-R); D1 is a spectral
coefficient variable with three TYPE-S non-identifications on record (ed49624b:277-279).
THE DEDUP DOES NOT COLLAPSE THE COUNT. The four are facets of a missing structure, not
names for an existing one.

## PART II — WHERE LAWFUL ACCUMULATION LIVES (the structural theorem-candidate)

CLAIM II.1: the 6.1 countermodel (ed49624b:385-402) proves more than it states: ANY
depth functional ON OBJECTS of the directed system is forced constant by the restriction
square (phi_M(j_NM y) = phi_N(y) => D_M = D_N). Object-level accumulation is DEAD as a
class. This is not an obstacle; it is a LOCATION theorem: accumulation, if it exists
lawfully, must live on the ARROWS.

CLAIM II.2 (the cocycle): define, on the inclusion system's arrows, the assignment
  c_dep(j_NM) := sum over the cells appended by j_NM of log w(cell)
where w(cell) is the SEALED per-cell dephasing factor (the closed forms of Q-279:
the equal-history weight 1 - p + p e^{-Q/2} and its probe-resolved refinements;
citations to c7624d88 required at verification). Then:
  (a) ADDITIVITY: c_dep(j_NL) = c_dep(j_NM) + c_dep(j_ML) — a genuine cocycle;
  (b) SEALED PROVENANCE: every increment is a sealed closed form; nothing authored;
  (c) IDENTITY CELLS: zero-extension appends identity labels; their dephasing factor
      is 1; their increment is log 1 = 0.

## PART III — THE BRIDGE (the named package's candidate realization)

CLAIM III.1 (cocycle holonomy): define, for a cycle c in the incidence structure,
  phi_div(c) := sum over the cells traversed by c of log w(cell)
— the cocycle evaluated around the cycle. Then:
  (a) IT IS A SCALAR FUNCTIONAL ON K_cycle (the bridge package's required type);
  (b) RESTRICTION-COMPATIBLE AUTOMATICALLY: under zero-extension, a fixed cycle's
      cells are unchanged and appended identity cells contribute 0 — phi_div passes
      the 6.1 square BY CLAIM II.2(c), evading the countermodel that kills every
      object-level construction;
  (c) GENUINELY ACCUMULATING: phi_div grows with the cycle's dephasing content —
      depth = loop content weighted by log-dephasing;
  (d) THE PRINCIPAL'S THREAD, EXECUTABLE: "the log divergence is the coupling"
      becomes "the coupling is the record system's arrow-cocycle read on loops" —
      the running-coupling-on-Wilson-loops structure, from sealed data;
  (e) p-CONTENT EXPLICIT: log(1 - p + p e^{-Q/2}) carries p — the coupling's
      p-dependence would be MANIFEST and symbolic, feeding Q-408's exact forms.

## PART IV — WHAT VERIFICATION MUST TEST (the kill-list I request)

  K1 the identity-cell increment really is 0 under the SEALED zero-extension (not an
     idealization) — check the closed forms at identity labels;
  K2 additivity holds for the SEALED w (product structure verified at source);
  K3 phi_div is well-defined on the PHYSICAL cycle carrier (quotient-compatibility:
     Gate-4 rephasing and the visibility quotient — does log w descend?);
  K4 reality covariance (log of a modulus-type weight — check the involution action);
  K5 the flat-family relation: is phi_div all-orders-flat on the active section
     (membership in the admitted class) or does it EXCEED the class (which would be
     a finding about the class, not necessarily a defect);
  K6 the bridge-package checklist of ed49624b:570-585 satisfied item by item;
  K7 the countermodel lineage (relabeling, target-tuning: phi_div's form cites ONLY
     dephasing data — verify no consequence-driven choice anywhere);
  K8 Q-408 consequence: Delta M and delta G_K with phi = phi_div — computable?

NOTHING HERE IS TYPE-P. If K1-K8 survive, the bridge exists from sealed data and the
divergence draft (409) re-runs with phi_div constructible. If any kill lands, the
refutation is the deliverable.
