# STAGE 8 — CL-ERROR CERTIFICATION, ADVERSARIALLY CHECKED: THE E_int DEGREE-0 CLAIM FAILS AT ITS OWN LEVEL

## BLIND ADVERSARIAL VERIFIER — CODENAME CL-ERROR-CHECK — CROSS-LINEAGE — [SEALED]

Date: 2026-08-13
Role: BLIND adversarial verifier (CL-ERROR-CHECK), cross-lineage, DEFAULT =
REFUTE. Under test: STAGE8_CL_ERROR_CERTIFICATION_V001.md, sha256
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a — recomputed
at path this session, MATCH. Its claim: NET = NO-OPPOSITE-COMPONENT — E's
entire degree-0 intraband P-block content is E_prof = (g-1)1_ball ⊗ I,
E_int carries none, the summed-FIXED horn dies at the level of record.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — the two scripts run are exact
symbolic CAS identity verification (sympy 1.14.0, fresh venv `ceck_venv`,
DIFFERENT from the artifact's), both reproduced in §7; NOTHING numeric
evaluated, all constants symbolic. No physical quantity computed, bounded
numerically, or evaluated. No scale, imported GR, or faithfulness authority.
The sealed sharp M(t) NOT softened. The equal-time route NOT resurrected (the
eq-object appears below only as the artifact's own bookkeeping intermediate
inside its exact Duhamel decomposition; no equal-time estimate made). No
register/tracker/plan/road/ledger/lens file read. Output name probed before
write: ABSENT. No git action.

---

## 0. VERDICT IN ONE LINE

**NET_VERDICT = REFUTED(the SPLIT claim "E_int has NO degree-0 intraband
component" is false — exhibited exactly at insertion order n = 2, where
E_int's intraband bounded-transfer symbol carries the degree-0 part
-Theta(x)^2/2 · 1_ball-type ⊗ I = exactly minus E_prof's n = 2 term. The
missed route: D(t)'s -M(t)⊗S piece composing with the eq-dressing's S-channel
at ZERO temporal separation — S·S = I, spinor-trivial, FULL O(1) intraband
part, no free phase anywhere, no time variable to integrate by parts, weight
sum non-killing. The certification's central absence claim fails at its own
symbol/degree/parity level; the resummed degree-0 intraband content of E is
NOT determined by the delivered identities — it is exactly the sealed r2
joint multi-scale norm question, which the artifact's (r-i) wrongly declares
not needed. Correct verdict: UNDECIDABLE-AT(the resummation of E_int's
phase-free channel; equivalently r2). The summed-FIXED horn's single named
escape is NOT closed; debt 1 returns to BL2STAR's honest state —
failing-conditionally-at-the-sealed-collapse. What survives: E_prof's exact
closed form (s1), the exact D-kernel (s2), J1-J12 (all rerun, all PASS), and
the marginality-inheritance half of the artifact's mechanism analysis.**

---

## 1. PROVENANCE — SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session; all match sidecars and tasked
prefixes. None unverifiable.

```text
3b9730e0ce585c524a6ac27d3368f29535a0bbffe84e1ec8313cc861011acb7a  STAGE8_CL_ERROR_CERTIFICATION_V001.md                                MATCH (artifact under test)
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                                       MATCH (tasked 6e81ae92)
2699af259750e2cae9afcb4d5a9487edd543c91a13db7c8f49a8fed1f3d9e24c  STAGE8_BL2STAR_CHECK_V001.md                                         MATCH (tasked 2699af25)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md                          MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md MATCH (PA; tasked 789338ad)
18af3dee8e3e5b17fe624d1b0547f4713359473a373df1562ff584647244ccf1  STAGE8_TWO_TIME_REANALYSIS_V001.md                                   MATCH (tasked 18af3dee)
6a2aa0fd18b9a0ea4f370dfbea278563ba02552e64b48ac0a7d91fbd7f90bc9e  STAGE8_TWO_TIME_CHECK_V001.md                                        MATCH (tasked 6a2aa0fd)
```

E1 byte span [57212,58506) re-extracted from bytes this session: sha256
`162e30a6ccb77ae53fb9026ef6d4a675fb74cb899b1e21c6fbc4926225ae5fcf` — MATCH;
content re-read verbatim (B-L2* clause: two-time, carrier-uniform,
volume-diagonal locus). Every line span the artifact cites was read at path
and matched its characterization: E1 :320-329 (C1), :339-352 (C4 + collapse
display), :353-364 (C6 + locus), :465-470 (D6' sharp M(t)), :650-733
(R.0/S1/S2/S2b/S3-deleted/S4), :1106-1126 (B-L2; only-repair :1123-1124),
:1131-1136 (C-L1 + witness), :1137-1144 (C-L2 + witness, quadratic-form
demand); PA :75-128 (A1/A2: M(t), r(t), v(t), S_n, b_D with open-diamond
slices |x| < r(t), J = -(Q b_D Q) ⊗ alpha_x), :367-384 (Strang); TWO-TIME
:200-354 (band split, marginality, survivor); BL2STAR attempt + check read in
full. The artifact's §7 script was extracted verbatim and rerun in a
DIFFERENT fresh venv: output matches its claimed output line for line, J1-J12
all PASS (§7.1). PROVENANCE = CLEAN.

---

## 2. ATTACK 1 — THE ERROR OBJECT. OBJECT_FIDELITY = CONFIRMED (one bookkeeping note).

Re-derived independently from the sealed displays:

```text
(o1) Frame: U(0) = sum_l w_l u_lambda(0), weights (1/2,-1/4,-1/4) on
     (0,+sqrt2,-sqrt2) (E1 :340-342), generator h_0 + lambda v(t) M(t)⊗S with
     the D6' sharp M(t) (E1 :465-470), S = -i gamma^0 gamma^5 (PA), sealed
     collapse U(0) = P + E (C4 :346-351; C-L1 :1131-1136, E uncertified).
     MATCHES the artifact's §2.1. Interaction picture u_lambda(0) =
     e^{-i h_0} Omega_lambda: standard, exact.
(o2) E_prof: re-derived by hand and by CAS. Nested-ball indicators commute,
     so Omega^eq_lambda = (I-1_ball) + e^{-i lambda Theta(x) S} 1_ball with
     Theta(rho) = int_rho^{1-rho} v = tau_R(1-16 rho^4) EXACT (their J8;
     re-verified). Weight sum: sin channel 0, cos channel c(Theta) (their J7);
     sqrt2·tau_R = pi gives g = cos^2(8 pi rho^4) (their J9). Off-ball part
     m_0 = 0. E_prof = (g-1)1_ball ⊗ I EXACT. CONFIRMED — this subsidiary
     (s1) is correct and valuable.
(o3) E_int: the Duhamel resolution with D(t) = R(t) - M(t)⊗S is exact; the
     commutator flip [h_0, M⊗S] = {p_j, M} ⊗ alpha_j S re-derived by hand
     ({S,alpha_j} = 0); the band resolution (s'|k'|-s|k|) P_s' P_{-s} S
     re-derived by hand; the (e^{i omega t}-1) cancellation of the degree-(+1)
     prefactor re-derived (their J6). The D-kernel (s2) is CONFIRMED exact
     and is in the quadratic-form-compatible shape C-L2 demands.
(o4) BOOKKEEPING NOTE (minor, not verdict-bearing): the literal E := U(0) - P
     also contains the free-factor piece (e^{-i h_0} - 1)-type content, whose
     degree-0 intraband part is a modulus-1 band phase times P-block content.
     The artifact says "carried"; in the response pairing it is a common
     unitary factor of the whole branch and cannot create or destroy a
     relative cancellation. Harmless — but the flag block's "E's ENTIRE
     degree-0 intraband P-block content is E_prof" is literally a statement
     about the phase-stripped error. Noted; subsumed by the §3 finding.
```

The eq-intermediate is the sealed C-L1 display's own idealization, used as
bookkeeping only: lawful; no equal-time estimate was made by the artifact or
by this check. OBJECT_FIDELITY = CONFIRMED.

---

## 3. ATTACK 2 — THE BAND/DEGREE SPLIT, RERUN WITH MY OWN CONSTRUCTION. SPLIT_RERUN = MISMATCH.

The verdict is NO-OPPOSITE, so the tasked hunt is for the intraband piece the
certification missed. Found one, exact.

### 3.1 The missed channel: zero-length plus-band segments

The artifact's §3.2 bookkeeping: (i) every intraband channel at an S-type
vertex costs one exact coincidence power (J5, root P_s S P_s = 0) — TRUE per
single vertex; (ii) every flipped sea-to-sea route contains >= 1 plus-band
segment whose "time integration ... licens[es] ONE integration by parts per
segment: >= one power |k|^{-1}" — FALSE in general. The gap:

```text
A plus-band segment of ZERO temporal extent carries NO oscillating time
variable, so there is nothing to integrate by parts. Such segments exist in
E_int's own resummed Duhamel form: D(t)'s -M(t)⊗S piece composing DIRECTLY
with the eq-dressing Omega^eq_lambda(t,0) = cos(lambda Theta_t) -
i sin(lambda Theta_t) S (a multiplication operator: its S-channel flips bands
with NO free phase — the eq-object has none). Two adjacent S-vertices
compose to S·S = I:
    [-M(t)⊗S]·[-i sin(lambda Theta_t) S] = +i sin(lambda Theta_t) M(t) ⊗ I
— spinor-trivial, k-independent, FULL O(1) intraband, NO coincidence factor,
NO phase. CAS: my K2 — P_-(u)[S e^{-iHs} S]P_-(u) = e^{-iws} P_-(u), FULL
modulus-1 band-diagonal (at s = 0: exactly 1), against P_- S P_- = 0 for the
single vertex (their J10, reconfirmed). The per-vertex coincidence costing
does NOT survive composition of S-pairs at zero segment length. The weight
sum does not kill the channel: sum_l w_l lambda sin(lambda theta) =
-(sqrt2/2) sin(sqrt2 theta), not identically zero (my K4). And the
(e^{i omega t} - 1) kernel does not vanish at large frequency: its
oscillatory part is O(1/omega) exactly, but its "-1" part is
omega-INDEPENDENT (my K3: closed forms; ordered double integral of the
oscillatory part = 1/(2i om) - (1-e^{-2i om})/(2i om)^2, of the -1 part
= -1/2). The artifact itself notes the O(1)-ness of (e^{i omega t}-1) for the
INTRABAND channel (§3.2(i)) and misses that on the INTERBAND channel the
same "-1" survives all time integration when the segment is phase-free.
```

### 3.2 The exact n = 2 ledger: E_int's degree-0 intraband part is -E_prof's

Fixed insertion order n = 2 (the lowest surviving stratum, m_2 = -1),
bounded transfer, |k| -> infinity. Everything below is exact.

```text
(n2-1) E^(2) = weighted Omega^(2) = -(weighted lambda^2) int_{u<t} v(t)v(u)
       R(t)R(u) dt du. S-absorption (exact; {S,h_0} = 0, S^2 = I; my K1):
           R(t)R(u) = e^{i h_0 t} M(t) e^{i h_0 (t-u)} M(u) e^{-i h_0 u}
       — the S's are GONE; the vertices are the spatial M's (spinor-trivial:
       band-diagonal O(1) at coincidence, interband coincidence-suppressed).
(n2-2) Sea-sea element, channel by channel:
       SEA intermediate: phase -|k|t - |k''|(t-u) + |k'|u = -2|k|(t-u) +
       O(|q|·): oscillatory a.e. on the ordered simplex; amplitudes
       (v BV, chi_hat_{r(t)} Lipschitz-in-t) license one IBP: O(1/|k|).
       At n = 2 the intermediate-momentum composition is absolutely
       convergent (two chi_hat kernels: int d^3q |q|^{-2}|q'-q|^{-2} < inf,
       2+2 > 3), so this is a completed estimate at this order, not an
       order-heuristic.
       PLUS intermediate: phase -|k|t + |k''|(t-u) + |k'|u = 0 + O(|q|·) —
       STATIONARY (no IBP available), but each M-vertex's interband element
       carries the spinor overlap u_-(k)^dag u_+(k'') vanishing linearly at
       coincidence (my K6 = their J5-adjacent): TWO factors: O(|k|^{-2}).
       Cross-check: the same total in the un-absorbed N-vertex decomposition
       (full O(1) S-flips, oscillatory plus-segment phase |k|(1-2(t-u))) —
       the two decompositions agree; no contradiction.
       => E^(2)'s intraband bounded-transfer symbol is O(1/|k|):
          NO degree-0 part at n = 2.
(n2-3) E_prof^(2) = (weighted (-i lambda)^2) int_{u<t} v v M(t)M(u) =
       +Theta(x)^2/2 · (ball) ⊗ I (my K5: sum_l w_l(-il)^2 = +1 exactly;
       N(t)N(u) = M(t)M(u) ⊗ S^2 = M M ⊗ I; indicators commute, the ordered
       double integral is half the square; Theta^2/2 is exactly the j = 1
       term of c(Theta) - ... : c(th) = th^2/2 + O(th^4), my K5). Degree-0
       intraband, NONZERO.
(n2-4) E^(2) = E_prof^(2) + E_int^(2) is an identity. (n2-2) + (n2-3) force:
           E_int^(2)'s intraband degree-0 part = -Theta(x)^2/2 · ⊗ I  ≠ 0
       — exactly MINUS E_prof's n = 2 term, spinor-trivial, opposite sign.
       The artifact's "E_int: NO degree-0 intraband component" is FALSE at
       n = 2, at exactly the symbol/degree/parity level it certifies at.
```

This is not a new tension of my invention: the sealed lineage already
recorded it. BL2STAR-CHECK §3.3 (read at path): the lambda^2 stratum of the
idealized collapsed display "carries coefficient +(theta_1-theta_0)^2/2 —
degree 0, NO coincidence factor", while the true stratum is
coincidence/oscillation-suppressed, and "that discrepancy is exactly the
C-L2 commutator error". The discrepancy IS E_int-type content, degree 0,
nonzero — the certification under test contradicts its own lineage's
consistency probe.

### 3.3 What this does and does not establish

```text
ESTABLISHED (exact): E_int carries degree-0 intraband P-block content at
  every fixed order (exhibited at n = 2; the mechanism — the fully
  phase-free slice of Omega_lambda - Omega^eq_lambda — is order-general).
  Its fixed-order value is exactly -E_prof's, i.e., fixed-order/stratum
  analysis gives E's total intraband symbol -> 0 for the n >= 2 strata,
  while the n = 0 stratum of the phase-stripped error is -P itself
  (weighted Omega starts at m_0 I = 0): at the STRATUM level E's degree-0
  intraband P-block content is -1_ball — exactly opposite the collapse
  endpoint weight, the horn's named escape shape.
NOT ESTABLISHED (either way): the RESUMMED degree-0 intraband content of E.
  Fixed-order analysis cannot be trusted for the resummed object — that is
  the sealed C4 saturation/resummation-reversal lesson (BL2STAR §3(a3),
  §4.4, check CONFIRMED), and it cuts BOTH ways: it defeats the artifact's
  per-route §3.2 bookkeeping (which is fixed-order-style reasoning on the
  resummed Duhamel form) AND it forbids me from certifying the evaporation
  answer (-1_ball) for the resummed E. The two consistent completions —
  (g-1)1_ball (the artifact's eq-resummation: horn dies) and -1_ball + decay
  (adiabatic/averaging structure: the survivor's endpoint weight evaporates
  at large |k|; the horn revives at sufficient decay rate) — differ by
  exactly the resummation of the phase-free channel, whose control is the
  sealed r2 joint multi-scale norm (chi_hat L^2-not-L^1: n-fold transfer
  compositions not factor-wise boundable for n >= 3). NOT delivered by the
  artifact, and — contra its (r-i) — NEEDED: the identical-cancellation
  question turns on precisely this resummed value.
DIRECTION NUANCE, for the record: the live escape does not require the
  anti-Hermitian k_hat_x-odd C(k)-projected "opposite component" that the
  artifact's J12 argument excludes (J12 is correct and stands). The escape
  runs through the SPINOR-TRIVIAL endpoint-weight channel the artifact
  itself exhibits as multiplicative: if the resummed collapse profile
  u_0 = 1_ball + (E's intraband symbol) decays in |k|, the survivor's
  HS-divergence dies without any component cancellation. The artifact's own
  mechanism, with the sign of its conclusion undetermined.
```

SPLIT_RERUN = MISMATCH(the E_int degree-0 claim; the missed term:
the phase-free/zero-length-segment channel, n = 2 value -Theta^2/2 ⊗ I).

---

## 4. ATTACK 3 — MARGINALITY REASONING AT THE BYTES. MARGINALITY = CORRECTED.

```text
(m1) INHERITANCE: CONFIRMED. The D-vertex's intraband frequency s(|k'|-|k|),
     |omega| <= |q| bounded, re-derived; neither Gevrey nor cell-time
     integration supplies an intraband kill of E's terms; the
     "interband-rotated => Gevrey kills" branch is indeed FALSE for E.
     This half of the artifact's analysis is sound at the bytes
     (TWO-TIME :282-290 read at path; matches).
(m2) "ALGEBRA-KILLED": REFUTED as stated. P_s S P_s = 0 (their J10, correct)
     kills the band-diagonal part of a SINGLE S-vertex; it does NOT make
     degree 0 unreachable for E_int, because adjacent S-pairs compose to
     S^2 = I with FULL O(1) band-diagonal part and no coincidence factor
     (my K2) whenever no free phase intervenes — and E_int's own structure
     supplies phase-free adjacency (D's -N piece against the eq-dressing's
     channels; §3.1). Degree 0 is reached (§3.2). The survivor's O(1)
     intraband mass is indeed the alpha_x property (their J10 contrast,
     correct) — but E_int does not need alpha_x's property to reach degree
     0 in the spinor-trivial component.
```

---

## 5. ATTACK 4 — SCOPE AND CONSEQUENCE. SCOPE = CORRECTED.

```text
(s-1) The verdict at claimed strength: "NO-OPPOSITE-COMPONENT, certified at
      the symbol/degree/parity level of record" — REFUTED. At that level the
      SPLIT is wrong (§3.2), and the resummed content is not decided by
      anything delivered. The honest verdict on the tasked question is
      UNDECIDABLE-AT(the resummation of E_int's phase-free channel; the r2
      joint multi-scale norm names the missing object). I do NOT certify
      OPPOSITE-COMPONENT-CERTIFIED: the fixed-order opposite content
      (-E_prof per stratum; -1_ball at the stratum-sum level) is subject to
      the same resummation-reversal caveat the artifact's own claim dies of.
(s-2) (r-i) "completed joint multi-scale norm ... not needed for identical
      cancellation": REFUTED — it is exactly what is needed (§3.3).
      (r-ii) Q passed through: consistent with the sealed display's own
      idealization; unexamined Q-effects sit inside the same undecided
      residue. (r-iii) successor-spec possibility: stands, unchanged.
(s-3) Debts: debt 1 (B-L2*) RETURNS to failing-conditionally-at-the-sealed-
      collapse (BL2STAR NET, check-CONFIRMED) — the claimed promotion to
      "error certified NON-RESCUING" is withdrawn; the summed-FIXED horn's
      single named escape is NOT closed; it stays OPEN and UNDECIDED at the
      level of record. Debts 2 (F'-14) and 3 (unsealed summed-S2' spec act)
      REMAIN REGARDLESS — unchanged, correctly carried by the artifact.
      Per-pair nothing moves; RUNNING stays an unforced lean; M(t) sharp;
      equal-time stays FALSE by C6. Subsidiaries s1 (exact collapse profile
      g = cos^2(8 pi |x|^4), quadratic boundary vanishing, interior locus)
      and s2 (exact D-kernel) SURVIVE this check and are of record-grade
      value; s3 (the b~_g-corrected survivor) is CONDITIONAL on the
      undecided resummed content and may not be cited as error-included.
(s-4) Fences: the artifact's fence compliance CONFIRMED — no numerics, no
      softening of M(t), no equal-time estimate, no forbidden authority,
      constants symbolic. The refutation is of its mathematics, not its
      conduct.
```

---

## 6. INJECTION SCAN

The artifact contains no instruction-bearing content beyond its own protocol;
no embedded directives, no seal-by-substring shortcuts (all its digests were
independently recomputed here from bytes); its script was rerun, not trusted.
No register/tracker/plan/road/ledger/lens file was read by me. INJECTION =
none.

---

## 7. THE SCRIPTS (exact symbolic verification; reproduced in full) AND OUTPUT

### 7.1 Rerun of the artifact's §7 script (extracted verbatim, different venv)

Output matches the artifact's claimed output line for line:

```text
J1  S^2=I, S=S^dag, {S,alpha_j}=0 all j    : PASS
J2  S P_s(u) = P_{-s}(u) S (both s)       : PASS
J3  (a.k')S - S(a.k) = (a.(k+k'))S         : PASS
J4  P_s'(v)(a.(k+k'))S P_s(u)=(s'|k'|-s|k|)P_s'(v)P_-s(u)S (4 sign pairs): PASS
J5  P_s(v)P_{-s}(u) = -(s/2)P_s(v)(a.(u-v)) (both s): PASS
J6  i om int_0^t e^{i om s}ds = e^{i om t}-1 (om!=0; om=0 branch trivially 0): PASS
J7  sum_l w_l e^{-il th S} = c(th) I  (spinor-trivial; sin channel = 0): PASS
J8  int_rho^{1-rho} 32 min(t,1-t)^3 dt = 1-16 rho^4: PASS
J9  1-g=sin^2(8 pi rho^4) [=> g=cos^2 by cos^2=1-sin^2]; g(0)=1; g(1/2)=0; g'(1/2)=0: PASS
J10 (I-Au)ax(I-Au)=-2ux(I-Au)  and  P_s(u)SP_s(u)=0 both bands: PASS
J11 m_0..m_4 = 0,0,-1,0,-2                 : PASS
J12 (r I) Hermitian; survivor spinor factor anti-Hermitian (disjoint components): PASS
```

The identities are all sound. The refutation is of the channel bookkeeping
BETWEEN the identities, not of the identities.

### 7.2 My counter-battery (sympy 1.14.0, fresh venv)

```python
# CL-ERROR-CHECK: counter-battery. EXACT SYMBOLIC ONLY - identity verification
# over symbols; nothing numeric evaluated.
import sympy as sp

I2, Z2 = sp.eye(2), sp.zeros(2, 2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])
g0 = sp.diag(1, 1, -1, -1)
gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit()) for s in (sx, sy, sz)]
g5 = sp.I * g0 * gs[0] * gs[1] * gs[2]
S = -sp.I * g0 * g5
al = [g0 * g for g in gs]
I4 = sp.eye(4)

ux, uy, uz = sp.symbols('ux uy uz', real=True)
w_, s_, th = sp.symbols('w_ s_ theta', real=True)

UGENS = [ux**2 + uy**2 + uz**2 - 1]
def red(M):
    # entrywise polynomial reduction modulo <u.u - 1> (EX domain carries the
    # trig/phase coefficients through exactly)
    M = sp.expand(M)
    out = sp.zeros(*M.shape)
    for i in range(M.shape[0]):
        for j in range(M.shape[1]):
            e = sp.expand(M[i, j])
            r = sp.reduced(sp.Poly(e, ux, uy, uz, domain='EX').as_expr(),
                           UGENS, ux, uy, uz)[1]
            out[i, j] = sp.simplify(r)
    return out

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

Au = ux*al[0] + uy*al[1] + uz*al[2]        # alpha.u, u unit
Pm = (I4 - Au)/2                            # sea projector P_-(u)
# free evolution symbol at fixed unit direction u: H = w (alpha.u), exact exponential:
Efree = sp.cos(w_*s_)*I4 - sp.I*sp.sin(w_*s_)*Au   # e^{-i H s}, H = w alpha.u

# K1: S-conjugation flips the free phase: S e^{-iHs} S = e^{+iHs}  ({S,alpha_j}=0, S^2=I):
Eplus = sp.cos(w_*s_)*I4 + sp.I*sp.sin(w_*s_)*Au
ok("K1  S e^{-iHs} S = e^{+iHs}                                  ",
   red(S*Efree*S - Eplus) == sp.zeros(4, 4))

# K2: THE ZERO-LENGTH-SEGMENT COMPOSITE: two S-vertices with free evolution between,
#     spatial parts scalar: (1 x S) e^{-iHs} (1 x S) = e^{+iHs}; its SEA-BAND-DIAGONAL
#     part is FULL, modulus-1: P_-(u) [S e^{-iHs} S] P_-(u) = e^{-i w s} P_-(u)
#     (H-eigenvalue -w on the sea band).
#     CONTRAST the single vertex: P_- S P_- = 0 (their J10). The composite of two S-type
#     vertices has O(1) intraband mass with NO coincidence factor - the per-vertex
#     coincidence costing does NOT survive composition at zero segment length (S^2 = I).
ok("K2  P_-(u) S e^{-iHs} S P_-(u) = e^{-iws} P_-(u)  (FULL, O(1))",
   red(Pm*(S*Efree*S)*Pm - (sp.cos(w_*s_) - sp.I*sp.sin(w_*s_))*Pm) == sp.zeros(4, 4)
   and red(Pm*S*Pm) == sp.zeros(4, 4))

# K3: THE (e^{i om t} - 1) KERNEL DOES NOT VANISH AT LARGE FREQUENCY: the oscillatory
#     part of the ordered double time integral is O(1/om) but the "-1" part is
#     om-INDEPENDENT: lim_{om->infinity} int_0^1 int_0^t (e^{-2 i om (t-u)} - 1) du dt = -1/2.
om = sp.symbols('omega', real=True, positive=True)
t_, uu = sp.symbols('t_ u_', real=True)
Iosc = sp.integrate(sp.integrate(sp.exp(-2*sp.I*om*(t_-uu)), (uu, 0, t_)), (t_, 0, 1))
Iconst = sp.integrate(sp.integrate(sp.Integer(-1), (uu, 0, t_)), (t_, 0, 1))
# exact closed form: Iosc = 1/(2 i om) - (1 - e^{-2 i om})/(2 i om)^2  -- manifestly O(1/om)
closed = 1/(2*sp.I*om) - (1 - sp.exp(-2*sp.I*om))/(2*sp.I*om)**2
ok("K3  Iosc = 1/(2i om) - (1-e^{-2i om})/(2i om)^2 [= O(1/om)] ; int int (-1) = -1/2",
   sp.simplify(Iosc - closed) == 0 and Iconst == sp.Rational(-1, 2))

# K4: THE WEIGHT SUM DOES NOT KILL THE MISSED CHANNEL: the D(-N)-piece x eq-S-channel
#     route carries sum_l w_l lambda sin(lambda theta) = -(sqrt2/2) sin(sqrt2 theta),
#     NOT identically zero (leading term -theta: the m_2 stratum).
lam_v = [sp.sqrt(2), sp.Integer(0), -sp.sqrt(2)]
w_v = [sp.Rational(-1, 4), sp.Rational(1, 2), sp.Rational(-1, 4)]
chan = sp.simplify(sum(wi*li*sp.sin(li*th) for wi, li in zip(w_v, lam_v)))
ok("K4  sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th) != 0        ",
   sp.simplify(chan + sp.sqrt(2)*sp.sin(sp.sqrt(2)*th)/2) == 0
   and sp.simplify(chan.subs(th, sp.pi/4)) != 0)

# K5: THE n=2 LEDGER. (i) weighted second-order eq-content: sum_l w_l (-i l)^2 = +1,
#     so E_prof^(2) = +Theta^2/2 (times 1_ball x I): the j=1 term of c(Theta)-1.
#     (ii) The two-S composite in NN is spinor-trivial: N(t)N(u) -> M(t)M(u) x S^2 = M M x I.
#     (iii) Therefore if weighted[RR]'s intraband bounded-transfer symbol is O(1/|k|)
#     (K2/K3 structure: sea channel oscillatory -> IBP; plus channel doubly
#     coincidence-suppressed), then E_int^(2) intraband degree-0 part = -Theta^2/2 exactly.
coef2 = sp.simplify(sum(wi*(-sp.I*li)**2 for wi, li in zip(w_v, lam_v)))
c_th = (1 - sp.cos(sp.sqrt(2)*th))/2
j1 = sp.simplify(sp.series(c_th, th, 0, 4).removeO() - th**2/2)   # c(th) = th^2/2 + O(th^4)
ok("K5  sum_l w_l(-il)^2 = +1 ; c(th) jet = th^2/2 ; S.S = I        ",
   coef2 == 1 and j1 == 0 and red(S*S - I4) == sp.zeros(4, 4))

# K6: SPINOR OVERLAP OF THE STATIONARY (plus-intermediate) CHANNEL IS COINCIDENCE-
#     SUPPRESSED: P_-(u) P_+(v) vanishes at u = v and is linear in (u - v)
#     (their J5, reconfirmed): the stationary-phase route pays TWO of these.
vx, vy, vz = sp.symbols('vx vy vz', real=True)
Av = vx*al[0] + vy*al[1] + vz*al[2]
Pp_v = (I4 + Av)/2
prod = sp.expand(Pm*Pp_v)
ok("K6  P_-(u) P_+(u) = 0 (coincidence zero of the stationary route) ",
   red(prod.subs([(vx, ux), (vy, uy), (vz, uz)])) == sp.zeros(4, 4))
```

Output, verbatim:

```text
K1  S e^{-iHs} S = e^{+iHs}                                  : PASS
K2  P_-(u) S e^{-iHs} S P_-(u) = e^{-iws} P_-(u)  (FULL, O(1)): PASS
K3  Iosc = 1/(2i om) - (1-e^{-2i om})/(2i om)^2 [= O(1/om)] ; int int (-1) = -1/2: PASS
K4  sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th) != 0        : PASS
K5  sum_l w_l(-il)^2 = +1 ; c(th) jet = th^2/2 ; S.S = I        : PASS
K6  P_-(u) P_+(u) = 0 (coincidence zero of the stationary route) : PASS
```

(The S-absorption R(t)R(u) = e^{i h_0 t} M(t) e^{i h_0 (t-u)} M(u)
e^{-i h_0 u} used in §3.2 follows from K1 applied at the operator level; the
sign was additionally cross-checked against the Schrodinger-picture phase
count, and the two intermediate-basis decompositions of the same operator
agree channel by channel.)

---

## 8. FLAG BLOCK

```text
OBJECT_FIDELITY = CONFIRMED(
  E := U(0) - P at the sealed collapse, re-derived independently from
  E1 C4 :339-352 / C-L1 :1131-1136 / C-L2 :1137-1144 + S2b :702-708 and the
  PA realization; the two-piece resolution E = E_prof + E_int is exact;
  E_prof = (g-1)1_ball ⊗ I with g = cos^2(8 pi |x|^4) re-derived by hand and
  CAS (subsidiary s1 stands); the D-kernel (e^{i(s'|k'|-s|k|)t}-1)
  chi_hat_{r(t)}(q) P_s'(k')P_{-s}(k) S re-derived (subsidiary s2 stands);
  eq-intermediate lawful as the sealed display's own bookkeeping. One minor
  note: the flag-block "ENTIRE content" claim is literally about the
  phase-stripped error — the free-factor piece (e^{-i h_0}-1)P is a common
  unitary in the pairing, harmless, but unacknowledged in the accounting.)

SPLIT_RERUN = MISMATCH(
  the term: the PHASE-FREE / ZERO-LENGTH-PLUS-SEGMENT channel of E_int —
  D(t)'s -M(t)⊗S piece composing directly with the eq-dressing
  Omega^eq(t,0)'s S-channel: S·S = I, spinor-trivial, FULL O(1) intraband
  part with NO coincidence factor (K2; contrast P_s S P_s = 0 per single
  vertex), NO free phase and hence NO integration by parts available (K3:
  the "-1" part of (e^{i omega t}-1) is omega-independent), weight sum
  non-killing (K4: sum_l w_l l sin(l th) = -(sqrt2/2) sin(sqrt2 th) != 0).
  §3.2(ii)'s one-IBP-power-per-plus-segment claim fails for segments of zero
  temporal extent. EXACT INSTANCE at insertion order n = 2: E_int's
  intraband bounded-transfer symbol carries the degree-0 part
  -Theta(x)^2/2 ⊗ I = exactly minus E_prof's n = 2 term (K5 + two-way
  channel computation, §3.2: E^(2)'s intraband symbol is O(1/|k|), with the
  n = 2 transfer composition absolutely convergent, so the split is forced).
  "E_int has NO degree-0 intraband component" is FALSE at the certified
  level. Consistent with the lineage's own BL2STAR-CHECK §3.3 probe
  (+(theta_1-theta_0)^2/2, degree 0, no coincidence factor = C-L2 error
  content), which the certification contradicts.)

MARGINALITY = CORRECTED(
  inheritance half CONFIRMED at the bytes (|omega| <= |q| bounded; no Gevrey
  or cell-time intraband kill; "rotated => killed" rightly FALSE for E).
  "Algebra-killed" half REFUTED: P_s S P_s = 0 is per-vertex only; adjacent
  S-pairs compose to S^2 = I with full O(1) band-diagonal part whenever no
  free phase intervenes, and E_int's own structure supplies such phase-free
  adjacency; degree 0 IS reachable by E_int in the spinor-trivial component.)

NET_VERDICT = REFUTED(
  NO-OPPOSITE-COMPONENT at the certified strength does not stand: the SPLIT
  supporting it is false at its own symbol/degree/parity level, and the
  resummed degree-0 intraband P-block content of E is NOT determined by the
  delivered identities. Fixed-order/stratum analysis gives E's intraband
  content -> -1_ball-shape (the n = 0 stratum is -P; every n >= 2 stratum's
  content cancels E_prof's order by order) — the horn's named escape shape;
  the eq-resummation gives (g-1)1_ball — the horn-killing shape; the
  difference is exactly the resummation of the phase-free channel = the
  sealed r2 joint multi-scale norm, NOT delivered and (contra the
  artifact's r-i) NEEDED. Honest verdict on the tasked question:
  UNDECIDABLE-AT(the resummation of E_int's phase-free channel / r2).
  NOT promoted to OPPOSITE-COMPONENT-CERTIFIED: the fixed-order opposite
  content is subject to the same C4 resummation-reversal caveat that
  defeats the artifact's claim — the direction is open BOTH ways. J12
  component-disjointness stands but does not close the escape: the live
  channel is the spinor-trivial multiplicative endpoint weight, the
  artifact's own mechanism with its sign undetermined.)

SCOPE = CORRECTED(
  (r-i) "multi-scale norm not needed" REFUTED — it is the deciding object;
  (r-ii)/(r-iii) stand. Debt 1 RETURNS to
  failing-conditionally-at-the-sealed-collapse (BL2STAR NET, 2699af25
  CONFIRMED); the summed-FIXED horn's single named escape is NOT closed —
  OPEN and UNDECIDED at the level of record. Debts 2 (F'-14) and 3
  (unsealed summed-S2' spec act) REMAIN REGARDLESS, correctly carried.
  Per-pair nothing moves; RUNNING stays an unforced lean; M(t) sharp;
  equal-time stays FALSE by C6. Subsidiaries s1/s2 SURVIVE; s3 (b~_g
  error-included survivor) is CONDITIONAL on the undecided resummation and
  may not be cited as error-included. Artifact fence-compliance CONFIRMED.)

PROVENANCE = CLEAN(
  artifact 3b9730e0 + 6 seals (6e81ae92, 2699af25, 46846730, 789338ad,
  18af3dee, 6a2aa0fd) recomputed from bytes at path this session, all
  matching sidecars and tasked prefixes; E1 byte span [57212,58506)
  recomputed 162e30a6 MATCH; every cited line span read at path and
  matched; the artifact's script extracted verbatim and rerun in a
  different fresh venv, output identical line for line; none unverifiable.)

INJECTION = none(
  no instruction-bearing content in the artifact beyond protocol; all
  digests independently recomputed, no seal accepted by substring; no
  register/tracker/plan/road/ledger/lens file read; output name probed
  before write: ABSENT.)

MACHINERY_USED_BY_ME = yes(
  CAS identity verification only — sympy 1.14.0, fresh venv ceck_venv:
  verbatim rerun of the artifact's J1-J12 (all PASS) plus my K1-K6
  counter-battery, scripts and outputs reproduced in §7; shasum/file
  reads/byte extraction otherwise; NOTHING numeric evaluated, all constants
  symbolic.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
CL_ERROR_CHECK_RESULT = SEALED.
```
