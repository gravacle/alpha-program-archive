# STAGE 8 / ACT 2 / CARRIER-ADMISSION PACKAGE — INDEPENDENT ADVERSARIAL AUDIT

Lane: ACT2-AUDIT, independent adversarial auditor, cross-lineage. DEFAULT = REFUTE;
where verdicts differ, this audit's governs. Date: 2026-08-14.
Under test: `STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md`, sha256 recomputed at path
THIS session BEFORE opening = `38239f93de8b3a720659beba62f52dc2899e507f0fca72b57d88053dd59a09d8`
— MATCHES the tasked digest and the file's own `.seal.sha256` sidecar.
ALL_RESULTS of this audit = CLAIMED until the registrar disposes.

## VERDICT — ONE LINE

**AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS.** The forcing determination
(NOT-FORCED, wider-class display) re-derives exactly from the sealed requires and
matched this auditor's answer PRE-FIXED before the build was opened; the CAS re-runs
byte-identical in a fresh venv; the sheet is steering-free and honest; construction
stayed inside the determination's license. Four corrections and two nits, NONE
converting: one class-display member (A2b) fails the frozen face-sign shape under
the sealed-slot-binding reading (a conforming variant A2b′ is exhibited here); the
§2 sweep-exhaustiveness claim fails at its own displayed pattern; the clause-(i)
compliance sentence is overstated at bytes by one borderline contact in §3.5; the
fence-exclusion display omits its case handling.

```text
GATES, LIVE THROUGHOUT THIS AUDIT: alpha_computed = false ;
proof_authorized = false ; kappa_record_computed = false.
FENCES HELD BY ME: exact symbolic only (fresh-venv sympy + exact integer
enumeration; no floats as ground, no measured constants, no value frozen; mu, n,
r_0, r_ch never assigned); no scale, no imported GR, no faithfulness authority;
no file matching register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened;
"Q-..." tokens EXPECTED-UNLOCATABLE, noted not chased; no git; no
register/commit/push; no existing file edited; ONE output + seal sidecar, name
probed ABSENT before any write this session.
```

---

## 1. INDEPENDENCE DISCLOSURE (with hashes)

```text
PROTOCOL RUN: (1) the audit output path probed ABSENT (artifact and sidecar);
(2) ALL seed-stock seals verified at path (table §3); (3) the seed stock read IN
FULL (min-carrier pair, addendum, mandate, cross-block pair, rank-pinning pair,
anchoring pair, dilation gate), PLUS the sweep-surfaced act-relevant stock
(typing-necessity + R-CLW pairs, filled-two-cell census, 5c staging sheet) —
BEFORE the build was opened; (4) my forcing answer worked from that stock alone
and FIXED to scratchpad, hashed:
  93e47e8234e20315809d55e82c41f9841bce0b137d024e0e88ad65006976e07f
  scratchpad/ACT2_AUDIT_PREFIXED_FORCING_ANSWER.md   (pre-fixed, pre-build)
(5) only then the build read ONCE in full and attacked at bytes.
UNAVOIDABLE EXPOSURE, DISCLOSED: the commissioning text carried a one-line
orientation summary containing the build's verdict category (NOT-FORCED,
wider-class). The pre-fixed note therefore proves independence of the GROUNDS
and of the class display, not of the bare category label; every ground below
(G1-G7) was derived from the sealed seed before the build's §3 was seen, and my
adversarial battery (§5) goes beyond the build's own checks in three places.
```

## 2. THE PRE-FIXED FORCING ANSWER (verbatim substance of the hashed note)

```text
MY ANSWER, FIXED BEFORE OPENING THE BUILD: NOT-FORCED.  The admissible class at
the sealed requires is INFINITE; no sealed instrument selects a member; the
minimal member's identity itself forks on an unsupplied reading; the class is
NON-EMPTY, so the act is meaningful and required for record status.
GROUNDS (mine):
G1 no sealed minimality require — the lexicographic (F,E,V) order is
   LANE-DECLARED (f1881511 §2.1), the only instrument distinguishing K_dd;
   targeted hunt over V011 / 92dba8d3 / V007 / the addendum found NO sealed
   minimality clause (re-run this session, negative).
G2 the conforming class is infinite: the sealed family rows above the floor plus
   unbounded upward (verified by explicit family, §5 B3).
G3 the one entered premise (b_1 = 1 operative domain, V007 :162-172, addendum
   :33-34) EXCLUDES every both-blocks carrier (b_1 >= 2 arithmetic) from its own
   domain; it selects nothing in the class.
G4 the minimum forks on the R-g reading supply (K_dd vs A5, f1881511 §2.4) —
   FORCED-to-K_dd fails on this alone.
G5 sealed text affirms non-derivation: NONE_SEALED survives (f1881511 H-1,
   bb2e6570); carrier = D1 debt with lane/act holders (f3165db1);
   complete_physical_carrier_derived = false (b807aa8e); the missing content IS
   "the cell-DECLARATION/ADMISSION itself" (cdeb019d :341-352, checked SOUND).
G6 closure-route exhaustion (minimality absent; Attach domain wrong-b_1;
   anchoring law value-forked with inert entered ports, L-1..L-4 ACT-REQUIRED;
   arithmetic a floor not a pin; floor-uniqueness quantifier not an
   admissible-class quantifier; no emptiness bar).
G7 false-NOT-FORCED direction: the act is NOT needless — the class is non-empty
   (K_dd conforms, MC SOUND) and no derivation preempts the admission (the
   O11/O12/O-D1 constructor is unbuilt; the forcing slot stands NO_VERDICT).
MATCH AGAINST THE BUILD: verdict category, class display, and grounds all agree
(build §3.1-§3.5 = G1-G7 point for point; its three-axis residual-freedom
statement F-I/F-II/F-III is a faithful partition of my G2/G4/G6(iii)).
FALSE-FORCED risk: n/a (verdict is NOT-FORCED).  FALSE-NOT-FORCED risk: G7
re-checked against the build's §3.4 routes — holds.
```

## 3. SEAL TABLE (every digest recomputed by `shasum -a 256` at path THIS session, BEFORE reliance; sidecars compared)

```text
BUILD UNDER TEST:
38239f93de8b3a720659beba62f52dc2899e507f0fca72b57d88053dd59a09d8  STAGE8_ACT2_CARRIER_PACKAGE_S9AD_V001.md          MATCH (tasked + sidecar)
TASKED SEED (11/11 MATCH, sidecars matched; dilation gate via its old-convention
sidecar FULL_CARRIER_..._GATE_V001.seal.sha256):
f1881511c8a93cca3d8eb45f71b3be901cf337c9ce9321da0004901bfded6647  STAGE8_MIN_CARRIER_CANDIDATE_V001.md
bb2e6570e17bb5d7734d306d94ac672df73bb0377b71e252b48e4751b8dc28f5  STAGE8_MIN_CARRIER_CHECK_V001.md
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md
f3165db1a3b88a4d0667d3e3b214a55020aef31d424c5e3fce32d03cdb7774b1  STAGE8_CROSS_BLOCK_READ_V001.md
f13974b240369ab4718efb8482dc77c50105db4d2d64a2706ef208b67daff051  STAGE8_CROSS_BLOCK_READ_CHECK_V001.md
ca64f77e9808021405fc99a5351df40a0d32755e7f9e97d18193d1d350fd5d4b  STAGE8_RANK_PINNING_V001.md
d6edf0dc907112b2563eff742c89e89649a87e8ca9af5d39dab109a45ef3bfd0  STAGE8_RANK_PINNING_CHECK_V001.md
ca7fa4572a8b7037b40dd280700827081fec0e369ac0a8f9be1607113d719763  STAGE8_ANCHORING_DERIVATION_V001.md
4a048901d4d2869352b5daf668bc341c0a5a715ef9e6ef232ba5772a6c911625  STAGE8_ANCHORING_CHECK_V001.md
b807aa8e1f68298b554e02c5848feb908246837785d8f456ef0b98f66ff1ee82  FULL_CARRIER_DILATION_SELECTOR_OBSTRUCTION_GATE_V001.md
SWEPT/ACT-RELEVANT (verified by me before reliance; all sidecars matched):
cdeb019d3e528bffdd40527663a9e631e11f1196a3b591787e8a437b1f81b977  STAGE8_TYPING_NECESSITY_V001.md
f09798c18b11ef290cf7ff71be9bbdc5c8a4fcd6d10f52b87670f6be4e9e5642  STAGE8_TYPING_NECESSITY_CHECK_V001.md   (NECESSITY_VERDICT = SOUND at :414, verified at bytes)
74f9470189a72d0d214ce72153559e17f1dfb65abeb159afb30538f6774b954a  STAGE8_TYPING_RULE_CANDIDATE_V001.md
ee29ad93c6920abe85b7e7e1969f8a05af337159704e21014d5fc2861e0c0069  STAGE8_TYPING_RULE_CANDIDATE_CHECK_V001.md
bf75f3929d739eccd2c0b495f3ec99e3db7564fbc2cdbf78eb503e63d4ee7082  STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md
e7138b083fe73c060bcea26396a3ff0ff1b39a600e3ab8f7a63ea5a9e347fcb6  PROSPECTIVE_ENTRY_AND_CASCADE_AUTHORIZATION_DECISION_OF_RECORD_V001.md  (digest also pinned at addendum line 5 — chain verified)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md  (for quote checks)
064f0e2ffcdab596234e8f122202bbc30f6785ac9f36b7e70387e1e95596ee66  supervision/FIVE_C_PRINCIPAL_ACTS_STAGING_V001.md  (Act-2 definition; sidecar matched)
UNVERIFIABLE: none.  QUOTES VERIFIED AT BYTES: V007 :158-160 (the no-selector
bar) and :162-172 (operative domain / b_1 > 1 OUT OF SCOPE); bf75f392 :23 ("...
object selected by the specification, not a face forced by record composition");
cdeb019d :68/:323 ("T1_STATUS = DEAD-OF-RECORD" — the build's phrase is that
artifact's own claim language) and :341-352 (T0 SMUGGLES / the admission line);
f09798c1 :414.  The build's remaining span citations ride the custody model
(spans through verified seals' own certified tables) disclosed in its §1 and
previously verified of record by f13974b2 — accepted as disclosed.
```

---

## 4. THE ATTACK — WHAT RAN AND WHAT LANDED

### 4.1 Attack on the forcing determination (both failure directions)

```text
A FALSE FORCED would fabricate a lemma: hunted for any sealed instrument that
  closes the class — minimality clause (targeted grep of V011/92dba8d3/V007/
  addendum: NONE), the Attach domain (excludes b_1 >= 2 — cannot select), the
  anchoring law (value-forked; entered ports inert; ledger rows ACT-REQUIRED/
  PARTIAL per 4a048901), rank/germ structure (rank-blind, ca64f77e CONFIRMED),
  prior censuses (bf75f392: K_L frozen test object; NONE_SEALED survives), the
  forcing slot (NO_VERDICT).  The build's §3.4 six-route exhaustion is the same
  set; each route re-checked at its seal.  NO ROUTE CLOSES.  NOT-FORCED STANDS.
A FALSE NOT-FORCED would spend a principal act needlessly: the act is needless
  only if the class is EMPTY (it is not — K_dd conforms; floor re-enumerated by
  me, §5 B4) or a derivation preempts the admission (none: the O11/O12/O-D1
  constructor is unbuilt; the anchoring fragment derives a LAW whose entered-
  horn value is degenerate and licenses no identification; typing-necessity
  itself names the admission as the missing act).  THE ACT IS GENUINE.
QUANTIFIER DISCIPLINE: the build's §3.2 separation — "unique conforming complex
  at the minimal census under a lane-declared order" vs "unique admissible
  physical family" — is exactly right and is the load-bearing distinction; my
  independent floor re-enumeration (§5 B4) confirms the 16-labeled-pair
  uniqueness WITHOUT confirming any admissible-class uniqueness.  CORRECT.
```

### 4.2 Attack on the construction license (anti-fitting clause (ii))

```text
§4 consolidation = re-verification of sealed numbers + descriptive consequence
maps + falsifier inventory: consolidation-grade, no new geometry, no new typing
content, no promotion of any candidate (K_dd is "distinguished ONLY by the
lane's minimality instrument" in the build's own words).  §5 sheet = the
commissioned staging instrument.  §6 CAS = identities only.  NO LEAK FOUND.
```

### 4.3 Attack on the sheet (anti-fitting clause (iii))

```text
STEERING HUNT: four options (K_dd family / A5 / the conforming class / decline-
defer) in the candidate table's own order; decline present with no penalty
framing; no option recommended; per-option falsifier exposures are exact
conformance facts (FV-3 vs FV-4 assignments re-verified: f1881511 §2.4 and V007
:159-160 with dim H = 4 on A5); the two prior-ruling sub-options (R-g reading;
h_j typing) displayed with holders, not preferences; PROPOSED-NOT-DECLARED
stated twice.  STEERING = NONE FOUND.  DESCRIPTIVE HONESTY: each option's
would/would-not rows re-checked against the seals — accurate (Option 1's "D2
shrinks to the domain extension" is f1881511's own reduction; Option 2's "D2
at FULL strength" is exact).  One completeness NIT (N-2 below).
```

### 4.4 Attack on anti-fitting clause (i) — the 5d-FORCED-branch grep

```text
Every appearance of FORCED-branch content in the build was walked:
  §4.2 "what each candidate does to the cross-block" — DESCRIPTIVE consequence
    display of sealed V3/D-list content; task-2 scope; not a ground.  CLEAN.
  §4.4 "CONSUMES: the cross-block FORCED branch (D1..D4)" — interface map;
    descriptive.  CLEAN.
  §3.5 "NOT DECLARATION-PREMATURE: consumers ... exist and are named of record
    — the cross-block's D1-D4 ... and typing-necessity's first-future-consumer"
    — HERE the FORCED branch's debt list is used as part of the GROUND for a
    mechanics judgment (that the act is not premature).  A borderline LETTER
    contact with clause (i): see CORRECTION C-3.  It steers nothing (the same
    paragraph routes both F-II/F-III paths and the sheet keeps decline whole),
    and the second ground (typing-necessity) suffices alone — but the flag
    block's "cited nowhere as ground" is thereby OVERSTATED at bytes.
```

### 4.5 Attack on the sweep and its exhaustiveness claim

```text
The build's SWEEP 2 pattern was re-run VERBATIM this session:
  grep -rlEi "physical.carrier|admitted as physical|admission declaration|
  carrier of record" workspace/ supervision/ --include="*.md"
RESULT: ~120 surviving files, including files in NEITHER of the build's §2
lists: STAGE8_B1_CELL_INSTANCE_V001.md (a3bedc7e — the entered-cell instance
whose conformance ruling is load-bearing in the build's own R-f),
STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md, the GLUED_TOPOLOGY pair, and dozens
of TASK4A/TASK5/TASK6/AXN files.  The §2 claim ("no file surviving SWEEPS 1-3
... other than those listed ... bears on which cell family is admitted") FAILS
AT ITS OWN LETTER.  SUBSTANCE CHECK: every unlisted surviving file that bears
on the admission is carried through the build's verified seed-seal custody
chain (B1C via f1881511/f3165db1; both-blocks/hunt likewise) — NOTHING
SUBSTANTIVE WAS MISSED; my own filename+content sweeps surfaced no additional
admission-bearing sealed stock beyond what the build consumed.  CORRECTION C-2.
Fence-exclusion display: the displayed exclusion pattern is lowercase
(register|road_|...); run case-sensitively it fails to exclude uppercase fenced
names (QUESTIONS_SETTLED_REGISTER..., ROAD_REMAINING_FLAT..., PRINCIPAL_
EXPECTATIONS_LEDGER... all survive the -i MATCH patterns).  No evidence any
fenced file was opened — the build asserts none and carries no fenced content —
but the display does not show the case handling.  NIT N-1.
```

### 4.6 CAS re-run (the build's script, fresh venv) — MATCH

```text
Script §6.1 extracted VERBATIM from the sealed bytes; fresh venv (`auditvenv`,
python3 -m venv, pip install sympy) under this session's scratchpad; sympy
1.14.0 (matches the build's declared version).  Exit 0.  stdout diffed against
the build's §6.2: BYTE-IDENTICAL.  RERUN = MATCH.
```

---

## 5. THE ADVERSARIAL BATTERY (my own instrument; script `audit_battery_v001.py`, verbatim, and its output)

### 5.1 Script (verbatim; fresh venv `auditvenv`, sympy 1.14.0 + exact integer enumeration)

```python
#!/usr/bin/env python3
# audit_battery_v001.py — ACT2-AUDIT adversarial battery (independent of the build's
# script).  EXACT integer arithmetic only (python ints + sympy exact ranks).
# B1: frozen-face-shape realizability test (cyclic pattern (+,+,-,-) up to rotation
#     and reversal; authored-edge orientation flips FREE, sealed-edge orientations
#     FROZEN) applied to the sealed family rows A2b, A2, A1, W2, A5, K_dd.
# B2: exhibit of a CONFORMING opposite-edges variant A2b' at census 4/6/1.
# B3: upward-unbounded conforming family (K_dd + pendant path length k).
# B4: floor re-enumeration at (F,E,V)=(1,5,4): all authored extra edges x all
#     closed-4-walk faces, shape test applied -> conforming set == K_dd family.
import sympy as sp
from itertools import product

SEALED_E = [('e_a0','v00','v10'), ('e_0b','v00','v01'), ('e_ab','v10','v11'), ('e_ba','v01','v11')]
KSQ_V = ['v00','v10','v01','v11']

def build(V, E, faces):
    nV, nE = len(V), len(E)
    vi = {v:i for i,v in enumerate(V)}
    B = sp.zeros(nV, nE)
    for j,(_,s,t) in enumerate(E):
        B[vi[s], j] -= 1; B[vi[t], j] += 1
    D1 = sp.Matrix([[f.get(j,0) for j in range(nE)] for f in faces]) if faces else sp.zeros(0,nE)
    if faces: assert (D1*B.T).is_zero_matrix
    r0, r1 = B.rank(), (D1.rank() if faces else 0)
    return dict(B=B, D1=D1, nV=nV, nE=nE, r0=r0, r1=r1, comp=nV-r0, b1=nE-r0,
                gauge=r0, flux=r1, harm=(nE-r1)-r0)

FROZEN = (1,1,-1,-1)
def rotations_reflections(pat):
    out = set()
    p = list(pat)
    for r in range(4):
        q = p[r:]+p[:r]
        out.add(tuple(q))
        out.add(tuple(-x for x in reversed(q)))  # walk reversal: reverse order, flip signs
    return out
FROZEN_ORBIT = rotations_reflections(FROZEN)

def shape_realizable(walk_signs, authored_slots):
    """walk_signs: sign of traversal vs CURRENT orientation, in walk order (len 4).
    authored_slots: indices whose edge orientation is authored (sign may be flipped).
    Frozen orientations bind the other slots.  Test: does ANY flip assignment put the
    walk-order sign sequence into the frozen orbit (rotation+reversal closed)?"""
    for flips in product([1,-1], repeat=len(authored_slots)):
        s = list(walk_signs)
        for f, slot in zip(flips, authored_slots):
            s[slot] *= f
        if tuple(s) in FROZEN_ORBIT:
            return True
    return False

print('== B1. FROZEN-SHAPE REALIZABILITY OF THE SEALED FAMILY ROWS ==')
# each row: walk as list of (edge_name, sign_vs_orientation), authored edge names
rows = {
 'K_dd ': ([('e_a0',1),('e_ab',1),('e_ba',-1),("e_0b'",-1)], ["e_0b'"]),
 'A2b  ': ([('e_a0',1),('e_x',1),('e_ba',1),('e_y',1)],      ['e_x','e_y']),
 'A2   ': ([('e_a0',1),('e_ab',1),('e_x',1),('e_y',1)],      ['e_x','e_y']),
 'A1   ': ([('e_ab',1),('e_x',1),('e_y',1),('e_z',1)],       ['e_x','e_y','e_z']),
 'W2   ': ([('f1',1),('f2',1),('f3',1),('f4',1)],            ['f1','f2','f3','f4']),
 'A5   ': ([("e_a0'",1),("e_ab'",1),("e_ba'",-1),("e_0b'",-1)], ["e_a0'","e_ab'","e_ba'","e_0b'"]),
}
results = {}
for name,(walk,auth) in rows.items():
    signs = [s for _,s in walk]
    slots = [i for i,(e,_) in enumerate(walk) if e in auth]
    ok = shape_realizable(signs, slots)
    results[name] = ok
    print('  %s walk signs %s authored slots %s -> frozen shape realizable: %s' % (name, signs, slots, ok))
assert results['K_dd '] and results['A2   '] and results['A1   '] and results['W2   '] and results['A5   ']
assert not results['A2b  '], 'A2b unexpectedly conforms'
print('  => A2b AS DISPLAYED (sealed opposite pair e_a0,e_ba both traversed forward)')
print('     CANNOT realize the frozen (+ + - -) shape under ANY authored-edge flip,')
print('     rotation, or reversal: like signs at opposite slots vs frozen opposite-slot')
print('     opposite signs.  A2/A1/W2/A5/K_dd all realizable.  A2b ROW FAILS B4/R-c.')

print()
print('== B2. A CONFORMING OPPOSITE-EDGES VARIANT A2b′ (census 4/6/1) ==')
# e_x: v10->v11 (authored), e_y: v00->v01 (authored); walk v00 -e_a0-> v10 -e_x-> v11
# -e_ba^{-1}-> v01 -e_y^{-1}-> v00 ; word +e_a0 +e_x -e_ba -e_y  (frozen shape, slots
# 1,2 sealed+authored +, slots 3,4 -; sealed pair e_a0(+), e_ba(-): opposite signs OK)
E2 = SEALED_E + [('e_x','v10','v11'), ('e_y','v00','v01')]
C = build(KSQ_V, E2, [{0:1, 4:1, 3:-1, 5:-1}])
w = C['D1'].T
g = sp.Matrix([1,-1,1,-1,0,0])
assert (C['B']*g).is_zero_matrix and (C['B']*w).is_zero_matrix
assert C['comp'] == 1
assert sp.Matrix.hstack(w, g).rank() == 2          # [gamma] survives
# harmonic block pairs gamma: project gamma onto ker(d1) cap im(d0)^perp
# ker(B) basis; harmonic H = ker(B) cap w^perp
ker = C['B'].nullspace()
assert len(ker) == C['b1']
# solve for harmonic vector pairing gamma nonzero: h in span(ker), <h,w>=0, <h,g> != 0
import itertools
M = sp.Matrix.hstack(*ker)
a = sp.symbols('a0:%d' % len(ker))
hv = M * sp.Matrix(a)
sol = sp.solve([hv.dot(w)], a, dict=True)
found = False
for s in sol:
    hs = hv.subs(s)
    free = [x for x in a if x not in s]
    for vals in product([0,1,2,-1], repeat=len(free)):
        ht = hs.subs(dict(zip(free, vals)))
        if not ht.is_zero_matrix and ht.dot(g) != 0:
            found = True; break
    if found: break
assert found, 'no harmonic generator pairing gamma on A2b-prime'
walk_signs = [1,1,-1,-1]  # e_a0 fwd, e_x fwd, e_ba backward, e_y backward
assert shape_realizable(walk_signs, [1,3])
print('  A2b′: V/E/F = 4/6/1, blocks %d/%d/%d, connected; word +e_a0 +e_x -e_ba -e_y' % (C['gauge'],C['flux'],C['harm']))
print('  frozen shape realizable (sealed pair at opposite slots, OPPOSITE signs);')
print('  [gamma] survives; harmonic generator pairing gamma nonzero EXHIBITED.')
print('  => the opposite-edges CLASS retains a conforming member; only the sealed')
print('     table’s A2b ROW fails.')

print()
print('== B3. UPWARD-UNBOUNDED CONFORMING FAMILY: K_dd + pendant path length k ==')
for k in range(1, 5):
    V = KSQ_V + ['p%d' % i for i in range(1, k+1)]
    E = SEALED_E + [("e_0b'",'v00','v01')]
    prev = 'v11'
    for i in range(1, k+1):
        E.append(('q%d' % i, prev, 'p%d' % i)); prev = 'p%d' % i
    C = build(V, E, [{0:1, 2:1, 3:-1, 4:-1}])
    w = C['D1'].T
    g = sp.Matrix([1,-1,1,-1,0] + [0]*k)
    assert C['comp'] == 1 and (C['B']*g).is_zero_matrix
    assert sp.Matrix.hstack(w, g).rank() == 2
    assert (C['gauge'], C['flux'], C['harm']) == (3+k, 1, 1)
    h = 4*g - 3*w
    assert h.dot(w) == 0 and (C['B']*h).is_zero_matrix and h.dot(g) == 7
    print('  k=%d: V/E/F=%d/%d/1 blocks %d/1/1 — CONFORMS (gamma survives, <h,gamma>=7)' % (k, len(V), len(E), 3+k))
print('  => an explicit infinite conforming family; the class is unbounded upward.  MATCH')

print()
print('== B4. FLOOR RE-ENUMERATION AT (F,E,V) = (1,5,4) WITH THE SHAPE TEST ==')
# authored 5th edge: all ordered pairs (a,b) over the 4 vertices (loops incl.)
vi = {v:i for i,v in enumerate(KSQ_V)}
conforming = []
for a in KSQ_V:
    for b in KSQ_V:
        E5 = SEALED_E + [('x', a, b)]
        # incidence for walk enumeration on the multigraph
        # enumerate closed 4-walks using 4 DISTINCT edges, from every start
        edges = list(range(5))
        srcs = [vi[s] for _,s,_ in E5]; tgts = [vi[t] for *_,t in E5]
        def neighbors(v):
            out = []
            for j in range(5):
                if srcs[j] == v: out.append((j, +1, tgts[j]))
                if tgts[j] == v: out.append((j, -1, srcs[j]))
            return out
        for start in range(4):
            def walks(v, used, seq):
                if len(seq) == 4:
                    if v == start: yield seq
                    return
                for (j, s, u) in neighbors(v):
                    if j in used: continue
                    yield from walks(u, used | {j}, seq + [(j, s)])
            for seq in walks(start, frozenset(), []):
                word = [0]*5
                for j,s in seq: word[j] += s
                if all(x == 0 for x in word):  continue
                # trace-class survival: word must not be +-gamma-supported kill
                g5 = [1,-1,1,-1,0]
                W = sp.Matrix([word]); G = sp.Matrix([g5])
                if sp.Matrix.vstack(W, G).rank() < 2:  continue   # kills the class
                # face uses edge 4 (the authored edge)?  if not, word lives on K_square
                if word[4] == 0: continue
                # frozen shape: authored slot(s) = positions where edge 4 sits
                signs = [s for _,s in seq]
                slots = [i for i,(j,_) in enumerate(seq) if j == 4]
                if not shape_realizable(signs, slots): continue
                C5 = build(KSQ_V + ([] if b in KSQ_V and a in KSQ_V else []), E5,
                           [dict((j, c) for j, c in enumerate(word) if c)])
                if C5['comp'] != 1: continue
                if C5['harm'] < 1 or C5['flux'] < 1: continue
                # harmonic must pair gamma: dim H = 1 here so check the generator
                conforming.append((a, b, tuple(word)))
labels = set(conforming)
# canonicalize: complexes keyed by (a,b) unordered? keep labeled count
par = [(a,b,w4) for (a,b,w4) in labels if (a,b) in {('v00','v10'),('v10','v00'),('v00','v01'),('v01','v00'),('v10','v11'),('v11','v10'),('v01','v11'),('v11','v01')}]
nonpar = [t for t in labels if t not in par]
print('  labeled conforming (extra-edge, word) pairs: %d ; non-parallel-edge conformers: %d' % (len(labels), len(nonpar)))
assert len(nonpar) == 0, nonpar
print('  every conforming realization doubles a ring edge (parallel), none on a loop or')
print('  chord — the K_dd family exactly; labeled count %d (= variants x orientations x' % len(labels))
print('  walk representations, collapsing to the 8 isomorphic variants).  MATCH M-3.')

print()
print('ALL AUDIT BATTERY ASSERTIONS PASSED')
```

### 5.2 Output (verbatim; exit 0)

```text
== B1. FROZEN-SHAPE REALIZABILITY OF THE SEALED FAMILY ROWS ==
  K_dd  walk signs [1, 1, -1, -1] authored slots [3] -> frozen shape realizable: True
  A2b   walk signs [1, 1, 1, 1] authored slots [1, 3] -> frozen shape realizable: False
  A2    walk signs [1, 1, 1, 1] authored slots [2, 3] -> frozen shape realizable: True
  A1    walk signs [1, 1, 1, 1] authored slots [1, 2, 3] -> frozen shape realizable: True
  W2    walk signs [1, 1, 1, 1] authored slots [0, 1, 2, 3] -> frozen shape realizable: True
  A5    walk signs [1, 1, -1, -1] authored slots [0, 1, 2, 3] -> frozen shape realizable: True
  => A2b AS DISPLAYED (sealed opposite pair e_a0,e_ba both traversed forward)
     CANNOT realize the frozen (+ + - -) shape under ANY authored-edge flip,
     rotation, or reversal: like signs at opposite slots vs frozen opposite-slot
     opposite signs.  A2/A1/W2/A5/K_dd all realizable.  A2b ROW FAILS B4/R-c.

== B2. A CONFORMING OPPOSITE-EDGES VARIANT A2b′ (census 4/6/1) ==
  A2b′: V/E/F = 4/6/1, blocks 3/1/2, connected; word +e_a0 +e_x -e_ba -e_y
  frozen shape realizable (sealed pair at opposite slots, OPPOSITE signs);
  [gamma] survives; harmonic generator pairing gamma nonzero EXHIBITED.
  => the opposite-edges CLASS retains a conforming member; only the sealed
     table’s A2b ROW fails.

== B3. UPWARD-UNBOUNDED CONFORMING FAMILY: K_dd + pendant path length k ==
  k=1: V/E/F=5/6/1 blocks 4/1/1 — CONFORMS (gamma survives, <h,gamma>=7)
  k=2: V/E/F=6/7/1 blocks 5/1/1 — CONFORMS (gamma survives, <h,gamma>=7)
  k=3: V/E/F=7/8/1 blocks 6/1/1 — CONFORMS (gamma survives, <h,gamma>=7)
  k=4: V/E/F=8/9/1 blocks 7/1/1 — CONFORMS (gamma survives, <h,gamma>=7)
  => an explicit infinite conforming family; the class is unbounded upward.  MATCH

== B4. FLOOR RE-ENUMERATION AT (F,E,V) = (1,5,4) WITH THE SHAPE TEST ==
  labeled conforming (extra-edge, word) pairs: 16 ; non-parallel-edge conformers: 0
  every conforming realization doubles a ring edge (parallel), none on a loop or
  chord — the K_dd family exactly; labeled count 16 (= variants x orientations x
  walk representations, collapsing to the 8 isomorphic variants).  MATCH M-3.

ALL AUDIT BATTERY ASSERTIONS PASSED
```

### 5.3 What the battery establishes

```text
B4 independently re-derives the floor uniqueness (16 labeled pairs, all
   parallel-edge = the K_dd family; no loop, no chord) with MY OWN shape
   criterion — MC M-3 confirmed cross-instrument.
B3 exhibits an explicit infinite conforming family — the build's "unbounded
   upward" is now a THEOREM-BY-WITNESS, not only an observation.
B1/B2 land the one substantive correction (C-1 below): at census 4/6/1 the two
   readings of the frozen sign shape DIVERGE, and the sealed table's A2b row
   sits on the wrong side of one of them.  At the floor (E = 5) the readings
   coincide, so no headline object moves.
```

---

## 6. CORRECTIONS (none converting; the verdict NOT-FORCED and the act's genuineness stand)

```text
C-1  [VERDICT-CHAIN, non-converting]  THE A2b ROW OF THE CLASS DISPLAY.  Under
     the sealed-slot-binding reading of the frozen (+ + - -) face-sign shape
     (sealed-edge orientations frozen, authored-edge orientations free, pattern
     closed under rotation and walk reversal), the A2b complex AS DISPLAYED in
     the sealed family table — and EVERY closed-4-walk face on its 1-skeleton —
     fails R-c: its two sealed opposite edges are forced to like traversal
     signs at opposite walk slots, while the frozen word carries opposite signs
     at opposite slots (proof: §5 B1, exhaustive over flips x rotations x
     reversal).  A conforming opposite-edges variant A2b′ (4/6/1, word
     +e_a0 +e_x -e_ba -e_y, blocks 3/1/2, class survival and harmonic pairing
     exhibited) exists (§5 B2).  Under the PERMISSIVE reading (the sign pattern
     as labeling-relative — any oriented 4-gon on four distinct edges), A2b
     conforms and this correction dissolves.  THE SEALED CORPUS DOES NOT
     ADJUDICATE between these readings: the sign shape's binding strength on
     sealed-edge slots is unwritten — a SECOND reading-supply, parallel to
     R-g's, surfaced here for the record.  Root: the "conforming" verdict
     column of f1881511 §2.2 was verified by bb2e6570 at DIMENSIONS only (its
     M-3 shape enumeration ran at E = 5, where the readings coincide); the
     build inherited the column.  CONSEQUENCE FOR THE BUILD: the §3.3/§4.1
     displays should carry A2b as READING-CONTINGENT (or replace it with A2b′);
     the class remains infinite under BOTH readings (A2, A1, W2, A5, and the
     §5 B3 family are reading-robust), so NOT-FORCED, the floor, the 16-pair
     uniqueness, and every §3.5 conclusion are UNAFFECTED.
C-2  [CONFIRMED, non-converting]  THE §2 EXHAUSTIVENESS CLAIM FAILS AT ITS OWN
     SWEEP.  SWEEP 2 re-run verbatim returns files in neither §2 list,
     including admission-bearing sealed stock (a3bedc7e B1_CELL_INSTANCE —
     load-bearing in the build's own R-f; 92dba8d3 BOTH_BLOCKS; the
     GLUED_TOPOLOGY pair).  Substance intact: every such file is consumed
     through the verified custody chain, and my independent sweeps surfaced NO
     admission-bearing sealed stock the build failed to consume.  The claim
     should be re-scoped to: "no file surviving the sweeps carries admission-
     bearing content not already carried through the verified seed-seal custody
     chain."
C-3  [COSMETIC]  CLAUSE-(i) COMPLIANCE OVERSTATED AT BYTES.  §3.5's
     NOT-DECLARATION-PREMATURE paragraph grounds partly on the cross-block
     FORCED branch's debt list (D1-D4) — a borderline letter-contact with
     anti-fitting clause (i) — while the flag block asserts the branch's needs
     were "cited nowhere as ground."  The contact steers nothing and the
     independent ground (typing-necessity's first-future-consumer) suffices
     alone; the fix is to rest prematurity on that ground and mark the D-list
     mention descriptive.
C-4  [COSMETIC]  "T1 (DEAD-OF-RECORD)" in §4.1 is the typing-necessity
     artifact's own claim language (cdeb019d :68/:323; checked SOUND f09798c1
     :414) — faithful quotation, but the strength rider (a lane determination
     checked SOUND, registration the registrar's) is carried only implicitly;
     a strength tag at first use would prevent an of-record overread.
NITS (not charged):
N-1  the §2 sweep display omits the fence-exclusion's case handling (uppercase
     fenced names survive the displayed pattern run case-sensitively); no
     evidence any fenced file was opened.
N-2  the §5 mechanics display omits the mandate's battery-overdetermination
     clause (ad9fc14e clause 3: the candidate may not be shaped by or tested
     against any downstream target during authoring) — binding on any admission
     candidate and worth a row on the sheet.
```

---

## 7. CHOICE LEDGER (this audit's decisions, typed, with grounds)

```text
AL-1  Corpus roots = workspace/ + supervision/.  PREMISE(named).  Grounds: the
      build's CL-1 premise, adopted for comparability; my sweeps ran on both.
AL-2  Verdict = CONFIRMED-WITH-CORRECTIONS.  FORCED.  Grounds: the forcing
      determination re-derives exactly (pre-fixed match, §2); RERUN = MATCH;
      the sheet is steering-free; four corrections exist at bytes and none
      converts a headline claim (§6).
AL-3  The sealed-slot-binding reading of the frozen sign shape used in B1/B4.
      YOURS.  Grounds: my construction of the pattern's orientation-invariant
      content; the permissive alternative is displayed alongside and C-1 is
      stated fork-explicitly, adjudication left to the record (it is a reading
      supply, not an audit's to rule).
AL-4  Instrument = sympy fresh venv + exact integer enumeration.  IMMATERIAL
      (derived).  Grounds: exact identities are instrument-independent; four-way
      agreement now displayed (Fraction-RREF / SNF / build sympy / this battery).
AL-5  Reading the 5c staging sheet and typing pairs BEFORE fixing my answer.
      FORCED.  Grounds: they define the act under audit and bear on the forcing
      question; all fence-clean, seals verified first (064f0e2f sidecar matched).
AL-6  C-3's severity (letter-contact, COSMETIC, non-converting).  OPEN at the
      margin.  Grounds: the commissioned clause's referent ("the 5d-FORCED
      verdict-branch") is not defined at any byte I may open; I rule the §3.5
      use a letter-contact because the D-list IS that branch's needs list and
      it appears inside a ground clause; final adjudication is the principal's/
      registrar's, and the finding converts nothing either way.
```

## 8. TOY_SEPARATION self-assessment

```text
Every object this audit touched or built (K_dd, A5, A2b, A2b′, the pendant
family, the floor enumeration) is a record-typed combinatorial exhibit at the
CANDIDATE stratum or below; A2b′ and the pendant family are AUDIT-INSTRUMENT
witnesses only — exhibited to test the build's class display, proposed for
nothing, admitted as nothing.  No object is called physical; NONE_SEALED at the
physical write-carrier stratum survives this audit untouched.  No number was
promoted: mu, n, r_0, r_ch untouched; the only rationals handled are the sealed
exact fractions and my witnesses' integer entries.  The audit's own verdict
lives at the audit stratum: it confirms/corrects the BUILD, and neither
performs nor advances the admission act, which remains the principal's alone.
```

---

## FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS(the Act-2 package's forcing
  determination NOT-FORCED re-derives exactly from the sealed requires and
  matched this auditor's PRE-FIXED answer (scratchpad note hashed 93e47e82…
  BEFORE the build was opened; grounds G1-G7 independent; the commission's
  one-line orientation summary disclosed as unavoidable category exposure);
  the licensed package and staged sheet survive the license and steering
  attacks; the CAS re-runs byte-identical; four corrections and two nits, NONE
  converting: C-1 A2b reading-contingent (VERDICT-CHAIN, non-converting), C-2
  sweep-exhaustiveness letter-failure (CONFIRMED), C-3 clause-(i) compliance
  overstated by one borderline contact (COSMETIC), C-4 T1 strength tag
  (COSMETIC); N-1 fence-case display, N-2 mandate clause-3 row.)
FORCING_DETERMINATION_AUDIT = CONFIRMED(NOT-FORCED; both failure directions
  attacked: no sealed closure route exists — minimality is lane-declared and a
  targeted hunt found no sealed minimality require; the entered premise's
  b_1 = 1 domain excludes every both-blocks carrier; the anchoring law is
  value-forked with inert entered ports; the floor-uniqueness quantifier does
  not reach the admissible class — and the act is NOT needless: the class is
  non-empty (floor re-enumerated: 16 labeled pairs, all K_dd-family, no loop,
  no chord — MC M-3 confirmed by an independent shape criterion) and no
  derivation preempts the admission.  The class is INFINITE under BOTH readings
  of the face-sign shape: explicit witness family K_dd + pendant path, k = 1..4
  computed, blocks (3+k)/1/1, <h,gamma> = 7 preserved — unbounded upward now
  witness-proved.)
CORRECTIONS = C-1(VERDICT-CHAIN, non-converting: A2b as displayed fails the
  frozen (+ + - -) shape under the sealed-slot-binding reading — exhaustive
  proof over flips x rotations x reversal; conforming variant A2b′ 4/6/1
  exhibited with word +e_a0 +e_x -e_ba -e_y, blocks 3/1/2, class survival and
  harmonic pairing shown; under the permissive labeling-relative reading A2b
  conforms — the corpus does not adjudicate: a SECOND reading-supply, parallel
  to R-g's, surfaced for the record; root = f1881511 §2.2's "conforming"
  column, dims-only-verified by bb2e6570; class infinitude and NOT-FORCED
  unaffected under both readings) ;
  C-2(CONFIRMED: SWEEP 2 re-run verbatim returns unlisted admission-bearing
  files incl. a3bedc7e/92dba8d3/the glued pair — the §2 exhaustiveness claim
  fails at its own letter; substance intact, all such stock consumed through
  the verified custody chain; re-scope recommended) ;
  C-3(COSMETIC: §3.5 grounds prematurity partly on the FORCED branch's D1-D4 —
  borderline clause-(i) letter-contact; flag-block compliance sentence
  overstated; no steering results; independent ground suffices) ;
  C-4(COSMETIC: "DEAD-OF-RECORD" is cdeb019d's own language, checked SOUND
  f09798c1 :414 — carry the lane-strength rider explicitly).
RERUN = MATCH(the build's §6.1 script extracted verbatim, fresh venv, sympy
  1.14.0 = declared version, exit 0, stdout BYTE-IDENTICAL to §6.2).
BATTERY = PASSED(audit_battery_v001.py, verbatim in-artifact with output:
  B1 shape realizability across the family rows; B2 the A2b′ exhibit; B3 the
  infinite conforming family; B4 the floor re-enumeration = 16 labeled pairs,
  0 non-parallel conformers; exit 0, all assertions).
SHEET_AUDIT = STEERING-FREE, FALSIFIER-GUARDED, DESCRIPTIVELY HONEST,
  PROPOSED-NOT-DECLARED(four options incl. decline, table order, none
  recommended; FV-3/FV-4 exposure assignments re-verified at bytes — f1881511
  §2.4, V007 :159-160; N-2: add the mandate's clause-3 no-target-shaping row).
LICENSE_AUDIT = NO CONSTRUCTION BEYOND THE DETERMINATION(consolidation +
  commissioned sheet + CAS identities only; no new geometry, no new typing
  content, no candidate promoted).
SEALS_VERIFIED = build 38239f93 + 11 tasked seed + 8 swept/act-relevant (incl.
  f09798c1, e7138b08 — digest chain to addendum line 5 verified; ccca6bb4 and
  064f0e2f for quote/definition checks) — every digest recomputed at path by
  shasum -a 256 THIS session BEFORE reliance, sidecars matched (dilation gate
  via its old-convention sidecar); quotes verified at bytes: V007 :158-160,
  :162-172; bf75f392 :23; cdeb019d :68/:323/:341-352; f09798c1 :414.
  UNVERIFIABLE: none.
FORBIDDEN_IMPORTS = none(no scale, no imported GR, no faithfulness authority;
  no float ground, no measured constant, no value frozen; mu/n/r_0/r_ch never
  assigned; no register|road_|ledger|lens|plan|tracker|THE_HANDOFF file opened;
  "Q-..." tokens noted, not chased).
MACHINERY_INVOKED = no(shasum at path, file reads, greps, ls; fresh-venv sympy
  exact algebra + exact integer enumeration for identities only; no git; no
  register/commit/push; no existing file edited; output name probed ABSENT
  before write; one output + seal sidecar).
alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false.
ALL_RESULTS = CLAIMED until the registrar disposes; the admission act remains
  the principal's alone, and nothing here performs, urges, or forecloses it.
```
