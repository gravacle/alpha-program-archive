# STAGE 8 TASK 5 — Ξ_N V004: THE THREE DEFECTS CLOSED — LANE 3 V004

Date: 2026-08-05
Lane: CODEX LANE 3 (SOL, HIGH EFFORT), drafting lane
Task: PASTE 590 / Task 5
Base text: Dario V003 (d8f5112951c5378ce10126f91e8c89d0f31ebaae7ba6477677c9beb17b979e6c)
Supersedes: V003, re-reviewed DEFECTIVE on three bounded items at b179f2e175ca4d136fa14cdef35ebe64931e651434ea11cf4fde58038e32f4e9
Authority to draft: Q-516 and DoR-020-A8. **THIS ARTIFACT ADOPTS NOTHING.**

## Lead result

```text
REGISTER_HEAD = Q-516

ADOPTION LICENSES THE INTERFACE; IT DOES NOT INHABIT IT.

BASE_CARRIAGE = V003 carried verbatim outside the three marked mathematical
  deltas, metadata, dependency re-audit, and consequence boards
CANDIDATE_V004 = DRAFTED (15 clauses, (Z1)-(Z13) + (Z-A7) + (Z-U))
THIRD_HORN = TYPED_AS_LAW  (the discontinuity at zero is stated as a clause with
  the exact continuity-at-zero boundary, and read as the
  topological-charge signature -- a tradeoff the candidate accepts, not a defect
  it hides)
A7_CLAUSE = REAL_OBLIGATION  (V002 wrote a BICONDITIONAL -- pure character
  theory, true for every p, refutable by nothing. (Z-A7) asserts one side.)
ANTI_COUNTERTERM = STATED AS (Z13), conditional on uninhabited FULL-G4
ORIENTED_STRATA = TYPED, conditional on uninhabited G4-D/G5
CHARACTER_JUMP = along the section-5 displayed positive ray iff p not in 2 pi Z;
  false unconditional form withdrawn
DEPENDENCY_REAUDIT = DISPLAYED
READY_FOR_REVIEW = yes
VERB_AUDIT_SELF = CLEAN

THE (R9) REPAIR is sealed and confirmed at Q-515. It is cited for the addressed
orbit family and is not duplicated or consumed as an Xi_N inhabitance claim.

MEMBER_BOUND = false ; FIXED_POINT_EXECUTED = false ; END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false ; MEASURED_CONSTANT_COMPARISON = none
MACHINERY_APPEAL = G4-D + FULL-G4 + G5 + ExtSrc/Ker/transport/scalarization
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

## 0. Preflight

| Check | Result |
|---|---|
| Register head Q-516 | verified before construction |
| Base V003 `d8f51129…` | 398 lines; seal verified; mirror byte-identical |
| Finder's re-review `b179f2e1…` | 1,018 lines; seal verified; read in full |
| Loc B7 `b53d9e93…` | hidden-holonomy-counterterm hazard read |
| COMMON `5de94e16…` / COMMON-V2 `2525096b…` / confirmation `7150a768…` | addressed-orbit family and Q-515 status verified |
| Output name absent before construction | verified — no clobber |

### 0.1 Verbatim carriage and bounded-delta protocol

[PROVABLE — mechanical basis] This file began as a no-clobber byte copy of the
sealed V003 base. Except for metadata, the new dependency audit, the consequence
boards, and the three regions listed below, base-carried text is retained
verbatim. First-person statements inside retained V003 prose refer to the V003
drafter; they are historical carriage, not new Lane-3 claims.

~~~text
DELTA G1 =restore Y9 as (Z13), type FULL-G4, and restore its V3 falsifier;
DELTA G2 =replace unsigned fixed-support constancy by the orbit-addressed
          oriented-support factor and rerun the collapse regression;
DELTA G3 =condition character visibility by p notin 2 pi Z and retain the
          p in 2 pi Z blind regression; repair the dependent Z6 G2/G2-N
          booking without altering Z6's winding class.

NO OTHER MATHEMATICAL CLAUSE IS CHANGED.                    (P0)
~~~

[PROVABLE — byte comparison] The base clause blocks (Z7)-(Z10), (Z-A7),
(Z-U), the redone positive-ray computation through its value at `t=0`, and
(Z5) compare byte-for-byte with V003. The (Z6) gate-booking sentence is changed
only in G3's dependency re-audit to preserve the already-separated G2/G2-N
typing.

## 1. U1 — the third horn, the stratum gate, and G5

### 1.1 The type, restated with the sign arm honest

```text
(Z1) TYPE. Xi_N : D_N^Loc -> Lambda_N^cyc, a SET MAP. No R-linearity, no
     additivity in general, and NO CONTINUITY is claimed. The specification is
     CONDITIONAL on an alleged ExtSrc member; it inhabits neither D_N^Loc nor
     Xi_N. Integrality is the AUTHORED codomain condition of this clause -- see
     (Z8) -- and is not derived anywhere else.

(Z2) COMPATIBILITY, split by what is actually supportable:
  (i)  POSITIVE-scale invariance: Xi_N(t x) = Xi_N(x) for every t > 0,
       and Xi_N(0) = 0.
  (ii) [CONDITIONAL ON G5] The negative-scale arm is NOT asserted. If G5
       inhabits the H6b sign-odd source action, then reversal acts on the
       D side and the lawful form is Xi_N(-x) = -Xi_N(x); if G5 inhabits a
       sign-EVEN action, all-real invariance would collapse a nonzero member
       and is therefore excluded. Absent G5 the physical sign action is
       UNFORMED, not refuted, and this clause asserts nothing about t < 0.
  (iii)[CONDITIONAL ON G4-D] Disjoint-support additivity, on a typed D-side
       support relation only. Not asserted on overlapping supports.
```

[PROVABLE] **V002 over-claimed here and the re-review is right.** I wrote all-real scale invariance
(`t != 0`). That is a conditional kill on a sign-odd sector, and the physical sign action is
undetermined pending G5. **(Z2)(ii) now carries the dependence explicitly instead of assuming the
convenient branch.**

### 1.2 (Z3) The third horn, typed as law

```text
(Z3) DISCONTINUITY AT THE ZERO STRATUM -- stated, not hidden.

  DEFINED:      Xi_N(0) = 0. The zero source carries the zero cycle.
  STATED [CONDITIONAL ON G4-D]: for the addressed stratum map q_D,N^addr
  typed in (Z4),

       q_D,N^addr(x)=q_D,N^addr(y)
        =>Xi_N(x)=Xi_N(y).

  Constancy is asserted on one ORIENTED addressed stratum, never on the
  unsigned support fiber that contains both a stratum and its reverse.
  CONTINUITY STATUS: continuity at 0 is not a candidate requirement. If
  Xi_N has a nonzero value, the theorem below proves continuity at 0
  impossible; the zero-map case does not satisfy that antecedent.

  ZERO THEOREM (accepted as the price of (Z1)-(Z2)): if Xi_N(x_0) = z != 0,
  then for any positive t_n -> 0, t_n x_0 -> 0 in D while
  Xi_N(t_n x_0) = z != 0 = Xi_N(0). So no nonzero member is continuous at 0.

  SUPPORT-BIRTH THEOREM [CONDITIONAL ON G4-D]: where the typed D-side
  disjointness of (Z2)(iii), G4-D positive-scale support stability, and
  Xi_N(y) = z != 0 are all inhabited, x + t_n y -> x while
  Xi_N(x + t_n y) = Xi_N(x) + z != Xi_N(x). Discontinuity then recurs at that
  typed nonzero SUPPORT-BIRTH boundary.
```

[YOURS] **The reading, offered as the physical content rather than as an excuse.** A cycle assignment
is a **topological charge**: which loop a source encircles cannot vary continuously to "no loop at
all". The discontinuity at zero, and at each typed disjoint support-birth boundary carrying a
nonzero Xi contribution, is exactly the signature one should expect of an integer-valued
encirclement datum — it is what distinguishes such a datum from a field amplitude. **The candidate
accepts this as a tradeoff and displays it as law.** A future consumer needing continuity must
either restrict to one oriented addressed stratum or reject the candidate; it may not assume
smoothness across a typed nonzero support birth.

[YOURS — V004 DELTA G2] **The V003 correction is narrowed at its exact failure point.** V002's
"locally constant away from 0" remains REFUTED in the ambient topology by the support-birth
argument above. The lawful replacement is constancy **within one oriented addressed support
stratum**, not across the unsigned support fiber, and (Z3) now says only that. V002 also
offered "locally constant away from 0" as the reason the connectedness argument fails; that reason
was a non-sequitur — the escape is carried by (Z1)'s outright surrender of continuity, not by local
constancy.

### 1.3 (Z4) The support-stratum factorization is a gate item

```text
(Z4) ORBIT-ADDRESSED SUPPORT-STRATUM FACTORIZATION
     -- GATE ITEM G4-D, UNINHABITED.

For every primitive unoriented orbit [a]={b,b^-}, COMMON (O11)-(O17)
supplies the free address involution b|->b^- and carries the whole family
Triv_[a] isomorphic_to R_(>0) x Sigma_[a]. No b, sign, scale, or T is chosen.
G4-D must supply

  R_N^rec                                      common record-support carrier,
  supp_D,N:D_N^Loc->P(R_N^rec)                D-side support,
  AddrOrb_N                                    carried primitive unoriented
                                               address-orbit family at N,
  Prim_N:P(R_N^rec)->P_fin(AddrOrb_N)          primitive orbits in a support,
  S_D,N^addr                                   a fixed-point-free Z_2-set,
  forg_N:S_D,N^addr->P(R_N^rec)               forgets orientation only,
  q_D,N^addr:D_N^Loc->{0_D} disjoint-union S_D,N^addr,

with

  q_D,N^addr^(-1)({0_D})={0},
  supp_D,N(0)=emptyset,
  supp_D,N^(-1)({emptyset})={0},
  forg_N(q_D,N^addr(x))=supp_D,N(x)             for x!=0,
  forg_N(s^-)=forg_N(s),
  q_D,N^addr(t x)=q_D,N^addr(x)                 for t>0,
  supp_D,N(t x)=supp_D,N(x)                     for t>0,
  (s^-)^-=s,
  s^-!=s                                        on every nonzero addressed sector.

For every realized nonzero support

  S in im(supp_D,N|_(D_N^Loc minus {0})),

G4-D requires Prim_N(S)!=emptyset and a Z_2-equivariant bijection

  phi_(N,S):forg_N^(-1)(S)
   isomorphic_to product_([a] in Prim_N(S)) [a],

  phi_(N,S)(s^-)
   =(phi_(N,S)(s)_[a]^-)_[a in Prim_N(S)].

Because the product is nonempty and every primitive address orbit carries a
free involution, reversal has no fixed point on the realized fiber. The whole
orbit family is retained. q returns an addressed fiber, but no global address
representative, sign convention, scale, or T is selected.

The authored constancy law is exactly

  q_D,N^addr(x)=q_D,N^addr(y)
   =>Xi_N(x)=Xi_N(y).                            (Z4a)

It says nothing when the two q-values are mate strata s and s^-.

If G5 supplies a source reversal r_N^D on a sign-odd sector, its required
address law and Xi law are

  q_D,N^addr(r_N^D x)=q_D,N^addr(x)^-,
  Xi_N(r_N^D x)=-Xi_N(x).                       (Z4b)

If r_N^D x=-x on that sector, (Z4b) types scalar negation through the mate
address. Absent G5, neither the physical reversal nor an Xi value at the
reverse is inferred. Until G4-D exists, (Z2)(iii), (Z3)'s addressed
constancy, and the support-birth theorem are STATED BUT UNRUNNABLE.
```

[YOURS — V004 DELTA G2] The clause set licenses the addressed factor; it does
not inhabit it. The signed carrier R_or,[a] must not be used as q's value:
COMMON (O11) intentionally identifies (r,b^-) with (-r,b), whereas (Z4)
must retain the two mate strata so constancy cannot erase the sign action.

[PROVABLE — collapse regression conditional on G4-D/G5] Let x!=0 lie in a
G5 sign-odd sector with r_N^D x=-x, and put s:=q_D,N^addr(x). Then

~~~text
q_D,N^addr(-x)
 =q_D,N^addr(r_N^D x)
 =s^-
 !=s
 =q_D,N^addr(x).                                (Z4c)
~~~

The antecedent of (Z4a) is false for x and -x, so (Z4a) supplies no equation
Xi_N(-x)=Xi_N(x). G5 supplies only

~~~text
Xi_N(-x)
 =Xi_N(r_N^D x)
 =-Xi_N(x).                                     (Z4d)
~~~

For any formal z in the torsion-free lattice, the paired two-stratum
assignment

~~~text
Xi_N|_s=z,
Xi_N|_(s^-)=-z                                  (Z4e)
~~~

satisfies addressed constancy and (Z4b). This is the collapse regression, not
a full Xi member: Z13, transports, and every other gate remain separate.
No equation z=-z occurs, so no 2z=0 and no forced z=0 follow. Both mate
fibers and the entire Triv_[a] family remain carried; none is selected.

### 1.4 (Z13) The full-G4 anti-counterterm law, with its own falsifier

[YOURS — V004 DELTA G1] Loc (B6)-(B7) types the hazard: adding an
independently unconstructed Hol_N:D_N^Loc->im(P_H,N) to Loc_N^C creates an
enlarged family, and hiding that term inside localization is a holonomy
counterterm. B7 does not derive the following clause. It motivates the
failure mode; this draft states the support fence independently.

~~~text
(FULL-G4) CROSS-CARRIER SUPPORT BRIDGE -- GATE ITEM, UNINHABITED.

For every admitted finite stage N, FULL-G4 extends the same G4-D datum by

  supp_Lambda,N:Lambda_N^cyc->P(R_N^rec)      cycle-side support,
  cl_record,N:P(R_N^rec)->P(R_N^rec)          record closure,

with the support and closure laws

  supp_Lambda,N(0)=emptyset,
  supp_Lambda,N(n z)=supp_Lambda,N(z)
    for every z in Lambda_N^cyc and every nonzero n in Z,
  cl_record,N(emptyset)=emptyset,

where cl_record,N is extensive, monotone, and idempotent.

G4-D in (Z4) consumes the common R_N^rec and supplies supp_D,N plus its
addressed stratum laws. It does NOT supply supp_Lambda,N or the
cross-carrier comparison.

(Z13) SUPPORT / NO HIDDEN HOLONOMY COUNTERTERM.

For every alleged Xi_N and every x in D_N^Loc,

  supp_Lambda,N(Xi_N(x))
   subset cl_record,N(supp_D,N(x)).             (Z13a)

Z13 is an AUTHORED candidate law, not an inherited consequence of B7.
It is STATED BUT UNRUNNABLE until FULL-G4 is inhabited. It binds no Xi
member and proves no holonomy route exists.
~~~

The failure set and the one-element falsifier are displayed:

~~~text
CT_N(Xi_N;FULL-G4)
 :={x in D_N^Loc:
      supp_Lambda,N(Xi_N(x))
       not subset cl_record,N(supp_D,N(x))}.

(Z13) at N
 iff CT_N(Xi_N;FULL-G4)=emptyset.                (Z13b)

(V3) HIDDEN-COUNTERTERM VOID
 :=one displayed x_* in CT_N(Xi_N;FULL-G4).      (Z13c)
~~~

Once FULL-G4 is formed, one x_* refutes Z13. Before FULL-G4 is formed,
CT_N is UNFORMED, not empty; this is a gate gap, not passage of the clause.
The fence excludes support-remote period content. It does not claim that
every possible same-support counterterm is excluded.

[PROVABLE — conditional permanent failure-capability regression] Assume an
admitted epsilon=0 stage N and a FULL-G4 test datum with

~~~text
 D_N^Loc=R e,
 supp_D,N(t e)=S for every t!=0,

 z_out in Lambda_N^cyc,
supp_Lambda,N(z_out)
 not subset cl_record,N(S),

Xi_N(0)=0,
Xi_N(t e)=z_out for every t!=0.                 (Z13d)
~~~

With G5 uninhabited, the V003 positive-scale, integral-codomain, and
disjoint-support clauses admit this algebraic assignment; disjoint additivity
is vacuous apart from zero. The CT substitution is

~~~text
Xi_N(e)=z_out,
supp_D,N(e)=S,

supp_Lambda,N(Xi_N(e))
 =supp_Lambda,N(z_out)
 not subset cl_record,N(S)
 =cl_record,N(supp_D,N(e)),

therefore e in CT_N(Xi_N;FULL-G4).              (Z13e)
~~~

Thus Z13c rejects the test assignment once the assumed FULL-G4 datum is
formed. The regression does not assert that such a z_out exists at an actual
stage. No reader, magnitude, fixed point, or end test enters the falsifier.

## 2. U2 — units native, and the two transports exact

### 2.1 (Z7)/(Z8) Units and integrality, retyped natively

```text
(Z7) PHASE UNITS -- DERIVED NATIVELY, NOT TRANSFERRED.
     The U(1) edge relation gives
       h_e(A_1) h_e(A_0)^(-1) = exp( i integral_(gamma_e) (A_1 - A_0) ),
     whose exponent is a phase by construction; the integral sum paired with an
     INTEGRAL z therefore has phase class 1. Hence NO phase-unit conversion is
     required anywhere in (Z5)/(Z6).

     WITHDRAWN FROM V002: the claim that the raw D-side input x in D_N^Loc is
     dimensionless. That was a transfer of a result about C_N^k, and no
     displayed map carries it back to the D-side carrier.

(Z8) INTEGRALITY. Xi_N(x) in Lambda_N^cyc is the AUTHORED codomain condition of
     (Z1). It is NOT a unit consequence and is NOT supplied or derived by (Z2).
```

[PROVABLE] **V002's (Y5) was killed correctly.** The dimensionless result belongs to the Hodge
pairing on `C_N^k`; `Ξ_N` consumes raw `D_N^Loc`. **(Z7) re-derives what is actually true — the
*phase* is dimensionless, natively — and drops what was not.**

### 2.2 (Z9) Rank-preserving transport — the exact square

```text
(Z9) For rank-preserving f : N -> M, with S_f^Lambda := S_f|_(Lambda_N^cyc) the
     integral cycle transport and tau_f^Loc : D_N^Loc -> D_M^Loc the D-side map:

        Xi_M compose tau_f^Loc = S_f^Lambda compose Xi_N.

     CONSEQUENCE (relative form), for A_0, A_1 in Conn(P_M):
        Theta_N^rel(x; eta_conn,f(A_1), eta_conn,f(A_0))
         = Theta_M^rel(tau_f^Loc x; A_1, A_0).

     STATUS: tau_f^Loc is an UNINHABITED ExtSrc member (gate item G3). This is a
     GATED CONDITION, not a proved square.
```

[PROVABLE — provenance correction] V002 itself called the D-side transport
unsealed and attributed the stale "sealed D-side transport of f" phrase to
V001. **(Z9) retains the actual future type and books it; its displayed clause
is unchanged.**

### 2.3 (Z10) Cycle-creating transport — the false seal claim withdrawn

```text
(Z10) For cycle-creating f : N -> M, the old-image typing, completed:

     Lambda_M^(cyc,old) := S_f^Lambda(Lambda_N^cyc),
     r_f^(Lambda,old)   := (S_f^Lambda)^(-1) : Lambda_M^(cyc,old) -> Lambda_N^cyc,
     rho_f^Loc          : D_M^(Loc,old) -> D_N^Loc,

     IMAGE PREMISE:  Xi_M( D_M^(Loc,old) ) subset Lambda_M^(cyc,old),
     DOWNWARD SQUARE: Xi_N compose rho_f^Loc
                       = r_f^(Lambda,old) compose Xi_M|_(D_M^(Loc,old)).

     CONSEQUENCE, on the old image only:
        Theta_N^rel(rho_f^Loc y; eta_conn,f(A_1), eta_conn,f(A_0))
         = Theta_M^rel(y; A_1, A_0).

     NO value and NO lift is assigned on the target-new-cycle complement --
     a typed absence, not a zero map. S_f^Lambda is used ONLY to name the
     admitted old-image sublattice; it is NOT an upward Xi law.
     STATUS: rho_f^Loc is a future member (gate item G3-old).
```

[PROVABLE] **V002 called `rho_f^D` sealed. It is not** — it is a future member — and V002 omitted
both the `D^Loc` old-image typing and the cycle-image premise, so it failed before the consequence
could be stated. **(Z10) withdraws the false seal claim and supplies both missing premises.**

## 3. U3 — (Z-A7): the tautology replaced by a real obligation

[PROVABLE] **My V002 (Y-A7) was a tautology and I claimed refutability I did not have.** It wrote

```text
Theta_N^rel(...) = 1_(U(1))  <=>  p_x in 2 pi Z,
```

which is elementary character theory: for every real `p_x`, both sides are false together or true
together, so the biconditional holds always and **no exhibited `x` can refute it.** My stated
"single-element refutability" was therefore false. I wrote a **definition** where the gate needed an
**assertion**.

```text
(Z-A7) A7 IDENTITY-BRANCH OBLIGATION (epsilon = 1) -- ASSERTED, NOT DEFINED.

  For EVERY retained ordered pair (A_1, A_0), EVERY allowed K, EVERY addressed
  identity-branch response x, and the addressed factor basis element:

        Theta_N^rel( Ker_(a,K)(x); A_1, A_0 ) = 1_(U(1)),
     equivalently
        p_x := u_( Xi_N(Ker_(a,K)(x)) )(A_1 - A_0) in 2 pi Z.

  ONE displayed x with p_x not in 2 pi Z REFUTES this clause for that alleged
  tuple. On epsilon = 0 no such constraint is imposed.
```

[PROVABLE] **And it repays only RAW neutrality.** Full `(HOL2)` additionally needs, for every carried
scalarization member `S`:

```text
1_(U(1)) in U_b^S,   log_b^S(1_(U(1))) = 0,   U^Hol(0) = 0.     (A4)
```

**V002's sentence "G1/G2 carry the rest" is WITHDRAWN:** G2 says only "certified chart" and states
neither identity containment nor zero normalization; G1 types a singular unindexed map and states no
zero preservation. **(A4) is booked separately as gate item G2-N.** With (Z-A7) and (A4) both
inhabited the raw-neutrality chain closes; without them the identity branch is **unformed**.

## 4. U4 — (Z-U): U^Hol, carrier consumed, inhabitance gated

```text
(Z-U) THE ROUTE-NEUTRAL BARE CANDIDATE.
   From COMMON's orientation carrier, for every retained T in Triv_[a]:

      U_(a,T)^bare := T compose iota_a : R -> K_amb,
      U_(a^-,T)^bare = - U_(a,T)^bare.

   This exhibits a candidate of the REQUIRED BARE MAP TYPE, T-indexed, with no
   sign and no positive dimensionless scale selected.

   IT DOES NOT INHABIT G1. Inhabitance additionally requires:
     - a complete retained Scal^Hol member (P3);
     - the HOL-specific address-parity equations: a D-side reversal r_N^D with
       r_N^D o Ker_(a,K) = Ker_(a^-,K^-) o r_(a,K)^O and Xi_N(r_N^D x) = -Xi_N(x);
     - chart normalization (A4);
     - A7_HOL_BRANCH_COMPAT.
   STATUS: RESIDUE -- carrier built, bare candidate exhibited, no HOL member
   inhabited.
```

[PROVABLE] **The narrowed parity rule, applied correctly.** My own 584 narrowing: the rule constrains
**rules, not constructions**. So:

- **Sound form, which I assert:** COMMON's route-neutral carrier is a *construction* available to
  both routes, and (Z-U) exhibits the bare candidate form on it.
- **Unsound stronger form, which I explicitly disclaim:** that any H-specific discharge automatically
  proves the HOL log/phase seam. It does not. The H orientation proof consumes a *sealed* odd input
  evaluation (`c_(a^-) = -c_a`); the HOL analogue is the unbuilt chain above.

## 5. U5 — the winding proof, redone for the zero-homogeneous type

[PROVABLE] **V002 inherited V001's winding proof, which assumes `Ξ` linear. Under (Z2)(i) it is
inapplicable.** Redone:

```text
Put k := Ker_(a,K)(Rhat_(Kcycle,a)),  p := u_(Xi_N(k))(A_1 - A_0).

For t > 0, POSITIVE-scale invariance and linearity of Ker give
   Xi_N(Ker_(a,K)(t Rhat)) = Xi_N(t k) = Xi_N(k),
hence
   Theta_N^rel(Ker_(a,K)(t Rhat); A_1, A_0) = exp(i p)   -- CONSTANT in t,
NOT exp(i t p).

At t = 0: Xi_N(0) = 0 and u_0 = 0, so the value is exp(i u_0) = 1.
```

[PROVABLE — V004 DELTA G3] **The ray does not wind; character visibility at
the origin is conditional.** Define the displayed character along the ray by

~~~text
Theta(t)
 :=Theta_N^rel(Ker_(a,K)(t Rhat);A_1,A_0).

For t>0, Theta(t)=exp(i p);
Theta(0)=1_(U(1)).

CHARACTER_JUMP_AT_ZERO
 iff exp(i p)!=1_(U(1))
 iff [p]!=[0] in R/(2 pi Z)
 iff p notin 2 pi Z.                            (W4)
~~~

The false unconditional phrase "then jumps to 1" is WITHDRAWN. The V001
full-line winding attack still does **not** apply, because Theta is constant
on the punctured positive ray rather than exp(i t p).

[PROVABLE — permanent blind-case regression] If p in 2 pi Z, then

~~~text
Theta(t)=exp(i p)=1_(U(1)) for every t>0,
Theta(0)=1_(U(1)).                              (W5)
~~~

The character then sees no discontinuity. If Xi_N(k)!=0, Xi_N itself is
still discontinuous at zero by (Z3); the U(1) character is simply blind to
that integral-period jump. Whenever the section-5 displayed input Rhat is an epsilon=1
addressed response satisfying (Z-A7), p in 2 pi Z is required, so (W5), not
a character jump, is the required regression. No inference Xi_N(k)=0 is
licensed.

[PROVABLE] **G2 nonetheless remains open, on an independent basis.** Different support strata and
different rays may send the whole route image outside every single logarithm chart. Absent a
**whole-image containment certificate**, G2 stands. **(Z6) states the gate on that basis and not on
the withdrawn winding argument.**

```text
(Z5) RELATIVE CONSUMPTION -- unchanged from V002 and passed with scope:
     Theta_N^rel(x; A_1, A_0) := Hol_(A_1)(Xi_N x) Hol_(A_0)(Xi_N x)^(-1)
      = exp( i u_(Xi_N(x))(A_1 - A_0) ),
     for an ordered same-bundle pair; no absolute connection functional and no
     reference selection.

(Z6) WINDING. The defined object is the class [ u_(Xi_N(x))(A_1 - A_0) ] in
     R / 2 pi Z, the ambiguity being exactly 2 pi <z,n> with z, n integral.
     A REAL LOGARITHM is gated by G2: whole-image containment in a certified
     proper chart with a continuous single-valued logarithm/inverse and the
     other non-normalization Scal^Hol conditions -- NOT the withdrawn
     ray-winding argument. Identity containment and zero normalization (A4)
     remain separately gated by G2-N.

(Z11) COVARIANCE / NO SELECTION. Xi_N o u_D = u_Lambda o Xi_N, with the
     connection pair transforming SIMULTANEOUSLY. u_D is gate item G5. The whole
     Xi family, the whole affine family, and both Sigma members are retained;
     nothing is bound.

(Z12) READER- AND CONSEQUENCE-FREEDOM. Unchanged; passed.
```

## 6. U6 — V004 dependency re-audit

### 6.1 Dependency roots

[PROVABLE — completeness against the changed clauses] The only changed
mathematical roots and their direct consumers are

| Delta root | Direct consumers re-audited below |
|---|---|
| G1: FULL-G4 and Z13 | Z1, Z2(iii), Z4, Z5/Z6, Z9/Z10, Z11/Z12, V3, gate and stopping boards |
| G2: q_D,N^addr and Z4a/Z4b | Z2(i)-(iii), Z3 zero/support-birth arms, Z5/Z6, Z9/Z10, Z11, Z-A7, Z-U, V4 |
| G3: W4/W5 | Z3 visibility, G2 chart gate, Z5/Z6, Z-A7, winding/stopping/verb boards |

Z7/Z8 native units, Z9/Z10's equations and directions, Z-A7's universal
assertion, and the relative formula Z5 were not rewritten. They are
rechecked here because the new clauses consume or constrain their arguments.

### 6.2 G1 dependencies — support cannot be inherited by rhetoric

[PROVABLE] Z13 is well typed only after Z1 forms

~~~text
Xi_N(x) in Lambda_N^cyc,                         (D1)
~~~

and G4-D/FULL-G4 form both supports on the same R_N^rec. The address decoration
cannot hide a remote cycle because

~~~text
supp_D,N(x)
 =forg_N(q_D,N^addr(x))                         for x!=0,

forg_N(s^-)=forg_N(s).                           (D2)

Xi_N(r_N^D x)=-Xi_N(x)
 =>supp_Lambda,N(Xi_N(r_N^D x))
   =supp_Lambda,N(-Xi_N(x))
   =supp_Lambda,N(Xi_N(x)).                     (D2a)
~~~

Thus, on a formed G5 sign-odd tuple, Z13 compares the same unsigned source
support and the same cycle support on the two mate strata while Xi may still
take opposite lattice values.

[PROVABLE] Z2(i) and Z2(iii) remain unchanged. Z13 constrains where an output
cycle may live, not its magnitude and not addition on overlapping supports.
The algebraic remote-cycle assignment (Z13d) shows failure capability; Z13c
rejects it once FULL-G4 is formed.

[PROVABLE] Z5 remains unchanged. Z6 retains the same winding-class
mathematics, while its stale G2/G2-N booking is repaired as a G3 dependency:

~~~text
Theta_N^rel(x;A_1,A_0)
 =exp(i u_(Xi_N(x))(A_1-A_0)).                   (D3)
~~~

Z13 neither selects a connection nor converts a local F^2 symbol into a
period. It only forbids support-remote period content. Loc B7 remains a
hazard citation; no Hol_N map is introduced.

[PART-PROVABLE] Z9 and Z10 retain their exact equations and their uninhabited
tau_f^Loc/rho_f^Loc premises. Z13 is required separately at every stage.
This draft does not infer transport of supp_D, supp_Lambda, q_D^addr, or
cl_record from the Xi squares. Any such rank-preserving or old-image support
covariance is future FULL-G4 data. In particular,

~~~text
NEW-CYCLE UPWARD LIFT = absent.                  (D4)
~~~

[PART-PROVABLE] Z11's no-selection statement survives. A future FULL-G4 member
must be carried covariantly with the whole Xi/connection/address family; this
draft selects no support map, closure, address, or Xi member. Z12 survives
because Z13 and its falsifier mention no reader, response consequence,
threshold, fixed point, end test, or measured constant.

### 6.3 G2 dependencies — every sign-sensitive argument rerun

[PROVABLE conditional on G4-D] Positive scaling stays in one addressed
stratum:

~~~text
q_D,N^addr(t x)=q_D,N^addr(x)

 =>Xi_N(t x)=Xi_N(x)                             by (Z4a),

supp_D,N(t x)=supp_D,N(x)                       for t>0.     (D5)
~~~

Thus Z2(i) is consistent with, and is factored by, the new stratum type.

[PROVABLE conditional on G4-D] The support-birth computation now has every
premise displayed. If

~~~text
supp_D,N(x) disjoint supp_D,N(y),
Xi_N(y)=z!=0,
t_n>0,
t_n->0,
~~~

then D5 gives

~~~text
supp_D,N(t_n y)=supp_D,N(y),

supp_D,N(x) intersect supp_D,N(t_n y)
 =supp_D,N(x) intersect supp_D,N(y)
 =emptyset,

Xi_N(x+t_n y)
 =Xi_N(x)+Xi_N(t_n y)
 =Xi_N(x)+Xi_N(y)
 =Xi_N(x)+z
 !=Xi_N(x),                                      (D6)

x+t_n y->x.                                      (D7)
~~~

So discontinuity occurs at that typed disjoint support birth. D6 does not
claim a jump when Xi_N(y)=0 and does not cover an overlapping-support birth.

[PROVABLE] The zero-stratum theorem is independent of addressed constancy:

~~~text
Xi_N(x_0)=z!=0,
t_n>0,
t_n->0

 =>Xi_N(t_n x_0)=z!=0=Xi_N(0).                  (D8)
~~~

The status line in Z3 now says exactly what D8 proves: continuity is not
required, is impossible for a nonzero-image member, and is not refuted for
the zero map by this antecedent.

[PROVABLE conditional on G4-D/G5] The full sign-collapse regression is
(Z4c)-(Z4e). The old unsigned step

~~~text
same ordinary support
 =>Xi_N(-x)=Xi_N(x)                              (KILLED)
~~~

has no premise in V004. Mate strata have the same forg value but distinct q
values. When G5 identifies r_N^D x=-x, it yields only Xi_N(-x)=-Xi_N(x);
no 2 Xi_N(x)=0 follows.

[PROVABLE conditional on G5 and a formed relative tuple] If z=Xi_N(x), then
the mate value -z gives the inverse relative character:

~~~text
Theta_N^rel(r_N^D x;A_1,A_0)
 =Hol_(A_1)(-z) Hol_(A_0)(-z)^(-1)
 =Hol_(A_1)(z)^(-1) Hol_(A_0)(z)
 =Theta_N^rel(x;A_1,A_0)^(-1).                  (D9)
~~~

This rechecks Z5 and the sign arm of Z11. It does not build the logarithm
inversion, Ker address square, or U^Hol parity member; Z-U remains RESIDUE.

[PROVABLE] The positive punctured ray used by the winding calculation remains
in one q-stratum by D5. The negative ray, when a G5 reversal is formed, lies
in its mate. Hence the base computation

~~~text
Xi_N(t k)=Xi_N(k) for t>0                        (D10)
~~~

survives unchanged. G2 still requires a whole-image logarithm chart and stays
distinct from G2-N normalization.

[PROVABLE] Z-A7 is orbit-stable. If reversal sends p to -p, then

~~~text
p in 2 pi Z
 iff -p in 2 pi Z.                               (D11)
~~~

One p outside 2 pi Z still refutes the universally quantified Z-A7 tuple,
and its mate is outside as well. No address representative is selected.

[PART-PROVABLE] Z9/Z10 remain unchanged. No transport of q_D,N^addr is
inferred, and the cycle-creating clause remains old-image downward only.

### 6.4 G3 dependencies — character visibility separated from Xi discontinuity

[PROVABLE] W4 is now the only character-jump theorem along the section-5 displayed
positive ray and its formed tuple:

~~~text
CHARACTER_JUMP_AT_ZERO_ALONG_SECTION5_RAY
 iff p notin 2 pi Z.                             (D12)
~~~

W5 is the permanent regression:

~~~text
p in 2 pi Z
 =>Theta(t)=1 for every t>=0.                    (D13)
~~~

D13 does not imply Xi_N(k)=0. If Xi_N(k)!=0, D8 still proves Xi-space
discontinuity while the U(1) character is blind.

[PROVABLE] Whenever the section-5 displayed ray is built from a satisfying epsilon=1 Z-A7
response, it lies in D13. Therefore the identity branch requires raw
neutrality and forbids an assertion of a character jump on that ray. The
separately gated chart normalization A4 remains unchanged.

[PROVABLE] Conditioning W4 does not close G2. Whole-image containment,
single-valued logarithm, response independence, address covariance, and
normalization remain uninhabited. The class in R/(2 pi Z) remains exactly Z6.

### 6.5 Cross-delta and void regression

| Check | Re-audit result |
|---|---|
| G1 consumes G2 support | PASS: supp_D=forg o q; FULL-G4 separately adds supp_Lambda and closure |
| G2 consumes G1 | NO: addressed constancy is meaningful at G4-D scope before cross-carrier comparison |
| G3 consumes G1 | NO: W4 uses only Ker linearity, Z2(i), Xi(0)=0, and Z5 |
| G3 consumes addressed constancy | NO: the positive-ray law D10 suffices |
| native units | UNCHANGED |
| exact transports | UNCHANGED in equations, directions, and gate status |
| A7 assertion | UNCHANGED; D11/D13 are its regressions |

The new or restored failure conditions are:

~~~text
V3 hidden counterterm
 =one x_* satisfying (Z13c), once FULL-G4 is formed;

V4a address erasure
 =q_D,N^addr(r_N^D x)=q_D,N^addr(x) on a nonzero G5 sign-odd sector;

V4b selection
 =binding a global address representative, Sigma member, positive scale, or T;

V4c support-birth overclaim
 =using (D6) without support-scale stability, disjointness, and Xi_N(y)!=0.
~~~

V4a and V4c are UNFORMED before their gate data are formed. V4b is a live
syntactic falsifier now: inspection of this draft finds no bound global
address representative, Sigma member, positive scale, or T, so V4b is CLEAN.
None authorizes member selection or numerical execution.

### 6.6 V004 verb audit

| V004 line | Strongest displayed basis | Audit |
|---|---|---|
| CANDIDATE_V004 = DRAFTED | authored clause text only; nothing adopted | CLEAN |
| Z13 is the B7 support fence | YOURS clause with B7 only as hazard provenance | CLEAN — no derivation from B7 claimed |
| FULL-G4 / G4-D | exact uninhabited types in Z4/Z13 | CLEAN |
| oriented constancy | (Z4a), with mate inequality and collapse regression (Z4c)-(Z4e) | CLEAN |
| third horn | D8 at Xi level; D6 only on its typed nonzero birth scope | CLEAN |
| character jump | along the section-5 ray, W4/D12 iff p notin 2 pi Z; W5/D13 blind case | CLEAN |
| native units | base Z7 carried verbatim | CLEAN |
| exact transports | base Z9/Z10 carried verbatim; no support transport inferred | CLEAN |
| A7 obligation | base Z-A7 carried verbatim and rechecked by D11/D13 | CLEAN |
| R9 status | Q-515 confirms the separate seams repair; no Xi inhabitance inferred | CLEAN |
| READY_FOR_REVIEW = yes | all three commissioned deltas and dependencies displayed | CLEAN |

The following section is V003's disclosure ledger carried for provenance.
Its first-person pronouns and CANDIDATE_V003/R9-in-flight rows are historical,
not the V004 self-audit and not current status.

## 7. U7 — carried V003 disclosure ledger

| My line | Strongest verb my sources carry | Honest? |
|---|---|---|
| `CANDIDATE_V003 = DRAFTED` | authored, tagged YOURS; nothing adopted | **CLEAN** |
| `THIRD_HORN = TYPED_AS_LAW` | the re-review's (H10)/(H11), adopted as (Z3) with what is and is not defined | **CLEAN** — carried as an accepted tradeoff with its physical reading, not buried |
| `A7_CLAUSE = REAL_OBLIGATION` | the tautology finding, accepted in full | **CLEAN** |
| (Z7) units native | re-derived from the U(1) edge relation, not transferred | **CLEAN** — and the V002 D-side claim is *withdrawn*, not quietly dropped |
| (Z9)/(Z10) transports | the re-review's exact squares, consumed and cited | **CLEAN** — I do not re-present them as mine |
| (Z-U) parity | my own 584 narrowing applied; the unsound stronger form disclaimed by name | **CLEAN** |
| (Z6) G2 basis | the withdrawn winding argument replaced by whole-image containment | **CLEAN** |
| (R9) | cited as in flight; no position taken | **CLEAN** — and I note it cuts *against* my own route's rhetorical advantage |

[PROVABLE] **Five disclosed defects in my own V002, all real and all mine:**

1. **(Y-A7) was a tautology.** I wrote a biconditional of character theory and claimed
   single-element refutability. Nothing could refute it. **A definition where an assertion was
   required** — and I audited others for exactly this class of error in the same session.
2. **(Y5) transferred a unit result across carriers.** The dimensionless conclusion is about `C_N^k`;
   `Ξ_N` consumes `D_N^Loc`, and no displayed map carries it back.
3. **(Y8) called `rho_f^D` sealed.** It is a future member, and I also omitted the `D^Loc` old-image
   typing and the cycle-image premise.
4. **(Y4)'s proof half was inherited from V001 and is inapplicable** under my own zero-homogeneity —
   the very type change I made in V002. I changed the type and kept the old proof.
5. **"Locally constant away from 0" is false**, and I additionally used it as the *reason* the
   connectedness argument fails, which was a non-sequitur. The escape is carried by dropping
   continuity outright.

[PROVABLE] Item 4 is the one I find most instructive against myself: **I corrected a type and did not
re-audit the arguments that depended on it.** That is the same failure mode as carrying a stale
phrase, which I disclosed at 580.

```text
VERB_AUDIT_SELF = CLEAN (+5 disclosed defects in my own V002)
```

### 7.1 Carried V003 fence and stopping board (historical)

```text
STATUS = DRAFT FOR RE-REVIEW. NOTHING ADOPTED.
TYPE = integral set map; positive-scale invariant; sign arm CONDITIONAL on G5
THIRD HORN = typed as law; discontinuity at zero and at support births accepted
             and read as the topological-charge signature
UNITS = phase dimensionless NATIVELY; the V002 D-side claim WITHDRAWN
INTEGRALITY = authored codomain condition of (Z1), not derived
TRANSPORTS = exact squares stated; both gated on future ExtSrc members
A7 = a real obligation, refutable by one displayed x; (A4) booked separately
U^Hol = bare candidate exhibited on COMMON's carrier; G1 NOT inhabited
WINDING = V001 ray argument WITHDRAWN; G2 rebased on whole-image containment
GATE ITEMS = G1, G2, G2-N, G3, G3-old, G4-D, G5, plus the common ExtSrc/Ker package
ADOPTION_DELIVERS = the interface, NOT its inhabitance
MACHINERY_APPEAL = false
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

No register, plan, tracker, git, commit, push, member-binding, fixed-point, end-test, or evaluation
action was performed by this lane. No law was adopted.

CANDIDATE_V003 = DRAFTED (+14 clauses (Z1)–(Z12), (Z-A7), (Z-U): integral set-map type with no
continuity claimed; the compatibility law split so that only POSITIVE-scale invariance is asserted and
the sign arm is an explicit conditional on G5's source action; the third horn as (Z3); the
support-stratum factorization booked as gate item G4-D and explicitly uninhabited; relative
consumption unchanged; the winding class with G2 rebased; phase units derived natively with the V002
D-side transfer withdrawn; integrality as the authored codomain condition; the exact `tau_f^Loc`
rank-preserving square; the completed cycle-creating old-image typing with the false `rho_f^D` seal
claim withdrawn and no upward law; covariance with the connection pair transforming simultaneously;
reader-freedom; the A7 obligation; and the route-neutral bare `U^Hol` candidate)
THIRD_HORN = TYPED_AS_LAW (+DEFINED: `Xi_N(0) = 0` and constancy within a fixed-support stratum;
NOT DEFINED: any continuous extension through 0; +the theorem that no nonzero member is continuous at
zero, and that discontinuity recurs at every support-birth boundary wherever the typed disjointness is
inhabited; +read as the topological-charge signature — which loop a source encircles cannot vary
continuously to no loop at all — and accepted as the price of the type rather than hidden; +"locally
constant away from 0" REFUTED and withdrawn, together with the non-sequitur that used it as the escape
from the connectedness argument)
A7_CLAUSE = REAL_OBLIGATION (+my V002 wrote `Theta = 1 <=> p in 2πZ`, a biconditional of elementary
character theory that is true for every `p` and refutable by nothing, so my claimed single-element
refutability was false; +(Z-A7) now ASSERTS one side universally over the retained family, so one
displayed `x` with `p_x not in 2πZ` refutes it; +and it repays only RAW neutrality — the chart
conditions `1 in U_b^S`, `log_b^S(1) = 0`, `U^Hol(0) = 0` are booked separately as G2-N, with V002's
"G1/G2 carry the rest" withdrawn)
READY_FOR_REREVIEW = yes
VERB_AUDIT_SELF = CLEAN (+5 disclosed defects in my own V002: the A7 tautology; the cross-carrier unit
transfer; the false `rho_f^D` seal claim with two omitted premises; the winding proof inherited from
V001 and inapplicable under the very type change V002 made; and "locally constant away from 0" both
false and used as a non-sequitur — the fourth being the most instructive, since I corrected a type and
did not re-audit the arguments depending on it)

## 8. V004 fence, consequence, and stopping board

### 8.1 Current candidate board

~~~text
STATUS = DRAFT FOR FRESH REVIEW. NOTHING ADOPTED.

TYPE
 =integral set map; positive-scale invariant;
  physical sign arm CONDITIONAL on G5.

THIRD HORN
 =Xi-space discontinuity at zero for every nonzero-image member;
  support-birth propagation only on the typed disjoint/nonzero scope D6-D7.

ORIENTED FIXED-SUPPORT CONSTANCY
 =stated through q_D,N^addr;
  mate strata are distinct;
  unsigned support is forg_N(q), not the constancy class.

ANTI-COUNTERTERM
 =Z13 stated with one-element falsifier V3;
  FULL-G4 uninhabited;
  no Hol_N constructed;
  support-remote hidden content forbidden by Z13 once FULL-G4 is formed;
  same-support counterterms not excluded by this clause.

CHARACTER JUMP
 =along the section-5 displayed positive ray iff p notin 2 pi Z;
  p in 2 pi Z is the permanent character-blind regression;
  no inference Xi_N(k)=0.

UNITS
 =phase dimensionless NATIVELY;
  the V002 D-side claim WITHDRAWN.

INTEGRALITY
 =authored codomain condition of Z1, not derived.

TRANSPORTS
 =base Z9/Z10 equations carried verbatim;
  both gated on future ExtSrc members;
  no support transport and no upward new-cycle lift inferred.

A7
 =base real obligation carried verbatim;
  one typed p outside 2 pi Z refutes;
  every section-5 ray sourced by a satisfying epsilon=1 response lies in W5;
  A4 normalization remains separate.

U^Hol
 =bare candidate carried verbatim;
  no G1/Scal^Hol member inhabited.

R9
 =separate seams repair confirmed at Q-515;
  supplies no Xi inhabitance.

GATE ITEMS
 =G1, G2, G2-N, G3, G3-old,
  G4-D, FULL-G4, G5,
  plus common ExtSrc/Ker and scalarization packages.

ADOPTION_DELIVERS =the interface, NOT its inhabitance.
DEPENDENCY_REAUDIT =D1-D13 plus V3/V4 regressions displayed.

MACHINERY_APPEAL
 =G4-D + FULL-G4 + G5
  +ExtSrc/Ker/tau_f^Loc/rho_f^Loc
  +whole-image chart/scalarization inhabitance.

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
NUMERIC_EVALUATION = false
MEASURED_CONSTANT_COMPARISON = none
~~~

### 8.2 Bounded-delta closure

| Commissioned item | Closure |
|---|---|
| G1 Y9/full-G4 | Z13a is the clause; Z13b-Z13c are its own failure set and one-element falsifier; Z13d is the permanent remote-cycle regression |
| G2 fixed support | Z4a is constancy on one addressed stratum; Z4b carries reversal; Z4c-Z4e show the old 2z=0 collapse cannot run |
| G3 character jump | along the section-5 ray, W4 gives the iff condition; W5 gives the blind case; the unconditional phrase is withdrawn |
| G4 dependency audit | D1-D13 rewalk every direct consumer; V3/V4 and the verb board are rerun |

### 8.3 Battery

~~~text
F_PLDEC = CLEAN:
  Z13, q_D,N^addr, W4, and every falsifier are fixed before any reader,
  response consequence, threshold, fixed point, end test, or numeric output.

ANTI_TUNING = CLEAN:
  no clause, support, address, period class, or verdict was chosen from a
  desired numerical or route consequence.

NO_SELECTION = CLEAN:
  the whole [a] orbit, every mate stratum, the whole Triv_[a] family, the
  affine connection family, both A7 branches, and every alleged Xi member
  remain carried; none is bound.

CONTRAVARIANCE = CLEAN:
  Z10 remains old-image downward only; Z13 authors no lift.

FALSE_ANCHOR = QUARANTINED:
  no equation-of-record reader identity is consumed.

VERBATIM_CARRIAGE
 =base Z5, Z7-Z10, Z-A7, and Z-U text retained verbatim;
  all other base changes are within G1-G3 or their displayed dependencies.
~~~

No register, plan, tracker, Git-repository, commit, push, member-binding, fixed-point,
end-test, numerical-evaluation, or measured-constant-comparison action was
performed by this lane. No law was adopted.

CANDIDATE_V004 = DRAFTED
DEPENDENCY_REAUDIT = DISPLAYED
READY_FOR_REVIEW = yes
VERB_AUDIT_SELF = CLEAN
