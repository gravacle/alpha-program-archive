# STAGE8 TASK 4B: JET RACE ADJUDICATION - LANE 1 V001

Date: 2026-08-03  
Task: PASTE 453 / Task 4b / jet-race adjudication  
Lane: CODEX LANE 1  
Register head at preflight: Q-371  
Custody: adversarial adjudicator; no adoption or registration authority

```text
LEAD_RESULT = RADIAL_GERM_NOT_READY

KILLING_ITEMS = G2 | G3 | G7

G2_TARGET_TUNING = KILL |
  J3 selects complement dependence for nonzero CC/CK response jets;
  the reverse-engineered target profile is exactly the submitted profile

G3_CERTIFICATES = KILL |
  required physical tangent Hilbert norms, unit maps, and Riesz maps are absent;
  covariance and rho_H,N therefore do not follow

G7_FRESH_ATTACK = KILL |
  cycle-creating restriction changes the radial coefficient and generically
  prevents the proposed stationary roots from forming a restriction-natural family

INSTANTIATION = NOT_READY | G2,G3,G7
READY_FOR_DOR018_RULING = no

P_VERDICT_COMPUTED = false
NUMERIC_RESPONSE_VALUE_COMPUTED = false
VOID_CANDIDATE = true | proposal-level J1/J3 conditions

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

The radial formulas are mathematically differentiable and their finite
active jet is exactly zero.  Those facts do not cure the gate failures.  The
proposal chooses its complement factor because of the response blocks that
factor creates, imports uninstantiated inner products as if they were
ratified carrier structure, and overclaims restriction naturality of its
stationary family.  The candidate therefore cannot go to DoR-018 in V001.

---

## 0. Preflight and authority ledger

`alpha_supervision/LOCKED_PROCESS.md` was read in full.  Its sidecar and the
questions-settled register sidecar verified.  The register head was Q-371.
The proposal hash and sidecar verified before substantive reading.

```text
TARGET = STAGE8_TASK4B_N_MEMBER_JET_INSTANTIATION_PROPOSAL_LANE2_V001.md
TARGET_SHA256 = 0a7b45a68899054be3266e2f1a2fb23806e7ba75e73a6326c683eebda216fac1
TARGET_SEAL = OK
```

| Authority | Verified SHA-256 | Use |
|---|---|---|
| Lane-1 derive arm | `8cb2ad22b8b7c4a807c72b2fc03d56e6781a8a96e0631adb1fbe63fd200dd7f1` | underivability standard and exact finite zero jet |
| stationary response computation | `be570c182ef875b557395b62c382ee875420ac0462e2efb5774e9600f794b27a` | exact Schur consumer and chronology |
| response cross-verification | `cac131d949a917576e30332b2f4ec76ca7db57d6d4683e91cfeac658994c499b` | confirmed component boundary and tags |
| square V004 | `abf6d366a5a7e375b9b53df75402f35d37f7c6a4b1bb0b10a44309ad3b0e1912` | R1/R4/R5 interfaces and Hessian cube |
| divergence/action draft | `ed49624b6f5f0bcda94ee88a939a5751113b30d73192b2c28a5aec25829e1797` | flat-family and generator boundary |
| DoR-017 decision | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | N-member ruling and void discipline |
| DoR-015 field V005 | `7ecf04e903512c64af96922f3535f271176d9d376b7ee2d5645ed676dd841b12` | actual quotient/source-rigging content |

The DoR-015 decision sidecar also verified.  Its adoption text identifies
the source rigging as one of six openly authored amplitude-side structures;
it does not add a Hilbert inner product to both R5 physical tangent sectors.

```text
DOES_THE_OBJECT_EXIST = yes | proposal and its adjudication obligation
IS_THE_VERSION_CURRENT = yes | Q-371
ARE_ITS_INPUTS_PRESENT = yes for review | no for the proposal's claimed Riesz data
PREFLIGHT = PASS_FOR_REVIEW
```

---

## 1. Verdict table G1-G7

| Item | Verdict | One-line reason |
|---|---|---|
| G1 no derived-status smuggling | **PASS** | the germ, radial primitive, flat gate, complement factor, normalizer, and stationary branch are explicitly proposed in J1-J5; their derivatives are only conditional consequences of that authored germ |
| G2 target-tuning attack | **KILL** | J3 selects `a(c)` for the nonzero `CC` and `CK` jets it creates; the exact reverse-engineered finite-invisible/nonzero-response profile is the submitted product `a(c)f(s(k))` |
| G3 instantiated certificates | **KILL** | the derivative algebra is correct, but covariance and the Hessian cube assume physical tangent Hilbert norms, carrier-unit maps, and Riesz maps not supplied by DoR-015 or R4 |
| G4 finite consistency | **PASS** | conditional on a Hilbert norm, `exp(-1/||k||^2)` is smooth and flat at `k=0`, so every finite active derivative is exactly zero |
| G5 Schur substitutions | **PASS** | the loop and S8-A block substitutions are algebraically correct and properly tagged; no p verdict or value appears |
| G6 zero degeneration | **PASS** | direct germs and jets converge linearly to Z as `nu->0`; stationary convergence is honestly limited to uniform inverse charts |
| G7 minimality/choice table plus fresh attack | **KILL** | the table omits the tangent inner products/unit maps and the divergence-to-radius identification; independently, cycle creation destroys the claimed stationary-root restriction square |

---

## 2. G1 - authored status against Q-370

Q-370 proved that

```text
Gen_m(delta_div,m)=phi_m
```

is interface-only and that no completed stationary two-jet follows from the
ratified divergence datum or symbolic `nu`.  V001 does not contradict that
negative in its formal standing.  It marks itself
`PROPOSED_NOT_ADOPTED`, and it puts the following fields on J1-J5:

```text
J1 radial primitive (a,s),
J2 flat gate f(s),
J3 complement weight a(c),
J4 local stationary branch,
J5 coefficient normalizer.
```

The displayed jets are direct derivatives of the proposed `phi`; that is a
valid conditional derivation rather than a claim that Q-370 forced the
germ.  The stationary point is likewise a proposed local inverse-branch
family, not a computed point.

There is one imprecise phrase: the proposal calls the N member "already
ratified" while replacing its opaque component record with R001.  Read with
the DoR-018 standing, this is an openly proposed instantiation of the
existing extensional member, not an additional member.  V001 does not gain
TYPE-P standing from the phrase.

```text
RADIAL_GERM_OPENLY_AUTHORED = true
RADIAL_JETS_DERIVED_FROM_AUTHORED_GERM = true
STATIONARY_BRANCH_OPENLY_PROPOSED = true
Q370_NEGATIVE_RESTATED = true
G1 = PASS
```

---

## 3. G2 - target-tuning attack

### 3.1 Order-of-construction audit

The textual order inside V001 is

```text
radial formula -> jet calculation -> Schur substitution.
```

That order alone cannot certify independence.  Before this proposal was
authored, Q-368 had already published the exact response consumer and Q-369
had confirmed that the missing data were precisely

```text
D_C phi, D_C^2 phi, D_C D_K phi, D_K D_C phi, D_K^2 phi
```

at the stationary point.  V001 lists both artifacts as inputs.  Historical
access to the target structure is therefore established.

More importantly, the proposal's own J3 justification is outcome-facing:

```text
J3 minimality = "least even norm weight with nonzero CC and potentially
                 nonzero CK jets".
```

`CC` and `CK` are not primitive record or divergence data.  They are the
response-facing blocks Q-368 said the new germ must populate.  Thus the
choice is justified by what it causes in the response architecture.

### 3.2 Independent reverse-engineering attempt

Start only from a desired response pattern:

1. Keep every finite active jet zero.  Use a flat cycle factor
   `f(s)=exp(-1/s)`.
2. Avoid cycle-basis and orientation selections.  Use the radial invariant
   `s=||k||^2`.
3. Make the direct member nonzero away from the active section.  Multiply by
   symbolic `nu`.
4. Make `D_C phi` and `D_C^2 phi` nonzero so that the stationary point and
   complement inverse can move.  Multiply by a nonconstant invariant
   `a(c)`.
5. Make that factor even, basis-free, and minimally quadratic.  Choose
   `a(c)=1+(1/2)||c||^2`.

This reverse-engineering produces exactly

```text
phi(c,k)=nu (1+(1/2)||c||^2) exp(-1/||k||^2),     (G2-1)
```

with zero extension at `k=0`, which is V001's proposal.

The simpler candidate

```text
phi_cycle(k)=nu f(||k||^2)                        (G2-2)
```

already satisfies smoothness, finite flatness, reality, radial covariance,
identity anchoring, and nonzero off-section standing.  It was rejected in
J3 because it would not create the complement/mixed response blocks.  That
is a response consequence deciding between otherwise admissible authored
members.

### 3.3 Divergence provenance check

V001 also defines

```text
delta_div,m^R001 := delta_rad := (a(c),s(k)),
Depth := delta_rad.
```

No cited log-divergence or record-depth result identifies its divergence
datum with this pair of physical tangent radii.  The assignment is an
authored relabeling, not a descent from the ratified divergence content.
The profile consequently gains no target-independent preference from the
principal's "divergence is the coupling" steer.

The proposal's J1 void condition includes "datum chosen from a response
target."  J3's own nonzero-block justification and the reconstruction above
meet that condition.  This finding does not allege a desired numerical p
answer; tuning the support of the response is already target-aware.

```text
PROFILE_CONTAINS_P = false
PROFILE_FITTED_TO_NUMERIC_VALUE = false
PROFILE_SELECTED_FOR_RESPONSE_BLOCK_SUPPORT = true
DIVERGENCE_TO_RADIAL_PAIR_DERIVED = false
J1_J3_VOID_CONDITION_FIRED = true
G2 = KILL
```

---

## 4. G3 - certificate recomputation

### 4.1 Jet algebra

Conditionally take positive Hilbert forms with Riesz maps `R_C,R_K`, and
set

```text
a(c)=1+(1/2)||c||_C^2,
s(k)=||k||_K^2,
phi=nu a(c)f(s).
```

Then

```text
D_C a=R_C c=:c^flat,
D_C^2 a=R_C,
D_K s=2R_K k=:2k^flat,
D_K^2 s=2R_K.
```

The independently recomputed blocks are

```text
D_C phi=nu f(s)c^flat,

F_CC=nu f(s)R_C,
F_CK=2nu f'(s)c^flat tensor k^flat,
F_KC=(F_CK)^T,
F_KK=nu a(c)[2f'(s)R_K
                   +4f''(s)k^flat tensor k^flat]. (G3-1)
```

For `f(s)=exp(-1/s)`,

```text
f'(s)=f(s)/s^2,
f''(s)=f(s)(s^(-4)-2s^(-3)).                      (G3-2)
```

V001's formulas are correct.  They scale linearly with `nu` at a fixed
point.  Reality is also correct if the inner products are real and the
admitted signed maps are isometries.

### 4.2 The missing carrier geometry

The conditional clause above is load-bearing.  V001 says:

```text
"DoR-015 supplies the basis-free physical Hilbert norms"
```

on both `C_G` and `K_G`, and says carrier-unit isomorphisms are already
required by R1/R4.  The cited texts do not say this:

1. DoR-015 V005 constructs `J_phys^005` as the completion of finite
   conserved **source currents** in the retained A4 proposal norm.
2. V005 expressly records `A4_NORM_FORCED_BY_GATE4 = false | TYPE-U`.
3. Door B forms a Hilbert source rigging.  It does not instantiate a
   positive inner product on the R5 physical complement `C_prop`.
4. R4 declares the units of the scalar action and normalizer.  It contains
   no carrier-unit isomorphism for `C_prop` or `K_cycle`.
5. R5 names `Y=C_prop direct-sum K_cycle`, a topology, reducing support, and
   inverse.  It does not name `R_C`, `R_K`, or orthogonal transport on both
   sectors.

Therefore `||c||_C`, `||k||_K`, `R_C`, and `R_K` are additional authored
fields.  V001 neither places them on its choice table nor proves that every
admitted `alpha_C,alpha_K` preserves them.

The covariance equation

```text
phi_(G')(alpha_C c,alpha_K k)=phi_G(c,k)           (G3-3)
```

holds only after the missing isometry theorem.  Differentiating `(G3-3)`
cannot prove the theorem whose premise is missing.  The same gap propagates
to the rank-two exchange, the pendant quotient norm, the completed Hessian
transport, and `rho_H,N`.

For the rank-two witness, an exchange matrix `P` would give

```text
s(Pk)=s(k),
R_K P=P^(-*)R_K,
(Pk)^flat tensor (Pk)^flat
 =P^(-*)[k^flat tensor k^flat]P^(-1),              (G3-4)
```

exactly when `P` is an isometry of the missing form.  Under that premise the
V001 witness passes.  For the pendant witness, the descent sends the
pendant/tree component to zero in `K_cycle`; the jet is unchanged if the
norm descends to that quotient.  That quotient-norm descent is also one of
the unproved pieces.  Neither witness supplies the premise it tests.

Batching and `nu` homogeneity pass algebraically.  The rank-two and pendant
tests pass only in a chosen invariant norm model, not on the ratified carrier
as cited.

```text
JET_FORMULAS = PASS_CONDITIONALLY
NU_HOMOGENEITY = PASS
REALITY = PASS_CONDITIONALLY
BATCHING = PASS
PHYSICAL_TANGENT_HILBERT_FORMS_PRESENT = false
CARRIER_UNIT_MAPS_PRESENT = false
R1_COV_REPROVED = false
RHO_H_N_CUBE_REPROVED_FOR_R001 = false
RANK_TWO_WITNESS = conditional_only
PENDANT_WITNESS = conditional_only
G3 = KILL
```

---

## 5. G4 - finite consistency

Assume any fixed positive Hilbert norm on a finite cycle carrier and define

```text
F(k)=exp(-1/||k||^2) for k!=0,
F(0)=0.
```

For every integer `r>=0`,

```text
lim_(s->0+) s^(-r) exp(-1/s)=0.
```

Every derivative of the radial composite is a finite sum of a polynomial
in `k` and powers of `s^(-1)` times `exp(-1/s)`.  Each term tends to zero.
Thus `F` is smooth and all derivatives vanish at `k=0`.  Multiplication by
the polynomial `a(c)` preserves that statement along
`S_N=C_N direct-sum {0}`:

```text
j^infinity(phi_N)|_(S_N)=0.                       (G4-1)
```

Consequently every first and second finite active member block is zero, and
the Q-243/Q-279 finite active retarded block remains exactly zero, p-free,
and `nu`-free.  This verifies the theorem from Q-370.

G4 evaluates the formula's flatness only.  It does not supply the missing
physical norms identified in G3.

```text
FINITE_ACTIVE_FLATNESS = PASS
FINITE_ACTIVE_JET = zero | all orders
Q370_REPRODUCED = true
G4 = PASS
```

---

## 6. G5 - Schur substitutions

### 6.1 Reciprocal loop

On a temporary unit chart `k=t u_K`, `(G3-1)` becomes

```text
F_CC=nu f(t^2)R_C,
F_CK=2nu t f'(t^2)c^flat tensor u_K^flat,
F_KC=(F_CK)^T,
F_KK=nu a(c)[2f'(t^2)+4t^2f''(t^2)]R_K.          (G5-1)
```

Substitution into

```text
S=H_KK+F_KK
 -(H_KC+F_KC)(H_CC+F_CC)^(-1)(H_CK+F_CK)          (G5-2)
```

gives V001's E4-3 exactly, with all fields evaluated at the proposed local
stationary branch.  No term is dropped and no `i`, `hbar`, or sign is added.

### 6.2 S8-A

For rank two, `s=||k||^2`, and `(G3-1)` gives V001's E4-4/E4-6 exactly.
Under an orthogonal signed exchange, `s`, `R_K`, and
`k^flat tensor k^flat` transform covariantly.  This statement is again
conditional on the missing invariant inner product from G3; the block
algebra itself is correct.

The sensitivity ledger is honest:

```text
direct jets, stationary point, total blocks, inverse, Schur = MEMBER-SENSITIVE;
differentiation, formal restriction, RetExtract = MEMBER-INDEPENDENT operations;
finite active zero = MEMBER-INDEPENDENT over the proposed scaling line.
```

The formulas contain symbolic base `p` only inside the pre-existing base
action and blocks.  They do not evaluate p, `nu`, a response value, a root,
or a physical verdict.

```text
RECIPROCAL_LOOP_SUBSTITUTION = PASS
S8A_SUBSTITUTION = PASS_ALGEBRAICALLY
TAG_LEDGER = PASS
P_VERDICT_LEAK = false
NUMERIC_VALUE_LEAK = false
G5 = PASS
```

---

## 7. G6 - zero-member degeneration

At fixed `(c,k)`,

```text
phi_nu=nu psi,
D phi_nu=nu D psi,
D^2 phi_nu=nu D^2 psi.
```

Hence the germ and every direct jet converge continuously to the Z member
as `nu->0`.  If the R5 complement inverse remains uniformly defined, the
implicit-function theorem carries the stationary branch continuously to
the base/Z branch.  V001 expressly withdraws stationary continuity at a
support-rank jump.  That is the correct boundary.

```text
DIRECT_ZERO_DEGENERATION = PASS
STATIONARY_ZERO_DEGENERATION = PASS_ON_UNIFORM_INVERSE_CHARTS
SUPPORT_JUMP_OVERCLAIM = false
G6 = PASS
```

---

## 8. G7 - minimality, omitted choices, and fresh attack

### 8.1 Choice-table completeness

J1-J5 do not cover all authored data used by the proposal.  Missing rows
are:

```text
J6_CARRIER_METRICS := positive Hilbert forms on C_prop and K_cycle,
                      their Riesz maps, and their automorphism/restriction law;

J7_CARRIER_UNITS   := the maps making both squared norms dimensionless;

J8_DIVERGENCE_MAP := the identification of the ratified log-divergence datum
                     with (a(c),s(k)).
```

Each has genuine alternatives and consequences.  For example, changing the
relative Hilbert weights changes the radial profile and every off-section
jet while preserving the finite active zero.  None is gauge.  The current
table therefore understates the authored fiber.

J3's minimality is also invalid as a non-target argument: `a(c)=1` is
strictly simpler and satisfies every kinematic certificate; it is rejected
only because it does not populate the desired complement and mixed blocks.

### 8.2 Fresh attack - cycle-creating stationary restriction

This attack does not appear in V001's battery.  It targets the operation
ledger's claim that finite restrictions commute with the stationary solve.

Take a rank-increasing stage `N<=M` in an orthogonal chart, with

```text
k_M=(k_N,q),
s_N=||k_N||^2,
s_M=s_N+||q||^2,
q!=0.
```

Let the complement be one-dimensional, let its restriction be the identity,
and choose the admissible local base model

```text
Gamma_base(c)=(1/2)(c-b)^2,
b nu !=0.
```

Both complement Hessians are invertible wherever their displayed
denominators are nonzero.  The proposed stationary equations give

```text
c_star,N=b/[1+nu f(s_N)],
c_star,M=b/[1+nu f(s_M)].                         (G7-1)
```

For `s>0`,

```text
f'(s)=exp(-1/s)/s^2>0.
```

Therefore `s_M>s_N` implies `f(s_M)!=f(s_N)`, and generically

```text
rho_C,N(c_star,M) != c_star,N.                    (G7-2)
```

The R2 vertical cocycle honestly records the difference between the two
action coordinates.  It does not make their critical points commute with
restriction.  R5's existing restriction theorem transports generated
Hessians and inverses on a supplied compatible package; it does not prove
that the new radial critical family is compatible.  V001 supplies no
additional correction or theorem.

This refutes the unqualified operation-ledger statement as a consequence of
the premises V001 lists:

```text
stationary solve ... finite restrictions commute on declared branch.
```

The model does not prove that the program's still-unevaluated physical base
has this exact failure; it proves that R5 local invertibility plus the radial
formula does not entail the asserted restriction theorem.  The actual
R001 stationary restriction certificate is therefore TYPE-U, while the
claimed implication is TYPE-R.

The repair must either narrow the stationary claim to fixed-stage germs,
add and prove a stage-compatible stationary transport, or change the radial
generation rule.  None is a bounded citation repair.

```text
CHOICE_TABLE_COMPLETE = false
OMITTED_AUTHORED_FIELDS = carrier metrics | carrier units | divergence map
FRESH_ATTACK = cycle_creating_stationary_restriction
STATIONARY_RESTRICTION_FROM_LISTED_PREMISES = false | TYPE-R
ACTUAL_R001_STATIONARY_RESTRICTION = TYPE-U | missing certificate
G7 = KILL
```

---

## 9. Final adjudication

```text
G1_NO_DERIVED_STATUS_SMUGGLING = PASS
G2_TARGET_TUNING = KILL
G3_INSTANTIATED_CERTIFICATES = KILL
G4_FINITE_CONSISTENCY = PASS
G5_SCHUR_SUBSTITUTIONS = PASS
G6_ZERO_DEGENERATION = PASS
G7_MINIMALITY_AND_FRESH_ATTACK = KILL

INSTANTIATION = NOT_READY | G2,G3,G7
READY_FOR_DOR018_RULING = no

REPAIR_SURFACE = remove response-defined J3 justification and re-author from
  independently sourced divergence content; disclose/ratify carrier metrics
  and units; prove or correctly narrow cycle-creating stationary restriction

VOID_CANDIDATE = true | proposal-level only
FENCE_BLOCKED_STRUCTURAL_RESULT = false

P_VERDICT_COMPUTED = false
NUMERIC_RESPONSE_VALUE_COMPUTED = false
NU_EVALUATED = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

No alpha, `K_*`, physical root, numerical response value, rank ratio, or
measured constant was evaluated.  No p verdict was issued.  No register,
plan, tracker, git, commit, or push action was performed.
