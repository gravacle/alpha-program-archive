CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY
PREDECLARATION_OUTPUT_SCAN = 0 hits
CLOSURE_END_BYTE = 00003157
VERDICT_BEARING_SET = exactly the 12 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2
ROLE = OPPOSITE-LANE CHECK OF THE SURFACE-NECESSITY HUNT (relay 1082)
JOINT_ANCHOR_DERIVED = false
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1082_SURFACE_NECESSITY_CHECK_CODEX2_V001.md` | `de42d737f82f6f83a45b2a7c1c58f3cb476018afd122b40a21953dfc5ded2ac5` | assignment |
| 02 | `STAGE8_DESC_SURFACE_NECESSITY_HUNT_DARIO_V001.md` | `6bbebedcca8e1be5e21b21f319aeffe3816380330d800838ef5e9463f0faa95b` | hunt under attack |
| 03 | `gen_stage8_desc_surface_necessity_hunt_dario_v001.py` | `b7e9039a80bf5307cfa780187d3371a7b6a3f021e9a61ac96967ae753908018b` | sealed generator; inspected, not executed |
| 04 | `STAGE8_DESC_NECESSITY_PROBE_DARIO_V001.md` | `cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577` | byte-untouched predecessor probe |
| 05 | `STAGE8_MEASURE_V002_DESCENT_CHECK_CODEX2_V001.md` | `6a3297513f9a1d3981cf85553062f2b1137584d76c2a2d8bdd66a9e62b3cc15c` | controlling 1080 void |
| 06 | `MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | measure mandate and typed object clause |
| 07 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | state port and common-origin certificate |
| 08 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | dynamics port and mixed observable |
| 09 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | placement square and SM-1--SM-8 |
| 10 | `STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874` | derivational provenance definition |
| 11 | `STAGE8_DESC_MEASURE_HUNT_DARIO_V001.md` | `03dcaa17c78922f90bb905cd1ea7d9e9673514b74a229f0e6808a7a1dad1edd8` | exact same-marginal witness ingredients |
| 12 | `STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | `B iso C(Y)` and state-space typing |

All twelve members and adjacent seals rehashed at the displayed paths before semantic work and at
the pre-seal pin check. Strict and stable reads agreed. No undeclared source has verdict weight.

```text
BLIND HELD. EVERY SCALE SYMBOLIC. PE-1..17 POINTER-ONLY.
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
NO MEMBER BOUND. NO FIXED-POINT EXECUTION. NO END TEST. NO FREEZE.
NO NUMERIC EVALUATION OF A PHYSICAL QUANTITY. NO MEASURED-CONSTANT COMPARISON.
omega_phys IS THE SUBJECT; IT IS NOT ENTERED, ADOPTED, OR EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — SURFACE-NECESSITY HUNT — OPPOSITE-LANE CHECK

Date: 2026-08-12  
Lane: Codex 2  
Status: **CORRECTED — JOINT READ-OFF STANDS; THE SUPPLY/SM-6 CONTRADICTION AND FORK DO NOT**

## 1. Boundary and result

The assignment, hunt, generator, predecessor, 1080 ruling, and all load-bearing source files passed
their seals before reading. The output name was absent in the cleanroom and archive workspace. This
check authors no measure, joint state, producer, certificate, or entry.

The hunt has two independent hinges. Its first is sound: the dynamics exports a mixed
source/history observable and the state port separately consumes a density on the joint carrier, so
a history marginal alone is insufficient. Its second inference is not sound: the mandate does not
authorize a bare object in opposition to common-origin provenance. It mandates a measure **with** a
common-origin descent map and a provenance certificate and explicitly permits a disclosed authored
core. An authored, target-independent producer or trace extension can be proposed and later ratified;
its output may truthfully descend from that frozen trace. The present corpus has no such producer,
but absence is not logical contradiction.

The correction removes the hunt's claimed principal fork. The lawful present requirement is joint:
build a mandate-compliant, SM-6-conformant producer. Relaxing SM-6 is not “close to the mandate,”
because the mandate itself retains the descent and provenance fields.

## 2. Hinge A — the read-off genuinely consumes the joint state

### 2.1 The port types

Member 07 bytes `[10824,11934)`, SHA-256
`4313fb92fd78f6b26a5eba216e7ae7540f6b0698dc82eddd98236ceee5d7cf1b`, define

```text
omega_phys : B -> C,
pi_omega : A_C0 -> B(H_omega),
rho_pre in trace-class(H_omega),
rho_pre >= 0,
Tr_omega(rho_pre)=1.
```

`omega_phys` constructs the scalarized carrier and its representation, but `rho_pre` is a separate
input field. Member 10 bytes `[6923,8532)`, SHA-256
`c522dab9f57f152a6a2df34c2635924eea8f7759267baa929b3ba818539d12dc`, say the P5 functionals
consume `rho_pre`, `E_r`, and `U_BR`; they do not derive them from one microscopic source. The
placement square in member 09 is

```text
Omega_C0(a) := Tr(rho_pre pi_omega(a)),
omega_phys(b) = Omega_C0(i_B(b)).
```

Thus `omega_phys` is the history restriction of the joint state, not a replacement for it.

### 2.2 The mixed observable

Member 08 bytes `[12501,13326)`, SHA-256
`af7c1cdda2417e0cc25ee923ed361f5a7d3aed81363008977e65973463030b89`, place `F_N` in the finite
dynamics port. Its equation (4), bytes `[5939,6270)`, SHA-256
`c96e0be784481915cdd6790a5028dfdb4e2a58d0e0dd760cc9429e89e1a9b600`, is

```text
F_N = P_0 tensor 1 + P_ch tensor Z_N.
```

The record contraction leaves a source operator, while `Z_N` is a nonconstant history function in
`B=C(Y)`. Hence `F_N` lies in the mixed algebra `A_src tensor B`, not in either marginal subalgebra.
For a joint state,

```text
Omega_C0(F_N)
  = Omega_C0(P_0 tensor 1)
    + Omega_C0(P_ch tensor Z_N).
```

The second term is not fixed by the two marginals.

### 2.3 The attempted Y-only construction is a product assumption

A history state `mu` does define the slice

```text
(id_src tensor mu)(F_N) = P_0 + mu(Z_N) P_ch.
```

Applying a source state then evaluates `F_N`, but the resulting functional is precisely
`omega_src tensor mu`. It is a product joint state. It is therefore one admissible evaluation after
a joint-state choice, not a Y-marginal-only recovery of an arbitrary joint expectation.

Nonrecoverability can be seen without inspecting output. Member 11 bytes `[12197,13783)`, SHA-256
`57aacdc1d125eebf337da74962ce3c6ae12bad7d9f87e9b03bc96a5636ebc659`, supply invariant history
states on which `Z_N` has expectations `+1` and `-1`. Let the abstract source marginal assign a
symbolic weight `lambda` strictly between its two nonzero sectors. Couple the charged sector to the
`+1` history state and the neutral sector to the `-1` history state; compare that correlated state
with the product of its source and history marginals. The two states have identical marginals, while
their values on `P_ch tensor Z_N` differ by the nonzero symbolic term
`2 lambda (1-lambda)`. This is a structural countermodel, not a physical parameter evaluation.

No sealed reduction identifies those two extensions. Therefore a state on `Y` alone cannot evaluate
the port's general mixed observable. The hunt's constructive/output distinction is also correct:
the joint input is necessary to instantiate the read-off, while whether correlation changes a later
number remains outside this check.

## 3. The 1077 correction

The predecessor's lines 89--111, bytes `[6758,7927)`, SHA-256
`eaed1a82e9fe56404ed8e70f2a526ac620ddf3343560cbf0e38d57d72c59a27f`, correctly identify
`omega_phys` as the port's first field and `H_omega` as built from it, but overstate that three later
fields are defined from it. The port syntax contains no definition of `rho_pre` from `omega_phys`;
it only places `rho_pre` in `trace-class(H_omega)` and constrains it.

The hunt's withdrawal is therefore sound. `CONSUMES-FULL-MEASURE` remains true because the carrier
construction consumes the complete functional. No prior downstream disposition changes. The
correction is applied consistently to Hinge A: it strengthens the need for a separate joint density.

One interest statement in the hunt is backwards. Withdrawing “rho_pre is built from omega_phys”
does not point toward the Y-marginal answer; it removes the only asserted determination of the joint
density by the marginal and therefore points toward the joint-state answer, which validates the
author's prior descent work. This framing error does not invalidate the correction or the separate
Y-only attack.

The predecessor itself remains byte-identical at
`cc3ce8ca29736ede2b5d47e76bca34955c2da31c86147e8cd279fde4a0530577`.

## 4. SM-1--SM-8 as a system

Member 09 bytes `[17865,20008)`, SHA-256
`aa04e0ea2c901d0a33312a23d6ade023f8c1e92f2e90d1ff6b447f4e98cea171`, support the hunt's basic
partition:

| Rows | Receiver |
|---|---|
| SM-1 | producer source/codomain typing |
| SM-2 | joint object positivity and normalization |
| SM-3/SM-4 | record and history marginals |
| SM-5 | finite-history/quotient compatibility on the history restriction |
| SM-6 | producer's common primitive provenance |
| SM-7 | producer generation/admission before downstream outputs |
| SM-8 | producer-family coverage or bounded reopening statement |

No row fixes the value of the correlation term. SM-6 is a predicate on the producer/package and its
provenance, not on a bare measure considered in isolation. A bare measure therefore cannot by itself
satisfy or violate SM-6. A producer package whose output includes that measure can satisfy or violate
it. This qualification is load-bearing: “SM-6 does not range over a bare measure” does not imply
that a mandate-supplied producer package is disallowed.

The result depends only on the typed system and the mixed observable. It does not use V002's refuted
positivity universal and does not reopen member 05's void.

## 5. Hinge B — from-trace provenance is required, but no contradiction follows

### 5.1 The provenance field is from-trace

The field names in member 07 are not free-form labels. Its lines 524--548, bytes `[20236,21178)`,
SHA-256 `cd174351bcf2c4b3c464eee8fd841585fc4f3497a15ef986b841ee013d7af8f7`, bind the state port to the
`P5_COMMON_ORIGIN_DESCENT_PRESENTATION`: executable state/effect/domain/dynamics descent maps, one
construction trace, certificates, and a target-independent admissibility manifest.

Member 10 lines 60--125, bytes `[2622,5894)`, SHA-256
`e017e9da18d5476171fd0e26f4496b3fb54efbf8ea006b419e4ecda375d512c2`, make the receiver exact:

```text
d_state(Omega) = rho_pre,
d_effect(Omega,r) = E_r,
d_domain(Omega) = domains,
d_dynamics(Omega) = U_BR or S_CTP,
```

with one common construction record, no independently imported post hoc parameter, and admission
frozen before downstream information. A certificate saying only “authored under the mandate” is not
sufficient. `HINGE_B_PROVENANCE_FIELD=FROM-TRACE`.

### 5.2 The mandate asks for the same thing

Member 06 bytes `[1470,2433)`, SHA-256
`758023b00d6bfebf0d518036370dd625a26d206acd1c17813b8e0eac576c4c97`, do not authorize a bare
measure. They type the supplied object as a U1-invariant probability measure **with**

```text
a descent map from common-origin/outgoing-record state data to State(C(Y)),
and a provenance certificate per the demand map.
```

The same span requires every ingredient to be labeled forced or authored and expressly permits a
minimal authored core. Therefore the two sealed demands are aligned:

```text
MANDATE: propose an object plus its common-origin descent and provenance.
SM-6: accept the package only if its producer genuinely supplies those fields from one frozen trace.
```

An independently selected measure accompanied only by an authorship receipt would fail both. But an
authored, target-independent microscopic rule or trace extension can be frozen before output, and a
measure or joint state can then be derived from it. The authorship belongs to the new rule or
primitive; the output's from-trace provenance remains truthful. Principal ratification would still
be required, and no such producer is present now. Logical compatibility does not claim completion.

The hunt's sentence “an authored object cannot carry from-trace provenance” conflates authorship of
a post hoc output with authorship of the producer or its premises. The sealed mandate explicitly
keeps those classes separate through its forced/authored ingredient ledger. Consequently
`SUPPLY_AND_SM6=REFUTED`: they are jointly demanding, not contradictory.

## 6. The offered fork is misframed

The hunt's lines 340--380, bytes `[18261,20445)`, SHA-256
`323f59ea4bca317686d346f7f49092519887d8317d0c4a913ad9cddc9499b6c9`, offer:

1. retain the port fields and build from trace; or
2. relax SM-6 and “close to the mandate” with disclosed authored provenance.

The second is not mandate-compliant: member 06 itself demands a common-origin descent map and the
demand-map provenance certificate. The first does not conflict with the mandate; it is how the
mandate must be fulfilled. There is no present choice between believing the port and believing the
mandate.

The missing construction surface is a mandate-compliant authored trace extension or producer whose
joint output passes SM-1--SM-8. This may require new physics and principal ratification, and its
feasibility is not established here. It is nevertheless a logically available class distinct from
both “bare supplied measure” and “relax SM-6.” Thus the claimed exhaustive/mutually-exclusive fork
does not stand. On the current law, the lawful direction is the combined requirement, not a fork:

```text
BUILD A TARGET-INDEPENDENT COMMON-ORIGIN PRODUCER UNDER THE MANDATE;
DO NOT RELAX SM-6; DO NOT CALL AUTHORSHIP ALONE PROVENANCE.
```

Declining to invent the producer remains correct. Declining because two principal acts conflict is
not.

## 7. Self-favour guard and scope

The guard is materially real. Hunt lines 83--96, bytes `[6496,7302)`, SHA-256
`95f74ccb34e8fcbd85e4962861b22ae201c54a65f10b1900686903de4936f806`, disclose the author's
interest before the ruling. Its lines 183--263, bytes `[10857,14472)`, SHA-256
`ea628bade5b899b5d17010856c483aba0d309c58a3e4c9ebaaf616fb53b0574a`, construct the strongest
Y-marginal reading, identify its product assumption, and preserve the constructive/output boundary.
Those are failure-capable checks, not decorations.

The guard does not save Hinge B: the contradiction section does not attack the mandate's explicit
descent-map requirement or distinguish authorship of a producer from authorship of its detached
output. The incorrect interest-direction sentence about the 1077 correction is recorded in section
3 above. The guard is therefore real but incomplete, which is consistent with a corrected verdict.

## 8. Mechanical and custody replay

- All 15 declared hunt members rehashed at their full paths and adjacent seals; `15/15`
  `STRICT==STABLE`.
- All seven pinned spans rehashed from the named member bytes; `7/7` matched.
- All ten distinct positional member citations in the hunt body resolve into its closure.
- The hunt has 42 fence markers and final depth zero.
- Its closure begins at byte zero and ends marker-exclusively at byte `00006046`, matching the
  fixed-point declaration.
- The authoring gate is clean: no ledger, selection disclosure, candidate header, entry header, or
  certificate-shape apparatus appears; `AUTHORS NOTHING` is declared.
- Independent stripped-code inspection of member 03 found no random/sampling path, numeric library,
  complex-math path, tolerance, or floating literal. The generator was not executed.
- A 27-token residue scan of operative hunt prose returned zero hits. Ports were read only for their
  input contracts. No downstream return, score, observational datum, alpha, `kappa_record`, or
  battery result was opened.
- Every full digest in this check resolves to a declared member or an explicitly recomputed span.
  Strict/stable rereads agree.

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the 12 closed members; the state and dynamics port interfaces; F_N; the placement square;
  SM-1..SM-8; the P5 derivational-provenance definition; the mandate's typed object clause;
  the 1080 void and every charter fence.

DERIVED HERE:
  the failure of the Y-only slice to recover an arbitrary joint expectation;
  an exact same-marginal/different-mixed-term countermodel;
  confirmation of the 1077 correction and its actual interest direction;
  the SM-system partition with the bare-measure qualification;
  the from-trace meaning of the provenance field;
  refutation of the asserted supply/SM-6 contradiction and the two-prong fork;
  the digest, span, citation, fence, authoring, numeric-path, residue, and verb audits.

AUTHORED OR SELECTED HERE:
  nothing. The countermodel is a structural witness, not a candidate. No measure, joint state,
  producer, trace extension, certificate, member, selector, entry, or physical value is adopted.

NOT CONSUMED:
  kappa_record, alpha, battery output, downstream score, observational datum, member selection,
  fixed point, end test, entry addendum, or freeze.
```

## 10. FLATTENING CHECK AND VERB AUDIT

- A mixed observable outside both marginal subalgebras was not flattened into a claim that a later
  output numerically depends on correlation.
- A product-state slice was not flattened into recovery from a Y marginal alone.
- A separate joint-density field was not flattened into an object derived by the scalarization
  functional.
- A producer provenance condition was not flattened into a predicate on a bare measure.
- Authorship of a target-independent producer was not flattened into post hoc authorship of its
  output, and conditional derivation under authored premises was not flattened into prior forcing.
- Current absence of a producer was not flattened into contradiction or impossibility.
- The mandate's required descent fields were not flattened into optional fields that a principal
  must waive to honor the mandate.
- The 1080 void was not reopened; no positivity conclusion is used here.

Verb audit: **CLEAN.** “Consumes” is confined to port inputs; “confirmed” is scoped to constructive
joint-state necessity and the producer nature of SM-6; “refuted” is confined to the claimed logical
contradiction; no producer, descent, member, output, or physical value is claimed constructed.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false

PREDECESSOR_PROBE = BYTE-UNTOUCHED (cc3ce8ca…)
READOFF_CONSUMES = JOINT-STATE-CONFIRMED
HINGE_B_PROVENANCE_FIELD = FROM-TRACE (field confirmed; contradiction inference refuted)
SUPPLY_AND_SM6 = REFUTED
CORRECTION_1077 = SOUND (interest direction corrected: it supports JOINT-STATE)
SM6_PARTITION = PRODUCER-CONDITION-CONFIRMED (bare measure alone is not the package)
FORK = NOT-EXHAUSTIVE/MISFRAMED (mandate-compliant authored trace-extension/producer; SM-6 retained)
GUARD = REAL (Hinge-A attack and scope are failure-capable; Hinge-B gap recorded)
AUTHORING_GATE = CLEAN
PROSE_DIGESTS = 15/15 STRICT==STABLE
OUTPUT_INSPECTION_AUDIT = CLEAN
RESIDUE_GREP = CLEAN (27 own patterns; 0 operative hits)
VERDICT = CORRECTED (joint read-off stands; supply/SM-6 contradiction and principal fork do not)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
