# STAGE 8 — REQUIRE-BUILD-CLUSTER: THE CONNECTION-ONLY CLUSTER-SUMMABILITY SUB-ESTIMATE

## BLIND BUILDER — REQUIRE-BUILD-CLUSTER — [CLAIMED]

Date: 2026-08-13
Role: BLIND BUILDER. ATTEMPT the program's single primary gap ("the short one
power"): certify, CONNECTION-ONLY and SCALE-FREE, that the post-thermodynamic-limit
Gate-5 connected cluster covariance kernel `C` of the transported per-cell write
generators EXISTS as an intensive object — i.e. discharge V011's unbuilt connected
linked-cluster density / cellulation-independent thermodynamic limit. Honest outcomes
only: PROVED / FAILS-AT(step, why) / REFORMULATED(what remains). No value of `n`,
`kappa`, `alpha`, any exponent, coupling, norm, scale, length, or spectrum is computed,
bounded, estimated, or compared. Every numeric string is a sealed-text quotation or an
exact structural integer.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false` ; `coupling_evaluation_authorized = false`

Fences held throughout: CONNECTION-ONLY, SCALE-FREE, symbolic. NO scale used to
certify (no `ell_P`, `K_KK`, `c^2`, metric length, `G_4`, Lorentzian causal diamond,
`|D|_3`/`|D|_4` volumes, sea-covariance-on-diamonds as authority, spatial Schatten-2
as certifier). Scale-bearing sources were read ONLY to CLASSIFY them and to LOCATE the
obstruction — a negative/fencing use, never a positive certifier; every such object is
flagged at first entry (§4). NO imported GR, NO faithfulness premise. No register/
tracker/plan/road/ledger/lens read. No git action.

---

## 0. VERDICT IN ONE LINE

**FAILS-AT the multi-cell operator-norm clustering / activity bound. The operator-norm
reformulation genuinely DISSOLVES the vacuous-Schatten-2 obstruction for the per-cell /
finite-range piece (the write generator is a bounded unit-modulus-character derivative;
the sea kernel-as-operator is Calderón-Zygmund L^2-bounded), and the whole intensivity
question reduces — connection-only, scale-free — to ONE load-bearing hypothesis: a
geometric operator-norm activity bound `|Phi_gamma| <= eta^|gamma|` on the combinatorial
cell-adjacency graph, equivalently uniform (geometric) clustering of the prepared state.
That hypothesis is NOT dischargeable connection-only: geometric clustering is equivalent
to a spectral gap, and the sealed prepared state is the GAPLESS / MASSLESS sea (its
covariance is homogeneous degree -3 — a pure power law, the fingerprint of masslessness),
whose clustering is at best algebraic. Worse, the operator-norm two-point modulus is
ITSELF degree -3 (`||C_off||_op = 1/(2 pi^2 |r|^3)`), so switching Schatten-2 -> operator-
norm removes the per-cell infinity but leaves the large-separation decay a power law; and
the very DECAY EXPONENT in combinatorial cell-separation is not even statable without a
scale (the cell-to-metric conversion — the fenced node). At the sole sealed realization
the aggregate is one power short of summability (the area law), and the deciding
difference-cancellation is UNKNOWN of record and is a computation on the metric kernel.
This is the SCALE-ENTANGLED / running-`kappa_record` branch (an RG/critical-marginal
object). It is NOT a proof of impossibility: the abstract combinatorial summability
predicate remains scale-free, so a connection-only algebraic bound of sufficient power is
not excluded — it is simply UNBUILT and un-inputtable of record, and the only realized
decay is metric and marginal.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Full digests recomputed
by `shasum -a 256` at path this relay BEFORE reading; first 16 hex shown. CONN =
connection-only/scale-free of record; SCALE = scale/GR-bearing (read only to classify).

| Tag | Source | Seal (first 16) | Type | Role in this attempt |
|---|---|---|---|---|
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b` MATCHES-TASKED | CONN | `C`, `C_jk = mu` (all-pairs, rank-one) at finite N; `R = n^2 Phi^T C Phi`; the typing of `C` |
| G1B | `STAGE8_REQUIRE_BUILD_G1_MATERIALITY_V001.md` | `b28c08130887b361` MATCHES-TASKED | CONN | names the target + obstructions; the "short one power" |
| G1C | `STAGE8_REQUIRE_G1_CHECK_V001.md` | `2846272b79974568` MATCHES-TASKED | CONN | verifier: summability is a scale-free predicate; impossibility not proven |
| V011 | `review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md` | `aa7c6d4904706276` SEALED-OK | CONN | the Gate-5 obligation; Duhamel-reproduction; the false status flags |
| MON | `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md` | `451550c382528869` SEALED-OK | CONN | THE frozen derivation V011 cites: the cumulant expansion + the five closure conditions |
| ECO | `STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md` | `0f3082cab910f2eb` SEALED-OK | SCALE | the degree -3 sea covariance; `||C_off||_op`; HS-infinity; running-`kappa` fallback |
| RFA | `STAGE8_RL2B_FRAME_ANSWER_AND_CAMPAIGN_TARGET_V001.md` | `2ede02aea415157a` SEALED-OK | SCALE | `alpha>=1` iff full cancellation; the area vs volume law; "exponent needs a single scale" |

Seven seals recomputed at path; all match (the three tasked digests exactly; ECO/RFA
against the digests G1B/G1C independently verify; MON's seal is recorded here for the
record). CONN/SCALE typing recomputed independently below, not inherited.

Typing check, independent: FORM/G1B/G1C/V011/MON carry only connection/holonomy-indexed,
count-intensive, `n`-independent, combinatorial content — CONN. ECO/RFA carry the sea
covariance `C(r)` with an explicit length `|r|`, the coupling `alpha`, Lorentzian causal
diamonds `D`, `|D|_3`/`|D|_4`, the scale `L`, the spatial Schatten-2 norm, and the
`alpha>=1` exponent — SCALE. Confirmed at the bytes (ECO :150-171; RFA :31, :51-77,
:98-140).

---

## 2. THE TARGET, RESTATED CONNECTION-ONLY, AND THE EXACT REDUCTION

### 2.1 The object and what "exists as an intensive object" means

From the FORM (D5 :234-265): after the record sandwich and any scalarization, the
transported linear-response operator is `V_L(a) = n · sum_j <ell_j,a> X_j`, with `X_j`
the FIXED, `n`-independent per-cell write generators, and the sealed Duhamel constraint
(V011 :1299-1300) requires the covariance of the transported `G_L` to reproduce the
Hessian, giving

```text
R_record,L(a,b) = n^2 · sum_(j,k) C_jk <ell_j,a><ell_k,b>,
C_jk = the intensive post-limit Duhamel/connected covariance of X_j, X_k in the base state.
```

`C` "exists as an intensive object" iff the post-limit `C_jk` is well-defined per `N_4`
(per 4-cell), cellulation-independent, with subextensive boundary corrections — i.e. the
double sum, divided by the cell count, converges as the cellulation exhausts the complex.
Concretely this is SUMMABILITY of `C_jk` over cell-separation (translation-averaging the
`N_4 x N_4` double sum leaves a single sum over relative combinatorial separation).

### 2.2 The finite-N object is the wrong (non-clustering) object — by construction

FORM D4/D5: the coherent finite-N kernel is `C_jk^(finite) = mu` for ALL pairs (j,k) —
an ALL-PAIRS, separation-INDEPENDENT, rank-one weight (`n^2 mu Phi_tot ⊗ Phi_tot`). This
is maximally NON-clustering; its per-separation sum is extensive, not intensive. So the
finite-N object cannot be the post-limit `C`; the thermodynamic/linked-cluster limit MUST
replace the all-pairs `mu` by a cluster-DECAYING `C_jk`. The task is exactly to certify
that a decaying `C_jk`, summing to an intensive density, exists — connection-only.

### 2.3 The exact connection-only reduction (MON Theorem 3 = an abstract cluster expansion)

The frozen derivation `BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001` (which V011 :1279-1281
cites as the seat of the obligation) states the mechanism verbatim. With
`B_K(A) = sum_(X subset K) b_X(A)` the local finite-range generator on the tensor-product
record carrier,

```text
MON :92-97:  Log A_K(A) = sum_(n>=1) (-i tau_R)^n/n! <B_K(A)^n>_connected .   (cumulant/linked-cluster series)
MON :99-108: "A volume-density limit follows only if V011 proves:
   [c1] uniform finite-range/bounded local terms;
   [c2] uniform preparation clustering;
   [c3] a volume-independent open neighborhood of A=0 in which A_K(A) is nonzero;
   [c4] uniform absolute convergence of the differentiated connected-cluster sum;
   [c5] and subextensive boundary corrections across the sealed refinement class."
MON :112-118: "Under those conditions, gamma(A) = lim -Log|A_K(A)|/N_4(K) exists ...
   If a zero of A_K approaches the origin, the cumulant series fails uniformly, or the
   limit depends on cellulation, the connected response gate fails."
```

**Structural recognition (mine, connection-only).** Conditions [c1]-[c5] are EXACTLY the
hypotheses of an abstract polymer / linked-cluster convergence theorem (Kotecký-Preiss /
Mayer type) on the COMBINATORIAL cell-adjacency graph. In that abstract form: polymers
`gamma` = connected subsets of cells; activity `Phi_gamma` = the connected cumulant
supported on `gamma`; and IF there is a geometric activity bound

```text
(KP)   |Phi_gamma| <= eta^|gamma|   with eta below the graph's connective threshold
       (eta < 1/branching, where "branching" is the combinatorial coordination of the
        cell-adjacency graph — a purely combinatorial constant),
```

THEN the series converges absolutely and uniformly in the cellulation, `gamma_density`
exists, and boundary corrections are subextensive. This implication is SCALE-FREE: it
needs the cell-adjacency graph (an incidence datum) and the activity values, and NO metric
length. This is the connection-only skeleton the task asks for, and it exactly matches the
task's named sub-estimate ("a refinement-invariant per-cell operator-norm activity bound
`|Phi_gamma| <= eta^|gamma|` on a combinatorial cell-adjacency connectivity ... summing to
a volume law (alpha=1)"). So the WHOLE Gate-5 intensivity reduces, connection-only, to
discharging (KP) + [c3] (the zero-free neighborhood).

**This reduction is the REFORMULATED content** (see §3.1, §3.2). What remains is (KP). The
rest of §3 attempts (KP) and locates the exact failure.

---

## 3. THE ATTEMPT — STEP BY STEP

### 3.1 [c1] the per-cell / finite-range operator-norm activity: PROVED connection-only, and it DISSOLVES the vacuous-Schatten-2 obstruction

The first obstruction is that the per-cell spatial Schatten-2 / HS norm is claimed
vacuous/infinite (`||X||_2 = infinity`). Confronted head-on:

- CONNECTION-ONLY: the per-cell write generator is `X_j = d/ds z_j^(n)[s a_j]|_0`, and
  `z_j^(n) = chi_n(h_j[a_j])` is a UNIT-MODULUS holonomy character (FORM B6/W). A unit-
  modulus character has a BOUNDED self-adjoint generator; the "exactly one character power"
  clause (FORM B6) gives a single bounded generator direction per cell. Hence
  `||X_j||_op < infinity` per cell, connection-only — the SINGLE-cell (`|gamma|=1`)
  operator-norm activity is finite WITHOUT any metric. [c1]'s "bounded local terms" holds.
- The finite-RANGE part of [c1] holds connection-only too: the transported generator
  `G_L(a) = int_0^(tau_R) e^{iB_L(0)t} V_L(a) e^{-iB_L(0)t} dt` is a fixed-time (`tau_R`)
  Heisenberg integral, whose operator-norm spread is controlled by a Lieb-Robinson /
  finite-propagation bound on the cell-adjacency graph — a combinatorial, gap-INDEPENDENT
  locality. So the transported per-cell term has uniformly bounded combinatorial range.

- FLAG (scale, read-to-classify): the SCALE route independently confirms the per-cell
  boundedness and, crucially, EXPLAINS the Schatten-2 infinity as an artifact of the wrong
  norm: ECO :168-171 "|| 1_D C 1_D ||_2 = INFINITY, ON EVERY DIAMOND, AT EVERY SCALE. ...
  a Calderón-Zygmund kernel of degree -3 in three dimensions is **L^2-BOUNDED BUT NOT
  HILBERT-SCHMIDT**." I do NOT import this as a certifier; it is used only to name the
  obstruction as a Schatten-2 artifact. The connection-only bound above stands on its own.

**Result: the vacuous-Schatten-2 obstruction is DISSOLVED in the operator-norm
formulation.** The kernel-as-operator (and each per-cell generator) is operator-norm
bounded; the infinity was purchased by the Schatten-2 choice, a spatial/metric certifier
the fences forbid anyway. [c1] is discharged connection-only.

### 3.2 The reduction to (KP) + [c3], connection-only and scale-free: REFORMULATED

Given [c1] (§3.1), the abstract cluster-expansion conditional of §2.3 reduces the entire
existence-of-`C` question to two combinatorial predicates on the cell-adjacency graph:
(KP) the geometric multi-cell activity bound, and [c3] the volume-uniform zero-free
neighborhood (V011 :2184 `volume_uniform_zero_free_neighborhood_proved = false` — also
unbuilt, and downstream of clustering). Both are scale-free predicates. **This confirms,
constructively, the verifier's structural finding (G1C §2): intensivity is combinatorial
summability, a scale-free predicate — NOT intrinsically a metric statement.** The metric
does not enter the SKELETON. It enters only when one tries to SUPPLY the activity values —
which is §3.3-§3.5, where the attempt fails.

### 3.3 (KP) geometric activity requires a gap; the prepared state is gapless: FAILS-AT

(KP) demands `|Phi_gamma| <= eta^|gamma|` — GEOMETRIC decay of the connected cumulant in
the NUMBER of cells spanned. The connected cumulant supported on a cluster spanning cells
is, up to the bounded local factors of [c1], the connected CORRELATION of the prepared
(base record) state across those cells. Geometric decay of connected correlations in
cluster size is precisely UNIFORM (exponential) CLUSTERING of the state — condition [c2].

The decisive fact: **uniform/exponential clustering is equivalent to a spectral gap**;
a state with a gapless (massless) spectrum does NOT cluster exponentially — its connected
correlations decay only ALGEBRAICALLY (as a power law). And the sealed prepared state IS
gapless/massless:

- CONNECTION-ONLY, the prepared state's cross-cell correlation DECAY is not fixed by any
  ratified object. The disjoint monoidal additivity that IS proved connection-only (MON
  Thm 1; FORM P2) gives EXACT factorization for STRICTLY DISJOINT independent components —
  i.e. zero correlation at infinite separation — but says NOTHING about the DECAY RATE for
  adjacent/connected cells. That rate is exactly the connected-cluster content MON Thm 3
  leaves open. So connection-only there is no geometric-clustering datum to invoke.
- FLAG (scale, read-to-classify): the ONLY sealed realization of that decay is the sea
  covariance, whose off-diagonal modulus is `||C_off(r)||_op = 1/(2 pi^2 |r|^3)`,
  HOMOGENEOUS DEGREE -3 (ECO :154-155) — a PURE POWER LAW, which is the fingerprint of a
  gapless/massless spectrum (a gapful kernel would decay exponentially, not as a pure
  power). This realization has NO geometric decay to offer.

Therefore (KP) — the geometric operator-norm activity bound — is NOT dischargeable
connection-only: the connection-only content supplies no gap and no geometric clustering,
and the sole sealed realization is manifestly gapless. **This is the exact step that
breaks.** Note the operator-norm switch does NOT help here: it dissolves the per-cell
infinity (§3.1) but the operator-norm two-point modulus is ITSELF degree -3 (ECO :155),
so Schatten-2 -> operator-norm leaves the large-separation decay a power law. Gaplessness
is not a norm artifact; it survives the reformulation.

Lieb-Robinson does not rescue this: LR controls DYNAMICAL spreading of commutators
(gap-independent), which delivers [c1]'s finite range, but STATIC clustering of the state
is a spectral-gap property, not a dynamical-locality property. For a gapless state LR
holds and clustering still fails geometrically. So the strongest connection-only locality
tool cannot manufacture (KP).

### 3.4 The algebraic fallback is, at the sealed value, one power short — and its exponent needs a scale: the SCALE-ENTANGLED branch

Geometric clustering is unavailable, but summability of `C_jk` does NOT strictly require
it: an ALGEBRAIC (power-law) decay of sufficient power is summable, giving an intensive
density with only polynomially-corrected convergence. So the honest fallback question is:
does the gapless power-law decay fall off FAST ENOUGH in combinatorial cell-separation to
be summable over the complex?

Two connection-only obstructions block this, and together they define the "short one
power":

1. **The decay EXPONENT is not statable connection-only.** To convert the sole sealed
   decay (metric degree -3 in `|r|`) into a per-cell rate in COMBINATORIAL separation, one
   must identify combinatorial distance with metric length — i.e. supply the cell-to-metric
   conversion (cell size). That conversion is exactly the fenced internal/external metric
   node (XSM: `ell_P`/`G_4`/Lorentzian diamond). FLAG (scale): RFA :98-102 states this in
   the corpus's own words — "A diamond is ISOTROPIC with ONE parameter, so the cancellation
   rate is a function of a SINGLE SCALE ... On a general anisotropic atom 'the exponent'
   would NOT be a number — there would be no single scale for it to be an exponent OF."
   Connection-only, WITHOUT a scale, the combinatorial decay exponent is not a number.

2. **At the sole sealed (metric) value the aggregate is one power short — the area law.**
   FLAG (scale, read-to-classify, negative use): the raw degree -3 covariance, aggregated
   over separation, is NOT summable — it is IR-marginal/divergent (the extensive/area
   behavior). The corpus states the finiteness is "purchased ENTIRELY by cancellation in
   the difference `V(a)-V(0)` ... alpha IS SET BY THE CANCELLATION RATE, NOT BY `|D|_4`"
   (ECO :176-179), and the whole question collapses to ONE yes/no: "DOES THE DEGREE -3
   SINGULARITY OF C FULLY CANCEL IN THE DIFFERENCE?" — FULLY -> `alpha=1` (aggregation
   flat, the volume law, intensive); PARTIALLY -> `alpha<1` (aggregate divergence, the
   area/`alpha=1/2` law) (RFA :63-77, :118-131). And RFA :137-138: "**whether the
   singularity fully cancels is UNKNOWN**." The difference-cancellation supplies at most
   ONE power; even a FULL single-power cancellation lands on the borderline (a marginal /
   logarithmic aggregate — the exact `alpha=1` boundary), not comfortably inside it. This
   is the "short one power," and it sits precisely on the critical/marginal RG boundary.

The marginal-at-the-boundary character is the signature of a SCALE-DEPENDENT (running)
response rather than an intensive constant. FLAG (scale): the corpus names exactly this
fallback — ECO :221 `running_kappa_record_fallback = NAMED_NOT_PURSUED; would_be_a_NEW_
PRINCIPLE`; a scale-dependent (RG) `kappa_record`. **So the live branch here is the
SCALE-ENTANGLED / running-`kappa_record` branch: the only realized decay is a metric power
law whose combinatorial exponent needs a scale to state and which, at the sealed value, is
critically marginal (one power short, deciding cancellation unknown).**

### 3.5 [c2] the clustering premise: FREE / NEEDED, not forced by the state

Collecting §3.3-§3.4: condition [c2] (uniform preparation clustering) is a POSITED input
of the cluster expansion, not a consequence of the ratified connection-only content. The
connection-only content fixes no cross-cell decay for the prepared state at all; the only
sealed realization (the gapless sea) actively FAILS the geometric form of [c2] and does
not even fix the algebraic power connection-only. So [c2] is a FREE ASSUMPTION — needed,
un-supplied, and contradicted in its geometric form by the sole realized state.

### 3.6 Why this is FAILS-AT and not a proof of impossibility

The estimate cannot be closed connection-only (§3.3-§3.5). But it is NOT proven
impossible, and I flag this to avoid over-claiming the opposite horn:

- The abstract cluster-expansion SKELETON (§2.3, §3.2) is genuinely scale-free; the
  combinatorial summability predicate is well-posed WITHOUT a metric (G1C §2).
- What is missing is the ACTIVITY INPUT `Phi_gamma`. A hypothetical connection-only
  object that fixed the connected cumulants with a power fast enough to be summable is not
  excluded — it is simply UNBUILT and un-inputtable of record, and the only realization
  that could supply it is metric and marginal.
- The corpus itself declines the impossibility proof (ECO :161, via G1C: "absence of
  proof that they decay is not proof that they do not").

So: FAILS-AT (open), on the gapless -> metric (scale-entangled) branch; impossibility not
established.

---

## 4. SCALE OBJECTS CONFRONTED — READ TO CLASSIFY, NONE IMPORTED AS CERTIFIER

Each was used ONLY to name/locate the obstruction (a negative, fencing use); none entered
a positive certification, and no numeric value was computed from any.

```text
- degree -3 sea covariance C(r) = (1/2)delta^3(r)I - i alpha·r/(2 pi^2 |r|^4), with the
  explicit length |r| and the coupling alpha (ECO :151-155; RFA :31) — used to identify
  the sole sealed decay as a gapless power law and to locate the "one power" deficit.
- ||C_off||_op = 1/(2 pi^2 |r|^3) [operator-norm modulus, still degree -3] (ECO :155) —
  used to show Schatten-2 -> operator-norm does not remove the power-law large-separation
  decay.
- spatial Schatten-2 / HS norm and its ~eps^-3 divergence; "L^2-bounded but not Hilbert-
  Schmidt" (ECO :157-171) — used to identify the vacuous-per-cell-norm as a Schatten-2
  artifact (obstruction dissolved).
- Lorentzian causal diamond D, |D|_3 ~ L^3, |D|_4 ~ L^4, scale L (RFA :44-89 via G1B) —
  used to name the volume/area distinction; NOT used to bound anything.
- the exponent target alpha (>=1 volume/flat; =1/2 area; the aggregate A^(3(1-alpha)))
  (RFA :51-77, :118-131) — used symbolically to name volume-vs-area; NO value computed.
- the single-scale requirement for "the exponent" to be a number (RFA :98-102) — used to
  show the combinatorial decay exponent is not statable connection-only.
- running_kappa_record / RG "new principle" fallback (ECO :221) — used to name the
  scale-entangled branch.
- XSM's internal/external metric conversion (ell_P/G_4/Lorentzian diamond) — named as the
  scale content the cell-to-metric exponent conversion would require; fenced OUT.
```

No `alpha`, `kappa`, `n`, `eta`, `mu`, `C`, `beta`, volume, length, or spectrum value was
computed, bounded, estimated, or compared. The degree -3 and the cell-shell / branching
growth appear only as symbolic orders-of-growth to LOCATE the marginal/area-law
obstruction.

---

## 5. HONEST OUTCOME — SYNTHESIS

```text
WHAT IS PROVED (connection-only):
  [c1] the per-cell / finite-range operator-norm activity is bounded (bounded unit-
       modulus-character generator + Lieb-Robinson finite range). The vacuous-Schatten-2
       obstruction is DISSOLVED: it was a wrong-norm artifact.
  The reduction of the WHOLE Gate-5 intensivity to a single scale-free combinatorial
  hypothesis (KP) + zero-free [c3] on the cell-adjacency graph. The skeleton is scale-free.

WHAT FAILS (the exact step):
  (KP) the geometric operator-norm activity bound |Phi_gamma| <= eta^|gamma| — equivalently
  uniform/geometric clustering [c2] — cannot be discharged connection-only. Geometric
  clustering <=> spectral gap; the sealed prepared state is gapless/massless (its
  covariance is a pure degree -3 power law). Operator norm does not help (the two-point
  op-norm modulus is itself degree -3). Lieb-Robinson does not help (static clustering is
  a gap property, not a dynamical-locality property).

THE FALLBACK, AND WHY IT DOES NOT CLOSE:
  algebraic summability could suffice IF the power beats the cell-shell growth, but
  (i) the combinatorial decay EXPONENT is not statable connection-only (it needs the cell-
  to-metric scale — the fenced node); and (ii) at the sole sealed (metric degree -3) value
  the aggregate is one power short (the area law), and the deciding full-cancellation is
  UNKNOWN of record and is a computation on the metric kernel. Even full single-power
  cancellation is critically marginal (the alpha=1 boundary) — the running-kappa_record /
  RG branch.

THE PREMISE:
  [c2] uniform clustering is a FREE/NEEDED assumption, not forced by the state; the sole
  realized (gapless) state fails its geometric form and fixes no algebraic power.
```

**NET = FAILS-AT.** The connection-only certification of `C`'s intensive existence breaks
at the multi-cell operator-norm clustering / activity bound (KP)/[c2]: the prepared state
is gapless, so no geometric clustering exists; the only realized decay is a metric power
law whose combinatorial exponent needs a scale and which, at the sealed degree -3, is one
power short (area law) with the deciding cancellation unknown. The live branch is
SCALE-ENTANGLED / running-`kappa_record`. Impossibility is NOT proven (the abstract
combinatorial summability skeleton is scale-free; a sufficient connection-only algebraic
bound is unbuilt, not excluded). A closure would need EITHER a connection-only object that
fixes the prepared state's cross-cell connected-cumulant decay with a summable power on
the cell-adjacency graph (currently non-existent of record, and the gapless realization
supplies only a marginal power), OR the acceptance that the response is scale-dependent
(running-`kappa_record`), which is the scale-entangled horn, a NEW PRINCIPLE.

Fork note (not pursued, gated): once `C` were certified, the next object is the cross-
block reading `Phi_f^T C Phi_H`, still gated by the write-chain datum G3 and the
scalarization pair G2 (FORM §4; G1B §4). Not reached here.

---

## 6. IMPORT / MACHINERY AUDIT (MINE)

```text
No value of n, kappa, alpha, eta, mu, C, beta, or any scale/spectrum computed, bounded,
  estimated, or compared. The (KP) threshold, the shell/branching growth, and the degree
  -3 decay are used only as symbolic orders-of-growth to LOCATE the obstruction.
No scale/faithfulness/GR imported BY ME as authority. ECO/RFA (sea covariance, diamonds,
  |D|_3/|D|_4, L, Schatten-2, alpha) and XSM (ell_P/G_4/metric) were read ONLY to classify
  them scale-bearing and to name the gapless/metric/marginal obstruction — the fence
  itself. Every positive step (the operator-norm [c1] bound; the scale-free cluster-
  expansion reduction) rests on connection-only bedrock (FORM/W character structure; MON
  Thm 3; the abstract KP conditional).
No register/tracker/plan/road/ledger/lens read. Seals recomputed at path before reading.
No git action; output name probed before write: ABSENT.
```

---

## 7. FLAG BLOCK

```text
ACTIVITY_BOUND = REFORMULATED(operator-norm reformulation DISSOLVES the vacuous-Schatten-2
  obstruction and PROVES the per-cell/finite-range activity connection-only — the write
  generator is a bounded unit-modulus-character derivative (FORM B6/W), and a Lieb-
  Robinson finite-propagation bound gives uniformly bounded combinatorial range; the sea
  kernel-as-operator is Calderón-Zygmund L^2-bounded (ECO :168-171, read to classify), so
  ||X||_2=infinity was a wrong-norm artifact. The whole Gate-5 intensivity then reduces,
  scale-free, to ONE hypothesis on the cell-adjacency graph: the geometric activity bound
  |Phi_gamma| <= eta^|gamma| (KP) + the zero-free neighborhood [c3] (V011 :2184 false).
  This is the connection-only skeleton the target names. WHAT REMAINS UNBUILT AND NOT
  DISCHARGEABLE CONNECTION-ONLY: the geometric bound itself — see COVARIANCE_DECAY /
  CLUSTERING_PREMISE.)

COVARIANCE_DECAY = FORCED_METRIC_POWER_LAW(gapless => scale-entangled: geometric/uniform
  combinatorial clustering is EXCLUDED — exponential clustering <=> spectral gap, and the
  sealed prepared state is the gapless/massless sea whose off-diagonal operator-norm
  modulus is homogeneous degree -3, ||C_off||_op = 1/(2 pi^2 |r|^3) (ECO :154-155), a pure
  power law. Schatten-2 -> operator-norm removes the per-cell infinity but NOT the degree
  -3 large-separation decay; Lieb-Robinson gives dynamical range, not static clustering.
  So the only realized decay is a METRIC POWER LAW, and its combinatorial exponent is not
  even statable connection-only — converting degree -3 to a per-cell rate needs the cell-
  to-metric scale (the fenced XSM node); RFA :98-102 "there would be no single scale for it
  to be an exponent OF." At the sealed value the aggregate is ONE POWER SHORT of summability
  (the area law, alpha=1/2): finiteness is "purchased ENTIRELY by cancellation in the
  difference ... alpha set by the cancellation rate, not by |D|_4" (ECO :176-179), and
  "whether the singularity fully cancels is UNKNOWN" (RFA :137-138); even full single-power
  cancellation is critically marginal (the alpha=1 boundary) — the running-kappa_record /
  RG branch (ECO :221). SCOPE: this is the LIVE/realized branch, NOT a proof of
  impossibility — the abstract combinatorial summability predicate stays scale-free (G1C
  §2), so a sufficient connection-only algebraic bound is UNBUILT, not excluded.)

CLUSTERING_PREMISE = FREE_ASSUMPTION(uniform preparation clustering [c2] is a POSITED input
  of the cluster expansion, not forced by the ratified connection-only content. The proved
  connection-only locality — disjoint monoidal additivity (MON Thm 1; FORM P2) — gives
  exact factorization only for STRICTLY DISJOINT independent components (zero correlation
  at infinite separation) and fixes NO decay rate for adjacent/connected cells. The sole
  sealed realization (the gapless sea) FAILS the geometric form of [c2] and fixes no
  algebraic power connection-only. So [c2] is needed, un-supplied, and contradicted in its
  geometric form by the only realized state.)

NET = FAILS-AT(the connection-only certification of C's intensive existence breaks at the
  multi-cell operator-norm clustering / activity bound (KP)/[c2]. Exact step: discharging
  |Phi_gamma| <= eta^|gamma| on the cell-adjacency graph. Why: geometric clustering <=>
  spectral gap; the sealed prepared state is gapless/massless (covariance degree -3, pure
  power law); operator norm and Lieb-Robinson do not manufacture static clustering. The
  algebraic fallback is not statable connection-only (exponent needs a scale) and at the
  sole sealed metric value is one power short (area law), deciding cancellation UNKNOWN.
  Live branch: gapless -> metric = SCALE-ENTANGLED / running-kappa_record (ECO :221, a NEW
  PRINCIPLE). NOT proven impossible: the abstract cluster-expansion skeleton is scale-free
  and a sufficient connection-only algebraic activity bound is unbuilt, not excluded. A
  closure would need a connection-only object fixing the prepared state's connected-cumulant
  decay with a summable power on the cell-adjacency graph — non-existent of record, and the
  gapless realization supplies only a marginal power. Salvaged REFORMULATED byproduct: the
  operator-norm formulation dissolves the vacuous-Schatten-2 obstruction, proves [c1], and
  reduces the whole obligation to the single scale-free (KP)+[c3] combinatorial hypothesis.)

FORBIDDEN_IMPORTS = none entered the certification. Flagged, read-to-classify only (§4):
  degree -3 sea covariance C(r) with |r| and alpha; ||C_off||_op degree -3; spatial
  Schatten-2 / HS ~eps^-3; Lorentzian diamond D, |D|_3/|D|_4, scale L; the alpha exponent
  (>=1 volume / =1/2 area); the single-scale-makes-it-a-number requirement; running-
  kappa_record; XSM ell_P/G_4/metric conversion. Each used only to name/locate the
  obstruction; none as a positive certifier; no numeric value computed from any.

MACHINERY_INVOKED = no. Nothing computed, bounded, estimated, or compared; C, mu, alpha,
  eta, n, kappa, beta and every scale/spectrum left symbolic; the (KP) threshold and the
  shell/branching/degree-3 growths used only as symbolic orders-of-growth to locate the
  marginal/area-law obstruction; scale-bearing sources read only to classify-and-fence;
  seals recomputed at path before reading; no register/tracker/plan/road/ledger/lens read;
  no git action.

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel.
```
