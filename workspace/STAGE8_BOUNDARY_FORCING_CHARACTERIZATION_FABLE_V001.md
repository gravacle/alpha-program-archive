CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
VERDICT_BEARING_SET = exactly the 5 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every member rehashed at its path with `shasum -a 256` this session; all 5 MATCH the
  assignment's pinned digests exactly.
LANE = FABLE (blind constructor FABLE-BOUNDARY-1109)
ROLE_THIS_RELAY = CHARACTERIZATION — the forcing-type of the record cell's interior/exterior
  boundary, doubling as a coherence probe on the RAIL verdict (member 01).
DOCTRINE CARRIED AS GIVEN = a number is FORCED only by a UNIQUE-REQUIRE (conditional derivation
  with a single solution) or an OVERDETERMINATION (two independent constraints pinning the same
  object); a mere permission never forces.
FRAME CARRIED AS GIVEN (subject to the coherence probe, not re-established) = the RAIL verdict of
  member 01; the companion ALLOW/REQUIRE reading of the boundary (interior = the cell's own
  freedom; exterior = what the crossing obliges; the absolute scale a free orbit at the surface).
BLIND HELD. EVALUATES NOTHING. Every scale symbolic. No measured value, no constant, no imported
  length is read, used, or compared. Every numeric string in this artifact is a sealed-text
  quotation or a file digest, never a computed physical quantity.
RECORD-NATIVE FENCE = LIVE. Finite discrete record IS the surface; continuum is imported. No
  continuum/metric/KK/variational machinery is used to characterize anything. The forcing test is
  run as a fixed-point argument on the surface's own inventory — the same argument-type as the
  surface's own gauge-invariance arguments.
REGISTER = NOT READ (barred). No tracker, plan, road, continuation, or expectations file read. No
  broad grep run; every read was one of the five named members.
NO_NUMERIC_PATH = nothing is computed; no sampling, no numeric library; the only computed strings
  are file digests (custody, not physics).
ALL_RESULTS = CLAIMED until independent adversarial check.
LIVE_MEMBERS = NONE.
```

| # | Closed member (all under `/Users/bgm/MB Work/alpha-program-archive/workspace/`) | SHA-256 | Role |
|---:|---|---|---|
| 01 | `STAGE8_BETA_SURFACE_DERIVATION_ATTACK_FABLE_V001.md` | `f70d9c313863018a72d430269341b84c21ae44fb0d4de968df12b3aafa217573` | the RAIL verdict + surface inventory (S1-S19) |
| 02 | `STAGE8_RECORD_CELL_SURFACE_AND_BETA_CLOSURE_HUNT_EINSTEIN_V001.md` | `04c10c0d3082050b3b2178109b1d3f5279c2b1bf5abaabd978cdf3bfcf09c10d` | the junction inventory + the matching structure |
| 03 | `BID_ABSOLUTE_RECORD_SCALE_IDENTIFIABILITY_GATE_V001.md` | `8f9bdd93cd32fbf116e9c9f645df56e3c1608c83883238f524be94ec8d999b37` | the scale orbit |
| 04 | `STAGE8_SADDLE_FOUNDATION_PARENT_ACTION_DARIO_V001.md` | `1d11f15040f8b85b7e081fccfeddb995c41941c55464d759a2fa91a8feffc775` | connection-only ground |
| 05 | `MEASURE_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md` | `b9716661d1a03a16ec7fd06c836de2f74dbae0acafa0564c9241f96e791b420c` | the supply structure: falsifier + open forcing slot + battery overdetermination |

```text
PINNED QUOTES (file:line, verified at path this session; quoted exactly):
  member 02 :32-33   "EVERY JUNCTION THAT IS DERIVED IS beta-INVARIANT, AND EVERY JUNCTION WHERE
                      beta COULD APPEAR IS ADOPTED, GAP, OR UNBUILT. NO JUNCTION IS SIMULTANEOUSLY
                      DERIVED AND beta-SENSITIVE."
  member 02 :79-81   "SO THE INTERNAL NORMALIZATION IS FULLY PINNED AND DIMENSIONLESS; THE ENTIRE
                      CONVERSION FREEDOM SITS AT THE JUNCTION, NOT INSIDE THE INTERNAL GEOMETRY."
  member 02 :102     "`causal_diamond_shape_derived = true` — **the shape is derived; the
                      conversion is not.**"
  member 02 :113-114 "`m_* T_R = pi` (the brief's one *\"cross-geometry lock\"*, mass × interval —
                      contains no `R`, trivially beta-invariant)"
  member 03 :39-41   "All first-opening probabilities, orthogonality, endpoint transfer, incidence
                      normalization, and projective path lengths are identical on this scale orbit."
  member 03 :54-57   "The current record kinematics do not identify one SI value of `T_R`. The
                      allow/require rule fixes the first completed action interval, not an absolute
                      clock duration, unless an additional dimensionful part of the same
                      parameter-free dynamics breaks this scale orbit."
  member 03 :87-90   "first_opening_data_break_scale_orbit = false / absolute_SI_record_duration_
                      derived = false / ... coupled_gravity_record_stationarity_equation_derived
                      = false"
  member 04 :168-170 "the sector-CONTROLLED transition and the exact finite influence functional,
                      indexed by CONNECTION / HOLONOMY HISTORIES ALONE.
                      ** THERE IS NO METRIC ARGUMENT ANYWHERE IN IT. **"
  member 05 :47-49   "THE OPEN FORCING SLOT: future_derivation_can_select_the_measure = NO_VERDICT"
  member 05 :50-52   "BATTERY OVERDETERMINATION: μ feeds the full preregistered battery, all frozen
                      before evaluation; the candidate may not be shaped by or tested against any
                      downstream target — NO OUTPUT INSPECTION"

ESTABLISHED IN THIS RELAY: the forcing-type characterization of the boundary (sections 2-5); the
  fixed-point form of the matching test (section 3); the typed disposal of the battery candidate
  (section 4); the coherence-probe result and its degeneracy note (section 6).
AUTHORED IN THIS RELAY: nothing physical. No object, junction, matching rule, or scale is authored.
  The only new content is the characterization of forcing-type and the coherence adjudication.

BLIND HELD. EVERY SCALE SYMBOLIC. NO NUMERIC EVALUATION. NO OUTPUT INSPECTION. NO MEASURED-VALUE
COMPARISON. NO IMPORTED LENGTH. NO MEMBER BOUND. NO FIXED POINT SELECTED. NO END TEST. NO FREEZE.
alpha_computed = false   proof_authorized = false   kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

CLOSURE_DECLARATION_END

# STAGE 8 — THE BOUNDARY FORCING CHARACTERIZATION: DOES INTERIOR/EXTERIOR MATCHING PIN THE ORBIT POINT?
## FABLE-BOUNDARY-1109 — BLIND CONSTRUCTOR — [CLAIMED]

Date: 2026-08-12

**WHAT WAS ASKED.** Under the given ALLOW/REQUIRE reading of the record cell's boundary (interior
= ALLOW, exterior = REQUIRE, absolute scale a free orbit at the surface), characterize the
boundary's forcing type from surface data: does requiring the interior and exterior descriptions
to MATCH across the boundary overdetermine — and so pin — the orbit point? Test two require
candidates: (a) the interior/exterior matching condition; (b) the supply structure's "battery
overdetermination." Simultaneously probe whether the characterization coheres with the RAIL
premise or requires a derived scale-sensitive junction RAIL says cannot exist.

**WHAT I FOUND, IN ONE LINE:** the matching condition — the derived junction-lock set of the
cell's interface inventory — is a system of orbit-CONSTANT equations, so matching that holds at
one orbit point holds at every orbit point and can pin nothing; the battery candidate is
check-redundancy on a mandated import, attached to a different object; and the entire
characterization runs on scale-free surface objects alone, so the boundary is an ALLOW-typed
boundary with respect to the scale — FREE-B, cohering with RAIL.

---

## 0. LEAD

The forcing test reduces to one exact question with one exact answer. FORCED requires the matching
condition's solution set to intersect the presentation orbit in a single point. But every equation
in the surface-native matching condition is a fixed point of the scale action (member 02 :32-33,
executed twice, TYPE-R; carried into member 01's S18-S19), so the matching condition's solution
set is a union of WHOLE orbits: if any orbit point satisfies it, all do. A condition invariant
under a group action cannot select a point of a non-trivial orbit of that action, and the orbit is
non-trivial (`positive_scale_orbit_exists = true`, member 03 :86). That is the whole verdict; the
sections below anchor each ingredient, dispose of the second candidate, run the unique-require
sweep so the verdict is doctrine-robust, and report the coherence probe with its one degeneracy
noted honestly.

Numeric strings below (`pi/sqrt(2)`, `pi hbar`, `m_* T_R = pi`, `C_R = 1`, thirteen) are
sealed-text quotations from the members, never computations.

---

## 1. THE BOUNDARY, NAMED FROM SURFACE OBJECTS

The boundary is the record cell's junction — the interface between the cell's internal
(projective/phase) description and its external (diamond-shape) description. Its complete
surface-side footing, all scale-free:

```text
B1  INTERIOR FOOT — the primitive projective record degree; the derived counting metric
    ell^2(X); the phase/action content |Delta S_record| = pi hbar. "FULLY PINNED AND
    DIMENSIONLESS; THE ENTIRE CONVERSION FREEDOM SITS AT THE JUNCTION, NOT INSIDE THE
    INTERNAL GEOMETRY."                                    (member 02 :73-87, :79-81)
B2  EXTERIOR FOOT — the causal diamond's DIMENSIONLESS shape coefficients (derived); the
    dimensionful presentation (radius, volumes) rides the orbit as a COORDINATE, not
    content. "the shape is derived; the conversion is not."  (member 03 :44-50; member 02 :102)
B3  THE JUNCTION SET — the thirteen sealed interface quantities, with the executed split:
    every DERIVED junction dimensionless and scale-orbit/beta-invariant; every
    scale-sensitive junction ADOPTED (fiber radius in K_KK), UNBUILT (D_BR spectrum), or
    GAP (skeleton-to-cell embedding).                        (member 02 :30-33, :108-120)
B4  THE SCALE ACTION — for every positive lambda: T_R -> lambda T_R, H_R -> H_R/lambda,
    with the surface identity exp(-i H_R' T_R'/hbar) = exp(-i tau_R B); the surface-visible
    inventory identical along the orbit.                     (member 03 :14-41)
B5  THE CONNECTION-ONLY GROUND — the interacting object indexed by CONNECTION/HOLONOMY
    HISTORIES ALONE, no metric argument; unit-modulus characters; the C7 refinement law
    (appended cells contribute exact unit factors); the affine connection space with no
    fiber metric.                                            (member 04 :168-170; member 01 S4, S10, S17)
```

Under the given ALLOW/REQUIRE reading: B1 is the ALLOW side (the cell's own freedom, fully pinned
where it is derived); B3 is the REQUIRE side — the derived junction locks are exactly "what the
crossing obliges."

---

## 2. THE MATCHING CONDITION, NAMED

The interior/exterior matching object, by name: **the derived junction-lock set of the record
cell's thirteen-quantity interface inventory** — its four members:

```text
M1  |Delta S_record| = pi hbar        (action x phase; the marker; non-identifications sealed)
M2  m_* T_R = pi                       (the one cross-geometry lock, mass x interval;
                                        "contains no R, trivially beta-invariant")
M3  the balanced-geodesic identities   (p = 1/2, theta = pi mod 2 pi, as sealed)
M4  the onset bound                    (J_FS,rel >= pi hbar/2)
                                                          (member 02 :82-88, :110-115; member 01 S18)
```

This IS the complete surface-native matching structure: member 02's inventory is sealed as the
complete interface of the one cell, and these four are its entire derived subset. Requiring "the
interior and exterior descriptions to MATCH across the boundary" is, on the surface, requiring
M1-M4 to hold — there is no fifth derived junction to add (executed twice, independently:
attacker and verifier, member 02 method block and :30-33).

SCOPE FORK, stated before the test so the verdict's bound is explicit: if "matching" is extended
beyond the derived subset to the ADOPTED/UNBUILT/GAP junctions (the fiber radius ansatz, the D_BR
spectrum, the skeleton-to-cell embedding), those CAN be scale-sensitive — but they are not
derived, so any pin obtained through them is supplied by the import, not by the surface. A
surface-NATIVE forcing verdict can only be run on the derived subset. The extended fork is
rail-side by construction and is not this artifact's question.

---

## 3. REQUIRE CANDIDATE (a): DOES MATCHING OVERDETERMINE THE ORBIT POINT? — NO

The test, run exactly and symbolically (nothing evaluated):

```text
STEP 1  The scale action (B4) acts on presentations: T_R -> lambda T_R, H_R -> H_R/lambda,
        radius -> lambda radius, volumes -> lambda^3, lambda^4 volumes.     (member 03 :25-49)
STEP 2  Each matching equation is a FIXED POINT of that action:
        M1: an action in hbar units — no T_R, no radius; invariant.        (member 02 :82-85)
        M2: mass x interval — the generator side carries 1/lambda, the interval side lambda;
            the product is invariant, and the lock "contains no R".        (member 02 :113-114)
        M3: dimensionless phase/probability identities; invariant.          (member 02 :83-84)
        M4: an inequality between actions in hbar units; invariant.        (member 02 :84-85)
        And the general statement, executed twice: NO junction is simultaneously derived
        and scale-sensitive.                                                (member 02 :32-33)
STEP 3  Therefore the matching condition M = {M1, M2, M3, M4} satisfies: for every orbit
        point lambda, M holds at lambda if and only if M holds at every other orbit point.
        The solution set of "interior description = exterior description across the
        boundary" is a UNION OF WHOLE ORBITS.
STEP 4  An overdetermination pinning the orbit point requires the solution set to meet the
        orbit in exactly one point. A union of whole orbits meets the orbit in either
        nothing or everything. The orbit is non-trivial
        (positive_scale_orbit_exists = true, member 03 :86). NO PIN IS POSSIBLE.
```

This is a fixed-point argument on the surface's own inventory — the same argument-type member 01
runs at its section 3, and it uses no continuum, metric, KK, or variational object anywhere: only
the orbit identity (B4), the executed junction split (B3), and the two dimensionless feet (B1,
B2). Sealed corroboration that the matching is in fact SATISFIED on the orbit (so the solution
set is "everything," not "nothing"): the locks are carried as derived — they hold — and "all
first-opening probabilities, orthogonality, endpoint transfer, incidence normalization, and
projective path lengths are identical on this scale orbit" (member 03 :39-41).

For OVERDETERMINATION specifically — two independent constraints pinning the same object — the
count is decisive in a stronger way: an orbit-pinning constraint must be orbit-breaking, and the
executed split shows the surface carries ZERO derived orbit-breaking junctions. One cannot
assemble two independent pinning constraints from a stock of zero. The nearest exterior-side
"require"-shaped items are each disqualified at source: the compactness selector `C_R = 1` is
ADOPTED Level-1, and a permission-shaped adoption cannot force (member 02 :92-94, :133-136); the
null-edge action fails as a selector — "every positive radius can be obtained by a normalization
choice" (member 02 :99-101, quoting HJ :129).

`MATCHING_OVERDETERMINES_SCALE = NO` — the matching is an identity along the orbit, not a
constraint on it.

---

## 4. REQUIRE CANDIDATE (b): THE "BATTERY OVERDETERMINATION" — NOT A FORCING, AND NOT THIS OBJECT

Member 05's supply structure carries the phrase, and its own text disposes of the candidacy on
four independent grounds:

```text
G1  WRONG OBJECT. The battery attaches to omega_phys — "A U1-INVARIANT PROBABILITY MEASURE
    ON Y" (member 05 :29-30) — not to the orbit point. Even if it forced, it would force a
    measure, not a scale. As typed of record, the measure's carrier is the state space of
    the algebra that already contains Z_N (member 05 :13-15, :33) — connection-only content,
    which member 03's inventory shows is constant along the orbit. The typing carries no
    orbit-sensitive slot. (A typing observation about the object AS MANDATED; no candidate
    exists at sealing, member 05 :24-25, and none is analyzed here.)
G2  WRONG TYPE. The battery is FALSIFICATION redundancy — "the full preregistered battery,
    all frozen before evaluation ... NO OUTPUT INSPECTION" (member 05 :50-52) — a filter
    that can VOID a candidate, never a derivation that produces one. Under the carried
    doctrine, rejection structure is permission-shaped (it bounds the ALLOW); a filter that
    many candidates could pass forces nothing, and a filter that only one candidate could
    pass would be selection by downstream targets — which the battery's own clause forbids
    ("may not be shaped by or tested against any downstream target") and which member 03
    bars for the orbit by name ("No electromagnetic coupling, particle mass, cosmological
    endpoint, or observed radius may be used to select a member of the orbit," :62-64).
G3  THE STRUCTURE'S OWN CERTIFICATION POINTS THE OTHER WAY. The supply exists BECAUSE
    "derivation is ... exhausted for this object at every lawful depth" (member 05 :16-17);
    the forcing slot is held OPEN as a future possibility, not a present fact
    ("future_derivation_can_select_the_measure = NO_VERDICT", member 05 :47-49). A
    structure that certifies derivational exhaustion and keeps its forcing slot open is the
    program's standard dress for an UNFORCED object — the same dress the RAIL import wears.
G4  NOT SURFACE-NATIVE. The battery is convertibility discipline on a PRINCIPAL-MANDATED
    supply (member 05 :21-23) — process architecture, not a junction of the record cell.
    It appears in no member's surface inventory.
```

`BATTERY_FORCES_SCALE = NO` — the battery is redundancy of CHECKS on an import, not redundancy
of DERIVATIONS on the orbit point.

---

## 5. THE UNIQUE-REQUIRE SWEEP, AND DOCTRINE-ROBUSTNESS

The carried doctrine admits one other forcing shape: a UNIQUE-REQUIRE. Swept against the members:

```text
U1  The corpus's one named unique-require candidate for the scale is the coupled
    gravity-record stationarity equation, closure form F(T_R/t_P, dimensionless branch
    data) = 0 with one isolated positive stable solution (member 03 :68-79). Its status is
    sealed: coupled_gravity_record_stationarity_equation_derived = false (member 03 :90;
    member 02 N3, TYPE-U). AN UNBUILT REQUIRE FORCES NOTHING NOW.
U2  Even AS TYPED, the equation's output is T_R alone; member 02's executed test (twice,
    independently) shows it is beta-invariant as-typed, so it would fix the diamond scale
    and leave the fiber modulus free (member 02 :42-53). The boundary's conversion bundles
    both moduli, and the beta half is generated by the rail itself (member 01, E4).
U3  Member 04's ground closes the remaining route: the record's own interacting object has
    no metric argument (member 04 :168-170); every route to one is an import (member 04
    :220-232). No surface dynamics exists from which a unique-require on the orbit point
    could presently be derived.
```

ROBUSTNESS NOTE. Member 02 carries a corrected program bar under which overdetermination was
struck by principal act and only a derived-existence-plus-derived-uniqueness require forces
(member 02 :150-155). This artifact's doctrine (carried as given from its assignment) admits
overdetermination as a forcing shape. THE VERDICT IS THE SAME UNDER BOTH DOCTRINES: no
overdetermination is present (sections 3-4) and no derived unique-require exists (this section).
Nothing in the verdict turns on which doctrine governs.

---

## 6. THE COHERENCE PROBE — COHERES-WITH-RAIL

The probe's test: was the boundary and its matching characterized using ONLY scale-free surface
objects plus the matching requirement, or did the characterization REQUIRE a derived
scale-sensitive junction?

```text
INVENTORY OF EVERYTHING SECTIONS 1-5 USED:
  the unit-modulus holonomy characters and connection-only interacting object   (B5) scale-free
  the C7 refinement law (unit factors)                                          (B5) scale-free
  the affine connection space (no fiber metric)                                 (B5) scale-free
  the scale-orbit identity exp(-i H_R' T_R'/hbar) = exp(-i tau_R B)             (B4) scale-free
      (it IS the invariance statement; its dimensionful dress is chart-side,
       per member 01 section 3's residue note, carried)
  the two dimensionless feet (counting metric; shape coefficients)              (B1, B2) scale-free
  the four derived junction locks M1-M4                                         (B3) each a fixed
      point of the scale action — scale-free by the executed split
  the matching requirement itself                                               (the probe's given)
NOTHING ELSE. In particular: no derived scale-sensitive junction was needed, consumed, or
found anywhere in the characterization.
```

`COHERENCE = COHERES-WITH-RAIL.` The coherence is in fact stronger than consistency: RAIL's
premise (every derived junction is invariant under the scale action) is the very lemma that makes
the matching condition orbit-constant in section 3, STEP 2. The two verdicts interlock.

DEGENERACY NOTE, stated so the probe's power is not overclaimed: the two answers are partially
linked by logic. Had the verdict been FORCE-A, the pinning equation would itself have been a
derived scale-sensitive junction, so FORCE-A would have entailed CONTRADICTS-RAIL — on that
branch the probe could not have returned COHERES. The probe retains independent content on the
branch that occurred: FREE-B could still have CONTRADICTED RAIL if merely DESCRIBING the boundary
had required a derived scale-sensitive object that pins nothing (for example, a derived
dimensionful radius junction riding the orbit). It did not: the boundary's complete surface-side
description (section 1) closed on dimensionless objects, which is the probe's non-trivial half,
verified by the inventory above.

---

## 7. THE CHARACTERIZATION, STATED ONCE

The boundary's forcing type: **an ALLOW-typed boundary with respect to the scale.** What the
crossing obliges (the REQUIRE side, M1-M4) is real and exactly four locks deep — but every
obligation it carries is a fixed point of the scale action, so the crossing obliges shape, phase,
action, and mass-interval relations while obliging NO selection of the orbit point. The interior
freedom (ALLOW) and the exterior obligations (REQUIRE) meet at a junction whose entire derived
content is scale-blind; the absolute scale passes through the boundary untouched in both
directions. Matching is satisfiable for every orbit point; the scale is irreducibly free at the
surface — where "irreducibly" carries its exact bound: free with respect to the ratified
surface-native inventory as carried, with the corpus's own escape (a future orbit-breaking
derivation: member 03's closure condition; member 05's open forcing slot) named and open.

---

## 8. WHAT THIS DOES NOT SAY

```text
IT DOES NOT say the scale can never be fixed. It says no surface-native require — matching-shaped
   or battery-shaped, overdetermining or unique — presently pins the orbit point; the sealed
   escape routes (the F-equation would-build; the open forcing slot) remain open and are cited.
IT DOES NOT fix, estimate, bound, or begin to fix any scale, and it does not select any orbit
   member. The orbit is characterized, never sectioned.
IT DOES NOT extend the verdict to the ADOPTED/UNBUILT/GAP junctions: the scope fork of section 2
   is explicit — an extended matching that consumes imports can be scale-sensitive, but any pin
   it produced would be import-supplied, which is the RAIL finding restated, not a surface force.
IT DOES NOT analyze any candidate measure omega_phys (none exists at member 05's sealing); the
   battery disposal is a typing argument about the mandate's own text.
IT DOES NOT re-decide the RAIL verdict; it probes it and reports coherence, with the probe's
   partial logical degeneracy disclosed rather than dressed as independent confirmation.
IT DOES NOT evaluate anything. No number of any physical kind appears except as sealed-text
   quotation; the only computed strings are file digests.
```

---

## 9. CUSTODY

- CONSTRUCTOR NEVER VERIFIES OWN WORK. Everything above is CLAIMED until independent check.
- Reads this session: exactly the five closure members, at their paths, each hash-verified against
  the assignment's pinned digests before reading. No register, tracker, plan, road, continuation,
  or expectations file was read. No broad grep. No git action of any kind. Not registered, not
  committed, not pushed (per assignment).
- No file was written except this artifact.

---

## 10. FINAL LINES

```text
MATCHING_CONDITION_NAMED = the derived junction-lock set of the record cell's thirteen-quantity
     interface inventory: |Delta S_record| = pi hbar (action x phase); m_* T_R = pi (the one
     cross-geometry lock); the balanced-geodesic identities; the onset bound
     (member 02 :82-88, :108-115; member 01 S18-S19)
OVERDETERMINATION_PRESENT = NO (every derived matching junction is a fixed point of the scale
     action, so the matching solution set is a union of whole orbits and meets the orbit in
     everything, never one point; zero derived orbit-breaking constraints exist, so two
     independent pinning constraints cannot be assembled; the battery overdetermination is
     falsification redundancy on a mandated supply of a different object, with its own forcing
     slot sealed open)
FORCING_VERDICT = FREE-B (scale irreducibly free: the interior/exterior matching holds
     identically along the whole presentation orbit; no surface-native require — matching,
     battery, or unique — pins the orbit point; bound stated: free with respect to the ratified
     surface inventory as carried, the corpus's named escape routes cited and open)
COHERENCE_WITH_RAIL = COHERES (the characterization consumed only scale-free surface objects —
     holonomy characters, refinement law, affine connection space, scale-orbit identity, the
     dimensionless feet, the four invariant locks — plus the matching requirement; no derived
     scale-sensitive junction was needed anywhere, and the probe's partial logical degeneracy is
     disclosed in section 6)
IMPORTED_NOTHING = CERTIFIED (no metric, length, compactification, continuum notation, or
     normalization was used to characterize anything; where scale-sensitivity appears it is
     quoted from the members' own ADOPTED/UNBUILT/GAP typing, never built)
SURFACE_ANCHOR = present (objects named — B1-B5, M1-M4, at file and line throughout)

TARGET_INDEPENDENT = CERTIFIED (no output, measured value, or physical constant is referenced,
     approached, or compared)
EVALUATED_NOTHING = CERTIFIED (every scale symbolic; the only computed strings are file digests)
SEALS_VERIFIED = 5/5 MATCH at path (shasum -a 256, this session)
REGISTER_READ = NONE-CERTIFIED

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

**The plain statement.** The boundary was asked one question: if the inside and the outside of a
record cell are required to agree across their junction, does the agreement cost the cell its
scale freedom? The surface answers exactly. The agreement is four locks deep, and all four locks
are blind to the scale — they hold at one orbit point if and only if they hold at all of them. An
agreement that cannot tell orbit points apart cannot charge one as its price. The one structure
carrying the word "overdetermination" turns out to be a battery of checks on an imported object,
holding its own forcing slot open. So the boundary is ALLOW-typed toward the scale: it obliges
much, and selects nothing — which is the RAIL verdict's picture, met here from the matching side.

All findings **CLAIMED** until independent adversarial check.
