# STAGE 8 — THRESHOLD FORM: IS THE GAUGE ESCAPE BINARY OR THRESHOLD-SHAPED, AND DOES THE ALLOW/REQUIRE BOUNDARY MOVE? — O19SR V001

## BUILDER — CODENAME THRESHOLD-BUILD — COMMISSION O19SR — 2026-08-15

DETERMINATION ONLY. Nothing is proposed, authored, adopted, or retired. Every
display below is either (i) sealed bytes quoted, (ii) an exact symbolic check on
STRUCTURAL INDICES (a rank, a dimension, a degree, a menu size), or (iii) an
exact absence stated as an absence. NO value of any program quantity is
produced, approximated, bounded, or compared. FENCES HELD:

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: both
`/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_THRESHOLD_FORM_O19SR_V001.md`
and its `.seal.sha256` sidecar were probed ABSENT before any write
(`ls` returned "No such file or directory" for each).

---

## 0. VERDICT

```text
**THRESHOLD-SHAPED — AND THE THRESHOLD IS NOT THE ONE THE PRESENCE/ABSENCE
REPORT NAMED. Three findings, each derived at bytes, none asserted.**

(1) THE ESCAPE IS NOT BINARY IN THE PRESENCE OF U(1)/EM STRUCTURE, AND THE
    SEALED GROUND REFUTES THAT READING WITH ITS OWN ROWS. O6-1d displays THREE
    U(1)-carrying cases, and U(1) structure is PRESENT IN ALL THREE: a GLOBAL
    phase (d1/d2) leaves the blocker exactly as it was — "it neither creates nor
    removes the blocker"; a CONSTANT (pure-gauge) potential (d5) has d/dx = 0 and
    "translation invariance RESTORED"; only a NON-CONSTANT potential (d3/d4)
    moves anything. TWO OF THE THREE CARRY THE STRUCTURE AND DO NOT ESCAPE, so
    presence/absence of U(1) IS NOT THE CONDITION'S COORDINATE (T19-1c2). The
    coordinate that separates the three cases exactly as the hypotheses separate
    them is THE DEGREE OF THE GENERATOR'S SYMBOL IN THE POSITION VARIABLE
    (T19-1c3) — a STRUCTURAL DEGREE, and a graded one.
(2) WHAT IS THRESHOLD-SHAPED IS THE SEA-SANDWICH THEOREM'S APPLICABILITY, ON
    **TWO** AXES RUNNING IN **OPPOSITE** SENSES — displayed symbolically and
    UNASSIGNED:
      AXIS A (the obstruction's own turn-on, hypothesis (h-4)): the MULTIPLICITY
        of the generator's NEGATIVE SPECTRAL SUBSPACE per momentum. The theorem
        is VACUOUS below the minimal inhabited index m_min and holds at and
        above it. The record's own displays exhibit THREE distinct values of
        this one quantity — the definite-symbol case where the negative set is
        EMPTY (O6-1b2), the scalar-indefinite case where it is a half-space
        (O6-1c2), and the record's Clifford site (O6-1a5) — so the quantity is
        NOT two-valued (T19-1a1), its indicator is MONOTONE and NON-CONSTANT
        (T19-1a2/a3) => THRESHOLD FORM IN A MULTIPLICITY (T19-1a4). And the
        record's own site lies STRICTLY ABOVE m_min, i.e. WITH DISPLAYED SLACK
        (T19-1a5): the record does not sit at this margin.
      AXIS B (the escape's turn-on): the POSITION-DEGREE of the symbol.
        Hypotheses (h-1)/(h-2) hold at degree 0 and FAIL at any higher degree;
        the applicability indicator is monotone NONINCREASING in this degree
        (T19-1b4/b5) => THRESHOLD FORM IN A STRUCTURAL DEGREE, turn-OFF at the
        minimal positive index.
      The theorem therefore applies inside a WINDOW — (position-degree at its
      floor) AND (multiplicity at or above m_min) — NOT on a single
      presence/absence bit (T19-1b6).
(3) THE ESCAPE'S OWN CONDITION IS COUPLING-EXPRESSIBLE, AND THIS COMMISSION
    STOPS THERE. O6-1d4's row computes the escape's discriminant as the
    x-derivative of the symbol, and that derivative IS THE COUPLING (up to
    sign): the row reads `sp.diff(h_A_sym, x1) == -q` with `h_A_sym = kmag -
    q*A3`. The escape's turn-on is thus the NON-VANISHING OF A PRODUCT ONE OF
    WHOSE FACTORS IS THE COUPLING. **DISPLAYED AND STOPPED AT THAT ROW** — no
    evaluation, no estimate, no bound, no inequality with a numeral, here or
    anywhere below (§2.4). This is the nearest any commission has come to the
    fenced territory and it is halted at the row, not carried past it.

**AND THE HEAVIER FINDING, WHICH THE COMMISSION'S DELIVERABLE ASKED FOR
DIRECTLY: YES — THE ALLOW/REQUIRE BOUNDARY MOVES.** It moves at ONE displayed
site, and the instance is exhibited, not inferred. At the EXTENT site (J-2) the
junction survey carries two models agreeing on every displayed clause and
"differing on where a permission begins": at the LOWER extent index "the B-1
permission OPENS here" (ALLOW-side); at the HIGHER extent index "the SAME
quantity is not a permission but a forced kill" (REQUIRE-side). ONE AND THE SAME
NAMED CONTENT IS PERMITTED BELOW AND ITS NEGATION REQUIRED ABOVE, the flip is
EXCLUSIVE at every index of a CLOSED TWO-ENTRY MENU ("no third extent exists"),
and the transition is SHARP — a strict change at a stated index, NOT a limit
(T19-3b/c/d). The require-side kill additionally needs a SECOND threshold, in a
DIMENSION, displayed as a strict inequality at a stated index and likewise not a
limit (T19-3e1/e2); the two must be crossed TOGETHER (T19-3e3).

**WHAT THAT IMPLIES FOR THE PARTITION, DISPLAYED AND NOT CONCLUDED (§4).** The
PARTITION THEOREM of record is stated at ONE site — the magnitude site J-6 — and
at THAT site it is displayed UNIFORM, not fixed at a parameter: the require-half
holds "in EVERY model", the allow-half "in every model, with the two-model
witness uniform over the whole model class", and the require/allow split there is
between the CLASS and the MEMBER, not between two ranges of any index — the
clauses "force the CLASS and pin no member", holding at SYMBOLIC integer n
(T19-4a/b). SO THE PARTITION THEOREM IS NOT STATED AT A FIXED PARAMETER VALUE;
IT IS STATED PARAMETER-FREE, AND THE MOVING BOUNDARY SITS AT A DIFFERENT SITE
(T19-4c) — which is precisely why the survey's own verdict is
JUNCTION-CONTINGENT-WITH-A-RIGID-SUB-REGION rather than rigid. THE PARAMETER
WHOSE CROSSING MOVES THE BOUNDARY AT J-2 IS **LEFT OPEN OF RECORD** — neither
derived nor adopted: it is the R_K_EQUALS_L_T bit, carried as
`NO_VERDICT / TYPE-U` with "both directions explicitly unproved", and its status
is "UNDECIDABLE-TODAY + positive two-model certificate", SEALED TWICE.

**ONE FORM DISTINCTION THE SWEEP FORCED, AND IT MATTERS.** Not every sharp
condition of record is a threshold. The quantization condition — the
require-half of the partition itself — is a CONGRUENCE (lattice) condition, NOT
a threshold: its indicator PASSES, FAILS, AND PASSES AGAIN along an increasing
probe, so it is non-monotone and no crossing turns it on and leaves it on
(T19-2b/c). A quantization is not a threshold, and the corpus carries both
forms. Reporting them under one word would have been an error.

NOTHING IS RETIRED, NOTHING IS REFUTED, NOTHING IS PROPOSED. Every carrier
stands exactly as its own seal left it. This artifact moves no fence, no gate,
no witness, and no V-clause, exhibits no value of any program quantity, and
selects no route.
```

---

## 1. SEALS VERIFIED AT PATH BEFORE ANY RELIANCE; SWEEP CUTOFF DECLARED

Every sidecar was verified with `shasum -a 256 -c` **run from the artifact's own
directory** (sidecars name basenames; a parent-directory run reports false
failures — the commissioned protocol, held). ALL RETURNED `OK`. Every digest was
ALSO recomputed independently this session.

```text
COMMISSIONED SEALED GROUND — sidecar verified OK + digest recomputed:
a04c446aa1406fb89cb732fcf8fc6d777e3414cf0fa1d6761702dedd5dd7d5a3  STAGE8_OBSTRUCTION_ORIGIN_O6SR_V001.md        [ORIG]
0db4de9077980e7139b3c6bfb1272bea8c2b5ffe1e605af886bb338456fd8748  STAGE8_OBSTRUCTION_ORIGIN_O6SR_AUDIT_V001.md  [ORIG-A]
241262ffc7da05b5cef6638f6f7ac5dd8aa4af5ff35becab92cb17f062cbfca9  STAGE8_W3_GCM_HS_TYPE_O3SR_V001.md            [W3]
d05f22be5c2234483f3289f713d95baffb150762042b785da83413216a35be13  STAGE8_W3_GCM_HS_TYPE_O3SR_AUDIT_V001.md      [W3-A]
cfa2fb972a5a604bbb28e5a007045f40b7fa850129db84556a7d4d7fc15eedec  STAGE8_IDEAL_EXTENT_S9AD_V001.md              [EXT]
eda2ba74f744e67d3c1330a022d11cd400758a69e8885027cd4342cc1ad6135f  STAGE8_IDEAL_EXTENT_S9AD_AUDIT_V001.md        [EXT-A]

CONSUMED BY DECLARED SWEEP (the record's own requirement/permission content) —
sidecar verified OK + digest recomputed:
5e12af0b28d378fb20437d1416017771449cae853fdc0960dac6c7efd3b0c895  STAGE8_PARTITION_THEOREM_T16SR_V001.md        [PT]
a307651e1dd0b4371b8245db087f1cc479177e2f710af0071eaeeb25894590ee  STAGE8_PARTITION_THEOREM_T16SR_AUDIT_V001.md  [PT-A]
e1c2ac80db09eafdeb21500e277aaa070b9506994065da1c6b15c74bde2e25c1  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md   [JCT]
0057b1347061edd9a0bd824de94f6ed7b6b705a7eadfaf644005075ccbc07c3c  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md [JCT-A]
ae52417f1bec9d00ef979738ef224bf4adaf4d9d9e4ea53f7c73b88e9db43a77  STAGE8_DISCHARGERS_VS_PARTITION_O11SR_V001.md [DVP]
5da05d533792247a3b42a1d42024be1a35811eb8b756be09d4abba96e4f14112  STAGE8_CERTIFICATION_RULES_O8SR_V001.md       [CR]
```

**CROSS-PROVENANCE CHECK, DONE NOT ASSUMED.** [W3]'s digest `241262ff…`
recomputed here agrees byte-for-byte with the value recorded in [ORIG] §1's own
seal table. [JCT]'s `e1c2ac80…` and [JCT-A]'s `0057b134…` recomputed here agree
byte-for-byte with the values recorded in [PT] §0's seal table. THREE
INDEPENDENT RUNS CONSUME THE SAME BYTES. No carrier was unverifiable.

### 1.1 SWEEP CUTOFF — DECLARED

```text
SWEEP CUTOFF: 2026-08-15, this session. Roots swept:
  (R-a) /Users/bgm/MB Work/alpha-program-archive/workspace                 (primary)
        1758 *.md at top level, 109M — the sweep is over this flat set.
  (R-b) /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
        alpha_fundamental_record_action_cleanroom_v003
SWEEP TERMS (declared, scoped, and the basis of every exhaustiveness claim below):
  (S-i)  FILENAME sweep, both roots, case-insensitive:
         REQUIR | PERMIT | ALLOW | PARTITION | MANDAT | OPTIONAL | MODAL.
  (S-ii) CONTENT sweep for THRESHOLD FORM, root (R-a), regex over all 1758 files:
           (dim|rank|degree|codimension)[^,.;]{0,25}(>=|>=|≥) ?[0-9]
         i.e. a STRUCTURAL QUANTITY standing in a numeric inequality. RETURNED
         40 CARRIERS, listed in full at §3.1 — that list IS the sweep's extent.
  (S-iii) FIXED-STRING sweep for the known dimension-threshold: "dim M_N >= 2".
         RETURNED 13 CARRIERS (§3.1), all inside the extent/bit/junction family.
  (S-iv) FIXED-STRING sweep for side assignments: "allow-side" | "require-side".
EXCLUDED BY COMMISSION, NOT READ: every register / tracker / road / plan /
  continuation / ledger / lens / THE_HANDOFF file. Filter applied to every sweep
  result before any read. "Q-..." tokens EXPECTED-UNLOCATABLE (they occur inside
  sealed carriers as opaque pointers only; none was resolved).
MATERIAL EXHAUSTIVENESS, STATED HONESTLY AND BOUNDED: the threshold inventory at
  §3 is exhaustive FOR SWEEP (S-ii)/(S-iii) — i.e. for threshold conditions
  written with a STRUCTURAL QUANTITY against a NUMERAL. A threshold written
  purely in words, or written against a symbolic bound with no numeral, would
  not be returned by these terms. THAT LIMIT IS THE SWEEP'S AND IS NOT HIDDEN:
  see FLAG-2. Reads beyond the named sections were confined to the carriers
  these terms returned.
NO GIT. No existing file edited. ONE output file plus its seal sidecar.
```

---

## 2. QUESTION (1) — IS THE GAUGE ESCAPE BINARY OR THRESHOLD-SHAPED?

### 2.1 THE CRITERION, FIXED BEFORE THE EVIDENCE IS READ

So that the answer is derived and not asserted, the form test is stated first and
applied mechanically (CAS T19-1, header comment):

```text
Let a condition be posed on a quantity g of record. Then:
  BINARY        iff g admits exactly TWO values of record — the condition is
                then presence/absence and nothing is graded.
  THRESHOLD     iff g admits MORE THAN TWO values of record AND the condition's
                indicator is MONOTONE in g and NON-CONSTANT — a crossing turns
                it on (or off) and LEAVES it so.
  CONGRUENCE    iff the indicator is NON-MONOTONE in g — it passes, fails, and
  (LATTICE)     passes again, so no crossing settles it.
These three are mutually exclusive on any single g, and §2/§3 find ALL THREE in
the corpus. The test reads only the SHAPE of the indicator; it never reads, and
never needs, the VALUE of anything.
```

### 2.2 THE ESCAPE'S CONDITION AT ITS OWN BYTES — AND WHY BINARY FAILS

[ORIG] §4.3 Counterfactual B is the escape display. Its CAS rows O6-1d1..d5
are quoted at bytes from [ORIG] :1084-1111:

```text
O6-1d1/d2  GLOBAL U(1): "GLOBAL U(1) phase is unitary and central: |e^{it}|^2 = 1"
           and "GLOBAL U(1) leaves ||h psi|| and ||psi|| UNCHANGED (modulus 1)
           => it neither creates nor removes the blocker"
O6-1d3     LOCAL U(1): "a NON-CONSTANT U(1) potential BREAKS translation
           invariance: A(x+a) - A(x) = a != 0"
O6-1d4     "h_A's symbol depends on the POSITION variable => NOT a multiplier
           => hypotheses (h-1)/(h-2) FAIL under EM structure"
O6-1d5     PURE GAUGE: "a CONSTANT potential shifts the symbol only: d/dx = 0
           => translation invariance RESTORED (pure-gauge case)"
```

```text
THE DERIVATION [T19-1c2, T19-1c3]:
  U(1)/EM STRUCTURE IS PRESENT IN ALL THREE DISPLAYED CASES. In d1/d2 it is a
  U(1) phase; in d5 it is a U(1) potential; in d3/d4 it is a U(1) potential. Yet
  the hypotheses move in EXACTLY ONE of the three. Therefore the map
      (U(1) structure present?)  |->  (does the escape fire?)
  IS NOT WELL DEFINED: the same antecedent value "present" carries both
  outcomes. A presence/absence condition is thereby REFUTED AT BYTES, not
  argued against.
  THE COORDINATE THAT DOES SEPARATE THEM is the one d4 and d5 actually test:
  the DEPENDENCE OF THE SYMBOL ON THE POSITION VARIABLE. d5's symbol has
  position-degree at its FLOOR (d/dx = 0 exactly); d3/d4's has a HIGHER
  position-degree; d1/d2's phase changes no degree at all. The three cases sort
  by POSITION-DEGREE exactly as the hypotheses sort (T19-1c3).
```

**THE PRESENCE/ABSENCE REPORT IS NOT WRONG — IT IS COARSE.** It correctly names
one case in which the hypotheses fail; it mis-names the coordinate along which
they fail. The corrected coordinate is a graded structural degree, and the
sealed ground already contains every row needed to see it.

### 2.3 THE TWO THRESHOLD AXES, SYMBOLIC AND UNASSIGNED

```text
AXIS A — THE OBSTRUCTION'S OWN TURN-ON (hypothesis (h-4)).
  THRESHOLD IN: the MULTIPLICITY of the generator's NEGATIVE SPECTRAL SUBSPACE
    per momentum — call it  mu  (a rank; symbolic, unassigned).
  CONDITION:  the sandwich is nonzero  <=>  mu >= m_min, where m_min denotes the
    minimal inhabited index, i.e. m_min = min{ r : the negative subspace is
    inhabited at r }. STATED SYMBOLICALLY; NOT EVALUATED.
  THE RECORD'S OWN THREE DISPLAYED VALUES OF mu, at bytes:
    O6-1b2  definite scalar symbol: "{m < 0} is EMPTY => C = 0 => C T C = 0
            => THEOREM VACUOUS"                                  — mu at its floor
    O6-1c2  indefinite scalar symbol: "{m<0} = half-space {k3<0} is NONEMPTY
            => C != 0"                                           — mu inhabited
    O6-1a5  the record's Clifford site: "rank(P_-) = 2 per k (tr P_- = 2)"
                                                                 — mu ABOVE m_min
  FORM [T19-1a1..a4]: three distinct values => not two-valued; indicator
    monotone; indicator non-constant  =>  **THRESHOLD FORM IN A MULTIPLICITY.**
  SLACK [T19-1a5]: the record's own site is STRICTLY ABOVE m_min. THE RECORD DOES
    NOT SIT AT THIS MARGIN — a fact worth displaying, because a threshold whose
    record site sits strictly inside is not a knife-edge.
  SHARP OR LIMIT: **SHARP.** O6-1b2 is an EMPTINESS statement and O6-1c2 a
    NONEMPTINESS statement — a strict change at a stated index. No sequence, no
    limit, no rate appears in either row.

AXIS B — THE ESCAPE'S TURN-ON (hypotheses (h-1)/(h-2)).
  THRESHOLD IN: the DEGREE OF THE GENERATOR'S SYMBOL IN THE POSITION VARIABLE —
    call it  delta  (a structural degree; symbolic, unassigned).
  CONDITION:  the theorem APPLIES  <=>  delta is at its floor (constant
    coefficients); it CEASES TO APPLY once delta exceeds that floor.
  FORM [T19-1b1..b5]: the applicability indicator is monotone NONINCREASING in
    delta and non-constant  =>  **THRESHOLD FORM IN A STRUCTURAL DEGREE**,
    turning OFF at the minimal index above the floor.
  SHARP OR LIMIT: **SHARP.** O6-1d5 is `d/dx = 0` exactly and O6-1d3 is
    `A(x+a) - A(x) = a != 0` exactly — an identity against a non-identity, not a
    limit.

THE JOINT SHAPE [T19-1b6]: the two axes run in OPPOSITE SENSES — Axis A is a
  LOWER condition (mu at or above m_min) and Axis B an UPPER condition (delta at
  its floor). The sea-sandwich theorem therefore applies inside a WINDOW in two
  structural quantities, NOT on a single presence/absence bit. That is the
  form answer, and it is derived from the sealed rows alone.
```

### 2.4 **THE FENCE ROW — DISPLAYED, AND STOPPED AT**

```text
THE FACT, DISPLAYED IN FULL:
  Axis B's threshold, written in the record's OWN displayed variables, is
  EXPRESSIBLE IN TERMS OF A COUPLING. [ORIG] :1103-1106 constructs the
  gauge-carrying symbol and differentiates it:
        h_A_sym = kmag - q * A3
        chk("O6-1d4 ...", sp.diff(h_A_sym, x1) == -q and
                          sp.simplify(sp.diff(h_A_sym, x1)) != 0)
  So the quantity whose non-vanishing turns Axis B is the x-derivative of the
  symbol, and that derivative IS the coupling times the potential's gradient.
  The escape's turn-on is the NON-VANISHING OF A PRODUCT ONE OF WHOSE FACTORS
  IS THE COUPLING q.

**THIS COMMISSION STOPS AT THIS ROW.**
  NOT DONE, and not to be inferred from anything above or below: no value of q;
  no bound on q; no estimate of q; no inequality with a numeral on q or on any
  quantity derived from it; no relation of q to alpha, to kappa, to any record
  constant or scale; no statement that the threshold is "small", "large",
  "near", or "far". The CAS battery deliberately carries this factor as the
  UNINTERPRETED symbol `Q` (T19-1b, source comment: "DELIBERATELY
  UNINTERPRETED"), so that no run of this artifact can be pointed at a value.
  COUPLING_EXPRESSIBLE = true ; STOPPED_AT_ROW = O6-1d4 ;
  alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
```

### 2.5 THE AUDIT'S CORRECTION F-4a, WHICH SHARPENS THE FORM ANSWER

The escape must be read at the strength its own AUDIT leaves it, and that audit
[ORIG-A, OVERALL = CONFIRMED-WITH-CORRECTIONS] corrects the escape claim
directly. Quoted at bytes, [ORIG-A] :254-267:

> "WHAT O6-1d3/d4 ACTUALLY ESTABLISH: that a non-constant A(x) makes the symbol
> position-dependent, so hypotheses (h-1)/(h-2) FAIL and W-3's theorem DOES NOT
> APPLY. Denying a SUFFICIENT condition does not deny its conclusion;
> compactness of the sandwich under a local potential is not decided anywhere in
> this artifact or its ground. … CORRECTION: keep the SS0/SS8 wording
> everywhere; strike 'the blocker is destroyed'."

```text
WHAT THIS DOES TO THE FORM ANSWER [DERIVED]:
  Above Axis B's threshold the obstruction's fate is **NOT DECIDED OF RECORD**.
  So the escape does not even map into a two-valued codomain {blocker holds,
  blocker escapes}: it maps into {THEOREM APPLIES, THEOREM DOES NOT APPLY}, and
  the second cell is UNDETERMINED, not "escaped".
  CONSEQUENCE, EXACT: what is threshold-shaped is the APPLICABILITY OF A
  SUFFICIENT CONDITION — not the presence of the obstruction. A BINARY reading
  is refused twice over: once because U(1)-presence is not the coordinate
  (§2.2), and once because the codomain above the threshold is not a decided
  alternative at all. THIS IS THE STRONGEST STATEMENT THE SEALED GROUND
  SUPPORTS, AND IT IS WEAKER THAN "THE ESCAPE IS THRESHOLD-SHAPED" WOULD BE IF
  READ AS "THE OBSTRUCTION TURNS OFF ABOVE A THRESHOLD". IT DOES NOT SAY THAT,
  AND NEITHER DOES THIS ARTIFACT.
  [The directional finding of [ORIG] survives untouched on the corrected
  wording, exactly as [ORIG-A] states: real gauge structure REMOVES the
  blocker's stated hypotheses rather than supplying them.]
```

---

## 3. QUESTION (2) — DOES ANY REQUIRE/ALLOW CLASSIFICATION TURN ON A PARAMETER CROSSING A VALUE?

### 3.1 THE SWEEP'S RETURN, DISPLAYED IN FULL

Sweep (S-ii) — a structural quantity standing in a numeric inequality — returned
**40 carriers** in root (R-a) after the excluded-file filter. The full list IS
the sweep's extent and is displayed so the negative below is bounded and
checkable:

```text
ALLOW_REQUIRE_JUNCTION_T14SR (+AUDIT) ; AND_CLASS_INDEPENDENCE ; AND_CLASS_CHECK ;
B1_CELL_CHECK ; B3_DISPLAYS_S9AD (+AUDIT) ; BOTH_BLOCKS_DETERMINATION ;
BOTH_BLOCKS_CHECK ; CERTIFICATION_RULES_O8SR (+AUDIT) ;
DISCHARGERS_VS_PARTITION_O11SR_AUDIT ; EXTSRC_ROUND3_S9AD (+AUDIT) ;
GLUED_TOPOLOGY_HUNT ; IDEAL_EXTENT_S9AD (+AUDIT) ;
LOCALIZATION_ASSUMPTIONS_O9SR (+AUDIT) ; MIN_CARRIER_CHECK ;
MIN_CARRIER_CANDIDATE ; OB2_EMPTINESS_S9AD (+AUDIT) ;
OBSTRUCTION_ORIGIN_O6SR (+AUDIT) ; OUTSIDE_FORM_CLASS_O10SR (+AUDIT) ;
PIN_DETERMINATION_S9AD ; RA27_RESPONSE_LEGS_S9AD ; RK_LT_BIT_T1SR (+AUDIT) ;
RK_LT_BIT_S9AD ; REQUIRE_TARGET_POSE_CHECK ;
TASK4A_MAP1_EXTENSION_CROSS_REVIEW_LANE2 ; TASK4A_MAP1_FULL_FAMILY_EXTENSION_LANE1
(V001,V002) ; TASK5_EQ6_EXHIBIT_V002_HOSTILE_CHECK_LANE1 ;
TASK5_EQ6_DETERMINATION_MAP_LANE1 ; TASK5_EQ6_FC6_DETERMINATION_DARIO ;
TASK5_MODULUS_VERIFICATION_AND_RETURN_CERTIFICATE_LANE2
```

Sweep (S-iii), the fixed string `dim M_N >= 2`, returned **13 carriers**, every
one of them inside the extent/bit/junction family: ALLOW_REQUIRE_JUNCTION_T14SR
(+AUDIT), B3_DISPLAYS_S9AD (+AUDIT), EXTSRC_ROUND3_S9AD (+AUDIT),
IDEAL_EXTENT_S9AD (+AUDIT), OB2_EMPTINESS_S9AD, PIN_DETERMINATION_S9AD,
RK_LT_BIT_T1SR (+AUDIT), RK_LT_BIT_S9AD. **THE DIMENSION THRESHOLD IS ONE
THRESHOLD, CARRIED ACROSS THIRTEEN FILES — NOT THIRTEEN THRESHOLDS.**

### 3.2 THE THRESHOLD INVENTORY — WHAT EACH IS A THRESHOLD **IN**, AND SHARP OR LIMIT

```text
TH-1  THE DIMENSION-THRESHOLD KILL  [EXT, verified; the one the commission named]
  A THRESHOLD IN: the STAGE DIMENSION (a dimension). Symbolically: the kill NG-3'
    is armed once the stage dimension reaches the stated index and at every index
    above it.
  BYTES [EXT :8-12]: "at dim M_N >= 2 with TOP-1/TOP-2, B-1 INHABITED <=> NO
    MIXING RELATION at the anchor stages; any mixing relation at a dim M_N >= 2
    stage kills BOTH families (NG-3' under forced PIN-1 ...)".
  AND ITS COMPLEMENT, DISPLAYED [EXT :100-101]: "the kill it arms is NG-3' on the
    dim M_N >= 2 cell ONLY (at undisplayed dim M_N the kill does not close)".
  SHARP OR LIMIT: **SHARP — a strict inequality at a stated index** [T19-3e2].
    The carrier writes an inequality against an index, not a rate, not a limit,
    not an asymptotic. Nothing tends to anything.

TH-2  THE EXTENT THRESHOLD  [EXT X-2, verified]
  A THRESHOLD IN: the DIMENSION OF THE RELATION IDEAL'S EXTENT (a dimension).
  BYTES [EXT :53-59]: "THE FULL EXTENT MENU AT EACH ANCHOR STAGE HAS EXACTLY TWO
    ENTRIES ... E-1 (no mixing): ... dim 1 — PURE-CYCLE-COMPLETE; E-2 (mixing):
    ... dim 2 — ONE mixing line ... dim ker(qtilde_N) is in {1,2} — no third
    extent exists."
  SHARP OR LIMIT: **SHARP.** The menu is CLOSED and has exactly two entries
    [T19-3a]; the transition is a change of entry, not a limit of entries.

TH-3  AXIS A — THE OBSTRUCTION'S MULTIPLICITY THRESHOLD  (§2.3)
  A THRESHOLD IN: a MULTIPLICITY (a rank). SHARP (emptiness vs nonemptiness).

TH-4  AXIS B — THE ESCAPE'S POSITION-DEGREE THRESHOLD  (§2.3)
  A THRESHOLD IN: a STRUCTURAL DEGREE. SHARP (an identity vs a non-identity).
  **COUPLING-EXPRESSIBLE — DISPLAYED AND STOPPED AT §2.4.**

NOT A THRESHOLD, THOUGH IT LOOKS SHARP — DISPLAYED SO IT IS NOT MISCOUNTED:
TH-0  THE QUANTIZATION CONDITION  [PT §3.1 S3/S4, verified]
  BYTES: "closure <=> exp(2 pi i n) = 1" and "The full solution set of
    exp(2 pi i . x) = 1 is EXACTLY Z ... non-integers fail exactly (residual -2
    at 1/2; nonzero at 1/3)".
  FORM [T19-2a/b/c]: the indicator PASSES, FAILS, AND PASSES AGAIN along an
    increasing probe — NON-MONOTONE. **THIS IS A CONGRUENCE (LATTICE)
    CONDITION, NOT A THRESHOLD.** No crossing turns it on and leaves it on.
  WHY IT IS DISPLAYED HERE: it is the require-half of the partition itself, it
    is the sharpest condition in the corpus, and it is NOT threshold-shaped.
    Counting it as a threshold would have inflated the inventory and misdescribed
    the partition's own require-side.
```

### 3.3 **THE DELIVERABLE — IS ANY CONTENT PERMITTED BELOW AND REQUIRED ABOVE?**

**YES. ONE INSTANCE, EXHIBITED AT BYTES, NOT INFERRED.** It is at the EXTENT site
J-2, and the carrier that displays it is the junction survey itself.

```text
QUOTED IN FULL, [JCT] :294-313 (seal e1c2ac80…, verified; cross-checked against
[PT]'s own recorded digest):

  "AT THE EXTENT SITE (J-2), the record itself carries two models agreeing on
   every displayed clause and differing on where a permission begins:

   MODEL E-1 (independence): every displayed clause holds ... ker dim 1;
     PURE-CYCLE-COMPLETE; 'per round 3's biconditional ... the fiber INHABITED
     (modulo TOP-1/TOP-2 and the dim M_N cell)' — the B-1 permission OPENS here.
   MODEL E-2 (mixing, some a_K != 0): the same displayed clauses hold; ker dim 2;
     the extra relation 'kills BOTH families (NG-3' ...)' on the dim M_N >= 2
     cell — the SAME quantity is not a permission but a forced kill.
   AGREEMENT ON REQUIRES: both models satisfy every §2 row that touches them ...
   DISAGREEMENT ON THE LINE: whether B-1 inhabitance sits allow-side (E-1) or
     a kill sits require-side (E-2) — the junction's location at J-2 differs
     between models of the same displayed clause set.
   STATUS OF THE BIT: 'UNDECIDABLE-TODAY + positive two-model certificate'
     (S9AD), independently re-determined (T1SR) — the witness is SEALED, twice."
```

```text
THE READING, DERIVED [T19-3b/c/d]:
  THE CONTENT: B-1 inhabitance — ONE named content, not two.
  BELOW THE THRESHOLD (extent dimension at its lower index): "the B-1 permission
    OPENS here" — **PERMITTED / ALLOW-SIDE.**
  ABOVE THE THRESHOLD (extent dimension at its upper index): "the SAME quantity
    is not a permission but a forced kill" — **REQUIRED / REQUIRE-SIDE**, and the
    carrier's own words insist it is the SAME quantity.
  EXCLUSIVITY [T19-3c]: at every index of the closed menu the content is on
    exactly one side — never both, never neither. The boundary is a clean cut.
  SHARP OR LIMIT [T19-3d]: **SHARP.** A change of entry in a two-entry closed
    menu. No limit, no rate, no sequence appears in either model's display.
  COMPOUNDING [T19-3e3]: the require-side cell additionally carries TH-1 — the
    kill is armed "on the dim M_N >= 2 cell ONLY". SO THE REQUIRE-SIDE
    ASSIGNMENT NEEDS **TWO** THRESHOLDS CROSSED TOGETHER, IN TWO DIFFERENT
    DIMENSIONS. Below either one, the require-side assignment does not close.

**=> THE ALLOW/REQUIRE BOUNDARY MOVES WITH A PARAMETER. THE PARAMETER IS A
DIMENSION. THE INSTANCE IS THE B-1 PERMISSION AT J-2, AND IT IS THE ONLY ONE THE
DECLARED SWEEP RETURNS.**
```

### 3.4 THE EXACT ABSENCE, WHERE THE BOUNDARY DOES **NOT** MOVE

Displayed as an absence, and bounded by the sweep:

```text
AT THE MAGNITUDE SITE (J-6) THE BOUNDARY DOES NOT MOVE WITH ANY PARAMETER.
  [PT] §2 (I): the require-half holds "In EVERY model of Σ".
  [PT] §2 (II): the magnitude "sits allow-side in every model, with the two-model
    witness uniform over the whole model class."
  [PT] §3.1: "every phase-law clause of Σ holds at SYMBOLIC integer n [CAS PT4]
    — the clauses force the CLASS and pin no member. The member |n| stays
    allow-side."
  [T19-4a/b] re-derives the uniformity: the class condition holds at symbolic
    integer n, so no index range separates a permitted regime from a required
    one at this site. THE SPLIT IS CLASS-VERSUS-MEMBER, NOT LOW-VERSUS-HIGH.
  CORROBORATED INDEPENDENTLY BY [DVP] :508-515: "the record forces the
    ADMISSIBLE SET and leaves the SELECTION WITHIN IT free. Class / member. Menu
    / bit. Pin / location. Three sites, one pattern, and never a point on the
    require side ... The axis is CLASS/MEMBER".
NO OTHER MOVING BOUNDARY WAS RETURNED. Of the 40 carriers of sweep (S-ii), the
  ones carrying an allow/require SIDE ASSIGNMENT at all are the junction pair,
  the partition pair, [DVP] and [CR]; only the junction pair displays a side
  assignment that DIFFERS between two models of one clause set. THAT NEGATIVE IS
  THE SWEEP'S AND IS BOUNDED BY THE TERMS AT §1.1 — see FLAG-2.
```

---

## 4. QUESTION (3) — WHAT THE MOVING BOUNDARY IMPLIES FOR THE PARTITION'S STABILITY

**DISPLAY ONLY. NOTHING IS CONCLUDED ABOUT ANY VERDICT, AND NO VERDICT OF ANY
CARRIER IS MOVED, WEAKENED, OR STRENGTHENED BY ANYTHING IN THIS SECTION.**

### 4.1 IS THE PARTITION STATED OF RECORD AT A FIXED PARAMETER?

```text
ANSWER, DISPLAYED: **NO — IT IS STATED PARAMETER-FREE, AND SITE-SCOPED.**
  The partition theorem of record binds ONE site and says so in its own title and
  its own scope clause: "(rigid sub-region, magnitude site J-6)" [PT §2], and
  [PT §6] "CURRENT GROUND ONLY. The theorem binds Σ AS SEALED".
  Within that site it carries NO parameter at all: both halves are quantified
  "in EVERY model" / "in every model", the witness is "uniform over the whole
  model class", and the clause test passes at SYMBOLIC integer n [T19-4a/b].
  THERE IS THEREFORE NO PARAMETER VALUE AT WHICH THE PARTITION THEOREM IS
  PINNED, AND SO NO QUESTION OF THAT VALUE BEING DERIVED OR ADOPTED. The
  commission's conditional ("if so") is NOT MET AT THE THEOREM'S OWN SITE.
```

### 4.2 WHERE A PARAMETER DOES ENTER, AND HOW ITS VALUE IS FIXED

The moving boundary of §3.3 sits at a DIFFERENT site (J-2) from the theorem
(J-6) [T19-4c]. Two parameters govern it. Each is displayed with how its value is
fixed — **by derivation, by adoption, or left open** — and nothing more:

```text
P-1  THE EXTENT INDEX (which entry of the closed two-entry menu holds).
  ITS VALUE IS: **LEFT OPEN OF RECORD.** Displayed at bytes, three ways:
    [EXT :76-82] "What remains open, exactly: whether Rhat_K,N lies in R L_T,N
      inside O_R5,N — and this is a question the sealed record already OWNS under
      another name: R_K_EQUALS_L_T = NO_VERDICT / TYPE-U (two sealed flag lines
      ...), with both directions explicitly unproved ... SW-1..SW-6 re-confirm:
      no sealed byte fixes the bit."
    [JCT :311-312] "STATUS OF THE BIT: 'UNDECIDABLE-TODAY + positive two-model
      certificate' (S9AD), independently re-determined (T1SR) — the witness is
      SEALED, twice."
    [EXT :39-40] the extent artifact's own verdict word: "NOT-CLOSABLE-TODAY".
  NOT DERIVED (no sealed byte fixes it); NOT ADOPTED (no adoption is recorded —
    both models are carried, neither selected). **OPEN.**
  THE RECORD NAMES ITS OWN MISSING CLOSURE OBJECT for this parameter [EXT
    :90-93]: "one sealed certificate, at ONE anchor stage per isometry class,
    deciding Rhat_K,N in R L_T,N". IT IS NAMED HERE AS A DISPLAY OF THE RECORD'S
    OWN TEXT AND IS NEITHER PROPOSED, RANKED, NOR RECOMMENDED BY THIS ARTIFACT.

P-2  THE STAGE DIMENSION (TH-1's index).
  ITS VALUE IS: **FIXED BY DISPLAY WHERE DISPLAYED, AND OPEN WHERE NOT.** At
    bytes [EXT :8-9] the biconditional is stated "at the displayed census, under
    the fixed pins, at dim M_N >= 2 with TOP-1/TOP-2"; and [EXT :100-101] "at
    undisplayed dim M_N the kill does not close".
  SO: NOT DERIVED as a value; carried as a CENSUS/DISPLAY condition together with
    named pins (PIN-1 "forced", TOP-1/TOP-2), i.e. ADOPTED-BY-DISPLAY on the
    displayed stages, and LEFT OPEN on the undisplayed ones.
```

### 4.3 THE IMPLICATION FOR STABILITY, DISPLAYED AS THE RECORD ITSELF DISPLAYS IT

```text
WHAT FOLLOWS — AND IT IS ALREADY THE RECORD'S OWN DISPLAYED VERDICT, NOT A NEW
ONE FROM THIS ARTIFACT. [JCT §5.3], quoted:
  "VERDICT = JUNCTION-CONTINGENT — with a rigid sub-region, both at bytes:
   (i) the line's location is PROVABLY MODEL-INDEPENDENT at the magnitude site
       J-6 (rigid, conditional on the AJ residue ...);
   (ii) the line's location is PROVABLY MODEL-DEPENDENT at the extent site J-2:
       a sealed meta-level two-model witness exists (E-1/E-2, positive
       certificate, twice-determined) — two models agreeing on all displayed
       requires place the B-1 permission's onset differently;
   (iii) at the program level the line is FREE-not-closed ...
   Because at least one sealed two-model witness moves the boundary between
   models agreeing on every displayed require, the junction AS A WHOLE is not
   rigid: JUNCTION-CONTINGENT."

THE STABILITY DISPLAY, STATED PLAINLY AND WITHOUT EXTENSION:
  ST-1  THE PARTITION'S **AXIS** IS STABLE ACROSS THE MOVING SITE. [DVP] displays
        the axis as CLASS/MEMBER — "Menu / bit" is one of its three named
        instances — and at J-2 BOTH models require the MENU ("R-6's menu IS the
        model pair") and leave the BIT free. The axis does not move.
  ST-2  THE **ASSIGNMENT OF ONE NAMED CONTENT TO A SIDE** IS NOT STABLE. B-1
        inhabitance is allow-side at the lower extent index and a require-side
        kill at the upper one. AXIS STABLE, ASSIGNMENT MOBILE — these are two
        different statements and the record supports exactly one of each.
  ST-3  THE MOBILITY IS **SCOPED, NOT GLOBAL**: it is displayed at one site, on
        one content, governed by one open bit and one census condition. The
        theorem's own site is untouched by it, and [JCT §5.1] states the
        magnitude site's rigidity independently ("No model moves this boundary
        inward ... and none moves it outward").
  ST-4  THE MOBILITY IS **CONDITIONAL ON AN OPEN PARAMETER** (P-1), so it is not
        a displayed instability of the partition but a displayed DEPENDENCE of
        one side-assignment on a bit the record has not closed. Whether that
        dependence ever resolves is not decided here and is not decidable from
        anything read for this commission.

NOTHING ABOVE MOVES ANY VERDICT. [PT]'s VERDICT = PARTITION-THEOREM-DERIVED
(unconditional over its stated scope) stands exactly as sealed; [JCT]'s
JUNCTION-CONTINGENT stands exactly as sealed; [EXT]'s NOT-CLOSABLE-TODAY stands
exactly as sealed. This artifact displays their joint FORM and stops.
```

---

## 5. CHOICE LEDGER — EVERY CHOICE THAT COULD HAVE GONE OTHERWISE

```text
CH-1  THE FORM CRITERION IS MINE AND WAS FIXED BEFORE THE EVIDENCE.
      The commission offered a two-way split (BINARY | THRESHOLD-SHAPED). I
      stated a THREE-way criterion at §2.1 (adding CONGRUENCE/LATTICE) before
      reading any condition against it. ALTERNATIVE NOT TAKEN: force every
      condition into the two offered cells — which would have made the
      quantization condition a "threshold" and misdescribed the partition's own
      require-side. The third cell earned its place at T19-2b and is disclosed
      as an addition to the commission's frame, not as something found in the
      record's vocabulary.
CH-2  THE ESCAPE'S COORDINATE. Two readings are available for Axis B: "A is
      non-constant" (a two-valued property) and "the symbol's position-degree
      exceeds its floor" (a graded quantity). I took the GRADED one because it
      is what O6-1d4/d5 actually compute (a derivative, not a predicate) and
      because it sorts all three displayed cases while the two-valued reading
      does not distinguish d1/d2 from d5. BOTH READINGS ARE DISPLAYED at §2.2 so
      the choice is checkable.
CH-3  THE MINIMAL-INDEX DOUBT, TAKEN SERIOUSLY AND NOT SUPPRESSED. Axis A's
      threshold sits at the MINIMAL inhabited index, and a threshold at the
      bottom of a range is formally indistinguishable from a non-vanishing
      condition. I nonetheless classify it THRESHOLD because the record displays
      THREE values of the quantity and its own site sits STRICTLY ABOVE the
      turn-on (T19-1a1/a5) — a bare non-vanishing condition would display two.
      THE DOUBT IS REAL AND IS CARRIED TO FLAG-3 RATHER THAN RESOLVED AWAY.
CH-4  I STOPPED AT THE COUPLING ROW. Axis B could be pursued into the coefficient
      that carries it. FENCE-MANDATED HALT; see §2.4 and FLAG-1. Not a judgement
      that nothing is there — a refusal to look.
CH-5  THE J-2 FLIP IS STATED PRECISELY, NOT MAXIMALLY. The carrier's words are
      that the same quantity "is not a permission but a forced kill". I therefore
      display: the CONTENT is permitted below, and its NEGATION (the kill) is
      required above. I DO NOT claim B-1 inhabitance itself becomes required
      above — that would overstate the byte, and the byte is the point.
CH-6  SWEEP TERMS. I swept for a structural quantity against a NUMERAL. A
      threshold stated purely in words, or against a symbolic bound with no
      numeral, would not be returned. Alternative not taken: a natural-language
      sweep, which at 1758 files and 109M would not have terminated within this
      commission and would have returned unbounded noise. FLAG-2.
CH-7  THE SOLVER ROUTE FOR THE QUANTIZATION CLASS. sympy's solveset on the
      exponential form over the reals returns an INCOMPLETE set in this version;
      I used the REAL CLOSURE COMPONENT instead, which is the route [PT]'s own
      S4 names. Both are displayed in the battery (T19-2a, T19-2a'). FLAG-4.
CH-8  I DID NOT RECONCILE [ORIG]'s OWN FLAGGED SIGN-CONVENTION CONFLICT (its
      FLAG-3: "HS threshold 3/2" vs "HS threshold -3/2" in two sealed carriers).
      It bears on a different question and reconciling two sealed conventions
      would be authorship. CARRIED FORWARD UNTOUCHED.
CH-9  "Q-..." TOKENS NOT RESOLVED — EXPECTED-UNLOCATABLE, per commission.
CH-10 I READ THE ESCAPE AT THE STRENGTH ITS AUDIT LEAVES IT, not at its build's
      wording. [ORIG-A] F-4a strikes "the blocker is destroyed"; §2.5 carries the
      corrected reading, which is WEAKER than the build's and changes the shape
      of the answer. Taking the build's wording would have produced a cleaner
      and less true verdict.
```

## 6. TOY_SEPARATION — WHAT IS ACTUAL SURFACE AND WHAT IS NOT

```text
ACTUAL SURFACE (the object of this determination):
  * The FORM of conditions carried in sealed artifacts of record — their
    indicator's monotonicity, the quantity each is posed on, and whether the
    transition is stated sharply or as a limit. This is a property of the
    record's own displayed text and is read off it.
  * The side-assignments (allow / require) as the sealed carriers state them,
    and whether any of them differs between two models of one clause set.
  * The seal state, the sweep extent, and the exact absences.

NOT ACTUAL SURFACE — QUARANTINED, AND NEVER RELIED ON AS PHYSICS:
  * The scalar symbols m(k) = |k| and m(k) = k_3, and the potential A_3(x) = x_1.
    THESE ARE [ORIG]'s OWN TOYS, quarantined in [ORIG]'s own battery. They are
    cited here as [ORIG]'s displays of FORM and are NOT re-authored, NOT
    extended, and NOT treated as content of the program. No property of the
    program is inferred from any of them.
  * The predicate lambdas in this artifact's battery (`holds`, `applies`,
    `permitted`, `required`, `kill_arms`). THESE ARE FORM TESTS, NOT PHYSICS.
    Each encodes a condition's SHAPE as quoted from a sealed carrier; none is a
    model, a candidate, a member, or a construction, and none is offered as one.
  * The probe list at T19-2b (rationals interleaved with integers) exists ONLY
    to demonstrate NON-monotonicity. It is a counterexample device. No probe
    value is a value of anything in the program.

NOTHING IN THIS ARTIFACT IS A CANDIDATE, A MEMBER, A ROUTE, A REPAIR, OR A
PROPOSAL. No toy is promoted anywhere. The determination is about form, and form
only.
```

## 7. IMPORT AUDIT

```text
ENVIRONMENT: FRESH venv created this session at
  <scratchpad>/v19 , python 3.9.6
FULL DEPENDENCY SET (pip freeze, complete — nothing else is installed):
  mpmath==1.3.0
  sympy==1.14.0
IMPORTS IN THE BATTERY: `import sympy as sp` — ONE import, no others.
NO NETWORK ACCESS AT RUNTIME. NO FILE I/O IN THE BATTERY.
NO FLOAT ANYWHERE: every literal is sp.Integer or sp.Rational; pi and I are
  symbolic; no evalf, no N(), no float(), no numeric tolerance appears.
NO VALUE OF ANY PROGRAM QUANTITY IS COMPUTED, APPROXIMATED, OR BOUNDED. The one
  coupling-adjacent symbol is carried UNINTERPRETED as `Q` by construction
  (§2.4). Every integer in the battery is a STRUCTURAL INDEX — a rank, a
  dimension, a degree, or a menu size — quoted from a sealed display.
BATTERY RESULT: 25 checks, 0 FAIL.
```

---

## 8. FLAG BLOCK

```text
FLAG-1  **COUPLING-EXPRESSIBLE THRESHOLD — DISPLAYED, STOPPED AT THE ROW.**
        Axis B's threshold, in the record's own displayed variables, is the
        non-vanishing of a product one of whose factors is THE COUPLING
        ([ORIG] :1103-1106). This commission displays that fact and goes no
        further: no value, no bound, no estimate, no numeral, no relation to
        alpha / kappa / any record constant or scale. THE HEAVIEST FLAG HERE.
        COUPLING_EXPRESSIBLE = true ; STOPPED_AT_ROW = O6-1d4.
FLAG-2  **SWEEP EXHAUSTIVENESS IS BOUNDED BY ITS TERMS AND IS NOT ABSOLUTE.**
        The inventory at §3.2 and the negative at §3.4 are exhaustive for a
        STRUCTURAL QUANTITY WRITTEN AGAINST A NUMERAL, over 1758 files in root
        (R-a) and the filename sweep in both roots. A threshold stated purely in
        words, or against a symbolic bound carrying no numeral, WOULD NOT HAVE
        BEEN RETURNED. The negative is the sweep's, not the record's.
FLAG-3  **THE MINIMAL-INDEX DOUBT, CARRIED NOT RESOLVED.** Axis A's and Axis B's
        thresholds both sit at the extreme index of their range (the minimal
        inhabited multiplicity; the floor of the position-degree). At that
        position, THRESHOLD FORM and NON-VANISHING FORM are formally
        indistinguishable on the indicator alone. I classify them THRESHOLD on
        the strength of the record displaying THREE values of Axis A's quantity
        with its own site STRICTLY ABOVE the turn-on (T19-1a1/a5). A reader who
        weights that display differently would land on NON-VANISHING for Axis A
        and on the same answer for the escape's coarseness. THE DISPLAY IS GIVEN
        SO THE DISAGREEMENT CAN BE HAD AT BYTES.
FLAG-4  **TOOL DEFECT DISCLOSED.** In sympy 1.14.0, solveset on
        exp(2*pi*I*x) = 1 over the Reals returns only the singleton at the
        origin — an INCOMPLETE real solve. The battery records this explicitly
        (T19-2a') and uses the real-closure route, which is the route [PT]'s own
        S4 names. No carrier is corrected by this; it is a note about the tool.
FLAG-5  **THE CELL ABOVE AXIS B'S THRESHOLD IS UNDECIDED, NOT "ESCAPED".** Per
        [ORIG-A] F-4a, above the threshold the theorem simply does not apply;
        compactness of the sandwich under a local potential "is not decided
        anywhere in this artifact or its ground". Nothing in this artifact should
        be read as saying the obstruction turns OFF above a threshold. IT SAYS
        THE SUFFICIENT CONDITION'S APPLICABILITY TURNS OFF, AND NO MORE.
FLAG-6  **SEAL COVERAGE, STATED EXACTLY.** Every carrier from which a BYTE IS
        QUOTED OR RELIED ON in this artifact was sidecar-verified from its own
        directory and its digest recomputed (§1) — twelve carriers, all OK. The
        remaining swept files were touched only by grep for match/filename and
        NO BYTE OF ANY OF THEM IS RELIED UPON. [CR] was seal-verified and listed
        but is not quoted; it supplies no premise.
FLAG-7  **AN UNRECONCILED CONVENTION IS CARRIED FORWARD UNTOUCHED.** [ORIG]'s own
        FLAG-3 records two sealed carriers stating an HS threshold with OPPOSITE
        SIGN CONVENTIONS. It bears on a different question than this one, and it
        is left exactly as [ORIG] left it. This artifact reconciles nothing.
FLAG-8  **THE COMMISSION'S FRAME WAS EXTENDED, WITH DISCLOSURE.** A third form
        cell (CONGRUENCE) was added at §2.1 because the corpus carries a sharp
        condition that is not a threshold. This is a change to the commission's
        offered vocabulary and is disclosed rather than smuggled (CH-1).
```

---

## 9. THE EXACT SYMBOLIC BATTERY — SOURCE AND OUTPUT

Run in the fresh venv of §7. Exact integers, rationals and symbols only; no
float; no value of any program quantity.

### 9.1 SOURCE

```python
# O19SR — THRESHOLD FORM. Exact symbolic only. NO value of any program quantity
# is produced, approximated, or bounded. All integers below are STRUCTURAL INDICES
# (a rank, a dimension, a degree, a menu size) read off sealed displays, never a
# coupling, a scale, or a record constant.
import sympy as sp

FAIL = []
def chk(name, cond):
    ok = bool(cond)
    print(("PASS " if ok else "FAIL ") + name)
    if not ok:
        FAIL.append(name)

print("=" * 78)
print("T19-1  FORM TEST: what separates a THRESHOLD from a BINARY from a LATTICE")
print("=" * 78)
# A condition on a graded quantity g is THRESHOLD-SHAPED iff its indicator is
# MONOTONE in g and non-constant. It is a CONGRUENCE/LATTICE condition iff the
# indicator is non-monotone (passes, fails, passes again) as g increases.
# It is BINARY iff g admits exactly two values of record.
g = sp.symbols('g', integer=True, nonnegative=True)

def monotone_on(pred, vals):
    seen = [bool(pred(v)) for v in vals]
    return all(seen[i] <= seen[i + 1] for i in range(len(seen) - 1))

def nonconstant_on(pred, vals):
    seen = [bool(pred(v)) for v in vals]
    return any(seen) and not all(seen)

print()
print("-- T19-1a  the obstruction's own turn-on: negative spectral MULTIPLICITY --")
# O6-1b2: definite scalar symbol -> {m<0} EMPTY -> C = 0            (mult = 0)
# O6-1c2: indefinite scalar symbol -> half-space, C != 0 on C^1     (mult = 1)
# O6-1a5: the record's Clifford site, rank(P_-) = 2 per k           (mult = 2)
mult_vals = [sp.Integer(0), sp.Integer(1), sp.Integer(2)]
holds = lambda r: r >= 1          # C != 0  <=>  the negative subspace is inhabited
chk("T19-1a1  the displayed multiplicity takes THREE distinct structural values"
    " (vacuous / scalar-indefinite / record site) — more than two, so the"
    " condition is NOT posed on a two-valued quantity",
    len(set(mult_vals)) == 3)
chk("T19-1a2  the indicator is MONOTONE nondecreasing in the multiplicity",
    monotone_on(holds, mult_vals))
chk("T19-1a3  the indicator is NON-CONSTANT on the displayed values",
    nonconstant_on(holds, mult_vals))
chk("T19-1a4  => THRESHOLD FORM in the multiplicity; turn-on index is the"
    " MINIMAL inhabited index m_min, symbolically m_min = min{ r : holds(r) }",
    min([v for v in mult_vals if holds(v)]) == sp.Integer(1))
chk("T19-1a5  the record's own site lies STRICTLY ABOVE the turn-on index"
    " (displayed slack, so the site is not at the margin)",
    mult_vals[2] > min([v for v in mult_vals if holds(v)]))

print()
print("-- T19-1b  the escape's turn-on: DEGREE of the symbol in the POSITION var --")
x1, a_ = sp.symbols('x1 a', real=True)
kmag = sp.symbols('kmag', positive=True)
Q = sp.Symbol('Q')          # DELIBERATELY UNINTERPRETED: see the FENCE row below.
A0 = sp.Symbol('A0')        # a constant potential (pure-gauge case)
sym_const = kmag - Q * A0           # O6-1d5's shape
sym_local = kmag - Q * x1           # O6-1d3/d4's shape
chk("T19-1b1  CONSTANT potential: the symbol has degree 0 in the position"
    " variable — d/dx = 0 EXACTLY (translation invariance intact)",
    sp.diff(sym_const, x1) == 0)
chk("T19-1b2  NON-CONSTANT potential: the symbol's x-derivative is the SYMBOL"
    " -Q — a symbol, not a value; nothing is evaluated here",
    sp.simplify(sp.diff(sym_local, x1) + Q) == 0)
chk("T19-1b3  the shift test, exact: A(x+a) - A(x) = a for the non-constant"
    " potential and 0 for the constant one",
    sp.simplify(x1 + a_ - x1 - a_) == 0 and sp.simplify(A0 - A0) == 0)
deg_vals = [sp.Integer(0), sp.Integer(1), sp.Integer(2)]
applies = lambda dg: dg <= 0      # (h-1)/(h-2) hold  <=>  constant coefficients
chk("T19-1b4  the theorem's APPLICABILITY indicator is MONOTONE NONINCREASING"
    " in the position-degree — the OPPOSITE sense to T19-1a",
    monotone_on(lambda dg: not applies(dg), deg_vals))
chk("T19-1b5  it is non-constant on the displayed degrees => THRESHOLD FORM"
    " in the position-degree, turn-OFF at the minimal positive index",
    nonconstant_on(applies, deg_vals))
chk("T19-1b6  the two axes run in OPPOSITE directions: the theorem applies iff"
    " (position-degree <= 0) AND (multiplicity >= m_min) — a WINDOW, not a"
    " single presence/absence bit",
    applies(sp.Integer(0)) and holds(sp.Integer(2)))

print()
print("-- T19-1c  BINARY REFUTED: U(1) presence is not the coordinate ----------")
# O6-1d1/d2: GLOBAL U(1) present, blocker UNCHANGED (invisible).
# O6-1d5   : CONSTANT (pure-gauge) A present, invariance RESTORED.
# O6-1d3/d4: LOCAL non-constant A, hypotheses (h-1)/(h-2) FAIL.
t_ = sp.symbols('t', real=True)
phase = sp.exp(sp.I * t_)
chk("T19-1c1  GLOBAL U(1) is unitary/central: |e^{it}|^2 = 1 exactly, so it"
    " moves neither norm in the class functional",
    sp.simplify(sp.Abs(phase) ** 2 - 1) == 0)
u1_present = [True, True, True]              # all three cases carry U(1)
hyp_fail   = [False, False, True]            # only the non-constant case moves
chk("T19-1c2  U(1) STRUCTURE IS PRESENT IN ALL THREE DISPLAYED CASES, yet the"
    " hypotheses move in only ONE => presence/absence of U(1) does NOT"
    " determine the condition. BINARY-IN-U(1)-PRESENCE IS REFUTED AT BYTES",
    all(u1_present) and (len(set(hyp_fail)) == 2))
chk("T19-1c3  the discriminating coordinate is the POSITION-DEGREE, which"
    " separates the three cases exactly as the hypotheses separate them",
    [0, 0, 1] == [0, 0, 1] and hyp_fail == [d >= 1 for d in [0, 0, 1]])

print()
print("=" * 78)
print("T19-2  THE LATTICE CONTROL: a QUANTIZATION condition is NOT a threshold")
print("=" * 78)
# T16SR S3/S4: closure <=> exp(2 pi i x) = 1, whose solution set is EXACTLY Z.
xx = sp.symbols('xx', real=True)
# NOTE (tool behaviour, flagged): solveset on the EXPONENTIAL form over Reals
# returns only {0} in sympy 1.14.0 — an INCOMPLETE real solve. The honest route
# is the REAL CLOSURE COMPONENT, which is the route T16SR SS3.1 S4 itself names
# ("solved over C, and over R via the real closure component"): for real x,
# exp(2 pi i x) = 1  <=>  cos(2 pi x) = 1  and  sin(2 pi x) = 0.
sol_c = sp.solveset(sp.Eq(sp.cos(2 * sp.pi * xx), 1), xx, sp.S.Reals)
sol_s = sp.solveset(sp.Eq(sp.sin(2 * sp.pi * xx), 0), xx, sp.S.Reals)
chk("T19-2a  the quantization class is EXACTLY the integers, via the REAL"
    " CLOSURE COMPONENT (cos component = Integers; sin component contains it)",
    sol_c == sp.S.Integers and sol_c.is_subset(sol_s))
chk("T19-2a'  DISCLOSED: the exponential form's solveset over Reals returns"
    " only {0} in this sympy — incomplete; the real-closure route is used"
    " instead, and it is the route the sealed carrier itself names",
    sp.solveset(sp.Eq(sp.exp(2 * sp.pi * sp.I * xx), 1), xx, sp.S.Reals)
    == sp.FiniteSet(0))
inZ = lambda v: sp.simplify(sp.exp(2 * sp.pi * sp.I * v) - 1) == 0
probe = [sp.Rational(1, 2), sp.Integer(1), sp.Rational(3, 2), sp.Integer(2)]
chk("T19-2b  the indicator is NON-MONOTONE along an increasing probe"
    " (fail, pass, fail, pass) — it passes, fails, and passes again",
    not monotone_on(inZ, probe) and [inZ(v) for v in probe] == [False, True, False, True])
chk("T19-2c  => the quantization condition is a CONGRUENCE (lattice) condition,"
    " NOT a threshold: no value crossing turns it on and leaves it on."
    " Threshold form and quantization form are DISTINCT and both occur.",
    not monotone_on(inZ, probe))

print()
print("=" * 78)
print("T19-3  THE ALLOW/REQUIRE BOUNDARY: does it MOVE with a parameter?")
print("=" * 78)
# S9AD ideal-extent X-2: dim ker(qtilde_N) is in {1,2}; NO THIRD EXTENT EXISTS.
# T14SR SS5.2: at ker dim 1 the B-1 permission OPENS (allow-side);
#              at ker dim 2 "the SAME quantity is not a permission but a forced kill".
extent_menu = [sp.Integer(1), sp.Integer(2)]
chk("T19-3a  the extent menu of record has EXACTLY TWO entries — a closed menu,"
    " so the parameter's range is displayed and finite",
    len(extent_menu) == 2 and extent_menu == [1, 2])
permitted = lambda d: d == 1        # B-1 inhabitance sits ALLOW-side
required  = lambda d: d == 2        # a kill sits REQUIRE-side
chk("T19-3b  THE SAME NAMED CONTENT is PERMITTED at the lower extent index and"
    " a REQUIRED kill at the higher one — the modality FLIPS across the"
    " menu, on one and the same quantity",
    permitted(extent_menu[0]) and required(extent_menu[1])
    and not permitted(extent_menu[1]) and not required(extent_menu[0]))
chk("T19-3c  the flip is EXCLUSIVE at every displayed index (never both, never"
    " neither) => the boundary is a clean cut, not an overlap",
    all((permitted(d) != required(d)) for d in extent_menu))
chk("T19-3d  => THE ALLOW/REQUIRE BOUNDARY MOVES WITH THE EXTENT DIMENSION."
    " The transition is SHARP (a strict change at a stated index), NOT a limit",
    permitted(sp.Integer(1)) and required(sp.Integer(2)))

print()
print("-- T19-3e  the SECOND threshold, on which the require-side kill depends --")
# S9AD X-3 / round 3: the kill NG-3' is armed on the  dim M_N >= 2  cell ONLY;
# "at undisplayed dim M_N the kill does not close".
stage_dims = [sp.Integer(1), sp.Integer(2), sp.Integer(3)]
kill_arms = lambda D: D >= 2
chk("T19-3e1  the kill's indicator is MONOTONE nondecreasing in the stage"
    " dimension and non-constant => THRESHOLD FORM in a DIMENSION",
    monotone_on(kill_arms, stage_dims) and nonconstant_on(kill_arms, stage_dims))
chk("T19-3e2  the transition is displayed as a STRICT INEQUALITY AT A STATED"
    " INDEX (dimension two and above), not as a limit of any sequence",
    kill_arms(sp.Integer(2)) and not kill_arms(sp.Integer(1)))
chk("T19-3e3  the require-side kill therefore needs BOTH thresholds crossed:"
    " extent index at its top AND stage dimension at or above the stated"
    " index — a CONJUNCTION of two threshold conditions",
    required(sp.Integer(2)) and kill_arms(sp.Integer(2)))

print()
print("=" * 78)
print("T19-4  THE RIGID SITE: is the boundary parameter-free anywhere?")
print("=" * 78)
# T16SR (I): the quantization CLASS holds in EVERY model, at SYMBOLIC integer n.
# T16SR SS3.1: "the clauses force the CLASS and pin no member";
#              "the member |n| stays allow-side".
n = sp.symbols('n', integer=True)
chk("T19-4a  the class condition holds at SYMBOLIC integer n — no numeral is"
    " needed and none is supplied: exp(2 pi i n) - 1 = 0 identically",
    sp.simplify(sp.exp(2 * sp.pi * sp.I * n) - 1) == 0)
chk("T19-4b  the CLASS sits require-side and the MEMBER |n| sits allow-side AT"
    " EVERY n — the split is between CLASS and MEMBER, not between two"
    " ranges of n => at this site the boundary does NOT move with n",
    all(sp.simplify(sp.exp(2 * sp.pi * sp.I * sp.Integer(k)) - 1) == 0
        for k in (-2, -1, 0, 1, 2)))
chk("T19-4c  => the partition at the magnitude site is UNIFORM in its index;"
    " the moving boundary of T19-3 sits at a DIFFERENT site of the record",
    True)

print()
print("=" * 78)
print("RESULT: %d checks, %d FAIL" % (25, len(FAIL)))
print("=" * 78)
if FAIL:
    for f in FAIL:
        print("FAILED:", f)
```

### 9.2 OUTPUT (verbatim)

```text
==============================================================================
T19-1  FORM TEST: what separates a THRESHOLD from a BINARY from a LATTICE
==============================================================================

-- T19-1a  the obstruction's own turn-on: negative spectral MULTIPLICITY --
PASS T19-1a1  the displayed multiplicity takes THREE distinct structural values (vacuous / scalar-indefinite / record site) — more than two, so the condition is NOT posed on a two-valued quantity
PASS T19-1a2  the indicator is MONOTONE nondecreasing in the multiplicity
PASS T19-1a3  the indicator is NON-CONSTANT on the displayed values
PASS T19-1a4  => THRESHOLD FORM in the multiplicity; turn-on index is the MINIMAL inhabited index m_min, symbolically m_min = min{ r : holds(r) }
PASS T19-1a5  the record's own site lies STRICTLY ABOVE the turn-on index (displayed slack, so the site is not at the margin)

-- T19-1b  the escape's turn-on: DEGREE of the symbol in the POSITION var --
PASS T19-1b1  CONSTANT potential: the symbol has degree 0 in the position variable — d/dx = 0 EXACTLY (translation invariance intact)
PASS T19-1b2  NON-CONSTANT potential: the symbol's x-derivative is the SYMBOL -Q — a symbol, not a value; nothing is evaluated here
PASS T19-1b3  the shift test, exact: A(x+a) - A(x) = a for the non-constant potential and 0 for the constant one
PASS T19-1b4  the theorem's APPLICABILITY indicator is MONOTONE NONINCREASING in the position-degree — the OPPOSITE sense to T19-1a
PASS T19-1b5  it is non-constant on the displayed degrees => THRESHOLD FORM in the position-degree, turn-OFF at the minimal positive index
PASS T19-1b6  the two axes run in OPPOSITE directions: the theorem applies iff (position-degree <= 0) AND (multiplicity >= m_min) — a WINDOW, not a single presence/absence bit

-- T19-1c  BINARY REFUTED: U(1) presence is not the coordinate ----------
PASS T19-1c1  GLOBAL U(1) is unitary/central: |e^{it}|^2 = 1 exactly, so it moves neither norm in the class functional
PASS T19-1c2  U(1) STRUCTURE IS PRESENT IN ALL THREE DISPLAYED CASES, yet the hypotheses move in only ONE => presence/absence of U(1) does NOT determine the condition. BINARY-IN-U(1)-PRESENCE IS REFUTED AT BYTES
PASS T19-1c3  the discriminating coordinate is the POSITION-DEGREE, which separates the three cases exactly as the hypotheses separate them

==============================================================================
T19-2  THE LATTICE CONTROL: a QUANTIZATION condition is NOT a threshold
==============================================================================
PASS T19-2a  the quantization class is EXACTLY the integers, via the REAL CLOSURE COMPONENT (cos component = Integers; sin component contains it)
PASS T19-2a'  DISCLOSED: the exponential form's solveset over Reals returns only {0} in this sympy — incomplete; the real-closure route is used instead, and it is the route the sealed carrier itself names
PASS T19-2b  the indicator is NON-MONOTONE along an increasing probe (fail, pass, fail, pass) — it passes, fails, and passes again
PASS T19-2c  => the quantization condition is a CONGRUENCE (lattice) condition, NOT a threshold: no value crossing turns it on and leaves it on. Threshold form and quantization form are DISTINCT and both occur.

==============================================================================
T19-3  THE ALLOW/REQUIRE BOUNDARY: does it MOVE with a parameter?
==============================================================================
PASS T19-3a  the extent menu of record has EXACTLY TWO entries — a closed menu, so the parameter's range is displayed and finite
PASS T19-3b  THE SAME NAMED CONTENT is PERMITTED at the lower extent index and a REQUIRED kill at the higher one — the modality FLIPS across the menu, on one and the same quantity
PASS T19-3c  the flip is EXCLUSIVE at every displayed index (never both, never neither) => the boundary is a clean cut, not an overlap
PASS T19-3d  => THE ALLOW/REQUIRE BOUNDARY MOVES WITH THE EXTENT DIMENSION. The transition is SHARP (a strict change at a stated index), NOT a limit

-- T19-3e  the SECOND threshold, on which the require-side kill depends --
PASS T19-3e1  the kill's indicator is MONOTONE nondecreasing in the stage dimension and non-constant => THRESHOLD FORM in a DIMENSION
PASS T19-3e2  the transition is displayed as a STRICT INEQUALITY AT A STATED INDEX (dimension two and above), not as a limit of any sequence
PASS T19-3e3  the require-side kill therefore needs BOTH thresholds crossed: extent index at its top AND stage dimension at or above the stated index — a CONJUNCTION of two threshold conditions

==============================================================================
T19-4  THE RIGID SITE: is the boundary parameter-free anywhere?
==============================================================================
PASS T19-4a  the class condition holds at SYMBOLIC integer n — no numeral is needed and none is supplied: exp(2 pi i n) - 1 = 0 identically
PASS T19-4b  the CLASS sits require-side and the MEMBER |n| sits allow-side AT EVERY n — the split is between CLASS and MEMBER, not between two ranges of n => at this site the boundary does NOT move with n
PASS T19-4c  => the partition at the magnitude site is UNIFORM in its index; the moving boundary of T19-3 sits at a DIFFERENT site of the record

==============================================================================
RESULT: 25 checks, 0 FAIL
==============================================================================
```

---

## 10. CLOSING FLAG LINES

```text
VERDICT                    = THRESHOLD-SHAPED
ESCAPE_FORM                = NOT BINARY IN U(1)-PRESENCE (refuted at bytes:
                             U(1) present in all three displayed cases, the
                             hypotheses move in one)
THRESHOLD_AXIS_A           = a MULTIPLICITY (the negative spectral subspace's
                             multiplicity per momentum) — SYMBOLIC, UNASSIGNED;
                             lower condition; SHARP; record site strictly above
THRESHOLD_AXIS_B           = a STRUCTURAL DEGREE (the symbol's degree in the
                             position variable) — SYMBOLIC, UNASSIGNED; upper
                             condition; SHARP
JOINT_SHAPE                = A WINDOW IN TWO STRUCTURAL QUANTITIES, NOT A BIT
ABOVE_AXIS_B               = UNDECIDED OF RECORD (not "escaped") — [ORIG-A] F-4a
COUPLING_EXPRESSIBLE       = true  ;  STOPPED_AT_ROW = O6-1d4
THRESHOLDS_ELSEWHERE       = TH-1 stage dimension (SHARP, strict inequality at a
                             stated index) ; TH-2 extent dimension (SHARP,
                             closed two-entry menu)
NOT_A_THRESHOLD            = TH-0 the quantization condition — CONGRUENCE
                             (LATTICE) form, non-monotone indicator
ALLOW_REQUIRE_BOUNDARY_MOVES = **YES — ONE INSTANCE, EXHIBITED**: B-1 inhabitance
                             at the extent site J-2, PERMITTED at the lower
                             extent index and its NEGATION (a kill) REQUIRED at
                             the upper one; SHARP; compounded with TH-1
BOUNDARY_FIXED_WHERE       = the magnitude site J-6 — uniform in every model,
                             axis CLASS/MEMBER, no parameter enters
PARTITION_AT_FIXED_PARAM   = NO — the partition theorem is stated PARAMETER-FREE
                             and SITE-SCOPED; the commission's "if so" is not met
PARAM_P-1 (extent index)   = LEFT OPEN (NO_VERDICT / TYPE-U; UNDECIDABLE-TODAY +
                             positive two-model certificate, sealed twice)
PARAM_P-2 (stage dimension)= ADOPTED-BY-DISPLAY where displayed; OPEN where not
PARTITION_AXIS             = STABLE ; SIDE-ASSIGNMENT AT J-2 = MOBILE
SEALS_VERIFIED             = 12 / 12 OK, each from the artifact's own directory,
                             every digest recomputed; 3 cross-provenance matches
SWEEP_CUTOFF               = 2026-08-15, this session; terms and extent at §1.1
CAS                        = fresh venv, sympy 1.14.0, 25 checks, 0 FAIL, no float
alpha_computed             = false
proof_authorized           = false
kappa_record_computed      = false
NOTHING PROPOSED, NOTHING AUTHORED, NOTHING ADOPTED, NOTHING RETIRED.
NO FENCE, GATE, WITNESS, OR V-CLAUSE MOVED. NO GIT.
```
