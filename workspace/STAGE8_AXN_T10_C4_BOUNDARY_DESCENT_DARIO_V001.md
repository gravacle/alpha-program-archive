# STAGE 8 — T10 CONTRACT, STAGE C4 — BOUNDARY-CLOSURE-DESCENT-ATTACK
## DARIO LANE (Builder B) — RELAY 880 — [PLAN:AXN-BUILD-B8]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

No member bound; no fixed-point execution; no end test; no smooth import; no EM identification; no
common cell formed; no junction map evaluated; no numeric evaluation of any physical quantity; no
comparison to measured constants. PE-1..PE-13 pointer-only, none opened or consulted. Builder-A code
not opened. `~/.codex` untouched; memory-bank never searched. No register, plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.**

**THIS IS A FAILURE-CAPABLE BOUNDED ATTACK.** Per the relay and the contract, whatever results **may
not** be reported as a completed action, a closure, a T10 discharge, or a slot-2 discharge.
**`DISCHARGE = NOT-CLAIMED` was fixed before the attack began, not concluded at its end.**

**THE CONTRACT GOVERNS OVER THE PASTE**, as the relay directs. I read §3.4 verbatim from
`STAGE8_AXN_T10_DERIVATION_CONTRACT_CODEX2_V001.md` = `11a662fef62ca12f` (seal verified BEFORE
reading) and executed **its** input freeze, products, stops and forbidden list — not the paste's
summary of them. **The terminal values below are transcribed from the contract's own stop list.**

**INPUT FREEZE VERIFIED AT ITS PIN:** boundary-data span `[2165,7160)` **computed
`0c6b792d1c7f2973` = pinned**; the 857 structural-carrier sentence located at byte 13212; B0 census
`a614a694…`; round-2 frontier `5e163d85…`. **Measure and contour fields excluded, as the freeze
requires — and none is touched below.**

**THE HAZARD, NAMED BECAUSE IT IS MINE.** At 878 I ruled `T10 = STRUCTURAL-ADMISSIBLE` and wrote that
it *"is one word away from reading as permission, and an assembly lane could take it as licence to
write a structural gravity object."* **Boundary-closure descent is exactly the route that ruling
un-barred, and I am now the lane running it.** Producing a boundary-closure object that quietly
constitutes the gravitational sector would cash my own un-barring. **Nothing below constitutes
anything.**

---

## 0. LEAD

**The attack runs, produces one of its four required products, and stops at five of its six typed
stops. The sixth is not reached.**

```text
FOUR NODES COMPOSE.  NOT ONE ARROW BETWEEN THEM IS DERIVED.
```

That is the same shape as my 873 assembly finding — layers that compose and a joint that does not —
**one level down, and this time with four nodes and four missing edges instead of two layers and one
missing joint.**

| product required by C4 | result |
|---|---|
| 1. carrier diagram distinguishing the four objects | **PRODUCED** — §1 |
| 2. a sourced correspondence **if one exists** | **NONE EXISTS** — hunted, not assumed; §2 |
| 3. global-to-cell restriction/descent theorem | **ABSENT** — §3 |
| 4. boundary/domain-typed gravitational variation/Euler receiver | **TYPING PRESENT, RECEIVER ABSENT** — §4 |

---

## 1. PRODUCT 1 — THE CARRIER DIAGRAM (produced)

Four distinct objects, each content-addressed, **none identified with another**:

```text
  N1  B0                          CompleteMicroscopicBoundaryOriginCandidate
                                  (Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)
      status: UNINHABITED         B0 census a614a694..., row 1 = MATERIAL-ABSENT,
                                  "no frozen inhabitant"

  N2  the complete Boundary-Resolved generator
      status: NOT BUILT           SC5-E's closer, not present of record

  N3  the global Dirac boundary form  (the CTP vanishing target)
      status: TARGET, stated      "make the global Dirac boundary form vanish under the
                                  CTP preparation/gluing variations"  — an obligation ON N2
      DISCLOSED: wrong generator and scale, per Q-750/752 and my own 846

  N4  the per-cell closure-face receivers  { closure face of Omega_c }_{c in C_p}
      status: CARRIERS TYPED, RECEIVERS NOT CONSTRUCTED
                                  SC3/C1 type Omega_c per primitive incidence;
                                  no closure-face receiver object is built
```

**Required arrows, and every one of them is missing:**

```text
  N1 --correspondence--> N2   :  SEALED FALSE   (§2)
  N2 --carries--------> N3    :  the vanishing condition is an OBLIGATION ON N2, not a derived map
  N3 --descent-------> N4     :  ABSENT         (§3)
  N4 --Euler receiver-> T10   :  TYPING PRESENT, RECEIVER ABSENT   (§4)
```

**FORBIDDEN-LIST COMPLIANCE, stated per item because this is where the diagram could have cheated:**
`N1` is **not** identified with `N2`; no global condition is relabeled per-cell (there is no
restriction theorem, so §3 stops rather than relabels); **no measure or contour is inserted** — the
freeze excludes them and nothing below reaches for them; **this diagram is expressly NOT a completed
descent**; and no downstream boundary data is used to reconstruct `B0` backwards — which the B0 bar
forbids in terms (*"the construction rule may not receive any descendant or discharge flag as an
input"*).

---

## 2. PRODUCT 2 — THE CORRESPONDENCE: **none exists**

The contract says *"a sourced correspondence **if one exists**"* — so I hunted rather than assumed.

**Law 9b, closure declared first.** `C4_CLOSURE :=` {the 857 row-typing's cited sources; boundary data
`887b0107…`; B0 census `a614a694…`; round-2 frontier `5e163d85…`; the B0 stop spec `ec848d29…`; the
boundary-route cross-check; the R3_4 parent SPEC's fifteen pinned authorities}. **"Missing" below
means missing from that declared closure**, not from a prose sweep.

Result: the identifier `B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived` occurs in four
sealed files across the closure, and **every occurrence carries the same single value**:

```text
B0_TO_BOUNDARY_RESOLVED_GENERATOR_CORRESPONDENCE_derived = false      (sole value, all occurrences)
```

with the would-build *"independently type both objects and derive a direction-bearing map"*, and the
governing sentence from my own 846:

> *"That source **does not equate** its complete Boundary-Resolved generator with package B0. The
> causal/boundary conditions are obligations on the named generator; their bearing on B0 **requires a
> separately derived correspondence**."*

**`CORRESPONDENCE_GAP` fires.** No correspondence exists, and identifying `N1` with `N2` is the
contract's first forbidden move.

---

## 3. PRODUCT 3 — THE GLOBAL-TO-CELL DESCENT: **absent**

Vocabulary is not thin — within the workspace, `restriction theorem` 24 files, `descent theorem` 9,
`closure-face`/`closure face` 27/30. **So the absence is not a failure to find words.**

**No `source_grounded_restriction*` flag anywhere carries `= true`.** And the object is already typed
of record as **SC5-E**, whose closer is *"complete Boundary-Resolved generator; per-cell/closure-face
carrier; source-grounded restriction/descent theorem"* with the disposition **"Law 9 found no carrier,
schema, or restriction theorem"** — a `MATERIAL-ABSENCE` I confirmed myself at 854 and again in the
858 row pass.

**`DESCENT_GAP` fires.** This is Q-750's third lack, confirmed at Q-752, and it is unchanged.

---

## 4. PRODUCT 4 — THE GRAVITATIONAL EULER RECEIVER: **typing present, receiver absent**

The **typing** is real and sealed — this is the material-present subcomponent the contract's C4
preamble names:

- gravity is **constituted by accumulated source-capacity boundary closure**, with `G` as the
  primitive source-geometry conversion (857's structural carrier, located at byte 13212);
- the receiver is **variation-shaped**: round 2's own phrasing names a *"metric Euler receiver"* as
  the object that is missing, alongside *"background `g` is not gravity dynamics"*.

**The receiver itself is not constructed.** Round 2 records the finite parent as providing no
*"gravitational action, metric Euler receiver, or measure"*, and no closure member supplies a
boundary/domain-typed gravitational Euler law.

**`DOMAIN_GAP` fires** — the boundary/domain package is incomplete: S3 (joint physical carrier and
boundary structure) and S4 (unbounded operator realization) both stand open, per my 864 and my 869
cross-check, and the U3 partial's four stops are unfilled.

**Held expressly:** *"constituted by boundary closure"* is a **sealed characterisation of gravity's
role**, not a construction I may complete. Completing it here is precisely the un-barred route my 878
warned must not be cashed. **It is not completed.**

---

## 5. THE STOP LEDGER — five fire, one is not reached

| stop | status |
|---|---|
| `B0_GAP` | **FIRES** — no inhabited `B0` object; census row 1 `MATERIAL-ABSENT`, *"no frozen inhabitant"* |
| `GENERATOR_GAP` | **FIRES** — no complete Boundary-Resolved generator; SC5-E/SC5-B closer not present |
| `CORRESPONDENCE_GAP` | **FIRES** — sealed `= false`, sole value across the closure (§2) |
| `DESCENT_GAP` | **FIRES** — no source-grounded restriction/descent theorem (§3) |
| `DOMAIN_GAP` | **FIRES** — boundary/domain package incomplete; S3 and S4 open (§4) |
| `NATURALITY_GAP` | **NOT REACHED — prerequisite absent** |

**The sixth is the one worth getting right, and I decline to fire it.** `NATURALITY_GAP` asks whether
*the descent commutes with admitted restriction*. **There is no descent** (§3). A stop reported as
*fired* would assert that a descent exists and fails to commute; that is a stronger and **false**
claim. **Reporting an untestable condition as a failure would manufacture a finding**, which is the
failure mode I have twice been caught in this session (860's grading probe; 857's cross-object
bridge). **`NOT REACHED` is the accurate typing.**

---

## 6. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the boundary form as a TARGET on a DIFFERENT named generator, with wrong generator
  and scale DISCLOSED (Q-750/752, my 846); "gravity is constituted by accumulated source-capacity
  boundary closure" as a SEALED CHARACTERISATION and NOT a construction to complete; G as PRIMITIVE
  CONVERSION; the SC5 roots at their statuses (A/D/E per Q-754/756); the partial U3's four stops
  UNFILLED; B0's bar, including that its construction rule may receive no descendant or discharge flag
  as input; the correspondence flag at its sole sealed value.

DERIVED HERE: (a) the four-node carrier diagram with all four required arrows shown missing; (b) the
  correspondence hunt over a DECLARED closure, returning one sealed value; (c) the observation that
  descent vocabulary is abundant while no flag carries "= true", so the absence is not a search
  artifact; (d) the typing/receiver split at product 4; (e) the NATURALITY_GAP NOT-REACHED typing and
  the reason it is not a firing.

SELECTED HERE: NOTHING.  No B0 candidate, generator, correspondence, restriction theorem, descent,
  closure-face receiver, Euler law, boundary datum, measure, or contour is constructed, chosen,
  identified, relabeled, or promoted.  NO ARROW IS DRAWN THAT THE RECORD DOES NOT DERIVE.  NO FLAG
  MOVES.  DISCHARGE = NOT-CLAIMED.

NOT DONE AND DISCLOSED: I did not attempt any of the four missing objects — each is a named stop and
  attempting one would exceed a bounded attack.  My descent finding rests on the declared closure plus
  a flag-value check, NOT on per-file resolution of the 24 restriction-theorem and 30 closure-face
  files; SC5-E's ABSENT typing is carried from 854/858 rather than re-derived here.  I did not test
  naturality, because its prerequisite is absent.

SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at product 4 and at the diagram.** The available cheat was to take
*"gravity is constituted by boundary closure"* — a sealed, structural, material-present sentence — and
read it as licence to write the closure. **It is a characterisation of a role, not a construction, and
it is not completed.** The second was to draw the `N3 → N4` arrow "by restriction" and call the global
condition per-cell; **the contract forbids exactly that and §3 stops instead of relabeling.**
**S12** — every status carried as the status it is; `TARGET` never drifting to installed; `= false`
carried as a sealed negative.
**S26 / S08 / S19 / S24** untouched — measure and contour are **excluded by the freeze** and are not
reached for anywhere.
**T1 / T5** untouched.
**BR-1 HELD.** The contract is a producer-declared object and got no weight toward itself: its C4
preamble's *"MATERIAL = PARTIAL, with an exact material-present subcomponent"* was treated as the
claim under test, and §4 splits it into a present **typing** and an absent **receiver** rather than
adopting it whole.

---

## 7. FINAL LINES — the C4 stage's typed terminal values, as the contract states them

```text
B0_GAP             = FIRES.  No inhabited B0 object.  B0 census a614a694..., row 1 MATERIAL-ABSENT,
                     "no frozen inhabitant"; Obj_B0/Sig_B0/Carrier_B0/Core_B0/Prov_B0 has open type,
                     signature, representation, maps and equivalence.
GENERATOR_GAP      = FIRES.  No complete Boundary-Resolved generator.  It is the closer of SC5-E and
                     SC5-B and is not present of record.
CORRESPONDENCE_GAP = FIRES.  HUNTED, NOT ASSUMED, over a closure DECLARED FIRST (law 9b): the
                     identifier occurs in four sealed files across C4_CLOSURE and EVERY OCCURRENCE
                     CARRIES THE SAME SINGLE VALUE — B0_TO_BOUNDARY_RESOLVED_GENERATOR_
                     CORRESPONDENCE_derived = false — with the would-build "independently type both
                     objects and derive a direction-bearing map", and the governing sentence that the
                     source "DOES NOT EQUATE its complete Boundary-Resolved generator with package B0
                     ... their bearing on B0 REQUIRES A SEPARATELY DERIVED CORRESPONDENCE."
DESCENT_GAP        = FIRES.  No global-to-cell restriction/descent theorem.  THE ABSENCE IS NOT A
                     SEARCH ARTIFACT: descent vocabulary is abundant in the workspace (restriction
                     theorem 24 files, descent theorem 9, closure-face/closure face 27/30) and NO
                     source_grounded_restriction* FLAG ANYWHERE CARRIES "= true".  The object is
                     SC5-E, disposition "Law 9 found no carrier, schema, or restriction theorem" —
                     Q-750's third lack, confirmed at Q-752, unchanged.
DOMAIN_GAP         = FIRES.  The boundary/domain package is incomplete: S3 (joint physical carrier and
                     boundary structure) and S4 (unbounded operator realization) both stand open per
                     my 864 and my 869 cross-check, and the partial U3's four stops are unfilled.
NATURALITY_GAP     = NOT REACHED — PREREQUISITE ABSENT, AND I DECLINE TO FIRE IT.  The stop asks
                     whether THE DESCENT commutes with admitted restriction; THERE IS NO DESCENT.
                     Reporting it as FIRED would assert that a descent exists and fails to commute —
                     a STRONGER AND FALSE claim.  Reporting an untestable condition as a failure would
                     MANUFACTURE A FINDING, the failure mode I was caught in twice this session.
MUST-PRODUCE LEDGER: product 1 CARRIER DIAGRAM = PRODUCED (four content-addressed nodes — B0; the
                     complete Boundary-Resolved generator; the global Dirac boundary form as a TARGET
                     with wrong generator and scale DISCLOSED; the per-cell closure-face receivers
                     with carriers typed and receivers unbuilt).  FOUR NODES COMPOSE AND NOT ONE ARROW
                     BETWEEN THEM IS DERIVED — the same shape as my 873 assembly finding, one level
                     down, four missing edges instead of one missing joint.  Product 2 CORRESPONDENCE
                     = NONE EXISTS.  Product 3 DESCENT THEOREM = ABSENT.  Product 4 EULER RECEIVER =
                     TYPING PRESENT, RECEIVER ABSENT — gravity IS sealed as "constituted by
                     accumulated source-capacity boundary closure" with G as primitive conversion, and
                     the receiver is variation-shaped, but NO boundary/domain-typed gravitational
                     Euler law is constructed.  THE CHARACTERISATION IS OF A ROLE, NOT A CONSTRUCTION
                     TO COMPLETE, AND IT IS NOT COMPLETED.
C4_RESULT          = ATTACK RUN, STOPPED.  One of four products delivered; five stops fired; one not
                     reached.  C4 IS NOT DISCHARGED AND CANNOT BE WITHOUT THE MISSING OBJECTS, exactly
                     as the contract's STARTABILITY note anticipated.
DISCHARGE = NOT-CLAIMED.  Not a completed action, not a closure, not a T10 discharge, not a slot-2
  discharge.  Fixed before the attack began.  NO FLAG MOVES.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) I AM THE LANE MY OWN 878 WARNED ABOUT.  Two relays ago I ruled T10 STRUCTURAL-ADMISSIBLE and
      wrote that it "is one word away from reading as permission, and an assembly lane could take it
      as licence to write a structural gravity object."  BOUNDARY-CLOSURE DESCENT IS EXACTLY THE ROUTE
      THAT RULING UN-BARRED, AND I RAN IT.  The restraint that mattered was at product 4: "gravity is
      constituted by accumulated source-capacity boundary closure" is sealed, structural, and
      material-present, and completing it would have looked like executing my own ruling.  IT IS A
      CHARACTERISATION OF A ROLE AND IT IS NOT COMPLETED.
  (2) THE ONE JUDGMENT CALL IS NATURALITY_GAP, AND IT CUTS AGAINST A FULLER-LOOKING RESULT.  Firing
      all six stops would have read as a more thorough attack; it would also have asserted something
      false.  I record that the six-stop version was available and rejected, since a reader comparing
      stop counts across C-stages should know this one reports five-plus-one rather than six by
      DELIBERATE TYPING, not by oversight.
  (3) MY DESCENT FINDING IS THINNER THAN ITS CONFIDENCE SOUNDS.  It rests on the declared closure plus
      a flag-value check, NOT on per-file resolution of the 24 restriction-theorem and 30 closure-face
      files, and SC5-E's ABSENT typing is CARRIED FROM MY OWN 854/858 rather than re-derived here.
      Given that my enumerations have twice this session been cut by a bad query shape (869, 875), a
      descent theorem hiding under vocabulary I did not sweep is the live risk, AND THE OPPOSITE LANE
      SHOULD PRESS THERE RATHER THAN AT THE STOPS.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** Charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-13
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
