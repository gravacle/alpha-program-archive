# STAGE 8 — FORM-TO-HS BRIDGE: ADVERSARIAL AUDIT — S9AD AUDIT V001

## ADVERSARIAL AUDITOR — CODENAME FHB-AUDIT — DEFAULT: REFUTE — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15). Target:
`STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001.md` (9a295498, seal MATCH at path before
opening). Auditor NOT told the build's outcome. Gates LIVE THROUGHOUT:
`alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
`fhb_venv` under the session scratchpad, distinct from the build's `fhbvenv`);
no floats as ground; no value frozen. No fenced-class file opened knowingly
(ONE deviation disclosed, ch-1). "Q-..." tokens EXPECTED-UNLOCATABLE, noted
not chased. No git; ONE output + seal sidecar at this commission-distinct
path, probed ABSENT before write. Verdict menu: CONFIRMED |
CONFIRMED-WITH-CORRECTIONS | REFUTED-AT | UNDETERMINED-AT.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS. Every site verdict re-derives exactly: SITE (i)
R.3 = FAILS-AT(compression type) CONFIRMED — [C, h_0] = 0 preserves the form
bound verbatim (form-grade bridge exact) and h_0-in-class + C h_0 C = -|k|C(k)
(a nonzero translation-invariant multiplier, never HS) closes the HS question
negatively at the asset's own strength; SITE (ii) WALL F3 = PARTIAL CONFIRMED
— the compression lemma L-1..L-5 is exact, the carrier prices E_n = 6n^4/ell^2,
E_leak = 6n^3/ell^2, r_n = 4n^3 re-derive independently, sigma_{Q,2}(r) closed
form re-derives to the digit, and the n-uniform negative (FB9 non-implication)
is valid; SITE (iii) HPL layer-1 = FAILS-AT(smallness threshold) CONFIRMED —
sigma_{Q,2}(1/2) = (1303/210)e^{-1/4}/sqrt(pi) > 3909/1680 > 1 > log 2 exact;
FC-1 (the bulk term 2·1_B h_0 S missing from CERT §3.4(ii) and audit P-1) is
REAL — verified at CERT/audit bytes AND independently on my own state pair;
the three of-record blocks (B-L2*, beta, F1) are NOT re-walked and the shared
residue = MO-3/R-L2b/B-L2* is correctly named; witness mechanics clean
(retirement left to the registrar); the named hazard (form bound quoted at
op/HS grade without the licensing compression) is NOT committed anywhere.
THREE CORRECTIONS, none verdict-moving: COR-A the build's neg-2 "ORDER-SHARP
within the class" claim is FALSE at the HS grade — its own FB9 witness has
||Pi h Pi||_2^2 = E exactly, so actual/bound = 1/(2 sqrt(N)) -> 0; only the
trace display is order-sharp (limit 3/16). COR-B the headline enclosures are
not the build's own calculus's best: L-1 + sqrt(rank) + the exact carrier form
bound |p|^2 <= (6n-3)/ell^2 (from p^2 + x^2 = 2N+1, re-derived exactly) gives
||C_n T_g C_n||_2 <= 12 n^{3/2} sqrt(2n-1)/ell (order n^2, = 24 sqrt(6)/ell at
n = 2, vs the build's 12 sqrt(2) n^{7/2}/ell = 192/ell — exact factor chain
24 sqrt(6) < 192 iff 6 < 64) and ||.||_1 <= 24 n^3 sqrt(2n-1)/ell (order
n^{7/2} vs n^5); register the sharper displays if the enclosures are consumed.
COR-C site (iii)'s layer-1 pair coefficient is asserted "exact" but never
displayed; on the build's own §3.1 constants the factor is pi/2 > 1 (exact:
pi > 8/3 > 2), so the threshold conclusion survives a fortiori — complete the
display at registration. All divergences one-sided; all consuming quantities
remain UNDECIDED; no verdict, gate, fence, or witness moves. CAS: build
battery re-run verbatim 27/27 PASS in MY venv; pre-fix battery 18/18; audit
extension battery 11/11.**

---

## 1. INDEPENDENCE PROTOCOL — VERDICTS FIXED AND HASHED BEFORE OPENING

Before the build was opened, I worked the question from the of-record ground
alone (CERT :23-42/:307-360, WALL :399-420, BL2STAR :30-49 + CHECK :23-31,
E1 :805-820, r-2 :144-148) and hashed:

```text
98c69c9d1d70d015e6c66c67e02c335513b8daa6328b1aef378024378968bb57  FHB_AUDIT_PREFIXED_VERDICTS_V001.md
cb2216cbe4c10c9cb46446d7e0d266e1a39f15cae7063b59baa66360cc47b933  fhb_cas_v001.py   (18/18 PASS)
```

PRE-FIXED GLOBAL VERDICT (verbatim substance): no form->HS bridge exists on
sealed ground; the three blocks (B-L2* / beta / F1) stand; the only sound
build outcomes are NOT-BRIDGED / FAILS-AT, possibly with the compression
lemma as new EXACT content typed as NOT the consuming type, carrying no
|C|_4 power; any BRIDGED claim must exhibit (a) a certified C-L1 error
cancellation, (b) a beta derivation, or (c) a sub-volume HS rate (= MO-3).
Plus a six-item hazard checklist (H1-H6). MATCH AGAINST THE OPENED BUILD:
the build's outcome coincides with the pre-fix at every overlapping point —
no full bridge, blocks met again from inside, compression content typed
correctly, no |C|_4 power, no numeric beta, witness retirement left to the
registrar. SCOPE DISCLOSURE: my pre-fix worked the three of-record BLOCKS
(the commission's "three of-record blocks"); the build's commission's three
CONSUMING sites (R.3 / WALL F3 / HPL layer-1) were verified post-open by
exact independent re-derivation of every load-bearing display (§3-§4) — for
those, the independence protection is the re-derivation, not the pre-fix.

## 2. SEALS

Build: 9a295498... MATCH vs sidecar at path (verified BEFORE opening). All 9
of the build's consumed digests recomputed by me at path: e6838ffc, 180114a7,
80db260f, f5b887dc, f6f45e6d, 46846730, 789338ad, 3b5e95b6, bebc0f08 — 9/9
MATCH the build's seal table. Site pins verified at bytes: r-2 :144-148
(C = P_-, symbol (I - alpha.p-hat)/2); E1 :811-814 (A(0) = the C-L2 error on
the surviving sector, beta symbolic); WALL :399-420 (F1/F2/F3); HPL audit c-2
(two-layer residue, layer 1 = the C-L1/C-L2 deviation); PA A1 (3-D Hermite
carrier, oscillator length ell, 0 <= a,b,c < n, tensor C^4 => r_n = 4n^3);
CERT §3.4(ii) :307-331 and CERT-AUDIT :130 (both display the S-dressed
commutator SURFACE-ONLY — FC-1's two accused bytes confirmed).

## 3. THE ATTACK — WHAT WAS RE-DERIVED AND WHAT SURVIVED

(a-1) THE HYPOTHESIS CLASS. Bilinear sqrt(3)(||h_0 phi||||psi|| +
  ||phi||||h_0 psi||), quadratic 2 sqrt(3)||h_0 psi||||psi||, relative form
  bound ZERO: independently re-derived pre-open (my C1-C4: flip identity
  needs {alpha_j, S} = 0, matrix step -S alpha_j = alpha_j S, Cauchy-Schwarz
  over 3 axes, SOS eps-split). cl-2 (h_0 in class, constant 1 <= sqrt(3)):
  trivially correct and correctly load-bearing.
(a-2) THE COMPRESSION LEMMA L-1..L-5. Each line re-derived: L-1 from the
  bilinear display on unit vectors; L-2 via the quadratic display +
  Cauchy-Schwarz; L-3 via |t[e_i,e_j]| <= c(a_i + a_j) and
  Sum_{ij}(a_i+a_j)^2 = 2r Sum a^2 + 2(Sum a)^2 <= 4r Sum a^2; L-4 rank
  Cauchy-Schwarz; L-5 Schatten ideal property. SOUND. The lemma is the
  licensing compression step the hazard demands — present at every
  op/trace/HS-grade display in the build.
(a-3) CARRIER PRICES. E_n: Sum_{a,b,c<n}(a+b+c+3/2) = (3/2)n^4 re-derived
  (hand + CAS), spinor factor 4, => 6n^4/ell^2. E_leak: ladder one-shell
  argument re-derived (top shell count n^2 per axis, mass n/(2 ell^2),
  3 axes, spinor 4; off-axis cross terms vanish by orthogonality BEFORE the
  spinor trace) => 6n^3/ell^2. Constants 12 sqrt(2) n^{7/2}/ell and
  24 sqrt(2) n^5/ell arithmetically exact. CORRECT (but see COR-B).
(a-4) sigma_{Q,2}(r). Re-derived from scratch: Christoffel diagonal
  phi_0^2 + phi_1^2 = pi^{-1/2}e^{-u^2}(1+2u^2); spherical moments 1/3,
  1/15, 1/105; product average 1 + 2r^2 + (4/5)r^4 + (8/105)r^6; times
  4 (spinor) x 4 pi r^2 x pi^{-3/2} e^{-r^2}. At r = 1/2: 840+420+42+1 =
  1303 over 840, x4 = 1303/210. TO THE DIGIT. The threshold chain
  (e^{-1/4} > 3/4 from e < 256/81; pi^{-1/2} > 1/2 from pi < 4; log 2 < 1)
  is exact and float-free. The factorization ||Pi T_surf Pi||_1 <=
  ||R Pi||_2^2 = tr(Pi delta_dB Pi) via Hoelder with the pointwise-unitary
  vertex: SOUND.
(a-5) SITE (i) LOGIC. The negative has the correct logical shape: any
  inference "member of H(sqrt3) => C(.)C in HS" is refuted by the member
  h_0 (C h_0 C = -|k|C(k), nonzero translation-invariant => non-compact =>
  never HS; the formal-Lambda display is decoration, the compactness
  argument is the ground). The scope "from class membership alone" is
  honest; the extra input needed is correctly identified as the standing
  blocks. The form-grade bridge (constant unchanged through the commuting
  contraction sandwich) re-derives in one line: ||h_0 C psi|| =
  ||C h_0 psi|| <= ||h_0 psi||.
(a-6) SITE (iii) LOGIC. To certify |den_n - 1| < 1 the route must produce
  an upper bound < log 2 (Simon / det pins re-checked at scalar and 2x2
  grade); the route's minimal available ceiling includes the surface
  channel; sigma exceeds log 2 at the first sealed member and every other
  channel is larger — so the asset cannot certify, at any n. VALID given
  the layer-1 coefficient >= 1 (see COR-C). The "NOT REFUTED" hygiene
  (one-sided ceilings; cancellation invisible to the class; UNDECIDED
  stands) is correct and required by the fences.
(a-7) FC-1. Verified three independent ways: (i) at bytes — CERT §3.4(ii)
  and CERT-AUDIT :130 display i(alpha.n-hat (x) S)delta_dB with no bulk
  term; (ii) by derivation — the CERT's own §3.2 flip at M = 1_B yields
  i(alpha.n-hat (x) S)delta_dB + 2·1_B h_0 S (so the CERT was internally
  inconsistent at bytes between §3.2 and §3.4(ii)); (iii) by CAS on MY OWN
  parity-mixed state pair, distinct from the build's (A2a/A2b: identity
  exact, gap nonzero). The reach statements fc-1a/fc-1b (certified bound
  and of-record S-less exclusion untouched; §3.4(i)'s witness ARGUMENT and
  P-1/P-2 consume the surface-only reading and need the bulk term carried)
  are correctly typed as a registrar note. FC-1 STANDS as correction-grade.
(a-8) THE THREE BLOCKS NOT RE-WALKED. Verified: no two-time estimate
  attempted (B-L2* untouched, reinforced from outside); beta nowhere
  numeric, no |C|_4 power attached to any display (grep + read); neg-3
  correctly distinguishes rank x energy from rank x op and claims no
  supersession of F1. The energy channel's non-closure is displayed by its
  own arithmetic.
(a-9) MISSED-ROUTES HUNT (refusals attacked): eps-optimization of the
  relative-bound-zero split reproduces 2 sqrt(3 r E) — no gain;
  interpolation to HS impossible (the op endpoint is +infinity of record);
  det_2/Carleman variant consumes ||.||_2 <= ||.||_1 <= sigma — same
  conclusion; rank x (compressed op) (COR-B) sharpens constants but stays
  polynomially divergent — no verdict moves; tr(C delta_dB C) divergence at
  site (i) re-checked (tr C(x,x) = integral of tr C(k) = +infinity). NO
  missed route that bridges.
(a-10) WITNESS MECHANICS. witnesses_retired_here = none; retirement of
  E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED explicitly left to the
  registrar; FC-1 explicitly "Registrar's splice"; no edit of any existing
  file. CLEAN.

## 4. THE CORRECTIONS (none verdict-moving)

```text
COR-A  neg-2's "the bounds are ORDER-SHARP within the class (the FB9 witness
  saturates the r.E shape up to the constant)" is FALSE at the HS grade.
  FB9's own witness (T = h = diag(1..N), full compression): ||Pi h Pi||_2^2
  = Sum k^2 = E EXACTLY, so actual^2/bound^2 = E/(4NE) = 1/(4N) -> 0 (CAS
  A4a). Only the TRACE display is order-sharp (limit 3/16, A4b); at HS
  grade the witness saturates rank x (compressed op) instead (limit 1/3,
  A4c). The n-uniform negative is UNAFFECTED (divergence stands).
COR-B  The headline enclosures are not the calculus's own best. The exact
  1-D identity p^2 + x^2 = 2N + 1 (re-derived, A3a) gives the carrier form
  bound |p|^2 <= (6n-3)/ell^2 on ran Q, hence ||h_0 Q||_op <=
  sqrt(6n-3)/ell, and L-1 + sqrt(rank):
     ||C_n T_g C_n||_2 <= 12 n^{3/2} sqrt(2n-1)/ell     (order n^2)
     ||C_n T_g C_n||_1 <= 24 n^3 sqrt(2n-1)/ell         (order n^{7/2})
  vs the build's 12 sqrt(2) n^{7/2}/ell and 24 sqrt(2) n^5/ell. At n = 2:
  24 sqrt(6)/ell vs 192/ell (exact: 6 < 64). If the enclosures are
  registered as of-record content, the sharper displays should be the ones
  registered. All still divergent; F1/MO-3 untouched.
COR-C  Site (iii)'s exact chain compares BARE sigma_{Q,2}(1/2) to log 2;
  the pair coefficient multiplying the surface ceiling in the layer-1
  bound is asserted "exact" (§4.3) but never displayed. On the build's own
  §3.1 constants the weighted single-insertion factor is pi/2 > 1 (exact:
  pi > 8/3 > 2, A5), so the conclusion survives a fortiori; the display
  gap should be completed at registration.
```

## 5. CHOICE LEDGER

```text
ch-1 DEVIATION DISCLOSED: while locating the commission pre-build, I opened
  supervision/THE_HANDOFF_2026-08-15_V001.md :1-80 — a file in the fenced
  list — BEFORE the fence list was in view. Used only to identify the
  build/audit pair; no verdict, no ground, no consumption drawn from it;
  no further fenced file opened. Tag: YOURS (disclosed).
ch-2 PRE-FIX SCOPE: the three of-record BLOCKS pre-fixed and hashed; the
  build's three CONSUMING sites verified post-open by exact re-derivation
  (§1 scope disclosure). Tag: YOURS (protocol reading), PROVABLE (hashes).
ch-3 The build battery was RE-RUN VERBATIM in MY venv (extracted from the
  artifact bytes, script hash 122d946d): 27/27 PASS reproduced. Tag:
  PROVABLE.
ch-4 COR-B's sharper bound uses only the build's own L-1 plus an exact
  operator identity — classed as a correction to the DISPLAYS, not a new
  route (same divergence class, no verdict pressure). Tag: PROVABLE
  (content), YOURS (classing).
ch-5 Site (ii)'s L-5 reduction premise (C_n <= Q_(n,ell), C_n a carrier-
  supported contraction) accepted at WALL/PA sealed grade, not re-derived
  from r-2 continuum bytes. Tag: GROUNDED (record cited at grade).
ch-6 The build's process claims (its venv freshness, its absence probes,
  CL-ERROR not opened) are UNVERIFIABLE-BY-NATURE post hoc; verified
  instead: no existing file edited (all 9 consumed digests still MATCH
  their sidecars), exactly one output pair on disk. Tag: PROVABLE (the
  checked part), noted (the rest).
```

## 6. TOY_SEPARATION

```text
T-1 My A2 states (FC-1) are 1-D toy-schema witnesses of NON-vanishing —
  correct logical grade for confirming a refuted identity; the 3-D reach
  rides the operator algebra (flip + {alpha_j,S} = 0), dimension-blind.
T-2 A4's sharpness refutation lives on the build's own abstract FB9
  Lambda-model — it refutes a sharpness CLAIM about that model; no record
  authority claimed for the toy beyond that.
T-3 My pre-fix toys (form-bdd/op-unbdd diag family; I_r for op-vs-HS)
  established the grade separations abstractly; every record-facing verdict
  above rides the build's site-faithful objects, re-derived.
T-4 Nothing numeric as ground: every comparison an exact chain
  (6 < 64; pi > 8/3; e < 256/81; 3909/1680 > 1; 1303/210 < 32).
```

## 7. SWEEP RECORD

Roots `/Users/bgm/MB Work/alpha-program-archive` and `/Users/bgm/MB Work/
alpha_supervision`, minus fenced classes (register|road_|ledger|lens|plan|
tracker|THE_HANDOFF — filename-level exclusion; ch-1's deviation predates
this sweep), `.git` excluded. **SWEEP CUTOFF DECLARED: 2026-08-14 23:03:27
-0500.** Files newer than the build at cutoff: RA27_RESPONSE_LEGS audit,
MO3_P_EXPONENT audit, B3_DISPLAYS (+sidecars) — classed at headline+grep
grade: no form-to-HS/compression/C-L2-bridge content (the RA27 audit's two
hits are its own sweep's cross-references to this build's landing). Files
sealed after the cutoff: UNSWEPT, declared. "Q-..." tokens:
EXPECTED-UNLOCATABLE, not chased.

## 8. CAS RECORD

Fresh venv `fhb_venv` (sympy 1.14.0), scratchpad-local, distinct from the
build's `fhbvenv`. Three batteries, scripts at scratchpad, hashes of record:

```text
cb2216cb  fhb_cas_v001.py                 18/18 PASS  (pre-fix: flip, Dirac algebra,
          2 sqrt(3) assembly, rel-bound-zero SOS, resolvent compression sqrt(3),
          rank x op arithmetic 2 n^{3/2}, grade-separation toys)
122d946d  fhb_build_battery_verbatim.py   27/27 PASS  (the build's §7 battery,
          extracted from artifact bytes, re-run in MY venv — output matched the
          artifact's verbatim block line for line)
3c4329d8  fhb_audit_battery_v001.py       11/11 PASS  (A2 own-states FC-1; A3
          p^2+x^2 = 2N+1 + the sharper enclosure 12 n^{3/2} sqrt(2n-1)/ell +
          24 sqrt(6) < 192; A4 sharpness limits 1/(4N) -> 0, 3/16, 1/3; A5
          pi/2 > 1; A6 constant-origin consistency)
```

---

## FLAG BLOCK — STAGE8_FORM_TO_HS_BRIDGE_S9AD_AUDIT_V001

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false

FHB_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( target 9a295498; all three
  site verdicts re-derived exactly — (i) FAILS-AT(compression type) VALID,
  (ii) PARTIAL VALID, (iii) FAILS-AT(smallness threshold) VALID given
  COR-C's coefficient >= 1 on the build's own constants; FC-1 REAL,
  triple-verified (bytes, derivation, own-states CAS); shared residue =
  MO-3 / R-L2b(beta) / B-L2* correctly named; blocks not re-walked; hazard
  H1-H6 checklist: NOT committed anywhere; witness mechanics clean. )
CORRECTIONS = ( COR-A neg-2's ORDER-SHARP claim false at HS grade (its own
  witness: 1/(4N) -> 0; trace grade order-sharp, 3/16); COR-B sharper
  in-calculus enclosures ||.||_2 <= 12 n^{3/2} sqrt(2n-1)/ell and ||.||_1
  <= 24 n^3 sqrt(2n-1)/ell supersede the n^{7/2}/n^5 headline displays if
  registered; COR-C the layer-1 pair coefficient must be displayed (it is
  pi/2 > 1 on the build's own constants; conclusion survives a fortiori).
  None verdict-moving. )
INDEPENDENCE = ( verdicts pre-fixed and hashed BEFORE opening the build:
  98c69c9d (verdict file) / cb2216cb (CAS); pre-fixed global verdict =
  no bridge on sealed ground, blocks stand — MATCHES the build at every
  overlapping point; consuming-site content verified by exact independent
  re-derivation post-open, disclosed ch-2. )
DEVIATION_DISCLOSED = ( THE_HANDOFF_2026-08-15 :1-80 opened pre-fence-
  awareness while hunting the commission; nothing consumed from it; ch-1. )
witnesses_retired_here = none ; witnesses_created_here = none
V_clauses_touched = none ; gates_moved = none ; fences_moved = none
new_numbers_frozen = none (all constants exact symbolic)
seals_verified = build 9a295498 MATCH pre-open; 9/9 consumed digests
  re-recomputed MATCH; site pins byte-verified (r-2 :144-148, E1 :811-814,
  WALL :399-420, HPL-audit c-2, PA A1, CERT :307-331, CERT-AUDIT :130)
sweep = both roots minus fenced classes; CUTOFF 2026-08-14 23:03:27 -0500;
  post-build files classed (no bridge content); post-cutoff UNSWEPT
CAS = sympy 1.14.0 fresh venv fhb_venv; 18/18 + 27/27 (verbatim re-run) +
  11/11 PASS; scripts hashed cb2216cb / 122d946d / 3c4329d8
output = ONE artifact + seal sidecar at this commission-distinct path,
  probed ABSENT before write; no git; no existing file edited
ALL consuming quantities remain UNDECIDED; nothing here computes alpha.
FHB_AUDIT_RESULT = SEALED.
```
