# STAGE 8 / RA27-4 RESPONSE LEGS — ADVERSARIAL AUDIT OF STAGE8_RA27_RESPONSE_LEGS_S9AD_V001.md

Lane: FABLE adversarial audit, codename RRL-AUDIT (S9AD). Default posture:
REFUTE. Independence protocol: obligations identified and an own attempt FIXED
AND HASHED BEFORE the build was opened. Date: 2026-08-14.
**ALL_RESULTS = CLAIMED until checked (registrar's check is this artifact's
own successor).**

## VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS (three corrections, none touching a certificate).**
Every BUILT certificate of the target (RL-A conformal isometry + sections; RL-B
Gram transport with the exact G_K numbers 17/32 diagonal / denominator 96; RL-C
forced constraint + canonical section with zero defect; RL-E freedom table
153 / 0/0/9163/1122/107727/947223; RL-F relation A0∘A0 = L_id and the composite
A1∘A1 at m = (1,4,16)) was independently re-derived by a DIFFERENT route
(matrix-level identities in place of per-basis-vector refined solves; subspace-
restricted parent lifts in place of stacked solves; my own primes 15485863 /
32452843 / 49979687; full C-C defect censuses in place of single witnesses) and
every one LANDED EXACTLY; the build's script §5 was re-run VERBATIM in a fresh
venv and reproduced its §6 output byte-identically modulo timing suffixes. The
obligation choice is CORRECT against the POSED ordering and matches this
auditor's own pre-fixed identification; the void condition is honored (Sec is
never named eta_resp); naming is not usurped; the potential-route death is
respected (no certificate consumes C-C); full-D3 is carried OPEN; seals 17/17
and spans 8/8 MATCH at path. THE CORRECTIONS: (1) RL-D's universal sentence
"any response route through potential-level (0-/1-cochain) data inherits the
C-C witnesses and dies with it" is OVER-QUANTIFIED and false as a universal —
an exact counter-exhibit exists (a multilinear/Whitney interpolation pullback
on A1 that IS a cochain map and a UNIT section of the sealed aggregation); the
certified content is only that the TRANSPOSE candidate r* is not a cochain map;
(2) §2.4's "descend to RQ-1 a fortiori" holds for the square/class-stability
certificates but NOT for the forcing claims — under RQ-1 the im×im pin holds
only up to positive scalar (net forced dim 152, not 153); RL-C(i)/RL-E are
RQ-0/plain-carrier statements; (3) "no floats anywhere" is literally false of
the script (time.time() timing displays) though no float is arithmetic ground —
the exact-symbolic fence HELD. Registration should carry the narrow forms.

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       EXACT SYMBOLIC ONLY throughout this audit: every number is a structural
       integer, an exact rational, an exact rank, or a sealed-text quotation.
       No physical quantity computed, bounded, or evaluated. No scale, no
       imported GR, no faithfulness. No member of the 1887-dim variety bound;
       no F-rule, J_1 section value of record, or write chain supplied; no
       kernel C bound. The X5 counter-exhibit is a TAGGED TEST INSTRUMENT
       refuting one universal sentence; it is NOT adopted, NOT a J_1 selection
       of record, and binds nothing.
```

---

## 0. Preflight, independence, seals, sweep cutoff

Output name probed BEFORE any write: `STAGE8_RA27_RESPONSE_LEGS_S9AD_AUDIT_V001.md`
ABSENT (artifact and sidecar; probed 22:48 CDT, re-probed 22:57 CDT). No
register, tracker, plan, road, ledger, lens, or THE_HANDOFF file OPENED;
"Q-..." tokens EXPECTED-UNLOCATABLE. No git action. Fresh venvs (Python 3.9.6,
stdlib only) for all three scripts (own battery, verbatim re-run, extension).

**INDEPENDENCE, OF RECORD:** the obligations were identified from the sealed
POSED artifact (22a2a478) and the sealed predecessor OBD-BUILD (4a6718fe) plus
its audit (c05e89ae); an own attempt (obligation ordering, the lawful-fragment
identification, the certificate program RL-1..RL-6, predictions P1–P8, and the
attack menu A1–A6) was written, its battery run, and all three files hashed at
**22:46:48 CDT, BEFORE the first byte of the build was read**:

```text
ac3f33ff9401ed249b53056717b0d4307eb6db8325eac865e4c50a63026bc76f  RRL_AUDIT_OWN_ATTEMPT_FIXED.md
e7f65fcdb3d1dee18861e4741d0a5bb1b1ae497e7364a06fe912c06a2284ada4  rrl_audit_battery.py
ccf9076ee24ff18e6fefe87132f441f837de62f9d4769b87f1934280bee2e0cf  rrl_audit_battery_output.txt
(session scratchpad; contents summarized in §2–§3 below)
```

The pre-fixed attempt independently identified the SAME lawful target the
build chose (the bottom-leg/constraint/section fragment of O7, with eta_resp
proper and the naming declared not-derivable) and the SAME certificate family
(its RL-1 retraction = the build's C-D(ii) consequence; RL-2/RL-3 projector
splitting; RL-4 counting covariance with factor m_1 = the build's RL-B; RL-5
conditional kernel characterization = the build's RL-C(i) forcing read; RL-6 =
flux-Gram non-scalarity, complementing RL-B).

**Preflight disclosure (mine):** while locating the commission BEFORE the
attempt was fixed, a supervision-file grep emitted register-row text describing
the PREDECESSOR transport step (content duplicating the sealed 4a6718fe, which
was then read lawfully in full). It contained nothing about the build under
audit; no register file was opened; independence w.r.t. the build is intact.

**Build seal:** sidecar present; `shasum -a 256` at path =
`19e143471327630a2041b9eed32f67072161a98074fe9ee78d99ad912041d5f6` MATCHES the
sidecar. Not UNDETERMINED-AT.

**Seals and spans of the build's citations, re-verified at path this session:**
all 17 cited file digests MATCH (22a2a478, bb1b88ad, d83655ae, 4a6718fe,
c05e89ae, 38bbb9fc, c0e7192b, 5e49d209, 74bbb7aa, aa7c6d49 packet member ONLY
— trap T6 honored, top-level same-name file not read — df4514a5, 3534ca53,
97f073c1, 614e20c8, 590b3979, 8dd59b35, 00e2654a); all 8 cited byte spans
re-extracted and re-hashed, 8/8 MATCH, including the two load-bearing negative
citations read in the bytes: the JREF void-condition row ("zero-extension
renamed response pullback"; table header "Authored alternatives disclosed,
none installed") and the D-LAYER sequencing ("Seal ResponseData, Eval, Q_resp,
and the response topology; ... construct eta_resp ... not a zero fill"). The
sealed D012 §2.6.3 display was read at bytes and DOES define the bottom leg
exactly as the build consumes it: `((r_F^*)^* Q)(xi,zeta) := Q(r_F^*xi,
r_F^*zeta)` — the build's "licensed buildable fragment" reading is grounded.

**SWEEP CUTOFF, DECLARED: 2026-08-14 22:57:19 CDT** (first sweep 22:43:06 CDT;
both roots `workspace/` and `cleanroom_output/` minus fenced classes, for
eta_resp / response-leg / response-topology artifacts: the ONLY response-legs
build is the target; two post-build same-night landings — FORM_TO_HS_BRIDGE,
B3_DISPLAYS — are other lineages, checked at head and by grep: zero
response-leg content; landings after the cutoff are outside this sweep).

---

## 1. ATTACK 2 — OBLIGATION CHOICE vs THE POSED ORDERING: LAWFUL

Per the sealed POSED list and the predecessor's own §1 audit, steps 1–3
(d_0-square; AND-class; r_F^* leg) are of record; O7's remainder is (a)
eta_resp — ABSENT, kernel-C-gated; (b) the named topology/quotient — principal
business; O8/O9/O10 not buildable before O7. The pre-fixed attempt (hashed
above) concluded independently: *the only lawful lane build is the kinematic/
conditional fragment — derived carriers + exact conditional certificates
isolating the unsealed load without supplying it.* The build did exactly that
(CH-1), explicitly REJECTED the void-condition move (tautological eta_resp on
Quad(F_phys)), never names Sec "eta_resp", adopts no quotient, and its
NOT_DERIVABLE_TODAY block names the blockers at the same bytes my attempt
predicted (f_g/F_g absent; D-LAYER sequencing; JREF void row). NO
WRONG-OBLIGATION FINDING.

## 2. ATTACK 1/5 — THE CERTIFICATES, RE-DERIVED BY MY OWN ROUTE, AND THE CAS RE-RUNS

**(a) Verbatim CAS re-run.** The build's §5 script (22,716 bytes) was
extracted byte-exact and run in a fresh venv: exit 0; output IDENTICAL to the
§6 display modulo the bracketed timing suffixes (`[N prime(s), Ns]`, `[carrier
built in Ns]`) — every certificate line, census, rank, witness, count, and the
closing multiplicity table byte-equal.

**(b) My own battery (fixed pre-read; different decomposition route — the
d_0-check precedent).** Route differences: the C-D(ii) load carried as the
all-flux MATRIX identity `sd*_2 d_1' r*_1 = m_1 d_1` (strictly stronger than
the build's 17-basis verification); C-D(i) closed via C-B + the exact rank
equalities `ker d_1' = im d_0'`, with NO refined-side linear solve anywhere;
parent lifts by restriction to an explicit Fraction basis of ker(d_0^T) (dim
17) instead of stacked 40x32 solves; ranks at MY primes 15485863 / 32452843 /
49979687; full defect censuses on C-C. ALL LANDED on all five generators:
censuses, d_1 d_0 = 0, both sealed squares, dim H = 0 exact, C-A with
(m_0,m_1,m_2) = (1,1,1)/(1,1,1)/(1,2,4)/(1,1,2)/(1,2,8), C-B entrywise, the
all-flux matrix identity, 17/17 horizontal lifts; C-C TRUE on L_id/A0 and
FALSE on A1/A2-F/A2-B with **full defect censuses 64 / 66 / 240 failing
entries** and first witnesses identical to the build's. Additionally landed
from the pre-fixed program: the projector identity T^2 = m_1 T on F_phys(K')
(the exact splitting F_phys(K') = im(r_F^*) ⊕ (ker sd*_2 ∩ F_phys), the
carrier-level content of the forced/free split), and the complement RL-6/P8:
the FLUX-level Gram is NOT scalar-transported on A1 (witness pair (0,7):
G' = -11/12 vs G = -1 against base ratio 17/12) — confirming that the m_1
covariance lives exactly at the horizontal-lift level where the build put it,
and nowhere cheaper.

**(c) Extension battery (post-read; targets the build's NEW numbers).** All
exact: **X1** G_K diagonal uniformly 17/32 and common denominator 96 via my
subspace lifts (the build's claimed numbers CONFIRMED); **X2** RL-B on A1
re-derived with my lifts, all pairs (with the observation of record: RL-B is
ENTAILED by RL-A on all of C^1 — a corollary display, not an independent
certificate; analogous to the build's own CH-6 disclosure for N_r); **X3** the
A0 relation at all three levels; **X4** the composite A1∘A1 rebuilt from MY
parametric constructor: censuses (625,2000,2400), all controls, C-A at
(1,4,16), C-B, the all-flux matrix identity at m_1 = 4, ranks 624/1376 at my
prime, C-C FALSE with 192 defect entries. The freedom table re-derived by
hand: Sym^2 dims 153; 9316-153 = 9163; 1275-153 = 1122; 107880-153 = 107727;
947376-153 = 947223 — all MATCH. The forcing argument RL-C(i) verified
analytically: r_F^* injective (C-D(ii)) makes the im×im restriction of any
square-satisfying refined form determined by the coarse form — sound in the
plain carrier (see correction C-2 for the quotient scope).

**Break-at-the-boundary probes:** the composite carrier (the generator
family's boundary) passed every certificate at my route's strength; the m = 1
relabelings (where forced dim = full Sym^2) and the m_1 = 2 generators (where
m_1^2 != m_2 on the simplicial side) both behave exactly as displayed; the
§2.2 m_1^2-vs-m_2 table is arithmetic of certified multiplicities and MATCHES.

## 3. ATTACK 3/4 — THE NAMED HAZARDS, AND THE THREE CORRECTIONS

**Full-D3 quoting (the named hazard): NO VIOLATION.** The build carries the
full-D3 quantifier OPEN in the verdict, K-4, NOT_DERIVABLE_TODAY, and
TOY_SEPARATION (which also names the H^1 > 0 reopening of the forced-geometry
premise); no universality is claimed; the regression-fixture warning is
honored. Certificates bind to the five licensed instances + the A0 relation +
one composite — quoted nowhere beyond.

**Potential-route death (respected, then OVER-EXTENDED — Correction C-1).**
No certificate consumes C-C: the dependency table RL-D is accurate for the
build's own package, and the death of the NAIVE route (the transpose candidate
r* as a cochain map) stands re-verified with witnesses. But the build's
sentence — §2.1 RL-D and its flag-block echo — *"any response route through
potential-level (0-/1-cochain) data inherits the C-C witnesses and dies with
it"* — is a UNIVERSAL claim certified by nothing. It is FALSE as a universal:

```text
X5 COUNTER-EXHIBIT (exact rationals, A1, verified entrywise this session):
   U_0 : C^0(K) -> C^0(K')  multilinear interpolation weights at u/2;
   U_1 : C^1(K) -> C^1(K')  integrals of the tensor-product (Whitney/Nedelec)
                            edge forms along child edges (weights in {1/2} x
                            products of {0,1/4,1/2,1});
   (i)   U_1 d_0 = d_0' U_0   ENTRYWISE (U is a genuine cochain map);
   (ii)  sd*_1 U_1 = I_32     EXACTLY (a UNIT-multiplicity section of the
                              sealed aggregation);
   (iii) U_1 != r*_1          (different support and weights).
   So a potential-level pullback EXISTS on a proper refinement that does NOT
   inherit the C-C witnesses. The witnesses attach to the TRANSPOSE candidate
   r* alone (and, of record, the min-norm section is NOT NATURAL — a different
   defect). TAGGED TEST INSTRUMENT: exhibited solely to refute the universal
   quantifier; NOT adopted; NOT a J_1 selection of record; binds nothing; and
   it does NOT rescue any physical eta_resp — nothing physical rides on it.
   CORRECTED SENTENCE for registration: "the transpose/section candidate r*
   is not a cochain map on any proper refinement (exact witnesses); the naive
   potential-pullback route through r* is dead; the response-leg certificates
   consume C-A/C-B/C-D only."
```

**Correction C-2 (quotient-descent scope).** §2.4: "The certificates above
hold for RQ-0 outright and descend to RQ-1 a fortiori." True for the exact
square (RL-C(ii)) and the class-stability/m_1^2 transport. NOT true for the
FORCING claims: in RQ-1 (projective quotient) the square pins the refined
form's im×im block only UP TO POSITIVE SCALAR — net forced dim 152, not 153,
and "eta forced UNIQUELY on relabelings" becomes "forced up to scale". RL-C(i)
and RL-E are correct as PLAIN-CARRIER (RQ-0) statements and should be
registered with that tag. No number in the table changes; the quantifier does.

**Correction C-3 (textual).** "no floats anywhere" (§5 header comment, flag
SCRIPT_REPRODUCED): the script imports `time` and prints float-formatted
runtimes. No float enters any certificate arithmetic — the exact-symbolic
fence HELD — but the sentence is literally false and should read "no floats in
any certificate arithmetic".

**Not findings (checked and cleared):** the "conformal isometry" RL-A is the
transpose reading of sealed C-A — presented as such, no new-content claim; the
A1∘A1 composite is the same sealed constructive law at the next grid level,
censuses honestly labeled arithmetic-not-sealed, validated by its own stage-2
squares before use (CH-3 lawful); the Gram probe is fence-tagged and no kernel
is bound (§2.3 verified); K-1's "existence of a completion certified /
uniqueness fails off-image" is exactly what the certificates support; the
verdict's "C-C stays FALSE on every proper refinement" binds, in context, to
the exhibited instances (lineage usage; the toy separation says so).

## 4. CHOICE LEDGER (auditor's own discretionary choices)

```text
ACH-1 ROUTE (YOURS, fixed pre-read): matrix-identity strength for C-D(ii),
      solve-free C-D(i), subspace lifts, own primes, full defect censuses —
      chosen to be maximally disjoint from the predecessor lineage's stacked-
      solve route while deciding the same claims.
ACH-2 THE X5 EXHIBIT (YOURS, post-read; the only post-read construction):
      multilinear/Whitney interpolation — the canonical non-naive pullback —
      selected BECAUSE the universal sentence names "any ... route"; one
      exhibit suffices; no alternative was tried or tuned; tagged instrument.
ACH-3 P7/P8 sampling: projector identity verified on 8-flux samples for
      A1/A2-F (it is a corollary of the all-flux matrix identity elsewhere);
      flux-Gram scalarity decided on A1 (one witness refutes scalarity).
ACH-4 The A1.A1 rebuild reuses my parametric bisection constructor (the same
      sealed law) — matching the build's CH-3 scope; Freudenthal composites
      not attempted (a new pose, not the audit's business).
ACH-5 Register-content preflight exposure disclosed in §0; no register file
      opened; nothing about the build was known before the attempt was fixed.
ACH-6 Corrections C-1/C-2/C-3 are graded corrections, not refutations,
      because no BUILT certificate, no quantifier of a certificate, and no
      displayed number is touched by any of them.
```

## TOY_SEPARATION

```text
Everything decided here binds to the sealed single-4-cell working class: the
five licensed generator instances, the A0 relation, one composite A1∘A1, and
(X5) one interpolation exhibit on A1. TOY relative to: (t1) the record complex
proper (C_ref frozen AUDIT class; no multi-cell physical complex); (t2) full
D3 (star-refined atoms of unbounded facet count) — NOTHING here quantifies
over it; (t3) H^1 > 0 carriers, where dim H = 0 fails and the uniqueness/
forcing geometry reopens; (t4) the physical response — no kernel C, no d_K, no
coefficient value, no member of the 1887-dim variety is touched by build or
audit. The X5 exhibit is combinatorial linear algebra on integer/rational
incidence data; it carries no physical reading and installs nothing.
```

## FLAG BLOCK

```text
AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS (menu: CONFIRMED | CONFIRMED-WITH-
  CORRECTIONS | REFUTED-AT | UNDETERMINED-AT). Every BUILT certificate of
  19e14347 re-derived exactly by an independent route AND reproduced by
  verbatim CAS re-run; obligation choice lawful per the POSED ordering and
  matching the pre-fixed independent attempt; licensed-generator scope
  honored (no full-D3 quoting); the potential-route death respected by every
  certificate; void condition honored; naming not usurped; member-independence
  preserved; seals 17/17, spans 8/8, sweep clean to the declared cutoff.

CORRECTIONS = C-1 RL-D UNIVERSAL SENTENCE OVER-QUANTIFIED ("any potential-
  level route inherits the C-C witnesses" — refuted by the exact X5 exhibit:
  a multilinear/Whitney pullback on A1 that is a cochain map AND a unit
  section of sd*_1; register the narrow form: the TRANSPOSE candidate r* is
  not a cochain map; the naive route through r* is dead; the certificates
  consume C-A/C-B/C-D only); C-2 RQ-1 DESCENT SCOPE (forcing claims RL-C(i)/
  RL-E are RQ-0/plain-carrier statements; under RQ-1 the pin holds up to
  positive scalar, net forced dim 152); C-3 "no floats anywhere" textually
  false (timing displays); no float is arithmetic ground; fence held.
  NO correction touches a certificate, a displayed number, or the verdict
  structure of the build.

INDEPENDENT_REPLICATION = OWN ROUTE (fixed and hashed pre-read at 22:46:48
  CDT: ac3f33ff / e7f65fcd / ccf9076e): matrix-identity strength for
  sd*_2 d_1' r*_1 = m_1 d_1 (all-flux, strictly stronger than the build's
  17-basis checks); solve-free C-D(i) via C-B + exact rank equalities; own
  primes 15485863/32452843/49979687; full C-C defect censuses 64/66/240
  (+192 on A1∘A1); G_K diagonal 17/32 and denominator 96 CONFIRMED via
  subspace lifts; freedom table re-derived by hand — all MATCH. PLUS the
  pre-fixed complements: T^2 = m_1 T (the exact projector splitting of
  F_phys(K')) and the flux-Gram NON-scalarity on A1 (witness (0,7):
  -11/12 vs -1) — the covariance is a horizontal-lift-level fact, confirming
  the build's placement.

CAS_VERBATIM_RERUN = yes (script §5 extracted byte-exact, fresh venv, exit 0;
  output byte-identical to §6 modulo bracketed timing suffixes).

SWEEP_CUTOFF_DECLARED = 2026-08-14 22:57:19 CDT (first sweep 22:43:06; both
  roots minus fenced classes; the target is the only response-legs artifact;
  post-build landings FORM_TO_HS_BRIDGE / B3_DISPLAYS / MO3_P_EXPONENT are
  other lineages, checked; post-cutoff landings outside this sweep).

FENCES = alpha_computed = false ; proof_authorized = false ;
  kappa_record_computed = false ; coupling_evaluation_authorized = false ;
  exact symbolic only; no floats as ground; no value frozen; no member of the
  1887-dim variety bound; no F-rule or write chain supplied; no kernel C
  bound; no fenced-class file opened ("Q-..." EXPECTED-UNLOCATABLE; one
  preflight grep emission disclosed §0, independence intact); no git; ONE
  output + seal sidecar at the commission-distinct path, probed ABSENT first
  (22:48 and 22:57 CDT).
ALL_RESULTS = CLAIMED until checked.
```
