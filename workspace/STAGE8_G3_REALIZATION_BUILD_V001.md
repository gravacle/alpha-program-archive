# STAGE 8 — G3 REALIZATION BUILD: THE WRITE-CHAIN SUPPORT / CURRENT DENSITY, ASSEMBLED FROM THE SEALED CONSTRUCTION
## BLIND BUILDER (G3-REALIZE) — REALIZATION FROM SEALED CONTENT, NOT CONSTRAINT INVENTORY — [CLAIMED]

Date: 2026-08-13
Role: ASSEMBLE one datum. Node G3 is the per-cell write chain `ell_j` — which
edges/cells the per-cell holonomy actually reads, with what current density. The
panel-confirmed interior constraint set (gauge-invariant, cell-local, conserved-typed,
total-nonzero, block-split free) is CARRIED, not re-derived. This artifact assembles
what the SEALED CONSTRUCTION ITSELF — the ratified finite-N influence functional's
one-cell origin, the source coupling, the two-time Dyson-dressed write vertex, the
cell/CTP structure — FIXES about the realization, and states precisely where assembly
stops. No value of `n`, `kappa`, `alpha`, `R_record,L`, `mu`, `C`, any scale, length,
coefficient, norm, exponent, or constant is computed, bounded, estimated, or compared.
Every numeric string below is a sealed-text quotation or an exact structural integer.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false` ; `coupling_evaluation_authorized = false`

Fences held throughout: CONNECTION-ONLY, SCALE-FREE, symbolic. NO scale (ell_P, K_KK,
c^2, metric length, G_4, the Lorentzian causal diamond Omega_c's metric/contractibility,
sea-covariance-on-diamonds, |D|_4) as authority — scale-bearing sealed text read
SUSPECT-ONLY to classify, never to certify. NO imported GR. NO faithfulness premise.
No register/tracker/plan/road/ledger/lens file read. No git action.

---

## 0. VERDICT IN ONE LINE

**The sealed construction fixes SEVEN realization data about the per-cell write chain
`ell_j` — cell-factorized read, exactly one chain per cell attached to the
ready-to-pointer amplitude alone, unit-modulus phase pairing, a TWO-PORT open-transport
incidence with sealed endpoint representations (gauge-invariance realized by
common-port cancellation across the CTP branches, NOT by chain-closedness), a
two-branch same-chain conjugated read with no single-branch read at all,
sector-diagonal coupling through the charged relative phase only, and one concrete
two-time vertex realization `J = -(Q b_D Q) (x) alpha_x` (smooth interior density
against a SHARP record localizer, Dyson-dressed over the cell interval;
SUSPECT-classified: fixed-gauge, scale-bearing carrier) — and fixes NOTHING more: the
incidence set (which edges) is consumed as an input, no sealed transport carries the
diamond realization to the record complex, and the block-split across
`im(d_1^dagger)` vs `H` is NOT DETERMINED, with the exact missing input named. The
regularity typing at the sealed vertex is MIXED (smooth connection-read factor, sharp
record-write factor); at the record-complex level it is NOT DETERMINED.**

---

## 1. SOURCES READ, SEALS VERIFIED AT PATH

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest below
recomputed by `shasum -a 256` at path this session BEFORE reading; sidecars compared
where present (all matched).

| Tag | Source | Seal (sha256, full) | Status |
|---|---|---|---|
| FORM | `STAGE8_R_RECORD_L_FORM_FABLE_V001.md` | `5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37` | MATCHES-TASKED |
| GB | `STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md` | `1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb` | MATCHES-TASKED |
| GC | `STAGE8_REQUIRE_G3_CHECK_V001.md` | `8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3` | MATCHES-TASKED |
| M03 | `STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md` | `2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f` | SIDECAR-MATCH |
| E1 | `STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md` | `468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5` | SIDECAR-MATCH (seal begins 46846730, as tasked) |
| PA | `STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md` | `789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3` | SIDECAR-MATCH (located from E1's Phase-A citation, E1 :82) |
| FR | `STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md` | `4d072e7671113357764b61e11ef1d3d336ce34e307ba07c545ec7cf1a38a6abc` | SIDECAR-MATCH (the RA27-3 transport-field board, located from FORM's references :54, :117-118) |

The E1 file materially omitted by a prior build is READ here in full at its verified
seal. No register, tracker, plan, road, ledger, or lens file read. Output name probed
before write: ABSENT.

Symbol-collision flags, stated once for the corpus audit: (i) `alpha_x` is the Dirac
velocity matrix in the x-direction (PA :79, :122; E1 :345), NOT the fine-structure
constant — no value of any coupling appears in this artifact; (ii) RA27-3's test
instance named "A2" (FR :22, :29) is a cellulation instance, distinct from PA's
section §A2 (the connection-direction definition); (iii) M03's `a_+, a_-` are
connection histories, its `sigma_+, sigma_-` are discrete source sectors (M03
:159-166) — that sealed distinction is honored throughout.

---

## 2. THE SEALED CONSTRUCTION, QUOTED

**Q1 — the one-cell origin and the source coupling (M03).** On the one-cell record
basis `(|r>,|p_Q>,|e_Q>)`: `S = [[0,1,0],[1,0,0],[0,0,-1]]`;
`D_(n,j)[a_j] := diag(1, z_j^(n)[a_j], 1)`; `W_(1,j)^(n)[a_j] := D_(n,j)[a_j] S_j`;
`W_N^(n)[a] := tensor_(j=1)^N W_(1,j)^(n)[a_j]` (DoR-009's ratified finite-locality
row); `U_N^(n)[a] := P_0 tensor I + P_ch tensor W_N^(n)[a]` (M03 :121-150). Per cell,
`D_n[a]S|r> = z_n[a]|p_Q>` (M03 :177-181). `F_N = P_0 + Z_N P_ch` with
`Z_N = product_j conjugate(z_j[a_(-,j)]) z_j[a_(+,j)]`; cross-history entries
`I_N(0,ch) = I_N(ch,0) = 0` "because `<R_N|P_N> = 0`" (M03 :15-41, :199-213).

**Q2 — the sealed gauge action on the one-cell origin (M03 §5.3).** "For one oriented
cell, let a gauge change act on the holonomy character by `z^g = t z s^dagger`. The
ratified post endpoint representations are `G_out(t) = D(t)`, `G_in(s) = S D(s) S`.
Exact multiplication gives `W_post(z^g) = G_out(t) W_post(z) G_in(s)^dagger`." Under
the simultaneous/common gauge transformation of both CTP histories,
`conjugate(t z_- s^dagger)(t z_+ s^dagger) = conjugate(z_-) z_+`, so `Z_N` and `F_N`
are invariant; "independent transformations of the two open histories carry relative
endpoint characters and are not silently identified with the common transformation"
(M03 :325-354).

**Q3 — the two-time Dyson-dressed write vertex (E1, PA).** The sealed Phase-A per-cell
objects (E1 :82 cites PA as the sealed authority for `h_0, M(t), v(t), b_D, J, S,
w_lambda`): `r(t) = min(t, 1-t)`; `M(t) = Q 1_(|x|<=r(t)) Q` (PA :83-88);
`b_D(t,x) = exp(16 - 1/s(t,x))` on the open unit cell diamond, zero outside — the
extension "is `C-infinity`, compactly supported in the closed diamond, vanishes to all
orders at its boundary and tips" (PA :99-113); `A_x^(a)(t,x) = a b_D(t,x)`,
`A_y = A_z = A_0 = 0`; `B_D(t) = Q b_D(t,x) Q`; `J(t) = -B_D(t) tensor alpha_x`;
`h_lambda(t;a) = h_0 + lambda v(t) M(t) tensor S + a J(t)` (PA :115-127). E1's typing
of the same objects: "a-VERTEX SECTOR: ... `J = -(Q b_D Q) ⊗ alpha_x` with `b_D`
Gevrey, all derivatives vanishing at boundary and tips, so its spacetime transform
decays faster than any power. RECORD VERTEX: `M(t)` is a SHARP indicator and by C4 the
pointer weights put it in the LEAD at full tau_R (`sum_lambda w_lambda u_lambda = P`).
The locus is the VOLUME DIAGONAL" (E1 :1111-1116; C4 at E1 :339-351; C6 locus
refinement at E1 :353-366). The dressing: "the actual `V_i - 1` are Dyson-dressed by
free `h_0` propagation over the whole interval" (E1 :510-513); "Every Dyson term with
fewer than two record insertions PER CTP BRANCH is annihilated; the bare a-linear
tadpole vanishes identically (also killed by the odd spinor trace
`tr_spinor[C(p) alpha_x] = -2 p-hat_x`)" (E1 :343-345).

**Q4 — the sealed self-classification of Q3 (PA).** "This is explicitly a fixed-gauge
finite response diagnostic. Phase A claims no local gauge covariance" (PA :130-135);
the carrier is the Hermite/oscillator Galerkin family with oscillator length and the
causal diamond (PA :50-70, :99-113) — SCALE-BEARING; the probed histories are frozen
rational values of the single coefficient `a` on the fixed profile (PA :137-141).

**Q5 — the missing transport (FR).** "support — UNDERDETERMINED, structurally
immediate and unsealed" (FR :37, :228-235); "current density — UNSUPPLIED ... No
transport law exists. It is the most downstream of the seven fields and nothing
upstream of it is complete" (FR :38, :237-242); the refinement edge assignment "is
free on a 32-dimensional set at fixed energy, and ... the difference is not a
coboundary — physical, not gauge" (FR :27-31, :176-191).

**Q6 — the carried constraint inventory (GB, GC; not re-derived).** The record fixes
`ell_j` gauge-invariant on the difference branch, cell-local, conserved-typed
(`∂ell_j = 0` = the same `⊥ im(d_0)` condition, GC :86-91), total-nonzero for
`n != 0`; it does NOT fix the block-split (GB :149-160, flag :306-340; GC flag
:252-262, verdict SOUND :279-285). `ell_j` is "an incidence datum of the write, NOT
sealed as an object" (FORM :164-165); G3 NOT SUPPLIED (FORM :277-281, B7 :114-119).

---

## 3. DELIVERABLE 1 — THE REALIZED WRITE, AS FAR AS THE SEALED CONSTRUCTION GOES

The maximal realization the sealed text supports is the following seven data. Each is
an exact consequence of quoted sealed identities; assembly steps of mine are marked
YOURS inline.

```text
R1  CELL-FACTORIZED READ (support inside the writing cell). W_N = tensor_j W_(1,j)[a_j]
    and D_(n,j) reads a_j ONLY (Q1; M03 :127-150). Zero-extension appends identity
    holonomies that drop out exactly (C7, M03 :387-399). REALIZED: the chain ell_j
    reads nothing outside cell j's own history carrier — the panel's cell-locality is
    here REALIZED by the ratified finite-locality tensor row, not imposed.

R2  EXACTLY ONE CHAIN PER CELL, ATTACHED TO THE ready->pointer AMPLITUDE ALONE.
    D_n[a] = diag(1, z, 1): the connection phase multiplies ONLY the pointer slot;
    D_n[a]S|r> = z_n[a]|p_Q> is the single character power (Q1; M03 :127-133,
    :177-181; the explicit one-cell matrix M03 :235-248 shows the eraser slot |e_Q>
    carrying the connection-INDEPENDENT entry -1). REALIZED: the entire connection
    read of a cell is one functional <ell_j, .>, inserted at the ready->pointer write
    transition and nowhere else in the record carrier.

R3  UNIT-MODULUS PHASE PAIRING, FLAT ON THE BRANCH DIAGONAL. Each z is a unit-modulus
    character; Z_N[a,a] = 1 and F_N[a,a] = I exactly (M03 :190-198, :286-293).
    REALIZED: ell_j pairs into a pure phase; the modulus channel is blind to it on the
    diagonal — matching FORM D3 (:180-199).

R4  TWO-PORT OPEN-TRANSPORT INCIDENCE; GAUGE-INVARIANCE BY COMMON-PORT CANCELLATION.
    The sealed law z^g = t z s^dagger with endpoint representations G_out(t) = D(t),
    G_in(s) = S D(s) S (Q2) fixes the per-cell holonomy as an OPEN transport with
    exactly TWO gauge ports per cell — one in, one out — realized on the record
    carrier by named endpoint representations. Common transformation of both CTP
    branches cancels PORT BY PORT (abelian: conjugate(t)t = 1, s s^dagger's
    conjugate cancels; M03 :342-351); independent branch transformations carry
    relative endpoint characters (M03 :352-354).
    YOURS (exact adjoint algebra on Q2): reading t, s as values of a gauge 0-cochain
    at the two ports gives d_0^dagger ell_j = e_out - e_in on the SINGLE branch —
    the single-branch chain is OPEN (two-point boundary), and the write's
    gauge-invariance is realized by COMMON-PORT CANCELLATION ACROSS THE TWO BRANCHES,
    not by chain-closedness. CONSISTENCY with the carried inventory (Q6): the
    panel-typed conserved/gauge-invariant object is the DIFFERENCE-BRANCH functional
    on the gauge quotient, and on that quotient only the H (+) im(d_1^dagger)
    projection of ell_j is defined — no contradiction; R4 refines the MECHANISM.
    NOT SEALED: the identification of the two ports with named 0-cells of the record
    complex. The ports are record-carrier endpoint slots; their incidence anchoring is
    not displayed anywhere in the sealed text.

R5  TWO-BRANCH SAME-CHAIN CONJUGATED READ; NO SINGLE-BRANCH READ EXISTS. Z_N reads the
    SAME per-cell functional on both branches, conjugated on the minus branch (Q1;
    M03 :15-22, :190-198), so the tangent-level read is <ell_j, a_Delta>; and the
    single-branch charged amplitude is IDENTICALLY zero, <R_N|W_N[a]|R_N> = 0
    (M03 :205-213, :364-370). REALIZED: there is no one-branch realization of the
    write to assemble — the realization is intrinsically doubled.

R6  SECTOR-DIAGONAL COUPLING THROUGH THE CHARGED RELATIVE PHASE ONLY. F_N = P_0 +
    Z_N P_ch, cross-sector entries zero because <R_N|P_N> = 0 (Q1; M03 :24-41,
    :205-213); "the source coupling changes the charged relative-history phase; it
    does not erase the exact ready/pointer record distinction" (M03 :376-378).
    REALIZED: the write's current couples the connection to the charged sector alone;
    the neutral sector's read is empty.

R7  THE ONE CONCRETE VERTEX REALIZATION (SUSPECT-CLASSIFIED). The sealed construction
    contains exactly one concrete realization slot for the per-cell write vertex —
    the Phase-A/E1 two-time object (Q3):
      - connection read: J = -(Q b_D Q) (x) alpha_x with A_x = a b_D, all other
        components zero — the connection is read against a SMOOTH interior density
        (C-infinity, compactly supported in the closed cell diamond, all derivatives
        vanishing at boundary and tips) contracted with the Dirac velocity alpha_x.
        NO edge-concentrated/singular current appears in the connection read of this
        realization.
      - record write localizer: M(t), a SHARP indicator, promoted to the leading
        in-cell object by the sealed pointer weights (sum_lambda w_lambda u_lambda
        = P), singular locus the VOLUME DIAGONAL.
      - two-time structure: the vertex acts Dyson-dressed by free h_0 propagation
        over the whole cell interval; terms with fewer than two record insertions per
        CTP branch are annihilated; the bare a-linear tadpole vanishes.
    CLASSIFICATION FLAGS, all sealed in the source itself (Q4): fixed-gauge
    diagnostic claiming no local gauge covariance; scale-bearing carrier (oscillator
    Galerkin family + causal diamond) — read SUSPECT-ONLY here, used to CLASSIFY the
    realization's structure, never as a certifier of any record-native property; the
    response is probed along ONE frozen tangent direction (a single coefficient on
    the fixed profile), not against a spanning family.
```

**Where assembly stops — the four exact stopping points:**

```text
S1  THE INCIDENCE SET IS AN INPUT, NOT A CONSTRUCTION. The ratified law consumes the
    holonomy map h_j[a_j] as given ("admitted finite histories", M03 :516; the law's
    inputs are the characters z_j[a_j], Q1). Nowhere in the sealed construction is
    h_j displayed as an incidence sum over named edges with weights. WHICH edges the
    per-cell holonomy reads is not in the sealed text. (Concurs with FORM :164-165:
    ell_j is "an incidence datum of the write, NOT sealed as an object".)

S2  NO DIAMOND-TO-COMPLEX TRANSPORT EXISTS. The one concrete realization (R7) is
    indexed on the continuum cell diamond; the record complex is subdivision-indexed.
    The bridge is sealed ABSENT: support transport UNDERDETERMINED, current density
    UNSUPPLIED, "No transport law exists" (Q5; FR :37-38, :228-242; carried in GB
    §2.2-2.3 :97-145). R7 therefore cannot be pushed to a 1-chain on the complex by
    any sealed object.

S3  PORT ANCHORING UNSEALED. R4's two ports are record-carrier endpoint slots; their
    identification with 0-cells of the complex is not displayed (R4).

S4  ONE PROBED DIRECTION. Even granting R7's SUSPECT content in full, the sealed
    realization exhibits the write's pairing along one frozen profile direction only
    (PA :137-141, with the sealed bar on selecting by output); a chain is determined
    by its pairing against a SPANNING family, which no sealed object supplies.
```

---

## 4. DELIVERABLE 2 — THE BLOCK-SPLIT UNDER THE REALIZATION

```text
BLOCK_SPLIT_UNDER_REALIZATION = NOT_DETERMINED.

WHAT THE REALIZATION DOES PIN:
  (i)  the gauge-block content of the SINGLE-branch chain: by R4, d_0^dagger ell_j =
       e_out - e_in is pinned by the two ports alone, hence the im(d_0) component of
       the single-branch chain is a function of the port pair (YOURS, exact); this
       component is INVISIBLE to the difference-branch functional (common-port
       cancellation) and is NOT part of the phi_f/phi_H question.
  (ii) the total physical part is nonzero for n != 0 (carried, Q6; not re-derived).

WHAT IT DOES NOT PIN: the decomposition of the physical part across im(d_1^dagger)
  (phi_f) vs H (phi_H). None of R1-R7 discriminates the two blocks: R1-R3, R5, R6 are
  block-blind structure; R4 pins only the gauge block; R7 is diamond-indexed and
  cannot be pushed to the complex (S2), is probed in one direction (S4), and is
  fixed-gauge (Q4).

THE EXACT MISSING INPUT, NAMED: a sealed transport of the realized smooth diamond
  density (Q b_D Q, alpha_x-directed) to a 1-chain on the record complex — i.e. the
  Ref_a-indexed current-density receiver PLUS the exhaustion<->refinement index bridge
  (the two slots sealed UNSUPPLIED/absent: FR :37-38, :228-242; GB :122-145) —
  followed by orthogonal projection onto im(d_1^dagger) and H. Equivalently: a
  ratified successor of the transition law displaying h_j[a_j] as an explicit
  incidence sum (discharging S1). ABSENT either, the split is exactly the open datum
  G3 of record (FORM :277-281, :334-343; GB flag :306-340; GC SOUND :279-285).

BARRED SHORTCUT, not taken: deciding the split from R7 directly would require the
  cell diamond's contractibility/H_1 as authority (smooth density on a contractible
  region => its chain image bounds => phi_H = 0). That is a SCALE/GR import — the
  diamond is sealed as "a Lorentz-covariant CONTINUUM DIAMOND, NOT A CW OBJECT" with
  uniqueness open — and the G3-CHECK closed exactly this refutation line as forbidden
  (GC :122-148). Independently: regularity of a density and the homology class of its
  transported chain are logically independent data — smoothness decides NOTHING about
  straddle vs single-block.
```

---

## 5. DELIVERABLE 3 — THE REGULARITY / SUPPORT STRUCTURE UNDER THE REALIZATION

```text
REGULARITY = MIXED at the sealed vertex realization, in the following exact sense —
  the realized write vertex is a TENSOR of two factors of OPPOSITE regularity type:

  SMOOTH factor — the CONNECTION-READ density: Q b_D Q with b_D C-infinity/Gevrey,
    compactly supported in the closed cell diamond, ALL derivatives vanishing at the
    boundary and both tips, spacetime transform decaying faster than any power
    (PA :99-113; E1 :1056-1058, :1111-1113). Under this realization the current
    density of the write is a smooth interior density in the Dirac direction alpha_x;
    it has NO edge-concentrated singular part.

  SHARP factor — the RECORD-WRITE localizer: M(t) = Q 1_(|x|<=r(t)) Q, a sharp
    indicator (PA :83-88; E1 :1114 "M(t) is a SHARP indicator"), promoted to the
    leading in-cell object by the sealed pointer weights (sum_lambda w_lambda
    u_lambda = P; E1 :346-351), singular locus the VOLUME DIAGONAL (E1 :353-366).

  TWO-TIME: both factors act Dyson-dressed by free h_0 propagation over the whole
    cell interval (E1 :510-513); the object is a cell-S-matrix-type two-time vertex,
    the only type C6 admits (E1 :353-358).

  SCOPE OF THE TYPING (binding on any consumer): the typing is SYMBOLIC
  (smooth-vs-indicator), consumes no scale value, and attaches to the SUSPECT
  diamond-indexed realization (Q4). The singular structure sits on the RECORD-WRITE
  side, not the connection-read side. AT THE RECORD-COMPLEX LEVEL — the ell_j whose
  derivatives a downstream remainder estimate would consume — the regularity is NOT
  DETERMINED: no transported density exists of record (S2), so no derivative datum of
  ell_j on the complex is available from sealed content. A consumer differentiating
  ell may cite the smooth typing ONLY for the diamond realization and ONLY jointly
  with the S2 gap.
```

---

## 6. DELIVERABLE 4 — WHAT REMAINS GENUINELY FREE

```text
THE FREE SPACE (the admissible realizations of G3): per admitted cell, the
  PHYSICAL-BLOCK component of ell_j — an element of H (+) im(d_1^dagger) constrained
  only by (a) total-nonzero for n != 0 (phi_f + phi_H != 0, carried Q6), (b)
  cell-local support (R1), (c) the two-port boundary data (R4, pinning only the
  single-branch gauge-block part) — together with, over the working refinement class,
  the refinement-lift freedom: sealed witnessed FREE on a 32-dimensional set at fixed
  energy on the tested instance, difference NOT a coboundary — physical, not gauge
  (FR :27-31, :176-191; FORM B7 :114-119). The block-split weights (phi_f, phi_H) are
  coordinates on this space; no sealed object fixes them either way (Q6).

WHAT KIND OF OBJECT WOULD SELECT ONE, in order of construction-nearness:
  (1) the Ref_a-indexed current-density receiver + exhaustion<->refinement bridge
      (the two named UNSUPPLIED slots, FR :228-242), which would transport R7's
      sealed smooth density to the complex and COMPUTE the split by projection;
  (2) a ratified successor of the transition law displaying h_j[a_j] as an explicit
      incidence sum over named edges with weights (discharging S1 at the law level);
  (3) a principal decision-of-record fixing the realization directly.
NON-SELECTORS, stated to close false doors: a gauge choice selects nothing (the
  freedom is non-coboundary of record, FR :27-31); the scalarization/state-port pair
  G2 is NOT needed for this datum (it gates the cross TERM's coefficient mu, not the
  realization of ell_j; GB :217-238); and no response VALUE may select (PA :144-145's
  sealed bar, and the fences here).
```

---

## 7. WHAT THIS DOES NOT SAY / OVERCLAIM AUDIT

```text
IT DOES NOT assert that the Phase-A/E1 object IS the record's realization of ell_j:
   it is the sealed construction's only concrete realization slot for the write
   vertex, and it is sealed as a fixed-gauge, scale-bearing diagnostic (Q4) — read
   SUSPECT-ONLY, used to classify structure (smooth-vs-sharp, two-time, one
   direction), never to certify a record-native property.
IT DOES NOT decide straddle vs single-block, nor lean either way: NOT_DETERMINED with
   the missing input named (§4). The smoothness of b_D is explicitly NOT read as
   evidence for either branch.
IT DOES NOT re-derive or contest the panel-confirmed constraint inventory (GB/GC):
   that inventory is carried; this artifact adds realization data R1-R7 and the
   stopping points S1-S4 on top of it, and R4 is checked for consistency with it.
IT DOES NOT compute, bound, or compare any value: no n, kappa, alpha, mu, C, norm,
   exponent, spectrum, or coefficient; PA's frozen rational history values and M03's
   fixture rationals are quoted-context only, never consumed; 32 and all displayed
   integers are sealed-text quotations or exact structural integers.
IT DOES NOT import scale or GR: the diamond metric, oscillator length, Vol_4,
   sea-covariance-on-diamonds, and |D|_4 are never consumed as authority; the one
   place a scale/GR fact would have decided the question (diamond contractibility =>
   single-block) is explicitly BARRED and not taken (§4).
IT DOES NOT consume faithfulness or any injectivity premise anywhere.
YOURS assemblies, marked: the adjoint/port reading d_0^dagger ell_j = e_out - e_in
   and the common-port-cancellation mechanism (R4); the block-blindness audit of
   R1-R7 (§4); the tensor-of-opposite-types regularity framing (§5); the
   coordinatization of the free space (§6). Each runs on quoted sealed identities;
   none consumes an unsealed value.
FLATTENING CHECK: "one concrete realization exists" is not flattened into "the
   realization is determined" — S1-S4 stand; "MIXED at the vertex" is not flattened
   into a complex-level regularity claim — that level is NOT DETERMINED; the port
   structure is not flattened into a 0-cell incidence claim — the anchoring is
   unsealed (S3).
```

---

## 8. FINAL FLAG BLOCK

```text
REALIZED_WRITE = FIXED-BY-CONSTRUCTION(seven data, assembled): (R1) cell-factorized
  read — ell_j reads nothing outside cell j (M03 :127-150, :387-399); (R2) exactly
  one chain per cell, attached to the ready->pointer amplitude alone, eraser slot
  connection-independent (M03 :127-133, :177-181, :235-248); (R3) unit-modulus phase
  pairing, flat on the branch diagonal (M03 :190-198, :286-293); (R4) two-port
  open-transport incidence from the sealed one-cell gauge law z^g = t z s^dagger with
  endpoint representations G_out(t) = D(t), G_in(s) = S D(s) S — gauge-invariance
  realized by common-port cancellation across the CTP branches, NOT chain-closedness;
  single-branch boundary d_0^dagger ell_j = e_out - e_in (YOURS, exact adjoint
  algebra); port-to-0-cell anchoring UNSEALED (M03 :325-354); (R5) two-branch
  same-chain conjugated read <ell_j, a_Delta>, no single-branch read exists
  (M03 :15-22, :190-213, :364-370); (R6) sector-diagonal coupling through the charged
  relative phase only (M03 :24-41, :205-213, :376-378); (R7) the one concrete
  two-time vertex realization J = -(Q b_D Q) (x) alpha_x — smooth interior density in
  the Dirac direction against the SHARP record localizer M(t), Dyson-dressed over the
  whole cell interval, weights promoting the sharp projector to the lead (PA :83-127;
  E1 :343-366, :510-513, :1056-1058, :1111-1116) — SUSPECT-classified: sealed
  fixed-gauge diagnostic, no local gauge covariance claimed, scale-bearing carrier
  (PA :50-70, :130-135). ASSEMBLY STOPS AT: S1 the incidence set is a consumed input,
  h_j never displayed as an edge sum (M03 :516; FORM :164-165); S2 no
  diamond-to-complex transport exists — current density UNSUPPLIED, "No transport law
  exists" (FR :37-38, :228-242); S3 port anchoring unsealed; S4 one probed tangent
  direction only (PA :137-141).

BLOCK_SPLIT_UNDER_REALIZATION = NOT_DETERMINED(exact missing input: the Ref_a-indexed
  current-density receiver + exhaustion<->refinement index bridge (FR :228-242;
  GB :122-145) transporting the realized diamond density to a 1-chain on the record
  complex, then projected onto im(d_1^dagger) vs H — equivalently a ratified
  successor law displaying h_j as an incidence sum. The realization pins only the
  single-branch GAUGE-block part (the two ports, R4) and the total-nonzero constraint
  (carried); none of R1-R7 discriminates phi_f from phi_H. The one shortcut — diamond
  contractibility => single-block — is a barred SCALE/GR import (GC :122-148), and
  smoothness of the density decides nothing about the homology class of its
  transported chain.)

REGULARITY = MIXED(at the sealed vertex realization: SMOOTH connection-read factor —
  Q b_D Q, C-infinity/Gevrey, compactly supported, all derivatives vanishing at cell
  boundary and tips, transform decaying faster than any power (PA :99-113;
  E1 :1056-1058, :1111-1113) — TENSORED WITH SHARP record-write factor — M(t)
  indicator, promoted to lead by the pointer weights, singular locus the volume
  diagonal (PA :83-88; E1 :346-351, :353-366, :1114-1116) — two-time Dyson-dressed
  (E1 :510-513). Symbolic typing only; the smooth factor is diamond-indexed and
  SUSPECT; the singular structure sits on the record-write side, not the
  connection-read side. AT THE RECORD-COMPLEX LEVEL the regularity of ell_j is NOT
  DETERMINED — no transported density exists of record (S2), so no derivative datum
  of ell_j on the complex is available from sealed content.)

RESIDUAL_FREEDOM = the physical-block component of ell_j per cell — an element of
  H (+) im(d_1^dagger) constrained only by total-nonzero (n != 0), cell-locality, and
  the two-port boundary data (which pin the single-branch gauge block only) — plus
  the refinement-lift freedom, sealed FREE on a witnessed 32-dimensional set at fixed
  energy, non-coboundary, physical not gauge (FR :27-31, :176-191). SELECTORS: (1)
  the current-density receiver + index bridge (the two UNSUPPLIED slots); (2) a
  ratified successor displaying h_j as an explicit incidence sum; (3) a principal
  decision-of-record. NON-SELECTORS: gauge choice (freedom is non-coboundary); the
  scalarization pair G2 (gates the cross term's mu, not this datum); any response
  value (barred).

SEALS_VERIFIED = STAGE8_R_RECORD_L_FORM_FABLE_V001.md 5e49d2093d4ee17b840cc096d5caf3
  a6edafda4e37abbd175a7a371c34658f37 ; STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md
  1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb ;
  STAGE8_REQUIRE_G3_CHECK_V001.md 8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b88
  5a0bf6ee10cab3 ;
  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md
  2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f ;
  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md 468467303a109dc825b015107897dacc107800
  fc981030377c3f006b384cccb5 ; STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE
  _SPEC_V001.md 789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3 ;
  STAGE8_7A_RA27_3_FRONTIER_DARIO_V001.md 4d072e7671113357764b61e11ef1d3d336ce34e307
  ba07c545ec7cf1a38a6abc — all recomputed at path before reading; sidecars matched
  where present; no seal failed.

FORBIDDEN_IMPORTS = none consumed. SUSPECT-classified only, never certifying:
  (a) the Phase-A/E1 diamond + oscillator Galerkin carrier (SCALE; PA :50-70,
  :99-113) — used to CLASSIFY R7's structure only; (b) the causal diamond Omega_c's
  metric/contractibility (SCALE/GR) — the one decisive use of it is BARRED and not
  taken (§4); (c) fixed-gauge status of PA sealed in the source itself (PA :130-135)
  — flagged, bounds R7's authority. NO faithfulness/injectivity premise anywhere.
  NO imported GR. alpha_x is a Dirac matrix, not the fine-structure constant.

MACHINERY_INVOKED = no (assembly and typing only; exact symbolic algebra on sealed
  forms; no numeric evaluation, no fixed-point execution, no end test, no member
  binding; no register/tracker/plan/road/ledger/lens read; no git action; output name
  probed ABSENT before write).

alpha_computed = false ; kappa_record_computed = false ; proof_authorized = false ;
coupling_evaluation_authorized = false
ALL_RESULTS = CLAIMED until the adversarial panel.
```
