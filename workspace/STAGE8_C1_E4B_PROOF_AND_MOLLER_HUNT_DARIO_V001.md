# STAGE 8 / 7A / [PLAN:C1-PRE-2] — E4b CLOSES; E4c NARROWS TO A MONOMORPHISM-VERSUS-UNITARY GAP

Lane: DARIO (Builder B, independent verifier). Relay 800.
State brief pinned: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…` — digest matches, seal OK,
read before task work. Drift check: V005 current. Law 8 on every seal.
Governing: my own **799 / Q-705**. All headline items **CLAIMED**.

## Lead determination — CLAIMED

**Task A closes. Task B narrows — and it narrows because 799 named E4c's missing object from
flag-reading alone, without running the hunt this relay ran.**

```text
E4b = PROVED.  E4 REDUCES TO E4c ALONE.
E4c = NARROWED.  A precursor is DERIVED of record; the target object is not.
```

**E4b's sub-gap — the one I named at 799 and declined to supply — is supplied by RED's own
clause, not by a new hypothesis.** RED requires that *"Causally dependent writes retain their
causal order"*, which forces the write order to be a **linear extension** of the causal order.
Every prefix of a linear extension is a **down-set** (verified). So the exhaustion stages are
causally down-closed *by derivation*, and the identification 799 left open follows:

```text
On any finite down-set, any two linear extensions are connected by ADJACENT TRANSPOSITIONS OF
INCOMPARABLE elements — which is exactly RED's licensed move, "interchanging spacelike-disjoint
writes."   Verified: 120 random posets, n = 4,5,6, every one connected, up to 360 extensions.
Cofinality + local finiteness carry it to every finite stage; ARCH section 3's strong convergence
carries the agreement to the limit.
```

**And E4b has a second, independent support I had not found at 799**: the packet-sealed outgoing
record-GNS completion proves the exact compatibility `omega_M(iota_NM(A)) = omega_N(A)` — *"for the
full matrix algebra and every finite `M>N`; it is not inferred from the three-cell regression."*

**Task B's finding: a Møller-type precursor EXISTS and is DERIVED**, in files 799 never cited:

```text
stable_dressed_record_monomorphism_derived = true          [ERRATUM, packet-sealed]
OUTGOING_RECORD_GNS_AND_DRESSED_NET_DERIVED                [GNS COMPLETION, packet-sealed]
```

**and the same files state, unprompted, exactly what it is not:**

> *"This is not a construction of a global source-inclusive state limit or an infinite-future
> source Moller **unitary**."*

```text
THE GAP, TYPED EXACTLY:  MONOMORPHISM vs UNITARY, and COMPLETED-RECORDS/FINITE vs
INFINITE-FUTURE SOURCE-INCLUSIVE.  A unital injective star-homomorphism whose range is NOT
generally inside the bare record-only algebra does not supply an intertwiner between the
dressings induced by two cofinal exhaustions.  That is what E4c needs and what is missing.
```

**E4c is a real boundary, but a narrower one than 799 reported, and 799's picture was incomplete.**

---

## 0. Preflight

```text
OUTPUT NAME  probed recursively: artifact and sidecar ABSENT.
RED   CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_RESULT_V001.md   3359960fb411eff8…  manifest OK
ARCH  CAUSAL_DIRECT_LIMIT_ARCHITECTURE_ADJUDICATION_RESULT_V001.md  9be3f55fd527b9a8…  manifest OK
CDLP  CAUSAL_DIRECT_LIMIT_RECORD_PRINCIPLE_V001.md          625b4ed9c91b28dd…  manifest OK
799   STAGE8_C1_E4_VERIFICATION_DARIO_V001.md               b760e1b91c931545…  .md.seal OK

TASK-B SURVIVORS, seal provenance stated precisely (LAW 8, and mode 3 was decisive):
   ERR   R3_4_SHARED_SOURCE_OUTGOING_RANGE_ERRATUM_V001.md      5f8e60629f2b0fc7…
   GNS   R3_4_OUTGOING_RECORD_GNS_COMPLETION_RESULT_V001.md     10909b5c21e73ecf…
   RNG   R3_4_DRESSED_OUTGOING_RECORD_RANGE_RESULT_V001.md      3240d935ef558948…
   All three carry NO adjacent sidecar in EITHER spelling.  All three are PACKET MEMBERS and
   STAGE7_PACKET_MANIFEST_V001.sha256 verifies each: OK.
   INTEGRITY CHECK RUN (the V011 hazard pattern): the workspace copy and the packet copy of each
   are BYTE-IDENTICAL, so the quotations below are packet-sealed content.
   SCP   R3_4_SHARED_SOURCE_CAUSAL_PARENT_RESULT_V001.md        781608f2fe4c8753…  — seal not
   established by any of the three modes; cited only as the SUPERSEDED historical phrasing, and
   carried at that grade.

GATES: alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false.
No member binding; no fixed-point execution; no end test; no numeric evaluation of physical
quantities; no comparison to measured constants.  NO CHANNEL DECOMPOSITION SELECTED (ARCH's bar).
NO SMOOTH IMPORT (S26).  NO EM IDENTIFICATION (S08).  No common cell formed; no junction map
evaluated.  PE-1..PE-7 pointer-known, zero weight, not opened, not consulted.
```

---

## 1. TASK A — E4b: THE PROOF

### 1.1 The statement

> Any two cofinal exhaustions of the locally finite causal complex differ only by
> spacelike-disjoint interchanges — precisely, passage between two cofinal increasing sequences of
> finite subcomplexes can be realized through RED's licensed moves.

### 1.2 Step 1 — the write order is a linear extension. **Derived from RED, not assumed.**

`RED`, verbatim: *"Spacelike-disjoint controlled writes commute. Different linear extensions of the
same causal order therefore give the same circuit whenever they differ only by interchanging
spacelike-disjoint writes. **Causally dependent writes retain their causal order.**"*

The final clause is the constraint: a write order must respect `≤`. **That is precisely the
definition of a linear extension of the causal order.**

### 1.3 Step 2 — exhaustion stages are causally down-closed. **Derived, not hypothesised.**

```text
VERIFIED: 200 random linear extensions of random 6-element posets, all prefixes tested
          ->  down-set violations: 0.
```
Every prefix of a linear extension is a down-set (order ideal). An exhaustion compatible with a
write order has its stages among those prefixes. **So down-closure is a consequence of step 1, and
the ingredient 799 named as missing is supplied by RED's own clause.**

### 1.4 Step 3 — the connectivity core. **Verified, not asserted.**

```text
CLAIM: on a finite poset, any two linear extensions are connected by ADJACENT TRANSPOSITIONS OF
       INCOMPARABLE elements — i.e. by RED's licensed move.

 n   posets tested   all connected?   (min,max) # linear extensions
 4        40              TRUE            (1, 24)
 5        40              TRUE            (2, 60)
 6        40              TRUE            (4, 360)
```

Incomparable in the causal order = **spacelike-disjoint**. So the connecting moves are exactly the
ones `RED` licenses, and `RED`'s clause then gives *the same circuit* along the whole path.

### 1.5 Step 4 — from finite stages to the limit

Let `{V_n}`, `{W_m}` be two cofinal exhaustions of `V`. Cofinality gives, for each `n`, an `m` with
`V_n ⊆ W_m`, and conversely. Hence every finite down-set `D ⊆ V` is eventually contained in both.
On `D`, §1.4 connects the two induced write orders by licensed moves, so `RED` gives the same
circuit on `D`. Local finiteness makes each `D` finite, so this applies to every stage. `ARCH`
§3's strong convergence then carries stagewise agreement to the direct limit.

```text
E4b = PROVED.   E4 REDUCES TO E4c ALONE.
CARRIED HYPOTHESES, unchanged from 799 and not enlarged: the adopted primitive controlled-write
rule; ready_state_boundary_condition_derived = false; finite interaction-window closure.
```

### 1.6 An independent second support, found in Task B

`GNS` proves the exact compatibility of the stabilized states along the embeddings:

```text
omega_M(iota_NM(A)) = omega_N(A),
"This proof applies to the full matrix algebra and every finite M>N; it is not inferred from the
 three-cell regression."
```

**That is exhaustion-compatibility of the record states, proved independently of §1.2–1.5.** E4b
therefore rests on two supports that do not share a step.

---

## 2. TASK B — THE MØLLER HUNT

### 2.1 Searched space, controls, probes

```text
GLOB: ./workspace/**/*.{md,json} + ./supervision/**/*.{md,json}
      evaluator_build_A/ and checks/ EXCLUDED BY LAW; memory-bank never searched.
CORPUS SEARCHED: 2,987 files.
CONTROLS (case-sensitive, all non-zero): Moller 86 ; outgoing 232 ; "causal exhaustion" 19 ;
      "direct limit" 46.
PROBES (18 forms): Moller ; wave operator 4 ; scattering limit 1 ; asymptotic intertwiner 0 ;
      intertwiner 233 ; channel unitary 0 ; channel decomposition 4 ; in/out map 0 ; in-out map 0 ;
      dressing transformation 0 ; dressed map 13 ; adiabatic limit 0 ; interaction picture 7 ;
      S-matrix 7 ; S matrix 0 ; asymptotic completeness 1 ; asymptotic algebra 0 ; tail algebra 8.
OCCURRENCE TYPING of all 'Moller' hits: 41 status-FLAG (…_derived = false/true), 39 DEMAND /
      absence, 127 NEITHER — the 127 were read.
```

### 2.2 Survivors — what each DISPLAYS

| # | file | displays | grade |
|---|---|---|---|
| **S1** | `ERR` `5f8e6062…` | corrects *"public-record Moller endomorphism"* to **"stable dressed outgoing-record monomorphism"**; the map is *"a unital injective star-homomorphism"* whose *"range is a source-dressed record algebra inside the full parent"* and *"not generally contained in the bare record-only algebra"*. Flags: `stable_dressed_record_monomorphism_derived = true`, `bare_record_endomorphism_derived = false`, `complete_parent_to_outgoing_GNS_map_derived = false` | **DISPLAY, packet-sealed** |
| **S2** | `GNS` `10909b5c…` | verdict `OUTGOING_RECORD_GNS_AND_DRESSED_NET_DERIVED`; supplies a compatible state on the quasi-local outgoing record algebra, its GNS representation, strongly continuous identity dynamics on completed records, a coherent source-dressed incoming record net, a recoverable central sequence separating public output labels; and the exact compatibility theorem of §1.6 | **DISPLAY, packet-sealed** |
| **S3** | `RNG` `3240d935…` | the full matrix-unit range audit behind S1 | **DISPLAY, packet-sealed** |
| **S4** | `SCP` `781608f2…` | the historical *"outgoing local endomorphism"* / *"public-record Moller endomorphism"* phrasing | **SUPERSEDED by S1; seal not established — carried at that grade** |

### 2.3 Does `ARCH`'s bar apply to USING them?

```text
NO.  ARCH bars "SELECTING that channel decomposition now".  S1-S3 are DERIVED RESULTS, not
selections, so consuming them is lawful and no channel decomposition is chosen here.
BUT they do not reach E4c, for the reason the files themselves state.
```

### 2.4 What is still missing — the same files say it

`GNS`, verbatim: *"This is not a construction of a global source-inclusive state limit or an
**infinite-future source Moller unitary**."*
`ERR` flags: `complete_parent_to_outgoing_GNS_map_derived = false`,
`complete_physical_durability_derived = false`.

```text
THE GAP, TYPED:
  (i)  MONOMORPHISM vs UNITARY.  S1 gives a unital injective star-homomorphism.  E4c needs an
       INTERTWINER between the dressings induced by two cofinal exhaustions.  A monomorphism
       whose range is not generally inside the bare record-only algebra does not supply one:
       injectivity gives no inverse on the complement of the range.
  (ii) COMPLETED-RECORDS / FINITE vs INFINITE-FUTURE SOURCE-INCLUSIVE.  S2's compatibility is
       exact for every finite M > N; the object E4c needs is the infinite-future, source-inclusive
       limit, which S2 explicitly declines to construct.
```

```text
E4C_BOUNDARY = NARROWED (precursor found, scope stated).
   NOT CERTIFIED-REAL: the hunt did not return empty — a derived precursor exists.
   NOT DISSOLVED: the target object is absent, and the same sealed files say so unprompted.
   SOLE LAWFUL ROUTE unchanged, per ARCH section 5: "from the same target-independent
   source-record-gravity-gauge action required by R3."
```

---

## 3. CONSEQUENCE FOR C1

```text
C1 MAY NOW ASSUME, in addition to 799's E4a:
   E4b — the stabilized local record states agree across cofinal exhaustions — PROVED, on two
   independent supports (section 1.5 combinatorial; section 1.6 the sealed compatibility theorem),
   carrying only 799's three unchanged hypotheses.
C1 MUST STILL CARRY:
   the dressed-maps disjunct alone, now as a NARROWED live falsifier: not "no Moller object exists"
   but "the derived object is a MONOMORPHISM on completed records, not a UNITARY on the
   infinite-future source-inclusive limit."  C1 may consume S1-S3 lawfully; it may not treat them
   as closing E4c, and it may not select a channel decomposition to bridge the difference.
```

---

## 4. FREEDOMS CONSUMED, FLATTENING CHECK

### 4.1 `FREEDOMS_CONSUMED` — item by item against §1–§3 (law 2a)

| datum | tag | where |
|---|---|---|
| the exhaustion `{V_n}` | **CARRIED-AS-PARAMETER** — none selected; §1 quantifies over all cofinal pairs | §1.5 |
| the write order / linear extension | **DERIVED FROM RED**, not chosen — and §1.4 shows the choice among them is immaterial | §1.2–1.4 |
| down-closure of stages | **DERIVED** (§1.3), not assumed | §1.3 |
| the primitive controlled-write rule | **CARRIED AS ADOPTED ELSEWHERE** | §1.5 |
| the ready-state boundary condition | **CARRIED AS HYPOTHESIS** (flag false) | §1.5 |
| the outgoing channel decomposition | **NOT SELECTED — ARCH's bar quoted and obeyed** | §2.3 |
| the Møller unitary | **CARRIED AS ABSENT**; the precursor consumed only as a derived result | §2.4 |
| the root spectral measure `rho_f` | **NOT SELECTED** | not used |
| the causal complex `V` | **CARRIED-AS-PARAMETER** — no complex selected | §1 |
| the `F`-assignment rule (795/798) | **NOT SELECTED** — untouched | — |
| scaling weights (law 2a) | **NONE CONSUMED** | — |

**SUBSTITUTED: none.**

### 4.2 `FLATTENING_CHECK` — against `DECLINE_REGISTER_V002` (S01–S37)

```text
S24  "any lane reach for a CLUSTERING AXIOM blocks immediately; adoption is principal-only and may
     not rescue a route"                    LIVE AND OBSERVED — E4c is not rescued by reaching for
     a clustering or channel-decomposition axiom; the precursor is consumed as a DERIVED result.
S26  C_ref barred as a source               CLEAN — not invoked.
S08  no EM / smooth-field identification    CLEAN — the argument is order-theoretic and operator-
     algebraic; no physical field reading attached.
S27, S28, S01 and the remaining rows: not touched.
FLATTENING_CHECK = clean (37 rows walked; 3 live, all discharged).
```

---

## 5. JURISDICTION AND VERB AUDIT

**Jurisdiction.** *What was the rule written to protect?* The relay's own standard: *"false
boundaries come from summaries; real ones are record-voiced AND survive their dedicated hunt."*
The risk was live and it landed on me — 799's E4c was record-voiced but had **not** survived a
dedicated hunt, because I had not run one. *Does the outcome space distinguish narrowing from
dissolving?* Yes: §2.4 shows the target absent in the same files that supply the precursor.
*Would evidence look different if E4b were false?* Yes — §1.4's connectivity would fail on some
poset. It failed on none.

**VOID CONDITION.** No exhaustion, channel decomposition, spectral density, or complex selected.
The one move that would close E4c is the one `ARCH` bars, quoted at §2.3.

**BR-1.** The relay authorises both audits; it does not authorise their results. Task A closes in
the record's favour; Task B narrows a boundary I had reported more absolutely at 799.

**Builder independence.** No `evaluator_build_A/` or `checks/` file read. `~/.codex` untouched;
`memory-bank` never searched.

### 5.1 Self verb audit — **NOT CLEAN: three disclosures**

1. **799 named E4c's missing object from flag-reading alone.** I cited two `= false` flags and a
   prose bar, and called it "the exact failing step". A derived precursor — a packet-sealed
   monomorphism with a GNS completion and an exact compatibility theorem — existed the whole time
   in files I never opened. **The obstruction was real; my account of it was thin, and only a
   dedicated hunt showed how thin.**
2. **The same hunt found E4b's second support**, which I also missed at 799 while calling E4b
   "SUPPLIED-MODULO". The compatibility theorem was two files away from the ones I did read.
3. **Law 8 mode 3 was decisive again, and my first probe still got it wrong.** My per-file loop
   reported all four survivors "unsealed?" because it hashed the workspace copies, which carry no
   adjacent sidecars; the packet copies are manifest-verified. **I now also ran the byte-identity
   check between the two copies** — the V011 hazard pattern — and they match, so the quotations
   are packet-sealed content. Without that check the citations would have been ungraded.

*Direction check:* all three run against my own prior relay. Task A's closure is the relay's
positive result and it is delivered with its carried hypotheses unchanged and unenlarged.

---

```text
E4B = PROVED (E4 reduces to E4c alone).
   Step 1: RED's clause "Causally dependent writes retain their causal order" FORCES the write
   order to be a LINEAR EXTENSION of the causal order — derived, not assumed.
   Step 2: every prefix of a linear extension is a DOWN-SET (verified: 200 random extensions, all
   prefixes, ZERO violations), so exhaustion stages are causally down-closed BY DERIVATION — this
   supplies the very ingredient 799 named as missing, from RED's own clause.
   Step 3: on a finite poset any two linear extensions are connected by ADJACENT TRANSPOSITIONS OF
   INCOMPARABLE (= spacelike-disjoint) elements — verified on 120 random posets, n = 4,5,6, every
   one connected, up to 360 extensions.  Those are exactly RED's licensed moves, so RED's clause
   gives THE SAME CIRCUIT along the connecting path.
   Step 4: cofinality + local finiteness put every finite down-set eventually inside both
   exhaustions; ARCH section 3's strong convergence carries stagewise agreement to the limit.
   SECOND, INDEPENDENT SUPPORT (found in Task B, missed at 799): GNS proves
   omega_M(iota_NM(A)) = omega_N(A), "for the full matrix algebra and every finite M>N; not
   inferred from the three-cell regression."  E4b rests on two supports sharing no step.
   CARRIED HYPOTHESES unchanged and unenlarged: the adopted primitive controlled-write rule;
   ready_state_boundary_condition_derived = false; finite interaction-window closure.
MOLLER_HUNT = ./workspace/**/*.{md,json} + ./supervision/**/*.{md,json} (2,987 files;
   evaluator_build_A/ and checks/ excluded by law; memory-bank never searched).
   Controls all non-zero: Moller 86, outgoing 232, "causal exhaustion" 19, "direct limit" 46.
   HITS_RAW = 18 probe forms; 'Moller' occurrences typed 41 status-FLAG / 39 DEMAND / 127 NEITHER,
   the 127 read individually.
   SURVIVORS = 4: ERR R3_4_SHARED_SOURCE_OUTGOING_RANGE_ERRATUM_V001 (5f8e6062…, packet-sealed) —
   "stable dressed outgoing-record monomorphism", a unital injective star-homomorphism whose range
   is a source-dressed record algebra inside the full parent and NOT generally inside the bare
   record-only algebra, stable_dressed_record_monomorphism_derived = true ; GNS R3_4_OUTGOING_
   RECORD_GNS_COMPLETION_RESULT_V001 (10909b5c…, packet-sealed) — verdict OUTGOING_RECORD_GNS_AND_
   DRESSED_NET_DERIVED, with compatible state, GNS representation, strongly continuous identity
   dynamics, source-dressed incoming net, recoverable central sequence, and the exact compatibility
   theorem ; RNG R3_4_DRESSED_OUTGOING_RECORD_RANGE_RESULT_V001 (3240d935…, packet-sealed) — the
   matrix-unit range audit behind ERR ; SCP R3_4_SHARED_SOURCE_CAUSAL_PARENT_RESULT_V001
   (781608f2…) — the SUPERSEDED "outgoing local endomorphism" phrasing, seal not established by any
   of the three modes and carried at that grade.
   ARCH's bar does NOT apply to USING S1-S3: it bars SELECTING a channel decomposition, and these
   are DERIVED results.  No channel decomposition is selected here.
E4C_BOUNDARY = NARROWED (precursor found, scope stated).  NOT CERTIFIED-REAL — the hunt did not
   return empty.  NOT DISSOLVED — the target is absent and the same sealed files say so unprompted:
   "This is not a construction of a global source-inclusive state limit or an infinite-future
   source Moller UNITARY."  THE GAP, TYPED: (i) MONOMORPHISM vs UNITARY — E4c needs an INTERTWINER
   between the dressings induced by two cofinal exhaustions, and a unital injective
   star-homomorphism whose range is not generally inside the bare record-only algebra supplies
   none, injectivity giving no inverse off the range; (ii) COMPLETED-RECORDS/FINITE vs
   INFINITE-FUTURE SOURCE-INCLUSIVE — GNS's compatibility is exact for every finite M > N, while
   E4c needs the infinite-future source-inclusive limit GNS declines to construct.  SOLE LAWFUL
   ROUTE unchanged (ARCH section 5): "from the same target-independent source-record-gravity-gauge
   action required by R3."
   C1: may now assume E4a AND E4b; must still carry the dressed-maps disjunct alone, as a NARROWED
   live falsifier — not "no Moller object exists" but "the derived object is a MONOMORPHISM on
   completed records, not a UNITARY on the infinite-future source-inclusive limit."  C1 may consume
   S1-S3 lawfully; it may not treat them as closing E4c, nor select a channel decomposition to
   bridge the difference.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3): (1) 799 NAMED E4c's MISSING OBJECT FROM FLAG-READING ALONE — I
   cited two "= false" flags and a prose bar and called it the exact failing step, while a derived
   packet-sealed precursor (monomorphism + GNS completion + exact compatibility theorem) sat in
   files I never opened; the obstruction was real, my account of it was thin, and only a dedicated
   hunt showed how thin; (2) the same hunt found E4b's SECOND SUPPORT, which I also missed at 799
   while calling E4b "SUPPLIED-MODULO"; (3) LAW 8 MODE 3 WAS DECISIVE AGAIN AND MY FIRST PROBE
   STILL GOT IT WRONG — the per-file loop reported all four survivors "unsealed?" because it hashed
   the workspace copies, which carry no adjacent sidecars, while the packet copies are
   manifest-verified; I then ran the byte-identity check between the two copies (the V011 hazard
   pattern) and they match, so the quotations are packet-sealed content.
   All three run against my own prior relay.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
