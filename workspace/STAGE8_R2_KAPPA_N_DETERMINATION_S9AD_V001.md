# STAGE 8 — r-2 OF RECORD: THE BEHAVIOR OF kappa_n FOR THE CARRIER COMPRESSION FAMILY OF RECORD

## BLIND BUILDER — CODENAME R2-BUILD — COMMISSION S9AD — [SEALED]

Date: 2026-08-14
Role: BLIND builder (R2-BUILD). Task: determine, from the sealed stock only,
the behavior of kappa_n := ||[C_n, P]||_2^2 as n -> infinity for THE CARRIER
COMPRESSION FAMILY OF RECORD — the actual sealed family named by the adopted
clause R-L4a-N (Q-pointer tokens "Q-1059"/"Q-1062" inside the sealed artifacts
are EXPECTED-UNLOCATABLE by design; noted, not chased) — not for a model
family, not for the abstract constraint class.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC DERIVATION ONLY — one CAS battery (sympy 1.14.0,
fresh venv `r2venv` under /private/tmp scratch), reproduced verbatim in §6
(6/6 PASS, one final run, no pre-final corrections); every constant symbolic
or an exact rational; NOTHING numeric evaluated; no measured constant; no
value frozen (no numeric kappa, M, delta, or D; the conclusion is a LIMIT
statement, not a number); M(t) and 1_{D_t} consumed SHARP (D6'); the stricken
display (E1 :773-778) consumed NOWHERE as ground; no register/road/ledger/
lens/plan/tracker/THE_HANDOFF file opened; no git action; no existing file
edited; ONE file written (this artifact) plus its seal sidecar; output path
probed before write: ABSENT. Every enumeration/uniqueness sentence below
claims only its own displayed sweep. Marks: DERIVED / CLAIMED /
CONDITIONAL(premise named) span by span.

---

## 0. VERDICT IN ONE LINE

**(B1-DERIVED): kappa_n -> +infinity at the FULL-family quantifier (every
subsequence), per admitted cell at every collapse-bearing cell time
(r(t) > 0), per frozen oscillator length ell in {1, sqrt2}, uniformly the
same object for all four opposite-phase pairs — derived from three sealed
inputs and one exact functional-analytic step: (i) the continuum commutator
is NOT Hilbert-Schmidt of record (C6 frozen input: ||[C, 1_B]||_2 =
+infinity, radius-independent by the exact dilation invariance of C1's sealed
sea symbol C(p) = (I - alpha.p-hat)/2); (ii) the family of record converges
STRONGLY to the continuum pair — C_n -> P_- = C sealed and DERIVED
(a79939ad; and the mixed alternative Q_n P_- Q_n -> P_- by the exact
splitting QPQ - P = QP(Q-I) + (Q-I)P with Q_n -> I strongly, PA A1); (iii)
the Hilbert-Schmidt norm is lower semicontinuous under strong operator
convergence (exact finite-subset Bessel argument, displayed in §3 step L),
so liminf_n kappa_n >= ||[C, P]||_2^2 = +infinity. The identification of the
named family's projection realization is derived at a displayed sweep of the
sealed candidate set and carried as the ledger's one PREMISE(named), with
the divergence conclusion itself IMMATERIAL(derived) across both sealed
covariance schemes. Per the adopted clause N.3, this is a candidate
(B1)-type input: it discharges det_n(0) -> 0 at the baseline point once
sealed and checked; the registrar consumes, this artifact fires nothing.
r-3 (the joint polydisc rate) is untouched and remains the named unsealed
input; R-L4b is NOT discharged here. Consistency with the sealed dichotomy's
displayed inequalities checked exactly in §5; CAS battery 6/6 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

Workspace files under `/Users/bgm/MB Work/alpha-program-archive/workspace/`;
the clause and the guard under `/Users/bgm/MB Work/alpha_supervision/`.
Every digest recomputed from bytes at path THIS session by FULL digest and
compared against sidecar and/or the citing artifact's pinned digest.
13/13 MATCH at their governing references; one superseded-prefix note (G-13).

```text
G-1  72d5343ea2961e16733b01a90e517f92171be7f603eb95bf1999efdfc4839aa7
     R_L4A_N_CLAUSE_V002_ADOPTED_2026-08-14.md          MATCH (path, sidecar,
     tasked digest) — THE ADOPTED CLAUSE; N.1 naming; N.3 dichotomy/kappa_n
     definition; N.4; N.5 F-a..F-d; read in full.
G-2  e1da7446242de98a4997b778eaad5e791e192084852d4a8e4024e4b87c690ed6
     STAGE8_R1_NAMING_CANDIDATE_V001.md                 MATCH (tasked digest)
     — S2 necessity determination (witness models V/NV, KNOWN OF RECORD,
     cited not re-derived); §1 seal table; read in full.
G-3  468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5
     STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md        MATCH — §B frozen
     inputs C1/C2/C4/C6 (:319-360); R.0 block-triangular identity R-L1
     (:657-666); R.1 S3-deletion grounds (:710-718); R.2 baseline display
     and clauses (:739-800).
G-4  a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0
     STAGE8_RL4_RL0_CERTIFICATION_V001.md               MATCH — §2.3(a)-(d)
     read in full: the fixed-n exact structure, the sealed dichotomy, the
     sealed divergence tr(C_n P C_n) -> infinity, the (r-2) residue.
G-5  685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52
     STAGE8_RL4_RL0_CHECK_V001.md                       MATCH (digest only)
     — consumed only via the adopted clause's N.3(B3) (check n-1:
     sufficiency, not partition); bytes not re-read this session.
G-6  26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7
     STAGE8_ZERO_FREE_DERIVATION_V001.md                MATCH — §4.1(a)-(c)
     (:321-346) and §4.2-§4.3 (:348-425) read: the n-indexed family of
     record, A_{mu lambda, n}(a) = C_n(V(a)-1)C_n; o-3.
G-7  52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241
     STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md      MATCH (digest only)
     — consumed via CERT §2.3(b)/ZF §4.1(c) byte-verified citations (the
     per-pair uncompressed limit refuted; only the n-indexed family exists).
G-8  789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3
     STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md
     MATCH — PA carrier construction read (A1-A5): H_(n,ell) Hermite-
     Galerkin carriers, nested, strongly convergent; M_(n,ell)(t) =
     Q 1_(|x|<=r(t)) Q; the two pinned state schemes; frozen-authority
     digest table (used to chase G-9..G-12).
G-9  a79939adf1d7185fdf4d6ec5ccb929de2e4f5997bee2ed085c0d63164dc8e370
     STAGE8_T7_PURE_HERMITE_VACUUM_STRONG_CONVERGENCE_RESULT_V001.md
     MATCH (path + PA table) — read in full; verdict
     PURE_HERMITE_VACUUM_STRONG_CONVERGENCE_DERIVED:
     C_n^(pure) = 1_(-infinity,0)(Q_n h_0 Q_n) -> P_- STRONGLY.
G-10 235246abd1c4df69c80bda8f79494c342e30178504dadec411612c18d6f8685b
     STAGE8_T7_EXACT_HERMITE_MIXED_COVARIANCE_RESULT_V001.md
     MATCH (path + PA table) — read in full; C_n^(mix) = Q_n P_- Q_n,
     Hermitian contractions, nest exactly; "does not ... select an
     envelope realization".
G-11 5cbcd28ee493ba43e3d36158d80c4202230f056808caf2b36420f08c38fbd0d7
     STAGE8_ROUTE2_COMPLETE_QSPEC_STATE_BINDING_V001.md MATCH (path + PA
     table) — read in full; the bound state = the stationary quasifree
     state of the frozen free Hamiltonian h_0; no post-hoc state selection.
G-12 a1258dcf40732f0e3fce358a68ffdbd34bc347d70283550440582a1129bec510
     STAGE8_T7_CONTINUUM_GALERKIN_PROVENANCE_CORRECTION_V001.md
     MATCH (path + PA table) — read in full; the successor-construction
     requirements (Q_n finite rank, nested, Q_n -> I strongly); the two
     carried-forward state restrictions; THE FORMULA TYPING: "For the
     mixed state the finite quasifree formula is the general Klich
     determinant, not a Slater-compression determinant. For the pure
     state the Slater formula applies."
G-13 2baa4c31fbe566516c3f61dcac351889e914368bd33b8c96ff40a161788c4fd5
     ACTUAL_SURFACE_GUARD_V001.md                       MATCH (own sidecar)
     — NOTE: the R1-era pinned prefix 43db1d77 differs; consistent with the
     guard append the adopted clause's N.7 itself records ("MACHINERY/
     RELEVANCE (guard append)"). The guard's ledger discipline is followed
     as tasked; no mathematical content is consumed from it.
```

EXPECTED-UNLOCATABLE tokens this session: "Q-1059", "Q-1062", "Q-1046",
"DERIVE-BEFORE-AUTHOR" (register pointers inside sealed artifacts) — noted
per standing design; not chased; nothing defaulted.

---

## 2. SUB-TASK 1 — CONSTRUCTIBILITY: WHAT THE SEALED STOCK FIXES, WHAT IT LEAVES FREE

### 2.1 The two operators of the continuum pair, fixed exactly

```text
C  (the sea):  C = 1_(-infinity,0)(h_0) = P_-, an orthogonal PROJECTION,
    symbol C(p) = (I - alpha.p-hat)/2, kernel exactly pinned including the
    off-diagonal modulus 1/(2 pi^2 |r|^3), homogeneous degree -3.
    [E1 §B C1, FROZEN INPUT, reviewer re-derived — G-3. The mixed result's
    P_-(p) = [I - alpha.p/|p|]/2 is the SAME symbol: P_- = C identified at
    bytes — G-10.]
P  (the record projector): the SHARP causal-ball projector. C4 (frozen
    input): at full tau_R, on the ball where M(t) = 1,
    sum_lambda w_lambda u_lambda = m_0 (I - P) + 1.P = P — "the weights
    PROMOTE the sharp causal-ball projector to the leading in-cell object";
    the collapse display R.2: V_{mu lambda}(0) - 1 = (phase_mu.phase_lambda
    - 1) P = -2P on OPPOSITE-phase pairs, the SAME P for all four such
    pairs. P = multiplication by 1_(|x| <= r(t)) (spinor-diagonal); sharp
    consumption is MANDATED (D6', AR-2). r(t) = min(t, 1-t).
    [E1 §B C4 :339-352; R.2 :742-762; clause N.2 — G-3, G-1.]
```

### 2.2 The family of record, and its sealed spectral-relevant data

```text
(a) THE CARRIER. H_(n,ell) = span{Hermite triples, 0 <= a,b,c < n} tensor
    C^4; NESTED in n; CONVERGES STRONGLY to the continuum carrier
    (equivalently Q_n -> I strongly, the successor-construction requirement
    G-12 restated at PA A1); ell in {1, sqrt2} both frozen and retained.
    [PA A1 — G-8; G-12.]
(b) THE FIXED-n OBJECTS. A_{mu lambda, n}(a) = C_n(V(a) - 1)C_n, finite-
    rank analytic; at the baseline on opposite-phase pairs A_n(0) =
    -2 C_n P C_n on ran C_n; det_n(0) = det_{ran C_n}(1 - 2 C_n P C_n) =
    prod_i (1 - 2 s_i), s_i in [0,1] the eigenvalues of C_n P C_n on
    ran C_n; kappa_n := ||[C_n, P]||_2^2 = 2 sum_i s_i(1 - s_i), of
    record. [ZF §4.1(b) — G-6; clause N.1/N.3 — G-1; CERT §2.3(c) — G-4.]
(c) SEALED DIVERGENCES. tr(C_n P C_n) -> infinity (the sealed divergence,
    CERT §2.3(d), C2's own divergence carried to the family — G-4, G-3).
    Uncompressed: tr(CPC) = +infinity (C2) AND ||CPC||_2 = +infinity
    (S3-deletion grounds; radius-independent by dilation covariance) AND
    ||[C, 1_B]||_2 = +infinity (C6, frozen input). [G-3, G-4.]
(d) STRONG CONVERGENCE OF THE FAMILY, SEALED: C_n^(pure) =
    1_(-infinity,0)(Q_n h_0 Q_n) -> P_- strongly, verdict DERIVED. [G-9.]
```

### 2.3 The projection realization of {C_n}: derived at a displayed sweep

The adopted clause defines {C_n} by pointing at the defining stock (N.1:
ZF §4.1(b); PA 789338ad; ZF §4.1(c); 52f2490b) and by its own displayed
algebra. Three sealed constraints jointly pin the realization:

```text
K-1 COVARIANCE SLOT. E1 R.0's determinant frame det(1 + C(V-1)) carries
    the STATE covariance in the C slot (C1 names C the sea; R.2 reads
    tr(CPC) as "the mode count of the SEA inside the ball"); the bound
    state is the quasifree state of the frozen h_0, with no post-hoc
    selection (G-11). The fixed-n C_n must be the sea object at carrier n
    — NOT the bare carrier projector Q_n (Q_n is no state's covariance,
    and Q_n -> I != C, so {Q_n} regularizes nothing sea-shaped).
K-2 PROJECTION. The clause's displayed identities are the R-L1 block-
    triangular reduction det_{ran C_n}(.) and the pair-of-projections
    identity kappa_n = 2 sum s_i(1 - s_i) (E1 :657-659, :773-776; CERT
    §2.3(c)) — both are PROJECTION algebra. G-12 types the formulas of
    record: Slater-compression determinant = the PURE scheme; the mixed
    scheme is Klich, NOT Slater-compression. The clause's frame is the
    Slater-compression frame.
K-3 CARRIER REALIZATION. The family lives on the PA carrier (clause N.1;
    R1 §2.2 "the PA carrier is the sealed construction the family lives
    on").
SWEEP (displayed; exhaustive at this sweep only): the n-indexed operator
    families the defining stock constructs are exactly four — Q_n (carrier
    projector), M_n(t) = Q_n 1_B Q_n (compressed localizer; not a
    projection), C_n^(mix) = Q_n P_- Q_n (Hermitian contraction, G-10;
    fails K-2), C_n^(pure) = 1_(-infinity,0)(Q_n h_0 Q_n) (finite-rank
    projection, G-9). Q_n and M_n fail K-1; C_n^(mix) fails K-2;
    C_n^(pure) satisfies K-1, K-2, K-3. UNIQUE at the displayed sweep.
RESIDUE (honest): no sealed sentence literally writes "C_n := C_n^(pure)";
    G-12 carries BOTH state restrictions forward "without selecting between
    them by output". The identification is therefore logged as the ledger's
    one PREMISE(named) (CH-A, §7) — with the divergence conclusion itself
    proved scheme-robust in §3 step 4 (IMMATERIAL for kappa_n -> infinity;
    the pure realization is load-bearing only for the eigenvalue-product
    DISPLAY det_n(0) = prod(1 - 2 s_i)).
```

CONSTRUCTIBILITY VERDICT: **CONSTRUCTED.** The sealed stock constructs the
actual pair (C_n, P) concretely (Hermite-Galerkin sea projections; the sharp
causal-ball projector), does NOT construct the individual eigenvalues s_i in
closed form (no closed-form spectrum exists in the stock; none is needed
below), and DOES construct exactly the three operator-grade data the
determination consumes: the strong convergence (2.2(d)), the continuum
non-Hilbert-Schmidt commutator (2.2(c)/C6), and the exact identity frame
(2.2(b)). What the stock leaves free: nothing load-bearing for the limit
behavior; the per-member zero-mode convention for 1_(-infinity,0) at a
hypothetical zero eigenvalue of Q_n h_0 Q_n (G-12's "when the finite
operator has no zero mode") affects single members only, never the tail
statement (§3, step 3 note).

---

## 3. SUB-TASK 2 — THE DETERMINATION: kappa_n -> +infinity, DERIVED

All steps exact; every input a sealed byte cited in §1-§2.

```text
STEP R (radius-independence of the continuum input). C6 states
  ||[C, 1_B]||_2 = +infinity with the divergence at the coincidence locus
  (volume diagonal). For EVERY radius r > 0: let U_r be the unitary
  dilation (U_r f)(x) = r^(-3/2) f(x/r). C1's symbol C(p) =
  (I - alpha.p-hat)/2 is a function of p-hat alone, hence EXACTLY dilation
  invariant: U_r^dag C U_r = C (CAS D1). And U_r^dag 1_(B_1) U_r = 1_(B_r)
  (|x/r| <= 1 iff |x| <= r; CAS D2). Hence
    ||[C, 1_(B_r)]||_2 = ||U_r^dag [C, 1_(B_1)] U_r||_2 = ||[C, 1_(B_1)]||_2
  (HS norm unitarily invariant) = +infinity for EVERY r > 0 — the same
  ground E1's S3-deletion already displays for ||CPC||_2 ("radius-
  independent by dilation covariance and infinite by C6"). So at every
  collapse-bearing cell time (r(t) > 0):  ||[C, P]||_2^2 = +infinity.
  [DERIVED from G-3 (C1, C6, S3 grounds); exact.]

STEP S (strong convergence of the commutators). C_n -> C strongly (G-9,
  sealed verdict). P is a fixed orthogonal projection, ||P|| = 1. For every
  vector x:
    [C_n, P] x - [C, P] x = C_n(Px) - C(Px) - P(C_n x - C x) -> 0
  (first difference: G-9 at the vector Px; second: G-9 at x, then P
  bounded). Hence [C_n, P] -> [C, P] STRONGLY. [DERIVED; exact; no
  determinant-convergence claim is used — the sealed scope caveat of G-9
  ("does not by itself imply convergence of ... determinants") is
  respected: nothing here consumes a determinant limit.]

STEP L (lower semicontinuity of the HS norm under strong convergence).
  Let T_n -> T strongly. Fix any orthonormal basis {e_j} and any FINITE
  subset J. Then sum_{j in J} ||T_n e_j||^2 -> sum_{j in J} ||T e_j||^2
  (norm convergence per vector), and for every n
    sum_{j in J} ||T_n e_j||^2 <= ||T_n||_2^2
  (a finite orthonormal-subset sum never exceeds the full HS sum; the
  omitted terms are squares — CAS D4 displays the 3x3 instance
  ||T||_F^2 - sum_{j in J} = the omitted square sum >= 0; the general case
  is the same identity term by term). Hence
    liminf_n ||T_n||_2^2 >= sum_{j in J} ||T e_j||^2   for every finite J,
  and taking the supremum over finite J:
    liminf_n ||T_n||_2^2 >= ||T||_2^2.
  [DERIVED; exact; basis-independence of ||.||_2 is classical and nothing
  below depends on the basis choice — CH-D.]

STEP 3 (the conclusion). Apply L to T_n = [C_n, P], T = [C, P] (step S).
  By step R, ||T||_2^2 = +infinity: the basis sum sum_j ||T e_j||^2
  diverges, so for every real R there is a finite J with
  sum_{j in J} ||T e_j||^2 > R, and therefore an N with
    kappa_n = ||[C_n, P]||_2^2 > R   for ALL n >= N.
  That is:   kappa_n -> +infinity   AT THE FULL-FAMILY QUANTIFIER
  (for every R, cofinitely many n; every subsequence diverges; AR-5/CH-2
  of the adopted clause honored). Each kappa_n is FINITE (finite rank per
  member, AR-1): the family diverges without any member being infinite —
  no member failure, V-N1 untouched. Per-member zero-mode note: a
  hypothetical zero mode of Q_n h_0 Q_n at some single n would alter that
  member's projection convention only; the sealed strong-convergence
  verdict (G-9) and the cofinite lower bounds above are tail statements,
  unaffected by any single member.

STEP 4 (scheme-robustness — the identification made immaterial for the
  divergence). Under the OTHER sealed covariance scheme, C_n^(mix) =
  Q_n P_- Q_n: the exact splitting QPQ - P = QP(Q - I) + (Q - I)P (CAS D6)
  with Q_n -> I strongly (PA A1/G-12) and ||Q_n P_-|| <= 1 gives
  C_n^(mix) -> P_- strongly; steps S, L, R then run VERBATIM and
  ||[C_n^(mix), P]||_2^2 -> +infinity as well. The commutator-mass
  divergence therefore holds under BOTH sealed realizations of the
  fixed-n sea; only the display det_n(0) = prod(1 - 2 s_i) (Slater-
  compression frame) is specific to the pure realization (K-2). Under the
  excluded Q_n-reading (K-1 fails) no divergence is derivable
  ([Q_n, P] -> 0 strongly gives liminf >= 0 only) — displayed as the one
  reading on which the stock decides nothing; it is excluded at K-1's
  sealed grounds, not assumed away.

SCOPE OF THE DETERMINATION (exact quantifiers): per admitted state (M-3),
  per admitted cell of D3, at every cell time with r(t) > 0 (at r(t) = 0
  the record support is empty, P = 0, and the opposite-phase collapse
  object is trivial — no statement is made or needed there), per frozen
  ell in {1, sqrt2} (each family runs the same steps; the limit pair
  (C, P) is ell-free), and for ALL FOUR opposite-phase pairs at once
  (A_n(0) = -2 C_n P C_n is pair-independent, R.2). WHY THE TRACES COULD
  NOT DECIDE AND THE COMMUTATOR DOES: both tr_n = sum s_i -> infinity
  (sealed, 2.2(c)) and sum s_i^2 = ||C_n P C_n||_2^2 -> infinity (same
  L-argument applied to C_n P C_n -> CPC with ||CPC||_2 = +infinity of
  record) — so kappa_n = 2(tr_n - sum s_i^2) is an infinity-minus-infinity
  form at trace grade, exactly the indeterminacy the witness models V/NV
  exhibit (KNOWN OF RECORD, G-2; cited, not re-derived). The commutator
  route bypasses it: [C_n, P] converges strongly to a LIMIT OBJECT whose
  HS mass is infinite — operator-grade data the abstract eigenvalue
  constraint class does not contain. That is precisely "consuming the
  actual sealed family's spectral data".
```

---

## 4. SUB-TASK 3 — THE BRANCH VERDICT

```text
BRANCH OUTCOME: (B1-DERIVED). kappa_n -> infinity, derived (§3), at the
full-family quantifier, for the carrier compression family of record at
the derived identification (§2.3; ledger CH-A), scheme-robust per §3
step 4.

WHAT THIS DOES AND DOES NOT DO (clause N.3/N.4 respected):
- Once this artifact is SEALED AND CHECKED it is a candidate (B1)-type
  input: per N.3(B1) it discharges the regularized vanishing at the
  baseline point, det_n(0) -> 0, via |det_n(0)|^2 <= exp(-2 kappa_n) (of
  record). CONSUMPTION IS THE REGISTRAR'S; this artifact fires nothing,
  flips no flag, retires no witness (all three R-L4 witnesses STAND until
  the registrar's check).
- (B2)/F-a CANNOT fire on any artifact consistent with this one: B2's
  hypothesis sup_n kappa_n = 2M < infinity is REFUTED by the derivation
  (a family diverging to +infinity has no finite supremum — hypothesis-
  level exclusion, no numeric evaluation). F-b/F-c/F-d likewise cannot be
  exhibited consistently with kappa_n -> infinity at the baseline point
  (|det_n(0)| <= e^{-kappa_n} -> 0 kills any liminf > 0 and any
  non-vanishing-modulus sign alternation AT a = 0; F-d concerns the
  polydisc and is r-3's territory, untouched here).
- R-L4b IS NOT DISCHARGED HERE: N.3's discharge requires the (B1) input
  PLUS the joint-rate input on the closed pair polydisc (r-3 of record:
  ||R_n Delta_n(a)||_1 = o(-log|det_n(0)|) uniformly). r-3 is NOT
  supplied, presumed, or valued by this artifact. The r-2 slot is the
  only slot addressed.
- No value is frozen: no numeric kappa_n, no M, no delta, no D, no limit
  value beyond the exact statement "kappa_n -> +infinity, det_n(0) -> 0
  at a = 0 once consumed".
```

---

## 5. SUB-TASK 4 — CONSISTENCY WITH THE SEALED DICHOTOMY'S DISPLAYED INEQUALITIES (EXACT, NEVER NUMERIC)

```text
X-1 THE B1 INEQUALITY CHAIN, exact: with u_i := s_i(1 - s_i) in [0, 1/4],
    |det_n(0)|^2 = prod_i (1 - 2 s_i)^2 = prod_i (1 - 4 u_i)
                <= prod_i e^{-4 u_i}          [1 - 4u <= e^{-4u}, u >= 0;
                                               CAS D5 = CERT §5 A4 ground]
                 = e^{-4 sum_i u_i} = e^{-2 kappa_n}
    [4 sum u_i = 2 (2 sum u_i) = 2 kappa_n, the of-record identity, CAS
    D3]. With §3's kappa_n -> infinity the right side -> 0 EXACTLY (limit
    statement, no evaluation): the displayed sufficiency (B1) is
    consistent and effective.
X-2 THE B2 LOWER BOUND (4 delta^2)^{M/(2U)}: its hypotheses are
    sup_n kappa_n = 2M < infinity AND uniform 1/2-avoidance. §3 refutes
    the FIRST hypothesis for the family of record; the bound is therefore
    never instantiated — no conflict is possible between X-1 and the B2
    display (the two branches are sufficient conditions with disjoint
    hypotheses here; the check's n-1 non-partition note is respected: the
    third region simply does not arise for this family).
X-3 THE SEALED TRACE DIVERGENCE (CERT §2.3(d)): independently RECOVERED by
    the same lower-semicontinuity route (L applied to C_n P C_n -> CPC
    with tr(CPC) = +infinity, C2): liminf tr_n = +infinity. The derivation
    is consistent with — and re-derives — the one spectral divergence the
    record had already sealed.
X-4 THE WITNESS MODELS (KNOWN OF RECORD, G-2): model NV (bounded kappa)
    violates NO sealed input at eigenvalue-data grade, and §3 does not
    contradict its existence — it shows the ACTUAL family is not NV-like,
    because NV carries no strongly-convergent operator realization with a
    non-HS limit commutator. Family-relativity is escaped by actual data,
    exactly as the necessity determination said it must be.
X-5 THE STRICKEN DISPLAY (E1 :773-778): the continuum inequality
    D^2 <= exp(-2||[C,P]||_2^2) is consumed NOWHERE above; no value of D
    is inferred; the continuum fact used (C6) enters only as the limit
    object's non-HS property inside a lower-semicontinuity argument about
    the REGULARIZED family, and the vanishing conclusion is the
    regularized det_n(0) -> 0 of the adopted clause's own (B1) — E1
    :779-781's "different and honest argument", run at the adopted naming.
```

---

## 6. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `r2venv` under /private/tmp scratch; ONE final
run, 6/6 PASS; nothing written to the workspace but this artifact and its
seal.

```python
# R2-BUILD CAS battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv r2venv
# under /private/tmp scratch). Every constant symbolic or an exact rational.
# Nothing numeric evaluated. All checks are exact-identity consistency checks
# of steps whose operator-theoretic content is derived in the artifact text.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== D1 -- sea-symbol dilation invariance (C1's exact symbol) =====
# C(p) = (I - alpha.p-hat)/2 depends on p only through p-hat = p/|p|:
# substituting p -> r p (r > 0) leaves the symbol EXACTLY invariant.
px, py, pz, r = sp.symbols('p_x p_y p_z r', real=True)
rp = sp.Symbol('r', positive=True)
sx = sp.Matrix([[0, 1], [1, 0]]); sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
sz = sp.Matrix([[1, 0], [0, -1]]); I2 = sp.eye(2)
nrm = sp.sqrt(px**2 + py**2 + pz**2)
Csym = (I2 - (px*sx + py*sy + pz*sz)/nrm)/2
Csym_r = Csym.subs([(px, rp*px), (py, rp*py), (pz, rp*pz)], simultaneous=True)
ok("D1 C((r p)) = C(p) EXACTLY for r > 0 (symbol is a function of p-hat"
   " alone; ground of radius-independence under U_r dilation)",
   sp.simplify(Csym_r - Csym) == sp.zeros(2, 2))

# ===== D2 -- dilation carries the unit ball to the r-ball =====
x, rr = sp.symbols('x r', positive=True)
ok("D2 |x/r| <= 1 iff |x| <= r for r > 0 (U_r^dag 1_{B_1} U_r = 1_{B_r};"
   " reduce-inequality equivalence, exact)",
   sp.simplify((x/rr <= 1) == (x <= rr)) in (True, sp.true)
   or bool(sp.solve_univariate_inequality(x/rr - 1 <= 0, x, relational=False)
           == sp.solve_univariate_inequality(x - rr <= 0, x, relational=False))
   or sp.Eq(sp.Piecewise((1, x <= rr), (0, True)),
            sp.Piecewise((1, x/rr <= 1), (0, True))) is not None
   and sp.simplify(sp.Piecewise((1, x <= rr), (0, True))
                   - sp.Piecewise((1, x/rr <= 1), (0, True))) == 0)

# ===== D3 -- pair-of-projections kappa identity (consistency re-run) =====
th = sp.Symbol('theta', real=True)
cth, sth = sp.cos(th), sp.sin(th)
Cm = sp.Matrix([[cth**2, cth*sth], [cth*sth, sth**2]])
Pm = sp.Matrix([[1, 0], [0, 0]])
CPC = Cm*Pm*Cm
lhs = sp.trace((Cm*Pm - Pm*Cm).T*(Cm*Pm - Pm*Cm))
rhs = 2*sp.trace(CPC) - 2*sp.trace(CPC*CPC)
ok("D3 ||[C,P]||_F^2 = 2 tr(CPC) - 2 tr((CPC)^2) ALL theta (the of-record"
   " identity kappa_n = 2 sum s_i(1-s_i); consistency with CERT A3a/R1 N4)",
   sp.simplify(Cm*Cm - Cm) == sp.zeros(2) and Pm*Pm == Pm
   and sp.simplify(lhs - rhs) == 0)

# ===== D4 -- finite-subset Bessel bound for the HS norm (LSC step ground) ==
T = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f't_{a}{b}', real=True))
fro2 = sp.trace(T.T*T)
col_sums = [sum(T[k, j]**2 for k in range(3)) for j in range(3)]
ok("D4 ||T||_F^2 - (||T e_1||^2 + ||T e_2||^2) = ||T e_3||^2 >= 0 (finite"
   " orthonormal-subset sum never exceeds the full HS sum; square displayed)",
   sp.expand(fro2 - (col_sums[0] + col_sums[1]) - col_sums[2]) == 0)

# ===== D5 -- per-factor exponential bound (consistency with CERT A4/R1 C2) =
u = sp.Symbol('u', nonnegative=True)
h = sp.exp(-4*u) - 1 + 4*u
ser = sp.Poly(sp.series(sp.exp(4*u) - 1, u, 0, 6).removeO(), u)
ok("D5 1 - 4u <= e^{-4u} on u >= 0 (h(0) = 0, h' = 4(1 - e^{-4u}),"
   " e^{4u} - 1 coefficientwise > 0): |det_n(0)|^2 = prod(1 - 4u_i)"
   " <= exp(-2 kappa_n), u_i = s_i(1 - s_i)",
   h.subs(u, 0) == 0
   and sp.simplify(sp.diff(h, u) - 4*(1 - sp.exp(-4*u))) == 0
   and all(cf > 0 for cf in ser.coeffs()))

# ===== D6 -- the mixed-family strong-convergence algebra identity =====
Q = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'q_{a}{b}'))
Pv = sp.Matrix(3, 3, lambda a, b: sp.Symbol(f'p_{a}{b}'))
I3 = sp.eye(3)
ok("D6 QPQ - P = QP(Q - I) + (Q - I)P for ALL Q, P (the exact splitting"
   " that carries Q_n -> I strongly to Q_n P_- Q_n -> P_- strongly)",
   sp.expand(Q*Pv*Q - Pv - (Q*Pv*(Q - I3) + (Q - I3)*Pv)) == sp.zeros(3, 3))

print("R2-BATTERY-DONE")
```

Output, verbatim (6/6 PASS):

```text
D1 C((r p)) = C(p) EXACTLY for r > 0 (symbol is a function of p-hat alone; ground of radius-independence under U_r dilation): PASS
D2 |x/r| <= 1 iff |x| <= r for r > 0 (U_r^dag 1_{B_1} U_r = 1_{B_r}; reduce-inequality equivalence, exact): PASS
D3 ||[C,P]||_F^2 = 2 tr(CPC) - 2 tr((CPC)^2) ALL theta (the of-record identity kappa_n = 2 sum s_i(1-s_i); consistency with CERT A3a/R1 N4): PASS
D4 ||T||_F^2 - (||T e_1||^2 + ||T e_2||^2) = ||T e_3||^2 >= 0 (finite orthonormal-subset sum never exceeds the full HS sum; square displayed): PASS
D5 1 - 4u <= e^{-4u} on u >= 0 (h(0) = 0, h' = 4(1 - e^{-4u}), e^{4u} - 1 coefficientwise > 0): |det_n(0)|^2 = prod(1 - 4u_i) <= exp(-2 kappa_n), u_i = s_i(1 - s_i): PASS
D6 QPQ - P = QP(Q - I) + (Q - I)P for ALL Q, P (the exact splitting that carries Q_n -> I strongly to Q_n P_- Q_n -> P_- strongly): PASS
R2-BATTERY-DONE
```

---

## 7. CHOICE LEDGER (Actual-Surface Guard discipline; every unforced choice, classified)

```text
CH-A THE PROJECTION REALIZATION OF {C_n} (pure Hermite vacuum
     C_n^(pure) = 1_(-infinity,0)(Q_n h_0 Q_n)): PREMISE(named),
     derivation-backed. Grounds: the three sealed constraints K-1/K-2/K-3
     with the G-12 formula typing (Slater-compression = pure), unique at
     the displayed sweep {Q_n, M_n(t), C_n^(mix), C_n^(pure)} (§2.3).
     Residue: no sealed sentence performs the identification literally;
     G-12 carries both schemes forward. What would prove it FORCED: a
     sealed byte (or the registrar's confirmation at the naming) equating
     the clause's C_n with the pure family. What makes it IMMATERIAL for
     the branch outcome: §3 step 4 — kappa-mass divergence DERIVED under
     BOTH sealed schemes; the premise is load-bearing ONLY for the
     eigenvalue-product display det_n(0) = prod(1 - 2 s_i), which is the
     adopted clause's own display (V-N3 protects it; if the clause's C_n
     were the bare Q_n — excluded at K-1's sealed grounds — the
     determination would instead be NOT-DERIVABLE-TODAY, stated openly in
     §3 step 4).
CH-B DOMAIN RESTRICTION r(t) > 0: IMMATERIAL(derived) — at r(t) = 0 the
     record support is empty (P = 0, b_D vanishes at the tips), the
     opposite-phase collapse object is trivial, and no r-2 statement is
     required there; the clause's own domain (N.2, "as displayed at E1
     :746-762") is the collapse-bearing display.
CH-C CONSUMING C6 AT COMMUTATOR GRADE (not via the stricken display):
     FORCED — the stricture (E1 :773-778) bars evaluating the CONTINUUM
     inequality and inferring D; it does not bar consuming the frozen
     input C6 itself. The only uses here are (i) the non-HS property of
     the limit object inside lower semicontinuity and (ii) the fixed-n
     inequality the clause itself displays. X-5 exhibits compliance.
CH-D BASIS CHOICE IN STEP L: IMMATERIAL(derived) — the HS norm is basis-
     independent (classical; the argument uses one arbitrary fixed basis
     and never its identity).
CH-E OSCILLATOR LENGTH ell: IMMATERIAL(derived) — both frozen values are
     covered by the same steps; the limit pair (C, P) is ell-free; no
     ell-selection by output occurs.
MACHINERY/RELEVANCE (guard append honored): the derivation's machinery is
     classical operator theory (strong convergence, lower semicontinuity)
     applied to SEALED constructions with booked surface traces (G-3,
     G-8, G-9, G-10, G-12); it is SURFACE-DERIVED, not surface-native; no
     surface verdict is anchored beyond the named conditional (CH-A) and
     the adopted clause's own consumption interface.
```

## 8. TOY_SEPARATION (self-assessment)

```text
CLAIMED CLEAN at the stated quantifiers. The witness models V/NV appear
ONLY as KNOWN-OF-RECORD context (cited from G-2, never re-derived, never
used as premises); every load-bearing input is the ACTUAL sealed family's
operator data (G-3 C1/C4/C6; G-8 A1; G-9; G-10; G-12) at its own sealed
quantifier. The CAS instances (2x2 theta-family, 3x3 symbolic matrices)
are IDENTITY-GRADE exhibits of all-matrix identities (each displayed with
its universal quantifier in the check name), not model families; no
instance is quoted wider than its derivation. The determination consumes
no eigenvalue model and freezes no spectral datum. The RULING is the
checker's/audit's, not this artifact's.
```

---

## 9. FLAG BLOCK

```text
R2_DETERMINATION = B1-DERIVED( kappa_n = ||[C_n, P]||_2^2 -> +infinity at
  the FULL-family quantifier, for the carrier compression family of
  record, per admitted state and cell, at every collapse-bearing cell
  time r(t) > 0, both frozen ell, all four opposite-phase pairs at once.
  Derivation: C6 frozen input ||[C, 1_B]||_2 = +infinity, made radius-
  independent by the exact dilation invariance of C1's sealed symbol
  (CAS D1/D2); sealed strong convergence C_n = 1_(-inf,0)(Q_n h_0 Q_n)
  -> P_- = C (a79939ad, DERIVED verdict); [C_n, P] -> [C, P] strongly;
  Hilbert-Schmidt lower semicontinuity under strong convergence (finite-
  subset Bessel, CAS D4): liminf kappa_n >= ||[C, P]||_2^2 = +infinity.
  Every kappa_n finite per member (AR-1 intact, V-N1 untouched); the
  divergence lives in the family index only (AR-4/AR-5 honored). )
CONSTRUCTIBILITY = CONSTRUCTED( the stock constructs the actual pair:
  C = sea projection with exact symbol (C1); P = sharp causal-ball
  projector (C4 promotion; D6' sharp); C_n = the Hermite-Galerkin sea
  (PA A1 + a79939ad); individual s_i not in closed form and NOT needed —
  the determination consumes operator-grade data: strong convergence +
  non-HS limit commutator + the of-record identity frame. Identification
  of the projection realization: unique at the displayed sweep
  {Q_n, M_n(t), C_n^(mix), C_n^(pure)} under sealed constraints K-1
  (covariance slot: R.0/C1/state binding 5cbcd28e), K-2 (Slater-
  compression typing: provenance correction a1258dcf; R-L1), K-3 (PA
  carrier); carried as CH-A PREMISE(named), and made IMMATERIAL for the
  divergence itself by the scheme-robustness derivation (mixed family
  Q_n P_- Q_n -> P_- strongly via QPQ - P = QP(Q-I) + (Q-I)P, CAS D6:
  same conclusion). )
BRANCH_INTERFACE = B1-INPUT-CANDIDATE-NOTHING-FIRED( once sealed and
  checked this is a (B1)-type input per adopted N.3: det_n(0) -> 0 at
  the baseline via |det_n(0)|^2 <= exp(-2 kappa_n) (X-1 exact chain).
  The registrar consumes; no flag flipped, no witness retired here; all
  three R-L4 witnesses STAND. B2/F-a hypothesis-level refuted for this
  family (no finite sup of a divergent family); F-b/F-c impossible at
  a = 0 consistently with this result; F-d/polydisc = r-3 territory,
  UNTOUCHED — the joint-rate input ||R_n Delta_n(a)||_1 =
  o(-log|det_n(0)|) is NOT supplied, presumed, or valued; R-L4b is NOT
  discharged by this artifact. )
CONSISTENCY = CHECKED-EXACT( X-1 the B1 chain prod(1-4u_i) <= e^{-2
  kappa_n} (CAS D3/D5 grounds, of record); X-2 B2's bound never
  instantiated (hypothesis refuted, no numeric touch); X-3 the sealed
  trace divergence tr_n -> infinity independently recovered by the same
  lower-semicontinuity route from C2; X-4 witness-model non-contradiction
  (NV has no operator realization with a non-HS strong limit — family-
  relativity escaped by actual data, as the necessity determination
  requires); X-5 stricken-display compliance (continuum inequality never
  evaluated, no value of D inferred; the vanishing is the regularized
  clause-(B1) statement, E1 :779-781's honest argument at the adopted
  naming). )
CHOICE_LEDGER = ONE-PREMISE-FOUR-CLASSIFIED( CH-A the projection
  realization: PREMISE(named), derivation-backed, IMMATERIAL(derived)
  for the divergence conclusion via scheme-robustness, load-bearing only
  for the clause's own eigenvalue-product display; CH-B r(t) > 0:
  IMMATERIAL(derived); CH-C C6-at-commutator-grade: FORCED (stricture
  compliance exhibited); CH-D basis: IMMATERIAL(derived); CH-E ell:
  IMMATERIAL(derived). TOY_SEPARATION self-assessed CLEAN at stated
  quantifiers; ruling is the checker's. )
SEALS_VERIFIED = 13/13( 72d5343e clause; e1da7446 R1; 46846730 E1;
  a903716c CERT; 685afac8 CERT-CHK (digest only); 26f9314b ZF; 52f2490b
  per-pair refutation (digest only); 789338ad PA; a79939ad pure-vacuum
  strong convergence; 235246ab mixed covariance; 5cbcd28e state binding;
  a1258dcf provenance correction; 2baa4c31 GUARD (own sidecar; R1-era
  prefix 43db1d77 superseded by the guard append recorded at adopted
  N.7). EXPECTED-UNLOCATABLE register tokens noted, not chased. )
FORBIDDEN_IMPORTS = none( exact symbolic only; no floating-point
  numerics; no measured constant; no value frozen (no numeric kappa, M,
  delta, D); M(t)/1_{D_t} sharp; the stricken display consumed nowhere
  as ground; no register/road/ledger/lens/plan/tracker/THE_HANDOFF file
  opened; no git action; no existing file edited; ONE file written plus
  its seal; commission-distinct path S9AD probed ABSENT before write. )
MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv r2venv under
  /private/tmp scratch; ONE final run, 6/6 PASS, script and output
  reproduced verbatim in §6; no pre-final corrections.
alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
R2_KAPPA_N_DETERMINATION_RESULT = SEALED.
```
