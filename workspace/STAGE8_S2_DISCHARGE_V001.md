# STAGE 8 — THE S2'/R-L2b^Sigma DISCHARGE ATTEMPT, THE R.3'(c) SEGMENT REMAINDER, AND THE R-L0b PHASE LOCALIZATION: WORKED AGAINST THE ADOPTED AMENDMENT

## BLIND ANALYST — CODENAME S2-DISCHARGE — [SEALED]

Date: 2026-08-14
Role: BLIND analyst (S2-DISCHARGE). Three attempts against the ADOPTED
summed-quantifier amendment (adopted by entry addendum cbfbb74c): (A) the
discharge of S2'/R-L2b^Sigma from the discharged trilemma-form bound, with
the |C|_4 bookkeeping worked exactly at the forced exponent value; (B) the
R.3'(c) segment-remainder require-clause; (C) the R-L0b per-pair phase
localization (the Q-1048 residue). Honest outcomes equal citizens per item:
DISCHARGED / PARTIAL(exact residue) / FAILS-AT / REFUTED(V-1 fires). The
amendment's falsifiers are LIVE and treated as first-class.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: EXACT SYMBOLIC ANALYSIS ONLY — one CAS script (sympy 1.14.0,
fresh venv `s2venv`), reproduced in full in §7 with output verbatim (22/22
PASS); NOTHING numeric evaluated; every constant symbolic or an exact
rational/surd; the dilation parameter L below is a FORMAL symbol, never
instantiated. No physical quantity computed, bounded numerically, or
evaluated. No scale, imported GR, or faithfulness authority. M(t) sharp
(the dilation acts on the sharp indicator branchwise-exactly, §2.3 step 1;
nothing mollified). Equal-time FALSE by C6 (every HS object below is
two-time). No register/tracker/plan/road/ledger/lens file read. Output name
probed before write: ABSENT. No git action; not registered, not committed,
not pushed.

---

## 0. VERDICT IN ONE LINE

**TASK A: PARTIAL — the discharged trilemma bound DELIVERS the summed
certification n-uniformly per admitted cell at the all-pairs instance, and
the |C|_4 bookkeeping, extracted honestly from the chain's structure by an
EXACT dilation-covariance argument, gives the summed scaling exponent
alpha' = 1/4 on the similarity class of the entered cell — NOT the forced
value 1/2. The forced alpha = beta = 1/2 (re-verified: the unique PER-PAIR
x-bookkeeping cancellation solution) does not transfer to the summed
object: the cell scale enters the a-difference ONLY as the source-polydisc
contraction a -> La (one Duhamel time-extent power — exactly the mechanism
E1 :829-830 names), while the two-time HS norm's position-volume (L^3) and
momentum-volume (L^-3) factors cancel EXACTLY. At alpha' = 1/2 the clause
is NOT delivered, and on the similarity class it is deliverable-with-content
by NO mechanism (the covariance is an equality: the |C|_4^{-1/2}-normalized
density diverges as L^{-1} under refinement unless the assembled linear
response is HS-null). V-1 does NOT fire: alpha' is a SYMBOL in the adopted
clause (NF-1) and the summed estimate HOLDS at the amendment's own
quantifier, at the derived exponent — the clause's own caution ("alpha' may
not be assumed" to equal alpha) is realized in the negative. TASK B:
FAILS-AT — the segment remainder decomposes exactly into per-pair Carleman
segments plus exponential tails; every majorant of either factors through
per-pair HS norms times rho_res; the resolvent is pair-diagonal (the colour
sum cannot move inside the segment — exact witness), the c-positivity of
the derived convexity footing BLOCKS majorant-level cancellation
(sum c_p ||Delta_{p,n}||_2^2 >= (1/8) max_p, divergent in n of record,
52f2490b), and H-R may never default; the per-pair route is BARRED by the
clause's own text. Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
STANDS. TASK C: FAILS-AT — the localization consumes the same segment
(H-R + per-pair HS), carrier-divergent of record; no fixed (n-uniform)
region is derivable. DERIVED MEANWHILE, exact: the Q-1048 region condition
must be CORRECTED — star-shaped-about-1 + zero-free is INSUFFICIENT (exact
witness: rho_{00} = i, four pairs at -i, both on zero-free stars about 1,
c-combination = 0 EXACTLY at the derived weights); the sufficient and
consumable condition is half-plane separation (0 outside the closed convex
hull): Re(e^{i theta} rho_p) >= delta for all five forces |Z_hat| >= delta.
IF that localization ever closes, the licensed consequence is exactly:
R-L0 is not needed for the n = 1 leg — no stronger. NET: the gate-list
MEMBERSHIP is unchanged (R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D); the
interior moves — the S2' HS-uniformity witness's content is delivered at
alpha' = 1/4 on the similarity class (retirement is the check/registrar's,
not mine); R.3'(c) is now THE one live analytic wall of the summed horn.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session; all match sidecars and
tasked prefixes. NONE UNVERIFIABLE.

```text
ce59b4800aecddaaae4f741da0068595439b6474631a652d795c49694677397e  STAGE8_SUMMED_S2_CANDIDATE_V001.md           MATCH (S2CAND; tasked ce59b480)
c234600005c83b2373ce1d3a33d680eacc3444964c5f0eb8f5421c546f30bb7b  STAGE8_SUMMED_S2_CANDIDATE_CHECK_V001.md     MATCH (S2CAND-CHK; tasked c2346000)
cbfbb74c59f5a88742524dc41958514b4c38418d025f85154f6e1d3594ee1a43  STAGE8_SUMMED_S2_ENTRY_ADDENDUM_V001.md      MATCH (ENTRY; tasked cbfbb74c — the ADOPTION of record)
a903716c23c1a6969932e988f90d464abe9e8ee96624cd2dce3e32416de132e0  STAGE8_RL4_RL0_CERTIFICATION_V001.md         MATCH (RL4RL0; tasked a903716c)
685afac8205b4ed2ed0a309a321f6eccc940882e89ec3dfbce70fd9b8d74af52  STAGE8_RL4_RL0_CHECK_V001.md                 MATCH (RL4RL0-CHK; tasked 685afac8)
2e4e5163bb5b9c06078890fc573dd149905975b55860dcabcc7050fb1aa02666  STAGE8_REMAINDER_UNIFORMITY_V001.md          MATCH (REM; tasked 2e4e5163)
89641f762d35c0e6d3c8fb0475e2de742663f9dcf9f08a44621b059c2bd927ec  STAGE8_REMAINDER_UNIFORMITY_CHECK_V001.md    MATCH (REM-CHK; tasked 89641f76)
26f9314bdbbf1d7638ecbdf398c891cb3ba54251d4b40314df39c03ab48c08b7  STAGE8_ZERO_FREE_DERIVATION_V001.md          MATCH (ZF; tasked 26f9314b)
aed551e3dba40a0846e540709f0608398d2f5d28764e01033efbd9b20ed1d90f  STAGE8_ZERO_FREE_CHECK_V001.md               MATCH (ZF-CHK; tasked aed551e3)
a7969f0c3a42112ee300f73617494fb77c2a415bf5b6cf4d33598c6b6c8bf9cf  STAGE8_R2_RESUMMATION_V001.md                MATCH (R2; tasked a7969f0c)
a4f2e1b7878139afd017abe5fb62c6af7bf19836f7f162ef62902265bfc03cb5  STAGE8_R2_RESUMMATION_CHECK_V001.md          MATCH (R2-CHK; tasked a4f2e1b7)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md  MATCH (E1; tasked 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  MATCH (PA; tasked 789338ad)
52f2490b187fd4b307c2af45f6238ea02f1d6839b23466fefee1dbba47ed6241  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md  MATCH (per-pair refutation; load-bearing for Tasks B/C, verified here)
```

Sealed content read at path this session and relied on: S2CAND §3.3
(S2'-O: Delta^Sigma_n, both instances, cell sup deferred to R-L0, carrier
sup in trilemma form), §4.1 (S2'/R-L2b^Sigma, alpha' a symbol), §4.3
(R.3'(a)-(e); the (c) require-clause; per-pair route BARRED), §4.4
(D-i..D-iv), §5.3 (V-1..V-6), §5.4 (NF-1 no value; alpha' a symbol);
ENTRY (adoption in force; two witnesses LIVE; falsifiers in force; f-1/f-2
corrections adopted); REM §2.1-§3.5, §5, §6 (the trilemma bound
sup_n ||X_n||_2 <= ||X||_2 <= M; X = a_+ C U(0)^dag F C + a_- C F~^dag
U(0) C + O(a^2); the double m_0 kill; dominated a-series on the closed
polydisc; grade e-i/e-ii with named overturn conditions); REM-CHK
(CONFIRMED); RL4RL0 §2.2 (census m-1..m-4), §3.1-§3.5 (R-L0 quantifier at
bytes: D3 unrestricted; x's four unvalued items u-1..u-4; R-L0b residue =
Q-1048; convexity footing all c > 0), RL4RL0-CHK §4.1 (X1/X2: powers
cancel iff alpha = beta = 1/2 UNIQUE — the forcing, CONFIRMED); ZF §4.2
(normalization; nonvanishing display), §4.3 (o-1/o-2/o-3), §4.1(c)
(per-pair carrier limit does not exist); ZF-CHK (landing-set correction);
R2 (slab closed forms; tau_R = pi/sqrt2; weights/moments of record); E1
:652-666 (R.0 objects; the linear Carleman display, valid only on the
surviving sector), :679-689 (per-cell S_i; cell sup deferred to R-L0),
:691-708 (S1/S2/S2b), :720-727 (S4 H-R; F'-3 no-default), :803-833 (R.3;
R-L2b :826-832 — "cell time extent from the Duhamel bound, and the Gevrey
b_D"; the bookkeeping downgrade :817-824), :835-848 (colour-sum closure),
:851-894 (R-L0/R-L0b; the hazard; :864 "||b_D^{(c)}||_inf = 1 EXACTLY for
every admitted cell"; :865 "the diamond profile scale-free"), :417-426 (D3
frozen unrestricted quantifier); PA :75-128 (carrier; r(t) = min(t,1-t);
M(t) = Q 1_{|x|<=r(t)} Q; v(t) = (pi/sqrt2) 32 r(t)^3; b_D; frozen
polydisc); 52f2490b flag block (RL2b_uniformity_refuted = true;
X_HS_norm_infinite = true).

---

## 2. TASK A — S2'/R-L2b^Sigma: THE DISCHARGE ATTEMPT AND THE EXACT |C|_4 BOOKKEEPING

### 2.1 The adopted clause and what the discharged chain hands it

The adopted certification (S2CAND §4.1, in force by ENTRY):
`G_hs^Sigma(C, eps) = |C|_4^{-alpha'} ||Delta^Sigma_n(a)||_2`; certify
`sup_n ||Delta^Sigma_n(a)||_2 <= |C|_4^{alpha'} G_hs^Sigma` with
G_hs^Sigma finite uniformly over the D3 refinement quantifier, a functional
of (||b_D||, tau_R, sea-kernel decay data, |w_lambda|) ONLY, EXHIBITING the
mechanism that supplies the powers; alpha' a SYMBOL (NF-1), explicitly not
assumed equal to R-L2b's alpha.

The tasked identification (check-verified, S2CAND D-NOW-1 + S2CAND-CHK):
the ALL-PAIRS instance `Delta^{Sigma,all}_n(a)` IS the assembled X_n of the
discharged chain — U(0) and F each weight-summed, the branch product
carrying `sum_{mu,lambda} w_mu^* w_lambda` per pair, all a-orders included
(REM §3.5 carries the O(a^2) polydisc orders). The discharged bound of
record: `sup_n ||X_n||_2 <= ||X||_2 <= M` (REM, CONFIRMED 89641f76), M
symbolic, carrier-free, on the closed pair polydisc at radius eps, with the
C4 kill operating BEFORE the norm — grade: identity-grade core with
estimate-grade steps e-i/e-ii carried at REM's declared grade, overturn
conditions as sealed (REM §5).

**Immediate delivery (the carrier axis).** Per admitted cell, the n-uniform
certification of the all-pairs instance is DELIVERED: sup over the
D3/Hermite-Galerkin carrier family of ||Delta^{Sigma,all}_n(a)||_2 is
bounded by the carrier-free M — this is the amendment's own certifying
quantifier (S2'-O: "claims about the n-INDEXED FAMILY WITH n-UNIFORM
CONSTANTS"; the cell sup deferred to R-L0). The C4-kill-before-the-norm
clause of S2'-O is the discharged chain's own deciding step (REM §3.2), so
V-5 has no purchase. What the delivery does NOT yet contain is the |C|_4
content — the clause demands the POWERS with their mechanism. That is
worked next, honestly and exactly.

### 2.2 The forced exponents, located exactly (what "forced" is and is not)

Of record (RL4RL0 §3.2, CONFIRMED at RL4RL0-CHK X1/X2, re-run here §7
S2j): the |C|_4 powers of the sealed PER-PAIR majorant x cancel IFF
`1 - alpha - beta = 0 AND 1 - 2 alpha = 0`, whose UNIQUE solution is
`alpha = beta = 1/2`. That is the forcing: 1/2 is the one value at which
the per-pair x-bookkeeping is scale-free — E1's own downgrade (:817-824)
marks it BOOKKEEPING CONDITIONAL ON R-L2b, and the adopted clause carries
"alpha' may turn out to equal the alpha of R-L2b; it may not be assumed
to". So the tasked question is exactly: does the discharged chain deliver
alpha' = 1/2, or does its honest |C|_4-scaling say otherwise?

### 2.3 The |C|_4 extraction — the exact dilation-covariance chain

The D3 cell family's only sealed shape datum: every admitted cell carries
its own diamond profile (RL4RL0 §3.1, confirmed by its check), the profile
scale-free (E1 :865), ||b_D^{(c)}||_inf = 1 exactly per cell (E1 :864),
and every cell inserts the FULL record cycle tau_R (the sealed hazard, E1
:867-872). Work on the SIMILARITY CLASS of the entered cell (translated
dilates, scale L, with L <= 1 for cells of refinements of the parents —
refinement only shrinks); the non-congruent residue is named in §2.6.
Every step below is CAS-pinned in §7 (labels in brackets).

```text
STEP 1 (shape congruence, exact): r_L(t) = min(t, L - t) = L r_1(t/L)
   branchwise [S2a]; |C_L|_4 = pi L^4/24 EXACTLY, so L = (24|C|_4/pi)^{1/4}
   [S2b]. The sharp M(t) is carried sharp: the dilation maps the indicator
   1_{|x|<=r_L(t)} to 1_{|x|<=r_1(t/L)} exactly — no mollification.
STEP 2 (record dressing, exact): tau_R scale-invariance (the sealed
   hazard) forces v_L(t) = (tau_R 32/L^4) r_L(t)^3 = v_1(t/L)/L —
   int_0^L v_L = tau_R exactly, sup v_L = 4 tau_R/L [S2c]. The record
   phases are UNDAMPED under refinement (lambda int v = +-pi at every
   depth) — the O7 hazard is REAL for the dressing and is not contradicted
   by anything below.
STEP 3 (free generator, exact): h_0 = |k| is homogeneous degree 1 and the
   massless band projectors are homogeneous degree 0 [S2e]; the L^2
   dilation U_L is unitary [S2g]. Hence U_L^dag h_0 U_L = h_0/L and
   U_L^dag C U_L = C, U_L^dag P_s U_L = P_s.
STEP 4 (dressed evolution, exact): substituting t = L tau in the Dyson
   series, int v_L dt is invariant (STEP 2) and every free phase becomes
   e^{-i omega L tau}: the two-level amplitudes obey
   A^{(L)}(L xi, omega) = A^{(1)}(xi, L omega) EXACTLY — pinned in closed
   form on the slab: A_{mu/L, LT}(omega) = A_{mu,T}(L omega) [S2d].
STEP 5 (the source, exact — THE MECHANISM): each Duhamel source insertion
   integrates the J vertex over the cell's time extent with amplitude
   ||b_D^{(c)}||_inf = 1 (scale-free): the insertion carries ONE factor L.
   Constant-generator pin: (H/L + aJ)(LT) = (H + (La)J)T as matrices, so
   e^{-i(H/L + aJ)LT} = e^{-i(H + LaJ)T} [S2f]; by time-ordered
   composition the same holds per Dyson order. Hence, ALL a-orders:
        V^{(L)}_{mu lambda}(a) = U_L V^{(1)}_{mu lambda}(L a) U_L^dag ,
   and therefore, for ANY weight combination (the covariance is per-pair
   exact, weight-blind):
        Delta^{Sigma,(L)}_n(a) = U_L Delta^{Sigma,(1)}_{n'}(L a) U_L^dag ,
   n' the dilated carrier index — PREMISE (named, §2.6): the
   D3/Hermite-Galerkin carrier family is closed under dilation.
STEP 6 (the norm, exact): the HS norm is unitarily invariant:
   ||Delta^{Sigma,(L)}_n(a)||_2 = ||Delta^{Sigma,(1)}_{n'}(L a)||_2 —
   AN EQUALITY. The position-volume factor (L^3 from d^3x) and the
   momentum-volume factor (L^-3 from d^3k) of the two-time HS integral
   cancel EXACTLY [S2g kernel ledger]: the cell scale survives ONLY inside
   the contracted source argument L a. This is the honest answer to "the
   discharged M is carrier-free but its |C|_4-scaling must be extracted":
   the scaling rides entirely on the polydisc contraction a -> La.
STEP 7 (the power, exact given REM's grade): REM's a-series is dominated
   on the closed polydisc with carrier-free order coefficients K_m,
   sum_m K_m eps^m = M_dom(eps) < infinity, and the m = 0 term ABSENT
   (Delta^Sigma(0) = 0 identically — the subtracted object). For
   |a_+-| <= eps and 0 < L <= 1: (L eps)^m = L·L^{m-1} eps^m <= L eps^m
   for every m >= 1 [S2h]. Hence
        sup_n ||Delta^{Sigma,(L)}_n(a)||_2 <= L · M_dom(eps)
                = (24/pi)^{1/4} |C|_4^{1/4} M_dom(eps)      [S2i].
```

**THE EXTRACTED EXPONENT: alpha' = 1/4 on the similarity class, with
G_hs^Sigma = (24/pi)^{1/4} M_dom(eps)** — carrier-free, cellulation-free,
a functional of exactly the frozen tuple (M_dom is REM's dominated
majorant: ||b_D||, tau_R, sea-kernel decay data, |w_lambda|; the
unit-normalized profile constants are the sealed construction's own). The
mechanism exhibited is EXACTLY the one E1 :829-830 names for R-L2b — "cell
time extent from the Duhamel bound, and the Gevrey b_D": one time-extent
power per source insertion (STEP 5), b_D capped at 1 (of record), the
two-time volume factors cancelling (STEP 6). Grade: STEPS 1-6
identity-grade given the sealed constructions; STEP 7 inherits REM's
e-i/e-ii estimate grade and its overturn conditions unchanged.

### 2.4 The forced value 1/2: not delivered, and not deliverable-with-content on the class

At alpha' = 1/2 the normalized density is, by the STEP-6 EQUALITY (not an
estimate),

```text
  |C|_4^{-1/2} ||Delta^{Sigma,(L)}(a)||_2
      = (24/pi)^{1/2} L^{-2} ||Delta^{Sigma,(1)}(L a)||_2 .
```

The unit-cell object's a-series has leading order m = 1 (the assembled
linear response — REM's leading V_2 (x) V_2 stratum), so
||Delta^{Sigma,(1)}(La)||_2 = L·||first-order term(a)||_2 + O(L^2): the
normalized density behaves as L^{-1}·(the linear response's HS norm) under
refinement — DIVERGENT on the similarity class [S2i limit], UNLESS the
assembled linear response is HS-NULL identically. No sealed artifact
certifies a norm LOWER bound, so this is stated as the exact conditional:
alpha' = 1/2 is deliverable on the similarity class ONLY IF
||[Delta^{Sigma,(1)}]_{O(a)}||_2 = 0 — a degeneracy nothing sealed
supports and which REM's nonzero leading-stratum display (V_2 (x) V_2,
V_2 = (8 pi^2/7)(1 - 128 rho^7) > 0 interior) structurally contradicts,
though its norm was never lower-bounded of record. CONSEQUENCE: the
discharged chain does NOT deliver the clause at alpha' = 1/2, and no other
mechanism can on the similarity class except through that degeneracy.

**V-1 ANALYSIS (the falsifier treated first-class).** V-1 voids on "an
exhibited admissible refutation of the summed estimate AT THE AMENDMENT'S
OWN QUANTIFIER". The amendment's own quantifier is the n-indexed family
with n-uniform constants, per admitted cell, cell sup deferred to R-L0
(S2'-O); and alpha' is a SYMBOL, not asserted (NF-1: "alpha' is a SYMBOL
(not 1/2, not anything)"). The summed estimate HOLDS at that quantifier —
delivered at the derived alpha' = 1/4 (§2.3). What fails is one CANDIDATE
VALUE of the symbol (the per-pair-forced 1/2) on the cell axis, an axis
the amendment defers to R-L0. V-1 does NOT fire; the amendment survives
with its own caution realized: alpha'(Sigma) = 1/4 != 1/2 = alpha(per-pair
bookkeeping, forced). No other falsifier engages: V-2 (no summed->per-pair
bridge is used or created here), V-3 (REM/ZF anchors consumed at their
sealed grade, not overturned), V-4 (no bytes contradiction — the 1/4 vs
1/2 finding contradicts no sealed FORCED result: 1/2 is of record as
conditional bookkeeping, E1 :817-824), V-5 (the kill operates, of record).

### 2.5 Instance bookkeeping (nothing papered over)

The delivery of §2.3 is at the ALL-PAIRS instance Delta^{Sigma,all} (= X,
the identification). The scaling exponent 1/4 holds for BOTH instances —
the covariance is weight-blind (STEP 5) — but the unit-cell FINITENESS
M_dom is of record only for the all-pairs object (the C4 kill needs the
full product sum; the surviving-sector restriction breaks the m_0
factorization — ZF o-2(ii), restated at S2CAND §4.4 D-i). The transfer of
finiteness to Delta^{Sigma,surv(,c)} is exactly D-i's obligation
(R-L4a/b), untouched here.

### 2.6 Task A verdict and exact residue

```text
TASKA = PARTIAL.
DELIVERED: (a) the n-uniform per-cell certification of the all-pairs
  instance (the amendment's own quantifier), via the identification + the
  trilemma bound, grade carried; (b) the |C|_4 bookkeeping, exact: the
  summed scaling exponent alpha' = 1/4 on the similarity class, mechanism
  exhibited (source contraction = Duhamel time extent; Gevrey b_D; exact
  two-time volume cancellation), G_hs^Sigma = (24/pi)^{1/4} M_dom(eps) a
  frozen-tuple functional; (c) the negative: alpha' = 1/2 (the forced
  per-pair value) is NOT delivered and is unavailable-with-content on the
  similarity class (exact conditional, §2.4); V-1 does not fire.
RESIDUE, exact: (r-A1) non-congruent admitted cells — if the unrestricted
  D3 quantifier admits cell shapes outside the similarity class of the
  entered diamond, the extraction does not cover them; that axis is
  exactly R-L0's (unchanged; ZF-CHK's landing-set correction honored).
  (r-A2) the carrier-family dilation-closure premise (STEP 5): U_L maps
  the scale-L Hermite-Galerkin family onto a carrier family at scale 1 —
  its membership in the SAME sealed family is a spec-reading no artifact
  pins; if it fails, §2.3 still bounds the uncompressed norm (which
  dominates every compression, ||Pi X Pi||_2 <= ||X||_2 of record) — the
  premise is therefore DISCHARGEABLE by that one sealed line, and is named
  only for the n' index bookkeeping. (r-A3) sharpness of 1/4 (a norm lower
  bound) is NOT claimed — 1/4 is the certified upper-bound exponent; the
  divergence claim at 1/2 is conditional on the linear response being
  HS-nonnull (§2.4). (r-A4) grade: REM's e-i/e-ii carried, overturn
  conditions live (V-3 tracks them).
WITNESS: SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED — its content is delivered
  at alpha' = 1/4 on the similarity class SUBJECT TO CHECK; retirement is
  the check/registrar's act, never this artifact's. It STANDS as of this
  writing.
```

---

## 3. QUANTIFIER DAYLIGHT — THE EXACT ACCOUNTING (the tasked question)

```text
THREE SUPS, SEPARATED AT THE BYTES:
  (q-i)  THE CARRIER AXIS (D3/Hermite-Galerkin refinement family, index n):
         the discharged bound's quantifier. Delivered by
         ||Pi X Pi||_2 <= ||X||_2 (REM) — NO cell content whatsoever.
  (q-ii) THE CELL AXIS (admitted cells of D3 = the FROZEN UNRESTRICTED
         common-refinement quantifier, E1 :417-426, :856): NOT touched by
         the discharged bound — ZF-CHK's correction of record ("the
         summed-S2' axis is the carrier/colour-sum axis and does not
         deliver the cell sup"), carried into the amendment at S2CAND §4.4
         D-ii.
  (q-iii) THE COLOUR AXIS: replaced by the sum inside (adopted, S2'-O).
THE DAYLIGHT, exactly: the adopted clause's phrase "uniformly over the D3
refinement quantifier" (S2CAND §4.1) carries E1 :826-832's discipline, in
which "the D3 refinement quantifier" is the CELL family — while S2'-O in
the same adopted text defers "the sup over CELLS ... to R-L0" and takes
"the sup over the CARRIER index n ... as in the trilemma form". So the
adopted text itself holds BOTH readings: the certification's uniformity
phrase points at the cell axis; the object's quantifier statement points
at the carrier axis. THIS ARTIFACT RESOLVES THE DAYLIGHT BY COVERING BOTH
HONESTLY: the carrier axis is discharged of record (q-i); the cell axis is
NEWLY covered on the similarity class by the exact covariance (§2.3) at
alpha' = 1/4, and REMAINS OPEN exactly on the non-congruent remainder of
the unrestricted family — which is R-L0's landing, unchanged. Under
NEITHER reading is the forced alpha' = 1/2 delivered. No daylight remains
UNNAMED: every sup is assigned, and the one unresolved quantifier segment
(non-congruent cells) is a named member of the standing gate list.
```

---

## 4. TASK B — THE R.3'(c) SEGMENT REMAINDER

### 4.1 The exact decomposition (derived)

On the surviving sector, per pair, the sealed linear Carleman display (E1
:663-666) gives `Log rho_p(a) = tr[Delta_p] - int_0^1
tr[(1+A_{s,p})^{-1} A_{s,p} Delta_p] ds`, and with R.3'(b)'s exact trace
linearity (sum_surv c = 1):

```text
  Z_hat_n(a) - 1 - tr[Delta^{Sigma,surv,c}_n(a)]
    = sum_surv c_p ( - int_0^1 tr[(1+A_{s,p})^{-1} A_{s,p} Delta_p] ds
                     + (e^{Log rho_p} - 1 - Log rho_p) ) ,
```

with the tail coefficientwise-exact: |e^z - 1 - z| <= |z|^2 e^{|z|}/2
(1/n! <= 1/(2(n-2)!) for n >= 2, i.e. (n-2)(n+1) >= 0 [S3a]).

### 4.2 The three walls, exact

```text
W-1 THE RESOLVENT IS PAIR-DIAGONAL: no linearity moves the colour sum
    inside the segment — sum_p c_p (1+B_p)^{-1} A_p is NOT a function of
    the summed operator; exact scalar witness at the derived weights:
    c = (1/2, 1/2 grouped): 3/4 != 2/3 [S3b]. R.3'(b)'s trace linearity
    is the ONE per-pair-free passage (the amendment says so itself, §4.4
    D-i); the second-order content has none. This is the m_0-factorization
    break (ZF o-2(ii)) in second-order dress.
W-2 THE MAJORANT ROUTE IS DEAD n-UNIFORMLY, OF RECORD: every standard
    majorant of either term factors through per-pair HS norms
    (|tr[BXY]| <= ||B||_op ||X||_2 ||Y||_2 forces ||Delta_p||_2, ||A_s,p||_2;
    the tail forces |Log rho_p| <= x_p). The c-weighted quadratic majorant
    obeys sum_p c_p ||Delta_{p,n}||_2^2 >= (1/8) max_p ||Delta_{p,n}||_2^2
    — every coefficient of the difference is nonnegative because ALL FIVE
    c ARE POSITIVE [S3c]: the derived convexity footing (RL4RL0 §3.5)
    turns AGAINST the estimate at the majorant level — no sign
    cancellation is available, and each per-pair norm's carrier sup is
    +infinity OF RECORD (52f2490b; ZF §4.1(c)). The scaling covariance of
    §2.3 does not rescue this: it contracts a -> La but the per-pair
    unit-cell sup is already infinite (infinity times L is infinity for
    every L > 0).
W-3 H-R: the segment consumes rho_res, a NAMED HYPOTHESIS that "may NEVER
    default to 1 or to any other value" (E1 :725-727, F'-3). Undischarged.
AND THE BAR: the per-pair discharge route is BARRED by R.3'(c)'s own
    adopted text ("the per-pair route to it is REFUTED of record
    (52f2490b) and is hereby BARRED").
```

### 4.3 Task B verdict

```text
TASKB = FAILS-AT( the summed quantifier: the second-order Carleman content
  admits NO per-pair-free passage (W-1, exact witness); every majorant
  route factors through per-pair HS norms whose c-weighted sum diverges in
  n of record with positivity blocking cancellation (W-2); and the
  segment consumes undischarged H-R (W-3). At FIXED n the remainder bound
  exists but is n-DEPENDENT (ZF o-1) — exactly what the clause forbids.
  EXACT RESIDUE: a NEW summed-level second-order identity/estimate
  controlling sum_surv c_p (rho_p - 1 - tr Delta_p) with n-uniform
  constants WITHOUT per-pair HS majorants — an object-level cancellation,
  on no sealed artifact; plus H-R; plus the R-L4-administered sector
  definition (D-i). Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
  STANDS. NOT REFUTED: the remainder itself is not shown infinite — the
  MAJORANTS diverge, the object is undecided; FAILS-AT is the honest
  class. )
```

---

## 5. TASK C — THE R-L0b PHASE LOCALIZATION (Q-1048)

### 5.1 The attempt and where it fails

The residue of record (RL4RL0 §3.5): localize the five surviving rho's in
a fixed star-shaped zero-free region about 1 on the closed pair polydisc,
from the linear Carleman estimate alone (+ the derived convexity footing,
all c > 0, conditional on R-L4b). The linear estimate IS the two-term
display: trace + segment. Localizing Arg/modulus of rho_p requires
bounding BOTH terms: the trace term consumes S1's uncertified enclosure;
the segment consumes rho_res (H-R — may never default, F'-3) AND the
per-pair HS norms ||A_{s,p}||_2, ||Delta_p||_2 — whose carrier sups are
+infinity of record (52f2490b). At fixed n a localization exists but its
region is n-DEPENDENT; the clause's "fixed" region (consumable by the
n = 1 leg uniformly) is therefore NOT derivable on the discharged basis.
The same wall as Task B, one order down.

### 5.2 What IS derived — the region condition CORRECTED (exact)

```text
SHARPENING 1 (the Q-1048 statement is INSUFFICIENT as written): star-shaped
  about 1 + zero-free does NOT close the convex-combination step at the
  derived weights. EXACT WITNESS [S4a/S4b]: the two segments
  [1, 1 + sqrt2 e^{+3 i pi/4}] and [1, 1 + sqrt2 e^{-3 i pi/4}] form a
  closed region S, star-shaped about 1, ZERO-FREE
  (|1 + s sqrt2 e^{+-3 i pi/4}|^2 = (1-s)^2 + s^2 > 0 on [0,1]); its
  endpoints are 1 + sqrt2 e^{+-3 i pi/4} = +-i EXACTLY; and the derived
  c-combination hits zero EXACTLY:
      (1/2)(+i) + 4·(1/8)(-i) = 0 .
  Five rho's in a star-shaped zero-free region about 1 can have
  Z_hat = 0.
SHARPENING 2 (the sufficient and consumable condition): HALF-PLANE
  SEPARATION — 0 outside the closed convex hull of the region.
  If Re(e^{i theta} rho_p) >= delta > 0 for all five surviving p (some
  fixed theta), then by exact linearity with sum c = 1, all c > 0 [S4c]:
      Re(e^{i theta} Z_hat) >= delta ,  hence |Z_hat| >= delta [S4d],
  Log Z_hat is defined and analytic there, and the closure is
  EXPONENTIATION-FREE — no |rho - 1| <= x e^x, no absolute x bound: the
  R-L0b shape, exactly. (A convex zero-free region also suffices —
  half-plane separation is the Hahn-Banach normal form.)
```

### 5.3 Task C verdict

```text
TASKC = FAILS-AT( the localization inputs: the linear estimate's segment
  consumes H-R (no-default, F'-3) and per-pair HS majorants
  carrier-divergent of record; no FIXED (n-uniform) region is derivable;
  fixed-n regions are n-dependent. DELIVERED MEANWHILE, exact: the
  Q-1048 region condition corrected of record — star-shaped-about-1 +
  zero-free is insufficient (exact witness at the derived weights);
  the sufficient condition is half-plane separation (0 outside the closed
  convex hull), under which the exponentiation-free closure is EXACT and
  elementary. THE LICENSED CONSEQUENCE, stated exactly and only
  conditionally: IF the corrected localization ever closes (an act
  consuming H-R plus a per-pair phase bound at the summed-compatible
  quantifier, on no sealed artifact), THEN R-L0 is not needed for the
  n = 1 leg — NO STRONGER. It does not close here. Witness
  SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND stands; R-L0b remains OPEN
  with its residue now stated at the corrected region condition. )
```

---

## 6. NET — WHAT THE "C EXISTS" GATE LIST BECOMES

```text
MEMBERSHIP UNCHANGED: R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D (the ENTRY
addendum's list), with H-R/H-B0 carried inside as sealed.
THE INTERIOR MOVES (all subject to check; nothing retired here):
  n-1  SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED: its clause's content is
       DELIVERED at alpha' = 1/4 on the similarity class (all-pairs
       instance; carrier axis of record; mechanism exhibited; grade
       carried). Retirement is the check/registrar's. The forced per-pair
       value 1/2 is NOT the summed exponent — downstream consumers of
       "alpha' = 1/2" have nothing to consume.
  n-2  SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED: STANDS. R.3'(c) is now
       THE one live analytic wall of the summed horn (given n-1): walls
       W-1/W-2/W-3 exact, residue named.
  n-3  R-L0: UNMOVED as an obligation (per-pair x still indeterminate —
       u-1..u-4 of record stand). NEW of record for its ground (i): on
       the summed axis and the similarity class, the compensating weight
       EXISTS and is derived — the source contraction supplies |C|_4^{1/4}
       DECAY (stronger than scale-invariance); the record-cycle hazard
       attaches to the dressing, which the a-difference cancels at
       baseline (V(0) = I surviving) — the sealed "what keeps R-L0 alive"
       (RL4RL0 §3.3(c)) answered POSITIVELY on that axis and class.
       The non-congruent-cell axis remains R-L0's entirely.
  n-4  R-L0b: OPEN; its region condition corrected (§5.2); its residue
       (per-pair phase localization) unchanged in substance, now with the
       exact insufficiency witness on the record.
  n-5  R-L4a/b, C-L2, OBL-D, H-R, H-B0: untouched, stand as sealed.
NO FLAG FLIPS (GOV-F reserved); no witness retired; the per-pair record
(52f2490b) untouched; B-L2*'s discharge unaltered; no verdict on
FIXED/RUNNING advanced or retarded by this artifact.
```

---

## 7. THE CAS SCRIPT (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `s2venv`. Exact symbolic / exact rational only;
nothing numeric evaluated.

```python
# S2-DISCHARGE CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv s2venv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated.
# Groups: S1 cross-checks (moments/unitarity/normalization); S2 the scaling
# covariance chain (Task A's |C|_4 extraction); S3 Task B obstruction pieces;
# S4 Task C region pieces.
import sympy as sp
from sympy import Rational as R

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

# ================= S1 — TASKED CROSS-CHECKS (moments, unitarity, normalization) =================
lam_v = [sp.Integer(0), sp.sqrt(2), -sp.sqrt(2)]
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
mom = lambda n: sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v)))
ok("S1a moments m_0 = m_1 = 0, m_2 = -1, m_4 = -2, m_6 = -4 (m_2j = -2^{j-1})",
   [mom(n) for n in (0, 1, 2, 4, 6)] == [0, 0, -1, -2, -4])
om = sp.symbols('omega', nonnegative=True)
mu, T = sp.symbols('mu T', positive=True)
Om = sp.sqrt(om**2 + mu**2)
A = sp.cos(Om*T) + sp.I*om*sp.sin(Om*T)/Om
B = -sp.I*mu*sp.sin(Om*T)/Om
ok("S1b unitarity |A|^2 + |B|^2 = 1 (slab closed form, re-run)               ",
   sp.simplify(sp.expand_trig(A*sp.conjugate(A) + B*sp.conjugate(B) - 1)) == 0)
ok("S1c modulus identity Om^2|A|^2 = omega^2 + mu^2 cos^2(Om T) (re-run)     ",
   sp.simplify(Om**2*A*sp.conjugate(A) - (om**2 + mu**2*sp.cos(Om*T)**2)) == 0)
ph = [1, -1, -1]
pairs = [(i, j) for i in range(3) for j in range(3)]
surv = [(i, j) for (i, j) in pairs if ph[i] == ph[j]]
opp = [(i, j) for (i, j) in pairs if ph[i] != ph[j]]
Nsurv = sp.nsimplify(sum(w_v[i]*w_v[j] for i, j in surv))
c_wts = {p: sp.nsimplify(w_v[p[0]]*w_v[p[1]]/Nsurv) for p in surv}
ok("S1d normalization: 5/4 census; N_surv(0) = 1/2; c = (1/2, 1/8 x 4) > 0;  sum c = 1 = kappa_bal",
   len(surv) == 5 and len(opp) == 4 and Nsurv == R(1, 2)
   and c_wts[(0, 0)] == R(1, 2)
   and all(c_wts[p] == R(1, 8) for p in surv if p != (0, 0))
   and sum(c_wts.values()) == 1 and sum(abs(v) for v in c_wts.values()) == 1)
ok("S1e all-pairs weight total = |m_0|^2 = 0 (the C4 kill; re-run)           ",
   sp.nsimplify(sum(w_v[i]*w_v[j] for i, j in pairs)) == 0)

# ================= S2 — THE |C|_4 EXTRACTION: EXACT SCALING COVARIANCE CHAIN =================
L = sp.symbols('L', positive=True)
t = sp.symbols('t', real=True)
tauR = sp.pi/sp.sqrt(2)

# S2a  shape congruence: the scaled diamond profile is the dilated unit profile (branchwise exact)
r_L = sp.Piecewise((t, t <= L/2), (L - t, True))
r_1 = sp.Piecewise((t, t <= R(1, 2)), (1 - t, True))
ok("S2a r_L(t) = L r_1(t/L) branchwise: t<=L/2 gives t = L*(t/L); t>L/2 gives L-t = L*(1-t/L)",
   sp.simplify(t - L*(t/L)) == 0 and sp.simplify((L - t) - L*(1 - t/L)) == 0)

# S2b  4-volume of the scaled diamond: |C_L|_4 = L^4 pi/24 ; L = (24 |C_L|_4 / pi)^(1/4)
V4L = sp.integrate(R(4, 3)*sp.pi*r_L**3, (t, 0, L))
ok("S2b |C_L|_4 = pi L^4/24 EXACT ; (24 |C_L|_4/pi)^(1/4) = L                ",
   sp.simplify(V4L - sp.pi*L**4/24) == 0
   and sp.simplify((24*(sp.pi*L**4/24)/sp.pi)**R(1, 4) - L) == 0)

# S2c  record-cycle invariance forces amplitude ~ 1/L: v_L(t) = (tau_R 32/L^4) r_L(t)^3
IvL = sp.integrate((32/L**4)*r_L**3, (t, 0, L))
ok("S2c int_0^L v_L = tau_R EXACT (cycle scale-INVARIANT) ; sup v_L = 4 tau_R/L",
   sp.simplify(tauR*IvL - tauR) == 0
   and sp.simplify(tauR*(32/L**4)*(L/2)**3 - 4*tauR/L) == 0)

# S2d  THE SLAB COVARIANCE PIN: A^{(L)}(omega) = A^{(1)}(L omega) exactly
#      (coupling mu/L over duration L T vs coupling mu over duration T at momentum L omega)
A_L = (sp.cos(sp.sqrt(om**2 + (mu/L)**2)*L*T)
       + sp.I*om*sp.sin(sp.sqrt(om**2 + (mu/L)**2)*L*T)/sp.sqrt(om**2 + (mu/L)**2))
A_1_scaled = (sp.cos(sp.sqrt((L*om)**2 + mu**2)*T)
              + sp.I*(L*om)*sp.sin(sp.sqrt((L*om)**2 + mu**2)*T)/sp.sqrt((L*om)**2 + mu**2))
ok("S2d slab covariance: A_{mu/L, LT}(omega) = A_{mu, T}(L omega) EXACT       ",
   sp.simplify(A_L - A_1_scaled) == 0)

# S2e  free-generator homogeneity: |k/L| = |k|/L (degree 1) and k-hat invariant (degree 0)
kx, ky, kz = sp.symbols('kx ky kz', real=True)
nk = sp.sqrt(kx**2 + ky**2 + kz**2)
nkL = sp.sqrt((kx/L)**2 + (ky/L)**2 + (kz/L)**2)
ok("S2e h_0 degree-1: |k/L| = |k|/L ; band projector degree-0: (k/L)_i/|k/L| = k_i/|k|",
   sp.simplify(nkL - nk/L) == 0
   and sp.simplify((kx/L)/nkL - kx/nk) == 0)

# S2f  THE SOURCE-CONTRACTION IDENTITY (constant-generator instance, matrices):
#      (H/L + a J)(L T) = (H + (L a) J) T  =>  e^{-i(H/L + aJ)LT} = e^{-i(H + LaJ)T}
a_ = sp.symbols('a')
h11, h12, h21, h22, j11, j12, j21, j22 = sp.symbols('h11 h12 h21 h22 j11 j12 j21 j22')
H = sp.Matrix([[h11, h12], [h21, h22]])
J = sp.Matrix([[j11, j12], [j21, j22]])
ok("S2f exponent identity: (H/L + a J) L T - (H + L a J) T = 0 (matrix, exact)",
   sp.expand((H/L + a_*J)*L*T - (H + L*a_*J)*T) == sp.zeros(2, 2))
# hence, per slab and (by time-ordered composition, each J insertion carrying one
# time-extent factor L) per Dyson order: V^{(L)}(a) = U_L V^{(1)}(L a) U_L^dag exactly.

# S2g  dilation unitarity and the HS volume cancellation (1D exact instance + exponent ledger)
x_ = sp.symbols('x_', real=True)
lhs = sp.integrate(sp.exp(-2*x_**2/L**2)/L, (x_, -sp.oo, sp.oo))
rhs = sp.integrate(sp.exp(-2*x_**2), (x_, -sp.oo, sp.oo))
ok("S2g dilation is unitary: int |f(x/L)|^2 dx/L = int |f|^2 dx (exact instance);"
   " kernel ledger (1/L^6)*L^3*L^3 = 1",
   sp.simplify(lhs - rhs) == 0 and sp.simplify((1/L**6)*L**3*L**3 - 1) == 0)

# S2h  the polydisc contraction absorbs one full power of L on 0 < L <= 1:
#      (L eps)^m = L^m eps^m and L^m <= L for every order m >= 1 (d/dm L^m = L^m log L <= 0)
m_, eps_ = sp.symbols('m_ eps_', positive=True)
ok("S2h (L eps)^m = L * L^(m-1) eps^m (exact split) ; d/dm L^m = L^m log L    ",
   sp.simplify((L*eps_)**m_ - L*L**(m_ - 1)*eps_**m_) == 0
   and sp.simplify(sp.diff(L**m_, m_) - L**m_*sp.log(L)) == 0)
# for 0 < L <= 1: log L <= 0 so L^m is nonincreasing in m: L^m <= L^1 for m >= 1.
# Hence sum_m K_m (L eps)^m <= L sum_m K_m eps^m = L * M_dom(eps): ONE full power of L.

# S2i  the exponent match and the alpha' = 1/2 mismatch, exact
Msym = sp.symbols('M', positive=True)
ok("S2i L M = (24/pi)^(1/4) |C_L|_4^(1/4) M (alpha' = 1/4 match) ;"
   " |C_L|_4^(-1/2) L M = (24/pi)^(1/2) M/L -> oo as L -> 0+ (alpha' = 1/2 fails)",
   sp.simplify(L*Msym - (24/sp.pi)**R(1, 4)*(sp.pi*L**4/24)**R(1, 4)*Msym) == 0
   and sp.simplify((sp.pi*L**4/24)**R(-1, 2)*L*Msym - (24/sp.pi)**R(1, 2)*Msym/L) == 0
   and sp.limit((24/sp.pi)**R(1, 2)*Msym/L, L, 0, '+') == sp.oo)

# S2j  the forced-exponent display re-run (the bookkeeping alpha = beta = 1/2 is the
#      unique cancellation solution of the PER-PAIR x-majorant — of record, re-verified)
V4s, alpha, beta = sp.symbols('V4s alpha beta', positive=True)
sol = sp.solve([1 - alpha - beta, 1 - 2*alpha], [alpha, beta], dict=True)
ok("S2j per-pair x-majorant powers cancel iff alpha = beta = 1/2 (unique; re-run)",
   sol == [{alpha: R(1, 2), beta: R(1, 2)}])

# ================= S3 — TASK B (segment remainder) OBSTRUCTION PIECES =================
n_ = sp.symbols('n_', integer=True, positive=True)
# S3a coefficientwise: |e^z - 1 - z| <= |z|^2 e^{|z|} / 2 :
#     1/n! <= 1/(2 (n-2)!) for n >= 2  <=>  n(n-1) >= 2  <=>  (n-2)(n+1) >= 0
ok("S3a n!/(n-2)! = n(n-1) (exact) ; n(n-1) - 2 = (n-2)(n+1) (exact factor)  ",
   sp.simplify(sp.factorial(n_)/sp.factorial(n_ - 2) - n_*(n_ - 1)) == 0
   and sp.expand((n_ - 2)*(n_ + 1) - (n_*(n_ - 1) - 2)) == 0)
# S3b the colour sum CANNOT move inside the Carleman segment (resolvent is pair-diagonal):
#     exact scalar counterexample with the surviving c-weights
c00, c1 = R(1, 2), R(1, 2)          # c_{00} = 1/2 ; the four 1/8-pairs grouped = 1/2
B0, B1, A0, A1 = sp.Integer(1), sp.Integer(2), sp.Integer(1), sp.Integer(1)
lhs_seg = c00*A0/B0 + c1*A1/B1
rhs_seg = (c00*A0 + c1*A1)/(c00*B0 + c1*B1)
ok("S3b sum c (1+A)^{-1} A  !=  (1 + sum c A)^{-1} (sum c A): exact witness 3/4 != 2/3",
   lhs_seg == R(3, 4) and rhs_seg == R(2, 3) and lhs_seg != rhs_seg)
# S3c positivity of c kills majorant-level cancellation: sum c_p q_p >= (1/8) max q_p
q0, q1, q2, q3, q4 = sp.symbols('q0 q1 q2 q3 q4', nonnegative=True)
qs = [q0, q1, q2, q3, q4]
cs = [R(1, 2), R(1, 8), R(1, 8), R(1, 8), R(1, 8)]
diffs = [sp.expand(sum(ci*qi for ci, qi in zip(cs, qs)) - R(1, 8)*qj
                   - sum(ci*qi for ci, qi in zip(cs, qs) if qi != qj)
                   + sum(ci*qi for ci, qi in zip(cs, qs) if qi != qj)) for qj in qs]
ok("S3c all c_p >= 1/8 > 0: sum c_p q_p - (1/8) q_j = (c_j - 1/8) q_j + sum_{i!=j} c_i q_i,"
   " every coefficient >= 0",
   all(all(coeff >= 0 for coeff in
           sp.Poly(sum(ci*qi for ci, qi in zip(cs, qs)) - R(1, 8)*qj, *qs).coeffs())
       for qj in qs))

# ================= S4 — TASK C (R-L0b localization) REGION PIECES =================
s_ = sp.symbols('s_', nonnegative=True)
# S4a the exact witness: star-shaped-about-1, zero-free, c-combination = 0
rho1 = 1 + sp.sqrt(2)*sp.exp(3*sp.I*sp.pi/4)      # = i exactly
rho2 = 1 + sp.sqrt(2)*sp.exp(-3*sp.I*sp.pi/4)     # = -i exactly
Zw = R(1, 2)*rho1 + 4*R(1, 8)*rho2
ok("S4a witness: 1 + sqrt2 e^{+3 i pi/4} = i ; 1 + sqrt2 e^{-3 i pi/4} = -i ;"
   " (1/2) rho1 + 4*(1/8) rho2 = 0 EXACTLY",
   sp.simplify(sp.expand_complex(rho1 - sp.I)) == 0
   and sp.simplify(sp.expand_complex(rho2 + sp.I)) == 0
   and sp.simplify(sp.expand_complex(Zw)) == 0)
# S4b the two star segments are zero-free: |1 + s sqrt2 e^{+-3 i pi/4}|^2 = (1-s)^2 + s^2 > 0
seg = (1 + s_*sp.sqrt(2)*sp.exp(3*sp.I*sp.pi/4))
ok("S4b |1 + s sqrt2 e^{3 i pi/4}|^2 = 1 - 2s + 2s^2 = (1-s)^2 + s^2 (discriminant -4 < 0)",
   sp.simplify(sp.expand_complex(seg*sp.conjugate(seg)) - (1 - 2*s_ + 2*s_**2)) == 0
   and sp.expand((1 - s_)**2 + s_**2 - (1 - 2*s_ + 2*s_**2)) == 0
   and (2)**2 - 4*2*1 < 0)
# S4c the corrected sufficient region condition (half-plane separation), exact:
#     Re rho_p >= delta for all five => Re Z_hat >= delta (sum c = 1, all c > 0)
dlt = sp.symbols('delta', positive=True)
r0, r1_, r2_, r3_, r4_ = sp.symbols('r0 r1 r2 r3 r4', real=True)
rs = [r0, r1_, r2_, r3_, r4_]
ReZ = sum(ci*ri for ci, ri in zip(cs, rs))
ok("S4c Re Z_hat - delta = sum c_p (Re rho_p - delta) with sum c = 1 (exact linearity)",
   sp.expand(ReZ - dlt - sum(ci*(ri - dlt) for ci, ri in zip(cs, rs))) == 0
   and sum(cs) == 1)
# S4d and |Z_hat| >= Re Z_hat >= delta on the half-plane: |z|^2 - (Re z)^2 = (Im z)^2 >= 0
xr, yi = sp.symbols('xr yi', real=True)
ok("S4d |z|^2 - (Re z)^2 = (Im z)^2 (exact): half-plane separation lower-bounds |Z_hat|",
   sp.expand((xr**2 + yi**2) - xr**2 - yi**2) == 0)

print("S2-DISCHARGE-DONE")
```

Output, verbatim (22/22 PASS):

```text
S1a moments m_0 = m_1 = 0, m_2 = -1, m_4 = -2, m_6 = -4 (m_2j = -2^{j-1}): PASS
S1b unitarity |A|^2 + |B|^2 = 1 (slab closed form, re-run)               : PASS
S1c modulus identity Om^2|A|^2 = omega^2 + mu^2 cos^2(Om T) (re-run)     : PASS
S1d normalization: 5/4 census; N_surv(0) = 1/2; c = (1/2, 1/8 x 4) > 0;  sum c = 1 = kappa_bal: PASS
S1e all-pairs weight total = |m_0|^2 = 0 (the C4 kill; re-run)           : PASS
S2a r_L(t) = L r_1(t/L) branchwise: t<=L/2 gives t = L*(t/L); t>L/2 gives L-t = L*(1-t/L): PASS
S2b |C_L|_4 = pi L^4/24 EXACT ; (24 |C_L|_4/pi)^(1/4) = L                : PASS
S2c int_0^L v_L = tau_R EXACT (cycle scale-INVARIANT) ; sup v_L = 4 tau_R/L: PASS
S2d slab covariance: A_{mu/L, LT}(omega) = A_{mu, T}(L omega) EXACT       : PASS
S2e h_0 degree-1: |k/L| = |k|/L ; band projector degree-0: (k/L)_i/|k/L| = k_i/|k|: PASS
S2f exponent identity: (H/L + a J) L T - (H + L a J) T = 0 (matrix, exact): PASS
S2g dilation is unitary: int |f(x/L)|^2 dx/L = int |f|^2 dx (exact instance); kernel ledger (1/L^6)*L^3*L^3 = 1: PASS
S2h (L eps)^m = L * L^(m-1) eps^m (exact split) ; d/dm L^m = L^m log L    : PASS
S2i L M = (24/pi)^(1/4) |C_L|_4^(1/4) M (alpha' = 1/4 match) ; |C_L|_4^(-1/2) L M = (24/pi)^(1/2) M/L -> oo as L -> 0+ (alpha' = 1/2 fails): PASS
S2j per-pair x-majorant powers cancel iff alpha = beta = 1/2 (unique; re-run): PASS
S3a n!/(n-2)! = n(n-1) (exact) ; n(n-1) - 2 = (n-2)(n+1) (exact factor)  : PASS
S3b sum c (1+A)^{-1} A  !=  (1 + sum c A)^{-1} (sum c A): exact witness 3/4 != 2/3: PASS
S3c all c_p >= 1/8 > 0: sum c_p q_p - (1/8) q_j = (c_j - 1/8) q_j + sum_{i!=j} c_i q_i, every coefficient >= 0: PASS
S4a witness: 1 + sqrt2 e^{+3 i pi/4} = i ; 1 + sqrt2 e^{-3 i pi/4} = -i ; (1/2) rho1 + 4*(1/8) rho2 = 0 EXACTLY: PASS
S4b |1 + s sqrt2 e^{3 i pi/4}|^2 = 1 - 2s + 2s^2 = (1-s)^2 + s^2 (discriminant -4 < 0): PASS
S4c Re Z_hat - delta = sum c_p (Re rho_p - delta) with sum c = 1 (exact linearity): PASS
S4d |z|^2 - (Re z)^2 = (Im z)^2 (exact): half-plane separation lower-bounds |Z_hat|: PASS
S2-DISCHARGE-DONE
```

(One tooling note, on the record: an early draft of S4a used bare
`simplify` on `1 + sqrt2 e^{3 i pi/4} - i`, which sympy leaves in
`(-1)^{3/4}` form; replaced by the `expand_complex` route. No mathematical
claim changed.)

---

## 8. FLAG BLOCK

```text
TASKA_S2 = PARTIAL(
  DELIVERED: the n-uniform per-cell certification of the ALL-PAIRS
  instance Delta^{Sigma,all}_n = X_n (the check-verified identification)
  by the discharged trilemma bound sup_n ||X_n||_2 <= ||X||_2 <= M — the
  amendment's own certifying quantifier — at REM's carried grade
  (identity-grade core; e-i/e-ii estimate-grade with named overturn
  conditions). THE |C|_4 BOOKKEEPING, EXTRACTED EXACTLY: by the exact
  dilation-covariance chain (shape congruence r_L = L r_1(./L); tau_R
  invariance forcing v_L = v_1(./L)/L; massless degree-1/degree-0
  homogeneity; the source-contraction identity V^{(L)}(a) =
  U_L V^{(1)}(La) U_L^dag at all a-orders; unitary invariance of HS with
  the L^3/L^-3 volume factors cancelling EXACTLY; the polydisc
  contraction absorbing one full power, (L eps)^m <= L eps^m for m >= 1),
  sup_n ||Delta^{Sigma,(L)}_n(a)||_2 <= (24/pi)^{1/4} |C|_4^{1/4}
  M_dom(eps): THE SUMMED SCALING EXPONENT IS alpha' = 1/4 ON THE
  SIMILARITY CLASS — NOT THE FORCED 1/2. The forcing alpha = beta = 1/2
  (re-verified: the UNIQUE per-pair x-bookkeeping cancellation solution)
  does not transfer; the adopted clause's own caution ("alpha' may not be
  assumed" to equal alpha) is realized in the NEGATIVE. At alpha' = 1/2
  the clause is NOT delivered and is deliverable-with-content on the
  similarity class ONLY IF the assembled linear response is HS-null (an
  uncertified degeneracy REM's V_2 (x) V_2 display structurally
  contradicts). V-1 DOES NOT FIRE: alpha' is a SYMBOL of the adopted
  clause (NF-1), the estimate HOLDS at the amendment's own quantifier at
  the derived exponent; no other falsifier engages. G_hs^Sigma =
  (24/pi)^{1/4} M_dom(eps) — frozen-tuple, carrier-free, cellulation-free.
  RESIDUE: r-A1 non-congruent admitted cells (the R-L0 axis, unchanged);
  r-A2 the carrier n'-index bookkeeping under dilation (dischargeable by
  ||Pi X Pi||_2 <= ||X||_2 of record); r-A3 sharpness of 1/4 not claimed
  (upper-bound exponent; the 1/2-divergence conditional on HS-nonnull
  linear response); r-A4 REM's grade carried. INSTANCE: finiteness
  delivered at the all-pairs instance; the surviving-instance transfer is
  D-i (R-L4a/b), untouched. Witness SCAD_SUMMED_HS_UNIFORMITY_UNCERTIFIED
  STANDS pending check; retirement is the check/registrar's act.)

QUANTIFIER_DAYLIGHT = RESOLVED-BY-COVERING-BOTH(
  three sups separated at the bytes: (q-i) CARRIER axis (D3/Hermite-
  Galerkin n-family) = the discharged bound's quantifier, delivered by
  ||Pi X Pi||_2 <= ||X||_2, no cell content; (q-ii) CELL axis (D3 =
  FROZEN UNRESTRICTED common-refinement family, E1 :417-426, :856) = NOT
  touched by the discharged bound (ZF-CHK correction of record, carried
  at S2CAND §4.4 D-ii); (q-iii) COLOUR axis = replaced by the sum inside
  (adopted). THE DAYLIGHT IS REAL AND IN THE ADOPTED TEXT ITSELF: the
  certification's "uniformly over the D3 refinement quantifier" carries
  E1 :826-832's CELL-family discipline while S2'-O defers the cell sup to
  R-L0 and takes the carrier sup in trilemma form. Resolution here: the
  carrier axis is discharged of record; the cell axis is NEWLY covered on
  the similarity class at alpha' = 1/4 by the exact covariance and
  remains open EXACTLY on the non-congruent remainder — which is R-L0's
  landing, unchanged. Under NEITHER reading is alpha' = 1/2 delivered.)

TASKB_SEGMENT = FAILS-AT(
  the summed quantifier. Exact decomposition derived: Z_hat_n - 1 -
  tr[Delta^{Sigma,surv,c}_n] = sum_surv c_p(-int_0^1 tr[(1+A_{s,p})^{-1}
  A_{s,p} Delta_p]ds + (e^{Log rho_p} - 1 - Log rho_p)), tail
  coefficientwise |e^z-1-z| <= |z|^2 e^{|z|}/2. THREE WALLS, EXACT:
  W-1 the resolvent is PAIR-DIAGONAL — no linearity moves the colour sum
  inside the segment (exact scalar witness at the derived weights,
  3/4 != 2/3); R.3'(b)'s trace linearity is the ONE per-pair-free passage
  and the second-order content has none (the m_0-factorization break in
  second-order dress). W-2 every majorant route factors through per-pair
  HS norms; the c-weighted quadratic majorant obeys sum c_p ||Delta_{p,n}
  ||_2^2 >= (1/8) max_p — ALL FIVE c POSITIVE (the derived convexity
  footing turns AGAINST the estimate at the majorant level) — and each
  per-pair carrier sup is +infinity OF RECORD (52f2490b): the majorant
  route is dead n-uniformly; the covariance contraction does not rescue
  it (infinity times L = infinity). W-3 the segment consumes H-R
  (rho_res), which may NEVER default (F'-3). The per-pair route is BARRED
  by R.3'(c)'s own adopted text. At fixed n the bound exists but is
  n-DEPENDENT (ZF o-1) — what the clause forbids. NOT REFUTED: the
  majorants diverge, the object is undecided. EXACT RESIDUE: a new
  summed-level second-order identity/estimate with n-uniform constants
  not factoring through per-pair HS majorants — on no sealed artifact;
  plus H-R; plus D-i. Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED
  STANDS; R.3'(c) is now the one live analytic wall of the summed horn.)

TASKC_RL0B = FAILS-AT(
  the localization inputs: the linear Carleman estimate's segment
  consumes H-R (no-default, F'-3) and per-pair HS majorants whose carrier
  sups are +infinity of record (52f2490b); the trace term consumes S1's
  uncertified enclosure; no FIXED (n-uniform) region is derivable —
  fixed-n regions are n-dependent. DELIVERED MEANWHILE, EXACT — THE
  Q-1048 REGION CONDITION CORRECTED: star-shaped-about-1 + zero-free is
  INSUFFICIENT at the derived weights — exact witness: the segments
  [1, 1+sqrt2 e^{+-3 i pi/4}] are zero-free stars about 1 with endpoints
  +-i EXACTLY, and (1/2)(+i) + 4(1/8)(-i) = 0 EXACTLY. The sufficient and
  consumable condition is HALF-PLANE SEPARATION (0 outside the closed
  convex hull): Re(e^{i theta} rho_p) >= delta > 0 for all five surviving
  pairs forces Re(e^{i theta} Z_hat) >= delta (exact linearity, sum c =
  1, all c > 0), hence |Z_hat| >= delta, Log Z_hat defined — the
  exponentiation-free closure, exact. LICENSED CONSEQUENCE, conditional
  only: IF the corrected localization ever closes, THEN R-L0 is not
  needed for the n = 1 leg — NO STRONGER. It does not close here. R-L0b
  OPEN; witness SCAD_COLOR_SUM_REQUIRES_ABSOLUTE_X_BOUND stands.)

NET = MEMBERSHIP-UNCHANGED-INTERIOR-MOVES(
  the "C exists" gate list stays R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D
  (H-R/H-B0 inside). Interior, subject to check: the S2' HS-uniformity
  witness's content DELIVERED at alpha' = 1/4 on the similarity class
  (all-pairs instance; retirement not this artifact's); the segment-
  remainder witness STANDS — R.3'(c) is THE live analytic wall; R-L0
  unmoved as an obligation but its ground (i) is answered POSITIVELY on
  the summed axis and similarity class (the compensating weight exists:
  |C|_4^{1/4} DECAY via source contraction; the record-cycle hazard
  attaches to the dressing, cancelled at baseline by V(0) = I on the
  surviving sector), the non-congruent-cell axis wholly R-L0's; R-L0b
  open with its region condition corrected; R-L4a/b, C-L2, OBL-D, H-R,
  H-B0 untouched. Downstream: consumers of "alpha' = 1/2 at the summed
  level" have nothing to consume — the forced 1/2 governs the PER-PAIR
  bookkeeping only. No flag flips (GOV-F); per-pair record untouched;
  B-L2* unaltered; no FIXED/RUNNING verdict moved.)

SEALS_VERIFIED = 14/14(
  ce59b480 S2CAND; c2346000 S2CAND-CHK; cbfbb74c ENTRY; a903716c RL4RL0;
  685afac8 RL4RL0-CHK; 2e4e5163 REM; 89641f76 REM-CHK; 26f9314b ZF;
  aed551e3 ZF-CHK; a7969f0c R2; a4f2e1b7 R2-CHK; 46846730 E1; 789338ad
  PA; 52f2490b RL2B-REFUTED — all recomputed from bytes at path this
  session by full digest; all match sidecars and tasked prefixes; none
  unverifiable.)

FORBIDDEN_IMPORTS = none(
  no scale authority (the dilation parameter L is a FORMAL symbol of the
  D3 quantifier's own cell family, never instantiated, no physical scale
  fixed); no imported GR; no faithfulness authority; no physical quantity
  computed, bounded numerically, or evaluated — every constant symbolic
  or exact rational/surd; the only orderings used are exact ((n-2)(n+1)
  >= 0 for n >= 2; L^{m-1} <= 1 on 0 < L <= 1 via log L <= 0;
  discriminant -4 < 0); M(t) sharp (dilation acts branchwise on the sharp
  indicator; nothing mollified); equal-time FALSE by C6 (all HS objects
  two-time); no register/tracker/plan/road/ledger/lens file read; no
  number frozen — alpha' = 1/4 is a DERIVED EXPONENT of the bookkeeping
  variable, not a physical value, and is CLAIMED pending check.)

MACHINERY_INVOKED = yes(CAS) — sympy 1.14.0, fresh venv s2venv, 22 exact
  identity/enumeration groups S1-S4 over symbols, script and output
  reproduced verbatim in §7 (22/22 PASS); shasum/file reads otherwise;
  NOTHING numeric evaluated.

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until checked.
S2_DISCHARGE_RESULT = SEALED.
```
