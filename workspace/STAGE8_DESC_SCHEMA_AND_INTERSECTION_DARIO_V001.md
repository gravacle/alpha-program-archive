CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits (empty byte range -- no possible hit)
VERDICT_BEARING_SET = exactly the 23 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = DARIO   ROLE_THIS_RELAY = FREEZE THE SCHEMA IF ENTAILED, THEN INTERSECT THE FULL SURFACE.
CITATION_RULE = CLOSURE_MEMBER_CITATION_RULE_V001.  Every published span carries CANONICAL PATH +
  FULL SOURCE SHA-256 + HALF-OPEN [a,b) + FULL SPAN SHA-256.
DIGEST_RULE = every digest and offset COMPUTED by member 23 and quoted from its output.
SPAN_CONVENTION = DECLARED.  Raw bytes, half-open [a,b), no decoding, no newline normalisation,
  no trimming.  ANCHOR = [start-anchor offset, end-anchor offset).  FIXED = the interval its
  upstream published, re-verified against the published pin at its published width.  10/10 FIXED
  re-verified -- and five of those ten (T01-T05) are the OPPOSITE LANE'S OWN pins from member 04,
  reproduced here independently rather than copied.
LIVE_MEMBER_CLASS = DECLARED, AND IT IS A RESPONSE TO A CHECK FINDING.  Member 09 (the register)
  appends lawfully between seals; member 04 found my 1045 artifact current-reproducible at 56/57
  rather than its seal-time 57/57 for exactly that reason.  Member 09 is therefore marked LIVE:
  its WHOLE-FILE digest is a run-time snapshot with NO VERDICT WEIGHT, and the verdict-bearing
  authority for that member is its SPAN digests, which are stable under append.  This artifact
  quotes no LIVE whole-file digest, and member 23's audit runs in strict and stable modes so that
  the identity of the two results is checkable rather than asserted.
GENERATOR_REFUSAL = member 23 emits NOTHING on R1 unreadable member, R2 FIXED-pin disagreement,
  R3 absent/AMBIGUOUS start anchor, R4 absent/AMBIGUOUS/pre-start end anchor, R5 a span row naming
  a barred member.  NONE FIRED ON THE FINAL RUN; R3/R4 fired four times in construction -- §11.
SELF_CITATION_BAR = NO span is drawn from ANY artifact of this lane.  Members 13, 21 and 22 are
  closed FOR CARRIAGE ONLY and R5 refuses any span row naming them; 0 rows do.
SUBJECT_READ_RULE = member 12 read ONLY WITH member 13 (overlay V002) and member 11 (DoR-013).
CARRIED_NOT_REARGUED = member 04's hinge ruling GOVERNS and is EXECUTED here: the seven co-frozen
  port-contract schemas are CLASS A; actual candidate outputs stay barred.  It corrected me, the
  correction was the residual I myself flagged, and this relay runs on the surface it defines.
  The baseline tally is member 04's 0/9/0, not my superseded 0/3/6.
ALL_RESULTS = CLAIMED until the Codex 2 check.
CLOSURE_END_BYTE = 00007673
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1047_SCHEMA_FREEZE_AND_INTERSECTION_DARIO_V001.md` | `83687256ad8cc3fa5816f729cb80f18405f67dfb9b6507cf87306032124e7a58` | the assignment |
| 02 | `relay_inbox/RELAY_PASTE_1044_FRESH_SESSION_BOOTSTRAP_DARIO_V006.md` | `bfec6e1f9e6d6e15b3d0a37552fab8222f9c861a30aa0752a6a315e5c84fee98` | the bootstrap; the SYMBOLIC LINE |
| 03 | `workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md` | `f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0` | **THE FULL PHASE-A SURFACE: freeze tuple, nine fields, seven port contracts, §10, SIG-T1/T2/T11** |
| 04 | `workspace/STAGE8_DESC_SELECTION_CHECK_CODEX2_V001.md` | `00ac4928a247acfe088b5374cc972e3d5528e8a2fdc7cd21d9d043cb52abeede` | **THE CHECK OF RECORD — the admissibility hinge** |
| 05 | `workspace/STAGE8_RANK1_CTP_PRODUCER_ALGEBRA_Q52_SPEC_V001.md` | `e191d37917c933a537c20152ac5899ab3d0d027d1f865709f2e816cad40ba706` | THE LAW SIDE: class definition, P0–P7 |
| 06 | `workspace/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md` | `1b8c9fd63b140bfee10e68c952beec24885082184762883f3ea505810b5c58db` | Q-95/Q-94; the noncircularity sentence |
| 07 | `workspace/STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md` | `476baa18b1e2e97f8b8c8a349bf803627f53c47224b030f538527848f3fb8deb` | Q-97: the inventory is too thin |
| 08 | `workspace/STAGE8_ACCESSOR_THEOREM_SUBSTITUTION_ADJUDICATION_EINSTEIN_V001.md` | `dc47fbe0d4202dacadcda0775aaf4879e5e639d33ddd98ce08cef7361fc0638f` | THIRD LANE: the uniqueness sense |
| 09 | `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | *(LIVE — run-time snapshot, no verdict weight; spans are the authority)* | Q-955/956/**Q-957: the hinge and the hook** |
| 10 | `supervision/DECISION_OF_RECORD_008_FIELD_CTP_ADOPTION_RATIFIED_WITH_FALSIFIER_2026-08-01_V001.md` | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | DoR-008: the two undischarged items |
| 11 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | DoR-013: family level; no member, ever |
| 12 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | READ ONLY, with 13 and 11 |
| 13 | `workspace/MEMBER12_HEADER_OVERLAY_RECORD_V002.md` | `e85f444cbbd32db7cd8a3f794faee38d189f4bd12bd8e8bbfb43d8463c23cd75` | the read rule — **CARRIAGE ONLY; NO SPAN** |
| 14 | `workspace/STAGE8_FIELD_CTP_CARRIER_AND_C0_JOIN_INSTANTIATION_ATTEMPT_V001.md` | `e916f15742805a9f79f9386133c3a9662201e6363f739bddc682fbebb402ba37` | the open NO_VERDICT slot |
| 15 | `workspace/STAGE8_FIELD_CTP_ALGEBRAIC_PRESENTATION_ADOPTION_PROPOSAL_V002.md` | `76916244bdbcac7c2a6d4afae40f35127540d1d9e4cfc86fb72318506671161f` | the provenance ceiling |
| 16 | `workspace/STAGE8_TASK2D_SCALARIZATION_FUNCTIONAL_FORCING_PROTOCOL_RESULT_V001.md` | `d13920e2a7687ac53a896e70cd0d12168f74fe0f368425179a455a8ae249ae98` | TASK2D: the scalarization continuum |
| 17 | `workspace/DEMAND_READING_DECISION_OF_RECORD_V001.md` | `c9043abfeb4c9429eda092686dcb191d364e8a7ca581c9708463ab2341f81173` | the governing receiver reading |
| 18 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 19 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 20 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 21 | `workspace/STAGE8_DESC_PHYSICALSIG_SELECTION_DARIO_V001.md` | `a844c9eac0c4502cf4ca9b3073171ce058e38bc23b64d34d702ffea0831dc004` | 1045 — **CARRIAGE ONLY; NO SPAN** |
| 22 | `workspace/build_physicalsig_selection_pins_v001.py` | `a413d52da7d3055e56db61e6afffeaa6747891847df716a9a6fa5e66d3059f92` | the 1045 generator — **CARRIAGE ONLY; NO SPAN** |
| 23 | `workspace/build_schema_intersection_pins_v001.py` | *(computed at run time; reported in the outbox)* | the pin generator; declared inputs are exactly members 01–23 |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF ANY PHYSICAL QUANTITY.  NO COMPARISON
TO A MEASURED CONSTANT.  NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.  NO FIELD
SELECTED.  NO SCHEMA FROZEN -- and Step 1 is the one place this relay was authorised to freeze, so
that line is the load-bearing one.  omega_phys UNTOUCHED.  NOTHING AUTHORED, ADOPTED, IDENTIFIED,
ENTERED, OR PROMOTED.  NO SUBAGENT DELEGATION.  PE LEDGER NOT OPENED.  CLEANROOM NOT ENTERED.
NO GIT ACTION OF ANY KIND, READ-ONLY INCLUDED -- member 01 sets that letter from my own 1045
disclosure, and it binds.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT — THE SCHEMA FREEZE AND THE INTERSECTION RE-ATTEMPT — DARIO LANE — V001
## RELAY 1047 — `[PLAN:DESC-31]` — FREEZE IF ENTAILED, THEN INTERSECT THE FULL SURFACE

Date: 2026-08-11
Status: **BOTH STEPS RAN AND BOTH STOP, FOR THE SAME REASON — AND THAT REASON IS THIS RELAY'S
RESULT. STEP 1: the schema is NOT entailed. `SHAPE-SPACE-DISPLAYED`. Its FLOOR is exhibited at
bytes — per entry an authority path, a content hash, an exact type, a domain and a multiplicity;
a coverage relation; a closure requirement; a freeze-ordering — but there is NO CEILING, because
every sealed constraint bearing on the schema is a REQUIREMENT and none is a prohibition on its
field structure. A set defined by lower bounds alone is closed upward and has no unique least
element without a minimality principle, and the record declares none. Three further axes are open
on top of that: indexing (the genericity question in its exact form), witness location (the record
runs two conventions and assigns neither), and where multiplicity lives. STEP 2: the same shape
defeats the intersection. `0 SELECTED / 9 CONSTRAINED / 0 UNCONSTRAINED` — `TALLY_DELTA = NONE`.
THE INTERSECTION CANNOT PIN, AND NOT FOR WANT OF CONSTRAINTS: the Class-A surface the check
restored is an intersection of LOWER BOUNDS, and lower bounds never isolate a point. I looked for
the one exception that could — a universal-property-shaped requirement — and the record contains
exactly one, P1's "completed" carrier, whose own input is unfixed, so it does not fire. THE
CONCRETE DISPLAY: what survives is a nonempty PRODUCT of declared-open branches — at least four
category candidates × two `DomainKind_1` branches × two `FamilyKind_5` branches × an
`EvolutionEquivalence_6` the record itself writes as `EXACT RELATION TO BE DERIVED AND FROZEN` —
and for BOTH equivalence relations the record has already searched and recorded
`qualifying_definition_file_list: EMPTY`. AND THE CENSUS THAT REFRAMES THE ROAD: of the TEN members
of `SignatureFreezeInputs_0`, THREE have no typed shape, TWO are searched-and-empty relations, ONE
is completeness-`TYPE-U`, THREE have content, and ONE is the thing being selected. The gate is not
one missing object. NOTHING WAS FROZEN AND NOTHING CHANGED HANDS.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 0. The ruling that governs, and the correction I carry

Member 04 corrected me at the exact point I had named as the relay's residual. The ruling:

```text
CITATION
path   workspace/STAGE8_DESC_SELECTION_CHECK_CODEX2_V001.md
source 00ac4928a247acfe088b5374cc972e3d5528e8a2fdc7cd21d9d043cb52abeede
span   [9183,9408)  ANCHOR
digest e082fcac6d34bcbc1369cdb91d68d569f56d3ce4b092bfabf461c5d14edf1dac
```

> `Therefore the port schemas are lawful **Class A constraints**. "Co-frozen" means jointly fixed`
> `before candidate/target output; it does not mean descendant. The subject's precaution was`
> `directionally sound but overbroad.`

and the tally it leaves, which is this relay's baseline:

```text
CITATION
path   workspace/STAGE8_DESC_SELECTION_CHECK_CODEX2_V001.md
source 00ac4928a247acfe088b5374cc972e3d5528e8a2fdc7cd21d9d043cb52abeede
span   [12277,12410)  ANCHOR
digest 7b2c3256dad7b59d6d5e938d945248adb302dff119872abfbef2eeb0c4eea55a
```

> `FIELDS = 0 SELECTED / 9 CONSTRAINED-NOT-SELECTED / 0 UNCONSTRAINED`
> `MOVED = fields 3,4,5,6,7,8 from UNCONSTRAINED to CONSTRAINED`

ratified at the register:

```text
CITATION
path   supervision/QUESTIONS_SETTLED_REGISTER_V001.md   (LIVE member -- spans are the authority)
span   [1920261,1920624)  ANCHOR
digest d61aab07dfe5a7a58def65abedbe7b8fd2e00fbd2baa7059ea6e6ad0461d4bbf
```

> `THE HINGE RULING: the seven co-frozen port-contract schemas are PHASE-A CONSTRAINTS, not`
> `descendant outputs … Actual candidate OUTPUTS stay inadmissible`

**I do not re-litigate it, and I will not perform the tempting half-defence either.** The line I
drew was right in kind and too tight in extent; the distinction that survives is the check's — a
port *contract* is a co-frozen schema and constrains, a port *output* is a candidate and does not.
Everything below uses constraints and never candidates. What I keep is the standard, not the
boundary: **a wider surface makes selection possible where it was blocked; it does not lower the
bar.**

The instruction this relay executes, and against which its outcome is scored:

```text
CITATION
path   supervision/QUESTIONS_SETTLED_REGISTER_V001.md   (LIVE member -- spans are the authority)
span   [1921659,1922028)  ANCHOR
digest 0d7591567d63295b7689bfd0991b73ec9f3b64485cab7461b04669e4b02c134a
```

> `The remaining lawful depth is genuinely live: freeze the schema and re-attempt the selection`
> `through the INTERSECTION of the full Class-A constraint surface — nine constrained sets may pin`
> `what one pass could not.`

**Nine constrained sets did not pin what one pass could not, and §7 says exactly why in a form that
can be checked rather than believed.**

---

# STEP 1 — THE SCHEMA

## 1. The freeze tuple, and a census of its ten members [PROVABLE]

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [27434,27819)  ANCHOR
digest 720243e85bd435061df28f98cfc348ed4a5a32345a9a751c09032e1bfc599314
```

> `SignatureFreezeInputs_0 :=` `( PhysicalSig_0, FrozenPrimitiveBindingSchema_0,`
> `FrozenDerivedSectorReferenceTypeSchema_0, FrozenPortContractSchemas_1_to_7,`
> `FrozenCrossRowCoherenceSchema_0, FrozenPortDependencyGraphSchema_0,`
> `FrozenOriginPredicateAndTraceSchema_0, FrozenCertificateSchema_0,`
> `DomainEquivalence_1_if_used, EvolutionEquivalence_6 )`

**Nobody has censused this tuple, and doing so changes what "the schema gate" means.**

```text
PHASE-A READINESS CENSUS -- ten members, each status at bytes

 1  PhysicalSig_0                             UNDER SELECTION.  0/9 (member 04 [12277,12410)).
 2  FrozenPrimitiveBindingSchema_0            NO TYPED SHAPE.  §2 below.
 3  FrozenDerivedSectorReferenceTypeSchema_0  NO TYPED SHAPE -- THE TWIN.  §3 below.
 4  FrozenPortContractSchemas_1_to_7          TYPED CONTENT: 13,273 bytes at member 03
                                              [34855,48128).  Carries THREE open branch parameters.
 5  FrozenCrossRowCoherenceSchema_0           CONTENT: eight named conjuncts, member 03
                                              [46217,46696).
 6  FrozenPortDependencyGraphSchema_0         EDGE KINDS listed; completeness TYPE-U, member 03
                                              [47571,48128).
 7  FrozenOriginPredicateAndTraceSchema_0     APPLIED, NEVER DEFINED.  Fifteen applications of
                                              OriginTrace_ in member 03 by scan; the argument list
                                              is pinned at eight; the type body is nowhere.
 8  FrozenCertificateSchema_0                 PARTIAL: the assembler is typed and the aggregate
                                              shape is quoted from the flat presentation.
 9  DomainEquivalence_1_if_used               SEARCHED; qualifying_definition_file_list: EMPTY.
10  EvolutionEquivalence_6                    "EXACT RELATION TO BE DERIVED AND FROZEN";
                                              SEARCHED; qualifying_definition_file_list: EMPTY.

  3 WITH NO TYPED SHAPE (2,3,7).  2 SEARCHED-AND-EMPTY RELATIONS (9,10).  1 TYPE-U FOR
  COMPLETENESS (6).  3 WITH CONTENT (4,5,8).  1 UNDER SELECTION (1).

THE SCHEMA GATE IS NOT ONE MISSING OBJECT.  At 1045 I reported it as one, and that was true of the
object I was looking at and false of the gate.  Five of the ten Phase-A inputs are not objects.
```

## 2. `FrozenPrimitiveBindingSchema_0` — is its typed shape ENTAILED? [DERIVED HERE]

### 2.1 A method correction against myself, before the answer

At 1045 I made the case with an occurrence count — *ten in the archive, one law-side*. Member 04
noted the raw count has since inflated. Member 23's scan now returns **35**, of which **13 are my own
1045 artifact**. The count moved by a factor of three and the finding did not move at all.

```text
THE COUNT WAS THE WRONG INSTRUMENT AND I SHOULD SAY SO PLAINLY.  FrozenPortContractSchemas_1_to_7
occurs ONCE law-side too -- exactly as often as the primitive-binding schema -- and it has 13,273
bytes of typed content behind that one occurrence.  A NAME COUNT CANNOT DISTINGUISH THEM.
CONTENT AT BYTES CAN, AND IT IS WHAT §1'S CENSUS USES.  The 1045 conclusion survives on the better
instrument; the argument that reached it was weaker than the conclusion deserved.
```

### 2.2 The floor, exhibited

Everything the record requires of the schema, at bytes. The twelve conjuncts:

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [49744,50735)  ANCHOR
digest e8172eec2598715dde1b19313b81f1e911f9d88435f36bf4c50eb74d8cd84261
```

> `PRIMITIVE_INTERFACE_REQUIREMENT(I_prim,PhysicalSig_0) :=`
> `  frozen_before_any_target_or_candidate_output(I_prim)`
> `  AND authority_path_and_content_hash_recorded_for_every_entry(I_prim)`
> `  AND exact_cover_of_every_free_primitive_ground_sort_operation_parameter_domain(`
> `        I_prim, PhysicalSig_0, AttemptPort_1_to_7)`
> `  AND unique_unambiguous_type_and_domain_assignment(I_prim)` …

the tightest sealed statement of an entry:

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [55060,55466)  ANCHOR
digest 22534b40c80cb0dd9caaf61fb3ac9a2487d2f6171b419a600079237ae2a7eef4
```

> `A free primitive ground item lacks an explicit frozen authoritative binding with exact`
> `type/domain/multiplicity`

and the coverage sentence that is the schema's only description:

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [67332,67543)  ANCHOR
digest c470323d07b4b01103e03e2cd887038d4bb611591ed0ab60f7b2f83639b4bba0
```

> `2. exact frozen primitive-binding and derived-sector-reference **type** schemas`
> `   for every free ground type/domain used by that signature, without`
> `   enumerating the separately owned primitive inventory here;`

```text
THE FLOOR -- everything any admissible shape must carry, each clause at a cited span:

  PER ENTRY   authority path; content hash          [P02 conjunct 2]
              exact type; domain                    [P02 conjunct 4; P06]
              multiplicity                          [P06]
  INDEX       exact cover of every free primitive ground sort / operation / parameter / domain
              of the signature AND the seven port schemas                    [P02 conjunct 3; P05]
  CLOSURE     under every declared primitive constructor composition         [P02 conjunct 5]
  ORDERING    frozen before any target or candidate output                   [P02 conjunct 1]
  ENTRY BARS  no reclassified derived-sector reference; no P1-P7 output or discharge flag; no
              downstream target or validation fixture; no unproved B0/U_BR/D_BR/BID identity;
              no catch-all or post-output supplement                         [P02 conjuncts 8-12]

EXHIBITING THE FLOOR IS NOT FREEZING THE SCHEMA, and the difference is the whole of Step 1.
A lower bound is a constraint satisfied by many objects.  Promoting it into "the" schema would be
a lane authoring an object the record has not entailed -- the exact act member 01 permits only in
the exactly-one case.
```

### 2.3 Why the floor has no ceiling — the monotonicity result [DERIVED HERE]

```text
CLAIM.  The set of typed shapes admitted by the sealed constraints is CLOSED UPWARD under adding
fields.  Hence it is not a singleton, and exactly-one fails by the KIND of constraint the record
contains rather than by any shortage of constraints.

PROOF, on the cited spans and nothing else.
  (i)   Every sealed clause bearing on the schema is a REQUIREMENT: "records X for every entry",
        "assigns a unique unambiguous type and domain", "covers exactly the free ground items",
        "is closed under declared compositions", "is frozen before output", "carries exact
        type/domain/multiplicity."  Each is satisfied by a shape that provides the named data.
  (ii)  The prohibitions in the same block -- conjuncts 8 through 12 -- bar CONTENT FROM ENTRIES
        (a descendant, a target, a fixture, a catch-all, a relabelled sector reference, an
        unproved identity).  NONE of them bars a FIELD from the binding record's type.
  (iii) Therefore if a shape S satisfies every clause, so does S extended by any further field
        whose content violates no entry bar: the requirements are still met (they are lower
        bounds) and no prohibition is triggered (they are about entry content, not field count).
  (iv)  A nonempty upward-closed family has a unique member only if the ambient admits one point.
        It does not.
  (v)   Isolation would need either an UPPER BOUND on the schema's fields or a MINIMALITY /
        CANONICITY principle for Phase-A type schemas.  I searched for both.  THE RECORD DECLARES
        NEITHER, for this object or for any Phase-A type schema.
  QED, and the qualification matters: this is a statement about the CURRENT sealed constraint set,
  not an impossibility.  A later sealed minimality principle would collapse it at once.
```

### 2.4 Three further axes, each open, each at bytes

Even setting monotonicity aside, three genuinely inequivalent choices remain.

```text
AXIS 1 -- INDEXING.  THE GENERICITY QUESTION, IN ITS EXACT FORM.
  Member 03 [67332,67543) scopes the schema to "every free ground type/domain used by that
  signature."  The build order of record puts the schema BEFORE the signature.  So a schema frozen
  first must be generic, and there are at least three inequivalent ways to be generic:
    (a) SIGNATURE-PARAMETRIC   a dependent family indexed by the signature's free-symbol set --
                               which makes the "frozen" object a schema scheme, not a schema;
    (b) SORT-UNIVERSE          a fixed universe of admissible ground sorts, with the signature
                               required to embed -- signature-independent, but committing to which
                               ground sorts exist is a physics commitment sealed nowhere;
    (c) OPEN-RECORD            an extensible record with a mandatory field set -- under which the
                               exact-cover clause is not checkable at freeze time at all.
  These differ in what they PERMIT, not merely in notation.  Nothing in the record chooses.

AXIS 2 -- WHERE A WITNESS LIVES.  THE RECORD RUNS TWO CONVENTIONS AND ASSIGNS NEITHER.
  The port convention puts witnesses INSIDE the package as fields -- member 03 [36239,36694) and
  [46217,46696) carry domain_kind, equivalence witnesses and coherence witnesses as record fields.
  The §10 convention puts them OUTSIDE, as conjuncts of a predicate on the context --
  [49744,50735) and [50735,51494).  The primitive-binding schema sits exactly between the two and
  no sealed sentence says which governs it.

AXIS 3 -- WHERE MULTIPLICITY LIVES.
  SIG-T2 [55060,55466) makes multiplicity BINDING DATA: "exact type/domain/multiplicity."
  The §10 prose [51494,52017) makes it SIGNATURE-SUPPLIED: "Multiplicity or alternatives may occur
  only when the selected exact signature fixes their type and admissible multiplicity."
  THESE ARE RECONCILABLE IN CONTENT -- the binding may record what the signature fixes -- and I do
  not allege a contradiction.  What is unsettled is the SHAPE: whether multiplicity is a field of
  the binding record or a projection of the signature.  A type schema must answer that; the record
  does not.
```

### 2.5 STEP 1 RULING

```text
SCHEMA = SHAPE-SPACE-DISPLAYED.  THE GENERICITY QUESTION IS REACHED.  THE STEP STOPS.

  CARDINALITY / STRUCTURE:  at least 3 (indexing) x 2 (witness location) x 2 (multiplicity
    location) inequivalent shapes before any field is added, and UNBOUNDED ABOVE by §2.3.
    Not one; not finite.
  WHAT PINS IT PARTIALLY:  the floor of §2.2, in full, at cited spans.  It is a real and usable
    result -- whoever builds the schema does not start from nothing -- and it is a lower bound.
  WHAT WOULD FINISH IT (named, not invented):  a sealed minimality or canonicity principle for
    Phase-A type schemas; a sealed ruling assigning the witness convention on the primitive side;
    and a sealed resolution of the indexing axis, which IS the genericity question.
  A LANE DOES NOT CHOOSE A SCHEMA ANY MORE THAN A VALUE.  Member 01 authorised a freeze in the
  entailment case only.  The case did not arise.  NO SCHEMA IS FROZEN HERE.
```

## 3. The twin — the same finding is not about one object [DERIVED HERE]

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [50735,51494)  ANCHOR
digest 772d10be2b2efbf4e42903ded785e8e3fadd73fc2c219f423f99316080e8418d
```

> `DERIVED_SECTOR_REFERENCE_REQUIREMENT(S_sector,ExactSig_0) :=`
> `  frozen_before_port_execution_and_candidate_output(S_sector)`
> `  AND authority_path_content_hash_and_derivation_status_recorded_for_every_entry(S_sector)` …

```text
FrozenDerivedSectorReferenceTypeSchema_0 IS IN THE IDENTICAL CONDITION: a name in the freeze tuple,
a nine-conjunct predicate on its CONTEXT rather than on its own type, and no typed shape anywhere.
Its twelve-versus-nine conjunct count differs; its structural situation does not.

THEREFORE THE GENERICITY QUESTION IS NOT A PROPERTY OF ONE OBJECT.  It is a property of the way the
record specifies Phase-A TYPE SCHEMAS: by constraining the CONTEXT that will instantiate them.  Two
of the ten freeze inputs are specified that way and both are shapeless; the ones with content are
specified directly, as types.  THAT IS THE PATTERN, AND IT IS THE ACTIONABLE PART OF STEP 1 --
a minimality principle would have to cover the class, not the instance.
```

---

# STEP 2 — THE INTERSECTION

## 4. The surface, restored [PROVABLE]

Run **against the full Class-A surface but WITHOUT the schema**, since Step 1 stopped — declared
here as member 01 requires. The restored constraints are the seven port contracts
(member 03 `[34855,48128)`, 13,273 bytes), the receiver-side map, and Q-94:

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [71912,72797)  FIXED   digest 94afb97be61f3471bcba203ecaccc6f7ea09bd68c1c78fbe3a4bdf9b98215365
span   [20815,21632)  FIXED   digest 7624391c8ee61446cc7a45dd52e2f85fcf45155a419853fb92a2142cc27ed678
```

> `It must supply actual/canonical constructors or accessors for: P1 completed carrier/domain;`
> `P2 source embedding and restriction coherence; P3 record embedding and Q-42/Q-43 coherence;`
> `P4 exact physical CTP tuple; P5 state, effects, domains, property contract, and origin trace;`
> `P6 an exact dynamics form, common-domain action, normalization interface, and any required`
> `proven equivalence; and P7 contacts, source rules, raw correlator evaluator, and metadata.`

> `STRUCTURE. ALL SEVEN. ZERO PURE-EXISTENCE ROWS.`

**And the boundary that keeps the restoration honest**, which I cite because it is the sentence
that would convict me if I crossed it:

```text
CITATION
path   workspace/STAGE8_JOINT_P0_BUILDABILITY_IN_PRINCIPLE_RESULT_V001.md
source 1b8c9fd63b140bfee10e68c952beec24885082184762883f3ea505810b5c58db
span   [37480,37635)  ANCHOR
digest 0d5ede923490a1183f709a2ca8c56d1bd56acff0eac1f21e89c4f975166c3dae
```

> `Treating any row in this table as a preconstruction input would make the graph`
> `circular.  They are tested only after ConstructP0 produces its codomain.`

```text
THE ONE DISTINCTION THE WHOLE OF STEP 2 RIDES ON, STATED BEFORE IT IS USED:
  AttemptPort_i are operations of the INTERFACE INSTANCE.  PhysicalArityAndOperations_0 is the
  INTERNAL operation signature of the physical object.  The port contracts constrain the internal
  signature by RECOVERABILITY -- the internal operations must suffice for the canonical accessors
  to yield each package -- and they do NOT identify interface operations with internal ones.
  Reading the seven ports off as Obj_0's nine internal operations would be the interface-for-object
  flattening, and it would manufacture a selection at field 3.  It is not committed below.
```

## 5. The three open branch parameters [PROVABLE]

These are the concrete reason the intersection stays wide, and two of them sit **inside the freeze
tuple itself**.

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [36239,36694)  ANCHOR   digest 5c0ee839a26036af878a4fa315638937af9679d44a2f597941d0e3a0cee099b4
span   [35730,36239)  ANCHOR   digest 7bda2c8944690bb2e14f58f6ecc74e45e3a33f84dbe591508fc45e4597125d7c
```

> `DomainKind_1 := COMMON_DENSE | PROVED_EQUIVALENT`

> `qualifying_definition_file_list: EMPTY`

```text
CITATION
path   workspace/STAGE8_OBJ0_EXACT_SIGNATURE_DOMAIN_CODOMAIN_SPEC_AND_BUILD_STOP_V001.md
source f7fa3c0ff4f7a13bcb953fced2b3e073a472410a6bc115b66157a4dae715c3a0
span   [41270,41892)  ANCHOR   digest 7608f542f553e2bd62ecd96c2741373f01d78647492e90116767261d1528ee20
span   [44304,44599)  ANCHOR   digest 2d123c420af5155345e7c6207a171e2e81287c5067da5cd343d1406871d0c0be
span   [43744,44304)  ANCHOR   digest 6dd989e740103fd1f8ab160e4f17d1a1ec5f39b45ae9807ad259800e3430ff95
```

> `FamilyKind_5 := EXHAUSTIVE | NONEXHAUSTIVE`

> `EvolutionEquivalence_6 := EXACT RELATION TO BE DERIVED AND FROZEN`

```text
BRANCH CENSUS
  B1  DomainKind_1        2 branches.  The PROVED_EQUIVALENT branch needs an equivalence predicate
                          the record SEARCHED for and did not find: qualifying_definition_file_list
                          EMPTY.  Bears on fields 4 and 7.
  B2  FamilyKind_5        2 branches, frozen before execution.  The NONEXHAUSTIVE branch needs an
                          exact target-independent criterion that does not exist.  Bears on 5 and 6.
  B3  EvolutionEquivalence_6   Not a fork between two named options but a NAME WHOSE DEFINITION LINE
                          IS THE WORDS "TO BE DERIVED AND FROZEN."  Searched; EMPTY.  Bears on 3, 5.

  TWO OF THE TEN FREEZE INPUTS ARE THESE UNRESOLVED RELATIONS.  Member 23's scan finds
  `qualifying_definition_file_list: EMPTY` exactly twice in member 03 -- once for each.  The record
  did the hunt and recorded the emptiness; I am reading its own result, not repeating its search.
```

## 6. The nine fields, intersected, in head order [PROVABLE / DERIVED]

Head order respected: fields 1–3 first, then those that depend on them.

```text
F1  PhysicalCategory_0                          CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  microscopic / one / three-sector (05 [1163,1581), 05 [8085,8160));
      not a temporal creator of the antecedent carrier (03 [18324,18479));
      no B0 type transferred; a linear-operator choice would be an extra premise;
      RESTORED: the category must admit an object from which all seven packages are recoverable
      through the exact contracts (03 [34855,48128); 03 [71912,72797); Q-94 03 [20815,21632)).
    SURVIVES:  at least four pairwise non-isomorphic candidates across two lists that disagree
      (03 [20247,20734)), with no exhaustiveness claim anywhere.  The recoverability requirement
      excludes NONE of them: the packages are produced by the ports FROM o together with I_prim,
      S_i and prior c_j -- never by o alone -- so no candidate category is ruled out by having to
      supply them.
    EXACTLY-ONE FAILS NEGATIVELY under both uniqueness senses.

F2  PhysicalObjectType_0                        CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  the semantic ROLE is fixed to exactly one value (03 [18324,18479));
      the type must inhabit the seven-port interface; the record lists the internal object type
      among what sealed text does not fix (03 [71143,71361)).
    SURVIVES:  indexed by F1 -- the types in each surviving category that bear the fixed role.
      A ROLE IS NOT A TYPE, and the restored surface does not change that.

F3  PhysicalArityAndOperations_0                CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  the internal operations must suffice, via canonical accessors, to yield
      the seven packages, the cross-row coherence constructor and the certificate assembler
      (03 [34855,48128); 03 [46217,46696)); B3 bears -- whether the dynamics form is S_CTP or a
      proved-equivalent alternative changes what internal operation must exist (03 [44304,44599)).
    SURVIVES:  every internal signature above that floor.  RECOVERABILITY IS A LOWER BOUND; it
      names no arity and forbids no additional operation.  Upward-closed, exactly as in §2.3.

F4  PhysicalOperationDomains_0                  CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  typed inputs per operation, the common-domain branch, frozen
      primitive/reference subcontexts, explicit prior-result dependencies (03 [34855,48128));
      B1 bears directly.
    SURVIVES:  2 (DomainKind_1) x the F1 index, unbounded above.
    NOT IMPORTED:  P1's common dense domain is the CARRIER's domain, not Obj_0's internal
      operation domain.  Refused at 1045, still refused, and the restoration of the port schemas
      as CONSTRAINTS does not convert a port's codomain content into a source field's VALUE.

F5  PhysicalOperationCodomains_0                CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  each port returns a failure-capable dependent Result with a named
      package AND an origin trace; bare existence excluded (Q-94, 03 [20815,21632)).
    SURVIVES:  unbounded -- AND WITH A HOLE THE CENSUS EXPOSES: the trace component of every such
      codomain is FrozenOriginPredicateAndTraceSchema_0, which is applied fifteen times and defined
      never (03 [30498,30664)).  The constraint on this field references an undefined type.

F6  PhysicalParameterRecord_0                   CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  frozen domain/evolution equivalence parameters, per-port contexts and
      witnesses, the pre-execution family_kind_5 and, on one branch, its exact criterion
      (03 [41270,41892)).
    SURVIVES:  2 (B2) x 2 (B1) x the unresolved B3 -- and the parameters the intersection names are
      exactly the ones the record has searched for and not found.  THE CONSTRAINT NAMES ITS OWN
      MISSING CONTENT.
    THE VACUITY TRAP stays refused: §10's quantifier runs from the selected signature to the
      inventory that must cover it, never backward, so a thin inventory does not force an empty
      parameter record.

F7  PhysicalSupportAndCommonCore_0              CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  the support must suffice to type the seven-port interface; cross-port
      common-domain coherence -- "P4_P5_P6_P7_domains_reference_the_same_P1_domain"
      (03 [46217,46696)); B1 bears again.
    SURVIVES:  2 (B1) x the F1/F2 index.
    THE Core_0 GAP STANDS, with the check's discharge requirement carried: a sealed typed
      relation/transport plus preservation and noncollapse proofs.  NO CANDIDACY IS ENTERED.

F8  PhysicalRepresentationData_0                CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  source and record embeddings, carrier accessors, the physical CTP tuple,
      reference accessors and coherence witnesses positively require representation data
      (03 [34855,48128)); the flat field list keeps it conditional -- representation_data_if_required.
    SURVIVES:  unbounded, with the conditionality live.
    NOT IMPORTED:  the presentation-level scalarization continuum.  Class B, and the ceiling is the
      sentence that forbids reading the presentation upward.

F9  PhysicalAlgebraicCausalAnalyticConditions_0 CONSTRAINED-NOT-SELECTED
    IN THE INTERSECTION:  POSITIVE -- origin co-reference, the eight cross-row coherences, domain
      closure, target independence (03 [58090,58342)), graph acyclicity;
      NEGATIVE -- the Obj_0-directed causal exclusion, the builder's reading UPHELD by member 04.
    SURVIVES:  ALGEBRAIC and CAUSAL now have positive content; ANALYTIC has none.  And the field's
      value is a SET: bounded below by the named coherences, bounded above nowhere.
```

```text
TALLY = 0 SELECTED / 9 CONSTRAINED-NOT-SELECTED / 0 UNCONSTRAINED
TALLY_DELTA = NONE, from member 04's 0/9/0.

AND NO FIELD MOVES TO UNCONSTRAINED EITHER.  Member 01 permits that only where the check's
constraint assignment FAILS AT BYTES.  I tested all six of its U-to-C moves against member 03's
port spans and every one holds.  Q-957's assignment is confirmed, not merely inherited.
```

---

# STEP 3 — WHY THE INTERSECTION CANNOT PIN

## 7. The display of record [DERIVED HERE]

Member 04 refuted my "exclusion only" finding and its narrower replacement is right:

```text
CITATION
path   workspace/STAGE8_DESC_SELECTION_CHECK_CODEX2_V001.md
source 00ac4928a247acfe088b5374cc972e3d5528e8a2fdc7cd21d9d043cb52abeede
span   [15896,15992)  ANCHOR
digest a01e9eaf84c563192c9786ac015cddd0753e9af722b3415b31af5a4b04d51abe
```

> `NO FIELD IS POSITIVELY SELECTED.`
> `ALL NINE FIELDS ARE POSITIVELY OR NEGATIVELY CONSTRAINED.`

**This relay's contribution is the next sentence, and it is the one the terminal scoring will rest
on.**

```text
REASON 1 -- MONOTONICITY.  THE POSITIVE CONSTRAINTS ARE ALL LOWER BOUNDS, AND LOWER BOUNDS DO NOT
ISOLATE.

  Every restored port-schema constraint has the form "the signature must supply X", "the port must
  return Y", "the category must admit Z."  Each narrows the admissible set from below and none
  bounds it from above.  An intersection of lower bounds is upward-closed; an upward-closed set is
  a point only in a one-point ambient.  This is the same argument as §2.3 and it is not a
  coincidence that it recurs: THE SCHEMA AND THE NINE FIELDS ARE SPECIFIED THE SAME WAY, so they
  fail exactly-one the same way.

  THE EXCEPTION I WENT LOOKING FOR, BECAUSE THE ARGUMENT IS WORTHLESS WITHOUT IT.  A lower bound
  CAN isolate if it has UNIVERSAL-PROPERTY form -- "the universal such object" pins up to canonical
  isomorphism.  The record contains exactly one universal-property-shaped requirement: P1's
  COMPLETED carrier (05 [1163,1581)).  Completion is universal.  IT DOES NOT FIRE, because the
  object being completed is the source-record-field structure that Obj_0 is supposed to generate,
  and that is unfixed.  One candidate exception, examined, does not apply.  If a second is found,
  this reason weakens and the finding should be re-scored -- that is the honest form of it.

  AND THE UNIQUENESS SENSE STILL DOES NOT BITE (08 [6373,6499)).  Had completion fired, it would
  have pinned only up to isomorphism and the undeclared sense would have decided the verdict.  It
  did not fire, so no disposition above turns on the sense.  I record the near-miss because it is
  the one place in the section where that undeclared question would have become load-bearing.

REASON 2 -- THE SURVIVING SET IS A NONEMPTY PRODUCT OF DECLARED-OPEN BRANCHES.

  >= 4 category candidates   x  2 DomainKind_1  x  2 FamilyKind_5  x  EvolutionEquivalence_6 undefined
  >= 16 combinations before the undefined components are counted at all, and each factor is open by
  a SEALED SENTENCE THAT NAMES ITS OWN MISSING DEFINITION:
     categories                   four NO_VERDICT rows whose deciding evidence is the field itself
     DomainKind_1's predicate     searched; qualifying_definition_file_list EMPTY
     EvolutionEquivalence_6       "EXACT RELATION TO BE DERIVED AND FROZEN"; searched; EMPTY
     FamilyKind_5's NONEXHAUSTIVE criterion   does not exist
     three of the ten freeze inputs           have no typed shape at all

  THE INTERSECTION IS NOT EMPTY AND IT IS NOT A POINT.  Reporting only "no field selected" would
  hide that the surviving set has KNOWN STRUCTURE, and the structure is what a later derivation
  has to cut.
```

```text
WHAT WOULD CUT IT -- named from the record, not invented, and none of it a lane act:
  (1)  derive and freeze the P1 domain-equivalence predicate      -- TYPE-S, searched, EMPTY
  (2)  derive and freeze EvolutionEquivalence_6                   -- its own line says "TO BE DERIVED"
  (3)  a sealed minimality/canonicity principle for Phase-A type schemas  -- §2.3, §3
  (4)  a sealed assignment of the witness convention on the primitive side -- §2.4 axis 2
  (5)  a sealed resolution of the indexing axis                   -- THE GENERICITY QUESTION
  (6)  for field 9's analytic component: d_C0's common-origin provenance and DoR-007's
       discrete-to-continuum theorem, both named NOT discharged at 10 [2086,2176)
  ITEMS (1) AND (2) ARE DERIVATIONS SOMEONE CAN ATTEMPT.  Items (3)-(5) are rulings.  That
  distinction is the road's, not mine, and it is the useful part of this display.
```

## 8. The fork, stated for the record

```text
IF SELECTIONS APPEAR LATER, the remainder is whatever the branch product still carries: the
  category index propagates to fields 2, 4, 5, 7, 8, so cutting F1 collapses more of the product
  than cutting any other factor.  PhysicalCategory_0 remains the internal head, confirmed again
  here on the restored surface.

IF NONE APPEAR, the display above IS the record's answer to the section's open question, and it now
  has a shape: the record CONSTRAINS ALL NINE FROM BELOW AND BOUNDS NONE FROM ABOVE.  That is a
  sharper statement than "no field is selected," it is checkable clause by clause, and it locates
  the missing ingredient as a KIND of statement the record has never made about this object rather
  than as a fact it happens not to contain.

NEITHER BRANCH IS TAKEN HERE.  No slot moves; member 14 [16880,17121) is untouched; the ceiling is
  not exceeded and no common-origin claim is made or grounded.
```

---

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  member 04's hinge ruling and its 0/9/0 tally, EXECUTED not re-argued; its Field-9 upholding, its
  seven import refusals, its Core_0 discharge requirement, its genus/species and internal-head
  confirmations, and its 56/57 live-append finding; Q-955's build order and Q-957's hook; the
  provenance ceiling as disclaimer with its anti-circularity constraint; DoR-008's premises and two
  undischarged items; DoR-013's family-level ratification and no-member clause; Q-97's too-thin
  inventory; the third lane's uniqueness-sense requirement; S01-S37; all charter fences.

DERIVED HERE:
  the PHASE-A READINESS CENSUS of all ten freeze inputs, each status at bytes;
  the MONOTONICITY RESULT -- the sealed constraints on the schema are lower bounds with entry-level
    prohibitions only, hence upward-closed, hence no unique shape without a minimality principle;
  the FLOOR of the schema, exhibited in full, together with the ruling that a floor is not an object;
  the THREE OPEN AXES (indexing, witness location, multiplicity location), each at bytes;
  the TWIN FINDING -- the derived-sector schema is in the identical condition, so the genericity
    question is about a CLASS of Phase-A type schemas, not one object;
  the extension of monotonicity to the nine fields, and the UNIVERSAL-PROPERTY EXCEPTION examined
    and found not to fire;
  the BRANCH PRODUCT: >= 4 x 2 x 2 with two searched-and-empty relations, as the concrete display;
  the per-field intersections and the confirmation, at member 03's port spans, of all six of the
    check's U-to-C moves;
  the METHOD CORRECTION against my own 1045 occurrence-count instrument;
  the LIVE MEMBER CLASS and the two-mode prose-digest audit, responsive to the 56/57 finding.

SELECTED OR AUTHORED:
  NOTHING.  No physical category, object type, arity, operation, domain, codomain, parameter,
  support, core, representation, condition, primitive, binding, type schema, equivalence relation,
  branch, member, candidate, state, route or physical result is selected, authored, frozen,
  adopted, promoted, entered or identified.  NO SCHEMA IS FROZEN -- Step 1 was the one authorised
  freeze and its condition did not arise.  NO FIELD IS DECIDED.  No Core_0 or field-7 candidacy.
  DoR-013's no-member clause carried live.  The ceiling not exceeded; nothing changed hands.

SCALING WEIGHTS: none consumed, formed, fixed, compared, substituted or evaluated.  Every scale
  symbolic.  The branch counts above are counts of SEALED NAMED ALTERNATIVES, not magnitudes; no
  norm, trace, rank, dimension, cardinality-as-quantity or ratio was computed or bounded.
```

---

## 10. NO OUTPUT INSPECTION — CERTIFIED, TEMPTATION NAMED IN ADVANCE

```text
THIS RELAY'S TEMPTATION IS NOT 1045'S, AND NAMING THE OLD ONE WOULD HAVE BEEN A DODGE.

  1045's temptation was to reach for the rich premise-side objects.  1047's is different and it
  comes from having been corrected: HAVING HAD MY BOUNDARY RULED TOO TIGHT, THE PRESSURE IS TO
  OVER-CORRECT -- to treat every restored port constraint as if it were content, and to read the
  seven exact codomain packages as the nine field values.  T04 is thirteen kilobytes of typed
  material and it is RIGHT THERE.  Field 5 would have looked filled in a paragraph.

  IT IS REFUSED AT §4 BY A DISTINCTION STATED BEFORE THE FIELD PASS RAN: interface operations are
  not internal operations, and a port's codomain package is not the source's codomain field.  The
  check's own flattening list says the same thing from the other side, and member 06 [37480,37635)
  is the sentence that makes consuming a descendant row circular.

  THE SECOND PRESSURE, EQUALLY REAL: Step 1 authorised a freeze.  Having exhibited a floor in full,
  the next move wants to call the floor "the schema" and report FROZEN-BY-ENTAILMENT.  §2.2 answers
  it in the same breath as the exhibition, not afterwards.

  ORDER OF WORK: the census and the floor were built from bytes before the monotonicity argument
  was written; each field was intersected at its own spans before the tally, the branch product or
  §7 existed; §7 and §8 consume the field results and never fed them.  Class C -- K4, the trace,
  kappa_record, alpha, the battery, the response chain, C_record(K), and every candidate output --
  was not opened, not imagined, and not consulted.
```

---

## 11. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.**
  - **A lower bound was not flattened into an object** (§2.2, §2.5) — the floor is exhibited and
    the schema is not frozen. This is the relay's central refusal.
  - **A requirement was not flattened into a determination** (§6, §7) — positive constraints are
    real and they do not isolate.
  - **An interface operation was not flattened into an internal operation** (§4, F3), and **a port
    codomain package was not flattened into a source codomain value** (F5).
  - **A co-frozen constraint was not flattened into a value** — the correction is carried in the
    direction it was made, and not over-applied.
  - **A name was not flattened into a typed shape** (§1, §2), and **an applied type was not
    flattened into a defined one** (freeze input 7).
  - **An occurrence count was not flattened into an argument** (§2.1) — reported against myself.
  - **A shape-space was not flattened into a shape** (§2.5), and **an open branch was not flattened
    into a default** (§5) — no branch is picked anywhere.
  - **A precedent pattern was not flattened into a rule** (§3) — the twin shows a pattern, and I
    do not legislate from it.
  - **A universal property was not flattened into an active constraint** (§7) — completion is
    universal and its input is unfixed, so it is displayed and not used.
  - **A confirmation was not flattened into a re-derivation** (§6) — the six U-to-C moves are the
    check's; I tested them and report them as confirmed.
  - **A live-append digest drift was not flattened into a stable pin** (closure, §11) — member 09's
    whole-file digest is declared a snapshot and carries no verdict weight.
- **BUILDER-NEVER-VERIFIES:** all results CLAIMED until the opposite-lane check. I did not
  re-adjudicate member 04's hinge, its Field-9 upholding, its import refusals, Q-97, DoR-008,
  DoR-013 or the third lane's rulings. I read them and executed them.
- **SELF-CITATION BAR HELD:** no span from any DARIO artifact. Members 13, 21, 22 carriage-only;
  R5 refuses; 0 span rows name them.
- **BLIND HELD.** No physical quantity formed, evaluated or compared. No member bound. No fixed
  point executed. `omega_phys` untouched. Member 12 read only with members 13 and 11.
- **PE-1..PE-21: POINTER-ONLY, LEDGER NOT OPENED**, the nine-values hook included. Q-957 requires
  the outcome scored against it by name; the outcome is reported in the form the scoring needs and
  the scoring is the registrar's or the principal's act.
- **CUSTODY:** archive-side only; two files written under `workspace/`, both sealed, plus the outbox
  reports. Cleanroom not entered. No register, plan, tracker, chain execution, end test or subject
  edit. **NO GIT ACTION OF ANY KIND, READ-ONLY INCLUDED.** NO SUBAGENT DELEGATION.
- **BYTE AUDIT:** all 23 members rehashed at their archive-root paths at run time by member 23; all
  40 spans computed and published as full tuples; **10/10 FIXED re-verified**, and five of those ten
  are member 04's own T01–T05 pins, independently reproduced here rather than copied — a cross-lane
  agreement worth having in the record. 30 anchors resolved uniquely. Sidecar check: **22/23
  SEAL-OK**, member 23's own `NO-SIDECAR` at run time because it is sealed after the run — absent
  is reported as absent, never as agreement. Closure declared at byte 0 with its end computed as a
  fixed point under a fixed-width field; predeclaration region empty.
- **GENERATOR CUSTODY (Q-920/Q-924):** declared inputs exactly members 01–23; five refusal paths;
  final run `FILES=23 SPANS=40 FIXED-REVERIFIED=10/10 MATCH ANCHORS=30`. **R3/R4 fired four times in
  construction and are reported rather than asserted:** three were flag names occurring twice —
  `P1_domain_equivalence_relation_found`, `P6_action_evolution_equivalence_relation_found`,
  `FrozenPortDependencyGraph_0_derived` — each once in its own section and once in a consolidated
  ledger, and one was `Proposed port signature:` occurring five times. **THE FLAG-NAME TRAP HAS NOW
  FIRED EIGHT TIMES ACROSS THREE RELAYS.** It is no longer a curiosity: in this corpus a flag name
  is never a unique anchor, and a generator that does not refuse on ambiguity will silently pin a
  consolidated-ledger restatement instead of the section it means. R1, R2 and R5 did not fire;
  not-fired is reported as not-fired.
- **THE LIVE-APPEND FINDING, ANSWERED IN MECHANISM RATHER THAN IN PROSE.** Member 04 found my 1045
  artifact at 56/57 because the register grew between seal and check. Member 23 now declares member
  09 LIVE, excludes its whole-file digest from the stable accounting set, and runs the audit in both
  modes. This artifact quotes no LIVE whole-file digest, so its two results are identical and stay
  identical however far the register advances. **A finding about an instrument is best answered by
  fixing the instrument.**
- **RESIDUE, per Q-921:** five-family scan over authored prose, fenced blocks excluded, run before
  sealing on the final bytes, loci read rather than predicted. Families, result and loci are inside
  the fence below, because at 1045 the accounting counted itself twice before it was fenced.

  ```text
  FAMILIES  F1 reproduc*
            F2 reconstruct* | rebuild* | re-run* | rerun* | replay*
            F3 byte-identical | stable | stability | byte-stability | verbatim
            F4 remov* | delet* | nothing-removed
            F5 set-delta | diff-triple | membership movement | row-continuity

  RESIDUE = 4 RAW / 0 OPERATIVE        (fence delimiter lines: 70, even)
     line 452   F2 "preconstruction"  -- NOT MY PROSE.  It is inside a QUOTED SEALED SPAN from
                member 06, and the reconstruct-family pattern is matching a substring of a
                different word.  The same substring hit was recorded and ruled lawful at 1042.
     line 794   F3 "stable pin"       the LIVE-member mechanism, in the flattening list.
     line 811   F1 "reproduced"       of member 04's five T01-T05 pins.  THE ONE HIT WORTH
                PAUSING ON, because it is a reproduction claim and the family exists to catch
                those.  It is COMPUTED, not asserted: member 23 re-verified those five intervals
                against the check's published digests and reports 10/10 FIXED.  It is a
                cross-lane agreement on present bytes, NOT a historical rerun certificate and
                NOT a claim that any earlier run is replayable.
     line 828   F3 "stable accounting set"   the LIVE-member mechanism again.

  NO LOCUS ASSERTS artifact reconstruction, a historical rerun certificate, deletion carriage, a
  byte-stable copy, or membership movement.  This artifact supersedes nothing and carries no
  predecessor's bytes.  A DRAFT OF THIS BLOCK PREDICTED 2 AT FENCES 60 AND THE SCAN RETURNED 4 AT
  FENCES 70; the predicted figures are replaced by the instrument's, which is the third relay
  running in which a drafted residue count has been wrong and the scan right.
  ```

  **CLAIMED, not clean** — the hand that wrote the prose ran the scan.

---

## 12. The certificate

Per Q-913/Q-917/Q-921/Q-930 and CLOSURE_MEMBER_CITATION_RULE_V001: **the certificate is the
twenty-three-member digest table plus the forty full citation tuples**, as member 23 emits them,
with member 09's whole-file digest declared a run-time snapshot carrying no verdict weight. Member
23's own digest is computed by itself and reported in the outbox. **There is nothing below the
digests.**

---

## 13. Final lines

```text
CLOSURE = declared-first (byte position, scan)
     byte 0; closure end 7673 under a fixed-width field; predeclaration region EMPTY, scan 0
     possible hits.

SCHEMA = SHAPE-SPACE-DISPLAYED (genericity reached; not frozen)
     THE FLOOR IS EXHIBITED IN FULL and it is a real result: per entry an authority path, a content
     hash, an exact type, a domain and a multiplicity (03 [49744,50735); 03 [55060,55466)); exact
     cover of the free primitive ground sorts/operations/parameters/domains of the signature AND the
     seven port schemas; closure under declared compositions; frozen before any candidate output;
     five entry-content prohibitions.
     THERE IS NO CEILING.  Every sealed clause bearing on the schema is a REQUIREMENT; the only
     prohibitions bar CONTENT FROM ENTRIES, never a FIELD from the record type.  The admissible
     shapes are therefore CLOSED UPWARD, and an upward-closed family has no unique least element
     without a minimality principle.  THE RECORD DECLARES NONE, here or for any Phase-A type schema.
     CARDINALITY/STRUCTURE: >= 3 (indexing: signature-parametric / sort-universe / open-record --
     THE GENERICITY QUESTION IN EXACT FORM) x 2 (witness location: the port convention puts
     witnesses in the Sigma-package, the section-10 convention puts them in a context predicate, and
     no sentence assigns the primitive side) x 2 (multiplicity: binding datum per SIG-T2, or
     signature-supplied per section 10's prose -- reconcilable in content, unsettled in shape),
     and unbounded above.  NOT ONE.  A LANE DOES NOT CHOOSE A SCHEMA.  THE STEP STOPS.
     THE TWIN: FrozenDerivedSectorReferenceTypeSchema_0 is in the identical condition
     (03 [50735,51494)).  The genericity question is about a CLASS of Phase-A type schemas -- those
     specified by constraining the CONTEXT that will instantiate them -- not about one object.

PHASE-A CENSUS = 10 FREEZE INPUTS: 3 WITH NO TYPED SHAPE (primitive-binding, derived-sector,
     origin-predicate/trace -- the last APPLIED FIFTEEN TIMES AND DEFINED NEVER); 2 SEARCHED-AND-
     EMPTY RELATIONS (DomainEquivalence_1, EvolutionEquivalence_6); 1 COMPLETENESS-TYPE-U (graph);
     3 WITH CONTENT (port contracts 13,273 B, cross-row coherence, certificate-partial); 1 UNDER
     SELECTION.  THE GATE IS NOT ONE MISSING OBJECT, which is how I reported it at 1045.

FIELDS = 0/9/0 of 9 (intersection surface; each with locus at section 6)
TALLY_DELTA = NONE, from member 04's 0/9/0.  No field selects and none reverts: I tested all six
     U-to-C moves against member 03's port spans and every one holds, so Q-957's assignment is
     CONFIRMED rather than merely inherited.

WHY_NOT_PINNED = DISPLAYED, two reasons, both checkable
     (1) MONOTONICITY.  The restored Class-A constraints are LOWER BOUNDS -- "must supply", "must
     return", "must admit".  An intersection of lower bounds is upward-closed and never isolates a
     point.  THE SAME ARGUMENT THAT STOPS STEP 1 STOPS STEP 2, because the schema and the nine
     fields are specified the same way.  THE EXCEPTION WAS LOOKED FOR: a universal-property-shaped
     lower bound CAN isolate, the record contains exactly one -- P1's COMPLETED carrier -- and it
     does not fire because the object being completed is unfixed.  One candidate, examined, does not
     apply; a second would weaken this reason and should re-score it.
     (2) THE BRANCH PRODUCT.  >= 4 category candidates x 2 DomainKind_1 x 2 FamilyKind_5 x an
     EvolutionEquivalence_6 whose definition line reads "EXACT RELATION TO BE DERIVED AND FROZEN"
     -- >= 16 combinations before the three shapeless inputs are counted, and EVERY factor is open
     by a sealed sentence that names its own missing definition.  For both equivalence relations the
     record already ran the hunt and recorded qualifying_definition_file_list: EMPTY -- twice in
     member 03, by scan.  THE INTERSECTION IS NEITHER EMPTY NOR A POINT; the surviving set has known
     structure, and cutting F1 collapses more of it than cutting any other factor.
     WHAT WOULD CUT IT, named from the record: derive the P1 domain equivalence; derive
     EvolutionEquivalence_6; declare a minimality principle for Phase-A type schemas; assign the
     witness convention; resolve the indexing axis; and for field 9's analytic component, d_C0's
     common-origin provenance and DoR-007's theorem (10 [2086,2176)).  THE FIRST TWO ARE
     DERIVATIONS SOMEONE CAN ATTEMPT; THE REST ARE RULINGS.

CASCADE_STOPS = NONE.  No field's disposition required a numeric evaluation.  Boundary named and not
     approached: a finite dimension or sector-dimension ratio on the Gen_Omega side.  The branch
     counts are counts of sealed named alternatives, not magnitudes.

SLOT = REMAINS-NO-VERDICT (14 [16880,17121), untouched).  CEILING = NOT EXCEEDED; NOTHING CHANGED
     HANDS.  No common-origin claim is made or grounded.

PROSE_DIGESTS = 39/39-ACCOUNTED (51/51 occurrences), STRICT AND STABLE MODES IDENTICAL, OUTSIDE SET
     EMPTY IN BOTH -- which is the property the LIVE member class exists to guarantee, and it answers
     member 04's 56/57 finding in mechanism rather than in prose: this artifact quotes no LIVE
     whole-file digest, so its audit result does not move when the register next advances.
     A DRAFT OF THIS LINE PREDICTED 60/60 AND 105/105 AND BOTH WERE WRONG; the instrument's figures
     replace them, as with the residue count two lines below.  I should stop drafting counts from
     memory -- that is the same lesson, from the same relay, twice in one artifact.

RESIDUE_SCAN = 4 RAW / 0 OPERATIVE (fences 70, even).  Loci and their classification are inside a
     fence at section 11.  The one worth naming here: "reproduced", of member 04's five pins --
     a COMPUTED cross-lane agreement (10/10 FIXED re-verified), never a replay certificate.

SIDECARS = 22/23 SEAL-OK; member 23's own NO-SIDECAR at run time, sealed after the run.

OUTPUT_INSPECTION = NONE-CERTIFIED.  This relay's own temptation named in advance and refused: having
     been corrected for a boundary too tight, the pressure is to OVER-CORRECT and read the seven
     exact codomain packages as the nine field values.  Refused at section 4 by a distinction fixed
     before the field pass -- interface operations are not internal operations, and a port's codomain
     package is not a source field's value.  Class C never opened.

CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
     "FROZEN" appears only in quotation, in the name of the objects, and in the sentence saying no
     schema is frozen; the count of freezes performed is 0.  "EXHIBITED" is used of the floor and
     never of the schema.  "ENTAILED" is used only in the negative.  "CONSTRAINED" is used where a
     sealed clause bounds a field from below or excludes an entry, never where a field is merely
     dependent.  "PINS" is used of what a constraint would do and never of what one did.  "BRANCH"
     is used of sealed named alternatives, never of a chosen one.  "CONFIRMED" is used of the check's
     six moves, which I tested, and never of anything I merely inherited.  Nothing adopted is called
     derived.  No member bound, no field selected, no schema authored, no branch chosen, no stop
     lifted or defended.
```

All findings CLAIMED until the opposite-lane check.

**The step to press first is §2.3's monotonicity result, because everything else in this relay
leans on it and it is mine.** It says the sealed constraints on the schema are lower bounds with
entry-level prohibitions only, hence upward-closed, hence no unique shape. **The way to break it is
to exhibit an upper bound I missed** — a sealed clause that forbids a field, restricts a Phase-A
type schema's structure, or imposes minimality anywhere in the corpus. I read the twelve conjuncts,
the nine conjuncts of the twin, SIG-T1, SIG-T2, SIG-T11 and §12.1's four rows for exactly that and
found only entry-content bars. **If one exists, Step 1 may be entailed after all and the schema
freezes** — which is why I state the claim in the falsifiable form rather than as a conclusion.

**The second thing to press is the universal-property exception at §7.** I claim the record has
exactly one universal-property-shaped requirement and that it does not fire. That is a census claim
over thirteen kilobytes of port contracts, made by reading them for a particular logical shape, and
census claims are exactly what a second reader breaks. **A second universal property — a completion,
a limit, a free construction, an initial or terminal object anywhere in the seven contracts — would
weaken reason 1 directly**, and if its input were fixed it could pin a field up to isomorphism, at
which point the undeclared uniqueness sense stops being a near-miss and becomes the deciding
question. That is the shape of the attack I would run against this artifact.
