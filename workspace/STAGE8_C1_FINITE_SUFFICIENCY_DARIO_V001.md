# STAGE 8 / [PLAN:C1-3] — THE FINITE-SUFFICIENCY AUDIT: DOES ANYTHING DOWNSTREAM CONSUME THE INFINITE-FUTURE SOURCE OBJECTS?
## DARIO LANE (Builder B) — V001

RELAY 824. Lane guard PASS (DARIO). Inbox `RELAY_PASTE_824_FINITE_SUFFICIENCY_DARIO_V001.md`
= `c9e64daa1837f2bc23a5ec168b0111bae13f573ec2182f7e17452484625ae02c`, seal verified BEFORE reading.
State-brief pinning: `PROGRAM_STATE_BRIEF_V005.md` = `e26f0d16055f3e83…`, verified and read.

GATES DECLARED AND HELD: **AUDIT ONLY.** `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. Nothing reformulated; **no requirement weakened by fiat — the
sentences decide**; no smooth import; no EM identification; no member bound; no numeric evaluation
of physical quantities; no comparison to measured constants.

**PE-POINTER-ONLY (law 6) — A COMPLIANCE NOTE ABOUT THIS RELAY'S OWN TEXT.** The relay names the
D_BR consumer as *"the D_BR family evaluation's needs (PE-6's discrimination point)"*. PE-6 is a
principal-expectations ledger entry: **pointer-known, zero verdict weight, record but do not
consult.** I record the pointer and **did not open it**. The D_BR consumer below is audited from the
sealed D_BR stock instead. The same bar was written into the instructions of every delegated agent.

**ALL HEADLINE ITEMS ARE CLAIMED.**

---

## 0. THE INSTRUMENT, AND WHY IT IS A LEXICAL ONE FIRST

The hypothesis under test is a **negative**: that nothing downstream consumes O1 (source-inclusive
projective state limit), O2 (infinite-future source Møller unitary), or O4 (complete source-inclusive
GNS). A negative over a corpus is only as good as the probe, and my probes have been the weak part
of my last five relays. So I ran a **lexical decider first** — a case-insensitive count of sixteen
source-infinite terms across every named consumer — before reading a single sentence for meaning.
A consumer with zero hits over tens of thousands of characters cannot be consuming the objects,
whatever a reading might suggest; a consumer with hits gets read sentence by sentence.

```text
consumer document                             bytes   source-infinite term hits
junction 13-item checklist (relay 816)        26801   8   {source-inclusive:3, infinite-future:3,
                                                            moller:1, asymptot:1}
recognition prereg V002 FROZEN (MINE)         48484   0   -- NONE --
D_BR tower contest (MINE)                     22367   0   -- NONE --
B2 gluing scope and run (Codex)                9678   0   -- NONE --
B2 corpus verify (MINE)                       19574   0   -- NONE --
THE PLAN V004 (F3a)                            8911   0   -- NONE --
E4 verification 799 (MINE)                    23590  23   (the reporter of the gap)
E4b proof + Moller hunt 800 (MINE)            22259  45   (the reporter of the gap)
```

Terms swept: `source-inclusive`, `source inclusive`, `infinite-future`, `infinite future`, `moller`,
`møller`, `asymptot`, `s-matrix`, `scattering`, `t -> infinity`, `all-time`, `global source`,
`projective limit`, `source limit`, `in/out state`, `asymptotic completeness`.

**Five of the six named consumer classes return ZERO.** Only the junction checklist has hits among
the consumers proper, and 799/800 have many — but those are my own artifacts that *diagnosed* E4c;
they are the reporters of the gap, not consumers of the object. Each of the junction's 8 hits is
read in §1.1 below.

---

## 1. AS1 — THE CONSUMER LIST, TYPED

### 1.1 The junction's 13 runnable prerequisites — the decisive consumer

Source: `STAGE8_C1_COMMON_CELL_POSED_CODEX2_V001.md` (`fa4909a4611f2d09`, `.md.seal` OK), §3.2,
span `[19653,21917)` sha `982ee4eea0d6aac9`, all thirteen quoted in full in the source. Register
anchor Q-723 (relay 816). Typing each from its own words:

| # | prerequisite (abridged; the span carries them verbatim) | type | the words that decide it |
|---|---|---|---|
| 1 | response-complete grammar, equivalence/exclusion, coefficient descent, no-outside (E15) | NO-SOURCE-MENTION | map OBJECT 1 — the action axis |
| 2 | supply `f_g`, physical-path realization of every `Ref_a` generator (E13) | NO-SOURCE-MENTION | map OBJECT 2 |
| 3 | supply `F_g`, CIS interaction-density support → `Ref_a` subdivision support (E12) | NO-SOURCE-MENTION | map OBJECT 3; *"CIS interaction-density support"* is finite by CIS |
| 4 | supply `eta_resp`, `ResponseData`, `Eval`, `Q_resp`, response topology (E14) | NO-SOURCE-MENTION | map OBJECT 4 |
| 5 | supply and replay `U_Omega` — one diamond **per primitive incidence** (E07) | NO-SOURCE-MENTION | map OBJECT 5; per-incidence, hence finite |
| 6 | materialize **an actual named common record cell `e`** with content-addressed bytes (E01) | FINITE-SUFFICIENT | *"an actual named … cell"* |
| 7 | replay R1–R9 for the Ward-symbol subject **on a named oriented cell** (E02) | FINITE-SUFFICIENT | *"on a named oriented cell"* |
| 8 | replay R10–R14 for the normalization subject **on the same cell** (E03) | FINITE-SUFFICIENT | *"the same cell"* |
| 9 | exhibit **the same cell's** junction as derived and `beta`-sensitive (E05) | FINITE-SUFFICIENT | *"the same cell's"* |
| 10 | exhibit the two typed returns as independently formed **on `e`** (E01/E04) | FINITE-SUFFICIENT | *"on `e`"* |
| 11 | replay J1/J2 — crossings, equality, invariance under re-presentation (E04) | FINITE-SUFFICIENT | cell-local replay |
| 12 | replay J3/J4 — no implicit value, no weak-rule surviving family (E04) | FINITE-SUFFICIENT | cell-local replay |
| 13 | **pin the support/exhaustion evidence**: containment, one-use, inductive compatibility, **narrowed cofinal state agreement**, E4a, and **the still-live dressed-map disjunct**; **report U2 sufficiency as ungranted** (E06/E08–E11/E16–E19) | FINITE-SUFFICIENT | it asks for the **narrowed** agreement and to **pin/report** the disjunct — not to close it |

**Item 13 is the only one that touches the limit layer, and it asks for the finite form.** It
requires *narrowed* cofinal state agreement — E4b's finite shape — plus E4a (proved), plus that the
dressed-map disjunct be **pinned as still-live** and U2 sufficiency **reported as ungranted**.
Reporting an open is not consuming its object.

**The consumer states its own consumption rule**, and this is the single most decisive sentence in
the audit (`[22171,…)` sha `9733c6a930ca`):

> *"E4a may be consumed exactly as D15 proves it … No exhaustion is chosen.
> — **E4b may be consumed only in D16's narrowed form**: the promoted ordinary `3+1` flat-asymptotic
> branch, the same finite source-record parent, future primitive cells that do not subdivide old
> primitive cells, and **equality after common finite causal support and its buffer**. **The
> stronger adjacent-swap/prefix statement is not used.**"*

The junction does not merely fail to require the infinite form — **it declares in its own voice that
it consumes only the finite one.**

**All 8 source-infinite hits in this document, read in place, are reports-of-open, not requirements:**

```text
char 14741/14757  E17 in the 20-element interface CENSUS: "agreement of dressed maps in the
                  infinite-future source-inclusive comparison" — a catalogued interface element,
                  and NOT among the 13 runnable prerequisites.
char 15738/15754  the GAP list: "E4c's monomorphism-versus-unitary and finite-completed-record
                  versus infinite-future-source-inclusive gaps (D17)" — an enumeration of gaps.
char 22615/22631  "E4c remains open. … None is an inverse-bearing infinite-future source-inclusive
                  intertwiner between two cofinal dressings" — a report that the object is absent.
char  4644        'MOLLER' occurs only inside the FILENAME of my own 800 artifact in a citation row.
char 22171        'asymptot' occurs only in "flat-asymptotic branch" — a scope name, not an object.
```

### 1.2 The recognition specification — NO-SOURCE-MENTION

`STAGE8_7A_OBS22_RECOGNITION_PREREG_V002_FROZEN.md` (`9f0d12b4556427eb`, `.md.seal` OK, 48,484
chars — **my own lane's artifact**, see §5). **Zero occurrences of all sixteen source-infinite
terms.** Its criteria are record-native throughout: the Ward-symbol map, the length normalization,
the joint-landing criterion, the anti-scar guards, the divergence typing. Recognition evaluates
displayed maps and equalities on named subjects. **It does not reach the source sector at all.**

### 1.3 The D_BR family evaluation — NO-SOURCE-MENTION (PE-6 pointer NOT followed)

Audited from sealed D_BR stock (`STAGE8_7A_TOWER_CONTEST_DARIO_V001.md`, `03db8d3da273f42c`, **also
mine**): **zero** source-infinite terms across 22,367 chars. The D_BR evaluation consumes spectral
and bound data of a finite operator family (`λ ≥ C2_parent`, the kind-fork, the five free data). The
relay's pointer to PE-6's discrimination point is recorded and **not opened** (law 6).

### 1.4 E4c as C1 must carry it — the one AMBIGUOUS item, both readings displayed

My 800 states what C1 must carry: *"the dressed-maps disjunct alone, now as a **NARROWED live
falsifier**: not 'no Moller object exists' but 'the derived object is a MONOMORPHISM on completed
records, not a UNITARY on the infinite-future source-inclusive limit.'"*

```text
READING A (FINITE-SUFFICIENT): what C1 must CARRY is a correctly typed REPORT — that the derived
  object is a monomorphism, not a unitary.  C1 can carry that today, with no infinite object in
  hand.  On this reading nothing is consumed.
READING B (INFINITE-REQUIRED in effect): E4 is a three-conjunct negation of a DISJUNCTIVE falsifier,
  so a LIVE disjunct is a live route to falsification.  The program is therefore EXPOSED to the
  absence of the infinite object even though it never uses it — the object is not consumed, but its
  non-existence could still kill the result.
```

**Both readings are available in the sealed text and I decline to pick.** This is the audit's one
genuine ambiguity at the item level.

### 1.5 The B2 diagnostic and the F-chain's F3a — NO-SOURCE-MENTION

`STAGE8_B2_GLUING_SCOPE_AND_RUN_CODEX2_V001.md` (0 hits), `STAGE8_7A_CORPUS_VERIFY_B2_DARIO_V001.md`
(0 hits, **mine**), and `THE_PLAN_TO_ALPHA_AND_GRAVITY_V004.md` for F3a (0 hits). None reaches the
source-infinite sector.

### 1.6 The adversarial sweep: I hunted for a consumer that DOES require them

Archive-wide regex pairing any source-infinite term with requirement language (`requires`, `must`,
`needed`, `prerequisite`, `consumes`, `depends on`) within a ±420-character window: **86 sentence
hits across 46 distinct files.** Read in place, they partition cleanly:

- **Scope boundaries reporting the objects as open** — the large majority (R3's own scope boundary,
  the GNS result's *"It does not establish: a projective limit of full source-record states; an
  infinite-future source Moller unitary"*, the CDL Principle, Fork-8's non-promotions, the Stage-7
  review conditions, `CONTINUATION_STATE`).
- **Failed-attempt post-mortems listing what a dead route would have needed.** The strongest-looking
  candidate is exactly this. `STAGE8_TASK4A_RESPONSE_CLASS_FORCING_AND_TAIL_OUTPUT_INERTNESS_DETERMINATION_V001.md`
  `[85,1065)` sha `c0e90ace7af7fcf6`: *"**The proof attempt fails at the physical source
  completion.** The sealed finite operations are norm/module preserving, but the physical chain then
  requires: source differentiation …; **source-inclusive continuum/projective completion**;
  convolution inversion …; stationary 2PI-to-1PI reduction …; contour/i-epsilon boundary values;
  retarded extraction …; and, downstream, a `q^2 -> 0` Thomson limit."* — followed by *"No sealed
  source types any of those limit or derivative operations as norm preserving."* This is a report of
  **why an attempt died**, not a live consumer holding a claim on the object.
- **A live chain that would need it, and its path status is the audit's second ambiguity.** The
  seven-item list above is the **physical retarded-response → Thomson-limit chain**, and the same
  shape recurs in the T7 / Task-3c / CTP-amplitude artifacts (*"one complete normalized scalar
  source-inclusive relative-history CTP amplitude"*). THE PLAN V003 and V004 both carry, verbatim:
  *"**ALPHA'S STIFFNESS IS K_\***. The R-L2b/linked-cluster campaign is **OFF alpha's value path**."*
  **But the plan types the R-L2b campaign, not the Thomson-response chain, and it does not identify
  the two.** I therefore display this rather than resolve it: if the Thomson chain is the R-L2b
  campaign, the last consumer of the infinite objects is off the value path; if it is not, that
  chain is the true holder of the source question. **A sealed sentence identifying or separating
  them does not exist, and I decline to supply one.**

**No sentence anywhere in the sweep requires an infinite-future source object as a live
prerequisite of a consumer on the junction's path.**

---

## 2. AS2 — THE VERDICT — CLAIMED

**O1 and O2 (and O4) LEAVE THE CRITICAL PATH TO JUNCTION RUNNABILITY.**

Nothing among the junction's 13 prerequisites, the recognition specification, the D_BR family
evaluation, the B2 diagnostic, or F3a consumes them. The junction states affirmatively that it
consumes E4b **only in the narrowed finite form**, and the one prerequisite that touches the limit
layer asks for the narrowed agreement plus a *report* that the disjunct stays live. Five of six
consumer classes contain no source-infinite vocabulary at all.

**The principal's structural hypothesis is supported by the record's own architecture**, and R3
draws the line itself (`[3729,4025)` sha `1cede082dad45162`):

> *"**For every finite set of causal cells, compact support makes the Moller maps finite propagator
> expressions. The output record states are restriction compatible and define a quasi-local record
> state.** For the full source-inclusive algebra, however, later cells continue to act on the shared
> source."*

The record side needs only finite Møller maps. The infinite objects are for the source-inclusive
algebra — and nothing on the junction's path consumes that algebra.

### 2.1 Two things that keep this from being a clean sweep — CLAIMED

**(a) The record side is not entirely free of source asymptotics, and I will not claim it is.**
The CDL Principle makes durability *"consist jointly of **thresholded source nonreturn** and exact
completed-record persistence"*, and thresholded nonreturn is an asymptotic source statement, obtained
from Riemann–Lebesgue on an absolutely continuous free-tail spectral density (R3 `[3150,3611)` sha
`cd0937d23fac71a0`). **It is however already derived** (`thresholded_source_return_derived = true`,
`free_tail_source_spectrum_absolutely_continuous = true`) and it comes from free-branch spectral
data, **not** from O1/O2/O4. So the record side consumes one source asymptotic, and that one is paid.

**(b) Runnability is not a physical verdict.** The checklist's own closing sentence: *"Passing them
would only make the test runnable; it would not by itself supply the frozen U2 sufficiency theorem,
**close E4c**, or authorize a physical verdict."* Whether a physical verdict *requires* E4c closure,
or whether E4c is merely another open the 13 do not reach, **is not settled by that sentence** — it
is a list of things not achieved, not a statement of preconditions. Displayed, not resolved.

---

## 3. AS3 — THE RE-PRICED REMAINDER — CLAIMED

**Under this verdict:**

```text
CRITICAL PATH TO RUNNABILITY  =  checklist items 1-5  =  THE FIVE MAP OBJECTS, exactly
                                 (grammar/no-outside; f_g; F_g; eta_resp; U_Omega)
                              +  items 6-12, cell-local replays on a materialized cell e
                              +  item 13, pinning evidence that already exists
OFF THE CRITICAL PATH         =  O1 source-inclusive projective state limit
                                 O2 infinite-future source Moller unitary
                                 O4 complete source-inclusive GNS
                                 — they remain NAMED OPENS of the completion; the junction does not
                                   consume them, and no consumer audited here does.
```

This converges exactly with the registrar's own remainder map — *"10 interface elements already
sealed + these five objects + nothing else"* — and it explains **why** that count is right: the
junction's 13 items reduce to the five map objects plus mechanical cell-local replays.

**The re-pricing is therefore structural, not arithmetic.** My 820 priced the completion's remainder
as though the source-side opens were on the road to the junction. They are not. What that changes:

- **820's O1/O2/O4 do not need to be scheduled before the junction runs.** The 4–8 and
  not-estimable figures I attached to them at 820 are not on the runnability path.
- **The junction's price is the five map objects**, whose own statuses are unchanged by this audit:
  OBJECT 1 fenced (T5), OBJECTS 2/3 undecidable with S26 barring one door, OBJECT 4 constrained not
  supplied, OBJECT 5 undecidable with its lemma named.
- **Nothing here makes the program cheaper overall.** It moves three items off one road; it does not
  discharge them, and it does not touch the five objects that actually block the junction.

**And the honest counterweight to the whole audit:** the finding is a *negative about consumption*,
not a licence to stop caring about O1/O2/O4. They remain named opens; the dressed-map disjunct
remains a live falsifier route (§1.4 Reading B); and if the Thomson-response chain turns out to be on
alpha's value path (§1.6), the source question returns with that chain as its holder.

---

## 4. FREEDOMS-CONSUMED (law 2 / law 2a)

```text
CARRIED, NOT CONSUMED:
  O1, O2, O4                        CARRIED AS NAMED OPENS; not discharged, not weakened
  E4a / E4b / E4c                   CARRIED AT SEALED SCOPE; E4b at its NARROWED form only
  the 13 prerequisites              CARRIED AS THE CONSUMER STATES THEM; none reformulated
  the five map objects              CARRIED AT THEIR EXISTING STATUSES, unchanged by this audit
  thresholded source nonreturn      CARRIED AS ALREADY DERIVED, and its source-asymptotic
                                      character DISCLOSED rather than suppressed (§2.1a)
  PE-6                              POINTER RECORDED, NOT OPENED (law 6)
DERIVED HERE:                       nothing.  This is an audit.
COMPUTED HERE:                      a 16-term lexical sweep over 8 consumer documents; an
                                      archive-wide regex pairing source-infinite terms with
                                      requirement language (86 hits / 46 files), each read in place.
                                      No physical quantity, no member, no coefficient.
NO REQUIREMENT WEAKENED BY FIAT:    the relay's own bar.  Every typing is quoted to the words that
                                      decide it; where two readings survive I display both and pick
                                      neither (§1.4, §2.1b, §1.6).
SCALING WEIGHTS (law 2a):           NONE CONSUMED.
SUBSTITUTED:                        NONE.
```

## 5. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

37 rows walked. Live and discharged:

- **S12** (status flags never substituted for manifests, generators or proofs) — **LIVE.** The audit
  leans on flags (`thresholded_source_return_derived = true`, the false O-flags). Each is carried as
  the record's own typed status; none is promoted into an object.
- **S26 / S08** — untouched. Notably the audit's verdict *reduces* any temptation to reach for a
  smooth or asymptotic import, since the consumers turn out not to want one.
- **S25, S03, S28, S34** — untouched or carried as typed.

**No undecidable is rescued by reaching for an axiom, and no open is closed by this audit.** The
verdict is about *consumption*, not about existence: O1/O2/O4 remain exactly as open as before.

`FLATTENING_CHECK = clean (37/37 rows walked; S12 live and discharged).`

## 6. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+4).**

**(1) FOUR OF THE SIX CONSUMER CLASSES ARE MY OWN LANE'S ARTIFACTS.** The recognition prereg, the
D_BR tower contest, the B2 corpus verify, and 799/800 are all DARIO. The audit's central negative —
"these consumers do not mention the source-infinite sector" — is therefore substantially a statement
about **my own writing**. If my lane has a blind spot about the source sector, this audit would
reproduce it rather than detect it. The lexical instrument mitigates but does not remove that: a
consistent blind spot produces consistent zeros. **This is the weakest structural feature of the
result and it cannot be repaired from inside my lane.**

**(2) I RAN THE LEXICAL DECIDER FIRST PRECISELY BECAUSE MY PROBES HAVE BEEN THE WEAK PART.** Five
consecutive relays have turned on an instrument failure of mine — probe width at 809, sidecar
spelling at 792, basis extraction at 804, signal-density bias at 820, variance vocabulary at 820's
post-seal. Leading with a mechanical count rather than a reading is a deliberate correction. It is
also an admission: I no longer trust my reading-first instinct on corpus negatives.

**(3) THE DELEGATED AUDIT RETURNED NOTHING BEFORE I SEALED.** I commissioned a six-agent parallel
consumer audit; **zero agents had reported at seal time**, and every finding above is my own,
verified against bytes. The artifact does not depend on it. If it later contradicts anything here I
will report that against this artifact rather than quietly absorb it.

**(4) I DID NOT DELIVER A CLEAN SWEEP AND I DECLINE TO ROUND ONE UP.** Two ambiguities survive and
both cut against the tidy answer: the dressed-map disjunct is a live falsifier route even though
nothing consumes its object (§1.4), and the physical Thomson-response chain does list source-inclusive
completion among its needs while the plan types only the *adjacent* R-L2b campaign as off the value
path (§1.6). Resolving either by fiat would have produced a cleaner verdict and violated the relay's
own instruction that the sentences decide.

---

```
CONSUMERS = 18 audited (spans) across 6 named classes
  13 junction prerequisites (STAGE8_C1_COMMON_CELL_POSED_CODEX2_V001.md fa4909a4611f2d09, §3.2 span
  [19653,21917) sha 982ee4eea0d6aac9, relay 816 / Q-723), plus the recognition specification, the
  D_BR family evaluation, E4c-as-C1-carries-it, the B2 diagnostic, and the F-chain's F3a.
  INSTRUMENT DISCLOSED: a 16-term lexical decider run BEFORE any reading — five of six consumer
  classes return ZERO source-infinite terms (recognition 0/48484 chars; D_BR 0/22367; B2 0 and 0;
  F3a 0).  Only the junction checklist has hits (8), and all 8 are read in place below.

TYPES = 0 INFINITE-REQUIRED (none found) / 17 FINITE-SUFFICIENT or NO-SOURCE-MENTION / 1 AMBIGUOUS (displayed)
  9 NO-SOURCE-MENTION: junction items 1-5 (the five map objects — a different axis), recognition,
    D_BR, B2, F3a.
  8 FINITE-SUFFICIENT: junction items 6-12 (cell-local: "on a named oriented cell", "the same cell",
    "on e") and item 13, which asks for the NARROWED cofinal state agreement + E4a + PINNING the
    still-live dressed-map disjunct and REPORTING U2 sufficiency as ungranted.  Reporting an open is
    not consuming its object.
  1 AMBIGUOUS, both readings displayed, neither picked: E4c as C1 must carry it.  (A) what C1 carries
    is a correctly typed REPORT — "a MONOMORPHISM on completed records, not a UNITARY on the
    infinite-future source-inclusive limit" — which C1 can carry today with no infinite object in
    hand.  (B) E4 negates a DISJUNCTIVE falsifier, so a live disjunct is a live falsification route:
    the object is never consumed, but its non-existence could still kill the result.
  0 INFINITE-REQUIRED.  Adversarial sweep run: 86 requirement-language sentences across 46 files
    pairing with source-infinite terms; read in place they are scope boundaries reporting the objects
    open, or FAILED-ATTEMPT POST-MORTEMS listing what a dead route would have needed — the strongest
    candidate, TASK4A [85,1065) sha c0e90ace7af7fcf6, opens "The proof attempt FAILS at the physical
    source completion."  No live consumer on the junction's path holds a claim on the objects.
  THE DECISIVE POSITIVE: the junction states its OWN consumption rule, [22171,…) sha 9733c6a930ca —
    "E4b may be consumed only in D16's narrowed form … equality after common finite causal support
    and its buffer.  The stronger adjacent-swap/prefix statement is not used."  It does not merely
    fail to require the infinite form; it declares it consumes only the finite one.

VERDICT = O1/O2 (and O4) leave the critical path to JUNCTION RUNNABILITY
  Supported by the record's own architecture.  R3 draws the line itself, [3729,4025) sha
  1cede082dad45162: "For every finite set of causal cells, compact support makes the Moller maps
  FINITE propagator expressions.  The output record states are restriction compatible and define a
  quasi-local record state.  For the FULL SOURCE-INCLUSIVE ALGEBRA, however, later cells continue to
  act on the shared source."  The record side needs only finite Moller maps; the infinite objects are
  for the source-inclusive algebra, and nothing on the junction's path consumes that algebra.
  TWO THINGS KEEP THIS FROM BEING A CLEAN SWEEP, both stated rather than rounded away:
   (a) the record side DOES consume one source asymptotic — durability is "thresholded source
       nonreturn AND exact completed-record persistence" — but it is ALREADY DERIVED, from
       Riemann-Lebesgue on an absolutely continuous free-tail spectral density, NOT from O1/O2/O4.
   (b) RUNNABILITY IS NOT A PHYSICAL VERDICT.  The checklist's closing sentence lists E4c among what
       the 13 do not achieve, but does not say a verdict REQUIRES closing it.  Displayed, not resolved.
   (c) and the second ambiguity: the physical retarded-response -> Thomson-limit chain DOES list
       "source-inclusive continuum/projective completion" among its needs.  THE PLAN V003/V004 types
       "the R-L2b/linked-cluster campaign is OFF alpha's value path" — but it types THAT campaign,
       not the Thomson chain, and identifies neither with the other.  If they are the same, the last
       consumer is off the value path; if not, that chain is the true holder of the source question.
       No sealed sentence settles it and I decline to supply one.

REPRICED = the critical path to runnability IS the five map objects, exactly; O1/O2/O4 come off it
  CRITICAL PATH = checklist items 1-5 (grammar/no-outside; f_g; F_g; eta_resp; U_Omega) + items 6-12
    cell-local replays on a materialized cell e + item 13 pinning evidence that already exists.
  OFF IT = O1, O2, O4 — still NAMED OPENS of the completion, consumed by nothing audited here.
  This converges with the registrar's remainder map ("10 interface elements already sealed + these
  five objects + nothing else") AND EXPLAINS WHY that count is right: the 13 items reduce to the five
  objects plus mechanical replays.
  WHAT IT CHANGES: my 820 priced O1/O2/O4 as though they sat on the road to the junction; they do not,
  so their 4-8 and not-estimable figures are off the runnability path.  WHAT IT DOES NOT CHANGE: the
  five objects' statuses are untouched (OBJECT 1 fenced at T5; OBJECTS 2/3 undecidable with S26
  barring one door; OBJECT 4 constrained-not-supplied; OBJECT 5 undecidable, lemma named).  NOTHING
  HERE MAKES THE PROGRAM CHEAPER OVERALL — it moves three items off one road, discharges none of
  them, and does not touch what actually blocks the junction.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4)
  (1) FOUR OF THE SIX CONSUMER CLASSES ARE MY OWN LANE'S ARTIFACTS (recognition prereg, D_BR tower
      contest, B2 corpus verify, 799/800).  The central negative is substantially a statement about MY
      OWN WRITING, and a consistent lane blind spot about the source sector would produce consistent
      zeros rather than be detected by them.  THE WEAKEST STRUCTURAL FEATURE OF THIS RESULT, AND IT
      CANNOT BE REPAIRED FROM INSIDE MY LANE.
  (2) I RAN A MECHANICAL LEXICAL DECIDER BEFORE ANY READING, deliberately, because five consecutive
      relays have turned on an instrument failure of mine.  That is a correction and also an
      admission: I no longer trust my reading-first instinct on corpus negatives.
  (3) THE DELEGATED SIX-AGENT AUDIT RETURNED ZERO RESULTS BEFORE I SEALED.  Every finding here is my
      own and byte-verified; the artifact does not depend on it.  If it later contradicts anything
      here I will report that against this artifact rather than absorb it quietly.
  (4) I DID NOT DELIVER A CLEAN SWEEP AND DECLINE TO ROUND ONE UP.  Two ambiguities survive and both
      cut against the tidy answer.  Resolving either by fiat would have produced a cleaner verdict and
      broken the relay's own instruction that the sentences decide.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
