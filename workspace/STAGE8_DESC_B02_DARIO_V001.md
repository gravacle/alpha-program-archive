CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; the closure block is the first content)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep) -- CLAIMED, not clean (Q-921)
VERDICT_BEARING_SET = exactly the 18 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
PATH_RULE = every closure member carries its FULL path from the alpha-program-archive root and is
  rehashed at that path before sealing (Q-913 standard).
LANE = DARIO   ROLE_THIS_RELAY = BUILD B02 AND NOTHING BEYOND IT
DIGEST_RULE = every pinned digest and every pinned byte offset COMPUTED by member 18 from its file at
  seal time.  No digest and no offset in this artifact is typed by hand.
ALL_RESULTS = CLAIMED until the opposite-lane check.
CLOSURE_END_BYTE = 4689
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_1005_B02_BUILD_DARIO_V001.md` | `da873b3ba0d6bcadb3e10abc2a670cddafa654c023b34a4c16e55d5f849eea28` | assignment |
| 02 | `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 03 | `supervision/LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law; LAW 10 at `[35883,…)` |
| 04 | `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 05 | `supervision/QUESTIONS_SETTLED_REGISTER_V001.md` | `c768923e34ee57aaf31ff7c76f9c30c33fb1829395cf241055a0282aae584558` | the register, live-append; digest at MY seal time, later than the audit's snapshot |
| 06 | `supervision/DECISION_OF_RECORD_013_GEN_OMEGA_RATIFIED_FAMILY_LEVEL_2026-08-02_V001.md` | `f2a7838d41b7b7df42ae92bc3d59399ee04bbec65cecadc8a43f6a36ac4756ec` | **the ratified origin, and the clause that decides this relay** |
| 07 | `workspace/STAGE8_DESC_AUDIT_CODEX2_V002.md` | `078d6d54a2590f1caed4cea0245508981bd98e07adcef7f5b5df62d8419f838d` | **the supply map; the B02 row; the stop rule** |
| 08 | `workspace/STAGE8_DESC_DEMAND_DARIO_V006.md` | `33d8c0d4abf16b2ba3dd75ff77776a27b1bb82968aa24e753ca4ddb163932aba` | the demand; §2 lists which of its 36 bind |
| 09 | `workspace/STAGE8_DESC_B01_DARIO_V002.md` | `19bf16f7ec1dda3e4c039e57b99a2896db4e9074ff1904e0e52096e2e90cf01b` | B01, the inventory this constructor would consume, and its forward obligation |
| 10 | `workspace/STAGE8_AXN_B0_ACCEPTANCE_INVERSE_CODEX2_V001.md` | `ae93720f5f5534a8d0d9915ab84368639f3fd96927b0d51a206a35c6b40b6019` | A01–A05, the receiver bar |
| 11 | `workspace/STAGE8_CTP_PHYS_INPUT_PACKAGE_B0_LOAD_BEARING_STOP_SPEC_V001.md` | `ec848d293d0d824150c21b252452ae46732705cf972c857c2dc02671af53878c` | the candidate tuple, the signature disclosure, the role contents, the interface, T1 |
| 12 | `workspace/STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | **subtrace A** — and the file DoR-013 names by digest `1be5f6a7…`, matching |
| 13 | `workspace/STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | the field-by-field inhabitance audit: where the joint objects are not |
| 14 | `workspace/STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md` | `a614a69409b40ab839e036d9439ac0591df64854d4275d721a624454b7213141` | the corpus census for the candidate and the signature |
| 15 | `workspace/STAGE8_TASK2D_MULTIAXIAL_STATE_TRANSITION_ENVELOPE_SPECIFICATION_V001.md` | `bda00e99e964bd75d60429549e5f6c70762bf1607270b9a6294dc4c97cf2c635` | **subtrace B** — `PathCert(Omega)` and its commuting squares |
| 16 | `workspace/STAGE8_P5_COMMON_ORIGIN_DESCENT_REQUIREMENT_V001.md` | `a33be83c1ee7cbfbda2cc3857425cb9e7e90a23bbe3d61c9ec89432e50b77874` | the P5 requirement the trace serves |
| 17 | `workspace/STAGE8_FROZEN_PRIMITIVE_INVENTORY_PREFREEZE_RESULT_V001.md` | `476baa18b1e2e97f8b8c8a349bf803627f53c47224b030f538527848f3fb8deb` | the record's own `NO_VERDICT` on exhaustiveness, blocked on this relay's object |
| 18 | `workspace/build_b02_pins_v001.py` | `7daa1b841d5aa32ad1675b732b1a7de3f7664c297581adda8a3d1bf50b3fa345` | the pin generator, sealed alongside; **its declared inputs are exactly members 01–17** |

```text
BLIND HELD.  EVERY SCALE SYMBOLIC.  NO NUMERIC EVALUATION OF PHYSICAL QUANTITIES.
NO MEMBER BOUND.  NO FIXED POINT EXECUTED.  NO END TEST.  NO COMPARISON TO A MEASURED CONSTANT.
NO SELECTOR EXERCISED, FROZEN, OR PROPOSED FOR FREEZING.  NOTHING ADOPTED.  NOTHING REGISTERED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — DESCENT SECTION — B02 BUILD — DARIO LANE — V001
## RELAY 1005 — `[PLAN:DESC-21]` — THE CONSTRUCTOR, THE SIGNATURE, THE JOINT TRACE

Date: 2026-08-11
Status: **B02 STOPS, THREE TIMES, EACH STOP NAMED AT BYTES. The candidate stops at A01's inhabitance
bar against a standing decision of record. The joint trace stops at `omega_phys`, the section's one
live selector, under stop-rule clause 3. The join between the two sealed subtraces stops at a typed
codomain the record has already ruled wrong for this receiver. The signature is delivered as a
per-field constraint reading with every field typed FORCED / OPEN / STOPPED — expressly NOT a filled
signature, because a signature filled with no object to conform to it is the vacuous pass Q-98 rules
structural to this corpus. One field, `Params_B0`, is forced at the ratified scope, and it is the
field against which B01's exhaustiveness comparison is runnable.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

---

## 1. The row, at its bytes [PROVABLE — quoted]

Member 07 at `[32292,32547)`, span SHA-256
`05d5c2ef695729973c1c81eac754acdc7fa666ffac291a94dec976cfe9b58509`:

```text
| `B02` | target-independent constructor producing one inhabited B0 candidate, complete signature/carrier/core/provenance, and frozen joint construction trace | INPUT plus MIXED certificate half | A01/A03–A05 and the common origin for every later row |
```

**The row convention is checked, not assumed.** B01 V002 pinned its own row at `[32217,32292)` with
span digest `397e300c707237e02c021fa27a501ac089c9dd39e032feddc65d78d794f727a9`. Member 18 recomputes
that span from member 07 and lands on `397e300c…` exactly, which fixes the convention — a row span
runs from its leading pipe **through its terminating newline** — and B02's row is pinned under the
same rule. Had I guessed the convention, B02's row digest would have been wrong by one byte and no
later check could have told which of us had slipped.

The row's own words govern this build, and they name six deliverables: a **constructor** that is
**target-independent**; **one inhabited B0 candidate**; a **complete signature**; **carrier**;
**core**; **provenance**; and the **frozen joint construction trace**.

### 1.1 What the audit typed these ingredients as

Member 07's §6, four rows, each pinned:

```text
[24598,24705) dea1bffb…  | 02 | inhabited complete B0 candidate | INPUT | acceptance schema
                            validates but does not create A01 |
[24705,24872) 6eec2e5f…  | 03 | target-independent B0 constructor and frozen joint trace | INPUT |
                            A03 is independent construction work; finite/subtraces do not determine
                            the full trace |
[24872,25034) 8be92352…  | 04 | complete candidate signature instance | MIXED | type/parameters/
                            carrier values require construction; conformance is mechanically
                            checkable afterward |
[25034,25169) bd0006d2…  | 05 | B0 provenance/domain certificate | MIXED | the witness/proof must be
                            supplied; replay verdict is determined once supplied |
```

Two INPUTs and the independent halves of two MIXEDs. Stop-rule clause 2 (member 07 `[30261,31690)`,
span SHA-256 `1a7e5e7ddbdd575c32db1d6f1ae6d80ae1db9e81f057985227a33b0076d2c642`) governs what happens
when one of those is absent, and clause 3 governs what happens when a SELECTOR is reached. Both fire
below.

---

## 2. Which of the demand's 36 conditions bind this object [PROVABLE]

Member 08 carries 35 + 1. The relay names the U7 group as binding directly; I list the rest honestly
rather than let a short list flatter the build.

**BINDS DIRECTLY — the U7 group, member 08 `[23582,24198)`, span SHA-256
`418b7cc9300ef608aa7d15e76e32f287a2a4d1b81dfec9cc0b0927bb91f78d5b`:**

| # | Condition | Where it bites this build |
|---:|---|---|
| 19 | An **inhabited** frozen B0-role candidate `(Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)` | §4 — STOPPED |
| 20 | A content-addressed primitive inventory frozen before construction, with its coverage boundary | the **linkage** binds: B01 V002 is the declared inventory and A03 requires the rule to run *from it*. B01 is PARTIAL, so the linkage is to a partial inventory and says so |
| 21 | A target-independent construction rule from the inventory to the candidate, **plus the frozen trace** | §5, §6 — rule OPEN at its first step, trace GAPS-DISPLAYED |
| 22 | A **filled** candidate signature with conformance witness and independent T1 oracle | §5 — PARTIAL; T1 disposition NO_VERDICT by its own route |
| 23 | A provenance/domain certificate binding the candidate to the inventory and trace, proved at actual scope | §5.6 — PARTIAL at bytes |

**ALSO BINDS, and I do not get to omit these because they are not in the named group:**

| # | Condition | Why it binds B02 |
|---:|---|---|
| 2 | An actual common-origin construction trace `Omega` — **concrete, not existential** | this *is* B02's trace deliverable; the adjective "concrete" is the whole of §6 |
| 12 | Provenance: shared primitive inputs and construction record; no post hoc import | A05's own content, re-bound at the P5 receiver |
| 14 | Target-independent admissibility, frozen before downstream response or root information exists | the row's word *target-independent*; §7 |
| 15 | One trace supplies state, effects, domains, dynamics **before any response output is inspected** — the temporal bar | the trace must be frozen, and §6's subtrace A carries its own T0–T8 order |
| 16 | No **ad hoc cross-row assembly** — the compositional bar, from common-origin provenance | **the sharpest one for this relay.** "Assemble the frozen joint trace from the sealed partial subtraces" is one wrong step away from exactly what condition 16 forbids. §6 is written to be testable against it |
| 17 | No **undeclared external** supplement to any required datum after a descendant output is inspected | the closure may not grow after the fact |

**DOES NOT BIND — named so the build is not scored against other rows' objects.** Conditions 1, 3, 4,
5 (the P5 maps, `Cert_P5`, the manifest, `d_U2`) belong to B04/B05/B13/B15; 6–11 are the certificate's
content at B15; 13 is the U2 role signature at B05/B13; 18 is K7, a downstream receiver; 24–26 are the
descendants at B05–B10; 27–29 are the right-domain maps at B11–B14; 30–33 are the closing certificate
machinery at B15; 34 is MD-3, downstream of an inhabited candidate; 35 is the StatePort placement at
B04; 36 is the coverage theorem, verdict-relative and outside the 35.

**Count: 11 of 36 bind.**

---

## 3. The receiver bar, exactly [PROVABLE — quoted]

Member 11 `[35679,36267)`, span SHA-256
`8727d110139fc0c7c79e1ee7c64f3a276ee80f7747790063b0b13af5ba25606c`:

```text
CompleteMicroscopicBoundaryOriginCandidate :=
  (
    Obj_B0,           # the candidate boundary operator/dynamics object
    Sig_B0,           # its frozen type/arity/domain/codomain/parameter record
    Carrier_B0,       # carrier/representation data, if that type requires them
    Core_B0,          # common operator core, if that type requires one
    Prov_B0           # provenance from B0_CONSTRUCTION_WITNESS
  ).
```

```text
Sig_B0 := (Type_B0,Arity_B0,Dom_B0,Cod_B0,Params_B0)
Obj_B0 conforms_to Sig_B0
```

Member 10 `[7262,10112)`, span SHA-256
`843c25b5f88255919b20587fe25fecc8b5d908974af50b5aa4af8b0094088b70`, carries A01–A05 whole. Its two
load-bearing sentences for this relay, quoted exactly:

> *"A schema, family, codomain fiber, or empty candidate slot does not pass inhabitance."*

> *"The type may be an operator, flow, family, relation, algebraic generator, or another fully
> declared mathematical type; acceptance does not demand a unique microscopic ontology."*

And A04's fill requirement, quoted exactly: the realization must *"fill—not merely quote—the sealed
schema"*, and *"No undeclared linearity, boundedness, self-adjointness, unitarity, locality,
representation, or existence property is inherited from the schema."*

### 3.1 The word `family` does two opposite jobs inside one clause [DISPLAYED, NOT ADJUDICATED]

The two sentences above are eleven lines apart in the same span. The first bars a **family** from
passing inhabitance. The second admits **family** as a declared type. Both are A01's own bytes.

There are two readings and I decline to choose between them:

- **STRICT.** The non-inhabitance list is the operative one: whatever the admitted-type list says, a
  family cannot be the inhabitant, because "the candidate is somewhere in this family" is precisely
  the empty-slot failure the clause is written against.
- **PERMISSIVE.** The two uses are distinct: the bar refutes an *indeterminate* family standing in
  for a candidate, while the admitted list allows a candidate whose declared mathematical type *is* a
  single, fully specified family object.

**Choosing is an adjudication, and adjudication is not this lane's act.** §4.2 shows why the choice
does not decide this relay: the build stops under both readings, by different routes. I record the
tension because a later lane that reads only one of the two sentences will think A01 settles something
it does not.

### 3.2 What the candidate must be usable as — the role scope, fixed

The phrase *"at the B0 role scope"* does real work in §5 and §6, so it is pinned rather than left to
the reader. Member 11 `[37523,38115)`, span SHA-256
`e16f8b9438c518546fa7eda93c724316f4470f9a8298a50ac8d275eb09d293ad`:

```text
C0 = joint carrier/algebra, representation, common domain, branch embeddings,
     and physical source maps only
U1 = physical branch/source orientation, metric, involution, compound-index
     order, source symmetry, and embeddings on C0
U2 = microscopic action/evolution, positive normalized pre-state, inclusive
     identity, admitted effects, action/source contact rules, and common domains
U3 = physical orbit/constraint map, quotient, descended contour/spacetime
     measure, boundary/edge/gluing and endpoint operator domains, and the
     predeclared contour prescription
```

Member 11 `[38121,38437)`, span SHA-256
`cb264602a0537a07948c31d2763094395c5670a5001ea8378d9e7f83b67ed72c`:

```text
d_C0 : B0_candidate -> C0
d_Ui : (B0_candidate,C0) -> Ui,  i in {1,2,3}

C0 = d_C0(B0_candidate)
Ui = d_Ui(B0_candidate,C0),  i in {1,2,3}

no required physical datum in C0 or any Ui may be supplied by an undeclared
external supplement after any descendant output is inspected
```

**These constrain the candidate's USE, not its signature**, and A04 is explicit that no property is
inherited from a schema. B02 is not asked to supply `d_C0` or any `d_Ui` — those are B11–B14. What the
interface fixes for this relay is the **scope** at which A05 must prove a declared property: the
carrier and domain that `d_C0` would consume are the **joint** ones named in the first block, which is
why a finite source-sector receipt cannot discharge a role-scope obligation.

---

## 4. The candidate: STOPPED [PROVABLE]

### 4.1 The only ratified origin object in the record, and the clause that governs it

Member 06 `[355,992)`, span SHA-256
`bc83e53db7e46f27b570b1492ebd9ea53f168c344e1b06d1b82b45bb36a00750`:

```text
- THE ANCHORED GENERATIVE FAMILY — all three convergent anchor classes (bistochasticity,
  finite-trace detailed balance, irreducible symmetry covariance) adopted AS A FAMILY.
  *** NO MEMBER IS SELECTED, EVER. *** The proven p_ch-neutrality certificate (Q-265,
  independently verified Q-266) is the license: every member forces the same normalized-
  identity invariant state and the same symbolic sector-dimension-ratio form for p_ch.
- The generative maps: d_state (rho_S), d_ready (the single forced plus-root — the
  exchanged root is REFUTED, Q-264, not chosen against), d_law (reproducing the ratified
  DoR-009 law exactly).
```

**A custody note, because it nearly cost me the reading.** Member 12 carries the header
`PROPOSED_NOT_ADOPTED — PENDING PRINCIPAL RATIFICATION (DoR-013 RESERVED)` at `[0,147)`, span SHA-256
`0319cb6d5a7970000537eaa20a42a3da1c56db5a897fc9feb9d3a6b82d7e072a`, and repeats that string at nine
further offsets inside the file. Read alone it says the origin rule is a proposal. It is not: member
06 ratifies it, names member 12 by the digest `1be5f6a7…`, and member 18 recomputes member 12's file
digest as `1be5f6a7e40c…`, matching — so the ratified object and the file carrying the pre-ratification
header are the same bytes. **The header is stale, not governing.** I record it because a lane that
stops on that header stops for a reason the record does not support, and a lane that never looks at
member 06 would build on a proposal.

### 4.2 The stop, and why it survives both readings of §3.1

**Under the STRICT reading.** The record's one ratified origin is a family. A01 bars a family from
inhabitance. There is no other inhabitant: member 14 `[11160,11437)`, span SHA-256
`be13ed252882549c1efd20dbea49912727def653789e555da73faec746d9f54f`, over the corpus census —

```text
| inhabited frozen B0-role candidate | candidate family/schema in the B0 stop spec and codomain-fiber
test | **MATERIAL-ABSENT** | `Obj_B0/Sig_B0/Carrier_B0/Core_B0/Prov_B0` has no frozen inhabitant; the
family has open type, signature, representation, maps, and equivalence |
```

Reaching an inhabitant from the ratified family requires selecting a member. Member 06 forbids that
**permanently** — not "pending", not "at the principal's convenience", but *ever* — and my own fences
forbid member binding independently. **So under the strict reading B02's candidate is not blocked by
missing work; it is blocked by a standing decision of record.**

**Under the PERMISSIVE reading.** Suppose `Type_B0 = family` passes, with the ratified family as the
inhabitant and the p_ch-neutrality certificate as the license. The candidate then still needs
`Carrier_B0`, `Core_B0`, `Prov_B0` and the joint trace at the **B0 role scope**, and member 13
`[8902,10649)`, span SHA-256 `20b0b65035f4dbf75ae72f2c62983f881a71cb8bca69fe568e9d9eaa83c7e77f`,
finds five of the required joint objects absent — `omega_phys` **UNBOUND**, `pi_omega`
**UNINSTANTIATED**, `rho_pre` **WRONG CODOMAIN**, `state_domain` **UNBOUND**, `state_descent_map`
**WRONG CODOMAIN** — with its own summary sentence: *"the joint physical state port still does not"*
exist. §6's gaps then fire unchanged.

```text
CANDIDATE = STOPPED
STOPPED_AT = A01 inhabitance for (Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0)
WARRANT_NAMED_AT_BYTES = member 06 [355,992) bc83e53d… (no member, ever)
                       + member 10 [7262,10112) 843c25b5… (a family does not pass inhabitance)
                       + member 14 [11160,11437) be13ed25… (MATERIAL-ABSENT over the census)
READING_DEPENDENCE = NONE.  The stop holds under both readings of §3.1, by different routes.
```

### 4.3 The countermodel I owe this stop [Q-98 discipline]

Q-98 rules the vacuity/name-match defect **structural to this corpus's test idiom**, and records that
what caught three bad constructions was the adversarial countermodel, not the build discipline. So I
attacked my own stop.

**Attack.** "A01 nowhere requires the candidate to descend from the ratified origin. It requires an
inhabited tuple and a rule from A02's inventory. B01 V002 has thirty-two rows, most of which are not
Gen_Omega rows. Build the candidate from those instead and the decision of record never binds."

**Result — the attack lands, and narrows the claim rather than killing it.** It is true that A01 does
not name Gen_Omega, and the strict-reading stop is therefore **scoped to construction from the
ratified origin**, not proved over all possible constructions. What the attack cannot reach: any such
alternative candidate would itself be the INPUT member 07 types absent at `[24598,24705)`, supplying
it is independent construction work that this relay's declared closure does not contain, and member
14's census reports MATERIAL-ABSENT after a four-mode hunt over 1094 sealed artifacts. **So the honest
form of the claim is the narrow one, and I state it narrowly above and in §11.** I record the attack
because a stop that has not been attacked is a stop that has not been tested.

---

## 5. The signature: PARTIAL, field by field [PROVABLE]

### 5.1 Why this is a constraint reading and not a fill

A04 demands a **filled** `Sig_B0`, and `Sig_B0` is the signature **of `Obj_B0`**. With `Obj_B0`
stopped at §4, any field I "filled" would be the signature of nothing. That is the vacuous pass Q-98
names — *"passed vacuously on an empty class and empty inventory"* — and this corpus has three dead
constructions on record from exactly that move.

So this section delivers what the receiver can actually use: **each of the five fields typed FORCED,
OPEN, or STOPPED, against the record's own bytes**. It is expressly **not** a conformance instance and
must not be carried as one.

The census already types this exactly, and I adopt its typing rather than a friendlier one. Member 14
`[11970,12200)`, span SHA-256
`0de777c7b129a9807c578d97f2c860d99c5739da98fd07de05c1ac996e3e74cc`:

```text
| complete candidate signature | exact `Sig_B0 := (Type,Arity,Dom,Cod,Params)` schema and conformance
rule | **MATERIAL-PARTIAL** | the receiving schema is sealed; no candidate values or complete
signature instance are supplied |
```

**"No candidate values" is the census's phrase, and this build does not change it.** What follows adds
what the record *forces about each field*, which the census does not enumerate — not candidate values,
which it correctly reports absent.

### 5.2 T1's own disposition, so no later lane has to guess it

Member 11 `[44213,45048)`, span SHA-256
`5681c31deff9b727bd9ac0fa538fb8df88cd762a2954eab6aad17a04f76cf5da`:

```text
test_id = B0-T1-OBJECT-DOMAIN-CONFORMANCE
applicability =
  an independently frozen Obj_B0 with Sig_B0, any applicable
  Carrier_B0/Core_B0 data, declared properties, and an independent
  signature/domain/codomain oracle
no_verdict_route =
  candidate, declared signature, or independent domain oracle missing
```

**T1 returns `NO_VERDICT` by its own `no_verdict_route`, not `FAIL`.** Stop-rule clause 5 retains a
forced `NO_VERDICT` exactly. That is the disposition of record for this signature, and it is the test's
ruling, not mine.

### 5.3 The five fields

| field | what the record forces, at bytes | type |
|---|---|---|
| `Type_B0` | A01 `[7262,10112)` admits operator / flow / family / relation / algebraic generator / other fully declared type, and declines a unique microscopic ontology; the same span bars a family from inhabitance (§3.1). Member 11 `[35679,36267)` glosses `Obj_B0` as *"the candidate boundary operator/dynamics object"* — narrower than A01's list, and a gloss rather than a clause | **OPEN, AND CONTESTED.** Not chosen here. The gloss and the admitted list pull different ways and A01's two uses of *family* pull against each other |
| `Arity_B0` | member 12 `[22842,24541)` indexes the ratified origin over `A0` and over every finite `N`; member 06 `[355,992)` adds the anchor class as a third index and forbids collapsing it | **OPEN.** The three index directions are displayed, not summed into an arity: arity is a property of `Obj_B0`, and there is no `Obj_B0` |
| `Dom_B0` | member 12 `[22842,24541)` declares finite domains for the primitive tuple. A05 `[7262,10112)`: *"Finite bounded-domain receipts do not certify later unbounded endpoint domains."* Member 13 `[8902,10649)` finds `state_domain` **UNBOUND** at the joint scope | **STOPPED at the B0 role scope.** Declared only at the ratified finite/source-sector scope, which is not the B0 domain |
| `Cod_B0` | A04 requires codomain data declared **at the type**. The ratified maps do declare codomains — `d_state → Dens_1^+(H_A)`, `d_ready → C\|r>^(⊗N)`, `d_law →` the DoR-009 `E_post` form. Member 13 types the ratified `d_state` **WRONG CODOMAIN** for this receiver | **STOPPED.** The only declared codomains are ones the record has already ruled wrong for the receiver B02 must serve |
| `Params_B0` | member 12 `[22842,24541)` enumerates: `A0`; `P_0,P_ch,E_ch,Tr_A,I_src`; `P_src` with anchor tag and `Cert_anchor_IF(P_src)`; `K_cell,iota_cell,plus_root`; `chi_n` with `n ∈ {+1,-1}`; `E_post`; the tensor/zero-extension grammar; the declared finite domains. The same span states the negative: the tuple *"contains no `rho_S`, ready ray, `U_N[a]`, `p_ch`, response, or target output"* | **FORCED AT THE RATIFIED SCOPE.** The one field whose content the record fixes — and still not a fill, because it parameterizes the ratified origin family, not an `Obj_B0` |

### 5.4 What this makes runnable — the Q-914 forward obligation

Member 09 `[18061,19891)`, span SHA-256
`78c1922d527d52ce4b2a9c63cb478d890b70c3702e65a6f4de7261328e50b93e`, carries B01's exhaustiveness
stop, and member 17 `[1763,2009)`, span SHA-256
`28bbe36695068feda14ffc167a8f2526a61b37fc81ca0ee78daf553634b79074`, carries the blocker in the
record's own words:

```text
complete_corpus_wide_primitive_inventory_exhaustiveness = NO_VERDICT |
  blocked_by: Q-95's exact `Obj_0` signature/domain/codomain is a separate
  missing input and the standing premise inventory itself disclaims an
  exhaustive premise universe
```

The obligation is to supply the signature that makes B01's comparison possible. **What §5.3 supplies
is exactly one field of it.** The comparison is runnable against `Params_B0`, whose entries are
expressed at the same granularity as B01's rows so the opposite lane can run it without re-deriving
anything. It is **not** runnable against `Type_B0`, `Arity_B0`, `Dom_B0`, or `Cod_B0`, and the reason
is the same one T1 gives: the object is missing.

**I do not run the comparison.** Builder never verifies own work, and the relay assigns it to the
opposite lane. Supplying the ground and running the test are different acts and I am doing only the
first.

```text
Q-914_FORWARD_OBLIGATION = CARRIED, NOT DISCHARGED
DISCHARGEABLE_NOW = Params_B0 only (1 of 5 fields)
BLOCKED = Type_B0, Arity_B0, Dom_B0, Cod_B0 — for T1's own no_verdict reason
```

### 5.5 `Carrier_B0` and `Core_B0` — the conditional, honoured

Member 11 types both conditionally: carrier data *"if that type requires them"*, a common operator
core *"if that type requires one"*. A01 carries the same conditional in the same words. **With `Type_B0` OPEN,
the antecedent of both conditionals is open, so neither is reachable.**

- `Carrier_B0` — **SCOPE-LIMITED, NOT REACHABLE AT THE ROLE SCOPE.** Member 06 adopts `A0`, the
  authored finite scalar source realization, so carrier data does exist at the source-sector scope.
  Member 13 finds the joint representation UNINSTANTIATED and `pi_omega` not a scalar Hilbert
  representation at the role scope.
- `Core_B0` — **NOT REACHABLE.** Its antecedent is `Type_B0`.

Recording the conditional rather than reporting two empty fields matters: an earlier lane's
cross-check found `Carrier_B0` and `Core_B0` had no census row of their own and closed that gap on
A01's conditional. Reporting them as flat absences would reopen a closed question.

### 5.6 `Prov_B0` — PARTIAL, at bytes

Member 13 `[8902,10649)`, final row, quoted exactly:

> *"provenance certificate | Gen_Omega certifies common-input descent of source state, ready ray, and
> law | **PARTIAL**: no certificate includes `omega_phys`, the C0 scalarization, joint density, and
> joint domain"*

A05 requires proof of every declared property **at its actual scope**. The four named absences are
exactly the joint-scope properties. `Prov_B0` is real at the source-sector scope and does not reach
the role scope.

```text
SIGNATURE = PARTIAL
FORCED  = Params_B0
OPEN    = Type_B0, Arity_B0
STOPPED = Dom_B0 (role scope), Cod_B0
CONDITIONALS UNREACHABLE = Carrier_B0, Core_B0 (antecedent is Type_B0)
Prov_B0 = PARTIAL (four joint-scope absences named)
T1 = NO_VERDICT by its own no_verdict_route
```

---

## 6. The frozen joint construction trace: elements pinned, gaps displayed [PROVABLE]

The relay's instruction is exact: assemble from the sealed partial subtraces, every element pinned,
**and where the subtraces genuinely do not join, display the gap at bytes rather than bridge it by
prose.** Condition 16 forbids ad hoc cross-row assembly, so a join I could not warrant would fail the
demand even if it read well.

### 6.1 The two sealed subtraces, and what each actually is

Member 07's own typing, at bytes:

```text
[12613,12986) 1414237a…  | `P1` | frozen primitive origin and three maps d_state, d_ready, d_law |
   Real at source-sector/family scope. … It does not land in the complete joint StatePort, C0/U1/U2/U3
   package, or the four B0 maps.
[12986,13287) ca0fcd70…  | `P2` | finite PathCert/common-origin subtrace and local certificates |
   Real at finite/premise scope. … classifies trace/provenance/no-post-output material PARTIAL, never
   aggregate.
```

**SUBTRACE A — the ratified origin.** Member 12 `[22842,24541)`, span SHA-256
`84a5b7050188448e2a0cc526de0131b297cb734bc96b292968e1e37080b61cf0`: the primitive tuple
`Omega_prim,N^v003`; the three maps `d_state`, `d_ready`, `d_law`; and a frozen construction order:

```text
T0 freeze A0, anchor family, certificate format and all non-output fields;
T1 verify the finite carrier/restriction certificate;
T2 verify Cert_anchor_IF before solving a fixed state;
T3 solve the unique fixed-state problem;
T4 prove the anchor theorem and obtain rho_S;
T5 apply the sealed plus-root inclusion;
T6 build the exact E_post transition law;
T7 issue common-input, covariance, domain, no-supplementation and finite checks;
T8 expose outputs only after T0-T7 are frozen.
```

**SUBTRACE B — the path-level witness.** Member 15 `[19909,21513)`, span SHA-256
`02e56051cb65a2428f86111064205f8aa137117889c1d54e669ce02bdc73915f`: the twenty-field
`PathCert(Omega)` schema, five squares that must commute on every declared finite member, and its own
status —

```text
PATH_LEVEL_PROVENANCE_WITNESS_SPECIFIED = true
PATH_LEVEL_PROVENANCE_WITNESS_INSTANTIATED = false | TYPE-U
COMMON_CARRIER_COLOCATION_EQUALS_COMMON_ORIGIN = false | TYPE-R
```

### 6.2 The joins that hold

- **J1 — internal to subtrace A.** Tuple → three maps → T0–T8 order. This joins because member 06
  adopts exactly these three maps together with the tuple; the join is a decision of record, not my
  assembly.
- **J2 — the temporal bar, condition 15.** T8 exposes outputs only after T0–T7 are frozen, so subtrace
  A satisfies the temporal bar **at its own scope**. Subtrace B states the same discipline
  independently: *"The trace is frozen before any response output; no state, effect, domain, boundary
  rule, or outgoing generator may be appended later."*

Two joins. That is all of them.

### 6.3 The gaps — DISPLAYED, NOT BRIDGED

**G1 — THE CODOMAIN GAP, where A meets B.** Subtrace B's first commuting square reads
`Res_N(d_state(Omega)) = rho_S(Omega) ⊗ ready_N(Omega)`. It therefore **presupposes a joint
`d_state(Omega)` whose restriction factorizes** — and subtrace A does not produce that map. A produces
what would be its restriction. Member 13 `[8902,10649)` types the mismatch in the record's own words:

> *"`state_descent_map` | `d_state:Omega_prim,N->Dens_1^+(H_A)` is ratified | **WRONG CODOMAIN**: it is
> not `d_state_Omega:Omega->StatePort_U2_008` or `d_joint:Omega->State(A_C0)`"*

**Composing A into B would require the joint object as an input to the step that is supposed to
produce it.** Stop-rule clause 4: a finite/premise subtrace or wrong-domain map *"cannot substitute
for the full receiving object."* **NOT BRIDGED.**

**G2 — THE SELECTOR GAP, and the one that decides §6.** Subtrace B's squares and the joint state both
run through `omega_phys`. Member 07's §6 types `omega_phys:B->C` at the open StatePort as the
section's **one SELECTOR**, and its tally line reads
`SELECTOR = omega_phys only at the current cut`. Member 13's first row:

> *"`omega_phys:B->C` | `B` is isomorphic to `C(Y)` and its entire state simplex exists |
> **UNBOUND**: no origin map lands in `State(B)`"*

Stop-rule clause 3, quoted exactly: *"If a SELECTOR is reached, require either a sealed record-native
derivation or one prospective principal freeze made before output inspection. Otherwise STOP as
SELECTOR-UNFROZEN."* There is no sealed record-native derivation, and a prospective principal freeze
is the principal's act — not a builder's, and my fences bar me from it independently.

```text
TRACE_STOP = SELECTOR-UNFROZEN at omega_phys (stop rule clause 3)
```

**This gap is reading-independent and scope-independent.** It fires whatever §3.1 is adjudicated to,
and it is the reason the frozen **joint** trace is out of reach in this relay regardless of what
happens to the candidate.

**G3 — THE LIMIT GAP.** Subtrace B's `{iota_NM}`, `d_out`, and `Cert_limit` legs have no sealed
inhabitant; the span's own flag is `PATH_LEVEL_PROVENANCE_WITNESS_INSTANTIATED = false | TYPE-U`.
Subtrace A is stated *"For every finite `N`"* and stops there. **NOT BRIDGED.**

**G4 — THE SCOPE GAP.** Member 14 records a genuine local pass — `CERT_TARGET_INDEPENDENCE = PASS` and
`CERT_NO_POST_OUTPUT_SUPPLEMENTATION = PASS` **for the finite family** — and immediately types the
aggregate MATERIAL-PARTIAL because *"T2 requires the exhaustive full-package checklist and all four
maps; aggregate T2 remains unexecuted."* A05 bars finite receipts from certifying unbounded endpoint
domains. **A real pass at the wrong scope is not a pass at this scope. NOT BRIDGED.**

**G5 — THE ANCHOR GAP.** Subtrace A's tuple carries `P_src`, and member 06 adopts the anchor **as a
family, with no member ever selected**. Any square in subtrace B that must commute *for the candidate*
rather than *for every member* has no single member at which to commute. **This gap is not of the kind
construction closes** — it is closed in the negative by a standing decision. Displayed, not bridged.

```text
TRACE = GAPS-DISPLAYED
ELEMENTS PINNED = 2 subtraces (member 12 [22842,24541) 84a5b750…; member 15 [19909,21513) 02e56051…)
JOINS HELD      = 2 (J1 internal-to-A; J2 the temporal bar, at each subtrace's own scope)
GAPS DISPLAYED  = 5 (G1 codomain; G2 selector-unfrozen; G3 limit; G4 scope; G5 anchor)
GAPS BRIDGED    = 0
```

Member 07 `[15455,16878)`, span SHA-256
`4e954fdd1bb3089360ae0abd0e94c7663f3765366a0fa5bc547c90c66c1ae131`, reaches the same verdict from the
audit side — *"the frozen joint common-origin trace required by K7 does not exist as a sealed
instantiated object"* — and this build agrees with it **at five named byte positions rather than by
citing it**, which is the only kind of agreement worth having between lanes.

---

## 7. Target-freedom: SHOWN, not claimed [PROVABLE — law 10]

**No output, root value, response, fiber, rank, ratio, or physical target was inspected at any point
in this relay.** Every gate above reads `false` and no artifact in this session evaluated a physical
quantity.

**Every choice this build made, traced to sealed ground or stopped as a freedom:**

| choice | disposition |
|---|---|
| closure membership | **DECLARED** from the relay's three named subjects plus the receivers those subjects themselves cite by name; each member listed with its full path and computed digest. No member was added after any reading |
| span boundaries | **COMPUTED**, not typed — member 18 locates each by anchor string and computes the offsets. Two independent controls: the B01 row span recomputes to B01 V002's own pin `397e300c…`, and six further spans recompute to the demand's and audit's pins |
| `Type_B0` | **STOPPED AS OPEN.** Not chosen. §3.1 displays why the record does not force it |
| the A01 `family` tension | **STOPPED AS UNADJUDICATED.** §4.2 shows the choice is not load-bearing here |
| the anchor member | **NOT APPROACHED.** Member 06 forbids selection permanently; my fences bar member binding independently |
| `omega_phys` | **NOT APPROACHED.** Not chosen, not derived, not proposed for a prospective freeze |
| the trace joins | **TWO TAKEN, FIVE REFUSED.** Each refusal named at bytes in §6.3 |

**LAW 10 — a surfaced selector is a finding, not a licence.** This build surfaced no new selector.
Two things that could be mistaken for one:

1. **The anchor member is not a live selector — it is a closed one.** Member 06 closes it in the
   negative, permanently. Recording it as a fresh supply-shaped freedom would reopen a settled act.
2. **`Type_B0` is not a selector.** Member 07's governing bright line: construction/proof inputs *"do
   not become selectors without an act expressly reserving supply"*, and no act reserves supply of the
   candidate's mathematical type. It stays construction work. **I expressly did not retype it**, which
   would have converted an honest stop into a request for a principal freeze the record has not asked
   for.

```text
TARGET_FREEDOM = SHOWN
```

---

## 8. Findings this build surfaced [PROVABLE — law 10]

**F1 — The stop is reading-independent, and that is a stronger result than the stop.** §4.2. A lane
that adjudicated A01's `family` tension first would think the adjudication decides B02. It does not:
the candidate stops under both readings and the trace stops at G2 under both. **Adjudicating §3.1 is
therefore not on B02's critical path**, though it remains on the path to whether B02 is ever buildable.

**F2 — The B01/B02 dependency is two-sided, and neither side closes now.** B01 V002 recorded that
*"exhaustive"* is signature-relative and backward-depends on B02. This build supplies one of five
signature fields. So B01's adjective waits on `Params_B0` **only if** the comparison it needs is a
parameter comparison; if it needs `Dom_B0` or `Cod_B0`, it waits on the joint objects G1–G3 name.
**Which of those B01's adjective actually needs is not mine to rule** — I record that the answer
determines whether B01's stop is one relay away or the whole descent away.

**F3 — A reason-level gap in the audit, reported and not repaired.** Member 07 `[24598,24705)` types
item 02 INPUT with the reason *"acceptance schema validates but does not create A01."* That is true,
and it names **non-forcing**. It does not name that the record's only ratified origin is adopted at
family level with member selection permanently barred — which is a different and stronger obstruction
than non-forcing, because non-forcing is discharged by construction and a standing decision is not.
**Reported, not repaired in place; supersession is the registrar's act, not mine.**

**CORRECTION PROPAGATION — downstream consumers of F3, named:** B02's own row `[32292,32547)`; the
audit's item-03 reason at `[24705,24872)`, which inherits item 02's framing; and basis row `B10` at
member 07 `[33909,…)`, which aggregates U3 *"on the B02 trace"* and therefore consumes whatever B02's
obstruction turns out to be. No other row cites item 02's reason.

---

## 9. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT   = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the B02 row and the audit's four ingredient rows at their bytes; the descent stop rule whole;
  A01-A05 whole; the stop spec's candidate tuple, signature disclosure, role contents, interface and
  T1; DoR-013's family-level adoption, its no-selection clause and its three generative maps; the
  Gen_Omega primitive tuple and T0-T8 order; PathCert's schema, squares and TYPE-U/TYPE-R flags; the
  origin producer's eight-row inhabitance audit; the B0 root census rows; the demand's eleven binding
  conditions; B01 V002's inventory, stop and forward obligation; the prefreeze NO_VERDICT; Q-95,
  Q-98, Q-913, Q-914, Q-917, Q-920, Q-921; all gates.

DERIVED HERE:
  the identification of eleven binding conditions out of thirty-six, with the twenty-five that do not
    bind named at their own rows;
  the display of A01's two opposite uses of `family` inside one span, and the two readings that
    follow;
  the result that B02's stop is INDEPENDENT of that adjudication -- both readings, both routes;
  the per-field constraint reading of Sig_B0, typed FORCED/OPEN/STOPPED, with T1's own no_verdict
    route named as the disposition of record;
  the assembly of the two sealed subtraces to the exact point where they stop joining, and the five
    gaps at their byte positions;
  the custody note that member 12's PROPOSED_NOT_ADOPTED header is stale against member 06;
  the three findings of section 8 and F3's named downstream consumers.

SELECTED HERE:
  nothing.  No candidate, constructor, signature field, carrier, core, provenance object, trace, map,
  certificate, manifest, anchor member, or omega_phys is chosen, supplied, ranked, recommended, or
  proposed for a freeze.  No basis row is reordered and no reordering is proposed.  No reading of A01
  is adopted.  No gap is bridged.  The audit is not amended.

SCALING WEIGHTS: none consumed, fixed, formed, compared, or substituted.  No rank, ratio, or physical
  quantity was read, formed, or evaluated.  Every scale symbolic.
```

---

## 10. Flattening, custody, byte audit

- **S01–S37 FLATTENING CHECK — walked, clean.** A ratified **family** was not flattened into an
  inhabitant — the whole of §4 turns on keeping them apart. A **specification** was not flattened into
  an **instance**: PathCert is specified and `INSTANTIATED = false`, and §6 carries both words. A
  **constraint reading** was not flattened into a **filled signature** — §5.1 exists to make that
  confusion impossible. A **local pass at the finite scope** was not flattened into a pass at the role
  scope (G4). A **stale header** was not flattened into a governing status, and a **decision of
  record** was not flattened into a pending proposal (§4.1). A **stop** was not flattened into a
  **shortfall**: §4.2's obstruction is a standing act, not missing work. **Non-forcing was not
  flattened into impossibility, and impossibility was not flattened into non-forcing** — F3 turns on
  exactly that distinction. An **attack that landed** was not flattened away: §4.3 narrows the claim
  rather than restating it.
- **VACUITY CHECK, run explicitly (Q-98).** No field of `Sig_B0` is reported as passing. The one
  FORCED field is typed as parameterizing the ratified origin family and **not** as conforming to an
  `Obj_B0`. T1's disposition is `NO_VERDICT`, quoted from T1's own route rather than assigned by me.
- **BUILDER-NEVER-VERIFIES:** everything here is CLAIMED until the opposite-lane check. I did not run
  B01's exhaustiveness comparison; I supplied the field it runs against and said which four it cannot.
- **BLIND held.** No rank read, no ratio formed, no fiber compared, no physical quantity evaluated.
- **PE-1..PE-17:** pointer-only, zero verdict weight. No expectation text was opened.
- **SCOPE:** B02 only. B03's `omega_phys`, B04's placement square, B05–B15 are untouched. No register,
  plan, tracker, git, commit, or push action. Nothing adopted, nothing registered.
- **BUILDER-B INDEPENDENCE:** `evaluator_build_A/` and `checks/` were not read.
- **BYTE AUDIT:** all 18 members re-hashed at their full paths at seal time; all 25 spans located by
  anchor and computed by member 18, which was **run pre-seal** with its output compared against every
  pin quoted above. Seven of the twenty-five spans recompute to digests already pinned by upstream sealed
  artifacts — the B01 row against B01 V002, and A01–A05, the role contents, the interface, Gen_Omega
  §7, the origin audit and PathCert against the demand and the audit — **7/7 MATCH**. Closure declared
  at byte 0 with its end computed on bytes as a fixed point; 27-token pre-closure scan over authored
  prose, 0 hits.
- **RESIDUE, per Q-921:** a removed-class grep was run before sealing over the vocabulary Q-913 and
  Q-917 retired. This document carries no historical subsection for that vocabulary and asserts no
  retired class. **ONE HIT IS DECLARED RATHER THAN SWEPT.** It falls at §4.1's fenced quotation of
  member 06 — one word of the third bullet of `[355,992)`, inside a decision of record's own sentence
  about the `d_law` map. It is the principal's wording, not a claim of mine, and **I will not edit a
  quotation to pass my own gate.** It is pointed at rather than restated here, because relay 1002's
  self-catch is that a report which names what it swept can reintroduce it. **The whole result is
  CLAIMED, not clean: the gate settles on the opposite lane, whose pattern set is not authored by the
  hand that wrote this prose.**
- **REGISTER SNAPSHOT:** member 05's digest is its value at my seal time and is later than the
  snapshot member 07 pins. The register is live-append; both values are on the record so the registrar
  can see the interval.

---

## 11. The certificate

Per Q-913 and Q-917, terminal: **the certificate is the digest table.** Members 01–18 above with their
full paths, and the twenty-five span digests quoted in place. There is nothing below it.

---

## 12. Final lines

```text
CLOSURE = declared-first (byte 0; closure end 4689; scan 0 hits, CLAIMED per Q-921)
B02_ROW = quoted (member 07 [32292,32547) 05d5c2ef695729973c1c81eac754acdc7fa666ffac291a94dec976cfe9b58509;
     row-span convention CHECKED by recomputing B01's [32217,32292) to B01 V002's own pin 397e300c…)
CONDITIONS = listed (11 of 36 bind: 19-23 the U7 group directly; 2 the trace; 12, 14, 15, 16, 17 the
     process bars — 16 the sharpest, since assembling subtraces is one wrong step from the ad hoc
     cross-row assembly it forbids.  The other 25 are named at their own rows and belong to B03-B15)
CANDIDATE = STOPPED (the inhabited (Obj_B0, Sig_B0, Carrier_B0, Core_B0, Prov_B0) tuple).  Warrant at
     bytes: DoR-013 [355,992) bc83e53d… adopts the record's ONE ratified origin AS A FAMILY with
     *** NO MEMBER SELECTED, EVER ***; A01 [7262,10112) 843c25b5… bars a family from inhabitance; the
     census [11160,11437) be13ed25… reports MATERIAL-ABSENT.  NOT A SHORTFALL OF EFFORT — a standing
     decision of record.  HOLDS UNDER BOTH READINGS of A01's two opposite uses of `family`, by
     different routes, so that adjudication is NOT on B02's critical path.  Attacked per Q-98: the
     attack LANDS and narrows the claim to construction from the ratified origin, which is how §4.2
     and this line state it.
SIGNATURE = PARTIAL (stops named).  FORCED: Params_B0, at the ratified scope, enumerated at member 12
     [22842,24541) 84a5b750….  OPEN: Type_B0 (A01 admits six type-classes and declines a unique
     ontology; the stop spec's gloss narrows, a gloss is not a clause), Arity_B0 (three index
     directions, not summed — arity is a property of an object that is not there).  STOPPED: Dom_B0 at
     the role scope, Cod_B0 (the only declared codomains are ones member 13 already typed WRONG for
     this receiver).  UNREACHABLE CONDITIONALS: Carrier_B0, Core_B0 — antecedent is Type_B0, and A01's
     own conditional is honoured rather than reported as two empty fields.  Prov_B0 = PARTIAL, four
     joint-scope absences named.  NOT A FILL AND NOT CARRIABLE AS ONE: a signature filled with no
     object to conform to it is the vacuous pass Q-98 rules structural to this corpus.  T1 =
     NO_VERDICT by its OWN no_verdict_route [44213,45048) 5681c31d…, retained exactly per stop-rule
     clause 5.
TRACE = GAPS-DISPLAYED (where).  2 subtraces pinned — A member 12 [22842,24541) 84a5b750… (tuple,
     d_state/d_ready/d_law, T0-T8 order); B member 15 [19909,21513) 02e56051… (PathCert, five squares,
     INSTANTIATED = false | TYPE-U).  2 JOINS HELD (internal to A; the temporal bar at each subtrace's
     own scope).  5 GAPS DISPLAYED, 0 BRIDGED: G1 CODOMAIN — B's first square presupposes a joint
     d_state(Omega) that A does not produce; A produces what would be its restriction; member 13
     [8902,10649) 20b0b650… types it WRONG CODOMAIN, so composing A into B would need the joint object
     as an input to the step that produces it.  G2 SELECTOR — the squares run through omega_phys,
     UNBOUND and the section's ONE selector; stop-rule clause 3 gives STOP as SELECTOR-UNFROZEN, and
     freezing it is the principal's act.  THIS GAP DECIDES §6 AND IS READING-INDEPENDENT.  G3 LIMIT —
     {iota_NM}, d_out, Cert_limit uninhabited; A is stated for finite N only.  G4 SCOPE — a genuine
     finite-family PASS is not a role-scope pass; aggregate T2 unexecuted.  G5 ANCHOR — no member at
     which a candidate-level square can commute, closed in the negative by DoR-013.
TARGET_FREEDOM = SHOWN.  No output, root value, response, fiber, rank, ratio, or physical target
     inspected.  Seven choices traced in a table: closure DECLARED from the relay's own named subjects
     and the receivers they cite; span boundaries COMPUTED by the sealed generator with 7/7 upstream
     pins recomputed and matched; Type_B0, the A01 tension, the anchor member, omega_phys, and five of seven joins
     all STOPPED rather than taken.  LAW 10: no NEW selector surfaced.  The anchor member is a CLOSED
     selector, not a live one, and Type_B0 was expressly NOT retyped as a selector — no act reserves
     supply of the candidate's mathematical type, so it stays construction work.
GENERATOR = SEALED-AND-RUN.  build_b02_pins_v001.py computes every file digest and locates every span
     by anchor, so no digest and no byte offset here is typed.  Its DECLARED INPUTS ARE EXACTLY
     CLOSURE MEMBERS 01-17 — it opens no other file, per the input-custody rule Q-920 installed when a
     generator was caught reading an undeclared, unsealed input.
FINDINGS = 3.  F1 the stop is reading-independent.  F2 the B01/B02 dependency is two-sided and which
     signature field B01's adjective needs decides whether its stop is one relay away or the whole
     descent away.  F3 the audit's item-02 reason names NON-FORCING where a STANDING DECISION is what
     obstructs — reported, NOT repaired in place, with downstream consumers named (the B02 row, the
     item-03 reason, and basis row B10 which aggregates on the B02 trace).
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```

All findings CLAIMED until the opposite-lane check. The step to press first is **§5.3's `Params_B0`
row** — the single field I typed FORCED. I read member 12's tuple as fixing the ratified origin's
parameter content because DoR-013 adopts that section by digest and the span states its own negative
list. A lane could hold instead that `Params_B0` is the parameter record **of `Obj_B0`**, so that with
`Obj_B0` stopped the field is OPEN like the other four and this build forces nothing at all — in which
case B01's comparison has no field to run against and the Q-914 obligation is carried whole rather
than by four-fifths. That is the sentence to check, and it is the one my own construction has the most
reason to want to be true.
