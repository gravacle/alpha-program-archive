# STAGE 8 — AXN BUILD — MATERIAL CENSUS FOR ROOT D'S FOUR FIELDS
## DARIO LANE (Builder B, verifier) — RELAY 864 — [PLAN:AXN-BUILD-A31]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

No member bound; no fixed-point execution; no end test; no smooth import; no EM identification; no
common cell formed; no junction map evaluated; no numeric evaluation of any physical quantity; no
comparison to measured constants. PE-1..PE-12 pointer-only, none opened or consulted. Builder-A code
not opened. `~/.codex` untouched; memory-bank never searched. No register, plan, tracker, or git
action.

CLAIM STATUS: **all headline items CLAIMED.**

**NOTHING IS BUILT AND NO STOP IS FILLED.** This is a census. The relay's bar — *"a census verdict is
not a license"* — is held: no measure, base family, contour, ε-family, boundary package, carrier,
operator, or domain is constructed, selected, or preferred anywhere below.

**LAW 9b IS MY OWN 862 FAILURE, LEGISLATED.** *"A positive absence over a bundle must enumerate the
bundle's pinned authority tables, not just prose tokens"* is exactly what I got wrong twice inside
860. It is the sixth standing law originating from a DARIO disclosure and **the second that exists
because I broke the previous one**. Compliance is stated in every absence line below, and §1.2
displays the enumeration.

**THE REQUIREMENTS WERE RE-READ FROM SOURCE, NOT FROM MY OWN ARTIFACT.** This census is over my own
855 stop table, so I re-read the flag block at `f97ee43e052969f0` `[1907,2702)` directly:
**span sha `8368dcbbc5d4af2f`, matching my 855 pin exactly, requirements verbatim, no divergence.**

---

## 0. LEAD

| field | requirement (verbatim) | class | verdict |
|---|---|---|---|
| **S1** | *branch-joint prequotient measure and descended physical measure* | **MATERIAL-PARTIAL** | **GATED** |
| **S2** | *interacting contour/analytic/i-epsilon prescription* | **MATERIAL-PARTIAL** (weakest) | **GATED** |
| **S3** | *complete preparation/gluing and boundary package* | **MATERIAL-PARTIAL** (richest) | **GATED** |
| **S4** | *named unbounded operators and common invariant closure domain* | **PARTIAL** on the domain conjunct / **ABSENT** on the operator conjunct | **GATED** |

**`BUILDABLE_NOW = NONE.`** Every field has real, exact, named material and every field is gated on a
*named* missing object. **Not one gate is "more work"; each is a specific object the record says is
not generated by any enumerated source.**

**The census's most useful output is that all four gaps are now type-sharp, and three of the four
were narrowed by an existing sealed attack rather than by me:**

- **S1** narrows from *"some measure"* to **a covariant projective family of history-event
  functionals**. What exists — the cylinder amplitudes — are *"the integrands/characteristic data such
  a family must reproduce; they are not the family itself."*
- **S2**'s existing material **constrains a future contour but does not construct one.**
- **S3** narrows to: *"no new finite glue is needed. The missing datum is the joint physical
  completion and its boundary structure."*
- **S4** narrows to **the operator realization itself, not another density argument.**

**Two findings the four-field stop table does not carry (§4):**

1. **`DOR_OVERLAY`: the fragility is total. All four fields' existing material is `TYPE-P` on **both**
   DoR-008 and DoR-009 — **4/4**, with S1 and S3 additionally on DoR-013/014. My 858/Q-763
   correlated-dependence finding reproduces exactly at the four-field level: **root D's entire
   existing stock stands or falls together.**
2. **S4 is not a U3-local stop.** A sealed cross-arc row types the stationarity arc's *"common
   completed domain"* and U3 field (4) as **`SHARED UNBUILT SUBOBJECT`** — *"the same missing
   unbounded-domain obligation at a broader package interface, not a built synonym."*

---

## 1. METHOD

### 1.1 Sources, all seal-verified BEFORE reading

| source | digest | seal |
|---|---|---|
| partial U3 (mine, the stop table) | `54d75ea787820b3e` | OK |
| its cross-check | `71f419acb4b0259d` | OK |
| flag-block source (four-field stop) | `f97ee43e052969f0` | OK |
| P3 second attack (the per-field attack) | `8e9a09c104f4b635` | OK |
| U3 premise-level determination | `b0118d89e0b4b321` | OK |
| R3.4 causal-diamond spectral pullback SPEC v002 | `b4cfec665d7a16e7` | OK |
| stationarity / last-wall synonym audit | `671a94a7f55a5649` | OK |
| W_gen tail-creation image theorem | `5f341414cda10013` | OK |

### 1.2 LAW 9b COMPLIANCE — pinned authority tables enumerated: **8**

Every absence line below is taken over **these tables as well as prose tokens**:

```text
1  P3 second attack 8e9a09c104f4b635      - §1.3 "Current authorities", PROSE-NAMED (0 hashes, 2 named .md)
2  P3 four-field stop f97ee43e052969f0    - 0 hashes, 3 named .md
3  U3 premise-level b0118d89e0b4b321      - 8 full hashes, 6 named .md
4  partial U3 (mine) 54d75ea787820b3e     - 10 short digests, 3 named .md
5  partial U3 crosscheck 71f419acb4b0259d - 17 full hashes, 10 named .md
6  R3_4 parent SPEC (15-authority table)  - enumerated in full at my 862
7  stationarity/last-wall audit 671a94a7  - 17 full hashes
8  W_gen tail theorem 5f341414cda10013    - 13 full hashes
```

**Disclosed under 9b's own logic:** tables 1 and 2 pin **no hashes at all** — their authorities are
prose-named. So for those two bundles the 9b enumeration reduces to a *named-file* enumeration, which
is weaker than a hash-pinned one. **I state that limit rather than reporting "8 tables enumerated"
as if all eight were equally strong.**

### 1.3 Query families run (law 9)

```text
S1  prequotient(16) coarea(18) pushforward(76) "orbit measure"(1) "quotient measure"(5)
    "descended.*measure"(26) branch-joint(13) Haar(22)
S2  i-epsilon(41) "i epsilon"(25) contour(231) "analytic continuation"(7) thimble(4)
    "Wick rotat"(18) "Keldysh contour"(2)
S3  "preparation.*gluing"(24) "boundary package"(13) "edge mode"(34) "boundary data"(91)
    "gluing variation"(19) "boundary orbit"(5) "edge variable"(18)
S4  unbounded(132) "essentially self-adjoint"(0) self-adjoint(137) "invariant domain"(15)
    "common domain"(83) "closure domain"(4) "core domain"(2) deficiency(7)
```

**The stock is not thin.** No field's verdict below rests on a failure to find vocabulary.

---

## 2. THE FOUR FIELDS

### S1 — measure — **MATERIAL-PARTIAL / GATED**

**EXISTS (named, exact):** the law-side cylinder-amplitude system

```text
OscCyl = { Fbar_N , Abar_N^[A] , j_NM^Q }
P3_LAW_SIDE_CYLINDER_AMPLITUDE_SYSTEM_EXISTS = true | TYPE-P |
  premises: DoR-008, DoR-009, DoR-013, DoR-014
```

**MISSING, at its type boundary:** *"`C7` is pullback consistency of amplitudes along the **zero
section**; it is not Kolmogorov marginal consistency of measures under a projection."* The would-build
is a base family `lambda_N` with `dnu_N = A_N d lambda_N`, plus `(pi_MN)_* nu_M = nu_N`, variation
control, descent `q_* nu`, and invariance certificates.

**GATE, byte-named:** *"**No such base family or equivalent history-event functional is generated by
the ratified law, germ, P2, or PathCert.**"* And the one named candidate is **refuted of record**:
*"Intrinsic Haar remains refuted as the required descended measure by Q-239: it bypasses prequotient
provenance and omits spacetime, contour, boundary, and representation data."*

**Why PARTIAL and not ABSENT:** the amplitudes are not a requirement restated — they are the
integrands the future family must reproduce, and they exist exactly. **LAW 9b:** the absence of a base
family is taken over all eight tables of §1.2 plus the eight S1 query families; the four-field
bundles' pin tables name no measure supplier, and the Haar candidate is present-and-refuted rather
than unenumerated.

### S2 — contour — **MATERIAL-PARTIAL (the weakest of the four) / GATED**

**EXISTS:** `EpostEndpointOrientation`, `P3_LAW_SIDE_CONTOUR_ORIENTATION_RECEIPT_EXISTS = true |
TYPE-P | premises: DoR-008, DoR-009` — DoR-009's `E_post` is a time-oriented endpoint-charge
assignment, *"charge follows the write."*

**MISSING:** an instantiated ε-indexed analytic family `K_epsilon`/`U_epsilon`, an ε-domain and
sign/insertion rule, an analytic half-domain and boundary-value map, a common invariant operator
domain, and a limit `epsilon -> 0` in a named topology.

**GATE:** *"No such epsilon-indexed object appears in DoR-009, `E_post`, the finite law, PathCert, or
P2"*, with the sealed typing

```text
EPOST_ENDPOINT_ORIENTATION_EQUALS_INTERACTING_I_EPSILON_PRESCRIPTION = false | TYPE-R
I_EPSILON_LIMIT_IN_TOPOLOGY_SRC = NO_VERDICT | prerequisite: no epsilon-indexed family exists
```

**Why this is the weakest of the four:** what exists **constrains** a future contour rather than
partially constituting one — *"It constrains a future contour but does not construct one."* And the
structure here is inverted relative to the other three: **a receiver exists with nothing to receive**
— `topology_src` is a topology on source increments, and *"without an operator-valued map
`epsilon -> K_epsilon` there is no limit to take in it."* I classify PARTIAL rather than ABSENT
because the orientation receipt is a genuine compatibility constraint any future family must satisfy,
but I record that this is the one field where a reader could defensibly argue ABSENT.

**LAW 9b:** the ε-family absence is taken over all eight tables plus the seven S2 families; the two
`Keldysh contour` files and four `thimble` files were checked and none is a U3-scope prescription
(the thimble hits are the slot-2 inventory, a census-deliverable report, and my own 857).

### S3 — boundary / edge — **MATERIAL-PARTIAL (the richest of the four) / GATED**

**EXISTS:** two exact, honest completions — `BdGlue_law_src` (P2 norm source completion) and
`BdGlue_law_rec` (PathCert outgoing **record-only** GNS direct limit) —
`P3_LAW_SIDE_BOUNDARY_GLUE_LIMIT_PAIR_EXISTS = true | TYPE-P | premises: DoR-008, DoR-009, DoR-013,
DoR-014`.

**MISSING:** a completed joint physical carrier; preparation/gluing variation; physical boundary gauge
orbit; edge reductions and boundary functionals; microcausal support; a vanishing global boundary
form; and intertwiners to both exact limits. Sealed:

```text
LAW_SIDE_LIMIT_PAIR_EQUALS_COMPLETE_CTP_BOUNDARY_PACKAGE = false | TYPE-R
```

**GATE, and it is the most precisely located of the four:** *"**no new finite glue is needed.** The
missing datum is the **joint physical completion and its boundary structure**."* S3 is gated on a
physical-carrier completion, **not** on any finite construction — so no amount of finite work advances
it.

**LAW 9b:** the absence of a completed joint carrier is taken over all eight tables plus the seven S3
families; PathCert's own certificate lists physical restriction, joint effects, joint
completed/outgoing nodes, effects and domains among its **absent** full fields, with
`ScopeCeiling = record_only_outgoing`.

### S4 — unbounded operators and common invariant closure domain — **PARTIAL on one conjunct, ABSENT on the other / GATED**

The requirement is a **conjunction**, and the two conjuncts classify differently. I split them rather
than averaging.

**Conjunct (i), the common invariant closure domain — MATERIAL-PARTIAL.** `Dom_bdd` and `GNS_out`
exist (`P3_OUTGOING_RECORD_BOUNDED_DOMAIN_EXISTS = true | TYPE-P | premises: DoR-008, DoR-009`), and
the finite PathCert vectors are dense in `H_out`. That is the carrier on which a future core would be
certified — genuine partial material.

**Conjunct (ii), the named unbounded operators — MATERIAL-ABSENT.** *"`GNS_out` represents the
outgoing record algebra by **bounded** operators. **No named unbounded physical endpoint operator
descends to it.**"* Sealed:

```text
PATHCERT_NAMES_UNBOUNDED_PHYSICAL_ENDPOINT_OPERATOR = false | TYPE-R |
  test: ScopeCeiling = record_only_outgoing; d_domain listed among absent fields
PURE_PHASE_IMPLIES_ESSENTIAL_SELF_ADJOINTNESS_OF_FUTURE_OPERATOR = false | TYPE-R
ESSENTIAL_SELF_ADJOINTNESS_PROVED = NO_VERDICT |
  prerequisite: no named symmetric unbounded operator/domain pair exists
```

**My independent corroboration:** `essentially self-adjoint` occurs in **zero** workspace files. That
is not evidence of an oversight — it is what one expects when the operator whose self-adjointness
would be at issue **does not exist**. Density alone *"does not prove that they are invariant under,
form a core for, or consist of analytic vectors for an uninstantiated operator."*

**GATE:** *"The narrowed domain stop is the **operator realization itself**, not another density
argument."*

**LAW 9b, and it changed my answer.** I did not stop at the P3 attack's word. I ran
`unbounded + endpoint + operator` across the workspace — **39 files** — and read the strongest. Two
results that a prose-token sweep would have missed:

1. **The unbounded object is *specified* of record, with a named convergence mode.**
   `R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_SPEC_V002.md` (`b4cfec665d7a16e7`, seal OK) lists under
   *"Layer P: physical provenance"*: *"**strong-resolvent convergence to the unbounded continuum
   generator**, or an independently derived continuum parent"* and *"its **self-adjoint
   outgoing-sector domain**"*. Its v001→v002 defect list opens with *"it did not distinguish a bounded
   graph-incidence limit from an unbounded positive-energy continuum generator."*
   **So S4's missing operator has a named target and a named convergence mode — a requirement, not a
   build, so the ABSENT classification stands, but the target is sharper than "an operator".**
2. **Independent confirmation from a third arc.** `5f341414cda10013` (seal OK) states that the built
   P3 law-side subpackage *"supplies no projective measure, analytic boundary-value limit, joint
   completed boundary carrier, or unbounded closure"* — **all four stops confirmed from outside the
   U3 arc.**

---

## 3. VERDICTS — `BUILDABLE_NOW = NONE`

| field | gated on | is the gate finite work? |
|---|---|---|
| S1 | a base family `lambda_N` / covariant projective family of history-event functionals | **No** — not generated by ratified law, germ, P2 or PathCert; Haar refuted (Q-239) |
| S2 | an ε-indexed analytic operator family | **No** — absent from DoR-009, `E_post`, the finite law, PathCert, P2 |
| S3 | the joint physical completion and its boundary structure | **No, expressly** — *"no new finite glue is needed"* |
| S4 | the operator realization itself | **No** — *"not another density argument"* |

**Not one field is gated on effort; each is gated on a named object the record says no enumerated
source generates.** A `BUILDABLE-NOW` verdict on any of them would require supplying one of those
objects, which is exactly what the relay forbids and what the void condition names.

---

## 4. THE TWO OVERLAY FINDINGS

### 4.1 `DOR_OVERLAY` — shared fragility is **4/4**

| field | existing material | premise dependence |
|---|---|---|
| S1 | `OscCyl` cylinder amplitudes | DoR-008, DoR-009, **DoR-013, DoR-014** |
| S2 | `EpostEndpointOrientation` | DoR-008, DoR-009 |
| S3 | `BdGlue_law_src`, `BdGlue_law_rec` | DoR-008, DoR-009, **DoR-013, DoR-014** |
| S4 | `Dom_bdd`, `GNS_out` | DoR-008, DoR-009 |
| *(the already-discharged trace)* | `P3_COMMON_ORIGIN_PROVENANCE_PORT` | **DoR-013, DoR-014 only** |

**Every one of the four fields' existing material is `TYPE-P` on both DoR-008 and DoR-009 — 4/4.**
Two of four additionally carry DoR-013/014.

**This is my 858/Q-763 correlated-dependence finding reproduced one level down.** There it was the
thirteen PARTIAL B0 rows; here it is the four fields of root D. **Root D's entire existing stock
stands or falls together on {DoR-008, DoR-009}**, and the four-field stop table does not disclose it,
because a per-field table structurally cannot.

**One asymmetry worth the registrar's attention:** the **discharged** trace depends on DoR-013/014
*only*, while all four **undischarged** fields depend on DoR-008/009. The premises carrying the part
that is finished are **not** the premises carrying the parts that are not.

### 4.2 S4 is a shared subobject with another arc — of record

`671a94a7f55a5649` (seal OK, 17-row pin table) carries:

> *common completed domain | Q-278 bounded domains, P2 closures, Q-239 field (4) |
> **DISTINCT-FROM-BUILT; SHARED UNBUILT SUBOBJECT WITH Q-239(4)** | lower-scope domains do not
> discharge it; Q-239(4) is the same missing unbounded-domain obligation at a broader package
> interface, not a built synonym.*

**Q-239 field (4) is S4.** So S4 is not a U3-local stop: the stationarity arc's *"common completed
domain"* and U3's fourth field are **the same missing subobject at two interfaces**. Consequences,
both directions: building S4 would discharge a shared obligation, **and** S4 inherits whatever blocks
that obligation elsewhere. The row also forecloses the cheap move — *"lower-scope domains do not
discharge it"* — which is exactly the `Dom_bdd`/`GNS_out` material of conjunct (i).

---

## 5. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the four requirements VERBATIM from the flag-block source (span sha
  8368dcbbc5d4af2f, re-read from source rather than from my own 855); every cited flag at its sealed
  type (TYPE-P / TYPE-U / TYPE-R / NO_VERDICT); DoR-008/009/013/014 CARRIED AS PREMISES AND NOT
  DISCHARGED; the P3 law-side subpackage at its sealed scope; Q-239's Haar refutation carried as a
  refutation; the discharged common-origin trace carried as discharged, on ITS OWN premises.

DERIVED HERE: (a) four per-field exists/missing splits with the gate named for each; (b) the
  conjunct split in S4 and the ABSENT classification of its operator half; (c) the corpus-wide
  "essentially self-adjoint" = 0 corroboration; (d) the Layer-P specification of the unbounded
  continuum generator with strong-resolvent convergence as its named mode; (e) the 4/4 DoR overlay
  and the discharged-versus-undischarged premise asymmetry; (f) the cross-arc shared-subobject
  reading of S4; (g) the law-9b enumeration, including the disclosure that two of the eight tables
  pin no hashes.

SELECTED HERE: NOTHING. No measure, base family, complex measure, contour, epsilon-family, insertion
  rule, boundary package, joint carrier, edge functional, operator, core, or domain is constructed,
  proposed, preferred, or adopted. NO STOP IS FILLED. NO FLAG MOVES. Root D is not advanced; it is
  only mapped.

NOT DONE AND DISCLOSED: I did not re-verify the P3 attack's own four type-boundary arguments from
  their upstream sources — I checked that they are sealed, self-consistent, and corroborated from a
  third arc (5f341414cda10013), which is weaker than re-deriving each. S2's PARTIAL is the one class
  I would call contestable and I say so in its own line. My S4 hunt read the strongest of 39 files,
  not all 39. The two hash-free pin tables limit 9b's strength for those two bundles.

SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at all four fields simultaneously**, which is why this census is
the most exposed artifact I have written: four named, type-sharp gaps sit here with their would-builds
spelled out, and **writing any one of them is a short step from a document that already describes it
precisely.** Nothing is written. In particular the S1 base family `lambda_N` — the single object that
would move the field — is named, its constraints are displayed, and **it is not supplied**.
**S12** — every flag carried as the status it is; `NO_VERDICT` carried as a refusal; `TYPE-P` never
silently promoted.
**S26 / S08 / S19 / S24** untouched — no measure, regularity class, contour, or domain is imported
from any smooth or clustering source; the Haar candidate is carried **as refuted**.
**T1 / T5** untouched.
**BR-1 HELD.** The partial U3 stop table is **my own** producer-declared object: it may accuse and
never exculpate. I re-read the requirements from the flag-block source rather than from my artifact,
and every class below rests on the underlying sources, not on my 855's restatement of them.

---

## 6. FINAL LINES

```text
S1 = MATERIAL-PARTIAL / GATED.  EXISTS: the law-side cylinder-amplitude system OscCyl =
  {Fbar_N, Abar_N^[A], j_NM^Q}, TYPE-P on DoR-008/009/013/014, exact.  MISSING at its TYPE BOUNDARY:
  C7 is pullback consistency along the ZERO SECTION, NOT Kolmogorov marginal consistency under a
  projection — the law supplies a contravariantly compatible cylinder-amplitude family, not a
  branch-joint prequotient measure.  GATED ON a base family lambda_N with dnu_N = A_N d lambda_N (a
  COVARIANT PROJECTIVE FAMILY OF HISTORY-EVENT FUNCTIONALS), which "is NOT generated by the ratified
  law, germ, P2, or PathCert"; the one named candidate, intrinsic Haar, is REFUTED OF RECORD by Q-239
  for bypassing prequotient provenance.  PARTIAL not ABSENT because the amplitudes are the INTEGRANDS
  the future family must reproduce, and they exist exactly.
S2 = MATERIAL-PARTIAL (THE WEAKEST OF THE FOUR) / GATED.  EXISTS: EpostEndpointOrientation, TYPE-P on
  DoR-008/009 — a time-oriented endpoint-charge assignment, "charge follows the write".  MISSING: an
  instantiated epsilon-indexed analytic family K_epsilon/U_epsilon, epsilon-domain and sign/insertion
  rule, analytic half-domain and boundary-value map, common invariant operator domain, and a limit in
  a named topology.  GATED ON that family: "No such epsilon-indexed object appears in DoR-009, E_post,
  the finite law, PathCert, or P2", with I_EPSILON_LIMIT_IN_TOPOLOGY_SRC = NO_VERDICT | prerequisite:
  no epsilon-indexed family exists.  WEAKEST BECAUSE WHAT EXISTS CONSTRAINS RATHER THAN CONSTITUTES —
  "it constrains a future contour but does not construct one" — and the structure is INVERTED
  relative to the other three: A RECEIVER EXISTS WITH NOTHING TO RECEIVE (topology_src is a topology
  on source increments).  THIS IS THE ONE FIELD WHERE A READER COULD DEFENSIBLY ARGUE ABSENT AND I
  SAY SO.
S3 = MATERIAL-PARTIAL (THE RICHEST OF THE FOUR) / GATED.  EXISTS: TWO exact honest completions,
  BdGlue_law_src (P2 norm source completion) and BdGlue_law_rec (PathCert outgoing RECORD-ONLY GNS
  direct limit), TYPE-P on all four DoRs.  MISSING: completed joint physical carrier, preparation/
  gluing variation, physical boundary gauge orbit, edge reductions and boundary functionals,
  microcausal support, vanishing global boundary form, and intertwiners to both exact limits;
  LAW_SIDE_LIMIT_PAIR_EQUALS_COMPLETE_CTP_BOUNDARY_PACKAGE = false | TYPE-R.  GATED ON the joint
  physical completion — AND THIS IS THE MOST PRECISELY LOCATED GATE OF THE FOUR: "NO NEW FINITE GLUE
  IS NEEDED.  The missing datum is the joint physical completion and its boundary structure."  NO
  AMOUNT OF FINITE WORK ADVANCES S3.
S4 = MATERIAL-PARTIAL on the DOMAIN conjunct / MATERIAL-ABSENT on the OPERATOR conjunct / GATED.  The
  requirement is a CONJUNCTION and I split it rather than averaging.  (i) COMMON INVARIANT CLOSURE
  DOMAIN — PARTIAL: Dom_bdd and GNS_out exist (TYPE-P, DoR-008/009) with finite PathCert vectors
  dense in H_out; that is the carrier a future core would be certified on.  (ii) NAMED UNBOUNDED
  OPERATORS — ABSENT: "GNS_out represents the outgoing record algebra by BOUNDED operators.  NO NAMED
  UNBOUNDED PHYSICAL ENDPOINT OPERATOR DESCENDS TO IT", with PATHCERT_NAMES_UNBOUNDED_PHYSICAL_
  ENDPOINT_OPERATOR = false | TYPE-R (ScopeCeiling = record_only_outgoing) and
  ESSENTIAL_SELF_ADJOINTNESS_PROVED = NO_VERDICT | prerequisite: no named symmetric unbounded
  operator/domain pair exists.  MY INDEPENDENT CORROBORATION: "essentially self-adjoint" occurs in
  ZERO workspace files — what one expects when the operator whose self-adjointness would be at issue
  DOES NOT EXIST.  GATED ON "THE OPERATOR REALIZATION ITSELF, NOT ANOTHER DENSITY ARGUMENT".
  LAW 9b CHANGED MY ANSWER HERE: I did not stop at the P3 attack's word but swept 39 files, and found
  that THE MISSING OBJECT IS SPECIFIED OF RECORD — R3_4_CAUSAL_DIAMOND_SPECTRAL_PULLBACK_SPEC_V002
  (b4cfec665d7a16e7, seal OK) Layer P requires "STRONG-RESOLVENT CONVERGENCE TO THE UNBOUNDED
  CONTINUUM GENERATOR" and "its SELF-ADJOINT OUTGOING-SECTOR DOMAIN", and its v001->v002 defect list
  opens with failing to distinguish a bounded graph-incidence limit from an unbounded positive-energy
  continuum generator.  A REQUIREMENT, NOT A BUILD — so ABSENT stands — BUT THE TARGET IS SHARPER
  THAN "AN OPERATOR".
DOR_OVERLAY = displayed (shared-fragility count: 4/4).  ALL FOUR fields' existing material is TYPE-P
  on BOTH DoR-008 AND DoR-009; S1 and S3 additionally carry DoR-013/014.  ROOT D'S ENTIRE EXISTING
  STOCK STANDS OR FALLS TOGETHER, and the four-field stop table does not disclose it because a
  per-field table structurally cannot.  This is my 858/Q-763 correlated-dependence finding REPRODUCED
  ONE LEVEL DOWN — there the thirteen PARTIAL B0 rows, here the four fields of root D.  ONE ASYMMETRY
  FOR THE REGISTRAR: the DISCHARGED common-origin trace depends on DoR-013/014 ONLY, while all four
  UNDISCHARGED fields depend on DoR-008/009 — THE PREMISES CARRYING THE FINISHED PART ARE NOT THE
  PREMISES CARRYING THE UNFINISHED PARTS.  SECOND OVERLAY FINDING: S4 IS NOT A U3-LOCAL STOP — the
  stationarity/last-wall audit (671a94a7f55a5649, seal OK) types the stationarity arc's "common
  completed domain" and Q-239 field (4) as a SHARED UNBUILT SUBOBJECT, "the same missing
  unbounded-domain obligation at a broader package interface, not a built synonym", and forecloses
  the cheap move: "lower-scope domains do not discharge it".
BUILDABLE_NOW = NONE.  Every field has real, exact, named material and every field is gated on a
  NAMED missing object.  NOT ONE GATE IS "MORE WORK": S1 on a base family no enumerated source
  generates; S2 on an epsilon-indexed family absent from all five named sources; S3 expressly NOT on
  finite work; S4 on an operator realization.  A BUILDABLE-NOW verdict would require supplying one of
  those objects, which the relay forbids and the void condition names.  NOTHING IS BUILT AND NO STOP
  IS FILLED.
LAW9B = complied (tables enumerated: 8).  P3 second attack; P3 four-field stop; U3 premise-level;
  partial U3 (mine); partial U3 crosscheck; the R3_4 parent SPEC's 15-authority table (enumerated in
  full at my 862); the stationarity/last-wall audit (17 rows); the W_gen tail theorem (13 rows).
  DISCLOSED UNDER 9b'S OWN LOGIC: TABLES 1 AND 2 PIN NO HASHES AT ALL — their authorities are
  prose-named — so for those two bundles the enumeration reduces to a NAMED-FILE enumeration, which
  is WEAKER than a hash-pinned one.  I state that limit rather than reporting "8 tables" as if all
  eight were equally strong.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) LAW 9b EXISTS BECAUSE I BROKE LAW 9, AND IT CHANGED THIS ARTIFACT'S CONTENT.  Complying with it
      at S4 is what surfaced the Layer-P specification of the unbounded continuum generator and the
      cross-arc shared-subobject row — NEITHER reachable from the four-field bundles' prose.  The law
      I earned by failing is the reason two of this census's findings exist.  I record that without
      treating it as credit: the correct account is that my 860 method would have missed them.
  (2) THIS CENSUS IS OVER MY OWN 855 STOP TABLE, and a mis-transcription there would have propagated
      silently into every class above.  I re-read the requirements from the flag-block source and the
      span sha matched my 855 pin exactly — BUT THAT CHECK ONLY WORKS BECAUSE I PINNED A SPAN AT 855;
      had I paraphrased, nothing here would have caught it.
  (3) S2's PARTIAL IS THE ONE CLASS I WOULD CALL CONTESTABLE.  What exists constrains rather than
      constitutes, and the census's own ABSENT definition ("only requirements, negatives, or
      unrelated objects") arguably reaches a pure constraint.  I classify PARTIAL because the
      orientation receipt is a real compatibility condition any future family must satisfy, and I
      flag it as the row most worth an opposite-lane attack — the same disclosure I made for PR-3 at
      858, which the opposite lane then confirmed.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** Charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-12
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
