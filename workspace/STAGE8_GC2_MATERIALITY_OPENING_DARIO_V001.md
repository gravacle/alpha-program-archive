# STAGE 8 / 7A — Gc2: MATERIALITY'S TWO SEALED SPEC GAPS, GIVEN THE STANDARD TREATMENT
## DARIO LANE (Builder B, independent verifier) — V001

RELAY 815. Lane guard PASS (header names DARIO). Inbox
`RELAY_PASTE_815_MATERIALITY_OPENING_DARIO_V001.md`
= `82a7fb2b3ed6dd142a1718e9254d976d9eb943d430bc1b626d5500255720281d`, seal verified
BEFORE reading. State-brief pinning: `supervision/PROGRAM_STATE_BRIEF_V005.md`
= `e26f0d16055f3e83…`, seal OK, read before task work.

GATES DECLARED AND HELD: `alpha_computed = false`; `proof_authorized = false`;
`kappa_record_computed = false`. Nothing authored; no response realized; no member
bound; no smooth import; no EM identification; no fixed-point execution; no end test;
no numeric evaluation of physical quantities; no comparison to measured constants; no
common cell formed; no junction map evaluated. PE-1..PE-10 pointer-only — recorded,
not opened, not consulted. Builder-B independence held: no `evaluator_build_A/` or
`checks/` file read. No register, plan, tracker, git action.

**ALL HEADLINE ITEMS ARE CLAIMED.** Two of them correct my own prior sealed work and
are marked as such. One classification question is MARKED RULING-SHAPED and left
unrouted (§3.4).

---

## 0. CUSTODY — INPUTS VERIFIED BEFORE READING

Law 8 applied to every seal (`<name>.md.seal.sha256`, `<name>.seal.sha256`, and
packet-manifest membership all probed before any UNSEALED call).

| Object | SHA-256 (16) | Seal |
|---|---|---|
| `review_packets/STAGE7_QSPEC_CANDIDATE_V001/…_V011.md` (78,794 B) | `aa7c6d4904706276` | packet-manifest member |
| `workspace/STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md` (739, mine) | `660e0c1464aee4f3` | OK |
| `workspace/STAGE8_T7_COMPLETED_…_SCHEMA_SPEC_V001.md` | `4fe8d42dc82879b1` | `shasum -c` OK |
| `supervision/DECLINE_REGISTER_V002.md` | `957476c8c605a370` | OK |
| `supervision/PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e83` | OK |

**V011 HAZARD RESTATED.** The packet copy `aa7c6d4904706276` (78,794 B) and the
workspace-root copy `20a3a17d44e15841` (84,987 B) are DIFFERENT byte-versions. Every
offset in this artifact indexes the **packet copy**. Offsets are not interchangeable
between the two, and a reader who resolves these spans against the root copy will read
the wrong bytes.

**PLAN DRIFT, flagged not adjudicated.** `supervision/THE_PLAN_TO_ALPHA_AND_GRAVITY`
has moved V001 → V004 across this session. V003 carried the typing *"kappa_record is
the record-side intensive Hessian; ALPHA'S STIFFNESS IS K_*"* and *"the
R-L2b/linked-cluster campaign is OFF alpha's value path."* Both gaps treated here sit
on the record-side Hessian, so a lane consuming V001–V003 and a lane consuming V004
may be answering different questions. Registrar-shaped; not mine to route.

---

## 1. AS1 — THE TWO DEMANDS, EXTRACTED VERBATIM

Both gaps are opened in **my own sealed 739**, §3.3, single span **[27723,28958)**,
sha `8c5d460c3f633d54`, in file `660e0c1464aee4f3`. Verbatim:

> ### 3.3 What a structural settlement would require
>
> **Path A — a factorization derivation.** Derive that `Q_flux` is a conformal isometry
> in the sense above. Requires a **sealed structural property of `R_record,L`'s form** on
> connection tangents. [PROVABLE, negative, searched space as §1.7.2] `V011` supplies
> `R_record,L` as a *definition* — a second derivative of a normalized record functional
> after a limit — and I found no sealed statement of its **form** anywhere in the 1,958
> files searched. **That is a SPEC GAP**, and by the Builder-B law it is a gap to report,
> not a detail to go looking for in a producer's code.
>
> **Path B — a sealed domain restriction.** Seal the set `D` of pairs the criteria
> actually evaluate and prove agreement on `D` only (reading (F-c)). Requires an
> **enumeration of the pairs the sealed criteria consume**. [PROVABLE, negative, same
> searched space] No such enumeration is sealed. **Second SPEC GAP** — and it is the one
> that makes outcome 3 live rather than hypothetical, because without `D` the question
> cannot even be asked in its material form.
>
> **Neither path is available today.** Both are blocked on an object that does not exist
> in sealed form, not on an unperformed computation.

### 1.1 Demand separated from restatement — the audit's lesson applied

The relay asks me to distinguish the demand from restatement. Doing so is not a
formality here; it is the structural move that makes the rest of this artifact
possible, because **each paragraph contains three different kinds of sentence**, and
they have different lifespans.

| Kind | Path A instance | Path B instance | Lifespan |
|---|---|---|---|
| **DEMAND** (the object required) | "a **sealed structural property of `R_record,L`'s form** on connection tangents" — sufficient to derive `Q_flux` conformal-isometric | "an **enumeration of the pairs the sealed criteria consume**" — sufficient to prove agreement on `D` only | Does not go stale. Unchanged. |
| **RESTATEMENT** (what the record already had) | "`V011` supplies `R_record,L` as a *definition* — a second derivative of a normalized record functional after a limit" | (none; Path B restates only the purpose) | Verified still accurate. |
| **HUNT RESULT** (a negative over a searched corpus) | "I found no sealed statement of its **form** anywhere in the 1,958 files searched" | "No such enumeration is sealed" | **Goes stale. Both did.** |

**The two demands are intact and I do not touch them.** What §2 re-runs, and what §3
revises, is only the third row. A demand states what would settle a question; a hunt
result states what a corpus happened to contain on one day. 739 wrote them adjacent and
in the same voice, and the closing sentence — "blocked on an object that does not exist
in sealed form" — is a *hunt result wearing a demand's grammar*. That is precisely the
conflation the audit warned about, and I committed it in my own artifact.

Note also that both hunt results are tagged in 739 as `[PROVABLE, negative]`. A negative
over a corpus is provable only relative to that corpus. The tag was too strong for the
sentence it decorated; a corpus negative is at best PART-PROVABLE, and its provable part
is "not in these 1,958 files," not "does not exist in sealed form."

---

## 2. AS2 — THE HUNT

Corpus now **3,006 files** against 739's **1,958**. Before treating the delta as new
physics I characterised it, because a stale-negative claim that rests on a miscounted
delta is worthless:

> The delta is **not** 1,048 files of new physics. It is 2,248 `.md` — of which the
> DARIO artifacts **739 excluded from itself** are now included — plus 757 `.json`
> that 739's glob **never touched**.

So the delta is dominated by (a) my own lane's subsequent output and (b) a file class
739's instrument could not see. That matters for attribution: where the hunt now finds
something, I must check whether it is genuinely new stock or something **739 could have
seen and did not**. For both gaps, the decisive content turns out to be the latter.

### 2.1 GAP 1 — what the record displays or constrains toward `R_record,L`'s form

**Four properties are sealed or forced. 739 asserted none existed.**

**(F1) The definition, unchanged.** V011 [43562,43760), sha `60d00acec83647e0`:

> The primitive record susceptibility is the intensive Hessian
> `R_record,L(a,b) = d^2/ds dt [Gamma_K(sa+tb)/N_4(K)]|_(s=t=0)`, evaluated after the
> thermodynamic/linked-cluster limit.

**(F2) SYMMETRY — forced, by me, from the definition itself.** `R_record,L` is a
**mixed second partial derivative** of a single scalar functional in two independent
directions. Where the regularity holds that lets the mixed partials be interchanged,
`R_record,L(a,b) = R_record,L(b,a)` follows immediately. This is a property **of the
form**, it is derivable from sealed text alone, and 739 missed it — not through a
corpus gap but by not reading the object it had itself quoted. Conditional: the
regularity is exactly what the T7 S5.3 burden covers (§2.1 (F5)); I mark the symmetry
**forced-modulo-regularity**, not free-standing.

**(F3) REAL and BILINEAR — sealed.** V011 [45187,45319), sha `f87fd3e80c16bb3f`:

> Complexification is allowed only for Fourier analysis and must return the same real
> bilinear form from the paired sine/cosine modes.

"the same real bilinear form" is a statement **about the form** in the record's own
voice. It is the exact category of sentence 739 declared absent.

**(F4) POSITIVE — sealed, of the record susceptibility.** V011 [52191,52560), sha
`c9532a2a452f414c`: *"The positive record susceptibility is compared only with this
positive tensor, never directly with the indefinite Lorentz bivector contraction."*
Positivity is predicated of the record susceptibility, and the same span forbids
after-the-fact tolerance on the equalities it governs.

**(F5) The T7 Duhamel decomposition — real constraint, DOUBLY FENCED.** File
`4fe8d42dc82879b1`, seal OK, lines 567–577:

> conditioned Duhamel covariance minus the intensive diagonal correction:
> `R_record = lim_K g_(D,c,K)-density - (1/2) lim_K (log q_K)''-density`, with all three
> limits existing and the equality exact. **The identification** of `lim_K
> g_(D,c,K)`-density with the V011 `G_L`-computed Duhamel covariance **is IMPORTED
> through interface I3 … and is NOT proved by this schema.**

Fence 1 — the identification with V011's object is imported, not proved. Fence 2 — the
schema's own flags, lines 822–824:

```
volume_uniform_zero_free_neighborhood_proved = false      # (H1): named underived input
connected_linked_cluster_density_proved      = false      # (H2): named underived input
Duhamel_intensive_Hessian_equality_proved    = false      # remains false even on pass
```

and line 793–794 confirms *"(H1)/(H2) remain underived and
`Duhamel_intensive_Hessian_equality_proved` remains false ON PASS"* — i.e. the flag does
not flip even when the schema succeeds. A third fence, F2.2 (lines 71–75), forbids
checking the completed chain's Hessian against the three-way `H_CTP/g_FS/g_Duhamel`
agreement *"as if it were the same number."*

This is the sharpest constraint in the corpus on `R_record`'s form, and it is
unusable for Path A as it stands. I record it as constraining stock, not as supply.

**(F6) The Fubini–Study route is CLOSED by V011 itself** — [43828,43951), sha
`c0a290dd54f7e31c`:

> Fubini-Study is a consistency check on each normalized local output ray, not an
> independently selectable action multiplier.

Elsewhere V011 pins the leading quadratic to (1/2) Fubini–Study ([37949,38216)). A
reader could take that as the sealed form. **It is not**, and V011 says so itself: FS is
a check, not a determinant of the object. Anyone attempting to close GAP 1 through the FS
identification is barred by the record. I flag this because it is the most attractive
wrong move available and it looks like a supply.

**What is still missing for Path A.** Path A needs a structural property sufficient to
derive that `Q_flux` is a **conformal isometry** — a statement relating `R_record,L`'s
values on *pairs of connection tangents* to a metric structure on those tangents.
Real + bilinear + symmetric + positive does not give conformality; every inner product
satisfies all four. **The demand is unmet.** But it is unmet for a *narrower* reason
than 739 gave.

### 2.2 GAP 2 — what the record displays or constrains toward the pair set `D`

**(D1) The ambient is sealed.** V011 [45361,45440), sha `a0416621e199eede`:
`mathcal_K_L(xi,zeta) = R_record,L(Q_flux xi, Q_flux zeta), xi,zeta in F_phys.`
So `D ⊆ F_phys × F_phys` — sealed, though a continuum.

**(D2) A FINITE, TOTALLY ORDERED CANDIDATE ENUMERATION IS SEALED.** V011
[48795,49068), sha `4f7fa9294b98f299`:

> Candidate momenta are all `n in {-1,0,1}^4\{0}`, identifying `n~-n` by requiring the
> first nonzero component to be positive, ordered by `(norm(n)^2,n)` lexicographically.
> Candidate polarizations, in order, are `e_j` and then `e_j+e_k` for `j<k`; zero
> bivectors are skipped.

I counted it rather than eyeballing it: 80 raw `n`, **40** after the `n ~ -n`
identification (= (3⁴−1)/2), and **10** polarizations (4 + 6), so **≤ 400** candidate
`(n, v)` modes, the bound being loose only because zero bivectors are skipped. This is
a finite, totally ordered, explicitly generated list — the object 739 called absent, in
a file 739 had read.

**(D3) THE SEALED CRITERION CONSUMES DIAGONAL PAIRS ONLY.** V011 [48145,48390), sha
`3496a42ef17c4bc9`:

> The paired responses `y_c=mathcal_K_L(xi_c,xi_c), y_s=mathcal_K_L(xi_s,xi_s)` must
> agree exactly or by a preregistered certified enclosure. Complex exponentials may be
> used only in an independent reconstruction of the same real pair.

Both arguments are the **same mode** in each case. These are, across all 3,006 files,
the **only concrete arguments `mathcal_K_L` is ever applied to**. So the pairs the
sealed criterion visibly consumes lie on the **diagonal** of the candidate list — at
most ~400 pairs — not in the continuum `F_phys × F_phys`.

**(D4) Why this does NOT close GAP 2 — the step I decline to take.** It is tempting to
conclude `D ⊆ diag(candidate list)` and call Path B available, since a *domain
restriction* argument is sound on an over-approximation: any property proved for every
element of a superset holds on `D`. The containment would therefore do Path B's work
even without equality, and that is a real weakening of the demand.

**But the containment is not sealed.** (D3) is a **hunt result** — "no other concrete
arguments appear in 3,006 files" — and an absence over a corpus is not a universal
statement about what the criteria consume. Promoting it would be exactly the move §1.1
just convicted 739 of, committed in the same artifact that convicts it, and it would
let a corpus scan exculpate a producer-side object. **BR-1 bars it directly**: a
hunt-derived containment would be carrying a criterion's direction on evidence that can
only accuse, never exculpate. I therefore record (D3) as strong evidence and refuse to
promote it to a premise.

### 2.3 The adjacent stock the relay named

- **`~_R` and the two response-inequivalent admission witnesses (Q-720/Q-722):** these
  bear on *which* parents are response-distinguishable, not on the Hessian's form or on
  which pairs the criteria read. Checked; **no bite on either gap**. Recording the
  negative so the next lane does not re-walk it.
- **The microstate spheres (32/118-dim non-gauge):** these are what makes the gaps
  material — assignment is free there and seen only by `K_L` — but they supply neither
  demand. They are the *reason* the gaps matter, not stock toward closing them.
- **ERR/GNS dressed-record stock; `K_G^fin`/`J_c`/`S_R` lineage; the conservation law's
  carrier:** swept. Nothing that states `R_record,L`'s form on connection tangents;
  nothing that enumerates consumed pairs. Negatives recorded, corpus stated (3,006).
- **`eta_resp` overlap:** `STAGE8_B1C_DENSITY_LAYER_CODEX2_V001` (810) [18050,18881)
  adjudicates `eta_resp` head-on. The three gaps **compose rather than collapse** —
  none is a restatement of another. This is the finding that makes §3.3 answerable.

---

## 3. AS3 — CLASSIFICATION (the 776/785 standard)

Standard: SUPPLIED / DERIVABLE-FORCED (pin displayed) / FREE (two sketches) /
UNDECIDABLE (missing object named). RULING-LAST's passes ran first: register grep, hunt
(§2), forcing classification — before any class is stated.

### 3.1 GAP 1 — the response Hessian's form

**CLASS: UNDECIDABLE (missing object named)** — for the demand *as posed*, with a
forced sub-ledger that 739 denied.

*Missing object, named:* a sealed statement relating `R_record,L`'s values on **pairs of
connection tangents** to a metric structure on those tangents — sufficient to yield
`Q_flux` conformal-isometric. Equivalently and more usefully: the object is blocked
behind **(H1) `volume_uniform_zero_free_neighborhood`** and **(H2)
`connected_linked_cluster_density`**, both named underived inputs, plus the **I3
identification** of the Duhamel covariance with V011's `G_L`-computed object, which the
T7 schema explicitly does not prove.

*Forced sub-ledger (pins displayed, none of which suffices):* real (F3, sealed);
bilinear (F3, sealed); symmetric (F2, forced-modulo-regularity, derived here);
positive (F4, sealed). Barred route: Fubini–Study (F6, closed by V011 itself).

**Relation to `eta_resp`: CONSTRAINS, does not supply.** Closing GAP 1 would fix the
structure the response Hessian must have, which any realization of `eta_resp` must then
respect — but it hands over no carrier. Per 810's adjudication the gaps compose; GAP 1
closing leaves `eta_resp` still absent.

**Relation to the `f_g` bridge: NEITHER.** `f_g` is fenced by **S26** (`C_ref` barred as
a source; no smooth import). Because that bar sits *upstream* of the materiality
question, no amount of Hessian-form supply can reach `f_g`. Stated explicitly because
"more structure on the record side" reads as though it should eventually deliver the
physical bridge, and under S26 it cannot.

### 3.2 GAP 2 — the evaluated-pair set `D`

**CLASS: UNDECIDABLE (missing object named)** — but at a *radically* shorter distance
from closure than GAP 1, and than 739 implied.

*Missing object, named:* a sealed **universal** statement that the criteria evaluate
only diagonal pairs drawn from the sealed candidate enumeration. Not an enumeration of
`D` — **one sentence of quantification** over the criteria.

*Forced sub-ledger (pins displayed):* `D ⊆ F_phys × F_phys` (D1, sealed); a finite,
totally ordered candidate mode list of ≤ 400 `(n,v)` (D2, sealed, counted here);
diagonal-only concrete arguments across 3,006 files (D3, hunt-supported, **not**
promoted — §2.4/(D4)).

**Relation to `eta_resp`: CONSTRAINS, does not supply.** Sealing `D` bounds the
evaluation domain that `eta_resp`'s realization would have to cover; it supplies no
carrier.

**Relation to the `f_g` bridge: NEITHER.** Same S26 reasoning as §3.1.

### 3.3 Why both land in the same bucket, and why that is not 739's answer restated

Both classify UNDECIDABLE, which superficially reproduces 739's "neither path is
available today." The substantive change is in **what blocks each, and how far**:

- **739:** both blocked on *nonexistent objects*, symmetrically, indefinitely.
- **Here:** GAP 1 is blocked on **two named underived inputs plus a disclaimed
  identification** — producer-side work with named handles. GAP 2 is blocked on **a
  single missing quantifier** over content that is already sealed and enumerated.

An "UNDECIDABLE" that names `(H1)`, `(H2)`, `I3` and a one-sentence quantifier is a
different instruction to the program than an "UNDECIDABLE" that names nothing. The
class is the same; the actionability is not.

### 3.4 MARKED RULING-SHAPED — not routed

Whether a **sealed containment** (rather than an enumeration) discharges Path B's demand
is an adjudication of a path's sufficiency. I have given the logic in (D4) — an
over-approximation is sound for a restriction argument — and I stop there. Marking and
moving on per AS3. I note only that if the registrar rules containment sufficient, GAP 2
becomes closable by sealing one universal statement whose evidence base is already
3,006 files wide.

---

## 4. AS4 — Gc2 STATUS

**WHAT CLOSES NOW: nothing.** Neither demand is met; no path becomes available; no
member is bound; `kappa_record` is not computed. Two things are **retired**, both of
them mine:

1. 739's *"no sealed statement of its form anywhere"* — **WITHDRAWN**, replaced by the
   four-property pin ledger of §3.1.
2. 739's *"No such enumeration is sealed"* — **WITHDRAWN**, replaced by §3.2's finding
   that a finite totally ordered candidate enumeration **is** sealed at V011
   [48795,49068), and that what is unsealed is the quantifier, not the list.

**WHAT IS LANE-DERIVABLE NEXT.** One item, and it is a derivation, not an adoption:
attempt to derive **from the sealed criteria's own text** that every argument they take
is diagonal. This is a finite structural task over sealed stock — it authors nothing,
adopts nothing, and either produces the missing quantifier as a *derived* statement or
exhibits the criterion that breaks it. Either outcome closes §3.2's question. It must be
run by a lane that did not produce the criteria.

Explicitly **not** lane-derivable: anything on GAP 1. Its blockers are producer-side
named inputs, and reaching for them from here would be a peek, not a derivation.

**WHAT GENUINELY WAITS.** (H1) `volume_uniform_zero_free_neighborhood`; (H2)
`connected_linked_cluster_density`; the I3 identification of the Duhamel covariance with
V011's `G_L` object. All three sit in the producer lane with the flags held false by the
schema's own text *even on pass*. Also waiting, and separable: `eta_resp`'s carrier,
`F_g`, and `f_g` — the last barred at the source by S26, so it waits on a ruling, not on
work.

**THE STANDING NEGATIVE HOLDS.** No native forcer has ever output a unique positive
value, and nothing here changes that. Both gaps remain gaps. What changed is that they
are now gaps with named edges.

---

## 5. FREEDOMS-CONSUMED (mandatory block, law 2 / law 2a)

```
CARRIED, NOT CONSUMED:
  R_record,L                       carried as V011's DEFINITION only [43562,43760)
  mathcal_K_L, Q_flux, F_phys      carried as sealed, unrealized
  the candidate mode enumeration   carried as SEALED CANDIDATES, not as D
  the T7 Duhamel decomposition     carried WITH BOTH FENCES ATTACHED; identification
                                     NOT inherited; (H1)/(H2) NOT discharged
  the microstate spheres           carried as the materiality context, not as stock
  eta_resp, F_g, f_g               carried as ABSENT (810's adjudication respected)
  the B1a family, parent frame     carried whole, untouched
DERIVED HERE (one item):
  symmetry of R_record,L           forced-modulo-regularity from the mixed-second-
                                     derivative form; regularity NOT assumed, it is
                                     named as the S5.3 burden
COUNTED HERE (one item):
  |candidate (n,v)| <= 400         arithmetic over V011's own generating rule;
                                     no selection, no ordering imposed beyond V011's
SCALING WEIGHTS (law 2a):          NONE CONSUMED.  No weight, multiplier or
                                     normalization is fixed anywhere in this artifact.
                                     Fubini-Study explicitly NOT taken as a multiplier
                                     (V011 [43828,43951) bars it).
SUBSTITUTED:                       NONE.
DECLINED HERE:                     the promotion of (D3) from hunt result to premise
                                     (BR-1); the FS identification (V011's own bar).
```

## 6. FLATTENING CHECK — `DECLINE_REGISTER_V002` (`957476c8c605a370`)

All 37 rows walked before sealing. Live and discharged: **S01** (the `+1`/`Φ†Φ`
decline — not touched; no trace substitution occurs here), **S26** (`C_ref` barred as a
source — load-bearing in §3.1/§3.2 and honoured, not bridged), **S27**, **S28**,
**S30** (Hessian-first only IF R-L2b FAILS — this artifact does not run Hessian-first;
it classifies a gap), **S13**, **S19/S21/S24/S25** (fence-posts intact). **No
undecidable in this artifact is rescued by reaching for an axiom.** Both gaps are
reported unclosed. **CHECK CLEAN.**

## 7. CORRECTION PROPAGATION (law 7) — downstream consumers named

The two withdrawn negatives (§4) are restated or relied on in:

- `supervision/THE_PLAN_TO_ALPHA_AND_GRAVITY_V001.md`, `…_V003.md`, `…_V004.md`
- `supervision/QUESTIONS_SETTLED_REGISTER_V001.md`
- `workspace/STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md` (the source, mine)
- `relay_inbox/RELAY_PASTE_815_MATERIALITY_OPENING_DARIO_V001.md` (this relay's own
  framing inherits them)

Additionally citing 739 by name: `workspace/STAGE8_B1C_DENSITY_LAYER_CODEX2_V001.md`,
`workspace/STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md`, `relay_outbox/739_DONE.md`.

Each consumer inherits a negative that is now known to be overstated. **The demands they
inherit are unaffected** (§1.1); only the "no such object exists" clauses need
narrowing. I name the consumers and route nothing.

---

## 8. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+3).**

**(1) BOTH OF THIS RELAY'S FINDINGS ARE CORRECTIONS TO MY OWN SEALED ARTIFACT, AND THE
CONSTRAINING CONTENT WAS IN A FILE 739 HAD ALREADY READ.** The candidate enumeration
[48795,49068), the diagonal-pair criterion [48145,48390), and the real-bilinear
statement [45187,45319) are all in V011 — which 739 read, quoted, and cited by span.
This was not a corpus limitation and the 3,006-vs-1,958 delta did not supply it; the
delta is mostly my own later output plus a file class the glob never saw. **739 searched
1,958 files for an object that was in the file it was already holding.** The honest
description is not "stale negative" but *under-read source*, and it is now the **fifth**
instance of me under-reading my own sealed artifacts (795, 798, 804, 815-on-739, and
this). That is a pattern in my instruments, not in the corpus.

**(2) I DECLINED THE STEP THAT WOULD HAVE MADE THIS RELAY LOOK DECISIVE.** Promoting
(D3) to a sealed containment would have let me report GAP 2 as effectively closed and
Path B as available. The evidence is genuinely strong — 3,006 files, zero
counterexamples. It is still a corpus absence, BR-1 bars a hunt result from carrying a
criterion's direction, and §1.1 of this same artifact convicts 739 of exactly that
conflation. Declining it costs this relay its headline. Recorded so the next lane knows
the option was seen, weighed, and refused rather than missed.

**(3) FOURTH WRONG FIRST PROBE.** My first propagation grep used an unquoted `*.md`
glob and zsh returned "no matches found" for all three queries — a false universal
negative that I would have reported as "no downstream consumers" had I not re-run it.
Requoted, it returned **seven** files. Following 809 (pattern too narrow), 792 (single
seal spelling), 753/770 (same), this is the fourth time a first probe has produced a
wrong negative in this session. The consistent finding stands and I restate it: **my
reasoning survives contact; my instruments and bookkeeping are the weak part.** Every
one of these was caught by a control rather than by care, which argues for more
controls, not more care.

None of the three disclosures flatters this artifact's conclusions. The two substantive
things I add — that both gaps are narrower than my own prior work said, and that the
narrowing still does not close either — cut in opposite directions, and I report both.

---

```
DEMANDS = 2 extracted verbatim (spans)
  Both in workspace/STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md (739, MINE)
  = 660e0c1464aee4f3…, §3.3, span [27723,28958), sha 8c5d460c3f633d54.
  Path A demand: "a sealed structural property of R_record,L's form on connection
    tangents" (sufficient to derive Q_flux conformal-isometric).
  Path B demand: "an enumeration of the pairs the sealed criteria consume"
    (sufficient to prove agreement on D only).
  DEMAND vs RESTATEMENT vs HUNT RESULT separated (§1.1): the demands are intact; only
  the attached corpus negatives are revised.

HESSIAN_FORM = UNDECIDABLE (missing object named)
  Missing object: a sealed statement relating R_record,L on PAIRS OF CONNECTION
    TANGENTS to a metric structure — blocked behind (H1)
    volume_uniform_zero_free_neighborhood, (H2) connected_linked_cluster_density (both
    named underived inputs), and the I3 identification the T7 schema does not prove
    (Duhamel_intensive_Hessian_equality_proved = false EVEN ON PASS).
  FORCED SUB-LEDGER 739 DENIED EXISTED: real + bilinear (V011 [45187,45319)
    f87fd3e80c16bb3f); positive (V011 [52191,52560) c9532a2a452f414c); symmetric
    (derived here from the mixed-second-derivative form, modulo the S5.3 regularity).
    None suffices — every inner product has all four.
  BARRED ROUTE: Fubini-Study, closed by V011 itself [43828,43951) c0a290dd54f7e31c
    ("a consistency check … not an independently selectable action multiplier").
  eta_resp: CONSTRAINS, does not supply the carrier (810's compose-not-collapse).
  f_g bridge: NEITHER — S26 bars the source upstream of materiality entirely.

PAIR_SET_D = UNDECIDABLE (missing object named) — one quantifier from closure
  Missing object: a sealed UNIVERSAL statement that the criteria evaluate only diagonal
    pairs from the sealed candidate list.  NOT an enumeration — one sentence.
  SEALED STOCK 739 DENIED EXISTED: D ⊆ F_phys × F_phys (V011 [45361,45440)
    a0416621e199eede); a FINITE TOTALLY ORDERED candidate enumeration, ≤ 400 (n,v)
    modes — 40 momenta × 10 polarizations, counted here (V011 [48795,49068)
    4f7fa9294b98f299); and the sealed criterion consumes DIAGONAL pairs only,
    y_c = K_L(xi_c,xi_c), y_s = K_L(xi_s,xi_s) (V011 [48145,48390) 3496a42ef17c4bc9) —
    the only concrete arguments K_L takes in 3,006 files.
  NOT PROMOTED: the containment D ⊆ diag(candidates) is a HUNT RESULT, not a seal.
    BR-1 bars a corpus absence from carrying a criterion's direction.  Declined
    deliberately (§8.2) though it would have read as closure.
  MARKED RULING-SHAPED, unrouted: whether a sealed CONTAINMENT discharges Path B's
    demand (an over-approximation is sound for a restriction argument).
  eta_resp: CONSTRAINS the evaluation domain, does not supply the carrier.
  f_g bridge: NEITHER (S26).

GC2_STATUS = closes nothing / derives next: the diagonal quantifier / waits: (H1),(H2),I3
  CLOSES NOW: nothing.  Neither demand met; no path available; no member bound.
    RETIRED (both mine): 739's "no sealed statement of its form anywhere" and "No such
    enumeration is sealed" — both WITHDRAWN, replaced by the pin ledgers above.
  DERIVES NEXT (lane-derivable, authoring nothing): derive from the sealed criteria's
    own text that every argument they take is diagonal.  Finite, structural, over sealed
    stock; must be run by a lane that did not produce the criteria.  Either it yields the
    missing quantifier as DERIVED or it exhibits the criterion that breaks it.
    NOT lane-derivable: anything on GAP 1 — its blockers are producer-side.
  WAITS: (H1)/(H2)/I3, flags held false by the schema's own text even on pass; and
    separately eta_resp's carrier, F_g, and f_g (the last barred at source by S26, so
    it waits on a ruling, not on work).
  Standing negative intact: no native forcer has yet output a unique positive value.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+3)
  (1) Both findings correct MY OWN sealed 739, and the decisive content was in V011 —
      a file 739 read, quoted and span-cited.  Not a corpus limitation: the 3,006-vs-
      1,958 delta is mostly my own later output plus a .json class the glob never saw.
      Fifth instance of under-reading my own sealed artifacts.
  (2) I declined the promotion that would have made this relay decisive (§8.2) — GAP 2
      would have read as closed.  Refused, not missed.
  (3) Fourth wrong first probe: an unquoted zsh glob returned a false "no matches" on
      all three propagation queries; requoted it found seven consumers.  My reasoning
      survives contact; my instruments are the weak part.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
