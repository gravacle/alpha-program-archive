# STAGE 8 / [PLAN:AXN-BUILD-A22] — THE PARTIAL U3 RECORD (ROOT D)
## DARIO LANE (Builder B) — V001

RELAY 855. Lane guard PASS (DARIO). Inbox `94938330caa74519…`, seal verified BEFORE reading.
State brief `e26f0d16055f3e83…` verified and read. Charter `c0ad6decf156ef06`, seal OK.

**SUBJECTS, both seal-verified BEFORE reading:** my own 854 cross-check
`STAGE8_AXN_BUILD_B0_CENSUS_CROSSCHECK_DARIO_V001.md` = `8625fbba3d8dd500`, `.md.seal` OK; and its
subject census `STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md` = `a614a69409b40ab8`, `.md.seal` OK.

**DEPENDENCY DISCLOSED IN THE HEADER, NOT BURIED:** this relay executes a contract **I myself
displayed** at 854, and the startability finding it rests on is mine and un-cross-checked. **If 854's
split is wrong, this record is aimed at a move that does not exist.** The relay states the execution
is lawful with opposite-lane verification to follow; I proceed as directed and mark the shape.

GATES: `alpha_computed = false`; `proof_authorized = false`; `kappa_record_computed = false`.
**NOTHING SELECTED BEYOND THE CONTRACT'S PLACEMENT.** No smooth import; no EM identification; no
member binding. PE-1..PE-12 pointer-only. **ALL HEADLINE ITEMS CLAIMED.**

---

## 1. THE BAR CHECK, RUN FIRST — NO BARRING SENTENCE

The relay directs me to stop if any sealed sentence bars a partial U3 **package**, and to keep 854's
distinction: the partial-substitute bar applies to **B0 (root A)**, not to U3's assembly.

**I swept for a barring sentence and found none.** The nearest hits are unrelated in object — a
Thomson-scoping remark about upstream slots, a contract-disposition rule about implementations, an
interruption record about partial outputs. **No sealed sentence bars a partial U3 package.**

**And the distinction holds at bytes:** `ce46032d8bbac6ab` reads *"an anonymous or partial substitute
is not [admissible]"* in a document titled **B0 Substitute Admissibility Adjudication**, whose subject
is the `B0_candidate` consumed by `DESCEND_B0`. **U3 is a descendant package, not a B0 substitute.**
Extending that bar to U3 would be exactly the conflation 854 warned against.

**A governing specification the relay did not name, found and conformed to.** Codex's round-2
frontier map `STAGE8_AXN_BUILD_ROUND2_FRONTIER_MAP_CODEX2_V001.md` = `5e163d85c49361d3`, `.md.seal`
OK, **has already propagated my 854 split** and specifies this artifact:

> *"**Must produce:** one content-addressed partial U3 record that places the exact existing P3
> law-side subpackage in its typed U3 slot, carries its scope unchanged, and displays separate
> sealed-status rows for the still-absent measure, interacting contour, complete boundary/edge
> carrier, and unbounded endpoint domains. The output must say `PARTIAL`, not complete, and must not
> claim root-D discharge or construct `d_U3`."*
>
> *"**Stop conditions:** the placed bytes differ from the sealed P3 package; any one of the four
> absent fields is filled, inferred, defaulted, or merged; a measure, contour, boundary value,
> extension, or endpoint domain is authored; the partial record is presented as admissible where the
> receiver requires a complete package."*

**I conform to it rather than to my own 854 phrasing where they differ.** It is the opposite lane's
specification of my contract, and it is sealed.

---

## 2. THE PARTIAL U3 RECORD

### 2.1 The filled leg — placed, not built

```text
SLOT:      U3's law-side subfield
PLACED:    the CERTIFIED LAW-SIDE SUBPACKAGE, exactly as sealed
SOURCE:    STAGE8_TASK4A_P3_SECOND_ATTACK_LAW_SIDE_CONSTRUCTION_AND_NARROWED_STOP_V001.md
           = 8e9a09c104f4b635, .md.seal OK  (digest matches the frontier map's pin)
STATUS AT SOURCE, carried verbatim and unchanged:
           "CERTIFIED LAW-SIDE SUBPACKAGE BUILT; COMPLETE U3 INSTANCE REMAINS UNBUILT"
SCOPE CARRIED UNCHANGED — INCLUDING ITS PREMISE DEPENDENCE:
           TYPE-P | premises: DoR-008, DoR-009, DoR-013, DoR-014
```

**The premise dependence is carried, not absorbed.** The placed leg is **TYPE-P**, not TYPE-R: it
holds *on* four declared premises. A reader who treats the filled leg as unconditional would upgrade
its type silently. **The placement inherits the premises; it does not discharge them.**

**Two flags are true at the source and are the content of the filled leg**
(`f97ee43e052969f0`, flag block `[1907,2702)` sha `8368dcbbc5d4af2f`):

```text
P3_SOURCE_TOPOLOGICAL_GLUE_SUBFIELD_BUILT     = true | TYPE-P | premises: DoR-008,009,013,014
P3_COMMON_ORIGIN_PROVENANCE_PORT_DISCHARGED   = true | TYPE-P | premises: DoR-013, DoR-014
```

### 2.2 The four stops — displayed at their sealed statuses, nothing filled

Same block, same span. **Verbatim, with each would-build as the stop's requirement:**

| # | field | sealed status | would-build (the stop's requirement) |
|---|---|---|---|
| S1 | measure | `P3_MEASURE_BUILT = false \| TYPE-U` | *"branch-joint prequotient measure and descended physical measure"* |
| S2 | interacting contour | `P3_INTERACTING_CONTOUR_BUILT = false \| TYPE-U` | *"interacting contour/analytic/i-epsilon prescription"* |
| S3 | complete boundary/edge carrier | `P3_COMPLETE_BOUNDARY_EDGE_BUILT = false \| TYPE-U` | *"complete preparation/gluing and boundary package"* |
| S4 | unbounded endpoint domains | `P3_UNBOUNDED_ENDPOINT_DOMAINS_BUILT = false \| TYPE-U` | *"named unbounded operators and common invariant closure domain"* |

**None is filled, inferred, defaulted, or merged. No measure, contour, boundary value, extension, or
endpoint domain is authored anywhere in this artifact.**

### 2.3 The record's own type

```text
PARTIAL_U3 := { filled: the certified law-side subpackage (TYPE-P on DoR-008/009/013/014) }
              { stops:  S1 measure, S2 contour, S3 boundary/edge, S4 endpoint domains — all
                        false | TYPE-U }

THIS IS NOT A COMPLETE U3.  U3_008_INSTANCE_EXISTS = false | TYPE-U remains false and untouched.
THIS IS NOT A DISCHARGE OF ROOT D.
d_U3 IS NOT CONSTRUCTED and no d_U3 precondition is claimed satisfied.
IT IS NOT ADMISSIBLE WHERE THE RECEIVER REQUIRES A COMPLETE PACKAGE.
```

---

## 3. WHAT THIS MAKES NEWLY REACHABLE — AND WHAT IT CANNOT

**The reachability is real and it is byte-grounded in one line of the same block:**

```text
U3_008_INSTANCE_EXISTS = false | TYPE-U |
  would-build: THE FOUR FIELDS ABOVE ON THE DISCHARGED COMMON-ORIGIN TRACE
```

**Read against `P3_COMMON_ORIGIN_PROVENANCE_PORT_DISCHARGED = true`, the roll-up's own condition is
half-met of record.** U3's instance requires the four fields **on a discharged common-origin trace**
— **and that trace is discharged.**

```text
NEWLY REACHABLE:
  the four remaining fields are NOT each independently un-founded.  They share ONE
  ALREADY-DISCHARGED common-origin provenance port, which the roll-up names as the thing they must
  be built ON.  So U3's remaining work is FOUR FIELDS OVER AN EXISTING TRACE, not four fields plus a
  trace.  That is a structural narrowing of root D's remaining content, visible only once the
  law-side leg is placed and its two true flags are read against the roll-up's condition.

WHAT IT CANNOT DO:
  it satisfies NO d_U3 precondition — d_U3 consumes a COMPLETE U3, and 850's graph puts complete U3
    as an INPUT to that leg (confirmed at my 854);
  it discharges NO part of root D — root D IS the complete U3 package;
  it does not touch roots A, B, C or E, and nothing here bears on B0's inhabitation;
  it does not upgrade its own premises: the filled leg stays TYPE-P.
```

**The honest size of this:** the partial U3 moves **no flag from false to true.** What it produces is
a placement plus a reading — the trace's discharge was already sealed, and the contribution is
displaying that the four stops stand *on* it rather than beside it.

---

## 4. FREEDOMS-CONSUMED · FLATTENING CHECK

```text
CARRIED, NOT CONSUMED: the law-side subpackage AT ITS SEALED SCOPE AND TYPE-P PREMISE DEPENDENCE
  (DoR-008, 009, 013, 014), carried and NOT discharged; the four stops at their sealed false/TYPE-U
  statuses; U3_008_INSTANCE_EXISTS = false, untouched; the B0 partial-substitute bar CARRIED AS
  APPLYING TO ROOT A ONLY, per 854 and per its source's own subject.
PLACED, NOT BUILT: one leg.  THE PLACEMENT IS THE ONLY ACT IN THIS ARTIFACT.
AUTHORED HERE: NOTHING — no measure, no contour, no boundary value, no extension, no endpoint
  domain, no default, no merge, no inference across the four stops.
DERIVED HERE: one reading — that the roll-up's "on the discharged common-origin trace" condition
  meets an already-true flag, so the four stops stand ON an existing trace.
SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37 rows walked.** **S03 / the void condition — LIVE AND THE GOVERNING PRESSURE
HERE.** Four TYPE-U fields sit adjacent to a filled leg, and filling any one would look like progress;
850's gate and the frontier map's stop conditions both bar it. **Nothing filled.** **S12** — the
placement is of a *subpackage*, and the true/false flags are carried as the record's statuses, never
as the objects; in particular `..._DISCHARGED = true` is read as a status meeting the roll-up's
stated condition, **not** as a discharge of root D. **S26 / S08 / S19 / S24** — untouched; no measure
or domain imported from any smooth or clustering source. **T5** untouched.
`FLATTENING_CHECK = clean (37/37; S03 and the void condition live at the four stops).`

## 5. SELF-AUDIT

**VERB AUDIT: NOT CLEAN (+3).**

**(1) I EXECUTED A CONTRACT I MYSELF WROTE, ON A SPLIT I MYSELF FOUND, UN-CROSS-CHECKED.** 854
produced the startability finding; 855 acts on it. The loop is closed only by the opposite lane's
pass, which has not run. **If root D is not startable, this artifact is a well-formed answer to a
question that does not exist.** Stated in the header as well as here.

**(2) THE OPPOSITE LANE HAD ALREADY SPECIFIED THIS ARTIFACT AND THE RELAY DID NOT NAME IT.** I found
the frontier map's *"Must produce"* clause and its four stop conditions myself and conformed to them
over my own 854 phrasing. **Had I not looked, I would have built to my own specification of my own
contract** — two layers of self-authorship with no external check on either.

**(3) THE RESULT MOVES NO FLAG AND I SAY SO PLAINLY.** The only derived content is that the four
stops stand on an already-discharged trace. That is a genuine narrowing of root D's remaining shape
and it is **not** progress on any of the four fields. A reader wanting a build to have built something
will not find it here.

---

```
PARTIAL_U3 = BUILT (one leg filled, four stops displayed)
  BAR CHECK RUN FIRST: NO SEALED SENTENCE BARS A PARTIAL U3 PACKAGE.  The partial-substitute bar
    ce46032d8bbac6ab sits in the B0 SUBSTITUTE ADMISSIBILITY ADJUDICATION and governs the
    B0_candidate consumed by DESCEND_B0; U3 IS A DESCENDANT PACKAGE, NOT A B0 SUBSTITUTE, and
    extending it would be the conflation 854 warned against.
  CONFORMED TO A SPEC THE RELAY DID NOT NAME: Codex's round-2 frontier map 5e163d85c49361d3 (.md.seal
    OK) has ALREADY PROPAGATED my 854 split and specifies "one content-addressed partial U3 record
    that places the exact existing P3 law-side subpackage in its typed U3 slot, carries its scope
    unchanged, and displays separate sealed-status rows ... must say PARTIAL, not complete, and must
    not claim root-D discharge or construct d_U3", with four stop conditions.  I CONFORMED TO IT OVER
    MY OWN 854 PHRASING.
  FILLED LEG: the CERTIFIED LAW-SIDE SUBPACKAGE, 8e9a09c104f4b635 (.md.seal OK, digest matches the
    frontier pin), carried verbatim — "CERTIFIED LAW-SIDE SUBPACKAGE BUILT; COMPLETE U3 INSTANCE
    REMAINS UNBUILT" — WITH ITS PREMISE DEPENDENCE CARRIED AND NOT ABSORBED: TYPE-P | premises
    DoR-008, DoR-009, DoR-013, DoR-014.  Its content is the two true flags
    P3_SOURCE_TOPOLOGICAL_GLUE_SUBFIELD_BUILT and P3_COMMON_ORIGIN_PROVENANCE_PORT_DISCHARGED.
  FOUR STOPS, at f97ee43e052969f0 flag block [1907,2702) sha 8368dcbbc5d4af2f, NONE FILLED:
    S1 P3_MEASURE_BUILT = false | TYPE-U ; S2 P3_INTERACTING_CONTOUR_BUILT = false | TYPE-U ;
    S3 P3_COMPLETE_BOUNDARY_EDGE_BUILT = false | TYPE-U ; S4 P3_UNBOUNDED_ENDPOINT_DOMAINS_BUILT =
    false | TYPE-U.
  EXPRESSLY: NOT a complete U3 (U3_008_INSTANCE_EXISTS = false, untouched); NOT a discharge of root D;
    d_U3 NOT constructed; NOT admissible where the receiver requires a complete package.

NEWLY_REACHABLE = displayed (one structural narrowing, byte-grounded)
  The roll-up's own condition reads "would-build: THE FOUR FIELDS ABOVE ON THE DISCHARGED
  COMMON-ORIGIN TRACE" — and P3_COMMON_ORIGIN_PROVENANCE_PORT_DISCHARGED = true.  SO THE TRACE THE
  FOUR FIELDS MUST BE BUILT ON IS ALREADY DISCHARGED: U3's remaining work is FOUR FIELDS OVER AN
  EXISTING TRACE, not four fields PLUS a trace.  Visible only once the law-side leg is placed and its
  true flags are read against the roll-up's condition.
  WHAT IT CANNOT DO: satisfies NO d_U3 precondition (d_U3 consumes a COMPLETE U3, and complete U3 is
  an INPUT to that leg — 850's graph, confirmed at my 854); discharges NO part of root D; does not
  touch roots A, B, C, E; does not upgrade its own premises — the filled leg stays TYPE-P.

STOP_REQUIREMENTS = byte-cited list (all from f97ee43e052969f0, [1907,2702) sha 8368dcbbc5d4af2f)
  S1 measure                  -> "branch-joint prequotient measure and descended physical measure"
  S2 interacting contour      -> "interacting contour/analytic/i-epsilon prescription"
  S3 boundary/edge carrier    -> "complete preparation/gluing and boundary package"
  S4 endpoint domains         -> "named unbounded operators and common invariant closure domain"
  ROLL-UP                     -> "the four fields above on the discharged common-origin trace"

ROOT_D = NARROWED (in shape, not in count)
  Its remaining content is now displayed as FOUR FIELDS OVER AN ALREADY-DISCHARGED TRACE, with one
  leg placed and each stop's requirement byte-cited.  NO FLAG MOVED FROM FALSE TO TRUE.  Root D is
  not discharged and is no closer in count — only better shaped.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+3)
  (1) I EXECUTED A CONTRACT I MYSELF WROTE, ON A SPLIT I MYSELF FOUND, UN-CROSS-CHECKED.  If root D
      is not startable, this is a well-formed answer to a question that does not exist.  Stated in
      the header as well as here.
  (2) THE OPPOSITE LANE HAD ALREADY SPECIFIED THIS ARTIFACT AND THE RELAY DID NOT NAME IT — I found
      the frontier map's "Must produce" clause and its stop conditions myself and conformed to them
      over my own phrasing.  HAD I NOT LOOKED, I WOULD HAVE BUILT TO MY OWN SPECIFICATION OF MY OWN
      CONTRACT — two layers of self-authorship with no external check on either.
  (3) THE RESULT MOVES NO FLAG AND I SAY SO PLAINLY.  The only derived content is that the four stops
      stand on an already-discharged trace — a genuine narrowing of shape and NOT progress on any of
      the four fields.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
