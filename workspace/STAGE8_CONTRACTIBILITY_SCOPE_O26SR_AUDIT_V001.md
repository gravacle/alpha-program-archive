# STAGE8 CONTRACTIBILITY SCOPE — AUDIT OF O26SR V001

**COMMISSION O26SR — SCOPE-AUDIT — 2026-08-15 — DEFAULT-REFUTE**

**DETERMINATION ONLY. THIS ARTIFACT DISPLAYS AND TYPES. IT PROPOSES NOTHING,
AUTHORS NOTHING, ADOPTS NOTHING, AND CONSTRUCTS NO JOINER.**

```text
alpha_computed          = false
proof_authorized        = false
kappa_record_computed   = false
```

Testimony was given ZERO weight. Every claim of the subject artifact was
re-derived at bytes from seal-verified sources, or from an independently
written instrument in a fresh venv. No value originates here as a program
quantity. Every numeral below is a quoted digest, a line index, a file count,
or a pure combinatorial index. No measured constant appears. No comparison to
any measured quantity is made. No git operation was performed.

---

## 0. HEADLINE

```text
OVERALL VERDICT: CONFIRMED-WITH-CORRECTIONS.

THE CENTRAL FIND SURVIVES, AND IS STRONGER THAN THE BUILD MADE IT.
  Two countings of one reciprocal two-node network exist at sealed bytes.
  Re-derived here independently: simple category b_1 = 0; multigraph
  category b_1 = 1. Both source spans quoted whole and matched character for
  character. Cross-root byte identity re-verified by digest, not by testimony.

FOUR CORRECTIONS, TWO OF THEM MATERIAL AND ONE OF THEM A REVERSAL OF THE
BUILD'S OWN MATERIAL FLAG.

  C-1  MATERIAL — F-2 AND W-2 ARE REFUTED AT BYTES. The build asserts an
       "unexplained reversion": that V003 restricts to a tree by assumption
       and that "the cycle-carrying reading displayed at V002 is not carried
       forward." V003 :1011 carries it forward explicitly, in its own
       comparison table, one row below the tree row. There is no reversion.
       The build read V003 :196-214 only (its own import audit says so) and
       drew a version-family conclusion from that one span.

  C-2  MATERIAL — "CLAUSE A" IS NOT A CLAUSE, AND SILENCE IS READ AS A BAR.
       The build's Clause A is built from a sentence that says V001 "does not
       define a sum." An absence of definition is silence. The build converts
       it into something that "VOIDS", and neither V001's nor V002's row `N`
       void-condition column contains any in-degree condition. The build's own
       §12 calls it "the ABSENCE of an aggregation law" and then calls that
       absence a "structural clause" in the same sentence.

  C-3  SUBSTANTIVE — TEST (a)'s ADMITS RESTS PARTLY ON A NON-BAR, AND P-3
       RESTS ON NOTHING ELSE. The affirmative display is real, but it sits
       inside a document the record types as not adopted, and the hunt's own
       standard for "permitting" is LAW STRENGTH. Under the build's own rule
       the honest cell for (a) is a displayed-but-unadopted showing, not an
       admission by the surface.

  C-4  MINOR — the cause is a conjunction, not a single conjunct; a sweep
       denominator is wrong at bytes; several cited line spans are off; one
       quoted sentence is elided inside without ellipsis; one quotation is
       announced as "quoted whole" and is not.

NOT FOUND — CHECKED AND CLEAR.
  (E) No joiner is constructed, proposed, sketched, or recommended.
  (G) No lens token. No number of program import. No fence approached.
```

---

## 1. STEP 0 — PROBE AND SEAL

```text
TARGET      STAGE8_CONTRACTIBILITY_SCOPE_O26SR_V001.md          PRESENT, 62718 bytes
SIDECAR     STAGE8_CONTRACTIBILITY_SCOPE_O26SR_V001.md.seal.sha256   PRESENT
SHASUM -C   run from /Users/bgm/MB Work/alpha-program-archive/workspace   OK
DIGEST      520936a40636930772bba0cb4fd62437ed123d8b264cb07dc59e9444500258fe
OUTPUT PATH STAGE8_CONTRACTIBILITY_SCOPE_O26SR_AUDIT_V001.md      ABSENT before write
```

**EVERY CONSUMED SEAL RE-VERIFIED BY THIS AUDIT**, `shasum -a 256 -c`, executed
from the artifact's own directory, independently of the build's assertion:

```text
OK   STAGE8_GLUED_TOPOLOGY_HUNT_V001.md
OK   STAGE8_INGREDIENT_CENSUS_O17SR_V001.md
OK   STAGE8_INGREDIENT_CENSUS_O17SR_AUDIT_V001.md
OK   STAGE8_GLUING_CANDIDATE_O23SR_V001.md
OK   STAGE8_GLUING_CANDIDATE_O23SR_AUDIT_V001.md
OK   STAGE8_MIN_CARRIER_CANDIDATE_V001.md
OK   STAGE8_INDEX_VARYING_SETS_O20SR_V001.md
OK   ..._NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V001.md
OK   ..._NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V002.md
OK   ..._NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V003.md
OK   ..._NETWORK_SOURCING_LAW_V002_RE_REVIEW_LANE1_V001.md
```

**DIGESTS RE-COMPUTED, NOT COPIED.** The four digests the build prints in its
§1 for the NET family were recomputed here and match:

```text
87f696261651567e04242abc1a54d5a2b457a19e07926e9e9856b02dc1719eb1  NET V001
9b2e42f8c1cf2791ce859ca8be64864dbce84d87a357426989db083b0db8641f  NET V002
51724fae30f6985d4dbf0b4df72a47d0c4ede8dfecdabfd8266d9a60e18296ec  NET V003
e8a4e8ff00fa68418210669e391649f2a2bf6d924994817766d3217baaae678e  V002 re-review
```

**CROSS-ROOT CLAIM RE-VERIFIED INDEPENDENTLY.** The build asserts NET V002 and
its lane-1 re-review are byte-identical in both roots. Digested directly in the
SECONDARY root: `9b2e42f8...` and `e8a4e8ff...`. IDENTICAL. **CONFIRMED.**

---

## 2. THE COMPUTATION, RE-RUN INDEPENDENTLY (FRESH VENV)

The build's instrument was NOT reused as evidence. An independent instrument was
written for this audit and run in a fresh venv. **It computes cycle ranks and
first cohomology of ABSTRACT graphs. It computes NO program quantity, NO physical
quantity, and NO constant. It builds no joiner and instantiates nothing.**

```text
ENVIRONMENT  FRESH venv, python 3.9.6, sympy 1.14.0. Exact over Z/Q. No floats.
             Exit code 0, every assertion held.
SCRIPT       o26sr_audit_cas.py
             sha256 63b9f47fd7e551cb7cd9f890e9e3b1e60fc9a3ea946d630dfe67c09ece23fc73
METHOD       TWO INDEPENDENT ROUTES, not one:
               b_1 = dim ker d_1,  d_1 : C_1 -> C_0
               H^1 = dim coker d^0, d^0 = d_1^T : C^0 -> C^1
             The build reported only the homological route. Both are computed
             here and agree everywhere, which is what licenses the build's
             interchange of "H^1" and "b_1" on a 1-complex.
BUILD SCRIPT o26_cas.py located and digested: 545b0c1d5d448c4a074c38895d8d9be
             4427cca9044ec3702e8ec241e24075f25 — MATCHES its declaration.
             It is NOT in the workspace root; the artifact does not say where
             it lives. Recorded, not relied on.
```

```text
RESULT — EVERY LINE OF THE BUILD'S §6 OUTPUT REPRODUCED INDEPENDENTLY

  SIMPLE V=2 E=1   b_1 = 0   AND   H^1 = 0          CONFIRMED
  MULTI  V=2 E=2   b_1 = 1   AND   H^1 = 1          CONFIRMED
  E-V+C  simple 1-2+1 = 0 ; multi 2-2+1 = 1          CONFIRMED
  V=1..8 reciprocal max-degree<=1 counts
         1, 2, 4, 10, 26, 76, 232, 764               CONFIRMED, all b_1 = 0
         total 1115                                  CONFIRMED
  TRIANGLE (V=3, E=3)          b_1 = 1               CONFIRMED
  reciprocal triangle max in-degree 2                CONFIRMED
  directed 3-cycle max in-degree 1                   CONFIRMED
  DIGON antiparallel: in-degree 1 each, b_1 = 1      CONFIRMED
  SELF-EDGE at one node        b_1 = 1               CONFIRMED
```

```text
NET V002's OWN ALGEBRA, RE-DERIVED SYMBOLICALLY RATHER THAN COPIED

  B_2 = [[-1, 1], [1, -1]]                                       (NS2-A5)
  B_2^T (q_12, q_21)^T  =  (-q_12 + q_21,  q_12 - q_21)          (NS2-A6)
  nullspace computed exactly: dimension 1, spanned by (1,1)
  s(1,1) annihilated for symbolic s                              CONFIRMED
  rank(B_2) = V - 1 = 1, the rank-nullity route of (NS2-A3)      CONFIRMED
  dim ker(B_2^T) = E - V + 1 = 1                                 CONFIRMED

THE SUBJECT'S LOAD-BEARING ARITHMETIC IS CORRECT IN BOTH CATEGORIES.
```

**ONE ARITHMETIC FACT THE BUILD USES AND DOES NOT STATE CORRECTLY.** §3.0 asserts
"Under reciprocity, a node's in-degree equals its undirected degree." Re-derived:
in the antiparallel digon each node has IN-DEGREE 1 and UNDIRECTED DEGREE 2. The
equality holds in the simple category only — which is the category the build's
own central find is arguing against. The build does confine the exhaustive
enumeration to the simple category in §6, so nothing downstream breaks; the
sentence in §3.0 is nonetheless false as written.

---

## 3. HUNT (A) — SILENCE READ AS PERMISSION

The commission's own rule: a surface saying nothing about a richer joining is
SILENT, not permissive. The comfortable answer here is SPECIFIC. Tested hardest.

### 3.1 WHAT THE BUILD GOT RIGHT, STATED FIRST

The build states the rule three times and enforces it where it costs it
something. On TEST (b) it declines to read NET V002's naming of "a three-vertex
triangle" as an admission (CL-7), and reports (b) as SILENT. On TEST (c) it
reports SILENT in both directions and counts the licensing side at zero at
bytes. Its §5.2 closes GENERAL with "IT IS AN ABSENCE, NOT A BAR". **On (b) and
(c) the rule is honoured.** That is not the comfortable direction and it took it.

### 3.2 WHERE IT SLIPS — TEST (a), AND P-3

```text
SLIP 1 — THE VERDICT SENTENCE ON (a) CARRIES A NON-BAR AS A REASON.
  §3.1 verdict, at bytes: "ADMITTED — DISPLAYED AT BYTES INSIDE THE JOINER'S
  OWN VERSION FAMILY, WITH ITS CYCLE SPACE COMPUTED. NOT BARRED ANYWHERE.
  NOT ADOPTED."
  The first clause is an affirmative display and is REAL — re-verified here at
  NET V002 :317-342 and re-derived symbolically. The third clause, "NOT BARRED
  ANYWHERE", is a non-bar and does no work the display has not already done;
  carried into a verdict cell it is exactly the move the commission flags.

SLIP 2 — §12's SPECIFIC HALF MAKES THE NON-BAR INFERENTIAL.
  "NOTHING SEALED BARS A SECOND INDEPENDENT CONNECTION. So the stated ground
  of dismissal is not a limit of any joiner on this surface."
  The "So" runs from an absence of prohibition to a conclusion about the
  surface. The affirmative display sits in the PRECEDING sentence and would
  have carried the conclusion on its own; the build chose to hinge on the
  non-bar instead.

SLIP 3 — P-3 IS A "PERMISSION" ITEM WITH NOTHING UNDER IT BUT A NON-BAR.
  §5.1's heading: "WHAT IN THE SEALED SURFACE WOULD ALREADY PERMIT A JOINER
  WHOSE OUTPUT IS NOT CONTRACTIBLE". Under that heading, P-3 reads: the
  self-edge "is barred BY THE RULE'S OWN TEXT ('no self-edge') and by nothing
  on the surface". An item BARRED by the rule and merely not barred by the
  surface is not something the surface PERMITS. P-3's entire content is a
  non-bar. IT DOES NOT BELONG UNDER THAT HEADING.
```

### 3.3 THE DEEPER POINT THE BUILD DOES NOT PUT TO ITSELF

```text
THE HUNT'S OWN STANDARD FOR "PERMITTING" IS LAW STRENGTH, AND THE BUILD
CARRIES THAT STANDARD WITHOUT APPLYING IT TO (a).

  Hunt :349-357, re-read here at bytes: "(b) SEALED-AND-PERMITTING (loops
  admissible, instance unbuilt)?  NOT AT LAW STRENGTH."

  NET V002 is PROPOSED_NOT_ADOPTED at its own bytes (:199, re-read here). The
  build says so itself, repeatedly, and its TOY_SEPARATION says "Nothing here
  asserts that the network sourcing law holds, applies, or is available."

  SO: the (a) display is SEALED, and it is NOT AT LAW STRENGTH — the same
  status the hunt refuses to call "permitting". Entering YES in the ADMITS
  column of §3.4 applies a laxer standard to (a) than the build applies to
  (b) and (c), and than the corpus applies to itself.

CORRECTED CELL FOR (a), on the build's own rule and the hunt's own standard:
  NOT "ADMITS". A SEALED, UNADOPTED DISPLAY EXISTS, AND THE SURFACE IS
  OTHERWISE SILENT. The display is real; the admission is not.
```

**IS THIS A KILL? NO — AND THE REASON IS EXACT.** The commission's kill
condition is a SPECIFIC verdict resting on the surface merely not forbidding
something. The build's SPECIFIC half does not rest there. Its load-bearing
content is a claim about what the hunt's arithmetic CONSUMES — that
`H^1 = 1 - 2 + 1` is blind to the counting convention, which the build
establishes by decomposition and this audit re-derived independently. That claim
survives the correction of (a) untouched: it is a statement about an arithmetic,
not about a permission. **The SPECIFIC half stands as "the dismissal's ground is
convention-dependent." It does NOT stand as "the surface already permits a
non-contractible joiner." The build's §12 asserts the first and drifts into the
second.**

---

## 4. HUNT (B) — THE CAUSE MISIDENTIFIED

The isolation was checked independently, not read off the build.

### 4.1 THE ARITHMETIC DECOMPOSITION IS CORRECT

Re-derived: `H^1 = E - V + C`, inputs E, V, C only; on a 1-complex over a field
`dim H^1 = dim H_1 = E - V + C`, verified here by BOTH routes (§2). None of
"matched carriers", "identity cell matching", "one-tier delay", the transport
`tau`, or the cells appears in it. **CONFIRMED at bytes and by instrument.**

### 4.2 CORRECTION C-4a — THE CAUSE IS A CONJUNCTION, AND THE BUILD'S OWN BODY SAYS SO

```text
§0    "b_1 = 0 is forced by V = 2 ALONE and uses NO content of the rule
       whatsoever."
§2.4  "TWO NODES PLUS SIMPLE COUNTING makes it contractible."
§12   "the COUNTING CATEGORY, not the rule's content ... consumes no content
       of the rule."

THESE DO NOT AGREE. §2.4 is the correct one. V = 2 IS content of the rule:
row `N` proposes "the reciprocal two-node swap" (V001 :242, re-read here), and
the O23SR audit's C3 — which the build adopts wholesale at CL-5 and applies
"throughout" — types "two" at bytes as:

   "'two' is THE CONTENT OF A SELECTION MADE — a property of the rule as
    proposed, sitting in the proposed-content column"
   (O23SR audit :279-281, seal OK, re-read here)

So the build carries a correction establishing that V = 2 is rule content, and
then twice asserts the vanishing uses no rule content. Verified by instrument
(§2, block E): in the simple category b_1 = 0 for EVERY edge set on 2 labelled
vertices — but "simple" and "V = 2" are two premises, one supplied by the hunt's
counting and one selected by the rule.

CORRECTED CAUSE: A CONJUNCTION — the rule's SELECTED two-node scope AND the
hunt's SIMPLE counting. Neither conjunct alone forces it. The build's
substantive point survives: the second conjunct is not fixed by the rule, and
the same family counts it the other way.
```

### 4.3 CORRECTION C-2 — MATERIAL: "CLAUSE A" IS NOT A CLAUSE

This is the sharpest defect found, and it is the mirror image of hunt (A).

```text
WHAT THE SOURCE SAYS. NET V001 :415-417, re-read here at bytes, whole:

  "For several incoming edges, V001 does not define a sum: that would introduce
   a new aggregation law. The instantiated network has two nodes and one
   incoming edge per node, so `(NS-13)` is complete."

THIS IS A STATEMENT OF SCOPE-COMPLETENESS FOR THE INSTANTIATED CASE. It says
V001 DOES NOT DEFINE the several-incoming-edge case. That is SILENCE.

WHAT THE BUILD MAKES OF IT.
  §3.0  "So EVERY node has IN-DEGREE AT MOST ONE."           <- a cap
  §3.0  "Clause A therefore caps undirected degree at 1"     <- a cap
  §3.2  "a reciprocal triangle gives every node in-degree 2, voiding Clause A"
  §6    "TRIANGLE reciprocal : max in-degree 2 -> VOIDS the no-aggregation
         clause"
  §12   "the rule's own two structural clauses — reciprocity, and THE ABSENCE
         of an aggregation law ... together cap undirected degree at 1"

AN ABSENCE THAT CAPS. THE BUILD'S OWN §12 NAMES IT AN ABSENCE IN THE SAME
SENTENCE THAT CALLS IT A STRUCTURAL CLAUSE.

CHECKED AGAINST THE VOID-CONDITION COLUMNS, WHICH ARE WHERE VOIDING LIVES:
  V001 row `N` (:242, quoted whole here): "Voids if reciprocity or E_post
    orientation fails, a cell matching is silently selected outside the
    matched-carrier class, or output feeds the tier that produced it."
  V002 row `N` (:208): "retroaction, E_post reversal, hidden edge/member
    selection".
  NEITHER CONTAINS ANY IN-DEGREE OR AGGREGATION CONDITION. Nothing voids.

CONTRAST — CLAUSE R IS REAL. "Voids if reciprocity ... fails" is in the void
column at bytes. The build's directed-triangle bar is sound. Only Clause A is
manufactured.

CONSEQUENCE, STATED WITHOUT SOFTENING:
  (i) §3.0's "REAL STRUCTURAL RESULT ... NOT THE SOURCE'S" (the matching
      theorem, b_1 = 0 for all V <= 8) is a theorem about a premise the BUILD
      supplied, not about the rule. The instrument confirms the theorem; the
      premise is not of record.
  (ii) §3.2's "BARRED for a RECIPROCAL joiner by the rule's own Clause A"
      should read: UNDEFINED BY V001 FOR THAT CASE — outside the instantiated
      scope, not voided.
  (iii) §12's "SECOND-ORDER, AND REAL" second cause is not of record.
  THE TABLE CELLS DO NOT MOVE: §3.4 already enters BARS = "no" for (b), so the
  scope-test result is unaffected. The defect is in the reasoning offered for
  it, and in a claimed structural result.
```

**AND NOTE THE ASYMMETRY THIS EXPOSES.** On (b) and (c) the build refuses to
read silence as permission. On the aggregation clause it reads silence as
prohibition. The same discipline, applied evenly, removes Clause A.

---

## 5. HUNT (D) — THE CYCLE-STRATUM CLAIM, AND THE SEARCH FOR A MISSED BRIDGE

A missed bridge inverts the finding. Hunted independently, in both roots.

### 5.1 THE STRATA — CONFIRMED AT BYTES

```text
CYCLE CONTENT AT THE AUDIT/PREDICTION STRATUM        CONFIRMED
  census :506-508  "two objects in §1.A (A-1 K_square, A-2 K_L) ARE multi-cell
                    glued complexes with nonzero first cohomology (H^1 = 1 and
                    H^1 = 4 respectively)"                        exact
  census :510-511  "K_square being a prediction carrier and K_L 'the finite
                    translation-complete TEST OBJECT' for audit"  exact
  hunt   :368-369  "neither is sealed as a physical record carrier, so neither
                    discharges the physical-stratum constructor"  exact
  census :216-217  "The exact kernel identity is a separate yield:
                    ker(d_0^dagger)|C_1(K_square) = Q*gamma_j"    exact

PHYSICAL STRATUM CARRIES SINGLE CELLS ONLY, H^1 = 0                CONFIRMED
  hunt G-10 :269-271, re-read here: the five sealed single-cell complexes,
  "dim H^1 = 0 exactly on every one".

THE JOINER IS UNTYPED OF RECORD                                    CONFIRMED
  Independently searched: no sealed stratum typing for NET was found in either
  root. The negative claim holds.
```

### 5.2 CORRECTION C-4b — THE ARGUMENT FROM OMISSION IS WEAKER THAN PRESENTED

```text
THE BUILD SAYS (§4.2): "The hunt's sweep answer enumerates the physical-stratum
position row by row — G-1, G-2, G-3, G-4, G-5, G-8 — and OMITS G-9."

THE SENTENCE AT BYTES (hunt :274-281, re-read whole here) enumerates
OBSTRUCTIONS, not occupants: "...NO glued multi-cell complex exists: composition
is disjoint-only (G-1/G-2), the finite-N law is a tensor row (G-3), the ports are
unanchored (G-4), the connected case is an open obligation with no built instance
(G-5), and C_ref is an unrealized class declaration (G-8)."

G-10 — THE WORKING CLASS, the five sealed single-cell complexes, unambiguously
AT the physical stratum — IS ALSO ABSENT FROM THAT LIST. So "absent from the
enumeration" cannot mean "stratum not of record", or the working class would be
untyped too. The inference does not carry the weight put on it.

THE CONCLUSION SURVIVES ANYWAY, on the independent search of §5.1: nothing
sealed types NET's stratum. The build reached a right answer by a wrong route
and, to its credit, declined to resolve it (CL-8) and flagged it (F-4).
```

### 5.3 THE BRIDGES — THREE SOUGHT, THREE ABSENT. NO MISSED BRIDGE.

```text
BRIDGE 1 (a sealed map audit-object -> working class)     ABSENT. CONFIRMED.
  hunt :324-326 quoted exactly by the build and re-read here, sitting under
  "What the sealed data does NOT determine".

BRIDGE 2 (a rule taking cells and yielding K_square/K_L)  ABSENT. CONFIRMED.
  census :511-513 quoted exactly.

BRIDGE 3 (an artifact placing the two countings side by side)  ABSENT.
  CONFIRMED BY RE-RUNNING THE SWEEP. "contractible object", PRIMARY, at this
  audit's cutoff: 6 files. Two of them are the subject artifact itself and one
  post-cutoff artifact (see §5.5). The remaining FOUR are exactly the four the
  build names: hunt, census audit, O23SR build, O23SR audit. SECONDARY: ZERO.
  The build's count is exact.

TWO CORROBORATING SOURCES THE BUILD DID NOT CONSUME — FOUND HERE, SEALS OK,
BOTH IN-CUTOFF, BOTH STRENGTHENING §4 RATHER THAN INVERTING IT:

  STAGE8_GLUED_TOPOLOGY_CHECK_V001.md (2026-08-13, seal OK)
    :288-289 "NONE_SEALED at the physical write-carrier stratum SURVIVES on the
    strict reading — no complex instance with H^1 > 0 is sealed"; :146 records
    that its own M-1..M-3 "all CORROBORATE the hunt's" list and "both stratum
    verdicts survive". An independent check of the hunt's stratum typing that
    the build never cites.

  STAGE8_COMPOSITION_LOOP_STRUCTURAL_PREDICTION_BUILD_V001.md (seal OK)
    :323-328 carries a bridge NAMED AS UNBUILT:
      composition_loop_to_temporal_plaquette_identity_derived = false | TYPE-S
      would-build: "an explicit carrier-extension or functor from K_square to
      the complete-Qspec temporal two-cell preserving connection and response"
    A would-build is not a bridge. It shows the corpus has NAMED a missing map
    out of K_square — which the build's "THREE INDEPENDENT ABSENCES" does not
    record, and which makes the absence sharper, not softer.

A NEAR-BRIDGE THE BUILD CITES BUT NEVER TESTS AS ONE.
  Census B-4 NOTE :214-217 is the ONE place a per-cell write chain is put in
  relation to C_1(K_square): "Under the entered reading ell_j = gamma_j is
  closed". Tested here: "entered reading" occurs ONCE in the census, at that
  line; it is an entered premise, not a sealed map, and the same note records
  that the yield degenerates. BRIDGE 1 SURVIVES. The build uses B-4 only for
  its degeneracy (G-e) and never asks whether it bridges. It does not.

VERDICT ON HUNT (D): NO MISSED BRIDGE. THE FINDING IS NOT INVERTED.
```

### 5.4 THE STRATUM CONCLUSION, AS THIS AUDIT WOULD STATE IT

The build's plain statement in §4.5 — the record's cycles and the record's
joinings live at different strata with no sealed bridge — **is CONFIRMED**, on
evidence independent of the build's own route, and corroborated by a sealed
source the build never opened.

### 5.5 POST-CUTOFF ARTIFACT, EXCLUDED AND DECLARED

`STAGE8_JOIN_ADMISSIBILITY_O28SR_V001.md` carries the phrase "contractible
object" and postdates the subject artifact by one minute (16:42:27 against
16:41:31, 2026-08-15). It is OUTSIDE the build's declared sweep cutoff. It was
NOT opened and is NOT counted against the build. Recorded so the exclusion is
visible rather than silent.

---

## 6. CORRECTION C-1 — MATERIAL — THE BUILD'S OWN FLAG F-2 IS REFUTED AT BYTES

The build raises F-2 at SEVERITY: MATERIAL, and repeats it as W-2 in §5.1 and
again in §12's "WHY NOBODY HAS A BETTER ONE". It is wrong.

### 6.1 WHAT THE BUILD ASSERTS

```text
F-2   "AN UNEXPLAINED REVERSION IN THE VERSION FAMILY. V002 §4.3 displays cycle
       rank 1 in the multigraph category. V003 :208-214 computes on 'a connected
       tree' and obtains dim C_N = E-V+1 = 0, restricting to a tree BY
       ASSUMPTION. Nothing of record explains the reversion."
W-2   "THE VERSION FAMILY MOVED BACK, NOT FORWARD. ... V003 RESTRICTS TO A TREE
       BY ASSUMPTION. The cycle-carrying reading displayed at V002 is not
       carried forward."
§12   "V003 reverted to a tree by assumption".
```

### 6.2 WHAT V003 CARRIES, AT BYTES, SEAL OK (51724fae..., re-verified)

`STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V003.md`,
§11 "Updated branch comparison — no recommendation", :1002 and :1010-1011,
read at bytes by this audit, verbatim and to the row end:

```text
| Question | Branch A — projective scalar law | Branch B — doubled endpoint access |
...
| Tree scalar | exactly zero | no scalar emitted; access may be nonzero |
| Minimal nonzero carrier | reciprocal two-edge cycle if complete within one declared increment; triangle in simple graph category | one open path with both CTP branch transports |
```

```text
THE CYCLE-CARRYING READING IS CARRIED FORWARD, EXPLICITLY, IN V003's OWN
COMPARISON TABLE, ONE ROW BELOW THE TREE ROW — AND THE TWO GRAPH CATEGORIES ARE
NAMED THERE SIDE BY SIDE: "reciprocal two-edge cycle ... ; triangle in simple
graph category".

V003 :209-215 (the span the build read) is a COROLLARY for the tree case, sitting
beside the tree row of the same table. IT IS NOT A REVERSION. It is the same
two-category structure V002 states, restated at a different place in the
document.

FURTHER, IN-CUTOFF, AT V003's OWN BYTES:
  :54-55  "the reciprocal-loop `x`, `Xi`, and `A_k` remain unchanged"
  :65     "The C5 character happens to agree on that balanced two-edge stage."
  :537    "specific balanced two-edge stage"
V003 COMPUTES ON A TWO-EDGE STAGE THROUGHOUT.
```

### 6.3 HOW THE BUILD REACHED IT — ITS OWN IMPORT AUDIT SAYS

```text
"51724fae30f6985d4dbf0b4df72a47d0c4ede8dfecdabfd8266d9a60e18296ec
   ..._V003.md   USED FOR: :196-214 only (the tree reading, NS3-3, dim C_N = 0)."

A NINETEEN-LINE WINDOW ON A 1000+ LINE DOCUMENT, AND A VERSION-FAMILY VERDICT
DRAWN FROM IT. The build's CL-2 congratulates itself for "SWEPT THE VERSION
FAMILY FOR CONTENT, NOT ONLY FOR STATUS" and for opening "V002 §4.3 and V003 §3
at bytes". It opened V003 §3. It did not open V003 §11, where the answer sits.
The very discipline CL-2 claims — read the content, not the status line — is the
discipline that was not applied to V003.
```

### 6.4 WHAT FALLS, AND WHAT DOES NOT

```text
FALLS   F-2 in full. W-2 in full. The "reverted to a tree by assumption" clause
        of §12's WHY-NOBODY answer.
        Also weakened: W-3's "The two countings have never been placed side by
        side" — V003 :1011 places them side by side inside one table cell, as
        V002 :338-342 already does in prose. W-3's NARROW claim survives and is
        the one that matters: no artifact has read V002 §4.3 AGAINST the hunt's
        G-9. BRIDGE 3 stands (§5.3).

DOES NOT FALL — AND IS STRENGTHENED. The central find. V003's :1011 is a THIRD
        sealed place in the family where the reciprocal two-edge cycle is
        carried as the minimal nonzero carrier, alongside V002 §4.3 and the
        lane-1 re-review §5.1. The build's own thesis — that the family counts
        this network the other way — is better supported than the build knew.
        It flagged as a defect the very evidence that corroborates it.
```

---

## 7. HUNT (F) — QUOTATION INTEGRITY, ENFORCED STRICTLY

Every quotation in the subject was checked against its full source span, to the
span end, character by character. **The load-bearing quotations are clean.**

```text
CLEAN — EXACT TO THE SPAN END, VERIFIED CHARACTER BY CHARACTER

  hunt G-9 :262-267 (the dismissal)                             EXACT, span exact
  NET V001 row `N` :242, quoted as a WHOLE ROW incl. voids      EXACT
  NET V001 :221 edge typing                                     EXACT
  NET V001 :415-417 no-aggregation sentence                     EXACT
  NET V001 NS-8 :340-345 / Adj_2 :446-450 (NS-14)               EXACT
  NET V002 §4.3 body, the LOAD-BEARING FIND                     EXACT
  V002 re-review §5.1 :320-329, to its span end                 EXACT
  V002 §3 row `N` live-alternatives cell                        EXACT
  NET V003 :209-215 tree corollary                              EXACT
  hunt :324-326, :356-357, :364-366, :368-369, :377             EXACT
  hunt G-4 :199-204                                             EXACT
  census :130-131, :142, :216-217, :506-513, :571, :573,
         :606-607, :657, :659-661, :698-700                     EXACT
  MIN_CARRIER :405, :883                                        EXACT
  INDEX_VARYING :515 / :521 / :524 — ellipsis MARKED correctly  EXACT
  O23SR :682 sweep-scope line; O23SR audit C3 :279-281          EXACT

THE PATTERN THE COMMISSION WAS CONVENED OVER — MATERIAL ELIDED FROM INSIDE A
QUOTED SENTENCE, IN THE QUOTER'S FAVOUR — IS NOT PRESENT IN ANY LOAD-BEARING
QUOTATION. NET V002 §4.3 AND THE RE-REVIEW §5.1, THE TWO SPANS THE WHOLE
ARTIFACT RESTS ON, ARE EXACT.
```

```text
THREE FAILURES, ALL MINOR, NONE IN THE QUOTER'S FAVOUR — BUT REPORTED, BECAUSE
THE STANDARD IS STRICT

  F-i   "QUOTED WHOLE" IS FALSE. §3.1 introduces NET V002 §4.3 as "quoted
        whole". §4.3 runs to :347. The quotation stops at :342. The omitted
        final paragraph (:344-347) reads: "No cycle orientation is selected
        physically. `(1,1)` is a calculation representative for the displayed
        edge orientations; reversing an edge sends both its incidence row and
        coefficient sign together, leaving the contraction and kernel class
        unchanged." NOT ADVERSE — it reinforces the non-selection the build
        reports at F-5 — but "quoted whole" is a claim, and it is false.

  F-ii  UNMARKED ELISION INSIDE A QUOTED SENTENCE. §5.2 quotes the hunt's
        trichotomy as: "(b) SEALED-AND-PERMITTING? NOT AT LAW STRENGTH".
        Source :349-350: "(b) SEALED-AND-PERMITTING (loops admissible, instance
        unbuilt)?  NOT AT LAW STRENGTH." The parenthetical is dropped with no
        ellipsis. It is the phrase that says loops are ADMISSIBLE — i.e. the
        material dropped runs AGAINST the build's GENERAL half, not for it.
        Still an unmarked elision from inside a quoted sentence.

  F-iii ONE WORD CHANGED INSIDE A QUOTATION ATTRIBUTED TO THE CENSUS. §5.2 G-a
        renders census O12's yield as "ports become 0-cells of THE complex".
        Census :583-584 reads "ports become 0-cells of A complex". Indefinite
        to definite. Small, and it tilts toward presupposing a complex already
        there for ports to join into — the opposite of what the build argues.
        Corrected of record.

ALSO NOTED, BELOW FAILURE THRESHOLD
  - ADDED EMPHASIS INSIDE A "VERBATIM" QUOTE: §5.2 G-e renders census :215-216
    as "DEGENERATES TO A SINGLE COMMON 0-CELL"; the source is lowercase. Text
    identical; capitalisation is the build's.
  - LINE SPANS RUN A FEW LINES OFF IN SEVERAL CITATIONS: V002 §4.3 cited
    ":312-341", actual body :317-342; V001 no-aggregation cited ":416-419",
    actual :415-417; hunt G-4 cited ":200-206", quoted :199-204; V003 cited
    ":208-214", quoted :209-215. Every quotation still resolves uniquely and
    every text matches. Imprecision, not misdirection.
```

---

## 8. HUNTS (C), (E), (G) — CHECKED, AND CLEAR

### 8.1 HUNT (C) — GENERAL ASSERTED FROM ONE RULE WITHOUT THE SURFACE DISPLAY

**NOT FOUND. This is the build's strongest section.** The GENERAL half does not
generalise from NET's properties. Every one of G-a through G-e is anchored in a
SURFACE-LEVEL display, each re-read at bytes here:

```text
G-a  O12 status ABSENT — census :579-585, "Status of record: ABSENT";
     licensing counted at zero — hunt :356-357, exact.
G-b  O11 status ABSENT — census :571-577; S1 "h_j never displayed as an edge
     sum" — census :573, exact.
G-c  the connected-composition obligation, input class empty — census :698-700,
     exact.
G-d  complete_causal_parent_derived = false — census :657, exact.
G-e  the B-4 degeneracy — census :214-216, exact.

ONLY G-a's OPENING SENTENCE uses a property of the one rule (NET's edge typed
"causal feeding arrow, not E_post endpoint charge"). It is used to say what that
rule does NOT do, and the GENERAL claim is then carried by O11/O12's absence,
which is surface-level and rule-independent. The move the commission watches for
is not made.
```

The GENERAL half is also stated with the correct modality: "IT IS AN ABSENCE,
NOT A BAR: the sealed state is SILENCE, in the corpus's own word." **CONFIRMED.**

### 8.2 HUNT (E) — A JOINER CONSTRUCTED, PROPOSED, OR RECOMMENDED

**NOT FOUND. THE ARTIFACT IS NOT VOIDED.**

```text
SCANNED   the whole artifact for constructive and advocative forms — propose,
          recommend, should, ought, adopt, the fix is, must be. ZERO hits.
CHECKED   §5.1 P-1..P-3 are DISPLAYS of objects already at sealed bytes
          (e_12/e_21 are named in NET V002 :322-323; "larger graph" and
          "self-edge" are in V002's live-alternatives cell :208). Nothing is
          defined here that is not already sealed elsewhere.
CHECKED   §5.2 G-a..G-e are QUOTATIONS of the corpus's own named obligations.
          No construction, no sketch, no costing, no routing.
CHECKED   §6's instrument builds abstract graphs only — vertices and 1-cells —
          and instantiates nothing of the corpus. Re-derived independently here
          with the same result.
```

```text
ONE DRIFT, RECORDED, BELOW THE VOID THRESHOLD.
  P-1's "WHAT IT WOULD COST: nothing new" and "AND IT SURVIVES THE RULE'S OWN
  CLAUSES", read together with §5.1's heading "WOULD ALREADY PERMIT", frame one
  counting as the cheap option. The build's CL-3 expressly declines to select a
  category, and F-5 attaches the non-selection to every ADMITS verdict. The
  drift is tonal and is contradicted by the artifact's own ledger. It is NOT a
  proposal and does NOT void. Reported so it is visible.
```

### 8.3 HUNT (G) — LENS TOKENS AND NUMBERS OF PROGRAM IMPORT

```text
LENS TOKENS                          ZERO. Scanned; no occurrence of any form.
PROGRAM QUANTITIES                   ZERO. The only occurrences of "alpha" and
                                     "kappa" are the three fence declarations
                                     and two filesystem path components.
MEASURED CONSTANTS                   ZERO. No comparison to any measured
                                     quantity anywhere.
NUMERALS, CLASSIFIED EXHAUSTIVELY    sha256 digests; line indices; file counts;
                                     vertex/edge/component counts; cycle ranks
                                     0, 1, 2, 4; the quoted H^1 = 1 and H^1 = 4
                                     of K_square and K_L; enumeration counts
                                     1, 2, 4, 10, 26, 76, 232, 764 and 1115.
                                     EVERY ONE is a quoted index or a pure
                                     combinatorial index. NONE originates as a
                                     program quantity.
GIT                                  None performed by the build; none here.
FENCE-SCAN                           alpha_computed = false, proof_authorized =
                                     false, kappa_record_computed = false.
                                     NO FENCE APPROACHED, in the subject or in
                                     this audit.
```

---

## 9. DECLARED SCOPED SWEEPS AND SWEEP CUTOFF (THIS AUDIT)

```text
SWEEP CUTOFF: 2026-08-15, commission O26SR SCOPE-AUDIT. Filesystem state as of
              this run. Nothing entering either root after this timestamp is
              covered. One in-root artifact postdating the SUBJECT was found and
              excluded by declaration (§5.5).

ROOTS, SWEPT SEPARATELY, COUNTS RE-COUNTED BY THIS AUDIT
  PRIMARY    /Users/bgm/MB Work/alpha-program-archive/workspace
             1,916 *.md files at cutoff — RE-COUNTED, matches the build's §7.
  SECONDARY  /Users/bgm/Documents/New project/gravity_emergence_evidence_program/
             alpha_fundamental_record_action_cleanroom_v003
             2,308 *.md files at cutoff — RE-COUNTED, matches the build's §7.
  NOT SWEPT  .../alpha-program-archive/supervision — out of the declared roots.
             Not listed, not opened.

AUDIT SWEEP A — BRIDGE VOCABULARY (declared, scoped)
  QUESTION   Does any sealed artifact relate K_square/K_L to the working class,
             or yield either from cells?
  SCOPE      PRIMARY, *.md; K_square/K_L co-occurring with "working class" /
             "write-carrier"; and map-forms ("sends ...", "functor from ...").
  RESULT     270 files mention K_square or K_L; 20 co-occur with the working
             class. Opened and seal-verified: GLUED_TOPOLOGY_CHECK_V001,
             COMPOSITION_LOOP_STRUCTURAL_PREDICTION_BUILD_V001, B1_CELL_CHECK.
             NO BRIDGE. Reported at §5.3. The remaining co-occurrences were not
             opened; none is relied on.

AUDIT SWEEP B — THE DISMISSAL PHRASE (declared, scoped)
  SCOPE      Both roots, *.md, exact phrase "contractible object".
  RESULT     PRIMARY 6 = the build's four + the subject itself + one post-cutoff
             artifact. SECONDARY 0. THE BUILD'S SWEEP 3 COUNT IS EXACT.

AUDIT SWEEP C — FACE-IDENTIFICATION VOCABULARY (declared, scoped)
  SCOPE      PRIMARY, *.md, the build's own five patterns.
  RESULT     5 files — MATCHES the build's §7 SWEEP 2 count exactly. One of the
             five is STAGE8_LANE_STATUS.md, a status/tracker-class file: NOT
             OPENED, by rule, in this audit as in the build.

NOT SWEPT, BY RULE
  No register, tracker, road, plan, or continuation file was opened at any
  point. STAGE8_LANE_STATUS.md was identified and skipped.
  "Q-..." items are EXPECTED-UNLOCATABLE; not resolved, not followed, not
  relied on.
```

### 9.1 CORRECTION C-4c — ONE SWEEP DENOMINATOR IS WRONG AT BYTES

```text
§3.3 of the subject: "A corpus-wide sweep of both roots ... returns FIVE files
across 3,613 workspace .md files."

RE-COUNTED HERE: the workspace holds 1,916 .md files. Both roots together hold
4,224. 3,613 IS NEITHER, and it contradicts the build's OWN §7, which states
1,916 correctly. The FIVE is right; the denominator is wrong and internally
inconsistent. No verdict moves — the count of hits, not the size of the haystack,
carries §3.3 — but a stated number that fails at bytes is a stated number that
fails at bytes.
```
