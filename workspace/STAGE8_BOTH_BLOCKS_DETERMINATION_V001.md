# STAGE 8 / CROSS-BLOCK / BOTH-BLOCKS — THE F=0 SCOPE, THE COMPOSITION QUESTION, AND THE K_3 AND-DECISION

Lane: FABLE blind determination, codename BOTH-BLOCKS. Three exact sub-questions of
record settled; one net stated. Date: 2026-08-13.
ALL_RESULTS = CLAIMED until checked.

## Lead determination — CLAIMED

1. **F0_SCOPE = TRACE-CARRIER-SPECIFIC.** The sealed sentence "No filled `2`-cell is
   included in the trace carrier" (V011 :1881-1882) binds, by its own definite
   article and placement, to ONE object: the composition-loop prediction's trace
   carrier `C_0(K_square) ⊕ C_1(K_square)` — the complex the entered Attach map's
   θ_j is read on. It is not a universal property of admitted physical cells: the
   working-class cube cell carries F = 24 filled faces OF RECORD, and its faces are
   typed load-bearing by sealed text (V011 :301-303, :306-313, :1322-1355). Clean
   division of roles, no contradiction: TRACE CARRIER (θ_j read; F = 0) vs RESPONSE
   CARRIER (R_record,L on F_phys = im(d_1); F > 0 licensed and required).
2. **COMPOSITION = UNBUILT-UNSPECIFIED.** No sealed text bars a complex carrying both
   features — K_L itself is a SEALED both-features complex (H^1 = 4 with 6L^4 filled
   plaquettes), sealed at the AUDIT stratum. No sealed text licenses one at the
   PHYSICAL write-carrier stratum either: composition of record is disjoint-only,
   the connected case is an open Gate-5 obligation, and the ports are unanchored —
   the O11/O12/O-D1 gap again. The barrier is absence, not prohibition.
3. **K3_BLOCKS = 80 / 240 / 4 EXACT** (gauge / flux / harmonic), verified by
   two-sided exact sandwiches, not assumed. **K3_AND = TYPING-DEPENDENT, and
   SATISFIABLE under every typing that leaves the harmonic sector reachable:**
   explicit exact witnesses exhibited with BOTH components nonzero (unconstrained:
   `c_0 + d_1^†δ_f`; circuit-local: the coordinate 3-circuit cochain, exact split
   `φ_H = c_0/27`, `φ_f = λ − c_0/27`). The ONE typing that kills the AND is strict
   plaquette-locality — exactly the working-class collapse mechanism. The
   conjunction is NOT algebraically obstructed.
4. **NET: no contradiction anywhere.** What stands between the record and a
   both-blocks PHYSICAL carrier is a construction plus two supplies (§4).

---

## 0. Preflight, seals, and fences

Output name probed before any write: `STAGE8_BOTH_BLOCKS_DETERMINATION_V001.md`
ABSENT (artifact and sidecar). No register, tracker, plan, road, ledger, or lens
file read. Every digest below recomputed from bytes at path by `shasum -a 256` /
hashlib THIS session, BEFORE reliance.

```text
SEALS VERIFIED (recomputed at path):
a3bedc7ef092ebd932d254331dc10053aa123334a013b95fb1e15019bdfea016  STAGE8_B1_CELL_INSTANCE_V001.md                   MATCH
86b6134d9ae9ad646da2c67f8d9477677effc8aee69824c0b27bce48cfb13b37  STAGE8_B1_CELL_CHECK_V001.md                      MATCH
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md                     MATCH (the entered premise)
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md  MATCH (the supply-mandate DoR)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md    MATCH (the entered artifact)
5154c203ee237c4dee1b02176f8fa64c84b3ec97e561b720304f1a58219ad29b  STAGE8_ATTACH_CANDIDATE_V007_CHECK_CODEX2_V001.md MATCH (the certifying check)
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md     MATCH (V011)
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md                MATCH (the hunt/census)
a22ef820c5e665cbf9a5c941d24be57efb06c9c921ee579e5291d0cdc65feaa3  STAGE8_GLUED_TOPOLOGY_CHECK_V001.md               MATCH (vs own sidecar at path)
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md             MATCH (the method; the working class)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md               MATCH (the write constraints, Q6)
5e49d2093d4ee17b840cc096d5caf3a6edafda4e37abbd175a7a371c34658f37  STAGE8_R_RECORD_L_FORM_FABLE_V001.md              MATCH (FORM D1 block definitions)
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md                   MATCH (GC — matches AND §0's pinned digest)

V011 BYTE SPANS, sha256 of the span recomputed before extraction:
[29318,30557) 7d81c132980e65bc0b318036215075aa51d41f660efe58f55ab3542eed29d3c9  MATCH (K_L presentation; lines 826-862)
[30559,30843) 96e164afdbddafea6f26832db83b8ebd69de637e0d8994b9e58bb63872212370  MATCH (frozen plaquette boundary word; lines 864-875)
[44530,45446) 9bbd9525cbd84a6b6365383da6cd6e38d33f945e01c98c1a0d3052f322b74efc  MATCH (tangent complex A^0/A^1/A^2, F_phys; lines 1322-1355)
[58559,58704) 7103582e1eae96b0b0bd89180ccc7b25d4a296c7a419815811175effab8e743c  MATCH (Gate-5 pass line; line 1740)
[80185,80240) a6cc611f2f9c616235dcd1b230fe50ecf3ccff7a8e35fe07cb651e5881eddbf8  MATCH — SPAN-DRIFT NOTED: the hunt
    cites this digest at V011 [73992,74047) (hunt :91); the bytes there do NOT carry it.
    The exact 55-byte content "periodic_translation_complete_test_object_frozen = true"
    is present at [80185,80240) (V011 line 2241) and hashes to a6cc611f... EXACTLY.
    Content verified at the corrected offset; digest exact; reliance is on the content.
UNVERIFIABLE: none. Every relied-on file and span verified; one stale offset corrected.
```

FENCES HELD: exact structural algebra only (integer/rational; the two rational
coefficients 3/81 and 1/27 are exact Fractions); NO physical quantity computed,
bounded, or evaluated; no scale, no imported GR, no faithfulness authority; continuum
content not consumed (the Gate-5 pass line is read for its obligation TYPE only);
register/tracker/plan/road/ledger/lens families untouched. K_3 is handled as an
AUDIT-stratum object throughout; every typing choice on it is explicit and flagged.

PROVENANCE: the b1-cell instance and the Attach lineage are ENTERED-AUTHORED-PREMISE
material (addendum 420ab02f); every claim of §1 and §4 that leans on them is TYPE-P
on that entry and voids with it. The K_3 computation (§3) leans only on V011's sealed
K_L law and the sealed method files — it is not TYPE-P on the Attach entry.

---

## 1. QUESTION 1 — THE F=0 SCOPE, decided at the exact words

### 1.1 The mandate's sealed words, quoted at spans

V011 :1840 (the object the clause lives with — the composition-loop prediction's
complex, opened by "## Fully specified composition-loop prediction" and "This is a
new forward mathematical consequence, not an empirical holdout"):

> Use the unfilled oriented `1`-skeleton `K_square` with no `2`-cell and:

V011 :1881-1882 (the operative sentence, in the same construction, immediately
before `B_square(Phi)` is defined "on the full eight-dimensional space
`C_0(K_square) direct-sum C_1(K_square)`"):

> Use the Hilbert forms produced by Gate 3. No filled `2`-cell is included in
> the trace carrier.

V011 :2184 (the void-trigger line that names the trace carrier as a frozen COMPONENT
OF THAT PREDICTION, alongside its loop complex, gauge, phase, and output list):

> the loop complex, gauge, phase, trace carrier, or output list changes;

Addendum 420ab02f :33-34 (the operative-domain clause — it types the Attach map's
DOMAIN, in b_1, and says nothing about faces of cells in general):

> OPERATIVE DOMAIN: connected admitted
> b₁ = 1 cells (trees inert by the record's own determination; b₁ > 1 out of scope).

### 1.2 The grammatical and structural scope

The operative noun is "the trace carrier" — definite article, bound to the object
just defined: `B_square(Phi)` on `C_0(K_square) ⊕ C_1(K_square)`. The sentence
excludes filled 2-cells from THAT carrier. Nothing in the sentence, its section, or
the void-trigger list quantifies over "admitted physical cells"; the void trigger
(:2184) treats "trace carrier" as one frozen ingredient of one prediction. The
addendum's clause types the Attach map's domain ("connected admitted b₁ = 1 cells");
its supply chain then pins the concrete sector: V007 :162-164 "connected admitted
cells with b_1 = 1 — the specified square sector, member 10 :1840-1852" — which
resolves (per the b1-cell instance's note N-1, verified by its check) to exactly the
V011 :1840-1851 display: K_square, V = 4, E = 4, F = 0. So of record: the F = 0
exclusion attaches to the ATTACH TRACE CARRIER, the object θ_j is read on.

### 1.3 The cross-examination — the OTHER sealed cell

The working-class cube cell HAS filled faces of record: parent K census
V/E/F = 16/32/24 with rank d_1 = 17 (AND 38bbb9fc :192-199, the sealed result table;
certified there by exact sandwich + Fraction RREF). Which sealed text types ITS
faces:

V011 :301-303 (the general cell category — filled faces with CURVATURE are
deliberately admitted):

> No flatness around a filled two-cell is imposed. In dimension one this is the
> usual rank-one local-system data; in dimension two it is deliberately called
> a discrete unitary connection, because nontrivial face holonomy must remain
> admissible.

V011 :306-313 (the coefficient carriers — degree 2 is a first-class carrier):
`C_2(K) = direct-sum_(f in K_2^+) C |f>`.

V011 :1322-1355 = byte span [44530,45446) (digest 9bbd9525, "Tangent complex and
unique flux lift" — the ratified finite-N response functional's own carrier):
`A_R^2(K_L) = R^(K_2^+)`; `(d_1 a)_f = sum_(e in boundary f) incidence(f,e) a_e`;
`F_phys = im(d_1)`; the flux lift `Q_flux`; and the generated face response
`mathcal_K_L(xi,zeta) = R_record,L(Q_flux xi, Q_flux zeta), xi,zeta in F_phys`.
The response functional is DEFINED on the image of d_1 inside the face space:
filled faces are not merely permitted on the response carrier — the ratified
functional's flux sector is empty without them.

### 1.4 Verdict and the contradiction question

**F0_SCOPE = (b) TRACE-CARRIER-SPECIFIC.** Support for reading (a) UNIVERSAL: none
found at any sealed span — no sealed sentence extends the exclusion beyond "the
trace carrier", and a universal reading would contradict the sealed working-class
table (F = 24, rank d_1 = 17) plus V011 :301-303's explicit admission. The two
sealed cells are a CLEAN DIVISION OF ROLES, both sealed, non-overlapping:

```text
role              object      F     sector carried        sealed typing
TRACE CARRIER     K_square    0     harmonic (H^1 = 1)    V011 :1840, :1881-1882 (exclusion)
RESPONSE CARRIER  cube cell   24    flux (im d_1^†, 17)   V011 :301-313, :1322-1355 (admission + use)
```

NO CONTRADICTION OF RECORD. One structural remark makes the division canonical
rather than arbitrary (proof in §3.5): on any connected complex,
`H^1 = b_1(1-skeleton) − rank d_1` (because d_1's rows are cycles). At b_1 = 1,
any face with a Q-nonzero boundary word forces H^1 = 0 — so WITHIN the Attach
operative domain, filled faces are either Q-inert or harmonic-killing. The trace
carrier's F = 0 is the only shape that keeps θ_j readable; the sealed text chose
the only workable census.

---

## 2. QUESTION 2 — COMPOSITION: both features in one complex

Searched: any sealed bar or license on a complex carrying non-contractible 1-cycles
AND filled 2-cells simultaneously.

### 2.1 No sealed bar — a sealed both-features complex already EXISTS

K_L (V011 :826-862, span 7d81c132; boundary word :864-875, span 96e164af): "the
oriented 2-skeleton of (Z/LZ)^4, L >= 3" with "every elementary plaquette" — census
L^4 / 4L^4 / 6L^4, so 6L^4 FILLED 2-cells (each with the frozen nonzero boundary
word and a traceable face curvature), and H^1 = 4 EXACT at L = 3, 4 (hunt :298-317,
certified; reproduced independently in §3). K_L is sealed, frozen
(`periodic_translation_complete_test_object_frozen = true`, V011 :2241), and carries
BOTH features. Its sealed role: "the finite translation-complete test object" "for
the filtration audit" — an AUDIT object (hunt :238-251). No sealed text anywhere
read this session bars such a complex; the one candidate sentence (V011 :1881-1882)
is carrier-specific (§1).

### 2.2 No physical-stratum license — the gap is the same O11/O12 gap

At the PHYSICAL write-carrier stratum (where the admissible space and block split
are defined):

- Composition of record is DISJOINT-ONLY: "K = disjoint_union_i K_i, H(K) =
  tensor_i H(K_i)" (hunt G-1 :165-173); the finite-N object is a tensor row with
  EMPTY inter-cell incidence (G-3).
- The connected case is an OPEN OBLIGATION, not a law: the Gate-5 pass line
  REQUIRES "the many-record amplitude to be monoidal on disjoint cells and to
  possess a local linked-cluster thermodynamic density on connected cellulations"
  (V011 :1740, span 7103582e) — obligation language pointing AT connected
  complexes; `connected_linked_cluster_density_proved = false` stands.
- The ports are unanchored: "their identification with 0-cells of the complex is
  not displayed"; "NOTHING licenses identifying any out-port with any in-port ...
  and nothing forbids it" (hunt G-4 :193-204) — "exactly O11 + O12 of POSED, both
  UNBUILT."
- AND :344-346: "the multi-cell/exhaustion side is UNBUILT (charter O-D1; port
  anchoring O12)"; and no sealed map connects the audit objects to the write
  carrier: "no sealed map sends either audit object onto the working class or vice
  versa" (hunt :324-326).
- The AND §7 reopening clause (AND :337-348) even anticipates the both-features
  physical complex prospectively ("A sealed record complex with dim H > 0 ...
  would ... REOPEN the AND-class question at that level") — a license SHAPE, not
  a bar.

### 2.3 Verdict

**COMPOSITION = UNBUILT-UNSPECIFIED.** Nothing sealed bars the both-features
complex (one exists sealed, at the audit stratum: K_L). Nothing sealed licenses one
as a physical carrier (glued check :288-291: "NONE_SEALED at the physical
write-carrier stratum"). The barrier is the unbuilt O11/O12/O-D1 construction and
the unwritten gluing rule — absence, not prohibition.

---

## 3. QUESTION 3 — THE K_3 AND-DECISION (the concrete test)

### 3.1 The sealed law and the exact build

Built EXACTLY from the sealed presentation (V011 spans 7d81c132 + 96e164af,
verified at bytes): vertices (Z/3Z)^4; four edge directions e_mu(x): x -> x+mu with
periodic indices; every elementary plaquette f_(mu nu)(x), mu < nu, with the FROZEN
boundary word verbatim:

> partial_2 f_(mu nu)(x) = e_mu(x) + e_nu(x+mu) - e_mu(x+nu) - e_nu(x).

d_0/d_1 per the sealed tangent laws (span 9bbd9525): `(d_0 lambda)_e =
lambda_t - lambda_s`; `(d_1 a)_f = sum_(e in boundary f) incidence(f,e) a_e`.
Census: **V = 81, E = 324, F = 486** — as expected, and matching the sealed
L^4/4L^4/6L^4 at L = 3.

### 3.2 The three block dimensions — EXACT, two-sided (script §5)

```text
gauge     dim im(d_0)        = rank_Q d_0 = 80    d_0.(constants) = 0 gives <= 80;
                                                  rank_p d_0 = 80 at 1000003/1000033/1000037
                                                  gives >= 80.  EXACT.  (BFS: connected.)
flux      dim im(d_1^dagger) = rank_Q d_1 = 240   the four direction cocycles c_mu (1 on every
                                                  e_mu(x)) satisfy d_1 c_mu = 0 AND bdry c_mu = 0
                                                  exactly, Gram 81*I_4 => dim ker d_1 >= 84 =>
                                                  rank_Q d_1 <= 240; rank_p d_1 = 240 at all
                                                  three primes gives >= 240.  EXACT.
harmonic  dim H              = 4                  H = ker(d_1) ∩ im(d_0)^perp (FORM D1, counting
                                                  metric); dim = (324-240) - 80 = 4; the four
                                                  exhibited members c_0..c_3 meet the dimension,
                                                  so {c_0,c_1,c_2,c_3} IS an exact orthogonal
                                                  basis of H.
sum check: 80 + 4 + 240 = 324 = E EXACT.   d_1 d_0 = 0 entrywise over Z.
```

**K3_BLOCKS = 80 / 240 / 4, verified — agrees with the hunt's sealed census row
(hunt :298-301) by an independent instrument set** (certificate sandwiches here;
Fraction RREF there).

### 3.3 Typing the write constraints onto K_3 — every choice explicit

The sealed constraints (G3 Q6 :130-135; GC :82-91): `ell` gauge-invariant =
conserved-typed (both exactly `⊥ im(d_0)`, i.e. `bdry.ell = 0` — GC: one condition,
not two), cell-local, total-nonzero for n != 0. The block split is NOT fixed by the
record (GC: "It ... imposes NOTHING on the split"). **The sealed text supplies NO
definition of "cell" or "cell-local" on K_3** — K_3 is an audit object; G3's
locality is defined on the working-class write carrier, and the b1-cell instance
satisfied it trivially (its whole complex was one cell). Three typings, run
separately, each minimal and flagged:

```text
T0  UNCONSTRAINED      no cell-local clause at all (the honest "no sealed basis" run).
T1  PLAQUETTE-LOCAL    "cell" = one filled 2-cell f with its closure; "cell-local" =
                       support inside f's 4 boundary edges.  (The working-class shape:
                       each single filled cell.)
T2  CIRCUIT-LOCAL      "cell" = a connected b_1 = 1 subcomplex (the Attach entry's own
                       cell shape, addendum :33-34); minimal noncontractible instance
                       in K_3 = the coordinate 3-circuit gamma_0 = {e_0(0), e_0(100...),
                       e_0(200...)}; "cell-local" = support on that circuit's edges.
```

### 3.4 The decisions (all exact; script §5)

```text
T0: SATISFIABLE.  WITNESS ell = c_0 + w,  w := d_1^dagger(delta_{f_01(0)}) (the transpose
    column = the boundary word as an edge vector).
    VERIFIED EXACTLY: bdry.ell = 0 (conserved = gauge-invariant); ell != 0 (total-nonzero);
    w ⊥ c_mu for all mu (so w ⊥ H against H's exact basis) => phi_H(ell) = c_0 != 0;
    <c_0, d_1^dagger y> = <d_1 c_0, y> = 0 => c_0 ⊥ im(d_1^dagger) => phi_f(ell) = w != 0.
    BOTH components nonzero simultaneously: phi_f != 0 AND phi_H != 0.  AND = TRUE.

T1: UNSATISFIABLE.  The on-cell conserved cochains are EXACTLY k.w (the restricted null
    space has nullity 1 — computed by Fraction RREF on the 4 support columns, and the
    basis vector IS the boundary word).  w ∈ im(d_1^dagger) and w ⊥ H, so phi_H ≡ 0 on
    the whole admissible set: the harmonic sector is dead on-cell.  AND = FALSE — the
    working-class collapse reproduced mechanism-exactly (a single filled cell is
    contractible; its only conserved cochain is its own boundary).

T2: SATISFIABLE — with a cell-local witness.  WITNESS lam = the 3-circuit cochain
    (support = 3 edges, entries +1).
    VERIFIED EXACTLY: bdry.lam = 0; lam != 0; <lam, c_0> = 3.
    EXACT DECOMPOSITION (H's exact orthogonal basis makes the projections closed-form):
      phi_H(lam) = (3/81) c_0 = c_0/27      != 0   (pairing 3 against a Gram-81 basis member)
      phi_f(lam) = lam - c_0/27             != 0   (verified: ⊥ H, ⊥ im(d_0) — hence in
                                                    im(d_1^dagger) by FORM D1's split — and
                                                    d_1(lam - c_0/27) = d_1 lam has 18
                                                    nonzero entries, so it is nonzero)
    BOTH components nonzero simultaneously.  AND = TRUE under the Attach-shaped locality.
```

**K3_AND = TYPING-DEPENDENT: SATISFIABLE under T0 and T2 (explicit exact witnesses,
conserved, gauge-invariant, total-nonzero, both block components nonzero);
UNSATISFIABLE under T1.** Said honestly: the verdict depends on an under-determined
typing choice — the sealed text gives no basis for "cell-local" on K_3 — but the
dependence has a clean shape: the conjunction fails ONLY under strict
plaquette-locality, the exact mechanism of the working-class collapse. Every typing
that lets one admissible write touch both a non-bounding cycle and a filled face
satisfies the AND. The flux/harmonic conjunction is NOT algebraically obstructed;
both prior collapses (working class: H = {0}; b1-cell: flux = {0}) are recovered as
the two degenerate single-sector restrictions of this one complex.

### 3.5 The structural proposition behind all three results

On any connected complex, d_1 d_0 = 0 makes every row of d_1 a cycle, so
`rank d_1 <= b_1(1-skeleton)` and `H^1 = b_1(1-skeleton) − rank d_1`. Hence BOTH
blocks nonzero (H^1 >= 1 AND rank d_1 >= 1) forces **b_1(1-skeleton) >= 2**.
Instantiated: K_square 1 − 0 = 1 (harmonic only); one filled plaquette 1 − 1 = 0
(flux only); K_3 244 − 240 = 4 (both). A both-blocks carrier is impossible at the
Attach entry's b_1 = 1; K_3 is the sealed corpus's smallest certified instance of
the b_1 >= 2 regime.

---

## 4. QUESTION 4 — THE NET, stated honestly

**No contradiction.** The F = 0 mandate is carrier-specific of its own words (§1);
the both-features complex is sealed at the audit stratum and unbarred (§2); the AND
is exactly satisfiable on K_3 under non-degenerate typings (§3). What stands between
the record and a both-blocks PHYSICAL carrier is ONE CONSTRUCTION PLUS TWO SUPPLIES:

```text
N-1  THE CONSTRUCTION (the O11/O12/O-D1 gap, §2): a derived/ratified CONNECTED
     multi-cell physical complex with b_1(1-skeleton) >= 2 (forced by §3.5) and
     filled faces.  Nothing sealed builds, derives, or anchors one; the Gate-5
     obligation points at it; the disjoint-only composition cannot produce it
     (H^1 additive over coproducts of H^1 = 0 cells).
N-2  SUPPLY (scope, the Attach side): the entered Attach map stops at b_1 = 1 —
     "DISPOSITION OF b_1 > 1: OUT OF SCOPE ... would require a lawful cycle
     selector, which this artifact does not supply and may not freeze"
     (V007 :170-171).  On any both-blocks carrier (b_1 >= 2 by §3.5), theta_j's
     cycle class is no longer unique: reading the harmonic sector there requires
     either a target-independent derivation of the selector or a new
     principal-ratified entry.  A scope RULING on F = 0 is NOT needed — the sealed
     words already decide §1; what is needed is a SUPPLY at b_1 > 1.
N-3  SUPPLY (typing, the write side): "cell-local" is undefined of record on any
     multi-cell carrier (§3.3).  The K_3 result shows the AND verdict turns on
     exactly this choice; the typing rule must be written (derived or ratified)
     before any both-blocks satisfaction can be claimed of record rather than
     exhibited on an audit object.
```

Honest placement of this artifact's own K_3 result: an AUDIT-stratum structural
fact. It removes the "algebraically impossible" objection to the cross-block and
localizes the failure mechanism (plaquette-locality), but it makes nothing
physical: NONE_SEALED at the physical write-carrier stratum stands (glued check
:288-291), the three carried Attach conditions stand (addendum :43-47), and the
post-limit and physical-domain questions are untouched.

---

## 5. SCRIPT, VERBATIM (`k3_both_blocks_v001.py`, Python 3 stdlib only)

```python
#!/usr/bin/env python3
# k3_both_blocks_v001.py — BOTH-BLOCKS: build K_3 = oriented 2-skeleton of (Z/3Z)^4
# EXACTLY from the sealed law (V011 20a3a17d spans [29318,30557) and [30559,30843):
# "K_L = the oriented 2-skeleton of (Z/LZ)^4, L>=3", four edge directions, every
# elementary plaquette, periodic indices; frozen boundary word
#   partial_2 f_(mu nu)(x) = e_mu(x) + e_nu(x+mu) - e_mu(x+nu) - e_nu(x);
# d_0/d_1 laws per the sealed tangent complex (V011 span [44530,45446)):
#   (d_0 lambda)_e = lambda_t - lambda_s ;  (d_1 a)_f = sum incidence(f,e) a_e.
# Exact integer/rational arithmetic only (fractions for the two exact rational
# coefficients); mod-p ranks are used ONLY inside two-sided exact sandwiches whose
# other side is an exact integer certificate. No physical quantity is computed,
# bounded, or evaluated. Python 3 stdlib only.
from fractions import Fraction
from itertools import product as iproduct

L = 3
DIRS = range(4)
PAIRS = [(0, 1), (0, 2), (0, 3), (1, 2), (1, 3), (2, 3)]  # mu < nu

VERTS = list(iproduct(range(L), repeat=4))
vidx = {x: i for i, x in enumerate(VERTS)}
def shift(x, mu):
    y = list(x); y[mu] = (y[mu] + 1) % L; return tuple(y)

# edges e_mu(x): source x -> target x+mu ; index = mu*81 + vidx(x)
EDGES = [(mu, x) for mu in DIRS for x in VERTS]
eidx = {e: i for i, e in enumerate(EDGES)}
# faces f_(mu nu)(x), mu < nu ; index = pair*81 + vidx(x)
FACES = [(mu, nu, x) for (mu, nu) in PAIRS for x in VERTS]

nV, nE, nF = len(VERTS), len(EDGES), len(FACES)
assert (nV, nE, nF) == (81, 324, 486)
print('CHECK 0  census V=%d E=%d F=%d  == sealed L^4 / 4L^4 / 6L^4 at L=3' % (nV, nE, nF))

# d_0 : C^0 -> C^1, rows = edges (sparse): (d_0 lambda)_e = lambda_target - lambda_source
d0_rows = []
for (mu, x) in EDGES:
    d0_rows.append(((vidx[shift(x, mu)], +1), (vidx[x], -1)))
# d_1 : C^1 -> C^2, rows = faces (sparse), the FROZEN boundary word verbatim:
#   +e_mu(x)  +e_nu(x+mu)  -e_mu(x+nu)  -e_nu(x)
d1_rows = []
for (mu, nu, x) in FACES:
    d1_rows.append(((eidx[(mu, x)], +1), (eidx[(nu, shift(x, mu))], +1),
                    (eidx[(mu, shift(x, nu))], -1), (eidx[(nu, x)], -1)))

# CHECK 1: d_1 d_0 = 0 entrywise, exact integers (sparse composition)
for frow in d1_rows:
    acc = {}
    for (e, se) in frow:
        for (v, sv) in d0_rows[e]:
            acc[v] = acc.get(v, 0) + se * sv
    assert all(c == 0 for c in acc.values())
print('CHECK 1  d_1 d_0 = 0 entrywise over Z (all %d x %d composite entries zero)' % (nF, nV))

# CHECK 2: d_0 . (constants) = 0  =>  rank_Q d_0 <= V - 1 = 80  (exact upper bound)
assert all(sum(s for (_, s) in row) == 0 for row in d0_rows)
print('CHECK 2  d_0 . (constants) = 0  =>  rank_Q d_0 <= V-1 = 80')

# CHECK 3: connectivity by BFS (no linear algebra)
adj = {v: [] for v in VERTS}
for (mu, x) in EDGES:
    y = shift(x, mu); adj[x].append(y); adj[y].append(x)
seen, stack = set(), [VERTS[0]]
while stack:
    v = stack.pop()
    if v in seen: continue
    seen.add(v); stack.extend(adj[v])
assert len(seen) == nV
print('CHECK 3  BFS: connected (components = 1)')

# exact mod-p rank (forward elimination; used only inside two-sided sandwiches)
def rank_mod_p(sparse_rows, ncols, p):
    rows = []
    for r in sparse_rows:
        d = [0] * ncols
        for (c, s) in r:
            d[c] = (d[c] + s) % p
        rows.append(d)
    rank, prow = 0, 0
    m = len(rows)
    for col in range(ncols):
        piv = None
        for i in range(prow, m):
            if rows[i][col] % p:
                piv = i; break
        if piv is None: continue
        rows[prow], rows[piv] = rows[piv], rows[prow]
        inv = pow(rows[prow][col], p - 2, p)
        R = rows[prow] = [(v * inv) % p for v in rows[prow]]
        for i in range(prow + 1, m):
            f = rows[i][col]
            if f:
                ri = rows[i]
                rows[i] = [(a - f * b) % p for a, b in zip(ri, R)]
        prow += 1; rank += 1
        if prow == m: break
    return rank

PRIMES = (1000003, 1000033, 1000037)  # the method of record's three instrument primes

r0p = [rank_mod_p(d0_rows, nV, p) for p in PRIMES]
assert all(r == 80 for r in r0p)
GAUGE = 80  # rank_p <= rank_Q <= 80 (CHECK 2) and rank_p = 80  =>  rank_Q d_0 = 80 EXACT
print('CHECK 4  rank_p d_0 = %s @ p=%s; sandwich with CHECK 2 => rank_Q d_0 = 80 EXACT' % (r0p, list(PRIMES)))

# CHECK 5: the four direction cocycles c_mu (1 on every e_mu(x), else 0) lie in
#          H = ker(d_1) ∩ im(d_0)^perp, exactly — integer certificates, no elimination
c = []
for mu in DIRS:
    v = [0] * nE
    for x in VERTS:
        v[eidx[(mu, x)]] = 1
    c.append(v)
for mu in DIRS:
    # d_1 c_mu = 0: every frozen boundary word carries +e_rho(.) and -e_rho(.) per direction
    assert all(sum(s * c[mu][e] for (e, s) in frow) == 0 for frow in d1_rows)
    # c_mu ⊥ im(d_0): <c_mu, d_0 delta_v> = (bdry c_mu)_v = 0 for every vertex (telescoping)
    bd = [0] * nV
    for j, (e, x) in enumerate(EDGES):
        for (v, s) in d0_rows[j]:
            bd[v] += s * c[mu][j]
    assert bd == [0] * nV
gram = [[sum(a * b for a, b in zip(c[m], c[n])) for n in DIRS] for m in DIRS]
assert gram == [[81 if m == n else 0 for n in DIRS] for m in DIRS]
print('CHECK 5  c_0..c_3: d_1 c_mu = 0 and bdry c_mu = 0 exactly; Gram = 81*I_4 =>')
print('         4 independent members of H  =>  dim ker d_1 >= 80 + 4 = 84  =>  rank_Q d_1 <= 240')

r1p = [rank_mod_p(d1_rows, nE, p) for p in PRIMES]
assert all(r == 240 for r in r1p)
FLUX = 240  # rank_p <= rank_Q <= 240 (CHECK 5) and rank_p = 240 => rank_Q d_1 = 240 EXACT
print('CHECK 6  rank_p d_1 = %s @ p=%s; sandwich with CHECK 5 => rank_Q d_1 = 240 EXACT' % (r1p, list(PRIMES)))

# CHECK 7: the three blocks (FORM D1: A^1 = im(d_0) ⊕ H ⊕ im(d_1^dagger), counting metric)
HARM = (nE - FLUX) - GAUGE   # dim H = dim ker d_1 - dim im d_0  (im d_0 ⊆ ker d_1 by CHECK 1)
assert GAUGE + HARM + FLUX == nE and HARM == 4
print('CHECK 7  blocks: gauge dim im(d_0) = %d, harmonic dim H = %d, flux dim im(d_1^dagger) = %d;'
      % (GAUGE, HARM, FLUX))
print('         sum %d + %d + %d = %d = E EXACT; dim H = 4 = number of exhibited members' % (GAUGE, HARM, FLUX, nE))
print('         =>  { c_0, c_1, c_2, c_3 } IS an exact orthogonal basis of H (Gram 81*I_4)')

# helper: exact inner products / maps (integers or Fractions — both exact)
def dot(u, v): return sum(a * b for a, b in zip(u, v))
def bdry_apply(u):
    out = [u[0] * 0] * nV     # zero of the entry type (int 0 or Fraction 0)
    for j in range(nE):
        if u[j]:
            for (v, s) in d0_rows[j]:
                out[v] = out[v] + s * u[j]
    return out
def d1_apply(u):
    return [sum(s * u[e] for (e, s) in frow) for frow in d1_rows]

# ---------------------------------------------------------------------------
# THE AND-DECISION, per typing choice (write constraints per G3 Q6 / GC:
#   conserved-typed == gauge-invariant == bdry.ell = 0 (⊥ im d_0);
#   total-nonzero: phi_f + phi_H != 0;  cell-local: NO sealed basis on K_3 —
#   three explicit typings run separately)
# ---------------------------------------------------------------------------
print()
f_star = 0  # face f_(01)(0,0,0,0): the first face row
w = [0] * nE
for (e, s) in d1_rows[f_star]:
    w[e] += s        # w = d_1^dagger delta_f*  (counting metric: the transpose column)

# --- TYPING T0: UNCONSTRAINED (no cell-local clause) -----------------------
ell0 = [a + b for a, b in zip(c[0], w)]
assert bdry_apply(ell0) == [0] * nV                     # conserved / gauge-invariant
assert any(v != 0 for v in ell0)                        # total-nonzero
assert all(dot(w, c[mu]) == 0 for mu in DIRS)           # w ⊥ H (against H's exact basis)
# P_H ell0 = c_0 exactly (c_0 in H by CHECK 5; w ⊥ H just verified) — nonzero
# P_f ell0 = w exactly  (c_0 ⊥ im(d_1^dagger) since <c_0, d_1^dagger y> = <d_1 c_0, y> = 0;
#                        w ∈ im(d_1^dagger) by construction) — nonzero
assert any(v != 0 for v in c[0]) and any(v != 0 for v in w)
print('T0 UNCONSTRAINED: witness ell = c_0 + d_1^dagger(delta_f01(0))')
print('   bdry.ell = 0; phi_H(ell) = c_0 != 0; phi_f(ell) = w != 0  (both EXACT components)')
print('   => AND SATISFIABLE under T0')

# --- TYPING T1: PLAQUETTE-LOCAL (cell = one filled 2-cell; support ⊆ its 4 edges) ---
sup = sorted(e for (e, s) in d1_rows[f_star])
# exact null space of bdry restricted to the 4 support columns (Fraction RREF, 4 cols)
cols = [[0] * 4 for _ in range(nV)]
for k, e in enumerate(sup):
    for (v, s) in d0_rows[e]:
        cols[v][k] += s
A = [[Fraction(x) for x in row] for row in cols]
r, piv = 0, []
for cc in range(4):
    pr = next((i for i in range(r, nV) if A[i][cc] != 0), None)
    if pr is None: continue
    A[r], A[pr] = A[pr], A[r]
    pv = A[r][cc]; A[r] = [x / pv for x in A[r]]
    for i in range(nV):
        if i != r and A[i][cc] != 0:
            f = A[i][cc]; A[i] = [a - f * b for a, b in zip(A[i], A[r])]
    piv.append(cc); r += 1
assert r == 3  # nullity 1 on the 4 support columns
free = [cc for cc in range(4) if cc not in piv]
vec = [Fraction(0)] * 4; vec[free[0]] = Fraction(1)
for ri, pc in enumerate(piv): vec[pc] = -A[ri][free[0]]
null_full = [0] * nE
for k, e in enumerate(sup): null_full[e] = vec[k]
scale = 1
for x in vec: scale = scale * x.denominator // __import__('math').gcd(scale, x.denominator)
null_int = [int(x * scale) for x in null_full]
if dot(null_int, w) < 0: null_int = [-x for x in null_int]
assert null_int == w  # the only conserved on-cell cochain is the boundary word itself
print('T1 PLAQUETTE-LOCAL: admissible on-cell cochains = k * (boundary word w) only (nullity 1)')
print('   w = d_1^dagger delta_f ∈ im(d_1^dagger) and w ⊥ H  =>  phi_H(k w) = 0 identically')
print('   => AND UNSATISFIABLE under T1 (harmonic sector dead on-cell — the working-class mirror)')

# --- TYPING T2: CIRCUIT-LOCAL (cell = connected b_1 = 1 subcomplex, the Attach shape;
#     minimal noncontractible instance in K_3 = the coordinate 3-circuit gamma_0) -----
x0 = (0, 0, 0, 0); x1 = shift(x0, 0); x2 = shift(x1, 0)
lam = [0] * nE
for x in (x0, x1, x2): lam[eidx[(0, x)]] = 1
assert bdry_apply(lam) == [0] * nV                      # conserved / gauge-invariant
assert any(v != 0 for v in lam)                         # total-nonzero
pair = [dot(lam, c[mu]) for mu in DIRS]
assert pair == [3, 0, 0, 0]                             # <lam, c_0> = 3 != 0
# exact decomposition using H's exact orthogonal basis:  P_H lam = (3/81) c_0 = c_0/27
pH = [Fraction(3, 81) * v for v in c[0]]
pf = [Fraction(a) - b for a, b in zip(lam, pH)]
assert any(v != 0 for v in pH)
d1lam = d1_apply(lam)
assert any(v != 0 for v in d1lam)                       # lam ∉ ker d_1
assert d1_apply(pf) == [Fraction(v) for v in d1lam]     # d_1 pf = d_1 lam exactly (d_1 c_0 = 0)
assert all(dot(pf, c[mu]) == 0 for mu in DIRS)          # pf ⊥ H (against H's exact basis)
assert bdry_apply(pf) == [Fraction(0)] * nV             # pf ⊥ im(d_0), verified directly
# pf ⊥ im(d_0) and pf ⊥ H  =>  FORM D1's split forces pf ∈ im(d_1^dagger) exactly;
# pf != 0 because d_1 pf = d_1 lam != 0
nz = sum(1 for v in d1lam if v != 0)
print('T2 CIRCUIT-LOCAL: witness lam = the coordinate 3-circuit cochain (support = 3 edges of')
print('   the b_1 = 1 circuit cell gamma_0); bdry.lam = 0; <lam, c_0> = 3')
print('   EXACT decomposition: phi_H(lam) = c_0/27 != 0 ; phi_f(lam) = lam - c_0/27 != 0')
print('   (pf ⊥ H and ⊥ im(d_0) => pf ∈ im(d_1^dagger); d_1 lam has %d nonzero entries)' % nz)
print('   => AND SATISFIABLE under T2, with a cell-local (circuit-local) witness')

print()
print('RESULT   K3_BLOCKS: gauge = 80, flux = 240, harmonic = 4 (all EXACT, two-sided sandwiches)')
print('RESULT   K3_AND: SATISFIABLE under T0 (unconstrained) and T2 (circuit-local);')
print('         UNSATISFIABLE under T1 (plaquette-local) — TYPING-DEPENDENT overall')
print('ALL ASSERTIONS PASSED')
```

## 6. EXACT SCRIPT OUTPUT, VERBATIM (`python3 k3_both_blocks_v001.py`, exit code 0)

```text
CHECK 0  census V=81 E=324 F=486  == sealed L^4 / 4L^4 / 6L^4 at L=3
CHECK 1  d_1 d_0 = 0 entrywise over Z (all 486 x 81 composite entries zero)
CHECK 2  d_0 . (constants) = 0  =>  rank_Q d_0 <= V-1 = 80
CHECK 3  BFS: connected (components = 1)
CHECK 4  rank_p d_0 = [80, 80, 80] @ p=[1000003, 1000033, 1000037]; sandwich with CHECK 2 => rank_Q d_0 = 80 EXACT
CHECK 5  c_0..c_3: d_1 c_mu = 0 and bdry c_mu = 0 exactly; Gram = 81*I_4 =>
         4 independent members of H  =>  dim ker d_1 >= 80 + 4 = 84  =>  rank_Q d_1 <= 240
CHECK 6  rank_p d_1 = [240, 240, 240] @ p=[1000003, 1000033, 1000037]; sandwich with CHECK 5 => rank_Q d_1 = 240 EXACT
CHECK 7  blocks: gauge dim im(d_0) = 80, harmonic dim H = 4, flux dim im(d_1^dagger) = 240;
         sum 80 + 4 + 240 = 324 = E EXACT; dim H = 4 = number of exhibited members
         =>  { c_0, c_1, c_2, c_3 } IS an exact orthogonal basis of H (Gram 81*I_4)

T0 UNCONSTRAINED: witness ell = c_0 + d_1^dagger(delta_f01(0))
   bdry.ell = 0; phi_H(ell) = c_0 != 0; phi_f(ell) = w != 0  (both EXACT components)
   => AND SATISFIABLE under T0
T1 PLAQUETTE-LOCAL: admissible on-cell cochains = k * (boundary word w) only (nullity 1)
   w = d_1^dagger delta_f ∈ im(d_1^dagger) and w ⊥ H  =>  phi_H(k w) = 0 identically
   => AND UNSATISFIABLE under T1 (harmonic sector dead on-cell — the working-class mirror)
T2 CIRCUIT-LOCAL: witness lam = the coordinate 3-circuit cochain (support = 3 edges of
   the b_1 = 1 circuit cell gamma_0); bdry.lam = 0; <lam, c_0> = 3
   EXACT decomposition: phi_H(lam) = c_0/27 != 0 ; phi_f(lam) = lam - c_0/27 != 0
   (pf ⊥ H and ⊥ im(d_0) => pf ∈ im(d_1^dagger); d_1 lam has 18 nonzero entries)
   => AND SATISFIABLE under T2, with a cell-local (circuit-local) witness

RESULT   K3_BLOCKS: gauge = 80, flux = 240, harmonic = 4 (all EXACT, two-sided sandwiches)
RESULT   K3_AND: SATISFIABLE under T0 (unconstrained) and T2 (circuit-local);
         UNSATISFIABLE under T1 (plaquette-local) — TYPING-DEPENDENT overall
ALL ASSERTIONS PASSED
```

---

## FLAG BLOCK

```text
F0_SCOPE = TRACE-CARRIER-SPECIFIC(V011 20a3a17d :1840 "Use the unfilled oriented
  1-skeleton K_square with no 2-cell", :1881-1882 "No filled 2-cell is included in
  the trace carrier" — definite article bound to B_square's carrier
  C_0(K_square) ⊕ C_1(K_square); :2184 types "trace carrier" as one frozen component
  of the composition-loop prediction; addendum 420ab02f :33-34 types only the Attach
  map's OPERATIVE DOMAIN, in b_1, never faces-in-general.  The cube cell's own
  licensing spans: V011 :301-303 "No flatness around a filled two-cell is imposed
  ... nontrivial face holonomy must remain admissible", :306-313 the C_2 carrier,
  :1322-1355 = byte span [44530,45446) 9bbd9525 (A_R^2 = R^(K_2^+); F_phys = im(d_1);
  the face response defined ON F_phys); AND 38bbb9fc :192-199 parent K census
  16/32/24 with rank d_1 = 17 of record.  Clean division of roles — trace carrier
  (theta_j read, F = 0) vs response carrier (R_record,L, F > 0) — NO contradiction;
  the division is structurally canonical: at b_1 = 1, H^1 = 1 - rank d_1, so any
  Q-active face kills the readable class (§3.5).  No sealed span supports the
  universal reading.)
COMPOSITION = UNBUILT-UNSPECIFIED(no sealed BAR — K_L is itself a sealed
  both-features complex, V011 :826-875 spans 7d81c132/96e164af, flag :2241
  a6cc611f, H^1 = 4 certified hunt 1892c08e :298-317 and reproduced here — sealed
  at the AUDIT stratum; no physical-stratum LICENSE — composition disjoint-only
  (hunt :165-173), ports unanchored = O11+O12 UNBUILT (hunt :193-204), connected
  case an open Gate-5 obligation (V011 :1740 span 7103582e), "the
  multi-cell/exhaustion side is UNBUILT (charter O-D1; port anchoring O12)"
  (AND :344-346), "no sealed map sends either audit object onto the working class
  or vice versa" (hunt :324-326), NONE_SEALED at the physical write-carrier stratum
  (glued check :288-291).  The AND §7 reopening clause (:337-348) is a prospective
  license shape, not a bar.  The barrier is absence of construction, not sealed
  prohibition — the O11/O12 gap again.)
K3_BLOCKS = gauge 80 / flux 240 / harmonic 4, EXACT (V=81, E=324, F=486 built from
  the sealed law verbatim; d_1 d_0 = 0 entrywise over Z; every dimension certified
  by a TWO-SIDED exact sandwich: constants-kernel + three-prime ranks for d_0; the
  four direction-cocycle certificates (d_1 c_mu = 0, bdry c_mu = 0, Gram 81*I_4)
  + three-prime ranks for d_1; block sum 80 + 4 + 240 = 324 = E; {c_0..c_3} an
  exact orthogonal basis of H; agrees with hunt :298-301 by independent instruments.)
K3_AND = TYPING-DEPENDENT(the sealed text supplies NO "cell"/"cell-local" typing on
  K_3 — every choice explicit and flagged.  T0 UNCONSTRAINED: SATISFIABLE, witness
  ell = c_0 + d_1^dagger(delta_f01(0)), bdry.ell = 0, phi_H(ell) = c_0 != 0,
  phi_f(ell) = w != 0 — both components exact and simultaneously nonzero.
  T2 CIRCUIT-LOCAL (the Attach-shaped cell, b_1 = 1 subcomplex): SATISFIABLE,
  witness lam = the coordinate 3-circuit cochain, exact split phi_H(lam) = c_0/27
  != 0, phi_f(lam) = lam - c_0/27 != 0 (⊥ H, ⊥ im(d_0), d_1-image nonzero at 18
  entries).  T1 PLAQUETTE-LOCAL: UNSATISFIABLE — the on-cell conserved set is
  exactly k*(boundary word) ⊂ im(d_1^dagger), phi_H ≡ 0, the working-class collapse
  mechanism reproduced.  The conjunction fails ONLY under strict plaquette-locality;
  it is NOT algebraically obstructed, and both prior single-sector collapses are
  recovered as degenerate restrictions of this one complex.)
NET = no contradiction; between the record and a both-blocks physical carrier stand
  ONE CONSTRUCTION (a derived connected physical complex with b_1 >= 2 — forced by
  H^1 = b_1 - rank d_1 — and filled faces: the unbuilt O11/O12/O-D1 object) and TWO
  SUPPLIES (a lawful cycle selector beyond the Attach entry's b_1 = 1 domain, V007
  :170-171 "does not supply and may not freeze"; and the unwritten cell-local typing
  rule for the write on multi-cell carriers, on which the K_3 AND verdict turns).
SEALS_VERIFIED = a3bedc7e (b1 instance), 86b6134d (b1 check), 420ab02f (addendum),
  ad9fc14e (DoR), ccca6bb4 (V007), 5154c203 (V007 check), 20a3a17d (V011), 1892c08e
  (hunt), a22ef820 (glued check), 38bbb9fc (AND), 3e35ffe2 (G3), 5e49d209 (FORM),
  8ed95b5a (GC) — all recomputed at path; V011 byte spans 7d81c132, 96e164af,
  9bbd9525, 7103582e, a6cc611f rehashed exactly (a6cc611f at corrected offset
  [80185,80240) — the hunt's cited offset [73992,74047) is stale; content and digest
  verified exactly at the corrected span; SPAN-DRIFT NOTED, nothing else moved).
  None unverifiable.
SCRIPT_REPRODUCED = yes (in-artifact verbatim with verbatim output; exit 0; Python 3
  stdlib only; exact integer/rational arithmetic; mod-p only inside two-sided exact
  sandwiches; 8 checks + 3 typing runs, all asserted).
FORBIDDEN_IMPORTS = none (no scale, no imported GR, no faithfulness authority; no
  continuum object consumed; K_3 handled as an audit-stratum object throughout).
MACHINERY_INVOKED = no.
alpha_computed = false, kappa_record_computed = false, proof_authorized = false.
ALL_RESULTS = CLAIMED until checked.
```
