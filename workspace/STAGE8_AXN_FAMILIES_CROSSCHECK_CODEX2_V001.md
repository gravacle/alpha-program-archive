CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
CLOSURE_END_BYTE = 00004196
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 11 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2   ROLE_THIS_RELAY = OPPOSITE-LANE CROSS-CHECKER
```

| # | Closed member | SHA-256 / bounded span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_962_FAMILIES_CROSSCHECK_CODEX2_V001.md` | `c950772db537b01bbf5e1a7013be6fde54acfe46c198d92e00da09d48cbd5265` | assignment |
| 02 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 03 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process and claim-status law |
| 04 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01-S37 flattening guard |
| 05 | `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V001.md`; basis press `[3434,5292)`; family/list carriers `[6156,11535)` | whole `27a8fcd7c6d351c220d5e85a8b73db7746f42c58e76aebce43a25a0ee2d1ce25`; spans `793f6a3287de85751ddaeeeefd3b98ec97e5bd95a37d8f2a53be54e8412fee3f`, `37c9be26bec887a9929dfd00c4a604f0e66fb9f4ce4a05093f9012e55ed871e9` | subject |
| 06 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V003.md`; `G_joint` `[13731,14295)`; `for_class` schema `[23079,23825)`; totality law `[26569,27186)`; theorem `[33114,33385)` | whole `79f0c35161204f846666badb38f28398ae317d56a64d8fe70f7f29e2ee01072e`; spans `44793c2969234f911e7a2d3e472b64d4787e2f214adbf9a2f1676a1494f20c17`, `08f9e6c316b8126dad7412cea95afae55be88d9615bc2ee7fc7686513a009fbb`, `39a5a5943b9619bb036f225757377b0103b4940642869a68e754c1768796c044`, `17112b03d6dc1a210ebf6756acb367a5458fe9a31a8247e0e3041c80b2eaa1c0` | governing receiver and universal candidate direction |
| 07 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V002.md`; `E_joint` and certificates `[9457,14353)`; pairing/faithfulness `[14354,18227)` | whole `fd2625a079c77fbc0a102a54a0dd8ba1d97dcfb393035c2b691b0475de254444`; spans `ba65f87c9347ede0c261cf30560b92d3c6f1535512f633e94d648b35c094f917`, `3163fe5a863a8de5192445a349ca9ce8b9207753831aa951efcd4f5db0e9749d` | booked inputs and their exact receivers |
| 08 | `STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V002.md`; stage labels `[4210,4698)`; connecting maps `[7673,8726)`; missing-candidate boundary `[18462,19475)` | whole `f450b0a356b249509fb59b897c4f6a14e6996ba7da5ea513e69112a89907eed9`; spans `d0e733a3be5e6aaad265c0ebdef44ec7c8ae272c12b69459c8de9a8b6d93e2a8`, `94064497188457ff511ee25b88694e44c2672b87f80cdf4488df36dfaa39df67`, `1a774913dbe5487c1338e6ea9b465999a99ebeb77751b92220d4211eb61657e4` | stage grammar, zero extension, and absence boundary |
| 09 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`; source carrier `[12477,12994)`; proposed-not-adopted basis schema `[19894,20073)` | whole `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0`; spans `0b3a59256f6d21359c0bf3d20b3ad2b1fe8b7da23f45445796250befec35535e`, `a3dbd428a63fffc92f6f3ca90c221b4f2c55b11d355735cd785602476bf2066d` | source decomposition and false-route control |
| 10 | `R3_4_COMPLETE_CAUSAL_SUPERCONNECTION_PARENT_SPEC_V001.md`, `[4320,4594)` | whole `40890e753463b8c4c49844864f3f4811f15ec5f71fe9c044f9eb7d91428899a9`; span `593a5dbde021a96c761cb663fb1cb6f8acb3577b8a7c62051d83bd6b6c71d26e` | named record basis; whole hash is the subject's closed-member pin |
| 11 | `STAGE8_AXN_SPEC_ACTS_DARIO_V001.md`, `[6959,7406)` | whole `241ccf880266a895f5955173c8c87e7d180d8c6ba6dffefd10f257267454eca1`; span `d1803a0ed3001261e56b40a356f70aad7f15b855d336fe5040f4d01ff85b5402` | unrelated-eigenbasis false-route control |

```text
MEMBERS 01-09 AND 11: ADJACENT SEAL SIDECARS VERIFIED BEFORE SEMANTIC USE.
MEMBER 10: WHOLE-FILE HASH RECOMPUTED AND MATCHED THE SUBJECT'S CONTENT-ADDRESSED CLOSED-MEMBER PIN;
           NO ADJACENT ROOT SIDECAR WAS CLAIMED.
NO PHYSICAL QUANTITY WAS NUMERICALLY EVALUATED.
```

CLOSURE_DECLARATION_END

# STAGE 8 — AXN `for_class` FAMILIES CROSS-CHECK — CODEX 2 LANE — V001

Date: 2026-08-10  
Relay: 962 / `[PLAN:AXN-BUILD-C61]`  
Status: **THE BASIS SELECTOR IS REAL, BUT THE FIVE DISPLAYED PAYLOADS DO NOT FILL THE FIVE GOVERNING `for_class` RECEIVERS. THEIR BYTES, DIGESTS, CLOSED TUPLE SHAPE, AND BASIS-INDEPENDENCE CHECK OUT. THEIR SEMANTIC RECEIVER AND TOTALITY DO NOT: FC-04 THROUGH FC-07 REPLAY PROPERTIES OF `E_joint`, NOT OF EVERY CANDIDATE `Phi_joint`; FC-08 MERELY STATES UNPINNED `Phi_joint` EQUATIONS. VERDICT: FAILURES NAMED + SELECTOR-CONFIRMED.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
JOINT_ANCHOR_DERIVED = false
```

## 1. Basis determination first

### 1.1 Leg-by-leg byte result

| Leg | Sealed byte result | Determination |
|---|---|---|
| record `R_N` | member 10 writes `R_c=span{|r_c>,|p_c>,|e_c>}` in that displayed order; member 08 writes `R_N=tensor_(j=1)^N M_3(C)` | **HAS.** The three named record vectors and cell order are present. |
| history/field `B_N` | member 08 writes `Lambda_N=Z^N`, `A_F,N=C*(Z^N)`, the two CTP factors, and zero extension | **UNORDERED.** Coordinates and connecting maps are fixed; no bijective enumeration or total serialization order on all of `Z^N` is fixed. |
| source `Fix(E_ch)` | member 09 writes `H_src^A=P_0 H_src direct-sum P_ch H_src`, finite dimension, ordinary trace, and orthogonal `P_0,P_ch` summing to `I_src` | **NONE.** The two spectral subspaces are named, but no vector basis inside either subspace is named. |

The record leg therefore agrees with the subject. The other two legs do too. A coordinate tuple is not
an enumeration of all tuples. A pair of projectors fixes two eigenspaces, not bases inside eigenspaces:
unitary rotation within either nontrivial block leaves `P_0`, `P_ch`, `E_ch`, the trace, and the displayed
direct sum unchanged. No block dimension was read, so neither block may be silently treated as one
dimensional.

### 1.2 Four-mode press and false routes

| Mode | Query/receiver pressed | Result |
|---|---|---|
| exact-name | `P_0 H_src`, `P_ch H_src`, `Z^N`, `basis`, `order`, `eigenbasis` | decomposition and labels found; no entered interior bases or history enumeration found |
| normalized-name | `P0/Pch`, subscript/ASCII variants, `Z^N`/integer-label variants, basis/span/diagonal forms | no additional receiver-bound basis or order |
| fixed-string | the subject's cited first-nonzero-positive / norm-then-lex convention | convention belongs to a finite momentum-representative receiver; it does not state an order on `Z^N` and does not name source vectors |
| byte-span/semantic receiver | complete commuting operators, simple spectra, charge eigenvectors, stage-coordinate order, and trace-zero basis construction | no qualifying forced route; the apparent routes below fail at their receivers |

The additional apparent routes do not transfer:

1. `P_0,P_ch` order the **two blocks** as displayed, but are scalar on their respective eigenspaces.
   They cannot distinguish vectors within a block.
2. The stage grammar fixes coordinate positions and the map `lambda -> (lambda,0,...,0)`. It does
   not choose among lexicographic, shell-then-lexicographic, signed-zigzag, or other exact
   enumerations of the countable set. Ordinary lexicographic order on `Z^N` is not by itself the
   required finite-prefix enumeration.
3. Member 09's `B_ss=(Delta_1,...,Delta_m)` occurs under **PROPOSED_NOT_ADOPTED — FINITE
   AUTHORITY EXPLICIT**. It is a request to freeze a basis of `Delta_0^ss`, not an entered basis of
   either source block, and no `Delta_i` values are supplied.
4. Member 11's eigenbasis is for an unrelated self-adjoint one-particle operator in a fermionic-lift
   theorem. It supplies neither a source-block operator nor a history-label order.
5. A charge-representation convention can label charge sectors or chiral components without
   selecting Hilbert-space vectors within `P_0 H_src` or `P_ch H_src`. No such convention is used as
   verdict-bearing evidence here.

Hence `BASIS = SELECTOR-CONFIRMED`. The subject's nontransfer ruling is confirmed.

### 1.3 Exact freeze content

A lawful selector act would have to freeze all of the following, content-addressed and before any
candidate output:

1. one exact ordered orthonormal basis of `P_0 H_src` and one exact ordered orthonormal basis of
   `P_ch H_src`, including vector encodings and phase conventions;
2. for every `N>=1`, an exact bijective enumeration/order of `Z^N`, together with its declared
   behavior under the sealed zero-extension maps;
3. the induced order on the `+` and `-` history factors and their ordered pairs in `B_N`;
4. the deterministic tensor-product assembly order combining source, the already named record
   basis, and history; and
5. a deterministic trace-zero-hyperplane basis rule for `Delta_0^joint`—including the pivot or
   diagonal-combination convention, normalization, ordering, canonical bytes, and stage coherence.

Items 3-5 cannot be inferred merely by having factor bases. In particular, deleting an identity
coordinate does not canonically choose a basis for a trace-zero hyperplane without an elimination
or orthogonalization convention.

### 1.4 Lawful option space, unranked

| Option | What would be frozen | Cost / obligation |
|---|---|---|
| direct witness | the two ordered source-block bases, all history enumerations, and the joint assembly/trace-zero rule | explicit new selector data; breaks otherwise invisible within-block unitary freedom; must be pinned and stage-coherent |
| operator-derived witness | a content-addressed complete commuting operator family with simple joint spectrum, phase/tie-break rules, plus a content-addressed history ordering law | adds operator structure and proofs of simplicity/completeness; any degeneracy leaves the selector unresolved |
| basis-free schema amendment | replace FC-01..FC-03 matrix/basis receivers with invariant operator, Gram, factorization, and inverse statements | changes the instrument and replay interface; requires a finite spec delta, independent review, and new failure controls |
| equivalence-class schema amendment | accept a content-addressed basis orbit plus an invariance theorem instead of a representative | requires an orbit/equivalence schema and proof that every downstream verdict is representative-independent; no concrete matrix may be consumed without a representative law |

No option is selected or ranked by this lane.

## 2. Five displayed family payloads

### 2.1 Byte and closed-tuple replay

The declared grammar has seven pipe-delimited fields:

```text
JAC14-FC-FAMILY | v=001 | id | quantifier | inputs | procedure | accept
```

Terminal linefeeds were excluded exactly as the subject declares. Independent byte replay gives:

| Family | Bytes | SHA-256 | Seven fields | Named inputs resolve to booked members |
|---|---:|---|---|---|
| FC-04 | 695 | `1a14255f7f40f58bd539325a2f7d33c40f8d7a22b519ba057b94fdda1f233236` | yes | yes: `EJOINT`, conditional-expectation, total-typing, stage |
| FC-05 | 622 | `8d5ae19237917d13bd95be33f9ce84b74775b51340f14957fa70e909a1a78df9` | yes | yes: `EJOINT`, commutation, stage |
| FC-06 | 691 | `acabc0534123d03b905806ddd82028548705b5a4aaa58fb383d94d41ddd019c5` | yes | yes: `EJOINT` bundle, commutation, stage, receipt root |
| FC-07 | 673 | `a6cfa698ef82bbc24583d82731a1dc5c06e4528cae6869631773863c4da88486` | yes | yes: `EJOINT`, fixed-space certificate, stage |
| FC-08 | 827 | `0c13577e78d5f2132d522b9775f9cb6cf0979699e9c5a5880c7bfc9600dad0b8` | yes | yes for the listed pairing, faithfulness, `Delta_0`, `EJOINT`, and stage inputs |

The payload integrity claim is therefore **5/5 confirmed**. This does not establish semantic replay.

### 2.2 Governing receiver and common totality failure

Member 06 defines `G_joint` as a family of candidate maps `Phi_joint` and binds CPTP, charge
covariance, `Phi_joint E_joint=E_joint Phi_joint`, unique mixing invariant state, kernel, tag, exact
certificate, and stage-square predicates to each candidate. It then requires every `for_class`
family to be an exact total replay object, and preregisters:

```text
FOR EVERY Phi_joint IN G_joint:
  inv(Phi_joint) = Omega_anchor,C0.
```

Every displayed quantifier is only `all-N>=1-on-the-F_cyl-bounded-class`. None quantifies over, lists,
or content-addresses the candidates `Phi_joint in G_joint`. Member 08 expressly records at its
missing-candidate boundary that CPTP, charge, superselection, fixed-space/mixing, and anchor
certificates require the exact entered `Phi_joint` and are not consequences of the stage rule. The
five payloads do not cure that absence.

Thus the common defect is not a basis defect. It is receiver-bound totality: `for every N` is not
`for every candidate Phi_joint and every N`, and a property of `E_joint` is not a property of
`Phi_joint`.

### 2.3 Per-family semantic replay

| Family | What the booked inputs actually prove | What the governing receiver requires | Result |
|---|---|---|---|
| FC-04 CPTP | `E_joint` is total, unital completely positive, bimodular, and idempotent; the stage payload transports restrictions | every candidate `Phi_joint` is normal and CPTP at every stage | **FAIL-RECEIVER.** No candidate digest, trace-preservation replay, or candidate-totality carrier occurs. |
| FC-05 charge covariance | `E_joint` commutes with the source embedding through `E_ch` and is identity on record/history embeddings | every candidate `Phi_joint` is joint-charge covariant | **FAIL-RECEIVER.** Embedding identities for `E_joint` are not charge covariance for `Phi_joint`; no candidate or charge-action replay object is an input. |
| FC-06 superselection commutation | the `E_joint` bundle and seven stage receipts propagate properties of `E_joint` | `Phi_joint E_joint=E_joint Phi_joint` for every candidate | **FAIL-RECEIVER.** The relation requires both maps; no `Phi_joint` is supplied. |
| FC-07 fixed space/mixing | `Fix(E_joint)=Fix(E_ch) graded-tensor R graded-tensor B` as a subspace identity | each candidate has one normalized mixing invariant joint state, with exact fixed-space/mixing replay | **FAIL-RECEIVER.** The restraint is genuine and basis-free, but it concerns the conditional expectation. “Complementary decay” is asserted in the accept field without a candidate channel or a replayable candidate-mixing ground. |
| FC-08 BI anchor | the listed pairing is faithful on `Delta_0^joint`; `E_joint`, the stage grammar, and the BI definition bytes are pinned | an exactly one-total closed BI replay family proving the entered equations for every candidate | **FAIL-BINDING/TOTALITY.** `Phi_joint` appears only as an unpinned noun in `procedure=...state-the-entered-BI-equations`; no candidate operand or proof is supplied. Stating the equations is not replaying them. |

All named input digests are booked-only. The missing `Phi_joint` ground is not papered over: it is the
same absence member 08 records. The report therefore does not call a wrong-receiver certificate
unbooked; it calls the five **family objects semantically insufficient** for their governing keys.

### 2.4 Basis-independence press

None of FC-04 through FC-08 consumes coordinates, a matrix, an ordered vector family, a history-label
enumeration, or a source-block basis. FC-07's fixed-space statement is explicitly a subspace identity,
and its complementary-decay wording is basis-free. FC-08 consumes the `Delta_0^joint` definition and
pairing, not a `Delta_0` basis. Consequently:

```text
BASIS_INDEPENDENCE = VERIFIED-ALL
```

This is a narrow positive result. Basis independence does not repair missing candidates, receiver
drift, or totality.

## 3. Partial list carrier

The exact list line independently rehashes as:

```text
JAC14-FC-LIST_BYTES = 450
FOR_CLASS_PARTIAL_LIST_SHA256 = 0497742e60a81b0aed4deb93e3e747d546332dc5b4ef3c18ca5ee3dc2452d4ed
count = 5
built = FC-04,FC-05,FC-06,FC-07,FC-08
stopped = FC-01,FC-02,FC-03
```

The carrier names both sets and cannot be parsed as an eight-family complete list. `built` above is
the subject's literal carrier label; this cross-check confirms the bytes and partition, not the
semantic sufficiency of the five claimed built members. No suite hash, replay entry point, or frozen
complete-suite receipt follows from the partial list.

## 4. FREEDOMS-CONSUMED

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  the governing V003 receiver, sealed source decomposition, named record basis, stage grammar,
  zero-extension maps, booked E_joint/pairing/certificates, and the five literal payload lines.

CHECKED BUT NOT SELECTED:
  source-block basis freedom; history enumeration freedom; tensor/trace-zero assembly freedom;
  the four unranked lawful resolution routes in section 1.4.

AUTHORED OR SELECTED BY THIS LANE:
  nothing. The option table is a disposition surface, not a freeze.

SUBSTITUTED:
  nothing. In particular, E_joint was not substituted for Phi_joint, a stage quantifier was not
  substituted for candidate totality, and an accept-field assertion was not substituted for replay.

STOPPED AND ROUTED:
  FC-01..FC-03 remain selector-stopped; FC-04..FC-08 fail semantic receiver/totality cross-check.

SCALING WEIGHTS = NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED
```

## 5. Batteries and custody

- **FLATTENING CHECK:** a block decomposition was not flattened into block bases; tuple coordinates
  were not flattened into a label enumeration; factor bases were not flattened into a canonical
  trace-zero basis; `E_joint` certificates were not flattened into `Phi_joint` certificates; `for
  every N` was not flattened into `for every Phi_joint`; a list carrier was not flattened into a
  complete suite.
- **F_PLDEC:** only exact bytes, hashes, schemas, symbolic subspaces, maps, and quantifier directions
  were examined. No physical quantity or measured constant was numerically evaluated.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string, and byte-span/semantic-receiver
  checks covered the three basis legs, ordering convention, eigenbasis and charge false routes,
  all five payloads, every named input, the candidate receiver, and the list carrier.
- **BLIND:** held. No dimension, rank, multiplicity, source ratio, fiber value, candidate output, or
  downstream comparison was read or computed.
- **PE-1..PE-14:** pointer-only, zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** the opposite lane's payload construction remained a claim until this
  independent byte and receiver check. Payload integrity is confirmed; family sufficiency is not.
- **CHAIN:** no member binding, fixed-point execution, end test, gauntlet, state solve, full-fiber
  computation, physical numerical evaluation, or measured-constant comparison was invoked.

## 6. Final lines

```text
CLOSURE = declared-first (byte position 0, closure end 4196; scan 0 hits)
BASIS = SELECTOR-CONFIRMED (freeze content enumerated)
LEGS = CONFIRMED (record has / history unordered / source none)
NONTRANSFER = CONFIRMED
FAMILIES = FC-04,FC-05,FC-06,FC-07,FC-08 FAIL receiver-bound totality (payload bytes/digests 5/5 replayed)
BASIS_INDEPENDENCE = VERIFIED-ALL
CARRIER = BOTH-SETS-CONFIRMED (literal partial partition only)
OPTIONS = enumerated
VERDICT = FAILURES-NAMED + SELECTOR-CONFIRMED
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
