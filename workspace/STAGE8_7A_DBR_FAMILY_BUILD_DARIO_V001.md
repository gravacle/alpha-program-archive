# STAGE 8 / 7A / Q-126 ROUTE — THE TWO-MEMBER FAMILY, BUILT; THE CONSISTENCY GATE, VIOLATED; THE RULE, DRAFTED WITH SIX HOLES

Lane: DARIO (Builder B, independent verifier). Relay 777.
Governing: `DECISION_CARRY_BOTH_KINDS_2026-08-09` `27c15262e2a99cae4ce86d1e0737bca2455876fe4d584114ba1b534ff9a3839e`
SEALED-OK — the principal adopted NEITHER kind; **FAMILY-BEFORE-DETERMINANT** is the route's law.
Resumed layer: Q-682 / `083e86e3…` (775), SEALED-OK.

## Lead determination

**The family builds. AS3's consistency gate then FIRES on `U7`, and it fires against my own
775 core.**

```text
U7 (775):  "The branch BOUNDARY at ell = 0, for every class."
775 §4.1's ground: "the pairing fails exactly at the level the index theorem makes unpaired."

At n = 0 the two branch formulas COINCIDE at ell = 0:   d_S2(0) = n = 0  and  4·0 + 2n = 0.
There is NO discontinuity, and the index vanishes, so NO level is unpaired.
U7's stated ground is UNAVAILABLE at member A.
```

Under the structural reading `U7` is **false** for member A; under the notational reading it is
**vacuous** for member A. **Neither reading delivers U7 as a substantive fact holding identically
from both members**, which is exactly the condition AS3 directs me to stop on and display as a
principal-facing Q-682 correction.

**This costs me the thing 775 called its deliverable.** `U7` must move from `UNIFORM_CORE` to
`CLASS_DEPENDENT`, shrinking the selector-independent core from nine items to eight — and it
*strengthens* the 775 trigger, because `U7` was the item that contained `C5` as mere "occupancy of
a uniform structure." If the boundary itself exists only on kind B, the **structure** differs, not
just its occupancy.

**Delivered before the gate fired**, and delivered in full:

- **AS1 — the family**, with the core quoted verbatim (not paraphrased) and factored out once, and
  with a derived integer reparametrisation `n := 2|q| = |deg L| ∈ Z_{>=0}` under which every
  class-dependence becomes **integer-affine**. Member A validated against the standard untwisted
  S² Dirac spectrum exactly.
- **A structural finding the two-kind framing hides:** the carried family has **two KINDS but
  countably many MEMBERS**. Kind B is the sub-family `{B_n}_{n>=1}`, whose members differ from
  *each other*. A rule written as a two-way A-vs-B comparison is ill-posed on the object the
  decision actually carries.
- **AS2 — the rule**, drafted to a closed schema `R1–R7` — and then **corrected downward**: three
  of the five free choices I first claimed to have eliminated were not eliminated. **Six free
  choices are named as un-eliminable, four derivation gaps are named, and the relay's
  "executable-in-principle WITHOUT further choices" bar is NOT met.** I report that rather than
  presenting a schema that would look closed.

**And the sealed falsifier does not say what the route has been paraphrasing it to say.** Read
verbatim from `PRIN`, the hard-failure rule enumerates **radii, spin structure, bundle class, or
odd profile** — and its consequent is ***"a further physical selection principle is required,"***
not "the route dies." Whether the axis this family varies is even inside that enumeration turns on
an unresolved referent (**FC-f**). I display the discrepancy and resolve nothing: the decision is
the principal's, not the lane's.

---

## 0. Preflight

### 0.1 Output name

Probed recursively across the archive **before any write**: artifact and sidecar both **ABSENT**.

### 0.2 Seals verified before use (each `shasum -c` run from the file's own directory)

```text
27c15262e2a99cae…  supervision/DECISION_CARRY_BOTH_KINDS_2026-08-09.md          OK   [governing]
083e86e357a63e0a…  workspace/STAGE8_7A_DBR_PUBLIC_LAYER_CAMPAIGN_DARIO_V001.md  OK   [775, resumed]
b1f19eaac48238a0…  relay_inbox/RELAY_PASTE_777_FAMILY_BUILD_DARIO_V001.md       OK   [verified BEFORE reading]
```
Also read, digests recorded: 768 `03db8d3d…`, 770 `3c6cd9a2…`, 773 `54d9cb5a…`, 765, 762,
`PRIN` `ae1d0492…`.

### 0.3 PE-6 — custody, and a custody finding

`supervision/PRINCIPAL_EXPECTATIONS_LEDGER_V001.md` = `64eef7d166c784c9…`, **sealed OK**, 8,414 B
(was `a25af72e…` at 773 — the ledger has grown). **I hashed it and did not open it.**

[YOURS] **That is not sufficient, and I say so plainly.** The governing decision — which every
relay under it must read — **restates PE-6 in full at its own line 18.** So PE-6's isolation
cannot be discharged by non-reading; only by non-use.

```text
CUSTODY FINDING (principal-facing, no verdict weight):
  A "RECORD, DO NOT CONSULT" item cannot be isolated while the decision-of-record that
  governs the relay republishes it in full.  Hash-only handling of the ledger is defeated
  by the decision's own text.  Reported, not remedied — the remedy is registrar business.
```

No step below references an expected agreement of the kinds. The one place it could have
biased me — the design of the rule's outcome set — runs the **other** way: §2.6 records that my
first draft's only scheme-free branch was the survival branch, and I flag that as a defect.

### 0.4 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No determinant evaluated.  No member evaluated alone (FAMILY-BEFORE-DETERMINANT): every
per-member row in §1.2 is one column of a family display and is stated only alongside its
partner.  No bundle class adopted OR ELIMINATED.  No member binding; no fixed-point
execution; no end test; no comparison to measured constants; no common cell formed; no
junction map evaluated; no smooth data derived.  No register, plan, tracker, git action.
NUMERIC-EVALUATION GATE, disclosed precisely: the integers printed in §1.3 are dimensionless
multiplicities and eigenvalue LABELS of a symbolic tower.  No physical quantity is evaluated;
p^2_min, C2_parent, the radii, and beta remain symbols throughout, and §3 below is a
correction issued BECAUSE a draft of this artifact silently set p^2_min = 0.
```

---

## 1. AS1 — THE FAMILY, BUILT

### 1.1 SHARED STRATUM — `U1–U9` quoted **verbatim** from 775, factored out once

[PROVABLE] Reproduced byte-exact from `083e86e3…` (offset 12698). **I quote rather than
paraphrase deliberately**: a draft of this artifact silently rewrote `U2` into the new parameter
`n`, and rewriting a frozen core in a reparametrisation is precisely the drift this lane cannot
afford.

```text
U1  The carrier and its rank:  H_BR = L2(Sigma_BR, S_Sigma tensor E_parent),
    rank(S_Sigma tensor E_parent) = 4 x 16 = 64.
U2  The eigenvalue FORM: lambda = p_t^2 + p_q^2 + ell(ell+2|q|) + 1 + C2_parent,
    over the label set (p_t, p_q, ell) — the labels and the form, not the values.
U3  The multiplicity FORM: 32 · d_S2(ell) · N_lattice, the constant 32 = 2 x 16 being
    class-independent.
U4  |q| ENTERS IN EXACTLY TWO PLACES, BOTH AFFINE — the weight d_S2 and the shift 2 ell |q|.
U5  The uniform lower bound lambda >= 1 + C2_parent.
U6  The grading structure (768): H_0 block-diagonal in the bigrading; the cross term T of
    bidegree (1,1) with IDENTICALLY ZERO DIAGONAL, so the tower is exact to first order and
    the whole correction is second order and Phi-dependent.  No step of that argument
    mentions |q|.
U7  The branch BOUNDARY at ell = 0, for every class.
U8  The beta scaling weights — three of them, beta^-2 / beta^-1 / beta^0 (section 7) — none
    of which depends on the class.
U9  The public layer's five open elements and their TYPE-R closures (762), which are
    class-independent obstructions.
```

**`U7` does not survive §3. It is quoted here as 775 wrote it, and corrected there.**

### 1.2 The parameter, derived (a display, not a replacement)

[YOURS, verified] `n := 2|q| = |deg L| ∈ Z_{>=0}` — Dirac quantization gives `2q ∈ Z`; the
zero-mode count is `2|q| = |deg L|` by Atiyah–Singer (764 §1.2, R10). Verified identical to 775's
`|q|` forms for `2|q| = 0…15`, `ell = 0…39`, **zero mismatches**; second finite differences in `n`
vanish identically at every `ell` tested.

```text
d_S2(0) = n        d_S2(ell>=1) = 4ell + 2n        lambda_S2^2 = ell(ell+n) = ell^2 + n·ell
```

[YOURS] **All three are integer-affine in `n`.** This sharpens `U4` without amending it: 775's
"both affine" becomes "both affine with integer coefficients" under the derived parameter.

**Disclosed flattening:** `deg L = 2q` is **signed**, and 770 records its chirality as an
orientation convention. `n := |deg L|` merges the members `+q` and `−q`. That is lawful for every
quantity in this artifact (all depend on `|q|` only) but **it is a merge, and a rule quantifying
over "all members" must decide whether `±q` are one member or two.** Folded into FC-f.

### 1.3 SIDE BY SIDE — only `C1–C5` instantiated

| | **member A** (`n = 0`) | **kind B** (`n >= 1`) |
|---|---|---|
| `C1  d_S2(0) = n` | `0` — label present, **fiber empty** | `n` — occupied, unpaired, one chirality |
| `C2  d_S2(ell>=1)` | `4ell` | `4ell + 2n` |
| `C3  shift n·ell` | `0` | `n·ell` |
| `C4  bottom` | `p2min + 1 + 1 + C2_parent` | `p2min + 0 + 1 + C2_parent` |
| `C5  branch cardinality` | **1** (`P` only) | **2** (`Z` and `P`) |
| branch set | `{P}` | `{Z, P}` |
| index (Atiyah–Singer) | `2q = 0` | `2q = ±n ≠ 0` |
| trace | `32 Σ_Λ Σ_{ell>=1} 4ell · f(p² + ell² + 1 + C2)` | `32[ n Σ_Λ f(p² + 1 + C2) + Σ_{ell>=1}(4ell+2n) Σ_Λ f(p² + ell² + n·ell + 1 + C2) ]` |
| `U5` status | bound HOLDS; slack by `p2min + 1` | bound HOLDS; slack by `p2min` |
| raw bottom weight | `32 · 4 · N = 128N` | `32 · n · N` — **differs with `n`** |

**`p2min` is carried as a symbol in every row.** Attainment of `1 + C2_parent` is **NOT
asserted**: it requires `p2min = 0`, i.e. `0 ∈ Λ`, i.e. the periodic spin structure on `T2_TQ`,
which 768 §2.4 records as **unselected** — *"a scope restriction, not a selection."* 775 states
only the inequality and **expressly declines to locate the bottom** (775 §5.2). A draft of this
artifact asserted attainment; the claim is withdrawn. **What is member-relative and `p2min`-free
is the OFFSET: B's bottom sits exactly one unit below A's, for every spin structure**, because
`p2min` is common to both members.

### 1.4 The seam — exactly what is shared and where they part

[YOURS] Every formula is affine in `n`, and member A is the `n = 0` evaluation, so **the FORMULAS
are continuous across the seam**. What is discontinuous is the **TYPE**: `dim = n` is a
non-negative integer, and at `n = 0` branch Z's fiber empties and the cardinality drops `2 → 1`.

> **They part exactly where a dimension, which cannot be fractional, reaches zero.**

**Member A validated independently before anything was built on it:** occupied levels
`(ell, λ², mult) = (1,1,4), (2,4,8), (3,9,12), (4,16,16)` — the standard untwisted S² Dirac
spectrum `(k+1)²` with degeneracy `2(k+1)` per sign. The `n = 0` endpoint is not an extrapolation.

### 1.5 The family has two KINDS but countably many MEMBERS

[YOURS] `B_1` and `B_2` agree on bottom location (`+0`) and branch cardinality (2) but differ in
raw bottom weight — `32N` vs `64N`. **Kind B is not a member; it is the sub-family
`{B_n}_{n>=1}`.**

Consequence for AS2: the falsifier quantifies over *inequivalent choices*, and distinct `n` are
distinct choices, so **the rule must range over all members pairwise, not over two kinds.** If
`B_1` and `B_2` already differ at the comparison point, the falsifier fires **inside** kind B
without member A ever being consulted.

[YOURS] **Stated at the strength the record supports, and no further.** The `32N` vs `64N`
difference is **pre-quotient**; whether the unbuilt public quotient collapses it is undetermined.
765 §3 already recorded the falsifier's antecedent as structurally satisfied on exactly this pair;
this relay neither strengthens nor retreats from that — it types it.

---

## 2. AS2 — THE DISCRIMINATION RULE (**DRAFT-FOR-SEAL**; freezing is registrar/principal business)

### 2.1 What the sealed clause actually says

[PROVABLE] `PRIN`, verbatim (byte 2032):

> *"The route fails to compute alpha if two inequivalent choices of radii, spin structure, bundle
> class, or odd profile satisfy all adopted record rules while producing different weighted
> charged determinants. In that case a further physical selection principle is required; the
> desired coupling may not select the missing data retroactively."*

[YOURS] **Four features of that text the route's working paraphrase has been dropping:**

```text
(a) It enumerates FOUR axes: radii, spin structure, bundle class, odd profile.
(b) Its consequent is "A FURTHER PHYSICAL SELECTION PRINCIPLE IS REQUIRED" — not "the route
    dies."  The decision-of-record's "kills the route" is a paraphrase, and it is stronger
    than its source.
(c) The comparison quantity is the "WEIGHTED CHARGED determinant" — two qualifiers, neither
    of which is defined anywhere I can reach.
(d) PRIN's separate unselected list DOES name "the chiral index"; the hard-failure rule
    does NOT.  768 already recorded that omission as a gap in the falsifier's enumeration.
```

### 2.2 The well-posedness gate

[YOURS] A determinant needs a regularisation. Under an operator rescaling `H → mu^2 H`,
`log det` shifts by **`+2 log mu · zeta(0)`**, with `zeta_m(0) = a_{d/2}[m] − dim ker_m`,
`d = dim Sigma_BR = 4` ⇒ `a_2`, in the convention `Tr e^{-tH} ~ Σ_k a_k t^{k−d/2}`.

**Sign disclosed:** a draft of this artifact carried `−2 log mu · zeta(0)`, which belongs to the
opposite convention (`mu` as reference scale in `zeta(s;mu) = Σ(λ/mu²)^{-s}`). I settled it on a
finite spectrum where `log det` is elementary, and the corrected sign is above. **The criterion is
unaffected** — the relative shift is `±2 log mu ·(zeta_m(0) − zeta_{m'}(0))`, which vanishes for
all `mu` exactly when `zeta(0)` is member-independent.

**Scope, stated honestly:** this closes the **scale** ambiguity that necessarily attends any
regularised determinant. It does **not** close "scheme" in the broad sense. It is a
well-posedness gate on the decision's named quantity, **not a substitution for it.**

### 2.3 The schema

```text
R1  Build the public equivalence and quotient Q.                          [FC-a: BLOCKED]
R2  Instantiate L_BR^public[m] for ALL m in {A} ∪ {B_n}_{n>=1} simultaneously.  Never one alone.
R3  Form zeta_m(0) symbolically in n.                                     [DG-1 attaches here]
R4  GATE: is zeta_m(0) constant across m ?    NO -> outcome per FC-c.    YES -> R5.
R5  Form the relative quantity R(m,m') := det L[m] / det L[m'].           [DG-2, DG-3 attach]
R6  CRITERION: R(m,m') = 1 for every pair, for ALL beta, for all admissible C2_parent
    normalizations, and for all admissible (R_T : r_flux : R_Q), spin structures, and Phi.
R7  CONSEQUENT on failure: per PRIN — "a further physical selection principle is required."
    NOT an elimination.                                                   [FC-b, DG-4]
```

### 2.4 Free choices ELIMINATED — with the mechanism, and only three survive scrutiny

- **regularisation scale** — closed by R3/R4 (scheme-free invariant plus gate). §2.2.
- **comparison functional** — closed by using the **relative** quantity, which is
  normalization-free. *Residual, disclosed:* whether the object is `det D` or `det L = det D²`,
  and whether the criterion is `R = 1` or `|R| = 1` (a Dirac-type determinant carries a phase/eta
  ambiguity a ratio need not cancel), is **not settled** — folded into DG-2.
- **order (compare-then-eliminate)** — fixed by decision clause 4, not a lane choice.
- **which members** — closed *as a choice*: R2/R6 quantify universally, leaving the lane no
  discretion over which members enter and no surface for post-hoc selection. **Executability is
  NOT claimed** — see DG-1.

### 2.5 Free choices NOT eliminable — **SIX, named**

- **FC-a — the public quotient.** Unbuilt. The same element that stopped 775 AS1(i). Blocks
  execution; does not block drafting.
- **FC-b — an elimination criterion.** The falsifier's outputs are {further principle required,
  selector dissolves}; **neither eliminates a member.** Decision clause 4 is therefore satisfied
  **vacuously** on the derivable rule. And the gap cannot be filled from the evaluation itself —
  clause 4 forbids using a result to choose a member post hoc — so any elimination criterion must
  be **non-spectral and sealed**. 773 searched 2,952 files and returned `SELECTOR_CONSTRAINT =
  NONE`. **The clause is vacuous by the record's actual content, not by my oversight.**
- **FC-c — the reading of a `zeta(0)` difference.** (i) comparison ill-posed ⇒ NEUTRAL, or (ii) a
  scheme-free invariant differs ⇒ the antecedent is met. The choice decides "further principle
  required" versus "no verdict." I make no recommendation: recommending the harsher reading
  because it is safer would still be the lane choosing the rule.
- **FC-d — the `C2_parent` normalization.** `C2_parent` carries weight `beta^0` (U8), so **no
  value of `beta` reaches it**, and R4 gates only *multiplicative* rescaling `H → mu²H`, never the
  *additive* shift `H → H + C2_parent`, under which a relative determinant is not invariant. 764
  ledgers `C2_parent`'s numerical value as convention-carrying (`45/8` vs `45/4`, `45/64`,
  `45/16`). **Common-mode is not cancellation**, and the additive sector had no gate at all in my
  first draft. R6 now quantifies over it.
- **FC-e — PRIN's three axes that R6 did not quantify over.** `beta` is a **single** rescaling of
  all three radii — the shared `beta^-2` weight across `p_t²/R_T²`, `p_q²/R_Q²`,
  `ell(ell+n)/r_flux²` proves it is ratio-preserving — but the radii are **three independent
  unselected numbers**, so the two `beta`-invariant ratios `R_T : r_flux : R_Q` survive
  quantification over `beta` entirely. **Quantifying over `beta` is not quantifying over the
  radii.** The same holds for the spin structure and the odd profile `Phi`, both unselected
  (768 §2.4), both named by PRIN, and `Phi` load-bearing in `U6`'s entire second-order correction.
  R6 now quantifies over all three. *Partial protection, recorded and not oversold:* `n` also
  enters the **dimensionless** channel — `d_S2(0) = n`, bottom weight `32nN`, the cardinality drop
  `2 → 1` — which is radius-blind, so no ratio can cancel the whole `n`-dependence.
- **FC-f — the referent of "bundle class" in the hard-failure rule.** If it means the **flux line
  bundle**, the carried family is squarely inside the falsifier's scope. If it means the **parent
  bundle's characteristic class** — the phrase PRIN's unselected list actually uses — then this
  family's index enters only as *"the chiral index,"* which the hard-failure rule **omits**, and
  **the discrimination rule would be drafted over an axis its own governing falsifier does not
  name.** The sealed text supports both readings. This also decides whether `±q` are one member or
  two (§1.2). **I resolve nothing here; this is the most consequential open item in the draft.**

### 2.6 Derivation gaps — **FOUR, named** (obligations, not choices)

- **DG-1 — finite reducibility.** `U4`'s affinity is affinity of **eigenvalues and
  multiplicities** in `n`. It does **not** propagate to `log det = −zeta'(0)`, which is
  transcendental in `n` (the family's own shape `ell² + n·ell = (ell + n/2)² − n²/4` yields
  Hurwitz zetas, hence `log Γ` terms). So "equal for all `n`" does **not** reduce to a finite
  coefficient check. What *is* polynomial in `n` is the **gate** quantity `zeta_m(0)` — so R3/R4,
  and only R3/R4, reduce finitely. **My first draft claimed this reduction for R6. Withdrawn.**
- **DG-2 — "weighted."** PRIN's quantity is the *weighted* charged determinant. The weight is
  undefined anywhere I can reach, and it absorbs the `det D` / `det D²` and `R = 1` / `|R| = 1`
  residuals from §2.4.
- **DG-3 — "charged," and whether the quantity is defined on member A.** If a *charged*
  determinant means the determinant of the charged sector, member A (`q = 0`, index `0`) may have
  no such sector, in which case the comparison is **not type-correct on A** and the family is not
  comparable as constituted. Named, not adjudicated.
- **DG-4 — the consequent mismatch.** PRIN: *"a further physical selection principle is
  required."* The decision-of-record: *"kills the route."* R7 follows PRIN, being the sealed
  source; the divergence is displayed for the principal, not resolved by me.

### 2.7 The survival-bias hazard, disclosed against my own draft

[YOURS] Equal spectral multisets ⇒ equal determinants in every scheme — **scheme-free**. Unequal
multisets ⇒ **not** scheme-free. So the naive comparison can lawfully return only AGREE: **a rule
that may exculpate but never accuse** — the exact shape BR-1 exists to forbid. R4's gate is what
cures it, and only when it passes.

```text
NEUTRAL MUST NOT BE RECORDED AS SURVIVAL.  A neutral outcome leaves the falsifier UNTESTED.
It is not the route passing a test.
```

### 2.8 The bar the relay set, and whether I met it

AS2 requires a draft *"executable-in-principle by a future relay WITHOUT further choices."*

```text
RULE_DRAFT = complete as a SCHEMA with named holes.  The "without further choices" bar is
   NOT met: six free choices (FC-a … FC-f) remain, and R1 is BLOCKED.  I report the shortfall
   rather than presenting a schema that would read as closed.  Naming them IS the discharge
   AS2 provides for ("eliminate them all or name the ones that cannot be eliminated") — but
   the executability clause is a separate bar and I did not clear it.
```

---

## 3. AS3 — THE CONSISTENCY GATE: **VIOLATED**

### 3.1 The violation

```text
U7  "The branch BOUNDARY at ell = 0, for every class."     ***MEMBER-DEPENDENT***

    d_S2 is piecewise:  d_S2(0) = n ;  d_S2(ell>=1) = 4ell + 2n.
    Evaluate the SECOND formula at ell = 0:  4·0 + 2n = 2n.

      n = 0 :  d_S2(0) = 0  and  2n = 0     -> formulas AGREE.  NO BREAK.
      n >= 1:  d_S2(0) = n  and  2n = 2n    -> n != 2n.  GENUINE BREAK.

    And 775 §4.1's stated GROUND — "the pairing fails exactly at the level the index theorem
    makes unpaired" — is unavailable at n = 0: the index is 0 and NOTHING is unpaired.
    Every level of member A is chirality-paired (verified level by level).
```

**Structural reading:** `U7` is false for A — there is no boundary, because there is no break and
nothing unpaired. **Notational reading:** the case split is *written* at `ell = 0` regardless of
`n`, so `U7` holds trivially — but then it is a fact about notation, not the substantive
structural invariant the uniform core is supposed to carry.

**Under neither reading is `U7` a substantive fact derivable identically from both members.**

### 3.2 The correction, principal-facing

```text
Q-682 CORRECTION (mine, against my own 775):
   U7 moves from UNIFORM_CORE to CLASS_DEPENDENT.  The selector-independent core is EIGHT
   items, not nine.  Add to the class-dependent family:
   C6  the existence of a branch boundary at ell = 0 — present on kind B, absent on member A.

   DIRECTION OF THE ERROR: this REMOVES an item from what 775 called "the deliverable," and
   it STRENGTHENS 775's trigger — U7 was the item containing C5 as mere occupancy of a
   uniform structure.  If the boundary exists only on B, the STRUCTURE differs, not just its
   occupancy.  The correction costs the lane its claimed win; it does not buy one.
```

### 3.3 The remaining eight — and three disclosed asymmetries

| item | identical from both? | disclosure |
|---|---|---|
| `U1` | **rank YES; carrier NOT SETTLED** | `rank = 4 × 16 = 64` is twist-independent. But `H_BR = L2(Σ_BR, S_Σ ⊗ E_parent)` **does not display the flux line bundle at all**, so whether members are sections of the *same* space is unsettled by the sealed text — and R5 compares determinants across members. **SPEC GAP, reported not peeked.** |
| `U2` | YES *as written* | 775's own qualifier *"the labels and the form, not the values"* is **load-bearing**: under a SPECTRUM reading `U2` fails for A (the `ell=0` values are absent from A's spectrum). The qualifier does work it was not obviously doing when written. |
| `U3` | YES in content | **warrant asymmetric in ONE conjunct only.** 768's flat `×16` is conditional on (a) the flux U(1) being external to Spin(10) — **vacuous for A**, load-bearing for B — and (b) a flat parent connection, which carries **no `n`-dependence** and is therefore equally load-bearing on both. A draft of this artifact called the whole condition vacuous for A, dropping conjunct (b). Corrected. |
| `U4` | YES | sharpened to integer-affine under `n` (§1.2). |
| `U5` | YES | bound uniform and identical. **Attainment is NOT a member asymmetry** — it turns on `0 ∈ Λ` (spin structure, unselected), not on `n`. What is member-relative is the **offset**: B's bottom sits exactly 1 below A's, for every spin structure. |
| `U6` | YES | no step of the bidegree/zero-diagonal argument mentions `n`. `∇_A Φ` does not degenerate at `n = 0`: the connection includes the parent, and `Φ` is unselected but common. |
| `U8` | YES | all three weights `n`-independent. |
| `U9` | YES | the five open elements and their TYPE-R closures are class-independent obstructions. *Noted:* the quotient — one of those five — is doing member-relevant work at §1.5, where it may or may not collapse intra-B variation. That is the quotient being **unbuilt**, not `U9` being class-dependent. |

---

## 4. AS4 — ELEMENTS UNDERIVABLE FROM SEALED STOCK

```text
R1 / FC-a   the public equivalence and quotient      UNBUILT   (775 AS1(i), unchanged)
FC-b        an elimination criterion                 ABSENT from the falsifier; unfillable
                                                     from the evaluation; no sealed selector
                                                     exists (773, 2,952 files, NONE)
DG-2        the "weight" in "weighted determinant"   UNDEFINED in reachable stock
DG-3        whether "charged determinant" is         UNSETTLED
            type-correct on member A
FC-f        the referent of "bundle class"           AMBIGUOUS in the sealed text
```

### 4.1 A record correction carried forward

[PROVABLE] 765 §3 records *"Eliminated: q = 0 only (R9, and by the branch being CHARGED)."*
**Both grounds are superseded of record and I restate that here so no future reader reinstates
them from 765 alone:** 768 §1.3 refuted R9 — the untwisted spectrum `(k+1)²` also satisfies the
locked floor, since `(k+1)² − k(k+1) = k+1 > 0` — and 770 refuted the charged ground, since an
ordinary charged durable source has `Q_Sigma = 1` and `deg L = 0`. **Member A is admissible of
record**, which is what the governing decision presupposes in carrying it.

---

## 5. GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Jurisdiction

**On reporting a gate violation against my own prior core.** *What was the rule written to
protect?* AS3 exists so a silent member-dependence in the core cannot ride into a downstream rule.
The risk was live: `U7` was the item containing `C5`. *Does the outcome space distinguish false
from cannot-see?* Yes — §3.1 shows the arithmetic both ways and names the reading under which
`U7` is merely vacuous rather than false. *Would the evidence appear if 775 were right?* Yes: if
the pairing failed at `ell = 0` for A too, the formulas would break there and the index would not
vanish. Both are checkable and both come out against 775.

**On the VOID CONDITION.** No class adopted, preferred, or eliminated. The `U7` finding is
symmetric — it reports that the core item is member-dependent, not that either member is right.
FC-c is left open rather than resolved toward the harsher reading, precisely because choosing a
rule for its outcome is the thing the condition forbids.

**On BR-1.** The decision authorises the family build; it does not authorise its result. Every
finding here runs against the lane's own prior work.

**On builder independence.** No file under `evaluator_build_A/` or `checks/` was read, by me or by
any commissioned checker. `U1`'s carrier gap (§3.3) is reported as a **SPEC GAP**, not resolved by
looking where I may not look. `~/.codex` untouched; `memory-bank` never searched.

### 5.2 Self verb audit — **NOT CLEAN: five disclosures**

1. **AS3's gate fired against my own 775 core.** `U7` is member-dependent. The uniform deliverable
   is eight items, not nine.
2. **Three of the five free choices I first claimed to have eliminated were not eliminated** —
   `C2_parent`'s additive sector (FC-d), the `beta`-invariant radius ratios plus spin structure and
   `Phi` (FC-e), and the finite-reducibility claim for R6 (DG-1, **withdrawn**). My first draft
   said "three free choices I cannot eliminate." The honest count is **six**, plus four gaps.
   *"Common-mode is not cancellation"* is the lesson, and it was available in my own §2.2.
3. **I asserted `U5` ATTAINED**, silently setting `p2min = 0` — which selects the spin structure
   768 records as unselected, and fixes exactly the quantity 775 §5.2 expressly declined to fix.
   Withdrawn; `p2min` restored as a symbol in every row. **This is the over-claiming-determination
   failure mode again, and it is the fourth occurrence this session.**
4. **A sign error** in the zeta-scaling relation (`−2 log mu` for `+2 log mu`), self-caught and
   settled numerically. The criterion was unaffected — but the lane's record on
   convention-dependent constants is exactly why I checked rather than reasoned.
5. **I drafted the entire rule against a paraphrase of the falsifier rather than its text**, and
   only read `PRIN`'s hard-failure rule verbatim after a completeness critic asked why nobody had.
   The text names four axes my R6 did not quantify over, carries two qualifiers my `det L[m]`
   dropped, and has a **different consequent**. **Everything in §2.1 should have been the first
   thing I read, not the last.**

*Instrument note:* 1, 2, 3 and 5 were surfaced by commissioned adversarial verification (five
independent hostile lenses plus a completeness critic; eleven defects confirmed after each was
itself attacked, nineteen findings refuted). 4 was self-caught. **The pattern from 746/755/764/770
holds: my reasoning survives contact; my instruments and my reading discipline are the weak part.**
This is the first relay this session where the hostile check ran **before** the seal rather than
after, and it changed the artifact substantially.

---

```text
FAMILY = built (A and {B_n}_{n>=1} side by side; U1-U9 quoted verbatim and factored out once;
   only C1-C5 instantiated per member).  Derived integer reparametrisation n := 2|q| = |deg L|
   makes every class-dependence INTEGER-AFFINE (verified, 2|q| = 0..15, ell = 0..39, zero
   mismatches; second differences in n vanish identically).  Member A validated against the
   standard untwisted S^2 Dirac spectrum EXACTLY: (1,1,4) (2,4,8) (3,9,12) (4,16,16).
   STRUCTURAL FINDING: the carried family has TWO KINDS but COUNTABLY MANY MEMBERS — kind B is
   the sub-family {B_n}_{n>=1}, whose members differ from EACH OTHER (raw bottom weight 32nN),
   so a two-way A-vs-B rule is ill-posed on the object the decision carries.  p^2_min carried
   as a symbol throughout; the A-B bottom OFFSET is exactly 1 and is p^2_min-free.
CORE_INVARIANCE = VIOLATED (displayed).  U7 ("the branch BOUNDARY at ell = 0, for every class")
   is MEMBER-DEPENDENT: at n = 0 the two branch formulas coincide at ell = 0 (d_S2(0) = n = 0
   and 4·0+2n = 0), so there is no break; and 775 §4.1's stated ground — "the pairing fails
   exactly at the level the index theorem makes unpaired" — is unavailable at n = 0, where the
   index vanishes and every level is chirality-paired.  Structural reading: FALSE for A.
   Notational reading: VACUOUS for A.  Neither is a substantive fact derivable identically from
   both members.  Q-682 CORRECTION: U7 moves to CLASS_DEPENDENT as C6; the selector-independent
   core is EIGHT items, not nine.  The correction removes an item from what 775 called its
   deliverable and STRENGTHENS 775's trigger — U7 was the item containing C5 as mere occupancy
   of a uniform structure.  Three further asymmetries disclosed without violation: U1 invariant
   in RANK but its CARRIER unsettled (H_BR's expression does not display the flux line bundle —
   SPEC GAP, reported not peeked); U2 invariant only under its own load-bearing qualifier "the
   labels and the form, not the values"; U3 warrant asymmetric in ONE conjunct only (externality
   vacuous for A; flat parent connection common to both).  U5 attainment WITHDRAWN as a member
   asymmetry — it turns on 0 in Lambda (spin structure, unselected), not on n.
RULE_DRAFT = complete as a SCHEMA with named holes (free choices: SIX, named).
   FC-a the unbuilt public quotient.  FC-b an elimination criterion — the falsifier's outputs
   eliminate no member, so decision clause 4 is satisfied VACUOUSLY; unfillable from the
   evaluation (clause 4 forbids post-hoc selection) and no sealed non-spectral selector exists
   (773: 2,952 files, SELECTOR_CONSTRAINT = NONE).  FC-c the reading of a zeta(0) difference.
   FC-d the C2_parent normalization — beta^0 weight, so no beta reaches it, and R4 gates only
   multiplicative rescaling, never the additive shift; COMMON-MODE IS NOT CANCELLATION.
   FC-e PRIN's three axes R6 did not quantify over — the two beta-invariant radius ratios
   (beta is ONE rescaling; the radii are THREE unselected numbers), the spin structure, and Phi.
   FC-f the referent of "bundle class" in the hard-failure rule: flux line bundle (family in
   scope) versus parent characteristic class (family indexed by the chiral index, which the
   hard-failure rule OMITS) — the sealed text supports both, and the second reading would mean
   the rule is drafted over an axis its own falsifier does not name.
   FOUR derivation gaps named: DG-1 finite reducibility WITHDRAWN (log det is transcendental in
   n; only the gate quantity zeta_m(0) reduces finitely); DG-2 "weighted" undefined; DG-3
   whether a "charged" determinant is type-correct on member A at all; DG-4 the consequent
   mismatch — PRIN says "a further physical selection principle is required", the decision-of-
   record paraphrases "kills the route", and R7 follows PRIN as the sealed source.
   AS2's "executable-in-principle WITHOUT further choices" bar is NOT met, and I report the
   shortfall rather than present a schema that would read as closed.
   SURVIVAL-BIAS HAZARD disclosed: only the AGREE branch is scheme-free unaided, so the naive
   comparison may exculpate but never accuse; R4's gate is the cure, and NEUTRAL MUST NOT BE
   RECORDED AS SURVIVAL.
NOTHING_EVALUATED = true.  No determinant, no zeta, no heat coefficient, no f chosen, no sum
   evaluated, no member evaluated alone, no class adopted or eliminated.  Printed integers are
   dimensionless multiplicities and eigenvalue labels; p^2_min, C2_parent, the radii and beta
   remain symbols throughout.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+5): (1) AS3's gate fired against my own 775 core; (2) three of
   five "eliminated" free choices were not eliminated — honest count six, not three; (3) I
   asserted U5 ATTAINED by silently setting p^2_min = 0, fixing exactly what 775 declined to
   fix — the over-claiming-determination failure mode, fourth occurrence this session;
   (4) a sign error in the zeta-scaling relation, self-caught, criterion unaffected; (5) I
   drafted the whole rule against a PARAPHRASE of the falsifier and read PRIN's hard-failure
   rule verbatim only after a completeness critic asked why nobody had.
   1, 2, 3 and 5 were surfaced by commissioned adversarial verification run BEFORE the seal
   rather than after — the first time this session — and it changed the artifact substantially.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
