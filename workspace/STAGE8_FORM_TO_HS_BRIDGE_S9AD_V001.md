# STAGE 8 — THE FORM-TO-HS BRIDGE: THE CERTIFIED C-L2 FORM BOUND AT THE THREE CONSUMING SITES' OWN COMPRESSIONS — S9AD V001

## BLIND BUILDER — CODENAME FHB-BUILD — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15)
Commission: the one residue three items share — does the certified C-L2 form
bound (Q-1079's build, consumed at audited grade), used AT THE CONSUMING
SITES' OWN STRUCTURES (sandwiched by the compressions/projections those sites
actually apply), yield the needed HS/trace-grade control at ANY site — full,
partial, or provably not? Site-by-site, exact. Sites: (i) R.3's HS-typed
||A(0)||_2 slot; (ii) the wall's F3 identity-consumption sites; (iii) the
half-plane's layer-1 (the slice denominator's C-L2 deviation, per the audit's
c-2 extension). Known blocks NOT re-walked (B-L2* stratum-resummation
FAILS-AT; beta a symbol; the rank x op grade ceiling) — gone around or named.

Gates, LIVE THROUGHOUT: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
`fhbvenv` under the session scratchpad; script + output verbatim §7); no
floats as ground; no measured constant; no value frozen (the formal-Lambda
displays use the record's standing WB7 license). No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened. "Q-..." tokens
EXPECTED-UNLOCATABLE, noted not chased. No git; nothing registered/committed/
pushed; no existing file edited. ONE output + seal sidecar at the
commission-distinct path, probed ABSENT at session start AND re-probed
immediately before write. Marks: DERIVED / CLAIMED / CONDITIONAL(premise
named) / GROUNDED(record cited at grade) span by span.

---

## 0. VERDICT IN ONE LINE

**THE BRIDGE EXISTS AS AN EXACT COMPRESSION CALCULUS AND ITS ANSWER IS
SPLIT THREE WAYS, NONE FULL: SITE (i) R.3 = FAILS-AT(compression type) — the
site's own sandwich is the INFINITE-RANK, h_0-COMMUTING sea projection C =
1_(-inf,0)(h_0) (r-2 bytes), which preserves the certified form bound
VERBATIM (constant 2 sqrt(3) unchanged — form-grade BRIDGED, exact, delivered
meanwhile) and provably creates NO HS decay: the certified hypothesis class
contains h_0 itself, and C h_0 C = -|k| C(k) is a nonzero Fourier multiplier
— translation-invariant, non-compact, NOT Hilbert-Schmidt (CAS FB1b/FB1c/FB7)
— so NO HS conclusion at this site follows from the certified asset alone,
ever; the discharge must consume kernel structure beyond form-class
membership, which is exactly the standing (d-2) B-L2* + (d-3) beta blocks,
confirmed binding from a NEW direction. SITE (ii) WALL F3 = PARTIAL — at the
wall's own finite-rank compressions (C_n <= Q_(n,ell), reduced by the Schatten
contraction property), the form bound DOES convert, exactly: the FIRST
trace/HS-grade enclosures of the F3 modulo-layer's GENERATOR at the
identity-consumption sites — |tr| and ||.||_2 <= 2 sqrt(3) sqrt(r_n E_n) =
12 sqrt(2) n^{7/2}/ell and ||.||_1 <= 24 sqrt(2) n^5/ell per (n, cell), with
the sealed Hermite carrier's energy trace DERIVED IN CLOSED FORM, E_n =
tr(Q h_0^2 Q) = 6 n^4/ell^2 EXACT (FB5), and a refined three-channel split
(surface/bulk/leak) whose surface channel is a genuine SUB-VOLUME trace law
||C_n T_surf C_n||_1 <= sigma_n(r) with sigma_{Q,2}(r) = 16 r^2 e^{-r^2}
pi^{-1/2}(1 + 2r^2 + (4/5)r^4 + (8/105)r^6) EXACT at the executed member
(FB6) — but the n-UNIFORM discharge F3 needs is PROVABLY NOT a consequence of
the form bound (non-implication witness FB9; every ceiling polynomially
divergent; sigma_n -> +infinity with the continuum surface trace), and the
full LAYER (interaction-dressed, multi-insertion) stays behind the cert's own
named H^1-propagation residual. SITE (iii) HALF-PLANE LAYER-1 = FAILS-AT(the
smallness threshold) — the determinant-deviation consumption needs
||A_dev||_1 < log 2, and the asset's BEST channel already exceeds that
threshold at the FIRST sealed carrier member by exact chain
(sigma_{Q,2}(1/2) = (1303/210) e^{-1/4}/sqrt(pi) > 3909/1680 > 1 > log 2,
FB6g; growing in n; higher Duhamel layers outside the asset) — the form route
cannot certify den_n away from 0 at ANY n, so c-2's layer-1 remains
undischarged with its gap now QUANTIFIED. SHARED RESIDUE, named exactly: an
n-uniform (or cell-power-carrying) trace/HS rate for the SPECIFIC C-L2 kernel
beyond form-class membership — verbatim MO-3 / R-L2b(beta) / B-L2*, the known
blocks, now shown binding at every site from inside the form route itself.
BONUS FINDING, correction-grade (FC-1, CAS FB2/FB3): the cert's §3.4(ii)
exhibit and the audit's P-1 display are INCOMPLETE at bytes — [h_0, 1_B (x) S]
= i(alpha.n-hat (x) S) delta_{dB} + 2*1_B h_0 S: the S-DRESSED commutator
carries a VOLUME-supported bulk term (the {S, h_0} = 0 echo, [h_0, S] =
2 h_0 S) missing from both displays, at bilinear AND quadratic grade (FB2b/
FB2d, explicit states); the S-LESS [h_0, 1_B] IS pure surface (FB2c), so
E1's of-record exclusion locus (:1139-1141) is untouched and the certified
bound (which never used the exhibit) is unaffected — but every consumer of
the "surface pairing" reading of the C-L2 layer must carry the bulk term,
and the wall's F2 phrasing "every closure decay asset is boundary-supported"
acquires a bookkeeping footnote: the S-dressed generator itself has an
interior piece. NOT REFUTED: all ceilings are one-sided; no divergence of
any actual compressed error-layer object is exhibited; every consuming
quantity remains UNDECIDED. CAS 27/27 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every digest
recomputed from bytes at path THIS session by FULL digest; each compared
against its `.seal.sha256` sidecar AND the tasked prefixes (where tasked).
9/9 MATCH; NONE UNVERIFIABLE.

```text
MATCH  e6838ffc  STAGE8_CL2_GCM_CERTIFICATION_S9AD_V001.md        (tasked e6838ffc) [CERT — the asset]
MATCH  180114a7  STAGE8_CL2_GCM_CERTIFICATION_S9AD_AUDIT_V001.md  (tasked 180114a7) [CERT-AUDIT — CONFIRMED-WITH-CORRECTIONS; COR-1..COR-6 carried]
MATCH  80db260f  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md        (tasked 80db260f) [WALL — site (ii)]
MATCH  f5b887dc  STAGE8_HALF_PLANE_CLOSURE_S9AD_V001.md           (tasked f5b887dc) [HPL — site (iii)]
MATCH  f6f45e6d  STAGE8_HALF_PLANE_CLOSURE_S9AD_AUDIT_V001.md     (the c-2 extension carrier)
MATCH  46846730  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md      [E1 — site (i) at :803-833; C-L2 :1131-1144]
MATCH  789338ad  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md  [PA — carrier A1 at bytes]
MATCH  3b5e95b6  STAGE8_R2_KAPPA_N_DETERMINATION_S9AD_V001.md     [r-2 — C = 1_(-inf,0)(h_0) = P_-, symbol (I - alpha.p-hat)/2, :144-148]
MATCH  bebc0f08  STAGE8_R3_JOINT_RATE_S9AD_V001.md                [r-3 — the rank x op ceiling lineage; consumed via WALL WB6 at audited grade]
```

Consumption grades: CERT consumed AT AUDITED GRADE — the audit's corrections
carried throughout (COR-4 byte re-pins used: F1 = WALL :401-407; R.3 = E1
:811-814; COR-5's ruling that the OPERATIVE definition of the certified form
is the derivative-placed BILINEAR display is adopted as the hypothesis class
below; COR-1's CL-ERROR OBJECT_FIDELITY-only rule respected — CL-ERROR not
opened here; nothing consumed from it beyond what CERT displays). WALL and
HPL(+audit) consumed at their own sealed grades; the HPL audit's c-1
correction respected (den_n NOT identified with GAP-C; its C-L2 deviation is
site (iii)'s object, per c-2). PA :35-90 read for the carrier definition
(Hermite functions, oscillator length ell; executed member n = 2,
ell in {1, sqrt(2)}, spatial dimension 8, total dimension 32).

SWEEP CUTOFF, declared: 2026-08-14 22:33:52 -0500 (both roots; fenced classes
matched by filename only, never opened). Workspace artifacts sealed AFTER my
seed stock and before cutoff were classed at verdict/headline grade:
STAGE8_PIN_DETERMINATION (semantic pins), STAGE8_AMPLITUDE_JUNCTION (+audit;
bare-surface junction), STAGE8_OB2_EMPTINESS (+audit; J-II families),
STAGE8_EARN_THE_TRACE (+audit; horn-a surface landing) — none carries
form-to-HS, compression, or C-L2-bridge content (headers + targeted greps §10).
Anything sealed after the cutoff is UNSWEPT and unclassed here.

---

## 2. THE ASSET AT AUDITED GRADE, AND ONE EXHIBIT CORRECTION (FC-1)

### 2.1 The hypothesis class (the certified asset, as consumed)

Per CERT §3.2 Step 2 (the operative bilinear display, per audit COR-5) with
the continuum consolidation Step 3, the certified asset is EXACTLY:

```text
H(sqrt3) :=  the statement that t_g[phi, psi] := <phi, [h_0, M(t) (x) S] psi>
   (defined by the derivative-placed bilinear display) obeys, for all
   phi, psi in D(h_0) = H^1(R^3) (x) C^4, all t in [0,1], every admitted cell:
      |t_g[phi, psi]| <= sqrt(3) ( ||h_0 phi||.||psi|| + ||phi||.||h_0 psi|| )
   quadratic case: |q_t(psi)| <= 2 sqrt(3) ||h_0 psi||.||psi||; relative form
   bound ZERO w.r.t. h_0^2; free-dressed single insertion D(t) in the same
   class with constant t.sqrt(3); weight-summed v-integrated constant
   pi.sqrt(3) exact. (All GROUNDED: CERT §3.2-§3.3, audit §4.1 a-1..a-6.)
```

Two facts about the class, both load-bearing below [DERIVED]:

```text
(cl-1) FREE-DRESSING INVARIANCE: t_s[phi,psi] := t_g[e^{-i h_0 s}phi,
       e^{-i h_0 s}psi] is in H(sqrt3) with the SAME constant
       (||h_0 e^{-i h_0 s}phi|| = ||h_0 phi||, functional calculus, named).
       Every energy-channel display below is therefore dressing-uniform.
(cl-2) h_0 IS IN THE CLASS: |<phi, h_0 psi>| <= ||phi||.||h_0 psi||
       (Cauchy-Schwarz), constant 1 <= sqrt(3). The class pins NOTHING about
       its members' kernels beyond one derivative's worth of form growth —
       this is the exact reason the bridge cannot be full (§5).
```

### 2.2 FC-1 — THE EXHIBIT CORRECTION (correction-grade; CAS FB2a-d, FB3)

The CERT §3.4(ii) exhibit and the audit's P-1 object line both display

```text
   [h_0, 1_B (x) S] = i(alpha.n-hat (x) S) delta_{dB}     (surface only)
```

This is INCOMPLETE at bytes. The exact identity (operator algebra:
[h_0, 1_B S] = [h_0, 1_B]S + 1_B[h_0, S], with [h_0, S] = 2 h_0 S from
{S, alpha_j} = 0 — CAS FB3 entrywise over noncommutative p_j) is

```text
   [h_0, 1_B (x) S] = i(alpha.n-hat (x) S) delta_{dB}  +  2 . 1_B h_0 S
```

— the S-DRESSED commutator carries a VOLUME-supported bulk term. CAS FB2a
verifies the corrected identity exactly (1-D two-band schema, complex smooth
states, every integral exact); FB2b and FB2d show the gap is NONZERO at both
bilinear and quadratic grade on explicit states; FB2c shows the S-LESS
[h_0, 1_B] IS pure surface. Consequences, stated at their exact reach:

```text
(fc-1a) THE CERTIFIED BOUND IS UNAFFECTED: CERT §3.2 Steps 1-4 never use the
   exhibit; the audit re-derived them independently (a-1..a-4). H(sqrt3)
   stands as consumed.
(fc-1b) THE EXCLUSION VERDICT (o3) IS UNAFFECTED: its conclusion rides E1's
   of-record S-LESS display ||[h_0, 1_B]|| = +infinity (:1139-1141), which
   FB2c confirms is the pure-surface object. But the CERT §3.4(i) witness
   ARGUMENT and the audit's own pre-committed P-1/P-2 route consume the
   surface-only reading of the S-DRESSED form and would need the bulk term
   carried; registrar note, not a verdict change (the of-record ground is
   independent).
(fc-1c) CONSUMERS OF "THE FORM IS A SURFACE PAIRING": must carry the bulk
   term. In particular the wall's F2 phrasing ("every closure decay asset is
   boundary-supported ... the phase collapse is exact on the ball") acquires
   a bookkeeping footnote: the C-L2 generator itself has an INTERIOR piece
   2.1_B h_0 S; F2's locus RULING (diagonal vs boundary, E1 C6 R5) is not
   touched — the bulk term is a divergence-source-side object, not a decay
   asset — but the sentence "the odd sector's would-be majorant lives in the
   [purely boundary-supported] layer" is not byte-accurate about the layer's
   support. No wall verdict moves.
(fc-1d) DETECTION SURFACE, disclosed: the radial/real test states used by
   B7 (CERT) and I4a' (audit) make the bulk term vanish identically by
   parity — exactly the degeneracy my own first FB2b state pair reproduced
   before parity-mixing (§8 ch-8). One mechanism, three artifacts.
```

This artifact EDITS NOTHING; FC-1 is reported for the registrar's splice.

---

## 3. THE BRIDGE — THE COMPRESSION CALCULUS [DERIVED]

The commission's hinge is exact: form bounds compose with compressions
differently than operator bounds. Operator bounds compose as ||Pi T Pi|| <=
||T|| — VACUOUS here (||T||_op = +infinity of record). Form bounds compose
through the STATES: the compression pays an energy price, and for finite rank
that price is a trace. All displays exact; standard steps named (the same
license as WALL C-5).

### 3.1 The lemma (finite-rank compression of a class member)

Let t in H(c) (constant c; here c = sqrt3), and let Pi be an orthogonal
projection, rank r < infinity, ran Pi c D(h_0), with ONB {e_i}. Define
T_Pi := the operator on ran Pi with matrix t[e_i, e_j] (basis-independent).
With E := tr(Pi h_0^2 Pi) = Sum_i ||h_0 e_i||^2:

```text
(L-1) OP:        ||T_Pi||_op <= 2c ||h_0 Pi||_op                    (finite)
(L-2) TRACE:     |tr T_Pi|   <= 2c Sum_i ||h_0 e_i|| <= 2c sqrt(r E)
                                        (quadratic display + Cauchy-Schwarz)
(L-3) HS:        ||T_Pi||_2  <= 2c sqrt(r E)
                     (entrywise |t[e_i,e_j]| <= c(a_i + a_j), a_i = ||h_0 e_i||;
                      Sum_{ij}(a_i+a_j)^2 <= 4 r Sum a^2 — CAS FB4a/FB4c)
(L-4) TRACE-NORM: ||T_Pi||_1 <= sqrt(r) ||T_Pi||_2 <= 2c r sqrt(E)
                                        (singular-value Cauchy-Schwarz, FB4b shape)
(L-5) CONTRACTION REDUCTION: for any contraction X with X = XQ = QX (Q a
      projection with ran Q c D(h_0)) and any Schatten grade gamma:
      ||X T_Q X||_gamma <= ||Q T Q||_gamma (Schatten ideal property, named).
      All sites' sandwiches (C_n pure or mixed, C_n <= Q_(n,ell)) reduce to
      the carrier projection Q_(n,ell).
```

By (cl-1) every display holds verbatim for the free-dressed insertion (extra
factor t) and for the lambda-weighted v-integrated single-insertion layer
(extra factor Sum|w||lambda| . Int v = (sqrt2/2)(pi/sqrt2) = pi/2 on the
CERT's constants — exact, in-alphabet).

### 3.2 The sealed carrier's exact prices (PA A1 Hermite carrier) [DERIVED]

ran Q_(n,ell) = span of 3-D Hermite functions (0 <= a,b,c < n) tensor the
spinor factor — Schwartz class, so ran Q c D(h_0) holds with room (premise
of §3.1 DERIVED at PA bytes, not assumed). Then, all EXACT (CAS FB5):

```text
r_n = 4 n^3                          (executed member n = 2: 32 = PA's dimension)
E_n = tr(Q h_0^2 Q) = 4/ell^2 . Sum_{a,b,c<n}(a+b+c+3/2) = 6 n^4 / ell^2
      (1-D <p^2> = a + 1/2 exact, FB5a; spinor factor 4; closed form FB5c)
E_leak = tr(Q h_0 (1-Q) h_0 Q) = 6 n^3 / ell^2
      (h_0 moves the Hermite carrier by ONE shell — the ladder, FB5b; only the
       top shell leaks; count and spinor trace exact, FB5d)
=> the lemma's carrier constants (c = sqrt3):
   |tr|, ||.||_2  <=  2 sqrt(3) sqrt(r_n E_n)  =  12 sqrt(2) n^{7/2} / ell
   ||.||_1        <=  24 sqrt(2) n^5 / ell                       (CAS FB4d)
   ||.||_op       <=  2 sqrt(3) ||h_0 Q||_op  <=  2 sqrt(3) sqrt(E_n) (crude)
```

### 3.3 The three-channel refinement at the sealed sharp vertex [DERIVED]

At the carrier sites the sealed vertex is M(t) = Q 1_B Q (PA bytes; D6'
sharp). On carrier states the generator's form splits EXACTLY (each step
displayed; FC-1's corrected identity):

```text
t = t_surf + t_bulk + t_leak :
t_surf[phi,psi] = i oint_{dB_r} phi^dag (alpha.n-hat (x) S) psi dS
   — FACTORIZES through L^2(dB_r): T_surf,Pi = i (R Pi)^* (alpha.n-hat S) (R Pi),
   R = sphere restriction, the vertex matrix unitary pointwise; hence
      ||Pi T_surf Pi||_1 <= ||R Pi||_2^2 = tr(Pi delta_{dB_r} Pi) =: sigma_n(r)
   — a SUB-VOLUME (surface) trace law. At the executed member (ell = 1):
      sigma_{Q,2}(r) = 16 r^2 e^{-r^2} pi^{-1/2} (1 + 2 r^2 + (4/5) r^4 + (8/105) r^6)
   EXACT (Christoffel diagonal FB6b + spherical moments FB6a -> FB6c);
   general ell by the exact scaling sigma(r; ell) = sigma(r/ell; 1)/ell.
t_bulk[phi,psi] = 2 <phi, 1_B h_0 S psi>   (FC-1's bulk term):
      |tr| , ||.||_2 <= 2 sqrt( v_n(r) E_n ) ,  ||.||_1 <= 2 sqrt(r_n) sqrt(v_n E_n)
   with v_n(r) := tr(Pi 1_{B_r} Pi) <= r_n the carrier ball-volume mass
   (S-conjugation handled by S h_0 S^dag = -h_0, FB3, and monotonicity
   through ran Q).
t_leak[phi,psi] = <(1-Q) h_0 phi, (1_B (x) S) psi> - <(1_B (x) S) phi, (1-Q) h_0 psi>
   (the Galerkin top shell; the price is E_leak = 6 n^3/ell^2):
      |tr| , ||.||_2 <= 2 sqrt( r_n E_leak ) = 4 sqrt(6) n^3 / ell .
```

### 3.4 What the calculus PROVES CANNOT follow (the ceiling on the bridge)

```text
(neg-1) NON-IMPLICATION [DERIVED, CAS FB9 — abstract witness, TOY-tagged
   §9]: there is an in-class form (T = h on its own space, constant 1) and
   finite-rank projections in D(h) with compressed trace and HS masses
   diverging. "H(sqrt3) => n-uniform compressed trace/HS control" is FALSE
   as an inference. Any n-uniform discharge at any site must consume
   structure of the SPECIFIC kernel beyond class membership.
(neg-2) SITE-FAITHFUL GROWTH: every delivered ceiling diverges on the sealed
   family — E_n, E_leak polynomially (closed forms above); sigma_n(r) ->
   +infinity (trace lower semicontinuity toward the continuum surface trace
   tr(C delta_{dB} C), whose divergence is the record's own boundary-class
   story, WALL CS-3 kappa_n side); and the bounds are ORDER-SHARP within the
   class (the FB9 witness saturates the r.E shape up to the constant).
(neg-3) THE NEW CHANNEL DOES NOT REOPEN F1: the energy channel is
   rank x ENERGY, not rank x op — r-3's proof does not literally cover it —
   but its carrier-power arithmetic lands the same way (n^{7/2}, n^5 vs an
   n-free target; the power never cancels to n^0). The F1/MO-3 ceiling is
   gone AROUND and found standing, not re-walked.
```

---

## 4. SITE BY SITE — THE HONEST MENU, EXACT

### 4.1 SITE (i) — R.3's ||A(0)||_2 <= |C|_4^beta G_cm (E1 :811-814)

The site's OWN structure: A(0) = C (error layer) C on the surviving sector —
the sandwich is the CONTINUUM sea projection C = 1_(-inf,0)(h_0) = P_-,
symbol (I - alpha.p-hat)/2 (r-2 :144-148): INFINITE RANK and a SPECTRAL
PROJECTION OF h_0.

```text
DELIVERED [DERIVED, CAS FB1b]: [C, h_0] = 0 and ||C|| = 1, so the certified
   form bound survives the site's sandwich VERBATIM:
      |<psi, C [h_0, M(t)(x)S] C psi>| <= 2 sqrt(3) ||h_0 psi||.||psi||,
   constant unchanged, all quantifiers of the CERT intact.
   FORM-GRADE = BRIDGED (exact).
VERDICT AT THE SITE'S TYPE (HS): FAILS-AT(compression type) — PROVABLY NOT
   from the asset [DERIVED, CAS FB1c + FB7]: h_0 is IN the certified class
   (cl-2), and C h_0 C = h_0 C = the Fourier multiplier -|k| C(k), nonzero
   (tr|(-|k|C)(k)|^2 = 2|k|^2), translation-invariant, non-compact, NOT
   Hilbert-Schmidt (formal-Lambda display (8 pi/5) Lambda^5 -> +infinity,
   WB7 license). The infinite-rank h_0-commuting sandwich preserves form
   grade and can create NO HS decay for a class member. The surface channel
   fails identically here: its price tr(C delta_{dB} C) is the divergent
   continuum boundary-class object. So no repair ON THE FORM SIDE can
   discharge this site — the discharge must consume kernel decay beyond
   class membership, i.e. exactly the standing blocks (d-2) B-L2*
   stratum-resummation (FAILS-AT of record) and (d-3) beta (symbol, R-L2b).
   The bridge question at site (i) is closed NEGATIVELY at the asset's own
   strength, not merely "not today".
FIXED-n SUBLAYER (u-4's x_n family): C_n finite rank — the site then reads
   as site (ii)'s calculus; per-n exact ceilings exist (§3.2), non-uniform.
```

### 4.2 SITE (ii) — the wall's F3 (identity-consumption sites; WALL :414-419, audit re-pin :415-420)

The site's OWN structures: the four trace/HS-typed quantities per (n, cell)
built from C_n-sandwiches (C_n <= Q_(n,ell); contraction reduction L-5), with
the closure identities holding modulo the C-L1 [error] layer whose generator
is the C-L2 commutator; F3 needs the layer's DISCHARGE at those sites.

```text
DELIVERED [DERIVED]: the first trace/HS-grade enclosures of the modulo-
   layer's GENERATOR at the wall's own compressed sites, per (n, cell),
   every constant exact and carrier-explicit:
      |tr(C_n T_g C_n)| , ||C_n T_g C_n||_2 <= 12 sqrt(2) n^{7/2} / ell
      ||C_n T_g C_n||_1                     <= 24 sqrt(2) n^5 / ell
   (T_g the generator; §3.1-§3.2); free-dressed single insertion: same
   displays with factor t (dressing-uniform by cl-1); lambda-weighted
   v-integrated single-insertion layer: extra exact factor pi/2. Refined
   split (§3.3): surface channel ||.||_1 <= sigma_n(r) — SUB-VOLUME: at the
   executed member sigma_{Q,2}(1/2) < 1303/210 < 32 = r_2 (exact chain,
   FB6g) — an exact factor >= 5 below the rank budget, the first realized
   instance of the MO-3 SHAPE (a sub-volume trace rate) at the generator
   layer; bulk and leak channels priced exactly (v_n E_n; E_leak = 6n^3/ell^2).
   Before tonight the generator had NO bound of any trace/HS grade at these
   sites (op-norm +infinity; rank x op vacuous). This TIGHTENS what the
   modulo-layer is, per (n, cell), quantitatively.
NOT DELIVERED, with proof shape: (a) the n-UNIFORM discharge — provably not
   a consequence of the form bound (neg-1/neg-2); every ceiling diverges;
   escape = a sub-volume rate that is also n-FREE for the SPECIFIC kernel =
   verbatim MO-3 / R-L2b, open of record; (b) the LAYER vs the GENERATOR —
   the interaction-dressed multi-insertion content of E_int stays behind the
   CERT's own §3.3 residual (H^1-propagation through interaction dressings,
   unattempted there, unattempted here); the identity-consumption sites
   consume the DRESSED deviation ([C, U] content), so the per-(n,cell)
   generator-layer enclosures do not yet discharge F3 even at fixed n.
VERDICT: PARTIAL( per-(n,cell), generator + free-dressed single-insertion
   layer, exact; n-uniform + full-layer provably-not-from-the-asset ).
   F3 stays standing; what changes if registered is its QUANTITATIVE
   content at fixed n, plus the FC-1 bookkeeping footnote on F2's support
   language (fc-1c). No wall verdict moves.
```

### 4.3 SITE (iii) — the half-plane's layer-1 (HPL audit c-2: the C-L1/C-L2 deviation of den_n from 1)

The site's OWN structure: den_n = det_{ran C_n}(C_n V_{+-}(0) C_n); layer-1
= the deviation of den_n from 1 through the C-L2 error; consumption is
DETERMINANT-typed: |det(1 + A) - 1| <= e^{||A||_1} - 1 (scalar/2x2 pins
FB8a/FB8b; general finite rank: Simon's inequality over singular values,
named; det_2/Carleman variant consumes ||A||_2, same conclusion below), so
the needed control is TRACE-NORM SMALLNESS: ||A_dev||_1 < log 2 to keep
|den_n - 1| < 1 with margin.

```text
DELIVERED [DERIVED]: the exact typing of c-2's layer-1 gap, plus per-n
   ceilings for the FIRST Duhamel layer of the deviation at the site's own
   compression (§3.1-§3.3 with the pair coefficients; all exact).
VERDICT: FAILS-AT(the smallness threshold) [DERIVED, CAS FB6g]: the asset's
   BEST channel (surface, before adding bulk/leak/energy) already exceeds
   the threshold at the FIRST sealed carrier member:
      sigma_{Q,2}(1/2) = (1303/210) e^{-1/4} / sqrt(pi)
                       > (1303/210)(3/4)(1/2) = 3909/1680 > 1 > log 2
   (exact chain: e < 3 via the displayed factorial majorization => e^{-1/4}
   > 3/4; pi < 4 => pi^{-1/2} > 1/2; log 2 < 1 since e > 5/2 partial-sum) —
   and the ceilings GROW in n; higher Duhamel layers are outside the asset
   (the CERT §3.3 residual). The form route cannot certify den_n away from
   0 at ANY n, a fortiori not n-uniformly: HA10's conditional slice
   nonvanishing (Z_hat >= 1/2 CONDITIONAL(R-L4a/b + C-L2)) does NOT have
   its C-L2 input discharged by the certified asset, and cannot, on any
   route that consumes only class membership. Layer-2 of c-2 (the numerator
   phase) is untouched here (a different object, no claim).
NOT REFUTED: the ceilings are one-sided — nothing here shows den_n is NEAR
   0 or far from 1; den_n != 0 per n stays constitutive (E1 :666); the
   deviation could be small by cancellation the class cannot see. UNDECIDED
   stands.
```

---

## 5. THE SHARED RESIDUE, NAMED EXACTLY

All three sites stop at ONE object, reached from inside the form route:

```text
THE RESIDUE: a trace/HS-grade rate for the SPECIFIC C-L2 kernel that
   (a) beats the compression's energy/surface price (n-uniformly, or with a
   compensating |C|_4-power — which IS beta/R-L2b), and (b) survives the
   two-time/stratum resummation (which IS B-L2*), i.e. sub-volume AND
   n-free for the dressed composite (which IS MO-3). The form bound is
   CLASS-typed; the class contains h_0 (cl-2); no class-typed input can
   supply (a) or (b) — CAS FB9, FB1c. The known blocks are therefore not
   merely unpassed obstacles on this route: they are THE residue, met again
   from a new direction, at every site.
WHAT THE BRIDGE ADDS TO THE RESIDUE HUNT (successor-facing, exact):
   sigma_n(r) — the compressed surface trace — is now a NAMED, per-(n,ell,r)
   COMPUTABLE object (closed form at the executed member, FB6c) realizing
   the sub-volume SHAPE at the generator's surface channel; the missing
   n-free rate is exactly the statement that the DRESSED kernel's
   cancellations beat sigma_n's growth. And FC-1 says any such hunt must
   carry the interior bulk term 2.1_B h_0 S — the layer is NOT purely
   boundary-supported, so a pure surface-decay mechanism cannot be the
   whole discharge even in principle.
```

---

## 6. CONSISTENCY — KNOWN BLOCKS, WITNESSES, NO-STRONGER

```text
vs B-L2* (6e81ae92/2699af25, FAILS-AT of record): NOT re-walked. No two-time
   HS estimate is attempted; site (i)'s negative shows the form route cannot
   substitute for it — consistent, from the other side.
vs beta (R-L2b, SCAD_HS_SCALING_EXPONENT_UNDERIVED): NOT re-walked; no
   exponent derived, none assumed; the residue statement names it.
vs THE RANK x OP CEILING (r-3 D10 = WALL F1/WB6): NOT re-walked; the energy
   channel is a DIFFERENT conversion (rank x energy) and its non-closure is
   displayed by its own arithmetic (neg-3); no claim that F1's proof covers
   or fails to cover it beyond that display.
vs C6 (equal-time FALSE) and CS-3 (kappa_n divergent): respected — no
   equal-time HS object is resurrected; the surface-price divergence at
   site (i) is cited as the same boundary-class story, no new object.
vs D6' (sharp M(t)): respected — nothing smoothed; the three-channel split
   is an identity, not a regularization.
vs THE HPL AUDIT c-1/c-2: respected — den_n never identified with GAP-C;
   layer-1 consumed exactly as c-2 defines it; layer-2 untouched.
WITNESSES: E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED — STANDS (the CERT's
   (o1)-(o3) layer is consumed at audited grade; (o4) remains undischarged;
   nothing here retires or splits anything — registrar's call, and FC-1
   should be recorded alongside per fc-1b). SCAD witnesses, wall witnesses,
   HPL witnesses: all STAND, untouched. witnesses_created_here = none
   (every negative rides an existing named block or an in-artifact display).
V-CLAUSES / GATES / FENCES: untouched; no n >= 2 claim; no threshold claim;
   no alpha-adjacent number; H-R never defaulted; no baseline normed;
   no Phase-1 consumption; kappa_bal uncited.
NOT REFUTED, global: no divergence of any actual consuming quantity is
   exhibited anywhere in this artifact; every ceiling is one-sided; every
   consuming quantity (||A(0)||_2, the four wall quantities, den_n's
   deviation) remains UNDECIDED.
```

---

## 7. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

sympy 1.14.0, fresh venv `fhbvenv` under the session scratchpad; nothing
written to the workspace but this artifact and its seal. 27 checks, 27/27
PASS on the final run; pre-final script repairs disclosed in §8 ch-8.

```python
# FHB-BUILD CAS battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv fhbvenv).
# Every constant symbolic or an exact rational/surd. Nothing numeric evaluated
# (formal-Lambda displays use the record's standing WB7 license).
# Groups:
#   FB1  Clifford grounds + the site-(i) multiplier witness symbol algebra
#   FB2  the CORRECTED S-dressed boundary identity (1-D exact, complex states):
#        [h, 1_B (x) S] = surface + BULK 2*1_B h S — the cert §3.4(ii) exhibit
#        is missing the bulk term; checked both ways (with bulk: 0; without: != 0)
#   FB3  [h_0, S] = 2 h_0 S and S h_0 S^dag = -h_0 (entrywise, noncommutative p)
#   FB4  compression-lemma assembly arithmetic (perfect squares, Cauchy-Schwarz)
#   FB5  Hermite carrier exact data: <p^2> = a + 1/2; ladder structure; the
#        energy trace E_n = 6 n^4 / ell^2 and leak trace E_leak = 6 n^3 / ell^2
#   FB6  spherical moments + the carrier surface trace sigma_{Q,2}(r) closed form
#        at the executed member (n = 2), and the exact threshold chains
#        (e < 3; pi < 4; pi > 1; sigma(1/2) > 1 > log 2; sigma(1/2) < 32 = rank)
#   FB7  site-(i) formal-Lambda HS divergence of the multiplier witness
#   FB8  determinant-deviation arithmetic (the |det(1+A) - 1| <= e^{||A||_1} - 1
#        shape at its scalar/2x2 exact pins)
#   FB9  the Lambda-model non-implication witness (in-class form with divergent
#        compressed trace/HS masses)
import sympy as sp
from sympy import Rational as R
from sympy import I as Im

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== FB1 — Clifford grounds + multiplier witness algebra =====
I2 = sp.eye(2); Z2 = sp.zeros(2, 2)
s1 = sp.Matrix([[0, 1], [1, 0]]); s2 = sp.Matrix([[0, -Im], [Im, 0]]); s3 = sp.Matrix([[1, 0], [0, -1]])
def blk(A, B, C, D):
    return sp.Matrix(sp.BlockMatrix([[A, B], [C, D]]))
g0 = blk(I2, Z2, Z2, -I2)
g = [blk(Z2, s, -s, Z2) for s in (s1, s2, s3)]
g5 = Im * g0 * g[0] * g[1] * g[2]
alpha = [g0 * gj for gj in g]
S = -Im * g0 * g5
k1, k2, k3 = sp.symbols('k1 k2 k3', real=True)
km = sp.Symbol('km', positive=True)   # |k|, positive symbol; constraint below
kv = [k1, k2, k3]
KSUB = {k1**2: km**2 - k2**2 - k3**2}  # the exact constraint k1^2+k2^2+k3^2 = km^2
h0k = sp.zeros(4, 4)
for j in range(3):
    h0k += kv[j] * alpha[j]
Ck = (sp.eye(4) - h0k / km) / 2       # the sea symbol C(k) = (I - alpha.k-hat)/2
def zk(M):
    return sp.expand(M).subs(KSUB).expand() == sp.zeros(*M.shape)
ok("FB1a grounds: {S, alpha_j} = 0; S^2 = I; C(k)^2 = C(k); C(k) Hermitian; tr C(k) = 2"
   " (|k| = km positive symbol; constraint k1^2+k2^2+k3^2 = km^2 substituted exactly)",
   all(sp.simplify(S * a + a * S) == sp.zeros(4, 4) for a in alpha)
   and sp.simplify(S * S - sp.eye(4)) == sp.zeros(4, 4)
   and zk(Ck * Ck - Ck)
   and zk(Ck - Ck.H)
   and sp.expand(sp.trace(Ck) - 2).subs(KSUB) == 0)
ok("FB1b THE COMMUTATION [C, h_0] = 0 AT THE SYMBOL: h_0(k) C(k) = -|k| C(k) = C(k) h_0(k)"
   " — C is the spectral projection 1_(-inf,0)(h_0) (r-2 bytes), so the R.3 sandwich"
   " preserves the form bound VERBATIM",
   zk(h0k * Ck + km * Ck) and zk(Ck * h0k + km * Ck))
ok("FB1c THE MULTIPLIER WITNESS: tr[(h_0 C)(k)^dag (h_0 C)(k)] = 2 |k|^2 != 0 — the"
   " C-sandwich of the in-class member h_0 is the NONZERO Fourier multiplier -|k| C(k):"
   " translation-invariant, non-compact, NOT Hilbert-Schmidt",
   sp.expand(sp.trace((h0k * Ck).H * (h0k * Ck)) - 2 * km**2).subs(KSUB).expand() == 0)

# ===== FB2 — the corrected S-dressed boundary identity, 1-D exact =====
# h = -i s1 d/dx ; vertex 1_{[-r,r]} (x) s3 ; {s1, s3} = 0. CLAIM (corrected):
#   <phi, [h, 1_B s3] psi> = { -i [phi^dag s1 s3 psi](-r) + i [phi^dag s1 s3 psi](+r) }
#                            + 2 * Int_{-r}^{r} phi^dag (-i s1 s3) psi' dx
# i.e. SURFACE + BULK (the bulk = 2 <phi, 1_B h s3 psi>). The cert §3.4(ii)/audit P-1
# display carries the surface term only.
x = sp.Symbol('x', real=True); r = sp.Symbol('r', positive=True)
phi = sp.Matrix([(1 + x) * sp.exp(-x**2), (1 + Im) * x * sp.exp(-x**2)])
psi = sp.Matrix([(2 - Im) * x * sp.exp(-x**2), (1 + Im * x) * sp.exp(-x**2)])
dphi = phi.diff(x); dpsi = psi.diff(x)
hphi = -Im * s1 * dphi; hpsi = -Im * s1 * dpsi
# the form, classically defined on smooth states:
LHS = sp.integrate((hphi.H * s3 * psi)[0, 0], (x, -r, r)) \
    - sp.integrate(((s3 * phi).H * (hpsi))[0, 0], (x, -r, r))
bt = (phi.H * s1 * s3 * psi)[0, 0]
SURF = -Im * bt.subs(x, -r) + Im * bt.subs(x, r)
BULK = 2 * sp.integrate((phi.H * (-Im) * s1 * s3 * dpsi)[0, 0], (x, -r, r))
ok("FB2a CORRECTED IDENTITY EXACT: form = SURFACE + BULK (complex smooth states,"
   " every integral exact)", sp.simplify(sp.expand(LHS - SURF - BULK)) == 0)
ok("FB2b THE EXHIBIT GAP IS REAL: form - SURFACE = BULK != 0 on the displayed states"
   " (the cert §3.4(ii)/audit P-1 surface-only display is incomplete for the"
   " S-DRESSED commutator; the S-less [h_0, 1_B] IS pure surface — FB2c)",
   sp.simplify(sp.expand(LHS - SURF)) != 0)
# S-less check: [h, 1_B] (no s3) = -i s1 (d 1_B) — pure surface, NO bulk:
LHS0 = sp.integrate((hphi.H * psi)[0, 0], (x, -r, r)) \
     - sp.integrate((phi.H * hpsi)[0, 0], (x, -r, r))
bt0 = (phi.H * s1 * psi)[0, 0]
SURF0 = -Im * bt0.subs(x, -r) + Im * bt0.subs(x, r)
ok("FB2c S-LESS CONTROL: <phi,[h, 1_B]psi> = surface EXACTLY (no bulk) — E1's"
   " ||[h_0, 1_B]|| = +infinity locus display is about THIS object and is untouched",
   sp.simplify(sp.expand(LHS0 - SURF0)) == 0)
# FB2d — the gap is real at the QUADRATIC grade too (phi = psi):
qLHS = sp.integrate(((-Im * s1 * phi.diff(x)).H * s3 * phi)[0, 0], (x, -r, r)) \
     - sp.integrate(((s3 * phi).H * (-Im * s1 * phi.diff(x)))[0, 0], (x, -r, r))
qbt = (phi.H * s1 * s3 * phi)[0, 0]
qSURF = -Im * qbt.subs(x, -r) + Im * qbt.subs(x, r)
qBULK = 2 * sp.integrate((phi.H * (-Im) * s1 * s3 * phi.diff(x))[0, 0], (x, -r, r))
ok("FB2d QUADRATIC GRADE: q(psi) - surface = BULK != 0 on an explicit state — the"
   " surface-only display fails as a quadratic form as well; q(psi) = i oint ... dS"
   " is NOT the S-dressed form's value (it omits 2<psi, 1_B h_0 S psi>)",
   sp.simplify(sp.expand(qLHS - qSURF - qBULK)) == 0
   and sp.simplify(sp.expand(qBULK)) != 0)

# ===== FB3 — [h_0, S] = 2 h_0 S and S h_0 S^dag = -h_0 (noncommutative p) =====
p = [sp.Symbol(f'p{j}', commutative=False) for j in range(3)]
flip3a = True; flip3b = True
for a_ in range(4):
    for b_ in range(4):
        h0S_ab = sum((alpha[j] * S)[a_, b_] * p[j] for j in range(3))
        Sh0_ab = sum((S * alpha[j])[a_, b_] * p[j] for j in range(3))
        if sp.expand(h0S_ab - Sh0_ab - 2 * h0S_ab) != 0:
            flip3a = False
        Sh0Sd_ab = sum((S * alpha[j] * S.H)[a_, b_] * p[j] for j in range(3))
        h0_ab = sum(alpha[j][a_, b_] * p[j] for j in range(3))
        if sp.expand(Sh0Sd_ab + h0_ab) != 0:
            flip3b = False
ok("FB3 [h_0, S] = 2 h_0 S and S h_0 S^dag = -h_0 (entrywise, noncommutative p_j)"
   " — the bulk term's origin, and the SC_nS^dag sector flip used in the trace"
   " assembly", flip3a and flip3b)

# ===== FB4 — assembly arithmetic =====
a_, b_ = sp.symbols('a b', real=True)
ok("FB4a (a+b)^2 <= 2a^2 + 2b^2 (gap = (a-b)^2 EXACT)",
   sp.expand(2 * a_**2 + 2 * b_**2 - (a_ + b_)**2 - (a_ - b_)**2) == 0)
aa = sp.symbols('a1:4', real=True)
ok("FB4b (a1+a2+a3)^2 <= 3(a1^2+a2^2+a3^2) (SOS witness; general r NAMED Cauchy-Schwarz)",
   sp.expand(3 * sum(v**2 for v in aa) - sum(aa)**2
             - ((aa[0]-aa[1])**2 + (aa[1]-aa[2])**2 + (aa[2]-aa[0])**2) / 1) == 0)
rr_, E_ = sp.symbols('r_n E_n', positive=True)
ok("FB4c Sum_{ij}(a_i + a_j)^2 = 2 r Sum a^2 + 2 (Sum a)^2 <= 4 r Sum a^2 (display"
   " at r = 3, exact; the HS assembly 12 r E and the trace assembly 2 sqrt(3) sqrt(rE))",
   sp.expand(sum((ai + aj)**2 for ai in aa for aj in aa)
             - (2 * 3 * sum(v**2 for v in aa) + 2 * sum(aa)**2)) == 0)
ok("FB4d carrier constants: 2*sqrt(3)*sqrt(4 n^3 * 6 n^4/ell^2) = 12 sqrt(2) n^{7/2}/ell"
   " and sqrt(4 n^3) * that = 24 sqrt(2) n^5 / ell EXACT",
   sp.simplify(2 * sp.sqrt(3) * sp.sqrt(4 * rr_**3 * 6 * rr_**4 / E_**2)
               - 12 * sp.sqrt(2) * rr_**R(7, 2) / E_) == 0
   and sp.simplify(sp.sqrt(4 * rr_**3) * 12 * sp.sqrt(2) * rr_**R(7, 2) / E_
                   - 24 * sp.sqrt(2) * rr_**5 / E_) == 0)

# ===== FB5 — Hermite carrier exact data =====
ell = sp.Symbol('ell', positive=True)
def phi_a(aidx, u):
    return sp.hermite(aidx, u) * sp.exp(-u**2 / 2) / sp.sqrt(2**aidx * sp.factorial(aidx) * sp.sqrt(sp.pi))
u = sp.Symbol('u', real=True)
p2_ok = all(sp.simplify(sp.integrate(sp.diff(phi_a(A, u), u)**2, (u, -sp.oo, sp.oo)) - (A + R(1, 2))) == 0
            for A in range(4))
ok("FB5a <phi_a, p^2 phi_a> = a + 1/2 EXACT (a = 0..3; ell scaling 1/ell^2 by the"
   " exact substitution u = x/ell, named)", p2_ok)
lad_ok = True
for A in range(4):
    for B in range(6):
        val = sp.simplify(sp.integrate(phi_a(B, u) * (-Im) * sp.diff(phi_a(A, u), u), (u, -sp.oo, sp.oo)))
        expect = 0
        if B == A + 1:
            expect = Im * sp.sqrt(R(A + 1, 2))
        elif B == A - 1:
            expect = -Im * sp.sqrt(R(A, 2))
        if sp.simplify(val - expect) != 0:
            lad_ok = False
ok("FB5b LADDER: <phi_b, p phi_a> = 0 unless b = a±1; |<phi_{a+1}, p phi_a>|^2 ="
   " (a+1)/2 EXACT — h_0 moves the Hermite carrier by ONE shell only; the Galerkin"
   " leak is TOP-SHELL only", lad_ok)
n_, A_, B_, C_ = sp.symbols('n a b c', positive=True, integer=True)
S3sum = sp.summation(sp.summation(sp.summation(A_ + B_ + C_ + R(3, 2),
        (A_, 0, n_ - 1)), (B_, 0, n_ - 1)), (C_, 0, n_ - 1))
ok("FB5c E_n = 4/ell^2 * Sum_{a,b,c<n}(a+b+c+3/2) = 6 n^4 / ell^2 EXACT (the sealed"
   " Hermite carrier's h_0^2 energy trace; spinor factor 4; rank r_n = 4 n^3)",
   sp.simplify(sp.factor(S3sum) - R(3, 2) * n_**4) == 0)
ok("FB5d E_leak = tr(Q h_0 (1-Q) h_0 Q) = 4 * 3 * n^2 * (n/2) / ell^2 = 6 n^3 / ell^2"
   " EXACT (top shell a = n-1 per axis, count n^2, ladder-up mass n/(2 ell^2), 3 axes,"
   " spinor trace tr(alpha_i alpha_j) = 4 delta_ij)",
   sp.simplify(4 * 3 * n_**2 * (n_ / 2) - 6 * n_**3) == 0
   and all(sp.simplify(sp.trace(alpha[i_] * alpha[j_]) - (4 if i_ == j_ else 0)) == 0
           for i_ in range(3) for j_ in range(3)))

# ===== FB6 — spherical moments; sigma_{Q,2}(r); exact threshold chains =====
th_, ph_ = sp.symbols('theta phi', real=True)
rv = sp.Symbol('r', positive=True)
nx = sp.sin(th_) * sp.cos(ph_); ny = sp.sin(th_) * sp.sin(ph_); nz = sp.cos(th_)
dS = sp.sin(th_)
def sphavg(expr):
    return sp.simplify(sp.integrate(sp.integrate(expr * dS, (th_, 0, sp.pi)), (ph_, 0, 2 * sp.pi)) / (4 * sp.pi))
m2 = sphavg(nz**2); m22 = sphavg(nx**2 * ny**2); m222 = sphavg(nx**2 * ny**2 * nz**2)
ok("FB6a spherical moments: <x_j^2> = r^2/3, <x_i^2 x_j^2> = r^4/15, <x1^2x2^2x3^2>"
   " = r^6/105 EXACT", m2 == R(1, 3) and m22 == R(1, 15) and m222 == R(1, 105))
# K_2(x,x) = pi^{-3/2} e^{-|x|^2} Prod_j (1 + 2 x_j^2)  (ell = 1); check from phi_0, phi_1:
k2_1d = sp.simplify(phi_a(0, u)**2 + phi_a(1, u)**2 - sp.exp(-u**2) * (1 + 2 * u**2) / sp.sqrt(sp.pi))
ok("FB6b 1-D Christoffel diagonal: phi_0^2 + phi_1^2 = pi^{-1/2} e^{-u^2}(1 + 2u^2)"
   " EXACT", k2_1d == 0)
prodavg = sphavg(sp.expand((1 + 2 * rv**2 * nx**2) * (1 + 2 * rv**2 * ny**2) * (1 + 2 * rv**2 * nz**2)))
sigma_closed = 16 * rv**2 * sp.exp(-rv**2) / sp.sqrt(sp.pi) * (1 + 2 * rv**2 + R(4, 5) * rv**4 + R(8, 105) * rv**6)
ok("FB6c sigma_{Q,2}(r) = 4 * 4 pi r^2 * pi^{-3/2} e^{-r^2} * <Prod(1+2x_j^2)> ="
   " 16 r^2 e^{-r^2} pi^{-1/2} (1 + 2r^2 + (4/5) r^4 + (8/105) r^6) EXACT (ell = 1;"
   " the executed member's carrier surface trace, closed form)",
   sp.simplify(4 * 4 * sp.pi * rv**2 * sp.pi**R(-3, 2) * sp.exp(-rv**2) * prodavg - sigma_closed) == 0)
sig_half = sp.simplify(sigma_closed.subs(rv, R(1, 2)))
ok("FB6d sigma_{Q,2}(1/2) = (1303/210) e^{-1/4} / sqrt(pi) EXACT",
   sp.simplify(sig_half - R(1303, 210) * sp.exp(-R(1, 4)) / sp.sqrt(sp.pi)) == 0)
# exact chains, no float as ground:
efac_ok = all(R(1, sp.factorial(kk)) <= R(1, 2**(kk - 1)) for kk in range(1, 11))
geo = sp.summation(R(1, 2)**(sp.Symbol('k', positive=True, integer=True) - 1),
                   (sp.Symbol('k', positive=True, integer=True), 1, sp.oo))
ok("FB6e e < 3 EXACT CHAIN: 1/k! <= 2^{-(k-1)} (k = 1..10 displayed; k >= 1 by"
   " induction, named) and Sum 2^{-(k-1)} = 2, so e = 1 + Sum 1/k! < 1 + 2 = 3;"
   " hence e < 256/81 = (4/3)^4, i.e. e^{-1/4} > 3/4", efac_ok and geo == 2 and R(243, 81) < R(256, 81))
xint = sp.integrate(1 - 1 / (1 + u**2), (u, 0, 1))
ok("FB6f pi < 4 EXACT: Int_0^1 [1 - 1/(1+x^2)] dx = 1 - pi/4 = Int_0^1 x^2/(1+x^2) dx"
   " with POSITIVE integrand => pi < 4 => 1/sqrt(pi) > 1/2; and pi > 1 (arctan 1 >"
   " 1 - 1/3 = 2/3 alternating-series lower bound => pi > 8/3 > 1) => 1/sqrt(pi) < 1",
   sp.simplify(xint - (1 - sp.pi / 4)) == 0 and R(8, 3) > 1)
ok("FB6g THE THRESHOLD CHAIN AT THE EXECUTED MEMBER: sigma_{Q,2}(1/2) >"
   " (1303/210)(3/4)(1/2) = 3909/1680 > 1 > log 2 (log 2 < 1 since 2 < e: partial"
   " sum 1+1+1/2 = 5/2 > 2) — the surface-channel ceiling EXCEEDS the determinant"
   " smallness threshold already at n = 2; AND sigma_{Q,2}(1/2) < 1303/210 < 32 ="
   " r_2 — an exact factor >= 5 below the rank budget (the sub-volume shape)",
   R(3909, 1680) > 1 and R(5, 2) > 2 and R(1303, 210) < 32)

# ===== FB7 — site-(i) formal-Lambda HS divergence of the multiplier witness =====
kk_, Lam = sp.symbols('k Lambda', positive=True)
hs_int = sp.integrate(2 * kk_**2 * 4 * sp.pi * kk_**2, (kk_, 0, Lam))
ok("FB7 the multiplier witness HS mass: Int_0^Lambda tr|(-|k|C)(k)|^2 4 pi k^2 dk ="
   " (8 pi/5) Lambda^5 -> +infinity (Lambda FORMAL, the record's WB7 license):"
   " C h_0 C is NOT Hilbert-Schmidt — no HS conclusion at site (i) follows from"
   " class membership alone",
   sp.simplify(hs_int - R(8, 5) * sp.pi * Lam**5) == 0 and sp.limit(hs_int, Lam, sp.oo) == sp.oo)

# ===== FB8 — determinant-deviation arithmetic =====
s_, t_ = sp.symbols('s t', positive=True)
ok("FB8a scalar pin: |(1+a) - 1| = |a| <= e^{|a|} - 1 (e^s - 1 - s = Sum_{k>=2} s^k/k!"
   " >= 0, leading coefficient 1/2)",
   sp.series(sp.exp(s_) - 1 - s_, s_, 0, 4).removeO().coeff(s_, 2) == R(1, 2))
ok("FB8b 2x2 pin: |(1+a)(1+b) - 1| <= |a| + |b| + |a||b| = (1+|a|)(1+|b|) - 1 <="
   " e^{|a|+|b|} - 1 (triangle + FB8a; general finite rank NAMED: Simon's"
   " Pi(1+s_i) - 1 <= e^{||A||_1} - 1 over singular values)",
   sp.expand((1 + s_) * (1 + t_) - 1 - (s_ + t_ + s_ * t_)) == 0)

# ===== FB9 — the Lambda-model non-implication witness =====
N_ = sp.Symbol('N', positive=True, integer=True)
kk2 = sp.Symbol('k', positive=True, integer=True)
trsum = sp.summation(kk2, (kk2, 1, N_))
hssum = sp.summation(kk2**2, (kk2, 1, N_))
ok("FB9 NON-IMPLICATION WITNESS: h = diag(1..N), T = h satisfies the certified"
   " bilinear class bound with constant 1 <= sqrt(3) (Cauchy-Schwarz, displayed);"
   " tr(Pi_N T Pi_N) = N(N+1)/2 -> inf, ||Pi_N T Pi_N||_2^2 = N(N+1)(2N+1)/6 -> inf:"
   " 'certified form bound => n-uniform compressed trace/HS control' is FALSE as an"
   " inference — any n-uniform discharge must consume structure beyond the form bound",
   sp.simplify(trsum - N_ * (N_ + 1) / 2) == 0
   and sp.simplify(hssum - N_ * (N_ + 1) * (2 * N_ + 1) / 6) == 0
   and sp.limit(trsum, N_, sp.oo) == sp.oo and sp.limit(hssum, N_, sp.oo) == sp.oo)

print("FHB-BATTERY-DONE")
```

Output, verbatim (27/27 PASS, final run):

```text
FB1a grounds: {S, alpha_j} = 0; S^2 = I; C(k)^2 = C(k); C(k) Hermitian; tr C(k) = 2 (|k| = km positive symbol; constraint k1^2+k2^2+k3^2 = km^2 substituted exactly): PASS
FB1b THE COMMUTATION [C, h_0] = 0 AT THE SYMBOL: h_0(k) C(k) = -|k| C(k) = C(k) h_0(k) — C is the spectral projection 1_(-inf,0)(h_0) (r-2 bytes), so the R.3 sandwich preserves the form bound VERBATIM: PASS
FB1c THE MULTIPLIER WITNESS: tr[(h_0 C)(k)^dag (h_0 C)(k)] = 2 |k|^2 != 0 — the C-sandwich of the in-class member h_0 is the NONZERO Fourier multiplier -|k| C(k): translation-invariant, non-compact, NOT Hilbert-Schmidt: PASS
FB2a CORRECTED IDENTITY EXACT: form = SURFACE + BULK (complex smooth states, every integral exact): PASS
FB2b THE EXHIBIT GAP IS REAL: form - SURFACE = BULK != 0 on the displayed states (the cert §3.4(ii)/audit P-1 surface-only display is incomplete for the S-DRESSED commutator; the S-less [h_0, 1_B] IS pure surface — FB2c): PASS
FB2c S-LESS CONTROL: <phi,[h, 1_B]psi> = surface EXACTLY (no bulk) — E1's ||[h_0, 1_B]|| = +infinity locus display is about THIS object and is untouched: PASS
FB2d QUADRATIC GRADE: q(psi) - surface = BULK != 0 on an explicit state — the surface-only display fails as a quadratic form as well; q(psi) = i oint ... dS is NOT the S-dressed form's value (it omits 2<psi, 1_B h_0 S psi>): PASS
FB3 [h_0, S] = 2 h_0 S and S h_0 S^dag = -h_0 (entrywise, noncommutative p_j) — the bulk term's origin, and the SC_nS^dag sector flip used in the trace assembly: PASS
FB4a (a+b)^2 <= 2a^2 + 2b^2 (gap = (a-b)^2 EXACT): PASS
FB4b (a1+a2+a3)^2 <= 3(a1^2+a2^2+a3^2) (SOS witness; general r NAMED Cauchy-Schwarz): PASS
FB4c Sum_{ij}(a_i + a_j)^2 = 2 r Sum a^2 + 2 (Sum a)^2 <= 4 r Sum a^2 (display at r = 3, exact; the HS assembly 12 r E and the trace assembly 2 sqrt(3) sqrt(rE)): PASS
FB4d carrier constants: 2*sqrt(3)*sqrt(4 n^3 * 6 n^4/ell^2) = 12 sqrt(2) n^{7/2}/ell and sqrt(4 n^3) * that = 24 sqrt(2) n^5 / ell EXACT: PASS
FB5a <phi_a, p^2 phi_a> = a + 1/2 EXACT (a = 0..3; ell scaling 1/ell^2 by the exact substitution u = x/ell, named): PASS
FB5b LADDER: <phi_b, p phi_a> = 0 unless b = a±1; |<phi_{a+1}, p phi_a>|^2 = (a+1)/2 EXACT — h_0 moves the Hermite carrier by ONE shell only; the Galerkin leak is TOP-SHELL only: PASS
FB5c E_n = 4/ell^2 * Sum_{a,b,c<n}(a+b+c+3/2) = 6 n^4 / ell^2 EXACT (the sealed Hermite carrier's h_0^2 energy trace; spinor factor 4; rank r_n = 4 n^3): PASS
FB5d E_leak = tr(Q h_0 (1-Q) h_0 Q) = 4 * 3 * n^2 * (n/2) / ell^2 = 6 n^3 / ell^2 EXACT (top shell a = n-1 per axis, count n^2, ladder-up mass n/(2 ell^2), 3 axes, spinor trace tr(alpha_i alpha_j) = 4 delta_ij): PASS
FB6a spherical moments: <x_j^2> = r^2/3, <x_i^2 x_j^2> = r^4/15, <x1^2x2^2x3^2> = r^6/105 EXACT: PASS
FB6b 1-D Christoffel diagonal: phi_0^2 + phi_1^2 = pi^{-1/2} e^{-u^2}(1 + 2u^2) EXACT: PASS
FB6c sigma_{Q,2}(r) = 4 * 4 pi r^2 * pi^{-3/2} e^{-r^2} * <Prod(1+2x_j^2)> = 16 r^2 e^{-r^2} pi^{-1/2} (1 + 2r^2 + (4/5) r^4 + (8/105) r^6) EXACT (ell = 1; the executed member's carrier surface trace, closed form): PASS
FB6d sigma_{Q,2}(1/2) = (1303/210) e^{-1/4} / sqrt(pi) EXACT: PASS
FB6e e < 3 EXACT CHAIN: 1/k! <= 2^{-(k-1)} (k = 1..10 displayed; k >= 1 by induction, named) and Sum 2^{-(k-1)} = 2, so e = 1 + Sum 1/k! < 1 + 2 = 3; hence e < 256/81 = (4/3)^4, i.e. e^{-1/4} > 3/4: PASS
FB6f pi < 4 EXACT: Int_0^1 [1 - 1/(1+x^2)] dx = 1 - pi/4 = Int_0^1 x^2/(1+x^2) dx with POSITIVE integrand => pi < 4 => 1/sqrt(pi) > 1/2; and pi > 1 (arctan 1 > 1 - 1/3 = 2/3 alternating-series lower bound => pi > 8/3 > 1) => 1/sqrt(pi) < 1: PASS
FB6g THE THRESHOLD CHAIN AT THE EXECUTED MEMBER: sigma_{Q,2}(1/2) > (1303/210)(3/4)(1/2) = 3909/1680 > 1 > log 2 (log 2 < 1 since 2 < e: partial sum 1+1+1/2 = 5/2 > 2) — the surface-channel ceiling EXCEEDS the determinant smallness threshold already at n = 2; AND sigma_{Q,2}(1/2) < 1303/210 < 32 = r_2 — an exact factor >= 5 below the rank budget (the sub-volume shape): PASS
FB7 the multiplier witness HS mass: Int_0^Lambda tr|(-|k|C)(k)|^2 4 pi k^2 dk = (8 pi/5) Lambda^5 -> +infinity (Lambda FORMAL, the record's WB7 license): C h_0 C is NOT Hilbert-Schmidt — no HS conclusion at site (i) follows from class membership alone: PASS
FB8a scalar pin: |(1+a) - 1| = |a| <= e^{|a|} - 1 (e^s - 1 - s = Sum_{k>=2} s^k/k! >= 0, leading coefficient 1/2): PASS
FB8b 2x2 pin: |(1+a)(1+b) - 1| <= |a| + |b| + |a||b| = (1+|a|)(1+|b|) - 1 <= e^{|a|+|b|} - 1 (triangle + FB8a; general finite rank NAMED: Simon's Pi(1+s_i) - 1 <= e^{||A||_1} - 1 over singular values): PASS
FB9 NON-IMPLICATION WITNESS: h = diag(1..N), T = h satisfies the certified bilinear class bound with constant 1 <= sqrt(3) (Cauchy-Schwarz, displayed); tr(Pi_N T Pi_N) = N(N+1)/2 -> inf, ||Pi_N T Pi_N||_2^2 = N(N+1)(2N+1)/6 -> inf: 'certified form bound => n-uniform compressed trace/HS control' is FALSE as an inference — any n-uniform discharge must consume structure beyond the form bound: PASS
FHB-BATTERY-DONE
```

---

## 8. CHOICE LEDGER

```text
ch-1 SITE STRUCTURES read at bytes: (i) the continuum C-sandwich (E1
     :811-814, audit COR-4 pin; C's identity at r-2 :144-148); (ii) the
     carrier C_n <= Q_(n,ell) sandwiches (WALL §3.1/KD2 lineage; PA A1);
     (iii) the ran-C_n determinant (HPL P-b; audit c-2's layer-1). Tag:
     PROVABLE (each cited at grade).
ch-2 HYPOTHESIS CLASS H(sqrt3) fixed as the audited OPERATIVE definition
     (COR-5: the derivative-placed bilinear display). Tag: PROVABLE.
ch-3 THREE-CHANNEL SPLIT (surface/bulk/leak) at the sealed sharp vertex:
     the split is an exact identity FORCED by FC-1's corrected display;
     carrying all three rather than the crude energy channel alone is the
     builder's display choice. Tag: PROVABLE (content), YOURS (display).
ch-4 WITNESS SELECTION: site (i) uses the IN-SPACE, site-faithful witness
     (h_0 in class; C h_0 C a multiplier); the finite-rank sites use the
     ABSTRACT Lambda-model (FB9) for the non-implication plus the exact
     divergent ceilings for site-faithful growth — NO site-faithful LOWER
     bound on any actual compressed object is claimed (none is needed for
     the negative, and claiming one would overreach). Tag: YOURS, grounds
     displayed.
ch-5 NOT ATTEMPTED, named: the H^1-propagation/multi-insertion layer (the
     CERT's own ch-7 residual, carried unchanged); layer-2 of c-2 (the
     numerator phase); any repair of the CERT's §3.4(i) witness argument
     under FC-1 (registrar's splice decision, not mine). Tag: YOURS.
ch-6 FC-1 REPORTED, NOT REPAIRED IN PLACE: no existing file edited; the
     correction is displayed here with its CAS pins and exact reach
     (fc-1a..fc-1d). Tag: PROVABLE (mechanics), YOURS (the classing as
     correction-grade, not verdict-bearing — grounds: the certified bound
     and the of-record exclusion both stand on independent ground).
ch-7 SWEEP SCOPE: both roots minus fenced classes, cutoff declared at
     22:33:52 -0500; post-seed same-day artifacts classed at verdict grade
     (§1); the HPL c-3 lesson applied (newest-file check run, classing
     displayed, post-cutoff files declared unswept). Tag: PROVABLE.
ch-8 SCRIPT REPAIRS DISCLOSED (pre-final runs): (r-1) conjugate-of-surd
     handling — |k| carried as a positive symbol with the polynomial
     constraint KSUB (sympy cannot push conjugate through sqrt of a sum;
     mathematical content unchanged); (r-2) FB2 test-state widths unified
     to one Gaussian after a Risch-integrator failure; (r-3) the FIRST
     FB2b state pair made the bulk term vanish IDENTICALLY BY PARITY (odd
     integrand) — replaced by parity-mixed states. r-3 is itself evidence:
     the same parity degeneracy is how the radial/real checks of B7 (CERT)
     and I4a' (audit) could pass while the surface-only display is
     incomplete (fc-1d). No mathematical check failed on the final run;
     27/27 single pass. Tag: YOURS (disclosure), PROVABLE (repairs visible
     in the verbatim script).
```

---

## 9. TOY_SEPARATION

```text
T-1 FB9 (the Lambda-model) is an ABSTRACT witness: it establishes the
    non-implication "class bound => n-uniform compressed trace/HS" as a
    statement-shape fact and carries NO record authority about the actual
    C-L2 kernel. Every record-facing negative in §4 rides either the
    in-space witness (site (i): h_0 in class, C h_0 C a multiplier — the
    site's own Hilbert space and its own sandwich) or exact divergent
    ceilings for the sealed carriers (sites (ii)/(iii)).
T-2 FB2 (1-D two-band schema) pins the corrected identity's ALGEBRA; the
    3-D corrected display in §2.2 is derived in text from the operator
    algebra ([h_0,1_B]S + 1_B[h_0,S], FB3, distributional gradient of 1_B
    — each step named and dimension-blind), not from the 1-D toy alone.
T-3 The FB2/FB2d explicit states are witnesses of NON-vanishing (the gap
    is nonzero somewhere), the correct logical grade for refuting a
    claimed identity; they are not promoted to any quantifier.
T-4 Nothing numeric evaluated: every constant exact symbolic (2 sqrt3,
    12 sqrt2, 24 sqrt2, 6n^4/ell^2, 6n^3/ell^2, pi/2, (1303/210)e^{-1/4}
    /sqrt(pi), 3909/1680, log 2 bounds via displayed exact chains);
    Lambda formal (WB7 license); no measured constant; no value frozen.
```

---

## 10. SWEEP RECORD (bounded negatives)

Roots: `/Users/bgm/MB Work/alpha-program-archive` and
`/Users/bgm/MB Work/alpha_supervision`, minus fenced classes
(register|road_|ledger|lens|plan|tracker|THE_HANDOFF — filename matches
listed by sweep only, never opened); `.proof_deps/` vendored trees excluded
as non-corpus. Cutoff: §1.

```text
s-neg-1 NO PRIOR FORM-TO-HS BRIDGE: queries "form.to.HS", "compression
   lemma", "R*BR" factorization shapes, "surface densit", "energy trace",
   "6n^4" — the only hit is the CERT itself (its §3.5 distance statement).
   The compression calculus of §3 is NEW HERE, subject to check. | TYPE-S |
s-neg-2 "sigma_n" hits resolve to the J-II functor symbol sigma_n^fin in
   the TASK5 lane artifacts — a DIFFERENT object (checked at context
   grade); no collision, and my sigma_n(r) is defined fresh in §3.3.
   | TYPE-S |
s-neg-3 POST-SEED SAME-DAY ARTIFACTS (PIN_DETERMINATION, AMPLITUDE_JUNCTION
   + audit, OB2_EMPTINESS + audit, EARN_THE_TRACE + audit): classed at
   verdict/headline grade — none carries C-L2, form-bound, or bridge
   content (headers + greps). Files sealed after 22:33:52 -0500: UNSWEPT,
   declared. | TYPE-S |
s-neg-4 "Q-..." tokens inside consumed artifacts: EXPECTED-UNLOCATABLE,
   noted, not chased. | TYPE-C |
```

---

## FLAG BLOCK — STAGE8_FORM_TO_HS_BRIDGE_S9AD_V001

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false

FHB_SITE_I_R3 = FAILS-AT( compression type: the site's sandwich C =
  1_(-inf,0)(h_0) is infinite-rank and h_0-commuting — it preserves the
  certified form bound VERBATIM (form-grade BRIDGED exact, constant
  2 sqrt(3) unchanged, FB1b) and provably creates no HS decay: h_0 is in
  the certified class and C h_0 C = -|k|C(k) is a nonzero multiplier, not
  HS (FB1c/FB7); the surface channel's price tr(C delta_dB C) is the
  divergent continuum boundary class. NO HS conclusion at this site follows
  from the certified asset, ever; discharge requires kernel input beyond
  class membership = the standing (d-2) B-L2* + (d-3) beta blocks,
  confirmed binding from a new direction. )
FHB_SITE_II_WALL_F3 = PARTIAL( delivered: first trace/HS-grade enclosures
  of the modulo-layer's GENERATOR at the wall's own compressed sites, per
  (n, cell), exact and carrier-explicit — |tr|, ||.||_2 <= 12 sqrt(2)
  n^{7/2}/ell, ||.||_1 <= 24 sqrt(2) n^5/ell, with E_n = 6 n^4/ell^2 and
  E_leak = 6 n^3/ell^2 DERIVED closed-form (FB5), dressing-uniform (cl-1),
  and the three-channel split with the sub-volume surface law ||.||_1 <=
  sigma_n(r), sigma_{Q,2}(r) exact (FB6); NOT delivered: the n-uniform
  discharge (provably not a consequence of the form bound — FB9 + divergent
  ceilings) and the full interaction-dressed layer (the CERT's H^1
  residual stands). F3 stands; its fixed-n content is tightened. )
FHB_SITE_III_HPL_LAYER1 = FAILS-AT( the smallness threshold: determinant-
  deviation consumption needs ||A_dev||_1 < log 2; the asset's best channel
  exceeds it at the FIRST sealed member — sigma_{Q,2}(1/2) = (1303/210)
  e^{-1/4}/sqrt(pi) > 3909/1680 > 1 > log 2, exact chain FB6g — and grows;
  higher Duhamel layers outside the asset. The form route cannot certify
  den_n away from 0 at any n; c-2's layer-1 stays undischarged, its gap
  now quantified. Layer-2 untouched. )
FHB_SHARED_RESIDUE = NAMED( an n-free (or |C|_4-power-carrying) trace/HS
  rate for the SPECIFIC C-L2 kernel beyond form-class membership =
  MO-3 / R-L2b(beta) / B-L2* exactly — the known blocks met again from
  inside the form route at every site; the class-typed asset can never
  supply it (cl-2 + FB9). New named object for the hunt: sigma_n(r), the
  compressed surface trace, computable per (n, ell, r), closed form at
  n = 2. )
FC1_EXHIBIT_CORRECTION = REPORTED( [h_0, 1_B (x) S] = i(alpha.n-hat (x) S)
  delta_dB + 2 . 1_B h_0 S — the CERT §3.4(ii) and audit P-1 surface-only
  displays are incomplete at bytes (bulk term nonzero at bilinear AND
  quadratic grade, FB2b/FB2d; origin [h_0,S] = 2 h_0 S, FB3); the S-LESS
  [h_0, 1_B] is pure surface (FB2c), so E1's of-record exclusion locus
  and the certified bound are UNTOUCHED; consumers of the surface-pairing
  reading must carry the bulk term; wall F2 support language gets a
  bookkeeping footnote, no verdict moves; detection surface: parity-
  degenerate test states (fc-1d, ch-8 r-3). Registrar's splice. )
NOT_REFUTED = ( every ceiling one-sided; no divergence of any actual
  compressed error-layer object exhibited; ||A(0)||_2, the four wall
  quantities, and den_n's deviation ALL UNDECIDED. )
witnesses_retired_here = none ; witnesses_created_here = none
V_clauses_touched = none ; gates_moved = none ; fences_moved = none
new_numbers_frozen = none (all constants exact symbolic)
seals_verified = 9/9 at path vs sidecars + tasked prefixes (e6838ffc,
  180114a7, 80db260f, f5b887dc, f6f45e6d, 46846730, 789338ad, 3b5e95b6,
  bebc0f08)
sweep = both roots minus fenced classes; cutoff 2026-08-14 22:33:52 -0500;
  post-seed artifacts classed; post-cutoff declared unswept
CAS = sympy 1.14.0, fresh venv fhbvenv, 27/27 PASS final run, script +
  output verbatim §7, pre-final script repairs disclosed ch-8
output = ONE artifact + seal sidecar at the commission-distinct path,
  probed ABSENT at session start and re-probed immediately before write;
  no git, no register, no existing file edited
ALL_RESULTS = CLAIMED until checked.
FHB_FORM_TO_HS_BRIDGE_RESULT = SEALED.
```
