# STAGE 8 — BLIND ADVERSARIAL CHECK OF THE B-L2* ATTEMPT (BL2STAR-CHECK)

## BLIND VERIFIER — CODENAME BL2STAR-CHECK — CROSS-LINEAGE — [SEALED]

Date: 2026-08-13
Under test: STAGE8_BL2STAR_ATTEMPT_V001.md, sha256
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027 — recomputed at
path THIS session, MATCH to the tasked digest.
Default posture: REFUTE. Every load-bearing claim re-derived independently or
traced to sealed bytes at path. All five tasked attacks run.

Gates: `alpha_computed = false` ; `kappa_record_computed = false` ;
`proof_authorized = false`. Fences held: exact symbolic analysis only; CAS used
for identity verification only, own script reproduced in full in §6, nothing
numeric evaluated; M(t) not softened; equal-time not resurrected; no
register/tracker/plan/road/ledger/lens file read; no scale, imported GR, or
faithfulness authority. Output name probed before write: ABSENT.

---

## 0. VERDICT IN ONE LINE

**NET_VERDICT = CONFIRMED(FAILS-AT the stratum-resummation step at the sealed
saturation clause E1 :346-351, at exactly the claimed conditional strength).
The stratum object is the sealed one; every one of the attempt's seventeen
identities re-derives under an independent construction (and in a second gamma
representation); the collapse identity is in fact UNCONDITIONAL — stronger than
the attempt checked; the threshold bookkeeping is exact on both sides; the
obstruction survived a directed hunt for missed cancellations (six candidate
kill channels checked, all closed); the scope statements are exact; provenance
clean; no injection.**

---

## 1. SEALS VERIFIED AT PATH (all recomputed THIS session)

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`.

```text
6e81ae92b32fa34691396102ef79f1fdfcb8c15101a44d9206aeeded599a5027  STAGE8_BL2STAR_ATTEMPT_V001.md                                       MATCH (tasked)
468467303a109dc825b015107897dacc107800fc981030377c3f006b384cccb5  STAGE8_T7_E1_SUCCESSOR_PROGRAM_SPEC_V002.md                          MATCH (E1; sidecar + tasked prefix 46846730)
789338adb7d3d36da453113e98f371a4f92543cf2652b047f8481c407bed5bc3  STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md MATCH (PA; 789338ad)
18af3dee8e3e5b17fe624d1b0547f4713359473a373df1562ff584647244ccf1  STAGE8_TWO_TIME_REANALYSIS_V001.md                                   MATCH (TWO-TIME; 18af3dee)
6a2aa0fd18b9a0ea4f370dfbea278563ba02552e64b48ac0a7d91fbd7f90bc9e  STAGE8_TWO_TIME_CHECK_V001.md                                        MATCH (TT-CHECK; 6a2aa0fd)
0a10c0305d4cde8b226d844ed0fb9289f393b670e02b144bf762dd49c853c9c4  STAGE8_ASSEMBLY_QUANTIFIER_V001.md                                   MATCH (AQ; 0a10c030)
331035d393695519eaa061a343abb67129a8ea48fcc47a765daaef1883b0aa22  STAGE8_ASSEMBLY_QUANTIFIER_CHECK_V001.md                             MATCH (AQ-CHECK; 331035d3)
3d8aea1a4779b0bfe7a472dca1fab0642750e8e010e339b9e3b100197b75a18c  scripts/derive_stage8_t7_primitive_operator_response_v001.py         MATCH (PA-pinned executor; 3d8aea1a)
```

E1 bytes [57212,58506) re-extracted this session: sha256 prefix `162e30a6` —
MATCH to the tasked span digest; content re-read verbatim (B-L2* obligation,
two-time, carrier-uniform, VOLUME-diagonal locus, boundary-only control does
not discharge, witness E1_SEA_OFFDIAGONAL_HS_SHARP_RECORD_LOCALIZER). None
unverifiable. 8/8 including the attempt itself.

---

## 2. ATTACK 1 — THE STRATUM OBJECT. STRATUM_FIDELITY = CONFIRMED.

Re-derived from the sealed displays independently, byte-first:

```text
(1a) Per-pair object: E1 bytes [42492,42704) verbatim —
     V_{mu lambda}(a) := u_mu(a_-)^dagger u_lambda(a_+), per record-color
     pair, on the closed pair polydisc. MATCH to the attempt's §2.1.
(1b) Assembly weights: PA bytes [8096,8192) verbatim — R_pointer(a_+,a_-) =
     sum_(mu,lambda) w_mu^* w_lambda Gamma(u_mu(a_-)^dagger u_lambda(a_+));
     Z_comp at E1 bytes [43297,43698) verbatim. MATCH.
(1c) Weights REAL and equal to (1/2, -1/4, -1/4) on lambda = (0, +sqrt2,
     -sqrt2): I derived them MYSELF spectrally from the PA A3 qutrit c-matrix
     (c = [[0,0,-i],[0,0,+i],[+i,-i,0]], |ready> = |0>, |pointer> = |1>,
     w_lambda = <pointer|P_lambda|ready>) — §6 K14: spectrum {0, +-sqrt2},
     w = (1/2, -1/4, -1/4), all real. Consistent with the E1 :340-342 stencil
     sum_lambda w_lambda f(lambda) = -(1/4)[f(+sqrt2) - 2f(0) + f(-sqrt2)]
     read at path. GIVEN real weights, the branch factorization
     X = C[U(a_-)^dag U(a_+) - U(0)^dag U(0)]C, U(a) = sum_lambda w_lambda
     u_lambda(a), is exact algebra — CONFIRMED, not an idealization.
(1d) Kraus/moment structure: E1 :340-345 verbatim (m_0 = m_1 = 0; every
     Dyson term with < 2 record insertions PER CTP BRANCH annihilated;
     m_{2j} = -2^{j-1}); my §6 K12 re-derives m_0..m_8 =
     (0,0,-1,0,-2,0,-4,0,-8). Survivors n_-, n_+ >= 2 even. MATCH.
(1e) Two-time typing: E1 :512 verbatim ("Dyson-dressed by free h_0
     propagation over the whole interval"); PA Strang display :367-380 read
     at path. The attempt's "insertions at independent Dyson-ordered times
     interleaved with h_0" is the sealed reading. MATCH.
(1f) Sharp localizer: E1 D6' verbatim — M(t) = Q 1_{|x|<=r(t)} Q, r(t) =
     min(t,1-t), v(t) = tau_R 32 r(t)^3, not softenable; my §6 K15 verifies
     int_0^1 32 min(t,1-t)^3 dt = 1 exactly (so int v = tau_R = pi/sqrt2
     exact). PA A1 (:85-90) realizes the same S_n = -i slash(n) gamma^5;
     the executor's dirac_data() (bytes read at path, digest MATCH) returns
     (gamma^0 gamma^1, -i gamma^0 gamma^5): the attempt's pinned realization
     is the sealed one. MATCH.
(1g) Quantifier: carrier-uniform = the D3 refinement quantifier, R-L2b
     E1 :826-832 verbatim; locus = VOLUME diagonal (E1 §O.X span + C6
     refinement :359-364 verbatim); record_vertex_two_time_HS_certified =
     false at exactly E1 :2191 (line printed at path). The attempt's honest
     note that the SUMMED-quantifier S2' target is unsealed (debt 3) is
     AQ Det.1(iii) verbatim, and AQ-CHECK (331035d3, §2, §6) holds it
     CONFIRMED / AQ_VERDICT = SOUND. MATCH.
```

No idealization found. The object X, its grading, its quantifier, its locus,
and its threshold are the sealed ones.

---

## 3. ATTACK 2 — THE ORDER/BAND ALGEBRA, RERUN. ALGEBRA_RERUN = MATCH.

### 3.1 Independent re-derivation (own CAS, own construction)

My script (§6) differs from the attempt's in construction: entrywise
polynomial-ideal reduction modulo <u·u-1, v·v-1> (not their substitution
loop); the identities run in BOTH the pinned Dirac representation AND the
Weyl/chiral representation as a representation-independence control; the
pointer weights are DERIVED from the PA c-matrix, not typed in. Results:

```text
K1-K3   S^2 = I, S = S^dag, {S, alpha_j} = 0 (all j), S alpha_x S = -alpha_x:
        PASS in both representations. (=> S J S = -J at the full operator
        level: S acts only on the spinor slot of J = -(Q b_D Q) tensor
        alpha_x, so (I tensor S) J (I tensor S) = -(Q b_D Q) tensor
        (S alpha_x S) = -J. The attempt's I6 is the load-bearing content.)
K4-K5   The coincidence identities (I -+ alpha.v) S (I -+ alpha.u) =
        +- S (alpha.(v-u)) (I -+ alpha.u) under u^2 = v^2 = 1: PASS both
        representations. Hand re-derivation (two lines, anticommutation +
        (alpha.u)^2 = I) also reproduces it. => P_s(k') S P_s(k) =
        (+-1/2) S (alpha.(k_hat'-k_hat)) P_s(k): ONE exact coincidence order
        per band-diagonal record insertion. CONFIRMED.
K6-K7   Equal-momentum band-diagonal kill P_s S P_s = 0 (both bands); flip
        channel full: PASS both representations.
K8-K9   C(k) alpha_x C(k) = -k_hat_x C(k) (the smooth vertex's O(1)
        longitudinal intraband part — the contrast object) and the S1 trace
        tr[C(u) alpha_x] = -2 u_x: PASS.
K10     THE COLLAPSE IDENTITY, UNCONDITIONAL: sum_l w_l e^{-il th1 S} alpha_x
        e^{-il th0 S} = c(th1-th0) alpha_x with c(th) =
        (1-cos(sqrt2 th))/2 = sin^2(sqrt2 th/2), for FREE symbolic th0, th1 —
        NO constraint th0 + th1 = tau_R needed (the attempt's I13 imposed the
        constraint; the identity is stronger than checked: the odd moments
        kill the JS channel and S J S = -J kills the J + SJS channel for all
        phases). PASS. STRENGTHENING NOTED, verdict-neutral.
K11     c(0) = c'(0) = 0, c(tau_R) = 1, c = sin^2 form: PASS.
K12-K13 Moments through m_8; full-wrap saturation cos(lam tau_R) =
        (-1, +1, -1) on (+sqrt2, 0, -sqrt2) [= the sealed (+1,-1,-1) on
        (0, +sqrt2, -sqrt2)], sin = 0, sum w phase = 1 = sum |w| (l1-tight):
        PASS. With the C-L1 display u_lambda = (I-P) + phase_lambda P this
        yields U(0) = m_0 (I-P) + 1·P = P exactly — the sealed E1 :346-351
        display arithmetic, reproduced.
```

The attempt's own script was also extracted verbatim from its §9 and rerun in
a DIFFERENT venv: output matches its claimed output verbatim, line for line.

### 3.2 The FAILS-AT obstruction: hunted for missed cancellations. REAL.

The verdict is FAILS-AT, so the tasked hunt is for a cancellation the attempt
missed (which would overturn the failure). Six candidate kill channels were
checked; all are closed:

```text
(c1) Wrapped-phase cancellation of the longitudinal channel: the only
     channel the stencil phases could cancel is (J + SJS) — IDENTICALLY ZERO
     by K3; the surviving channel (J - SJS)/2 = J passes with weight
     c(Delta-theta) = sin^2(sqrt2 Delta-theta/2) >= 0, zero only quadratically
     at t' = 1/2 (K10-K11). No kill available.
(c2) t'-reflection oddness: v(t) is symmetric about t' = 1/2, so
     theta_0(1-t') = theta_1(t'), Delta-theta(1-t') = -Delta-theta(t'); c is
     EVEN in Delta-theta and b_D(t',x) is symmetric under t' -> 1-t': the
     integrand c·b_D is symmetric, not odd — no sign-cancellation of b~_P.
(c3) Branch combination: J is Hermitian (b_D real, alpha_x Hermitian); the
     a_+ term carries -i, the a_- term +i, so the P-blocks COMBINE to
     -i a_Delta C P J~ P C with a_Delta = a_+ - a_- — the sealed TT
     combination (TT-CHECK: a_Delta CONFIRMED). Builds, does not cancel.
(c4) The S1 trace oddness (K9: tr ~ -2 k_hat_x, odd) kills the TRACE only;
     the HS norm squares the symbol, |k_hat_x|^2 even — the sealed S1/S2
     division, reproduced at the summed quantifier. No kill.
(c5) The collapse display's cross blocks ((I-P)JP, PJ(I-P), weights c(th_0),
     c(th_1)) are OFF-DIAGONAL in the P-grading: they cannot cancel the
     P-diagonal block, and ||X||_2 >= ||P X P||_2 (compression is
     HS-decreasing) — a carrier-volume-divergent P-block forces failure of
     the whole regardless of every other block. The exhibit is robust.
(c6) Higher a-orders: separated by the a-grading (Cauchy coefficient bounds
     on the closed pair polydisc — a uniform ||X||_2 bound would bound each
     coefficient); a failing a-linear coefficient suffices. No kill.
=> The ONLY remaining cancellation locus is a degree-0 intraband P-block
   component of the UNCERTIFIED C-L1/C-L2 error with exactly opposite sign —
   precisely the conditionality the attempt states, with real sealed
   witnesses (E1 :1131-1143 read at path: C-L1 "extend to u_lambda = (I-P) +
   phase_lambda P + [error]", witness
   E1_RECORD_PHASE_OPERATOR_COLLAPSE_UNCERTIFIED; C-L2 commutator
   [h_0, M(t) tensor S], witness
   E1_RECORD_KINK_COMMUTATOR_FORM_UNCERTIFIED). Nothing sealed supplies the
   conspiracy. The obstruction is REAL at the sealed display.
```

### 3.3 Internal-consistency probe (per-stratum gain vs resummed O(1))

Adversarial probe run: the lambda^2 (two-insertion) stratum of the idealized
collapsed one-branch display carries coefficient +(theta_1-theta_0)^2/2 —
degree 0, NO coincidence factor (my K17: it is exactly c's quadratic jet;
SJS = -J and m_2 = -1). Is that a contradiction with the attempt's per-stratum
degree <= -2 claim? NO — and the resolution is the attempt's own: the
per-stratum coincidence suppression lives in the TRANSFER structure (S
sandwiched between projectors at different momenta, via the sharp localizer's
chi_hat), which the idealized phase display sets to zero; that discrepancy is
exactly the C-L2 commutator error, and the per-stratum degree bounds carry
n-GROWING constants (moments -2^{j-1}, n-fold transfer convolutions of an
L^2-not-L^1 kernel), so the degree of the sum is not the degree of the terms.
The attempt states precisely this (§3 (a3), §4.4 "GROWING moments... fed
through the mixed transfer regimes", §4.3 SCOPE, residual r2). The sealed C4
identity (resummation to P) governs the sum; the per-stratum certification is
correctly confined to reformulated grade. CONSISTENT.

Degree bookkeeping re-run: per-insertion band-diagonal factor
|k_hat'-k_hat| <= 2|q|/max(|k|,|k'|) (standard unit-vector estimate,
re-derived); per plus-band segment the phase omega = |k|+|k'| ~ 2|k|
(TWO-TIME :282-290 read at path: interband carries |k'|+|k| -> infinity;
intraband |omega| <= |q| bounded — the sealed massless marginality) with BV
t-amplitudes licenses ONE integration by parts, one power, power-law only —
matching the sealed record-strata typing (power-law, outside the smooth symbol
category; AQ S-2, TT-CHECK §5). Chain count: N - 2f band-diagonal insertions
and f segments give degree <= -(N-2f) - f = -(N-f); J's possible extra flip
only improves the parity cases; worst N = 4, f = 2: degree <= -2;
all-intraband: <= -N <= -4. Mixed-regime trade |k|^(gamma-1) · |k|^(-2 gamma)
= |k|^(-1-gamma) <= |k|^(-1): never better for the adversary. MATCH — as
DEGREE bookkeeping, with the completed joint norm honestly left as r2.

---

## 4. ATTACK 3 — HS THRESHOLD BOOKKEEPING. THRESHOLD = CONFIRMED.

```text
(t1) Volume-diagonal side: a kernel of coincidence degree -p on a 3-D volume
     is locally HS iff int_{|r|<1} |r|^{-2p} d^3 r < infinity iff 2p < 3 iff
     degree > -3/2. EXACT. C's off-diagonal kernel: degree -3 EXACT with
     modulus EXACTLY 1/(2 pi^2 |r|^3) (E1 C1 :320-329 verbatim at path;
     marginality (2/pi) log(R/a)). Two coincidence orders: -3 + 2 = -1 >
     -3/2 — past threshold. CONFIRMED.
(t2) Momentum side at bounded transfer: amplitude O(|k|^{-a}) gives HS^2 ~
     int d^3 k |k|^{-2a}: integrable iff a > 3/2; O(1) amplitude gives
     HS^2 ~ Lambda^3 — the C2 failure shape verbatim (E1 :330-338, "~ |D_t|
     Lambda^3 under carrier-blindness; finite only on a fixed carrier").
     Degree <= -2 (worst per-stratum configuration): 2a = 4 > 3 — strictly
     past. CONFIRMED, both sides.
(t3) Transfer tails: chi_hat_r(q) = 4 pi (sin(qr) - qr cos(qr))/q^3
     re-derived EXACTLY by symbolic integration (§6 K16); leading tail
     -4 pi r cos(qr)/q^2, degree -2, power-law only (M(t) untouched);
     |chi_hat|^2 ~ q^{-4}: int d^3 q q^{-4} UV-convergent — tails clear the
     threshold; int d^3 q |chi_hat| ~ int dq divergent — L^2 not L^1, so no
     factor-wise absolute transfer bound (r2 real). The tails are NOT the
     wall; the wall is the momentum volume at bounded transfer = the VOLUME
     diagonal — exactly the sealed C6 refinement (E1 :359-364 verbatim), and
     the exhibited term (P-block, ball interior) survives boundary smoothing.
     CONFIRMED.
```

---

## 5. ATTACK 4 — SCOPE. SCOPE = EXACT. And ATTACK 5 — PROVENANCE/INJECTION.

```text
(s1) NET strength: FAILS-AT is claimed CONDITIONAL at the sealed C4
     operator-level display, with the exact escape named (a certified
     exactly-opposite degree-0 intraband P-block in the C-L1/C-L2 error) and
     the only other named repair (softening M(t)) verified sealed away at
     E1 :1123-1124 verbatim ("whose only repair would be to soften M(t) — a
     SEALED PHASE-A A1 DEFINITION, NOT A LANE'S TO CHANGE"). The verdict
     taxonomy defense (not PROVED / not UNDECIDABLE-AT / not bare
     REFORMULATED) matches the sealed lineage standard (TWO-TIME NET = FAILS
     at per-pair, TT-CHECK CONFIRMED). EXACT.
(s2) EFFECT_ON_SUMMED_FIXED: the three-debt structure is AQ Det.4 verbatim
     at path ((1) B-L2*, (2) F'-14, (3) unsealed summed-S2' spec act;
     AQ-CHECK: SOUND); the attempt moves debt 1 to
     attempted-and-failing-at-a-named-step, CLAIMED and conditional — and
     carries debts 2 and 3 REGARDLESS, in both §8 and the flag. Per-pair
     untouched: FIXED dead as architected (52f2490b resolves at path to
     STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md, sealed); RUNNING left an
     unforced lean, no target created. The subsidiary is claimed at
     reformulated grade with residuals r1/r2/r3 named — correctly NOT claimed
     as B-L2*. EXACT.
(s3) PROVENANCE: 8/8 digests recomputed at path this session (attempt + 7
     sources), all matching sidecars and tasked digests; span digest
     162e30a6 recomputed; every line citation spot-read at path (C1, C2, C4,
     C6, D6', R-L2b, B-L2 typing, C-L1/C-L2, :2191, PA A1/A3/A4/Strang,
     executor dirac_data()); every byte citation re-extracted ([42492,42704),
     [43297,43698), [8096,8192)); all verbatim. The attempt's §9 script
     reproduces its claimed output verbatim on independent rerun. CLEAN.
(s4) INJECTION: the attempt contains no directive content, no register/
     tracker/plan/road/ledger/lens material, no numeric physical evaluation,
     no unsealed authority; sources scanned for injection-shaped directives:
     none (E1's two "override" hits are the spec's own internal language).
     Gates all false throughout. none.
(s5) Nits, verdict-neutral: (n1) the attempt's I13 verified the collapse
     identity only under theta_0 + theta_1 = tau_R; it holds unconditionally
     (my K10) — a strengthening, not a defect. (n2) §1 cites "seventeen"
     identities; the script prints I1-I15 with I7/I8/I9a/I9b/I9c making
     seventeen checks — consistent as counted by printed lines. (n3) The
     stencil-ordering note (§9, I11) is accurate.
```

---

## 6. MY SCRIPT (independent; reproduced in full) AND ITS OUTPUT

Run with sympy 1.14.0 in the checker lineage's venv (NOT the attempt's).
Exact symbolic only; nothing numeric evaluated.

```python
# BL2STAR-CHECK: INDEPENDENT exact symbolic re-derivation (cross-lineage).
# Own construction throughout: polynomial-ideal reduction instead of the
# attempt's substitution loop; TWO gamma representations (Dirac = the
# PA-pinned executor realization, and Weyl/chiral as a representation-
# independence control); pointer weights DERIVED spectrally from the PA
# qutrit c-matrix (not typed in); the collapse identity checked WITHOUT
# the theta0+theta1 = tau_R constraint (stronger than the attempt's I13).
# EXACT SYMBOLIC ONLY. Nothing numeric evaluated.
import sympy as sp

def reps():
    I2, Z2 = sp.eye(2), sp.zeros(2, 2)
    sx = sp.Matrix([[0, 1], [1, 0]])
    sy = sp.Matrix([[0, -sp.I], [sp.I, 0]])
    sz = sp.Matrix([[1, 0], [0, -1]])
    out = {}
    # Dirac representation (the PA-pinned executor's dirac_data()):
    g0 = sp.diag(1, 1, -1, -1)
    gs = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit())
          for s in (sx, sy, sz)]
    out['dirac'] = (g0, gs)
    # Weyl/chiral representation (control; same defining relations):
    g0w = sp.Matrix(sp.BlockMatrix([[Z2, I2], [I2, Z2]]).as_explicit())
    gsw = [sp.Matrix(sp.BlockMatrix([[Z2, s], [-s, Z2]]).as_explicit())
           for s in (sx, sy, sz)]
    out['weyl'] = (g0w, gsw)
    return out

ux, uy, uz, vx, vy, vz = sp.symbols('ux uy uz vx vy vz', real=True)
t0, t1 = sp.symbols('theta0 theta1', real=True)
GENS = [ux**2 + uy**2 + uz**2 - 1, vx**2 + vy**2 + vz**2 - 1]  # unit-vector ideal
SYMS = [ux, uy, uz, vx, vy, vz]

def zero_mod_unit(M):
    # entrywise polynomial reduction modulo the ideal <u.u-1, v.v-1>
    for e in M:
        num, den = sp.fraction(sp.cancel(sp.expand(e)))
        r = sp.reduced(sp.Poly(sp.expand(num), *SYMS, domain='QQ_I').as_expr(),
                       GENS, *SYMS)[1]
        if sp.simplify(r) != 0:
            return False
    return True

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}")

for rep, (g0, (g1, g2, g3)) in reps().items():
    g5 = sp.I * g0 * g1 * g2 * g3
    S = -sp.I * g0 * g5                       # S = -i slash(n) gamma^5, slash(n)=gamma^0
    al = [g0 * g for g in (g1, g2, g3)]       # alpha_j
    I4 = sp.eye(4)
    Mu = ux*al[0] + uy*al[1] + uz*al[2]
    Mv = vx*al[0] + vy*al[1] + vz*al[2]
    Du = (vx-ux)*al[0] + (vy-uy)*al[1] + (vz-uz)*al[2]
    print(f"--- representation: {rep} ---")
    ok("K1  S^2=I, S=S^dag        ", S*S == I4 and sp.simplify(S.H - S) == sp.zeros(4, 4))
    ok("K2  {S,alpha_j}=0 (j=x,y,z)", all(sp.expand(S*a + a*S) == sp.zeros(4, 4) for a in al))
    ok("K3  S alpha_x S = -alpha_x ", sp.expand(S*al[0]*S + al[0]) == sp.zeros(4, 4))
    ok("K4  (I-Mv)S(I-Mu)=S Du(I-Mu)", zero_mod_unit((I4-Mv)*S*(I4-Mu) - S*Du*(I4-Mu)))
    ok("K5  (I+Mv)S(I+Mu)=-S Du(I+Mu)", zero_mod_unit((I4+Mv)*S*(I4+Mu) + S*Du*(I4+Mu)))
    ok("K6  P_s(k)SP_s(k)=0 both bands", zero_mod_unit((I4-Mu)*S*(I4-Mu)) and zero_mod_unit((I4+Mu)*S*(I4+Mu)))
    ok("K7  flip full (I+Mu)S(I-Mu)=2S(I-Mu)", zero_mod_unit((I4+Mu)*S*(I4-Mu) - 2*S*(I4-Mu)))
    ok("K8  (I-Mu)ax(I-Mu)=-2ux(I-Mu)", zero_mod_unit((I4-Mu)*al[0]*(I4-Mu) + 2*ux*(I4-Mu)))
    ok("K9  tr[C(u) alpha_x]=-2ux  ", sp.expand(sp.trace(sp.Rational(1, 2)*(I4-Mu)*al[0])) == -2*ux)
    if rep == 'dirac':
        # K10: UNCONDITIONAL collapse identity (no theta0+theta1 constraint):
        w_of = {sp.sqrt(2): sp.Rational(-1, 4), sp.Integer(0): sp.Rational(1, 2),
                -sp.sqrt(2): sp.Rational(-1, 4)}
        E = lambda lam, th: sp.cos(lam*th)*I4 - sp.I*sp.sin(lam*th)*S  # exp(-i lam th S), S^2=I
        L = sum((w*E(lam, t1)*al[0]*E(lam, t0) for lam, w in w_of.items()), sp.zeros(4, 4))
        c = (1 - sp.cos(sp.sqrt(2)*(t1 - t0)))/2
        D = sp.expand_trig(sp.expand(L - c*al[0]))
        ok("K10 sum_l w_l e^{-il th1 S} ax e^{-il th0 S} = c(th1-th0) ax, UNCONDITIONAL",
           sp.simplify(D) == sp.zeros(4, 4))
        ok("K11 c(th)=sin^2(sqrt2 th/2); c(0)=c'(0)=0; c(tauR)=1",
           sp.simplify(c - sp.sin(sp.sqrt(2)*(t1-t0)/2)**2) == 0
           and c.subs(t1, t0) == 0
           and sp.diff(c, t1).subs(t1, t0) == 0
           and sp.simplify(c.subs([(t1, sp.pi/sp.sqrt(2)), (t0, 0)])) == 1)

# Record-side arithmetic (representation-free):
lam_v = [sp.sqrt(2), sp.Integer(0), -sp.sqrt(2)]
w_v = [sp.Rational(-1, 4), sp.Rational(1, 2), sp.Rational(-1, 4)]
j = sp.symbols('j', positive=True, integer=True)
moms = [sp.nsimplify(sum(w*l**n for w, l in zip(w_v, lam_v))) for n in range(9)]
ok("K12 m_0..m_8 = 0,0,-1,0,-2,0,-4,0,-8 (= -2^{j-1} even, 0 odd)",
   moms == [0, 0, -1, 0, -2, 0, -4, 0, -8])
tauR = sp.pi/sp.sqrt(2)
ph = [sp.cos(l*tauR) for l in lam_v]
sn = [sp.sin(l*tauR) for l in lam_v]
ok("K13 full wrap: cos(lam tauR)=(-1,1,-1), sin=0; sum w ph = 1 = sum|w| (l1-tight)",
   [sp.simplify(x) for x in ph] == [-1, 1, -1]
   and all(sp.simplify(x) == 0 for x in sn)
   and sp.simplify(sum(w*p for w, p in zip(w_v, ph))) == 1
   and sum(abs(w) for w in w_v) == 1)
# K14: pointer weights DERIVED from the PA A3 qutrit c-matrix (not typed in):
cmat = sp.Matrix([[0, 0, -sp.I], [0, 0, sp.I], [sp.I, -sp.I, 0]])
ready = sp.Matrix([1, 0, 0])
pointer = sp.Matrix([0, 1, 0])
derived = {}
for lam, mult, basis in cmat.eigenvects():
    Pl = sp.zeros(3, 3)
    for b in basis:
        b = b / sp.sqrt((b.H * b)[0, 0])
        Pl += b * b.H
    derived[sp.nsimplify(lam)] = sp.nsimplify(sp.simplify((pointer.H * Pl * ready)[0, 0]))
ok("K14 PA c-matrix spectrum {0,+-sqrt2}; w_lambda=<pointer|P_lambda|ready>=(1/2,-1/4,-1/4), REAL",
   set(derived) == {0, sp.sqrt(2), -sp.sqrt(2)}
   and derived[0] == sp.Rational(1, 2)
   and derived[sp.sqrt(2)] == sp.Rational(-1, 4)
   and derived[-sp.sqrt(2)] == sp.Rational(-1, 4))
# K15: v-profile normalization: int_0^1 32 min(t,1-t)^3 dt = 1 EXACTLY (rational identity):
t = sp.symbols('t', real=True)
ok("K15 int_0^1 32 min(t,1-t)^3 dt = 1 exactly",
   sp.integrate(32*t**3, (t, 0, sp.Rational(1, 2)))
   + sp.integrate(32*(1-t)**3, (t, sp.Rational(1, 2), 1)) == 1)
# K16: ball-indicator transfer kernel, exact closed form + leading tail:
q, r = sp.symbols('q r', positive=True)
x = sp.symbols('x', real=True)
chi_hat = sp.integrate(2*sp.pi*x**2 * sp.sin(q*x)/(q*x) * 2, (x, 0, r))  # int_{|y|<=r} e^{-iq.y} d^3y
closed = 4*sp.pi*(sp.sin(q*r) - q*r*sp.cos(q*r))/q**3
ok("K16 chi_hat_r(q) = 4pi(sin(qr)-qr cos(qr))/q^3 exactly; leading tail -4pi r cos(qr)/q^2 (degree -2)",
   sp.simplify(chi_hat - closed) == 0
   and sp.simplify(closed - (-4*sp.pi*r*sp.cos(q*r)/q**2)
                   - 4*sp.pi*sp.sin(q*r)/q**3) == 0)
# K17: the lambda^2 (two-insertion) stratum of the collapsed one-branch display
#      has coefficient +(th1-th0)^2/2 (degree 0, NO coincidence factor in the
#      idealized display) — the consistency point: per-stratum suppression
#      lives in the TRANSFER structure, not in the idealized phases; summed,
#      the phases rebuild c(th1-th0) exactly. (SJS=-J used; m_2=-1.)
c2 = -moms[2]*(t1 - t0)**2/2
cser = sp.series((1 - sp.cos(sp.sqrt(2)*(t1 - t0)))/2, t1, t0, 3).removeO()
ok("K17 lambda^2 stratum coefficient = (th1-th0)^2/2 = c's quadratic jet",
   sp.simplify(c2 - (t1 - t0)**2/2) == 0 and sp.simplify(cser - c2) == 0)
```

Output, verbatim:

```text
--- representation: dirac ---
K1  S^2=I, S=S^dag        : PASS
K2  {S,alpha_j}=0 (j=x,y,z): PASS
K3  S alpha_x S = -alpha_x : PASS
K4  (I-Mv)S(I-Mu)=S Du(I-Mu): PASS
K5  (I+Mv)S(I+Mu)=-S Du(I+Mu): PASS
K6  P_s(k)SP_s(k)=0 both bands: PASS
K7  flip full (I+Mu)S(I-Mu)=2S(I-Mu): PASS
K8  (I-Mu)ax(I-Mu)=-2ux(I-Mu): PASS
K9  tr[C(u) alpha_x]=-2ux  : PASS
K10 sum_l w_l e^{-il th1 S} ax e^{-il th0 S} = c(th1-th0) ax, UNCONDITIONAL: PASS
K11 c(th)=sin^2(sqrt2 th/2); c(0)=c'(0)=0; c(tauR)=1: PASS
--- representation: weyl ---
K1  S^2=I, S=S^dag        : PASS
K2  {S,alpha_j}=0 (j=x,y,z): PASS
K3  S alpha_x S = -alpha_x : PASS
K4  (I-Mv)S(I-Mu)=S Du(I-Mu): PASS
K5  (I+Mv)S(I+Mu)=-S Du(I+Mu): PASS
K6  P_s(k)SP_s(k)=0 both bands: PASS
K7  flip full (I+Mu)S(I-Mu)=2S(I-Mu): PASS
K8  (I-Mu)ax(I-Mu)=-2ux(I-Mu): PASS
K9  tr[C(u) alpha_x]=-2ux  : PASS
K12 m_0..m_8 = 0,0,-1,0,-2,0,-4,0,-8 (= -2^{j-1} even, 0 odd): PASS
K13 full wrap: cos(lam tauR)=(-1,1,-1), sin=0; sum w ph = 1 = sum|w| (l1-tight): PASS
K14 PA c-matrix spectrum {0,+-sqrt2}; w_lambda=<pointer|P_lambda|ready>=(1/2,-1/4,-1/4), REAL: PASS
K15 int_0^1 32 min(t,1-t)^3 dt = 1 exactly: PASS
K16 chi_hat_r(q) = 4pi(sin(qr)-qr cos(qr))/q^3 exactly; leading tail -4pi r cos(qr)/q^2 (degree -2): PASS
K17 lambda^2 stratum coefficient = (th1-th0)^2/2 = c's quadratic jet: PASS
```

(K16's radial form uses int_{|y|<=r} e^{-iq·y} d^3y = int_0^r 4 pi x^2
sinc(qx) dx, an exact identity, evaluated symbolically.)

---

## 7. FLAG BLOCK

```text
STRATUM_FIDELITY = CONFIRMED(the sealed object exactly: per-pair V at E1
  [42492,42704) verbatim; assembly weight sum at PA [8096,8192) and Z_comp at
  E1 [43297,43698) verbatim; weights independently DERIVED from the PA A3
  c-matrix as (1/2,-1/4,-1/4) REAL (K14), so the branch factorization
  X = C[U(a_-)^dag U(a_+) - U(0)^dag U(0)]C is exact; Kraus second
  difference + moments m_0 = m_1 = 0, m_{2j} = -2^{j-1} re-derived (K12);
  survivors n_-, n_+ >= 2 even; two-time Dyson interleaving per E1 :512 and
  PA Strang; M(t) sharp per D6' with int v = tau_R = pi/sqrt2 re-verified
  exact (K15); S = -i gamma^0 gamma^5 verified at the executor bytes;
  carrier-uniform = D3 refinement quantifier (R-L2b :826-832); locus =
  VOLUME diagonal; span digest 162e30a6 recomputed MATCH;
  record_vertex_two_time_HS_certified = false at exactly E1 :2191; the
  unsealed summed-S2' caveat is AQ Det.1(iii), AQ-CHECK SOUND. No
  idealization found.)

ALGEBRA_RERUN = MATCH(all seventeen attempt identities re-derived under an
  independent construction — polynomial-ideal reduction, pointer weights
  derived not assumed, run in BOTH the pinned Dirac and a control Weyl
  representation: all PASS (K1-K13); the attempt's own script reruns to its
  claimed output verbatim in a different venv; STRENGTHENING of record: the
  collapse identity sum_l w_l e^{-il th1 S} J e^{-il th0 S} = c(th1-th0) J
  holds UNCONDITIONALLY — no theta_0 + theta_1 = tau_R constraint needed
  (K10) — the attempt checked only the constrained form; the FAILS-AT
  obstruction survived a directed six-channel cancellation hunt ((J+SJS)=0
  identically so no phase kill; c >= 0 quadratic zero only; t'-reflection
  gives even not odd integrand; branches combine to a_Delta, no kill; S1
  trace oddness kills trace not HS; P-grading cross blocks cannot cancel the
  P-block and ||X||_2 >= ||PXP||_2) — the only cancellation locus is a
  degree-0 intraband P-block of the uncertified C-L1/C-L2 error, exactly the
  claimed conditionality, witnesses verified at E1 :1131-1143; the
  per-stratum-gain vs resummed-O(1) tension probed and resolved consistently
  (K17: the idealized display's lambda^2 stratum is degree 0 BECAUSE the
  coincidence suppression lives in the transfer structure = the C-L2 error;
  n-growing constants; the sealed C4 identity governs the sum); the
  -(N-f) degree count and the mixed-regime trade re-derived, worst case -2.)

THRESHOLD = CONFIRMED(volume side: degree -p locally HS on a 3-D volume iff
  2p < 3, i.e. > -3/2 exact; C degree -3 exact per C1 verbatim; two orders
  land at -1 > -3/2; momentum side: O(1) amplitude gives HS^2 ~ Lambda^3 =
  the C2 shape verbatim, degree <= -2 gives 2a = 4 > 3 integrable; tails:
  chi_hat closed form and -2 leading tail re-derived exactly (K16),
  |chi_hat|^2 ~ q^{-4} integrable so tails clear, chi_hat L^2 not L^1 so
  joint estimates required (r2 real); the wall is the volume diagonal at
  bounded transfer, matching the sealed C6 refinement :359-364 verbatim.)

NET_VERDICT = CONFIRMED(FAILS-AT(the stratum-resummation step at the sealed
  saturation clause E1 :346-351), at exactly the claimed strength: the
  P-restricted longitudinal intraband survivor -i a_Delta k_hat_x
  b~_P(x,k_hat) C(k), degree 0 at the volume diagonal, carrier-volume HS^2,
  exhibited AT the sealed C4 operator-level display and robust in the
  P-block against every other block; CONDITIONAL exactly as flagged on the
  uncertified C-L1/C-L2 error not supplying an exactly-opposite degree-0
  intraband P-block component — nothing sealed supplies one; softening M(t)
  sealed away at :1123-1124 verbatim and untouched; the subsidiary
  per-stratum degree certification stands at reformulated grade with
  residuals r1/r2/r3 correctly named; verdict-taxonomy defense sound per the
  sealed TWO-TIME FAILS standard.)

SCOPE = EXACT(the three-debt structure is AQ Det.4 verbatim (AQ-CHECK SOUND);
  debt 1 moved to attempted-and-failing-at-a-named-step, CLAIMED and
  conditional; debts 2 (F'-14) and 3 (summed-S2') carried REGARDLESS in both
  §8 and the flag; per-pair untouched — FIXED dead as architected, 52f2490b
  resolves at path (STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md); RUNNING
  an unforced lean, no target created; equal-time not resurrected; nothing
  overstated.)

PROVENANCE = CLEAN(8/8 digests recomputed at path this session — the attempt
  6e81ae92 plus 46846730 E1, 789338ad PA, 18af3dee TWO-TIME, 6a2aa0fd
  TT-CHECK, 0a10c030 AQ, 331035d3 AQ-CHECK, 3d8aea1a executor — all matching
  sidecars and tasked digests; span [57212,58506) recomputed 162e30a6 MATCH;
  every line and byte citation spot-read verbatim at path; the attempt's §9
  script reproduces its claimed output verbatim on independent rerun; none
  unverifiable.)

INJECTION = none(no directive content, no register/tracker/plan/road/ledger/
  lens material, no numeric physical evaluation, no unsealed authority in
  the attempt; sources scanned for injection-shaped directives: none; gates
  all false throughout.)

MACHINERY_USED_BY_ME = yes(CAS identity checks only — sympy 1.14.0 in the
  checker lineage's venv, 26 PASS lines over two gamma representations, own
  script + output reproduced verbatim in §6; shasum/byte extraction/line
  reads otherwise; NOTHING numeric evaluated.)

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
BL2STAR_CHECK_RESULT = SEALED.
```
