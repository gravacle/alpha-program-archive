# STAGE 8 — THE REFUTING BRANCH, REALIZED: WHAT THE FOUR-OBLIGATION CAMPAIGN BINDING IMPLIES NOW THAT p = -3 IS DERIVED — OBLIGATION-BY-OBLIGATION ADJUDICATION WITH THE SEALED CONSUMER MAP

## BLIND BUILDER — CODENAME RFB-BUILD — COMMISSION S9AD — [CLAIMED]

Date: 2026-08-14 (UTC 2026-08-15)
Role: adjudicator of record for the gap named at 52f2490b :160-166
(`extensivity_binding_on_refuting_branch = UNSTATED_IN_CORPUS`): the campaign
binding (0f3082ca :86-99) was written for the closing branch; the refuting
branch has now been REALIZED at two sealed grades (the uniform constant,
52f2490b; the coincidence-vanishing rate, 9fdc3d1c p = -3, audited 549362d4);
this artifact states, obligation by obligation and consumer by consumer, what
the realized branch does — at lane grade. NOTHING IS RULED: O-1/F'-5, the
fourth horn, and any successor charter are the principal's; register/tracker
acts are the registrar's.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until checked.

Fences held: BYTES-LEVEL ADJUDICATION — occurrence- and type-reading of
sealed artifacts, consumed at their audited grades; exact symbolic where
needed (CAS sympy 1.14.0, fresh venv `rfbvenv`, script + output verbatim §8,
11/11 PASS); no floats as ground; no value frozen; no rate derived; NO
VERDICT CONTENT FOR 5d — this artifact maps consequences and takes no
position of any kind on whether anything is FORCED, FREE, or RUNS; the
registered RUNS-support token (Q-1089) is EXPECTED-UNLOCATABLE and is
neither consumed nor extended. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened (ONE filename-
level deviation disclosed, §9 CL-5 — content not relied on). "Q-..." tokens
EXPECTED-UNLOCATABLE, noted not chased. No git action. ONE output (this
artifact) + seal sidecar at the commission-distinct path, probed ABSENT
before write. Every enumeration sentence claims only its own displayed sweep.

---

## 0. VERDICT LINE (honest-outcome menu, top line)

**ADJUDICATED (the four dispositions + consumer map). Obligation 1 (R-L2b
itself): REFUTED-WITH-IT as written at BOTH sealed grades — the uniform
constant (52f2490b) and now the coincidence-vanishing rate (p = -3 at the
polydisc-sup quantifier, 9fdc3d1c/549362d4) — and RE-POSES as r-3's MO-3
restricted to the three named non-coincidence routes (assembly cancellation;
weight-summed re-quantification onto the record strata; region-level
cumulants), the per-pair-sup quantifier being dead as architected of record.
Obligation 2 (T11's response half): RE-POSES — it consumed R-L2b as written
and cannot pass by consuming it; T11 response naturality itself is NOT
refuted (measure half derived separately; naturality open); any successor
obligation must retype it explicitly, and the adopted summed discharge does
NOT pass it (the summed-to-per-pair bridge is barred by the amendment's own
falsifier V-2 and by the sealed one-way quantifier record). Obligation 3
(the D3 refinement-natural weight): weight side UNTOUCHED — closed exactly
and unconditionally of record, no realized-branch content reaches it;
response side REFUTED-WITH-IT on the per-pair coincidence route (the
kernel-level mechanism that would have set the exponent through the RFA
arithmetic is refuted at its root) and SURVIVES-VIA-A-NAMED-OPEN-ROUTE at
the SUMMED quantifier, where alpha' = 1/4 is delivered of record on the
similarity class of the entered cell (all-pairs instance; the cell axis is
R-L0's; the surviving-sector transfer is D-i = R-L4a/b). Obligation 4
(connected extensivity): NOT REFUTED-WITH-IT — the binding's own
refuting-branch sentence is the one sentence it DID state ("the reason
extensivity is unresolved is R-L2b's named failure and not a general
malaise"), and the realized branch SHARPENS the named failure without
touching the truth value: the transfer of p = -3 to the connected-cumulant
shrink rate is blocked by the booked non-identity of record (A-B REDUCIBLE
B->A on the CLOSING branch only, NS-1..NS-5; the sea-kernel-to-prepared-
state-clustering identification is recorded-DECLINED); connected extensivity
SURVIVES-VIA three named open routes at three stated quantifiers. The
summed-S2' adopted content, its alpha' = 1/4 discharge, and its R.3'(c) wall
are UNTOUCHED by the realized branch — indeed the branch's kernel-grade
mechanization CORROBORATES the booked per-pair/summed split and RAISES the
R.3'(c) wall's W-2 from divergence-of-record to derived-mechanism grade.
r-3's MO-2+MO-4 package is UNTOUCHED in content and status, with ONE stated
interaction: it is now the only sealed r-3 closure package that does not
pass through a non-coincidence MO-3 route. The running-kappa_record
contingency (0f3082ca §4) is DISPLAYED at its carrier with its two distinct
triggers adjudicated: the consequence-map trigger (p <= 3/2 horn) has its
antecedent REACHED at the coincidence route; §4's own literal trigger
("IF connected extensivity does fail") has NOT fired. The charter is the
principal's; nothing is staged. CAS battery 11/11 PASS.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session and matched against its
`.seal.sha256` sidecar (and the commission's pinned prefixes). First 8 hex.
19/19 MATCH.

```text
COMMISSION-PINNED SEALED STATE:
0f3082ca  STAGE8_EXTENSIVITY_VERDICT_AND_RL2B_CAMPAIGN_OPENING_V001.md    OK (the binding :86-99; §4 the contingency :130-146)
52f2490b  STAGE8_RL2B_UNIFORMITY_REFUTED_RESULT_V001.md                   OK (the refutation; §4 :150-166 the named gap; flag :196)
9fdc3d1c  STAGE8_MO3_P_EXPONENT_S9AD_V001.md                              OK (p = -3 DERIVED; §5 what it settles; §8 scope)
549362d4  STAGE8_MO3_P_EXPONENT_S9AD_AUDIT_V001.md                        OK (CONFIRMED-WITH-CORRECTIONS; C1 quantifier; CONSUMER_NOTE)
THE OBLIGATIONS' OWN CARRIERS AND SEALED CONSUMER LISTS:
d1807b96  STAGE8_RL2B_COMMUTATOR_ROUTE_REFUTED_AND_TARGET_SHARPENED_V001.md OK (threshold Ground 1; the sharpened question)
bebc0f08  STAGE8_R3_JOINT_RATE_S9AD_V001.md                               OK (MO-1..MO-4; :815 the closure arithmetic)
72c95d42  STAGE8_R3_JOINT_RATE_S9AD_AUDIT_V001.md                         OK
80db260f  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md                       OK (F1 = MO-3 exactly; MO-4 absence re-confirmed)
3de0502c  STAGE8_RATE_GAP_LINKAGE_S9AD_V001.md                            OK (A-B/B-C/A-C typings; §2 consumer lists; §6 consequence map)
6ca465a6  STAGE8_RESPONSE_MAP_PULLBACK_S6_ADJUDICATION_EINSTEIN_V001.md   OK (N7 T11 consumed R-L2b; N4 anti-deflation; N6 hypothesis-list)
dcf29eea  STAGE8_O7_RESPONSE_PULLBACK_TRANSFER_CHECK_V001.md              OK (:211-222 T11 pullback open, not refuted)
5fbae703  STAGE8_EVALUATOR_CORRECTION_PATH_DETERMINATION_AND_THREE_OBLIGATIONS_V001.md OK (T11 OVER-CLAIMED; "same open question, two labels")
430f0971  STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md   OK (§7.3 the boundary-subextensivity NO_VERDICT + named prerequisite)
THE SUMMED-S2' ADOPTED CONTENT:
ce59b480  STAGE8_SUMMED_S2_CANDIDATE_V001.md                              OK (S2'-O / S2'/R-L2b^Sigma / R.3'; V-1..V-6; NF-1/NF-2)
c2346000  STAGE8_SUMMED_S2_CANDIDATE_CHECK_V001.md                        OK (READY-FOR-PRINCIPAL)
cbfbb74c  STAGE8_SUMMED_S2_ENTRY_ADDENDUM_V001.md                         OK (the ADOPTION of record; gate list; falsifiers in force)
e5b0879b  STAGE8_S2_DISCHARGE_V001.md                                     OK (alpha' = 1/4 on the similarity class; R.3'(c) FAILS-AT W-1..W-3)
cbe6ff4f  STAGE8_S2_DISCHARGE_AUDIT_V001.md                               OK (CONFIRMED per its own grade)
PROPOSED-GRADE PRIOR (consumed at that grade, corroboration only):
f6fe4072  STAGE8_RL2B_REFUTING_BRANCH_FOUR_OBLIGATION_ANALYSIS_PROPOSED_V001.md OK (2026-07-28; PROPOSED, NOT ADOPTED; pre-dates p = -3)
```

Key spans read at the bytes this session: 0f3082ca full; 52f2490b full;
9fdc3d1c full; 549362d4 full; d1807b96 full; f6fe4072 full; bebc0f08 :38-83,
:443-537; 80db260f :46-49 (verdict line), section heads; 3de0502c full;
e5b0879b :1-110, :277-320; cbfbb74c full; 6ca465a6 :240-275; dcf29eea
:205-225; 5fbae703 :130-160; 430f0971 :555-600.

---

## 2. WHAT "REALIZED" MEANS — THE TWO GRADES, AND THE EXACT QUANTIFIER CONSUMED

```text
GRADE 1 (2026-07-28, 52f2490b): R-L2b AS WRITTEN — sup_n ||X_n||_2 <= M
  with M admissible under F'-5 — REFUTED: no such M exists (||X||_2 =
  infinity by C1's degree -3 modulus; weak lower semicontinuity). At that
  date a RATE successor remained possible and was the sharpened target
  (d1807b96 §4: does the difference kernel vanish like |r|^p, p > 3/2?).
GRADE 2 (2026-08-14, 9fdc3d1c + 549362d4): THE SHARPENED TARGET'S CLOSING
  BRANCH REFUTED — p = -3 DERIVED: the unsandwiched difference kernel does
  not vanish at coincidence at any order; it diverges there at degree -3
  plus an exact contact term. Threshold p > 3/2 missed by 9/2 powers plus a
  delta (exact; CAS G1c/G2a). The coincidence-vanishing route to R-L2b /
  r-3's MO-3 / the wall's F1 escape CLOSES NEGATIVELY.
THE QUANTIFIER CONSUMED (audit C1, binding on every consumption below):
  the sup-quantified (polydisc-sup) certification via coincidence vanishing
  is dead REGARDLESS of any exceptional pair set; pointwise p = -3 holds
  for all pairs outside an at-worst-discrete exceptional set; the frozen
  pairs are NOT individually certified against an exact all-orders
  cancellation. Per 549362d4 CONSUMER_NOTE: consumers may consume the
  NEGATIVE closure at the polydisc-sup quantifier; any consumer needing
  pointwise p at the frozen pairs must first close C1's gap. Every
  disposition in §3-§6 consumes ONLY the polydisc-sup form.
WHAT REMAINS OPEN OF RECORD AT MO-3's OBJECT (9fdc3d1c §5, THRESHOLD flag):
  routes not through coincidence vanishing of the unsandwiched kernel —
  (a) per-pair cancellation at ASSEMBLY; (b) WEIGHT-SUMMED
  re-quantification onto the record strata; (c) REGION-LEVEL (cumulant)
  objects (29084cd2 §4's route: untouched in logic, its INPUT now p = -3).
```

---

## 3. THE FOUR OBLIGATIONS, ONE BY ONE

### 3.1 Obligation 1 — R-L2b itself (the Schatten-2 scaling exponent)

```text
DISPOSITION: REFUTED-WITH-IT as written (both grades, §2) + RE-POSES as
  MO-3 restricted to the three named non-coincidence routes.
AT BYTES: the obligation demanded a finite HS bound uniform in the carrier
  (52f2490b §4(1): "the obligation as written cannot be met"); the rate
  successor's only sealed mechanism-route through the kernel's coincidence
  behavior is now closed ("no certified sub-volume trace-norm rate for
  Delta_n(a) can be sourced from coincidence vanishing of the unsandwiched
  kernel, because there is none" — 9fdc3d1c THRESHOLD flag, audited).
  The per-pair-sup quantifier is dead as architected of record (0a10c030
  :348-349 via 3de0502c Chain 1). What survives as the obligation's ROLE:
  a certified sub-volume trace-norm rate for Delta_n(a) — r-3's MO-3 as a
  NAMED OBJECT — reachable only via routes (a)/(b)/(c) of §2, each with
  its own unbuilt object and NO claim here that any closes.
RE-POSED FORM (stated, not chartered): any successor obligation must
  (i) state its quantifier (per-pair-sup is dead; polydisc-sup coincidence
  certification is dead; pointwise-at-frozen-pairs needs C1's gap closed);
  (ii) name which of routes (a)/(b)/(c) it runs; (iii) restate what
  constants may depend on and how the carrier/index issue is handled —
  the retyping demand of f6fe4072 Decision A (PROPOSED grade), grounded
  independently here on the sealed refutations themselves.
CONSUMERS (sealed lists — 3de0502c :181-186; 52f2490b §4; bebc0f08;
  80db260f), each with its realized-branch state:
  - SCAD S2/S2b's alpha/beta (E1 :696-708): alpha REMAINS A SYMBOL of
    record; the kernel-level mechanism that would have set it through the
    RFA arithmetic is refuted at its root; the RFA kernel-to-alpha
    dichotomy remains self-disclaimed power counting, NOT run here in
    either direction (9fdc3d1c §5, consumed at grade).
  - r-3 Route 1 via MO-3 (bebc0f08 :480-484): gated on a non-coincidence
    MO-3; see §5 for the MO-2+MO-4 interaction.
  - The wall's F1 escape (80db260f: "F1 = MO-3 exactly"): CONFIRMED DEAD
    at the coincidence route; the wall's carriers-needed R5 topology (the
    MO-3 shape) remains open only via routes (a)/(b)/(c).
  - The sliver-naturality question / C_ref-vs-D3 conflict (218be86d, via
    3de0502c): input updated — the derived jet is aspect-ratio-blind
    (9fdc3d1c §6), so no sliver-direction rescue of a coincidence route
    exists; the conflict itself is its own item, not resolved here.
  - O-1 / F'-5 (52f2490b §4): RIPE, NOT RULED — the principal's,
    unchanged; the realized branch adds only that the fourth-horn
    question's factual ground is now sharper (the object has NO
    coincidence vanishing at all), and answers nothing.
```

### 3.2 Obligation 2 — T11's response half (response-map pullback commutation + boundary-subextensive invariance)

```text
DISPOSITION: RE-POSES. Not REFUTED-WITH-IT; not passable as consumed.
AT BYTES: T11's response half consumed R-L2b (52f2490b §4(2); 6ca465a6 N7:
  T11_response_half_rests_on_a_met_obligation = false, "R-L2b's uniformity
  is REFUTED as written and T11's response half consumed it"). Of record
  the two are "THE SAME OPEN QUESTION WEARING TWO LABELS" (5fbae703): what
  is refuted is the QUESTION'S R-L2b-as-written form; T11 response
  naturality itself is NOT refuted — the pullback is "open; not proved by
  the measure-side derivation; not refuted" (dcf29eea :211-222), and the
  anti-deflation clause binds (6ca465a6 N4: proved = false AND refuted =
  false, carried).
WHY NO SEALED ROUTE NOW PASSES IT: (i) via R-L2b as written — refuted;
  (ii) via the coincidence-rate successor — closed at grade 2;
  (iii) via the adopted summed discharge — BARRED: the amendment's V-2
  forbids creating a summed->per-pair bridge, none is used or created of
  record (e5b0879b §2.4), and the sealed quantifier record is one-way
  (0a10c030 via 3de0502c NS-4; fdc09c07's recorded-DECLINED reverse
  identification). So the response half stands OPEN with its consumption
  route dead: it RE-POSES against whatever successor the principal
  charters, and must be retyped there explicitly, not silently inherited.
CONSUMERS (sealed), each with its realized-branch state:
  - The S6/P8 gluing hypothesis list (6ca465a6 N6): T11 naturality is one
    hypothesis in one theorem's hypothesis list — inherits OPEN; nothing
    flips.
  - Boundary-subextensive invariance (dcf29eea :219-222): "separately
    open / already routed through the connected-extensivity/R-L2b family"
    — inherits obligation 4's disposition (§3.4).
  - TASK4D's boundary-subextensivity clause (430f0971 §7.3):
    BOUNDARY_SUBEXTENSIVITY_FOR_T4D = NO_VERDICT with the named
    prerequisite "a replacement failure-capable boundary estimate or a
    ruling connecting the R-L2b refuting branch to this exact clause."
    This artifact supplies the CONNECTION AT LANE GRADE (the refuting
    branch neither proves nor refutes the clause; transport of either
    verdict remains barred); the RULING the clause names remains the
    principal's. RL2B_REFUTATION_AUTOMATICALLY_REFUTES_T4D_BOUNDARY_CLAUSE
    = false stands.
```

### 3.3 Obligation 3 — the D3 refinement-natural weight's RESPONSE side

```text
DISPOSITION: weight side UNTOUCHED; response side REFUTED-WITH-IT on the
  per-pair coincidence route + SURVIVES-VIA-A-NAMED-OPEN-ROUTE at the
  summed quantifier.
WEIGHT SIDE — UNTOUCHED, WHY: "its weight side is closed exactly and
  unconditionally" (0f3082ca :90-91); no realized-branch statement touches
  the volume-weight object; nothing in 9fdc3d1c/549362d4 names it; the
  distinction is the binding's own and is preserved (f6fe4072 §4(3)
  corroborates at PROPOSED grade).
RESPONSE SIDE — REFUTED-WITH-IT at its per-pair route: the response-side
  certification was to arrive as R-L2b's exponent (||C(V(a)-V(0))C||_2 <=
  |D|_4^alpha G_hs). Of record the finiteness of that object was
  "purchased entirely by cancellation in the difference ... alpha IS SET
  BY THE CANCELLATION RATE" (0f3082ca §5 :173-179); p = -3 establishes
  that the cancellation supplies NO coincidence vanishing at all, so the
  per-pair response-side exponent cannot be certified by any coincidence
  mechanism (polydisc-sup quantifier). alpha stays a symbol; the RFA
  dichotomy (p <= 3/2 -> its sliver-aggregate growth display
  A^{3(1-alpha)}) is of record self-disclaimed power counting (RFA
  :86-88, :137-140 via 9fdc3d1c §5) and is NOT run here in either
  direction.
SURVIVES-VIA — WHICH ROUTE, AT WHAT QUANTIFIER: the SUMMED quantifier of
  the adopted amendment. There a response-side scaling exponent IS
  delivered of record: alpha' = 1/4 on the SIMILARITY CLASS of the
  entered cell, all-pairs instance, n-uniform per admitted cell, exact
  dilation-covariance mechanism (one Duhamel time-extent power; exact
  two-time volume cancellation — e5b0879b, audit-CONFIRMED; CAS G6a).
  Scope walls carried verbatim: non-congruent admitted cells = R-L0's
  axis (r-A1); surviving-sector transfer = D-i (R-L4a/b); alpha' = 1/4 is
  the SUMMED object's exponent and does NOT certify the per-pair D3
  response side (V-2; the barred bridge). The realized branch touches
  none of this (§4).
CONSUMERS (sealed): D5's action-density form via the closing-branch
  supply chain "p > 3/2 -> ... alpha = 1 and D5's action-density form
  holds, flat in aspect ratio" (d1807b96 :116-118) — that chain's
  ANTECEDENT IS NOW REFUTED at the coincidence route; the chain supplies
  nothing and asserts nothing on the realized branch (it was
  one-directional). The 2ede02ae :122-131 dissolve-chain (T11 + D3 +
  extensivity dissolve on FULLY-CANCELS) — same: antecedent refuted,
  chain inert, nothing transported in the negative direction.
```

### 3.4 Obligation 4 — CONNECTED EXTENSIVITY

```text
DISPOSITION: NOT REFUTED-WITH-IT; status corrected to UNRESOLVED at
  R-L2b's NAMED FAILURE, the failure now LOCALIZED; SURVIVES-VIA three
  named open routes.
THE ONE REFUTING-BRANCH SENTENCE THE BINDING DID STATE, now operative:
  "IF R-L2b FAILS, THE REASON EXTENSIVITY IS UNRESOLVED IS R-L2b's NAMED
  FAILURE AND NOT A GENERAL MALAISE" (0f3082ca :98-100). That is an
  attribution sentence, not a falsity sentence (f6fe4072 §3, corroborating
  at PROPOSED grade). On the realized branch it GAINS CONTENT: the named
  failure is no longer only "no admissible uniform M" but "the sealed
  two-time object's difference kernel diverges at coincidence at degree -3
  with an exact contact term — there is no coincidence vanishing for any
  certification to consume" (polydisc-sup quantifier).
WHY THE TRUTH VALUE IS NOT TOUCHED (the transfer is blocked at bytes):
  the binding's identity sentence ("...the shrink rate of the connected
  cumulants, which IS R-L2b's exponent", 0f3082ca :92-95) is
  ONE-DIRECTIONAL AND CLOSING-BRANCH of record; the adjudicated linkage
  (3de0502c, A-B) is REDUCIBLE B->A ONLY, closing-branch and
  quantifier-scoped, with SAME-OBJECT refuted at NS-1..NS-5 — in
  particular NS-1: the connected-cumulant object is CONNECTION-ONLY and
  the sea-covariance representation (where p = -3 lives) is "the only
  WRITTEN realization, not a proof"; the sea-kernel-to-prepared-state-
  clustering identification is recorded-DECLINED (58f89435 via 3de0502c).
  p = -3 therefore does NOT derive a connected-cumulant shrink rate, in
  either direction. Extensivity remains of record
  REQUIRED_NOT_DERIVED_NOT_CONTRADICTED (0f3082ca §1), with the ECO §1.2
  joint statement standing ("tau_R makes the estimate hard. It does not
  make extensivity false") and the sealed grammar's structure displayed
  exactly at CAS G4 (tau_R fixed; the entire burden on the cumulants).
SURVIVES-VIA — THE THREE ROUTES, AT THEIR QUANTIFIERS:
  (i)  CONNECTION-ONLY: the (KP) bound + [c3] zero-free neighborhood on
       the cell-adjacency graph (5cdd5daf via 3de0502c §2.1) —
       FAILS-AT/unbuilt, not impossible; scale-free combinatorial
       quantifier; independent of p entirely.
  (ii) SUMMED (Route B): the ADOPTED S2'/R.3' spec content; B-L2*
       discharged at estimate grade; "C exists as an intensive object at
       the summed quantifier" gated on the entry addendum's list —
       R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D (H-R/H-B0 inside), plus the
       compute pre-gate and F0 break [1]'s construction gate (cbfbb74c
       item 5). The realized branch changes NOTHING on this gate list
       (§4).
  (iii) REGION-LEVEL: the cumulant/Gamma_K-log route (29084cd2 §4 via
       9fdc3d1c §4.2) — "untouched in logic; its INPUT changes (p = -3,
       not 0 or 1)"; a different functional, not decided here.
CONSUMERS (sealed lists — 3de0502c §2.1; cbfbb74c), each inheriting
  UNRESOLVED-NOT-REFUTED: R_record,L's definition (V011 :1292-1300 via
  df4514a5); the FORM post-limit fork arbitration and the F0 chain
  break [1] (df4514a5 §6) — arbitration unchanged, no branch is selected
  by the realized refutation; the Gate-5 "C exists" gate list (above) —
  membership unchanged of record; the per-pair record (52f2490b)
  untouched, per the entry addendum's own sentence.
```

---

## 4. THE SUMMED-S2' ADOPTED CONTENT, alpha' = 1/4, AND THE R.3'(c) WALL

```text
DOES THE REALIZED BRANCH TOUCH THE ADOPTION? NO — UNTOUCHED, and the check
  is run against the amendment's own falsifiers: V-1 fires only on
  refutation of the SUMMED estimate at the amendment's own quantifier;
  p = -3 is a PER-PAIR UNSANDWICHED coincidence statement. The booked
  non-conflict of record (df4514a5 :260-264 via 3de0502c NS-2): "the
  summed-level discharge is of the ASSEMBLED object whose degree-0
  content is annihilated by the C4 kill — exactly the content whose
  per-pair survivor drives the per-pair infinity." The MO3 jet's leading
  content IS that degree-0 intraband survivor, now at kernel grade — the
  realized branch is the per-pair half of the SAME booked quantifier
  split, and CORROBORATES it. V-2..V-6: none engages (no bridge used or
  created; no sealed anchor overturned; no bytes contradiction; the kill
  operates of record).
alpha' = 1/4 (e5b0879b, audit cbe6ff4f): UNTOUCHED at its own quantifier —
  similarity class of the entered cell, all-pairs instance, n-uniform per
  admitted cell; G_hs^Sigma = (24/pi)^{1/4} M_dom(eps) a frozen-tuple
  functional; the exact covariance mechanism (CAS G6a: one power of L)
  contains no coincidence-vanishing premise for p = -3 to disturb.
  1/4 != 1/2 exact (CAS G6b) — the per-pair-forced 1/2 was already NOT
  delivered, on the discharge's own derivation, before the realized
  branch; nothing here re-litigates that. Residues r-A1 (non-congruent
  cells = R-L0) and r-A2/D-i (surviving-sector transfer = R-L4a/b) stand
  exactly as sealed.
THE R.3'(c) WALL (e5b0879b Task B): STANDS, REINFORCED IN MECHANISM,
  UNCHANGED IN RESIDUE. Its three walls: W-1 the resolvent is
  pair-diagonal (exact scalar witness 3/4 != 2/3); W-2 every majorant of
  either factor routes through PER-PAIR HS norms, carrier-divergent of
  record (52f2490b), with derived c-positivity blocking majorant-level
  cancellation; W-3 H-R may never default. The realized branch UPGRADES
  W-2's ground from divergence-of-record to derived-mechanism grade: the
  per-pair HS divergence is now MECHANIZED as the delta + CZ coincidence
  jet (9fdc3d1c §4.2), and no coincidence-vanishing rescue of per-pair
  majorants can exist (there is no vanishing). The wall's exact residue
  is UNCHANGED: a NEW summed-level second-order identity/estimate that
  never factors through per-pair HS majorants, plus H-R, plus D-i.
  Witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS. R.3'(c)
  remains the summed horn's one live analytic wall.
```

---

## 5. r-3's MO-2 + MO-4 PACKAGE — CONFIRMED UNTOUCHED, WITH ONE STATED INTERACTION

```text
MO-2 (certified LOWER rate for kappa_n or -log|det_n(0)|, at a = 0):
  UNTOUCHED. B-C are COUSINS of record with NO rate chain between them
  (3de0502c §3.2: disjoint loci — B's object vanishes identically at
  a = 0, C is entirely an a = 0 baseline statement; the one attempted
  bridge is refuted, C[C,Y]C = 0). p = -3 is an a != 0, first-order
  statement; it neither supplies nor obstructs any kappa_n rate.
MO-4 (product-level cancellation identity for R_n Delta_n(a)):
  UNTOUCHED AS A NAMED ABSENCE — "none exists in the corpus" (bebc0f08
  sweep s-6; independently re-confirmed by the wall's splitting sweep,
  80db260f). p = -3 does not construct one and does not exclude one:
  MO-4 is assembly-shaped — it lives on route (a) of §2, which the
  realized branch names OPEN.
THE ONE INTERACTION, STATED (descriptive route arithmetic, bebc0f08 :815
  re-pinned; CAS G5 re-verifies the underlying n-free-ratio bookkeeping):
  of the two sealed closure packages — Route 1 = MO-1+MO-2+MO-3, direct =
  MO-2+MO-4 — Route 1 now depends on a NON-COINCIDENCE realization of
  MO-3, while MO-2+MO-4 never passed through MO-3 at all. On the realized
  branch MO-2+MO-4 is therefore the only sealed r-3 closure package whose
  route content is entirely unclipped by p = -3. This is a statement
  about which doors remain, not about which opens; no rate is derived
  and no package is claimed closable.
```

---

## 6. THE RUNNING-kappa_record CONTINGENCY AT ITS CARRIER (0f3082ca §4) — DISPLAYED, NOT STAGED

```text
THE CARRIER'S EXACT CONTENT (0f3082ca §4 :130-146, consumed verbatim):
  "IF connected extensivity does fail, NO SEALED REPLACEMENT CONSTRUCTION
  EXISTS — both lanes confirm this independently. THE NEAREST SHAPE,
  named and NOT pursued: a RUNNING kappa_record — a response that depends
  on the cellulation scale rather than converging to an intensive
  constant, i.e. a RENORMALIZATION-GROUP structure rather than a
  thermodynamic-limit one. Suggestive adjacency, not a claim: EM
  Dependency-Order step 4 is 'parent-derived functional regulator and
  FINITE RENORMALIZATION', flagged as where response normalization can
  originate. STATUS: A CONTINGENCY. It would be a NEW PRINCIPLE. It is
  the principal's to charter IF R-L2b fails. NOT PURSUED NOW, and it
  would collide with Theorem 1's derived intensivity for the disjoint
  part unless reconciled."
THE TWO TRIGGERS, ADJUDICATED SEPARATELY:
  T-A (the consequence-map trigger, 3de0502c :523-526): "IF B CLOSES on
    the p <= 3/2 horn ... the named contingency shape is the
    running-kappa_record NEW PRINCIPLE — a principal's charter, no
    lane's." ANTECEDENT REACHED at the coincidence route: p = -3 <= 3/2
    is derived at the polydisc-sup quantifier. (Reached AT THAT ROUTE
    only: the non-coincidence routes of §2 are open of record, so "B
    closed on the p <= 3/2 horn" is exact for the coincidence mechanism
    and is not extended beyond it.)
  T-B (§4's own literal trigger): "IF connected extensivity does fail" —
    NOT FIRED. Extensivity is UNRESOLVED, not failed (§3.4). The stronger
    trigger's antecedent is not satisfied by anything sealed.
WHAT THE NEW PRINCIPLE CHARTER WOULD HAVE TO CONTAIN — DESCRIPTIVELY, AS
THE RECORD STATES IT (no charter text is authored or staged; the act is
the principal's):
  c-1 THE SHAPE: a response depending on the cellulation scale rather
      than converging to an intensive constant — an RG structure rather
      than a thermodynamic-limit one (:135-138). It is a NEW PRINCIPLE,
      not a repair: no sealed replacement construction exists (:133-134).
  c-2 THE RECONCILIATION OBLIGATION: it must reconcile with Theorem 1's
      DERIVED exact intensivity of the disjoint part (:143) — the
      disjoint additivity A_K = prod A_Ki, Gamma_K = sum Gamma_Ki is
      DERIVED of record and any scale-dependent response must not
      contradict it.
  c-3 THE NAMED ADJACENCY, carried at its own grade: EM Dependency-Order
      step 4 ("parent-derived functional regulator and FINITE
      RENORMALIZATION") is flagged as where response normalization can
      originate — "suggestive adjacency, not a claim" (:138-140).
  c-4 THE DISCLOSURE PATTERN OF RECORD for any fallback in this family
      (recorded at §3 for the NEIGHBOR fallback, the Hessian-first
      supersession, terms F1-F5 — attaching to THAT fallback, displayed
      here as the recorded no-renegotiation pattern): state what is
      SUPERSEDED and what is NOT satisfied or proved (F1); carry the
      quantitative zero-stiffness control (F2); prove
      cellulation-independence of what replaces it (F3); source the
      supersession explicitly (F4); list Theorem 3's five conditions
      verbatim as an open register, marking what is discharged (NONE
      FULLY) and what is merely not needed (F5).
FENCE, RESTATED AT THE POINT OF MAXIMUM RISK: the above is a DISPLAY of a
  sealed contingency SHAPE. No sentence in this artifact asserts,
  supports, or opposes that any physical response or coupling RUNS; the
  registered RUNS-support (Q-1089) is not consumed, not extended, and not
  locatable from here (EXPECTED-UNLOCATABLE). kappa_record_computed =
  false; nothing is evaluated at any scale. The RUNS-vs-CLOSES horn of
  5801681a is of record UNDETERMINED and is not adjudicated here.
```

---

## 7. WHAT THIS ARTIFACT DOES NOT DO

```text
does_not_rule_O1_F5 = true                      (ripe, not ruled — principal's)
does_not_answer_fourth_horn = true              (principal's)
does_not_charter_any_successor = true           (running-kappa_record, Hessian-first, or other)
does_not_declare_connected_extensivity_false = true
does_not_declare_connected_extensivity_true = true
does_not_derive_any_rate = true                 (no p successor, no kappa_n rate, no alpha value)
does_not_extend_p_to_frozen_pairs_pointwise = true   (C1 gap open; polydisc-sup only)
does_not_adjudicate_RUNS_vs_CLOSES = true       (5801681a horn stands)
does_not_carry_5d_verdict_content = true        (consequence mapping only)
does_not_flip_any_flag_or_witness = true        (registrar's; SCAD witnesses stand as sealed)
does_not_retype_T11_or_D3_itself = true         (retyping is the successor charter's burden)
```

---

## 8. THE CAS BATTERY (VERBATIM) AND ITS OUTPUT (VERBATIM)

Run with sympy 1.14.0 in a fresh venv (`rfbvenv`). Exact symbolic only.

```python
# RFB-BUILD CAS battery -- EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv rfbvenv).
# Consequence-mapping arithmetic only: every constant symbolic or an exact
# rational. NOTHING numeric evaluated; no physical magnitude computed, bounded,
# estimated, or evaluated; no value of alpha (coupling), kappa_record, or n.
import sympy as sp

R = []
def chk(name, ok):
    R.append((name, ok)); print(("PASS " if ok else "FAIL ") + name)

r, eps, tau, B, L, V, c, n = sp.symbols('r epsilon tau B L V c n', positive=True)
p = sp.symbols('p', real=True)
k = sp.symbols('k', integer=True, nonnegative=True)

# G1 -- the sealed threshold grammar (d1807b96 Ground 1): 4 pi int_eps^1 r^{2p-4} dr
#       converges at 0 iff 2p - 3 > 0 (p > 3/2). Convergent instance p = 2; divergent
#       instances p = 3/2 and p = -3 (the realized value).
I_p2 = sp.integrate(r**(2*sp.Integer(2) - 4), (r, eps, 1))
chk("G1a convergent instance p = 2: int_eps^1 r^0 dr -> 1 as eps -> 0",
    sp.simplify(sp.limit(I_p2, eps, 0) - 1) == 0)
I_p32 = sp.integrate(r**(2*sp.Rational(3, 2) - 4), (r, eps, 1))
chk("G1b threshold p = 3/2 itself DIVERGES (int r^-1)",
    sp.limit(I_p32, eps, 0) == sp.oo)
I_pm3 = sp.integrate(r**(2*(-3) - 4), (r, eps, 1))
chk("G1c realized p = -3 DIVERGES, exact rate eps^-9",
    sp.limit(I_pm3, eps, 0) == sp.oo and
    sp.simplify(sp.limit(eps**9 * I_pm3, eps, 0) - sp.Rational(1, 9)) == 0)

# G2 -- shortfall and exponent bookkeeping of the realized branch (9fdc3d1c).
chk("G2a shortfall 3/2 - (-3) = 9/2 EXACT (the '4.5 powers')",
    sp.Rational(3, 2) - (-3) == sp.Rational(9, 2))
chk("G2b pair-integrand exponent at p = -3: 2p - 4 = -10 (needs > -1)",
    2*(-3) - 4 == -10)

# G3 -- ECO section 5's own divergence display: int d^3 r |r|^-6 = 4 pi int r^-4 dr,
#       divergence rate eps^-3 exact (0f3082ca :159-165).
I_m4 = sp.integrate(r**(-4), (r, eps, 1))
chk("G3  ECO rate: eps^3 * int_eps^1 r^-4 dr -> 1/3 exactly (rate eps^-3)",
    sp.simplify(sp.limit(eps**3 * I_m4, eps, 0) - sp.Rational(1, 3)) == 0)

# G4 -- obligation 4's sealed grammar (0f3082ca :92-95): sum_n tau_R^n <B^n>_c / n!.
#       With tau_R FIXED and the cumulant content held at a symbol B, the series is
#       ENTIRE: sum tau^k B^k / k! = exp(tau B). The convergence burden sits entirely
#       on the cell-size dependence of the cumulants -- the binding's own sentence,
#       displayed as exact structure (no cumulant is evaluated).
S = sp.summation(tau**k * B**k / sp.factorial(k), (k, 0, sp.oo))
chk("G4  sum_k tau^k B^k / k! = exp(tau B) exactly (tau_R fixed; burden = cumulants)",
    sp.simplify(S - sp.exp(tau*B)) == 0)

# G5 -- r-3's D10 ceiling arithmetic re-pinned (bebc0f08 :340-362): numerator budget
#       4n^3 c against kappa ceiling 2n^3 -- the carrier power cancels EXACTLY; the
#       ratio is n-FREE and never o(1).
ratio = sp.simplify((4*n**3*c) / (2*n**3))
chk("G5  rank x op ratio = 2c, n-FREE exactly (no o(1) from budgets)",
    ratio == 2*c and not ratio.has(n))

# G6 -- the summed-quantifier scaling bookkeeping (e5b0879b): alpha' = 1/4 means the
#       bound scales as |C|_4^{1/4}; under isotropic dilation |C|_4 -> L^4 |C|_4 this
#       is ONE power of L (the Duhamel time-extent power) -- exact covariance identity.
chk("G6a (L^4 V)^{1/4} = L V^{1/4} exactly (one power of L)",
    sp.simplify((L**4 * V)**sp.Rational(1, 4) - L * V**sp.Rational(1, 4)) == 0)
chk("G6b 1/4 != 1/2 exact (alpha'(Sigma) is not the forced per-pair 1/2)",
    sp.Rational(1, 4) != sp.Rational(1, 2))

# G7 -- the frozen pairs are nonzero exact rationals (PA :140-141): a_Delta = 18/100, 9/100.
chk("G7  a_Delta in {18/100, 9/100}, both != 0 exact (no float)",
    sp.Rational(18, 100) != 0 and sp.Rational(9, 100) != 0)

print()
ok = all(x for _, x in R)
print("BATTERY:", ("ALL %d PASS" % len(R)) if ok else "FAILURES PRESENT")
```

Output, verbatim (single run, no corrections needed):

```text
PASS G1a convergent instance p = 2: int_eps^1 r^0 dr -> 1 as eps -> 0
PASS G1b threshold p = 3/2 itself DIVERGES (int r^-1)
PASS G1c realized p = -3 DIVERGES, exact rate eps^-9
PASS G2a shortfall 3/2 - (-3) = 9/2 EXACT (the '4.5 powers')
PASS G2b pair-integrand exponent at p = -3: 2p - 4 = -10 (needs > -1)
PASS G3  ECO rate: eps^3 * int_eps^1 r^-4 dr -> 1/3 exactly (rate eps^-3)
PASS G4  sum_k tau^k B^k / k! = exp(tau B) exactly (tau_R fixed; burden = cumulants)
PASS G5  rank x op ratio = 2c, n-FREE exactly (no o(1) from budgets)
PASS G6a (L^4 V)^{1/4} = L V^{1/4} exactly (one power of L)
PASS G6b 1/4 != 1/2 exact (alpha'(Sigma) is not the forced per-pair 1/2)
PASS G7  a_Delta in {18/100, 9/100}, both != 0 exact (no float)

BATTERY: ALL 11 PASS
```

---

## 9. CHOICE LEDGER, TOY_SEPARATION, SWEEP CUTOFF

```text
CHOICE LEDGER (every discretionary choice, with its blast radius):
 CL-1 DISPOSITION-TAXONOMY APPLICATION (which of the commission's four
      labels each obligation takes): YOURS (judgment), displayed with the
      byte evidence per obligation in §3; the strongest counter-reading in
      each case is displayed beside it (e.g. ECO :95's "which IS R-L2b's
      exponent" against NS-1..NS-5 for obligation 4).
 CL-2 f6fe4072 CONSUMED AT PROPOSED GRADE ONLY, as corroboration: every
      disposition is grounded on sealed adopted/audited text independently
      of it. Blast radius: none — striking f6fe4072 changes no §3 sentence.
 CL-3 "REALIZED" READ AS TWO-GRADE (constant + coincidence-rate): FORCED
      by the commission's own text and by 549362d4's corrected quantifier.
 CL-4 p = -3 CONSUMED AT THE AUDITED GRADE ONLY (polydisc-sup +
      generic-pair; frozen-pair pointwise NOT individually certified —
      audit C1). Every consumption in §3-§6 is sup-quantified; the
      pointwise form is used nowhere. FORCED (549362d4 CONSUMER_NOTE).
 CL-5 FENCE DEVIATION, DISCLOSED: one early content grep for "T11" was run
      against workspace/*.md before the fenced-name filter was applied and
      matched STAGE8_MASTER_PLAN_TO_ALPHA_V001.md (a plan-class name); two
      lines of its content surfaced in the grep output. NOT RELIED ON:
      every T11 statement in §3.2 is sourced to 0f3082ca / 52f2490b /
      6ca465a6 / dcf29eea / 5fbae703 / 430f0971, all seal-verified. The
      file was not opened; no other fenced-class content was touched.
 CL-6 THE RFA DICHOTOMY (alpha; A^{3(1-alpha)}) DISPLAYED AT ITS
      SELF-DISCLAIMED POWER-COUNTING GRADE AND NOT RUN in either
      direction: FORCED (RFA's own disclaimer, carried by 9fdc3d1c §5).
 CL-7 RUNS-vs-CLOSES (5801681a) NOT ADJUDICATED; the §6 contingency
      display carries an explicit non-support clause: FORCED (commission
      fence; 5d).
 CL-8 T-A vs T-B TRIGGER SPLIT FOR THE CONTINGENCY (§6): YOURS in
      presentation, FORCED in content — the two sealed sentences have
      different antecedents and only one antecedent is satisfied; merging
      them would overstate the record in exactly the direction the fences
      bar.
TOY_SEPARATION: CLEAN — no toy family, model instance, or witness
  construction is used anywhere; every claim is an occurrence- or
  type-reading of sealed displays at their own quantifiers, or exact
  bookkeeping arithmetic of those displays (§8), with grades
  (CLAIMED/PROPOSED/audited) carried inline at every consumption.
SWEEP CUTOFF (declared): ROOTS = /Users/bgm/MB Work/alpha-program-archive
  (workspace/ content-swept; cleanroom_output/ NOT swept) and
  /Users/bgm/MB Work/alpha_supervision (FILENAME LISTING ONLY; no file
  under it opened). Fenced name classes
  (register|road_|ledger|lens|plan|tracker|THE_HANDOFF) excluded from
  every open (one filename-level grep deviation disclosed at CL-5).
  Searches run: sidecar-hash lookups for all commission digests; content
  greps for "T11", "refinement-natural", "alpha' = 1/4", "R.3'(c)",
  "refuting branch", "extensivity_binding_on_refuting_branch",
  "p = -3|p=-3", "S2PRIME|SUMMED|SIMILARITY", "CONSUMER". Files opened in
  full or at named spans: exactly those in §1's seal table. The "p = -3"
  carrier sweep found NO consumer artifact beyond the MO3 pair — this
  adjudication is first. The refuting-branch carrier sweep found exactly:
  52f2490b, f6fe4072, 3de0502c, 430f0971 (all consumed above). CUTOFF: no
  full-corpus read; "Q-..." register tokens (Q-1048 at e5b0879b, Q-33 at
  6ca465a6, Q-1089 at the commission) noted EXPECTED-UNLOCATABLE, not
  chased. Enumeration claims are limited to these sweeps.
```

---

## 10. FLAG BLOCK

```text
REFUTING_BRANCH_ADJUDICATION = ADJUDICATED(the four dispositions + the
  sealed consumer map, at lane grade; the gap of 52f2490b :160-166 is now
  STATED at this grade — whether the corpus flag
  extensivity_binding_on_refuting_branch flips from UNSTATED_IN_CORPUS is
  the registrar's act, not taken here.)

OB1_RL2B = REFUTED-WITH-IT(as written, both grades: uniform constant
  52f2490b; coincidence-vanishing rate 9fdc3d1c/549362d4 at polydisc-sup)
  + RE-POSES(as r-3's MO-3 restricted to the three named non-coincidence
  routes — assembly cancellation / weight-summed re-quantification onto
  record strata / region-level cumulants — with the successor's
  quantifier, route, and constant-dependence to be restated; per-pair-sup
  dead as architected of record. Consumers: SCAD S2/S2b alpha stays a
  symbol, coincidence mechanism refuted at root; wall F1 confirmed dead
  at that route; r-3 Route 1 gated on non-coincidence MO-3; sliver
  direction supplies no rescue (jet aspect-ratio-blind); O-1/F'-5 ripe
  not ruled, fourth horn open — both the principal's.)

OB2_T11_RESPONSE_HALF = RE-POSES(consumed R-L2b as written — unmeetable;
  every sealed pass-route now dead: as-written refuted, coincidence
  successor closed, summed bridge BARRED (V-2; one-way quantifier record);
  T11 naturality itself NOT refuted — measure half derived separately,
  pullback open, anti-deflation clause binding; must be retyped against
  any principal-chartered successor, not silently inherited. Consumers:
  S6/P8 hypothesis list inherits OPEN; boundary-subextensivity inherits
  OB4's disposition; TASK4D §7.3's NO_VERDICT stands with its named
  prerequisite — the connection is supplied here at lane grade, the
  RULING it names remains the principal's.)

OB3_D3_WEIGHT = WEIGHT_SIDE_UNTOUCHED(closed exactly and unconditionally
  of record; no realized-branch content reaches it) ;
  RESPONSE_SIDE = REFUTED-WITH-IT(on the per-pair coincidence route: the
  cancellation supplies no vanishing, so no coincidence mechanism can set
  the per-pair exponent; RFA dichotomy self-disclaimed, not run) +
  SURVIVES-VIA(the SUMMED quantifier: alpha' = 1/4 delivered of record on
  the similarity class, all-pairs instance; cell axis = R-L0; sector
  transfer = D-i/R-L4a/b; not a per-pair certification — bridge barred.
  Consumers: the D5 action-density closing-branch chain and the 2ede02ae
  dissolve-chain are INERT — antecedents refuted, nothing transported in
  either direction.)

OB4_CONNECTED_EXTENSIVITY = NOT-REFUTED-WITH-IT +
  UNRESOLVED-AT-THE-NAMED-FAILURE(the binding's own refuting-branch
  sentence operates; the named failure now LOCALIZED to the derived
  coincidence jet at the polydisc-sup quantifier) +
  SURVIVES-VIA(three routes at three quantifiers: connection-only
  (KP)+[c3] unbuilt-not-impossible; summed Route B behind the adopted
  gate list R-L4a/b + R-L0(/R-L0b) + C-L2 + OBL-D + compute pre-gate +
  F0 break [1]; region-level cumulants with logic untouched and input
  updated to p = -3. Truth-value transfer BLOCKED at bytes: A-B is
  REDUCIBLE B->A closing-branch only, SAME-OBJECT refuted NS-1..NS-5,
  sea-kernel/clustering identification recorded-DECLINED. Consumers:
  R_record,L definition, FORM fork arbitration, F0 break [1], Gate-5
  gate list — all inherit UNRESOLVED-NOT-REFUTED, membership unchanged.)

SUMMED_S2PRIME_ADOPTION = UNTOUCHED(V-1..V-6 checked: none engages; the
  realized branch is the per-pair half of the booked quantifier split of
  record and CORROBORATES it — the MO3 jet's leading content is the very
  degree-0 survivor the summed C4 kill annihilates.)
ALPHA_PRIME_QUARTER = UNTOUCHED(its covariance mechanism carries no
  coincidence-vanishing premise; scope walls r-A1/r-A2/D-i stand as
  sealed; 1/4 != 1/2 exact.)
R3PRIME_C_WALL = STANDS-REINFORCED(residue UNCHANGED — a summed-level
  second-order identity/estimate without per-pair HS majorants + H-R +
  D-i; W-2 upgraded from divergence-of-record to derived-mechanism grade
  by the kernel jet; no coincidence rescue of per-pair majorants exists;
  witness SCAD_SUMMED_SEGMENT_REMAINDER_UNCERTIFIED STANDS.)

R3_MO2_MO4 = UNTOUCHED(MO-2: disjoint locus a = 0, no rate chain to p of
  record; MO-4: named absence re-confirmed, assembly-shaped, on the OPEN
  route (a)) ; INTERACTION_STATED(on the realized branch MO-2+MO-4 is the
  only sealed r-3 closure package not passing through a non-coincidence
  MO-3 route — a statement about remaining doors, not about closability.)

RUNNING_KAPPA_RECORD_CONTINGENCY = DISPLAYED(0f3082ca §4 verbatim at its
  carrier; trigger T-A (p <= 3/2 horn, 3de0502c :523-526) antecedent
  REACHED at the coincidence route; trigger T-B (§4's literal "IF
  connected extensivity does fail") NOT FIRED; charter contents displayed
  DESCRIPTIVELY as the record states them (c-1 shape / c-2 Theorem-1
  reconciliation / c-3 adjacency at its own grade / c-4 the recorded
  disclosure pattern F1-F5 of the neighbor fallback); THE CHARTER IS THE
  PRINCIPAL'S — NOTHING STAGED; explicit non-support clause carried: no
  sentence herein supports or opposes any RUNS-shaped claim; Q-1089 not
  consumed, not extended.)

VERDICT_CONTENT_FOR_5D = NONE(consequence mapping only; no FORCED / FREE /
  RUNS sentence anywhere; the RUNS-vs-CLOSES horn of 5801681a stands
  unadjudicated.)
MACHINERY_INVOKED = yes(exact symbolic CAS only — sympy 1.14.0, fresh venv
  rfbvenv, 11-check battery reproduced verbatim with output in §8, single
  clean run; shasum -a 256, file reads, and greps otherwise. NOTHING
  numeric: no value of n, kappa_record, alpha (coupling), any physical
  norm, scale, or spectrum computed, bounded, estimated, or evaluated.)
FORBIDDEN_IMPORTS = none(no scale, imported GR, or faithfulness premise as
  authority; scale-bearing sealed text consumed at adjudicated grade only;
  fenced-class opens: none — one filename-level grep deviation disclosed
  (CL-5), content not relied on; "Q-..." tokens not chased; sweep cutoff
  declared (§9); 19/19 seals recomputed at path before reliance; no git
  action; output + sidecar at the commission-distinct path, probed ABSENT
  before write.)

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
production_authorized = false
ALL_RESULTS = CLAIMED until the adversarial check.
REFUTING_BRANCH_ADJUDICATION_RESULT = COMPLETE AT COMMISSION SCOPE.
```
