# STAGE 8 — CROSS-CHECK OF THE SLOT-2 DISCHARGE PATH
## DARIO LANE (Builder B, verifier) — RELAY 878 — [PLAN:AXN-BUILD-B6]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

No member bound; no fixed-point execution; no end test; no smooth import; no EM identification; no
common cell formed; no junction map evaluated; no numeric evaluation of any physical quantity; no
comparison to measured constants. PE-1..PE-13 pointer-only, none opened or consulted. Builder-A code
not opened. `~/.codex` untouched; memory-bank never searched. No register, plan, tracker, git action.

CLAIM STATUS: **all headline items CLAIMED.**

SUBJECT: `STAGE8_AXN_SLOT2_DISCHARGE_PATH_CODEX2_V001.md` = `e2b33845e08c163e`, seal verified BEFORE
reading. Principal-sealed condition `SLOT2_DISCHARGE_CONDITION_REANCHORED_DECISION_OF_RECORD_V001.md`
verified from its own directory (the recurring bare-filename cwd artifact defeated the first check;
requoted from `supervision/` it reads **OK**).

**DISCLOSED AT THE TOP.** The decisive question is one I already answered. At 857 I typed the
`BOX_gravity` row `NEITHER-FORCED` and found that slot 2's own deepest missing-list holds six items,
**not one a term**. 876 now rules `STRUCTURAL-ADMISSIBLE`. **That agrees with me, which is exactly why
I must not confirm it cheaply — two lanes agreeing because the second recognised its own prior
conclusion is one lane counted twice.** So the test I ran is the one that could break my own 857: a
**positive hunt for a term-demanding sentence**, not a confirmation that none exists.

*(Noted without weight: this principal-sealed condition exists because of my own 857 law-7 report —
Q-760 carried it. A repair I caused is not evidence for a reading I hold.)*

---

## 0. LEAD

| item | result |
|---|---|
| `T10` | **STRUCTURAL-ADMISSIBLE-CONFIRMED (bytes)** — and I resolve the one element I myself flagged at 857 as real term-side force |
| exhibit list | **AMENDED — in both directions** |
| `I2` spots | **CONFIRMED** — and the S1 `PARTIAL` matches my own 864 independently |
| path | **CONFIRMED** |
| `JOIN_UNIQUE` | **CONFIRMED — by two independent routes** |

**The positive hunt came back empty, and emptily in a way that matters.** Across the three governing
sources — the Q_spec ledger, the status map, the principal-sealed re-anchored condition —
`summand` = 0, `additive` = 0, `Einstein-Hilbert` = 0, `Lagrangian` = 0. **Every** `term` hit is a
substring of another word: *inter*mediate-time (ledger), coun*terterm* (status map **slot 6**), and
*determin*ing / *determin*es / *determin*ation (status map **slot 13**). **Not one occurrence in slot
2's row.** The R-49 row carries none either.

**My independent contribution is the F1 resolution.** At 857 I flagged F1 — *"the complete
coupled-Hessian P2 execution failing to find a finite isolated stationary point"* — as *"real (T)-side
force,"* because a coupled Hessian looked like it needed gravity dynamics in the action. **R-49 keeps
F1 live, so it is the strongest surviving term-side candidate.** It resolves structural:

> **A coupled Hessian in `g`-directions requires `g`-DEPENDENCE, not a `g`-SUMMAND.** In the active
> form `Gamma_rest[Abar,G; source,record,g,gauge,edge]`, `g` is a **peer argument**, so second
> variation in `g`-directions is structurally posable **without any additive gravitational term**.

**The one element that could have demanded term-shape does not.** That is what makes this
confirmation independent rather than an echo of my 857.

---

## 1. TASK A(1) — THE STRUCTURAL RULING: **CONFIRMED**

### 1.1 The live condition, at its own bytes

The principal-sealed re-anchored condition demands, verbatim:

> *"a complete coupled gravitational action and gravitational quantum measure — **both conjuncts** …
> derived under the standing fences, with R-30's surviving falsifier F1 remaining live. **No route is
> privileged: any lawful route may discharge it.**"*

and it states its own substance-preservation: *"the object demanded is character-for-character the
same."*

**"Any lawful route may discharge it" is dispositive against a mandated representation.** A term-shaped
requirement would privilege exactly one route, which this sentence forbids.

### 1.2 The positive hunt — run as a hunt, reported at bytes

| governing source | `term` (standalone) | `summand` | `additive` | `Einstein-Hilbert` | `Lagrangian` |
|---|---|---|---|---|---|
| Q_spec ledger `7995f6fda75e7879` | **0** (1 substring: *inter*mediate-time) | 0 | 0 | 0 | 0 |
| slot-status map `c26daa7e9cde29b7` | **0** (4 substrings: coun*terterm* in **slot 6**; three *determin*\* in **slot 13**) | 0 | 0 | 0 | 0 |
| principal-sealed condition | **0** | 0 | 0 | 0 | 0 |
| R-49 row (`STAGE8_LANE_STATUS.md`) | **0** | 0 | 0 | 0 | 0 |

**No governing sentence demands term-shape, and none of the near-misses is even in slot 2's row.**

### 1.3 F1 — the strongest term-side candidate, resolved

At 857 I wrote that F1's coupled Hessian was *"real (T)-side force."* I now resolve it against me:

- A Hessian is a **second variation**. Second variation in `g`-directions exists iff the functional
  **depends on `g`** in a differentiable-in-the-record-native sense.
- The active decomposition places `g` as a **peer argument**:
  `Gamma_rest[Abar,G; source,record,g,gauge,edge]` — the same list position as source, record, gauge,
  edge, **none of which is an additive summand there either**.
- Therefore `δ²/δg²` is posable from argument-dependence alone. **F1 constrains `g`-dependence, not
  `g`-summand-hood.**

**This is the strongest structural confirmation available**, because it is the one place a term-shape
demand could have hidden and it is the place I myself had flagged.

`T10 = STRUCTURAL-ADMISSIBLE-CONFIRMED (bytes).`

**Carried unchanged, and it is the ruling's real content:** structural-admissible is **not**
permission. The displayed term-shaped object `S_grav,D` remains **DECLARED** with its mixed domain
open and its parent route convicted as an import; and exact termwise membership in `Gamma_rest`
remains `NO_VERDICT` for want of a decomposition theorem. **Nothing is discharged; a route is only
un-barred.**

---

## 2. TASK A(2) — THE EXHIBIT LIST: **AMENDED, IN BOTH DIRECTIONS**

I tested both directions rather than checking that each element is defensible.

**First, a count discrepancy the relay's own summary carries.** The relay describes *"the six-element
exhibit list (carrier, variation, coupling, boundary closure, no-outside, compatibility)."* **876's
actual table has EIGHT rows.** The six-element phrasing merges coupling with no-outside (876's row 3
carries both) and **drops two rows entirely**.

**UNDER-DEMANDING, as a SLOT-2 list.** The six-element summary omits **876's row 7, the separate
gravitational quantum measure (I2)**. The principal-sealed condition demands **"both conjuncts, per
the slot's own name."** An artifact satisfying the six-element list would discharge **T10 only** and
could be mistaken for discharging **slot 2**. **This is the more consequential of the two amendments**,
because it is exactly the conflation Codex V2 §1.3 warned about (*"T10 is only the action half"*) and
that I confirmed at 857.

**PART OVER-DEMANDING, as a T10-only list.** 876's row 4 requires a *"physical
measure/contour/boundary/domain package."* An **action** is a functional and needs a **domain**;
**measure and contour are amplitude-side objects** belonging to the measure conjunct and the CTP
amplitude, not to the action. **Row 4's boundary/domain content is properly demanded of T10; its
measure/contour content is not.**

`EXHIBIT_LIST = AMENDED.` Complete for T10 once row 4's measure/contour content is moved to the
measure conjunct; **under-demanding for slot 2 until the I2 row is restored to any six-element
restatement.**

---

## 3. TASK B — I2 SPOTS, PATH, JOIN

### 3.1 `I2_SPOTS = CONFIRMED`

| spot | check |
|---|---|
| intrinsic flat-cell measure — `MATERIAL-PRESENT` | **CONFIRMED.** R3.3 classifies `mu_D(A) = Vol_4(A)/Vol_4(D)` in a stated positive absolutely-continuous class, with `uniform_flat_cell_measure_derived = true`. Present, and correctly scoped to the **flat primitive diamond** |
| quasi-local state — `MATERIAL-PRESENT` | **CONFIRMED, and the scope caveat is the right one**: 876 says *"this is a state, not a gravitational path measure."* That is the same object I used at 875 — and it is carried there as a **state on the algebraic limit**, not a measure. **Consistent across both artifacts** |
| free-source spectral measure — `PRESENT at free scope / PARTIAL for I2` | **CONFIRMED.** `mu_psi(B) = ⟨psi, P_h0(B) psi⟩` is state-**dependent** and expressly does not claim one state-independent profile. The split classification is correct, not a hedge |
| projective base family `λ_N` — `PARTIAL` | **CONFIRMED, AND IT MATCHES MY OWN 864 INDEPENDENTLY.** The required square `dν_N = A_N dλ_N`, `(π_MN)_*ν_M = ν_N` is displayed; **no inhabited base family is generated**, and intrinsic Haar is **present-and-refuted**. My 864 reached the identical split from a different starting point |

### 3.2 `PATH = CONFIRMED`

Dependency order sound; no double-counting detected in the roots I resolved. **The supply points are
correctly limited**, and the row that matters most is right: root 8 (gravity / `Delta_Gamma`) reads
**"NO term-selection act is needed under the T10 ruling"**, with a principal act required *only if an
unresolved structural family survives*. **That is the correct consequence of §1** — a structural
ruling must not create a term-selection supply point, and it does not.

Root 9 (I2 leaves) likewise records **"NO automatic principal act"**, with any surviving
non-equivalent measure/contour family to be **surfaced before a ruling, never chosen in-lane**. **That
is the void condition correctly installed in a path document.**

**Disclosed:** I resolved roots 8, 9, 10 and the supply-point column; **I did not individually
re-resolve all fifteen roots**. That is the same thinness I disclosed at 858, and I disclose it again
rather than letting `CONFIRMED` imply uniform depth.

### 3.3 `JOIN_UNIQUE = CONFIRMED — by two independent routes`

**Route 1 (Codex's, checked):** a six-family content sweep over the declared `C_JOIN` closure returns
gravitational content **only** at slot 2 — the F0 source-requirement artifact and the status map name
the same slot-2 object; the governing plan's two hits are a historical status mention and the cutover
declaration, neither an extra consumer. **The positive chain is displayed**: `kappa_Thomson` requires
complete `Q_spec` → complete `Q_spec` includes the eighteen slots → slot 2 is the gravitational pair →
`kappa_Thomson` is on `K_*`'s alpha path.

**Route 2 (mine, and Codex did not use it): the plan's own typed exclusion.** The state brief seals
**Q-34: *"alpha's stiffness is `K_*`, NOT `kappa_record`."*** `kappa_record` is the gravity-side
coupling. **A sealed typing therefore puts the gravitational coupling off alpha's path directly**,
independently of any content sweep.

**Two routes, different in kind — a sweep over a declared closure, and a typed exclusion — agreeing.**
That is worth more than either alone, and it is why I ran the second.

**Law 9b, closure-first, and the honest limit.** My alternative-family probe (`ell_P`, `l_P`, `Planck
length`, `Newton`, `kappa_5`, `radion`, `kappa_record` — all **outside** Codex's six) returns heavy
co-occurrence with alpha-path vocabulary (`kappa_record` alone: 268 files). **Co-occurrence is not a
join**, and Q-34 explains the volume: the corpus is repeatedly *drawing the distinction*, not creating
consumers. **But I did not per-file resolve those 268.** My confirmation rests on the declared closure
plus the typed exclusion, **not** on an exhaustive corpus resolution, and I state that rather than
claiming a completeness I did not execute.

---

## 4. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: the principal-sealed condition verbatim, INCLUDING "both conjuncts" and "any lawful
  route"; F1 as LIVE; slot 2 as OPEN and REQUIRED; S_grav,D as DECLARED with its domain open and its
  parent route convicted; termwise membership in Gamma_rest as NO_VERDICT; the I2 classifications at
  their stated scopes, including the flat-cell measure's FLAT scope and the free-source measure's
  state-DEPENDENCE; Q-34's typing; my own 857 verdict carried as a PRIOR UNDER TEST, given no
  exculpatory weight toward itself.

DERIVED HERE: (a) the positive hunt and its substring resolution — every "term" hit is intermediate /
  counterterm (slot 6) / determin* (slot 13), NONE in slot 2's row; (b) THE F1 RESOLUTION — a coupled
  Hessian demands g-DEPENDENCE, not a g-SUMMAND, and g is a peer argument of Gamma_rest; (c) the
  eight-versus-six count discrepancy and the two-directional amendment; (d) Route 2 for JOIN_UNIQUE
  from Q-34's typed exclusion.

SELECTED HERE: NOTHING.  No gravitational action, term, form, measure, contour, route, or coefficient
  is chosen, preferred, adopted, or discharged.  NO SLOT MOVES.  NO FLAG MOVES.  A route is un-barred;
  nothing is built.

NOT DONE AND DISCLOSED: I did not individually re-resolve all fifteen roots (8, 9, 10 and the
  supply-point column only).  My JOIN_UNIQUE rests on the declared closure plus a typed exclusion, NOT
  on per-file resolution of the 268 kappa_record co-occurrences.  I did not audit 876's C_T10 / C_I2 /
  C_JOIN closure tables member-by-member; I verified the subject's seal and re-derived the decisive
  rulings from primary sources.

SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at the ruling.** `STRUCTURAL-ADMISSIBLE` is one word away from
reading as permission, and an assembly lane could take it as licence to write a structural gravity
object. **The ruling un-bars a route and discharges nothing**, and §1.3 keeps `S_grav,D` at `DECLARED`
and termwise membership at `NO_VERDICT` precisely so the un-barring cannot be cashed.
**S12** — every status carried as the status it is; `NO_VERDICT` as a refusal; `DECLARED` never
drifting to derived.
**S26 / S08 / S19 / S24** untouched — the flat-cell measure is carried **at flat scope** and not
promoted toward the interacting spectral measure.
**T1 / T5** untouched.
**BR-1 HELD IN BOTH DIRECTIONS.** 876 is a producer-declared object and got no weight toward itself —
the ruling was re-derived from the principal condition and the Q_spec sources. **And my own 857 is
also a producer-declared object**, so it got none either: the hunt was run **as a hunt**, and the F1
element I had flagged was pressed rather than quietly dropped.

---

## 5. FINAL LINES

```text
T10 = STRUCTURAL-ADMISSIBLE-CONFIRMED (bytes).  POSITIVE HUNT RUN AS A HUNT, NOT AS A CONFIRMATION:
  across the Q_spec ledger, the slot-status map, the principal-sealed re-anchored condition, and the
  R-49 row, "summand" = 0, "additive" = 0, "Einstein-Hilbert" = 0, "Lagrangian" = 0, and EVERY "term"
  hit is a SUBSTRING of another word — inter-MEDIATE-time (ledger), coun-TERTERM (status map SLOT 6),
  and deTERMining / deTERMines / deTERMination (status map SLOT 13).  NOT ONE OCCURRENCE IN SLOT 2's
  ROW.  The live condition's "ANY LAWFUL ROUTE MAY DISCHARGE IT" is dispositive against a mandated
  representation.  MY INDEPENDENT CONTRIBUTION IS THE F1 RESOLUTION: at 857 I MYSELF flagged F1's
  coupled Hessian as "real (T)-side force", and R-49 keeps F1 LIVE, so it is the strongest surviving
  term-side candidate.  IT RESOLVES STRUCTURAL — a coupled Hessian in g-directions requires
  g-DEPENDENCE, not a g-SUMMAND, and g is a PEER ARGUMENT of Gamma_rest[Abar,G; source,record,g,gauge,
  edge], the same list position as source/record/gauge/edge, none of which is an additive summand
  there either.  THE ONE PLACE A TERM-SHAPE DEMAND COULD HAVE HIDDEN, AND THE PLACE I HAD FLAGGED,
  DOES NOT DEMAND ONE.  CARRIED: structural-admissible is NOT permission — S_grav,D stays DECLARED
  with its domain open and its parent route convicted, and termwise membership in Gamma_rest stays
  NO_VERDICT.  A ROUTE IS UN-BARRED; NOTHING IS DISCHARGED.
EXHIBIT_LIST = AMENDED, IN BOTH DIRECTIONS, tested both ways rather than element-by-element.  COUNT
  DISCREPANCY FIRST: the relay describes a SIX-element list; 876's actual table has EIGHT rows, the
  six-element phrasing merging coupling with no-outside and DROPPING TWO ROWS.  UNDER-DEMANDING AS A
  SLOT-2 LIST — it omits 876's row 7, THE SEPARATE GRAVITATIONAL QUANTUM MEASURE (I2), while the
  principal-sealed condition demands "BOTH CONJUNCTS, per the slot's own name"; an artifact satisfying
  the six-element list would discharge T10 ONLY and could be mistaken for discharging SLOT 2 — the
  exact conflation Codex V2 §1.3 warned of and I confirmed at 857.  THIS IS THE MORE CONSEQUENTIAL
  AMENDMENT.  PART OVER-DEMANDING AS A T10-ONLY LIST — row 4 requires a "physical measure/contour/
  boundary/domain package", but AN ACTION IS A FUNCTIONAL AND NEEDS A DOMAIN, WHILE MEASURE AND
  CONTOUR ARE AMPLITUDE-SIDE OBJECTS belonging to the measure conjunct and the CTP amplitude.  Row 4's
  boundary/domain content is properly demanded of T10; its measure/contour content is not.
I2_SPOTS = CONFIRMED.  Intrinsic flat-cell measure PRESENT, correctly scoped to the FLAT primitive
  diamond (mu_D(A) = Vol_4(A)/Vol_4(D), uniform_flat_cell_measure_derived = true).  Quasi-local state
  PRESENT with the right caveat — "a state, NOT a gravitational path measure" — and it is the SAME
  object I used at 875, carried there as a state on the algebraic limit: CONSISTENT ACROSS BOTH
  ARTIFACTS.  Free-source spectral measure PRESENT AT FREE SCOPE / PARTIAL FOR I2, correctly split
  because mu_psi(B) = <psi, P_h0(B) psi> is STATE-DEPENDENT and claims no state-independent profile.
  Projective base family lambda_N PARTIAL — the required square dnu_N = A_N d lambda_N,
  (pi_MN)_* nu_M = nu_N is displayed, NO inhabited base family is generated, and intrinsic Haar is
  PRESENT-AND-REFUTED: THIS MATCHES MY OWN 864 INDEPENDENTLY, reached from a different starting point.
PATH = CONFIRMED.  Dependency order sound and no double-counting in the roots I resolved.  THE ROW
  THAT MATTERS MOST IS RIGHT: root 8 (gravity / Delta_Gamma) reads "NO term-selection act is needed
  under the T10 ruling", with a principal act required ONLY IF an unresolved structural family
  survives — the correct consequence of the ruling, since a structural typing must not create a
  term-selection supply point.  Root 9 records "NO automatic principal act", with any surviving
  non-equivalent measure/contour family to be SURFACED BEFORE A RULING, NEVER CHOSEN IN-LANE — the
  void condition correctly installed in a path document.  DISCLOSED: I resolved roots 8, 9, 10 and the
  supply-point column, NOT all fifteen individually — the same thinness I disclosed at 858, disclosed
  again rather than letting CONFIRMED imply uniform depth.
JOIN_UNIQUE = CONFIRMED, BY TWO INDEPENDENT ROUTES.  ROUTE 1 (Codex's, checked): the six-family sweep
  over the declared C_JOIN closure returns gravitational content ONLY at slot 2, with the positive
  chain displayed — kappa_Thomson requires complete Q_spec, complete Q_spec includes the eighteen
  slots, slot 2 is the gravitational pair, kappa_Thomson is on K_*'s alpha path.  ROUTE 2 (MINE, AND
  CODEX DID NOT USE IT): the plan's OWN TYPED EXCLUSION — Q-34 seals "alpha's stiffness is K_*, NOT
  kappa_record", and kappa_record is the gravity-side coupling, SO A SEALED TYPING PUTS THE
  GRAVITATIONAL COUPLING OFF ALPHA'S PATH INDEPENDENTLY OF ANY CONTENT SWEEP.  TWO ROUTES DIFFERENT IN
  KIND AGREEING.  LAW 9b CLOSURE-FIRST AND THE HONEST LIMIT: my alternative-family probe (ell_P, l_P,
  Planck length, Newton, kappa_5, radion, kappa_record — ALL OUTSIDE Codex's six) returns heavy
  co-occurrence with alpha-path vocabulary (kappa_record alone: 268 files); CO-OCCURRENCE IS NOT A
  JOIN, and Q-34 explains the volume as the corpus repeatedly DRAWING THE DISTINCTION.  BUT I DID NOT
  PER-FILE RESOLVE THOSE 268, and my confirmation rests on the declared closure plus the typed
  exclusion, NOT on exhaustive corpus resolution.
CHAIN_INVOKED = false
VERB_AUDIT_SELF = NOT CLEAN (+3):
  (1) I CROSS-CHECKED A RULING THAT AGREES WITH MY OWN 857, WHICH IS ONE LANE COUNTED TWICE UNLESS THE
      CHECK IS BUILT AGAINST THAT.  I named it at pickup and built the check accordingly: the hunt was
      run AS A POSITIVE HUNT for a demanding sentence, and the element I MYSELF had flagged as
      term-side force (F1) was PRESSED rather than quietly dropped.  That F1 resolves structural is
      the part of this artifact that is genuinely independent of 857; everything else would have been
      an echo.
  (2) I AMENDED THE RELAY'S OWN SIX-ELEMENT SUMMARY, NOT ONLY THE SUBJECT.  876's table has EIGHT
      rows; the relay's compression drops the I2 conjunct, which the principal-sealed condition
      expressly requires.  Correcting the instruction I was handed is the right move but not a
      comfortable one, and I record that the under-demand I found lives in the RELAY'S phrasing rather
      than in the artifact under check.
  (3) THREE OF FIVE HEADLINE ITEMS CAME BACK CONFIRMED AND THE TWO AMENDMENTS ARE SCOPE CORRECTIONS,
      NOT DEFECTS.  A cross-check that finds only scope corrections should be read as a WEAK check
      unless its coverage is stated — so: fifteen roots resolved at three, 268 co-occurrences resolved
      at zero, closure tables not audited member-by-member.  The decisive items (the ruling, F1, the
      exhibit list, the join) were re-derived from PRIMARY SOURCES; the rest was spot-resolved, AND I
      DO NOT LET "CONFIRMED" IMPLY UNIFORM DEPTH.
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** Charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-13
pointer-only, none opened or consulted. Builder-B independence held. `~/.codex` untouched;
memory-bank never searched. No register, plan, tracker, or git action.
