# STAGE 8 TASK 5 / EQ6 — ACTUAL Q-408 PRIMITIVE-GENERATOR EXHIBIT — LANE 2 V001

Date: 2026-08-04  
Lane: Codex Lane 2  
Task: 5 / `B_Q408_REFINEMENT`  
Custody: generator-data exhibit for the conditionally adopted DoR-020 package; cross-check required

## Lead result

```text
GENERATORS = EXHIBITED_AS_ONE_COVARIANT_ACTUAL_FAMILY_PER_SAME_REGION_ORBIT
COMPOSITION = PROVEN_ON_THE_FREE_REFINEMENT_CATEGORY
REFINEMENT = PROVEN_ON_CANONICAL_COMMON_REFINEMENT_DIAMONDS

B_Q408_REFINEMENT_GENERATOR_DATA = PROPOSED_FOR_CROSS_CHECK
B_Q408_REFINEMENT_JOINT_EQ6_DISCHARGE = not_claimed
EQ6_MEMBER_BOUND = false
FIXED_POINT_EXECUTED = false
END_TEST_EXECUTED = false
```

The exhibit is an **actual physical realization**, not an abstract operator
substitution.  For each same-region primitive-refinement orbit it declares a
covariant orbit of tame, edge-separating physical cell carriers.  Each carrier
has an actual embedded one-skeleton, a full coframe torsor, its positive density,
and connection/curvature data.  Every refined edge is the image of a real PL
edge in that carrier; test-form and conserved-current maps are then the actual
Q-408 line-integral maps.

Only the physical carrier datum is authored.  Once it is fixed as a covariant
orbit, the new paths, analysis maps, current transports, coframe/density
transports, connection/curvature transports, free-category composites, and
common-refinement diamonds below are forced.  No representative coframe,
background, edge, orientation, cycle basis, filtration, reader, response, or
EQ6 member is selected.

This exhibit inhabits the `B_Q408_REFINEMENT` **generator-data carrier**.  It
does not prove the other five roots, their J1--J15 equalizer, or a bound package
member.  Therefore DoR-020's execution prohibitions remain in force.

---

## 0. Preflight, authority, and register sweep

### 0.1 Three-line preflight

```text
DOES THE OBJECT EXIST?  The carrier and its generator maps are constructed below.
IS THE VERSION CURRENT? YES: built through Q-424; send-time reconciled through Q-425.
ARE ITS INPUTS PRESENT? YES: Q-408, the Ref_path subpackage, V005, DoR-016,
                        DoR-020, and the Q-424 frontier are hash-verified.
```

Preflight authority hashes:

| Authority | SHA-256 | Use |
|---|---|---|
| locked process | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` | lane custody |
| Q-424 frontier map | `b8294e917531dd4f1731aedd54fd1c52a320840335061bbb4ab872830d001028` | exact 1A route |
| Q-423 witness hunt | `452f1bb87aeb7f7bfe4ab4556134cf723ab6df697f93c1a59c33671146cb0083` | built `Ref_path` boundary |
| continuum package V005 | `09883a0d7a5d1329033a09c59ff8d7c843a0098cba569d2a2b97f64f45b2dd95` | `X4(R)` and partial P4 rule |
| continuum constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | J10--J15 interfaces |
| Q-408 physical kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | actual analysis/kernel maps |
| DoR-016 / network law V004 | `69f4d93b9f84075a3112fb011b9838f380fb8c0341610572170cf7a13d5aed08` | doubled/reality/batching discipline |
| DoR-020 decision | `bead32b7a12dd70b48a5ddd729395f23914dce06047783226a33d3ccb1eafa31` | conditional adoption and prohibitions |
| minimal-six theorem | `288a3d4147cb10f2e51180b21f7c6b2b4749503504c57b8a1121e3925c70c94c` | independence of the other roots |

All named artifact seals passed before construction.  The supervision
register SHA-256 at write-time preflight was
`4678020faa00a03b0916215dd825a40d8d7b38ff7ba4c9936f5a9364e1fa264f`,
with Q-424 as its last entry.

### 0.2 Register sweep

The following settled entries were checked before the exhibit:

```text
Q-408  actual finite path-current/bilocal kernel and Ward calculus;
Q-410  finite realization skeleton does not manufacture geometric refinement;
Q-411  J1--J15 form a joint equalizer;
Q-415  abstract kernel, circular reader, and false nonemptiness witness killed;
Q-418  rank-preserving physical path subdivision square derived;
Q-420  actual primitive physical realization is the first P4 stop;
Q-421  six roots are independent and jointly constrained;
Q-422  DoR-020 conditionally adopts the package and forbids member execution;
Q-423  finite/Ref_path current-kernel subpackages built;
Q-424  B_Q408_REFINEMENT is new-physics member data with the exhibit route;
Q-425  the parallel C1 build stops at the full P4 core plus two C1-local data.
```

No prior entry exhibits an actual new-edge physical cell carrier.  Thus this
artifact does not duplicate an existing synonym.

### 0.3 Send-time Q-425 reconciliation

Q-425 registered while this lane was completing custody.  Its sealed artifact,
`STAGE8_TASK5_EQ6_C1_COMPLETION_BUILD_LANE1_V001.md`, verified at
`5203347c9bba7f99c2f033467815defa7ac047f74ea73b313972f44975d250a5`.
It independently stops C1 at

```text
FULL_P4_CORE + CROSS_ORBIT_BOUNDED_GEOMETRY + HILBERTIZABLE_W5.
```

That result does not supersede this exhibit.  It makes the actual P4 core
built here a named C1 input.  Conversely, this artifact does not claim the
cross-orbit uniform estimates or the positive covariant Hilbertizing weight;
those remain C1-local after the handoff.  The two artifacts are therefore
type-compatible, and no construction clause below changes on reconciliation.

```text
SEND_TIME_REGISTER_HEAD = Q-425
Q425_COMPATIBILITY = PASS
Q425_RESULT_CONSUMED_DURING_CONSTRUCTION = false
```

### 0.4 Load-bearing symbol distinctions

```text
Ref_path       = subdivision of an already realized path;
Ref_phys       = physical refinement category constructed below;
Ref_free       = free category on primitive Ref_phys generators;
same-region    = two cellulations of the same declared physical carrier;

Gamma_R        = actual refined-edge path family, not a graph edge label;
P_R            = physical test-form transport;
S_R            = cellular conserved-current transport;
A_G            = actual Q-408 line-integral analysis map;
Kern_G^fin     = actual Q-408 finite bilocal distribution kernel;

Cof_R          = coframe-torsor transport, not a selected frame;
Dens_R         = positive density transport, not a fitted coefficient;
eta_conn,R     = connection transport;
eta_curv,R     = curvature transport;

primitive generator != composite arrow;
generator-data family != bound EQ6 member;
coframe torsor != coframe representative;
algebraic cycle != closed timelike path.
```

---

## 1. T1 — the actual primitive generator

### 1.1 The authored physical carrier datum

Let `O_reg` denote the set of isomorphism classes of same-region components
of the proposed PL refinement category.  For every `o in O_reg`, declare the
following **carrier orbit**, not a representative:

```text
Q_o = [
  (X_o, N_o^CTP, iota_o^CTP, h_o^CTP, E_o^CTP,
   mu_o^CTP, nabla_o^CTP, F_o^CTP)
]_Cov.                                                    (T1-1)
```

Formally, `(T1-1)` denotes the action groupoid `Q_o`, not the set of its
representatives.  Its objects are the displayed actual tuples and its arrows
are admitted pushforwards, frame changes, branch reality maps, and
simultaneous relabelings.  For every realization morphism `f`, the assignment
`q |-> f_*q` is the family map.  A stabilizer therefore acts internally on
`Q_o`; it is not required to fix a chart or an edge representative.  This is
the same family-covariance discipline that excluded a rank-two
cycle-selective scalar, now applied before any scalar contraction.  Thus
`Q_o` is one covariant generator object per orbit without disguising a
non-natural representative as a section.

Its terms have these concrete meanings.

1. `X_o` is the finite PL carrier shared by all cellulations in the
   same-region component `o`.
2. `N_o^CTP=N_(o,+) disjoint-union N_(o,-)` is the ratified doubled CTP
   carrier, with the sealed endpoint gluing kept as boundary data.
3. `iota_o^CTP` is a tame piecewise-smooth embedding of the complete
   branch-marked one-skeleton into an actual member of the ratified A1/A2
   physical-background family.  Open edge interiors are disjoint; incidences
   are exactly the declared incidences; the sealed E_post/CTP word assigns
   each oriented edge to its admissible branch.
4. Each `N_(o,+/-)` is a tubular physical neighborhood of the embedded
   branch one-skeleton.
5. `h_o^CTP` is a positive carrier coframe Gram field on those neighborhoods;
   it is new
   carrier geometry and is not a response block or spacetime-member choice.
6. `E_o^CTP=Fr^+_(h_o^CTP)(T^*N_o^CTP)` is the full oriented
   `h_o^CTP`-orthonormal coframe
   torsor.  No section of this torsor is part of `(T1-1)`.
7. `mu_o^CTP=vol_(h_o^CTP)` is the positive density induced by that Gram field.
   Oriented orthonormal coframe changes have determinant one, so they change
   local components but not `mu_o^CTP`.
8. `nabla_o^CTP` is the Levi-Civita connection of `h_o^CTP` on the coframe
   carrier, together with the **full** ratified U(1) connection affine space
   on `N_o^CTP`; no U(1) connection value is selected.
9. `F_o^CTP` is the corresponding curvature operator (`nabla^2` on the
   coframe carrier and `A |-> dA` in the U(1) affine direction), globally
   defined rather than represented by a selected local potential.
10. `Cov` quotients simultaneous relabeling and retains the full pushforward
   family under admitted realization morphisms, orientation/reality, and
   endpoint-frame changes.

This is an actual object.  A finite graph embeds tamely in a coordinate ball
of every admitted four-dimensional smooth background.  A tubular
neighborhood of that embedded graph is carried inside the coordinate ball;
the oriented coframe torsor and a positive density are nonempty there.
Metric-compatible connections form a nonempty affine carrier and curvature
is then defined.  Since the datum retained is the entire covariance orbit,
this existence proof chooses no background member, chart, edge, or coframe
representative.

The E_post requirement is not an extra ordering choice.  The sealed doubled
CTP word distinguishes forward and backward contour branches.  An algebraic
incidence cycle therefore need not be a closed timelike path in one copy of
spacetime: each edge is E_post-admissible on its assigned branch, and the CTP
endpoint glue closes the relative history.  An orbit lacking the sealed
branch assignment is outside the DoR-009/DoR-016 admissible class and is not
silently repaired here.

```text
ACTUAL_CARRIER_FAMILY_NONEMPTY = true / PROPOSAL-CONDITIONAL CONSTRUCTION
BACKGROUND_MEMBER_SELECTED = false
COFRAME_MEMBER_SELECTED = false
FRAME_MEMBER_SELECTED = false
```

### 1.2 Every stage is a cellulation of the same actual carrier

For `G` in component `o`, let

```text
r_G:|G| -> X_o                                      (T1-2)
```

be its PL realization homeomorphism.  Define every edge path by

```text
gamma_(G,e)(t)
 =iota_(o,epsilon(e))(r_G(|e|(t))), 0<=t<=1,        (T1-3)
```

where `epsilon(e) in {+,-}` is the sealed CTP branch word, not a selected
branch.

Thus a new edge is not renamed combinatorics: `(T1-3)` is a genuine path in
the adopted physical background.  Distinct open edges have disjoint tubular
sub-neighborhoods.  On the same physical neighborhood use

```text
T_o=Omega_c^1(N_(o,+);iR) direct-sum Omega_c^1(N_(o,-);iR),

<J_(G,c),a>
  =sum_(e in E(G)) c_e integral_(gamma_(G,e)) a,

A_G:T_o -> K_G^*,
(A_G a)(c)=<J_(G,c),a>,

K_G=ker(B_G^T).                                    (T1-4)
```

These are precisely Q-408's actual paths, test forms, conserved currents, and
analysis maps.  Conservation is still exact:

```text
<partial J_(G,c),varphi>
 =<J_(G,c),d varphi>
 =<B_G^T c,varphi>
 =0.                                               (T1-5)
```

The edge-separating condition makes `A_G` onto.  Indeed, choose a test
one-form supported in the interior tube of one edge and normalized to have
unit line integral there.  These test forms give all edge-coordinate
functionals, hence their restrictions give every element of `K_G^*`.
Therefore

```text
A_G is surjective,
Kern_G^fin is faithful,
no nonzero record-visible cycle is deleted.        (T1-6)
```

This is the exact place where the prior zero-new-edge candidate failed.  New
cycle directions exist at `G` and are separated by actual local test forms;
they are not inferred from an operator dimension.

### 1.3 Primitive-arrow tuple

Let `R:G->G'` be a primitive refinement in component `o`.  Since `G` and
`G'` triangulate the same `X_o`, define the cellular subdivision map

```text
s_R:C_1(G)->C_1(G')                                (T1-7)
```

by sending an oriented coarse edge to the signed sum of the oriented refined
edge chain representing the same physical path.  It satisfies

```text
partial_(G') s_R=s_R^0 partial_G,
s_R(K_G) subset K_(G').                            (T1-8)
```

Both stages use the same physical test space, so

```text
P_R=id_(T_o),
S_R=s_R|_(K_G).                                    (T1-9)
```

Line-integral additivity gives the required actual Q-408 square:

```text
A_G=S_R^* A_(G') P_R.                              (T1-10)
```

The DoR-019 semiform is the A4 pairing of the actual currents.  Since
`J_(G,c)=J_(G',S_R c)` as distributions on the same carrier, its finite
restriction is exact even when `G'` has additional cycle directions:

```text
R_(K,G)=S_R^* R_(K,G') S_R.                       (T1-10a)
```

This is an equality on the old current image, not an assertion that the new
orthogonal cycle sector is absent.

Let `S_R^dagger` be the finite metric left inverse determined by the
ratified positive current metrics:

```text
S_R^dagger
 =(S_R^* R_(K,G') S_R)^(-1) S_R^* R_(K,G'),
S_R^dagger S_R=id_(K_G).                           (T1-11)
```

Fullness and positivity make `(T1-11)` well defined.  The old-cylinder
profile inclusion is

```text
(j_R^H H)(x',y')
 =H(S_R^dagger x',S_R^dagger y').                 (T1-12)
```

It vanishes only on the orthogonal complement **of the embedded old
cylinder**.  It does not set the new-stage physical profile carrier to zero;
all new `H'` remain independently present.  With the restriction
`rho_R^H(H')=S_R^*H'S_R`, Q-408 gives

```text
rho_R^H j_R^H=id,
Kern_G^fin(rho_R^H H')
  =Res_R Kern_(G')^fin(H'),
Kern_G^fin(H)
  =Res_R Kern_(G')^fin(j_R^H H).                  (T1-13)
```

The full primitive generator is now the concrete tuple

```text
x_R=(Gamma_R,P_R,S_R,Cof_R,Dens_R,
     eta_conn,R,eta_curv,R),                      (T1-14)
```

where

```text
Gamma_R     ={gamma_(G',e') from (T1-3)}_(e' in E(G'));
Cof_R       =pullback/restriction of (h_o^CTP,E_o^CTP)
              along the PL subdivision;
Dens_R      =pullback/restriction of mu_o^CTP;
eta_conn,R  =restriction of nabla_o^CTP and of the U(1) connection affine space;
eta_curv,R  =restriction of F_o^CTP, with F(nabla)=nabla^2.   (T1-15)
```

`(T1-14)` is in V005's `X4(R)`: it contains actual paths, not only the maps
`P_R,S_R`; `(T1-10)` and `(T1-13)` are the two hostile-check squares.

### 1.4 Ref_path and new-edge ledger

The construction is uniform over all primitive-arrow isomorphism classes.
The ledger below states how each orbit type is inhabited.

| Primitive orbit type | Actual path data | Test/current data | Geometry data | Standing |
|---|---|---|---|---|
| identity | unchanged `gamma`; `P=S=id` | unchanged `A,J,Kern` | all transports identity | exhibited |
| signed isomorphism/relabeling | paths pushed forward and relabeled | induced signed permutation on currents | torsor/density/connection/curvature pushforward | exhibited covariantly |
| `Ref_path` subdivision | actual subpaths of the old `gamma` | Q-418 chain subdivision and `P=id` | restrictions of `E_o^CTP,mu_o^CTP,nabla_o^CTP,F_o^CTP` | fills the four prior `Ref_path` geometry slots |
| new interior edge | `(T1-3)` in the actual cell carrier | new edge-local test forms and full `K_(G')` | inherited carrier geometry | exhibited; not a formal edge |
| cycle-creating refinement | all new paths in `(T1-3)` | new cycles separated by `(T1-6)` | inherited carrier geometry | exhibited; no upward inverse asserted |
| orientation/reality mate | path orientation and CTP branch transformed | current sign/conjugation transformed | density even; connection/curvature conjugated | exhibited as the same covariant family |
| common-refinement leg | subdivision of the common actual carrier | cellular chain map and Q-408 restriction | pullback of the same master geometry | exhibited; Section 3 proves the diamond |

Because every primitive belongs to one of these isomorphism-orbit forms,
`(T1-14)` is a per-orbit exhibit, not a schema with an empty instance slot.

### 1.5 Covariance, reality, units, and restriction certificates

For an admitted realization morphism `f`, push forward the whole carrier
tuple:

```text
f_* gamma_(G,e)=gamma_(fG,fe),
f_* J_(G,c)=J_(fG,fc),
A_(fG) f_*=(f_* on K)^(-*) A_G,
f_* h_o^CTP=h_(fo)^CTP,
f_* E_o^CTP=E_(fo)^CTP,
f_* mu_o^CTP=mu_(fo)^CTP,
f_* nabla_o^CTP=nabla_(fo)^CTP,
f_* F_o^CTP=F_(fo)^CTP.                           (T1-16)
```

Thus edge exchanges act by permutation, orientation reversal supplies the
current sign, and simultaneous relabeling changes only a presentation.  A
bilocal kernel contains two current factors, so the orientation signs occur
twice.  The sealed CTP involution conjugates the connection and curvature and
leaves the positive density even.  No frame representative appears.

Unit classes are explicit:

```text
[coframe]=U_L,
[mu_o^CTP]=U_L^(dim N_o^CTP),
[connection]=U_L^(-1),
[curvature]=U_L^(-2),
[integral_gamma a]=the sealed dimensionless U(1) phase class. (T1-17)
```

`P_R,S_R` carry no conversion unit.  `R_(K,G)` and `(T1-11)` use only the
ratified R4 unit seam.  No C/K cross-sector conversion is introduced.

For every sealed finite stage, restriction literally returns the same paths,
line integrals, incidence currents, and Q-408 kernel.  Hence reciprocal-loop,
S8-A, Q-243, Q-279, and Q-309 bottom legs are unchanged.  Nothing is evaluated.

```text
Q408_ACTUAL_PATH_CERTIFICATE = PASS_WITHIN_EXHIBIT
Q408_ANALYSIS_SQUARE = PASS_WITHIN_EXHIBIT
Q408_KERNEL_RESTRICTION = PASS_WITHIN_EXHIBIT
COFRAME_DENSITY_NATURALITY = PASS_WITHIN_EXHIBIT
CONNECTION_CURVATURE_NATURALITY = PASS_WITHIN_EXHIBIT
DOR008_FINITE_REPRODUCTION = PASS_WITHIN_EXHIBIT
```

### 1.6 Choice table — every authored datum

All alternatives remain recorded; none is chosen for a downstream result.

| Datum | Live declaration | Alternatives considered | Carrier-only reason | Void condition |
|---|---|---|---|---|
| same-region physical carrier | covariant orbit `(T1-1)` of tame edge-separating cell carriers | formal PL carrier; arbitrary independent paths; imported geodesic choice | smallest datum that makes every new edge an actual physical path and supports localized test forms | any admitted orbit has empty carrier fiber or fails E_post |
| representative discipline | full covariance orbit; no representative exposed | fixed chart/background; averaged path; selected frame | realizes the no-selection family as one quotient object | stabilizer changes a physical output instead of acting covariantly |
| primitive PL realization | all cellulations realize one `X_o`; new edges use `(T1-3)` | unrelated edge paths at each stage; boundary concatenation | forces restriction and common refinement before consumers | two presentations of one PL edge give different admitted line integrals |
| coframe datum | positive Gram field `h_o^CTP` and full oriented orthonormal torsor `E_o^CTP` | chosen coframe; no coframe; response-defined frame | supplies the requested positive geometry without a frame selection | torsor empty or transition cocycle fails |
| density | positive determinant density `mu_o^CTP` of the same coframe datum | independent density scale; counting density; fitted normalization | one density for J11, current normalization, and refinement | positivity, units, or composition fails |
| connection | Levi-Civita connection of `h_o^CTP` plus the full ratified U(1) affine space | selected local potential; unrelated connection per stage | the coframe connection is forced by the Gram field, while U(1) data stay a covariant affine carrier | gauge covariance or restriction fails |
| curvature | `F=nabla^2`, transported with the connection | independently authored curvature | Bianchi-compatible minimal curvature content | curvature is not the declared connection's curvature |
| test-form transport | `P_R=id` on the common physical neighborhood | arbitrary bounded operator | both cellulations live in the same actual field neighborhood | analysis square `(T1-10)` fails |
| current transport | cellular subdivision `S_R` | zero on new edges; arbitrary injection | boundary-commuting chain functor forced by refinement | `(T1-8)` or a finite restriction fails |
| profile cylinder inclusion | metric left inverse `(T1-11)` | selected complement; full zero profile | canonical old-cylinder embedding while retaining all new profiles | `rho_R^H j_R^H!=id` or units fail |
| common refinement | physical overlay followed by barycentric subdivision | chosen path through the diamond; independent remeshing | symmetric carrier-level construction, independent of response | either leg ceases to be a refinement of the same carrier |

The only genuinely authored row is the first physical carrier/coframe orbit;
the remaining rows are forced constructions or derived companions once it is
declared.  The table exposes rather than hides that new-physics cost.

### 1.7 DoR-016 rank-one profile service

For `x in K_G`, let the sealed rank-one profile be written basis-freely as

```text
H_x(v,w)=R_(K,G)(x,v) R_(K,G)(x,w).               (T1-18)
```

Substitution in Q-408's definition gives the actual physical kernel

```text
Kern_G^fin(H_x)[a,b]
 =<J_(G,x),a><J_(G,x),b>
 =J_(G,x) tensor J_(G,x)[a,b].                    (T1-19)
```

Thus the generator serves DoR-016's rank-one profiles with genuine path
currents.  Under restriction, `(T1-10a)` and `(T1-13)` carry `(T1-19)` to the
sealed finite profile.  New-stage rank-one profiles use the newly separated
currents from `(T1-6)`; none is supplied by abstract conjugation alone.

```text
DOR016_RANK_ONE_PROFILE_PHYSICAL_REALIZATION = PASS_WITHIN_EXHIBIT
```

---

## 2. T2 — free-category composition

### 2.1 Composition of primitive exhibits

Let

```text
G --R--> G' --S--> G''                            (T2-1)
```

be composable primitive exhibits in one same-region component.  Define the
composite by ordinary map composition:

```text
P_(S o R)=P_S P_R=id,
S_(S o R)=S_S S_R,
Cof_(S o R)=Cof_S Cof_R,
Dens_(S o R)=Dens_S Dens_R,
eta_conn,(S o R)=eta_conn,S eta_conn,R,
eta_curv,(S o R)=eta_curv,S eta_curv,R.           (T2-2)
```

The actual path family of the composite is the `G''` one-skeleton in the
same physical carrier, not a concatenation chosen after the fact.  Cellular
subdivision gives

```text
partial S_S S_R=S_S^0 S_R^0 partial,
A_G=(S_S S_R)^* A_(G'') (P_S P_R).                (T2-3)
```

The second identity follows by applying `(T1-10)` twice.  The kernel
restriction composes contravariantly:

```text
rho_(S o R)^H=rho_R^H rho_S^H,
Res_(S o R)=Res_R Res_S.                          (T2-4)
```

Therefore

```text
Kern_G^fin(rho_(S o R)^H H'')
 =Res_(S o R) Kern_(G'')^fin(H'').                (T2-5)
```

### 2.2 Category axioms on the exhibits

For every object `G`, the identity generator uses the unchanged cellulation,
so all seven entries of `(T1-14)` are identities.  Hence left and right unit
laws hold.

For three composable arrows, associativity of function, chain, pullback, and
restriction composition gives

```text
x_(T o (S o R))=x_((T o S) o R).                  (T2-6)
```

No relation among distinct words has been imposed at this step.  Thus the
primitive exhibits extend uniquely to a functor on `Ref_free`.  Reality,
orientation reversal, batching, and unit transport are natural
transformations because each is componentwise and commutes with `(T2-2)`.

```text
FREE_CATEGORY_IDENTITIES = PROVEN
FREE_CATEGORY_ASSOCIATIVITY = PROVEN
Q408_SQUARE_UNDER_COMPOSITION = PROVEN
COMPOSITION = PROVEN
```

---

## 3. T3 — common refinement

### 3.1 Canonical common carrier

Let `G_1` and `G_2` be two finite cellulations in the same-region component
`o`.  Their physical images lie in the same declared carrier `X_o`.  Form the
finite PL overlay of the two cellulations and then take its barycentric
subdivision:

```text
G_12=Sd(G_1 meet_Xo G_2).                         (T3-1)
```

The construction is invariant under exchanging `G_1,G_2`, relabeling, and
orientation reversal.  It supplies refinement legs

```text
G_1 --T_1--> G_12 <--T_2-- G_2.                   (T3-2)
```

Every edge of `G_12` is again realized by `(T1-3)` in the same actual
physical carrier.  Hence `(T3-2)` has the full tuple `(T1-14)` on both legs.

### 3.2 Diamond computation

Suppose `R_i:G_0->G_i` are two refinement paths from a common coarse stage.
Cellular subdivision of the same coarse chain into the common overlay gives

```text
S_(T_1) S_(R_1)=S_(T_2) S_(R_2),
P_(T_1) P_(R_1)=P_(T_2) P_(R_2)=id.              (T3-3)
```

Both paths pull back the same
`h_o^CTP,E_o^CTP,mu_o^CTP,nabla_o^CTP,F_o^CTP`, so

```text
Cof_(T_1) Cof_(R_1)=Cof_(T_2) Cof_(R_2),
Dens_(T_1) Dens_(R_1)=Dens_(T_2) Dens_(R_2),
eta_conn,(T_1) eta_conn,(R_1)
  =eta_conn,(T_2) eta_conn,(R_2),
eta_curv,(T_1) eta_curv,(R_1)
  =eta_curv,(T_2) eta_curv,(R_2).                 (T3-4)
```

Substituting `(T3-3)` into the analysis definition gives equal Q-408
pullbacks on every coarse current and test form.  Substituting into `(T1-13)`
gives equal finite kernel restrictions.  Thus the complete physical diamond
commutes—not merely its abstract operator projection.

### 3.3 Scope of the refinement theorem

The theorem covers every PL **refinement** arrow in the adopted proposed
category: cellulations are of one underlying same-region carrier, and their
common overlay is finite.  A topology-changing attachment is not a refinement
of the same carrier and is outside this theorem.  If such an arrow is later
admitted, it requires a new same-region declaration rather than being silently
called a subdivision.

The theorem supplies the geometric part of J10 and J11 for this generator
family.  It does not manufacture the response maps, boundary cocycle,
completion estimates, faithfulness horn, or Hodge data belonging to the
other five roots.

```text
COMMON_REFINEMENT_EXISTS = true / WITHIN_DECLARED_REFINEMENT_SCOPE
REFINEMENT_DIAMONDS_Q408 = PROVEN
REFINEMENT_DIAMONDS_GEOMETRY = PROVEN
J10_GEOMETRIC_PART = INHABITED_BY_EXHIBIT
J11_COFRAME_DENSITY_PART = INHABITED_BY_EXHIBIT
J12_RESPONSE_PART = not_claimed
REFINEMENT = PROVEN
```

---

## 4. T4 — falsifiers, regressions, and anti-tuning

### 4.1 DoR-008 finite falsifiers

| Finite check | Computation | Result |
|---|---|---|
| one-edge/tree | `K_G=0`; scalar cycle output remains zero while the actual path and test-form carrier remains present upstream | pass |
| reciprocal loop | old paths and line integrals are literally unchanged under restriction; its one cycle is retained | pass |
| S8-A | all conserved directions remain in `K_G`; exchange relabels the entire tuple and deletes none | pass |
| identity zero-extension | identity cells add no path-current contribution; restriction returns the sealed stage | pass |
| new cycle | edge-local form in `(T1-6)` pairs nontrivially with every nonzero cycle | pass |
| rank-preserving `Ref_path` | `(T1-10)` reduces exactly to Q-418's line-integral square | pass |
| cycle-creating restriction | only the old cylinder is compared; no inverse onto a new cycle is asserted | pass |

### 4.2 Five permanent regressions

| Permanent regression | Exercise on this exhibit | Verdict |
|---|---|---|
| abstract-kernel substitution | `Gamma_R` is given first by `(T1-3)`; `P_R,S_R,A_G,Kern` are derived afterward | **PASS** |
| circular `pi_Mx` | no reader, localization, response, or `pi_Mx` enters any definition | **PASS** |
| misstated reader nonemptiness | no reader inhabitance is claimed; it remains conditional on the other roots | **PASS** |
| Hodge from isometry | no closed-range, local-symbol, or Hodge conclusion is drawn | **PASS** |
| objectwise-minimum/restriction | no minimum is taken; one coherent carrier orbit generates every arrow before restriction | **PASS** |

The joint-equalizer regression is also retained: this root's exhibit does not
prove that its tuple is compatible with independently supplied R1, C1,
faithfulness, C2, or C3 data.

### 4.3 Additional hostile attacks

1. **Zero-new-edge attack.**  Refuted by `(T1-6)`: every new open edge has a
   local test form, and every nonzero new cycle is detected.
2. **Boundary-concatenation attack.**  No route along an old boundary is
   selected.  The new path is the image of its actual PL edge in `X_o`.
3. **Imported-geodesic attack.**  No geodesic, Cauchy slice, or target
   spacetime solution is chosen.  The authored object is exactly the
   carrier/coframe orbit commissioned as new physics.
4. **Rank-two selective-member attack.**  An edge exchange acts on the whole
   path/current/coframe tuple.  No cycle coordinate is fixed or suppressed.
5. **Frame attack.**  `E_o^CTP` is a torsor; local calculations may choose a
   representative, but all outputs descend under its transition action.
6. **Hidden-density scale attack.**  `mu_o^CTP` is the determinant density of the
   same coframe datum.  No independent scalar multiplier or response
   normalization is inserted.
7. **Diamond-path attack.**  Equations `(T3-3)`--`(T3-4)` compute both paths;
   the conclusion is not an assertion of common refinement.
8. **Topology-changing-edge attack.**  Such an attachment is explicitly not
   smuggled into the same-region theorem.
9. **Target-tuning attack.**  The carrier, barycentric overlay, and geometric
   transports are fixed before any reader, response, contraction modulus, or
   protected output exists.

No attack kills the exhibit at its stated generator-data scope.

### 4.4 Anti-tuning construction order

```text
1  Freeze Q-408's actual line-integral map, V005's X4(R), and J10--J15.
2  Freeze the same-region primitive-orbit set and the DoR-009 orientation.
3  Declare the actual tame physical carrier/coframe orbit Q_o.
4  Define all physical edge paths from the PL realization.
5  Derive test-form/current maps and prove the Q-408 square.
6  Derive coframe, density, connection, and curvature transports.
7  Prove free-category composition.
8  Construct and test the common-refinement overlay.
9  Run finite restrictions and hostile regressions.
10 Inspect no response, modulus, fixed point, end test, or measured datum.
```

```text
TARGET_TUNING_USED = false
ABSTRACT_STANDIN_USED = false
CIRCULAR_CERTIFICATE_USED = false
MEMBER_BINDING_USED = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
MACHINERY_APPEAL = false
```

---

## 5. Exact frontier after this exhibit

The artifact supplies one proof-carrying generator-data family for
`B_Q408_REFINEMENT`.  It does not certify the full DoR-020 condition:

| Root/interface | After this artifact |
|---|---|
| `B_Q408_REFINEMENT` actual primitive carrier and maps | **EXHIBITED / awaiting adversarial cross-check** |
| free composition and common refinement | **PROVEN on the exhibit** |
| `B_R1_NATURAL` | unchanged open root |
| `B_C1_COMPLETION` | Q-425 partial build awaits this full P4 core plus cross-orbit bounded geometry and Hilbertizable W5; neither C1-local datum is claimed here |
| `B_FAITHFULNESS` | unchanged, downstream of C1 |
| `B_C2_RESPONSE_BOUNDARY` | unchanged; this exhibit supplies only its geometric prerequisite |
| `B_C3_MAXWELL_HODGE` | unchanged, downstream of C1 and this geometry |
| joint J1--J15 equalizer | **OPEN** |

In particular, the response map `Eta`, boundary cocycle `beta`, uniform
kernel estimates, completed faithfulness, local symbol, and Hodge/Maxwell
operator are not contained here.  Supplying them by implication would repeat
the minimal-six refutation.

```text
GENERATORS = EXHIBITED_AS_COVARIANT_PER_ORBIT_FAMILY
COMPOSITION = PROVEN
REFINEMENT = PROVEN

Q408_ROOT_READY_FOR_CROSS_CHECK = yes
Q408_ROOT_REGISTERED = false
JOINT_EQ6 = OPEN
EQ6_MEMBER_BOUND = false
DOWNSTREAM_CONDITIONAL_EXECUTION_STARTED = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

Seal this artifact, mirror artifact and sidecar byte-identically to
`alpha-program-archive/workspace/`, report hashes, and stop.  No register,
plan, tracker, git, commit, or push action belongs to this lane.
