# STAGE 8 — AXN BUILD — THE THIRTEEN PARTIAL B0 ROWS, RESOLVED INDIVIDUALLY
## DARIO LANE (Builder B, verifier) — RELAY 858 — [PLAN:AXN-BUILD-A25]

```text
alpha_computed         = false
proof_authorized       = false
kappa_record_computed  = false
```

No member bound; no fixed-point execution; no end test; no numeric evaluation of any physical
quantity; no comparison to any measured constant; no smooth carrier imported; no EM identification;
no common cell formed; no junction map evaluated. PE-1..PE-12 pointer-only, none opened or consulted.
Builder-A code (`evaluator_build_A/*.py`, `checks/`) not opened. `~/.codex` untouched. memory-bank
never searched. No register, plan, tracker, or git action.

CLAIM STATUS: **all headline items CLAIMED.**

SUBJECT: `STAGE8_AXN_BUILD_B0_ROOT_CENSUS_CODEX2_V001.md` = `a614a69409b40ab8`, seal verified BEFORE
reading. My 854 cross-check `8625fbba3d8dd500` (seal OK) governs everything it resolved.

**THE DEBT THIS PAYS IS MINE.** At 854 I disclosed that my task-A(2) check was *"thinner than the
relay asked"* and that *"those thirteen are where a mis-classification would be easiest to hide."*
The relay also has me run the tenth search family I proposed in my own 854 robustness note. Both
halves are self-authored gaps. **The result below is 13/13 PARTIAL-CONFIRMED — no upgrade, no
downgrade — and a uniform confirmation is exactly the outcome that would look like rubber-stamping,
so §4 displays the two rows that came closest to moving and §5 states what I found that the census
does not say.**

---

## 0. LEAD

**`ROWS = 13 adjudicated (13 PARTIAL-CONFIRMED / 0 SHOULD-BE-PRESENT / 0 SHOULD-BE-ABSENT).`**
**`ROOT_A_IMPACT = NONE.`** No row moved, so the realization-material picture for root A is unchanged,
850's MATERIAL classification is untouched, and the three-root buildability conclusion is not
disturbed. Nothing here requires the registrar to reopen anything.

**The census survives the individual pass.** Its seventeen rows are 2 ABSENT + 2 PRESENT + 13 PARTIAL,
confirming the count I asserted at 854. Every one of the thirteen reasons is accurate at the bytes,
and one of them — the `d_C0` row — reproduces a distinction the source states under a heading of its
own: *"The two descent maps that must not be conflated."*

**Two findings the census does not state, both structural, both for the registrar (§5):**

1. **The thirteen are not independent.** Every partial-row source carries its material as `TYPE-P`
   conditional on **DoR-008** (and mostly DoR-009). The partial stock rests on one premise base; if
   DoR-008/009 were withdrawn, most of the thirteen would go to ABSENT **together**. The census
   classifies the rows one at a time and does not disclose the shared conditional foundation.
2. **The corpus already owns the test that kills the one move that would upgrade rows 7–10.** The
   relabeling test in `576da30f300a0362` refutes renaming coordinate projections as descent maps. The
   upgrade route for the four arrow rows is not merely unbuilt — it is **sealed shut**.

**`TENTH_FAMILY = run (27 B0-scoped files reachable only by it; ZERO supply realization material).`**
My 854 note proposed the tenth family would close a coverage gap "at no cost." It closes it, and
**the gap is empty** — see §3.

---

## 1. METHOD, AND WHAT WOULD HAVE COUNTED AS A MOVE

The census's own definitions govern, verbatim:

> `MATERIAL-PRESENT` means the exact component exists at the bar's required scope. `MATERIAL-PARTIAL`
> means sealed bytes can enter a future construction but do not inhabit the complete component.
> `MATERIAL-ABSENT` means the sweep found only requirements, negatives, or unrelated objects.

So the two failure modes I hunted for, per row, are precise:
- **SHOULD-BE-PRESENT** — the material inhabits the complete component at the bar's scope and was
  under-called.
- **SHOULD-BE-ABSENT** — the cited stock is **only a requirement, a negative, or an unrelated
  object**, and "partial" dresses a requirement as progress. *This is the failure mode that matters
  here*: a bar's own schema, restated, can masquerade as material toward satisfying it.

Sources seal-verified before reading (14 files, all OK): `ec848d293d0d8241`, `576da30f300a0362`,
`1467ab9850022931`, `b0118d89e0b4b321`, `8e9a09c104f4b635`, `f97ee43e052969f0`, `db1808e4da38cbfe`,
`64dec22c9cca6910`, `71f419acb4b0259d`, `3d4ad604fdc46e2d`, `dd8de647d5d38d66`, `4c1ba9b5dad0d130`,
`b765965fcd5eeb8e`, `3cd4b080aff0fc07`; plus `476baa18b1e2e97f`, `185157a393f14dfa`,
`ee4dc9df3dde806f`, `f7fa3c0ff4f7a13b`, and the PathCert subtrace file (seal OK).

---

## 2. THE THIRTEEN ROWS

### PR-1 — frozen primitive-input inventory — **PARTIAL-CONFIRMED**

**Exists:** six Level-1 adopted premises with current authority-ledger entries; the conversion
constants `c,hbar,G`; the compound index `I=(a,mu,x)` with Keldysh ordering; the formal CTP branch
metric/reality convention (`4c1ba9b5dad0d130`, seal OK).
**Missing, at bytes:** `476baa18b1e2e97f` (seal OK) states
`complete_corpus_wide_primitive_inventory_exhaustiveness = NO_VERDICT | blocked_by: Q-95's exact
Obj_0 signature/domain/codomain is a separate missing input and the standing premise inventory itself
disclaims an exhaustive premise universe`. The census's reason is verbatim right.
**The route stops where the census says:** *"`I_prim` reaches only the formal presentation layer …
It stops before a complete microscopic source-record-field operator/dynamics, physical
quotient/measure, correlator, or common-origin certificate exists."*
**Law 9 (this row):** I tested an apparent count discrepancy — the census says **six** Level-1
candidates while the sealability audit is named **SEVEN**. **These are two different lists and the
census cites the right one:** six Level-1 *adopted premises* (which survive) versus seven
*used-as-primitive objects*, of which `dd8de647d5d38d66` finds **0 of 7 sealable**, including
`PrimitiveInventory_0` itself — *"INDEFINITE and circular as currently posed: an inventory cannot
certify its own membership before an external inventory rule and manifest exist."* **No error.**
**Not ABSENT:** the six Level-1 rows are adopted material with ledger authority, not requirements.

### PR-2 — target-independent construction rule / frozen trace — **PARTIAL-CONFIRMED**

**Exists:** a genuine target-independence certificate at finite scope, with named verdict owners
(`Cert_target_independence`, `Cert_no_post_output_supplementation`), plus the family common-origin
trace and the law-side P3 receipt.
**Missing:** a complete B0-role production record for C0/U1/U2/U3. Decisive at bytes,
`576da30f300a0362`: *"**There is no `Gen_Omega`.** This is exactly the 'declared bag' that the sealed
Piece-1 root test excludes"*, with `CANDIDATE_IS_P5_DERIVATIONAL_COMMON_ORIGIN = false | TYPE-R`.
**Not PRESENT:** the trace covers a finite family, not the four-descendant role.

### PR-3 — complete candidate signature — **PARTIAL-CONFIRMED (thinnest of the thirteen; see §4.1)**

**Exists, exactly:** `ec848d293d0d8241` seals
`Sig_B0 := (Type_B0,Arity_B0,Dom_B0,Cod_B0,Params_B0)` and `Obj_B0 conforms_to Sig_B0`, with the
conformance relation explicitly *not* asserting linearity, boundedness, self-adjointness, a
representation, **or existence**.
**Missing:** any candidate values. Corroborated in the same file by
`B0_candidate_mathematical_properties_derived = false | TYPE-U`.
**Law 9 — I pressed the one route that could have upgraded this row and it self-excludes.** If any
artifact supplied a `Sig_B0` instance it would be the adversarial countermodel suite
(`185157a393f14dfa`, seal OK), which constructs concrete objects. Its own preamble forecloses it:
*"**`B0` WAS NOT CONSTRUCTED.** Every object below is a description of a *wrong* object, built to be
checked against sealed constraints — **never a candidate `B0`**."* The codomain-route test
(`ee4dc9df3dde806f`, seal OK) likewise tests a route, not an instance.

### PR-4 — provenance and domain certificate — **PARTIAL-CONFIRMED**

**Exists:** `Prov_C0_008` as an actual provenance record (`1467ab9850022931`); and, in the U3
skeleton (`b0118d89e0b4b321`), instantiated item 6, *"full-domain anchor for every currently built
bounded operator."*
**Missing, and the boundary is sharp:** the same file's uninstantiated item 4 is *"endpoint domains
for the later **unbounded** physical operators."* **The domain certificate is complete for bounded
operators and absent for unbounded ones** — a cleaner statement of the census's *"the missing physical
endpoint domains."* `Prov_B0` additionally requires the `TYPE-U` `B0_CONSTRUCTION_WITNESS`.

### PR-5 — U2 descendant content — **PARTIAL-CONFIRMED**

**Exists:** the U2 skeleton, finite law/state/effect/path material, and the ratified source-coupled
law; `64dec22c9cca6910` records U2's finite dynamics subfield accepting the ratified finite
transition **cleanly**.
**Missing:** one complete U2 package on the required common physical domains/contact data, exhibited
as a B0 descendant. The same file corrects an over-broad prior reading in its own lane: *"This
corrects the scope of Q-234's phrase 'four-consumer object gap closed.' The object reaches all four
interfaces, but it closes only their shared finite-transition input. It does not close every
consumer-specific codomain."* **The material is real and its ceiling is stated by its own producer.**

### PR-6 — U3 descendant content — **PARTIAL-CONFIRMED** (and see §5.3)

**Exists:** `U3_SKEL_008_FINITE_ASSEMBLED = true | TYPE-P | premises: DoR-008, DoR-009`, with **six**
instantiated items (finite common-gauge orbit map and quotient; physical identity and
constant-stabilizer removal; exact quotient descendants; branch grammar; endpoint action and
sequential gluing; bounded-operator domain anchor).
**Missing:** `U3_008_INSTANCE_EXISTS = false | TYPE-U`. Census reason confirmed verbatim.

### PR-7 — `d_C0` — **PARTIAL-CONFIRMED (the strongest-stated row of the thirteen)**

The census's reason — *"the source expressly distinguishes `d_C0^P` from the common-origin `d_C0`"* —
is not a paraphrase. `1467ab9850022931` carries a section titled **"3. The two descent maps that must
not be conflated"**:

```text
d_C0^P : P_008 -> C0_008          (premise-level; TOTAL; TYPE-P | premises: DoR-008;
                                   frozen trace; certificate C0-008-C6)
d_C0   : B0_candidate -> C0        (the required common-origin descent)
```

**A total map exists from the wrong domain.** That is the exact content of MATERIAL-PARTIAL, and the
source itself warns against the conflation that would upgrade it.

### PR-8 — `d_U1` — **PARTIAL-CONFIRMED**

The bar requires each `d_Ui` to be *"a witness from the same candidate together with its descended
C0"* — domain `(B0,C0)`. Test leg T31 fails or returns `NO_VERDICT` unless a claimed `d_U1` is
well-typed on an admitted `(B0,C0)` input **and** an independently derived U1 reference exists.
Premise-level U1 material exists; no B0-bound, comparison-ready `d_U1` is sealed. Census reason
confirmed.

### PR-9 — `d_U2` — **PARTIAL-CONFIRMED**

Finite PathCert state/effect/domain/dynamics legs and source-law material exist; the C7 naturality /
one-cell restriction / target-independence construction for the finite `d_dynamics,N` portion is
listed in `64dec22c9cca6910` as queue item **Q4, `PREFLIGHT_CLEAN_NOW`** — i.e. *not yet built,* and
scoped to the finite subtrace. These are lower-row legs, not the complete B0-to-U2 map. Confirmed.

### PR-10 — `d_U3` — **PARTIAL-CONFIRMED**

`8e9a09c104f4b635` states it outright: `d_U3_TOTAL = false | TYPE-U | would-build: executable descent
maps for all four fields`, alongside `U3_008_INSTANCE_EXISTS = false | TYPE-U` and
`P3_COMPLETE = false | TYPE-U`. Confirmed.

**Common to PR-7…PR-10, and this is the load-bearing structural point:** in every case a *total,
certificated* map exists at the **premise level** and the **B0-domain** map does not. The single move
that would upgrade all four at once — renaming the premise-level projections as the descent maps — is
**already refuted of record** by `576da30f300a0362`: *"Promoting the two coordinate projections to
names `d_law` and `d_state` does not produce either output from antecedent primitive data"*, with
`CANDIDATE_SURVIVES_RELABELING_TEST = false | TYPE-R`.

### PR-11 — property certificates — **PARTIAL-CONFIRMED**

**Exists:** finite covariance, restriction, target-independence, bounded-domain and local-law
certificates, each with a **named verdict owner** — a real quality signal, not decoration.
**Missing:** a complete property suite over one role realizer and all four descendants; the aggregate
tests are unexecuted (PR-13). Confirmed.

### PR-12 — no-post-output supplementation certificate — **PARTIAL-CONFIRMED, byte citation verified**

**The census's byte citation is exact and I checked it rather than accepting it.** It cites "PathCert
bytes `[13562,13750)`" without naming the file; I located the span by content across the corpus. It
is in `STAGE8_ENVELOPE_FINITE_PATHCERT_DYNAMICS_SUBTRACE_CONSTRUCTION_V001.md` (seal OK), at exactly
`[13562,13750)`, **span sha `0678f17fb4466295`**, reading:

```text
CERT_TARGET_INDEPENDENCE = PASS
CERT_NO_POST_OUTPUT_SUPPLEMENTATION = PASS
TARGET_AWARE_OUTPUT_USED_TO_NARROW_PATH_FAMILY = false | TYPE-S |
  scope: family predicate and equations (1)-(4)
```

A genuine local pass, correctly scoped. **Missing:** T2's applicability requires *"a frozen B0
candidate; exhaustive frozen C0/U1/U2/U3 output checklist; `d_C0` and three `d_Ui` maps"*, and
`B0_T2_executed = false | TYPE-U`. Census reason confirmed. *(An unnamed file in a byte citation is a
minor citation-hygiene note, not a defect: the span is unique and correct.)*

### PR-13 — applicable T1–T6 executions — **PARTIAL-CONFIRMED**

**Exists:** the full test law — six failure-capable property families, T3/T5/T6 split into separately
routed legs, with declared `applicability` / `procedure` / `failure_condition` / `failure_route` /
`no_verdict_route` per test, and explicit anti-contamination wiring (*"a U2 positivity failure or U3
quotient failure does not report into T1"*).
**Missing:** every execution. `B0_T1_executed = false | TYPE-U`, `B0_T2_executed = false | TYPE-U`,
and the governing rule *"A missing candidate, fixture, oracle, domain, descent map, or independent
reference returns `NO_VERDICT` for that test; **it is not evidence against B0**."* Confirmed.

---

## 3. THE TENTH FAMILY — COVERAGE GAP CLOSED, AND EMPTY

Run as proposed at 854: `ROLE_REALIZATION` / `realizer` / `inhabit` / `ORIGIN_ROLE`, case-insensitive,
over the workspace, excluding `.proof_deps`, `rd22_run_*`, and `evaluator_build_A`.

- **151** files are B0-scoped **and** carry a tenth-family term.
- **27** of those are reachable by the tenth family and by **none** of the census's nine query
  families — the concrete size of the gap my 854 note predicted.
- **Of those 27, ZERO supply B0-role realization material.**

I read the tenth-family context in each. **Every one uses the vocabulary in the opposite direction —
to report an absence of inhabitants:**

```text
"This seals a type, not an inhabitant."
"no full receiver inhabitant is constructible and the forcing question remains gated"
"CERTIFICATE = BLOCKED_AT_INHABITANT"
"never affirmed to have an inhabitant"
"It is not a theorem that the interface has an inhabitant."
"neither inhabited"          "A_RP^+ inhabitance = EMPTY_OF_RECORD"
"a gate PASS ... never asserts that any class is inhabited"
```

The sole affirmative — my own 746, *"THE THIRD CONSTITUENT IS INHABITED OF RECORD"* — is about the
7A class's third chamber, an unrelated object.

**Second-order finding, which explains why the nine sufficed:** in this corpus the
inhabit/realizer/role-realization vocabulary is **predominantly the vocabulary of reporting
non-inhabitation**. A genuine realization artifact carries descent identifiers and is therefore caught
by Q3/Q4/Q5; the pure-vocabulary residue is absence-reporting. **My 854 note was right that the gap
existed and wrong to imply it was where a realizer might hide.** Coverage is now closed by execution
rather than by argument, and the census's Q1–Q9 enumeration is confirmed adequate for its purpose.

The one file the 854 note flagged — `576da30f300a0362` — **is reachable by the nine** (it carries
`C0_008`, `U1_008`, `PathCert`), exactly as I found at 854, and it does not supply a realizer: it is
the artifact that **refutes** the strongest candidate.

---

## 4. THE TWO ROWS THAT CAME CLOSEST TO MOVING

A uniform 13/13 is only worth anything if the near-misses are shown.

### 4.1 PR-3 (candidate signature) — the one row with a live SHOULD-BE-ABSENT argument

**The argument against the census:** PR-3's entire cited stock is the bar's **own schema**. Under the
census's definition, ABSENT is "only requirements, negatives, or unrelated objects" — and a schema
with no instance is arguably *precisely a requirement*. Sharpening it: **row 1 (ABSENT) and PR-3
(PARTIAL) cite the same schema material** — row 1's stock is *"candidate family/schema in the B0 stop
spec and codomain-fiber test."* Same bytes, two classifications.

**Why the census is nevertheless right.** The rows ask for different things. Row 1 asks for an
**inhabited candidate**; a schema is not partial evidence of an inhabitant — it is the demand itself,
so ABSENT. PR-3 asks for a **complete candidate signature**; a signature *is* the schema plus values,
so the sealed schema is genuine partial material with a stated remainder. The distinction is
principled and it holds.

**Recorded honestly: this is the thinnest of the thirteen**, and it is the one row where a
differently-scoped reading of "requirement" would change the call. I confirm it, and I flag it as the
row most worth an opposite-lane look.

### 4.2 PR-1 (inventory) — the count discrepancy that turned out not to be one

"Six Level-1 candidates" beside an audit named `SEVEN_USED_AS_PRIMITIVES` is the shape of a
miscitation, and it is the class of error I convicted 814 of at 818. **I checked it and the census is
correct** — two disjoint lists, and the census cites the right one. Reported because a reader
comparing filenames would see a conflict that is not there.

---

## 5. WHAT THE CENSUS DOES NOT SAY — FOR THE REGISTRAR

### 5.1 The thirteen partials share one premise base — CORRELATED, NOT INDEPENDENT

`TYPE-P` / DoR-008 / DoR-009 counts over the sources carrying the partial material:

| source | TYPE-P | DoR-008 | DoR-009 |
|---|---:|---:|---:|
| C0 assembly `1467ab9850022931` | 26 | 37 | 0 |
| U3 premise-level `b0118d89e0b4b321` | 27 | 32 | 31 |
| PathCert subtrace | 10 | 11 | 11 |
| `MINIMAL_OMEGA` `576da30f300a0362` | 35 | 29 | 29 |
| handoff receipts `64dec22c9cca6910` | 13 | 11 | 14 |
| P3 second attack `8e9a09c104f4b635` | 13 | 12 | 14 |

**Every partial-row source holds its material as TYPE-P conditional on DoR-008, mostly with DoR-009.**
The census classifies row by row and never states that the thirteen rest on a common conditional
foundation. **Consequence for the registrar: the thirteen PARTIALs are not thirteen independent
hedges. A withdrawal or narrowing of DoR-008/009 would move most of them toward ABSENT together.**
This is a correlated-dependence disclosure, not a defect in any row's classification — each row is
correctly typed *given* the premises — and it is the kind of thing a per-row table structurally
cannot show.

### 5.2 The upgrade route for the four arrow rows is sealed shut, not merely unbuilt

Worth recording as a positive: an adjudicator wanting to upgrade `d_C0`/`d_U1`/`d_U2`/`d_U3` would
reach for the premise-level maps, which are total and certificated. `576da30f300a0362` has already
run and failed exactly that move (`CANDIDATE_SURVIVES_RELABELING_TEST = false | TYPE-R`). **The
corpus owns its own defence here.**

### 5.3 A five-versus-four field count that resolves — and independently corroborates my 855

`b0118d89e0b4b321` (Codex lane 1, Q-238) lists **five** uninstantiated U3 fields, the fifth being
*"common-origin provenance for the complete U3 tuple."* The census, the frontier map's SC5-D, and my
own 855 all work with **four**.

**Resolution, and later bytes control:** the later P3 record seals
`P3_COMMON_ORIGIN_PROVENANCE_PORT_DISCHARGED = true`. The fifth field was discharged; the count went
5 → 4 **by discharge, not by omission.**

**This independently corroborates the one derived reading in my 855** — that U3's remainder is *"four
fields over an existing trace, not four fields plus a trace."* I did not go looking for that
confirmation; it fell out of PR-6. I record it as corroboration and **claim no more weight for it
than a verifier may claim for evidence that happens to favour his own prior artifact** — it is
CLAIMED, and it remains for the opposite lane to check, exactly as 855 itself does.

---

## 6. FREEDOMS-CONSUMED (law 2, law 2a)

```text
CARRIED UNCHANGED: all seventeen census rows at their sealed classifications; the census's own
  PRESENT/PARTIAL/ABSENT definitions used as the governing rubric rather than a rubric of my own; the
  two ABSENT and two PRESENT rows NOT re-adjudicated (out of this relay's scope, and 854 governs);
  every cited flag at its sealed type (TYPE-P / TYPE-U / TYPE-R / TYPE-S / NO_VERDICT); DoR-008 and
  DoR-009 carried as PREMISES AND NOT DISCHARGED; the bar's Sig_B0 schema carried as a schema.

DERIVED HERE: (a) thirteen individual adjudications, each with its exists/missing split byte-cited;
  (b) the shared-DoR-008/009 correlated-dependence finding; (c) the observation that the relabeling
  test forecloses the upgrade route for the four arrow rows; (d) the 5-vs-4 U3 field-count resolution
  and its corroboration of 855; (e) the six-vs-seven list check; (f) the row-1/PR-3 same-material
  tension and its principled resolution; (g) the tenth family's execution and its negative result,
  including the second-order observation about absence-reporting vocabulary; (h) location and
  verification of the census's unnamed byte citation, span sha 0678f17fb4466295.

SELECTED HERE: NOTHING. No inventory frozen, no signature instantiated, no trace completed, no map
  built, no certificate executed, no candidate proposed, no premise discharged, no row re-classified.
  NO FLAG MOVES. Root A's material picture is untouched and the buildability verdict was NOT rerun —
  impact is NAMED FOR THE REGISTRAR ONLY, per the relay.

NOT DONE AND DISCLOSED: I did not re-adjudicate the two ABSENT or two PRESENT rows. For PR-8 and
  PR-11 my check is a bar-conformance check (the required domain/scope is not met) rather than an
  exhaustive re-hunt of every candidate map or certificate in the corpus — thinner than PR-7 and
  PR-12, and I say so. The parent-tree files cited BY the sealed archive records were not opened;
  every claim is scoped to sealed archive bytes.

SCALING WEIGHTS (law 2a): NONE CONSUMED.  SUBSTITUTED: NONE.
```

**FLATTENING CHECK — 37/37 walked, clean.**
**S03 and THE VOID CONDITION — live at the place I named at pickup.** An upgrade was the interesting
outcome and I disclosed that pull in the ACK before doing the work. **No row was upgraded, and the
one row with a live argument for movement (PR-3) had that argument running toward ABSENT, not toward
PRESENT** — the direction that would have been *less* interesting, which is some evidence the pull
did not steer the result. **S12** — PRESENT/PARTIAL/ABSENT and every sealed flag carried as the
status it is, never as the object; `NO_VERDICT` carried as a refusal, not as evidence.
**S26 / S08 / S19 / S24** untouched — no measure, domain, or regularity class imported; the
bounded/unbounded domain boundary in PR-4 is **read off sealed bytes, not supplied**.
**T1 / T5** untouched.
**BR-1 HELD, and it is the governing law of this relay.** The census is Codex's producer-declared
object. It may accuse and never exculpate: I did **not** treat "the census says PARTIAL" as evidence
for PARTIAL anywhere. Each row was resolved against its underlying sources, and the census's own
sentences were used only as the claim under test. Where the census turned out to be right, the
warrant cited is the source, not the census.

---

## 7. FINAL LINES

```text
ROWS = 13 adjudicated (tally: 13 confirmed / 0 up / 0 down).
  PR-1 inventory | PR-2 construction rule/frozen trace | PR-3 candidate signature | PR-4
  provenance+domain certificate | PR-5 U2 content | PR-6 U3 content | PR-7 d_C0 | PR-8 d_U1 |
  PR-9 d_U2 | PR-10 d_U3 | PR-11 property certificates | PR-12 no-post-output certificate |
  PR-13 T1-T6 executions — ALL PARTIAL-CONFIRMED, each with its exists/missing split byte-cited.
  THE CENSUS SURVIVES THE INDIVIDUAL PASS.  Strongest row: PR-7, whose stated reason reproduces a
  heading in its own source — "The two descent maps that must not be conflated" (d_C0^P : P_008 ->
  C0_008 total at TYPE-P, versus the required d_C0 : B0_candidate -> C0).  Thinnest row: PR-3,
  displayed in full at §4.1 with the live SHOULD-BE-ABSENT argument and why it fails.
  CITATION CHECKED, NOT ACCEPTED: the census's unnamed "PathCert [13562,13750)" is EXACT — located in
  STAGE8_ENVELOPE_FINITE_PATHCERT_DYNAMICS_SUBTRACE_CONSTRUCTION_V001.md, span sha 0678f17fb4466295.
  CHECKED AND CLEARED: "six Level-1 candidates" beside an audit named SEVEN_USED_AS_PRIMITIVES is TWO
  DISJOINT LISTS, and the census cites the right one.

ROOT_A_IMPACT = NONE.  No row moved to PRESENT, so no bar component is fed, 850's MATERIAL
  classification is untouched, and the three-root buildability conclusion is undisturbed.  The
  buildability verdict was NOT rerun, per the relay.  Nothing here requires the registrar to reopen
  anything.  TWO STRUCTURAL ITEMS ARE NAMED FOR THE REGISTRAR ANYWAY: (1) the thirteen PARTIALs are
  CORRELATED, NOT INDEPENDENT — every partial-row source holds its material as TYPE-P conditional on
  DoR-008, mostly with DoR-009, so a withdrawal or narrowing of DoR-008/009 would move most of the
  thirteen toward ABSENT TOGETHER, which a per-row table structurally cannot show; (2) the upgrade
  route for the four arrow rows is SEALED SHUT, not merely unbuilt — 576da30f300a0362 has already run
  and failed the relabeling move, CANDIDATE_SURVIVES_RELABELING_TEST = false | TYPE-R.

TENTH_FAMILY = run (result: gap closed, and EMPTY).  151 B0-scoped files carry a tenth-family term;
  27 are reachable by it and by NONE of the census's nine; ZERO of the 27 supply realization
  material.  EVERY ONE USES THE VOCABULARY TO REPORT AN ABSENCE OF INHABITANTS — "seals a type, not
  an inhabitant", "BLOCKED_AT_INHABITANT", "never affirmed to have an inhabitant", "EMPTY_OF_RECORD".
  SECOND-ORDER FINDING: in this corpus the inhabit/realizer vocabulary is predominantly the
  vocabulary of reporting NON-inhabitation, which is why the nine sufficed — a genuine realization
  artifact carries descent identifiers and is caught by Q3/Q4/Q5.  MY 854 NOTE WAS RIGHT THAT THE GAP
  EXISTED AND WRONG TO IMPLY A REALIZER MIGHT HIDE IN IT.  Coverage now closed BY EXECUTION rather
  than by argument; Q1-Q9 confirmed adequate.

CHAIN_INVOKED = false

VERB_AUDIT_SELF = NOT CLEAN (+4):
  (1) I RETURNED 13/13 CONFIRMED ON A CHECK I MYSELF CALLED THE EASIEST PLACE TO HIDE A
      MIS-CLASSIFICATION.  That is the single most suspicious possible outcome of this relay, and I
      state it as the audit's first item rather than letting the tally speak for itself.  My defence
      is displayed, not asserted: §4 shows the two rows that came closest to moving, including the
      full SHOULD-BE-ABSENT argument against PR-3 and why it fails.
  (2) MY OWN 854 ROBUSTNESS NOTE IS PARTLY REFUTED BY ITS OWN EXECUTION.  I proposed the tenth family
      as closing a gap "at no cost."  It closes it and finds NOTHING — and the reason is structural:
      the vocabulary is used for reporting absence, so the gap was never where a realizer could hide.
      The proposal was cheap and correct to make; its implied risk was overstated, and I say so.
  (3) TWO ROWS ARE THINNER THAN THE OTHER ELEVEN.  PR-8 and PR-11 rest on bar-conformance (the
      required domain/scope is not met) rather than an exhaustive re-hunt of every candidate map or
      certificate.  Having convicted 854 of exactly this thinness, I will not repeat the pattern of
      disclosing it only in a closing line — it is in §6 and here.
  (4) ONE OF MY FINDINGS FAVOURS MY OWN PRIOR ARTIFACT.  The 5-vs-4 U3 field-count resolution
      corroborates the derived reading of my own 855.  I did not go looking for it and I claim no
      extra weight for it: it is CLAIMED, and it remains for the opposite lane to check, exactly as
      855 does.  A verifier who finds evidence for his own prior work should say so louder than a
      verifier who finds evidence against it.

alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

---

**GATES HELD.** Charter fences live; nothing selected; no smooth import; no EM identification; no
member binding; no fixed-point execution; no end test; no numeric evaluation of physical quantities;
no comparison to measured constants; no common cell formed; no junction map evaluated. PE-1..PE-12
pointer-only, none opened or consulted. Builder-B independence held — no Builder-A code opened.
`~/.codex` untouched; memory-bank never searched. No register, plan, tracker, or git action.
