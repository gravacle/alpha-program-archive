# STAGE 8 / 7A / STEP 8 — RA27-2 AGAINST D012, DEMAND BY DEMAND; RA27-3 OPENED; THE MATERIALITY QUESTION TYPED

Lane: DARIO (Builder B, independent verifier). Relay 739.
Governing: the corrected verdict `c854c8b2…` (Q-645, cross-family stable); the built
index `66f078ba…`; `DECISION_RA27_2_ADOPTIONS` `31e42812…` and its correction
`72894c1c…`.

## Lead determination

**RA27-2 does not discharge, and the demand that stops it is not the one that was
blocking it.**

The blocker of record — the measure's refinement functoriality, named in
`DECISION_CORRECTION_FC1_TRANSPORT` as what "must be DERIVED … before RA27-2's
discharge … can be booked" — **is supplied**, cross-family stable, and
branch-ruling-free. Four of D012's five demands are supplied outright. The fifth,
*"prove generator completeness/reachability"*, splits: **reachability is supplied by
the adopted directedness axiom** (which D012's own modality column licenses, and which
a sealed source now witnesses on one mixed pair), while **completeness is supplied only
as a bounded certificate, never as the proof D012's verb demands.**

The register itself typed the honest deliverable as *"a completeness/reachability
certificate over a generator set, plus a surfaced adoption request iff stock does not
force"* — and flagged it **"a PRINCIPAL FORK."** That fork was flagged and has never
been decided. Until it is, the demand's standing verb is *prove*, and a certificate is
not a proof.

So: **one ruling — certificate suffices, or proof required — discharges or holds
RA27-2 on the display in §1 below, and nothing else is missing.** That is the whole
distance remaining on this demand.

I refused this exact booking at 727 (refusal #4: *"Booking RA27-2 as discharged on four
of five D012 items"*). The other blocker clearing does not license reversing it.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively over the whole archive
(`find . -name 'STAGE8_7A_RA27_2_DISCHARGED_DARIO_V001.md*'`): **absent**, artifact and
sidecar both. Nothing overwritten.

### 0.2 Sources verified before use

```text
D012   workspace/STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md          74bbb7aa…
V011   workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/
         BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md               aa7c6d49…
JREF   workspace/STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md
                                                                     8dd59b35…
4d     workspace/STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md
                                                                     430f0971…
MAJ    workspace/stage8_execution/work/
         MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md                   08b91543…
IDX    workspace/STAGE8_7A_RA27_2_INDEX_BUILT_DARIO_V001.md          66f078ba…
FUNC   workspace/STAGE8_7A_FUNCTORIALITY_CHARACTERIZATION_DARIO_V001.md
                                                                     5003d917…
CVER   workspace/STAGE8_7A_COUNTEREXAMPLE_CONTEST_DARIO_V001.md      c854c8b2…
X2     workspace/STAGE8_7A_BRANCH_F_CROSSCHECK_CODEX2_V001.md        97587de8…
FC     supervision/DECISION_RA27_2_ADOPTIONS_2026-08-08.md           31e42812…
FCC    supervision/DECISION_CORRECTION_FC1_TRANSPORT_2026-08-08.md   72894c1c…
REG    supervision/QUESTIONS_SETTLED_REGISTER_V001.md                fd6c774c…
```

**A pin hazard, disclosed.** Two different files in the archive are named
`BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md`: the sealed packet member under
`review_packets/…` (78,794 bytes, `aa7c6d49…`) and an **unsealed 84,987-byte copy at
`workspace/` top level** (`20a3a17d…`). Every V011 byte offset in this artifact and in
all my prior artifacts is against `aa7c6d49…`. A citation of the form
`V011 [46882,47019)` **without the path lands in different text** in the other file. I
hit this on my first `find … | head -1` this relay and caught it on the digest, not on
the content. Recorded as an archive hazard, not a finding against either file.

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.
No numeric evaluation of physical quantities.  No comparison to measured constants.
No common cell formed.  No junction map evaluated.  NO RESPONSE EVALUATED.
Nothing adopted here.  No register, plan, tracker, or git action.
```

---

## 1. UU1 — RA27-2 AGAINST D012'S OWN TEXT, DEMAND BY DEMAND

### 1.1 The demand, at its span

[PROVABLE] `D012` `74bbb7aa…[44324,44664)`:

```text
| RA27-2 | `Ref_a` with generators, same-region relation, common refinements, and
composition | TYPE-U | define familywise on addressed record complexes; prove
generator completeness/reachability | target-independent grammar may require
**adoption** if stock does not force it; one member may not be selected | forms
C27.3 physical source |
```

Five demands: **generators · same-region relation · common refinements · composition ·
prove generator completeness/reachability.** Two modality clauses bind the reading:
adoption is licensed *for the grammar* "if stock does not force it", and *"one member
may not be selected."*

### 1.2 Demand 1 — generators — **SUPPLIED**

[PROVABLE] Supplier: `IDX` `66f078ba…` §1.3, arrows `A0/A1/A2`.

| Generator | Content | Status | Authority |
|---|---|---|---|
| `A0` | identity arrows; certified signed relabelings/isomorphisms; ratified rank-preserving W3 restriction/inclusion squares | **BUILT / TYPE-P** | `JREF` `8dd59b35…[11610,11743)`, status at `[12001,12049)` = `FINITE_REALIZATION_SKELETON_J_0 = BUILT / TYPE-P` |
| `A1` | cubical bisection | **LICENSED of record** | `V011` `aa7c6d49…[46882,47019)` |
| `A2` | oriented simplicial/barycentric subdivision | **LICENSED of record** | `V011` `aa7c6d49…[46882,47019)` |

[PROVABLE] The `V011` span is C_ref's freeze list, verbatim:

```text
cubical bisection;
oriented simplicial/barycentric subdivision;
and common refinements preserving the same smooth coframe and connection.
```

The **third** constituent is barred as a source (TYPE-R, `4d` `430f0971…[20690,20930)`;
DoR-007 forecloses lifting the bar) and is not used as a generator. Its *function* —
common refinements exist — is supplied at demand 3 by adoption instead.

[PROVABLE] **The "one member may not be selected" clause is satisfied.** The index
carries two distinct generating families plus `A0` plus all finite composites; it is not
narrowed to one member. `CVER`'s equal-energy finding does not bear on this: that
freedom is in the *field assignment*, not in the index's arrow set.

### 1.3 Demand 2 — same-region relation — **SUPPLIED**

[PROVABLE] Supplier: `IDX` §1.3, `P2`:

```text
P2 (same region)  K ~ L iff K and L are ZIGZAG-CONNECTED under P3:
                  a finite chain K = X_0, …, X_n = L in which each consecutive
                  pair is related by a P3 arrow in ONE of the two directions.
```

Reflexive by `A0`, symmetric by the zigzag's construction, transitive by concatenation —
an equivalence relation on objects. The zigzag closure is my construction (`IDX` tags it
`[YOURS]`), and it is the standard closure of a directional relation, adding no content;
716 established that a directional generator set cannot express "same region" without
closing under spans.

### 1.4 Demand 3 — common refinements — **SUPPLIED AS ADOPTED**

[PROVABLE] Supplier: `FC` `31e42812…`, FC-1, cited **AS ADOPTED**:

```text
DIRECTEDNESS: ADOPTED AS AXIOM — the one element Q-624 proved stock cannot
   supply. Adopted, not derived, and recorded as such wherever the index is used.
```

carried inside the object itself at `IDX` §1.3:

```text
DIRECTEDNESS  *** ADOPTED AS AXIOM ***
   For any K, L with K ~ L there exists M with K -> M and L -> M in P3.
   THIS IS NOT DERIVED.
```

[PROVABLE] This is the demand's own anticipated case. D012's modality column reads
*"target-independent grammar **may require adoption** if stock does not force it"*, and
Q-624 established that stock does not force it (three TYPE-R verdicts; the span closure
was the located obstruction). The adoption is licensed by the demand text and ratified
by FC-1. **Supplied — and it may never be reported without the adoption tag.**

### 1.5 Demand 4 — composition — **SUPPLIED**

[PROVABLE] Supplier: `IDX` §1.3 — *"ARROWS generated by, and closed under finite
composition of: A0, A1, A2."* `P3` is defined as reachability by a finite composite;
closure is by construction, and `A0`'s identities give the unit.

### 1.6 The precondition that was blocking — **NOW SUPPLIED**

[PROVABLE] `FCC` `72894c1c…` states the blocker exactly:

```text
WHAT CHANGES: the measure's REFINEMENT FUNCTORIALITY is not supplied by any
sealed law; it must be DERIVED (or its failure derived) before RA27-2's
discharge, T_ref's instance transport, or J2's truth condition can be booked.
```

That is what `IDX` §3.1 booked as `RA27-2 = blocked (named)`. It is now discharged, in
three steps, none of them mine alone:

[PROVABLE] **(a) Four-volume additivity is sealed for both licensed moves.** `MAJ`
`08b91543…[19632,19996)`: family-A member = one bisection of the unit 4-cube, 16
subcubes at `|C|_4 = 1/16`; family-B member = the oriented order-simplex (Freudenthal)
subdivision, 24 simplices at `|C|_4 = 1/24` each; common refinement Z = 384 cells at
1/384. Exact, and sealed by a source that is not mine.

[PROVABLE] **(b) The measure is a sum over cells.** `V011` `aa7c6d49…[46074,46387)`:
each cell contributes `V_cell · Σ F²`; the measure formula and its orthogonal-cell
quantifier at `[45718,46068)`. So if the field restricts to the parent's `F` on each
child, functoriality **is** four-volume additivity — exact, no boundary term, no
freedom. That reduction is `FUNC` `5003d917…`.

[PROVABLE] **(c) The reduction survives the field freedom.** `CVER` `c854c8b2…`
`[21874,…)`: `CORRECTED_VERDICT = … forced: the ENERGY, sum F^2 per cell, exactly, on
the whole equal-energy set`. I reconstructed the counterexample independently — 24
simplices, 16 vertices, 65 edges, 32 fixed, 33 free, rank 33, `E_min = 191/252`,
`E(x_alt) = E(x_F) = 1` — and confirmed it exactly against `X2` `97587de8…`. The
32-dimensional freedom moves the assignment and **leaves the measure's value
unchanged.**

[PROVABLE] **Therefore the discharge of this precondition is branch-ruling-free**, which
is Q-645's finding and which I confirm here from the same bytes: every transport in play
that preserves the measure preserves it exactly, and the ξ-branch — whose residual is
bulk, `V F²(2s−1)²`, and never subextensive — is excluded by `V011`'s own tolerance
clause rather than by a choice among candidates.

**Scope flag carried, per `CVER`:** verified exhaustively for the **simplicial** move;
for the **cubical** move both `CVER` §3 and `X2` §2.1 quantify over a one-parameter
share family, so the cubical move's own free space is **untested**. The additivity in
(a) is sealed for both; the "on the whole free space" strengthening is earned for one.

### 1.7 Demand 5 — *prove generator completeness/reachability* — **SPLIT**

#### 1.7.1 Reachability — supplied AS ADOPTED, and now witnessed once

[PROVABLE] Reachability in the index is the adopted directedness of §1.4: for `K ~ L`
there is `M` with `K → M` and `L → M`. Adopted, tagged, licensed by the modality column.

[PROVABLE] **A sealed witness exists and I had not used it.** `MAJ`
`08b91543…[19632,19996)` names `Z` — the Freudenthal subdivision of each subcube of the
bisected 4-cube — **"Common refinement Z"** of the family-A member and the family-B
member. `Z = A2 ∘ A1`: a composite of exactly the two generators. So on the one mixed
pair the sealed corpus actually exhibits, **directedness is realized by a generator
composite**, which is what "reachability by the generators" asserts.

[PART-PROVABLE] What the witness is worth, stated at its strength and no higher. `MAJ`
**names** `Z` a common refinement and gives its exact cell count and volume; the span
does **not** display a proof that `Z` refines the family-B member. That containment —
that each of `Z`'s 384 cells lies inside one of the coarse Freudenthal complex's 24
simplices — is the Kuhn-stability fact, and it is asserted there, not proved.

[YOURS] I can see why it should hold: a Kuhn simplex is cut out by a total order on the
coordinates, and inside a subcube the shifted coordinates re-order only by moving the
bisected coordinate to an end, which the coarse order already fixes. **I did not verify
this and I do not book it.** Recording it because it is the argument a verifier should
be pointed at, and because my own constructions are the part of my work that has been
failing — this one wants an independent check before anyone leans on it. Even if it
holds, it covers **one pair**, not arbitrary `K ~ L`, so it would **reduce the reach of
the adopted axiom, not remove it.**

#### 1.7.2 Completeness — a bounded certificate, and only that

[PROVABLE] The certificate: **no refinement move named anywhere in the searched space
lies outside the index's generator set.**

Searched space, stated per the standing clause: **1,958 `.md` files, recursive glob over
`./workspace/**` and `./supervision/**`**, `*_DARIO_V001.md` excluded so the census
cannot corroborate itself from my own artifacts. Probed by meaning, not by phrase —
seventeen move-names plus fifteen word-boundary variants:

| Probe class | Result |
|---|---|
| `cubical bisection` (20), `bisection` (24), `barycentric` (29), `Freudenthal` (5), `Kuhn` (1) | all **inside** `{A1, A2}` |
| `common refinements preserving` (10) | C_ref's **third** constituent — barred as a source |
| `elementary refinement` (18), `refinement move` (15), `regular-CW refinement` (9) | descriptions of the same class, no new move |
| `octasect`, `trisect`, `stellar`, `quadrisect`, `coarsen`, `amalgamat`, `merge cells`, `vertex insertion`, `insert a vertex`, `split a cell`, `cone subdivision`, `cubical subdivision`, `hypercubic refinement`, `Whitney subdivision`, `newest-vertex`, `longest-edge`, `cell(ulation) move` | **0** |
| `edge collapse` (9) | **not a refinement move** — V009's record-category morphism `epsilon_e` with the endpoint-swap `S_e`; a morphism of the record, not of the cellulation |
| `dual cell` (2) | not a move |
| `red refinement` (7 → **0**) | **substring artifact** — `sha`**`red refinement`**, `decla`**`red refinement`**; zero at word boundary |

**Two instrument disclosures.** The `red refinement` count was a false positive of my own
substring probe and I found it only by reading the contexts; the word-boundary re-run
zeroed it. And `edge collapse` looked like a coarsening move until read — it is a
morphism in the record category, not in the cellulation. Both are the kind of defect
that has cost me a headline result twice; both were caught by reading, not by counting.

[PART-PROVABLE] So the certificate holds: **relative to the sealed corpus's named
stock, the generator set is complete** — `A0` from `JREF`, `A1`/`A2` from C_ref's
freeze, and C_ref's third constituent covered by the adopted axiom rather than left out.

#### 1.7.3 Why the certificate is not the discharge

[PROVABLE] D012's procedure column says **"prove"**. A certificate over a searched
corpus is not a proof that the generators exhaust the class: `V011`'s freeze reads
*"Freeze an admissible class `C_ref` of oriented, shape-regular periodic regular-CW
cellulations **with**: …"*. That "with" **enumerates what the class comes with**; it
does not say *and nothing else*. Reading it as a closure declaration would be the
stretch this relay names.

[PROVABLE] The register already saw this and typed it. `REG` `fd6c774c…[1261941,1262121)`:

```text
The honest deliverable is a completeness/reachability certificate over a
generator set, plus a surfaced adoption request iff stock does not force —
a PRINCIPAL FORK, flagged now.
```

**Flagged. Not decided.** I searched the supervision decisions for a ruling on it: FC-1
adopts the index, the directedness, the shape class and the preregistered relaxation,
and is silent on the completeness standard; FCC corrects the transport premise and is
silent on it; no other decision addresses it. **The fork is open**, so the demand's
standing verb remains *prove*, and I may not lower it to *certify* on my own authority —
that is a criterion's direction, and BR-1 bars me from carrying it.

### 1.8 The booking

| # | D012 demand | Status | Supplier |
|---|---|---|---|
| 1 | generators | **SUPPLIED** | `IDX` §1.3 `A0/A1/A2`; `JREF` `8dd59b35…[11610,11743)`, `[12001,12049)`; `V011` `aa7c6d49…[46882,47019)` |
| 2 | same-region relation | **SUPPLIED** | `IDX` §1.3 `P2` zigzag |
| 3 | common refinements | **SUPPLIED — AS ADOPTED** | `FC` `31e42812…` FC-1 directedness axiom |
| 4 | composition | **SUPPLIED** | `IDX` §1.3, closure under finite composition |
| — | *(precondition of record)* measure refinement functoriality | **SUPPLIED** | `MAJ` `08b91543…[19632,19996)` + `V011` `aa7c6d49…[46074,46387)` + `FUNC` `5003d917…` + `CVER` `c854c8b2…`, cross-family stable per Q-645 |
| 5a | reachability | **SUPPLIED — AS ADOPTED**; one sealed witness (`Z = A2∘A1`) | `FC` FC-1; `MAJ` `[19632,19996)` |
| 5b | **generator completeness** | **CERTIFICATE ONLY — PROOF NOT SUPPLIED** | census §1.7.2; standard-fork **flagged and undecided** at `REG` `[1261941,1262121)` |

```text
RA27_2 = STOPPED — demand named: generator completeness (5b), at the PROVE
         standard D012's procedure column states.
```

[YOURS] **And the exact distance remaining, stated plainly, because it is short.** One
principal ruling closes 5b either way. If the fork is decided *certificate suffices*,
RA27-2 discharges on the display in §1.2–§1.7 with no further work — every other demand
is supplied and the blocking precondition is gone. If it is decided *proof required*, the
proof obligation is a bounded combinatorial statement about `{A0, A1, A2}` and C_ref's
admissible class, and the Kuhn-stability lemma of §1.7.1 is its first piece.

[YOURS] I am not booking the discharge, and I want the reason on the record rather than
inferred. At 727 I refused this exact move — refusal #4, *"Booking RA27-2 as discharged
on four of five D012 items. Four are supplied; the object as demanded is not delivered.
Refused."* The blocker that cleared since is a **different** demand. A refusal does not
expire because a neighbouring obstacle was removed.

---

## 2. UU2 — RA27-3, RESTATED AND OPENED

### 2.1 The demand, at its span

[PROVABLE] `D012` `74bbb7aa…[44665,45007)`:

```text
| RA27-3 | full `J_ref` realization carrying incidence, degree, coframe,
connection, volume, support, and current density | TYPE-U | construct natural
transports on every RA27-2 generator and composite | **derivation** once source
grammar/premises are lawful; importing smooth `(M,g)` as source is barred by
DoR-007 | forms field transports |
```

Seven fields. One procedure: **natural transports on every generator and composite.**
One modality: **derivation** — no adoption clause here, unlike RA27-2 — with the smooth
import barred.

### 2.2 What RA27-2, as booked, supplies it

[PROVABLE] **The domain.** RA27-3 must be natural on "every RA27-2 generator and
composite"; §1.2/§1.5 supply exactly that quantifier — `A0`, `A1`, `A2`, and all finite
composites — together with `P2`'s classes and the adopted directedness. This is what
`IDX` §3.4 already recorded as the one thing the build unblocked.

[PROVABLE] **The `volume` field's transport is now supplied**, and this is new since
727. Four-volume additivity is sealed for both moves (`MAJ` `[19632,19996)`), the
measure is a per-cell sum (`V011` `[46074,46387)`), and the reduction survives the
32-dimensional field freedom (`CVER`). So one of the seven fields has its natural
transport, exactly, with no boundary term — and RA27-3's prerequisite, which `IDX` §3.4
said it "inherits rather than escapes", is discharged.

[PART-PROVABLE] **The `incidence` field is supplied on the generators, not on
composites.** `FUNC`'s `INCIDENCE = derived PROVABLE` rests on `MAJ`'s exact cell
counts for the two moves. That gives incidence data on `A1` and `A2` individually.
Naturality on arbitrary composites is not separately proven, and `FUNC` §2.2 records
the one time I got this wrong — my first trichotomy omitted the transverse multiplicity
and was short by exactly `m`, which is a composite-facing quantity. **Booked at
generator strength only.**

### 2.3 What remains — field by field

| Field | Status | Why |
|---|---|---|
| `volume` | **SUPPLIED** | §2.2 |
| `incidence` | **PARTIAL** — generators yes, composites unproven | §2.2 |
| `coframe` | **NOT SUPPLIED** | `FCC` `72894c1c…`: T11 derives a **change-of-coframe** law (one cell, one bivector, two frames); the index's arrows are **subdivisions** (one frame, one cell replaced by many). The obvious route — import smooth `(M,g)` — is **barred by DoR-007**, in the demand's own modality column |
| `connection` | **NOT SUPPLIED** | Same bar. C_ref's only clause that would transport a connection across refinement is the third constituent, *"common refinements preserving the same smooth coframe and connection"* — **barred as a source**, TYPE-R, `4d` `430f0971…[20690,20930)` |
| `degree` | **NOT SUPPLIED** | No transport law located; searched space as §1.7.2 |
| `support` | **NOT SUPPLIED** | ditto |
| `current density` | **NOT SUPPLIED** | ditto; three files in the searched space mention it at all, none with a refinement transport |

[YOURS] **The structural observation, and it is not encouraging.** `coframe` and
`connection` are the two fields whose only sealed transport clause is the barred one,
and RA27-3's modality column offers **no adoption clause** — it says *derivation*. So
RA27-3 cannot be closed the way RA27-2's demand 3 was closed. Either those two fields
get a derived subdivision transport, or DoR-007's bar is what stops RA27-3, and it will
stop it in a place where adoption is not on the table.

### 2.4 The two scope flags, carried

Per the relay's instruction, both flags from `CVER` travel with RA27-3:

1. **The untested cubical free space.** "The cubical assignment is forced" is the
   natural reading of both `CVER` §3 and `X2` §2.1 and **neither lane has earned it in
   general** — both quantify over a one-parameter share family. Any RA27-3 transport
   claimed natural on `A1` inherits this untested region.
2. **The open simplicial assignment, where the face response is concerned.** The
   equal-energy set is 32-dimensional; `x_alt − x_F` is **not** a coboundary (I solved
   `d_0 λ = x_alt − x_F` over all 65 edges and 16 vertices by exact elimination:
   inconsistent), so the freedom is physical, not gauge. Fields that consume the
   **assignment** rather than the **energy** — and §3 shows the face response is one —
   have no natural transport until that is settled.

```text
RA27_3 = opened.  Domain supplied (A0/A1/A2 + composites + P2 + adopted
         directedness).  volume SUPPLIED; incidence PARTIAL (generators only);
         coframe, connection, degree, support, current density NOT SUPPLIED.
         Modality is DERIVATION — no adoption clause.  Two scope flags carried.
```

---

## 3. UU3 — THE MATERIALITY QUESTION, TYPED

**No evaluation. No adoption.** What follows types a question and states what would
settle it. It computes nothing and rules nothing.

### 3.1 The two objects, at their spans

[PROVABLE] The generated face response — `V011` `aa7c6d49…[45361,45440)`:

```text
mathcal_K_L(xi,zeta)
  = R_record,L(Q_flux xi,Q_flux zeta),  xi,zeta in F_phys.
```

[PROVABLE] The lift — `V011` `aa7c6d49…[44961,45186)`:

```text
For every `xi in F_phys`, `Q_flux xi` is the unique horizontal, minimum-norm
lift orthogonal to `ker(d_1) intersect im(d_0)^perp`. It obeys
`d_1 Q_flux xi=xi`. Individual unit faces outside `F_phys` are never assigned
a lift.
```

with `Q_flux = P_h d_1^dagger(d_1 P_h d_1^dagger)^+|_(F_phys)` at
`aa7c6d49…[44801,44955)`.

[PROVABLE] The record susceptibility — `V011` `aa7c6d49…[43562,43760)`:

```text
R_record,L(a,b)
  =d^2/ds dt [Gamma_K(sa+tb)/N_4(K)]|_(s=t=0),
```

*"evaluated after the thermodynamic/linked-cluster limit."*

[PROVABLE] The energy — `V011` `aa7c6d49…[45718,46068)`, `[46074,46387)`: per cell
`V_cell Σ_(μ<ν) F²`, i.e. the inner product `⟨ξ,ζ⟩_(2,ell)` whose diagonal is the
quantity §1.6 showed to be functorial.

### 3.2 The question, typed

[PROVABLE] **`mathcal_K_L` is a bilinear form on `F_phys`; the energy is the quadratic
form of a different inner product on the same space; and the two live at different
levels of the complex.** `Q_flux` maps a 2-cochain field to its minimum-norm **1-cochain
potential**, and `R_record,L` is a Hessian on **connection tangents**. So
`mathcal_K_L` is the pullback of a form on potentials, while the energy is a form on
fields. The question is whether the pullback lands back on the energy.

[YOURS] Three readings of "factors through the energy", and only one is the live one:

- **(F-a) strict** — `mathcal_K_L(ξ,ζ) = φ(E(ξ), E(ζ))`. **Dead on type.** Fix `ζ`;
  then `ξ ↦ mathcal_K_L(ξ,ζ)` is linear, but a function of `E(ξ)` alone is even in `ξ`,
  so `mathcal_K_L(−ξ,ζ) = mathcal_K_L(ξ,ζ) = −mathcal_K_L(ξ,ζ)`, forcing
  `mathcal_K_L ≡ 0`. The strict reading is not the question anyone means.
- **(F-b) polarized — THE OPERATIVE READING.** By polarization a symmetric bilinear
  form is exactly its quadratic form, so "factors through the energy" is
  `mathcal_K_L = c_L·⟨·,·⟩_(2,ell)` on `F_phys`, for a scalar `c_L`.
- **(F-c) material** — the relay's own phrase, *"on the pairs the program's sealed
  criteria will ever evaluate"*: agreement on an evaluated domain `D ⊆ F_phys × F_phys`,
  even if (F-b) fails globally.

[YOURS] **The criterion, stated and not evaluated.** Under (F-b):

```text
mathcal_K_L factors through the energy
  <=>  Q_flux^dagger . R_record,L . Q_flux  =  c_L . G_(2,ell)   on F_phys
  <=>  Q_flux is a conformal isometry from (F_phys, <.,.>_(2,ell))
       into (connection tangents, R_record,L).
```

`G_(2,ell)` is the energy Gram operator. Nothing here is computed; the criterion is a
statement of what would have to be true.

[YOURS] **The decisive typing result: this is not a kinematic question.** `R_record,L`
is the intensive Hessian of `Gamma_K/N_4(K)` — dynamical content — and it is defined
*"evaluated after the thermodynamic/linked-cluster limit."* So the criterion cannot be
settled from the cochain complex alone, and any settlement of it inherits the limit's
burden. **This is why the materiality question is genuinely open rather than merely
uncomputed:** the object on the left of the criterion has a *definition* in the sealed
corpus and no *form*.

[YOURS] **A conditional reduction, offered as a lead and flagged as conditional.** *If*
`R_record,L` were the identity on the `P_h`-gauge connection tangents — which **is not
sealed and which I do not assert** — then `Q_flux^† Q_flux = A^+` where
`A := d_1 P_h d_1^dagger`, and (F-b) would reduce to `A ∝ G_(2,ell)^(-1)`: a finite
linear-algebra question on `F_phys`, decidable exactly. **I did not evaluate it and this
relay bars evaluating it.** I record it because it converts an open question into a
checkable one the moment `R_record,L`'s form is sealed.

### 3.3 What a structural settlement would require

**Path A — a factorization derivation.** Derive that `Q_flux` is a conformal isometry
in the sense above. Requires a **sealed structural property of `R_record,L`'s form** on
connection tangents. [PROVABLE, negative, searched space as §1.7.2] `V011` supplies
`R_record,L` as a *definition* — a second derivative of a normalized record functional
after a limit — and I found no sealed statement of its **form** anywhere in the 1,958
files searched. **That is a SPEC GAP**, and by the Builder-B law it is a gap to report,
not a detail to go looking for in a producer's code.

**Path B — a sealed domain restriction.** Seal the set `D` of pairs the criteria
actually evaluate and prove agreement on `D` only (reading (F-c)). Requires an
**enumeration of the pairs the sealed criteria consume**. [PROVABLE, negative, same
searched space] No such enumeration is sealed. **Second SPEC GAP** — and it is the one
that makes outcome 3 live rather than hypothetical, because without `D` the question
cannot even be asked in its material form.

**Neither path is available today.** Both are blocked on an object that does not exist
in sealed form, not on an unperformed computation.

### 3.4 The three outcomes, and what each would mean

| Outcome | Meaning | Consequence |
|---|---|---|
| **FACTORS** — `mathcal_K_L = c_L⟨·,·⟩_(2,ell)` | The 32-dimensional assignment freedom is invisible to every criterion that consumes the response | **The freedom is gauge everywhere and the thread closes.** No principal ruling, no selector. RA27-2, T_ref's instance transport and J2's `u(e)` already consume the energy (`CVER` §3.2); the response would join them. The scope flags of §2.4 would still have to be closed, since "factors" must hold over **both** moves' free spaces, not only the simplicial one |
| **DOES NOT FACTOR** | Equal-energy refined configurations give **different** responses; the response is not a function of the record's measure | **The assignment is physical**, and a **principal ruling or a sealed selector becomes necessary BEFORE any response evaluation** — otherwise the response value is a function of an unmade choice. Two constraints on any such selector: the **VOID CONDITION** bars choosing it from a desired coefficient, and the only sealed clause that would single out `x_F` — C_ref's *"preserving the same smooth coframe and connection"* — is **barred as a source**, with DoR-007 foreclosing the lift. So the selector would have to be **built, not found** |
| **UNDETERMINED** | Neither path in §3.3 can be run because `R_record,L`'s form and the evaluated-pair set `D` are both unsealed | **The S5.3-style burden carries**: an instantiated coefficient limit must **prove** (H1)/(H2)-class convergence, never assume it — and here it would additionally have to prove the pullback's form rather than assume it factors. This is the status **today**, on the two spec gaps of §3.3 |

[YOURS] **Where it sits upstream.** `CVER` §3.2 recorded that the response sits upstream
of the coefficient: fixing the assignment for the response settles the chain, while
fixing only the coefficient does not. That ordering is what makes this the next question
rather than a side one — the three consumers that take the energy are already safe, and
the one that takes the assignment gates everything downstream of it.

### 3.5 What I do not do here

I do not evaluate `mathcal_K_L`. I do not compute `A`, `G_(2,ell)`, or any spectrum. I
do not claim the response's dependence is material, and I do not claim it is not. I do
not rank the three outcomes by likelihood — the conditional reduction in §3.2 leans one
way and it leans on an unsealed hypothesis, which is not evidence.

```text
MATERIALITY = typed.  Criterion stated: Q_flux^dagger R_record,L Q_flux = c_L G_(2,ell).
              Two settlement paths named, BOTH blocked on sealed-object gaps.
              Three outcomes stated.  Nothing evaluated; nothing adopted.
```

---

## 4. GROUNDING, JURISDICTION, VERB AUDIT

### 4.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | RA27-2's five demands and both modality clauses | `74bbb7aa…[44324,44664)` | PROVABLE |
| 2 | RA27-3's seven fields, procedure, DoR-007 bar | `74bbb7aa…[44665,45007)` | PROVABLE |
| 3 | C_ref's three frozen constituents | `aa7c6d49…[46882,47019)` | PROVABLE |
| 4 | `Ref_0`'s arrows; BUILT / TYPE-P | `8dd59b35…[11610,11743)`, `[12001,12049)` | PROVABLE |
| 5 | The third constituent barred as a source, TYPE-R | `430f0971…[20690,20930)` | PROVABLE |
| 6 | Directedness ADOPTED, "not derived", recorded wherever used | `31e42812…` FC-1 | PROVABLE |
| 7 | Functoriality was the blocker "before RA27-2's discharge" | `72894c1c…` | PROVABLE |
| 8 | Both moves' exact cell counts and four-volume additivity; `Z` named a common refinement | `08b91543…[19632,19996)` | PROVABLE |
| 9 | Per-cell contribution `V_cell Σ F²`; measure formula and quantifier | `aa7c6d49…[46074,46387)`, `[45718,46068)` | PROVABLE |
| 10 | Corrected verdict: forced = the energy, on the whole equal-energy set | `c854c8b2…[21874,…)` | PROVABLE |
| 11 | The certificate/proof fork flagged, never decided | `fd6c774c…[1261941,1262121)` | PROVABLE |
| 12 | The response form `mathcal_K_L = R_record,L(Q_flux ξ, Q_flux ζ)` | `aa7c6d49…[45361,45440)` | PROVABLE |
| 13 | `Q_flux` is the unique horizontal minimum-norm lift; `d_1 Q_flux ξ = ξ` | `aa7c6d49…[44961,45186)`, `[44801,44955)` | PROVABLE |
| 14 | `R_record,L` is an intensive Hessian evaluated after the limit | `aa7c6d49…[43562,43760)` | PROVABLE |
| 15 | Completeness census: no move outside `{A0,A1,A2}` in 1,958 searched files | §1.7.2 | PART-PROVABLE |
| 16 | (F-a) is dead on type; (F-b) is the operative reading; the conformal-isometry criterion | §3.2 | YOURS |
| 17 | Kuhn-stability lemma sketch | §1.7.1 | YOURS, **unverified, not booked** |

**Pin check: 17/17.** Every span re-read from bytes this relay against the stated
digest; all V011 spans against `aa7c6d49…` and not the same-named top-level file.

### 4.2 Jurisdiction check

**On booking 5b a stop rather than a certificate-discharge.** *What was the rule written
to protect?* D012's "prove" protects against a generator set that silently omits a move
the program later needs — the risk is live, since C_ref's "with" is an enumeration and
not a closure declaration. *Does the outcome space distinguish false from cannot-see?*
Yes, and this artifact returns the third: the census is a certificate, the proof is
absent, and I say which. *Would it permit the evidence to appear if the theory is
right?* Yes — a decided fork or a completeness proof discharges it immediately, on the
display already given.

**On the VOID CONDITION.** No topology, index, rule, branch or selector is chosen here
from a desired coefficient. §3.4 in fact records the void condition as a **constraint on
any future selector**, which is the opposite of exercising it. No downstream numeric was
consulted; nothing physical was evaluated.

**On R9 / R9-JII.** Untouched. The carrier stays PENDING on its common-cell quantifier;
nothing here makes the junction test runnable, and §2.4's flags do not move it.

### 4.3 Self verb audit — **CLEAN, with five disclosures**

1. **I did not book the discharge, and the relay's own header says "DISCHARGED."** The
   task text authorises the stop and I took it. Recording that the header pointed one way
   — a producer-declared object may accuse, never exculpate, and it may not carry a
   criterion's direction either.
2. **The `red refinement` count was my own false positive** — 7 files, all substring
   artifacts of `shared refinement` / `declared refinement`. Found by reading contexts,
   not by counting. Seventh consecutive relay in which a search-shaped defect surfaced in
   my instruments; the difference this time is that the meaning-probe rule from 733
   caught it **before** it reached a claim.
3. **`edge collapse` nearly became a missing generator.** It is V009's record-category
   morphism, not a cellulation move. Nine files, and the count alone would have supported
   the wrong finding.
4. **The V011 name collision caught me mid-relay** — my first lookup returned the
   unsealed 84,987-byte top-level copy. The digest caught it; the filename would not
   have. §0.2 records it as an archive hazard.
5. **§1.7.1's Kuhn-stability argument is mine and unverified.** It is offered as a
   pointer for a verifier and explicitly not booked. Even if it holds it covers one pair
   and would not remove the adopted axiom.

One thing I owe the record positively: **the `Z = A2∘A1` witness in `MAJ` was already
sealed and I had cited that exact span twice before** — at 731 for additivity and at
738 for the construction — **without noticing it also witnesses reachability.** The
finding was in bytes I had read. That is the same failure mode as 727, where the
refutation sat in a file I had already cited for a different sentence.

---

```text
RA27_2 = STOPPED (demand named: generator completeness, 5b — at D012's PROVE
    standard; the certificate/proof fork is flagged at REG and undecided of record).
    Demands 1, 2, 4 SUPPLIED; demand 3 SUPPLIED AS ADOPTED; demand 5a reachability
    SUPPLIED AS ADOPTED with one sealed witness (Z = A2 o A1, MAJ [19632,19996));
    demand 5b CERTIFICATE ONLY over 1,958 searched files.  The blocking precondition
    of record — the measure's refinement functoriality — IS SUPPLIED, cross-family
    stable, branch-ruling-free.  One principal ruling on the fork closes RA27-2
    either way, on the display in section 1.
RA27_3 = opened (demand restated at 74bbb7aa...[44665,45007)).  Domain supplied:
    A0/A1/A2 + composites + P2 + adopted directedness.  volume SUPPLIED (new);
    incidence PARTIAL, generators only; coframe, connection, degree, support,
    current density NOT SUPPLIED — and its modality column is DERIVATION with no
    adoption clause, while the only sealed transport for coframe/connection is the
    BARRED third C_ref family.  Scope flags carried: the untested cubical free
    space; the open simplicial assignment where the face response is concerned.
MATERIALITY = typed (settlement path + three outcomes stated).  Criterion:
    mathcal_K_L factors through the energy iff Q_flux^dagger R_record,L Q_flux
    = c_L G_(2,ell), i.e. iff Q_flux is a conformal isometry.  NOT kinematic —
    R_record,L is a Hessian of Gamma_K/N_4 evaluated after the limit.  Path A
    (factorization derivation) blocked: R_record,L's FORM is unsealed — SPEC GAP.
    Path B (sealed domain restriction) blocked: the evaluated-pair set D is
    unsealed — SPEC GAP.  Outcomes: FACTORS = freedom is gauge everywhere, thread
    closes; DOES NOT FACTOR = assignment is physical, a principal ruling or a
    sealed selector is required BEFORE any response evaluation, and the void
    condition plus the C_ref bar mean such a selector must be built, not found;
    UNDETERMINED = the S5.3-style burden carries, which is the status today.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+5 disclosures at section 4.3)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
