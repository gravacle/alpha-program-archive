# STAGE 8 TASK 5 / EQ6 — JOINT FINITE ASSEMBLY, STAGE 1 — LANE 2 V001

```text
ARTIFACT_TYPE = FINITE_CONSTRUCTIVE_BUILD
REGISTER_HEAD_CHECKED = Q-456
SOURCE_EXHIBITION = STAGE8_TASK5_EQ6_AXIOM_V002_AND_EXHIBITION_LANE2_V001.md
SOURCE_EXHIBITION_SHA256 = a681c784b451790c1163d083865988d2256170d1f0c468609b9a803864a0ab4b

BUILD_SCOPE = full_live_tower I_F
COMPLETED_OBJECT_USED = false
COMPLETED_EXISTENCE_AXIOM_INVOKED = false
MEMBER_SELECTED = false

PACKAGE_FUNCTOR = BUILT
FULL_FINITE_BOTTOM = BUILT
J4_J12_J15 = CLOSED
CLASH_FOUND = none

STAGE_2_REMAINDER = physical_J2 + joint_J1-J15_overlap_diamonds

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Preflight and register sweep

### 1.1 Three-line preflight

```text
DOES_THE_OBJECT_EXIST?
  No prior full `I_F -> FinPackage_020` functor or package-wide finite bottom
  exists.  Q-455/Q-456 name this exact absence.

IS_THE_VERSION_CURRENT?
  Yes, through register head Q-456.  Q-456 requires the full functor to carry
  the retained R1 action/Hessian legs; a bare categorical skeleton is not the
  commissioned object.

ARE_ITS_INPUTS_PRESENT?
  Yes.  The full finite object family, actual surface arrows, component maps,
  W3 old-sector restrictions, DoR-017 finite-stage action/Hessian cube,
  DoR-020-A1 bundle/path laws, and exact finite shadows are present.
```

The target filename did not exist at preflight. The source exhibition hash
was recomputed and matched. The sealed questions-settled register verified
and ended at Q-456.

### 1.2 Prior-construction sweep

| Settled object | What exists | Why this build is not duplication |
|---|---|---|
| Q-313 Map 1 | `T_N^char = Hol_c^{-1} o Z_N` and its scalar/cycle restriction scope | no R1 action/Hessian coordinate, C2/C3 package transport, or package bottom |
| Q-315 squares | exact P2/raw-G/Map-1/G007 finite response restrictions | not one DoR-020 package morphism on all `I_F` arrows |
| pass-1 subpackages, `452f1bb8…` | genuine Q-408, Ref_path C1, finite faithfulness, C2 zero cocycle, and finite Ward pieces | maps have different maximal scopes and do not form one functor |
| bedrock graph, `288a3d41…` | six independent roots and dependency order | proves non-implication; it supplies no missing joint map |
| DoR-017 square V004, `abf6d36…` | `rho_Gamma,N` and `rho_H,N` on finite-stage/R5-generated Hessian scope | does not by itself act on every Ref_path/flip arrow |
| Q-438–Q-444 physical constructions | actual Q-408 flip/excision component faces | one generator's physical faces, not the full finite package |
| Q-451 scoped audit | internal flip restrictions | expressly records the package-strength bottom as absent |

No prior artifact has the target signature. This artifact uses the existing
component maps and constructs only their missing joint finite transport.

## 2. Types and variance

### 2.1 The full live category is unchanged

`I_F` is exactly the exhibition's full actual finite consumer/refinement
category. Its objects include all sealed active stages and every covariance,
reality, frame, bundle-gauge, orientation, Ref_path, flip, common-refinement,
and finite-consumer image. Its arrows are generated by:

```text
(i)   identities and admitted surface isomorphisms;
(ii)  W3/DoR-008 finite restrictions;
(iii) rank-preserving physical path subdivisions;
(iv)  cycle-creating flip attachments;
(v)   actual disjoint/contact common refinements;
(vi)  every finite-bottom arrow used by a sealed consumer.
```

No object or arrow is removed to make a component map total.

### 2.2 A morphism in `FinPackage_020`

The apparent variance is resolved at the type, not by reversing an arrow.
For an actual refinement arrow `f:n -> m`, a finite-package morphism is a
structured pair:

```text
forward geometric legs:
  i_f^Y       : Y_n -> Y_m              old physical carrier inclusion,
  j_f^H       : H_n -> H_m              test/profile transport,
  S_f^J       : J_n -> J_m              conserved-current transport,
  f_base      : M_n -> M_m              actual surface map,
  tilde_f     : P_n -> P_m              U(1)-bundle lift;

backward physical/restriction legs:
  r_f^Y       := (i_f^Y)^dagger          W3 old-sector restriction,
  rho_f^Gamma : Adm_fin(m) -> Adm_fin(n),
  rho_f^Hess  : Hess_fin(m) -> Hess_fin(n),
  rho_f^C2, rho_f^C3, rho_f^R2, rho_f^A,
  rho_f^bot   : finite shadows at m -> finite shadows at n.
```

Here

```text
Y_n = C_prop,n direct_sum K_cycle,n.
```

For rank-preserving arrows, `i_f^Y` is the ratified isometric
identification. For cycle-creating arrows it is the isometric inclusion of
the old sector furnished by OLD_FID and the adopted local/bundle geometry.
The target's new-cycle complement remains a target coordinate. The adjoint
restriction forgets it only when returning to the old stage; no upward map,
quotient, or deletion at the target is asserted.

Composition in `FinPackage_020` composes forward legs in arrow order and
restriction legs contravariantly. Thus an ordinary covariant functor into
this structured category lawfully carries both variances.

### 2.3 Symbol ledger

| Symbol | Meaning |
|---|---|
| `Adm_fin(n)` | the entire retained finite R1 action family at stage `n`, never a selected member |
| `Gamma_m` | a variable member of `Adm_fin(m)` |
| `Kern_n^fin` | actual Q-408 finite current/kernel map |
| `C2_n^fin` | finite response/contact/Ward boundary datum |
| `C3_n^fin` | finite Ward and finite Maxwell-symbol datum, not a completed Hodge object |
| `Bot_n` | the one sealed finite-shadow package at stage `n` |
| `v_f` | target vertical/new-cycle coordinate relative to `i_f^Y(Y_n)` |

No symbol denotes a completed carrier, a completed member, or a physical
reader.

## 3. C1 — construction of the full package functor

### 3.1 Object assignment

For every `n in Obj(I_F)`, set

```text
F_fin(n) := (
  Adm_fin(n),
  Kern_Q408^fin(n),
  C1_core^fin(n),
  Faith_fin(n),
  C2_fin(n),
  C3_fin(n),
  R2_alg^fin(n),
  A_fin(n),
  Where_A1^fin(n),
  Bot_n
).
                                                        (C1-1)
```

This is the exhibition's `F_actual` tuple at `n`; no object component is
changed.

### 3.2 Retained R1 action-family leg

For every generator `f:n -> m`, define the action restriction on the whole
family by old-sector pullback:

```text
rho_f^Gamma(Gamma_m) := Gamma_m o i_f^Y.          (C1-2)
```

This is well typed on each generator:

1. **W3/DoR-008 restriction.** On an ordinary finite restriction, `(C1-2)`
   is DoR-017 R2's coordinate restriction `rho_Gamma,N`.
2. **Ref_path.** The path subdivision is rank preserving; the actual carrier
   map is an isometric identification, so `(C1-2)` is the unique pullback of
   the finite action coordinate.
3. **Flip/cycle creation.** `i_f^Y` embeds the old sector with new-cycle
   coordinates left in the target. `(C1-2)` restricts a target action to its
   old-image section. It does not extend an old action upward and does not set
   the target vertical action to zero.
4. **Isomorphism/covariance arrows.** The same formula is the signed
   realization pullback already required by R1-COV.
5. **Bundle/frame/reality arrows.** Equivariance of `i_f^Y` makes pullback
   commute with the U(1), frame-torsor, orientation, and antilinear reality
   actions.

The image remains in `Adm_fin(n)`: active sections satisfy

```text
i_f^Y(S_n) subseteq S_m,
```

so every sealed finite jet, the `h=0` anchor, action units, reality,
batching, quotient compatibility, and the R2 vertical-cocycle condition
pull back unchanged. The map acts on the full family and therefore selects
no representative.

### 3.3 Hessian leg

For every R5-generated finite Hessian, define

```text
rho_f^Hess(D^2 Gamma_m)
  := (i_f^Y)^dagger (D^2 Gamma_m) i_f^Y
   = D^2(rho_f^Gamma Gamma_m).                    (C1-3)
```

Equation `(C1-3)` is the DoR-017 definition of `rho_H,N`, extended along the
actual old-sector inclusion. It preserves the `CC`, `CK`, `KC`, and `KK`
block typing because `i_f^Y` respects the ratified `C_prop direct_sum
K_cycle` decomposition and the R4 unit seam. It also gives the automorphism
cube

```text
rho_(sigma f sigma^-1)^Hess alpha_H,sigma
  = alpha_H,sigma rho_f^Hess.                     (C1-4)
```

For a cycle-creating arrow, `(C1-3)` is a compression to the old sector,
not inversion on the new cycle and not an upward transport. The target
vertical Hessian blocks remain part of `F_fin(m)`.

### 3.4 C2 response-boundary leg

For a finite bilocal/current kernel `K_m`, define its old-image restriction
by

```text
(rho_f^ker K_m)(H_n,J_n)
  := K_m(j_f^H H_n, S_f^J J_n).                   (C1-5)
```

The actual Q-408 square yields

```text
rho_f^ker Kern_m^fin = Kern_n^fin.                (C1-6)
```

Define the response/contact transport `rho_f^C2` by the same test/current
pullback, including the endpoint and bundle data supplied by DoR-020-A1.
Its discrepancy cocycle is

```text
beta_f := rho_f^C2(C2_m^fin) - C2_n^fin = 0       (C1-7)
```

on the old image. For a cycle-creating arrow, `(C1-7)` says the old response
restricts exactly; it says nothing false about the target-only new-cycle
component. The latter remains visible in `C2_m^fin`. The local orthogonal
excision certificate ensures contact and disjoint support pieces do not leak
into the old response coordinate.

The definition extends the pass-1 `Ref_path` zero cocycle to every generator
of `I_F`: W3 restrictions use the sealed response square; flips use OLD_FID,
exclusive-region RNL, and LR; common refinements use the already-proved
path/current and bundle diamonds.

### 3.5 C3 finite Ward/Maxwell-symbol leg

Write a finite C3 datum as

```text
C3_n^fin = (Ward_n^fin, sigma_n^fin),
```

where `sigma_n^fin` is only the finite local symbol carried by the actual
kernel. Define

```text
rho_f^C3(Ward_m^fin, sigma_m^fin)
 := (
      (j_f^H)^* Ward_m^fin S_f^J,
      (j_f^fld)^* sigma_m^fin j_f^fld
    ).                                             (C1-8)
```

Here `j_f^fld` is the support-local field/test transport derived from the
adopted U(1)-bundle lift. The pullback-bundle isomorphism and
characteristic-class law give its identity and composition certificates.
Finite Ward annihilation is preserved exactly:

```text
Kern_n^fin(H_n)(u_n,d beta_n)
 = Kern_m^fin(j_f^H H_n)(S_f^J u_n,d(j_f beta_n))
 = 0.                                             (C1-9)
```

On a flip, target-only cycle-symbol data stay at the target. On a
rank-preserving subdivision, `(C1-8)` is the pass-1 transported finite
symbol. No closed-range, Hodge, or completed Maxwell conclusion is made.

### 3.6 Remaining component legs

The components already total on the required finite arrows are installed
without alteration:

| Component | Arrow leg in `F_fin(f)` | Provenance |
|---|---|---|
| `Kern_Q408^fin` | `(j_f^H,S_f^J,rho_f^ker)` | actual Q-408 square |
| `C1_core^fin` | pullback seminorm/topology core on the actual old image | pass-1 orbit bounds + fixed finite topology |
| `Faith_fin` | isometric transport of the faithful finite representation | finite separation and Ref_path/flip OLD_FID |
| `R2_alg^fin` | precomposition with the algebraic finite-profile map, carrying the whole `(chi_K,T)` family | Q-407 algebraic reader family; no reader member |
| `A_fin` | the same family pullback as `(C1-2)`, preserving `h=0` | DoR-017 R2/R5 and the finite Shape-K anchor |
| `Where_A1^fin` | actual path/current transport plus smooth full-rank U(1)-bundle pullback | DoR-020-A1 |

### 3.7 The package morphism

For each generator, set

```text
F_fin(f) := (
  i_f^Y,r_f^Y,j_f^H,S_f^J,f_base,tilde_f,
  rho_f^Gamma,rho_f^Hess,rho_f^ker,
  rho_f^C1,rho_f^Faith,rho_f^C2,rho_f^C3,
  rho_f^R2,rho_f^A,rho_f^Where
).                                                 (C1-10)
```

Every coordinate is defined on the same `f`; this is the missing common
arrow-domain object.

### 3.8 Identity and composition

For the identity arrow, every forward leg and every pullback is identity, so

```text
F_fin(id_n)=id_(F_fin(n)).                         (C1-11)
```

For `n -f-> m -g-> l`, actual geometry gives

```text
i_(gf)^Y=i_g^Y i_f^Y,
j_(gf)^H=j_g^H j_f^H,
S_(gf)^J=S_g^J S_f^J,
tilde_(gf)=tilde_g tilde_f.                       (C1-12)
```

Hence

```text
rho_(gf)^Gamma=rho_f^Gamma rho_g^Gamma,
rho_(gf)^Hess =rho_f^Hess  rho_g^Hess,
rho_(gf)^C2   =rho_f^C2    rho_g^C2,
rho_(gf)^C3   =rho_f^C3    rho_g^C3,              (C1-13)
```

and similarly for every other restriction leg. Equations `(C1-12)` and
`(C1-13)` compute both routes around the rank-preserving, disjoint-flip,
contact-flip, and mixed subdivision/flip **transport** diamonds. Therefore

```text
F_fin(gf)=F_fin(g) F_fin(f).                      (C1-14)
```

This closes the functorial transport diamonds. It does not pre-claim the
later joint J1–J15 equalizer diamond, whose physical J2 face is absent.

## 4. C2 — the full finite bottom

### 4.1 Bottom object

At every active finite stage `n`, define one bottom package

```text
Bot_n := (
  Gate1_n, Gate2_n, Gate3_n, Gate4_n,
  Q243_n, Q279_n, Q309_n,
  Q408_n,
  ActionBottom_n,
  WardBottom_n,
  ReaderCoreBottom_n,
  WhereBottom_n
),                                                 (C2-1)
```

with a coordinate present exactly where that sealed consumer is defined.
This is a tagged product, not an identification of source, action, kernel,
or reader types.

### 4.2 Package-wide bottom map

Define one map

```text
rho_n^pkg : F_fin(n) -> Bot_n                    (C2-2)
```

by the following simultaneous coordinates:

```text
Adm_fin/A_fin      -> independently ratified finite action bottom and jets;
Kern_Q408^fin      -> actual finite current/kernel and Gate-4 cycle quotient;
C1_core/Faith_fin  -> finite seminorm, bound, and separation shadow;
C2_fin             -> finite response/contact data and Q-309 zero cocycle;
C3_fin             -> finite Ward and finite-symbol shadow;
R2_alg^fin         -> full normalized algebraic reader family shadow;
Where_A1^fin       -> actual finite path/current/bundle restriction.
                                                        (C2-3)
```

No coordinate is manufactured from another. In particular, the algebraic
reader does not define a physical reader, and the finite source functional
does not define the independent physical action bottom.

### 4.3 Exact naturality square

For every `f:n -> m`, the required square is

```text
F_fin(m)  --rho_m^pkg-->  Bot_m
   | F_fin(f)                | Bot(f)
   v                         v
F_fin(n)  --rho_n^pkg-->  Bot_n,

rho_n^pkg o F_fin(f) = Bot(f) o rho_m^pkg.       (C2-4)
```

The equality is exact coordinate by coordinate:

1. **Gate 1–4/current quotient:** actual incidence, endpoint, conserved
   current, and Gate-4 quotient restrictions commute by DoR-015 W3 and the
   Q-408 square.
2. **Action/Hessian:** `(C1-2)`–`(C1-3)` agree with the independently
   instantiated DoR-017 finite bottom on the finite action-jet quotient.
3. **Q-243:** the sealed finite active retarded block and its zero/mixing
   structure are reproduced, not inferred from a completed object.
4. **Q-279:** every probe-on finite table and dephasing/source pattern is
   reproduced by the same coordinate restrictions.
5. **Q-309:** the exact finite kernel and response-mixing zeros are preserved
   by `(C1-5)`–`(C1-7)`.
6. **Ward/symbol:** equation `(C1-9)` gives the same sealed finite Ward
   shadow after either route.
7. **Reader and where-data:** normalized algebraic restriction and the A1
   pullback-bundle square commute independently.

Thus `(C2-4)` covers every sealed consumer in item (vi) of `I_F`; no consumer
outside `I_flip` is dropped.

### 4.4 Exactness and tail

`rho_n^pkg` is a product of already exact finite reducts. Therefore

```text
APPROXIMATION_USED = false;
LIMIT_USED = false;
COMMON_FINITE_RESTRICTION_TAIL_CREATED = {0};
TARGET_NEW_CYCLE_CONTENT_DELETED = false.         (C2-5)
```

For a cycle-creating arrow, the bottom at `m` records the new-cycle finite
shadow. Restriction to `n` omits that target-only coordinate in the lawful
old-stage reduct; it does not identify or erase it at `m`.

## 5. C3 — J4/J12/J15 closure

### 5.1 J4

The previously stage-scoped R1 family now has `(C1-2)` and `(C1-3)` on every
generator and composite of `I_F`. R1-COV, reality, batching, units, and the
Hessian cube commute by `(C1-4)` and `(C1-13)`. No member is selected.

```text
J4 = CLOSED_BY_FULL_FAMILY_ACTION_HESSIAN_PULLBACKS.    (C3-1)
```

### 5.2 J12

Equations `(C1-5)`–`(C1-9)` install response/contact/Ward naturality on the
same package arrows. Their common-refinement composition is `(C1-13)`.

```text
J12 = CLOSED_BY_FULL_PACKAGE_RESPONSE_LEGS.             (C3-2)
```

### 5.3 J15

Equation `(C2-4)` is the one package-wide exact finite bottom square.

```text
J15 = CLOSED_BY_rho_pkg.                                (C3-3)
```

### 5.4 What remains

Stage 1 does not construct the physical equality

```text
reader = pi_Mx o Loc o Kernbar o Q.
```

That is J2 and remains open. The transport diamonds of the finite functor
commute, but the **joint J1–J15 equalizer diamonds** cannot close until the
independent physical J2 face and its J7 comparison are inserted on the same
shared tuple.

```text
FC2 = REPAIRED_BY_STAGE_1;
FC3 = REPAIRED_BY_STAGE_1;
J4  = CLOSED;
J12 = CLOSED;
J15 = CLOSED;

STAGE_2_REMAINDER:
  physical J2;
  J7 as a physical-reader equation;
  the simultaneous joint overlap diamonds.
```

No axiom or completion is invoked by these finite statements.

## 6. C4 — clash audit

| Potential clash | Both existing/new routes computed | Result |
|---|---|---|
| ordinary finite action restriction | `(C1-2)` equals DoR-017 `rho_Gamma,N` | **AGREE** |
| finite Hessian restriction | `(C1-3)` equals `D^2(rho_Gamma,N Gamma)` and the R1-COV cube | **AGREE** |
| Ref_path action/Hessian | no prior action leg existed; new pullback is licensed by the actual isometric carrier map | **NO CLASH** |
| flip action/Hessian | no prior upward action map is used; downward old-sector pullback preserves target vertical content | **NO CLASH** |
| C2 Ref_path | new general formula reduces to the pass-1 exact kernel transport and `beta=0` | **AGREE** |
| C2 flip/contact | old-image restriction uses OLD_FID + exclusive-region RNL + LR | **AGREE** |
| C3 finite Ward | `(C1-9)` reproduces the sealed annihilation | **AGREE** |
| package bottom | every shared coordinate is the already-sealed finite shadow | **AGREE** |
| mixed subdivision/flip | both routes reduce to composite actual embeddings and bundle pullbacks | **AGREE** |

No existing componentwise objects force unequal values on an overlap.

```text
CLASH_FOUND = none
HONESTY_STOP_TRIGGERED = false
```

## 7. C5 — regressions, falsifiers, and anti-tuning

### 7.1 Nine geometric regressions

| Regression | Execution on `F_fin` | Result |
|---|---|---|
| pure new-cycle profile | target vertical coordinates remain in `F_fin(m)` and `Bot_m`; only the old-stage pullback omits them | **PASS** |
| covariance orbit/moduli | every action, reader, bundle, orientation, and frame family is transported whole | **PASS** |
| all-stage skeleton | all objects/arrows of `I_F`, including item (vi) consumers, are retained | **PASS** |
| rank-preserving `Ref_path` | isometric `i_f`, canonical bundle pullback, and the exact Q-408 square give `(C1-12)` | **PASS** |
| cycle-creating upward quotient | none is defined; all new action/kernel/cycle data remain target data | **PASS** |
| arbitrary-profile restriction | `(C1-5)` is old-image restriction for every profile; the target new-cycle kernel is retained | **PASS WITH LAWFUL SCOPE** |
| Q-430 old-to-new Riesz mixer | rejected because it violates relative no-leakage/local range and cannot be an `i_f^Y` leg | **PASS — REJECTED** |
| Q-432 `P=id` overreach | admitted on rank-preserving arrows without imposing global orthogonality | **PASS — ADMITTED** |
| Q-435 bundle/topology attack | smooth full-rank lift, pullback-bundle isomorphism, units, and characteristic class are required in `F_fin(f)` | **PASS — ILL-TYPED MAP REJECTED** |

### 7.2 Permanent joint-package attacks

| Attack | Execution | Result |
|---|---|---|
| abstract kernel | `(C1-5)` uses the actual Q-408 kernel and actual test/current maps | **PASS** |
| circular physical reader | no `pi_Mx` is defined; J2 remains open | **PASS** |
| false nonemptiness | no completed or joint-equalizer inhabitance is asserted | **PASS** |
| Hodge from finite Ward | C3 remains finite-symbol/Ward data only | **PASS** |
| objectwise selection | every map acts on the full family; no member, basis, frame, gauge, path, or rank is selected | **PASS** |
| marginal-to-joint promotion | functorial transport is distinguished from the still-open J1–J15 equalizer | **PASS** |
| source/action relabeling | independent action bottoms remain independent; source data do not define them | **PASS** |
| scope shrink | `I_F` is unchanged and includes every active consumer | **PASS** |
| downstream target tuning | all maps are induced by carrier/support/bundle restriction before J2 or any downstream computation exists | **PASS** |

### 7.3 Surface geometry versus rails

```text
SURFACE_GEOMETRY:
  the actual incidence/current maps, old-sector inclusions, test transport,
  support, U(1)-bundle lifts, and characteristic-class restrictions license
  every component formula;

RAILS:
  collecting those formulas into one structured functor and one natural
  finite-bottom transformation is categorical bookkeeping;

NEW_PHYSICS_ADDED = false.
```

The construction is not justified by a response, threshold, verdict, or
number. Its only justifications are existing carrier geometry and exact
finite restriction laws.

### 7.4 Anti-tuning order

```text
1  freeze I_F and every actual geometric arrow;
2  freeze W3/DoR-019 old-sector inclusions and A1 bundle lifts;
3  define family pullbacks and kernel/response/Ward restrictions;
4  prove identities and composition;
5  assemble rho_pkg from the already sealed bottoms;
6  only then inspect the J ledger.
```

No downstream consequence was available to choose a clause or map.

## 8. Delta against the componentwise stock

| Stock before Stage 1 | Stage-1 delta | Status |
|---|---|---|
| `Adm_fin` on ratified stage arrows | add family pullback `(C1-2)` on every Ref_path/flip/common-refinement/consumer arrow | **BUILT** |
| `rho_H,N` finite-stage cube | add induced Hessian compression `(C1-3)` on the same full arrow set | **BUILT** |
| C2 exact on component scopes | install actual old-image response/contact transport on every generator and composite | **BUILT** |
| C3 finite Ward on component scopes | install bundle-typed finite Ward/symbol pullback on every generator and composite | **BUILT** |
| remaining components on their proved arrows | package their existing maps without changing them | **CARRIED** |
| no common package morphism | construct `F_fin(f)` with every coordinate | **BUILT** |
| component finite shadows | construct one natural transformation `rho^pkg:F_fin=>Bot` | **BUILT** |
| J4 stage-scoped | full action/Hessian family naturality | **CLOSED** |
| J12 component-scoped | full finite response naturality | **CLOSED** |
| J15 component bottoms | one exact package bottom | **CLOSED** |
| physical J2 absent | unchanged | **OPEN / STAGE 2** |
| joint equalizer diamonds absent | unchanged beyond transport-functor diamonds | **OPEN / STAGE 2** |

Nothing else in `F_actual` is modified.

## 9. Final board

```text
F_ACTUAL_JOINT_FINITE_PACKAGE_CERT_STAGE1 = BUILT

PACKAGE_FUNCTOR = BUILT
FULL_FINITE_BOTTOM = BUILT
J4_J12_J15 = CLOSED
CLASH_FOUND = none

FULL_LIVE_TOWER = I_F / unchanged
SCOPE_SHRINK = false
MEMBER_SELECTED = false
COMPLETED_OBJECT_USED = false
COMPLETED_EXISTENCE_AXIOM_INVOKED = false

STAGE_2_REMAINDER = physical_J2
                    + J7_physical_reader_equation
                    + joint_J1-J15_overlap_diamonds

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
