# STAGE 8 / 7A / STEP 8 — THE EMPTY CHAMBER: REFUTED. THE THIRD CONSTITUENT IS INHABITED OF RECORD

Lane: DARIO (Builder B, independent verifier). Relay 746.
Governing: my 742 refutation `863bc606…`; the principal's reframe — *does the class's
third chamber PROVABLY LACK MEMBERS at the record level?*

## Lead determination

**No. The chamber is inhabited, and the members are sealed, explicit, and four in number.**

The emptiness case is genuinely strong and I built it to full strength before testing it:
`constructs smooth` returns **zero** across 1,970 files; the PRPS→Gate-4 localization
bridge is sealed `= false | TYPE-U`; the downstream smooth-connection audit is stamped
`NO CONSTRUCTION`; DoR-007 puts adoption of `(M,g)` *"off the table"* and grounds
base-smoothness as existing *"only as a refinement limit"* whose cellulation-independence
is the theorem's own content. Every one of those holds.

**None of them is the membership question.** They say the smooth limit is not *derived*.
The third constituent does not ask for a derived limit; it asks for a **preserved smooth
coframe and connection**. And sealed stock exhibits both:

- **`V011` itself freezes a flat Lorentzian metric and orientation** — *"Freeze the
  ordinary-branch inputs: `eta=diag(+1,-1,-1,-1), epsilon_0123=+1`"* — 4.5 kB before it
  freezes `C_ref`, in the same sealed principle.
- **A sealed spec exhibits an explicit `C-infinity` connection one-form**, headed
  *"Forward-sealed smooth connection direction"*, with its bump certified smooth in the
  sealed text.
- A second sealed spec exhibits **real-analytic connection histories**, frozen by name.
- A sealed, **executed** certificate **consumes** the frozen `eta`/`epsilon` as data and
  certifies `J_star^2 = -I_6` from them — an object a certificate can lawfully consume is
  an inhabitant of the record, not an absence.

**And the decisive check is internal to my own prior work.** My sealed 738 artifact
`c854c8b2…` §2.2 records that this clause *"kills `x_alt` cleanly"* and *"demands exactly
that global object."* **A clause that discriminates between two concrete configurations
cannot have an empty domain.** I cannot hold both that the clause would exclude `x_alt`
and that it has no members. It has members; what it lacks is permission to be used as a
source.

**The emptiness argument fails as the mirror image of the error I corrected at 742.**
There I showed *barred as a source ≠ excluded from the class*. Here the same distinction,
run the other way, gives *barred as a source ≠ nonexistent*. I wrote that distinction into
the hunt's own instructions and it came back and refuted me with it.

`CHAMBER = INHABITED (member displayed)`. YY1 refutes, so YY2 and YY3 do not execute:
**`COMPLETENESS = n-a`, `RA27_2 = stopped`**, and the three Q-651 routes return to the
principal.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively over the whole archive
(`find . -name 'STAGE8_7A_EMPTY_CHAMBER_DARIO_V001.md*'`): **absent**, artifact and
sidecar both. Nothing overwritten.

### 0.2 Sources verified before use

```text
742    workspace/STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md          863bc606…  SEALED-OK
738    workspace/STAGE8_7A_COUNTEREXAMPLE_CONTEST_DARIO_V001.md      c854c8b2…
V011   workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/
         BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md               aa7c6d49…  packet-manifest sealed
CAR    workspace/STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md
                                                                     789338ad…  SEALED-OK
TPQ    workspace/COMPLETE_QSPEC_TEMPORAL_PLAQUETTE_RESPONSE_DIAGNOSTIC_SPEC_V001.md
                                                                     632aa96a…  sidecar present
CERT   workspace/STAGE8_TASK6_RA25_2_RA27_1_FINITE_CERTS_LANE3_V001.md  7a07d3b8…  SEALED-OK
4d     workspace/STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md 430f0971…
BRIDGE workspace/STAGE8_PRPS_GATE4_LOCALIZATION_BRIDGE_ATTEMPT_V001.md  f651817f…
AUDIT  workspace/STAGE8_DOWNSTREAM_SMOOTH_CONNECTION_REQUIREMENT_AUDIT_V001.md 46bfeb7d…
JII    workspace/STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md          fdf20bd4…
X2     workspace/STAGE8_7A_BRANCH_F_CROSSCHECK_CODEX2_V001.md        97587de8…
DoR007 supervision/DECISION_OF_RECORD_007_SMOOTH_FORK_DERIVE_THE_LIMIT_2026-08-01_V001.md
                                                                     17dc023e…  SEALED-OK
DoR019 supervision/DOR_019_CARRIER_METRIC_AND_UNITS_RATIFICATION_2026-08-03.md  6ab72b0c…
```

Seal checks run **from each file's own directory** (sidecars carry bare filenames; running
`shasum -c` from elsewhere produces false mismatches — the shell defect disclosed at 742).
All `V011` offsets are against the packet copy `aa7c6d49…`, never the longer unsealed
same-named file at `workspace/` top level (`20a3a17d…`).

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.
No numeric evaluation of physical quantities.  No comparison to measured constants.
No common cell formed.  No junction map evaluated.  No response evaluated.
NO SMOOTH DATA DERIVED — and none is derived below.  The members displayed are
  EXHIBITED FROM SEALED TEXT, not constructed here.
Nothing adopted.  No register, plan, tracker, or git action.
```

---

## 1. YY1 — EMPTINESS-OF-RECORD: DERIVED AT FULL STRENGTH, THEN REFUTED

### 1.1 The claim and its logical shape

The proposition to be derived or refuted:

```text
Membership in C_ref's third constituent requires exhibiting the preserved
smooth coframe and connection.  The witness class is unpopulated of record.
Population requires the barred act or 7B's limit.
THEREFORE the chamber is EMPTY OF RECORD.
```

[PROVABLE] The membership condition, `V011` `aa7c6d49…[46772,47023)`: *"…**and common
refinements preserving the same smooth coframe and connection**."* The condition is
**preservation**, and preservation quantifies over a coframe and a connection that must
exist to be preserved.

### 1.2 The case FOR emptiness, built to full strength

I built this before testing it, because a refutation is only worth having if the thing
refuted was strong.

[PROVABLE] **(a) The corpus constructs no smooth limit.** Meaning-probe over the searched
space (§1.2.1): `constructs smooth` = **0 files**; `smooth limit exists` = **0**;
`soldering` = 0; `frame field` = 0; `moving frame` = 0; `connection 1-form` = 0.

[PROVABLE] **(b) `V011` itself does not construct a coframe.** *"coframe"* occurs exactly
**twice** in all of sealed `V011`: at `[46197]` inside *"For a general coframe
`theta^a=e^a_mu dx^mu`, the same map is defined by pulling the bivector through
`wedge^2(e^(-1))`…"* — a **bound variable** in a change-of-frame law — and at `[46996]`,
the membership condition itself. Neither exhibits an object.

[PROVABLE] **(c) The object that would relate discrete refinement to smooth data is sealed
NOT BUILT.** `BRIDGE` `f651817f…`: *"The canonical bridge does **not** build from the
sealed text currently present"*, `canonical_PRPS_to_Gate4_localization_bridge_built =
false | TYPE-U`. `AUDIT` `46bfeb7d…` is stamped *"NO ADOPTION / NO CONSTRUCTION"* and
records *"No such theorem is supplied by the searched corpus."*

[PROVABLE] **(d) The route by adoption is closed.** `DoR007` `17dc023e…`: *"**ADOPTION OF
(M,g) FOR THIS SUBSET IS OFF THE TABLE.**"* and *"base-smoothness exists only as a
refinement limit; the limit's cellulation-independence is the theorem's content."*

[PROVABLE] **(e) The route by derivation-from-the-clause is barred.** `4d`
`430f0971…[20690,20930)`: `C_REF_AS_WRITTEN_DERIVES_SMOOTH_DATA_FROM_GATE4 = false |
TYPE-R`.

**That case is sound, every leg verified from bytes — and it does not establish the
conclusion.** (a)–(e) all speak to whether the smooth limit is **derived**. The membership
condition does not ask for a derived limit. It asks for a coframe and connection that a
refinement can be said to **preserve**. Those are different objects, and §1.3 exhibits the
second kind.

#### 1.2.1 Searched space

Recursive glob `./workspace/**/*.md` + `./supervision/**/*.md` — **1,970 files** —
`*_DARIO_V001.md` excluded so the probe cannot corroborate itself from my own artifacts,
`evaluator_build_A/` code excluded under the independence law.

### 1.3 THE MEMBERS — four, displayed

| # | Object | Pin | Seal |
|---|---|---|---|
| **M1** | **The frozen Lorentzian metric and orientation** | `aa7c6d49…` ~`[51516,51661)` | packet manifest |
| **M2** | **An explicit `C-infinity` connection one-form** | `789338ad…[5206,5918)` | sidecar OK |
| **M3** | **Frozen real-analytic connection histories** | `632aa96a…[1476,1949)` | sidecar present |
| **M4** | **An executed certificate consuming M1 as data** | `7a07d3b8…[3498,3701)` | sidecar OK |

[PROVABLE] **M1 — and it is in `V011` itself, 4.5 kB before the `C_ref` freeze:**

```text
#### Lorentzian Hodge and Maxwell test

Freeze the ordinary-branch inputs

eta=diag(+1,-1,-1,-1),  epsilon_0123=+1,
(*F)_(mu nu)=(1/2)epsilon_(mu nu rho sigma)F^(rho sigma).
```

The verb is **Freeze** — the same performative `V011` uses on `C_ref`. `eta =
diag(+1,-1,-1,-1)` is a constant-coefficient Lorentzian metric; with the Cartesian
coordinates it presupposes, the associated coframe is `dx^mu` and is flat and smooth. This
is the *"frozen metric/orientation"* A27's row says the Lorentzian Hodge matrix is
generated from — and A27's row is a demand, while **this** is the freeze that answers it.

[PROVABLE] **M2 — an explicit smooth connection, sealed, verified verbatim:**

```text
## A2 - Forward-sealed smooth connection direction
…
b_D(t,x) = exp(16-1/s(t,x)),  0<t<1 and s_-(t,x)>0 and s_+(t,x)>0;
         = 0,                 otherwise.

The zero extension is `C-infinity`, compactly supported in the closed
diamond, vanishes to all orders at its boundary and tips…

Use the fixed-gauge, unit-charge spatial connection:
A_x^(a)(t,x)=a b_D(t,x);   A_y=A_z=A_0=0;
```

`s_-(t,x) = t^2-|x|^2` is the flat interval, so the ambient data is Minkowski with its
standard coframe. The section header names it a **smooth connection**; the sealed text
certifies the bump `C-infinity` itself. This is not a demand, a bar, or a wish — it is a
displayed object with a displayed formula.

[PROVABLE] **M3 — a second, independent family:** `TPQ` `632aa96a…[1476,1949)`, *"## P1 -
Frozen endpoint-trivial connection histories"*, `f_n(t) = sin(n pi t/2)/(n pi/2)` with
`f_n(0)=f_n(2)=0` and orthonormal derivatives — real-analytic, frozen by name.

[PROVABLE] **M4 — and one of them is consumed by an executed certificate:** `CERT`
`7a07d3b8…[3498,3701)`, *"[PROVABLE] RA27-1 consumes only the frozen local data: `eta =
diag(+1,-1,-1,-1), epsilon_0123 = +1, …`"*. The certificate generates `J_star` from `eta`
and `epsilon` and certifies `J_star^2 = -I_6`. **An object that a sealed, executed
certificate lawfully consumes as input exists of record.**

[PART-PROVABLE] **Why these inhabit the chamber.** The condition is *preservation*, not
*derivation*. `eta`, its coframe `dx^mu`, and a connection such as M2 or M3 are **ambient
and fixed**: refining a cellulation of the region does not alter them. Two cellulations
and a common refinement of them therefore all carry the same smooth coframe and the same
smooth connection, and the refinement **preserves** them in the clause's own sense. The
machinery for a cellulation to carry a coframe is exhibited in the same file — `V011`
`[46074,46387)`'s pullback by `wedge^2(e^(-1))` times `|det e|` — so nothing further is
needed to state the preservation.

### 1.4 The internal check that settles it

[PROVABLE] My own sealed 738 artifact, `c854c8b2…` §2.2 at `~[10095,10755)`:

```text
### 2.2 The one clause that would kill it — and it is barred

There **is** a clause that kills `x_alt` cleanly. `x_alt`'s per-simplex fields
differ from one another, so the refined configuration is **not** the sampling
of a single global 2-form; `x_F` is. `C_ref`'s third family — "common
refinements preserving the same smooth coframe and connection" — demands
exactly that global object…
```

[YOURS] **A clause with no members excludes nothing.** I recorded, from an exact
computation, that this clause discriminates between two concrete refined configurations —
admitting `x_F`, excluding `x_alt`. That is live discriminating content, and it is
incompatible with an empty domain. Codex 2's sealed cross-check `97587de8…` reaches the
same place independently, saying the clause *"bears directly"* and that if promoted it
*"could exclude the piecewise non-F connection used in the countermodel."* **Two lanes,
including mine, have already used this chamber's occupancy without noticing they were
using it.**

### 1.5 Why the emptiness argument failed, named precisely

[YOURS] It is the **mirror of the 742 error**. At 742 I found I had collapsed *barred as a
source* into *not in the class*, across four relays. The emptiness argument collapses the
same distinction in the opposite direction: *barred as a source* into *not in existence*.
Both collapses delete the qualifier `as a source`, and the sealed text keeps that qualifier
every time it states the bar — `4d`'s block carries `C_REF_USABLE_AS_TARGET_INTERFACE =
true` in the same fence, and FC-1 writes *"barred **as a source**."*

[YOURS] **And I built the guard against this into the hunt's own instructions** — *"barred
as a SOURCE is NOT the same as does not exist … If you find a sealed coframe/connection
that is merely BARRED FOR USE, that is still an INHABITANT"* — then had to be shown the
members by the hunt anyway. The distinction was correct, explicit, and mine, and I still
did not apply it to my own draft conclusion until the witnesses were on the table.

[YOURS] One further reason this needed the hostile check: **the emptiness conclusion would
have booked RA27-2 and unblocked my own lane.** Under the void condition that is precisely
the direction to distrust, and the result came back against it.

### 1.6 Verdict

```text
CHAMBER = INHABITED (members displayed: M1-M4)
```

Per the relay: *"A member found = refutation, displayed … If YY1 refutes: STOP with the
member displayed; the three Q-651 routes return to the principal."*

---

## 2. YY2 AND YY3 — NOT EXECUTED

YY2 (re-run completeness over the inhabited class) and YY3 (book RA27-2) are both
conditioned on YY1 landing. It refutes, so neither executes.

```text
COMPLETENESS = n-a
RA27_2 = stopped
```

**742's position is unchanged and unweakened.** The third constituent is licensed by
`V011`'s freeze, its bar is source-only, and it is **now also shown to be occupied**. The
completeness claim over `{A0, A1, A2}` stays refuted, and the existential/universal gap of
742 §1.7 stays open — with the universal's domain now known to be non-empty, which makes
that gap wider, not narrower.

**The three routes return to the principal** — the same three displayed at my 742 §2, which
the relay names the Q-651 routes: restrict the quantifier and say so aloud (`V011`'s own
pre-registered branch); finish FC-2's half-met sliver trigger; or resolve the C_ref/D3
conflict open since 2026-07-26. **A fourth is now foreclosed:** emptiness-of-record. It was
worth testing and it does not hold.

---

## 3. WHAT THE REFRAME NONETHELESS BUYS

[YOURS] The relay's question was not wasted, and three things are now on the record that
were not before.

1. **The chamber's occupancy is established, not assumed.** Before this relay, no artifact
   had exhibited a member. The clause was treated as a barred abstraction by every lane
   including mine. M1–M4 are now displayed with pins.
2. **The bar's true scope is now bounded from both sides.** 742 showed it does not empty
   the class; 746 shows it does not empty the world. What it does is exactly what it says:
   forbid *deriving* smooth data *from* the clause.
3. **The distance to a legitimate emptiness result is now measurable, and it is not
   zero.** Nothing here shows the members are *usable* — M2 and M3 live on flat
   backgrounds, and whether a member of the third constituent can be exhibited **without
   ambient flatness** is untouched by this relay and is a different question from the one
   asked. If the principal wants that question, it is well-posed and I did not answer it.

---

## 4. THE COMMISSIONED HUNT

[YOURS] Because the claim under test was a **negative existential** — the claim type my
instruments have failed at three times (713's searched space, 716's object names, 733's
predicate phrasing) — I commissioned four blind hunters with distinct modalities (coframe
carriers; connection and metric carriers; the preservation clause itself; the status of
smoothness), each with adversarial verification, denied my own artifacts and the producer's
code.

**It refuted my draft conclusion.** One hunter's chain had returned when I wrote; it
supplied M1–M4, and **I verified all four myself from bytes** — verbatim text, digests, and
seal checks run from each file's own directory — rather than adopting them. Three hunters
and the verification stage were still running at seal time.

**Stated without the comfortable framing this time:** at 742 I wrote that outstanding
hunters *"can only add to an enumeration that is already refuted"*, and that framing
contemplated only the outcome favourable to me. Here the outstanding work could plainly cut
either way — further members would reinforce this verdict, and a verifier that successfully
demolished M1–M4 would reopen the emptiness case. **I did not wait for it**, because the
four members verify from bytes on my own reading and because M4 — an executed certificate
consuming M1 — does not depend on any hunter's judgement. If the verification stage later
kills all four, this artifact is wrong and the emptiness question reopens; that is the
honest exposure and I would rather name it than let a completed-sweep summary imply a
closure I did not have at seal time.

---

## 5. GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | The membership condition is **preservation** | `aa7c6d49…[46772,47023)` | PROVABLE |
| 2 | `coframe` occurs exactly twice in sealed `V011`; both non-exhibiting | `aa7c6d49…[46197]`, `[46996]` | PROVABLE |
| 3 | `constructs smooth` = 0 over 1,970 files | §1.2.1 | PROVABLE |
| 4 | The PRPS→Gate-4 bridge is `false \| TYPE-U` | `f651817f…` | PROVABLE |
| 5 | The smooth-connection audit: `NO CONSTRUCTION` | `46bfeb7d…` | PROVABLE |
| 6 | `(M,g)` adoption off the table; smoothness only as a refinement limit | `17dc023e…` | PROVABLE |
| 7 | The TYPE-R bar, with `C_REF_USABLE_AS_TARGET_INTERFACE = true` beside it | `430f0971…[20690,20930)` | PROVABLE |
| 8 | **M1** — the frozen `eta`/`epsilon`, verb "Freeze", in `V011` | `aa7c6d49…~[51516,51661)` | PROVABLE |
| 9 | **M2** — explicit `C-infinity` connection one-form | `789338ad…[5206,5918)` | PROVABLE |
| 10 | **M3** — frozen real-analytic connection histories | `632aa96a…[1476,1949)` | PROVABLE |
| 11 | **M4** — executed certificate consuming M1, `J_star^2 = -I_6` | `7a07d3b8…[3498,3701)` | PROVABLE |
| 12 | My 738: the clause kills `x_alt` cleanly | `c854c8b2…~[10095,10755)` | PROVABLE |
| 13 | Codex 2: the clause "bears directly"; could exclude the countermodel connection | `97587de8…` | PROVABLE |
| 14 | The members inhabit because ambient data is unchanged by refinement | §1.3 | PART-PROVABLE |
| 15 | R9's discipline: unformed domain ⇒ `UNEXECUTABLE/PENDING`, "neither passed, failed, nor vacuously true" | `fdf20bd4…[58429,58653)` | PROVABLE, **not needed** — §5.3 |

**Pin check: 15/15**, every span re-read from bytes this relay against the stated digest.

### 5.2 Jurisdiction check

**On refuting the reframe the principal proposed.** *What was the rule written to protect?*
The emptiness route would have discharged a universal by declaring its domain unpopulated —
the risk is discharging an obligation by fiat rather than by proof, and it was live. *Does
the outcome space distinguish false from cannot-see?* Yes, and this returns **false with
members displayed**, not cannot-see. *Would it permit the evidence to appear if the theory
is right?* Yes — a single exhibited member decides it, and four appeared.

**On the VOID CONDITION.** The refuted conclusion was the one that would have unblocked this
lane and booked RA27-2 today. Nothing here is chosen from a desired coefficient, and the
result runs against my own interest.

**On the gates.** No smooth data is derived here. M1–M4 are **exhibited from sealed text**;
displaying an object that already exists is not constructing one, and I neither used them as
a source nor lifted any bar.

**On R9 / R9-JII.** Untouched; the carrier stays PENDING on its common-cell quantifier.

### 5.3 Self verb audit — **CLEAN, with four disclosures**

1. **My draft conclusion was EMPTY, and it was wrong.** I had legs (a)–(e) verified and a
   derivation written before the hunt returned. Every leg was true; the conclusion did not
   follow, because all five speak to *derivation* and the clause asks for *preservation*.
2. **It was the mirror of the error I corrected one relay ago**, and I had written the guard
   against it into the hunt's own prompt. Fourth occurrence of this distinction deciding a
   relay — and the first where I supplied the correct rule and still misapplied it.
3. **I prepared an argument I did not need and would have leaned on.** §5.1 row 15 —
   R9's `UNEXECUTABLE/PENDING` discipline — was located and pinned to support YY2. It is
   correct, it is sharper than "vacuously true", and it is **irrelevant now**. I record it
   because it was ready to carry weight in a conclusion that has since failed, and a
   verifier should know it was in hand.
4. **I sealed with three of four hunters and the whole verification stage still running.**
   §4 states the exposure. My reason is that the members verify from my own byte-reading and
   M4 stands independent of any agent's judgement — but the honest label is that this
   artifact is sealed against an incomplete hostile check, and I say so rather than let the
   completion notice imply otherwise.

---

```text
CHAMBER = INHABITED (members displayed) — four sealed witnesses, each verified from
    bytes by this lane rather than adopted from the hunt that surfaced them:
    M1  V011 aa7c6d49...~[51516,51661) — "Freeze the ordinary-branch inputs:
        eta=diag(+1,-1,-1,-1), epsilon_0123=+1" — a flat Lorentzian metric and
        orientation, frozen by the same verb V011 uses on C_ref, 4.5 kB earlier
        in the same sealed principle.
    M2  789338ad...[5206,5918) — "Forward-sealed smooth connection direction",
        A_x = a b_D(t,x) with b_D certified C-infinity in the sealed text.
    M3  632aa96a...[1476,1949) — "Frozen endpoint-trivial connection histories",
        f_n(t) = sin(n pi t/2)/(n pi/2), real-analytic.
    M4  7a07d3b8...[3498,3701) — an EXECUTED certificate consuming M1 as data and
        certifying J_star^2 = -I_6.
    The emptiness case was built to full strength first and every leg verifies —
    constructs-smooth = 0 over 1,970 files; the PRPS->Gate-4 bridge false|TYPE-U;
    the smooth-connection audit stamped NO CONSTRUCTION; DoR-007's (M,g) adoption
    off the table; 4d's TYPE-R source bar. All five speak to whether the smooth
    limit is DERIVED. The third constituent asks for a coframe and connection to be
    PRESERVED. Different objects; the second kind is exhibited.
    DECISIVE INTERNAL CHECK: my own sealed 738 c854c8b2...~[10095,10755) records
    that this clause "kills x_alt cleanly" and "demands exactly that global object".
    A clause with no members excludes nothing.
    The failure is the MIRROR of the 742 error: barred-as-a-source collapsed into
    nonexistent, where 742 collapsed it into not-in-the-class. Same deleted
    qualifier, opposite direction.
COMPLETENESS = n-a
RA27_2 = stopped.  742's refutation stands and is WIDENED: the universal's domain
    is now known non-empty.  The three Q-651 routes return to the principal, and a
    fourth — emptiness of record — is foreclosed.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+4 disclosures at section 5.3, including: my draft
    conclusion was refuted by a hunt I commissioned, using a distinction I had
    written into its own instructions; and this artifact is sealed against an
    incomplete hostile check, stated rather than implied)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
