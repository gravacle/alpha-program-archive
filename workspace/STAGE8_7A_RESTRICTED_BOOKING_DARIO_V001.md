# STAGE 8 / 7A / STEP 8 — THE RESTRICTED BOOKING: RA27-2 AGAINST THE WORKING CLASS

Lane: DARIO (Builder B, independent verifier). Relay 750.
Governing: `DECISION_RESTRICT_AND_SAY_SO_2026-08-08` `0f2d5943…` — route (a) ruled; the
working class declared; the cost stated; the trigger preregistered.

## Lead determination

**RA27-2 BOOKS over the working class, demand by demand, with the restriction on its face.**

All five D012 demands are suppliable under the restriction, and demand 5's completeness —
the one that has stopped this thread since 739 — is **PROVEN at the PROVE standard for the
working class**, not certified. The proof is 742's licensing enumeration restated: within the
working class every licensing act licenses `A1`, `A2` and their composites and nothing else,
because the single act that reached outside is now, **by declaration of record**, outside the
claim's scope.

**What makes this lawful where I refused it at 742.** My 742 §1.11 stated exactly this
scoped result and declined to book it: *"closure relative to a class chosen to make it true …
the shape of argument this program's discipline exists to refuse."* That judgment was
correct **for a lane choosing its own scope.** It does not apply to a principal declaring the
scope as an act of record with the cost riding on the face of every result. The content is
the same; the authority is not. **I book it as declared, and I do not represent it as the
unrestricted completeness D012 asked for.**

**The gap closes by restriction, not by derivation.** 742's existential/universal gap —
FC-1's adopted directedness is existential, A27's quantifier universal — is not resolved
here. A27's universal is *narrowed*. That is precisely the cost, and it is stated aloud.

**One finding the restriction surfaces, flagged and not leaned on:** `V011`'s own naturality
require **survives the restriction intact** — its universal ranges over *"each elementary
refinement"* (`A1`/`A2`, in class) and its common-refinement clause is *existential*. It is
**A27's audit-row restatement** that quantifies universally over *"common-refinement moves"*.
So the declared cost lands on the row's phrasing, not on the source require. I display the
divergence; I do not use it to discount the cost the principal declared.

---

## 0. Preflight

### 0.1 Output name

Probed **before any write**, recursively over the whole archive: **absent**, artifact and
sidecar both. Nothing overwritten.

### 0.2 Sources verified before use

```text
DEC750 supervision/DECISION_RESTRICT_AND_SAY_SO_2026-08-08.md     0f2d5943…  SEALED-OK
D012   workspace/STAGE8_TASK6_A25_A27_SCOPING_LANE2_V003.md       74bbb7aa…
V011   workspace/review_packets/STAGE7_QSPEC_CANDIDATE_V001/
         BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md            aa7c6d49…  packet-manifest sealed
A27s   workspace/STAGE8_TASK6_LP_MATRIX_LEDGER_LANE3_V001.md      bc6c3e49…  SEALED-OK
JREF   workspace/STAGE8_TASK5_JREF_AND_LOCAL_KERNEL_ESTIMATES_LANE2_V001.md  8dd59b35…
4d     workspace/STAGE8_TASK4D_STITCHING_THEOREM_GERM_INDEPENDENT_HALF_V001.md 430f0971…
MAJ    workspace/stage8_execution/work/MAJORANT_PHASE2_O3_O7_PROOF_DRAFT_V001.md 08b91543…
IDX    workspace/STAGE8_7A_RA27_2_INDEX_BUILT_DARIO_V001.md       66f078ba…
742    workspace/STAGE8_7A_COMPLETENESS_PROOF_DARIO_V001.md       863bc606…
749    workspace/STAGE8_7A_FLAT_CHAMBER_DARIO_V001.md             3e89b5ea…
FC     supervision/DECISION_RA27_2_ADOPTIONS_2026-08-08.md        31e42812…
```

Seal checks run **from each file's own directory**. All `V011` offsets are against the packet
copy `aa7c6d49…`, never the unsealed same-named top-level file (`20a3a17d…`).

### 0.3 Gates

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
No member binding.  No fixed-point execution.  No end test.  No numeric evaluation of
physical quantities.  No comparison to measured constants.  No common cell formed.
No junction map evaluated.  No response evaluated.  No smooth data derived.
Nothing adopted beyond FC-1's existing ledger.  No register, plan, tracker, or git action.
```

---

## 1. AB1 — THE RESTRICTION, DECLARED OF RECORD

### 1.1 The working class

```text
THE WORKING CLASS  (per DEC750, route (a))

OBJECTS   admissible finite oriented record complexes carrying the Gate-4 data,
          restricted to the SHAPE-REGULAR admission class      [ADOPTED, FC-2]

ARROWS    generated by, and closed under finite composition of:
  A0  every arrow of Ref_0 — identities; certified signed relabelings/
      isomorphisms; ratified rank-preserving W3 restriction/inclusion squares
                                                               [BUILT, JREF]
  A1  cubical bisection                                        [LICENSED, V011]
  A2  oriented simplicial/barycentric subdivision               [LICENSED, V011]

EXCLUDED  C_ref's third constituent — common refinements preserving the same
          smooth coframe and connection.
```

[PROVABLE] `A1` and `A2` at their frozen span, `V011` `aa7c6d49…[46772,47023)`; `A0` at
`JREF` `8dd59b35…[11610,11743)` with status `[12001,12049)`
`FINITE_REALIZATION_SKELETON_J_0 = BUILT / TYPE-P`.

### 1.2 The excluded constituent — its status, displayed in full

Three facts, none of which cancels the others, each pinned:

| # | Fact | Pin |
|---|---|---|
| 1 | **It is LICENSED in the frozen class.** `V011` freezes `C_ref` *"with"* three items under one verb; the third is one of them | `aa7c6d49…[46772,47023)` — established at 742 §1.4 |
| 2 | **Its use as a SOURCE is BARRED.** TYPE-R, and the bar is source-only — the same block carries `C_REF_USABLE_AS_TARGET_INTERFACE = true` | `430f0971…[20690,20930)`; `FC` FC-1, *"barred **as a source**"* |
| 3 | **No verified member satisfies its own conjunction.** The condition is *"the same smooth coframe **and** connection"*; connections are witnessed, coframes are not | `aa7c6d49…[46940,47023)`; 749 §1, after 16 agents / 12 adversarial verifications / `survivors: []` |

[YOURS] **The excluded constituent is not being declared empty, false, or unlicensed.** It is
being declared **out of the working class's scope**. 742 established it is licensed; 749
established its content is not exhibitable from sealed stock. The restriction takes it out of
scope without asserting anything further about it, which is the only move the record supports.

### 1.3 The cost, stated aloud

Verbatim from `DEC750` `0f2d5943…`:

```text
THE COST, stated aloud per DET's requirement: the third constituent — smooth-
coframe-and-connection-preserving common refinements — remains licensed in the
frozen class and excluded from the working class; its content is not
exhibitable from sealed stock (no verified member satisfies its own
conjunction), its use as a source is barred, and every result booked against
the working class carries that restriction on its face.
```

[PROVABLE] `DET` required exactly this and required it not be left implicit: *"the object is
proved on a class STRICTLY SMALLER than the one A-L5 and recast Q6 quantify over, and those
two remain unserved — which must be said in the verdict language rather than left implicit."*
**So it is said here: A-L5 and recast Q6 remain unserved by this booking.**

### 1.4 The re-opening trigger, preregistered

Verbatim from `DEC750`:

```text
THE RE-OPENING TRIGGER, preregistered: a future SEALED member satisfying the
full conjunction (coframe AND connection) re-opens A27's universal as a
surfaced revision of record — 7B's Einstein-limit machinery named as the
expected populator.
```

[YOURS] The trigger is well-formed and **testable**: 749 §1.2 gives the exact test a
candidate must pass — both halves of the conjunction, coframe and connection, in one
exhibited object. My four 746 candidates failed precisely that test, so the trigger's bar is
calibrated against a real attempt rather than set abstractly.

---

## 2. AB2 — RA27-2 BOOKED AGAINST D012, OVER THE WORKING CLASS

[PROVABLE] The demand, `D012` `74bbb7aa…[44324,44664)`: *"`Ref_a` with generators,
same-region relation, common refinements, and composition"*, TYPE-U, *"define familywise on
addressed record complexes; prove generator completeness/reachability"*, with adoption
licensed for the grammar *"if stock does not force it"* and *"one member may not be
selected."*

### 2.1 Demand 1 — generators — **SUPPLIED**

Supplier: `IDX` `66f078ba…` §1.3, arrows `A0`/`A1`/`A2` (§1.1 above). `A0` is BUILT/TYPE-P
from `JREF`; `A1` and `A2` are LICENSED of record from `V011`'s freeze.

[PROVABLE] **"One member may not be selected" is satisfied.** The working class carries two
distinct generating families plus `A0` plus all finite composites. The restriction removes a
*constituent*, not a *member*, and does not narrow the class to one.

### 2.2 Demand 2 — same-region relation — **SUPPLIED**

`IDX` §1.3, `P2`: `K ~ L` iff `K` and `L` are zigzag-connected under `P3`. Reflexive by `A0`,
symmetric by the zigzag's construction, transitive by concatenation. Over the working class
`P3`'s arrows are finite composites of `A1`/`A2` only, so `P2` is well-defined on exactly the
class declared in §1.1.

### 2.3 Demand 3 — common refinements — **SUPPLIED AS ADOPTED**

[PROVABLE] Supplier, cited **AS ADOPTED**: `FC` `31e42812…` FC-1 —

```text
DIRECTEDNESS: ADOPTED AS AXIOM — the one element Q-624 proved stock cannot
   supply. Adopted, not derived, and recorded as such wherever the index is used.
```

carried inside the object at `IDX` §1.3: *"For any `K`, `L` with `K ~ L` there **exists** `M`
with `K → M` and `L → M` in `P3`."*

[PROVABLE] **Exhibited witness:** `MAJ` `08b91543…[19632,19996)` — `Z`, the Freudenthal
subdivision of each subcube of the bisected unit 4-cube, 384 cells at `1/384`, named
*"Common refinement Z"*. `Z = A2 ∘ A1` is a composite of the working class's own generators,
so the witness lies **inside** the working class.

[YOURS] The axiom is **existential** and remains so. It is not superseded on any scope. What
the restriction changes is not the axiom's strength but the demand placed on it: A27's
universal over common-refinement moves is narrowed to the working class, where the
existential suffices for D012's object-level demand.

### 2.4 Demand 4 — composition — **SUPPLIED**

`IDX` §1.3: arrows *"generated by, and closed under finite composition of"* `A0`, `A1`, `A2`.
`P3` is defined as reachability by a finite composite; `A0` supplies the unit. Closed by
construction, and closed **within** the working class since the generators are.

### 2.5 Demand 5a — generator completeness — **PROVEN, for the working class**

[PROVABLE] 742's licensing enumeration, restated over the working class. Searched space as
742: recursive glob `./workspace/**/*.md` + `./supervision/**/*.md`, `*_DARIO_V001.md`
excluded, `evaluator_build_A/` code excluded.

| # | Licensing act | Disposition over the working class |
|---|---|---|
| L1a | `V011`'s freeze — cubical bisection | **IN** — it *is* `A1` |
| L1b | `V011`'s freeze — oriented simplicial/barycentric subdivision | **IN** — it *is* `A2` |
| L1c | `V011`'s freeze — the third constituent | **OUT OF SCOPE by declaration** (§1.2–1.3) |
| L2 | `JREF`'s `Ref_0` | **IN** — and carries **no refinement** by TYPE (Q-624: four invertible-only moves whose closure is the relabeling groupoid) |
| L3 | T13's battery pinning | **IN** — inherits `A1`/`A2`; its common refinement is existential and lands inside the class (`Z`) |
| L4 | D3's frozen quantifier | **NOT A LICENSING ACT** (742 §1.5, and 20/20 adversarial verifications concurred); its breadth is outside the working class by declaration |
| L5 | A27's criterion | its common-refinement conjunct is **outside by declaration — this is where the cost lands** (§1.3, §4) |
| L7 | FC-2's preregistered relaxation | **CONDITIONAL, NOT TRIGGERED** — and its trigger is the re-opening trigger of §1.4 |
| L8 | `V011`'s naturality require | **IN** — see §4; its universal is over elementary refinements |
| L9 | Q-408 / `Ref_PL` / P4 | **DIFFERENT INDEX** (742 §1.9) |

[PROVABLE] **Therefore, over the working class, every licensing act licenses `A1`, `A2` and
their finite composites and nothing else.** No act remains unclassified; the single act that
reached outside (L1c, with L5 quantifying over it) is out of scope by declaration of record.
**Completeness is proven for the working class at the PROVE standard**, and the restriction
is on the face of the claim.

[YOURS] What this is **not**: it is not the unrestricted completeness `DEC742` demanded and
742 refuted. That refutation stands unchanged. This proves a different, smaller, and honestly
labelled proposition.

### 2.6 Demand 5b — reachability — **SUPPLIED AS ADOPTED**

Reachability in the index is the adopted directedness of §2.3, licensed by D012's own
modality column (*"target-independent grammar may require **adoption** if stock does not
force it"*) and ratified at FC-1, with `Z` as an exhibited witness inside the class. Adopted,
tagged, and not represented as derived.

### 2.7 The booking

| D012 demand | Status over the working class | Supplier |
|---|---|---|
| 1 generators | **SUPPLIED** | `IDX` §1.3; `JREF` `8dd59b35…[11610,11743)`, `[12001,12049)`; `V011` `aa7c6d49…[46772,47023)` |
| 2 same-region relation | **SUPPLIED** | `IDX` §1.3 `P2` |
| 3 common refinements | **SUPPLIED — AS ADOPTED** | `FC` FC-1; witness `MAJ` `08b91543…[19632,19996)` |
| 4 composition | **SUPPLIED** | `IDX` §1.3 |
| 5a generator completeness | **PROVEN for the working class** | §2.5 |
| 5b reachability | **SUPPLIED — AS ADOPTED** | §2.6 |
| — precondition: measure refinement functoriality | **SUPPLIED** | 739 §1.6; and stronger than claimed there — `volume_weight_natural_on_full_D3 = true`, exact |

```text
RA27_2 = BOOKED over the working class (demand-by-demand displayed),
         with the restriction of §1 on its face.
```

### 2.8 The quantifier gap — closed by restriction, not by derivation

[YOURS] 742 §1.7 found the index supplies an **existential** where A27 demands a
**universal**. Nothing here derives the universal. A27's quantifier is **narrowed** to the
working class, where the existential is what D012's object-level demand asks for. **The gap
is not resolved; it is scoped out, and the scoping is the declared cost.** Any reader taking
this booking as having closed the existential/universal question has read it wrongly, and
§1.3 is the sentence that prevents that reading.

---

## 3. AB3 — RA27-3, AND THE CONSEQUENCES

### 3.1 RA27-3, restated over the working class

[PROVABLE] The demand, `D012` `74bbb7aa…[44665,45007)`: the full `J_ref` realization carrying
**incidence, degree, coframe, connection, volume, support, and current density**, TYPE-U,
*"construct natural transports on every RA27-2 generator and composite"*, modality
**derivation** — no adoption clause — with importing smooth `(M,g)` as source **barred by
DoR-007**.

**Inherited from the booking:** the quantifier it ranges over is now settled and closed —
`A0`, `A1`, `A2` and their finite composites, with `P2`'s classes and the adopted
directedness. RA27-3 no longer waits on the index.

| Field | Status | Note |
|---|---|---|
| `volume` | **SUPPLIED** | 739 §2.2; and natural on full D3 per `SLIV`, a class wider than the working one |
| `incidence` | **PARTIAL** | 742 §3: the composite question dissolves by induction; the **cochain-incidence half** — naturality of `incidence(f,e)` under subdivision — is untouched and remains named |
| `coframe` | **NOT SUPPLIED — and now worse** | its only sealed transport clause was the excluded third constituent; the restriction removes that route explicitly, and DoR-007 bars the import |
| `connection` | **NOT SUPPLIED — and now worse** | same ground |
| `degree` | **NOT SUPPLIED** | no transport law located |
| `support` | **NOT SUPPLIED** | ditto |
| `current density` | **NOT SUPPLIED** | ditto |

[YOURS] **The restriction's sharpest effect on RA27-3 is on `coframe` and `connection`.**
Their only sealed transport clause is the constituent now out of scope, RA27-3's modality
column offers **no adoption clause**, and DoR-007 forecloses the import. So those two fields
are not merely unsupplied — the restriction has removed the one written route to them, and
that must be visible to whoever takes RA27-3.

### 3.2 J2

[PROVABLE] `FC`'s standing consequences already record it: *"J2 acquires a domain (and with
the ruled subject, a truth condition); the R9-JII carrier remains PENDING on its common-cell
quantifier — this decision does NOT make the junction test runnable."* The restriction
sharpens the domain — J2's re-presentation family is now exactly `P2`'s classes over the
working class, a settled quantifier — and **changes nothing about the carrier**, which stays
PENDING. Nothing here makes the junction test runnable, and the R9-JII carrier is untouched.

### 3.3 T_ref

[PROVABLE] `4d` `430f0971…[10671,10885)` lists the would-build fields: `D_ref`, `eta_K`,
`P_KR`, `Resp_K`, with `P_KR eta_K = eta_R pullback_KR` to be proved. **Field 1 discharges** —
the index is instantiated and now closed. **Fields 2 and 4–8 remain**: `J_ref` unbuilt,
`eta_conn`/`eta_curv`/`eta_resp` unbuilt, `BoundaryCert` at NO_VERDICT of record,
`PhysInterface`/`PublicEquiv` unbuilt. **The S5.3 burden carries untouched**: an instantiated
coefficient limit must **prove** (H1)/(H2)-class convergence, never assume it. The restriction
neither discharges nor lightens it.

### 3.4 The sealed density instance

[PROVABLE] The exhaustion-vs-refinement gap recorded at `IDX` §3.3 is **unchanged**: the
sealed density instance remains **exhaustion-indexed** (`K` a member of the relayed
causally-sequential exhaustion — growing volume), while the working class is a **refinement**
index. Supplying a refinement index does not transport the sealed instance onto it. The
restriction narrows the refinement index; it does not build the bridge between the two
indexings, and no result here should be read as having moved that.

---

## 4. A FINDING THE RESTRICTION SURFACES — SOURCE REQUIRE vs AUDIT ROW

[PROVABLE] `V011`'s own naturality require, `aa7c6d49…[47025,47247)`:

```text
The response map must commute with pullback to a common refinement, and the
intensive quadratic coefficient must be invariant under each elementary
refinement up to a boundary term whose ratio to four-volume tends to zero.
```

[PROVABLE] A27's audit row, sealed instance `bc6c3e49…[37467,37661)`:

```text
The local coefficient is invariant under a sealed class of regular-CW
refinements and elementary subdivision/common-refinement moves; …
```

[PART-PROVABLE] **They do not quantify alike.** `V011`'s universal is over *"each
**elementary** refinement"* — `A1`/`A2`, inside the working class — and its
common-refinement clause is **existential** (*"to **a** common refinement"*), witnessed by
`Z`. **A27's row makes the common-refinement moves part of a universal.** So `V011`'s require
survives the restriction intact, and the declared cost lands on the audit row's restatement.

[YOURS] **I flag this and do not lean on it.** It would be convenient to argue the cost is a
restatement artifact and therefore cheap — that is precisely the convenience-reading this
program refuses, and the row is what the audit board evaluates regardless of how it came to
be worded. The cost stands as `DEC750` declared it. What I put on the record is the **factual
divergence between the source require and the row that restates it**, because if a later lane
reconciles them the restriction's price may be lower than it looks, and that is a question for
the registrar, not a discount I may take here.

---

## 5. GROUNDING, JURISDICTION, VERB AUDIT

### 5.1 Grounding

| # | Claim | Pin | Tag |
|---|---|---|---|
| 1 | Route (a) ruled; working class; cost; trigger | `0f2d5943…` | PROVABLE |
| 2 | D012's RA27-2 demand and both modality clauses | `74bbb7aa…[44324,44664)` | PROVABLE |
| 3 | D012's RA27-3 demand | `74bbb7aa…[44665,45007)` | PROVABLE |
| 4 | `A1`/`A2` at the frozen span; the three-item list | `aa7c6d49…[46772,47023)` | PROVABLE |
| 5 | `A0` BUILT / TYPE-P | `8dd59b35…[11610,11743)`, `[12001,12049)` | PROVABLE |
| 6 | The bar is source-only | `430f0971…[20690,20930)`; `FC` FC-1 | PROVABLE |
| 7 | The conjunction, and no verified member | `aa7c6d49…[46940,47023)`; 749 §1 | PROVABLE |
| 8 | Directedness ADOPTED, existential | `31e42812…` FC-1; `66f078ba…` §1.3 | PROVABLE |
| 9 | `Z = A2 ∘ A1`, inside the working class | `08b91543…[19632,19996)` | PROVABLE |
| 10 | The licensing enumeration restated | §2.5, from 742 `863bc606…` | PROVABLE |
| 11 | DET's requirement that the cost be said aloud | `6934f858…` | PROVABLE |
| 12 | T_ref's would-build fields | `430f0971…[10671,10885)` | PROVABLE |
| 13 | `V011`'s require quantifies over elementary refinements | `aa7c6d49…[47025,47247)` | PROVABLE |
| 14 | A27's row quantifies over common-refinement moves | `bc6c3e49…[37467,37661)` | PROVABLE |
| 15 | The source/row divergence | §4 | PART-PROVABLE, **not leaned on** |

**Pin check: 15/15**, every span re-read from bytes this relay against the stated digest.

### 5.2 Jurisdiction check

**On booking a restricted result after refusing the same content at 742.** *What was the rule
written to protect?* D012's PROVE verb protects against a generator set that silently omits a
needed move. The omission here is **not silent** — it is declared, pinned, costed, and
carries a preregistered trigger. That is the difference between §1.11 of 742 and §2 of this
artifact, and it is a difference of authority and disclosure, not of content. *Does the
outcome space distinguish false from cannot-see?* Yes: §2.5 returns **proven for a stated
class**, and §2.8 states exactly what is not proven. *Would it permit the evidence to appear
if the theory is right?* Yes — §1.4's trigger re-opens A27's universal on a sealed member,
and 749 §1.2 gives the test that member must pass.

**On the VOID CONDITION.** No topology, index, rule, branch or selector is chosen from a
desired coefficient. The restriction was ruled by the principal after two force-shaped routes
were spent, and this lane's own work closed both of them **against** its interest.

**On BR-1.** `DEC750` is authority, not a producer declaration. Where its words are the
operative act — the cost and the trigger — I reproduce them **verbatim** rather than
paraphrasing them into my own framing.

**On R9 / R9-JII.** Untouched. §3.2 records the carrier PENDING and declines to read a truth
condition into it.

### 5.3 Self verb audit — **CLEAN, with three disclosures**

1. **The booking rests on a restriction, and every line of §2 is scoped by it.** I have used
   the phrase "for the working class" on the completeness result deliberately and repeatedly,
   because a reader skimming to `RA27_2 = BOOKED` would otherwise take a smaller proposition
   for a larger one. 742's unrestricted refutation stands.
2. **I declined an available discount.** §4's source-require/audit-row divergence would let me
   argue the declared cost is largely a phrasing artifact. It might be — and establishing that
   is not this relay's commission, and taking the discount would be reading a criterion in the
   direction that flatters the booking. Displayed, not used.
3. **This is the fourth consecutive relay in which the load-bearing content is a restatement
   of the same distinction** — licensed vs in-scope, source-barred vs absent, present vs
   exhibitable. The distinction has now decided 742, 746, 749 and 750. I record that pattern
   because it suggests the program's remaining difficulty on this thread is **definitional
   hygiene around C_ref's third constituent**, not new mathematics — and that is a finding
   about where to spend effort, offered to the registrar rather than acted on here.

---

```text
RESTRICTION = declared (cost sentence displayed verbatim; trigger preregistered
    verbatim).  WORKING CLASS = A0 + A1 + A2 + finite composites, shape-regular
    admission.  EXCLUDED = C_ref's third constituent, with all three of its facts
    displayed and none cancelled: LICENSED in the freeze (742), source-BARRED
    (4d 430f0971...[20690,20930): C_REF_USABLE_AS_TARGET_INTERFACE = true), and
    NO VERIFIED MEMBER of its own conjunction (749, after 16 agents / 12
    adversarial verifications / survivors: []).  DET's requirement met: A-L5 and
    recast Q6 remain UNSERVED by this booking, said aloud.
RA27_2 = BOOKED over the working class (demand-by-demand displayed).  Demands 1, 2,
    4 SUPPLIED; demand 3 SUPPLIED AS ADOPTED (FC-1's existential directedness, with
    Z = A2 o A1 as an exhibited witness inside the class); demand 5a COMPLETENESS
    PROVEN AT THE PROVE STANDARD FOR THE WORKING CLASS — 742's licensing
    enumeration restated, every act licensing A1/A2 and composites and nothing
    else, the one act that reached outside now out of scope BY DECLARATION;
    demand 5b reachability SUPPLIED AS ADOPTED per D012's own modality column.
    The precondition (measure refinement functoriality) SUPPLIED.
    THE QUANTIFIER GAP IS CLOSED BY RESTRICTION, NOT BY DERIVATION — A27's
    universal is narrowed, not met, and that narrowing IS the declared cost.
RA27_3 = updated.  Inherits a settled and closed quantifier — A0/A1/A2 + composites.
    volume SUPPLIED; incidence PARTIAL (cochain half named); degree, support,
    current density NOT SUPPLIED; and coframe/connection NOT SUPPLIED AND NOW
    WORSE — their only sealed transport clause is the excluded constituent, the
    modality column offers no adoption, and DoR-007 bars the import.
CONSEQUENCES = J2 gains a sharper domain (P2's classes over the working class) and
    the R9-JII carrier stays PENDING — not runnable.  T_ref field 1 discharges;
    fields 2 and 4-8 remain; the S5.3 burden carries untouched.  The sealed density
    instance stays EXHAUSTION-indexed while the working class is a REFINEMENT
    index — that gap is unchanged and unbridged.
VOID = clean
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN (+3 disclosures at section 5.3, including a discount
    available at section 4 that I displayed and declined to take)
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
