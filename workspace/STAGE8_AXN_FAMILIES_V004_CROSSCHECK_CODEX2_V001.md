CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = DECLARED-FIRST
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_REGION = EMPTY (closure opens at byte 0; no text precedes it)
PREDECLARATION_OUTPUT_SCAN = 0 hits in authored prose (27-token sweep)
VERDICT_BEARING_SET = exactly the 11 content-addressed members below
UNDECLARED_SEARCH_SURFACE_VERDICT_WEIGHT = forbidden
LANE = CODEX 2   ROLE_THIS_RELAY = OPPOSITE-LANE CHECKER
SUBJECT_STATUS = CLAIMED until this check
```

| # | Closed member | SHA-256 / bounded-span SHA-256 | Role |
|---:|---|---|---|
| 01 | `relay_inbox/RELAY_PASTE_970_FAMILIES_V004_CROSSCHECK_CODEX2_V001.md` | `5e86a3e08139739c0186d1f014630ff8f1cb0c0a16695793b550f17bb5c29f45` | assignment |
| 02 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 03 | `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V004.md` | `28bc43a0a8e841f11d382a11d83634938f5eab0b94fe0f59737b290dbc1d6222`; suite JSON `[4450,6615)` `9a96cfbe1b704504385fa8008021df01cf93b33b99d8f1448cff82b607b7f3fc` | subject and displayed suite bytes |
| 04 | `STAGE8_AXN_ANCHOR_INSTRUMENT_CODEX2_V004.md` | `a195edb03b36be44bf8ce1b71dbf1a01a3a0956c4afc65ddf17382cdca4ed0ab`; suite schema `[21854,24285)` `e8b7132a2cbaa05e2eb4a45f2fbbe60a8ca12a0bfeef4cb12ce4a71956eb4167`; mandatory-receivers paragraph `[26739,27355)` `1dcff5b3ed879c9cd71cec079bf870327364817dfcd2a0305d7c05160155e146` | governing closed schema and V004 law |
| 05 | `JOINT_ANCHOR_DECISION_INSTANCE_V003.md` | `089af246cbc0d66e6ce70971dbb14d355a78ee0f5e294706a1acaeacd0d4236d`; BI tag `[7250,7299)` `9a42dac44da482d07f186040d6e62edb85c23c0c886858a14ba8ce63e0d3a72c`; BI content `[7300,7469)` `19585bb3d660895ca65f1c52b0d97903f96257ec02ec5fd4b21b3b26a2146d38` | entered tag/content and blind fiber digest copy |
| 06 | `STAGE8_AXN_FAMILIES_V003_CROSSCHECK_CODEX2_V001.md` | `ff587bada82efa5d829422fa33ac7116b727a774d4112a4e02c7ac00f1d58d52` | five findings to recheck and this lane's prior wording |
| 07 | `STAGE8_AXN_FOR_CLASS_FAMILIES_DARIO_V003.md` | `25845e9223e62374df699b474f0770191ef731f8123f2299b21af9683bf1b581` | 966 mathematical base |
| 08 | `STAGE8_AXN_ENTERED_OBJECTS_BUILD_CODEX2_V002.md` | `fd2625a079c77fbc0a102a54a0dd8ba1d97dcfb393035c2b691b0475de254444`; commutation object `[12736,13198)` `b6bc91777da3a69691f1b00ac4b30cfe61a472a7c55466098701018d4735d864`; fixed-space object `[11943,12588)` `7019826c3febf445b22892198d6e98839579f464a4f3d4be0e903c43c0ee3a45` | exact booked edge grounds |
| 09 | `STAGE8_AXN_STAGE_RULE_CONSTRUCTION_CODEX2_V002.md` | `f450b0a356b249509fb59b897c4f6a14e6996ba7da5ea513e69112a89907eed9`; gate 3 `[19300,19474)` `bfae33506a71836249bb282a694813bda717292229490a057a3b6fb81be3eb0d` | stage material and claimed freeze-deferral ground |
| 10 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process and custody law |
| 11 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01-S37 flattening guard |

All eleven whole-file members and all available adjacent seals verified before use. The output name
and sidecar were absent before write. No physical quantity was evaluated.

CLOSURE_DECLARATION_END

# STAGE 8 — AXN `for_class` FAMILIES V004 — FINAL OPPOSITE-LANE CHECK
## CODEX 2 — RELAY 970 — `[PLAN:AXN-BUILD-C69]`

Date: 2026-08-10  
Status: **FAILURES NAMED. The ten tuple repairs, BI binding, edge repairs, FC-07 scope, and the carried
mathematics check out. The displayed suite does not replay against V004's closed schema: required
fields are absent or renamed, forbidden extras are present, list-valued receivers are replaced by a
root, and both freeze and suite-hash fields are missing. The 2165 bytes rehash to the advertised
digest, but they are not a `JointAnchorCertificateSuite.v001` instance. RUN 4 does not go.**

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
member_binding = false
fixed_point_execution = false
end_test = false
JOINT_ANCHOR_DERIVED = false
```

## 1. Tuple replay and the CERT-A ruling

The subject declares two different eight-field grammars at its own bytes:

| Declaration | Subject span | Declared fields | SHA-256 |
|---|---:|---:|---|
| `JAC14-FC-FAMILY|v=004|id|quantifier|inputs|carrier|procedure|accept` | `[9593,9660)` | 8 | `abdf4fc047762d2f51261f671d8744d5ebd97b68445e1b4d7ae2bc8046421e18` |
| `JAC14-CERT|v=002|id|instantiates|quantifier|inputs|procedure|accept` | `[9661,9728)` | 8 | `3f9eb6308179479d0e2adfb7ae0d5ba2cef161b3e3a5fd24c7e26324354f5042` |

No value in any V004 tuple contains the separator byte. Terminal linefeeds were excluded.

| Object | Subject span | Bytes | Parsed fields | SHA-256 | Verdict |
|---|---:|---:|---:|---|---|
| FC-01 | `[9753,10825)` | 1072 | 8 | `8215042bb8e60b15a310b1c8a4438015ee55039312a4ccc7f6a450a566463d3e` | PASS |
| FC-02 | `[10850,12196)` | 1346 | 8 | `8bb6080205f6451cb6b5fc9852711ddf9f5431aaf7987fbee05ecf078548a7ad` | PASS |
| FC-03 | `[12221,13277)` | 1056 | 8 | `06233b1d5771903af2d5a723bb9d6e1fc9170467d1d7f2b129087a999350ca38` | PASS |
| FC-04 | `[13302,14411)` | 1109 | 8 | `b07339b7599fdc2e05f41815f77c9e6e9340a57d35396f7ad75286a835d453fb` | PASS |
| FC-05 | `[14436,15803)` | 1367 | 8 | `65aedce4c423b427adea9dfca8099223c600b637991adb4e07e30506340dd634` | PASS |
| FC-06 | `[15828,17065)` | 1237 | 8 | `03698d14572cc91914bc5f4f7346bdedd9fc2f61f9bdef59daeeed34382dad5b` | PASS |
| FC-07 | `[17090,18775)` | 1685 | 8 | `5b939147f0779f787a4f4cc5250372d83610a0112561060ddbfd23a9834cf03f` | PASS |
| FC-08 | `[18800,20372)` | 1572 | 8 | `f743aee3ce8cc616f35ad33029896bf6c65fc92f356209b01957ae34e446f0e1` | PASS |
| CERT-IF | `[20399,21290)` | 891 | 8 | `7620ef432c977283fbf467b16b2fed89e60781268a5d68a5a5e674e35b3b5bae` | PASS |
| CERT-A | `[21316,22201)` | 885 | 8 | `e18d2f4fe733ef1afb7403417dabe85a44d0cdd7c84cdacb9b066db58e88da51` | PASS |

**CERT-A was correct by design.** Its declaration includes `instantiates` as the fourth field and has
eight fields total. V003 CERT-A also parsed at eight. This lane's 967 report did **not** call CERT-A
malformed: it named FC-03, FC-08, and CERT-IF as the three malformed tuples and faulted CERT-A only
for its then-unbound BI input. The relay's contrary characterization is corrected here.

## 2. Suite replay against the closed schema

The displayed JSON is exactly 2165 bytes and independently rehashes to
`9a96cfbe1b704504385fa8008021df01cf93b33b99d8f1448cff82b607b7f3fc`.
That is a custody result. V004 declares `canonical bytes; closed fields; no extras`, so the decisive
test is exact field inventory and value type, not whether malformed content has a stable hash.

### 2.1 Outer fields

| V004-required field | Subject observation | Replay verdict |
|---|---|---|
| `schema` | exact const present | PASS |
| `carrier_sha256` | exact entered declared-joint-algebra realization `1467ab98...` | PASS |
| `a0_rank_fiber_sha256` | exact blind digest copy `5eaa8277...`; no fiber content opened | PASS |
| `finite_stage_inventory` | `BOUNDED_CLASS` tag present, but its closed member inventory fails §2.2 | **FAIL** |
| `restriction_and_limit_square_receipts` | one root digest string supplied where the schema requires an exhaustive ordered list | **FAIL-TYPE** |
| `canonical_serialization` | const text present, but the object has missing and extra fields | **FAIL-CONFORMANCE** |
| `replay_entry_point_sha256` | lowercase digest `d66e7587...` present; fixed-string search finds its supposed object nowhere outside this subject line | **FAIL-UNBOUND** |
| `suite_sha256` | absent from the JSON; the external `SUITE_SHA256 = ...` line is not a field | **FAIL-MISSING** |
| `frozen_pre_output_receipt_sha256` | deliberately absent | **FAIL-MISSING** |

The subject adds `cert_if_joint_sha256` and `cert_a_joint_sha256`. Neither is an outer field in
V004's closed schema. Thus the top-level exact-name comparison is:

```text
MISSING = suite_sha256, frozen_pre_output_receipt_sha256
EXTRAS  = cert_if_joint_sha256, cert_a_joint_sha256
```

### 2.2 `finite_stage_inventory.BOUNDED_CLASS`

| Schema receiver | Subject observation | Replay verdict |
|---|---|---|
| `escape_text` | exact const present | PASS |
| `stage_rule_ground_sha256` | booked stage payload digest present | PASS |
| `bounded_class_definition` | points to V003 `G_joint` candidate grammar `[13731,14294)`, not the content-addressed positive-integer stage index/object/map/limit rule | **FAIL-WRONG-OBJECT** |
| `universal_restriction_and_limit_square_receipts` | absent; renamed to `universal_restriction_and_limit_square_receipts_root_sha256` and reduced from the required exhaustive ordered list to one root | **FAIL-MISSING/EXTRA/TYPE** |
| `ordered_receipt_root_sha256` | exact booked root present | PASS |
| `outer_receipts_identity` | prose assertion present, but the two list-valued operands are absent, so byte/digest identity cannot replay | **FAIL-UNBOUND** |
| `explicit_reopening_trigger` | points to the same `G_joint` grammar span, not a content-addressed reopening predicate | **FAIL-WRONG-OBJECT** |
| `for_class` | eight digests occur, but under names `fc_01` through `fc_08`; all eight exact schema keys are absent and all eight short names are extras | **FAIL-CLOSED-KEYS** |

The required `for_class` keys are
`delta0_basis_family`, `phi_restriction_matrix_family`,
`factorization_and_inverse_family`, `cptp_certificate_family`,
`charge_covariance_certificate_family`, `superselection_commutation_certificate_family`,
`fixed_space_and_mixing_certificate_family`, and `anchor_certificate_family`. A closed schema does
not permit a private positional renaming, even when the eight values happen to be the intended row
digests.

### 2.3 The suite digest and freeze ruling

The advertised `9a96cfbe...` is correctly derived from the displayed **incomplete** 2165-byte JSON.
It does not confirm the required `suite_sha256` field, because that field is absent and no schema
convention says an external assignment substitutes for it.

The cited gate 3 says, at exact stage-ground bytes `[19300,19474)`, that
`replay_entry_point_sha256`, `suite_sha256`, and the frozen pre-output receipt are downstream of the
**complete suite bytes** and “cannot be computed over a partial suite.” It does not say any of the
three fields is optional or deferrable from an instance. V004's governing schema requires the field,
its JAC-14 row rejects absent pre-output freeze, and `[26739,27355)` says the replay-entry,
suite-hash, and pre-output-freeze receivers “remain mandatory.”

Accordingly the builder was right not to fabricate a receipt, but wrong to call the receipt-less
object an instantiated suite. The lawful status is **partial suite material awaiting a complete
instance and pre-output freeze**, not `JointAnchorCertificateSuite.v001`.

## 3. Edge repairs

### 3.1 FC-05

The booked object at `[12736,13198)` rehashes to `b6bc9177...` and proves exactly three identities:

```text
E_joint compose i_src = i_src compose E_ch
E_joint compose i_R   = i_R
E_joint compose i_B   = i_B
```

V004 FC-05 repeats precisely those conclusions and expressly refuses to derive stage compatibility
from that certificate. Predicate 2 supplies covariance and Lemma C supplies the admitted
candidate's stage stability. The procedure sends those premises to the stage equivariance square.
The earlier over-consumption is removed.

### 3.2 FC-07

The procedure now says `RECEIVE` and `EMITTING` and names the exact
`fixed_space_and_mixing_certificate_family` receiver. Existence and stage compatibility are replayed
inside `procedure`, rather than announced only in `accept`. This fixes the procedural binding defect.

```text
EDGES = FC-05 AND FC-07 CONFIRMED
```

## 4. BI at the instance bytes

Independent half-open rehashes give:

| Instance span | Bytes | SHA-256 | Content |
|---|---:|---|---|
| `[7250,7299)` | 49 | `9a42dac44da482d07f186040d6e62edb85c23c0c886858a14ba8ce63e0d3a72c` | entered `{BI}` tag row |
| `[7300,7469)` | 169 | `19585bb3d660895ca65f1c52b0d97903f96257ec02ec5fd4b21b3b26a2146d38` | entered BI equation/input-faithfulness row |

FC-08 and CERT-A cite both exact spans under the verified decision-instance digest. The V003 blank-
instrument-slot defect is repaired.

## 5. FC-07 scope and correction against 967

Instrument bytes `[23800,23898)` type the receiver as
`fixed_space_and_mixing_certificate_family: exact total replay object on the bounded class`.
Under Q-877's type reading, embodied in this lane's V004 row alignment, the common value type
controls: it requires an exact total replay object, not a silently restored finite-enumeration
conclusion. FC-07 supplies the exact theorem the grammar supports at every stage and candidate —
invariant-state existence and stage compatibility — and explicitly excludes the unproved stagewise
uniqueness claim.

Therefore FC-07 is **consistent as scoped**. This lane's 967 conclusion that the same object
“underfilled” the receiver was inconsistent with the V004 type reading already accepted at 965;
that conclusion is corrected here. The correction does not rescue the malformed suite wrapper.

## 6. Mathematical carriage and basis independence

The substantive Lemma A, B, and C derivations and the joint `(N>=1) x (Phi_joint in G_joint)`
quantifiers remain. The tuple edits replace separator-bearing notation, move BI citations to their
actual instance, and make the two edge procedures explicit; they do not alter the three underlying
mathematical implications. `MATH = CARRIED` is confirmed in that semantic sense; the V004 tuple
lines are not byte-identical to V003 and are not reported as such.

All ten rows remain basis independent. No positive basis, ordering, matrix-entry, matrix-inverse, or
`Omega_Phi` coordinate datum is supplied. The literal `matrix-inverse` and `Omega_Phi-coordinate`
occurrences are explicit `no-...` exclusions, not hidden data.

## 7. FREEDOMS-CONSUMED, flattening, and fences

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false

CARRIED-AS-PARAMETER:
  Lemmas A/B/C and the joint quantifiers;
  the entered carrier and blind fiber digest;
  the exact BI instance spans;
  E_joint certificates, stage payload, seven receipts, and ordered root.

SUBSTITUTED:
  NOTHING.

NOT FILLED, AND THEREFORE NOT AN INSTANCE:
  the exact closed suite field inventory;
  the bound replay-entry object;
  suite_sha256 as an in-object field under a declared convention;
  frozen_pre_output_receipt_sha256.

SCALING WEIGHTS: NONE CONSUMED; NONE FIXED; NONE SUBSTITUTED.
```

- **FLATTENING CHECK:** held. A stable digest was not flattened into schema validity; a root was not
  flattened into an exhaustive ordered list; eight positional names were not flattened into eight
  exact closed keys; an external assignment was not flattened into an absent field; and a gate
  saying “downstream of complete bytes” was not flattened into permission to omit a mandatory field.
- **BLIND:** held. The A0 fiber remained a digest copy; no rank-shaped object, ratio, or fiber
  comparison was opened.
- **PE-1..PE-15:** pointer-only and zero verdict weight.
- **F_PLDEC:** tuple arities, symbolic implications, byte spans, field names, and digests only. No
  physical quantity was numerically evaluated and no measured constant was compared.
- **PIN CHECK:** output name absent before write; all closed whole-file members and available
  sidecars matched; exact bounded spans rehashed.
- No member binding, fixed-point execution, end test, gauntlet run, or downstream chain was invoked.

## 8. Verdict and bounded repair

Four of the five 967 repair classes are now clean: tuples, edges, BI binding, and FC-07 scope. The
suite repair must be rebuilt from the schema's exact names and value types. It must carry the seven
receipt objects as ordered lists at both required receivers, the correct bounded-class rule and
reopening predicate, all eight named `for_class` keys, a bound replay entry, the suite-hash field
under a nonrecursive declared convention, and the actual pre-output receipt. Schema amendments, if
needed to make the digest/freeze order realizable, require an instrument delta; they cannot be
invented inside a family payload.

Self verb audit: “confirmed” is confined to tuple parsing, CERT-A's declared count, edge reception,
BI spans, the FC-07 type ruling, mathematical carriage, and basis independence. The external suite
digest is called rehashed, not schema-conforming. The receipt-less JSON is called partial material,
not instantiated.

## 9. Final lines

```text
CLOSURE = declared-first (byte position 0, scan 0 hits)
TUPLES = 10/10-AT-COUNT
CERTA_RULING = WAS-CORRECT-BY-DESIGN (the JAC14-CERT declaration has 8 fields; 967 did not call CERT-A malformed)
SUITE = FAILS (missing/extra/renamed closed fields; list receivers replaced by a root; replay entry unbound; suite_sha256 and freeze receipt absent; 9a96cfbe... rehashes incomplete bytes only)
FREEZE_DEFERRAL = DEMANDED-AT-INSTANCE (gate 3 bars computation over a partial suite; V004 schema and mandatory-receivers bytes require the field)
EDGES = CONFIRMED
BI_SPANS = REHASHED-CONFIRMED
FC07 = CONSISTENT-AS-SCOPED
MATH = CARRIED
BASIS_INDEPENDENCE = 10/10
VERDICT = FAILURES NAMED (SUITE WRAPPER); NOT EIGHT-BOOK; RUN 4 DOES NOT GO
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
```
