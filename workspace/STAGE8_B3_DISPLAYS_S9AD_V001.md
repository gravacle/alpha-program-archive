# STAGE 8 — THE THREE ARMED FINITE DISPLAYS: OB-2's B-3(i)/(ii)/(iii) — V001

## BLIND BUILDER — CODENAME B3-BUILD — [SEALED]

Date: 2026-08-14 (session UTC 2026-08-15). Commission: display, at bytes and
exact-integer grade, the three finite facts OB-2 named "decidable at bytes by
future work, undisplayed today" (`c8053f6c…` §6.2 B-3): (i) the kernel-class
extent (evaluates NG-2), (ii) the mixing relation in the sealed `qtilde_N`
ideal (evaluates NG-3), (iii) the admitted-and-carried stage census (arms
NG-1). Each is a kill-or-survive check on the H/HOL member family. Evaluated,
not leaned.

Fences held throughout: `alpha_computed = false` ; `proof_authorized = false`;
`kappa_record_computed = false`. DETERMINATION/DISPLAY ONLY: no member bound,
no candidate authored, no adoption, no route/branch/address/representative
selected, no score/gate moved. No file matching
register|road_|ledger|lens|plan|tracker|THE_HANDOFF opened; "Q-…" tokens
EXPECTED-UNLOCATABLE (cited only as carried inside sealed artifacts). No git;
no existing file edited; output + sidecar probed ABSENT at session start and
re-probed ABSENT at 2026-08-15T03:49:58Z immediately before this write; ONE
output file plus its seal sidecar. Exact symbolic/integer only: the battery
ran in a FRESH venv (sympy; exact integers/rationals/symbols; no float
anywhere; no value frozen).

**SWEEP CUTOFF: 2026-08-15T03:49Z** (SW-1..SW-6 below; parallel S9AD lanes
are landing tonight — anything sealed after this cutoff is outside this
artifact). Exhaustiveness is sweep-displayed only.

CONSUMED AT AUDITED GRADE (hashes re-verified at path this session):
the OB-2 pair (`c8053f6c` + audit `d9f4aad0`, CONFIRMED-WITH-CORRECTIONS)
and the pin determination (`09047c2a` + audit `abe71688`): PIN-1 =
FORCED(ACTUAL), PIN-2-HOL = FORCED(zero-admitted), PIN-3 = FORCED(MANDATORY:
NG-2 binds EVERY H member at every stage it carries) are consumed of record;
PIN-2-H is FREE (R-A / R-B), handled per display below.

---

## 0. VERDICT (the three menus)

```text
(i)   KERNEL-CLASS EXTENT   = PARTIAL, with the inequality itself EVALUATED:
      dim Kernbar_N^cyc(O_N^cyc) DISPLAYED EXACT at every displayed carried
      stage; dim Ker_N^Q408 displayed as certified bounds (its exact extent
      is unfixed at bytes — Joint HJ7, SURVIVES). NG-2 HOLDS at EVERY
      displayed carried stage, for EVERY lawful extent, under the real-carrier
      definitional bytes; under the never-displayed complex/sesquilinear
      reading it holds at rank <= 1 and is undecided (not failed) at rank 2.
      NO KILL. NOT-DISPLAYABLE-TODAY residue: the exact extent; the field pin.
(ii)  MIXING RELATION       = PARTIAL: the sweep half DISPLAYED — the sealed
      ideal's displayed relations are exactly TWO classes, (R4) and its q=2
      image (R6)-(R7), BOTH PURE-CYCLE (a = 0); mixing relations displayed:
      COUNT = 0. NG-3's antecedent is NOT met at today's bytes. NO KILL.
      Existence half NOT-DISPLAYABLE-TODAY (missing object named §4.3).
(iii) STAGE CENSUS          = PARTIAL: stage slots and cycle ranks DISPLAYED
      EXACT (max displayed rank = 2); represented-kernel column DISPLAYED
      (nonzero at every rank>=1 stage); dim M_N column NOT-DISPLAYABLE-TODAY
      (no sealed byte fixes any background dimension — missing object named
      §5.3); the (SA-1) closure's rank reach open both ways.
      NG-1 remains ARMED-NOT-FIREABLE.

NET: NO kill condition fires. The H family is NOT proved empty by these
displays — and display (i) comes out the OTHER way from the sealed evidence
lean: at every displayed carried stage NG-2 holds with room (rank 1: 1 <= 2)
or exactly (rank 2 real: 4 <= 4). The NG-2 kill surface on today's census is
EMPTY under the real-carrier bytes. What could still kill, exactly:
  K-a  a carried stage of cycle rank >= 3 (real reading; none displayed;
       (SA-1)-open) or rank >= 2 under a sealed complex reading with sealed
       extent >= 6 (no complex byte displayed);
  K-b  one mixing relation (a != 0) with zero cycle-side value (none
       displayed; existence undecided; PIN-1 has met NG-3's F-A premise,
       so a future display completes the kill without further pins);
  K-c  NG-1's census cell: a named carried stage with displayed
       dim M_N >= 2 (dim M_N is displayed NOWHERE).
SK-2 = NOT FIRED ; ACT-3 = NOT FORCED ; D-1 menu UNCHANGED.
PIN-2-H: every display below is IDENTICAL under R-A and R-B (all three are
Class-1/H-inequality objects; the pin sheet's ROW 3; nothing here routes
through (JD-3)'s words). Stated per display.
```

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256), THIS SESSION, BEFORE RELIANCE

```text
COMMISSION STOCK
c8053f6c  STAGE8_OB2_EMPTINESS_S9AD_V001.md              MATCH (read in full)
d9f4aad0  STAGE8_OB2_EMPTINESS_S9AD_AUDIT_V001.md        MATCH (read in full)
09047c2a  STAGE8_PIN_DETERMINATION_S9AD_V001.md          MATCH (read in full)
abe71688  STAGE8_PIN_DETERMINATION_S9AD_AUDIT_V001.md    MATCH (hash verified;
          consumed through the pair's own displays)
CHASED CARRIERS (each verified before its bytes were used)
fdf20bd4  STAGE8_TASK5_JII_REALIZATION_LANE3_V001.md     MATCH (SPEC-2 tuple;
          §3.4 Joint HJ7 (JH-1)/(JH-2); §5.2 anchor board; (SA-1) at §5.2/5.x)
ba9430c1  STAGE8_TASK5_JII_REVIEW_DARIO_V001.md          MATCH (hash verified)
b53d9e93  STAGE8_TASK5_EQ6_THE_LOC_BUILD_LANE3_V001.md   MATCH ((S1)/(S2)/
          (S2a)/(S2b)/(S4)/(S5)/(S8) at bytes)
ccb94dfa  STAGE8_TASK5_R4_KERNEL_REALIZATION_AND_SYMBOL_CALCULUS_LANE2_V001.md
          MATCH (§0.4 symbol table; (B1-3)-(B1-12); (B2-2)-(B2-15) at bytes)
3fd4b924  STAGE8_TASK5_EQ6_PHYSICAL_J2_LANE2_V001.md     MATCH (§1 carrier
          naming at bytes: "Ker_N^Q408 actual finite path-bilocal kernel class")
d662889b  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V002.md        MATCH (read in full;
          (S2)/(Q5)/(R1)-(R10)/§4.4/(B3) at bytes)
075a2b16  STAGE8_TASK5_EXTSRC_BUILD_LANE3_V001.md        MATCH (hash verified)
474bf721  STAGE8_TASK5_EQ6_LOC_NATURALITY_LANE3_V001.md  MATCH (§3.4 (Q13)/
          (Q13-F)/(Q14): S_nm bijective; the rank-one q=2 carrier at bytes)
(unsealed-hash carrier, read at bytes for (SA-1)/(D4)/(D5) only)
          STAGE8_TASK5_EQ6_MANIFESTS_LANE3_V001.md       ((D4) rank display
          (b,b+1,b+1,b+2); (D5) prototypes b=0; D-sub = certified b=1;
          "carrier/cycle-rank parameters are unbounded" at its own line)
MISMATCHES = 0. The held construction 506bfe2a untouched, unopened,
unpresupposed. No fenced-class file opened.
```

## 2. SWEEPS RUN (cutoff 2026-08-15T03:49Z; roots = workspace, supervision, cleanroom_output, relay_inbox, relay_outbox; fenced filename classes excluded)

```text
SW-1  "B3_DISPLAYS|B-3 displays|kernel-class extent" -> only OB-2 itself.
      NO competing B-3 display artifact exists.
SW-2  "mixing relation|mixed relation" -> exactly the known lineage
      (LOC-NAT, ExtSrc V001/V002, MEMBERSHIP_THEOREM V002 [a DIFFERENT
      family, disposed at OB-2 SW-4], OB-2+audit, PIN). No new mixing-
      relation display anywhere.
SW-3  "rank 3|rank-three|b=2|b+2" x "stage|cycle|carrier|census" -> the
      MANIFESTS (D4) schema row (walked) plus non-J-II program strata
      (7A/B1A/D0/anchoring). NO sealed rank->=3 admitted-and-carried J-II
      stage display (concurs with audit §2 row B-3(iii) SURVIVES).
SW-4  "dim M_N" -> occurs ONLY in OB-2 and PIN. No sealed byte displays
      any stage's background dimension.
SW-5  "sesquilinear|Hermitian" -> many hits, NONE a definitional site for
      the J-II kernel class; the TASK5_EQ6 hits are metric-positivity
      displays carried "over R and over C" (both-compatible), fixing no
      field. No sealed sesquilinear kernel-class display exists.
SW-6  "Ker_N^Q408 :=|=" -> no extent-fixing line anywhere (B-3(i)'s
      openness re-verified by my own sweep).
DISCLOSURE: sweep listings displayed fenced-class BASENAMES (including one
register-class basename); no fenced file's content was opened, read, or used.
```

---

## 3. DISPLAY (i) — THE KERNEL-CLASS EXTENT (evaluates NG-2)

### 3.1 The carriers at their own bytes

```text
(S2)/(S2a) LOC: E_N^cyc := {0} (+) R Rhat_K,N (+) S_(1,sa)(K_N);
           O_N^cyc := Q_N(E_N^cyc); Kernbar_N^cyc : O_N^cyc -> Ker_N^Q408.
           The codomain typing is the sealed LOWER-BOUND containment:
           im(Kernbar_N^cyc) subset Ker_N^Q408.
(B1-3) KER: A_prof,G = R R_K,G (+)_1 S_1,sa(K_G) — S_(1,sa) is the
           trace-class self-adjoint part of the cycle carrier K_G
           ("completed physical cycle Hilbert carrier", §0.4).
(B1-5) KER: "The REAL dual is E_G^* = R (+) R (+) B_sa(K_G)" with the
           trace pairing Tr(TC).
(B2-5)/(B2-6) KER: the realization is a continuous BILINEAR functional,
           defined on ALL of Hom(K_G^fin,(K_G^fin)^*); every finite
           path-bilocal kernel is a finite sum sum_ab H~_ab J_a (x) J_b —
           so the class of such kernels IS the sigma_cyl image, giving the
           UPPER-BOUND containment for the PJ2-named class.
(B2-7) KER: Kern_G^fin(H) = 0 iff H = 0 (FAITHFUL) — so operator-space
           dimensions transfer to kernel-class dimensions EXACTLY.
(B1-8)/(B1-11) KER: P_x = r^2 R_K ; R_K = P_(e_1)+P_(e_2) — R_K,N is a
           sum of rank-one projectors: symmetric/self-adjoint, trace-class
           at finite rank, hence R R_K,N subset S_(1,sa)(K_N).
PJ2 §1:    "Ker_N^Q408 = actual finite path-bilocal kernel class" (VERBATIM,
           audit-confirmed).
(JH-1)/(JH-2) fdf20bd4 §3.4: Joint HJ7 = exactly the extent question;
           "No conclusion that FullJ7_N is abstractly empty is drawn."
```

FIELD NOTE (displayed, not adopted): the walked definitional bytes are
uniformly real/bilinear — "real dual" (B1-5), "bilinear" (B2-5), "a real
self-adjoint invariant tensor" (KER §1.5) — and SW-5 finds NO sesquilinear
kernel-class display anywhere. OB-2 nevertheless carried a
complex/sesquilinear reading of the extent question; both readings are
therefore evaluated below. Fixing the field is a pin determination and is
NOT performed here.

### 3.2 Both dimensions, exact (fresh-venv battery, integers only)

By (B2-7) faithfulness plus R_K in S_(1,sa): at a finite stage of cycle
rank r,

```text
dim Kernbar_N^cyc(O_N^cyc) = dim( R R_K,N + S_(1,sa)(K_N) )
                           = dim S_(1,sa)(K_N)          [EXACT]
   real reading:    r(r+1)/2      complex reading:  r^2   (real dims)
   (exact-rank check: r=1: domain 2, rank 1, overlap 1 = the (R4) line;
    r=2: domain 4, rank 3, overlap 1; identical for a generic SPD R_K)

dim Ker_N^Q408 : certified bounds (extent unfixed at bytes, SW-6)
   dim S_(1,sa)(K_N)  <=  dim Ker_N^Q408  <=  dim_R Hom
   real:    r(r+1)/2 <= d <= r^2
   complex: r^2      <= d <= 2 r^2
```

### 3.3 NG-2 evaluated at every displayed carried stage

NG-2 (binding on EVERY H member per forced PIN-3):
`dim Ker_N^Q408 <= 1 + dim Kernbar_N^cyc(O_N^cyc)` at every carried stage.

```text
                          real reading                complex reading
 rank r   LHS in    RHS   verdict          LHS in    RHS   verdict
   0      [0,0]      1    HOLDS            [0,0]      1    HOLDS
   1      [1,1]      2    HOLDS            [1,2]      2    HOLDS
   2      [3,4]      4    HOLDS (all       [4,8]      5    UNDECIDED
                          extents; max                     (fails iff a
                          4 <= 4, sharp)                   sealed extent
                                                           >= 6 appears)
   3      [6,9]      7    fails iff        [9,18]    10    fails iff
          (no such        extent >= 8      (no such        extent >= 11
          stage           — matches         stage
          displayed)      audit's           displayed)
                          "9 > 1+6"
```

Every displayed carried stage has rank <= 2 (§5). Under the real-carrier
bytes NG-2 therefore HOLDS at every displayed carried stage FOR EVERY
LAWFUL EXTENT — the kill cannot fire on today's census under that reading,
whatever Joint HJ7 later fixes. Under the complex reading it holds at
rank <= 1 and is UNDECIDED at the rank-2 stages (both the field and the
extent are unfixed; neither failure nor clearance is displayed).

NOTE N-1 (verdict-neutral): the audit's threshold parenthetical "complex …
rank 2 (8 > 1+5)" reads the RHS from the E^cyc upper bound (1+4) before the
overlap quotient; my exact count is RHS = 1 + dim Herm(C^2) = 1 + 4 = 5.
Both counts agree the full-extent complex case violates and the real case
does not.

### 3.4 Menu outcome (i)

```text
DISPLAY (i) = PARTIAL
  DISPLAYED: dim Kernbar_N^cyc(O_N^cyc) exact at every displayed carried
    stage; certified two-sided bounds on dim Ker_N^Q408; the NG-2
    evaluation table above; NO KILL — the inequality HOLDS everywhere
    displayed under the real-carrier bytes, sharp at rank 2 (4 <= 4).
  NOT-DISPLAYABLE-TODAY:
    (a) the exact extent of Ker_N^Q408 (missing object: a sealed
        extent-fixing display — Joint HJ7's FULL_Q408_IMAGE gap; SW-6);
    (b) the field pin for S_(1,sa)/Hom (missing object: a sealed field
        determination; real has the definitional bytes, complex has no
        displayed byte, neither is forced here).
  VALIDITY UNDER PIN-2-H: identical under R-A and R-B.
```

---

## 4. DISPLAY (ii) — THE MIXING RELATION (evaluates NG-3)

### 4.1 The sealed ideal at its own bytes

```text
(S2)/(Q5) d662889b:  qtilde_N(a,b,C) := a L_T,N + j_R4,N(b R_K,N + C),
                     E_N = R L_T,N (+) R Rhat_K,N (+) S_(1,sa)(K_N);
                     Rel_N = closure(ker(qtilde_N))            ((Q2), SEAM1).
MIXING relation := a relation with a != 0 (the (R8) form); its cycle-side
value := Kernbar_N^cyc(Q_N^quot(0, b/a, C/a)).
NG-3 (kill, both families): ONE sealed mixing relation with ZERO cycle-side
value forces kappa_T,N = 0 against Sch_N injective (+ PIN-1's forced ACTUAL
reading supplying the F-A linearity premise) and M_N^loc != 0.
```

### 4.2 The sweep of displayed generators/relations — exact classification

Every relation displayed anywhere in the swept sealed record (SW-2):

```text
 #  relation (at bytes)                    a-coeff  class       cycle-side value
 1  (R4) reciprocal rank-one:             a = 0    PURE-CYCLE  Kernbar(Q(r_x))
    r_x = (0, -t, P_x), P_x = t R_K,n              (a=0: not    = Kernbar(0)
    qtilde_n(r_x) = j(t R_K - t R_K) = 0           mixing)      = 0   [EXACT]
 2  (R6)-(R7) its q=2 Ref_path image:     a = 0    PURE-CYCLE  = Kernbar(0)
    j_nm^E(r_x) = (0, -t, t R_K,m)                              = 0   [EXACT]
    qtilde_m(...) = 0
 MIXING RELATIONS DISPLAYED: COUNT = 0.
```

(Both re-derived exactly in the fresh venv with t symbolic: the inner
argument -t*R + t*R vanishes identically; no t evaluated, no address chosen.)

NG-3's antecedent is therefore NOT met at today's bytes: NO KILL. The
displayed relations, being pure-cycle zero classes, are exactly the ones the
sealed `Kernbar_N^cyc` already kills — consistent with the audit's PROVABLE
consistency half (differences of mixing relations are pure-cycle).

### 4.3 What the record does not fix

The EXISTENCE of a mixing relation is undecided in BOTH directions at
sealed bytes: (R8)/§4.4 state no sealed law proves the general mixed-
relation theorem, no display exhibits a mixing relation, and nothing proves
(R4) generates every kernel relation. Equivalently undecided: whether
L_T,N is linearly independent of j_R4,N(R R_K,N + S_(1,sa)(K_N)) inside
O_R5,N (independence would make the antecedent FALSE and retire NG-3
permanently; dependence would exhibit a mixing relation whose cycle-side
value then decides both families).

```text
DISPLAY (ii) = PARTIAL
  DISPLAYED: the complete sweep of the ideal's displayed relations (two
    classes, both pure-cycle, both cycle-side value 0 exactly); mixing
    count = 0; NG-3 does NOT fire; PIN-1 has completed NG-3's premise
    list, so the kill is one future display away — in EITHER direction.
  NOT-DISPLAYABLE-TODAY: the mixing-relation existence bit (missing
    object: a displayed basis/extent of ker(qtilde_N) beyond the named
    classes — equivalently a sealed independence certificate for L_T,N
    modulo the j_R4,N cycle image in O_R5,N, or one exhibited mixing
    relation with its exactly-evaluated cycle-side value).
  VALIDITY UNDER PIN-2-H: identical under R-A and R-B.
```

---

## 5. DISPLAY (iii) — THE STAGE CENSUS (arms NG-1)

### 5.1 What the sealed record fixes — the census at bytes

```text
 stage (defining bytes)                        cycle   repr. kernel   dim M_N
                                               rank r  nonzero?
 N_RL  reciprocal source (KER §1.4 "the          1     YES  (B2-7)+   UNDISPLAYED
       one-dimensional carrier"; anchor board)         R_K != 0
 M_RL  reciprocal target (u:N_RL->M_RL           1     YES            UNDISPLAYED
       "rank-preserving", fdf20bd4 l.1258)
 N_n   q=2 Ref_path source ("reciprocal          1     YES            UNDISPLAYED
       rank-one source carrier", LOC-NAT §3.4)
 N_m   q=2 Ref_path target (S_nm BIJECTIVE,      1     YES            UNDISPLAYED
       LOC-NAT (Q13); e_m := S_nm e_n)
 D-sub G_0^sub,G_s,G_t,G_st (certified b=1       1     YES            UNDISPLAYED
       instance; rank-preserving subdivision)  (x4)
 D-disj G_0^disj (b=0 source)                    0     NO (class = 0)  UNDISPLAYED
 D-disj G_U,G_V (b+1)                            1     YES            UNDISPLAYED
 D-disj G_UV (b+2)  ((D4) at bytes)              2     YES            UNDISPLAYED
 D-contact G_0^ct / mids / G_UV^ct               0/1/2 as D-disj      UNDISPLAYED

 MAXIMUM DISPLAYED CYCLE RANK = 2. No rank->=3 stage is displayed anywhere
 (SW-3; audit concurs). The four anchor stages are carried by every member
 per the spec's own §5.2 minimum-display rows on u and r_nm (OB-2 Class-1
 typing, consumed at audited grade); the diamond stages are the certified
 (SA-1) prototype generators (carriage displayed only to the S25/(SPEC-14)
 clauses' own scope — verdict-neutral here: every row HOLDS in §3.3).
```

CENSUS CORRECTION TO A PRIOR PARENTHETICAL (verdict-neutral, displayed):
OB-2 §4.2(ii) read "the exchange/q=2 geometry, rank 2" as one named stage.
At bytes these are TWO objects: the q=2 Ref_path pair is RANK 1 at both
ends (S_nm bijective from the rank-one carrier, LOC-NAT (Q13)/(Q14)); the
rank-2 S8-A EXCHANGE geometry (KER §1.5: orthonormal pair, R_K = P_e1+P_e2,
invariant fiber (chi_K, tau_cross)) is an admitted actual profile geometry
that appears NOWHERE in fdf20bd4's anchor board — it is not displayed as a
J-II carried stage. The displayed rank-2 carried slots are the diamond tops
G_UV/G_UV^ct. Either way max displayed rank = 2, so no NG-2 consequence
moves; the audit's B-3(iii) "no rank->=3 stage" verdict is unchanged.

### 5.2 What (SA-1) leaves open

```text
(SA-1) at bytes (fdf20bd4): D_cert = Cl_(covariance orbit, identity,
transpose, whiskering by certified R_sub/C arrows, finite pasting)
{D_RsubRsub, D_CC^disjoint, D_CC^contact} — "no closure-wide realization
induction exists"; MANIFESTS: "the carrier/cycle-rank parameters are
unbounded". OPEN BOTH WAYS: whether the generated closure contains stages
of rank >= 3 (whiskering by cycle-creating C arrows raises rank with no
displayed bound), and the full carried-stage set of any member. A sealed
closure display exhibiting a carried rank->=3 stage would put NG-2's real
threshold (first violation at rank 3 iff extent >= 8) in play; a sealed
rank bound <= 2 would retire NG-2's kill surface entirely under the real
reading.
```

### 5.3 NG-1's arming state

```text
NG-1 (H only; PIN-1's forcing dissolved its reading-stop; microlocal step
remains imported external mathematics exactly as OB-2 tagged it) needs one
carried stage with dim M_N >= 2 AND a nonzero represented kernel.
  kernel half:  DISPLAYED — nonzero at every rank>=1 stage ((B2-7)
                faithfulness + R_K,N != 0), i.e. at 10 of 12 slots above.
  dim M_N half: NOT-DISPLAYABLE-TODAY — no sealed byte in the swept roots
                displays any stage's background dimension (SW-4). The
                A1/A2 family is typed "globally hyperbolic oriented
                Lorentzian U(1) backgrounds" (KER §0.3, DoR-015 by
                reference) with no dimension displayed; DoR-015's own
                "physical field signature" bytes are unlocated in the
                swept roots. MISSING OBJECT: a sealed display fixing
                dim M_N at one named carried stage.
NG-1 = ARMED-NOT-FIREABLE from today's census.
```

```text
DISPLAY (iii) = PARTIAL
  DISPLAYED: the full stage-slot/rank/kernel census above; max rank = 2;
    the (SA-1) openness stated both ways; the census correction (§5.1).
  NOT-DISPLAYABLE-TODAY: the dim M_N column (missing object named above);
    the closure's rank reach ((SA-1) induction absent of record).
  VALIDITY UNDER PIN-2-H: identical under R-A and R-B.
```

---

## 6. CONSEQUENCE BOARD (descriptive; nothing moves)

```text
SK-2 DOES NOT FIRE: no display above proves the family empty. ACT-3 is NOT
forced. The D-1 menu stands exactly as staged. The member-authorship act is
NOT retired by derivation today.

WHAT MOVED (display, not decision): NG-2, previously "evaluation
undisplayed", is now EVALUATED on the whole displayed census and HOLDS
everywhere under the real-carrier bytes — the sealed evidence lean toward
EMPTY is NOT borne out at today's bytes on arm (i). The whole EMPTY-side
surface now sits on exactly four named future displays (K-a..K-c of §0 plus
the ExtSrc B-1 fiber for the NON-EMPTY side), each finite and
bytes-checkable, each stated with the exact threshold it must cross.

Any commissioned H member still owes the NG-2 proof at ITS carried stages
(unconditional per PIN-3) and the NG-3 consistency display; this artifact
supplies the member-independent halves (the RHS dimensions; the pure-cycle
classification) those obligations will consume.
```

## 7. CHOICE LEDGER (terminal; per the guard)

```text
CL-1  CORPUS BOUNDARY (the named unforced choice): roots = workspace,
      supervision, cleanroom_output, relay_inbox, relay_outbox minus fenced
      filename classes; a32_holdout not content-swept (holdout discipline).
      Sweeps SW-1..SW-6, cutoff 2026-08-15T03:49Z; parallel S9AD lanes
      landing tonight are outside the cutoff. A wider sweep changes a
      display only by exhibiting a sealed extent/field/mixing/dimension/
      closure byte this sweep missed. PREMISE(named).
CL-2  AUDITED-GRADE CONSUMPTION: the OB-2 pair and PIN determination
      consumed as commissioned (PIN-1 ACTUAL, PIN-2-HOL zero-admitted,
      PIN-3 MANDATORY of record; PIN-2-H carried both ways; audit
      corrections C-1/C-2 respected — NG-3 used only WITH the F-A premise,
      met by PIN-1). PREMISE(named).
CL-3  FIELD READINGS: both carried symmetrically; the real reading's
      definitional bytes displayed ((B1-3),(B1-5),(B2-5),KER §1.5); no
      field pin adopted — that determination is not this commission.
      PREMISE(named).
CL-4  R_K,N in S_(1,sa)(K_N) consumed from (B1-8)/(B1-11) bytes (sum of
      rank-one projectors; SPD Riesz map; trace-class at finite rank).
      PREMISE(named).
CL-5  DIMENSION TRANSFER via (B2-7) faithfulness (exact at finite rank; no
      infinite-dimensional step used; the audit's N-2 caveat inherited: if
      a kernel extension is ever sealed, §3 must be re-proved on the
      extended class). PREMISE(named).
CL-6  CARRIED-STAGE READING: the four anchor stages read as carried by
      every member per fdf20bd4 §5.2's minimum-display rows + OB-2's
      Class-1 typing (audited grade); the diamond prototype stages
      displayed without a carriage claim (verdict-neutral: all rows HOLD).
      PREMISE(named).
CL-7  THE CENSUS CORRECTION (§5.1) is a bytes display (LOC-NAT (Q13)/(Q14)
      vs OB-2's parenthetical), not a contradiction of any verdict at its
      own walked bytes; OB-2 did not walk LOC-NAT. PREMISE(named).
CL-8  NOTE N-1: the audit's complex rank-2 RHS parenthetical differs from
      my exact count by the overlap quotient; displayed verdict-neutrally.
      PREMISE(named).
CL-9  DISCLOSURE: sweep listings displayed fenced-class BASENAMES (one
      register-class basename among grep hits); no fenced content opened,
      read, or used. MANIFESTS was read without a commission-listed hash;
      used only for (SA-1)/(D4)/(D5) rank displays, flagged in §1.
      PREMISE(named).
ZERO entries in class OPEN.
```

## TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The battery's identity Gram and one generic SPD matrix ([[2,1],[1,3]])
  are verification instruments for a rank statement displayed to be
  choice-invariant; the symbol t is never evaluated; no toy value, stage,
  address, or number enters any conclusion. All census/dimension entries
  are exact integers read from or derived by displayed clauses. The two
  relation re-derivations are exact symbolic replays of sealed displays,
  not new constructions; nothing is promoted to a member, candidate, or
  adoption.
```

## FLAG BLOCK

```text
B3_DISPLAYS = (i) PARTIAL / (ii) PARTIAL / (iii) PARTIAL — all three armed
  checks EVALUATED to the full depth today's bytes permit; NO KILL FIRES
B3_I_KERNEL_EXTENT = dim Kernbar_N^cyc(O_N^cyc) EXACT at every displayed
  carried stage (= dim S_(1,sa): rank 1 -> 1; rank 2 -> 3 real / 4 complex);
  dim Ker_N^Q408 in certified bounds [S_(1,sa), Hom]; NG-2 HOLDS at EVERY
  displayed carried stage for EVERY lawful extent under the real-carrier
  bytes (sharp 4 <= 4 at rank 2); complex reading: holds rank <= 1,
  UNDECIDED rank 2 (fails iff sealed extent >= 6); extent + field = the
  named blockers
B3_II_MIXING_RELATION = displayed relations of the sealed qtilde_N ideal
  = exactly {(R4), (R6)-(R7)}, BOTH PURE-CYCLE (a = 0), cycle-side values
  = 0 exactly; MIXING COUNT = 0; NG-3 antecedent NOT met, NO KILL;
  existence undecided both ways (missing object: ker(qtilde_N) beyond the
  named classes / L_T,N independence certificate); F-A premise met via
  PIN-1, kill is one future display away in either direction
B3_III_STAGE_CENSUS = 12 stage slots displayed with exact cycle ranks; MAX
  DISPLAYED RANK = 2; no rank->=3 stage exists at bytes; represented kernel
  nonzero at every rank>=1 stage ((B2-7)); dim M_N displayed NOWHERE
  (NG-1 = ARMED-NOT-FIREABLE); (SA-1) closure rank reach open BOTH ways;
  census correction: q=2 Ref_path pair is rank 1 at both ends, the rank-2
  S8-A exchange geometry is NOT a displayed J-II carried stage
KILL_SURFACE_AFTER_B3 = K-a rank->=3 carried stage (or sealed complex
  extent >= 6 at rank 2); K-b one mixing relation with zero cycle-side
  value; K-c dim M_N >= 2 displayed at a carried stage (NG-1) — none
  displayed today
FAMILY_H = NOT PROVED EMPTY, NOT REFUTED ; FAMILY_HOL = unchanged
SK2 = NOT FIRED ; ACT3 = NOT FORCED ; D1_MENU = UNCHANGED
PIN2H_HANDLING = every display identical under R-A and R-B (stated per
  display; ROW-3 pattern)
EVIDENCE_LEAN = NOT borne out on arm (i) at today's bytes: the finite
  check the lean pointed at came out HOLDS everywhere displayed
SEALS = commission 4/4 + carriers 8/8 MATCH ; MISMATCHES = 0 ;
  MANIFESTS read hash-unlisted (CL-9)
SWEEPS = SW-1..SW-6 ; CUTOFF 2026-08-15T03:49Z ; exhaustiveness
  sweep-displayed only ; parallel S9AD landings post-cutoff excluded
DETERMINATION_ONLY = held (no member bound, no candidate authored, no
  adoption, no selection, no common cell, no reader, no fixed point, no
  end test) ; MEMBER_BOUND = false ; CHAIN_INVOKED = false
NUMERIC_EVALUATION = exact integers/symbols only, fresh venv, no float,
  no value frozen ; MEASURED_CONSTANT_COMPARISON = none
HELD_CONSTRUCTION = untouched, unopened, unpresupposed (506bfe2a by bar)
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false
NET = the three armed checks were run as far as sealed bytes reach: no
  kill fires, and the one check the evidence leaned on (NG-2) comes out
  HOLDS at every displayed carried stage — sharply at rank 2. The whole
  emptiness question now sits on four exactly-named future displays plus
  the ExtSrc B-1 fiber, each finite, each with its threshold printed.
  Nothing moved; the gates are where the principal left them.
```
