# Stage 8 Raw-Correlator-to-Retarded-Hessian Map Specification v001

Date: 2026-07-30
Lane: CODEX 2

## 0. Status and F-GK3 declaration at the outset

This artifact is a specification written under Q-52 in order to make the
missing response-extraction map testable. It is not a derivation or an
execution.

```text
specification_authority = Q52_TESTABILITY_RELEASE
specification_status = DECLARED_FOR_TEST
raw_correlator_to_retarded_Hessian_map_specified = true
raw_correlator_to_retarded_Hessian_map_derived = false | TYPE-U | would-build: one completed gauge-fixed physical CTP bilocal-source package, its invertible raw connected correlator, and a derivation that the inverse/branch-rotation construction below is the physical action Hessian including contacts and boundary terms
test_design_attached = true
T1_through_T6_execution_completed = false | TYPE-U | would-execute: run each applicable test in Section 6 after its independently named fixture and applicability inputs exist
correspondence_type_test_execution_status = EXECUTED
new_physical_premise_status = NO_NEW_PREMISE_ADOPTED [PROCESS_ATTESTATION; NON-Q54_RESULT]
```

The specification uses the following bundled label:

```text
CTP_PHYS_INPUT_PACKAGE
```

That label is local shorthand for the domain data enumerated in Section 5. It
does not assert that those data exist. Their current status is:

```text
CTP_PHYS_INPUT_PACKAGE_derived = false | TYPE-U | would-build: the complete microscopic inclusive CTP functional, gauge-fixed physical quotient, invariant contour/spacetime measure, CTP branch metric/reality/index order, nonzero differentiable Log_0 neighborhood, physical inverse domain, prescription, contacts, boundary/edge data, and Ward-compatible endpoint domains
```

No premise beyond the current stack is adopted. The entries below are
specification inputs whose absence prevents instantiation; they are not
licences:

| ID | Specification input | Current typed status |
|---|---|---|
| P1 | Complete gauge-fixed physical CTP quotient and invariant contour/spacetime measure | `derived = false | TYPE-U | would-build: construct the quotient and measure from the completed microscopic operator` |
| P2 | Fixed branch metric, reality involution, compound-index order, and Keldysh rotation | `derived = false | TYPE-U | would-build: derive the branch conventions and their physical block action from the same completed operator` |
| P3 | Nonzero differentiable `Log_0` neighborhood with fixed prescription | `derived = false | TYPE-U | would-build: prove a source neighborhood on which the normalized inclusive functional is nonzero and differentiable` |
| P4 | Raw connected contour correlator `G` on the symmetric physical bilocal-source domain | `derived = false | TYPE-U | would-build: differentiate the completed `W_inc[J,R]` on P1-P3` |
| P5 | Two-sided inverse of `G` on the physical quotient, with contact and boundary domains included | `derived = false | TYPE-U | would-build: prove quotient invertibility and construct the convolution inverse in the declared domains` |
| P6 | Retarded support, Ward, boundary, edge, and endpoint-intertwiner package | `derived = false | TYPE-U | would-build: derive the full curved CTP Ward/contact/boundary system and endpoint intertwiners` |
| P7 | Stationary 2PI-to-1PI reduction and its physical Schur complement | `derived = false | TYPE-U | would-build: solve delta Gamma_2PI/delta G=0 on the physical package and derive the second-variation Schur complement on its invertible tangent domain` |
| P8 | Finite-to-complete carrier/refinement intertwiners, if a finite control is promoted | `derived = false | TYPE-U | would-build: construct a source-inclusive continuum/refinement system and prove response naturality under its embeddings` |

The source of P1-P5 is
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v004.md:19-69,107-123`.
It says:

```text
Step 5 must construct that quotient and its contour measure from the
microscopic operator before the identity can be turned into a physical Dyson
kernel.
```

and:

```text
G is the raw connected contour correlator, not an action Hessian.
```

It then names the remaining data:

```text
The CTP metric, index order, Keldysh block inversion, gauge quotient, contact
terms, and boundary terms must therefore be derived before a physical Dyson
residual can be written.
```

Q-52 expressly permits this declaration while withholding derivation status.
`/Users/bgm/MB Work/alpha_supervision/TESTABILITY_PRECEDES_CHARTER_SCOPE_DECISION_2026-07-30_V001.md:47-56`
says:

```text
The response-extraction layer is hereby an authorized construction target in
its own right.
```

and:

```text
A lane may write a specification, provided it is declared at the outset,
marked derived = false, and never reported as derived.
```

## 1. Live authority and negative typing

A register-only currency check found head Q-55 before drafting and head Q-56
during hostile review. Q-56 was checked because it landed mid-run.
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2332-2335`
rules:

```text
ZERO OF FIVE ARE UNCONDITIONAL STRUCTURAL TYPE-R CLOSURES OF THE
PARAMETER-LOCATION QUESTION.
```

Q-56 retypes and reduces theory candidate 001; it does not derive or retype
the raw-correlator map, either scalarization, the induced kernel, or the
covariant local projector. It supplies no construction premise used here.
Q-50 through Q-54 remain the latest substantive authority for this item.

Process disclosure: a broad pre-evidentiary path probe surfaced excluded
relay/path names, and the later register-tail currency check incidentally
surfaced Q-55's register paragraph. No Q-55 construction artifact was opened,
cited, or used, and the final correspondence search was rerun over the
explicit allowed list in Section 2.3. This is recorded as a scope-process
deviation rather than silently described as an exclusion.

`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2132-2169`
records Q-51:

```text
FOUR OBJECTS SIT UPSTREAM OF Gamma_K / C_record(K): the complete BR/CTP
fluctuation-response operator; the exact induced kernel; the
raw-correlator-to-retarded-Hessian map G -> H_R[G]; and the covariant local
projector.
```

Its pre-Q-54 flag is retyped here:

```text
raw_correlator_to_retarded_Hessian_map_derived = false | TYPE-U | would-build: the completed physical CTP package and the response-extraction producer specified in Section 5
```

Q-54 changes the type of that old flag. The adopted protocol at
`/Users/bgm/MB Work/alpha_supervision/NEGATIVE_RESULT_TYPING_PROTOCOL_V001.md:35-53`
defines:

```text
TYPE-R — REFUTED. A test capable of failing was executed and returned
evidence against the claim.

TYPE-U — UNBUILT. The object has not been constructed. Says nothing about
whether it can be.

TYPE-S — SCOPE-EMPTY. A bounded search returned nothing.

TYPE-C — CONSTRAINT-BLOCKED. A constraint prevented the check from running.
```

The protocol names this exact map as its TYPE-U example at `:41-43`.
Accordingly, this artifact never reads the unbuilt status as physical
evidence.

Q-51's registered reopen condition at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2169`
says:

```text
REOPENS IF: the response-extraction layer is specified, or Gamma_K's charter
is amended to include it.
```

Q-52 amended the charter and authorized this specification. The resulting
two-part status is:

```text
Q51_REOPEN_CONDITION_FIRED_BY_Q52 = true
response_layer_specification_status = PARTIAL_RAW_MAP_ONLY
```

Only this one conditional interface is specified here. Its derivation and
Q-51's other three upstream objects remain open.

## 2. Bounded correspondence search

### 2.1 Roots and exclusions

The correspondence inspection used these roots:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program
/Users/bgm/MB Work/alpha-program-archive/workspace
/Users/bgm/MB Work/alpha_supervision
```

The final evidentiary search was run over the explicit governing-source list
in Section 2.3. That list was constructed before the content query, so paths
containing spaces were passed as discrete quoted arguments rather than through
`xargs`.

Excluded from the evidentiary search and from this construction:

```text
a32_holdout/custodian_private/                 [pruned before descent]
S8 write-tail join and exchange_magnitude
C_R, tau = 1, premise classification, ER-A, and A6
binary artifacts, JSON execution payloads, and scripts
superseded source versions except where a current file itself names one
```

Fences in force:

```text
no alpha, kappa_record, kappa_Thomson, coupling, radius, scale, root,
eigenvalue, or beta-function computation;
no measured-constant comparison;
read-only outside this lane's artifact and shared-status append;
report refutations without repair.
```

### 2.2 Exact queries

The direct correspondence query was:

```text
raw_correlator_to_retarded_Hessian_map
raw-correlator-to-retarded-Hessian
G -> H_R
raw contour correlator
retarded action-valued Hessian
physical block inversion
Keldysh block inversion
operator-to-scalar bridge
primitive_source_scalarization_derived
Duhamel_intensive_Hessian_equality
```

The alternate-name check also inspected the conjunction, at the file level,
of:

```text
{retarded inverse | inverse kernel | inverse-kernel |
 physical block inversion | Schur complement}
AND
{CTP | Keldysh | contour correlator | contour metric}
AND
{Hessian | Dyson kernel | Legendre | action kernel | response kernel}.
```

### 2.3 Exact governing-source hit list

The final explicit query returned these 22 files:

```text
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_complete_dimension_convention_ledger_v004.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_post_cleanroom_route_state_v002.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_surface_symbolic_spine_v004.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_symbolic_first_proof_gate_v003.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_record_cell_selection_principle_v004.md
/Users/bgm/Documents/New project/gravity_emergence_evidence_program/primitive_zero_bare_induced_response_projection_principle_v004.md
/Users/bgm/MB Work/alpha-program-archive/workspace/COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_RESULT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_EM_GR_SURFACE_BODY_SCOPE_FOR_GAMMA_K_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_GAMMA_K_RESPONSE_OPERATOR_CORRESPONDENCE_DETERMINATION_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_GAMMA_K_SECTION1_DEPENDENCY_AND_RECOVERY_INVENTORY_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_ROUTE2_COMPLETE_QSPEC_STATE_BINDING_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_IDENTITY_AND_INTERCHANGE_SCHEMA_SPEC_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_COMPLETED_CONDITIONED_DUHAMEL_IDENTITY_RESULT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_EXACT_DERIVATION_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_RESULT_V001.md
/Users/bgm/MB Work/alpha-program-archive/workspace/STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md
/Users/bgm/MB Work/alpha_supervision/NEGATIVE_RESULT_TYPING_PROTOCOL_V001.md
/Users/bgm/MB Work/alpha_supervision/TESTABILITY_PRECEDES_CHARTER_SCOPE_DECISION_2026-07-30_V001.md
```

The result of that bounded inspection is:

```text
already_derived_raw_correlator_to_retarded_Hessian_map_found = false | TYPE-S | roots: the 22 explicit governing sources in Section 2.3 under the three roots in Section 2.1 | excl: protected custodian, concurrent S8 and premise families, binary/JSON/script payloads, superseded versions | fences: read/cite and no forbidden computation | query: the direct and alternate-name strings in Section 2.2
```

This is a scope-empty finding, not a statement that the map is impossible or
absent from physics. The hits state the formal role, status, dimensions, and
required ingredients; none supplies an executed producer or a derived
`G -> H_R[G]` implementation.

## 3. Correspondence verdict

### 3.1 Type table

| Object | Domain | Codomain | Relation to `G -> H_R[G]` |
|---|---|---|---|
| Raw-correlator extraction map | Raw connected bilocal CTP correlator on the completed physical quotient | Retarded action-valued inverse-kernel/Hessian operator on the same quotient | The object specified in Section 5 |
| Primitive source scalarization | Completed-record/source operator on the finite primitive source carrier | One complex scalar amplitude | **Not the same mathematical function; an obsolete-branch sibling inside Q-50/Q-51's broader missing response-extraction layer** |
| Actual finite-parent operator-to-scalar bridge | Finite actual-parent CAR/Gaussian-sum response operator | State-evaluated finite scalar relative-history functional | **Prospectively upstream of the domain of `G -> H_R[G]`, but not currently composable with it** |
| Complete-`Q_spec` state evaluation | Exhaustive relative-history operator `R_all` | Finite/algebraic scalar `Z_K[A_+,A_-]` | Positive finite bridge; still requires a bilocal-source/log/differentiation and complete-CTP extension before it supplies raw `G` |
| `p_loc` | Retarded action/inverse-kernel operator | Dimensionless local coefficient | **Downstream** of `G -> H_R[G]`; a different scalarization |

Q-51 at
`/Users/bgm/MB Work/alpha_supervision/QUESTIONS_SETTLED_REGISTER_V001.md:2156-2162`
calls the raw-correlator map:

```text
the object Q-50 predicted from the theory side
```

and describes the two routes as converging on:

```text
the same missing map
```

That ruling controls layer-level program accounting. The stricter question
asked here is whether the sealed primitive operator-to-number function is
literally the same mathematical function as the raw-bilocal-kernel-to-
retarded-operator function.

The following correspondence type test was executed:

```text
test = STRICT_FUNCTION_IDENTITY_BY_SEALED_SIGNATURE_AND_ACTIVE_COMPOSITION
inputs =
  STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md:83-153;
  STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md:81-103;
  primitive_record_cell_selection_principle_v004.md:49-69,107-155;
  primitive_zero_bare_induced_response_projection_principle_v004.md:94-120
applicability =
  both compared signatures are fixed in sealed text and the active/superseded
  architecture relation is stated
failure criterion for identity =
  domains or codomains differ, or the active architecture does not compose
  the functions as one arrow
execution_status = EXECUTED
observed =
  finite completed-record/source operator -> complex scalar, versus raw
  connected bilocal correlator -> retarded action-valued operator; the
  primitive-scalar path is superseded in the active architecture
```

The strict same-function hypothesis therefore fails:

```text
primitive_scalarization_strictly_identical_to_raw_correlator_map = false | TYPE-R | test: STRICT_FUNCTION_IDENTITY_BY_SEALED_SIGNATURE_AND_ACTIVE_COMPOSITION executed above
```

This refutes only strict function identity. It does not refute either
object's physical constructibility, and it does not overturn Q-51's
layer-level classification. The correct combined typing is:

```text
same_mathematical_function = NO
same_missing_response_extraction_layer_under_Q51 = YES
primitive_scalarization_active_composition = OBSOLETE_BRANCH
actual_finite_parent_bridge_relation = PROSPECTIVELY_UPSTREAM
```

If any cited signature or the active/superseded architecture relation were
not fixed, the type test would return `NO_VERDICT`; that inapplicability did
not occur for the four cited inputs.

### 3.2 Primitive scalarization

`STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md:83-153` defines:

```text
V_K[A]: H_S -> H_S tensor H_R
M_(xi,mu)[A] in End(H_S)
R_all,K[A_+,A_-] in End(H_S)
R_comp,K[A_+,A_-] in End(H_S).
```

It says at `:139-152`:

```text
This gate does not select either kernel as a scalar physical action.
...
Neither kernel applies a source state.
```

The active amendment at
`STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md:81-103`
says:

```text
The old primitive-scalar path remains in the record as a failed or
special-case lane.
```

and:

```text
the original primitive scalar-amplitude obligation is superseded by a type
correction
```

with scalar analyticity beginning after complete-`Q_spec` state evaluation.
Therefore the primitive scalarization flag is not an active upstream producer
for the raw correlator, even though Q-51 groups both deficits in the same
response-extraction layer.

Its typed status is:

```text
primitive_source_scalarization_derived = false | TYPE-U | would-build: a scalarization in the superseded primitive-scalar architecture; the active Route-2 replacement instead uses the independently predeclared complete-Q_spec state on R_all
```

Two narrower negative results must not be conflated:

```text
finite_completed_compression_automatically_scalar = false | TYPE-R | test: the executed non-scalar finite compression witness in STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_RESULT_V001.md:70-75
```

and the independently executed primitive-authority no-go gives:

```text
sealed_primitive_authorities_canonically_force_a_scalar_functional = false | TYPE-R | test: STAGE8_T7_SOURCE_SCALARIZATION_NO_GO_SPEC_V001.md and STAGE8_T7_SOURCE_SCALARIZATION_NO_GO_RESULT_V001.md:22-85,136-150 executed the finite non-scalar compression witness, normalized-trace covariance test, continuum trace obstruction, and exhausted named closure classes over the sealed primitive authority set
```

The result's ceiling at
`STAGE8_T7_SOURCE_SCALARIZATION_NO_GO_RESULT_V001.md:17-20` says:

```text
This is a scope result. It does not say that no physical scalar amplitude
exists.
```

Complete-`Q_spec` scalar closure is positively derived, so neither negative is
a refutation of physical scalar closure.

### 3.3 Actual finite-parent bridge and its missing composition

The actual-parent Phase-A spec defines a finite Gaussian-sum operator response
at
`STAGE8_T7_ACTUAL_PARENT_REGULATED_CAR_OPERATOR_RESPONSE_SPEC_V001.md:184-229`.
Its ordering clause at `:478-496` says a later Phase B evaluates predeclared
finite states only after Phase A seals, and that even a passing Phase A:

```text
does not establish a continuum limit, regulator independence, zero-free
neighborhood, linked-cluster density, physical connection normalization,
coupling, or alpha.
```

Its status is:

```text
actual_finite_parent_operator_to_scalar_bridge_derived = false | TYPE-U | would-build: execute and seal the actual-parent Phase-A response, then forward-seal Phase B and apply the pinned complete-Q_spec state without selecting a state or response after output
```

The generic finite/algebraic type transition does exist.
`STAGE8_ROUTE2_COMPLETE_QSPEC_STATE_BINDING_V001.md:31-59` states:

```text
Z_K[A_+,A_-]
 =omega_in(W_K[A_-]^dagger W_K[A_+])
 =omega_source(R_all,K[A_+,A_-]).
```

and:

```text
The binding closes the state identity and the finite/algebraic
operator-to-scalar type transition.
```

To feed the raw-map domain, that finite external-history scalar would still
have to be extended to the full bilocal-source functional `Z_inc[J,R]`, placed
on the physical quotient, given a differentiable `Log_0` branch, and
differentiated to obtain raw `G`. The current status is:

```text
finite_scalar_CTP_to_complete_bilocal_raw_G_extension_derived = false | TYPE-U | would-build: a source-inclusive continuum/projective-limit CTP functional with symmetric bilocal source, regulator/gauge/edge completion, nonzero differentiable log domain, and a proof that its derivatives restrict to the finite scalar closure
```

Thus the actual finite bridge is upstream by prospective composition, not the
same map and not a present implementation of it.

### 3.4 The genuinely downstream scalar map

`primitive_record_cell_selection_principle_v004.md:127-155` states:

```text
p_loc[L_T] = 1.
iota_loc(b) = b L_T.
Pi_loc = iota_loc compose p_loc.
```

and:

```text
p_loc maps an inverse-kernel operator to a dimensionless coefficient
```

That is the operator-to-number map downstream of `H_R[G]`.
`primitive_zero_bare_induced_response_projection_principle_v004.md:103-106`
warns:

```text
The scalar p_loc and operator Pi_loc are different typed objects.
...
The map from the raw contour correlator to the action-valued retarded Hessian
is not yet derived.
```

Accordingly, `p_loc` is neither of Q-50's finite scalarization flags and is not
the map specified here.

## 4. What the finite results give and withhold

### 4.1 Derived finite operator content

`STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_RESULT_V001.md:5-15` reports:

```text
FINITE_PRIMITIVE_OPERATOR_RESPONSE_BUNDLE_DERIVED
INDEPENDENT_PRIMITIVE_OPERATOR_RESPONSE_CONFIRMED
```

and:

```text
The finite primitive parent produces a Stinespring/PVM operator-response
bundle and a finite operator Duhamel tangent. It does not produce a canonical
primitive source scalar.
```

The exact derivative is typed at
`STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_EXACT_DERIVATION_V001.md:131-170`.
For a norm-`C^1` finite Hamiltonian family it supplies:

```text
dW_s(T,0)/ds |_(s=0)
 =-i integral_0^T W_0(T,t) J(t) W_0(t,0) dt
```

and the first-history variation of the finite operator kernel, together with
the adjoint-exchanged second-history variation.

Therefore the finite tangent is:

```text
external connection-history direction
  -> first variation of a finite source operator response.
```

It is not raw bilocal `G`, a convolution inverse, or an action-valued retarded
kernel.

### 4.2 Derived finite scalar extension

The active architecture does extend the finite exhaustive operator through a
predeclared state:

```text
primitive parent
 -> Stinespring/PVM operator bundle
 -> exhaustive R_all
 -> independently predeclared complete-Q_spec state omega_in
 -> scalar relative-history functional Z.
```

Source:
`STAGE8_T7_OPERATOR_VALUED_PRIMITIVE_RESPONSE_ARCHITECTURE_AMENDMENT_V001.md:47-70`.

`COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md:11-24`
positively derives that finite/algebraic scalar closure while saying:

```text
It does not retroactively scalarize primitive Stage 8.
```

The finite global-holonomy diagnostic then records at
`COMPLETE_QSPEC_FINITE_HOLONOMY_RESPONSE_RESULT_V001.md:131-165` that its
finite CTP Hessian, finite-difference metric, and Duhamel response agree. The
same source expressly withholds local transverse response, continuum and
regulator independence, and the intensive Duhamel-Hessian theorem.

### 4.3 Why the finite result is not the complete BR/CTP map

The finite primitive output lives in `End(H_S)` and is differentiated with
respect to an external history parameter. The requested map begins with a
raw connected bilocal contour correlator on a completed gauge-fixed physical
quotient and ends in an action-valued retarded inverse-kernel operator.

The current extension status is:

```text
finite_operator_bundle_to_complete_BR_CTP_response_extension_derived = false | TYPE-U | would-build: the actual-parent response and state evaluation, source-inclusive continuum/refinement limit, complete bilocal-source quotient, physical gauge/edge domains, and an intertwining theorem that makes restriction of the complete map agree with the finite Duhamel diagnostics
```

The load-bearing source at
`primitive_record_cell_selection_principle_v004.md:107-123` forbids replacing
that construction by the shorthand `G^(-1)-K_ind[G]`; the CTP metric, index
order, block inversion, quotient, contacts, and boundary terms must all be
derived.

The finite bundle is therefore a restriction/control candidate, not a licence
to report the complete map as derived.

### 4.4 Symbol collisions excluded

Three same-surface tokens denote different objects:

1. `H_R` in `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_SPEC_V001.md:37-81` is
   the finite **record Hilbert space**. `H_R[G]` here is the **retarded action
   Hessian**.
2. `R_comp,K` in that finite spec at `:129-140` is the
   **completed-record operator kernel**. `R_comp[G]` in
   `primitive_record_cell_selection_principle_v004.md:158-168` is the
   **complementary Dyson residual**.
3. `G` in
   `STAGE8_T7_PRIMITIVE_OPERATOR_RESPONSE_EXACT_DERIVATION_V001.md:101-121`
   is a passive source-basis unitary. `G` in
   `primitive_record_cell_selection_principle_v004.md:49-69` is the raw
   connected contour correlator.

No identification among those pairs is used.

## 5. Conditional map specification

### 5.1 Name and type

Define the extraction operator:

```text
E_R^(CTP_PHYS_INPUT_PACKAGE):
  Dom_R(CTP_PHYS_INPUT_PACKAGE)
    -> RetHess(CTP_PHYS_INPUT_PACKAGE)

E_R(G) := H_R[G].
```

This definition is a test target. It is not a producer proof.

The current relay's stop condition is applied as follows:

```text
missing_instantiation_object = CTP_PHYS_INPUT_PACKAGE
construction_stop_point = BEFORE INSTANTIATION OR DERIVATION OF THAT PACKAGE
```

Sections 5.2-5.5 contain only the conditional signature and test oracle
authorized by Q-52. They do not construct, import, or assume the missing
package. If "stop there" were instead read to prohibit even a conditional
signature after naming an unbuilt input, the requested domain, codomain, and
relation could not be supplied; Q-52 expressly authorizes a specification
whose inputs remain marked `derived = false`.

### 5.2 Domain

An input is a pair `(G, CTP_PHYS_INPUT_PACKAGE)` satisfying all of the
following independently checkable conditions:

1. `G^(IJ)` is the raw connected contour correlator obtained with the same
   symmetric bilocal-source convention as
   `primitive_record_cell_selection_principle_v004.md:28-55`:

   ```text
   G^(IJ) = 2 delta W_inc/delta R_IJ - Abar^I Abar^J.
   ```

2. Compound indices include the CTP branch, physical field label, and
   spacetime point; contraction uses the declared oriented branch metric and
   invariant measure.
3. Antisymmetric bilocal directions have been excluded and the CTP
   reality/Hermiticity involution is fixed.
4. Gauge-null directions have been removed by the completed physical quotient.
   The physical identity and both operator domains are fixed.
5. `G` has a two-sided convolution inverse on that quotient with the declared
   prescription. Boundary, edge, and contact conditions are part of the
   inverse domain, not later corrections chosen from output.
6. The linear-source and bilocal-source connected two-point definitions agree
   in the declared convention:

   ```text
   delta Abar/delta J = (i/hbar) G.
   ```

7. The input lies in the nonzero differentiable `Log_0` neighborhood and
   satisfies the declared CTP reality and source-surface normalization.

An input that lacks any domain datum is outside the applicability of the
physical test. That returns `NO_VERDICT`; it is not evidence against the map.

### 5.3 Codomain

`RetHess(CTP_PHYS_INPUT_PACKAGE)` is the space of kernels that:

1. are action-valued bilinear operators from physical common-history
   connection perturbations to dual difference-history perturbations;
2. use the same quotient, measure, branch metric, index ordering, prescription,
   contacts, and boundary/edge domains as the input;
3. have retarded support, including declared equal-time/contact and boundary
   distributions;
4. satisfy the CTP reality relation with the corresponding advanced block;
5. are covariant under independently derived physical endpoint intertwiners;
6. satisfy the full Ward identities on the quotient once those identities are
   part of the input package.

The dimension ledger at
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_complete_dimension_convention_ledger_v004.md:355-375`
states:

```text
The raw contour correlator has [G]=L^-2; its convolution inverse has
[G^-1]=L^-6, while the retarded action Hessian has units hbar L^-6.
```

It also says the missing map contains:

```text
the CTP metric, i hbar, index order, physical block inversion, contacts, and
boundary terms.
```

The codomain is therefore an operator space, not a scalar coefficient space.

### 5.4 Defining relation

First convert the contravariant raw correlator to an operator using the exact
compound-index order, branch metric, and measure in the input package. Define
its physical two-sided convolution inverse `I_C[G]` by:

```text
integral_C dmu_C(z)
  I_C[G]_(I K)(x,z) G^(K J)(z,y)
    = delta_I^J delta_phys(x,y),

integral_C dmu_C(z)
  G^(I K)(x,z) I_C[G]_(K J)(z,y)
    = delta^I_J delta_phys(x,y).
```

Set the physical bilocal source to `R=0`. Let the physical reduced 1PI
functional be the stationary reduction:

```text
Gamma_1PI[Abar]
  := Gamma_2PI[Abar,G_*(Abar)] |_(R=0),

delta Gamma_2PI/delta G |_(G_*,R=0) = 0.
```

This is the reduction stated at
`primitive_record_cell_selection_principle_v004.md:172-178`. The same source
at `:71-79` gives the unreduced 2PI identities:

```text
delta Gamma_2PI/delta Abar = -J - R Abar,
delta Gamma_2PI/delta G    = -R/2.
```

Thus `delta Gamma_1PI/delta Abar=-J` only on the declared `R=0` source
surface. When the `G`-tangent block is invertible, the **total** second
variation of the reduced functional is the physical Schur complement:

```text
D^2 Gamma_1PI
  = Gamma_(Abar Abar)
    - Gamma_(Abar G) Gamma_(G G)^(-1) Gamma_(G Abar),
```

all blocks being evaluated at `G_*` on the same quotient and domain. This
Schur relation is a required consistency condition, not an independently
derived program result.

The reduced contour action Hessian is fixed by Legendre duality:

```text
H_C[G] := i hbar I_C[G].
```

This factor is not a fitted convention. With

```text
W_inc = -i hbar Log_0 Z_inc,
Abar = delta W_inc/delta J,
delta Abar/delta J = (i/hbar)G,
Gamma_1PI[Abar] = Gamma_2PI[Abar,G_*(Abar)] |_(R=0),
delta Gamma_1PI/delta Abar = -J,
```

the inverse-function relation gives:

```text
delta^2 Gamma_1PI/delta Abar delta Abar
  = -delta J/delta Abar
  = i hbar G^(-1)
```

on the completed physical quotient. This is a conditional defining
calculation; it does not prove that P1-P8 exist for the program's completed
microscopic object.

Pin the Keldysh variables exactly as
`alpha_complete_dimension_convention_ledger_v004.md:260-267`:

```text
A_c     = (A_+ + A_-)/2,
A_delta = A_+ - A_-.
```

Equivalently:

```text
[A_+]   [1  1/2] [A_c    ]
[A_-] = [1 -1/2] [A_delta].
```

Let this displayed matrix be `T_CTP`. Transform the contour Hessian as a
bilinear form:

```text
H_(c,delta)[G] = T_CTP^T H_C[G] T_CTP.
```

The retarded extraction is the ordered mixed block:

```text
E_R(G)
  = H_R[G]
  := (H_(c,delta)[G])_(delta,c)
  = delta^2 Gamma_1PI
      / (delta A_delta delta A_c)
    at A_delta=0 and R=0,
```

with the first argument at `x`, the second at `y`, and all branch metric,
DeWitt, contact, and boundary conventions inherited from the package.

The codomain test additionally requires:

```text
supp H_R(x,y) subset {y in J^-(x)} union declared contact/boundary support,
H_A(x,y) = H_R(y,x)^dagger
```

and the independently derived Ward/endpoint identities.

### 5.5 Consumer relation and stopping point

Only after an exact induced retarded Hessian exists may a consumer form:

```text
R_phys[G] := H_R[G] - Pi_R,ind[G].
```

Source:
`primitive_record_cell_selection_principle_v004.md:115-123`.

The exact induced kernel and the covariant local projector are separate Q-51
objects. They are not specified here:

```text
exact_induced_retarded_kernel_derived = false | TYPE-U | would-build: the complete induced CTP operator and its exact second variation on the same physical package
covariant_local_projector_derived = false | TYPE-U | would-build: a target-independent coefficient functional and inclusion on the derived retarded operator space, with its pairing if orthogonality is claimed
```

The missing object required to instantiate this specification is
`CTP_PHYS_INPUT_PACKAGE`. The construction stops here: it neither supplies
that package nor imports finite response data as a premise for it.

## 6. Attached failure-capable test

The test suite below is specified, not executed. A missing applicability
precondition returns `NO_VERDICT`, following
`NEGATIVE_RESULT_TYPING_PROTOCOL_V001.md:82-99`.

### T1. Quadratic CTP Legendre calibration

Construct a separately sealed finite quadratic CTP fixture on a nondegenerate
physical quotient. Independently derive:

1. its action Hessian directly from the quadratic action;
2. its normalized `Z`, `W`, and raw connected `G` from the same source
   conventions;
3. its stationary 2PI Schur complement, where the fixture has nontrivial
   mixed `Abar/G` blocks;
4. its retarded block directly in the pinned `(c,delta)` ordering.

Apply `E_R` to the independently obtained `G`.

Failure condition:

```text
E_R(G) differs from the independently differentiated retarded action Hessian
or stationary Schur complement in sign, i*hbar factor, compound-index order,
domain, contact term, boundary term, or retarded block.
```

Such a mismatch is `TYPE-R` evidence against this specification. Passing T1
calibrates the algebra and conventions only; it does not derive the complete
physical input package.

### T2. CTP representation and endpoint-intertwiner naturality

Represent the same domain-valid fixture in two independently fixed admissible
CTP bases. When endpoint gauge intertwiners are available, also use two
gauge-related representatives of the same physical `G`.

For a passive invertible CTP coordinate change `A=S A'`, the contravariant
correlator and the **full contour Hessian** must obey the distinct congruence
laws:

```text
G'   = S^(-1) G S^(-T),
H_C' = S^T H_C S.
```

The already-extracted mixed retarded block has no free full-CTP index and
must not be transformed by the second display directly. Each package instead
uses its own independently fixed common/difference injections:

```text
H_R^[package]
  = i_delta^[package]* H_C^[package] i_c^[package].
```

The test first checks full-contour congruence, then applies the two packages'
injections, and only then compares the retarded blocks through the induced
physical-field maps. This basis leg does not assume that the pinned
`(c,delta)` rotation is unitary. Separately, for a physical endpoint gauge
intertwiner, require:

```text
E_R(U_G[G]) = U_H[E_R(G)],
```

where `U_G` and `U_H` are the independently derived transformations on the
contravariant correlator and covariant Hessian domains. They may not be fit
from response output.

Failure condition: a certified non-intertwining output on a domain-valid pair
is `TYPE-R` against the specification. Until the physical endpoint
intertwiners exist, the gauge leg returns `NO_VERDICT`; the basis leg remains
a mathematical calibration.

### T3. Retarded support, reality, and Ward test

For a domain-valid causal input:

1. test retarded support including declared contacts and boundary terms;
2. test `H_A(x,y)=H_R(y,x)^dagger`;
3. test both endpoint Ward identities on the physical quotient.

A certified violation is `TYPE-R` against the specification. If the
quotient, prescription, or boundary/Ward package is unbuilt, the test returns
`NO_VERDICT`, not a failure.

### T4. Boundary/contact mutation test

Use two independently admissible quadratic packages with the same local bulk
expression and different predeclared boundary or contact domains. Derive each
Green operator and action Hessian independently.

Failure condition:

```text
E_R erases a boundary/contact difference present in the independently
derived Hessians, or changes when only a representational convention changes.
```

If both packages are independently domain-valid, this outcome is `TYPE-R`
against the specification. If either boundary/contact domain has not been
independently derived, the test returns `NO_VERDICT`.

This test prevents the phrase "inverse of G" from silently dropping the
domain data that the sealed sources identify as load-bearing.

### T5. Finite-restriction commuting-square test

After a complete physical package and finite-to-complete restriction
intertwiners exist, derive a finite raw correlator from the restricted finite
`Z/W` using the same source convention. Apply a separately fixed finite
extraction `E_R^fin`, then contract its finite retarded Hessian in the same
state and history direction as the independently derived finite Duhamel
diagnostic.

Required square:

```text
complete raw G --E_R--> complete retarded Hessian
      | rho_G                         | rho_H
      v                               v
 finite raw G --E_R^fin--> finite retarded Hessian
                                  |
                                  | predeclared contraction
                                  v
                         finite mixed diagnostic
```

The independent finite `Z/W -> finite raw G` derivation and the restriction
intertwiners are part of applicability, not hidden vertical arrows.

Failure condition: a certified mismatch on identical carriers, states,
history directions, orderings, and boundary data is `TYPE-R` against the
specification.

Current applicability:

```text
finite_restriction_commuting_test_executable = false | TYPE-U | would-build: CTP_PHYS_INPUT_PACKAGE, finite-to-complete carrier/refinement intertwiners, actual-parent Phase-A/B response, finite Z/W derivatives yielding finite raw G, and the independently fixed E_R^fin
```

Therefore T5 currently returns `NO_VERDICT`. Different carriers or missing
intertwiners may not be treated as a failed map.

### T6. Hidden-input mutation test

Enumerate every convention that the domain leaves independently admissible.
Vary one at a time before execution.

Applicability requires a certified exhaustive convention list. An incomplete
or unproved enumeration returns `NO_VERDICT`.

Failure condition:

```text
H_R changes under a pure representation change without the derived
intertwiner transformation, or inequivalent H_R outputs survive because a
physical convention was left unfixed.
```

On a certified exhaustive, domain-valid fixture, the first outcome is
`TYPE-R` against covariance and the second is `TYPE-R` against completeness
of the specification. No convention may be retuned after an outcome.

### Test verdict rule

```text
SPEC_REFUTED
  if any applicable T1-T6 failure condition is certified;

SPEC_SURVIVES_APPLICABLE_TESTS
  only if at least one applicable test was actually executed, every executed
  applicable test passed, and every inapplicable test was named;

NO_VERDICT
  with precedence over survival language if the claimed scope depends on an
  unbuilt, untested, or inapplicable prerequisite, or if zero tests are
  applicable.
```

`SPEC_SURVIVES_APPLICABLE_TESTS` is not `derived = true`. Derivation
additionally requires construction from the completed microscopic operator
and independent verification on that object.

## 7. Q-54 negative registry

| Negative | Type | Evidentiary meaning | Builder, scope, or test |
|---|---|---|---|
| Derived map under another name not found | TYPE-S | Empty only in the bounded governing-source inspection | Roots, exclusions, fences, query, and exact file list in Section 2 |
| Primitive scalarization is strictly the same mathematical function as `G -> H_R[G]` | TYPE-R | Strict function-identity hypothesis refuted; Q-51's broader same-layer accounting remains controlling | Executed signature/composition test in Section 3 |
| Primitive completed compression is automatically scalar | TYPE-R | Refuted only for the executed finite primitive compression | Finite non-scalar witness; does not refute complete scalar closure |
| Sealed primitive authority set canonically forces a scalar functional | TYPE-R | Narrow no-go executed over its frozen primitive authority set | Non-scalar witness, finite trace test, continuum obstruction, and exhausted closure classes in Section 3.2 |
| Primitive scalarization derived | TYPE-U | Superseded primitive-scalar object unbuilt | Active Route-2 replacement is complete-`Q_spec` state evaluation |
| Actual-parent finite bridge derived | TYPE-U | Actual Phase A/B not executed | Build Phase A, then forward-sealed Phase B |
| Finite scalar CTP extended to raw bilocal `G` | TYPE-U | Missing composition between finite `Z` and map domain | Build full bilocal-source/log/continuum package |
| Finite response extended to complete BR/CTP layer | TYPE-U | Finite result is not automatically complete | Build continuum/refinement/gauge/edge intertwiners and complete package |
| Raw correlator map derived | TYPE-U | Specification exists; producer does not | Build and independently verify Section 5 from completed microscopic input |
| Exact induced retarded kernel derived | TYPE-U | Separate Q-51 object unbuilt | Complete induced CTP second variation |
| Covariant local projector derived | TYPE-U | Separate downstream Q-51 object unbuilt | Derive `p_loc`, inclusion, operator projector, and pairing if needed |
| T5 executable at complete scope | TYPE-U | Applicability inputs unbuilt | Build complete package and restriction intertwiners |

No TYPE-C negative is used. Q-52 removed the former charter-scope constraint
for this specification. No TYPE-S statement in this artifact is permitted to
escape its recorded roots and exclusions.

## 8. Result and protected status

The four requested answers are:

1. The correlator-to-Hessian map is not strictly the same mathematical
   function as the primitive scalarization bridge. That strict-identity
   hypothesis is TYPE-R refuted by the executed sealed-signature test, while
   Q-51's same-response-extraction-layer accounting remains controlling.
   Primitive scalarization is an obsolete-branch sibling. The actual-parent
   bridge is TYPE-U and prospectively upstream by its sealed specification;
   its instantiated identity question returns `NO_VERDICT` until Phase A/B
   exists.
2. The finite operator bundle, finite Duhamel tangent, complete-`Q_spec`
   finite/algebraic scalar closure, and finite diagnostic crosscheck are
   derived. They withhold the complete physical quotient, bilocal `G`,
   continuum/refinement extension, retarded inverse map, contacts/boundaries,
   and complete BR/CTP response.
3. Section 5 specifies the conditional domain, codomain, inverse/Legendre
   relation, Keldysh retarded extraction, and premises. Its required unbuilt
   object is named `CTP_PHYS_INPUT_PACKAGE`, and construction stops there.
4. Section 6 attaches six failure-capable tests. Finite algebraic tests can
   test the specification; complete physical applicability returns
   `NO_VERDICT` until its TYPE-U inputs exist.

```text
artifact_type = Q52_RESPONSE_EXTRACTION_SPECIFICATION_AND_TEST_DESIGN
correspondence_check_completed = true
correspondence_type_test_executed = true
primitive_scalarization_strict_identity_refuted = true
Q51_same_response_extraction_layer_status = RETAINED
finite_primitive_operator_response_bundle_derived = true
finite_primitive_operator_Duhamel_tangent_derived = true
finite_algebraic_complete_Qspec_state_evaluation_scalar_closure_derived = true

raw_correlator_to_retarded_Hessian_map_derived = false | TYPE-U | would-build: Section 5 from CTP_PHYS_INPUT_PACKAGE, followed by independent T1-T6 verification
CTP_PHYS_INPUT_PACKAGE_derived = false | TYPE-U | would-build: P1-P8 in Section 0
finite_operator_bundle_to_complete_BR_CTP_response_extension_derived = false | TYPE-U | would-build: actual-parent Phase A/B plus source-inclusive continuum/refinement and physical quotient completion
actual_finite_parent_operator_to_scalar_bridge_derived = false | TYPE-U | would-build: actual-parent Phase A followed by forward-sealed Phase B state evaluation
exact_induced_retarded_kernel_derived = false | TYPE-U | would-build: complete induced CTP operator and exact second variation
covariant_local_projector_derived = false | TYPE-U | would-build: target-independent coefficient functional, inclusion, projector, and any claimed pairing

T1_through_T6_execution_completed = false | TYPE-U | would-execute: run each test after its named applicability inputs exist
physical_verdict = NO_VERDICT
construction_import_status = NONE [PROCESS_ATTESTATION; NON-Q54_RESULT]
protected_holdout_access_status = PROHIBITED_AND_NOT_PERFORMED [PROCESS_FENCE_ATTESTATION; NON-Q54_RESULT]

terminal_fence_declarations_below = Q54_EXEMPT
alpha_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
proof_authorized = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_record_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
kappa_Thomson_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
coupling_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
radius_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
scale_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
root_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
eigenvalue_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
beta_function_computed = false [TERMINAL_FENCE_DECLARATION; Q54_EXEMPT]
```
