# STAGE 8 — ADVERSARIAL AUDIT OF THE WALL'S LOG-FREE-RATE DETERMINATION (STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md, 172e8bbf) — T4SR AUDIT V001

## ADVERSARIAL AUDITOR — CODENAME RATE-AUDIT — COMMISSION T4SR — [SEALED]

Date: 2026-08-15 (session CDT early). Target:
`STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md` (172e8bbff1c9c17a..., FULL digest
recomputed at path, sidecar MATCH via `shasum -a 256 -c`, verified BEFORE
the file was opened). Posture: DEFAULT-REFUTE; testimony carries ZERO
weight — every load-bearing claim re-derived at the bytes of the sealed
ground, every load-bearing symbolic step re-run in a fresh venv, every
input seal verified by me before consumption.

Gates: `alpha_computed = false` ; `proof_authorized = false` ;
`kappa_record_computed = false`. ALL_RESULTS = CLAIMED until registered.
DETERMINATION ONLY — no adoption, no authored physics, no member binding.

Fences held: EXACT SYMBOLIC ONLY — the build's 8-check CAS battery
extracted VERBATIM from the target's §7 and re-run in a fresh venv
(`t4srauditvenv`, sympy 1.14.0, session scratchpad): 8/8 PASS, output
BYTE-IDENTICAL to the recorded block (diff empty); plus my own 10-check
adversarial battery (verbatim §7 below, output verbatim; TWO pre-final
auditor-side corrections disclosed in §7's header — an expectation error
whose failure itself CONFIRMED the audited parity, and a harness bool()
error; no check weakened). Every constant symbolic or an exact
rational/surd; NOTHING numeric evaluated (Lambda, ell, n, M, eps, C_G,
c_G FORMAL); no floats as ground; no measured constant; no value frozen.
Scoped reads: the target plus the SIX tasked ground artifacts (the
WALL_SUMMED_REQUANT pair, the WALL_BOUNDARY_CLOSURE pair, and — the
Gram-transfer route having been used — the MO4_CANCELLATION pair) and NO
others; no file matching register|road_|ledger|lens|plan|tracker|
THE_HANDOFF opened (fenced classes and all other files matched by
FILENAME ONLY in sweeps, never opened). "Q-..." tokens
EXPECTED-UNLOCATABLE per the tasking; noted, never chased. No git; no
existing file edited; ONE output (this artifact) plus its seal sidecar at
the commission-distinct path, probed ABSENT at session start AND
re-probed ABSENT immediately before write. SWEEP CUTOFF DECLARED (§6).
Homonym discipline inherited: n = carrier index; k = momentum inside |k|
and khat; (o, c) = the REM strata indices as quoted at bytes.

---

## 0. VERDICT IN ONE LINE

**CONFIRMED-WITH-CORRECTIONS — the T4SR verdict UNDECIDABLE-TODAY is CORRECT and stands adjudicated at the bytes: no log-free rate for the degree-3 diagonal class of the summed assembly is certifiable on the named sealed ground, no certification was smuggled at any other grade, and each of the four blockers is REAL at my own independent sweep of the bytes. PER CLAIM: (1) THE ONE-VERTEX TRACE-PARITY LEMMA — CONFIRMED AND STRENGTHENED: the one-J/one-alpha_x count is at WSR §2.2 bytes (F carries ONE J; U(0) none; J = -(Q b_D Q) tensor alpha_x at the PA display), the alphabet closure is the record's own type ledger (WSR T-2/WS6), and the parity itself is not merely a swept fact but an ALL-LENGTHS IDENTITY — joint momentum inversion on the letter span IS conjugation by S (S(u.alpha)S = -u.alpha generic, S S S = S, S 1 S = 1), so W(-khat,-khat') = S W S for EVERY word of EVERY length and tr[alpha_x W(-)] = -tr[alpha_x W] by cyclicity + S alpha_x S = -alpha_x (my R2; the build's C-4 length-cutoff caveat is DISCHARGED, not merely disclosed — the sweep hides no even subclass; my exhaustive two-momentum re-sweep one full length past the build's, all 364 words, concurs); (2) THE REDUCTION — CONFIRMED-WITH-CORRECTION COR-A: the verdict-bearing direction is EXACT (every degree-3 amplitude khat-even ==> every stratum's angular integral dies ==> count at degree <= -4 = 1 - 1/Lambda BOUNDED, T4/T5 re-run + my R4/R5), but the displayed "IFF" and the flag's refutation branch ("any stratum with a nonzero odd moment ==> the rate is REFUTED") OVERSTATE the converse — a nonzero odd moment at ONE stratum makes THAT stratum's carrier integral log-divergent while the CLASS total could in principle still cancel across strata; the correct branch condition is a nonzero TOTAL first moment (the census's displayed moments SUM to the class's log coefficient, so THE NAMED CENSUS STILL DECIDES BOTH WAYS — the deciding-object claim survives verbatim); VERDICT-NEUTRAL: the weaker true premise is undecidable on the SAME absences; (3) THE BLOCKERS — ALL FOUR CONFIRMED at my own sweeps: B-1 the degree-3 angular census is ABSENT (my token sweep of all SIX ground artifacts: the only "o = 3" line is WSR-AUDIT :152's existence claim, the only "segment-corrected" line is WSR :393's naming, NO angular symbol display anywhere; AC-4 verbatim at WSR-AUDIT :558-561), B-2 the c-class refusal is REAL at the MO4 §3.3 display (tr[jet] = 4 khat_x(b_+ - b_-) joint-EVEN — my R6 re-derives it AND the alpha_x-traced piece 4 khat_x^2(b_+ + b_-), also joint-even with nonvanishing moment: parity kills nothing at the displayed shape; no moment certificate for beta_s exists in the six — swept), B-3 every trace budget polynomial (8n^3 and 12 sqrt2 n^{7/2}/ell at WSR/WBC bytes; the two new caps re-derived independently — the direct summed Gram chain ||K^Sigma_n||_1 <= 2||F||_1 <= 2 G_n <= 2 C_G n^{3/2} with every norm fact re-pinned (R7: split reconstruction, tensor trace-norm multiplicativity, ||alpha_x||_1 = 4, PSD trace-norm = trace, sum|w_l| = 1; unitarity and ||C_n||_op <= 1 of record) and NO V-2 crossing (branch facts feed the summed bound — the allowed direction; no per-pair claim is emitted anywhere), and the rank x HS cap |tr K^Sigma_n| <= 2 n^{3/2} M/eps re-derived with the Lagrange identity at r = 3 AND r = 4 plus the Hermitian-trace route (R8) — both caps REFUSED as certification exactly per the commission's own bar, the refusal now pinned on BOTH axes (R9): the caps are Lambda-free while the class's symbol-count log Lambda is Lambda-divergent, and at the sealed Lambda = sqrt(2n)/ell linkage (MO4 §4.6 bytes) the SHARP floor c_G n^{3/2} itself outruns the log — estimation, never elimination, final not provisional), B-4 region cumulants EMPTY (my grep: ZERO "cumulant" hits in all six); (4) GRADE UPGRADED, one notch, in the build's favor: my commission names the MO4 PAIR — the MO4 audit (bfd86217, sealed 00:25:xx, BEFORE the build's 02:58:05 cutoff, OUTSIDE the build's five-artifact scope; its "no audit in scope" is literally accurate) is CONFIRMED-WITH-CORRECTIONS with all corrections cosmetic and the CL-A two-sided Gram mass EXPLICITLY confirmed — the build's CONDITIONAL(CL-A)-at-CLAIMED-grade hedge (C-3) dissolves in the FAVORABLE direction: the delivered-meanwhile budget and B-3's sharpness clause now carry AUDITED grade (registrar margin note N-a; no re-issue forced — the build's verdict never rested on them, per its own C-3, which I verified); AND the MO4 audit contains NO census, NO moment certificate, NOTHING that decides the premise — the UNDECIDABLE-TODAY verdict is insensitive to the pair-read in both directions. FENCE-SCAN CLEAN: no float ground, no numeric evaluation, no fenced-class content, gates present, one output + sidecar, cutoff-before-write in order, sw-1/sw-2/sw-3 sweep claims independently verified (mtime and token re-sweeps; the CENSUS-named files are family/carrier censuses by name only; the cleanroom root is unoccupied — its one grep hit is a false positive inside "DECORATED"). NOT-REFUTED discipline held on both sides: no divergence of tr K^Sigma_n exhibited anywhere. SCOPE GUARD held: per-composite wall, T_n, o-2-OBS, V-2 — untouched, verified span by span (the lemma's grammar ASSIGNS the o-2-OBS even-integrand refusal to the amplitude side — my R10 re-pins the consistency). CAS: build battery 8/8 BYTE-IDENTICAL re-run; audit battery 10/10 PASS. THE HEADLINE, adjudicated at the bytes: UNDECIDABLE-TODAY, with the commission REDUCED to the degree-3 angular census (branch condition per COR-A) — CONFIRMED.**

---

## 1. SEALS VERIFIED AT PATH (shasum -a 256 -c), BEFORE ANY RELIANCE

All under `/Users/bgm/MB Work/alpha-program-archive/workspace/`. Every
digest recomputed from bytes at path THIS session by FULL digest via
`shasum -a 256 -c` against its `.seal.sha256` sidecar; the target
verified BEFORE opening. 7/7 MATCH (OK); NONE UNVERIFIABLE. Scoped
reads: these seven files and NO others.

```text
172e8bbff1c9c17a  STAGE8_WALL_LOGFREE_RATE_T4SR_V001.md            OK  [THE TARGET]
6e5762a8b785f4b7  STAGE8_WALL_SUMMED_REQUANT_S9AD_V001.md          OK  [WSR — primary ground; read IN FULL]
549f14238ec42496  STAGE8_WALL_SUMMED_REQUANT_S9AD_AUDIT_V001.md    OK  [WSR-AUDIT — CONFIRMED-WITH-CORRECTIONS; read IN FULL]
80db260fa1561d76  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_V001.md        OK  [WBC — read IN FULL]
46938251aeaa9af3  STAGE8_WALL_BOUNDARY_CLOSURE_S9AD_AUDIT_V001.md  OK  [WBC-AUDIT — CONFIRMED-WITH-CORRECTIONS; §0-§5 + flags at bytes]
0e894372d1c53dc6  STAGE8_MO4_CANCELLATION_S9AD_V001.md             OK  [MO4 — §0-§5 + CH-8 + flags at bytes]
bfd86217319ecabc  STAGE8_MO4_CANCELLATION_S9AD_AUDIT_V001.md       OK  [MO4-AUDIT — the pair completed HERE; §0-§4 + flags at bytes]
```

Load-bearing byte-pins chased independently of the build's citations:
WSR §2.2 (K^Sigma = C(U(0)^dag F + F^dag U(0))C; F = -i int_0^1 sum_l
w_l W_l(1,t') J(t') W_l(t',0) dt' — ONE J; U(0) = sum_l w_l u_l(0) —
none), §4 (||U(0)||_op <= 1; ||K^Sigma_n||_2 <= M/eps; 8n^3), §5
(t-1..t-3; s-3 budgets; §5.3 the residue = this commission), §10-§12;
WSR-AUDIT :152 ("degree -3 strata exist (o = 3, c = 0; c-bearing
routes)"), :500 (A3 tr[alpha_x C(k)] = -2 khat_x), :528-535 (a-2: MO4
adjacent, "neither occupies this commission nor decides tr K_H^Sigma"),
:558-561 (AC-4 verbatim: "no stronger claim (e.g., stratum-by-stratum
parity at degree -3) is made by me or by the build"); WBC §2 CS-5
(J = -(Q b_D Q) tensor alpha_x; b_D in [0,1]), §3.3 (rank <= 4n^3 on
ran C_n; ||C_n||_op <= 1 via 52f2490b LINK 1; F1), §4; WBC-AUDIT §0/§5
(COR-1 splitting sign; COR-4 F1/F3 each sufficient) and flag block; MO4
§4.1 (the profile split, verbatim), §4.2 (g_n; G_n = 4 int_0^1 g_n),
§4.3 (CL-A two-sided; "no better n-power can come out of this split"),
§3.3(ii) (the jet amplitude "k_hat_x [(beta_+ - beta_-) I + (beta_+ +
beta_-) alpha.k_hat]" verbatim), §4.6 (Lambda = sqrt(2n)/ell), CH-8
(Q_n-compressed, scheme-blind); MO4-AUDIT §0 (CONFIRMED-WITH-CORRECTIONS,
all cosmetic; CL-A ceiling AND floor "derived exactly from CL-A's SEALED
statement bytes"), §4 c-1..c-5 (cosmetic), flags. "Q-..." tokens:
EXPECTED-UNLOCATABLE; noted, never chased.

---

## 2. METHOD — WHAT ZERO-WEIGHT TESTIMONY MEANT HERE

```text
m-1 Every load-bearing quote in the target was re-located at the ground
    bytes by my own reads/greps (§1 pins), not trusted from the target.
m-2 Every load-bearing symbolic step was re-run: the target's battery
    VERBATIM (byte-identical output), then my own battery testing what
    the build did NOT run (all-lengths conjugation; a two-momentum
    sweep one length deeper; degree-4 moment spot checks; the
    alpha_x-traced jet piece; independent norm instances; Lagrange at
    r = 4; the fixed-n axis-honesty limit).
m-3 Every absence claim (B-1, B-2, B-4, sw-1, sw-2) was re-swept by my
    own token greps over all SIX ground artifacts — one artifact MORE
    than the build's scope (the MO4 audit), so each absence is
    confirmed on a STRICTLY LARGER basis than claimed.
m-4 The sweep-hygiene claims (sw-3 newest-files; cutoff-before-write;
    one-output) were verified by filename/mtime listing only.
m-5 The hunt list was worked item by item (§3.6): log-into-constant;
    wrong quantifier / V-2 leakage; enclosure out of domain;
    cancellation against a refusing ground; cutoff-hidden subclass;
    hidden numerics. Findings: NONE fatal; one framing overstatement
    (COR-A); one favorable grade upgrade (N-a).
```

---

## 3. THE CLAIMS, RE-DERIVED

### 3.1 The lemma (target §3.1 a-1..a-3): CONFIRMED, and strengthened to ALL lengths

```text
r-1 ONE VERTEX: at WSR §2.2 bytes F carries exactly one J and U(0)
    none; J = -(Q_n b_D Q_n) tensor alpha_x at the PA display quoted
    by WSR §1/WBC CS-5/MO4 §4.1 — one alpha_x per stratum. GROUNDED.
r-2 ALPHABET CLOSURE: WSR T-2/WS6 at bytes — h_0 massless (alpha.k),
    the chiral vertex S = -i gamma^0 gamma^5, band/sea projectors
    affine in alpha.khat, every spatial profile scalar tensor
    1_spinor; no beta letter enters the assembly. GROUNDED at the
    record's own type ledger, exactly as the build marks (C-4).
r-3 THE PARITY, ALL LENGTHS (my R2 — the build's T2/T3 sweep is
    correct and is now a corollary): on the letter span
    {a.alpha + b S + c I}, joint momentum inversion IS conjugation by
    S (S(a.alpha)S = -a.alpha generic-coefficient, S S S = S,
    S 1 S = 1, letterwise homomorphism, S^2 = I), so for EVERY word W
    of EVERY length W(-khat,-khat') = S W S, and
      tr[alpha_x W(-)] = tr[S alpha_x S W] = -tr[alpha_x W].
    The spinor factor of EVERY diagonal stratum at EVERY degree is
    joint-ODD — identity grade, no length cap. My exhaustive
    two-momentum re-sweep to length 5 (364 words, one full length past
    the build's 121) concurs: odd-or-zero throughout, nonzero
    instances present. The C-4 caveat is DISCHARGED.
r-4 CONSISTENCY: tr[alpha_x C(k)] = -2 khat_x (WSR-AUDIT A3) reproduced
    from the same ground (R3); o-2-OBS's even integrand
    khat_x(khat_x - khat'_x) re-pinned EVEN (R10) — the refusal lives
    on the amplitude side in the lemma's grammar; the record unmoved.
```

### 3.2 The reduction (target a-4, §2 q-3, §4, flag): CONFIRMED-WITH-CORRECTION (COR-A, §4 below)

```text
r-5 THE VERDICT-BEARING DIRECTION IS EXACT: amplitude khat-even ==>
    stratum angular integral = 0 (R4: first moment against every even
    monomial to degree 2 AND four degree-4 spot checks = 0) ==> the
    class dies ==> count at degree <= -4 ==> carrier integral
    1 - 1/Lambda BOUNDED (R5; WSR t-3's "every surviving diagonal
    contribution has degree <= -3" at bytes). The all-even branch of
    the census DOES mechanically certify the rate on the lemma.
r-6 THE CONVERSE IS NOT DERIVED (COR-A): see §4. The commissioned
    equivalence should read: the class's log coefficient equals the
    SUM of the strata's first angular moments (each the census would
    display); all-even is the sufficient structural form; the census
    decides BOTH ways by displaying the exact moments. The deciding-
    object claim (target §4) survives VERBATIM under this restatement.
```

### 3.3 Route B (target §3.2, B-4): CONFIRMED

```text
r-7 My grep: "cumulant" — ZERO hits in ALL SIX ground artifacts (the
    build claimed five; the sixth concurs). No region-decomposition
    object exists in scope; constructing one is fenced
    (proof_authorized = false). EMPTY, correctly refused.
```

### 3.4 Route C (target §3.3, B-3): CONFIRMED; grade upgraded (N-a)

```text
r-8 THE DIRECT SUMMED CHAIN, re-derived step by step:
      ||K^Sigma_n||_1 <= 2 ||U(0)||_op ||F||_1        [K^Sigma =
        C(U^dag F + F^dag U)C; ||C_n||_op <= 1 of record (WBC §3.3);
        triangle + Hoelder; two terms]
      ||F||_1 <= int_0^1 sum_l |w_l| ||W_l J W_l||_1 dt
              = int_0^1 ||J(t)||_1 dt                  [Bochner
        triangle; W_l(1,t'), W_l(t',0) a = 0 propagators, UNITARY of
        record; sum_l |w_l| = 1 (WSR WS1; R7 re-pin)]
      ||J(t)||_1 = ||Q_n b_D Q_n||_1 ||alpha_x||_1 = 4 g_n(t)
        [the split reconstructs: (Q b^{1/2})(b^{1/2} Q) = Q b Q — R7;
         tensor trace-norm multiplicativity — R7 independent instance;
         Q b Q PSD (b_D >= 0 at PA/WBC bytes) so ||.||_1 = tr — R7;
         ||alpha_x||_1 = 4 — R7]
      int_0^1 4 g_n = G_n <= C_G n^{3/2}               [MO4 §4.2/§4.3;
        CL-A — now AUDIT-CONFIRMED, N-a]
    ==> |tr K^Sigma_n| <= ||K^Sigma_n||_1 <= 2 G_n <= 2 C_G n^{3/2},
    per (n, cell), at the summed quantifier throughout. NO V-2
    crossing: the triangle DISTRIBUTES the weight sum to consume
    per-branch norm facts (unitarity, ||J||_1) INTO the summed bound —
    per-pair/per-branch content FEEDING the summed object is the
    allowed direction of the one-way record; no per-pair claim is
    emitted anywhere in the target (span-checked).
r-9 THE RANK x HS CAP, re-derived (R8): K^Sigma is HERMITIAN of record
    (WSR k-1), so |tr| <= sum|eig| = sum sigma by the real triangle
    inequality (pinned as an exact identity), and (sum sigma)^2 <=
    r sum sigma^2 (Lagrange, r = 3 AND r = 4 both pinned); rank <=
    4n^3 on ran C_n (WBC of record; K^Sigma is C_n-sandwiched at its
    defining display — domain-correct reuse); ||K^Sigma_n||_2 <= M/eps
    (WSR h-4 — the enclosure's OWN object, no domain overreach):
    |tr K^Sigma_n| <= sqrt(4n^3) M/eps = 2 n^{3/2} M/eps.
r-10 THE REFUSAL, adjudicated on BOTH axes (R9): the commission's bar
    (q-3, disclosed C-2) types polynomial majorants as estimates —
    exactly WSR s-3's own typing. (i) n-axis: 2 C_G n^{3/2}/log -> oo
    and the SHARP floor c_G n^{3/2}/log -> oo at the sealed Lambda =
    sqrt(2n)/ell linkage (MO4 §4.6 AT BYTES — the linkage is of
    record, not imported); the floor c_G n^{3/2} <= G_n is MO4 §4.3
    at bytes: the refusal is FINAL for the route, exactly as claimed.
    (ii) Lambda-axis: every delivered cap is Lambda-free while the
    class's symbol-count is log Lambda -> oo (formal): a bound on the
    TOTAL trace at trace grade neither eliminates nor decides the
    CLASS's carrier integral at the KF1 symbol grade. Estimation,
    never elimination — the caps are correctly registered as
    delivered-meanwhile assets and refusals, not certificates.
r-11 MO-4-R disjointness: the residual concerns R_n; the four summed
    quantities are baseline-free of record (WBC/WSR) — R_n appears
    nowhere in K^Sigma. Correctly excluded.
```

### 3.5 B-1/B-2, at my own sweep: CONFIRMED

```text
r-12 B-1: token sweep of the SIX for degree-3 angular content — the
    ONLY "o = 3" line is WSR-AUDIT :152 (existence, no symbols); the
    only "segment-corrected" line is WSR :393 (naming); NO V_3, NO
    degree-3 angular symbol display exists anywhere in the six; the
    only bytes-quoted subsidiary is the U-side -i V_2(x)/(2|k|)
    (angular-scalar). AC-4 verbatim at WSR-AUDIT :558-561. ABSENT.
r-13 B-2: the jet display at MO4 §3.3(ii) bytes; my R6 re-derives
    tr[jet] = 4 khat_x(b_+ - b_-), joint-EVEN under (khat -> -khat,
    b_+ <-> b_-) — the build's T6 — AND one step past it: the
    alpha_x-traced piece tr[alpha_x jet] = 4 khat_x^2 (b_+ + b_-),
    ALSO joint-even, angular moment (4pi/3)(b_+ + b_-) != 0 at
    constant coefficients: at the displayed shape parity supplies NO
    kill on the c-bearing class in EITHER traced slot. "First angular
    moment" appears in the six ONLY for the degree -2 V_2 (x) V_2
    kill (WSR :58/:885): no moment certificate for the ray averages
    exists in scope. ABSENT + structurally refused, as claimed.
```

### 3.6 The hunt list, item by item

```text
h-1 LOG ABSORBED INTO A CONSTANT: NONE. No rate is certified; C_G,
    c_G, M, eps are n-free and Lambda-free symbols at their defining
    displays (MO4 §4.3 assembles C_G from M_{K0}, ell, pi); the log
    is kept explicit at every appearance (T5/T7/R5/R9).
h-2 WRONG QUANTIFIER / V-2 LEAKAGE: NONE. Every delivered statement is
    per (n, cell) OF the summed object; the Gram chain is direct on
    the summed F (r-8); no per-composite quantity is quoted, moved,
    or decided; the per-composite wall's four quantities, T_n,
    o-2-OBS stand untouched at their own quantifiers (span-checked;
    g-1..g-4 accurate).
h-3 ENCLOSURE OUTSIDE ITS DOMAIN: NONE. ||K^Sigma||_2 <= M/eps is
    WSR's own summed-object enclosure; rank <= 4n^3 rides the C_n
    sandwich at the defining display; the profile split is an
    identity of the sealed J itself (object-identical at WSR/WBC/MO4
    bytes); CL-A is consumed for the same G_n MO4 defines.
h-4 CANCELLATION AGAINST A REFUSING GROUND: NONE claimed. The target
    asserts NO new cancellation — the lemma is parity structure; the
    c-class refusal (T6/R6) and the o-2-OBS consistency (R10) hold at
    bytes; H_A^Sigma = 0 is consumed FROM the record, not re-derived
    wider.
h-5 CUTOFF-HIDDEN SUBCLASS: NONE. The lemma's length cutoff hides
    nothing (r-3: all-lengths identity). The corpus cutoff
    (02:58:05) is coherent: sw-3's newest-files claim verified by
    mtime (RK_LT_BIT pair, 02:26/02:27); post-cutoff arrivals
    (JD_MEMBER_CANDIDATE_T2SR 03:00; MO4R_GRAM_MASS_T3SR 03:04 —
    the latter AFTER the target's own seal) correctly unswept there,
    filename-only here, unopened. The one pre-cutoff unconsumed
    relevant file IS the MO4 audit — outside the build's named scope,
    consumed here, and it STRENGTHENS the build (N-a).
h-6 HIDDEN NUMERICS: NONE. Fence-scan (§5): no float literals (the
    three regex hits are section headings), no evalf/numeric calls;
    all constants exact rationals/surds/pi; the verbatim re-run
    reproduces byte-identically in a clean venv.
```

---

## 4. THE CORRECTIONS

```text
COR-A (framing/logic; VERDICT-NEUTRAL; the one substantive finding):
  The target's §0 ("the commissioned log-free rate holds IFF every
  degree-3 scalar amplitude is khat-EVEN"), (a-4) ("equivalent ... to
  ONE premise"), §4 and the flag's refutation branch ("ANY stratum
  with a nonzero odd moment ==> ... the rate is REFUTED") OVERSTATE
  the converse direction. DERIVED is: all amplitudes khat-even ==>
  rate (exact, T4/T5/R4/R5). NOT derived: rate ==> all even. A
  nonzero odd first moment at one stratum makes THAT stratum's
  carrier integral genuinely logarithmic, but the CLASS's log
  coefficient is the SUM of the strata's first moments, and an exact
  cross-stratum cancellation of that sum — however non-generic — is
  not excluded by any sealed identity (the build's own a-5 excludes
  only the SEALED kill mechanisms, not an accidental census-level
  zero). CORRECT FORM (registrar margin; no re-issue forced): the
  reduction lands on "the class's summed first angular moment
  vanishes", of which all-even is the sufficient structural case; the
  DEGREE-3 ANGULAR CENSUS as named — which displays each stratum's
  EXACT first moments — still decides BOTH ways mechanically (sum the
  displayed moments: zero ==> RATE-CERTIFIED at the symbol-count
  grade; nonzero ==> the log genuine, the rate refuted). WHY
  VERDICT-NEUTRAL: UNDECIDABLE-TODAY rests on the ABSENCE of the
  census (B-1) and of any moment certificate (B-2) — the weaker true
  premise is undecidable on exactly the same absences; no blocker,
  no route verdict, and no flag class moves.

N-a (grade note, FAVORABLE; registrar margin): the MO4 audit
  (bfd86217, sealed ~00:25, before the build's cutoff) was outside
  the build's five-artifact scope — its "no audit sidecar in scope"
  (C-3) is literally accurate, and its filename sweep made no
  contrary claim. Consumed HERE at the tasked pair grade: MO4-AUDIT =
  CONFIRMED-WITH-CORRECTIONS, all corrections cosmetic (c-1..c-5),
  the CL-A two-sided Gram mass ceiling AND floor explicitly
  confirmed, and NO census/moment/degree-3 content anywhere in it.
  Effect: the build's CONDITIONAL(CL-A)-at-CLAIMED-grade marks on the
  delivered-meanwhile budget and on B-3's sharpness clause UPGRADE to
  audited grade; the build's C-3 insulation ("the verdict stands
  without it") was verified and is no longer needed. Nothing else
  moves.

N-b (note): the build's §0 phrase "verified exhaustively at the
  displayed sweep" for an "at EVERY degree" claim carried a real gap
  honestly disclosed at its C-4; r-3 CLOSES it (all-lengths identity;
  my 364-word deeper sweep concurs). Registered as a strengthening,
  not a defect.
```

---

## 5. FENCE-SCAN (standard)

```text
f-1 FLOATS/NUMERICS: grep for float literals over the target — the
    only regex hits are the §3.1/§3.2/§3.3 heading numbers; no
    evalf/N()/float()/numpy tokens; every displayed constant is an
    exact rational, surd, or pi multiple; Lambda, ell, n, M, eps,
    C_G, c_G formal throughout. CLEAN.
f-2 FENCED CLASSES: no register/road_/ledger/lens/plan/tracker/
    THE_HANDOFF content appears; the tokens occur only in the
    target's own fence declarations and ledger-named sections
    (CHOICE LEDGER, sign/type/moment ledgers — record vocabulary,
    not files). CLEAN.
f-3 GATES: present at header and flag block (alpha_computed = false;
    proof_authorized = false; kappa_record_computed = false);
    DETERMINATION-ONLY posture held — no adoption, no authored
    physics, no member binding, no witness action, no flag of the
    record flipped (span-checked). CLEAN.
f-4 HYGIENE: one output + sidecar at the commission-distinct path
    (03:03, after its declared cutoff 02:58:05 — in order); no git;
    seal verifies. CLEAN.
```

---

## 6. SWEEP CUTOFF AND CORPUS SWEEP

SWEEP CUTOFF, DECLARED: **2026-08-15 03:13:05 -0500**. Corpus roots:
`/Users/bgm/MB Work/alpha-program-archive/workspace` (primary) and the
cleanroom root
`/Users/bgm/Documents/New project/gravity_emergence_evidence_program/alpha_fundamental_record_action_cleanroom_v003`
— swept by FILENAME ONLY except the seven §1 files (token greps ran
inside those seven only); fenced classes and ALL other files matched by
filename only, never opened. Artifacts sealed after the cutoff are
UNSWEPT and unclassed here.

```text
sa-1 OCCUPATION: no filename in either root matches an audit of the
     target (this artifact's path was ABSENT at session start and at
     the pre-write re-probe); the CENSUS-named workspace files are
     family/carrier/slot censuses by name, different commissions,
     unopened; the cleanroom root's single grep hit on this
     commission's tokens is a false positive ("RATE" inside
     "DECORATED"); the cleanroom holds no artifact of this
     commission's ground — untouched.
sa-2 sw-1/sw-2/sw-3 of the target: INDEPENDENTLY VERIFIED (h-5, r-7,
     r-12; mtime listing).
sa-3 ADJACENT-BY-NAME, unopened: STAGE8_MO4R_GRAM_MASS_T3SR_V001
     (03:04, post-dates the target's seal; T3SR = a different
     commission); STAGE8_JD_MEMBER_CANDIDATE_T2SR_V001 (03:00).
     Neither is consumed; neither can bear on adjudicating the target
     at its own cutoff.
sa-4 Enumerations here claim only their displayed sweeps.
```

---

## 7. THE CAS BATTERIES (VERBATIM) AND OUTPUT (VERBATIM)

Fresh venv `t4srauditvenv` (sympy 1.14.0) under the session scratchpad;
nothing written to the workspace but this artifact and its seal.

RUN 1 — the target's battery, extracted verbatim from its §7 (awk fence
extraction, 176 lines): 8/8 PASS, output BYTE-IDENTICAL to the target's
recorded block (diff empty; it is the target's own §7 text, verified
identical — not reproduced here).

RUN 2 — my adversarial battery. TWO pre-final auditor-side corrections,
disclosed: (i) my first R2 spot instances chose length-6/7 words with
EVEN momentum-letter counts, whose traces vanish IDENTICALLY — the FAIL
was an expectation error, and the vanishing itself CONFIRMS the audited
parity (odd-or-ZERO); repaired by the exhaustive length-5 two-momentum
sweep (a STRENGTHENING, not a weakening); (ii) R8's symbolic Abs
inequality could not be bool()-ed by sympy — replaced by the exact
identity form. No check condition was weakened; the final run is single
and complete.

```python
# RATE-AUDIT adversarial battery — EXACT SYMBOLIC ONLY (sympy 1.14.0, fresh venv
# t4srauditvenv). Checks the BUILD DID NOT RUN, hunting the tasked failure modes.
# R1  Dirac ground, independent construction (chiral-ordered blocks re-derived).
# R2  THE ALL-LENGTHS UPGRADE of the one-vertex trace-parity lemma: joint momentum
#     inversion = conjugation by S on the letter span (S(u.alpha)S = -u.alpha for
#     GENERIC real u; S S S = S; S 1 S = 1), hence for EVERY word W of EVERY length
#     over the sealed alphabet, W(-khat,-khat') = S W S and tr[alpha_x W(-)] =
#     tr[S alpha_x S W] = -tr[alpha_x W] (cyclicity + S alpha_x S = -alpha_x):
#     the build's length-5/length-4 sweep hides NO diverging subclass — the parity
#     is an identity at ALL degrees. CAS: generic-letter conjugation (3 letters,
#     symbolic coefficients), the trace-flip on generic length-3 letter products,
#     and pure-alphabet instances at lengths 6 and 7 (beyond the build's sweep).
# R3  exemplar tr[alpha_x alpha.khat] = 4 khat_x; consistency with the primary
#     audit's A3: tr[alpha_x C(k)] = -2 khat_x (C(k) = (1 - alpha.khat)/2).
# R4  the kill vs the refusal, exact moments: int_S2 khat_x m dOmega = 0 for every
#     even monomial m (deg <= 2, and deg-4 spot checks the build did not run);
#     int khat_x^2 = 4pi/3 != 0; the 8pi/3 odd-defeat exhibit re-derived.
# R5  threshold arithmetic: deg -3 carrier integral = log Lambda (divergent,
#     formal); deg -4 = 1 - 1/Lambda -> 1 BOUNDED (Lambda FORMAL, never valued).
# R6  the c-class jet at the DISPLAYED bytes (MO4 §3.3): amplitude khat_x[(b+ - b-)I
#     + (b+ + b-) alpha.khat]; its trace 4 khat_x (b+ - b-) joint-EVEN under
#     (khat -> -khat, b+ <-> b-); the alpha_x-traced piece 4 khat_x^2 (b+ + b-)
#     ALSO joint-even with angular moment (4pi/3)(b+ + b-) != 0 at constant
#     coefficients: parity supplies NO kill at the displayed shape — the build's
#     B-2 refusal re-derived one step past its own display.
# R7  Gram-split mechanics on INDEPENDENT instances: (Q b^{1/2})(b^{1/2} Q) = QbQ
#     (PSD square-root reconstruction); ||A tensor B||_1 = ||A||_1 ||B||_1 at a
#     sign-indefinite instance (diag(a1,-a2) tensor sigma_y); ||alpha_x||_1 = 4;
#     ||PSD||_1 = tr at instance 6; sum|w| = 1 re-pin (l1-tight weights).
# R8  rank x HS: Lagrange identity at r = 3 AND r = 4 (the build ran r = 3 only);
#     sqrt(4 n^3) = 2 n^{3/2} exact; Hermitian instance |tr| <= sum|eig|.
# R9  the refusal limits AND the axis honesty: 2 C_G n^{3/2}/log(sqrt(2n)/ell) ->
#     oo and c_G n^{3/2}/log -> oo (the sealed Lambda = sqrt(2n)/ell linkage, MO4
#     §4.6 bytes); AND at FIXED n the symbol-count log Lambda -> oo while the cap
#     is Lambda-free: a polynomial trace budget cannot decide the carrier-axis
#     log at the KF1 grade in either reading — estimation, never elimination.
# R10 the sweep cardinalities: sum_{L=0}^{5} 2^L = 63, sum_{L=0}^{4} 3^L = 121
#     (the build's word counts are exact); o-2-OBS consistency re-pin:
#     khat_x(khat_x - khat'_x) EVEN under joint inversion (the g-2 grammar).
import sympy as sp
from sympy import Rational as R
from sympy import I as Im
from itertools import product as iproduct

ok = lambda name, cond: print(f"{name}: {'PASS' if cond else 'FAIL'}", flush=True)

# ===== R1 — Dirac ground, independent construction =====
s0 = sp.eye(2)
sx = sp.Matrix([[0, 1], [1, 0]])
sy = sp.Matrix([[0, -Im], [Im, 0]])
sz = sp.Matrix([[1, 0], [0, -1]])
Z2 = sp.zeros(2, 2)
blk = lambda a, b, c, d: sp.Matrix(sp.BlockMatrix([[a, b], [c, d]]))
beta = blk(s0, Z2, Z2, -s0)
al = [blk(Z2, s, s, Z2) for s in (sx, sy, sz)]
g5 = blk(Z2, s0, s0, Z2)
S_op = sp.expand(-Im*beta*g5)
Zq = lambda M: sp.expand(M) == sp.zeros(*M.shape)
ok("R1 DIRAC GROUND (independent): {alpha_i, alpha_j} = 2 delta_ij; S = -i b g5"
   " Hermitian, S^2 = I, {S, alpha_j} = 0 all j — the sealed spinor bytes stand",
   all(Zq(sp.expand(al[i]*al[j] + al[j]*al[i] - 2*sp.KroneckerDelta(i, j)*sp.eye(4)))
       for i in range(3) for j in range(3))
   and Zq(sp.expand(S_op - S_op.H)) and Zq(sp.expand(S_op*S_op - sp.eye(4)))
   and all(Zq(sp.expand(S_op*a + a*S_op)) for a in al))

# ===== R2 — the ALL-LENGTHS lemma upgrade (conjugation by S) =====
u1, u2, u3, v1, v2, v3, w1, w2, w3 = sp.symbols('u1 u2 u3 v1 v2 v3 w1 w2 w3', real=True)
b1, b2, b3, c1, c2, c3 = sp.symbols('bb1 bb2 bb3 cc1 cc2 cc3', real=True)
def letter(a_vec, b_s, c_s):
    return sp.expand(a_vec[0]*al[0] + a_vec[1]*al[1] + a_vec[2]*al[2]
                     + b_s*S_op + c_s*sp.eye(4))
L1 = letter((u1, u2, u3), b1, c1)
L2 = letter((v1, v2, v3), b2, c2)
L3 = letter((w1, w2, w3), b3, c3)
L1m = letter((-u1, -u2, -u3), b1, c1)
L2m = letter((-v1, -v2, -v3), b2, c2)
L3m = letter((-w1, -w2, -w3), b3, c3)
conj_letters = (Zq(sp.expand(S_op*L1*S_op - L1m))
                and Zq(sp.expand(S_op*L2*S_op - L2m))
                and Zq(sp.expand(S_op*L3*S_op - L3m)))
W3 = sp.expand(L1*L2*L3)
W3m = sp.expand(L1m*L2m*L3m)
conj_word = Zq(sp.expand(S_op*W3*S_op - W3m))          # S^2 = I: letterwise homomorphism
tr_flip_generic = sp.expand((al[0]*W3m).trace() + (al[0]*W3).trace()) == 0
kx, ky, kz = sp.symbols('kh_x kh_y kh_z', real=True)
kxp, kyp, kzp = sp.symbols('khp_x khp_y khp_z', real=True)
A1 = kx*al[0] + ky*al[1] + kz*al[2]
A2 = kxp*al[0] + kyp*al[1] + kzp*al[2]
inv2 = {kx: -kx, ky: -ky, kz: -kz, kxp: -kxp, kyp: -kyp, kzp: -kzp}
odd_all5, nonzero5 = True, 0
for L in range(6):
    for word in iproduct((A1, A2, S_op), repeat=L):
        Mw = al[0]
        for f in word:
            Mw = Mw*f
        tr = sp.expand(Mw.trace())
        odd_all5 = odd_all5 and sp.expand(tr + tr.subs(inv2, simultaneous=True)) == 0
        if tr != 0:
            nonzero5 += 1
ok("R2 ALL-LENGTHS LEMMA UPGRADE: S L S = L(-a) for GENERIC letters a.alpha + b S"
   " + c I (symbolic coefficients); S W S = W(-) on generic length-3 products"
   " (homomorphism + S^2 = I, iterable to EVERY length); tr[alpha_x W(-)] ="
   " -tr[alpha_x W] on the generic product (cyclicity + S alpha_x S = -alpha_x);"
   " AND the exhaustive TWO-momentum sweep EXTENDED one full length past the"
   " build (all 364 words over {alpha.khat, alpha.khat', S} to length 5): every"
   " tr[alpha_x W] ODD-or-zero under joint inversion, nonzero instances present"
   " — the displayed length cutoff hides NO even subclass: the one-vertex"
   " parity is an identity at ALL degrees",
   conj_letters and conj_word and tr_flip_generic
   and Zq(sp.expand(S_op*al[0]*S_op + al[0])) and odd_all5 and nonzero5 > 0)

# ===== R3 — exemplar + A3 consistency =====
Ck = sp.expand((sp.eye(4) - A1)/2)
ok("R3 EXEMPLAR + A3 CONSISTENCY: tr[alpha_x alpha.khat] = 4 khat_x;"
   " tr[alpha_x C(k)] = -2 khat_x at C(k) = (1 - alpha.khat)/2 — the primary"
   " audit's spinor-trace kill line reproduced from the same ground",
   sp.expand((al[0]*A1).trace() - 4*kx) == 0
   and sp.expand((al[0]*Ck).trace() + 2*kx) == 0)

# ===== R4 — kill vs refusal moments (with degree-4 spot checks) =====
th, ph = sp.symbols('theta phi', real=True)
sub = {kx: sp.sin(th)*sp.cos(ph), ky: sp.sin(th)*sp.sin(ph), kz: sp.cos(th)}
def ang(e):
    return sp.integrate(sp.integrate(e.subs(sub)*sp.sin(th), (ph, 0, 2*sp.pi)),
                        (th, 0, sp.pi))
kv = [kx, ky, kz]
evens = [sp.Integer(1)] + [kv[i]*kv[j] for i in range(3) for j in range(i, 3)] \
        + [kx**4, kx**2*ky**2, ky**2*kz**2, kx**2*ky*kz]
kills = all(sp.simplify(ang(kx*m)) == 0 for m in evens)
refusal = sp.simplify(ang(kx*kx) - R(4, 3)*sp.pi) == 0
f_odd = kx
inv1 = {kx: -kx, ky: -ky, kz: -kz}
moment = sp.simplify(ang(kx*(f_odd - f_odd.subs(inv1, simultaneous=True))))
ok("R4 KILL vs REFUSAL: int_S2 khat_x m dOmega = 0 for EVERY even monomial m"
   " (deg <= 2 plus four deg-4 spot checks the build did not run); int khat_x^2"
   " = 4pi/3 != 0; the odd-defeat exhibit int khat_x(f(khat) - f(-khat)) dOmega"
   " = 8pi/3 at f = khat_x — re-derived",
   kills and refusal and sp.simplify(moment - R(8, 3)*sp.pi) == 0)

# ===== R5 — thresholds (Lambda FORMAL) =====
kk = sp.Symbol('k_r', positive=True)
Lam = sp.Symbol('Lambda_c', positive=True)
I3 = sp.integrate(kk**(2 - 3), (kk, 1, Lam))
I4 = sp.integrate(kk**(2 - 4), (kk, 1, Lam))
ok("R5 THRESHOLDS: int_1^Lam k^2 k^-3 = log Lambda (formal limit oo — the"
   " degree-3 class IS the log); int k^2 k^-4 = 1 - 1/Lambda -> 1 BOUNDED —"
   " one exact angular kill on the whole class lands log-free; Lambda never"
   " valued",
   sp.simplify(I3 - sp.log(Lam)) == 0 and sp.limit(I3, Lam, sp.oo) == sp.oo
   and sp.simplify(I4 - (1 - 1/Lam)) == 0 and sp.limit(I4, Lam, sp.oo) == 1)

# ===== R6 — the c-class jet at the displayed bytes, one step past the build =====
bp, bm = sp.symbols('b_plus b_minus', real=True)
jet = sp.expand(kx*((bp - bm)*sp.eye(4) + (bp + bm)*A1))
tr_jet = sp.expand(jet.trace())
tr_ax_jet = sp.expand((al[0]*jet).trace())
swap = lambda e: e.subs({kx: -kx, ky: -ky, kz: -kz, bp: bm, bm: bp}, simultaneous=True)
ok("R6 THE c-CLASS SHAPE AT BYTES (MO4 §3.3 display): tr[jet] = 4 khat_x(b+ - b-)"
   " joint-EVEN under (khat -> -khat, b+ <-> b-); tr[alpha_x jet] = 4 khat_x^2"
   " (b+ + b-) ALSO joint-even, with angular moment (4pi/3)(b+ + b-) != 0 at"
   " constant coefficients: at the displayed shape parity supplies NO kill on"
   " the coincidence-bearing class — the build's B-2 refusal holds one step"
   " past its own display",
   sp.expand(tr_jet - 4*kx*(bp - bm)) == 0
   and sp.expand(swap(tr_jet) - tr_jet) == 0
   and sp.expand(tr_ax_jet - 4*kx**2*(bp + bm)) == 0
   and sp.expand(swap(tr_ax_jet) - tr_ax_jet) == 0
   and sp.simplify(ang(kx*kx)*(bp + bm) - R(4, 3)*sp.pi*(bp + bm)) == 0)

# ===== R7 — Gram-split mechanics, independent instances =====
q11, q12, q22 = sp.symbols('q11 q12 q22', real=True)
bb_1, bb_2 = sp.symbols('bpos1 bpos2', positive=True)
Qm = sp.Matrix([[q11, q12], [q12, q22]])
bD = sp.diag(bb_1, bb_2)
bhalf = sp.diag(sp.sqrt(bb_1), sp.sqrt(bb_2))
recon = Zq(sp.expand(Qm*bhalf*bhalf*Qm - Qm*bD*Qm))
a1s, a2s = sp.symbols('a1s a2s', positive=True)
def kron(A, B):
    m, n = A.shape; p, q = B.shape
    return sp.Matrix(m*p, n*q, lambda i, j: A[i//p, j//q]*B[i % p, j % q])
Mten = kron(sp.diag(a1s, -a2s), sy)
tn_ten = sp.expand(sum(sp.sqrt(v)*m for v, m in (Mten.H*Mten).eigenvals().items()))
alx1 = sp.expand(sum(sp.sqrt(v)*m for v, m in (al[0].H*al[0]).eigenvals().items()))
v7 = sp.Matrix([1, 1, 2]); P7 = v7*v7.T
tnP = sp.expand(sum(sp.sqrt(v)*m for v, m in (P7.T*P7).eigenvals().items()))
w_v = [R(1, 2), R(-1, 4), R(-1, 4)]
ok("R7 GRAM-SPLIT MECHANICS (independent instances): (Q b^{1/2})(b^{1/2} Q) ="
   " Q b Q exact (PSD square-root reconstruction — the split multiplies back to"
   " the sealed J profile); ||A tensor B||_1 = ||A||_1 ||B||_1 at the"
   " sign-indefinite instance diag(a1,-a2) tensor sigma_y (= 2(a1+a2));"
   " ||alpha_x||_1 = 4; ||PSD||_1 = tr (instance 6); sum|w_l| = 1 (l1-tight"
   " weights re-pinned) — every norm fact of the direct summed chain holds",
   recon and sp.simplify(tn_ten - 2*(a1s + a2s)) == 0 and alx1 == 4
   and tnP == 6 and sum(abs(w) for w in w_v) == 1)

# ===== R8 — rank x HS at r = 3 AND r = 4; Hermitian trace route =====
s1, s2, s3, s4 = sp.symbols('sig1 sig2 sig3 sig4', nonnegative=True)
lag3 = sp.expand(3*(s1**2 + s2**2 + s3**2) - (s1 + s2 + s3)**2
                 - ((s1 - s2)**2 + (s1 - s3)**2 + (s2 - s3)**2))
lag4 = sp.expand(4*(s1**2 + s2**2 + s3**2 + s4**2) - (s1 + s2 + s3 + s4)**2
                 - ((s1 - s2)**2 + (s1 - s3)**2 + (s1 - s4)**2
                    + (s2 - s3)**2 + (s2 - s4)**2 + (s3 - s4)**2))
n_ = sp.Symbol('n', positive=True)
Meps = sp.Symbol('M_over_eps', positive=True)
lam_a, lam_b = sp.symbols('lam_a lam_b', real=True)
tri_lhs = sp.expand((sp.Abs(lam_a) + sp.Abs(lam_b))**2 - (lam_a + lam_b)**2)
tri_rhs = sp.expand(2*(sp.Abs(lam_a*lam_b) - lam_a*lam_b))
ok("R8 RANK x HS: (sum sigma)^2 <= r sum sigma^2 at r = 3 AND r = 4 (Lagrange,"
   " both exact — the build ran r = 3 only); sqrt(4 n^3) = 2 n^{3/2} exact;"
   " K^Sigma is HERMITIAN of record so |tr| <= sum|eig| = sum sigma needs only"
   " the real triangle inequality — pinned as the exact identity"
   " (|a|+|b|)^2 - (a+b)^2 = 2(|ab| - ab) with |ab| - ab >= 0 a named fact:"
   " |tr K^Sigma_n| <= 2 n^{3/2} M/eps at the sealed HS enclosure — the cap"
   " re-derived",
   lag3 == 0 and lag4 == 0
   and sp.simplify(sp.sqrt(4*n_**3)*Meps - 2*n_**R(3, 2)*Meps) == 0
   and sp.simplify(tri_lhs - tri_rhs) == 0)

# ===== R9 — the refusal limits AND the axis honesty =====
ell_, CG_, cG_ = sp.symbols('ell C_G c_G', positive=True)
ratio_cap = (2*CG_*n_**R(3, 2))/sp.log(sp.sqrt(2*n_)/ell_)
ratio_floor = (cG_*n_**R(3, 2))/sp.log(sp.sqrt(2*n_)/ell_)
ok("R9 REFUSAL + AXIS HONESTY: 2 C_G n^{3/2}/log(sqrt(2n)/ell) -> oo AND"
   " c_G n^{3/2}/log -> oo (the SHARP floor outruns the log at the sealed"
   " Lambda = sqrt(2n)/ell linkage, MO4 §4.6 bytes); and at FIXED n the"
   " symbol-count log Lambda -> oo while every delivered cap is Lambda-free:"
   " the polynomial budgets ESTIMATE the trace and cannot ELIMINATE the"
   " carrier-axis log at the KF1 grade — the commission's bar holds in both"
   " readings",
   sp.limit(ratio_cap, n_, sp.oo) == sp.oo
   and sp.limit(ratio_floor, n_, sp.oo) == sp.oo
   and sp.limit(sp.log(Lam), Lam, sp.oo) == sp.oo)

# ===== R10 — sweep cardinalities + o-2-OBS consistency =====
count1 = sum(2**L for L in range(6))
count2 = sum(3**L for L in range(5))
I_pc = kx*(kx - kxp)
ok("R10 CARDINALITIES + o-2-OBS CONSISTENCY: sum_{L<=5} 2^L = 63 and"
   " sum_{L<=4} 3^L = 121 (the build's word counts exact); the per-composite"
   " integrand khat_x(khat_x - khat'_x) is EVEN under joint inversion — the"
   " lemma's grammar assigns the sealed refusal to the amplitude side, the"
   " record unmoved",
   count1 == 63 and count2 == 121
   and sp.expand(I_pc.subs(inv2, simultaneous=True) - I_pc) == 0)

print("T4SR-AUDIT-BATTERY-DONE")
```

Output, verbatim (10/10 PASS, final run):

```text
R1 DIRAC GROUND (independent): {alpha_i, alpha_j} = 2 delta_ij; S = -i b g5 Hermitian, S^2 = I, {S, alpha_j} = 0 all j — the sealed spinor bytes stand: PASS
R2 ALL-LENGTHS LEMMA UPGRADE: S L S = L(-a) for GENERIC letters a.alpha + b S + c I (symbolic coefficients); S W S = W(-) on generic length-3 products (homomorphism + S^2 = I, iterable to EVERY length); tr[alpha_x W(-)] = -tr[alpha_x W] on the generic product (cyclicity + S alpha_x S = -alpha_x); AND the exhaustive TWO-momentum sweep EXTENDED one full length past the build (all 364 words over {alpha.khat, alpha.khat', S} to length 5): every tr[alpha_x W] ODD-or-zero under joint inversion, nonzero instances present — the displayed length cutoff hides NO even subclass: the one-vertex parity is an identity at ALL degrees: PASS
R3 EXEMPLAR + A3 CONSISTENCY: tr[alpha_x alpha.khat] = 4 khat_x; tr[alpha_x C(k)] = -2 khat_x at C(k) = (1 - alpha.khat)/2 — the primary audit's spinor-trace kill line reproduced from the same ground: PASS
R4 KILL vs REFUSAL: int_S2 khat_x m dOmega = 0 for EVERY even monomial m (deg <= 2 plus four deg-4 spot checks the build did not run); int khat_x^2 = 4pi/3 != 0; the odd-defeat exhibit int khat_x(f(khat) - f(-khat)) dOmega = 8pi/3 at f = khat_x — re-derived: PASS
R5 THRESHOLDS: int_1^Lam k^2 k^-3 = log Lambda (formal limit oo — the degree-3 class IS the log); int k^2 k^-4 = 1 - 1/Lambda -> 1 BOUNDED — one exact angular kill on the whole class lands log-free; Lambda never valued: PASS
R6 THE c-CLASS SHAPE AT BYTES (MO4 §3.3 display): tr[jet] = 4 khat_x(b+ - b-) joint-EVEN under (khat -> -khat, b+ <-> b-); tr[alpha_x jet] = 4 khat_x^2 (b+ + b-) ALSO joint-even, with angular moment (4pi/3)(b+ + b-) != 0 at constant coefficients: at the displayed shape parity supplies NO kill on the coincidence-bearing class — the build's B-2 refusal holds one step past its own display: PASS
R7 GRAM-SPLIT MECHANICS (independent instances): (Q b^{1/2})(b^{1/2} Q) = Q b Q exact (PSD square-root reconstruction — the split multiplies back to the sealed J profile); ||A tensor B||_1 = ||A||_1 ||B||_1 at the sign-indefinite instance diag(a1,-a2) tensor sigma_y (= 2(a1+a2)); ||alpha_x||_1 = 4; ||PSD||_1 = tr (instance 6); sum|w_l| = 1 (l1-tight weights re-pinned) — every norm fact of the direct summed chain holds: PASS
R8 RANK x HS: (sum sigma)^2 <= r sum sigma^2 at r = 3 AND r = 4 (Lagrange, both exact — the build ran r = 3 only); sqrt(4 n^3) = 2 n^{3/2} exact; K^Sigma is HERMITIAN of record so |tr| <= sum|eig| = sum sigma needs only the real triangle inequality — pinned as the exact identity (|a|+|b|)^2 - (a+b)^2 = 2(|ab| - ab) with |ab| - ab >= 0 a named fact: |tr K^Sigma_n| <= 2 n^{3/2} M/eps at the sealed HS enclosure — the cap re-derived: PASS
R9 REFUSAL + AXIS HONESTY: 2 C_G n^{3/2}/log(sqrt(2n)/ell) -> oo AND c_G n^{3/2}/log -> oo (the SHARP floor outruns the log at the sealed Lambda = sqrt(2n)/ell linkage, MO4 §4.6 bytes); and at FIXED n the symbol-count log Lambda -> oo while every delivered cap is Lambda-free: the polynomial budgets ESTIMATE the trace and cannot ELIMINATE the carrier-axis log at the KF1 grade — the commission's bar holds in both readings: PASS
R10 CARDINALITIES + o-2-OBS CONSISTENCY: sum_{L<=5} 2^L = 63 and sum_{L<=4} 3^L = 121 (the build's word counts exact); the per-composite integrand khat_x(khat_x - khat'_x) is EVEN under joint inversion — the lemma's grammar assigns the sealed refusal to the amplitude side, the record unmoved: PASS
T4SR-AUDIT-BATTERY-DONE
```

---

## 8. CHOICE LEDGER (commission T4SR)

```text
AC-1 [PROVABLE, run first]: seal verification of all seven inputs
     (shasum -a 256 -c) before any reliance; the target verified
     before opening; the verbatim battery re-run; the byte-pins of §1;
     the mtime/filename sweeps — all mechanical, all before judgment.
AC-2 VERDICT CLASS [YOURS]: CONFIRMED-WITH-CORRECTIONS over CONFIRMED.
     Ground: COR-A is a real logic overstatement in the displayed
     equivalence (the converse direction undelivered), even though it
     moves no flag and no route verdict — the record's convention for
     audits that repair framing while every chain stands (the WSR-
     AUDIT COR-1 precedent). REFUTED requires a false load-bearing
     claim; none exists: the verdict-bearing direction is exact and
     the blockers are real at my own sweeps.
AC-3 PAIR-READ SCOPE [TASKED, disclosed]: my commission names the
     MO4_CANCELLATION PAIR where the build's named scope was five
     artifacts without the MO4 audit. I adjudicate the build AGAINST
     ITS OWN SCOPE (no defect: its scope statements are literally
     accurate) and report the pair-read's effect separately (N-a,
     favorable). The headline claim ("not certifiable on the five
     named sealed artifacts") is ALSO confirmed on my strictly larger
     six-artifact basis.
AC-4 COR-A SEVERITY [YOURS]: correction, not refutation. The "IFF"
     span is load-ADJACENT, not load-bearing: the UNDECIDABLE-TODAY
     verdict consumes only the sufficient direction plus the
     absences; the deciding object survives verbatim with the branch
     condition restated (sum of displayed moments). A REFUTED-grade
     reading would require the verdict to have rested on the
     converse; it does not (target §4 NOT-REFUTED discipline and C-1
     rest on B-1/B-2 absences).
AC-5 THE ALL-LENGTHS UPGRADE [YOURS, displayed]: delivered as audit
     yield (r-3/R2) because it discharges the one disclosed
     exhaustiveness caveat (C-4) and closes the cutoff-hidden-
     subclass hunt at the lemma level; it consumes only the sealed
     alphabet (WSR T-2/WS6) and 4x4 Dirac algebra — determination-
     grade, no new physics (the same license the build's T1-T3
     carried).
AC-6 AXIS HONESTY [YOURS, displayed]: R9's fixed-n limit is displayed
     to pin that the refusal of the polynomial caps does not depend
     on the sealed Lambda(n) linkage (it holds on the Lambda-axis
     alone); the linkage itself is nonetheless of record (MO4 §4.6
     bytes) — both readings refuse, so B-3 is doubly grounded.
AC-7 HOMONYM SPLIT [inherited, disclosed in header]: n carrier index;
     k momentum; (o, c) the REM strata indices as quoted.
```

---

## 9. TOY_SEPARATION

```text
AT-1 R1-R3, R6 use the EXACT 4x4 Dirac algebra of the record (not a
     toy: the sealed spinor structure itself); R2's generic letters
     are the sealed alphabet with symbolic coefficients — the
     conjugation identity is type-level, valid termwise for the
     operator instantiation (the same license as WSR T-2/WS6 and the
     build's T1-T3). No matrix-grade result is carried to the carrier
     quantifier.
AT-2 R4/R5 are exact continuum angular/radial integrals at the
     record's KF1/KF2 effective-degree license; Lambda FORMAL, never
     valued.
AT-3 R7/R8's exact instances (PSD reconstruction; the sign-indefinite
     Kronecker instance; ||PSD||_1 = tr at 6; Lagrange r = 3, 4) are
     identity-grade exhibits of displayed general facts — the same
     license the build's T7/T8 and the sealed MO4 battery used. No
     instance is promoted beyond its displayed general proof.
AT-4 R9/R10 are formal limits and exact cardinalities; nothing
     testbed-grade appears; nothing witness-family-grade is promoted;
     no spectral datum of the actual family is valued.
```

---

## 10. FLAG BLOCK

```text
T4SR_AUDIT_VERDICT = CONFIRMED-WITH-CORRECTIONS( the target's
  UNDECIDABLE-TODAY stands adjudicated at the bytes on all four
  blockers, each re-verified independently on a strictly larger
  (six-artifact) basis: B-1 census ABSENT (my token sweeps; AC-4 at
  WSR-AUDIT bytes), B-2 c-class parity refusal REAL at the MO4 §3.3
  display (re-derived one step past the build's own trace slot; no
  moment certificate in scope), B-3 every trace budget polynomial and
  refused on BOTH axes (the direct summed Gram chain and the
  rank x HS cap both re-derived step by step; the sharp floor of
  record; estimation never elimination), B-4 cumulants EMPTY (zero
  hits, six artifacts). The ONE-VERTEX TRACE-PARITY LEMMA is
  CONFIRMED and STRENGTHENED to an all-lengths identity (joint
  inversion = conjugation by S; the build's length caveat
  discharged). CORRECTIONS: COR-A — the displayed "IFF"/per-stratum
  refutation branch overstates the underived converse; correct
  branch condition = nonzero SUM of the census's displayed first
  moments; the census still decides BOTH ways; verdict-neutral.
  N-a — the MO4 audit (outside the build's scope, consumed here at
  the tasked pair) is CONFIRMED-WITH-CORRECTIONS-cosmetic and
  upgrades the build's CONDITIONAL(CL-A)/CLAIMED marks to audited
  grade, favorably; it contains nothing that decides the premise.
  N-b — the deeper sweep and conjugation identity are strengthening
  yield. Registrar margin notes suffice; NO re-issue forced. )

HEADLINE_ADJUDICATION = CONFIRMED( UNDECIDABLE-TODAY: no log-free
  rate for the degree-3 diagonal class of the summed assembly is
  certifiable on the named sealed ground — confirmed on the build's
  five AND on my six; the commission stands REDUCED to the DEGREE-3
  ANGULAR CENSUS as the deciding object, with COR-A's branch
  restatement (the displayed exact first moments SUM to the class's
  log coefficient: zero ==> RATE-CERTIFIED mechanically on the lemma;
  nonzero ==> the log genuine, the rate refuted). NOT REFUTED: no
  divergence of tr K^Sigma_n exhibited anywhere in target or ground. )

CAS = 8/8 verbatim re-run BYTE-IDENTICAL (fresh venv t4srauditvenv,
  sympy 1.14.0) + 10/10 audit battery (two pre-final auditor-side
  corrections disclosed §7; final run single and complete; NOTHING
  numeric evaluated).

SEALS_VERIFIED = 7/7( the target before opening + the six tasked
  ground artifacts (WSR pair, WBC pair, MO4 pair) — every digest
  recomputed from bytes at path this session by full digest via
  shasum -a 256 -c against sidecars; all OK; none unverifiable; every
  load-bearing span read at path; scoped reads of these seven ONLY. )

PER_COMPOSITE_WALL = UNTOUCHED( summed quantifier only, verified span
  by span; no per-composite or per-pair quantity quoted, moved, or
  decided by target or by this audit; V-2 not crossed in either
  direction (the Gram chain's weight-sum triangle feeds branch facts
  INTO the summed bound — the allowed direction); T_n, o-2-OBS,
  B-L2*, alpha' = 1/4, MO-2, MO-3/p = -3, MO4's PARTIAL and MO-4-R,
  the WBC enclosure — all at their own quantifiers; no flag of the
  record flipped; no witness action — witness status is the
  registrar's. )

FORBIDDEN_IMPORTS = none( exact symbolic only — every constant
  symbolic or an exact rational/surd; NOTHING numeric evaluated
  (Lambda, ell, n, M, eps, C_G, c_G formal); no float ground; no
  measured constant; no value frozen; alpha never computed or
  approached; kappa_record nowhere; no per-pair route traversed; H-R
  nowhere defaulted; no fenced-class or out-of-scope file opened
  (filename matches only); "Q-..." tokens noted, never chased; no
  flag flipped; no witness action; no git; ONE output + sidecar at
  the commission-distinct path, probed ABSENT at session start and
  re-probed ABSENT immediately before write. )

SWEEP_CUTOFF = 2026-08-15 03:13:05 -0500 (workspace root + cleanroom
  root; fenced classes and all non-scoped files filename-matched
  only, never opened; token greps inside the seven scoped files only;
  artifacts sealed after the cutoff UNSWEPT).

alpha_computed = false ; kappa_record_computed = false ;
proof_authorized = false
ALL_RESULTS = CLAIMED until registered; witness/flag actions are the
registrar's.
T4SR_LOGFREE_RATE_AUDIT = SEALED.
```
