# STAGE 8 TASK 5 / CONTINUUM PACKAGE - CONSTRAINT ARM - LANE 1 V001

Date: 2026-08-04  
Task: PASTE 490 / Task 5 / continuum package  
Lane: Codex Lane 1  
Custody: derivation only; independent of the authored arm

## Lead result

```text
CONSTRAINT_SYSTEM = 15 forced constraint families |
  presentation descent and normalization;
  physical-reader factorization;
  covariance/reality/restriction;
  R1 admissibility;
  R5 anchoring;
  Shape-K/prepared-direction scope;
  the exact R1-reader pairing;
  R4-only units;
  ordered batching;
  Ref/J_ref functoriality;
  density naturality;
  response naturality;
  boundary/contact coherence;
  completion continuity plus the faithfulness/annihilator dichotomy;
  the joint DoR-008 restriction square.

JOINT_FIBER = EQUALIZER / FIBER_PRODUCT, NOT A FINITE-DIMENSIONAL BOX |
  at a fixed finite carrier the algebraic reader slice has
    dim = 1+m_G-r_G,
  before physical-locality and completed-kernel constraints;
  the reciprocal profile slice has dimension 1 and the sealed S8-A exchange
  slice dimension 2 before extra stabilizers.  R1 contains at least the
  nontrivial affine mu direction; R5 contains an anchored admissible function
  space h(K); the four J_ref pieces are unparameterized moduli.  Therefore no
  total numerical dimension is derivable, and joint-fiber nonemptiness is not
  proved before an authored continuum package supplies those four pieces.

EXCLUDED =
  reader not annihilating Rel or the represented-kernel/contact kernel;
  faithfulness asserted with a nonzero completed kernel;
  R5 path not anchored at its R1 representative;
  a complement-coupled direction sold as the prepared Shape-K/Q396 subfiber;
  independent scalarization of the Q396 profile;
  hidden C/K conversion or numerical unit frame;
  thin zero-extension relabeled as geometric refinement;
  density/response/boundary choices that fail common-refinement composition;
  record-visible content deleted as contact;
  ordered-pair batching replaced by a joint contraction;
  any completed restriction disagreeing with a sealed finite result.

FORCED_CONDITIONALLY =
  continuum map chosen -> the physical reader is its normalized coefficient
    pullback, not an independent second scalarization;
  completed kernel N chosen -> every reader annihilates N;
  faithfulness proved -> N=0 and no kernel-induced reader relation;
  R1 witness coefficient mu and reader (chi_K,T) chosen -> its scalar image
    is mu[f(s)chi_K+2f_1(s)<x,T x>_K];
  Ref+density+eta_resp+BoundaryCert+C1 chosen coherently -> the C2 stitched
    class and conditional completed Ward theorem follow uniquely.

THRESHOLD_LINK = PRODUCT_AND_SHARED-DIAGRAM_ONLY |
  q_loop=|chi_K| A_loop remains exact and dimensionless.  The same continuum
  diagram must induce chi_K and the operator whose coefficient defines
  A_loop, but no forced equation relates their magnitudes and no forced
  inequality follows.  Q396 supplies an independent operator-side direction
  while the algebraic reader is held fixed; when the eventual physical reader
  does not annihilate that direction, it changes the pre-supremum response
  coefficient and can change A_loop (equal suprema are not excluded).  No
  ratified clause forces such annihilation, so a universal
  chi_K=F(A_loop) relation is not derivable from the current stack.  A future
  package could impose a disclosed correlation.  A nonfaithful kernel could
  force a linear reader condition (including chi_K=0 if Rhat_K lies in that
  kernel), but that horn is incompatible with a package simultaneously
  claiming full faithfulness.
```

The continuum clauses are therefore not independent, but neither do their
compatibility equations collapse them to one continuum parameter.  Their
lawful object is the equalizer constructed below.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
member_selected = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight and register sweep

### 0.1 Preflight

The locked process was read in full before the authority artifacts.  The
register head is Q-410.  DoR-020 is reserved and is not treated as in force.

| Authority | Verified SHA-256 / result |
|---|---|
| `alpha_supervision/LOCKED_PROCESS.md` | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` |
| `QUESTIONS_SETTLED_REGISTER_V001.md` | `c39ed4ebc346e45e5241d5f9e853efe46165b69d9eb0a3c220ac18bef6dc0d8e` |
| Q-406 residue ledger | `c35ef84636ec1501bd99858538988711d0c717ac98671e6162abb0a0e5978c74`; sidecar OK |
| Q-396 witness family | `f0f8b09b1aa6a16c0ed0dffedbd275aba9e647fb5841fc8ce06ce9e6a8b00857`; sidecar OK |
| Q-407 reader family | `bae34116c4d6792b5e39b913addeeff1650989660d89ba01bf5de62ec2d9aa50`; sidecar OK |
| Q-409 conditional theorems | `2aad6d8379777b8651a7e8dda78e9b085a69aa0203f455209eeb5e6e7da3adeb`; sidecar OK |
| Q-410 `J_ref` skeleton | `8dd59b35bb9f63f8c8107d438c757c0cb9a110ee1078c173213c6da657bdfb24`; sidecar OK |
| DoR-008 | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19`; sidecar OK |
| DoR-016 | `b4157df6f327e261f40389d5a3011a0aef66ee0f198d8ebba8b1b9303142d708`; sidecar OK |
| DoR-017 | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45`; sidecar OK |
| DoR-018 | `57532849d0766741a365d001596275ef4f2ca612063ef6d05031292711a0614d`; sidecar OK |
| DoR-019 | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f`; sidecar OK |

```text
DOES_THE_OBJECT_EXIST = yes_as_a_constraint_problem |
  no_as_an_instantiated_continuum_package
IS_THE_VERSION_CURRENT = yes | through Q-410
ARE_ITS_INPUTS_PRESENT = yes_for_deriving_all_compatibility_equations |
  no_for_proving_the_joint_fiber_inhabited
PREFLIGHT = PASS
```

### 0.2 Register sweep, Q-391 through Q-410

| Entries | Binding result for this arm |
|---|---|
| Q-391/Q-393 | the response criterion and `RetExtract` kernel are exact, but typed symmetries do not fix completed Keldysh support |
| Q-394/Q-396 | completion selects no representative; the admissible R1 family is nontrivial and has the explicit `mu` witness |
| Q-397/Q-400 | the completed operator is consumed; the reader and local Maxwell map remain independent typed interfaces until composed |
| Q-401/Q-405 | the Banach result is conditional; on the reciprocal branch `q_loop=|chi_K|A_loop` and strict contraction has the stated symbolic threshold |
| Q-406 | R3 is a coordinate of R2; R5 is a path through action data whose constant slices include R1; R8/R11 are downstream determinations |
| Q-407 | the algebraic reader fiber is `(chi_K,T)` modulo `Rel_G` and naturality; loop and S8-A dimensions are known |
| Q-408 | finite/cylindrical kernels are faithful and Ward-transverse; no finite reader relation is added |
| Q-409 | C1/C2 conditional theorems are exact; completed faithfulness is independent of continuity |
| Q-410 | only `J_0` and fixed-stage estimates build; four continuum completion pieces remain absent |

No row supplies a magnitude relation between `chi_K` and `A_loop`, identifies
the four `J_ref` pieces, or declares completed faithfulness.

---

## 1. Variables and the joint-fiber object

### 1.1 Freedom variables

At each admitted carrier/stage `G`, write:

```text
I_G in Adm_base,G
  = the R1 completed action/response representative;

ell_G=ell_(chi_G,T_G)
  = the normalized reader on O_prof,G;

A_(I,h),G:K_scalar -> Act_phys,G
  = the R5 scalar-K-to-action path through I, with admissible K-dependence h;

C_J=(GRef,Dens,Eta,Bound)
  = the four J_ref completion pieces;

Faith=(Kernbar,faith)
  = the completed kernel map and either its zero-kernel certificate or its
    disclosed kernel.                                (F1-1)
```

`faith` is a proved status, not an authored Boolean switch.  The `true` horn
requires a lower-bound/zero-kernel theorem; otherwise the package must expose
the kernel and use the quotient horn.

The four continuum pieces mean exactly:

```text
GRef  = Ref + same-region/common-refinement relation + target-independent
        J_ref carrying incidence, degree, sources, algebra, representation,
        connection/current data, and intertwiners;

Dens  = coframe/volume/current-density normalization in one tau_ker;

Eta   = common-refinement connected-response natural transformation;

Bound = BoundaryContactCert, including the contact ideal and subextensive
        boundary comparison in that same tau_ker.     (F1-2)
```

`tau_ker` and the family-uniform C1 estimates are compatibility data shared
by `Dens`, `Eta`, `Bound`, and `Faith`; they are not an unlisted fifth
physical member.

### 1.2 Joint fiber

Let the unconstrained product be

```text
F_raw := Adm_base x R2_alg x R5_path
         x GRef x Dens x Eta x Bound x Faith.          (F1-3)
```

The lawful continuum package is

```text
F_joint := Eq(J1,...,J15) subset F_raw,                (F1-4)
```

where `Eq` means the simultaneous equalizer/fiber product of the equations
below.  This formulation matters: a package can pass every row separately
and still fail because its reader, action path, and refinement maps do not
form the same commuting diagram.

---

## 2. F1 - the 15 forced compatibility families

### J1. Presentation descent and normalized reader

Q-407 gives

```text
E_G=R L_T,G direct-sum_1 R R_K,G direct-sum_1 S_1,sa(K_G),
Q_G:E_G -> O_prof,G=E_G/Rel_G.                       (J1-1)
```

The reader is necessarily

```text
ell_G(Q_G(a,b,C))=a+chi_G b+Tr(T_G C),
ell_G(L_T,G)=1,                                      (J1-2)

a+chi_G b+Tr(T_G C)=0
for every (a,b,C) in Rel_G.                          (J1-3)
```

This is a forced affine constraint.  `chi_G` is a coordinate of `ell_G`, not
an extra freedom beside the reader.

### J2. Physical-reader factorization through the same continuum map

Once a continuum package supplies a completed kernel, local realization,
and Maxwell/contact split, its physical coefficient projection
`pi_Mx,G` must induce the selected reader:

```text
ell_G
 =pi_Mx,G compose Loc_G compose Kernbar_G compose Q_G,

pi_Mx,G(Loc_G(L_T,G))=1.                            (J2-1)
```

Here quotient maps are inserted where `Kernbar` or `Loc` is defined only on
the represented class.  Equation `(J2-1)` is the main cross-constraint
between `(chi_K,T)` and the four `J_ref` pieces.  An authored reader and an
authored locality package that give different maps are incompatible even if
both are separately covariant.

### J3. Reader covariance, reality, and restriction

For every admitted isometry/relabeling `U:G->G'` and every rank-preserving
W3 inclusion `j_NM`,

```text
chi_G'=chi_G,
T_G'=U T_G U^*,                                      (J3-1)

T_N=j_NM^* T_M j_NM,
chi_N=chi_M                                           (J3-2)
```

on the compatible profile image.  Equivalently,

```text
ell_M(j_prof,NM H)=ell_N(H),
ell_G'(Alpha_U H)=ell_G(H).                          (J3-3)
```

Reality conjugates the profile and reader together, preserving the real
self-adjoint slice.  For cycle-creating extensions, `(J3-2)` is asserted only
contravariantly on the proved restriction square; no upward quotient map is
invented.

### J4. R1 admissibility

Every representative family member must obey

```text
rho_Gamma,N(I_M)=I_N on the proved restriction scope;
I_(G')=U.I_G under covariance;
Theta(I_G)=I_(Theta G);
Jet_N(I_G)=sealed Jet_N for every active finite stage;
all R1-COV, rho_H,N, reality, batching, quotient, unit, and reducing-domain
certificates commute.                                 (J4-1)
```

The Q-396 affine action `I -> I+Psi` preserves `(J4-1)` for
`Psi in V_adm`; it is not gauge.

### J5. R5 anchoring to R1

R5 is a path through action data, not an unrelated action family.  At its
declared scalar anchor `K_0`,

```text
A_(I,h),G(K_0)=Gamma_(I,G).                         (J5-1)
```

For the prepared form

```text
A_(I,h),G(K)=Gamma_(I,G)+h_G(K) Psi_G,             (J5-2)
```

the anchor equation is `h_G(K_0)=0`.  Naturality requires

```text
rho_Gamma,N A_(I,h),M(K)=A_(I,h),N(K),
h_M=h_N on a common scalar branch,                    (J5-3)
```

with any vertical differences carried by the already-declared cocycle rather
than an upward map.  `h` is real, regular on the declared domain, target
blind, and unit-correct.  A package cannot choose `I` and then use an R5 path
anchored at another representative.

### J6. Shape-K and prepared-direction scope

DoR-018 fixes the N-member's added content to the K sector.  The existing
Q-396 prepared R5 directions are likewise complement-independent:

```text
D_C Psi=0,
D_C D_K Psi=0,
delta A=delta B=delta C=0,
delta D=D_K^2 Psi.                                  (J6-1)
```

Thus `(J5-2)` preserves the ratified Shape-K separation.  A continuum
package may propose a broader action dependence only as separately disclosed
physics; it cannot call a complement-coupled term part of the prepared
Q-396/Shape-K fiber.

### J7. Exact R1-reader pairing

For the Q-396 retarded witness coefficient `mu`, at `k=(x,0)` and
`s=||x||_K^2`,

```text
H_mix(x)=f(s)R_K+2f_1(s)x^flat tensor x^flat,

ell_(chi,T)(mu H_mix(x))
 =mu[f(s)chi_K+2f_1(s)<x,T x>_K].                 (J7-1)
```

Including the sealed base-entry derivative gives

```text
ell_(chi,T)(Delta B_i)
 =dot_omega_i mu_i
    [f(s)chi_K+2f_1(s)<x,T x>_K].                 (J7-2)
```

The scalar on the right is forced once `I` and `ell` are chosen.  Assigning a
different coefficient to the same represented profile would violate
linearity or `(J2-1)`.

### J8. R4-only unit seam

Every C/K mixed expression in `A_(I,h)`, `Kernbar`, `Loc`, `pi_Mx`, and
`ell` must route through the declared R4 conversion set.  In particular:

```text
ell(L_T)=1 is a normalization in the L_T unit class;
it does not set chi_K, mu, nu, h, or a density scale;
mu H_mix carries its declared action/reality unit before ell is applied;
no bare C/K identity or additional conversion coefficient appears. (J8-1)
```

All sides of `(J7-1)` and `q_loop=|chi_K|A_loop` are therefore unit-correct;
the latter product is dimensionless.  DoR-019 leaves no silent residual
cross-sector unit parameter.

### J9. Ordered batching and network law

DoR-016 requires

```text
O_prof,(G1,G2)=O_prof,G1 x O_prof,G2,
ell_(G1,G2)=(ell_G1,ell_G2),
A_(G1,G2)=(A_G1,A_G2),
Eta_(G1,G2)=(Eta_G1,Eta_G2).                       (J9-1)
```

No product of amplitudes, common inverse, common trace, or joint scalar
contraction is created.  Any later scalar aggregation requires its own
licensed consumer.

### J10. Geometric refinement and `J_ref` functoriality

`GRef` must supply a category `Ref`, a same-region relation, common
refinements, and a target-independent functor

```text
J_ref:Ref -> CompletedCurrentKernelClass,
J_ref(S compose R)=J_ref(S) compose J_ref(R),
J_ref(id)=id.                                        (J10-1)
```

It must extend the built skeleton `J_0` on identities, certified signed
isomorphisms, and W3 squares.  A support filtration or thin zero-extension is
not a `Ref` arrow unless the missing geometric data and same-region theorem
are supplied.

### J11. Density/coframe naturality

For a refinement arrow `R:G->G'`, let `d_R` denote the coframe/volume/current-
density transport.  Compatibility requires

```text
d_(S compose R)=d_S compose d_R,
d_id=id,                                             (J11-1)

J_ref(R)(J_c)=J_(R_*c) after the declared density normalization,
```

with orientation/reality covariance and no coefficient chosen from a reader,
threshold, or response output.  The same `tau_ker` must be used by `Dens`,
`Eta`, `Bound`, and `Faith`; a topology switch breaks the equalizer.

### J12. Common-refinement response naturality

For every refinement arrow and profile `H`,

```text
Kernbar_(G')(j_prof,R H)
 =Eta_R(Kernbar_G(H)),                              (J12-1)

Eta_(S compose R)=Eta_S compose Eta_R,
Eta_id=id.                                          (J12-2)
```

On a common refinement `R` of `G1,G2`, the two pullbacks must agree modulo the
declared boundary/contact class.  Every square must commute with the existing
W3 restrictions and the DoR-008 bottom legs.  Zero-extension cannot be used
as `Eta_R` merely because both maps point toward a larger index.

### J13. Boundary/contact coherence

Let `beta_R` be the boundary/contact discrepancy along a refinement.  Path
independence forces the cocycle law

```text
beta_(S compose R)=beta_S+Eta_S(beta_R),            (J13-1)
```

up to the declared sign convention.  `Bound` must prove in the one topology

```text
beta_R / Vol_R -> 0 on every admitted cofinal family,
I_contact is closed, invariant, restriction-natural, and contains no
record-visible bulk cycle class.                    (J13-2)
```

The quotient/direct-sum member and its signs must be stated.  Calling every
off-diagonal path kernel `contact` fails `(J13-2)`.

### J14. Completion continuity and faithfulness/annihilator dichotomy

For every defining current/kernel seminorm, the same continuum package must
prove uniform estimates

```text
p_q(J_c)<=C_q||c||_K,
p_Q(Kern_fin(H))<=C_Q||H||_prof,                  (J14-1)
```

over the directed core and no-selection family.  These upper estimates give
the unique continuous extension.  Let

```text
N_comp,G:=ker(Kernbar_G).                           (J14-2)
```

Then exactly one of the following lawful horns applies:

```text
faith=true:
  N_comp,G={0}, proved by a lower-bound/separation certificate;

faith=false/disclosed quotient:
  ell_G(N_comp,G)=0 and pi_Mx,G(Loc_G(N_comp,G))=0. (J14-3)
```

The corner `faith=true` with `N_comp!=0` is empty.  The corner
`faith=false` with a reader detecting `N_comp` is also empty.  If
`Rhat_K in N_comp`, `(J14-3)` forces `chi_K=0`; that conditional consequence
does not apply on the faithful horn.

### J15. Joint DoR-008 restriction square

Every choice above must form one completed-to-finite square:

```text
r_N Kernbar_G(H)=Kern_N(rho_N H),
r_N A_(I,h),G(K)=A_N(K) on every sealed finite active datum,
ell_N(rho_N H)=the sealed finite scalar whenever that scalar exists,
all finite Gate 1-4, Q-243, Q-279, Q-309, and subsequently sealed results
are reproduced exactly.                              (J15-1)
```

For the Q-396 flat family,

```text
Jet_N(Psi)=0 to every finite active order,             (J15-2)
```

so `(J15-1)` constrains the joint diagram but does not fix `mu`, `h`,
`chi_K`, `T`, or a geometric density separately.  The completion remains
answerable to the finite data; the finite data do not choose among equal-
shadow completed members.

---

## 3. F2 - lawful region, dimensions, excluded corners, forced closures

### 3.1 Equalizer structure

The lawful region is the set

```text
F_joint={
 (I,ell,A,C_J,Faith): J1-J15 hold
}.                                                     (F2-1)
```

Projection to any one factor need not be surjective.  In particular,

```text
R2_phys(C_J,Faith)
 ={(chi,T) in R2_alg:
    ell=pi_Mx Loc Kernbar Q,
    ell(N_comp)=0,
    J3,J8,J9,J15 hold}.                              (F2-2)
```

This is the precise lawful subfamily the authored continuum package must
inhabit.

### 3.2 Dimension and nonemptiness

At fixed finite `G`, before `(F2-2)` adds physical-locality or kernel
conditions,

```text
dim R2_alg(G)=1+m_G-r_G.                            (F2-3)
```

If `N_comp` induces `r_N` independent affine reader constraints, then

```text
dim R2_phys(G)<=1+m_G-r_G-r_N,                     (F2-4)
```

when the finite-dimensional count applies.  On the faithful horn `r_N=0`,
but the physical factorization `(J2-1)` may still restrict which algebraic
readers are realized by a chosen Maxwell split.

Known slices remain:

```text
reciprocal profile: one coordinate chi_K;
S8-A exchange profile: (chi_K,tau_cross), dimension 2 before extra
  stabilizers;
R1: at least one affine action/reality coordinate mu per covariant source
  orbit, with a larger family not excluded;
R5: an anchored regular function space {h:h(K_0)=0} multiplying admissible
  directions;
GRef,Dens,Eta,Bound: unparameterized categorical/functional moduli. (F2-5)
```

Therefore the total joint fiber is generally infinite-dimensional or a
moduli groupoid, not a finite-dimensional affine space.  Its total dimension
cannot be assigned before the four continuum pieces are parameterized.  Its
nonemptiness is also not proved: Q-410 constructs only `J_0`, not an
inhabitant of `GRef x Dens x Eta x Bound`.

### 3.3 Proven incompatible corners

| Corner | Reason it is empty |
|---|---|
| `ell(Rel_G)!=0` | reader is not defined on `O_prof` |
| `ell != pi_Mx Loc Kernbar Q` | two scalarizations of one physical profile; violates J2 |
| `faith=true`, `N_comp!=0` | contradicts the zero-kernel certificate |
| `faith=false`, `ell(N_comp)!=0` | reader is not defined on the represented quotient |
| `A_(I,h)(K_0)!=Gamma_I` | R5 path does not pass through its R1 representative |
| complement-coupled `Psi` called the prepared Q-396/Shape-K direction | contradicts J6 |
| bare C/K identity or hidden unit coefficient | violates DoR-019 and J8 |
| network product/joint contraction | violates DoR-016 and J9 |
| thin zero-extension called geometric refinement | refuted by Q-410 and J10 |
| density not compositional with `J_ref` | the represented current depends on refinement path |
| response `Eta` not compositional | common-refinement output is path-dependent |
| boundary ideal deletes a visible bulk cycle | violates fullness, descent visibility, and J13 |
| topology differs between continuity and boundary limit | the C1/C2 composition theorem has no common target |
| any finite mismatch | DoR-008 voids the package and downstream TYPE-P results |

### 3.4 Conditional derivations once clauses are supplied

The following are forced and need not be re-authored:

1. `J14-1` plus completeness uniquely extends the finite current/kernel map.
2. `J10-J13`, P7 separation, and C1 instantiate the C2 stitching theorem,
   yielding a cellulation-independent local class.
3. C1 continuity extends exact finite Ward annihilation by density and
   closedness.
4. `N_comp` fixes the reader annihilator condition `(J14-3)`.
5. Full faithfulness sets `N_comp=0` and removes only the kernel-induced
   reader relation; it does not select `(chi_K,T)`.
6. `I`, `mu`, and `ell` fix `(J7-1)`-`(J7-2)`.
7. `I`, `h`, and the stationary solution package later fix `A_loop`; no
   independent response coefficient may be inserted.
8. The joint finite square fixes every finite shadow but leaves the proven
   flat completion fiber intact.

---

## 4. F3 - threshold interaction

### 4.1 Exact reciprocal factorization

On a regular reciprocal branch with the true modulus,

```text
q_loop=|chi_K| A_loop,

chi_K=ell[Rhat_K]
     =pi_Mx Loc Kernbar(Rhat_K),                  (F3-1)

A_loop
 =sup_(K in D)|a_loop(K,p,nu,I,h,C_J)|.           (F3-2)
```

The arguments displayed in `(F3-2)` make the new compatibility precision
explicit: geometry/refinement data can enter the represented operator and
its domain, while the fixed reciprocal reader enters the factorization
through `chi_K`.  Both factors are induced in one joint diagram and carry
the R4 unit seam, so their product is dimensionless.

### 4.2 What is and is not linked

The forced relations imply:

```text
0<A_loop<infinity:
  q_loop<1 iff |chi_K|<A_loop^(-1);               (F3-3)

A_loop=0:
  q_loop=0 for every finite chi_K;

A_loop=infinity:
  only the pointwise annihilating chi_K=0 horn gives q_loop=0. (F3-4)
```

They do **not** imply a magnitude equation between `chi_K` and `A_loop`.
There are two independent controls:

1. On the currently ratified completed operator carrier, hold `ell` fixed and
   vary the admissible R1 representative along the Q-396 `mu` direction.
   Equation `(J7-2)` changes the pre-supremum scalar response coefficient
   whenever `ell(H_mix)` is nonzero, while `chi_K=ell[Rhat_K]` is unchanged.
   This can change `A_loop`, although two members may accidentally have equal
   suprema.  No ratified clause forces `ell(H_mix)=0`; the eventual J2/J14
   package could impose it only by a disclosed physical factorization or
   kernel.
2. Before the physical locality member is chosen, the algebraic reader fiber
   varies `chi_K` at fixed carrier/action data.  No ratified equation makes
   that variation a compensating change in `A_loop`.

Thus the current stack forces no universal `chi_K=F(A_loop)`, inverse
rescaling law, sign relation, upper bound, or threshold inequality.  This is
a non-derivability statement at Q-410, not a prohibition on a future
continuum package proving or authoring a disclosed correlation.

### 4.3 Faithfulness horn

The only conditional structural link arises from a nonfaithful completed
kernel:

```text
Rhat_K in N_comp => chi_K=0 => q_loop=0.           (F3-5)
```

More generally, `N_comp` gives linear equations in `(chi_K,T)`.  This is not
available on the full-faithfulness horn, where `N_comp=0`.  A package cannot
claim both full faithfulness and use `(F3-5)` to force contraction.

```text
THRESHOLD_PARTIALLY_STRUCTURAL = only_the_product_and_kernel_horn
THRESHOLD_INEQUALITY_FORCED = false
CHI_K_MAGNITUDE_BOUND_FORCED = false
A_LOOP_MAGNITUDE_BOUND_FORCED = false
```

The S8-A modulus still contains the perpendicular reader coordinate through
`ell[J_perp]`; the reciprocal one-factor threshold cannot be promoted to
that carrier without an additional bound.

---

## 5. F4 - falsifiers and finite checks

### 5.1 One-edge/tree check

For a connected tree, `K_G=0`.  Hence `Rhat_K`, `P_x`, `H_mix`, `chi_K` as a
cycle-profile coordinate, and the Q-396 cycle deformation all have zero
image.  Only the independently normalized `L_T` line remains.

```text
ONE_EDGE_TREE_CHECK = PASS
```

No scalar cycle emission or hidden cycle reader is generated.

### 5.2 Reciprocal-loop check

On `K_G=R e`, `P_(r e)=r^2 R_K`; therefore

```text
ell(H_(r e))
 =[f(r^2)+2r^2 f_1(r^2)]chi_K.                   (F4-1)
```

The Q-396 mixed representative variation is exactly `(J7-1)`.  On every
finite active section `x=0`, smooth flatness gives zero to every finite jet,
so Q-243/Q-279/Q-309 remain zero, `p`-free, `nu`-free, and representative-
independent on restriction.

```text
RECIPROCAL_READER_PAIRING = PASS
FINITE_ACTIVE_FLATNESS = PASS
```

### 5.3 S8-A exchange check

For the sealed exchange carrier,

```text
T=[[chi_K/2,tau_cross],[tau_cross,chi_K/2]],

ell(H_x)
 =[f(s)+s f_1(s)]chi_K
  +4f_1(s)tau_cross x_1 x_2.                     (F4-2)
```

Exchanging `x_1,x_2` leaves `(F4-2)` invariant and preserves the two
coordinates.  On either exchange axis the cross term vanishes.  No
continuum constraint derived here deletes `tau_cross`.

```text
S8A_EXCHANGE = PASS
S8A_PERPENDICULAR_COORDINATE_RETAINED = PASS
```

### 5.4 Rank-preserving restriction check

Using

```text
j_NM^* R_K,M j_NM=R_K,N,
j_NM^* P_(j_NM x) j_NM=P_x,
T_N=j_NM^*T_M j_NM,                              (F4-3)
```

both sides of `(J7-1)` restrict to

```text
mu[f(s)chi_K+2f_1(s)<x,T_N x>].                  (F4-4)
```

Thus the reader/representative pairing commutes on the actual W3 scope.

```text
RANK_PRESERVING_RESTRICTION = PASS
```

Cycle-creating naturality remains contravariant and no forbidden upward map
is inferred.

### 5.5 Reality, units, and batching

Reality conjugates `mu`, the mixed block, and the reader together, leaving
the real scalar pairing invariant.  The R4 seam appears once.  Ordered
batching gives the pair of componentwise formulas and no joint contraction.

```text
REALITY = PASS
R4_ONLY_UNITS = PASS
ORDERED_BATCHING = PASS
```

### 5.6 DoR-008 joint check

Every finite action jet of the Q-396 deformation is zero.  `J_0` is exactly
the Q-408 finite current/kernel realization.  Therefore every currently
computable bottom square reproduces its sealed finite result.

```text
DOR008_CURRENTLY_COMPUTABLE_SQUARES = PASS
```

The geometric common-refinement, boundary, and completed-faithfulness tests
are not finitely executable because their objects do not exist.  Their
failure-capable tests are:

```text
REFINEMENT_TEST:
  two same-region cellulations disagree after common-refinement pullback;

DENSITY_TEST:
  composition or orientation changes the normalized current;

RESPONSE_TEST:
  Eta_(S compose R)!=Eta_S Eta_R or the W3/DoR-008 square fails;

BOUNDARY_TEST:
  normalized beta does not vanish or deletes a visible bulk cycle;

FAITHFULNESS_TEST:
  a nonzero completed profile has Kernbar(H)=0.       (F4-5)
```

Each test is typed `NOT_YET_EXECUTABLE / TYPE-U`, not passed by absence.

### 5.7 Anti-tuning and fresh corner attack

The constraint system was derived before any authored continuum member or
threshold outcome was inspected.  As a fresh attack, suppose one density
rescales `Rhat_K` so that `chi_K` satisfies `(F3-3)`, while `Eta` and `A_loop`
are left from another density.  Each clause may separately be covariant, but
`(J11-1)`, `(J12-1)`, and `(J2-1)` fail: the reader and response do not live
in the same diagram.  This excludes threshold-by-mixed-normalization even
without a numerical test.

```text
MIXED_NORMALIZATION_ATTACK = KILLS_THE_CORNER
TARGET_TUNING_USED = false
```

---

## 6. Final board

| Item | Determination |
|---|---|
| compatibility system | 15 simultaneous equation/condition families, J1-J15 |
| lawful region | equalizer/fiber product `(F2-1)`, not proven inhabited before the four continuum pieces exist |
| reader dimension | `1+m_G-r_G` algebraically; loop 1; S8-A 2 before extra stabilizers; physical dimension may fall only through proved factorization/kernel constraints |
| R1/R5 structure | nontrivial affine representative fiber plus anchored admissible function-space paths |
| continuum structure | four unparameterized moduli `GRef,Dens,Eta,Bound`; no total dimension derivable |
| faithfulness | independent zero-kernel theorem; otherwise readers annihilate the disclosed kernel |
| threshold | exact product and shared-diagram constraint only; no forced magnitude relation or inequality |

```text
CONSTRAINT_SYSTEM = J1-J15
JOINT_FIBER = EQUALIZER_FIBER_PRODUCT / NONEMPTINESS_NOT_PROVED
EXCLUDED_CORNERS = listed_in_Section_3.3
FORCED_CONDITIONALS = listed_in_Section_3.4
THRESHOLD_LINK = q_loop_abs_chi_times_A_loop_only |
  plus conditional kernel-annihilator equations

AUTHORED_FIELDS_INSTALLED = 0
MEMBER_SELECTED = none

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```
