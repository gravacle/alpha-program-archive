CLOSURE_DECLARATION_BEGIN

```text
CLOSURE_STATUS = CLAIMED
CLOSURE_BEGIN_BYTE = 00000000
PREDECLARATION_OUTPUT_SCAN = 0 hits for this artifact and its sidecar
READ_SET = exactly the 22 content-addressed members below
UNDECLARED_SOURCE_VERDICT_WEIGHT = forbidden
ADOPTION_EXECUTED = false
```

| # | Closed member | SHA-256 | Role |
|---:|---|---|---|
| 01 | `PROGRAM_STATE_BRIEF_V005.md` | `e26f0d16055f3e833307c893704561cfb683065f0798e80e1dc0a9db7ed7799c` | state pin |
| 02 | `LOCKED_PROCESS.md` | `38149496a2b5d89d20b614d972bffef1867d7cde573b26d77dfb207ece3446fb` | process law |
| 03 | `DECLINE_REGISTER_V002.md` | `957476c8c605a37015d51e209ae3197ef3f7c2275fcdea6682f8074edac3802a` | S01–S37 |
| 04 | `QUESTIONS_SETTLED_REGISTER_V001.md` | `d64778161dbfc741614cba6a7f82b892a8f451fd2b4d61d3cb50e1099d9a8c10` | sealed live snapshot; Q-818/Q-821; seal-time live append tolerated |
| 05 | `relay_inbox/RELAY_PASTE_917_ANCHOR_INSTRUMENT_CODEX2_V001.md` | `141439c321452245f68fcd7dd6d5af1a114af94855c30341f5c3467faf98e2ca` | assignment |
| 06 | `STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md` | `35f581dee0683add232367f953ec7807e89bb5eac47e34f4ad5d781a87c95be0` | relay-915 benchmark/hinge; builder's own prior work carries no self-verifying weight |
| 07 | `STAGE8_AXN_CORE_PAIR_CROSSCHECK_DARIO_V001.md` | `8d44cc22437c5de177d39c197c6cd038e86cd910401b03e3525c1a850874ccf7` | opposite-lane producer/fork check |
| 08 | `STAGE8_GEN_OMEGA_NONCIRCULAR_GENERATIVITY_NO_GO_ATTEMPT_V001.md` | `7000963e769362d87e17e36f06903e63aaa8caaeb284ee9c9f3676febe4f4163` | seven-item benchmark and countermodels |
| 09 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md` | `1be5f6a7e40c34586f3b5ab78f9129de0b5d2041cb6695b0e549443ddd6c6ee0` | authored-anchor template |
| 10 | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_V003_ADVERSARIAL_REVIEW_DETERMINATION_V001.md` | `ae1f60b049f21073c7513f8133712d17b9abf4dfb8c46ccc6ea894fc2283c7eb` | independent singleton-image proof |
| 11 | `STAGE8_TASK4A_ORIGIN_FED_REFINEMENT_TOWER_GENOMEGA_PORT_TYPECHECK_DETERMINATION_CODEX_LANE2_V001.md` | `4e00c7edbec105cd9c60cba483f11c1888a541e5963f3c1168fc6d00085669b0` | DoR-013 three-map boundary |
| 12 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_C0_DOR008_ASSEMBLY_AND_FIRST_RESTRICTION_TEST_V001.md` | `1467ab9850022931e68dc9ffa625d95099d8f6bb74d734dc9d0466f169bd00b6` | joint algebra and factor inclusions |
| 13 | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md` | `2b6291227b7ac5967796014c2ad217c9eb084b5bb6df23fed0a7bca464e8bfab` | complete state-port schema |
| 14 | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md` | `a67ed4352e939bd92e886672b9dfdb848cfcaa453a3760f0daa83d6586d60782` | `res_B`, marginal square, SM-1–SM-8 |
| 15 | `STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md` | `4574b5a1339acd317030df9de2bd9da3edf766dd5f829dd8b966f9d8599c77ab` | producer frontier and K4 handoff |
| 16 | `STAGE8_AXN_ORIGIN_MORPHISM_HUNT_CODEX2_V001.md` | `1278b700cc94a021ab872a6074edb68b33ce3cf80a6111d95d01f4900539c448` | exact missing-origin package |
| 17 | `STAGE8_AXN_S1_THIRD_ROUTE_CODEX2_V001.md` | `aaffb7bee7464064a85e369837a212282e7659346148de4c6695f0ef2e86ab4e` | derived S1 carrier/class |
| 18 | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md` | `32725f12a119bbb456dbe80e2c304e9dfb839acc9a05fda3d9f64e7317158842` | K4 fiber test and K5–K7 |
| 19 | `STAGE8_AXN_BASE_FAMILY_INVERSE_CODEX2_V001.md` | `e857302bd35c01dd7eb30062a48f2ec1937fc02f87a802aa7efd447e222465b2` | exact K1–K7 clauses |
| 20 | `STAGE8_AXN_BASE_FAMILY_CROSSCHECK_DARIO_V001.md` | `3c803cdafb3d61adb338c214ee619a69cef54fbd494d91be1cd750ce9a78c78b` | K1–K7 and point/fiber confirmation |
| 21 | `STAGE8_AXN_FORCING_ORDER_AUDIT_CODEX2_V001.md` | `49c7bb3df048e1a20abc48ecd684b413dd2bfc96fbefba4f9489c954710c1b1f` | prospective-freeze/stop-rule source |
| 22 | `STAGE8_AXN_FORCING_AUDIT_CROSSCHECK_DARIO_V001.md` | `45bad587dd1f8705d3697075ef2f90268bce826bf4aa5197f6c9cc2330c2a019` | stop rule and supersession check |

CLOSURE_DECLARATION_END

# STAGE 8 — AXN JOINT-ANCHOR ADOPTION INSTRUMENT
## CODEX 2 LANE — RELAY 917 — `[PLAN:AXN-BUILD-C15]` — [CLAIMED]

Date: 2026-08-10  
Status: **DRAFT FOR PRINCIPAL DECISION — NOTHING ADOPTED HERE**.  
Scope: a complete two-horn instrument for either approving or declining one prospective,
certificate-quantified joint-state anchor act. The document specifies what the principal would have
to enter and what the acceptance run would execute. It chooses no carrier, tag class, channel,
state, marginal, rank, measure, or outcome. Every substantive headline is **CLAIMED** pending
opposite-lane or registrar disposition.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
```

## 1. Load-bearing byte receivers [CLAIMED]

Each span digest was recomputed from the sealed cleanroom bytes.

| Receiver | Source, half-open span, span SHA-256 | Instrument use |
|---|---|---|
| Q-818 closed hunt | `QUESTIONS_SETTLED_REGISTER_V001.md`, `[1669464,1670877)`, `76c2cf53cce07cd299d944a97a0aaf7c544adc6d06a029ec0cfc5b21775cd943` | derivation route remains open outside the closed hunt; no supply authorized there |
| Q-821 strategic state | same, `[1675467,1678215)`, `246e3678f7c576849ee724daca9e69842b60e0536c488198c92c1ddac10f3b06` | both live families fragments; honestly adopted anchor is remaining named pattern |
| V003 benchmark adjudication | `STAGE8_AXN_LIVE_FAMILIES_CODEX2_V001.md`, `[21159,23459)`, `964319f27af7cfe7cbfe66adfd3d97ddb46aa5be3bae940f7cbac717d47370e0` | seven requirements rendered itemwise; no self-verification claimed |
| hinge | same, `[23459,24815)`, `36a86cd5b1d30e2dfca30a76e068f92318c74abef1c523b9800aeceeb49b5a4b` | authored-and-frozen versus derived |
| CP countermodel pruning | `STAGE8_GEN_OMEGA_NONCIRCULAR_GENERATIVITY_NO_GO_ATTEMPT_V001.md`, `[8818,10521)`, `404a71815a74dd1b6164315a48b765d37e987fe779ff35ca8cdca770666bc1cc` | weak channel conditions and minimal dilation do not generate a state |
| `C_BI` theorem | same, `[10521,14304)`, `1fa8b997d98f6a22a2298b7e46e4d9bd99c7e7346df9f0880eaf75769e3fcf15` | BI plus input-faithfulness; singleton image and nonempty witness |
| seven-item benchmark | same, `[14304,16251)`, `39678c339a7bee39bf5b91d734858ca7defc59fdc49ecb913c4a964872a9bb20` | full no-go successor test and derived-or-honestly-adopted fork |
| authored carrier price | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_ADOPTION_PROPOSAL_V003.md`, `[11706,14861)`, `a41a573289492e28be185eb74a7cdb9dfb312815d68a1faa3192c7c1fea980f1` | carrier/trace adoption disclosed before anchor |
| anchor class/certificates | same, `[14861,21958)`, `b3bc42c69320a93a423257990a0ccbab2203b6b80ed4c7de0e23bf25372a5ad1` | `{BI,DB,SYM}`, input-faithfulness, exact certificate, singleton theorem, nonempty family |
| maps and freeze order | same, `[22842,24541)`, `84a5b7050188448e2a0cc526de0131b297cb734bc96b292968e1e37080b61cf0` | output-free primitive tuple; solve only after T0–T2 |
| CO1–CO4 | same, `[27346,28161)`, `9873c177edd8ab88cee735ff8000fe04c12d7cf9bf354d9eef8a87119c25821e` | common provenance, target independence, no supplementation |
| independent singleton proof | `STAGE8_GEN_OMEGA_GENERATIVE_ORIGIN_RULE_V003_ADVERSARIAL_REVIEW_DETERMINATION_V001.md`, `[568,2549)`, `c76879e1ed486f68aedfebb42b44ebcfde8d372d3ba19012adbb1ebd7413a17a` | BI/DB/SYM all converge at fixed carrier; carrier/anchor remain authored |
| DoR-013 scope | `STAGE8_TASK4A_ORIGIN_FED_REFINEMENT_TOWER_GENOMEGA_PORT_TYPECHECK_DETERMINATION_CODEX_LANE2_V001.md`, `[1993,4235)`, `19bd3cf1958705f2f8b0bb399d20af3c35f76bb892849cd82d506af2be73bfc4` | source-state success pattern; no joint-state codomain |
| complete StatePort | `STAGE8_CTP_PHYS_INPUT_PACKAGE_U2_DOR008_PREMISE_LEVEL_PARTIAL_ASSEMBLY_DETERMINATION_V001.md`, `[10824,11934)`, `4313fb92fd78f6b26a5eba216e7ae7540f6b0698dc82eddd98236ceee5d7cf1b` | required joint state, representation, density, domains, descent, provenance |
| `res_B` and slice direction | `STAGE8_AXN_STATE_ALGEBRA_MAP_CODEX2_V001.md`, `[13916,16298)`, `8670002524fb54092bf38e1682b685eeea13f8c61d62df2725a5bb6959eea039` | actual joint state restricts to history state; slice does not choose it |
| exact missing composition | same, `[16298,17631)`, `2fd79b2e25c7afc381df9b65f7195b8aa0954b729d4b84c37292490a7653e012` | `d_joint -> Omega_C0 -> res_B` |
| SM-1–SM-8 | same, `[17631,20234)`, `546c0a09cb9fa581e54088c129615463c34209df3f26aa43266461fb48bd1602` | complete joint-producer acceptance frame |
| prior producer frontier | `STAGE8_AXN_ORIGIN_PRODUCER_CODEX2_V001.md`, `[13989,16009)`, `7b74ca3e695c7db8f8e8bf2264c18f9b8221e615524b0d51001b3db0726a7dab` | current source map stops at SM-1; source subchecks cannot count as joint rows |
| `res_B`/K4 release | same, `[16009,16804)`, `215df3b46e36171be10922b510e193f39e363575264b0c7583388cfe595216dd` | actual marginal required before K4 |
| missing-origin package | `STAGE8_AXN_ORIGIN_MORPHISM_HUNT_CODEX2_V001.md`, `[23268,23788)`, `324e9958c5089efefd6c32e1156b0156cf5457685e8282fb63e2c9f3c85cd225` | state, marginals, finite compatibility, provenance, no supplementation, coverage |
| derived S1 class | `STAGE8_AXN_S1_THIRD_ROUTE_CODEX2_V001.md`, `[9045,11649)`, `e6792ade970a117fd33c13ec6e720f6cad408de0b644855e70c02a9f2d4e811e` | Borel carrier/class exists; physical member and K4–K7 remain |
| K4 fiber test | `STAGE8_AXN_S1_MEMBER_ATTEMPT_CODEX2_V001.md`, `[11600,13268)`, `745940907443f77e7ba8e4d46431c6e50c4ad13312a679ffa2f82033a470a3ca` | weighted projectivity and Dirac/Haar controls |
| K5–K7 | same, `[13268,14812)`, `1675a328cc8b2559fdc94c5243c50f7fb687360c8dbc8567759741bfc99f45be` | covariance, typed normalization, common-origin descent |
| exact K1–K7 | `STAGE8_AXN_BASE_FAMILY_INVERSE_CODEX2_V001.md`, `[4885,8052)`, `67e792dadaca8ef6858d20cef4834c4b60d4f67eb4beb8723c5df930462b96b4` | complete S1 acceptance clauses |
| K-system confirmation | `STAGE8_AXN_BASE_FAMILY_CROSSCHECK_DARIO_V001.md`, `[3040,4984)`, `eabf0ba0226830730461780ca27d8c8744933b3d90d144a579c8de97c6a28b63` | K3 point versus K4 fiber; normalization type |
| stop-rule item 7 | `STAGE8_AXN_FORCING_ORDER_AUDIT_CODEX2_V001.md`, `[20236,21096)`, `6e89538c20517e5708560d9252459049fdd70b8b48a20ad0b782b85e4a21a6c2` | no retrospective selector fitting; failed clause may not substitute for supply |
| prospective/superseding pattern | `STAGE8_AXN_FORCING_AUDIT_CROSSCHECK_DARIO_V001.md`, `[7291,10002)`, `0862e18ecdc6cbec78e6ff5cc0911be491550bd4d6aa394645b29e25d038694c` | freeze before consumers; supersede by record-native derivation; log and disclose |

## 2. Decision envelope — no decision embedded [CLAIMED]

The principal receives one closed choice with two lawful dispositions. The instrument is invalid if
an `APPROVE` entry leaves any required field open.

```text
JointAnchorDecision.v001 := {
  schema: const "rd22.axn-joint-anchor-decision.v001",
  disposition: enum { APPROVE, DECLINE },                    [PRINCIPAL ENTRY]
  decision_artifact_sha256: lowercase_sha256,                [PRINCIPAL ENTRY]
  decision_time: declared timestamp,                         [PRINCIPAL ENTRY]
  governing_instrument_sha256: sha256(this sealed instrument),
  scope: const "joint-state producer into State(A_C0)",

  if disposition == DECLINE:
    decline_scope: const "this adoption instrument only",
    derivation_route_status: const "remains open",
    downstream_movement: const "none",

  if disposition == APPROVE:
    joint_carrier_entry: JointCarrierEntry.v001,              [PRINCIPAL ENTRY]
    anchor_tag_class_entry: nonempty subset {BI,DB,SYM},      [PRINCIPAL ENTRY]
    anchor_content_entry: exact tag equations below,          [PRINCIPAL ENTRY]
    certificate_manifest_sha256: lowercase_sha256,            [PRINCIPAL ENTRY]
    prospective_freeze_receipt_sha256: lowercase_sha256,      [PRINCIPAL ENTRY]
    supersession_rule: const "DERIVATION_SUPERSEDES_PROSPECTIVELY",
    post_freeze_mutation: const "FORBIDDEN",
    member_selection: const "FORBIDDEN"
}
```

No default is supplied for `disposition`, carrier, tag class, or tag content. `APPROVE` is a whole
act: all fields, certificates, and exact hashes must seal together. `DECLINE` neither proves the
anchor false nor narrows the derivation route.

## 3. Joint carrier entry and the disclosed price [CLAIMED]

V003's theorem works only after its authored scalar carrier and trace are fixed. The joint version
must disclose the corresponding physical price rather than smuggling a history state into a
“reference trace.” The principal's `JointCarrierEntry.v001` must contain:

```text
JointCarrierEntry.v001 := {
  carrier_schema: const "rd22.axn-joint-carrier.v001",
  declared_joint_algebra: content-addressed realization of sealed A_C0,
  stage_index_and_limit_rule: exact finite/cylindrical system and limit target,
  joint_identity: I_C0 (or exact stage family I_N),
  scalar_pairing_or_trace: faithful declared pairing with normalization convention,
  source_factor_embedding: i_src,
  record_factor_embedding: i_R,
  history_factor_embedding: i_B,
  joint_superselection_map: E_joint,
  restriction_and_limit_certificates: exact content-addressed list,
  equivalence_scope: exact representation-change invariance statement,
  physical_price: explicit statement of every authored carrier/pairing datum
}
```

If the pairing/trace on the commutative history factor already encodes a preferred measure, that
fact must be stated as authored input. It may not be called derived merely because it is written as
a trace. A reference-density balance rule is inadmissible unless its reference density is itself
independently derived or expressly included in the principal's adoption price. This is the joint
version of V003's reverse-A2 self-kill.

## 4. Anchor statement the principal would freeze [CLAIMED]

### 4.1 Common joint-producer family [CLAIMED]

For the exact joint carrier entered by the principal, define the declared traceless,
superselection-fixed tangent space

```text
Delta_0^joint := {
  Delta : Tr_joint(Delta)=0 and E_joint(Delta)=Delta
}.
```

The adoption would freeze **the whole family**, never one convenient member:

```text
G_joint := {
  Phi_joint : joint normal-state/density carrier -> same carrier :
    Phi_joint is normal and CPTP;
    Phi_joint is joint-charge covariant;
    Phi_joint E_joint = E_joint Phi_joint;
    Phi_joint has one normalized mixing invariant joint state Omega_Phi;
    ker(Phi_joint restricted to Delta_0^joint) = {0};
    Cert_IF_joint(Phi_joint) is an exact replayable certificate;
    one tag A is frozen in ANCHOR_TAG_CLASS_ENTRY;
    Cert_A_joint(Phi_joint) is exact and replayable;
    every finite/cylindrical restriction and limit square commutes
}.
```

The family definition contains no `Omega_Phi` coordinate, history measure, `K4` result, response,
coupling, physical residual, measured target, or downstream verdict. Candidate enumeration,
carrier, tag class, tag certificate, and all admission predicates freeze before the invariant-state
solve.

### 4.2 Exact tag conditions — principal entry, not builder selection [CLAIMED]

The principal may enter any nonempty tag class allowed below, but the entered class is then frozen
whole. Every admitted tag must prove the same joint state at the fixed carrier.

| Tag | Exact template condition | Required implication |
|---|---|---|
| `BI` — leading case | `Phi_joint(I_C0)=I_C0` together with the common trace-preserving and input-faithful conditions | the normalized joint identity/tracial functional is invariant; mixing uniqueness makes it the only joint state |
| `DB` — alternate | `Phi_joint=Phi_joint^dagger` for the **already entered** joint pairing/trace; no free reference density | trace preservation implies the identity is fixed, reducing to the BI theorem |
| `SYM` — alternate | a finite/content-addressed unitary action `G_joint` is frozen; `Comm(G_joint)=C I_C0`; `Phi_joint` is `G_joint`-covariant | fixed-state uniqueness makes the state invariant under the action; scalar commutant plus normalization gives the same state |

```text
ANCHOR_TAG_CLASS_ENTRY = [PRINCIPAL ENTRY: nonempty subset of {BI,DB,SYM}]
ANCHOR_CONTENT_ENTRY   = [PRINCIPAL ENTRY: exact carrier-bound equations/certificates]
BUILDER_SELECTED_TAG   = false
BUILDER_SELECTED_STATE = false
```

DB is void if its pairing imports an undisclosed reference state. SYM is void if its action is
incomplete, its commutant is nonscalar, or covariance fails. BI is void if identity and trace are
not typed on the actual joint carrier. The class is void if any tag produces a different invariant
state or if its advertised nonempty member class is empty.

### 4.3 State follows only after freeze [CLAIMED]

The frozen construction order is:

```text
J0 seal the principal decision, joint carrier, tag class, candidate grammar, and certificates;
J1 verify carrier/restriction/limit typing;
J2 enumerate candidates and verify Cert_IF_joint plus the tag certificate;
J3 close the candidate set; expose no downstream result;
J4 solve each admitted candidate's unique invariant-state problem;
J5 replay the tag-specific anchor theorem;
J6 prove the family-wide singleton image and finite/limit compatibility;
J7 construct the three marginals and common-origin trace;
J8 only then expose Omega_C0 and res_B(Omega_C0) to the acceptance gauntlet.
```

No fixed-point solve occurs in this instrument. The display is a future proof order, not execution.

## 5. Required certificate manifest — 13 certificates [CLAIMED]

An approval is incomplete unless one content-addressed manifest carries all thirteen certificates.

| ID | Certificate | Failure-capable receiving test |
|---|---|---|
| `JAC-01` | **carrier and type** — exact `A_C0` realization, identity, pairing/trace, factor embeddings, `E_joint`, domains, restriction and limit maps | reject a source-only, record-only, co-located, or wrong-representation object |
| `JAC-02` | **output-free primitive tuple** — no joint state, history measure, response, K4 value, or target occurs as primitive data | reject an answer carried as an input field |
| `JAC-03` | **target independence** — candidate grammar and every admission predicate fixed without downstream vocabulary | fixed-string and semantic scan over the complete frozen manifest |
| `JAC-04` | **failure-capable anchor** — at least one admitted candidate passes and at least one structurally comparable candidate fails; exact witnesses supplied | reject tautological, impossible, or cannot-fail membership |
| `JAC-05` | **no hidden state coordinate** — tag coefficients, DB pairing, SYM action, and carrier trace contain no undisclosed freely chosen density/measure | reverse-A2 inspection; any adopted measure-like datum must be priced openly |
| `JAC-06` | **joint positivity and normalization** — every generated `Omega_C0` is a state on the declared carrier | kernel/positivity and unit tests at finite stages and limit |
| `JAC-07` | **three-marginal certified square** — source, record, and history restrictions bind at `i_src`, `i_R`, `i_B`; source and record reproduce sealed inputs; history is the generated output | compare all three restrictions; no independently attached marginal |
| `JAC-08` | **finite/cylindrical/quotient compatibility** — restrictions commute with embeddings, `q_N`, sequential extension, branch conventions, and the limit | one failed square rejects the candidate |
| `JAC-09` | **common provenance** — carrier, state, ready data, law, record state, history state, domains, and required effects descend from one frozen trace | reject product assembly by declaration |
| `JAC-10` | **no supplementation** — no field is added after J3 and no product factor is independently chosen after seeing an output | hash-diff J3 against final trace; any new datum fails |
| `JAC-11` | **coverage/reopening** — enumerate the admitted family with completeness certificate, or state a bounded class and explicit reopening trigger | reject one successful member reported as forced |
| `JAC-12` | **prospective freeze** — decision and all fields seal before K4–K7 or any downstream consumer executes; stop-rule item 7 bars retrospective fitting | timestamps, input-root hashes, and unread-output receipt |
| `JAC-13` | **supersession by derivation** — a later record-native derivation at the same receiver automatically supersedes the authored anchor prospectively after equivalence/type checks; provenance and earlier uses remain logged | reject silent replacement, backdating, or result-driven mutation |

`JAC-13` is not a license to switch anchors after a failed run. A non-equivalent later proposal
requires a new principal act. Automatic supersession applies only to a sealed record-native
derivation with the same receiver and a passed equivalence/naturality certificate.

## 6. Proof obligations after an approval [CLAIMED]

The approval supplies an authored input class; it does not supply the theorem. The producing lane
must discharge all obligations below without changing the frozen act.

| ID | Proof obligation | Required conclusion |
|---|---|---|
| `JPO-01` | carrier realization | every symbol and map in `JointCarrierEntry` is total and typed on the actual joint lineage |
| `JPO-02` | tag-by-tag anchor theorem | BI, every admitted DB presentation, and every admitted SYM presentation each make the same declared normalized joint identity/tracial functional invariant |
| `JPO-03` | mixing uniqueness | the invariant state in `G_joint` is unique for every admitted candidate |
| `JPO-04` | input-faithfulness | the exact restriction of `Phi_joint` to `Delta_0^joint` has zero kernel; every replacement channel fails |
| `JPO-05` | nonempty/failure-capable family | exact passing and failing witnesses exist independently of downstream results |
| `JPO-06` | singleton-image theorem | `{inv(Phi_joint): Phi_joint in G_joint}` is exactly one state at the fixed carrier, across all admitted tags and transient fibers |
| `JPO-07` | stage/limit compatibility | the finite/cylindrical invariant states form one compatible family and define `Omega_C0 in State(A_C0)` |
| `JPO-08` | three-marginal theorem | `Omega_C0 compose i_src`, `Omega_C0 compose i_R`, and `Omega_C0 compose i_B` exist and bind to the certified source, record, and generated history states |
| `JPO-09` | common-origin/no-supplement theorem | all outputs are functions of the J3-frozen trace; no desired marginal is appended |
| `JPO-10` | representation/equivalence honesty | allowed carrier or tag presentations preserve the state verdict and every gauntlet input |
| `JPO-11` | coverage theorem | the declared family coverage or bounded-class/reopening statement is true at the sealed receiver |

The anchor theorem's core statement is preregistered as:

```text
FOR EVERY Phi_joint IN G_joint:
  inv(Phi_joint) = Omega_anchor,C0;

StateImage(G_joint) = {Omega_anchor,C0};

Omega_anchor,C0 is proved at J4-J6 from the J0-J3 frozen carrier and anchor,
not supplied as a coordinate and not selected after any acceptance output.
```

## 7. Full ordered acceptance gauntlet [CLAIMED]

The run is fail-closed and ordered. A failure records the first failed receiver and stops; it does
not reopen the frozen candidate class.

### G0 — decision and pin completeness [CLAIMED]

1. Validate the decision schema and exact disposition.
2. On `DECLINE`, record no adoption and stop with no movement.
3. On `APPROVE`, require every principal entry, 13-certificate manifest row, source pin, carrier
   inventory, and prospective-freeze receipt.
4. Refuse an approval containing an unfilled field, non-content-addressed object, or post-freeze
   mutation.

### G1 — seven-item no-go benchmark [CLAIMED]

Run, in order:

1. complete structural predicate fixed before the state solve;
2. every pure replacement channel excluded;
3. affine state-valued freedom excluded, not merely supplemented by transient action;
4. nonempty passing family exhibited;
5. singleton invariant-state image, or an exact physical-equivalence theorem for residual members;
6. joint charge covariance, `E_joint` commutation, law fidelity, and finite/cylindrical authority;
7. every new physical price stated openly.

Any failure stops. A 7/7 source-side precedent is not a 7/7 joint result; the joint carrier and joint
certificates must replay independently.

### G2 — anchor theorem and certificate replay [CLAIMED]

Replay `JPO-01` through `JPO-11` and `JAC-01` through `JAC-13`. Run every admitted tag class, not
only BI. Compare their state outputs and require a singleton image. Perturb identity/unitality,
input-faithfulness, the DB adjoint equation, the SYM commutant/covariance equation, restriction maps,
and freeze order; each advertised control must bite.

### G3 — SM-1 through SM-8, unchanged [CLAIMED]

| Order | Row | Required receiving verdict |
|---:|---|---|
| 1 | `SM-1` typed source/codomain | the frozen common-origin input lands in `State(A_C0)` or a complete `StatePort_U2_008`; totality proved |
| 2 | `SM-2` joint positivity/normalization | the actual output, not a source subcheck, is a positive normalized joint state |
| 3 | `SM-3` record marginal | restriction along `i_R` reproduces the admitted outgoing-record state |
| 4 | `SM-4` history binding | `omega_phys=Omega_C0 compose i_B` is a construction output, not an independently chosen attachment |
| 5 | `SM-5` finite/quotient compatibility | all finite history cylinders, `q_N`, embeddings, branch conventions, and limit squares commute |
| 6 | `SM-6` common primitive provenance | carrier, law, record state, history state, domains, and required effects descend from the frozen trace |
| 7 | `SM-7` target independence/no supplementation | no K4–K7 or downstream result entered candidate generation or added a field |
| 8 | `SM-8` coverage | complete family enumeration/certificate, or bounded scope plus reopening trigger, is displayed |

No source-only subcheck counts as an SM row. Any row failure stops the core acceptance run.

### G4 — `res_B` construction [CLAIMED]

Only after eight SM passes, compute structurally

```text
omega_hist := res_B(Omega_C0) := Omega_C0 compose i_B.
```

Verify positivity, normalization, declared representation, and equality to the `JAC-07` history
marginal. The slice `E_out` is not a substitute and no independent `omega_B` may enter.

### G5 — S1 K1 through K3 preconditions [CLAIMED]

| Row | Test on the actual `omega_hist` descendant |
|---|---|
| `K1` | use the already derived regular-Borel carrier, sigma-algebra, and bounded-total-variation extension class; do not treat `A_N` as their source |
| `K2` | construct `d nu_N=A_N d lambda_N` from the state restrictions and prove countable additivity/reproduction at that receiver |
| `K3` | replay `j_NM^{Q,*} A_M=A_N` as zero-section naturality; do not call it projectivity |

### G6 — K4 fiber test on the generated member [CLAIMED]

Let `lambda_N` be the finite history bases obtained from `omega_hist`, and let
`nu_N=A_N lambda_N`. For every `N<=M`, require

```text
(pi_MN)_*(A_M lambda_M) = A_N lambda_N.
```

The test integrates over the full fiber. K3 evaluates only on the zero-section image and cannot
stand in for K4. The identity-Dirac and product-Haar families remain controls:

```text
Dirac: algebraic K4 PASS, but no independent promotion or provenance credit.
Haar on a nontrivial charged character: K4 FAIL by fiber integration.
```

If the adopted joint anchor produces a history marginal in the failing control class, the run
records `K4 FAIL` and stops. The anchor is not revised, narrowed, or swapped after seeing that result.

### G7 — S1 K5 through K7 [CLAIMED]

| Row | Required test |
|---|---|
| `K5` | the generated bases and weighted measures carry the matching common-gauge, boundary, contour, and domain transformations; amplitude invariance alone is insufficient |
| `K6` | measure normalization is checked at its own type and is K4-compatible; `A_N(1)=1` and state normalization do not substitute |
| `K7` | the base and weighted family descend from the frozen joint common-origin trace; record-only Q7 or a provenance status flag cannot substitute |

The reproduction/characteristic theorem and every receiver-bound interface must close on the same
generated family. Only a G0–G7 all-pass run may issue `JOINT_ANCHOR_ACCEPTED_AT_CORE = true`.

### G8 — stop-rule and mutation guard [CLAIMED]

The complete run records all inputs, the first failed clause, and all pass/fail receipts. It may not:

- rerun with a different tag, trace, action, carrier, or marginal because a downstream result was
  inconvenient;
- discard a failed adopted anchor as though it had never been adopted;
- count repeated negative runs as a derivation;
- move dependent lines before the full gauntlet passes.

## 8. Honest decision ledger [CLAIMED]

### 8.1 Before the principal decides [CLAIMED]

```text
JOINT_ANCHOR = NOT ADOPTED
JOINT_STATE = NOT PRODUCED
CORE = unchanged
LINES_DIRECTLY_CLOSED = 0
DEPENDENT_LINES_UNBLOCKED = 0
```

This instrument itself moves nothing.

### 8.2 If the principal declines [CLAIMED]

```text
JOINT_ANCHOR = DECLINED AT THIS INSTRUMENT
ROAD_1_NAMED_VOCABULARY = exhausted at the registered scope
DERIVATION_ROUTE = remains open
CORE = open at the joint producer
DOWNSTREAM = unchanged
```

Decline is not a no-go theorem and does not authorize a different silent supply.

### 8.3 If the principal approves, before the gauntlet [CLAIMED]

Every downstream `FREEDOMS-CONSUMED` block must carry exactly:

```text
JOINT_ANCHOR_INPUT = ADOPTED-AND-FROZEN
JOINT_ANCHOR_DERIVED = false
JOINT_ANCHOR_SOURCE = <principal decision artifact + instrument hash>
ANCHOR_TAG_CLASS = <principal entry>
STATE_OUTPUT = not yet exposed
SUPERSESSION = future same-receiver record-native derivation, prospectively logged
```

Approval supplies the input class and price. It does not assert the anchor theorem, SM passes, K4,
or core closure.

### 8.4 If the entire gauntlet passes [CLAIMED]

The assignment's program-level tally is carried without inflation:

```text
CORE = closed at the joint-state producer
CORE_LINES_DIRECTLY_CLOSED = 2/5
DEPENDENT_LINES_UNBLOCKED = 3/4
DEPENDENT_LINES_PASSED = 0/4 by this instrument
```

“Unblocked” means the dependent construction/test may start on a generated common-origin input. It
does not mean its own receiver passes. `res_B` supplies the S1 member only after the gauntlet reaches
that step; K4–K7 can still reject it.

### 8.5 If the gauntlet fails [CLAIMED]

```text
JOINT_ANCHOR_INPUT = remains ADOPTED-AND-FROZEN
CORE_ACCEPTANCE = FAIL at the first named clause
CORE_LINES_DIRECTLY_CLOSED = 0/5
DEPENDENT_LINES_UNBLOCKED = 0/4
RETROSPECTIVE_REPAIR = forbidden
```

A later non-equivalent anchor requires a new principal decision. A same-receiver record-native
derivation may supersede prospectively under `JAC-13`; its equivalence and downstream consumer list
must be displayed.

## 9. Balanced decision display — zero advocacy [CLAIMED]

| Disposition | What it knowingly does | What it does not claim |
|---|---|---|
| `APPROVE` | openly authors and freezes the exact joint carrier/anchor class before output; commits to carrying `ADOPTED-AND-FROZEN`; authorizes the preregistered proof/acceptance run | does not derive the anchor, guarantee the theorem, guarantee K4, or guarantee downstream success |
| `DECLINE` | leaves the joint producer and core open; keeps derivation-only work lawful; records that the completed instrument was declined | does not prove impossibility, revive a closed named route, or authorize a hidden substitute |

The instrument recommends neither disposition. The choice and every principal entry belong to the
principal.

## 10. FREEDOMS-CONSUMED [CLAIMED]

```text
CARRIED:
  the registered Road-1 exhaustion at its bounded scopes;
  V003's authored carrier/anchor template and DoR-013 freeze discipline;
  the no-go countermodels and seven-item benchmark;
  A_C0, StatePort_U2_008, res_B, SM-1..SM-8;
  the derived S1 carrier/class and exact K1..K7 conditions;
  the stop-rule prospective/supersession pattern;
  the assignment's 2/5 direct and 3/4 unblocked ledger tallies.

DERIVED HERE:
  a closed two-disposition decision schema;
  the joint-carrier and tag-entry schema;
  the 13-certificate manifest;
  the 11 proof obligations;
  the fail-closed G0..G8 acceptance order;
  the approve/decline/pass/fail/supersession ledger semantics.

SELECTED HERE:
  nothing. No disposition, carrier, trace/pairing, E_joint, tag class, tag,
  channel, state, marginal, measure, history member, K4 outcome, line movement,
  or principal entry was chosen or filled.

AUTHORED HERE:
  instrument schemas, field names, order, and failure semantics only. These are
  a decision frame, not physical input content and not an adoption.

FREEDOMS NOT CONSUMED:
  a principal-entry slot was not flattened into an entered value;
  adoption was not flattened into derivation;
  an anchor theorem was not flattened into its premise;
  a source-state precedent was not flattened into a joint-state proof;
  a trace/pairing was not flattened into a derived state;
  K3 point evaluation was not flattened into K4 fiber integration;
  unblocked was not flattened into passed;
  supersession was not flattened into result-driven mutation;
  no member binding, fixed-point execution, end test, physical numeric evaluation,
  or comparison with measured constants occurred.
```

## 11. Flattening, batteries, and custody [CLAIMED]

- **S01–S37 FLATTENING CHECK:** walked. Load-bearing protections include S03 (principal entries remain
  visibly unfilled), S05 (co-location is not a joint extension), and S12 (status, certificate, trace,
  state, and verdict remain distinct). No declined identification is reversed.
- **F_PLDEC:** structural schemas and symbolic equations only; no physical quantity was numerically
  evaluated and no measured constant was consulted.
- **ANTI-TUNING:** candidate class, certificate set, proof obligations, gauntlet, controls, and failure
  semantics all precede any output. The whole candidate family is the unit of adoption.
- **M-2 / four modes:** exact-name, normalized-name, fixed-string, and byte-span/semantic-receiver
  checks were run. Hyphen/spacing variants of `common-origin`, `no-supplementation`,
  `input-faithful`, `ADOPTED-AND-FROZEN`, and `supersession-by-derivation` were included.
- **PE-1..PE-13:** pointer-only and zero verdict weight.
- **BUILDER-NEVER-VERIFIES:** this is a CLAIMED draft. The builder's own relay-915 artifact is used
  only to carry its registered benchmark/hinge display; it receives no self-confirming weight.
- **Search-surface disclosure:** exploratory fixed-string searches traversed names/snippets beyond
  the 22-member evidence closure. Those bytes carry zero premise, citation, inference, or verdict
  weight; every load-bearing statement was rerun on the pinned spans above.
- **PIN CHECK:** all 22 closure members matched their sidecars before writing. The live register is
  checked again at seal under live-append tolerance.
- **CHAIN:** no anchor, fixed-point solve, acceptance run, evaluator, K4 test, or downstream chain was
  invoked.

## 12. Byte-position and self-verb audit [CLAIMED]

```text
CLOSURE_BEGIN_BYTE = 0
CLOSURE_END_BYTE = 4031
PREDECLARATION_OUTPUT_SCAN = 0 hits
PIN_FAILURES = 0
PRINCIPAL_ENTRIES_FILLED = 0
```

Verb audit: “adopt” appears only in historical V003/DoR-013 scope or conditional decision branches;
this artifact adopts nothing. “Prove” appears only as a future obligation or as a cited sealed
theorem. “Close” and “unblock” are conditional on the all-pass gauntlet and retain distinct ledger
meanings. “Supersede” is prospective, receiver-bound, and never a post-result rewrite.
`VERB_AUDIT_SELF = CLEAN`.

## 13. Final lines [CLAIMED]

```text
CLOSURE = declared-first (byte position 0; scan 0 hits)
ANCHOR_TEMPLATE = displayed (tag class + conditions)
CERTIFICATES = enumerated (13)
OBLIGATIONS = enumerated
GAUNTLET = ordered (the full acceptance run)
LEDGER = displayed (closes / marks)
CHAIN_INVOKED = false
VERB_AUDIT_SELF = CLEAN
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```
