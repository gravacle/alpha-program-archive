# STAGE 8 — THE PARTITION THEOREM AT THE RIGID SUB-REGION (T16SR) V001

DATE: 2026-08-15
COMMISSION: T16SR (PARTITION-BUILD). OBJECT: upgrade the junction survey's model-independence
display at the magnitude site (J-6) into a STANDALONE THEOREM — from the closure/junction
clause structure alone: the crossing's magnitude sits allow-side and the quantization class
sits require-side, and no model of the displayed clauses places them otherwise — every proof
step through named clauses, displayed. Plus: the AJ residue (C12 categorical-scope premise;
finite-text enumeration warrant) discharged at bytes where derivable, displayed exactly where
not.
STATUS: DETERMINATION ONLY — no adoption, no authored physics, no value, no number as a value
of anything. Nothing here discharges any named absence of the program; the residue disposition
below concerns THIS theorem's premise set only.

```text
alpha_computed = false ; proof_authorized = false ; kappa_record_computed = false
```

OUTPUT-PATH PROBE: /Users/bgm/MB Work/alpha-program-archive/workspace/
STAGE8_PARTITION_THEOREM_T16SR_V001.md — ABSENT at start of commission (probed before any
write; ls returned "No such file or directory").

---

## §0 — GROUND CONSUMED, SEALS VERIFIED; SWEEP CUTOFF

All seals verified sha256(file) == sidecar digest this commission, in the primary root:

```text
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_V001.md        e1c2ac80...  (the survey; §5 whole,
                                                                     J-6 displays, AJ residue)
OK  STAGE8_ALLOW_REQUIRE_JUNCTION_T14SR_AUDIT_V001.md  0057b134...  (its audit; strengthenings
                                                                     incl. §5.2 slice-identity)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_V001.md             15148ab5...  (conservativity theorem,
                                                                     exhaustive partition, C12)
OK  STAGE8_AMPLITUDE_JUNCTION_S9AD_AUDIT_V001.md       217ac664...  (C-1/C-2/C-3, X5/X7/X11)
OK  STAGE8_FORCING_BOUNDARY_ADJUDICATION_EINSTEIN_V001.md  05c832ad...  (three require shapes,
                                                                     its lines 54-57)
OK  supervision/SURFACE_DEFINITION_OF_RECORD_V003.md   c80c09a2...  (== the AJ's own recorded
    digest and its sidecar; DECLARED SCOPED READ, §0.1 — the displayed-clause carrier)
```

All five commissioned artifacts read IN FULL (named sections and whole bodies). Audit verdicts
consumed at their own verdict lines: T14SR audit OVERALL = CONFIRMED-WITH-CORRECTIONS (D4
meta-verdict CONFIRMED; findings F-1..F-5 all correction-grade, none verdict-bearing); AJ audit
= CONFIRMED-WITH-CORRECTIONS (corrections C-1/C-2/C-3 named exactly, none load-bearing;
convergent verdicts by independent routes).

### §0.1 — SWEEP CUTOFF (declared)

```text
SWEEP_CUTOFF = the five commissioned artifacts at verified seals + ONE declared
  scoped read: supervision/SURFACE_DEFINITION_OF_RECORD_V003.md (seal c80c09a2
  verified against both its sidecar and the AJ's recorded digest BEFORE reading;
  read for its §I–§II displayed-clause enumeration — the finite-text warrant's
  own object; its §III–§VII were necessarily read inside the sealed file and
  supply NO premise anywhere below, with two displayed exceptions cited AS
  CORROBORATION ONLY and so marked: §VII.15's quantization sentence and
  §VII.16's C-A/C-B/C-C carriage, both = the I-2/Q-1056 chain the AJ already
  consumed as ground). File-location listings (filenames only) in both corpus
  roots to resolve the named paths. NOTHING ELSE OPENED. No register/tracker/
  road/plan/continuation file read; "Q-..." tokens EXPECTED-UNLOCATABLE,
  treated as opaque labels carried inside sealed artifacts. No git.
```

The theorem's exhaustion below is by PROOF over an enumerated finite clause set, not by sweep;
the cutoff bounds file access, not the quantifier.

---

## §1 — THE CLAUSE BASE Σ, ENUMERATED AT BYTES (the closure/junction clause structure)

The theorem is stated over Σ = the DISPLAYED EQUATION SET of the sealed surface — the same set
the AJ's ground fact M8 names ("the three lock equations, the character map, the
transition/cocycle law, the conjugate-pair closure, the orbit action") — here RE-DERIVED by
direct inspection of the sealed surface text (scoped read, seal c80c09a2), clause by clause,
with its source line:

```text
Σ1  m_* T_R = π                       SII.9  ("the four derived locks")
Σ2  |ΔS_record| = π ℏ                 SII.9
Σ3  τ_R = π/√2                        SII.7 / SII.9
Σ4  CHARACTER MAP: z_j^(n) = χ_n(h_j[a_j]), unit modulus, with the
    definitional sector pair n ∈ {+1,−1}          SI.3
Σ5  TRANSITION LAW: z_j = g_ij z_i, g_ij = exp(iθ_ij) ∈ U(1)   SI.3
Σ6  CONJUGATE-PAIR CLOSURE: χ_{+1}·χ_{−1} = 1 (the displayed n→−n pair's
    one-step closure)                             SI.3 definitional pair
Σ7  ORBIT ACTION: (T_R → λT_R, H_R → H_R/λ)       SII.7
```

CONCORDANCE: this inventory coincides clause-for-clause with the AJ's M8 list (sealed, seal
15148ab5) and with the audit-corrected I-2 Chain A inventory the AJ carries (cross-sector items
exactly {L1 character map, transition law, L2 indexing declaration}; first two phase-to-phase
with integer slots). Three-way agreement: my inspection, the AJ's enumeration, the AJ audit's
mechanical completion (its X5).

WHAT IS DISPLAYED BUT IS NOT AN EQUATION OF Σ (each named, since the theorem's honesty turns
on this boundary):

```text
D-1  SI.1 fiats: "no native metric argument anywhere in the ratified finite
     object, and it takes no logarithm" — bans, not equations; cited below
     only as reinforcement, never load-bearing.
D-2  SII.6 wording: write weights unit-modulus; "Whatever converts internal
     to external is NOT a magnitude on record" — the categorical booking.
     Its DISPLAYED sentence (carried weights unit-modulus) feeds only the
     G = 1 corollary (§3.4); its CATEGORICAL SCOPE (every stage and junction,
     withheld content) is the C12 premise — consumed NOWHERE in the theorem
     (§5.1).
D-3  SII.7's response-NAMING clause: the orbit "leaves all first-opening
     probabilities, orthogonality, endpoint transfer, and projective lengths
     identical" — an INVARIANCE declaration about named response quantities
     with NO displayed form and NO phase-sector symbol.
D-4  SII.8's booking: every derived junction β-invariant (thirteen sealed
     interface quantities) — a quantified booking, forms withheld.
D-5  WITHHELD-FORM ITEMS named by the text: the balanced-geodesic identities
     and onset bound (SII.9), the refinement law (SI.5; V003 §VII.17 names
     its candidate home OPEN), the thirteen interface identities (SII.8).
     None displays an equation; none is in Σ.
D-6  L2-TYPE DECLARATIONS (the AJ audit's C-1 terrain): indexing/existence
     assertions that a dependence exists while displaying no form. The
     theorem is stated over Σ; §3.5 displays exactly what survives their
     adjunction.
```

THE VOCABULARY FACT AT MY OWN BYTES (the M8/X5 co-occurrence claim, both halves): the free-
symbol inventory of Σ is exactly {m_*, T_R, H_R, ℏ, |ΔS|, λ} ∪ {h, n, θ} (CAS PT1). No clause
of Σ carries any junction/response symbol (CAS PT2a). The two response-NAMING clauses (D-3,
D-4) carry no phase-sector symbol (CAS PT2b). Therefore: NO displayed clause of the sealed
surface contains a response magnitude and a phase-sector variable co-occurring — re-derived
here by inspection plus mechanical check, concording with AJ M8 and AJ-audit X5.

---

## §2 — THE THEOREM, STATED

MODEL NOTION (the AJ's own, made explicit): a MODEL OF Σ is a structure interpreting the
displayed ground vocabulary (§1's symbol inventory) and satisfying every clause Σ1–Σ7;
junction/response quantities enter as EXPANSIONS by new symbols. This is exactly the model
class the AJ's Theorem II quantifies over ("extend the displayed ground structure by the
response profiles..."; "derivability from the ground implies truth in every model of the
ground") and the class the survey's §5.1 display quantifies over. Σ is non-vacuous: the
displayed ground structure itself models it (CAS PT0a/PT0b — lock system consistent, unique
solution over its own variables; orbit fixes the dimensionless locks).

**THE PARTITION THEOREM (rigid sub-region, magnitude site J-6).** Over the displayed clause
set Σ as sealed:

**(I) REQUIRE-HALF.** In EVERY model of Σ, the quantization class holds: every sector/winding
label admissible under Σ4's single-valued closure lies in ℤ — exactly ℤ, no more — and the
definitional pair {+1, −1} inhabits it.

**(II) ALLOW-HALF.** In NO model of Σ is the crossing's magnitude forced: over every model of
Σ, the expansion by profile f₁, the expansion by profile f₂, and the junction-free reduct are
all again models of Σ, pairwise disagreeing on the junction — so Σ derives no response
profile, no profile is pinned in any model, and the magnitude sits allow-side in every model,
with the two-model witness uniform over the whole model class.

**(III) EXCLUSIVITY.** No model of Σ places them otherwise: (a) no model places the
quantization class allow-side; (b) no model places the magnitude require-side; (c) neither
side can be re-read as the other — the class's output is an integer label, not a continuum
response magnitude, and no clause of Σ exists through which a require-chain could reach a
magnitude.

Every step of the proof passes through a named clause of §1 or a displayed logical step, and
each is listed in §3 with its clause and its CAS check.

---

## §3 — THE PROOF, EVERY STEP THROUGH NAMED CLAUSES

### §3.1 REQUIRE-HALF — the quantization class, forced in every model

```text
S1  [Σ4, SI.3] Sector-n phase data enter ONLY as U(1) characters χ_n on the
    holonomy argument: z^(n) = χ_n(h), with g = exp(iθ) ∈ U(1).
S2  [Σ4's own domain typing] χ_n is a map ON U(1): h and h + 2π present the
    same group point, so well-definedness (single-valuedness) of the
    displayed clause itself demands χ_n(h + 2π) = χ_n(h). This is not an
    added axiom; it is Σ4's "∈ U(1)" read as displayed. (The sealed surface
    says it in its own words at §VII.15 — "integer-quantized by U(1)
    single-valuedness" — cited as CORROBORATION, not premise.)
S3  [exact algebra] χ_n(h + 2π)/χ_n(h) = exp(2πin); closure ⟺
    exp(2πin) = 1.  [CAS PT3a]
S4  [exact algebra — the class, solved] The full solution set of
    exp(2πi·x) = 1 is EXACTLY ℤ (solved over ℂ, and over ℝ via the real
    closure component)  [CAS PT3d]; integer labels all pass at symbolic n
    [PT3a]; non-integers fail exactly (residual −2 at 1/2; nonzero at 1/3)
    [PT3b, PT3c]; the definitional pair {+1,−1} inhabits the class and
    closes magnitude-free  [PT3e].
S5  [soundness — the AJ's displayed logical step] "Derivability from the
    ground implies truth in every model of the ground." S1–S4 is a
    derivation from Σ4 alone; hence the quantization class holds in EVERY
    model of Σ. REQUIRE-SIDE, every model.  ∎(I)
```

Class-vs-member boundary (carried from the T14SR audit's strengthening A-X-B, re-executed):
every phase-law clause of Σ holds at SYMBOLIC integer n [CAS PT4] — the clauses force the
CLASS and pin no member. The member |n| stays allow-side (the survey's J-1/A-3 display); the
theorem's require-half claim is the CLASS, exactly as commissioned.

### §3.2 ALLOW-HALF — the magnitude, free in every model

```text
S6  [§1 enumeration] Σ is exactly Σ1–Σ7: re-derived by direct inspection of
    the sealed surface (scoped read at verified seal), concordant with AJ M8
    and AJ-audit X5.  [CAS PT1]
S7  [vocabulary fact] No clause of Σ carries any junction/response symbol;
    the response-NAMING clauses (D-3, D-4) carry no phase-sector symbol.
    [CAS PT2a, PT2b]
S8  [invariance lemma — displayed logical step] The satisfaction of a clause
    depends only on the interpretation of the clause's own vocabulary. By S7
    the junction vocabulary is disjoint from Σ's; hence Σ-satisfaction is
    INVARIANT under every reinterpretation of junction structure: expanding,
    exchanging, or deleting a profile touches no clause of Σ.
S9  [the model pair, concrete — TOYS, quarantined §9] Over ANY model M of Σ:
    adjoin f₁ = (2 − w − conj w)/4 = sin²(Δ/2) exactly [PT5a], or f₂ = f₁².
    Both real/magnitude-valued [PT5b]; both β-invariant — Σ7's orbit test
    passed [PT5d]; both transition-invariant — they factor through the
    invariant drive datum w, which Σ5's covariance supplies [PT5e, PT6];
    log-free per SI.1 [PT5f]; both normalize exactly [PT5g]. No clause of Σ
    mentions the profile symbol or its argument [PT5h], so BOTH expansions
    and the junction-free reduct satisfy Σ clause-by-clause [S8 + PT5h].
    The two expansions differ as functions with exact separation residual
    1/4 at the toy argument [PT5c].
S10 [two-model criterion — the AJ's displayed step, now uniform] Two models
    of Σ agree on every displayed clause and differ on the junction; by S5's
    soundness principle no profile clause is derivable from Σ. By S8 this
    freedom is available OVER EVERY MODEL, not just at one exhibit: in no
    model does Σ-satisfaction pin any profile. ALLOW-SIDE, every model, with
    the witness pair uniform.  ∎(II)
```

### §3.3 EXCLUSIVITY — no model of Σ places them otherwise

```text
S11 (a) The class cannot sit allow-side in any model: S1–S4 is a derivation,
    so by S5 a structure violating the class fails Σ4's own closure — it is
    not a model of Σ at all. There is no model-side freedom to move it.
S12 (b) The magnitude cannot sit require-side in any model: "forced in M"
    would mean M's Σ-satisfaction pins the junction; S8's invariance says
    any junction interpretation in M can be exchanged for a disagreeing one
    (S9's pair, constructed over M itself) with every clause of Σ
    untouched. No pinning exists in any model.
S13 (c) No cross-reading:
    (c1) The class's output cannot serve as the crossing's magnitude: the
         closure equation's solution set is a discrete label set (PT3d);
         the sealed AJ battery says it in its own words — "any winding-route
         output is an integer label, not a continuum response magnitude"
         (P3b, sealed).
    (c2) No require-chain can reach the magnitude through Σ: a forcing
         clause would be a displayed clause where a response magnitude and
         phase-sector data co-occur; §1's vocabulary fact (PT2a/PT2b) is
         that no such clause exists on the sealed surface.
    (c3) The joint-constraint rescue (Gleason-style forcing) fails on Σ,
         carried at the AJ audit's corrected scope: kill 1 — the forcing
         substrate is itself adjoined (general, decisive); kill 3 — the
         joint constraints Σ actually displays (normalization, orbit,
         transition) are absorbed by both exhibited models [PT5d, PT5e,
         PT5g]; kill 2 (dim-1 fiber) carried at its fiber-local scope per
         audit C-2.  ∎(III)
```

### §3.4 COROLLARIES (each with its named premise, displayed)

```text
COR-1 [Σ5, Σ6 only] The charge structure holds in every model: transition
      covariance at symbolic n and unit-modulus invariant content [PT6];
      conjugate closure magnitude-free [PT3e].
COR-2 [Σ + D-2's DISPLAYED sentence — the carried-weight unit-modulus
      wording of SII.6; NOT its categorical scope] On-record amplitude is
      uniquely trivial in every model: G·(native composite) on record forces
      G = 1 [PT7a, PT7b]. The corollary's extra premise is named here
      precisely so the CORE theorem stays SII.6-free.
```

### §3.5 THE L2 BOUNDARY (D-6), displayed exactly

Adjoining the L2-type indexing declarations (existence-of-dependence assertions with no
displayed form) to Σ changes NEITHER half: the require-half chain S1–S5 never touches them;
the allow-half's disagreement pair survives because an existence assertion with no form
SELECTS nothing — both f₁ and f₂ satisfy it (the AJ audit's X7: "P := f₁ and P := f₂ both
nonconstant and both normalizing — L2's existence assertion selects nothing"). ONE
strengthening is Σ-relative only: the junction-free reduct (S9's third model, the AJ audit's
X11 pattern) is a model of Σ, but under Σ ∪ {L2-existence} the existence-freedom claim is not
asserted here. The commissioned claim — the MAGNITUDE (the profile) unforced — holds over both
Σ and Σ ∪ {L2}.

---

## §4 — CAS BATTERY (fresh venv; exact symbolic; zero numerics)

Environment: fresh venv under the session scratchpad, sympy 1.14.0; no evalf, no N(), no float
literal (mechanical scan of script and output: the only pattern hit is the fence-declaration
comment itself); ℏ, π, √2 symbolic throughout; exact integers/rationals appearing (1, 1/2,
1/3, 1/4, −2) are algebra residuals of identity checks on ground-symbolic or quarantined-toy
objects, never physical values. Script sha256
`84e277da5bae0ed8d2a4aaf0a2b693aedd035924f0e5b17a533edccbbe9db26c`. Output, verbatim:

```text
T16SR PARTITION-BUILD CAS -- exact symbolic, zero numerics (sympy)
[PT0a] PASS -- displayed lock system consistent; unique solution over its own variables (Sigma non-vacuous)
[PT0b] PASS -- tau_R orbit-fixed; the displayed ground structure itself models Sigma
[PT1] PASS -- Sigma symbol inventory exact: locks/orbit {m_star,T_R,H_R,hbar,dS_abs,lambda}; phase laws {h,n,theta}
[PT2a] PASS -- NO displayed equation carries any junction/response symbol (both halves: locks AND phase laws)
[PT2b] PASS -- the two response-NAMING clauses (SII.7 invariance, SII.8 booking) carry no phase-sector symbol: no displayed clause has response magnitude and phase datum co-occurring (M8 at bytes, completed)
[PT3a] PASS -- single-valued closure holds at SYMBOLIC integer n: exp(2*pi*i*n) == 1, all n at once
[PT3b] PASS -- n = 1/2 fails closure EXACTLY (residual -2): half-integer labels excluded
[PT3c] PASS -- n = 1/3 fails closure (exact nonzero residual): second non-integer witness
[PT3d] PASS -- the FULL admissible label set of the closure equation is EXACTLY the integers (solved over C, and over R via the real closure component): the class ZZ, no more, no less -- the quantization class as a solved object
[PT3e] PASS -- the definitional pair n in {+1,-1} inhabits the class and closes magnitude-free (chi_+1 chi_-1 == 1)
[PT4] PASS -- every phase-law clause holds at SYMBOLIC integer n (covariance + unit modulus): the clauses force the CLASS and pin no member
[PT5a] PASS -- toy profile f1 == sin^2(Delta/2) exactly (magnitude-valued, phase-driven)
[PT5b] PASS -- both toy profiles real
[PT5c] PASS -- f1 != f2 as functions; exact separation residual 1/4 at the toy argument
[PT5d] PASS -- both toy profiles beta-invariant (orbit-fixed): SII.7's admissibility test passed
[PT5e] PASS -- both toy profiles transition-invariant (factor through the invariant drive datum w)
[PT5f] PASS -- no logarithm anywhere (SI.1: the object takes no log)
[PT5g] PASS -- both toy models normalize exactly: normalization separates nothing
[PT5h] PASS -- no displayed equation mentions the profile symbol or the profiles' argument: expansion by f1, by f2, and the junction-free reduct all satisfy Sigma clause-by-clause
[PT6] PASS -- relative holonomy transition-invariant AND unit-modulus at symbolic n: the invariant internal content is pure phase (P1/P2 at bytes)
[PT7a] PASS -- every native composite unit-modulus at symbolic n (SI.3 + SII.6 displayed wording)
[PT7b] PASS -- G*(native object) on record forces G == 1 uniquely: trivial amplitude, every model
TOTAL: 22 checks; FAIL count = 0
```

One formulation repair during the run, displayed: the first PT3d formulation (solveset over ℝ
directly on the complex exponential) returned a sympy domain artifact ({0}); reformulated to
the two sound exact forms (over ℂ; over ℝ via the real closure component), both returning
`Integers` exactly. No claim ever rested on the failed formulation.

---

## §5 — THE RESIDUE, DISPOSED (deliverable 2)

The survey's §5.1 carried the rigidity as "conditional on the AJ residue: C12 + finite-text
warrant" — because it displayed the rigidity AS the AJ closure's own property, and the AJ
closure (VACANCY-CLOSED, the full vacancy) does carry both. The standalone theorem's premise
set is SMALLER, and each residue is disposed against IT, at bytes:

### §5.1 — R-A: THE C12 CATEGORICAL-SCOPE PREMISE — DISCHARGED BY PROVEN NON-CONSUMPTION

CONTENT (AJ §8.2, verbatim in substance): the categorical scope of SII.6's sealed wording
("unit modulus"; "NOT a magnitude on record"; "provably do not carry" — at every stage and
junction), inherited from the I-2 pair; it carries the AJ's withheld-content typing (§6.4) and
Theorem I's refinement-stage step.

NON-CONSUMPTION, proven step-by-step — the premise audit of every proof step:

```text
S1–S4   consume Σ4 + exact algebra.               SII.6: NOT touched.
S5      consumes the soundness step.              SII.6: NOT touched.
S6–S7   consume the §1 enumeration + PT1/PT2.     SII.6: appears only as
        D-2's NAMING in the non-equation list — no scope claim used.
S8      consumes the invariance lemma (logic).    SII.6: NOT touched.
S9–S10  consume Σ5/Σ7 + the toy pair + PT5.       SII.6: NOT touched.
S11–S13 consume S5, S8, PT3d, PT2, and the AJ's   SII.6: NOT touched (c3's
        §6.3 kills 1+3 at audit-corrected scope.  kill 1 is substrate-
                                                  adjunction, not typing).
COR-2   consumes SII.6's DISPLAYED carried-weight sentence — the corollary
        is OUTSIDE the core theorem and carries its premise by name.
```

Zero steps of (I)/(II)/(III) consume SII.6's categorical scope. This is the byte-level
instantiation of the AJ's own sealed sentence: "Theorem II's conservativity core does NOT
depend on C12 — it rests only on the displayed equation set" (AJ §8.2), which the T14SR survey
itself carried in parenthesis ("the conservativity core itself rests only on the displayed
equation set"). The require-half consumes even less (one clause and algebra).

DISCHARGED — FOR THIS THEOREM: the C12 premise does not appear in the theorem's premise DAG,
so it cannot condition the theorem. WHAT C12 STILL CONDITIONS (displayed exactly, since the
discharge is by non-consumption, not by proving C12): the AJ's FULL vacancy closure — 
specifically its withheld-content arm ("no disclosure of withheld content can contain the
missing forcing equation") and Theorem I's refinement-stage step. The standalone theorem does
NOT assert that arm; under this theorem's scope clause (§6) a disclosure adding a displayed
forcing clause is a CHANGE OF GROUND and re-poses. The sealed object that would discharge C12
ITSELF, for the AJ's full claim: a booked result fixing SII.6's categorical scope
(stage-unqualified, junction-covering) at derived grade — the AJ's named need, not this
theorem's.

### §5.2 — R-B: THE FINITE-TEXT ENUMERATION WARRANT — DISCHARGED AT BYTES

CONTENT (AJ §8.2): "The displayed-operation and displayed-equation enumerations are exhaustive
because the ground is a finite sealed text, enumerable by inspection" — marked [DERIVED] by
the AJ itself (it was never a conditional premise there; the survey folded it into "the named
residue" as the closure's second warrant).

ITS LOAD-BEARING INSTANCE FOR THIS THEOREM: that Σ1–Σ7 is the COMPLETE displayed equation set
of the sealed surface, and that no displayed clause carries junction vocabulary co-occurring
with phase vocabulary (S6–S7).

DISCHARGED AT BYTES, THIS COMMISSION'S OWN HANDS: the inspection was RE-PERFORMED here — the
sealed surface text (167 lines, seal c80c09a2 verified against both its sidecar and the AJ's
recorded digest) was read at §I–§II and its displayed equations enumerated clause-by-clause
with source lines (§1); the vocabulary fact was checked mechanically on BOTH halves (PT1,
PT2a, PT2b — locks, phase laws, AND the response-naming clauses). Concordance three ways: this
inspection == AJ M8 == AJ-audit X5. The warrant is thereby not carried but EXECUTED: for a
finite sealed text, "enumerable by inspection" discharges by performing the enumeration, which
is what §1 is. Residual scope, displayed: the warrant is (and can only ever be) relative to
THE sealed text at THE verified seal — a new displayed clause is a change of ground (§6).

### §5.3 — RESIDUE VERDICT

```text
R-A (C12):         DISCHARGED for this theorem (proven non-consumption,
                   premise-DAG audit above); its surviving conditionality
                   attaches ONLY to the AJ's fuller vacancy claim, displayed
                   exactly, with its discharging object named.
R-B (finite-text): DISCHARGED at bytes (enumeration re-performed at verified
                   seal; mechanical vocabulary check both halves; three-way
                   concordance).
=> The PARTITION THEOREM as stated in §2 is UNCONDITIONAL over its stated
   scope: no unproven premise remains in its DAG.
```

---

## §6 — SCOPE OF THE THEOREM (honesty rails, displayed)

```text
CURRENT GROUND ONLY. The theorem binds Σ AS SEALED — the displayed clause
  set of SURFACE_DEFINITION_OF_RECORD_V003.md at seal c80c09a2, enumerated
  in §1. A change of ground — a new booked displayed clause, disclosure of
  any withheld form AS a displayed clause (geodesic identities, onset bound,
  refinement law, the thirteen interface identities), a weakening of SI.3/
  SII.7's display — RE-POSES the question; the theorem forecloses nothing
  about changed ground. This is the AJ's own supply route restated: an
  entering forcing clause would create a NEW Σ (AJ §8.3: the new structure
  must supply "a booked clause in which a response magnitude and
  phase-sector data co-occur"), and the theorem is ABOUT the Σ that has no
  such clause.
WHAT THE THEOREM DOES NOT CLAIM: the AJ's withheld-content arm (C12-
  conditional, §5.1); the existence-freedom strengthening over Σ ∪ {L2}
  (§3.5); anything about the member |n| beyond its allow-side display;
  any program-level FORCED/FREE verdict (the survey's §5.3(iii) stands
  untouched).
NO LENS: no lens reading is named, used, or motivated anywhere; the one
  lens-fenced display in the survey (its J-4 row) belongs to a site this
  theorem does not touch.
J-2 OUT OF SCOPE: the extent site's movability is decided of record (the
  survey's §5.2 sealed two-model witness; twice-determined) and is NOT
  re-walked, re-derived, or disturbed here. The theorem's rigid sub-region
  is J-6 and only J-6; the survey's JUNCTION-CONTINGENT overall verdict is
  untouched — this theorem is the rigid sub-region's upgrade, not a rival
  global verdict. (The audit's A-X-A slice-identity strengthening was read
  as commissioned and is cited nowhere in the proof: it is J-2 material.)
DETERMINATION ONLY: the theorem re-grounds a display as a theorem; it moves
  no gate, flag, witness, or verdict of any consumed artifact; the AJ's
  artifact-level status (PROPOSED-NOT-ADOPTED, panel discipline) is
  untouched — this commission derives FROM its sealed content and upgrades
  nothing about its record status.
```

---

## §7 — VERDICT

```text
VERDICT = PARTITION-THEOREM-DERIVED (unconditional over its stated scope).

THE THEOREM (§2), proven whole at §3, every step through named clauses
(Σ1–Σ7 at their SI/SII lines; the displayed logical steps named; 22/22 CAS):
  (I)   the quantization class sits REQUIRE-SIDE in every model of Σ —
        Σ4's own single-valuedness solves the label set to EXACTLY ℤ;
  (II)  the crossing's magnitude sits ALLOW-SIDE in every model of Σ —
        junction vocabulary is absent from Σ (both halves mechanical), so
        the two-model freedom is uniform over the whole model class;
  (III) no model of Σ places them otherwise — a class-violator fails Σ4
        and is no model; a magnitude-pinner is defeated by vocabulary
        invariance in its own model; no cross-reading exists (integer
        label ≠ continuum response; no co-occurrence clause to carry a
        require-chain; joint-constraint rescue dead at corrected scope).
BOTH RESIDUES DISCHARGED (§5): C12 by proven non-consumption (premise-DAG
  audit; surviving conditionality attaches only to the AJ's fuller claim,
  displayed with its discharging object); finite-text warrant by
  re-performed enumeration at verified seal with mechanical vocabulary
  check (three-way concordance).
SCOPE: current ground only (Σ as sealed at c80c09a2); a change of ground
  re-poses (§6). The survey's site-split meta-verdict is untouched; what
  changes is the rigid sub-region's GRADE: from model-independence DISPLAY
  carrying the AJ residue, to STANDALONE THEOREM carrying no unproven
  premise on its own scope.
```

---

## §8 — CHOICE LEDGER (commission T16SR; every unforced choice, classified)

```text
CH-1  MODEL NOTION (structures interpreting the displayed ground vocabulary,
      junction quantities as expansions): FORCED in substance — it is the
      AJ Theorem II's own proof form ("extend the displayed ground
      structure...") and the survey §5.1's quantifier; the proof consumes
      only soundness + vocabulary invariance, which any faithful model
      notion supplies.
CH-2  Σ = the displayed EQUATION set (locks, character map, transition law,
      conjugate closure, orbit action): FORCED by the AJ M8's own
      definition of "the full displayed equation set," re-derived at my
      scoped read; the non-equation displayed material is not dropped but
      DISPLAYED and disposed (D-1..D-6, §3.5).
CH-3  DECLARED SCOPED READ of the sealed surface §I–§II to execute the
      finite-text warrant: PREMISE(named, §0.1) — seal verified against the
      AJ's recorded digest BEFORE reading; the alternative (carried-grade
      discharge via AJ [DERIVED] + audit X5) exists and concords; the
      theorem's content is identical under either; the read makes R-B's
      discharge this commission's own act rather than a carriage.
CH-4  G = 1 kept as COROLLARY with named premise (SII.6 displayed sentence)
      rather than core content: FORCED by the commission's two targets
      (magnitude, quantization class) + the C12-cleanliness requirement:
      the core must consume no SII.6 scope, and does not.
CH-5  CONJUGATE CLOSURE (Σ6) listed as a displayed clause though it is a
      one-step consequence of Σ4's definitional pair: PREMISE(named) —
      follows the AJ M8 list verbatim; IMMATERIAL (as consequence or clause
      it holds in every model either way; nothing else cites it).
CH-6  PT3d reformulation after the sympy domain artifact: FORCED — the
      first formulation returned a provably wrong set for the stated
      equation ({0} for exp(2πix)=1 over ℝ); both sound forms agree;
      displayed in §4.
CH-7  BATTERY SCOPE (22 checks; re-derivations of sealed identities plus
      the theorem's own vocabulary/solve checks): PREMISE(named) — the
      textual/structural steps (S2, S5, S8) are logic, not CAS objects;
      everything algebraic in the proof is checked.
CH-8  CITING §VII.15/§VII.16 as corroboration only: FORCED in substance —
      the AJ declared V003 §VII non-load-bearing for its ground; this
      theorem inherits that discipline; both citations are marked and
      carry no premise.
ZERO entries in class OPEN.
```

---

## §9 — TOY_SEPARATION

```text
TOY_SEPARATION = clean
  The only toy objects are the AJ pair's own quarantined model profiles
  f₁, f₂, re-executed at PT5 EXACTLY as the sealed build quarantines them:
  meta-level counterexamples about the derivability relation, entering the
  proof solely as S9's model exhibits (which IS the theorem's content, as
  the AJ audit ruled for the AJ's own use). Neither is asserted as surface
  content, candidate, or physics; no toy value, profile, or model leaks
  into any theorem clause, residue disposition, verdict, or flag. Every
  load-bearing non-toy object is a sealed clause read at verified seal or
  an exact-algebra identity.
```

---

## §10 — FLAG BLOCK

```text
COMMISSION = T16SR (PARTITION-BUILD; the survey's J-6 model-independence
  display upgraded to a standalone theorem; residues disposed).
SEALS = 5/5 commissioned artifacts MATCH + the surface at c80c09a2 MATCH
  (sidecar AND the AJ's recorded digest); MISMATCHES = 0; all named
  sections read in full.
THEOREM = PARTITION THEOREM AT J-6, PROVEN WHOLE (§2/§3): (I) quantization
  class require-side in every model of Σ (Σ4 single-valuedness; label set
  solved EXACTLY ℤ; definitional pair inhabits it); (II) crossing's
  magnitude allow-side in every model of Σ (junction vocabulary absent from
  Σ — both halves mechanical; two-model freedom uniform over the model
  class; junction-free reduct also a model over Σ); (III) exclusivity — no
  model places them otherwise (class-violators are non-models; magnitude-
  pinners defeated by vocabulary invariance; no cross-reading: integer
  label not a continuum response, no co-occurrence clause, Gleason-style
  rescue dead at audit-corrected scope). Every step through named clauses
  Σ1–Σ7 at their SI/SII lines + named logical steps (soundness; invariance
  lemma). Corollaries carried with named premises (charge structure Σ5/Σ6
  only; G = 1 with SII.6's displayed sentence). L2 boundary displayed
  (§3.5): the profile stays unforced over Σ ∪ {L2} too.
RESIDUES = BOTH DISCHARGED (§5): C12 by proven non-consumption (step-by-
  step premise audit; zero core steps touch SII.6's scope; the surviving
  C12 conditionality attaches only to the AJ's withheld-content arm, which
  this theorem does not assert — displayed exactly, discharging object
  named); finite-text warrant by RE-PERFORMED enumeration at verified seal
  (§1) + mechanical vocabulary checks (PT1/PT2a/PT2b), three-way
  concordance (this inspection = AJ M8 = AJ-audit X5).
VERDICT = PARTITION-THEOREM-DERIVED — unconditional over its stated scope:
  Σ as sealed; a change of ground re-poses (§6). The survey's overall
  JUNCTION-CONTINGENT verdict untouched; J-2 not re-walked; no lens; the
  AJ's record status (PROPOSED-NOT-ADOPTED) untouched.
CAS = fresh venv, sympy 1.14.0, 22/22 PASS, exact symbolic only, zero
  numerics (scan: only the fence-declaration comment matches); script
  sha256 84e277da...; one formulation repair displayed (§4), no claim ever
  rested on the failed form.
SWEEP_CUTOFF = DECLARED (§0.1): five commissioned artifacts + one declared
  scoped read of the surface at verified seal + filename listings; no
  register/tracker/road/plan/continuation file read; "Q-..."
  EXPECTED-UNLOCATABLE.
CHOICE_LEDGER = CH-1..CH-8 (§8): 5 FORCED/FORCED-in-substance, 3
  PREMISE(named); ZERO OPEN. TOY_SEPARATION = clean (§9).
DETERMINATION_ONLY = held: no adoption, no authored physics, no value, no
  number as a value of anything; no gate/flag/witness/status of any
  consumed artifact moved; nothing here discharges any named absence of
  the program (the §5 discharges concern this theorem's premise set).
OUTPUT = ONE file + seal sidecar at the commissioned path, probed ABSENT
  before first write; no git; no existing file edited.
alpha_computed = false ; proof_authorized = false ;
kappa_record_computed = false — ALL REMAIN.
NET = the rigid sub-region is now a THEOREM of the displayed clause
  structure itself: on the sealed surface's own seven displayed clauses,
  every model quantizes the class to exactly ℤ and no model pins any
  magnitude — proven with both AJ residues off its premise DAG (one
  executed at bytes, one proven never consumed) — so the survey's J-6
  rigidity no longer rides on the AJ closure's conditionality: it stands
  alone on the clauses, exactly as far as the sealed ground reaches, and
  not one clause farther.
```
