# STAGE 8 — GLUED-TOPOLOGY CHECK: BLIND ADVERSARIAL VERIFICATION OF THE GLUED-TOPOLOGY HUNT

Lane: FABLE blind adversarial verifier, codename GLUED-TOPOLOGY-CHECK, cross-lineage.
DEFAULT = REFUTE. Under test: STAGE8_GLUED_TOPOLOGY_HUNT_V001.md
(1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c — recomputed at
path, MATCH). **All results here CLAIMED until checked.**

## Lead determination — CLAIMED

**All four deliverable verdicts of the hunt SURVIVE independent attack — the object
list is complete at the sealed-complex-instance level, every H^1 value recomputes
exactly on an independent implementation, the loop trichotomy horn (c) stands for the
question as posed, and no sealed cyclic identification exists anywhere — but the hunt's
sweep is INCOMPLETE as a coverage claim and one flag-block universal is REFUTED at the
bytes: a sealed, executed finite record trace EXISTS in the finite-N CTP lineage
(TASK2D c49d3120: `I_N := Tr_R_N[V_{a+} |R_N><R_N| V_{a-}^dagger]`, flag
`FINITE_KERNEL_COMMON_ORIGIN_TRACE_CERTIFIED = true`) — and the same sealed bytes
reduce that trace to the ready-record matrix element (rank-one state), so it induces
NO cyclic identification and the hunt's consequence reading holds while its universal
negative does not. Separately, the hunt missed the sealed-and-ENTERED Attach lineage
(2026-08-12, one day before the hunt), which supplies the per-cell holonomy reading
(the S1/O11 intra-cell half) as AUTHORED premise of record on connected admitted
b_1 = 1 cells — without gluing any cells, displaying any complex instance, or claiming
the physical domain — so "nothing of record supplies it" is overstated while the horn
survives at derivation strength.**

```text
GATES  alpha_computed = false ; kappa_record_computed = false ;
       proof_authorized = false ; coupling_evaluation_authorized = false.
       No physical quantity computed, bounded, or evaluated.  Every displayed number
       is a structural integer, an exact rank, or a sealed-text quotation.
```

---

## 0. Preflight and seals

Output name probed before any write: `STAGE8_GLUED_TOPOLOGY_CHECK_V001.md` ABSENT
(artifact and both sidecar spellings). No register, tracker, plan, road, ledger, or
lens file read. All verification by `shasum -a 256` at path BEFORE reliance.

Recomputed at path — the hunt's full seal set, all MATCH:

```text
1892c08ec7afb683cec641bff6ac4b42d5ebb0be6313475dbf3549be229c755c  STAGE8_GLUED_TOPOLOGY_HUNT_V001.md          TASKED
38bbb9fc58b93cadf3be37117291fcf4caa7ac4cf9d670207b7b8cbca48af071  STAGE8_AND_CLASS_INDEPENDENCE_V001.md       TASKED (AND)
22a2a478fb9f529692000a61b0f294757976156521922a609ff2ea8ae324ad0b  STAGE8_TRANSPORT_LAW_POSED_V001.md          TASKED (POSED)
aa7c6d4904706276514728819df20f48e8fdca0ff83f97ad5f1724c5f81f108a  review_packets/STAGE7_QSPEC_CANDIDATE_V001/BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md  MANIFEST row :64 of the 113-entry STAGE7_PACKET_MANIFEST_V001.sha256 + TASKED
451550c3825288d699db35c7289e408e8314ad042450253b58f32722c4ead46b  BID_MONOIDAL_EXTENSIVITY_DERIVATION_V001.md MANIFEST row :109; top-level copy verified BYTE-IDENTICAL to packet member (cmp)
3e35ffe2a67ea863b7dfb432567a5937cf92f2f6810bc54cca67eb1598b19a70  STAGE8_G3_REALIZATION_BUILD_V001.md         QUOTED in BOTH anchors (AND :57, POSED :61)
1a96e0954dc440aa616894e23fc932a1a81e5eb5fc75838d01af34f466d49bfb  STAGE8_REQUIRE_BUILD_G3_FINITE_N_DATUM_V001.md  SIDECAR
2cd1ffcefd68ac03c6c09a4eca0dc9fe8d1adc8ac564cc0d050dfd41d79e6d0f  STAGE8_RATIFIED_SOURCE_COUPLED_FINITE_N_INFLUENCE_FUNCTIONAL_RESULT_V001.md  SIDECAR
ddb51c32e1ec45e6145be4c688f2af5fa87823ab20b4f523fc4657823165e544  COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_SPEC_V001.md  SIDECAR
273e1473a1a8bf0be0467634411cec1b7daeee0c9f24c330fad5d288d191dcbb  COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.md  listed in the RESULT's multi-line sidecar (see P-2 below)
e30f2e631204df2416b9aa38e55c2710db1d676749fcd2fbdb6604388f3ea391  COMPLETE_ONE_CELL_CTP_KERNEL_GATE_V001.md   SIDECAR
87f696261651567e04242abc1a54d5a2b457a19e07926e9e9856b02dc1719eb1  STAGE8_TASK4A_NETWORK_SOURCING_LAW_ADOPTION_PROPOSAL_CODEX_LANE2_V001.md  SIDECAR
dfb3f5b20a077db0587ca0cefdccabd04c7e89ddd9ba736f991c0c16914c8b4d  STAGE8_NDIST_COLD_OPUS_V001.md              SIDECAR (DIST digest carried at its N5 row — MATCH)
d7d14f9f1e57b1fcb6458a611dd5cbe034466e697381e14d2361e94e8efe1262  BID_DISTINGUISHABLE_RECORD_CELL_COMPOSITION_DERIVATION_V001.md  QUOTED (NDIST_COLD N5)
8ed95b5a1ebd64d6899b5c6feabdfb17196095226f26ea0b885a0bf6ee10cab3  STAGE8_REQUIRE_G3_CHECK_V001.md             QUOTED in AND :61 ONLY (see P-1 below)
e6bf47f6a7b21c89ae1142e5e0d57d5169d011cb64b3e3b0e906df67bbf0d77e  STAGE8_BRIDGE_ITEM2_COMPLETE_CTP_ACTION_BOUNDARY_TIMEFLOW_SPEC_V001.md  SIDECAR ("specifies a missing object", P1-P3 at :20-:36 — MATCH)
e6fae1428cecffb3c81deb7227c389e4c9d2f2bca3bad63cb9b0f5b03a3ca6ee  STAGE8_EMERGENT_BOUNDARY_WINDING_FABLE_V001.md  SIDECAR (symbol-collision classification spot-confirmed)
20a3a17d44e15841baded9eaed3fdbecfde0ecb14bdb8162ea41a8bcd21d1a48  BOUNDARY_INCIDENCE_DYNAMICS_PRINCIPLE_V011.md (TOP-LEVEL) — DIFFERS from packet member; NOT consumed (T6 honored by hunt AND by this check; matches POSED :73's recorded digest)
```

Additional seals verified at path for THIS check's sweep findings:

```text
c49d31200460e96209375f06a7a655d343767695ef09805f1e396d1814833b6b  STAGE8_TASK2D_FINITE_N_INFLUENCE_FUNCTIONAL_INSTANCE_CONSTRUCTION_RESULT_V001.md  SIDECAR + QUOTED in RAT :109
ad9fc14e1f07494f7527d95f2a94ee7a26da9d49cc784c409f31325f54572213  ATTACH_SUPPLY_MANDATE_DECISION_OF_RECORD_V001.md  SIDECAR (RATIFIED principal decision, 2026-08-12)
420ab02f5ddb56ec8b3b49d3da4937c0045f2e2928fa233177dccda956914c73  ATTACH_ENTRY_ADDENDUM_V001.md  SIDECAR (STATUS: ENTERED premise of record, 2026-08-12)
ccca6bb43a47f3eb5ee8dca1539a65d42ecd46ec1fa2a8f8588128e12ecd00fc  STAGE8_ATTACH_CANDIDATE_PROPOSAL_DARIO_V007.md  SIDECAR (the ENTERED artifact; digest matches the addendum's naming)
5154c203ee237c4dee1b02176f8fa64c84b3ec97e561b720304f1a58219ad29b  STAGE8_ATTACH_CANDIDATE_V007_CHECK_CODEX2_V001.md  recomputed; matches the addendum's quoted certifying digest
5810377123b823e680891a3337855750ac8f362951dca048dad3a78a34c79d79  STAGE8_COMPOSITION_LOOP_STRUCTURAL_PREDICTION_BUILD_V001.md  SIDECAR (2026-08-01)
bf75f3929d739eccd2c0b495f3ec99e3db7564fbc2cdbf78eb503e63d4ee7082  STAGE8_FILLED_TWO_CELL_CARRIER_CENSUS_AND_EXTENSION_DETERMINATION_V001.md  SIDECAR (2026-08-01)
58fd2e60632633786b49053697170224bcac99b9ea28002752f81f5d26bee4bb  STAGE8_TREE_LOOP_VERSUS_ALLOW_REQUIRE_ADJUDICATION_EINSTEIN_V001.md  SIDECAR (2026-08-01)
```

All 30 byte spans of the hunt's §0 table were re-extracted from the sealed bytes and
rehashed by this check's own script: **30/30 MATCH**, and every quotation the hunt
hangs on a span is verbatim in the extracted bytes (including GB S1/S3, the SPEC/RES
withholdings, the K_square vertex/edge display, the K_L definition and frozen
boundary word, the C_ref declaration, the NET adoption banner and Adj_2, the DIST
non-determination, and the GC barred-import span).

---

## 1. ATTACK 1 — COMPLETENESS (independent sweep)

Independent angles run: by-structure (`Adj_`, adjacency, torus, lattice, 2-skeleton,
multi-cell / record-net / chain-of-cells, cellulation, glued), by-name (K_square,
K_L, C_ref, Adj_2, W_N), by-citation-graph (RAT's inventory, V011's prediction
sections, MON/GB/POSED citations), plus ban/license probes (acyclic, loop-free,
tree-of-cells, port identification, "may be identified"). 1,572 top-level .md files
plus the review packets swept by grep; every candidate hit opened and typed.

**Result on the overturn condition: NO missed sealed loop and NO missed sealed net.**
No sealed artifact displays a glued multi-cell record complex instance beyond the
hunt's two (K_square, K_L, both V011, both audit/prediction stratum). The "torus"
hits outside V011 are character/parameter tori (`T^(2N)` point-evaluation domains,
the sequential relative-history torus — continuum parameter spaces, SUSPECT-only,
no incidence structure sealed on them); the "lattice" hits outside V011 are the U(1)
character lattice and ledger filenames (not read); the only adjacency displays are
NET's Adj_2 and its cross-review.

**But the sweep IS incomplete as a coverage claim.** Sealed artifacts bearing
directly on the hunt's questions, all PREDATING the hunt (hunt mtime 2026-08-13
14:24), absent from its source list and disclosures:

```text
M-1  THE ATTACH LINEAGE (the gluing-constructor commissioning the hunt never names):
     the supply-mandate DoR (ad9fc14e, RATIFIED 2026-08-12: the source-history
     Attach map "typed by the record, adjudicated absent", supply MANDATED as
     PROPOSED PHYSICS), candidates V001-V007 (V007 = ccca6bb4), their checks, and
     ATTACH_ENTRY_ADDENDUM_V001 (420ab02f, STATUS: ENTERED — premise of record,
     registrar-sealed, 2026-08-12).  The ENTERED content supplies the per-cell
     holonomy reading: theta_j := contour integral over gamma_j of A, gamma_j THE
     UNIQUE CYCLE CLASS of a connected admitted b_1 = 1 cell; trees (b_1 = 0)
     inert; b_1 > 1 out of scope.  This is the S1/O11 INTRA-cell half, supplied at
     entered-authored-premise strength (15 FORCED rows + 8 AUTHORED, forcing slot
     future_derivation_can_select_the_attach_map = NO_VERDICT open, three carried
     conditions open incl. "no result shows [the operative sector] is the physical
     domain").  It glues NOTHING: W_N := tensor over j of (D_n S_j) remains a
     disjoint tensor row; no port-to-0-cell inter-cell anchoring; no complex
     instance displayed ("no derived cell", V007 §7.3).
M-2  TASK2D (c49d3120, sidecar + quoted in RAT :109): the executed finite record
     trace — see §3.  Reachable from RAT's own citation table; the hunt read RAT
     and did not follow it.
M-3  Corroborating sealed analyses of the SAME objects: the composition-loop build
     (58103771: K_square cycle rank EXACTLY 1; "the loop is only permitted — not
     forced — by the sealed composition law"; K_square_forced_by_general_composition
     = false), the filled-two-cell census (bf75f392: "No derived physical filled
     two-cell was found in the bounded whole-program sweep"; K_L the closest,
     "a frozen test object selected by the specification, not a face forced by
     record composition"), the tree/loop-vs-allow/require adjudication (58fd2e60).
     Each INDEPENDENTLY corroborates the hunt's typings; none was cited.
M-4  A sealed acyclicity constraint the "no loop ban" probe did not surface: the
     causal architecture is typed "locally finite future-directed ACYCLIC"
     (CAUSAL_DIRECT_LIMIT_REDUNDANT_RECORD_SPEC_V001 :24; R3_4_CAUSAL_CELL_MOVING_
     FRONT_SPEC_V001 :17).  This EXCLUDES cyclic successor (causal/temporal) chains
     of cells; it is SILENT on spatial adjacency cycles and on record-complex
     1-cycles (V011 itself seals loop-carrying complexes; the ENTERED Attach
     operates b_1 = 1 cells).  A qualification to horn (a), not a reassignment.
```

SWEEP verdict: **MISSED (M-1..M-4) — but no missed sealed loop or net; the object
list and both stratum verdicts survive.** M-1..M-3 all CORROBORATE the hunt's
structural findings; M-2 refutes one flag universal (§3); M-4 qualifies one horn
claim (§4).

---

## 2. ATTACK 2 — TYPING (independent recomputation)

Independent implementation (different pivot orders — max-column and right-to-left —
different primes 65521 / 999983 / 1000000007, own Fraction RREF, own integer
certificates; script `h1_recheck.py`, exit 0):

```text
K_square   my incidence matrix from the sealed vertex/edge list == the sealed
           D_square at u = 1 (entrywise) : True
           rank d_0 = 3 (Fraction AND 3 primes); d_1 = 0 (no 2-cell)
           H^1 = 4 - 3 - 0 = 1  EXACT                      -> hunt CONFIRMED
K_3        census 81/324/486 == sealed law L^4/4L^4/6L^4 : True ; d_1.d_0 = 0 : True
           rank_p d_0 = 80 = V-1 (all primes); rank_p d_1 = 240 (all primes)
           integer certificate H^1 >= 4 (direction cocycles, loop pairing L*I_4,
           coboundary-telescoping) : True ; Fraction instrument: 324-240-80 = 4
           H^1 = 4  EXACT                                   -> hunt CONFIRMED
K_4        census 256/1024/1536 : True ; d_1.d_0 = 0 : True
           rank_p d_0 = 255 = V-1; rank_p d_1 = 765; certificate True
           H^1 = 4  EXACT                                   -> hunt CONFIRMED
two-node   Adj_2 = [[0,1],[1,0]] (sealed span): tree, H^1 = 1-2+1 = 0  -> CONFIRMED
finite-N   RAT spans 440562a2/386177bb re-read: W_N is a tensor row, no successor
           map, no inter-cell incidence displayed; H^1 = 0 trivially  -> CONFIRMED
C_ref      span ee11aa62 re-read: class declaration, no member displayed;
           NOT COMPUTABLE                                    -> CONFIRMED
working    AND 38bbb9fc carried (its own two-instrument certification); binding
class      and §7 reopening language verified at the file    -> CARRIED
```

Exactness note (same logic, independently executed): rank_p <= rank_Q always;
constants in ker d_0 give rank_Q d_0 <= V-1, met mod p, forcing equality; the
integer H^1 >= 4 certificate forces rank_Q d_1 <= E-(V-1)-4, met mod p, forcing
equality — both bounds close, no probabilistic step. The independent known-math
cross-light (2-skeletons carry the full H^1 of the 4-torus) agrees and was used as
sanity only, never as authority.

**TYPING_CORRECT = CONFIRMED, all rows.**

---

## 3. ATTACK 3 — THE TRACE SUB-QUESTION (both directions)

Missed-trace attack: every occurrence of "trace" in the CTP stack was enumerated.
SPEC :44 / :101 and RES :20 carry the three withholdings VERBATIM (hunt T-1
confirmed). KG :70/:75/:95 — "trace-preserving" is the diagonal-normalization
PROPERTY, no operation performed (hunt T-4 confirmed). But RAT itself carries
`CHECK_C7_TRACE_N1_TO_N2 = PASS`, `C7_ZERO_EXTENSION_TRACE PASS`, "the sealed
one-cell transition and Q-227 trace baseline", and its :109 inventory row names
TASK2D: "Executed zero-source finite trace". At TASK2D (c49d3120, sealed), the
bytes display:

```text
I_N(a_+,a_-) := Tr_R_N[ V_(a_+)^(N) |R_N><R_N| (V_(a_-)^(N))^dagger ]
             =  <R_N| (V_(a_-)^(N))^dagger V_(a_+)^(N) |R_N>.
"This trace is canonical on the finite matrix algebra R_N; it does not import a
trace onto the ratified Hilbert C-star-module C0_008."
FINITE_KERNEL_COMMON_ORIGIN_TRACE_CERTIFIED = true
FULL_C0_PATH_COMMON_ORIGIN_CERTIFIED = false | TYPE-U
```

**Findings:**

```text
T-A  REFUTED AS WORDED: "no sealed trace exists anywhere in the CTP stack" is
     FALSE at these bytes — a sealed, executed finite record trace exists in the
     finite-N CTP lineage, and a finite-kernel COMMON-ORIGIN trace is CERTIFIED
     true of record.  The hunt's "the ONE named trace ... NOT SUPPLIED" is also
     imprecise: TWO common-origin trace objects are named of record — the
     finite-kernel one (SUPPLIED, certified) and the full-C0-path/DynPort_U2_008
     one (NOT supplied; RAT §7.1 span a934fd02 — the hunt is right about THAT one,
     and TASK2D's own false flag on the path-level object agrees).
T-B  CONFIRMED IN CONSEQUENCE, at the same bytes: the sealed display ITSELF
     reduces the trace to the ready-record matrix element — a trace against the
     RANK-ONE pure projector |R_N><R_N| IS the vector-state closure, algebraically
     identical to RAT's "record matrix element" (span 386177bb).  It identifies
     nothing at the complex level, induces NO cyclic identification, and caps the
     two branches at the common ready-record origin — exactly the contractible
     state-closure the hunt describes.  The N cells remain disjoint tensor
     factors.  No sealed cyclic/normalized trace over the record algebra exists;
     the sealed bytes still withhold "finite normalized trace" three times.
T-C  The slogan "a sealed trace = a sealed loop: the corpus has NEITHER" is
     doubly wrong as stated: the corpus HAS a sealed trace (T-A), and that trace
     seals NO loop (a pure-state trace is not a cyclic closure) — the implication
     itself fails in the rank-one case.  The operative conclusion — no sealed
     algebraic loop-closure of any cell chain — remains TRUE.
T-D  Also unreported by the hunt: V011's own composition-loop protocol REQUIRES
     reporting Tr[B_square^2], Tr[B_square^4], and their ratio over the
     8-dimensional carrier C_0(K_square) (+) C_1(K_square) — a sealed
     spectral-trace REQUIREMENT sitting on exactly the loop-carrying audit
     object.  It is a finite matrix trace of an operator on an already-glued
     complex, not a history closure; it moves no verdict, and it is consistent
     with (indeed illustrates) trace-and-loop cohabiting ONLY at the audit
     stratum.
```

**TRACE_READING = REFUTED as universal (the bytes above) / CONFIRMED in its
complex-level consequence — TRACE_CLOSURE's operative content (state-closure,
contractible, no cyclic identification, path-level common-origin object absent)
stands.**

---

## 4. ATTACK 4 — THE TRICHOTOMY

Horn (a) — forbidden? My independent ban probe agrees with the hunt for SPATIAL
loops: no sealed text forbids loops of cells; GC (span 8e1f821b, rehashed,
hash-identical to AND's extraction) bars only the contractibility IMPORT; AND §7
names dim H > 0 as the live reopening; V011 seals two loop-carrying complexes; the
composition-loop build (M-3) states the loop is "permitted — not forced".
QUALIFICATION (M-4): the sealed causal architecture is "locally finite
future-directed acyclic" — cyclic SUCCESSOR (causal) chains are excluded of
record; this is a real sealed constraint the hunt's "corpus-wide probe finds no
loop ban" fails to report, but it does not ban the H^1-relevant spatial/record
1-cycles. Horn (a) still cannot be occupied for the posed question.

Horn (b) — permitted-at-law-strength? CONFIRMED: no sealed clause licenses
identifying any port with any 0-cell or other port. The ENTERED Attach's E_j maps
identify per-cell carrier endpoints with source-fiber VECTORS (|r>, |p_Q>) — typed
in its own check as "authored identifications", per-cell, never port-to-port or
port-to-0-cell across cells. NET remains PROPOSED_NOT_ADOPTED. Nothing affirmative
to cite; horn (b) stays empty at law strength.

Horn (c) — the verdict: **CONFIRMED for the question as posed** (the inter-cell
loop-of-cells constructor is genuinely unspecified: no ban, no license, connected
composition open, O11/O12 UNBUILT as a DERIVED law — POSED's own "nothing above is
a ratified discharge" survives the entry because the entry is authored physics, not
a derivation). **CORRECTION to the hunt's supporting characterization:** "nothing
of record supplies it" and the citation of GB S1 as the current state are STALE as
of 2026-08-12 — the ENTERED Attach supplies the intra-cell holonomy-reading half
(which edges: the unique cycle class gamma_j of the b_1 = 1 cell) at
entered-authored-premise strength, on a typed domain, with no built instance. What
remains unsupplied of record: any DERIVATION of the constructor; the INTER-cell
successor/anchoring (O12 across cells); a built b_1 = 1 physical cell instance; the
connected composition (MON Thm 3, five conditions); the path-level common-origin
object. The gap is real; its boundary moved one intra-cell half-step the hunt did
not see.

Consequence for H1_POSITIVE_CARRIER: NONE_SEALED at the physical write-carrier
stratum SURVIVES on the strict reading — no complex instance with H^1 > 0 is sealed
as a physical carrier, and the Attach entry itself carries the open condition "no
result shows [the operative sector] is the physical domain". SHARPENING the hunt
missed: the ENTERED operative domain is typed "connected admitted b_1 = 1 cells"
(with trees inert and no derived physical filled two-cell of record, per M-3) — the
record's entered premise now OPERATES on loop-carrying cells as a typed class, which
tightens "construction gap, not formalism gap" further than the hunt stated.

**TRICHOTOMY = CONFIRMED (horn (c)), with the O11-half staleness correction and
the M-4 causal-acyclicity qualification recorded.**

---

## 5. ATTACK 5 — PROVENANCE + INJECTION

```text
P-1  MINOR ERROR: the hunt's seal table marks GC (8ed95b5a) "QUOTED (both
     anchors)".  FALSE for POSED: zero occurrences of the digest or filename in
     STAGE8_TRANSPORT_LAW_POSED_V001.md.  AND :61 quotes it; the path rehash
     matches; reliance is unaffected.  Attachment-note error only.
P-2  MINOR LABEL IMPRECISION: RES (273e1473) is listed in
     COMPLETE_QSPEC_RELATIVE_HISTORY_CTP_AMPLITUDE_RESULT_V001.seal.sha256 — the
     RESULT's multi-line sidecar — not "SPEC's sidecar" (SPEC's own sidecar is
     single-line, SPEC only).  The attachment EXISTS and matches; label wrong.
P-3  CONFIRMED CLEAN: manifest is the 113-entry STAGE7_PACKET_MANIFEST_V001.sha256
     with V011 at row :64 and MON at row :109; MON top-level is byte-identical to
     the packet member (cmp); top-level V011 differs (20a3a17d, = POSED :73's
     record) and was not consumed by hunt or check; the WINDING sidecar matches
     and its "K_L" is the face-response kernel (symbol collision, correctly
     classified); NDIST_COLD carries DIST's digest at its N5 row; BRIDGE-ITEM2
     opens "specifies a missing object" with P1-P3 as cited; the GB/FN nickname
     collision is resolved correctly (AND's "GB" = the FINITE_N datum, POSED's
     "GB" = the G3 realization build — both verified in the anchors' own tables).
P-4  30/30 spans rehash; every load-bearing quotation verbatim.  The hunt's script
     output reproduces on independent reimplementation (§2).
INJECTION: none found.  All consumed content is sealed-text quotation; no
     instruction-bearing text was followed as authority; no source contradicts its
     quoted use; the two anchors' digests match the tasking's.
```

---

## 6. Fences and audit

```text
DETERMINATION/TYPING ONLY.  Exact structural algebra on sealed integer data (own
  implementation: mod-p ranks at 65521/999983/1000000007 with rational sandwiches,
  Fraction-exact RREF, integer cocycle/pairing certificates).  No floats.  No
  physical quantity computed, bounded, or evaluated; no scale, no imported GR, no
  faithfulness.  Continuum objects (R^4 exhaustion, character tori T^(2N), C_ref's
  smooth constituents, causal-architecture typings) read SUSPECT-ONLY to classify.
  No continuum contractibility imported; every topology statement rests on a sealed
  combinatorial presentation.
NO register / tracker / plan / road / ledger / lens file read (ledger FILENAMES seen
  in directory listings only; none opened).  No git action.  Output name probed
  ABSENT before write.  Nothing registered, committed, or pushed by this lane.
HONEST OUTCOMES: the check found confirmations (typing, horns, consequence), a
  refutation (the trace universal), staleness (the Attach entry), and coverage
  gaps (M-1..M-4) — no outcome was pre-assigned; REFUTE was the default and the
  deliverables survived it.
```

---

## FLAG BLOCK

```text
SWEEP_COMPLETE = MISSED(the ATTACH lineage — supply-mandate DoR ad9fc14e RATIFIED
  2026-08-12, candidates V001-V007 (V007 ccca6bb4), checks (5154c203), and the
  ENTRY ADDENDUM 420ab02f ENTERED as premise of record 2026-08-12 — plus TASK2D
  c49d3120 (reachable from RAT :109), the composition-loop build 58103771, the
  filled-two-cell census bf75f392, the tree/loop adjudication 58fd2e60, and the
  sealed future-directed-acyclic causal-architecture typing.  HOWEVER: no missed
  sealed LOOP and no missed sealed NET — no sealed glued multi-cell complex
  instance exists beyond K_square and K_L, so the hunt's object list and both
  stratum verdicts SURVIVE; the missed artifacts corroborate the typings they
  touch.)
TYPING_CORRECT = CONFIRMED(all rows, independent implementation, different primes
  and pivot orders: K_square incidence == sealed D_square(u=1), H^1 = 1 exact;
  K_3 census 81/324/486 and K_4 census 256/1024/1536 match the sealed law,
  d_1 d_0 = 0, H^1 = 4 exact both, sandwich + integer certificate + Fraction
  instrument; two-node tree H^1 = 0; finite-N tensor row empty inter-cell
  incidence, H^1 = 0; C_ref not computable — class declaration only.)
TRACE_READING = REFUTED(the universal "no sealed trace exists anywhere in the CTP
  stack" — at TASK2D c49d3120 the sealed bytes display the executed finite record
  trace I_N := Tr_R_N[V_{a+} |R_N><R_N| V_{a-}^dagger] with
  FINITE_KERNEL_COMMON_ORIGIN_TRACE_CERTIFIED = true, visible from RAT's own :109
  row and C7 TRACE check lines; "the ONE named trace" is also wrong — two are
  named, the finite-kernel one SUPPLIED, the DynPort_U2_008 path-level one NOT) ;
  CONFIRMED(in consequence, at the same bytes: the sealed display itself reduces
  that trace to the ready-record matrix element — rank-one pure-state closure,
  contractible, NO cyclic identification, cells stay disjoint tensor factors; the
  three "finite normalized trace" withholdings are verbatim; KG's
  "trace-preserving" is the diagonal-normalization property; the path-level
  common-origin object remains uncertified (FULL_C0_PATH_COMMON_ORIGIN_CERTIFIED
  = false); so TRACE_CLOSURE's operative content — no trace-induced cyclic
  identification anywhere, no sealed loop-closure of any cell chain — STANDS.)
TRICHOTOMY = CONFIRMED(horn (c) GENUINELY-UNSPECIFIED for the inter-cell
  loop-of-cells constructor, at derivation strength — POSED's "nothing above is a
  ratified discharge" survives; horn (a) still unoccupiable for spatial/record
  loops, horn (b) still has no citable license.  TWO corrections of record:
  (i) "nothing of record supplies it" is STALE — the ENTERED Attach (420ab02f,
  2026-08-12) supplies the intra-cell holonomy-reading half (theta_j on gamma_j,
  the unique cycle class of a connected admitted b_1 = 1 cell) as AUTHORED premise
  of record, gluing nothing, displaying no instance, claiming no physical domain;
  (ii) horn (a) carries a sealed qualification — the causal architecture is typed
  future-directed ACYCLIC, excluding cyclic successor chains while silent on the
  H^1-relevant spatial 1-cycles.)
HUNT_VERDICT = SOUND(all four deliverables survive attack: the object list is
  complete at the sealed-complex-instance level, every H^1 exact, horn (c) stands,
  physical-stratum H1-positive carrier NONE_SEALED stands, no sealed cyclic
  identification exists — WITH three named defects that do not reach any verdict:
  the refuted trace universal (TRACE_READING), the stale "nothing supplies it"
  (TRICHOTOMY (i)), and the sweep-coverage misses (SWEEP_COMPLETE), plus two
  attachment-note errors (GC "both anchors" — AND only; RES "SPEC's sidecar" —
  the RESULT's multi-line sidecar).)
PROVENANCE = CLEAN(all 16 hunt-cited files + 8 check-found files recomputed at
  path before reliance; 30/30 spans rehashed; manifest rows :64/:109 of the
  113-entry packet manifest verified; MON top-level byte-identical to packet
  member; top-level V011 (20a3a17d) differs and was consumed by no one; the two
  minor attachment-note errors above are labeling errors, not integrity breaks.)
INJECTION = none
MACHINERY_USED_BY_ME = no(reads, shasum -a 256 at path, byte-span extraction and
  rehashing, corpus-location greps, and one exact integer/rational script of my
  own; no numeric evaluation of any physical quantity, no member binding, no
  end test, no comparison to any measured constant; no register/tracker/plan/
  road/ledger/lens read; no git action; nothing registered, committed, or pushed.)
alpha_computed=false, kappa_record_computed=false, proof_authorized=false
ALL_RESULTS = CLAIMED until checked.
```
