# Fundamental Boundary Record Action Principle v002

Date: 2026-07-23

## Why v002 exists

Version 001 mixed two logically different claims:

```text
the pointwise active relative-phase stabilizer is derived;
a smooth local charged connection is part of the microscopic theory.
```

The first claim follows from the ordered two-endpoint carrier. The second does
not follow from the common-phase redundancy of a projective lift. Version 002
therefore makes the field-content choice explicit instead of reporting it as a
derived connection theorem.

Version 001 and `LOCAL_PROJECTIVE_RECORD_BUNDLE_V001.md` remain provenance
records, but they are not authority for a physical charged connection.

## Logical status

This is a target-value-free, target-aware, forward-sealed Level-1 Gravacle
principle. It is an adopted microscopic theory premise, not a consequence of
the sealed pre-alpha results.

The project historically knows the electromagnetic target. Consequently,
even a complete forward derivation under this principle requires a genuinely
unused prediction before it can defeat the hindsight objection.

## Derived kinematic input

For the declared ordered two-endpoint comparison context, the derived
pointwise active stabilizer is

```text
U(1)_rel
```

with integer character lattice and primitive unit winding. This statement is
pointwise. It does not itself localize the symmetry or produce a connection.

The common phase of a local ray lift is representational redundancy and is not
identified with `U(1)_rel`.

## Adopted microscopic field content

The ordinary charged-record branch contains a smooth principal
`U(1)_rel` bundle and an auxiliary compact connection `a` on that bundle.
This localization and connection are adopted Level-1 field content.

They are not derived from:

```text
the common phase of a projective lift;
a passive basis rephasing;
the demand to reproduce electromagnetism;
or the measured value of alpha.
```

The primitive unit character fixes the normalization of holonomy:

```text
Hol_gamma(a) = exp(i integral_gamma a).
```

No additional charge-normalization factor may be inserted later.

## Fundamental record degrees

Durable records are generated by physical boundary degrees of freedom. On
every admissible primitive causal record cell, the one-handle record carrier
reduces to the previously derived two-endpoint projective carrier.

The local record degree and the active charged comparison field are distinct:

```text
the record carrier supplies the compared alternatives;
the connection transports their active relative phase.
```

## One complete specification

Gravity, the adopted charged comparison field, charged sources, and record
closure belong to one complete quantum specification

```text
Q_spec[g, a, psi, R].
```

`Q_spec` must fix fields, action, state, contour, domain, boundaries,
gauge/ghost structure, edge modes, measure, regulator, record operator,
closure map, branch enumeration, and matching rule. No sector may receive an
independent normalization after `Q_spec` is sealed.

## Branch inputs

The ordinary public branch is `3+1` dimensional, Lorentzian,
diffeomorphism-covariant, CPT-compatible, and local at the primitive causal
cell before coarse graining. These are disclosed inputs, not outputs.

## Unique record cell

When a primitive durable record forms, the boundary law selects one physical
Lorentz-covariant interval, support region, and closure configuration over
which its action is accumulated.

An explicit causal-cell construction must realize this postulate before any
response is evaluated. A cell shape, duration, density, orientation measure,
or cutoff chosen after seeing a coupling is forbidden.

## Parameter-free microscopic action

After dimensionful units are fixed by disclosed constants and the derived
record cell, `Q_spec` contains no adjustable dimensionless coupling,
multiplicity, kernel coefficient, finite subtraction, or response
normalization. Discrete data must be derived from the carrier, source branch,
and causal-cell construction.

## Compositeness condition

At the microscopic scale and prescription ultimately fixed by `Q_spec`, the
auxiliary connection has no independent bare parity-even curvature stiffness:

```text
K_bare[Q_spec, regulator, causal-cell scale] = 0.
```

This is an adopted compositeness condition, not a result of compactness,
projective geometry, or gauge covariance. Public Maxwell dynamics must be
generated by the same record action.

The route fails to compute the coupling if the completed specification admits
an arbitrary finite deformation

```text
delta S = -(hbar c/4) integral F wedge *F
```

at the matching scale.

## Forward-seal prohibitions

After this file is sealed:

```text
no action term may be added because a coupling residual remains;
no cell or multiplicity may be chosen by target comparison;
no operator may be selected because it gives the desired response;
no endpoint or cosmological value may normalize the microscopic action;
no measured coupling may enter construction or branch selection;
no script may certify a physical premise that it merely encodes.
```

Any new premise requires a new version, an explicit provenance break, and a
fresh seal before evaluation.

## Authorization state

```text
pointwise_active_relative_U1_derived = true
common_ray_lift_U1_identified_with_charged_U1 = false
smooth_principal_relative_U1_bundle_adopted = true
auxiliary_compact_connection_adopted = true
physical_public_EM_connection_derived = false
K_bare_zero_adopted_as_compositeness_condition = true
unique_causal_record_cell_derived = false
complete_parameter_free_Q_spec_frozen = false
finite_c_F2_deformation_excluded = false
unique_finite_response_derived = false
coupling_evaluation_authorized = false
alpha_computed = false
proof_authorized = false
```

