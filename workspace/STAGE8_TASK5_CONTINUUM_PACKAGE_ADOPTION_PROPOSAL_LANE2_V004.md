# STAGE 8 TASK 5 — CONTINUUM PACKAGE ADOPTION PROPOSAL — LANE 2 V004

Date: 2026-08-04  
Task: PASTE 496 / Task 5 / continuum package V004  
Lane: Codex Lane 2  
Custody: builder repair after Q-417  
Standing: **PROPOSED_NOT_ADOPTED**; DoR-020 reserved

## Lead result

```text
CONTINUUM_PACKAGE = PROPOSED_NOT_ADOPTED

SQUARE = DERIVED |
  Ref_path carries the physical path-current and Q-408 bilocal-kernel
  restriction squares on every rank-preserving subdivision of already
  realized paths.

ROUTE_A_BOUNDARY =
  NEW_INTERIOR_PL_EDGE_CELL_PHYSICAL_REALIZATION_AND_GEOMETRY / TYPE-U |
  physical paths for genuinely new PL edges, plus their coframe, density,
  Riesz, Hodge, and cycle-creating data.  Existing-path subdivision paths,
  the identity test-form map, the current square, and its rank-preserving
  kernel square are no longer authored fields.

ROUTE_B = BOUND(P_CM020) |
  the computation may run only on the named carrier-minimal package member
  P_CM020 with its proof certificate c_CM020 after DoR-020 ratification.
  Existential H_020 and free-P notation are removed from the execution path.

RIGIDITY = NOT_DERIVED |
  the ratified constraints retain completed-representative, reader, and
  K-action freedoms and contain no theorem making the sealed consequence
  constant across all admissible members.

ROUTE_SELECTED = none
READY_FOR_FINAL_CHECK = yes
```

The end test is now bound and failure-capable: a mismatch refutes
`P_CM020` and therefore the **bound Route-B model** proposed for DoR-020.
It does not refute every member of the pre-gate family.  Family-level
refutation would require a rigidity/uniformity theorem, an all-member
failure theorem, or a proof that the admissible fiber is empty; none is
claimed here.

```text
alpha_computed = false
proof_authorized = false
kappa_record_computed = false
numeric_evaluation = false
registered_verdict_written = false
measured_constant_compared = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```

---

## 0. Preflight, authorities, and custody

### 0.1 Three-line preflight

The locked process was read in full.  The register head is Q-417.  The
Q-417 cross-check and V003 were hash-verified, including their sidecars,
before either was consumed.

```text
DOES_THE_OBJECT_EXIST = yes | V003 exists and Q-417 names two bounded repairs
IS_THE_VERSION_CURRENT = yes | through register head Q-417
ARE_ITS_INPUTS_PRESENT = yes | V003, Q-417 check, Q-408, Q-411, DoR-016--019
PREFLIGHT = PASS
```

| Authority | Verified SHA-256 | Use |
|---|---|---|
| locked process | `e8a4c00d3cd13126bd8d20588419aba344a50d27c6d084a8243ed5494d7721f2` | custody and fences |
| questions-settled register at Q-417 | `85c4c7f8c2debf338ff2750c2f34f3740df086287cd172bf6da42cfafef4268a` | current scope |
| V003 | `ee69fd1cb2a519d44d15520f838d243aff4fe734f6a120dda7523cffe751b938` | frozen package clauses and two-route baseline |
| V003 final cross-check | `7465d6f42dcdee027c38cf89e5163e8ada63cfa69f326b794ea03ad7f02066bd` | derived square and Route-B quantifier defects |
| Q-408 kernel build | `ccb94dfa8927cf8d2ec76cf85ff2f402d02d5aa5673b9d39c214a6de4c92309c` | exact physical path-current/bilocal kernel |
| Q-411 constraint system | `d7dcbc3ad7c470c8aaf0d8407db625d3e910cf180cd7b035fbc96bdfe58471b4` | J1--J15 equalizer |
| Q-415 hostile check | `510ebda9c05220b3eb229e865a0a41868f5ce166ff2a5805762d540309a6ab1b` | permanent witness regressions |
| DoR-016 | `b4157df6f327e261f40389d5a3011a0aef66ee0f198d8ebba8b1b9303142d708` | doubled CTP access and batching |
| DoR-017 | `ee9d81bf78ab0ac9361cad5b48dde2b6b8b1e9fb0e28a4fdbba41e1c43db3e45` | physical action/2PI square and member tagging |
| DoR-018 | `57532849d0766741a365d001596275ef4f2ca612063ef6d05031292711a0614d` | Shape K and symbolic `nu` |
| DoR-019 | `6ab72b0cb3a93e123eb1d3c5088fc83361d86c6dc739f0a886380dbd2d143f1f` | physical carrier metrics, units, and Riesz maps |
| DoR-008 | `d51a6d5c5bb0020a081cfd2adfb545b9f5ed86ce660d3feacadc5ef68140fb19` | finite falsifier |

### 0.2 Register sweep

```text
Q-408  exact finite physical current and bilocal distribution maps;
Q-409  C1--C3 conditional on physical refinement/completion;
Q-410  full geometric refinement and uniform physical estimates absent;
Q-411  J1--J15 define the joint equalizer, not its inhabitance;
Q-415  abstract F_PLDEC witness killed; PL core survives;
Q-416  V003 physical-witness and conditional routes;
Q-417  rank-preserving Ref_path square derivable; Route B must bind one P.
```

### 0.3 Bearing symbol distinctions

```text
Ref_path    = subdivisions of already realized Q-408 paths;
Ref_PL      = full edgewise/order-complex category, including new edges;
S_nm        = rank-preserving current inclusion on Ref_path;
S_nm^*      = its lawful adjoint pullback, not naive truncation;
P_CM020     = one named proposed package member;
c_CM020     = its full J1--J15 and clause certificate;
ContAdm_020 = the pre-gate admissible family;
N_020(P)    = the future sealed consequence of a certified P;
nu          = symbolic member scale, not a package-binding score;
chi_K,T     = coordinates of P_CM020's reader, not separately selected inputs;
```

---

## 1. Frozen V003 package core

V004 retains V003's eight-clause object

```text
P=(
  P1 Sec_base_or_Fam,
  P2 Reader,
  P3 KAction,
  P4 RefGeom,
  P5 KerTopologyAndBounds,
  P6 CompletedFaithfulness,
  P7 ResponseBoundaryNaturality,
  P8 LocalWardContactHodge
),

ContAdm_020=Eq(J1,...,J15) subset F_raw.          (F-1)
```

The live clause contracts remain:

| Clause | Frozen contract |
|---|---|
| P1 | completed physical representative in `Adm_base`, with covariance and both restriction cubes |
| P2 | one normalized physical reader factoring through the actual completed physical kernel |
| P3 | anchored scalar-K action map, finite-flat and R4-unit correct |
| P4 | record-side refinement, common refinement, physical realization, coframe/density, connection/curvature transport |
| P5 | one complete physical kernel topology with uniform actual-kernel estimates |
| P6 | faithful completion or the disclosed physical-kernel quotient |
| P7 | response naturality and boundary/contact cocycle |
| P8 | Ward/contact/Hodge/local-symbol package with domains and normalization |

The three repaired equalizer seams remain exactly:

```text
ell_G
 =p_loc,G
 =pi_Mx,G compose Loc_G compose Kernbar_G compose Q_G,

pi_Mx,G(Loc_G(Kernbar_G(Q_G(L_T,G))))=1;          (F-J2)

A_(I,h),G(K_0)=Gamma_(I,G),
h_G(K_0)=0,
rho_Gamma,N A_(I,h),M(K)=A_(I,h),N(K);            (F-J5)

beta_(S compose R)=beta_S+eta_resp,S(beta_R),
beta_id=0,                                         (F-J13)

beta_R/Vol_R -> 0 in tau_ker
on every admitted cofinal family.                  (F-J13b)
```

No V003 clause is promoted from proposal to premise.  No equation is added
between `nu`, `(r_0,r_ch)`, `(chi_K,T)`, the geometry scale, or the kernel
bounds.  DoR-019's R4 seam remains the only lawful C/K unit conversion.

### 1.1 Retained package-scale choice table

The following V003 alternatives and voids remain normative.  The Route-B
binding in Section 3 chooses a member only through DoR-020; it does not erase
the alternatives against which that gate-grade choice is judged.

| Clause | Live alternatives | Carrier-structural minimum | Void conditions |
|---|---|---|---|
| global mode | one covariant tuple; a full family with a uniform/family-valued consumer; reject | a consumer receives either a bound member or an honestly family-valued result | averaging, posterior selection, or an unbound member-sensitive scalar |
| P1 representative | natural section of `Adm_base`; full family with uniform rule; reject | supplies the completed representative absent from finite data | output-selected section, failed R1-COV, or source/action relabeling |
| P2 reader | one natural `(chi_K,T)` member induced by `(F-J2)`; full induced family; reject | supplies the normalized physical scalar dual | separate `ell/p_loc`, result-selected coordinate, or failed relation annihilation |
| P3 K action | anchored constant, affine, regular target-blind class, family, reject | supplies the otherwise absent scalar-K action port | failed anchor, output-selected `h`, undeclared units, or changed finite jet |
| P4 refinement | target-independent physical cellular/refinement family; proved equivalent record-side family; reject | supplies DoR-007's geometric arrows | smooth target as source, selected filtration, or renamed combinatorics without physical realization |
| P5 topology/bounds | refinement-derived local current topology; covariant conormal/microlocal topology; reject | supplies C1's complete topology and actual-kernel estimates | response graph norm, topology switch, moving tail, or member-dependent unbounded constants |
| P6 faithfulness | lower-bound faithful route; disclosed covariant kernel quotient; reject | decides the completed representation kernel | finite injectivity promotion, record-visible deletion, or unrecomputed reader |
| P7 boundary/naturality | quotient contact ideal with `(F-J13)`; invariant direct-sum ideal with `(F-J13)`; reject | supplies C2 response/common-refinement and boundary theorem | failed cocycle, hidden boundary term, or unproved response pullback |
| P8 local symbol | covariant microlocal route; family-wide spectral route with domains; reject | supplies C3's Ward/Hodge/local-symbol interface | frame/slice/domain selection, Ward-called-locality, or target-tuned normalization |

For every admitted automorphism `sigma`, a live tuple obeys

```text
sigma_* P_i(G)=P_i(sigma G), i=1,...,8.           (F-2)
```

Semilinear reality and orientation signs act covariantly, not as falsely
trivial invariances.  The package coordinates remain independent:

```text
NO_EQUATION(nu,chi_K)=true;
NO_EQUATION(geometry_scale,nu)=true;
NO_EQUATION(C_sep,chi_K)=true;
NO_IMPLICIT_CROSS_SECTOR_UNIT=true.               (F-3)
```

### 1.2 Retained composition and restriction obligations

For any certified member, P4/P5/P6 supply C1 completion and its explicit
kernel branch; P4/P7 supply C2 common-refinement stitching; P5/P7/P8 supply
C3 Ward/contact/Hodge/Maxwell structure; P2 is then the same normalized
physical pullback required by `(F-J2)`.  This is a theorem schema on a
certified member, not inhabitance.

Every certified member must also satisfy:

```text
rho_Gamma,N(P1)=the independent sealed finite action leg;
P3 completion directions have their declared zero/flat finite jets;
Res_N J_ref and Res_N Kernbar equal Q-408's physical finite maps;
P5 completion changes no finite value;
P6 quotient intersects every record-visible finite image trivially;
P7 commutes with rho_Gamma,N and rho_H,N and reproduces all finite zeros;
P8's Ward/contact/Hodge quotient changes no sealed finite restriction;
P2 is induced by the same physical map, never a stagewise replacement;
one-edge, tree, pendant, reciprocal-loop, S8-A, reality, batching, and
identity-zero-extension regressions pass.             (F-4)
```

Failure of any row in `(F-4)` voids the whole bound member.

---

## 2. M1 — derived rank-preserving physical path-subdivision square

### 2.1 The physical subcategory

Let `Ref_path` have the actual Q-408 carriers as objects.  A morphism
`r_nm:G_n->G_m` divides each already realized oriented path into an ordered
finite set of children and introduces no independent cycle direction.
For a `q`-fold subdivision of

```text
gamma_e:[0,1]->M,
```

define, using only its existing parameterization,

```text
gamma_(e,j)(t)=gamma_e((j-1+t)/q),
j=1,...,q.                                         (M1-1)
```

This is parameter restriction, not a new target-space path, scale, frame,
or cellulation choice.  Concatenation gives

```text
gamma_e=gamma_(e,1) star ... star gamma_(e,q),

sum_j integral_(gamma_(e,j)) a
 =integral_(gamma_e) a                             (M1-2)
```

for every already admitted physical test one-form `a`.

### 2.2 Current square

Let `S_nm:K_n->K_m` copy a coarse oriented current coefficient to its
oriented children, with the orientation sign carried by the child.  Let the
test-form map be

```text
P_nm=id_T.                                         (M1-3)
```

The Q-408 physical current functional obeys

```text
J_(S_nm c)^m(P_nm a)=J_c^n(a).                    (M1-4)
```

Writing `A_n:T_n->K_n^*` for the analysis map, `(M1-4)` is exactly

```text
A_n=S_nm^* A_m P_nm.                               (M1-5)
```

The arrow is rank-preserving, so `S_nm` identifies the complete conserved
cycle carrier of the coarse path with that of the subdivided path.  DoR-019
then gives on this image

```text
S_nm^* R_(K,m) S_nm=R_(K,n).                      (M1-6)
```

No W3 arrow is reversed: `S_nm^*` is the adjoint restriction required by
the ratified precision.

### 2.3 Bilocal-kernel square

For a coarse bilinear profile `H_n`, define its rank-preserving transport
`j_nm^H H_n` by

```text
(j_nm^H H_n)(S_nm x,S_nm y)=H_n(x,y).             (M1-7)
```

Because `S_nm` is an isomorphism of the conserved carrier in this scope,
`(M1-7)` is representative-independent.  With

```text
Kern_n^fin(H)[a,b]
 =H(R_(K,n)^(-1)A_n a,R_(K,n)^(-1)A_n b),         (M1-8)
```

substitution of `(M1-3)`--`(M1-7)` yields

```text
Kern_m^fin(j_nm^H H_n)[P_nm a,P_nm b]
 =Kern_n^fin(H_n)[a,b].                            (M1-9)
```

Thus the physical restriction diagram commutes:

```text
Prof_n  --j_nm^H--> Prof_m
  | Kern_n^fin         | Kern_m^fin
  v                    v
Biloc(T_n) <--P_nm^*-- Biloc(T_m),                (M1-10)
```

with `P_nm=id` on the same physical test-form space.

### 2.4 Certificates

**Composition.**  Nested parameter restrictions satisfy
`gamma_(e,j,k)=gamma_e` restricted to the composed subinterval.  Therefore
`S_nl=S_ml S_nm`, `P_nl=P_ml P_nm=id`, and both squares compose.

**Automorphism covariance.**  Edge relabeling permutes parent and child
labels on both sides.  Orientation reversal reverses child order and applies
the same sign to `S_nm` and the line integral.  Equations `(M1-4)` and
`(M1-9)` are unchanged as covariant equations; no orientation is selected.

**Reality.**  Conjugation commutes with path restriction and line
integration; the ratified semilinear action is carried through `(M1-8)`.

**Batching.**  Disjoint components subdivide componentwise.  Direct sums of
`S_nm`, `A_n`, and `H_n` reproduce the ordered DoR-016 batching law.

**Zero extension.**  An identity cell contributes no realized path current.
Where zero extension and path subdivision are both defined, their current
inclusions commute and `(M1-5)` gives the same adjoint restriction.

**Quotient.**  Tree/pendant coboundaries remain in the Gate-4 kernel.  Since
the subdivision creates no cycle rank, it neither creates nor deletes a
record-visible cycle class.

**DoR-008.**  At the bottom object `P_nn=id` and `(M1-9)` is the exact Q-408
finite bilocal map.  Every restriction is equality, not convergence.

```text
REF_PATH_PHYSICAL_CURRENT_SQUARE = DERIVED / TYPE-P
REF_PATH_Q408_KERNEL_SQUARE = DERIVED_ON_RANK_PRESERVING_IMAGE / TYPE-P
SQUARE = DERIVED
```

### 2.5 New exact Route-A boundary

`Ref_path` is strictly smaller than `Ref_PL`.  An interior/barycentric PL
edge that is not a subinterval of an existing `gamma_e` has no physical path
from `(M1-1)`.  A cycle-creating refinement is not in the isomorphism scope
of `(M1-6)`.  Nothing above supplies its coframe, positive cell volume,
density, Riesz comparison, Hodge star, or response-detail action.

```text
ROUTE_A_BUILT:
  exact finite Q408 current and kernel;
  Ref_path child paths by parameter restriction;
  P_nm=id;
  current square (M1-5);
  rank-preserving bilocal square (M1-9).

ROUTE_A_NEW_FIRST_STOP:
  NEW_INTERIOR_PL_EDGE_CELL_PHYSICAL_REALIZATION_AND_GEOMETRY / TYPE-U.
```

The P4 declaration no longer needs to author child paths, the test-form map,
the current pullback, or the rank-preserving kernel square on `Ref_path`.
It still must supply genuinely new/interior edges and cells, cycle-creating
directions, coframe/density/Riesz/Hodge data, uniform actual-kernel estimates,
completed localization/Maxwell structure, physical readers, and response
detail.  All later Route-A stops from V003 remain downstream of this exact
boundary.

---

## 3. M2 — Route B bound to one declared package member

### 3.1 Why existential H_020 is removed from execution

The generic theorem remains valid:

```text
for every P in ContAdm_020, c_P:C(P) -> T(P).      (M2-1)
```

But `exists P:C(P)` supplies no name at which to evaluate `T`.  V004
therefore replaces the executable tag `[H_020;P]` by one bound pair:

```text
[P_CM020,c_CM020],
c_CM020:C(P_CM020).                                (M2-2)
```

`P_CM020` means **the carrier-minimal continuum-package member**.  It is a
single proposed physical instantiation, not the whole family and not an
existential variable.  Its declaration and certificate are the Route-B
ratification item.  They have no authority before DoR-020.

### 3.2 Carrier-minimal binding rule

The binding uses this target-blind dependency order:

```text
physical refinement/realization
 -> actual-kernel topology and faithful quotient
 -> Ward/contact/Hodge/Maxwell decomposition
 -> normalized reader pullback
 -> completed representative and anchored K action
 -> response/boundary naturality.                 (M2-3)
```

At every affine choice, `min` means the unique least DoR-019/R4-transported
carrier norm member of the closed nonempty constraint set.  At every ideal
or quotient choice, `min` means the smallest closed invariant object
generated by the required finite data.  Existence, closedness, uniqueness,
and J1--J15 compatibility are clauses of `c_CM020`; failure of any one voids
the bound member before an end test.

The named manifest is:

| Field | `P_CM020` binding | Carrier-structural reason |
|---|---|---|
| P1 | least-norm natural representative in `Adm_base` subject to both restriction cubes | deletes no finite datum and adds the smallest completed displacement |
| P2 | least-norm normalized relation-annihilating physical reader induced by `(F-J2)`; coordinates named `(chi_CM,T_CM)` | Riesz minimality after the Maxwell line is built; no output coefficient used |
| P3 | `h_CM=0` at every `K`, with the independently anchored P1 representative | unique zero-norm anchored addition; no return-map consequence used |
| P4 | derived `Ref_path` square plus the least-norm natural physical extension satisfying the full new-edge/cell, coframe, density, and common-refinement constraints | extends the derived boundary without choosing a target geometry |
| P5 | completion of the actual Q-408 physical-kernel image in the coarsest Hausdorff complete topology generated by the required local seminorm orbit | no reader, response, or threshold graph norm |
| P6 | quotient by the actual completed physical-kernel null space, with the finite record-visible intersection theorem | smallest lawful deletion; nondegeneracy by construction |
| P7 | smallest closed invariant contact ideal generated by actual boundary discrepancies; `beta_CM` is the resulting cocycle; response is the least-norm solution of the independent action/restriction squares | no blanket off-diagonal deletion and no desired response used |
| P8 | least-norm Ward/contact/Hodge/Maxwell member normalized on `L_T`, built before P2 is pulled back | prevents circular `pi_Mx` construction |

The least-norm clauses are simultaneous constraints on the **natural
family objects**, not objectwise selections later patched together.  The
admitted automorphisms act isometrically and preserve their constraint sets;
uniqueness therefore gives covariance.  Restriction and batching are in the
constraint set before minimization, so the bound object cannot acquire them
afterward by selection.

All coordinates remain symbolic:

```text
chi_CM = ell_CM(Rhat_K);
T_CM=T_CM^*;
nu remains independent;
(r_0,r_ch) remain symbolic;
no ratio, scale, frame, orientation, filtration, or result is selected.
```

### 3.3 Binding choice table

| Binding alternative | What it adds | Void condition | V004 status |
|---|---|---|---|
| `B-CM` carrier-minimal `P_CM020` | the manifest above and `c_CM020` | nonempty/closed constraint failure, nonunique minimum, failed J row, target-aware norm | **live proposed binding** |
| `B-EX` separately enumerated explicit member | every map and certificate written directly | any unnamed member or output-selected coefficient | alternative, not carried |
| `B-FAM` full family plus rigidity theorem | a proof `N_020(P)=N_020(Q)` for all certified P,Q | member-sensitive output or absent theorem | unavailable; rigidity not derived |
| `B-REJ` reject Route B | no continuum declaration | none | live alternative |

The choice of `B-CM` is itself gate-grade and is not adopted here.  If
DoR-020 ratifies Route B, the pre-gate family collapses to `P_CM020` for this
model.  A different future member would be a different package proposal,
not an unnoticed replacement.

```text
ROUTE_B_BOUND_MEMBER = P_CM020
ROUTE_B_BOUND_CERTIFICATE = c_CM020
ROUTE_B_EXECUTION_TAG = [P_CM020,c_CM020]
ROUTE_B_FREE_P = none
ROUTE_B_EXISTENTIAL_H020_EXECUTION = removed
```

### 3.4 Rigidity/uniformity attempt

The desired family theorem would be

```text
RIGID_020:
  for all P,Q in ContAdm_020 with C(P),C(Q),
  N_020(P)=N_020(Q).                               (M2-4)
```

The sweep does not derive `(M2-4)`:

1. Q-396 supplies distinct admissible completed representatives with the
   same active finite shadows; the package constraints preserve rather than
   identify that fiber.
2. Q-407 supplies the relation-aware `(chi_K,T)` reader coordinates; J2
   identifies `ell`, `p_loc`, and the physical pullback for one member but
   does not equate different compatible readers.
3. P3 admits distinct finite-flat anchored K actions.  J5 fixes their anchor
   and restrictions, not their completed off-section values.
4. DoR-017 requires member-sensitive tags through the stationary/Schur
   chain.  No later decision file removes those tags.
5. The known finite restrictions are identical on these directions and
   therefore cannot prove a completed-output equality.

This is enough to show that rigidity is **not derivable from the present
premises**.  It does not evaluate two final numbers and does not assert that
every pair differs.

```text
RIGIDITY = NOT_DERIVED
MISSING_RIGIDITY_CERTIFICATE = family_uniformity_of_N_020_over_ContAdm_020
FAMILY_LEVEL_END_TEST = not_licensed
```

### 3.5 Honest end-test semantics for the bound member

| Future event | Licensed conclusion |
|---|---|
| `P_CM020` fails a structural or finite certificate | `P_CM020` and bound Route B are void before a number |
| authorized sealed consequence of certified `P_CM020` disagrees when opened | `P_CM020` and the ratified bound Route-B model are refuted |
| it agrees | that bound model survives this end test; no other member is established |
| some other admissible P disagrees | that P is refuted, not `P_CM020` and not the whole pre-gate family |
| every admissible P is proved to disagree | the physical family is refuted |
| a future rigidity theorem gives one prediction and it disagrees | the inhabited family is refuted |
| no bound member has been ratified/evaluated | no end test has occurred |

The unbound V003 Cases 2 and 3 and its unconditional package-wide end-test
sentence are deleted.  Failure to produce a member is not proof that the
fiber is empty.  Apparent agreement without an exhibited, certified bound
member is not constructive vindication.

```text
END_TEST_BOUND_TO = P_CM020
END_TEST_EXECUTED = false
FAMILY_REFUTATION_CLAIMED = false
```

---

## 4. M3 — regressions, battery, falsifiers, and anti-tuning

### 4.1 Four hostile regressions re-exercised

| Regression | Route A | Bound Route B | Verdict |
|---|---|---|---|
| abstract-kernel substitution | `(M1-8)` is the actual Q-408 path-bilocal distribution | P4/P5 must extend the actual map; `c_CM020` rejects an abstract `UHU*` stand-in | **PASS** |
| circular `pi_Mx` | localization/Maxwell remains downstream TYPE-U | P8 is constructed and normalized before P2 is its pullback | **PASS** |
| false reader nonemptiness | remains TYPE-U after the corrected square | existence and uniqueness of `ell_CM` are explicit failure-capable rows of `c_CM020`, not inferred from Q-407 | **PASS** |
| Hodge from isometry | `(M1-6)` is used only for the current/kernel square, never Hodge | P4/P8 must certify the actual mass-weighted Hodge comparison | **PASS** |

The one-edge Hodge witness remains:

```text
Delta_2 S(1)=(1,1) != (2,2)=S Delta_1(1).         (REG-1)
```

It does not contradict `(M1-9)`; it prevents promoting that kernel square
to a Hodge square.

### 4.2 New permanent regressions

```text
REG_PATH_SQUARE:
  on every rank-preserving subdivision of an existing path,
  P_nm=id, A_n=S_nm^*A_m, and (M1-9) must hold.

REG_NEW_EDGE_SCOPE:
  no edge outside the image of existing path restriction receives a path,
  density, coframe, Riesz, or Hodge datum from Ref_path.

REG_BOUND_MEMBER:
  every executable Route-B expression contains [P_CM020,c_CM020];
  a free P or existential H_020 is a failure.

REG_SINGLETON_REFUTATION:
  failure of P!=P_CM020 cannot refute P_CM020 or every admissible member.
```

### 4.3 Hostile battery

| Attack | Result |
|---|---|
| target tuning | **PASS** — Ref_path is derived before Route B; `P_CM020` minimizes carrier data only |
| hidden binding normalization | **PASS** — least-norm is after R4 unit transport; `nu` and ranks never enter the score |
| posterior member selection | **PASS** — ratification binds `P_CM020` before any execution |
| family-by-existential evasion | **PASS** — H_020 cannot execute a branch |
| singleton-refutation fallacy | **PASS** — end-test table is member-scoped |
| abstract physical kernel | **PASS** — actual Q-408 codomain required |
| circular reader/projection | **PASS** — P8 precedes P2 pullback |
| false reader nonemptiness | **PASS** — certificate is gate-grade and failure-capable |
| Hodge from coefficient isometry | **PASS** — `(REG-1)` retained |
| smooth-target import | **PASS** — new-edge P4 rule must remain record-side |
| cycle-creating overclaim | **PASS** — derived square explicitly rank-preserving |
| graph-norm shortcut | **PASS** — P5 uses the orbit of actual local seminorms, not a desired response norm |
| contact deletion | **PASS** — smallest ideal plus finite intersection theorem required |
| DoR-008 | **PASS WITH SCOPE** — Route A exact on Ref_path; Route B conditional on `c_CM020` |
| hidden cross-clause compensation | **PASS** — independent falsifiers and R4-only conversion remain |

### 4.4 Falsifiers

Route A is falsified by any failure of `(M1-2)`, `(M1-5)`, `(M1-6)`, or
`(M1-9)` on its stated subcategory.  Direct substitution proves all four.
It makes no claim outside that subcategory.

Bound Route B is void if `P_CM020` lacks a unique carrier-minimal member,
any J1--J15 row, an actual physical Q-408 restriction, an admitted-family
covariance certificate, or the independent P8-to-P2 order.  After lawful
authorization, the sealed-number end test tests `P_CM020`; it is not run in
this artifact.

### 4.5 Anti-tuning ledger

```text
1  Freeze V003's clauses, J2/J5/J13, Q-408, and DoR-008 targets.
2  Derive Ref_path by parameter restriction and line-integral additivity.
3  Prove the current/Riesz/kernel squares before restating Route A's stop.
4  Define the carrier-minimal rule and its alternatives without a response,
   threshold, fixed point, sealed value, or measurement.
5  Bind the name P_CM020 and certificate c_CM020.
6  Attempt family rigidity from J1--J15; record NOT_DERIVED.
7  Scope the end test to the bound member.
8  Install regressions and only then state downstream licensing.
9  Compute no number and compare nothing.
```

---

## 5. M4 — neutral route comparison

| Dimension | Route A: physical construction | Route B: bound declaration |
|---|---|---|
| newly closed | rank-preserving existing-path physical square | free-P/existential ambiguity |
| current boundary | new/interior PL edge-cell physical geometry | ratification of `P_CM020,c_CM020` |
| present license | exact finite and Ref_path structural results | none before DoR-020 |
| post-ratification license | unconditional results only after remaining physical objects build | execution only on `P_CM020` with member tags |
| cost | construct new-edge geometry, estimates, localization, reader, response detail | declare the carrier-minimal tuple and its existence/uniqueness certificates |
| main risk | later physical incompatibility or empty reader equalizer | carrier-minimal fiber empty/nonunique or wrong bound physics |
| end-test scope | any actually constructed physical witness | the bound member only; no family rigidity |
| recommendation | none | none |

```text
ROUTE_RECOMMENDED = none
ROUTE_SELECTED = none
```

---

## 6. Delta table versus V003

| V004 location | Change from V003 | Status |
|---|---|---|
| lead and preflight | Q-417 custody; square/bound-member result | required |
| frozen P1--P8 core | retained in substance | unchanged |
| J2/J5/J13 | reproduced exactly in substance | unchanged |
| Route A | `Ref_path` physical current/Riesz/kernel square derived | M1 repair |
| Route A first stop | moved to genuinely new/interior PL edges/cells and their geometry | M1 repair |
| P4 declaration burden | existing-path child paths, `P_nm`, current and rank-preserving kernel squares removed | shrink by derivation |
| Route B | existential `H_020` execution replaced by `[P_CM020,c_CM020]` | M2 repair |
| Route B choice table | carrier-minimal binding plus explicit alternatives and voids | M2 repair |
| rigidity | attempted; `NOT_DERIVED` with exact missing theorem | M2 repair |
| end-test cases | bound-member semantics replace family overclaim | M2 repair |
| regressions | four hostile checks rerun; four new scope/binding checks added | M3 |
| battery/falsifiers | rerun on derived square and bound member | M3 |
| comparison/board | updated neutrally; final-check readiness | M4 |

All V003 symbol separations, clause alternatives, unit rules, conditional
C1--C3 composition theorem, DoR-008 obligations, standing finite
regressions, and target-blind consequence map remain in force.  The killed
F_PLDEC witness remains withdrawn.

---

## 7. Consumer and licensing statement

If Route A continues, C1--C3 may consume `(M1-9)` immediately on `Ref_path`;
they remain TYPE-U on new/interior PL geometry and completion.

If DoR-020 ratifies bound Route B, downstream work consumes only

```text
[P_CM020,c_CM020]
```

and retains that tag through the completed symbol, R6/R7/R8, return theorem,
fixed-point theorem, sensitivity calculation, and future protected
evaluation.  A member-sensitive output may not drop the tag.  No other
`P in ContAdm_020` may be substituted without a new gate ruling.  None of
those downstream calculations is performed here.

---

## 8. Final DoR-020 board

```text
CONTINUUM_PACKAGE_V004 = FINAL_CHECK_CANDIDATE
STANDING = PROPOSED_NOT_ADOPTED

SQUARE = DERIVED
REF_PATH_CURRENT_SQUARE = TYPE-P
REF_PATH_Q408_KERNEL_SQUARE = TYPE-P_ON_RANK_PRESERVING_IMAGE

ROUTE_A_BOUNDARY =
  NEW_INTERIOR_PL_EDGE_CELL_PHYSICAL_REALIZATION_AND_GEOMETRY / TYPE-U
ROUTE_A_WITNESS_BUILT = false

ROUTE_B = BOUND(P_CM020)
ROUTE_B_CERTIFICATE = c_CM020
ROUTE_B_BINDING = PROPOSED_NOT_ADOPTED / GATE_GRADE
ROUTE_B_FREE_P = none
ROUTE_B_H020_EXECUTION = removed

RIGIDITY = NOT_DERIVED
FAMILY_LEVEL_REFUTATION = not_licensed
BOUND_MEMBER_REFUTATION = licensed_after_ratification_and_authorization

J2_READER_EQUALITY = RETAINED
J5_ACTION_ANCHOR = RETAINED
J13_BOUNDARY_COCYCLE = RETAINED
PL_REFINEMENT_CORE = RETAINED

ABSTRACT_KERNEL_REGRESSION = PASS
CIRCULAR_PI_REGRESSION = PASS
READER_NONEMPTY_REGRESSION = PASS
HODGE_REFINEMENT_REGRESSION = PASS
PATH_SQUARE_REGRESSION = PASS
SINGLETON_REFUTATION_REGRESSION = PASS

ROUTE_SELECTED = none
RECOMMENDATION = none
READY_FOR_FINAL_CHECK = yes

p_symbolic = true
nu_symbolic = true
chi_CM_symbolic = true
T_CM_symbolic = true
rank_or_ratio_selected = none
orientation_frame_filtration_selected = none
numeric_evaluation = false

alpha_computed = false
proof_authorized = false
kappa_record_computed = false
FENCE_BLOCKED_STRUCTURAL_RESULT = false
```
